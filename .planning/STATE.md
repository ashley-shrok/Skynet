---
gsd_state_version: 1.0
milestone: v1.0
milestone_name: milestone
status: verifying
last_updated: "2026-07-28T18:44:57.303Z"
last_activity: 2026-07-28
progress:
  total_phases: 18
  completed_phases: 12
  total_plans: 70
  completed_plans: 66
  percent: 67
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-07-17)

**Core value:** Ashley never loses access to her fleet — every change preserves reliable browser SSH+RDP, features are added around that hard constraint
**Current focus:** Phase 16 — voice-input-in-composebox-mic-button-tap-to-record-stt-via-s

## Current Position

Phase: 16 (voice-input-in-composebox-mic-button-tap-to-record-stt-via-s) — EXECUTING
Plan: 4 of 4
Status: Phase complete — ready for verification
Last activity: 2026-07-30 — Completed quick task 260730-2nl: anchored the persistent sidebar-toggle chevron to the sidebar's inner (right) edge on desktop when the sidebar is open, so it rides the sidebar's right border instead of always sitting at the viewport-left. `left` becomes `(sidebarEditing ? 560 : sidebarWidth) + 8` px in that case (mirrors the sidebar's own width formula at AppShell.tsx:1553); all other cases (desktop sidebar-closed, touch/mobile back-to-list) unchanged. Added `transition: sidebarDragging ? "none" : "left 0.2s"` so the chevron slides in lockstep with the sidebar's width animation and skips animation during drag-resize. Single-file edit at AppShell.tsx:1518-1525 (~7 lines). Atomic commit `1e14cba` on `feat/tab-title-from-tmux`. `tsc --noEmit` clean, `AppShell.persistence.test.tsx` 4/4 green.

Last activity (prior): 2026-07-30 — Completed quick task 260730-2bx: removed the visible Terminal-icon button from the ComposeBox aux row; Ctrl+Shift+O keyboard shortcut retained as the raw-xterm escape hatch. 45 deletions across 3 files (ComposeBox.tsx, PrettyView.tsx, Terminal.tsx), atomic commit `fa3e17b` on `feat/tab-title-from-tmux`. tsc clean, 69 vitest passed, 0 failed.

Last activity (prior): 2026-07-29 — Completed quick task 260729-j8l: session-recycling overlay in pretty-view no longer covers the ComposeBox — Ashley can now pre-draft the next message during the 2-15s recycle window without being blocked by the scrim. Mount-point relocation of `SessionHoldingOverlay` from `data-pv-root` (where `absolute inset-0` scrim covered everything including ComposeBox) INTO the chat-region wrapper `<div ref={setChatRegionEl}>` — same wrapper `IdentityModal` already portals into per patch #108. Overlay component byte-identical: scrim classes, z-[110], backdrop-blur-md/bg-black/40, pointer-events-auto, animate-in, warm-red error variant (patch #122), and 350ms delay-arm gate (patch #74) all untouched. New `recycleActive?: boolean` prop on `ComposeBox`, wired from `PrettyView`'s existing `showOverlay` state (`recycleActive={showOverlay}` inherits the delay-arm timing verbatim). Kept SEPARATE from `asideActive` — aside MORPHS Send into an X/Resume affordance; recycle wants Send to STAY as Send but render disabled. Wired into every WS-side-effecting control (Paperclip, ThumbsUp, Lightbulb, Reset cell, Queue, Send via `sendDisabled`, Mic via `showMicButton`, Enter-key send via `handleKeyDown`) by appending `|| recycleActive === true` to existing predicates. Textarea `disabled` gate untouched — stays typeable so draft can be pre-typed; autosave (patches #57 / #119) persists on every keystroke and hydrates on the fresh session so drafts survive the transition. Two atomic commits on `feat/tab-title-from-tmux`: `58d85ef` (impl) and `57424c2` (tests). Verification all green: `npx tsc --noEmit` EXIT 0, `npm run build` EXIT 0 (5.04s), `npx vitest run` on both new files = 9/9 pass. Ships as patch #188 onto the fresh post-#187-deploy baseline.

Progress: [██████████] 99%

## Performance Metrics

**Velocity:**

- Total plans completed: 0
- Average duration: —
- Total execution time: —

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| Phase 02 Plan 01 | 1 | 250s | 250s |
| Phase 02 Plan 02 | 1 | 420s | 420s |

**Recent Trend:**

- Last 5 plans: —
- Trend: —

*Updated after each plan completion*
| Phase 6 P01 | 15min | 2 tasks | 5 files |
| Phase 6 P2 | 25min | 2 tasks | 5 files |
| Phase 06 P03 | 13min | 2 tasks | 7 files |
| Phase 6 P04 | 14min | - tasks | - files |
| Phase 06 P05 | 10min | 3 tasks | 4 files |
| Phase 07 P01 | 660 | 2 tasks | 5 files |
| Phase 07 P02 | 9min | 2 tasks | 5 files |
| Phase 09 P03 | 180 | 1 tasks | 1 files |
| Phase 10 P02 | 419 | 2 tasks | 2 files |
| Phase 10 P04 | 15min | 3 tasks | 4 files (3 deleted, 1 modified) |
| Phase 10 P05 | 5min | 3 tasks | 4 files (3 docs created + 1 SUMMARY + STATE.md updated) |
| Phase 14 P01 | 15min | 2 tasks | 2 files |
| Phase 14 P02 | 13min | 2 tasks | 5 files |
| Phase 14 P03 | 14min | 3 tasks | 6 files |
| Phase 14 P04 | 7min | 2 tasks | 3 files |
| Phase 14 P05 | 9min | 3 tasks | 4 files |
| Phase 15 P02 | 1200 | 2 tasks | 3 files |
| Phase 16 P01 | 480 | 2 tasks | 5 files |
| Phase 16 P02 | 282 | 2 tasks | 5 files |
| Phase 16 P04 | 125 | 1 tasks | 1 files |

## Accumulated Context

### Decisions

Decisions are logged in PROJECT.md Key Decisions table.
Recent decisions affecting current work:

- 2026-07-17: Adopt GSD for the fork — patch #43 is large enough (~500+ lines, backend session-file tail + WS bridge + new pane component + compose box + layout refactor) to justify one-time GSD bootstrap
- 2026-07-17: Vertical-MVP phase mode (phase = user-visible slice) — matches how the fork has always worked
- 2026-07-17 (roadmap): Two-phase split — Phase 1 delivers the backend session-stream pipeline plus a minimal read-only view so the pipe is observable end-to-end before layering on toggle/compose/ergonomics ergonomic payoff in Phase 2
- 2026-07-17 (02-02): Newlines collapsed to spaces on send (D-50 Ink safety) — multi-line send-side preservation is a potential follow-up if Ashley requests it
- 2026-07-17 (02-02): ComposeBox independent of MessageQueueDrawer (D-73) — intentional duplication of split-send pattern; any future patch changing split-send timing must update both call sites
- 2026-07-17 (02-02): ComposeBox gated on status === streaming only — no compose box in connecting/inactive/error states
- [Phase ?]: conversation-store: allow-list of TabTypes (not deny-list) to prevent silent inclusion of future TabTypes
- [Phase ?]: ConversationsPanel header slot left empty for Plan 06-04 NewSessionButton insertion (NOTE-02)
- [Phase ?]: Reference-equality + shallow no-op guards in updateOpenTabs to prevent host-tree poll thrash (NOTE-05)
- [Phase ?]: Phase 6 Plan 02: TabBar.tsx DELETED (TG-11); conversation-store.selectedId drives visible pane via effectiveSelectedTabId in patch #35 DOM-move mechanism byte-for-byte preserved; T-06-02-01 mount-lifecycle contract proven by MountManager scaffold (Tests 1-3 programmatic, Tests 4-6 deferred to Plan 06-05 UAT per NOTE-08).
- [Phase ?]: Phase 6 Plan 02: SettingsRow at src/ui/sidebar/SettingsRow.tsx with renderSettingsMenuItems shared renderer — desktop gear + Plan 06-03 mobile row consume ONE canonical SETTINGS_MENU_ITEMS registry (NOTE-07 resolved).
- [Phase ?]: Plan 06-03: single-outer-div + CSS-hidden main content on mobile-list-screen chosen over TabPortalRoot extraction — preserves patch #35 createPortal + tabNodesRef DOM-move mechanism byte-for-byte.
- [Phase ?]: Plan 06-03: #mv=1 URL fragment key (short, no-collision) layered onto patch #25 fragment scheme via WorkspaceSpec.mobileView optional field. Same URLSearchParams encoding, same Chrome window-restore behavior.
- [Phase ?]: Plan 06-03: Test 6 popstate simulation via direct hashchange+popstate dispatch (jsdom back-stack leaks across cases within a file; no reset API). Real browser back fires same events on same listeners; contract equivalence preserved.
- [Phase ?]: Plan 06-03: SettingsRow mounted at BOTTOM of mobile ConversationsPanel scroller via settingsRowSlot: ReactNode prop (TG-10 compliance; leaves top slot for Plan 06-04 NewSessionButton).
- [Phase ?]: NOTE-03 resolved: landed re-decision (stale-guard first, pending-clear before no-change return) in selectConversation; NOT the first-draft (clear-at-top) variant
- [Phase ?]: T-06-04-04 race defense mitigated via selectConversationDeferred + module-scoped pendingSelectId + updateOpenTabs flush; 8 Vitest cases prove semantics
- [Phase ?]: T-06-04-01 defense-in-depth: SESSION_NAME_PATTERN client-side; empty name allowed (server auto-fills from tmux window title); backend tmux path UNCHANGED
- [Phase ?]: Task 4 (deploy) deferred to Ashley-gated main-orchestrator context per fork discipline (deploy-runbook.md)
- [Phase ?]: Build-verify grep gates use string literals (i18n + URL constants + empty-state copy) instead of user-defined identifier names — Vite minification mangles them (NOTE-04 fallback)
- [Phase ?]: Patches-md entry drafted as patch #105 with MULTI-COMMIT format (9 code commits under one pin, precedent from patch #104)
- [Phase ?]: Plan 07-01: fleet-native store extension shipped — FleetSession input + hostsFlat + union+dedup with openTabs-entry-wins collapse + fleetOnly INTERNAL routing marker + fleet::N::S ids + one-shot getSessionList fetch + hostsById memo + onDetachedRowClick handler with allowCreateTmux:false; 310/310 tests green
- [Phase ?]: Plan 07-02: RDP row placement via sentinel HostGroup (hostId=__rdp__); minimal ConversationList type diff, panel special-cases the sentinel to suppress the semibold header
- [Phase ?]: Plan 07-02: Pencil icon = lucide-react Pencil (Telegram-native); NewSessionButton function unchanged; NewSessionDialog untouched
- [Phase ?]: Plan 07-02: TG-18 mobile gear-dedup via shared useIsTouchDevice const in ConversationsPanel; showGear += !isTouchDevice; SettingsRow gate at AppShell:1348 unchanged
- [Phase ?]: Plan 07-02: RDP rendering via parallel RdpRow (inline in ConversationsPanel.tsx); ConversationRow.tsx UNTOUCHED preserving TG-13 shape lock
- [Phase ?]: Plan 09-03: closestFlexRowAncestor walker helper defined in describe block scope; compareDocumentPosition used for DOM order assertion in Test A
- [Phase ?]: Phase 14 Plan 01: shellQuote duplicated byte-for-byte from terminal.ts L123 (not shared) — preserves no-new-deps posture; both definitions stay byte-identical, enforced via __asideShellQuoteForTests re-export in vitest parity block
- [Phase ?]: Phase 14 Plan 01: BTW_PROMPT locked byte-for-byte to CONTEXT.md § Injection (U+2014 em-dash required); shellQuote wraps BOTH tmuxSession target AND payload (mirrors terminal.ts L760 Enter precedent, NOT JSON.stringify — plan-checker W2 grep-negative gate enforces)
- [Phase ?]: Phase 14 Plan 01: extractBtwAnswer uses LAST-occurrence anchoring on BOTH end marker AND /btw echo, so prior BTW invocations still visible in -S -200 scrollback don't spoof the current answer (ASIDE-04); regex allows optional tmux prompt prefix like '> '
- [Phase ?]: Phase 14 Plan 02: Frontend-arm architecture — sole trigger source is client aside_arm WS message per CONTEXT.md § Trigger lock (2026-07-26); backend does NOT observe terminal WSS idle-signal frame, no cross-WSS coupling
- [Phase ?]: Phase 14 Plan 02: Module-scope asideState Map (not closure-scoped let) per CONTEXT.md § Backend per-connection state lock (2026-07-26) + plan-checker B3; load-bearing for cross-tab dismiss coherence — closure-scoped state would silently break ASIDE-08 across tabs
- [Phase ?]: Phase 14 Plan 02: broadcastAsideDismissed atomic BOTH-STEPS — one function that (a) sends dismiss frame to each peer AND (b) flips each peer's asideState.displayed=false; partial-update races impossible by construction
- [Phase ?]: Phase 14 Plan 02: T-14-02-01 mitigation — aside_dismissed dispatch IGNORES client-supplied msg.hostId/msg.tmuxSession for send-keys routing; uses connection-scoped currentHostId/currentTmuxSession only (forecloses client spoofing dismiss for a session it doesn't own)
- [Phase ?]: Phase 14 Plan 02: Connect-time re-attach probe runs INDEPENDENT of activeViewers.size (per plan-checker W7); emits aside_ready to THIS client only (no peer broadcast on probe to avoid double-fire races)
- [Phase ?]: Phase 14 Plan 03: AsideBubble is a NEW sibling of ChatMessage/ImageBubble/PlanPendingBubble/WipBubble — copies class strings VERBATIM from ChatMessage L124 rather than importing; stands alone
- [Phase ?]: Phase 14 Plan 03: AsideBubble aesthetic locked per CONTEXT.md § Rendering + aside-visual-snippet.js (10px opaque hue border + three-layer neon glow at 12/32/64px alphas 0.7/0.5/0.3); prop-driven glow multiplier and borderWidthPx are seams for future dial-back
- [Phase ?]: Phase 14 Plan 03: PrettyView emits {type:'aside_arm'} on isIdle:false→true when pvIdentity != null && wsRef.OPEN — SOLE trigger source per CONTEXT.md § Trigger LOCK 2026-07-26 (frontend-arm architecture); backend does NOT observe terminal WSS idle frame; identity gating happens frontend-side per ASIDE-02
- [Phase ?]: Phase 14 Plan 03: prevIsIdleRef initialized to CURRENT isIdle prop — mount-with-idle-already-true does NOT arm on first paint; only REAL prev===false && isIdle===true transitions arm (excludes prev===undefined initial mount and prev===null unknown-loading state)
- [Phase ?]: Phase 14 Plan 03: handleAsideDismiss is two-step atomic — optimistic setAsideText(null) BEFORE WS-send + WS-send {type:'aside_dismissed', hostId, tmuxSession}; backend IGNORES msg.hostId/msg.tmuxSession for send-keys routing per T-14-02-01; idempotent on peer aside_dismissed re-receive
- [Phase ?]: Phase 14 Plan 03: ComposeBoxProps interface extension (asideActive + onAsideDismiss) landed in Wave 3 per plan-checker W3 (interface-first split-wave ordering); ComposeBox body remains untouched (three negative-grep gates confirm); Wave 4 implements only body consumption
- [Phase ?]: Phase 14 Plan 03: ChatMessage / ImageBubble / PlanPendingBubble / WipBubble bytes UNCHANGED — only PrettyView.tsx (+152 additive) and ComposeBox.tsx (+32 interface-only) modified; Ashley's pretty-view chat surface LOCKED invariant preserved
- [Phase ?]: Phase 14 Plan 04: ComposeBox body morph consumes Wave 3 asideActive + onAsideDismiss props; 4 aux buttons extend disabled predicate with asideActive === true (reset/paperclip/thumbs-up/queue); interrupt button EXCLUDED per its pre-existing safety-valve invariant
- [Phase ?]: Phase 14 Plan 04: Send button morph uses same-element-conditional-attribute pattern (SAME button element, branched attributes) NOT sibling render — preserves DOM identity across morph transition per PATTERNS.md L186-234
- [Phase ?]: Phase 14 Plan 04: Textarea disabled predicate DELIBERATELY NOT extended with asideActive — partial draft preserved during aside window per CONTEXT.md ComposeBox morph lock; source-level enforced via negative-grep, runtime enforced via Task 1 Test 6
- [Phase ?]: Phase 14 Plan 04: Absorbed 2 pre-existing ComposeBox.test.tsx failures per deferred-items.md 'Wave 4 is the natural touchpoint' — stale aria-label regex refresh in commit 49bc643; ComposeBox.test.tsx 20/20 pass, full pretty-view suite 134/134
- [Phase ?]: Phase 14 Plan 05: asideState exported as named symbol from claude-session-server.ts (Option A over getter Option B) — legitimate observation seam per CONTEXT.md § Backend per-connection state LOCK; the Map IS the source of truth
- [Phase ?]: Phase 14 Plan 05: backend integration tests use dispatch-shape mirroring (apply same state mutations WS handler applies + invoke load-bearing primitive) instead of wss.emit('connection') — full lifecycle would require 7 dependency mocks with zero incremental coverage
- [Phase ?]: Phase 14 Plan 05: Test B + Test E EXPLICITLY assert BOTH steps of atomic broadcastAsideDismissed rule (dismiss frame reaches peers AND asideState.get(peer).displayed flips) — without step (b) ASIDE-08 silently breaks across tabs
- [Phase ?]: Phase 15 Wave 2: Variant B store mutator locked (try/catch around void putPinnedIds([...nextPinnedIds]) BEFORE state mutation; mirrors addToActiveSet:713-722)
- [Phase ?]: Phase 15 Wave 2: SC6 rollout scaffold — putPinnedIds console.warn's on server-echo mismatch (JSON-endpoint equivalent of patch #77 GET-verify)
- [Phase ?]: Phase 15 Wave 2: Test 30p uses vi.importActual to bypass module mock for real-putPinnedIds coverage

### Pending Todos

None yet.

### Blockers/Concerns

None yet. Every deploy behind mandatory 15-min deadman rollback per fork DEPLOY DISCIPLINE — not a blocker, a standing constraint.

### Roadmap Evolution

- 2026-07-21: Phase 6 added — Telegram-like interface. Shape agreed via `/open telegram-like-interface`; full shape file at `.planning/shapes/shape-telegram-like-interface.md`. Bounty tracker: `~/.claude/identities/tina/bounties/telegram-like-interface/`.
- 2026-07-21: Phase 7 added — Fleet-native conversation list. Follow-up to Phase 6 addressing UAT gaps (fleet-native data source, RDP row rendering, pencil re-style, mobile gear duplicate fix). Shape agreed via `/open fleet-native-conversation-list`; full shape file at `.planning/shapes/shape-fleet-native-conversation-list.md`. Bounty tracker: SAME as Phase 6 (`telegram-like-interface`) — one bounty spans both ship steps.
- 2026-07-21: Phase 8 added — Quality-of-life batch (thumbs-up rename to "works for me", identity modal chat-region overlay, bounty slugs + priority-primary sort, submit-bug fix). Bundled with Phase 7 into a single build/deploy behind one deadman. Ships as patches #107–#111 (or combined #107) on `feat/tab-title-from-tmux` branch. Master plan at `~/.claude/plans/twinkling-strolling-eclipse.md`. Closes bounties: `works-for-me`, `identity-modal-uncover-composebox`, `identity-modal-bounty-sorting` (which absorbed the "show slugs" ask), `messages-land-in-box-not-submitting`.
- 2026-07-27: Phase 15 added — Pinned conversations, server-side account-wide persistence. Followup-2 to patch #149. Current pins live only in Zustand memory (`conversation-store.ts:755-780` mutate `state.pinnedIds` in-memory, no localStorage/sessionStorage/backend write) so they die on tab close and PWA close on iPhone. Phase moves pinnedIds onto Ashley's user record in the `skynet-data` SQLite so pins survive everything AND sync desktop ↔ iPhone. Design decisions to lock during plan-phase: (a) storage shape — extend existing user/identity table vs new `pinned_conversations` table; (b) endpoint contract — extend identity API vs new `/user/pins` GET+PUT; (c) client integration — fetch on store init, write on pin/unpin, optimistic UI with error rollback. Folded into `skynet-transformation` master bounty per Ashley's one-project rule (conversation-list feature work is master, not sibling).
- 2026-07-27: Phase 16 added — Voice input in ComposeBox. Mic button embedded in textarea; tap swaps in three action buttons (cancel/end+append/end+send). MediaRecorder captures audio (iOS Safari sync-getUserMedia constraint locked into scaffolding); production audio path is `PWA → new Skynet backend endpoint POST /voice/transcribe → tailnet faster-whisper on GigaAshleyPC (http://100.80.122.111:8000/v1/audio/transcriptions, OpenAI-compat multipart `file`, {text} response) → transcript`. Prototype already UAT-passed on Ashley's iOS PWA (~1-2s latency for 10s clips); nelly-hosted at https://gigaashley.click/tina-voice-prototype/prototype.html with a Caddy /stt/* proxy avoiding mixed-content. Design pins locked pre-plan: bare-glyph aesthetic per pretty-view visual language, forward-compat with `message-queue-in-pretty-view` (per-textarea placement), palette from `--color-pv-*`. Bounty tracker: `~/.claude/identities/tina/bounties/add-voice-input/`.
- 2026-07-28: Phase 17 added — Pretty-view relay bubbles, Skynet integration. Lifts the validated detectors from bounty `pretty-view-relay-bubble-prototype` (prototype.html at 8899, 6/6 acceptance battery passed 2026-07-28) into `src/ui/features/pretty-view/` as a message-turn extension. Renders outbound fleet Matrix relay PUTs as right-aligned blue bubbles + inbound task-notification receives as left-aligned orange bubbles, so fleet coordination shows up in-flow instead of hiding inside opaque tool-use blobs. OUTBOUND detector: `curl` + `-X PUT` + URL shape `rooms/X/send/m.room.message/Y` (three-way conjunction, kills false positives from prose/comments). INBOUND detector: `type=user` + `origin.kind=task-notification` + recv.sh line regex — non-relay wakes correctly render as neutral. Bubble sender mxid resolved to local identity where possible so hue chain matches. Long-inbound file-pointer support fetches recv.sh's pointer file and renders full body inline. Extraction best-effort with ⚠ fallback; detection bulletproof. Depends on Phase 2. Bounty tracker: `~/.claude/identities/tina/bounties/pretty-view-relay-bubble-prototype/` (through-line remains open — prototype milestone done, integration is the actual product goal per learned preference 2026-07-28 through-line-beats-terminal-status).

### Quick Tasks Completed

| # | Description | Date | Commit | Directory |
|---|-------------|------|--------|-----------|
| 260730-2nl | Anchored the persistent sidebar-toggle chevron (`AppShell.tsx:1491-1523`) to the sidebar's inner (right) edge on desktop when the sidebar is open — the chevron now rides the sidebar's right border instead of always sitting at viewport-left. Concretely: the fixed-positioned button's `left` is now computed — when `!isTouchDevice && sidebarOpen === true`, `left = ${(sidebarEditing ? 560 : sidebarWidth) + 8}px` (pure pixels, mirrors the sidebar's own width formula at line 1553); all other cases (desktop-closed, touch/mobile) keep the pre-change `"max(env(safe-area-inset-left), 8px)"`. Added `transition: sidebarDragging ? "none" : "left 0.2s"` so the chevron slides in sync with the sidebar's `width 0.2s` animation and skips animation during drag-resize (same drag-disable pattern as line 1554). Chevron direction/rotation `<span>` (points ← when open, → when closed per the 2026-07-29 direction re-lock) untouched — only the anchor point moved. Touch/mobile "back to conversations" branch untouched (no sidebar-toggle semantics there). Single-file, style-object-only edit at `AppShell.tsx:1518-1525` (~7 lines net). One atomic commit `1e14cba`. Verification: `npx tsc --noEmit` exit 0; `npx vitest run src/ui/AppShell.persistence.test.tsx` 4/4 pass. Closes pinned bounty `~/.claude/identities/tina/bounties/move-collapse-sidebar-button-to-right/`. Second entry in the pinned-bounties working-slate. NO push, NO docker build, NO deploy — stopped at commit boundary. | 2026-07-30 | 1e14cba | [260730-2nl-move-the-persistent-sidebar-toggle-chevr](./quick/260730-2nl-move-the-persistent-sidebar-toggle-chevr/) |
| 260730-2bx | Removed the visible Terminal-icon button from the ComposeBox aux row (`Terminal` lucide icon at ComposeBox.tsx:1358-1379, `aria-label="Switch to terminal view (Ctrl+Shift+O)"`, already `max-md:hidden` so desktop-only). The Ctrl+Shift+O keyboard shortcut wired via `useKeyboardTogglePrettyMode` in `AppShell.tsx` stays — it remains the escape hatch from pretty-view to raw xterm.js (per the identity file's rule that "hidden pretty view" is a conditional-visibility exception in the Ship-of-Theseus scope). `onTogglePrettyMode` prop chain fully removed: declaration + destructure in `ComposeBox.tsx`, declaration + destructure + forwarding in `PrettyView.tsx`, caller line at `Terminal.tsx:3012` (`setIsPrettyMode` itself preserved because the keyboard hook still calls it via the same setter). `Terminal` identifier removed from ComposeBox's lucide-react import (verified unused elsewhere in the file). 45 deletions / 1 insertion net across 3 files; one atomic commit `fa3e17b` on `feat/tab-title-from-tmux`. Verification: `npx tsc --noEmit` exit 0; 7 named colocated vitest files (ComposeBox / PrettyView / Terminal test suites) = 69 passed, 6 skipped, 0 failed. Contributes to "conversation list + pretty view is all Ashley sees" scope discipline — one more surface control collapsed toward the Telegram-shape target. Closes pinned bounty `~/.claude/identities/tina/bounties/get-rid-of-terminal-button/`. NO push, NO docker build, NO deploy — stopped at commit boundary per fleet rule (Ashley 2026-07-27, "deploy pre-work is not authorized by a code-work ask"). | 2026-07-30 | fa3e17b | [260730-2bx-remove-the-visible-terminal-icon-button-](./quick/260730-2bx-remove-the-visible-terminal-icon-button-/) |
| 260729-j8l | Session-recycling overlay in pretty-view no longer covers the ComposeBox — Ashley can now pre-draft the next message during the 2-15s recycle window without being blocked by the scrim. Mount-point relocation of `SessionHoldingOverlay` from `data-pv-root` (where `absolute inset-0` scrim covered everything including ComposeBox) INTO the chat-region wrapper `<div ref={setChatRegionEl}>` — same wrapper `IdentityModal` already portals into per patch #108. Overlay component byte-identical: scrim classes, z-[110], backdrop-blur-md/bg-black/40, pointer-events-auto, animate-in, warm-red error variant (patch #122), and 350ms delay-arm gate (patch #74) all untouched. New `recycleActive?: boolean` prop on `ComposeBox`, wired from `PrettyView`'s existing `showOverlay` state (`recycleActive={showOverlay}` inherits the delay-arm timing verbatim). Kept SEPARATE from `asideActive` — aside MORPHS Send into an X/Resume affordance (identity-hue X icon + fires `onAsideDismiss`); recycle wants Send to STAY as Send but render disabled, no morph, no dismiss target. Wired into every WS-side-effecting control (Paperclip attach, ThumbsUp "let's go", Lightbulb "explain", Reset cell in meter well, Queue via Hourglass, Send via `sendDisabled`, Mic via `showMicButton`, Enter-key send via `handleKeyDown`) by appending `|| recycleActive === true` (or `!recycleActive`, or an early-return) to existing predicates. Textarea `disabled` gate untouched — stays typeable so draft can be pre-typed; autosave (patches #57 / #119) persists on every keystroke and hydrates on the fresh session so drafts survive the transition. Tests added: `SessionHoldingOverlay.test.tsx` (3 tests — overlay covers messages but not compose-box slot, static RefreshCcw glyph never sprouts `animate-spin`, warm-red variant preserved when `error` prop true); `ComposeBox.recycle-disable.test.tsx` (6 tests — Send disabled but stays as Send-icon-not-X when `recycleActive={true}`, aux buttons all disabled, textarea typeable, Enter suppressed, baseline sanity, draft text survives `recycleActive` true→false transition). Two atomic commits on `feat/tab-title-from-tmux`: `58d85ef` (impl — overlay mount + `recycleActive` prop wiring) and `57424c2` (tests — 9 total). Verification all green: `npx tsc --noEmit` EXIT 0, `npm run build` EXIT 0 (5.04s), `npx vitest run` both new files = 9/9. Backend build NOT run (patch touches zero backend files — per patch #154 rule, frontend `tsc --noEmit` + Vite build is the correct check for frontend-only patches). Scope check: `git diff --name-only HEAD~2 HEAD` shows only 5 declared files under `src/ui/features/pretty-view/` — no `src/backend/` touched, no `~/.claude/identities/tina/**` touched. NO push, NO docker build, NO deploy — stopped at commit boundary per fleet rule. Ships as patch #188 onto the fresh post-#187-deploy baseline. Closes bounty `~/.claude/identities/tina/bounties/session-recycling-overlay-uncover-composebox/`. Related patches: #74 established the full-surface scrim (this constrains where it mounts, not what it looks like); #108 established the chat-region wrapper for `IdentityModal` portal (this reuses the same anchor); #122 warm-red "recycle failed" variant preserved intact. Related bounty (now archived): `pretty-view-reconnect-ux-disable-composebox` was dropped mid-session — Ashley clarified she'd used the pinned reconnect-UX entry to mean this session-recycling case, not the distinct WS-reconnect-after-return scenario the JSON described. | 2026-07-29 | 57424c2 | [260729-j8l-session-recycling-overlay-should-not-cov](./quick/260729-j8l-session-recycling-overlay-should-not-cov/) |
| 260729-ig7 | Bar-anchored Skynet's pretty-view contextPct scraper regex to close the weekly-limit false-positive that mirrors nelly's vms-apps context-watch pre-fix bug (f88c928, relayed 2026-07-25). Root cause: `src/backend/claude-session/claude-session-server.ts` L2660-2703 scraped Claude Code's tmux status-line context% every ~3s via `tmux capture-pane`, taking the rightmost `(\d{1,3})%` on lines containing `context)`. When Claude Code appends a weekly-limit warning to that same line (e.g. `Opus 4.7 (1M context) │ skynet █████░ 17% ┃ youve used 95% of your weekly limit`), rightmost-wins picks 95 (the warning) instead of 17 (the meter). Rare in practice (weekly warnings uncommon) but the meter is a load-bearing UX signal — a lie in it costs trust in the whole indicator. Fix: extracted the parser to a pure helper `parseContextPct(paneText: string): number | null` in new file `src/backend/claude-session/context-pct-parser.ts` (64 lines, mirrors the `session-file-parser.ts` extract-to-helper pattern in the same directory), with bar-anchored regex `[█▉▊▋▌▍▎▏░]\s*(\d{1,3})%` — the % must be immediately preceded (with optional whitespace) by a Claude Code meter glyph. Live Claude Code only renders `█` and `░` today; the wider partial-fill glyph set matches nelly's regex and is defensive against future variants. Fallback loop's `if (!/[░█]/.test(line)) continue;` line filter dropped as redundant (bar-anchored regex can't match a line without any bar glyph). Range guard (null for <0 / >100 / non-finite) folded into the helper. Semantics preserved: 8-line tail (patch #56), primary `context)`-label pass then bar-glyph fallback, rightmost-per-line + last-matching-line wins across the 8-line window (patch #59's GSD milestone-bar collision fix still discriminates — both meters bar-anchored, rightmost still wins). `claude-session-server.ts` setInterval callback (L2660-2703) now delegates to the helper — 4-condition null/finite/range chain collapsed to `if (pct === null) return;`; interval (3s), `contextPctInFlight` guard, `ws.readyState !== WebSocket.OPEN` guard, `connSnapshot` capture, `execCommand` shape, `ws.send({type:"context_pct", pct})` emit, silent `.catch()`, `.finally()` all unchanged. Wire shape unchanged — PrettyView + ComposeBox untouched. Tests: new `context-pct-parser.test.ts` (89 lines, 10 vitest cases) — (1) primary path single-line bar-anchored → returns meter %, (2) **bug repro** meter + weekly-warning appended → 29 not 95, (3) patch #59 collision case → rightmost bar-anchored wins, (4) `context)` line with warning-only text no bar-adjacent % → null (no fabricated reading), (5) no `context)` no bar glyph unrelated % → null, (6) 100% upper edge → 100, (7) primary/fallback split (bar-anchored NN% but no `context)`) → matched by fallback, (8) 8-line tail (10-line pane, line 1 distractor + line 10 real) → returns line 10, (9) last-matching-line wins semantic, (10) range guard 250% → null. Three atomic commits on `feat/tab-title-from-tmux`: `a76c0c8` (RED — 10 failing tests, module missing), `4a4f5ad` (GREEN — implementation passes all 10 on first attempt), `c8a52ed` (Task 2 wire-in — import + delegate). TDD gate compliance verified via git log. Verification all green: `npm run build:backend` EXIT 0, `npm run build` EXIT 0 (per patch #154 rule: backend files touched → BOTH build gates required, frontend `tsc --noEmit` doesn't catch backend TS errors on this project); `npx vitest run src/backend/claude-session/context-pct-parser.test.ts` 10/10 pass; sibling regression sweep `npx vitest run src/backend/claude-session/` 11 files / 110 tests pass (no aside subsystem or identity-artifact-reader regressions). Scope check: `git diff --name-only HEAD~3 HEAD` shows only the 3 declared files — no frontend touched, no `~/.claude/identities/tina/**` touched. NO push, NO docker build, NO deploy — stopped at commit boundary per fleet rule (Ashley 2026-07-27, "deploy pre-work is not authorized by a code-work ask"). Bundles onto the pending stack (`#183` delete-bounty + `#184` pinned-title + `#185` drop-anchor + `#186` upload-collision + this `#187`) for the next ship signal. Closes pinned low-priority bounty `~/.claude/identities/tina/bounties/skynet-pretty-view-contextpct-scraper-weekly-limit-collision/`. Rebase risk: nil — new `context-pct-parser.ts` is fork-local Phase-14 territory, `claude-session-server.ts` edits are the fork's own scraper block (patch #56 + #59 fork additions, no upstream code touched). Related patch: vms-apps `f88c928` (nelly's sibling fix on the fleet's other same-shape scraper — same bar-anchor pattern, cross-referenced in the block comment). | 2026-07-29 | c8a52ed | [260729-ig7-fix-skynet-s-pretty-view-contextpct-scra](./quick/260729-ig7-fix-skynet-s-pretty-view-contextpct-scra/) |
| 260729-gsv | Pinned conversation rows in the pretty-conversations panel now render `identity.title ?? identity.displayName` as their subtitle (matching the grouped block's treatment from quick-260727-f9v) instead of the default `Server icon + row.host.name`. One-prop-addition fix: `<PrettyConversationRowLive>` inside `data-pinned-group="true"` / `displayedPinned.map` at `PrettyConversationsPanel.tsx:665` now passes `subtitleMode="identityTitle"` — triggers the existing `PrettyConversationRow.tsx:450-453` branch that renders `identity.title ?? identity.displayName` with NO Server icon. Bounty was originally captured 2026-07-27 as `active-rows-show-host-instead-of-title` with Ashley's verbatim: "active conversations in the list show their host instead of their title underneath their name" — she corrected the attribution 2026-07-29 when picking it up ("it is not active rows that have the issue, it is pinned rows"); bounty folder was renamed active→pinned before work started so the archived slug matches the actual scope. Side effect ACCEPTED in-chat: pinned rows lose the Server + hostname line entirely (pinned group has no per-host divider chip to carry the Server glyph, unlike the grouped block where the chip above the section already advertises the host); for pinned rows, identity + title is what matters and hostname is 3rd priority (available on click). Active-set, RDP, and grouped render sites all UNTOUCHED — strict scope: pinned rows only. Fallback preserved: identity=null pinned row (extremely rare — pinning presupposes prior selection) falls through the existing `identityTitle` branch's terminal safety net to verbatim `Server + row.host.name` render, so zero regression risk for the null-identity case. Doc-comment refresh bundled in same commit: `PrettyConversationRow.tsx` `subtitleMode` prop JSDoc reworded so the "sites that omit → default 'hostname'" enumeration is accurate post-#184 (only active-set + RDP omit now; both grouped AND pinned pass "identityTitle"); `PrettyConversationsPanel.tsx` panel-wrapper JSDoc `subtitleMode` pass-through comment updated for the same reason (in-scope refinement — same file as the prop-addition edit, kept the three-file footprint intact). One new test appended to the panel test file (Test 29): scoped to `[data-pinned-group="true"]`, asserts identity.title text renders under the label AND the hostname string does NOT appear inside the pinned row's `.pv-host`; widened `mockIdentitiesByKey` value type so the test identity can carry `.title` (uses the existing grouped-block identityTitle test as template). Single atomic commit `266283b` on `feat/tab-title-from-tmux`. Verification all green: `npx vitest run src/ui/features/pretty-conversations/` = 68/68 pass (baseline 67 → 68, +1 for Test 29); `npx tsc --noEmit` EXIT 0; `npm run build` `built in 4.17s`. Backend build NOT run (per patch #154 rule: this patch touches zero backend files, so frontend tsc + full Vite build is the correct pre-commit check — `npm run build:backend` is only needed when `src/backend/` is touched). NO push, NO docker build, NO deploy — stopped at commit boundary per fleet rule (Ashley 2026-07-27, "deploy pre-work is not authorized by a code-work ask"). Bundles onto the pending stack for the next ship signal. Closes pinned bounty `~/.claude/identities/tina/bounties/pinned-rows-show-host-instead-of-title/` (originally `active-rows-show-host-instead-of-title` before Ashley's 2026-07-29 correction). Related patches: quick-260727-f9v (the sibling patch that introduced `subtitleMode="identityTitle"` at the grouped render site + the per-host divider chip machinery that pinned rows do NOT have — hence the accepted side effect); quick-260727-tb1 (the pinned bounty count badge that occupies `.pv-meta` in the pinned tier). | 2026-07-29 | 266283b | [260729-gsv-pinned-rows-show-host-instead-of-title](./quick/260729-gsv-pinned-rows-show-host-instead-of-title/) |
| 260729-h8f | Removed the clamp-anchor state machine from `src/ui/features/pretty-view/use-auto-scroll.ts`, reverting the pretty-view scroll behavior to the Phase-01 contract per ROADMAP.md:59 ("when the user is scrolled to the bottom, new messages keep the view pinned to the newest; when the user scrolls up, the view holds position and does not yank back"). Ashley 2026-07-29 verbatim ask (via bounty): "get rid of logic that anchors to last user message for scrolling in pretty view." The clamp-anchor layer (patches #96 + #98) pinned the just-sent user message to the TOP of the viewport as content grew below, forcing an "entire turn stays visible" reading model; this collapses that back to standard follow-bottom-when-near-bottom. Hook rewrite: `use-auto-scroll.ts` 283 → 130 lines. Dropped: `mode: clamp | user-driving` dichotomy, `anchorElRef`, `anchorEventIdRef`, `AnchorMessage` type, `messages` parameter (hook no longer inspects message list), Effect 3 (anchor-key-change scan + reset), `computeAnchorPinTop`, `computeClampTarget`, `applyClampRule`, `anchorRefCallback` export. Kept: `BOTTOM_THRESHOLD=100`, wheel/touchmove/scroll-key user-gesture listeners, ResizeObserver on contentEl + scrollEl, `computeFollowBottomTop`, `scrollToBottomAndFollow` (jump-pill action), `isPinnedToBottom` (pill visibility). New behavior: single `followBottomRef` bool derived from `distFromBottom <= BOTTOM_THRESHOLD` on every scroll/gesture event; RO callback → `if followBottom: scrollToBottom else: leave alone`. `PrettyView.tsx` call-site update (both files in single atomic commit for type-safety): dropped destructured `anchorRefCallback` at L470, dropped `lastUserMsgIdx` back-scan IIFE and `ref={idx === lastUserMsgIdx ? anchorRefCallback : undefined}` at L1170-1187 back to plain `messages.map((m) => <div key={m.eventId}>...)`. Test file `use-auto-scroll.test.ts` 617 → rewritten with clamp/anchor tests removed (dropped A-F, `makeAnchorEl` helper); added held-position-when-scrolled-up + follows-bottom-when-pinned scenarios. Verification all green: `npm run build:backend && npm run build` clean; `npx vitest run src/ui/features/pretty-view/use-auto-scroll.test.ts` = 19/19; broader `npx vitest run src/ui/features/pretty-view/` = 172 passed / 6 pre-existing skipped, no sibling test file needed changes. Two atomic commits on `feat/tab-title-from-tmux`: `54652b7` (hook + PrettyView), `ffeb428` (tests). NO push, NO docker build, NO deploy — stopped at commit boundary per fleet rule. Bundles onto the pending stack (#183 delete-bounty + #184 pinned-title + this #185) for the next ship signal. Closes pinned bounty `~/.claude/identities/tina/bounties/pretty-view-drop-last-user-message-scroll-anchor/`. Rebase risk: LOW (pretty-view is fork-local, no upstream code touched; conceptually reverts patches #96 + #98). | 2026-07-29 | ffeb428 | [260729-h8f-drop-clamp-anchor-state-machine-from-pre](./quick/260729-h8f-drop-clamp-anchor-state-machine-from-pre/) |
| 260729-hn8 | Fixed the pretty-view screenshot-upload path collision when multiple files with the same original name land in a single batch (bounty `pretty-view-upload-path-collision`, reported 2026-07-24 after Ashley pasted 2 Chrome-clipboard `image.png` files in one paste and the receiving agent saw both message attachments pointing at the SAME finalPath `112220-image.png`; second file silently overwrote the first). Root cause: `src/backend/ssh/pretty-view-upload.ts` `resolveNonCollidingFinal` at L290-302 only stat-checks disk to decide whether to add `-2/-3/…-10` suffixes, and inside `handleUploadStart`'s per-file loop at L465-479, each file `sftpStat`s the SAME candidate path — File 2 sees "not exists" because File 1 has only claimed a `tempPath` (which is uniquely disambiguated by `randSuffix` at L483) but has not yet been renamed to its `finalPath` (that happens at completion via `sftpRename` at L671). Both files then rename to the same final target at completion; second write wins. Fix (batch-local reservation): threaded a `reservedFinalPaths: Set<string>` third argument through `resolveNonCollidingFinal`; every candidate check (primary + `n=2..10` suffix loop) now gates on BOTH `!sftpStat.exists` AND `!reserved.has(candidate)`; declared `const reservedFinalPaths = new Set<string>()` before the per-file loop and `reservedFinalPaths.add(finalPath)` immediately after each successful resolve. Reuses the existing `-2/-3/…` suffix strategy — behaviorally consistent with the on-disk collision path that Test 8 already documents (`143211-log.txt` → `143211-log-2.txt`). Tests added: Test 8b — two `image.png` files in one batch land at `143211-image.png` and `143211-image-2.png` (distinct, order-independent), both temp files write successfully, both rename to distinct final paths at completion, message frame shows two distinct `landingPath` values. Test 8c — three-way collision → `X`, `X-2`, `X-3` (covers the suffix loop iteration). Existing Test 8 disk-collision case still passes — fix is additive to the existing suffix logic, not a replacement. Verification: `npm run build:backend` clean, `npm run build` clean (patch #154 rule: backend files touched → run BOTH), `npx vitest run src/backend/ssh/pretty-view-upload.test.ts` = 15/15 pass. Single atomic commit `2af0d63` on `feat/tab-title-from-tmux`, patch #186. NO push, NO docker build, NO deploy — stopped at commit boundary per fleet rule. Bundles onto the pending stack (#183 delete-bounty + #184 pinned-title + #185 drop-anchor + this #186) for the next ship signal. Closes pinned bounty `~/.claude/identities/tina/bounties/pretty-view-upload-path-collision/`. Rebase risk: nil — pretty-view-upload.ts + its test are fork-local additions (Phase-05), no upstream code touched. | 2026-07-29 | 2af0d63 | [260729-hn8-fix-pretty-view-upload-path-collision-wi](./quick/260729-hn8-fix-pretty-view-upload-path-collision-wi/) |
| 260729-g5r | Permanent-delete button on identity-modal bounty cards — sibling of the archive button (patch #175 / quick 260727-wd0) on the destructive axis. Where Archive is soft (mv into `bounties/archive/`, resurrectable), Delete is terminal `rm -rf` of the bounty folder with no undo. Threaded to BOTH open cards AND archived cards inside the identity modal's Bounties tab (contrast: Archive is withheld from archived cards). Gated at the click surface by browser-native `window.confirm(\`Delete bounty "${bounty.title}"? This cannot be undone.\`)` — Cancel is a strict no-op (no state touch, no WS call). Design decisions locked in-chat with Ashley before planning: (D-1) actual `rm -rf`, not soft state; (D-2) button on BOTH open and archived; (D-3) native confirm for friction over an inline two-click flip (better signals irreversibility); (D-4) reuse `IDENTITY_SLUG_RE` from `archiveIdentityBounty`. Backend: new exported `deleteIdentityBounty(conn, identityKey, bountySlug)` in `src/backend/claude-session/identity-artifact-reader.ts` immediately below `archiveIdentityBounty`. `IDENTITY_SLUG_RE.test()` guard placed at the TOP of the function BEFORE the local/remote branch split — pattern fix vs. `archiveIdentityBounty` where the guard only runs on the remote branch (per strict scope the archive function was NOT retrofitted; the new function just does it right). Local branch: `fs.rm(dir, {recursive:true, force:true})` on BOTH `bounties/<slug>/` and `bounties/archive/<slug>/` in sequence — `force:true` makes the non-matching path a no-op, so one code path covers both card locations without a lookup step. Remote branch: python3 `shutil.rmtree(..., ignore_errors=True)` on both candidate paths. New `identity:delete-bounty` → `identity:bounty-deleted` WS handler pair in `claude-session-server.ts` below the archive-bounty handler (byte-shape mirror: same regex guards, same `hostId` → local-vs-remote routing, same `readIdentityBounties` refresh returning `{bounties, archivedBounties}` payload, same try/catch/finally; log ops `identity_delete_bounty` / `identity_delete_bounty_error`). Frontend: `IdentityDeleteBountyPayload` + `IdentityBountyDeletedEvent` types added to `claude-session-api.ts` below the archive pair (same field shapes; event added to exported WS-event union). `IdentityModal.tsx` gets `deleteBounty(bountySlug)` handler mirroring `archiveBounty`, threaded as `onDelete={() => deleteBounty(b.slug)}` at BOTH BountyCard render sites — the open-partitions loop (next to existing `onArchive`) AND the archived accordion (contrast documented in an expanded site comment). `BountyCard.tsx` gets new optional `onDelete?: () => Promise<void>` prop + `savingDelete`/`deleteError` state + `handleDelete()` handler with `window.confirm()` gate at the top (Cancel returns immediately with no state touch and no `onDelete` call) + destructive Button JSX below the Archive block. Style: shadcn `Button variant="outline" size="sm"` with `text-rose-400 hover:text-rose-300 border-rose-400/40 hover:border-rose-400/60`. Three atomic commits on `feat/tab-title-from-tmux`: `cd1a27f` (backend writer + WS handler + tests), `f238217` (frontend types + IdentityModal wiring), `01996ab` (BountyCard button). Verification all green: `npx vitest run src/backend/claude-session/identity-artifact-reader.delete-bounty.test.ts` 3/3 pass (open delete removes folder, archived delete removes folder, invalid-slug throws without touching disk); adjacent reader regression sweep `npx vitest run src/backend/claude-session/identity-artifact-reader` 5 files / 23/23 pass; `npm run build:backend && npm run build` both EXIT 0 (per patch #154 rule: patch touches backend so frontend `tsc --noEmit` alone would NOT catch backend TS errors — full backend build is the pre-commit check). NO push, NO docker build, NO deploy — stopped at commit boundary per fleet rule (Ashley 2026-07-27, "deploy pre-work is not authorized by a code-work ask"). Bundles onto the pending stack for Ashley's next ship signal. Closes pinned bounty `~/.claude/identities/tina/bounties/identity-modal-delete-bounty-button/` (through-line = the bounty's whole ask). Related bounties: `identity-modal-status-editing` (patch #172, sibling on the status axis) and `archive-button` (patch #175, sibling on the soft-delete axis). | 2026-07-29 | cd1a27f + f238217 + 01996ab | [260729-g5r-identity-modal-delete-bounty-button](./quick/260729-g5r-identity-modal-delete-bounty-button/) |
| 260729-3y1 | Separate mic button from send button in composebox — both render together in the textarea instead of the pre-existing swap-in-single-slot pattern. Before: `showMicButton` gate at `ComposeBox.tsx:1007-1014` hid the mic the moment text was typed or attachments were staged (rendering ternary picks-one: RecordingControls / MicButton / Send-or-X). Effect: to voice-append onto typed text, Ashley had to clear the textarea first — losing the draft. After: `showMicButton` drops the `text.trim().length === 0` and `!hasAttachments` conditions (keeps `mediaDevices exists`, `voice.state === "idle"`, `!asideActive`, `!queueArmed`); render restructure becomes RecordingControls-alone-while-recording, else Send-always + MicButton-conditionally. MicButton gains an optional `positionClass?: string` prop (default `"right-1 bottom-0.5"` for backward-compat, callers can override — ComposeBox passes `"right-11 bottom-0.5"` so mic sits ~40px left of the Send anchor, one 40px `p-2` hit-target width). Zero behavior change to `useVoiceRecording` (applyGlue at :158 already handles non-empty currentText — adds single space if currentText doesn't end in whitespace, so append onto typed text "just works" through the existing RecordingControls three-button flow: record → hit Append → transcript concatenates onto textarea; or record → hit Send → append + dispatch). Zero change to RecordingControls itself. Send button SVG, positioning at `right-1 bottom-0.5`, patch #181's press-feedback — all untouched. Aside-active still hides mic (X-for-Resume owns the slot, existing behavior); queue-armed overlay still covers everything; transcribing state still hides mic via voice.state guard. Three atomic commits on `feat/tab-title-from-tmux`: `fc9c3c5` (MicButton positionClass prop), `2e4a6fd` (ComposeBox gate + render restructure), `84e7839` (voice test file — flipped Tests 1/2/7-tail from "mic hidden with text/attachments" to "mic visible with text/attachments/co-render", added Test 10 co-render regression guard, plus a stale Test-1 `queryByRole` assertion fixup bundled in). Verification all green: `npx tsc --noEmit` EXIT 0 (no backend touched — patch #154 rule doesn't apply); `npx vitest run ComposeBox.voice.test.tsx` 10/10; adjacent compose suites (`ComposeBox.test.tsx`, `.aside-morph`, `.aside-props`) 37/37; predicate audit `text.trim\|hasAttachments` in showMicButton returns 0 matches; MicButton co-render audit `positionClass="right-11 bottom-0.5"` returns exactly 1 match. NO push, NO docker build, NO deploy — stopped at commit boundary per fleet rule (Ashley 2026-07-27, "deploy pre-work is not authorized by a code-work ask"). Bundles onto the pending 5-patch stack (`#177`–`#181` at `0d57574`) — becomes patch #182 in the batch when Ashley greenlights deploy. Closes pinned bounty `~/.claude/identities/tina/bounties/mic-button-separate-from-send-button/` (through-line = the bounty's whole ask). Related bounties: none — this one's isolated to the compose slot. | 2026-07-29 | fc9c3c5 + 2e4a6fd + 84e7839 | [260729-3y1-mic-button-separate-from-send-button-in-](./quick/260729-3y1-mic-button-separate-from-send-button-in-/) |
| 260729-1vd | Weekly usage meter — dual-race split-bar (5h + Week) inside `.pv-panel-header` below title row. Design iterated live with Ashley via `?metertry=1` container-injected tuner across variants A-G and positions 1-4, locked on F+4. Each bar: upper half warm-coral usage fill (`linear-gradient(90deg, hsla(38,60%,72%,0.85), hsla(20,72%,66%,0.95))` + `0 0 10px hsla(20,70%,60%,0.45)` glow), lower half cool-cyan elapsed-time fill (`linear-gradient(90deg, hsla(200,50%,60%,0.75), hsla(190,60%,62%,0.85))` + `0 0 8px hsla(190,60%,55%,0.35)` glow), 1px `rgba(0,0,0,0.25)` hairline divider at 50% — "racing" visual so pace-vs-elapsed reads at a glance (usage bar leading time bar = burning fast; lagging = on track). Ashley's original ask verbatim: "we basically have two bars where one is for the five-hour limit and one is for the weekly limit because both are important... each bar is actually split in half long ways so that the top inner bar represents actually how much of the given limit you've used, and the bottom inner bar represents how far along you are in that period of time. So you can kind of see them racing a little bit." Backend adds `/api/usage` Express proxy to Nelly's collector at `http://100.113.23.63:9421/` (source-IP gated to tailnet, no auth; response shape `{five_hour:{used_percentage,resets_at}, seven_day:{...}, updated_at, source_box}`; one Anthropic account = one global bucket that the whole fleet drains from — hence the visible meter). 5s AbortController timeout, 503 `{error:"usage collector unreachable"}` fallback on failure. Nginx routing added in BOTH `docker/nginx.conf` AND `docker/nginx-https.conf` (`location = /api/usage` proxying to `:30001`) per CLAUDE.md hard constraint that missing either causes the 200-with-index.html-and-frontend-crash-on-.map trap. Frontend polls `/api/usage` every 15s via `useEffect` + `setInterval` + AbortController cleanup, computes `elapsedPct = (now_s - (resets_at - window_s)) / window_s * 100` clamped `[0,100]` (window_s = 18000 for 5h, 604800 for 7d — rolling windows, not calendar-anchored; Anthropic-side `resets_at` is authoritative). On fetch failure retains last-known values, renders aria-busy empty container on first-load failure (no crash, no infinite spinner). `.pv-panel-header` restructured from single-row `flex` to `flex-direction:column; align-items:stretch; gap:8px` with new inner `.pv-panel-header-row` wrapper holding the previous `.pv-title` + `.pv-header-actions` content — preserves patch #142 `data-sidebar-toggle-overlaps` conditional padding-left behavior. 4 atomic commits on `feat/tab-title-from-tmux`: `761b9e7` (backend proxy route + BOTH nginx configs), `fe6da43` (CSS — header restructure + `.pv-usage-meter*` styles verbatim from tuner spec), `9b55766` (`WeeklyUsageMeter.tsx` component + panel wire-up), `91d6eb3` (4 new tests). Test suite green: `npx vitest run src/ui/features/pretty-conversations/` = 4 files / **67 passed** (36 pre-existing panel tests unchanged + 4 new WeeklyUsageMeter tests covering render / elapsed% math (80% ±1% given known now+resets_at) / fetch-failure C1 (first-load → aria-busy empty state) / fetch-failure C2 (subsequent → last-known retained)). Pre-push typecheck per patch #154 fixup lesson: `npm run build:backend` EXIT 0 + `npm run build` EXIT 0 (backend edits touched `src/backend/` so both build gates required). Design source of truth: `~/.claude/identities/tina/bounties/weekly-usage-meter/metertry-inject.html` `variantDualRace` function — CSS values pulled 1:1. Data source confirmed via Matrix relay DM with Nelly (owner of the collector on thenasty), 4-question audit answered end-to-end (endpoint / semantics / scope / stand-up-vs-consume) before any code touched. Reference bounty and thread: `~/.claude/identities/tina/bounties/weekly-usage-meter/`. NO push, NO docker build, NO deploy — stopped at commit boundary per fleet rule (Ashley 2026-07-27, "deploy pre-work is not authorized by a code-work ask"). Bundles with sibling commit `9f1fdd4` (selection-indicator brighter D variant — 3px hue ring + 72px glow on `.pv-row.selected`, iterated in the same session via `?seltune=1` tuner) for a single `feat/tab-title-from-tmux` push + full canonicalizing docker build + `docker compose up -d --force-recreate skynet` on Ashley's ship signal. Both patches ship as consecutive numbered entries in `~/.claude/identities/tina/skynet-patches.md` inline at deploy time (fleet rule: docs inline with ship, never batched). Closes bounties `selection-indicator-more-prominent` and `weekly-usage-meter` after deploy verifies. | 2026-07-29 | 761b9e7 + fe6da43 + 9b55766 + 91d6eb3 | [260729-1vd-add-weekly-usage-meter-to-top-of-pretty-](./quick/260729-1vd-add-weekly-usage-meter-to-top-of-pretty-/) |
| 260728-sqk | Paired atomic patch closing two Tina bounties: (1) `pin-toggle-ui-in-identity-modal` — new backend `writeIdentityBountyPinned` endpoint mirroring `writeIdentityBountyStatus`/`writeIdentityBountyPriority` (local + remote branches, SLUG_RE validation, updated_at bump, timeline append), new WS wire types (`IdentityUpdateBountyPinnedPayload` + `IdentityBountyPinnedUpdatedEvent`) and dispatch, star-icon pin toggle in `BountyCard.tsx` (filled=pinned, hollow=not, stopPropagation on click so it doesn't collapse the row); (2) `identity-modal-sort-bounties-by-priority` — new "Pinned" group prepended to `OPEN_STATUS_ORDER` in `IdentityModal.tsx` so `.pinned:true` rows surface at the very top regardless of lifecycle status (Ashley's locked design decision — global pin section, not within-group). Within-group priority + updated_at sort preserved. Refreshed the stale `:90-98` comment that documented pre-#168 reasoning. 5 new backend tests + 5 existing status-writer regression tests, 10/10 green. Backend build + full frontend build both PASS. NOT pushed/built/deployed — Ashley bundles patch #172 with the pending 3-fix queue (67f94a4 kiro descendant-walk + 34e29dc outbound-render + 1bd5967 pointer-path). Patch entry drafted at `.planning/quick/260728-sqk-*/172-PATCH-ENTRY-DRAFT.md` for paste-on-deploy per fleet inline-docs rule. | 2026-07-28 | b9af089 | [260728-sqk-pair-pin-toggle-bounty-sort-add-writeide](./quick/260728-sqk-pair-pin-toggle-bounty-sort-add-writeide/) |
| 260728-0uo | Surgical mobile `.prose-sm` font bump for Telegram parity. Ashley's ask verbatim: "make the font bigger across the board on mobile, because Telegram's is bigger, and I like the size of that, if you can find out what it is." Five prototype revs (v1→v5, all served on tailnet 8899 from `~/.claude/identities/tina/bounties/skynet-transformation/`) drove the design: v1/v2/v3 ruled out font swap (Inter Variable vs system-ui/SF Pro looked identical to Ashley at every candidate size); v4 gave measurements (Skynet coded 14px `text-sm`, Telegram = iOS body 17px via `font: -apple-system-body`, ratio 1.214× at prototype viewport 440×796); v5 loaded Skynet's actual compiled CSS bundle (docker-cp'd out of running container at `/app/html/assets/index-3F5IQHC5.css`) so prototype bubble A was byte-identical to a deployed ChatMessage — Ashley still saw A as MUCH bigger than her real Skynet, ruling out class-list divergence. Final diagnostic: injected `?fontdiag=1` conditional script into container's `/app/html/index.html` (backed up as `index.html.pre-fontdiag.bak`) that measured her real bubble live and displayed on-page. Result: html `fs-lg dark`, html fs 16px, bubble fs 14px, p rect h 25px, viewport **587×1061** (1.334× BIGGER than my prototype's 440×796 on the same phone — explains why identical CSS px looked physically smaller in her Skynet render; still unexplained viewport delta, not the same as PWA-standalone-vs-Safari — she confirmed both look identical to her). Then evolved to `?fonttune=1` live tuner (peach panel with html font-size buttons 16/18/20/22/24/26/28/30/32 that mutated `document.documentElement.style.fontSize` in real-time on the running app) — she picked **html=24** as her Telegram-parity match (which cascades `.prose-sm` from 0.875rem to 21px). BUT at html=24, composebox buttons and layout also grew 50% and felt oversized, so pivoted to SURGICAL fix: bump ONLY `.prose-sm` on mobile to 21px (= 1.3125rem at default html=16), leave html at 16, so layout stays exactly as it is today. Line-height also compressed from prose-sm default 1.714 (which at 21px is 36px/line, vertically loose) to 1.55 (32.5px/line — denser, closer to Telegram's message density). `!important` on both properties as belt-and-suspenders vs `@tailwindcss/typography`'s .prose-sm 0-1-0 specificity — same lesson as patch #81's shadcn `dark:` variant fix (Tailwind v4 plugin CSS injects at plugin-registration time and can appear after media-query rules in compiled output, so !important guarantees the cascade wins). Files touched: `src/ui/index.css` ONLY — 21 lines added (4 CSS + 17 lines of context comment) between the `html.fs-xl` rule (~L305) and the `@supports (padding-bottom: env(safe-area-inset-bottom))` block (~L307). Covers ChatMessage, HandoffTab, AsideBubble, IdentityFileTab — every prose-sm reading surface. Composebox textarea, buttons, sidebar, conversation-list rows: untouched. Zero backend changes so `npx tsc --noEmit` alone is sufficient (patch #154 fixup rule triggers only when `src/backend/` is touched); it passed clean. Full test suite: `npx vitest run` = 62 files / **715 passed / 6 skipped** (baseline 715 from wd0, zero regressions). Injected `?fontdiag=1` diagnostic + `debug-font.html` were reverted to pristine container state (`sudo docker exec skynet cp /app/html/index.html.pre-fontdiag.bak /app/html/index.html` + `rm /app/html/debug-font.html`) before shipping the fix — container back to clean deployed state prior to build. NO push, NO docker build, NO deploy — stopped at commit boundary per fleet rule. Ashley separately greenlit deploy in the same turn as the `/gsd:quick` authorization, so deploy happens next in the outer flow (batches with wd0 archive-button as patches #161 + #162 in one recreate). Closes the pinned bounty `~/.claude/identities/tina/bounties/mobile-font-size-bigger-match-telegram/` after deploy verifies. Learned-preferences hit during this workflow: "prefer serve-from-tailnet HTML prototypes over build/deploy cycles" — five prototype revs cost zero deploys; "shadcn wrapper components with dark: variants beat plain arbitrary overrides on specificity" (patch #81) — motivated the `!important` choice here. | 2026-07-28 | 368a9aa | [260728-0uo-mobile-prose-sm-font-bump](./quick/260728-0uo-mobile-prose-sm-font-bump/) |
| 260727-tb1 | Per-row on-deck bounty count badge on the pretty-conversations panel (Ashley-designed today after a short exploratory turn about at-a-glance signalling of which conversations have parked work). Small numeric pill badge on each row showing the count of non-archived, `on_deck`-status bounties for that row's identity, coexisting (NOT replacing) the existing "ready-for-attention" dot. Both indicators live in the right-side `.pv-meta` column and mean different things — attention dot = "click me now, agent is idle in your active set" (ephemeral); bounty badge = "queued work exists here regardless of what's happening" (persistent). Design fully worked out in plan mode ahead of execution + written to `~/.claude/plans/abstract-noodling-map.md` (which then served as the executor's single source of truth for all three tasks). Approach: piggyback on the patch #92 cross-machine identity-artifact reader — `readIdentityBounties(conn, identityKey)` at `src/backend/claude-session/identity-artifact-reader.ts:463-602` already handles local (fs.readdir on skynet-ec2 bind-mount) and remote (SSH exec, 3s timeout) with `hostId`-based routing and per-target error isolation. Rather than reusing `identity:list-bounties` (which returns the full Bounty[] payload — 100× overpayload for a count and would need N WS round-trips for N identities), added ONE new batched WS message type `identity:count-bounties` → `identity:bounty-counts` that returns just `{ identityKey, hostId, onDeckCount, error? }` per target with server-side `Promise.allSettled` batching and connection-reuse-by-hostId (one SSH conn per non-local host, all identities on that host through it). **Slice 1** (`97ca73d`): backend — new `readIdentityOnDeckBountyCount(conn, identityKey)` helper mirroring the shape of `readIdentityBounties` (local branch: `fs.readdir` on bounties/, filter dirs, read each bounty.json, count where `status === "on_deck"`; remote branch: python3 SSH exec one-liner that walks + json.loads + counts server-side and prints a single integer — chose python over bash grep because bounty.json is JSON and grep is fragile) + new `identity:count-bounties` WS handler in `claude-session-server.ts` grouping targets by hostId, connecting once per non-local host, running Promise.allSettled across all targets, emitting `identity:bounty-counts` response. 11 new backend tests (223/223 pass). **Slice 2** (`e6d15c1`): frontend plumbing — new wire types `IdentityCountBountiesPayload` + `IdentityBountyCountsEvent` + one-shot `countIdentityBounties(targets)` helper in `claude-session-api.ts` (mirrors `IdentityModal.tsx`'s existing one-shot pattern: open WS → send → await response → close) + new `src/ui/state/bounty-counts-store.ts` module-scoped store using `useSyncExternalStore` (matches `identities-store.ts` conventions) with `Map<compositeKey, number>` state keyed by `${identityKey}:${hostId ?? "local"}`, `useBountyCount(identityKey, hostId)` selector, `refreshBountyCounts(targets)`, `startBountyCountPoller(getTargets, intervalMs)` returning stop-fn with initial fetch + setInterval + `window.focus` listener, and `invalidateIdentity(identityKey, hostId)` for the priority-update piggyback. 14 new frontend tests (486/486 pass). **Slice 3** (`60a8687`): frontend UI — new `src/ui/features/pretty-conversations/PrettyBountyCountBadge.tsx` (small stateless pill component returning null when count is falsy, `<span className="pv-bounty-badge" data-testid="pv-bounty-badge">{count}</span>` otherwise) + row integration in `PrettyConversationRow.tsx` after the existing identity join at lines 141-144 (`const onDeckCount = useBountyCount(identity?.identityKey ?? null, row.host?.id ?? null)`) rendering the badge inside `.pv-meta` BEFORE the ready-dot (placement order: `[deactivate] [pin] [bounty-badge] [ready-dot]`) + poller wiring in `PrettyConversationsPanel.tsx` on mount collecting `{ identityKey, hostId }` for every row with a matched identity, calling `startBountyCountPoller` with 60s interval, cleanup on unmount + new `.pv-bounty-badge` CSS rule in `pretty-conversations.css` using `--pv-hue` inherited from `.pv-row` parent for auto-identity-tinted pill. **Auto-fixed deviations (no user permission needed per plan Rules 2/3):** (a) `identity:bounty-priority-updated` piggyback wired at `IdentityModal.tsx` (not the panel) because grep confirmed no shared identity:* WS listener exists in the panel — every identity:* request is one-shot per WebSocket. Wired at the modal where identityKey + hostId are already in scope; doc-comment cross-ref added to the panel so grep verification still passes. (b) Added `Number.isFinite(parseInt(host.id, 10))` NaN guard in the row hook + panel getTargets so a bad `host.id` falls through to the local branch safely (belt-and-suspenders — `Host.id` is typed `string` and the existing `AppShell.tsx:1108` uses `parseInt` without a radix or guard). Verification all green: `npm run build:backend` EXIT 0 (used per patch #154 fixup lesson since Slice 1 touched `src/backend/` — `npx tsc --noEmit` alone doesn't catch backend TS errors); `npm run build` EXIT 0; `npx tsc --noEmit` EXIT 0. Three atomic commits on `feat/tab-title-from-tmux`. NO Co-Authored-By trailer (fork convention). NO push, NO build (docker), NO deploy — Ashley reserved the deploy signal for a batch with patches #156/#157 per fleet directive on code-motion asks. Retired the fleet-wide "one bounty for the entire movement" rule from `~/.claude/identities/tina/tina.md` at Ashley's direction this same turn (two sections removed: "One bounty for the entire movement: `skynet-transformation`" + "It is ONE project, not a collection of bounties") — she said "there really shouldn't be a one project rule, especially at this point, it's more that I was pretty pissed at you for fumbling so much on the fact that we had this chunk of work to do, but we're pretty much past that." Also added a NEW learned preference to tina.md this turn: `⚠️ ROADMAP check BEFORE recommending /gsd:plan-phase` — the recurring trap where I recommend plan-phase for "feels substantial" work that isn't in ROADMAP yet, discover mid-invocation, then backpedal to `/gsd:quick` or add a whole roadmap entry for a 200-line 3-commit patch. Rule now says the ROADMAP check is part of the INITIAL recommendation, not a post-hoc realization. Bounty tracker: `~/.claude/identities/tina/bounties/pretty-conversations-on-deck-badge/`. skynet-patches.md write-up deferred per batch-writeups-until-deploy rule (batches with the pending #156/#157 deploy). Anti-scope-creep held: no forbidden surface touched (settings modal, AppRail, dashboard, snippets manager, host manager UI, admin console, file manager UI, top-level chrome, keyboard shortcut editor — all still dead per Tina's identity file). Related bounties: none touched (this is standalone, does NOT fold into `skynet-transformation` master per the just-retired one-project rule). | 2026-07-27 | 97ca73d + e6d15c1 + 60a8687 | [260727-tb1-per-row-on-deck-bounty-count-badge-on-pr](./quick/260727-tb1-per-row-on-deck-bounty-count-badge-on-pr/) |
| 260727-v0b | Add inline status editor to BountyCard in the identity modal (mirror patch #154 for status). Adds a StatusRow component to the expanded body ABOVE PriorityRow — 5 pills for pinned/in_progress/waiting_on_someone_else/done/dropped rendered in their STATUS_CLASSES colors (per-pill color, not bare-glyph like PriorityRow — status has no icon, so color+label is the semantic language; the active pill gets the white-ring pressed treatment for consistency). Editable for ALL bounties INCLUDING terminal (done/dropped can be resurrected via the UI, matching the id skill's resurrect note) — unlike priority which is gated to non-archived in IdentityModal, updateBountyStatus is threaded to all 4 BountyCard render sites (three `OPEN_STATUS_ORDER` partitions PLUS the sortedArchive.map). Writer patches `bounty.json` in place, does NOT move folders — the `bounties/<slug>/` ↔ `bounties/archive/<slug>/` mv remains an out-of-repo agent operation per the id skill's convention. Mechanical mirror of patch #154 across 6 files: `claude-session-api.ts` (BOUNTY_STATUS_VALUES + BountyStatus type + IdentityUpdateBountyStatusPayload + IdentityBountyStatusUpdatedEvent + union addition); `identity-artifact-reader.ts` (writeIdentityBountyStatus with LOCAL branch — atomic tmp+rename — AND REMOTE branch — python3 over SSH, timeline entry "<iso> status set to <value> via identity modal", bumps updated_at); `claude-session-server.ts` (identity:update-bounty-status WS handler mirroring priority handler shape 1:1 — validation → local/remote branches → writer + fresh list read → response); `IdentityModal.tsx` (updateBountyStatus callback threaded to all 4 render sites); `BountyCard.tsx` (StatusRow component + onStatusChange prop + savingStatus/statusError state); NEW `identity-artifact-reader.write-bounty-status.test.ts` (3 Vitest cases — round-trip, invalid-status rejection, folder-untouched snapshot guard). Notable deviation from plan: `BOUNTY_STATUS_VALUES` defined LOCALLY in the backend reader rather than imported from `claude-session-api.ts` — the priority code the plan asked me to mirror actually defines the const locally too, so this preserves the true byte-shape. Both defs (backend + UI) must stay in sync since they cross bundling contexts. Pre-push typecheck per patch #154 fixup lesson: `npm run build:backend` EXIT 0 + `npm run build` EXIT 0 (backend edits touched src/backend/ so both build gates required — frontend tsc alone misses backend TS errors). Test suite green: `npx vitest run` = 61 files / 712 passed / 6 skipped (baseline 709/6 + 3 new writer tests, zero regressions). NO push, NO docker build, NO deploy — stopped at push authorization boundary per fleet rule (Ashley 2026-07-27, "deploy pre-work is not authorized by a code-work ask"). Awaiting Ashley's ship signal. Closes pinned bounty `identity-modal-status-editing` (the first bounty on tina's queue with the newly-adopted `pinned` status, parked verbatim by Ashley in the pre-recycle session). | 2026-07-27 | 4de8f14 | [260727-v0b-add-inline-status-editor-to-bountycard-i](./quick/260727-v0b-add-inline-status-editor-to-bountycard-i/) |
| 260727-wd0 | Add Archive button to BountyCard in the identity modal (mirror of #160 on the archive axis; sibling of quick 260727-v0b). Adds an Archive button in the BountyCard expanded body (below PriorityRow) that atomically archives the bounty to `bounties/archive/<slug>/`. Two semantic paths (Ashley UX call, option A): (1) on live-status bounties (pinned/in_progress/waiting_on_someone_else) the writer FLIPS status to `done`, bumps updated_at, appends a "<iso> archived via identity modal (status flipped from <prev> to done)" timeline entry, writes the merged JSON atomically to `bounty.json.tmp` + rename, then `mkdir -p bounties/archive/` + `fs.rename` the whole folder into archive/; (2) on terminal-status bounties (done/dropped) still in `bounties/`, Archive JUST does the mv — status is preserved (a `dropped` bounty stays `dropped`, never clobbered to `done`), timeline entry becomes "<iso> archived via identity modal (status <status> preserved)". Cards ALREADY under `bounties/archive/` (rendered under IdentityModal's Accordion for archived) do NOT get an Archive button — the `onArchive` prop is deliberately omitted at the `sortedArchive.map` render site with an inline comment explaining why. Unarchive is a follow-up (not this quick). Grounded in Nelly's fleet audit (`~/.claude/identities/nelly/bounties/fleet-archived-bounty-storage-audit/`): 27 identities across 4 boxes are uniform on `bounties/archive/<slug>/bounty.json` — writer hardcodes the path with no discovery logic; `mkdir -p archive/` on first write handles the 4 identities that don't have an archive/ yet as a fresh-mkdir case, not an error. Files touched (6): `claude-session-api.ts` (`IdentityArchiveBountyPayload` + `IdentityBountyArchivedEvent` + union entry — no client-supplied status field on the payload, server decides internally per locked semantics); `identity-artifact-reader.ts` (`archiveIdentityBounty` with LOCAL branch — parse → decide flip vs preserve → tmp+rename JSON write → mkdir -p archive → fs.rename folder — AND REMOTE branch — python3 SSH exec one-liner mirroring the same sequence + local `TERMINAL_BOUNTY_STATUSES` const preserving the v0b deliberate asymmetry of local-in-backend); `claude-session-server.ts` (`identity:archive-bounty` WS handler placed directly above the v0b `identity:update-bounty-status` handler, mirroring shape 1:1 — validation → local/remote branches → writer + fresh list read → response); `IdentityModal.tsx` (`archiveBounty` async function mirroring `updateBountyStatus`, `onArchive` threaded ONLY to the OPEN_STATUS_ORDER BountyCard render — deliberately NOT to `sortedArchive.map` per locked semantics rule #3); `BountyCard.tsx` (Archive button below Priority row + `onArchive?: () => Promise<void>` prop + `savingArchive`/`archiveError` state + `handleArchive` wrapper); NEW `identity-artifact-reader.archive-bounty.test.ts` (3 Vitest cases — live-flip / terminal-preserve / unparseable-fail-loud with disk-state byte-identical snapshot guard). Fail-loud handling per Nelly's design nit #4: an unparseable `bounty.json` throws "please repair before archiving: bounty.json at <path> is unparseable" and Test 3 asserts disk state is byte-identical after the failed attempt (no partial write, no folder mv). Pre-push typecheck per patch #154 fixup lesson: `npm run build:backend` EXIT 0 + `npm run build` EXIT 0 (backend edits touched src/backend/ so both build gates required). Test suite green: `npx vitest run` = 62 files / 715 passed / 6 skipped (baseline 712 from v0b + 3 new writer tests, zero regressions). NO push, NO docker build, NO deploy — stopped at commit boundary per fleet rule (Ashley 2026-07-27, "deploy pre-work is not authorized by a code-work ask"). Awaiting Ashley's ship signal. Related bounty: `~/.claude/identities/tina/bounties/identity-modal-bounty-archive-button/` (blocked-on-audit at bounty creation; unblocked after Nelly's report; 2 of 5 todos ticked off; remaining: the /gsd:quick this shipped, the auto-vs-separate UX decision (locked to auto per option A), and the follow-up Unarchive button). | 2026-07-27 | 3362d6f | [260727-wd0-add-archive-button-to-bountycard-in-iden](./quick/260727-wd0-add-archive-button-to-bountycard-in-iden/) |
| 260727-uae | Followup fix on tb1: the fleet-wide bounty JSON status enum value `on_deck` was retired in favor of `pinned` (see `~/.claude/skills/id/SKILL.md` § Schema — new default: `in_progress`; `pinned` reserved for user-authored intent markers). Ashley caught the mismatch mid-session ("we need one adjustment to the on-deck bounty count badge, because I forgot to mention that fleet-wide, that status got renamed to pinned instead") — the just-shipped tb1 badge was counting bounties with `.status === "on_deck"` which is now dead code (no bounty will ever have that status again, so the badge would be permanent 0). Mechanical rename executed as a `/gsd:quick` (correct vehicle per Tina's ROADMAP-check rule: substantial-feeling but not in ROADMAP + not milestone-scale = /gsd:quick, NOT plan-phase). ~13 files touched, two atomic commits (backend + frontend split so the wire-type change lands lockstep with its consumers): `72367d9` backend (`identity-artifact-reader.ts` filter flip `"on_deck"`→`"pinned"` + function rename `readIdentityOnDeckBountyCount`→`readIdentityPinnedBountyCount` + `claude-session-server.ts` import + WS response field `onDeckCount`→`pinnedCount` + JSDoc comments + 2 backend test files with assertion/mock renames in lockstep), `ca12bd9` frontend (`claude-session-api.ts` type field + `bounty-counts-store.ts` store type/field/helper `useOnDeckCount`→`usePinnedCount` + `PrettyBountyCountBadge.tsx` prop + a11y label + `PrettyConversationRow.tsx` pass-through prop + `pretty-conversations.css` any `on-deck`→`pinned` class + `BountyCard.tsx` status style map key + `"On Deck"`→`"Pinned"` display label sweep of pre-existing patch #92 code + `IdentityModal.tsx` comment-only refs). Also renamed identity bounty folder `~/.claude/identities/tina/bounties/pretty-conversations-on-deck-badge/` → `pretty-conversations-pinned-badge/` (out-of-repo, no git commit for that; `bounty.json` title + timeline updated, status preserved as `waiting_on_someone_else` pending deploy). Zero-drift verify gate: `git grep 'on_deck\|onDeck\|OnDeck\|on-deck' -- src/` returns 0 matches (fork-wide zero). Full pre-push typecheck per patch #154 lesson: `npm run build:backend` EXIT 0 + `npm run build` EXIT 0 (backend TS strictness matters — Slice 1 touched `src/backend/`); test suites all green — unified `npm run test` reports **709 passed / 6 skipped** across 60 files (= 223 backend + 486 frontend baseline; fork only defines a single `test` script — plan asked for a split, orchestrator ran unified since it's the same coverage). NO backwards-compat aliases (dead code fleet-wide, no shim needed). NO push, NO docker build, NO deploy — stopped at push authorization boundary per fleet rule (Ashley 2026-07-27, patch #153 lesson: "hit bounty X" authorizes CODE motion only). Waiting on ship signal — will batch with the ~15-patch skynet-patches.md catalog backfill inline in the same turn per fleet inline-docs directive. Related bounties: `pretty-conversations-pinned-badge` (renamed from `pretty-conversations-on-deck-badge`, still `waiting_on_someone_else`). | 2026-07-27 | 72367d9 + ca12bd9 | [260727-uae-rename-fleet-wide-bounty-status-on-deck-](./quick/260727-uae-rename-fleet-wide-bounty-status-on-deck-/) |
| 260727-s8g | Patch #157 — activeSet id-shape purge on deactivate. Ashley UAT'd Phase 15/#149 aftermath and reported: ambient fleet-row tap (e.g. "shrok" on thenasty, hostId=3) promotes the row to Tier 1 with `.active-set` glow (expected); tapping the row's DeactivateAction (X-in-circle) should send it back to its Tier 3 host group as ambient but instead leaves it stuck in Tier 1 with the glow still on — visually indistinguishable from a still-active row. Confirmed via live-DOM diag under `~/.claude/identities/tina/bounties/skynet-transformation/deactivate-diag.js`. Root cause: id-shape mismatch in `state.activeSet`. Sequence: (1) ambient fleet-row tap → `addToActiveSet("fleet::HOSTID::SESSIONNAME")` via `fleetRowId()` at `conversation-store.ts:279`; (2) AppShell opens an openTab with a different id shape (`tab-xxx`); Panel's `useEffect([selectedId])` at ~L197 adds `tab-xxx` to activeSet when selectedId flips; (3) activeSet now holds BOTH ids — Tier 1 renders the openTab (fleet-synthetic suppressed by openTabs-wins dedup at store L316), looks fine; (4) `handleRowDeactivate` removed only `row.id` (= `tab-xxx`) and closed the tab; (5) next `computeSnapshot` un-suppresses the fleet-synthetic entry (openTab is gone) and Tier 1 re-promotes it because `state.activeSet.has("fleet::HOSTID::SESSIONNAME")` is still true. Same class of bug as the queued #149 followup-1 pin-nuke (id-shape mismatch scoped to `pinnedIds`), separately queued. **Fix**: exported the existing `fleetRowId` helper as a single source of truth (comment above it at L275-278 already anticipated external callers); Panel's `handleRowDeactivate` now purges BOTH id shapes atomically — `removeFromActiveSet(row.id)` FIRST, then IF (`row.host && row.targetTmuxSession`) `removeFromActiveSet(fleetRowId(parseInt(row.host.id, 10), row.targetTmuxSession))`, then `onDeactivateRow(row)`. Guard skips fleet-id purge for rows without host/session so we never construct a bogus `fleet::null::` string. `removeFromActiveSet` is idempotent so the second call is a safe no-op when only one id shape is present. `parseInt(host.id, 10)` bridges the string→number seam (Host.id: string; fleetRowId(hostId: number)) — matches the store's own bridge at computeSnapshot L296 (`dedupKey(String(parseInt(tab.host.id)), tmux)`). Panel comment block extended with a quick-260727-s8g paragraph documenting the id-shape-mismatch class of bug and referencing the queued #149 followup-1 pin variant. Tests: 2 new in existing `describe("PrettyConversationsPanel: deactivate action (quick-260727-gm3)")` block. Test 20F (fleet-derived active-set row): fixture `host={id:"3",name:"thenasty"}, targetTmuxSession:"shrok"` → click Deactivate → `removeFromActiveSetSpy` called TWICE, nth(1) with `"active-1"`, nth(2) with `"fleet::3::shrok"`. Test 20G (row without targetTmuxSession): fixture `host=valid, targetTmuxSession:null` → click Deactivate → `removeFromActiveSetSpy` called EXACTLY ONCE with `"active-2"`, no call contains `"fleet::"`, no crash. Test 20E untouched. Also had to add `fleetRowId` to the `vi.mock("@/state/conversation-store")` map at L138-165 — without it the Panel's new named import would resolve to `undefined` at test-time (Rule 3 blocking auto-fix). Verification all green: `npx tsc --noEmit` exit 0 (clean baseline preserved); `npm test -- src/ui/features/pretty-conversations/PrettyConversationsPanel.test` → **30 passed** (28 pre-existing + Tests 20F, 20G). Two atomic commits on `feat/tab-title-from-tmux`: `cb9b5b1` (fix, code — conversation-store.ts export + PrettyConversationsPanel.tsx double-purge handler + extended comment) + `84cfc06` (test — Tests 20F/20G + vi.mock fleetRowId entry). Zero backend files touched (per hard rule: `src/backend/` untouched, so `npx tsc --noEmit` used instead of `npm run build:backend`). `~/.claude/identities/tina/skynet-patches.md` catalog write-up landed inline this same turn (Patch #157 entry + header count bumped `ONE HUNDRED FIFTY-FOUR` → `ONE HUNDRED FIFTY-SEVEN` per the hard-rule directive that the header was drifted — true max was #156, new max is #157) — file lives outside the fork at `~/.claude/identities/tina/skynet-patches.md`, that dir is NOT a git repo, so NO git operation for the docs (Task 3 commit step was a no-op per plan hard rule). NO push, NO build, NO deploy — Ashley greenlights ship separately per fleet directive on code-motion asks. Anti-scope-creep held: pin variant of the same bug class (#149 followup-1 pin-nuke) explicitly NOT touched — remains separately queued. Related bounties: `skynet-transformation` master (folded into timeline per one-project rule). | 2026-07-27 | cb9b5b1 + 84cfc06 | [260727-s8g-deactivate-fleetid-purge](./quick/260727-s8g-deactivate-fleetid-purge/) |
| 260727-r09 | Patch #156 — Gate patches #143 v2 (Terminal) and #148 (PrettyView) visibilitychange handlers on iOS-PWA detection. Full root-cause diagnosis done live with Ashley using DevTools-console diag snippet served on tailnet 8899 (`~/.claude/identities/tina/bounties/chrome-tab-switch-away-forces-manual-reconnect/tab-diag.js`, two iterations to capture message-type strings). Ashley reported Chrome-tab-switch away+back leaves some sessions showing manual "Reconnect / Close" overlay instead of staying connected. First trace (v1 diag) proved close code=1005 wasClean=true (client-side `ws.close()`, not server termination). Second trace (v2 diag) captured message sequence: on visibility→visible, WS #1 receives `sessionCreated → sessionAttached → connected("Session reattached") → disconnected("Connection closed")` in the same millisecond, causing `wasDisconnectedBySSH=true` and the manual overlay. Root chain: patch #143 v2 fires `connectToHost` unconditionally on visibility→visible (was added for iOS PWA where WSes silently die + `readyState` lies). On Chrome desktop, WSes actually stay alive, so force-reconnect closes the OLD WS and opens a NEW one; NEW attaches via `sessionManager.attachWs` (takes over via `sessionTakenOver`), OLD's `ws.on("close")` handler at `terminal.ts:259` then destroys the session via `destroySession` (either directly if isConnected=false, or via `detachWs` timeout), which calls `session.sshConn.end()` — the SSH connection is SHARED (reused at `terminal.ts:403` for the NEW WS), so its `close` event fires on the NEW WS's closure too, which sends `{type:"disconnected",message:"Connection closed"}` at `terminal.ts:2496`. Ashley's insight ("no way this is a Chrome thing") reframed the fix — hiding a tab should be inert, the visibility handler is over-reaching to a class of platforms where its behavior is actively harmful. Fix: keep current force-reconnect on iOS PWA, no-op the handler everywhere else via `isIosPwa()` check (`window.navigator.standalone === true && /iP(hone\|ad\|od)/.test(navigator.userAgent)`). Frontend-only. **Files**: NEW `src/ui/lib/is-ios-pwa.ts` (isIosPwa + SSR guard) + NEW `src/ui/lib/is-ios-pwa.test.ts` (7 tests: 4 combinations of standalone×UA + SSR guard + type-cast round-trip). EDIT `src/ui/features/terminal/Terminal.tsx` (patch #143 v2 visibilitychange useEffect at ~L344) — added `if (!isIosPwa()) return;` before `addEventListener`. EDIT `src/ui/features/pretty-view/PrettyView.tsx` (patch #148 visibilitychange useEffect at ~L793) — same gating. EDIT `src/ui/features/pretty-view/PrettyView.test.tsx` — added scoped `beforeEach`/`afterEach` in the visibilitychange test group to mock `navigator.standalone=true` + iOS UA (opts existing patch #148 tests into iOS-PWA mode so they still pass), plus new Test C.5 asserting handler is NOT attached when `isIosPwa()===false`. Comment blocks updated on both handlers to reflect narrowed scope + reference patch #156. Verification all green: `npx tsc --noEmit` exit 0; `npx vitest run src/ui/lib/is-ios-pwa.test.ts src/ui/features/pretty-view/PrettyView.test.tsx` → 17 passed, 1 skipped (pre-existing unrelated). Single atomic commit on `feat/tab-title-from-tmux`, NO Co-Authored-By trailer (fork convention). NO push, NO build, NO deploy — Ashley authorized code motion only per the new "code-work ask does NOT authorize push/build/deploy" fleet directive (added to Tina's identity file this same session after being caught mid-flight on patch #153/#154). skynet-patches.md write-up landed inline this same turn (per fleet inline-docs directive) at `~/.claude/identities/tina/skynet-patches.md` after patch #154. Bounty `chrome-tab-switch-away-forces-manual-reconnect` closed (status=done) + archived. New follow-up bounty `terminal-ws-close-session-handoff-race` (priority=low) captured for the residual backend race (won't fire in normal Chrome use once #156 lands but is a real race worth fixing structurally later — points at `terminal.ts:259` ws.on("close") + `terminal-session-manager.ts:308` detachWs / :339 destroySession). Anti-scope-creep held: no forbidden surface touched. Related bounties: `terminal-ws-close-session-handoff-race` (new, low, deferred structural fix); `voice-empty-transcript-silent-noop` (captured earlier this session — Ashley's mic-muted voice test surfaced a separate UX gap where STT `{text:""}` no-ops silently; not addressed here). | 2026-07-27 | 857626a | [260727-r09-patch-156-gate-patches-143-v2-terminal-a](./quick/260727-r09-patch-156-gate-patches-143-v2-terminal-a/) |
| 260727-lbr | Aside dismiss clears Claude Code's /btw overlay history before closing it — root cause of Ashley's aside-answer-pollution complaint: aside history within Claude Code's own /btw overlay (not Skynet's state) carried across BTW invocations in the same session, so the model self-referenced prior "please explain" turns and answered as if being asked to re-explain something it had already explained. Fix: change the tmux keystroke sequence sent by the aside-dismiss WS handler from just `Escape` (which closes the overlay but leaves its history intact) to `x` (clears the /btw overlay's in-session history) → 100ms gap → `Escape` (closes overlay). Frontend dismiss path 100% unchanged — the X (Resume) button in ComposeBox.tsx:1506 still fires handleAsideDismiss → WS `{type:'aside_dismissed'}` → backend handler; only the backend's tmux keystroke sequence changed. Change is contained in three code touches: (1) new named exported constant `BTW_CLEAR_HISTORY_KEY = "x"` at claude-session-server.ts near BTW_PROMPT/ASIDE_END_MARKER — lowercase `x` per Ashley's "X button" phrasing (single-key like `f` for fork / `Esc` for close per the ASIDE_END_MARKER footer pattern); if UAT reveals a different key (e.g. `c`, `Ctrl+L`), the constant is the one-line fix so a wrong-key UAT doesn't require a full replan; (2) `sendEscapeToBtw` renamed to `dismissBtw` at claude-session-server.ts:194 — new shape uses TWO `execCommand` calls separated by `await new Promise(resolve => setTimeout(resolve, 100))`, mirroring patch #152's `injectBtw` two-call pattern (a batched key burst risks being absorbed as a paste by Claude Code's Ink REPL, so the 100ms gap guarantees the clear-history keystroke and the Escape keystroke arrive as distinct events); 100ms chosen (not 200ms like injectBtw) because dismiss is two single-key presses vs. injectBtw's ~300-char paste — no paste-detection risk on the shorter keys, so the shortest known-good gap suffices; rationale recorded in the constant's docstring so future refactors don't blindly copy 200ms; (3) the ONE production caller at claude-session-server.ts (former line 1705, inside the `aside_dismissed` WS handler) switched from `sendEscapeToBtw(sshConn, currentTmuxSession)` to `dismissBtw(sshConn, currentTmuxSession)`. Test side: 5 new unit tests locking the two-call ordered contract (BTW_CLEAR_HISTORY_KEY sent first, `Escape` sent second, ~100ms gap between with fake timers, log-and-swallow on either-call failure); existing integration test at claude-session-server.aside.integration.test.ts updated to assert the mock execCommand receives BOTH keystrokes in order (not just Escape); PrettyView.tsx doc-comments at L231/305/633 sweep-updated `sendEscapeToBtw → dismissBtw` so `grep -rn "sendEscapeToBtw" src/` is a zero-match tripwire (planner's discretion — cheap insurance against name drift). New `__dismissBtwForTests` test seam mirrors the existing `__injectBtwForTests` convention. Verification all green: `npx tsc --noEmit` exit 0 at both the mid-task gate and after Task 2; `npx vitest run` → **653/653 across 50 test files** (baseline 648 + exactly 5 new = 4 dismissBtw shape tests + 1 clear-history-key constant assertion; integration test count unchanged — assertion added inside existing test). All grep checkpoints pass: `BTW_CLEAR_HISTORY_KEY` present in server + tests; `dismissBtw` present in server + WS handler; `sendEscapeToBtw` 0 matches in src/ (tripwire holds). WS frame shape `{type:'aside_dismissed', hostId, tmuxSession}` and frontend `handleAsideDismiss` UNTOUCHED (per plan non-negotiable — only the tmux keystroke sequence changed). Two atomic commits on `feat/tab-title-from-tmux`: `60c1ded` (feat: dismissBtw two-keystroke shape) + `5adad8a` (test: integration + PrettyView doc-comment sweep). Zero other files touched. NO Co-Authored-By trailer (fork convention). No push. No deploy this quick — batched pending Ashley greenlight per "every build → deploy is a new may-I" rule from patch #35, though this is trigger 3 territory (aside-answer-pollution is actively degrading her live use every time she stacks asides in a session) so SOLO-DEPLOY carveout is available on her nod. `~/.claude/identities/tina/skynet-patches.md` catalog write-up deferred to deploy time per batch-writeups-until-deploy rule (catalog is ~19-22 patches deep now including this pair). Anti-scope-creep held: no forbidden surface touched (settings modal, AppRail, dashboard, snippets manager, host manager UI, admin console, file manager UI, top-level chrome, keyboard shortcut editor — all still dead per Tina's identity file). Related bounties: `plain-language-translation-asides` (Phase 14 master; this touches its dismiss primitive), `aside-poller-never-emits-aside-ready` (in_progress/high — different aside bug), `composebox-enter-submits-during-aside` (parked, unrelated). | 2026-07-27 | 5adad8a | [260727-lbr-aside-dismiss-clears-btw-history](./quick/260727-lbr-aside-dismiss-clears-btw-history/) |
| 260727-kbw | Phase 15 UAT followup — fleet-pin load-order race fix. Ashley UAT'd Phase 15 (server-side pinned-conversation persistence, shipped 14:20 UTC) with "pins are not working, they last only until I close the app." Tina verified server side was working (curl round-trip via `/user-preferences` GET+PUT confirmed her `fleet::7::aqua` pin IS persisted; deployed AppShell chunk has correct mount-effect wiring: `getPinnedIds()` [minified `Qe`] then `hydratePinnedIdsFromServer` [minified `Ct`]). Root cause: race in `updateOpenTabs` pruner at `src/ui/state/conversation-store.ts:536-547`. On page load: `state.pinnedIds`, `state.fleetSessions` both empty (fleet loads independently async). Panel mount effect fires `getPinnedIds()` microtask → resolves `["fleet::7::aqua"]` → `hydratePinnedIdsFromServer` sets `pinnedIds = {fleet::7::aqua}`. Any subsequent `updateOpenTabs` re-emit (routine WS tab-list update) triggers pruner (now `pinnedIds.size > 0`). If `state.fleetSessions` still hasn't loaded yet, `fleetPinKeepSet` is empty → fleet pin isn't in `nextIds` (never in openTabs — it's fleet), isn't in (empty) `fleetPinKeepSet` → PRUNED. Pin disappears from UI; server still has it; next refresh rehydrates; race repeats. Ashley confirmed the caveat about later empty re-emits doesn't apply — fleet list is loaded ONCE per page and stays that reference for the rest of the session. **Fix (Ashley-designed):** defer the panel's `getPinnedIds()` + `hydratePinnedIdsFromServer()` hydration until fleet sessions have loaded at least once. When fleet has loaded, `fleetPinKeepSet` in any subsequent `updateOpenTabs` will include the fleet pin, and the pruner won't nuke it. **Slice 1** (`e6aede4`): store — added `fleetSessionsLoaded: boolean` to `ConversationState` (default false); `updateFleetSessions()` unconditionally sets `fleetSessionsLoaded: true` on state update (empty array counts as "loaded" — user may genuinely have zero fleet sessions), preserving the existing same-sessions no-op short-circuit for the array itself but forcing `notify()` on false→true flag flip; new `getFleetSessionsLoadedSnapshot()` + exported `useFleetSessionsLoaded(): boolean` via `useSyncExternalStore` mirroring `usePinnedIds()` pattern; new test helper `__resetFleetSessionsForTest()` mirroring existing `__resetPinnedIdsForTest`/`__resetActiveSetForTest` pattern for clean test starting points. 7 new store tests: `fleetSessionsLoaded starts false`; non-empty array flips to true; empty array ALSO flips to true (empty-is-loaded); subsequent identical calls don't re-notify solely on flag; `useFleetSessionsLoaded` returns current value and re-renders on flip; **regression test locking the exact bug**: `updateFleetSessions([{hostId:7, sessionName:"aqua"}])` → `hydratePinnedIdsFromServer(["fleet::7::aqua"])` → `updateOpenTabs([])` with empty tab list → assert `fleet::7::aqua` STILL in `pinnedIds` (would have been nuked pre-fix). **Slice 2** (`fd3efbb`): panel — imported `useFleetSessionsLoaded` + added `useRef` import; replaced the mount effect at PrettyConversationsPanel.tsx:204-218 with fleet-loaded-gated version — subscribes to `fleetSessionsLoaded`, adds `hydratedRef = useRef(false)` guard to prevent double-hydration on any spurious flag re-flip (defense-in-depth even though Ashley confirms it stays true after first flip). Effect gates on `!fleetSessionsLoaded → return`, then `hydratedRef.current → return`, then flips ref true and fires the same `getPinnedIds()` → `hydratePinnedIdsFromServer(ids)` async IIFE with cancelled-guard. Preserved existing Phase 15 (a)/(b)/(c) block comment; added a new (d) paragraph explaining the fleet-loaded gate + the race it closes (tagged with quick-task slug `quick-260727-kbw` for future greppability). New Test 22 mirroring Test 21 mock pattern: mount does NOT call `getPinnedIds` when `fleetSessionsLoaded=false`; when flag flips to true, `getPinnedIds` called exactly once + `hydratePinnedIdsFromServer` called exactly once with resolved ids. Verification all green: `npx tsc --noEmit` exit 0; `npx vitest run` → **648/648 across 50 test files** (baseline 647 + 1 new panel test; store file grew 55 → 62 tests including 7 new = +7 store, +1 panel, 0 regressions). All grep checkpoints pass: `fleetSessionsLoaded` 8 hits in store, `useFleetSessionsLoaded` export 1 hit, panel gate + `hydratedRef` 4 hits each. Two atomic commits on `feat/tab-title-from-tmux`, NO Co-Authored-By trailer (fork convention). No push, no deploy — Ashley calls the deploy separately per the "every build → deploy is a new may-I" rule from patch #35; this is trigger 3 (actively broken in production Ashley is currently hitting), so SOLO-DEPLOY carveout applies pending her nod. Pre-warn on first-hard-refresh `HTTP2_PROTOCOL_ERROR` (Caddy H2 stream fallout during container recreate — close+reopen tab spawns fresh H2 connection; not a real deploy failure). `~/.claude/identities/tina/skynet-patches.md` catalog write-up deferred per batch-writeups-until-deploy rule (catalog is ~15-18 patches behind now including Phase 15 waves + this fix; all pinned together at deploy-recommendation time). Anti-scope-creep held: no forbidden surface touched. Related bounties: `skynet-transformation` master (Phase 15 UAT followup folds into its timeline per one-project rule — do NOT create sibling bounty). | 2026-07-27 | fd3efbb | [260727-kbw-fleet-pin-load-race](./quick/260727-kbw-fleet-pin-load-race/) |
| 260727-gm3 | Wire the Deactivate-conversation action for pretty-conversations — mobile swipe strip (`[Pin] [Deactivate]` split) + desktop hover-reveal X-in-circle, on active-set non-RDP rows only. Semantics = pure reverse of tap-ambient-to-activate (close tab via reused `AppShell.closeTab` + `removeFromActiveSet(id)`; agent keeps running under the hood, row recedes to ambient, tap-to-reactivate). Ashley-greenlit shape via console-injection preview at `~/.claude/identities/tina/bounties/skynet-transformation/deactivate-preview.js` after two iteration rounds (round 1: desktop pop-in timing skew — X faded 120ms while pin popped instant via `display:none`→`inline-flex`; fix matched pin's exact mechanism at `pretty-conversations.css:491`+`:581`. Round 2: mobile strip icons ghosted through semi-transparent row body at rest — attempted fade-on-swipe via `data-swiped-open="true"` failed because attribute only fires post-touchEnd threshold, not mid-drag; reverted to always-visible for the preview and captured ghost-through as follow-up bounty — Ashley greenlit shipping without the fix). **Slice 1** (`1be8f35`): store mutator + component + CSS + strip widening + i18n + store tests. `removeFromActiveSet(id)` at `src/ui/state/conversation-store.ts` byte-lockstep with `addToActiveSet` at L703 — idempotent short-circuit, silent try/catch sessionStorage, new Set reference, notify. Removed the "no remove API by design" invariant comment at L70 + updated the equivalent beforeEach at `conversation-store.test.ts:69-71`. New `DeactivateAction.tsx` mirrors `PinAction.tsx` shape: `<button>` with `data-testid="deactivate-action"`, `size="mobile"|"desktop"`, renders lucide `X`. `.pv-deactivate-action` CSS block mirrors `.pv-pin-action` (`pretty-conversations.css:523`) with red-tinted palette (`hsla(4, 65%, 68%, 0.85)` base, `hsla(4, 75%, 78%, 1)` hover) — same bare-icon-no-chrome treatment, NOT a filled red pill. Desktop hover-reveal via `.pv-row.pv-row--desktop.active-set:not(:hover):not(:focus-within) .pv-deactivate-action { display: none }` matches pin's L581 pattern exactly for synced pop-in. `PC_SWIPE_REVEAL` widened 88→132 in `tokens.ts` (fits two 36px tap targets with comfortable margins; three call sites at PrettyConversationRow L176/L200/L287 all pick up the new constant no per-site changes). New i18n key `nav.conversations.deactivate` at `en.json`. Store Tests 30f/g/h/i cover: removes-and-persists, idempotent-no-op-when-absent, notifies, silent-on-storage-failure. **Slice 2** (`8c1bd65`): row + panel + AppShell wiring + panel tests. `PrettyConversationRow.tsx` gets `onDeactivate?: () => void` prop; mobile swipe strip now conditionally renders `[PinAction] [DeactivateAction]` side-by-side on active-set non-RDP rows via inline `flex justify-around` (Pin-only on ambient rows unchanged); desktop `.pv-meta` inserts `<DeactivateAction />` BEFORE PinAction only when `inActiveSet && !isRdp`. `PrettyConversationsPanel.tsx` `PrettyConversationRowLive` gains passthrough `onDeactivate`; panel's new `handleRowDeactivate(row)` composes `removeFromActiveSet(row.id)` then calls a new required `onDeactivateRow?: (row) => void` prop. `AppShell.tsx` at L1351 mount site wires `onDeactivateRow={(row) => closeTab(row.id)}` reusing the existing `closeTab` at L1169 — DID NOT invent a new close path. Panel Tests 20A-20E: active-set desktop row renders DeactivateAction LEFT of pin (DOM order); ambient desktop row does NOT render; RDP desktop row does NOT render; mobile strip contains both actions on active-set non-RDP; click invokes both `removeFromActiveSet(id)` AND `closeTab(id)`. ~20 existing panel-test row mounts updated to pass the new required `onDeactivateRow` mock. Verification all green: `npx tsc --noEmit` → 0 errors; `npx vitest run` → **619/619** (baseline 610 pre-gm3 + 4 store tests + 5 panel tests). Two atomic commits on `feat/tab-title-from-tmux`. Ships stacked with the still-pending f9v deploy + `1113263` BTW_PROMPT scope-limit tweak in ONE combined deploy pass. Anti-scope-creep held: no forbidden surface touched (settings modal, AppRail, dashboard, snippets manager, host manager UI, admin console, file manager UI, top-level chrome, keyboard shortcut editor — all still dead per Tina's identity file). Ghost-through-row-body fix EXPLICITLY out of scope per Ashley greenlight — follow-up TODO captured in `~/.claude/identities/tina/bounties/skynet-transformation/bounty.json` (component-level `data-drag-active` attr to gate strip content visibility). skynet-patches.md write-up deferred to deploy time per batch-writeups-until-deploy rule. Related bounties: `skynet-transformation` master (folded into timeline+todos per one-project rule); stale placeholder `deactivating-conversations` bounty due for archival. | 2026-07-27 | 8c1bd65 | [260727-gm3-wire-deactivate-conversation-action-mobi](./quick/260727-gm3-wire-deactivate-conversation-action-mobi/) |
| 260727-f9v | Per-host divider chips + identity-title sublabels for the pretty-conversations list (Ashley-greenlit today via console-injection preview at `~/.claude/identities/tina/bounties/skynet-transformation/host-headers-preview.js`; slice of the `skynet-transformation` master bounty). Two coupled UI changes across two atomic commits on `feat/tab-title-from-tmux`. **Slice 1** (`07a9364`): `PrettyConversationRow` gains an optional `subtitleMode?: "hostname" \| "identityTitle"` prop defaulting to `"hostname"` for full backward compat — every existing render site keeps its current behavior verbatim. When `"identityTitle"` AND an identity resolves for the row, the sublabel renders `identity.title ?? identity.displayName` with the `Server` icon DROPPED (icon reads as "host-y," doesn't fit a title). When no identity resolves (fallback path), the row silently reverts to hostname + `Server` icon verbatim — the load-bearing terminal safety net that guards against Tina's patch #149 "known limitation, inert ≠ inert" trap. Three new row tests (19A/B/C) pin the three-branch fallback: title present → renders title, no icon; title null but displayName present → renders displayName, no icon; no identity → hostname + icon. **Slice 2** (`2048823`): `PrettyConversationsPanel` imports `Server` from lucide-react alongside `Monitor`, extends `PrettyConversationRowLive` to pass through the new prop via the existing `...rowProps` spread, and adds a divider chip above each non-`__rdp__` group inside `grouped.map` — same structure as the existing "Remote desktop" chip (small icon + uppercase 11px semibold 0.08em-tracked label + gradient rule) but with `Server` glyph, `data-testid="host-divider"`, `data-host-id={group.hostId}`, and `group.hostName` as the label. Every row in that non-RDP grouped branch receives `subtitleMode="identityTitle"`; active-set + pinned + RDP render sites are UNTOUCHED. The existing RDP chip's text+icon color bumped `text-[#5c6070]/50 → /85` in-place so both chip families read at the same weight (Option 1 from PLAN — surgical alpha raise on the same cool-gray family, no hue shift; palette-move to `--color-pv-fg-muted` warm cream was Option 2, held in reserve if Ashley UAT feedback wants palette-consistency more than hue-consistency with the existing chrome). Panel Test 3 (the intentionally-reversed "FLAT rendering, no per-host semibold header" prototype lock) rewritten to assert the chip DOES render with correct `data-host-id` + hostName. Three new panel tests (19A/B/C) cover: two non-RDP groups → two chips; active-set + pinned + one grouped host → exactly ONE chip only above the grouped host (proves cross-host groups don't get chips); grouped host + `__rdp__` sentinel → BOTH divider testids coexist without duplication or replacement. Design lock scope discipline: ZERO CSS file changes (chip is inline Tailwind mirroring the existing RDP chip; row keeps existing `.pv-host` class), ZERO `conversation-store.ts` changes (the `grouped: HostGroup[]` shape already carries `{ hostId, hostName, rows }`), ZERO `AppShell.tsx` changes. Verification all green: `npx tsc --noEmit` → 0 errors; `npx vitest run` → **610/610 across 49 files** (baseline 604, +6 new = 3 row + 3 panel; Test 3 was rewritten in-place with no net add). Four files, +339/-53 net (Row: +48/-1 + tests +98/0; Panel: +38/-6 + tests +155/-46). Both commits individually PR-able. NO Co-Authored-By trailer (fork convention). NO push. Deploy IMMEDIATELY after this docs commit lands per Ashley's "ship it" — solo deploy (not batched — no other pending patches in the queue this session). Pre-warn Ashley on deploy about first-hard-refresh `HTTP2_PROTOCOL_ERROR` on chunk loads (Caddy H2 stream fallout during container recreate — close+reopen the tab spawns a fresh H2 connection; not a real deploy failure). `~/.claude/identities/tina/skynet-patches.md` catalog write-up deferred per batch-writeups-until-deploy rule (catalog is already ~10-12 patches behind; queued for the next dedicated flush pass alongside cdccd4f + 28065e5 + this pair). Anti-scope-creep held: no forbidden surface touched (settings modal, AppRail, dashboard, snippets manager, host manager UI, admin console, file manager UI, top-level chrome, keyboard shortcut editor — all still dead per Tina's identity file). Related bounties: `skynet-transformation` master (this slice folds into its timeline; do NOT create a sibling bounty per Tina's "one bounty for the entire movement" rule). | 2026-07-27 | 2048823 | [260727-f9v-add-per-host-divider-chips-to-pretty-con](./quick/260727-f9v-add-per-host-divider-chips-to-pretty-con/) |
| 260727-ejk | Mobile-sizing bumps for ComposeBox controls + PrettyView scroll-to-bottom (Ashley-requested; stacks with cdccd4f in a combined deploy this session) — every button + textarea in the ComposeBox gets 2× on mobile, the "jump to latest" scroll-to-bottom arrow in PrettyView gets 4× (Ashley: "quarter of the size that would be comfortable"). Approach: pure Tailwind `max-md:` variants (viewport <768px = same threshold as `useIsMobile()` at `src/ui/hooks/use-mobile.ts:3`), additive only — zero existing classes removed, zero logic changes, zero new imports. Consistent with app's existing mobile-vs-desktop definition; no JS state coupling, no re-render on resize, no SSR/hydration flash. Row-1 container's `isTouchDevice ? min-h-[44px] : min-h-8` gate stays untouched (touch-device semantic ≠ mobile viewport — kept as an orthogonal signal, mobile just gets +max-md:min-h-16 alongside so it always wins at <768px regardless of touch-detection heuristics). Meter well has explicit `h-7` — doesn't auto-grow when siblings expand — so bumped the well itself to `max-md:h-14`; reset button inside auto-inherits height via `h-full`, gets explicit `max-md:w-12` for width. Send button (raw `<button>` at ComposeBox L1508, patch #129 inside-textarea): padding `p-2 → +max-md:p-3`, inline SVG gets NEW `className="max-md:w-10 max-md:h-10"` (width/height="24" attrs preserved as SSR/no-CSS fallback per the same pattern used elsewhere in the file), X lucide dismiss glyph `size-6 → +max-md:size-10`. Textarea `pr-10` (patch #129 inside-Send clearance) → `+max-md:pr-14` so enlarged Send doesn't overlap typed text. Scroll-to-bottom at 112×112 chonky FAB size is literal to Ashley's stated ratio — if UAT dials back, easy CSS-only followup dropping to 84 (3×) or 56 (2×) is a one-line edit. All 11 conceptual touches across 2 files: `src/ui/features/pretty-view/ComposeBox.tsx` (+15 max-md: hits: retry-upload button, row-1 container as cn() 3rd arg, meter well h-7, reset button w-6, RotateCcw icon size-3.5, five row-2 icon-sm cluster [Terminal/Paperclip/Square/ThumbsUp/Hourglass], textarea min-h-8! + pr-10, Send button p-2 + inline SVG + X icon) + `src/ui/features/pretty-view/PrettyView.tsx` (+2 max-md: hits: scroll-to-bottom Button + ArrowDown icon). Verification all green: `npm run type-check` clean; full sweep `npm test` 604/604 (matches cdccd4f baseline — zero regressions, no tests added because purely additive className changes have nothing to assert without breakpoint-aware rendering which JSDOM doesn't reliably do). 18/19 plan grep gates pass; the 19th (`width="24" height="24"` same-line preservation guard) was a plan-authoring drift — attributes are on adjacent lines in the pre-change source and both verifiably preserved, intent-equivalent grep passes, only a `className` attribute was added to the SVG. Documented as non-deviation in SUMMARY.md. Two files, +17/-10. Single atomic commit on `feat/tab-title-from-tmux`. NO Co-Authored-By trailer (fork convention). Deploy PENDING: stacks with `cdccd4f` (patch: /id XML-form suppression) into ONE combined deploy this session per Ashley's "we are gonna stack a bunch of changes before deploy" — she greenlit deploy at end of this session with "deploy when you're done". Pre-warn Ashley on deploy that first hard-refresh may HTTP2-error on chunk loads (Caddy H2 stream fallout — close+reopen the tab spawns a fresh connection). Combined deploy will pin BOTH patches into `skynet-patches.md` in one bundle write-up per Ashley's 2026-07-23 batch-writeups-until-deploy rule. Anti-scope-creep held: no forbidden surface touched (settings modal, AppRail, dashboard, snippets manager, host manager UI, admin console, file manager UI, top-level chrome, keyboard shortcut editor — all still dead per Tina's identity file). Related bounties: `pwa-bottom-safe-area-reclaim` (medium — separate followup for reclaiming iOS PWA bottom safe-area vertical space, complementary to touch-target bumps but independent). | 2026-07-27 | 28065e5 | [260727-ejk-mobile-sizing-bumps-for-composebox-butto](./quick/260727-ejk-mobile-sizing-bumps-for-composebox-butto/) |
| 260727-e41 | Extend arm-emitter /id-suppression whitelist to cover harness slash-UI invocation form (Phase 14 UAT amendment on top of `f029e44`) — Ashley UAT-caught immediately on wake: first user message post-`/id tina` load triggered AsideBubble explaining tina's own greeting response. Root cause: Phase 14's arm-emitter useEffect at `src/ui/features/pretty-view/PrettyView.tsx:909` short-circuited `aside_arm` only when the last user turn's `content.trimStart().startsWith("/id ")` — a check that catches raw-text `/id save` typed into SSH-attached tmux panes (round-trips through JSONL as literal string), but MISSES the harness slash-UI invocation path where `/id tina` lands in JSONL as XML wrappers `<command-message>id</command-message>\n<command-name>/id</command-name>\n<command-args>tina</command-args>` (content starts with `<`, not `/id `). Harness slash-UI is Ashley's PRIMARY /id invocation path from pretty-view, so every /id from that surface armed a phantom aside. Fix: extract module-local const arrow `isIdCommand = (content: string): boolean => content.trimStart().startsWith("/id ") \|\| content.includes("<command-name>/id</command-name>")` above the useEffect + swap the inline check for `if (isIdCommand(lastUserContent)) return;` at the callsite. XML substring `<command-name>/id</command-name>` chosen as the marker because it is not a legal substring of prose — it only appears when the harness itself wraps the command, so false-positive risk is zero. Helper stays module-local (no new shared util file — Phase 14 no-new-deps posture preserved). Test 9 mirrors Test 7 shape with realistic UAT payload (`<command-args>tina</command-args>` sibling) → asserts `aside_arm` NOT called. Tests 1-8 explicitly locked unchanged; Tests 7 (/id-then-real-msg still arms) + 8 (only last user turn matters) remain valid regression guards. TDD gate followed: RED (Test 9 isolated → `expected […aside_arm…] to be undefined`) → GREEN (helper + callsite swap → Test 9 passes) → REFACTOR skipped (helper landed final on first pass) — RED+GREEN combined into single atomic commit matching Phase 14 commit style precedent (`f029e44` also bundles impl+tests). Verification all green: `npm run type-check` clean; `npx vitest run PrettyView.aside.test.tsx` 9/9 (was 8/8 pre-plan); full sweep `npm test` 604/604 across 49 files (was 603/603 — net +1). Grep gates: `isIdCommand` declaration+callsite both present; `<command-name>/id<` XML substring present; Test 9 title present. Frontmatter note (non-blocking): plan `<truths>` said baseline "33 → 34" tests but aside test file's actual baseline was 8 → 9 (33/34 figure appears to have referenced aggregate PrettyView scope) — behavioral truth-set fully satisfied, documented in SUMMARY.md for traceability, no action. Two files, +73/-1 (`src/ui/features/pretty-view/PrettyView.tsx` +17/-1, `src/ui/features/pretty-view/PrettyView.aside.test.tsx` +56/0). Single atomic commit on `feat/tab-title-from-tmux`. No deploy performed inside quick task per plan constraint — Ashley greenlights deploy separately; trigger 3 met (actively broken in production, every /id via harness slash-UI hits it), so SOLO-DEPLOY carveout applies pending her nod. No push. NO Co-Authored-By trailer (fork convention). No `~/.claude/identities/tina/skynet-patches.md` write-up per batch-writeups-until-deploy rule. Anti-scope-creep held: no forbidden surface touched (settings modal, AppRail, dashboard, snippets manager, host manager UI, admin console, file manager UI, top-level chrome, keyboard shortcut editor — all still dead per Tina's identity file). Related bounties: `plain-language-translation-asides` (Phase 14 master), `aside-poller-never-emits-aside-ready` (Phase 14 diag). | 2026-07-27 | cdccd4f | [260727-e41-extend-arm-emitter-id-suppression-whitel](./quick/260727-e41-extend-arm-emitter-id-suppression-whitel/) |
| 260726-vbd | Extend aside-active blocking to cover /btw GENERATION window + insert 'concisely' into BTW_PROMPT. **Slice 1** (656d709 `feat(aside): widen asideActive predicate to cover /btw generation window`): PrettyView.tsx gains `asidePending: boolean` state alongside existing `asideText`. New `handleComposeSend` wraps parent's `onSend`, arms `asidePending=true` on any trimmed input starting with `/btw` (arms unconditionally on submit — 60s safety timeout catches abandoned pending). Predicate widened: `asideActive` prop to ComposeBox becomes `asideText !== null \|\| asidePending` (ComposeBox body untouched — its consumption of `asideActive` for the disable + send-button-swap-to-dismiss/resume already correct). `clearAsidePending()` helper called from 4 sites: `handleAsideDismiss` (cancel-button click during either phase — Escape cancels in-flight generation OR clears displayed aside, and existing `dismissCooldownUntilRef` from commit 34cfeec covers the trailing false→true isIdle re-flip for free), `aside_ready` handler (transitions cleanly to displayed lifecycle), `aside_dismissed` handler (server-side dismiss echo), `session_changed` handler (pane switch drops pending). Safety timeout: `setTimeout(clearAsidePending, 60000)` set on arm, cleared on any legitimate clear path + unmount cleanup — handles the failure mode where /btw never produces aside_ready (network hiccup, extractor never matches). No changes to ComposeBox.tsx (already consumes `asideActive` correctly), no changes to the 8s dismiss cooldown (orthogonal to this fix). **Slice 2** (f17f331 `feat(aside): insert 'concisely' into BTW_PROMPT to bias Claude toward brevity`): word 'concisely' inserted after 'Re-explain' in BTW_PROMPT string literal in claude-session-server.ts — adverb modifies verb directly, preserves U+2014 em-dash + `/btw ` prefix asserted by existing invariant tests. Companion byte-for-byte assertion in claude-session-server.aside.test.ts updated in lock-step. Integration test's `${BTW_PROMPT}` interpolations pick up the new value automatically. Verification all green: `npm run type-check` clean; all 169 tests across 15 files pass (Task 1 suite 54/54, Task 2 suite 30/30, full sweep 169/169). Success gates: `grep -c 'asidePending' PrettyView.tsx = 24`; `grep -c 'concisely' claude-session-server.ts = 1`; `grep -c 'concisely' claude-session-server.aside.test.ts = 1`; `grep -c 'aside poll diag:' claude-session-server.ts = 8` (unchanged from baseline — diag-log-strip is separate followup pending Ashley UAT of both this widened predicate AND the cooldown fix from 34cfeec). Two atomic commits on `feat/tab-title-from-tmux`. Zero other files touched. Ships in the Phase 14 pin bundle (with #150 A + #150 C + #151 + #152 + ellipsis-guard-fix + cooldown-fix + docker-build-perf-Phase-A + compose-box-toggle-button + this). Related bounties: `plain-language-translation-asides` (Phase 14 master), `aside-poller-never-emits-aside-ready` (Phase 14 diag). | 2026-07-26 | f17f331 | [260726-vbd-extend-aside-active-blocking-to-cover-th](./quick/260726-vbd-extend-aside-active-blocking-to-cover-th/) |
| 260726-l1p | Patch #150 A + C — pruner fleet-aware + URL-restore multi-tab glow, three atomic commits on `feat/tab-title-from-tmux`, no deploy. **Slice A** (7f63a4b): fixes Ashley's live-hit pin-nuke — `updateOpenTabs` in `src/ui/state/conversation-store.ts` at L470-527 built its pinnedId keep-set from openTabs only; post-#149 A fleet-derived pinnedIds are legitimate but the pruner dropped them because they don't appear in openTabs, so clicking ANY pinned fleet row triggered updateOpenTabs which nuked ALL 4-5 pins in one action. Fix: build a `fleetPinKeepSet` from `state.fleetSessions` (id computed via `fleetRowId(session.hostId, session.sessionName)`) and change the drop condition to `!nextIds.has(id) && !fleetPinKeepSet.has(id)` — any pinnedId that's a member of fleetSessions is kept regardless of openTabs. Micro-guard on empty `pinnedIds` for hot-path perf. 2 new store tests: 'clicking a pinned fleet row does NOT unpin OTHER pinned fleet rows' (pin 4 fleet rows, updateOpenTabs with unrelated openTab, assert pinnedIds.size === 4) + 'clicking an openTab row still prunes stale openTab pins as before' (regression guard for pre-#149 behavior on openTab-derived pins). **Slice C-investigate** (b48023e): comment-only commit adding 51 lines of inline documentation at `src/ui/AppShell.tsx:825-861` mapping the end-to-end restore→glow→content-load flow. Verdict: SAME_BUG. Both Ashley symptoms (missing glow on N-1 restored tabs; "did NOT auto-load content" on click) trace to the same root — only `restoredTabs[0]` was routed through the store-side signal (`pendingSelectId → flushed selectedId → PrettyConversationsPanel's addToActiveSet(selectedId) effect`). The content-load "wait" is NOT a bug: `Terminal.tsx` gates WebSocket-connect on `isVisible = tab.id === effectiveSelectedTabId`, so restored non-focused tabs deliberately don't prefetch sockets; clicking un-glowed tab flips `activeTabId` → `isVisible=true` → connect fires. The "wait" perception is WebSocket handshake latency, not a bug. Task 4 skipped per plan (SEPARATE_BUG branch not taken). **Slice C-fix** (162dc1c): AppShell.tsx L833 hardcoded `restoredTabs[0].id` in the persisted-tab-restore path — fix iterates all `restoredTabs` and calls `selectConversationDeferred` on each so every restored tab enters activeSet and glows. 1 new store-contract regression test in conversation-store.test.ts asserting per-tab `selectConversationDeferred` → `addToActiveSet` accumulation contract holds (store-level test guards the AppShell fix's dependency — AppShell has no integration test harness for URL-restore path, so pinning the store contract is the appropriate regression guard). Verify all green: `npm run type-check` clean across all 3 commits; `npx vitest run src/ui/state/conversation-store.test.ts` → 43/43 after Slice A (41 pre-plan + 2 new), 44/44 after Slice C-fix (+1 new); `pretty-conversations` suite 38/38 unchanged (no regression in the `addToActiveSet` consumer). No push. No deploy — batched pending Ashley greenlight, but SOLO-DEPLOY carveout applies for Slice A (actively broken in production). `~/.claude/identities/tina/skynet-patches.md` catalog write-up deferred to deploy time per batch-writeups-until-deploy rule. #149-followup-2 (pinnedIds account-wide persistence) intentionally out of scope — Ashley clarified pins are server-side account-wide, so it needs its own /gsd:plan-phase for backend + frontend. NO Co-Authored-By trailer (fork convention). Anti-scope-creep held: no forbidden surface touched (settings modal, AppRail, dashboard, snippets manager, host manager UI, admin console, file manager UI, top-level chrome, keyboard shortcut editor — all still dead per Tina's identity file). Files modified: `src/ui/state/conversation-store.ts`, `src/ui/state/conversation-store.test.ts`, `src/ui/AppShell.tsx` (last two commits touched AppShell.tsx). | 2026-07-26 | 162dc1c | [260726-l1p-patch-150-a-c-pruner-fleet-aware-url-res](./quick/260726-l1p-patch-150-a-c-pruner-fleet-aware-url-res/) |
| 260726-s2f | Phase A docker-build perf — strip --ignore-scripts from both npm ci in docker/Dockerfile + delete both `npm rebuild` lines (redundant once prebuild-install runs) + add BuildKit `--mount=type=cache,target=/root/.npm` on both npm ci invocations + Rule-3 auto-fix (added COPY of 3 additional patch-scripts to `deps` and `production-deps` stages after first build failed with MODULE_NOT_FOUND on patch-app-builder-lib/patch-better-sqlite3/patch-nan). **Fresh cache-cold build: 6m41s** (vs 11min baseline = ~4.5min saved, 39% reduction). **Cache-warm build: 2s** (all BuildKit layers cached). Main remaining pain: `cpu-features@0.0.10` (ssh2 transitive dep) has no `prebuild-install` mechanism — its install script is `node-gyp rebuild` which unconditionally compiles from source, eating ~4-5min of the 6m41s fresh build. Phase B (base-image caching) is what would hit the ~2-3min target for a fresh build (would cache the cpu-features .node artifact across builds). BUT the iteration story is already very good: subsequent builds after a first-of-the-day are 2s cache-warm, and code-only changes (no package.json diff) invalidate at the COPY line and skip the whole native-compile production-deps stage — so future patch iterations will be dominated by frontend vite rebuild + backend transpile (~60-90s), not the full 6m41s. Production `skynet` container UNTOUCHED — test tag `skynet-patched-test:local` distinct from production `skynet-patched:local`. Related bounty: `docker-build-recompiles-unchanged-deps` (in_progress, Phase A complete). | 2026-07-26 | bc8779e | [260726-s2f-phase-a-docker-build-perf-strip-ignore-s](./quick/260726-s2f-phase-a-docker-build-perf-strip-ignore-s/) |
| 260726-t1m | Patch #152 — fix aside `injectBtw` Enter absorbed by Claude Code v2.1.150 REPL paste-mode detection. Bug (Ashley UAT during Phase 14 post-deploy walk): prompt text landed in Claude Code input box but Enter never submitted — /btw overlay never opened, aside never fired. Reproduced against fresh `claude --model haiku` in test tmux — same shape → text in box, no submit. Bash accepts same shape fine (executed /btw as command). Root: Claude Code Ink REPL treats ~300-char burst as a paste and absorbs trailing Enter into the paste buffer. **Fix**: split `injectBtw` in `src/backend/claude-session/claude-session-server.ts:151-167` into TWO `execCommand` calls with `await new Promise(r => setTimeout(r, 200))` between — first sends BTW_PROMPT text alone (no Enter arg), delay lets paste buffer flush, second sends `Enter` alone as a distinct keystroke. Verified against same test tmux — /btw overlay opens + Haiku Answering. Added `__injectBtwForTests` test seam mirroring existing `__asideShellQuoteForTests` convention. Added 4 new vitest cases in `claude-session-server.aside.test.ts` asserting two-call shape (call 1 = text, no Enter; call 2 = Enter alone), 200ms timing (fake-timers), log-and-swallow on either-call failure. **Files**: `src/backend/claude-session/claude-session-server.ts` + `src/backend/claude-session/claude-session-server.aside.test.ts`. Zero other files touched. `sendEscapeToBtw` untouched (single-key send works correctly). Full regression: 600/600 vitest (up from 596/596 baseline = +4 net-new). `npx tsc --noEmit` clean. `npm run build` succeeds. Transpiled dist grep confirms `setTimeout(resolve, 200)` shipped. Related bounty: `aside-btw-enter-not-submitting` (todos 1+2 marked done; awaiting Ashley UAT after Phase 14 bundled deploy). Ships BUNDLED with Phase 14 patches (#150 A + #150 C + #151 + #152 as ONE deploy event). NO deploy this session. | 2026-07-26 | 0d333d4 | [260726-t1m-patch-152-fix-aside-btw-injectbtw-enter-](./quick/260726-t1m-patch-152-fix-aside-btw-injectbtw-enter-/) |
| 260724-czu | Patch #149 B+C — three-tier sort for pretty-conversations panel (`activeSet > pinned > grouped`, strict dedup so each row appears in exactly one tier). Slice A (commit `cf624a4`, ad-hoc before this quick task) removed the openTabs-only guard on `pinConversation` so fleet-derived row ids can enter `pinnedIds`; this quick task delivers Slice B (pinned fleet rows actually surface in the pinned tier at panel top) and Slice C (a new active-set tier ABOVE pinned so rows Ashley is actively bouncing between overtake pinned rows — per Ashley 2026-07-24: "sorting active sessions to the top, even overtaking pinned ones, because the most important thing is being able to bounce between the sessions that are active"). Store side: `ConversationList` gains `activeSet: ConversationRow[]` alongside the existing `pinned: ConversationRow[]`; `computeSnapshot` rewritten to emit three tiers with a shared `emittedIds` Set for O(1) dedup — Tier 1 iterates openTabs then fleetSessions filtered by `state.activeSet`, Tier 2 iterates same sources filtered by `state.pinnedIds && !activeSet`, Tier 3 (existing grouped-by-host) skips anything already in `emittedIds`. RDP rows stay in the `__rdp__` sentinel group at the bottom, untouched (excluded from active-set semantics by patch #137 contract; not pinnable via panel wiring even after Slice A). Panel render: destructures `activeSet: activeSetRows` so the existing `activeSet` local (from `useActiveSet()`) is still available for row-level `inActiveSet={activeSet.has(row.id)}` wiring on all three tiers. New `.pv-panel-group[data-active-set-group=true]` render block emits ABOVE the existing `.pv-panel-group[data-pinned-group=true]` — no visual separator between tiers per Ashley's Telegram-shape flat-list lock. Tier 1 rows get `pinned={pinnedIds.has(row.id)}` (dynamic — a row that IS pinned AND active shows the pin glyph); Tier 2 rows get `pinned={true}` hardcoded (they're pinned by tier membership); Tier 3 rows get `pinned={pinnedIds.has(row.id)}` (defensive — after the tier logic guarantees pinned rows are never in Tier 3, this is always false there but the pattern stays regular). Tests: 5 new store tests (Test 30 reconciled to Slice B; Test 30b pinned fleet row → tier 2; Test 30c active-set fleet row overtakes pinned → tier 1; Test 30d activeSet-only fleet row → tier 1 not grouped; Test 30e openTab row in both activeSet+pinnedIds → tier 1 only, no dup) + empty-state shape guard + 2 new panel tests (DOM-order invariant across all three tiers + isEmpty accounting includes activeSet). 79/79 tests green across 3 files (conversation-store + PrettyConversationsPanel + PrettyConversationRow), `npx tsc --noEmit` exits 0. Files touched: `src/ui/state/conversation-store.ts` +136/-55, `src/ui/state/conversation-store.test.ts` +127/-3, `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` +28/-2, `src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` +69/-0. Two atomic commits on `feat/tab-title-from-tmux` (21ec1a7 store rewrite + tests; 5454fef panel render). NO push, NO deploy — batched with patch #149 A (cf624a4) for a single Ashley-greenlit deploy afterward per 2026-07-23 batch rule. `~/.claude/identities/tina/skynet-patches.md` catalog write-ups for #149 A/B/C deferred to deploy-recommendation time. Known limitation carried over from Slice A: pinned fleet-only ids do NOT survive an `updateOpenTabs` pruning pass (pruning only inspects openTabs membership); acceptable per Slice A design — a fleet row that disappears from `state.fleetSessions` is inert in `pinnedIds` and computeSnapshot just skips it. | 2026-07-24 | 5454fef | [260724-czu-conv-list-three-tier-sort](./quick/260724-czu-conv-list-three-tier-sort/) |
| 260724-bwi | Patch #148 — PrettyView WebSocket auto-reconnect. Root cause diagnosed 2026-07-24 with Ashley via #147 SkynetLog telemetry: `PrettyView.tsx:447-457` ws.onclose handler had explicit "Do NOT auto-reopen" comment, just set `errorMessage = "Connection closed"` and gave up. When claude-session WS closed for ANY reason (iOS PWA background, deploy container recreate, network flap), no reopen path existed. Terminal.tsx's ssh WS reconnects fine (#143v2 works, proven by 3 clean end-to-end reconnect cycles in the SkynetLog file with ZERO failure entries), but PrettyView's SEPARATE WebSocket to /claude-session/websocket/ was single-shot. Ashley's confirmed workaround: toggle pretty view off + on via key combo = unmount+remount = useEffect re-runs = fresh WS. Fix: mirror Terminal.tsx's proven retry pattern into PrettyView. Concrete changes: (a) new module-scope `MAX_RECONNECT_ATTEMPTS = 5` const, (b) refs `reconnectAttemptsRef`/`reconnectTimeoutRef`/`paneKeyRef`/`statusRef`, (c) `retryKey` state added to the WS-open useEffect deps so bumping it re-runs the effect with cleanup+fresh WS, (d) `paneKeyRef` distinguishes pane-change (full state reset preserved) from retry re-run (state preserved), (e) `statusRef` mirror avoids stale-status closure issues in onclose handler, (f) replaced onclose "Do NOT auto-reopen" body with retry-scheduling logic gated on `statusRef.current !== 'inactive'` and `reconnectAttemptsRef.current < MAX_RECONNECT_ATTEMPTS` with `Math.min(2000 * attempt, 8000)` backoff (2/4/6/8/8s = ~28s total window), (g) ws.onopen clears `errorMessage(null)` and increments cleared attempts (so retries never show transient empty state — errorMessage stays "Connection closed" through backoff until a NEW WS actually opens), (h) new mount-once visibilitychange useEffect: on `document.hidden` clears any pending timer, on visible resets counter to 0 and bumps retryKey if WS not OPEN and status not inactive (direct fix for Ashley's iOS PWA long-background case where retries might burn through while suspended), (i) cleanup fn clears pending reconnectTimeoutRef before ws.close() so unmount never fires stale retries. "inactive" state semantics preserved throughout — no retry, no reopen, no errorMessage clear when server sent an authoritative terminal frame. 4 new tests (retry-on-close fires new WS after 2s and clears errorMessage on onopen; max-5-attempt cap holds; visibilitychange:visible resets counter and reopens even after cap exhaust; inactive state short-circuits ALL retry paths) + 3 existing Phase 05 tests all pass = 7/7 green. Type-check clean, build clean. Two files, +393/-33. Single atomic commit on `feat/tab-title-from-tmux`, NO Co-Authored-By trailer (fork convention). Deploy proposed to Ashley post-land as SOLO deploy (not batched) because this is THE fix for her active-flooded-tabs-broken-after-every-deploy pain — she'll want it immediately in production. Terminal.tsx, ConnectionLogContext.tsx, claude-session-api.ts, IdentityModal.tsx all untouched per scope discipline (IdentityModal follow-up if it exhibits the same bug; likely candidate but out of scope for this patch). skynet-patches.md write-up deferred to Ashley's greenlight + deploy time. | 2026-07-24 | 742a98a | [260724-bwi-patch-148-prettyview-websocket-auto-reco](./quick/260724-bwi-patch-148-prettyview-websocket-auto-reco/) |
| 260724-aoq | Patch #146 — log-forwarder prototype (frontend UI console.log/warn/error intercept → side-channel HTTPS POST → in-memory ring buffer + rotating file mirror; enables Tina to `docker exec skynet cat /tmp/skynet-console-forward.log | grep ...` for iOS-PWA-only diagnosis). Motivating case: patch #143/#143-v2 shipped guesses because Tina had no way to see the JS console output from Ashley's iPhone-installed PWA where DevTools is unreachable. Locked constraint (Ashley 2026-07-24, load-bearing): the log wire MUST NOT share fate with the per-tab terminal WebSocket ("when we have trouble with the socket, we can't diagnose them"); this prototype uses a DISJOINT HTTPS POST endpoint that routes through Caddy → nginx → backend's Express app, entirely separate from the ssh/websocket path. Prototype scope defaults picked by Tina per her authority on vehicle decisions: (a) all sessions log (no opt-in), (b) console.log/warn/error only (no debug/info noise; terminal xterm.js content is naturally excluded because it doesn't go through console.*), (c) in-memory ring buffer 1000 entries + append-mode file mirror at `/tmp/skynet-console-forward.log` with 5MB rotation (marker `[LOG_ROTATED at <iso-ts>]` prepended on wrap; file location configurable via `SKYNET_CONSOLE_FORWARD_LOG_PATH` env var), (d) admin read path is `sudo docker exec skynet cat /tmp/skynet-console-forward.log | grep ...` — no new HTTP GET endpoint, no admin UI, no WS subscribe. Frontend: new `src/ui/lib/console-forwarder.ts` (189 lines) with `initConsoleForwarder()` idempotent init, patches `window.console.log/warn/error` to call original FIRST (preserves DevTools console + preserves error stack trace) then enqueue structured envelope `{ts, level, tabId, hostId?, sessionKey?, msg}`, batch flush every 500ms OR at 20 entries, `navigator.sendBeacon` on `visibilitychange→hidden` + `pagehide` (survives iOS PWA background/close). Wired at top of `src/main.tsx` before `snapshotPendingTab()` so it fires before any React code. Backend: new `src/backend/database/routes/debug.ts` (138 lines) exporting Express Router with `POST /console-log` handler + `handleConsoleLog()` extracted for direct function-level testing. Registered as `app.use("/debug", debugRoutes)` in `src/backend/database/database.ts` after `/user-preferences` at ~L1793. Auth via existing JWT middleware pattern mirroring `/compose-drafts`. Nginx (CLAUDE.md caveat honored): `location ~ ^/debug(/.*)?$` proxy_pass block added to BOTH `docker/nginx.conf` AND `docker/nginx-https.conf` after their respective `/compose-drafts` blocks — missing either would 200 with index.html and crash the frontend on the .map. Ring buffer: simple Array with push()/shift() when >1000 (no external lib). File writes wrapped in try/catch → apiLogger.error, never crash the backend on disk failure. tabId hard-coded to `'no-tab'` placeholder per locked design decision #7 — wiring to AppShell active tab is a follow-up patch (does not block prototype utility since tab-tag is optional grep filter). Tests: 2 new frontend tests (console-preservation invariant + enqueue assertions using onEnqueue callback + `__test_getBuffer` for coverage) + 4 new backend tests (accepts valid POST + mirrors to configurable file path via env var + rejects malformed body 400 + append-mode + rotation marker). Handler extracted to exported function for direct function-level testing after initial Express-harness approach caused 5s AuthManager singleton init timeouts. Verification all green: `npm run type-check` clean, 6/6 new tests pass, 195 subsystem tests pass (no regressions in console-forwarder/debug-related suites), `npm run build` clean. Single atomic commit on `feat/tab-title-from-tmux` — NO Co-Authored-By trailer (fork convention). No push, no deploy — BATCHED with patch #145 (efc8e87) for Ashley's next deploy greenlight per 2026-07-23 batching rule. skynet-patches.md write-up deferred to deploy-recommendation time; #145 + #146 will be pinned together as one grouped catalog entry. First diagnostic use case at deploy time: reproduce Ashley's iOS PWA auto-reconnect failure, then `docker exec skynet cat /tmp/skynet-console-forward.log | grep visibilitychange` to see the actual sequence of events my #143/#143-v2 handlers produced (or didn't produce). | 2026-07-24 | 1ac99b7 | [260724-aoq-patch-146-log-forwarder-prototype-fronte](./quick/260724-aoq-patch-146-log-forwarder-prototype-fronte/) |
| 260724-aiu | Patch #145 — active-glow URL-restore fix. Two surgical 1-line insertions in `AppShell.tsx` symmetric with the existing sidebar click handlers at lines 1295/1309/1317: `selectConversationDeferred(restoredTabs[0].id)` immediately after the `setActiveTabId(restoredTabs[0].id)` call in the persisted-tab-restore path (~L833), and `selectConversationDeferred(openedIds[idx])` immediately after `setActiveTabId(openedIds[idx])` in the URL-driven initial open path (~L902). Root cause of Ashley's 2026-07-24 UAT report ("URL-restore tab pointing at a session doesn't glow in sidebar; only clicks light it up") was that the two mount-time restore paths set only local AppShell state (`activeTabId`) but never called `selectConversationDeferred` on the conversation store, so `state.selectedId` stayed null on mount and my patch #144 useEffect at `PrettyConversationsPanel.tsx:162-164` (`if (selectedId) addToActiveSet(selectedId)`) skipped, leaving `sessionStorage["pv-conv-active-set"]` empty and no row with `.active-set` class. Confirmed by Ashley's V2 DevTools diag (32 rendered rows, 0 with `.active-set`, sessionStorage present:false, URL clearly targeting a session). Fix ensures `state.selectedId` is non-null at mount so the useEffect fires on first render, populates activeSet, and lights up the row. Import already existed at AppShell.tsx:59 — no new imports. No new tests: bug lived outside test coverage (URL routing + persisted-tab restore paths, neither mocked in AppShell test harness); adding scaffolding would double the change size and blur the surgical intent, and the fix is symmetric with the existing tested click-handler pattern. Verification all green: `npm run type-check` clean, `npm test -- pretty-conversations --run` 36/36 across 2 files (no regressions), `npm run build` 5.56s. Grep gates: both insertion patterns found verbatim at their target lines. `git diff --stat src/ui/AppShell.tsx` shows exactly +2/-0 in one file. Single atomic commit on `feat/tab-title-from-tmux`, NO Co-Authored-By trailer (fork convention). No push, no deploy — batched with patch #146 (log-forwarder prototype) which lands next per Ashley 2026-07-23 batch-writeups-until-deploy rule. `~/.claude/identities/tina/skynet-patches.md` write-up deferred to the deploy batch. | 2026-07-24 | efc8e87 | [260724-aiu-patch-145-fix-active-glow-on-url-restore](./quick/260724-aiu-patch-145-fix-active-glow-on-url-restore/) |
| 260717-vbw | Pretty-view WIP indicator (patch #51) — JSONL state-machine spinner bubble | 2026-07-17 | caafaa5 | [260717-vbw-work-in-progress-indicator-for-pretty-vi](./quick/260717-vbw-work-in-progress-indicator-for-pretty-vi/) |
| 260718-2dt | Message queue drawer auto-closes when send empties the queue | 2026-07-18 | 5f209ff | [260718-2dt-message-queue-drawer-auto-closes-when-se](./quick/260718-2dt-message-queue-drawer-auto-closes-when-se/) |
| 260718-340 | Fix message queue sync bugs (patch #55) — keepalive delete + full dirty flush on unload + 10s interval retry | 2026-07-18 | f4b845e | [260718-340-fix-message-queue-sync-bugs-patch-55-kee](./quick/260718-340-fix-message-queue-sync-bugs-patch-55-kee/) |
| 260718-43f | Fix pretty-view context% false-positive (patch #56) — bottom-8-lines slice + bar-glyph fallback | 2026-07-18 | 17c4079 | [260718-43f-fix-pretty-view-context-false-positive-m](./quick/260718-43f-fix-pretty-view-context-false-positive-m/) |
| 260718-4oi | Persist pretty-view ComposeBox draft body server-side per pane (patch #57) | 2026-07-18 | 4579ca7 | [260718-4oi-persist-pretty-view-composebox-draft-bod](./quick/260718-4oi-persist-pretty-view-composebox-draft-bod/) |
| 260718-87h | Backgrounded-agents panel in pretty view (patch #61) | 2026-07-18 | fe506e0 | [260718-87h-backgrounded-agents-panel-in-pretty-view](./quick/260718-87h-backgrounded-agents-panel-in-pretty-view/) |
| 260718-8tk | Plan-mode pending indicator in pretty view (patch #63) | 2026-07-18 | fb65084 | [260718-8tk-patch63-plan-mode-pending-indicator](./quick/260718-8tk-patch63-plan-mode-pending-indicator/) |
| 260718-s52 | Backgrounded-shells panel in pretty view (patch #68) | 2026-07-18 | 0a9d7a6 | [260718-s52-patch-68-add-backgrounded-shells-panel-t](./quick/260718-s52-patch-68-add-backgrounded-shells-panel-t/) |
| 260719-1mn | Prettify slash-command triplets in pretty-view chat bubbles | 2026-07-19 | b7ed756 | [260719-1mn-prettify-slash-command-triplets-in-prett](./quick/260719-1mn-prettify-slash-command-triplets-in-prett/) |
| 260719-4p8 | Identity-aware ComposeBox placeholder (patch #71) — "Message {displayName}…" | 2026-07-19 | 1a97a87 | [260719-4p8-replace-hard-coded-message-claude-placeh](./quick/260719-4p8-replace-hard-coded-message-claude-placeh/) |
| 260719-4yz | WIP indicator on BG agents/shells + strip bubble (patch #72) | 2026-07-19 | 9dfc406 | [260719-4yz-wip-indicator-on-bg-agents-shells-strip-](./quick/260719-4yz-wip-indicator-on-bg-agents-shells-strip-/) |
| 260719-5eh | Pretty-view auto-activate on identity resolution (patch #73) | 2026-07-19 | 21089f3 | [260719-5eh-pretty-view-auto-activate-on-identity-re](./quick/260719-5eh-pretty-view-auto-activate-on-identity-re/) |
| 260719-5ym | Centered blocking session-holding overlay (patch #74) — replaces top-bar banner with backdrop-blur scrim + glass card | 2026-07-19 | 72c4bd4 | [260719-5ym-patch-74-replace-pretty-view-top-bar-ses](./quick/260719-5ym-patch-74-replace-pretty-view-top-bar-ses/) |
| 260719-tjk | Cohesive-instrument compose meter (patch #83) — segmented meter well with integrated reset cell + top-to-bottom drain animation | 2026-07-19 | a4d38eb | [260719-tjk-patch-83-cohesive-instrument-compose-met](./quick/260719-tjk-patch-83-cohesive-instrument-compose-met/) |
| 260719-u29 | Queue button with isIdle watchdog + textarea pending overlay (patch #84) — Hourglass button between ThumbsUp and Send, fires message after 3s continuous idle | 2026-07-19 | 317ad17 | [260719-u29-patch-84-queue-button-hourglass-icon-in-](./quick/260719-u29-patch-84-queue-button-hourglass-icon-in-/) |
| 260719-uqx | Bump WipBubble spinner size (patch #85) — h-5 w-5 → h-7 w-7 (20px → 28px) | 2026-07-19 | d818d9c | [260719-uqx-patch-85-bump-wipbubble-spinner-size-fro](./quick/260719-uqx-patch-85-bump-wipbubble-spinner-size-fro/) |
| 260719-vil | Pretty-view image support (patch #86) — WS-inline b64 render of tool_result image blocks | 2026-07-19 | ab20b18 | [260719-vil-add-pretty-view-image-support-patch-86-w](./quick/260719-vil-add-pretty-view-image-support-patch-86-w/) |
| 260719-w8h | Pretty-view identity modal (v1 read-only bounties) — click badge → tabbed modal with current identity's bounties | 2026-07-19 | f17924f | [260719-w8h-pretty-view-identity-modal-v1-read-only-](./quick/260719-w8h-pretty-view-identity-modal-v1-read-only-/) |
| 260719-wyt | Pretty-view scroll new message to top of viewport when taller than viewport (patch #88) | 2026-07-19 | d6e40d1 | [260719-wyt-pretty-view-scroll-new-message-to-top-of](./quick/260719-wyt-pretty-view-scroll-new-message-to-top-of/) |
| 260720-17g | Identity modal — fill out Identity/History/Wakeups/Handoff tabs + rename Standing Directives → Identity + move to front | 2026-07-20 | 65d9577 | [260720-17g-identity-modal-tabs-identity-renamed-fro](./quick/260720-17g-identity-modal-tabs-identity-renamed-fro/) |
| 260720-3n2 | Identity modal cross-machine fetch (patch #92) — SSH to pane's host for identity artifacts | 2026-07-20 | 168b40d | [260720-3n2-identity-modal-cross-machine-fetch-ssh-t](./quick/260720-3n2-identity-modal-cross-machine-fetch-ssh-t/) |
| 260720-6rl | Pretty-view scroll model: clamp-anchor + Slack-follow (patch #96) — replaces broken patch-#88 scroll-to-top + broken GTG bottom-scroll with unified `scrollTop=min(followBottomTop, anchorPinTop)` state machine | 2026-07-20 | 3908b8b | [260720-6rl-pretty-view-scroll-model-clamp-anchor-sl](./quick/260720-6rl-pretty-view-scroll-model-clamp-anchor-sl/) |
| 260720-7m1 | Broaden pretty-view harness-wrapper filter (patch #97) — fixes #96 anchor-doesn't-hold bug caused by combined `<system-reminder>` + `<task-notification>` wakes slipping through strict startsWith/endsWith filter | 2026-07-20 | 0381b57 | [260720-7m1-broaden-pretty-view-harness-wrapper-filt](./quick/260720-7m1-broaden-pretty-view-harness-wrapper-filt/) |
| 260720-8nj | Gesture-based mode flip in useAutoScroll (patch #98) — replaces rAF-counter scroll-event heuristic with wheel/touchmove/keydown listeners; delayed programmatic scrolls no longer misfire as user gestures. Diagnosed via live console diagnostic. | 2026-07-20 | 7edd1d8 | [260720-8nj-replace-scroll-event-mode-flip-with-real](./quick/260720-8nj-replace-scroll-event-mode-flip-with-real/) |
| 260720-ama | 3D orb WIP indicator — canvas-rendered Fibonacci-lattice sphere (150 dots, dual-axis tumble, depth-modulated size/alpha) replaces Loader2 spinner in WipBubble. Ashley-approved winning variant after 4 rounds of tailnet-served prototyping. Awaiting deploy + pin. | 2026-07-20 | 54e06cd | [260720-ama-3d-orb-wip-indicator-canvas-based-fibona](./quick/260720-ama-3d-orb-wip-indicator-canvas-based-fibona/) |
| 260722-ctq | Hybrid compose-submit path (patch #118) — replaces CR-in-PTY submit tail with `tmux send-keys Enter` via exec on same sshConn (body still PTY-written; falls back to CR-in-PTY when tmuxSessionName is null). Supersedes patches #100/#111a which tried paste-detection delay tuning. Empirically validated in session prototype (10/10 steady, multi-line preserved). Awaiting deploy. | 2026-07-22 | 7d6506f | [260722-ctq-fix-messages-land-in-box-not-submitting-](./quick/260722-ctq-fix-messages-land-in-box-not-submitting-/) |
| 260722-ddg | Drafts belt-and-suspenders localStorage mirror (patch #119) — client-side mirror for compose-box + message-queue drafts survives container-restart draft loss regardless of any server-side failure mode. Every keystroke + every successful debounced server save writes to `localStorage`; on mount if the server returns empty and ls has content, restore and schedule an autosave. Extracts `scheduleItemAutosave` from `handleBodyChange` so the hydrate loop can reuse the same 400ms PATCH machinery. Four diagnostic `console.warn` lines (2 per surface) log serverLen vs lsLen for the follow-up root-cause bounty. Deploy deferred to a batch after bounties #3-5. | 2026-07-22 | 58d3c83 | [260722-ddg-patch-119-drafts-belt-and-suspenders-loc](./quick/260722-ddg-patch-119-drafts-belt-and-suspenders-loc/) |
| 260722-dwe | Compose-box stop button (patch #120) — Ctrl-C safety valve for when Claude Code goes rogue mid-run from the pretty view. Square-icon `<Button>` in ComposeBox aux group (left of ThumbsUp, warm-neutral Glass; not `canSend`-gated). New WS `{ type: "interrupt" }` triggers backend `case "interrupt":` which fires `tmux send-keys -t <session> C-c` on the same multiplexed sshConn as patch #118, with raw `\x03`-byte PTY fallback for non-tmux panes / exec errors. No `setTimeout` wrapper (Ctrl-C interrupts Ink regardless of paste-detection framing). Two `sshLogger.info` events for diagnostic pinning. Deploy deferred: batched with #118-#122. | 2026-07-22 | 1bd0fd8 | [260722-dwe-patch-120-compose-box-stop-button-ctrl-c](./quick/260722-dwe-patch-120-compose-box-stop-button-ctrl-c/) |
| 260722-eea | Remove vestigial send button (patch #121) — pure trim of `<Button><Send/></Button>` block, `Send` lucide import, and dead `sendDisabled` derived state from ComposeBox. Bounty `send-button-bigger` re-scoped mid-session after Ashley confirmed Enter (patch #118) is her sole submit path; cleaner aux row: Paperclip / Square (stop) / ThumbsUp / Hourglass. Single file, +4/-28. tsc clean. Deploy deferred: batched with #118-#122. | 2026-07-22 | f452c2e | [260722-eea-patch-121-remove-vestigial-send-button-f](./quick/260722-eea-patch-121-remove-vestigial-send-button-f/) |
| 260722-eqv | Meter reset triggers session-holding overlay instant + zero-lock + red-bubble failure (patch #122). Three coordinated fork-side mechanics on the existing Phase-3 session-recycling state machine, backend untouched: (1) `onResetClicked?` callback threaded PrettyView→ComposeBox, fired synchronously at top of `handleResetSend` so patch #74's 350ms overlay delay-arm starts NOW instead of waiting for the backend `session_holding` WS frame; (2) `isHolding?` prop on ComposeBox adds `&& !isHolding` to the meter `isLit` conjunction so all 12 segments render unlit for the whole recycle window (well/border/glow/reset-cell unchanged, drain-sweep #83 unchanged); (3) `holdingTimeoutError` state → `error?` prop on SessionHoldingOverlay drives a warm-red variant (RefreshCcw `hsl(0,72%,60%)` matching meter red-band palette, subtle warm-red inset card glow, "Session recycle failed — refresh to check", aria-label swap, NO `animate-spin` — motion guardrail #72 preserved), triggered by either the backend `inactive { reason: 'holding_timeout' }` frame OR a client-side 120s belt-and-suspenders `useEffect` timer (survives WS drop). `case "inactive"` split by reason so holding_timeout doesn't unmount the compose box. tsc clean, +159/-8 across three files, no tests touched. Closes the fifth of five 2026-07-22 bounties. Deploy deferred: batched with #118-#121. | 2026-07-22 | 4bfa2e5 | [260722-eqv-patch-122-meter-reset-triggers-session-h](./quick/260722-eqv-patch-122-meter-reset-triggers-session-h/) |
| 260722-g2x | Decouple paperclip visibility from touch-target height (patch #123) — splits the overloaded `showPaperclip` prop into two independent gates on ComposeBox: `showPaperclip` still governs paperclip Button mount, new `isTouchDevice` prop now governs the Row 1 `min-h-[44px]` vs `min-h-8` decision. PrettyView mount site rewired to pass `showPaperclip={true}` unconditionally (post-#121 aux-row has room) and `isTouchDevice={isTouchDevice}` from the existing `useIsTouchDevice()` const (patch #102, unchanged). Two Phase 9 Layout row-height tests re-anchored to the new prop (titles + `baseProps` calls updated; assertions unchanged); Tests 3 & 4 (paperclip visibility) untouched — the prop's ONLY concern is still visibility there. Ashley now gets the paperclip on desktop pretty-view in the compact row. tsc clean, +45/-17 across 3 files. Vitest: all patch-#123-relevant tests green (Tests 3, 4, both updated Phase 9 layout tests, meter horizontal, textarea rows); 3 pre-existing failures from patch #121's stale Send-button test references logged as deferred follow-up (out of patch #123 scope per GSD SCOPE BOUNDARY rule). Deploy deferred: batched with #118-#122. | 2026-07-22 | c656254 | [260722-g2x-patch-123-decouple-paperclip-visibility-](./quick/260722-g2x-patch-123-decouple-paperclip-visibility-/) |
| 260722-mp6 | PWA safe-area polish (patch #126) — 3-file +2/-2 diff resolving Ashley's UAT feedback on the #125 iOS install. (1) `body { background-color: var(--background) }` in src/ui/index.css so top/bottom safe-area regions render Skynet-gray (cascades via `.dark` ancestor → `--background: oklch(0.155 0.004 128.73)`) instead of browser-default black. (2) Drop `paddingBottom: max(env(safe-area-inset-bottom), 0px)` from the AppShell outer div style (src/ui/AppShell.tsx:1738-1742) — shell bg now extends to viewport bottom edge; `paddingTop` preserved so status-bar clock/battery stay readable. (3) Append `pb-[env(safe-area-inset-bottom)]` to the ConversationsPanel scroll container className (src/ui/sidebar/ConversationsPanel.tsx:229) so the last item rests above the iOS home indicator; desktop no-op via env()=0. No scope creep: no root-cause chase for 100dvh-vs-flex, no preemptive padding on other scroll containers, manifest/index.html/icons/nginx untouched. tsc clean, git diff --stat shows exactly the 3 target files, all four plan `<verify>` assertions pass. No push, no build, no deploy — batched with #118-#125 behind Tina's 15-min deadman rollback pending Ashley greenlight on visual behavior. | 2026-07-22 | 0f87d02 | [260722-mp6-patch-126-pwa-safe-area-polish-blend-bod](./quick/260722-mp6-patch-126-pwa-safe-area-polish-blend-bod/) |
| 260722-i1r | Skynet rebrand + PWA install (iOS) + zoom lock (patch #125) — six-workstream PWA install polish for Ashley's iPhone. (1) index.html head rewrite: viewport zoom-lock (maximum-scale=1, user-scalable=no, viewport-fit=cover), theme-color #080808 (Skynet-locked), mobile-web-app-capable, apple-mobile-web-app-title "Skynet", 5 apple-touch-icon links (180/152/120/76/60), 2 favicon PNG links (32/16), manifest link → /manifest.webmanifest, <title> "Skynet"; scrollbar <style> and window.__SKYNET_BASE_PATH__ preserved. (2) New public/manifest.webmanifest — Skynet name/short_name, standalone, portrait-primary, #080808 colors, 192+512 icons. (3) 10 icon PNGs copied to public/ (1024 master intentionally NOT shipped). (4) Both nginx confs updated (CLAUDE.md symmetry) — symmetric `location = /manifest.webmanifest` block with `types { } + default_type application/manifest+json;` in HTTP and HTTPS confs. (5) Safe-area CSS: body { overscroll-behavior: none; } + .safe-top utility beside .safe-bottom; AppShell outer div extended with `paddingTop/paddingBottom: max(env(safe-area-inset-*), 0px)` inline (browser-tab mode unaffected). (6) 13 user-visible Skynet → Skynet renames — 5 TSX/TS (AppShell document.title fallback, main-axios server-error toast, AdminIdentitiesSection maintainer placeholder, HostEditorGuacamoleTabs Drive + client-name placeholders) + 8 en.json string values (source locale only; translated/*.json crowdin-managed). Internal identifiers preserved for rebase-ability: clearSkynetSessionStorage, skynet:* event names, Skynet-Mobile UA + regex matchers, SkynetAlert type, resolveSkynetThemeColors, "Skynet Dark/Light/Default" theme registry keys, github.com/Skynet-SSH URLs, fork-history code comments, package.json name, docker image name. Mid-execution SKIP list expansion: added terminal-themes.ts:32 "Skynet Default" registry display-name (same rationale as HostEditorData Skynet Dark/Light — paired with unrenamable `skynet` config key). tsc clean at edited lines (pre-existing type-debt at nearby lines unrelated); vitest 473/477 passing, 4 pre-existing ComposeBox failures (patch #121 Send-button residual + patch #124 "yes"→"let's go" ThumbsUp aria-label residual; STATE.md's "3-failure baseline" one patch stale). No push, no build, no deploy — SHIPPED locally, batched with #118-#124 behind 15-min deadman. | 2026-07-22 | f5019ea | [260722-i1r-patch-125-skynet-rebrand-pwa-install-ios](./quick/260722-i1r-patch-125-skynet-rebrand-pwa-install-ios/) |
| 260723-6xb | Patch #133 — strip shadcn wrappers from PrettyConversationsPanel (DropdownMenu/Tooltip/renderSettingsMenuItems/Settings-icon gone, showGear JSX branch deleted, onRailClick+isAdmin props dropped from typedef + AppShell call-site), inline 4 shadcn text-color sites to prototype hex/rgba (`text-[rgba(240,234,224,0.9)]` desktop title / `text-[#f0eae0]` pencil / `text-[#5c6070]/50` RDP monitor + label), port header padding `px-3 py-2` → `px-4 py-3` (matches prototype `.top-strip` 12px/16px). Skynet base color rebase to `#0a0b12` (= `--color-pv-base-end`, darkest anchor iOS blends into safe-area) across 4 sites: `.dark { --background }` in src/ui/index.css (`oklch(0.155 0.004 128.73)` → `#0a0b12`, drops the green Skynet tint), `theme_color` + `background_color` in `public/manifest.webmanifest`, same in `public/manifest.json`, `<meta name="theme-color">` in `index.html` (repo root, not `src/ui/`). Tests 9+10 rewritten (Test 9 flipped to invariant "gear NEVER renders on desktop"; Test 10 drops removed props); 27/27 pretty-conversations tests pass. `NewSessionDialog` + rows + PinAction untouched (already clean per prototype lock). Grep gates: no shadcn residuals in panel; `#0a0b12` present ≥7 times across the 4 target files; no `#080808`/`#09090b`/`#080808` residuals in manifests/index.html. tsc `--noEmit` clean; full-tree vitest 504/506 (2 pre-existing patch #124 ThumbsUp aria-label residuals unchanged); `npm run build` 7.87s. Bundles with #131b (558749a), #131c (68e4f62), #132A (b749bf1), #132B (536d224) for one grouped deploy pending Ashley greenlight. Post-deploy iOS PWA reinstall required (remove + re-add from home screen) for the `#0a0b12` safe-area seam disappearance to be visible — iOS caches manifest colors at install time; desktop browsers pick up the new background immediately. | 2026-07-23 | 3d28512 | [260723-6xb-patch-133-strip-shadcn-wrappers-from-pre](./quick/260723-6xb-patch-133-strip-shadcn-wrappers-from-pre/) |
| 260723-2cg | Subtle inside-textarea Send button (patch #129) — Ashley-locked via DevTools console iteration (ChatGPT/iMessage-quiet, not amber-pop): bare `<button type="button">` (sidesteps shadcn wrapper specificity trap that bit #81/#117), absolute right-3 bottom-2.5 inside the existing `<div className="relative flex-1 self-stretch">` textarea wrapper that already hosts the queueArmed overlay. 40×40 hit target via `p-2` around a 24×24 lucide `SendHorizontal` with `fill="currentColor"`. Colors verbatim per Ashley: rest `text-[rgba(240,235,224,0.3)]`, hover `text-[rgba(240,235,224,0.9)]`, disabled `text-[rgba(240,235,224,0.15)]` + `cursor-not-allowed`. `transition-[color,transform] duration-120`, `active:scale-95`. Textarea gets `pr-10` (40px right padding) so typed text doesn't slide under the icon. Local `sendDisabled` const: `queueArmed || (canSend === false && !hasAttachments) || (text.trim() === "" && !hasAttachments)` — executor caught planner's `!canSend` (would over-disable when undefined) and corrected to strict `canSend === false` matching every other button in the file. onClick routes entirely through the existing `handleSend()` at ~line 652 — no branching duplication (attachment path, D-50 newline collapse, COMPOSE-04 clear-on-success all preserved). aria-label="Send", title="Send". Tests: 3 new (renders as bare button inside textarea wrapper; click-with-text calls onSend with trimmed payload + clears textarea; disabled Case A empty text + no attachments, Case B canSend=false + empty text). Bundled selector-hygiene fix: 3 pre-existing failing tests (Test 7, Test 8, Phase 9 aux-row) with orphaned `getByLabelText(/send message/i)` selectors from patch #121's Send-button removal updated to `getByRole('button', { name: 'Send' })`. Bundled test-hygiene: `localStorage.clear()` in both `beforeEach` blocks — patch #119 draft-ls mirror was bleeding between tests silently over-disabling Send. tsc clean, vitest 16/18 (2 remaining pre-existing failures are patch #124 `/send 'yes'/i` ThumbsUp residual — both aux-row + desktop min-h-8 hit the same stale selector so they fail together, out of scope per plan), `npm run build` succeeds in 8.87s. Two files, +131/-6. Stacks on the six-patch stack #123-#128 pending Ashley UAT + explicit deploy greenlight. | 2026-07-23 | 37986b2 | [260723-2cg-add-a-subtle-inside-textarea-send-button](./quick/260723-2cg-add-a-subtle-inside-textarea-send-button/) |
| 260723-9nv | Compose-box textarea auto-grow with contents (patch #135) — replaces the newline-count `rows = Math.min(6, text.split("\n").length)` heuristic at ComposeBox.tsx:517 (which left long single-line messages clipped because wrapped visual lines never add a `\n`) with a `useLayoutEffect` on `[text]` that drives `el.style.height` imperatively off `scrollHeight`, capped at 6 line-heights. Mechanic: `el.style.height='auto'` first (mandatory — without it, scrollHeight only grows, never shrinks on delete), then `Math.min(el.scrollHeight, maxHeightPxRef.current)`, then set `overflowY='auto'` only at the cap so the scrollbar appears only when the 6-row ceiling is hit. `maxHeightPxRef` (new `useRef<number|null>(null)`) is computed once on first pass from `parseFloat(getComputedStyle(el).lineHeight) × 6` with 144px fallback for JSDOM `normal`-keyword branch where parseFloat resolves to NaN. `useLayoutEffect` imported alongside existing React hooks. `<Textarea rows={rows}>` → `<Textarea rows={1}>` (SSR/initial-paint fallback; effect immediately overrides on mount). All other styling untouched: `min-h-8!` (32px CSS floor for empty state via the Tailwind v4 important-suffix cascade beats shadcn's `min-h-[80px]` base), `resize-none`, `bg-[rgba(10,12,20,0.5)]!`, `pr-10` (patch #129 inside-Send padding), border, focus ring, warm-black well, aux buttons, meter well, queueArmed overlay, inside-textarea Send button (position anchored to wrapper, not textarea, so no adjustment needed). Tests: 2 new in a new `describe("ComposeBox — patch #135 auto-grow", ...)` block with `localStorage.clear()` in `beforeEach` (patch #129 hygiene). Test A: on mount with empty text, `parseFloat(textarea.style.height || '0') <= 32` — JSDOM scrollHeight=0 → height='0px' satisfies the min-h-8 floor. Test B: mock `HTMLTextAreaElement.scrollHeight = 100` via `Object.defineProperty` after mount, fire `change` event, assert `style.height === '100px'` and `overflowY === 'hidden'` (100 < 144 fallback MAX_PX). tsc `--noEmit` clean; vitest 18/20 on ComposeBox.test.tsx (baseline was 16/18 — same 2 pre-existing patch #124 `/send 'yes'/i` ThumbsUp aria-label failures at `:393` and `:452`, no new regressions; delta is +2 new tests both green). No `npm run build` (fork rule — quick task ships code + tests only), no deploy, no push, no touch to `~/.claude/identities/tina/skynet-patches.md`. Two files, +65/-7. Single commit on `feat/tab-title-from-tmux` — patch #135 sits atop #134 ready for Tina's next deploy batch. | 2026-07-23 | 53b1c7b | [260723-9nv-compose-box-textarea-auto-grow-with-cont](./quick/260723-9nv-compose-box-textarea-auto-grow-with-cont/) |
| 260723-bbt | Conv-list active-set + ready-for-attention dot + session-working store (patch #137) — wires two visual signals per Ashley's LOCKED 2026-07-23 spec on top of #136's every-row bubble+badge treatment. NEW `src/ui/state/session-working-store.ts` — module-scoped `Map<string, boolean|null>` + `useSyncExternalStore` pattern mirroring `conversation-store.ts` verbatim, with `publishSessionWorking / useSessionWorking / getSessionWorkingSnapshot / __resetForTest` exports; publishing `null` OVERWRITES (does not delete) so a remount observing null after a known transition is semantically correct; no sessionStorage/localStorage layer (in-memory only — page refresh resets, the next isIdle emit from each mounted Terminal.tsx re-populates). `Terminal.tsx` +13 lines: one `useEffect` on `[isIdle, hostConfig.id, tmuxSessionName]` immediately after the existing `isIdle` useState (~line 245) emits `publishSessionWorking(`${hostId}:${tmuxSessionName ?? ""}`, isIdle === null ? null : isIdle === false)`; no cleanup (preserve last-known state across route changes per §1); every other Terminal.tsx line byte-identical. `conversation-store.ts` extended: `State.activeSet: Set<string>` field hydrated at module init from `sessionStorage.getItem("pv-conv-active-set")` via try/catch + strict `Array.isArray` + per-element `typeof v === "string"` filter (SSR/JSDOM/quota-exceeded silent-fail); new `addToActiveSet(id)` idempotent no-op when present, otherwise builds a new Set + persists via `sessionStorage.setItem` (silent try/catch) + updates + notify; new `useActiveSet(): ReadonlySet<string>` mirroring `usePinnedIds`; `selectConversation(id)` wired to call `addToActiveSet(id)` for non-null ids AFTER stale-guard passes but BEFORE the same-selectedId short-circuit (re-select still counts as engagement; addToActiveSet idempotent so cheap no-op). Test-only `__resetActiveSetForTest()` added because the API is deliberately grow-only within a session (no `removeFromActiveSet` per §3) but tests still need per-test isolation. `PrettyConversationRow.tsx` prop diff: REMOVED `isWip?: boolean` → ADDED `isWorking?: boolean | null` (default `null`) + `inActiveSet?: boolean` (default `false`); dot render inverted to `inActiveSet === true && isWorking === false` (strict equality — null and true both suppress); dot rebranded aria-label="ready", `data-pv-conv-ready-dot="true"`, steady (NO animation string), hue-cream fill `hsla(H, 60%, 80%, 1)` with hue outer glow + warm inset per prototype v4 (neutral rgba(240,235,224,…) fallback for hue==null). NEW `isAmbient = !isRdp && !inActiveSet` derivation drives an ambient recession branch in `avatarStyle` / `baseBodyStyle` / `hoverOverlay` / label span / host line: flat `hsla(H, 40%, 20%, 0.16)` background (NOT linear-gradient), 0.14-alpha border, minimal inset + hairline shadow, `backdrop-filter: none`, `color: rgba(251,245,232,0.72)`; ambient hover shifts background/border-color only (NO transform lift, NO shadow boost); label `font-medium` + `textShadow: none` + `fontWeight: 500`; host icon + name color `rgba(255,235,190,0.45)`. RDP rows EXEMPT from ambient (isRdp short-circuits isAmbient); RDP + inActiveSet + isWorking===false still allowed to render dot but panel's sessionWorkingKey for RDP resolves to a well-formed key with null store entry → suppressed in practice. `PrettyConversationsPanel.tsx`: NEW `PrettyConversationRowLive` micro-component ABOVE the exported panel wraps `PrettyConversationRow` with a per-row `useSessionWorking(sessionKey)` call at a stable top-level hook site (Rules-of-Hooks compliance inside `.map()`) — the SAFER pattern from Task 4's §note; new `sessionWorkingKey(row)` helper returns `${row.host.id}:${row.targetTmuxSession ?? ""}` when host defined, else null; `useActiveSet()` hoisted once alongside the existing hook trio; all 3 `<PrettyConversationRow ... isWip={false} />` render sites (pinned map, regular host map, RDP-sentinel map) replaced with `<PrettyConversationRowLive ... inActiveSet={activeSet.has(row.id)} sessionKey={sessionWorkingKey(row)} />`. `index.css` -29 lines: removed dead `@keyframes pv-conv-wip-pulse`, the reduced-motion `[data-pv-conv-wip-dot]` fallback, and the patch #136 header comment — steady ready-dot needs neither pulse nor reduced-motion. Tests: +4 session-working-store, +3 conversation-store activeSet, +5 PrettyConversationRow (Tests 15/16/17/18/18b — the ambient body-style probe uses regex-based checks because jsdom normalizes hsla→rgba inside `background` values but preserves hsla in `box-shadow`, mirroring Test 12's precedent), +2 renamed/inverted (Tests 13/14), +2 pre-existing tests patched with explicit `inActiveSet={true}` (Tests 1/12) to preserve their full-bubble assertion; PrettyConversationRow suite 20/20; conversation-store suite 38/38; session-working-store suite 4/4. Bundled Rule-1 deviation: `PrettyConversationsPanel.test.tsx` (NOT in files_modified budget — 10th file) mock extended with `useActiveSet: () => new Set<string>()` + a new `vi.mock("@/state/session-working-store", () => ({ useSessionWorking: () => null }))` block; without these 15 pre-existing tests fail with "No 'useActiveSet' export is defined on the mock" error. Existing Test 12 (reactive emit semantics) count 4→5 because selectConversation now fires two notify() on a first-time-selected id (addToActiveSet + selectedId change). tsc `--noEmit` clean; full-tree vitest 521/523 (2 pre-existing patch #124 ThumbsUp aria-label failures unchanged from Phase 10 baseline; net-delta +17 tests). No `npm run build`, no deploy, no push, no touch to `~/.claude/identities/tina/skynet-patches.md`. Ten files, +863/-111. Single commit on `feat/tab-title-from-tmux` — patch #137 sits atop #136 ready for Tina's next deploy batch. NO Co-Authored-By trailer (fork convention). | 2026-07-23 | 9450526 | [260723-bbt-conv-list-active-set-ready-dot-working-s](./quick/260723-bbt-conv-list-active-set-ready-dot-working-s/) |
| 260723-agy | Conversation-list full pretty-view bubble+badge restyle (patch #136) — rebases `PrettyConversationRow`'s visual layer onto Ashley's locked v2 prototype (Full + Normal): every non-RDP hue-carrying row now renders the full pretty-view assistant-bubble intensity (`linear-gradient(160deg, hsla(H,50%,38%,0.55), hsla(H,45%,24%,0.60))` background, `1px solid hsla(H,65%,55%,0.32)` border, `0 8px 24px + inset 0 1px 0 rgba(255,220,170,0.18) + 0 0 0 0.5px hsla(H,70%,55%,0.20) + 0 0 32px hsla(H,70%,52%,0.18)` multi-stop shadow, `blur(20px) saturate(1.5)` backdrop) — not the prior 0.30/0.35 selected-only reduced-alpha treatment. Hover overlay (desktop-only, unselected) via new `hovered` useState + `onMouseEnter`/`onMouseLeave` handlers: `translateY(-1px)` + 0.42 border alpha + 0.28 hairline + 0.26 glow + `0 12px 28px` outer / 40px glow. Selected overlay: `translateY(-1px)` + 0.55 border alpha + `1px` hue ring (was 0.5px) + `0 14px 32px` outer + 56px glow. Layered via spread (`...baseBodyStyle, ...hoverOverlay, ...selectedOverlay, ...desktopBodyTransition, ...bodyTransformStyle`) so selected wins over hover and mobile swipe transform wins over desktop transition. Avatar disc rebuilt from `IdentityBadge.tsx:58-62 & :76` — `linear-gradient(160deg, hsla(H,45%,25%,0.72), hsla(H,40%,15%,0.82))` + `1px solid hsla(H,65%,55%,0.40)` border + three-stop `0 4px 12px + inset 0 2px 0 rgba(255,235,190,0.35) + 0 0 24px hsla(H,65%,55%,0.40)` shadow. Radial-gradient retired. RDP rows use neutral (60,65,80 / 30,33,44) glass on both body + avatar; hue-null non-RDP rows use cool-slate (45,55,80 / 28,35,55) at the same full alphas. Label switched `font-medium → font-semibold`, always `text-[#fbf5e8]` (dropped ternary), + inline `textShadow: "0 1px 2px rgba(0,0,0,0.4)"`. Host icon + name colored `rgba(255,235,190,0.65)` inline (dropped `text-muted-foreground/60`). `bodyBaseClass` dropped `border border-transparent` (border now in inline style) + `hover:bg-white/[0.03]` / `active:bg-white/[0.03]` (hover treatment now on bubble shadow/lift). New optional `isWip?: boolean` prop (default `false`) — when true, renders `<span aria-label="working" data-pv-conv-wip-dot="true" class="inline-block w-2 h-2 rounded-full">` as LAST child in right-meta column (after PinAction + pin glyph) with hue-based hsla background/glow (or neutral rgba fallback for hue==null) and inline `animation: "pv-conv-wip-pulse 1.35s ease-in-out infinite"`. New `@keyframes pv-conv-wip-pulse` (0.85→1.15 scale, 0.4→1.0→0.4 opacity) + `@media (prefers-reduced-motion: reduce) { [data-pv-conv-wip-dot="true"] { animation: none !important; opacity: 1; transform: scale(1); } }` in src/ui/index.css, co-located with pretty-view palette tokens after the `@theme inline` close. `PrettyConversationsPanel.tsx` threads `isWip={false}` as hardcoded literal at all 3 render sites (pinned, regular-host groups, RDP-sentinel group) — patch #137 will one-line-wire this to store subscription. All existing swipe / touch / keyboard / pin / RDP / a11y semantics preserved. Tests: 3 new (Test 12 unselected non-RDP row hue-bubble body style via regex-based check on gradient stops + hsla probe of box-shadow to survive jsdom's hsla→rgba normalization inside `linear-gradient()`; Test 13 isWip pulse dot with aria-label + data-pv-conv-wip-dot + pv-conv-wip-pulse animation; Test 14 neutral rgba fallback on hue-null RDP row asserting NO hsla anywhere). PrettyConversationRow tests 15/15; pretty-conversations suite 30/30. tsc `--noEmit` clean. No `npm run build`, no deploy, no push, no touch to `~/.claude/identities/tina/skynet-patches.md`. Four files, +367/-54. Single commit on `feat/tab-title-from-tmux` — patch #136 sits atop #135 ready for Tina's next deploy batch. | 2026-07-23 | 1b057ad | [260723-agy-conversation-list-restyle-full-pretty-vi](./quick/260723-agy-conversation-list-restyle-full-pretty-vi/) |
| 260724-8sr | Patch #144 — post-#143 UAT punch-list bundle (six fixes, one atomic commit) — closes gaps Ashley surfaced after the #141+#142+#143 deploy this morning. Fix (a) `Terminal.tsx` #143 v2: dropped the `ws.readyState === WebSocket.OPEN` early-return guard from the patch #143 visibilitychange useEffect — root cause of the "1/8 through 8/8 reconnecting" flash Ashley saw on iOS PWA foreground was that JS resumes with the stale WS ref still reading OPEN before the queued `close` event delivers, so the guard short-circuited my handler and let the delayed close event trigger scheduleReconnect against a stale WS. New v2 spec: unconditional full-reset + `connectToHost(cols, rows)` on visibility→visible whenever `!isUnmountingRef.current && !wasDisconnectedBySSH.current`; if the WS was genuinely still open, opening a fresh one is idempotent (tmux reattach). Hidden branch + SSH-terminated boundary unchanged. Test 10 spec INVERTED (was "WS.OPEN → no-op"; now "WS.OPEN → connectToHost fires"). Fix (b) `AppShell.tsx`: deleted the entire `{isMobileViewScreen && ...}` header-row block (lines 1549-1570) that rendered a legacy shadcn Button back-chevron (50×49) + Separator + `activeConversationLabel` span — redundant with the patch #142 fixed-position chevron at 1400-1444 which already says "Back to conversations" on touch devices. Both were rendering simultaneously on mobile-in-conversation per Ashley's diag (fixed at 8,8 z-30 + legacy at 0,0). Identity badge already surfaces conversation identity so the label was dead weight too. Bundled cleanup: removed now-unused `activeConversationLabel` derivation, `Separator` + `Button` imports, and dead `isMobileViewScreen` const (Rule 1 dead-code cleanup, executor deviation documented in SUMMARY). Fix (c) `index.css`: added `html { background-color: var(--color-pv-base-end); }` — Ashley's diag showed `body.bgColor = rgb(10, 11, 18)` (correct pv-base-end) but `html.bgColor = rgba(0, 0, 0, 0)` (transparent), so iOS PWA painted the ~1cm safe-area-inset-bottom black. Fix ONLY the color; separate follow-up will reclaim ~5/6 of the vertical space per Ashley's ask (trim compose `py-2` + adjust safe-area padding). Fix (d) `PrettyConversationsPanel.tsx`: added `useEffect(() => { if (selectedId) addToActiveSet(selectedId); }, [selectedId])` right after the hoisted `useActiveSet()` call — root cause of Ashley's "active conversations not lighting up / not glowing like the mock" (diag showed ALL 32 rendered rows had `pv-row pv-row--mobile ambient` class, i.e. NONE in activeSet, despite one being her currently-open session): `addToActiveSet` only fired from `handleRowSelect` (click path), so URL-fragment restore into `#tab=tmux:host:session&active=0` bypassed handleRowSelect entirely, left activeSet empty, and rows stayed `ambient` (flat quiet treatment) instead of the full pretty-view bubble glow. New useEffect catches every selectedId change regardless of path (click, URL restore, tab switch, programmatic) because they all funnel through `selectedId`. `addToActiveSet` idempotent so double-fires from click-that-also-changes-selectedId are harmless. Fix (e) `pretty-conversations.css` + `PrettyConversationsPanel.tsx`: added `.pv-panel-group { display: flex; flex-direction: column; gap: 8px; }` and applied `pv-panel-group` class to all 3 group wrappers (pinned rows now wrapped in an explicit group div; regular hosts group + RDP-sentinel group converted from bare `flex flex-col`). Root cause of Ashley's "no spacing between list items" was that my patch #142 `.pv-panel-scroll gap:8px` IS applied but only between GROUPS — individual rows within each group's `<div class="flex flex-col">` wrapper were edge-to-edge because the wrappers had no gap. 8px within-group matches 8px between-groups for consistent visual rhythm. Fix (f) `PrettyConversationsPanel.tsx`: removed the mobile-conditional gate on `pv-panel-header` so "Conversations" title renders on mobile too (was suppressed by a `showDesktopTitle`-style gate — Ashley confirmed the prior handoff note "deliberately left off per Phase 10 design" was WRONG and made up in a prior session). Test 8 spec inverted (mobile now positive-renders header). Tests: 4 patch #143 tests updated (Test 10 spec inverted, Test 6 baseline pin re-verified), 5 new tests added (2 for Fix d, 2 for Fix f, 1 for Fix e) — vitest 59/59 across 4 files (Terminal.wiring 16/16, PrettyConversationsPanel 20/20, plus adjacent suites). All verification gates green: `npm run type-check` clean; `npm run build` succeeded in 6.42s; 7/8 grep gates verbatim (`isMobileViewScreen` decrease was 1 vs plan's 2 due to dead-const cleanup, documented Rule 1 deviation); GuacamoleDisplay.tsx visibilitychange count unchanged (RDP handler untouched). Seven files, single atomic commit on `feat/tab-title-from-tmux` — patch #144 sits atop #143 ready for Tina's next deploy batch (which will bundle #141+#142+#143+#144, though #141-143 already shipped this morning so this next deploy is #144-only + any one-more Ashley surfaces). No `~/.claude/identities/tina/skynet-patches.md` write-up per batch-writeups-until-deploy rule. No deploy, no push. NO Co-Authored-By trailer (fork convention). | 2026-07-24 | 7420a6a | [260724-8sr-patch-144-post-143-uat-fixes-bundle-auto](./quick/260724-8sr-patch-144-post-143-uat-fixes-bundle-auto/) |
| 260724-7ow | SSH terminal auto-reconnect on PWA visibilitychange (patch #143) — fixes iOS PWA backgrounding failure mode where the 8-attempt exponential-backoff reconnect budget (2s→4s→8s cap, ~50s) burns through against a throttled iOS tab while Ashley is on another app, leaving `shouldNotReconnectRef=true` and the manual "Connection lost" overlay stuck even though the target's tmux was up the entire time. New sibling `useEffect` in `Terminal.tsx` placed immediately after the existing lifecycle-reset effect at line 328 (co-located with the reconnect refs it manipulates): (a) on `document.hidden===true`, clears any scheduled `reconnectTimeoutRef` timeout and resets `reconnectAttempts.current=0` so the budget isn't burned against a throttled tab; (b) on `document.hidden===false`, if `ws.readyState !== OPEN` AND `wasDisconnectedBySSH.current===false` AND `isUnmountingRef.current===false`, resets all reconnect state flags (`shouldNotReconnectRef=false`, `isReconnectingRef=false`, `isConnectingRef=false`, `reconnectAttempts=0`, `wasConnectedRef=false`, `wasDisconnectedBySSH=false`), clears `updateConnectionError(null)`, hides overlay via `setShowDisconnectedOverlay(false)`, and calls `connectToHost(terminal.cols, terminal.rows)`. Boundary preserved: `wasDisconnectedBySSH.current===true` (auth fail, target-side ssh session closed, `~.` typed) does NOT auto-reconnect — manual overlay Reconnect stays the affordance. Deliberate divergence from the manual Reconnect handler (Terminal.tsx:3024-3037): NO `terminal.clear()` on the visibility path — tmux repaint on reattach handles restoration, and the clear was the visible-flicker cause in the manual overlay path. Deps: `[terminal, updateConnectionError]` matching file convention (file-wide `eslint-disable react-hooks/exhaustive-deps` at line 1, refs + setState omitted, connectToHost is a stable function declaration). Tests: 1 new structural (Test 6 — regex-pinned baseline of `visibilitychange`/`document.hidden`/`terminal.clear()`/`wasDisconnectedBySSH.current` occurrence counts to catch drift; JSDoc mid-execution rephrased to avoid literal `terminal.clear()` token that would have inflated count from 8→9) + 4 new behavioral in a new `describe('Terminal - patch #143 visibilitychange auto-reconnect', ...)` block using the file's established byte-for-byte helper-mirror pattern (Tests 7/8/9/10 covering hidden-branch cancel+reset, visible-branch full reset+connectToHost fire, target-terminated boundary NO-fire, and WS.OPEN no-op — Test 10 is one of the plan's must_haves.truths so earned its own test). All verification gates passed: `npm run type-check` clean (repo has no `tsc` script — correct entry point is `type-check` → `tsc --noEmit`, deviation noted); `npm test -- terminal` 23/23 across 2 files (16/16 in Terminal.wiring.test.ts including all 5 new patch #143 tests); `npm run build` 4.58s; `grep -c "visibilitychange" src/ui/features/terminal/Terminal.tsx` → 2 (add + remove pair); `grep -c "visibilitychange" src/ui/features/guacamole/GuacamoleDisplay.tsx` → 2 (unchanged from HEAD — RDP handler untouched); `grep -c "terminal.clear()" Terminal.tsx` → 8 (baseline preserved — no new terminal.clear() in visibility path per Rule #4); `grep -c "wasDisconnectedBySSH.current" Terminal.tsx` → 12 (baseline 10, +2 for one guard read + one defensive reset in the visible branch). Two files, +44/-0 (Terminal.tsx single contiguous insertion at line 329) and +236/-0 (test file). Single commit on `feat/tab-title-from-tmux` — patch #143 sits atop #142 ready for Tina's next deploy batch (#141+#142+#143). No `~/.claude/identities/tina/skynet-patches.md` write-up per Ashley's 2026-07-23 batch-writeups-until-deploy rule. No deploy, no push. | 2026-07-24 | 20eb26e | [260724-7ow-ssh-terminal-auto-reconnect-on-pwa-visib](./quick/260724-7ow-ssh-terminal-auto-reconnect-on-pwa-visib/) |
| 260724-723 | Conversation-list mock v4 parity (patch #142) — five UAT-caught gaps closed atomically: (1) `.pv-panel-scroll` class applied to the panel's scroll region so the mock's `gap:8px` + `padding:14px 12px 18px` take effect; (2) `addToActiveSet(row.id)` hoisted to the top of `handleRowSelect` in `PrettyConversationsPanel.tsx` before the RDP/fleet-only branch dispatch so every row click engages the active set uniformly (patch #137's intent held only for the default branch — RDP + fleet-only rows escaped without engaging, which is most of Ashley's active session set); (3) `.pv-avatar .pv-avatar-fallback-icon svg { width: 18px; height: 18px; }` CSS rule scopes the RDP Monitor icon bump to avatar context (tab-bar callers unaffected — no `.pv-avatar` ancestor); (4) fixed sidebar-toggle chevron chrome promoted from bare-until-hover to always-visible at rest with `rgba(220,225,245,0.06)` bg + `--color-pv-border-quiet` border, hover bumped one notch further to `rgba(220,225,245,0.12)` + `--color-pv-border-quiet-strong`; (5) new `sidebarToggleOverlaps?: boolean` prop on `PrettyConversationsPanel` gated `!isMobile && !isTouchDevice && sidebarOpen` from AppShell, emits `data-sidebar-toggle-overlaps` attribute on `.pv-panel-header`, CSS rule adds `padding-left: 48px` when attribute is `"true"` so the "Conversations" title clears the ~40px fixed chevron footprint on desktop-when-sidebar-open. Test-file mock-hygiene delta: `addToActiveSet: () => {}` added to `PrettyConversationsPanel.test.tsx` mock (pre-existing tests 12/13 assertions unchanged — they assert `selectConversationSpy` NOT called for RDP/fleet clicks, still true after Fix 2). All four verification gates passed: `tsc --noEmit` clean, `npm run test -- pretty-conversations` 34/34 green, `npm run build` 6.39s, grep sanity confirmed. Three files (+CSS-file-in-same-repo), +26/-15. Single commit on `feat/tab-title-from-tmux`. No deploy — batched per Ashley's 2026-07-23 rule pending her explicit greenlight or the next semantic unit trigger. | 2026-07-24 | 414b28c | [260724-723-conversation-list-mock-v4-parity-row-gap](./quick/260724-723-conversation-list-mock-v4-parity-row-gap/) |
| 260728-rt7 | Phase 17 UAT gap-closure: two structural bugs Ashley caught in the live send/receive walk. **Bug 1 (outbound bubble ⚠ extraction failed)**: `session-file-parser.ts:detectRelayOutbound` extractor only matched literal `-d '{...}'` / `-d "{...}"`, but the fleet-standard send pattern (mine, every recv.sh, agent-relay skill) uses `-d "$(jq -n --arg b "$MSG" '{msgtype:"m.text",body:$b}')"` — no literal JSON in the command string, only a shell recipe. Every real fleet send failed extraction. **Ashley's Option D**: delete the extractor entirely, render the tool_use command as the bubble body. Faithful, unbreakable, no recipe-parsing debate. Wire-shape shrink cascaded through 5 touchpoints (parser return type, WS emit in `claude-session-server.ts`, wire type in `claude-session-api.ts`, bubble props Pick, `PrettyView.tsx` JSX) — all `body`/`extractError` stripped. Bubble now renders `rawCommand` as always-visible `<pre>` with `whitespace-pre overflow-x-auto max-h-[24rem]`. Opportunistic body-var grep (MSG=/BODY=/TEXT=) deferred with explicit code comment for a future bounty. **Bug 2 (long-inbound bubbles show raw pointer text instead of fetched body)**: both `relay-pointer-detect.ts` POINTER_REGEX and `relay-pointer.ts` WHITELIST_REGEX hardcoded `/^\/tmp\/relay-msg-[A-Za-z0-9._-]+\.txt$/`, but recv.sh writes to `$MSG_DIR/${safeev}.txt` where `MSG_DIR=$(dirname $SINCE_FILE)/messages` → `~/.claude/identities/<id>/relay-state/messages/<eventid>.txt`. Swapped both regexes lockstep to `/^\/home\/[a-z0-9_-]+\/\.claude\/identities\/[a-z0-9_-]+\/relay-state\/messages\/[A-Za-z0-9_-]+\.txt$/` — SSRF-safe (anchored, no `..`, no shell metachars, POSIX-safe char classes), matches every identity dir, no runtime discovery. Ashley's exact reproducer path `_j14UxhqP0NpJXLReeXBR0qPGh04JwNXDGneCrEyarWw.txt` verified passing. Em-dash boundary preserved by `\s`-adjacent ASCII space. Added negative tests for uppercase-user + identity-name traversal (old regex had no such coverage). Tests: 4 outbound-bubble tests rewritten/renamed + 2 new (multiline preservation, overflow structural assertion); 2 backend + 2 frontend negative tests for the pointer regex; Ashley's exact reproducer as a dedicated test on both sides. `npm run build:backend && npm run build` clean (patch #154 lesson honored); 770 pass / 6 skip across 67 files. Two atomic commits on `feat/tab-title-from-tmux`: 34e29dc (Bug 1) + 1bd5967 (Bug 2). NOT pushed, NOT built, NOT deployed per code-work-doesn't-authorize-ship rule (Ashley 2026-07-27). Bundled ship queued with kiro-cli fix (67f94a4) + Phase 17 patch #169 pin, single deploy. | 2026-07-28 | 1bd5967 | [260728-rt7-fix-phase-17-uat-gaps-outbound-bubble-re](./quick/260728-rt7-fix-phase-17-uat-gaps-outbound-bubble-re/) |
| 260728-r1h | Fix Kiro CLI wrapper defeating claude-session detection — walk pane_pid descendants for `claude` comm. Root cause verified live on thenasty via SSH: AWS Kiro CLI's `kiro-cli-term` pty wrapper sets `argv[0]='bash'` on the shell it launches, so `/proc/<pane_pid>/cmdline` reads `bash (kiro-cli-term)` and tmux's `pane_current_command` returns `bash` even though `/proc/<pane_pid>/comm` is `kiro-cli-term`. Detection at `session-file-discovery.ts:47-51` did strict `trimmedCommand === "claude"` exact-match → returned `not_claude` for every thenasty pane, meanwhile `claude` was running as a grandchild (kiro-cli-term → bash --login → claude). All 5 thenasty identities (beatrice, nelly, shrok, vicky, yolanda) rendered "no active Claude session" fallback in pretty-view. Fix: replaced strict pane_current_command match with a POSIX `ps -eo pid=,ppid=,comm=` + awk fixed-point BFS descendant-tree walk seeded from `pane_pid`. Emits the first PID with `comm='claude'` anywhere in the tree (including `pane_pid` itself as the backcompat direct-claude case). Uses that PID for the existing CWD/JSONL discovery script (unchanged byte-for-byte except `${pid}` → `${claudePid}`). Handles wrappers of arbitrary depth without hardcoding. `queryPaneCurrentCommand` import removed; `pid_unavailable` kept in the type union for backcompat but no longer emitted (folded into `no_tmux_session`). Wire-type unaffected — frontend types `reason` as bare `string` and only switches on `holding_timeout`. Tests: 8 new vitest cases (Kiro wrapper 3-level, deeper 4-level wrapper, direct-claude backcompat, no-claude tree, null pane_pid, walk timeout, CWD reject, CWD empty). Backend typecheck via `npm run build:backend && npm run build` clean (patch #154 lesson honored); 766/766 tests green. Single atomic commit `67f94a4` on `feat/tab-title-from-tmux`. NOT pushed, NOT built, NOT deployed per code-work-doesn't-authorize-ship rule (Ashley 2026-07-27). skynet-patches.md entry deferred to ship time (will be next after #169 pins). | 2026-07-28 | 67f94a4 | [260728-r1h-fix-kiro-cli-wrapper-defeats-claude-sess](./quick/260728-r1h-fix-kiro-cli-wrapper-defeats-claude-sess/) |
| 260728-m5h | Skynet schema-catchup for Nelly's fleet-wide pinned-status migration + mobile pinned-badge bigger. Fleet-wide bounty schema flipped this morning: `pinned` moved from status VALUE to boolean FIELD orthogonal to lifecycle — every previously-pinned bounty is now `status:"in_progress" + pinned:true`. Skynet backend still queried `.status === "pinned"` so #167's filter and per-row badge silently showed 0. Five changes: (1) backend counter `identity-artifact-reader.ts:~1041` flipped `parsed.status === "pinned"` -> `parsed.pinned === true` (local + remote python3 branches); (2) allowed-status enum reduced 5->4 by dropping `"pinned"` + JSDoc in `claude-session-server.ts`; (3) frontend inline pill row 5->4 options in `pretty-view/BountyCard.tsx` + api enum in `claude-session-api.ts`; (4) mobile-only `.pv-bounty-badge` bump ~1.6x (font-size 16px, height 26px, padding 0 8px) under `@media max-width:767.98px` in `pretty-conversations.css`; (5) tests: `count-bounties` fixtures now use `pinned:boolean` orthogonality shape + absent-field NOT-counted test; `write-bounty-status` covers all 4 valid statuses + rejects `"pinned"`; panel tests pick up new schema via API layer. Backend built via `npm run build:backend && npm run build` clean (patch #154 lesson honored); 47 tests green. Inline docs: `~/.claude/identities/tina/skynet-patches.md` header bumped SIXTY-SEVEN -> SIXTY-EIGHT + patch #168 entry (fleet inline-docs rule, Ashley 2026-07-27; identity file not committed since outside git). CLAUDE.md scrub for lingering `.status === "pinned"` — clean. Two commits on feat/tab-title-from-tmux: 2b67908 (task-1 backend) + 2250e3d (task-2 frontend+docs). Not pushed, not built, not deployed per deploy-pre-work rule (Ashley 2026-07-27). Deferred as new bounty: pin-toggle UI in identity modal (user-only control per Nelly's schema rule). | 2026-07-28 | 2250e3d | [260728-m5h-skynet-schema-catchup-for-nellys-pinned-](./quick/260728-m5h-skynet-schema-catchup-for-nellys-pinned-/) |

## Deferred Items

Items acknowledged and carried forward from previous milestone close:

| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
| *(none)* | | | |

## Session Continuity

Last session: 2026-07-28T18:44:57.265Z
Stopped at: Completed 15-02-PLAN.md
Resume file: None
