---
phase: quick-260829-n5z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/shell/use-injected-turn-relay.ts               # NEW — extracted hook (Shape B)
  - src/ui/shell/use-injected-turn-relay.test.ts          # NEW — unit tests for extracted hook
  - src/ui/shell/IdentitySessionPane.tsx                  # thin wire-up: replace inline callbacks with hook
autonomous: true
requirements:
  - QUICK-260829-N5Z-01  # queue-and-replay on null pvSendInputRef
  - QUICK-260829-N5Z-02  # pv-inject structured logging (entry + branches + unregister + stale-displacement WARN)
  - QUICK-260829-N5Z-03  # preserve split-send shape byte-identical on both immediate + drain-on-rebind paths
  - QUICK-260829-N5Z-04  # regression coverage (5 tests: live-dispatch, null-replay, stale-displacement, register-cycle-no-pending, log assertions)

must_haves:
  truths:
    - "handleInjectedTurnReady with null pvSendInputRef stashes (text, mqid) into a single-slot pending ref and returns — no silent drop."
    - "onRegisterSendInput drains any pending turn via queueMicrotask, replaying the same split-send shape (send(text) then setTimeout(60, send('\\r', mqid))). StrictMode double-invoke of the ref callback fires exactly one drain."
    - "onUnregisterSendInput sets pvSendInputRef.current=null and emits a [pv-inject] structured log line for WS-close trace visibility."
    - "A second onInjectedTurnReady call arriving while a first is still pending displaces the first, logs a [pv-inject] stale-displacement WARN, and only the second replays on next register."
    - "Split-send shape is byte-identical in both the immediate-dispatch path and the drain-on-rebind path (body first, \\r+mqid at +60ms)."
    - "No changes to src/ui/features/pretty-view/PrettyView.tsx, src/ui/features/pretty-view/use-pretty-view-uploads.ts, or any queued fh3/ih3/mbp commits."
  artifacts:
    - path: "src/ui/shell/use-injected-turn-relay.ts"
      provides: "useInjectedTurnRelay({ getSendFn }) hook returning { onInjectedTurnReady, onRegisterSendInput, onUnregisterSendInput } with queue-and-replay + structured logs"
      exports: ["useInjectedTurnRelay"]
    - path: "src/ui/shell/use-injected-turn-relay.test.ts"
      provides: "5+ regression tests (live-dispatch, null-queue-and-replay, stale-displacement, register-cycle-no-pending, log assertions)"
    - path: "src/ui/shell/IdentitySessionPane.tsx"
      provides: "PrettyView props onInjectedTurnReady / onRegisterSendInput / onUnregisterSendInput sourced from useInjectedTurnRelay; pvSendInputRef declaration unchanged; onUnregisterSendInput's ref-null side-effect preserved."
  key_links:
    - from: "src/ui/shell/IdentitySessionPane.tsx"
      to: "src/ui/shell/use-injected-turn-relay.ts"
      via: "useInjectedTurnRelay hook call; getSendFn: () => pvSendInputRef.current"
      pattern: "useInjectedTurnRelay\\("
    - from: "src/ui/shell/use-injected-turn-relay.ts"
      to: "PrettyView (via IdentitySessionPane wiring)"
      via: "onInjectedTurnReady, onRegisterSendInput, onUnregisterSendInput returned callbacks"
      pattern: "onInjectedTurnReady|onRegisterSendInput|onUnregisterSendInput"
---

<objective>
Fix the silent-null-drop in `handleInjectedTurnReady` (`src/ui/shell/IdentitySessionPane.tsx:189-203`).

**Root cause:** when the pretty-view's WebSocket is mid-reconnect / mount-race at the exact moment `upload_ready_to_inject` fires from the backend (delivered via `use-pretty-view-uploads.ts` -> `PrettyView` -> `onInjectedTurnReady` callback), `pvSendInputRef.current === null` and the callback silently no-ops. Compose has already cleared (via `outcome.ok=true` in the ComposeBox attachment path). User: attached a file, hit send, compose emptied, message never arrived. UAT evidence: Ashley 2026-08-29 8-attempt matrix — tina 16:20:52 496 KB PNG fizzle with fresh WS session id (`c5224f20-...`) reattached 14 s later at 16:21:06 (previous was `BIpr4v48Zg4I5a2pVf2hQ`).

**Fix shape (Shape B — extracted hook, chosen for testability):** create `useInjectedTurnRelay({ getSendFn })` hook returning `{ onInjectedTurnReady, onRegisterSendInput, onUnregisterSendInput }`. Single-slot `pendingRef` stashes the (text, mqid) tuple when getSendFn() returns null. On register, drain via `queueMicrotask` (StrictMode double-invoke guard). Split-send shape byte-identical to current inline code. `[pv-inject]` structured logs at every branch so live diagnosis has a trace.

Purpose: eliminate the silent-drop failure mode Ashley hit 1-in-8; make WS-blip during injected-turn arrival recoverable; add the structured logs the current path is missing.

Output: new `use-injected-turn-relay.ts` hook + dedicated test file + thin wire-up in IdentitySessionPane. Total change ~40-70 lines across production + test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Files the executor MUST read fully to understand the seam:
@src/ui/shell/IdentitySessionPane.tsx
@src/ui/shell/IdentitySessionPane.test.tsx

# Upstream call sites — READ but DO NOT MODIFY:
# - PrettyView.tsx lines 1337-1360 (usePrettyViewUploads wiring; onInjectedTurnReady?.(injectedText, messageQueueItemId) call site)
# - use-pretty-view-uploads.ts lines 55-90 (batch outcome contract; motivating incident comment for the sibling bug)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract useInjectedTurnRelay hook + failing regression test + IdentitySessionPane wire-up (single atomic vertical slice — RED then GREEN)</name>
  <files>
    src/ui/shell/use-injected-turn-relay.ts,
    src/ui/shell/use-injected-turn-relay.test.ts,
    src/ui/shell/IdentitySessionPane.tsx
  </files>
  <behavior>
    Test cases (5 minimum, all in `src/ui/shell/use-injected-turn-relay.test.ts`, standard vitest + `renderHook` from `@testing-library/react`):

    - **Test 1 — live-ref immediate dispatch**: register a `mockSend`, then fire `onInjectedTurnReady("hello world", "mqid-abc")`. Assert `mockSend` called with `("hello world")` synchronously (1 call). Advance 60 ms via `vi.advanceTimersByTime(60)`. Assert `mockSend` called with `("\r", "mqid-abc")` (2 calls total). No pending ref residue.

    - **Test 2 — null-ref queue-and-replay**: do NOT register. Fire `onInjectedTurnReady("queued text", "mqid-xyz")`. Assert `mockSend` NOT called immediately (0 calls), no error thrown. Assert a `[pv-inject]` info log fires with `mqid: "mqid-xyz"`, `textLen: 11`, `refLive: false`, `hasPending: false`. Now register `mockSend`. Flush microtasks (`await Promise.resolve()` twice, or `await vi.waitFor(...)`). Assert `mockSend` called with `("queued text")` (1 call). Advance 60 ms. Assert `mockSend` called with `("\r", "mqid-xyz")` (2 calls total).

    - **Test 3 — stale-pending displacement**: fire two `onInjectedTurnReady` calls with null getSendFn(). Fire `("first turn", "mqid-1")` then `("second turn", "mqid-2")`. Assert a `[pv-inject]` WARN log fires on the second call naming displacement of `mqid-1`. Now register `mockSend`. Flush microtasks + advance 60 ms. Assert ONLY the second turn replays: `mockSend` calls are exactly `("second turn")` then `("\r", "mqid-2")` (no `"first turn"` anywhere in call history).

    - **Test 4 — register / unregister / register with no pending**: register `mockSend`, immediately unregister (assert `[pv-inject] ref unregistered` log fires), re-register a NEW mock. Assert neither mock ever received a call. No replay attempt fires; `pendingRef` remains null throughout.

    - **Test 5 — log assertions matrix**: `vi.spyOn(console, "info")` + `vi.spyOn(console, "warn")` set up in `beforeEach`. Assert `[pv-inject]` log lines fire (via prefix match on the first arg — structured object with `operation` key starting `pv_inject_` OR string starting `[pv-inject]`, executor's discretion but must be greppable) at each branch: (a) entry (mqid + textLen + refLive + hasPending), (b) dispatching-immediately (live ref path), (c) queued-for-replay (null ref path), (d) draining-on-rebind (on register with pending), (e) ref-unregistered (on unregister), (f) stale-displacement WARN (Test 3 branch). Additionally exercise the "ref went null in the 60 ms window" branch: register mockSend, fire onInjectedTurnReady, unregister BEFORE the 60 ms timer fires, advance 60 ms, assert a `[pv-inject]` log fires noting the second-half no-op (executor's discretion on info vs warn — recommend WARN because it means the `\r` was lost).

    Bonus optional Test 6 (executor's discretion): StrictMode double-invoke guard. Fire onInjectedTurnReady with null getSendFn(). Register mockSend TWICE in rapid succession (simulating StrictMode's double ref-callback invoke). Flush microtasks + advance 60 ms. Assert mockSend was called exactly twice total: once with `("queued text")`, once with `("\r", "mqid")`. NOT four times. The second StrictMode register-invoke's queueMicrotask must observe `pendingRef.current === null` and skip.

    All tests MUST pass byte-identical split-send shape assertions: mockSend.mock.calls[0] = `[text]` (single arg), mockSend.mock.calls[1] = `["\r", mqid]` (two args). Do NOT assert `mockSend` was called with an object or extra args — the current inline shape is exactly `send(text)` + `send("\r", messageQueueItemId)`.
  </behavior>
  <action>
    Execute this task as ONE atomic vertical slice split into TWO commits (RED then GREEN, matching recent quick-260829-fh3 / ih3 / mbp precedent). Do NOT split into three tasks in the plan — the hook + test + wire-up are tightly coupled (extract, verify, wire) and total change is ~40-70 lines.

    **Commit 1 (RED — test only):**
    Create `src/ui/shell/use-injected-turn-relay.test.ts` with the 5 tests (plus optional Test 6) described in `<behavior>`. Import `useInjectedTurnRelay` from `./use-injected-turn-relay` (module does not exist yet — RED-phase import failure is expected and correct). Use `renderHook` from `@testing-library/react` — the hook takes `{ getSendFn }` where `getSendFn: () => ((text: string, mqid?: string) => boolean) | null`. Emulate the pvSendInputRef via `let currentSend: MockFn | null = null; renderHook(() => useInjectedTurnRelay({ getSendFn: () => currentSend }))`; register/unregister in tests by assigning `currentSend = mockSend` / `currentSend = null` AND calling the returned `onRegisterSendInput(currentSend)` / `onUnregisterSendInput()` (both are needed — getSendFn is the read seam, onRegister/onUnregister are the notify seams that trigger drain + log). Use `vi.useFakeTimers()` in `beforeEach` (matches existing IdentitySessionPane.test.tsx convention), `vi.useRealTimers()` in `afterEach`. Use `vi.spyOn(console, "info")` + `vi.spyOn(console, "warn")` (do NOT mock console.error — nothing here should error). Run `npx vitest run src/ui/shell/use-injected-turn-relay` — expect ALL tests to fail (module missing). Commit as: `test(quick-260829-n5z): failing regression for handleInjectedTurnReady queue-and-replay`.

    **Commit 2 (GREEN — implementation + wire-up):**

    (2a) Create `src/ui/shell/use-injected-turn-relay.ts` exporting `useInjectedTurnRelay({ getSendFn })`. The hook body:

    - `const pendingRef = useRef<{ text: string; messageQueueItemId: string } | null>(null);` — single-slot pending.
    - `const onInjectedTurnReady = useCallback((text: string, messageQueueItemId: string) => { ... }, [getSendFn])` — entry log with `operation: "pv_inject_entry"` or string `"[pv-inject] entry"` (executor's discretion on structured-vs-string, but MUST be greppable and MUST include mqid, textLen, refLive (=`getSendFn() !== null`), hasPending (=`pendingRef.current !== null`)). Branch on `getSendFn()`: if null, log queued-for-replay (WARN if hasPending was true and displacing → include `displacedMqid`), stash `{ text, messageQueueItemId }` into pendingRef, return. If live, log dispatching-immediately, run the EXACT split-send `send(text); setTimeout(() => { const send2 = getSendFn(); if (send2) send2("\r", messageQueueItemId); else console.warn("[pv-inject] ref went null in 60ms window", { mqid: messageQueueItemId }); }, 60);`.
    - `const onRegisterSendInput = useCallback((_fn) => { const pending = pendingRef.current; if (!pending) return; queueMicrotask(() => { const drain = pendingRef.current; if (!drain) return; pendingRef.current = null; const send = getSendFn(); if (!send) { console.warn("[pv-inject] ref went null before microtask drain", { mqid: drain.messageQueueItemId }); return; } console.info("[pv-inject] draining on rebind", { mqid: drain.messageQueueItemId, textLen: drain.text.length }); send(drain.text); setTimeout(() => { const send2 = getSendFn(); if (send2) send2("\r", drain.messageQueueItemId); else console.warn("[pv-inject] ref went null in drain 60ms window", { mqid: drain.messageQueueItemId }); }, 60); }); }, [getSendFn]);` — the `_fn` param is unused (the hook reads via getSendFn to guarantee freshness against StrictMode double-invoke), but the callback signature must match PrettyView's `onRegisterSendInput?: (fn: (text: string, mqid?: string) => boolean) => void` shape so the wire-up in IdentitySessionPane can pass it directly.
    - `const onUnregisterSendInput = useCallback(() => { console.info("[pv-inject] ref unregistered", { hasPending: pendingRef.current !== null }); }, []);` — the actual `pvSendInputRef.current = null` assignment stays in IdentitySessionPane (see 2c) because the hook does not own the ref.
    - Return `{ onInjectedTurnReady, onRegisterSendInput, onUnregisterSendInput }`.

    Byte-verify: the setTimeout arg order `send2("\r", drain.messageQueueItemId)` matches current line 199 EXACTLY (arg 1 = `"\r"`, arg 2 = mqid). Do NOT swap. Do NOT add fields to the pendingRef slot beyond `text` and `messageQueueItemId`.

    (2b) Run `npx vitest run src/ui/shell/use-injected-turn-relay` — all tests MUST pass. Fix any test/impl mismatches. `[pv-inject]` grep count check: `grep -c '\[pv-inject\]' src/ui/shell/use-injected-turn-relay.ts` should be >= 6 (entry + immediately + queued + draining + unregistered + 60ms-window warnings). Filter comments: `grep -v '^\s*//' src/ui/shell/use-injected-turn-relay.ts | grep -c '\[pv-inject\]'` for the runtime count.

    (2c) Wire IdentitySessionPane.tsx: (i) add import `import { useInjectedTurnRelay } from "./use-injected-turn-relay";`. (ii) DELETE the current `handleInjectedTurnReady = useCallback((text, mqid) => { const send = pvSendInputRef.current; if (!send) return; ... }, []);` block at lines 189-203 (10 source lines). (iii) Just above the `hostConfig` const (~line 205), insert `const injectedTurnRelay = useInjectedTurnRelay({ getSendFn: () => pvSendInputRef.current });`. (iv) Update PrettyView JSX props: `onInjectedTurnReady={injectedTurnRelay.onInjectedTurnReady}`, and REPLACE the inline `onRegisterSendInput={(fn) => { pvSendInputRef.current = fn; }}` with `onRegisterSendInput={(fn) => { pvSendInputRef.current = fn; injectedTurnRelay.onRegisterSendInput(fn); }}`, and REPLACE `onUnregisterSendInput={() => { pvSendInputRef.current = null; }}` with `onUnregisterSendInput={() => { pvSendInputRef.current = null; injectedTurnRelay.onUnregisterSendInput(); }}`. The `pvSendInputRef.current = fn;` and `pvSendInputRef.current = null;` assignments stay INLINE (the hook is a notify-and-drain listener; it does not own the ref). Order matters: assign the ref FIRST, then call the hook — so when the hook's drain microtask fires and calls `getSendFn()`, the ref is already populated. Do NOT touch `onRegisterSendInterrupt` / `onUnregisterSendInterrupt` (unrelated).

    (2d) Run scoped tests to catch regressions in the existing test file:
      - `npx vitest run src/ui/shell/use-injected-turn-relay` — all NEW tests pass.
      - `npx vitest run src/ui/shell/IdentitySessionPane` — all 7 existing tests (P1-P7) still pass. The mock PrettyView already captures `onRegisterSendInput`; the added inline wrapper does not change its shape.

    (2e) Byte-verify no forbidden touches: `git diff --stat` shows exactly 3 files modified — `use-injected-turn-relay.ts` (new), `use-injected-turn-relay.test.ts` (new), `IdentitySessionPane.tsx` (modified). Confirm zero diff in: `src/ui/features/pretty-view/PrettyView.tsx`, `src/ui/features/pretty-view/use-pretty-view-uploads.ts`, and any of the queued commit files (aee16c8f, 5b3ff27a, ee88877c stack — SplitView.tsx, CollapsedPanelCloseLane.tsx, AppShell.tsx, etc.). Run: `git diff --name-only HEAD` and confirm the output is exactly those 3 files.

    Commit as: `fix(quick-260829-n5z): queue-and-replay in useInjectedTurnRelay hook + pv-inject structured logs`.

    Do NOT push. Do NOT run docker build. Do NOT run full suite (`npx vitest run` without a path). Do NOT touch `~/.claude/roles/box-maintainer/skynet-patches.md`. Do NOT touch `.planning/STATE.md` (orchestrator wraps up).
  </action>
  <verify>
    <automated>npx vitest run src/ui/shell/use-injected-turn-relay src/ui/shell/IdentitySessionPane 2>&amp;1 | tail -30</automated>
    <automated>test $(git diff --name-only HEAD | wc -l) -eq 3 &amp;&amp; git diff --name-only HEAD | grep -E '^src/ui/shell/(use-injected-turn-relay\.(ts|test\.ts)|IdentitySessionPane\.tsx)$' | wc -l | grep -q '^3$' &amp;&amp; echo "OK: exactly 3 files modified, all in expected paths" || echo "FAIL: unexpected files modified"</automated>
    <automated>grep -v '^\s*//' src/ui/shell/use-injected-turn-relay.ts | grep -c '\[pv-inject\]'</automated>
    <automated>git log --oneline -3 | grep -E '(test|fix)\(quick-260829-n5z\)'</automated>
  </verify>
  <done>
    - `src/ui/shell/use-injected-turn-relay.ts` exists, exports `useInjectedTurnRelay`, implements queue-and-replay with byte-identical split-send shape (send(text) + setTimeout(60, send("\r", mqid))) on BOTH the immediate-dispatch and drain-on-rebind paths.
    - `src/ui/shell/use-injected-turn-relay.test.ts` exists with 5+ tests, all pass.
    - `src/ui/shell/IdentitySessionPane.tsx` no longer contains an inline `handleInjectedTurnReady` `useCallback` with the `if (!send) return;` silent-drop branch; the PrettyView JSX wires the hook's three callbacks + preserves the inline `pvSendInputRef.current = fn` / `= null` assignments alongside the hook notifications.
    - `npx vitest run src/ui/shell/use-injected-turn-relay src/ui/shell/IdentitySessionPane` exits 0. Existing P1-P7 tests still pass.
    - Runtime `[pv-inject]` log grep count (comments filtered) >= 6 in `use-injected-turn-relay.ts`.
    - `git diff --name-only HEAD` returns EXACTLY the 3 target files. Zero diff on PrettyView.tsx, use-pretty-view-uploads.ts, or any queued fh3/ih3/mbp commit files.
    - Two atomic commits present on branch: `test(quick-260829-n5z): ...` then `fix(quick-260829-n5z): ...`.
    - NO push, NO docker, NO full-suite run, NO skynet-patches.md edit, NO STATE.md edit.
  </done>
</task>

</tasks>

<verification>
Scoped test gates only (per constraints — full-suite is orchestrator ship-gate scope):

1. `npx vitest run src/ui/shell/use-injected-turn-relay` — new suite green (5+ tests).
2. `npx vitest run src/ui/shell/IdentitySessionPane` — existing 7 tests (P1-P7) still green (no regression from the wire-up rewrite).
3. `grep -v '^\s*//' src/ui/shell/use-injected-turn-relay.ts | grep -c '\[pv-inject\]'` >= 6 — confirms the six log branches are wired.
4. `git diff --name-only HEAD` returns exactly: `src/ui/shell/use-injected-turn-relay.test.ts`, `src/ui/shell/use-injected-turn-relay.ts`, `src/ui/shell/IdentitySessionPane.tsx`. Zero other files touched.
5. `git log --oneline -3` shows two atomic commits with quick-260829-n5z slug: one `test(...)` (RED), one `fix(...)` (GREEN).
</verification>

<success_criteria>
- Silent null-drop failure mode eliminated: when `pvSendInputRef.current === null` at the moment `onInjectedTurnReady` fires, the (text, mqid) tuple stashes into `pendingRef` and replays on the next `onRegisterSendInput` via `queueMicrotask`. StrictMode double-invoke of the ref callback fires exactly ONE drain (the second invoke's microtask observes `pendingRef.current === null` and skips).
- Split-send shape preserved BYTE-IDENTICAL on both paths (immediate-dispatch + drain-on-rebind): `send(text)` synchronously, `setTimeout(60, () => send("\r", messageQueueItemId))` for the CR half.
- Stale-pending displacement: a second `onInjectedTurnReady` with null ref before the first has drained displaces the first with a `[pv-inject]` WARN and only the second replays.
- `[pv-inject]` structured logs fire at 6 branches (entry, dispatching-immediately, queued-for-replay, draining-on-rebind, ref-unregistered, ref-went-null-in-60ms-window), plus WARN on stale-displacement — giving future Ashley-live UAT diagnosis a full trace where currently there is nothing.
- Shape B extraction: hook is unit-testable in isolation (no PrettyView / MessageQueueDrawer / Terminal / identities-store mock scaffolding needed — just `renderHook` + a mock send fn + fake timers). The 387-line existing `IdentitySessionPane.test.tsx` scaffolding is not needed to prove this fix works.
- Zero collateral: PrettyView.tsx, use-pretty-view-uploads.ts, and all queued fh3/ih3/mbp commit files are byte-unchanged.
- Two atomic commits (RED test + GREEN impl+wire-up), matching recent quick-task precedent (`4d9d0d46`+`ee88877c` for mbp; `d051b0b3`+`429802f8`+`1c8e3b6d` for ih3). NO push, NO deploy motion at executor exit — orchestrator handles ship gate.
</success_criteria>

<output>
Create `.planning/quick/260829-n5z-fix-handleinjectedturnready-silent-null-/260829-n5z-SUMMARY.md` when done (executor's standard summary shape — plan objective, task outcomes, verify results, deviations, commits, ship-gate handoff note).
</output>
