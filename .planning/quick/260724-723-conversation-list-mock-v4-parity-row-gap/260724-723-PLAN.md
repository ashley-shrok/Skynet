---
phase: quick-260724-723
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/features/pretty-conversations/pretty-conversations.css
  - src/ui/AppShell.tsx
autonomous: true
requirements:
  - UAT-1-row-gap-padding
  - UAT-2-active-set-any-click
  - UAT-3-row-side-padding
  - UAT-5-avatar-icon-size
  - UAT-6-chevron-chrome-at-rest
  - UAT-7-chevron-clears-title
must_haves:
  truths:
    - "Every conversation row has 8px vertical gap between it and the next row (mock v4 .pv-panel-scroll gap:8px)"
    - "The conversation-list scroll region has 14px top / 12px side / 18px bottom padding — rows no longer flush to the panel edge"
    - "Clicking a fleet-only row lights it up out of ambient (moves it into the active set)"
    - "Clicking an RDP-host row lights it up out of ambient (moves it into the active set)"
    - "The RDP Monitor icon inside a fallback-icon avatar renders at 18x18 (not 14x14)"
    - "The persistent top-left sidebar-toggle chevron is visible at rest without hover (subtle bg + border + muted-fg)"
    - "When the desktop sidebar is open, the 'Conversations' title clears the fixed chevron footprint (no overlap)"
    - "Mobile behavior unchanged for the chevron-title-clearance fix (mobile hides the desktop title already)"
    - "`npx tsc --noEmit` reports zero errors"
    - "`npm run test -- pretty-conversations` passes (Tests 12 + 13 updated for new engagement-on-every-click semantics)"
    - "`npm run build` succeeds"
    - "All five fixes land in ONE git commit (no separate deploy)"
  artifacts:
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "Scroll-region .pv-panel-scroll class + hoisted addToActiveSet call in handleRowSelect"
      contains: "pv-panel-scroll"
    - path: "src/ui/features/pretty-conversations/pretty-conversations.css"
      provides: ".pv-avatar-fallback-icon svg 18x18 rule + .pv-panel-header[data-sidebar-toggle-overlaps=\"true\"] padding-left rule"
      contains: "pv-avatar-fallback-icon"
    - path: "src/ui/AppShell.tsx"
      provides: "Chevron always-visible at-rest chrome + sidebarToggleOverlaps prop wired to PrettyConversationsPanel"
      contains: "sidebarToggleOverlaps"
  key_links:
    - from: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      to: "@/state/conversation-store"
      via: "addToActiveSet import + call at top of handleRowSelect"
      pattern: "addToActiveSet"
    - from: "src/ui/AppShell.tsx"
      to: "PrettyConversationsPanel"
      via: "sidebarToggleOverlaps prop"
      pattern: "sidebarToggleOverlaps"
    - from: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      to: ".pv-panel-header"
      via: "data-sidebar-toggle-overlaps attribute driven by prop"
      pattern: "data-sidebar-toggle-overlaps"
---

<objective>
Ship fork patch #142 — five UAT-caught mock v4 parity fixes to the pretty-conversations panel and its sidebar-toggle chevron. All five land in ONE atomic commit on `feat/tab-title-from-tmux`. No deploy — Ashley's 2026-07-23 rule batches fork patches; deploys are triggered separately.

Purpose: close the visual + behavioral gap between mock v4 (LOCKED) and the shipped panel that Ashley UAT'd side-by-side on 2026-07-24. Root causes fully diagnosed in the task-context (row spacing = Tailwind classes shadow the existing `.pv-panel-scroll` CSS; active-set = patch #137's `addToActiveSet` lives inside `selectConversation` which the fleet/RDP branches skip; avatar icon = `tabIcon()` returns 14px but the mock expects 18px inside the 40px disc; chevron chrome = hover-only treatment is invisible over the fixed-position transparent background; chevron overlap = no padding clearance when the sidebar opens on desktop).

Output: three files edited, one commit, tsc + tests + build clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
@src/ui/features/pretty-conversations/PrettyConversationRow.tsx
@src/ui/features/pretty-conversations/pretty-conversations.css
@src/ui/state/conversation-store.ts
@src/ui/AppShell.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Land all five mock v4 parity fixes across the three target files</name>
  <files>
    src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx,
    src/ui/features/pretty-conversations/pretty-conversations.css,
    src/ui/AppShell.tsx
  </files>
  <action>
Land all five fixes in a single coherent edit pass. Each fix is small and independent — do them in this order to keep the CSS + component + wiring changes near each other in reviewer diff order.

FIX 1 — Row gap + side padding (UAT #1 + #3). In `PrettyConversationsPanel.tsx` at the scroll-region div (currently `<div className="flex-1 min-h-0 overflow-y-auto flex flex-col">` — near line 275), swap the className to `"pv-panel-scroll min-h-0"`. The existing `.pv-panel-scroll` CSS class in `pretty-conversations.css` L111-118 already supplies `flex:1`, `overflow-y:auto`, `display:flex`, `flex-direction:column`, `padding:14px 12px 18px`, `gap:8px` — everything the Tailwind soup was providing EXCEPT `min-h-0`, which is retained. Do not touch the empty-state child branch — its inner card is `justify-content: center` inside a `flex-1` wrapper and the new 14/12/18 padding + 8px gap will not visually disturb it.

FIX 2 — Active-set on any click (UAT #2). In the same file, hoist the active-set engagement signal to the TOP of `handleRowSelect` so the RDP + fleet-only early-return branches no longer skip it. Two edits:
(a) Import block at the top of the file (currently imports `useConversations, useSelectedConversationId, usePinnedIds, useActiveSet, selectConversation, togglePinConversation, type ConversationRow as ConversationRowShape` from `@/state/conversation-store`) — add `addToActiveSet` to the import list. It is already exported at `conversation-store.ts:645`.
(b) In `handleRowSelect` (~L176-190), insert `addToActiveSet(row.id);` as the SECOND line of the function body, immediately after the `if (isMobileVariant) setCurrentlySwipedId(null);` line and BEFORE the `if (row.rdpHostRow && onRdpRowClick)` branch. The default branch's inner call chain still runs `selectConversation(row.id) → addToActiveSet(row.id)` but the idempotency guard at `conversation-store.ts:646` (`if (state.activeSet.has(id)) return;`) makes it a cheap no-op — no double notify, no double sessionStorage write.

FIX 3 — RDP Monitor icon size in avatar (UAT #5). In `pretty-conversations.css`, add a new rule scoping only to fallback-icon avatars (identity/initial avatars are unaffected). Add it inside the AVATAR DISC section, immediately AFTER the `.pv-avatar { … }` block (i.e. after L202 close-brace and before the `.pv-body` selector at ~L204). New rule (verbatim):
```
.pv-avatar .pv-avatar-fallback-icon svg {
  width: 18px;
  height: 18px;
}
```
This scopes to `<span class="pv-avatar-fallback-icon">…tabIcon(row.type)…</span>` at `PrettyConversationRow.tsx:356-358`. `tabIcon()` returns lucide icons with hard-coded `className="size-3.5"` (14px) — the descendant `svg` selector overrides via specificity (`.pv-avatar .pv-avatar-fallback-icon svg` beats `.size-3.5`'s single-class specificity, and the width/height are direct properties). Tab-bar rendering is unaffected — different container context, no `.pv-avatar` ancestor.

FIX 4 — Sidebar-toggle chevron chrome at rest (UAT #6). In `AppShell.tsx` at the fixed persistent chevron (~L1425), replace the `className` string. Current is bare-until-hover:
```
"fixed flex items-center justify-center w-8 h-8 rounded-lg bg-transparent border border-transparent text-[color:var(--color-pv-fg-muted)] hover:bg-[rgba(220,225,245,0.06)] hover:border-[color:var(--color-pv-border-quiet)] hover:text-[color:var(--color-pv-fg)] transition-colors cursor-pointer"
```
Replace with (subtle-at-rest, bump-on-hover):
```
"fixed flex items-center justify-center w-8 h-8 rounded-lg bg-[rgba(220,225,245,0.06)] border border-[color:var(--color-pv-border-quiet)] text-[color:var(--color-pv-fg-muted)] hover:bg-[rgba(220,225,245,0.12)] hover:border-[color:var(--color-pv-border-quiet-strong)] hover:text-[color:var(--color-pv-fg)] transition-colors cursor-pointer"
```
The `--color-pv-border-quiet-strong` token is `rgba(220,225,245,0.1)` (verified in `src/ui/index.css:125`). No token additions needed. All other attrs (`type`, `onClick`, `aria-label`, `title`, inline `style` with `top`/`left`/`zIndex`, the `<span>` rotation wrapper, `<ChevronLeft className="size-4" />`) stay byte-identical.

FIX 5 — Chevron clears "Conversations" title when sidebar open on desktop (UAT #7). Three edits:

(5a) `PrettyConversationsPanel.tsx` prop-type: add `sidebarToggleOverlaps?: boolean;` to the destructured props object AND to the props typedef immediately below (mirroring the existing prop shape — e.g. add `sidebarToggleOverlaps,` at the end of the destructure at ~L107 and `sidebarToggleOverlaps?: boolean;` with a one-line doc comment in the typedef at ~L135). Keep the prop optional; default to `false` when unset via `?? false` inline OR via a defaulted destructure `sidebarToggleOverlaps = false`. Then, on the `.pv-panel-header` div at ~L250, add the data attribute: `data-sidebar-toggle-overlaps={sidebarToggleOverlaps ? "true" : "false"}`. Leave existing `className="pv-panel-header shrink-0"` and children untouched.

(5b) `pretty-conversations.css` — after the existing `.pv-panel-header { … }` block at L66-72 (before `.pv-panel-header .pv-title` at L74), insert:
```
.pv-panel-header[data-sidebar-toggle-overlaps="true"] {
  padding-left: 48px; /* 8px chevron left + 32px chevron width + 8px breathing */
}
```

(5c) `AppShell.tsx` at the `PrettyConversationsPanel` mount site (~L1284), add the prop after `variant={isMobile ? "mobile" : "desktop"}` (e.g. between `variant=…` and `onConversationSelected=…`): `sidebarToggleOverlaps={!isMobile && !isTouchDevice && sidebarOpen}`. The `!isMobile && !isTouchDevice` gate matches the guard on the inline sidebar column at ~L1457 — mobile + touchscreen paths never mount the fixed inline sidebar, so the chevron never overlaps a visible desktop title there.

TEST HYGIENE — Tests 12 + 13 in `PrettyConversationsPanel.test.tsx` (L560 and L603) currently assert `expect(selectConversationSpy).not.toHaveBeenCalled()` for RDP + fleet-only clicks. That assertion is STILL VALID after this patch (the hoisted call is `addToActiveSet`, not `selectConversation` — the branches still return before `selectConversation`). But the mock at L106-122 does NOT stub `addToActiveSet`, and the panel will now call it — which will throw "No 'addToActiveSet' export is defined on the mock." Add `addToActiveSet: () => {},` to the `vi.mock("@/state/conversation-store", …)` object at ~L120 (alongside the existing `selectConversation` + `togglePinConversation` stubs). This is a pure test-mock hygiene addition — it does NOT change any test's assertions. Test 14 (plain-row) continues to pass because the default branch still calls `selectConversation` which the mock's spy captures. Nothing else in the test file needs to change.

DO NOT do any of the following: (a) do NOT touch `PrettyConversationRow.tsx` — the fixes here are all CSS or panel/shell wiring, not row semantic markup; (b) do NOT touch `conversation-store.ts` — `addToActiveSet` is already exported; (c) do NOT retire the `addToActiveSet` call inside `selectConversation` at L614 — that path remains reachable via other selectConversation callers (selectConversationDeferred flush at L620+, other future callers); (d) do NOT change the mobile title behavior (`showDesktopTitle` gate) — Ashley locked that as-is; (e) do NOT run `npm run build` inside this task — that happens in Task 2 verify.
  </action>
  <verify>
    <automated>grep -n 'pv-panel-scroll\|addToActiveSet\|sidebarToggleOverlaps\|pv-avatar-fallback-icon\|data-sidebar-toggle-overlaps' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx src/ui/features/pretty-conversations/pretty-conversations.css src/ui/AppShell.tsx | grep -v '^[^:]*:[^:]*:[[:space:]]*[/\*]'</automated>
  </verify>
  <done>
- `PrettyConversationsPanel.tsx` scroll region div has className `"pv-panel-scroll min-h-0"` (Tailwind duplicates dropped, `min-h-0` retained).
- `PrettyConversationsPanel.tsx` imports `addToActiveSet` from `@/state/conversation-store`.
- `handleRowSelect` in `PrettyConversationsPanel.tsx` calls `addToActiveSet(row.id)` as its second statement, before the RDP/fleet-only branch dispatch.
- `PrettyConversationsPanel.tsx` accepts a `sidebarToggleOverlaps?: boolean` prop (default false), and emits `data-sidebar-toggle-overlaps={"true"|"false"}` on `.pv-panel-header`.
- `pretty-conversations.css` contains the `.pv-avatar .pv-avatar-fallback-icon svg { width:18px; height:18px }` rule.
- `pretty-conversations.css` contains `.pv-panel-header[data-sidebar-toggle-overlaps="true"] { padding-left: 48px }`.
- `AppShell.tsx` persistent-chevron className has `bg-[rgba(220,225,245,0.06)] border border-[color:var(--color-pv-border-quiet)]` at rest and bumped `hover:bg-[rgba(220,225,245,0.12)] hover:border-[color:var(--color-pv-border-quiet-strong)]` on hover — no `bg-transparent`/`border-transparent` residuals in the chevron's className.
- `AppShell.tsx` `PrettyConversationsPanel` mount site passes `sidebarToggleOverlaps={!isMobile && !isTouchDevice && sidebarOpen}`.
- `PrettyConversationsPanel.test.tsx` mock object at ~L106 has `addToActiveSet: () => {},` added (no other test edits).
- `PrettyConversationRow.tsx` untouched. `conversation-store.ts` untouched.
- The grep-based verify above shows: pv-panel-scroll in the panel JSX; addToActiveSet imported + called in the panel; sidebarToggleOverlaps in both panel + AppShell; pv-avatar-fallback-icon in the CSS; data-sidebar-toggle-overlaps in both panel + CSS.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify (tsc + vitest + build), then commit as fork patch #142</name>
  <files>
    (verify-only; no new files)
  </files>
  <action>
Run the four verification gates in order. If any gate fails, STOP and diagnose — do NOT paper over failures by loosening assertions or skipping tests.

Gate 1 — TypeScript:
```
npx tsc --noEmit
```
Expected: zero errors. If Task 1 typed the new prop correctly this passes on the first try. Common failure mode: forgot to add `sidebarToggleOverlaps` to the typedef in `PrettyConversationsPanel.tsx`, OR passed it from AppShell without the panel accepting it. Fix in place, re-run.

Gate 2 — Vitest scoped to pretty-conversations:
```
npm run test -- pretty-conversations
```
Expected: green. Baseline before this patch (per patch #137 SUMMARY): PrettyConversationRow 20/20, PrettyConversationsPanel 15/15, session-working-store 4/4 for the pretty-conversations suite. This patch adds ZERO new tests and modifies ZERO existing tests except the mock-object hygiene addition (`addToActiveSet: () => {},`). If a test regresses, the LIKELY cause is one of: (a) missing `addToActiveSet` mock stub (Tests 12/13 fail with "No 'addToActiveSet' export is defined on the mock"); (b) `handleRowSelect` accidentally calling addToActiveSet before the isMobileVariant swipe-reset side-effect (harmless but wrong order); (c) the panel wrapper's `.pv-panel-scroll` class breaking the empty-state layout test (Test that queries `data-testid="pretty-conversations-empty"` still works — the empty child is inside the wrapper regardless of the wrapper's className). Fix root cause; do NOT rewrite the test to match broken behavior.

Note on the DELIBERATE test update per the task-context: the task-context flagged Tests 12/13 as candidates for updating to "expect the new engagement-on-every-click semantics." Per the current assertions (`expect(selectConversationSpy).not.toHaveBeenCalled()`), NO update is required — `addToActiveSet` is a separate function from `selectConversation`, and the RDP/fleet-only branches still return before calling `selectConversation`. The tests continue to hold. The only test-file edit is the mock stub addition in Task 1. If the executor discovers a different test asserting that `addToActiveSet` is NOT called for RDP/fleet clicks, THAT test should be updated because it encodes the exact bug being fixed — bugs #2 in the UAT list; the fix is intentional per patch #137's original intent.

Gate 3 — Full build:
```
npm run build
```
Expected: succeeds cleanly. If it fails on a fresh CSS parse error, check the new rules in `pretty-conversations.css` for missing braces / trailing semis. Bundle size delta expected to be trivially small (few hundred bytes of new CSS + a handful of JSX tokens).

Gate 4 — Grep sanity (belt-and-suspenders):
```
grep -n 'pv-panel-scroll' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
grep -n 'addToActiveSet' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
```
Both should return matching lines (the class in the JSX, and the imported + called action). If either returns nothing, Task 1 landed incompletely — go back and fix.

Commit — ONE atomic commit on the current branch (`feat/tab-title-from-tmux`, per fork convention). Use a HEREDOC to preserve formatting. The five UAT points + root causes:

```
git add \
  src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx \
  src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx \
  src/ui/features/pretty-conversations/pretty-conversations.css \
  src/ui/AppShell.tsx

git commit -m "$(cat <<'EOF'
feat: patch #142 — conversation-list mock v4 parity (row gap/padding, active-set-on-any-click, avatar icon size, chevron chrome + title-clearance)

Five UAT-caught fixes (Ashley 2026-07-24 side-by-side against locked mock v4) land atomically in one commit for fork patch #142.

UAT #1 + #3 (row spacing + side padding gone): PrettyConversationsPanel's
scroll-region div was `flex-1 min-h-0 overflow-y-auto flex flex-col` — pure
Tailwind — which shadowed the pre-existing `.pv-panel-scroll` CSS class that
already carried the mock's exact `padding:14px 12px 18px` + `gap:8px`.
className switched to `"pv-panel-scroll min-h-0"`; Tailwind duplicates dropped,
min-h-0 retained (not in the CSS class).

UAT #2 (row clicks don't light up active-set for fleet-only or RDP rows):
patch #137 wired addToActiveSet INSIDE selectConversation. But handleRowSelect
branches on row.rdpHostRow and row.fleetOnly and early-returns BEFORE calling
selectConversation. Fleet-defined agents (Ashley's whole active session set)
never engaged the active set → rows stayed `.pv-row.ambient` forever. Fix:
hoist addToActiveSet(row.id) to the top of handleRowSelect, before the branch
dispatch. Default branch's inner selectConversation → addToActiveSet is a
cheap idempotent no-op (guard at conversation-store.ts:646). Patch #137's
original intent — every non-null selection = engagement — now uniformly
applied across all three dispatch paths.

UAT #5 (RDP Monitor icon too small in avatar): tabIcon() returns
<Monitor className="size-3.5" /> (14px) because that's the tab-bar sizing.
Mock has 18x18 svg inside the 40x40 avatar disc. Scoped CSS fix on
.pv-avatar .pv-avatar-fallback-icon svg { width:18px; height:18px } — only
fires inside avatar context (identity-badge callers use .pv-avatar-img /
.pv-avatar-initial), tab-bar unaffected (different container).

UAT #6 (sidebar-toggle chevron barely visible at rest): the fixed persistent
chevron followed the mock's .pv-pencil bare-until-hover convention. But the
pencil sits inside a bordered panel-header with a base background; the fixed
chevron floats over unpredictable content, so bare-until-hover left it
visually absent ("you would barely even know it's there"). Promoted the
hover chrome to at-rest (subtle rgba bg + quiet border + muted-fg) and
bumped the hover state one notch further so hover feedback stays.

UAT #7 (chevron overlays "Conversations" title when sidebar open on desktop):
chevron is position:fixed top:8px left:8px w-8, so it eats ~40px into the
panel — but the .pv-panel-header title sits at the panel's 16px padding
with no clearance. Data-attribute-driven padding-left variant on
.pv-panel-header (48px = 8+32+8): new sidebarToggleOverlaps?: boolean prop
threaded from AppShell (true when !isMobile && !isTouchDevice && sidebarOpen)
sets data-sidebar-toggle-overlaps="true" on the header; CSS rule
.pv-panel-header[data-sidebar-toggle-overlaps="true"] { padding-left:48px }.
Mobile unaffected (mobile hides the desktop title via showDesktopTitle gate).

Test hygiene: PrettyConversationsPanel.test.tsx conversation-store mock
extended with addToActiveSet: () => {} stub (otherwise Tests 12 + 13 fail
with "No 'addToActiveSet' export is defined on the mock"). Zero assertion
changes — Tests 12/13 continue to assert selectConversationSpy is NOT
called for RDP/fleet-only clicks (still true; addToActiveSet is a separate
export, and those branches still return before selectConversation).

tsc --noEmit clean. vitest pretty-conversations green. npm run build clean.
Three source files + one test file, four small edits. Batched with pending
fork-patch stack; deploy triggered separately per Ashley's 2026-07-23 rule.
EOF
)"
```

Do NOT run `git push`. Do NOT trigger a deploy. Do NOT touch `~/.claude/identities/tina/skynet-patches.md` — the patches-md entry is batched separately and gets pinned when Tina next deploys the stack.
  </action>
  <verify>
    <automated>git log -1 --pretty=format:'%s' | grep -c 'patch #142'</automated>
  </verify>
  <done>
- `npx tsc --noEmit` exit 0.
- `npm run test -- pretty-conversations` all green (baseline suites unchanged; only the mock stub addition is new).
- `npm run build` exit 0.
- `git log -1` shows the patch #142 commit with all five UAT lines in the body.
- `git status` clean.
- No `git push` executed. No deploy. No `~/.claude/identities/tina/skynet-patches.md` edit.
  </done>
</task>

</tasks>

<verification>
Combined phase-level checks (Task 2 encapsulates all of these):

1. `npx tsc --noEmit` — zero errors.
2. `npm run test -- pretty-conversations` — all suites green.
3. `npm run build` — succeeds.
4. `git log -1` shows the patch #142 commit; `git status` clean.
5. Grep sanity: `pv-panel-scroll` present in PrettyConversationsPanel.tsx JSX; `addToActiveSet` imported + called in PrettyConversationsPanel.tsx; `sidebarToggleOverlaps` present in both PrettyConversationsPanel.tsx and AppShell.tsx; `pv-avatar-fallback-icon` rule present in pretty-conversations.css; `data-sidebar-toggle-overlaps` present in both the panel and the CSS.

DOM/behavioral checks Ashley will confirm post-deploy (out of this task's automated scope, but the change is small and every visual delta traces to a specific verified CSS/JSX change):

- Rows visibly gapped by 8px, panel padding 14/12/18.
- Fleet-only + RDP row clicks trigger the same lit/ambient transition as plain rows.
- RDP avatar shows a larger 18px Monitor glyph.
- Chevron visible without hover; hover still bumps.
- Chevron does not overlap the "Conversations" title on desktop with sidebar open.
</verification>

<success_criteria>
All five UAT-caught mock v4 parity fixes shipped in ONE atomic commit as fork patch #142 on the current `feat/tab-title-from-tmux` branch. TypeScript clean, pretty-conversations tests green, build clean. No deploy triggered (Ashley batches fork patches per her 2026-07-23 rule). No patches.md pin (Tina handles at deploy time).
</success_criteria>

<output>
Create `.planning/quick/260724-723-conversation-list-mock-v4-parity-row-gap/260724-723-SUMMARY.md` when done, following the standard summary template.
</output>
