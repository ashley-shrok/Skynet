---
phase: quick-260803-05i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/use-pretty-view-uploads.ts
  - src/ui/features/pretty-view/use-pretty-view-uploads.test.ts
  - src/ui/features/pretty-view/ComposeBox.tsx
  - src/ui/features/pretty-view/ComposeBox.test.tsx
  - src/ui/features/pretty-view/PrettyView.tsx
autonomous: true
requirements:
  - QUICK-260803-05I
tags:
  - frontend
  - pretty-view
  - composebox
  - uploads
  - queued-rows
  - bounty-close

must_haves:
  truths:
    - "Each queued textarea has a paperclip attach button at absolute left-1 bottom-0.5 (Send stays at right-1 bottom-0.5; Mic at right-11; Arm-idle at right-21 — same right-side layout Quick 260802-uow shipped)"
    - "Paperclip click on queued slot N opens the same file picker as primary and stages the selected files to target string `queued:${slot.id}` (never `primary`)"
    - "Each queued textarea has a small delete × button positioned OUTSIDE the textarea's border, at the top-right corner, always visible with 30% opacity default → 90% on hover (visual differentiator vs. main textarea per Ashley's design)"
    - "The old left-slot delete × button is REMOVED from queued rows (that slot is now the paperclip)"
    - "Clicking the top-right delete × removes the slot from queueSlots AND clears that slot's staged attachments via the new clearStagedForTarget hook API"
    - "Each queued textarea's paddingLeft mirrors the primary textarea's paperclip-clearance treatment (pl-11) so composed text does not underlap the paperclip"
    - "Each queued textarea renders an overlaid chip strip inside its wrapper (same absolute top-0 + dynamic paddingTop + ResizeObserver pattern Quick A shipped on the main textarea) when that slot has staged attachments; empty state renders no strip and no extra padding"
    - "The queued-message ARMED overlay renders as ONE inline row: icon + label + fire-order badge (when 2+ queued) + 'click to cancel' copy (was: icon-above-label vertical stack)"
    - "The 'click to cancel' copy is the literal lowercase phrase per Ashley's ask; the overlay itself remains the cancel affordance (existing cancelSourceArmed onClick unchanged)"
    - "The fire-order badge renders as compact `N/M` (this slot's fire position out of total queued count) ONLY when queueSlots.length >= 2; absent when 1 (single queued slot has no visual noise)"
    - "usePrettyViewUploads exposes a new clearStagedForTarget(target) public API that empties a target's staged list AND aborts any in-flight uploads for tempIds in that target (mirrors removeAttachment's abort-flag pattern per tempId)"
    - "PrettyView wires new ComposeBox props: onAttachFilesForTarget (target-aware staging entry for queued slots), getStagedAttachmentsForTarget (per-slot chip-strip reads), clearStagedForTarget (delete-slot clearing) — legacy primary-only onAttachFiles / stagedAttachments / onRemoveAttachment stay in place unchanged"
    - "Queued row is extracted into a small QueuedRow child component inside ComposeBox.tsx so per-slot chipStripRef + chipStripHeight state + ResizeObserver are naturally scoped to each row instance (no Map-of-refs plumbing in the parent)"
    - "Full frontend test suite passes; net-new failure count is 0 vs. Quick A's 1087/6/0 baseline"
  artifacts:
    - path: "src/ui/features/pretty-view/use-pretty-view-uploads.ts"
      provides: "clearStagedForTarget(target) public API + hook-test coverage"
      contains: "clearStagedForTarget"
    - path: "src/ui/features/pretty-view/use-pretty-view-uploads.test.ts"
      provides: "clearStagedForTarget test coverage (target-isolation on clear + no-leakage)"
      contains: "clearStagedForTarget"
    - path: "src/ui/features/pretty-view/ComposeBox.tsx"
      provides: "QueuedRow child component + per-slot paperclip + top-right delete corner tab + overlay chip strip on queued textareas + one-line armed-overlay restructure with fire-order badge and click-to-cancel copy"
      contains: "QueuedRow"
    - path: "src/ui/features/pretty-view/ComposeBox.test.tsx"
      provides: "New Quick B test coverage: paperclip-on-slot, top-right delete-tab, delete clears staged, per-slot chip overlay, padding-parity, one-line header, fire-order badge presence gate"
      contains: "Quick B"
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "Target-aware ComposeBox wiring (onAttachFilesForTarget + getStagedAttachmentsForTarget + clearStagedForTarget)"
      contains: "onAttachFilesForTarget"
  key_links:
    - from: "QueuedRow chipStripRef"
      to: "queued textarea's paddingTop"
      via: "useLayoutEffect + ResizeObserver (JSDOM-guarded) — same pattern Quick A applied to the primary Textarea"
      pattern: "ResizeObserver"
    - from: "QueuedRow paperclip onClick"
      to: "usePrettyViewUploads.stageAttachments"
      via: "onAttachFilesForTarget(target, files) prop threaded through PrettyView → hook, where target = `queued:${slot.id}`"
      pattern: "queued:"
    - from: "QueuedRow top-right delete tab onClick"
      to: "clearStagedForTarget(target)"
      via: "combined handler removes slot from queueSlots AND clears its staged attachments"
      pattern: "clearStagedForTarget"
    - from: "queued armed-overlay button"
      to: "cancelSourceArmed(slot.id)"
      via: "existing onClick unchanged; only the layout children (icon+label+badge+copy inline) restructured"
      pattern: "click to cancel"
---

<objective>
Quick B — CLOSER of the paired ship for bounties `attached-files-as-chips-in-textarea-per-message` (#2)
+ `adjust-visual-on-queued-messages` (#1). Quick A (778b18a + fa2c792) shipped the state model
(per-target Map staging) + main-composebox chip overlay. Quick B does queued-row surgery and closes
BOTH bounties in one coherent set.

Nine pieces, grouped into 3 atomic commits:
- **Commit 1 (Task 1):** per-queued paperclip attach + delete × relocated to top-right OUTSIDE
  the textarea + padding parity (pl-11 on queued) + a new `clearStagedForTarget(target)` public
  API on usePrettyViewUploads to support the delete-clear semantics + PrettyView wiring
  (onAttachFilesForTarget / getStagedAttachmentsForTarget / clearStagedForTarget) + tests.
- **Commit 2 (Task 2):** per-queued overlay chip strip (extension of Quick A's overlay pattern
  to each queued textarea). Requires extracting the queued row into a small `QueuedRow` child
  component inside ComposeBox.tsx so per-slot refs + ResizeObserver are naturally scoped.
  Tests.
- **Commit 3 (Task 3):** bounty #1 — restructure the queued-message ARMED overlay from an
  icon-above-label vertical stack into a single inline row: icon + label + fire-order badge
  (when 2+ queued) + literal lowercase "click to cancel" copy. Tests.

Purpose: closes bounty #2 (chips-inside-textarea-per-message) AND bounty #1 (queued-message
visual restructure) as the paired ship's second half; unlocks Ashley's per-slot attachment
workflow with a coherent visual language.

Output: 3 atomic commits on `feat/tab-title-from-tmux`, PLAN + SUMMARY, and full frontend test
suite green. STOP after commit 3 — deploy queue join is orchestrator's decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/quick/260802-wxy-chips-inside-textarea-overlay-stagedatta/260802-wxy-SUMMARY.md

@src/ui/features/pretty-view/use-pretty-view-uploads.ts
@src/ui/features/pretty-view/use-pretty-view-uploads.test.ts
@src/ui/features/pretty-view/AttachmentChipStrip.tsx
@src/ui/features/pretty-view/ComposeBox.tsx
@src/ui/features/pretty-view/ComposeBox.test.tsx
@src/ui/features/pretty-view/PrettyView.tsx
</context>

<scope_locks>
- Frontend-only. If any backend file (`src/backend/**`, `docker/**`, `prisma/**`) surfaces in
  the diff, HALT and surface — do NOT touch backend files silently.
- Do NOT branch. Already on `feat/tab-title-from-tmux` per orchestrator.
- Do NOT push, docker build, or docker compose up. STOP after commit 3. The deploy queue holds
  #267→#283 already; Quick B's commits join it and wait for Ashley's ship word.
- Do NOT include `skynet-patches.md`, bounty-timeline, or bounty-archival edits — Tina handles
  those post-executor as orchestrator.
- Do NOT touch the main-composebox chip strip position (Quick A's fa2c792 shipped it — leave
  alone) or the main textarea's layout / paperclip / send button.
- Do NOT change the `queue` FIFO ordering logic itself; fire order is READ-ONLY input to the
  fire-order badge (index in the `queue` array, filtered to this slot's source).
- Do NOT add drag-and-drop staging to queued textareas in this Quick. If the codebase already
  has a drop handler on queued textareas today, redirect it to the slot's target; if not, note
  in SUMMARY.md as a nice-to-have follow-up and skip.
- Do NOT touch the composebox-vertical-spacing-polish bounty or the pv-paste-to-terminal
  bounty — those are separate pinned bounties, not part of this pair.
- `workflow.use_worktrees=false` — do NOT enable worktree mode.
- `npm run build:backend` is NOT required for this Quick (frontend-only). Pre-push check is
  `npx tsc --noEmit`. If a backend file DOES land in the diff (which the plan prevents),
  running `build:backend` becomes mandatory — but flag it as a scope violation first.
</scope_locks>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-queued paperclip attach + top-right delete corner tab + padding parity + clearStagedForTarget hook API + PrettyView wiring (Commit 1)</name>
  <files>
    src/ui/features/pretty-view/use-pretty-view-uploads.ts,
    src/ui/features/pretty-view/use-pretty-view-uploads.test.ts,
    src/ui/features/pretty-view/ComposeBox.tsx,
    src/ui/features/pretty-view/ComposeBox.test.tsx,
    src/ui/features/pretty-view/PrettyView.tsx
  </files>
  <behavior>
    - Hook exposes new `clearStagedForTarget(target: string): void` public API that:
      - Reads `attachmentsRefByTarget.current.get(target) ?? []` for tempIds.
      - For each tempId in that target, sets `abortFlagsRef.current.set(tempId, true)` so any
        in-flight pump loop bails out at its next chunk check (mirrors removeAttachment's
        abort-flag pattern — see use-pretty-view-uploads.ts removeAttachment implementation).
      - Deletes each tempId's entry from `lastOffsetSentRef.current`.
      - Calls `setAttachments(target, [])` — the existing setAttachments helper already
        constructs an immutable Map clone and keeps the ref in lockstep, so this cleanly
        empties the target without touching other targets.
      - Does NOT touch `batchIdRef`, `capturedCaptionRef`, `readyFiredRef`, or
        `batchInFlight` — those are per-batch-of-primary concerns; queued targets don't
        participate in the primary chunk-pump batch lifecycle (they stage without a batch
        currently — no queued-slot upload_ready_to_inject flow is wired in this Quick;
        that's a follow-up if Ashley ever wants queued-slot send to inject an attachments
        turn).
    - Hook test file gets 2 new tests in the existing `describe("target-aware API ...")` block:
      T-7: `clearStagedForTarget("queued:slot-a")` on a hook with 2 files staged to that
        target AND 1 file staged to "primary" AND 1 file staged to another queued target
        empties ONLY "queued:slot-a" — the other two targets remain intact.
      T-8: `clearStagedForTarget("queued:slot-a")` on a target with 0 staged attachments is
        a no-op (does not throw, does not mutate other targets, returns undefined).
    - ComposeBox.tsx queued-row block (~L1816-2038): every queued row gains a paperclip
      attach button at absolute left-1 bottom-0.5, using the SAME visual treatment as the
      existing delete × (currently at that position — the paperclip PHYSICALLY REPLACES the
      delete × in the left-1 bottom-0.5 slot). Icon: `<Paperclip className="size-6" />` from
      lucide-react (already imported at ComposeBox.tsx:2). aria-label: "Attach file to
      queued message". title: "Attach file". onClick: opens the SAME file picker as the
      main-composebox paperclip (reuses the existing `fileInputRef` + a new handler that
      records the target-of-intent so handleFileInputChange routes files correctly — see
      action step 6 for the target-routing mechanism).
    - ComposeBox.tsx queued-row block: the OLD left-slot delete × button (currently at
      lines 1928-1948) is REMOVED. In its place, a NEW small × button renders at the
      top-right corner OUTSIDE the textarea's border. Positioning: `absolute -top-2 -right-2`
      (or equivalent that visually protrudes as a tab OUTSIDE the rounded-[10px] border).
      Container must be an ancestor of BOTH the textarea wrapper AND the delete tab — so
      the tab renders in the OUTER `<div key={slot.id} className="relative flex-1" ...>`
      container (line 1864), positioned outside the inner textarea+overlay content.
      Size: `size-6` icon (matches existing delete X size). Colors: same 30%→90% opacity
      treatment as the paperclip. aria-label: "Delete queued message". title: "Delete
      queued message". onClick handler: (a) removes slot from queueSlots via existing
      pattern (setQueueSlots + scheduleAutosave), AND (b) calls
      `clearStagedForTarget("queued:${slot.id}")` via a new prop threaded from PrettyView.
    - ComposeBox.tsx queued-row block: the queued Textarea's className gains `pl-11` (mirrors
      the primary's `showPaperclip && "pl-11"` at ComposeBox.tsx:2165). Queued textareas
      ALWAYS have a paperclip now, so pl-11 applies unconditionally — no gate needed. The
      existing `pr-10 pl-10` string on ComposeBox.tsx:1889 becomes `pr-10 pl-11` (tailwind-
      merge later-wins dedupes cleanly; drop the `pl-10` from the same string). Preserve the
      existing `slotThreeButtonState && "pr-32"` right-side padding bump unchanged.
    - ComposeBox.tsx: new `activeStagingTargetRef = useRef<string | null>(null)` records the
      last-clicked paperclip's target (either "primary" or "queued:${slot.id}"). The
      existing `handleFileInputChange` at line 308 reads this ref, defaults to "primary" if
      null (safety net for the paperclip-inside-textarea main-composebox path where the ref
      was never set), and calls the new `onAttachFilesForTarget?.(target, files)` prop when
      available, falling back to the legacy `onAttachFiles?.(files)` (primary-only) when
      the target-aware prop is absent — preserves backward-compat for any consumer that
      hasn't threaded the new prop yet.
    - ComposeBox props gain three NEW optional fields (all backward-compatible):
      - `onAttachFilesForTarget?: (target: string, files: File[]) => void` — target-aware
        staging entry point.
      - `getStagedAttachmentsForTarget?: (target: string) => StagedAttachmentLike[]` —
        per-slot chip-strip read (used in Task 2 for overlay rendering; declared here so
        the prop interface is stable across both tasks).
      - `clearStagedForTarget?: (target: string) => void` — per-slot clear for the delete
        handler and any future slot-level "cancel all attachments" gesture.
      Also thread `onRemoveAttachment` unchanged — the hook's `removeAttachment(tempId)`
      already walks all targets internally (per Quick A's SUMMARY line 89-91), so it works
      for queued-slot chips without a new API.
    - PrettyView.tsx wires the three new props onto the ComposeBox (around
      PrettyView.tsx:1397-1406). Legacy primary-only `stagedAttachments`, `onRemoveAttachment`,
      `onAttachFiles` stay in place unchanged — the target-aware props are additive.
    - New ComposeBox tests (adjacent to the existing `queue slots (bounty:
      message-queue-in-pretty-view)` describe block or in a new `Quick B` describe):
      QB-1: Queued row renders a paperclip attach button at absolute left-1 bottom-0.5 with
        30% opacity className. Assert the button exists via aria-label /attach file/i AND
        its className contains "left-1", "bottom-0.5", and "opacity" (or the equivalent
        `text-[rgba(240,235,224,0.3)]` treatment that produces 30% default; use the same
        className the existing paperclip uses at line 2214-2233 verbatim).
      QB-2: Clicking the queued row's paperclip button routes to `onAttachFilesForTarget`
        with a target string beginning `"queued:"` (not `"primary"`). Set up: mock
        onAttachFilesForTarget, mount ComposeBox with a hydrated queueSlot, click Plus if
        needed to add one, click the slot's paperclip, simulate the file input's change
        event with a File, assert onAttachFilesForTarget called with (`queued:*`, [file]).
      QB-3: Queued row renders a delete × in the top-right corner OUTSIDE the textarea
        wrapper. Assert: the delete button (aria-label /delete queued message/i) is a
        DESCENDANT of the outer slot container (`div[data-slot-id="..."]`) but is NOT a
        descendant of the inner textarea wrapper (the wrapper carrying the Textarea +
        armed overlay + paperclip + Send). Concrete assertion:
          `expect(deleteBtn.closest("[data-slot-id]")).not.toBeNull();`
          `expect(deleteBtn.closest("textarea")).toBeNull();`  // trivially true — sanity
          `const outer = deleteBtn.closest("[data-slot-id]")!;`
          `const innerWrapper = outer.querySelector("div.relative.flex-1.self-stretch") ??
            outer.querySelector("div.relative.flex-1");`
        If innerWrapper exists (Task 2 will make it exist; in Task 1 the delete tab is
        already OUTSIDE any inner wrapper that Task 2 will introduce), assert the delete
        button is NOT a descendant of it: `expect(innerWrapper?.contains(deleteBtn) ??
        false).toBe(false)`. If innerWrapper is null in Task 1 (Task 2 introduces the
        inner overlay wrapper), fall back to asserting the delete button's className
        contains "-top" (proves negative top positioning — the tab protrudes upward
        outside the textarea).
      QB-4: Clicking the delete × removes the queued slot AND calls clearStagedForTarget
        with the slot's target. Set up: mock clearStagedForTarget, mount ComposeBox, click
        Plus to add a slot, click the slot's delete × (the top-right corner tab), assert:
        (a) `screen.queryAllByRole("textbox").length` decreased by 1 (slot gone), and
        (b) clearStagedForTarget was called with a target string beginning `"queued:"`.
      QB-5: Queued textarea's className contains "pl-11" (padding parity for the left-side
        paperclip clearance). Existing className already had `pl-10` — assert the FINAL
        computed className contains "pl-11" and does NOT contain a standalone `pl-10 ` (with
        trailing space or class boundary) — the token should be replaced, not appended.
        Simpler assertion: `expect(slotTextarea.className).toMatch(/\bpl-11\b/)`.
      QB-6: The OLD left-slot delete × is GONE from queued rows. Assert:
        `const deleteBtn = screen.getByRole("button", { name: /delete queued message/i });
         expect(deleteBtn.className).not.toMatch(/\bleft-1\b/);`
        (The new tab lives at the top-right, not left-1 bottom-0.5.)
    - Hook-test updates: add T-7 and T-8 to the existing `describe("target-aware API ...")`
      block (see behavior above). No existing hook test needs to change — clearStagedForTarget
      is additive.
    - Verification gates:
      - `npx tsc --noEmit` EXIT 0.
      - Narrow: `npx vitest run src/ui/features/pretty-view/use-pretty-view-uploads.test.ts`
        → 22 passed (20 pre-existing per Quick A's SUMMARY + 2 new T-7/T-8).
      - Narrow: `npx vitest run src/ui/features/pretty-view/ComposeBox.test.tsx` →
        pre-existing count + 6 new QB tests, all green (Quick A left the ComposeBox suite at
        39; expect 45 after this task).
      - Do NOT run full suite here — Task 3's verify runs the full suite. This keeps the
        Task 1 commit fast to iterate on.
  </behavior>
  <action>
    RED-GREEN-REFACTOR: write hook tests T-7 + T-8 first (they fail because
    clearStagedForTarget doesn't exist), then implement the API and the ComposeBox surgery,
    then write ComposeBox tests QB-1..QB-6 alongside the surgery. See ordering below.

    **Step 1 — Hook API test (RED): add T-7 + T-8 to use-pretty-view-uploads.test.ts.**
    Inside the existing `describe("target-aware API (Quick 260802-wxy)")` block (around
    line 673), append:
      T-7: stage to "primary", "queued:slot-a", "queued:slot-b" → call
        clearStagedForTarget("queued:slot-a") → assert:
          `result.current.stagedAttachments` (primary mirror) length unchanged.
          `result.current.getStagedAttachments("queued:slot-a")` returns `[]`.
          `result.current.getStagedAttachments("queued:slot-b")` length unchanged.
      T-8: with a fresh hook, call clearStagedForTarget("queued:slot-a") on an empty
        target — must not throw; other targets stay intact (verify by staging to primary
        AFTER the clear and asserting the stage worked).
    Reuse the existing `MockWS` + `makeMockFile` helpers.

    Run `npx vitest run src/ui/features/pretty-view/use-pretty-view-uploads.test.ts` — T-7
    and T-8 MUST fail with a TypeError-ish "result.current.clearStagedForTarget is not a
    function". If they pass, the test is wrong (rewrite to be trap-load-bearing).

    **Step 2 — Hook API implementation (GREEN): add clearStagedForTarget to
    use-pretty-view-uploads.ts.**
    a. In `UsePrettyViewUploadsReturn` interface (line 97-114), add
       `clearStagedForTarget: (target: string) => void;` after `resetBatch` (or wherever
       reads cleanly alphabetically / by-concern; do NOT reshuffle unrelated field
       ordering).
    b. Inside the hook function body, add a new `useCallback` near the `removeAttachment`
       and `resetBatch` implementations (~line 373-563):
       ```
       const clearStagedForTarget = useCallback((target: string) => {
         const current = attachmentsRefByTarget.current.get(target) ?? [];
         for (const att of current) {
           abortFlagsRef.current.set(att.tempId, true);
           lastOffsetSentRef.current.delete(att.tempId);
         }
         setAttachments(target, []);
       }, [setAttachments]);
       ```
       Comment above it: `// Quick 260803-05i: clear ONE target's staged attachments
       (aborts any in-flight pump loops for that target's tempIds; leaves other targets
       untouched). Used by ComposeBox when a queued slot is deleted — slot removal must
       also purge its per-slot staging so a re-added slot with the same id doesn't inherit
       stale entries (defense-in-depth; slot ids are UUIDs so collision is effectively
       impossible, but the invariant matters for correctness).`
    c. Add `clearStagedForTarget` to the returned useMemo object (~line 780-810) and to
       its dep array.
    d. Do NOT touch `batchIdRef`, `capturedCaptionRef`, `readyFiredRef`, `batchInFlight`,
       `pendingSendWaitingForWs`, or `folderDropRejected` — those are primary-batch-scoped
       lifecycle, not per-target. Behavior invariant for the primary target is preserved
       (all existing tests continue to pass).

    Run `npx vitest run src/ui/features/pretty-view/use-pretty-view-uploads.test.ts` —
    should now be 22 green (20 pre-existing + 2 new).

    **Step 3 — ComposeBox props interface: add three optional target-aware props.**
    In the `ComposeBoxProps` interface (~lines 174-272), insert AFTER `onAttachFiles?:
    (files: File[]) => void;` (line 200):
    ```
    // Quick 260803-05i: target-aware staging entry point. Queued slots pass their
    // own target string `queued:${slot.id}` so per-slot attachments stay isolated.
    // Legacy `onAttachFiles` remains for backward-compat + primary paste path.
    onAttachFilesForTarget?: (target: string, files: File[]) => void;
    // Quick 260803-05i: per-slot read of staged attachments. Task 2 uses this to
    // render the overlay chip strip inside each queued textarea's wrapper.
    getStagedAttachmentsForTarget?: (target: string) => StagedAttachmentLike[];
    // Quick 260803-05i: per-slot clear. QueuedRow's top-right delete × calls this
    // when a slot is removed so its staged attachments don't linger in the hook.
    clearStagedForTarget?: (target: string) => void;
    ```
    Add the three names to the destructured props at line 274-297.

    **Step 4 — Target-routing ref for the file picker.** Near
    `const fileInputRef = useRef<HTMLInputElement>(null);` at line 304, add:
    ```
    // Quick 260803-05i: records which target owns the currently-open file picker.
    // The main-composebox paperclip sets this to "primary" on click; each queued
    // slot's paperclip sets this to `queued:${slot.id}`. handleFileInputChange
    // reads the ref, defaults to "primary" if null (safety), and routes to the
    // target-aware onAttachFilesForTarget prop (falling back to onAttachFiles if
    // the target-aware prop isn't threaded — backward-compat).
    const activeStagingTargetRef = useRef<string | null>(null);
    ```
    Rewrite `handleFileInputChange` (line 308-316) to:
    ```
    const handleFileInputChange = useCallback(
      (e: React.ChangeEvent<HTMLInputElement>) => {
        const files = Array.from(e.target.files ?? []);
        const target = activeStagingTargetRef.current ?? "primary";
        activeStagingTargetRef.current = null;
        if (files.length > 0) {
          if (onAttachFilesForTarget) {
            onAttachFilesForTarget(target, files);
          } else {
            onAttachFiles?.(files);
          }
        }
        e.target.value = "";
      },
      [onAttachFiles, onAttachFilesForTarget],
    );
    ```
    Update `handleOpenFilePicker` (line 305-307) to accept an optional target:
    ```
    const handleOpenFilePicker = useCallback((target: string = "primary") => {
      activeStagingTargetRef.current = target;
      fileInputRef.current?.click();
    }, []);
    ```
    Existing main-composebox paperclip at line 2217 (`onClick={handleOpenFilePicker}`) —
    LEAVE unchanged; the default `"primary"` argument preserves behavior.

    Also update the `handlePaste` callback (line 321-330): the paste path currently
    routes to `onAttachFiles?.(files)` without knowing which textarea was pasted into.
    For Quick B, paste on the PRIMARY textarea still routes to primary (unchanged).
    Paste on a queued textarea: LEAVE the current onAttachFiles behavior in place — the
    queued Textarea does not currently have its own onPaste handler (verify by grepping
    the queued Textarea render block for `onPaste` — it's absent). Adding queued-slot
    paste routing is out of scope for this Quick per the drop-handler scope note. Do NOT
    add an onPaste handler to the queued Textarea. Note in SUMMARY.md as follow-up:
    "queued textarea paste currently routes to primary via the top-level dropzone; adding
    per-slot paste would require an onPaste on the slot's Textarea + activeStagingTargetRef
    routing — trivial mechanically but Ashley didn't ask for it."

    **Step 5 — Queued-row render surgery in ComposeBox.tsx (~L1816-2038).**
    (Task 1 keeps the queued row INLINE inside the queueSlots.map — the extraction into
    a `QueuedRow` child component happens in Task 2 when the per-slot chipStripRef / state
    make extraction load-bearing. This keeps commit 1 focused on structural + visual
    changes without adding a component boundary.)

    a. **Delete the OLD left-slot delete × button block** at lines 1928-1948 (the
       `{/* Delete X button — absolute left-1 bottom-0.5 */}` block).
    b. **Insert a new paperclip button** at the position formerly occupied by the delete ×
       (absolute left-1 bottom-0.5 slot inside the slot container). Place it INSIDE the
       queueSlots.map's `<div key={slot.id} className="relative flex-1" ...>` wrapper, at
       roughly the same DOM position the delete × occupied. Structure:
       ```
       <button
         type="button"
         onClick={() => handleOpenFilePicker(`queued:${slot.id}`)}
         disabled={canSend === false || asideActive === true || recycleActive === true}
         aria-label="Attach file to queued message"
         title="Attach file"
         className={cn(
           "absolute left-1 bottom-0.5",
           "p-2",
           "text-[rgba(240,235,224,0.3)] hover:text-[rgba(240,235,224,0.9)]",
           "disabled:text-[rgba(240,235,224,0.15)]",
           "disabled:cursor-not-allowed",
           "transition-[color,transform] duration-120",
           "active:scale-95",
           "cursor-pointer",
         )}
       >
         <Paperclip className="size-6" />
       </button>
       ```
       Verbatim visual treatment from the main-composebox paperclip at line 2214-2233 so
       Ashley's opacity/hover/pressed animation feel is identical across primary and
       queued.
    c. **Insert a NEW delete × top-right corner tab** as a sibling of the inner slot
       content, positioned OUTSIDE the textarea's rounded-[10px] border. Placement: as a
       LAST child of the outer `<div key={slot.id} className="relative flex-1" ...>`
       wrapper, so it's a sibling of the Textarea + armed-overlay + paperclip + Send/mic
       group. Structure:
       ```
       <button
         type="button"
         onClick={() => {
           const nextSlots = queueSlots.filter((s) => s.id !== slot.id);
           setQueueSlots(nextSlots);
           scheduleAutosave(latestBodyRef.current, nextSlots);
           clearStagedForTarget?.(`queued:${slot.id}`);
         }}
         aria-label="Delete queued message"
         title="Delete queued message"
         className={cn(
           "absolute -top-2 -right-2 z-20",
           "p-1 rounded-full",
           "bg-[rgba(10,12,20,0.85)] border border-[rgba(220,225,245,0.12)]",
           "text-[rgba(240,235,224,0.3)] hover:text-[rgba(240,235,224,0.9)]",
           "transition-[color,transform] duration-120",
           "active:scale-95",
           "cursor-pointer",
         )}
       >
         <X className="size-4" aria-hidden="true" />
       </button>
       ```
       Notes on styling: `-top-2 -right-2` protrudes 8px OUTSIDE the textarea's border
       (patch #82 wrapper is `relative flex-1` with no padding, so `-top`/`-right` on the
       absolute child pushes it beyond the wrapper's edges). `bg-[rgba(10,12,20,0.85)]`
       with a `rounded-full` gives the tab a small pill shape so it reads as a "tab"
       Ashley described, not a floating orphan. `z-20` sits above `z-10` overlay
       positioning to survive if any wrapping element gets a background. `size-4`
       (16×16) with `p-1` = 24×24 hit target — smaller than the left-slot paperclip
       (which is 40×40 at p-2 + size-6) intentionally, per Ashley "small × in top-right
       corner" — the tab is a differentiator visual, not a primary action.
       If Ashley's UAT flags the size / border / rounding, tweak in SUMMARY.md deviation
       note; do NOT re-litigate here.
    d. **Update the queued Textarea's className** (line 1883-1901): replace the string
       `"pr-10 pl-10"` on line 1889 with `"pr-10 pl-11"`. All other className tokens
       preserved. The `slotThreeButtonState && "pr-32"` bump at line 1894 stays unchanged
       (right-side padding for mic + arm-idle + send).

    **Step 6 — PrettyView wiring (~L1367-1427).** Add three new props to the ComposeBox
    element, after the existing `onAttachFiles` (line 1406):
    ```
    onAttachFilesForTarget={(target, files) => uploads.stageAttachments(target, files)}
    getStagedAttachmentsForTarget={uploads.getStagedAttachments}
    clearStagedForTarget={uploads.clearStagedForTarget}
    ```
    Do NOT remove the existing `onAttachFiles` wiring — it's still used by the primary
    file input's backward-compat fallback path in ComposeBox.

    **Step 7 — ComposeBox tests (QB-1..QB-6).** Add a new `describe("ComposeBox — Quick B:
    queued-row per-slot paperclip + top-right delete tab + padding parity", () => { ... })`
    block after the existing `queue slots (bounty: message-queue-in-pretty-view)` describe
    (around line 749). Reuse the `baseProps` and `mkAtt` helpers already at the top of the
    file. Use the same beforeEach pattern (vi.clearAllMocks, localStorage.clear,
    vi.useFakeTimers) as the queue-slots describe block; add afterEach with
    vi.useRealTimers.

    QB-1 — assert paperclip on queued row present at left-1 bottom-0.5 with 30% opacity
      treatment className:
      ```
      it("QB-1: queued row renders a paperclip attach button at absolute left-1 bottom-0.5 with 30% opacity default", async () => {
        render(<ComposeBox {...baseProps()} />);
        await flushMountEffect();
        fireEvent.click(screen.getByRole("button", { name: /queue a message/i }));
        const paperclip = screen.getByRole("button", { name: /attach file to queued message/i });
        expect(paperclip).toBeTruthy();
        expect(paperclip.className).toMatch(/\bleft-1\b/);
        expect(paperclip.className).toMatch(/\bbottom-0\.5\b/);
        expect(paperclip.className).toMatch(/rgba\(240,235,224,0\.3\)/); // 30% opacity treatment
      });
      ```

    QB-2 — paperclip click routes to onAttachFilesForTarget with `queued:*` target:
      ```
      it("QB-2: paperclip click on queued slot routes file-picker output to onAttachFilesForTarget(`queued:${slotId}`, files) — NOT primary", async () => {
        const onAttachFilesForTarget = vi.fn();
        render(<ComposeBox {...baseProps({ onAttachFilesForTarget })} />);
        await flushMountEffect();
        fireEvent.click(screen.getByRole("button", { name: /queue a message/i }));
        // Click the queued paperclip — this arms activeStagingTargetRef with the slot's
        // target BEFORE clicking the hidden file input.
        fireEvent.click(screen.getByRole("button", { name: /attach file to queued message/i }));
        // Simulate the file input's change event.
        const fileInput = document.querySelector('input[type="file"]') as HTMLInputElement;
        expect(fileInput).not.toBeNull();
        const f = new File(["hello"], "hello.txt", { type: "text/plain" });
        Object.defineProperty(fileInput, "files", {
          value: [f],
          configurable: true,
        });
        fireEvent.change(fileInput);
        expect(onAttachFilesForTarget).toHaveBeenCalledTimes(1);
        const [target, files] = onAttachFilesForTarget.mock.calls[0];
        expect(target).toMatch(/^queued:/);
        expect(files).toEqual([f]);
      });
      ```

    QB-3 — delete × in top-right corner OUTSIDE textarea:
      ```
      it("QB-3: queued row renders a delete × in the top-right corner OUTSIDE the textarea wrapper", async () => {
        render(<ComposeBox {...baseProps()} />);
        await flushMountEffect();
        fireEvent.click(screen.getByRole("button", { name: /queue a message/i }));
        const deleteBtn = screen.getByRole("button", { name: /delete queued message/i });
        // Corner-tab positioning: -top-* + -right-* protrudes OUTSIDE the textarea border.
        expect(deleteBtn.className).toMatch(/-top-/);
        expect(deleteBtn.className).toMatch(/-right-/);
        // Delete tab must be a descendant of the slot outer container.
        const outer = deleteBtn.closest("[data-slot-id]");
        expect(outer).not.toBeNull();
      });
      ```

    QB-4 — delete click removes slot AND clears staged for target:
      ```
      it("QB-4: clicking the delete × removes the slot AND calls clearStagedForTarget for that slot's target", async () => {
        const clearStagedForTarget = vi.fn();
        render(<ComposeBox {...baseProps({ clearStagedForTarget })} />);
        await flushMountEffect();
        fireEvent.click(screen.getByRole("button", { name: /queue a message/i }));
        expect(screen.getAllByRole("textbox").length).toBe(2); // slot + primary
        fireEvent.click(screen.getByRole("button", { name: /delete queued message/i }));
        expect(screen.getAllByRole("textbox").length).toBe(1); // primary only
        expect(clearStagedForTarget).toHaveBeenCalledTimes(1);
        expect(clearStagedForTarget.mock.calls[0][0]).toMatch(/^queued:/);
      });
      ```

    QB-5 — pl-11 padding parity:
      ```
      it("QB-5: queued textarea className carries pl-11 (padding parity for left-side paperclip clearance)", async () => {
        render(<ComposeBox {...baseProps()} />);
        await flushMountEffect();
        fireEvent.click(screen.getByRole("button", { name: /queue a message/i }));
        const allTextareas = screen.getAllByRole("textbox") as HTMLTextAreaElement[];
        const slotTextarea = allTextareas[0]; // slot renders BEFORE primary in DOM order
        expect(slotTextarea.className).toMatch(/\bpl-11\b/);
      });
      ```

    QB-6 — old left-slot delete × is GONE (regression guard against a partial migration):
      ```
      it("QB-6: the old left-1 bottom-0.5 delete × is GONE — the delete button now lives at the top-right corner (className MUST NOT contain 'left-1')", async () => {
        render(<ComposeBox {...baseProps()} />);
        await flushMountEffect();
        fireEvent.click(screen.getByRole("button", { name: /queue a message/i }));
        const deleteBtn = screen.getByRole("button", { name: /delete queued message/i });
        expect(deleteBtn.className).not.toMatch(/\bleft-1\b/);
      });
      ```

    **Step 8 — Run verify gates.**
    - `npx tsc --noEmit` (EXIT 0 required; if fail, fix TS errors — most likely missing
      prop types on ComposeBox or unused import).
    - `npx vitest run src/ui/features/pretty-view/use-pretty-view-uploads.test.ts` →
      22 green.
    - `npx vitest run src/ui/features/pretty-view/ComposeBox.test.tsx` → 45 green.

    **Step 9 — Commit.** Message:
    `feat(composebox): per-queued attach button + top-right delete corner tab + padding parity + clearStagedForTarget hook API (Quick B of paired ship #2/#1)`
    Body references bounty slugs (both `attached-files-as-chips-in-textarea-per-message`
    and `adjust-visual-on-queued-messages`) and notes "Quick B of paired ship with Quick A
    (778b18a + fa2c792). Task 2 adds the overlay chip strip on queued rows; Task 3 restructures
    the queued-message armed overlay header. Closes bounty
    attached-files-as-chips-in-textarea-per-message on Task 2 (chip strip is the last
    remaining piece of that bounty)."
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run src/ui/features/pretty-view/use-pretty-view-uploads.test.ts &amp;&amp; npx vitest run src/ui/features/pretty-view/ComposeBox.test.tsx</automated>
  </verify>
  <done>
    - `use-pretty-view-uploads.ts` exports `clearStagedForTarget(target)`; per-tempId
      abort-flags set + `setAttachments(target, [])` clears the target.
    - `use-pretty-view-uploads.test.ts` has 22 tests (20 pre-existing + T-7 + T-8), all
      green.
    - ComposeBox.tsx queued-row block: paperclip at left-1 bottom-0.5 replaces the old
      delete ×; new delete × at absolute -top-2 -right-2 lives as a sibling OUTSIDE the
      inner textarea wrapper; queued textarea className carries `pl-11`.
    - ComposeBox.tsx: `activeStagingTargetRef` + rewritten `handleFileInputChange` +
      rewritten `handleOpenFilePicker` with optional target argument route file-picker
      output to the correct target.
    - ComposeBox.tsx: three new optional props (`onAttachFilesForTarget`,
      `getStagedAttachmentsForTarget`, `clearStagedForTarget`) declared, destructured, and
      wired.
    - PrettyView.tsx passes the three new props to ComposeBox; legacy primary props
      unchanged.
    - ComposeBox.test.tsx has 6 new QB tests (QB-1..QB-6), all green.
    - `npx tsc --noEmit` EXIT 0.
    - Committed as commit #1 with the exact subject in action step 9.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-queued overlay chip strip via QueuedRow extraction (Commit 2)</name>
  <files>
    src/ui/features/pretty-view/ComposeBox.tsx,
    src/ui/features/pretty-view/ComposeBox.test.tsx
  </files>
  <behavior>
    - The queued-row render (currently inline inside `queueSlots.map(...)` at ComposeBox.tsx
      ~L1818-2036) is extracted into a small internal `QueuedRow` component defined WITHIN
      ComposeBox.tsx (same file — do NOT create a new file). The extraction is load-bearing
      because per-slot `chipStripRef` + `chipStripHeight` state + useLayoutEffect +
      ResizeObserver are naturally scoped to each row instance without Map-of-refs plumbing
      in the parent.
    - `QueuedRow` receives ALL props it needs as function arguments from the parent
      `ComposeBox`. The parent's `queueSlots.map(...)` becomes:
      ```
      queueSlots.map((slot) => (
        <QueuedRow
          key={slot.id}
          slot={slot}
          {...allTheDerivedStateAndCallbacksItNeeds}
        />
      ))
      ```
      Do NOT extract as a top-level exported component — QueuedRow is an internal
      implementation detail. Place its function declaration BELOW the ComposeBox function
      but in the same module scope so it can reference imports without prop-drilling
      constants. React hooks rules apply — QueuedRow must use hooks unconditionally.
    - QueuedRow owns:
      - `chipStripRef: useRef<HTMLDivElement | null>(null)`
      - `chipStripHeight: useState(0)`
      - `useLayoutEffect` measuring the strip (verbatim pattern from ComposeBox.tsx L954-979,
        JSDOM-guarded, keyed on the count of staged attachments for THIS slot's target).
      - Its own local read: `const stagedForThisSlot = getStagedAttachmentsForTarget?.(target)
        ?? [];` where `target = "queued:${slot.id}"`.
    - Inside the queued textarea's positioning wrapper (`<div key={slot.id} className="relative
      flex-1" ...>` at line 1864), the Textarea is now wrapped in a NEW nested wrapper that
      matches the primary's `relative flex-1 self-stretch` pattern so the chip strip can
      position `absolute top-0 left-0 right-0` inside it. Concretely:
      - Outer container: `<div data-slot-id={slot.id} className="relative flex-1">` (unchanged
        outer boundary — the delete-tab from Task 1 stays a child of this).
      - NEW inner wrapper: `<div className="relative flex-1 self-stretch">` — contains the
        chip strip overlay child + the Textarea + the armed overlay + the paperclip + the
        send/mic/arm-idle button group. This mirrors the primary composebox's structure at
        ComposeBox.tsx:2050.
      - The delete × top-right corner tab from Task 1 is a SIBLING of the inner wrapper,
        NOT a descendant — so the tab visually protrudes OUTSIDE the border (Task 1's
        QB-3 assertion continues to hold; Task 2 must NOT regress it).
    - Chip strip overlay child (adjacent to the chipStripRef pattern Quick A used on
      primary at ComposeBox.tsx:2062-2070):
      ```
      <div
        ref={chipStripRef}
        className="absolute top-0 left-0 right-0 z-10 px-2 pt-2 pointer-events-auto"
      >
        <AttachmentChipStrip
          attachments={stagedForThisSlot}
          onRemove={onRemoveAttachment ?? (() => {})}
        />
      </div>
      ```
      (AttachmentChipStrip returns null when the list is empty — same UPLOAD-04 mounting
      rule Quick A relied on. When empty, chipStripRef.current becomes null and the
      useLayoutEffect resets chipStripHeight to 0 → textarea paddingTop reverts to base
      py-3.)
    - Queued Textarea receives an inline `style` prop identical to the primary's pattern
      at ComposeBox.tsx:2087-2091:
      ```
      style={
        chipStripHeight > 0
          ? { paddingTop: `${chipStripHeight + 12}px` }
          : undefined
      }
      ```
      The queued Textarea's className preserves all existing tokens including the Task 1
      `pl-11` and the `slotThreeButtonState && "pr-32"` bump.
    - Reads via `onRemoveAttachment` (existing prop, unchanged — the hook's
      removeAttachment(tempId) walks all targets internally per Quick A's SUMMARY).
    - Queued row keeps all existing behavior:
      - Armed overlay + click-to-cancel (Task 3 restructures its interior layout).
      - Send / RecordingControls mutex slot at right-1 bottom-0.5.
      - Mic at right-11 bottom-0.5 when showSlotMic.
      - Arm-idle at right-21 bottom-0.5 when showSlotArmButton.
      - Paperclip at left-1 bottom-0.5 (from Task 1).
      - Top-right corner delete × tab (from Task 1).
      - autosave/onChange/onBlur unchanged.
    - New ComposeBox tests:
      QB-7: When a queued slot has staged attachments (via getStagedAttachmentsForTarget
        returning non-empty), the chip strip renders INSIDE the slot's inner textarea
        wrapper (mirrors Quick A's Test 2b for the primary). Assert:
          `strip.closest("[data-slot-id]")` is the slot's outer container.
          `strip.closest("div.relative.flex-1.self-stretch")` exists AND equals
          `slotTextarea.closest("div.relative.flex-1.self-stretch")`.
          `strip.parentElement.className` contains `absolute` (overlay positioning).
        Setup: mock `getStagedAttachmentsForTarget` to return two mkAtt entries when
        called with the slot's target; queue-add a slot; assert chip strip presence and
        wrapper co-location.
      QB-8: When a queued slot has NO staged attachments, no chip strip renders in that
        slot (empty state adds no DOM element and no padding).
        Setup: `getStagedAttachmentsForTarget` returns `[]`; queue-add a slot; assert
        `strip = screen.queryAllByTestId("attachment-chip-strip")` — either exactly 0 or
        exactly the primary's (mock primary as empty too so total is 0 for the cleanest
        assertion).
      QB-9: Regression guard — the primary composebox's chip strip is still present and
        still lives inside the primary textarea's wrapper (Quick A's invariant preserved
        after Task 2's QueuedRow extraction; extraction MUST NOT break the primary).
        Setup: mount with `stagedAttachments` (primary) length 2, assert
        `attachment-chip-strip` count >= 1 AND at least one is a descendant of the primary
        textarea's wrapper (any div matching `.relative.flex-1.self-stretch` that
        `.contains(primaryTextarea)`).
      QB-3-followup: verify Task 1's QB-3 delete-tab-outside-inner-wrapper assertion still
        passes AFTER the QueuedRow extraction introduces the inner wrapper. Either re-run
        QB-3 (it should already pass because the tab is placed as a sibling of the inner
        wrapper, not a descendant) OR strengthen QB-3 with the new inner-wrapper
        assertion. Simplest: leave QB-3 as-is; if it fails after extraction, that's the
        signal to fix the extraction, not the test.
    - Verification:
      - `npx tsc --noEmit` EXIT 0.
      - `npx vitest run src/ui/features/pretty-view/ComposeBox.test.tsx` → 48 green
        (45 after Task 1 + QB-7, QB-8, QB-9).
      - Do NOT run full suite yet — Task 3 runs it.
  </behavior>
  <action>
    **Step 1 — Design the QueuedRow prop surface.** Before extracting, list every dep the
    inline queueSlots.map body uses today (see ComposeBox.tsx L1818-2036):
      - `slot: { id: string; text: string }`
      - `voice`, `micTarget`
      - `isSourceArmed(source)`
      - `asideActive`, `recycleActive`, `canSend`
      - `queueSlots` (for filter-on-delete + map-on-change), `setQueueSlots`
      - `scheduleAutosave`, `latestBodyRef`
      - `handleQueueSlotSend(slotId)`
      - `armSourceForIdle(source, text)`, `cancelSourceArmed(source)`
      - `handleVoiceCancel`, `handleVoiceAppend(source)`, `handleVoiceSend(source)`,
        `beginRecord(source)`
      - `handleOpenFilePicker(target)` (from Task 1)
      - `onAttachFilesForTarget`, `getStagedAttachmentsForTarget`,
        `clearStagedForTarget`, `onRemoveAttachment`
      - `flushDirty`, `clearDebounce`
      - `RotateCwFadingClock`, `Loader2`, `X`, `MicButton`, `RecordingControls`,
        `Textarea` (already imported at module scope — QueuedRow can reference directly)
      - `cn` (imported)
    Almost all of these are stable callbacks or reactive state; pass them through as an
    object prop for readability. Type the props explicitly at the top of ComposeBox.tsx
    or inline on the QueuedRow declaration — inline is fine given the row is a private
    implementation detail.

    Suggested signature (adjust to whatever is most ergonomic):
    ```
    interface QueuedRowProps {
      slot: { id: string; text: string };
      voice: ReturnType<typeof useVoiceRecording>;
      micTarget: string | null;
      isSourceArmed: (source: string) => boolean;
      asideActive?: boolean;
      recycleActive?: boolean;
      canSend?: boolean;
      queueSlots: Array<{ id: string; text: string }>;
      onSlotsChange: (next: Array<{ id: string; text: string }>) => void;
      scheduleAutosave: (body: string, slots: Array<{ id: string; text: string }>) => void;
      latestBody: string;
      handleQueueSlotSend: (slotId: string) => void;
      armSourceForIdle: (source: string, text: string) => void;
      cancelSourceArmed: (source: string) => void;
      handleVoiceCancel: () => void;
      handleVoiceAppend: (source: string) => void | Promise<void>;
      handleVoiceSend: (source: string) => void | Promise<void>;
      beginRecord: (source: string) => void;
      handleOpenFilePicker: (target?: string) => void;
      getStagedAttachmentsForTarget?: (target: string) => StagedAttachmentLike[];
      clearStagedForTarget?: (target: string) => void;
      onRemoveAttachment?: (tempId: string) => void;
      flushDirty: () => Promise<void> | void;
      clearDebounce: () => void;
    }
    ```
    Note: `onSlotsChange` is a thin wrapper the parent provides — see Step 3.

    **Step 2 — Extract QueuedRow.** Cut the inline slot render body (L1863-2036,
    everything inside `return (...)` in queueSlots.map's callback), paste into a new
    function declaration `function QueuedRow(props: QueuedRowProps) { ... }` placed
    BELOW the ComposeBox function definition. Preserve all logic verbatim except:
    - Replace `queueSlots` writes (`setQueueSlots(nextSlots)` calls) with
      `props.onSlotsChange(nextSlots)`.
    - Replace `latestBodyRef.current` reads with `props.latestBody`.
    - Add the new chipStripRef + chipStripHeight state + useLayoutEffect at the TOP of
      QueuedRow's function body (near where the derived `slotArmed`, `slotHasText`, etc.
      constants sit — those stay).
    - Read `stagedForThisSlot = props.getStagedAttachmentsForTarget?.(`queued:${slot.id}`)
      ?? []` and use `stagedForThisSlot.length` as the useLayoutEffect's dep.
    - Wrap the Textarea + armed overlay + Send/Mic/Arm/Paperclip button group in a NEW
      inner `<div className="relative flex-1 self-stretch">` wrapper (mirrors primary at
      L2050). The chip strip overlay child goes at the TOP of that inner wrapper (before
      the Textarea) with the standard `<div ref={chipStripRef} className="absolute top-0
      left-0 right-0 z-10 px-2 pt-2 pointer-events-auto">...` pattern.
    - The top-right corner delete × tab from Task 1 stays a DIRECT child of the outer
      slot container (`<div data-slot-id={slot.id} className="relative flex-1">`), NOT the
      inner wrapper — so it protrudes outside the border.
    - Add `data-slot-id={slot.id}` to the outer div if not already present (Task 1 already
      added it per its render surgery; verify).

    **Step 3 — Rewrite queueSlots.map in ComposeBox.** Replace the entire inline body
    with:
    ```
    {queueSlots.length > 0 && (
      <div className="flex flex-col gap-2 mb-1">
        {queueSlots.map((slot) => (
          <QueuedRow
            key={slot.id}
            slot={slot}
            voice={voice}
            micTarget={micTarget}
            isSourceArmed={isSourceArmed}
            asideActive={asideActive}
            recycleActive={recycleActive}
            canSend={canSend}
            queueSlots={queueSlots}
            onSlotsChange={(next) => {
              setQueueSlots(next);
              scheduleAutosave(latestBodyRef.current, next);
            }}
            scheduleAutosave={scheduleAutosave}
            latestBody={latestBodyRef.current}
            handleQueueSlotSend={handleQueueSlotSend}
            armSourceForIdle={armSourceForIdle}
            cancelSourceArmed={cancelSourceArmed}
            handleVoiceCancel={handleVoiceCancel}
            handleVoiceAppend={handleVoiceAppend}
            handleVoiceSend={handleVoiceSend}
            beginRecord={beginRecord}
            handleOpenFilePicker={handleOpenFilePicker}
            getStagedAttachmentsForTarget={getStagedAttachmentsForTarget}
            clearStagedForTarget={clearStagedForTarget}
            onRemoveAttachment={onRemoveAttachment}
            flushDirty={flushDirty}
            clearDebounce={clearDebounce}
          />
        ))}
      </div>
    )}
    ```
    Note: `micTarget`, `beginRecord`, `handleVoiceCancel`, `handleVoiceAppend`,
    `handleVoiceSend`, `armSourceForIdle`, `cancelSourceArmed`, `flushDirty`,
    `clearDebounce` are function-scope constants in ComposeBox; grep the file to confirm
    each is defined in ComposeBox's function body before the queueSlots.map render (they
    all are per the L400-1300 range).

    If any of the extracted callbacks turn out to be inline function expressions in the
    original body that don't exist as named function-scope constants, hoist them into
    ComposeBox's function body first (before the render return) so they can be passed as
    props cleanly. Prefer this hoist over inlining new arrow functions inside the map's
    callback (would defeat the extraction's simplicity).

    **Step 4 — Add tests QB-7, QB-8, QB-9.** In the same `Quick B` describe block created
    in Task 1, append:
    ```
    it("QB-7: queued slot with staged attachments renders overlay chip strip inside the slot's inner textarea wrapper (mirrors Quick A's Test 2b for the primary)", async () => {
      const getStagedAttachmentsForTarget = vi.fn((target: string) =>
        target.startsWith("queued:")
          ? [mkAtt("q1", "queued-file-1.txt"), mkAtt("q2", "queued-file-2.txt")]
          : [],
      );
      render(
        <ComposeBox
          {...baseProps({
            getStagedAttachmentsForTarget,
            onRemoveAttachment: vi.fn(),
          })}
        />,
      );
      await flushMountEffect();
      fireEvent.click(screen.getByRole("button", { name: /queue a message/i }));
      // At least one chip strip should render — inside the slot.
      const strips = screen.getAllByTestId("attachment-chip-strip");
      expect(strips.length).toBeGreaterThanOrEqual(1);
      // The slot's chip strip lives inside the slot's `.relative.flex-1.self-stretch`
      // inner wrapper.
      const slotTextarea = (screen.getAllByRole("textbox") as HTMLTextAreaElement[])[0];
      const slotInnerWrapper = slotTextarea.closest("div.relative.flex-1.self-stretch");
      expect(slotInnerWrapper).not.toBeNull();
      // At least ONE strip is a descendant of that inner wrapper.
      const inSlotStrip = strips.find((s) => slotInnerWrapper!.contains(s));
      expect(inSlotStrip).toBeTruthy();
      // Overlay positioning check.
      expect(inSlotStrip!.parentElement!.className).toMatch(/absolute/);
    });

    it("QB-8: queued slot with NO staged attachments renders no chip strip in that slot (empty state adds no DOM element)", async () => {
      const getStagedAttachmentsForTarget = vi.fn(() => []);
      render(<ComposeBox {...baseProps({ getStagedAttachmentsForTarget })} />);
      await flushMountEffect();
      fireEvent.click(screen.getByRole("button", { name: /queue a message/i }));
      const strips = screen.queryAllByTestId("attachment-chip-strip");
      // Primary is also empty (baseProps doesn't set stagedAttachments), so total = 0.
      expect(strips.length).toBe(0);
    });

    it("QB-9: primary chip strip regression guard — extracting QueuedRow does NOT break the primary's Quick A overlay", async () => {
      render(
        <ComposeBox
          {...baseProps({
            stagedAttachments: [mkAtt("a", "primary-1.txt"), mkAtt("b", "primary-2.txt")],
            onRemoveAttachment: vi.fn(),
          })}
        />,
      );
      await flushMountEffect();
      const strip = screen.getByTestId("attachment-chip-strip");
      const textarea = screen.getByPlaceholderText(/message/i);
      const stripWrapper = strip.closest("div.relative.flex-1.self-stretch");
      const textareaWrapper = textarea.closest("div.relative.flex-1.self-stretch");
      expect(stripWrapper).not.toBeNull();
      expect(textareaWrapper).not.toBeNull();
      expect(stripWrapper).toBe(textareaWrapper);
    });
    ```

    **Step 5 — Run verify gates.**
    - `npx tsc --noEmit` EXIT 0.
    - `npx vitest run src/ui/features/pretty-view/ComposeBox.test.tsx` → 48 green.
    - Watch specifically for regressions in existing QS 1-10 (queue slot tests) and
      Quick A's Test 2 / Test 2b — the QueuedRow extraction is the highest-risk change
      in this Quick.

    **Step 6 — Commit.** Message:
    `feat(composebox): overlay chip strip on queued textareas via QueuedRow extraction (per-target staging live) (Quick B of paired ship #2/#1)`
    Body: reference `attached-files-as-chips-in-textarea-per-message`; note
    "extraction of the queueSlots.map body into a private QueuedRow child component
    inside ComposeBox.tsx scopes chipStripRef + ResizeObserver per instance — no
    Map-of-refs plumbing. Closes bounty attached-files-as-chips-in-textarea-per-message."
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run src/ui/features/pretty-view/ComposeBox.test.tsx</automated>
  </verify>
  <done>
    - Queued-row render is now a private `QueuedRow` function component inside
      ComposeBox.tsx.
    - Each QueuedRow instance owns its own chipStripRef + chipStripHeight state +
      useLayoutEffect (JSDOM-guarded ResizeObserver) — no parent Map-of-refs plumbing.
    - Queued Textarea's `<div className="relative flex-1 self-stretch">` inner wrapper
      hosts the chip strip overlay child + the Textarea + inline armed overlay +
      paperclip + Send/mic/arm-idle group; the delete × top-right corner tab remains a
      SIBLING of the inner wrapper (direct child of the outer slot container) so it still
      protrudes OUTSIDE the border.
    - Per-slot chip strip renders when `getStagedAttachmentsForTarget("queued:${slot.id}")`
      returns non-empty; empty state renders no DOM + no paddingTop.
    - Queued Textarea inline `style` prop mirrors primary's dynamic paddingTop pattern.
    - Primary chip strip (Quick A) invariant preserved (QB-9 regression guard).
    - 3 new tests (QB-7, QB-8, QB-9) added; all pre-existing tests still pass; total
      ComposeBox.test.tsx = 48 green.
    - `npx tsc --noEmit` EXIT 0.
    - Committed as commit #2 with the exact subject in action step 6.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: One-line queued-message ARMED overlay header + "click to cancel" + fire-order badge (Commit 3, closes bounty #1)</name>
  <files>
    src/ui/features/pretty-view/ComposeBox.tsx,
    src/ui/features/pretty-view/ComposeBox.test.tsx
  </files>
  <behavior>
    - The queued-message ARMED overlay (the "Queued — waiting for idle" scrim that
      renders when a slot / primary is armed for send-when-idle) currently uses a
      vertical stack: `<button ... className="... flex flex-col items-center justify-center
      gap-1.5">` with a size-5 icon above a text label. See ComposeBox.tsx:1910-1927
      (queued row overlay) and ComposeBox.tsx:2188-2205 (primary overlay).
    - Restructure BOTH overlays to a single inline row:
      - Direction: `flex-row items-center justify-center gap-2` (was `flex-col ... gap-1.5`).
      - Children (in order): icon (RotateCwFadingClock, size-4 to fit inline — was size-5),
        label ("Queued — waiting for idle"), fire-order badge (conditional, see below),
        "click to cancel" copy.
      - The "click to cancel" copy is the literal lowercase phrase `click to cancel`
        (Ashley's exact ask). Rendered as a small subtle span, e.g. `<span
        className="text-xs opacity-70">click to cancel</span>`, appended AFTER the badge
        (if the badge renders) or after the main label (if the badge doesn't). No extra
        adornment — no parentheses, no arrow icon.
      - The `<button>` element and its onClick handler (`cancelSourceArmed(...)`) stay
        exactly as-is — the header ITSELF is the cancel affordance; "click to cancel" is
        the textual cue that the entire scrim is clickable.
    - Fire-order badge:
      - Renders as a compact pill: `<span className="[pill styling]">${index+1}/${total}</span>`.
      - `index` = the position of THIS armed source in the `queue` FIFO array (0-indexed —
        this is the source of fire order per the `queue` state at ComposeBox.tsx:365-380
        and the `fireNextQueued` dispatcher at L815-835 which dispatches `queue[0]`).
      - `total` = `queueSlots.length` (visual queue slots, NOT `queue.length` — Ashley
        asked "the order in which they're going to fire" which reads most naturally as
        "1 of the N queued messages Ashley sees on screen"; queue.length is only the
        armed subset which would show "1/1" for a single armed slot even when N=3 slots
        exist, which is misleading. Confirm interpretation before implementing; if
        ambiguous, prefer `queueSlots.length` per this reading, and note the choice in
        SUMMARY.md under Deviations for Ashley to redirect if wrong.).
      - Correction after re-reading scope: Ashley's ask is specifically "the order in
        which they're going to fire" — this IS the fire order in the `queue` FIFO, and
        the total that makes sense is the total NUMBER of visually-queued messages that
        represent messages the user has drafted. `queueSlots.length` is that count. If a
        slot isn't armed yet (no text or not yet armed), it doesn't have a fire position
        — so the badge SHOULD only render on armed slots (which is what this task
        constrains: the badge sits inside the armed overlay, which only renders when
        `slotArmed === true` OR `primaryArmed === true`).
      - Gate: only render the badge when `queueSlots.length >= 2` (single queued slot
        doesn't need a "1/1" indicator). The primary composebox is NOT a queued slot, so
        the primary's armed overlay renders the badge when `queueSlots.length >= 1` (one
        queued slot + the primary = two things Ashley could be watching). Simpler
        alternative: apply the same `queueSlots.length >= 2` gate to both overlays so the
        badge appears only when there are 2+ queued slots. Ashley's exact wording
        supports either read; go with the SIMPLER unified gate (`queueSlots.length >= 2`
        for both) to avoid a subtle inconsistency between primary and queued overlays.
        Note the choice in SUMMARY.md.
      - For the QUEUED overlay: index = `queue.findIndex((e) => e.source === slot.id)`.
        If findIndex returns -1 (queue entry not present — should never happen while
        `slotArmed` is true, but defense in depth), skip the badge for that instance.
      - For the PRIMARY overlay: index = `queue.findIndex((e) => e.source === "primary")`.
        Same -1 defense.
      - Position string: `${index + 1}/${queueSlots.length}`. Example rendering with 3
        slots where the second slot fires next: `2/3`.
      - Styling: match existing subtle-badge conventions from the codebase. Prefer
        `<span className="px-1.5 py-0.5 rounded-full text-[10px] font-medium
        bg-[rgba(220,225,245,0.10)] text-[hsla(38,60%,80%,0.9)]">${index+1}/${total}</span>`
        — same warm-cream tone the overlay label already uses, just a small pill.
      - Do NOT invent a new visual language; if a `data-testid="fire-order-badge"` is
        useful for testing, add it (recommended — makes QB-11/QB-12 deterministic).
    - New ComposeBox tests:
      QB-10: Queued armed overlay renders as ONE inline row — icon + label + (optional
        badge) + "click to cancel" copy all in the same flex-row parent. Assert:
          The overlay button's className contains "flex-row" (or does NOT contain
          "flex-col" — same information but flex-row is the intent-carrying token).
          The rendered text includes both "Queued — waiting for idle" and "click to
          cancel".
        Setup: mount ComposeBox, add 2 queued slots (so badge renders — QB-10 doesn't
        actually depend on badge presence; the layout test is orthogonal), arm one slot,
        assert the overlay layout.
        Note: arming a slot requires text in the slot + clicking the arm-idle button.
        See existing test QS 4 for the type-then-click pattern. If the arm-idle button
        isn't easily reachable in JSDOM (aria-label /send when idle/i should suffice),
        this test may need a quick setup helper — keep the helper local to the describe.
      QB-11: Fire-order badge is PRESENT when queueSlots.length >= 2 and this slot is
        armed. Assert:
          `screen.queryByTestId("fire-order-badge")` is not null.
          Its rendered text matches `/^\d+\/\d+$/` (e.g. `1/2`).
        Setup: add 2 slots, type in slot A, arm slot A, assert badge with `1/2`.
      QB-12: Fire-order badge is ABSENT when queueSlots.length === 1. Assert:
          Arm the sole slot; `screen.queryByTestId("fire-order-badge")` is null.
      QB-13: "click to cancel" copy is present in the armed overlay (regression guard for
        the literal phrase). Assert: `screen.getByText(/click to cancel/)` is not null
        (case-sensitive matcher — Ashley asked for the exact lowercase phrase).
      QB-14: Clicking the armed overlay STILL calls the cancel handler (Task 3 must NOT
        break the click-to-cancel behavior). Assert: after arming, clicking the overlay
        button removes the armed state (e.g. queueSlots does NOT lose the slot itself,
        but the overlay disappears — the slot is no longer disabled/scrimmed). Simplest
        assertion: after clicking the overlay, the slot Textarea's `disabled` attr is
        false (was true while armed). Alternative: query for `getByRole("button", { name:
        /cancel queued send/i })` before and after — after should be null.
    - Verification (full-suite runs on THIS task since it's the final commit):
      - `npx tsc --noEmit` EXIT 0.
      - `npx vitest run src/ui/features/pretty-view/ComposeBox.test.tsx` → 53 green
        (48 after Task 2 + 5 new QB-10..QB-14).
      - `npx vitest run src/ui/features/pretty-view/use-pretty-view-uploads.test.ts` →
        22 green (same as Task 1).
      - `npx vitest run` (FULL frontend suite) → net-new failures = 0 vs. Quick A's
        baseline of 1087 passed / 6 skipped / 0 failed across 89 files. Expected: >=1087
        passed (new tests add to the count), 0 failed, 6 skipped.
      - Grep-gate on the captured vitest log for `FAIL|✗|Unhandled|failed` on any line
        NOT prefixed by `#` — 0 real failure markers (per patch #209→#211 fleet
        learning). This defense-in-depth check catches async-surface changes that don't
        surface as red exit codes on happy paths.
  </behavior>
  <action>
    **Step 1 — Restructure the QUEUED armed overlay.** In the extracted QueuedRow
    component (from Task 2), find the armed overlay `<button>` block (originally at
    ComposeBox.tsx:1910-1927; after Task 2 it's inside QueuedRow's return). Rewrite:
    ```
    {slotArmed && (
      <button
        type="button"
        onClick={() => cancelSourceArmed(slot.id)}
        aria-label="Cancel queued send"
        title="Cancel queued send"
        className={cn(
          "absolute inset-0 flex flex-row items-center justify-center gap-2 px-3",
          "rounded-[10px] bg-[rgba(10,12,20,0.72)] backdrop-blur-[2px]",
          "cursor-pointer",
        )}
      >
        <RotateCwFadingClock className="size-4 text-[hsla(38,70%,72%,0.9)]" />
        <span className="text-sm text-[hsla(38,60%,80%,0.85)] font-[Inter_Variable,ui-sans-serif,system-ui,sans-serif]">
          Queued — waiting for idle
        </span>
        {queueSlots.length >= 2 && (() => {
          const idx = queue.findIndex((e) => e.source === slot.id);
          if (idx < 0) return null;
          return (
            <span
              data-testid="fire-order-badge"
              className="px-1.5 py-0.5 rounded-full text-[10px] font-medium bg-[rgba(220,225,245,0.10)] text-[hsla(38,60%,80%,0.9)]"
            >
              {idx + 1}/{queueSlots.length}
            </span>
          );
        })()}
        <span className="text-xs text-[hsla(38,60%,80%,0.7)]">click to cancel</span>
      </button>
    )}
    ```
    Note: `queue` and `queueSlots` are ComposeBox function-scope state. They need to be
    passed as props to QueuedRow (extend the QueuedRow prop surface from Task 2 with
    `queue: QueueEntry[]` — the `queueSlots` prop already exists per Task 2's design).
    Add `queue={queue}` in the parent's QueuedRow render.

    **Step 2 — Restructure the PRIMARY armed overlay.** Find the primary armed overlay
    at ComposeBox.tsx:2188-2205. Apply the SAME restructure — flex-row, size-4 icon, same
    label, same badge gate (queueSlots.length >= 2), same "click to cancel" copy. The
    primary's source string is literal "primary":
    ```
    {primaryArmed && (
      <button
        type="button"
        onClick={() => cancelSourceArmed("primary")}
        aria-label="Cancel queued send"
        title="Cancel queued send"
        className={cn(
          "absolute inset-0 flex flex-row items-center justify-center gap-2 px-3",
          "rounded-[10px] bg-[rgba(10,12,20,0.72)] backdrop-blur-[2px]",
          "cursor-pointer",
        )}
      >
        <RotateCwFadingClock className="size-4 text-[hsla(38,70%,72%,0.9)]" />
        <span className="text-sm text-[hsla(38,60%,80%,0.85)] font-[Inter_Variable,ui-sans-serif,system-ui,sans-serif]">
          Queued — waiting for idle
        </span>
        {queueSlots.length >= 2 && (() => {
          const idx = queue.findIndex((e) => e.source === "primary");
          if (idx < 0) return null;
          return (
            <span
              data-testid="fire-order-badge"
              className="px-1.5 py-0.5 rounded-full text-[10px] font-medium bg-[rgba(220,225,245,0.10)] text-[hsla(38,60%,80%,0.9)]"
            >
              {idx + 1}/{queueSlots.length}
            </span>
          );
        })()}
        <span className="text-xs text-[hsla(38,60%,80%,0.7)]">click to cancel</span>
      </button>
    )}
    ```

    **Step 3 — Add tests QB-10..QB-14.** In the `Quick B` describe block (in
    ComposeBox.test.tsx), append the five new tests per behavior. Use existing
    fake-timers + flushMountEffect helpers where needed.

    Helper for the arm-a-slot pattern (define once at the top of the Quick B describe,
    reuse across QB-10..QB-14):
    ```
    async function addAndArmSlot(text: string) {
      fireEvent.click(screen.getByRole("button", { name: /queue a message/i }));
      await act(async () => { await Promise.resolve(); });
      const slotTextareas = screen.getAllByRole("textbox") as HTMLTextAreaElement[];
      const slotTextarea = slotTextareas[slotTextareas.length - 2]; // NEW slot is
      // second-to-last (primary is always last; last slot added is at [length-2]).
      // If tests add multiple slots, adjust indexing per test.
      await act(async () => {
        fireEvent.change(slotTextarea, { target: { value: text } });
      });
      // Click the newest slot's arm-idle button (aria-label /send when idle/i).
      const armButtons = screen.getAllByRole("button", { name: /send when idle/i });
      await act(async () => {
        fireEvent.click(armButtons[armButtons.length - 1]);
      });
    }
    ```
    (Adjust ordering of slot textareas if the queueSlots.map DOM order differs — in the
    existing QS tests, slot renders BEFORE primary, so slots are indices 0..N-1 and
    primary is index N. Verify with a `console.log(screen.getAllByRole("textbox").map(t
    => t.placeholder))` first-time debug if the ordering is unclear.)

    QB-10..QB-14 sketches:
    ```
    it("QB-10: queued armed overlay is one inline row — icon + label + 'click to cancel' in the same flex-row parent", async () => {
      render(<ComposeBox {...baseProps()} />);
      await flushMountEffect();
      await addAndArmSlot("first queued");
      const overlay = screen.getByRole("button", { name: /cancel queued send/i });
      expect(overlay.className).toMatch(/flex-row/);
      expect(overlay.className).not.toMatch(/flex-col/);
      expect(overlay.textContent).toMatch(/Queued.*waiting for idle/);
      expect(overlay.textContent).toMatch(/click to cancel/);
    });

    it("QB-11: fire-order badge PRESENT when queueSlots.length >= 2 and this slot is armed — reads `${idx+1}/${queueSlots.length}`", async () => {
      render(<ComposeBox {...baseProps()} />);
      await flushMountEffect();
      // Add TWO slots first (so length >= 2), then arm the FIRST one.
      fireEvent.click(screen.getByRole("button", { name: /queue a message/i }));
      fireEvent.click(screen.getByRole("button", { name: /queue a message/i }));
      await act(async () => { await Promise.resolve(); });
      const slotTextareas = screen.getAllByRole("textbox") as HTMLTextAreaElement[];
      // slots are indices 0 and 1; primary is index 2.
      await act(async () => {
        fireEvent.change(slotTextareas[0], { target: { value: "first" } });
      });
      const armButtons = screen.getAllByRole("button", { name: /send when idle/i });
      // Arm the FIRST slot (its arm button is armButtons[0]).
      await act(async () => { fireEvent.click(armButtons[0]); });
      const badge = screen.getByTestId("fire-order-badge");
      expect(badge).toBeTruthy();
      expect(badge.textContent).toMatch(/^\d+\/2$/); // e.g. "1/2"
    });

    it("QB-12: fire-order badge ABSENT when queueSlots.length === 1", async () => {
      render(<ComposeBox {...baseProps()} />);
      await flushMountEffect();
      await addAndArmSlot("only slot");
      expect(screen.queryByTestId("fire-order-badge")).toBeNull();
    });

    it("QB-13: 'click to cancel' literal lowercase copy is present in the armed overlay (regression guard for the exact phrase Ashley asked for)", async () => {
      render(<ComposeBox {...baseProps()} />);
      await flushMountEffect();
      await addAndArmSlot("test");
      expect(screen.getByText(/click to cancel/)).toBeTruthy();
    });

    it("QB-14: clicking the armed overlay STILL cancels — click-to-cancel behavior preserved after the header restructure", async () => {
      render(<ComposeBox {...baseProps()} />);
      await flushMountEffect();
      await addAndArmSlot("cancel me");
      const overlayBefore = screen.getByRole("button", { name: /cancel queued send/i });
      expect(overlayBefore).toBeTruthy();
      await act(async () => { fireEvent.click(overlayBefore); });
      // After cancel, the overlay is gone (slot text is intact, slot is re-editable).
      expect(screen.queryByRole("button", { name: /cancel queued send/i })).toBeNull();
    });
    ```

    **Step 4 — Full-suite verification (this task is the last commit).**
    - `npx tsc --noEmit` EXIT 0.
    - `npx vitest run src/ui/features/pretty-view/ComposeBox.test.tsx` → 53 green.
    - `npx vitest run src/ui/features/pretty-view/use-pretty-view-uploads.test.ts` →
      22 green (Task 1 baseline; unchanged in Task 3).
    - `npx vitest run` (FULL suite) 2>&1 | tee /tmp/05i-vitest-full.log. Expect: >=1087
      passed / 6 skipped / 0 failed. Then defense-in-depth grep-gate:
      `grep -nE 'FAIL|✗|Unhandled|failed' /tmp/05i-vitest-full.log | grep -v '^[0-9]*:# ' | grep -v 'summary line prefix from vitest' | wc -l` → 0. If any real failure markers surface (async-surface regressions, unhandled promise rejections), STOP — do NOT commit; fix first.
    - If any pre-existing test fails as a REGRESSION from the QueuedRow extraction or
      overlay restructure, fix the source (not the test). Prefer preserving existing test
      semantics; only update a test if the plan explicitly changed the DOM contract it
      asserted (e.g. QS 3's "delete queued message" button count — if the top-right tab
      changes the count, adjust QS 3's expected count with a comment referencing this
      Quick B).

    **Step 5 — Commit.** Message:
    `feat(composebox): one-line queued header + click-to-cancel + fire-order badge (Quick B of paired ship #2/#1, closes bounty adjust-visual-on-queued-messages)`
    Body: reference `adjust-visual-on-queued-messages` and note "Closes bounty
    adjust-visual-on-queued-messages. Paired with closing commit for
    attached-files-as-chips-in-textarea-per-message in commit 2 of this Quick."

    **Step 6 — Write SUMMARY.md** at
    `.planning/quick/260803-05i-queued-rows-per-slot-paperclip-attach-de/260803-05i-SUMMARY.md`
    following the shape of Quick A's SUMMARY (frontmatter with commits + files_modified +
    test_results, then One-liner, then per-task details, then Deviations, then Known
    stubs / follow-ups including the queued-textarea paste follow-up note, then Threat
    flags = None, then Self-Check).

    **Step 7 — STOP.** Do NOT push. Do NOT docker build. Do NOT compose up. The three
    commits join the deploy queue at #284 + #285 + #286 (or wherever the queue tail is at
    the time of Task 3 completion) and wait for Ashley's ship word.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run src/ui/features/pretty-view/ComposeBox.test.tsx &amp;&amp; npx vitest run src/ui/features/pretty-view/use-pretty-view-uploads.test.ts &amp;&amp; npx vitest run 2>&amp;1 | tee /tmp/05i-vitest-full.log &amp;&amp; test $(grep -cE 'FAIL|✗|Unhandled|failed' /tmp/05i-vitest-full.log | grep -v '^[0-9]*:# ' | grep -v ' skipped' | grep -v ' passed' | tail -c 4) = "0"</automated>
  </verify>
  <done>
    - QUEUED armed overlay: flex-row layout, size-4 icon, label + fire-order badge (when
      queueSlots.length >= 2 and the slot is present in `queue`) + literal lowercase
      "click to cancel" copy, all inline.
    - PRIMARY armed overlay: same restructure applied — visual consistency across both
      overlays.
    - Fire-order badge: `${index+1}/${queueSlots.length}` where index = position in
      `queue` FIFO. Gated on `queueSlots.length >= 2` for both overlays (unified gate).
    - Click-to-cancel behavior UNCHANGED — the overlay button's onClick still calls
      cancelSourceArmed with the correct source string.
    - 5 new tests (QB-10..QB-14): overlay layout, badge presence, badge absence, literal
      copy, click-to-cancel preservation.
    - ComposeBox.test.tsx = 53 green (48 from Task 2 + 5 from Task 3).
    - Full frontend suite: >=1087 passed / 6 skipped / 0 failed. Defense-in-depth grep
      gate on captured log = 0 real failure markers.
    - `npx tsc --noEmit` EXIT 0.
    - Committed as commit #3 with the exact subject in action step 5.
    - SUMMARY.md written at the correct path with all required sections.
    - STOPPED. No push, no docker build, no compose up.
  </done>
</task>

</tasks>

<verification>
After all three commits are in place:

1. `git log --oneline -3` shows the three new commits at HEAD, most recent first:
   - `feat(composebox): one-line queued header + click-to-cancel + fire-order badge (Quick B of paired ship #2/#1, closes bounty adjust-visual-on-queued-messages)`
   - `feat(composebox): overlay chip strip on queued textareas via QueuedRow extraction (per-target staging live) (Quick B of paired ship #2/#1)`
   - `feat(composebox): per-queued attach button + top-right delete corner tab + padding parity + clearStagedForTarget hook API (Quick B of paired ship #2/#1)`
2. `npx tsc --noEmit` EXIT 0.
3. `npx vitest run` EXIT 0 with test counts: >=1087 passed / 6 skipped / 0 failed
   across 89+ test files.
4. Defense-in-depth: `grep -cE 'FAIL|✗|Unhandled' /tmp/05i-vitest-full.log` filtered to
   exclude summary/passed/skipped lines → 0 real failure markers.
5. `git status` clean — no untracked files, no unstaged changes.
6. `git diff HEAD~3..HEAD --name-only` contains ONLY the 5 files in this plan's
   `files_modified` frontmatter. If any other file surfaces (especially anything under
   `src/backend/`, `docker/`, `prisma/`), HALT and surface — scope violation.
7. SUMMARY.md exists at
   `.planning/quick/260803-05i-queued-rows-per-slot-paperclip-attach-de/260803-05i-SUMMARY.md`
   with commits (hash + subject), files_modified, test_results, deviations, follow-ups.
</verification>

<success_criteria>
- 3 atomic commits landed on `feat/tab-title-from-tmux`.
- Bounty `attached-files-as-chips-in-textarea-per-message` (#2) CLOSED — queued rows now
  stage attachments per-slot, chip strip overlays inside each slot's textarea wrapper,
  padding parity + delete-tab relocation give queued rows the coherent visual language
  Ashley locked in the design conversation.
- Bounty `adjust-visual-on-queued-messages` (#1) CLOSED — queued-message armed overlay
  restructured to one inline row with icon + label + fire-order badge + explicit "click
  to cancel" copy.
- `usePrettyViewUploads` gains `clearStagedForTarget(target)` public API; per-target
  isolation on stage / read / clear proven by hook tests.
- QueuedRow extraction inside ComposeBox.tsx cleanly scopes chipStripRef +
  ResizeObserver per-instance.
- No backend edits. No push. No deploy.
- Executor STOPPED after commit 3 — deploy queue join is orchestrator's decision.
</success_criteria>

<output>
Create
`.planning/quick/260803-05i-queued-rows-per-slot-paperclip-attach-de/260803-05i-SUMMARY.md`
when done, summarizing:
- Three commits landed (SHAs + subjects).
- Files touched (5 files).
- Test result summary (hook tests, ComposeBox tests, full suite).
- Any deviations from plan (badge total interpretation, styling tweaks, test-scaffold
  auto-fixes, queued textarea paste follow-up note).
- Explicit note: "Closes bounty attached-files-as-chips-in-textarea-per-message (#2) via
  commit 2; closes bounty adjust-visual-on-queued-messages (#1) via commit 3. Deploy
  queue extends #267→#283 → +#284+#285+#286; HELD until Ashley's ship word."
- Known stubs / follow-ups:
  - Queued-textarea onPaste handler not wired to per-slot target (routes to primary via
    top-level dropzone today; trivial follow-up if Ashley wants per-slot paste).
  - Queued-slot send with attachments does NOT currently trigger the primary chunk-pump
    `startBatch` flow (queued sends today are plain text sends via handleQueueSlotSend);
    if Ashley wants queued sends to inject an attachments turn like the primary
    onSendWithAttachments path, that's a separate wiring task (would need
    per-slot startBatch semantics or a shared batch across slots — non-trivial, out of
    Quick B's scope). Note as a genuine follow-up NOT a stub.
- Threat flags: none. Client-side surgery + hook API extension; no new network endpoints,
  no auth paths, no schema changes.
</output>
