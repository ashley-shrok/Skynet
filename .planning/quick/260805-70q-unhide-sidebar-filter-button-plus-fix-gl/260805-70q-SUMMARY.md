---
phase: quick-260805-70q
plan: "01"
subsystem: ["ui/pretty-conversations", "backend/global-files"]
tags: ["filter-button", "tilde-expansion", "ssh-exec", "test-unskip"]
key-files:
  modified:
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
    - src/backend/database/routes/global-files-read-write.ts
  created:
    - src/backend/database/routes/global-files-read-write.test.ts
decisions:
  - "Inserted tilde-expansion block in READ handler between SSH connect and cat/stat (renumbered step 5→6), mirroring WRITE handler L400-417 exactly"
  - "Changed shellEscape(path) → shellEscape(absPath) so resolved path reaches cat/stat"
  - "Filter-button JSX stripped of {false && (...)} wrapper; no other JSX touched"
  - "describe.skip flipped to describe; stale Patch #317 comment replaced with attribution"
metrics:
  duration: "~8 minutes"
  completed: "2026-08-05"
  tasks_completed: 3
  files_modified: 3
  files_created: 1
---

# Phase quick-260805-70q Plan 01 Summary

**One-liner:** Un-hide sidebar pinned-bounty filter button (reverses patch #317 gate) and fix `~/` path tilde-expansion in POST /global-files/read SSH handler, with 4 new backend tests and 7 previously-skipped frontend tests now green.

## Per-Task Commits

| Task | Commit | Subject |
|------|--------|---------|
| Task 1: Fix A — filter button + test un-skip | `5f5f10c` | feat(quick-260805-70q-01): Fix A — un-hide sidebar filter button, un-skip 7 filter tests |
| Task 2: Fix B — tilde-expansion + backend tests | `5e468c8` | feat(quick-260805-70q-02): Fix B — mirror tilde-expansion into READ handler + add backend tests |
| Task 3: Full-suite regression gate | (verify-only, no new commit) | — |

## Test Totals (`npx vitest run` — Task 3 full-suite run)

| Metric | Count |
|--------|-------|
| Test files | 117 passed |
| Tests passed | 1414 |
| Tests skipped | 6 (pre-existing, unrelated to this task) |
| Tests failed | 0 |

The 7 previously-skipped filter tests (Tests 23–29 in `PrettyConversationsPanel.test.tsx`) now run and pass. Total for that file went from 43 → 50.

## `npm run build:backend` Exit Code

Exit code: **0** (clean build, no TypeScript errors)

## Assertion Drift

None. The 7 un-skipped filter tests (Tests 23–29) passed as authored — no assertion drift detected. The button JSX shape (`data-testid`, `data-active`, `aria-pressed`, `className`, click handler) was unchanged by the intervening patch #317 gate; only the `{false && (...)}` wrapper was removed.

## Changes Made

### Fix A — Un-hide sidebar filter button

**`PrettyConversationsPanel.tsx`** (~L761–778):
- Removed `{false && (` opening and matching `)}` closing from the `.pv-filter` button block.
- Replaced the Patch #317 hide-comment with a one-line attribution: `// quick-260805-70q: filter button un-hidden per Ashley 2026-08-05 (reverses patch #317 gate).`
- No other JSX touched. State (`filterPinnedOnly`), handler (`setFilterPinnedOnly`), and all downstream filter logic (`displayedPinned`, `bountyCounts`, etc.) were already live and unmodified.

**`PrettyConversationsPanel.test.tsx`** (~L1734–1739):
- Changed `describe.skip` → `describe` for the "PrettyConversationsPanel: pinned-bounty filter (Patch #167)" suite.
- Replaced the Patch #317 skip-comment block (L1734–1738) with a single attribution comment.

### Fix B — Tilde-expansion in POST /global-files/read

**`global-files-read-write.ts`** (READ handler):
- Inserted a tilde-expansion block between SSH connect (step 4) and cat/stat (now step 6, renumbered from 5).
- Block declares `let absPath = path;` and branches on `path.startsWith("~/")`.
- Calls `execWithTimeout(conn, "echo $HOME")`, returns 502 `{ error: "could not resolve remote HOME" }` when HOME is empty or starts with `~`, logs `operation: global_files_read_home`.
- Changed `shellEscape(path)` → `shellEscape(absPath)` so cat/stat receive the resolved path.
- WRITE handler untouched. `GlobalFileTab.tsx` and `GlobalFilesModal.tsx` untouched (deferred per plan).

**`global-files-read-write.test.ts`** (new file):
- 4 tests covering: tilde path expansion (B1), absolute path pass-through (B2), unresolvable HOME empty string (B3), unresolvable HOME literal `~` (B4).
- Mocking style mirrors `roles-list-for-host.test.ts` (same auth-manager, SSH-primitives, and host-resolver mock pattern).

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None introduced by this task.

## Threat Flags

None — no new network endpoints, auth paths, or schema changes introduced.

---

deploy NOT run — awaiting Ashley greenlight
