---
phase: quick-260805-70q
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
  - src/backend/database/routes/global-files-read-write.ts
  - src/backend/database/routes/global-files-read-write.test.ts
autonomous: true
requirements: [QUICK-260805-70q]
must_haves:
  truths:
    - "Sidebar filter button (data-testid=pv-filter-pinned-bounties) renders unconditionally when the panel mounts"
    - "The pinned-bounty filter describe block in PrettyConversationsPanel.test.tsx runs (not skipped) and every it() inside passes"
    - "POST /global-files/read expands ~/ paths by resolving remote $HOME before running cat/stat, mirroring the write handler at L400-417"
    - "Reading a whitelisted ~/.claude/CLAUDE.md returns the actual file contents from the resolved absolute path — not an empty string"
    - "Absolute paths are passed through unchanged (no echo $HOME probe fired)"
    - "Unresolvable remote HOME on a ~/ path returns 502 { error: \"could not resolve remote HOME\" }"
    - "npm run build:backend exits 0"
    - "npx vitest run exits 0 with zero failures"
  artifacts:
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "Un-gated .pv-filter button JSX (no more {false && ...} wrapper)"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx"
      provides: "Un-skipped filter describe block (was describe.skip at L1739)"
    - path: "src/backend/database/routes/global-files-read-write.ts"
      provides: "READ handler with tilde-expansion mirroring the WRITE handler (L400-417)"
    - path: "src/backend/database/routes/global-files-read-write.test.ts"
      provides: "New backend test file covering tilde-expansion + absolute-path + unresolvable-HOME cases for the READ handler"
  key_links:
    - from: "PrettyConversationsPanel.tsx (~L765)"
      to: ".pv-filter button JSX (~L766-778)"
      via: "removal of `{false && (` wrapper and trailing `)}`"
      pattern: "data-testid=\"pv-filter-pinned-bounties\""
    - from: "global-files-read-write.ts POST /read handler"
      to: "cat/stat commands using shellEscape(absPath)"
      via: "tilde-expansion block (echo $HOME) inserted between step 4 (SSH connect) and step 5 (cat/stat)"
      pattern: "if \\(path.startsWith\\(\"~/\"\\)\\)"
    - from: "global-files-read-write.test.ts"
      to: "connectOneShot + execCommand + resolveHostById + loadGlobalFilesConfig/getFilesForHost mocks"
      via: "vi.mock() top-of-file setup mirroring roles-list-for-host.test.ts / identity-clone.test.ts patterns"
      pattern: "vi.mock\\(\"\\.\\./\\.\\./ssh/(ssh-one-shot|tmux-helper|host-resolver)\\.js\""
---

<objective>
Two independent bug fixes plus their tests, bundled into one quick task.

Fix A: Un-hide the sidebar pinned-bounty filter button (patch #317 temporarily
gated it behind `{false && (...)}`) and un-skip the 7 filter tests it disabled
(`describe.skip` at PrettyConversationsPanel.test.tsx L1739).

Fix B: The Phase 23 `POST /global-files/read` handler shell-escapes the raw
whitelisted path with single quotes then hands it to `cat`/`stat` over SSH.
Single-quotes suppress tilde expansion, so `~/.claude/CLAUDE.md` gets literally
looked up (fails, returns empty). The WRITE handler in the same file already
has the correct tilde-expansion block at L400-417 — mirror it into READ, then
add backend tests for the three tilde-expansion branches.

Purpose:
- Restore Ashley's filter-by-pinned-bounties affordance in the sidebar.
- Fix the "No content in this file yet" empty-state UX dead-end for legit
  files that live under a `~/` path — she cannot get any global file to load
  right now.
- Match the fleet rule: "never leave tests failing" — un-skipping the filter
  suite must not introduce red.

Output:
- PrettyConversationsPanel.tsx: `.pv-filter` renders unconditionally
- PrettyConversationsPanel.test.tsx: filter describe block runs
- global-files-read-write.ts: READ handler resolves `~/` before cat/stat
- global-files-read-write.test.ts: new file with 3+ tilde-expansion tests
- Green `npm run build:backend`
- Green `npx vitest run`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

# Fix A source + tests
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx

# Fix B source (both handlers live here — mirror WRITE L400-417 into READ)
@src/backend/database/routes/global-files-read-write.ts

# Mocking-style analogues for the new backend test file
@src/backend/database/routes/roles-list-for-host.test.ts
@src/backend/database/routes/identity-clone.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix A — un-hide sidebar filter button and un-skip its test suite</name>
  <files>
    src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx,
    src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
  </files>
  <behavior>
    - After edit: PrettyConversationsPanel renders `<button data-testid="pv-filter-pinned-bounties">` unconditionally (no `false &&` gate wrapping it)
    - After edit: `describe(...)` for "PrettyConversationsPanel: pinned-bounty filter (Patch #167)" is NOT skipped and every `it()` inside (Tests 23-29 — 7 tests in total, task description said "6" but the actual count in the file is 7 in the single describe.skip block) runs and passes
    - Filter default state remains `data-active="false"` + `aria-pressed="false"` (Test 23 still passes as authored)
    - Clicking the button flips both attributes (Test 24 still passes as authored)
    - No other JSX change to PrettyConversationsPanel — only the `{false &&` wrapper and its matching trailing `)}` are removed
  </behavior>
  <action>
    Fix A step 1 — Un-hide the filter button in PrettyConversationsPanel.tsx:

    Locate the `.pv-filter` block currently gated by `{false && (` (in the current file this sits inside `.pv-header-actions`, roughly L761-778, immediately above the `showPencilButton && <button ref={menuButtonRef} ...>` MoreVertical trigger). The exact pattern is:

    ```
    {false && (
    <button
      type="button"
      onClick={() => setFilterPinnedOnly((v) => !v)}
      ...
      data-testid="pv-filter-pinned-bounties"
    >
      <Filter />
    </button>
    )}
    ```

    Remove the `{false && (` opening and the matching `)}` closing so the `<button ...>...</button>` renders unconditionally. Do NOT reflow, re-indent, or touch anything else in the JSX. Also update or remove the "Patch #317: ... HIDDEN TEMPORARILY per Ashley 2026-08-04 ..." comment immediately above the block — the button is no longer hidden. A one-line note like `// quick-260805-70q: filter button un-hidden per Ashley 2026-08-05 (reverses patch #317 gate).` is fine; comment cleanup is required so the source doesn't lie about current behavior.

    Verify Filter and setFilterPinnedOnly are still imported/declared (they should be — patch #317 explicitly left state + handler intact). Do NOT re-add or touch any other filter logic (`displayedPinned`, `displayedGrouped`, `hasPinnedForRow`, `bountyCounts` subscription) — all of it is still live from patch #167 and only the button JSX was ever gated.

    Fix A step 2 — Un-skip the filter test block in PrettyConversationsPanel.test.tsx:

    At L1739 the file has `describe.skip("PrettyConversationsPanel: pinned-bounty filter (Patch #167)", () => {`. Change `describe.skip` → `describe`. Also update/remove the "Patch #317 (2026-08-04): filter button intentionally hidden ..." block-comment immediately above that describe (L1734-1738) — the button is no longer hidden. A short replacement note like `// quick-260805-70q: un-skipped after Fix A un-hid the filter button.` is fine.

    Fix A step 3 — Verify no assertion drift:

    Run `npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` and inspect all 7 un-skipped tests (Tests 23-29). They were authored against the pre-#317 button JSX and the surrounding chrome has not changed structurally in the ~1 day of intervening commits (the button still lives inside `.pv-header-actions`, still uses `data-testid="pv-filter-pinned-bounties"`, still uses `data-active` + `aria-pressed`, still uses the same click handler and the same downstream store subscriptions). Expect all 7 to pass as-is.

    IF a test fails: fix the assertion to match the CURRENT UI. Do NOT weaken the assertion just to make it green (fleet rule per Ashley 2026-08-05). Prefer: identify the drift, patch the test to assert the current-correct behavior. If the assertion is inherently correct and the UI has genuinely regressed, fix the UI instead. If neither and you're unsure, stop and surface it — do not silently loosen.

    Also grep the WHOLE `src/ui/features/pretty-conversations/` tree for `describe.skip` / `it.skip` to belt-and-suspenders confirm nothing else filter-related was skipped (already verified during planning: only one match). If any other filter-related skip appears (task description said "6", could indicate a stale draft in another test file), un-skip it too and apply the same drift-check.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && grep -n "describe\.skip\|it\.skip" src/ui/features/pretty-conversations/*.test.tsx; test -z "$(grep -n 'describe\.skip\|it\.skip' src/ui/features/pretty-conversations/*.test.tsx)" &amp;&amp; grep -n "false &&" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx | (! grep -q "pv-filter\|Filter") &amp;&amp; npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx</automated>
  </verify>
  <done>
    - `grep -n 'describe\.skip\|it\.skip' src/ui/features/pretty-conversations/*.test.tsx` returns empty
    - `grep -n 'false &&' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns no matches near `.pv-filter` / `Filter` / `pv-filter-pinned-bounties`
    - `npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` exits 0 with every previously-skipped filter test now passing
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix B — mirror tilde-expansion into READ handler + add backend tests</name>
  <files>
    src/backend/database/routes/global-files-read-write.ts,
    src/backend/database/routes/global-files-read-write.test.ts
  </files>
  <behavior>
    Behavior of the FIXED READ handler:

    - Test B1 (tilde path): POST /global-files/read with `{hostId: 1, path: "~/.claude/CLAUDE.md"}`
      → execCommand IS called with `echo $HOME` first
      → subsequent cat/stat commands use the RESOLVED absolute path (`/home/testuser/.claude/CLAUDE.md`) — assert the exact command strings contain `'/home/testuser/.claude/CLAUDE.md'` (single-quoted) and NOT the literal `~`
      → 200 response with content, mtime, size derived from mocked exec outputs

    - Test B2 (absolute path): POST /global-files/read with `{hostId: 1, path: "/etc/foo/bar"}`
      → execCommand is NOT called with `echo $HOME`
      → cat/stat commands use the original `/etc/foo/bar` (single-quoted) — assert the commands contain `'/etc/foo/bar'` and no `$HOME` probe fired
      → 200 response with content, mtime, size

    - Test B3 (unresolvable HOME on tilde path): POST /global-files/read with `{hostId: 1, path: "~/foo"}`, and `execCommand("echo $HOME")` mocked to return `""` (empty string)
      → 502 response with body `{error: "could not resolve remote HOME"}`
      → cat/stat NOT called
      → conn.end() still called (cleanup)

    - Test B4 (unresolvable HOME variant): same as B3 but `echo $HOME` returns `~` (literal tilde — the WRITE handler's guard treats this as unresolvable too)
      → 502 with same error body
  </behavior>
  <action>
    Fix B step 1 — Patch READ handler in `src/backend/database/routes/global-files-read-write.ts`.

    Locate the `POST /read` handler (starts around L119 with `router.post("/read", ...)`). Inside its `try { ... }` block, between "4. SSH connect" (ends at ~L192 where `conn = await connectOneShot(...)` succeeds) and "5. Read the file via exec channel" (starts at ~L194 with the comment block ending at L202, and the actual read logic beginning at ~L203 with `const escapedPath = shellEscape(path);`), insert a tilde-expansion block that MIRRORS the WRITE handler at L400-417.

    The WRITE handler's block for reference (verbatim from L400-417):
    ```
    let absPath = filePath;
    if (filePath.startsWith("~/")) {
      const remoteHome = (
        await execWithTimeout(conn, "echo $HOME")
      ).trim();
      if (!remoteHome || remoteHome.startsWith("~")) {
        // echo $HOME failed to resolve — bail rather than write to a bogus path.
        sshLogger.warn("global-files-write: could not resolve remote HOME", {
          operation: "global_files_write_home",
          hostId,
          remoteHome,
        });
        res.status(502).json({ error: "could not resolve remote HOME" });
        return;
      }
      absPath = `${remoteHome}/${filePath.slice(2)}`;
    }
    ```

    The READ variant must:
    - Use the READ handler's local variable name for the request path (currently `path`, defined at L151) — declare `let absPath = path;` and switch on `path.startsWith("~/")`.
    - Use log operation name `global_files_read_home` (NOT `global_files_write_home`), matching the module's existing naming (already uses `global_files_read_connect`, `global_files_read_error`).
    - Use the sshLogger warn message prefix `"global-files-read: could not resolve remote HOME"` (mirror the module's other READ log messages).
    - Keep the same 502 shape: `res.status(502).json({ error: "could not resolve remote HOME" })`.

    Then in "step 5", change the escape line from `const escapedPath = shellEscape(path);` to `const escapedPath = shellEscape(absPath);` so cat/stat operate on the resolved path. Do NOT keep escaping `path` — the whole point of the fix is that cat/stat must use the tilde-expanded absPath. Leave the response body shape unchanged (still returns `{content, mtime, size}`).

    Do NOT touch the WRITE handler. Do NOT touch `GlobalFileTab.tsx` (deferred per description). Do NOT touch `GlobalFilesModal.tsx` (deferred).

    Fix B step 2 — Create `src/backend/database/routes/global-files-read-write.test.ts` (new file). Mirror the mock-setup style of `src/backend/database/routes/roles-list-for-host.test.ts` (closest analogue: same connectOneShot + execCommand + resolveHostById triple, same auth-manager mock pattern). Do NOT invent a different mocking pattern.

    Required test setup (top of file):
    - `vi.mock("../../utils/auth-manager.js", ...)` — same pattern as roles-list-for-host.test.ts (settable `mockUserId`, 401 when null)
    - `vi.mock("../../ssh/ssh-one-shot.js", () => ({ connectOneShot: vi.fn() }))`
    - `vi.mock("../../ssh/tmux-helper.js", () => ({ execCommand: vi.fn() }))`
    - `vi.mock("../../ssh/host-resolver.js", () => ({ resolveHostById: vi.fn() }))`
    - `vi.mock("./global-files-config-loader.js", () => ({ loadGlobalFilesConfig: vi.fn(), getFilesForHost: vi.fn() }))` — the handler calls both to enforce the whitelist BEFORE opening SSH; tests must stub `getFilesForHost` to return `[{path: "~/.claude/CLAUDE.md"}, {path: "/etc/foo/bar"}, {path: "~/foo"}]` (or a superset) so the whitelist check passes.
    - `vi.mock("../../claude-session/identity-artifact-reader.js", () => ({ writeMarkdownFileAtomic: vi.fn() }))` — not used by READ, but the import in the source file will resolve during test-time require; mocking it prevents any transitive SFTP setup from firing.

    Then import the router with `const router = (await import("./global-files-read-write.js")).default;` (dynamic import AFTER vi.mock declarations, mirroring the analogue tests). Mount it on an express app under `/global-files` and start an http server bound to port 0 for supertest-style HTTP roundtrip.

    Tests to write (mirror the <behavior> block above):

    - Test B1: tilde path expands. Mock `resolveHostById` → `{id: 1, name: "hostA"}`. Mock `execCommand` to return `/home/testuser\n` for `echo $HOME` and reasonable stubs (`hello\n`, `1700000000\n`, `5\n`) for cat/stat. POST `{hostId: 1, path: "~/.claude/CLAUDE.md"}`. Assert response 200 with `content: "hello\n"`, `mtime: 1700000000`, `size: 5`. Assert `execCommand` mock was called at least once with the second-arg equal to `echo $HOME`. Assert the subsequent cat/stat calls include `'/home/testuser/.claude/CLAUDE.md'` (single-quoted) in the second arg and do NOT include a literal `~`.

    - Test B2: absolute path passes through unchanged. Mock resolveHostById same as B1. Mock execCommand to return content/mtime/size stubs only (NO `echo $HOME` case handled — this test asserts it's not called). POST `{hostId: 1, path: "/etc/foo/bar"}`. Assert response 200. Assert `execCommand` was NEVER called with `echo $HOME`. Assert cat/stat commands include `'/etc/foo/bar'` and no `$HOME`.

    - Test B3: unresolvable HOME (empty). Mock `execCommand` to return `""` (empty) for `echo $HOME`. POST `{hostId: 1, path: "~/foo"}`. Assert response 502 with body `{error: "could not resolve remote HOME"}`. Assert cat/stat NOT called (execCommand mock's calls array contains only the `echo $HOME` entry, not the content/mtime/size commands). Assert conn.end() was called on the stub connection (best-effort cleanup — check via a `vi.fn()` on the stub returned by connectOneShot).

    - Test B4 (optional but low-cost): unresolvable HOME (literal `~`). Same as B3 but `echo $HOME` returns `~\n`. Same 502 assertion. This gates the `remoteHome.startsWith("~")` half of the guard so it doesn't silently regress.

    Use `execCommand` mock's `mockImplementation` (not `mockResolvedValueOnce` chains) — the source calls execCommand via `execWithTimeout` which awaits a `Promise.race`; the mockImplementation lets you branch on the command string (e.g., `if (cmd === "echo $HOME") return "/home/testuser\n"`). This matches identity-clone.test.ts style (see L327 `.mockImplementation(async (_conn, cmd) => ...)`).

    Grep `src/backend/database/routes/roles-list-for-host.test.ts` and `identity-clone.test.ts` for exact vi.mock ordering, the "import after mock" idiom, the `Mock` type import from vitest, and the stub-connection shape (should include an `.end()` mock so cleanup does not throw).

    Do NOT bump the patches file. Do NOT push. Commit + tests only.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; grep -n "path.startsWith(\"~/\")" src/backend/database/routes/global-files-read-write.ts | grep -c "global_files_read_home\|absPath\|startsWith" &amp;&amp; grep -n "shellEscape(absPath)" src/backend/database/routes/global-files-read-write.ts &amp;&amp; npx vitest run src/backend/database/routes/global-files-read-write.test.ts &amp;&amp; npm run build:backend</automated>
  </verify>
  <done>
    - `src/backend/database/routes/global-files-read-write.ts` READ handler contains a `path.startsWith("~/")` tilde-expansion block that calls `execWithTimeout(conn, "echo $HOME")`, logs `global_files_read_home` on failure, and returns 502 `{error: "could not resolve remote HOME"}` when HOME resolves to `""` or begins with `~`
    - cat/stat commands in the READ handler use `shellEscape(absPath)`, not `shellEscape(path)`
    - `src/backend/database/routes/global-files-read-write.test.ts` exists with tests B1, B2, B3 (and ideally B4) all passing
    - `npx vitest run src/backend/database/routes/global-files-read-write.test.ts` exits 0
    - `npm run build:backend` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression gate (fleet rule: never leave tests failing)</name>
  <files>
    (no files modified — verification-only)
  </files>
  <action>
    Run the entire vitest suite AND the backend build one last time to catch any cross-test regression the two edits above might have introduced (e.g., a shared snapshot the un-skipped filter tests touch, or a backend module the new test file loads that mutates global state).

    If ANY red appears: fix it in the same commit stream before declaring done. Fleet rule per Ashley 2026-08-05: "never leave tests failing, regardless of where they came from."

    Explicitly out of scope for this task:
    - `git push` — do not push
    - `docker build` / `docker compose up` — Ashley grants deploy separately
    - `~/.claude/roles/box-maintainer/skynet-patches.md` — orchestrator (Tina) handles patches-backfill; do NOT bump patch numbers
    - `GlobalFileTab.tsx` empty-branch fallthrough — deferred
    - `GlobalFilesModal.tsx` picker auto-select — deferred
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npm run build:backend &amp;&amp; npx vitest run</automated>
  </verify>
  <done>
    - `npm run build:backend` exits 0
    - `npx vitest run` exits 0 with zero failures across the whole suite
  </done>
</task>

</tasks>

<verification>
- Fix A gate: the `.pv-filter` button JSX in PrettyConversationsPanel.tsx has no `{false && (...)}` wrapper. The `describe.skip` at PrettyConversationsPanel.test.tsx L1739 is now `describe`. The 7 tests inside all pass.
- Fix B gate: POST /global-files/read handler contains a tilde-expansion block mirroring WRITE L400-417, uses `shellEscape(absPath)` in cat/stat, and the new test file covers tilde / absolute / unresolvable-HOME branches.
- Whole-suite gate: `npm run build:backend` and `npx vitest run` both exit 0.
- No touches to `GlobalFileTab.tsx`, `GlobalFilesModal.tsx`, or `skynet-patches.md`.
</verification>

<success_criteria>
- Ashley can see the pinned-bounty filter button in the sidebar again
- Ashley can open a whitelisted `~/.claude/CLAUDE.md` on any managed host and see the actual file contents (not the "No content in this file yet" empty state)
- All previously-skipped filter tests are running and green
- New backend tests cover the tilde-expansion, absolute-path, and unresolvable-HOME branches of the READ handler
- `npm run build:backend` and `npx vitest run` both exit 0
- No deferred items touched (GlobalFileTab.tsx empty-state, GlobalFilesModal.tsx picker, patches file bump, deploy)
</success_criteria>

<output>
Write summary to `.planning/quick/260805-70q-unhide-sidebar-filter-button-plus-fix-gl/260805-70q-SUMMARY.md`
</output>
