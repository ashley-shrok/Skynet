---
phase: 260730-mj2-sticky-scroll-strip
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/use-auto-scroll.ts
  - src/ui/features/pretty-view/use-auto-scroll.test.ts
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-conversations/PrettyConversationRow.tsx
autonomous: true
requirements:
  - 260730-mj2-01
  - 260730-mj2-02
must_haves:
  truths:
    - "Fresh page load with NO `?scroll=stick` query or hash still lands at the bottom of a conversation and follows new incoming messages — the sticky-bottom behavior is now the sole default."
    - "There is exactly one exported `useAutoScroll` hook in `use-auto-scroll.ts`, and it is the sticky-bottom implementation (formerly `useStickyBottomScroll`)."
    - "`useAutoScroll(paneKey)` resets to bottom on paneKey change via the rAF-chain jump for LOAD_LOCK_MS after a conversation swap."
    - "Only real scroll-up gestures (wheel deltaY<0, PageUp/ArrowUp/Home keys, touchmove that reduces scrollTop by >2px) exit sticky mode; passive touchmove that does not move scrollTop up leaves sticky mode intact."
    - "`forceStickAndJump()` re-enters sticky mode + jumps to bottom + arms the load-lock, exactly as it does today when called from PrettyView's `handleComposeSend`."
    - "The scroll container gets `overflow-anchor: none` applied inline while the hook is mounted, and the prior value is restored on unmount."
    - "No source file under `src/` references `SCROLL_VARIANT`, `useDefaultAutoScroll`, `readScrollVariant`, `?scroll=stick`, or `#scroll=stick` anymore (dead-code / dead-comment sweep is complete)."
    - "The rewritten `use-auto-scroll.test.ts` passes under vitest and covers paneKey reset, gesture-only exit from sticky, forceStickAndJump, and overflow-anchor style application."
    - "PrettyView.tsx and PrettyConversationRow.tsx still compile and their existing test suites still pass — no consumer-visible API break to `useAutoScroll`."
  artifacts:
    - path: "src/ui/features/pretty-view/use-auto-scroll.ts"
      provides: "Single sticky-bottom useAutoScroll hook; no gate, no dispatcher, no default variant"
      contains: "export function useAutoScroll"
    - path: "src/ui/features/pretty-view/use-auto-scroll.test.ts"
      provides: "Test coverage for sticky-bottom semantics (rewritten from follow-bottom-when-near-bottom coverage)"
      contains: "sticky"
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "Consumer of useAutoScroll — no behavior change, comments updated to remove ?scroll=stick references"
      contains: "useAutoScroll(paneKey)"
    - path: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx"
      provides: "Sibling variant that referenced SCROLL_VARIANT pattern in a comment — comment updated to self-reference CONV_MENU_VARIANT"
      contains: "CONV_MENU_VARIANT"
  key_links:
    - from: "src/ui/features/pretty-view/use-auto-scroll.ts"
      to: "export function useAutoScroll(paneKey?: string): UseAutoScrollResult"
      via: "single exported hook (formerly useStickyBottomScroll body, promoted)"
      pattern: "export function useAutoScroll\\(paneKey\\?: string\\)"
    - from: "src/ui/features/pretty-view/PrettyView.tsx (line ~476)"
      to: "useAutoScroll(paneKey)"
      via: "destructures { scrollRef, contentRef, scrollToBottomAndFollow, forceStickAndJump, isPinnedToBottom }"
      pattern: "useAutoScroll\\(paneKey\\)"
    - from: "src/ui/features/pretty-view/PrettyView.tsx handleComposeSend"
      to: "forceStickAndJump via forceStickAndJumpRef"
      via: "ref-forwarded call in the send-message wrapper (unchanged wiring)"
      pattern: "forceStickAndJumpRef.current\\(\\)"
---

<objective>
Strip the `?scroll=stick` URL-param gate from `use-auto-scroll.ts` and promote the sticky-bottom variant to the sole, unconditional default. Ashley confirmed via the gated URL that the sticky variant works; this quick task rips out the losing default (`useDefaultAutoScroll`) and the dispatcher machinery so every fresh page load gets sticky-bottom without needing the URL flag.

Purpose: Close the two-variant fork in the auto-scroll code path that shipped as patch #201 (commit 060c524 — sticky-bottom behind `?scroll=stick`). Retaining both variants is a maintenance and cognitive tax; only one behavior is intended to ship. This task deletes the loser, keeps the winner, and rewrites the test file whose 19 tests exercise the deleted semantics.

Output: Single-file rewrite of `src/ui/features/pretty-view/use-auto-scroll.ts` (delete gate + default + dispatcher, promote sticky to sole hook), full rewrite of `use-auto-scroll.test.ts` for sticky semantics (paneKey reset, gesture-only exit, forceStickAndJump, overflow-anchor), plus two small comment-only edits in `PrettyView.tsx` and `PrettyConversationRow.tsx` that reference the retired gate. Two atomic commits on `feat/tab-title-from-tmux`. NO push, NO docker build, NO deploy — commit-only, per fleet rule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

@src/ui/features/pretty-view/use-auto-scroll.ts
@src/ui/features/pretty-view/use-auto-scroll.test.ts
@src/ui/features/pretty-view/PrettyView.tsx
@src/ui/features/pretty-conversations/PrettyConversationRow.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Strip URL-param gate + losing variant; promote sticky-bottom to sole useAutoScroll</name>
  <files>src/ui/features/pretty-view/use-auto-scroll.ts, src/ui/features/pretty-view/PrettyView.tsx, src/ui/features/pretty-conversations/PrettyConversationRow.tsx</files>
  <action>
    Edit `src/ui/features/pretty-view/use-auto-scroll.ts` — rewrite the file so it exports exactly one hook (`useAutoScroll`) whose body is the current `useStickyBottomScroll` implementation. Concrete steps:

    1. Delete the entire URL-param gate machinery: the `readScrollVariant` function (currently lines ~35-50), the module-scope `SCROLL_VARIANT` const (line ~51), and the dispatcher body inside `useAutoScroll` (currently lines ~82-91). The `SCROLL_KEYS` const and `computeFollowBottomTop` helper are ONLY used by `useDefaultAutoScroll` — verify with grep and delete both if they have no remaining callers after step 2. If any test file still references `computeFollowBottomTop`, delete those tests too (they were unit tests for a pure helper of the deleted variant).

    2. Delete the entire `useDefaultAutoScroll` function body (currently lines ~93-194) — the whole follow-bottom-when-near-bottom implementation goes away.

    3. Promote `useStickyBottomScroll` (currently lines ~220-393) to become the sole exported `useAutoScroll`. Rename `useStickyBottomScroll` to `useAutoScroll` — do NOT keep a re-export alias, do NOT keep the old name as a private inner helper. The exported signature stays `export function useAutoScroll(paneKey?: string): UseAutoScrollResult` (keep paneKey optional so PrettyView's existing call `useAutoScroll(paneKey)` continues to type-check without a signature change).

    4. Keep `UseAutoScrollResult` interface exactly as-is (`scrollRef`, `contentRef`, `scrollToBottomAndFollow`, `forceStickAndJump`, `isPinnedToBottom`). The `forceStickAndJump` alias-to-`scrollToBottomAndFollow` hack that lived in the deleted default variant is no longer needed — the promoted sticky variant already has both as distinct callbacks.

    5. Keep the `BOTTOM_THRESHOLD` and `LOAD_LOCK_MS` module constants that the sticky implementation uses. Keep the `overflow-anchor: none` effect, the paneKey/rAF-chain effect, the ResizeObserver effect, and the user-gesture listener effect verbatim from the current sticky implementation — do NOT re-architect, do NOT rename internal refs, do NOT change the LOAD_LOCK_MS value.

    6. Rewrite the file-level doc comment (currently lines ~3-28) to describe ONLY the sticky-bottom model. Remove the entire "Phase-01 contract" section, the "URL-param-gated sticky-bottom variant (?scroll=stick)" paragraph, and the "WIP overhaul for bounty #4" language. New header comment describes: single sticky-bottom axis, paneKey-driven reset, gesture-only exit criteria (wheel deltaY<0, keydown PageUp/ArrowUp/Home, touchmove that reduces scrollTop), forceStickAndJump as the send-intent re-entry point, and overflow-anchor: none as the browser scroll-anchor suppression. Keep the "Sticky-bottom variant" section-header comment (currently line ~197) — collapse it into the main file header since there is no longer a variant to distinguish.

    7. Grep gate before finishing this file: `grep -c 'SCROLL_VARIANT\|useDefaultAutoScroll\|readScrollVariant\|scroll=stick' src/ui/features/pretty-view/use-auto-scroll.ts` MUST return 0. `grep -c 'export function useAutoScroll' src/ui/features/pretty-view/use-auto-scroll.ts` MUST return 1.

    Edit `src/ui/features/pretty-view/PrettyView.tsx` — comment-only cleanup, NO behavior change:

    8. Line ~384: update the "Sticky-bottom overhaul (bounty #4): handleComposeSend needs to trigger forceStickAndJump on send, but useAutoScroll is called later in this render body." comment — drop the "(bounty #4)" reference and the "overhaul" framing since there is no longer an ongoing overhaul; the comment now just documents the TDZ-avoidance-via-ref pattern for `forceStickAndJump`. Keep the load-bearing explanation of WHY the ref exists (handleComposeSend declared before useAutoScroll in render body).

    9. Line ~406: delete the sentence "No-op in the default variant (aliases scrollToBottomAndFollow there)." — that variant no longer exists. Keep the preceding sentence "A send is the strongest possible 'I want to see the reply' signal — force stick + jump regardless of prior scroll position."

    10. Line ~473: update the comment "paneKey is passed so the sticky-bottom variant (?scroll=stick) can reset to bottom on conversation swap. The default variant ignores it." to read "paneKey is passed so useAutoScroll can reset to bottom on conversation swap (fresh conversation load always lands at bottom via a rAF-chain jump for LOAD_LOCK_MS)." — drop the variant references entirely.

    11. Do NOT touch the actual `useAutoScroll(paneKey)` call at line ~476 or the `forceStickAndJumpRef` wiring at lines ~482. Do NOT touch the pill JSX (~lines 1236-1246). Do NOT touch the `handleComposeSend` body other than the comment cleanup in step 9. Do NOT change any other `sticky`/`Sticky` reference in PrettyView.tsx — the pill's `className="sticky bottom-2 …"` (CSS position), the `sticky/fixed descendants` comment at ~line 1081, and the `Replaces the previous sticky top-of-scroll banner (retired…)` history note at ~line 1161 are unrelated to this task.

    Edit `src/ui/features/pretty-conversations/PrettyConversationRow.tsx` — comment-only cleanup:

    12. Line ~84: update the comment "Read ONCE at module load (SCROLL_VARIANT pattern from use-auto-scroll.ts): URL doesn't change without reload, so the const stays stable across renders and there's no hook-order risk from the conditional render." to remove the "SCROLL_VARIANT pattern from use-auto-scroll.ts" back-reference (that const no longer exists). Replace with "Read ONCE at module load: URL doesn't change without reload, so the const stays stable across renders and there's no hook-order risk from the conditional render." — keep the technical explanation, drop the dead reference.

    13. Do NOT touch anything else in `PrettyConversationRow.tsx` — this is a comment-only edit. The `CONV_MENU_VARIANT` machinery is a live, still-shipping URL-param variant (bounty #6, `?menu=context`) and stays exactly as-is.

    Global scope guards for this task:
    - Do NOT touch any file under `src/backend/`.
    - Do NOT touch `~/.claude/identities/tina/skynet-patches.md`, any bounty file under `~/.claude/identities/tina/bounties/`, or `~/.claude/identities/tina/tina.md`.
    - Do NOT `git push`, do NOT `docker build`, do NOT `docker compose up`.

    Commit boundary for Task 1: one atomic commit on `feat/tab-title-from-tmux` with message shape `refactor(pretty-view): promote sticky-bottom scroll to sole useAutoScroll variant` (or similar). The test file rewrite is Task 2's separate commit — do NOT include test changes in this commit.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; grep -v '^#' src/ui/features/pretty-view/use-auto-scroll.ts src/ui/features/pretty-view/PrettyView.tsx src/ui/features/pretty-conversations/PrettyConversationRow.tsx | grep -c 'SCROLL_VARIANT\|useDefaultAutoScroll\|readScrollVariant\|scroll=stick' &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - `grep -c 'SCROLL_VARIANT\|useDefaultAutoScroll\|readScrollVariant\|scroll=stick' src/ui/features/pretty-view/use-auto-scroll.ts src/ui/features/pretty-view/PrettyView.tsx src/ui/features/pretty-conversations/PrettyConversationRow.tsx` returns 0 across all three files (gate residue fully purged from source and comments).
    - `grep -c 'export function useAutoScroll' src/ui/features/pretty-view/use-auto-scroll.ts` returns exactly 1.
    - `grep -c 'export function useStickyBottomScroll\|function useStickyBottomScroll\|function useDefaultAutoScroll' src/ui/features/pretty-view/use-auto-scroll.ts` returns 0 (both old names gone).
    - `npx tsc --noEmit` exits 0 (no type errors in the promoted hook or the consumer edits).
    - `git diff --stat` for this commit shows exactly three files: `use-auto-scroll.ts`, `PrettyView.tsx`, `PrettyConversationRow.tsx`. NO `use-auto-scroll.test.ts` (that is Task 2). NO backend files. NO identity-side files.
    - Atomic commit on `feat/tab-title-from-tmux`; `git log --oneline -1` shows the commit; branch remains unpushed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite use-auto-scroll.test.ts for sticky-bottom semantics</name>
  <files>src/ui/features/pretty-view/use-auto-scroll.test.ts</files>
  <action>
    The existing 19 tests in `use-auto-scroll.test.ts` exercise the DELETED `useDefaultAutoScroll` semantics (follow-bottom-when-near-bottom, scroll-event syncs pinned via distFromBottom threshold, RO held-position vs follows-bottom split). After Task 1, those semantics no longer exist — the hook is now sticky-bottom-with-gesture-exit. Full rewrite of the test file.

    Preserve infrastructure from the current file:
    1. Keep the `CapturingResizeObserver` polyfill block (currently lines 1-16) verbatim — it captures the most recent RO callback so tests can trigger content-growth notifications without a real ResizeObserver. JSDOM does not ship RO.
    2. Keep the `makeScrollEl` helper (currently lines 26-63) verbatim — it builds a mock scroll container with configurable `scrollHeight`, `clientHeight`, `scrollTop`, and `getBoundingClientRect`.
    3. Keep the imports of `describe, it, expect` from vitest and `renderHook, act` from `@testing-library/react`.
    4. Update the imports from `./use-auto-scroll` to import ONLY `useAutoScroll` — drop `computeFollowBottomTop` (that helper is deleted in Task 1).

    Delete the entire existing test body (all `describe` blocks from line ~69 to end of file). Replace with a fresh test suite covering sticky-bottom semantics. Required coverage (write one or more `it()` blocks per bullet; group under `describe("useAutoScroll — sticky-bottom", () => { … })`):

    5. **overflow-anchor CSS application (mount/unmount)**: On mount with a scrollEl bound via `scrollRef`, the container's `style.overflowAnchor` becomes `"none"`. On unmount, the prior value is restored. Test both directions: mount → `expect(scrollEl.style.overflowAnchor).toBe("none")`; unmount via `unmount()` from renderHook → the prior value returns.

    6. **Fresh mount starts sticky + isPinnedToBottom true**: After `scrollRef(scrollEl)` binds, `result.current.isPinnedToBottom` is `true` (initial state per hook's `useState<boolean>(true)`).

    7. **paneKey reset on conversation switch**: Render the hook with `paneKey="pane-A"`, bind scrollEl + contentEl, then exit sticky via a wheel-up gesture (`scrollEl.dispatchEvent(new WheelEvent("wheel", { deltaY: -100 }))`), assert `isPinnedToBottom` is now `false`. Then `rerender({ paneKey: "pane-B" })` (renderHook's `initialProps` + `rerender` API) and assert `isPinnedToBottom` returns to `true`. Extra assertion: after the paneKey change, firing the captured RO callback with content-growth should still write `scrollEl.scrollTop = scrollEl.scrollHeight` (proving sticky was re-entered).

    8. **Gesture-only exit: wheel deltaY < 0 (up) exits sticky; wheel deltaY > 0 (down) does NOT exit sticky**: Two sub-cases in one `it()` or two adjacent `it()` blocks. Dispatch `new WheelEvent("wheel", { deltaY: -50 })` → `isPinnedToBottom` becomes `false`. Reset (rerender with new paneKey to re-enter sticky). Dispatch `new WheelEvent("wheel", { deltaY: +50 })` → `isPinnedToBottom` STAYS `true`. Note: because the mount effect arms the load-lock for `LOAD_LOCK_MS` (300ms), tests must either wait past the lock (`vi.useFakeTimers()` + `vi.advanceTimersByTime(350)`) or dispatch the wheel event after mocking `Date.now` past the lock. Prefer `vi.useFakeTimers()` for determinism; call `vi.useRealTimers()` in an `afterEach`. Document the lock behavior with an inline comment so future readers understand the timer setup.

    9. **Gesture-only exit: keydown PageUp / ArrowUp / Home exits sticky; PageDown / ArrowDown / End / letters do NOT**: Loop over the exit-triggering keys and assert each flips `isPinnedToBottom` to `false` (from a fresh sticky state per key). Then loop over the non-exit keys (`"PageDown"`, `"ArrowDown"`, `"End"`, `"a"`, `" "`) and assert each leaves `isPinnedToBottom` at `true`. Same load-lock caveat as bullet 8 — advance fake timers past LOAD_LOCK_MS before dispatching.

    10. **Gesture-only exit: touchmove that REDUCES scrollTop by > 2px exits sticky; touchmove where scrollTop is unchanged or increases does NOT**: The sticky hook's touchmove handler compares `scrollEl.scrollTop` against `lastScrollTopRef.current` and only exits sticky when `now < prev - 2`. Test sub-case A: set scrollTop to 500, dispatch touchmove to prime `lastScrollTopRef` (via scroll event or initial mount), then set scrollTop to 400 and dispatch touchmove → assert `isPinnedToBottom` becomes `false`. Test sub-case B: with `lastScrollTopRef` primed at 500, set scrollTop to 500 (unchanged) and dispatch touchmove → assert `isPinnedToBottom` stays `true`. Test sub-case C (regression guard for the mobile-tap bug the sticky variant fixed): with `lastScrollTopRef` primed at 500, set scrollTop to 501 (moved DOWN by 1px, simulating passive tap-drag) and dispatch touchmove → assert `isPinnedToBottom` stays `true`.

    11. **forceStickAndJump re-enters sticky + jumps + arms load-lock**: Render, exit sticky via wheel-up, then call `result.current.forceStickAndJump()` inside `act()`. Assert three things: `isPinnedToBottom` becomes `true`; `scrollEl.scrollTop` equals `scrollEl.scrollHeight`; AND a subsequent wheel-up event fired IMMEDIATELY after (within LOAD_LOCK_MS) does NOT exit sticky again (proving the load-lock re-arm). This is the send-path contract that PrettyView's `handleComposeSend` relies on.

    12. **ResizeObserver — content growth while sticky writes scrollTop = scrollHeight**: Mount, bind refs, simulate content growth by mutating `scrollHeight` (use the pattern from the existing "follows bottom when pinned" test at lines 415-464 — the `scrollHeightState` mutable-getter setup transfers directly), then fire `capturedROCallback?.([], {} as ResizeObserver)` inside `act()`. Assert `scrollEl.scrollTop === new scrollHeight`. Assert `isPinnedToBottom` stays `true`.

    13. **ResizeObserver — content growth while NOT sticky does NOT write scrollTop**: Same setup as bullet 12 but first exit sticky via wheel-up. After firing the RO callback, `scrollEl.scrollTop` is unchanged from its pre-callback value. `isPinnedToBottom` reflects the current distance-from-bottom (whether it flipped back to `true` or stays `false` depends on the mock geometry — assert against the computed `distFromBottom <= BOTTOM_THRESHOLD` check to keep the test tied to the actual contract).

    14. **scrollToBottomAndFollow sets sticky + jumps** (PrettyView pill's "jump-to-latest" affordance calls this): Mount, exit sticky, call `result.current.scrollToBottomAndFollow()` inside `act()`. Assert `isPinnedToBottom` is `true` and `scrollEl.scrollTop === scrollEl.scrollHeight`. Note the difference vs `forceStickAndJump`: `scrollToBottomAndFollow` does NOT arm the load-lock — a wheel-up immediately after CAN exit sticky. Assert this too (fire wheel-up right after, assert `isPinnedToBottom` becomes `false`).

    15. **Scroll-back-to-bottom re-enters sticky without pill**: With hook exited from sticky (dist > BOTTOM_THRESHOLD), programmatically set `scrollTop` so `dist <= BOTTOM_THRESHOLD`, dispatch a scroll event, and assert `isPinnedToBottom` becomes `true` again. Confirms the auto-re-enter-on-manual-scroll-to-bottom behavior in the sticky variant's scroll handler.

    Structural rules for the rewritten file:
    - Total test count target: 12-18 `it()` blocks (roughly matches the 19 deleted tests; a bit fewer is fine since the sticky model has fewer branches).
    - Keep the file self-contained — no new test-fixture files, no shared helpers moved out.
    - Use `beforeEach` / `afterEach` for timer setup (`vi.useFakeTimers()` / `vi.useRealTimers()`) if bullets 8-9 need it. Import `beforeEach, afterEach, vi` from vitest.
    - When resetting `capturedROCallback` between tests, either add `beforeEach(() => { capturedROCallback = null; })` or accept that each renderHook installs a fresh RO (in which case the module-scope `capturedROCallback` naturally overwrites — document the choice with an inline comment).
    - Do NOT test `computeFollowBottomTop` — that pure helper is deleted in Task 1.
    - Do NOT test the deleted `SCROLL_VARIANT` gate — no `?scroll=stick` semantics remain to verify.

    Grep gates before finishing this file:
    - `grep -c 'computeFollowBottomTop\|useDefaultAutoScroll\|SCROLL_VARIANT\|scroll=stick' src/ui/features/pretty-view/use-auto-scroll.test.ts` MUST return 0.
    - `grep -c 'sticky\|paneKey\|forceStickAndJump\|overflow-anchor\|overflowAnchor' src/ui/features/pretty-view/use-auto-scroll.test.ts` MUST return > 0 (proves the new coverage exists).

    Global scope guards for this task:
    - Do NOT touch `use-auto-scroll.ts` in this commit (that was Task 1).
    - Do NOT touch `PrettyView.tsx`, `PrettyView.test.tsx`, or `PrettyView.aside.test.tsx` — the latter two still import PrettyView and their existing ResizeObserver polyfill setup is unaffected by the source rewrite.
    - Do NOT touch any file under `src/backend/`.
    - Do NOT touch `~/.claude/identities/tina/` at all.
    - Do NOT `git push`, do NOT `docker build`, do NOT `docker compose up`.

    Commit boundary for Task 2: one atomic commit on `feat/tab-title-from-tmux` with message shape `test(pretty-view): rewrite use-auto-scroll tests for sticky-bottom semantics`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -20 &amp;&amp; npx vitest run src/ui/features/pretty-view/use-auto-scroll.test.ts 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    - `npx tsc --noEmit` exits 0 (rewritten test file type-checks).
    - `npx vitest run src/ui/features/pretty-view/use-auto-scroll.test.ts` reports 0 failed. Test count is 12-18 (rough match for the 19 deleted).
    - `grep -c 'computeFollowBottomTop\|useDefaultAutoScroll\|SCROLL_VARIANT\|scroll=stick' src/ui/features/pretty-view/use-auto-scroll.test.ts` returns 0.
    - `grep -c 'sticky\|paneKey\|forceStickAndJump\|overflowAnchor' src/ui/features/pretty-view/use-auto-scroll.test.ts` returns > 0.
    - Sibling PrettyView suites still pass: `npx vitest run src/ui/features/pretty-view/PrettyView.test.tsx src/ui/features/pretty-view/PrettyView.aside.test.tsx` = 0 failed.
    - ComposeBox suites (which don't import useAutoScroll directly but are the last-touched files before this task per STATE.md) still pass: `npx vitest run src/ui/features/pretty-view/ComposeBox.test.tsx src/ui/features/pretty-view/ComposeBox.voice.test.tsx src/ui/features/pretty-view/ComposeBox.aside-morph.test.tsx src/ui/features/pretty-view/ComposeBox.aside-props.test.tsx src/ui/features/pretty-view/ComposeBox.recycle-disable.test.tsx` = 0 failed.
    - `git diff --stat` for this commit shows exactly one file: `use-auto-scroll.test.ts`. NO other files.
    - Atomic commit on `feat/tab-title-from-tmux`; branch remains unpushed.
  </done>
</task>

</tasks>

<verification>
Post-plan global checks (run once after both commits land):

1. Type check clean: `cd /home/ubuntu/skynet && npx tsc --noEmit` exits 0.
2. Rewritten hook tests green: `npx vitest run src/ui/features/pretty-view/use-auto-scroll.test.ts` = 0 failed.
3. Sibling pretty-view suites green: `npx vitest run src/ui/features/pretty-view/` = 0 failed (this scopes to the whole directory including PrettyView.test.tsx, PrettyView.aside.test.tsx, ChatMessage.test.tsx, ComposeBox.*.test.tsx, AsideBubble.test.tsx, AttachmentChipStrip.test.tsx, DropOverlay.test.tsx, PrettyLandingCard.test.tsx, RelayInboundBubble.test.tsx, RelayOutboundBubble.test.tsx, RecordingControls.test.tsx, SessionHoldingOverlay.test.tsx, commandTags.test.ts, relay-mxid-resolve.test.ts, use-pretty-view-uploads.test.ts, useVoiceRecording.test.ts, use-auto-scroll.test.ts).
4. PrettyConversationRow sibling suite still passes: `npx vitest run src/ui/features/pretty-conversations/` = 0 failed. This validates the comment-only edit in PrettyConversationRow.tsx did not break the `CONV_MENU_VARIANT` machinery it references.
5. Full-suite regression sanity: `npx vitest run` = 0 failed across all frontend suites.
6. Gate purge confirmed: `grep -rn 'SCROLL_VARIANT\|useDefaultAutoScroll\|readScrollVariant\|scroll=stick' src/` returns nothing. The comment in `PrettyConversationRow.tsx:84` referencing "SCROLL_VARIANT pattern" has been rewritten to remove the back-reference.
7. Public API preserved: `grep -c 'useAutoScroll' src/ui/features/pretty-view/PrettyView.tsx` returns >= 3 (import + call + JSDoc/comment). No consumer code paths changed shape.
8. Scope guards: `git diff --name-only HEAD~2` (post both commits) shows only files under `src/ui/features/pretty-view/` and `src/ui/features/pretty-conversations/`. NO `src/backend/` files. NO `~/.claude/identities/tina/` files.
9. Terminal boundary: STOP after Task 2 commits. Do NOT `git push`. Do NOT `docker build`. Do NOT `docker compose up`. Do NOT edit `~/.claude/identities/tina/skynet-patches.md` (Tina writes the patch entry after this executor returns). Do NOT flip the bounty status at `~/.claude/identities/tina/bounties/conversations-scroll-to-bottom-on-load/bounty.json` (Tina archives it after). Do NOT touch `~/.claude/identities/tina/tina.md` in any way.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → pretty-view scroll container | Local UI-only change; no data crosses a trust boundary. Wheel/touchmove/keydown events already flow into the hook and continue to do so unchanged. |
| user → URL query/hash | The `?scroll=stick` / `#scroll=stick` gate is being DELETED. After this task, that URL param no longer has any effect (it becomes an inert unknown param). Not a new boundary; a removed observation point. |
| PrettyView → useAutoScroll hook | Existing call `useAutoScroll(paneKey)` — signature preserved. No boundary change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-mj2-01 | Tampering | `use-auto-scroll.ts` public API surface (`UseAutoScrollResult`) | mitigate | Interface is preserved exactly (`scrollRef`, `contentRef`, `scrollToBottomAndFollow`, `forceStickAndJump`, `isPinnedToBottom`); the `useAutoScroll(paneKey?)` signature is preserved. TypeScript strict mode + `npx tsc --noEmit` gate in Task 1 and Task 2 `<verify>` blocks catches any accidental shape drift at compile time. PrettyView's existing destructure at line ~476 acts as a live consumer check — if the shape changed, it would fail to type-check. |
| T-mj2-02 | Denial of Service | Auto-scroll behavior regression on fresh page load | mitigate | The sticky variant has been running under `?scroll=stick` and Ashley greenlit its behavior for shipping as default. The rewritten test suite (Task 2) covers paneKey reset, gesture-only exit, forceStickAndJump, and RO-driven content-growth handling — all the behaviors PrettyView relies on to keep the message stream pinned. Sibling `PrettyView.test.tsx` / `PrettyView.aside.test.tsx` in the post-plan verification (item 3) provide integration coverage that the promoted hook still works inside its consumer. |
| T-mj2-03 | Repudiation | Load-bearing comments that referenced the deleted gate | mitigate | Task 1 explicitly enumerates every comment touching `SCROLL_VARIANT`/`?scroll=stick`/`useDefaultAutoScroll` (in `use-auto-scroll.ts`, `PrettyView.tsx` at lines 384/406/473, `PrettyConversationRow.tsx` at line 84) and specifies the replacement text. The `<done>` grep gates on the task confirm zero residue. Prevents stale-comment misdirection for future readers. |
| T-mj2-04 | Information Disclosure | None | accept | Pure UI-side scroll behavior; no PII, no auth material, no backend interaction. No IDOR / auth-scope concerns. |
| T-mj2-05 | Elevation of Privilege | None | accept | Local UI hook — no privilege boundary in scope. |
| T-mj2-SC | Tampering | Dependencies | accept | Zero new packages added. No `npm install`, no `package.json` diff. `react` / `@testing-library/react` / `vitest` all already present and pinned. Package-legitimacy checkpoint gate does not apply (no install tasks in this plan). |
</threat_model>

<success_criteria>
- `use-auto-scroll.ts` exports exactly one hook: `useAutoScroll(paneKey?: string): UseAutoScrollResult`, whose body is the promoted sticky-bottom implementation (formerly `useStickyBottomScroll`).
- `SCROLL_VARIANT`, `readScrollVariant`, `useDefaultAutoScroll`, and the dispatcher body are fully deleted from `use-auto-scroll.ts`.
- `computeFollowBottomTop` is deleted from `use-auto-scroll.ts` (it was only used by the deleted default variant).
- The `overflow-anchor: none` inline style, the paneKey-driven rAF-chain jump, the RO-driven bottom-follow, the gesture-only sticky-exit logic (wheel deltaY<0, PageUp/ArrowUp/Home, touchmove-reducing-scrollTop-by->2px), and the `forceStickAndJump` + load-lock re-arm all remain byte-for-behavior-equivalent to the current sticky variant.
- PrettyView.tsx's `useAutoScroll(paneKey)` call is byte-identical; the three surrounding comments at lines ~384, ~406, ~473 are updated to drop `?scroll=stick` / "default variant" / "overhaul" references.
- PrettyConversationRow.tsx's comment at line ~84 no longer back-references the deleted `SCROLL_VARIANT` const; the still-live `CONV_MENU_VARIANT` machinery is untouched.
- `use-auto-scroll.test.ts` is rewritten from scratch (module-level infrastructure preserved — polyfill, `makeScrollEl` helper, imports); new tests cover: overflow-anchor mount/unmount, initial sticky state, paneKey reset, gesture-only exit (wheel/keydown/touchmove sub-cases including the mobile passive-tap regression guard), forceStickAndJump re-entry + load-lock re-arm, ResizeObserver behavior in both sticky and non-sticky states, `scrollToBottomAndFollow` (no load-lock), and manual-scroll-back-to-bottom re-enters sticky.
- `npx tsc --noEmit` exits 0.
- `npx vitest run src/ui/features/pretty-view/use-auto-scroll.test.ts` = 0 failed.
- `npx vitest run src/ui/features/pretty-view/ src/ui/features/pretty-conversations/` = 0 failed (no sibling regressions).
- `grep -rn 'SCROLL_VARIANT\|useDefaultAutoScroll\|readScrollVariant\|scroll=stick' src/` returns nothing.
- Two atomic commits on `feat/tab-title-from-tmux`: Task 1 (source strip + comment cleanup across 3 files) and Task 2 (test file rewrite). Executor stops at commit boundary — NO push, NO docker build, NO docker compose, NO edits under `~/.claude/identities/tina/`, NO edits to `skynet-patches.md`, NO edits to any bounty file, NO edits to `tina.md`.
</success_criteria>

<output>
Create `.planning/quick/260730-mj2-sticky-scroll-strip/260730-mj2-01-SUMMARY.md` when done, documenting:
- Exact line-range of the `use-auto-scroll.ts` rewrite (net line delta — old sticky was ~197 lines inside a variant, new sole hook should be smaller since the dispatcher + default + gate are gone).
- Line-range summary of the three comment edits in `PrettyView.tsx` (~384, ~406, ~473) and the one comment edit in `PrettyConversationRow.tsx` (~84).
- New test count in `use-auto-scroll.test.ts` (12-18 `it()` blocks) and the coverage bullets they map to (bullets 5-15 from Task 2's `<action>` block).
- Both commit SHAs on `feat/tab-title-from-tmux`.
- Verification output: `tsc --noEmit` exit code, `vitest run src/ui/features/pretty-view/` pass count, `vitest run src/ui/features/pretty-conversations/` pass count.
- Grep-purge confirmation: `grep -rn 'SCROLL_VARIANT\|useDefaultAutoScroll\|readScrollVariant\|scroll=stick' src/` output (should be empty).
- Explicit confirmation: no push, no docker build, no docker compose, no files touched under `src/backend/`, no files touched under `~/.claude/identities/tina/` (no skynet-patches.md edit, no bounty.json flip, no tina.md edit).
- Branch state: `git status` shows clean working tree; branch is `feat/tab-title-from-tmux`, ahead of origin by 2 unpushed commits (Task 1 + Task 2 for this quick task, on top of whatever prior unpushed commits already existed per the STATE.md deploy queue).
</output>
