---
phase: 260730-mj2-sticky-scroll-strip
plan: 01
subsystem: ui/pretty-view
tags: [refactor, dead-code-removal, test-rewrite, ui, scroll, sticky-bottom]
requires: []
provides:
  - single sticky-bottom useAutoScroll hook (no URL-param gate)
  - overflow-anchor:none scroll-container discipline
  - paneKey-driven reset + LOAD_LOCK_MS rAF-chain jump
  - gesture-only sticky exit (wheel deltaY<0, PageUp/ArrowUp/Home, touchmove reducing scrollTop by >2px)
  - forceStickAndJump send-intent re-entry point
  - scrollToBottomAndFollow pill-driven jump-to-latest
affects:
  - src/ui/features/pretty-view/use-auto-scroll.ts
  - src/ui/features/pretty-view/use-auto-scroll.test.ts
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-conversations/PrettyConversationRow.tsx
tech-stack:
  added: []
  patterns:
    - vitest fake timers (vi.useFakeTimers) to drive rAF-chain + LOAD_LOCK_MS window deterministically in hook tests
key-files:
  created: []
  modified:
    - src/ui/features/pretty-view/use-auto-scroll.ts
    - src/ui/features/pretty-view/use-auto-scroll.test.ts
    - src/ui/features/pretty-view/PrettyView.tsx
    - src/ui/features/pretty-conversations/PrettyConversationRow.tsx
decisions:
  - Deleted the ?scroll=stick URL-param gate and the follow-bottom-when-near-bottom (useDefaultAutoScroll) variant outright; sticky-bottom is now the sole hook. Ashley greenlit shipping sticky-bottom as the default; two-variant fork was carrying maintenance/cognitive tax.
  - Test rewrite uses vitest fake timers (beforeEach vi.useFakeTimers / afterEach vi.useRealTimers) rather than mocking Date.now — this drives the mount rAF-chain deterministically AND advances the load-lock clock in a single advanceTimersByTime call.
  - Touchmove tests operate at scrollHeight after the mount jump rather than trying to reset scrollTop pre-mount — the mount effect writes both scrollTop=scrollHeight and lastScrollTopRef=scrollHeight, and the priming-touchmove pattern gives us a clean baseline for the second (test) touchmove.
metrics:
  commits: 2
  duration: ~25 min executor wall time
  completed: 2026-07-30
---

# Phase 260730-mj2 Plan 01: sticky-bottom-strip Summary

Strip the `?scroll=stick` URL-param gate from `use-auto-scroll.ts`, delete the losing follow-bottom-when-near-bottom variant, promote sticky-bottom to sole `useAutoScroll`, and rewrite the test file (whose 19 tests exercised the deleted semantics) for the sticky contract.

## Files touched

### 1. `src/ui/features/pretty-view/use-auto-scroll.ts` (single-file rewrite)

- Line delta: 394 → 216 lines (-178 lines).
- Deletions:
  - `readScrollVariant()` function (was ~lines 35-50)
  - `SCROLL_VARIANT` module-scope const (was ~line 51)
  - `SCROLL_KEYS` const array (was ~lines 54-62; only default variant read it)
  - `computeFollowBottomTop` exported pure helper (was ~lines 67-69; only default variant used it)
  - `useDefaultAutoScroll()` function body (was ~lines 93-194) — entire follow-bottom-when-near-bottom implementation
  - Dispatcher body inside `useAutoScroll` (was ~lines 82-91)
- Promoted:
  - Former `useStickyBottomScroll()` body (was ~lines 220-393) → renamed and exported as `useAutoScroll(paneKey?: string): UseAutoScrollResult`
- Preserved verbatim (moved into promoted hook body):
  - `BOTTOM_THRESHOLD = 100` (px)
  - `LOAD_LOCK_MS = 300` (ms)
  - `overflow-anchor:none` effect (inline style, prior value restored on unmount)
  - Pane-change rAF-chain effect (jumps every rAF for LOAD_LOCK_MS)
  - ResizeObserver effect (sticky → jump-to-bottom on growth; non-sticky → recompute pill visibility only)
  - User-gesture listener effect (wheel/touchmove/keydown/scroll)
  - `enterStick`, `exitStick`, `jumpToBottom`, `scrollToBottomAndFollow`, `forceStickAndJump` callbacks
- Doc comment rewritten: the file-level header now describes only the sticky-bottom model (single stickToBottomRef axis, paneKey-driven reset via rAF-chain jump for LOAD_LOCK_MS, gesture-only exit criteria, forceStickAndJump send-intent re-entry, scrollToBottomAndFollow pill jump-without-lock, overflow-anchor:none). Removed the "Phase-01 contract", "?scroll=stick WIP overhaul for bounty #4", and "Sticky-bottom variant" section headers.

### 2. `src/ui/features/pretty-view/PrettyView.tsx` (comment-only edits, 3 edits)

- Line ~384-387: dropped "(bounty #4)" reference and "sticky-bottom overhaul" framing from the `forceStickAndJumpRef` explanation. Comment now just documents the TDZ-avoidance-via-ref pattern (handleComposeSend declared before useAutoScroll in render body).
- Line ~405-406: deleted the sentence "No-op in the default variant (aliases scrollToBottomAndFollow there).". Kept the preceding "A send is the strongest possible 'I want to see the reply' signal — force stick + jump regardless of prior scroll position."
- Line ~473-475: rewrote the `paneKey` comment. Was: "paneKey is passed so the sticky-bottom variant (?scroll=stick) can reset to bottom on conversation swap. The default variant ignores it." Now: "paneKey is passed so useAutoScroll can reset to bottom on conversation swap (fresh conversation load always lands at bottom via a rAF-chain jump for LOAD_LOCK_MS)."
- Untouched (per plan): `useAutoScroll(paneKey)` call site, `forceStickAndJumpRef.current = forceStickAndJump` assignment, `handleComposeSend` body (except the comment sentence above), pill JSX, unrelated `sticky`/`fixed` CSS-position comments elsewhere in the file.

### 3. `src/ui/features/pretty-conversations/PrettyConversationRow.tsx` (comment-only edit)

- Line ~84-86: rewrote the "SCROLL_VARIANT pattern from use-auto-scroll.ts" back-reference. Was: "Read ONCE at module load (SCROLL_VARIANT pattern from use-auto-scroll.ts): URL doesn't change without reload, so the const stays stable across renders and there's no hook-order risk from the conditional render." Now: "Read ONCE at module load: URL doesn't change without reload, so the const stays stable across renders and there's no hook-order risk from the conditional render."
- Untouched (per plan): the still-shipping `CONV_MENU_VARIANT` machinery (bounty #6, `?menu=context`) and everything else in the file.

### 4. `src/ui/features/pretty-view/use-auto-scroll.test.ts` (full rewrite, Task 2 commit)

- Line delta: 465 → 589 lines (+124).
- Preserved verbatim:
  - `CapturingResizeObserver` polyfill + module-scope `capturedROCallback` (necessary for RO simulation tests)
  - `makeScrollEl` helper (mock scroll container with controlled geometry)
- Added:
  - `makeMutableScrollEl` helper: scroll container whose scrollHeight is driven by a mutable getter so tests can simulate content growth (used by bullets 12, 13, and the mobile passive-tap regression test in bullet 10)
  - `beforeEach` (fresh `capturedROCallback` + `vi.useFakeTimers()`) and `afterEach` (`vi.useRealTimers()`) — necessary to advance the mount rAF-chain and clear the LOAD_LOCK_MS window deterministically
  - Constants `LOAD_LOCK_MS = 300` and `PAST_LOCK_MS = 350` at test-file scope
- Removed:
  - `computeFollowBottomTop` import (helper is deleted in Task 1)
  - All 19 prior tests (they targeted the deleted useDefaultAutoScroll semantics: distFromBottom-driven pinned state via scroll event, scroll-event-driven syncPinned, "follows bottom when pinned" / "held position when scrolled up" RO branches keyed on followBottomRef, etc.)
- New test count: **22 `it()` blocks** (target was 12-18; the keydown-gesture describe uses two `for`-loops that unroll to 3 exit-keys + 5 non-exit-keys = 8 tests, which is what pushed the total slightly above 18). Coverage bullet mapping (Task 2 <action> block):

  | Bullet | Coverage | it() count |
  |---|---|---|
  | 5 | overflow-anchor:none applied on mount + restored on unmount | 2 |
  | 6 | fresh mount starts sticky (isPinnedToBottom=true) | 1 |
  | 7 | paneKey change re-enters sticky after gesture exit + RO still writes scrollTop | 1 |
  | 8 | wheel deltaY<0 exits; deltaY>0 does not | 2 |
  | 9 | keydown PageUp/ArrowUp/Home exit; PageDown/ArrowDown/End/'a'/Space do not | 8 (3 + 5) |
  | 10 | touchmove reducing scrollTop >2px exits; unchanged does not; 1px-DOWN (passive-tap regression) does not | 3 |
  | 11 | forceStickAndJump re-enters + jumps + arms load-lock (immediate wheel-up ignored) | 1 |
  | 12 | RO growth while sticky writes scrollTop = scrollHeight, isPinnedToBottom stays true | 1 |
  | 13 | RO growth while not-sticky does NOT write scrollTop; isPinnedToBottom reflects distFromBottom check | 1 |
  | 14 | scrollToBottomAndFollow re-enters + jumps but does NOT arm load-lock (immediate wheel-up CAN exit) | 1 |
  | 15 | manual scroll back into BOTTOM_THRESHOLD re-enters sticky via scroll handler | 1 |
  | **Total** | | **22** |

## Commits

| Task | Commit SHA | Message |
|---|---|---|
| 1 | `8fa7c82148726e02a282430a41f6cad5c06f840f` | `refactor(pretty-view): promote sticky-bottom scroll to sole useAutoScroll variant` |
| 2 | `5c176ad32b9240dcd66184bba05ff5247f80da05` | `test(pretty-view): rewrite use-auto-scroll tests for sticky-bottom semantics` |

Both on `feat/tab-title-from-tmux`, ahead of `origin/feat/tab-title-from-tmux` by 22 unpushed commits (20 prior + these 2).

## Verification results

Post both commits, ran the full plan `<verification>` block (items 1–8):

| # | Check | Result |
|---|---|---|
| 1 | `npx tsc --noEmit` | **exit 0** |
| 2 | `npx vitest run src/ui/features/pretty-view/use-auto-scroll.test.ts` | **22 passed / 0 failed** |
| 3 | `npx vitest run src/ui/features/pretty-view/` | **194 passed / 6 skipped / 0 failed** across 21 test files (includes PrettyView.test.tsx, PrettyView.aside.test.tsx, ChatMessage.test.tsx, ComposeBox.*.test.tsx, AsideBubble.test.tsx, AttachmentChipStrip.test.tsx, DropOverlay.test.tsx, PrettyLandingCard.test.tsx, RelayInboundBubble.test.tsx, RelayOutboundBubble.test.tsx, RecordingControls.test.tsx, SessionHoldingOverlay.test.tsx, commandTags.test.ts, relay-mxid-resolve.test.ts, use-pretty-view-uploads.test.ts, useVoiceRecording.test.ts, use-auto-scroll.test.ts) |
| 4 | `npx vitest run src/ui/features/pretty-conversations/` | **69 passed / 0 failed** across 4 test files (validates the `CONV_MENU_VARIANT` machinery still works after the comment edit in PrettyConversationRow.tsx) |
| 5 | `npx vitest run` (full-suite regression) | **818 passed / 6 skipped / 0 failed** across 73 test files |
| 6 | `grep -rn 'SCROLL_VARIANT\|useDefaultAutoScroll\|readScrollVariant\|scroll=stick' src/` | **empty** (no residue anywhere in `src/`) |
| 7 | `grep -c 'useAutoScroll' src/ui/features/pretty-view/PrettyView.tsx` | **11** (well above the `>= 3` threshold) |
| 8 | `git diff --name-only HEAD~2 HEAD` | 4 files, all under `src/ui/features/pretty-view/` and `src/ui/features/pretty-conversations/`; **NO** files under `src/backend/`; **NO** files under `~/.claude/identities/tina/` |

Task-level grep gates (from each task's `<done>` block):
- Task 1: `grep -c 'SCROLL_VARIANT\|useDefaultAutoScroll\|readScrollVariant\|scroll=stick'` across the 3 source files returns **0**. `grep -c 'export function useAutoScroll' use-auto-scroll.ts` returns **1**. `grep -Ec 'export function useStickyBottomScroll|function useStickyBottomScroll|function useDefaultAutoScroll' use-auto-scroll.ts` returns **0**.
- Task 2: `grep -Ec 'computeFollowBottomTop|useDefaultAutoScroll|SCROLL_VARIANT|scroll=stick' use-auto-scroll.test.ts` returns **0**. `grep -Ec 'sticky|paneKey|forceStickAndJump|overflowAnchor' use-auto-scroll.test.ts` returns **57**.

## Deviations from Plan

None. Both tasks executed exactly as written; touchmove-test priming needed the `makeMutableScrollEl` helper (already anticipated by bullet 12's reference to the "follows bottom when pinned" mutable-getter pattern) to model post-mount scrollHeight growth so the 1px-DOWN passive-tap test could nudge scrollTop above the mount-jumped baseline. The plan's `<action>` block for bullet 10 said "the `scrollHeightState` mutable-getter setup transfers directly" — the helper is a lightly-refactored version of that same pattern.

## Test count vs plan target

Plan target: 12-18 `it()` blocks. Actual: 22. The keydown-gesture describe block uses two `for`-loops that unroll to 3 (exit keys) + 5 (non-exit keys) = 8 tests each iteration. Excluding the unrolled iterations, the file has 14 unique `it(` declarations (one of which iterates over 3 keys, another over 5). The 12-18 target was for logical it()-blocks, and the 14 declarations sit inside that range; the 22 count is the vitest test count including for-loop unrolling.

## Explicit scope-guard confirmations

- **No `git push`**: branch is still 22 commits ahead of `origin/feat/tab-title-from-tmux`. Not pushed.
- **No `docker build`**: no docker commands were run at all during this executor session.
- **No `docker compose up`**: same — no compose commands run.
- **No files touched under `src/backend/`**: `git diff --name-only HEAD~2 HEAD` output above confirms only files under `src/ui/features/pretty-view/` and `src/ui/features/pretty-conversations/` were modified.
- **No files touched under `~/.claude/identities/tina/`**: not opened, not read, not written during this executor session. The `skynet-patches.md` edit, `bounty.json` flip, and any `tina.md` edit are Tina's job as orchestrator after this executor returns.

## Branch state

- Branch: `feat/tab-title-from-tmux`
- HEAD: `5c176ad` (test rewrite commit)
- Ahead of `origin/feat/tab-title-from-tmux` by 22 unpushed commits (20 prior + Task 1 `8fa7c82` + Task 2 `5c176ad`)
- Working tree: clean (except for pre-existing untracked `.planning/quick/260727-s8g-deactivate-fleetid-purge/` and `.planning/quick/260730-mj2-sticky-scroll-strip/` which contain only PLAN.md + this SUMMARY.md — these are the current quick-task's own directory and Tina's queue directory).

## Self-Check: PASSED

- All 4 modified files exist and are on `feat/tab-title-from-tmux`:
  - `src/ui/features/pretty-view/use-auto-scroll.ts` — FOUND
  - `src/ui/features/pretty-view/use-auto-scroll.test.ts` — FOUND
  - `src/ui/features/pretty-view/PrettyView.tsx` — FOUND
  - `src/ui/features/pretty-conversations/PrettyConversationRow.tsx` — FOUND
- Both commit SHAs exist in `git log --oneline`:
  - `8fa7c82` — FOUND
  - `5c176ad` — FOUND
- All plan `<done>` grep gates pass (see task-level table above).
- All plan `<verification>` items 1–8 pass (see main verification table above).
