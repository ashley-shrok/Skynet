---
phase: quick-260811-8we
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/ChatMessage.tsx
  - src/ui/features/pretty-view/ChatMessage.autoplay.test.tsx
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-view/PrettyView.autoplay.test.tsx
autonomous: true
requirements:
  - QUICK-260811-8we-01
  - QUICK-260811-8we-02
  - QUICK-260811-8we-03
tags:
  - pretty-view
  - tts
  - autoplay
  - long-press

must_haves:
  truths:
    - "Long-pressing any assistant bubble's speak button (~500ms) both arms pane-scoped autoplay AND starts speaking that bubble"
    - "While armed, each new incoming assistant `message`-type WS frame auto-fires the same speak path (via the existing shared player) — preempting any current playback"
    - "A second long-press on any assistant speak button disarms + stops current playback"
    - "Regular tap on the speak button preserves existing play/pause/resume/preempt semantics (patches #223 + #240) byte-for-byte"
    - "Armed autoplay does NOT fire when the pane is hidden (isVisible=false)"
    - "Armed autoplay ignores image / relay_inbound / relay_outbound / malformed_line / pane_state / dormancy frames — only parsed.type==='message' with role==='assistant' qualifies"
    - "Armed state clears on session change / pane unmount / reload (per-pane, ephemeral)"
    - "Assistant speak buttons render a subtle hue-cream tint when armed; unarmed = existing chrome unchanged"
  artifacts:
    - path: "src/ui/features/pretty-view/ChatMessage.tsx"
      provides: "long-press detection on speak button + autoplayArmed/autoplayTargetEventId/onLongPressSpeak props + autoplay effect + tint class"
      contains: "onLongPressSpeak"
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "pane-scoped autoplayArmed + autoplayTargetEventId state + handleLongPressSpeak callback + dispatch-side target-set in case 'message' + reset on paneKey change + prop-drilling to ChatMessage"
      contains: "autoplayArmed"
    - path: "src/ui/features/pretty-view/ChatMessage.autoplay.test.tsx"
      provides: "long-press detection tests + autoplay effect tests + armed-tint visual test"
    - path: "src/ui/features/pretty-view/PrettyView.autoplay.test.tsx"
      provides: "dispatch-side tests: armed+visible+message sets target; armed+hidden does not; disarmed does not; relay frames don't; sequential preempt updates target"
  key_links:
    - from: "src/ui/features/pretty-view/ChatMessage.tsx"
      to: "onLongPressSpeak(eventId) callback prop"
      via: "pointerdown timer firing at ~500ms with pointermove/pointerup/pointercancel cancels"
      pattern: "onLongPressSpeak"
    - from: "src/ui/features/pretty-view/ChatMessage.tsx"
      to: "existing play-start path (postSpeakStream + createWebAudioStreamPlayer)"
      via: "extracted local startSpeak() called by BOTH tap-play-path AND autoplay effect AND long-press"
      pattern: "startSpeak"
    - from: "src/ui/features/pretty-view/PrettyView.tsx (case 'message')"
      to: "setAutoplayTargetEventId(parsed.eventId)"
      via: "guarded by autoplayArmed && isVisible && parsed.role === 'assistant', AFTER appendDedup"
      pattern: "setAutoplayTargetEventId\\(parsed\\.eventId\\)"
    - from: "src/ui/features/pretty-view/PrettyView.tsx (fresh-pane reset)"
      to: "setAutoplayArmed(false) + setAutoplayTargetEventId(null)"
      via: "paneKey-change reset block (co-located with existing resets)"
      pattern: "setAutoplayArmed\\(false\\)"
---

<objective>
Extend the existing per-bubble TTS with a pane-scoped "autoplay armed" mode.
Long-pressing any assistant bubble's speak button (~500ms) arms autoplay for
the pane AND starts speaking that bubble in a single gesture. While armed,
newly-arriving assistant `message` frames auto-fire the same speak path,
preempting any current playback. Second long-press disarms + stops.

Purpose: Ashley wants to keep her hands off the phone during long assistant
answers — arm once, then listen as new messages roll in. Preserves the tap
semantics she has muscle memory for (patches #223 + #240), and preserves the
existing preempt/pause/resume machinery in the shared web-audio player.

Output: 4 files touched (2 source + 2 colocated tests), 2 atomic commits
recommended (one per task), full frontend suite green, no npm deps, no
backend/WS-protocol/relay-bubble changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/ui/features/pretty-view/ChatMessage.tsx
@src/ui/features/pretty-view/webAudioStreamPlayer.ts
@src/ui/features/pretty-view/ChatMessage.speak.test.tsx
@src/ui/features/pretty-view/ChatMessage.test.tsx

# ChatMessage current signature (line 50-60):
#   export function ChatMessage({ role, content, identityVoice = null, ts })
#   role: "user" | "assistant"; content: string; identityVoice?: string|null; ts?: number
# The speak button (line 302-339) is only rendered when !isUser.
# onSpeakClick (line 77) is the ONLY caller of the play-start path that needs extracting.
# currentPlayer/currentOwner are MODULE-LEVEL singletons — the autoplay path
# reuses them, so cross-bubble preempt happens for free.

# PrettyView.tsx facts (already grep-verified):
#   - Single <ChatMessage> render site at line 2114 (inside virtualizer map at 2063).
#     Task brief mentions "multiple render sites" — outdated; there is ONE site.
#   - case "message" is at line 1103-1106. AFTER-appendDedup is the correct insertion point.
#   - `isVisible: boolean` is a prop (line 159) mirrored to `isVisibleRef` (line 635).
#     For the WS dispatch branch we can read the prop directly (React closure over the
#     latest onmessage handler — the WS-setup effect keys on retryKey which bumps on
#     isVisible flips, so `isVisible` in scope of onmessage is current).
#     Safer: read `isVisibleRef.current` in onmessage to match established pattern.
#   - Fresh-pane reset lives around line 971-990 (paneKey change). The existing block
#     is where autoplay state gets cleared for pane-scope ephemerality.
#   - `paneKey = ${hostId}::${tmuxSession}` at line 704 is the identity used for reset.
#   - MessageEvent shape (src/ui/api/claude-session-api.ts line 31-37):
#         { type: "message"; role: "user" | "assistant"; content: string;
#           eventId: string; ts: number }
#     eventId is guaranteed present on every message frame — safe to key autoplay off.

# --pv-id-hue CSS variable (PrettyView.tsx line 1868) is set on the outer PrettyView
# container and inherits down into every ChatMessage. Armed tint uses:
#     hsla(var(--pv-id-hue), 60%, 70%, 0.28)  -- faint hue-cream, matching identity palette
# Placed as a conditional class merged with the existing pv-speak-btn treatment;
# does NOT replace the existing background — layers a hue tint on top so the
# existing speakState visuals (Loader2/Pause/Play/Volume2) still read.

# Long-press mechanics — mirror the recently-shipped IdentityBadge onLongPress
# primitive (quick 260806-lzd, STATE.md line 58):
#     - pointerdown starts ~500ms setTimeout
#     - pointerup BEFORE timer fires = tap (existing onClick path — do NOT preventDefault
#       tap behavior — set a longPressFiredRef flag so tap can bail if it did fire)
#     - pointermove beyond ~10px cancels the timer
#     - pointercancel (iOS-safe) cancels the timer
#     - Timer firing = long-press: call onLongPressSpeak(eventId?) + START PLAY via
#       the extracted startSpeak() helper (single-gesture-single-action per Ashley's
#       locked semantics)
#     - longPressFiredRef gates the subsequent pointerup-triggered click so tap
#       does not double-fire play on top of the long-press-fire.

# What NOT to touch:
#     - webAudioStreamPlayer.ts (do not modify per task brief HARD LOCK)
#     - The existing speakState machine (idle|loading|playing|paused) — layer, don't rewrite
#     - Cross-bubble preempt at line 100 — the module-level `currentPlayer` singleton
#       already stops the previous player before starting a new one, so autoplay
#       preempt is free
#     - RelayInboundBubble/RelayOutboundBubble — separate components, never reach
#       ChatMessage, and the type gate filters them out at the dispatch level anyway
#     - Backend, WS protocol, session file, docker, deploy motion, git push
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ChatMessage long-press + autoplay effect + tint + tests</name>
  <files>
    src/ui/features/pretty-view/ChatMessage.tsx,
    src/ui/features/pretty-view/ChatMessage.autoplay.test.tsx
  </files>
  <behavior>
    LONG-PRESS DETECTION on the speak button:
      - pointerdown starts a 500ms setTimeout stored in a ref
      - pointerup BEFORE 500ms = tap: clear the timer, allow existing onClick
        to proceed (tap semantics unchanged)
      - Timer firing at 500ms = long-press:
          (a) set a longPressFiredRef flag so the subsequent (implicit) click
              triggered by pointerup does NOT also fire onSpeakClick
          (b) call props.onLongPressSpeak(eventId) — parent toggles armed state
          (c) call the extracted startSpeak() so the long-pressed bubble
              immediately begins speaking (single-gesture-single-action)
      - pointermove where hypot(dx, dy) > 10 cancels the timer
      - pointercancel cancels the timer (iOS-safe)
      - onClick handler reads longPressFiredRef: if set, clear it and return
        WITHOUT calling onSpeakClick (prevents double-fire of play on top
        of the long-press start)

    STARTSPEAK EXTRACTION:
      - Extract the lines currently at ChatMessage.tsx L98-L157 (the
        cross-bubble preempt + setSpeakState("loading") + createWebAudioStreamPlayer
        + fetch/response/setSpeakState("playing")/player.play(response) path)
        into a local function `startSpeak()` — captures `owner`, `content`,
        `identityVoice`, `containerRef` from closure exactly as today.
      - onSpeakClick's same-bubble-pause and same-bubble-resume branches at
        L83-96 stay INSIDE onSpeakClick unchanged. startSpeak is ONLY the
        fresh-play path (from "another bubble playing → stop it" onwards).
      - onSpeakClick calls startSpeak() for the fresh-play case.
      - Autoplay effect calls startSpeak() directly — bypasses the pause/resume
        checks because autoplay is always a fresh-play scenario for a
        newly-arrived message that hasn't been spoken yet.

    AUTOPLAY EFFECT (assistant bubbles only):
      - useEffect([props.autoplayTargetEventId, props.eventId])
      - Fire startSpeak() iff:
          (a) autoplayTargetEventId != null
          (b) autoplayTargetEventId === props.eventId (this bubble is the target)
          (c) role === "assistant" (defense-in-depth; user bubbles have no speak btn)
      - Fire exactly ONCE per target set: use a ref (autoplayLastFiredRef)
        that stores the last-fired eventId; only fire when the target differs
        from what we already handled. This prevents double-fire if React
        re-renders while the effect is settling.
      - Do NOT clear autoplayTargetEventId from inside ChatMessage — parent
        (PrettyView) owns that state. This effect only listens.

    ARMED TINT (visual):
      - When props.autoplayArmed === true AND !isUser, merge an additional
        class onto the speak button that adds a faint hue-cream tint:
        `pv-speak-btn-armed` mapped via inline style or a tailwind arbitrary
        variant to `background: hsla(var(--pv-id-hue), 60%, 70%, 0.28)` on
        top of the existing rgba(0,0,0,0.28) — the layered value should still
        read against theme dark bubbles. Concretely: add a conditional class
        that overrides the button's inline `background` via a
        `style` prop merge or via a `pv-speak-btn-armed` class in a new
        rule inside the existing className expression. Preserve all existing
        opacity/hover/focus/active classes.

    PROP SIGNATURE (new — additive; existing props unchanged):
      export function ChatMessage({
        role, content, identityVoice = null, ts,
        eventId,                       // NEW - REQUIRED for assistant bubbles to
                                       //   participate in autoplay; optional in
                                       //   TS to keep the user-bubble branch
                                       //   from needing a stub value in tests
                                       //   that predate this prop.
        autoplayArmed = false,         // NEW - drives tint
        autoplayTargetEventId = null,  // NEW - triggers autoplay effect
        onLongPressSpeak,              // NEW - optional callback (fires on long-press;
                                       //   parent toggles armed + set target)
      }: { role: 'user'|'assistant'; content: string;
           identityVoice?: string|null; ts?: number;
           eventId?: string;
           autoplayArmed?: boolean;
           autoplayTargetEventId?: string|null;
           onLongPressSpeak?: (eventId: string) => void; })

    TESTS (src/ui/features/pretty-view/ChatMessage.autoplay.test.tsx —
    NEW file, colocated per fork convention; mirrors ChatMessage.speak.test.tsx
    mock setup for postSpeakStream + createWebAudioStreamPlayer):

      - LP1: pointerdown then advance fake timers 500ms fires onLongPressSpeak
        exactly once with the bubble's eventId AND fires the play path (mockPlay
        called once + mockedPostSpeakStream called once).
      - LP2: pointerup BEFORE advancing 500ms fires only the tap path (existing
        onClick behavior — mockedPostSpeakStream called), does NOT fire
        onLongPressSpeak.
      - LP3: pointerdown + pointermove with dx=15 (beyond 10px threshold) +
        advance 500ms → onLongPressSpeak NOT called, mockPlay NOT called.
      - LP4: pointerdown + pointercancel + advance 500ms → onLongPressSpeak
        NOT called (iOS-safe path).
      - LP5: long-press fires + the browser-synthetic click that pointerup
        triggers is suppressed by longPressFiredRef → mockedPostSpeakStream
        called exactly ONCE (not twice — no double-fire).

      - AP1: bubble rendered with eventId="msg-A" and autoplayTargetEventId="msg-A"
        + autoplayArmed=true → mockPlay called once + mockedPostSpeakStream
        called once (autoplay effect fires).
      - AP2: bubble rendered with eventId="msg-A" and autoplayTargetEventId="msg-B"
        → NEITHER mockPlay NOR mockedPostSpeakStream called (unrelated target
        is ignored).
      - AP3: bubble rendered with eventId="msg-A" and autoplayTargetEventId="msg-A"
        AND re-rendered with the SAME target still set → mockedPostSpeakStream
        called EXACTLY ONCE (guard against double-fire on re-render).

      - TINT1: autoplayArmed={true} on an assistant bubble → the speak button
        has the armed-tint class/style (assert on className match OR computed
        style for the hue-cream signal).
      - TINT2: autoplayArmed={false} → speak button lacks the armed-tint
        signal; existing pv-speak-btn base classes still present.

      - REGRESSION: run through the equivalent of Tests 1/3/5/6 from
        ChatMessage.speak.test.tsx (assistant-only render; click fires
        postSpeakStream; pause/resume cycle; cross-bubble preempt) to prove
        the startSpeak extraction is behavior-preserving. Do NOT duplicate
        all 7 tests — 2-3 targeted regression assertions in a distinct
        describe block is enough; the full existing speak test file continues
        to run and provides comprehensive coverage.
  </behavior>
  <action>
    In src/ui/features/pretty-view/ChatMessage.tsx:

    (1) Widen the props interface with the four new additive props (eventId,
        autoplayArmed, autoplayTargetEventId, onLongPressSpeak) — all optional
        so pre-existing test call-sites that render `<ChatMessage role="..."
        content="..." />` do not break. Destructure in the function signature.

    (2) Refactor onSpeakClick: extract L98-L157 (the fresh-play path — from
        "if (currentPlayer)" cross-bubble preempt down through the fetch/play
        try-catch) into a local `async function startSpeak() { ... }` declared
        inside the component body BEFORE onSpeakClick. Preserve the closure
        capture of owner via `const owner = bubbleIdRef.current;` inside
        startSpeak. onSpeakClick keeps the same-bubble-pause and
        same-bubble-resume branches (L83-96) and calls startSpeak() at the end.
        Verify byte-for-byte behavior via mental walkthrough — no logic
        change, only relocation.

    (3) Add long-press detection state:
          const longPressTimerRef = useRef<number | null>(null);
          const longPressFiredRef = useRef<boolean>(false);
          const pointerStartRef = useRef<{x: number, y: number} | null>(null);

        Add three handlers on the speak <button>:
          onPointerDown={(e) => {
            longPressFiredRef.current = false;
            pointerStartRef.current = { x: e.clientX, y: e.clientY };
            if (longPressTimerRef.current != null) window.clearTimeout(longPressTimerRef.current);
            longPressTimerRef.current = window.setTimeout(() => {
              longPressFiredRef.current = true;
              longPressTimerRef.current = null;
              if (eventId && onLongPressSpeak) onLongPressSpeak(eventId);
              void startSpeak();
            }, 500);
          }}
          onPointerMove={(e) => {
            const start = pointerStartRef.current;
            if (!start || longPressTimerRef.current == null) return;
            const dx = e.clientX - start.x;
            const dy = e.clientY - start.y;
            if (Math.hypot(dx, dy) > 10) {
              window.clearTimeout(longPressTimerRef.current);
              longPressTimerRef.current = null;
            }
          }}
          onPointerCancel={() => {
            if (longPressTimerRef.current != null) {
              window.clearTimeout(longPressTimerRef.current);
              longPressTimerRef.current = null;
            }
          }}
          onPointerUp={() => {
            // Clear the pending timer if it hasn't fired yet — this is a tap.
            // Do NOT clear longPressFiredRef here — the subsequent onClick
            // needs to read it.
            if (longPressTimerRef.current != null) {
              window.clearTimeout(longPressTimerRef.current);
              longPressTimerRef.current = null;
            }
          }}
          onClick={(e) => {
            // Suppress the tap-driven click if a long-press already fired.
            if (longPressFiredRef.current) {
              longPressFiredRef.current = false;
              e.stopPropagation();
              return;
            }
            void onSpeakClick(e);
          }}

    (4) Add the autoplay effect BELOW the cleanup useEffect (around current
        line 75):
          const autoplayLastFiredRef = useRef<string | null>(null);
          useEffect(() => {
            if (
              !isUser &&
              autoplayTargetEventId != null &&
              eventId != null &&
              autoplayTargetEventId === eventId &&
              autoplayLastFiredRef.current !== eventId
            ) {
              autoplayLastFiredRef.current = eventId;
              void startSpeak();
            }
          }, [autoplayTargetEventId, eventId, isUser]);

        Note: startSpeak is stable across renders because it's declared inside
        the component body but closes over refs/props whose changes are
        acceptable to re-capture on each render (matches existing onSpeakClick
        pattern — no useCallback needed; the effect deps are the ONLY thing
        that gates re-firing).

    (5) Cleanup on unmount: extend the existing cleanup effect (current L67-75)
        to ALSO clear the long-press timer if pending:
          if (longPressTimerRef.current != null) {
            window.clearTimeout(longPressTimerRef.current);
            longPressTimerRef.current = null;
          }

    (6) Armed-tint on the speak button: in the className expression at line 327,
        add a conditional class merge:
          autoplayArmed &&
            "!bg-[hsla(var(--pv-id-hue),60%,70%,0.28)] border-[hsla(var(--pv-id-hue),70%,70%,0.35)]"
        The `!bg-...` uses Tailwind's important modifier to override the inline
        style `background: rgba(0,0,0,0.28)`. Alternatively, replace the inline
        `background:` in the style prop with a conditional value driven by
        autoplayArmed. Pick whichever is cleaner given the existing `style={{...}}`
        block — leaning toward switching the inline `background` conditionally
        because it avoids Tailwind-important escalation:
          background: autoplayArmed
            ? "hsla(var(--pv-id-hue),60%,70%,0.28)"
            : "rgba(0,0,0,0.28)",
          borderColor: autoplayArmed
            ? "hsla(var(--pv-id-hue),70%,70%,0.35)"
            : "rgba(255,255,255,0.10)",
        (Wrap the borderColor inside the existing `border` shorthand or add
        a discrete style key — the button uses inline `border: "1px solid ..."`
        so switch to `borderColor` + keep `border: 1px solid` implicit via
        `borderWidth: 1, borderStyle: 'solid'`.)

    Create src/ui/features/pretty-view/ChatMessage.autoplay.test.tsx: mirror
    the mock setup pattern from ChatMessage.speak.test.tsx (vi.mock for
    @/api/voice-api + ./webAudioStreamPlayer; beforeEach clears + re-installs
    default mock impls; afterEach restores). Use vi.useFakeTimers() at the
    top of the long-press describe block + vi.useRealTimers() in that
    describe's afterEach so the timer-advance tests are deterministic.
    Use fireEvent.pointerDown / pointerMove / pointerUp / pointerCancel
    (not click — pointer events are what the component handles). Structure:
      describe("ChatMessage long-press detection", ...)  // LP1-LP5
      describe("ChatMessage autoplay effect", ...)       // AP1-AP3
      describe("ChatMessage armed-tint visual", ...)     // TINT1-TINT2
      describe("ChatMessage speak regression (post-startSpeak-extract)", ...)  // 2-3 targeted

    No new npm deps. No changes to any other file in this task.
  </action>
  <verify>
    <automated>
      cd /home/ubuntu/skynet &amp;&amp; npx vitest run src/ui/features/pretty-view/ChatMessage.autoplay.test.tsx src/ui/features/pretty-view/ChatMessage.speak.test.tsx src/ui/features/pretty-view/ChatMessage.test.tsx
    </automated>
  </verify>
  <done>
    - src/ui/features/pretty-view/ChatMessage.tsx: props widened; startSpeak
      extracted; onSpeakClick pause/resume branches unchanged; long-press
      handlers wired on the speak button; longPressFiredRef gates the
      subsequent onClick; autoplay effect fires once per target; armed tint
      renders when autoplayArmed=true; unmount clears long-press timer.
    - src/ui/features/pretty-view/ChatMessage.autoplay.test.tsx: LP1-LP5,
      AP1-AP3, TINT1-TINT2, and 2-3 regression assertions all pass.
    - The existing ChatMessage.speak.test.tsx (7 tests) STILL passes without
      modification — startSpeak extraction is behavior-preserving.
    - The existing ChatMessage.test.tsx (chip/copy tests) STILL passes.
    - No new npm deps; no other file modified in this task.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PrettyView autoplay state + dispatch wiring + reset + tests</name>
  <files>
    src/ui/features/pretty-view/PrettyView.tsx,
    src/ui/features/pretty-view/PrettyView.autoplay.test.tsx
  </files>
  <behavior>
    PANE-SCOPED STATE:
      - const [autoplayArmed, setAutoplayArmed] = useState<boolean>(false);
      - const [autoplayTargetEventId, setAutoplayTargetEventId] =
          useState<string | null>(null);
      - Declared alongside the other pane-scoped useState blocks (near the top
        of PrettyView component body, e.g. next to existing `dormant` or
        `waking` state).

    LONG-PRESS CALLBACK (stable ref via useCallback):
      const handleLongPressSpeak = useCallback((longPressedEventId: string) => {
        setAutoplayArmed((currentlyArmed) => {
          if (currentlyArmed) {
            // Disarm + stop current playback + clear target.
            setAutoplayTargetEventId(null);
            // The shared player is a MODULE-LEVEL singleton inside ChatMessage
            // — stopping it requires a call the child can honor. Simplest
            // path: rely on the fact that the long-press ALSO starts speaking
            // the just-pressed bubble (single-gesture-single-action per
            // Ashley's semantics). But when DISARMING, we WANT playback
            // stopped and NO new speak. Options:
            //   (a) expose a stopAll() from ChatMessage's module (do NOT
            //       modify webAudioStreamPlayer per HARD LOCK)
            //   (b) publish a "disarm" pulse via a separate state slot
            //       (autoplayDisarmedTick counter) that ChatMessage watches
            //       via useEffect and calls its own stop path
            //   (c) accept that the second long-press starts speaking the
            //       newly-pressed bubble, which itself preempts the currently
            //       playing bubble (cross-bubble preempt at ChatMessage L100)
            //       — that IS a stop, and Ashley's semantics say the long-press
            //       gesture starts play too. So the practical effect of a
            //       disarm long-press is: preempt current playback with the
            //       just-pressed bubble's playback, then IMMEDIATELY do NOT
            //       autoplay any further new arrivals. This matches the
            //       task-brief semantics reading of "single gesture, single
            //       action" more literally than option (b).
            //
            // ADOPT (c): the long-press ALWAYS starts speaking the pressed
            // bubble (ChatMessage does that unconditionally). What we control
            // here is the ARMED flag. Toggling armed→false disarms future
            // autoplay dispatch. The currently-triggered playback (from THIS
            // long-press) is intended — Ashley long-pressed a bubble; that
            // bubble speaks. Future arrivals do NOT autoplay because armed
            // is now false.
            //
            // If Ashley later reports "second long-press should also stop
            // playback immediately," add option (b) via a new module export
            // from ChatMessage.tsx (`stopSharedPlayer()` reading its own
            // module-level currentPlayer + currentOwner) called from this
            // branch. Not needed for v1 per task-brief locked semantics.
            return false;
          } else {
            // Arm + set target to the just-long-pressed bubble.
            setAutoplayTargetEventId(longPressedEventId);
            return true;
          }
        });
      }, []);

    DISPATCH-SIDE TARGET-SET in case "message":
      case "message": {
        setMessages((prev) => appendDedup(prev, parsed));
        // Autoplay dispatch: only fire when armed + visible + assistant role.
        // Read isVisibleRef.current to match the established stale-closure
        // pattern used by the onclose retry scheduler (L1417) and the
        // visibilitychange handler (L1491) — the onmessage closure is
        // captured once per WS-setup effect run, so relying on isVisibleRef
        // is defensive against a false→true flip mid-WS-lifetime.
        if (autoplayArmedRef.current && isVisibleRef.current &&
            parsed.role === "assistant") {
          setAutoplayTargetEventId(parsed.eventId);
        }
        break;
      }

      NOTE: `autoplayArmedRef` mirrors `autoplayArmed` for stale-closure
      safety inside onmessage — mirror pattern from `isVisibleRef` at
      L1518. Add a useEffect that keeps `autoplayArmedRef.current` in sync
      with the `autoplayArmed` state after each render.

    FRESH-PANE RESET (autoplay is per-pane, ephemeral):
      In the paneKey-change reset block (~L971-990), add:
        setAutoplayArmed(false);
        setAutoplayTargetEventId(null);
        autoplayArmedRef.current = false;
      Alongside the existing state resets that fire on `hostId/tmuxSession`
      change. This satisfies "cleared on session change / pane unmount /
      reload" — pane unmount tears down the component entirely so state is
      gone naturally; paneKey change (switching to a different pane) hits
      this reset block; reload starts from a fresh mount.

    PROP DRILLING to ChatMessage (single render site at L2114):
      <ChatMessage
        role={m.role}
        content={m.content}
        identityVoice={pvIdentity?.voice ?? null}
        ts={m.ts}
        eventId={m.eventId}
        autoplayArmed={autoplayArmed}
        autoplayTargetEventId={autoplayTargetEventId}
        onLongPressSpeak={handleLongPressSpeak}
      />

    TESTS (src/ui/features/pretty-view/PrettyView.autoplay.test.tsx — NEW
    colocated file; mock the WebSocket + the parsed frames using the same
    pattern established by PrettyView.test.tsx / PrettyViewLoadingOverlay
    integration tests):

      - D1: Armed + visible + a "message" frame with role="assistant" arrives
        → the child ChatMessage sees autoplayTargetEventId === new eventId
        (assert via a mock ChatMessage that records its props, OR via the
        actual startSpeak firing under mocked postSpeakStream — mock-props
        approach is cleaner and faster).
      - D2: Armed + hidden (isVisible=false prop) + assistant "message"
        arrives → autoplayTargetEventId NOT set.
      - D3: Disarmed + visible + assistant "message" arrives →
        autoplayTargetEventId NOT set.
      - D4: Armed + visible + a "relay_inbound" frame arrives →
        autoplayTargetEventId NOT set (type gate).
      - D5: Armed + visible + a "relay_outbound" frame arrives →
        autoplayTargetEventId NOT set.
      - D6: Armed + visible + a "message" frame with role="user" arrives →
        autoplayTargetEventId NOT set (role gate).
      - D7: Sequential preempt — two "message" frames arrive back-to-back
        while armed+visible; autoplayTargetEventId ends up equal to the
        LATEST eventId.
      - D8: onLongPressSpeak called with "msg-X" while disarmed →
        autoplayArmed becomes true + autoplayTargetEventId becomes "msg-X"
        (invoked via the mock ChatMessage prop callback).
      - D9: onLongPressSpeak called again while armed → autoplayArmed becomes
        false + autoplayTargetEventId becomes null.
      - D10 (paneKey reset): change hostId+tmuxSession props → autoplayArmed
        resets to false + autoplayTargetEventId resets to null.

      Test strategy:
        - vi.mock("./ChatMessage") returning a component that renders a
          div with data-* attributes reflecting its autoplay props AND
          exposes onLongPressSpeak via a global test hook (e.g. window
          .__testLongPressFire[eventId] = onLongPressSpeak) so the test
          can invoke it. This isolates PrettyView state logic from
          ChatMessage internals — cleaner + faster than a full WebSocket
          integration test.
        - Mock the WS-setup by directly invoking the parsed-frame path,
          OR use the existing PrettyView.test.tsx WebSocket mock pattern
          (whichever exists in the fork — check PrettyView.test.tsx first
          and mirror it).
  </behavior>
  <action>
    In src/ui/features/pretty-view/PrettyView.tsx:

    (1) Add pane-scoped state slots (near existing useState declarations —
        co-locate with `dormant`/`waking` if that's where the pane-scoped
        UI state lives, otherwise near the top of the component body):
          const [autoplayArmed, setAutoplayArmed] = useState<boolean>(false);
          const [autoplayTargetEventId, setAutoplayTargetEventId] =
            useState<string | null>(null);
          const autoplayArmedRef = useRef<boolean>(false);

    (2) Add a mirror-effect (co-locate with isVisibleRef mirror at L1518):
          useEffect(() => {
            autoplayArmedRef.current = autoplayArmed;
          }, [autoplayArmed]);

    (3) Add the useCallback:
          const handleLongPressSpeak = useCallback((longPressedEventId: string) => {
            setAutoplayArmed((currentlyArmed) => {
              if (currentlyArmed) {
                setAutoplayTargetEventId(null);
                return false;
              }
              setAutoplayTargetEventId(longPressedEventId);
              return true;
            });
          }, []);

    (4) In the WS dispatch switch at case "message" (L1103), AFTER the
        existing setMessages(appendDedup) call and BEFORE break, add:
          if (
            autoplayArmedRef.current &&
            isVisibleRef.current &&
            parsed.role === "assistant"
          ) {
            setAutoplayTargetEventId(parsed.eventId);
          }

    (5) In the fresh-pane reset block (~L971-990 — the block that resets
        on hostId/tmuxSession change), add:
          setAutoplayArmed(false);
          setAutoplayTargetEventId(null);
          autoplayArmedRef.current = false;

    (6) At the single <ChatMessage> render site (L2114), thread the four
        new props:
          <ChatMessage
            role={m.role}
            content={m.content}
            identityVoice={pvIdentity?.voice ?? null}
            ts={m.ts}
            eventId={m.eventId}
            autoplayArmed={autoplayArmed}
            autoplayTargetEventId={autoplayTargetEventId}
            onLongPressSpeak={handleLongPressSpeak}
          />

    Create src/ui/features/pretty-view/PrettyView.autoplay.test.tsx:
      - vi.mock("./ChatMessage", () => ({
          ChatMessage: vi.fn((props) => {
            // Register the long-press callback in a keyed test hook
            // so tests can fire it by eventId.
            const w = window as any;
            w.__pvTestLongPress = w.__pvTestLongPress ?? new Map();
            if (props.eventId && props.onLongPressSpeak) {
              w.__pvTestLongPress.set(props.eventId, props.onLongPressSpeak);
            }
            return (
              <div
                data-testid={`chat-message-${props.eventId ?? 'noid'}`}
                data-autoplay-armed={String(props.autoplayArmed)}
                data-autoplay-target={props.autoplayTargetEventId ?? ""}
              />
            );
          }),
        }));
      - Mock the WebSocket setup by inspecting how PrettyView.test.tsx does it
        (there's a wide pattern established in the fork's existing pretty-view
        WS integration tests — mirror it). Use fake-timers if needed to advance
        past reconnect debounce.
      - Structure the describe block:
          describe("PrettyView autoplay dispatch", () => {
            // D1-D7 (dispatch matrix)
          });
          describe("PrettyView autoplay long-press toggle", () => {
            // D8-D9 (arm/disarm via long-press callback)
          });
          describe("PrettyView autoplay pane-scope reset", () => {
            // D10 (paneKey reset)
          });

    Do NOT modify any other file in this task. Do NOT touch backend, WS
    protocol, or the relay bubble components. Do NOT run `docker build`,
    do NOT push, do NOT deploy.
  </action>
  <verify>
    <automated>
      cd /home/ubuntu/skynet &amp;&amp; npx vitest run src/ui/features/pretty-view/PrettyView.autoplay.test.tsx &amp;&amp; npx vitest run
    </automated>
  </verify>
  <done>
    - src/ui/features/pretty-view/PrettyView.tsx: autoplayArmed + autoplayTargetEventId
      state added; autoplayArmedRef mirror maintained; handleLongPressSpeak
      useCallback stable-ref; dispatch-side target-set in case "message"
      guarded by armed+visible+assistant; fresh-pane reset clears all three;
      single ChatMessage render site threads the four new props.
    - src/ui/features/pretty-view/PrettyView.autoplay.test.tsx: D1-D10 all pass.
    - Full frontend suite green: `npx vitest run` exits 0, ZERO regressions
      vs the pre-plan baseline (1847 tests pass / 7 skip / 1 todo / 0 fail
      per STATE.md line 30). Expected new count: baseline + (LP1-LP5 +
      AP1-AP3 + TINT1-TINT2 + ~3 regression + D1-D10) ≈ baseline + ~23
      new tests, still 0 fail.
    - No worktrees (main ~/skynet tree, branch feat/tab-title-from-tmux).
    - No npm deps added.
    - No backend / WS-protocol / relay-bubble changes.
    - NO deploy / NO docker build / NO git push / NO patch entry.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user pointer input → speak button | Ashley's touch/click reaches the long-press timer; benign — no external input |
| WS frame → autoplay dispatch | Backend-emitted `type:"message"` frame with `role`/`eventId` fields triggers autoplay; the WS is authenticated + connection-scoped (patch #148/#344 posture unchanged) |
| autoplay effect → shared player | ChatMessage-internal call to startSpeak reuses the existing postSpeakStream + createWebAudioStreamPlayer path — same trust surface as manual tap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-QUICK-8we-01 | Spoofing | WS "message" frame with role="assistant" | accept | Backend is the sole authoritative emitter of role field (from JSONL); no client-supplied role trust; same posture as existing message dispatch |
| T-QUICK-8we-02 | Tampering | autoplayArmedRef mirror | accept | Ref is component-local, not exposed to any external surface; only set via useEffect on state change |
| T-QUICK-8we-03 | Information Disclosure | speak.tts request payload | accept | Content of assistant message → TTS backend; SAME surface as existing manual-tap speak (patch #223); no widened exposure |
| T-QUICK-8we-04 | Denial of Service | autoplay firing on every message frame | mitigate | isVisible gate (hidden panes have WS closed anyway per patch #344; the ref gate is defense-in-depth); role gate blocks user-echo self-fire loop; single-fire guard (autoplayLastFiredRef) blocks duplicate-fire per target |
| T-QUICK-8we-05 | Elevation of Privilege | long-press bypasses tap semantics | accept | Long-press is a NEW authorized gesture per Ashley 2026-08-11 locked semantics; adds capability, does not remove any protection |
| T-QUICK-8we-SC | Tampering | npm/pip/cargo installs | mitigate | **N/A — no new npm dependencies added by this plan.** package.json byte-untouched. Package legitimacy gate not triggered. |
</threat_model>

<verification>
  Whole-suite gate (fleet rule: never leave tests failing, 2026-08-05):
    cd /home/ubuntu/skynet && npx vitest run

  Expected outcome:
    - 0 failing tests (baseline 1847 pass / 7 skip / 1 todo / 0 fail per
      STATE.md — plus ~23 new tests from this plan, all passing).
    - Zero regressions in ChatMessage.speak.test.tsx (7 tests) — proves
      the startSpeak extraction is behavior-preserving.
    - Zero regressions in ChatMessage.test.tsx (chip/copy tests).
    - Zero regressions in PrettyView.test.tsx / PrettyViewLoadingOverlay
      test suites.

  Byte-checks (grep gates, all `grep -v '^#'` filtered so header prose
  can't self-invalidate):
    - `grep -v '^#' src/ui/features/pretty-view/ChatMessage.tsx | grep -c 'onLongPressSpeak'` → >= 3 (prop, handler call, tests reference)
    - `grep -v '^#' src/ui/features/pretty-view/ChatMessage.tsx | grep -c 'startSpeak'` → >= 3 (definition + onSpeakClick call + autoplay effect call + long-press call)
    - `grep -v '^#' src/ui/features/pretty-view/PrettyView.tsx | grep -c 'autoplayArmedRef'` → >= 3 (declare + mirror + dispatch guard + reset)
    - `grep -v '^#' src/ui/features/pretty-view/PrettyView.tsx | grep -c 'setAutoplayTargetEventId'` → >= 3 (dispatch + reset + long-press callback)
    - `git diff --stat src/ui/features/pretty-view/webAudioStreamPlayer.ts` → empty output (HARD LOCK: NOT modified)
    - `git diff --stat package.json package-lock.json` → empty output (NO new deps)
    - `git diff --stat src/backend/` → empty output (frontend-only change)
    - `git diff --stat src/ui/features/pretty-view/RelayInboundBubble.tsx src/ui/features/pretty-view/RelayOutboundBubble.tsx` → empty output (relay bubbles untouched)

  TypeScript gate:
    cd /home/ubuntu/skynet && npm run build

  Expected: exit 0. No TS errors from the new props.
</verification>

<success_criteria>
  Code-execution complete when ALL of these hold:

  (1) Ashley can long-press any assistant bubble's speak button in the
      PrettyView pane and observe:
        (a) that bubble starts speaking immediately (existing speak path)
        (b) the speak button on ALL assistant bubbles in the pane picks up
            a subtle hue-cream tint (armed visual affordance)
        (c) next assistant message frame that arrives while the pane is
            visible auto-fires speak (preempting the currently-speaking
            bubble via the existing cross-bubble preempt)

  (2) Long-pressing again on ANY assistant speak button disarms:
        (a) tint disappears on all assistant speak buttons
        (b) subsequent message frames do NOT auto-fire speak
        (c) the just-long-pressed bubble starts speaking (semantically:
            single-gesture-single-action; the disarm implicitly preempts
            the previously-speaking bubble because a NEW bubble started)

  (3) Regular tap on any speak button behaves EXACTLY as pre-plan:
        (a) idle → play; playing → pause; paused → resume;
            cross-bubble → preempt (all 7 ChatMessage.speak.test.tsx
            tests continue to pass without modification)

  (4) Autoplay is per-pane and ephemeral:
        (a) switching to a different pane (paneKey change) clears armed
            + target
        (b) reload clears (fresh mount)
        (c) hidden panes cannot fire autoplay (isVisibleRef gate; also
            WS is paused per patch #344 so no frames arrive anyway)

  (5) Autoplay only fires for `parsed.type==='message'` with
      `role==='assistant'`. Image / relay_inbound / relay_outbound /
      malformed_line / pane_state / dormancy / context_pct frames are
      all ignored.

  (6) Full frontend suite green: `npx vitest run` = 0 failing
      (baseline + ~23 new tests all passing).

  (7) TypeScript build clean: `npm run build` exit 0.

  (8) Zero-diff on hands-off surfaces:
        - webAudioStreamPlayer.ts (HARD LOCK)
        - package.json / package-lock.json (no new deps)
        - src/backend/ (frontend-only)
        - RelayInboundBubble.tsx / RelayOutboundBubble.tsx (excluded)

  (9) Two atomic commits on feat/tab-title-from-tmux (main ~/skynet tree,
      NO worktrees per fleet rule 2026-07-31):
        - Commit A (Task 1): ChatMessage long-press + startSpeak extract
          + autoplay effect + tint + colocated tests
        - Commit B (Task 2): PrettyView state + dispatch wiring + reset
          + prop threading + colocated tests

  (10) NOT pushed / NOT built / NOT deployed / NO patch entry — orchestrator
       handles the deploy motion after Ashley greenlight per box-maintainer
       fleet rule (Ashley 2026-08-08 verbatim: "sub-agents should not do
       deploys").
</success_criteria>

<output>
  Create .planning/quick/260811-8we-prettyview-auto-play-new-assistant-messa/260811-8we-SUMMARY.md
  when the plan is executed and both tasks are done. The summary should record:
    - Files modified (exact paths + line ranges of the notable insertions)
    - Test counts (baseline vs post — expect +~23)
    - Commit hashes (post-any-rebase)
    - Any deviations from this plan and why
    - Explicit statement of NOT-pushed / NOT-built / NOT-deployed status
    - Bounty tag: prettyview-auto-play-new-assistant-messages
</output>
