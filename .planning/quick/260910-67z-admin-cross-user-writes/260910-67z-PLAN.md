---
phase: quick-260910-67z
plan: "01"
type: execute
wave: 1
depends_on: [quick-260910-439]
files_modified:
  - src/backend/utils/simple-db-ops.ts
  - src/backend/utils/simple-db-ops.test.ts
  - src/backend/database/routes/host.ts
  - src/backend/database/routes/host.test.ts
autonomous: false
requirements: [SKYNET-ADMIN-CROSS-USER-WRITE]
tags: [admin, authz, host, credentials, cross-user, encryption, simple-db-ops]
must_haves:
  truths:
    - "Admin cross-user PUT of non-sensitive fields on rows 21/22/23 returns 200 against LIVE term.example.com"
    - "Admin cross-user POST of non-sensitive fields creates a row under target user and returns 201-shaped success"
    - "SimpleDBOps.updateNonSensitive and insertNonSensitive skip DataCrypto.validateUserAccess but still fire DatabaseSaveTrigger.triggerSave"
    - "Passing a sensitive field to updateNonSensitive or insertNonSensitive throws with a directive error naming the offending field"
    - "Same-user PUT/POST paths still route through SimpleDBOps.update/insert (encryption preserved for own writes)"
    - "After PUT of rows 21/22/23, the 'Unsupported authType.*fleetHostId:(21|22|23)' log noise stops (grep -c == 0 within 60s)"
  artifacts:
    - path: "src/backend/utils/simple-db-ops.ts"
      provides: "updateNonSensitive + insertNonSensitive exports on the SimpleDBOps class"
      contains: "updateNonSensitive"
    - path: "src/backend/utils/simple-db-ops.test.ts"
      provides: "Unit tests for the two new methods (8 tests total: 4 per method)"
      contains: "updateNonSensitive_throws_on_sensitive_field"
    - path: "src/backend/database/routes/host.ts"
      provides: "PUT + POST cross-user branches routed through updateNonSensitive/insertNonSensitive"
      contains: "SimpleDBOps.updateNonSensitive"
    - path: "src/backend/database/routes/host.test.ts"
      provides: "Regression tests A7-A10 extending the A1-A6 suite"
      contains: "A7"
    - path: ".planning/quick/260910-67z-admin-cross-user-writes/260910-67z-SUMMARY.md"
      provides: "Executor summary including verbatim smoke-test capture for rows 21/22/23"
  key_links:
    - from: "src/backend/database/routes/host.ts (PUT cross-user branch, ~L1247)"
      to: "SimpleDBOps.updateNonSensitive"
      via: "direct call replacing SimpleDBOps.update when isAdminForUpdate && effectiveUpdateUserId !== userId"
      pattern: "SimpleDBOps\\.updateNonSensitive"
    - from: "src/backend/database/routes/host.ts (POST cross-user branch, ~L466)"
      to: "SimpleDBOps.insertNonSensitive"
      via: "direct call replacing SimpleDBOps.insert when effectiveUserId !== userId"
      pattern: "SimpleDBOps\\.insertNonSensitive"
    - from: "src/backend/utils/simple-db-ops.ts (both new methods)"
      to: "DatabaseSaveTrigger.triggerSave"
      via: "post-write side-effect"
      pattern: "triggerSave\\(`(update|insert)_nonsensitive_"
---

<objective>
Extend `SimpleDBOps` with `updateNonSensitive` and `insertNonSensitive` variants that
skip `DataCrypto.validateUserAccess` (target user's data key is not required for
cleartext-at-rest metadata fields like `credentialId`, `authType`, `runsFleetSubstrate`,
`ip`, `port`, `name`) while still firing `DatabaseSaveTrigger.triggerSave`. Wire them into
the admin cross-user branches of `POST /db/host` and `PUT /db/host/:id` in `host.ts` so
Alice can actually migrate rows 21/22/23 to shared credentials via the live API — closing
the write-half of the `skynet-admin-cross-user-host-visibility` bounty that quick 260910-439
left with a "target-user-not-logged-in → 400" wall in the way.

Purpose: The predecessor plan (260910-439) shipped the admin bypass gate and the
sensitive-field-block guard, but the actual `SimpleDBOps.insert/update` calls still demand
the target user's cached data key via `validateUserAccess`. Since Alice is the only admin
and joe/zoey aren't always logged in, every cross-user PUT currently 400s. The new methods
give the admin path an explicit "cleartext-only" DB write that mirrors the existing
`SimpleDBOps.delete` shape (which also skips validation) and closes the loop.

Output:
  - Two new SimpleDBOps static methods with unit-tested guards + triggerSave fires
  - Cross-user POST/PUT branches in host.ts rewired to use them
  - A7-A10 regression tests in host.test.ts extending A1-A6
  - Verbatim smoke-test capture in SUMMARY.md showing rows 21/22/23 accepted via LIVE PUT
    to term.example.com and the "Unsupported authType" log noise silenced
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/quick/260910-439-admin-cross-user-host/260910-439-PLAN.md
@.planning/quick/260910-439-admin-cross-user-host/260910-439-SUMMARY.md
@src/backend/utils/simple-db-ops.ts
@src/backend/utils/lazy-field-encryption.ts
@src/backend/database/routes/host.ts
@src/backend/database/routes/host.test.ts
</context>

<interface_context>
## SimpleDBOps — existing shapes (reference)

Current class exports (from `src/backend/utils/simple-db-ops.ts`):

- `insert<T>(table, tableName, data, userId): Promise<T>` — L14-65
  - Calls `DataCrypto.validateUserAccess(userId)` FIRST (throws if target not logged in)
  - Encrypts fields via `DataCrypto.encryptRecord`
  - System-key dual-encrypts `ssh_credentials` rows
  - `getDb().insert(table).values(encryptedData).returning()`
  - `DatabaseSaveTrigger.triggerSave(\`insert_${tableName}\`)`
  - Returns decrypted single row

- `update<T>(table, tableName, where, data, userId): Promise<T[]>` — L112-158
  - Same validateUserAccess-first pattern
  - `getDb().update(table).set(encryptedData).where(where).returning()`
  - `triggerSave(\`update_${tableName}\`)`
  - Returns decrypted rows

- `delete(table, tableName, where): Promise<unknown[]>` — L160-173
  - **NO userId arg, NO validateUserAccess** — this is the shape template for the new methods
  - `getDb().delete(table).where(where).returning()`
  - `triggerSave(\`delete_${tableName}\`)`
  - Returns raw rows

- `TableName` union: `"users" | "ssh_data" | "ssh_credentials" | "recent_activity" | "socks5_proxy_presets"`

## Sensitive-field union — authoritative source

`src/backend/utils/lazy-field-encryption.ts:301-327` — `LazyFieldEncryption.getSensitiveFieldsForTable(tableName)`
returns the per-table sensitive list. For our two use cases:

- `ssh_data`: `["password", "key", "keyPassword", "sudoPassword", "autostartPassword", "autostartKey", "autostartKeyPassword", "socks5Password", "rdpPassword", "vncPassword", "telnetPassword"]`
- `ssh_credentials`: `["password", "key", "keyPassword", "privateKey", "publicKey"]`
- `users`: `["totpSecret", "totpBackupCodes"]`

**Design decision (executor: implement exactly this):** DO NOT import `LazyFieldEncryption`
into `simple-db-ops.ts` — it would introduce a circular-ish dependency shape (both live in
`utils/`) and the sensitive-field set is small + stable enough to hard-list. Define a
module-private `const SENSITIVE_FIELDS_BY_TABLE: Record<TableName, ReadonlySet<string>>`
mirroring `getSensitiveFieldsForTable`'s output. Add a comment linking to
`lazy-field-encryption.ts` as the source-of-truth so future edits stay in sync.

## host.ts — cross-user branch anchors (line numbers as of HEAD 49b1428e)

**POST /db/host — L122+ handler, L275-304 admin gate, L466-491 insert call:**
```
let effectiveUserId = userId;                          // L275
if (targetUserId && ... targetUserId !== userId) {     // L276
  const isAdminForCreate = await callerIsAdmin(userId);
  if (!isAdminForCreate) return 403;
  // sensitive-field block → 400 if any sensitive field in body
  effectiveUserId = targetUserId;                      // L303
}
...
result = await SimpleDBOps.insert(                     // L466
  hosts, "ssh_data", sshDataObj, effectiveUserId,      // L470 uses effectiveUserId
);
```

**PUT /db/host/:id — L805+ handler, L1102-1131 admin gate, L1247-1275 update call:**
```
const isAdminForUpdate = await callerIsAdmin(userId);  // L1104
let effectiveUpdateUserId = userId;
if (targetUserId && ...) {                             // L1107
  if (!isAdminForUpdate) return 403;
  // sensitive-field-block same shape
  effectiveUpdateUserId = targetUserId;                // L1131
}
...
if (isAdminForUpdate && effectiveUpdateUserId !== userId) {
  sshDataObj.userId = effectiveUpdateUserId;           // L1239 re-assigns ownership column
}
const encryptKeyUserId = effectiveUpdateUserId !== userId
  ? effectiveUpdateUserId : ownerId;                   // L1245
await SimpleDBOps.update(                              // L1248
  hosts, "ssh_data", eq(hosts.id, Number(hostId)),
  sshDataObj, encryptKeyUserId,
);
```

**Branch decision the executor must implement:**

For BOTH POST and PUT, split into two paths:
1. **Cross-user (effectiveUserId/effectiveUpdateUserId !== userId AND isAdmin):**
   Use `SimpleDBOps.insertNonSensitive` / `SimpleDBOps.updateNonSensitive` — no userId arg,
   no target-user-key needed. The 400 "target not logged in" try/catch becomes unreachable
   in this branch (the new methods can't throw that error) but leave the outer try/catch in
   place — it's a defense-in-depth safety net for the same-user path.
2. **Same-user (target absent or equals caller):** Keep the existing `SimpleDBOps.insert`/
   `SimpleDBOps.update` calls untouched. Encryption for own writes must still work.

The sensitive-field-block guard from commit 298845b2 (POST L286-302, PUT similar) already
returns 400 BEFORE reaching the write, so `insertNonSensitive`/`updateNonSensitive`'s
internal throw is a defense-in-depth net that should not fire on well-formed calls.

## host.test.ts — mock scaffolding pattern (reference)

- Full mock setup for db, SimpleDBOps, auth, DataCrypto: L25-165
- Handler extraction via router-stack walk: L238-282
- `beforeEach` resets: L288-330 — restores SimpleDBOps mock defaults AND db.select chain
- A1-A6 tests: L956-1168 — follow this exact shape for A7-A10
- `mockDbSelectForAdmin(isAdmin, hostOwnerUserId)` helper: L924-954 — reuse for A7/A10

**Critical mock note:** `vi.clearAllMocks()` in beforeEach does NOT drain the
`mockReturnValueOnce` queue. If a test sets `mockReturnValueOnce` and the code path
doesn't consume it, the queued value leaks into the next test. See the A1 redesign note in
260910-439-SUMMARY.md § "Deviations from Plan" — always confirm the code path actually
consumes each `mockReturnValueOnce`.
</interface_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SimpleDBOps.updateNonSensitive + insertNonSensitive + unit tests</name>
  <files>src/backend/utils/simple-db-ops.ts, src/backend/utils/simple-db-ops.test.ts</files>
  <behavior>
    - updateNonSensitive_writes_non_sensitive_fields: calling with `{ credentialId: 42, authType: "credential" }` invokes `getDb().update(table).set(data).where(where).returning()` and returns the raw returned rows unmodified.
    - updateNonSensitive_throws_on_sensitive_field_in_data: passing `{ password: "x" }` for tableName "ssh_data" throws `Error` whose message contains "updateNonSensitive called with sensitive field: password" AND does NOT reach `getDb().update(...)`.
    - updateNonSensitive_throws_lists_all_sensitive_fields_for_credentials_table: passing `{ privateKey: "y" }` for tableName "ssh_credentials" throws with "privateKey" in message (verifies per-table sensitive list is honored).
    - updateNonSensitive_calls_triggerSave: after a successful call for "ssh_data", `DatabaseSaveTrigger.triggerSave` is called exactly once with the string `"update_nonsensitive_ssh_data"`.
    - updateNonSensitive_does_NOT_call_validateUserAccess: spy on `DataCrypto.validateUserAccess`; assert `.mock.calls.length === 0` after a successful call.
    - Same 5 tests for insertNonSensitive using `getDb().insert(table).values(data).returning()` and trigger string `"insert_nonsensitive_ssh_data"`.
  </behavior>
  <action>
    RED first: create `src/backend/utils/simple-db-ops.test.ts` with the 10 tests above (5 per method) following the vitest patterns from `field-crypto.test.ts` and the mock shape from `host.test.ts` L25-107. Mock `../database/db/index.js` to expose `getDb()` returning a stub with `.update()/.insert()` builders whose `.set()/.values().where().returning()` chains resolve to a controllable array, plus `DatabaseSaveTrigger: { triggerSave: vi.fn() }`. Mock `./data-crypto.js` to expose `DataCrypto: { validateUserAccess: vi.fn() }` so the spy assertion is direct. Run the file; expect all 10 to fail with "updateNonSensitive is not a function" / "insertNonSensitive is not a function".

    GREEN: add both methods to `SimpleDBOps` in `src/backend/utils/simple-db-ops.ts` immediately after the existing `delete` method (L173). Signatures:

    - `static async updateNonSensitive<T extends Record<string, unknown>>(table: SQLiteTable, tableName: TableName, where: unknown, data: Partial<T>): Promise<T[]>`
    - `static async insertNonSensitive<T extends Record<string, unknown>>(table: SQLiteTable, tableName: TableName, data: T): Promise<T[]>` — note plural return (returning() gives array; leave the caller to `.at(0)` or index if it wants a single row, matching `delete`'s raw-array shape)

    Body pattern (identical for both, differing only in the drizzle verb):

    1. Define a module-private const at the top of the file (below imports, above the class):
       ```
       const SENSITIVE_FIELDS_BY_TABLE: Record<TableName, ReadonlySet<string>> = {
         users: new Set(["totpSecret", "totpBackupCodes"]),
         ssh_data: new Set(["password", "key", "keyPassword", "sudoPassword",
           "autostartPassword", "autostartKey", "autostartKeyPassword",
           "socks5Password", "rdpPassword", "vncPassword", "telnetPassword"]),
         ssh_credentials: new Set(["password", "key", "keyPassword",
           "privateKey", "publicKey"]),
         recent_activity: new Set(),
         socks5_proxy_presets: new Set(),
       };
       ```
       Add a comment above it linking to `lazy-field-encryption.ts:301-327` as authoritative
       source and noting: "keep in sync manually — no import to avoid utils/utils cycle".
    2. Guard loop: `for (const key of Object.keys(data)) { if (SENSITIVE_FIELDS_BY_TABLE[tableName]?.has(key)) throw new Error(\`updateNonSensitive called with sensitive field: ${key} — use SimpleDBOps.update instead\`); }` (adjust method name in the message per method).
    3. Raw drizzle: `const result = await getDb().update(table).set(data).where(where as SQL | undefined).returning();` (or `.insert(table).values(data).returning();` for insert).
    4. `DatabaseSaveTrigger.triggerSave(\`update_nonsensitive_${tableName}\`);` (or `insert_nonsensitive_${tableName}`).
    5. `return result as T[];` — NO decryption, NO validateUserAccess.

    Re-run tests; expect all 10 green.

    REFACTOR (only if the two method bodies are copy-paste): consider extracting the guard
    loop into a private static helper `assertNoSensitiveFields(tableName, data, methodName)`.
    Skip refactor if the diff isn't clearly cleaner.

    Do NOT modify the existing `insert`/`update`/`select`/`delete` methods.

    Commit atomically as Task 1: `feat(260910-67z-T1): SimpleDBOps.updateNonSensitive + insertNonSensitive with sensitive-field guard`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina && npx vitest run src/backend/utils/simple-db-ops.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - simple-db-ops.test.ts exists with 10 tests, all passing
    - simple-db-ops.ts exports SimpleDBOps.updateNonSensitive and SimpleDBOps.insertNonSensitive
    - Guard throws with directive error including the offending field name
    - triggerSave fires with "update_nonsensitive_${tableName}" / "insert_nonsensitive_${tableName}"
    - validateUserAccess is NOT called by either method (spy asserts 0 calls)
    - `npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "src/backend/utils/simple-db-ops(\.test)?\.ts"` returns empty (clean)
    - Atomic commit landed with T1 message
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire updateNonSensitive/insertNonSensitive into host.ts cross-user branches</name>
  <files>src/backend/database/routes/host.ts, src/backend/database/routes/host.test.ts</files>
  <behavior>
    - A7: admin cross-user PUT with only non-sensitive fields (targetUserId="other-user", credentialId=42, authType="credential", no password/key) → 200. Assert `SimpleDBOps.updateNonSensitive` was called exactly once. Assert `SimpleDBOps.update` was NOT called.
    - A8: admin cross-user POST with only non-sensitive fields (targetUserId="other-user", credentialId=42) → 200 (existing handler uses `res.json(host)` not `res.status(201)` — do not invent a 201 shape). Assert `SimpleDBOps.insertNonSensitive` was called exactly once with first three args (table, "ssh_data", sshDataObj-with-userId="other-user"). Assert `SimpleDBOps.insert` was NOT called.
    - A9: admin same-user PUT (isAdminForUpdate:true, no targetUserId, isOwner:true) still routes through `SimpleDBOps.update` → 200. Assert `SimpleDBOps.update` called once, `SimpleDBOps.updateNonSensitive` NOT called. This is the "same-user path preserved" regression guard.
    - A10: admin cross-user PUT succeeds when target's data key is not cached — mock `DataCrypto.getUserDataKey` to return `null` for the target userId; PUT still returns 200 because updateNonSensitive skips validation. Assert `SimpleDBOps.updateNonSensitive` was called, no 400/403.
  </behavior>
  <action>
    RED first: append A7-A10 tests to `src/backend/database/routes/host.test.ts` in a new
    `describe("Admin cross-user WRITE extensions (A7-A10) — quick 260910-67z", () => { ... })`
    block after the existing A1-A6 describe (L956). Extend the `vi.mock("../../utils/simple-db-ops.js", ...)` at L45-51 to also expose `updateNonSensitive: vi.fn(async () => [{...}])` and `insertNonSensitive: vi.fn(async () => [{...}])`. Update the `beforeEach` at L288-330 to reset both new mocks with the same default row shape as `update`/`insert`. Reuse `mockDbSelectForAdmin(true, "other-user")` helper for A7 and A10. Run the file; expect A7-A10 to fail because the handler still calls the old methods.

    GREEN — POST /db/host wiring (around L466-491):
    Change:
    ```
    result = await SimpleDBOps.insert(hosts, "ssh_data", sshDataObj, effectiveUserId);
    ```
    to a branch:
    ```
    if (effectiveUserId !== userId) {
      // Admin cross-user path: cleartext-at-rest metadata only.
      // The sensitive-field-block guard at L289 already rejected inline creds.
      const inserted = await SimpleDBOps.insertNonSensitive(hosts, "ssh_data", sshDataObj);
      result = inserted[0];
    } else {
      result = await SimpleDBOps.insert(hosts, "ssh_data", sshDataObj, effectiveUserId);
    }
    ```
    Leave the outer `try/catch` for "not logged in" errors in place — it still guards the
    same-user path. Do NOT change the surrounding audit-log or `resolveHostCredentials` code.

    GREEN — PUT /db/host/:id wiring (around L1245-1254):
    Keep the existing `sshDataObj.userId = effectiveUpdateUserId` re-assignment (L1238-1240)
    and the `encryptKeyUserId` computation (L1245) unchanged for the same-user branch.
    Replace the single update call with a branch:
    ```
    if (isAdminForUpdate && effectiveUpdateUserId !== userId) {
      // Admin cross-user path: skip target-user data key validation.
      await SimpleDBOps.updateNonSensitive(
        hosts,
        "ssh_data",
        eq(hosts.id, Number(hostId)),
        sshDataObj,
      );
    } else {
      await SimpleDBOps.update(
        hosts,
        "ssh_data",
        eq(hosts.id, Number(hostId)),
        sshDataObj,
        encryptKeyUserId,
      );
    }
    ```
    The outer try/catch for validateUserAccess errors stays — it's a safety net now
    unreachable from the cross-user branch but still valid for the same-user branch.

    Confirm the sensitive-field-block guards from commit 298845b2 (POST L286-302 and its
    PUT twin) are UNCHANGED — they still return 400 before reaching the write. The new
    methods' internal throw is defense-in-depth, not the primary gate.

    Re-run host.test.ts. All 31 tests (P1-P6, U1-U5, T1-T10, A1-A6, A7-A10) must pass. If
    A1-A6 fail, an accidental change to the same-user path leaked in — revert and re-scope.

    Commit atomically as Task 2: `feat(260910-67z-T2): route host.ts admin cross-user POST+PUT through insertNonSensitive/updateNonSensitive + A7-A10 regression tests`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina && npx vitest run src/backend/database/routes/host.test.ts --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>
    - host.ts POST /db/host cross-user branch calls SimpleDBOps.insertNonSensitive
    - host.ts PUT /db/host/:id cross-user branch calls SimpleDBOps.updateNonSensitive
    - Same-user paths for BOTH endpoints unchanged (still call SimpleDBOps.insert/update)
    - Sensitive-field-block guard from 298845b2 unchanged and still fires 400 first
    - host.test.ts extended with A7-A10 in a new describe block
    - All 31 tests pass (P1-P6 + U1-U5 + T1-T10 + A1-A6 + A7-A10)
    - `npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "src/backend/(database/routes/host|utils/simple-db-ops)"` returns empty
    - Atomic commit landed with T2 message
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: LIVE smoke test — cross-user PUT of rows 21/22/23 against term.example.com</name>
  <what-built>
    Backend changes are committed but NOT yet deployed. The smoke test is definitional:
    the plan's success criterion is that Alice can migrate rows 21/22/23 to shared
    credentials via the LIVE Skynet API without hand-patching the DB. Alice's 2026-09-10
    fleet rule: use Skynet's own newly-fixed endpoint, not sqlite hand-edits.

    This task requires the orchestrator or Alice to run the deploy cycle first, then
    the executor captures the smoke result verbatim.
  </what-built>
  <how-to-verify>
    ## Step A — Deploy the two commits from T1+T2 to production

    On the EC2 (via SSM if needed):
    ```
    cd /path/to/skynet && sudo docker compose build skynet && \
      sudo docker compose up -d --no-deps skynet
    ```
    Wait ~15s for the container to come up. Confirm:
    ```
    sudo docker ps --format '{{.Names}} {{.Status}}' | grep skynet
    ```

    ## Step B — Verify session cookie

    ```
    curl -sS -b /tmp/sk-cj https://term.example.com/users/me | jq -r .username
    ```
    Expected: `alice`. If not `alice`, re-login via
    `~/.claude/roles/box-maintainer/runbooks/skynet-admin/skynet-admin.md § Get an admin cookie`
    (creds at `~/.claude/roles/box-maintainer/skynet-creds.json`, TOTP dance).

    Also confirm admin+data_unlocked:
    ```
    curl -sS -b /tmp/sk-cj https://term.example.com/users/me | jq '{isAdmin, data_unlocked}'
    ```

    ## Step C — Fetch + PUT each row via admin cross-user endpoint

    For each of rows 21/22/23, the pattern is:
    1. `GET /host/db/host/${id}/export` as admin (admin READ bypass from quick 260910-439 is already live) — capture the JSON body.
    2. Strip any sensitive fields from the body if the export includes them (`password`, `key`, `keyPassword`, `sudoPassword`, `autostartPassword`, `autostartKey`, `autostartKeyPassword`, `socks5Password`, `rdpPassword`, `vncPassword`, `telnetPassword` set to null or removed).
    3. Add `targetUserId` and `credentialId` per the row map below.
    4. `PUT /host/db/host/${id}` with the modified body. Expect 200.

    Row map:
    | Row | targetUserId                | credentialId | Credential name       |
    |-----|-----------------------------|--------------|-----------------------|
    | 21  | T4C1p6jcQgUZT9y6o3xGo       | 2            | workstation-ssh       |
    | 22  | pcW9dfHqIw8aNU8k_Iz5A       | 5            | thenasty-ssh          |
    | 23  | pcW9dfHqIw8aNU8k_Iz5A       | 3            | ZoeyBattlestation-ssh |

    Reference shell (adapt for each row):
    ```
    ID=21; TARGET=T4C1p6jcQgUZT9y6o3xGo; CRED=2
    BODY=$(curl -sS -b /tmp/sk-cj "https://term.example.com/host/db/host/${ID}/export" | \
      jq --arg t "$TARGET" --argjson c "$CRED" '
        del(.password, .key, .keyPassword, .sudoPassword, .autostartPassword,
            .autostartKey, .autostartKeyPassword, .socks5Password, .rdpPassword,
            .vncPassword, .telnetPassword)
        | .targetUserId = $t | .credentialId = $c | .runsFleetSubstrate = true
      ')
    curl -sS -b /tmp/sk-cj -X PUT -H 'Content-Type: application/json' \
      -d "$BODY" "https://term.example.com/host/db/host/${ID}" | jq '{id, userId, credentialId, authType}'
    ```
    Capture the response verbatim for each of 21/22/23. All three MUST show HTTP 200 (jq
    output parses without error) AND the response body MUST reflect `credentialId` set to
    the expected value.

    ## Step D — Post-PUT read-back via admin cross-user GET

    For each row:
    ```
    curl -sS -b /tmp/sk-cj "https://term.example.com/host/db/host/${ID}" | jq '{id, userId, credentialId, authType, runsFleetSubstrate}'
    ```
    Expected: `credentialId` equals the value from the row map; `userId` unchanged
    (still the target user); `runsFleetSubstrate` = true.

    ## Step E — Log noise silenced

    ```
    sudo docker logs skynet --since 60s 2>&1 | grep -cE "Unsupported authType.*fleetHostId:(21|22|23)"
    ```
    Expected: `0`.

    ## Step F — Capture in SUMMARY.md

    Paste the verbatim outputs of Steps B, C (all 3 rows), D (all 3 rows), and E into
    `.planning/quick/260910-67z-admin-cross-user-writes/260910-67z-SUMMARY.md` under a
    `## LIVE Smoke Test` section. Do NOT paraphrase — copy the terminal output as-is.

    ## Step G — Third atomic commit

    After SUMMARY.md is written, commit ONLY the summary + any test fixtures (if any):
    ```
    git commit -m "docs(260910-67z-T3): capture LIVE smoke test — rows 21/22/23 cross-user PUT successful"
    ```
    (Per constraint: do NOT commit .planning/ docs from executor; the orchestrator handles
    the docs commit in its Step 8. If executor is running under the orchestrator, write
    SUMMARY.md but skip the commit — orchestrator will bundle.)

    ## Multi-identity role guardrail

    Before ANY `git push`: `git pull --rebase origin feat/tab-title-from-tmux`.
  </how-to-verify>
  <resume-signal>
    Type "approved: rows 21/22/23 all 200 + log noise silenced" to accept and close the plan.
    Type "issues: <describe>" if any row fails; the executor will diagnose and iterate.
    Type "defer: <reason>" ONLY if deploy is blocked (e.g., not the operator's session);
    executor will mark smoke as pending in SUMMARY.md and hand back to orchestrator.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `/host/db/host` PUT + POST | Authenticated cross-user writes; admin gate + sensitive-field-block already enforced from quick 260910-439 |
| SimpleDBOps.updateNonSensitive/insertNonSensitive → drizzle | New API surface skips DataCrypto.validateUserAccess; guard must reject sensitive fields BEFORE the DB write |
| host.ts branch selection (cross-user vs same-user) | Decision boundary — a wrong branch on same-user path would skip encryption for own writes; A9 regression test enforces the invariant |

## STRIDE Threat Register

| Threat ID  | Category | Component | Disposition | Mitigation Plan |
|------------|----------|-----------|-------------|-----------------|
| T-Q67Z-01  | Tampering | SimpleDBOps.updateNonSensitive `data` param | mitigate | Guard loop rejects any key in SENSITIVE_FIELDS_BY_TABLE[tableName] before drizzle call. Test `updateNonSensitive_throws_on_sensitive_field_in_data` locks the behavior. |
| T-Q67Z-02  | Tampering | SimpleDBOps.insertNonSensitive `data` param | mitigate | Same guard, symmetric test. |
| T-Q67Z-03  | Information Disclosure | Cross-user writes writing sensitive fields in cleartext to shared columns | mitigate | Two-layer defense: (a) host.ts API boundary returns 400 (298845b2 guard, unchanged); (b) SimpleDBOps guard throws if any sensitive field slips past the API layer. |
| T-Q67Z-04  | Elevation of Privilege | Non-admin reaching the new methods | accept | The new methods have no admin check themselves — that's the responsibility of the caller. host.ts routes them ONLY inside the `isAdminForUpdate && effectiveUpdateUserId !== userId` branch (PUT) and the `effectiveUserId !== userId` branch (POST), both gated by `callerIsAdmin` (298845b2). Direct import from other callers is a future risk documented in the JSDoc: "for admin cross-user writes only — must be gated by callerIsAdmin". |
| T-Q67Z-05  | Repudiation | Missing audit trail for cross-user writes | mitigate | Existing `host_create_admin_cross_user` and `host_update_admin_cross_user` audit logs (298845b2, L517-524 and L1277-1284) fire regardless of which SimpleDBOps method is used — unchanged. |
| T-Q67Z-06  | Denial of Service | Same-user path accidentally routed through non-sensitive methods (encryption skipped for own writes → data leak on read) | mitigate | A9 regression test asserts `SimpleDBOps.update` is called (NOT `updateNonSensitive`) for admin same-user PUT. Branch condition `effectiveUpdateUserId !== userId` on PUT and `effectiveUserId !== userId` on POST is the only way to reach the non-sensitive methods. |
| T-Q67Z-SC  | Tampering | npm/pip/cargo installs | n/a | No new package installs in this plan. |
</threat_model>

<verification>
## Automated

- Task 1: `npx vitest run src/backend/utils/simple-db-ops.test.ts` → 10/10 pass
- Task 2: `npx vitest run src/backend/database/routes/host.test.ts` → 31/31 pass (existing 27 + A7-A10)
- TypeScript: `npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "src/backend/(database/routes/host|utils/simple-db-ops)"` → empty

## LIVE (Task 3, human-verified)

- Cookie is admin `alice` with `isAdmin:true, data_unlocked:true`
- Rows 21/22/23 PUT via LIVE `PUT /host/db/host/${id}` all return 200
- Post-PUT `GET /host/db/host/${id}` shows `credentialId` = 2 (row 21), 5 (row 22), 3 (row 23)
- `sudo docker logs skynet --since 60s 2>&1 | grep -cE "Unsupported authType.*fleetHostId:(21|22|23)"` = 0
- Verbatim output captured in SUMMARY.md § "LIVE Smoke Test"

## Regression guards

- A1-A6 still pass (predecessor plan untouched)
- Same-user PUT/POST paths continue to use SimpleDBOps.update/insert (A9 asserts this)
- Sensitive-field-block 400 from 298845b2 still fires before either non-sensitive method is invoked
</verification>

<success_criteria>
- Two atomic commits from executor + one from smoke test = 3 total (per constraint)
  1. `feat(260910-67z-T1): SimpleDBOps.updateNonSensitive + insertNonSensitive with sensitive-field guard`
  2. `feat(260910-67z-T2): route host.ts admin cross-user POST+PUT through insertNonSensitive/updateNonSensitive + A7-A10 regression tests`
  3. `docs(260910-67z-T3): capture LIVE smoke test — rows 21/22/23 cross-user PUT successful` (or orchestrator-bundled per constraint)
- All 31 host.test.ts tests + all 10 simple-db-ops.test.ts tests pass
- Rows 21/22/23 successfully migrated to shared credentials via LIVE PUT (definitional)
- "Unsupported authType.*fleetHostId:(21|22|23)" log noise silenced (grep -c == 0)
- No sensitive-field cleartext leaks (guard tests + regression tests confirm)
- SUMMARY.md captures verbatim smoke test output
</success_criteria>

<output>
Create `.planning/quick/260910-67z-admin-cross-user-writes/260910-67z-SUMMARY.md` when done,
following `$HOME/.claude/get-shit-done/templates/summary.md`. Include:

- Task table with commit SHAs
- What was built (per task)
- LIVE smoke test section with verbatim curl outputs for rows 21/22/23
- TypeScript check result
- Deviations from plan (if any)
- Known stubs (should be none)
- Threat flags (T-Q67Z-01..06 all mitigated, T-Q67Z-SC n/a)
- Self-check
</output>
