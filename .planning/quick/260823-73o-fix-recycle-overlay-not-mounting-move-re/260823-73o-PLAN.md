---
phase: quick-260823-73o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/fleet-status/ssh-poll-orchestrator.ts
  - src/backend/fleet-status/ssh-poll-orchestrator.test.ts
autonomous: true
requirements:
  - QUICK-260823-73O-01
must_haves:
  truths:
    - "During tina's /id reset window, `fleet_status_recycling_armed` fires for identity tina on every 2s tick where ANY of the three axes evaluate true — regardless of tina's PID lifecycle state (pre-teardown live PID, mid-teardown, post-teardown no PID)."
    - "Source B publishes `SessionState.recycling === true` for an identity whose folder has `.recycle-requested` present, even when that identity also has a live claude PID in `liveTmuxSet` (the historical skip is lifted when recycling axis fires)."
    - "Source B publishes `SessionState.recycling === true` for an identity whose folder has `.recycled-at` present, even when that identity also has a live claude PID (same lifted-skip semantics)."
    - "Source B publishes `SessionState.recycling === true` for an identity whose newest `/id <identityName>` JSONL file has `/id reset` as the last-parseable real user turn in the last 256KB tail, even when that identity has a live claude PID."
    - "Source B publishes `SessionState.recycling === false` (or preserves cache) for an identity with none of the three axes true — no false positives."
    - "Source A's `processPid` no longer computes any of the three recycle axes (they are removed from source A's pipeline entirely) — source A publishes DO NOT stamp `recycling` from its own derivation; source B is the sole publisher of the recycling axis."
    - "Fingerprint suppression for source B still works — an identity with unchanged (dormant, recycling) across ticks does NOT re-publish."
    - "Source A's non-recycle axes (dormant, lastMessageAt, aiTitle, status, waitingFor, backgroundTasks, per-PID fingerprint) are UNTOUCHED; existing dormant / aiTitle / lastMessageAt / status tests still pass byte-for-byte."
    - "`sentinel-detect.ts`'s optional `log` field on `__applySentinelCheckForTests` deps stays optional (backward compat with the existing sentinel-detect tests)."
    - "All 68 existing tests in `ssh-poll-orchestrator.test.ts` still pass; new tests extend the suite (do not replace)."
  artifacts:
    - path: "src/backend/fleet-status/ssh-poll-orchestrator.ts"
      provides: "Three recycle axes migrated from source A `processPid` into source B `pollDormantOnlyIdentities` per-identity iteration; source B extended to perform per-identity JSONL discovery + Layer 1 tail scan; source B liveTmuxSet skip lifted for recycling=true identities."
      contains: "quick-260823-73o"
    - path: "src/backend/fleet-status/ssh-poll-orchestrator.test.ts"
      provides: "New describe block `quick-260823-73o — recycle axes in source B (per-identity, PID-independent)` with 6 tests locking the migration."
      contains: "quick-260823-73o"
  key_links:
    - from: "src/backend/fleet-status/ssh-poll-orchestrator.ts pollDormantOnlyIdentities (source B) per-identity loop"
      to: "SessionState.recycling axis published to registry"
      via: "OR-composed derivedRecyclingComposed = layer1 || requested || sentinel"
      pattern: "derivedRecyclingComposed"
    - from: "src/backend/fleet-status/ssh-poll-orchestrator.ts pollDormantOnlyIdentities per-identity loop"
      to: "discoverIdentityJsonlPathViaChannel + scanTailForLayer1RecyclingSignal"
      via: "per-identity jsonlPath cache in new source-B per-identity cache entry"
      pattern: "discoverIdentityJsonlPathViaChannel\\(channel, name\\)"
    - from: "src/backend/fleet-status/ssh-poll-orchestrator.ts pollDormantOnlyIdentities skip branch"
      to: "identity with live PID + recycling axis true still publishes source-B recycling frame"
      via: "conditional skip: liveTmuxSet.has(name) && !isRecycling"
      pattern: "liveTmuxSet.has\\(name\\) && !isRecycling"
    - from: "src/backend/fleet-status/ssh-poll-orchestrator.ts processPid (source A)"
      to: "no derivedLayer1Recycling / derivedRequestedRecycling / derivedSentinelRecycling anywhere in processPid"
      via: "recycle-axes code blocks REMOVED from processPid"
      pattern: "derivedLayer1Recycling|derivedRequestedRecycling|derivedSentinelRecycling"
---

<objective>
Move the three recycle axes (`derivedLayer1Recycling`,
`derivedRequestedRecycling`, `derivedSentinelRecycling`) out of source A's
per-PID `processPid` function in
`src/backend/fleet-status/ssh-poll-orchestrator.ts` and into source B's
unconditional per-identity iteration in `pollDormantOnlyIdentities`, so the
recycling axis arms for identity `tina` throughout the entire recycle window
regardless of the claude PID's lifecycle state.

Purpose: Patch #495 shipped a fix that FAILED user verification — Ashley
narrated a full `/id reset` cycle and never saw the recycling overlay.
Preliminary log RCA confirmed:
- Layer 1 detection (`transitionToHolding reason:id_reset`) fires correctly at
  04:45:47Z, right after the reset command is submitted.
- BUT: `fleet_status_recycling_armed` (patch #495's per-tick log when
  `derivedRecyclingComposed=true`) NEVER fires for identity tina during the
  reset window.
- Root cause: all three axes live inside source A's per-PID loop. Source A
  iterates `~/.claude/tasks/*.json` (Claude Code PID files) — during tina's
  reset, tina's claude PID is being torn down and source A's per-PID iteration
  hits a lifecycle-timing gap where none of the three axes ever evaluate true
  across the sentinel-present window.
- Source B is the identity-folder-keyed path that already parallel-stats
  `.dormant` + `.recycled-at` + `.recycle-requested`, but source B explicitly
  SKIPS any identity in `liveTmuxSet` — so during the first seconds of recycle
  (tina still has a live PID + sentinel on disk), source B also skips tina.
- Result: overlay never mounts.

The agent-supervisor renames `.recycle-requested` → `.recycled-at` on the
first reconcile tick after touch, then schedules deletion of `.recycled-at`
8s after `drive()` returns. So `.recycled-at` is on disk from `t=~+2s` through
`t=~+drive_returns+8s` — the sentinel-present state precisely brackets the
entire "should show overlay" window with an 8s guard band. NO PID observation
is needed. Source B, which iterates the identity folder (not PID files) and
runs unconditionally per identity per tick, is the correct architectural seam.

Output:
- `ssh-poll-orchestrator.ts` restructured:
  - Source A `processPid`: all three recycle-axis derivation blocks removed
    (three stat calls, Layer 1 scan block, OR-composition, `derivedSentinelRecycling`
    /`derivedRequestedRecycling`/`derivedLayer1Recycling` locals, and the
    `fleet_status_recycling_armed` log). Source A's published `SessionState`
    stamps `recycling: false` (or omits, if the wire schema tolerates) —
    source B is the sole publisher of the recycling axis going forward. The
    `PidCacheEntry` fields `recycling`, `layer1RecyclingCached`, and
    `recycleRequestedCached` are deleted (dead after the migration).
  - Source B `pollDormantOnlyIdentities` extended:
    - New per-identity cache map `identityRecycleState` (replaces or extends
      `dormantOnlyIdentities`) keyed by identity name, holding
      `{ dormant, recycling, layer1RecyclingCached, jsonlPath, staleTailTickCount, lastPublishedFingerprint }`.
    - Per-identity per-tick pipeline: parallel-stat `.dormant + .recycled-at +
      .recycle-requested`; then for identities with tmuxSession discoverable,
      call `discoverIdentityJsonlPathViaChannel(channel, name)` (cache the
      path in the per-identity entry, invalidate on stale-tail threshold
      identical to source A's `STALE_TAIL_REDISCOVERY_THRESHOLD=5`); tail
      `tail -c 262144 <jsonlPath>`; scan with
      `scanTailForLayer1RecyclingSignal`; cache the boolean (fail-open on
      SSH hiccup); OR-compose the three axes into `derivedRecyclingComposed`.
    - `liveTmuxSet.has(name)` skip is CONDITIONALLY lifted: skip only when
      `!isRecycling`. When `isRecycling === true`, publish the source-B
      recycling frame even if source A also published for that identity this
      tick (source A no longer stamps recycling, so no conflict).
    - Emit `fleet_status_recycling_armed` from source B (moved from source A)
      with the same field set (`layer1`, `requested`, `sentinel`, `composed`,
      cached values) plus `identityName` and `hasLivePid: liveTmuxSet.has(name)`.
    - Fingerprint suppression preserved: per-identity cache entry compares
      new `(dormant, recycling)` tuple against cached; only publish on delta.
- `ssh-poll-orchestrator.test.ts`: new describe block
  `quick-260823-73o — recycle axes in source B (per-identity, PID-independent)`
  with 6 tests covering:
  1. `.recycle-requested` present + live PID → source B publishes recycling:true.
  2. `.recycled-at` present + live PID → source B publishes recycling:true.
  3. No live PID + either sentinel present → source B publishes recycling:true
     (regression guard — Phase 53 CR C2 behavior preserved via new path).
  4. Live PID + newest jsonl's last user turn is `/id reset` → source B publishes
     recycling:true (Layer 1 in source B).
  5. Live PID + no sentinels + no `/id reset` in tail → source B does NOT
     publish recycling:true (negative case).
  6. Live PID + `.recycle-requested` present on tick 1, absent on tick 2, source A
     dormant-only frame between → fingerprint suppression on tick 2 does NOT
     re-publish stale recycling:true (delta-only publish preserved).
- All 68 existing tests still green under
  `npx vitest run src/backend/fleet-status/ssh-poll-orchestrator.test.ts`.
- New tests added to the same file; total green count = 68 + 6 = 74.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@src/backend/fleet-status/ssh-poll-orchestrator.ts
@src/backend/fleet-status/ssh-poll-orchestrator.test.ts
@src/backend/claude-session/sentinel-detect.ts
@src/backend/claude-session/session-file-discovery.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 6 RED tests locking source-B recycle-axis semantics under `quick-260823-73o` describe block</name>
  <files>src/backend/fleet-status/ssh-poll-orchestrator.test.ts</files>
  <behavior>
    Add a new describe block at the end of the test file titled:
    `quick-260823-73o — recycle axes in source B (per-identity, PID-independent)`.

    All 6 tests share a `wireLivePidAndIdentity(channel, opts)` helper (copy
    the shape of the existing `wireLivePid` helper from the Phase 52 T3
    describe block, ~L2290) that:
      - Wires `ls -1 ~/.claude/sessions/` → single live PID JSON path (PID 12345).
      - Wires `cat ~/.claude/sessions/12345.json`, `/proc/12345/stat`,
        `/proc/12345/environ`, `tmux display-message` all matching identity
        `tina` (so source A resolves tmuxSession → "tina" and publishes a
        non-recycling frame — source A no longer stamps recycling per the
        migration).
      - Wires `ls -1 ~/.claude/identities/` → "tina\n" so source B iterates
        tina every tick.
      - Wires source A's discovery + jsonl tail responses (`IDENTITY=` marker
        + `discovered.jsonl` marker) for non-recycling tail contents by default.
      - Wires source B's per-identity discovery — source B ALSO calls
        `discoverIdentityJsonlPathViaChannel(channel, "tina")` per tick, which
        emits the same `IDENTITY=tina` script; the MockSshChannel is
        first-match-wins on substring so a single `IDENTITY=` fixture suffices
        for both call sites in the same tick.
      - Overridable defaults for `.recycle-requested`, `.recycled-at`,
        `.dormant`, and the jsonl tail contents.

    Test list (using existing helpers `idResetLine`, `plainMessageLine`,
    `buildDiscoveryFixture` from the quick-260822-0vw describe block —
    hoist to file scope OR copy locally, executor's judgment):

    - Test QT-260823-73o-T1-i: `.recycle-requested` present + live PID for tina
      → source B publishes at least one frame with `state.recycling === true`,
      `state.tmuxSession === "tina"`, and `state.sessionId === "__dormant__"`
      (source-B synthetic sessionId even though tina has a live PID — the
      recycling frame is a source-B frame). Also expect source A publishes its
      own frame with `state.pid === 12345` AND `state.recycling === false`
      (source A no longer stamps recycling). So `publishedStates.length >= 2`.
    - Test QT-260823-73o-T1-ii: `.recycled-at` present + live PID for tina →
      same shape as T1-i but with `.recycled-at` "yes" instead of
      `.recycle-requested` "yes". Source B recycling:true frame present.
    - Test QT-260823-73o-T1-iii: no live PID for tina (empty
      `~/.claude/sessions/`) + `.recycle-requested` "yes" → source B publishes
      recycling:true (regression guard for the Phase 53 CR C2 behavior — now
      served by the new path). Exactly ONE publish (source A silent).
    - Test QT-260823-73o-T1-iv: live PID for tina + jsonl tail's last user turn
      is `idResetLine(2000)` + all sentinels false → source B publishes
      recycling:true via Layer 1 axis. Source A publishes recycling:false. So
      `publishedStates.length >= 2` and the source-B frame (identified by
      `state.sessionId === "__dormant__"`) has recycling:true.
    - Test QT-260823-73o-T1-v: live PID + all three axes false (plain
      assistant message tail + both sentinels "no") → source B publishes
      recycling:false (or DOES NOT publish a recycling frame — either outcome
      is acceptable per fingerprint semantics; the guard is that NO published
      frame this tick has `state.recycling === true`).
    - Test QT-260823-73o-T1-vi: tick 1: `.recycle-requested` "yes" → source B
      publishes recycling:true. Tick 2: `.recycle-requested` "no", `.recycled-at`
      "no", jsonl tail unchanged (still no /id reset in tail) → source B
      publishes recycling:false (fingerprint delta detected on recycling axis).
      No spurious extra publish, no stale recycling:true persistence.

    Assertion pattern: identify source-B frames by
    `p.state.sessionId === "__dormant__"` and source-A frames by
    `p.state.pid === 12345`; assert on the subset matching each source.

    Run tests before implementation — they MUST fail (RED). Failure mode: the
    current source A publishes ARE the only frames; source B never emits a
    recycling frame for a live-PID identity because of the current
    unconditional `liveTmuxSet.has(name)` skip.

    Verify all 68 existing tests still pass (untouched) under
    `npx vitest run src/backend/fleet-status/ssh-poll-orchestrator.test.ts`
    — the new describe block does not alter existing helpers or shared state.
  </behavior>
  <action>
    Append a new describe block `quick-260823-73o — recycle axes in source B
    (per-identity, PID-independent)` at the end of
    `src/backend/fleet-status/ssh-poll-orchestrator.test.ts`. Add 6 tests
    following the exact list in `<behavior>`. Reuse `MockSshChannel`,
    `MockRegistry`, `makeSessionJson`, `makeStatContents`, `makeValidPayload`
    (already at file scope) and `buildDeps`. Copy or hoist `idResetLine`,
    `plainMessageLine`, and `buildDiscoveryFixture` from the quick-260822-0vw
    describe block for JSONL fixture generation. Run
    `npx vitest run src/backend/fleet-status/ssh-poll-orchestrator.test.ts`
    and confirm the 6 new tests FAIL (RED) while all 68 existing tests still
    PASS. Do NOT proceed to Task 2 until this state is reached (RED-on-new,
    GREEN-on-existing).
  </action>
  <verify>
    <automated>npx vitest run src/backend/fleet-status/ssh-poll-orchestrator.test.ts 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    Test file has 74 total `it(` blocks (68 + 6 new). The 6 new tests fail with
    assertion errors on `state.recycling === true` expectations (or on
    `publishedStates` frame counts) — NOT with parse errors, TS errors, or
    infrastructure errors. The 68 existing tests all pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate three recycle axes from source A processPid into source B pollDormantOnlyIdentities (per-identity, PID-independent)</name>
  <files>src/backend/fleet-status/ssh-poll-orchestrator.ts</files>
  <behavior>
    After this task, all 74 tests in `ssh-poll-orchestrator.test.ts` pass —
    the 6 new Task 1 tests flip from RED to GREEN, and the 68 existing tests
    continue to pass.

    Concrete behavioral contract:
    - Source A `processPid` no longer computes any of the three recycle axes.
      A source A publish stamps `SessionState.recycling: false` (source A is
      no longer the publisher of this axis).
    - Source B `pollDormantOnlyIdentities` computes all three axes per identity
      per tick, OR-composes them, and publishes a source-B frame keyed by
      identity name whenever the composed value or the dormant axis changes
      vs the per-identity cache.
    - The `liveTmuxSet.has(name)` skip is conditionally lifted: skip only when
      `!isRecycling`. When any of the three axes fires true, source B always
      publishes (regardless of whether source A also published for the same
      identity in the same tick).
    - `fleet_status_recycling_armed` log line emits from source B (with
      `identityName`, `layer1`, `requested`, `sentinel`, `composed`,
      cached values, and `hasLivePid: liveTmuxSet.has(name)`).
    - Per-identity cache in source B holds
      `{ dormant, recycling, layer1RecyclingCached, jsonlPath,
      staleTailTickCount, lastPublishedFingerprint }` — mirroring the source A
      caching model but keyed by identity name (string) rather than PID
      (number). Cache eviction rule: DELETE the entry when
      `!isRecycling && liveTmuxSet.has(name)` (source A owns the publish only
      when NOT recycling, matching the pre-migration source B skip behavior
      for the dormant axis).
    - Fingerprint suppression: source B publishes only on delta —
      `(dormant, recycling)` tuple changes vs cache OR first appearance.
  </behavior>
  <action>
    In `src/backend/fleet-status/ssh-poll-orchestrator.ts`:

    (A) In `processPid`: REMOVE the three recycle-axis blocks:
        - The `.recycled-at` stat block (lines ~896-926, `let derivedSentinelRecycling`).
        - The `.recycle-requested` stat block (lines ~928-952, `let derivedRequestedRecycling`).
        - The Layer 1 sub-scan inside the tail-scan block (the
          `scanTailForLayer1RecyclingSignal(tailRaw)` call and the
          `let derivedLayer1Recycling` local, ~lines 988-1017 — keep the tail
          scan and the `scanTailForNewestMessageAt` + `scanTailForLatestAiTitle`
          calls, remove ONLY the Layer 1 branch).
        - The `derivedRecyclingComposed` OR-composition block (~lines 1104-1120).
        - The `fleet_status_recycling_armed` log block (~lines 1122-1142).
        - In the composed `SessionState`, change `recycling: derivedRecyclingComposed`
          → `recycling: false` (source A no longer stamps this axis).
        - In the two `livenessMap.set` calls at the bottom of `processPid`,
          drop the `recycling`, `layer1RecyclingCached`, and
          `recycleRequestedCached` cache fields.
        - Delete the corresponding fields from the `PidCacheEntry` interface
          (`recycling: boolean`, `layer1RecyclingCached: boolean`,
          `recycleRequestedCached: boolean`) — they are now dead.
        - Preserve UNTOUCHED: `derivedDormant`, `derivedLastMessageAt`,
          `derivedAiTitle`, `nextStaleTailTickCount`, jsonlPath discovery +
          caching for source A, status/waitingFor/backgroundTasks composition,
          fingerprint delta semantics for source A.

    (B) In `PerHostState`: widen `dormantOnlyIdentities` from
        `Map<string, { dormant: boolean; recycling: boolean }>` to a new
        `identityRecycleState: Map<string, IdentityRecycleCacheEntry>` where
        `IdentityRecycleCacheEntry` is a new local interface:
        ```
        interface IdentityRecycleCacheEntry {
          dormant: boolean;
          recycling: boolean;  // OR-composed (layer1 || requested || sentinel)
          layer1RecyclingCached: boolean;  // last non-null Layer 1 scan result
          jsonlPath: string | null;
          staleTailTickCount: number;
          lastPublishedFingerprint: string;
        }
        ```
        Keep the semantic contract: cache is populated per-tick on delta;
        cleared for a name when `!isRecycling && liveTmuxSet.has(name)`
        (source A takes over for non-recycling live-PID identities, matching
        the pre-migration eviction rule).

    (C) In `pollDormantOnlyIdentities`: rewrite the per-identity loop:
        1. Parallel-stat all three sentinels per identity (already in place,
           keep the three `channel.exec` calls unchanged).
        2. For each identity, if the per-identity cache has NO jsonlPath (or
           `staleTailTickCount >= STALE_TAIL_REDISCOVERY_THRESHOLD` — reuse
           source A's const), call
           `discoverIdentityJsonlPathViaChannel(channel, name)` (already
           defined in this file) and cache the result. Reset staleTailTickCount
           to 0 on successful discovery, keep at previous value on null return.
        3. If jsonlPath is non-null, `channel.exec("tail -c 262144
           ${jsonlPath} 2>/dev/null || true")` and pass the result to
           `scanTailForLayer1RecyclingSignal`. On null return preserve the
           cached `layer1RecyclingCached`; on `false`/`true` overwrite.
           Increment `staleTailTickCount` when the tail scan returns null AND
           the previous cached value was true (defensive against jsonl
           rotation); reset to 0 otherwise. If threshold reached, null out
           `jsonlPath` to force re-discovery next tick.
        4. `isRecycling = isRecycledAt || isRecycleRequested || layer1RecyclingCached`.
        5. Skip-and-evict branch: if `liveTmuxSet.has(name) && !isRecycling`,
           delete the cache entry (mirror the pre-migration skip semantics for
           non-recycling live-PID identities) and `continue`.
        6. Fingerprint compute: build a stable string of `(dormant, recycling,
           layer1RecyclingCached, jsonlPath, staleTailTickCount)` — the last
           three are internal state but including them in the fingerprint is
           harmless because they only change when the visible-to-registry
           axes also change (via causation). SIMPLER: use just
           `${isDormant?"1":"0"}|${isRecycling?"1":"0"}` — this matches the
           Phase 53 CR C2/C3 semantics exactly (source B was already
           fingerprinting only on those two axes; the internal jsonlPath /
           staleTailTickCount cache updates every tick and would spuriously
           publish otherwise).
        7. Compare against `cached.lastPublishedFingerprint`; if identical,
           update cache in place (advance internal state) but skip publish.
        8. If different (or first appearance), publish the source-B
           `SessionState` frame (existing shape: `sessionId: "__dormant__"`,
           `pid: null`, `status: "idle"`, `dormant: isDormant`,
           `recycling: isRecycling`), update the cache entry with the new
           fingerprint, and emit `fleet_status_recycling_armed` when
           `isRecycling` is true (moved from source A; include
           `identityName: name`, `hasLivePid: liveTmuxSet.has(name)`,
           `layer1`, `requested`, `sentinel`, `composed`, and the cached
           values from the entry).
        9. Emit the existing `fleet_status_source_b_publish` log line as
           before (preserve the field set including `dormant`, `recycling`,
           `previousDormant`, `previousRecycling`).

    (D) Preserve the source-B skip semantics for the `.dormant`-only case
        (identity with `.dormant` present but no recycle axes and a live PID
        → skip + evict). This is the pre-migration behavior for that shape
        and matches Test P52-01-T3-vi (live-PID identity with .dormant
        sentinel → source B skips) which MUST still pass.

    (E) In `tryAcquireHostChannel`, initialize the new
        `identityRecycleState` map empty (mirror the current
        `dormantOnlyIdentities: new Map()` initialization).

    (F) Run `npx vitest run src/backend/fleet-status/ssh-poll-orchestrator.test.ts`
        — all 74 tests green.

    (G) `sentinel-detect.ts` is UNTOUCHED — the optional `log` field on
        `__applySentinelCheckForTests` deps stays optional. Do NOT edit
        `sentinel-detect.ts` in this plan.
  </action>
  <verify>
    <automated>npx vitest run src/backend/fleet-status/ssh-poll-orchestrator.test.ts 2>&amp;1 | tail -30 &amp;&amp; grep -c "derivedLayer1Recycling\|derivedRequestedRecycling\|derivedSentinelRecycling" src/backend/fleet-status/ssh-poll-orchestrator.ts | grep -v '^#' &amp;&amp; echo "Source A recycle axes remaining count above (target: 0 in processPid, but may appear in source B loop as `layer1RecyclingCached` local — inspect grep output line context to confirm all remaining hits are in the pollDormantOnlyIdentities function, NOT in processPid)"</automated>
  </verify>
  <done>
    All 74 tests pass. The three `derived*Recycling` locals no longer exist
    inside `processPid` (grep confirms — count is 0 in the processPid body,
    which is one of the acceptance criteria). Source B's per-identity loop
    computes all three axes, OR-composes, publishes with fingerprint
    suppression on `(dormant, recycling)` tuple, and lifts the `liveTmuxSet`
    skip when `isRecycling === true`. The `fleet_status_recycling_armed` log
    fires from source B with `identityName` in the payload.
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit — code + tests locked as one atomic quick-260823-73o patch</name>
  <files>N/A (git commit)</files>
  <action>
    Stage the two modified files and commit with a single message. Do NOT
    push, do NOT deploy — orchestrator handles both at end-of-batch per fleet
    rule. Use HEREDOC for the commit body.

    Commit message:
    ```
    fix(quick-260823-73o): migrate recycle axes source A → source B per-identity

    Move `.recycle-requested` + `.recycled-at` + Layer 1 /id reset scan out
    of ssh-poll-orchestrator's per-PID source A loop and into source B's
    per-identity iteration. Fixes the overlay-never-arms bug Ashley narrated
    at 04:45:47Z on 2026-08-23 (patch #495 shipped diagnostic logs but no
    fix; this patch is the fix).

    Root cause: source A iterates ~/.claude/tasks/*.json (PID files). During
    a /id reset, the outgoing PID is being torn down and none of the three
    axes ever evaluate true across the sentinel-present window in source A's
    per-PID loop. Source B is the identity-folder-keyed path that already
    parallel-stats the sentinels; the fix lifts the `liveTmuxSet` skip
    conditionally (skip only when NOT recycling), adds Layer 1 JSONL scan
    to source B via discoverIdentityJsonlPathViaChannel + tail scan, and
    makes source B the sole publisher of the recycling axis.

    Test matrix locked (6 new tests, 68 existing preserved):
    - .recycle-requested + live PID → source B publishes recycling:true
    - .recycled-at + live PID → source B publishes recycling:true
    - No live PID + either sentinel → source B publishes recycling:true
    - Live PID + Layer 1 /id reset in tail → source B publishes recycling:true
    - Live PID + no axes → source B publishes recycling:false
    - Recycling flip false→true→false across ticks → fingerprint delta only
    ```

    Commit command (use HEREDOC exactly as shown):
    ```
    git add src/backend/fleet-status/ssh-poll-orchestrator.ts \
            src/backend/fleet-status/ssh-poll-orchestrator.test.ts
    git commit -m "$(cat <<'EOF'
    fix(quick-260823-73o): migrate recycle axes source A → source B per-identity

    Move `.recycle-requested` + `.recycled-at` + Layer 1 /id reset scan out
    of ssh-poll-orchestrator's per-PID source A loop and into source B's
    per-identity iteration. Fixes the overlay-never-arms bug Ashley narrated
    at 04:45:47Z on 2026-08-23 (patch #495 shipped diagnostic logs but no
    fix; this patch is the fix).

    Root cause: source A iterates ~/.claude/tasks/*.json (PID files). During
    a /id reset, the outgoing PID is being torn down and none of the three
    axes ever evaluate true across the sentinel-present window in source A's
    per-PID loop. Source B is the identity-folder-keyed path that already
    parallel-stats the sentinels; the fix lifts the `liveTmuxSet` skip
    conditionally (skip only when NOT recycling), adds Layer 1 JSONL scan
    to source B via discoverIdentityJsonlPathViaChannel + tail scan, and
    makes source B the sole publisher of the recycling axis.

    Test matrix locked (6 new tests, 68 existing preserved):
    - .recycle-requested + live PID → source B publishes recycling:true
    - .recycled-at + live PID → source B publishes recycling:true
    - No live PID + either sentinel → source B publishes recycling:true
    - Live PID + Layer 1 /id reset in tail → source B publishes recycling:true
    - Live PID + no axes → source B publishes recycling:false
    - Recycling flip false→true→false across ticks → fingerprint delta only
    EOF
    )"
    ```

    Do NOT run `git push`. Do NOT run any `docker` command. Do NOT run the
    full test suite (`npx vitest run` without arg) — orchestrator does that
    at end-of-batch.
  </action>
  <verify>
    <automated>git log -1 --format='%s' | grep -q "quick-260823-73o" &amp;&amp; git status --porcelain | wc -l | grep -q '^0$' &amp;&amp; echo "commit landed and worktree clean"</automated>
  </verify>
  <done>
    Exactly one new commit at HEAD titled
    `fix(quick-260823-73o): migrate recycle axes source A → source B per-identity`.
    `git status` reports a clean worktree. Nothing pushed, nothing deployed.
  </done>
</task>

</tasks>

<verification>
Scoped verification only (fleet directive — orchestrator owns full-suite
ship-gate at end of batch):

1. `npx vitest run src/backend/fleet-status/ssh-poll-orchestrator.test.ts`
   → 74 passing, 0 failing.
2. `grep -n "derivedLayer1Recycling\|derivedRequestedRecycling\|derivedSentinelRecycling" src/backend/fleet-status/ssh-poll-orchestrator.ts`
   → any hits must be inside the `pollDormantOnlyIdentities` function
   (source B), NEVER inside `processPid`.
3. `grep -n "fleet_status_recycling_armed" src/backend/fleet-status/ssh-poll-orchestrator.ts`
   → exactly one hit, inside `pollDormantOnlyIdentities` (source B), with
   `identityName` in the payload.
4. `git status --porcelain` → clean.
5. `git log -1 --format='%s'` → matches
   `fix(quick-260823-73o): migrate recycle axes source A → source B per-identity`.
</verification>

<success_criteria>
- Scoped test file (`ssh-poll-orchestrator.test.ts`) reports 74 passing tests.
- All three recycle-axis locals removed from `processPid` (grep confirms 0
  hits inside `processPid` body).
- Source B's per-identity loop is the ONLY publisher of
  `SessionState.recycling` from the orchestrator.
- `sentinel-detect.ts` is UNTOUCHED (backward compat with its existing tests).
- Fingerprint suppression semantics preserved: identity with unchanged
  `(dormant, recycling)` across ticks does NOT re-publish.
- Existing 68 tests pass byte-for-byte (no regressions in dormant/aiTitle/
  lastMessageAt/status/waitingFor/backgroundTasks/session_gone paths).
- Exactly one atomic commit landed with message beginning
  `fix(quick-260823-73o):`. Nothing pushed, nothing deployed, nothing built.
</success_criteria>

<output>
Create `.planning/quick/260823-73o-fix-recycle-overlay-not-mounting-move-re/260823-73o-SUMMARY.md` when done — see execution_context templates.
</output>
