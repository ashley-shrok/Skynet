---
phase: 260813-0qx
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/claude-session/claude-session-server.ts
  - src/backend/claude-session/claude-session-server.repoll.test.ts
autonomous: true
requirements:
  - QUICK-260813-0qx
tags:
  - claude-session
  - session-holding-overlay
  - discovery-repoll
  - fallback-01
  - reset-window
  - bug-fix

must_haves:
  truths:
    - "On initial-attach `discoverClaudeSession` returning `{status:'inactive', reason:'no_pid_session_file'}`, the WS enters `holding` (emits `{type:'session_holding'}` + `pane_state:{state:'holding',reason:'discovery_diff'}`), keeps `sshConn` open, and starts the discovery-repoll timer — NOT FALLBACK-01."
    - "Same behavior for `reason:'no_open_session_file'` on initial attach."
    - "Same behavior for `reason:'not_claude'` ON AN IDENTITY-SHAPED PANE WITH NO `.dormant` SENTINEL (i.e. `enteredDormantPoll===false` AND `isIdentityShapedCached===true`) — the identity pane is mid-reset, not a bare shell."
    - "`reason:'not_claude'` on a NON-identity-shape pane (`isIdentityShapedCached===false` — bare tmux) STILL falls through to FALLBACK-01 unchanged (emits `{type:'inactive'}` + `pane_state:{state:'inactive'}`, calls `conn.end()`, returns)."
    - "`reason:'no_tmux_session'` on initial attach STILL falls through to FALLBACK-01 unchanged (terminal — no pane to poll)."
    - "`reason:'exec_error'` on initial attach STILL falls through to FALLBACK-01 unchanged (SSH-side failure is not a reset-window signal — attach-time we have no prior successful discovery to distinguish transient from persistent)."
    - "After entering holding from the attach-path reset-window branch, the discovery-repoll ticker running every 3s: on active-with-new-file → calls `transitionToActiveNew` (starts tail, WS receives `session_changed`); on active-with-same-file (impossible since we had no prior file, but defensive-safe) → calls `transitionFromHoldingToActiveSameFile` no-op guarded by `changeoverState==='active'`; on inactive+not-exec-error → increments `holdingTicks`; at `HOLDING_TIMEOUT_TICKS` (200 × 3s = 10min) → calls `transitionToDead('holding_timeout')` which emits the terminal `{type:'inactive', reason:'holding_timeout'}` frame."
    - "The reset-window branch does NOT re-run the identity-shape SSH probe — it reads the already-computed `enteredDormantPoll` and `isIdentityShapedCached` locals from the dormant-branch block above (piggyback semantics)."
    - "The reset-window branch does NOT close `sshConn`, does NOT call `conn.end()`, and DOES `return` before FALLBACK-01 runs (mirrors the `enteredDormantPoll` early-return pattern at ~L5083)."
    - "No new wire-frame types introduced. Existing `session_holding` and `pane_state:holding` shapes are reused verbatim."
    - "`HOLDING_TIMEOUT_TICKS` value is UNCHANGED (still 200)."
    - "Frontend receives ONLY existing frame shapes (`session_holding`, `pane_state:{state:'holding',reason:'discovery_diff'}`, followed by either `session_changed`/`pane_state:active` on recovery or `inactive:holding_timeout`/`pane_state:inactive` on timeout) — no frontend code change required."
    - "The `startActiveSessionFlow` code path is UNCHANGED. Its inline discovery-repoll timer either (a) is now sourced from the same shared helper the attach branch uses, or (b) is inlined-copied — the behavioral contract of the steady-state repoll ticker is byte-preserved."
  artifacts:
    - path: "src/backend/claude-session/claude-session-server.ts"
      provides: "Attach-path reset-window branch that enters holding + starts discovery-repoll timer for {no_pid_session_file, no_open_session_file, not_claude+identity-no-dormant} instead of FALLBACK-01"
      contains: "reset-window"
    - path: "src/backend/claude-session/claude-session-server.repoll.test.ts"
      provides: "New test cases exercising the attach-path reset-window reducer via a test seam (mirrors __applyRepollResultForTests shape). Covers all six discovery reasons × applicable identity-shape states, plus the recovery-and-timeout paths of the attached repoll timer."
      contains: "attach-path reset-window"
  key_links:
    - from: "src/backend/claude-session/claude-session-server.ts (initial-attach inactive branch, ~L4789)"
      to: "src/backend/claude-session/claude-session-server.ts (transitionToHolding, ~L2360)"
      via: "new reset-window branch"
      pattern: "transitionToHolding\\(\"discovery_diff\"\\)"
    - from: "src/backend/claude-session/claude-session-server.ts (initial-attach reset-window branch)"
      to: "src/backend/claude-session/claude-session-server.ts (discovery-repoll timer body ~L4700-L4774)"
      via: "shared setup helper OR inlined copy (executor picks — see task 1 <action>)"
      pattern: "discoveryRepollTimer = setInterval"
    - from: "src/backend/claude-session/claude-session-server.ts (initial-attach reset-window branch)"
      to: "src/backend/claude-session/pane-state-emitter.ts (emit)"
      via: "paneStateEmitter.emit(\"holding\", \"discovery_diff\")"
      pattern: "paneStateEmitter\\.emit\\(\"holding\", \"discovery_diff\"\\)"
---

<objective>
Fix the deactivate → `/id reset` window → reactivate → "no active Claude session" latch on the initial-attach path.

Root cause (LOCKED by orchestrator diagnosis — do not re-litigate): the initial-attach `if (result.status === "inactive") { ... }` block at ~L4789 in `claude-session-server.ts` treats every inactive discovery result (other than the dormant-identity branch) as terminal via FALLBACK-01. Meanwhile the STEADY-STATE `discoveryRepollTimer` loop inside `startActiveSessionFlow` at ~L4700 does the RIGHT thing on the same inactive-with-reset-reason: transitions to holding, keeps SSH open, polls until either a fresh session file appears or `HOLDING_TIMEOUT_TICKS` (10min) elapses.

Purpose: reuse the steady-state holding + repoll semantics for the initial-attach path when the inactive reason is a reset-window signal (`no_pid_session_file`, `no_open_session_file`, or `not_claude` on an identity-shape pane WITHOUT a `.dormant` sentinel). Terminal reasons (`no_tmux_session`, `exec_error`) stay on FALLBACK-01.

Output: a new "reset-window" branch in the initial-attach inactive block, plus test coverage in `claude-session-server.repoll.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/backend/claude-session/claude-session-server.ts
@src/backend/claude-session/pane-state-emitter.ts
@src/backend/claude-session/session-file-discovery.ts
@src/backend/claude-session/claude-session-server.repoll.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add attach-path reset-window branch + wire discovery-repoll timer (with shared-helper or inlined-copy setup)</name>
  <files>src/backend/claude-session/claude-session-server.ts</files>

  <behavior>
    - After the existing dormant-identity branch runs (or is skipped) inside the `if (result.status === "inactive") { ... }` block at ~L4789, a new reset-window branch fires when:
      * `enteredDormantPoll === false` AND
      * `result.reason` is one of `"no_pid_session_file"` | `"no_open_session_file"` | (`"not_claude"` AND `isIdentityShapedCached === true`)
    - When the reset-window branch fires it MUST:
      1. Set `changeoverState = "holding"`, `holdingReason = "discovery_diff"`, `holdingTicks = 0` (fresh budget for this attach).
      2. Emit the legacy `{type:"session_holding"}` frame on the wire (with the same open-guard + try/catch shape used elsewhere in this file — grep `session_holding` for canonical form; the `transitionToHolding` helper at ~L2360 is the reference — see <action> for the reuse decision).
      3. Call `paneStateEmitter.emit("holding", "discovery_diff")`.
      4. NOT close `sshConn`, NOT call `conn.end()`.
      5. Start the `discoveryRepollTimer` running the SAME repoll body that lives inside `startActiveSessionFlow` at ~L4700 (see <action> for the shared-helper vs inlined-copy decision).
      6. `return` BEFORE the FALLBACK-01 block runs (mirrors the `enteredDormantPoll` early-return pattern at ~L5083).
    - When `result.reason` is `"not_claude"` on a non-identity-shape pane (`isIdentityShapedCached === false` after the dormant probe block ran), the reset-window branch DOES NOT fire — FALLBACK-01 runs unchanged (bare shell is a legitimate "no claude" answer; identity-shape + no-.dormant is the mid-reset signal we care about).
    - When `result.reason` is `"no_tmux_session"` or `"exec_error"` the reset-window branch DOES NOT fire — FALLBACK-01 runs unchanged.
    - The steady-state `discoveryRepollTimer` block inside `startActiveSessionFlow` at ~L4700 has byte-preserved behavior after this refactor (either it now calls the same shared helper the attach branch calls, or it stays inline and the attach branch has an inlined copy — see <action>).
    - Once the reset-window branch has set `changeoverState = "holding"` and started the repoll timer, subsequent ticks flow through the EXISTING `__applyRepollResultForTests` reducer / production timer body unchanged: active-with-new-file → `transitionToActiveNew`; active-with-same-file while holding → `transitionFromHoldingToActiveSameFile` (guarded by `holdingReason==="discovery_diff"` path since it's not `"id_reset"`, so it will fire and clear — this is the correct "false alarm" path if by some race the file materializes at the exact same UUID we're comparing against `null`); inactive+not-exec-error → `holdingTicks++`; at `HOLDING_TIMEOUT_TICKS` → `transitionToDead("holding_timeout")`.
    - `currentSessionFile` MUST remain `null` at the moment the attach-path repoll timer starts (there is no prior session-file baseline for this WS). The timer's `active` branch will observe `result.sessionFile !== currentSessionFile` (right side is null) on the first tick that finds a real file, so `transitionToActiveNew` will fire — this is the correct recovery.
    - `activeTmuxSession` for the repoll's `discoverClaudeSession(connSnapshot, activeTmuxSession)` call MUST be the `tmuxSession` from the current `connectToPane` message (in scope at the attach site). If the shared-helper route is picked, the helper takes `activeTmuxSession` as a parameter.
  </behavior>

  <action>
    Step 1 — pick the wiring shape (RECORD YOUR CHOICE in a code comment inside the new branch):

    Prefer OPTION A — extract the `setInterval(...) { ... }` block currently at `~L4700-L4774` (the whole `discoveryRepollTimer = setInterval(...)` assignment through its closing `}, DISCOVERY_REPOLL_INTERVAL_MS);`) into a small connection-scoped helper called e.g. `startDiscoveryRepollTimer(activeTmuxSession: string): void`. Declare it inside the same closure that owns the timer state (immediately BEFORE `startActiveSessionFlow`'s assignment at ~L4040, so it can be called from both `startActiveSessionFlow` and the attach-path reset-window branch below). The helper closes over the same closure variables the inline block already reads: `stopped`, `ws`, `sshConn`, `discoveryRepollInFlight`, `discoveryRepollTimer`, `currentSessionFile`, `changeoverState`, `holdingTicks`, and calls `transitionToHolding`, `transitionToActiveNew`, `transitionFromHoldingToActiveSameFile`, `transitionToDead`, `discoverClaudeSession`. Both call sites (steady-state inside `startActiveSessionFlow` and the new attach-path branch) now just call `startDiscoveryRepollTimer(activeTmuxSession)`. Behavior is byte-preserved for the steady-state site.

    Fall back to OPTION C — inline a copy of the `setInterval(...) { ... }` block into the attach-path reset-window branch verbatim — ONLY IF the extraction turns out to touch too many other closure variables (e.g. TDZ ordering conflicts with `transitionToActiveNew`/`transitionToDead` declarations below), or produces a helper signature with >5 params. If you inline, add a comment `// COPY of discoveryRepollTimer block from ~L4700 — keep in sync with the steady-state copy if either changes.` at both sites.

    JUSTIFY the choice in a leading comment inside the new branch. Do NOT use Option B (calling `startActiveSessionFlow` with a sentinel that skips the tail-open) — the orchestrator's diagnosis flagged that route as not the target.

    Step 2 — add the reset-window branch. Insert it inside the `if (result.status === "inactive") { ... }` block at ~L4789, AFTER the closing `}` of the dormant-identity `if (result.reason === "not_claude") { ... }` block that ends at ~L5082, AFTER the `if (enteredDormantPoll) { return; }` early-return at ~L5083-L5086, and BEFORE the FALLBACK-01 comment/block starting at ~L5087. Structure:

    ```
    (after enteredDormantPoll early-return)

    // quick 260813-0qx — attach-path reset-window branch.
    //
    // If discoverClaudeSession returned inactive with a reason that signals we
    // are inside a /id reset (or fresh spawn) window on THIS pane — rather
    // than a terminal "no claude here" verdict — enter holding + start the
    // discovery-repoll timer instead of falling through to FALLBACK-01. The
    // repoll timer's existing branches (see startDiscoveryRepollTimer /
    // __applyRepollResultForTests) then own the recovery path: active with a
    // new session file -> transitionToActiveNew; HOLDING_TIMEOUT_TICKS
    // elapsed with no recovery -> transitionToDead('holding_timeout') which
    // emits the terminal inactive frame FALLBACK-01 would have sent
    // immediately.
    //
    // Reset-window reasons:
    //   * no_pid_session_file  — claude process exists but hasn't written a
    //                            fresh PID file yet
    //   * no_open_session_file — PID file found but new JSONL not yet open
    //   * not_claude AND identity-shape pane WITHOUT .dormant sentinel — the
    //                            identity pane is mid-reset (claude process
    //                            gone briefly), not a bare shell. Piggybacks
    //                            on isIdentityShapedCached set inside the
    //                            dormant-identity probe block above; we do
    //                            NOT re-run the SSH probe.
    //
    // Terminal reasons (no_tmux_session, exec_error) fall through unchanged.
    const isResetWindowReason =
      result.reason === "no_pid_session_file" ||
      result.reason === "no_open_session_file" ||
      (result.reason === "not_claude" && isIdentityShapedCached === true);
    if (isResetWindowReason) {
      // (Record the wiring choice per Step 1 in a short comment here.)
      changeoverState = "holding";
      holdingReason = "discovery_diff";
      holdingTicks = 0;
      currentHostId = hostId;
      currentTmuxSession = tmuxSession;
      if (!stopped && ws.readyState === WebSocket.OPEN) {
        try {
          ws.send(JSON.stringify({ type: "session_holding" }));
        } catch (err) {
          databaseLogger.warn(`[ws-server] send-failed msgType=session_holding err="..."`, { operation: "ws_send_failed" });
        }
      }
      paneStateEmitter.emit("holding", "discovery_diff");
      sshLogger.info("Claude session entering attach-path reset-window holding", {
        operation: "claude_session_holding_attach_reset_window",
        reason: result.reason,
        identityShape: isIdentityShapedCached,
        userId, sessionId, hostId, tmuxSession,
      });
      // Start the discovery-repoll timer (Option A: shared helper; Option C: inlined copy).
      startDiscoveryRepollTimer(tmuxSession);
      // SSH stays alive; repoll timer is running. Return WITHOUT teardown
      // (mirrors the enteredDormantPoll early-return above).
      return;
    }

    // FALLBACK-01: (unchanged block from ~L5087 downward)
    ```

    Important: DO NOT reuse the `transitionToHolding` helper at ~L2360 verbatim from the attach path — its early-return guard `if (changeoverState !== "active") return;` at L2363 is designed to protect against Layer-1/Layer-2 double-fire during steady state, but at attach time `changeoverState` starts at `"active"` (module default at L1690) so the guard would actually PASS. HOWEVER, the helper's log payload references `currentSessionFile` which is `null` at attach time (fine — it logs "null"), and the helper is symmetric with what we need. IF the transition helper works cleanly from the attach site (test manually: does `transitionToHolding("discovery_diff")` produce the identical wire + emitter + log output when called with `changeoverState==="active"` + `currentSessionFile===null`?) — prefer calling `transitionToHolding("discovery_diff")` over inlining the emit block. If you go this route, delete the inline `ws.send({type:"session_holding"})` + `paneStateEmitter.emit(...)` above and just call `transitionToHolding("discovery_diff")`; keep the `currentHostId`/`currentTmuxSession` seeding + the reset-window-specific log line separately (the helper's log is a different `operation`). Record the choice in the leading comment.

    Do NOT modify `HOLDING_TIMEOUT_TICKS` at L187.
    Do NOT modify the FALLBACK-01 block starting at ~L5087.
    Do NOT touch the frontend.
    Do NOT introduce new wire-frame types.
    Do NOT modify the discovery result type in `session-file-discovery.ts`.

    Step 3 — export a new test seam. Following the pattern of `__applyRepollResultForTests` at L920, add a module-scope function:

    ```
    export function __classifyAttachInactiveForTests(
      result: import("./session-file-discovery.js").ClaudeSessionDiscoveryResult,
      isIdentityShapedCached: boolean | null,
    ): "reset_window" | "fallback_01";
    ```

    Body: if `result.status === "active"` return `"fallback_01"` (defensive — attach path only calls this on inactive); else match the exact `isResetWindowReason` predicate above and return `"reset_window"` or `"fallback_01"`. Use this seam from the production branch too (so the branch reads `if (__classifyAttachInactiveForTests(result, isIdentityShapedCached) === "reset_window") { ... }`) to guarantee the reducer and the test-observed classifier are the same code.
  </action>

  <verify>
    <automated>cd /home/ubuntu/skynet-tiffany &amp;&amp; npx vitest run src/backend/claude-session/claude-session-server.repoll.test.ts 2>&amp;1 | tail -30 &amp;&amp; npm run build:backend 2>&amp;1 | tail -10</automated>
  </verify>

  <done>
    - New reset-window branch present between the dormant-identity block and FALLBACK-01 in the attach path.
    - `__classifyAttachInactiveForTests` exported and used by the production branch (single source of truth for the classifier).
    - Wiring choice (shared helper vs inlined copy vs reuse-transitionToHolding) recorded in a leading comment inside the new branch.
    - `npx vitest run src/backend/claude-session/claude-session-server.repoll.test.ts` exits 0 (existing repoll tests still pass — this task must not regress them; the new tests are added in Task 2 but the file compiles as-is).
    - `npm run build:backend` exits 0 (no TypeScript errors).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add repoll-test coverage for the attach-path reset-window classifier and post-holding recovery/timeout flows</name>
  <files>src/backend/claude-session/claude-session-server.repoll.test.ts</files>

  <behavior>
    New describe blocks appended to `claude-session-server.repoll.test.ts`:

    Group A — `__classifyAttachInactiveForTests` classifier (pure reducer, no state box needed):
      - `reason:'no_pid_session_file'` + `isIdentityShapedCached:null` → `"reset_window"`.
      - `reason:'no_pid_session_file'` + `isIdentityShapedCached:false` → `"reset_window"` (identity-shape irrelevant for this reason).
      - `reason:'no_pid_session_file'` + `isIdentityShapedCached:true` → `"reset_window"`.
      - `reason:'no_open_session_file'` (all three identity-shape values) → `"reset_window"`.
      - `reason:'not_claude'` + `isIdentityShapedCached:true` → `"reset_window"` (identity pane mid-reset).
      - `reason:'not_claude'` + `isIdentityShapedCached:false` → `"fallback_01"` (bare shell — terminal).
      - `reason:'not_claude'` + `isIdentityShapedCached:null` → `"fallback_01"` (probe never ran / failed — treat as terminal, conservative).
      - `reason:'no_tmux_session'` (all three identity-shape values) → `"fallback_01"`.
      - `reason:'exec_error'` (all three identity-shape values) → `"fallback_01"`.
      - `status:'active'` (defensive — attach path only calls on inactive) → `"fallback_01"`.

    Group B — post-holding repoll behavior driven through the EXISTING `__applyRepollResultForTests` seam (proves the attach-path handoff works: after the attach branch flips `changeoverState="holding"` and `holdingReason="discovery_diff"` with `currentSessionFile=null`, the repoll ticker's existing branches recover correctly):
      - Case B1: state `{changeoverState:"holding", currentSessionFile:null, holdingTicks:0, holdingReason:"discovery_diff"}`, tick result `{status:"active", pid:100, sessionFile:"/some/new.jsonl"}` → `transitionToActiveNew` called with `"/some/new.jsonl"`; `transitionToHolding` NOT called (state was already holding); `transitionFromHoldingToActiveSameFile` NOT called (file differs from null).
      - Case B2: state `{changeoverState:"holding", currentSessionFile:null, holdingTicks:0, holdingReason:"discovery_diff"}`, N=199 consecutive `{status:"inactive", reason:"no_pid_session_file"}` ticks → `transitionToDead` NOT called yet, `holdingTicks===199`; one more `no_pid_session_file` tick → `transitionToDead` called with `"holding_timeout"`, `holdingTicks===200`. (Prove `HOLDING_TIMEOUT_TICKS` budget is intact.)
      - Case B3: state `{changeoverState:"holding", currentSessionFile:null, holdingTicks:0, holdingReason:"discovery_diff"}`, tick result `{status:"inactive", reason:"exec_error"}` → `transitionToDead` NOT called, `holdingTicks` UNCHANGED (`0`) — the existing `!isExecErrorTick` guard preserves budget across the attach path too.
      - Case B4: state `{changeoverState:"holding", currentSessionFile:null, holdingTicks:5, holdingReason:"discovery_diff"}`, tick result `{status:"active", pid:100, sessionFile:null}` — skip (impossible; `active` always carries `sessionFile`). Instead a defensive case: state as above, tick `{status:"active", pid:100, sessionFile:"/x.jsonl"}` after 5 prior ticks → `transitionToActiveNew("/x.jsonl")` called; `holdingTicks` value at time of dispatch not asserted (bookkeeping is downstream).

    Group C (integration smoke — one test): manually seed the state box as the attach-path branch would (`changeoverState:"holding"`, `holdingReason:"discovery_diff"`, `holdingTicks:0`, `currentSessionFile:null`), then feed a sequence of ticks through `__applyRepollResultForTests`:
      1. `{status:"inactive", reason:"no_pid_session_file"}` × 2 → holdingTicks becomes 2.
      2. `{status:"inactive", reason:"exec_error"}` × 3 → holdingTicks STILL 2 (transient SSH failures don't burn budget).
      3. `{status:"active", pid:100, sessionFile:"/recovered.jsonl"}` → `transitionToActiveNew` called with `"/recovered.jsonl"`.
      Assert exact call counts + args at each step.

    All tests import from `./claude-session-server.js` following the existing pattern at L23-L28. Reuse the `makeState` and `makeHelpers` factories at the top of the file. `__classifyAttachInactiveForTests` is a pure function — no state box, no helpers. Tests are pure vitest with no ws/ssh2/logger mocks (mirrors the file's existing "function seam" posture).
  </behavior>

  <action>
    Append the three new describe groups (A, B, C) to `src/backend/claude-session/claude-session-server.repoll.test.ts` after the existing `case (e)` describe block at L385. Add the `__classifyAttachInactiveForTests` name to the import list at L24-28.

    Format follows the existing style: bordered comment header (`// ── Case ... ────────`), `describe(...)` with a descriptive suffix, `it(...)` bodies with explicit expectations. Reuse `makeState`/`makeHelpers`/`SESSION_FILE` factories at the top of the file.

    For Group B Case B2, use a loop (`for (let i = 0; i < 199; i++)`) mirroring the case (d) pattern at L314-321. Assert `state.holdingTicks === 199` mid-loop, then fire the 200th tick and assert `transitionToDead` was called with `"holding_timeout"` and `state.holdingTicks === 200`. Do NOT hardcode 200 — import `HOLDING_TIMEOUT_TICKS` or reference it via a local `const TIMEOUT = 200;` with a comment tying it to `claude-session-server.ts:187`. If importing the constant requires re-exporting from server.ts, prefer the local const with the comment (surgical — matches the "don't modify HOLDING_TIMEOUT_TICKS" constraint).

    For Group C, chain calls to `__applyRepollResultForTests` in one `it` body with the same `state` + `stubs` across all three steps. Use `vi.fn.mock.calls.length` and `.mock.calls[N][0]` to assert per-step behavior. Wire `transitionToActiveNew` as a stub that also does `state.changeoverState = "active"; state.currentSessionFile = result.sessionFile;` so if the test accidentally fires a 4th tick after recovery, downstream branches read the right state (defensive; the 3-step test doesn't fire a 4th, but this makes the test robust to expansion).

    Do NOT add tests that spin up a real WS/ssh2 pair — the "function seam" posture (see L863-878 in claude-session-server.ts) is the file's contract.
    Do NOT modify existing tests in the file.
    Do NOT modify `__RepollStateForTests` or `__RepollHelpersForTests` shapes.
  </action>

  <verify>
    <automated>cd /home/ubuntu/skynet-tiffany &amp;&amp; npx vitest run src/backend/claude-session/claude-session-server.repoll.test.ts 2>&amp;1 | tail -40 &amp;&amp; npm run build:backend 2>&amp;1 | tail -10</automated>
  </verify>

  <done>
    - `npx vitest run src/backend/claude-session/claude-session-server.repoll.test.ts` reports ALL tests passing (existing + newly added Group A, B, C).
    - Group A covers the 12 classifier cases enumerated in <behavior>.
    - Group B covers the four post-holding recovery/timeout cases enumerated in <behavior>.
    - Group C runs the mixed-sequence integration smoke test end-to-end.
    - `npm run build:backend` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend WS client → backend claude-session-server | Client sends `connectToPane {hostId, tmuxSession}`; backend replies with pane-state frames. The classifier reads only backend-computed state — no client input reaches the new branch. |
| Backend → remote SSH | `discoverClaudeSession` result carries `reason` from the backend's own SSH probes, not client input. No new SSH commands issued by this change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260813-0qx-01 | Denial of Service | New attach-path holding path keeps `sshConn` open for up to 10min per stuck attach; a malicious client could deactivate/reactivate to hold connections open | accept | Same 10min budget the steady-state `HOLDING_TIMEOUT_TICKS` uses; per-connection state; user-facing surface (Ashley on her own workstation) is not an untrusted multi-tenant client. Existing WS ping/pong (L2597-L2620) reaps dead connections. |
| T-260813-0qx-02 | Information Disclosure | New `sshLogger.info("...attach_reset_window", {reason, identityShape})` payload leaks discovery reason + identity-shape verdict | accept | Both fields already appear in existing sshLogger payloads (see the identity-shape probe log at L4839 and repoll's `claude_session_holding` log at L2381). No new information class. |
| T-260813-0qx-03 | Tampering | Refactored/copied `discoveryRepollTimer` block could diverge from steady-state behavior over time (Option C inlined-copy risk) | mitigate | Prefer Option A (shared helper). If Option C is chosen, both sites carry a `// COPY ... keep in sync` comment. Group C integration test in Task 2 exercises the timer body through the existing `__applyRepollResultForTests` seam, catching drift in the reducer contract. |
| T-260813-0qx-04 | Denial of Service | If the reset-window classifier fires on a truly-terminal-but-misclassified reason (e.g. a not_claude on a permanently non-identity pane where the dormant probe threw), the WS holds for 10min instead of failing fast | mitigate | The `not_claude` branch requires `isIdentityShapedCached === true`; the dormant-probe SSH-throw catch at L5078-L5081 sets `isIdentityShapedCached = false`, so any probe failure defaults to FALLBACK-01. Group A Case `not_claude + isIdentityShapedCached:null → fallback_01` locks this in as a regression guard. |
</threat_model>

<verification>
Phase-level checks (executor runs these before commit):
- `cd /home/ubuntu/skynet-tiffany && npx vitest run src/backend/claude-session/claude-session-server.repoll.test.ts` → all tests pass (existing 5 case groups + new Groups A, B, C).
- `cd /home/ubuntu/skynet-tiffany && npx vitest run src/backend/claude-session/` → the wider claude-session directory suite (pane-state-emitter, session-file-discovery, layer1-detect, dormant-tail, etc.) still green — this change is additive to the attach-path branch and adds a new pure classifier + optional shared helper; nothing in the wider suite should regress.
- `cd /home/ubuntu/skynet-tiffany && npm run build:backend` → exit 0 (backend TypeScript compile passes; needed because `.ts` in `src/backend/` is edited).

NOT included (out of scope per constraints):
- No `git push`, no `docker build`, no `docker compose up`. Deploy is orchestrator-only.
- No frontend changes → no frontend build/test invocation.
</verification>

<success_criteria>
- On initial-attach `discoverClaudeSession` returning `{status:"inactive", reason:"no_pid_session_file"}` (or `"no_open_session_file"`, or `"not_claude"` on identity-shape pane without `.dormant`), the WS enters holding + starts the discovery-repoll timer instead of FALLBACK-01.
- On initial-attach with `reason:"no_tmux_session"` or `"exec_error"` (or `"not_claude"` on a non-identity-shape pane), FALLBACK-01 runs unchanged.
- After entering holding via the new attach-path branch, the existing repoll timer recovers the WS on active-with-new-file or times out to terminal-inactive at `HOLDING_TIMEOUT_TICKS`.
- Existing repoll tests still pass (no regression to steady-state behavior).
- New tests cover the 12-case classifier truth table + four post-holding repoll cases + one integration smoke test.
- `npm run build:backend` exits 0.
- No new wire-frame types; no frontend edits; `HOLDING_TIMEOUT_TICKS` unchanged.
</success_criteria>

<output>
Create `.planning/quick/260813-0qx-deactivate-reactivate-during-reset-latch/260813-0qx-SUMMARY.md` when done.
</output>
