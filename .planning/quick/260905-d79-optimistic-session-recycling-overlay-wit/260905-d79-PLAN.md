---
phase: 260905-d79-optimistic-session-recycling-overlay-wit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/ComposeBox.tsx
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-view/PrettyView.session-rotation.test.tsx
  - src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx
autonomous: true
requirements:
  - QUICK-260905-d79
must_haves:
  truths:
    - "Clicking reset on a connected socket mounts SessionHoldingOverlay immediately, before any backend recycling frame lands."
    - "Clicking reset on a disconnected socket (funnel.send returns false) does NOT mount SessionHoldingOverlay."
    - "When the backend wire flips isRecycling=true, the overlay stays mounted continuously; when it subsequently flips isRecycling=false with no optimistic slot active, the overlay unmounts."
    - "When neither the backend recycling frame ever lands nor another reset click occurs, the optimistic slot self-clears after 10 minutes and the overlay unmounts."
    - "ComposeBox's send/reset/paperclip/ThumbsUp/Lightbulb/Queue/Mic controls are disabled optimistically the same moment the overlay mounts optimistically (isHolding and recycleActive both track effectiveRecycling)."
  artifacts:
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "optimisticRecycling state + timer + effectiveRecycling composite; overlay + ComposeBox props wired to effectiveRecycling"
      contains: "effectiveRecycling"
    - path: "src/ui/features/pretty-view/ComposeBox.tsx"
      provides: "onResetClicked?.() moved from fireResetSyncFx into dispatchResetPayload's dispatched-branch"
      contains: "dispatchResetPayload"
    - path: "src/ui/features/pretty-view/PrettyView.session-rotation.test.tsx"
      provides: "three new cases covering optimistic mount, backend takeover + subsequent clear, and 10-min self-clear"
    - path: "src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx"
      provides: "dispatch-success-vs-fail case for onResetClicked firing"
  key_links:
    - from: "src/ui/features/pretty-view/ComposeBox.tsx (dispatchResetPayload)"
      to: "src/ui/features/pretty-view/PrettyView.tsx (onResetClicked prop -> setOptimisticRecycling)"
      via: "onResetClicked?.() called inside if (dispatched) branch, after funnel.send returns true"
      pattern: "if \\(dispatched\\)[\\s\\S]{0,120}onResetClicked"
    - from: "src/ui/features/pretty-view/PrettyView.tsx (effectiveRecycling)"
      to: "SessionHoldingOverlay mount + ComposeBox isHolding + ComposeBox recycleActive"
      via: "single composite `isRecycling || optimisticRecycling` consumed at all three sites"
      pattern: "effectiveRecycling"
---

<objective>
Reintroduce the documented client-hint-with-backend-override fallback for `SessionHoldingOverlay`, extended with a 10-min upper bound self-clear. Front-runs the backend `recycling` axis with a per-pane React state slot flipped ONLY on reset-dispatch success, cleared early when the backend authoritative frame arrives, and cleared unconditionally after 10 minutes if the backend signal never arrives.

Purpose: closes the UAT-surfaced visible gap between reset-button press and overlay mount (currently a few seconds while source B's SSH poll + JSONL tail scan complete), while preserving the "backend is source of truth" invariant — the optimistic slot only extends visibility, never contradicts an authoritative frame.

Output: 4 files modified, atomic single commit, scoped vitest green on the three test files, tsc clean.

**FULL specification lives in `TASK.md` in this same directory. Read it in full before starting — it contains exact code shapes, line numbers, placement guidance, and rationale. This PLAN.md is a wrapper, not a restatement.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/quick/260905-d79-optimistic-session-recycling-overlay-wit/TASK.md
@src/ui/features/pretty-view/PrettyView.tsx
@src/ui/features/pretty-view/ComposeBox.tsx
@src/ui/features/pretty-view/PrettyView.session-rotation.test.tsx
@src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implement optimistic recycling overlay + extend scoped tests</name>
  <files>src/ui/features/pretty-view/ComposeBox.tsx, src/ui/features/pretty-view/PrettyView.tsx, src/ui/features/pretty-view/PrettyView.session-rotation.test.tsx, src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx</files>
  <action>
    Follow TASK.md in this quick-task's directory as the exact spec. Summary of the four edits (do them in this order; commit as one atomic commit at the end):

    (a) `src/ui/features/pretty-view/ComposeBox.tsx` — In `fireResetSyncFx` (around line 1764), REMOVE the `onResetClicked?.()` call currently at line 1771. All other fireResetSyncFx behavior stays exactly as-is (source-scoped cancel, setErrorMessage(null), the entire drain-sweep + pulse timer block). In `dispatchResetPayload(body)` (around line 1808), inside the `if (dispatched) { ... }` branch (currently just `setText(""); clearAfterSend();`), add `onResetClicked?.();` as the FIRST statement in the branch — before `setText("")`. Do NOT add it to the `else` branch. Update the comment block above `fireResetSyncFx` (lines 1761-1770) to state the new invariant: `onResetClicked` now fires on dispatch success (not click) so a disconnected socket does not falsely mount the overlay for 10 minutes; the drain-sweep/pulse animation still fires unconditionally on click as an always-fire visual affordance. Keep patch #122 attribution wording accurate to the new reality.

    (b) `src/ui/features/pretty-view/PrettyView.tsx` — Replace the no-op `onResetClicked` at line 768 AND its entire preceding comment block (lines 738-767) with the real implementation given in TASK.md § Change 2: a `[optimisticRecycling, setOptimisticRecycling]` `useState(false)` slot, an `optimisticRecyclingTimerRef` `useRef<ReturnType<typeof setTimeout> | null>(null)`, a `useCallback` body that clears any prior timer, flips optimistic=true, and arms a 10*60*1000 ms setTimeout to flip it back to false. Place the state + ref slots near the other pane-scoped state slots around lines 715-736 (the `dormant`, `showResolvingSpinner`, `autoplayArmed` cluster is the natural home). Add TWO `useEffect`s: (1) unmount-cleanup that clears the timer, (2) backend-takeover clear that watches `isRecycling` — when it flips true, clear the timer and reset optimistic to false. Both effects can live adjacent to the `useCallback` or wherever the surrounding pane-scoped effects sit. Replace the deleted comment block with a fresh comment that: (i) records the client-hint-with-backend-override design is now implemented (was deferred at Phase 30 PS30-05), (ii) names the 10-min upper bound as the safety net for "backend authoritative frame never arrives", (iii) explains why this is per-pane React state and NOT promoted to session-working-store (Ashley's explicit decision this session — transcript-detection path in source B catches JSONL append within a few seconds, so "user switches tabs mid-recycle" gap is small enough to accept losing overlay-across-mount).

    Then insert `const effectiveRecycling = isRecycling || optimisticRecycling;` on the line IMMEDIATELY AFTER `const isRecycling = useSessionIsRecycling(sessionWorkingKey);` at line 1294. Update the THREE consumer sites (verify line numbers by grep — the additions shift them):
      - The mount gate `{isRecycling && <SessionHoldingOverlay />}` (~line 3069): change to `{effectiveRecycling && <SessionHoldingOverlay />}`.
      - `isHolding={isRecycling}` (~line 3455): change to `isHolding={effectiveRecycling}`.
      - `recycleActive={isRecycling}` (~line 3460): change to `recycleActive={effectiveRecycling}`.
    Do NOT change any OTHER `isRecycling` references (there are internal ref/derivation sites — leave them alone). Update the mount-gate comment block (lines 3057-3068) and the isHolding/recycleActive comment blocks (lines 3451-3460) to name `effectiveRecycling` as the new source and cite that `effectiveRecycling = isRecycling || optimisticRecycling` per the quick 260905-d79 UAT fix. Preserve the existing "one source for all three sites" invariant — now the single source is `effectiveRecycling`.

    (c) `src/ui/features/pretty-view/PrettyView.session-rotation.test.tsx` — Add THREE cases per TASK.md § Change 3. Reuse existing describe blocks / test harness patterns already in the file (import them, do not reinvent). The three cases:
      1. Reset click fires `onResetClicked` → SessionHoldingOverlay mounts immediately without any wire frame having been published. Assert overlay presence via the same query pattern existing tests use.
      2. After optimistic mount, publish a wire frame with `recycling: true` for the pane's session key — overlay stays mounted. Then publish a subsequent wire frame with `recycling: false` — overlay MUST unmount. (If the optimistic slot were still set at this point, the overlay would remain and the assertion fails — that's the invariant this case protects.)
      3. Optimistic mount with NO backend signal → advance fake timers by `10 * 60 * 1000 + 1` → overlay unmounts. Use `vi.useFakeTimers()` / `vi.advanceTimersByTime` matching the file's existing timer-mocking patterns; if the file does not already use fake timers, scope them per-test with `beforeEach`/`afterEach` to avoid leaking into other tests.

    (d) `src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx` — First grep this file (and any adjacent `ComposeBox.*.test.tsx`) for existing coverage of `onResetClicked` firing. If a test currently asserts `onResetClicked` fires on reset click regardless of dispatch outcome, ADJUST it to assert it fires on dispatch SUCCESS only, and add a companion assertion that a failed dispatch (funnel.send returns false — mock/stub the funnel to return false) does NOT fire `onResetClicked`. If no such coverage exists, add a fresh test in send-funnel.test.tsx with two adjacent cases: (i) reset click when funnel.send returns true → `onResetClicked` called once, (ii) reset click when funnel.send returns false → `onResetClicked` NOT called. Match existing test file's fixture/mocking patterns for funnel setup.

    Do NOT create any new test files. Do NOT modify SessionHoldingOverlay.tsx, useSessionIsRecycling hook, or any backend files. Do NOT touch worktrees. Do NOT `git push`, `docker build`, or `docker compose up`.

    Before committing: `git pull --rebase origin feat/tab-title-from-tmux` (branch may have moved during this session per TASK.md § Constraints). Then commit with message: `feat(pretty-view): optimistic session-recycling overlay with 10-min upper bound`. Single atomic commit — all four file edits in one commit.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tanya &amp;&amp; npx vitest run src/ui/features/pretty-view/PrettyView.session-rotation.test.tsx src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>
    - `onResetClicked?.()` no longer appears inside `fireResetSyncFx`; it appears exactly once, as the first statement of the `if (dispatched)` branch inside `dispatchResetPayload`.
    - `PrettyView.tsx` has: `optimisticRecycling` useState, `optimisticRecyclingTimerRef` useRef, a `useCallback` `onResetClicked` that arms a 10-min timeout, an unmount cleanup useEffect, a backend-takeover clearing useEffect watching `isRecycling`, and `const effectiveRecycling = isRecycling || optimisticRecycling;` right after line 1294's `useSessionIsRecycling` hook call.
    - The SessionHoldingOverlay mount gate AND both ComposeBox props (`isHolding`, `recycleActive`) all read from `effectiveRecycling`. No other `isRecycling` sites were changed.
    - All three new cases in `PrettyView.session-rotation.test.tsx` pass under `npx vitest run` scoped to that file.
    - The dispatch-success-vs-fail case in `ComposeBox.send-funnel.test.tsx` (adjusted or newly added) passes.
    - `npx vitest run` on the two scoped test files exits 0. `npx tsc --noEmit` exits 0.
    - Single atomic commit landed on `feat/tab-title-from-tmux` (after rebase) with message `feat(pretty-view): optimistic session-recycling overlay with 10-min upper bound`. No `git push`, no docker steps.
  </done>
</task>

</tasks>

<verification>
Executor-side green-gate (per TASK.md § Constraints):
1. `npx vitest run src/ui/features/pretty-view/PrettyView.session-rotation.test.tsx src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx` exits 0.
2. `npx tsc --noEmit` exits 0.
3. Grep sanity: `grep -n "effectiveRecycling" src/ui/features/pretty-view/PrettyView.tsx` returns at least 4 hits (declaration + 3 consumer sites).
4. Grep sanity: `grep -n "onResetClicked" src/ui/features/pretty-view/ComposeBox.tsx | grep -v "^#"` shows `onResetClicked?.()` inside the dispatched branch of `dispatchResetPayload` and NOT inside `fireResetSyncFx`.

Full vitest suite, `docker build`, `docker compose up`, and `git push` are all orchestrator-only — out of scope for this executor.
</verification>

<success_criteria>
- All 5 truths in `must_haves.truths` are demonstrable via the scoped vitest run.
- Zero test regressions in the two touched test files.
- `tsc --noEmit` clean (frontend type surface unchanged in shape — added a `useState<boolean>`, a `useRef` with explicit annotation, and a `const` composite; no prop-contract changes).
- Atomic single commit on `feat/tab-title-from-tmux` (rebased first).
- No worktrees created. No push. No docker steps.
</success_criteria>

<output>
Create `.planning/quick/260905-d79-optimistic-session-recycling-overlay-wit/260905-d79-01-SUMMARY.md` when done, recording: files changed, commit SHA, scoped vitest + tsc exit codes, and any deviation from TASK.md (there should be none).
</output>
