---
phase: 260730-o2m-context-menu-strip
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-conversations/PrettyConversationRow.tsx
  - src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx
  - src/ui/features/pretty-conversations/PrettyConversationContextMenu.test.tsx
autonomous: true
requirements:
  - 260730-o2m-01
  - 260730-o2m-02
must_haves:
  truths:
    - "Fresh page load with NO `?menu=context` query or hash gives every DESKTOP non-RDP row the context-menu behavior: `PinAction` + `DeactivateAction` are ABSENT from `.pv-meta`, and `onContextMenu` is wired on the row body."
    - "Mobile swipe-strip continues to render `PinAction` (always) and `DeactivateAction` (when `inActiveSet`) inside the reveal strip — the mobile behavior is untouched by this strip."
    - "Desktop RDP rows continue to render no `PinAction` and no `DeactivateAction` and no context-menu wiring — the `isRdp` guard survives verbatim."
    - "No source file under `src/` references `CONV_MENU_VARIANT`, `readConvMenuVariant`, `?menu=context`, or `#menu=context` anymore (dead-code / dead-comment sweep is complete)."
    - "`PrettyConversationRow.tsx` still exports the same `PrettyConversationRow` component with the same prop shape — no consumer-visible API break for `PrettyConversationsPanel`."
    - "`PrettyConversationRow.test.tsx` vitest run reports 0 failed; existing tests are updated where they asserted the pre-strip (desktop icon-visible) behavior."
    - "`PrettyConversationContextMenu.test.tsx` is a fresh file with a passing vitest suite covering portal mount, viewport clamp on right + bottom edges, Escape dismiss, outside-click dismiss, and item-click callback + dismiss."
    - "`PrettyConversationRow` right-click context menu still opens at cursor coords, still portal-mounts `PrettyConversationContextMenu` into `document.body`, and still filters items by `inActiveSet` (`Deactivate` gated) — the behavior formerly gated behind `CONV_MENU_VARIANT === 'context'` is now unconditional for desktop non-RDP rows."
  artifacts:
    - path: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx"
      provides: "PrettyConversationRow with context-menu behavior as sole default on desktop non-RDP rows; no URL-param gate; mobile swipe-strip untouched; RDP guard preserved"
      contains: "PrettyConversationContextMenu"
    - path: "src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx"
      provides: "Updated 23-test suite asserting the new default: desktop non-RDP rows have NO PinAction/DeactivateAction in .pv-meta, onContextMenu is wired on the row body, mobile swipe-strip still shows"
      contains: "onContextMenu"
    - path: "src/ui/features/pretty-conversations/PrettyConversationContextMenu.test.tsx"
      provides: "Fresh test file (fork-new component from patch #203 had zero coverage) — covers portal mount, viewport clamp, Escape dismiss, outside-click dismiss, item-click dispatches callback + dismisses"
      contains: "PrettyConversationContextMenu"
  key_links:
    - from: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx (row body, ~line 449)"
      to: "onRowContextMenu callback"
      via: "unconditional onContextMenu handler on desktop non-RDP rows (isMobile === false && isRdp === false)"
      pattern: "onContextMenu=\\{[^}]*!isMobile"
    - from: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx (~line 542)"
      to: "DeactivateAction + PinAction in .pv-meta"
      via: "DELETED: desktop non-RDP branch no longer renders these; only mobile swipe strip renders them now"
      pattern: "DeactivateAction|PinAction"
    - from: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx (~line 611)"
      to: "PrettyConversationContextMenu portal render"
      via: "unconditional on desktop non-RDP rows when ctxMenu !== null"
      pattern: "ctxMenu !== null && !isRdp"
---

<objective>
Strip the `?menu=context` URL-param gate from `PrettyConversationRow.tsx` and promote the context-menu behavior to the sole, unconditional default for desktop non-RDP rows. Ashley confirmed via the gated URL that the context-menu variant works; this quick task rips out the `readConvMenuVariant` reader, the module-scope `CONV_MENU_VARIANT` const, and every `CONV_MENU_VARIANT === 'context'` conditional so every fresh page load gets the context-menu-only meta column without needing the URL flag. Mobile swipe-strip and the `isRdp` guard stay untouched.

Purpose: Close the two-variant fork in `PrettyConversationRow.tsx` that shipped as patch #203 (context-menu behind `?menu=context`). Retaining both variants is a maintenance and cognitive tax, and — the load-bearing piece — patch #203 shipped with ZERO tests exercising the context-menu path because jsdom's `window.location.search` starts empty, so `CONV_MENU_VARIANT === 'default'` at test time and every existing test hits the pre-strip code path. This task deletes the loser, keeps the winner, updates the existing `PrettyConversationRow.test.tsx` where it asserted the pre-strip meta-icon rendering, and writes a fresh `PrettyConversationContextMenu.test.tsx` for the fork-new component that shipped with no coverage.

Output: Single-file rewrite of `PrettyConversationRow.tsx` (delete `readConvMenuVariant`, `CONV_MENU_VARIANT`, every `CONV_MENU_VARIANT === 'context'` conditional; also drop the comment at ~line 84 that self-references the deleted const), targeted edits to `PrettyConversationRow.test.tsx` to cover the new default behavior on desktop non-RDP rows, and a new `PrettyConversationContextMenu.test.tsx` file covering the fork-new context menu component. Two atomic commits on `feat/tab-title-from-tmux`. NO push, NO docker build, NO deploy — commit-only, per fleet rule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

@src/ui/features/pretty-conversations/PrettyConversationRow.tsx
@src/ui/features/pretty-conversations/PrettyConversationContextMenu.tsx
@src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Strip URL-param gate + promote context-menu to sole desktop non-RDP default</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationRow.tsx</files>
  <action>
    Edit `src/ui/features/pretty-conversations/PrettyConversationRow.tsx` — rewrite the file so the context-menu behavior is unconditional on desktop non-RDP rows. Concrete steps:

    1. Delete the entire URL-param gate machinery at the top of the file (currently lines ~79-101): the block comment "URL-param gate for the desktop right-click context-menu variant (bounty #6, ?menu=context)…" through the `Read ONCE at module load…` prose, the `readConvMenuVariant()` function body, AND the module-scope `const CONV_MENU_VARIANT = readConvMenuVariant();` line. All of it goes — no re-export alias, no keep-under-a-new-name. Nothing else at the module scope depends on either symbol (grep before deleting: `grep -n 'readConvMenuVariant\|CONV_MENU_VARIANT' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` must show only the definition site + the three conditional call sites in the render body — all of which are being rewritten below).

    2. Rewrite the row-body `onContextMenu` prop (currently at ~line 449) from:
       `onContextMenu={CONV_MENU_VARIANT === "context" && !isMobile && !isRdp ? onRowContextMenu : undefined}`
       to:
       `onContextMenu={!isMobile && !isRdp ? onRowContextMenu : undefined}`
       The `!isMobile && !isRdp` guard MUST stay — mobile still uses swipe-strip actions (no context menu), and RDP rows have no actions at all (isRdp guard is load-bearing per `T-Test-34` in the existing test suite header comment). Do NOT change the `onRowContextMenu` callback body itself (lines ~333-343) — it already does the correct `e.preventDefault(); e.stopPropagation(); setCtxMenu(…)` shape and stays as-is.

    3. Delete the `.pv-meta` gate for `DeactivateAction` (currently lines ~542-551): the `CONV_MENU_VARIANT !== "context" && !isMobile && !isRdp && inActiveSet && (<DeactivateAction … />)` block goes away entirely. Desktop non-RDP active-set rows will no longer render an inline DeactivateAction in the meta column — Deactivate now lives ONLY in the right-click menu (built by `items[]` in the render tree at ~line 616) and in the mobile swipe strip (unchanged).

    4. Delete the `.pv-meta` gate for `PinAction` (currently lines ~552-559): the `CONV_MENU_VARIANT !== "context" && !isMobile && !isRdp && (<PinAction … />)` block goes away entirely. Desktop non-RDP rows will no longer render an inline PinAction in the meta column — Pin now lives ONLY in the right-click menu and in the mobile swipe strip (unchanged).

    5. Do NOT touch the mobile swipe-strip render block (currently lines ~417-437). That block wires `PinAction` (always for mobile non-RDP) and `DeactivateAction` (mobile non-RDP + inActiveSet) inside the swipe reveal, and it is gated on `isMobile && !isRdp` — completely orthogonal to the URL-param strip. Leave the JSX exactly as-is.

    6. Delete the surrounding comment "Bounty #6 (?menu=context): when the context-menu variant is active, the always-visible desktop Pin + Deactivate icons are suppressed here — both actions move into the right-click menu wired on the row body via onRowContextMenu. Default variant keeps them visible so the URL-param opt-in leaves everyone else's UI untouched." at ~lines 536-541 — the two conditional branches it describes are gone, so the comment is meaningless. Also delete the sibling comment at ~lines 529-535 ("quick-260727-gm3: DeactivateAction renders BEFORE PinAction in DOM order on desktop active-set non-RDP rows…") — with both desktop meta icons deleted, that DOM-order note has no referent. The remaining `.pv-meta` render (`PrettyBountyCountBadge` + ready-dot) does not need either of those comments.

    7. Rewrite the row-body `onContextMenu` comment context: The row body render (~lines 443-460) is unchanged in shape except for the single-line `onContextMenu` rewrite in step 2. No new inline comment is needed there — the behavior is now the sole default and the guard reads self-documentingly (`!isMobile && !isRdp ? onRowContextMenu : undefined`).

    8. Rewrite the `ctxMenu` state comment (currently ~lines 327-329): the current comment reads "Bounty #6 (?menu=context): right-click context menu state. Only used when CONV_MENU_VARIANT === 'context' AND !isMobile AND !isRdp. Coords are cursor position at contextmenu time; null = menu closed." — rewrite as "Right-click context menu state for desktop non-RDP rows. Coords are cursor position at contextmenu time; null = menu closed." Drop the bounty and variant references.

    9. Rewrite the portal-render comment (currently ~lines 605-610): the current comment reads "Bounty #6 (?menu=context): right-click menu portal. Items filter by the same predicates that gate the (now-hidden) desktop icons: Pin renders for any non-RDP row (matches the always-visible desktop pin condition), Deactivate only when inActiveSet. RDP rows never open the menu (onRowContextMenu is only wired when !isRdp), so items[] can safely assume non-RDP." — rewrite as "Right-click menu portal. Items filter by row eligibility: Pin renders for any non-RDP row (always available); Deactivate only when inActiveSet. RDP rows never open the menu (onRowContextMenu is not wired when isRdp), so items[] can safely assume non-RDP." Drop the bounty and gate-history references; keep the items[] contract prose.

    10. Rewrite the portal-render conditional (currently ~line 611): from `{CONV_MENU_VARIANT === "context" && ctxMenu !== null && !isRdp && ( … )}` to `{ctxMenu !== null && !isRdp && ( … )}`. The `!isRdp` guard stays — same load-bearing rationale as step 2. Do NOT add `!isMobile` here: `ctxMenu` is only ever set by `onRowContextMenu`, and after step 2 `onRowContextMenu` is only wired when `!isMobile`, so `ctxMenu !== null` on mobile is a state-machine impossibility. Adding `!isMobile` here would be redundant and would misleadingly suggest a code path that does not exist.

    11. Also update the top-of-file block comment header if it back-references bounty #6 or the URL-param variant machinery in a way that is now stale. The current header comment (lines 1-46) describes the row's general structure (swipe reveal state machine, ready-dot conditional render, avatar image, click/keyboard/touch handlers, `--pv-hue` inline). None of it references bounty #6 or `CONV_MENU_VARIANT`, so no header edit is needed — verify with `grep -n 'bounty.*6\|menu=context\|CONV_MENU_VARIANT\|readConvMenuVariant' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` after edits and if any residue lives outside the render body, remove or rewrite it. The expected final grep count for those tokens is exactly 0.

    12. Sanity check the file compiles: `npx tsc --noEmit` MUST exit 0 after edits. The unused imports (`PinAction`, `DeactivateAction` from `./PinAction` / `./DeactivateAction` at lines ~66-67) are STILL used by the mobile swipe strip block (unchanged), so do NOT delete them. `PrettyConversationContextMenu` + `PrettyContextMenuItem` imports (lines ~69-72) are still used by the portal render — keep. `useState` at line 51 is still used for `swipedOpen`, `dxLive`, and `ctxMenu` — keep. Verify no import becomes unused.

    Grep gates before finishing this file:
    - `grep -c 'CONV_MENU_VARIANT\|readConvMenuVariant\|menu=context' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` MUST return 0.
    - `grep -c 'PrettyConversationContextMenu' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` MUST return >= 2 (one import, one JSX use — the portal render survives).
    - `grep -c 'isMobile && !isRdp' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` MUST return >= 4 (four onTouch* wire sites in the mobile swipe branch — unchanged by this task; the pattern proves the mobile branch is still intact).
    - `grep -c '!isMobile && !isRdp' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` MUST return >= 1 (the new unconditional onContextMenu guard from step 2).
    - `grep -c 'DeactivateAction' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` MUST return >= 2 (one import, one JSX use — mobile swipe strip only; desktop meta-column render is deleted).
    - `grep -c 'PinAction' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` MUST return >= 2 (one import, one JSX use — mobile swipe strip only; desktop meta-column render is deleted).

    Global scope guards for this task:
    - Do NOT touch any file under `src/backend/`.
    - Do NOT touch `~/.claude/identities/tina/skynet-patches.md`, any bounty file under `~/.claude/identities/tina/bounties/`, or `~/.claude/identities/tina/tina.md`. Those are Tina's orchestrator-side job AFTER this executor returns.
    - Do NOT `git push`, do NOT `docker build`, do NOT `docker compose up`.
    - Do NOT touch `PrettyConversationRow.test.tsx` or `PrettyConversationContextMenu.test.tsx` in this commit — those are Task 2. Task 1 is source-only.
    - Do NOT touch `PrettyConversationContextMenu.tsx` (the component itself is not being changed; the URL-param gate is on the CONSUMER side in `PrettyConversationRow.tsx` only).
    - Do NOT touch `PrettyConversationsPanel.tsx` (consumer of `PrettyConversationRow` — the prop shape is unchanged, so no consumer edits are needed).

    Commit boundary for Task 1: one atomic commit on `feat/tab-title-from-tmux` with message `refactor(pretty-conversations): promote context-menu to unconditional default; delete ?menu=context gate`. The test file changes are Task 2's separate commit — do NOT include test changes in this commit.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; grep -v '^\s*//' src/ui/features/pretty-conversations/PrettyConversationRow.tsx | grep -c 'CONV_MENU_VARIANT\|readConvMenuVariant\|menu=context' &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - `grep -c 'CONV_MENU_VARIANT\|readConvMenuVariant\|menu=context' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` returns 0 (gate residue fully purged from source and comments).
    - `grep -c '!isMobile && !isRdp' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` returns >= 1 (the new unconditional onContextMenu guard).
    - `grep -c 'PrettyConversationContextMenu' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` returns >= 2 (import + portal JSX render survive).
    - `grep -c 'DeactivateAction\|PinAction' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` returns >= 4 (two imports + two mobile-swipe-strip JSX uses; desktop meta-column renders are gone).
    - `npx tsc --noEmit` exits 0 (no type errors from the gate deletion or comment edits; imports still resolve).
    - `git diff --stat` for this commit shows exactly one file: `PrettyConversationRow.tsx`. NO test files. NO backend files. NO identity-side files.
    - Atomic commit on `feat/tab-title-from-tmux`; `git log --oneline -1` shows the commit; branch remains unpushed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update PrettyConversationRow.test.tsx for new default + write fresh PrettyConversationContextMenu.test.tsx</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx, src/ui/features/pretty-conversations/PrettyConversationContextMenu.test.tsx</files>
  <action>
    Two files in this task. First: the source file's existing 23 tests assume the pre-strip behavior (desktop icons visible). Several tests currently assert on `[data-testid="pin-action"]` presence in the desktop meta column — after Task 1 those assertions are wrong. Second: `PrettyConversationContextMenu.tsx` shipped as fork-new in patch #203 with zero tests; write a fresh suite.

    ─── Part A: Update `src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` ───

    1. Read the current test file end-to-end before editing. The suite has 23 `it()` blocks numbered Test 1 through Test 19C (with 7b, 18b, 19A/B/C sub-blocks). The header comment at lines 1-53 enumerates them. Update the header comment to reflect the new default: after "18 (+18b) tests covering the row component…" preamble, add a new paragraph noting "As of quick-260730-o2m, `PinAction` + `DeactivateAction` no longer render in the desktop `.pv-meta` column — those actions moved into the right-click context menu (unconditional on desktop non-RDP rows). Mobile swipe-strip render is unchanged. Tests that previously asserted `[data-testid=\"pin-action\"]` presence on desktop rows now assert absence + assert the row body carries an `onContextMenu` handler instead."

    2. Test 8 (line ~447-470, "Test 8: desktop pin click fires onTogglePin only, not onSelect"): this test rendered a desktop row and asserted a queryable `[data-testid="pin-action"]` element, then clicked it. Post-strip, the pin-action is no longer rendered in `.pv-meta` on desktop. Rewrite Test 8 as: "Test 8: desktop non-RDP row wires `onContextMenu` on the row body; dispatching a `contextmenu` event opens the context menu portal with `Pin` item; clicking the `Pin` menu item fires `onTogglePin` and NOT `onSelect` (item click stopPropagation still holds via PrettyConversationContextMenu's onClick handler)." Concretely: use `@testing-library/react`'s `fireEvent.contextMenu(body, { clientX: 100, clientY: 100 })` on the row body, then `screen.getByRole("menuitem", { name: /pin/i })` to find the Pin item in the portal (the menu is portal-mounted to `document.body`, so use `screen` — not `container` — to query it), `fireEvent.click(pinItem)`, and assert `onTogglePin` called once + `onSelect` not called. Note: `PrettyConversationContextMenu` uses `useLayoutEffect` for viewport clamping which reads `getBoundingClientRect` — jsdom returns zeros there, so the clamp is a no-op and the menu renders at (clientX, clientY). Document this in an inline comment.

    3. Test 10 (line ~507-533, "Test 10: pinned desktop row carries `pinned` class + PinAction in DOM"): the second half of this test asserts `container.querySelector('[data-testid="pin-action"]')` is truthy on a pinned desktop row. Post-strip, that assertion is false. Rewrite the second half to assert:
       - The `pinned` class assertion on the row body stays (that is a CSS-driven visual state on the row, not the pin button — the row itself is `.pinned` regardless of where the pin action lives).
       - Delete the `[data-testid="pin-action"]` presence check.
       - Add: dispatch a `contextmenu` event on the row body, assert the portal opens with a menu item whose text matches `/unpin/i` (because `pinned={true}` — see PrettyConversationRow.tsx `items.push({ label: pinned ? "Unpin" : "Pin", … })` at line ~618-620). This proves the pinned state flows through to the context menu label correctly, replacing the deleted pin-in-meta-column assertion.

    4. Test 7 (line ~385-441, "Test 7: mobile RDP row carries `rdp` class + skips swipe + no PinAction" / Test 7b: "desktop RDP row carries `rdp` class + no PinAction"): the "no PinAction" invariant is stronger post-strip on desktop — RDP rows had no PinAction before AND non-RDP desktop rows now also have no PinAction in `.pv-meta`. Test 7 (mobile) stays exactly as-is (mobile swipe-strip PinAction is gated on non-RDP, so RDP mobile still has no PinAction — the mobile branch of PinAction is not affected by this task). Test 7b (desktop RDP): keep the `expect(wrapper.querySelector('[data-testid="pin-action"]')).toBeNull();` assertion — it still passes (RDP row is still !isMobile && isRdp → the mobile swipe strip is skipped AND the desktop meta-column render was deleted, so the assertion holds by both gates). Add one more assertion to Test 7b: `expect(body.getAttribute("oncontextmenu")).toBeNull();` — RDP rows do NOT wire the context menu (the `!isRdp` guard survives in Task 1's step 2). This is the RDP regression guard: the new unconditional context-menu behavior MUST NOT reach RDP rows.

    5. Add a NEW test at the end of the desktop-only test block (right after Test 18b, before the Test 19 subtitleMode block): "Test 18c: desktop non-RDP row has NO PinAction and NO DeactivateAction in `.pv-meta` (post quick-260730-o2m strip)." Render with `variant="desktop"`, `inActiveSet={true}` (which pre-strip would have rendered DeactivateAction), `pinned={true}` (which pre-strip would have rendered PinAction), `onDeactivate={vi.fn()}`. Assert: `container.querySelector('[data-testid="pin-action"]')` is null; `container.querySelector('[data-testid="deactivate-action"]')` is null. This is the direct regression guard for the strip — if a future refactor re-introduces the meta-column icons, this test fails.

    6. Add a NEW test: "Test 18d: desktop non-RDP row body has an `onContextMenu` handler; dispatching a contextmenu event opens the portal menu with `Pin` and `Deactivate` items when `inActiveSet={true}` and `onDeactivate` provided." Render with `variant="desktop"`, `inActiveSet={true}`, `onDeactivate={vi.fn()}`, `pinned={false}`. Use `fireEvent.contextMenu(body, { clientX: 200, clientY: 150 })`, then use `screen.getByRole("menu")` to grab the portal-mounted menu, and `within(menu).getByRole("menuitem", { name: /pin/i })` + `within(menu).getByRole("menuitem", { name: /deactivate/i })` to assert both items are present. This proves the menu contents flow through for the active-set desktop path.

    7. Add a NEW test: "Test 18e: desktop non-RDP row NOT in active-set opens the context menu with only `Pin` (no `Deactivate`)." Render with `variant="desktop"`, `inActiveSet={false}`, `pinned={false}`. Dispatch contextmenu, assert menu opens with `Pin` menuitem present and NO `Deactivate` menuitem. This mirrors the `if (inActiveSet && onDeactivate) items.push({ label: "Deactivate", … })` predicate in the source at ~lines 622-628.

    8. Add a NEW test: "Test 18f: mobile row body does NOT wire `onContextMenu` (mobile swipe-strip carries the actions instead)." Render with `variant="mobile"`. Dispatch `fireEvent.contextMenu(body, …)`. Assert `screen.queryByRole("menu")` is null (no portal menu opens on mobile). Also assert the mobile swipe-strip PinAction is present via `container.querySelector('[data-testid="pin-action"]')` truthy. This is the mobile-untouched regression guard.

    9. Do NOT touch Tests 1, 2, 3, 4, 5, 6, 9, 11, 12, 13, 14, 15, 16, 17, 18, 18b, 19A, 19B, 19C. None of them assert on `[data-testid="pin-action"]` or `[data-testid="deactivate-action"]` in a way that conflicts with the strip. Verify by grepping the current test file: `grep -n 'pin-action\|deactivate-action' src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` — the hits are at Test 2 (mobile swipe strip, unchanged), Test 7 + 7b (RDP null-check, unchanged), Test 8 (rewritten in this task step 2), Test 10 (rewritten in this task step 3), and Test 11 (identity-badge null-check on both variants — unchanged; it queries `.pv-meta` for absence of `[data-testid='identity-badge']` which is unaffected by this strip).

    10. Update imports at the top of the test file: add `within` to the `@testing-library/react` import if not already there (needed for Test 18d's within-portal query). The current import is `import { render, fireEvent } from "@testing-library/react";` — extend to `import { render, fireEvent, screen, within } from "@testing-library/react";`. Both `screen` and `within` are used by the new/rewritten tests to query the portal-mounted menu.

    11. Do NOT change any mock in the `vi.mock(…)` block at lines 63-88. `react-i18next`, `session-hue`, `identities-store`, and `use-is-touch-device` mocks are all still needed and shape-compatible.

    Grep gates for `PrettyConversationRow.test.tsx` before finishing this file:
    - `grep -c 'CONV_MENU_VARIANT\|readConvMenuVariant\|menu=context' src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` MUST return 0.
    - `grep -c 'onContextMenu\|fireEvent.contextMenu' src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` MUST return >= 4 (one on Test 8 rewrite, one on Test 10 rewrite, one on Test 18d, one on Test 18f — at minimum).
    - `grep -c 'Test 18c\|Test 18d\|Test 18e\|Test 18f' src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` MUST return >= 4 (the four new tests exist).
    - `grep -c 'it(' src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` MUST return between 26 and 28 (was 23; +4 new = 27; small variance from `it.each` or block-count changes is acceptable).

    ─── Part B: Write `src/ui/features/pretty-conversations/PrettyConversationContextMenu.test.tsx` ───

    12. Create the file fresh — it does not exist today. Header comment at the top describes: "PrettyConversationContextMenu — Vitest coverage. Fork-new component from patch #203 shipped without tests because `?menu=context` was URL-gated and jsdom's window.location.search is empty by default. quick-260730-o2m promotes the context-menu behavior to unconditional default (see PrettyConversationRow.tsx strip); this suite covers the component in isolation. Coverage: portal mount into document.body, cursor positioning with viewport clamp on right + bottom edges, Escape-key dismiss, outside-click (mousedown outside menu) dismiss, item-click invokes callback + calls onClose."

    13. Imports: `import { describe, it, expect, vi, beforeEach, afterEach } from "vitest";` `import { render, fireEvent, screen } from "@testing-library/react";` `import { PrettyConversationContextMenu, type PrettyContextMenuItem } from "./PrettyConversationContextMenu";`. No mocks required — the component depends only on React, `react-dom` (createPortal), and DOM globals (all present in jsdom).

    14. `beforeEach`: reset `window.innerWidth` and `window.innerHeight` to known values (e.g., 1024 × 768) via `Object.defineProperty` — jsdom's defaults are 1024 × 768 already, but setting them explicitly makes viewport-clamp tests deterministic. `afterEach`: no teardown needed because renderHook + render's `unmount` handle cleanup; the portal node is inside document.body and each `render()` call's cleanup pulls it. If a test forgets to unmount, downstream tests could see a stale menu — add an explicit `unmount()` call at the end of each test that opens a menu, OR wrap the tests in `render()`'s default afterEach cleanup by importing `cleanup` if strictly needed (vitest's `@testing-library/react` auto-cleanup handles this; verify by running the suite twice and confirming no cross-test bleed).

    15. **Test 1: Portal mounts into `document.body`, not the render container.** Render with a single item `{ label: "Pin", onClick: vi.fn() }`, `x={100}`, `y={100}`, `onClose={vi.fn()}`. Assert `screen.getByRole("menu")` returns a node whose closest ancestor with a non-null parent is `document.body` (or more directly: `document.body.contains(menu)` is true AND the render `container` — which is a fresh div — does NOT contain the menu: `container.contains(menu)` is false). This proves the createPortal mount target is `document.body`.

    16. **Test 2: Menu positions at (x, y) when the viewport has room (no clamp).** Render at `x={100}`, `y={100}`, viewport 1024 × 768. Assert `menu.style.left` is `"100px"` and `menu.style.top` is `"100px"`. Note: `useLayoutEffect` runs the clamp read-then-write, but jsdom's `getBoundingClientRect` returns zeros for width/height by default, so `left + rect.width + VIEWPORT_MARGIN` = `100 + 0 + 8` = 108 < 1024 → no clamp; `top + rect.height + VIEWPORT_MARGIN` = 108 < 768 → no clamp; the effect calls `setPos({ left: 100, top: 100 })` and the render reflects it. Document the zero-BCR jsdom quirk with an inline comment.

    17. **Test 3: Viewport clamp on RIGHT edge.** Stub `HTMLDivElement.prototype.getBoundingClientRect` to return `{ width: 200, height: 40, left: 0, top: 0, right: 200, bottom: 40, x: 0, y: 0, toJSON: () => ({}) }` for this test (use `vi.spyOn(HTMLDivElement.prototype, 'getBoundingClientRect').mockReturnValue(…)` and restore in `afterEach`). Render at `x={900}` (viewport width 1024 → `900 + 200 + 8` = 1108 > 1024 → clamp), `y={100}`. Expected clamped left: `Math.max(8, 1024 - 200 - 8)` = 816. Assert `menu.style.left` is `"816px"`. Also assert `menu.style.top` is `"100px"` (top not clamped, viewport height 768 is well beyond 100 + 40 + 8 = 148).

    18. **Test 4: Viewport clamp on BOTTOM edge.** Same BCR mock. Render at `x={100}`, `y={700}` (viewport height 768 → `700 + 40 + 8` = 748 < 768 — that does NOT clamp. Use `y={730}` → `730 + 40 + 8` = 778 > 768 → clamp; expected clamped top: `Math.max(8, 768 - 40 - 8)` = 720). Assert `menu.style.top` is `"720px"` and `menu.style.left` is `"100px"`.

    19. **Test 5: Escape key dismisses menu.** Render with `onClose={vi.fn()}`. Get menu via `screen.getByRole("menu")`. Dispatch `fireEvent.keyDown(window, { key: "Escape" })` — the component listens at `window` with capture phase. Note that the current component uses `window.addEventListener("keydown", onKey, true)` — `@testing-library/react`'s `fireEvent.keyDown(window, …)` dispatches a real KeyboardEvent on window, and the capture-phase listener will catch it. Assert `onClose` was called exactly once. Bonus assertion: the Escape handler calls `e.stopPropagation()` — the test does not need to assert propagation but should not rely on it either.

    20. **Test 6: Outside-click (mousedown outside menu) dismisses.** Render with `onClose={vi.fn()}`. Get menu. Dispatch `fireEvent.mouseDown(document.body)` — this fires a mousedown event on the body, which is OUTSIDE `menuRef.current` (the menu is a child of body but the target `document.body` is not `menuRef.current.contains(document.body)` — it's the OTHER direction; the component checks `!el.contains(e.target as Node)` so a click on body proper passes the check). Assert `onClose` was called exactly once.

    21. **Test 7: Inside-click (mousedown ON menu) does NOT dismiss.** Render, get menu, dispatch `fireEvent.mouseDown(menu)`. The component's `onDown` handler checks `!el.contains(e.target as Node)` — menu contains itself, so the check fails and `onClose` is not called. Assert `onClose` was NOT called (`expect(onClose).not.toHaveBeenCalled()`).

    22. **Test 8: Item-click invokes item's onClick AND onClose.** Render with two items: `{ label: "Pin", onClick: itemPinClick }, { label: "Deactivate", onClick: itemDeactivateClick, danger: true }`. Grab the `Pin` menuitem via `screen.getByRole("menuitem", { name: /pin/i })`. `fireEvent.click(pinItem)`. Assert `itemPinClick` called once, `itemDeactivateClick` NOT called, `onClose` called once. Then re-render (or use a fresh render context) with the Deactivate item, click it, assert only its callback + onClose fire.

    23. **Test 9: `danger: true` styling applied to the danger item.** Render with `{ label: "Deactivate", onClick: vi.fn(), danger: true }`. Grab the item. Assert `item.style.color` is `"rgb(255, 154, 138)"` (the `"#ff9a8a"` color from the component's inline style — jsdom normalizes CSS colors to rgb notation). This is a light styling contract test — the component sets `color: item.danger ? "#ff9a8a" : "#e8e4d8"`, and this test locks the danger branch.

    24. **Test 10: Hue token drives the `--pv-id-hue` custom property.** Render with `hue={45}`. Grab the menu, read `menu.getAttribute("style")` (jsdom preserves custom properties in the raw style attribute — same pattern the RowConversation test file uses at line 202 for `--pv-hue`). Assert the raw style contains `--pv-id-hue: 45`. Also assert with `hue={undefined}` and `hue={null}`, the custom property is absent from the style attribute (component's `hueVarStyle` is `undefined` in those cases).

    25. Total `it()` block count target: 10-12 (matches the 10 bullets above — 15-24). The suite should be `describe("PrettyConversationContextMenu", () => { … })` with all `it()` blocks inside, OR split into two `describe` groups if it improves readability (e.g., `describe("mount + positioning")` and `describe("dismiss + item click")`).

    Grep gates for `PrettyConversationContextMenu.test.tsx` before finishing this file:
    - `grep -c 'it(' src/ui/features/pretty-conversations/PrettyConversationContextMenu.test.tsx` MUST return >= 10.
    - `grep -c 'createPortal\|document.body' src/ui/features/pretty-conversations/PrettyConversationContextMenu.test.tsx` MUST return >= 1 (Test 1 asserts portal mount into body).
    - `grep -c 'Escape\|mouseDown\|mouseDown(document' src/ui/features/pretty-conversations/PrettyConversationContextMenu.test.tsx` MUST return >= 2 (Escape + outside-click coverage present).
    - `grep -c 'getBoundingClientRect' src/ui/features/pretty-conversations/PrettyConversationContextMenu.test.tsx` MUST return >= 1 (Test 3 and Test 4 stub BCR for clamp tests).

    Global scope guards for this task:
    - Do NOT touch `PrettyConversationRow.tsx` in this commit (that was Task 1). If you find a bug in `PrettyConversationRow.tsx` while writing tests, note it in the SUMMARY and STOP — do NOT edit source in a test commit.
    - Do NOT touch `PrettyConversationContextMenu.tsx` (the component source stays as-is; this task ONLY adds tests).
    - Do NOT touch `PrettyConversationsPanel.tsx` or any sibling in `src/ui/features/pretty-conversations/` other than the two test files.
    - Do NOT touch any file under `src/backend/`.
    - Do NOT touch `~/.claude/identities/tina/` at all — not `skynet-patches.md`, not any bounty file, not `tina.md`.
    - Do NOT `git push`, do NOT `docker build`, do NOT `docker compose up`.

    Commit boundary for Task 2: one atomic commit on `feat/tab-title-from-tmux` with message `test(pretty-conversations): cover context-menu default behavior + new PrettyConversationContextMenu suite`. The commit MUST contain exactly two files: `PrettyConversationRow.test.tsx` (updated) and `PrettyConversationContextMenu.test.tsx` (new). NO source-file changes in this commit.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -20 &amp;&amp; npx vitest run src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx src/ui/features/pretty-conversations/PrettyConversationContextMenu.test.tsx 2>&amp;1 | tail -40</automated>
  </verify>
  <done>
    - `npx tsc --noEmit` exits 0 (both test files type-check against the post-strip source).
    - `npx vitest run src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` reports 0 failed; test count is between 26 and 28 (was 23; +4 new = 27; small variance acceptable).
    - `npx vitest run src/ui/features/pretty-conversations/PrettyConversationContextMenu.test.tsx` reports 0 failed; test count is between 10 and 12.
    - `grep -c 'CONV_MENU_VARIANT\|readConvMenuVariant\|menu=context' src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx src/ui/features/pretty-conversations/PrettyConversationContextMenu.test.tsx` returns 0.
    - `grep -c 'onContextMenu\|fireEvent.contextMenu' src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` returns >= 4.
    - `grep -c 'Test 18c\|Test 18d\|Test 18e\|Test 18f' src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` returns >= 4 (new tests exist).
    - Sibling pretty-conversations suites still pass: `npx vitest run src/ui/features/pretty-conversations/` reports 0 failed across the whole directory (`PrettyConversationRow.test.tsx`, `PrettyConversationContextMenu.test.tsx`, plus any other test files present).
    - `git diff --stat` for this commit shows exactly two files: `PrettyConversationRow.test.tsx`, `PrettyConversationContextMenu.test.tsx`. NO source files. NO backend files. NO identity-side files.
    - Atomic commit on `feat/tab-title-from-tmux`; branch remains unpushed.
  </done>
</task>

</tasks>

<verification>
Post-plan global checks (run once after both commits land):

1. Type check clean: `cd /home/ubuntu/skynet && npx tsc --noEmit` exits 0.
2. New context-menu test suite green: `npx vitest run src/ui/features/pretty-conversations/PrettyConversationContextMenu.test.tsx` = 0 failed with 10-12 tests.
3. Updated row test suite green: `npx vitest run src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` = 0 failed with 26-28 tests.
4. Whole pretty-conversations directory suite green: `npx vitest run src/ui/features/pretty-conversations/` = 0 failed.
5. Sibling pretty-view suites (immediate neighbors — the sticky-scroll strip landed here last) still pass: `npx vitest run src/ui/features/pretty-view/` = 0 failed. This validates that the context-menu strip did not spill into any consumer of the row.
6. Full-suite regression sanity: `npx vitest run` = 0 failed across all frontend suites.
7. Gate purge confirmed: `grep -rn 'CONV_MENU_VARIANT\|readConvMenuVariant\|menu=context' src/` returns nothing. Every reference — const, function, comment, conditional — is gone.
8. Public API preserved: `grep -c 'PrettyConversationRow' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns >= 1 (the panel still consumes the row component with the same prop shape). No consumer code paths changed shape.
9. Mobile swipe-strip untouched: `grep -c 'onTouchStart\|onTouchMove\|onTouchEnd\|onTouchCancel' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` returns >= 4 (the four touch handler wires on the row body survive; mobile branch intact).
10. Scope guards: `git diff --name-only HEAD~2` (post both commits) shows only files under `src/ui/features/pretty-conversations/`. NO `src/backend/` files. NO `~/.claude/identities/tina/` files.
11. Terminal boundary: STOP after Task 2 commits. Do NOT `git push`. Do NOT `docker build`. Do NOT `docker compose up`. Do NOT edit `~/.claude/identities/tina/skynet-patches.md` (Tina writes the patch entry after this executor returns). Do NOT flip the bounty status at `~/.claude/identities/tina/bounties/context-menu-strip/bounty.json` or wherever the bounty file lives (Tina archives it after). Do NOT touch `~/.claude/identities/tina/tina.md` in any way.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → pretty-conversations row surface | Local UI-only change; no data crosses a trust boundary. Right-click contextmenu event already flows into the hook (via the gated `onRowContextMenu`) and continues to do so unchanged, just without a URL-param gate. |
| user → URL query/hash | The `?menu=context` / `#menu=context` gate is being DELETED. After this task, that URL param has no effect (becomes an inert unknown param). Not a new boundary; a removed observation point. |
| PrettyConversationsPanel → PrettyConversationRow props | Existing prop shape (`row`, `selected`, `pinned`, `variant`, `onSelect`, `onTogglePin`, `onDeactivate`, `onSwipeOpenChange`, `forceClosed`, `isWorking`, `inActiveSet`, `subtitleMode`) — signature preserved. No boundary change. |
| PrettyConversationRow → PrettyConversationContextMenu (portal) | Existing `createPortal(…, document.body)` call site — signature and target preserved. No boundary change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-o2m-01 | Tampering | `PrettyConversationRow.tsx` public prop shape | mitigate | The prop-shape TypeScript interface (lines ~123-169) is not touched by this task; the strip only affects the render body (`.pv-meta` conditionals + `onContextMenu` prop on the row body) and the module-level `readConvMenuVariant` / `CONV_MENU_VARIANT` deletion. TypeScript strict mode + `npx tsc --noEmit` gate in both Task 1 and Task 2 `<verify>` blocks catches any accidental shape drift at compile time. `PrettyConversationsPanel.tsx` acts as a live consumer check — if the shape changed, panel tests would fail (verification item 4 covers this by running the whole `pretty-conversations/` directory). |
| T-o2m-02 | Denial of Service | Row action regression on fresh page load — Pin/Deactivate unreachable | mitigate | The context-menu variant has been running under `?menu=context` and Ashley greenlit its behavior for shipping as default. The updated `PrettyConversationRow.test.tsx` (Task 2 Part A) explicitly covers: Test 18d asserts context menu opens with `Pin` and `Deactivate` items when `inActiveSet={true}` on desktop non-RDP; Test 18e asserts `Pin`-only when `inActiveSet={false}`; Test 8 rewrite asserts `onTogglePin` still fires from the menu-item click path. The updated Test 7b asserts RDP rows do NOT wire context menu (isRdp guard survives). The new `PrettyConversationContextMenu.test.tsx` (Task 2 Part B) asserts the menu itself works: portal mount, dismiss paths, item-click dispatch. Together these cover the full "user can still reach Pin/Deactivate on every eligible row" invariant. |
| T-o2m-03 | Repudiation | Load-bearing comments that referenced the deleted `?menu=context` gate | mitigate | Task 1 steps 6, 8, 9 explicitly enumerate every comment touching `?menu=context` / `CONV_MENU_VARIANT` / bounty #6 in the render tree — with a `<done>` grep gate confirming zero residue via `grep -c 'CONV_MENU_VARIANT\|readConvMenuVariant\|menu=context' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` returning 0. Prevents stale-comment misdirection for future readers. |
| T-o2m-04 | Denial of Service | Mobile swipe-strip regression — accidental spillover from the strip | mitigate | Task 1 step 5 explicitly forbids touching the mobile swipe-strip JSX (lines ~417-437). The grep gate `grep -c 'isMobile && !isRdp' PrettyConversationRow.tsx returns >= 4` confirms the mobile branch is intact (four `onTouch*` wire sites). Task 2 Part A Test 7 (mobile RDP) is unchanged and still passes; new Test 18f asserts mobile row body does NOT wire onContextMenu AND mobile swipe-strip PinAction IS present. Together these lock the mobile branch in place. |
| T-o2m-05 | Denial of Service | RDP-row regression — new unconditional context menu spilling to RDP rows | mitigate | Task 1 step 2 preserves `!isMobile && !isRdp` guard on the `onContextMenu` prop; step 10 preserves `!isRdp` on the portal render conditional. Task 2 Part A Test 7b adds an assertion that the desktop RDP row body has no `onContextMenu` handler. Together this locks the RDP guard: RDP rows remain action-less regardless of variant, matching the pre-strip contract. |
| T-o2m-06 | Information Disclosure | None | accept | Pure UI-side row-render change; no PII, no auth material, no backend interaction. Context menu items already existed under URL gate; the labels (`Pin`, `Unpin`, `Deactivate`) do not leak anything a user couldn't already see. No IDOR / auth-scope concerns. |
| T-o2m-07 | Elevation of Privilege | None | accept | Local UI component — no privilege boundary in scope. |
| T-o2m-SC | Tampering | Dependencies | accept | Zero new packages added. No `npm install`, no `package.json` diff. `react`, `react-dom`, `@testing-library/react`, `vitest` all already present and pinned (the row test file already uses them; the new context-menu test file uses the same set). Package-legitimacy checkpoint gate does not apply (no install tasks in this plan). |
</threat_model>

<success_criteria>
- `PrettyConversationRow.tsx` no longer contains `readConvMenuVariant`, `CONV_MENU_VARIANT`, or any `CONV_MENU_VARIANT === 'context'` conditional. The comment block at lines ~79-101 describing the URL-param gate is deleted. The bounty #6 / `?menu=context` prose in comments at ~lines 327-329, 536-541, 605-610 is rewritten to describe the unconditional default (no gate references).
- `PrettyConversationRow.tsx` row-body `onContextMenu` prop is `{!isMobile && !isRdp ? onRowContextMenu : undefined}` — the context-menu behavior is unconditional on desktop non-RDP rows.
- `PrettyConversationRow.tsx` `.pv-meta` desktop branch no longer renders `PinAction` or `DeactivateAction`; those actions live ONLY in the mobile swipe strip (unchanged) and the right-click context menu (unconditional on desktop non-RDP).
- `PrettyConversationRow.tsx` portal render `{ctxMenu !== null && !isRdp && (<PrettyConversationContextMenu … />)}` — no URL-param gate; the `!isRdp` guard survives.
- Mobile swipe-strip render (lines ~417-437) is byte-identical: `isMobile && !isRdp` gate; PinAction always; DeactivateAction when `inActiveSet`.
- `PrettyConversationRow.test.tsx` is updated: Test 8 rewrite (contextmenu → Pin item → onTogglePin); Test 10 rewrite (contextmenu opens with `Unpin` label when pinned=true); Test 7b adds RDP no-onContextMenu assertion; new Test 18c (desktop no PinAction / no DeactivateAction in .pv-meta); new Test 18d (desktop contextmenu → Pin + Deactivate items when inActiveSet); new Test 18e (desktop contextmenu → Pin only when !inActiveSet); new Test 18f (mobile contextmenu no-op + swipe strip still shows PinAction).
- `PrettyConversationContextMenu.test.tsx` is a fresh file with 10-12 `it()` blocks covering: portal mount into document.body, no-clamp positioning at (x,y), viewport clamp on right edge, viewport clamp on bottom edge, Escape dismiss, outside-mousedown dismiss, inside-mousedown no-dismiss, item-click invokes callback + calls onClose, danger-color styling, `--pv-id-hue` custom property from `hue` prop.
- `npx tsc --noEmit` exits 0.
- `npx vitest run src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` = 0 failed with 26-28 tests.
- `npx vitest run src/ui/features/pretty-conversations/PrettyConversationContextMenu.test.tsx` = 0 failed with 10-12 tests.
- `npx vitest run src/ui/features/pretty-conversations/ src/ui/features/pretty-view/` = 0 failed (no sibling regressions).
- `grep -rn 'CONV_MENU_VARIANT\|readConvMenuVariant\|menu=context' src/` returns nothing.
- Two atomic commits on `feat/tab-title-from-tmux`: Task 1 (source strip — one file: `PrettyConversationRow.tsx`) and Task 2 (test updates + new test file — two files: `PrettyConversationRow.test.tsx`, `PrettyConversationContextMenu.test.tsx`). Executor stops at commit boundary — NO push, NO docker build, NO docker compose, NO edits under `~/.claude/identities/tina/`, NO edits to `skynet-patches.md`, NO edits to any bounty file, NO edits to `tina.md`.
</success_criteria>

<output>
Create `.planning/quick/260730-o2m-context-menu-strip/260730-o2m-01-SUMMARY.md` when done, documenting:
- Exact line-range of the `PrettyConversationRow.tsx` strip (net line delta — the deleted `readConvMenuVariant` function + const + the three conditional gates should net roughly -30 to -50 lines).
- Line-range summary of the comment edits at ~lines 327-329, 536-541, 605-610 in `PrettyConversationRow.tsx`.
- Updated test count in `PrettyConversationRow.test.tsx` (26-28 `it()` blocks) — enumerate the rewritten tests (8, 10, 7b augmented) and the new tests (18c, 18d, 18e, 18f).
- New test count in `PrettyConversationContextMenu.test.tsx` (10-12 `it()` blocks) — enumerate the bullets (portal mount, no-clamp position, right-edge clamp, bottom-edge clamp, Escape, outside-click, inside-click no-dismiss, item-click, danger styling, `--pv-id-hue` custom property).
- Both commit SHAs on `feat/tab-title-from-tmux`.
- Verification output: `tsc --noEmit` exit code, `vitest run src/ui/features/pretty-conversations/` pass count, `vitest run src/ui/features/pretty-view/` pass count.
- Grep-purge confirmation: `grep -rn 'CONV_MENU_VARIANT\|readConvMenuVariant\|menu=context' src/` output (should be empty).
- Explicit confirmation: no push, no docker build, no docker compose, no files touched under `src/backend/`, no files touched under `~/.claude/identities/tina/` (no skynet-patches.md edit, no bounty.json flip, no tina.md edit).
- Branch state: `git status` shows clean working tree; branch is `feat/tab-title-from-tmux`, ahead of origin by 2 unpushed commits (Task 1 + Task 2 for this quick task, on top of whatever prior unpushed commits already existed per the STATE.md deploy queue — including the two mj2 sticky-scroll commits from immediately before this task).
</output>
</content>
</invoke>