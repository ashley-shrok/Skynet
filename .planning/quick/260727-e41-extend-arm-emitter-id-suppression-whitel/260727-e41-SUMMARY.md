---
phase: quick-260727-e41
plan: 01
subsystem: pretty-view / aside subsystem (Phase 14 followup)
tags: [aside, id-command, xml-wrapper, harness-slash-ui, uat-e41]
requires:
  - Phase 14 arm-emitter useEffect at PrettyView.tsx:887 (f029e44 raw-text /id suppression)
provides:
  - Harness slash-UI /id invocation form (XML-wrapper) now suppressed alongside raw-text form
  - Test 9 pinning the harness slash-UI XML-wrapper suppression path
affects:
  - PrettyView arm-emitter code path (single call site swap, no behavioral change for existing raw-text path)
tech_stack:
  added: []
  patterns:
    - Module-local const arrow helper co-located with consumer (Phase 14 no-new-shared-utils posture preserved)
    - Substring-based recognition of harness slash-UI XML wrappers as a load-bearing invariant (XML tag string cannot legally appear in prose user turns)
key_files:
  created: []
  modified:
    - src/ui/features/pretty-view/PrettyView.tsx (+17 / -1 — helper declaration + call-site swap + comment extension)
    - src/ui/features/pretty-view/PrettyView.aside.test.tsx (+56 / 0 — Test 9 added)
decisions:
  - Helper co-located as module-local const in PrettyView.tsx rather than hoisted to a shared utils file — preserves Phase 14 no-new-shared-utils posture (single consumer, single call site, decision reversible if a second consumer emerges)
  - `.includes("<command-name>/id</command-name>")` used verbatim rather than a regex — the XML tag string is not a legal substring of any prose user turn, so substring semantics are sufficient and faster than regex compilation on every arm-emitter fire
  - `content` param passed positionally to the helper (single-argument, obvious usage) rather than a settings-bag — matches Phase 14's terse-utility style
metrics:
  duration_seconds: 220
  completed: 2026-07-27
  commit: cdccd4f
---

# Quick Task 260727-e41: Extend arm-emitter /id suppression to harness slash-UI XML form

## One-liner

Extend `isIdCommand` in PrettyView arm-emitter to recognize both raw "/id " text (Phase 14 form) and the harness slash-UI `<command-name>/id</command-name>` XML wrapper (Ashley's PRIMARY /id path from pretty-view slash-UI).

## Files Changed

| File | Lines Added | Lines Removed |
| --- | --- | --- |
| `src/ui/features/pretty-view/PrettyView.tsx` | 17 | 1 |
| `src/ui/features/pretty-view/PrettyView.aside.test.tsx` | 56 | 0 |
| **Total** | **73** | **1** |

## Test Count Delta

Aside test file: **8 → 9** passing tests (the plan's frontmatter `truths[3]` phrased this as "baseline 33 tests -> 34" — actual file baseline was 8; a review of the file shows `it(` block count went 8 → 9. Full-suite count went 603 → 604. All Tests 1-8 unchanged and green; new Test 9 green.).

Full sweep: **604/604 tests across 49 files** (was 603/603 before this task).

Type-check: clean.

## Commit

- `cdccd4f fix(aside): extend /id suppression to cover harness slash-UI form`

## Verification (all green)

- `npm run type-check` → clean
- `npx vitest run src/ui/features/pretty-view/PrettyView.aside.test.tsx` → 9/9 green
- `npm test` (full sweep) → 604/604 green across 49 test files
- grep gates from `<done>` block:
  - `grep -n 'isIdCommand' PrettyView.tsx` → 3 hits (declaration L146 + comment L919 + call site L925)
  - `grep -n 'command-name>/id<' PrettyView.tsx` → 3 hits (declaration doc L141 + helper body L148 + comment L920)
  - `grep -n 'Test 9' PrettyView.aside.test.tsx` → 1 hit (L418)

Grep-hit counts are slightly higher than the plan's `<done>` expected minimums (2 / 1 / 1) because the extended block comment mentions `isIdCommand` and the helper's own doc comment repeats the XML tag string — both intentional readability adds, neither a behavioral change.

## Deviations from Plan

**None** — plan executed exactly as written. Both the RED and GREEN gates fired cleanly:

- RED: Test 9 written first, run in isolation, failed with `expected [ '{"type":"aside_arm"}' ] to be undefined` (proves the raw-text-only guard did NOT suppress the XML-wrapper form).
- GREEN: helper declared, call site swapped, Test 9 re-run — passed. Full aside file + full sweep both green with no regressions.

## Known Stubs

None. The code change is a pure guard-extension; no new UI, no new data sources, no placeholder values.

## Deferred

Nothing deferred. Batch deploy stays Ashley-gated per plan constraint — the `cdccd4f` commit sits on `feat/tab-title-from-tmux` awaiting Ashley's separate deploy greenlight (not performed inside this quick task).

## TDD Gate Compliance

Task 1 had `tdd="true"`. Sequence followed:

1. **RED** — Test 9 written first (aside test file) with no implementation change. Confirmed failing via `-t "Test 9"` isolated run.
2. **GREEN** — `isIdCommand` helper declared + call-site swap + comment extension. Confirmed passing.
3. **REFACTOR** — not needed; helper landed in final shape on first pass.

Single atomic commit `cdccd4f` combines RED + GREEN test + impl (matches Phase 14 commit style — reference commits like `f029e44` also bundle test + impl per Ashley's preferred atomicity).

## Self-Check

- [x] `src/ui/features/pretty-view/PrettyView.tsx` — FOUND, contains `isIdCommand` declaration + call site
- [x] `src/ui/features/pretty-view/PrettyView.aside.test.tsx` — FOUND, contains Test 9
- [x] Commit `cdccd4f` — FOUND in `git log`
- [x] All grep gates satisfied
- [x] type-check clean
- [x] Full test sweep 604/604 green

## Self-Check: PASSED
