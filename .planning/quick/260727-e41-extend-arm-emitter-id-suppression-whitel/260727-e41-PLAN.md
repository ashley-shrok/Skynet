---
phase: quick-260727-e41
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-view/PrettyView.aside.test.tsx
autonomous: true
requirements:
  - E41-01
must_haves:
  truths:
    - "AsideBubble is suppressed after harness slash-UI /id invocations (XML-wrapped form), not just SSH-typed raw /id ... text"
    - "Existing Test 7 (raw '/id save' text suppresses arm) continues to pass unchanged"
    - "Existing Test 8 (real user turn after /id still arms) continues to pass unchanged"
    - "Full aside test file remains green (baseline 33 tests -> 34 after Test 9 added)"
  artifacts:
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "Extended /id suppression check in arm-emitter useEffect (line ~909)"
      contains: "isIdCommand"
    - path: "src/ui/features/pretty-view/PrettyView.aside.test.tsx"
      provides: "Test 9 pinning harness slash-UI XML-wrapper form suppression"
      contains: "Test 9"
  key_links:
    - from: "src/ui/features/pretty-view/PrettyView.tsx arm-emitter useEffect"
      to: "isIdCommand helper"
      via: "single call site replacing inline startsWith check"
      pattern: "isIdCommand\\(.*content"
    - from: "src/ui/features/pretty-view/PrettyView.aside.test.tsx Test 9"
      to: "arm-emitter useEffect suppression path"
      via: "fireWsMessage with XML-wrapper /id content -> assert no aside_arm sent"
      pattern: "<command-name>/id</command-name>"
---

<objective>
Extend the arm-emitter /id-suppression whitelist in PrettyView.tsx to cover the harness slash-UI invocation form (XML-wrapped) in addition to the existing raw-text form. Add Test 9 to pin the new behavior. Fix the Phase 14 UAT failure Ashley just hit where the AsideBubble appeared explaining the assistant's response to her first post-wake /id command (invoked via pretty-view's slash-UI, which lands in JSONL as `<command-name>/id</command-name>` XML rather than as literal `/id save` text).

Purpose: Phase 14 shipped /id suppression covering the SSH-typed raw path only. Ashley's PRIMARY /id invocation path is the harness slash-UI, so every /id from pretty-view currently triggers a spurious aside on the response.

Output: A 1-line inline check becomes a 4-line helper + 1-line call site; 1 new mirror-of-Test-7 test case (Test 9).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Arm-emitter useEffect containing the current /id check (line 909):
@src/ui/features/pretty-view/PrettyView.tsx

# Existing Tests 7 + 8 to mirror pattern for Test 9 (lines 305-416):
@src/ui/features/pretty-view/PrettyView.aside.test.tsx

# Reference commit that added original /id suppression:
# f029e44 feat(aside): suppress aside_arm when last user turn was an /id command
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend /id suppression to XML-wrapper form + add Test 9</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx, src/ui/features/pretty-view/PrettyView.aside.test.tsx</files>
  <behavior>
    - Test 9 (NEW): last user turn content is the harness slash-UI XML-wrapper form ("&lt;command-message&gt;id&lt;/command-message&gt;&lt;command-name&gt;/id&lt;/command-name&gt;&lt;command-args&gt;tina&lt;/command-args&gt;") → isIdle false→true transition does NOT emit aside_arm. Mirrors Test 7 structure (mock useSessionIdentity to non-null identity, render with isIdle=false, flipToStreaming, fireWsMessage with the XML content, capture ws.send count, rerender with isIdle=true, wait 40ms, assert no aside_arm send occurred).
    - Test 7 (PRESERVED): raw "/id save" text still suppresses — unchanged.
    - Test 8 (PRESERVED): /id turn followed by real user turn still arms — unchanged.
    - All other aside tests (1-6) unchanged — baseline count 33 → 34.
  </behavior>
  <action>
    In `src/ui/features/pretty-view/PrettyView.tsx`, locate the arm-emitter useEffect at line 887 (`useEffect(() => { const prev = prevIsIdleRef.current; ...`). The current suppression at line 909 reads `if (m.content.trimStart().startsWith("/id ")) return;`.

    Extract a small helper directly above the useEffect (module-local const arrow — do NOT export, do NOT hoist to another file, keep it co-located per Phase 14 no-new-shared-utils posture):

    Declare `isIdCommand` as an arrow function accepting `content: string` returning `boolean`. It returns true when EITHER (a) `content.trimStart().startsWith("/id ")` — the existing SSH-typed raw form, trailing space deliberate to exclude /identity / /idle / bare /id — OR (b) `content.includes("&lt;command-name&gt;/id&lt;/command-name&gt;")` — the harness slash-UI XML-wrapper form emitted by pretty-view's slash-UI as literal XML tags in JSONL. The `.includes` check is safe because the XML tag string is not a legal substring of any prose user turn — it only appears when the harness itself constructs the wrapper.

    Replace the inline check at line 909 with `if (isIdCommand(m.content)) return;`. Keep the surrounding for-loop, the `break` after the user-role match, and the `messages` dep in the useEffect deps array — all unchanged. Preserve the existing block comment at lines 895-905 and extend it with one sentence noting the XML-wrapper form (harness slash-UI) is now also covered per Ashley 2026-07-27 UAT amendment E41.

    In `src/ui/features/pretty-view/PrettyView.aside.test.tsx`, add Test 9 immediately after Test 8 (before the closing `});` of the outer describe at line 417). Copy Test 7's structure byte-for-byte, changing ONLY the `content` field in the user `fireWsMessage` call from `"/id save"` to the XML-wrapper form: `"&lt;command-message&gt;id&lt;/command-message&gt;&lt;command-name&gt;/id&lt;/command-name&gt;&lt;command-args&gt;tina&lt;/command-args&gt;"` (matches Ashley's real UAT payload — /id tina invoked via pretty-view slash-UI). The `it(...)` title: `"Test 9: isIdle transition does NOT emit aside_arm when last user turn was harness slash-UI /id XML-wrapper form"`. Include a one-line comment referencing UAT amendment E41 (Ashley 2026-07-27) above the test body, mirroring Test 7's Phase 14 followup comment.

    Do NOT modify Tests 1-8. Do NOT modify AsideBubble.tsx, ComposeBox.tsx, backend files, or any deploy config. Do NOT run docker compose deploy — batch-deploy discipline requires Ashley greenlights separately.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/PrettyView.aside.test.tsx 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - `PrettyView.aside.test.tsx` shows 34 passing tests (baseline 33 + new Test 9), zero failing.
    - Test 7 and Test 8 pass unchanged.
    - Test 9 passes: XML-wrapper /id content in last user turn short-circuits arm-emitter.
    - Grep confirms helper landed: `grep -n 'isIdCommand' src/ui/features/pretty-view/PrettyView.tsx` returns 2 lines (declaration + call site).
    - Grep confirms XML check present: `grep -n 'command-name>/id<' src/ui/features/pretty-view/PrettyView.tsx` returns 1 line.
    - Grep confirms Test 9 exists: `grep -n 'Test 9' src/ui/features/pretty-view/PrettyView.aside.test.tsx` returns 1 line.
    - No deploy performed; changes stay uncommitted until execute-plan wraps and Ashley greenlights the batch deploy separately.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/ui/features/pretty-view/PrettyView.aside.test.tsx` → 34/34 green
- `grep -n 'isIdCommand\|command-name>/id<' src/ui/features/pretty-view/PrettyView.tsx` → helper + XML check both present
- Manual sanity read of the diff: only 2 files touched, PrettyView.tsx diff is <30 lines (helper + call-site swap + comment extension), test file diff is ~52 lines (one new it() block mirroring Test 7)
</verification>

<success_criteria>
- Harness slash-UI /id invocations (XML-wrapper form) no longer trigger AsideBubble on the assistant's response
- SSH-typed raw /id invocations remain suppressed (regression-free)
- Non-/id user turns still trigger arm normally (regression-free)
- Full aside test file green: 34 tests
- No deploy performed inside the quick task — batch-deploy stays Ashley-gated
</success_criteria>

<output>
Create `.planning/quick/260727-e41-extend-arm-emitter-id-suppression-whitel/260727-e41-SUMMARY.md` when done, noting: files changed, lines added/removed, test count delta (33 → 34), and a one-line note that deploy is pending Ashley's greenlight (not performed).
</output>
