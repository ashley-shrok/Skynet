---
phase: quick-260810-299
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/PrettyView.tsx
autonomous: true
requirements:
  - pv-disable-auto-scroll-temp
must_haves:
  truths:
    - "PrettyView renders without invoking useAutoScroll's ResizeObserver/gesture/rAF-chain machinery"
    - "PrettyView.tsx typechecks under strict TS (npx tsc --noEmit exits 0)"
    - "All pretty-view vitest tests pass; auto-scroll-behavior tests are either skipped-with-pointer or updated to assert TEMP no-op behavior"
    - "Full frontend vitest suite passes; no test failures leak outside src/ui/features/pretty-view/"
    - "src/ui/features/pretty-view/use-auto-scroll.ts is preserved unchanged in-tree as reference"
    - "The stubbed hook call is marked with a TEMP 2026-08-10 comment that names bounty pv-disable-auto-scroll-temp and forward-ref bounty pv-auto-scroll-redesign"
    - "Stale narrative comments (RENDER-03 auto-scroll and AsideBubble ResizeObserver-lies) are rewritten to reflect TEMP-disabled state"
    - "Clicking the jump-to-bottom pill is a no-op (intended TEMP UX); composeScrollRefs ref merger stays intact"
  artifacts:
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "PrettyView with inlined useAutoScroll stub replacing the hook call at ~line 609-610"
      contains: "TEMP 2026-08-10: auto-scroll disabled per bounty pv-disable-auto-scroll-temp"
    - path: "src/ui/features/pretty-view/use-auto-scroll.ts"
      provides: "Original useAutoScroll hook kept as reference (UNTOUCHED)"
      contains: "export function useAutoScroll"
  key_links:
    - from: "src/ui/features/pretty-view/PrettyView.tsx"
      to: "composeScrollRefs (line ~666-672)"
      via: "stub scrollRef callback captured into scrollElRef via composeScrollRefs"
      pattern: "scrollElRef.current = el"
    - from: "src/ui/features/pretty-view/PrettyView.tsx"
      to: "forceStickAndJumpRef.current assignment (line ~677)"
      via: "no-op forceStickAndJump wired through the existing ref plumbing"
      pattern: "forceStickAndJumpRef\\.current = forceStickAndJump"
---

<objective>
Temporarily disable PrettyView's snap-to-bottom auto-scroll by replacing the
`useAutoScroll(paneKey)` call at src/ui/features/pretty-view/PrettyView.tsx
lines 605-610 with an inline ref-based stub. Keep use-auto-scroll.ts in-tree
UNTOUCHED for future redesign reference. Rewrite two stale narrative comments
that now lie about behavior. Add a TEMP marker naming the bounty pair
(pv-disable-auto-scroll-temp for the disable, pv-auto-scroll-redesign for the
future re-enable). Verify typecheck + full frontend vitest suite green, then
land ONE commit. Do NOT deploy — that's tina's job.

Purpose: Phase 27 virtualization made snap-to-bottom unusable; a full redesign
is scoped to bounty pv-auto-scroll-redesign. This is a narrow temp-disable so
the current bad UX stops shipping while the redesign is planned.

Output: One commit fix(pretty-view): temporarily disable snap-to-bottom
auto-scroll (pv-disable-auto-scroll-temp).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@src/ui/features/pretty-view/use-auto-scroll.ts

# PrettyView.tsx is 2000+ lines — read the four windows called out in the
# task_scope: lines 85-90 (RENDER-03 narration), 490-520 (forceStickAndJumpRef
# + handleComposeSend), 600-680 (useAutoScroll call + composeScrollRefs +
# forceStickAndJumpRef wire-up), 1790-1920 (scroll-container JSX + AsideBubble
# lying comment + jump-to-bottom pill). Do NOT read the whole file.
@src/ui/features/pretty-view/PrettyView.tsx
</context>

<constraints>
Fleet boundaries (BAKED IN — do not violate):

- DO NOT run git push. DO NOT run docker build. DO NOT run docker compose up.
  Deploy is orchestrator-only (tina, not this executor). STOP after commit +
  tests green.
- DO NOT delete src/ui/features/pretty-view/use-auto-scroll.ts.
- DO NOT modify src/ui/features/pretty-view/use-auto-scroll.ts itself — this
  is pure PrettyView.tsx surgery.
- DO NOT touch any file outside src/ui/features/pretty-view/.
- DO NOT do refactoring beyond what Task 1 lists. Even if nearby code looks
  improvable, LEAVE IT — this is a narrow TEMP-disable, not a cleanup pass.
- DO NOT use git worktrees — run on the main tree.
- If ANY test in the full suite fails post-change and it's NOT a pretty-view
  auto-scroll test that legitimately became obsolete, HALT and report; do not
  commit red.
- src/backend/ is not touched — skip npm run build:backend.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Stub useAutoScroll inline in PrettyView.tsx + rewrite stale narrative comments</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx</files>
  <action>
Single-file surgery on src/ui/features/pretty-view/PrettyView.tsx. Do NOT touch
any other file.

STEP A — Precondition audit (grep first, edit second):

1. Confirm callers of useAutoScroll's return values inside PrettyView.tsx via
   grep. Expected: scrollRef used at line ~669 (inside composeScrollRefs),
   contentRef used at line ~1812 (as ref on the sized virtualizer div),
   scrollToBottomAndFollow used at line ~1928 (jump-to-bottom pill onClick),
   forceStickAndJump used at line ~677 (forceStickAndJumpRef.current = ...),
   isPinnedToBottom used at line ~1923 (pill visibility conditional). If any
   additional callers show up, they still need to keep typechecking against
   the stub return shape.
2. Confirm `paneKey` (line 608) is ALSO used at lines ~714 and ~746
   (paneKeyRef comparison in the WS effect). This means paneKey MUST be
   preserved — do NOT remove it. The task_scope's "feel free to remove if only
   used for useAutoScroll" branch does NOT apply.
3. Confirm src/ui/features/pretty-view/use-auto-scroll.test.tsx does NOT
   exist (pre-verified: it does not). Skip the test-file adjustment branch.

STEP B — Replace the useAutoScroll call (lines 605-610):

Currently:

    // paneKey is passed so useAutoScroll can reset to bottom on conversation
    // swap (fresh conversation load always lands at bottom via a rAF-chain
    // jump for LOAD_LOCK_MS).
    const paneKey = `${hostId}::${tmuxSession}`;
    const { scrollRef, contentRef, scrollToBottomAndFollow, forceStickAndJump, isPinnedToBottom } =
      useAutoScroll(paneKey);

Replace with an inline stub that matches the `UseAutoScrollResult` interface
shape from use-auto-scroll.ts but does nothing:

- Update the paneKey comment (lines 605-607) to note paneKey is retained for
  the WS effect's paneKeyRef comparison (lines ~714/746), NOT for scroll
  reset (which is now disabled). Keep `const paneKey = \`${hostId}::${tmuxSession}\`;`
  UNCHANGED — it is still consumed downstream.
- Immediately BEFORE the stub block, insert the TEMP marker comment verbatim:

    // TEMP 2026-08-10: auto-scroll disabled per bounty pv-disable-auto-scroll-temp
    // after Phase 27 virtualization made snap-to-bottom unusable. use-auto-scroll.ts
    // is kept in-tree as reference for the eventual redesign (bounty
    // pv-auto-scroll-redesign — do not re-enable this stub without that redesign).

- Emit the stub. Behavior required per task_scope item 1:
  * scrollRef: STABLE useCallback that just captures the element into a local
    useRef (nothing else — no observers, no scrollTop writes, no listeners).
    Rationale: composeScrollRefs (lines 666-672) already assigns the outer
    scroll container into scrollElRef.current AND calls scrollRef(el). The
    virtualizer reads scrollElRef.current via getScrollElement, so keeping
    scrollRef stable + no-op-plus-capture keeps that path working.
  * contentRef: `useRef<HTMLDivElement>(null)`. It's passed as a JSX `ref`
    prop on the sized virtualizer div (line ~1812), so RefObject typing works.
    NOTE: the current useAutoScroll returns contentRef as a callback ref
    `(el: HTMLElement | null) => void`, while task_scope asks for
    `useRef<HTMLDivElement>(null)` (a RefObject). React accepts both shapes
    when passed to a `ref=` prop, so this shape change is fine for the JSX
    consumer at line 1812 — but the `UseAutoScrollResult.contentRef` type in
    use-auto-scroll.ts is `(el: HTMLElement | null) => void`. Since we are
    NOT calling useAutoScroll anymore, we are NOT bound by that interface —
    the destructured names just need to typecheck at their consumer sites.
    Verify with `npx tsc --noEmit`.
  * scrollToBottomAndFollow: `useCallback(() => {}, [])`. This is what the
    jump-to-bottom pill button calls onClick — a no-op click is the intended
    TEMP UX.
  * forceStickAndJump: `useCallback(() => {}, [])`. Assigned to
    forceStickAndJumpRef.current at line 677; handleComposeSend calls
    forceStickAndJumpRef.current() on send (line 519) — that becomes a no-op.
  * isPinnedToBottom: constant `true`. Rationale: the ONLY consumer is
    line 1923 `{!isPinnedToBottom && messages.length > 0 && (...jump pill...)}`
    — constant `true` HIDES the pill, which is the correct TEMP UX (no pill
    for a feature that doesn't work). Using `false` would leave a visible
    non-functional pill.

- Preserve the destructure shape so the surrounding code compiles unchanged:

    const scrollElLocalRef = useRef<HTMLElement | null>(null);
    const scrollRef = useCallback((el: HTMLElement | null) => {
      scrollElLocalRef.current = el;
    }, []);
    const contentRef = useRef<HTMLDivElement>(null);
    const scrollToBottomAndFollow = useCallback(() => {}, []);
    const forceStickAndJump = useCallback(() => {}, []);
    const isPinnedToBottom = true;

  (Choose a local ref name that does not collide with the existing
  `scrollElRef` at line 617 — the executor must pick a non-colliding
  identifier, e.g. `stubScrollElRef`. The point of the local ref is only to
  keep scrollRef stable / avoid unused-var lint; the real element reader
  is scrollElRef via composeScrollRefs.)

- Remove the `useAutoScroll` import from PrettyView.tsx (grep for
  `useAutoScroll` at the top of the file to find the exact import line).
  If tsc flags the import as unused otherwise, delete it. Do NOT delete
  the file it imports FROM.

STEP C — Preserve wiring (do NOT touch beyond a sanity read):

- composeScrollRefs (lines 666-672) stays intact. It calls scrollRef(el) —
  our stub captures el into scrollElLocalRef, harmless.
- forceStickAndJumpRef.current = forceStickAndJump (line 677) stays intact.
  It now points at a no-op — that's fine. handleComposeSend calling it
  becomes a no-op on send, which is the intended TEMP UX.
- The jump-to-bottom pill (lines 1923-1952) is now GUARDED by
  `!isPinnedToBottom` where isPinnedToBottom is constant `true` — the pill
  block will never render. That is the intended TEMP UX. Leave the JSX
  untouched — do NOT delete the pill block; re-enabling later is one edit.

STEP D — Rewrite stale narrative comments:

1. Lines 85-90 (RENDER-03 auto-scroll narration in the top-of-file comment).
   Current text lies about useAutoScroll being active. Rewrite to (approx.,
   preserving the numbered comment structure):

       //   2. RENDER-03 auto-scroll: TEMP-DISABLED 2026-08-10 per bounty
       //      pv-disable-auto-scroll-temp. Historically handled by
       //      `useAutoScroll` via a ResizeObserver on the inner content
       //      wrapper (`contentRef`) — any resize re-pinned to the bottom
       //      iff the user was pinned. Phase 27 virtualization made
       //      snap-to-bottom unusable; the hook call is stubbed inline
       //      (see line ~608 area) and use-auto-scroll.ts is kept in-tree
       //      as reference for the eventual redesign (bounty
       //      pv-auto-scroll-redesign).

   Do NOT delete the numbered item — future context needs the historical
   narration.

2. Lines 1906-1915 (AsideBubble narrative comment that says AsideBubble is
   "still watched by useAutoScroll's contentRef-side ResizeObserver via the
   shared scroll container's scrollHeight"). Rewrite to state auto-scroll is
   currently disabled and accessories are in-flow siblings below the sized
   container with no observer currently watching their mounts, e.g.:

       {/* Phase 27 Plan 27-02 Step B: AsideBubble mounts as an in-flow
           sibling immediately after the sized virtualizer container inside
           the scroll container. Post-refactor (Phase 27), it is no longer
           a child of the flex column that holds the messages — that column
           became the virtualizer's absolute-positioned sized container.
           AsideBubble stays visually below the message list.
           TEMP 2026-08-10 (bounty pv-disable-auto-scroll-temp): auto-scroll
           is disabled; NO observer currently watches accessories for
           scrollHeight-driven pin-to-bottom. In-flow, per ASIDE-05 — NOT
           an overlay, popup, or fixed-position element. */}

3. Also review the comment at lines 1917-1922 (jump-to-bottom pill narration
   that references `scrollToBottomAndFollow` setting `followBottomRef=true`).
   That description was already loosely accurate for the pre-stub hook.
   Under the stub, `scrollToBottomAndFollow` is a no-op and the pill is
   hidden. Add a one-line TEMP note inside that comment block, e.g.:

       {/* TEMP 2026-08-10 (bounty pv-disable-auto-scroll-temp): stubbed
           to no-op; pill is currently hidden because isPinnedToBottom is
           forced true. */}

STEP E — Sanity re-read (grep, do NOT re-read the whole file):

- `grep -n "useAutoScroll" src/ui/features/pretty-view/PrettyView.tsx` →
  should show ONLY commented mentions (in the rewritten narrative comments
  and the TEMP marker), no live call. If a live call remains, STOP and fix.
- `grep -n "scrollToBottomAndFollow\\|forceStickAndJump\\|isPinnedToBottom\\|contentRef\\|scrollRef" src/ui/features/pretty-view/PrettyView.tsx` →
  should show the stub declarations, the composeScrollRefs call, the
  forceStickAndJumpRef assignment, the sized-div `ref={contentRef}`, the
  pill's `!isPinnedToBottom` guard, and the pill's onClick — nothing else
  unexpected.
- Do NOT edit anything outside these grep hits.
  </action>
  <verify>
    <automated>
# From repo root. All three MUST exit 0.
npx tsc --noEmit
npx vitest run src/ui/features/pretty-view/
# Full frontend suite — 0 failures required. If failures land OUTSIDE
# src/ui/features/pretty-view/, HALT and report (leaked-scope smell).
npx vitest run
    </automated>
  </verify>
  <done>
    - PrettyView.tsx imports no longer include `useAutoScroll` (grep shows only
      comment mentions).
    - The stub block (six declarations: scrollElLocalRef, scrollRef, contentRef,
      scrollToBottomAndFollow, forceStickAndJump, isPinnedToBottom) is present
      at the former useAutoScroll call site (~line 608 area) and is preceded by
      the exact TEMP 2026-08-10 marker comment.
    - Lines 85-90, 1906-1915, and 1917-1922 comments rewritten per Step D;
      no removed narration (only added/rewritten).
    - `const paneKey = \`${hostId}::${tmuxSession}\`;` is preserved (still
      consumed by paneKeyRef comparison at ~L714/L746).
    - composeScrollRefs, forceStickAndJumpRef assignment, and the
      jump-to-bottom pill JSX block are STRUCTURALLY INTACT (guarded by the
      constant-true isPinnedToBottom, so the pill just never renders).
    - src/ui/features/pretty-view/use-auto-scroll.ts is byte-identical to
      the pre-change version (`git diff -- src/ui/features/pretty-view/use-auto-scroll.ts`
      shows no output).
    - No files outside src/ui/features/pretty-view/PrettyView.tsx are
      modified (`git status --porcelain` shows only that one M line).
  </done>
</task>

<task type="auto">
  <name>Task 2: Adjust obsolete auto-scroll behavior tests + verify full frontend suite green</name>
  <files>src/ui/features/pretty-view/*.test.{ts,tsx}</files>
  <action>
Only if `npx vitest run src/ui/features/pretty-view/` from Task 1 reports
failures. If Task 1's suite was already green, SKIP THIS TASK ENTIRELY and
proceed to Task 3.

If failures exist, categorize each failing test:

(a) Test asserts pin-to-bottom / snap-to-bottom / scroll-follow behavior
    (e.g. "scrolls to bottom on new message", "sticks to bottom after send",
    "re-pins after paneKey change"). These test a now-disabled feature.
    - PREFERRED: `it.skip(...)` with a pointer comment naming the bounties:
      `// SKIP TEMP 2026-08-10: auto-scroll disabled per bounty
      // pv-disable-auto-scroll-temp — restore this assertion when
      // bounty pv-auto-scroll-redesign lands.`
    - ACCEPTABLE alternative: rewrite the assertion to check the current
      TEMP behavior ("scroll position does NOT change on new message
      append") with the same bounty-pointer comment.

(b) Test asserts don't-yank-on-scroll-up (e.g. "does not scroll on
    scroll-up gesture"). Trivially satisfied by no auto-scroll at all —
    leave as-is; it may still pass. Do NOT preemptively edit.

(c) Test asserts jump-to-bottom PILL visibility. Under the stub,
    `isPinnedToBottom` is constant `true`, so the pill never renders.
    - If the test asserts "pill visible when scrolled up", `it.skip(...)`
      with the same bounty-pointer comment (feature disabled).
    - If the test asserts "pill NOT visible at bottom" → still passes,
      leave as-is.

(d) Any other failing test in `src/ui/features/pretty-view/*` that is NOT
    obviously an auto-scroll test → STOP and report. This is a scope
    leak signal — do NOT paper over it by skipping.

DO NOT delete tests wholesale. Skip or update with bounty-pointer comments
only.

After edits, re-run:

    npx vitest run src/ui/features/pretty-view/
    npx vitest run

Both must be green. If failures land OUTSIDE src/ui/features/pretty-view/
at any point, HALT and report — do NOT commit red.
  </action>
  <verify>
    <automated>
npx vitest run src/ui/features/pretty-view/
npx vitest run
    </automated>
  </verify>
  <done>
    - Both vitest commands exit 0.
    - Any modified tests carry a bounty-pointer comment mentioning
      `pv-disable-auto-scroll-temp` and `pv-auto-scroll-redesign`.
    - No test files outside src/ui/features/pretty-view/ were modified
      (`git status --porcelain` shows only pretty-view/ files).
    - No tests were deleted (`git diff --stat --diff-filter=D` for the
      test dir shows nothing).
  </done>
</task>

<task type="auto">
  <name>Task 3: Land one commit — stop at commit + green, do NOT deploy</name>
  <files>none (git only)</files>
  <action>
Preconditions — ALL must hold before this task runs:

- `npx tsc --noEmit` exit 0
- `npx vitest run` exit 0 (full suite, not just pretty-view)
- `git status --porcelain` shows ONLY changes under
  src/ui/features/pretty-view/ (PrettyView.tsx guaranteed, optionally
  test files if Task 2 ran). If ANY other path appears, HALT and report.
- `git diff -- src/ui/features/pretty-view/use-auto-scroll.ts` shows no
  output (file untouched).

If any precondition fails, HALT and report — do NOT commit.

Stage only pretty-view files explicitly (do NOT `git add -A` / `git add .`):

    git add src/ui/features/pretty-view/PrettyView.tsx
    # Only if Task 2 modified test files:
    git add src/ui/features/pretty-view/*.test.tsx src/ui/features/pretty-view/*.test.ts 2>/dev/null || true

Commit with a HEREDOC message:

    git commit -m "$(cat <<'EOF'
    fix(pretty-view): temporarily disable snap-to-bottom auto-scroll (pv-disable-auto-scroll-temp)

    Phase 27 virtualization made snap-to-bottom unusable. Stubs the
    useAutoScroll(paneKey) call in PrettyView.tsx with an inline no-op
    that matches the return-shape (scrollRef captures the element,
    contentRef is a RefObject, scrollToBottomAndFollow / forceStickAndJump
    are no-ops, isPinnedToBottom is constant true so the jump-to-bottom
    pill stays hidden).

    Kept use-auto-scroll.ts in-tree UNTOUCHED as reference for the
    eventual redesign (bounty pv-auto-scroll-redesign). Rewrote stale
    RENDER-03 auto-scroll narration and the AsideBubble ResizeObserver
    comment so they no longer lie about behavior. TEMP 2026-08-10 marker
    at the stub site names both bounties.

    Verified: npx tsc --noEmit clean; full vitest suite green.
    EOF
    )"

    git status

STOP HERE. Do NOT run:
  - git push
  - docker build
  - docker compose up / --force-recreate
  - any deploy script

Deploy is orchestrator-only (tina). This executor's job ends at commit +
tests green.
  </action>
  <verify>
    <automated>
# Confirm exactly one new commit on HEAD, no unstaged changes, no push.
git log -1 --pretty=%s | grep -F "pv-disable-auto-scroll-temp"
git status --porcelain | wc -l | grep -x 0
    </automated>
  </verify>
  <done>
    - Exactly one new commit exists at HEAD whose subject contains
      `pv-disable-auto-scroll-temp`.
    - `git status --porcelain` is empty (clean working tree).
    - No `git push`, no docker command, no deploy motion was executed.
  </done>
</task>

</tasks>

<verification>
Full-plan verification (all must hold before this plan is marked complete):

- `npx tsc --noEmit` exits 0.
- `npx vitest run src/ui/features/pretty-view/` exits 0.
- `npx vitest run` (full frontend suite) exits 0.
- `git diff --name-only HEAD~1 HEAD` lists ONLY files under
  `src/ui/features/pretty-view/` (PrettyView.tsx guaranteed; optionally
  test files). NO other paths. NO deletions.
- `git diff HEAD~1 HEAD -- src/ui/features/pretty-view/use-auto-scroll.ts`
  is empty (that file was NOT modified).
- `grep -n "useAutoScroll" src/ui/features/pretty-view/PrettyView.tsx`
  shows only comment mentions (no live import, no live call).
- `grep -n "TEMP 2026-08-10" src/ui/features/pretty-view/PrettyView.tsx`
  shows at least the marker above the stub block.
- No `git push`, no docker command, no deploy motion in the shell history
  since Task 1 started.
</verification>

<success_criteria>
- ONE commit at HEAD titled
  `fix(pretty-view): temporarily disable snap-to-bottom auto-scroll (pv-disable-auto-scroll-temp)`
- PrettyView renders (via the vitest render-path tests) without invoking
  any useAutoScroll machinery — no ResizeObserver install for scroll,
  no gesture listeners, no rAF-chain, no overflow-anchor mutation.
- Jump-to-bottom pill is hidden (isPinnedToBottom constant true) —
  intended TEMP UX.
- use-auto-scroll.ts preserved byte-for-byte for the redesign bounty.
- Stale narrative comments (RENDER-03 auto-scroll, AsideBubble
  ResizeObserver lie, jump-to-bottom pill narration) rewritten to
  reflect TEMP-disabled state and name both bounties.
- No deploy motion. Working tree clean. Stopped at commit.
</success_criteria>

<output>
Executor writes summary to
`.planning/quick/260810-299-pv-disable-auto-scroll-temp/260810-299-SUMMARY.md`
when done. Summary must include:

- The single commit SHA
- `git diff --stat HEAD~1 HEAD` output (proves scope containment)
- The three verify commands' exit statuses
- Any tests that were skipped/updated in Task 2 with the specific test
  names + skip reason
- Explicit confirmation: "No push. No docker. No deploy."
</output>
