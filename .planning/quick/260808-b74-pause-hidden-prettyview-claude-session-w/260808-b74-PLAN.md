---
phase: quick-260808-b74
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/terminal/Terminal.tsx
  - /home/ubuntu/.claude/roles/box-maintainer/skynet-patches.md
autonomous: false
requirements:
  - BOUNTY-pause-hidden-pretty-view-ws

must_haves:
  truths:
    - "When a PrettyView pane is hidden (isVisible=false), its Claude-session WebSocket is closed within one render tick."
    - "When a hidden PrettyView pane becomes visible again (isVisible=false→true), a fresh Claude-session WebSocket is opened via the existing patch #148 reconnect scheduler (retryKey bump)."
    - "The patch #148 auto-reconnect loop does NOT reopen a WS behind our back while isVisible=false — the retry scheduler is gated on isVisible."
    - "Messages state (bubbles, contextPct, harnessTasks, backgroundedAgents, backgroundedShells) is preserved across the hidden→visible transition — nothing is torn out of React state on close, and appendDedup absorbs tail-replay dupes on reopen."
    - "URL-restore active-set behavior is untouched: Terminal.tsx:2904's `attach` gate is not modified, and PrettyView only mounts when isPrettyMode is on (behavior unchanged at Terminal.tsx:3044)."
    - "Full npm test suite is green (baseline 1556 pass / 6 skip)."
    - "After ship + Ashley PWA UAT with hidden panes, diag-report shows hidden pretty-view panes emit cldFramesSinceLast ≈ 0 (down from baseline 10-13/30s)."
  artifacts:
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "isVisible prop, WS-lifecycle effect gated on isVisible, patch #148 retry loop gated on isVisible"
      contains: "isVisible"
    - path: "src/ui/features/terminal/Terminal.tsx"
      provides: "isVisible plumbed to PrettyView mount at line 3045"
      contains: "isVisible={isVisible}"
    - path: "/home/ubuntu/.claude/roles/box-maintainer/skynet-patches.md"
      provides: "Patch #344 changelog entry"
      contains: "## Patch #344"
  key_links:
    - from: "src/ui/features/terminal/Terminal.tsx (~line 3045 PrettyView mount)"
      to: "src/ui/features/pretty-view/PrettyView.tsx PrettyViewProps.isVisible"
      via: "new JSX prop isVisible={isVisible}"
      pattern: "isVisible=\\{isVisible\\}"
    - from: "PrettyView.tsx WS-lifecycle useEffect (existing, ~line 585-931)"
      to: "wsRef.current.close() + retryKey scheduler"
      via: "new isVisible-gated effect and guard inside onclose retry scheduler"
      pattern: "isVisible"
---

<objective>
Iteration 1 of the parent bounty `hidden-pane-cost-mitigation-empirical-rotation`: close the PrettyView Claude-session WebSocket when the pane is not visible; reopen when it becomes visible. Baseline diag shows 3 hidden PrettyViews contributing 33 combined Claude WS frames/30s + React re-render churn; after this iteration that drops to ~0 for hidden panes.

Purpose: Kill the biggest, simplest-to-eliminate per-hidden-pane cost on Ashley's iPhone 16 Pro Max PWA where 4-5 active-set sessions currently make the app chug. Patterns on the reconnect fix already shipped (patch #339) — the close→status='error'→bubbles-stay-visible→reopen machinery is already in place, so hidden→visible re-warm is the SAME UX pattern.

Output: PrettyView.tsx consumes new `isVisible: boolean` prop; Terminal.tsx passes it at the mount site; WS pauses on !isVisible, resumes on isVisible; existing patch #148 reconnect loop is gated on isVisible so it does not fight the pause; full test suite green; patch #344 shipped under 15-min deadman with HTTPS 200 health check; changelog entry added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/ubuntu/skynet/CLAUDE.md
@/home/ubuntu/.claude/roles/box-maintainer/bounties/pause-hidden-pretty-view-ws/bounty.json
@/home/ubuntu/.claude/roles/box-maintainer/bounties/hidden-pane-cost-mitigation-empirical-rotation/bounty.json
@/home/ubuntu/skynet/src/ui/features/pretty-view/PrettyView.tsx
@/home/ubuntu/skynet/src/ui/features/terminal/Terminal.tsx

# Architecture facts already loaded during planning:
# - PrettyView.tsx PrettyViewProps declared at ~line 96-139; component destructure at 171-182; component body starts at 183.
# - wsRef declared at PrettyView.tsx:483; WS-setup useEffect at 585-931 (deps [hostId, tmuxSession, retryKey]).
# - onclose handler at 878-914 sets status='error' and schedules a retry via setRetryKey unless status==='inactive' or attempt cap hit.
# - statusRef mirror at 981-983 keeps statusRef.current fresh for the onclose read.
# - visibilitychange handler at 954-976 is the model for how a NEW effect can bump retryKey to trigger reconnect via existing machinery.
# - Terminal.tsx SSHTerminalProps declares isVisible: boolean at line 84; destructured at 114; already referenced elsewhere (refs 321/580/649). It is NOT currently passed to PrettyView at the mount site (Terminal.tsx:3044-3049).
# - Terminal.tsx:2904 `attach` gate is INTENTIONAL for URL-restore — do NOT touch it (iteration 2 scope).
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add isVisible prop + WS-pause lifecycle to PrettyView</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx</files>
  <behavior>
    - New required prop `isVisible: boolean` added to PrettyViewProps.
    - Component destructures `isVisible` from props.
    - New `isVisibleRef` mirrors current `isVisible` (parallel to statusRef pattern at line 981-983) so the onclose retry scheduler can read it without adding isVisible to the main WS-setup effect deps (which would cause the WS to tear down and rebuild on every visibility flip — we want the NEW effect to own that, cleanly).
    - New WS-pause useEffect keyed on `[isVisible]`:
        * When `isVisible` flips false AND `wsRef.current` is non-null AND its readyState is OPEN (1) or CONNECTING (0): call `wsRef.current.close()`. The existing onclose at line 878-914 will fire; because isVisibleRef.current is now false, the retry scheduler will short-circuit (see next bullet) instead of scheduling a reopen. status transitions to 'error' which the patch #339 render gates already handle (bubbles stay visible, Send disabled, mic/attach live). Also clear any already-scheduled reconnect timer (reconnectTimeoutRef) so a timer in flight from a prior transient does not fire behind our back.
        * When `isVisible` flips true AND wsRef.current is null OR its readyState is CLOSING (2) / CLOSED (3): reset reconnectAttemptsRef.current = 0 (fresh budget, mirrors visibilitychange handler at 968-970) and call setRetryKey(k => k + 1) to trigger the existing WS-setup effect (deps include retryKey) to open a fresh WS. If wsRef is already OPEN or CONNECTING, no-op.
        * If `isVisible === true` on initial mount (first effect run), no-op — the main WS-setup effect at 585-931 is already opening the WS on mount; do not double-open.
    - Patch the onclose retry scheduler (lines 894-914): before scheduling the retry via setTimeout+setRetryKey, add a guard `if (!isVisibleRef.current) return;` immediately after the existing `if (statusRef.current === 'inactive')` block. Keep the setStatus('error') and setErrorMessage('Connection closed') calls unconditional — the render gates from patch #339 rely on status='error' to keep bubbles visible during the pause, exactly like a real reconnect.
    - Patch the visibilitychange handler (lines 954-976): before its "reconnect if needed" branch, also short-circuit when `!isVisibleRef.current` — a foreground event on a currently-hidden pane must NOT open a WS behind our back. Insert `if (!isVisibleRef.current) return;` right after the existing `if (statusRef.current === 'inactive') return;` line.
    - Also add a matching statusRef-style mirror useEffect for isVisible: `useEffect(() => { isVisibleRef.current = isVisible; }, [isVisible]);` placed adjacent to the statusRef mirror at 981-983 for locality.
    - Preserve messages/contextPct/harnessTasks/backgroundedAgents/backgroundedShells/planPending/asideText/isHolding state entirely — no setState calls in the pause path. The fresh-pane reset branch (line 592-619) is untouched because it fires only when paneKey changes, not on isVisible flips. On re-open, appendDedup absorbs any tail-replay dupes from the fresh tail's re-hydration exactly as it does on reconnect.
    - Add a concise inline comment block above the new pause effect referencing:
        * Parent bounty id (`hidden-pane-cost-mitigation-empirical-rotation`) and iteration 1.
        * Patch #339 as the template (reconnect UX inherited for free — bubbles stay visible via status='error' render gates).
        * The isVisibleRef gate on the retry scheduler (why: prevents patch #148 from fighting the pause).
        * The isVisibleRef gate on the visibilitychange handler (why: iOS PWA foreground event must not open a hidden pane).
        * That messages state is deliberately preserved (why: instant re-render on re-show; tail dedup handles replay).
  </behavior>
  <action>
    Modify PrettyView.tsx as specified in <behavior>. Do NOT create new files. Do NOT touch the main WS-setup useEffect deps (still `[hostId, tmuxSession, retryKey]` — do NOT add isVisible). Do NOT touch the fresh-pane reset branch. Do NOT touch the ComposeBox mount gate or the scroll-region gate (patch #339 already covers status='error' rendering — nothing to duplicate). Do NOT change Status type or add a new status value. Do NOT invent a new connect path — reuse setRetryKey. Do NOT create a `pausedByHiddenRef` — the isVisibleRef IS the single knob (per bounty design bullet 4, "single knob" is preferred).

    Verification of the WS-pause behavior is subtle enough that a targeted unit test would risk being brittle against the WS setup effect's internals — the bounty's own VERIFY section notes "No new test file required if the change is a pure prop + effect wiring". Skip a targeted new test file and instead rely on the full `npm test` suite green + Ashley's UAT + diag-pull post-ship as the verification story (matches bounty guidance).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - PrettyViewProps has `isVisible: boolean` field (required, not optional — Terminal.tsx is the sole caller and always has isVisible in scope).
    - Component destructures isVisible.
    - isVisibleRef declared adjacent to statusRef (line ~505), with a mirror useEffect adjacent to the statusRef mirror.
    - New WS-pause useEffect keyed on [isVisible] implements close-on-hide and setRetryKey-on-show as specified.
    - onclose retry scheduler and visibilitychange handler both guarded on `!isVisibleRef.current` before scheduling/triggering reconnects.
    - `npx tsc --noEmit` passes with no new errors introduced by this task.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire isVisible through Terminal.tsx PrettyView mount + run full test suite</name>
  <files>src/ui/features/terminal/Terminal.tsx</files>
  <behavior>
    - Terminal.tsx:3045 PrettyView JSX gains one new prop: `isVisible={isVisible}`. The `isVisible` name is already in scope (destructured at line 114 from SSHTerminalProps).
    - No other JSX changes; no changes to the `attach` gate at Terminal.tsx:2904; no changes to the xterm div's isVisible-driven pointerEvents at line 3031.
    - After the wiring, full `npm test` suite is green — baseline is 1556 pass / 6 skip per bounty (source: patch #343 ship notes). No new tests required from Task 1; existing WS-lifecycle tests should still pass because the fresh-pane mount path is unchanged (a mount with isVisible=true behaves identically to today — the pause effect no-ops on first mount because wsRef is opening; onclose retry gate only trips when isVisibleRef is false, which never happens in the current-visible-tab test path).
    - If any test fails with "PrettyViewProps requires isVisible", find the offending test file and add `isVisible={true}` (or `isVisible={false}` where the test is asserting hidden-state behavior — unlikely for pre-existing tests). Do NOT stub or skip tests to make them pass.
  </behavior>
  <action>
    Edit Terminal.tsx line 3045 area: add `isVisible={isVisible}` to the PrettyView JSX prop list (natural placement is right after `hostId=...` or right before `isIdle={isIdle}` — pick placement that reads cleanly alongside existing props).

    Then from /home/ubuntu/skynet: run `npm test` to full completion. If any test fails, fix it — do NOT proceed to ship until the suite is green at baseline 1556 pass / 6 skip.

    Also run `npx tsc --noEmit` (whole-repo typecheck) to catch any consumer that instantiates PrettyView with the new required prop missing. If test files instantiate PrettyView directly they will need `isVisible` too.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -5 &amp;&amp; npm test 2>&amp;1 | tail -25</automated>
  </verify>
  <done>
    - Terminal.tsx:3045 PrettyView JSX includes `isVisible={isVisible}`.
    - `npx tsc --noEmit` passes cleanly (no new errors).
    - `npm test` reports 1556 pass / 6 skip (or a higher pass count if new tests were added by other work) — no failures, no unexpected new skips introduced by this change.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Ship under 15-min deadman + Ashley PWA UAT + diag pull + patches.md entry</name>
  <what-built>
    - PrettyView's Claude-session WS closes when the pane is hidden (isVisible=false) and reopens when it becomes visible. Existing patch #148 auto-reconnect scheduler and iOS PWA visibilitychange handler are gated on isVisible so they do not fight the pause. Bubbles/state preserved across the pause via patch #339's status='error' render gates.
    - Terminal.tsx wires the existing `isVisible` prop into PrettyView's mount site.
    - Baseline expected win per bounty: 3 hidden pretty-view panes drop from 10+13+10 = 33 Claude WS frames/30s to ~0.
  </what-built>
  <how-to-verify>
    Executor runs these BEFORE calling the checkpoint (all automated, all under the 15-min deadman):

    1. Rebase + commit locally per skynet norm. Commit message subject:
       `Fix(pretty-view): pause claude-session WS on !isVisible + gate patch #148 retry loop (iteration 1 of hidden-pane cost mitigation) [quick-260808-b74]`

    2. Ship via the deadman-guarded compose recreate:
       `docker compose up -d --force-recreate skynet` under the standard 15-min deadman rollback timer per /home/ubuntu/skynet/CLAUDE.md (deploy safety constraint).

    3. HTTPS 200 health check after container comes up healthy:
       `curl -sSfI https://term.gigaashley.click | head -5` — must return HTTP/2 200.

    4. Append Patch #344 entry to /home/ubuntu/.claude/roles/box-maintainer/skynet-patches.md following the format of #343 (title line + one-paragraph body citing: parent bounty, iteration 1, isVisible prop wiring, isVisibleRef gate on retry scheduler + visibilitychange, patch #339 as the render-gate template, expected diag delta for hidden panes, commit SHA, files touched, quick-task slug 260808-b74).

    Then hand off to Ashley for UAT (this checkpoint):

    A. Ashley loads term.gigaashley.click on her iPhone 16 Pro Max PWA and clicks through 4 sessions (nelly + 3 others) so 3 end up hidden.
    B. Wait ~45 seconds for the diag emitter to tick past the pause window.
    C. Pull diag on the host:
       `sudo grep DIAG-REPORT /opt/skynet/console-forward-logs/console-forward.log | tail | jq -r '.msg' | sed 's/^\[DIAG-REPORT\] //' | jq .`
    D. EXPECTED: for the 3 hidden pretty-view panes, cldFramesSinceLast ≈ 0 (baseline was 10-13/30s each). messageCount unchanged. domNodeCount unchanged.
    E. Ashley switches back to a paused pane — expected re-warm feel: ~2s reconnect, bubbles stay visible during the flash, Send button disabled during the reconnect window (all inherited from patch #339). Compare to iteration goal: "acceptable = keep. too slow = re-evaluate."
    F. Ashley answers the parent bounty's iteration decision matrix:
       - Does the PWA still chug with 4 active-set sessions after this change?
       - Is the re-warm latency on switching back to a paused pane acceptable?
       - Diag numbers: how much did per-pane cost drop for hidden panes?
    G. Per the standing directive noted in the bounty todos: ask Ashley about DMing Stacy.
  </how-to-verify>
  <resume-signal>
    Ashley replies with UAT read + diag delta. If green ("acceptable + numbers dropped") → mark iteration 1 done and stage iteration 2 (pause hidden Terminal SSH WS) in the parent bounty timeline. If re-warm feel is bad → keep the code, open a follow-up sub-bounty for smoothing the re-warm before iteration 2 lands. Executor writes a QUICK-SUMMARY.md capturing final commit SHA, patch number (#344), diag before/after numbers, and Ashley's decision.
  </resume-signal>
</task>

</tasks>

<verification>
- Static: `npx tsc --noEmit` in /home/ubuntu/skynet returns zero new errors.
- Tests: `npm test` in /home/ubuntu/skynet reports baseline 1556 pass / 6 skip (or same pass count + zero new failures/skips).
- Ship: `docker compose up -d --force-recreate skynet` completes under 15-min deadman.
- Health: `curl -sSfI https://term.gigaashley.click | head -5` returns HTTP/2 200.
- Runtime win: diag-report shows hidden pretty-view panes with cldFramesSinceLast ≈ 0 after ~45s of hiddenness (down from baseline 10-13 per 30s per hidden pane).
- Runtime UX: Ashley confirms re-warm on switching to a paused pane is acceptable (~2s, bubbles stay visible).
- Changelog: Patch #344 entry appended to /home/ubuntu/.claude/roles/box-maintainer/skynet-patches.md.
</verification>

<success_criteria>
- PrettyView consumes `isVisible: boolean` and closes/reopens its Claude-session WS in response.
- Patch #148 retry loop AND iOS PWA visibilitychange handler both respect the isVisible pause (single-knob gate via isVisibleRef).
- No regression to URL-restore active-set attach behavior (Terminal.tsx:2904 unchanged).
- No regression to messages/state preservation (patch #339's render gates carry the re-warm UX).
- Full test suite green.
- Ship + health + Ashley UAT + diag pull all confirm the win.
- Patch #344 changelog entry lands in skynet-patches.md.
- Parent bounty timeline updated with iteration-1-complete note; iteration 2 (pause hidden Terminal SSH WS) staged as the next pickup.
</success_criteria>

<output>
Create `.planning/quick/260808-b74-pause-hidden-prettyview-claude-session-w/260808-b74-QUICK-SUMMARY.md` when done, capturing:
- Final commit SHA
- Patch number assigned (#344)
- Diag before/after numbers for hidden PrettyView panes (cldFramesSinceLast)
- Ashley's UAT verdict (acceptable / re-warm concerns / decision on iteration 2)
- Any follow-up sub-bounties opened
</output>
