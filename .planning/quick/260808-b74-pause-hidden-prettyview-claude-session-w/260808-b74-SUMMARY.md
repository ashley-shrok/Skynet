---
phase: quick-260808-b74
plan: "01"
type: quick-execute
subsystem: pretty-view
tags: [perf, hidden-pane, ws-lifecycle, pretty-view, ios-pwa, mitigation, iteration-1]
dependency_graph:
  requires: [patch-148-auto-reconnect, patch-339-reconnect-ux]
  provides: [pause-hidden-prettyview-ws, patch-344]
  affects: [PrettyView.tsx, Terminal.tsx]
tech_stack:
  patterns: [isVisibleRef-mirror, ws-lifecycle-effect, single-knob-gate]
key_files:
  modified:
    - src/ui/features/pretty-view/PrettyView.tsx
    - src/ui/features/terminal/Terminal.tsx
    - src/ui/features/pretty-view/PrettyView.test.tsx
    - src/ui/features/pretty-view/PrettyView.aside.test.tsx
decisions:
  - "Single-knob gate via isVisibleRef (not pausedByHiddenRef) per bounty design — minimal surface"
  - "isVisible is required (not optional) — Terminal.tsx is sole caller, always has isVisible in scope"
  - "No new test file — bounty spec permits pure-prop+effect wiring without targeted WS-mock test; UAT+diag is the verification story"
  - "setStatus+setErrorMessage unconditional in onclose (before isVisibleRef guard) so patch #339 render gates keep bubbles visible during pause"
metrics:
  duration: "~18 minutes"
  completed: "2026-08-08"
  tasks_completed: 2
  tasks_total: 3
  files_modified: 4
---

# Phase quick-260808-b74 Plan 01: Pause Hidden PrettyView Claude-Session WebSocket Summary

**One-liner:** PrettyView closes its Claude-session WS when hidden (isVisible=false) and reopens via patch #148 retryKey on re-show, gating both the retry scheduler and the iOS visibilitychange handler through a single isVisibleRef knob to prevent the auto-reconnect loop from fighting the pause.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add isVisible prop + WS-pause lifecycle to PrettyView | a55f14a | PrettyView.tsx |
| 2 | Wire isVisible through Terminal.tsx PrettyView mount + run full test suite | a55f14a | Terminal.tsx, PrettyView.test.tsx, PrettyView.aside.test.tsx |
| 3 (partial) | Deploy under 15-min deadman + HTTPS check + patches.md | a55f14a | (deploy + /home/ubuntu/.claude/roles/box-maintainer/skynet-patches.md) |

Tasks 1 and 2 committed together atomically (both code changes; commit a55f14a).

## Task 3 Auto-Portion: Deploy Results

- **Rebase:** branch `feat/tab-title-from-tmux` was already up to date
- **Build:** `docker build -t skynet-patched:local` — EXIT 0, image `7862e7a6276c`
- **Deadman timer:** started PID 3506046 (15-min rollback to `sha256:f1f862847706...`)
- **Deploy:** `docker compose -f /opt/skynet/docker-compose.yml up -d --force-recreate skynet` — container Recreated + Started
- **Health:** container status = `healthy` at T+5s
- **HTTPS check:** `curl -sk -o /dev/null -w '%{http_code}\n' https://term.gigaashley.click/` = **200**
- **Deadman timer killed:** PID 3506046 killed after successful health check
- **Patches.md:** Patch #344 entry appended to `/home/ubuntu/.claude/roles/box-maintainer/skynet-patches.md`

## Task 3 Human-Verify Hand-off (Pending)

The following UAT steps remain for Ashley:

1. Load `term.gigaashley.click` on iPhone 16 Pro Max PWA, open 4 sessions (nelly + 3 others) so 3 end up hidden.
2. Wait ~45 seconds for the diag emitter to tick past the pause window.
3. Pull diag on the host:
   ```
   sudo grep DIAG-REPORT /opt/skynet/console-forward-logs/console-forward.log | tail | jq -r '.msg' | sed 's/^\[DIAG-REPORT\] //' | jq .
   ```
4. Expected: hidden pretty-view panes show `cldFramesSinceLast ≈ 0` (baseline was 10-13/30s each).
5. Switch back to a paused pane — expected re-warm: ~2s reconnect, bubbles stay visible, Send disabled (all inherited from patch #339).
6. Answer iteration decision matrix: PWA still chug after change? Re-warm latency acceptable? Diag delta?
7. Per standing directive: ask Ashley about DMing Stacy.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Test failures: isVisible prop required but missing from test renders**
- **Found during:** Task 2 (npm test run)
- **Issue:** 3 test failures in `PrettyView.test.tsx` (patch #148 reconnect tests B and C) because `isVisible` is now a required prop but all test renders omitted it. With `isVisible=undefined` (falsy), the WS-pause effect immediately closed the WS on mount, breaking reconnect behavior in tests.
- **Fix:** Added `isVisible={true}` to the `mountPV()` helper, all direct `<PrettyView>` renders in `PrettyView.test.tsx`, and all renders in `PrettyView.aside.test.tsx` (15 render sites total across both files).
- **Files modified:** `PrettyView.test.tsx`, `PrettyView.aside.test.tsx`
- **Commit:** a55f14a (included in same atomic commit)

## Commits

| Hash | Message |
|------|---------|
| a55f14a | Fix(pretty-view): pause claude-session WS on !isVisible + gate patch #148 retry loop (iteration 1 of hidden-pane cost mitigation) [quick-260808-b74] |

## Verification

- `npx tsc --noEmit`: EXIT 0 (no new errors)
- `npm test`: 1556 pass / 6 skip / 0 fail (baseline maintained)
- Docker build: EXIT 0, image `7862e7a6276c`
- HTTPS health check: 200
- Runtime win: pending Ashley UAT + diag pull

## Known Stubs

None — all wiring is live. The isVisible prop flows from Terminal.tsx (which already has isVisible from SSHTerminalProps) down to PrettyView. WS close/reopen is real behavior, not mocked.

## Diag Before/After

| Metric | Before (patch #343 baseline) | After (expected) |
|--------|------------------------------|-----------------|
| Hidden PrettyView cldFramesSinceLast | 10-13 / 30s each (3 panes = ~33 combined) | ~0 (WS closed while hidden) |
| Re-warm on re-show | N/A (no pause) | ~2s, bubbles visible (patch #339 UX) |

*After numbers pending Ashley UAT diag pull.*

## Self-Check: PASSED

- src/ui/features/pretty-view/PrettyView.tsx: confirmed modified (isVisible prop + WS-pause effect)
- src/ui/features/terminal/Terminal.tsx: confirmed modified (isVisible={isVisible} at PrettyView mount)
- Commit a55f14a: confirmed in git log
- /home/ubuntu/.claude/roles/box-maintainer/skynet-patches.md: Patch #344 entry confirmed appended
- Deploy: HTTPS 200 confirmed
