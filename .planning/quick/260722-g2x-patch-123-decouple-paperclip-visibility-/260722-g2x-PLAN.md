---
phase: quick-260722-g2x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/ComposeBox.tsx
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-view/ComposeBox.test.tsx
autonomous: true
requirements:
  - QUICK-260722-g2x
tags:
  - compose-box
  - pretty-view
  - paperclip
  - patch-123

must_haves:
  truths:
    - "Desktop pretty-view shows the paperclip attach button in ComposeBox aux row"
    - "Desktop ComposeBox top row keeps the compact min-h-8 height (NOT min-h-[44px])"
    - "Touch devices still get the min-h-[44px] top row for WCAG 2.5.5 touch target"
    - "Paperclip visibility and touch-target height are gated by two independent props"
    - "Phase 9 layout tests pass with the new decoupled prop names"
    - "tsc clean and vitest green — no other ComposeBox behavior regressed"
  artifacts:
    - path: "src/ui/features/pretty-view/ComposeBox.tsx"
      provides: "ComposeBoxProps with independent isTouchDevice + showPaperclip props"
      contains: "isTouchDevice?: boolean"
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "ComposeBox mount site passing both showPaperclip={true} and isTouchDevice={isTouchDevice}"
      contains: "isTouchDevice={isTouchDevice}"
    - path: "src/ui/features/pretty-view/ComposeBox.test.tsx"
      provides: "Phase 9 layout tests asserting row height via isTouchDevice prop, not showPaperclip"
  key_links:
    - from: "src/ui/features/pretty-view/ComposeBox.tsx"
      to: "top row min-h class"
      via: "isTouchDevice ternary (not showPaperclip)"
      pattern: "isTouchDevice \\? \"min-h-\\[44px\\]\" : \"min-h-8\""
    - from: "src/ui/features/pretty-view/PrettyView.tsx"
      to: "ComposeBox showPaperclip prop"
      via: "showPaperclip={true} (always on) + isTouchDevice={isTouchDevice} (peer)"
      pattern: "showPaperclip=\\{true\\}"
---

<objective>
Patch #123 — decouple paperclip visibility from touch-target height in the pretty-view ComposeBox so Ashley gets the paperclip attach button on desktop without the chunky 44px WCAG touch row.

Purpose: The current `showPaperclip` prop conflates two concerns (button mount + row height). Patch #121 removed the vestigial Send button and freed room in the aux row, so Ashley now wants the paperclip visible on desktop pretty-view too — but keeping the compact `min-h-8` row on desktop. Splitting the concerns behind two props (`showPaperclip` = button mount, `isTouchDevice` = row height) makes each gate honest and lets desktop show the paperclip in a compact row.

Output: Single atomic commit `feat(compose): patch #123 — decouple paperclip visibility from touch-target height (desktop paperclip)` touching 3 files. tsc clean + vitest green. NOT deployed — Tina handles batch deploy + UAT separately.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/ui/features/pretty-view/ComposeBox.tsx
@src/ui/features/pretty-view/PrettyView.tsx
@src/ui/features/pretty-view/ComposeBox.test.tsx

Prior patches (background, no re-read required):
- Patch #102: `useIsTouchDevice()` hook — SOLE mobile-vs-desktop discriminator (pointer:coarse + hover:none). Currently threaded to ComposeBox as the `showPaperclip` prop.
- Patch #115: Phase 9 ComposeBox 2-row shell restructure. Introduced `min-h-[44px]` on Row 1 (instrument bar) gated by `showPaperclip`.
- Patch #116: Rotated context meter 90° horizontal (Row 1 orientation).
- Patch #117: Phase 9 UAT round 1 fixes — reset cell LEFT, `min-h-8!` textarea override, uniform-band meter color mode.
- Patch #121: Removed vestigial Send button + `sendDisabled` derived state. Aux row is now Paperclip / Square / ThumbsUp / Hourglass — vacated room is the "seam" this patch fills on desktop.

Current wiring (what we're changing):
- ComposeBox.tsx L164-167: JSDoc for `showPaperclip` says "desktop NEVER sees the paperclip regardless of window width" — obsolete after this patch.
- ComposeBox.tsx L200: destructures `showPaperclip` from props.
- ComposeBox.tsx L905-908: Row 1 ternary uses `showPaperclip ? "min-h-[44px]" : "min-h-8"` — the two-concerns-conflated line.
- ComposeBox.tsx L1086-1092: Paperclip Button block comment repeats the "Desktop NEVER sees this" language and gates the button on `showPaperclip`. Button gate stays; comment updates.
- PrettyView.tsx L224: `const isTouchDevice = useIsTouchDevice();` already exists — reuse it, no new hook call.
- PrettyView.tsx L828-833: mount site wires `showPaperclip={isTouchDevice}` with a comment claiming `showPaperclip` is "the useIsTouchDevice() output — SOLE gate." Rewire and rewrite comment.
- ComposeBox.test.tsx L342-376: Two Phase 9 Layout tests key row-height assertions to `showPaperclip: true/false`. Rewire to use the new `isTouchDevice` prop.
- ComposeBox.test.tsx L82-102: Tests 3 & 4 assert paperclip VISIBILITY via `showPaperclip: false/true` — these stay as-is (they test the visibility prop, which still governs visibility).
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Split showPaperclip into two independent props in ComposeBox.tsx</name>
  <files>src/ui/features/pretty-view/ComposeBox.tsx</files>
  <behavior>
    After this task the ComposeBox component accepts two independent props:
    - `showPaperclip?: boolean` — gates whether the Paperclip Button element renders (unchanged behavior at the Button gate site, updated JSDoc)
    - `isTouchDevice?: boolean` — gates the Row 1 top-row height between `min-h-[44px]` (touch, WCAG 2.5.5) and `min-h-8` (desktop compact)

    Neither prop implies the other. All 3 combinations must be valid:
    - `showPaperclip=true, isTouchDevice=true` → paperclip visible, 44px row (touch device — legacy behavior)
    - `showPaperclip=true, isTouchDevice=false` → paperclip visible, min-h-8 row (desktop with paperclip — the new patch #123 mode)
    - `showPaperclip=false, isTouchDevice=false` → no paperclip, min-h-8 row (desktop no-paperclip — legacy)
    - `showPaperclip=false, isTouchDevice=true` → no paperclip, 44px row (theoretical touch-no-paperclip; not wired but must compile)
  </behavior>
  <action>
    Make four edits to `src/ui/features/pretty-view/ComposeBox.tsx`:

    1. **Type addition (~L164-167)**: In `ComposeBoxProps`, add `isTouchDevice?: boolean;` as a peer of `showPaperclip?: boolean;`. Rewrite the `showPaperclip` JSDoc comment to drop the "desktop NEVER sees the paperclip regardless of window width" language — it is no longer true. Replace with something like "Gates whether the paperclip attach button renders in the aux row. Independent of `isTouchDevice`; either or both may be true. Threaded from PrettyView." Add a new JSDoc comment above `isTouchDevice` explaining it gates the Row 1 top-row height between `min-h-[44px]` (touch, WCAG 2.5.5) and `min-h-8` (desktop compact), and cite patch #102 as the discriminator source. Reference patch #123 in one of the two comments so future maintainers can find the decoupling rationale.

    2. **Destructure (~L200)**: Add `isTouchDevice,` to the destructured props list in the component signature, next to `showPaperclip,`.

    3. **Row 1 height ternary (~L905-908)**: Change the block comment above the `<div className={cn("flex items-center gap-2", ...)}>` from the current text mentioning "min-h-[44px] on touch (showPaperclip proxy)" to text stating: touch-target height is gated on `isTouchDevice` (patch #102's touch discriminator), paperclip visibility is separately gated on `showPaperclip`, and patch #123 decoupled the two concerns. Change the ternary expression itself from `showPaperclip ? "min-h-[44px]" : "min-h-8"` to `isTouchDevice ? "min-h-[44px]" : "min-h-8"`.

    4. **Paperclip Button block comment (~L1086-1091)**: Rewrite this comment to drop the "Desktop NEVER sees this regardless of window width — it's for touch devices only" language. Replace with: paperclip is gated on `showPaperclip` (decoupled from touch-device gating by patch #123 so desktop can also opt in), and the original patch #102 / patch #104 context still applies for the mobile use case. Keep the "matches ThumbsUp's warm-neutral Glass treatment" line. The Button gate itself (`{showPaperclip && (<Button ...`) is UNCHANGED.

    Do NOT rename `showPaperclip` or change its semantics beyond the JSDoc/comment refresh. Do NOT add a default value for `isTouchDevice` — `undefined` is truthy-coerced to `false` for the ternary, which is desktop-safe and matches how `showPaperclip` is treated. Do NOT touch any other line in the file.
  </action>
  <verify>
    <automated>cd /home/ubuntu/termix && npx tsc --noEmit 2>&1 | head -20 && grep -n "isTouchDevice" src/ui/features/pretty-view/ComposeBox.tsx | head -10 && grep -c 'isTouchDevice ? "min-h-\[44px\]" : "min-h-8"' src/ui/features/pretty-view/ComposeBox.tsx</automated>
  </verify>
  <done>
    - `isTouchDevice?: boolean` present in `ComposeBoxProps` (grep hits L~167 area)
    - `isTouchDevice` present in destructured props list (grep hits L~200 area)
    - Exactly 1 occurrence of the ternary `isTouchDevice ? "min-h-[44px]" : "min-h-8"` (i.e. the row-height line switched)
    - Zero occurrences of the old ternary `showPaperclip ? "min-h-[44px]" : "min-h-8"` (verify via `grep -c 'showPaperclip ? "min-h-\[44px\]"' src/ui/features/pretty-view/ComposeBox.tsx` returning 0)
    - Paperclip Button gate `{showPaperclip && (` still present at ~L1092 (visibility gating unchanged)
    - JSDoc + Row 1 + Paperclip Button block comments no longer contain the phrase "desktop NEVER sees" (verify via `grep -ci "desktop NEVER sees" src/ui/features/pretty-view/ComposeBox.tsx` returning 0)
    - `npx tsc --noEmit` clean (no new errors in ComposeBox.tsx)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewire ComposeBox mount in PrettyView.tsx to pass both props</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx</files>
  <behavior>
    After this task, the `<ComposeBox ...>` mount site in PrettyView.tsx passes:
    - `showPaperclip={true}` (always show the paperclip — desktop AND touch)
    - `isTouchDevice={isTouchDevice}` (WCAG touch-target row height only on touch devices)

    The `isTouchDevice` local at ~L224 (already exists — do not re-declare) is now consumed by both `showPaperclip`'s conceptual sibling and directly by the new `isTouchDevice` prop.

    Comment near the mount reflects the split: `showPaperclip` = universal (always true post-#123), `isTouchDevice` = touch-target height gate.
  </behavior>
  <action>
    Two edits to `src/ui/features/pretty-view/PrettyView.tsx`:

    1. **Adjacent comment (~L828-830)**: The block comment currently reads "Phase 05 upload wiring — all sourced from the local usePrettyViewUploads hook. showPaperclip is the useIsTouchDevice() output (patch #102) — SOLE gate." Rewrite to describe the patch #123 split: `showPaperclip={true}` universally (desktop paperclip enabled by patch #123 after patch #121 freed aux-row space), `isTouchDevice={isTouchDevice}` gates the Row 1 min-h-[44px] WCAG touch target separately. Cite patch #123 and keep the patch #102 reference for `useIsTouchDevice()`.

    2. **Prop wiring (~L833)**: Replace the single line `showPaperclip={isTouchDevice}` with two lines:
       - `showPaperclip={true}` (paperclip visible everywhere per patch #123)
       - `isTouchDevice={isTouchDevice}` (WCAG touch-target row height on touch only)
       Preserve the surrounding prop order and indentation. Do NOT reorder unrelated props.

    Do NOT re-declare `isTouchDevice` — the const at L224 stays as-is. Do NOT remove or re-import `useIsTouchDevice`. Do NOT touch any other line in the file.
  </action>
  <verify>
    <automated>cd /home/ubuntu/termix && npx tsc --noEmit 2>&1 | head -20 && grep -n "showPaperclip\|isTouchDevice" src/ui/features/pretty-view/PrettyView.tsx</automated>
  </verify>
  <done>
    - Exactly one `showPaperclip={true}` at the ComposeBox mount site (~L833)
    - Zero occurrences of `showPaperclip={isTouchDevice}` (the old wiring is gone; verify via `grep -c "showPaperclip={isTouchDevice}" src/ui/features/pretty-view/PrettyView.tsx` returning 0)
    - `isTouchDevice={isTouchDevice}` present as a peer prop of `showPaperclip={true}` at the mount site
    - `const isTouchDevice = useIsTouchDevice();` at ~L224 unchanged (still exactly one declaration)
    - Adjacent comment references patch #123 and drops the "SOLE gate" claim
    - `npx tsc --noEmit` clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update Phase 9 layout tests to assert row height via isTouchDevice, run full suite + atomic commit</name>
  <files>src/ui/features/pretty-view/ComposeBox.test.tsx</files>
  <behavior>
    After this task the two Phase 9 Layout row-height tests (currently at ~L342 and ~L360 of ComposeBox.test.tsx) assert row height via the NEW `isTouchDevice` prop. The paperclip visibility prop no longer governs row height — it governs paperclip visibility only.

    Specifically:
    - **"Phase 9 Layout: mobile touch target — top row carries min-h-[44px] when isTouchDevice=true"** (title updated). Passes `isTouchDevice: true`. May keep `showPaperclip: true` for the paperclip-anchor lookup convenience, but the assertion is about row height as a function of `isTouchDevice`.
    - **"Phase 9 Layout: desktop top row carries min-h-8 when isTouchDevice=false"** (title updated). Passes `isTouchDevice: false`. Uses the ThumbsUp anchor as it already does.

    Tests 3 & 4 (paperclip-visibility tests at L82-102) are UNCHANGED — they test the `showPaperclip` prop, which still governs paperclip visibility. Other tests that pass `showPaperclip: false` for unrelated setup reasons are UNCHANGED.
  </behavior>
  <action>
    Edit `src/ui/features/pretty-view/ComposeBox.test.tsx`:

    1. **First Phase 9 Layout row-height test (~L342-358)**:
       - Change the `it(...)` title from `"Phase 9 Layout: mobile touch target — top row carries min-h-[44px] when showPaperclip=true"` to `"Phase 9 Layout: mobile touch target — top row carries min-h-[44px] when isTouchDevice=true"`.
       - In the `baseProps({...})` call at ~L345, ADD `isTouchDevice: true,` alongside the existing `showPaperclip: true,`. Keep `showPaperclip: true` because the test uses `getByLabelText(/attach file/i)` as the row anchor — the paperclip must render for that lookup to succeed. The row-height assertion is now driven by the new prop; the visibility prop is incidental scaffolding.
       - Assertions (`toContain("min-h-[44px]")` + `not.toContain("min-h-8")`) UNCHANGED.

    2. **Second Phase 9 Layout row-height test (~L360-376)**:
       - Change the `it(...)` title from `"Phase 9 Layout: desktop top row carries min-h-8 when showPaperclip=false"` to `"Phase 9 Layout: desktop top row carries min-h-8 when isTouchDevice=false"`.
       - In the `baseProps({...})` call at ~L363, ADD `isTouchDevice: false,` alongside the existing `showPaperclip: false,`. Keep `showPaperclip: false` — the test uses the ThumbsUp anchor (`getByLabelText(/send 'yes'/i)`) which is always present, so it works either way; leaving the existing value avoids a spurious diff. The row-height assertion is now driven by the new prop.
       - Assertions (`toContain("min-h-8")` + `not.toContain("min-h-[44px]")`) UNCHANGED.

    3. Do NOT touch Tests 3 & 4 (paperclip-visibility, L82-102) or any other test in the file. They correctly test the `showPaperclip` prop's actual concern (visibility) and continue to work.

    Then run the full vitest suite scoped to the ComposeBox test file plus a broad tsc pass:
    - `npx vitest run src/ui/features/pretty-view/ComposeBox.test.tsx` — expect all tests green.
    - `npx tsc --noEmit` — expect clean.

    If both green, stage the three modified files and create a single atomic commit with message:
    ```
    feat(compose): patch #123 — decouple paperclip visibility from touch-target height (desktop paperclip)
    ```
    Use `git add` on the three specific paths (ComposeBox.tsx, PrettyView.tsx, ComposeBox.test.tsx) — do NOT `git add -A`. Do NOT push. Do NOT deploy. Do NOT run the container. Do NOT amend any prior commit.
  </action>
  <verify>
    <automated>cd /home/ubuntu/termix && npx vitest run src/ui/features/pretty-view/ComposeBox.test.tsx 2>&1 | tail -20 && npx tsc --noEmit 2>&1 | head -5 && git log -1 --format="%s" 2>&1</automated>
  </verify>
  <done>
    - Both Phase 9 Layout row-height tests updated: titles reference `isTouchDevice`, `baseProps` calls include `isTouchDevice: true` and `isTouchDevice: false` respectively
    - Tests 3 & 4 (paperclip visibility, L82-102) UNCHANGED — verify via `grep -n "paperclip hidden when showPaperclip=false" src/ui/features/pretty-view/ComposeBox.test.tsx` still returning L82 area
    - Full ComposeBox test file passes vitest run (all Phase 9 Layout tests green + Tests 3 & 4 still green)
    - `npx tsc --noEmit` clean
    - Single new git commit exists with subject `feat(compose): patch #123 — decouple paperclip visibility from touch-target height (desktop paperclip)` (verify via `git log -1 --format="%s"`)
    - Commit contains exactly 3 files: ComposeBox.tsx, PrettyView.tsx, ComposeBox.test.tsx (verify via `git show --name-only HEAD | tail -5`)
    - No push, no deploy, no container recreate
  </done>
</task>

</tasks>

<verification>
Overall verification after all three tasks:

1. **tsc clean**: `cd /home/ubuntu/termix && npx tsc --noEmit` — no errors introduced.

2. **vitest scoped**: `cd /home/ubuntu/termix && npx vitest run src/ui/features/pretty-view/ComposeBox.test.tsx` — all tests green, including:
   - Test 3 (paperclip hidden when `showPaperclip=false`)
   - Test 4 (paperclip visible when `showPaperclip=true`)
   - Phase 9 Layout row-height tests (now keyed to `isTouchDevice`)
   - Phase 9 Layout aux-row + meter-horizontal + textarea-rows tests

3. **Independence-of-props sanity check via grep**:
   - `grep -c 'isTouchDevice ? "min-h-\[44px\]" : "min-h-8"' src/ui/features/pretty-view/ComposeBox.tsx` returns 1
   - `grep -c 'showPaperclip ? "min-h-\[44px\]"' src/ui/features/pretty-view/ComposeBox.tsx` returns 0
   - `grep -c 'showPaperclip={true}' src/ui/features/pretty-view/PrettyView.tsx` returns 1
   - `grep -c 'isTouchDevice={isTouchDevice}' src/ui/features/pretty-view/PrettyView.tsx` returns 1
   - `grep -c 'showPaperclip={isTouchDevice}' src/ui/features/pretty-view/PrettyView.tsx` returns 0

4. **Commit shape**: `git show --stat HEAD` shows 3 files changed. Message matches `feat(compose): patch #123 — decouple paperclip visibility from touch-target height (desktop paperclip)`.

Non-goals (do NOT run):
- No `docker compose up` / container recreate — Tina handles batch deploy behind the 15-min deadman.
- No live browser walk / UAT — Tina + Ashley handle post-deploy.
- No git push.
</verification>

<success_criteria>
- Three files modified: ComposeBox.tsx (props type + destructure + row-height ternary + JSDoc/comments), PrettyView.tsx (mount site props + comment), ComposeBox.test.tsx (two Phase 9 Layout test updates).
- `showPaperclip` prop's ONLY effect is paperclip Button visibility. `isTouchDevice` prop's ONLY effect is Row 1 top-row `min-h-*` class selection.
- PrettyView passes `showPaperclip={true}` unconditionally (desktop paperclip enabled) and `isTouchDevice={isTouchDevice}` (row height still respects touch discriminator).
- Both Phase 9 Layout row-height tests key their assertions off `isTouchDevice`. Tests 3 & 4 (paperclip-visibility) untouched and still green.
- `npx tsc --noEmit` clean. `npx vitest run src/ui/features/pretty-view/ComposeBox.test.tsx` all green.
- One atomic git commit with the specified message. No push, no deploy, no container.
</success_criteria>

<output>
Create `.planning/quick/260722-g2x-patch-123-decouple-paperclip-visibility-/260722-g2x-SUMMARY.md` when done, recording:
- Files touched with brief diff shape (e.g. "ComposeBox.tsx: +1 prop, +JSDoc, ternary swap; PrettyView.tsx: split 1 line into 2; ComposeBox.test.tsx: 2 test titles + 2 baseProps additions").
- Commit SHA.
- tsc + vitest results (clean / all green).
- Note that deploy is deferred to Tina's next batch.
</output>
