---
phase: quick-260823-hd6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/claude-session/session-file-parser.ts
  - src/backend/claude-session/session-file-parser.outbound-body.test.ts
autonomous: true
requirements: [OUTBOUND-BODY-V3]
must_haves:
  truths:
    - "substituteShellVars accepts 6 assignment shapes: sq, dq, cmd-sub cat<<EOF heredoc, cmd-sub jq -n[c] with body:'X', $'...' ANSI-C, read [flags] VAR <<EOF"
    - "Strategy 11 json-envelope-any catches {msgtype:\"m.text\", body:\"X\"} envelopes (quoted or unquoted keys) that Strategy 10 inline-json misses"
    - "Strategy 12 jq-arg-passthrough-known-var runs BEFORE substituteShellVars over the ORIGINAL sanitized cmd and returns the assigned var value directly when the --arg <name> \"$<var>\" '{msgtype:...,body:$<name>}' shape matches AND <var> is in the pre-substitution assignments map"
    - "Cmds with zero assignments detected are byte-identical no-ops (existing corpus fixtures continue to pass)"
    - "Two latent wrong-body bugs (secondary --arg captured before primary heredoc, embedded double quotes) are fixed"
    - "All 10+ new fixtures pass; all pre-existing outbound-body tests still pass; typecheck is green"
  artifacts:
    - path: "src/backend/claude-session/session-file-parser.ts"
      provides: "Extended substituteShellVars (6 shapes) + Strategies 11 + 12 + preflight pass"
      contains: "quick-260823-hd6"
    - path: "src/backend/claude-session/session-file-parser.outbound-body.test.ts"
      provides: "10+ new fixtures covering all 6 assignment shapes + Strategy 12 + latent-bug regression + no-op guard"
      contains: "quick-260823-hd6"
  key_links:
    - from: "extractOutboundBody entry point"
      to: "_build_assignments (extended, 6 shapes)"
      via: "single map built from ORIGINAL sanitized cmd, passed to both Strategy 12 preflight and substituteShellVars"
      pattern: "quick-260823-hd6"
    - from: "Strategy 12 preflight"
      to: "assignments map"
      via: "regex match on ORIGINAL cmd; if <varname> in map, return assigned value immediately (bypasses substitution)"
      pattern: "JQ_ARG_PASSTHROUGH"
    - from: "Strategies 1-11 first-match-wins"
      to: "substituted cmd (via substituteShellVars)"
      via: "chained after Strategy 12 preflight fallthrough"
      pattern: "restoreApostrophes at every return site"
---

<objective>
Port the finalized extractor_v3.py algorithm verbatim into TypeScript,
extending `substituteShellVars` from 2 assignment shapes to 6 and adding two
new strategies (11 = json-envelope-any, 12 = jq-arg-passthrough-known-var) to
`extractOutboundBody`. This is a MECHANICAL translation — the algorithm is
FINALIZED and corpus-validated at 96.3% ok on 787 real fleet outbound cmds
(t1000 + workstation, 2 weeks) with zero regressions vs the current v0 and
catches 2 latent wrong-body bugs.

Purpose: the current TS extractor misses ~19% of real fleet sends and has 2
latent wrong-body bugs (secondary `--arg body 'label'` capturing before the
primary heredoc; embedded double-quotes in substituted body breaking regex).
The Python v3 has been proven end-to-end against real corpus. This plan ports
that verified algorithm across the language boundary so pretty-view relay
bubbles render the actual body instead of a rawCommand fallback for
~19-percentage-points more of the corpus.

Output:
  - Extended `substituteShellVars` (6 shapes) in session-file-parser.ts
  - Strategy 11 (`json-envelope-any`) added as final catch-all before return null
  - Strategy 12 (`jq-arg-passthrough-known-var`) added as PRE-substitution preflight
  - 10+ new fixtures in session-file-parser.outbound-body.test.ts
  - All pre-existing tests still passing
  - Byte-identical no-op preserved when no assignments detected
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@/home/ubuntu/.claude/roles/box-maintainer/bounties/relay-outbound-cmdsub-heredoc-body/extractor_v3.py
@/home/ubuntu/skynet-tina/src/backend/claude-session/session-file-parser.ts
@/home/ubuntu/skynet-tina/src/backend/claude-session/session-file-parser.outbound-body.test.ts
@/home/ubuntu/.claude/roles/box-maintainer/bounties/relay-outbound-cmdsub-heredoc-body/corpus/all.jsonl
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port extractor_v3.py verbatim to TS (extended substituteShellVars + Strategies 11/12) with corpus-derived fixtures</name>
  <files>src/backend/claude-session/session-file-parser.ts, src/backend/claude-session/session-file-parser.outbound-body.test.ts</files>

  <behavior>
    ADDITIVE fixtures appended to session-file-parser.outbound-body.test.ts.
    Every fixture pulls from real corpus at
    /home/ubuntu/.claude/roles/box-maintainer/bounties/relay-outbound-cmdsub-heredoc-body/corpus/all.jsonl
    (search by shape token; cite project + ts). Existing fixtures remain
    untouched and MUST continue passing.

    NEW FIXTURES (10+, grouped by shape):

    (A) cmd-sub cat<<EOF heredoc (3 tests, describe block "extractOutboundBody - shape A: cmd-sub cat heredoc"):
      - A1: `BODY=$(cat <<'EOF'\nshort body\nEOF\n)` in a real curl PUT cmd
            → expectedBody = "short body"
      - A2: `BODY=$(cat <<'EOF'\n<multi-line body with embedded newlines>\nEOF\n)`
            → expectedBody = the verbatim multi-line body
      - A3: `BODY=$(cat <<'EOF'\nbody with 'literal single quotes' inside\nEOF\n)`
            → expectedBody preserves apostrophes verbatim (sq inside heredoc is literal)

    (B) cmd-sub jq -n[c] '{msgtype:"m.text", body:"X"}' (2 tests, describe "shape B: cmd-sub jq -n body:"):
      - B1: `MSG=$(cat <<'EOF'\nfoo\nEOF\n)` then curl using $MSG
            → expectedBody = "foo"  (validates MSG var-name works with shape A too)
      - B2: `MSG=$(cat <<'EOF'\n<multi-line MSG>\nEOF\n)` in real deploy-announce shape
            → expectedBody = the verbatim body

    Note: the "(B)" heading label in the parent constraints mapped 2 tests to
    shape B (jq -n) and 2 tests to MSG-cat-heredoc. Split them cleanly:

    (B1-jq) cmd-sub jq -n body: (2 tests, describe "shape B: cmd-sub jq -n body:"):
      - Bjq-1: `BODY=$(jq -nc '{msgtype:"m.text", body:"hello world"}')` then curl using $BODY
              → expectedBody = "hello world"
      - Bjq-2: `BODY=$(jq -n '{"msgtype":"m.text", "body":"payload with \\"quotes\\""}')`
              → expectedBody = 'payload with "quotes"'  (dq backslash-decode via _extract_jq_body)

    (C) ANSI-C `VAR=$'...'` (2 tests, describe "shape C: ANSI-C $'...'"):
      - C1: `BODY=$'aqua morning digest\\nline 2\\nline 3'` in curl PUT
            → expectedBody = "aqua morning digest\nline 2\nline 3"  (literal newlines after decode)
      - C2: `MSG=$'tab\\there and \\'apos\\' too'`
            → expectedBody = "tab\there and 'apos' too"

    (D) `read [flags] VARNAME <<'EOF'` bash read heredoc (1 test, describe "shape D: read heredoc"):
      - D1: poppy→vicky column-filter shape: `read -r -d '' MSG <<'EOF'\n<body>\nEOF\n<curl using $MSG>`
            → expectedBody = the verbatim body

    (Strategy 12 preflight) jq-arg-passthrough-known-var (1 test, describe "extractOutboundBody - Strategy 12 preflight"):
      - S12-1: aqua chromehist shape: `BODY=$(cat <<'EOF'\n<body with "embedded double quotes">\nEOF\n)` and later
               `... --arg b "$BODY" '{msgtype:"m.text", body:$b}'`.
               Post-substitution regex would fail because the substituted body has embedded ",
               but Strategy 12 fires FIRST on the ORIGINAL cmd, sees `--arg b "$BODY"` with
               BODY in the assignments map, and returns the body directly.
               → expectedBody = the verbatim body (with embedded ")

    (Latent-bug regression) tabitha → nelly relaying-Ashley shape (1 test, describe "extractOutboundBody - latent bug regressions"):
      - LB-1: cmd has BOTH `BODY=$(cat <<'EOF'\nreal primary body\nEOF\n)` AND
              a secondary `--arg body 'label'` argument elsewhere. Under v0 the
              secondary --arg (Strategy 6/7 jq-arg-inline) captured 'label'
              before the primary heredoc body was reachable. Under v3, Strategy
              12 preflight OR the cmd-sub cat heredoc via BODY-sq post-substitution
              produces the primary body.
              → expectedBody = "real primary body"  (NOT "label")

    (No-op guard / byte-identical) (1 test, describe "extractOutboundBody - no-op invariant"):
      - NO-OP: pick ONE existing corpus fixture already in FIXTURES[] that v0
               handles correctly (e.g. the "BODY-sq — ack short message (tanya)"
               fixture at line 50+ of the existing test file) and duplicate it
               into a new `it()` explicitly asserting the SAME body is returned.
               This locks in that the v3 machinery is a byte-identical no-op
               when it has nothing to substitute. Note in comment which
               original fixture this duplicates.

    Each fixture MUST cite: `// corpus: project=<proj> ts=<iso>` if pulled
    from all.jsonl (grep for the shape token; e.g. `grep -l "\$(cat <<'EOF'" corpus/all.jsonl`
    then extract 3-5 candidates and pick the shortest representative). For
    synthetic-composition fixtures (LB-1 latent-bug regression), mark
    explicitly `// SYNTHETIC — composed to guard latent bug` and cite the
    real corpus cmd that would have exhibited the bug.

    Tests MUST fail RED before implementation. Confirm by running scoped
    vitest and observing the new fixtures fail (existing fixtures still
    pass — pre-existing v0 behavior is unchanged until the .ts edits land).
  </behavior>

  <action>
Port /home/ubuntu/.claude/roles/box-maintainer/bounties/relay-outbound-cmdsub-heredoc-body/extractor_v3.py
VERBATIM into src/backend/claude-session/session-file-parser.ts. The
algorithm is FINALIZED and corpus-validated — do NOT re-derive, do NOT
optimize, do NOT skip shapes. Every regex, every fallthrough order, every
first-assignment-wins rule mirrors the Python source-of-truth exactly.

STEP 1 — RED tests first.

Append the new fixtures + describe blocks to
src/backend/claude-session/session-file-parser.outbound-body.test.ts as
specified in <behavior>. Do NOT edit any existing fixture, describe block,
or test in the file. Append AFTER the current final `describe(...)` block
(after the "shell-var substitution" describe from quick-260822-9qf).

For each new fixture, grep the corpus at
/home/ubuntu/.claude/roles/box-maintainer/bounties/relay-outbound-cmdsub-heredoc-body/corpus/all.jsonl
for a real cmd matching the target shape:
  - shape A (cat heredoc): grep for `"\\$(cat <<'EOF'"` OR `"\\$(cat <<EOF"` OR marker-word variants
  - shape B (jq -n body): grep for `"\\$(jq -n"` and `"body:"`
  - shape C (ANSI-C): grep for `"=\\$'"`
  - shape D (read heredoc): grep for `"^read "` or `" read -"`
  - Strategy 12 (aqua chromehist): grep for cmds where a $VAR body contains embedded double quotes and a `--arg X "$VAR"` shape appears
  - LB-1 (tabitha nelly): grep for cmds with BOTH `BODY=$(cat <<` AND `--arg body '`
Pick the shortest realistic representative for each. Cite provenance in
a `// corpus: project=<proj> ts=<iso>` comment on the fixture. If corpus
grep returns nothing usable for a specific shape (unlikely — 787 records
cover all six), fall back to a SYNTHETIC fixture explicitly marked
`// SYNTHETIC — no real corpus match; shape validation only`.

Run scoped vitest to confirm the new fixtures fail RED:
  npx vitest run src/backend/claude-session/session-file-parser.outbound-body.test.ts
Commit as: `test(quick-260823-hd6): add RED fixtures for v3 port (10+ tests)`

STEP 2 — GREEN implementation.

Edit src/backend/claude-session/session-file-parser.ts:

(2a) EXTEND `substituteShellVars` — the current implementation (lines
245-316) builds an `assignments` map from ORIGINAL cmd via two regex passes
(sq + dq). Extend to SIX shapes, matching extractor_v3.py's
`_build_assignments` order verbatim. Preserve first-assignment-wins across
ALL six passes.

  Shape order (matches extractor_v3.py lines 68-99):
    (A) cmd-sub cat heredoc:
        regex: `(^|[\s;\n]|&&|\|\|)([A-Za-z_][A-Za-z0-9_]*)=\$\(\s*cat\s*<<\s*['"]?(\w+)['"]?\s*\n([\s\S]*?)\n\s*\3\s*\n?\s*\)`
        value = group(4) verbatim (heredoc body is literal)
    (B) cmd-sub jq -n[c]:
        regex: `(^|[\s;\n]|&&|\|\|)([A-Za-z_][A-Za-z0-9_]*)=\$\(\s*jq\s+-nc?\s*'([\s\S]*?)'\s*\)`
        value = _extract_jq_body(group(3)) — if null, skip; helper regex:
          `['"]?body['"]?\s*:\s*"((?:\\.|[^"\\])*)"`
          with backslash-decode: `.replace(/\\(.)/g, '$1')` on the captured body
    (C) ANSI-C `VAR=$'...'`:
        regex: `(^|[\s;\n]|&&|\|\|)([A-Za-z_][A-Za-z0-9_]*)=\$'((?:\\.|[^'\\])*)'`
        value = _decode_ansi_c(group(3)) — helper implementing the map
          `_ANSI_C_ESCAPES = {n:\n, t:\t, r:\r, \\:\\, ':', ":", a:\a, b:\b, f:\f, v:\v, 0:\0}`
          plus `\xHH` two-digit hex handling. See extractor_v3.py lines 45-64
          for the byte-exact algorithm — port it as-is (character-by-character
          state machine, NOT a regex substitution).
    (D) read heredoc:
        regex: `(^|[\s;\n]|&&|\|\|)read\s+(?:-\w+(?:\s+(?:''|""|\S+))?\s+)*([A-Za-z_][A-Za-z0-9_]*)\s*<<\s*['"]?(\w+)['"]?\s*\n([\s\S]*?)\n\s*\3\b`
        value = group(4) verbatim
    (E) sq (existing): `(^|[\s;\n]|&&|\|\|)([A-Za-z_][A-Za-z0-9_]*)='([\s\S]*?)'`
    (F) dq (existing): `(^|[\s;\n]|&&|\|\|)([A-Za-z_][A-Za-z0-9_]*)="((?:\\.|[^"\\])*)"` with backslash-decode

  IMPORTANT — order matters for first-assignment-wins: A → B → C → D → E → F.
  The heredoc/ANSI-C/jq shapes match MORE-SPECIFIC patterns than the bare sq/dq
  regexes would otherwise; running them first prevents E/F from swallowing
  parts of the specialized shapes as bare quoted assigns.

  Preserve everything else: length-descending name sort, `${name}` braces form,
  `$name(?![A-Za-z0-9_])` word-boundary guard on bare refs, no-op when
  assignments map is empty (byte-identical return), subCount debug log
  behavior.

(2b) EXTRACT `_buildAssignments(cmd)` as a private helper so both
`substituteShellVars` AND the new Strategy 12 preflight can share the same
map. Update `substituteShellVars` signature to accept an optional
pre-built assignments map (mirrors extractor_v3.py line 103).

(2c) ADD Strategy 11 `json-envelope-any` — INSERT into the strategy list
BEFORE the `return null` at the end of extractOutboundBody. Regex (matches
extractor_v3.py lines 129-131):
  `\{\s*['"]?msgtype['"]?\s*:\s*"m\.text"\s*,\s*['"]?body['"]?\s*:\s*"((?:\\.|[^"\\])*)"\s*\}`
kind = dq (backslash-decode via `.replace(/\\(.)/g, '$1')`).
Debug log: `strategy=json-envelope-any`. restoreApostrophes at return site.

(2d) ADD Strategy 12 `jq-arg-passthrough-known-var` — this is the PREFLIGHT
that runs BEFORE substituteShellVars. Insert at the TOP of
extractOutboundBody, immediately after the sanitize pass and immediately
after building the assignments map. Regex (mirrors extractor_v3.py lines
138-141):
  `--arg\s+(\w+)\s+"\$(\w+)"\s+'\{\s*msgtype\s*:\s*"m\.text"\s*,\s*body\s*:\s*\$\1\s*\}'`
The `\1` backreference guarantees the argname inside `body:$X` is the same
as the outer --arg name. When matched, look up group(2) (the $VAR reference)
in the assignments map. If present, return `restoreApostrophes(assignments[varname])`
immediately with debug log `strategy=jq-arg-passthrough-known-var`. If not
present, fall through to substitution + strategies 1-11.

(2e) RESTRUCTURE extractOutboundBody control flow to mirror extractor_v3.py
lines 144-177 EXACTLY:
  1. `const s0 = sanitizeBashSqEscapeIdioms(cmd)`
  2. `const assignments = _buildAssignments(s0)` — from ORIGINAL sanitized cmd
  3. Strategy 12 preflight over s0 — if match + varname in map → return
  4. `const s = substituteShellVars(s0, assignments)` — reuse the map
  5. Strategies 1-10 (existing) over `s`, first-match-wins
  6. Strategy 11 (new json-envelope-any) over `s`
  7. `return restoreApostrophes(null)`  →  simply `return null`

(2f) ADD the required comment block near substituteShellVars (verbatim from
constraints):
```
// quick-260823-hd6: substituteShellVars extended to 6 assignment shapes.
// Ported verbatim from ~/.claude/roles/box-maintainer/bounties/
//   relay-outbound-cmdsub-heredoc-body/extractor_v3.py — corpus-validated
// against 787 real fleet outbound cmds (t1000 + workstation, 2 weeks):
// 96.3% ok extraction, 0 regressions vs v0, catches 2 latent wrong-body
// bugs (secondary --arg captured before primary heredoc). Residual 15
// unrecoverable = external files + runtime-conditional picks.
```

Constraints on the TS port:
  - Use `const` regexes at module scope where the Python has `re.compile(...)`
    (matches existing session-file-parser.ts style).
  - Preserve the existing `sessionParserLogger.debug` cadence — one log line
    per strategy hit, format `[session-parser] extract result=outbound_body strategy=<name> bodyLen=<n>`.
  - Do NOT change any behavior of the existing strategies 1-10 code paths.
    Only ADD to the assignments-map builder, ADD strategies 11 and 12, and
    RESTRUCTURE the entry point to run Strategy 12 preflight first + reuse
    the shared assignments map.
  - Do NOT touch parseSessionLine, detectRelayOutbound, or any other
    exported function. Only extractOutboundBody + substituteShellVars +
    helpers.
  - Preserve `restoreApostrophes` at every return site (including the two
    new strategies) — sanitize/restore invariant is load-bearing per
    Phase 49.
  - Do NOT re-derive the algorithm; the Python is the source of truth. If
    the TS translation appears to differ from the Python at any point,
    STOP and match the Python.

STEP 3 — Confirm GREEN + no regressions.

Run:
  npx vitest run src/backend/claude-session/session-file-parser.outbound-body.test.ts src/backend/claude-session/session-file-parser.ts
Expect: 100% pass (all new fixtures + all pre-existing fixtures green).
Commit as: `feat(quick-260823-hd6): port extractor_v3.py — 6 assignment shapes + Strategies 11/12`

STEP 4 — Typecheck (backend-specific — durable box-maintainer rule).
Run: `npm run build:backend && npm run build`
Fix any TS errors. Do NOT run plain `npx tsc --noEmit` — the frontend
tsconfig does not compile backend files the same way (learned patch #154).

STOP after typecheck green. Executor's remit ends here. Do NOT run:
  - full vitest suite (`npx vitest run` without the scoped paths)
  - docker build, docker compose up, or any deploy command
  - git push
  - edits to skynet-patches.md (orchestrator owns the patch-file entry)
  - agent spawns
Do NOT attempt ship-gate. Orchestrator will run the full-suite green-gate,
patch-file entry, ship, and deploy after this plan completes.
  </action>

  <verify>
    <automated>npx vitest run src/backend/claude-session/session-file-parser.outbound-body.test.ts src/backend/claude-session/session-file-parser.ts && npm run build:backend && npm run build</automated>
  </verify>

  <done>
    - src/backend/claude-session/session-file-parser.outbound-body.test.ts contains 10+ new fixtures across the 6 new assignment shapes + Strategy 12 + latent-bug regression + no-op guard
    - Every new fixture cites provenance (corpus project+ts OR SYNTHETIC marker)
    - src/backend/claude-session/session-file-parser.ts substituteShellVars accepts 6 assignment shapes (A cat heredoc, B jq -n body, C ANSI-C, D read heredoc, E sq, F dq) in that order, with first-assignment-wins preserved
    - _buildAssignments extracted as a shared helper; substituteShellVars accepts optional pre-built map
    - Strategy 11 json-envelope-any inserted before final return null
    - Strategy 12 jq-arg-passthrough-known-var runs BEFORE substituteShellVars over ORIGINAL sanitized cmd, uses the shared assignments map
    - extractOutboundBody control flow matches extractor_v3.py lines 144-177 exactly
    - Required verbatim comment block present near substituteShellVars citing extractor_v3.py + 787-cmd corpus + 96.3% ok + 2 latent bugs
    - restoreApostrophes preserved at every return site
    - Scoped vitest command passes (all new fixtures + all pre-existing fixtures green, zero regressions)
    - `npm run build:backend && npm run build` exit 0 (backend typecheck green — NOT plain `tsc --noEmit`)
    - Two commits landed: RED test commit + GREEN implementation commit
    - No touches to skynet-patches.md, no docker/deploy commands run, no git push, no full-suite vitest, no agent spawns
  </done>
</task>

</tasks>

<verification>
Overall phase check (scoped — orchestrator handles full ship-gate after):
  1. `npx vitest run src/backend/claude-session/session-file-parser.outbound-body.test.ts src/backend/claude-session/session-file-parser.ts` — ALL green, no regressions
  2. `npm run build:backend && npm run build` — exit 0, no TS errors (durable rule: backend files need backend tsconfig, NOT plain tsc --noEmit)
  3. `git log --oneline -5` shows two new commits: `test(quick-260823-hd6): ...` + `feat(quick-260823-hd6): ...`
  4. Manual grep confirms the required verbatim comment block is present in session-file-parser.ts:
     `grep -c "quick-260823-hd6: substituteShellVars extended to 6 assignment shapes" src/backend/claude-session/session-file-parser.ts` → 1
  5. Manual grep confirms both new strategies referenced by name in the file:
     `grep -c "jq-arg-passthrough-known-var\|json-envelope-any" src/backend/claude-session/session-file-parser.ts` → ≥ 2
</verification>

<success_criteria>
Measurable completion:
  - Scoped vitest green (extractOutboundBody unit tests): 10+ new + all pre-existing tests passing
  - Backend typecheck green via `npm run build:backend && npm run build`
  - substituteShellVars handles all 6 assignment shapes with correct first-assignment-wins ordering
  - Strategy 11 (json-envelope-any) catches envelopes Strategy 10 (inline-json) misses
  - Strategy 12 (jq-arg-passthrough-known-var) bypasses post-substitution regex fragility for embedded-double-quote bodies
  - Latent-bug regression test (LB-1) passes: primary heredoc body wins over secondary `--arg body 'label'`
  - No-op invariant preserved: byte-identical extraction for cmds with zero assignments
  - Comment block near substituteShellVars cites extractor_v3.py + 787-cmd corpus + 96.3% ok + 2 latent bugs verbatim
  - Two commits landed (RED + GREEN)
  - Zero changes outside session-file-parser.ts + session-file-parser.outbound-body.test.ts
</success_criteria>

<output>
Create `.planning/quick/260823-hd6-outbound-body-parser-v3-port/quick-260823-hd6-01-SUMMARY.md` when done.
</output>
