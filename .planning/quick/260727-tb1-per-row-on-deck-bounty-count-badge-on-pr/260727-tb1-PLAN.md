---
phase: 260727-tb1-per-row-on-deck-bounty-count-badge-on-pr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/claude-session/identity-artifact-reader.ts
  - src/backend/claude-session/claude-session-server.ts
  - src/ui/api/claude-session-api.ts
  - src/ui/state/bounty-counts-store.ts
  - src/ui/features/pretty-conversations/PrettyBountyCountBadge.tsx
  - src/ui/features/pretty-conversations/PrettyConversationRow.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/features/pretty-conversations/pretty-conversations.css
autonomous: true
requirements:
  - QUICK-260727-tb1
must_haves:
  truths:
    - "A pretty-conversations row whose identity has N (N>=1) non-archived on_deck bounties displays a small numeric badge showing N inside .pv-meta, positioned before the ready-dot"
    - "A row with 0 on_deck bounties renders NO badge DOM element (absence, not empty pill)"
    - "A row that is BOTH in-active-set-and-idle AND has on_deck bounties shows both the badge AND the ready-dot side by side"
    - "Non-identity rows never trigger bounty-count network traffic"
    - "One WS request + one response per poll cycle covers all identities in the panel (batched, not N round-trips)"
    - "Local (skynet-ec2 bind-mount) reads and remote (SSH exec) reads both produce accurate counts"
    - "One slow/dead SSH host does not block other hosts' counts (per-target Promise.allSettled isolation)"
    - "Badge auto-updates within 60s of a disk change without browser refresh; within ~1s on window.focus; immediately on identity:bounty-priority-updated for the affected identity"
    - "Badge inherits per-identity color via --pv-hue tinting"
  artifacts:
    - path: "src/backend/claude-session/identity-artifact-reader.ts"
      provides: "readIdentityOnDeckBountyCount(conn, identityKey) helper (local + remote branches)"
      contains: "readIdentityOnDeckBountyCount"
    - path: "src/backend/claude-session/claude-session-server.ts"
      provides: "identity:count-bounties WS handler emitting identity:bounty-counts response"
      contains: "identity:count-bounties"
    - path: "src/ui/api/claude-session-api.ts"
      provides: "IdentityCountBountiesPayload, IdentityBountyCountsEvent wire types + countIdentityBounties() one-shot helper"
      contains: "countIdentityBounties"
    - path: "src/ui/state/bounty-counts-store.ts"
      provides: "useSyncExternalStore-based store: useBountyCount, refreshBountyCounts, startBountyCountPoller, invalidateIdentity"
      contains: "startBountyCountPoller"
    - path: "src/ui/features/pretty-conversations/PrettyBountyCountBadge.tsx"
      provides: "Stateless pill component; returns null when count is falsy"
      contains: "PrettyBountyCountBadge"
    - path: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx"
      provides: "Row consumes useBountyCount and renders <PrettyBountyCountBadge/> in .pv-meta before ready-dot"
      contains: "useBountyCount"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "startBountyCountPoller wiring on mount (60s + focus) + identity:bounty-priority-updated -> invalidateIdentity piggyback"
      contains: "startBountyCountPoller"
    - path: "src/ui/features/pretty-conversations/pretty-conversations.css"
      provides: ".pv-bounty-badge rule using --pv-hue tinting"
      contains: ".pv-bounty-badge"
  key_links:
    - from: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      to: "src/ui/state/bounty-counts-store.ts"
      via: "startBountyCountPoller(getTargets, 60000) on mount"
      pattern: "startBountyCountPoller"
    - from: "src/ui/state/bounty-counts-store.ts"
      to: "src/ui/api/claude-session-api.ts"
      via: "countIdentityBounties(targets) one-shot WS call"
      pattern: "countIdentityBounties"
    - from: "src/ui/api/claude-session-api.ts"
      to: "src/backend/claude-session/claude-session-server.ts"
      via: "WS message type identity:count-bounties -> identity:bounty-counts"
      pattern: "identity:count-bounties"
    - from: "src/backend/claude-session/claude-session-server.ts"
      to: "src/backend/claude-session/identity-artifact-reader.ts"
      via: "readIdentityOnDeckBountyCount(conn, identityKey)"
      pattern: "readIdentityOnDeckBountyCount"
    - from: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx"
      to: "src/ui/state/bounty-counts-store.ts"
      via: "useBountyCount(identityKey, hostId) selector"
      pattern: "useBountyCount"
---

<objective>
Add a small per-row numeric badge to the pretty-conversations panel showing the count of non-archived, on_deck-status bounties for each row's identity. Coexists with (does not replace) the existing ready-for-attention dot: attention dot = "click me now, agent is idle" (ephemeral), bounty badge = "queued work exists here" (persistent).

Purpose: Ashley currently has no at-a-glance signal for which rows have parked bounties waiting on their identity. She has to open the identity modal per row. This badge surfaces that count directly in the row.

Output: 6 modified files + 2 new files. Batched WS protocol (`identity:count-bounties` -> `identity:bounty-counts`) piggybacking on the patch #92 identity-artifact reader infrastructure. Frontend polls every 60s + window.focus, invalidates on `identity:bounty-priority-updated` (patch #154 event).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@/home/ubuntu/.claude/plans/abstract-noodling-map.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend — on-deck bounty count reader + batched WS handler</name>
  <files>src/backend/claude-session/identity-artifact-reader.ts, src/backend/claude-session/claude-session-server.ts</files>
  <read_first>
    - /home/ubuntu/.claude/plans/abstract-noodling-map.md (THE approved design spec; sections "Backend changes" and "Approach — piggyback on the patch #92 identity-artifact reader")
    - src/backend/claude-session/identity-artifact-reader.ts (pattern after existing readIdentityBounties at lines 463-602: same base-dir + validation + timeout/error shape; local branch uses fs.readdir on ${identityHostDir}/${identityKey}/bounties/ skipping archive/; remote branch uses connectOneShot with 3s exec timeout)
    - src/backend/claude-session/claude-session-server.ts (mirror the identity:list-bounties handler at lines 1400-1485 for message routing; note the useLocal = hostIdNum === undefined || isLocalHostId(hostIdNum) pattern for local-vs-SSH selection)
  </read_first>
  <behavior>
    - readIdentityOnDeckBountyCount(conn=null, identityKey="tina") returns integer matching `find ~/.claude/identities/tina/bounties -mindepth 2 -maxdepth 2 -name bounty.json -not -path "*/archive/*" -exec grep -l '"status".*"on_deck"' {} + | wc -l` on skynet-ec2
    - readIdentityOnDeckBountyCount(conn=<open SshConnection>, identityKey) returns the same shape from a remote host
    - Reader rejects on 3s SSH exec timeout (matches readIdentityBounties timeout policy); rejection is caught by handler and converted to per-target error
    - identity:count-bounties handler with targets=[{identityKey:"tina",hostId:null},{identityKey:"other",hostId:null}] emits ONE identity:bounty-counts response containing both targets
    - When 5 targets share the same non-local hostId, handler opens exactly ONE SSH connection for that hostId (connectOneShot) and runs all 5 identities through it before closing
    - When one target throws (SSH host dead, missing dir, JSON parse fail), that target's result is {identityKey, hostId, onDeckCount: 0, error: string} but OTHER targets still return successful counts (Promise.allSettled isolation)
    - Empty targets array -> empty counts array (no throw)
  </behavior>
  <action>
    Implement the backend half of the batched on-deck bounty count protocol per the approved design spec (/home/ubuntu/.claude/plans/abstract-noodling-map.md, sections "Backend changes" and Key design decisions #1 and #2).

    (A) In src/backend/claude-session/identity-artifact-reader.ts add:

        async function readIdentityOnDeckBountyCount(conn: SshConnection | null, identityKey: string): Promise<number>

        Local branch (conn === null): fs.readdir on ${identityHostDir}/${identityKey}/bounties/ using the same identityHostDir base-path + identityKey validation regex used by readIdentityBounties (do NOT invent new validation — reuse it). Skip any entry named "archive". For each remaining subdir, read subdir/bounty.json, JSON.parse it, count entries where parsed.status === "on_deck". Swallow per-file parse errors as "not on_deck" (do NOT throw the whole call).

        Remote branch (conn !== null): use conn.exec (same wrapper readIdentityBounties uses) with a 3s timeout. Prefer a python3 one-liner over grep — bounty.json is JSON so text-grep on '"status".*"on_deck"' is fragile against pretty-printed variants. Suggested command (single-line):
          python3 -c 'import os,json,sys; r=os.path.expanduser("~/.claude/identities/<KEY>/bounties"); n=0
          for d in os.listdir(r):
            if d=="archive": continue
            p=os.path.join(r,d,"bounty.json")
            try:
              j=json.load(open(p))
              if j.get("status")=="on_deck": n+=1
            except Exception: pass
          print(n)'
        Shell-escape identityKey into the <KEY> slot; parse stdout as int; reject on timeout.

        Export the function. Match export style of readIdentityBounties.

    (B) In src/backend/claude-session/claude-session-server.ts add a new WS message-type handler for "identity:count-bounties" mirroring the existing "identity:list-bounties" handler (lines 1400-1485).

        Input payload shape:
          { type: "identity:count-bounties"; targets: Array<{ identityKey: string; hostId: number | null }> }

        Handler steps:
          1. Group `targets` by hostId. For the null/local group, iterate identities calling readIdentityOnDeckBountyCount(null, identityKey) (no shared conn needed — local fs).
          2. For each non-local hostId group: open exactly ONE SshConnection via connectOneShot(...) (same helper readIdentityBounties handler uses), iterate all identities in that group through the same conn, then close the conn. This is the "connection reuse within a batch" optimization (Key design decision #2).
          3. Wrap ALL per-target reads in Promise.allSettled so one slow/dead SSH host does not block the batch.
          4. Map each settled result to { identityKey, hostId, onDeckCount } on success or { identityKey, hostId, onDeckCount: 0, error: String(reason) } on rejection.
          5. Emit one response message:
             { type: "identity:bounty-counts"; counts: Array<{ identityKey: string; hostId: number | null; onDeckCount: number; error?: string }> }

        Register the handler in the same switch/dispatch table that carries identity:list-bounties. Use the same useLocal = hostIdNum === undefined || isLocalHostId(hostIdNum) semantics (Key design decision #4: hostId=null means "read from skynet-ec2 local bind-mount").

    (C) Tests (behavior above): add wire-shape assertion tests for the request+response type pair and per-target error isolation. Keep tests in whatever backend test file the identity:list-bounties handler is covered by (grep for "identity:list-bounties" in tests/); if none exists, create a peer test file alongside.

    Do NOT touch nginx configs (WS routes are already carried by the existing claude-session WS bridge). Do NOT modify readIdentityBounties itself. Do NOT push, build, or deploy — this task ends at "code + tests + git commit."

    Commit at end of task: `patch: backend on-deck bounty count reader + batched WS handler`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; (npm test -- --testPathPattern='(identity-artifact-reader|claude-session-server)' --run 2>&amp;1 || npx vitest run --reporter=verbose 'identity-artifact-reader|claude-session-server' 2>&amp;1) | tail -60 &amp;&amp; grep -n 'readIdentityOnDeckBountyCount' src/backend/claude-session/identity-artifact-reader.ts &amp;&amp; grep -n 'identity:count-bounties' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -n 'identity:bounty-counts' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -n 'allSettled' src/backend/claude-session/claude-session-server.ts</automated>
  </verify>
  <done>
    - readIdentityOnDeckBountyCount export exists in identity-artifact-reader.ts with both local and remote branches
    - identity:count-bounties handler exists in claude-session-server.ts, groups targets by hostId, opens one conn per non-local hostId, wraps all reads in Promise.allSettled, emits identity:bounty-counts response
    - Handler test proves per-target error isolation: one rejected read does not fail the batch
    - Handler test proves connection reuse: 5 targets sharing one hostId open exactly one SshConnection
    - Verification #1 from spec (Local read): with tina identity present, `readIdentityOnDeckBountyCount(null, "tina")` returns the same integer as the equivalent shell command against ~/.claude/identities/tina/bounties
    - Commit landed with message starting `patch: backend on-deck bounty count reader`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Frontend plumbing — wire types + one-shot API helper + bounty-counts store</name>
  <files>src/ui/api/claude-session-api.ts, src/ui/state/bounty-counts-store.ts</files>
  <read_first>
    - /home/ubuntu/.claude/plans/abstract-noodling-map.md (sections "Frontend changes" — subsections claude-session-api.ts and bounty-counts-store.ts; Key design decisions #3, #5, #7)
    - src/ui/state/identities-store.ts (THE pattern to mirror: useSyncExternalStore module-scoped store with subscribe/getSnapshot/notify + selector hook — follow this file's structure exactly for bounty-counts-store.ts)
    - src/ui/api/claude-session-api.ts (specifically the area around line 248 where existing identity:list-bounties types + helper live; mirror those export patterns for the new count-bounties equivalents)
    - src/backend/claude-session/claude-session-server.ts (only re-scan to confirm the wire shapes Task 1 established; do not modify)
  </read_first>
  <behavior>
    - Wire types IdentityCountBountiesPayload and IdentityBountyCountsEvent are exported from claude-session-api.ts with exact shapes matching Task 1's backend handler
    - countIdentityBounties(targets) returns a Promise<IdentityBountyCountsEvent>; it sends one WS message and resolves with the matching response (mirror the existing one-shot helper pattern that identity:list-bounties uses)
    - useBountyCount(identityKey, hostId) returns undefined when no fetch has completed yet, and returns the last-known integer count once refreshBountyCounts has landed for that composite key (${identityKey}:${hostId ?? "local"})
    - useBountyCount returns undefined when identityKey is null (non-identity rows must not subscribe)
    - refreshBountyCounts(targets) applies the response to the internal Map and calls notify() so all subscribing hooks re-render
    - startBountyCountPoller(getTargets, intervalMs=60000) returns a stop-fn; on invocation it fires an initial fetch, sets a setInterval, adds a window.focus listener that triggers an extra fetch, and the returned stop-fn clears both
    - invalidateIdentity(identityKey, hostId) marks the composite key stale so the next poll (or immediate follow-up call) refetches it; a straightforward implementation is to fire a targeted refreshBountyCounts([{identityKey, hostId}]) immediately
    - Store is module-scoped (singleton), NOT per-component (matches identities-store.ts)
  </behavior>
  <action>
    Implement the frontend plumbing layer per /home/ubuntu/.claude/plans/abstract-noodling-map.md, sections "Frontend changes" > claude-session-api.ts and bounty-counts-store.ts.

    (A) In src/ui/api/claude-session-api.ts (mirror the existing identity:list-bounties types + helper near line 248):

        export type IdentityCountBountiesPayload = {
          type: "identity:count-bounties";
          targets: Array<{ identityKey: string; hostId: number | null }>;
        };
        export type IdentityBountyCountsEvent = {
          type: "identity:bounty-counts";
          counts: Array<{ identityKey: string; hostId: number | null; onDeckCount: number; error?: string }>;
        };
        export async function countIdentityBounties(
          targets: Array<{ identityKey: string; hostId: number | null }>
        ): Promise<IdentityBountyCountsEvent>;

        Implement countIdentityBounties using the SAME one-shot WS request/response pattern the existing listIdentityBounties (or equivalent) helper uses in this file — do not invent a new transport. If the file has a shared `oneShot(type, payload, responseType)` helper, use it. If not, inline the minimal listener-then-send-then-remove-listener sequence the neighboring helper uses.

    (B) Create src/ui/state/bounty-counts-store.ts as a NEW file, using src/ui/state/identities-store.ts as the structural template (module-scoped state, subscribe set, notify(), useSyncExternalStore-based hook):

        Internal state:
          const counts = new Map<string, number>();  // key = `${identityKey}:${hostId ?? "local"}`
          const subscribers = new Set<() => void>();
          function notify() { subscribers.forEach(fn => fn()); }
          function subscribe(fn) { subscribers.add(fn); return () => subscribers.delete(fn); }

        Exports:
          - useBountyCount(identityKey: string | null, hostId: number | null): number | undefined
            (returns undefined if identityKey===null; otherwise useSyncExternalStore(subscribe, () => counts.get(compositeKey)))
          - refreshBountyCounts(targets): calls countIdentityBounties(targets), for each result in response.counts sets counts.set(compositeKey, onDeckCount), then notify(). Errors on individual targets (result.error present) are logged to console.warn but do NOT throw and do NOT overwrite last-known good count with 0 (skip the .set for that target — hold last-known per spec verification #9).
          - startBountyCountPoller(getTargets: () => Array<{identityKey,hostId}>, intervalMs = 60_000): (a) fires refreshBountyCounts(getTargets()) immediately, (b) sets setInterval firing the same, (c) adds a window.addEventListener("focus", ...) that fires an extra refresh, (d) returns a stop-fn that clears the interval AND removes the focus listener. (Key design decision #3.)
          - invalidateIdentity(identityKey: string, hostId: number | null): calls refreshBountyCounts([{identityKey, hostId}]) so the next render for that composite key reflects fresh data. (Key design decision #5 — used by the identity:bounty-priority-updated piggyback in Task 3.)

        No React imports beyond useSyncExternalStore. No global side effects at module load (poller is started explicitly by the panel in Task 3, not on import).

    (C) Tests (behavior above): unit tests for the store covering (a) useBountyCount returns undefined pre-fetch and the fetched number post-refresh, (b) poll interval fires refresh at intervalMs (use vi.useFakeTimers or jest equivalent — check the project's existing store tests for the timer pattern), (c) window.focus triggers an extra refresh, (d) invalidateIdentity fires a targeted refresh, (e) per-target error in response does not clobber last-known count for that key. If the project uses vitest, place tests at src/ui/state/bounty-counts-store.test.ts.

    Do NOT wire the poller anywhere yet — that is Task 3. Do NOT modify identities-store.ts. Do NOT push, build, or deploy.

    Commit at end of task: `patch: frontend bounty-counts store + one-shot count-bounties API helper`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; (npm test -- --testPathPattern='(bounty-counts-store|claude-session-api)' --run 2>&amp;1 || npx vitest run --reporter=verbose 'bounty-counts-store|claude-session-api' 2>&amp;1) | tail -60 &amp;&amp; grep -n 'countIdentityBounties' src/ui/api/claude-session-api.ts &amp;&amp; grep -n 'IdentityBountyCountsEvent' src/ui/api/claude-session-api.ts &amp;&amp; grep -n 'startBountyCountPoller\|useBountyCount\|invalidateIdentity\|refreshBountyCounts' src/ui/state/bounty-counts-store.ts &amp;&amp; grep -n 'useSyncExternalStore' src/ui/state/bounty-counts-store.ts</automated>
  </verify>
  <done>
    - IdentityCountBountiesPayload, IdentityBountyCountsEvent, and countIdentityBounties are exported from claude-session-api.ts
    - src/ui/state/bounty-counts-store.ts exists and exports useBountyCount, refreshBountyCounts, startBountyCountPoller, invalidateIdentity
    - Store uses useSyncExternalStore (matches identities-store.ts pattern)
    - Store tests prove: initial undefined, post-refresh integer, interval firing, focus firing, invalidateIdentity targeted refresh, per-target error preserves last-known
    - useBountyCount(null, ...) short-circuits to undefined (no subscription cost for non-identity rows)
    - Verification #3 from spec is testable at this layer: a single call to countIdentityBounties with N targets sends one WS message (assertable via test double)
    - Commit landed with message starting `patch: frontend bounty-counts store`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Frontend UI — badge component + row integration + panel poller wiring + CSS</name>
  <files>src/ui/features/pretty-conversations/PrettyBountyCountBadge.tsx, src/ui/features/pretty-conversations/PrettyConversationRow.tsx, src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx, src/ui/features/pretty-conversations/pretty-conversations.css</files>
  <read_first>
    - /home/ubuntu/.claude/plans/abstract-noodling-map.md (sections "Frontend changes" > PrettyConversationsPanel.tsx, PrettyBountyCountBadge.tsx, PrettyConversationRow.tsx, pretty-conversations.css; Key design decisions #4, #5, #6, #7; Verification list #1-#10)
    - src/ui/features/pretty-conversations/PrettyConversationRow.tsx (specifically the identity join at lines 141-144 and the ready-dot render at lines 499-512 — the badge inserts inside .pv-meta before the ready-dot)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (locate where activeSet + pinned + grouped rows are assembled so the poller's getTargets callback can enumerate them; also locate the existing WS event listener path for identity:* messages so identity:bounty-priority-updated can be added or reused)
    - src/ui/features/pretty-conversations/pretty-conversations.css (confirm --pv-hue and --color-pv-fg CSS custom properties are defined on .pv-row scope so the new .pv-bounty-badge rule inherits them correctly)
    - src/ui/state/bounty-counts-store.ts (from Task 2 — the store this task consumes)
  </read_first>
  <behavior>
    - PrettyBountyCountBadge with count=undefined renders null
    - PrettyBountyCountBadge with count=0 renders null (falsy short-circuit — count=0 must NOT render a "0" pill; Key design decision #7)
    - PrettyBountyCountBadge with count=1 renders a single <span className="pv-bounty-badge" data-testid="pv-bounty-badge">1</span>
    - PrettyBountyCountBadge with count=99 renders <span ...>99</span> without layout overflow at compact mobile row height (verification #10)
    - PrettyConversationRow calls useBountyCount(identity?.identityKey ?? null, row.host?.id ?? null) and renders <PrettyBountyCountBadge count={onDeckCount} /> inside .pv-meta immediately before the ready-dot element (order left-to-right: [deactivate] [pin] [bounty-badge] [ready-dot])
    - A row that has BOTH an active-set idle state AND a positive on_deck count shows BOTH the badge and the ready-dot side by side (verification #4 coexistence)
    - PrettyConversationsPanel on mount calls startBountyCountPoller(getTargets, 60_000) where getTargets returns the current union of activeSet + pinned + grouped rows filtered to those whose sessionMatchKey resolves to an identity in identitiesByKey, mapped to {identityKey, hostId: row.host?.id ?? null}. On unmount the returned stop-fn is invoked.
    - PrettyConversationsPanel adds (or reuses if already present) a WS listener for identity:bounty-priority-updated that calls invalidateIdentity(evt.identityKey, evt.hostId ?? null). This is the immediate-refresh piggyback path (Key design decision #5).
    - .pv-bounty-badge CSS rule tints itself using hsla(var(--pv-hue), ...) so each row's identity color shows through (per palette-authority rule)
  </behavior>
  <action>
    Ship the visible UI half per /home/ubuntu/.claude/plans/abstract-noodling-map.md, sections "Frontend changes" > PrettyBountyCountBadge.tsx / PrettyConversationRow.tsx / PrettyConversationsPanel.tsx / pretty-conversations.css.

    (A) Create src/ui/features/pretty-conversations/PrettyBountyCountBadge.tsx as a NEW file with the exact stateless implementation from the spec:

        import React from "react";
        export function PrettyBountyCountBadge({ count }: { count: number | undefined }) {
          if (!count) return null;  // covers undefined and 0 — Key design decision #7
          return <span className="pv-bounty-badge" data-testid="pv-bounty-badge">{count}</span>;
        }

    (B) In src/ui/features/pretty-conversations/PrettyConversationRow.tsx: after the existing identity join at lines 141-144, add:

        const onDeckCount = useBountyCount(identity?.identityKey ?? null, row.host?.id ?? null);

        Import useBountyCount from ../../state/bounty-counts-store and PrettyBountyCountBadge from ./PrettyBountyCountBadge. In the .pv-meta JSX block that currently renders the ready-dot around lines 499-512, insert <PrettyBountyCountBadge count={onDeckCount} /> as the sibling IMMEDIATELY before the ready-dot node. Final left-to-right order inside .pv-meta must be: [deactivate button] [pin button] [PrettyBountyCountBadge] [ready-dot]. Do not reorder or remove any existing element.

    (C) In src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx:

        1. On mount (useEffect with empty deps or the panel's existing lifecycle hook — match the panel's current effect style): call startBountyCountPoller(getTargets, 60_000). getTargets is a stable closure that, when invoked, walks the CURRENT union of activeSet + pinned + grouped rows in scope, filters to rows where sessionMatchKey(row.targetTmuxSession) resolves to an identity in identitiesByKey, and returns Array<{identityKey: identity.identityKey, hostId: row.host?.id ?? null}>. The returned stop-fn from startBountyCountPoller is called on cleanup.

        2. Wire identity:bounty-priority-updated: locate the panel's existing WS event listener (there is one for identity:* traffic — the same one that surfaces the modal changes today). Add a case (or add a listener if none exists) that on receipt of {type: "identity:bounty-priority-updated", identityKey, hostId?} calls invalidateIdentity(identityKey, hostId ?? null). Import invalidateIdentity from ../../state/bounty-counts-store.

    (D) In src/ui/features/pretty-conversations/pretty-conversations.css append the exact rule from the spec:

        .pv-bounty-badge {
          min-width: 18px;
          height: 16px;
          padding: 0 5px;
          border-radius: 999px;
          font-size: 10px;
          font-weight: 600;
          line-height: 16px;
          color: var(--color-pv-fg);
          background: hsla(var(--pv-hue), 45%, 30%, 0.6);
          border: 1px solid hsla(var(--pv-hue), 55%, 55%, 0.35);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          flex: 0 0 auto;
        }

        --pv-hue and --color-pv-fg are inherited from .pv-row (palette-authority rule); do NOT redefine them here.

    (E) Tests: component snapshot of PrettyBountyCountBadge at count=undefined (renders null), count=0 (renders null), count=1, count=99. If the project uses vitest + @testing-library/react, follow the pattern used by any existing snapshot in this feature directory.

    Do NOT modify identities-store.ts. Do NOT touch nginx configs. Do NOT push, build, or deploy — this task ends at "code + tests + git commit."

    Commit at end of task: `patch: pretty-conversations per-row on-deck bounty count badge`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; (npm test -- --testPathPattern='(PrettyBountyCountBadge|PrettyConversationRow|PrettyConversationsPanel)' --run 2>&amp;1 || npx vitest run --reporter=verbose 'PrettyBountyCountBadge|PrettyConversationRow|PrettyConversationsPanel' 2>&amp;1) | tail -80 &amp;&amp; test -f src/ui/features/pretty-conversations/PrettyBountyCountBadge.tsx &amp;&amp; grep -n 'PrettyBountyCountBadge' src/ui/features/pretty-conversations/PrettyConversationRow.tsx &amp;&amp; grep -n 'useBountyCount' src/ui/features/pretty-conversations/PrettyConversationRow.tsx &amp;&amp; grep -n 'startBountyCountPoller' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx &amp;&amp; grep -n 'invalidateIdentity\|identity:bounty-priority-updated' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx &amp;&amp; grep -n '\.pv-bounty-badge' src/ui/features/pretty-conversations/pretty-conversations.css &amp;&amp; grep -n '\-\-pv-hue' src/ui/features/pretty-conversations/pretty-conversations.css</automated>
    <human-check>
      Manual verification against spec Verification list #1-#10 (deferred to post-deploy per fleet directive; PLAN.md ships code+tests+commits only):
      1. Local read (skynet-ec2 tina): row badge count matches shell find|grep|wc.
      2. Remote read (SSH-reachable identity): badge matches remote grep count.
      3. Batch behavior: DevTools Network shows one WS request + one response per poll cycle for N identities, not N round-trips.
      4. Coexistence: row with both active-set-idle AND on_deck bounties shows badge AND ready-dot side by side; capture regression screenshot.
      5. Absence: count=0 renders NO DOM element; non-identity rows fire no polls (network trace).
      6. Poll freshness: manually add a bounty file, badge updates within 60s without refresh.
      7. Focus invalidate: switch tab away, add bounty, switch back — updates within ~1s.
      8. identity:bounty-priority-updated piggyback: change bounty status via modal, badge updates immediately.
      9. SSH host dead: kill one host mid-poll, OTHER badges still update, dead-host badge holds last-known.
      10. Mobile row size at iPhone PWA form factor: badge does not push other .pv-meta items off-screen.
    </human-check>
  </verify>
  <done>
    - PrettyBountyCountBadge.tsx exists, renders null for undefined and 0, renders span.pv-bounty-badge for positive integers
    - PrettyConversationRow.tsx imports and uses useBountyCount + renders PrettyBountyCountBadge inside .pv-meta immediately before the ready-dot
    - PrettyConversationsPanel.tsx calls startBountyCountPoller(getTargets, 60_000) on mount and calls the returned stop-fn on unmount
    - PrettyConversationsPanel.tsx handles identity:bounty-priority-updated by calling invalidateIdentity
    - pretty-conversations.css contains a .pv-bounty-badge rule using var(--pv-hue) tinting
    - Badge component snapshot tests cover count in {undefined, 0, 1, 99}
    - Commit landed with message starting `patch: pretty-conversations per-row on-deck bounty count badge`
    - Post-deploy human verification checklist (10 items) captured in <human-check> for the follow-up deploy pass — NOT executed in this PLAN
  </done>
</task>

</tasks>

<verification>
Phase-level (all three tasks): the seven files listed in files_modified all exist and grep positive for the identifiers in must_haves.artifacts.contains. Running `cd /home/ubuntu/skynet && (npx tsc --noEmit 2>&1 || npm run typecheck 2>&1) | tail -40` returns zero errors attributable to these changes. All added test files pass. Three separate git commits landed with the prefixes named in each task's action.
</verification>

<success_criteria>
- All three tasks completed and committed (three commits: backend / frontend-plumbing / frontend-UI)
- All new automated tests pass (backend handler tests, store tests, badge snapshot tests)
- No TypeScript errors introduced
- Manual verification checklist (spec Verification #1-#10) captured in Task 3 <human-check> for post-deploy validation
- NO push, build, or deploy attempted — deploy pre-work requires separate greenlight per fleet directive
</success_criteria>

<output>
Create `.planning/quick/260727-tb1-per-row-on-deck-bounty-count-badge-on-pr/260727-tb1-SUMMARY.md` when done.
</output>
