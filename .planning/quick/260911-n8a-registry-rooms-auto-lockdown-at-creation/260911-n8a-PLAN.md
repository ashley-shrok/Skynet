---
quick_id: 260911-n8a
type: execute
autonomous: true
files_modified:
  - src/backend/matrix/matrix-admin-client.ts
  - src/backend/matrix/matrix-admin-client.test.ts
  - src/backend/relay-sessions/registry-rooms.ts
  - src/backend/relay-sessions/registry-rooms.test.ts

must_haves:
  truths:
    - "Fresh install: newly-created agents + humans registry rooms come out with all 8 power_level invariants at 100 and fleet-admin (getMatrixAdminCreds().userId) at PL 100 in users, with zero observable open-write window."
    - "Drifted room: on next boot, any invariant below 100 gets patched up to 100 via a single PATCH; existing PL 100 humans in users are preserved; only ADD fleet-admin if missing; NEVER lower any existing level."
    - "Already-locked room: on next boot, no PATCH is issued (read-then-optionally-write; zero-diff = no-op)."
    - "Matrix unreachable / permission denied on PATCH: structured warn log with roomId + reason; boot continues; retry happens automatically on next ensureRegistryRoomsExist call."
  artifacts:
    - path: "src/backend/matrix/matrix-admin-client.ts"
      provides: "getRoomPowerLevels + putRoomPowerLevels primitives (GET+PUT m.room.power_levels state event) + createRoom initial_state opt-in"
    - path: "src/backend/relay-sessions/registry-rooms.ts"
      provides: "assertRegistryRoomLockdown() called from ensureRegistryRoomsExist for both rooms; createRegistryRoom passes initial_state power_levels at birth"
    - path: "src/backend/relay-sessions/registry-rooms.test.ts"
      provides: "3 required test cases (fresh install, drift-patch, already-locked no-op) + preserve-existing-admins case + failure-continues-boot case"
    - path: "src/backend/matrix/matrix-admin-client.test.ts"
      provides: "unit coverage on the two new primitives (getRoomPowerLevels + putRoomPowerLevels: happy 2xx, 403, 404, network fail, malformed body)"
  key_links:
    - from: "src/backend/relay-sessions/registry-rooms.ts"
      to: "src/backend/matrix/matrix-admin-client.ts"
      via: "getRoomPowerLevels + putRoomPowerLevels + createRoom(initial_state)"
      pattern: "getRoomPowerLevels|putRoomPowerLevels|initial_state"
    - from: "src/backend/relay-sessions/registry-rooms.ts"
      to: "src/backend/matrix/matrix-admin-creds-store.ts"
      via: "getMatrixAdminCreds().userId as fleet-admin mxid source"
      pattern: "creds\\.userId"
---

<objective>
Extend `ensureRegistryRoomsExist` in `src/backend/relay-sessions/registry-rooms.ts` so both registry rooms (agents + humans) come out of every boot pass structurally locked: only the fleet-admin account (@skynet-admin, read from `getMatrixAdminCreds().userId`) can send messages, invite, kick, ban, redact, or change room state. Apply the invariants BOTH at `createRoom` time via `initial_state` (birth-locked, no open window) AND via a boot-time idempotent read-then-optionally-write PATCH on `m.room.power_levels` (repairs drift on existing rooms). Preserve any existing PL 100 humans; only ADD fleet-admin if missing; only RAISE defaults, never lower.

Purpose: kills the failure mode from 2026-09-11 where a misrouted forward landed in the ~90-member agents-registry room and fanned out as a chat message. Structural fix — the room itself refuses non-admin sends regardless of what upstream callers do.

Output: two new matrix-admin-client primitives (`getRoomPowerLevels` + `putRoomPowerLevels`), a lockdown assertion in `registry-rooms.ts`, `createRoom` gains optional `initialState` param, and the three required test cases plus preserve-existing-admins and log-and-continue-on-failure coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/shapes/shape-registry-rooms-auto-lockdown-at-creation.md
@src/backend/relay-sessions/registry-rooms.ts
@src/backend/relay-sessions/registry-rooms.test.ts
@src/backend/matrix/matrix-admin-client.ts
@src/backend/matrix/matrix-admin-creds-store.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add getRoomPowerLevels + putRoomPowerLevels + createRoom initial_state to matrix-admin-client</name>
  <files>src/backend/matrix/matrix-admin-client.ts, src/backend/matrix/matrix-admin-client.test.ts</files>
  <behavior>
    New primitive: `getRoomPowerLevels(roomId): Promise&lt;{ ok:true, content: MatrixPowerLevelsContent } | AdminErr&gt;`
      - GET `/_matrix/client/v3/rooms/{encodeURIComponent(roomId)}/state/m.room.power_levels`
      - Bearer creds.accessToken
      - REQUEST_TIMEOUT_MS with AbortController (mirror getRoomName shape)
      - 200 with a JSON object → ok:true with body typed loosely as `Record&lt;string, unknown&gt;` (m.room.power_levels content shape is well-known but we defensively pass unknown through and let the caller narrow — do NOT throw on unexpected extra fields)
      - 404 → { ok:false, status:404, error:ERR_NON_2XX } (state event unset is a degenerate case; caller may treat as "assume defaults")
      - 403 → { ok:false, status:403, error:ERR_NON_2XX }
      - Other non-2xx → same generic mapping
      - Timeout → ERR_TIMEOUT; network exception → ERR_PROXY (mirror existing patterns)
      - NEVER log the access token (proxy-error scrub matches other primitives)
    New primitive: `putRoomPowerLevels(roomId, content): Promise&lt;{ ok:true } | AdminErr&gt;`
      - PUT `/_matrix/client/v3/rooms/{encodeURIComponent(roomId)}/state/m.room.power_levels`
      - Body: raw content JSON (whole content object; Matrix replaces the state event body)
      - Bearer creds.accessToken; JSON content-type; REQUEST_TIMEOUT_MS with AbortController
      - 200 → { ok:true }
      - 403 (permission denied) → { ok:false, status:403, error:ERR_NON_2XX }
      - Other non-2xx / timeout / proxy → same shape as existing primitives
    Extend `createRoom` signature: add optional `initialState?: Array&lt;{ type: string; state_key?: string; content: Record&lt;string, unknown&gt; }&gt;`. When present, forwarded verbatim to the request body as `initial_state`. Zero behavioral change when the caller omits it.

    Test cases (append to matrix-admin-client.test.ts using the existing fetch-mocking pattern in that file):
      G1: getRoomPowerLevels — 200 with a valid content JSON → { ok:true, content: {...} }.
      G2: getRoomPowerLevels — 404 → { ok:false, status:404, error:ERR_NON_2XX }; caller can treat as "assume defaults".
      G3: getRoomPowerLevels — 403 → { ok:false, status:403, error:ERR_NON_2XX }.
      G4: getRoomPowerLevels — network exception → { ok:false, status:502, error:ERR_PROXY }; access token never appears in a log call payload.
      G5: getRoomPowerLevels — path-traversal-shaped roomId is encodeURIComponent-passed (assert the URL contains `%21` for `!` and `%2F` for `/`).
      P1: putRoomPowerLevels — 200 with body echoed → { ok:true }; assert request method=PUT, request body JSON.parse matches the content argument byte-for-byte.
      P2: putRoomPowerLevels — 403 → { ok:false, status:403, error:ERR_NON_2XX }.
      P3: putRoomPowerLevels — timeout via AbortController → { ok:false, status:504, error:ERR_TIMEOUT }.
      C1: createRoom — no initialState arg → request body has NO `initial_state` key (byte-for-byte preserves existing shape; regression guard on the two-registry-rooms fast path).
      C2: createRoom — initialState=[{type:"m.room.power_levels",content:{...}}] → request body has `initial_state` array present with the passed content passed through.
  </behavior>
  <action>
    Add two new primitives + createRoom extension. Follow the exact code shape of `getRoomName` (lines 799–851) for the GET+error-mapping scaffolding: getMatrixAdminCreds bail on null → ERR_CREDS_MISSING; encodeURIComponent(roomId); REQUEST_TIMEOUT_MS + AbortController; 502-ERR_PROXY on catch; 504-ERR_TIMEOUT on AbortError; databaseLogger.error on proxy-error path with operation label. Follow the `createRoom` (lines 888–951) shape for `putRoomPowerLevels` PUT-with-JSON-body. Do NOT swallow the response content on GET — return it typed as `Record&lt;string, unknown&gt;` and let the caller narrow. For `createRoom`, thread the optional `initialState` arg through and, when present, set `body.initial_state = opts.initialState` before the fetch — that is the ONLY delta on the existing createRoom path. Do not change any other createRoom fields or defaults. Add module-scope operation labels like `matrix_admin_get_room_power_levels`, `matrix_admin_put_room_power_levels` for the proxy-error log lines. Do NOT export the loose content type as a public interface — the shape lives inline; callers pattern-match on the fields they care about. Tests go into `src/backend/matrix/matrix-admin-client.test.ts` using the file's existing `vi.stubGlobal("fetch", ...)` pattern (grep the file for `stubGlobal` if unclear on shape; copy the getRoomName test scaffolding as a starting point).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tanya &amp;&amp; npx vitest run src/backend/matrix/matrix-admin-client.test.ts &amp;&amp; npm run build:backend &amp;&amp; npm run build</automated>
  </verify>
  <done>New tests G1-G5 + P1-P3 + C1-C2 all pass. Existing matrix-admin-client tests still pass. `npm run build:backend` exits 0. `npm run build` exits 0. Grep confirms `getRoomPowerLevels` and `putRoomPowerLevels` are exported (`grep -c "^export async function \\(getRoomPowerLevels\\|putRoomPowerLevels\\)" src/backend/matrix/matrix-admin-client.ts` == 2). Grep confirms `createRoom` accepts an `initialState` parameter (`grep -c "initialState" src/backend/matrix/matrix-admin-client.ts` >= 2 — signature + threading).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add lockdown assertion + birth-locked initial_state to registry-rooms.ts</name>
  <files>src/backend/relay-sessions/registry-rooms.ts, src/backend/relay-sessions/registry-rooms.test.ts</files>
  <behavior>
    New internal helper `buildLockdownPowerLevelsContent(fleetAdminMxid, existingContent | null): { content, needsPatch }`:
      - Takes the currently-fetched m.room.power_levels content (or null when GET returned 404 = state event unset = "assume defaults":
        events_default=0, state_default=50, users_default=0, invite=0, kick=50, ban=50, redact=50, historical=100).
      - Produces the target content object with these MINIMUMS enforced:
          events_default: 100
          state_default: 100
          redact: 100
          invite: 100
          kick: 100
          ban: 100
          historical: 100
        and `users` map containing `fleetAdminMxid: 100` in addition to every existing entry in the incoming `users` map that is already at level 100 (preserve human admins).
      - RAISE-only rule: for each numeric invariant above, `target[field] = Math.max(existing[field] ?? default, 100)` (never lowers).
      - users merge rule: start with a shallow copy of existing users map; ADD fleetAdminMxid at 100 if not already >= 100; NEVER remove or lower an existing entry.
      - Preserve any other content fields present (e.g. `events`, `notifications`, `users_default`) byte-identical — we only enforce the specific invariants; unmentioned fields pass through unchanged.
      - Return `needsPatch = false` iff every field the helper would set is already at the target value AND the users map already contains fleetAdminMxid at PL 100. Otherwise `needsPatch = true`.
    New exported (test-visible) async fn `assertRegistryRoomLockdown(role: "agents"|"humans", roomId: string, fleetAdminMxid: string): Promise&lt;void&gt;` used by ensureRegistryRoomsExist for BOTH rooms after room existence is confirmed:
      1. Wrap the whole body in try/catch. On any thrown error OR any AdminErr from the two primitives, log a structured warn with operation="registry_rooms_lockdown_assert_failed", role, roomId, reason (either the AdminErr status/error or the thrown err.message), and return. Never throws.
      2. Call `getRoomPowerLevels(roomId)`. If AdminErr status==404, treat as content=null (state event unset — assume defaults). If AdminErr otherwise, log warn (operation="registry_rooms_lockdown_get_failed", role, roomId, status, error) and return.
      3. Call `buildLockdownPowerLevelsContent(fleetAdminMxid, contentOrNull)`. If needsPatch==false, log info operation="registry_rooms_lockdown_noop" role roomId and return.
      4. Log info operation="registry_rooms_lockdown_patch_fire" role roomId. Call `putRoomPowerLevels(roomId, target)`. On AdminErr, log warn operation="registry_rooms_lockdown_patch_failed" role roomId status error and return. On ok, log info operation="registry_rooms_lockdown_patched" role roomId.
    Wire `assertRegistryRoomLockdown` into `ensureRegistryRoomsExist` so it fires on BOTH rooms on every boot — on the fast-path (both settings rows present, existing rooms) AND on the slow-path (after createRegistryRoom returns ok, still assert to defensively repair drift on a room this boot happened to create). Fleet-admin mxid source: `creds.userId` from the creds already loaded at the top of ensureRegistryRoomsExist.
    Birth-locked wiring in `createRegistryRoom`: pass `initialState` to `createRoom` containing exactly ONE state event: `{ type: "m.room.power_levels", content: buildLockdownPowerLevelsContent(creds.userId, null).content }`. This means new rooms come out of createRoom already locked — no open window. Update `createRegistryRoom` signature to accept the fleetAdminMxid (thread from ensureRegistryRoomsExist's already-loaded creds).

    Test cases (append to registry-rooms.test.ts using the file's existing scaffold — vi.mock createRoom + new mocks for getRoomPowerLevels + putRoomPowerLevels + new spy on `HAPPY_CREDS.userId` as fleetAdminMxid):
      L1 (fresh install / birth-locked): both settings rows absent → ensureRegistryRoomsExist calls createRoom TWICE, each time with an `initialState` arg containing `{type:"m.room.power_levels", content: {...}}` whose events_default/state_default/redact/invite/kick/ban/historical are all 100 and whose users map contains HAPPY_CREDS.userId at 100. After createRoom returns ok, assertRegistryRoomLockdown STILL runs — but since assertRegistryRoomLockdown's GET-then-diff sees the just-created content already at target (assuming the createRoom mock returns content matching the initial_state), needsPatch==false and no putRoomPowerLevels call fires. Assert: putRoomPowerLevelsSpy NOT called on the fresh-install path.
      L2 (drift): both settings rows already exist (fast-path); mock getRoomPowerLevels for agents room → returns content with events_default=0, users={"@skynet-admin:...":100} (already there); for humans room → returns content with events_default=50, invite=0, users={"@nelly-admin:...":100} (an existing human admin). Expect putRoomPowerLevels called for BOTH rooms; agents PATCH content has events_default=100 and preserves users["@skynet-admin"]=100; humans PATCH content has events_default=100, invite=100, and users={"@nelly-admin:...":100, "@skynet-admin:...":100} — human admin preserved AND fleet admin added.
      L3 (already-locked no-op): fast-path; mock getRoomPowerLevels for both rooms → returns content ALREADY at all target levels (all invariants at 100, users includes HAPPY_CREDS.userId at 100). Expect putRoomPowerLevels NOT called for either room. Expect an info log with operation="registry_rooms_lockdown_noop".
      L4 (preserve-existing-humans-and-never-lower): mock getRoomPowerLevels → content has kick=200 (someone bumped it above 100), invite=0 (drift), users={"@human-admin:...":100, "@another-human:...":100}. Expect PATCH fires; sent content has kick=200 (RAISE-only — never lowered), invite=100 (raised), users map contains all three: the two existing humans preserved AND HAPPY_CREDS.userId added.
      L5 (failure log-and-continue): mock getRoomPowerLevels → { ok:false, status:502, error:"proxy_error" } for agents room; humans room happy-path with drift. Expect: ensureRegistryRoomsExist STILL returns { ok:true, agentsRoomId, humansRoomId } (log-and-continue); warn log fires with operation="registry_rooms_lockdown_get_failed" role="agents"; humans PATCH still fires normally.
      L6 (putRoomPowerLevels failure log-and-continue): mock getRoomPowerLevels → drift; putRoomPowerLevels → { ok:false, status:403, error:"forbidden" }. Expect: boot completes ok; warn log operation="registry_rooms_lockdown_patch_failed"; no throw.
      L7 (buildLockdownPowerLevelsContent unit): if exposed for testing (via export), directly assert: (a) null input → target with defaults raised to 100 + fleetAdminMxid at 100; (b) exact-match input → needsPatch==false; (c) users_default present in input → passed through unchanged; (d) unrelated top-level fields (e.g. `notifications`) pass through unchanged.
  </behavior>
  <action>
    Add `buildLockdownPowerLevelsContent` as an EXPORTED function so tests can unit it directly (avoids indirect assertions through end-to-end mock plumbing). Signature: `export function buildLockdownPowerLevelsContent(fleetAdminMxid: string, existing: Record&lt;string, unknown&gt; | null): { content: Record&lt;string, unknown&gt;; needsPatch: boolean }`. Implement it as a pure function — no I/O, no logger. Defensive narrowing on `existing`: for each numeric field, coerce via `typeof existing[field] === "number" ? existing[field] as number : DEFAULT`. Take a shallow copy of `existing.users` (narrowed as `Record&lt;string, number&gt;` after typeof check on each entry) or `{}` if absent/wrong-type. Bake in the well-known Matrix `m.room.power_levels` defaults from the spec (referenced in the shape doc: events_default=0, state_default=50, users_default=0, invite=0, kick=50, ban=50, redact=50, historical=100) as the fallback when a field is missing.

    Add `assertRegistryRoomLockdown` as an EXPORTED async fn (also test-visible). Wire it into ensureRegistryRoomsExist at TWO points: (a) after selfHealAdminRoomsMembership on the fast-path for BOTH rooms; (b) after each successful createRegistryRoom on the slow-path (defensive — new room should already be locked from initial_state, but assert runs and no-ops).

    Update `createRegistryRoom` to accept `fleetAdminMxid: string` as a new required param (thread from ensureRegistryRoomsExist's already-loaded `creds.userId`) and pass `initialState: [{ type: "m.room.power_levels", content: buildLockdownPowerLevelsContent(fleetAdminMxid, null).content }]` to `createRoom`. Zero other changes to createRegistryRoom.

    Test file: extend the vi.mock("../matrix/matrix-admin-client.js", ...) call to also mock getRoomPowerLevels + putRoomPowerLevels (both `vi.fn()`); add matching typed spies at the top of the test file next to createRoomSpy/joinRoomSpy. Reset both in beforeEach. Add a new `describe("registry-rooms lockdown", ...)` block with tests L1–L7. Use HAPPY_CREDS.userId as the fleet-admin mxid throughout. Keep prior test blocks unchanged — L-suite is additive.

    Do NOT modify `joinAgentToAgentsRegistry` / `joinHumanToHumansRegistry` — the lockdown is orthogonal to the join hooks.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tanya &amp;&amp; npx vitest run src/backend/relay-sessions/registry-rooms.test.ts src/backend/matrix/matrix-admin-client.test.ts &amp;&amp; npm run build:backend &amp;&amp; npm run build</automated>
  </verify>
  <done>All tests L1–L7 pass. All prior tests in registry-rooms.test.ts still pass (fresh install, both-present no-op, join hooks, etc.). All prior tests in matrix-admin-client.test.ts still pass. `npm run build:backend` exits 0. `npm run build` exits 0. Grep confirms `assertRegistryRoomLockdown` is exported (`grep -c "^export async function assertRegistryRoomLockdown" src/backend/relay-sessions/registry-rooms.ts` == 1). Grep confirms `buildLockdownPowerLevelsContent` is exported (`grep -c "^export function buildLockdownPowerLevelsContent" src/backend/relay-sessions/registry-rooms.ts` == 1). Grep confirms lockdown wiring on BOTH fast-path (`grep -c "assertRegistryRoomLockdown" src/backend/relay-sessions/registry-rooms.ts` >= 3 — one export + two call sites minimum). Grep confirms birth-locked initial_state wiring (`grep -c 'initialState' src/backend/relay-sessions/registry-rooms.ts` >= 1).</done>
</task>

</tasks>

<verification>
Overall phase checks — executor runs these before considering the task green (deploy is NOT executor's remit; docker build + docker compose up + git push are orchestrator motions):

1. Scoped tests green on the two touched test files:
   `npx vitest run src/backend/relay-sessions/registry-rooms.test.ts src/backend/matrix/matrix-admin-client.test.ts`
   Exit 0. All pre-existing tests still pass. All new L-suite + G/P/C-suite tests pass.

2. Backend build clean:
   `npm run build:backend`
   Exit 0.

3. Full build (frontend + backend) clean:
   `npm run build`
   Exit 0.

4. Grep gates (invariant list from shape doc + must_haves):
   `grep -c "^export async function \(getRoomPowerLevels\|putRoomPowerLevels\)" src/backend/matrix/matrix-admin-client.ts` == 2
   `grep -c "^export async function assertRegistryRoomLockdown" src/backend/relay-sessions/registry-rooms.ts` == 1
   `grep -c "^export function buildLockdownPowerLevelsContent" src/backend/relay-sessions/registry-rooms.ts` == 1
   `grep -v '^#' src/backend/relay-sessions/registry-rooms.ts | grep -c "assertRegistryRoomLockdown"` >= 3
   `grep -v '^#' src/backend/relay-sessions/registry-rooms.ts | grep -c "initialState"` >= 1
</verification>

<success_criteria>
- Both registry rooms (agents + humans) come out of every boot pass with all 8 power_level invariants at 100 (events_default, state_default, redact, invite, kick, ban, historical, users[fleetAdmin]).
- Fresh-install path: new rooms are birth-locked via createRoom's initial_state — no observable open-write window between room creation and PATCH.
- Drifted-room path: boot pass detects any invariant below 100 and PATCHes it up on the next call.
- Already-locked room: boot pass detects no drift and does NOT issue a PATCH (zero-diff = no-op).
- Existing PL 100 human admins in the users map are preserved across every PATCH; fleet-admin is only ADDED, never a REMOVE.
- Existing levels ABOVE 100 (e.g. kick=200 set manually) are preserved — the assertion RAISES only, never LOWERS.
- Failure paths (Matrix unreachable, 403 on PATCH, GET returns non-2xx-non-404) log a structured warn with roomId + reason and return; ensureRegistryRoomsExist still returns ok on the surrounding success path so boot continues; next boot retries automatically.
- No new npm dependencies. No changes to join hooks or the observation-loop side. No changes to the two SETTINGS_KEY_* constants. No deploy from executor.
</success_criteria>

<output>
Create `.planning/quick/260911-n8a-registry-rooms-auto-lockdown-at-creation/260911-n8a-SUMMARY.md` when done. Report scoped-vitest pass count, `npm run build:backend` exit code, `npm run build` exit code, all 5 grep-gate results, and a one-line-per-test summary of L1–L7 + G1–G5 + P1–P3 + C1–C2 outcomes. Do NOT push, do NOT `docker build`, do NOT `docker compose up` — those are orchestrator (tanya) motions.
</output>
