---
phase: 260723-2cg-add-a-subtle-inside-textarea-send-button
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/ComposeBox.tsx
  - src/ui/features/pretty-view/ComposeBox.test.tsx
autonomous: true
requirements:
  - BOUNTY: send-button-inside-composebox-textarea (patch #129)
must_haves:
  truths:
    - "A subtle send-arrow button is visible inside the ComposeBox textarea at bottom-right (rest color = rgba(240,235,224,0.3))."
    - "Clicking the button when text is present dispatches the message via the existing handleSend() (same routing as Enter-to-send: newline collapse, attachment branching, no-optimistic-display)."
    - "The button is disabled (non-clickable, dimmer color, cursor-not-allowed) when text is empty AND no attachments are staged, or when queueArmed is true, or when canSend===false and no attachments are staged."
    - "Typed text does not slide under the button icon (textarea has 40px right padding via pr-10)."
    - "Existing Enter-sends, Shift-Enter-newlines, aux-row buttons (Paperclip/Stop/ThumbsUp/Queue), meter, queueArmed overlay, and reset-send behavior are all unchanged."
    - "npx tsc --noEmit is clean; npx vitest run src/ui/features/pretty-view/ComposeBox.test.tsx is all green; npm run build succeeds."
  artifacts:
    - path: "src/ui/features/pretty-view/ComposeBox.tsx"
      provides: "Bare <button type='button'> with lucide SendHorizontal icon positioned absolute inside the existing textarea wrapper; sendDisabled local const; pr-10 added to Textarea className; SendHorizontal added to lucide-react import."
      contains: "SendHorizontal"
    - path: "src/ui/features/pretty-view/ComposeBox.test.tsx"
      provides: "Three new tests covering: button renders, click-when-text-present calls onSend with trimmed payload, disabled-state renders as disabled + click is a no-op."
      contains: "inside-textarea send button"
  key_links:
    - from: "src/ui/features/pretty-view/ComposeBox.tsx (new button onClick)"
      to: "handleSend() at line ~652"
      via: "onClick={() => { if (!sendDisabled) handleSend(); }}"
      pattern: "if \\(!sendDisabled\\) handleSend\\(\\)"
    - from: "src/ui/features/pretty-view/ComposeBox.tsx (new button)"
      to: "src/ui/features/pretty-view/ComposeBox.tsx (existing wrapper `<div className='relative flex-1 self-stretch'>` at ~line 1250)"
      via: "sibling to <Textarea> and queueArmed overlay inside the same relative wrapper"
      pattern: "absolute right-3 bottom-2\\.5"
---

<objective>
Bake Ashley's console-locked "subtle inside-textarea send button" visual into React on the pretty-view ComposeBox. Bare `<button type="button">` (NOT shadcn Button — sidesteps the wrapper specificity trap that bit patches #81 and #117) positioned absolute bottom-right INSIDE the existing textarea-wrapper div, using lucide `SendHorizontal` at 24×24 with `fill="currentColor"`. All routing goes through the existing `handleSend()` at line 652 — no branching duplication. Add `pr-10` to the Textarea so typed text doesn't slide under the icon. Extend the existing test file with three tests (renders / click sends / disabled state).

Purpose: Close the `send-button-inside-composebox-textarea` bounty (patch #129 in the stack awaiting Ashley UAT + greenlight). Visual is Ashley-locked via DevTools console iteration — do NOT "improve" the values.

Output: Modified `ComposeBox.tsx` (one file, small diff) + extended `ComposeBox.test.tsx` (three new tests). One atomic commit — implementation + tests together, since the tests directly verify the implementation and the diff is small.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@src/ui/features/pretty-view/ComposeBox.tsx
@src/ui/features/pretty-view/ComposeBox.test.tsx
@src/ui/components/textarea.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add subtle inside-textarea send button + extend tests</name>
  <files>src/ui/features/pretty-view/ComposeBox.tsx, src/ui/features/pretty-view/ComposeBox.test.tsx</files>
  <behavior>
    New tests (append to the existing `describe("ComposeBox — Phase 05 upload wiring", ...)` block in ComposeBox.test.tsx, OR add a new `describe("ComposeBox — inside-textarea send button (patch #129)", ...)` block at the bottom of the file — planner leaves this to the executor):

    - Test N+1: Button renders when onSend is provided.
      Render `<ComposeBox {...baseProps()} />`. Assert `screen.getByRole('button', { name: 'Send' })` is truthy AND is a bare `<button>` (its `tagName` is `'BUTTON'`). Assert it lives inside the same wrapper as the textarea (walk parents until you find `[class*="relative"][class*="flex-1"]` — must contain both the textarea and the button).

    - Test N+2: Click when text is present calls onSend with the trimmed payload.
      Render with `onSend = vi.fn(() => true)`. Type "hi there  " into the textarea (via `fireEvent.change`). Click `screen.getByRole('button', { name: 'Send' })`. Assert `onSend` was called with `"hi there"` (trimmed by handleSend; newlines→spaces path also active but no newlines in this test). Assert textarea `.value` is `""` after the click (COMPOSE-04 clear-on-success).

    - Test N+3: Disabled state — button is disabled AND click is a no-op.
      Case A (empty text, no attachments, canSend not set): render `<ComposeBox {...baseProps({ onSend: vi.fn(() => true) })} />`. The button MUST report `disabled === true` (via the `disabled` attribute on the bare button element). Click it. Assert `onSend` was NOT called.
      Case B (canSend=false, no attachments, empty text): render `<ComposeBox {...baseProps({ onSend: vi.fn(() => true), canSend: false })} />`. Same assertions.

    IMPORTANT — pre-existing stale test cleanup:
      Tests 7, 8, and Phase 9 Layout's "aux button group renders in a row that precedes the Send button's row" test currently call `screen.getByLabelText(/send message/i)`. That label was on the amber aux-row Send button removed in patch #121 (STATE.md's "4 pre-existing failures" line). The new button carries `aria-label="Send"` — the regex `/send message/i` will NOT match `"Send"` (word-boundary mismatch). Update those three tests to `screen.getByRole('button', { name: 'Send' })` — that matches on aria-label exactly-equal-to `"Send"` per RTL's accessible-name algorithm. Do NOT change what the tests assert; only fix the selector so they resolve to the new inside-textarea button. The "Send 'let's go'" ThumbsUp button (patch #124) has a different aria-label and stays untouched.

    Also: the Phase 9 Layout test "aux button group renders in a row that precedes the Send button's row" (lines ~316-333) asserts the ThumbsUp row precedes the Send button's row in DOM order. With the new inside-textarea Send button, this is STILL true (Row 1 aux-group at line ~1101 precedes Row 2 textarea wrapper at line ~1244 which now contains the Send button). The assertion holds; only the selector needs updating.
  </behavior>
  <action>
    STEP 1 — Import SendHorizontal. In `src/ui/features/pretty-view/ComposeBox.tsx` line 2, add `SendHorizontal` to the lucide-react named-import list (keep alphabetical: `Hourglass, Paperclip, RefreshCw, RotateCcw, SendHorizontal, Square, ThumbsUp`).

    STEP 2 — Extract sendDisabled local const. In the ComposeBox function body, ABOVE the `return (` (locate it near the other derived state around line 1090ish — put it just before the JSX return, next to `hasAttachments` / `hasErroredChip` usage or near the queueArmed derivation, wherever fits the existing style). Define exactly:

        const sendDisabled = queueArmed || (!canSend && !hasAttachments) || (text.trim() === "" && !hasAttachments);

    Reasoning captured in Ashley's locked spec: queueArmed always disables; if canSend is false AND there are no attachments the send will fail (attachment path routes independently via onSendWithAttachments); if the text is empty AND there are no attachments there's nothing to send. Note the precedence — `canSend` may be `undefined` (default), and `!undefined === true`, so the second clause would over-disable if we removed the `!hasAttachments` guard. Verify the boolean math against the existing handleSend's guards before wiring.

    STEP 3 — Add `pr-10` to the Textarea className. In the `cn(...)` call on the `<Textarea>` around line 1279-1322, append `"pr-10"` as a new string in the class list (put it near the existing `"rounded-[10px] px-4 py-3"` line — right padding override goes AFTER `px-4` so tailwind-merge's later-wins dedupe keeps the 40px right padding while the 16px left padding survives). The shadcn base has `px-3 py-2` — our `px-4 py-3` already beats it via later-wins; adding `pr-10` continues that pattern (arbitrary spacing vs. plain spacing — same specificity, later wins). No `!` needed (no dark: variant conflict on padding).

    STEP 4 — Insert the button. INSIDE the existing wrapper `<div className="relative flex-1 self-stretch">` at line 1250 (which currently contains the `<Textarea>` and the `{queueArmed && <div className="absolute inset-0 ... />}` overlay), add a THIRD child — the send button — AFTER the queueArmed overlay's closing `)}` and BEFORE the wrapper's closing `</div>` (which is at line 1346). This keeps the queueArmed overlay's `inset-0 backdrop-blur pointer-events-none` layer visually on top of the button when queued, and the button's own `disabled` state (via `sendDisabled` — queueArmed forces it true) prevents interaction. Belt-and-suspenders per Ashley's locked spec.

    The button element (bare `<button>`, NOT shadcn `<Button>`):

    - `type="button"` (prevents implicit form-submit; textarea has no surrounding form but be explicit)
    - `onClick={() => { if (!sendDisabled) handleSend(); }}` (delegates ALL routing to handleSend — attachment branching, newline collapse, COMPOSE-04, error handling — nothing duplicated)
    - `disabled={sendDisabled}` (native button disabled attr; makes click a no-op AND lets screen-readers announce the state)
    - `aria-label="Send"`
    - `title="Send"`
    - `className={cn(...)}` with these classes in this exact order:
      - `"absolute right-3 bottom-2.5"` (12px right, 10px bottom inside wrapper — Ashley's locked position)
      - `"p-2"` (8px padding → 40x40 hit target around the 24x24 icon; verify: 24 + 8+8 = 40)
      - `"text-[rgba(240,235,224,0.3)]"` (rest color)
      - `"hover:text-[rgba(240,235,224,0.9)]"` (hover color)
      - `"disabled:text-[rgba(240,235,224,0.15)]"` (disabled color)
      - `"disabled:cursor-not-allowed"` (Tailwind maps to cursor: not-allowed)
      - `"transition-[color,transform] duration-120"` (120ms color+transform transition — arbitrary duration, Tailwind v4 supports)
      - `"active:scale-95"` (tactile press — Ashley wrote "scale-95 or scale-90 planner's choice"; choose 95 for a subtler feel matching the ChatGPT/iMessage-quiet directive)
      - `"cursor-pointer"` (default cursor when enabled, matches other clickable buttons in the file)

    Child: `<SendHorizontal className="size-6" fill="currentColor" />`. `size-6` = 24×24. `fill="currentColor"` (NOT stroke) makes the lucide arrow render as a solid paper-plane silhouette per Ashley's locked visual. lucide-react passes through the `fill` SVG attribute to the underlying `<svg>`.

    Note on VISUAL-08 comments: The comment block above line 1240 ("VISUAL-08 HARD LOCK on Send's amber gradient — never change") refers to the retired aux-row Send from patch #121, NOT this new subtle inside-textarea Send. Ashley wants ChatGPT/iMessage-quiet, deliberately NOT amber-pop. Per the <what_NOT_to_change> instruction, LEAVE THAT COMMENT ALONE (stale cleanup deferred to a future patch). Do not "correct" it, delete it, or move it.

    STEP 5 — Extend the test file. Update the three stale-selector tests as described in <behavior> (Test 7, Test 8, Phase 9 Layout aux-row test — swap `getByLabelText(/send message/i)` for `getByRole('button', { name: 'Send' })`). Then add the three new tests (N+1, N+2, N+3) at the end of the existing `describe("ComposeBox — Phase 05 upload wiring", ...)` block. `baseProps()` already provides `onSend: vi.fn(() => true)` — override where needed. Vitest + Testing Library idioms already established in the file; follow those (no new deps).

    STEP 6 — Verify. Run the three commands under <verify>. tsc must be clean, all vitest tests in ComposeBox.test.tsx green (this includes fixing the 3 stale ones from patch #121), and npm run build succeeds. Do NOT run docker build. Do NOT deploy. Do NOT push. This stacks with #123-#128 pending Ashley greenlight.

    Scope discipline: do not touch aux row buttons (line 1101-1237), do not touch queueArmed overlay logic, do not touch VISUAL-08 comments, do not touch the amber-Send-related legacy comments. If tsc surfaces pre-existing type-debt at nearby lines unrelated to this patch, note in the SUMMARY and skip (same posture STATE.md's patch #125 entry documents).
  </action>
  <verify>
    <automated>cd /home/ubuntu/termix && npx tsc --noEmit 2>&1 | tail -20 && npx vitest run src/ui/features/pretty-view/ComposeBox.test.tsx 2>&1 | tail -30 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `SendHorizontal` imported from `lucide-react` in ComposeBox.tsx.
    - `sendDisabled` const defined above the return with the exact predicate `queueArmed || (!canSend && !hasAttachments) || (text.trim() === "" && !hasAttachments)`.
    - `<Textarea>` className contains `pr-10` (right padding = 40px).
    - Bare `<button type="button">` sibling to the Textarea inside `<div className="relative flex-1 self-stretch">`, with all classes and attributes per STEP 4, containing `<SendHorizontal className="size-6" fill="currentColor" />`.
    - Three new tests added covering render / click-sends / disabled-state; the three stale `/send message/i` selectors in existing tests updated to `getByRole('button', { name: 'Send' })`.
    - `npx tsc --noEmit` exits clean (or only pre-existing unrelated type-debt at unrelated lines).
    - `npx vitest run src/ui/features/pretty-view/ComposeBox.test.tsx` — all tests green, including the 3 previously-failing tests now fixed by selector update.
    - `npm run build` exits 0.
    - No docker build, no deploy, no push.
  </done>
</task>

</tasks>

<verification>
Three-check gate at the end of the single task:

1. `npx tsc --noEmit` — clean. This catches the SendHorizontal import, the sendDisabled const type inference (booleans compose correctly), and any prop-type violations on the bare button.

2. `npx vitest run src/ui/features/pretty-view/ComposeBox.test.tsx` — ALL green. Success requires:
   - The 3 new tests pass (button renders, click sends, disabled state).
   - The 3 previously-failing tests (Tests 7, 8, Phase 9 aux-row) now pass with the updated `getByRole('button', { name: 'Send' })` selector resolving to the new inside-textarea button.
   - No test regressions elsewhere.
   Baseline reference: STATE.md notes "4 pre-existing ComposeBox failures" — 3 are Send-button-related (this patch fixes), 1 is the patch #124 ThumbsUp "let's go" aria-label residual (out of scope for this patch, leave alone). Final count should be: 0 or 1 failing tests. If 1 failing, it's the ThumbsUp "let's go" residual — verify in the vitest output by name; do NOT fix it here.

3. `npm run build` — exits 0. Confirms the change survives Vite's production transform (no dev-only syntax slipped in, no unresolved imports).

DO NOT run: `docker build`, `docker compose`, any deploy script, any push.
</verification>

<success_criteria>
- Ashley's exact console-locked visual is baked in (position, colors, transition, icon fill, hit target, disabled treatment) — no "improvements."
- Click-through routes to the existing `handleSend()` — no branching duplication, no COMPOSE-04 violation, no newline-collapse duplication, no attachment-branching duplication.
- Typed text does not slide under the icon (`pr-10` on textarea).
- Enter-to-send, Shift-Enter-newlines, aux-row buttons, meter, queueArmed overlay, reset-send — all unchanged.
- tsc clean, ComposeBox tests green (including 3 stale-selector fixes), production build succeeds.
- Nothing deployed. Nothing pushed. Nothing docker-built. Sits on the #123-#128 stack awaiting Ashley UAT + greenlight.
- Ready for a follow-up commit + patches-md entry as patch #129 in Tina's format.
</success_criteria>

<output>
Create `.planning/quick/260723-2cg-add-a-subtle-inside-textarea-send-button/260723-2cg-SUMMARY.md` when done, capturing:
- Files changed with line-delta counts.
- Confirmation of the three verify commands (tsc/vitest/build) passing.
- Which pre-existing failing tests were fixed by the selector update (Tests 7, 8, Phase 9 aux-row) and which — if any — remain (the patch #124 ThumbsUp residual).
- Draft patch #129 entry for Tina's termix-patches.md in her established format.
- Explicit note: NOT deployed, NOT pushed, batched with #123-#128 pending Ashley greenlight.
</output>
