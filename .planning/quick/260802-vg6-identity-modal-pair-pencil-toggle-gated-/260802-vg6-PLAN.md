---
phase: quick-260802-vg6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/IdentityModal.tsx
  - src/ui/features/pretty-view/IdentityModal.test.tsx
autonomous: true
requirements:
  - VG6-PATCH-277-pencil-toggle-edit-surface
  - VG6-PATCH-278-voice-option-contrast
  - VG6-PATCH-279-color-hue-picker-with-get-verify

must_haves:
  truths:
    - "Identity modal opens with the edit block hidden by default; only the identity.md preview shows."
    - "A pencil icon button sits immediately before the close (X) button in the DialogHeader, matching the glass affordance of the close button."
    - "Clicking the pencil reveals the entire edit block (avatar picker + title + voice + hue + save/cancel + inline error)."
    - "Clicking the pencil again (or after a successful save, or after Cancel) collapses the edit block back to hidden."
    - "When editing is active, the pencil renders visually 'pressed' (brighter icon color, hover-state background)."
    - "The identity.md markdown preview (IdentityFileTab) remains visible regardless of edit state."
    - "The Voice <select> options render with dark background (#1a1c26) and near-white text (#f0ebe0) instead of the browser-default unreadable white-on-white combo."
    - "The edit block contains a Color row with a full-hue-spectrum range slider, a live circular swatch, and a numeric degree readout — placed after Voice, before the inline error."
    - "Dragging the hue slider updates the swatch and readout in real time; Save button enables when hue differs from committed value."
    - "Saving with a hue change sends meta.colorHue in the multipart data field, updates committedHue from the server echo, and re-disables Save."
    - "Cancel reverts hueDraft to committedHue alongside the existing title/avatar/voice reverts."
    - "If the server echoes back a colorHue that does not match the one we sent (Skynet multipart-data silent-no-op trap), the save surfaces an inline error instead of falsely reporting success."
    - "IdentityModal.test.tsx passes: existing 5 tests gated behind a pencil-click prelude, plus at least one new hue-picker smoke test verifying hueDraft updates and hue is included in the save meta."
  artifacts:
    - path: "src/ui/features/pretty-view/IdentityModal.tsx"
      provides: "Pencil-toggle-gated edit surface, voice option contrast fix, colorHue picker with GET-verify"
      contains: "editing, setEditing, hueDraft, setHueDraft, committedHue, setCommittedHue, Pencil"
    - path: "src/ui/features/pretty-view/IdentityModal.test.tsx"
      provides: "Test coverage updated for pencil gate + hue picker smoke"
  key_links:
    - from: "src/ui/features/pretty-view/IdentityModal.tsx"
      to: "src/ui/api/identities-api.ts (updateIdentity, IdentityInput.colorHue)"
      via: "meta.colorHue in multipart data field"
      pattern: "meta\\.colorHue\\s*="
    - from: "src/ui/features/pretty-view/IdentityModal.tsx"
      to: "lucide-react (Pencil icon import)"
      via: "named import alongside existing X, Volume2, etc."
      pattern: "Pencil"
    - from: "src/ui/features/pretty-view/IdentityModal.tsx"
      to: "state resets on open (useEffect L382-395)"
      via: "identity.colorHue in dependency array + hueDraft/committedHue reset"
      pattern: "identity\\.colorHue"
---

<objective>
Ship the identity-modal pinned bounty pair (patches #277-equiv, #278-equiv, #279-equiv) as three atomic commits on branch `feat/tab-title-from-tmux` — sitting on top of the existing deploy queue (#267-#276). Adds pencil-toggle-gated edit surface, fixes the voice dropdown option contrast, and adds a colorHue picker with GET-verify defense against the Skynet multipart-`data`-field silent-no-op trap.

Purpose: Ashley's identity modal is currently over-eager — the entire editor block is always visible, cluttering the identity.md read view; the voice picker's options are unreadable on desktop; and there's no UI for colorHue despite `IdentityInput.colorHue` being wired end-to-end. This plan lands all three pinned bounties as separate rebase-able commits.

Output: 3 commits landed on `feat/tab-title-from-tmux`, IdentityModal.tsx behavior updated per spec, IdentityModal.test.tsx passing with pencil-gate preludes + new hue smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/ui/features/pretty-view/IdentityModal.tsx
@src/ui/features/pretty-view/IdentityModal.test.tsx
@src/ui/api/identities-api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pencil toggle + edit-mode state (patch #277-equiv)</name>
  <files>src/ui/features/pretty-view/IdentityModal.tsx, src/ui/features/pretty-view/IdentityModal.test.tsx</files>
  <behavior>
    - Test A (existing #1-5 preludes): each of the existing 5 tests that probe title input / avatar picker / voice select / Save / Cancel now first clicks a pencil button with aria-label matching /edit identity/i before running its existing assertions. All 5 tests continue to pass with that prelude added.
    - Test B (implicit, via existing tests): before pencil click, the title input (getByLabelText("Title")) MUST NOT be present in the DOM (queryByLabelText returns null).
    - Test C (implicit): after clicking pencil once, the title input IS present; after a successful save (test 1) the pencil returns to non-pressed state (aria-label back to /edit identity/i, not /done editing/i) — verify via getByRole("button", {name: /edit identity/i}) after the save-success await.
  </behavior>
  <action>
    Implement pencil-toggle-gated edit surface in `src/ui/features/pretty-view/IdentityModal.tsx`:

    1. Extend the lucide-react import at L2 to include `Pencil` alongside the existing named imports (`AlarmClock`, `Clock`, `Handshake`, `Target`, `User`, `Volume2`, `X`).

    2. Add `const [editing, setEditing] = useState(false);` in the state-declaration block near L184 (immediately after the `saveError` useState, before the voice picker state at L186). Keep grouping tidy — this state belongs with the editor state cluster.

    3. In the `DialogHeader` block (L928-992), insert a new Pencil icon button as the immediate sibling BEFORE `<DialogClose asChild>` at L968. Style-match the existing close-button's glass affordance defined at L969-990: same `size-9 rounded-full`, same `background: rgba(255,255,255,0.04)`, same hairline `border: 1px solid rgba(220,225,245,0.10)`, same muted rest color `#a89a80`, same hover recipe using `hsla(${hue}, 60%, 50%, 0.25)` for the box-shadow glow. Toggle onClick sets `setEditing((v) => !v)`. When `editing === true`, override the base style with the hover-state values (background `rgba(255,255,255,0.10)`, border `rgba(220,225,245,0.22)`, box-shadow `0 0 20px hsla(${hue}, 60%, 50%, 0.25)`) AND set color to `#f0ebe0` so it reads visually pressed. `aria-label` and `title` flip on state: `editing ? "Done editing" : "Edit identity"`. Render `<Pencil className="size-4" />` inside. Do NOT wrap in DialogClose — this button does NOT close the dialog.

    4. Wrap the ENTIRE inline editor block in `{editing && ( ... )}`. The block starts at L1011 (`<div className="mb-6">` with the "Edit identity" h3) and ends at its matching closing `</div>` around L1170 (immediately before the `<IdentityFileTab>` at L1173). This gates avatar picker + title + voice + save/cancel + inline error behind the pencil toggle. Do NOT wrap the `<IdentityFileTab>` at L1173 — the identity.md markdown preview stays visible unconditionally.

    5. In the Save success path around L831-835 (right after `setVoiceDraft(updated.voice ?? "");` and `setSaveError(null);`, before the `} catch` at L836), add `setEditing(false);` so the editor collapses after a clean save.

    6. In `onCancel` around L845-855, add `setEditing(false);` as the last line (after `setVoiceDraft(committedVoice ?? "");`) so Cancel also collapses the editor.

    7. Update `src/ui/features/pretty-view/IdentityModal.test.tsx`:
       - Grep the file for the 5 existing test blocks that reference `getByLabelText("Title")`, `document.querySelector("input[type='file']")`, `screen.getByRole("button", { name: /Save/i })`, or `screen.getByRole("button", { name: /Cancel/i })`.
       - In each such test, immediately after the `renderModal(...)` call, insert:
         `fireEvent.click(screen.getByRole("button", { name: /edit identity/i }));`
         This reveals the edit block before existing assertions run.
       - For test 1 (happy path), after the final `waitFor` that checks the Save button re-disables, ALSO wrap the button lookup inside the waitFor to survive React's rerender cycle — the existing waitFor structure already handles this, no change needed beyond the prelude click.
       - For test 1 specifically, after the save success is asserted, add one more assertion: `expect(screen.getByRole("button", { name: /edit identity/i })).toBeTruthy();` to verify the pencil returned to non-editing state (Save handler sets `setEditing(false)`).

    Do NOT introduce Radix-style controlled toggle patterns — a plain `<button type="button" onClick={...}>` matching the close-button's markup is the right shape per patch #91's precedent. Do NOT use `checkpoint:human-verify` — this is auto-typechecked + auto-tested.

    After the edit, from `/home/ubuntu/skynet` run `npx tsc --noEmit` (must pass), then `npm run build:backend` (no-op safety per tina.md standing directive), then `npm test -- IdentityModal` (must pass). Then commit:
      `git add src/ui/features/pretty-view/IdentityModal.tsx src/ui/features/pretty-view/IdentityModal.test.tsx`
      `git commit -m "feat(pv/identity-modal): pencil-toggle-gated edit surface (patch #277-equiv)"`
    Do NOT push, do NOT deploy, do NOT touch skynet-patches.md.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx tsc --noEmit && npm run build:backend && npm test -- IdentityModal</automated>
  </verify>
  <done>
    - `Pencil` imported from lucide-react.
    - `editing` state added; `setEditing(false)` called on Save success AND Cancel.
    - Pencil button rendered before DialogClose in DialogHeader, matching close-button styling, pressed state on `editing===true`, aria-label toggles between "Edit identity" / "Done editing".
    - Entire edit block (L1011-L1170) wrapped in `{editing && (...)}`.
    - `<IdentityFileTab>` at L1173 remains outside the gate.
    - `IdentityModal.test.tsx`: all 5 existing tests updated with pencil-click prelude; test 1 also asserts pencil returns to non-editing after save.
    - `npx tsc --noEmit` passes; `npm run build:backend` passes; `npm test -- IdentityModal` passes.
    - Single commit created on branch `feat/tab-title-from-tmux` with subject line `feat(pv/identity-modal): pencil-toggle-gated edit surface (patch #277-equiv)`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Voice dropdown option contrast fix (patch #278-equiv)</name>
  <files>src/ui/features/pretty-view/IdentityModal.tsx</files>
  <action>
    Fix the unreadable voice dropdown options in `src/ui/features/pretty-view/IdentityModal.tsx`. The `<select>` control itself has dark styling (L1097-1106) but the browser-default `<option>` background is white and inherits our near-white `#f0ebe0` text via cascade — invisible on desktop browsers.

    1. Locate the two `<option>` elements inside the Voice `<select>`:
       - The default option at L1108: `<option value="">(default)</option>`
       - The mapped voices at L1109-1113: `{voices.map((v) => (<option key={v.filename} value={v.filename}>{v.display_name}</option>))}`

    2. Add `style={{ background: "#1a1c26", color: "#f0ebe0" }}` to BOTH `<option>` elements. Same object literal in both places; do NOT extract to a shared constant (not worth the indirection for a 2-line stylistic patch).

    3. Do NOT swap the native `<select>` to shadcn's `Select` component — the native picker sheet on iOS is deliberately preserved (comment L1091 rationale applies).

    4. No test changes needed (this is styling, not behavior — the existing test suite does not assert on option colors).

    After the edit, from `/home/ubuntu/skynet` run `npx tsc --noEmit` (must pass) and `npm run build:backend` (safety no-op). Skip test run for this task per constraints (styling-only). Then commit:
      `git add src/ui/features/pretty-view/IdentityModal.tsx`
      `git commit -m "fix(pv/identity-modal): voice dropdown option contrast (patch #278-equiv)"`
    Do NOT push, do NOT deploy.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx tsc --noEmit && npm run build:backend</automated>
  </verify>
  <done>
    - Both `<option>` elements inside the Voice `<select>` carry `style={{ background: "#1a1c26", color: "#f0ebe0" }}`.
    - Native `<select>` element unchanged (no shadcn swap).
    - `npx tsc --noEmit` passes; `npm run build:backend` passes.
    - Single commit created on branch `feat/tab-title-from-tmux` with subject line `fix(pv/identity-modal): voice dropdown option contrast (patch #278-equiv)`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: colorHue picker with GET-verify (patch #279-equiv)</name>
  <files>src/ui/features/pretty-view/IdentityModal.tsx, src/ui/features/pretty-view/IdentityModal.test.tsx</files>
  <behavior>
    - Test A (new smoke): Render modal, click pencil to reveal edit block, locate the hue range input by role="slider" or by id="identity-hue-input". Fire a change event setting value to "180". Assert the visible degree readout updates to "180°" and the swatch's inline `background: hsl(180, ...)` reflects the new value.
    - Test B (new smoke — save meta includes colorHue): Render modal, click pencil, drag hue to 180, mock `updateIdentity` to resolve with `{...BASE_IDENTITY, colorHue: 180}`, click Save, assert `updateIdentity` was called with `meta` containing `colorHue: 180`.
    - Test C (implicit): Hue picker input MUST NOT be present before pencil click (gated by editing state from Task 1).
  </behavior>
  <action>
    Add colorHue picker to `src/ui/features/pretty-view/IdentityModal.tsx` with GET-verify defense against the Skynet multipart-`data`-field silent-no-op trap.

    1. Verify type — open `src/ui/api/identities-api.ts` and confirm `IdentityInput` at L17-23 already includes `colorHue?: number | null` (it does — L22 shows `colorHue?: number | null;`). No type edit needed. If for any reason the field is absent when you read it, ADD `colorHue?: number | null;` to `IdentityInput` as part of this task.

    2. Add state near L188 (alongside `voiceDraft` / `committedVoice`):
       ```
       const [hueDraft, setHueDraft] = useState<number>(identity.colorHue ?? hue);
       const [committedHue, setCommittedHue] = useState<number>(identity.colorHue ?? hue);
       ```
       Rationale: fall back to the prop `hue` when `identity.colorHue` is null so the slider starts at a meaningful position rather than 0.

    3. Extend the reset effect at L382-395: after `setCommittedVoice(identity.voice ?? null);` add
       `setHueDraft(identity.colorHue ?? hue);` and `setCommittedHue(identity.colorHue ?? hue);`.
       Extend the dependency array to include `identity.colorHue` (add after `identity.voice`). Do NOT add `hue` to deps — it is a stable prop from the parent and adding it would over-trigger the reset.

    4. Insert the hue picker row inside the edit block AFTER the Voice picker's closing `</div>` (the `</div>` at ~L1137 that closes the mb-3 wrapper containing the voice select + sample button), and BEFORE the inline error block at L1140. Mirror the label + control pattern used by the other rows. Markup:
       ```
       <div className="mb-3">
         <label className="block text-xs text-[var(--color-pv-fg-muted)] mb-1" htmlFor="identity-hue-input">
           Color
         </label>
         <div style={{ display: "flex", gap: 8, alignItems: "center" }}>
           <input
             id="identity-hue-input"
             type="range"
             min={0}
             max={360}
             step={1}
             value={hueDraft}
             onChange={(e) => setHueDraft(Number(e.target.value))}
             disabled={saving}
             style={{
               flex: 1,
               height: 10,
               borderRadius: 5,
               appearance: "none",
               WebkitAppearance: "none",
               background: "linear-gradient(to right, hsl(0,70%,50%), hsl(60,70%,50%), hsl(120,70%,50%), hsl(180,70%,50%), hsl(240,70%,50%), hsl(300,70%,50%), hsl(360,70%,50%))",
               outline: "none",
             }}
           />
           <div style={{
             width: 24, height: 24, borderRadius: "50%",
             background: `hsl(${hueDraft}, 65%, 55%)`,
             border: "1px solid rgba(255,255,255,0.15)",
             boxShadow: "inset 0 1px 0 rgba(255,255,255,0.2)",
             flexShrink: 0,
           }} />
           <span className="text-xs text-[var(--color-pv-fg-muted)]" style={{ minWidth: 32, textAlign: "right" }}>
             {hueDraft}°
           </span>
         </div>
       </div>
       ```

    5. Extend the Save-disabled check at L1152-1155. Current condition:
       `disabled={saving || (titleDraft === committedTitle && avatarFile === null && (voiceDraft || null) === committedVoice)}`
       Extend the parenthesized "no changes" clause with `&& hueDraft === committedHue`.

    6. In `onSave` around L815-818 (right after the voice meta block that assigns `meta.voice`, before the `const updated = await updateIdentity(...)` call at L819), add:
       ```
       if (hueDraft !== committedHue) {
         meta.colorHue = hueDraft;
       }
       ```

    7. In `onSave` after the successful save around L831-834 (right after `setCommittedVoice(updated.voice ?? null);` and `setVoiceDraft(updated.voice ?? "");`), add:
       `setCommittedHue(updated.colorHue ?? hueDraft);`
       (Task 1 already added `setEditing(false)` further down — that stays.)

    8. In `onCancel` around L845-855, add `setHueDraft(committedHue);` after the voice revert (`setVoiceDraft(committedVoice ?? "");`). Task 1's `setEditing(false)` stays as the last line.

    9. GET-verify guard against the Skynet multipart-`data`-field silent-no-op trap (documented in tina.md). Insert between the `const updated = await updateIdentity(...)` call at L819 and the `applyIdentityChange(updated)` call at L820:
       ```
       // GET-verify: Skynet's multipart handler has been known to silently no-op
       // on the `data` field when middleware order gets misconfigured. Defensive
       // check: if we sent a colorHue change but the server echo doesn't reflect
       // it, surface an inline error instead of trusting the 200. Only guards
       // colorHue changes (title/voice already have their own draft-vs-echo
       // recovery paths via setCommittedTitle/setCommittedVoice re-syncing).
       if (meta.colorHue !== undefined && updated.colorHue !== meta.colorHue) {
         setSaveError(`Server did not persist colorHue (sent ${meta.colorHue}, got ${updated.colorHue ?? "null"})`);
         setSaving(false);
         return;
       }
       ```
       Placement note: this returns EARLY before `applyIdentityChange(updated)` fires, so a silent-no-op does not poison the identities-store with the mismatched value. The `saving` flag is manually cleared here because the early return skips the `finally` block's `setSaving(false)` — actually, review the try/catch/finally structure at L806-841: it does have a `finally { setSaving(false); }` at L838-840, so the manual `setSaving(false)` above is redundant and should be REMOVED. Rewrite the guard to:
       ```
       if (meta.colorHue !== undefined && updated.colorHue !== meta.colorHue) {
         setSaveError(`Server did not persist colorHue (sent ${meta.colorHue}, got ${updated.colorHue ?? "null"})`);
         return;
       }
       ```
       The `finally` block clears `saving` correctly on early return.

    10. Update `src/ui/features/pretty-view/IdentityModal.test.tsx` — add ONE new test (Test B from behavior above) after the existing 5. Use the existing `renderModal()` helper. Prelude with `fireEvent.click(screen.getByRole("button", { name: /edit identity/i }))` (Task 1 gate). Then:
        ```
        const hueInput = document.getElementById("identity-hue-input") as HTMLInputElement;
        expect(hueInput).toBeTruthy();
        fireEvent.change(hueInput, { target: { value: "180" } });
        expect(screen.getByText("180°")).toBeTruthy();

        mockedUpdateIdentity.mockResolvedValue({ ...BASE_IDENTITY, colorHue: 180 });
        fireEvent.click(screen.getByRole("button", { name: /Save/i }));
        await waitFor(() => { expect(mockedUpdateIdentity).toHaveBeenCalledTimes(1); });
        const [, meta] = mockedUpdateIdentity.mock.calls[0];
        expect(meta).toEqual({ colorHue: 180 });
        ```
        (Note: `BASE_IDENTITY.colorHue` is currently `null` in the test fixture at L114; the reset effect will therefore initialize `hueDraft` to the prop `hue=200` — the change to 180 IS a real diff from committed=200, so `meta.colorHue: 180` will be set. This matches the assertion above.)

    Do NOT skip the GET-verify guard — that's the load-bearing "defensive against the Skynet multipart-`data`-field silent-no-op trap" requirement per the task_scope.

    After the edit, from `/home/ubuntu/skynet` run `npx tsc --noEmit` (must pass), `npm run build:backend` (safety no-op), and `npm test -- IdentityModal` (must pass — all 5 existing tests still green + new hue smoke). Then commit:
      `git add src/ui/features/pretty-view/IdentityModal.tsx src/ui/features/pretty-view/IdentityModal.test.tsx`
      `git commit -m "feat(pv/identity-modal): colorHue picker with GET-verify (patch #279-equiv)"`
    Do NOT push, do NOT deploy, do NOT touch skynet-patches.md.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx tsc --noEmit && npm run build:backend && npm test -- IdentityModal</automated>
  </verify>
  <done>
    - `hueDraft` + `committedHue` state added; reset effect updates both and includes `identity.colorHue` in deps.
    - Hue picker row rendered inside edit block between Voice and inline error, with range slider + swatch + degree readout.
    - Save-disabled check extended with `&& hueDraft === committedHue`.
    - `onSave` includes `meta.colorHue = hueDraft` when changed; success path updates `committedHue`; GET-verify guard returns early with inline error on server echo mismatch.
    - `onCancel` reverts `hueDraft` to `committedHue`.
    - `IdentityModal.test.tsx`: new hue-picker smoke test added (test 6) asserting hueDraft updates + `meta.colorHue` reaches `updateIdentity`.
    - `IdentityInput.colorHue` type confirmed present (no type edit needed) OR added if absent.
    - `npx tsc --noEmit` passes; `npm run build:backend` passes; `npm test -- IdentityModal` passes.
    - Single commit created on branch `feat/tab-title-from-tmux` with subject line `feat(pv/identity-modal): colorHue picker with GET-verify (patch #279-equiv)`.
  </done>
</task>

</tasks>

<verification>
End-of-plan phase checks (executor runs after Task 3 commit):

1. `git -C /home/ubuntu/skynet log --oneline -5` — expect the 3 new commits sitting atop `974e99b docs(quick-260802-uow)` in this exact order:
   - Task 3 subject (most recent)
   - Task 2 subject
   - Task 1 subject
   - `974e99b docs(quick-260802-uow): composebox cluster (4 pinned bounties)`

2. `git -C /home/ubuntu/skynet status` — clean working tree (no unstaged edits).

3. `git -C /home/ubuntu/skynet rev-parse --abbrev-ref HEAD` — must still be `feat/tab-title-from-tmux` (no branch switch).

4. Final full-suite sanity: `cd /home/ubuntu/skynet && npx tsc --noEmit && npm test -- IdentityModal` both pass.

5. Grep sanity — verify no accidental leftovers:
   - `grep -c "editing" src/ui/features/pretty-view/IdentityModal.tsx` — non-zero.
   - `grep -c "hueDraft" src/ui/features/pretty-view/IdentityModal.tsx` — non-zero.
   - `grep "background: \"#1a1c26\"" src/ui/features/pretty-view/IdentityModal.tsx | wc -l` — expect 2 (the two option elements).
</verification>

<success_criteria>
- 3 atomic commits landed on `feat/tab-title-from-tmux` in the exact order Task 1 → Task 2 → Task 3.
- Each commit subject matches the spec:
  - `feat(pv/identity-modal): pencil-toggle-gated edit surface (patch #277-equiv)`
  - `fix(pv/identity-modal): voice dropdown option contrast (patch #278-equiv)`
  - `feat(pv/identity-modal): colorHue picker with GET-verify (patch #279-equiv)`
- Only 2 files touched across all 3 commits: `src/ui/features/pretty-view/IdentityModal.tsx` and `src/ui/features/pretty-view/IdentityModal.test.tsx`.
- `npx tsc --noEmit` passes after each commit.
- `npm test -- IdentityModal` passes after Task 1 and Task 3 (Task 2 is styling-only, no test change).
- `npm run build:backend` runs cleanly after each task (no-op safety check).
- No worktrees created. No pushes. No deploys. No edits to `skynet-patches.md`.
- Branch remains `feat/tab-title-from-tmux` throughout.
</success_criteria>

<output>
Create `.planning/quick/260802-vg6-identity-modal-pair-pencil-toggle-gated-/260802-vg6-SUMMARY.md` when done, using the template referenced in `<execution_context>`. Summarize:
- 3 commit SHAs + subject lines
- Any test failures encountered + how they were resolved
- Confirmation the GET-verify guard is in place (defensive against multipart-`data`-field silent no-op)
- Confirmation `IdentityInput.colorHue` did not need adding (already present at L22 of identities-api.ts)
</output>
