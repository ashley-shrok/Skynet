---
phase: 260803-7vf-reset-while-recording
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/ComposeBox.tsx
  - src/ui/features/pretty-view/ComposeBox.voice.test.tsx
autonomous: true
requirements:
  - RESET-VOICE-01
must_haves:
  truths:
    - "Clicking the meter-well Reset button while voice.state === 'recording' fires the reset UI effects synchronously (onResetClicked, drain-sweep, cancel primary armed source) AND, after STT resolves, dispatches `/id reset (glued)` where glued = applyGlue(currentText, transcript)."
    - "If voice.endSend returns null (STT failed), reset STILL fires — falls back to dispatching `/id reset (existing textarea body)` (or plain `/id reset` if body is empty). Never a silent no-op."
    - "During voice.state === 'transcribing', the meter-well Reset button is disabled (matches send button's transcribing-disable pattern, prevents double-fire)."
    - "Idle-state Reset behavior is byte-identical to before (regression guard): text=empty → `/id reset`, text=non-empty → `/id reset (body)`, drain-sweep timers fire, primary armed source cancels, onResetClicked fires."
    - "No visual affordance change on the Reset button during recording — only behavior differs."
  artifacts:
    - path: "src/ui/features/pretty-view/ComposeBox.tsx"
      provides: "fireResetSyncFx + dispatchResetPayload helpers, handleVoiceResetSend async, handleResetClick router, updated reset button disabled+onClick wiring"
      contains: "handleVoiceResetSend"
    - path: "src/ui/features/pretty-view/ComposeBox.voice.test.tsx"
      provides: "Three new test cases: reset-while-recording glues+dispatches, STT-error fallback dispatches with existing body, transcribing disables reset button"
      contains: "reset-while-recording"
  key_links:
    - from: "src/ui/features/pretty-view/ComposeBox.tsx (reset button onClick, ~line 1560)"
      to: "handleVoiceResetSend() → voice.endSend() → dispatchResetPayload()"
      via: "handleResetClick router branching on voice.state === 'recording'"
      pattern: "voice\\.state === \"recording\""
    - from: "handleVoiceResetSend"
      to: "voice.endSend + applyGlue (already exposed by useVoiceRecording.ts, NO hook changes)"
      via: "await voice.endSend(baseText); result.glued fed to dispatchResetPayload"
      pattern: "voice\\.endSend"
---

<objective>
Wire the meter-well Reset button in ComposeBox.tsx so that clicking it while
`voice.state === "recording"` combines send-while-recording behavior (stop
recording, transcribe, glue transcript onto existing textarea body) with
reset behavior (drain-sweep animation, cancel primary armed source, dispatch
`/id reset (…)` payload, clear textarea on success). One click, one motion,
one dispatch.

Purpose: Ashley's verbatim ask (2026-08-03) — "if you are recording and you
hit the reset button, then it essentially combines the functionality of the
send button when you're recording and the functionality of the reset button."
Bounty pinned: `reset-button-while-recording-combines-send-and-reset`.

Output: One-file behavioral change in ComposeBox.tsx + three additive test
cases in ComposeBox.voice.test.tsx. No hook changes. No RecordingControls
changes. No new UI affordance. All frontend — `tsc --noEmit` is sufficient.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/ui/features/pretty-view/ComposeBox.tsx
@src/ui/features/pretty-view/useVoiceRecording.ts
@src/ui/features/pretty-view/ComposeBox.voice.test.tsx

Key facts extracted from the target files (executor: use these; do not re-derive):

**ComposeBox.tsx locations:**
- `handleResetSend` — lines 1224-1275 (the function to extract from)
- Reset button JSX — lines 1558-1587; onClick at 1560 (`onClick={handleResetSend}`),
  disabled at 1561 (`disabled={canSend === false || asideActive === true || recycleActive === true}`)
- `handleVoiceSend` (the send-while-recording reference implementation) — lines 1159-1208
- `collapseNewlinesForSend` — line 1029
- `clearAfterSend` — line 816 (already imported by handleResetSend)
- `isSourceArmed` / `cancelSourceArmed` — lines 407 / 425
- Drain-sweep timer refs — `drainEndTimerRef` (459), `pulseOnTimerRef` (460),
  `pulseOffTimerRef` (461), `clearDrainTimers` (462)
- `onResetClicked` prop — declared at 125, destructured at 287

**useVoiceRecording.ts contract (NO changes here):**
- `state: "idle" | "recording" | "transcribing"`
- `endSend(currentText: string): Promise<{transcript, glued} | null>` — line 298
- `applyGlue(currentText, transcript)` — internal, line 193; returns `currentText +
  (space if no trailing whitespace) + transcript`. Result already glued inside `endSend`.
- Returns `null` when STT fails; sets `errorMessage` internally.

**ComposeBox.voice.test.tsx patterns to follow:**
- Test 8 (line 291) is the canonical send-while-recording test — mirror its
  structure exactly. `render(<ComposeBox {...baseProps({ onSend })} />)`,
  click mic → wait for `"Send transcript"` → emit blob → click send-transcript
  → `waitFor(() => expect(onSend).toHaveBeenCalledWith(...))`.
- Test 9 (line 323) shows the STT-fail fetch mock (`ok: false, status: 500`).
- Test 11 (line 385) shows how to freeze the transcribing state
  (`vi.stubGlobal("fetch", vi.fn(() => new Promise(() => {})))`).
- Reset button query: `screen.getByRole("button", { name: "Reset context window" })`
  (from aria-label at line 1562).
- Default fetch mock returns `{ text: "hello world" }` — override per-test as needed.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor handleResetSend into fireResetSyncFx + dispatchResetPayload, add handleVoiceResetSend, add handleResetClick router, update reset-button disabled + onClick</name>
  <files>src/ui/features/pretty-view/ComposeBox.tsx</files>
  <behavior>
    - Idle-state Reset regression: text = "" → onSend("/id reset"); text = "hello" → onSend("/id reset (hello)"); drain-sweep timers fire; primary armed source cancels; onResetClicked fires synchronously; errorMessage cleared. Byte-identical to current handleResetSend behavior.
    - Recording-state Reset (new): voice.state === "recording", text = "hi there", transcript = "and one more thing" → click reset → onResetClicked fires SYNC on click; drain-sweep + cancel-armed + clearError fire SYNC; then after endSend resolves, onSend("/id reset (hi there and one more thing)") dispatched; setText("") on success.
    - STT-fail fallback: voice.state === "recording", text = "existing body", endSend returns null → onResetClicked + drain-sweep still fire SYNC; then onSend("/id reset (existing body)") dispatched (NOT plain "/id reset", NOT silent no-op). If text was empty, onSend("/id reset").
    - Transcribing-state gate: voice.state === "transcribing" → reset button `disabled` attribute is true (existing disabled expression OR voice.state === "transcribing").
    - No new UI affordance on the reset button during recording. Style / label / icon unchanged.
  </behavior>
  <action>
Refactor ComposeBox.tsx handleResetSend into two named helper functions and add
the recording-state branch. All changes are in ComposeBox.tsx — no hook
edits, no RecordingControls edits, no new imports (unless the executor
determines useCallback is needed for referential stability; prefer plain
`function` declarations to match the existing surrounding code style).

Step A — Extract fireResetSyncFx() from handleResetSend (currently 1224-1275):
Move the synchronous UI-effect block (lines 1225-1262 — everything from the
`onResetClicked?.()` call through the drain-end setTimeout assignment,
inclusive of the isSourceArmed/cancelSourceArmed block and
`setErrorMessage(null)`) into a new function `function fireResetSyncFx()`.
This function returns void and has no arguments. Place it directly above the
refactored handleResetSend.

Step B — Extract dispatchResetPayload(body: string) from handleResetSend:
Move the payload construction + dispatch tail (lines 1264-1274) into a new
function `function dispatchResetPayload(body: string)`. Parameter `body`
replaces the closed-over `text` variable. Body's `.trim()` and
`collapseNewlinesForSend` calls are identical to current handleResetSend.
Place directly below fireResetSyncFx. This function is void; on success it
calls setText("") + clearAfterSend(); on failure it calls setErrorMessage
with the existing "Not connected — try again in a moment" string.

Step C — Refactor handleResetSend to compose the two helpers:
The remaining `handleResetSend` body becomes literally two calls:
`fireResetSyncFx(); dispatchResetPayload(text);`. Preserve the existing
comments explaining patch #122, Vehicle C v2, patch #83 by relocating them
into fireResetSyncFx so provenance is not lost (executor's judgment on
comment placement — the exact prose stays intact, only the enclosing function
changes).

Step D — Add handleVoiceResetSend() async function, placed near
handleVoiceSend (~line 1208). Signature: `async function handleVoiceResetSend()`.
Body:
  1. Call `fireResetSyncFx()` synchronously first — this is the load-bearing
     latency guarantee (patch #122): SessionHoldingOverlay must pop on click,
     not after the ~1-3s STT round-trip.
  2. Capture `const baseText = text;` before awaiting (avoid stale-closure
     surprises if text mutates during the await).
  3. `const result = await voice.endSend(baseText);`
  4. `const body = result ? result.glued : baseText;` — STT-fail fallback per
     locked design decision #4: reset ALWAYS dispatches. If endSend returned
     null (STT failure), we fall back to the existing textarea body so Ashley
     still gets a reset with contextual hint attached. voice.errorMessage is
     set by the hook and surfaces in the composebox error area separately —
     this handler does NOT setErrorMessage on STT failure (endSend already
     did).
  5. `dispatchResetPayload(body);`

Step E — Wire the reset button (ComposeBox.tsx:1558-1587). Add a small router
function `function handleResetClick()` placed near handleResetSend that
branches on voice.state:
    if (voice.state === "recording") { void handleVoiceResetSend(); }
    else { handleResetSend(); }
Update line 1560 from `onClick={handleResetSend}` to
`onClick={handleResetClick}`.

Step F — Update the reset button's `disabled` prop at line 1561. Append
`|| voice.state === "transcribing"` to the existing check so it becomes:
`disabled={canSend === false || asideActive === true || recycleActive === true || voice.state === "transcribing"}`.
This is the transcribing-state gate per locked design decision #5.

Load-bearing constraints:
  - DO NOT change RecordingControls.tsx or useVoiceRecording.ts.
  - DO NOT add a new import for useCallback unless referential stability
    matters for a downstream memo dependency (spot-check the existing code —
    handleResetSend is a plain function, keep the same style).
  - DO NOT change reset button className / aria-label / title / icon. Purely
    behavioral change per locked design decision #1.
  - The synchronous UI effects (fireResetSyncFx) MUST run BEFORE the await in
    handleVoiceResetSend — do not await anything before firing them. This is
    the patch #122 UX guarantee.
  - Reset ALWAYS dispatches when the user pressed reset. Never silent no-op
    on STT failure. Fall back to the existing textarea body (locked design
    decision #4).
  - No `docker build`, no push. Code motion only.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
    - fireResetSyncFx and dispatchResetPayload extracted from handleResetSend; handleResetSend body reduced to two helper calls.
    - handleVoiceResetSend async function present, calls fireResetSyncFx synchronously before awaiting voice.endSend, falls back to baseText on null result, always calls dispatchResetPayload.
    - handleResetClick router present, branches on voice.state === "recording".
    - Reset button onClick wired to handleResetClick (was handleResetSend).
    - Reset button disabled prop includes `|| voice.state === "transcribing"`.
    - `npx tsc --noEmit` exits 0 (no new type errors introduced).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add three test cases to ComposeBox.voice.test.tsx covering reset-while-recording glue+dispatch, STT-error fallback, and transcribing-state disable</name>
  <files>src/ui/features/pretty-view/ComposeBox.voice.test.tsx</files>
  <behavior>
    - Test A (glue+dispatch): text="hi there", record, transcript="and one more thing" (fetch returns {text: "and one more thing"}), click reset button → onSend called with "/id reset (hi there and one more thing)", onResetClicked called synchronously on click (before await resolves), textarea cleared after dispatch.
    - Test B (STT-error fallback): text="existing body", record, fetch returns 500, click reset button → onSend called with "/id reset (existing body)" (NOT plain "/id reset", NOT no-op), onResetClicked called synchronously.
    - Test C (transcribing disables reset): mic → record → click "Send transcript" with frozen fetch (never-resolving Promise) → voice.state stays "transcribing" → reset button (aria-label "Reset context window") has `disabled` attribute true.
  </behavior>
  <action>
Add three new `it(...)` blocks at the end of the existing
`describe("ComposeBox — Phase 16 voice flow", ...)` block in
ComposeBox.voice.test.tsx (after Test 12 at line 445, before the closing
`});` at line 446).

Follow the exact mocking + setup patterns already in the file:
  - baseProps + onSend mock — see Test 8 (line 291) as the canonical template
  - MockMediaRecorder.instances[0].emitData for the blob push — see Test 8
    (line 305)
  - Reset button query: `screen.getByRole("button", { name: "Reset context window" })`
    (from aria-label at ComposeBox.tsx:1562)
  - onResetClicked prop: pass as `onResetClicked: vi.fn()` in baseProps
    overrides, then assert `onResetClicked` mock was called

Test A — "reset-while-recording glues transcript onto textarea body and
dispatches `/id reset (glued)`":
  1. Set fetch mock to return `{ text: "and one more thing" }` (default
     fetch mock returns "hello world" — override for this test).
  2. Render with `onSend` and `onResetClicked` mocks in baseProps.
  3. Type "hi there" into the textarea (fireEvent.change).
  4. Click the mic button; wait for "Send transcript" to appear (uses same
     RecordingControls wait pattern as Test 8) — this confirms we're in
     recording state.
  5. Push a data chunk via MockMediaRecorder.instances[0].emitData.
  6. Click the reset button (`screen.getByRole("button", { name: "Reset context window" })`).
  7. Assert `onResetClicked` was called (this proves the sync fire before
     await — no waitFor needed for this assertion; it fires synchronously on
     click).
  8. `waitFor` that `onSend` was called with `"/id reset (hi there and one more thing)"`.
  9. `waitFor` that the textarea value is `""` (cleared after success).

Test B — "reset-while-recording with STT failure falls back to dispatch with
existing textarea body":
  1. Override fetch mock with `{ ok: false, status: 500, json: () =>
     Promise.resolve({ error: "..." }) }` (mirrors Test 9 at line 325).
  2. Render with onSend + onResetClicked mocks.
  3. Type "existing body" into textarea.
  4. Click mic → wait for "Send transcript".
  5. Push blob.
  6. Click reset button.
  7. Assert onResetClicked was called synchronously.
  8. `waitFor` that onSend was called with `"/id reset (existing body)"` —
     the KEY assertion: NOT `"/id reset"` alone, NOT a no-op. Reset always
     fires with the existing textarea body when STT fails.

Test C — "transcribing state disables the reset button":
  1. Freeze fetch with `vi.stubGlobal("fetch", vi.fn(() => new Promise(() => {})))`
     (mirrors Test 11 at line 391).
  2. Render.
  3. Click mic → wait for "Send transcript".
  4. Push blob.
  5. Click "Send transcript" to enter transcribing state.
  6. `waitFor` that the reset button (`screen.getByRole("button", { name: "Reset context window" })`)
     has `disabled === true` as an `HTMLButtonElement` — mirror Test 11's
     `(btn as HTMLButtonElement).disabled` assertion pattern.

Load-bearing constraints:
  - Do NOT modify existing Tests 1-12. Add new tests at the end.
  - Do NOT change vi.mock or beforeEach/afterEach setup.
  - Test names should start with clear identifiers ("Test 13:", "Test 14:",
    "Test 15:") to match the existing file's convention.
  - No `docker build`, no push. Code + tests only.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx vitest run src/ui/features/pretty-view/ComposeBox.voice.test.tsx 2>&1 | tail -50</automated>
  </verify>
  <done>
    - Three new tests appended to ComposeBox.voice.test.tsx after Test 12.
    - Test A asserts onSend called with `"/id reset (hi there and one more thing)"` and onResetClicked fired synchronously.
    - Test B asserts onSend called with `"/id reset (existing body)"` on STT-fail (NOT `/id reset` alone, NOT no-op).
    - Test C asserts reset button `disabled === true` during transcribing state.
    - All 15 tests (12 existing + 3 new) pass in `npx vitest run src/ui/features/pretty-view/ComposeBox.voice.test.tsx`.
  </done>
</task>

</tasks>

<verification>
Overall verification for the plan:

1. **Type check** — `npx tsc --noEmit` from repo root exits 0. No new type
   errors introduced.

2. **Test suite** — `npx vitest run src/ui/features/pretty-view/ComposeBox.voice.test.tsx`
   passes 15/15 tests (12 existing + 3 new). Existing tests must still pass
   (regression guard for handleResetSend refactor).

3. **File scope guard** — Only two files touched: ComposeBox.tsx and
   ComposeBox.voice.test.tsx. Confirm with `git diff --name-only` before
   commit — MUST NOT include useVoiceRecording.ts, RecordingControls.tsx, or
   any other file.

4. **Behavioral spot-checks** (via reading, not runtime — deployment is held
   for Ashley's greenlight):
   - Grep for `handleResetClick` in ComposeBox.tsx — should appear as onClick
     handler on the reset button at ~line 1560.
   - Grep for `voice.state === "transcribing"` in the reset button's disabled
     prop at ~line 1561.
   - Grep for `handleVoiceResetSend` — should be defined and referenced from
     handleResetClick.
   - Grep for `fireResetSyncFx()` — should appear inside both handleResetSend
     and handleVoiceResetSend.
</verification>

<success_criteria>
- Reset-while-recording combines send-while-recording's transcript-glue behavior with reset's dispatch — one click, one motion.
- Idle-state reset behavior is byte-identical to before (regression-free refactor).
- STT failure never results in a silent no-op — reset always fires with the existing textarea body as fallback.
- Transcribing state disables the reset button to prevent double-fire.
- No UI affordance change on the reset button — behavior only, per Ashley's lock.
- `npx tsc --noEmit` clean.
- `npx vitest run src/ui/features/pretty-view/ComposeBox.voice.test.tsx` — 15/15 pass.
- Only ComposeBox.tsx and ComposeBox.voice.test.tsx modified.
- Atomic conventional commit created (`feat(compose):` or `fix(compose):` per existing repo convention — executor chooses). DO NOT push. DO NOT `docker build`. Code motion only until Ashley greenlights ship.
</success_criteria>

<output>
Create `.planning/quick/260803-7vf-reset-while-recording-combines-send-and-/260803-7vf-SUMMARY.md` when done.
Summary must record: files touched, test count before/after, tsc result, and a note that deployment is HELD pending Ashley greenlight.
</output>
