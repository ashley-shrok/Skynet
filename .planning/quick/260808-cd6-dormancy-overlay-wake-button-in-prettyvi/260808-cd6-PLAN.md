---
phase: quick-260808-cd6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/claude-session/claude-session-server.ts
  - src/ui/api/claude-session-api.ts
  - src/ui/features/pretty-view/DormancyOverlay.tsx
  - src/ui/features/pretty-view/DormancyOverlay.test.tsx
  - src/ui/features/pretty-view/ComposeBox.tsx
  - src/ui/features/pretty-view/ComposeBox.dormant-disable.test.tsx
  - src/ui/features/pretty-view/PrettyView.tsx
autonomous: false
requirements:
  - BOUNTY-dormancy-overlay-and-wake-button
user_setup: []
tags:
  - dormancy
  - pretty-view
  - overlay
  - wake
  - websocket
  - ios-pwa

must_haves:
  truths:
    - "When a pane's identity is dormant (~/.claude/identities/<name>/.dormant exists on the target host), a full-surface DormancyOverlay covers the chat-region of PrettyView with 'session is asleep' copy and a Wake button."
    - "While the DormancyOverlay is visible, ComposeBox Send/reset/ThumbsUp/Recap/queued-row-Send are disabled; textarea, mic, and paperclip remain live so Ashley can pre-draft."
    - "Tapping the Wake button sends {type:'wake'} to the backend, which SSH exec's `rm -f ~/.claude/identities/<name>/.dormant` on the target host and returns success/failure; overlay transitions to 'waking…' state."
    - "'waking…' state shows an elapsed-seconds hint after 15s (e.g. 'this can take up to 60s') so it does not feel hung during the supervisor's ≤30s CHECK_INTERVAL + ~30s claude-launch."
    - "Overlay auto-dismisses when the Claude session WS starts sending live JSONL frames again (natural signal that recover-path relaunched + /id ran)."
    - "Backend piggybacks a `stat ~/.claude/identities/<name>/.dormant` check on the existing 3s per-pane status poll (mirrors contextPctInFlight guard pattern with a new dormantInFlight guard); emits `{type:'dormant', dormant:true|false}` ONLY on state change."
    - "Dormant check is gated on identity-shaped tmux sessions only: one-time `stat ~/.claude/identities/<name>/` per pane on first attach, cached per connection; non-identity panes skip the check entirely."
    - "Only VISIBLE panes are polled — dovetails with patch #344 iteration 1 (hidden panes have their claude-session WS closed, so poll never fires; naturally satisfies the constraint without extra gating)."
    - "agent-supervisor is UNTOUCHED — sentinel-delete is 'path 3' (FREE wake) per its own 2026-08-07 design."
    - "Full `npm test` suite green (baseline 1556 pass / 6 skip after patch #344)."
    - "Ship via `docker compose up -d --force-recreate skynet` under 15-min deadman + HTTPS 200 check."
    - "New entry appended to /home/ubuntu/.claude/roles/box-maintainer/skynet-patches.md as patch #345."
  artifacts:
    - path: "src/ui/features/pretty-view/DormancyOverlay.tsx"
      provides: "Full-surface dormancy overlay component (mirrors SessionHoldingOverlay pattern)"
      exports: ["DormancyOverlay"]
      min_lines: 80
    - path: "src/ui/features/pretty-view/DormancyOverlay.test.tsx"
      provides: "Unit tests for DormancyOverlay (asleep state, waking state, elapsed-hint after 15s, Wake button click)"
      min_lines: 60
    - path: "src/ui/features/pretty-view/ComposeBox.dormant-disable.test.tsx"
      provides: "Truth-table tests parallel to reconnecting-disable / recycle-disable / plan-pending-disable (~7 tests)"
      min_lines: 150
    - path: "src/backend/claude-session/claude-session-server.ts"
      provides: "Dormant stat check on 3s poll + wake message handler + dormant WS message type"
      contains: "dormantInFlight"
    - path: "src/ui/api/claude-session-api.ts"
      provides: "DormantEvent type in ClaudeSessionServerEvent union"
      contains: "DormantEvent"
    - path: "src/ui/features/pretty-view/ComposeBox.tsx"
      provides: "dormantActive prop wired at all ~15 disable sites (mirroring recycleActive/planPendingActive/reconnectingActive)"
      contains: "dormantActive"
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "Dormant WS message consumer, DormancyOverlay mount, Wake handler, live-frame auto-dismiss, dormantActive prop wiring"
      contains: "DormancyOverlay"
  key_links:
    - from: "backend 3s poll (claude-session-server.ts ~line 3442)"
      to: "stat ~/.claude/identities/<tmuxSession>/.dormant on target host"
      via: "execCommand(sshConn, `stat -c %n ~/.claude/identities/<name>/.dormant 2>/dev/null`)"
      pattern: "dormantInFlight"
    - from: "backend emit"
      to: "frontend PrettyView WS handler"
      via: "ws.send({type:'dormant', dormant:boolean}) — emit-only-on-change"
      pattern: "type: \"dormant\""
    - from: "PrettyView dormant state"
      to: "DormancyOverlay mount"
      via: "{dormant && <DormancyOverlay onWake={handleWake} elapsedSeconds={...} waking={...} />}"
      pattern: "DormancyOverlay"
    - from: "PrettyView dormant state"
      to: "ComposeBox dormantActive prop"
      via: "dormantActive={dormant}"
      pattern: "dormantActive"
    - from: "DormancyOverlay Wake button click"
      to: "backend rm -f sentinel"
      via: "ws.send({type:'wake'}) → backend execCommand rm -f"
      pattern: "type: \"wake\""
    - from: "any incoming Claude JSONL frame (message/image/relay_*)"
      to: "auto-dismiss dormant state (setDormant(false))"
      via: "PrettyView WS onmessage handler flips dormant off if it was true when a live frame lands"
      pattern: "setDormant\\(false\\)"
---

<objective>
Ship the Skynet-side integration for the agent-supervisor's 2026-08-07 DORMANCY mode: a per-pane DormancyOverlay with a Wake button in PrettyView, ComposeBox disable during dormant/waking, and a backend poll+wake WS surface. Enables Ashley to wake dormant identities from the PWA (the "external actor" path 3 that the supervisor calls "FREE").

Purpose: Once an identity's tmux pane goes dormant, Ashley currently cannot talk to it from her PWA — matrix_peek and schedule_peek are automatic, but there is no user-triggered wake affordance. This bounty adds it.
Output: Patch #345 shipped under 15-min deadman with full test suite green; skynet-patches.md updated; Ashley UAT ready.

WHY ONE PLAN, NOT SPLIT: The bounty is a tight vertical slice — backend WS pair (dormant emit + wake handler) + frontend overlay + ComposeBox prop wiring — with a single ship gate at the end. The pieces are cheap individually but their contracts are load-bearing across each other (the WS message shape locks the frontend consumer; the ComposeBox prop shape mirrors 3 existing patterns verbatim). Splitting would fragment the atomic ship. Sized as 5 tasks × ~10-15% context = ~65% total, within the ~50% target with margin for the two heavier tasks (task 3 DormancyOverlay + task 5 PrettyView wiring).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@$HOME/.claude/roles/box-maintainer/bounties/dormancy-overlay-and-wake-button/bounty.json

# Templates the new components MUST mirror
@src/ui/features/pretty-view/SessionHoldingOverlay.tsx
@src/ui/features/pretty-view/ComposeBox.reconnecting-disable.test.tsx

# Sites of change (read to locate exact insert points; do NOT re-read after editing)
@src/backend/claude-session/claude-session-server.ts
@src/ui/api/claude-session-api.ts
@src/ui/features/pretty-view/ComposeBox.tsx
@src/ui/features/pretty-view/PrettyView.tsx

# Contract-adjacent (read once for signatures — DO NOT modify)
@src/ui/state/session-recycling-store.ts

# Ship recipe reference — patches #344 and #339 are the freshest templates
@$HOME/.claude/roles/box-maintainer/skynet-patches.md
</context>

<interface_contracts>
Extracted so executor does not scavenger-hunt:

**Backend WS message shapes:**
- Server → client (new): `{type: "dormant", dormant: boolean}` — emit-only-on-change, mirrors `harness_tasks` / `backgrounded_agents` discipline
- Client → server (new): `{type: "wake"}` — no payload; backend uses connection-scoped `currentTmuxSession` for identity name (SAME rule as aside_arm / raw_keystrokes, per T-14-02-01 mitigation — never trust hostId/tmuxSession from client for send-keys routing)
- Server → client (new, optional but recommended): `{type: "wake_result", ok: boolean, error?: string}` — response to the wake message; overlay uses this to know the sentinel was actually deleted vs. the wake failed on the SSH side

**Backend guards to add near existing contextPctInFlight (claude-session-server.ts:982):**
- `let dormantInFlight = false;`
- `let dormantLastEmitted: boolean | null = null;` (change-only emit guard, mirrors `planPendingLastSerialized = "null"` at :1055)
- `let isIdentityShapedCached: boolean | null = null;` (per-connection cache; null = "not yet probed", true = "identity-shaped, poll dormancy", false = "not identity-shaped, skip dormancy forever on this connection")
- `let identityShapeProbeInFlight = false;` (guard for the one-shot probe)

**Where the dormant stat runs:** inside the existing `contextPctTimer` `setInterval` async IIFE at claude-session-server.ts:3442-3658, added AFTER the plan_pending block finishes and BEFORE the `finally { contextPctInFlight = false; }`. Uses the same `connSnapshot`, same `currentTmuxSession` captured at the same point, same 3s cadence — zero new timers, zero new SSH connections.

**Identity-shape probe command:** `test -d ~/.claude/identities/'<escapedName>' && echo yes || echo no` — single-quote wrap the name (tmuxSession is already validated to a tmux-safe subset per the escape rule at claude-session-server.ts:3438-3441). Fires once per connection on first poll where `isIdentityShapedCached === null && !identityShapeProbeInFlight`. On any error → cache false (safe default: skip dormancy check forever on this connection; a subsequent reconnect re-probes).

**Dormant stat command:** `stat ~/.claude/identities/'<escapedName>'/.dormant 2>/dev/null >/dev/null && echo yes || echo no` — runs ONLY when `isIdentityShapedCached === true`.

**Wake command:** `rm -f ~/.claude/identities/'<escapedName>'/.dormant` — single execCommand, uses the same `sshConn` the poll uses. Do NOT try to fast-poke the supervisor; the supervisor's next CHECK_INTERVAL tick (30s default) picks it up naturally. Emit `{type:"wake_result", ok:true}` on exec success or `{type:"wake_result", ok:false, error:message}` on failure.

**Frontend event union addition (src/ui/api/claude-session-api.ts):**
```
export type DormantEvent = { type: "dormant"; dormant: boolean };
export type WakeResultEvent = { type: "wake_result"; ok: boolean; error?: string };
```
Both appended to `ClaudeSessionServerEvent` union (line 214-249).

**ComposeBox prop:**
```
// New optional prop, mirrors reconnectingActive verbatim in shape + doc pattern.
dormantActive?: boolean;
```
Wire at the EXACT SAME sites as `reconnectingActive` — grep for `reconnectingActive` in ComposeBox.tsx returns ~15 sites (interface declaration, destructure, gate predicates, disabled attrs, QueuedRow prop declaration+destructure+use). Every `|| reconnectingActive === true` OR `!reconnectingActive &&` predicate ALSO OR-in `|| dormantActive === true` / `&& !dormantActive`.

**DormancyOverlay component signature:**
```
interface DormancyOverlayProps {
  waking: boolean;              // false = "session is asleep" + Wake button; true = "waking…" spinner-free static
  elapsedSeconds: number;       // 0 until Wake clicked; PrettyView useEffect ticks this while waking=true
  onWake: () => void;           // Wake button click handler; disabled when waking=true
  error?: string | null;        // optional wake_result error message; renders warm-red variant like SessionHoldingOverlay's error prop
}
export function DormancyOverlay(props: DormancyOverlayProps): JSX.Element
```
Mirrors SessionHoldingOverlay verbatim for scrim + card + geometry. STATIC glyph (Moon or ZzZ from lucide — pick Moon; no `animate-spin` per SessionHoldingOverlay motion-channel guardrail). Elapsed-seconds hint (`this can take up to 60s`) renders ONLY when `waking && elapsedSeconds >= 15`.

**PrettyView wiring plan:**
1. Add `const [dormant, setDormant] = useState(false)` + `const [waking, setWaking] = useState(false)` + `const [wakingStartTs, setWakingStartTs] = useState<number | null>(null)` + `const [wakeError, setWakeError] = useState<string | null>(null)` near the other per-session state (after showOverlay at line 309).
2. Add `case "dormant":` to the WS onmessage switch (after `case "context_pct"` at line 722): on `parsed.dormant === true` → `setDormant(true); setWaking(false); setWakingStartTs(null); setWakeError(null);`. On `parsed.dormant === false` → `setDormant(false); setWaking(false); setWakingStartTs(null);` (natural resume path — supervisor path 1/2 auto-wake or race with our own wake).
3. Add `case "wake_result":` to switch: on `parsed.ok === true` do nothing (wait for live-frame auto-dismiss); on `parsed.ok === false` → `setWaking(false); setWakingStartTs(null); setWakeError(parsed.error ?? "wake failed")`.
4. In the SAME onmessage switch, at the TOP of the handler (right after `wsFramesRef.current += 1`), add an auto-dismiss hook: `if (dormant && (parsed.type === "message" || parsed.type === "image" || parsed.type === "relay_inbound" || parsed.type === "relay_outbound" || parsed.type === "context_pct" || parsed.type === "harness_tasks")) { setDormant(false); setWaking(false); setWakingStartTs(null); setWakeError(null); }` — live-frame signal that recover-path relaunched + /id ran. IMPORTANT: use a ref (`dormantRef`) instead of the `dormant` state to avoid stale-closure inside the WS onmessage — mirror the `isVisibleRef` pattern at line 518.
5. Add elapsed-seconds ticker: `useEffect` keyed on `[waking, wakingStartTs]`; when `waking && wakingStartTs !== null`, setInterval every 1000ms computing `Math.floor((Date.now() - wakingStartTs) / 1000)` and storing in a `const [elapsedSeconds, setElapsedSeconds] = useState(0)`; cleanup clears interval; reset elapsedSeconds to 0 when waking flips off.
6. Add `handleWake` callback: `wsRef.current?.send(JSON.stringify({type: "wake"}))` guarded on `wsRef.current?.readyState === WebSocket.OPEN`; `setWaking(true); setWakingStartTs(Date.now()); setWakeError(null);`.
7. Mount overlay in the chat-region wrapper (same insert point as SessionHoldingOverlay at line 1450): `{dormant && <DormancyOverlay waking={waking} elapsedSeconds={elapsedSeconds} onWake={handleWake} error={wakeError} />}` — CO-EXISTS with SessionHoldingOverlay (they cannot both be up at once by construction; dormant-only-when-alive is a supervisor invariant, but the mounts are independent so no gate needed).
8. Wire `dormantActive={dormant || waking}` prop on the ComposeBox (line ~1654 near reconnectingActive) — both dormant and waking states disable the compose box; only live-frame auto-dismiss re-enables it.

**Frontend gating note (visibility):** Backend has NO isVisible signal yet — patch #344 handles it by closing the WS on hidden panes. So the "visible-only poll" constraint is naturally satisfied: hidden pane = WS closed = no poll fires = no dormant emit. No frontend gating needed on the emit consumption. Add a one-line comment near the case "dormant" handler documenting this ("hidden-pane suppression is inherited from patch #344's WS-pause on !isVisible; no additional gate needed here").
</interface_contracts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DormantEvent + WakeResultEvent to ClaudeSessionServerEvent union</name>
  <files>src/ui/api/claude-session-api.ts</files>
  <behavior>
    - After the ErrorEvent block (line 208-212) and before the `export type ClaudeSessionServerEvent =` union (line 214), add:
      - `export type DormantEvent = { type: "dormant"; dormant: boolean };`
      - `export type WakeResultEvent = { type: "wake_result"; ok: boolean; error?: string };`
    - Add both `| DormantEvent` and `| WakeResultEvent` to the ClaudeSessionServerEvent union (after `| ErrorEvent` or in the same-position appropriate slot before the identity events group).
    - No test file needed for pure type declarations — the type-check is provided by task 2 (backend emit) and task 5 (frontend consume) failing at compile time if the union is wrong. Task 2's tsc --noEmit gate proves the type is exported and imported correctly.
  </behavior>
  <action>
    Open src/ui/api/claude-session-api.ts. Insert DormantEvent + WakeResultEvent type exports directly after ErrorEvent (line 212). Extend the ClaudeSessionServerEvent discriminated union to include both new variants. Do NOT reorder existing entries; append inside the union in the same visual group as the other WS server-emit events (before the identity events group). Comment each with a one-line docblock referencing "quick 260808-cd6 — dormancy overlay + wake button".
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>tsc --noEmit exits 0; grep -c "DormantEvent\|WakeResultEvent" src/ui/api/claude-session-api.ts returns >=4 (two type declarations + two union members).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backend — dormant stat check on poll + wake message handler</name>
  <files>src/backend/claude-session/claude-session-server.ts</files>
  <behavior>
    - Add module-scope-adjacent (connection-scoped) let-bindings near line 982 (after `let contextPctInFlight = false;`):
      - `let dormantInFlight = false;`
      - `let dormantLastEmitted: boolean | null = null;`
      - `let isIdentityShapedCached: boolean | null = null;`
      - `let identityShapeProbeInFlight = false;`
    - Inside the existing contextPctTimer setInterval async IIFE (line 3442-3658), AFTER the plan_pending block completes (around line 3654) and BEFORE the `finally { contextPctInFlight = false; }` (line 3655-3657):
      - If `dormantInFlight` OR `currentTmuxSession === null`: skip. Else set `dormantInFlight = true` and run inner try/finally that resets it.
      - If `isIdentityShapedCached === null && !identityShapeProbeInFlight`: fire the one-shot probe `test -d ~/.claude/identities/'<escapedName>' && echo yes || echo no` via execCommand. On yes → cache true. On no / any error → cache false. In either case do NOT continue to the dormant check on this tick (probe consumed this tick's budget); dormant check runs starting next tick.
      - If `isIdentityShapedCached === true`: run `stat ~/.claude/identities/'<escapedName>'/.dormant 2>/dev/null >/dev/null && echo yes || echo no` via execCommand. Parse output. Compare to `dormantLastEmitted`; if different, `ws.send({type:"dormant", dormant:isDormant})` and update `dormantLastEmitted`. Try/catch the send (ws may be mid-close).
      - If `isIdentityShapedCached === false`: skip entirely (cache is authoritative — never re-probe on this connection).
    - Escape the tmuxSession name inside the shell arg using the SAME single-quote wrap pattern already used at line 3441 (`tmux capture-pane -p -t '${tmuxSession}'`). Reuse a local `const escapedName = currentTmuxSession;` at the top of the block (tmuxSession is already validated to alphanumeric/dash/underscore by the frontend, so single-quote is sufficient).
    - In the `ws.on("message", async (raw: RawData) => {` handler (line 2017 onwards), add a new branch handling `msg.type === "wake"`. Guard: require `currentTmuxSession !== null && sshConn !== null && isIdentityShapedCached === true` (never attempt rm on a non-identity pane or a disconnected pane). Execute `rm -f ~/.claude/identities/'<escapedName>'/.dormant` via execCommand. On success: `ws.send({type:"wake_result", ok:true})`. On error/exception: `ws.send({type:"wake_result", ok:false, error:err.message})`. Do NOT try to fast-poke the supervisor. Do NOT clear dormantLastEmitted on the backend — the next poll tick will emit `dormant:false` naturally when the sentinel is gone AND the identity has become alive again (supervisor reconcile latency). If the pane goes back to dormant before that (rare), the emit-only-on-change guard keeps it silent.
    - Update the message-type doc block at lines 83-88 to include the two new frames: `{type: "dormant", dormant}` on the server → client side and `{type: "wake"}` / `{type: "wake_result", ok, error?}` on the client → server side. Reference "quick 260808-cd6".
    - Ensure the dormantInFlight/isIdentityShapedCached/identityShapeProbeInFlight state is RESET to their initial values whenever the connection-scoped teardown at line ~1115 fires (mirrors the contextPctTimer teardown pattern). Since these are connection-scoped `let` bindings inside the enclosing function, they naturally die with the closure — no explicit teardown needed IF and ONLY IF they are declared inside the same closure as `contextPctInFlight`. Verify placement matches.
    - New backend tests in a new file `src/backend/claude-session/dormant-poll.test.ts` (mirror the shape of existing backend unit tests):
      - Test A: identity-shape probe caches true → subsequent poll fires stat and emits `{type:"dormant", dormant:true}` when stat yields "yes".
      - Test B: identity-shape probe caches false → subsequent polls skip dormant check entirely (mock execCommand asserts NO stat call).
      - Test C: emit-only-on-change — two consecutive polls returning "yes" emit ONE frame, not two.
      - Test D: wake message with currentTmuxSession null → responds with `{type:"wake_result", ok:false, error:...}` and does NOT invoke execCommand.
      - Test E: wake message happy path → invokes `rm -f` execCommand and responds `{type:"wake_result", ok:true}`.
      - Test F: wake message with execCommand throw → responds `{type:"wake_result", ok:false, error:message}`.
      Use the existing execCommand-mock + WebSocket-mock pattern established in `context-pct-from-jsonl.test.ts` and `session-file-discovery.test.ts`.
  </behavior>
  <action>
    Read claude-session-server.ts once around lines 980-990, 1105-1200, 2015-2040, 3400-3660 to locate insert points. Add the four connection-scoped guards near contextPctInFlight. Extend the contextPctTimer poll IIFE with the two-tier stat logic (probe on first call, dormant check on subsequent calls). Add wake message branch in ws.on("message"). Update the message-type comment block at lines 83-88. Create the new dormant-poll.test.ts with 6 tests mirroring context-pct-from-jsonl.test.ts fixture patterns. Do NOT re-read the file after editing.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npm run build:backend 2>&1 | tail -20 && npx vitest run src/backend/claude-session/dormant-poll.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>build:backend EXIT 0; dormant-poll.test.ts 6/6 pass; grep -c "dormantInFlight\|isIdentityShapedCached" src/backend/claude-session/claude-session-server.ts returns >=4; grep -c "msg.type === \"wake\"" src/backend/claude-session/claude-session-server.ts returns >=1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DormancyOverlay component + unit tests</name>
  <files>src/ui/features/pretty-view/DormancyOverlay.tsx, src/ui/features/pretty-view/DormancyOverlay.test.tsx</files>
  <behavior>
    - New file DormancyOverlay.tsx mirroring SessionHoldingOverlay.tsx verbatim for scrim + card geometry (absolute inset-0, z-[99], backdrop-blur-md, iOS Safari isolate + translateZ(0) hardening, pointer-events-auto, animate-in fade-in duration-150, centered glass card mirror). Props exactly as specified in `<interface_contracts>` above.
    - "asleep" state (waking=false): Moon glyph (lucide-react `Moon`) + "session is asleep" copy + Wake button (Button component, `size="sm"` `variant="secondary"`, "Wake" label, onClick={onWake}, `aria-label="Wake identity"`).
    - "waking…" state (waking=true): Moon glyph (STATIC — no animate-spin per motion-channel guardrail in SessionHoldingOverlay lines 38-46; the whole file's header comment applies verbatim to this component) + "waking…" copy. Wake button REPLACED by disabled placeholder or hidden entirely (choose hidden — user cannot re-tap during wake window).
    - Elapsed-hint (waking=true && elapsedSeconds >= 15): render additional sub-text line "this can take up to 60s" below the main "waking…" copy in dim color (text-white/50 or similar).
    - Error variant (error !== null && !waking): warm-red card variant matching SessionHoldingOverlay error variant exactly (lines 62-73, 152-164 of that file). Copy: "wake failed — " + error. Wake button re-enabled so Ashley can retry.
    - role="status" and aria-label mirroring SessionHoldingOverlay's a11y treatment.
    - Test file DormancyOverlay.test.tsx (target ~7 tests):
      - Test 1: waking=false, error=null → renders "session is asleep" text + Wake button enabled.
      - Test 2: waking=false → Wake button click invokes onWake prop.
      - Test 3: waking=true, elapsedSeconds=0 → renders "waking…" text + NO Wake button.
      - Test 4: waking=true, elapsedSeconds=14 → does NOT render elapsed-hint.
      - Test 5: waking=true, elapsedSeconds=15 → DOES render "this can take up to 60s" hint.
      - Test 6: waking=false, error="rm failed" → renders warm-red variant + "wake failed — rm failed" text + Wake button re-enabled (retry).
      - Test 7: Moon glyph is STATIC — assert the rendered SVG does NOT carry the `animate-spin` class in any variant (motion-channel guardrail regression test).
    Mirror the render + fireEvent + expect fixture pattern from ComposeBox.reconnecting-disable.test.tsx.
  </behavior>
  <action>
    Copy SessionHoldingOverlay.tsx as the template — preserve every geometry class, motion guardrail comment, and iOS Safari hardening line VERBATIM; substitute Moon for RefreshCcw; add the Wake button in the "asleep" branch; add the elapsedSeconds prop + inline hint conditional; add the error variant using the SessionHoldingOverlay error-variant classes (bg-gradient warm-red, text-red-warm, warm-red inset shadow) verbatim. Write DormancyOverlay.test.tsx after the component, following the reconnecting-disable.test.tsx describe/it structure. Do NOT invent new geometry, colors, or z-indexes — every deviation from SessionHoldingOverlay must be justified in a comment. Include a top-of-file docblock referencing "quick 260808-cd6 — sibling to SessionHoldingOverlay pattern; motion-channel guardrail inherited verbatim".
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx vitest run src/ui/features/pretty-view/DormancyOverlay.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>DormancyOverlay.test.tsx 7/7 pass; grep -c "animate-spin" src/ui/features/pretty-view/DormancyOverlay.tsx returns 0 (motion-channel guardrail enforced); file exists with >=80 lines.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: ComposeBox dormantActive prop + dormant-disable test</name>
  <files>src/ui/features/pretty-view/ComposeBox.tsx, src/ui/features/pretty-view/ComposeBox.dormant-disable.test.tsx</files>
  <behavior>
    - Add `dormantActive?: boolean;` to the ComposeBoxProps interface right after `reconnectingActive?: boolean;` (line 308). Copy the reconnectingActive doc-comment block (lines 298-307) as the template; substitute: "Value from PrettyView: `dormant || waking` — flipped by the WS `dormant` frame handler; also true during the local 'waking…' window after the user clicks Wake, until a live JSONL frame arrives (natural auto-dismiss signal)." Keep INDEPENDENT-from-other-props note per CONTEXT § "Do NOT collapse".
    - Add `dormantActive` to the destructure at line 336-338 (after `reconnectingActive`).
    - grep for every occurrence of `reconnectingActive` in ComposeBox.tsx and mirror the OR-in / early-return at EACH site. Confirmed sites from bounty premise (~15):
      - Line 1297 (handleVoiceSend primary): `if (!recycleActive && !planPendingActive && !reconnectingActive)` → add `&& !dormantActive`
      - Line 1311 (handleVoiceSend slot variant): same treatment
      - Line 1506 (handleKeyDown Enter early-return): `if (recycleActive || planPendingActive || reconnectingActive) return;` → add `|| dormantActive`
      - Line 1577-1579 (showPrimaryArmButton predicate): `!recycleActive && !planPendingActive && !reconnectingActive` → add `&& !dormantActive`
      - Line 1611-1613 (sendDisabled predicate): `recycleActive === true || planPendingActive === true || reconnectingActive === true` → add `|| dormantActive === true`
      - Line 1750 (Send button disabled attr): add `|| dormantActive === true`
      - Line 1971 (reset context window button): add `|| dormantActive === true`
      - Line 2000 (ThumbsUp/Recap button): add `|| dormantActive === true`
      - Line 2042-2044 (QueuedRow prop passthrough): add `dormantActive={dormantActive}`
      - Line 2483-2490 (QueuedRow interface declaration): add `dormantActive?: boolean;`
      - Line 2521-2523 (QueuedRow destructure): add `dormantActive`
      - Line 2586-2589 (showSlotArmButton predicate inside QueuedRow): add `&& !dormantActive`
      - Line 2781-2783 (slot-send disabled inside QueuedRow): add `|| dormantActive === true`
    - Verify line numbers via a single grep pass BEFORE editing — line numbers drift as edits are applied. Grep pattern: `grep -n "reconnectingActive" src/ui/features/pretty-view/ComposeBox.tsx` and mirror at EACH hit.
    - New file ComposeBox.dormant-disable.test.tsx copied VERBATIM from ComposeBox.reconnecting-disable.test.tsx with `reconnectingActive` → `dormantActive` throughout (props names, describe/it labels, expect assertions). Preserves the D1-D7 truth-table pattern (D1 Send disabled but not morphed; D2 aux disabled; D3 textarea typeable; D4 Enter swallowed; D5 baseline sanity; D6 paperclip enabled; D7 primary mic enabled). Docblock references "quick 260808-cd6" and "mirror of ComposeBox.reconnecting-disable.test.tsx".
  </behavior>
  <action>
    Read ComposeBox.tsx once around the reconnectingActive prop declaration (260-310), destructure (330-340), and each disable site (1290-1315, 1500-1520, 1570-1615, 1745-1755, 1965-2010, 2035-2050, 2480-2530, 2580-2595, 2775-2790). Add dormantActive at every mirror site. Do NOT touch any site that does not also read reconnectingActive — the disable topology is intentionally symmetric. After edits, run a grep-diff check: `grep -c "dormantActive" src/ui/features/pretty-view/ComposeBox.tsx` should equal `grep -c "reconnectingActive" src/ui/features/pretty-view/ComposeBox.tsx` (both counts identical proves 1:1 mirror). Write dormant-disable.test.tsx as a global-substitute copy of reconnecting-disable.test.tsx.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx tsc --noEmit 2>&1 | tail -10 && npx vitest run src/ui/features/pretty-view/ComposeBox.dormant-disable.test.tsx src/ui/features/pretty-view/ComposeBox.reconnecting-disable.test.tsx 2>&1 | tail -20 && bash -c 'D=$(grep -c "dormantActive" src/ui/features/pretty-view/ComposeBox.tsx); R=$(grep -c "reconnectingActive" src/ui/features/pretty-view/ComposeBox.tsx); echo "dormantActive=$D reconnectingActive=$R"; [ "$D" = "$R" ] && echo OK || echo MIRROR-COUNT-MISMATCH'</automated>
  </verify>
  <done>tsc --noEmit EXIT 0; dormant-disable.test.tsx 7/7 pass; reconnecting-disable.test.tsx 7/7 still pass (regression); dormantActive-count === reconnectingActive-count in ComposeBox.tsx (1:1 mirror proven).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: PrettyView wiring — WS handlers, DormancyOverlay mount, Wake callback, live-frame auto-dismiss, dormantActive prop</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx</files>
  <behavior>
    - Import: add `DormancyOverlay` from `./DormancyOverlay` in the import block at line 26 (near SessionHoldingOverlay import).
    - State (near line 309, after showOverlay/holdingTimeoutError):
      - `const [dormant, setDormant] = useState(false);`
      - `const [waking, setWaking] = useState(false);`
      - `const [wakingStartTs, setWakingStartTs] = useState<number | null>(null);`
      - `const [elapsedSeconds, setElapsedSeconds] = useState(0);`
      - `const [wakeError, setWakeError] = useState<string | null>(null);`
    - Ref for stale-closure protection (mirror isVisibleRef at line 518): `const dormantRef = useRef(false);` + a mirror useEffect `useEffect(() => { dormantRef.current = dormant; }, [dormant]);`. Rationale: the WS onmessage closure captures state at closure-creation time; ref read gives current value inside the switch.
    - WS onmessage switch cases (after `case "context_pct"` at line 722):
      - `case "dormant":` → `setDormant(parsed.dormant); if (!parsed.dormant) { setWaking(false); setWakingStartTs(null); setElapsedSeconds(0); setWakeError(null); }`
      - `case "wake_result":` → `if (parsed.ok) { /* wait for live frame */ } else { setWaking(false); setWakingStartTs(null); setElapsedSeconds(0); setWakeError(parsed.error ?? "wake failed"); }`
    - Auto-dismiss on live frame — inserted at the TOP of the onmessage handler (right after `wsFramesRef.current += 1` at line 662, BEFORE the JSON.parse try/catch): after JSON.parse succeeds, check `if (dormantRef.current && (parsed.type === "message" || parsed.type === "image" || parsed.type === "relay_inbound" || parsed.type === "relay_outbound" || parsed.type === "context_pct" || parsed.type === "harness_tasks" || parsed.type === "session")) { setDormant(false); setWaking(false); setWakingStartTs(null); setElapsedSeconds(0); setWakeError(null); }`. Include a comment: "Live-frame auto-dismiss — supervisor's recover path relaunched claude + /id ran. Any live-shape JSONL frame is the definitive signal per bounty 260808-cd6."
    - Elapsed-seconds ticker useEffect: keyed on `[waking, wakingStartTs]`. When `waking && wakingStartTs !== null`: setInterval every 1000ms → `setElapsedSeconds(Math.floor((Date.now() - wakingStartTs) / 1000))`. Cleanup clearInterval. When waking flips off: setElapsedSeconds(0).
    - handleWake callback (useCallback near other WS-send callbacks like handleAsideDismiss): `wsRef.current?.readyState === WebSocket.OPEN` guard → `wsRef.current.send(JSON.stringify({type:"wake"})); setWaking(true); setWakingStartTs(Date.now()); setWakeError(null);`. Depend on `[]` since it reads only refs.
    - Mount DormancyOverlay in the chat-region wrapper — insert AFTER the SessionHoldingOverlay line 1450: `{dormant && <DormancyOverlay waking={waking} elapsedSeconds={elapsedSeconds} onWake={handleWake} error={wakeError} />}`. Comment above: "quick 260808-cd6: DormancyOverlay mirrors SessionHoldingOverlay geometry; independent mount gate; the two overlays cannot both be true at once by supervisor invariant (dormant-only-when-alive), but the mounts are independent for wire simplicity."
    - Wire `dormantActive={dormant || waking}` prop on the ComposeBox render (near line 1654, after reconnectingActive). Include a one-line comment referencing the bounty.
    - Documentation note in the visibility-gating comment area: add "hidden-pane suppression is inherited from patch #344's WS-pause on !isVisible; no additional gate needed here — a hidden pane's WS is closed so dormant frames are never received." near the new `case "dormant":` handler.
    - Extend the existing PrettyView.test.tsx integration test suite with ONE new test (or new describe block): "quick 260808-cd6 dormancy overlay integration" —
      - Test 1: WS emits `{type:"dormant", dormant:true}` → DormancyOverlay mounts, ComposeBox Send button is disabled (dormantActive path).
      - Test 2: WS emits `{type:"dormant", dormant:true}` then a `{type:"message", ...}` live frame → DormancyOverlay unmounts (auto-dismiss).
      - Test 3: WS emits `{type:"dormant", dormant:true}`; user clicks Wake → wsSendSpy called with `{type:"wake"}`; overlay transitions to waking state.
      - Test 4: Wake result error → overlay stays mounted, shows warm-red error variant, Wake button re-enabled.
    Mirror the existing PrettyView.test.tsx `mountPV()` fixture pattern; add `isVisible={true}` per patch #344 test-scaffolding pattern.
  </behavior>
  <action>
    Read PrettyView.tsx once at lines 1-50, 300-320, 510-525, 650-880, 1440-1670. Add import, state, ref, mirror effect, WS switch cases, top-of-handler auto-dismiss, ticker useEffect, handleWake callback, overlay mount, ComposeBox prop wiring. All at the exact insert points documented in `<interface_contracts>`. After edits, extend PrettyView.test.tsx with the 4-test dormancy integration block (append at end of file, new describe). Do NOT re-read PrettyView.tsx after editing.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx tsc --noEmit 2>&1 | tail -10 && npx vitest run src/ui/features/pretty-view/PrettyView.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>tsc --noEmit EXIT 0; PrettyView.test.tsx all tests pass including new dormancy integration block; grep -c "DormancyOverlay\|setDormant\|handleWake" src/ui/features/pretty-view/PrettyView.tsx returns >=6; grep -c "dormantActive" src/ui/features/pretty-view/PrettyView.tsx returns >=1.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Full suite green + ship under 15-min deadman + patch #345 note</name>
  <what-built>
    Backend dormant-poll (change-only emit) + wake message handler + WS message types; DormancyOverlay component; ComposeBox dormantActive prop (1:1 mirror of reconnectingActive at ~15 sites); PrettyView WS handlers + mount + Wake callback + live-frame auto-dismiss + elapsed-seconds ticker. All Skynet-side; agent-supervisor is untouched (sentinel-delete is path 3 "FREE").
  </what-built>
  <how-to-verify>
    Execution steps for the executor (this is a checkpoint but Claude drives every step; the human only confirms the ship at the end):

    1. Run full test suite: `cd /home/ubuntu/skynet && npm test 2>&1 | tail -40`. Expected: 1556 pass / 6 skip baseline PLUS the new tests (dormant-poll.test.ts +6, DormancyOverlay.test.tsx +7, ComposeBox.dormant-disable.test.tsx +7, PrettyView.test.tsx +4). Target: ~1580 pass / 6 skip / 0 fail. If ANY existing test regresses, stop and diagnose.
    2. Run `tsc --noEmit` + `npm run build:backend` + `npm run build` sanity gates.
    3. Ship under 15-min deadman using the standard skynet recipe (`docker compose up -d --force-recreate skynet` gated by the deadman rollback per Ashley 2026-07-03 CLAUDE.md constraint) — use the same pattern demonstrated by patches #344 and #339 in `~/.claude/roles/box-maintainer/skynet-patches.md`.
    4. After container reports healthy, run HTTPS 200 health check against `term.gigaashley.click` (or the pinned health URL used by prior patches). Verify byte-shipped: `dormantActive` and `DormancyOverlay` should be findable in the shipped frontend bundle assets under `/app/html/assets/`.
    5. Append patch #345 entry to `/home/ubuntu/.claude/roles/box-maintainer/skynet-patches.md` at the top (patches are ordered newest-first). Follow the entry style of #344 and #339: one paragraph, dense prose, cite files changed with +/- line counts, cite commit hash, cite test count delta, cite image hash, cite HTTPS response time. Include the bounty slug `dormancy-overlay-and-wake-button` and quick slug `260808-cd6`.
    6. **Ashley UAT prompt** — after ship, message Ashley: "Patch #345 shipped. To UAT: on T1000, force-dormant an identity (`touch ~/.claude/identities/<name>/.dormant` on the host, or wait for the 2h idle threshold). Open that identity's pretty view in the PWA — expect the sleep overlay + Wake button, ComposeBox disabled. Tap Wake — expect 'waking…' state, overlay auto-dismisses within ~60s when the recover path relaunches. Report any UX gaps."
    7. Per standing directive: ask Ashley about DMing Stacy.
  </how-to-verify>
  <resume-signal>
    Type "shipped" once patches file updated, deadman cleared, HTTPS 200 confirmed, and Ashley UAT prompt sent. Or describe any deploy failures / test regressions for diagnosis.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser client → backend WS | Ashley's PWA sends `{type:"wake"}` messages; backend must not trust any hostId/tmuxSession in the payload (per T-14-02-01 mitigation) |
| Backend → target host SSH | Executes `stat` (read) and `rm -f` (destructive) against `~/.claude/identities/<name>/.dormant` on Tailscale-reachable managed boxes |
| tmux session name → shell arg | Session name flows into two shell commands (stat + rm); already sanitized to alphanumeric/dash/underscore by frontend validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-cd6-01 | Tampering | client → server wake payload | mitigate | Backend ignores any hostId/tmuxSession in the `{type:"wake"}` payload; uses only connection-scoped `currentTmuxSession` set at connectToPane discovery time (mirrors T-14-02-01 mitigation applied to aside_arm) |
| T-cd6-02 | Tampering | tmux session name shell injection | mitigate | Single-quote wrap the session name in both stat and rm commands; tmuxSession is already validated by the frontend to alphanumeric/dash/underscore only (matches the same escape pattern used at claude-session-server.ts:3441 for capture-pane). No `$(...)` or backtick evaluation possible inside single-quote strings. |
| T-cd6-03 | Elevation of Privilege | wake path deleting non-dormancy sentinels | mitigate | The rm path is HARD-CODED to `~/.claude/identities/<name>/.dormant` with the session name interpolated into a fixed template — no user-supplied path segments. `rm -f` on a non-existent sentinel is a no-op; on an unexpected file it would delete precisely one specific sentinel filename inside the identity dir. Restricted blast radius. |
| T-cd6-04 | Denial of Service | poll pileup on slow SSH | mitigate | `dormantInFlight` guard mirrors `contextPctInFlight` — one probe/stat inflight per pane at a time; slow probes drop subsequent tick's dormant check silently rather than pile up. Identity-shape probe is one-shot per connection (cached), so a slow probe blocks dormancy detection for one extra tick then never again. |
| T-cd6-05 | Information Disclosure | leaking identity existence to non-owners | accept | Ashley is the sole PWA user (single-tenant fork); the only "attacker" model is a compromised browser session, which already has full pane access. Dormant sentinel presence adds no info beyond what the terminal WS already exposes. |
| T-cd6-06 | Repudiation | wake action attribution | accept | Fleet is single-user (Ashley). No multi-user audit trail needed. Supervisor logs the wake trigger via its own `metric event=wake-start trigger=$trigger` line — sufficient for post-hoc "did the wake fire" diagnostics. |
| T-cd6-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages added — DormancyOverlay uses `lucide-react` `Moon` icon (already in package.json via SessionHoldingOverlay's RefreshCcw import). No install task in this bounty. |
</threat_model>

<verification>
Phase-level checks (executor runs all after task 5, before checkpoint 6):

1. `cd /home/ubuntu/skynet && npx tsc --noEmit` → EXIT 0.
2. `cd /home/ubuntu/skynet && npm run build:backend` → EXIT 0.
3. `cd /home/ubuntu/skynet && npm run build` → EXIT 0.
4. `cd /home/ubuntu/skynet && npm test 2>&1 | tail -40` → ~1580 pass / 6 skip / 0 fail (baseline 1556 + ~24 new tests across the four new test blocks).
5. `grep -c "DormancyOverlay\|dormantActive\|dormant:" src/ui/features/pretty-view/PrettyView.tsx src/ui/features/pretty-view/ComposeBox.tsx src/ui/api/claude-session-api.ts src/backend/claude-session/claude-session-server.ts` → all >=1 (integration surface exists in every file that should have it).
6. Motion-channel regression: `grep "animate-spin" src/ui/features/pretty-view/DormancyOverlay.tsx` → NO matches (STATIC glyph guardrail).
7. Post-ship byte verification: after `docker compose up -d --force-recreate skynet`, `docker exec skynet grep -l "DormancyOverlay\|dormantActive" /app/html/assets/*.js 2>/dev/null | head` → at least one asset hit (frontend bundle carries the new symbols).
</verification>

<success_criteria>
- All 5 code tasks complete with green verifications; checkpoint 6 completes with human confirmation.
- Full `npm test` suite: baseline 1556 pass / 6 skip maintained + ~24 new tests added, 0 fail.
- `docker compose up -d --force-recreate skynet` completed under the 15-min deadman rollback timer (no manual override), HTTPS 200 health check passed against the pinned URL.
- `/home/ubuntu/.claude/roles/box-maintainer/skynet-patches.md` has a new patch #345 entry appended, following the #344/#339 prose style, citing bounty slug `dormancy-overlay-and-wake-button` and quick slug `260808-cd6`.
- agent-supervisor at `~/.local/bin/agent-supervisor` is UNMODIFIED (verify with `git status ~/.local/bin/agent-supervisor` if under git, or by re-reading and confirming byte-identical against the known dormancy design at lines 499-870).
- Ashley UAT prompt sent per the checkpoint instructions.
- Standing directive: Ashley asked about DMing Stacy.
</success_criteria>

<output>
Create `.planning/quick/260808-cd6-dormancy-overlay-wake-button-in-prettyvi/260808-cd6-SUMMARY.md` when done, mirroring the SUMMARY template with: patch #345 commit hash, image hash, test count delta, files changed with +/- line counts, HTTPS response time, byte-shipped confirmation, Ashley UAT status.
</output>
