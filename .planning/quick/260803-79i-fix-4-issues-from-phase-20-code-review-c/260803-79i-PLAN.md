---
phase: 260803-79i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/database/routes/identity-exists-on-host.ts
  - src/backend/database/routes/identity-exists-on-host.test.ts
  - src/backend/database/routes/identity-birth-orchestrator.ts
  - src/backend/database/routes/identity-birth-orchestrator.test.ts
  - src/ui/api/identities-api.ts
  - src/ui/api/identities-api.test.ts
  - src/backend/database/routes/identity-avatar-batch.ts
  - src/backend/database/routes/identity-avatar-batch.test.ts
autonomous: true
requirements:
  - CR-03
  - CR-02
  - CR-05
  - CR-06

must_haves:
  truths:
    - "SSH probe in /identities/exists-on-host emits shell command with path /identities/<name>/ (no literal single-quote characters wrapping the name)"
    - "birthIdentity() rejects a name containing '=', '/', '+', or '.' with a clear error BEFORE any SSH/exec/DB call"
    - "openBirthStream() cancels response.body on non-200 status and throws a clear error when a 200 response has a null body"
    - "identity-avatar-batch candidate cache evicts the oldest entry when the global cap of 100 is reached and evicts the oldest entry for a user when their per-user cap of 15 is reached"
  artifacts:
    - path: "src/backend/database/routes/identity-exists-on-host.ts"
      provides: "SSH probe with correctly quoted path"
      contains: '"$HOME/.claude/identities/${name}"'
    - path: "src/backend/database/routes/identity-birth-orchestrator.ts"
      provides: "TMUX_SAFE_NAME_RE tighter regex gate before shell/exec work"
      contains: "TMUX_SAFE_NAME_RE"
    - path: "src/ui/api/identities-api.ts"
      provides: "openBirthStream with reader cancellation and empty-body guard"
      contains: "response.body?.cancel"
    - path: "src/backend/database/routes/identity-avatar-batch.ts"
      provides: "Bounded candidate cache with GLOBAL_CACHE_MAX and PER_USER_CACHE_MAX eviction"
      contains: "GLOBAL_CACHE_MAX"
  key_links:
    - from: "src/backend/database/routes/identity-exists-on-host.test.ts"
      to: "src/backend/database/routes/identity-exists-on-host.ts"
      via: "Test 10 asserts no literal single-quotes wrap name in SSH command"
      pattern: "not.*toContain.*'.*'"
    - from: "src/backend/database/routes/identity-birth-orchestrator.test.ts"
      to: "src/backend/database/routes/identity-birth-orchestrator.ts"
      via: "New test rejects '=' / '/' / '+' / '.' names before any dep is called"
      pattern: "TMUX_SAFE_NAME_RE|tmux target|unsafe for tmux"
    - from: "src/ui/api/identities-api.test.ts"
      to: "src/ui/api/identities-api.ts"
      via: "New test mocks fetch 500 and asserts body.cancel is called"
      pattern: "cancel.*mockResolvedValue|cancel.*toHaveBeenCalled"
    - from: "src/backend/database/routes/identity-avatar-batch.test.ts"
      to: "src/backend/database/routes/identity-avatar-batch.ts"
      via: "New tests fill cache to caps and assert oldest-out eviction"
      pattern: "GLOBAL_CACHE_MAX|PER_USER_CACHE_MAX|eviction"
---

<objective>
Fix four Phase 20 code review findings with atomic per-fix commits:
- CR-03: broken SSH probe shell quoting in `/identities/exists-on-host` (real bug: probe always returns `{exists: false}` for SSH hosts, blocking the IDUI-05 target-host collision block)
- CR-02: tmux target regex too permissive in `birthIdentity()` (real risk: `=`, `/`, `+`, `.` accepted by `IDENTITY_KEY_RE` produce malformed tmux targets)
- CR-05: `openBirthStream()` leaks ReadableStream reader on error paths (defensive)
- CR-06: `identity-avatar-batch` candidate cache is unbounded (defensive DoS mitigation)

Purpose: Convert reviewer-approved fix specifications into 4 atomic commits, each with a regression test.

Output: 4 code commits. No STATE.md / ROADMAP.md / skynet-patches.md changes (orchestrator handles those). No push, no docker build, no docker compose. No git worktrees.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/20-identity-creation-ui/20-REVIEW.md

# CR-03 target + existing test
@src/backend/database/routes/identity-exists-on-host.ts
@src/backend/database/routes/identity-exists-on-host.test.ts

# CR-02 target + existing test
@src/backend/database/routes/identity-birth-orchestrator.ts
@src/backend/database/routes/identity-birth-orchestrator.test.ts

# CR-05 target + existing test
@src/ui/api/identities-api.ts
@src/ui/api/identities-api.test.ts

# CR-06 target + existing test
@src/backend/database/routes/identity-avatar-batch.ts
@src/backend/database/routes/identity-avatar-batch.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-03 — Fix broken SSH probe shell quoting in /identities/exists-on-host</name>
  <files>src/backend/database/routes/identity-exists-on-host.ts, src/backend/database/routes/identity-exists-on-host.test.ts</files>
  <behavior>
    - When the SSH branch runs, the shell command passed to `execCommand` must contain `/identities/${name}/` (no literal single-quote characters wrapping the name — single quotes do NOT nest inside double quotes in POSIX shell, so the current form `.../'name'/` matches a directory literally named `'name'` and always returns false).
    - Name safety is already enforced by `IDENTITY_KEY_RE = /^[a-z0-9._=/+-]+$/` at line 52 (validated BEFORE SSH is opened), so removing the inner single quotes does not weaken injection defenses.
    - Existing Test 10 currently asserts the buggy behavior (`expect(command).toContain(`'${safeName}'`)`); rewrite Test 10's Part B assertion to assert the CORRECT form and to explicitly forbid the buggy form.
  </behavior>
  <action>
    Edit `src/backend/database/routes/identity-exists-on-host.ts` at line 128: change the SSH probe template literal from
    `if [ -d "$HOME/.claude/identities/'${name}'" ]; then echo exists; else echo missing; fi`
    to
    `if [ -d "$HOME/.claude/identities/${name}" ]; then echo exists; else echo missing; fi`
    (removing the literal single-quote characters that were wrapping `${name}` inside the double-quoted path).

    Also update the comment block at lines 14-18 (SECURITY docstring) and lines 115-117 (inline SHELL SAFETY comment): replace the "Single-quoting the name in the SSH command is defense-in-depth" language with a note that the name is validated by `IDENTITY_KEY_RE` before the SSH command is constructed, and that no inner shell quoting is applied because single quotes do not nest inside double quotes in POSIX shell. Keep the reference to `identity-artifact-reader.ts` but reframe as "matches the validate-then-interpolate pattern".

    Rewrite Test 10 in `src/backend/database/routes/identity-exists-on-host.test.ts` (lines 318-344). Keep Part A (evil-name rejection via IDENTITY_KEY_RE) unchanged. Rewrite Part B to:
    - Use `safeName = "testname"` (matches the pre-decided fix spec's parameterization).
    - Mock `execCommand` to resolve `"missing"`.
    - After the HTTP request, assert `execCommand` was called exactly once.
    - Assert the emitted command string INCLUDES `/identities/testname` and INCLUDES `"$HOME/.claude/identities/testname"` (i.e., name interpolated raw inside the double-quoted path).
    - Assert the emitted command string does NOT include `'testname'` (the literal single-quoted form — this is the regression guard for CR-03).

    Also update the test's `describe` title / docstring at the top of the file if it mentions "shell safety — name is single-quoted": rename the Test 10 title to `"shell safety — name is validated pre-SSH and interpolated raw inside double-quoted path (CR-03 regression)"` and update the header comment on line 21 accordingly.

    Commit message prefix: `fix(20-review):` — e.g., `fix(20-review): CR-03 remove broken inner single-quotes from SSH probe path`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx vitest run src/backend/database/routes/identity-exists-on-host.test.ts && npm run build:backend</automated>
  </verify>
  <done>
    - Test 10 passes with the new assertion pair (contains `/identities/testname`, does NOT contain `'testname'`).
    - All other tests in the file still pass (10/10).
    - `npm run build:backend` compiles cleanly.
    - Single commit created with prefix `fix(20-review):` touching only `identity-exists-on-host.ts` and `identity-exists-on-host.test.ts`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CR-02 — Add TMUX_SAFE_NAME_RE stricter gate before tmux/exec work</name>
  <files>src/backend/database/routes/identity-birth-orchestrator.ts, src/backend/database/routes/identity-birth-orchestrator.test.ts</files>
  <behavior>
    - A name containing any of `=`, `/`, `+`, or `.` is rejected by `birthIdentity()` with a thrown error BEFORE `deps.getCandidateForBirth`, `deps.createIdentityRecord`, `deps.connectOneShot`, `deps.execCommand`, or `deps.execLocal` is called.
    - The error message contains the substring `unsafe for tmux target` and includes the JSON-stringified offending name.
    - The existing `IDENTITY_KEY_RE` check remains in place (db-side compatibility gate). The new `TMUX_SAFE_NAME_RE` check runs immediately AFTER `IDENTITY_KEY_RE` passes.
    - A pure-lowercase alpha-numeric name with `-` and `_` (e.g. `"test_agent-1"`) still passes both gates and reaches step 1.
  </behavior>
  <action>
    Edit `src/backend/database/routes/identity-birth-orchestrator.ts`:

    1. Add a new regex constant immediately after the existing `IDENTITY_KEY_RE` declaration at line 179:
       `const TMUX_SAFE_NAME_RE = /^[a-z][a-z0-9_-]*$/;`
       Add a JSDoc comment above it explaining: "Stricter than IDENTITY_KEY_RE — used as a second gate inside birthIdentity() to reject names that would be valid identity keys but produce malformed or ambiguous tmux target arguments. Rejects `=`, `/`, `+`, `.` because tmux interprets these in target syntax (`-t =name` = exact match, `.name` = window/pane reference, `+name` = relative pane offset, `/name` = malformed target). See CR-02 in .planning/phases/20-identity-creation-ui/20-REVIEW.md."

    2. Inside `birthIdentity()` (around line 200), immediately AFTER the existing `if (!IDENTITY_KEY_RE.test(opts.name))` block that throws, add a second check:
       ```
       if (!TMUX_SAFE_NAME_RE.test(opts.name)) {
         throw new Error(
           `identity name unsafe for tmux target — must match [a-z][a-z0-9_-]*; got: ${JSON.stringify(opts.name)}`,
         );
       }
       ```
       Keep the existing `IDENTITY_KEY_RE` check untouched.

    3. NOTE: this task does NOT change the tmux `-t` interpolation to use `escName` (that is a separate fix — the CR-02 review entry proposed BOTH tightening the regex AND switching to `escName`, but this quick task scope is limited to the regex tightening per user's fix spec). The regex gate ensures the tmux target is safe going forward; the `escName` refactor is out of scope for this plan.

    Add tests to `src/backend/database/routes/identity-birth-orchestrator.test.ts`. Find the existing describe block that covers name-validation (search for `IDENTITY_KEY_RE` or the existing throw-on-invalid-name test). Add a new `describe.each` or a small dedicated block titled `"CR-02: TMUX_SAFE_NAME_RE stricter gate"` with:
    - `it.each(["foo=bar", "foo/bar", "foo+bar", "foo.bar"])("rejects name with tmux-unsafe char %s before any dep is called", async (evilName) => { ... })` — construct `deps` with vi.fn() mocks for `getCandidateForBirth`, `createIdentityRecord`, `getIdentityRecord`, `connectOneShot`, `execCommand`, `execLocal`, `isLocalHostId`, `resolveHostById`, and `fsp`. Call `birthIdentity({ ..., name: evilName }, emit, deps)` and assert:
      - the call rejects/throws with message matching `/unsafe for tmux target/`
      - `deps.getCandidateForBirth`, `deps.createIdentityRecord`, `deps.connectOneShot`, `deps.execCommand`, `deps.execLocal` were NOT called
    - `it("accepts a lowercase name with dashes and underscores", async () => { ... })` — same setup but with `name: "test_agent-1"` and enough dep stubs to reach step 1. Assert no throw is raised by the pre-step-0 validation (the test can short-circuit after `getCandidateForBirth` is called by throwing "avatar candidate expired" from the mock, which is a step-1 error, not a validation error — the key assertion is that `getCandidateForBirth` WAS called for the well-formed name).

    Commit message prefix: `fix(20-review):` — e.g., `fix(20-review): CR-02 add TMUX_SAFE_NAME_RE gate rejecting =/+. before tmux work`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx vitest run src/backend/database/routes/identity-birth-orchestrator.test.ts && npm run build:backend</automated>
  </verify>
  <done>
    - New `describe`/`it.each` block for CR-02 passes.
    - All existing orchestrator tests still pass.
    - `TMUX_SAFE_NAME_RE` present in the source file with JSDoc referencing CR-02.
    - The second validation check exists immediately after the existing `IDENTITY_KEY_RE` throw block.
    - `npm run build:backend` compiles cleanly.
    - Single commit with prefix `fix(20-review):` touching only `identity-birth-orchestrator.ts` and `identity-birth-orchestrator.test.ts`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CR-05 — Fix openBirthStream reader leak on error and empty-body</name>
  <files>src/ui/api/identities-api.ts, src/ui/api/identities-api.test.ts</files>
  <behavior>
    - When `fetch()` returns a non-200 response, `openBirthStream` calls `response.body?.cancel()` (swallowing any errors from cancel) BEFORE throwing the error.
    - When `fetch()` returns a 200 with `response.body === null`, `openBirthStream` throws an error whose message contains the substring `empty SSE stream` (or similar — pick a clear message; test asserts on a stable substring).
    - The non-null assertion `response.body!` on the 200 path is removed and replaced with a runtime null check that throws the empty-stream error above.
    - Existing behavior on a valid 200 response (with body) is unchanged — the generator still yields BirthEvent frames.
  </behavior>
  <action>
    Edit `src/ui/api/identities-api.ts` in the `openBirthStream` async generator (lines 156-236):

    1. On the non-200 error path (line 171-180), BEFORE `throw new Error(errorMsg)`, add:
       ```
       await response.body?.cancel().catch(() => { /* swallow — best-effort release */ });
       ```
       Place this call before the `let errorMsg = ...` block so the reader is released even if JSON parsing of the error body throws unexpectedly. Note: the existing code already tries `response.json()` which itself consumes the body — the additional `cancel()` is a defensive belt-and-braces call that becomes a no-op if the body has been fully consumed. If placing cancel BEFORE `response.json()` prevents error-body extraction, place it AFTER the try/catch that reads the error JSON instead (immediately before `throw new Error(errorMsg)`). Choose whichever ordering preserves error-message extraction; document the choice with a one-line comment.

    2. On the 200 path (line 182), replace `const reader = response.body!.getReader();` with:
       ```
       if (!response.body) {
         throw new Error("birth failed: server returned empty SSE stream");
       }
       const reader = response.body.getReader();
       ```

    Add tests to `src/ui/api/identities-api.test.ts`. Add a new describe block `"CR-05: openBirthStream error paths"` with:
    - `it("cancels response.body on non-200 status")` — mock global `fetch` (or the module fetch shim depending on how existing tests mock it — inspect the existing test file first) to return an object mimicking a Response with `status: 500`, a `json()` method resolving to `{ error: "test error" }`, and a `body` object whose `cancel` is a `vi.fn().mockResolvedValue(undefined)`. Invoke `openBirthStream({...valid opts})` and await the generator; assert it rejects, and assert the mocked `body.cancel` was called at least once.
    - `it("throws 'empty SSE stream' when 200 response has null body")` — mock fetch to return `{ status: 200, body: null }`. Assert the generator rejects with a message containing `"empty SSE stream"`.

    If the existing `identities-api.test.ts` file has no fetch mock (only axios via `authApi`), the openBirthStream tests will need to install `global.fetch = vi.fn()` in a beforeEach and restore in afterEach — READ the existing file first to confirm; use its established pattern if `openBirthStream` is already covered by tests there. If not covered, add a fresh describe block with a self-contained fetch mock at the top of the block.

    Commit message prefix: `fix(20-review):` — e.g., `fix(20-review): CR-05 release reader in openBirthStream error paths + guard null body`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx vitest run src/ui/api/identities-api.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>
    - Both new tests pass.
    - All existing identities-api tests still pass.
    - Non-null assertion `response.body!` no longer present in `openBirthStream`.
    - `response.body?.cancel()` present on the non-200 error path.
    - `npx tsc --noEmit` produces no errors (this is a frontend file; plain tsc is the right check per constraints).
    - Single commit with prefix `fix(20-review):` touching only `identities-api.ts` and `identities-api.test.ts`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: CR-06 — Bound identity-avatar-batch candidate cache with global + per-user caps</name>
  <files>src/backend/database/routes/identity-avatar-batch.ts, src/backend/database/routes/identity-avatar-batch.test.ts</files>
  <behavior>
    - Two module-level constants are added: `GLOBAL_CACHE_MAX = 100` and `PER_USER_CACHE_MAX = 15`.
    - When inserting a new entry into `candidateCache` would push the total size above `GLOBAL_CACHE_MAX`, the OLDEST entry (by Map insertion order, i.e., `candidateCache.keys().next().value`) is deleted BEFORE the new entry is inserted. A warning is logged: `[identity-avatar-batch] cache eviction: evicting <id> to make room (global cap 100 reached)`.
    - When inserting a new entry for a user would push that user's count above `PER_USER_CACHE_MAX`, the OLDEST entry OWNED BY THAT USER (earliest `createdAt`, or earliest insertion-order among the user's entries) is deleted BEFORE the new entry is inserted. A similar warning is logged mentioning the per-user cap.
    - Both eviction paths use `candidateCache.delete(id)` (the cache holds base64 PNGs in memory — no other resource cleanup is required).
    - Behavior on cache miss / TTL expiry / user-scope guard for GET /candidate/:id is unchanged.
  </behavior>
  <action>
    Edit `src/backend/database/routes/identity-avatar-batch.ts`:

    1. Add module-level constants near the existing constants block (around line 42-45):
       ```
       /** Absolute cap on total cache entries across all users. Prevents unbounded memory growth (CR-06). */
       const GLOBAL_CACHE_MAX = 100;
       /** Per-user cap on cache entries. Prevents a single user from monopolizing the global budget (CR-06). */
       const PER_USER_CACHE_MAX = 15;
       ```

    2. Add a private helper `evictIfNeeded(userId: string): void` below the TTL sweeper (around line 74). Behavior:
       - Count entries owned by `userId` by iterating `candidateCache.values()`.
       - If per-user count `>= PER_USER_CACHE_MAX`, find the oldest entry owned by this user. Options: iterate `candidateCache` in insertion order (Maps preserve it) and pick the first entry whose `userId === userId`; OR collect user entries and pick min-by-createdAt. Either is acceptable; document the choice inline. Delete that entry from the cache. Log a warning via `console.warn` (project appears to use `databaseLogger` in some routes — check WR-06 in review; use console.warn if databaseLogger isn't already imported in this file to avoid scope-creep).
       - After the per-user check, if `candidateCache.size >= GLOBAL_CACHE_MAX`, delete `candidateCache.keys().next().value` (oldest global by insertion order). Log a warning.

    3. In the POST /batch handler at line 269-278, replace the direct `candidateCache.set(id, entry)` inside the map with a version that calls `evictIfNeeded(userId)` immediately BEFORE `candidateCache.set(id, {...})`. Since 3 entries are inserted per batch, `evictIfNeeded` should be called once per insertion (inside the map callback, before `.set`). Confirm each insertion triggers one eviction check.

    Add tests to `src/backend/database/routes/identity-avatar-batch.test.ts`. Add a new describe block `"CR-06: candidate cache eviction"`:
    - `it("evicts oldest global entry when cache size would exceed GLOBAL_CACHE_MAX")` — access the cache via `_getCandidateCacheForTest()` and pre-populate it with 100 entries for various user IDs (using `Buffer.from("stub")` bytes, distinct `createdAt` values ascending). Note the ID of the OLDEST inserted entry (first `.set`). Then trigger insertion of one more entry (either by calling `evictIfNeeded` directly if exported for tests, OR by driving a real POST /batch request end-to-end with mocked OpenAI — the direct approach is simpler if a test-only export exists; if not, add `export function _evictIfNeededForTest(userId: string): void` gated on `process.env.NODE_ENV === "test"` mirroring the existing `_getCandidateCacheForTest` / `_clearCandidateCacheForTest` pattern, then call it, then `candidateCache.set(...)` a new entry). Assert the oldest entry is no longer in the cache and the new entry IS present, and `candidateCache.size === 100`.
    - `it("evicts oldest per-user entry when a user's count would exceed PER_USER_CACHE_MAX")` — pre-populate 15 entries for `userId = "1"` and 5 entries for `userId = "2"`. Note the ID of user 1's OLDEST entry. Trigger insertion of one more entry for user 1 (same mechanism as above). Assert user 1's oldest is gone, user 1's newest is present, user 1 still has exactly 15 entries, and user 2's 5 entries are ALL still present (cross-user isolation).

    If adding `_evictIfNeededForTest`, mirror the exact pattern used by the existing `_clearCandidateCacheForTest` / `_getCandidateCacheForTest` helpers (test-only gate, returns undefined outside test env).

    Commit message prefix: `refactor(20-review):` — e.g., `refactor(20-review): CR-06 bound candidate cache with global + per-user eviction caps`. (Refactor because this is defensive — no user-visible bug is fixed; sustained abuse would eventually OOM, but no report exists.)
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx vitest run src/backend/database/routes/identity-avatar-batch.test.ts && npm run build:backend</automated>
  </verify>
  <done>
    - Both new tests pass.
    - All existing avatar-batch tests still pass.
    - `GLOBAL_CACHE_MAX` and `PER_USER_CACHE_MAX` constants exist and are referenced from the eviction logic.
    - `evictIfNeeded` is called before every `candidateCache.set` in the POST /batch handler.
    - `npm run build:backend` compiles cleanly.
    - Single commit with prefix `refactor(20-review):` touching only `identity-avatar-batch.ts` and `identity-avatar-batch.test.ts`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `/identities/exists-on-host` | HTTP GET with hostId + name query params (validated by IDENTITY_KEY_RE) |
| backend → target host via SSH | shell command with interpolated `${name}` (validated pre-interpolation) |
| browser → `/identities/birth` | HTTP POST with birth options (name flows into tmux target arguments) |
| browser → `/identities/avatar/batch` | HTTP POST — output cached server-side in unbounded Map (CR-06 mitigation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79i-01 | Tampering | `identity-exists-on-host.ts` SSH probe | accept | Original CR-03 bug BLOCKED probe (always false); fix restores correct behavior. Injection defense unchanged: IDENTITY_KEY_RE gates name pre-interpolation. Fixing the probe reduces false-negative attack surface (users could not verify collisions). |
| T-79i-02 | Tampering | `identity-birth-orchestrator.ts` tmux target | mitigate | Add `TMUX_SAFE_NAME_RE = /^[a-z][a-z0-9_-]*$/` as second gate inside `birthIdentity()`; rejects `=`, `/`, `+`, `.` which are all tmux target metacharacters. Existing IDENTITY_KEY_RE remains for db compatibility. |
| T-79i-03 | Denial of Service | `openBirthStream` client-side | mitigate | Cancel `response.body` on non-200 to release TCP/HTTP2 connection slot; guard against null body on 200 to convert unhandled `TypeError` into clean `Error`. |
| T-79i-04 | Denial of Service | `identity-avatar-batch` candidate cache | mitigate | Two-tier eviction: global cap 100 (protects Node heap from OOM under sustained abuse), per-user cap 15 (prevents one authenticated user from monopolizing the global budget). Eviction is oldest-first, deterministic. |
| T-79i-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan (all fixes use existing dependencies). N/A. |
</threat_model>

<verification>
All four tasks share these completion checks:
- `npm run build:backend` passes cleanly (validates CR-03/02/06 backend changes).
- `npx tsc --noEmit` passes cleanly (validates CR-05 frontend changes).
- `npx vitest run src/backend/database/routes/identity-exists-on-host.test.ts src/backend/database/routes/identity-birth-orchestrator.test.ts src/backend/database/routes/identity-avatar-batch.test.ts src/ui/api/identities-api.test.ts` — all 4 test suites pass.
- `git log --oneline -5` shows exactly 4 new commits with prefixes: `fix(20-review):` × 3 (CR-03, CR-02, CR-05), `refactor(20-review):` × 1 (CR-06).
- No untracked or unstaged changes to `.planning/STATE.md`, `.planning/ROADMAP.md`, or `skynet-patches.md` (orchestrator owns these).
- No `docker build` / `docker compose` / `git push` invoked.
</verification>

<success_criteria>
- CR-03: `identity-exists-on-host.ts` line ~128 no longer wraps `${name}` in literal single quotes. Regression test asserts the emitted SSH command contains `/identities/testname` and does NOT contain `'testname'`.
- CR-02: `TMUX_SAFE_NAME_RE = /^[a-z][a-z0-9_-]*$/` present at top of `identity-birth-orchestrator.ts`; `birthIdentity()` throws with `"unsafe for tmux target"` for names containing `=`, `/`, `+`, or `.`, BEFORE any dep is called. Existing IDENTITY_KEY_RE check preserved.
- CR-05: `openBirthStream` cancels `response.body` on non-200; throws clear error when 200 has null body. Non-null assertion `response.body!` removed. Two new tests pass.
- CR-06: `GLOBAL_CACHE_MAX = 100` and `PER_USER_CACHE_MAX = 15` present. `evictIfNeeded(userId)` runs before every `candidateCache.set(...)` in the POST /batch handler. Eviction is oldest-first for both tiers; per-user eviction does not affect other users. Two new tests pass.
- Exactly 4 commits created, each touching only the intended pair of source + test files.
</success_criteria>

<output>
Create `.planning/quick/260803-79i-fix-4-issues-from-phase-20-code-review-c/260803-79i-SUMMARY.md` when all 4 tasks complete, listing:
- CR-03 fix summary + commit SHA
- CR-02 fix summary + commit SHA
- CR-05 fix summary + commit SHA
- CR-06 fix summary + commit SHA
- Final `git log --oneline -5` output
- Confirmation that `npm run build:backend` and `npx tsc --noEmit` both pass
</output>
