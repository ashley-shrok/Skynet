---
phase: quick-260814-1hz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/MicButton.tsx
  - src/ui/features/pretty-view/ComposeBox.tsx
  - src/ui/features/pretty-view/ComposeBox.hold-to-send.test.tsx
  - src/ui/features/pretty-view/ComposeBox.hold-to-mic.test.tsx
autonomous: true
requirements:
  - QUICK-260814-1hz

must_haves:
  truths:
    - "Press-and-hold on the MicButton (primary + slot) begins voice recording and, on release inside bounds after >=250ms, sends the glued transcript via handleSend / handleQueueSlotSend."
    - "Short-tap on the MicButton opens RecordingControls (i.e., begins a mic-tap recording via beginRecord) instead of the previous mic-onClick behavior — the hook's onShortTap replaces the direct onClick path."
    - "Tap on the Send button (primary + slot) with typed text calls handleSend / handleQueueSlotSend once with the typed text and does NOT construct a MediaRecorder."
    - "The Send button in asideActive mode still fires onAsideDismiss on tap (the morph-to-X Resume affordance is preserved byte-for-byte)."
    - "The MicButton does NOT fall through to native long-press text-selection UI on mobile (select-none touch-none classes suppress it)."
    - "MicButton stays mounted for the ENTIRE duration of a hold-initiated recording (primary + slot): showMicButton / showSlotMic keep the button rendered so setPointerCapture stays attached and the hook's onPointerUp fires on release. Without this, the button would unmount the instant voice.state flips to 'recording', pointer capture would detach mid-hold, and the hold-to-send gesture would silently break at runtime."
    - "useHoldToRecord.ts is unchanged — D-16-02 iOS Safari sync-gesture invariant survives automatically."
    - "voice.state !== 'idle' guard in the hook still prevents double-arm; MicButton's disabled={voice.state !== 'idle'} stays as a belt-and-suspenders visual cue."
  artifacts:
    - path: "src/ui/features/pretty-view/MicButton.tsx"
      provides: "MicButton accepts optional pointer handlers + dataHoldActive prop; button has select-none touch-none classes"
      contains: "onPointerDown"
    - path: "src/ui/features/pretty-view/ComposeBox.tsx"
      provides: "primaryHold + slotHold wired to MicButton (not Send button); Send buttons restored to direct onClick=handleSend / handleQueueSlotSend; showMicButton + showSlotMic predicates preserve MicButton visibility during hold-initiated recording via the holdInitiatedRef disjunct"
      contains: "primaryHold.onPointerDown"
    - path: "src/ui/features/pretty-view/ComposeBox.hold-to-mic.test.tsx"
      provides: "Renamed hold-to-send integration suite, all queries retargeted to Record voice button; adds mic-short-tap-opens-recording test; Test 2 identity-preservation restored (mic stays mounted during hold)"
      contains: "Record voice"
  key_links:
    - from: "src/ui/features/pretty-view/ComposeBox.tsx (~line 2529 primary MicButton)"
      to: "primaryHold pointer handlers + holdActive"
      via: "prop spread onto MicButton"
      pattern: "primaryHold\\.onPointerDown"
    - from: "src/ui/features/pretty-view/ComposeBox.tsx (~line 2949 slot MicButton)"
      to: "slotHold pointer handlers + holdActive"
      via: "prop spread onto MicButton"
      pattern: "slotHold\\.onPointerDown"
    - from: "src/ui/features/pretty-view/ComposeBox.tsx primaryHold onShortTap (~line 1688)"
      to: "beginRecord(\"primary\")"
      via: "callback body"
      pattern: "beginRecord\\(\"primary\"\\)"
    - from: "src/ui/features/pretty-view/ComposeBox.tsx slotHold onShortTap (~line 2720)"
      to: "beginRecord(slot.id)"
      via: "callback body"
      pattern: "beginRecord\\(slot\\.id\\)"
    - from: "src/ui/features/pretty-view/ComposeBox.tsx primary Send button (~line 2446)"
      to: "handleSend / onAsideDismiss"
      via: "onClick={asideActive ? () => onAsideDismiss?.() : () => handleSend(undefined, \"send-button\")}"
      pattern: "onClick=\\{asideActive"
    - from: "src/ui/features/pretty-view/ComposeBox.tsx slot Send button (~line 2911)"
      to: "handleQueueSlotSend(slot.id)"
      via: "onClick"
      pattern: "handleQueueSlotSend\\(slot\\.id\\)"
    - from: "src/ui/features/pretty-view/ComposeBox.tsx showMicButton predicate (~line 1628)"
      to: "primaryHold.holdInitiatedRef"
      via: "|| primaryHold.holdInitiatedRef.current disjunct on the voice.state gate — keeps MicButton mounted during hold-initiated recording"
      pattern: "primaryHold\\.holdInitiatedRef\\.current"
    - from: "src/ui/features/pretty-view/ComposeBox.tsx showSlotMic predicate (~line 2684)"
      to: "slotHold.holdInitiatedRef"
      via: "|| slotHold.holdInitiatedRef.current disjunct on the isSlotActiveMic gate — keeps slot MicButton mounted during hold-initiated slot recording"
      pattern: "slotHold\\.holdInitiatedRef\\.current"
---

<objective>
Move the press-and-hold-to-record gesture from the Send `<button>` to the `<MicButton>` in both the primary and slot compose contexts within `src/ui/features/pretty-view/ComposeBox.tsx`.

Purpose: Hold-on-Send is currently broken on mobile — the raw `<button>` falls through to native long-press text-selection UI. MicButton already carries the correct semantic affordance (per the `one-handed-mobile-mode` bounty, Ashley 2026-07-27: "hold the mic button"), and moving the gesture there restores the original design intent while eliminating the mobile fallthrough via `select-none touch-none` on MicButton.

Output: MicButton extended with optional pointer handlers + `dataHoldActive`; ComposeBox rewired so `primaryHold` and `slotHold` drive MicButton (not Send); Send buttons restored to plain `onClick={handleSend}` (primary preserves aside-morph); `showMicButton` and `showSlotMic` predicates gain a `|| *hold.holdInitiatedRef.current` disjunct so MicButton stays mounted through the voice.state transitions of a hold (idle → starting → recording), preserving setPointerCapture and letting the hook's onPointerUp fire on release; test suite renamed and retargeted to the mic button plus one new test asserting mic-short-tap starts recording. `useHoldToRecord.ts` and `useVoiceRecording.ts` are NOT touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/ui/features/pretty-view/useHoldToRecord.ts
@src/ui/features/pretty-view/MicButton.tsx

Read-first source files (do NOT open in full — use targeted Grep + Read):
- `src/ui/features/pretty-view/ComposeBox.tsx` (~2979 lines — use offset/limit around the anchor lines called out in each task's <files>)
- `src/ui/features/pretty-view/ComposeBox.hold-to-send.test.tsx` (854 lines — will be renamed + rewired in Task 3)

Anchor lines to read with `Read` offset/limit before editing:
- ComposeBox.tsx L1625-1648 (primaryArmed / isPrimaryRecording / isPrimaryTranscribing derivations + showMicButton predicate)
- ComposeBox.tsx L1686-1701 (primaryHold construction + showRecordingControls gate)
- ComposeBox.tsx L2446-2536 (primary Send button + primary MicButton block)
- ComposeBox.tsx L2679-2732 (isSlotRecording / isSlotTranscribing / showSlotMic predicate + slotHold construction + showSlotRecording gate)
- ComposeBox.tsx L2900-2956 (slot Send button + slot MicButton block)
- ComposeBox.tsx L1235 (handleSend signature: `handleSend(overridePayload?, trigger?)`)
- ComposeBox.tsx L1203 (handleQueueSlotSend signature: `handleQueueSlotSend(slotId)`)

Invariant reminders sourced from useHoldToRecord.ts:
- MicButton.test.tsx does NOT currently exist (grep confirmed — no update needed there).
- Hook returns `onPointerUp` as `Promise<void>`; existing type signature in MicButtonProps must accept the Promise variant.
- Hook exposes `holdInitiatedRef: MutableRefObject<boolean>` — already consumed by `showRecordingControls` (L1701) and `showSlotRecording` (L2731); Task 4 adds two more read sites.
- voice.state transition during a hold-initiated recording: `"idle"` → `"starting"` → `"recording"` (useVoiceRecording.ts L88, L441/449). `showMicButton` is currently invalidated the moment state flips to `"recording"` (via `!isPrimaryRecording`), which would unmount the button mid-hold under the new design — Task 4 fixes this.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend MicButton with optional pointer handlers + hold-active data attribute + long-press-safe classes</name>
  <files>src/ui/features/pretty-view/MicButton.tsx</files>
  <behavior>
    - MicButtonProps continues to accept the existing `onClick`, `disabled`, `title`, `positionClass` props unchanged (backward compatible — callers that pass only the current four keep compiling).
    - MicButtonProps also accepts, all optional:
        - `onPointerDown?: (e: React.PointerEvent<HTMLButtonElement>) => void`
        - `onPointerUp?: (e: React.PointerEvent<HTMLButtonElement>) => void | Promise<void>`  (Promise variant matches useHoldToRecord's return signature)
        - `onPointerCancel?: (e: React.PointerEvent<HTMLButtonElement>) => void`
        - `onPointerLeave?: (e: React.PointerEvent<HTMLButtonElement>) => void`
        - `dataHoldActive?: boolean` — when defined, the rendered `<button>` gets `data-hold-active={dataHoldActive ? "true" : "false"}`; when undefined, the attribute is omitted entirely (do NOT emit `data-hold-active="false"` unconditionally — omission preserves DOM output for existing callers).
    - The underlying `<button>` spreads the four pointer handlers only when defined (React handles `undefined` handlers correctly, so direct passthrough is acceptable; the concern is only the `dataHoldActive` attribute emission).
    - The `cn(...)` className list adds `"select-none"` and `"touch-none"` (Tailwind utilities → `user-select: none` and `touch-action: none`). This is what prevents the native long-press text-selection UI on mobile. Insert them alongside the other layout classes (order does not matter semantically, but grouping with the other block-level utilities like `"absolute", positionClass, "p-2"` keeps the diff readable).
    - Import `type React` from "react" (or use the appropriate existing import — the file currently has no React import; add `import type React from "react";` at the top alongside the lucide import).
  </behavior>
  <action>
    Add `import type React from "react";` at the top of MicButton.tsx (alongside the existing `import { Mic } from "lucide-react";` and `import { cn } from "@/lib/utils";`).

    Extend the `MicButtonProps` interface with the four optional pointer handlers and `dataHoldActive?: boolean` per <behavior>. Keep the existing four props (`onClick`, `disabled`, `title`, `positionClass`) unchanged so all existing call sites in ComposeBox.tsx and any future consumers continue to type-check.

    In the destructuring inside the function component, pull out the new props (all optional).

    Add `"select-none"` and `"touch-none"` to the `cn(...)` class list — group them with the layout utilities (e.g., right after `positionClass` or `"p-2"`; exact position within the list is cosmetic). These are the fix for the mobile long-press-fallthrough problem — hold-on-Send fails today precisely because those utilities are absent on the raw send `<button>`.

    Pass the four pointer handlers straight through to the `<button>` element. Emit `data-hold-active={dataHoldActive ? "true" : "false"}` ONLY when `dataHoldActive !== undefined` — use a conditional spread or the JSX pattern `{...(dataHoldActive !== undefined ? { "data-hold-active": dataHoldActive ? "true" : "false" } : {})}` so existing zero-arg callers get byte-identical DOM output.

    Keep `onClick` as-is (required prop, still spread to the button). The hook's `onShortTap` handles the tap-to-record semantics for the primary + slot contexts; the MicButton's `onClick` becomes a fallback that the browser synthesizes from a valid pointerdown/pointerup pair — the hook's `onPointerUp` will fire first in the sync order, so `onClick`'s handler running afterward is a harmless double-invoke risk. Reason about it this way: for the hold-recording use case in this plan, the caller can either (a) keep `onClick={() => beginRecord("primary")}` as-is and rely on the hook's guard (voice.state !== "idle") to short-circuit the second call, or (b) pass a no-op onClick. Choose (a) for minimum diff — the hook's guard already handles it, and existing MicButton tests do not need to be re-thought. Document this choice in a code comment on the MicButton primary+slot call sites (Task 2), not in MicButton.tsx itself.

    Do NOT touch any other class in the `cn(...)` list. Do NOT change the default `positionClass`. Do NOT change the `aria-label` / `title` behavior. The Mic icon rendering stays byte-identical.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "MicButton|useHoldToRecord|ComposeBox" | head -20 || echo "no MicButton/ComposeBox TS errors"</automated>
  </verify>
  <done>
    - MicButton.tsx compiles under `tsc --noEmit`.
    - New optional props are typed correctly (onPointerUp accepts both `void` and `Promise<void>` return types).
    - `select-none` and `touch-none` appear in the button's className list.
    - `data-hold-active` attribute is emitted only when `dataHoldActive` prop is defined.
    - Existing ComposeBox MicButton call sites (still at lines ~2529 and ~2949 with the pre-existing four props) continue to type-check untouched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewire ComposeBox — move hold gesture from Send button to MicButton (primary + slot) AND preserve MicButton visibility during hold-initiated recording</name>
  <files>src/ui/features/pretty-view/ComposeBox.tsx</files>
  <behavior>
    Primary context (uses `primaryHold` at ~L1686 and `handleSend`):
    - `primaryHold.onShortTap` (~L1688): change from calling `handleSend(undefined, "send-button")` to calling `beginRecord("primary")`. Rationale: the hook now lives on MicButton, so a short-tap on the mic must begin recording (the previous mic-onClick behavior); tap-to-send is served by the Send button's own onClick, restored below.
    - Primary Send `<button>` at ~L2446-2515:
        - RESTORE `onClick` to fire `handleSend` directly on tap (was: undefined). Preserve the aside-morph branch verbatim:
          `onClick={asideActive ? () => onAsideDismiss?.() : () => handleSend(undefined, "send-button")}`
        - REMOVE all four pointer handler bindings: `onPointerDown`, `onPointerUp`, `onPointerCancel`, `onPointerLeave` (currently L2459-2462).
        - REMOVE the `data-hold-active={primaryHold.holdActive ? "true" : "false"}` attribute (currently L2463).
        - Preserve everything else byte-for-byte: `disabled` predicate, `aria-label`, `title`, `className`, the icon rendering branch (paperplane / Loader2 / X-morph), children.
        - Update the surrounding B-2 comment block (L2448-2457) to reflect the new reality: the hook no longer lives on this button; onClick is now the primary send trigger for BOTH the aside branch AND the normal typed-send branch (no longer split into "aside branch via onClick / non-aside via hook").
    - Primary MicButton at ~L2529-2536:
        - Pass through the four pointer handlers: `onPointerDown={primaryHold.onPointerDown}`, `onPointerUp={primaryHold.onPointerUp}`, `onPointerCancel={primaryHold.onPointerCancel}`, `onPointerLeave={primaryHold.onPointerLeave}`.
        - Pass `dataHoldActive={primaryHold.holdActive}`.
        - KEEP the existing `onClick={() => beginRecord("primary")}` — see Task 1's rationale (belt-and-suspenders for browsers that fire click without a matching hook-observable pointerup, and the hook's `voice.state !== "idle"` guard makes it idempotent). Add a brief inline comment noting the intentional overlap.
        - KEEP `disabled={voice.state !== "idle"}`, `title="Record voice"`, `positionClass="right-11 bottom-0.5"` unchanged.
    - `showRecordingControls` predicate at ~L1701 is unchanged (still `isPrimaryRecording && !primaryHold.holdInitiatedRef.current`) — the hook's holdInitiatedRef gate still prevents the RecordingControls swap during a hold-initiated recording, and this contract is symmetric regardless of which button hosts the pointer handlers.

    Slot context (uses `slotHold` at ~L2718 and `handleQueueSlotSend`):
    - `slotHold.onShortTap` (~L2720): change from `handleQueueSlotSend(slot.id)` to `beginRecord(slot.id)`. Same rationale as primary.
    - Slot Send `<button>` at ~L2911-2946:
        - RESTORE `onClick={() => handleQueueSlotSend(slot.id)}` (was: absent — the button relied entirely on hook-driven pointer handlers).
        - REMOVE `onPointerDown`, `onPointerUp`, `onPointerCancel`, `onPointerLeave`, and `data-hold-active`.
        - Preserve `disabled={slotSendDisabled}`, `aria-label`, `title`, `className`, icon rendering.
    - Slot MicButton at ~L2949-2955:
        - Pass through the four `slotHold.*` pointer handlers and `dataHoldActive={slotHold.holdActive}`.
        - KEEP existing `onClick={() => beginRecord(slot.id)}` unchanged.
        - KEEP `disabled`, `title`, `positionClass` unchanged.
    - `showSlotRecording` predicate at ~L2731 is unchanged.

    Invariant guardrail: do NOT modify `src/ui/features/pretty-view/useHoldToRecord.ts` or `src/ui/features/pretty-view/useVoiceRecording.ts`. If any editing pass touches either file, STOP and surface the finding rather than proceeding.
  </behavior>
  <action>
    Use Read with offset/limit to load the anchor windows listed in <context> before editing. Do not read the full 2979-line ComposeBox in one pass.

    Perform four Edit operations against ComposeBox.tsx, in this order:

    1. `primaryHold` (~L1686-1696): change `onShortTap` body from `if (!sendDisabled) handleSend(undefined, "send-button");` to `beginRecord("primary");` — note that the `sendDisabled` guard is no longer needed at the callsite because the hook already refuses to arm when `disabled: sendDisabled || showTranscribingSend` is true (guard chain at useHoldToRecord.ts L219), so a short-tap event only fires here when the hook was actually armed, and by construction `sendDisabled` was false at pointerdown. If a defensive re-check is desired, wrap `beginRecord("primary")` in `if (voice.state === "idle") { ... }` — but do not gate on sendDisabled (it doesn't apply to recording semantics).

    2. Primary Send `<button>` (~L2446): rewrite the `onClick` and delete the four pointer handler + data-hold-active lines per <behavior>. Update the preceding B-2 comment (L2448-2457) to state that the hook has moved to MicButton and onClick now serves both the aside-dismiss AND normal-send paths.

    3. Primary `<MicButton>` (~L2529-2536): add the four pointer handler props and `dataHoldActive`. Keep the four existing props. Insert a one-line inline comment above the four new pointer-handler lines: `// Hold-to-record: primaryHold pointer handlers govern press-and-hold → voice recording. The onClick above is the tap-to-record path (hook's onShortTap redundantly calls beginRecord as well — guarded by voice.state check inside).`

    4. `slotHold` onShortTap (~L2720): change body from `handleQueueSlotSend(slot.id)` to `beginRecord(slot.id)`.

    5. Slot Send `<button>` (~L2911): add `onClick={() => handleQueueSlotSend(slot.id)}` and remove the four pointer handlers + data-hold-active lines.

    6. Slot `<MicButton>` (~L2949-2955): add the four pointer handler props from `slotHold` and `dataHoldActive={slotHold.holdActive}`. Keep existing props.

    Cross-check via grep after edits:
    - `grep -n "primaryHold\\.onPointerDown\\|primaryHold\\.holdActive" src/ui/features/pretty-view/ComposeBox.tsx` — should return exactly ONE hit for each, both on the MicButton (NOT the Send button).
    - `grep -n "slotHold\\.onPointerDown\\|slotHold\\.holdActive" src/ui/features/pretty-view/ComposeBox.tsx` — same pattern.
    - `grep -n "data-hold-active" src/ui/features/pretty-view/ComposeBox.tsx` — should return ZERO hits (the attribute now flows through MicButton's `dataHoldActive` prop → JSX emits the attribute internally).
    - `grep -n "handleSend(undefined, \"send-button\")" src/ui/features/pretty-view/ComposeBox.tsx` — should now appear on the primary Send button's onClick line (previously only in the primaryHold onShortTap body).

    Do NOT touch useHoldToRecord.ts or useVoiceRecording.ts. Do NOT change the `showRecordingControls` / `showSlotRecording` predicates. Do NOT touch the RecordingControls JSX branches, the arm-idle button, or the paperclip button.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; grep -v '^\s*//' src/ui/features/pretty-view/ComposeBox.tsx | grep -c "data-hold-active" | grep -qx 0 &amp;&amp; grep -c "primaryHold\.onPointerDown\|slotHold\.onPointerDown" src/ui/features/pretty-view/ComposeBox.tsx | grep -qx 2 &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "ComposeBox|MicButton" | head -20 || echo "typecheck clean; grep gates passed"</automated>
  </verify>
  <done>
    - `data-hold-active` attribute no longer appears in ComposeBox.tsx (it now lives inside MicButton.tsx behind the `dataHoldActive` prop).
    - `primaryHold.onPointerDown` and `slotHold.onPointerDown` each appear exactly ONCE — on the primary and slot MicButton respectively, NOT on the Send buttons.
    - Primary Send button's `onClick` fires `handleSend(undefined, "send-button")` on tap, `onAsideDismiss?.()` in aside mode.
    - Slot Send button's `onClick` fires `handleQueueSlotSend(slot.id)`.
    - `primaryHold` onShortTap body calls `beginRecord("primary")`; `slotHold` onShortTap calls `beginRecord(slot.id)`.
    - `useHoldToRecord.ts` and `useVoiceRecording.ts` unchanged (verify via `git diff --name-only` — neither should appear).
    - `npx tsc --noEmit` reports no new errors for ComposeBox.tsx or MicButton.tsx.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Preserve MicButton visibility during hold-initiated recording — extend showMicButton and showSlotMic predicates</name>
  <files>src/ui/features/pretty-view/ComposeBox.tsx</files>
  <behavior>
    Problem statement (why this task exists, not just what it does): Task 2 moves the hold gesture onto MicButton, but MicButton's own visibility gates unmount it the moment `voice.state` transitions off `"idle"` — and `voice.start()` invoked from the hook's `onPointerDown` transitions state `"idle"` → `"starting"` → `"recording"` while the user is still holding the button (useVoiceRecording.ts L441/449, VoiceRecordingState L88). Concretely:

    - Primary predicate at L1628-1634: `showMicButton = navigator.mediaDevices != null && !isPrimaryRecording && !isPrimaryTranscribing && !asideActive && !primaryArmed`. `isPrimaryRecording` = `voice.state === "recording" && micTarget === "primary"` (L1626). The instant state flips to `"recording"` mid-hold, `isPrimaryRecording=true` → `showMicButton=false` → MicButton unmounts. React tearing down the button while pointer capture is active detaches the capture, the hook's `onPointerUp` never fires on the (now-gone) button, and the user is stuck in a recording with no way to release-to-send. Meanwhile `showRecordingControls` (L1701) is ALSO gated `!primaryHold.holdInitiatedRef.current` (B-3 lock, CONTEXT.md § "Visual during hold" LOCKED), so RecordingControls does NOT swap in either — the slot goes empty under the pointer.

    - Slot predicate at L2684-2689: `showSlotMic = navigator.mediaDevices != null && !isSlotActiveMic && !asideActive && !slotArmed`, where `isSlotActiveMic = isSlotRecording || isSlotTranscribing`. Same failure mode when a slot hold begins: state → recording, `isSlotActiveMic=true`, `showSlotMic=false`, slot MicButton unmounts mid-hold.

    Fix: extend each predicate with a `|| <hold>.holdInitiatedRef.current` disjunct on the voice.state-derived gate so MicButton stays mounted for the whole hold. This is symmetric to the B-3 gate already in place on RecordingControls: the LOCKED rule "morphing the button the user is pressing makes the slide-off-to-cancel gesture fuzzy" originally protected the send button (which was the pressed button); under the new design the pressed button is MicButton, so the SAME rule requires MicButton to stay mounted-and-visible for the hold's duration.

    Exact predicate rewrites:

    - Primary `showMicButton` (~L1628-1634): change to
      ```
      const showMicButton =
        typeof navigator !== "undefined" &&
        navigator.mediaDevices != null &&
        (!isPrimaryRecording || primaryHold.holdInitiatedRef.current) &&
        (!isPrimaryTranscribing || primaryHold.holdInitiatedRef.current) &&
        !asideActive &&
        !primaryArmed;
      ```
      Rationale for both `isPrimaryRecording` AND `isPrimaryTranscribing` disjuncts: after release-to-send, voice.state transitions "recording" → "transcribing" during the STT round-trip. During that window `holdInitiatedRef` stays true until the hook's `resetGestureState()` runs at the end of the send flow. Without the transcribing disjunct, MicButton would flicker back for a frame between the state transitions or unmount mid-transcribe — both worse than staying mounted for the tail of the hold-initiated flow.

      Alternative form (equivalent, arguably more readable, planner is neutral — pick whichever produces the smaller diff):
      ```
      const holdActiveOnPrimary = primaryHold.holdInitiatedRef.current;
      const showMicButton =
        typeof navigator !== "undefined" &&
        navigator.mediaDevices != null &&
        (!isPrimaryRecording || holdActiveOnPrimary) &&
        (!isPrimaryTranscribing || holdActiveOnPrimary) &&
        !asideActive &&
        !primaryArmed;
      ```

    - Slot `showSlotMic` (~L2684-2689): change to
      ```
      const showSlotMic =
        typeof navigator !== "undefined" &&
        navigator.mediaDevices != null &&
        (!isSlotActiveMic || slotHold.holdInitiatedRef.current) &&
        !asideActive &&
        !slotArmed;
      ```
      Here the compressed form uses the existing `isSlotActiveMic` local rather than splitting into two disjuncts, matching the codebase's existing shape (the primary predicate happens to use two split flags because that's how the surrounding code already reads them; the slot predicate already collapses them into `isSlotActiveMic`, so preserve that shape).

    Ordering constraint: `primaryHold` is declared at ~L1686 but `showMicButton` is currently derived at ~L1628 — the reference to `primaryHold.holdInitiatedRef.current` in `showMicButton` therefore requires reordering. Move the `primaryHold = useHoldToRecord({...})` block (L1683-L1696) to precede the `showMicButton` derivation (L1628-L1634). This is safe because `useHoldToRecord`'s inputs (`voice`, `handleSend`, `asideActive`, `sendDisabled || showTranscribingSend`) are all defined BEFORE L1628 — verify with a targeted grep before moving. If any input is derived AFTER L1628 and BEFORE L1686, STOP and surface the finding; the fix then requires a different ordering strategy (e.g., a plain ref hoisted above the derivations, populated after `useHoldToRecord`).

    For the slot: `slotHold` (~L2718) is declared AFTER `showSlotMic` (~L2684). Same reordering treatment — move `slotHold` construction to precede `showSlotMic`. Cross-check its inputs (`voice`, `handleQueueSlotSend`, `handleVoiceSend`, `asideActive`, `slotSendDisabled`) — note `slotSendDisabled` is declared at L2707, after `showSlotMic`. Reorder the derivations so the ordering is:
      1. `isSlotRecording`, `isSlotTranscribing`, `slotArmed`, `slotHasText`, `isSlotActiveMic` (unchanged, L2679-L2683)
      2. `showSlotTranscribingSend` (currently L2701 — move up)
      3. `slotSendDisabled` (currently L2707 — move up)
      4. `slotHold = useHoldToRecord({...})` (currently L2718 — move up)
      5. `showSlotMic` (currently L2684 — stays here in file order but now references slotHold.holdInitiatedRef)
      6. `showSlotArmButton`, `slotThreeButtonState`, `showSlotRecording`, `showSlotSend` — order preserved among themselves, adjusted for the moves above.

    Reading holdInitiatedRef in a render-derived local (as opposed to a proper reactive value) is intentional and correct: `useHoldToRecord` mutates `holdInitiatedRef.current` synchronously in `onPointerDown` BEFORE calling `voice.start()`, so by the time React re-renders in response to the setState inside voice.start(), `holdInitiatedRef.current` is already true. No effect / dependency plumbing needed. Existing consumers `showRecordingControls` (L1701) and `showSlotRecording` (L2731) do this same thing and work correctly — this task follows the established pattern.

    Do NOT change the RecordingControls swap-in gates: `showRecordingControls` (L1701) and `showSlotRecording` (L2731) stay as `isXRecording && !xHold.holdInitiatedRef.current` — those are the OPPOSITE gate (RecordingControls stays HIDDEN during a hold, MicButton stays SHOWN during a hold). Together the two gates preserve the LOCKED "the button the user is pressing does not morph" invariant under the new design.
  </behavior>
  <action>
    Read the anchor windows with `Read` offset/limit before editing:
    - L1580-1700 (showMicButton predicate, all its inputs, and primaryHold construction)
    - L2679-2735 (isSlotActiveMic, showSlotMic, slotHold construction, showSlotRecording)

    Verify prerequisite: run `grep -n "primaryHold\|useHoldToRecord" src/ui/features/pretty-view/ComposeBox.tsx | head -20` to confirm `primaryHold` and `slotHold` are the only two `useHoldToRecord` call sites in the file (there should be exactly two matches at L1686 and L2718). If a third call site exists, STOP — the reordering strategy needs revisiting.

    Perform the edits:

    1. Move the `primaryHold = useHoldToRecord({...})` block (currently L1683-L1696, including its two-line lead-in comment at L1683-L1684) to immediately BEFORE the `showMicButton` derivation (currently L1628). Preserve the block byte-for-byte; only its file position changes. After the move, `primaryHold` is declared before `showMicButton` reads it.

    2. Rewrite `showMicButton` to include the `|| primaryHold.holdInitiatedRef.current` disjuncts per <behavior>. Update the preceding comment block (L1587-L1596) to add a sentence explaining the new disjunct: `// Hold-initiated recording exception (Quick 260814-1hz): during a hold, holdInitiatedRef is true — keep MicButton mounted so setPointerCapture stays attached and the hook's onPointerUp can fire on release. Symmetric to showRecordingControls's B-3 gate below (which keeps RecordingControls HIDDEN during a hold); together they preserve the CONTEXT.md § "Visual during hold" LOCKED rule "the button the user is pressing does not morph" under the mic-hosted-gesture design.`

    3. Same treatment for the slot: move `showSlotTranscribingSend` (L2701), `slotSendDisabled` (L2707), and `slotHold = useHoldToRecord({...})` (L2716-L2726, including its lead-in comment) to precede `showSlotMic` (L2684). After the move, the derivation order is: `isSlotRecording`/`isSlotTranscribing`/`slotArmed`/`slotHasText`/`isSlotActiveMic` → `showSlotTranscribingSend` → `slotSendDisabled` → `slotHold` → `showSlotMic` → `showSlotArmButton` → `slotThreeButtonState` → `showSlotRecording` → `showSlotSend`.

    4. Rewrite `showSlotMic` to include the `|| slotHold.holdInitiatedRef.current` disjunct per <behavior>. Add an analogous comment above the predicate referencing Quick 260814-1hz.

    Cross-check via grep after edits:
    - `grep -n "primaryHold\\.holdInitiatedRef\\.current" src/ui/features/pretty-view/ComposeBox.tsx` — should return TWO hits: one on the showMicButton predicate, one on showRecordingControls.
    - `grep -n "slotHold\\.holdInitiatedRef\\.current" src/ui/features/pretty-view/ComposeBox.tsx` — should return TWO hits: one on showSlotMic, one on showSlotRecording.
    - `grep -n "const primaryHold\\|const showMicButton\\|const slotHold\\|const showSlotMic" src/ui/features/pretty-view/ComposeBox.tsx` — verify declaration order: `primaryHold` appears BEFORE `showMicButton`; `slotHold` appears BEFORE `showSlotMic`.

    Do NOT change the RecordingControls / showSlotRecording gates. Do NOT touch useHoldToRecord.ts or useVoiceRecording.ts.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; grep -c "primaryHold\.holdInitiatedRef\.current" src/ui/features/pretty-view/ComposeBox.tsx | grep -qx 2 &amp;&amp; grep -c "slotHold\.holdInitiatedRef\.current" src/ui/features/pretty-view/ComposeBox.tsx | grep -qx 2 &amp;&amp; awk '/const primaryHold/{p=NR} /const showMicButton/{m=NR} END{exit !(p&amp;&amp;m&amp;&amp;p&lt;m)}' src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; awk '/const slotHold/{p=NR} /const showSlotMic/{m=NR} END{exit !(p&amp;&amp;m&amp;&amp;p&lt;m)}' src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "ComposeBox" | head -20 || echo "typecheck clean; ordering + disjunct grep gates passed"</automated>
  </verify>
  <done>
    - `showMicButton` predicate references `primaryHold.holdInitiatedRef.current` as a disjunct on both the isPrimaryRecording and isPrimaryTranscribing gates.
    - `showSlotMic` predicate references `slotHold.holdInitiatedRef.current` as a disjunct on the isSlotActiveMic gate.
    - Declaration order: `primaryHold` precedes `showMicButton`; `slotHold` precedes `showSlotMic` (verified via awk gate).
    - `showRecordingControls` and `showSlotRecording` predicates UNCHANGED (still `isXRecording && !xHold.holdInitiatedRef.current`).
    - `npx tsc --noEmit` reports no new errors.
    - `useHoldToRecord.ts` and `useVoiceRecording.ts` UNCHANGED (git diff --name-only returns neither).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Rename and rewire integration test suite; add mic-short-tap-starts-recording assertion</name>
  <files>src/ui/features/pretty-view/ComposeBox.hold-to-send.test.tsx, src/ui/features/pretty-view/ComposeBox.hold-to-mic.test.tsx</files>
  <behavior>
    - File `ComposeBox.hold-to-send.test.tsx` is renamed to `ComposeBox.hold-to-mic.test.tsx`. No test suite is deleted; the 10 existing tests are all rewired so their pointer events target the MicButton instead of the Send button.
    - `getSendButton()` helper is renamed to `getMicButton()` and its query becomes `screen.getByRole("button", { name: "Record voice" })` (matching MicButton's aria-label default). All call sites updated accordingly.
    - The describe block name updates from `"ComposeBox — Phase 32 hold-to-send gesture (primary send button)"` to `"ComposeBox — 260814-1hz hold-to-record gesture (primary mic button)"`.
    - Test comments/descriptions that reference "send button" as the hold target are updated to say "mic button" (do NOT rewrite the assertion semantics — the gestures are byte-identical, just on a different DOM element).
    - Behavioral changes required per test due to the semantic shift:
        * Test 1 (short-tap CONTEXT L122): short-tap on MicButton now = "start recording via beginRecord" (hook's onShortTap → beginRecord). It NO LONGER fires `onSend` with the typed text (that path is now on the Send button). Rework Test 1's assertions: after a sub-threshold pointerup, `onSend` was NOT called, but `getUserMedia` WAS called (twice — once by the sync pointerdown-hook path with an immediate rollback, then once again by the beginRecord path that fires in onShortTap). If asserting "no MediaRecorder constructed" is impossible in this new world (beginRecord DOES construct one), reformulate the assertion to: (a) `onSend` was NOT called, (b) `getUserMedia` was called at least once, (c) voice.state ends in the recording/starting branch — i.e., RecordingControls is now visible via the `Cancel recording` button. If simpler: replace Test 1 wholesale with the new-behavior test "short-tap on mic starts recording (opens RecordingControls)".
        * Tests 2/3/4/8/9/10 (long-press branches, threshold-boundary): logically unchanged — the hook fires on MicButton, the send-on-release-inside-bounds path still calls `handleVoiceSend("primary")` → `handleSend(glued)` → `onSend`. Just target MicButton for the pointer events instead of Send. Assertions on `onSend` receiving the glued transcript remain valid.
        * Test 5 (aside-morph inertness CONTEXT L126): the aside-morph is on the SEND button (Resume/X), not the MicButton. But when `asideActive=true`, `showMicButton` is FALSE (predicate gates on `!asideActive`, and Task 3's changes do not touch the asideActive gate — the holdInitiatedRef disjunct only applies to the voice.state-derived gates), so MicButton does not render. Test 5 must be rewritten to assert: (a) when `asideActive=true`, `screen.queryByRole("button", { name: "Record voice" })` returns null (mic not rendered), (b) the Resume button's onClick still fires onAsideDismiss on tap. Since the hook is no longer on the send button, the "hold on Resume does nothing" assertion of the original test collapses to "there is no hold-on-Send code path anymore" — capture that by inverting the assertion: `expect(screen.queryByRole("button", { name: "Record voice" })).toBeNull()` and `expect(onAsideDismiss).toHaveBeenCalledTimes(1)` after a click on Resume.
        * Test 6 (disabled-state inertness CONTEXT L127): the send button's `disabled` predicate does NOT gate MicButton visibility. MicButton renders when `voice.state === "idle" && text.trim() === "" && !asideActive && !queueArmed && !hasAttachments` — so with empty text, MicButton IS visible. The Test 6 semantics need rethinking: the "disabled" state for the hold gesture now depends on MicButton's own `disabled` prop, which is `voice.state !== "idle"`. Test 6 becomes: with `voice.state === "recording"` (i.e., another recording already active), `showMicButton` is false so MicButton does not render → nothing to hold. Since simulating this cleanly requires driving voice into recording state first, an easier reformulation: assert that MicButton IS visible on an empty textarea (unlike the old Send button, which was disabled), and that pressing-and-holding it starts a recording (i.e., the disabled-state semantic simply doesn't apply — MicButton is always ready to record when visible). If that dilutes the test's intent, delete Test 6 outright and note the reason in a comment (`// Test 6 (disabled-state inertness) removed under 260814-1hz: MicButton's disabled predicate is voice.state !== "idle", not sendDisabled. The scenario the old test guarded against — hold on a disabled send button — is no longer a code path because the hold gesture no longer lives on the send button.`).
        * Test 7 (voice.state !== 'idle' guard CONTEXT L128): the original test asserted that when voice.state is not idle, the Send button disappears (because showRecordingControls swaps in RecordingControls). Under the new design, the mic-tap path (short-tap) leaves `holdInitiatedRef=false`, so `showRecordingControls` is TRUE mid-recording → RecordingControls swaps in → MicButton is not rendered (because showMicButton's hold disjunct is false when holdInitiatedRef is false, so the isPrimaryRecording gate wins). Assertion stays: `screen.queryByRole("button", { name: "Record voice" })` returns null during a mic-tap-initiated recording; `Cancel recording` button is visible.
    - Add ONE new test at the end of the describe block: `"mic short-tap opens RecordingControls (short-tap-starts-record path)"`. Type text into the textarea, tap the MicButton (pointerdown + pointerup at timeStamp: 100 < HOLD_THRESHOLD_MS), then assert that a `Cancel recording` button appears via the RecordingControls swap (i.e., `beginRecord("primary")` was invoked via the hook's onShortTap). Cleanup: click Cancel to return to idle.
    - Import path for `HOLD_THRESHOLD_MS` unchanged (`from "./useHoldToRecord"`).
    - `installBoundsShim`, `makeMockStream`, `MockMediaRecorder`, `beforeEach`, `afterEach` all remain byte-identical.

    Test 2 (long-press starts recording in place) — RESTORED assertion (Task 3 promotes this from KNOWN DEVIATION to expected pass):
      The original test asserts that during a long-press-initiated recording, the SAME button element is still in the DOM (identity preservation, `expect(button.getAttribute("data-hold-active")).toBe("true")` and same-element pointerup routes to the same handler). Under Task 3's fix, `showMicButton` stays TRUE during a hold-initiated recording (holdInitiatedRef disjunct), so MicButton stays mounted, the pointer capture stays attached, and identity preservation holds. Keep Test 2's original three-part assertion structure:
      (a) after pointerdown-then-advance-past-HOLD_THRESHOLD_MS, `voice.state === "recording"` (equivalent: `Cancel recording` button ABSENT because holdInitiatedRef=true keeps RecordingControls hidden per B-3; use `data-hold-active="true"` on the MicButton as the primary assertion — this proves both "hook is armed" and "MicButton is still mounted").
      (b) MicButton is still queryable by role — `screen.getByRole("button", { name: "Record voice" })` does NOT throw.
      (c) The rendered button carries `data-hold-active="true"` (proves the primaryHold.holdActive prop is flowing through MicButton correctly per Task 1, AND that MicButton has not been unmounted-then-remounted).

    If — despite Task 3 — Test 2c fails because the button IS unmounting mid-hold, STOP and surface the finding: Task 3's fix is not landing. Do NOT patch Test 2 to hide the regression.
  </behavior>
  <action>
    Use `git mv src/ui/features/pretty-view/ComposeBox.hold-to-send.test.tsx src/ui/features/pretty-view/ComposeBox.hold-to-mic.test.tsx` (or a plain filesystem rename via the Bash tool if git is not tracking the file yet — check with `git ls-files` first). Prefer `git mv` so the rename is preserved in history.

    After renaming, edit the file:

    1. Rename the header comment block: replace `Phase 32 Plan 32-03 — integration tests for the hold-to-send gesture on the ComposeBox primary send button.` with `Quick 260814-1hz — integration tests for the hold-to-record gesture on the ComposeBox primary mic button.` Update the paragraph about "primary send button only" to say "primary mic button only".

    2. Rename `getSendButton()` (L126-128) to `getMicButton()` and change its query to `screen.getByRole("button", { name: "Record voice" }) as HTMLButtonElement`.

    3. Global find/replace within the file: `getSendButton()` → `getMicButton()`.

    4. Update the describe title to `"ComposeBox — 260814-1hz hold-to-record gesture (primary mic button)"`.

    5. Walk each of the 10 tests in order and apply the per-test rewiring specified in <behavior>. For each test, verify the assertions still describe truth after the semantic shift; where the test's original invariant no longer maps cleanly (Tests 1, 5, 6), apply the reformulation described in <behavior>. Keep the CONTEXT.md L### references intact — those refer to Phase 32's context doc which still describes the hold semantic; the physical button hosting it has just moved.

    6. Test 2 (long-press identity preservation): keep the original three-part assertion (voice enters recording, mic button still queryable, `data-hold-active="true"` on the mic). Task 3 makes this pass by preserving MicButton visibility during hold-initiated recording. If it fails, STOP and surface — do NOT weaken the assertion.

    7. Add the new test at the end of the describe block:
       - Name: `"mic short-tap starts recording (opens RecordingControls path)"`
       - Body: render ComposeBox with `onSend` mock; skip typing text (recording works with empty text); grab `getMicButton()`; installBoundsShim; fireEvent.pointerDown at timeStamp=0; await act pointerUp at timeStamp=100 (< HOLD_THRESHOLD_MS); flush microtasks + advance 50ms; assert `screen.getByRole("button", { name: "Cancel recording" })` exists (proves beginRecord was called and RecordingControls swapped in — holdInitiatedRef is false in this branch because the hook's short-tap branch does `resetGestureState()` which clears holdInitiatedRef); assert `onSend` was NOT called (mic short-tap does not send). Cleanup: click Cancel, flush.

    8. Sanity: `grep -n "getSendButton\\|Phase 32 Plan 32-03" src/ui/features/pretty-view/ComposeBox.hold-to-mic.test.tsx` should return ZERO hits after the rewrite.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; test ! -f src/ui/features/pretty-view/ComposeBox.hold-to-send.test.tsx &amp;&amp; test -f src/ui/features/pretty-view/ComposeBox.hold-to-mic.test.tsx &amp;&amp; npx vitest run src/ui/features/pretty-view/ComposeBox.hold-to-mic.test.tsx 2>&amp;1 | tail -40</automated>
  </verify>
  <done>
    - `ComposeBox.hold-to-send.test.tsx` no longer exists; `ComposeBox.hold-to-mic.test.tsx` exists in its place (git rename preserved if applicable).
    - All 10 existing tests are rewired to target the MicButton (`{ name: "Record voice" }`) and pass under `npx vitest run`.
    - Test 2's identity-preservation assertion (`data-hold-active="true"` on the still-mounted MicButton) passes — proves Task 3's visibility fix landed correctly.
    - Tests 1, 5, 6 reformulated per <behavior> to match the new semantics (short-tap on mic starts recording; asideActive hides mic entirely; disabled-state test either reformulated or removed with a documented reason).
    - New test "mic short-tap starts recording (opens RecordingControls path)" exists and passes.
    - `grep -c "getSendButton\\|Phase 32 Plan 32-03" src/ui/features/pretty-view/ComposeBox.hold-to-mic.test.tsx` returns 0.
    - `git diff --name-only src/ui/features/pretty-view/useHoldToRecord.ts src/ui/features/pretty-view/useVoiceRecording.ts` returns EMPTY (neither hook file was modified).
  </done>
</task>

</tasks>

<verification>
End-to-end phase-level checks (run in order):

1. Type-check the whole tree: `cd /home/ubuntu/skynet && npx tsc --noEmit -p tsconfig.json 2>&1 | tail -20` — should surface no new errors in `pretty-view/` files.
2. Run the renamed test suite: `npx vitest run src/ui/features/pretty-view/ComposeBox.hold-to-mic.test.tsx` — all tests pass, including Test 2's identity-preservation assertion.
3. Run the broader pretty-view test suite to catch regressions (useHoldToRecord unit tests, useVoiceRecording tests, other ComposeBox tests): `npx vitest run src/ui/features/pretty-view/` — no new failures relative to the pre-change baseline.
4. Confirm the two hook files were not modified: `git diff --name-only src/ui/features/pretty-view/useHoldToRecord.ts src/ui/features/pretty-view/useVoiceRecording.ts` returns nothing (D-16-02 iOS Safari sync-gesture invariant preserved by construction).
5. Grep gates (each should return the expected count):
   - `grep -v '^\s*//' src/ui/features/pretty-view/ComposeBox.tsx | grep -c "data-hold-active"` → 0 (attribute moved into MicButton)
   - `grep -c "onPointerDown={primaryHold" src/ui/features/pretty-view/ComposeBox.tsx | grep -qx 1 && echo ok` → primaryHold pointer handler appears exactly once (on MicButton, not on Send)
   - `grep -c "primaryHold\.holdInitiatedRef\.current" src/ui/features/pretty-view/ComposeBox.tsx` → 2 (showMicButton + showRecordingControls)
   - `grep -c "slotHold\.holdInitiatedRef\.current" src/ui/features/pretty-view/ComposeBox.tsx` → 2 (showSlotMic + showSlotRecording)
6. Manual mobile spot-check (deferred to Ashley's next mobile session — not blocking): hold the mic button on iOS Safari, confirm the mic-permission prompt fires and the record → release-inside-bounds → send flow works. This is what the whole change is for.
</verification>

<success_criteria>
- Hold-to-record gesture is bound to MicButton in both primary and slot ComposeBox contexts.
- Send button is bound to a direct tap-to-send onClick in both contexts; primary Send preserves the aside-morph Resume/X branch.
- MicButton has `select-none touch-none` classes so mobile long-press does not fall through to native text-selection UI.
- MicButton stays mounted for the duration of a hold-initiated recording (primary + slot) via the `holdInitiatedRef` disjunct in `showMicButton` / `showSlotMic` — setPointerCapture stays attached, `onPointerUp` fires on release, hold-to-send actually works end-to-end.
- `useHoldToRecord.ts` and `useVoiceRecording.ts` are unchanged (D-16-02 invariant survives by construction).
- Renamed test suite `ComposeBox.hold-to-mic.test.tsx` passes all 10 existing tests (rewired, including Test 2's identity-preservation) + the new mic-short-tap-starts-recording test.
- Type-check and pretty-view test suite are green.
</success_criteria>

<output>
Create `.planning/quick/260814-1hz-move-hold-to-record-gesture-from-send-bu/260814-1hz-SUMMARY.md` when done. Cross-reference the bounty at `~/.claude/roles/box-maintainer/bounties/hold-to-record-move-to-mic-button/bounty.json` in the SUMMARY (Ashley may want the bounty entry closed out based on this SUMMARY).
</output>
</content>
</invoke>