---
phase: 260801-62m-vehicle-b-composebox-aux-row-cleanup-too
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/ComposeBox.tsx
  - src/ui/features/pretty-view/ChatMessage.tsx
  - src/ui/features/pretty-view/ComposeBox.test.tsx
  - src/ui/features/pretty-view/ComposeBox.aside-morph.test.tsx
  - src/ui/features/pretty-view/ComposeBox.recycle-disable.test.tsx
  - src/ui/features/pretty-view/PrettyView.test.tsx
  - src/ui/features/pretty-view/ChatMessage.test.tsx
autonomous: true
requirements:
  - VEHICLE-B-01-import-cleanup
  - VEHICLE-B-02-add-queued-textarea-button-rename-and-icon-swap
  - VEHICLE-B-03-interrupt-tooltip-simplify
  - VEHICLE-B-04-thumbs-up-payload-rename
  - VEHICLE-B-05-explain-to-recap-rename-and-icon-swap
  - VEHICLE-B-06-bounty-button-removal
  - VEHICLE-B-07-queue-button-removal
  - VEHICLE-B-08-reset-tooltip-simplify
  - VEHICLE-B-09-chatmessage-isQuickReply-narrowing
  - VEHICLE-B-10-test-file-updates

must_haves:
  truths:
    - "ComposeBox aux row shows a Queue-a-message button (ListPlus icon) that adds a queued-message textarea — same behavior as before, new label and icon"
    - "ComposeBox aux row shows a Recap button (CircleHelp icon) that sends '/explain the current situation'"
    - "ComposeBox aux row shows a Thumbs-up button that sends 'thumbs up'"
    - "ComposeBox aux row NO LONGER shows the /bounty (Target) prefix-send button"
    - "ComposeBox aux row NO LONGER shows the /queue (ListPlus prefix-send) button — the ListPlus icon now belongs only to the add-queued-textarea button"
    - "Interrupt button tooltip reads simply 'Interrupt' (no '(Ctrl-C)' or '(send Ctrl-C)')"
    - "Reset context window button tooltip reads 'Reset context window' (no '/id reset prefix' language)"
    - "ChatMessage renders the thumbs-up glyph ONLY for user content equal to 'thumbs up' (case/whitespace insensitive) — legacy payloads render as plain text bubbles"
    - "Full vitest suite passes 1049/6-skipped/0-failed across 86 files; grep for FAIL|failed|✗|Unhandled on captured log is empty of real markers"
    - "npx tsc --noEmit exits 0"
  artifacts:
    - path: "src/ui/features/pretty-view/ComposeBox.tsx"
      provides: "aux row with Queue-a-message, Interrupt, Thumbs-up, Recap buttons; no /queue and no /bounty prefix buttons; new Reset tooltip; updated lucide-react imports (drop Lightbulb/Plus/Target; add CircleHelp; keep ListPlus)"
    - path: "src/ui/features/pretty-view/ChatMessage.tsx"
      provides: "isQuickReply narrowed to a single equality check on 'thumbs up' — no alt-matches for 'yes'/'works for me'/'good to go'/'go ahead'/'let\\'s go'"
    - path: "src/ui/features/pretty-view/ComposeBox.test.tsx"
      provides: "label matchers updated (thumbs up / queue a message); /queue+/bounty prefix-button suite replaced with removal-assertion suite"
    - path: "src/ui/features/pretty-view/ComposeBox.aside-morph.test.tsx"
      provides: "Reset label matcher updated; thumbs-up label matcher updated; Task-1 Test-7/Test-8 (bounty+queue disabled tests) replaced with removal explainer comment"
    - path: "src/ui/features/pretty-view/ComposeBox.recycle-disable.test.tsx"
      provides: "three exact-match label lookups (Reset / Thumbs-up / Recap) updated to new labels"
    - path: "src/ui/features/pretty-view/PrettyView.test.tsx"
      provides: "querySelector for reset aria-label updated to match 'reset context window'"
    - path: "src/ui/features/pretty-view/ChatMessage.test.tsx"
      provides: "quick-reply payload tests updated — Test 14 = 'thumbs up' positive match; Tests 14b-14f = negative matches for stripped legacy payloads"
  key_links:
    - from: "src/ui/features/pretty-view/ComposeBox.tsx thumbs-up button"
      to: "src/ui/features/pretty-view/ChatMessage.tsx isQuickReply"
      via: "shared payload string 'thumbs up'"
      pattern: "handleQuickSend\\(\"thumbs up\"\\)"
    - from: "src/ui/features/pretty-view/ComposeBox.tsx recap button"
      to: "handleQuickSend"
      via: "shortened prompt string"
      pattern: "handleQuickSend\\(\"/explain the current situation\"\\)"
    - from: "src/ui/features/pretty-view/ComposeBox.tsx add-queued-textarea button"
      to: "setQueueSlots"
      via: "onClick — behavior unchanged, only aria-label/title/icon renamed"
      pattern: "aria-label=\"Queue a message\""
---

<objective>
Vehicle B — mechanical aux-row cleanup pass on the pretty-view ComposeBox: rename button labels/tooltips, swap the Recap icon (Lightbulb→CircleHelp), rename the add-queued-textarea button (Plus→ListPlus, "Add queued message textarea"→"Queue a message"), shorten the Recap prompt payload, rename the thumbs-up quick-reply payload ("let's go"→"thumbs up"), and DELETE the /bounty (Target) and /queue (ListPlus prefix-send) buttons entirely. The single behavioral change is narrowing `ChatMessage.isQuickReply` to a lone equality check on the new "thumbs up" payload — legacy alt-matches for 'yes'/'works for me'/'good to go'/'go ahead'/'let's go' are stripped so past session files render those bubbles as plain text.

Purpose: Design was locked over multi-round conversation this session with Ashley + validated twice via docker-cp scratch UAT (2026-08-01 03:59Z + 04:07Z). Ashley greenlit "let's go" on the final pass. The vehicle is a pure mechanical pass — no design decisions in the executor — grouped into one commit-sized surgical change because every edit touches the same file cluster and the test-file diffs are tightly coupled to the production-code diffs.

Output: Two production files edited (ComposeBox.tsx + ChatMessage.tsx) + five test files edited, all under `src/ui/features/pretty-view/`. Verification pipeline: `npx tsc --noEmit` → `npx vitest run` capturing to log → grep-gate on log → single atomic commit. NO push, NO `docker build`, NO `docker compose up --force-recreate` — commit-boundary stop per fleet rule (Ashley 2026-07-27).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

@src/ui/features/pretty-view/ComposeBox.tsx
@src/ui/features/pretty-view/ChatMessage.tsx
@src/ui/features/pretty-view/ComposeBox.test.tsx
@src/ui/features/pretty-view/ComposeBox.aside-morph.test.tsx
@src/ui/features/pretty-view/ComposeBox.recycle-disable.test.tsx
@src/ui/features/pretty-view/PrettyView.test.tsx
@src/ui/features/pretty-view/ChatMessage.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply all Vehicle B aux-row cleanup edits to ComposeBox.tsx + ChatMessage.tsx + five test files</name>
  <files>src/ui/features/pretty-view/ComposeBox.tsx, src/ui/features/pretty-view/ChatMessage.tsx, src/ui/features/pretty-view/ComposeBox.test.tsx, src/ui/features/pretty-view/ComposeBox.aside-morph.test.tsx, src/ui/features/pretty-view/ComposeBox.recycle-disable.test.tsx, src/ui/features/pretty-view/PrettyView.test.tsx, src/ui/features/pretty-view/ChatMessage.test.tsx</files>
  <action>
Apply all ten edit clusters below EXACTLY as specified. Design is locked from scratch/UAT (2026-08-01 03:59Z + 04:07Z rounds — Ashley greenlit both). Do NOT propose alternatives, do NOT restyle any button (VISUAL-08 HARD LOCK — glass treatment stays verbatim), do NOT touch send-when-idle wiring (that is Vehicle C). Do NOT touch anything outside `/home/ubuntu/skynet/`. Grep BEFORE each edit to locate the current line offsets — the ~L numbers in the design spec are guidance not gospel; the source may have drifted a handful of lines.

**Edit 1 — ComposeBox.tsx import statement (L2 area).** Grep `from "lucide-react"` in ComposeBox.tsx to find the import line. Replace with the spec's exact import list: drop `Lightbulb`, `Plus`, `Target`; add `CircleHelp`; preserve `ListPlus`. Final list alphabetized as: `CircleHelp, ListPlus, Loader2, Paperclip, RefreshCw, RotateCcw, RotateCwFadingClock, Square, ThumbsUp, X`.

**Edit 2 — Add-queued-textarea button (find via grep `Add queued message textarea` in ComposeBox.tsx).** Change `aria-label="Add queued message textarea"` → `aria-label="Queue a message"`. Change `title="Add queued message textarea"` → `title="Queue a message"`. Change icon `<Plus className="size-4" />` → `<ListPlus className="size-4" />`. Behavior (onClick `setQueueSlots((prev) => [...prev, ...])`) untouched.

**Edit 3 — Interrupt button tooltip (find via grep `Interrupt.*Ctrl-C` in ComposeBox.tsx).** Change `aria-label="Interrupt (send Ctrl-C)"` → `aria-label="Interrupt"`. Change `title="Interrupt (Ctrl-C)"` → `title="Interrupt"`.

**Edit 4 — Thumbs-up button (find via grep `handleQuickSend\("let's go"\)` in ComposeBox.tsx).** Change `handleQuickSend("let's go")` → `handleQuickSend("thumbs up")`. Change `aria-label="Send 'let's go'"` → `aria-label="Send 'thumbs up'"`. Change `title="Send 'let's go'"` → `title="Send 'thumbs up'"`.

**Edit 5 — Recap button, was Explain (find via grep `handleQuickSend\("/explain concisely` in ComposeBox.tsx).** Change the entire long payload string `"/explain concisely whatever's gone on since my last message (and ONLY since my last message) without using code symbols, in a conceptual model style. Not a metaphor and don't recast it as an extended analogy."` → `"/explain the current situation"`. Change `aria-label="Ask for a concise re-explanation"` → `aria-label="Recap the current situation"`. Change `title="Ask for a concise re-explanation"` → `title="Recap"`. Change icon `<Lightbulb className="size-4" />` → `<CircleHelp className="size-4" />`.

**Edit 6 — DELETE the /bounty (Target) button entirely (find via grep `Target = "/bounty"` in ComposeBox.tsx).** Remove the entire `<Button>` element for the /bounty prefix-send button AND its preceding `{/* Quick 260730-l34 (patch #204): Target = "/bounty" prefix-send button ... */}` comment block. Rationale: superseded by the natural-language "bounty bounty" voice trigger shipped as patch #241 (composeIntentTransform). Verify Target is not referenced elsewhere in ComposeBox.tsx after deletion (that's why Edit 1 drops it from the lucide import).

**Edit 7 — DELETE the /queue (ListPlus prefix-send) button entirely (find via grep `ListPlus = "/queue"` in ComposeBox.tsx, immediately after the Bounty button which is also being removed).** Remove the entire `<Button>` element for the /queue prefix-send button AND its preceding `{/* Quick 260730-l34 (patch #204): ListPlus = "/queue" prefix-send button. NAMING COLLISION acknowledgment ... */}` comment block. Rationale: superseded by the queued-message textareas below. Note: after this deletion, ListPlus is used ONLY by the Edit-2 add-queued-textarea button — that's why Edit 1 keeps it in the import.

**Edit 8 — Reset button tooltip (find via grep `handleResetSend` in ComposeBox.tsx to locate the `<button>` in the meter well).** Change `aria-label="Send with /id reset prefix"` → `aria-label="Reset context window"`. Change `title="Send with /id reset prefix"` → `title="Reset context window"`.

**Edit 9 — ChatMessage.tsx isQuickReply narrowing (find via grep `isQuickReply` in ChatMessage.tsx, ~L170-183).** Replace the entire IIFE block (comment + const) with the narrowed single-equality version specified verbatim in the design context: the new comment references "bounty `thumbs-up-button-rename-and-bubble-cleanup` (2026-08-01)" and the new predicate reads `content.trim().toLowerCase() === "thumbs up"` — no more IIFE. Full replacement text is in the plan's design_context §9 — paste verbatim.

**Edit 10 — Test file updates.** Five files, each with narrow targeted diffs:

  (10a) `ComposeBox.test.tsx`:
    - Grep all 3 occurrences of `send 'let's go'` (case-insensitive regexes in `getByLabelText` calls) → replace with `send 'thumbs up'`.
    - Grep all 9 occurrences of `add queued message textarea` in `getByRole` name matchers → replace with `queue a message`.
    - Find the `describe("ComposeBox — quick 260730-l34: /bounty and /queue prefix buttons", ...)` block (roughly L843-906 in the working tree). Replace the entire describe block with the Vehicle-B removal-assertion describe block specified verbatim in design_context §10 first bullet (title: `"ComposeBox — Vehicle B strip: /queue and /bounty prefix buttons removed"`, with `beforeEach` clearing mocks + localStorage, then two `it()` cases asserting `screen.queryByLabelText(...)` returns null for the /queue and /bounty labels).

  (10b) `ComposeBox.aside-morph.test.tsx`:
    - Replace both occurrences of the regex `/send with \\/id reset prefix/i` in `getByLabelText` → `/reset context window/i`.
    - Replace occurrence(s) of `send 'let's go'` in `getByLabelText` → `send 'thumbs up'`.
    - REMOVE Task 1 Test 7 (bounty button disabled test — button no longer exists) AND Task 1 Test 8 (queue button disabled test — button no longer exists). Replace both `it(...)` blocks with the single explanatory comment block specified verbatim in design_context §10 second bullet ("Task 1 Test 7 REMOVED..." / "Task 1 Test 8 REMOVED..." block).

  (10c) `ComposeBox.recycle-disable.test.tsx`:
    - In the ~L99-101 block: `screen.getByLabelText("Send with /id reset prefix")` → `screen.getByLabelText("Reset context window")`; `screen.getByLabelText("Send 'let's go'")` → `screen.getByLabelText("Send 'thumbs up'")`; `screen.getByLabelText("Ask for a concise re-explanation")` → `screen.getByLabelText("Recap the current situation")`. Grep first to confirm exact-match strings.

  (10d) `PrettyView.test.tsx` (~L700-702): change the querySelector `'button[aria-label*="/id reset" i]'` → `'button[aria-label*="reset context window" i]'`.

  (10e) `ChatMessage.test.tsx` (~L125-143): the current three tests for "good to go" / "works for me" / "go ahead" as quick-reply matches ARE BROKEN by the Edit-9 narrowing (they now render plain-text bubbles). Replace them with the six new `it()` cases specified verbatim in design_context §10 fifth bullet: Test 14 = positive match on "thumbs up"; Tests 14b/14c/14d/14e/14f = negative matches for the five stripped legacy payloads ('good to go', 'works for me', 'go ahead', 'yes', 'let\\'s go'). Each negative test asserts `screen.queryByLabelText(/quick reply/i)` returns null after rendering `<ChatMessage role="user" content="{payload}" />`.

Executor rule 1 (auto-fix): if any additional test not enumerated above breaks due to a stale label/payload string, apply the equivalent surgical rename (old label → new label from this vehicle's table) and note the file in the SUMMARY. Do NOT modify test intent — only the label strings that this vehicle renamed.

Executor rule 2 (no scope creep): stay inside `src/ui/features/pretty-view/`. Do NOT touch `~/.claude/identities/tina/*` — orchestrator handles patches.md + bounty archive AFTER this row per hn8 arc lesson. Do NOT touch backend, nginx configs, locale files, or any other subsystem — backend is not touched so `npm run build:backend` is SKIPPED per patch #154 rule.

Verification pipeline (all must pass before commit):
  1. `cd /home/ubuntu/skynet && npx tsc --noEmit` — MUST exit 0
  2. `cd /home/ubuntu/skynet && npx vitest run > /tmp/62m-vitest.log 2>&1` — MUST report 1049 passed / 6 skipped / 0 failed across 86 files (this is the exact baseline from the 2026-08-01 scratch validation round; if a different count comes back, investigate the delta before proceeding — DO NOT commit until reconciled)
  3. `grep -E "FAIL|failed|✗|Unhandled" /tmp/62m-vitest.log | grep -v "^\s*[0-9]" | grep -vE "0 failed|tests failed \(0\)"` — MUST be empty of real failure markers (per patch #209→#211 rule; runner's "0 failed" claim isn't authoritative when unhandled rejections tank tests silently)

After all three gates green, create a single atomic commit on the current branch (`feat/tab-title-from-tmux` per STATE.md most-recent-activity). Commit message shape follows recent entries in the fleet: subject line `feat(pretty-view): Vehicle B — ComposeBox aux-row cleanup + tooltips + thumbs-up rename + Explain→Recap + drop /queue and /bounty buttons`, body enumerates the 10 edit clusters with per-file counts + baseline reconciliation (1049 passed) + attribution to bounty `thumbs-up-button-rename-and-bubble-cleanup` + scratch/UAT validation timestamps.

NO push, NO `docker build`, NO `docker compose up -d --force-recreate skynet` — code-work greenlight authorizes commit only. NO git worktrees (config `workflow.use_worktrees: false`).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run > /tmp/62m-vitest.log 2>&amp;1 &amp;&amp; grep -qE "1049 passed" /tmp/62m-vitest.log &amp;&amp; ! grep -E "FAIL|Unhandled" /tmp/62m-vitest.log | grep -v "0 failed" | grep -qE "FAIL|Unhandled"</automated>
  </verify>
  <done>
- `npx tsc --noEmit` exits 0
- Full vitest suite reports 1049 passed / 6 skipped / 0 failed across 86 files (any delta MUST be reconciled before commit)
- Grep-gate on captured vitest log is clean of real FAIL/failed/✗/Unhandled markers
- `grep -c "Target\|Lightbulb\|Plus" src/ui/features/pretty-view/ComposeBox.tsx | head` shows Target=0, Lightbulb=0, Plus=0 imports references outside comments (spot-check: run `grep -vE "^\s*//|^\s*\*|^\s*/\*" src/ui/features/pretty-view/ComposeBox.tsx | grep -cE "\bTarget\b|\bLightbulb\b|\bPlus\b"` — expect 0)
- `grep -c "handleQuickSend(\"thumbs up\")" src/ui/features/pretty-view/ComposeBox.tsx` = 1
- `grep -c "handleQuickSend(\"/explain the current situation\")" src/ui/features/pretty-view/ComposeBox.tsx` = 1
- `grep -c "\"/bounty\"\\|\"/queue\"" src/ui/features/pretty-view/ComposeBox.tsx` = 0 (both prefix buttons gone)
- `grep -c "Queue a message" src/ui/features/pretty-view/ComposeBox.tsx` >= 2 (aria-label + title on the add-queued-textarea button)
- `grep -c "Reset context window" src/ui/features/pretty-view/ComposeBox.tsx` >= 2 (aria-label + title on the reset button)
- ChatMessage.tsx isQuickReply block is a single `content.trim().toLowerCase() === "thumbs up"` check with the new bounty-attribution comment; the old IIFE with alt-matches is gone (verify with `grep -c "let's go" src/ui/features/pretty-view/ChatMessage.tsx` = 0)
- Single atomic commit landed on `feat/tab-title-from-tmux` touching exactly the 7 declared files (verify via `git show --name-only HEAD` after commit)
- NO push, NO docker build, NO deploy issued
- NO files outside `/home/ubuntu/skynet/` modified (specifically NOT `~/.claude/identities/tina/*`)
  </done>
</task>

</tasks>

<verification>
Global check after the single task lands:

- Production behavior spot-check via grep on ComposeBox.tsx (must all return matches):
  - `grep -q "aria-label=\"Queue a message\"" src/ui/features/pretty-view/ComposeBox.tsx`
  - `grep -q "aria-label=\"Interrupt\"" src/ui/features/pretty-view/ComposeBox.tsx`
  - `grep -q "aria-label=\"Send 'thumbs up'\"" src/ui/features/pretty-view/ComposeBox.tsx`
  - `grep -q "aria-label=\"Recap the current situation\"" src/ui/features/pretty-view/ComposeBox.tsx`
  - `grep -q "aria-label=\"Reset context window\"" src/ui/features/pretty-view/ComposeBox.tsx`
  - `grep -q "CircleHelp" src/ui/features/pretty-view/ComposeBox.tsx`
  - `grep -q "<ListPlus className=\"size-4\" />" src/ui/features/pretty-view/ComposeBox.tsx`

- Production behavior spot-check via grep on ChatMessage.tsx:
  - `grep -q 'content.trim().toLowerCase() === "thumbs up"' src/ui/features/pretty-view/ChatMessage.tsx`

- Test file spot-checks (each MUST return >=1 match):
  - `grep -q "queue a message" src/ui/features/pretty-view/ComposeBox.test.tsx`
  - `grep -q "Vehicle B strip: /queue and /bounty prefix buttons removed" src/ui/features/pretty-view/ComposeBox.test.tsx`
  - `grep -q "reset context window" src/ui/features/pretty-view/ComposeBox.aside-morph.test.tsx`
  - `grep -q "Reset context window" src/ui/features/pretty-view/ComposeBox.recycle-disable.test.tsx`
  - `grep -q "reset context window" src/ui/features/pretty-view/PrettyView.test.tsx`
  - `grep -q "Test 14b (Vehicle B strip)" src/ui/features/pretty-view/ChatMessage.test.tsx`

- Absence checks (each MUST return 0 matches):
  - `grep -c "let's go" src/ui/features/pretty-view/ComposeBox.tsx` = 0
  - `grep -c "let's go" src/ui/features/pretty-view/ChatMessage.tsx` = 0
  - `grep -c "Ask for a concise re-explanation" src/ui/features/pretty-view/ComposeBox.tsx` = 0
  - `grep -c "Add queued message textarea" src/ui/features/pretty-view/ComposeBox.tsx` = 0
  - `grep -c "Send with /id reset prefix" src/ui/features/pretty-view/ComposeBox.tsx` = 0

- Scope check: `git show --name-only HEAD` after commit lists exactly the 7 declared files, all under `src/ui/features/pretty-view/` — no other files, no identity-side files.
</verification>

<success_criteria>
- All ten edit clusters applied verbatim per design_context spec
- Full vitest suite = 1049 passed / 6 skipped / 0 failed across 86 files (matches scratch baseline)
- `npx tsc --noEmit` = EXIT 0
- Grep-gate on captured vitest log = clean of real failure markers
- Single atomic commit on `feat/tab-title-from-tmux` touching exactly the 7 declared files
- NO scope creep outside `src/ui/features/pretty-view/`
- NO push, NO docker build, NO deploy
- NO identity-side files touched (Tina handles orchestrator bookkeeping AFTER this row)
- Ships as the next patch (# TBD by Tina) in the batched #198→#241+ deploy queue that remains HELD until Ashley's ship word
</success_criteria>

<output>
Create `.planning/quick/260801-62m-vehicle-b-composebox-aux-row-cleanup-too/260801-62m-01-SUMMARY.md` when done, summarizing: files touched with per-file diff shape, vitest baseline reconciliation (before/after counts), commit SHA, scope-check output from `git show --name-only HEAD`, and any executor auto-fixes applied under rule 1.
</output>
