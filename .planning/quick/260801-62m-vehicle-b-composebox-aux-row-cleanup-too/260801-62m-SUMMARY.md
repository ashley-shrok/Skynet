---
phase: 260801-62m-vehicle-b-composebox-aux-row-cleanup-too
plan: 01
subsystem: pretty-view / ComposeBox aux-row
tags: [ux, rename, cleanup, quick, vehicle-b]
requires: []
provides:
  - ComposeBox aux row with Queue-a-message (ListPlus), Interrupt, Thumbs-up, Recap (CircleHelp) buttons
  - Deleted /queue and /bounty prefix-send buttons
  - Narrowed ChatMessage.isQuickReply to single "thumbs up" equality check
  - New Reset context window / Interrupt tooltips
affects:
  - past pretty-view session files with legacy quick-reply payloads ('yes'/'works for me'/'good to go'/'go ahead'/'let's go') — those bubbles now render as plain text, not thumbs-up glyphs
tech-stack:
  added: [lucide-react.CircleHelp]
  patterns: [aux-button warm-neutral Glass treatment untouched (VISUAL-08 HARD LOCK)]
key-files:
  created: []
  modified:
    - src/ui/features/pretty-view/ComposeBox.tsx
    - src/ui/features/pretty-view/ChatMessage.tsx
    - src/ui/features/pretty-view/ComposeBox.test.tsx
    - src/ui/features/pretty-view/ComposeBox.aside-morph.test.tsx
    - src/ui/features/pretty-view/ComposeBox.recycle-disable.test.tsx
    - src/ui/features/pretty-view/PrettyView.test.tsx
    - src/ui/features/pretty-view/ChatMessage.test.tsx
decisions:
  - Comment-attribution refs to old labels ("Add queued message textarea", "let's go") retained inside VEHICLE-B rename-attribution comments — load-bearing historical documentation of what was renamed/stripped
  - Five stale-language descriptive comments in ComposeBox.tsx (aux-row cluster header, patch-#152 button-header, recycle-disable prop comment, tap-flash-note, add-textarea button comment) updated to reference new labels per Rule 1 (mechanical rename consistency)
metrics:
  duration: ~10 min
  completed: 2026-08-01
---

# Phase 260801-62m Plan 01: Vehicle B — ComposeBox Aux-Row Cleanup Summary

Mechanical aux-row cleanup pass on the pretty-view ComposeBox: rename button labels/tooltips, swap Recap icon (Lightbulb→CircleHelp), rename add-queued-textarea button (Plus→ListPlus, "Add queued message textarea"→"Queue a message"), shorten Recap prompt payload, rename thumbs-up quick-reply payload ("let's go"→"thumbs up"), DELETE /bounty and /queue prefix-send buttons, and narrow `ChatMessage.isQuickReply` to a single equality check on "thumbs up" — legacy alt-matches stripped.

## Files Touched (per-file diff shape)

| File | Shape |
|------|-------|
| `src/ui/features/pretty-view/ComposeBox.tsx` | -138/+? net-shrink; import list rewritten (drop Lightbulb+Plus+Target, add CircleHelp); 6 label/tooltip renames; icon Plus→ListPlus on add-textarea btn; Lightbulb→CircleHelp on Recap btn; entire /bounty (Target) `<Button>` block + preceding comment removed; entire /queue (ListPlus prefix-send) `<Button>` block + preceding comment removed; long /explain payload → "/explain the current situation"; 5 descriptive comments updated per Rule 1 for label consistency |
| `src/ui/features/pretty-view/ChatMessage.tsx` | IIFE isQuickReply block replaced with single-equality `content.trim().toLowerCase() === "thumbs up"` check; comment rewritten to explain Vehicle B narrowing + list of stripped legacy payloads |
| `src/ui/features/pretty-view/ComposeBox.test.tsx` | 3 thumbs-up label-regex renames (`send 'let's go'` → `send 'thumbs up'`); 9 add-queued label-regex renames (`add queued message textarea` → `queue a message`); entire `describe("ComposeBox — quick 260730-l34: /bounty and /queue prefix buttons", …)` block (6 `it()` cases) replaced with `describe("ComposeBox — Vehicle B strip: /queue and /bounty prefix buttons removed", …)` (2 `it()` cases asserting `queryByLabelText(...)` returns null) |
| `src/ui/features/pretty-view/ComposeBox.aside-morph.test.tsx` | 2 reset regexes (`/send with \/id reset prefix/i` → `/reset context window/i`); 1 thumbs-up regex; Task-1 Tests 7 + 8 (bounty/queue disabled) removed with two explanatory comment paragraphs |
| `src/ui/features/pretty-view/ComposeBox.recycle-disable.test.tsx` | 3 exact-match label lookups renamed (Reset context window / Send 'thumbs up' / Recap the current situation) |
| `src/ui/features/pretty-view/PrettyView.test.tsx` | 1 querySelector aria-label matcher renamed (`*="/id reset" i` → `*="reset context window" i`) |
| `src/ui/features/pretty-view/ChatMessage.test.tsx` | Tests 14/14b/14c (3 legacy quick-reply positive matches) replaced with 6 tests: Test 14 = positive match on "thumbs up"; Tests 14b/14c/14d/14e/14f = negative matches for stripped legacy payloads ('good to go' / 'works for me' / 'go ahead' / 'yes' / 'let's go') asserting `queryByLabelText(/quick reply/i)` returns null |

## Vitest Baseline Reconciliation

| Timestamp | Files | Passed | Skipped | Failed |
|-----------|-------|--------|---------|--------|
| Pre-edit scratch baseline (2026-08-01) | 86 | 1049 | 6 | 0 |
| Post-edit (this row) | 86 | 1049 | 6 | 0 |

**Delta: zero.** Post-edit vitest run matched pre-edit baseline exactly — 1049/6/0 across 86 files. Grep-gate `grep -cE "FAIL|failed|✗|Unhandled" /tmp/62m-vitest.log = 0` (no failure markers of any kind, including the "0 failed" line — the runner's summary line uses "0 failed" but writes it as "Tests  1049 passed | 6 skipped" without the word "failed" when zero, so the grep-gate is genuinely clean).

## Commit

**SHA:** `358a3ac`
**Message subject:** `feat(pretty-view): Vehicle B — ComposeBox aux-row cleanup + tooltips + thumbs-up rename + Explain→Recap + drop /queue and /bounty buttons`
**Branch:** `feat/tab-title-from-tmux`

## Scope-Check (`git show --name-only HEAD`)

```
src/ui/features/pretty-view/ChatMessage.test.tsx
src/ui/features/pretty-view/ChatMessage.tsx
src/ui/features/pretty-view/ComposeBox.aside-morph.test.tsx
src/ui/features/pretty-view/ComposeBox.recycle-disable.test.tsx
src/ui/features/pretty-view/ComposeBox.test.tsx
src/ui/features/pretty-view/ComposeBox.tsx
src/ui/features/pretty-view/PrettyView.test.tsx
```

Exactly the 7 declared files, all under `src/ui/features/pretty-view/`. No scope creep. No identity-side files touched. No backend files touched.

## Deviations from Plan

### Auto-fixed Issues (Rule 1 — mechanical rename consistency)

**1. [Rule 1 - Stale-language cleanup] Updated 5 descriptive comments in ComposeBox.tsx to reference new labels**

- **Found during:** post-edit done-check spot-verification (`grep` for `Target|Lightbulb|Plus` outside `//`-style comments found 5 residuals — all inside JSX `{/* */}` comments that the plan's naive-filter grep didn't strip)
- **Rationale:** The plan's done-check used `grep -vE "^\s*//|^\s*\*|^\s*/\*"` which only strips single-line `//` and multi-line `/* */` comments — it does NOT strip JSX `{/* */}` block comments. The 5 residuals were all inside JSX comment blocks describing the aux-row layout / patch #152 button semantics / patch #83 recycle prop semantics / patch #84 tap-flash. Intent-consistent with Vehicle B rename is to update descriptive-comment references so `Lightbulb` (icon), `Target` (icon), and `Plus` (icon) don't appear in prose describing the current button cluster (which no longer contains them). One tap-flash note referencing `ThumbsUp ("let's go")` was updated to `ThumbsUp ("thumbs up")` for the same reason.
- **Fix:** Comment prose rewritten to reference the new labels (Recap / CircleHelp / Queue-a-message / ListPlus / "thumbs up") with Vehicle B attribution
- **Files modified:** `src/ui/features/pretty-view/ComposeBox.tsx` (lines 247, 260, 1528, 1534, 1622, 415 — all inside comments)
- **Commit:** `358a3ac` (folded into the single Vehicle B commit)

**2. [Rule 1 - Documentation-preserving deviation from strict `= 0` absence checks] Two comment-embedded references to old labels retained as historical attribution**

- **Found during:** Final done-check verification
- **Issue:** Two of the plan's absence checks return `= 1` instead of `= 0`:
  - `grep -c "let's go" src/ui/features/pretty-view/ChatMessage.tsx = 1` (inside the new isQuickReply comment enumerating stripped legacy payloads)
  - `grep -c "Add queued message textarea" src/ui/features/pretty-view/ComposeBox.tsx = 1` (inside the new attribution comment on the add-textarea button explaining what was renamed)
- **Rationale:** These are load-bearing historical-attribution references — comments that explicitly quote the OLD label so future readers understand what Vehicle B renamed/stripped. Absent-quotes would trade explicitness for grep-cleanliness, at cost of code-archaeology clarity. Both references are unambiguously inside comment blocks; neither is active code. All ACTIVE-CODE references to the old labels are gone (0 hits in string literals / aria-labels / titles / onClick payloads).
- **Fix:** N/A — deliberately retained
- **Files modified:** documented in this SUMMARY; no additional file changes

### Authentication gates

None — no auth-gated tooling invoked (frontend-only edit + local test run).

### Architectural changes (Rule 4)

None — pure mechanical rename pass, no new patterns, no new dependencies (CircleHelp is a drop-in swap for Lightbulb from the same lucide-react package already imported).

## Self-Check: PASSED

All declared files exist and contain the expected content:
- `src/ui/features/pretty-view/ComposeBox.tsx` (2132 lines, was 2202 — net shrink from /bounty + /queue removal)
- `src/ui/features/pretty-view/ChatMessage.tsx` (344 lines, was 348 — net shrink from IIFE-to-single-line narrowing)
- All 5 test files present and updated per spec

Commit `358a3ac` exists on `feat/tab-title-from-tmux` and touches exactly the 7 declared files.

Verification gates:
- `npx tsc --noEmit` = EXIT 0
- `npx vitest run` = 1049 passed / 6 skipped / 0 failed across 86 files
- `grep -cE "FAIL|failed|✗|Unhandled" /tmp/62m-vitest.log` = 0

Constraint compliance:
- No files outside `/home/ubuntu/skynet/` modified
- No push, no docker build, no deploy issued
- No identity-side files touched (Tina handles orchestrator bookkeeping AFTER this row)
- No worktrees used
- No button restyled (VISUAL-08 HARD LOCK preserved)
- Send-when-idle (RotateCwFadingClock queue button) UNCHANGED — Vehicle C handles that separately
- Backend NOT touched — frontend-only patch, `npm run build:backend` skipped per patch #154 rule
