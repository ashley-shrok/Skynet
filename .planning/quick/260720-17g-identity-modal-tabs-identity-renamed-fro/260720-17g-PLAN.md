---
phase: 260720-17g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/claude-session/claude-session-server.ts
  - src/ui/api/claude-session-api.ts
  - src/ui/features/pretty-view/IdentityModal.tsx
  - src/ui/features/pretty-view/IdentityFileTab.tsx
  - src/ui/features/pretty-view/HistoryTab.tsx
  - src/ui/features/pretty-view/WakeupsTab.tsx
  - src/ui/features/pretty-view/HandoffTab.tsx
autonomous: true
requirements:
  - Q-17g-01
  - Q-17g-02
  - Q-17g-03
  - Q-17g-04
  - Q-17g-05
must_haves:
  truths:
    - "IdentityModal opens with 'Identity' tab as position 1 AND as the default active tab"
    - "Identity tab renders the whole ~/.claude/identities/<key>/<key>.md file as markdown (Inter font + dark:prose-invert), user scrolls / Ctrl+F to navigate the ~675-line surface"
    - "Bounties tab still renders exactly like patch #87 — same BountyCard, same accordion, same fetch flow — just moved to position 2"
    - "History tab renders one row per non-blank / non-comment history.md line in reverse-file order (most recent first), each row parsed into date/gist/slug chips with a graceful fallback for lines that don't match the format"
    - "Wakeups tab renders one card per <key>/wakeups/*.json with name, enabled chip (green 'on' / muted 'off'), server-humanized schedule string, and instruction prose"
    - "Handoff tab renders ~/.claude/identities/<key>/handoff.md as markdown; missing file shows 'no handoff carry (fresh session or first run)'"
    - "All 4 new artifacts fetch in parallel when the modal opens (single useEffect firing 4 requests, not lazy-per-tab-click); each tab has independent loading/error/empty state so one broken artifact does not take down the others"
    - "All 4 new backend handlers resolve identitiesRoot via `process.env.IDENTITIES_HOST_DIR || path.join(os.homedir(), '.claude', 'identities')` (patch #89 pattern) and constrain reads to whitelisted per-handler file paths (no user-controlled path traversal)"
  artifacts:
    - path: "src/backend/claude-session/claude-session-server.ts"
      provides: "4 new WS message handlers: identity:get-identity-file, identity:get-history, identity:list-wakeups, identity:get-handoff — each mirrors patch #87 identity:list-bounties handler shape (identityKey validation via IDENTITY_KEY_RE, IDENTITIES_HOST_DIR resolution, ENOENT → empty response, defensive try/catch per file, sshLogger.info on success + sshLogger.error on parse failures without poisoning response)"
      contains: "identity:get-identity-file"
    - path: "src/ui/api/claude-session-api.ts"
      provides: "4 new WS payload + event type pairs mirroring IdentityListBountiesPayload / IdentityBountiesEvent, plus Wakeup type; all 4 new events added to ClaudeSessionServerEvent union"
      exports:
        - "IdentityGetIdentityFilePayload"
        - "IdentityIdentityFileEvent"
        - "IdentityGetHistoryPayload"
        - "IdentityHistoryEvent"
        - "IdentityListWakeupsPayload"
        - "IdentityWakeupsEvent"
        - "IdentityGetHandoffPayload"
        - "IdentityHandoffEvent"
        - "Wakeup"
    - path: "src/ui/features/pretty-view/IdentityFileTab.tsx"
      provides: "Renders markdown string via ReactMarkdown + remarkGfm, applying same prose scaffolding as ChatMessage.tsx (Inter font, dark:prose-invert, prose-code JetBrains Mono override); handles loading/error/empty independently"
    - path: "src/ui/features/pretty-view/HistoryTab.tsx"
      provides: "Renders string[] as a list; each entry parsed on `·` separator into date (muted), gist (main), slugs (pill chips visible but not clickable in v1); fallback to raw text when line doesn't match format"
    - path: "src/ui/features/pretty-view/WakeupsTab.tsx"
      provides: "Renders Wakeup[] as cards using the same glass token family as BountyCard (identity-hue gradient, backdrop-blur, Inter font); shows name, enabled chip, scheduleHuman, instruction"
    - path: "src/ui/features/pretty-view/HandoffTab.tsx"
      provides: "Renders markdown string via same ReactMarkdown scaffold as IdentityFileTab; handles ENOENT with 'no handoff carry (fresh session or first run)'"
    - path: "src/ui/features/pretty-view/IdentityModal.tsx"
      provides: "Reordered TabsList: Identity / Bounties / History / Wakeups / Handoff; activeTab default now 'identity'; single useEffect now dispatches 5 parallel WS requests (1 existing bounties + 4 new artifacts) with independent per-tab state slots; existing bounties render path preserved byte-for-byte"
      contains: "value=\"identity\""
  key_links:
    - from: "src/ui/features/pretty-view/IdentityModal.tsx"
      to: "/claude-session/websocket/"
      via: "5 parallel WS one-shot requests fired in single useEffect on modal open"
      pattern: "identity:get-identity-file|identity:get-history|identity:list-wakeups|identity:get-handoff"
    - from: "src/backend/claude-session/claude-session-server.ts"
      to: "process.env.IDENTITIES_HOST_DIR || path.join(os.homedir(), '.claude', 'identities')"
      via: "identitiesRoot resolution in all 4 new handlers"
      pattern: "IDENTITIES_HOST_DIR"
    - from: "IdentityFileTab.tsx + HandoffTab.tsx"
      to: "react-markdown + remark-gfm"
      via: "same ReactMarkdown scaffold as ChatMessage.tsx"
      pattern: "ReactMarkdown"
---

<objective>
Populate the 4 placeholder tabs in the identity modal (Identity / History / Wakeups / Handoff), rename Standing Directives → Identity, and move Identity to tab position 1 as the default active tab. Bounties tab behavior is preserved byte-for-byte.

Purpose: The modal is Ashley's read-only window into each identity's live state. Post-patch-#87 the modal opens on Bounties but 4 of 5 tabs are "Coming soon" — this patch fills all 4 and elevates the identity's core file (`<name>.md`) to first-class default view so opening the modal shows "who this identity is" before "what they're working on."

Output: 4 new backend WS handlers + 4 new API type pairs + 4 new tab component files + wiring changes in IdentityModal.tsx. Ships as commits stacking on top of the #82-#89 stack; deploys together when Ashley greenlights.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — add 4 identity-artifact WS handlers + humanize helper</name>
  <files>src/backend/claude-session/claude-session-server.ts</files>
  <read_first>
    - src/backend/claude-session/claude-session-server.ts (lines 985-1180 — the existing `identity:list-bounties` handler is the pattern-mirror for all 4 new handlers; note the `IDENTITY_KEY_RE = /^[a-z0-9_-]{1,64}$/` validator, `identitiesRoot = process.env.IDENTITIES_HOST_DIR || path.join(os.homedir(), '.claude', 'identities')` resolution from patch #89, ENOENT → empty response pattern, `sshLogger.info` on success and `sshLogger.error` on per-file parse failures without poisoning the aggregate response)
    - src/backend/claude-session/claude-session-server.ts (lines 1-80 — module-header wire-protocol doc block: the new message types must be added to this doc alongside the existing `identity:list-bounties` / `identity:bounties` entries)
    - ~/.claude/identities/tina/wakeups/daily-box-check.json (real sample wakeup — confirms the { name, enabled, schedule: { type: "daily", at: "14:00" }, instruction } shape; humanize must handle `interval` / `daily` / `weekly` variants per ~/.claude/skills/id/SKILL.md spec)
    - ~/.claude/identities/tina/history.md (real sample — first two lines are `#` comments, subsequent lines are `YYYY-MM-DD · gist · slugs: foo,bar`; file order is chronological append, so backend must reverse for most-recent-first)
    - ~/.claude/identities/tina/handoff.md (real sample — plain markdown, no special parsing)
  </read_first>
  <action>
Add 4 new WS message handlers to `src/backend/claude-session/claude-session-server.ts` immediately after the existing `identity:list-bounties` block (before the `if (msg.type !== "connectToPane")` unknown-type guard around line 1182). Each handler mirrors the patch #87 shape: reuse the `IDENTITY_KEY_RE = /^[a-z0-9_-]{1,64}$/` validator (extract to a module-level const at the top of the file if not already, otherwise inline per-handler consistent with patch #87); reuse `identitiesRoot = process.env.IDENTITIES_HOST_DIR || path.join(os.homedir(), '.claude', 'identities')`; whitelist each handler's file path (identity file = `<root>/<key>/<key>.md`, history = `<root>/<key>/history.md`, wakeups dir = `<root>/<key>/wakeups`, handoff = `<root>/<key>/handoff.md`) — do NOT accept any user-controlled path suffix.

Handler 1 — `identity:get-identity-file` → `identity:identity-file`:
- Path: `path.join(identitiesRoot, identityKey, identityKey + ".md")`.
- Read file as utf-8; ENOENT → respond `{ type: "identity:identity-file", markdown: "" }` (empty, not error — frontend renders empty state).
- Success: `{ type: "identity:identity-file", markdown: <contents> }`.
- Errors other than ENOENT: `{ type: "identity:identity-file", markdown: "", error: <message> }`; log via `sshLogger.error` with operation `identity_get_identity_file_error`.

Handler 2 — `identity:get-history` → `identity:history`:
- Path: `path.join(identitiesRoot, identityKey, "history.md")`.
- Read utf-8; ENOENT → `{ type: "identity:history", entries: [] }`.
- Split by `\n`, trim each line, filter out empty strings AND lines starting with `#`; reverse so most-recent-first (file is chronologically append-only).
- Success: `{ type: "identity:history", entries: <string[]> }`.
- Other errors: `{ type: "identity:history", entries: [], error }`; log operation `identity_get_history_error`.

Handler 3 — `identity:list-wakeups` → `identity:wakeups`:
- Dir: `path.join(identitiesRoot, identityKey, "wakeups")`.
- `fs.readdir` the dir; ENOENT → `{ type: "identity:wakeups", wakeups: [] }`.
- Filter entries ending in `.json`; for each: read file, JSON.parse, extract `{ name: string, enabled: boolean, schedule: object, instruction: string }` with safe defaults (`name` fallback to the filename stem, `enabled` fallback to `false`, `instruction` fallback to `""`).
- Humanize schedule via a new module-level helper `humanizeWakeupSchedule(schedule: unknown): string`:
  - `{ type: "interval", every: "2h" }` → `"Every 2h"` (accepts `s`/`m`/`h`/`d` suffix; bare number → treat as minutes and render `"Every {n}m"`).
  - `{ type: "daily", at: "09:00" }` → `"Daily at 09:00 (box-local)"`.
  - `{ type: "weekly", day: "mon", at: "09:00" }` → `"Weekly on Mon at 09:00 (box-local)"` (title-case the day abbreviation; unknown day → render raw).
  - Unrecognized shape → `"custom schedule"`.
- Per-file try/catch: parse failures logged via `sshLogger.error` with operation `identity_list_wakeups_parse_error` and the filePath, but skipped from the response — mirrors the bounties defensive pattern (one bad JSON must not poison the list).
- Success: `{ type: "identity:wakeups", wakeups: [{ name, enabled, scheduleHuman, instruction }, ...] }`.
- Other errors: `{ type: "identity:wakeups", wakeups: [], error }`; log operation `identity_list_wakeups_error`.

Handler 4 — `identity:get-handoff` → `identity:handoff`:
- Path: `path.join(identitiesRoot, identityKey, "handoff.md")`.
- Read utf-8; ENOENT → `{ type: "identity:handoff", markdown: "" }` (frontend maps empty string → "no handoff carry" state).
- Success: `{ type: "identity:handoff", markdown: <contents> }`.
- Other errors: `{ type: "identity:handoff", markdown: "", error }`; log operation `identity_get_handoff_error`.

For all 4: identityKey validation failure → respond with the empty shape plus `error: "invalid identityKey"` (same behavior as patch #87 for the bounties handler). All 4 use identical `sshLogger.info` success logging with `operation` set to `identity_get_identity_file` / `identity_get_history` / `identity_list_wakeups` / `identity_get_handoff` and identity key + payload size.

Also update the module-level JSDoc wire-protocol block (lines 19-45) to add all 4 client-side and 4 server-side new message-type entries in the same style as the existing `identity:list-bounties` / `identity:bounties` entries; add a one-line comment `// patch #17g:` prefix on each new entry for grep-locatability.
  </action>
  <verify>
    <automated>cd /home/ubuntu/termix && grep -c 'identity:get-identity-file\|identity:get-history\|identity:list-wakeups\|identity:get-handoff' src/backend/claude-session/claude-session-server.ts | awk '$1 >= 8 {exit 0} {exit 1}' && grep -q 'humanizeWakeupSchedule' src/backend/claude-session/claude-session-server.ts && grep -q "process.env.IDENTITIES_HOST_DIR" src/backend/claude-session/claude-session-server.ts && npm run build 2>&1 | tail -20 | grep -E "built in|error" | head -5</automated>
  </verify>
  <done>
- All 4 new message-type strings appear in the file (at minimum: 1× request handler branch + 1× response send per handler = 8 occurrences total).
- `humanizeWakeupSchedule` helper defined at module scope.
- All 4 handlers resolve `identitiesRoot` via `process.env.IDENTITIES_HOST_DIR || path.join(os.homedir(), '.claude', 'identities')` — no bare `os.homedir()` in the new code.
- All 4 handlers whitelist their exact file path (no user-controlled path segments beyond the validated `identityKey`).
- ENOENT on the target file/dir returns the empty payload shape (never error).
- Per-file parse errors (wakeup JSON) are logged and skipped, not propagated to the aggregate response.
- Module-header wire-protocol JSDoc lists all 4 new client + server message types.
- `npm run build` completes without TypeScript errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend — 4 new WS type pairs in claude-session-api.ts + 4 new tab component files</name>
  <files>src/ui/api/claude-session-api.ts, src/ui/features/pretty-view/IdentityFileTab.tsx, src/ui/features/pretty-view/HistoryTab.tsx, src/ui/features/pretty-view/WakeupsTab.tsx, src/ui/features/pretty-view/HandoffTab.tsx</files>
  <read_first>
    - src/ui/api/claude-session-api.ts (lines 155-193 — patch #87 defined `IdentityListBountiesPayload` + `IdentityBountiesEvent` + added the event to `ClaudeSessionServerEvent` union at line 147; mirror this shape for all 4 new pairs)
    - src/ui/features/pretty-view/BountyCard.tsx (the glass-token card treatment to mirror for WakeupsTab card: `rounded-[var(--radius-pv-bubble)]`, `backdrop-blur-lg saturate-[1.3]`, `font-[Inter_Variable,...]`, hue-tinted `linear-gradient(160deg, hsla(${hue}, 40%, 22%, 0.5), hsla(${hue}, 35%, 14%, 0.55))`, border `hsla(${hue}, 60%, 50%, 0.24)`)
    - src/ui/features/pretty-view/ChatMessage.tsx (lines 1-40 imports + lines 60-130 render — the ReactMarkdown scaffold to mirror for IdentityFileTab + HandoffTab: `import ReactMarkdown from "react-markdown"` + `import remarkGfm from "remark-gfm"`, the prose class chain `prose prose-sm max-w-none font-[Inter_Variable,...] dark:prose-invert prose-code:...` and the same anchor-target-_blank component override)
    - ~/.claude/skills/id/SKILL.md (canonical history.md format spec: `YYYY-MM-DD · gist · slugs: foo,bar` where the slugs field is optional; parser must handle 2-field and 3-field variants)
  </read_first>
  <action>
Step A — extend `src/ui/api/claude-session-api.ts`:

Add 4 new type-pair blocks after the existing `IdentityBountiesEvent` type (around line 192), mirroring the existing patch #87 comment-then-types layout. Add a `// Patch #17g:` prefix comment before each block for grep-locatability:

```
export type IdentityGetIdentityFilePayload = { type: "identity:get-identity-file"; identityKey: string };
export type IdentityIdentityFileEvent = { type: "identity:identity-file"; markdown: string; error?: string };

export type IdentityGetHistoryPayload = { type: "identity:get-history"; identityKey: string };
export type IdentityHistoryEvent = { type: "identity:history"; entries: string[]; error?: string };

export type Wakeup = { name: string; enabled: boolean; scheduleHuman: string; instruction: string };
export type IdentityListWakeupsPayload = { type: "identity:list-wakeups"; identityKey: string };
export type IdentityWakeupsEvent = { type: "identity:wakeups"; wakeups: Wakeup[]; error?: string };

export type IdentityGetHandoffPayload = { type: "identity:get-handoff"; identityKey: string };
export type IdentityHandoffEvent = { type: "identity:handoff"; markdown: string; error?: string };
```

Add all 4 new event types (`IdentityIdentityFileEvent`, `IdentityHistoryEvent`, `IdentityWakeupsEvent`, `IdentityHandoffEvent`) to the `ClaudeSessionServerEvent` discriminated union at line 133–147, keeping the existing `IdentityBountiesEvent` line last-added-at-a-time style consistent.

Step B — create `src/ui/features/pretty-view/IdentityFileTab.tsx`:

Standalone React component `export function IdentityFileTab({ state }: { state: TabState<string> })` where `TabState<T> = { status: "loading" | "ready" | "error"; data?: T; error?: string }` — define `TabState<T>` in this file as a local exported type so all 4 tabs share the shape (import from here in the other 3 tab files).

Renders based on `state.status`:
- `loading`: 3 `Skeleton` placeholder blocks (same `Skeleton` primitive used in IdentityModal Bounties tab).
- `error`: `<div className="text-sm text-destructive">Couldn't load identity file: {state.error}</div>` (no retry — parallel fetch means retry logic lives per-tab in the modal if needed later; v1 shows the error).
- `ready` + empty markdown: `<div className="text-sm text-[var(--color-pv-fg-muted)]">No identity file found for this identity.</div>`.
- `ready` + non-empty markdown: `ReactMarkdown` wrapping the markdown with same prose classes as `ChatMessage.tsx` assistant bubble (`prose prose-sm max-w-none font-[Inter_Variable,ui-sans-serif,system-ui,sans-serif] dark:prose-invert` + the `prose-pre:...`, `prose-code:...` chain from ChatMessage lines 71-81), passing `remarkGfm` in `remarkPlugins`, and the same `a` component override (`target="_blank"` `rel="noopener noreferrer"`).
- Wrap in `<div className="prose ... overflow-y-auto h-full px-1">` so the ~675-line file scrolls inside the tab pane. Do NOT introduce section-jump nav (Ctrl+F is v1 per brief).

Step C — create `src/ui/features/pretty-view/HistoryTab.tsx`:

`export function HistoryTab({ state }: { state: TabState<string[]> })`. Loading = 3 skeleton rows. Error = `<div className="text-sm text-destructive">Couldn't load history: {state.error}</div>`. Empty entries = `<div className="text-sm text-[var(--color-pv-fg-muted)]">No history yet.</div>`. Ready:

Parse each entry via a local `parseHistoryLine(line: string): { date?: string; gist: string; slugs: string[] }` helper: split on ` · ` (space-middle-dot-space); if 2 parts → date + gist, slugs = []; if 3+ parts → first = date, second = gist, third = slug field `slugs: foo,bar` (strip the `slugs:` prefix, split remaining on `,`, trim + drop empties); if 1 part or split fails → return `{ gist: line, slugs: [] }` (fallback preserves raw line).

Render as a `<ul className="flex flex-col gap-2">` where each row is:
- Date: `<span className="text-xs text-[var(--color-pv-fg-dim)] font-mono shrink-0">{date}</span>` (only when present).
- Gist: `<span className="text-sm text-[#e8e4d8]/90 flex-1">{gist}</span>`.
- Slug chips: each slug rendered as `<span className="px-2 py-0.5 rounded-full text-[10px] bg-white/[0.08] border border-white/10 text-[#a89a80]">{slug}</span>` (visible but not clickable per non-goals).

Wrap the whole row in a `flex items-start gap-2 flex-wrap py-1 border-b border-white/[0.04]` so entries are visually separated but not heavy.

Step D — create `src/ui/features/pretty-view/WakeupsTab.tsx`:

`export function WakeupsTab({ state, hue }: { state: TabState<Wakeup[]>; hue: number })` (needs `hue` for the card gradient). Loading = 3 skeleton cards. Error = `<div className="text-sm text-destructive">Couldn't load wakeups: {state.error}</div>`. Empty = `<div className="text-sm text-[var(--color-pv-fg-muted)]">No scheduled wake-ups.</div>`. Ready:

`<div className="flex flex-col gap-3">` with one card per wakeup. Card mirrors BountyCard's outer treatment (byte-identical class + style tokens: `rounded-[var(--radius-pv-bubble)] px-4 py-3 flex flex-col gap-2 backdrop-blur-lg saturate-[1.3] [-webkit-backdrop-filter:blur(16px)_saturate(1.3)] font-[Inter_Variable,ui-sans-serif,system-ui,sans-serif]` + the hue linear-gradient background + hue border + boxShadow from BountyCard lines 102-114). Card content:
- Row 1: name (`font-semibold text-[15px] text-[#f0ebe0] flex-1`) + enabled chip: when `enabled=true` render `<span className="px-2 py-0.5 rounded-full text-[11px] font-medium uppercase tracking-wide bg-emerald-500/25 text-emerald-200 border border-emerald-500/40">on</span>`; when `false` render `<span className="... bg-slate-500/25 text-slate-300 border border-slate-500/40">off</span>` (mirror BountyCard STATUS pill shape).
- Row 2: schedule human, `<span className="text-xs text-[var(--color-pv-fg-muted)] font-mono">{scheduleHuman}</span>`.
- Row 3: instruction, `<div className="whitespace-pre-wrap text-sm text-[#e8e4d8]/90 leading-relaxed">{instruction}</div>` (plain prose, no markdown — wakeup instruction is free text).

Step E — create `src/ui/features/pretty-view/HandoffTab.tsx`:

`export function HandoffTab({ state }: { state: TabState<string> })`. Loading / error identical shape to IdentityFileTab. Empty (markdown === "") → `<div className="text-sm text-[var(--color-pv-fg-muted)]">No handoff carry (fresh session or first run).</div>`. Ready + non-empty → same `ReactMarkdown` scaffold as IdentityFileTab (share the exact prose class chain — copy-paste, not extract-to-shared: keeps each tab file self-contained and preserves the same "read like ChatMessage assistant bubble" pattern).

All 4 tab files import `Skeleton` from `@/components/skeleton`, `cn` from `@/lib/utils`; the two markdown tabs also import `ReactMarkdown` from `"react-markdown"` and `remarkGfm` from `"remark-gfm"` (already in package.json). If any shadcn primitive class needs an override that shadcn's base is fighting, use the `!` important suffix per patch #81 rule.
  </action>
  <verify>
    <automated>cd /home/ubuntu/termix && grep -q "IdentityGetIdentityFilePayload\|IdentityIdentityFileEvent" src/ui/api/claude-session-api.ts && grep -q "IdentityHistoryEvent\|IdentityWakeupsEvent\|IdentityHandoffEvent" src/ui/api/claude-session-api.ts && grep -q "export type Wakeup" src/ui/api/claude-session-api.ts && test -f src/ui/features/pretty-view/IdentityFileTab.tsx && test -f src/ui/features/pretty-view/HistoryTab.tsx && test -f src/ui/features/pretty-view/WakeupsTab.tsx && test -f src/ui/features/pretty-view/HandoffTab.tsx && grep -q "ReactMarkdown" src/ui/features/pretty-view/IdentityFileTab.tsx && grep -q "ReactMarkdown" src/ui/features/pretty-view/HandoffTab.tsx && grep -q "parseHistoryLine\| · " src/ui/features/pretty-view/HistoryTab.tsx && grep -q "scheduleHuman" src/ui/features/pretty-view/WakeupsTab.tsx && npm run build 2>&1 | tail -20 | grep -E "built in|error" | head -5</automated>
  </verify>
  <done>
- All 4 new payload/event type pairs + `Wakeup` type exported from `src/ui/api/claude-session-api.ts`.
- All 4 new event types added to `ClaudeSessionServerEvent` union.
- All 4 tab files created at the specified paths.
- IdentityFileTab + HandoffTab use `ReactMarkdown` with `remarkGfm` and the ChatMessage prose class chain.
- HistoryTab parses `·`-delimited lines with fallback to raw text for non-matching lines.
- WakeupsTab renders `scheduleHuman` (does NOT re-humanize client-side) and cards use BountyCard's glass token family with `hue`.
- All 4 tabs share the same `TabState<T>` shape and independently render loading / error / empty / ready states.
- `npm run build` passes with no TypeScript errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: IdentityModal wiring — tab reorder + rename + parallel-fetch of 4 new artifacts</name>
  <files>src/ui/features/pretty-view/IdentityModal.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/IdentityModal.tsx (whole file — the useEffect at lines 97-168 is the fetch pattern to extend; the TabsList at lines 278-284 is what reorders + renames; the 4 placeholder TabsContent blocks at lines 365-388 are what get replaced with the real tab components; the Bounties TabsContent block at lines 287-362 must NOT change apart from tab position)
    - src/ui/api/claude-session-api.ts (the 4 new payload/event types from Task 2 + existing `openClaudeSessionSocket()` helper — each new artifact fetch opens its own WebSocket and closes after response, same one-shot pattern as bounties)
    - src/ui/features/pretty-view/IdentityFileTab.tsx / HistoryTab.tsx / WakeupsTab.tsx / HandoffTab.tsx (the props each tab expects: all take `{ state: TabState<T> }`; WakeupsTab additionally takes `hue`)
  </read_first>
  <action>
Modify `src/ui/features/pretty-view/IdentityModal.tsx`:

Change 1 — imports:
Add imports for the 4 new tab components at the top (after the existing `BountyCard` import): `import { IdentityFileTab, type TabState } from "./IdentityFileTab";`, `import { HistoryTab } from "./HistoryTab";`, `import { WakeupsTab } from "./WakeupsTab";`, `import { HandoffTab } from "./HandoffTab";`.
Add imports for the 4 new type pairs from `@/api/claude-session-api`: `IdentityGetIdentityFilePayload`, `IdentityIdentityFileEvent`, `IdentityGetHistoryPayload`, `IdentityHistoryEvent`, `IdentityListWakeupsPayload`, `IdentityWakeupsEvent`, `IdentityGetHandoffPayload`, `IdentityHandoffEvent`, `Wakeup`.

Change 2 — default activeTab:
`const [activeTab, setActiveTab] = useState("bounties")` → `useState("identity")`. Identity is now position 1 and the default.

Change 3 — add 4 new state slots:
```
const [identityFileState, setIdentityFileState] = useState<TabState<string>>({ status: "loading" });
const [historyState, setHistoryState] = useState<TabState<string[]>>({ status: "loading" });
const [wakeupsState, setWakeupsState] = useState<TabState<Wakeup[]>>({ status: "loading" });
const [handoffState, setHandoffState] = useState<TabState<string>>({ status: "loading" });
```
Keep the existing bounties state (`bounties`, `archivedBounties`, `loading`, `error`, `refetchKey`) unchanged — Bounties tab is preserved byte-for-byte per the constraint.

Change 4 — parallel fetch in the useEffect:
Extend the existing useEffect (lines 97-168) so it ALSO opens 4 new one-shot WebSockets in parallel (in addition to the existing bounties WS, not replacing it). Reset all 4 new state slots to `{ status: "loading" }` at the top of the effect alongside the bounties reset.

Define a local helper inside the effect: `openOneShot<Req extends { type: string }, Res>(request: Req, expectedType: string, onSuccess: (data: Res) => void, onError: (err: string) => void)` that: opens `openClaudeSessionSocket()`, sends the request onopen, on message parses JSON and checks `raw.type === expectedType` — if yes calls `onSuccess(raw as Res)` and closes the WS; on error / close-before-response calls `onError("Connection failed")`. Register a per-request cancelled flag alongside the outer cancelled flag so cleanup closes all 5 sockets.

Fire the 4 new fetches immediately after the bounties fetch is set up, all with the validated `identity.identityKey`:
- `openOneShot({ type: "identity:get-identity-file", identityKey } as IdentityGetIdentityFilePayload, "identity:identity-file", (ev: IdentityIdentityFileEvent) => setIdentityFileState({ status: ev.error ? "error" : "ready", data: ev.markdown, error: ev.error }), (e) => setIdentityFileState({ status: "error", error: e }))`.
- Same shape for `identity:get-history` → `identity:history` (state = entries), `identity:list-wakeups` → `identity:wakeups` (state = wakeups), `identity:get-handoff` → `identity:handoff` (state = markdown).

Extend the cleanup return to close each of the 4 new sockets in try/catch alongside the bounties socket close.

Change 5 — TabsList reorder + rename:
Replace the TabsList block (lines 278-284) with the new order Identity / Bounties / History / Wakeups / Handoff:
```
<TabsTrigger value="identity">Identity</TabsTrigger>
<TabsTrigger value="bounties">Bounties</TabsTrigger>
<TabsTrigger value="history">History</TabsTrigger>
<TabsTrigger value="wakeups">Wakeups</TabsTrigger>
<TabsTrigger value="handoff">Handoff</TabsTrigger>
```
The old `value="directives"` trigger is REMOVED entirely (rename absorbs it — the Standing Directives tab does not exist anymore; the identity file IS the standing directives surface plus everything else).

Change 6 — TabsContent replacements:
- Add a new `<TabsContent value="identity" className="flex-1 min-h-0 overflow-y-auto px-6 py-4"><IdentityFileTab state={identityFileState} /></TabsContent>` (place FIRST since it's the new default; visual order in JSX doesn't affect Tabs behavior but keeps the file readable).
- Bounties TabsContent block (lines 287-362): unchanged apart from being placed second in JSX order.
- Replace the "Coming soon" placeholder History block with `<TabsContent value="history" className="flex-1 min-h-0 overflow-y-auto px-6 py-4"><HistoryTab state={historyState} /></TabsContent>`.
- Replace the placeholder Wakeups block with `<TabsContent value="wakeups" className="flex-1 min-h-0 overflow-y-auto px-6 py-4"><WakeupsTab state={wakeupsState} hue={hue} /></TabsContent>`.
- Replace the placeholder Handoff block with `<TabsContent value="handoff" className="flex-1 min-h-0 overflow-y-auto px-6 py-4"><HandoffTab state={handoffState} /></TabsContent>`.
- REMOVE the `value="directives"` TabsContent block entirely.

Update the file-header comment block (lines 29-43) to note patch #17g: rename Standing Directives → Identity, promote Identity to position 1 + default, parallel fetch of 4 new artifacts, tab renderers extracted to sibling files. Leave patch #87 attribution intact for the bounties-side logic.
  </action>
  <verify>
    <automated>cd /home/ubuntu/termix && grep -q 'useState("identity")' src/ui/features/pretty-view/IdentityModal.tsx && grep -q 'value="identity"' src/ui/features/pretty-view/IdentityModal.tsx && ! grep -q 'value="directives"' src/ui/features/pretty-view/IdentityModal.tsx && grep -q "IdentityFileTab\|HistoryTab\|WakeupsTab\|HandoffTab" src/ui/features/pretty-view/IdentityModal.tsx && grep -q "identity:get-identity-file\|identity:get-history\|identity:list-wakeups\|identity:get-handoff" src/ui/features/pretty-view/IdentityModal.tsx && grep -q "BountyCard" src/ui/features/pretty-view/IdentityModal.tsx && grep -q 'Coming soon' src/ui/features/pretty-view/IdentityModal.tsx && echo "FAIL: Coming soon still present" && npm run build 2>&1 | tail -20 | grep -E "built in|error" | head -5</automated>
  </verify>
  <done>
- Default `activeTab` is `"identity"`.
- TabsList order is Identity / Bounties / History / Wakeups / Handoff.
- No `value="directives"` remains in the file (the Standing Directives tab is deleted, not hidden).
- All 4 new tab components are imported and rendered in their respective TabsContent blocks.
- Bounties TabsContent block (loading skeleton, error retry, empty state, grouped render, archive accordion, BountyCard usage) is unchanged in structure — patch #87 behavior preserved byte-for-byte.
- Single useEffect fires 5 parallel WS requests on modal open (bounties + 4 new artifacts).
- Each of the 4 new tabs has independent loading / error / empty / ready state populated from its own WS response.
- No `Coming soon` strings remain in the file.
- `npm run build` passes with no TypeScript errors.
  </done>
</task>

</tasks>

<verification>
End-to-end smoke (manual, post-`npm run build`):
1. Open the identity modal on the dev/staging build by clicking the lg IdentityBadge in PrettyView on a pane resolved to `tina` (real fixture identity with populated history + wakeups + handoff + tina.md).
2. Confirm modal opens with **Identity** tab visible + active by default (not Bounties).
3. Confirm the Identity tab renders ~675 lines of tina.md as markdown with Inter font + prose styling that matches assistant chat bubble aesthetic; Ctrl+F finds text within.
4. Click Bounties (position 2) — confirm identical layout + behavior to patch #87 (grouped open bounties, archive accordion, BountyCard visuals unchanged).
5. Click History — confirm rows are most-recent-first, date/gist/slug chips visible, no crash on the `slugs:` (empty) trailing entry from tina's history.md.
6. Click Wakeups — confirm daily-box-check card renders with name, green `on` chip, `Daily at 14:00 (box-local)` schedule string (server-humanized), and the full instruction prose.
7. Click Handoff — confirm handoff.md renders as markdown.
8. Close and re-open the modal on an identity without a wakeups dir (any bare identity) — confirm Wakeups tab shows "No scheduled wake-ups", not an error.
9. Grep-verify at the CLI:
   - `grep -c 'identity:get-identity-file\|identity:get-history\|identity:list-wakeups\|identity:get-handoff' src/backend/claude-session/claude-session-server.ts` reports >= 8.
   - `grep -q 'IDENTITIES_HOST_DIR' src/backend/claude-session/claude-session-server.ts` (patch #89 pattern preserved in new handlers).
   - `! grep -q 'value="directives"\|Coming soon' src/ui/features/pretty-view/IdentityModal.tsx` (Standing Directives placeholder fully removed).

No deploy. Commits stack on top of the existing patch-89 stack in `feat/tab-title-from-tmux` — ship as commits only per bundle-context in the brief. Ashley greenlights the whole stack (#82-#89 + #17g) together as one deploy batch.
</verification>

<success_criteria>
- 4 new backend WS handlers written, each mirroring patch #87's `identity:list-bounties` handler exactly (identity-key regex validation, IDENTITIES_HOST_DIR resolution via patch #89 env var, ENOENT-to-empty response, per-file defensive parse, structured logger success + error operations, whitelisted per-handler file path — no path traversal).
- 4 new API payload/event type pairs + `Wakeup` type added to `src/ui/api/claude-session-api.ts`; all 4 events added to `ClaudeSessionServerEvent` union.
- 4 new tab component files created, each self-contained with its own loading / error / empty / ready state renderer. Markdown tabs (Identity + Handoff) reuse react-markdown + remark-gfm + the ChatMessage prose class chain — no new markdown library, no shared abstraction layer.
- IdentityModal.tsx: default active tab is `identity`; TabsList order is Identity / Bounties / History / Wakeups / Handoff; `value="directives"` fully removed; single useEffect fires all 5 fetches in parallel with independent state slots; patch #87 bounties render path unchanged.
- `npm run build` clean.
- No deploy — commits only, stacking on the existing patch-89 stack in `feat/tab-title-from-tmux`.
</success_criteria>

<output>
Create `.planning/quick/260720-17g-identity-modal-tabs-identity-renamed-fro/260720-17g-SUMMARY.md` when done.
</output>
