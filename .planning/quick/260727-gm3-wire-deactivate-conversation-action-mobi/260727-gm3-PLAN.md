---
quick_id: 260727-gm3
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/state/conversation-store.ts
  - src/ui/state/conversation-store.test.ts
  - src/ui/features/pretty-conversations/DeactivateAction.tsx
  - src/ui/features/pretty-conversations/PrettyConversationRow.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
  - src/ui/features/pretty-conversations/pretty-conversations.css
  - src/ui/features/pretty-conversations/tokens.ts
  - src/ui/AppShell.tsx
autonomous: true
must_haves:
  truths:
    - "Clicking Deactivate on an active-set row calls removeFromActiveSet(id) AND closes its tab; the row recedes to ambient."
    - "Ambient (non-active-set) rows never render a DeactivateAction; RDP rows never render a DeactivateAction."
    - "Desktop active-set rows show DeactivateAction inside .pv-meta, positioned BEFORE (left of) PinAction; it is hidden until row is hovered/focus-within (mirrors pin hover-reveal)."
    - "Mobile active-set non-RDP rows expose BOTH PinAction and DeactivateAction inside the swipe-reveal strip; ambient mobile rows expose only PinAction."
    - "removeFromActiveSet(id) is idempotent, updates sessionStorage under the existing ACTIVE_SET_STORAGE_KEY, and notifies subscribers."
    - "PC_SWIPE_REVEAL widens to 132 and all three call sites (row body swipe transform + strip anchor + touch clamp) pick it up automatically."
  artifacts:
    - path: "src/ui/features/pretty-conversations/DeactivateAction.tsx"
      provides: "Deactivate button component (mirrors PinAction shape; renders lucide X)"
      exports: ["DeactivateAction"]
    - path: "src/ui/state/conversation-store.ts"
      provides: "removeFromActiveSet(id) mutator alongside addToActiveSet"
      contains: "export function removeFromActiveSet"
    - path: "src/ui/features/pretty-conversations/pretty-conversations.css"
      provides: ".pv-deactivate-action styles + desktop hover-reveal"
      contains: ".pv-deactivate-action"
  key_links:
    - from: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      to: "src/ui/state/conversation-store.ts#removeFromActiveSet"
      via: "handleRowDeactivate(row.id) → removeFromActiveSet + closeTab"
      pattern: "removeFromActiveSet\\("
    - from: "src/ui/AppShell.tsx"
      to: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      via: "onDeactivateRow prop wires closeTab into the panel"
      pattern: "onDeactivateRow"
    - from: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx"
      to: "src/ui/features/pretty-conversations/DeactivateAction.tsx"
      via: "renders <DeactivateAction /> in .pv-meta (desktop) AND swipe strip (mobile) when inActiveSet && !isRdp"
      pattern: "DeactivateAction"
---

<objective>
Wire the deactivate-conversation action end-to-end on active-set conversation rows.
Semantics: Deactivate is the pure REVERSE of tap-ambient-to-activate — click closes
the tab AND removes the id from activeSet; the row recedes to ambient. Agent keeps
running under the hood; tapping the row again reactivates it.

Purpose: Ashley's active-set was previously additive-only (Patch #137 §3 lock). This
plan adds the deactivate handle Ashley signed off on 2026-07-27 via the deactivate-
preview.js console snippet — subtle red-tinted X glyph mirroring the pin's palette-
driven treatment, matching hover-reveal (desktop) or side-by-side swipe strip (mobile).

Output:
  - New DeactivateAction.tsx component (visual mirror of PinAction with red-tinted palette + lucide X)
  - New removeFromActiveSet(id) store mutator (mirror of addToActiveSet at conversation-store.ts:703)
  - Row + panel wired to render + dispatch on active-set non-RDP rows only
  - PC_SWIPE_REVEAL widened 88 → 132 to fit two 36px tap targets
  - CSS hover-reveal for desktop, plus base .pv-deactivate-action styles
  - Full test coverage matching existing byte-lockstep Test-NN lineage
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

@src/ui/features/pretty-conversations/PinAction.tsx
@src/ui/features/pretty-conversations/PrettyConversationRow.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
@src/ui/features/pretty-conversations/pretty-conversations.css
@src/ui/features/pretty-conversations/tokens.ts
@src/ui/state/conversation-store.ts
@src/ui/state/conversation-store.test.ts
@src/ui/AppShell.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Store mutator (removeFromActiveSet) + DeactivateAction component + CSS + widened swipe reveal</name>
  <files>
    src/ui/state/conversation-store.ts,
    src/ui/state/conversation-store.test.ts,
    src/ui/features/pretty-conversations/DeactivateAction.tsx,
    src/ui/features/pretty-conversations/pretty-conversations.css,
    src/ui/features/pretty-conversations/tokens.ts
  </files>
  <behavior>
    - conversation-store: removeFromActiveSet("id-not-in-set") is a silent no-op — no notify, no sessionStorage write, activeSet reference unchanged.
    - conversation-store: removeFromActiveSet("id-in-set") removes id from activeSet, produces a NEW Set reference, writes JSON of the new set to sessionStorage under ACTIVE_SET_STORAGE_KEY, and calls notify (subscribers observe one snapshot flip).
    - conversation-store: removeFromActiveSet keeps state.selectedId untouched (deactivate does not implicitly deselect at the store layer — panel wires closeTab separately).
    - conversation-store: sessionStorage try/catch is silent — a throwing setItem still updates the in-memory set + notifies.
    - DeactivateAction: renders a <button type="button"> with className "pv-deactivate-action", data-size prop reflected as data-size attr, data-testid="deactivate-action" by default (overridable via prop), aria-label + title from i18n key "nav.conversations.deactivate" with defaultValue "Deactivate", contains a lucide <X /> icon.
    - DeactivateAction: onClick prop forwards the raw MouseEvent unchanged (mirrors PinAction — row owns stopPropagation).
    - tokens.ts: PC_SWIPE_REVEAL exported value === 132.
  </behavior>
  <action>
    Extend conversation-store.ts: directly BELOW the existing addToActiveSet function (which ends at ~L719), add:

      export function removeFromActiveSet(id: string): void {
        if (!state.activeSet.has(id)) return;              // idempotent no-op
        const nextActiveSet = new Set(state.activeSet);
        nextActiveSet.delete(id);
        try {
          if (typeof sessionStorage !== "undefined") {
            sessionStorage.setItem(
              ACTIVE_SET_STORAGE_KEY,
              JSON.stringify([...nextActiveSet]),
            );
          }
        } catch {
          // Silent — mirrors addToActiveSet's storage-failure policy.
        }
        state = { ...state, activeSet: nextActiveSet };
        notify();
      }

    Preserve the same header-comment shape as addToActiveSet (design-locked semantics: "silent try/catch on sessionStorage", "idempotent no-op when absent", "notify at end"). Do NOT touch selectedId — deactivation is a store-layer operation orthogonal to selection.

    Update the pre-existing Patch #137 header comment at conversation-store.ts:70 ("no remove API by design") AND the parallel note in conversation-store.test.ts's beforeEach (L69-71) to reflect that quick-260727-gm3 introduces the remove API. Update the JSDoc note above hydrateActiveSetFromStorage / around L109 that says "no remove API; dies on tab close" to instead say "add + remove APIs; dies on tab close".

    Extend conversation-store.test.ts by adding a new describe block AFTER the Test 30d/30e block (~L1057), following the "Test 30x — removeFromActiveSet" naming convention:
      - Test 30f: removeFromActiveSet(id) on an id not in the set is a no-op (no snapshot version bump; use __subscribeForTest to assert notify count).
      - Test 30g: removeFromActiveSet(id) on an id in the set removes it, produces a new Set reference, writes sessionStorage under ACTIVE_SET_STORAGE_KEY (assert sessionStorage.getItem returns JSON without the id), notifies once.
      - Test 30h: addToActiveSet(x) → removeFromActiveSet(x) → activeSet empty; subsequent addToActiveSet(x) succeeds (round-trip smoke test).
      - Test 30i: removeFromActiveSet(id) does not mutate state.selectedId (call selectConversation(x), removeFromActiveSet(x), assert useSelectedConversationId still returns x).
    Import removeFromActiveSet in the test file's top import block alongside addToActiveSet.

    Create src/ui/features/pretty-conversations/DeactivateAction.tsx by copying PinAction.tsx's shape verbatim then substituting:
      - Component name → DeactivateAction
      - Prop shape → { hue: number | null; size: "mobile" | "desktop"; onClick: (e: MouseEvent<HTMLButtonElement>) => void; "data-testid"?: string }  (drop `pinned`; deactivate has no toggled state)
      - Icon import → `import { X } from "lucide-react"` (drop Pin/PinOff)
      - Rendered icon → <X />
      - className → "pv-deactivate-action"
      - data-testid default → "deactivate-action"
      - i18n key → t("nav.conversations.deactivate", { defaultValue: "Deactivate" })
      - Preserve the same header comment structure (mirroring PinAction's docstring); document that the row owns stopPropagation and hover-reveal is CSS-driven; document that DeactivateAction renders ONLY when the row is in the active-set AND non-RDP (row-level policy, component is dumb visual).
    Do NOT add any store subscription, gesture handling, or select-coordination — this is a dumb visual, identical to PinAction's contract.

    Widen PC_SWIPE_REVEAL in src/ui/features/pretty-conversations/tokens.ts from 88 → 132. Update the JSDoc for the token to note the widened value fits two side-by-side tap targets (pin + deactivate) with comfortable margins, per Ashley 2026-07-27. Do NOT touch PC_SWIPE_THRESHOLD or PC_SWIPE_ANGLE_TOLERANCE.

    Extend pretty-conversations.css by appending a new block AFTER the existing pin-action block (which ends ~L583). The new block:
      1) `.pv-deactivate-action` base: same layout (display: inline-flex, align-items/justify-content center, padding 0, background transparent, border 0, cursor pointer, -webkit-tap-highlight-color transparent, width/height 20px, transition filter+color 120ms ease) AS `.pv-pin-action` — but with a RED-TINTED palette:
           color: hsla(4, 65%, 68%, 0.85);
           filter: drop-shadow(0 0 4px hsla(4, 75%, 60%, 0.45));
      2) `.pv-deactivate-action svg` → width/height 14px, stroke-width 2.
      3) `.pv-deactivate-action:hover` → color: hsla(4, 75%, 78%, 1); filter: drop-shadow(0 0 6px hsla(4, 75%, 60%, 0.65));
      4) `.pv-deactivate-action:focus-visible` → outline 2px solid hsla(4, 65%, 60%, 0.6); outline-offset 2px.
      5) Mobile size variant `.pv-deactivate-action[data-size="mobile"]` → width/height 32px; nested svg width/height 20px.
      6) Desktop hover-reveal (mirror pin's L491 + L581 rules verbatim, only class + selector chain differ). Add BOTH forms:
           .pv-row.pv-row--desktop.active-set:not(:hover):not(:focus-within) .pv-deactivate-action { display: none; }
           .pv-row.pv-row--desktop .pv-meta [data-testid="deactivate-action"] { opacity: 1; transition: opacity 120ms ease; }
         Also add the opacity-0-when-not-hovered symmetric rule mirroring pin's L491:
           .pv-row.pv-row--desktop.active-set:not(:hover) .pv-meta [data-testid="deactivate-action"] { opacity: 0; }
         Do NOT scope by `.pinned` — Deactivate is orthogonal to pin state; it's gated on `.active-set` instead.
      7) Ambient safety net (defense-in-depth: the row-level render should never emit DeactivateAction on ambient rows, but declare the guard so any regression stays invisible):
           .pv-row.pv-row--desktop:not(.active-set) .pv-deactivate-action { display: none; }
      8) RDP safety net:
           .pv-row.rdp .pv-deactivate-action { display: none; }
    Preserve palette rationale in a header comment above the block ("subtle red-tinted glyph mirroring pin's palette-driven treatment per Ashley 2026-07-27 preview lockdown; NOT a red circle background — bare icon + hue-drop-shadow, red tuned to hue=4 red-orange to sit adjacent to the pin's identity-hue without clashing").

    Add i18n key nav.conversations.deactivate → "Deactivate" to src/ui/locales/en.json. Do NOT touch other locale files (defaultValue fallback covers non-English until translations sync).
  </action>
  <verify>
    <automated>bunx vitest run src/ui/state/conversation-store.test.ts src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx --reporter=verbose &amp;&amp; bunx tsc --noEmit</automated>
  </verify>
  <done>
    - removeFromActiveSet exported from conversation-store.ts and covered by Tests 30f/30g/30h/30i.
    - DeactivateAction.tsx renders bare-icon lucide X inside a <button> with class .pv-deactivate-action, data-size attr, data-testid="deactivate-action" (default), and forwards onClick.
    - .pv-deactivate-action CSS block added, including desktop hover-reveal (active-set-gated), red-tinted palette, and RDP/ambient safety nets.
    - PC_SWIPE_REVEAL === 132 in tokens.ts.
    - Existing pin/panel/store tests still pass (no regressions).
    - bunx tsc --noEmit clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Row + Panel wiring (render DeactivateAction; dispatch removeFromActiveSet + closeTab)</name>
  <files>
    src/ui/features/pretty-conversations/PrettyConversationRow.tsx,
    src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx,
    src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx,
    src/ui/AppShell.tsx
  </files>
  <behavior>
    - PrettyConversationRow desktop active-set non-RDP row: .pv-meta contains DeactivateAction rendered BEFORE PinAction in DOM order (leftmost meta child among pin/deactivate/dot).
    - PrettyConversationRow desktop ambient (!inActiveSet) row: [data-testid="deactivate-action"] returns null (component NOT in DOM tree).
    - PrettyConversationRow desktop RDP row: [data-testid="deactivate-action"] returns null.
    - PrettyConversationRow mobile active-set non-RDP row: swipe-reveal strip contains BOTH [data-testid="pin-action"] AND [data-testid="deactivate-action"].
    - PrettyConversationRow mobile ambient non-RDP row: swipe-reveal strip contains ONLY [data-testid="pin-action"] (no deactivate-action).
    - PrettyConversationRow mobile RDP row: no swipe strip at all (unchanged; RDP suppresses swipe wiring intrinsically per pre-existing contract).
    - Clicking DeactivateAction fires the panel's onDeactivate handler which calls both removeFromActiveSet(row.id) AND the AppShell closeTab(row.id) function. Row-body onSelect is NOT fired (stopPropagation on the button click).
    - Panel's Deactivate handler runs for rows in EVERY render site: active-set group, pinned group, non-RDP grouped rows. RDP rows never emit onDeactivate (they don't render DeactivateAction).
  </behavior>
  <action>
    Modify src/ui/features/pretty-conversations/PrettyConversationRow.tsx:
      1) Add import: `import { DeactivateAction } from "./DeactivateAction";`
      2) Extend the prop type of PrettyConversationRow with `onDeactivate?: () => void;` alongside the existing `onTogglePin: () => void`. Document that it MUST be provided by the panel whenever inActiveSet=true && !isRdp, and that the row is otherwise a no-op if the prop is missing.
      3) Add an onDeactivateClick handler mirroring onPinClick (stopPropagation lives in the row, not the component):

           const onDeactivateClick = useCallback(
             (e: MouseEvent<HTMLButtonElement>) => {
               e.stopPropagation();
               if (onDeactivate) onDeactivate();
             },
             [onDeactivate],
           );

      4) Mobile swipe strip (inside the `isMobile && !isRdp` block that currently renders <PinAction size="mobile" />): after PinAction, conditionally render <DeactivateAction /> when `inActiveSet === true`. Order in DOM: PinAction FIRST, DeactivateAction SECOND (matches Ashley's preview: pin on left, X on right when the strip is revealed from the right edge of the row body). Wrap the two children in a flex container so both sit side-by-side within the 132px strip; use inline style width: `${PC_SWIPE_REVEAL}px` (unchanged from current) — the row's swipe transform continues to use PC_SWIPE_REVEAL via the constant, no per-site edits needed beyond confirming L176, L200, L287 references pick up the new value automatically (they already do — the constant is imported once).
         Pass hue={hue}, size="mobile", onClick={onDeactivateClick} to DeactivateAction. Do NOT pass any pinned/inActiveSet prop — the component is a dumb visual.
      5) Desktop .pv-meta block (currently renders PinAction and the ready-dot inside `{!isMobile && !isRdp && ...}`): insert a conditional <DeactivateAction size="desktop" hue={hue} onClick={onDeactivateClick} /> BEFORE PinAction. Gate the render on `!isMobile && !isRdp && inActiveSet === true`. Keep PinAction's existing gate (`!isMobile && !isRdp`, unchanged; pin remains visible on desktop for non-RDP rows regardless of active-set membership per its own hover-reveal rules). Do NOT reorder or touch the ready-dot render — it stays last inside .pv-meta.
      6) DO NOT modify swipe-state constants inside the row — PC_SWIPE_REVEAL widened in Task 1 already flows through the three existing sites (L176 baseDxRef init, L200 clamp minimum, L287 targetDx open-value) via the shared import.

    Modify src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx:
      1) Import `removeFromActiveSet` from "@/state/conversation-store" alongside the existing addToActiveSet import.
      2) Add a required prop `onDeactivateRow: (row: ConversationRowShape) => void` to the panel's prop type. Document that AppShell wires this to closeTab(row.id). If callers omit it (defensive default), the deactivate action becomes a no-op at the row level (panel guards by not passing onDeactivate to the row). Prefer required — AppShell always wires it — but keep the type as required to force call-site update.
      3) Add a handleRowDeactivate function:

           const handleRowDeactivate = (row: ConversationRowShape) => {
             removeFromActiveSet(row.id);
             onDeactivateRow(row);
           };

         This is deliberately paired at the panel level (not the row) so the row stays a dumb consumer of a single callback and the panel owns the "store mutation + tab close" composition — same architectural shape as handleRowSelect + selectConversation + onConversationSelected.
      4) Extend PrettyConversationRowLive's props to accept optional `onDeactivate?: () => void` and forward it to PrettyConversationRow unchanged.
      5) At each render site inside PrettyConversationsPanel where PrettyConversationRowLive is instantiated, pass `onDeactivate={() => handleRowDeactivate(row)}` — but ONLY for the render sites where the row can be in the active-set:
           - Active-set group (L329-L347): ALWAYS pass onDeactivate (every row here is by definition in the active-set).
           - Pinned group (L360-L378): pass onDeactivate (pinned rows CAN also be in the active-set; the row-level gate `inActiveSet === true` decides whether DeactivateAction renders).
           - Grouped non-RDP block (L461-L480): pass onDeactivate (same reason — a grouped row may also be in the active-set via cross-tier presence rules).
           - RDP sentinel block (L415-L427): DO NOT pass onDeactivate (RDP rows never deactivate).

    Modify src/ui/AppShell.tsx: at the PrettyConversationsPanel mount site (~L1351), wire the new prop:

           onDeactivateRow={(row) => {
             closeTab(row.id);
           }}

      Reuse the existing closeTab function at L1169 verbatim — DO NOT add a new tab-close path. closeTab already routes to doCloseTab and handles the confirm-tab-close toast branch; that behavior is preserved for deactivate clicks too (deactivate is a tab-close variant, so the confirm-close toast is a feature not a bug).

    Extend src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx:
      1) In the top-of-file conversation-store mock block (~L113-L131), add:
             const removeFromActiveSetSpy = vi.fn();
         to the spy declarations, and add `removeFromActiveSet: (id: string) => removeFromActiveSetSpy(id),` to the mock module object. Keep the existing addToActiveSetSpy pattern.
      2) After Test 19C's closing describe (~L540), add a new describe block "PrettyConversationsPanel: deactivate action (quick-260727-gm3)" containing:
           - Test 20A: desktop active-set row renders [data-testid="deactivate-action"] inside .pv-meta, and its DOM position precedes [data-testid="pin-action"] (assert via compareDocumentPosition & 4).
           - Test 20B: desktop ambient (non-active-set) row renders NO [data-testid="deactivate-action"] (queryAllByTestId returns empty). Fixture: put the row inside `grouped`, keep useActiveSet mocked as empty Set.
           - Test 20C: desktop RDP row (in __rdp__ sentinel group) renders NO [data-testid="deactivate-action"] even when useActiveSet mock is patched to include the RDP row's id (belt-and-suspenders — RDP suppresses deactivate regardless).
           - Test 20D: mobile active-set non-RDP row's swipe strip contains BOTH pin-action AND deactivate-action; ambient mobile row's strip contains ONLY pin-action.
           - Test 20E: clicking DeactivateAction invokes removeFromActiveSetSpy(row.id) exactly once AND calls the panel's onDeactivateRow prop with the row object exactly once. Row-body's onSelect (via selectConversationSpy / onConversationSelected) is NOT called.
         For tests 20A/20C/20D that need useActiveSet to REPORT ids as active, override the mock inline via `vi.mocked(...)` or restructure the module-level `useActiveSet: () => new Set<string>()` to read from a mutable `mockActiveSet` variable set per-test — mirror the existing snapshot-mutation pattern used at L96-L104 to keep the mock module-level while allowing per-test overrides.
         Follow the byte-lockstep pattern used in existing pin/active-set tests: use the same MockRow/MockHost/setSnapshot helpers, cn helper, T-Test-NN naming, and the same `container.querySelector` / `queryByTestId` idioms.
      3) Also update Tests that render PrettyConversationsPanel (search for `<PrettyConversationsPanel variant=` — approximately 15+ call sites) to pass the new required `onDeactivateRow={() => {}}` prop. If the prop is truly required in the type, a no-op default in tests suffices; TypeScript will fail the build otherwise.

    Do NOT touch AppShell.tsx tests, tokens.ts tests (there aren't any), or DeactivateAction's own component-level test file (component behavior is covered via the panel tests — no separate DeactivateAction.test.tsx needed; PinAction has none either).
  </action>
  <verify>
    <automated>bunx vitest run src/ui/features/pretty-conversations/ src/ui/state/conversation-store.test.ts --reporter=verbose &amp;&amp; bunx tsc --noEmit</automated>
  </verify>
  <done>
    - Row renders DeactivateAction on desktop active-set non-RDP rows (BEFORE PinAction in .pv-meta) and inside the widened mobile swipe strip alongside PinAction.
    - Row does NOT render DeactivateAction on ambient rows or RDP rows.
    - Panel passes onDeactivate={() => handleRowDeactivate(row)} to the three eligible render sites (active-set group, pinned group, non-RDP grouped) and omits it from the RDP sentinel.
    - AppShell's PrettyConversationsPanel mount wires onDeactivateRow → closeTab(row.id) (reuses existing L1169 function).
    - Tests 20A-20E green; existing Tests 1-19C, 30a-30i still green.
    - Full sweep (`bunx vitest run`) clean; `bunx tsc --noEmit` clean.
    - Atomic commits: (1) store mutator + component + CSS + tokens; (2) row + panel + AppShell wiring + tests. Use quick-260727-gm3 slug per project convention.
  </done>
</task>

</tasks>

<verification>
- `bunx vitest run` — full test sweep passes with no regressions (particularly PrettyConversationsPanel.test.tsx and conversation-store.test.ts).
- `bunx tsc --noEmit` — no type errors introduced.
- Manual sanity (Ashley UAT — out of the plan's automated scope but the acceptance gate before ship): open the app, click any ambient row (row lights up in active-set treatment, tab opens), hover the row on desktop (X icon appears alongside pin), click X (row recedes to ambient, tab closes, agent process continues running server-side). On mobile: swipe row left to reveal both pin and X icons in the widened 132px strip.
</verification>

<success_criteria>
- Every `truth` in must_haves is observably true by a human interacting with the app.
- DeactivateAction never renders on ambient or RDP rows.
- removeFromActiveSet(id) is idempotent, notifies subscribers on real removal, silent-no-op on absent id.
- Tab-close reuses AppShell.closeTab (no new close path invented).
- No pre-existing tests fail; new Tests 20A-20E + 30f-30i pass.
- Ashley's preview-snippet visual match is preserved (bare red-tinted X icon with hue-drop-shadow, NOT a filled red pill).
</success_criteria>

<output>
Create `.planning/quick/260727-gm3-wire-deactivate-conversation-action-mobi/260727-gm3-SUMMARY.md` when done.
</output>
