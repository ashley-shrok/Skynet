---
quick_id: 260802-pq2
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-conversations/PrettyConversationRow.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/features/pretty-conversations/tokens.ts
  - src/ui/features/pretty-conversations/pretty-conversations.css
  - src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
autonomous: true

must_haves:
  truths:
    - "Mobile row long-press (500ms + <10px movement) opens the same PrettyConversationContextMenu as desktop right-click, at the touch coordinates."
    - "Mobile long-press triggers navigator.vibrate(10) when the API is available, and does NOT throw when it is absent."
    - "The long-press that opens the menu does NOT also fire the row's onSelect (tap-that-would-fall-through is suppressed)."
    - "The mobile swipe reveal strip no longer paints behind the row body — nothing bleeds through translucent ambient/hidden backgrounds."
    - "Short tap on a mobile row (<500ms) still fires onSelect."
    - "Movement >10px before the 500ms threshold cancels the pending long-press so vertical scroll wins."
    - "Mobile RDP rows never open the context menu (isRdp guard preserved)."
    - "Desktop right-click behavior on non-RDP rows is unchanged."
    - "tsc --noEmit passes with no unused-import errors after PinAction/HideAction/DeactivateAction are dropped from the row (if they become unused)."
    - "vitest run src/ui/features/pretty-conversations is green."
  artifacts:
    - path: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx"
      provides: "Long-press handler + unified context menu; zero swipe machinery."
      contains: "onTouchStart"
    - path: "src/ui/features/pretty-conversations/tokens.ts"
      provides: "No PC_SWIPE_* constants."
  key_links:
    - from: "PrettyConversationRow.tsx onTouchStart/Move/End/Cancel (mobile, non-RDP)"
      to: "setCtxMenu({ x, y }) + navigator.vibrate?.(10)"
      via: "500ms setTimeout armed on touchStart; cleared on early touchEnd or on movement>10px in touchMove"
      pattern: "setCtxMenu"
    - from: "PrettyConversationRow.tsx onBodyClick"
      to: "onSelect()"
      via: "direct call (no swipe-close branch)"
      pattern: "onSelect\\(\\)"
---

<objective>
Replace the mobile swipe-to-reveal action strip on `PrettyConversationRow` with a
long-press context menu that reuses the existing `PrettyConversationContextMenu`
(same component desktop uses for right-click). Delete the entire swipe state
machine, the reveal-strip JSX, the panel-side swipe coordination
(`forceClosed`/`onSwipeOpenChange`/`currentlySwipedId`), the `PC_SWIPE_*`
tokens, any CSS tied to the retired strip, and all swipe-strip-content tests.
Add long-press tests + mobile ergonomics CSS.

Purpose: Kill the class of bug where the reveal-strip's action buttons bleed
visually through translucent ambient/hidden row backgrounds (bounty
`swipe-actions-visible-through-translucent-rows`). Long-press-to-menu also
lands the intent already noted in `PrettyConversationRow.tsx:429`: "to hide,
long-press → menu". Nothing painted behind rows = no bleed-through, ever.

Output: Row + panel + tokens + CSS + two test files updated; `npx tsc
--noEmit` clean; vitest suite for `pretty-conversations` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@src/ui/features/pretty-conversations/PrettyConversationRow.tsx
@src/ui/features/pretty-conversations/PrettyConversationContextMenu.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
@src/ui/features/pretty-conversations/tokens.ts
@src/ui/features/pretty-conversations/pretty-conversations.css
@src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx

## Interface context the executor MUST honor

- `PrettyConversationContextMenu(props: { x, y, items, hue?, onClose })` is
  portal-mounted; call sites already exist inside `PrettyConversationRow.tsx`
  around lines 625-657. Wire the mobile long-press to the SAME
  `setCtxMenu({ x, y })` state hook — do NOT create a second menu instance.
- The `items[]` builder at ~L630 is the single source of truth for menu
  contents. Mobile long-press uses the SAME builder — no item-set divergence.
  Do NOT add mobile-specific menu items or omit any desktop items.
- The `!isRdp` guard on the `ctxMenu !== null && !isRdp` render at L625 is the
  single RDP safety net; keep it. Do NOT add a second RDP check — the touch
  handlers must be `undefined` for RDP rows so they never even arm a timer.
- Panel-level state to remove: `currentlySwipedId` (L493), `handleSwipeOpenChange`
  (L592-598), `forceClosedFor` (L603-606). Row props to remove: `forceClosed`
  (L98, L128), `onSwipeOpenChange` (L97, L127). Delete ALL four
  `onSwipeOpenChange={…}` + `forceClosed={forceClosedFor(row.id)}` prop passes
  in `PrettyConversationsPanel.tsx` (L758/L763, L818/L823, L923/L928,
  L986/L991).
- The panel's `handleRowSelect` at L512-513 has a `if (isMobileVariant)
  setCurrentlySwipedId(null);` line — remove it along with the state.
- `PinAction`, `DeactivateAction`, `HideAction` component imports at
  `PrettyConversationRow.tsx` L66-68 become unused after strip deletion
  (grep confirms they render ONLY inside the strip block). Remove the
  imports; `tsc --noEmit` will surface any regression.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Row — delete swipe machinery, wire long-press → context menu</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationRow.tsx, src/ui/features/pretty-conversations/tokens.ts, src/ui/features/pretty-conversations/pretty-conversations.css</files>
  <behavior>
    Row behavior contract after this task (tested by Task 2's rewritten row tests):
    - Mobile non-RDP row: touchStart arms a 500ms timer capturing the initial (clientX, clientY). If touchEnd fires before 500ms → no menu, no side effects, standard click path continues (short-tap onSelect via onBodyClick).
    - Mobile non-RDP row: touchMove computes |dx|+|dy| against the start point; if either exceeds ~10px before 500ms, cancel the pending timer (movement wins → scroll takes over).
    - Mobile non-RDP row: if the 500ms timer fires without cancellation, call setCtxMenu({ x: startClientX, y: startClientY }) AND call navigator.vibrate?.(10) (feature-checked — do NOT throw when undefined). Mark a suppress-next-click flag so the synthesized click that follows the long-press does NOT fire onSelect on the row body.
    - Mobile RDP row: touch handlers are undefined (isRdp gate preserved on the JSX prop wiring). No timer, no menu, ever.
    - Desktop: unchanged. onContextMenu still wires to setCtxMenu on non-RDP rows only. onSelect still fires on click.
    - Row wrapper: no `overflow-hidden` on mobile (there's no transform to clip). `data-swiped-open` attribute removed. `bodyStyle` only carries `--pv-hue` when hue !== null; no transform/transition branch.
    - `.pv-row` CSS carries `-webkit-touch-callout: none; user-select: none; -webkit-user-select: none;` on mobile so iOS long-press doesn't fight the handler with a text-selection callout. Verified by CSS text presence (grep).
  </behavior>
  <action>
    (1) `src/ui/features/pretty-conversations/PrettyConversationRow.tsx`:
      - DELETE the imports of `PC_SWIPE_REVEAL`, `PC_SWIPE_THRESHOLD`, `PC_SWIPE_ANGLE_TOLERANCE` at L74-78.
      - DELETE the imports of `PinAction`, `DeactivateAction`, `HideAction` at L66-68 (they become unused; tsc will scream if wrong).
      - DELETE props `onSwipeOpenChange` (L97, L127) and `forceClosed` (L98, L128) from the destructuring and the type block. Also delete the associated JSDoc comment block at L83-86.
      - DELETE state `swipedOpen`, `dxLive`, refs `startXRef`/`startYRef`/`activeRef`/`baseDxRef`, `effectiveOpen`, `emitSwipeOpenChange` callback, and the entire swipe state-machine block from L197-277 (onTouchStart / onTouchMove / onTouchEnd).
      - DELETE the swipe-close-instead-of-select branch inside `onBodyClick` (L282-289); the callback body is now just `onSelect()`.
      - DELETE the mobile transform branch in `bodyStyle` (L381-391): the IIFE returns `hue !== null ? { "--pv-hue": hue } : {}` cast to `CSSProperties`. No transform, no transition.
      - DELETE `activeDrag`/`targetDx` locals (L375-380).
      - DELETE the entire swipe-reveal strip JSX block at L415-477 (the `{isMobile && !isRdp && (<div className="absolute top-0 right-0 …" …)}` block).
      - CHANGE `wrapperClass` (L397-399) to `"relative"` for BOTH mobile and desktop (no more `overflow-hidden` on mobile — there's no transform to clip anymore).
      - DELETE `data-swiped-open` attribute on the wrapper div (L413).
      - ADD `useEffect` import to the existing hook import block at L48-56 (for the timer cleanup on unmount).
      - ADD a new mobile long-press hook block below the `ctxMenu` state (near L335). Sketch: keep a `longPressTimerRef = useRef<number | null>(null)` and a `longPressStartRef = useRef<{ x: number; y: number } | null>(null)` and a `suppressNextClickRef = useRef<boolean>(false)`. Define:
          - `onTouchStart(e)`: guard `isRdp || !isMobile`; capture `t = e.touches[0]`; store `{ x: t.clientX, y: t.clientY }` in longPressStartRef; set the timer via `window.setTimeout(() => { setCtxMenu({ x: t.clientX, y: t.clientY }); navigator.vibrate?.(10); suppressNextClickRef.current = true; longPressTimerRef.current = null; }, 500)` — assign the id to longPressTimerRef.current.
          - `onTouchMove(e)`: guard as above; if longPressTimerRef.current === null OR longPressStartRef.current === null → return. Compute dx/dy from start; if `Math.hypot(dx, dy) > 10` clear the timer + null the refs.
          - `onTouchEnd`/`onTouchCancel`: clear the pending timer (if any) and null longPressStartRef. Do NOT touch suppressNextClickRef here — the following click event needs to read it.
          - `useEffect` cleanup on unmount that clears any pending timer.
      - MODIFY `onBodyClick` (post-simplification): at the top, `if (suppressNextClickRef.current) { suppressNextClickRef.current = false; return; }` then `onSelect()`. This is the "long-press must not also fire onSelect" suppression.
      - WIRE the new handlers on the row body div (was L490-493): the four touch props stay on the mobile non-RDP branch, but now point at the new long-press handlers instead of the deleted swipe ones. `onTouchStart={isMobile && !isRdp ? onTouchStart : undefined}`, `onTouchMove={isMobile && !isRdp ? onTouchMove : undefined}`, `onTouchEnd={isMobile && !isRdp ? onTouchEnd : undefined}`, `onTouchCancel={isMobile && !isRdp ? onTouchCancel : undefined}`.
      - KEEP the context menu portal block at L621-657 UNCHANGED (items builder, `!isRdp` guard, `PrettyConversationContextMenu` render, `hue` passthrough — all identical).
      - KEEP the `subtitleMode` render paths, ready-dot logic, ambient/rdp class composition, avatar branches — none of that changes.
    (2) `src/ui/features/pretty-conversations/tokens.ts`:
      - DELETE the three `PC_SWIPE_REVEAL`, `PC_SWIPE_THRESHOLD`, `PC_SWIPE_ANGLE_TOLERANCE` exports and their JSDoc blocks. Grep the tree first to confirm no consumer remains outside the row file we just gutted. `grep -rn "PC_SWIPE_" src` MUST return 0 lines after Task 1.
      - Adjust the leading block comment so it no longer promises "swipe state machine constants remain" — the file is now header-only. If the file ends up empty, replace the body with a single-line export like `export {};` and keep the leading comment explaining that the swipe tokens were retired in quick 260802-pq2.
    (3) `src/ui/features/pretty-conversations/pretty-conversations.css`:
      - Grep for `data-swiped-open`, `swipe`, `swiped`, `PC_SWIPE`, `overflow-hidden` inside this file. Remove any `.pv-row` selectors keyed on those (there are references in comments at L44/L667/L736/L751/L761/L784/L821/L836/L878/L885 — comments must be updated to reflect the new long-press-to-menu model, or dropped entirely where the comment only made sense with a swipe strip). Do NOT change unrelated visual rules.
      - ADD to the `.pv-row` block (around L299) the three mobile ergonomics declarations: `-webkit-touch-callout: none; user-select: none; -webkit-user-select: none;`. Placement inside the base `.pv-row {…}` block is fine (they're inert on desktop). Confirm no existing rule already sets them (`grep -c "touch-callout" src/ui/features/pretty-conversations/pretty-conversations.css` must have been 0 before this edit and 1 after).
      - Delete the desktop-specific hover-reveal blocks ONLY if they depend on the retired strip. The `.pv-row.pv-row--desktop:not(:hover) .pv-meta [data-testid="pin-action"]` rule at L672 references PinAction which no longer renders anywhere on desktop (quick-260730-o2m already moved it into the context menu). Check with grep: if `data-testid="pin-action"` is only referenced from swipe-strip-adjacent CSS and from tests, DELETE those hover-reveal rules. If unsure, LEAVE the desktop rules and only prune the ones with explicit swipe wording. The bar is: no orphan CSS selectors referencing DOM that never renders. Executor: err on the side of leaving desktop hover-reveal rules alone — this quick is scoped to mobile swipe removal.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npx tsc --noEmit 2&gt;&amp;1 | tee /tmp/pq2-tsc.log &amp;&amp; test $(grep -c '^' /tmp/pq2-tsc.log) -le 3 &amp;&amp; grep -rn "PC_SWIPE_\|forceClosed\|onSwipeOpenChange\|data-swiped-open" src/ui/features/pretty-conversations 2&gt;&amp;1 | grep -v '^#' | grep -v '\.test\.tsx' &amp;&amp; echo "REGRESSION: swipe refs remain" &amp;&amp; exit 1 || echo "OK: no residual swipe refs in row/panel/tokens/css"</automated>
  </verify>
  <done>
    - `PrettyConversationRow.tsx` has no `swipedOpen`/`dxLive`/`forceClosed`/`onSwipeOpenChange`/`PC_SWIPE_*`/`data-swiped-open`/`translateX` anywhere; imports of `PinAction`/`DeactivateAction`/`HideAction` gone.
    - `tokens.ts` has no `PC_SWIPE_*` exports.
    - `pretty-conversations.css` has no `data-swiped-open` selectors; base `.pv-row` carries the three mobile-ergonomics declarations.
    - `PrettyConversationsPanel.tsx` NOT yet changed (Task 2 handles it) but tsc still passes because the deleted props were optional. If tsc surfaces a panel-side error because the panel still spreads a now-unknown prop into `PrettyConversationRow`, resolve by moving that panel edit into this task — the goal is a green tsc after Task 1.
    - `npx tsc --noEmit` in the skynet repo is clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Panel — drop swipe coordination; tests — swap swipe tests for long-press tests</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx, src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx, src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx</files>
  <behavior>
    Panel behavior contract after this task:
    - `PrettyConversationsPanel.tsx` no longer maintains `currentlySwipedId`, `handleSwipeOpenChange`, or `forceClosedFor`. `handleRowSelect` no longer clears any swipe-coordination state (the whole `if (isMobileVariant) setCurrentlySwipedId(null);` line is gone).
    - All four `PrettyConversationRowLive` call sites (active-set, pinned, non-RDP grouped, hidden section) drop the `onSwipeOpenChange={…}` and `forceClosed={forceClosedFor(row.id)}` props. The RDP call site was already props-clean.
    - `PrettyConversationRowLive` wrapper type no longer accepts `onSwipeOpenChange` or `forceClosed`.

    Test behavior contract after this task (row test file — src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx):
    - Tests 2, 3, 4, 5, 7 (swipe portion), 18f (mobile swipe-strip PinAction present assertion), and any other test that fires touchStart/touchMove/touchEnd expecting `data-swiped-open` or a reveal-strip `pin-action` are REMOVED or REWRITTEN.
    - NEW tests appended (numbered TL1..TL5 in a `describe("PrettyConversationRow: mobile long-press context menu (quick-260802-pq2)")` block):
        - TL1: mobile non-RDP row + touchStart at (200,100) + vi.useFakeTimers → vi.advanceTimersByTime(500) + touchEnd → `screen.getByRole("menu")` resolves; menuitem "Pin" is present; the row's onSelect was NOT called.
        - TL2: mobile non-RDP row + touchStart at (200,100) + touchMove to (215,105) (dx=15 > 10) + vi.advanceTimersByTime(500) + touchEnd → `screen.queryByRole("menu")` is null; onSelect was NOT called (movement cancelled the arm; the trailing click was already suppressed by the movement gate — jsdom does not synthesize a click on touchEnd so this assertion is trivially green, but keep it to lock the contract).
        - TL3: mobile non-RDP row + short tap (touchStart + touchEnd + fireEvent.click at <500ms, no advanceTimersByTime) → `screen.queryByRole("menu")` is null; onSelect called exactly once.
        - TL4: mobile RDP row + touchStart + advanceTimersByTime(500) + touchEnd → `screen.queryByRole("menu")` is null (isRdp guard); no throws.
        - TL5: mobile non-RDP row with `navigator.vibrate` stubbed → after 500ms hold, `vibrate` was called with `10`. Also assert that when `navigator.vibrate` is undefined the long-press still opens the menu and does NOT throw (delete-then-restore pattern).
    - Existing desktop right-click tests (Test 8, Test 10, Test 18c/d/e, Test 7b RDP-no-onContextMenu) are UNTOUCHED and must still pass.
    - Update Test 18f: change its assertion. Mobile row body no longer wires onContextMenu (unchanged), AND the mobile row no longer has an in-strip `[data-testid="pin-action"]` element. Rewrite as: "mobile row body does NOT wire onContextMenu (dispatching contextmenu is a no-op) AND mobile row has NO in-DOM `[data-testid="pin-action"]` (moved to long-press menu)".
    - Update Test 7: keep the RDP class + no-PinAction assertions; DELETE the touch sequence + `data-swiped-open` assertion (RDP long-press guard is covered by TL4).

    Test behavior contract after this task (panel test file — src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx):
    - Tests 20D, (k), (l), (m) — the four tests that assert on swipe-strip contents (`data-testid="pin-action"`/`deactivate-action"`/`hide-action-hide"`/`hide-action-show"` inside the mobile row) are REMOVED. Their coverage moves to the row long-press tests (TL1-TL5) — the panel-level menu-item assertions already live in Test 20A + 20E for desktop, and the same items[] builder feeds both entry points, so mobile menu content is guaranteed by transitivity.
    - Grep the panel test file for `forceClosed`/`onSwipeOpenChange`/`currentlySwipedId`/`data-swiped-open` after edits — must return 0 lines.
    - All other panel tests untouched.
  </behavior>
  <action>
    (1) `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`:
      - DELETE `currentlySwipedId` state (L493-495).
      - DELETE `handleSwipeOpenChange` (L592-598) and `forceClosedFor` (L603-606).
      - DELETE the `if (isMobileVariant) setCurrentlySwipedId(null);` line in `handleRowSelect` (L513) — no replacement needed.
      - Update the block comment at L28-33 to strike the "Swipe coordination (mobile): only one row swiped-open at a time…" bullet.
      - At the four `PrettyConversationRowLive` render sites (L747-767 active-set, L807-827 pinned, L911-932 non-RDP grouped, L976-995 hidden section) DELETE both the `onSwipeOpenChange={…}` and `forceClosed={forceClosedFor(row.id)}` prop passes. Leave every other prop.
      - In the `PrettyConversationRowLive` prop type block (L103-128), DELETE `onSwipeOpenChange?: (open: boolean) => void;` and `forceClosed?: boolean;` and the destructuring — the wrapper now just forwards without those two.
    (2) `src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx`:
      - DELETE Test 2 (L221-259), Test 3 (L261-293), Test 4 (L295-328), Test 5 (L330-367). These are the pure swipe tests — coverage moves to TL1-TL5.
      - MODIFY Test 7 (L400-435): DELETE the touch sequence (L423-431) + the `data-swiped-open` assertion; keep everything else (RDP class + no PinAction + data-rdp-host-row=true).
      - MODIFY Test 18f (L976-1004): keep the mobile-row-no-onContextMenu assertion (`fireEvent.contextMenu(body, …); expect(screen.queryByRole("menu")).toBeNull();`). REWRITE the trailing PinAction assertion — it currently expects the strip PinAction to be present; change to `expect(container.querySelector('[data-testid="pin-action"]')).toBeNull();` and add a comment referencing quick-260802-pq2 explaining why (long-press menu is the mobile action affordance now; no PinAction in the DOM).
      - APPEND a new describe block at the end of the file:
          ```
          describe("PrettyConversationRow: mobile long-press context menu (quick-260802-pq2)", () => {
            beforeEach(() => { vi.useFakeTimers(); });
            afterEach(() => { vi.useRealTimers(); });
            // TL1..TL5 as specified in <behavior>.
          });
          ```
        Use `act(() => { vi.advanceTimersByTime(500); })` around timer advances. For TL5, stub via `const original = navigator.vibrate; (navigator as any).vibrate = vi.fn();` in the arrange step and restore in a `finally`/`afterEach`. jsdom does not implement `navigator.vibrate` by default so the delete-then-restore is straightforward.
      - Use `screen.getByRole("menu")` / `screen.getByRole("menuitem", { name: /pin/i })` for menu assertions (portal-mounted to document.body — see PrettyConversationContextMenu.tsx L98-163).
    (3) `src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx`:
      - DELETE Test 20D (L857-896), Test (k) (L2372-2392), Test (l) (L2394-2424), Test (m) (L2426-2447). Their describe wrappers stay if they still contain other passing tests; if a describe becomes empty, remove it too.
      - Grep the file for `forceClosed`, `onSwipeOpenChange`, `currentlySwipedId`, `data-swiped-open`, `data-testid="pin-action"`, `data-testid="deactivate-action"`, `data-testid="hide-action` → address any remaining references. Panel tests that were checking DESKTOP `data-testid="pin-action"` absence (Test 20A block) stay — those assertions are still meaningful and correct.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run src/ui/features/pretty-conversations --reporter=default 2&gt;&amp;1 | tail -80 &amp;&amp; grep -rn "forceClosed\|onSwipeOpenChange\|currentlySwipedId\|data-swiped-open\|PC_SWIPE_" src/ui/features/pretty-conversations 2&gt;&amp;1 | grep -v '^#' | (grep . &amp;&amp; echo "REGRESSION: residual swipe refs" &amp;&amp; exit 1 || echo "OK: swipe references fully purged")</automated>
  </verify>
  <done>
    - `PrettyConversationsPanel.tsx` has no `currentlySwipedId`, `handleSwipeOpenChange`, `forceClosedFor`, `onSwipeOpenChange`, or `forceClosed` references (grep verifies 0 hits).
    - `PrettyConversationRow.test.tsx` has TL1-TL5 for long-press, has no Test 2/3/4/5, has updated Test 7 + Test 18f.
    - `PrettyConversationsPanel.test.tsx` has no Test 20D/(k)/(l)/(m); no residual swipe-strip DOM assertions.
    - `npx tsc --noEmit` is clean.
    - `npx vitest run src/ui/features/pretty-conversations` is green (all remaining tests + the 5 new TL tests pass).
  </done>
</task>

</tasks>

<verification>
Whole-plan checks (executor MUST run BOTH before completing):

1. `cd /home/ubuntu/skynet && npx tsc --noEmit` — no errors.
2. `cd /home/ubuntu/skynet && npx vitest run src/ui/features/pretty-conversations` — all suites green.

Optional sanity greps (fast, catch orphan references):
- `grep -rn "PC_SWIPE_\|forceClosed\|onSwipeOpenChange\|currentlySwipedId\|data-swiped-open" src/ui/features/pretty-conversations` — 0 lines.
- `grep -c "touch-callout" src/ui/features/pretty-conversations/pretty-conversations.css` — 1.

Deliberately skipped: `npm run build:backend`, `docker build`, `docker compose up`, `git push` (per scope's Do-NOT list — no backend changes, quick-only edit).
</verification>

<success_criteria>
- Mobile row long-press (500ms + <10px movement) opens the SAME context menu desktop right-click uses, at touch coordinates.
- The pending click that follows a long-press does NOT fire onSelect.
- Short tap fires onSelect exactly once; movement >10px cancels the pending long-press.
- Mobile RDP rows never open the menu.
- `navigator.vibrate` is called with 10 when the API exists; missing API does NOT throw.
- No swipe reveal strip exists in the DOM (grep on `data-swiped-open` returns nothing).
- No swipe machinery exists in code (grep on `PC_SWIPE_`/`forceClosed`/`onSwipeOpenChange`/`currentlySwipedId` returns nothing in `src/ui/features/pretty-conversations`).
- Bounty `swipe-actions-visible-through-translucent-rows` is architecturally impossible to reproduce (nothing paints behind rows).
- Desktop right-click behavior unchanged.
- Every existing non-swipe test still passes; 5 new long-press tests pass.
</success_criteria>

<output>
Write summary to: `.planning/quick/260802-pq2-replace-mobile-swipe-to-reveal-with-long/260802-pq2-SUMMARY.md`
</output>
