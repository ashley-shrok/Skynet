---
phase: quick-260719-u29
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/ComposeBox.tsx
  - src/ui/features/pretty-view/PrettyView.tsx
autonomous: true
requirements:
  - PATCH-84
must_haves:
  truths:
    - "New Hourglass button appears in compose icon column between ThumbsUp (top) and Send (bottom)"
    - "Clicking Hourglass with non-empty text arms the queue: button glows amber-pulse, textarea shows pending overlay with Hourglass icon + 'Queued — waiting for idle' label, textarea is disabled"
    - "Clicking Hourglass again while armed cancels the queue: overlay dismisses, textarea re-enables, focus returns to textarea"
    - "When queue is armed AND isIdle prop transitions to true, dispatch fires after 3s of continuous idle; on transient !isIdle during those 3s the timer resets"
    - "Successful dispatch clears textarea + persisted draft + queue state; failed dispatch clears queue and shows 'Not connected — queued send failed' inline error"
    - "Send, ThumbsUp (go ahead), and Reset actions while queue is armed cancel the queue and fire their action immediately (queue drops silently)"
    - "Hourglass button is disabled when text is empty and queue is not armed"
    - "PrettyView forwards its isIdle prop to ComposeBox"
    - "Existing Phase 4 Glass reskin + patch #83 meter well + drain animation + patch #57 draft persistence remain untouched and functional"
  artifacts:
    - path: "src/ui/features/pretty-view/ComposeBox.tsx"
      provides: "ComposeBox with isIdle prop, queue state, Hourglass button, watchdog effect, pending overlay"
      contains: "Hourglass"
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "isIdle prop forwarded to ComposeBox"
      contains: "isIdle={isIdle}"
  key_links:
    - from: "src/ui/features/pretty-view/ComposeBox.tsx"
      to: "isIdle prop transitions"
      via: "useEffect watchdog schedules setTimeout(3000) → fireQueuedDispatch"
      pattern: "isIdle.*queuedText|setTimeout.*3000"
    - from: "src/ui/features/pretty-view/PrettyView.tsx"
      to: "ComposeBox"
      via: "isIdle prop"
      pattern: "isIdle=\\{isIdle\\}"
---

<objective>
Patch #84 — Add a "Queue on idle" send path to the pretty-view ComposeBox. User composes a message, clicks a new Hourglass button (between ThumbsUp and Send in the icon column), and dispatch fires after the session has been continuously idle for 3s (on top of the backend's ~4s isIdle debounce = ~7s effective from Claude's last output). Semantically distinct from immediate Send (which enters Claude's own mid-round queue) and from MessageQueueDrawer (manual staging): the whole point is "wait until Claude's current turn is DONE, then fire."

Purpose: give Ashley a hands-off way to schedule a follow-up so she can walk away without either interrupting Claude mid-turn or babysitting the terminal. All decisions locked with Ashley 2026-07-19: 3s threshold, Hourglass icon, self-toggle cancel, immediate-actions-supersede, fail-loud on dispatch failure, single-slot queue.

Output: patch #84 landed on `feat/tab-title-from-tmux` — one commit modifying `ComposeBox.tsx` (new prop, new state, new button, new effect, textarea wrapper for overlay, cancel-guards prepended to Send/GoAhead/Reset handlers) and `PrettyView.tsx` (single-line prop forward at line ~562).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/ui/features/pretty-view/ComposeBox.tsx
@src/ui/features/pretty-view/PrettyView.tsx
@src/ui/components/button.tsx

Key interface facts (extracted from context read — do not re-read):

**ComposeBox (POST patch #83 state):**
- Imports on line 1-10: `useCallback, useEffect, useRef, useState` from react; `RotateCcw, Send, ThumbsUp` from lucide-react; `Button` from `@/components/button`; `Textarea` from `@/components/textarea`; `cn` from `@/lib/utils`; `flushComposeDraftKeepalive, getComposeDraft, putComposeDraft` from `@/api/compose-drafts-api`.
- `ComposeBoxProps` interface defined at line ~58: `onSend, canSend, contextPct, hostId, tmuxSession, identityName, className`.
- Destructure at function signature line ~88: matches props.
- `errorMessage` state declared at line 98.
- `textareaRef` declared at line 99.
- Existing handlers: `handleSend` (~316), `handleResetSend` (~345), `handleQuickSend` (~394), `handleKeyDown` (~411), `handleTextChange` (~306), `handleBlur` (~311).
- Existing `clearAfterSend` useCallback at ~291 — call after successful dispatch to reset draft persistence.
- Return JSX root at line ~431: `<div>` with `flex flex-col gap-1 px-2 py-2 shrink-0` etc.
- Compose row: `<div className="flex items-end gap-2">` at line ~460.
- Meter well: lines ~472-596 (UNTOUCHED — includes reset button in meter well bottom slot per patch #83).
- Textarea currently at line ~597, has `resize-none flex-1 self-stretch` at top of className, `rounded-[10px]` at line ~649. Has `!` on `bg-[rgba(10,12,20,0.5)]!` (line 647) per patch #81-fix — this pattern MUST be followed on the new Hourglass button.
- Icon column div at ~670: `<div className="flex flex-col gap-1">` containing ThumbsUp then Send Buttons.
- ThumbsUp Button at ~679-698: uses `size="icon-sm" variant="outline"`. Rest className is the reference pattern.
- Error message rendered at ~720-722.

**PrettyView (relevant slice):**
- `PrettyViewProps` interface line ~55, declares `isIdle?: boolean | null` at line 69 with the exact JSDoc semantics we mirror in ComposeBoxProps.
- Function signature destructures `isIdle` at line 88.
- ComposeBox render at lines 555-565: props currently forwarded are `onSend, canSend, contextPct, hostId, tmuxSession, identityName, className`. Need to add `isIdle={isIdle}` as a single-line addition.

**Button component (`src/ui/components/button.tsx`):**
- `outline` variant at line 15 includes `dark:bg-input/30 dark:hover:bg-input/50` (specificity 0-2-0). Plain arbitrary `bg-[...]` (0-1-0) LOSES the cascade → `!` suffix required on BOTH `bg-[...]` and `hover:bg-[...]` for arbitrary gradient overrides. This is the same trap patch #81 hit on Textarea; ThumbsUp Button (line ~689-694) uses `outline` variant with plain `bg-[linear-gradient(...)]` (NO `!`) — this may be pre-existing latent bug or may work because ThumbsUp inherits some other cascade. **For our NEW Queue button we apply `!` defensively on rest bg + hover bg + armed bg, matching the Textarea pattern the codebase already treats as canonical.**
- `size="icon-sm"` = `size-7 rounded-none` (line 32). ThumbsUp overrides with `rounded-md` on top.

**isIdle semantics (from PrettyView JSDoc line 64-68):**
- `true` = Claude quiet ≥4s AND foreground = claude → session idle
- `false` = actively working
- `null` = backend has not spoken yet on current attach

The watchdog fires only on `isIdle === true` (strict — NOT `!== false` — the `null` window must NOT trigger dispatch).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Queue button + isIdle watchdog + textarea pending overlay to ComposeBox.tsx</name>
  <files>src/ui/features/pretty-view/ComposeBox.tsx</files>
  <action>
Implement patch #84 ComposeBox changes as ELEVEN discrete edits to a single file. Perform them in dependency order to keep intermediate typecheck sane.

Edit A (imports, line ~2): Add `Hourglass` to the lucide-react import list, so it reads `import { Hourglass, RotateCcw, Send, ThumbsUp } from "lucide-react";` (alphabetical).

Edit B (ComposeBoxProps interface, line ~58-86): Add new optional prop `isIdle` after `identityName` and before `className`. JSDoc must mirror PrettyView.tsx's own line 64-68 doc semantics — see verbatim wording in the spec — and explicitly note it's used by the Queue button watchdog to schedule dispatch after 3s continuous idle (~7s effective delay). Shape: `isIdle?: boolean | null;`.

Edit C (function signature destructure, line ~88-96): Add `isIdle` between `identityName` and `className` to match interface order.

Edit D (new state, near line ~98 next to `errorMessage`): Declare `const [queuedText, setQueuedText] = useState<string | null>(null);` and `const dispatchTimerRef = useRef<ReturnType<typeof setTimeout> | null>(null);`. Follow the existing timer-ref pattern used by `drainEndTimerRef` / `pulseOnTimerRef` (line ~111-113) for consistency.

Edit E (fireQueuedDispatch — new useCallback, place AFTER `clearAfterSend` at ~304 so it can reference it): Define `fireQueuedDispatch` as a `useCallback` with dependency array `[queuedText, onSend, clearAfterSend]`. Read `queuedText` (which is guaranteed non-null when this fires — the watchdog effect gates on that), collapse newlines to spaces (`replace(/\r?\n/g, " ")`) matching D-50 Ink-safety policy already in place on handleSend line 323, call `onSend(payload)`. On dispatched=true: `setText("")`, `setQueuedText(null)`, `clearAfterSend()`. On dispatched=false: `setQueuedText(null)`, `setErrorMessage("Not connected — queued send failed")` (fail-loud per Ashley decision — do NOT retry silently). useCallback is REQUIRED here to avoid the stale-closure trap on queuedText that the spec calls out — do not use a bare function declaration.

Edit F (idle watchdog useEffect, place near existing pagehide/interval effects around line ~236-268): Effect with dependency array `[queuedText, isIdle, fireQueuedDispatch]`. Body: if `queuedText === null`, return. If `isIdle !== true` (strict — treats null and false the same, so the null-attach-window does NOT trigger dispatch), clear any pending `dispatchTimerRef.current` (with null guard) and return. If `dispatchTimerRef.current !== null`, return (idempotent — do not start a second timer). Otherwise `dispatchTimerRef.current = setTimeout(() => { dispatchTimerRef.current = null; fireQueuedDispatch(); }, 3000);`. Return cleanup that clears the timer with null guard. Add an inline comment referencing the ~4s backend gate + 3s watchdog = ~7s effective delay locked with Ashley 2026-07-19.

Edit G (unmount cleanup effect): Add a second effect with empty dep array `[]` that returns `() => { if (dispatchTimerRef.current) clearTimeout(dispatchTimerRef.current); }`. This guards against unmount mid-timer independently of the watchdog effect's own cleanup (which runs on every deps change and would already handle the common case; belt-and-suspenders for the unmount-while-idle-transitioning race).

Edit H (handleQueue function, place with other handlers around line ~316): Define a plain function (no useCallback needed — event handler passed inline via arrow-adapter or directly to onClick, no downstream memoization consumer). Body: if `queuedText !== null` (armed → cancel): clear `dispatchTimerRef.current` with null guard, `setQueuedText(null)`, refocus textarea via `textareaRef.current?.focus()`, return. Else: `const trimmed = text.trim();` → `if (!trimmed) return;` → `setErrorMessage(null); setQueuedText(trimmed);`.

Edit I (cancel-queue prefix on Send / GoAhead / Reset): At the TOP of each of `handleSend` (line ~316), `handleResetSend` (line ~345), and `handleQuickSend` (line ~394) — BEFORE any existing logic (before the drain-animation setup in handleResetSend, before the setErrorMessage(null) in handleSend/handleQuickSend) — add: `if (queuedText !== null) { if (dispatchTimerRef.current) { clearTimeout(dispatchTimerRef.current); dispatchTimerRef.current = null; } setQueuedText(null); }`. This implements the "immediate action wins, queue drops silently" rule. Do NOT setErrorMessage here — silent drop per spec.

Edit J (handleKeyDown guard, line ~411): At the very top of `handleKeyDown`, add `if (queuedText !== null) return;`. The textarea will also be disabled (Edit K) so keydown won't reach it in practice, but this is defense in depth against any focus-restoration race.

Edit K (textarea wrapper + pending overlay, line ~597-660): Restructure the current single `<Textarea>` element into a wrapping `<div className="relative flex-1 self-stretch">` containing the Textarea AND a conditional overlay. Three sub-changes:
  1. Drop `flex-1 self-stretch` from the Textarea's own className (currently line 624: `"resize-none flex-1 self-stretch"` → `"resize-none w-full h-full"`). The wrapper now owns flex sizing.
  2. Add `disabled={queuedText !== null}` prop on the Textarea. This blocks keystrokes while armed and matches Ashley's decision that "keep composing while queued" is out of scope.
  3. Immediately after the Textarea closing tag, add the overlay:
     `{queuedText !== null && (<div className="absolute inset-0 flex flex-col items-center justify-center gap-1.5 pointer-events-none rounded-[10px] bg-[rgba(10,12,20,0.72)] backdrop-blur-[2px]"><Hourglass className="size-5 text-[hsla(38,70%,72%,0.9)]" /><span className="text-sm text-[hsla(38,60%,80%,0.85)] font-[Inter_Variable,ui-sans-serif,system-ui,sans-serif]">Queued — waiting for idle</span></div>)}`
  The overlay `rounded-[10px]` MUST match the Textarea's own `rounded-[10px]` (line 649) so corners align — do not change this.

Edit L (Queue Button insertion in icon column, between ThumbsUp Button end tag ~698 and Send Button start tag ~699): Insert a new `<Button size="icon-sm" variant="outline">` between them. Compute the queueArmed boolean (`const queueArmed = queuedText !== null;`) and queueDisabled (`const queueDisabled = canSend === false || (queuedText === null && text.trim() === "");`) inline just above the return JSX or right before the icon-column div. Handler `onClick={handleQueue}`. `disabled={queueDisabled}`. `aria-label` and `title` computed inline as `queueArmed ? "Cancel queued send" : "Queue send for when session goes idle"`. className via `cn(...)`:
  - Rest state classes MUST mirror ThumbsUp EXACTLY, but WITH `!` on the `bg-[linear-gradient(...)]!` and `hover:bg-[linear-gradient(...)]!` due to the outline-variant `dark:bg-input/30` cascade trap documented in patch #81-fix and re-flagged in the Context section above (specificity 0-2-0 beats 0-1-0). Do NOT skip the `!` even though ThumbsUp currently omits it — Ashley's decision is defensive-`!` on the new button per Textarea canonical pattern.
  - Common base always applied: `"rounded-md cursor-pointer"`, `"text-[#e8e4d8]"` (overridden when armed).
  - Rest (non-armed) branch classes:
    `"border-white/10"`,
    `"bg-[linear-gradient(180deg,rgba(70,66,58,0.5),rgba(38,34,28,0.6))]!"`,
    `"shadow-[0_2px_4px_rgba(0,0,0,0.4),_inset_0_1px_0_rgba(255,240,210,0.12)]"`,
    `"hover:bg-[linear-gradient(180deg,rgba(100,85,55,0.7),rgba(60,50,32,0.8))]!"`,
    `"hover:border-[rgba(255,240,215,0.22)]"`,
    `"hover:shadow-[0_4px_8px_rgba(0,0,0,0.5),_inset_0_1px_0_rgba(255,240,210,0.2),_0_0_20px_rgba(255,240,215,0.14)]"`.
  - Armed (queueArmed=true) branch replaces the rest bg/border/shadow with amber-lit + pulse:
    `"bg-[linear-gradient(180deg,hsla(38,55%,50%,0.9),hsla(38,60%,32%,0.95))]!"`,
    `"border-[hsla(38,70%,55%,0.5)]"`,
    `"text-[#fff5e0]"`,
    `"shadow-[0_2px_6px_rgba(0,0,0,0.45),_inset_0_1px_0_rgba(255,235,190,0.35),_0_0_16px_hsla(38,70%,52%,0.35)]"`,
    `"animate-pulse"`.
  Use a ternary `queueArmed ? [...armed classes...] : [...rest classes...]` inside the `cn(...)` call, following the exact ternary-array pattern used by the reset button at line ~569-591. Include `<Hourglass className="size-4" />` as the button child, matching ThumbsUp's `<ThumbsUp className="size-4" />`.

Constraints/gotchas — DO NOT VIOLATE:
- Do NOT touch the meter well div (line ~472-596) or the reset button inside it (line ~563-595). Patch #83 must remain intact.
- Do NOT touch `clearAfterSend`, draft persistence useEffects, or handleTextChange autosave logic. Patch #57 must remain intact.
- Do NOT add error UI on autosave failures — COMPOSE-04 HARD LOCK stays in force.
- Do NOT add a "keep composing while queued" affordance — Ashley locked textarea-disabled while armed.
- Do NOT add multiple queue slots, retry logic, or a configurable threshold — all explicitly out of scope.
- The 3000ms threshold is a locked design constant per Ashley 2026-07-19; if Ashley later wants to tune it, do so as a separate patch, not via a config prop.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx tsc --noEmit 2>&1 | tail -20</automated>
    <automated>grep -c "Hourglass" src/ui/features/pretty-view/ComposeBox.tsx</automated>
    <automated>grep -c "queuedText" src/ui/features/pretty-view/ComposeBox.tsx</automated>
    <automated>grep -c "dispatchTimerRef" src/ui/features/pretty-view/ComposeBox.tsx</automated>
    <automated>grep -n "setTimeout" src/ui/features/pretty-view/ComposeBox.tsx | grep -v '^[0-9]*:[[:space:]]*//' | grep 3000</automated>
    <automated>grep -c "isIdle" src/ui/features/pretty-view/ComposeBox.tsx</automated>
    <automated>grep -c 'Queued — waiting for idle\|Queued -- waiting for idle' src/ui/features/pretty-view/ComposeBox.tsx</automated>
  </verify>
  <done>
- `npx tsc --noEmit` reports 0 errors.
- `Hourglass` appears in the lucide-react import and at least once in JSX (grep count ≥ 2).
- `queuedText` state referenced multiple times (grep count ≥ 8: declaration + watchdog + fireDispatch + 3 cancel-guards + handleQueue + overlay condition + Textarea disabled + button disabled + button className ternary).
- `dispatchTimerRef` referenced ≥ 6 times (declaration + watchdog set + watchdog cleanup + fireDispatch reset + 3 cancel-guards + unmount cleanup + handleQueue cancel branch).
- A `setTimeout(..., 3000)` call exists in the watchdog effect.
- `isIdle` referenced ≥ 4 times (prop interface + destructure + JSDoc + watchdog effect body + watchdog dep array).
- Overlay label string present.
- Reset button (patch #83), meter well, ThumbsUp Button, Send Button, and Textarea styling classes present and unchanged (visual regression tested manually in Task 3 verification).
  </done>
</task>

<task type="auto">
  <name>Task 2: Forward isIdle prop from PrettyView to ComposeBox</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx</files>
  <action>
Add exactly one line to the ComposeBox render at lines 555-565.

Between the existing `contextPct={contextPct}` line (~559) and `hostId={hostId}` (~560), add `isIdle={isIdle}` on its own line. Placement is stylistic — anywhere in the props block works — but adjacency to `contextPct` reads well since both are session-liveness signals from the WebSocket stream. Result should be:

```
<ComposeBox
  onSend={onSend}
  canSend={status === "streaming"}
  contextPct={contextPct}
  isIdle={isIdle}
  hostId={hostId}
  tmuxSession={tmuxSession}
  identityName={pvIdentity?.displayName}
  className="shrink-0"
/>
```

Do NOT alter the `PrettyViewProps` interface — `isIdle` is already declared at line 69 with the correct shape (`boolean | null`) that our new ComposeBoxProps.isIdle matches verbatim. Do NOT touch the destructure at line 88 — `isIdle` already present. Do NOT touch any other ComposeBox prop (all upstream state stays intact). Do NOT touch the WIP indicator logic or any other consumer of `isIdle` elsewhere in the file (both consumers coexist cleanly; the WIP indicator gates on isIdle to render/hide a bubble, our new consumer gates on isIdle to schedule a dispatch — no overlap).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx tsc --noEmit 2>&1 | tail -20</automated>
    <automated>grep -n "isIdle={isIdle}" src/ui/features/pretty-view/PrettyView.tsx</automated>
    <automated>grep -c "ComposeBox" src/ui/features/pretty-view/PrettyView.tsx</automated>
  </verify>
  <done>
- `npx tsc --noEmit` reports 0 errors.
- Exactly one `isIdle={isIdle}` occurrence within the ComposeBox JSX render block.
- ComposeBox import and render still present (grep count ≥ 2: import + JSX use).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual browser verification of queue flow</name>
  <what-built>Patch #84 landed in-tree: Hourglass Queue button between ThumbsUp and Send, isIdle watchdog with 3s threshold, textarea pending overlay, cancel-on-immediate-action semantics, PrettyView prop forward. Build clean, typecheck clean.</what-built>
  <how-to-verify>
Run local dev if not already: `npm run dev`. Open a Skynet tab attached to a live Claude Code session that emits isIdle events (any tmux+claude pane on the fleet).

Test 1 — Happy path with real idle transition:
1. Send a prompt to Claude that will take ~10+ seconds to complete (e.g. "list every file in this repo and describe each in one sentence").
2. While Claude is still working, type a follow-up message into the compose textarea (e.g. "actually also count the total LOC").
3. Click the Hourglass button (between ThumbsUp and Send).
4. VERIFY: Hourglass button glows amber and pulses. Textarea shows a dark overlay with the Hourglass icon centered above the text "Queued — waiting for idle". Textarea is disabled (cannot type).
5. Wait for Claude to finish its current turn. Watch the timing: the backend needs ~4s of quiet + foreground=claude to flip isIdle=true, then our 3s watchdog fires → total ~7s from Claude's last output.
6. VERIFY: Message dispatches automatically. Textarea clears. Overlay dismisses. Amber pulse stops. Message appears in the conversation (via session-file tail).

Test 2 — Self-toggle cancel:
1. Type a message. Click Hourglass to arm.
2. Click Hourglass again while armed.
3. VERIFY: Overlay dismisses, textarea re-enables, focus returns to textarea, existing text preserved, no dispatch occurred.

Test 3 — Send supersedes queue (silent drop):
1. Type a message. Click Hourglass to arm.
2. Click Send button.
3. VERIFY: Immediate send fires (message goes out now, textarea clears). No error message. Queue silently drops (no residual amber pulse).

Test 4 — GoAhead supersedes queue (silent drop):
1. Type a message. Click Hourglass to arm.
2. Click ThumbsUp button.
3. VERIFY: "go ahead" fires immediately. Composed text stays in textarea (existing handleQuickSend semantics), but queue drops silently (no residual amber pulse, textarea no longer disabled).

Test 5 — Reset supersedes queue (silent drop):
1. Type a message. Click Hourglass to arm.
2. Click the reset cell in the meter well.
3. VERIFY: /id reset (with parenthetical hint) fires immediately. Textarea clears. Drain animation plays as before. Queue drops silently.

Test 6 — Empty text disabled:
1. With textarea empty, hover Hourglass button.
2. VERIFY: Button appears disabled (grayed / opacity-40), cursor is not-allowed.

Test 7 — Fail-loud on dispatch failure:
1. Type a message. Click Hourglass to arm.
2. Kill the backend or disconnect the WS mid-arm (simulate a transient loss right as Claude goes idle).
3. VERIFY: When the watchdog attempts dispatch and onSend returns false, the queue clears AND the inline error "Not connected — queued send failed" appears below the compose row.

Test 8 — Existing behaviors unchanged:
- Meter well shows segments and animates on reset (patch #83 intact).
- Draft body persists across page reloads (patch #57 intact — reload while composing, before arming; text seed returns).
- Warm-glass compose surround + textarea styling unchanged (patch #79-82 intact).
- ThumbsUp still fires "go ahead" without touching textarea text.
- Send Enter-key path still works.
- Shift-Enter still inserts newline.

Test 9 — Timing feel (Ashley):
- Does the ~7s total (4s backend + 3s watchdog) feel right? Not too eager, not too laggy? If not, note the preferred threshold for a follow-up tuning patch.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to commit, or describe issues (visual, timing, functional) to loop back into revision.</resume-signal>
</task>

</tasks>

<verification>
Overall phase checks:
- `npx tsc --noEmit` clean.
- All 9 manual browser tests pass.
- No visible regression in patches #57, #79, #81, #82, #83.
</verification>

<success_criteria>
- Both files modified per spec.
- Typecheck clean.
- All 9 verification cases pass in browser.
- Ashley greenlights the ~7s timing feel (or explicitly requests a threshold tune as a separate follow-up).
- Ready to commit as `feat(pretty-view): patch #84 queue button with isIdle watchdog` on `feat/tab-title-from-tmux`.
</success_criteria>

<output>
Create `.planning/quick/260719-u29-patch-84-queue-button-hourglass-icon-in-/260719-u29-SUMMARY.md` when done — record the two file diffs, timing feedback from Test 9, and the commit hash.
</output>
