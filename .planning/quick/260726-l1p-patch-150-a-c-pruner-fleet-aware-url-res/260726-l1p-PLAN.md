---
quick_id: 260726-l1p
mode: quick
type: execute
branch: feat/tab-title-from-tmux
autonomous: true
files_modified:
  - src/ui/state/conversation-store.ts
  - src/ui/state/conversation-store.test.ts
  - src/ui/AppShell.tsx
tags: [skynet, fork-tina, patch-150, pinner-fleet, url-restore, glow, active-set]
must_haves:
  truths:
    - "Activating any conversation row (fleet-derived or openTab-derived) does NOT unpin OTHER pinned fleet-derived rows"
    - "Activating a conversation row STILL prunes any pinnedId that is neither an openTab id nor a fleet-derived id (regression guard on pre-#149 pruner behavior)"
    - "A URL that captured N active sessions restores with the .active-set glow on ALL N restored tabs (not just the first), for both N=2 and N>2"
    - "Clicking a restored-tab conversation row loads its content without waiting on a second click (or, if the click-to-load bug is confirmed independent of C, it is fixed in a separate atomic commit)"
  artifacts:
    - path: "src/ui/state/conversation-store.ts"
      provides: "Fleet-aware pruner in updateOpenTabs — pinnedIds membership in state.fleetSessions is a keep-signal alongside openTabs membership"
    - path: "src/ui/state/conversation-store.test.ts"
      provides: "Two new regression tests: (1) fleet-pinned survival across openTabs mutation, (2) stale openTab pin still pruned"
    - path: "src/ui/AppShell.tsx"
      provides: "URL-restore loop invokes selectConversationDeferred (or addToActiveSet, per investigation) for EVERY restoredTabs entry, not restoredTabs[0] only"
  key_links:
    - from: "src/ui/state/conversation-store.ts::updateOpenTabs"
      to: "state.fleetSessions"
      via: "Set<string> built from fleetRowId(session.hostId, session.sessionName) — union with nextIds is the new keep-set"
      pattern: "fleet::"
    - from: "src/ui/AppShell.tsx (~L824-834 persisted-tab-restore branch)"
      to: "selectConversationDeferred | addToActiveSet"
      via: "for-loop over restoredTabs (replaces restoredTabs[0] hardcode)"
      pattern: "restoredTabs\\["
---

<objective>
Ship patch #150 as 3 atomic commits (with an optional 4th) on feat/tab-title-from-tmux:

1. **#150 A** — fix the pruner in `updateOpenTabs` so activating any conversation no longer nukes legitimate fleet-derived pins. Root cause post-#149 A: `pinnedIds` now legitimately holds both openTab ids and fleet ids, but the pruner still drops anything not in `openTabs`.
2. **#150 C-investigate** — read AppShell's URL-restore + restored-tab click-to-load path, decide whether Ashley's "un-glowed session did NOT auto-load its content" symptom is the same bug as the multi-tab-glow gap or a separate second bug. Land findings as a code comment near L833 (or an inline SUMMARY note if no code change).
3. **#150 C** — iterate ALL `restoredTabs` in AppShell's persisted-restore branch instead of hardcoding `restoredTabs[0]`.
4. **(conditional #150 D)** — only if C-investigate finds a distinct second bug in the restored-tab click-to-load path.

Purpose:
- **#150 A is actively broken in production** and blocking Ashley's fleet-panel workflow (pin 4-5 fleet rows → single click destroys all pins).
- **#150 C** cleans up a visual regression from #145 that leaves Ashley uncertain which of her restored tabs are "live".

Output: 3 or 4 atomic commits on feat/tab-title-from-tmux, no push, no build, no deploy. Orchestrator will separately recommend deploy (solo-deploy carveout for the followup-1 fix).

Constraints (fork-specific, from planning_context):
- NO `Co-Authored-By` trailer on any commit (fork convention).
- NO write to `~/.claude/identities/tina/skynet-patches.md` (Ashley's 2026-07-23 batch-writeups-until-deploy rule).
- NO deploy at task end; NO push at task end (push happens at deploy time).
- Every commit MUST run `npm run type-check` + the relevant vitest suite in its verify step, gate-blocking.
- Stay on `feat/tab-title-from-tmux`; do NOT create a new branch.
- Anti-scope-creep: NO settings modal, AppRail extensions, dashboard, snippets manager, host manager UI, admin console, file manager UI, top-level Skynet chrome, keyboard shortcut editor. If a temptation surfaces during work, name it in commit body and reject.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@src/ui/state/conversation-store.ts
@src/ui/state/conversation-store.test.ts
@src/ui/AppShell.tsx

Interface anchors the executor should already have from the plan text (do NOT re-derive):
- `updateOpenTabs(tabs: Tab[])` at `src/ui/state/conversation-store.ts:494`. Pruner loop at L503-514 iterates `state.pinnedIds` and drops any id not in `nextIds` (set built from `tabs[].id`). This is the loop to teach about fleet membership.
- `pinConversation(id)` at `src/ui/state/conversation-store.ts:695`. Post-#149 A, no openTabs guard — accepts any id, including `fleet::${hostId}::${sessionName}` ids from `fleetRowId()` at L262.
- `FleetSession` type at `src/ui/state/conversation-store.ts:83` — `{ hostId: number; hostName: string; sessionName: string; created: number }`. Fleet row id = `` `fleet::${hostId}::${sessionName}` ``.
- `state.fleetSessions: FleetSession[]` at L150. Updated only via `updateFleetSessions(sessions)` at L578.
- `updateFleetSessions` at L578, `unpinConversation` at L711, `togglePinConversation` at L719 — helpers the tests already use.
- Test file layout: describe-blocks named `"conversation-store: <topic>"`, imports at L1-24, `beforeEach` resets state at L74-82 (calls `updateOpenTabs([])`, `updateFleetSessions([])`). Existing pin/prune describe blocks at L142 ("pin float + unpin restore"), L180 ("pin per-session"), L205 ("session-end lifecycle"). Add the two new tests as a new describe block AFTER L239 (right after "session-end lifecycle") named `"conversation-store: pruner fleet-aware (patch #150 A)"`.
- AppShell persisted-restore branch: `src/ui/AppShell.tsx:810-834` — `restoredTabs` array is populated in the for-loop above (~L722-822), then L824 checks `restoredTabs.length > 0`, L825-831 merges into `setTabs`, L832-833 does `setActiveTabId(restoredTabs[0].id)` + `selectConversationDeferred(restoredTabs[0].id)`. THIS is the hardcode to iterate.
- The URL-driven initial-open branch below at L849-903 (`if (pending)`) uses a similar `openedIds[]` accumulator + `openedIds[idx]` focus pattern — that branch is NOT the bug (it already handles multi-tab focus correctly via `activeIndex`), do NOT touch it.
- `selectConversationDeferred(id)` at `src/ui/state/conversation-store.ts:657` — parks id in `pendingSelectId` if not yet in openTabs; flushes into `selectedId` when `updateOpenTabs` sees it arrive. Direct `selectConversation(id)` at L620 also calls `addToActiveSet(id)` unconditionally when id is non-null.
- `addToActiveSet(id)` — Patch #137 activeSet mutator. `selectConversation` calls it at L646; `selectConversationDeferred`'s flush path (via `selectConversation` semantics) also lands on activeSet. Investigation must decide whether the multi-tab-glow fix needs one call site (`selectConversationDeferred` per tab, letting the flush cascade) or two (explicit `addToActiveSet` per restored id to guarantee glow BEFORE the deferred flush resolves).
- AppShell already imports `selectConversationDeferred` at line 59. If C-investigate concludes `addToActiveSet` is also needed, add it to the same import.

Anti-patterns to reject (name them in commit body if they surface):
- Do NOT change `pinConversation` to add back an openTabs guard — #149 A intentionally removed it.
- Do NOT change the fleet-row synthesis in `computeSnapshot` (L293-312) — pin visibility is a separate concern from row emission.
- Do NOT touch the RDP-row synthesis at L440-472 — RDP is out of scope for #150.
- Do NOT touch the URL-driven initial-open branch at AppShell L849-903 — it already handles multi-tab correctly.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: patch #150 A — pruner fleet-aware</name>
  <files>src/ui/state/conversation-store.ts, src/ui/state/conversation-store.test.ts</files>
  <behavior>
    New describe block in conversation-store.test.ts (INSERT after the "session-end lifecycle" describe that ends around L239), named `"conversation-store: pruner fleet-aware (patch #150 A)"`, containing:

    Test 1 — "clicking a pinned fleet row does NOT unpin OTHER pinned fleet rows":
      Setup: seed hostTree + hostsFlat with hostA (id 1) and hostB (id 2). Call `updateFleetSessions([...])` with 4 sessions: {hostId:1, sessionName:"work"}, {hostId:1, sessionName:"scratch"}, {hostId:2, sessionName:"srv"}, {hostId:2, sessionName:"dev"} — all four synthesize fleet-only rows with ids `fleet::1::work`, `fleet::1::scratch`, `fleet::2::srv`, `fleet::2::dev`. Call `pinConversation` for each of the four ids in an `act()`. Assert `__getSnapshotForTest().pinnedIds.size === 4` and all four ids present.
      Exercise: call `updateOpenTabs([makeTab("t1", "terminal", hostA)])` in an `act()` — an openTab that does NOT reference any of the four fleet ids.
      Assert (post-#150 A): `__getSnapshotForTest().pinnedIds.size === 4` and all four fleet ids still present. (Pre-#150 A, size would be 0.)

    Test 2 — "clicking an openTab row still prunes stale openTab pins as before" (regression guard):
      Setup: seed hostTree with hostA. Call `updateOpenTabs([makeTab("t1", "terminal", hostA), makeTab("t2", "terminal", hostA)])`. Pin "t1" and "t2" via `pinConversation` in `act()`. Assert both in pinnedIds.
      Exercise: call `updateOpenTabs([makeTab("t2", "terminal", hostA)])` in an `act()` — t1 is now gone from openTabs, and it is NOT a fleet id (no updateFleetSessions call ever made in this test → fleetSessions is empty per beforeEach).
      Assert: `__getSnapshotForTest().pinnedIds.has("t1") === false` AND `__getSnapshotForTest().pinnedIds.has("t2") === true`. (Pre-#150 pruner behavior on non-fleet ids is preserved.)

    Both tests use the existing `makeTab` / hostTree fixtures already in the file — do NOT invent new fixture helpers. If the existing fixtures don't already export a two-host layout, extend them minimally at the top of the new describe block (do NOT modify existing describe-block fixtures).
  </behavior>
  <action>
    RED phase: Add the new describe block with the two tests above. Run the suite; both new tests MUST fail (Test 1 because pruner drops the fleet ids; Test 2 either passes trivially or fails depending on how the fixtures land — the intent is that Test 2 is a passing regression guard both before AND after the fix, and Test 1 is the failing test that drives the fix).

    GREEN phase: Modify `updateOpenTabs` at `src/ui/state/conversation-store.ts:494` to make the pruner fleet-aware. Specifically:
      - Before the pruner loop at L503-514, build a `fleetPinKeepSet: Set<string>` from `state.fleetSessions` by calling `fleetRowId(session.hostId, session.sessionName)` for each session. (Only build it if `state.pinnedIds.size > 0` — micro-guard against paying the O(fleetSessions) cost on the common empty-pinned hot path.)
      - Inside the loop, the drop condition changes from `if (!nextIds.has(id))` to `if (!nextIds.has(id) && !fleetPinKeepSet.has(id))`. Semantics: keep the pin if the id is EITHER a currently-open tab OR a currently-known fleet session id. Drop it only when it is neither.
      - Preserve the existing copy-on-write pattern (`nextPinnedIds = new Set(state.pinnedIds)` on first drop only) — do NOT eagerly clone.
      - Add an inline comment referencing patch #150 A + the two test names, explaining the fleet-union keep-set (mirroring the pre-existing #149 A comment style at pinConversation L697-704).

    Do NOT change `pinConversation`, `unpinConversation`, `togglePinConversation`, `updateFleetSessions`, `computeSnapshot`, or any other function. Do NOT add an openTabs guard back to `pinConversation` (that would revert #149 A).

    Run the full conversation-store test suite. Both new tests MUST pass. All pre-existing tests MUST still pass (especially the pin/unpin and session-end-lifecycle blocks — if any of them fail, the fix has an unintended side effect and must be revised).

    REFACTOR phase: if the diff has repetition or the comment is unclear, tighten it. Do NOT expand scope.

    Commit as a single atomic commit with subject `patch #150 A: pruner fleet-aware — updateOpenTabs no longer nukes legitimate fleet-derived pinnedIds` and a body that: (1) states the pre/post symptom (Ashley's 4 pinned fleet rows survive activation), (2) names the fleet-union keep-set as the mechanism, (3) references the two test names as regression guards. NO Co-Authored-By trailer.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npm run type-check && npx vitest run src/ui/state/conversation-store.test.ts</automated>
  </verify>
  <done>
    - `updateOpenTabs` pruner reads `state.fleetSessions` and keeps any pinnedId whose id equals `fleet::${hostId}::${sessionName}` for some session in `fleetSessions`.
    - Two new tests exist in `conversation-store.test.ts` under a `"pruner fleet-aware (patch #150 A)"` describe block and both pass.
    - All pre-existing conversation-store tests still pass.
    - `npm run type-check` passes.
    - One atomic commit landed on `feat/tab-title-from-tmux` with the specified subject and body; no push.
  </done>
</task>

<task type="auto">
  <name>Task 2: patch #150 C-investigate — URL-restore multi-tab click-to-load path</name>
  <files>src/ui/AppShell.tsx (comment only; possibly SUMMARY note)</files>
  <action>
    Read `src/ui/AppShell.tsx` from ~L722 (start of `loadSavedTabs`) through ~L913 (end of the loadSavedTabs effect). Focus areas:

    1. Persisted-tab-restore branch L810-834: how `restoredTabs` is built, how it's merged into `setTabs`, and what `setActiveTabId(restoredTabs[0].id) + selectConversationDeferred(restoredTabs[0].id)` at L832-833 actually causes downstream. Trace: does `selectConversationDeferred` (conversation-store.ts:657) reach `addToActiveSet` via the flush path in `updateOpenTabs`, or does the activeSet remain empty for the non-focused restored tabs even after the fix in Task 3?

    2. The click-to-load path for a restored tab that is in `tabs` (and thus in the tab bar) but NOT in `activeSet` (no glow). Grep for the tab-bar click handler (likely in `TabBar.tsx` or a callback prop passed from AppShell to whatever renders the tab bar). Determine: when Ashley clicks the un-glowed restored tab, does the click handler:
       (a) call `setActiveTabId` + `selectConversationDeferred` (or the equivalent), which SHOULD auto-load content since the tab is already in the tab bar with a `restoredSessionId`, OR
       (b) somehow skip the content-load path because the tab was "restored but not activated" and the load-on-activate wiring only fires on the first-render focused tab.

    3. Sub-question — is Ashley's "did NOT auto-load its content, had to wait" symptom:
       (A) the SAME bug as the multi-tab glow gap — i.e. fixed by Task 3's iteration of `restoredTabs` (because "load content" is triggered by the same effect that gives glow, so fixing glow fixes load too), OR
       (B) a SEPARATE bug — i.e. after Task 3 lands, all restored tabs glow but the click-to-load handler still has a latency issue for the ones that were restored-but-never-focused, requiring a distinct fix.

    Land findings as an inline comment INSIDE `src/ui/AppShell.tsx` immediately BEFORE the `if (restoredTabs.length > 0)` block at L824. The comment must:
      - Be a `//`-style comment block, 5-20 lines.
      - Explicitly state the investigation outcome: `SAME_BUG` (Task 3's iteration covers both symptoms) or `SEPARATE_BUG` (a #150 D commit is required, and here's what it must fix).
      - If `SAME_BUG`: name the mechanism (why iterating `restoredTabs` for `selectConversationDeferred` also causes content-load on each restored tab, e.g. "the load-on-select effect keyed on selectedId fires for whichever tab is currently selected, and the deferred-select-per-tab pattern ensures each restored tab passes through selectedId briefly during flush" — VERIFY this by reading the actual load-on-select effect before writing this claim).
      - If `SEPARATE_BUG`: name the specific file+line where the click-to-load handler skips content-load for restored-but-unfocused tabs, describe the fix in one sentence, and note that `#150 D` will address it.
      - Reference patch #150 C + Ashley's 2026-07-24 UAT report.

    Do NOT modify any other code in this task. Do NOT add the fix — even if the investigation reveals the fix is trivial, it lands in Task 3 (SAME_BUG) or a Task 4 (SEPARATE_BUG). This task ONLY captures findings.

    Commit as atomic with subject `patch #150 C investigate: URL-restore multi-tab click-to-load path` and a body summarizing the finding (SAME_BUG or SEPARATE_BUG + mechanism). NO Co-Authored-By trailer. The commit will be small (comment-only) and that is expected.

    If, mid-investigation, you discover the investigation itself needs to touch more code to confirm the finding (e.g. temporarily adding a console.log to trace), do NOT commit that scaffolding — remove it before committing. The commit is comment-only.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npm run type-check</automated>
  </verify>
  <done>
    - `src/ui/AppShell.tsx` has an inline investigation comment immediately before the `if (restoredTabs.length > 0)` block (~L824) with an explicit `SAME_BUG` or `SEPARATE_BUG` verdict.
    - If `SEPARATE_BUG`, the comment names the file, line, and one-sentence fix for the second bug.
    - No other code in `src/ui/AppShell.tsx` was modified.
    - `npm run type-check` passes.
    - One atomic comment-only commit landed on `feat/tab-title-from-tmux`; no push.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: patch #150 C — URL-restore multi-tab glow</name>
  <files>src/ui/AppShell.tsx, src/ui/state/conversation-store.test.ts (or nearest AppShell/integration test)</files>
  <behavior>
    Locate the existing test file that exercises AppShell's tab-restore path (candidates in order: an `AppShell.test.tsx` beside AppShell, an integration test under `src/ui/__tests__/`, or a store-level test in `conversation-store.test.ts`). If NO existing AppShell/integration test exists AND creating one would require Vitest+React-Testing-Library setup that isn't already scaffolded, fall back to a store-level test that simulates the effect: call `updateOpenTabs([tabA, tabB])` then, mimicking what the fixed AppShell will do, `selectConversationDeferred("tabA-id")` and `selectConversationDeferred("tabB-id")` in the same `act()`, then assert `__getSnapshotForTest().activeSet` contains BOTH ids (glow parity). Choose the store-level fallback UNLESS an AppShell test file already exists and can host the new case.

    New test — "two-URL-tab restore glows both restored tabs":
      Setup: seed hostTree + hostsFlat with hostA. Create two tabs `tabA = makeTab("restored-a", "terminal", hostA)` and `tabB = makeTab("restored-b", "terminal", hostA)`.
      Exercise: call `updateOpenTabs([tabA, tabB])` in `act()`, then in the same or subsequent `act()` call `selectConversationDeferred("restored-a")` and `selectConversationDeferred("restored-b")` (mirroring the fixed AppShell loop). Note: `selectConversationDeferred` on an id already in openTabs behaves like `selectConversation`, which calls `addToActiveSet` — see conversation-store.ts L657 and L646.
      Assert: `__getSnapshotForTest().activeSet.has("restored-a") === true` AND `__getSnapshotForTest().activeSet.has("restored-b") === true`.

    Rationale: this asserts the STORE-LEVEL contract that per-tab deferred selects accumulate glow on all tabs. The AppShell fix (iterating restoredTabs) is what causes this store-level pattern to fire in production.
  </behavior>
  <action>
    RED phase: Add the test described above. Run it — it should already PASS (the store contract works; the bug is in AppShell not the store). If it passes trivially, KEEP it as a regression guard that pins the store contract Task 3's AppShell change depends on. If it FAILS, stop and investigate — the store contract may not be what Task 2's investigation assumed, and the C-investigate comment may need revision.

    GREEN phase: In `src/ui/AppShell.tsx` at L810-834, replace the hardcoded `restoredTabs[0]` behavior:
      - Keep `setActiveTabId(restoredTabs[0].id)` — only ONE tab can be the focused tab-bar tab, and the first restored tab is the sensible default. (If the investigation in Task 2 revealed a specific "last-focused" persistence signal that should drive this choice instead, use that; otherwise stay with `[0]` to preserve current focus behavior.)
      - Replace the single `selectConversationDeferred(restoredTabs[0].id)` call with a loop over ALL `restoredTabs`, calling `selectConversationDeferred(t.id)` for each. This causes each restored tab to be routed through the same select→addToActiveSet path that gives glow.
      - If Task 2's C-investigate concluded `SAME_BUG` and identified that `selectConversationDeferred` alone is insufficient to glow non-focused tabs (e.g. because the deferred slot is last-write-wins and only the final id lands in activeSet), then ALSO import `addToActiveSet` from `./state/conversation-store` (add to the existing import at line 6-24) and call `addToActiveSet(t.id)` for each restored tab inside the same loop. The C-investigate comment must be updated to match the actual implementation choice.
      - Add an inline comment referencing patch #150 C + the two-URL-tab test name, explaining why the loop replaces the `[0]` hardcode.

    Do NOT touch the URL-driven initial-open branch at L849-903 (already correct). Do NOT touch the tab-restore data-loading loop at L722-822 (that's the "what to restore" phase; the fix is in "what to focus/glow" phase).

    Run the full test suite for the file(s) touched. New test passes. All pre-existing tests pass.

    REFACTOR phase: if the loop can share iteration with the `newTabs` merge at L825-830, do NOT force the merge — keep the concerns separate for readability.

    Commit as atomic with subject `patch #150 C: URL-restore multi-tab glow — iterate all restoredTabs, not just [0]` and a body: (1) state Ashley's UAT symptom (2 restored tabs, only 1 glowed), (2) name the fix mechanism (per-tab selectConversationDeferred [+ addToActiveSet if C-investigate said so]), (3) reference the two-URL-tab test as regression guard, (4) note the retained `setActiveTabId(restoredTabs[0].id)` for tab-bar focus. NO Co-Authored-By trailer.

    Skip Task 4 if Task 2 concluded `SAME_BUG`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npm run type-check && npx vitest run src/ui/state/conversation-store.test.ts</automated>
  </verify>
  <done>
    - `src/ui/AppShell.tsx` at ~L824-834 iterates ALL `restoredTabs` (not just `[0]`) when routing through `selectConversationDeferred` (and optionally `addToActiveSet`, per C-investigate).
    - `setActiveTabId(restoredTabs[0].id)` is retained (or replaced per C-investigate's persisted-focus finding).
    - New two-tab-restore-glow regression test exists (store-level fallback if no AppShell integration test scaffolded) and passes.
    - All pre-existing tests for touched files pass.
    - `npm run type-check` passes.
    - One atomic commit landed on `feat/tab-title-from-tmux`; no push.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4 (CONDITIONAL — only if Task 2 concluded SEPARATE_BUG): patch #150 D — restored-tab click-to-load fix</name>
  <files>src/ui/AppShell.tsx (or the file identified in Task 2's investigation), plus test file for the click-to-load path</files>
  <behavior>
    Guard: Skip this task entirely if Task 2's investigation concluded `SAME_BUG`. If `SEPARATE_BUG`, execute below.

    Read Task 2's C-investigate comment in `src/ui/AppShell.tsx` for the specific file, line, and one-sentence fix description. Use THAT as the source of truth for what this task does — do NOT re-investigate. If the C-investigate comment is missing or ambiguous, STOP and return control to the planner (revise C-investigate first).

    Write a failing test that exercises the specific click-to-load path identified. The test structure depends on the actual bug location:
      - If the bug is in a click handler in AppShell.tsx or a tab-bar child component, write a test that mounts the relevant surface with two restored tabs (one focused, one un-focused), simulates a click on the un-focused tab, and asserts the tab's content-load side effect fires (e.g. a mock WebSocket connect, a fetch call, or a store mutation) WITHOUT requiring a second click / a wait.
      - If the bug is in a store-level path (e.g. selection does not trigger load for tabs that were restored-but-not-focused), write a store-level test that mirrors the setup.

    The exact test shape is derived from what Task 2 identified — the planner cannot pre-specify without knowing the bug. If Task 2's finding was too vague to write a test, that is a Task 2 failure; return control to the planner.
  </behavior>
  <action>
    Apply the fix as described in the C-investigate comment. Keep the fix minimally-scoped to the click-to-load path — do NOT expand into adjacent refactoring.

    Run the new test — it MUST pass post-fix and MUST fail pre-fix (RED→GREEN verified by staging the test alone first).

    Run the full test suite for touched files. All pre-existing tests pass.

    Commit as atomic with subject `patch #150 D: <specific name from C-investigate>` and a body: (1) state Ashley's UAT symptom (click did NOT auto-load content), (2) name the specific mechanism from C-investigate, (3) reference the new regression test. NO Co-Authored-By trailer.

    If, during implementation, the fix turns out to be substantially different from what C-investigate described, STOP and return control to the planner — do NOT silently expand scope. Ashley + Tina's fork rules require that surprises get surfaced, not absorbed.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npm run type-check && npx vitest run src/ui/state/conversation-store.test.ts</automated>
  </verify>
  <done>
    - EITHER: Task 2 concluded SAME_BUG and this task is skipped (documented in the final SUMMARY).
    - OR: Task 2 concluded SEPARATE_BUG and the fix identified in C-investigate is applied, a regression test guards it, all tests pass, `npm run type-check` passes, and one atomic commit landed on `feat/tab-title-from-tmux`.
  </done>
</task>

</tasks>

<verification>
Post-execution phase-level checks (all MUST pass before returning control):

1. `cd /home/ubuntu/skynet && git log --oneline feat/tab-title-from-tmux -n 5` — expect 3 new commits (SAME_BUG path) or 4 new commits (SEPARATE_BUG path). Each subject matches the patterns specified in Tasks 1-4.
2. `cd /home/ubuntu/skynet && git log feat/tab-title-from-tmux -n 5 --format='%B' | grep -v '^#' | grep -c 'Co-Authored-By'` — expect `0`. Fork convention.
3. `cd /home/ubuntu/skynet && npm run type-check` — passes.
4. `cd /home/ubuntu/skynet && npx vitest run src/ui/state/conversation-store.test.ts` — all tests pass, and the file contains a describe block titled `"conversation-store: pruner fleet-aware (patch #150 A)"` with the two tests specified in Task 1.
5. `cd /home/ubuntu/skynet && grep -n "patch #150" src/ui/state/conversation-store.ts src/ui/AppShell.tsx` — expect at least three anchor comments: one in `updateOpenTabs`, one in AppShell's C-investigate comment block, one in AppShell's C-fix loop.
6. `cd /home/ubuntu/skynet && git status` — clean; no uncommitted changes, no untracked files.
7. `cd /home/ubuntu/skynet && git branch --show-current` — outputs `feat/tab-title-from-tmux`. No new branch was created.
8. NO push (`git log origin/feat/tab-title-from-tmux..feat/tab-title-from-tmux --oneline` shows the new commits as unpushed — this is the correct state; push happens at deploy time).
9. NO write to `~/.claude/identities/tina/skynet-patches.md` (batch-writeups-until-deploy rule).
10. NO deploy attempted. The orchestrator will separately recommend deploy after this task completes.
</verification>

<success_criteria>
- Ashley's followup-1 (actively broken in production) is fixed: pinning 4-5 fleet rows then clicking one to activate its session preserves all pins — no visual or positional regression.
- Ashley's followup-3 (medium): a URL that captured 2 (or more) active sessions restores with `.active-set` glow on ALL restored tabs.
- If the click-to-load bug on the un-glowed session was a distinct second bug, it is fixed in a dedicated `#150 D` commit (else covered by `#150 C`).
- 3 or 4 atomic commits on `feat/tab-title-from-tmux`, no push, no build, no deploy, no Co-Authored-By, no skynet-patches.md write.
- Scope discipline held: no touched files outside `src/ui/state/conversation-store.ts`, `src/ui/state/conversation-store.test.ts`, `src/ui/AppShell.tsx` (plus optionally the file identified in C-investigate for Task 4).
- Anti-scope-creep held: no settings modal, AppRail, dashboard, snippets, host-manager UI, admin console, file-manager, top-level chrome, keyboard-shortcut-editor changes appeared.
</success_criteria>

<output>
Create `.planning/quick/260726-l1p-patch-150-a-c-pruner-fleet-aware-url-res/260726-l1p-SUMMARY.md` when done. The SUMMARY MUST include:
- Commit hashes + subjects for all 3 (or 4) commits.
- Task 2's investigation verdict (`SAME_BUG` or `SEPARATE_BUG`) and the mechanism named.
- Confirmation that Task 4 was executed or skipped, with the reason.
- Any anti-scope-creep temptations that surfaced and were rejected (per the CLAUDE.md fork convention).
- Confirmation that no push, build, deploy, or skynet-patches.md write occurred.
- A one-line recommendation to the orchestrator on whether to trigger the solo-deploy carveout (yes for followup-1's production-broken fix, unless the executor observed something concerning during Task 1 that warrants delay).
</output>
