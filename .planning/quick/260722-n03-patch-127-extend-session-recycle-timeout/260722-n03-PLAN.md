---
phase: quick-260722-n03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-view/SessionHoldingOverlay.tsx
autonomous: true
requirements: [PATCH-127]

must_haves:
  truths:
    - "PrettyView client-side holding_timeout watchdog now fires at 300000ms (5 min), not 120000ms (2 min)"
    - "SessionHoldingOverlay error variant renders a warm-red-tinted card body (gradient + text color), not the neutral navy gradient"
    - "SessionHoldingOverlay neutral variant (error === false) is byte-for-byte unchanged"
    - "Static glyph preserved in both variants (no animate-spin) — patch #74 motion-channel guardrail intact"
    - "tsc passes clean"
  artifacts:
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "300000ms watchdog constant + updated comment block"
      contains: "300000"
    - path: "src/ui/features/pretty-view/SessionHoldingOverlay.tsx"
      provides: "error-branch warm-red gradient + text color, ternary against error prop"
      contains: "rgba(85,30,35"
  key_links:
    - from: "src/ui/features/pretty-view/PrettyView.tsx"
      to: "setHoldingTimeoutError"
      via: "setTimeout(..., 300000) inside patch #122 belt-and-suspenders useEffect"
      pattern: "300000"
    - from: "src/ui/features/pretty-view/SessionHoldingOverlay.tsx"
      to: "error prop ternary on gradient + text-color classes"
      via: "error ? warm-red : neutral in the glass card className cn(...)"
      pattern: "error \\?"
---

<objective>
Patch #127 — extend the patch #122 client-side session-recycle watchdog from 120000ms (2 min) to 300000ms (5 min), AND strengthen the patch #122 SessionHoldingOverlay error variant so the whole card body reads as clearly red (warm-red-tinted gradient + text color) rather than only the glyph + subtle inset shadow reading red against a neutral navy card.

Purpose: (1) 2 minutes is not enough time for a genuine session-recycle in real-world backend conditions — legitimate slow recycles were tripping the red-bubble prematurely. Bumping to 5 minutes preserves the belt-and-suspenders WS-drop safety net while cutting false-positive rate. (2) The patch #122 error variant left the card gradient and text color neutral, so the "recycle failed" state doesn't visually pop the way Ashley needs it to — you have to look at the glyph to notice. This makes the whole card body warm-red-tinted so the failure state reads at a glance.

Output: Two files edited (~10 lines total diff), tsc-clean, no other constants or the neutral-variant styling touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/ui/features/pretty-view/PrettyView.tsx
@src/ui/features/pretty-view/SessionHoldingOverlay.tsx

# Palette anchor for warm-red hue family (do not edit this file, just cite it in your commit for continuity):
# src/ui/features/pretty-view/ComposeBox.tsx line ~981 uses `hsla(0,72%,55%,1)` as the canonical
# meter red-band warm-red — the new error-branch gradient stays inside this hue family so we
# get one warm-red across the app rather than a clash.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend session-recycle watchdog to 5 min + strengthen overlay error variant to full warm-red card body</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx, src/ui/features/pretty-view/SessionHoldingOverlay.tsx</files>
  <action>
Make TWO surgical edits per patch #127 spec. Do not touch any other constants, effects, or styles. Do not modify the neutral variant of SessionHoldingOverlay. Do not add motion (animate-spin) to the error state — patch #74 motion-channel guardrail (glyph is STATIC in BOTH variants; error is a STATE, not WORK) is a hard invariant.

EDIT 1 — src/ui/features/pretty-view/PrettyView.tsx (patch #122 client-side watchdog):

Locate the useEffect just above line 500 with the comment block starting "Patch #122: client-side belt-and-suspenders holding_timeout watchdog." (currently around lines 492-508.)

Inside that effect (currently line 504) change the setTimeout duration:
  FROM: `}, 120000);`
  TO:   `}, 300000);`

In the comment block above the effect (currently lines 492-499), update the duration references:
  - The phrase "start a 120000ms timer" (currently around line 493) becomes "start a 300000ms timer (5 minutes)".
  - If the surrounding rationale sentence references "2 minutes" as a duration, change it to "5 minutes". If it does not reference a human-readable duration, do not invent one — just the "120000ms" → "300000ms (5 minutes)" swap.

Do NOT touch:
  - The paired reset-effect immediately below (lines ~510+ "reset error state when isHolding clears") — it has no duration constant.
  - The 350ms delay-arm effect above (lines ~485-490) — that is patch #74 overlay-mount delay, unrelated to patch #122's watchdog.
  - Any other setTimeout / setInterval / numeric constant in the file.

EDIT 2 — src/ui/features/pretty-view/SessionHoldingOverlay.tsx (error variant card body):

Locate the inner glass card `<div>` (currently lines 94-114). Its className is a `cn(...)` block. Two lines inside that block are currently UNCONDITIONAL and must become error-ternary:

Line 103 (currently):
  `"bg-[linear-gradient(160deg,rgba(45,55,80,0.5),rgba(28,35,55,0.55))]",`

Replace with ternary:
  ```
  error
    ? "bg-[linear-gradient(160deg,rgba(85,30,35,0.55),rgba(55,20,25,0.6))]"
    : "bg-[linear-gradient(160deg,rgba(45,55,80,0.5),rgba(28,35,55,0.55))]",
  ```

The neutral (`error === false`) branch MUST preserve the existing gradient bytes-verbatim: `rgba(45,55,80,0.5)` and `rgba(28,35,55,0.55)`. The error branch uses `rgba(85,30,35,0.55)` → `rgba(55,20,25,0.6)` — warm-red-tinted, slightly higher opacity so the failure card reads as substantial rather than translucent.

Line 104 (currently):
  `"text-[#dfe3ee]",`

Replace with ternary:
  ```
  error ? "text-[#f5d0d4]" : "text-[#dfe3ee]",
  ```

The neutral branch preserves `#dfe3ee` bytes-verbatim. The error branch uses `#f5d0d4` — a warm-red-tinted very-light that reads red-white against the red bubble.

Do NOT touch:
  - The border (`"border border-white/[0.08]",`) — kept neutral in both variants by design.
  - The shadow ternary from patch #122 (currently lines 106-112) — already correct.
  - The glyph ternary from patch #122 (currently lines 116-124) — already correct.
  - The scrim wrapper (`absolute inset-0 z-[110] ...`) — unrelated to error variant.
  - The aria-label ternary at the top of the outer div — already correct.
  - The `<span>` copy ternary at the bottom — already correct.
  - The file header comment block — the "warm-red hue and subtle inset shadow" description remains accurate; do not rewrite it.

Rationale hue anchor: the new error gradient stops (`rgba(85,30,35,...)` → `rgba(55,20,25,...)`) sit in the same warm-red hue family as ComposeBox.tsx line ~981 `hsla(0,72%,55%,1)` — this is the fork's canonical meter red-band warm-red. Do not deviate from these exact rgba values; they were pre-picked to keep one warm-red across the app.

BOUNDARY: This task is exactly two files, ~10 lines diff, no other changes. If typecheck flags something at an unrelated line as pre-existing type-debt (see STATE.md — the fork has known non-patch-127 test/type debt), leave it — do not scope-creep.
  </action>
  <verify>
    <automated>
cd /home/ubuntu/skynet &amp;&amp; \
npx tsc --noEmit 2>&amp;1 | tee /tmp/n03-tsc.log; \
TSC_EXIT=${PIPESTATUS[0]}; \
echo "--- tsc exit: $TSC_EXIT ---" &amp;&amp; \
git diff --stat -- src/ui/features/pretty-view/PrettyView.tsx src/ui/features/pretty-view/SessionHoldingOverlay.tsx &amp;&amp; \
echo "--- new gradient present in error branch: ---" &amp;&amp; \
grep -c 'rgba(85,30,35' src/ui/features/pretty-view/SessionHoldingOverlay.tsx &amp;&amp; \
echo "--- 300000 constant present in PrettyView: ---" &amp;&amp; \
grep -c '300000' src/ui/features/pretty-view/PrettyView.tsx &amp;&amp; \
echo "--- watchdog line no longer uses 120000 (context grep, expects zero matches): ---" &amp;&amp; \
grep -c 'setHoldingTimeoutError.*120000\|}, 120000)' src/ui/features/pretty-view/PrettyView.tsx &amp;&amp; \
echo "--- neutral gradient bytes preserved verbatim: ---" &amp;&amp; \
grep -c 'rgba(45,55,80,0.5),rgba(28,35,55,0.55)' src/ui/features/pretty-view/SessionHoldingOverlay.tsx &amp;&amp; \
echo "--- neutral text color preserved verbatim: ---" &amp;&amp; \
grep -c 'text-\[#dfe3ee\]' src/ui/features/pretty-view/SessionHoldingOverlay.tsx &amp;&amp; \
echo "--- warm-red text color present in error branch: ---" &amp;&amp; \
grep -c 'text-\[#f5d0d4\]' src/ui/features/pretty-view/SessionHoldingOverlay.tsx &amp;&amp; \
echo "--- NO animate-spin was introduced (motion-channel guardrail): ---" &amp;&amp; \
grep -c 'animate-spin' src/ui/features/pretty-view/SessionHoldingOverlay.tsx
    </automated>
  </verify>
  <done>
- `npx tsc --noEmit` exits 0 (or, if there is pre-existing type-debt in the repo unrelated to these two files, no NEW errors are introduced at the edited lines — cross-check against git stash / prior baseline).
- `git diff --stat` shows exactly 2 files touched: `src/ui/features/pretty-view/PrettyView.tsx` and `src/ui/features/pretty-view/SessionHoldingOverlay.tsx`. Combined diff is roughly 4-8 lines added / 4-8 lines removed — nothing near the "~10 lines total" budget is exceeded.
- Grep `rgba(85,30,35` in SessionHoldingOverlay.tsx returns exactly 1 (the new error-branch gradient stop).
- Grep `300000` in PrettyView.tsx returns >= 1 (the new watchdog constant + the "300000ms" mention in the comment block).
- Grep `setHoldingTimeoutError.*120000\|}, 120000)` in PrettyView.tsx returns 0 (the old constant is gone from the watchdog site; unrelated 120000 usages elsewhere in the file, if any, are fine).
- Grep `rgba(45,55,80,0.5),rgba(28,35,55,0.55)` in SessionHoldingOverlay.tsx returns exactly 1 (neutral gradient preserved bytes-verbatim inside the ternary).
- Grep `text-[#dfe3ee]` in SessionHoldingOverlay.tsx returns exactly 1 (neutral text color preserved bytes-verbatim inside the ternary).
- Grep `text-[#f5d0d4]` in SessionHoldingOverlay.tsx returns exactly 1 (new error-branch text color).
- Grep `animate-spin` in SessionHoldingOverlay.tsx returns 0 (motion-channel guardrail intact — no motion was added).
- The comment block above the patch #122 watchdog effect in PrettyView.tsx now says "300000ms" (or "5 minutes"), not "120000ms" / "2 minutes".
- The paired reset-effect just below the watchdog, the 350ms delay-arm effect just above, and all other constants in PrettyView.tsx are untouched.
- No console warnings introduced (informal — no `console.warn`/`console.error` was added by this diff).
  </done>
</task>

</tasks>

<verification>
Overall phase checks:
- `npx tsc --noEmit` exits 0 (or no new errors at edited lines vs. baseline).
- `git diff --stat` shows exactly 2 files, ~10 lines combined.
- All grep gates in the task `<verify>` block pass with the expected match counts.
</verification>

<success_criteria>
- Client-side watchdog now fires at 300000ms; comment block reflects "5 minutes".
- SessionHoldingOverlay error variant renders warm-red gradient + warm-red-tinted text; neutral variant is byte-for-byte unchanged.
- No motion added on error (glyph stays static).
- tsc clean; commit is ready for the batched deploy behind the 15-min deadman.
</success_criteria>

<output>
Create `.planning/quick/260722-n03-patch-127-extend-session-recycle-timeout/260722-n03-SUMMARY.md` when done, following the standard quick-task summary template. The summary should note: the two files touched, the exact numeric constant swap (120000 → 300000), the two ternary insertions in SessionHoldingOverlay, and confirmation that neutral-variant bytes + motion-channel guardrail are preserved. Flag that deploy is deferred — this batches with #118-#126 behind the same 15-min deadman.
</output>
