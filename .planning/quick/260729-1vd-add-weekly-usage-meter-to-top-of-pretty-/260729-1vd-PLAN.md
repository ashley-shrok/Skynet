---
phase: 260729-1vd-add-weekly-usage-meter-to-top-of-pretty-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-conversations/WeeklyUsageMeter.tsx
  - src/ui/features/pretty-conversations/WeeklyUsageMeter.test.tsx
  - src/ui/features/pretty-conversations/pretty-conversations.css
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
  - docker/nginx.conf
  - docker/nginx-https.conf
  - "src/backend/**/* (Express route registration — exact file discovered during Task 1)"
autonomous: true
requirements:
  - WEEKLY-METER-01  # Dual-race split-bar meter (F variant) in .pv-panel-header
  - WEEKLY-METER-02  # /api/usage backend proxy to collector at 100.113.23.63:9421
  - WEEKLY-METER-03  # Nginx routing in BOTH nginx.conf and nginx-https.conf
  - WEEKLY-METER-04  # 15s polling + graceful failure (retain last-known)
  - WEEKLY-METER-05  # Test coverage: component render, elapsed% math, fetch failure

must_haves:
  truths:
    - "GET /api/usage on the deployed skynet returns the collector JSON payload with 200 status"
    - "GET /api/usage returns 503 with {error:...} when the collector is unreachable (5s timeout)"
    - "The pretty-conversations panel header displays two stacked split-bars (5h and Week) below the title row"
    - "Each split-bar shows warm-coral usage fill on the top half and cool-cyan elapsed-time fill on the bottom half, divided by a 1px dark hairline"
    - "The meter polls /api/usage every 15s and updates the bar widths + percent numbers without a page reload"
    - "On fetch failure the meter retains the last-known values and does not crash the panel"
    - "The existing .pv-title, .pv-header-actions, and data-sidebar-toggle-overlaps sidebar-toggle padding behavior (patch #142) continue to work — pre-existing panel tests still pass"
  artifacts:
    - path: "src/ui/features/pretty-conversations/WeeklyUsageMeter.tsx"
      provides: "Dual-race meter React component with 15s polling"
      exports: ["WeeklyUsageMeter", "default"]
      min_lines: 60
    - path: "src/ui/features/pretty-conversations/WeeklyUsageMeter.test.tsx"
      provides: "Component tests: render, elapsed% math, fetch failure resilience"
      min_lines: 40
    - path: "src/ui/features/pretty-conversations/pretty-conversations.css"
      provides: "New .pv-panel-header-row wrapper + .pv-usage-meter styles; modified .pv-panel-header (flex-direction:column)"
      contains: ".pv-usage-meter"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "Restructured .pv-panel-header with inner .pv-panel-header-row wrapper + <WeeklyUsageMeter /> sibling"
      contains: "WeeklyUsageMeter"
    - path: "docker/nginx.conf"
      provides: "location /api/usage proxy block to Node backend upstream"
      contains: "/api/usage"
    - path: "docker/nginx-https.conf"
      provides: "location /api/usage proxy block to Node backend upstream"
      contains: "/api/usage"
  key_links:
    - from: "src/ui/features/pretty-conversations/WeeklyUsageMeter.tsx"
      to: "/api/usage"
      via: "fetch inside useEffect with 15000ms setInterval"
      pattern: "fetch\\(['\"]/api/usage"
    - from: "Express /api/usage route"
      to: "http://100.113.23.63:9421/"
      via: "server-side fetch with AbortController 5s timeout"
      pattern: "100\\.113\\.23\\.63:9421"
    - from: "docker/nginx.conf AND docker/nginx-https.conf"
      to: "Node backend upstream"
      via: "location /api/usage proxy_pass"
      pattern: "location.*\\/api\\/usage"
    - from: "PrettyConversationsPanel .pv-panel-header"
      to: "WeeklyUsageMeter component"
      via: "sibling render after .pv-panel-header-row wrapper"
      pattern: "<WeeklyUsageMeter"
---

<objective>
Add a dual-race split-bar weekly usage meter inside the pretty-conversations panel header (variant F, position 4 — locked design). The meter shows two stacked bars (5h + weekly windows), each with warm-coral usage% on the top half and cool-cyan elapsed-time% on the bottom half. Data comes from a new same-origin `/api/usage` backend route that proxies the tailnet-only collector at `http://100.113.23.63:9421/` (bypasses CORS + mixed-content). Frontend polls every 15s and gracefully retains last-known values on failure.

Purpose: Ashley needs at-a-glance visibility into Anthropic 5h + weekly rate-limit burn while working — the collector already exists; this ships the UI + the same-origin proxy that makes it consumable.

Output: Working meter visible in the panel header of every pretty-conversations view, backed by a proxied backend route that ships with matching nginx routing in both HTTP and HTTPS configs (per the hard CLAUDE.md nginx constraint), plus tests covering render, elapsed% math, and fetch-failure resilience.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
@src/ui/features/pretty-conversations/pretty-conversations.css
@docker/nginx.conf
@docker/nginx-https.conf

Reference implementation (locked design source — read for exact styling values only, do not re-explore):
@$HOME/.claude/identities/tina/bounties/weekly-usage-meter/metertry-inject.html

Related in-flight patch context (do not modify, just aware): sibling commit `9f1fdd4` on branch `feat/tab-title-from-tmux` will ship together in the caller's next bundled deploy.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend /api/usage proxy route + dual nginx routing</name>
  <files>src/backend/**/* (locate the express app assembly file — grep for `app.use.*\/api` or `router.get.*\/api\/` under src/backend/, src/server/, or root server file), docker/nginx.conf, docker/nginx-https.conf</files>
  <action>Locate the Express app assembly (grep `/api/` under src/backend/, src/server/, or the repo root for existing `router.get`/`app.use` patterns — mirror whatever pattern the closest sibling route uses). Add `GET /api/usage` that server-side-fetches `http://100.113.23.63:9421/` with an `AbortController` set to a 5000ms timeout. On success: return the upstream JSON verbatim with `Content-Type: application/json` at 200. On any fetch error, timeout, or non-2xx upstream: return `{"error":"usage collector unreachable"}` with status 503. Do NOT add auth — the collector is IP-gated to the tailnet and skynet-ec2 is on the tailnet; this route surfaces public data (the same Ashley sees in her statusLine). Per WEEKLY-METER-02.

Then, per the CLAUDE.md hard constraint (WEEKLY-METER-03), add matching `location /api/usage` blocks to BOTH `docker/nginx.conf` AND `docker/nginx-https.conf`. Find an existing `/api/*` location block in each file and mirror its `proxy_pass` upstream + headers exactly (same backend upstream host:port, same `proxy_set_header` list). Missing either file will 200 with `index.html` and crash the frontend on `.map` — this is non-negotiable. Do NOT restart docker or reload nginx — the caller drives deploy separately.</action>
  <verify>
    <automated>grep -c "location.*\/api\/usage\|location.*~.*api/usage" docker/nginx.conf docker/nginx-https.conf | grep -v ':0$' | wc -l | grep -q '^2$' && grep -rn "100.113.23.63:9421" src/backend src/server 2>/dev/null | head -1 | grep -q "9421"</automated>
  </verify>
  <done>Both nginx configs have a `location` block for `/api/usage` mirroring the sibling `/api/*` pattern; Express route registered that returns collector JSON on success or 503 `{"error":...}` on timeout/failure; no docker/nginx reload attempted.</done>
</task>

<task type="auto">
  <name>Task 2: CSS — restructure .pv-panel-header + add .pv-usage-meter styles</name>
  <files>src/ui/features/pretty-conversations/pretty-conversations.css</files>
  <action>Modify `.pv-panel-header`: change from single-row flex to a column stack. Add `flex-direction: column; align-items: stretch; gap: 8px;` while KEEPING existing padding + border-bottom + any `data-sidebar-toggle-overlaps` conditional padding-left rule (patch #142 — do not touch that selector). Per WEEKLY-METER-01.

Add new `.pv-panel-header-row { display: flex; align-items: center; justify-content: space-between; width: 100%; gap: 8px; }` — this wrapper will hold what used to be direct children of `.pv-panel-header`.

Add `.pv-usage-meter` styles matching the locked F variant tuner spec exactly (do NOT re-explore; values come from the design_locked block):
- `.pv-usage-meter { display: flex; flex-direction: column; gap: 6px; padding: 2px 0 0 0; }`
- `.pv-usage-meter-row { display: flex; align-items: center; gap: 8px; }`
- `.pv-usage-meter-label { font: 700 10px/1 inherit; color: #e8e4d8; opacity: 0.75; text-transform: uppercase; letter-spacing: 0.08em; min-width: 36px; }`
- `.pv-usage-meter-bar { flex: 1; position: relative; height: 10px; background: rgba(255,255,255,0.08); border-radius: 3px; overflow: hidden; }`
- `.pv-usage-meter-fill-usage { position: absolute; top: 0; left: 0; height: 50%; background: linear-gradient(90deg, hsla(38,60%,72%,0.85), hsla(20,72%,66%,0.95)); box-shadow: 0 0 10px hsla(20,70%,60%,0.45); border-radius: 3px 3px 0 0; }`
- `.pv-usage-meter-fill-elapsed { position: absolute; bottom: 0; left: 0; height: 50%; background: linear-gradient(90deg, hsla(200,50%,60%,0.75), hsla(190,60%,62%,0.85)); box-shadow: 0 0 8px hsla(190,60%,55%,0.35); border-radius: 0 0 3px 3px; }`
- `.pv-usage-meter-divider { position: absolute; left: 0; right: 0; top: 50%; height: 1px; background: rgba(0,0,0,0.25); transform: translateY(-0.5px); pointer-events: none; }`
- `.pv-usage-meter-pct { font: 600 11px/1 inherit; color: #e8e4d8; min-width: 32px; text-align: right; }`

NO legend class (Ashley explicitly removed it).</action>
  <verify>
    <automated>grep -q "pv-panel-header-row" src/ui/features/pretty-conversations/pretty-conversations.css && grep -q "pv-usage-meter-fill-usage" src/ui/features/pretty-conversations/pretty-conversations.css && grep -q "pv-usage-meter-fill-elapsed" src/ui/features/pretty-conversations/pretty-conversations.css && grep -q "flex-direction: column" src/ui/features/pretty-conversations/pretty-conversations.css</automated>
  </verify>
  <done>CSS compiles cleanly; `.pv-panel-header` now stacks column with gap 8px; `.pv-panel-header-row` provides the horizontal wrapper; all `.pv-usage-meter*` classes present with exact hsla/rgba values from the locked spec; existing `data-sidebar-toggle-overlaps` padding rule untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WeeklyUsageMeter component + panel integration</name>
  <files>src/ui/features/pretty-conversations/WeeklyUsageMeter.tsx, src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx</files>
  <behavior>
    - Test 1 (in Task 4): renders two `.pv-usage-meter-row` blocks (5h + Week) with correct usage% and elapsed% fill widths given a mock fetch response
    - Test 2 (in Task 4): computes elapsed% correctly — given `resets_at = now + 3600` for the 5h window (window_s=18000), elapsed% = (18000-3600)/18000*100 = 80%
    - Test 3 (in Task 4): on fetch rejection, does not crash and retains previously-rendered values (or renders empty placeholder on first-load failure)
  </behavior>
  <action>Create `src/ui/features/pretty-conversations/WeeklyUsageMeter.tsx` — a default-exported React component (also named export `WeeklyUsageMeter` for tests). Per WEEKLY-METER-01 + WEEKLY-METER-04:

- `useState<UsageResponse | null>(null)` for last-known data. `UsageResponse` interface reflects the collector shape: `{ five_hour: { used_percentage: number; resets_at: number }, seven_day: { used_percentage: number; resets_at: number }, updated_at: number, source_box: string }`.
- `useEffect` on mount: define `async function poll()` that fetches `/api/usage`, checks `res.ok`, parses JSON, and `setState(json)`. Wrap in try/catch — on error do NOT clear state (retains last-known values per WEEKLY-METER-04). Call `poll()` immediately, then `const id = setInterval(poll, 15000)`. Cleanup returns `clearInterval(id)`.
- Compute elapsed% inline (not in state) each render: `const elapsedPct = (resetsAt: number, windowS: number) => { const nowS = Date.now() / 1000; const raw = (nowS - (resetsAt - windowS)) / windowS * 100; return Math.max(0, Math.min(100, raw)); }`. Constants: `const FIVE_HOUR_WINDOW_S = 18000; const SEVEN_DAY_WINDOW_S = 604800;`.
- If `data === null`: render an empty-state container `<div className="pv-usage-meter" aria-busy="true" />` so layout is stable while first fetch is in flight.
- Otherwise render:
  ```
  <div className="pv-usage-meter">
    <div className="pv-usage-meter-row">
      <span className="pv-usage-meter-label">5h</span>
      <div className="pv-usage-meter-bar">
        <div className="pv-usage-meter-fill-usage" style={{ width: `${data.five_hour.used_percentage}%` }} />
        <div className="pv-usage-meter-fill-elapsed" style={{ width: `${elapsedPct(data.five_hour.resets_at, FIVE_HOUR_WINDOW_S)}%` }} />
        <div className="pv-usage-meter-divider" />
      </div>
      <span className="pv-usage-meter-pct">{Math.round(data.five_hour.used_percentage)}%</span>
    </div>
    <div className="pv-usage-meter-row">
      <span className="pv-usage-meter-label">Week</span>
      <div className="pv-usage-meter-bar">
        <div className="pv-usage-meter-fill-usage" style={{ width: `${data.seven_day.used_percentage}%` }} />
        <div className="pv-usage-meter-fill-elapsed" style={{ width: `${elapsedPct(data.seven_day.resets_at, SEVEN_DAY_WINDOW_S)}%` }} />
        <div className="pv-usage-meter-divider" />
      </div>
      <span className="pv-usage-meter-pct">{Math.round(data.seven_day.used_percentage)}%</span>
    </div>
  </div>
  ```

Then modify `PrettyConversationsPanel.tsx` (around line 527 — grep for `.pv-panel-header` to locate). Wrap the existing children of `<div className="pv-panel-header" ...>` (currently `.pv-title` + `.pv-header-actions`) in a new `<div className="pv-panel-header-row">`, then add `<WeeklyUsageMeter />` as a sibling AFTER that row wrapper but still INSIDE the `.pv-panel-header` div. CRITICAL: preserve the existing `data-sidebar-toggle-overlaps` attribute on `.pv-panel-header` and any related conditional class/attr (patch #142 sidebar-toggle-overlap fix). Add the `import WeeklyUsageMeter from './WeeklyUsageMeter';` at the top per existing import ordering conventions in that file.</action>
  <verify>
    <automated>test -f src/ui/features/pretty-conversations/WeeklyUsageMeter.tsx && grep -q "fetch('/api/usage')\|fetch(\"/api/usage\")" src/ui/features/pretty-conversations/WeeklyUsageMeter.tsx && grep -q "setInterval.*15000\|15000.*setInterval" src/ui/features/pretty-conversations/WeeklyUsageMeter.tsx && grep -q "WeeklyUsageMeter" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx && grep -q "pv-panel-header-row" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx && npx tsc --noEmit 2>&1 | grep -v "^$" | grep -iE "WeeklyUsageMeter|pretty-conversations" ; [ $? -ne 0 ]</automated>
  </verify>
  <done>Component file exists with polling + graceful failure; PrettyConversationsPanel wraps original header children in `.pv-panel-header-row` and renders `<WeeklyUsageMeter />` as sibling inside `.pv-panel-header`; `data-sidebar-toggle-overlaps` attribute + patch #142 padding behavior preserved; TypeScript compiles clean for these files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Tests — new component tests + fix panel tests if restructure broke them</name>
  <files>src/ui/features/pretty-conversations/WeeklyUsageMeter.test.tsx, src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx</files>
  <behavior>
    - Test A: `WeeklyUsageMeter` renders two rows with labels "5h" and "Week" and shows rounded usage% on the right side when fetch resolves with a mock response
    - Test B: elapsed% math is correct — with `resets_at = Math.floor(Date.now()/1000) + 3600` for five_hour, the `.pv-usage-meter-fill-elapsed` inline style width equals ~80% (allow ±1% tolerance for clock drift within the test)
    - Test C: when `fetch` rejects, the component does not throw and (a) on first-load renders the aria-busy empty container, (b) on subsequent failure retains the last-known rendered percentages
    - Test D: pre-existing `PrettyConversationsPanel.test.tsx` still passes — if any assertion querySelects `.pv-title` as a direct child of `.pv-panel-header`, update it to query inside `.pv-panel-header-row`
  </behavior>
  <action>Create `src/ui/features/pretty-conversations/WeeklyUsageMeter.test.tsx` using the same testing framework the sibling `PrettyBountyCountBadge.test.tsx` and `PrettyConversationsPanel.test.tsx` use (read one of them first to match import style — vitest vs jest, @testing-library/react conventions, mock helpers).

Test setup: mock `global.fetch` per test. For Test B, freeze time with `vi.useFakeTimers()` (or jest equivalent) OR compute `resets_at` from `Date.now()` at test-start and read the rendered inline width — assert within ±1% tolerance to absorb sub-second clock drift between compute-and-render. For Test C, first render with a resolving fetch, then swap `global.fetch` to a rejecting mock and advance the 15s interval; assert previous DOM state persists (percentages unchanged) and no error is thrown.

Then run the existing panel test file. If the header restructure broke any assertion (e.g. a test that does `container.querySelector('.pv-panel-header > .pv-title')` — the direct-child selector), update it to query via `.pv-panel-header .pv-panel-header-row .pv-title` or use a non-direct-child selector. DO NOT weaken assertions unrelated to the restructure. If the existing tests pass unchanged, leave them alone. Per WEEKLY-METER-05.

Mock the `WeeklyUsageMeter` in the panel test file if it triggers unwanted fetch noise — e.g. `vi.mock('./WeeklyUsageMeter', () => ({ default: () => <div data-testid="mock-usage-meter" /> }))` at the top of the panel test file, mirroring how other child components are handled there (check first before adding).</action>
  <verify>
    <automated>test -f src/ui/features/pretty-conversations/WeeklyUsageMeter.test.tsx && npx vitest run src/ui/features/pretty-conversations/WeeklyUsageMeter.test.tsx src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx 2>&1 | tail -30 | grep -qE "passed|✓" && ! npx vitest run src/ui/features/pretty-conversations/WeeklyUsageMeter.test.tsx src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx 2>&1 | tail -30 | grep -qE "failed|✗"</automated>
  </verify>
  <done>New WeeklyUsageMeter.test.tsx exists with the three behaviors above (render, elapsed% math, fetch failure resilience); test run passes for both the new file and the pre-existing PrettyConversationsPanel.test.tsx; no unrelated assertions weakened.</done>
</task>

</tasks>

<verification>
End-to-end sanity check after all four tasks complete (do NOT deploy — the caller drives deploy separately):

1. TypeScript compiles: `npx tsc --noEmit` — no new errors in `src/ui/features/pretty-conversations/` or backend routes.
2. Frontend build succeeds: `npm run build` (or the project's equivalent) — no CSS or JSX errors surfacing.
3. Test suite passes: `npx vitest run src/ui/features/pretty-conversations/` — WeeklyUsageMeter tests + PrettyConversationsPanel tests all green.
4. Nginx dual-config parity check: `grep -c "/api/usage" docker/nginx.conf docker/nginx-https.conf` — both files return count ≥ 1. If either is 0, the frontend WILL crash on deploy (CLAUDE.md hard constraint).
5. Manual visual (deferred to caller post-deploy): open the pretty-conversations panel, confirm meter renders below the title row inside the header block, both bars animate their widths on first render, poll updates visible in devtools network tab every 15s.
</verification>

<success_criteria>
- [ ] Backend `/api/usage` route exists and proxies `http://100.113.23.63:9421/` with 5s AbortController timeout; returns 503 `{"error":"usage collector unreachable"}` on failure (WEEKLY-METER-02)
- [ ] BOTH `docker/nginx.conf` AND `docker/nginx-https.conf` contain a `location` block for `/api/usage` mirroring existing `/api/*` proxy pattern (WEEKLY-METER-03 — CLAUDE.md hard constraint)
- [ ] `.pv-panel-header` restructured to column stack with `.pv-panel-header-row` wrapper holding original title + actions; `data-sidebar-toggle-overlaps` padding behavior preserved (patch #142)
- [ ] `.pv-usage-meter*` CSS classes present with the exact hsla/rgba/px values from the locked F-variant spec; no legend class
- [ ] `WeeklyUsageMeter.tsx` component exists: polls `/api/usage` every 15s, computes elapsed% inline from `resets_at`, retains last-known values on fetch failure, renders two rows (5h + Week) with warm-coral usage fill + cool-cyan elapsed fill + hairline divider (WEEKLY-METER-01, WEEKLY-METER-04)
- [ ] `PrettyConversationsPanel.tsx` renders `<WeeklyUsageMeter />` as sibling AFTER `.pv-panel-header-row` but INSIDE `.pv-panel-header`
- [ ] `WeeklyUsageMeter.test.tsx` covers render, elapsed% math (80% case), and fetch-failure resilience (WEEKLY-METER-05)
- [ ] Pre-existing `PrettyConversationsPanel.test.tsx` still passes (assertions updated only where restructure genuinely broke them, no weakening otherwise)
- [ ] No docker restart, nginx reload, git push, or deploy performed — caller drives those
</success_criteria>

<output>
Create `.planning/quick/260729-1vd-add-weekly-usage-meter-to-top-of-pretty-/260729-1vd-SUMMARY.md` when all four tasks complete, capturing:
- Actual backend file location where `/api/usage` was registered
- Whether the existing panel tests needed updates (and which selectors)
- Any deviation from the locked spec + reason (should be none)
- Confirmation both nginx configs got the location block
</output>
