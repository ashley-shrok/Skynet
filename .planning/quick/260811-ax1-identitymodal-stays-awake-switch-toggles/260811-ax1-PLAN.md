---
phase: quick-260811-ax1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/database/routes/identity-no-dormancy.ts
  - src/backend/database/routes/identity-no-dormancy.test.ts
  - src/backend/database/database.ts
  - src/ui/api/identities-api.ts
  - src/ui/features/pretty-view/IdentityModal.tsx
  - src/ui/features/pretty-view/IdentityModal.stays-awake.test.tsx
autonomous: true
requirements: [260811-ax1]

must_haves:
  truths:
    - "IdentityModal DialogHeader shows a labeled 'Stays awake' Radix Switch between the name/title flex and the pencil button"
    - "Opening the modal loads the current sentinel state and reflects it in the switch (checked = sentinel PRESENT)"
    - "Toggling the switch to ON writes ~/.claude/identities/<key>/.no-dormancy on the identity's host (local or SSH); toggling to OFF removes it"
    - "Toggling is idempotent — repeat writes/removes succeed without error; missing-target removal succeeds (ENOENT swallowed)"
    - "SSH branch cannot be shell-injected via identityKey (IDENTITY_KEY_RE gate at handler entry; direct interpolation inside double quotes is safe)"
    - "Failed writes revert the optimistic UI state and surface a user-visible error; loading/saving disables the switch"
    - "Existing DialogHeader elements (avatar, name+title, pencil, close) are untouched in layout and behavior; all existing IdentityModal tests still pass"
  artifacts:
    - path: "src/backend/database/routes/identity-no-dormancy.ts"
      provides: "GET/PUT /identities/:key/no-dormancy Express router"
      exports: ["default"]
    - path: "src/backend/database/routes/identity-no-dormancy.test.ts"
      provides: "vitest coverage for local + SSH branches, key validation, idempotency, error paths"
    - path: "src/ui/api/identities-api.ts"
      provides: "getIdentityNoDormancy + setIdentityNoDormancy client helpers appended to existing module"
    - path: "src/ui/features/pretty-view/IdentityModal.tsx"
      provides: "Stays awake Switch wired into DialogHeader with load/toggle handlers"
    - path: "src/ui/features/pretty-view/IdentityModal.stays-awake.test.tsx"
      provides: "vitest coverage for mount fetch, toggle round-trip, optimistic revert, disabled states"
  key_links:
    - from: "src/backend/database/database.ts"
      to: "src/backend/database/routes/identity-no-dormancy.ts"
      via: "app.use('/identities', identityNoDormancyRoutes) mount, positioned with the other identity-* specific routes BEFORE the generic /identities router"
      pattern: "identityNoDormancy"
    - from: "src/ui/features/pretty-view/IdentityModal.tsx DialogHeader"
      to: "src/ui/api/identities-api.ts"
      via: "getIdentityNoDormancy + setIdentityNoDormancy imports"
      pattern: "(getIdentityNoDormancy|setIdentityNoDormancy)"
    - from: "src/ui/api/identities-api.ts"
      to: "/identities/:key/no-dormancy"
      via: "authApi.get / authApi.put calls with hostId as query/body param"
      pattern: "no-dormancy"
---

<objective>
Add a "Stays awake" Radix Switch to the IdentityModal DialogHeader that toggles the per-identity `.no-dormancy` sentinel file on the identity's host. Present sentinel = agent stays always-on; absent = normal dormancy. Direct polarity: switch checked = sentinel present.

Purpose: Ashley needs to flip specific identities into always-on mode from the UI without SSHing to boxes. The id-skill's sentinel file already gates dormancy on the supervisor side; this exposes it as a modal-header control that is discoverable regardless of active tab.

Output:
- New HTTP route file `identity-no-dormancy.ts` (GET + PUT) mirroring the shell-safety and local/SSH-branch discipline of `identity-exists-on-host.ts`.
- Companion vitest file with 10+ tests covering both branches, idempotency, key validation, and error paths.
- Two new named exports on `identities-api.ts` (`getIdentityNoDormancy`, `setIdentityNoDormancy`).
- Switch + label + load/toggle handlers wired into IdentityModal DialogHeader between the name/title flex-1 div (currently ends L985) and the pencil-toggle button (currently starts L989).
- Companion IdentityModal test file (`.stays-awake.test.tsx`) mirroring the mocking discipline of `IdentityModal.voice.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@src/backend/claude-session/identity-artifact-reader.ts
@src/backend/database/routes/identity-exists-on-host.ts
@src/backend/database/routes/identity-exists-on-host.test.ts
@src/backend/database/routes/identity-birth.ts
@src/backend/database/database.ts
@src/ui/api/identities-api.ts
@src/ui/features/pretty-view/IdentityModal.tsx
@src/ui/components/switch.tsx
@src/ui/features/pretty-view/IdentityModal.voice.test.tsx

Interfaces you will consume verbatim (already exported from `identity-artifact-reader.ts`):
- `IDENTITY_KEY_RE = /^[a-z0-9_-]{1,64}$/` — apply at handler entry as the shell-safety gate.
- `isLocalHostId(hostId: number | undefined): boolean` — routing predicate (LOCAL vs SSH branch).
- `getLocalIdentitiesRoot(): string` — resolves the local bind-mount / `~/.claude/identities` root; use this instead of hardcoding `/home/ubuntu/...` so the LOCAL branch works with the IDENTITIES_HOST_DIR bind-mount (patch #89).

Interfaces you will consume from other backend modules:
- `resolveHostById(hostId, userId)` from `src/backend/ssh/host-resolver.js` — returns null on cross-user / unknown hostId → 404.
- `connectOneShot(host, timeoutMs)` from `src/backend/ssh/ssh-one-shot.js` — pattern shown in `identity-exists-on-host.ts` L125-128 (must `.end()` in finally).
- `execCommand(conn, cmd)` from `src/backend/ssh/tmux-helper.js` — no stdin support; command must be a plain string.
- `AuthManager.getInstance().createAuthMiddleware()` from `src/backend/utils/auth-manager.js` — standard JWT gate; 401 baseline.

Frontend interfaces:
- `authApi` (axios instance) via `@/api/http` (or whatever `handleApiError` is imported from — grep existing helpers in `identities-api.ts` for the exact import). Match `getIdentityExistsOnHost` (identities-api.ts L127-139) as the byte-shape template.
- `Switch` from `@/components/switch.tsx` — Radix wrapper; props: `checked`, `onCheckedChange(next: boolean)`, `disabled`, `aria-label`, `className`.

IMPORTANT — architectural context:
- The `identities` DB table has NO `hostId` column. Identities are user-scoped, not host-scoped. The current pane's hostId is passed EXPLICITLY by callers (WS handlers take `hostId` in each message; `identity-exists-on-host.ts` takes it as a query param). Follow the same pattern here — accept `hostId` as a query param on GET and as a body field on PUT. Do NOT try to resolve identity → host via the DB.
- The IdentityModal component already receives `hostId: number` as a prop (line 162) alongside `identity: Identity`. Both are available to pass into the new API calls with zero prop-drilling.
- Existing IdentityModal error UX uses inline `useState<string | null>` for error state (e.g. `saveError` at L197), NOT toast. Use `sonner`'s `toast.error(...)` as the brief specifies — sonner IS already used elsewhere in the UI (AppShell, LoginPage, Terminal). Import from `"sonner"` directly.
- The IdentityModal component receives `identity.displayName` (not `identity.name`); use `displayName` in aria-label.
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → backend (HTTP) | Untrusted `identityKey` path parameter + `hostId`/`present` query/body |
| backend → target host (SSH) | Backend interpolates identityKey into shell commands run on remote box |
| backend → local FS | Backend writes/reads a dotfile inside `~/.claude/identities/<key>/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ax1-01 | Tampering (shell injection) | GET/PUT handler → SSH `test -e`/`touch`/`rm -f` commands | mitigate | Gate `:key` with `IDENTITY_KEY_RE = /^[a-z0-9_-]{1,64}$/` BEFORE any SSH work; validate-then-interpolate inside double-quoted paths (matches `identity-exists-on-host.ts` L86-88 + L133 pattern). 400 on gate failure. |
| T-ax1-02 | Tampering (path traversal) | LOCAL branch fs.stat/writeFile/unlink | mitigate | Same regex gate rejects `..`, `/`, `\`, and every shell metachar. Build path with `path.join(getLocalIdentitiesRoot(), key, ".no-dormancy")` after gate. |
| T-ax1-03 | Elevation of Privilege (cross-user identity access) | GET/PUT handler | mitigate | `resolveHostById(hostId, userId)` returns null for cross-user / unknown hostId → 404. Matches `identity-exists-on-host.ts` L91-94. |
| T-ax1-04 | Information Disclosure (SSH error detail leak) | SSH branch failure path | mitigate | Wrap SSH catch in generic `502 { error: "Failed to reach host" }` (or 504 timeout-style; match `identity-exists-on-host.ts` L145-147 for parity). No error message pass-through. |
| T-ax1-05 | Denial of Service (unbounded SSH hang) | SSH branch exec | mitigate | Use `Promise.race` with a 3000ms timeout wrapping `execCommand`; matches `identity-exists-on-host.ts` L135-140 SSH_EXEC_TIMEOUT_MS pattern. |
| T-ax1-06 | Spoofing (unauthenticated request) | Both routes | mitigate | `authenticateJWT` middleware from `AuthManager.getInstance().createAuthMiddleware()` on both routes; 401 baseline. |
| T-ax1-07 | Tampering (non-boolean `present` body) | PUT body parse | mitigate | Explicit `typeof body.present !== "boolean"` check → 400. Also guard against missing body / wrong content-type. |
| T-ax1-08 | Race condition (concurrent toggle) | PUT idempotency | accept | UI serializes clicks via `saving` flag; backend PUT is idempotent (create-if-missing / rm -f). Concurrent toggles from two tabs converge on last-write-wins; acceptable for this UX. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend no-dormancy route + tests + mount + frontend API client</name>
  <files>src/backend/database/routes/identity-no-dormancy.ts, src/backend/database/routes/identity-no-dormancy.test.ts, src/backend/database/database.ts, src/ui/api/identities-api.ts</files>

  <behavior>
    Backend route tests (in identity-no-dormancy.test.ts — 10 tests, mirroring identity-exists-on-host.test.ts structure at src/backend/database/routes/identity-exists-on-host.test.ts):
    1. GET local branch — sentinel exists → responds 200 `{ present: true }`. Mock: `isLocalHostId → true`, `fs.stat → { isFile: () => true }`.
    2. GET local branch — sentinel missing (ENOENT) → 200 `{ present: false }`. Mock: `fs.stat → reject with { code: "ENOENT" }`.
    3. GET SSH branch — sentinel exists → 200 `{ present: true }`. Mock: `isLocalHostId → false`, `resolveHostById → host`, `connectOneShot → conn`, `execCommand → "Y\n"`.
    4. GET SSH branch — sentinel missing → 200 `{ present: false }`. Mock: `execCommand → "N\n"`.
    5. PUT local branch — `{ present: true }` → 200 `{ present: true }`; asserts `fs.writeFile` called with the correct path (`<getLocalIdentitiesRoot()>/<key>/.no-dormancy`) and empty string / empty buffer content.
    6. PUT local branch — `{ present: false }` → 200 `{ present: false }`; asserts `fs.unlink` called with correct path AND that an ENOENT rejection from unlink is swallowed (test with `fs.unlink → reject ENOENT` and still expect 200).
    7. PUT SSH branch — `{ present: true }` → asserts `execCommand` invoked with a command matching `/mkdir -p .*\.claude\/identities\/moxie && touch .*\.claude\/identities\/moxie\/\.no-dormancy/`; response 200 `{ present: true }`.
    8. PUT SSH branch — `{ present: false }` → asserts `execCommand` invoked with a command matching `/rm -f .*\.claude\/identities\/moxie\/\.no-dormancy/`; response 200 `{ present: false }`.
    9. Invalid identity key (traversal `..`, shell metachar `;`, empty string, length > 64) — 400 for both GET and PUT. NOTE: `identity-exists-on-host.ts` uses a broader regex; you MUST use `IDENTITY_KEY_RE = /^[a-z0-9_-]{1,64}$/` from `identity-artifact-reader.ts` because that is the regex that guards every existing shell-interpolated `.claude/identities/<key>` path in the codebase. Import (do not copy) it to keep the gate single-sourced.
    10. Unknown / cross-user hostId (resolveHostById → null) → 404 `{ error: "Host not found" }` for both GET and PUT.
    11. PUT missing body / `{ present: "true" }` (string, not boolean) / missing `present` key → 400 `{ error: <descriptive> }`.
    12. GET SSH branch — exec timeout / connect failure → 502 or 504 (choose one and stay consistent with `identity-exists-on-host.ts` which uses 504 "Host unreachable"; matching 504 is preferred for consistency with the sibling route).
    13. 401 without JWT — both routes.
    14. Local fs unexpected error (EACCES on GET) → 500 with generic message (no error passthrough).

    Frontend API client tests are NOT required for this task; the two new helpers are trivial fetch wrappers matching the existing `getIdentityExistsOnHost` pattern in the same module (which is untested). Task 2 will exercise them via the IdentityModal test file.
  </behavior>

  <action>
Create `src/backend/database/routes/identity-no-dormancy.ts` as a new Express router file that mirrors the structure of `src/backend/database/routes/identity-exists-on-host.ts` (that file is the canonical template — read it in full before writing).

Imports needed (all `.js` extensions per ESM convention in this backend):
- `express`, `type { Request, Response }` from `express`
- `type { AuthenticatedRequest }` from `../../../types/index.js`
- `fs from "fs/promises"` (use the namespace form so tests can vi.mock it — `fs.stat`, `fs.writeFile`, `fs.unlink`)
- `path from "path"`
- `AuthManager` from `../../utils/auth-manager.js`
- `resolveHostById` from `../../ssh/host-resolver.js`
- `connectOneShot` from `../../ssh/ssh-one-shot.js`
- `execCommand` from `../../ssh/tmux-helper.js`
- `isLocalHostId`, `IDENTITY_KEY_RE`, `getLocalIdentitiesRoot` from `../../claude-session/identity-artifact-reader.js`

Router structure:
- `const router = express.Router()`
- `const authenticateJWT = AuthManager.getInstance().createAuthMiddleware()`
- Reuse `SSH_EXEC_TIMEOUT_MS = 3000` (define locally, matches the sibling route's naming).
- Both routes mounted at `/:key/no-dormancy` (see mount point rationale below).

GET handler (`router.get("/:key/no-dormancy", authenticateJWT, async (req, res) => {...})`):
1. Extract + validate `hostId` from `req.query.hostId` — parseInt, positive-integer gate, 400 on failure (mirror `identity-exists-on-host.ts` L71-78).
2. Extract `req.params.key`; validate against `IDENTITY_KEY_RE`; 400 on failure with message `"identity key must match [a-z0-9_-]{1,64}"`.
3. `const host = await resolveHostById(hostId, userId)`; 404 `{ error: "Host not found" }` on null.
4. If `isLocalHostId(hostId)`: build `filePath = path.join(getLocalIdentitiesRoot(), key, ".no-dormancy")`; `try { await fs.stat(filePath); res.json({ present: true }) } catch (err) { if (err.code === "ENOENT") res.json({ present: false }) else res.status(500).json({ error: "Failed to check sentinel" }) }`.
5. Else (SSH branch):
   - Open `connectOneShot(host, SSH_EXEC_TIMEOUT_MS)`; `.end()` in `finally`.
   - Run `test -e "$HOME/.claude/identities/${key}/.no-dormancy" && echo Y || echo N` via `execCommand`, wrapped in `Promise.race` with 3000ms timeout (mirror sibling L130-141).
   - Parse `stdout.trim() === "Y"` → true, else false. `res.json({ present })`.
   - On any thrown error inside the try (connect failure / exec timeout / SSH exception): `res.status(504).json({ error: "Host unreachable" })`. Do NOT leak error detail.

PUT handler (`router.put("/:key/no-dormancy", express.json(), authenticateJWT, async (req, res) => {...})`):
1. Same hostId + key validation as GET.
2. Body validation: `const { present } = req.body ?? {}`; if `typeof present !== "boolean"` → 400 `{ error: "present must be a boolean" }`.
3. Same `resolveHostById` → 404 gate.
4. If `isLocalHostId(hostId)`:
   - `const filePath = path.join(getLocalIdentitiesRoot(), key, ".no-dormancy")`.
   - If `present === true`: `await fs.writeFile(filePath, "", { flag: "w" })` (creates or overwrites — idempotent).
   - If `present === false`: `try { await fs.unlink(filePath) } catch (err) { if (err.code !== "ENOENT") throw err }` (missing-ok).
   - On success: `res.json({ present })`. On unexpected fs error: 500 with generic message.
5. Else (SSH branch): same `connectOneShot`/`finally` shape as GET.
   - If `present === true`: `mkdir -p "$HOME/.claude/identities/${key}" && touch "$HOME/.claude/identities/${key}/.no-dormancy"` — mkdir -p is defensive idempotency; the identity dir should exist but corner case (manual deletion) is handled.
   - If `present === false`: `rm -f "$HOME/.claude/identities/${key}/.no-dormancy"` — `-f` = missing-ok.
   - Wrap in the same 3000ms Promise.race.
   - On success: `res.json({ present })`. On SSH failure: 504 generic message.
6. DO NOT re-stat after write. Trust the exit code. Extra round-trip adds a race window and a shell command budget.

Error middleware: append the same generic 500 fallback shape used in `identity-exists-on-host.ts` L162-173.

Route mount in `src/backend/database/database.ts`:
- Add `import identityNoDormancyRoutes from "./routes/identity-no-dormancy.js";` next to the existing identity-* imports (L17-23 area).
- Add `app.use("/identities", identityNoDormancyRoutes);` in the same block where `identityExistsOnHostRoutes` is mounted (L1816-1818 area). Match the surrounding "specific paths BEFORE /identities" comment discipline. The paths `/identities/:key/no-dormancy` will resolve here first because express routing is match-order-dependent AND the generic `/identities` router at L1837 has no `:key/no-dormancy` route.
- IMPORTANT: mount BEFORE `app.use("/identities", identitiesRoutes)` at L1837. The generic identitiesRoutes uses `router.get("/:id", ...)` — putting no-dormancy AFTER it would cause `:id` to swallow `<key>` and 404 on the `/no-dormancy` suffix.

Nginx caveat per CLAUDE.md: `GET/PUT /identities/:key/no-dormancy` falls under the existing `/identities` location block in both `docker/nginx.conf` AND `docker/nginx-https.conf` (grep to confirm — the generic `/identities` proxy_pass already routes to the backend, so no nginx changes are needed here; only add nginx location blocks if the new path segment is not covered by existing `/identities` prefix match. Verify with `grep -n "location.*identities" docker/nginx*.conf` — if the existing block is `location /identities` prefix match, done).

Test file `src/backend/database/routes/identity-no-dormancy.test.ts`:
Copy the mocking scaffold verbatim from `identity-exists-on-host.test.ts` L24-140 (auth-manager mock, ssh mocks, resolver mock, isLocalHostId mock, fs/promises mock, httpRequest helper). Adjust:
- Add mocks for `fs.writeFile` and `fs.unlink` alongside `fs.stat` in the `vi.mock("fs/promises", ...)` block.
- Add mock for `IDENTITY_KEY_RE` + `getLocalIdentitiesRoot` in the `identity-artifact-reader.js` mock (return `/tmp/test-identities-root` from `getLocalIdentitiesRoot` mock so paths are predictable in assertions).
- The httpRequest helper needs a variant that accepts a JSON body — add a body arg + `Content-Type: application/json` header for PUT tests; write body to the request with `req.write(JSON.stringify(body))` before `req.end()`.

For each of the 14 tests listed under `<behavior>`, spin up `express()`, `.use(express.json())`, `.use("/identities", identityNoDormancyRoutes)`, `http.createServer(app).listen(0)` and issue the request via `httpRequest`. Assert on `status` + `body` shape AND (for SSH-write tests) on `execCommand.mock.calls[0][1]` matching the expected regex.

Frontend API client — append to `src/ui/api/identities-api.ts` (after `getIdentityExistsOnHost` at L127-139, before the Phase 22 roles section at L141):

```
// ─── Quick 260811-ax1: per-identity .no-dormancy sentinel toggle ─────────────
// Backing routes: src/backend/database/routes/identity-no-dormancy.ts
// GET /identities/:key/no-dormancy?hostId=<n> → { present: boolean }
// PUT /identities/:key/no-dormancy body { present: boolean } + query hostId=<n> → { present: boolean }
// Consumed by IdentityModal DialogHeader "Stays awake" switch.

export async function getIdentityNoDormancy(
  identityKey: string,
  hostId: number,
): Promise<boolean> {
  try {
    const response = await authApi.get(
      `/identities/${encodeURIComponent(identityKey)}/no-dormancy`,
      { params: { hostId } },
    );
    return (response.data as { present: boolean }).present;
  } catch (error) {
    handleApiError(error, "read stays-awake sentinel");
  }
}

export async function setIdentityNoDormancy(
  identityKey: string,
  hostId: number,
  present: boolean,
): Promise<boolean> {
  try {
    const response = await authApi.put(
      `/identities/${encodeURIComponent(identityKey)}/no-dormancy`,
      { present },
      { params: { hostId } },
    );
    return (response.data as { present: boolean }).present;
  } catch (error) {
    handleApiError(error, "update stays-awake sentinel");
  }
}
```

Verify the actual `authApi`/`handleApiError` imports at the top of the file — if the existing helpers use different symbol names (e.g. `apiClient` / `throwApiError`), match them exactly. The `getIdentityExistsOnHost` function immediately above this insertion point is the byte-shape template.
  </action>

  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npx vitest run src/backend/database/routes/identity-no-dormancy.test.ts</automated>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npm run build:backend</automated>
    <automated>cd /home/ubuntu/skynet &amp;&amp; grep -c "identityNoDormancyRoutes" src/backend/database/database.ts | grep -v '^0$'</automated>
  </verify>

  <done>
    - `src/backend/database/routes/identity-no-dormancy.ts` exists and exports a default express Router with GET + PUT handlers for `/:key/no-dormancy`.
    - `identity-no-dormancy.test.ts` runs green with 14+ tests covering both branches, key validation, idempotency, body validation, error paths.
    - `database.ts` imports + mounts `identityNoDormancyRoutes` BEFORE the generic `identitiesRoutes`.
    - `npm run build:backend` succeeds with no TypeScript errors.
    - `identities-api.ts` exports `getIdentityNoDormancy` and `setIdentityNoDormancy` and passes `npm run build` (frontend tsc happy).
    - Nginx configs verified to already route `/identities/*` prefix to the backend (no changes needed) — OR if the check reveals a more-specific block that would miss `/no-dormancy`, add the matching location entries to both `nginx.conf` and `nginx-https.conf`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IdentityModal DialogHeader Switch + load/toggle wiring + tests</name>
  <files>src/ui/features/pretty-view/IdentityModal.tsx, src/ui/features/pretty-view/IdentityModal.stays-awake.test.tsx</files>

  <behavior>
    Frontend tests (in `IdentityModal.stays-awake.test.tsx` — 6 tests, mirroring `IdentityModal.voice.test.tsx` scaffold):
    1. Mount: `getIdentityNoDormancy` called once with `(identity.identityKey, hostId)` on modal open; resolves to `false` → renders switch with `aria-checked="false"` and NOT `disabled`.
    2. Mount: `getIdentityNoDormancy` resolves to `true` → switch renders `aria-checked="true"` and NOT `disabled`.
    3. Click unchecked switch → `setIdentityNoDormancy(key, hostId, true)` called; switch immediately flips to checked (optimistic); on resolve the state stays checked.
    4. Click checked switch → `setIdentityNoDormancy(key, hostId, false)` called; switch immediately flips to unchecked; on resolve stays unchecked.
    5. `setIdentityNoDormancy` rejects → switch reverts to previous state; `toast.error` from `sonner` called with a user-visible message like `"Failed to update stays-awake"`.
    6. While loading (initial fetch not yet resolved) → switch is `disabled` (aria-disabled or `disabled` attribute); after resolve → enabled. During in-flight PUT (`saving === true`) → disabled.

    Additional smoke: rendering the modal with the new switch does NOT break any of the existing assertions in `IdentityModal.test.tsx` / `IdentityModal.voice.test.tsx` / `IdentityModal.role-tab.test.tsx` (the full test file suite runs green).
  </behavior>

  <action>
Edit `src/ui/features/pretty-view/IdentityModal.tsx`:

1. Add imports (merge into existing import blocks at the top):
   - `Switch` from `@/components/switch` (extend the existing shadcn component imports around lines 8-16).
   - `toast` from `"sonner"` (add a new import line near the other module imports around lines 30-35).
   - `getIdentityNoDormancy`, `setIdentityNoDormancy` from `@/api/identities-api` (extend the existing `updateIdentity` import at L33 to a multi-import).

2. Add state (inside the component function, near the other `useState<TabState<...>>` slots around L227):
   - `const [staysAwake, setStaysAwake] = useState<boolean | null>(null);` — null = loading, boolean = loaded.
   - `const [staysAwakeSaving, setStaysAwakeSaving] = useState<boolean>(false);`

3. Add load effect. Find the existing `useEffect(...)` block that resets tab state on `open && identity.identityKey` change (around L244). Add a SEPARATE `useEffect` immediately after it that:
   - Guards `if (!open || !identity.identityKey) return;`
   - Resets `setStaysAwake(null); setStaysAwakeSaving(false);` at effect entry (matches the surrounding pattern of resetting to loading on modal open).
   - Wraps `getIdentityNoDormancy(identity.identityKey, hostId)` in an async IIFE with a cancellation flag (`let cancelled = false;` + `return () => { cancelled = true; }` in cleanup) to avoid setting state on unmount, mirroring standard React fetch-effect discipline. On resolve: `if (!cancelled) setStaysAwake(present)`. On reject: `if (!cancelled) { setStaysAwake(null); toast.error("Failed to read stays-awake state"); }`.
   - Dependency array: `[open, identity.identityKey, hostId]` (matches other artifact-load effects in this component).

4. Add toggle handler (component-scoped function, near other handlers):
   ```
   async function onStaysAwakeToggle(next: boolean) {
     const prev = staysAwake;
     setStaysAwake(next);       // optimistic
     setStaysAwakeSaving(true);
     try {
       const confirmed = await setIdentityNoDormancy(identity.identityKey, hostId, next);
       setStaysAwake(confirmed);
     } catch {
       setStaysAwake(prev);
       toast.error("Failed to update stays-awake");
     } finally {
       setStaysAwakeSaving(false);
     }
   }
   ```

5. Render the switch INSIDE the existing `<DialogHeader>` element (currently spans L955-L1053), positioned BETWEEN the name/title `<div className="flex flex-col min-w-0 flex-1">…</div>` (which currently ends at L985 with `</div>`) and the pencil `<button>` (which currently starts at L989). Insert this JSX block on a new line between L985 and L989:

   ```
   <label
     className="shrink-0 flex flex-row items-center gap-2 cursor-pointer select-none"
     title="Toggle stays-awake sentinel for this identity"
   >
     <Switch
       checked={staysAwake === true}
       onCheckedChange={onStaysAwakeToggle}
       disabled={staysAwake === null || staysAwakeSaving}
       aria-label={`Toggle stays-awake for ${identity.displayName}`}
     />
     <span className="text-xs text-[#a89a80]">Stays awake</span>
   </label>
   ```

   Rationale for each utility class:
   - `shrink-0` — protects the flex-1 name/title block from being squeezed by the label taking header width.
   - `select-none` — prevents text selection when tapping the label to activate the switch.
   - `cursor-pointer` — Tailwind v4 dropped button default; explicit for the label.
   - `text-xs text-[#a89a80]` — matches the title subtitle typography at L981 for visual consistency.
   - `gap-2` — matches the DialogHeader's own `gap-3` slightly tightened for label+switch pairing.

6. Do NOT modify: the avatar `<img>`, the name/title `<div>`, the pencil `<button>`, the close `<DialogClose>`. All existing children stay byte-identical.

Create `src/ui/features/pretty-view/IdentityModal.stays-awake.test.tsx`:

Copy the mocking scaffold from `IdentityModal.voice.test.tsx` verbatim (L1-95: WsStub, makeFakeWs, `@/api/claude-session-api` mock, `@/api/identities-api` mock, `@/state/identities-store` mock, `@/state/bounty-counts-store` mock). Adjust the `@/api/identities-api` mock (currently L56-63 in voice.test.tsx) to ALSO expose `getIdentityNoDormancy` and `setIdentityNoDormancy` as `vi.fn()`s:

```
vi.mock("@/api/identities-api", async (importOriginal) => {
  const orig = await importOriginal() as Record<string, unknown>;
  return {
    ...orig,
    updateIdentity: vi.fn(),
    listIdentities: vi.fn().mockResolvedValue([]),
    getIdentityNoDormancy: vi.fn(),
    setIdentityNoDormancy: vi.fn(),
  };
});
```

Add a `vi.mock("sonner", () => ({ toast: { error: vi.fn(), success: vi.fn() } }))` block so the toast import in the component resolves against a spy in tests.

In `beforeEach`, reset all mocks and default `getIdentityNoDormancy.mockResolvedValue(false)` and `setIdentityNoDormancy.mockImplementation((_, __, present) => Promise.resolve(present))`. Each test overrides as needed.

Render the modal (import `IdentityModal` from `"./IdentityModal"`, build a fixture `identity: Identity` matching the shape used in voice.test.tsx, pass required props including `open={true}`, `identity`, `hostId={1}`, and any other required callbacks — copy from voice.test.tsx's render helper).

For each of the 6 tests:
- Use `await waitFor(() => expect(getIdentityNoDormancy).toHaveBeenCalled())` to sync past the load effect.
- Query the switch by `screen.getByRole("switch", { name: /toggle stays-awake/i })` OR by `screen.getByLabelText(/Stays awake/i)`. Radix Switch renders as `role="switch"` with `aria-checked`.
- For click tests: `await fireEvent.click(switchEl)` (Radix Switch fires `onCheckedChange` on click).
- Assert on switch `aria-checked` attribute for state, and on `setIdentityNoDormancy` mock calls for round-trip.
- For the revert test: `setIdentityNoDormancy.mockRejectedValueOnce(new Error("boom"))`, click, then `await waitFor(() => expect(toast.error).toHaveBeenCalled())` and re-assert `aria-checked` back to prev.
- For the disabled test: initial render → assert `switch.hasAttribute("disabled")` OR `aria-disabled="true"`; after `waitFor` load resolves → re-assert not disabled.

Real timers only (no `vi.useFakeTimers()`) — mirrors `IdentityModal.role-tab.test.tsx` per the comment in `GlobalFilesModal.test.tsx:100`.
  </action>

  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npx vitest run src/ui/features/pretty-view/IdentityModal.stays-awake.test.tsx</automated>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npx vitest run src/ui/features/pretty-view/IdentityModal</automated>
    <automated>cd /home/ubuntu/skynet &amp;&amp; grep -c 'Stays awake' src/ui/features/pretty-view/IdentityModal.tsx | grep -v '^0$'</automated>
    <automated>cd /home/ubuntu/skynet &amp;&amp; grep -v '^//' src/ui/features/pretty-view/IdentityModal.tsx | grep -c 'onStaysAwakeToggle' | grep -v '^0$'</automated>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npm run build</automated>
  </verify>

  <done>
    - `IdentityModal.tsx` renders the new "Stays awake" Switch inside `<DialogHeader>` between the name/title flex and the pencil button.
    - Existing DialogHeader children (avatar, name+title div, pencil button, close button) are unchanged in props/structure/layout — a git diff on `IdentityModal.tsx` shows ONLY the additive Switch label block, plus the state/effect/handler additions and the two new imports.
    - Load effect fetches `getIdentityNoDormancy(identity.identityKey, hostId)` once per modal open (and once per `identity.identityKey` or `hostId` change).
    - Toggling the switch calls `setIdentityNoDormancy(...)` and applies an optimistic update with revert-on-error + `toast.error` from `sonner`.
    - `IdentityModal.stays-awake.test.tsx` runs green with all 6 test cases.
    - All pre-existing IdentityModal test files (`IdentityModal.test.tsx`, `IdentityModal.voice.test.tsx`, `IdentityModal.role-tab.test.tsx`) still run green.
    - `npm run build` succeeds (frontend tsc + vite build).
  </done>
</task>

<task type="auto">
  <name>Task 3: Full vitest suite + backend+frontend build gate</name>
  <files>(no files edited — verification-only task)</files>

  <action>
Run the full vitest suite and both build steps to confirm no regressions elsewhere in the codebase. Per box-maintainer fleet rule (2026-08-05, referenced in the task brief), a red suite blocks completion. This task exists as an explicit last-mile gate so the executor does not sign off after only running the two new test files.

Do NOT deploy. Do NOT push. Do NOT run `docker compose`. Executor remit stops at code + commit + green tests + green builds per the CRITICAL note in the task brief.

If ANY previously-green test in the full suite fails after these changes, root-cause and fix before signaling done. Common failure modes to check first:
- IdentityModal snapshot tests (if any) — the new Switch changes header layout; snapshots need updating with `-u` after visual verification, but only if the snapshot delta contains ONLY the additive Switch label.
- Route mount ordering — if `identity-no-dormancy` accidentally mounts AFTER `identitiesRoutes`, integration tests exercising `/identities/:id` (e.g. `GET /identities/<uuid>`) MAY 404 if the id shape overlaps with `<key>/no-dormancy` — extremely unlikely because identity IDs are nanoid and the path suffix disambiguates, but check the mount order matches Task 1.
- TypeScript strictness — the identities-api.ts `handleApiError` helper's return type may be `never` (throws) rather than `Promise<boolean>`; if tsc complains about missing return from `getIdentityNoDormancy`, verify the sibling `getIdentityExistsOnHost` compiles fine (it uses the same shape) — if it does, the new functions will too.
  </action>

  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npx vitest run</automated>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npm run build:backend</automated>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npm run build</automated>
  </verify>

  <done>
    - `npx vitest run` reports full green — no failing tests anywhere in the repo.
    - `npm run build:backend` succeeds.
    - `npm run build` succeeds.
    - No deploy step attempted (fleet rule).
    - No git push attempted (fleet rule).
    - Working tree contains only the additive changes described in Tasks 1 and 2.
  </done>
</task>

</tasks>

<verification>
Phase-wide checks after all tasks complete:

1. Route surface — both new endpoints respond correctly to a hand-crafted curl (backend running locally):
   `curl -s -X GET "http://localhost:PORT/identities/moxie/no-dormancy?hostId=1" -H "Authorization: Bearer <jwt>"` → `{"present":false}` or `{"present":true}`.
   `curl -s -X PUT "http://localhost:PORT/identities/moxie/no-dormancy?hostId=1" -H "Authorization: Bearer <jwt>" -H "Content-Type: application/json" -d '{"present":true}'` → `{"present":true}` AND `ls -la ~/.claude/identities/moxie/.no-dormancy` shows the file.
   Repeat with `{"present":false}` → `{"present":false}` AND the file is removed.
   (This is manual verification — Ashley's remit — not part of the automated `<verify>` gates.)

2. UI smoke — open a browser to the running Skynet, click any identity badge to open the IdentityModal, confirm:
   - The "Stays awake" label + switch is visible in the header row between the identity name and the pencil button.
   - The switch state matches the sentinel state on disk (loads within ~200ms on local host, ~1s on SSH host).
   - Toggling the switch flips it visually AND (verified via a terminal) creates/removes the `.no-dormancy` file on the identity's host.
   - Failed toggles (simulated by stopping the backend mid-click) revert the switch AND surface a sonner toast.
   (Manual — deferred to Ashley post-deploy.)

3. Test suite health: `npx vitest run` — full green.
4. Build health: `npm run build:backend && npm run build` — both green.

Threat mitigations verified by tests:
- T-ax1-01 (shell injection) — test 9 in Task 1 covers `..`, `;`, empty, over-length keys → 400.
- T-ax1-02 (path traversal) — same test covers `..` rejection at the regex gate.
- T-ax1-03 (cross-user) — test 10 covers `resolveHostById → null` → 404.
- T-ax1-04 (SSH detail leak) — test 12 asserts response body is `{ error: "Host unreachable" }`, not the raw exception message.
- T-ax1-05 (SSH hang) — implicit via the 3000ms Promise.race pattern; test 12 exercises the timeout path.
- T-ax1-06 (unauthenticated) — test 13 covers 401 without JWT.
- T-ax1-07 (non-boolean present) — test 11 covers missing / wrong-type body → 400.
</verification>

<success_criteria>
- IdentityModal DialogHeader displays a labeled "Stays awake" Radix Switch between the name/title block and the pencil button; existing header elements are byte-identical in layout and behavior.
- Switch state on mount reflects the actual presence of `~/.claude/identities/<key>/.no-dormancy` on the identity's host (via a `GET /identities/:key/no-dormancy?hostId=<n>` round-trip).
- Toggling the switch to ON creates the sentinel; toggling to OFF removes it. Both operations are idempotent on the backend (repeat toggles succeed without error).
- Both LOCAL branch (bind-mounted `IDENTITIES_HOST_DIR`) and SSH branch (arbitrary remote host reachable via existing SSH plumbing) work correctly and are covered by tests.
- Failed writes revert the optimistic UI + surface a sonner toast; loading/saving disables the switch.
- Full `npx vitest run` passes green.
- `npm run build:backend && npm run build` both succeed.
- No deploy attempted. No git push attempted. Executor remit stops at green code + green tests + green build.
</success_criteria>

<output>
Create `.planning/quick/260811-ax1-identitymodal-stays-awake-switch-toggles/260811-ax1-SUMMARY.md` when done.
</output>
