---
phase: quick-260807-e4s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
autonomous: true
requirements:
  - E4S-01

must_haves:
  truths:
    - "An active-set row that is pinned only under its fleet-synthetic id shape (fleet::HOSTID::SESSIONNAME) shows 'Unpin' in its right-click context menu, not 'Pin'."
    - "The two active-set-tier render sites (active-set map, grouped host map) both compute the pinned prop with fleet-shadow-id fallback."
    - "The two hardcoded-pinned sites (pinned tier `pinned={true}`, hidden/RDP sentinel `pinned={false}`) are unchanged."
    - "`npm run build:backend && npm run build` exits 0 (no typecheck regressions)."
    - "`npx vitest run` exits 0 with zero failing tests."
  artifacts:
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "isRowPinned helper + wired-in call at line 901 and line 1057"
      contains: "isRowPinned"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx"
      provides: "Regression test locking in fleet-shadow-id pinned recognition for active-set rows"
      contains: "quick-260807-e4s"
  key_links:
    - from: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (isRowPinned helper)"
      to: "src/ui/state/conversation-store.ts (fleetRowId export)"
      via: "already-present import at PrettyConversationsPanel.tsx:66"
      pattern: "fleetRowId\\(parseInt\\(row\\.host\\.id, 10\\), row\\.targetTmuxSession\\)"
    - from: "PrettyConversationsPanel.tsx active-set map (line ~901)"
      to: "PrettyConversationRow pinned prop → items[0].label ternary at PrettyConversationRow.tsx:587"
      via: "pinned={isRowPinned(row)}"
      pattern: "pinned=\\{isRowPinned\\(row\\)\\}"
    - from: "PrettyConversationsPanel.tsx grouped host bucket map (line ~1057)"
      to: "PrettyConversationRow pinned prop → items[0].label ternary at PrettyConversationRow.tsx:587"
      via: "pinned={isRowPinned(row)}"
      pattern: "pinned=\\{isRowPinned\\(row\\)\\}"
---

<objective>
Fix the id-shape-mismatch bug where active-set (and grouped-host) rows in the
conversation list render "Pin" instead of "Unpin" in their right-click context
menu when the pin was persisted under the fleet-synthetic id shape
(`fleet::HOSTID::SESSIONNAME`) rather than the openTab id shape.

Purpose: Follow-up #1 to patch #149 (already flagged in the panel-side comment
at PrettyConversationsPanel.tsx:657). Mirrors the store's Tier 2 pinned
computation (conversation-store.ts:493-499) at the two panel-side render sites
that today do a naive `pinnedIds.has(row.id)` check.

Output:
  - `PrettyConversationsPanel.tsx` gets a local `isRowPinned(row)` helper and
    two `pinned=` prop callsites re-wired through it (lines ~901 and ~1057).
  - One new regression test in `PrettyConversationsPanel.test.tsx` that fails
    on `main` and passes after the fix.
  - Backend + frontend typecheck clean; whole vitest suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
@src/ui/state/conversation-store.ts
@src/ui/features/pretty-conversations/PrettyConversationRow.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add isRowPinned helper and wire it at the two active-set-tier callsites</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx</files>
  <behavior>
    - `isRowPinned(row)` returns `true` when `pinnedIds` contains `row.id`.
    - `isRowPinned(row)` returns `true` when `row.host` and `row.targetTmuxSession` are both truthy AND `pinnedIds` contains `fleetRowId(parseInt(row.host.id, 10), row.targetTmuxSession)`.
    - `isRowPinned(row)` returns `false` when `pinnedIds` contains neither shape.
    - Callsite at the active-set map (currently `pinned={pinnedIds.has(row.id)}` on the line inside `displayedActiveSetRows.map((row) => (...))` — line ~901) is replaced with `pinned={isRowPinned(row)}`.
    - Callsite at the grouped host bucket map (currently `pinned={pinnedIds.has(row.id)}` on the line inside `group.rows.map((row) => (...))` — line ~1057) is replaced with `pinned={isRowPinned(row)}`.
    - The hardcoded `pinned={true}` on the pinned tier (line ~956) is unchanged.
    - The hardcoded `pinned={false}` on the RDP sentinel (line ~1010) and the hidden section (line ~1116) are unchanged.
  </behavior>
  <action>
    In `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`, add a
    plain function (not memoized — the panel already re-renders on `pinnedIds`
    change via `usePinnedIds` at line 239) declared inside the
    `PrettyConversationsPanel` component after the existing pinned-id / hidden-id
    hooks are read and after `handleRowClone` (the neighborhood around line
    ~716). Place it near the other row-oriented helpers so future edits keep
    them co-located. The helper closes over `pinnedIds` in scope.

    Signature and body (prose, not fenced code):
      - name: `isRowPinned`
      - parameter: `row: ConversationRowShape`
      - return type: `boolean`
      - body: compute `shadowFleetId` as `fleetRowId(parseInt(row.host.id, 10), row.targetTmuxSession)` iff both `row.host` and `row.targetTmuxSession` are truthy, else `null`. Return `pinnedIds.has(row.id) || (shadowFleetId !== null && pinnedIds.has(shadowFleetId))`. This is the exact shape used by the store at `conversation-store.ts:493-499`.

    Add a short in-source comment above the helper referencing this fix:
    `// quick-260807-e4s (patch #149 followup-1 pin-nuke): mirror the store's` /
    `// Tier 2 shadow-fleet-id pinned check (conversation-store.ts:493-499) so` /
    `// active-set + grouped rows render "Unpin" when the pin was persisted` /
    `// under the fleet-synthetic id shape.`

    `fleetRowId` is ALREADY imported from `@/state/conversation-store` at line 66
    of PrettyConversationsPanel.tsx — do NOT add a new import, do NOT touch any
    barrel/index file. Confirm the existing import line reads `fleetRowId,` in
    the destructured import list before writing the helper; if for any reason
    it is missing, add it there (still no barrel touch).

    Replace `pinned={pinnedIds.has(row.id)}` at the active-set map callsite
    (line ~901, inside `displayedActiveSetRows.map`) with
    `pinned={isRowPinned(row)}`.

    Replace `pinned={pinnedIds.has(row.id)}` at the grouped host bucket map
    callsite (line ~1057, inside `group.rows.map`) with
    `pinned={isRowPinned(row)}`.

    Do NOT touch line 956 (`pinned={true}` on the pinned tier). Do NOT touch
    line 1010 (`pinned={false}` on the RDP sentinel). Do NOT touch line 1116
    (`pinned={false}` on the hidden section). Do NOT touch the comment at line
    657 — the followup it references is exactly this patch and future readers
    should still see the historical note in place. Optionally add a short
    trailing sentence to that comment noting it was closed by quick-260807-e4s;
    if the diff churn is not worth it, leave the comment alone.

    Do NOT change anything outside PrettyConversationsPanel.tsx in this task.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tiffany &amp;&amp; grep -n "isRowPinned" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx &amp;&amp; grep -n "pinned={pinnedIds.has(row.id)}" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx | { grep -v '^#' | grep -c . || true; } | awk '{ if ($1 != 0) { print "FAIL: legacy pinnedIds.has(row.id) callsites still present"; exit 1 } else { print "OK: no legacy pinnedIds.has(row.id) callsites remain" } }' &amp;&amp; npm run build:backend &amp;&amp; npm run build</automated>
  </verify>
  <done>
    - `isRowPinned` is defined once inside PrettyConversationsPanel and used at both formerly-buggy `pinned=` callsites.
    - Zero `pinned={pinnedIds.has(row.id)}` occurrences remain in PrettyConversationsPanel.tsx.
    - `pinned={true}` (pinned tier) and `pinned={false}` (RDP sentinel + hidden section) are untouched.
    - `npm run build:backend` exits 0 and `npm run build` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression test locking in fleet-shadow-id pinned recognition for active-set rows</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx</files>
  <behavior>
    - New test in the existing file, inside a fresh `describe("PrettyConversationsPanel: active-set fleet-shadow-id pinned recognition (quick-260807-e4s)", ...)` block placed adjacent to (immediately after) the Test 20A-H `describe` at line 759.
    - Fixture: one host with numeric-string id `"1"` and `targetTmuxSession: "alpha"` (mirroring Test 20F/20H shape). One row in `activeSet` with an openTab-shape id (e.g. `"active-alpha"`) whose `host` and `targetTmuxSession` map to that fleet id. `pinned: []`. `grouped: []`.
    - `pinnedIds` fixture: a `Set(["fleet::1::alpha"])` — ONLY the fleet-shadow id, NOT the openTab id.
    - `mockActiveSet` fixture: contains the openTab id so the row lands in the active-set tier (matches Test 20A/20F pattern).
    - Assertion: right-clicking the row's `[role="button"]` opens the portal menu; `within(menu).getByRole("menuitem", { name: /^unpin$/i })` succeeds; `within(menu).queryByRole("menuitem", { name: /^pin$/i })` returns null.
    - Test fails on `main` (before the Task 1 fix) with the "Pin" label present and "Unpin" absent, and passes after the Task 1 fix.
  </behavior>
  <action>
    Add ONE new `it(...)` inside a new `describe(...)` block in
    `src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx`,
    placed immediately after the closing brace of the existing
    `describe("PrettyConversationsPanel: deactivate action (quick-260727-gm3)", ...)`
    block (that block ends around line 1100). Do NOT insert it inside that
    describe — this is a distinct concern (pinned label, not deactivate).

    Describe title:
      `PrettyConversationsPanel: active-set fleet-shadow-id pinned recognition (quick-260807-e4s)`

    Test title:
      `Test E4S-01: active-set row whose pin lives under fleet::HOSTID::SESSIONNAME shows Unpin (not Pin) in the right-click context menu`

    Fixture setup — reuse the existing `makeHost` + `makeConversationRow`
    helpers and the `setSnapshot` / `mockActiveSet` machinery already in
    scope. Do NOT introduce new helpers.

    Concrete fixture values:
      - `const hostA = makeHost("1", "hostA");`
      - `const activeRow = makeConversationRow({ id: "active-alpha", label: "alpha", host: hostA, targetTmuxSession: "alpha" });`
      - `setSnapshot({ activeSet: [activeRow], pinned: [], grouped: [], pinnedIds: new Set(["fleet::1::alpha"]) });`
      - `mockActiveSet = new Set<string>(["active-alpha"]);`

    Render call: `render(<PrettyConversationsPanel variant="desktop" onDeactivateRow={() => {}} />);`

    Then:
      - Query the row via `container.querySelector('[data-conversation-id="active-alpha"]')` and assert it exists.
      - Grab the row body via `rowEl!.querySelector('[role="button"]')`.
      - `fireEvent.contextMenu(body, { clientX: 100, clientY: 100 });`
      - `const menu = screen.getByRole("menu");`
      - `expect(within(menu).getByRole("menuitem", { name: /^unpin$/i })).toBeTruthy();`
      - `expect(within(menu).queryByRole("menuitem", { name: /^pin$/i })).toBeNull();`
    Use anchored regexes (`^unpin$` / `^pin$`) so the "Unpin" match cannot be
    satisfied by the presence of "Pin" as a substring — the label toggle at
    PrettyConversationRow.tsx:587 emits exactly one of "Pin" or "Unpin", so
    anchoring guarantees the test discriminates the two states.

    Add a short leading comment above the describe block referencing the fix:
    `// quick-260807-e4s: locks in the id-shape-mismatch fix. Panel-side` /
    `// isRowPinned(row) must mirror the store's Tier 2 shadow-fleet-id` /
    `// pinned computation (conversation-store.ts:493-499) at the two` /
    `// active-set-tier render sites (active-set map + grouped host map).`

    Do NOT add new mocks — the existing `vi.mock("@/state/conversation-store", ...)`
    block at line 183-223 already exposes `usePinnedIds` reading from
    `snapshot.pinnedIds`, and `fleetRowId` as `(hostId, sessionName) =>` /
    `\`fleet::${hostId}::${sessionName}\`` at lines 211-212. That mock format
    matches the fixture id `"fleet::1::alpha"` verbatim.

    Do NOT modify any existing test, mock, or beforeEach block.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tiffany &amp;&amp; npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx -t "Test E4S-01" &amp;&amp; npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx &amp;&amp; npx vitest run</automated>
  </verify>
  <done>
    - New `Test E4S-01` runs and passes.
    - The full `PrettyConversationsPanel.test.tsx` file passes (no collateral breakage in the sibling tests).
    - `npx vitest run` exits 0 across the whole suite. Zero failing tests. If any pre-existing test starts failing, STOP — the root cause must be diagnosed and fixed before commit (project rule: never leave tests failing).
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit the fix + test</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx, src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx</files>
  <action>
    Before committing, run one final full-suite guard:
      `cd /home/ubuntu/skynet-tiffany && npm run build:backend && npm run build && npx vitest run`
    All three must exit 0. If anything fails, STOP and diagnose — do not commit
    a red tree.

    Then create one commit using the standard project prefix:
      `fix(pretty-conversations): recognize fleet-shadow-id pins at active-set + grouped render sites (quick-260807-e4s)`

    Commit body (single paragraph, no bullet padding):
    `Panel-side follow-up to patch #149. PrettyConversationsPanel's two
    active-set-tier render sites did a naive pinnedIds.has(row.id) check, so
    a row whose pin was persisted under the fleet-synthetic id shape
    (fleet::HOSTID::SESSIONNAME) rendered "Pin" instead of "Unpin" in the
    right-click context menu. Add an isRowPinned(row) helper that mirrors the
    store's Tier 2 shadow-fleet-id fallback (conversation-store.ts:493-499)
    and wire it at PrettyConversationsPanel.tsx:901 (active-set map) and
    :1057 (grouped host bucket map). Hardcoded pinned={true} / pinned={false}
    sites left untouched. New regression Test E4S-01 locks in the behavior.`

    Stage only the two files listed in `<files>` (no `git add -A`, no
    `git add .`). Use absolute paths in the `git add` argv since the harness
    does not persist cwd between Bash calls.

    Do NOT `git push`. Do NOT `docker build`. Do NOT `docker compose up`. Do
    NOT create a git worktree. Stop after the commit lands locally — the
    orchestrator (tiffany) handles deploy authorization separately.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tiffany &amp;&amp; git log -1 --pretty=%s | grep -E "^fix\(pretty-conversations\): .*quick-260807-e4s" &amp;&amp; git diff HEAD~1 HEAD --name-only | sort -u | awk 'BEGIN{ok=1} { if ($1 != "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx" &amp;&amp; $1 != "src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx") { print "FAIL: unexpected file in commit: " $1; ok=0 } } END{ if (ok) print "OK: commit touches only the two expected files" }' &amp;&amp; test -z "$(git status --porcelain)" &amp;&amp; echo "OK: working tree clean"</automated>
  </verify>
  <done>
    - Exactly one new commit on the current branch (feat/tab-title-from-tmux).
    - Commit subject starts with `fix(pretty-conversations):` and contains `quick-260807-e4s`.
    - Commit touches only the two files listed in `<files>`.
    - `git status --porcelain` is empty (working tree clean, no stray files).
    - No push, no docker build, no docker compose up performed.
  </done>
</task>

</tasks>

<verification>
Final phase-level gates (must all pass at end of plan):
  1. `cd /home/ubuntu/skynet-tiffany && npm run build:backend` → exit 0
  2. `cd /home/ubuntu/skynet-tiffany && npm run build` → exit 0
  3. `cd /home/ubuntu/skynet-tiffany && npx vitest run` → exit 0, zero failures across the whole suite
  4. `git log -1 --pretty=%s` → matches `^fix\(pretty-conversations\): .*quick-260807-e4s`
  5. `git status --porcelain` → empty (working tree clean)
  6. In `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`: `isRowPinned` is defined once, used at exactly two `pinned=` callsites; ZERO `pinned={pinnedIds.has(row.id)}` occurrences remain; `pinned={true}` and `pinned={false}` sites unchanged.
</verification>

<success_criteria>
  - Regression Test E4S-01 fails on `main` without the Task 1 change and passes with it.
  - The full vitest suite exits 0 (no collateral regressions, no pre-existing failures left unattended — project rule: never leave tests failing).
  - `npm run build:backend` and `npm run build` both exit 0 (backend + frontend typecheck clean).
  - Exactly ONE commit is created, touching only the two files in scope, using the standard `fix(pretty-conversations): ...` prefix and referencing quick-260807-e4s.
  - No file outside `src/ui/features/pretty-conversations/` is modified.
  - No push, no docker build, no docker compose up. No git worktree.
</success_criteria>

<output>
Create `.planning/quick/260807-e4s-fix-active-set-context-menu-shows-pin-in/260807-e4s-SUMMARY.md` when done.
</output>
