---
phase: quick-260812-ma8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/DormancyOverlay.tsx
  - src/ui/features/pretty-view/PrettyView.tsx
autonomous: true
requirements:
  - Q-MA8-01  # Convert DormancyOverlay from full-surface scrim to in-flow assistant-aligned bubble
  - Q-MA8-02  # Move mount from chat-region wrapper to inside scroll container as PlanPendingBubble sibling
  - Q-MA8-03  # Preserve all three states verbatim: asleep+Wake, waking+progress, warm-red error retry
  - Q-MA8-04  # Preserve STATIC Moon glyph motion-channel guardrail (NO animate-spin)

must_haves:
  truths:
    - "While a session is dormant, Ashley can read the tail of the message list (no full-surface blur/scrim covers the chat region)"
    - "The dormant bubble appears at the visual bottom of the message list, styled as an assistant-aligned bubble mirroring PlanPendingBubble"
    - "All three DormancyOverlay states still render correctly: asleep (Moon + copy + Wake button), waking (Moon + copy + progress bar filling to 95% over 90s), error (warm-red Moon + error copy + Wake retry)"
    - "The Moon glyph is STATIC in all three states — no animate-spin anywhere in the file"
    - "ComposeBox still reduces to the dormant treatment while renderedState === 'dormant' || waking (dormantActive prop unchanged)"
    - "Existing DormancyOverlay.test.tsx tests still pass (behavioral tests — no scrim-class assertions to update)"
  artifacts:
    - path: "src/ui/features/pretty-view/DormancyOverlay.tsx"
      provides: "Restyled in-flow assistant-aligned dormancy bubble (identity-hue gradient, no scrim)"
      contains: "PlanPendingBubble-style flex justify-start outer + max-w-[min(720px,80vw)] card"
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "DormancyOverlay mount moved from chat-region wrapper into scroll container as PlanPendingBubble sibling"
  key_links:
    - from: "src/ui/features/pretty-view/PrettyView.tsx (scroll container, after PlanPendingBubble mount ~line 2238)"
      to: "src/ui/features/pretty-view/DormancyOverlay.tsx"
      via: "{renderedState === 'dormant' && <DormancyOverlay ... />}"
      pattern: "renderedState === \"dormant\""
    - from: "src/ui/features/pretty-view/PrettyView.tsx (ComposeBox mount line 2388)"
      to: "src/ui/features/pretty-view/ComposeBox.tsx"
      via: "dormantActive={renderedState === 'dormant' || waking}"
      pattern: "dormantActive"
---

<objective>
Convert `DormancyOverlay` from a full-surface scrim (backdrop-blur + bg-black/40 covering the chat region) into an in-flow assistant-aligned bubble at the bottom of the message list, mirroring `PlanPendingBubble`. This lets Ashley read the tail of the conversation while a session is asleep, so she can decide whether to wake it. All three visual states (asleep + Wake button, waking + progress bar, warm-red error retry) are preserved verbatim; only the outer container geometry and treatment change.

Purpose: The current scrim blurs and blocks the whole chat region — Ashley cannot see the last few messages while a session is dormant, which is exactly the context she needs to decide whether to wake it. A `PlanPendingBubble`-style in-flow bubble scrolls with the conversation and leaves prior messages readable.

Output: Restyled `DormancyOverlay.tsx` (bubble geometry + identity-hue gradient, docblock updated) + `PrettyView.tsx` mount moved from the chat-region wrapper (sibling of `SessionHoldingOverlay`) into the scroll container (sibling of `PlanPendingBubble`) + patch entry appended to `~/.claude/roles/box-maintainer/skynet-patches.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/ui/features/pretty-view/DormancyOverlay.tsx
@src/ui/features/pretty-view/PlanPendingBubble.tsx
@src/ui/features/pretty-view/PrettyView.tsx
@src/ui/features/pretty-view/DormancyOverlay.test.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Restyle DormancyOverlay from scrim to in-flow assistant-aligned bubble</name>
  <files>src/ui/features/pretty-view/DormancyOverlay.tsx</files>
  <behavior>
    - Component's props contract (waking, elapsedSeconds, onWake, error?) UNCHANGED — no signature edits.
    - All three states render verbatim internal content:
      (a) asleep (waking=false, no error): Moon glyph + "This session is asleep" + Wake button.
      (b) waking (waking=true): Moon glyph + "Waking up…" + progress bar linearly filling to WAKE_ETA_PROGRESS_CAP (0.95) over WAKE_ETA_SECONDS (90). No Wake button.
      (c) error variant (showError = !waking && error != null): warm-red Moon + `Couldn't wake — {error}` + Wake button (retry).
    - Moon glyph is STATIC in every state — grep for `animate-spin` in the file MUST return zero non-comment hits.
    - Existing DormancyOverlay.test.tsx behavioral assertions (rendered copy, button presence, progressbar aria-valuenow, error copy) still pass unchanged.
    - `role="status"` and dynamic `aria-label` (asleep / waking / error) preserved on the outer status element.
  </behavior>
  <action>
    Rewrite the outer container of `DormancyOverlay` to mirror `PlanPendingBubble`'s bubble shell per Q-MA8-01, Q-MA8-03, Q-MA8-04:

    1. DROP from the outer wrapper: `absolute inset-0`, `z-[99]`, `flex items-center justify-center`, `backdrop-blur-md bg-black/40`, `[-webkit-backdrop-filter:blur(12px)]`, `isolate [transform:translateZ(0)]`, `pointer-events-auto`. Keep `animate-in fade-in duration-150`.

    2. New outer wrapper: `<div className={cn("flex", "justify-start")}>` (verbatim from PlanPendingBubble line 144). This wrapper carries no `role`/`aria-label` — those move to the inner card.

    3. Inner card gets the PlanPendingBubble treatment for the NON-ERROR path (asleep + waking): identity-hue gradient + warm-cream text + identity-hue border + identity-hue-tinted shadow. Copy these classes VERBATIM from `PlanPendingBubble.tsx` lines 164-172:
       - `leading-relaxed`
       - `rounded-[var(--radius-pv-bubble)] px-3 py-2`
       - `backdrop-blur-xl saturate-150`
       - `[-webkit-backdrop-filter:blur(20px)_saturate(1.6)]`
       - `bg-[linear-gradient(160deg,hsla(var(--pv-id-hue),50%,38%,0.55),hsla(var(--pv-id-hue),45%,24%,0.6))]`
       - `text-[#fbf5e8]`
       - `border border-[hsla(var(--pv-id-hue),65%,55%,0.32)]`
       - `shadow-[0_8px_24px_rgba(0,0,0,0.5),_0_1px_0_rgba(255,220,170,0.18)_inset,_0_0_0_0.5px_hsla(var(--pv-id-hue),70%,55%,0.2),_0_0_32px_hsla(var(--pv-id-hue),70%,52%,0.18)]`
       - `flex flex-col gap-2 text-sm max-w-[min(720px,80vw)]` (bubble structure — replaces the old `flex flex-col items-center gap-3 text-sm` centered-card layout since the bubble is now left-aligned in a scroll flow, not centered in a scrim).

    4. ERROR variant of the inner card: keep the existing warm-red treatment verbatim (patch #122 / SessionHoldingOverlay style), but apply it to the SAME new bubble geometry (px-3 py-2, max-w-[min(720px,80vw)], `flex flex-col gap-2 text-sm`, `leading-relaxed`, `rounded-[var(--radius-pv-bubble)]`, `backdrop-blur-xl saturate-150`, `[-webkit-backdrop-filter:blur(20px)_saturate(1.6)]`):
       - Background: `bg-[linear-gradient(160deg,rgba(85,30,35,0.55),rgba(55,20,25,0.6))]`
       - Text: `text-[#f5d0d4]`
       - Border: `border border-white/[0.08]` (unchanged from current — the warm-red variant does not use identity-hue border)
       - Shadow: `shadow-[0_8px_24px_rgba(0,0,0,0.5),_0_1px_0_rgba(255,200,200,0.14)_inset,_0_0_18px_hsla(0,72%,55%,0.18)]` (unchanged from current)

       Use a `showError ? "..." : "..."` ternary inside the `cn(...)` array to swap ONLY the background, text-color, border, and shadow between error and non-error branches; the structural classes (rounding, padding, backdrop-blur, flex layout, max-width, leading) are shared verbatim across both branches.

    5. Move `role="status"` and the existing dynamic `aria-label` expression to the inner CARD (not the new outer flex wrapper) so the semantics-carrying element remains the visible bubble, exactly as `PlanPendingBubble` does at line 146.

    6. Inner content (glyph row, progress bar, Wake button) — LEAVE VERBATIM. The Moon icon (`h-4 w-4 shrink-0` + `showError && "text-[hsl(0,72%,60%)]"` + `aria-hidden="true"`) with the STATIC guardrail (NO `animate-spin`), the progress bar block with all aria attrs and width math, and the Wake `<Button size="sm" variant="secondary" ...>` all stay exactly as they are. NOTE: the current card uses `items-center gap-3` on the inner card; the new bubble uses `gap-2` (no `items-center` — matches PlanPendingBubble's left-aligned column). The GLYPH ROW inside keeps `flex items-center gap-3` (or match PlanPendingBubble's `flex items-center gap-2` — pick `gap-3` to preserve the current glyph-to-copy spacing; PlanPendingBubble's `gap-2` is fine either way). Preserve `pt-1` on the Wake button row if the existing spacing looks tight — not strictly required, defer to visual match with PlanPendingBubble's footer row (which uses `pt-1`).

    7. Rewrite the file-header docblock (lines 1-40) to reflect the new pattern. Preserve intent but update the "Shape" section to describe an in-flow assistant-aligned bubble (not a scrim). Keep the motion-channel GUARDRAIL section verbatim (Moon stays static; reference to WipBubble / SessionHoldingOverlay motion-channel rule intact). Update the GATING section: still `renderedState === "dormant"` gate from PrettyView, but the mount site is now inside the scroll container as a sibling of PlanPendingBubble (not the chat-region wrapper as a sibling of SessionHoldingOverlay). Add a one-line note that this is quick 260812-ma8 — bubble-in-flow refactor from the original quick 260808-cd6 scrim.

    8. Do NOT touch `WAKE_ETA_SECONDS`, `WAKE_ETA_PROGRESS_CAP`, the `DormancyOverlayProps` interface, the `showError` derivation, or any inner render logic. This is a container-geometry + treatment restyle only.

    Rationale: PlanPendingBubble sets the precedent for "identity is waiting on you" bubbles in the pretty-view content column — dormancy is exactly that shape (identity is asleep, waiting on Ashley to wake it), so the same treatment applies. Dropping the scrim + backdrop-filter from the outer wrapper lets Ashley read the tail of the message list; the inner card keeps `backdrop-blur-xl` which is fine on iOS PWA per the PlanPendingBubble precedent.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tiffany && npm run tsc 2>&1 | tail -20 &amp;&amp; npm test -- src/ui/features/pretty-view/DormancyOverlay.test.tsx 2>&amp;1 | tail -30 &amp;&amp; grep -v '^//' src/ui/features/pretty-view/DormancyOverlay.tsx | grep -v '^\s*\*' | grep -c 'animate-spin' | awk '{ if ($1 != "0") { print "FAIL: animate-spin present in non-comment code"; exit 1 } else { print "OK: no animate-spin in non-comment code" } }'</automated>
  </verify>
  <done>tsc passes with zero errors, DormancyOverlay.test.tsx tests all pass unchanged, and the animate-spin grep gate returns 0 (motion-channel guardrail preserved). DormancyOverlay.tsx outer wrapper uses `flex justify-start` (no `absolute inset-0`, no `backdrop-blur-md bg-black/40`, no `z-[99]`, no `pointer-events-auto`, no `isolate [transform:translateZ(0)]`, no outer `[-webkit-backdrop-filter:blur(12px)]`). Inner card carries `role="status"`, dynamic aria-label, PlanPendingBubble-style identity-hue treatment for non-error branch, warm-red treatment for error branch, and all three states (asleep+Wake, waking+progress bar, error+Wake retry) still render their existing content verbatim.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Move DormancyOverlay mount from chat-region wrapper into scroll container</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx</files>
  <behavior>
    - Old mount site (lines 2062-2073, sibling of `SessionHoldingOverlay` inside the chat-region wrapper) is REMOVED entirely.
    - New mount site: inside the outer scroll container (`composeScrollRefs`-bound div at line 2114), after the `{planPending && <PlanPendingBubble ... />}` block (which ends at line 2238) and BEFORE the `{asideText !== null && <AsideBubble ... />}` block at line 2249.
    - Mount gate unchanged: `{renderedState === "dormant" && <DormancyOverlay waking={waking} elapsedSeconds={elapsedSeconds} onWake={handleWake} error={wakeError} />}` — all four props preserved verbatim.
    - SessionHoldingOverlay mount at line 2061 UNTOUCHED.
    - ComposeBox mount + all its props UNTOUCHED — `dormantActive={renderedState === "dormant" || waking}` at line 2388 already handles compose-reduction and requires no changes.
    - PrettyView.test.tsx behavioral tests still pass (no scrim assertions to update — the tests exercise state-transition behavior, not mount-site geometry).
  </behavior>
  <action>
    Move the DormancyOverlay mount per Q-MA8-02:

    1. DELETE lines 2062-2073 (the two-line block comment describing the DormancyOverlay chat-region-wrapper mount + the `{renderedState === "dormant" && <DormancyOverlay ... />}` JSX). The `{renderedState === "holding" && <SessionHoldingOverlay />}` at line 2061 stays; the `{showResolvingSpinner && <PrettyViewLoadingOverlay />}` at line 2084 becomes the next sibling after that.

    2. INSERT the new mount inside the scroll container between the `{planPending && <PlanPendingBubble ... />}` block (which ends around line 2238) and the `{asideText !== null && <AsideBubble ... />}` block at line 2249. Place it as a sibling of PlanPendingBubble/AsideBubble — same indentation, same in-flow shape (no wrapper div, just the conditional JSX).

    3. Include a fresh block comment above the new mount site explaining:
       - Quick 260812-ma8 moved the DormancyOverlay mount from the chat-region wrapper (sibling of SessionHoldingOverlay) into the scroll container (sibling of PlanPendingBubble) so the bubble scrolls in the message-list flow instead of covering the chat region with a scrim.
       - Gate unchanged (`renderedState === "dormant"` — backend-authoritative via paneState).
       - Rationale: Ashley needs to read the tail of the conversation while a session is dormant to decide whether to wake it; the old scrim blurred everything. Style now mirrors PlanPendingBubble ("identity is waiting on you") since dormancy has the same semantic ("session asleep, waiting on you to wake").
       - Cross-reference: DormancyOverlay.tsx file header docblock (updated in Task 1) covers the bubble shape + motion-channel guardrail.

    4. Keep the JSX form exactly:
       ```
       {renderedState === "dormant" && (
         <DormancyOverlay
           waking={waking}
           elapsedSeconds={elapsedSeconds}
           onWake={handleWake}
           error={wakeError}
         />
       )}
       ```

    5. VERIFY the ComposeBox mount at line ~2350 and its `dormantActive={renderedState === "dormant" || waking}` prop at line ~2388 are UNCHANGED — that gate already correctly reduces the composer in the dormant window and needs no edits per the CONTEXT.

    6. Also update the neighboring block comment at old lines ~2035-2055 (the SessionHoldingOverlay/DormancyOverlay wrapper description) to REMOVE the DormancyOverlay half of the story — after this refactor, the chat-region wrapper hosts SessionHoldingOverlay only. Leave the SessionHoldingOverlay portion of the comment intact.

    Rationale: Placing DormancyOverlay as a sibling of PlanPendingBubble inside the scroll container is the cleanest way to make it scroll in-flow with the message list. useAutoScroll's ResizeObserver watches this container's content, so pin-to-bottom continues to work naturally when the bubble mounts/unmounts. The scroll container mount is only active when the outer render gate at line 2112 is true (`status === "streaming" || ((status === "connecting" || status === "error") && messages.length > 0)`); dormant sessions reach this branch because status stays `streaming` while the pane is asleep (only the pane sentinel flips, not the WS status). If dormancy ever needs to render outside that scroll-container gate (e.g. dormant + zero messages), we cross that bridge later — current UX has dormant sessions always carrying a message history.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tiffany && npm run tsc 2>&1 | tail -20 &amp;&amp; npm test -- src/ui/features/pretty-view/ 2>&amp;1 | tail -40 &amp;&amp; grep -c 'renderedState === "dormant"' src/ui/features/pretty-view/PrettyView.tsx | awk '{ if ($1 != "2") { print "FAIL: expected exactly 2 dormant renderedState checks (mount + composebox), got " $1; exit 1 } else { print "OK: dormant gate count preserved (2)" } }'</automated>
  </verify>
  <done>tsc passes, all pretty-view tests still pass, the `renderedState === "dormant"` grep count is exactly 2 (one for the DormancyOverlay mount now inside the scroll container, one for the ComposeBox `dormantActive` gate), the old lines 2062-2073 mount site is gone, and the new mount site sits between the PlanPendingBubble block and the AsideBubble block inside the scroll container. Local smoke test (npm run tsc + npm test) is green; Ashley will verify visually after deploy.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Append patch entry to ~/.claude/roles/box-maintainer/skynet-patches.md</name>
  <files>~/.claude/roles/box-maintainer/skynet-patches.md</files>
  <behavior>
    - New patch entry appended at the bottom of the patches file with the NEXT sequential number (increment from the highest existing `## Patch #N` header — currently #421, so this will be #422).
    - Entry includes: one-line title, files touched, one-paragraph rationale.
    - File is edited via the Edit tool (not committed to the skynet-tiffany repo — the patches file lives OUTSIDE the repo at `~/.claude/roles/box-maintainer/skynet-patches.md`).
  </behavior>
  <action>
    Determine the next patch number: run `grep '^## Patch #' ~/.claude/roles/box-maintainer/skynet-patches.md | tail -1` to confirm the highest existing patch number, then use the next integer (expected: #422 — but ALWAYS re-check at execution time since another agent may have shipped intervening patches between planning and execution).

    Append (do NOT overwrite — use the Edit tool with an append-style edit or Read the file's tail first) a new patch entry at the bottom of the file, following the exact format Ashley uses for other entries (see patch #421 as the most recent template — includes ship date, bounties/quick reference, image/container placeholders, coord placeholder). For quick tasks the format is looser than for phase-shipped patches; a minimal-but-complete entry is:

    ```
    ## Patch #NNN — Quick 260812-ma8: DormancyOverlay as in-flow bubble (was full-surface scrim)

    **Ship date**: 2026-08-12 (tiffany)
    **Quick**: `260812-ma8-dormancy-bubble-in-flow`
    **Files**:
    - `src/ui/features/pretty-view/DormancyOverlay.tsx` (outer wrapper + docblock restyled to PlanPendingBubble-shaped bubble; three states + STATIC Moon guardrail preserved verbatim)
    - `src/ui/features/pretty-view/PrettyView.tsx` (mount moved from chat-region wrapper sibling of SessionHoldingOverlay into scroll container as sibling of PlanPendingBubble; block comments updated on both sites)

    Converted `DormancyOverlay` from a full-surface scrim (`absolute inset-0` + `backdrop-blur-md bg-black/40` + `pointer-events-auto`) into an assistant-aligned in-flow bubble at the bottom of the message list, mirroring `PlanPendingBubble`'s treatment (identity-hue gradient, warm-cream text, `flex justify-start` outer, `max-w-[min(720px,80vw)]` card, static Moon glyph). While a session is asleep, Ashley can now read the tail of the conversation to decide whether to wake it — the old scrim blurred and blocked the whole chat region. All three states (asleep + Wake button, waking + 90s progress bar, warm-red error retry) preserved verbatim. Motion-channel guardrail intact (Moon stays STATIC, no `animate-spin`). ComposeBox already reduces correctly via `dormantActive={renderedState === "dormant" || waking}` — no compose changes needed.
    ```

    Fill in the actual patch number at write time. Do NOT include ship-time-only fields (image sha, container id, WS coord round-trip) if this quick task ships JS-only via `docker cp` — leave a shorter form; if a full docker rebuild happens, add those fields per the standard template. Follow whatever detail level matches the deploy path chosen at execute time.

    CRITICAL: This file is OUTSIDE the skynet-tiffany repo (`~/.claude/roles/box-maintainer/skynet-patches.md`). It MUST NOT be added to any git commit inside the repo. Edit via the Edit tool directly; the repo commit for this quick task should only touch the two `src/ui/features/pretty-view/*.tsx` files.
  </action>
  <verify>
    <automated>grep -c "^## Patch #" ~/.claude/roles/box-maintainer/skynet-patches.md | awk '{ if ($1 &lt; 230) { print "FAIL: expected at least 230 patch entries (was 229 pre-patch), got " $1; exit 1 } else { print "OK: patch count incremented to " $1 } }' &amp;&amp; grep -q "260812-ma8" ~/.claude/roles/box-maintainer/skynet-patches.md &amp;&amp; echo "OK: quick slug 260812-ma8 present in patches file" &amp;&amp; ! git -C /home/ubuntu/skynet-tiffany status --porcelain | grep -q "skynet-patches.md" &amp;&amp; echo "OK: patches file is not staged in skynet-tiffany repo"</automated>
  </verify>
  <done>Patches file has one new entry with the next sequential patch number, containing the quick slug `260812-ma8` and referencing both touched files. Patches file is NOT part of any git status in the skynet-tiffany repo (it lives outside).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure UI-only restyle — no new inputs, network calls, or trust boundaries introduced. Existing WS `dormant` frame handling is untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ma8-01 | Denial of Service (UX-level) | DormancyOverlay bubble scroll behavior | mitigate | Bubble is in-flow inside the existing scroll container that already hosts PlanPendingBubble/AsideBubble; useAutoScroll's ResizeObserver already watches this container so pin-to-bottom continues to work when the bubble mounts/unmounts. No new observer needed. |
| T-ma8-02 | Information Disclosure | Dormancy state visibility | accept | Prior behavior fully blurred the message tail; new behavior reveals it. That is the DESIRED semantic per Ashley's request — the whole point is to let her read the last messages before deciding to wake. Not a leak (she owns both the session and the pane). |
| T-ma8-03 | Tampering | npm/pip/cargo installs | N/A | No new packages installed — pure edit of two existing tsx files + external patches log. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
Post-execution checks (Ashley will drive most manually via iOS PWA after deploy):

1. `npm run tsc` — zero TypeScript errors.
2. `npm test -- src/ui/features/pretty-view/` — all pretty-view tests pass unchanged (DormancyOverlay.test.tsx, PrettyView.test.tsx, plus siblings).
3. Grep gate: `grep -v '^//' src/ui/features/pretty-view/DormancyOverlay.tsx | grep -v '^\s*\*' | grep -c 'animate-spin'` MUST return 0 — motion-channel guardrail preserved (STATIC Moon).
4. Grep gate: `grep -c 'renderedState === "dormant"' src/ui/features/pretty-view/PrettyView.tsx` MUST return exactly 2 (one for the DormancyOverlay mount, one for the ComposeBox `dormantActive` OR-expression).
5. Deploy loop: `npm run tsc` + `npm test` clean → either `docker cp` the two changed JS bundles into the running container OR full `docker build` + `docker run` recycle (JS-only for a UI restyle is fine). Deploy path is `/opt/skynet` on this EC2.
6. HTTPS smoke test after deploy: `curl -sS -o /dev/null -w "%{http_code} %{time_total}s\n" https://term.gigaashley.click` returns 200 in <1s.
7. Ashley's visual sign-off on iOS PWA:
   - Dormant session shows an assistant-aligned bubble at the bottom of the message list with Wake button, and prior messages are READABLE above it (no blur, no scrim).
   - Tapping Wake transitions to the waking state (progress bar fills, Moon stays static).
   - If a wake fails, the warm-red error bubble appears with Wake retry.
   - ComposeBox still reduces to its dormant treatment.
   - No iOS backdrop-filter compositor glitches on the inner card's `backdrop-blur-xl` (PlanPendingBubble precedent — safe).
</verification>

<success_criteria>
- DormancyOverlay renders as an assistant-aligned in-flow bubble at the bottom of the message list (not a full-surface scrim).
- All three states (asleep, waking, error) render verbatim internal content; Moon is STATIC in every state.
- Message list tail is READABLE while a session is dormant.
- ComposeBox reduction (`dormantActive`) still fires correctly with zero changes to PrettyView's ComposeBox mount.
- All existing tests pass; tsc clean.
- Patch entry appended to `~/.claude/roles/box-maintainer/skynet-patches.md` (external, NOT committed to the repo).
- Ashley confirms visual behavior on iOS PWA post-deploy.
</success_criteria>

<output>
Create `.planning/quick/260812-ma8-dormancy-bubble-in-flow/260812-ma8-SUMMARY.md` when done, following the standard quick-task SUMMARY template (files touched, verify commands run + exit codes, patch # used, deploy notes if any, Ashley sign-off status).
</output>
