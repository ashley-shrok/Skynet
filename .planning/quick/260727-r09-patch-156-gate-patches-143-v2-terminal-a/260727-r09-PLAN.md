---
phase: quick-260727-r09
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/lib/is-ios-pwa.ts
  - src/ui/lib/is-ios-pwa.test.ts
  - src/ui/features/terminal/Terminal.tsx
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-view/PrettyView.test.tsx
autonomous: true
requirements: [PATCH-156]

must_haves:
  truths:
    - "On Chrome desktop, switching away from the Skynet tab and back does NOT force a WebSocket reconnect and does NOT surface the Reconnect/Close overlay for otherwise-healthy sessions"
    - "On iOS PWA (window.navigator.standalone === true AND iOS UA), the visibilitychange-driven reconnect behavior from patches #143 v2 and #148 is preserved byte-for-byte (same reset + connectToHost / retryKey bump path fires on hidden→visible)"
    - "`isIosPwa()` returns false in SSR (window === undefined), false on Chrome desktop, false on iOS-Safari-not-PWA, false on Android-PWA; returns true only when both standalone=true AND iOS UA"
    - "`npx tsc --noEmit` from the repo root exits 0"
    - "`npx vitest run` passes with no new regressions (existing patch #148 visibilitychange tests updated to stub isIosPwa=true, plus a new 'not iOS PWA → handler no-ops' test)"
    - "Patch #156 entry appended to `/home/ubuntu/.claude/identities/tina/skynet-patches.md` (motivation, root cause chain, files touched, risk, verification)"
    - "Bounty `chrome-tab-switch-away-forces-manual-reconnect` closed (status=done, timeline entry) and archived to `~/.claude/identities/tina/bounties/archive/`"
    - "New bounty `terminal-ws-close-session-handoff-race` created at `~/.claude/identities/tina/bounties/` for the deferred backend race"
  artifacts:
    - path: "src/ui/lib/is-ios-pwa.ts"
      provides: "isIosPwa(): boolean helper"
      exports: ["isIosPwa"]
    - path: "src/ui/lib/is-ios-pwa.test.ts"
      provides: "vitest coverage of the four (standalone × iOS-UA) combinations plus SSR guard"
    - path: "src/ui/features/terminal/Terminal.tsx"
      provides: "patch #143 v2 visibilitychange useEffect gated behind isIosPwa()"
      contains: "isIosPwa"
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "patch #148 visibilitychange useEffect gated behind isIosPwa()"
      contains: "isIosPwa"
    - path: "src/ui/features/pretty-view/PrettyView.test.tsx"
      provides: "existing visibilitychange tests updated to opt into iOS-PWA mode; new 'non-iOS-PWA no-op' test"
  key_links:
    - from: "src/ui/features/terminal/Terminal.tsx"
      to: "src/ui/lib/is-ios-pwa.ts"
      via: "import { isIosPwa } from '@/lib/is-ios-pwa'"
      pattern: "isIosPwa"
    - from: "src/ui/features/pretty-view/PrettyView.tsx"
      to: "src/ui/lib/is-ios-pwa.ts"
      via: "import { isIosPwa } from '@/lib/is-ios-pwa'"
      pattern: "isIosPwa"
    - from: "src/ui/features/terminal/Terminal.tsx (visibilitychange useEffect, ~line 344)"
      to: "early-return guard"
      via: "if (!isIosPwa()) return; before document.addEventListener('visibilitychange', ...)"
      pattern: "if\\s*\\(\\s*!isIosPwa\\(\\)\\s*\\)\\s*return"
    - from: "src/ui/features/pretty-view/PrettyView.tsx (visibilitychange useEffect, ~line 793)"
      to: "early-return guard"
      via: "if (!isIosPwa()) return; before document.addEventListener('visibilitychange', ...)"
      pattern: "if\\s*\\(\\s*!isIosPwa\\(\\)\\s*\\)\\s*return"
---

<objective>
Patch #156 — gate the visibilitychange-driven force-reconnect behavior introduced by patches
#143 v2 (`Terminal.tsx`) and #148 (`PrettyView.tsx`) on iOS-PWA detection so it fires ONLY
on iOS PWA (where the OS silently kills WebSockets during backgrounding and `readyState` lies),
and NO-OPS everywhere else (Chrome desktop / Android / regular Safari), where WebSockets
actually stay alive across tab-switches and force-reconnecting them creates a session-attachment
race that ends with the server sending `disconnected` on the new WS.

Purpose: Close bounty `chrome-tab-switch-away-forces-manual-reconnect` — Ashley loses zero
open sessions when she Alt-Tabs away from the Skynet tab on Chrome desktop and comes back.
iOS PWA behavior is preserved unchanged (the very reason #143 v2 and #148 exist).

Output: New `is-ios-pwa` helper + tests, gated visibilitychange effects in two files, a patch
#156 entry in Tina's skynet-patches.md catalog, closed source bounty, and a filed follow-up
bounty for the deferred backend race in `terminal.ts` / `terminal-session-manager.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

@src/ui/features/terminal/Terminal.tsx
@src/ui/features/pretty-view/PrettyView.tsx
@src/ui/features/pretty-view/PrettyView.test.tsx
@src/ui/lib/utils.ts

# Format reference — read the tail of this file for the last shipped patch (#154)
# to match tone, section ordering, and depth. Do NOT dump the whole 10k+ line file
# into context; target the last ~200 lines.
# Path: /home/ubuntu/.claude/identities/tina/skynet-patches.md

# Bounty to close after code lands (identity artifact — NOT in the Skynet repo):
# ~/.claude/identities/tina/bounties/chrome-tab-switch-away-forces-manual-reconnect/bounty.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `is-ios-pwa` helper and gate both visibilitychange useEffects</name>
  <files>src/ui/lib/is-ios-pwa.ts, src/ui/lib/is-ios-pwa.test.ts, src/ui/features/terminal/Terminal.tsx, src/ui/features/pretty-view/PrettyView.tsx, src/ui/features/pretty-view/PrettyView.test.tsx</files>
  <behavior>
    is-ios-pwa.test.ts (new, vitest):
    - Test 1: `isIosPwa()` returns true when `window.navigator.standalone === true` AND UA matches /iP(hone|ad|od)/
    - Test 2: returns false when standalone=true but UA is Chrome desktop
    - Test 3: returns false when standalone=false but UA is iPhone (iOS Safari, not PWA)
    - Test 4: returns false when standalone=false and UA is Chrome desktop
    - Test 5: SSR guard — returns false without throwing when `window` is undefined (simulate via temporarily deleting or stubbing `globalThis.window`; use `vi.stubGlobal` if convenient)
    - Each test restores original navigator/window state in an afterEach so tests don't leak
    PrettyView.test.tsx (edit existing):
    - Existing patch #148 tests that use `fireVisibilityChange(...)` (grep for it — the helper is defined around line 130) must be updated so `isIosPwa()` returns true for the duration of those tests. Simplest approach: in the shared beforeEach (or per-test setup), stub `window.navigator.standalone = true` and stub the userAgent to an iPhone string via `Object.defineProperty(navigator, 'userAgent', ...)`, restoring both in afterEach. Do NOT globally mock the `is-ios-pwa` module — real code path is more valuable and cheap here.
    - Add ONE new test asserting the non-iOS-PWA path: when standalone is unset/false and UA is Chrome desktop, mount PrettyView, fire `fireVisibilityChange(true)` then `fireVisibilityChange(false)`, and assert NO new WS stub was created (i.e. `retryKey` did not bump — the existing WsStub factory tracks instantiations; assert the count is unchanged from the post-mount baseline).
    Terminal.tsx: no test scaffold currently exists for the patch #143 v2 visibilitychange effect (grep confirmed — only PrettyView.test.tsx tests visibilitychange). Do NOT add a new Terminal.test.tsx for this — the isIosPwa unit tests + PrettyView non-iOS-PWA test + manual Chrome UAT (out of scope for the executor per Ashley's "code motion only" cap) are sufficient. Note this explicitly in the SUMMARY.
  </behavior>
  <action>
    (1) Create `src/ui/lib/is-ios-pwa.ts`. Export a single named function `isIosPwa(): boolean` with the body shown in the plan's task_scope:
    - Return `false` if `typeof window === "undefined"` (SSR guard).
    - Compute `standalone = (window.navigator as { standalone?: boolean }).standalone === true` — the cast is necessary because `Navigator.standalone` is iOS-Safari-only and not in the standard lib.dom.d.ts types.
    - Compute `isIosUa = /iP(hone|ad|od)/.test(window.navigator.userAgent)`.
    - Return `standalone && isIosUa`.
    - File header comment: one paragraph referencing patch #156 and stating the intent ("Gate helper for the patch #143 v2 / #148 visibilitychange force-reconnect effects — those are the correct behavior on iOS PWA where the OS silently kills WSes during backgrounding, but a source of session-attachment races everywhere else").

    (2) Create `src/ui/lib/is-ios-pwa.test.ts`. Use vitest. Cover the five cases in <behavior>. Restore state in `afterEach`. The tests should mutate `window.navigator` via `Object.defineProperty(navigator, 'userAgent', { value: ..., configurable: true })` and set `(navigator as any).standalone = true/false`. For the SSR test, use `vi.stubGlobal('window', undefined)` and `vi.unstubAllGlobals()` in afterEach — this is the pattern already used elsewhere in the repo (grep for `stubGlobal` in `src/ui/lib` to confirm the shape if needed).

    (3) Edit `src/ui/features/terminal/Terminal.tsx`:
    - Add `import { isIosPwa } from "@/lib/is-ios-pwa";` in the existing `@/lib/*` import cluster (double-quoted to match the file's existing quote style — verify by scanning existing imports).
    - Locate the visibilitychange `useEffect` (currently starts around line 344). Its comment block runs from ~line 330-343 explaining the reset-and-reconnect intent inherited from patch #143 v2.
    - As the FIRST statement inside the useEffect body (before `const handleVisibilityChange = ...`), add: `if (!isIosPwa()) return;` — the early return means no listener is registered and the cleanup is a no-op, which is exactly what we want on Chrome desktop.
    - Update the comment block above the useEffect: prepend a paragraph noting that patch #156 narrows the scope of this effect to iOS PWA only, and briefly explain WHY (WebSockets survive tab-switches on desktop browsers, and force-reconnecting them races the server's session-attachment path and ends with the "disconnected" overlay). Keep the existing #143 v2 rationale intact — it's still accurate for the iOS-PWA case.

    (4) Edit `src/ui/features/pretty-view/PrettyView.tsx`:
    - Add `import { isIosPwa } from "@/lib/is-ios-pwa";` — note this file uses single-quoted imports (verify by looking at the existing `import { cn } from "@/lib/utils"` line — actually mixed; match the surrounding cluster's style: single-quoted appears more common further down. Read the file first and match its convention. Use double quotes if the top-of-file utils import is double-quoted; single elsewhere in the file body do not override that convention).
    - Locate the visibilitychange `useEffect` starting around line 793 (comment block above starts at line 781 — "Patch #148: visibilitychange handler — the direct Ashley iOS PWA fix.").
    - As the FIRST statement inside the useEffect body (before `const handleVisibilityChange = ...`), add: `if (!isIosPwa()) return;`
    - Update the comment block: replace "the direct Ashley iOS PWA fix" language with "…the direct Ashley iOS PWA fix. Patch #156 hard-gates this effect on isIosPwa() because on Chrome desktop / Android / non-PWA Safari, WebSockets survive tab-switches and force-reconnect creates a session-attachment race that surfaces the Reconnect/Close overlay." Preserve the existing detailed hide/visible-branch rationale below.

    (5) Edit `src/ui/features/pretty-view/PrettyView.test.tsx`:
    - Grep the file for `fireVisibilityChange` to enumerate every test that exercises the patch #148 handler.
    - For each such test (they live under the patch #148 test block — the "Test C (should): visibilitychange:visible resets counter and reconnects after cap" example around line 366 is one; there will be others in the same block): ensure `window.navigator.standalone === true` and the UA is an iPhone string for the duration of the test. Preferred implementation: introduce two small helpers `enableIosPwa()` / `restoreIosPwa()` in the test file's helper block (near `fireVisibilityChange` around line 130), and call them in a `beforeEach`/`afterEach` scoped to the patch #148 describe block ONLY (do NOT globally enable iOS-PWA in the file — it would break any future non-visibilitychange test that assumes desktop). Store originals in module-scoped variables and restore.
    - Add ONE new `it(...)` inside the patch #148 describe block titled something like `"Patch #156: non-iOS-PWA (Chrome desktop) — visibilitychange handler is not attached and does not trigger reconnect"`. In this test, do NOT call `enableIosPwa()` (leave the environment as jsdom's default, which is a desktop UA and no standalone flag). Mount PrettyView with the standard `mountPV()` helper. Capture the current WS stub instance count (however the existing tests track this — the WsStub factory should expose a counter or a list). Fire `fireVisibilityChange(true)` then `fireVisibilityChange(false)`. Assert no additional WS stub was created. Assert `retryKey` didn't bump (indirectly — no new WS instantiation IS the proof; do not reach into React internals).
    - If enumerating the WsStub factory reveals it does NOT expose a count, the simplest observable proof is to check `WebSocket` construction — if the tests use `vi.spyOn(globalThis, 'WebSocket')` or a factory helper, use that. Otherwise, add an explicit counter to the existing WsStub factory as a minimal test-infrastructure tweak. Do NOT rewrite the factory.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx vitest run src/ui/lib/is-ios-pwa.test.ts src/ui/features/pretty-view/PrettyView.test.tsx</automated>
  </verify>
  <done>
    - `src/ui/lib/is-ios-pwa.ts` exists and exports `isIosPwa`.
    - `src/ui/lib/is-ios-pwa.test.ts` exists; all 5 cases pass.
    - `Terminal.tsx` visibilitychange useEffect has `if (!isIosPwa()) return;` as first body statement.
    - `PrettyView.tsx` visibilitychange useEffect has `if (!isIosPwa()) return;` as first body statement.
    - `PrettyView.test.tsx` existing patch #148 visibilitychange tests still pass (via per-block iOS-PWA setup); the new non-iOS-PWA test passes.
    - `cd /home/ubuntu/skynet && npx tsc --noEmit` exits 0.
    - `cd /home/ubuntu/skynet && npx vitest run` exits 0 with no new regressions.
    - Single atomic git commit for these 5 files with a message referencing patch #156 (title suggestion: `patch(156): gate #143v2/#148 visibilitychange reconnect on iOS PWA`). Do NOT push. Do NOT tag.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append patch #156 entry to Tina's skynet-patches.md catalog</name>
  <files>/home/ubuntu/.claude/identities/tina/skynet-patches.md</files>
  <action>
    Append a new patch entry immediately after the current tail entry (patch #154 — the file has NO #155 header currently, which is fine; patches are catalogued in insertion order and gap-tolerant, and #155 may be documented later or represent an in-flight identity-modal follow-up that never made it to the catalog).

    First, verify the tail is #154 by reading the last ~200 lines of `/home/ubuntu/.claude/identities/tina/skynet-patches.md`. Match the section ordering and prose style from #154 EXACTLY — bullet-indent style (4-space leading indent on top-level bullets, 6-space on sub-bullets), em-dash separator in headers, past-tense narration, dense per-file breakdown in "Fix summary" and "Files touched".

    Header: `### Patch #156 — gate patches #143 v2 (Terminal) and #148 (PrettyView) visibilitychange reconnect on iOS-PWA detection`

    Sections to include (in this order, matching #154's structure):

    - **Motivation.** Bounty `chrome-tab-switch-away-forces-manual-reconnect` — on Chrome desktop, tab-switching away and back leaves some agent sessions showing the "Reconnect / Close" overlay instead of just staying connected. Ashley's UX expectation: WebSockets survive a tab-switch on desktop browsers, so nothing should visibly change on foreground. Reference the bounty path.

    - **Root cause.** Chain the diagnosis: patches #143 v2 (Terminal.tsx visibilitychange useEffect ~line 344) and #148 (PrettyView.tsx visibilitychange useEffect ~line 793) both register `visibilitychange` handlers that force a fresh WebSocket connection on every hidden→visible transition, unconditionally. That was correct for iOS PWA — the OS silently kills WebSockets during backgrounding and `readyState` lies (still reports OPEN when the socket is dead). On Chrome desktop, WSes actually stay alive, and force-reconnecting them creates a session-attachment race: the old WS is still attached to the server-side session when the client opens a new WS, the server detaches the old on `ws.on("close")` (see `src/backend/ssh/terminal.ts:259` and `src/backend/ssh/terminal-session-manager.ts:308` `detachWs` / `:339` `destroySession`), but if the detach lands AFTER the new WS attaches, `destroySession` runs and the new WS receives `disconnected`. Result: overlay. Frontend-only fix is sufficient because once we stop force-reconnecting on desktop, the race window collapses to zero for the normal tab-switch flow.

    - **Fix summary.**
      - New `src/ui/lib/is-ios-pwa.ts` helper: `isIosPwa()` returns true only when `window.navigator.standalone === true` (iOS Safari home-screen PWA flag) AND UA matches `/iP(hone|ad|od)/`. SSR-safe via `typeof window === "undefined"` guard. Vitest coverage for all 4 combos + SSR.
      - `src/ui/features/terminal/Terminal.tsx` (patch #143 v2 visibilitychange useEffect): first-statement early return `if (!isIosPwa()) return;`. Effect cleanup becomes a no-op on desktop (no listener registered → no listener to remove). Comment block prepended with patch #156 rationale; existing patch #143 v2 rationale preserved for the iOS-PWA case.
      - `src/ui/features/pretty-view/PrettyView.tsx` (patch #148 visibilitychange useEffect): identical treatment.
      - `PrettyView.test.tsx`: existing patch #148 visibilitychange tests scoped-opted into iOS-PWA via a `beforeEach` that sets `navigator.standalone=true` + iPhone UA (restored in `afterEach`). One new test asserts non-iOS-PWA doesn't attach the handler / trigger reconnect.

    - **Files touched.**
      - `src/ui/lib/is-ios-pwa.ts` (new, ~15 lines including header comment).
      - `src/ui/lib/is-ios-pwa.test.ts` (new, ~60 lines, 5 cases).
      - `src/ui/features/terminal/Terminal.tsx` (import + 1-line guard + comment update — small delta).
      - `src/ui/features/pretty-view/PrettyView.tsx` (import + 1-line guard + comment update — small delta).
      - `src/ui/features/pretty-view/PrettyView.test.tsx` (per-describe iOS-PWA setup + 1 new test).

    - **Rebase risk.** LOW. `is-ios-pwa.ts` is a new file — pure additive. The two edits inside `Terminal.tsx` and `PrettyView.tsx` are surgical: a single-line early return and a comment update at the top of a fork-only useEffect (both effects are fork additions from patches #143 v2 and #148 — upstream Skynet has no equivalent). If upstream ever adds a similar handler, that would show up as a rebase conflict on the same line range — trivially resolvable.

    - **Verification.** `cd /home/ubuntu/skynet && npx tsc --noEmit` exit 0. `cd /home/ubuntu/skynet && npx vitest run` exit 0 (frontend-only change; no backend files touched so `npm run build:backend` not required per Tina's fleet-directive on scope-limited edits). Ashley's UAT on Chrome desktop is the practical coverage for the actual bounty behavior — code motion here, deploy authorization NOT included.

    - **Related bounty.** `~/.claude/identities/tina/bounties/chrome-tab-switch-away-forces-manual-reconnect/` — closed and archived to `~/.claude/identities/tina/bounties/archive/chrome-tab-switch-away-forces-manual-reconnect/` in Task 3 of this quick plan.

    - **Deferred.**
      - The backend `ws.on("close")` race in `src/backend/ssh/terminal.ts:259` + `terminal-session-manager.ts:308`/`:339` remains. Once #156 lands, it won't fire during a normal Chrome tab-switch (no more force-reconnect), but it's still latently there for genuine WS drops that overlap a session-attachment. Captured as follow-up bounty `terminal-ws-close-session-handoff-race` (Task 3, priority=low — won't fire in normal use).
      - No Terminal.test.tsx scaffold was added for the patch #143 v2 gate — the isIosPwa unit tests + PrettyView non-iOS-PWA test + Ashley's Chrome UAT are the coverage layer. Add on demand if a regression suggests the gate isn't holding.

    Style rules to hold to (from re-reading #154):
    - Prose voice: past tense, dense, no filler. Match #154's paragraph density — no bulleted stubs.
    - Code-references in prose: use `code spans` for file paths, function names, and line refs.
    - Do NOT invent facts. If a claim needs a citation and you don't have it, hedge or omit.

    Note: this file lives OUTSIDE the Skynet git repo (it's in `~/.claude/identities/tina/`). Do NOT `git add` it. Do NOT include it in the Task 1 commit. The write is durable via filesystem alone.
  </action>
  <verify>
    <automated>grep -c "^### Patch #156" /home/ubuntu/.claude/identities/tina/skynet-patches.md</automated>
  </verify>
  <done>
    - Exactly one new `### Patch #156` section appended after the tail (currently #154) with all required sections in the specified order.
    - Section prose matches #154's style depth and voice (no stub bullets; substantive per-file breakdowns).
    - No git changes attempted on this file.
  </done>
</task>

<task type="auto">
  <name>Task 3: Bounty housekeeping — close source bounty, archive it, file follow-up backend-race bounty</name>
  <files>~/.claude/identities/tina/bounties/chrome-tab-switch-away-forces-manual-reconnect/bounty.json, ~/.claude/identities/tina/bounties/archive/chrome-tab-switch-away-forces-manual-reconnect/, ~/.claude/identities/tina/bounties/terminal-ws-close-session-handoff-race/bounty.json</files>
  <action>
    (A) Close the source bounty. Read `~/.claude/identities/tina/bounties/chrome-tab-switch-away-forces-manual-reconnect/bounty.json`. Update fields:
    - `status`: `"done"`
    - `updated_at`: current ISO timestamp (use `new Date().toISOString()` shape or `date -u +%Y-%m-%dT%H:%M:%SZ` in bash — match the existing 2026-07-24 entry's format, which is `2026-07-24T11:52:06Z` — no fractional seconds, trailing `Z`)
    - `timeline`: APPEND (do not replace) a single entry: `"<ISO timestamp> patch #156 shipped — visibilitychange force-reconnect in patches #143 v2 (Terminal.tsx) and #148 (PrettyView.tsx) gated on isIosPwa(); Chrome desktop tab-switch no longer forces reconnect; iOS PWA behavior preserved"`
    - Preserve every other field verbatim (id, title, premise, priority, keywords, source_links, requested_by, created_at, todos).

    Write the updated JSON back to the same path with `JSON.stringify(obj, null, 2)` shape (two-space indent — verify by reading the existing file first and matching its indent).

    Note: there is also a `tab-diag.js` sibling file in that bounty directory. It moves with the bounty when archived.

    (B) Archive the whole bounty directory. Move `~/.claude/identities/tina/bounties/chrome-tab-switch-away-forces-manual-reconnect/` (with all its contents including `tab-diag.js`) to `~/.claude/identities/tina/bounties/archive/chrome-tab-switch-away-forces-manual-reconnect/`. Use `mv` — the archive/ dir already exists (verified during planning). Do NOT copy-then-delete; a plain `mv` preserves timestamps.

    (C) File the follow-up backend-race bounty. Create `~/.claude/identities/tina/bounties/terminal-ws-close-session-handoff-race/bounty.json`. Structure (matches the shape of the existing bounty):
    - `id`: fresh UUID v4 (use `crypto.randomUUID()` via `node -e` or `uuidgen` — the closed bounty used a v4-shape UUID, match that shape)
    - `title`: `"Backend session-attachment race: ws.on('close') detach can arrive after a fresh WS attaches, causing spurious 'disconnected' on the new socket"`
    - `premise`: A 3-5 sentence description of the race. Reference the specific code paths from the diagnostic session: `src/backend/ssh/terminal.ts:259` (`ws.on("close")` handler), `src/backend/ssh/terminal-session-manager.ts:308` (`detachWs`), `src/backend/ssh/terminal-session-manager.ts:339` (`destroySession`). Explain that once patch #156 lands the race won't fire during normal Chrome tab-switches (because the frontend no longer force-reconnects), but it's still latent for genuine WS drops that overlap a re-attach — a network flap during a tab-switch could still trigger it. Note that a proper fix would be server-side: seq-number the session-attach handshake so the server can detect and drop a stale detach that arrives after a fresher attach.
    - `status`: `"on_deck"`
    - `priority`: `"low"` (per the task_scope directive — won't fire in normal use once #156 lands)
    - `keywords`: `["terminal", "websocket", "backend", "race-condition", "session-attachment"]`
    - `source_links`: empty array
    - `requested_by`: `"tina"` (this is Tina's diagnostic capture, not an Ashley report — the original bounty was Ashley's, this deferral is Tina's)
    - `created_at`: current ISO timestamp
    - `updated_at`: same ISO timestamp as `created_at`
    - `timeline`: single entry `"<ISO timestamp> created — deferred from patch #156 diagnostic session; see closed bounty archive/chrome-tab-switch-away-forces-manual-reconnect for context"`
    - `todos`: empty array

    Write with the same 2-space indent JSON shape as the existing bounty.

    Do NOT attempt `git add` on any of these paths — they are identity-artifact files outside the Skynet repo. This task's work is purely filesystem-durable.
  </action>
  <verify>
    <automated>test -f ~/.claude/identities/tina/bounties/archive/chrome-tab-switch-away-forces-manual-reconnect/bounty.json && test ! -e ~/.claude/identities/tina/bounties/chrome-tab-switch-away-forces-manual-reconnect && test -f ~/.claude/identities/tina/bounties/terminal-ws-close-session-handoff-race/bounty.json && node -e 'const a=require("os").homedir()+"/.claude/identities/tina/bounties/archive/chrome-tab-switch-away-forces-manual-reconnect/bounty.json"; const b=require("os").homedir()+"/.claude/identities/tina/bounties/terminal-ws-close-session-handoff-race/bounty.json"; const A=require(a); const B=require(b); if(A.status!=="done") throw new Error("A.status not done: "+A.status); if(B.status!=="on_deck") throw new Error("B.status not on_deck: "+B.status); if(B.priority!=="low") throw new Error("B.priority not low: "+B.priority); if(!A.timeline.some(t=>t.includes("patch #156"))) throw new Error("A.timeline missing patch #156 entry"); console.log("ok");'</automated>
  </verify>
  <done>
    - Source bounty archived at `~/.claude/identities/tina/bounties/archive/chrome-tab-switch-away-forces-manual-reconnect/bounty.json` with `status="done"` and a patch #156 timeline entry.
    - Original bounty directory `~/.claude/identities/tina/bounties/chrome-tab-switch-away-forces-manual-reconnect/` no longer exists at the top level.
    - New bounty `~/.claude/identities/tina/bounties/terminal-ws-close-session-handoff-race/bounty.json` exists with `status="on_deck"`, `priority="low"`, and a premise that references the specific backend line numbers.
    - No git commit or push attempted (identity artifacts live outside the Skynet repo).
  </done>
</task>

</tasks>

<verification>
- `cd /home/ubuntu/skynet && npx tsc --noEmit` exit 0
- `cd /home/ubuntu/skynet && npx vitest run` exit 0 (no new failures)
- `grep -c "^### Patch #156" /home/ubuntu/.claude/identities/tina/skynet-patches.md` == 1
- Source bounty archived + follow-up bounty filed (see Task 3 verify)
- Single atomic git commit in the Skynet repo for Task 1 (5 files); Task 2 and Task 3 leave zero git state
</verification>

<success_criteria>
- On Chrome desktop, Alt-Tab-away-then-back on the Skynet tab does NOT force a WebSocket reconnect and does NOT surface the Reconnect/Close overlay (Ashley's UAT — out of scope for the executor per the "code motion only" cap; this is what closes the bounty in practice, and it's recorded as the expected outcome).
- On iOS PWA, patch #143 v2 and patch #148 reconnect behavior fires exactly as before (unit-observable via `isIosPwa()`-true tests in PrettyView.test.tsx).
- Patch #156 documented in `~/.claude/identities/tina/skynet-patches.md` at parity of depth with patch #154.
- Bounty `chrome-tab-switch-away-forces-manual-reconnect` is closed and archived.
- Backend-race follow-up captured as a new low-priority bounty referencing the exact code paths.
- Executor stops at commit — no push, no build, no deploy.
</success_criteria>

<output>
Create `.planning/quick/260727-r09-patch-156-gate-patches-143-v2-terminal-a/260727-r09-SUMMARY.md` when done. In the SUMMARY, explicitly note:
- Whether the WsStub factory in PrettyView.test.tsx needed a counter tweak or already exposed one (the executor discovers this).
- The exact ISO timestamps used for the closed-bounty timeline entry and the new-bounty created_at (for cross-referencing with the patch-catalog entry).
- The UUID assigned to the new `terminal-ws-close-session-handoff-race` bounty.
</output>
