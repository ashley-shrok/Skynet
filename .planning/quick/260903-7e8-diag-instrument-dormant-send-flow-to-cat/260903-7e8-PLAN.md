---
phase: quick-260903-7e8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/backend/claude-session/pv-send-watchdog.ts
  - src/backend/claude-session/claude-session-server.ts
autonomous: false
requirements:
  - Q-260903-7e8-instrument-dormant-send

must_haves:
  truths:
    - "Every arm of a client pending-send timer emits a greppable line with mqid, dormant flag, timeoutMs, arm_reason"
    - "Every fire of a client pending-send timer emits a greppable line with mqid, elapsedMs, dormant-at-fire, branch label"
    - "Every clear of a client pending-send (FIFO head-match, paste_send_failed, send_keys_error, WS-close, session_changed) emits a greppable line with mqid, matched_by, elapsedMs, replaced flag"
    - "Every incoming message matcher decision (matched pending vs new bubble vs relay dedup drop) emits a greppable line with pendingSends-at-arrival, matched_pending_mqid, action"
    - "Every relay_inbound matrixEventId dedup decision emits a greppable line covering all three code paths (appendDedup, appendDedupWithCap, load-more range-fetch) with incoming_matrixEventId, matched_existing, path"
    - "Backend dormant-send-start already exists (pv_input_dormant_send_start); no additional log needed there"
    - "Backend sentinel drop, marker-wait entry, marker-fresh, marker-fallback boundaries emit greppable lines carrying mqid + elapsedMs (most already exist as pv_input_dormant_* operations; verify + fill gaps)"
    - "Backend pv-send-watchdog arm site emits a greppable line with mqid, sessionId, contentHash, bodyBytes, dormantSend, timing constants used"
    - "Backend pv-send-watchdog fire sites (retry, full-resend, give-up, matched, cleared) already emit sshLogger lines carrying mqid + gapMs; verify prefix consistency + add [diag-dormant-send] alias so a single grep covers frontend + backend"
    - "docker compose build skynet + docker compose up -d --force-recreate skynet complete without error"
    - "term.gigaashley.click returns HTTPS 200 on / after recreate"
  artifacts:
    - path: src/ui/features/pretty-view/PrettyView.tsx
      provides: "Client-side [diag-dormant-send] instrumentation at arm/fire/cleanup/incoming-match/dedup sites"
    - path: src/backend/claude-session/pv-send-watchdog.ts
      provides: "Backend [diag-dormant-send] arm/retry/full-resend/give-up/matched/cleared log alias lines"
    - path: src/backend/claude-session/claude-session-server.ts
      provides: "Backend [diag-dormant-send] alias lines on dormant-send-start, sentinel-drop, marker-wait boundaries, split-send arm"
  key_links:
    - from: "PrettyView.tsx handleOptimisticSend arm site (L1197-1222)"
      to: "backend __applyInputMessageForTests dormant branch (claude-session-server.ts L2061-2184)"
      via: "shared mqid — client generates, threads through WS sendInput frame's messageQueueItemId, backend echoes back in every dormant-log line"
      pattern: "\\[diag-dormant-send\\] arm.*mqid=[a-zA-Z0-9_-]+"
    - from: "PrettyView.tsx FIFO head-match cleanup (L1789-1795)"
      to: "backend pv-send-watchdog notifyMatched (pv-send-watchdog.ts L447-464)"
      via: "same mqid — client clears optimistic on user-role frame arrival, backend clears watchdog on contentHash match"
      pattern: "\\[diag-dormant-send\\] (cleanup|matched).*mqid=[a-zA-Z0-9_-]+"
---

<objective>
Ashley reported a post-Phase-62 failure mode: dormant-session send → optimistic bubble → spinner → **goes red** → then a real bubble arrives **additionally** (red one persists) → **sometimes a duplicate real bubble**. She's also less-sure but suspects the session receives the message twice occasionally.

Phase 62 widened the client pending-send timer to 220s for dormant sends (commit b98f81f1, deployed 2026-09-02 18:06Z). That fix is in the current image but the symptom persists in a new form — meaning either the widened timer isn't arming on the dormant path Ashley is exercising, OR the timer is arming but firing early via a code path Phase 62 missed (WS-close cleanup racing? paste_send_failed still firing? send_keys_error?), OR the incoming user-role FIFO head-match isn't clearing the pending before the timer fires.

**This plan is diagnostic-only. No logic changes.** Purely add well-labeled log lines at every transition point in the dormant-send flow so the next production repro tells us:

1. Which branch armed the client timer (dormant=true → 220s? or dormant=false → 20s?).
2. Which branch fired the red bubble (client timer? paste_send_failed? send_keys_error? WS-close?).
3. Why the real message didn't cleanly clear the pending (FIFO head-match found no pending? matched wrong pending? arrived after flipToFailed?).
4. Why a second copy sometimes arrives (backend full-resend firing? matrixEventId dedup missing on some path?).

Follow the exact diag-commit pattern from adb90600 (`diag(terminal): instrument write pipeline + WS handler lifecycle`): single atomic commit, `diag(...):` scope, consistent greppable prefix, no logic changes, no test changes.

Purpose: give Ashley + next-shift maintainer a container-log + browser-console grep target that reconstructs a full send timeline from a single `grep '[diag-dormant-send]'`.

Output: instrumented PrettyView.tsx + pv-send-watchdog.ts + claude-session-server.ts committed atomically, built into a Docker image, deployed to term.gigaashley.click, HTTPS-200 verified. Ashley (or peer instance) then reproduces the failure and pastes the log slice — a follow-up phase (66+) plans the actual fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/ui/features/pretty-view/PrettyView.tsx
@src/backend/claude-session/pv-send-watchdog.ts
@src/backend/claude-session/claude-session-server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add [diag-dormant-send] instrumentation at every client + backend transition point in the dormant-send flow</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx, src/backend/claude-session/pv-send-watchdog.ts, src/backend/claude-session/claude-session-server.ts</files>
  <action>
Diagnostic-only: add greppable console.info/sshLogger.info lines at every transition point of the dormant-send flow. Use the consistent prefix `[diag-dormant-send]` in EVERY new line (both frontend console and backend sshLogger message field) so a single grep across container logs + browser console reconstructs a full send timeline.

**Common design rules (apply to every log line added):**
- Prefix: `[diag-dormant-send]` — always exact string, no variants.
- Correlation id: emit `mqid=<clientId-or-synth-or-"none">` on every line. The client generates the mqid; it threads through the WS `sendInput` frame's `messageQueueItemId`; backend echoes it in every dormant-log line. For sites where mqid genuinely does not exist (e.g. a range-fetch dedup decision on an inbound peer message), emit `mqid=n/a` and include whatever else uniquely identifies the event (matrixEventId, eventId).
- Timing: emit either an absolute `now=<Date.now()>` OR an `elapsedMs=<n>` from a labeled anchor (arm-time, sentinel-drop-time, etc.). Prefer elapsedMs where an anchor exists; use `now` at anchor sites.
- Branch state: emit the state that made the code go this way (`dormant=true|false`, `dormant_at_fire=true|false`, `branch=widened|normal|ws-close`, `matched_by=fifo-head-match|contenthash|matrixEventId|clientId`, `action=replace|newBubble|dedup-drop`, etc.).
- Keep lines ONE line — no multi-line JSON blobs. Concatenate `key=value` pairs space-separated.
- No behavior change. No new variables that participate in control flow. No refactoring. Only console.info / sshLogger.info additions (plus at most a small `writeDiag(...)` helper that ONLY calls console.info if you want to DRY the frontend lines — do not extract anything else).

**Frontend site 1 — `PrettyView.tsx` handleOptimisticSend arm (near L1197-1222):**
- Immediately AFTER `const armedDormant = dormantRef.current === true;` and BEFORE the `window.setTimeout` call, log:
  `[diag-dormant-send] arm mqid=<mqid> dormant=<armedDormant> timeoutMs=<timeoutMs> arm_reason=<timeoutReason> collapsedLen=<collapsed.length> pendingCount=<pendingSendsRef.current.length> now=<Date.now()>`
- Inside the setTimeout callback (right before `flipToFailed(mqid, timeoutReason)`), also log the pre-fire snapshot — this is the FIRE site:
  `[diag-dormant-send] fire mqid=<mqid> elapsedMs=<Date.now() - armSentAt> dormant_at_arm=<armedDormant> dormant_at_fire=<dormantRef.current === true> branch=<armedDormant ? "widened" : "normal"> pendingCount=<pendingSendsRef.current.length> stillPending=<pendingSendsRef.current.some(p => p.mqid === mqid && p.state === "sending")>`
- To capture `armSentAt` for elapsedMs at fire time, capture `const armSentAt = Date.now();` in the enclosing closure (this is not a behavior change — it's a diag-local const). Reuse the value already stored on the PendingSend record (`sentAt`) if you prefer — same value.

**Frontend site 2 — `PrettyView.tsx` flipToFailed (near L1127-1144):**
- Inside `setPendingSends`, AFTER the `found = prev.find(...)` check and BEFORE the `console.warn("[pv-optim] flip-to-failed ...)` line (leave that existing line intact — it's already useful), add:
  `[diag-dormant-send] flip-to-failed mqid=<mqid> reason=<reason> elapsedMs=<Date.now() - found.sentAt> foundState=<found.state> contentLen=<found.content.length>`

**Frontend site 3 — `PrettyView.tsx` FIFO head-match cleanup (near L1788-1795, inside `case "message":`):**
- Wrap the existing `if (parsed.role === "user")` block. Log the DECISION unconditionally (whether a match was found OR not), BEFORE the setPendingSends call. Two log shapes:
  - Match: `[diag-dormant-send] cleanup mqid=<match.mqid> matched_by=fifo-head-match elapsedMs=<Date.now() - match.sentAt> replaced=true incoming_eventId=<parsed.eventId> incoming_line=<parsed.line ?? "?"> pendingCountBefore=<list.length>`
  - No match: `[diag-dormant-send] incoming-user-frame mqid=n/a matched_by=none pendingCountBefore=<list.length> incoming_eventId=<parsed.eventId> incoming_line=<parsed.line ?? "?"> action=newBubble reason=no-sending-pending`
- Also log EVERY incoming user-role frame regardless of match — this is the "real message arrival → pending-send matching" instrumentation. Include a body preview (first 60 chars, whitespace collapsed) so we can correlate the incoming content against the pending's `content`:
  Extend the match/no-match line with `preview="<parsed.content.replace(/\s+/g, " ").trim().slice(0, 60)>"` (guard for parsed.content being undefined).

**Frontend site 4 — `PrettyView.tsx` paste_send_failed handler (near L2267-2272):**
- Before the `flipToFailed(parsed.mqid, parsed.reason ?? "paste_send_failed")` call, log:
  `[diag-dormant-send] ws-paste-send-failed mqid=<parsed.mqid ?? "none"> reason=<parsed.reason ?? "paste_send_failed"> action=flipToFailed-from-backend-frame`

**Frontend site 5 — `PrettyView.tsx` send_keys_error handler (near L2277-2282):**
- Before the `flipToFailed(...)` call, log:
  `[diag-dormant-send] ws-send-keys-error mqid=<parsed.mqid ?? "none"> reason=<parsed.reason ?? "send_keys_error"> action=flipToFailed-from-backend-frame`

**Frontend site 6 — `PrettyView.tsx` clearAllPendingSends (near L1240-1248):**
- Inside the `for` loop, per pending, log:
  `[diag-dormant-send] cleanup mqid=<p.mqid> matched_by=ws-close-or-unmount elapsedMs=<Date.now() - p.sentAt> replaced=false state=<p.state>`
- The caller sites are: WS-close unmount (useEffect return, L1250-1252) and session_changed (L2240). The reason label distinguishes them via CALL CONTEXT. To disambiguate, temporarily accept an optional string arg on clearAllPendingSends: `clearAllPendingSends(reason: string = "unspecified")` and log `matched_by=${reason}`. Callers pass `"ws-close-unmount"`, `"session-changed"`, `"session-rotation"` (L1769). This IS a signature change but callers are internal to this file and the default preserves behavior — acceptable within "no logic change" because no branch decision is affected.

**Frontend site 7 — `PrettyView.tsx` matrixEventId dedup decisions (appendDedup L298-329, appendDedupWithCap L342-380, load-more range-fetch loop L1970-2045):**
- In `appendDedup`: on the matrixEventId-dedup hit (line ~319, before `return prev;`), log:
  `[diag-dormant-send] dedup-drop mqid=n/a path=inbound incoming_matrixEventId=<next.matrixEventId> incoming_eventId=<next.eventId> matched_existing=true`
- In `appendDedupWithCap`: same shape, log:
  `[diag-dormant-send] dedup-drop mqid=n/a path=inbound-cap incoming_matrixEventId=<mid> incoming_eventId=<next.eventId> matched_existing=true`
- In range-fetch loop (~L1998, before `continue`): log:
  `[diag-dormant-send] dedup-drop mqid=n/a path=range-fetch incoming_matrixEventId=<asRelay.matrixEventId> incoming_eventId=<m.eventId> matched_existing=true`
- Also log the FIRST-time-seen path for each matrixEventId at the store site (right after `seenMatrixEventIds.add(mid)` in appendDedupWithCap, and after the equivalent implicit store in the range-fetch loop `seenMatrixEventIds.add(asRelay.matrixEventId)` ~L2002). Note: `appendDedup` (L298) does not maintain a Set — the check is `prev.some(...)`; nothing to log there for "store".
- For appendDedupWithCap store: `[diag-dormant-send] dedup-store mqid=n/a path=inbound-cap incoming_matrixEventId=<mid> incoming_eventId=<next.eventId>`
- For range-fetch store: `[diag-dormant-send] dedup-store mqid=n/a path=range-fetch incoming_matrixEventId=<asRelay.matrixEventId> incoming_eventId=<m.eventId>`

**Backend site 1 — `claude-session-server.ts` dormant branch entry (L2078-2086):**
- The existing `sshLogger.info("[pv-input] send received while pane dormant, dropping sentinel", { operation: "pv_input_dormant_send_start", ... })` already carries mqid. ADD a paired short-form line immediately after:
  `sshLogger.info(\`[diag-dormant-send] backend dormant-send-start mqid=${mqidForDormantLog.length > 0 ? mqidForDormantLog : "none"} sessionId=${currentTmuxSession} triggerTs=${triggerTs}\`);`

**Backend site 2 — `claude-session-server.ts` sentinel-drop success (right after L2102, after the successful `await exec(...)` for `rm -f .dormant`):**
- Emit: `sshLogger.info(\`[diag-dormant-send] backend sentinel-dropped mqid=${mqidForDormantLog.length > 0 ? mqidForDormantLog : "none"} elapsedMs=${Date.now() - triggerTs}\`);`
- Sentinel-drop failure branch (~L2103-2117) already logs `pv_input_dormant_sentinel_drop_failed`; add companion:
  `sshLogger.warn(\`[diag-dormant-send] backend sentinel-drop-failed mqid=${mqidForDormantLog.length > 0 ? mqidForDormantLog : "none"} elapsedMs=${Date.now() - triggerTs} error="${sentinelErr instanceof Error ? sentinelErr.message : String(sentinelErr)}"\`);`

**Backend site 3 — `claude-session-server.ts` marker-poll boundaries (L2137-2177):**
- The `while (true)` loop ticks every 500ms. Do NOT log every tick — noisy. Log ONLY the two exit paths (fresh and fallback) with elapsedMs, which are already logged with `pv_input_dormant_marker_fresh` / `pv_input_dormant_marker_fallback` (L2154-2177). Add companions:
  - After the fallback log (~L2165): `sshLogger.info(\`[diag-dormant-send] backend marker-fallback mqid=${mqidForDormantLog.length > 0 ? mqidForDormantLog : "none"} elapsedMs=${elapsedMs} branch=fallback\`);`
  - After the fresh log (~L2176): `sshLogger.info(\`[diag-dormant-send] backend marker-fresh mqid=${mqidForDormantLog.length > 0 ? mqidForDormantLog : "none"} elapsedMs=${elapsedMs} branch=fresh\`);`

**Backend site 4 — `claude-session-server.ts` split-send watchdog arm (~L2312-2318, right after the existing `sshLogger.info("[pv-input] armed split-send watchdog", ...)` call):**
- Add companion: `sshLogger.info(\`[diag-dormant-send] backend watchdog-arm mqid=${mqid} sessionId=${deps.sessionId} bodyBytes=${body.length} dormantSend=${wasDormant} contentHash=${contentHash.slice(0, 8)}\`);`

**Backend site 5 — `claude-session-server.ts` non-split retry-Enter-only arm (~L2365-2374, after the existing `sshLogger.warn("[pv-input] armed non-split retry-Enter-only watchdog"...)`):**
- Add companion: `sshLogger.warn(\`[diag-dormant-send] backend watchdog-arm-retryonly mqid=${synthMqid} sessionId=${deps.sessionId} bodyBytes=${nonSplitBody.length} dormantSend=${wasDormant} synth=true\`);`

**Backend site 6 — `pv-send-watchdog.ts` arm success (L423-434, the `logger.debug("pv-send-watchdog: armed", ...)` call):**
- Immediately after that debug call, add an INFO-level `[diag-dormant-send]` companion so the arm shows up without needing debug-level logging enabled:
  `logger.info("[diag-dormant-send] watchdog-arm-complete", { operation: "diag_dormant_watchdog_arm", mqid, sessionId, contentHash: contentHash.slice(0, 8), bodyBytes: body.length, dormantSend: args.dormantSend === true, retryEnterOnly: args.retryEnterOnly === true, retryDelayMs: retryDelay, fullResendDelayMs: args.retryEnterOnly ? null : fullResendDelay, giveUpDelayMs: args.retryEnterOnly ? null : giveUpDelay });`

**Backend site 7 — `pv-send-watchdog.ts` retry-Enter fire (L276-284):**
- Immediately after the existing `logger.warn("pv-send-watchdog: no signal within 2500ms...")` add:
  `logger.info("[diag-dormant-send] watchdog-fire-retry-enter", { operation: "diag_dormant_watchdog_retry", mqid, sessionId, elapsedMs: Date.now() - entry.armedAt });`

**Backend site 8 — `pv-send-watchdog.ts` full-resend fire (L322-330):**
- After the existing `logger.warn("pv-send-watchdog: no signal within 5500ms...")`:
  `logger.info("[diag-dormant-send] watchdog-fire-full-resend", { operation: "diag_dormant_watchdog_full_resend", mqid, sessionId, elapsedMs: Date.now() - entry.armedAt });`

**Backend site 9 — `pv-send-watchdog.ts` give-up fire (L388-397):**
- After the existing `logger.error("pv-send-watchdog: no signal within 20000ms...")`:
  `logger.info("[diag-dormant-send] watchdog-fire-give-up", { operation: "diag_dormant_watchdog_give_up", mqid, sessionId, elapsedMs: Date.now() - entry.armedAt, branch: "paste_send_failed_emitted" });`

**Backend site 10 — `pv-send-watchdog.ts` notifyMatched (L447-464):**
- After the existing `logger.debug("pv-send-watchdog: matched signal — clearing pending", ...)`:
  `entry.logger.info("[diag-dormant-send] watchdog-matched", { operation: "diag_dormant_watchdog_matched", mqid, sessionId, elapsedMs: Date.now() - entry.armedAt, matched_by: "contentHash" });`

**Backend site 11 — `pv-send-watchdog.ts` clearPvSendWatchdog (L472-485):**
- After the existing `entry.logger.debug("pv-send-watchdog: cleared", ...)`:
  `entry.logger.info("[diag-dormant-send] watchdog-cleared", { operation: "diag_dormant_watchdog_cleared", mqid, sessionId: entry.sessionId, elapsedMs: Date.now() - entry.armedAt, reason: "explicit_clear_or_ws_close" });`

**Backend site 12 — `pv-send-watchdog.ts` clearPvSendWatchdogsForSession (L507-527):**
- Inside the `for` loop iteration where an entry matches sessionId (right after the existing debug log), add:
  `entry.logger.info("[diag-dormant-send] watchdog-cleared", { operation: "diag_dormant_watchdog_cleared", mqid, sessionId: entry.sessionId, elapsedMs: Date.now() - entry.armedAt, reason: "session_recycle" });`

**Explicit non-goals for this task (do NOT do these):**
- Do NOT change any control-flow variable (no new state that gates a branch).
- Do NOT change timing constants, timeouts, or watchdog behavior.
- Do NOT alter test files (adb90600 shipped without test changes for pure diag adds — same policy).
- Do NOT delete or reword existing sshLogger / console.warn / console.info lines — ADD companion lines only. Rationale: preserves grep continuity for anyone tracing older repros that reference the old prefixes ([pv-optim], [pv-input], [pv-load-more-diag], etc.).
- Do NOT introduce a shared logger utility — keep additions minimal + local so a `git blame` on any line points to this single diag commit.

**Verification during coding:**
- After edits, run `grep -c "\[diag-dormant-send\]" src/ui/features/pretty-view/PrettyView.tsx` — expect >= 12 additions (arm, fire, flip-to-failed, cleanup match, cleanup no-match, ws-paste-send-failed, ws-send-keys-error, clearAllPendingSends loop, appendDedup drop, appendDedupWithCap drop + store, range-fetch drop + store). Filter comment lines: `grep -v '^\s*//' src/ui/features/pretty-view/PrettyView.tsx | grep -c "\[diag-dormant-send\]"` >= 12.
- `grep -v '^\s*//' src/backend/claude-session/pv-send-watchdog.ts | grep -c "\[diag-dormant-send\]"` >= 6 (arm, retry, full-resend, give-up, matched, cleared×2).
- `grep -v '^\s*//' src/backend/claude-session/claude-session-server.ts | grep -c "\[diag-dormant-send\]"` >= 6 (start, sentinel-dropped, sentinel-drop-failed, marker-fallback, marker-fresh, watchdog-arm, watchdog-arm-retryonly).
  </action>
  <verify>
    <automated>bash -lc "cd /home/ubuntu/skynet-tina && grep -v '^\s*//' src/ui/features/pretty-view/PrettyView.tsx | grep -c '\[diag-dormant-send\]' | (read n; test \"\$n\" -ge 12 && echo OK:frontend=\$n || (echo FAIL:frontend=\$n; exit 1)) && grep -v '^\s*//' src/backend/claude-session/pv-send-watchdog.ts | grep -c '\[diag-dormant-send\]' | (read n; test \"\$n\" -ge 6 && echo OK:watchdog=\$n || (echo FAIL:watchdog=\$n; exit 1)) && grep -v '^\s*//' src/backend/claude-session/claude-session-server.ts | grep -c '\[diag-dormant-send\]' | (read n; test \"\$n\" -ge 6 && echo OK:server=\$n || (echo FAIL:server=\$n; exit 1)) && npm run -s typecheck 2>&1 | tail -5"</automated>
  </verify>
  <done>
- Frontend: >= 12 non-comment lines containing `[diag-dormant-send]` in PrettyView.tsx covering all 7 sites listed above.
- Backend watchdog: >= 6 non-comment lines containing `[diag-dormant-send]` in pv-send-watchdog.ts covering arm/retry/full-resend/give-up/matched/cleared.
- Backend server: >= 6 non-comment lines containing `[diag-dormant-send]` in claude-session-server.ts covering dormant-send-start/sentinel-dropped/marker-boundaries/watchdog-arm sites.
- `npm run typecheck` passes (no new TS errors introduced by the const captures, string interpolations, or optional-arg addition to clearAllPendingSends).
- No test files modified. No behavior-changing logic edits.
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit the diag atomically (single commit, diag(...) scope)</name>
  <files>[git commit only — no file edits]</files>
  <action>
Stage the three modified files ONLY (no test files, no other paths):

```
git add src/ui/features/pretty-view/PrettyView.tsx src/backend/claude-session/pv-send-watchdog.ts src/backend/claude-session/claude-session-server.ts
```

Verify nothing else is staged:

```
git diff --cached --stat
```

If any other file appears staged, `git restore --staged` it before committing.

Create the commit with the message body BELOW using a heredoc (matches the adb90600 style — full body explaining what's instrumented + why + how to read the logs, no test changes claim, no deploy claim):

```
git commit -m "$(cat <<'EOF'
diag(pretty-view): instrument dormant-send flow to catch red-bubble + duplicate real-bubble failure mode

Ashley 2026-09-03: post-Phase-62 (client pending-send timer widened to 220s
for dormant sends, commit b98f81f1) she is still seeing dormant-session
sends produce optimistic bubble → spinner → RED bubble → then a real bubble
arrives additionally (red persists) → sometimes a duplicate real bubble.
Phase 62 fix is in the deployed image (built 2026-09-02 18:06Z) but the
symptom persists in a new-symptom form, meaning Phase 62 missed something.

Static-read narrows to four possible break points I cannot split without
runtime data:

  A. Client timer arming with dormant=false when Ashley perceives dormant
     — 20s normal timer fires before backend's ~90-120s dormant wake
     completes. Would show as `arm dormant=false` followed by
     `fire dormant_at_fire=true branch=normal` in the log slice.

  B. Client timer correctly arming with dormant=true (220s) but some
     OTHER path (paste_send_failed frame from backend, send_keys_error,
     WS-close cleanup) flipping the pending to failed early. Would show
     as `arm dormant=true timeoutMs=220000` followed by
     `ws-paste-send-failed` OR `ws-send-keys-error` OR `cleanup
     matched_by=ws-close-unmount` well before the 220s window.

  C. Backend full-resend firing (T+95.5s in dormant mode) after the
     initial send already delivered — producing the duplicate real
     bubble. Would show as `watchdog-fire-full-resend` when the pane
     already produced a matching user-role message frame.

  D. matrixEventId dedup missing on some path — one physical peer/self
     message rendering as 2-3 bubbles. Would show as multiple
     `dedup-store` for the same matrixEventId across paths without
     corresponding `dedup-drop`, OR a `dedup-drop` firing AFTER
     `newBubble` was already accepted for the same matrixEventId on a
     different path.

This patch adds greppable log lines at every transition point of the
dormant-send flow, on both sides of the WS, using a consistent
[diag-dormant-send] prefix. A single grep across container logs +
browser console reconstructs a full send timeline:

  Frontend (PrettyView.tsx):
    arm                  — pending-send timer registered; dormant flag, timeoutMs, reason label
    fire                 — pending-send timer expired; elapsedMs, dormant-at-arm vs at-fire, branch
    flip-to-failed       — any path flipping a pending to state:'failed'; reason, elapsedMs
    cleanup              — pending removed; matched_by (fifo-head-match / ws-close-unmount / session-changed / session-rotation), elapsedMs, replaced flag
    incoming-user-frame  — every user-role incoming; whether it head-matched a pending, preview
    ws-paste-send-failed — backend paste_send_failed frame received; mqid, reason
    ws-send-keys-error   — backend send_keys_error frame received; mqid, reason
    dedup-drop           — matrixEventId dedup dropped an incoming; path (inbound/inbound-cap/range-fetch), incoming matrixEventId + eventId
    dedup-store          — matrixEventId recorded as seen; path, matrixEventId + eventId

  Backend (claude-session-server.ts + pv-send-watchdog.ts):
    backend dormant-send-start        — pv-input entered dormant branch; mqid, triggerTs
    backend sentinel-dropped          — .dormant sentinel rm succeeded; elapsedMs
    backend sentinel-drop-failed      — .dormant sentinel rm threw; elapsedMs, error
    backend marker-fresh              — .resume-complete marker fresh, dispatching send-keys; elapsedMs
    backend marker-fallback           — MARKER_FALLBACK_MS elapsed, dispatching anyway; elapsedMs
    backend watchdog-arm              — split-send watchdog armed with dormantSend flag
    backend watchdog-arm-retryonly    — non-split retry-Enter-only watchdog armed (mqid-loss safety net)
    watchdog-arm-complete             — timer chain scheduled; retryDelayMs, fullResendDelayMs, giveUpDelayMs
    watchdog-fire-retry-enter         — Stage 1 retry Enter dispatched; elapsedMs
    watchdog-fire-full-resend         — Stage 2 full-resend (C-u + retype + Enter) dispatched; elapsedMs
    watchdog-fire-give-up             — Stage 3 paste_send_failed frame emitted; elapsedMs
    watchdog-matched                  — parser signal cleared this watchdog; matched_by, elapsedMs
    watchdog-cleared                  — explicit clear (ws-close, session-recycle); reason, elapsedMs

Correlation contract: every line carries mqid=<clientId-or-synth-or-"n/a">
so a single grep with `[diag-dormant-send]` and mqid=<slug> reconstructs
the full timeline for one send across both processes.

No behavior change. Existing sshLogger operations (pv_input_dormant_*,
pv_send_watchdog_*, [pv-optim], [pv-input], [pv-load-more-diag]) preserved
verbatim — new lines are ADDITIVE companions with the [diag-dormant-send]
prefix so old grep targets still hit.

To read a future repro: tail /opt/skynet/console-forward-logs/console-forward.log
plus docker compose logs skynet, grep for `[diag-dormant-send]`. Cluster
by mqid. The sequence tells the story:
  arm + backend dormant-send-start + backend sentinel-dropped +
  (backend marker-fresh OR backend marker-fallback) +
  backend watchdog-arm + watchdog-arm-complete +
  (incoming-user-frame with matched_by=fifo-head-match OR
   fire OR watchdog-fire-* OR ws-paste-send-failed OR ws-send-keys-error).

Not tested beyond typecheck. Deploy ships via the next task in this plan.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

Verify the commit landed:

```
git log -1 --format='%h %s'
git show --stat HEAD | head -20
```
  </action>
  <verify>
    <automated>bash -lc "cd /home/ubuntu/skynet-tina && git log -1 --format='%s' | grep -q '^diag(pretty-view): instrument dormant-send flow' && git show --stat HEAD | grep -E '(PrettyView\.tsx|pv-send-watchdog\.ts|claude-session-server\.ts)' | wc -l | (read n; test \"\$n\" -eq 3 && echo OK:files=\$n || (echo FAIL:files=\$n; exit 1))"</automated>
  </verify>
  <done>
- Latest commit subject begins `diag(pretty-view): instrument dormant-send flow`.
- Exactly 3 files changed in that commit: PrettyView.tsx, pv-send-watchdog.ts, claude-session-server.ts.
- No test files, no other paths in the commit.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Ship — build image, force-recreate container, verify HTTPS 200 (fleet-critical prod deploy)</name>
  <what-built>
Executor runs the standard Ashley 3-step ship sequence for skynet on term.gigaashley.click. This is a fleet-critical prod deploy (CLAUDE.md: "Every change must preserve reliable browser SSH+RDP access"), so the checker verifies the surface is live before signing off.

Executor MUST run, IN ORDER, from the repo root (/home/ubuntu/skynet-tina):

1. `docker compose build skynet 2>&1 | tail -40` — Vite build + backend TS compile inside the container image. Non-zero exit fails.
2. `docker compose up -d --force-recreate skynet 2>&1 | tail -20` — recreate the container against the freshly-built image. Non-zero exit fails.
3. `sleep 8 && curl -sS -o /dev/null -w '%{http_code}\n' https://term.gigaashley.click/` — HTTPS 200 sentinel. Non-200 fails.
4. `docker compose logs --since 30s skynet 2>&1 | tail -30` — post-recreate log tail; look for a clean listen line (`server listening on ...` or similar) and absence of stack traces.
5. Optional but useful: `docker compose logs --since 30s skynet 2>&1 | grep -c '\[diag-dormant-send\]'` — if a peer instance is exercising the dormant path, log lines with the new prefix should start appearing. Zero is acceptable at this moment (no one may be sending). >0 confirms the prefix is live in the built image.

The executor pastes the four command outputs into the checkpoint response BEFORE handing off to Ashley.
  </what-built>
  <how-to-verify>
Ashley verifies:

1. Visit https://term.gigaashley.click/ in a browser — page loads normally, no white-screen, no `.map` crash.
2. Log in and open any pretty-view pane — WS reconnects, messages render.
3. In the browser devtools console, run `console.info('[diag-dormant-send] smoke-test');` and confirm the console-forward pipe carries it to `/opt/skynet/console-forward-logs/console-forward.log` (tail on the EC2 side: `tail -n 5 /opt/skynet/console-forward-logs/console-forward.log`). This proves the log-collection path Ashley will use to read a real dormant-send repro is intact.
4. (Bonus, if a dormant pane is handy) Send one message into a dormant pane. Watch console + container logs — a `[diag-dormant-send] arm ... dormant=true` line should appear on the frontend, and `[diag-dormant-send] backend dormant-send-start` should appear on the backend, both with the same mqid.

Approve if all four checks pass. Report deltas if any step fails; executor investigates before signing off.
  </how-to-verify>
  <resume-signal>Type "approved" once HTTPS 200 is confirmed and the log-forwarding smoke test round-trips, OR paste the failing output.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-console → console-forward pipe | Ashley reads production browser console logs via a forwarded log file; this patch increases the volume of forwarded lines. |
| container stdout → docker compose logs | Backend sshLogger emissions flow to container stdout; this patch adds INFO-level lines that were previously debug-only. |
| production users → live skynet | Deploying a container recreate briefly disconnects existing WS sessions — routine but worth acknowledging. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-diag-01 | Information disclosure | New log lines could leak message body contents to console/log files | mitigate | Log lines emit content LENGTH (`contentLen`, `bodyBytes`) and short PREVIEWS (first 60 chars, whitespace collapsed) — never full payload. matrixEventId and mqid are opaque identifiers (already logged elsewhere). No new PII/secret exposure beyond current baseline. |
| T-diag-02 | Denial of service | Log volume from `[diag-dormant-send]` lines could balloon container log files or console-forward buffer | accept | Emission gated to specific transition points, not per-frame or per-tick. Marker-poll loop is EXPLICITLY not instrumented per tick — only fresh/fallback exit boundaries. Estimated cost: ~10-20 additional log lines per compose-send. At Ashley's normal fleet volume this is negligible vs. existing `[pv-input] rx` (fires on every WS input frame). |
| T-diag-03 | Tampering | Deploy recreate could pick up an unintended change if working tree has uncommitted edits | mitigate | Task 2 verifies exactly 3 files in HEAD commit before Task 3 ships. `docker compose build` reads the tracked tree via COPY, not the working dir — uncommitted files are excluded by design. |
| T-diag-04 | Repudiation | This patch is diagnostic-only; a downstream fix commit must not conflate log-add with behavior change | mitigate | Task 1 explicitly forbids control-flow changes; the diag lines are purely additive. `clearAllPendingSends(reason)` signature accretion is the only near-behavior edit — default preserves prior behavior and no branch outcome depends on the reason string. |
| T-diag-05 | Elevation of privilege | Ship step requires prod docker access | accept | Executor already has fleet-management access (SSM to EC2 gives root; ~/skynet-tina is the deploy checkout). Same access surface as any other ship. |
| T-diag-SC | Tampering | npm/pip/cargo installs | n/a | This patch adds ZERO new dependencies. No package.json / package-lock.json changes. Package legitimacy gate does not apply. |
</threat_model>

<verification>
- Frontend + backend both instrumented (grep counts checked in Task 1 automated verify).
- `npm run typecheck` clean.
- Commit is a single atomic diag(...) commit touching exactly 3 tracked files.
- `docker compose build skynet` succeeds.
- `docker compose up -d --force-recreate skynet` succeeds.
- `curl -sS -o /dev/null -w '%{http_code}' https://term.gigaashley.click/` returns 200.
- Post-recreate logs show clean startup, no stack traces.
- Ashley (or a peer) confirms browser-console → console-forward pipe carries a smoke-test `[diag-dormant-send]` line.
</verification>

<success_criteria>
- After next Ashley repro of the failure mode, a single `grep '[diag-dormant-send]'` across `console-forward.log` + `docker compose logs skynet` produces a mqid-sortable timeline that answers:
  1. Which timer armed (dormant=true → 220s, or dormant=false → 20s)?
  2. Which branch produced the red bubble (client fire, ws-paste-send-failed, ws-send-keys-error, ws-close cleanup)?
  3. Did the real message's incoming user-role frame head-match a pending, or did it arrive when no pending was left (so it rendered as a fresh bubble)?
  4. Did the backend full-resend fire (Stage 2 at T+95.5s dormant), producing the duplicate real bubble?
  5. Did matrixEventId dedup fire on all three paths for one physical peer message, or did one path miss?
- No fleet outage during deploy (HTTPS 200 sustained, no WS reconnect storm beyond the routine recreate blip).
</success_criteria>

<output>
Create `.planning/quick/260903-7e8-diag-instrument-dormant-send-flow-to-cat/260903-7e8-SUMMARY.md` when done, recording:
- Line count of new [diag-dormant-send] emissions per file (frontend / watchdog / server).
- Commit SHA of the diag(...) commit.
- Build + recreate + HTTPS 200 exit codes / status.
- Any post-deploy log slice observed (bonus dormant-send exercise from Task 3 step 4, if performed).
- Handoff note for the follow-up bug-fix phase: which `[diag-dormant-send]` fields to grep for the four hypotheses A/B/C/D listed in the commit body.
</output>
