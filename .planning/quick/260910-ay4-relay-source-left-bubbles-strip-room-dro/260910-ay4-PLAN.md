---
phase: quick-260910-ay4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/speak-singleton.ts
  - src/ui/features/pretty-view/ChatMessage.tsx
  - src/ui/features/pretty-view/RelayInboundBubble.tsx
  - src/ui/features/pretty-view/RelayInboundBubble.test.tsx
  - src/ui/features/pretty-view/RelayInboundBubble.speak.test.tsx
  - src/ui/features/pretty-view/PrettyView.tsx
autonomous: true
requirements:
  - SHAPE-RELAY-INBOUND-STRIP
  - LEFT-BUBBLE-HEADER-STRIP-ROOM
  - LEFT-BUBBLE-HEADER-BRIGHTEN
  - LEFT-BUBBLE-FOOTER-REMOVE
  - LEFT-BUBBLE-SPEAK-BUTTON
  - LEFT-BUBBLE-LONG-PRESS-AUTOPLAY
  - HARNESS-VIEW-REGRESSION-FLOOR
  - SPEAK-SINGLETON-SHARED

must_haves:
  truths:
    - "RelayInboundBubble in alwaysExpanded=true mode renders header as avatar-dot + displayName with NO ' · <room>' suffix."
    - "RelayInboundBubble in alwaysExpanded=true mode renders header text at #e8e4d8 (full body-text brightness), not the 60%-alpha of body color."
    - "RelayInboundBubble in alwaysExpanded=true mode does NOT render the 'via recv.sh' footer anywhere."
    - "RelayInboundBubble in alwaysExpanded=true mode has bubble padding pl-[18px] pr-[42px] py-[14px]."
    - "RelayInboundBubble in alwaysExpanded=true mode renders a speak button positioned absolute bottom-right, visually + behaviorally identical to ChatMessage's speak button (Volume2/Loader2/Pause/Play icon states)."
    - "Long-press-on-bubble in alwaysExpanded=true mode arms autoplay via onLongPressSpeak(eventId) + fires startSpeak('long-press'), matching ChatMessage's 500ms/move-cancel/tap-suppress pattern."
    - "RelayInboundBubble in alwaysExpanded=false mode (harness view) is BYTE-FOR-BYTE unchanged: header still shows ' · <room>', footer still shows 'via recv.sh', padding stays px-[18px] py-[14px] when expanded, and NO speak button renders."
    - "Speak singleton (currentPlayer/currentOwner) is a single module-scope pair in src/ui/features/pretty-view/speak-singleton.ts imported by BOTH ChatMessage and RelayInboundBubble — tapping speak on a ChatMessage stops speak on a RelayInboundBubble and vice versa."
    - "Voice resolution: resolved sender → identity.voice, unresolved sender → undefined (default-voice fallback in postSpeakStream). Button never hidden on unresolved senders."
  artifacts:
    - path: "src/ui/features/pretty-view/speak-singleton.ts"
      provides: "Shared currentPlayer/currentOwner module-scope singleton + helpers"
      exports: ["getCurrentPlayer", "setCurrentPlayer", "getCurrentOwner", "setCurrentOwner", "clearCurrentPlayer"]
    - path: "src/ui/features/pretty-view/ChatMessage.tsx"
      provides: "Refactored to consume speak-singleton (no local module-scope currentPlayer/currentOwner)"
      contains: "from \"./speak-singleton\""
    - path: "src/ui/features/pretty-view/RelayInboundBubble.tsx"
      provides: "alwaysExpanded=true branch: room-stripped brightened header, no footer, speak button, long-press bubble, autoplay wiring"
      contains: "onLongPressSpeak"
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "Threads eventId + autoplayArmed + autoplayTargetEventId + onLongPressSpeak into RelayInboundBubble at ~line 3888"
      contains: "onLongPressSpeak={handleLongPressSpeak}"
    - path: "src/ui/features/pretty-view/RelayInboundBubble.test.tsx"
      provides: "New test cases for Truths 1-4, 7 (header shape, brightness, no footer, padding, harness regression)"
    - path: "src/ui/features/pretty-view/RelayInboundBubble.speak.test.tsx"
      provides: "New test file mirroring ChatMessage.speak.test.tsx for Truths 5, 6, 8, 9"
  key_links:
    - from: "src/ui/features/pretty-view/RelayInboundBubble.tsx"
      to: "src/ui/features/pretty-view/speak-singleton.ts"
      via: "import"
      pattern: "from \"./speak-singleton\""
    - from: "src/ui/features/pretty-view/ChatMessage.tsx"
      to: "src/ui/features/pretty-view/speak-singleton.ts"
      via: "import"
      pattern: "from \"./speak-singleton\""
    - from: "src/ui/features/pretty-view/PrettyView.tsx"
      to: "src/ui/features/pretty-view/RelayInboundBubble.tsx"
      via: "prop threading at m.type === \"relay_inbound\" render site"
      pattern: "onLongPressSpeak=\\{handleLongPressSpeak\\}"
    - from: "src/ui/features/pretty-view/RelayInboundBubble.tsx"
      to: "src/ui/api/voice-api"
      via: "postSpeakStream(text, identity.voice ?? undefined)"
      pattern: "postSpeakStream"
---

<objective>
Land batch #6 of the Phase 97 UAT polish arc: strip the harness-derived tells
from the left-side (inbound) relay bubbles when they render in relay-source
view, and add the speak/long-press-autoplay affordance the app already has on
assistant-role bubbles.

Purpose: mirror batch #5 (sender-side, HEAD 9cd61269) on the receiver side.
Left bubbles today read like log lines ("dot Tina dot roomId … via recv.sh");
after this pass they read like chat ("dot Tina", body, small speak icon
bottom-right). Harness-view rendering of the same component stays exactly as
it is today — the changes gate on the alwaysExpanded discriminator.

Output:
  - Shared speak singleton at src/ui/features/pretty-view/speak-singleton.ts
  - ChatMessage.tsx refactored onto the shared singleton (no local duplicate)
  - RelayInboundBubble.tsx: alwaysExpanded=true branch strips room + footer,
    brightens header, widens right-pad, renders speak button, wires long-press
  - PrettyView.tsx: threads eventId + autoplay + long-press props at the
    RelayInboundBubble render site (~line 3888)
  - Tests: additive cases in RelayInboundBubble.test.tsx + new
    RelayInboundBubble.speak.test.tsx (mirror of ChatMessage.speak.test.tsx)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/shapes/shape-relay-inbound-bubble-chat-native-strip.md
@src/ui/features/pretty-view/RelayInboundBubble.tsx
@src/ui/features/pretty-view/RelayInboundBubble.test.tsx
@src/ui/features/pretty-view/ChatMessage.tsx
@src/ui/features/pretty-view/ChatMessage.speak.test.tsx
@src/ui/features/pretty-view/relay-mxid-resolve.ts
@src/ui/features/pretty-view/PrettyView.tsx
</context>

<interface_context>

**Existing shape — RelayInboundBubbleProps (RelayInboundBubble.tsx):**
```ts
export type RelayInboundBubbleProps = Pick<
  RelayInboundEvent,
  "room" | "sender" | "body"
> & {
  hostId: number;
  ts?: number;
  alwaysExpanded?: boolean;
};
```

**Extended shape (this pass adds):**
```ts
export type RelayInboundBubbleProps = Pick<
  RelayInboundEvent,
  "room" | "sender" | "body"
> & {
  hostId: number;
  ts?: number;
  alwaysExpanded?: boolean;
  eventId?: string;                       // NEW — for long-press callback + autoplay match
  autoplayArmed?: boolean;                // NEW — mirrors ChatMessage
  autoplayTargetEventId?: string | null;  // NEW — mirrors ChatMessage
  onLongPressSpeak?: (eventId: string) => void;  // NEW — mirrors ChatMessage
};
```

**resolveMxidToIdentity return shape (relay-mxid-resolve.ts):**
```ts
export interface MxidResolution {
  identity: Identity | null;          // full Identity when resolved (has .voice)
  colorHue: number | null;
  displayName: string;
}
```
Voice access: `resolveMxidToIdentity(sender, byKey).identity?.voice ?? null`.
Then pass `identity?.voice ?? undefined` to `postSpeakStream` (undefined =
default voice per voice-api contract).

**ChatMessage speak apparatus surface (ChatMessage.tsx lines 100–330 / 596–683):**
- Module-scope singleton pair: `currentPlayer: WebAudioStreamPlayer | null`
  and `currentOwner: symbol | null` (lines 21–22). THIS PASS EXTRACTS THIS
  PAIR to speak-singleton.ts.
- Per-bubble refs: `bubbleIdRef` (unique symbol per bubble),
  `longPressTimerRef`, `longPressFiredRef`, `pointerStartRef`,
  `autoplayLastFiredRef`, `containerRef`.
- State: `speakState: "idle" | "loading" | "playing" | "paused"`.
- Handlers: `startSpeak(trigger)`, `onSpeakClick(e)`, autoplay `useEffect`
  keyed on `[autoplayTargetEventId, eventId, isUser]`.
- Long-press wiring on the speak button: `onPointerDown` sets a 500ms
  timeout; on fire → `onLongPressSpeak(eventId)` + `startSpeak("long-press")`
  + sets `longPressFiredRef` to suppress the subsequent click.
  `onPointerMove` cancels the timer if pointer travels > 10px.
  `onPointerCancel`/`onPointerUp` clear the timer. `onClick` reads
  `longPressFiredRef` and short-circuits if a long-press already fired.

**PrettyView render site (PrettyView.tsx ~line 3888):**
```tsx
) : m.type === "relay_inbound" ? (
  <RelayInboundBubble
    room={m.room}
    sender={m.sender}
    body={m.body}
    ts={m.ts}
    hostId={hostId}
    alwaysExpanded={source.kind === "relay"}
  />
```
Extend with `eventId={m.eventId}`, `autoplayArmed={autoplayArmed}`,
`autoplayTargetEventId={autoplayTargetEventId}`,
`onLongPressSpeak={handleLongPressSpeak}`. All four handlers/state
already exist in PrettyView (lines 940, 941, 1002) — no new state needed,
just prop threading.

**RelayInboundEvent shape (claude-session-api.ts line 215):** has
`eventId: string` — safe to pass `m.eventId` at the render site.
</interface_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract shared speak-singleton, refactor ChatMessage onto it</name>
  <files>
    src/ui/features/pretty-view/speak-singleton.ts,
    src/ui/features/pretty-view/ChatMessage.tsx,
    src/ui/features/pretty-view/ChatMessage.speak.test.tsx
  </files>
  <behavior>
    - The existing ChatMessage.speak.test.tsx suite (Tests 1–5 + cross-bubble preempt) MUST continue to pass unchanged in intent — the extraction is a pure refactor of module-scope to a sibling module.
    - Cross-bubble preempt still works: mounting a second ChatMessage, clicking its speak, causes `currentPlayer.stop()` to fire on the first bubble's player (single-owner invariant preserved).
    - Module-scope `currentPlayer` / `currentOwner` disappear from ChatMessage.tsx; ChatMessage instead reads/writes them via the singleton module's accessor helpers.
    - The singleton module exports the accessor surface that RelayInboundBubble will consume in Task 2.
  </behavior>
  <action>
    Create `src/ui/features/pretty-view/speak-singleton.ts`. Move the two
    module-scope bindings out of ChatMessage.tsx (currently ChatMessage.tsx
    lines 21–22):

      let currentPlayer: WebAudioStreamPlayer | null = null;
      let currentOwner: symbol | null = null;

    into the new module. Expose them via accessor helpers so ChatMessage and
    RelayInboundBubble can share the same singleton without either module
    re-declaring it. Suggested exported surface:
      - `getCurrentPlayer(): WebAudioStreamPlayer | null`
      - `setCurrentPlayer(p: WebAudioStreamPlayer | null): void`
      - `getCurrentOwner(): symbol | null`
      - `setCurrentOwner(o: symbol | null): void`
      - `clearCurrentPlayer(): void`  // sets both to null in one call

    Import `WebAudioStreamPlayer` type from `./webAudioStreamPlayer` (it is
    already exported there — grep confirmed).

    In ChatMessage.tsx:
      - Delete the two `let currentPlayer` / `let currentOwner` declarations
        at the top of the file.
      - Add `import { getCurrentPlayer, setCurrentPlayer, getCurrentOwner,
        setCurrentOwner, clearCurrentPlayer } from "./speak-singleton";`
      - Replace every read of `currentPlayer` / `currentOwner` with the
        getter equivalent; replace every assignment with the setter equivalent
        (or `clearCurrentPlayer()` for the paired-to-null case). Preserve
        every existing log line and every race-check branch — the goal is
        byte-for-byte behavioral equivalence, the ONLY change is that the
        two variables now live in a sibling module.
      - Sites to update inside ChatMessage.tsx: the unmount cleanup effect
        (~line 122), the top of `startSpeak` (cross-bubble preempt branch
        ~line 146), the `player.play` install (~lines 207–208), the
        `onEnded` / `onError` callbacks (~lines 167, 182), the post-fetch
        race check (~line 216), the outer catch (~line 261), and both
        pause/resume branches inside `onSpeakClick` (~lines 275, 284).

    Do NOT introduce a React context, a hook wrapper, or an event bus. This
    is deliberately still a mutable module-scope pair — that's the pattern
    the existing cross-bubble preempt relies on. The extraction is purely
    to give RelayInboundBubble a place to import the same pair from.

    Run `npm test -- ChatMessage.speak.test.tsx` and confirm every test in
    that file still passes (this is the regression floor for the refactor).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-taylor && npm test -- --run src/ui/features/pretty-view/ChatMessage.speak.test.tsx 2>&1 | tail -40</automated>
  </verify>
  <done>
    speak-singleton.ts exists and exports the accessor surface listed above.
    ChatMessage.tsx no longer contains `let currentPlayer` or `let currentOwner`
    at module scope; all references go through the singleton module.
    ChatMessage.speak.test.tsx passes 100% (no test edits — this is a
    refactor, not a behavioral change).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strip room + footer + brighten header + add speak/long-press to RelayInboundBubble's alwaysExpanded branch, thread props in PrettyView</name>
  <files>
    src/ui/features/pretty-view/RelayInboundBubble.tsx,
    src/ui/features/pretty-view/RelayInboundBubble.test.tsx,
    src/ui/features/pretty-view/RelayInboundBubble.speak.test.tsx,
    src/ui/features/pretty-view/PrettyView.tsx
  </files>
  <behavior>
    Truth 1 (test): with alwaysExpanded=true, the header element for a
      resolved sender (e.g. Tina) contains "Tina" but does NOT contain any
      "·" separator followed by the room string. Room "!roomAlias:server.tld"
      must not appear inside the header element.

    Truth 2 (test): with alwaysExpanded=true, the header element's className
      contains the token `text-[#e8e4d8]` (or an equivalent bright brand
      class — the point is that it MATCHES the body text color, not the
      rgba(232,228,216,0.6) 60%-alpha the current header uses).

    Truth 3 (test): with alwaysExpanded=true, `screen.queryByText(/via recv\.sh/)`
      is null. The footer text is not rendered anywhere in the bubble.

    Truth 4 (test): with alwaysExpanded=true, the bubble root element has
      className tokens `pl-[18px]`, `pr-[42px]`, `py-[14px]` (the widened
      right-pad reserves the speak-button gutter, mirroring ChatMessage
      assistant's `pr-[42px]`).

    Truth 5 (test — speak.test.tsx): with alwaysExpanded=true, exactly one
      speak button renders (queryByLabelText(/speak message/i)); with
      alwaysExpanded=false, zero speak buttons render. Icon states
      transition idle→loading→playing→paused as click sequences execute
      (mirror ChatMessage.speak.test.tsx Tests 3, 4, 5).

    Truth 6 (test — speak.test.tsx): with alwaysExpanded=true, a pointerDown
      on the BUBBLE (data-testid="relay-inbound-bubble") followed by
      vi.advanceTimersByTime(500) fires `onLongPressSpeak(eventId)` with the
      passed eventId AND kicks off `postSpeakStream` (startSpeak("long-press")).
      A subsequent tap-click on the same bubble is suppressed
      (longPressFiredRef semantics).

    Truth 7 (test): with alwaysExpanded=false (default / harness view), the
      existing test cases (C1–C5 + Tests 1–6 + Test 6c–6d) all still pass —
      header still shows " · <room>", footer still shows "via recv.sh",
      expanded padding is still `px-[18px] py-[14px]` (NOT the new
      pl/pr split), and `queryByLabelText(/speak message/i)` is null.
      This is the regression floor.

    Truth 8 (test — speak.test.tsx): mount a ChatMessage(assistant) and a
      RelayInboundBubble(alwaysExpanded=true) in the same render; click
      speak on the ChatMessage, then click speak on the RelayInboundBubble
      — `mockStop` (from the shared singleton preempt path) is called at
      least once between the two clicks. Same singleton, one owner at a
      time.

    Truth 9 (test — speak.test.tsx): when the sender resolves to an
      identity with `voice: "sarah"`, clicking speak calls
      `postSpeakStream(anyText, "sarah")`. When the sender does NOT
      resolve, clicking speak calls `postSpeakStream(anyText, undefined)`.
      The button IS present in both cases (never hidden).
  </behavior>
  <action>
    ### RelayInboundBubble.tsx changes

    (1) Extend `RelayInboundBubbleProps` with the four new optional fields
    documented in <interface_context> above: `eventId?: string`,
    `autoplayArmed?: boolean`, `autoplayTargetEventId?: string | null`,
    `onLongPressSpeak?: (eventId: string) => void`. All four remain optional
    so the harness-view render site (which does not thread them) keeps
    compiling untouched.

    (2) Destructure the four new props with defaults matching ChatMessage:
    `autoplayArmed = false`, `autoplayTargetEventId = null`,
    `onLongPressSpeak` and `eventId` left as-passed. Preserve the existing
    `alwaysExpanded = false` default.

    (3) Add imports at the top:
      - From `lucide-react`: `Volume2, Loader2, Pause, Play` (mirror
        ChatMessage line 4).
      - From `@/api/voice-api` (per project convention — same path
        ChatMessage uses): `postSpeakStream`.
      - From `./webAudioStreamPlayer`: `createWebAudioStreamPlayer` +
        the `WebAudioStreamPlayer` type.
      - From `./speak-singleton` (Task 1's new module):
        `getCurrentPlayer`, `setCurrentPlayer`, `getCurrentOwner`,
        `setCurrentOwner`, `clearCurrentPlayer`.

    (4) Change the identity resolution call from destructuring only
    `colorHue, displayName` to also pulling `identity`:
      `const { colorHue, displayName, identity } = resolveMxidToIdentity(sender, byKey);`
    Compute `const identityVoice: string | undefined = identity?.voice ?? undefined;`

    (5) Add the speak-state machine + refs + handlers INSIDE the component,
    gated so they only fire when `alwaysExpanded` is true (the harness view
    must remain byte-for-byte unchanged). Structurally: keep the existing
    top-of-component identity/hue/bubbleStyle/pointer/fetchState block for
    both branches; ADD (only when alwaysExpanded=true is the effective
    render mode — see below) the speak plumbing:
      - `bubbleIdRef = useRef(Symbol("relay-speak-bubble"))`
      - `speakState = useState<"idle" | "loading" | "playing" | "paused">("idle")`
      - `containerRef = useRef<HTMLDivElement>(null)` on the bubble root
        (for `.innerText` extraction; same pattern as ChatMessage)
      - `longPressTimerRef`, `longPressFiredRef`, `pointerStartRef`,
        `autoplayLastFiredRef` refs (identical setup to ChatMessage)
      - Unmount cleanup effect: if this bubble owns the singleton
        (`getCurrentOwner() === bubbleIdRef.current`), call
        `getCurrentPlayer()?.stop()` and `clearCurrentPlayer()`. Also
        clear any pending long-press timer. Same-shaped effect as
        ChatMessage.tsx lines 122–136.
      - `startSpeak(trigger)` — copy the ChatMessage implementation
        verbatim from lines 143–267, substituting the singleton accessors
        for the direct variable reads and setting the text extraction
        source to `containerRef.current?.innerText ?? body`. Pass
        `identityVoice` (computed in step 4) into `postSpeakStream`.
        Log lines: keep them but prefix owner id with "relay:" for
        observability (Ashley preference — cheap tag lets logs
        disambiguate ChatMessage vs RelayInboundBubble owners).
      - `onSpeakClick(e)` — copy ChatMessage lines 269–292 verbatim,
        substituting singleton accessors as above.
      - Autoplay useEffect — copy ChatMessage lines 297–310 verbatim,
        substituting `isUser` with `false` (relay inbound bubbles are
        always non-self peer-side, so the `!isUser` guard is trivially
        true). Effect deps: `[autoplayTargetEventId, eventId]`.

    Do NOT re-implement the state machine, the WebAudioStreamPlayer
    creation, the cross-bubble preempt logic, the same-bubble pause/resume
    logic, or the log-line format. Every one of those must be the same
    code path as ChatMessage, differing only in which module hosts the
    singleton (Task 1 solved that) and which text source feeds
    postSpeakStream.

    (6) Render tree changes — gated ENTIRELY on `alwaysExpanded`:

    Set `containerRef` on the bubble root `<div>` (the one with
    `data-testid="relay-inbound-bubble"`). Also gate padding on
    `alwaysExpanded`:
      - `alwaysExpanded=true`: `"pl-[18px] pr-[42px] py-[14px]"`
      - `alwaysExpanded=false && collapsed`: `"px-[12px] py-[7px]"` (unchanged)
      - `alwaysExpanded=false && !collapsed`: `"px-[18px] py-[14px]"` (unchanged)

    In the header block: the existing `alwaysExpanded ? <div> : <button>`
    branch discriminator already gates the header. Modify ONLY the
    `<div>` (alwaysExpanded=true) branch:
      - Header className: replace `text-[rgba(232,_228,_216,_0.6)]` with
        `text-[#e8e4d8]` (matches the body text color one line down at
        line 160 of the current file — Truth 2).
      - Header content: change `{displayName} · {room}` to `{displayName}`.
        Delete the ` · {room}` fragment. NOTHING after the name. (Truth 1.)

    Leave the `<button>` branch (alwaysExpanded=false, harness view) UNTOUCHED.
    Every existing test that depends on " · {room}" in the harness header
    (Test 1's raw-mxid display, C1's displayName+room visibility, etc.)
    continues to pass. This is the regression floor.

    In the body-and-footer conditional block:
      - Wrap the footer `<div>` with `via recv.sh` in a `{!alwaysExpanded && (...)}`
        guard so the footer renders ONLY in the harness view. (Truth 3.)
      - The rest of the body block (pointer detection, fetch states, inline
        body render) is UNCHANGED and remains inside `{!collapsed && (...)}`.
        Note: with `alwaysExpanded=true`, `collapsed` starts false and
        stays false (no toggle button exists), so the body always renders.

    (7) Speak button JSX — add a new sibling INSIDE the bubble root, AFTER
    the body block, GATED on `alwaysExpanded`. Copy the entire `<button>`
    JSX from ChatMessage.tsx lines 596–683 verbatim. It already handles:
    Volume2/Loader2/Pause/Play icon state, `onPointerDown` 500ms long-press
    timer, `onPointerMove` >10px cancel, `onPointerUp` timer clear,
    `onClick` tap-suppress via `longPressFiredRef`, `aria-label` for each
    state, position: absolute bottom-right (right:6, bottom:6), autoplay-
    armed color treatment via `hsla(var(--pv-id-hue),...)`.

    Two adaptations required:
      - The `autoplayArmed` background/border expressions in ChatMessage
        use `hsla(var(--pv-id-hue),...)` — that CSS var is set at the pane
        chrome level. In relay-source view it is still set (source.kind =
        "relay" panes still carry the pane's identity hue). Keep the
        expressions as-is; no substitution needed.
      - The `!isUser &&` outer gate in ChatMessage becomes an
        `alwaysExpanded &&` gate here. (Harness-view relay bubbles
        deliberately do NOT get a speak button — Truth 7 regression floor.)

    (8) Long-press-on-bubble (Truth 6). Beyond the long-press on the speak
    button (copied verbatim in step 7), the shape file also asks for
    long-press ANYWHERE on the bubble to arm autoplay. To do this cheaply
    and without duplicating handlers: attach `onPointerDown`, `onPointerMove`,
    `onPointerCancel`, `onPointerUp` to the bubble root `<div>` (the one
    with `data-testid="relay-inbound-bubble"`) — but ONLY when
    `alwaysExpanded=true`. The handlers share the same refs
    (longPressTimerRef, longPressFiredRef, pointerStartRef) as the
    speak-button handlers. On 500ms timer fire, call
    `onLongPressSpeak?.(eventId ?? "")` + `void startSpeak("long-press")`.

    Do NOT attach these handlers on the speak-button branch (the button's
    own handlers already cover long-press on the button itself, and
    React event bubbling means a button-child pointerDown also fires the
    bubble-root pointerDown — the shared refs make this a no-op double-arm
    rather than a bug, but we prefer intentional single-source-of-truth).
    Concretely: give the speak button its own `onPointerDown` (already
    present from step 7), and put a SEPARATE `onPointerDown` on the bubble
    root that early-returns if the pointer target is or is inside the
    speak button. The simplest check: `if ((e.target as HTMLElement)?.closest('.pv-speak-btn')) return;`

    (9) `data-pv-bubble` marker — add `data-pv-bubble` attribute to the
    bubble root when `alwaysExpanded=true` so scroll/observer hooks that
    already exist and key on that attribute uniformly include relay-source
    inbound bubbles too. (Grep first to confirm the attribute is used
    that way; if not, skip this — add nothing speculative.)

    ### PrettyView.tsx changes

    At the `m.type === "relay_inbound"` render site (currently ~lines
    3888–3896), extend the props being passed to `RelayInboundBubble`:

      <RelayInboundBubble
        room={m.room}
        sender={m.sender}
        body={m.body}
        ts={m.ts}
        hostId={hostId}
        alwaysExpanded={source.kind === "relay"}
        eventId={m.eventId}                                        // NEW
        autoplayArmed={autoplayArmed}                              // NEW
        autoplayTargetEventId={autoplayTargetEventId}              // NEW
        onLongPressSpeak={handleLongPressSpeak}                    // NEW
      />

    All four of those are already in scope at that render site — they are
    threaded into the sibling `<ChatMessage>` at line ~3906 immediately
    below. Copy the exact same prop-passing pattern. No new state, no new
    handlers, no new callbacks — just prop threading.

    ### Tests — RelayInboundBubble.test.tsx (existing file, ADD cases)

    Add the following new `it(...)` cases at the end of the `describe`
    block, ALL guarded on `alwaysExpanded={true}`:

      - "alwaysExpanded: header does NOT contain the room string
        (room-strip)" — Truth 1. Render with room="!R:s", assert the header
        element's `textContent` does not include "!R:s" and does not
        include " · ". Sender's displayName IS in the header.
      - "alwaysExpanded: header text color matches body (#e8e4d8 brightness)"
        — Truth 2. Assert `header.className` contains `text-[#e8e4d8]` and
        does NOT contain the 60%-alpha token
        `text-[rgba(232,_228,_216,_0.6)]`.
      - "alwaysExpanded: no 'via recv.sh' footer rendered" — Truth 3.
        Assert `screen.queryByText(/via recv\.sh/)` is null.
      - "alwaysExpanded: bubble padding is pl-[18px] pr-[42px] py-[14px]" —
        Truth 4. Assert `bubble.className` contains all three tokens; does
        NOT contain `px-[18px]` (the harness-expanded pad).
      - "alwaysExpanded=false regression floor: header still shows
        ' · <room>' AND footer still says 'via recv.sh' AND padding is
        the pre-existing px-[18px] py-[14px] once expanded AND no speak
        button" — Truth 7. This is the byte-for-byte-unchanged guard.
        Assert `screen.getByText(/@tina:matrix\.example\.com · !roomAlias/)`
        after clicking to expand (or a more forgiving matcher — the
        important part is that a ` · ` separator + room string is present).
        Assert `screen.getByText(/via recv\.sh/)` renders after expand.
        Assert `bubble.className` contains `px-[18px]` + `py-[14px]` when
        expanded and does NOT contain `pr-[42px]`. Assert
        `screen.queryByLabelText(/speak message/i)` is null.

    Every existing test in RelayInboundBubble.test.tsx (Tests 1–6, 6c–6d,
    C1–C5, plus the pair of alwaysExpanded tests at lines 375–416) MUST
    continue to pass unchanged. Do not modify or delete any existing test.

    ### Tests — RelayInboundBubble.speak.test.tsx (NEW file)

    Mirror the structure of `ChatMessage.speak.test.tsx` (which lives at
    src/ui/features/pretty-view/ChatMessage.speak.test.tsx and passed
    verbatim through Task 1's refactor). Reuse the same mock pattern:
      - `vi.mock("@/api/voice-api", ...)` with `postSpeakStream` returning
        a small streaming Response.
      - `vi.mock("./webAudioStreamPlayer", ...)` returning
        `{ play, stop, pause, resume }` spies.
      - Mock `@/state/identities-store` (already done in
        RelayInboundBubble.test.tsx — copy that setup).

    Cases:
      - "alwaysExpanded=true renders exactly one speak button" — Truth 5.
      - "alwaysExpanded=false renders NO speak button" — Truth 7 partial.
      - "clicking speak calls postSpeakStream with the bubble body text" —
        Truth 5. Assert `mockedPostSpeakStream` called at least once after
        `fireEvent.click(screen.getByLabelText(/speak message/i))`.
      - "loading state renders Loader2; after resolution renders Pause" —
        Truth 5 (mirror ChatMessage.speak.test.tsx Test 4).
      - "resolved sender → postSpeakStream(text, identity.voice)" — Truth 9.
        Mock `useIdentities` to return an identity with `voice: "sarah"`
        for @tina. Click speak. Assert the second argument to
        `mockedPostSpeakStream` was `"sarah"`.
      - "unresolved sender → postSpeakStream(text, undefined) + button
        still present" — Truth 9. Mock `useIdentities` empty. Click speak.
        Assert `mockedPostSpeakStream` called with second arg
        `undefined` AND the speak button was found (not hidden).
      - "long-press on bubble root fires onLongPressSpeak(eventId) and
        starts speak; tap-click is suppressed" — Truth 6. Use fake timers:
        `vi.useFakeTimers()`. `fireEvent.pointerDown(bubbleEl, {clientX:0, clientY:0})`,
        `vi.advanceTimersByTime(500)`, then `fireEvent.pointerUp(bubbleEl)`
        and `fireEvent.click(bubbleEl)`. Assert `onLongPressSpeak` mock
        was called with the passed eventId; assert `mockedPostSpeakStream`
        was called (long-press started speak); assert `onSpeakClick`
        did NOT double-fire the speak (only one `postSpeakStream` call).
      - "cross-bubble singleton: speak on RelayInboundBubble stops speak
        on ChatMessage" — Truth 8. Mount both in the same render tree.
        Click speak on ChatMessage first (advance any needed ticks),
        then click speak on the RelayInboundBubble. Assert `mockStop`
        was called between the two — i.e., the second click's preempt
        branch fired.

    Use `data-testid="relay-inbound-bubble"` as the pointer-event target
    for the long-press test. Use `getByLabelText(/speak message/i)` for
    the speak button.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-taylor && npm test -- --run src/ui/features/pretty-view/RelayInboundBubble.test.tsx src/ui/features/pretty-view/RelayInboundBubble.speak.test.tsx src/ui/features/pretty-view/ChatMessage.speak.test.tsx 2>&1 | tail -80</automated>
  </verify>
  <done>
    (1) RelayInboundBubble.tsx: alwaysExpanded=true header shows only
        avatar-dot + displayName at #e8e4d8 brightness; no room, no footer;
        padding is pl-[18px] pr-[42px] py-[14px]; a speak button renders
        bottom-right with all four icon states; long-press on the bubble
        arms autoplay via onLongPressSpeak(eventId) + fires startSpeak.
    (2) RelayInboundBubble.tsx alwaysExpanded=false branch: byte-for-byte
        unchanged rendering (header ' · <room>', footer 'via recv.sh',
        px-[18px] py-[14px] expanded padding, no speak button).
    (3) PrettyView.tsx line ~3888: four new props threaded
        (eventId, autoplayArmed, autoplayTargetEventId, onLongPressSpeak).
    (4) RelayInboundBubble.test.tsx: 5 new cases (Truths 1–4, 7) added; all
        pre-existing tests still pass.
    (5) RelayInboundBubble.speak.test.tsx: new file with the 8 cases
        listed above (Truths 5, 6, 8, 9); all pass.
    (6) ChatMessage.speak.test.tsx: still passes 100% (regression floor
        from Task 1's refactor holds).
    (7) The speak singleton is a SINGLE module-scope pair (Task 1's
        speak-singleton.ts) shared by both bubbles — no second parallel
        singleton was introduced in RelayInboundBubble.tsx.
  </done>
</task>

</tasks>

<verification>
Scoped green gate (per fleet directive: executor runs SCOPED tests, not
full suite):

  cd /home/ubuntu/skynet-taylor && npm test -- --run \
    src/ui/features/pretty-view/RelayInboundBubble.test.tsx \
    src/ui/features/pretty-view/RelayInboundBubble.speak.test.tsx \
    src/ui/features/pretty-view/ChatMessage.speak.test.tsx

All three files must pass 100%. On green, commit and stop — orchestrator
handles deploy.

TypeScript must also compile with no new errors introduced by the props
extension:

  cd /home/ubuntu/skynet-taylor && npx tsc --noEmit 2>&1 | tail -20

(Any pre-existing errors unrelated to the touched files are not
regressions of this pass; they get flagged to the orchestrator, not fixed.)
</verification>

<success_criteria>
- Shape file's every promised change is present in code:
  * header stripped of room + brightened
  * footer removed
  * speak button (idle/loading/playing/paused) bottom-right
  * long-press on bubble arms autoplay
  * voice resolution uses identity.voice with default-voice fallback for unresolved
- Shape file's every "would make it wrong" is guarded by a test:
  * "who's talking" affordance kept (avatar dot + name still there)
  * speak button never appears on user's own outgoing bubbles (relay outbound is a different component, untouched; relay inbound button only fires when alwaysExpanded=true which is the peer-side rendering path)
  * harness-view rendering of relay bubbles UNCHANGED (regression-floor tests)
  * speak mechanism reused, not re-implemented (shared singleton, shared player factory, shared state machine)
  * unresolved sender → speak button still tappable, falls back to default voice (test asserts undefined second arg)
- Scoped test set (RelayInboundBubble.test + RelayInboundBubble.speak.test
  + ChatMessage.speak.test) is 100% green.
- No git worktrees, no git stash — all work on branch
  feat/tab-title-from-tmux in /home/ubuntu/skynet-taylor.
</success_criteria>

<output>
Create `.planning/quick/260910-ay4-relay-source-left-bubbles-strip-room-dro/260910-ay4-SUMMARY.md` when done — brief bullet list of:
  - files touched
  - test counts added / passing
  - the commit SHA that landed the pass
  - any deviations from the plan (should be zero — this is a tightly-shaped pass)
</output>
