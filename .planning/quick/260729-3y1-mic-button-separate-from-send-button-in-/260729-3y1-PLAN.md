---
phase: quick-260729-3y1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/MicButton.tsx
  - src/ui/features/pretty-view/ComposeBox.tsx
  - src/ui/features/pretty-view/ComposeBox.voice.test.tsx
autonomous: true
requirements:
  - QUICK-260729-3Y1
must_haves:
  truths:
    - "MicButton and Send button both render inside the textarea at the same time when voice.state==='idle' and no other guard blocks them (empty text, non-empty text, and attachments-present states all show both)."
    - "MicButton renders to the LEFT of the Send button (at right-11 bottom-0.5, one 40px hit-target width away from the right-1 anchor)."
    - "When voice.state==='recording', RecordingControls still owns the slot alone — neither MicButton nor Send renders."
    - "When asideActive=true, MicButton is hidden and the Send button morphs to X-for-Resume (unchanged behavior)."
    - "When queueArmed=true, MicButton is hidden (covered by overlay) and Send is hidden by the same overlay (unchanged behavior)."
    - "When voice.state==='transcribing', MicButton is hidden (voice.state guard) and Send renders disabled (patch #181 press-feedback intact)."
    - "MicButton with no positionClass prop still renders at the default 'right-1 bottom-0.5' anchor (backward-compatible default)."
    - "ComposeBox.voice.test.tsx assertions that MicButton is HIDDEN when text is non-empty or attachments are present have been flipped to expect MicButton visible; the recording-state assertions are unchanged."
    - "A new test 'MicButton and Send button both render together when idle + text non-empty' asserts both buttons coexist in the textarea slot."
  artifacts:
    - path: "src/ui/features/pretty-view/MicButton.tsx"
      provides: "MicButton with optional positionClass? prop, default 'right-1 bottom-0.5'"
    - path: "src/ui/features/pretty-view/ComposeBox.tsx"
      provides: "showMicButton without text-length or hasAttachments gates; refactored render structure that co-renders MicButton (left of Send at right-11 bottom-0.5) alongside Send/X-for-Resume"
    - path: "src/ui/features/pretty-view/ComposeBox.voice.test.tsx"
      provides: "Flipped assertions for Tests 2 and (any attachments test) + new co-render test asserting MicButton and Send both visible with non-empty text"
  key_links:
    - from: "src/ui/features/pretty-view/ComposeBox.tsx (send-button slot at ~L1670-1753)"
      to: "src/ui/features/pretty-view/MicButton.tsx MicButtonProps.positionClass"
      via: "positionClass='right-11 bottom-0.5' pass-through when ComposeBox renders MicButton alongside Send"
      pattern: "positionClass=\"right-11 bottom-0.5\""
    - from: "src/ui/features/pretty-view/ComposeBox.tsx showMicButton predicate (~L1007-1014)"
      to: "voice.state / asideActive / queueArmed / mediaDevices guards"
      via: "predicate keeps mediaDevices + voice.state==='idle' + !asideActive + !queueArmed; drops text.trim().length===0 and !hasAttachments"
      pattern: "showMicButton\\s*=[\\s\\S]{0,300}voice\\.state === \"idle\"[\\s\\S]{0,200}!asideActive[\\s\\S]{0,200}!queueArmed"
---

<objective>
Split the composebox microphone button off the send-button slot so both buttons render together inside the textarea when voice.state==='idle'. MicButton anchors to the LEFT of Send (one 40px hit-target width away) instead of swap-in-single-slot. Voice recording UX and Send behavior are otherwise unchanged.

Purpose: Ashley wants to be able to see and tap the mic regardless of textarea contents (typed text or attachments); today the mic hides the moment the textarea is dirty, forcing her to clear input before starting voice capture. The recording-state UI, hook wiring, glue rule, and Send press-feedback (patch #181) stay identical — this is a render-structure + gate-relaxation patch only.

Output: MicButton gains an optional positionClass prop; ComposeBox's showMicButton loses the text-length and hasAttachments gates; the send-button-slot ternary becomes "RecordingControls owns the slot, else Send/X renders AND MicButton co-renders when its guards allow." Tests flipped + one new co-render test added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

@src/ui/features/pretty-view/MicButton.tsx
@src/ui/features/pretty-view/ComposeBox.tsx
@src/ui/features/pretty-view/ComposeBox.voice.test.tsx
@src/ui/features/pretty-view/useVoiceRecording.ts
@src/ui/features/pretty-view/RecordingControls.tsx

Locked design (already settled with Ashley — do NOT relitigate):
- showMicButton (ComposeBox.tsx ~L1007-1014) currently gates on six conditions.
  KEEP: mediaDevices, voice.state==="idle", !asideActive, !queueArmed.
  REMOVE: text.trim().length===0, !hasAttachments.
- Render structure (ComposeBox.tsx ~L1670-1753) currently: ternary picks-one
  (RecordingControls : MicButton : Send/X). AFTER: RecordingControls still owns
  the slot alone when recording; ELSE render Send/X (unchanged) AND, when
  showMicButton, ALSO render MicButton positioned left of Send.
- MicButton.tsx currently hardcodes "absolute right-1 bottom-0.5" in its className.
  AFTER: accept optional positionClass?: string prop; default to
  "right-1 bottom-0.5" (backward compat for any non-ComposeBox caller —
  currently ComposeBox is the only caller). ComposeBox passes
  "right-11 bottom-0.5" so mic sits ~40px left of the send anchor (one 40px
  p-2 hit-target width away).
- Send button styling, X-for-Resume morph, transcribing-disabled behavior:
  UNCHANGED. Patch #181's press-feedback stays intact.
- useVoiceRecording, handleVoiceAppend, handleVoiceSend, RecordingControls:
  UNCHANGED. applyGlue at useVoiceRecording.ts:158 already handles non-empty
  currentText by inserting a single space when currentText doesn't end in
  whitespace.

Truth table after change (from locked_design):
| State                      | MicButton | Send/X                        |
|----------------------------|-----------|-------------------------------|
| idle, empty text           | shown     | shown (disabled — nothing to send) |
| idle, w/ text              | shown     | shown (enabled)               |
| idle, w/ attachments       | shown     | shown (enabled)               |
| aside active               | hidden    | X-for-Resume (unchanged)      |
| queue armed                | hidden by overlay | hidden by overlay     |
| recording                  | hidden (RecordingControls owns slot) | hidden |
| transcribing               | hidden (voice.state guard) | shown (disabled) |

Type-check discipline (Ashley 2026-07-27 patch #154 lesson): `verify` runs
`npx tsc --noEmit` from ~/skynet, NOT `npm run build:backend` — this patch
touches src/ui only, no backend/wire type changes.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add optional positionClass prop to MicButton with backward-compat default</name>
  <files>src/ui/features/pretty-view/MicButton.tsx</files>
  <behavior>
    - MicButton with no positionClass prop still renders at "absolute right-1 bottom-0.5" (existing call sites, if any, are unaffected — currently ComposeBox is the only caller).
    - MicButton with positionClass="right-11 bottom-0.5" renders at "absolute right-11 bottom-0.5" (mic slot 40px left of Send).
    - Prop is optional; type is `positionClass?: string`.
  </behavior>
  <action>
    Extend MicButtonProps interface to add `positionClass?: string` (optional, default "right-1 bottom-0.5"). Rewrite the className so `absolute` and the position tokens come from a variable — e.g. destructure `positionClass = "right-1 bottom-0.5"` in the function signature, then use `cn("absolute", positionClass, "p-2", ...rest)` for the button's className. Every other class token (p-2, text colors, disabled text, transition, active:scale-95, cursor-pointer) MUST remain byte-identical to the current file. Do NOT change the icon, aria-label defaulting, onClick semantics, or the Mic size-6 rendering. Comment banner above the component may stay; if you touch it, keep the "backward-compat default" note explicit so future readers know the position is now caller-controlled. Do NOT modify ComposeBox in this task — it's the caller-side change in Task 2.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx tsc --noEmit 2>&1 | tail -30</automated>
  </verify>
  <done>
    MicButton.tsx compiles under `npx tsc --noEmit` with no new errors. MicButtonProps exports `positionClass?: string`. The className template string references the positionClass variable instead of the hardcoded "right-1 bottom-0.5" literal. Default value is "right-1 bottom-0.5" so existing zero-argument callers get byte-identical output.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Relax showMicButton gates and co-render MicButton beside Send in ComposeBox</name>
  <files>src/ui/features/pretty-view/ComposeBox.tsx</files>
  <behavior>
    - showMicButton evaluates to true when: mediaDevices present, voice.state==='idle', !asideActive, !queueArmed. Text length and attachments no longer factor in.
    - When voice.state==='recording', RecordingControls still renders alone in the slot (MicButton and Send both absent) — matches existing behavior.
    - When NOT recording, the existing Send/X-for-Resume button renders (all its current attributes preserved), AND, when showMicButton is true, MicButton ALSO renders in the same slot container with positionClass="right-11 bottom-0.5".
    - When asideActive=true, Send morphs to X-for-Resume and MicButton is hidden (asideActive guard is still in showMicButton).
    - When voice.state==='transcribing', MicButton is hidden and Send renders disabled (patch #181 press-feedback intact).
  </behavior>
  <action>
    Two edits inside ComposeBox.tsx:

    Edit A — showMicButton predicate (~L1007-1014): remove the `text.trim().length === 0` clause AND the `!hasAttachments` clause. Final predicate keeps exactly four conditions: mediaDevices typeof-guard + mediaDevices != null + voice.state === "idle" + !asideActive + !queueArmed. Update the surrounding comment block (L995-1006) to reflect the new truth table — specifically strike the "idle + empty text + not morphed/queued/attached" phrasing and replace with "idle + not morphed/queued (text and attachments no longer gate)". Do NOT touch showRecordingControls or showTranscribingSend definitions.

    Edit B — render structure (~L1670-1753): convert the picks-one ternary into a picks-one-plus-mic pattern. The outer `showRecordingControls ? <RecordingControls .../> : <picks Send-or-MicOnly>` branch is replaced by `showRecordingControls ? <RecordingControls .../> : <>...Send-or-X always...MicButton conditionally...</>`. Concretely:

      - If showRecordingControls: render `<RecordingControls .../>` alone (unchanged).
      - Else: render a React fragment containing (1) the existing Send/X-for-Resume `<button>` block (byte-identical — same onClick, same disabled predicate `asideActive ? false : (sendDisabled || showTranscribingSend)`, same aria-label/title morph, same className with the asideActive-hue branching, same SVG paper-plane vs lucide X children) AND (2) `{showMicButton && <MicButton onClick={voice.start} title="Record voice" positionClass="right-11 bottom-0.5" />}`.

    Both children render inside the same absolute-positioned parent container that the slot already lives in — no wrapping element added, no layout containers changed. The MicButton's `absolute right-11 bottom-0.5` and the Send button's `absolute right-1 bottom-0.5` co-exist because both are positioned against the same relative parent; they will not collide visually (40px separation, matching the p-2 hit-target width).

    Preserve every comment on the Send-button branch (L1692-1701 rationale about same-button morph, patch #129 disabled logic, patch #181 press-feedback). Update the slot header comment (L1670-1674) to note the co-render change: e.g. "the slot hosts RecordingControls alone while recording; otherwise Send/X renders and MicButton co-renders when its guards allow."

    Do NOT add any new imports beyond what's already imported (MicButton is already imported). Do NOT touch handleVoiceAppend / handleVoiceSend / handleVoiceCancel / handleSend. Do NOT touch the transcribing-disabled logic on Send. Do NOT change queueArmed overlay behavior or displayError merging.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx tsc --noEmit 2>&1 | tail -30 && grep -c 'showMicButton' src/ui/features/pretty-view/ComposeBox.tsx</automated>
  </verify>
  <done>
    `npx tsc --noEmit` passes with no new errors. `showMicButton` predicate is a four-condition boolean (mediaDevices + voice.state==='idle' + !asideActive + !queueArmed) — the text-length and hasAttachments conditions are gone. The send-button-slot render tree renders RecordingControls alone when recording, else always renders the Send/X-for-Resume button AND additionally renders `<MicButton onClick={voice.start} title="Record voice" positionClass="right-11 bottom-0.5" />` when showMicButton is true. All Send-button attributes (className, disabled predicate, onClick, aria-label morph, SVG/X child) are byte-identical to before the patch (patch #181 press-feedback intact).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Flip voice test assertions and add MicButton+Send co-render test</name>
  <files>src/ui/features/pretty-view/ComposeBox.voice.test.tsx</files>
  <behavior>
    - Test 2 ("MicButton is NOT present when text is non-empty") flips to "MicButton IS present when text is non-empty; Send button also renders in the slot."
    - Any test that asserts MicButton hidden when attachments are present (if such a test exists in the current file) flips to assert MicButton visible.
    - A new test asserts MicButton and Send both render together when idle + text non-empty (both aria-labels resolvable via screen.getByRole).
    - Tests 3 (asideActive hides MicButton), 5 (recording state — RecordingControls owns slot, MicButton gone), 6/7/8/9 (voice flow) remain unchanged in behavior; assertion phrasing may be updated but semantics MUST match the new truth table.
    - Test 7's final assertion currently says "State returns to idle — MicButton visible again (text is now 'hello world' so showMicButton=false; but Send button should be present)" — the parenthetical is now wrong. Update to assert BOTH MicButton and Send button are visible after the append completes.
  </behavior>
  <action>
    Three edits inside ComposeBox.voice.test.tsx:

    Edit A — Test 2: rename and rewrite. New name: "Test 2: MicButton and Send button BOTH render when text is non-empty (co-render, no swap)". Body: type "hello" into the textarea, then assert both `screen.getByRole('button', { name: 'Record voice' })` and `screen.getByRole('button', { name: 'Send' })` resolve truthy. Delete the old `queryByRole('button', { name: 'Record voice' })).toBeNull()` assertion.

    Edit B — Test 7 tail assertion: replace the current `await waitFor(() => { expect(screen.getByRole('button', { name: 'Send' })).toBeTruthy(); });` block with an assertion that BOTH `screen.getByRole('button', { name: 'Record voice' })` and `screen.getByRole('button', { name: 'Send' })` are truthy after the append completes (text is now "hello world" but MicButton is no longer gated by text length).

    Edit C — Insert a new "Test 10: MicButton and Send both visible in idle + text non-empty (regression guard for co-render slot)" test after Test 9. Render `<ComposeBox {...baseProps()} />`, type "hello" into the textarea, then assert both buttons resolve via `screen.getByRole` and neither throws. This is the explicit regression guard for the swap-in-single-slot-was-removed invariant.

    Update the file-top comment block (L1-13) to reflect the new nine-plus-one behavior list — specifically flip item 2 ("MicButton absent when text is non-empty") to "MicButton present when text is non-empty (co-renders with Send)" and add item 10 ("MicButton and Send both visible — co-render regression guard").

    Do NOT touch Tests 1, 3, 4, 5, 6, 8, or 9. Do NOT modify the baseProps helper, MockMediaRecorder, beforeEach/afterEach setup, or the mock strategy. Do NOT modify ComposeBox.test.tsx / ComposeBox.aside-morph.test.tsx / ComposeBox.aside-props.test.tsx.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx vitest run src/ui/features/pretty-view/ComposeBox.voice.test.tsx 2>&1 | tail -50</automated>
  </verify>
  <done>
    `npx vitest run src/ui/features/pretty-view/ComposeBox.voice.test.tsx` passes all tests (10 total, including the new co-render regression guard). Test 2 asserts both MicButton and Send are present when text is non-empty. Test 7's tail assertion checks BOTH MicButton and Send after append completes. Test 10 exists and passes. Tests 1, 3, 4, 5, 6, 8, 9 pass unchanged.
  </done>
</task>

</tasks>

<verification>
End-of-plan verification (run after all three tasks complete):

1. Type check: `cd /home/ubuntu/skynet && npx tsc --noEmit` — MUST exit 0.

2. Voice test suite: `cd /home/ubuntu/skynet && npx vitest run src/ui/features/pretty-view/ComposeBox.voice.test.tsx` — all 10 tests pass.

3. Adjacent compose tests still pass (no regression from the render-structure change):
   `cd /home/ubuntu/skynet && npx vitest run src/ui/features/pretty-view/ComposeBox.test.tsx src/ui/features/pretty-view/ComposeBox.aside-morph.test.tsx src/ui/features/pretty-view/ComposeBox.aside-props.test.tsx` — all pass.

4. Predicate audit (grep gate): `cd /home/ubuntu/skynet && grep -A 10 'const showMicButton' src/ui/features/pretty-view/ComposeBox.tsx | grep -v '^#' | grep -Ec 'text\.trim|hasAttachments'` — MUST return 0 (both gates removed).

5. MicButton co-render audit: `cd /home/ubuntu/skynet && grep -c 'positionClass="right-11 bottom-0.5"' src/ui/features/pretty-view/ComposeBox.tsx` — MUST return 1 (exactly one call site, in the send-button slot).
</verification>

<success_criteria>
- MicButton exports `positionClass?: string` and defaults to "right-1 bottom-0.5" (backward compat).
- ComposeBox's `showMicButton` predicate no longer references `text.trim().length` or `hasAttachments`.
- ComposeBox renders MicButton (at right-11 bottom-0.5) AND Send/X (at right-1 bottom-0.5) simultaneously in every voice.state==='idle' + !asideActive + !queueArmed state — including states with typed text or attachments.
- RecordingControls still owns the slot alone when voice.state==='recording'.
- Send button styling, X-for-Resume morph, transcribing-disabled behavior, and patch #181 press-feedback are byte-identical to before the patch.
- ComposeBox.voice.test.tsx: Test 2 flipped, Test 7 tail updated, Test 10 added, all 10 tests pass.
- `npx tsc --noEmit` passes; adjacent compose tests pass unchanged.
</success_criteria>

<output>
Commit each task as a separate commit onto the current branch (feat/tab-title-from-tmux, at 0d57574) alongside patches #177-#181. Suggested commit messages:

- Task 1: `feat(compose): add positionClass prop to MicButton (default preserves current anchor)`
- Task 2: `feat(compose): co-render MicButton beside Send button in textarea (drop text-length and hasAttachments gates)`
- Task 3: `test(compose): flip voice test assertions to expect MicButton+Send co-render; add regression guard`

Do NOT create a new branch. Do NOT deploy — this is code-only; Ashley greenlights deploys separately.

After the three commits, write `.planning/quick/260729-3y1-mic-button-separate-from-send-button-in-/260729-3y1-SUMMARY.md` with the truths/artifacts checklist and the commit SHAs.
</output>
