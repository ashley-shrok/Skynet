---
quick_id: 260729-j8l
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/SessionHoldingOverlay.tsx
  - src/ui/features/pretty-view/ComposeBox.tsx
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-view/SessionHoldingOverlay.test.tsx        # NEW
  - src/ui/features/pretty-view/ComposeBox.recycle-disable.test.tsx    # NEW
autonomous: true
requirements:
  - Q1: constrain SessionHoldingOverlay scrim to messages-only, uncover ComposeBox
  - Q2: ComposeBox action buttons DISABLED during recycle; textarea stays typeable
  - Q3: draft survives across recycle (autosave/localStorage path unchanged)

must_haves:
  truths:
    - "During recycle, the ComposeBox is uncovered by the scrim and its textarea is typeable."
    - "During recycle, the Send button is disabled (does NOT morph to X/Resume — that is aside-only)."
    - "During recycle, the attach/paperclip, ThumbsUp, Lightbulb, Queue, and Mic buttons are disabled."
    - "During recycle, the reset-cell in the meter well is disabled (WS-side-effecting)."
    - "Draft text typed during recycle survives across the recycle completion (autosave path unchanged)."
    - "Warm-red 'recycle failed — refresh to check' variant (patch #122) still renders and still constrains to messages-only."
    - "The 350ms delay-arm gate for showOverlay (patch #74) is unchanged."
  artifacts:
    - path: "src/ui/features/pretty-view/SessionHoldingOverlay.tsx"
      provides: "scrim geometry now constrained to messages/scroll region (NOT full data-pv-root)"
    - path: "src/ui/features/pretty-view/ComposeBox.tsx"
      provides: "new `recycleActive?: boolean` prop wired into all WS-side-effecting button `disabled` predicates"
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "`recycleActive={showOverlay}` passed to ComposeBox"
    - path: "src/ui/features/pretty-view/SessionHoldingOverlay.test.tsx"
      provides: "geometry test — scrim inset covers scroll area, not full parent"
    - path: "src/ui/features/pretty-view/ComposeBox.recycle-disable.test.tsx"
      provides: "recycleActive gates buttons + textarea typeable + draft survival"
  key_links:
    - from: "src/ui/features/pretty-view/PrettyView.tsx"
      to: "src/ui/features/pretty-view/ComposeBox.tsx"
      via: "recycleActive={showOverlay} prop"
      pattern: "recycleActive=\\{showOverlay\\}"
    - from: "src/ui/features/pretty-view/PrettyView.tsx"
      to: "src/ui/features/pretty-view/SessionHoldingOverlay.tsx"
      via: "overlay mount at chat-region wrapper, not at data-pv-root"
      pattern: "SessionHoldingOverlay"
---

<objective>
Constrain the session-recycle scrim so it covers only the message-bubble area and leaves the ComposeBox uncovered + typeable. While the recycle overlay is up, ALL of ComposeBox's WS-side-effecting action buttons (Send, attach, ThumbsUp, Lightbulb, Queue, Mic, meter-well Reset cell) are DISABLED. Draft persistence is preserved so text Ashley pre-drafts during the recycle survives the transition to the fresh session.

Purpose: Ashley needs to pre-draft the next message while a session recycle is in flight. Today the full-surface patch-#74 scrim blocks all input for the whole 2-15s recycle window, which is a wasted composition opportunity.

Output: SessionHoldingOverlay constrained to messages-only; ComposeBox gains a `recycleActive` prop and gates every side-effecting control on it; PrettyView wires `recycleActive={showOverlay}`; two vitest files pin down the new behavior.

Executor judgment call — RESOLVED here so executor doesn't re-litigate:
- The disable set required by recycle is NOT identical to `asideActive`. Aside MORPHS Send to an X/Resume affordance (identity-hue X icon, onClick fires onAsideDismiss). Recycle wants Send to STAY as Send but be DISABLED (no morph, no dismiss handler — there is nothing to dismiss). Because the Send-button behavior differs, keep the two props SEPARATE. Do NOT collapse into `interactionsDisabled`.
- For the aux buttons (attach, ThumbsUp, Lightbulb, Queue) and Send-when-not-morphed, the disable EFFECT is the same (just render `disabled=true`), so those predicates simply extend to `|| recycleActive === true` — matching the existing `|| asideActive === true` pattern verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

@src/ui/features/pretty-view/SessionHoldingOverlay.tsx
@src/ui/features/pretty-view/ComposeBox.tsx
@src/ui/features/pretty-view/PrettyView.tsx
@src/ui/features/pretty-view/ComposeBox.aside-props.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Constrain overlay geometry and wire recycleActive prop through PrettyView → ComposeBox</name>
  <files>
    src/ui/features/pretty-view/SessionHoldingOverlay.tsx,
    src/ui/features/pretty-view/ComposeBox.tsx,
    src/ui/features/pretty-view/PrettyView.tsx
  </files>

  <behavior>
    - SessionHoldingOverlay: scrim now uses `absolute inset-0` inside a NEW inline wrapper that PrettyView mounts INSIDE the chat-region wrapper div (currently the `<div ref={setChatRegionEl} className="relative flex-1 min-h-0 flex flex-col">` at PrettyView.tsx:1141) — i.e. the SAME wrapper IdentityModal already portals INTO per the "identity modal covers only bubble/tasks/shells" comment at PrettyView.tsx:1134-1140. The overlay is therefore geometrically constrained to the chat-region, which excludes ComposeBox (peer, sibling below the wrapper at PrettyView.tsx:1293-1296).
    - The z-index (110), backdrop-blur-md, bg-black/40, pointer-events-auto, animate-in fade-in duration-150, centered card, error variant, motion-channel guardrail (STATIC RefreshCcw — NO animate-spin) — ALL UNCHANGED. This is a MOUNT-POINT relocation, not a scrim redesign.
    - Warm-red error variant (patch #122 / patch #127) preserved verbatim.
    - ComposeBox: add `recycleActive?: boolean` optional prop (defaults undefined, treated as false — matches asideActive precedent for backward compat).
    - ComposeBox aux buttons — extend every existing `disabled={... || asideActive === true}` predicate to ALSO OR-in `|| recycleActive === true`. Sites (identified by grep from asideActive line refs):
        * Meter-well Reset cell button (line 1175): `disabled={canSend === false || asideActive === true}` → append `|| recycleActive === true`
        * Paperclip attach (line 1359): same pattern → append `|| recycleActive === true`
        * ThumbsUp "let's go" (line 1425): same pattern → append `|| recycleActive === true`
        * Lightbulb "explain" (line 1454): same pattern → append `|| recycleActive === true`
        * Queue (Hourglass) (line 1488): `disabled={queueDisabled || asideActive === true}` → append `|| recycleActive === true`
    - ComposeBox `showMicButton` predicate (line 1008-1013): extend the AND chain to ALSO gate on `!recycleActive` — so the mic slot hides when recycle is active (WS-side-effecting: recording starts a stream). Keep the `!asideActive && !queueArmed` gates in place.
    - ComposeBox Send button (line 1704-1730): while `recycleActive === true`, force `disabled={true}` and DO NOT morph to X — recycle path is orthogonal to aside. Update the `sendDisabled` derived value (line 1035-1038) to OR-in `recycleActive === true`. Do NOT alter the `asideActive ? ... : ...` morph branch — recycle still renders the paper-plane Send icon, just disabled. Concretely: keep the existing `disabled={asideActive ? false : (sendDisabled || showTranscribingSend)}` expression — the extra `recycleActive` term flows in via `sendDisabled`, and the `asideActive ? false` branch is NEVER reached during recycle because aside and recycle are independent state paths (recycle without an aside → asideActive stays false → the `sendDisabled` branch runs → Send renders disabled).
    - Textarea (line 1537-1622): remains typeable — do NOT extend the `disabled={queueArmed}` gate with `recycleActive`. Autosave/localStorage path (patches #57 / #119) is untouched, so draft survives the recycle by the existing mechanism.
    - Textarea `handleKeyDown`: no change. Enter would call `handleSend()`, which routes through `onSend` (WS transport). During recycle the WS is temporarily unavailable (canSend may still report true because `status === "streaming"` doesn't necessarily flip), so we ALSO block the Enter-key send path: extend the early return at line 964-965 (`if (queuedText !== null) return;`) to also `if (recycleActive) return;`. This closes the gap where Send-button is disabled but Enter still fires.
    - PrettyView: at the ComposeBox mount site (line 1298-1346), add `recycleActive={showOverlay}` (NOT `isHolding` — must respect the 350ms delay-arm gate). Position alphabetically-adjacent to other props for readability; a good spot is right after `isHolding={isHolding}` at line 1302 so both recycle-derived props sit together.
    - PrettyView: MOVE the `{showOverlay && <SessionHoldingOverlay error={holdingTimeoutError} />}` mount (currently line 1118, sitting at data-pv-root level next to IdentityBadge) INTO the chat-region wrapper div. Specifically, place it as the FIRST child of the `<div ref={setChatRegionEl} className="relative flex-1 min-h-0 flex flex-col">` at line 1141 (i.e. just after that opening tag, before the `status === "connecting"` conditional at line 1145). This is what actually constrains the geometry — SessionHoldingOverlay's own `absolute inset-0` now resolves against the chat-region wrapper, not against data-pv-root. Update the mount-site comment at line 1106-1117 to reflect the new anchor (mention chat-region wrapper + "leaves ComposeBox uncovered so Ashley can pre-draft during recycle").
  </behavior>

  <action>
    Implement the behavior above. Order of edits:
    1. Edit `src/ui/features/pretty-view/ComposeBox.tsx`: add `recycleActive?: boolean` to `ComposeBoxProps`, destructure in the function signature, extend the five aux-button `disabled` predicates + `sendDisabled` + `showMicButton` + `handleKeyDown` early-return per the behavior block. Add a brief doc comment on the prop mirroring the `asideActive` doc-comment style (why-separate-from-asideActive rationale — recycle disables Send, aside morphs Send).
    2. Edit `src/ui/features/pretty-view/PrettyView.tsx`: (a) move the `{showOverlay && <SessionHoldingOverlay ...>}` line from ~1118 to become the first child of the chat-region wrapper opened at ~1141; (b) pass `recycleActive={showOverlay}` to ComposeBox at ~1302; (c) update the mount-point comment (~1106-1117) to describe the chat-region anchor + ComposeBox-uncovered rationale.
    3. Edit `src/ui/features/pretty-view/SessionHoldingOverlay.tsx`: NO behavioral changes (scrim geometry is now inherited from its new mount parent). Update the header comment block (specifically the "Shape" bullet at lines 10-13 talking about "Full-surface scrim `absolute inset-0`" and the "GATING" section at lines 38-43) to reflect that the overlay is now mounted INSIDE the chat-region wrapper, NOT at data-pv-root — so `absolute inset-0` covers messages + status branches only, and ComposeBox stays uncovered so a next message can be pre-drafted mid-recycle. Also update the "dims and blurs everything under it (messages, WIP / plan bubbles, compose box, ambient panels, jump-to-latest pill)" clause at lines 12-13 to remove the "compose box" mention. Do NOT touch the geometry classes on the `<div>` — they stay `absolute inset-0 z-[110] flex items-center justify-center backdrop-blur-md bg-black/40 pointer-events-auto animate-in fade-in duration-150` verbatim.
    4. Verify with `npx tsc --noEmit` (frontend-only per fleet typecheck rule) + `npm run build`.
  </action>

  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build</automated>
  </verify>

  <done>
    - `npx tsc --noEmit` exits 0.
    - `npm run build` exits 0.
    - `grep -n "recycleActive" src/ui/features/pretty-view/ComposeBox.tsx` shows the prop in the interface, in the destructure, and in each of the 5 aux-button disable predicates + `sendDisabled` + `showMicButton` + `handleKeyDown` early-return (≥9 hits).
    - `grep -n "recycleActive={showOverlay}" src/ui/features/pretty-view/PrettyView.tsx` returns exactly one line inside the ComposeBox JSX.
    - `grep -n "SessionHoldingOverlay" src/ui/features/pretty-view/PrettyView.tsx` shows the mount now lives after the `setChatRegionEl` wrapper opens, not at data-pv-root level (line number is > the line of `<div ref={setChatRegionEl}`).
    - `grep -c "isHolding" src/ui/features/pretty-view/PrettyView.tsx` is unchanged from before this task (delay-arm gate untouched).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add vitest coverage for overlay geometry, recycle-disable, and draft survival</name>
  <files>
    src/ui/features/pretty-view/SessionHoldingOverlay.test.tsx,
    src/ui/features/pretty-view/ComposeBox.recycle-disable.test.tsx
  </files>

  <behavior>
    - Test file A — `SessionHoldingOverlay.test.tsx` (NEW):
        * Test A1 "renders scrim geometry as `absolute inset-0` — parent controls what area it covers": render `<div style={{position:'relative',width:400,height:600}}><div style={{height:500}}>chat</div><SessionHoldingOverlay/></div>` and assert the overlay's root has classes `absolute` and `inset-0` (regexp match on className). No jsdom layout math needed — the geometric constraint is that the overlay's INSET-0 is scoped to WHATEVER PARENT ELEMENT mounts it, and we're pinning the class contract, not the pixel box (jsdom can't compute layout).
        * Test A2 "error variant preserved (patch #122 warm-red still renders)": render `<SessionHoldingOverlay error={true}/>` and assert `getByText('Session recycle failed — refresh to check')` is present and the RefreshCcw span has `text-[hsl(0,72%,60%)]` (regexp-in-classname).
        * Test A3 "static glyph — no animate-spin (motion-channel guardrail)": render both variants and assert `querySelector('.animate-spin')` returns null in each.

    - Test file B — `ComposeBox.recycle-disable.test.tsx` (NEW):
        * Uses the same render setup pattern as `ComposeBox.aside-morph.test.tsx` and `ComposeBox.test.tsx` (already-established fixtures). If those tests import a shared render helper, reuse it; if they inline the render call, inline the same shape here. Mock `@/api/compose-drafts-api` (`getComposeDraft` returns `Promise.resolve({body:""})`, `putComposeDraft` returns `Promise.resolve()`, `flushComposeDraftKeepalive` a no-op) mirroring how existing ComposeBox tests do it.
        * Test B1 "recycleActive=true: Send button is disabled but NOT morphed to X": render with `recycleActive={true}`, type some text into the textarea via `fireEvent.change` on the `Message Claude…` placeholder textarea, then assert:
            - `getByLabelText('Send')` exists (aria-label is still "Send", NOT "Resume").
            - `getByLabelText('Send').disabled === true`.
            - The inline `<svg><path d="M14.536 ...">` paper-plane is rendered (querySelector on the path `d` attr starting with "M14.536").
            - There is NO lucide X svg (no `getByLabelText('Resume')`).
        * Test B2 "recycleActive=true: aux buttons all disabled": with `showPaperclip={true}`, `onInterrupt` NOT passed, `recycleActive={true}`, assert each of these aria-labels resolves to a disabled button:
            - "Send with /id reset prefix" (meter-well Reset cell)
            - "Attach file"
            - "Send 'let's go'"
            - "Ask for a concise re-explanation"
            - "Queue send for when session goes idle"
        * Test B3 "recycleActive=true: textarea stays typeable": render with `recycleActive={true}`, get the textarea by placeholder, assert `textarea.disabled === false`, then `fireEvent.change(textarea, {target:{value:'draft during recycle'}})`, then assert `textarea.value === 'draft during recycle'`.
        * Test B4 "recycleActive=true: Enter key does NOT fire onSend": render with `recycleActive={true}` and an `onSend` spy, type text via change event, fire `keyDown` with `{key:'Enter'}` on the textarea, assert the spy was not called.
        * Test B5 "recycleActive=false (default): behavior is unchanged — Send enabled with text": baseline sanity — render without `recycleActive`, type text, assert Send is enabled and clicking it fires the `onSend` spy exactly once.
        * Test B6 "draft survives across a recycleActive true→false transition": render with `recycleActive={true}`, type "pre-draft", then rerender with `recycleActive={false}`, assert the textarea value is still "pre-draft". (This tests the invariant — the textarea `disabled` prop was never flipped by recycleActive, so React preserved its value.)

    - All new tests target vitest + @testing-library/react (already the fork's convention — see `ComposeBox.test.tsx` at the same directory).
  </behavior>

  <action>
    Write the two test files exactly per the behavior block. Reuse the render harness and mocks pattern from `ComposeBox.aside-morph.test.tsx` and `ComposeBox.test.tsx` for consistency. Do NOT introduce new test fixtures or a new mocking layer. Run `npx vitest run src/ui/features/pretty-view/SessionHoldingOverlay.test.tsx src/ui/features/pretty-view/ComposeBox.recycle-disable.test.tsx` and iterate until every test passes.
  </action>

  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/SessionHoldingOverlay.test.tsx src/ui/features/pretty-view/ComposeBox.recycle-disable.test.tsx</automated>
  </verify>

  <done>
    - Both test files exist at the paths above.
    - `npx vitest run <both files>` shows 9 passing tests (3 in file A, 6 in file B), 0 failing, 0 skipped.
    - `npx tsc --noEmit` still exits 0 (test files typecheck).
  </done>
</task>

</tasks>

<threat_model>
Not applicable — this is a UI-only defense-in-depth follow-through on patch #74. The overlay was never a security boundary (per SessionHoldingOverlay.tsx line 15-17 note referencing T-260719-5ym-02); the new recycle-disable gates are UX affordances, not authorization boundaries. Backend session-tracker code paths are untouched (constraint enforced in the outer plan).
</threat_model>

<verification>
- Frontend typecheck: `npx tsc --noEmit`
- Frontend build: `npm run build`
- New vitest suites green (Task 2 verify block).
- Sanity grep: `grep -c 'asideActive' src/ui/features/pretty-view/ComposeBox.tsx` unchanged from pre-plan count (asideActive semantics NOT touched — recycleActive is additive).
- Sanity grep: `grep -n 'patch #122' src/ui/features/pretty-view/SessionHoldingOverlay.tsx` still returns the warm-red error-variant comment block (recycle-failed variant intact).
- Sanity grep: `grep -c 'setTimeout' src/ui/features/pretty-view/PrettyView.tsx` unchanged (350ms delay-arm timer + 120s timeout timer both intact).
</verification>

<success_criteria>
- During a session recycle, the scrim covers messages/tasks/shells panels but NOT the ComposeBox.
- ComposeBox textarea is typeable during recycle; Send, Reset cell, attach, ThumbsUp, Lightbulb, Queue, and Mic are all disabled or hidden; Enter key does not fire onSend.
- Send button does NOT morph to X during recycle (morph is asideActive-only).
- Draft body persisted via patches #57 / #119 continues to autosave normally during recycle and survives the transition to the fresh session.
- Warm-red "Session recycle failed — refresh to check" variant continues to render on 120s timeout or `holding_timeout` inactive frame — with the SAME messages-only geometry.
- 350ms delay-arm gate for `showOverlay` (patch #74) still filters out instant recycles.
- 2 commits on the working branch: (a) implementation, (b) tests. STOP after commits — orchestrator handles deploy + bookkeeping OUT OF BAND per constraints.
</success_criteria>

<output>
Create `.planning/quick/260729-j8l-session-recycling-overlay-should-not-cov/260729-j8l-SUMMARY.md` when done. STOP after the two commits per constraint block — do NOT push, do NOT `docker build`, do NOT `docker compose up`, do NOT edit skynet-patches.md / bounty archive / tina.md (orchestrator owns those OOB).
</output>
