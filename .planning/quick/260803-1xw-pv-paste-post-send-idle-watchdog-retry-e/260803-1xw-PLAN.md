---
phase: quick-260803-1xw
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/ssh/terminal-session-manager.ts
  - src/backend/ssh/terminal.ts
  - src/backend/ssh/terminal-pv-watchdog.test.ts
  - src/ui/features/terminal/Terminal.tsx
  - src/ui/features/terminal/Terminal.wiring.test.ts
autonomous: true
requirements:
  - pv-paste-to-terminal-lands-as-unsent-bracket-paste
must_haves:
  truths:
    - "When a PV submit's Enter reaches Claude Code and produces stdout within 2.5s, no retry fires."
    - "When a PV submit produces no activity within 2.5s, backend fires a retry Enter via `tmux send-keys` on the same sshConn/target."
    - "When both the initial send and the retry produce no activity within a total 5s, backend emits `{type:\"paste_send_failed\", mqid, reason:\"no_activity_after_2_retries\"}` on the attachedWs."
    - "Watchdogs are per-submit and are cancelled if the WS closes or the session is destroyed before they fire."
    - "The non-tmux fallback branch (no tmuxSessionName / no sshConn) arms NO watchdog."
    - "Frontend receives `paste_send_failed` and surfaces a user-visible error toast."
  artifacts:
    - path: "src/backend/ssh/terminal-session-manager.ts"
      provides: "TerminalSession.pvSubmitWatchdogs Set<Timeout> field + cleanup in destroySession"
      contains: "pvSubmitWatchdogs"
    - path: "src/backend/ssh/terminal.ts"
      provides: "Post-send idle-watchdog attached to the isPrettyViewSubmit tmux send-keys path"
      contains: "pv_submit_watchdog"
    - path: "src/backend/ssh/terminal-pv-watchdog.test.ts"
      provides: "T-1 through T-7 backend watchdog behavior tests using vi.useFakeTimers"
    - path: "src/ui/features/terminal/Terminal.tsx"
      provides: "WS handler branch for msg.type === 'paste_send_failed' → toast.error"
      contains: "paste_send_failed"
    - path: "src/ui/features/terminal/Terminal.wiring.test.ts"
      provides: "T-F1/T-F2 structural assertions that the paste_send_failed handler + toast.error copy exist"
  key_links:
    - from: "src/backend/ssh/terminal.ts (isPrettyViewSubmit + tmux send-keys path, ~L720)"
      to: "TerminalSession.pvSubmitWatchdogs + TerminalSession.lastActivityAt"
      via: "setTimeout handles stored on session, snapshot of lastActivityAt at send time"
      pattern: "pvSubmitWatchdogs"
    - from: "src/backend/ssh/terminal.ts (2nd watchdog escalation)"
      to: "session.attachedWs.send({type:'paste_send_failed', mqid, reason})"
      via: "WebSocket JSON emit"
      pattern: "paste_send_failed"
    - from: "src/ui/features/terminal/Terminal.tsx (WS onmessage)"
      to: "toast.error"
      via: "new else-if branch on msg.type === 'paste_send_failed'"
      pattern: "paste_send_failed"
---

<objective>
Add a per-submit post-send idle-watchdog to the PV submit path in `src/backend/ssh/terminal.ts` (patch #118 tmux send-keys branch). At T+2.5s after the initial Enter, if `session.lastActivityAt` has NOT advanced past the snapshot taken at send time, fire a retry Enter via `tmux send-keys` on the same sshConn. At T+5.0s, if a second window of stagnation is observed, emit `{type:"paste_send_failed", mqid, reason:"no_activity_after_2_retries"}` on the attachedWs and surface it as a user-visible toast in the frontend.

Purpose: Ashley's pinned bounty `pv-paste-to-terminal-lands-as-unsent-bracket-paste` — three prior byte-level patches (#100/#111/#118) each failed at the next scale of paste body. Ashley's locked design: fix at the outcome layer (detect stuck-send + retry) rather than fight Ink's paste-detection state machine. Tina could NOT reproduce Ashley's failure locally with tmux+paste-buffer at 22KB / 100KB — the failure is specific to Skynet's real SSH-over-network pipeline. Post-hoc retry is safe: "hitting enter on an otherwise empty box does nothing" (Ashley 2026-08-02).

Output:
- Backend: `pvSubmitWatchdogs: Set<NodeJS.Timeout>` field on `TerminalSession`, initialised in `createSession` and cleared in `destroySession`; watchdog pair armed inside the existing `setTimeout(..., 250)` after the tmux send-keys exec is scheduled.
- Backend: new `paste_send_failed` WS event on the second-watchdog failure branch.
- Backend tests: `terminal-pv-watchdog.test.ts` with T-1 through T-7 covering happy path, retry-fires, retry-succeeds, retry-fails-escalate, WS-close-cancels, non-tmux-no-watchdog, and concurrent submits.
- Frontend: new `msg.type === "paste_send_failed"` branch in `Terminal.tsx`'s WS onmessage that fires `toast.error` (matches existing `toast.warning`/`toast.info` pattern at L1738/L1773).
- Frontend tests: two new structural assertions appended to `Terminal.wiring.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@src/backend/ssh/terminal.ts
@src/backend/ssh/terminal-session-manager.ts
@src/ui/features/terminal/Terminal.tsx
@src/ui/features/terminal/Terminal.wiring.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend watchdog on isPrettyViewSubmit path + backend tests</name>
  <files>
    src/backend/ssh/terminal-session-manager.ts,
    src/backend/ssh/terminal.ts,
    src/backend/ssh/terminal-pv-watchdog.test.ts
  </files>
  <action>
Extend `TerminalSession` in `src/backend/ssh/terminal-session-manager.ts` with a new field `pvSubmitWatchdogs: Set<NodeJS.Timeout>` (Set, not array — cheaper delete on per-timer cancellation). Initialise it as `new Set()` in `createSession` (alongside the other idle fields around L148-154). In `destroySession`, before the `sshStream.end()` calls, iterate `session.pvSubmitWatchdogs` and `clearTimeout` each handle, then `.clear()` the Set. This ensures no stray watchdog fires against a torn-down session. Also handle the WS-detach case: in `detachWs`, similarly walk + clearTimeout + `.clear()` `pvSubmitWatchdogs` (a detached WS means the second-watchdog `attachedWs.send(...)` would no-op anyway, but freeing the timers prevents delayed retry-Enter firing at a target the user has already navigated away from).

In `src/backend/ssh/terminal.ts`, inside the existing `isPrettyViewSubmit && ... endsWith("\r")` branch (L649-811), after the `submitConn.exec("tmux send-keys ... Enter", ...)` call is issued (i.e. inside the callback where we know the exec was scheduled — right after the `channel.on("error", ...)` handler, before the outer catch), and ONLY when `submitConn && tmuxTarget` are both truthy (i.e. skip the non-tmux fallback path per scope), arm the watchdog pair. Structure:

  const watchdogSession = currentSessionId ? sessionManager.getSession(currentSessionId) : null;
  if (watchdogSession && submitConn && tmuxTarget) {
    const lastActivityAtAtSend = watchdogSession.lastActivityAt;
    const watchdogMqid = mqid;
    const watchdogTarget = tmuxTarget;
    // First watchdog @ T+2500ms
    const firstHandle = setTimeout(() => {
      watchdogSession.pvSubmitWatchdogs.delete(firstHandle);
      const s = sessionManager.getSession(currentSessionId!);
      if (!s) return; // session torn down
      if (s.lastActivityAt > lastActivityAtAtSend) {
        sshLogger.debug("PV submit watchdog: activity within 2.5s, submit succeeded", { operation: "pv_submit_watchdog_ok", userId, mqid: watchdogMqid, sessionId: currentSessionId, gapMs: Date.now() - lastActivityAtAtSend });
        return;
      }
      sshLogger.warn("PV submit watchdog: no activity within 2.5s, firing retry Enter", { operation: "pv_submit_watchdog_retry", userId, mqid: watchdogMqid, sessionId: currentSessionId, gapMs: Date.now() - lastActivityAtAtSend });
      // Retry Enter — same shape as the initial send-keys call.
      try {
        s.sshConn?.exec(`tmux send-keys -t ${shellQuote(watchdogTarget)} Enter`, (retryErr, retryChannel) => {
          if (retryErr || !retryChannel) {
            sshLogger.error("PV submit watchdog: retry exec_open_failed", retryErr, { operation: "pv_submit_watchdog_retry_failed", userId, mqid: watchdogMqid });
            return;
          }
          retryChannel.on("data", () => {});
          retryChannel.stderr?.on("data", () => {});
          retryChannel.on("close", () => { try { retryChannel.end(); } catch {} });
          retryChannel.on("error", (e) => sshLogger.error("PV submit watchdog: retry channel error", e, { operation: "pv_submit_watchdog_retry_channel_error", userId, mqid: watchdogMqid }));
        });
      } catch (e) {
        sshLogger.error("PV submit watchdog: retry sync throw", e instanceof Error ? e : new Error(String(e)), { operation: "pv_submit_watchdog_retry_sync_throw", userId, mqid: watchdogMqid });
        return;
      }
      // Snapshot again for the second watchdog.
      const lastActivityAtAtRetry = s.lastActivityAt;
      // Second watchdog @ T+2500ms from retry (i.e. T+5000ms from initial send).
      const secondHandle = setTimeout(() => {
        s.pvSubmitWatchdogs.delete(secondHandle);
        const s2 = sessionManager.getSession(currentSessionId!);
        if (!s2) return;
        if (s2.lastActivityAt > lastActivityAtAtRetry) {
          sshLogger.info("PV submit watchdog: retry succeeded", { operation: "pv_submit_watchdog_retry_ok", userId, mqid: watchdogMqid, sessionId: currentSessionId });
          return;
        }
        sshLogger.error("PV submit watchdog: retry also failed — surfacing to frontend", undefined, { operation: "pv_submit_watchdog_escalate", userId, mqid: watchdogMqid, sessionId: currentSessionId });
        if (s2.attachedWs?.readyState === WebSocket.OPEN) {
          try {
            s2.attachedWs.send(JSON.stringify({ type: "paste_send_failed", mqid: watchdogMqid, reason: "no_activity_after_2_retries" }));
          } catch (wsErr) {
            sshLogger.error("PV submit watchdog: WS send failed", wsErr instanceof Error ? wsErr : new Error(String(wsErr)), { operation: "pv_submit_watchdog_ws_send_failed", userId, mqid: watchdogMqid });
          }
        }
      }, 2500);
      s.pvSubmitWatchdogs.add(secondHandle);
    }, 2500);
    watchdogSession.pvSubmitWatchdogs.add(firstHandle);
  }

Place this block INSIDE the `setTimeout(() => { ... }, 250)` block at ~L720-811, AFTER the `submitConn.exec(...)` try/catch — arm the watchdog only along the tmux send-keys path (not the `!submitConn || !tmuxTarget` fallback branch, per scope). The `submitConn && tmuxTarget` guard is redundant with the code path but explicit for readability and future refactor safety.

Do NOT modify `IDLE_THRESHOLD_MS`, the 1s idle-detector tick loop (L1985-2049), the 250ms initial send delay, or any non-PV input path. Do NOT introduce bracketed-paste wrapping.

Create `src/backend/ssh/terminal-pv-watchdog.test.ts` with `describe("PV submit watchdog", ...)` and tests T-1 through T-7. Use `vi.useFakeTimers()` in `beforeEach` and `vi.useRealTimers()` in `afterEach` (see `src/backend/claude-session/claude-session-server.aside.test.ts:377` for the pattern). Since the watchdog logic is embedded in the `case "input":` handler of a very large WebSocket message dispatcher, DO NOT try to mount the full terminal WS handler — instead, extract the watchdog arming into a small named helper `armPvSubmitWatchdog(session, submitConn, tmuxTarget, mqid, userId, currentSessionId)` exported from `terminal.ts` (or from a new sibling `terminal-pv-watchdog.ts` if preferred — planner discretion, but keep the call-site in the input handler literally one line: `armPvSubmitWatchdog(watchdogSession, submitConn, tmuxTarget, mqid, userId, currentSessionId)`). Then tests can import and unit-test the helper directly with fake timers + a hand-rolled mock `session` object + a mock `submitConn` with an `exec` spy + a mock `attachedWs` with a `send` spy.

Test list:
  - T-1 happy path: arm watchdog, advance `session.lastActivityAt = Date.now() + 1000`, `vi.advanceTimersByTime(2500)`, assert retry `submitConn.exec` NOT called, no `attachedWs.send` for paste_send_failed. (`session.lastActivityAt` is snapshot at arm time; bump it in-test to simulate activity.)
  - T-2 retry-fires: arm watchdog, do NOT bump `lastActivityAt`, `vi.advanceTimersByTime(2500)`, assert `submitConn.exec` called with `tmux send-keys -t <shellQuoted(target)> Enter`.
  - T-3 retry-succeeds: chain of T-2, then bump `lastActivityAt` (after the retry callback executes), `vi.advanceTimersByTime(2500)`, assert no paste_send_failed WS emit.
  - T-4 retry-fails-escalate: chain of T-2, keep `lastActivityAt` static, `vi.advanceTimersByTime(2500)`, assert `attachedWs.send` called once with JSON matching `{"type":"paste_send_failed","mqid":"<injected>","reason":"no_activity_after_2_retries"}`.
  - T-5 cancel-on-destroy: arm watchdog, immediately iterate `session.pvSubmitWatchdogs` and clearTimeout each + `.clear()` (simulates destroySession), `vi.advanceTimersByTime(5000)`, assert NO exec call and NO send call.
  - T-6 non-tmux-no-watchdog: call helper with `tmuxTarget = null` (or `submitConn = null`) — assert `session.pvSubmitWatchdogs.size === 0` after call and no timers scheduled. (Helper's first line should be an early return on missing target/conn.)
  - T-7 concurrent-submits: arm watchdog twice with two different mqids on the same session; bump `lastActivityAt` between arms so only the SECOND submit's snapshot is stagnant; `vi.advanceTimersByTime(2500)`; assert exactly ONE retry exec fired and only for the second mqid. Then advance another 2500ms with `lastActivityAt` still static; assert exactly ONE paste_send_failed emit and it carries the second mqid.

Mock shape reference (planner picks final shape but this is the intended surface):
  const session = { lastActivityAt: Date.now(), pvSubmitWatchdogs: new Set(), sshConn: submitConn, attachedWs: mockWs };
  const submitConn = { exec: vi.fn((_cmd, cb) => cb(undefined, mockChannel)) };
  const mockWs = { readyState: 1, send: vi.fn() }; // 1 = WebSocket.OPEN
  const mockChannel = { on: vi.fn().mockReturnThis(), stderr: { on: vi.fn() }, end: vi.fn() };

Use `sessionManager.getSession` mocking: since the helper's second-watchdog reads `sessionManager.getSession(currentSessionId)` again to guard against session-destroy races, use `vi.spyOn(sessionManager, "getSession").mockReturnValue(session)` in the tests. For T-5, flip the mock to return `null` right before advancing timers.

Verification note for the executor: the extracted helper form is the ONLY way to keep test complexity bounded (the raw `case "input":` block is 200+ lines and pulls in the whole WS dispatch). If extraction requires touching more than adding one exported function + replacing an inline block with a helper call, stop and reassess — the extraction should be a mechanical move-into-function refactor.

Backend commit message (subject line, executor drafts body): `feat(terminal): PV submit post-send idle-watchdog with auto-retry Enter (pinned bounty)`. Body must reference bounty slug `pv-paste-to-terminal-lands-as-unsent-bracket-paste` and note: "Ashley's design: fix at the outcome layer (detect + retry) rather than the byte layer, which #100/#111/#118 kept losing at the next scale."
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npx vitest run src/backend/ssh/terminal-pv-watchdog.test.ts 2>&amp;1 | tee /tmp/pv-watchdog-backend.log; grep -E "Tests +[0-9]+ passed" /tmp/pv-watchdog-backend.log</automated>
  </verify>
  <done>
    - `TerminalSession.pvSubmitWatchdogs: Set&lt;NodeJS.Timeout&gt;` exists; initialized in `createSession`; cleared+deleted in `destroySession` and `detachWs`.
    - `src/backend/ssh/terminal.ts` exports (or delegates to a sibling module exporting) `armPvSubmitWatchdog(...)`; the isPrettyViewSubmit send-keys branch calls it exactly once per submit; the non-tmux fallback branch does NOT call it.
    - `src/backend/ssh/terminal-pv-watchdog.test.ts` exists and all 7 tests pass.
    - No changes to `IDLE_THRESHOLD_MS`, the 1s idle-detector tick, the 250ms initial delay, or any non-PV input path.
    - `npx tsc --noEmit` clean.
    - `npm run build:backend` succeeds (patch #154 lesson — backend TS must build, not just typecheck).
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend paste_send_failed WS handler + toast + wiring test</name>
  <files>
    src/ui/features/terminal/Terminal.tsx,
    src/ui/features/terminal/Terminal.wiring.test.ts
  </files>
  <action>
In `src/ui/features/terminal/Terminal.tsx`, add a new `else if` branch to the WS `onmessage` dispatcher chain (right after the `msg.type === "tmux_detached"` branch at ~L1768, or grouped with the other `tmux_*` handlers — placement doesn't matter functionally, pick a location that reads naturally with the surrounding cases):

  } else if (msg.type === "paste_send_failed") {
    // Backend PV submit watchdog (patch [next-#]): initial Enter + 1 retry
    // both failed to produce activity within 2 × 2.5s windows. Surface as
    // a user-visible error so Ashley knows the send didn't take effect and
    // can retry manually / shorten the message. The pinned bounty
    // `pv-paste-to-terminal-lands-as-unsent-bracket-paste` motivates this;
    // Tina could not reproduce the underlying stuck-send locally, so this
    // is our observability + recovery UX rather than a byte-level fix.
    toast.error(t("terminal.pasteSendFailed"), { duration: 8000 });
    addLog({
      type: "error",
      stage: "connection",
      message: t("terminal.pasteSendFailed"),
    });
  }

The `toast` import (line 62 `import { toast } from "sonner";`) and `addLog` helper already exist in this file (used by the surrounding `tmux_unavailable` / `tmux_session_missing` handlers at L1738-1767) — do not add new imports.

For the i18n string `terminal.pasteSendFailed`, add an entry in `src/ui/i18n/i18n.ts` (or wherever the `terminal.*` namespace resource file lives — grep for `tmuxUnavailable` to find the existing translation-registration file and follow the same pattern) with English copy: `"Send didn't take effect after retry — try a shorter message or paste in chunks."` Match tone/length of the existing `terminal.tmuxUnavailable` / `terminal.clipboardWriteFailed` entries. Only add English; other locales fall back to English via i18next default. If `src/ui/i18n/i18n.ts` uses an inline resources object, add the key there; if it loads from separate JSON files, add to all locale files that already contain the other `terminal.*` keys (executor picks based on what's actually there — do not invent a new i18n mechanism).

Do NOT introduce a new toast primitive (sonner is already in use). Do NOT plumb the error through PrettyView props — the toast fires from Terminal.tsx directly at the WS-handler layer, matching how `tmux_unavailable` and `tmux_detached` currently surface. This keeps the change surface small and is consistent with existing WS-triggered user-visible errors.

Append two new structural tests to `src/ui/features/terminal/Terminal.wiring.test.ts` using the existing grep-on-source pattern (see Test 1a at L40-44 for the shape):

  it("Test PV-Watchdog 1: WS handler branch for paste_send_failed exists exactly once", () => {
    const matches = src.match(/msg\.type === "paste_send_failed"/g);
    expect(matches).not.toBeNull();
    expect(matches!.length).toBe(1);
  });

  it("Test PV-Watchdog 2: paste_send_failed branch calls toast.error with pasteSendFailed i18n key", () => {
    // Grep for a proximity match — the toast.error call must be inside the branch,
    // not somewhere else in the file. Use a permissive regex that spans the branch body.
    expect(
      /msg\.type === "paste_send_failed"[\s\S]{0,400}toast\.error\(t\("terminal\.pasteSendFailed"\)/.test(src),
    ).toBe(true);
  });

Also verify the i18n key is registered by grepping the resource file once at test-load. If the i18n file lives at a stable path (`src/ui/i18n/i18n.ts`), add a third structural test:

  it("Test PV-Watchdog 3: terminal.pasteSendFailed i18n key is registered", () => {
    const i18nSrc = readFileSync(join(HERE, "..", "..", "i18n", "i18n.ts"), "utf-8");
    expect(i18nSrc).toMatch(/pasteSendFailed/);
  });

(Executor: adjust the relative path if `Terminal.wiring.test.ts` sits at a different depth relative to `src/ui/i18n/i18n.ts`. If i18n is JSON-file-based, point the test at the English JSON file instead.)

Frontend commit message (subject): `feat(terminal): surface paste_send_failed WS event as user-visible toast (pinned bounty)`. Body: "Closes bounty pv-paste-to-terminal-lands-as-unsent-bracket-paste." Consolidating both tasks' commits into ONE is acceptable if the frontend diff is small (single-file handler add + one i18n key) — executor picks based on final line count. Prefer 2 commits split on backend/frontend boundary as default per the quick-mode `<commit_style>` block.

After both commits land: STOP. Do NOT push. Do NOT `docker build` or `docker compose up`. This joins the held deploy queue with patches #267-#286.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npx vitest run src/ui/features/terminal/Terminal.wiring.test.ts 2>&amp;1 | tee /tmp/pv-watchdog-frontend.log; grep -E "Tests +[0-9]+ passed" /tmp/pv-watchdog-frontend.log</automated>
  </verify>
  <done>
    - `Terminal.tsx` has a single `else if (msg.type === "paste_send_failed")` branch that calls `toast.error(t("terminal.pasteSendFailed"), ...)` and `addLog(...)`.
    - `terminal.pasteSendFailed` i18n key exists in the English resources with the specified copy.
    - `Terminal.wiring.test.ts` has 2-3 new PV-Watchdog tests, all passing.
    - `npx tsc --noEmit` clean AND `npm run build` succeeds (full frontend build — patch #154 lesson).
    - Full frontend suite `npx vitest run` passes: expect ~1105-1108 passed / 6 skipped / 0 failed (baseline 1103 + the 2-3 new tests, no regressions).
    - 1 or 2 atomic commits landed on `feat/tab-title-from-tmux`; NO push, NO docker build, NO compose up.
  </done>
</task>

</tasks>

<verification>
Full-suite gates the executor MUST run before declaring done:

  cd /home/ubuntu/skynet && npx tsc --noEmit 2>&1 | tail -20
  cd /home/ubuntu/skynet && npm run build:backend 2>&1 | tail -20
  cd /home/ubuntu/skynet && npm run build 2>&1 | tail -20
  cd /home/ubuntu/skynet && npx vitest run 2>&1 | tee /tmp/pv-watchdog-full.log; tail -30 /tmp/pv-watchdog-full.log

Full frontend suite (`npx vitest run`) MUST pass with baseline of 1103 passed + the new watchdog tests (7 backend + 2-3 frontend structural). Expected total: 1112-1113 passed / 6 skipped / 0 failed / 89-90 files. NO new failures.

Log-shape sanity check: grep the modified backend file for the four new log operations to confirm they exist and are grep-able for post-hoc observability:

  cd /home/ubuntu/skynet && grep -c "pv_submit_watchdog" src/backend/ssh/terminal.ts src/backend/ssh/terminal-pv-watchdog.ts 2>/dev/null

Expected: at least 5 hits across the operation strings `pv_submit_watchdog_ok`, `pv_submit_watchdog_retry`, `pv_submit_watchdog_retry_ok`, `pv_submit_watchdog_escalate`, and the various error operations.

Scope-guard grep (must return ZERO hits — enforces explicitly_out_of_scope):

  cd /home/ubuntu/skynet && git diff HEAD -- src/backend/ssh/terminal.ts | grep -E "IDLE_THRESHOLD_MS|bracketed.paste|BPM"

Any hit means the executor drifted into out-of-scope changes — revert those lines.
</verification>

<success_criteria>
- All 7 backend watchdog tests pass under fake timers.
- All 2-3 new frontend structural tests pass.
- `npx tsc --noEmit`, `npm run build:backend`, `npm run build`, and `npx vitest run` all clean.
- Backend `terminal.ts` adds ≤ ~60 lines of new code inside the existing isPrettyViewSubmit branch (or delegates to a small sibling module — mechanical extraction only).
- `TerminalSession.pvSubmitWatchdogs` is a `Set<NodeJS.Timeout>`, initialized on create, cleared on both `destroySession` AND `detachWs`.
- Non-tmux fallback path arms NO watchdog (guard verified by T-6).
- Frontend surfaces `paste_send_failed` via `toast.error` reusing sonner (no new toast primitive).
- 1 or 2 atomic commits on `feat/tab-title-from-tmux`, subject lines per `<commit_style>`.
- NO push, NO docker build, NO compose up. Executor STOPS after the last commit.
- Zero regressions in the full frontend vitest suite.
</success_criteria>

<output>
No SUMMARY file required (quick mode). Executor writes a short summary of what changed + the commit SHAs into its final message so Tina can update `skynet-patches.md` and close the pinned bounty as orchestrator post-run.
</output>
