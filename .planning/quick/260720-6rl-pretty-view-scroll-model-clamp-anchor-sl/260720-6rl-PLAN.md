---
phase: quick-260720-6rl
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/use-auto-scroll.ts
  - src/ui/features/pretty-view/use-auto-scroll.test.ts
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-view/ComposeBox.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "On send, the newly-appended user message becomes the anchor; view is placed so the anchor sits at TOP of viewport iff content extends >= 1 viewport below it, otherwise view follows bottom (unified clamp rule: scrollTop = min(followBottomTop, anchorPinTop))."
    - "During a turn in clamp mode, streaming/append growth never scrolls the anchor off the top of the viewport (the anchor's absolute offset within scroll content, computed via getBoundingClientRect, acts as a scrollTop ceiling)."
    - "Clicking the good-to-go (ThumbsUp) button jumps scrollTop to scrollHeight AND flips the scroll manager into user-driving + followBottom=true so subsequent growth keeps the view stuck to the tail (no anchor ceiling)."
    - "A real user scroll (any gesture NOT emitted while the programmatic-scroll counter > 0) flips mode to user-driving; followBottom is set to (distFromBottom <= 100px). While user-driving && !followBottom, no growth event moves the view."
    - "The scroll-to-top-of-new-message codepath from patch #88 (the `messageHeight > viewportHeight` branch in useAutoScroll) is REMOVED; the clamp rule subsumes it — an assistant message that grows past the viewport under a fresh anchor triggers the ceiling naturally instead of a one-shot scrollTop = newEl.offsetTop write."
  artifacts:
    - path: "src/ui/features/pretty-view/use-auto-scroll.ts"
      provides: "State machine (mode: 'clamp' | 'user-driving', followBottom, anchor ref) implementing the clamp rule + ResizeObserver dispatch + programmatic-scroll counter + user-scroll detection + scrollToBottomAndFollow (GTG)."
      exports: ["useAutoScroll"]
    - path: "src/ui/features/pretty-view/use-auto-scroll.test.ts"
      provides: "Vitest unit tests for anchorPinTop math, applyClampRule branch selection (follow-bottom vs anchor-pinned), and programmatic-vs-user scroll gating."
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "Passes the last user-role StreamEvent's eventId to useAutoScroll as the anchor key; wires anchorRefCallback onto that message's ChatMessage wrapper; passes scrollToBottomAndFollow to ComposeBox as onGoodToGo."
    - path: "src/ui/features/pretty-view/ComposeBox.tsx"
      provides: "ThumbsUp `good to go` handler invokes the new onGoodToGo prop before (or instead of) handleQuickSend — the SCROLL action fires locally so it does not depend on the JSONL round-trip landing the user-message echo."
  key_links:
    - from: "src/ui/features/pretty-view/PrettyView.tsx"
      to: "src/ui/features/pretty-view/use-auto-scroll.ts"
      via: "useAutoScroll({ messages, anchorRefCallback }) — hook consumes the messages array (not just length) so it can identify the LAST user-role event as the anchor eventId"
      pattern: "useAutoScroll\\("
    - from: "src/ui/features/pretty-view/ComposeBox.tsx"
      to: "src/ui/features/pretty-view/PrettyView.tsx"
      via: "onGoodToGo prop — new callback prop from PrettyView; ComposeBox calls it inside the ThumbsUp onClick BEFORE handleQuickSend so the mode flip + scroll-to-bottom fires synchronously on click"
      pattern: "onGoodToGo"
    - from: "src/ui/features/pretty-view/PrettyView.tsx"
      to: "src/ui/features/pretty-view/ChatMessage.tsx"
      via: "anchorRef callback attached to the wrapper div of the last user-role ChatMessage (PrettyView passes ref to a per-message wrapper div in its own map, not into ChatMessage itself) — anchor DOM element must be within the ResizeObserver-observed contentEl subtree"
      pattern: "ref=\\{anchorRefCallback\\}"
---

<objective>
Replace pretty-view's current scroll implementation (broken patch #88 "scroll to top of new assistant message" + broken good-to-go bottom-scroll) with the clamp-anchor + Slack-follow state machine already validated in `~/.claude/identities/tina/bounties/pretty-view-scroll-anchor/prototype.html`. Ships as fork patch #96.

Purpose:
- Fix the pretty-view scroll experience so a new turn always starts with the user's message pinned to the top-of-viewport (anchor ceiling) as content grows past a viewport, and short replies still ride the bottom naturally (unified `scrollTop = min(followBottomTop, anchorPinTop)`).
- Fix the good-to-go (ThumbsUp) button: currently just calls `handleQuickSend("good to go")` and relies on the broken scroll manager to catch up when the JSONL echo lands; new behavior jumps to bottom + enters user-driving/followBottom=true immediately on click so subsequent replies stay stuck to the tail.
- Retire the patch #88 tall-message top-align branch — the clamp rule subsumes it correctly.

Output:
- Rewritten `use-auto-scroll.ts` implementing the prototype's state machine in TypeScript (React-idiomatic: refs + ResizeObserver effect).
- New `use-auto-scroll.test.ts` covering the clamp math, mode transitions, and programmatic-vs-user scroll gating.
- Updated `PrettyView.tsx` — hook API surface change; anchor ref plumbing; onGoodToGo prop wired to ComposeBox; jump-to-latest pill onClick swapped to the new scroll-to-bottom-and-follow action.
- Updated `ComposeBox.tsx` — ThumbsUp button invokes `onGoodToGo` in addition to `handleQuickSend("good to go")`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/ubuntu/termix/.planning/STATE.md
@/home/ubuntu/termix/CLAUDE.md
@/home/ubuntu/.claude/identities/tina/bounties/pretty-view-scroll-anchor/prototype.html
@/home/ubuntu/termix/src/ui/features/pretty-view/use-auto-scroll.ts
@/home/ubuntu/termix/src/ui/features/pretty-view/PrettyView.tsx
@/home/ubuntu/termix/src/ui/features/pretty-view/ComposeBox.tsx
@/home/ubuntu/termix/src/ui/features/pretty-view/ChatMessage.tsx
@/home/ubuntu/termix/src/ui/api/claude-session-api.ts
</context>

<interface_notes>
Load-bearing facts the executor needs before writing code — do NOT re-derive from source; take these as given:

- **`MessageEvent`** (src/ui/api/claude-session-api.ts:31) has shape `{ type: "message", role: "user" | "assistant", content: string, eventId: string, ts: number }`. The `role` field distinguishes user vs assistant; `eventId` is the anchor-identity key (stable across re-renders; hash-based dedup key).
- **`StreamEvent`** (PrettyView.tsx:82) = `MessageEvent | ImageEvent`. Only `MessageEvent` with `role === "user"` is anchor-eligible. `ImageEvent` and `role === "assistant"` messages are NOT anchor candidates.
- **PrettyView does NOT observe local send.** `onSend` is a passthrough prop to ComposeBox; the user's message is not appended by PrettyView — it arrives via the JSONL echo through the WS as a `MessageEvent` with `role: "user"`, into `setMessages`. Therefore the anchor MUST be derived from the CURRENT `messages` state (the last user-role event), not from a "just sent" callback.
- **PrettyView already has an inner contentEl** (`<div ref={contentRef} className="flex flex-col gap-[18px]">` at PrettyView.tsx:512). Anchor DOM elements live in the ResizeObserver-observed subtree — good. To attach an anchor ref, wrap the map iteration so the div rendering the last user-role message gets a callback ref; do NOT modify ChatMessage's signature.
- **Current `useAutoScroll` API** (use-auto-scroll.ts:56): `useAutoScroll(messageCount: number) → { scrollRef, contentRef, scrollToBottom, isPinnedToBottom }`. This plan replaces it. New API:
  - Input: `{ messages: StreamEvent[] }` (hook selects anchor by scanning for last user-role event; imports StreamEvent type from PrettyView OR redefines a minimal `{ role?: string; eventId: string }` structural type).
  - Output: `{ scrollRef, contentRef, anchorRefCallback, scrollToBottomAndFollow, isPinnedToBottom, mode }`. `isPinnedToBottom` is preserved (derived: `mode === 'user-driving' && followBottom`) so the existing jump-to-latest pill visibility gate stays wired.
- **ComposeBox ThumbsUp handler** (ComposeBox.tsx:900): currently `onClick={() => handleQuickSend("good to go")}`. Change to invoke `onGoodToGo?.()` first, then `handleQuickSend("good to go")` — order matters (scroll before send so the mode flip is not racing the JSONL echo).
- **Prototype → TypeScript port**: prototype uses module-scope `let mode / followBottom / anchor / programmaticScroll`; port to React refs (`useRef`) so values survive re-renders without triggering them, and effects re-attach cleanly to `scrollEl` / `contentEl` swaps (they arrive via callback refs at commit time; must handle null → element → null transitions).
- **Vitest is the test runner** (package.json → `"test": "vitest run"`). JSDOM is the environment (see the existing `commandTags.test.ts` sibling). ResizeObserver does NOT exist in JSDOM — Wave 0 of Task 2 must polyfill it (see acceptance criteria).
</interface_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite useAutoScroll as clamp-anchor + Slack-follow state machine</name>
  <files>src/ui/features/pretty-view/use-auto-scroll.ts</files>
  <read_first>
    - /home/ubuntu/.claude/identities/tina/bounties/pretty-view-scroll-anchor/prototype.html (the `<script>` block IS the reference implementation — port function-for-function to TypeScript; DO NOT reinvent the state machine)
    - /home/ubuntu/termix/src/ui/features/pretty-view/use-auto-scroll.ts (current hook — full rewrite; keep the callback-ref pattern for scrollRef/contentRef, drop everything else)
    - /home/ubuntu/termix/src/ui/features/pretty-view/PrettyView.tsx lines 500-565 (see how contentRef wraps the message map — anchor DOM lives inside that subtree; you're changing the hook API surface, so you also need to know what PrettyView currently destructures)
    - /home/ubuntu/termix/src/ui/api/claude-session-api.ts lines 31-56 (MessageEvent + ImageEvent shapes; note `role` field on MessageEvent is the anchor selector)
  </read_first>
  <behavior>
    - anchorPinTop(anchorEl, scrollEl) returns `(anchorEl.getBoundingClientRect().top - scrollEl.getBoundingClientRect().top) + scrollEl.scrollTop`, matching prototype's `anchorPinTop()` at line 197-201.
    - followBottomTop(scrollEl) returns `Math.max(0, scrollEl.scrollHeight - scrollEl.clientHeight)`, matching prototype line 203-205.
    - applyClampRule() computes `target = min(followBottomTop, anchorPinTop)` and, if `|target - scrollEl.scrollTop| > 0.5`, calls doProgScroll(target) — matches prototype 207-219. When anchor is null, no-op.
    - doProgScroll(newTop) increments a `programmaticScrollRef.current` counter, sets `scrollEl.scrollTop = newTop`, then double-rAFs to decrement (`Math.max(0, count - 1)`) — matches prototype 188-195 verbatim.
    - Scroll event handler: if `programmaticScrollRef.current > 0`, ignore; else if `mode === 'clamp'` flip to `'user-driving'`; recompute `followBottom = (distFromBottom <= 100px)` — matches prototype 230-243.
    - ResizeObserver callback dispatches: `mode === 'clamp'` → applyClampRule(); `mode === 'user-driving' && followBottom` → scrollToBottom (i.e. doProgScroll(scrollHeight)); else no-op — matches prototype 246-254.
    - Anchor selection: the hook derives the anchor from the incoming `messages` prop by scanning back-to-front for the last event with `role === "user"`; its `eventId` is the anchor key. When the anchor key changes (new user message arrived), reset: mode = 'clamp', followBottom = false, and apply the clamp rule on the next microtask/rAF (so the DOM has committed and the anchor ref has fired).
    - scrollToBottomAndFollow() sets mode = 'user-driving', followBottom = true, and calls doProgScroll(scrollHeight) — this is the GTG action, matches prototype 380-386.
    - anchorRefCallback(el: HTMLElement | null) stores el into `anchorRef.current`; PrettyView attaches this to the wrapper div of the last user-role message.
    - New public API: `{ scrollRef, contentRef, anchorRefCallback, scrollToBottomAndFollow, isPinnedToBottom }` where `isPinnedToBottom` is state derived as `mode === 'user-driving' && followBottom` (preserves jump-to-latest pill visibility gate — pill shows when NOT pinned).
    - BOTTOM_THRESHOLD constant = 100 (px), matching prototype line 149. Delete the old 16px BOTTOM_TOLERANCE_PX; the new model uses 100px per the state machine spec.
    - REMOVED entirely: the patch #88 "tall-message top-align" branch (current lines 182-220). The clamp rule replaces it — do NOT preserve any messageCount-diff-driven `scrollTop = newEl.offsetTop` code path.
  </behavior>
  <action>
    Full rewrite of `src/ui/features/pretty-view/use-auto-scroll.ts`. Signature change: `useAutoScroll(messages: readonly Array<{ role?: string; eventId: string }>): { scrollRef, contentRef, anchorRefCallback, scrollToBottomAndFollow, isPinnedToBottom }`. Use `useRef` for `mode` ('clamp' | 'user-driving'), `followBottom` (boolean), `anchorEl` (HTMLElement | null), `programmaticScroll` (number counter), and `anchorEventId` (string | null, tracks which user-message eventId the current anchor represents). Use `useState` only for `isPinnedToBottom` (needed for pill re-render). Callback refs (`useCallback`) for `scrollRef`, `contentRef`, `anchorRefCallback`. Effects:
      (1) Attach `scroll` listener on scrollEl → handler per prototype 230-243. Use `{ passive: true }` (matches current hook).
      (2) Attach `ResizeObserver` on contentEl AND scrollEl → callback per prototype 246-254; disconnect on cleanup.
      (3) Effect keyed on `messages` (or on the derived anchor eventId) — when the derived last-user-role eventId differs from `anchorEventIdRef.current`, update the ref, set mode='clamp' + followBottom=false, and schedule `applyClampRule()` via `requestAnimationFrame` so DOM has committed.
    Port `doProgScroll` exactly as prototype 188-195 (double-rAF decrement). Port `applyClampRule`, `anchorPinTop`, `followBottomTop`, `scrollToBottom`, `isNearBottom` as internal helpers (closures inside effects, or module-scope pure functions that take scrollEl + anchorEl args — either fine, prefer pure functions with args for testability).
    Keep the header comment block explaining WHY the state machine is structured this way (mirror the prototype's script-block comment at prototype.html lines 98-134); replace the old comment block. Delete `BOTTOM_TOLERANCE_PX` (16px, old); add `BOTTOM_THRESHOLD = 100` (matches prototype).
    Export nothing new besides the updated `useAutoScroll`. Do NOT export the helper functions.
  </action>
  <verify>
    <automated>cd /home/ubuntu/termix &amp;&amp; npx tsc --noEmit 2&gt;&amp;1 | grep -E "src/ui/features/pretty-view/use-auto-scroll\.ts" ; test $? -eq 1</automated>
  </verify>
  <done>
    - `src/ui/features/pretty-view/use-auto-scroll.ts` exists with new signature `useAutoScroll(messages)`, new return type `{ scrollRef, contentRef, anchorRefCallback, scrollToBottomAndFollow, isPinnedToBottom }`.
    - `grep -c "messageHeight > viewportHeight" src/ui/features/pretty-view/use-auto-scroll.ts` returns 0 (patch #88 branch removed).
    - `grep -c "BOTTOM_TOLERANCE_PX" src/ui/features/pretty-view/use-auto-scroll.ts` returns 0 (old 16px constant deleted).
    - `grep -c "BOTTOM_THRESHOLD" src/ui/features/pretty-view/use-auto-scroll.ts` returns >= 1 (new 100px constant present).
    - `grep -cE "programmaticScroll|programmatic_scroll" src/ui/features/pretty-view/use-auto-scroll.ts` returns >= 2 (counter used at least once for increment site and once for decrement site).
    - `grep -c "ResizeObserver" src/ui/features/pretty-view/use-auto-scroll.ts` returns >= 1.
    - `npx tsc --noEmit` produces no errors in `use-auto-scroll.ts` (Task 2/3 handle downstream call-site drift).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for the clamp state machine + wire PrettyView + ComposeBox to the new API</name>
  <files>src/ui/features/pretty-view/use-auto-scroll.test.ts, src/ui/features/pretty-view/PrettyView.tsx, src/ui/features/pretty-view/ComposeBox.tsx</files>
  <read_first>
    - /home/ubuntu/termix/src/ui/features/pretty-view/commandTags.test.ts (existing vitest pattern in this directory — mirror the imports + test style)
    - /home/ubuntu/termix/vitest.config.ts (confirm JSDOM env; if ResizeObserver is missing from JSDOM you must polyfill it in the test file with a `class MockResizeObserver { observe() {} unobserve() {} disconnect() {} }` assigned to `globalThis.ResizeObserver` before importing the hook)
    - /home/ubuntu/termix/src/ui/features/pretty-view/use-auto-scroll.ts (the rewritten hook from Task 1 — under test)
    - /home/ubuntu/termix/src/ui/features/pretty-view/PrettyView.tsx lines 170-175 + 500-565 (current destructure + wiring — you're changing the destructure to include `anchorRefCallback` + `scrollToBottomAndFollow`, dropping `scrollToBottom`; also changing the arg from `messages.length` to `messages`, and adding a per-message wrapper div in the map that attaches anchorRefCallback to the last user-role message)
    - /home/ubuntu/termix/src/ui/features/pretty-view/ComposeBox.tsx lines 60-115 (props interface — add `onGoodToGo?: () =&gt; void`), lines 895-916 (ThumbsUp onClick — invoke onGoodToGo before handleQuickSend)
    - /home/ubuntu/.claude/identities/tina/bounties/pretty-view-scroll-anchor/prototype.html (reference for expected clamp math outputs when writing test fixtures)
  </read_first>
  <behavior>
    Test file `src/ui/features/pretty-view/use-auto-scroll.test.ts` covers:
    - **Test A (anchor selection):** given messages `[assistant, user#e1, assistant, user#e2, assistant]`, hook derives anchor eventId = `e2` (the LAST user-role event, not the first).
    - **Test B (anchor reset on new user message):** anchor eventId transitions `e1 → e2` when a new user message with different eventId is appended; mode is reset to `'clamp'` and followBottom to `false`.
    - **Test C (clamp math — follow-bottom branch):** with a mocked scrollEl where `scrollHeight=500, clientHeight=400, anchor offsetTop=100`, followBottomTop=100 and anchorPinTop=100 — target=100 (either interpretation lands here; content just barely fits).
    - **Test D (clamp math — anchor-pinned branch):** with `scrollHeight=1000, clientHeight=400, anchor offsetTop=100` (600px of content below anchor, > viewport), followBottomTop=600 and anchorPinTop=100 — target=100 (anchor ceiling wins, content lands below fold).
    - **Test E (clamp math — early-turn short reply):** with `scrollHeight=450, clientHeight=400, anchor offsetTop=200`, followBottomTop=50 and anchorPinTop=200 — target=50 (follow bottom wins; anchor irrelevant while content is short).
    - **Test F (programmatic scroll gate):** the programmatic-scroll counter must be > 0 while a doProgScroll is in flight; a scroll event fired synchronously after doProgScroll must NOT flip mode from 'clamp' to 'user-driving'. Verify via a spy on the internal mode ref or via observable side effect (mode-derived behavior).
    - **Test G (scrollToBottomAndFollow):** invoking the returned callback flips mode → 'user-driving', followBottom → true, and calls doProgScroll(scrollHeight). Verify `isPinnedToBottom` derived state becomes true.
    Prefer testing pure helpers (`anchorPinTop`, `followBottomTop`, `applyClampRule`-target-computation) as extracted pure functions taking element-like args; if Task 1 kept them as closures, extract minimal pure helpers to a colocated internal module or export them from `use-auto-scroll.ts` behind a `/* @internal for tests */` doc comment. Use `@testing-library/react`'s `renderHook` if available; if not, use `render` on a tiny test component that consumes the hook.
    Requires ResizeObserver polyfill at top of the test file — JSDOM does not ship it.

    PrettyView wiring changes:
    - Line 172-173 destructure: `const { scrollRef, contentRef, anchorRefCallback, scrollToBottomAndFollow, isPinnedToBottom } = useAutoScroll(messages);` (pass `messages`, not `messages.length`).
    - Message-map wrapper (around lines 513-530): compute `lastUserIdx = messages.findLastIndex(m =&gt; m.type === "message" &amp;&amp; m.role === "user")` once above the map. Wrap each ChatMessage/ImageBubble in a `<div>` (or use React fragments with keyed wrapper divs); attach `ref={anchorRefCallback}` to the wrapper of the message at `lastUserIdx`. Do NOT modify ChatMessage's signature.
    - Line 544 jump-to-latest pill onClick: change from `onClick={scrollToBottom}` to `onClick={scrollToBottomAndFollow}` (semantic parity: the pill also means "show me the tail; stay stuck to it").
    - ComposeBox invocation (~line 610): add `onGoodToGo={scrollToBottomAndFollow}` to the ComposeBox props.

    ComposeBox wiring changes:
    - Add `onGoodToGo?: () =&gt; void` to props interface (near line 76 alongside `onSend`).
    - Add `onGoodToGo` to the destructured props (near line 111).
    - Change ThumbsUp onClick (line 900) to: `onClick={() =&gt; { onGoodToGo?.(); handleQuickSend("good to go"); }}` — call `onGoodToGo` FIRST so the scroll action fires synchronously on click, then dispatch the "good to go" message. Ordering matters: the scroll must not wait for the JSONL echo of the "good to go" message to arrive.
  </behavior>
  <action>
    Create `src/ui/features/pretty-view/use-auto-scroll.test.ts` implementing tests A-G. Polyfill ResizeObserver at top of file: `class NoopResizeObserver { observe() {} unobserve() {} disconnect() {} } (globalThis as any).ResizeObserver = NoopResizeObserver;` BEFORE the `import { useAutoScroll } from "./use-auto-scroll"` statement (import order matters for global setup). Use `describe("useAutoScroll", () =&gt; { ... })` structure matching `commandTags.test.ts`. For DOM-dependent tests (C, D, E, F, G), construct a minimal `document.createElement("div")` for scroll container, manually set `scrollHeight`/`clientHeight` via `Object.defineProperty` on the element (JSDOM's default values are 0), and assign anchor DOM as another child div. If `applyClampRule` and its helpers are not exported, extract them into a colocated `use-auto-scroll-internals.ts` module in this same task (both hook and test import from it) — this is a legitimate refactor for testability, not scope creep.

    Edit `src/ui/features/pretty-view/PrettyView.tsx`:
    (a) Change line 172-173 destructure and call: swap `useAutoScroll(messages.length)` → `useAutoScroll(messages)`, add `anchorRefCallback` and `scrollToBottomAndFollow` to destructure (keep `scrollRef`, `contentRef`, `isPinnedToBottom`; DROP `scrollToBottom`).
    (b) Above the message map (around line 513), compute `const lastUserMsgIdx = messages.findLastIndex(m =&gt; m.type === "message" &amp;&amp; m.role === "user");` (Array.prototype.findLastIndex is ES2023 / TypeScript lib.es2023 — check tsconfig; if unavailable, use a manual reverse for-loop).
    (c) Wrap each mapped message element in a `&lt;div key={m.eventId} ref={idx === lastUserMsgIdx ? anchorRefCallback : undefined}&gt;...&lt;/div&gt;` (use the map's `(m, idx) =&gt;` form). Keep the existing key on the wrapper (move it OFF the ChatMessage/ImageBubble, ONTO the wrapper div). Do not alter WipBubble/PlanPendingBubble.
    (d) Line 544 pill: `onClick={scrollToBottomAndFollow}`.
    (e) ComposeBox instantiation (~line 611): add `onGoodToGo={scrollToBottomAndFollow}` as a prop.

    Edit `src/ui/features/pretty-view/ComposeBox.tsx`:
    (a) Add `onGoodToGo?: () =&gt; void;` to the props interface adjacent to `onSend` (around line 76).
    (b) Add `onGoodToGo` to the destructured props on the function signature (around line 111).
    (c) Change the ThumbsUp button onClick (line 900) from `onClick={() =&gt; handleQuickSend("good to go")}` to `onClick={() =&gt; { onGoodToGo?.(); handleQuickSend("good to go"); }}`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/termix &amp;&amp; npx vitest run src/ui/features/pretty-view/use-auto-scroll.test.ts</automated>
  </verify>
  <done>
    - `src/ui/features/pretty-view/use-auto-scroll.test.ts` exists; all tests A-G pass under `npx vitest run src/ui/features/pretty-view/use-auto-scroll.test.ts`.
    - `grep -c "anchorRefCallback" src/ui/features/pretty-view/PrettyView.tsx` returns >= 2 (destructure + attachment).
    - `grep -c "scrollToBottomAndFollow" src/ui/features/pretty-view/PrettyView.tsx` returns >= 2 (destructure + pill onClick + ComposeBox prop = actually >= 3, at minimum 2).
    - `grep -v '^\s*//' src/ui/features/pretty-view/PrettyView.tsx | grep -c "scrollToBottom\b"` returns 0 (old API name gone from live code; the `\b` word boundary prevents matching `scrollToBottomAndFollow`; comment lines excluded so leftover doc references don't fail the gate).
    - `grep -c "onGoodToGo" src/ui/features/pretty-view/ComposeBox.tsx` returns >= 2 (prop type + destructure); `grep -c "onGoodToGo" src/ui/features/pretty-view/PrettyView.tsx` returns >= 1 (prop pass).
    - `grep -A1 'onClick={() =>' src/ui/features/pretty-view/ComposeBox.tsx | grep -c "onGoodToGo"` returns >= 1 (the ThumbsUp onClick calls onGoodToGo).
    - `npx tsc --noEmit` produces no errors in any of the three modified files.
    - `npx vitest run` for the whole repo passes (regression gate — no other tests broken by API change).
    - `npm run build` completes successfully (verifies Vite bundles the new hook + call sites).
  </done>
</task>

</tasks>

<verification>
Final phase gates (executor runs these after both tasks complete):

1. `cd /home/ubuntu/termix && npx tsc --noEmit` — zero errors.
2. `cd /home/ubuntu/termix && npx vitest run` — all tests pass, including the new `use-auto-scroll.test.ts`.
3. `cd /home/ubuntu/termix && npm run build` — clean Vite build.
4. `grep -c "messageHeight > viewportHeight" src/ui/features/pretty-view/use-auto-scroll.ts` — returns 0 (patch #88 branch is gone; the clamp rule owns tall-message behavior).
5. `grep -v '^\s*//' src/ui/features/pretty-view/PrettyView.tsx | grep -c "scrollToBottom\b"` — returns 0 (old hook return name eliminated from live code; comment lines excluded).
6. `grep -c "anchorRefCallback" src/ui/features/pretty-view/use-auto-scroll.ts` — returns >= 1 (anchor ref callback exported from hook).
7. Manual smoke (deferred to Ashley's UAT — not gated here; noted for the summary):
   (a) Send a message; short assistant reply → view follows bottom, anchor visible but view not clamped.
   (b) Send a message; multi-viewport streaming assistant reply → user message stays pinned at top of viewport; streaming content lands below the fold.
   (c) During (b), scroll down manually → mode flips to user-driving; if within 100px of bottom, followBottom on; further growth stays stuck to tail.
   (d) During (b), scroll up manually → mode user-driving, followBottom=false; further growth does NOT move the view.
   (e) Click good-to-go button → view jumps to bottom immediately AND stays stuck to bottom as the reply comes in.
   (f) Jump-to-latest pill (arrow-down button) → same behavior as GTG (jump + follow).
</verification>

<success_criteria>
Plan is complete when:

- [ ] `use-auto-scroll.ts` implements the prototype's state machine faithfully (clamp / user-driving modes, followBottom flag, anchor ref, programmatic-scroll counter, ResizeObserver dispatch, unified clamp rule `scrollTop = min(followBottomTop, anchorPinTop)`).
- [ ] Patch #88's `messageHeight > viewportHeight` top-align branch is DELETED from use-auto-scroll.ts.
- [ ] `use-auto-scroll.test.ts` provides > 0 automated coverage of anchor selection, clamp math (both branches), and programmatic-scroll gating; passes under `npx vitest run`.
- [ ] `PrettyView.tsx` derives the anchor DOM ref from the last user-role message via `anchorRefCallback`; passes `scrollToBottomAndFollow` to both the jump-to-latest pill and the ComposeBox as `onGoodToGo`.
- [ ] `ComposeBox.tsx` accepts an `onGoodToGo` prop and invokes it in the ThumbsUp click handler BEFORE `handleQuickSend("good to go")` (so the scroll action is not gated on the JSONL echo landing).
- [ ] `npx tsc --noEmit` clean; `npx vitest run` all-green; `npm run build` clean.
- [ ] SUMMARY.md documents: what the state machine does, why the clamp rule subsumes patch #88, the new hook API surface, and the ComposeBox `onGoodToGo` prop addition — for the pin-time `termix-patches.md` patch #96 entry.
</success_criteria>

<output>
Create `.planning/quick/260720-6rl-pretty-view-scroll-model-clamp-anchor-sl/260720-6rl-SUMMARY.md` when done. SUMMARY must include:
- Full state machine port note (which prototype functions → which TypeScript identifiers).
- The old-vs-new hook API surface diff (for future readers grepping for `scrollToBottom` and finding only `scrollToBottomAndFollow`).
- The `onGoodToGo` prop addition on ComposeBox and its ordering-matters rationale.
- A one-line note that patch #88's tall-message top-align branch was removed (so the removal doesn't look accidental to a future rebaser).
- Explicit reminder for the pin-time turn: bump `termix-patches.md` header 95 → 96, add per-patch entry (files touched: use-auto-scroll.ts, use-auto-scroll.test.ts, PrettyView.tsx, ComposeBox.tsx), update the "Patch drift caveat" file list, and follow `~/.claude/identities/tina/deploy-runbook.md` (mandatory 15-min deadman) at deploy time — NOT during this plan's execution (Ashley handles deploy separately per fleet rule).
</output>
