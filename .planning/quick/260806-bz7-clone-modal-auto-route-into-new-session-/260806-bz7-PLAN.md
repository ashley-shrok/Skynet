---
phase: 260806-bz7-clone-modal-auto-route-into-new-session-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/sidebar/NewSessionDialog.tsx
  - src/ui/sidebar/CloneAgentDialog.tsx
  - src/ui/sidebar/CloneAgentDialog.test.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.clone-dialog.test.tsx
  - src/ui/AppShell.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "After a successful Clone, the app routes into a new terminal tab on the cloned identity's tmux session (parity with identity-birth's focus-follow) — no manual click required."
    - "The clone-driven session opens with allowCreateTmux=false (the backend already created the tmux session) — the frontend attaches instead of racing to create."
    - "NewSessionOnCreateOpts gains a third discriminant that models 'open a session in an already-created identity' WITHOUT requiring birth-only fields (brief, avatarCandidateId, colorHue)."
    - "Vitest suite stays green: `npx vitest run` exits 0."
  artifacts:
    - path: "src/ui/sidebar/NewSessionDialog.tsx"
      provides: "Widened NewSessionOnCreateOpts discriminated union with 3rd variant for existing-identity session open"
      contains: "identityMode:"
    - path: "src/ui/sidebar/CloneAgentDialog.tsx"
      provides: "onCreateSession prop wired to fire after successful clone, BEFORE onClose"
      contains: "onCreateSession"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "Threads onCreateSession from AppShell down into CloneAgentDialog"
      contains: "onCreateSession"
    - path: "src/ui/AppShell.tsx"
      provides: "onCreateSession callback narrows on the widened union — treats 'existing' identically to identityMode:true for openTab (allowCreateTmux false, sessionName from identity)"
  key_links:
    - from: "src/ui/sidebar/CloneAgentDialog.tsx"
      to: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      via: "onCreateSession prop fired inside handleSubmit success branch, before onClose()"
      pattern: "onCreateSession\\("
    - from: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      to: "src/ui/AppShell.tsx"
      via: "existing onCreateSession prop already plumbed via PrettyConversationsPanel props"
      pattern: "onCreateSession"
    - from: "src/ui/AppShell.tsx"
      to: "openTab"
      via: "callback branches on identityMode discriminant; treats 'existing' like true for allowCreateTmux/sessionName resolution"
      pattern: "allowCreateTmux"
---

<objective>
Give the Clone flow the same auto-route-into-new-session behavior that identity birth has: after a successful clone, the app opens a terminal tab on the cloned identity's tmux session and focuses it — no manual click. Currently the clone dialog closes with no navigation, so Ashley has to hunt for the new row in the sidebar and click it.

Purpose: Parity with birth. Birth already routes on success (NewSessionDialog → onCreate → AppShell onCreateSession → openTab + selectConversationDeferred). Clone should feel identical from the user's POV: fill form → click Create → land in the new session.

Output: Widened NewSessionOnCreateOpts discriminated union, CloneAgentDialog wired with an onCreateSession prop that fires on success before onClose, PrettyConversationsPanel threading the existing onCreateSession prop down to CloneAgentDialog, AppShell callback handling the new variant, and all tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/ui/sidebar/NewSessionDialog.tsx
@src/ui/sidebar/CloneAgentDialog.tsx
@src/ui/sidebar/CloneAgentDialog.test.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.clone-dialog.test.tsx
@src/ui/AppShell.tsx
@src/ui/api/identities-api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen NewSessionOnCreateOpts with a third variant for existing-identity session open</name>
  <files>src/ui/sidebar/NewSessionDialog.tsx, src/ui/AppShell.tsx</files>
  <behavior>
    - The exported type `NewSessionOnCreateOpts` in `src/ui/sidebar/NewSessionDialog.tsx` gains a THIRD variant discriminated on `identityMode: "existing"`. It carries the minimal fields needed to open a session in an identity that is ALREADY BORN server-side (i.e. no birth stream, no avatar candidate, no brief). Required fields: `host: Host`, `sessionName: string` (the identity key — doubles as tmux session name per the Nelly mechanism), `path: string`, `identityMode: "existing"`, `identityName: string`, `identityId: string`. NO `brief`, NO `avatarCandidateId`, NO `colorHue`, NO `title/voice` (all irrelevant — identity already exists).
    - AppShell's `onCreateSession` callback (currently at src/ui/AppShell.tsx line ~1442-1462, keyed on `opts.identityMode` as a boolean) narrows on the widened union so that the `"existing"` variant is handled identically to `identityMode: true` for the openTab call: `sessionName = opts.identityName` (or opts.sessionName — both equal the identity key), `allowCreateTmux: false` (backend already created the tmux session, same as birth's step 2). Do NOT introduce a new openTab code path — the two branches converge on the same openTab args except for where `sessionName` is sourced from.
    - The existing `opts.identityMode ? opts.name : opts.sessionName` truthy-check line at ~L1447 must be rewritten to switch on the discriminant explicitly (e.g. a small `if (opts.identityMode === false)` / `else if (opts.identityMode === "existing")` / `else` cascade — since `identityMode: true` still uses `opts.name`). Same for the `!opts.identityMode` line at ~L1457 (rewrite as `allowCreateTmux: opts.identityMode === false`). Both existing behaviors (regular session + birth) preserve their previous semantics — the widening is purely additive.
    - Type-check must pass: `npx tsc --noEmit` exit 0 with no NEW type errors introduced. If existing errors are present in unrelated files, confirm they predate this change.
    - No behavior change for the existing two variants — grep and confirm every existing call site of `onCreate(opts)` inside NewSessionDialog.tsx (birth-success path at line ~633 and regular-session path at ~L1120) still passes exactly the same shape as before. The widening ADDS a case; it must not alter existing ones.
  </behavior>
  <action>
    Widen `NewSessionOnCreateOpts` in `src/ui/sidebar/NewSessionDialog.tsx` (currently at lines 105-118) by adding a third arm to the discriminated union: `{ host: Host; sessionName: string; path: string; identityMode: "existing"; identityName: string; identityId: string }`. Keep the two existing arms byte-identical. Update the comment block above the type to document the third variant's purpose (opening a session in an already-born identity — clone flow — where no birth stream runs). Then update `src/ui/AppShell.tsx` at the `onCreateSession={(opts) => {...}}` block (around L1442-1462): replace the truthy `opts.identityMode ? opts.name : opts.sessionName` expression with an explicit discriminant switch that returns `opts.sessionName` for `false`, `opts.name` for `true`, and `opts.identityName` (or equivalently `opts.sessionName`) for `"existing"`; replace `allowCreateTmux: !opts.identityMode` with `allowCreateTmux: opts.identityMode === false`. Do NOT touch openTab, selectConversationDeferred, or the mobile navigation calls — they run for all three variants identically.

    VERIFY afterward: grep `identityMode` across `src/ui/**/*.{ts,tsx}` and confirm every read site either (a) uses explicit discriminant comparison (`=== false`, `=== true`, `=== "existing"`) or (b) reads a field only accessible in a specific narrowed branch. No implicit truthy checks on `identityMode` should remain in tsx files.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx tsc --noEmit 2>&1 | grep -E "NewSessionDialog|AppShell|identityMode" ; echo "---EXIT:$?"</automated>
  </verify>
  <done>
    - Type `NewSessionOnCreateOpts` in NewSessionDialog.tsx has three variants; the `"existing"` variant has exactly: `host`, `sessionName`, `path`, `identityMode`, `identityName`, `identityId` (no other fields).
    - AppShell's onCreateSession callback compiles under `npx tsc --noEmit` with no new type errors touching NewSessionDialog / AppShell / identityMode.
    - Grep for `opts.identityMode` in AppShell returns only explicit-discriminant reads (=== false, === true, === "existing"), no bare truthy checks.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add onCreateSession prop to CloneAgentDialog and fire it on successful clone before onClose</name>
  <files>src/ui/sidebar/CloneAgentDialog.tsx, src/ui/sidebar/CloneAgentDialog.test.tsx</files>
  <behavior>
    - `CloneAgentDialogProps` gains an OPTIONAL new prop: `onCreateSession?: (opts: NewSessionOnCreateOpts) => void` (import the type from `@/sidebar/NewSessionDialog`). When provided, it is invoked from inside `handleSubmit` immediately after `cloneIdentity(...)` resolves successfully, AFTER the existing `onCloned?.(result)` call, and BEFORE the existing `onClose()`. Call site passes an opts object with `identityMode: "existing"` shape: `{ host, sessionName: result.identityKey, path: path.trim() || "~", identityMode: "existing", identityName: result.identityKey, identityId: result.id }`. `host` is a Host object — but CloneAgentDialog only has `hostId: number | null` today; we need the parent to also pass in the source Host so we can forward it (Task 3 handles the parent side). To keep this task self-contained: EITHER (option A) add a `sourceHost?: Host | null` prop that the parent will populate in Task 3, OR (option B) accept a `buildSessionOpts?: (identity: Identity) => NewSessionOnCreateOpts | null` factory prop from the parent. **Chosen: option A** — sourceHost prop. Rationale: sourceHost is a piece of data (mirrors hostId), not a policy — same shape as hostId; simpler for tests to seed.
    - When `onCreateSession` is undefined (backward compat with any caller not yet threading it — none in production today but the prop being optional keeps the default sane), the success path is unchanged: `onCloned?.(result)` → `onClose()`. No crash, no throw, no console error.
    - When `onCreateSession` is provided but `sourceHost` is null OR the factory-safety check throws, do NOT block the modal from closing. Wrap the `onCreateSession(opts)` call in a try/catch (same pattern as the existing `onCloned` invocation at line ~184-188): swallow errors so a routing failure does not leave the modal open. Log nothing (best-effort — the modal already closes and the row appears in the sidebar via the parent's `onCloned` refresh; the user can click it manually).
    - `path` field in the opts object uses the SAME `path.trim()` value that was sent to `cloneIdentity` (line ~181) — normalize once, use twice.
    - Tests: update CloneAgentDialog.test.tsx.
      * Extend Test 21 (or add Test 21b) to assert that when `onCreateSession` prop is provided AND `sourceHost` is provided, it is called EXACTLY ONCE after a successful clone, BEFORE onClose, with the exact `{ host, sessionName, path, identityMode: "existing", identityName, identityId }` shape derived from the mock clone response.
      * Add Test 21c: when `onCreateSession` is undefined, the existing success path (onCloned → onClose) still fires unchanged (regression gate).
      * Add Test 21d: when `onCreateSession` throws, `onClose` still fires (try/catch swallow behavior).
    - All existing tests (17-24) must still pass verbatim.
  </behavior>
  <action>
    Edit `src/ui/sidebar/CloneAgentDialog.tsx`:
    1. Import `NewSessionOnCreateOpts` type from `@/sidebar/NewSessionDialog` and `Host` from `@/types/ui-types`.
    2. Add two new optional props to `CloneAgentDialogProps`: `sourceHost?: Host | null` (the source identity's Host — same origin as hostId, from the panel) and `onCreateSession?: (opts: NewSessionOnCreateOpts) => void`.
    3. Accept both props in the function signature destructure (alongside the existing `onCloned`).
    4. Inside `handleSubmit`, in the success branch (after `cloneIdentity` resolves, currently line ~174-190), keep the existing `onCloned?.(result)` try/catch, then INSERT a new block before `onClose()`: if `onCreateSession && sourceHost`, build `const normalizedPath = path.trim();` (or reuse the trimmed value already sent to cloneIdentity) and call `onCreateSession({ host: sourceHost, sessionName: result.identityKey, path: normalizedPath, identityMode: "existing", identityName: result.identityKey, identityId: result.id })` inside a try/catch that swallows errors. Then call `onClose()` as before.
    5. Do NOT change any UI, any state, any other handlers. This is a pure success-path wiring change.

    Then update `src/ui/sidebar/CloneAgentDialog.test.tsx`:
    1. Import `NewSessionOnCreateOpts` type (if needed) and a Host stub helper (mirror `stubHost` from PrettyConversationsPanel.clone-dialog.test.tsx — inline a small `makeHost()` if that helper isn't shared).
    2. Extend Test 21 to also pass `sourceHost={stubHost}` and `onCreateSession={vi.fn()}` and assert (a) `onCreateSession` was called EXACTLY once with the derived opts shape (host: stubHost, sessionName: "tina-2" [from the mockCloneIdentity resolved value], path: "~", identityMode: "existing", identityName: "tina-2", identityId: "new-id"), (b) it was called BEFORE `onClose` (compare `mock.invocationCallOrder`), (c) `onClose` was still called once.
    3. Add Test 21c: render without `onCreateSession`, submit, assert `onClose` is called once and no crash.
    4. Add Test 21d: render with an `onCreateSession` that throws synchronously, submit, assert `onClose` STILL fires (best-effort swallow).

    NO OTHER TESTS need to change — Tests 17-20, 22-24 stay identical.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx vitest run src/ui/sidebar/CloneAgentDialog.test.tsx 2>&1 | tail -30 ; echo "---EXIT:$?"</automated>
  </verify>
  <done>
    - `CloneAgentDialogProps` has `sourceHost?: Host | null` and `onCreateSession?: (opts: NewSessionOnCreateOpts) => void`.
    - Successful clone path fires `onCreateSession(opts)` (inside try/catch) BEFORE `onClose()` when both `onCreateSession` and `sourceHost` are provided.
    - `npx vitest run src/ui/sidebar/CloneAgentDialog.test.tsx` exits 0; the extended Test 21 and new 21c/21d pass; Tests 17-20, 22-24 unchanged and still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Thread onCreateSession + sourceHost from AppShell → PrettyConversationsPanel → CloneAgentDialog</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx, src/ui/features/pretty-conversations/PrettyConversationsPanel.clone-dialog.test.tsx</files>
  <behavior>
    - `PrettyConversationsPanel` already receives `onCreateSession?: (opts: NewSessionOnCreateOpts) => void` (line 209). No new prop on the panel is needed — reuse the existing one.
    - `cloneDialogState` (line 407) currently holds `{ sourceIdentity: Identity; hostId: number }`. Widen it to ALSO capture the source Host object: `{ sourceIdentity: Identity; hostId: number; sourceHost: Host }`. `handleRowClone` (line ~683) already resolves `row.host` (guards non-null at line 688) before setting state — capture that same Host into the new field.
    - The CloneAgentDialog render site (line 1137-1168) forwards `sourceHost={cloneDialogState?.sourceHost ?? null}` and `onCreateSession={onCreateSession}` (the panel's own prop — pass through by reference; no wrapping). If `onCreateSession` prop is undefined at the panel level (some tests don't provide it), forward `undefined` — CloneAgentDialog's optional prop handles that gracefully.
    - The panel's existing `onCloned` callback (which does the identities-store + fleetSessions refresh, currently at line 1142-1167) MUST stay wired as-is. It runs BEFORE onCreateSession in CloneAgentDialog's handleSubmit (Task 2 preserved that order: onCloned → onCreateSession → onClose). Refresh-before-route is correct because the newly-opened tab wants the identity resolved when the row renders (same rationale as patch #319 for birth).
    - Tests: update PrettyConversationsPanel.clone-dialog.test.tsx.
      * Extend Test 16 (or add Test 16b): after clicking Clone → dialog opens, assert that the rendered `<CloneAgentDialog>` receives an `onCreateSession` prop. Since the dialog is a real component (not a mock), assert INDIRECTLY: simulate a successful clone (fill name + click Create, with `cloneIdentity` mocked to resolve to a new Identity), then assert the `onCreateSession` vi.fn() passed to the PANEL was called with the expected opts shape (identityMode: "existing", host: stubHost, sessionName + identityName from the mock resolved identity's identityKey). This is a full-panel integration test — it exercises the same wiring path AppShell will use.
      * If the existing Test 16 does not currently mock `cloneIdentity`, add a `vi.mock("@/api/identities-api", ...)` block at the top of the file mirroring CloneAgentDialog.test.tsx's mock pattern (mock `cloneIdentity` + `postGenerateAvatarBatch` + re-export the real `IdentityCloneCollisionError`). Keep Test 16's original assertions intact.
    - Sanity check `PrettyConversationsPanel.chain.test.tsx`: no change expected — chain tests don't touch clone. Confirm `npx vitest run PrettyConversationsPanel.chain.test.tsx` still passes.
  </behavior>
  <action>
    Edit `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`:
    1. Update the `cloneDialogState` useState type (line 407-410) to include `sourceHost: Host`. Import `Host` from `@/types/ui-types` if not already imported at the top of the file (check line ~86 imports).
    2. In `handleRowClone` (line ~683-692), after the `if (!row.host) return;` guard, capture `row.host` and include it in the setCloneDialogState call: `setCloneDialogState({ sourceIdentity: identity, hostId: hostIdNum, sourceHost: row.host });`.
    3. Update the `<CloneAgentDialog>` render (line 1137-1168) to pass two new props:
       - `sourceHost={cloneDialogState?.sourceHost ?? null}`
       - `onCreateSession={onCreateSession}` — forward the panel's existing onCreateSession prop by reference.
    4. Leave the existing `onCloned` callback UNCHANGED (identities-store refresh + fleetSessions refresh stays — CloneAgentDialog fires onCloned BEFORE onCreateSession, so the refresh completes before the new tab tries to resolve the identity).

    Then update `src/ui/features/pretty-conversations/PrettyConversationsPanel.clone-dialog.test.tsx`:
    1. At the top of the file, after the existing vi.mock blocks, add a `vi.mock("@/api/identities-api", async (importOriginal) => { ... cloneIdentity: mockCloneIdentity, postGenerateAvatarBatch: mockPostGenerateAvatarBatch ... })` block (mirror CloneAgentDialog.test.tsx lines 41-52 and 76-77). Define the mocks at module top with `const mockCloneIdentity = vi.fn(); const mockPostGenerateAvatarBatch = vi.fn();`.
    2. Also mock `@/features/pretty-view/pickers/VoicePicker` to a simple <input> (mirror CloneAgentDialog.test.tsx lines 55-74) so the dialog renders in jsdom without pulling the real picker.
    3. In `beforeEach`, seed `mockCloneIdentity.mockResolvedValue({ id: "new-id", identityKey: "tina-2", displayName: "tina-2", title: "Fleet Operator", colorHue: 128, voice: "Elena.wav", avatarMime: "image/png", avatarUrl: "/identities/new-id/avatar", avatarEtag: "e", createdAt: "", updatedAt: "" })`.
    4. Add a new test "Test 16b: successful clone fires panel's onCreateSession with identityMode:'existing' opts BEFORE onClose": render the panel with `onCreateSession={onCreateSessionMock}`, right-click row → click Clone → dialog opens → fill name (`fireEvent.change(getByLabelText(/^name/i), { target: { value: "tina-2" } })`) → click Create button (`getByRole("button", { name: /clone|create/i })`) → `await waitFor(() => expect(mockCloneIdentity).toHaveBeenCalledTimes(1))` → `await waitFor(() => expect(onCreateSessionMock).toHaveBeenCalledTimes(1))` → assert the opts arg matches `{ host: stubHost, sessionName: "tina-2", path: "~", identityMode: "existing", identityName: "tina-2", identityId: "new-id" }`.
    5. Do NOT modify Test 16 — it stays intact as the wiring-existence check. Test 16b is the success-path integration check.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.clone-dialog.test.tsx src/ui/features/pretty-conversations/PrettyConversationsPanel.chain.test.tsx 2>&1 | tail -40 ; echo "---EXIT:$?"</automated>
  </verify>
  <done>
    - `cloneDialogState` includes `sourceHost: Host` and `handleRowClone` populates it from `row.host`.
    - `<CloneAgentDialog>` receives `sourceHost` and `onCreateSession` props from the panel.
    - `npx vitest run PrettyConversationsPanel.clone-dialog.test.tsx PrettyConversationsPanel.chain.test.tsx` exits 0 with Test 16 unchanged and new Test 16b passing.
  </done>
</task>

<task type="auto">
  <name>Task 4: Full vitest run + tsc gate — nothing else in the suite regresses</name>
  <files></files>
  <action>
    Run the full vitest suite and a full type-check to confirm no other test file is broken by the union widening or the CloneAgentDialog prop additions (both new props are OPTIONAL, so existing call sites should still typecheck without changes — but verify). This is a pure gate task; no code changes. If either command exits non-zero, diagnose the failure and fix in the appropriate file (typically another test file that manually stubs `NewSessionOnCreateOpts` with the old two-variant shape — the union widening is additive, so this should be a no-op for well-typed tests, but a test using `as NewSessionOnCreateOpts` with an underspecified object may need an `identityMode: false` narrowing hint).

    Do NOT run `npm run build` (backend build not needed — no backend changes). Do NOT run docker anything. Do NOT push. Executor commits atomically on `feat/tab-title-from-tmux` after this task passes.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx tsc --noEmit 2>&1 | tail -20 ; TSC=$? ; npx vitest run 2>&1 | tail -20 ; VIT=$? ; echo "---TSC:$TSC VIT:$VIT"</automated>
  </verify>
  <done>
    - `npx tsc --noEmit` exit 0 (or unchanged from baseline pre-existing errors — no new errors introduced by this plan's files).
    - `npx vitest run` exit 0.
    - Executor commits all six modified files in a single commit on `feat/tab-title-from-tmux` with a message like `feat(clone-modal): auto-route into new session after clone (parity with birth)` — no push, no other side effects.
  </done>
</task>

</tasks>

<verification>
  End-to-end verification that the plan delivered the goal:

  1. **Type gate**: `npx tsc --noEmit` exits 0 (or unchanged from baseline).
  2. **Test gate**: `npx vitest run` exits 0. Specifically the following files stay green (spot-check individually if the aggregate fails):
     - `src/ui/sidebar/CloneAgentDialog.test.tsx` (Tests 17-24 + new 21b/21c/21d)
     - `src/ui/features/pretty-conversations/PrettyConversationsPanel.clone-dialog.test.tsx` (Test 16 + new 16b)
     - `src/ui/features/pretty-conversations/PrettyConversationsPanel.chain.test.tsx` (no regression)
     - `src/ui/sidebar/NewSessionDialog.test.tsx` (union widening is additive — should be no-op)
  3. **Grep sanity**: `grep -rn "identityMode" src/ui/**/*.{ts,tsx}` shows explicit discriminant reads (`=== "existing"` or `=== true` or `=== false`) at the AppShell callback; no lingering bare `!opts.identityMode` truthy checks.
  4. **Manual smoke (optional, defer to human)**: In dev, right-click a conversation row → Clone → fill name → Create → confirm the app auto-routes into a new terminal tab on the cloned identity's session (no manual sidebar click needed). Same UX as birth's focus-follow. This is the goal-observable behavior; the automated tests exercise the wiring path but a human check confirms the openTab / attachment actually renders.
</verification>

<success_criteria>
  - Six files modified: NewSessionDialog.tsx, AppShell.tsx, CloneAgentDialog.tsx, CloneAgentDialog.test.tsx, PrettyConversationsPanel.tsx, PrettyConversationsPanel.clone-dialog.test.tsx.
  - `NewSessionOnCreateOpts` is a three-way discriminated union; the third arm carries only `host`, `sessionName`, `path`, `identityMode: "existing"`, `identityName`, `identityId` — no birth-only fields.
  - CloneAgentDialog fires `onCreateSession(opts)` (inside try/catch) after successful clone, BEFORE `onClose()`, when both props are provided.
  - PrettyConversationsPanel forwards its existing `onCreateSession` prop (unmodified) plus the newly-captured `sourceHost` into CloneAgentDialog.
  - AppShell's `onCreateSession` callback handles all three variants; the `"existing"` variant behaves identically to `identityMode: true` for openTab (allowCreateTmux: false, sessionName sourced from the identity).
  - `npx vitest run` exit 0; `npx tsc --noEmit` exit 0 (no new type errors).
  - One atomic commit on `feat/tab-title-from-tmux`. No push, no docker, no backend build.
</success_criteria>

<output>
  Executor commits changes on `feat/tab-title-from-tmux` (single commit). No SUMMARY file required for quick-mode plans — the orchestrator handles patch numbering and bounty updates after execution.
</output>
