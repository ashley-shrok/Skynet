---
phase: quick-260829-qb9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/RelayInboundBubble.tsx
  - src/ui/features/pretty-view/RelayInboundBubble.test.tsx
  - src/ui/features/pretty-view/RelayOutboundBubble.tsx
  - src/ui/features/pretty-view/RelayOutboundBubble.test.tsx
  - src/ui/features/pretty-view/ChatMessage.tsx
  - src/ui/features/pretty-view/ChatMessage.test.tsx
autonomous: true
requirements:
  - QB9-01
tags:
  - pretty-view
  - ui
  - collapse
---

<objective>
Collapse all left-side (agent-produced) message bubbles in PrettyView by default:
`RelayInboundBubble`, `RelayOutboundBubble`, and the assistant branch of
`ChatMessage`. Right-side user bubbles (Ashley's own messages) render
byte-identically to today.

Purpose: Left-side bubbles clutter Ashley's read of the agent she's actually
talking to. She rarely reads them but wants them available on demand.

Output: Three components each start collapsed showing only their header row
(existing header for the two Relay bubbles; a new minimal `● assistant` pill
for ChatMessage). Clicking the header expands the body; clicking again
re-collapses. State is per-mount (no persistence). Right-side user bubble is
untouched.

Fleet rule (Ashley 2026-08-29, VERBATIM): Skynet has NO message streaming
anywhere. No streaming carve-out, no auto-expand-while-streaming, no
streaming state — do not add any streaming affordances or gates. All
bubbles render atomically.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@src/ui/features/pretty-view/RelayInboundBubble.tsx
@src/ui/features/pretty-view/RelayOutboundBubble.tsx
@src/ui/features/pretty-view/ChatMessage.tsx
@src/ui/features/pretty-view/RelayInboundBubble.test.tsx
@src/ui/features/pretty-view/RelayOutboundBubble.test.tsx
@src/ui/features/pretty-view/ChatMessage.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add collapse-by-default to all three left-side bubble types (tests + implementation)</name>
  <files>
    src/ui/features/pretty-view/RelayInboundBubble.tsx,
    src/ui/features/pretty-view/RelayInboundBubble.test.tsx,
    src/ui/features/pretty-view/RelayOutboundBubble.tsx,
    src/ui/features/pretty-view/RelayOutboundBubble.test.tsx,
    src/ui/features/pretty-view/ChatMessage.tsx,
    src/ui/features/pretty-view/ChatMessage.test.tsx
  </files>
  <behavior>
    Shared behavior (applies to all three components):
    - On initial mount, bubble is collapsed. Body + footer are NOT in the DOM.
    - Header row is a native `<button type="button">` with `aria-expanded={!collapsed}`
      and `aria-label` that switches between "Expand message" (when collapsed) and
      "Collapse message" (when expanded).
    - Header row shows a small caret at the end: `▶` when collapsed, `▼` when expanded.
      (This is the outer-bubble caret. RelayOutboundBubble already has an inner
      `▸/▾ raw command` toggle for `rawExpanded` — that inner toggle is unrelated
      and MUST remain intact and independent.)
    - Clicking the header toggles collapsed state. Second click re-collapses.
    - Enter/Space work via native button semantics (no custom key handling needed).
    - No persistence — remount = collapsed again. No streaming carve-out.
    - Outer bubble chrome (padding, border, gradient, shadow, wrapper `flex justify-start`)
      is preserved on both collapsed and expanded state so the row does not jump
      on toggle. Only the inner body + footer are conditionally hidden.

    Per-component test cases:

    RelayInboundBubble (`RelayInboundBubble.test.tsx` — add cases to existing file):
    - Test C1: Renders collapsed on mount — header text ("displayName · room") visible,
      `data-testid="relay-inbound-body"` (added in impl) NOT in DOM, footer text
      "via recv.sh" NOT in DOM.
    - Test C2: `aria-expanded="false"` on the header button when collapsed.
    - Test C3: Click on header → body renders (`getByText(/Nelly says/)`) and
      "via recv.sh" footer appears. `aria-expanded="true"`.
    - Test C4: Second click → body + footer gone again. `aria-expanded="false"`.
    - Test C5: Existing tests that previously asserted body visibility on mount
      must be UPDATED to first click the header (or wrap the assertion after an
      expand). Keep the fetch-triggering behavior test coverage: file-pointer
      fetch still fires from within the collapsed body branch only after expand.
      NOTE: A collapsed inbound bubble MUST NOT fire the `/relay-pointer` fetch
      until expanded (the useEffect body should not run when collapsed, OR the
      fetch call itself must be gated on `!collapsed`). Pick one: simplest is
      to gate the `useEffect` early-return on `collapsed`. Update fetch-triggering
      tests to click-to-expand first.

    RelayOutboundBubble (`RelayOutboundBubble.test.tsx` — add cases to existing file):
    - Test C1: Renders collapsed on mount — header text (/relay send.*roomAlias/)
      visible, body (`rawCommand` or `body` text) NOT in DOM, "via curl" footer
      NOT in DOM. The inner "raw command" toggle button is also NOT in DOM.
    - Test C2: `aria-expanded="false"` on outer header button when collapsed.
    - Test C3: Click header → body renders, "via curl" footer renders,
      `aria-expanded="true"`. Independent `rawExpanded` inner toggle is now
      visible and still defaults to collapsed (▸ raw command).
    - Test C4: Click inner "raw command" toggle → raw pre appears. Click outer
      header again → outer collapses, hiding body AND inner toggle AND raw pre.
      Re-expand outer → inner state MAY reset to default collapsed (per
      "no persistence" — the inner `useState(false)` unmounts with the body).
    - Test C5: Update any existing test that asserts body-visible-on-mount to
      first click the header. Keep the null-body-fallback regression:
      after expand, `body=null` still renders `rawCommand` as always-visible
      mono block (no inner raw-command toggle in that fallback branch).

    ChatMessage (`ChatMessage.test.tsx` — add cases to existing file):
    - Test C1: Assistant message renders collapsed on mount — a header pill with
      text `assistant` and an avatar-dot span (add `data-testid="chatmessage-collapsed-header"`
      to the button in impl) is visible; the message body content ("hello") is
      NOT rendered.
    - Test C2: `aria-expanded="false"` on the header pill button when collapsed.
    - Test C3: Click header → body renders ("hello" visible via markdown),
      `aria-expanded="true"`. Second click → body hidden again.
    - Test C4: User bubble regression — `<ChatMessage role="user" content="hi" />`
      renders WITHOUT any `data-testid="chatmessage-collapsed-header"` element;
      body "hi" is visible immediately (no click required); the outer wrapper
      is still `flex justify-end`.
    - Test C5: Existing user-side tests (Tests 9, 10, 12, 13, 14, 14b–14f, 1–6
      of pendingState) MUST continue to pass byte-identically (user branch
      untouched).
    - Test C6: Existing assistant-side tests that rely on body visibility on
      mount (Tests 11, G, H, I, 18, 19, 20, 21) MUST be updated to first click
      the collapse header to expand. Speak-button tests: the speak button lives
      inside the assistant bubble body, so it is NOT in the DOM until expanded.
      Update those tests to click-expand first, then query for the speak button.
      Autoplay effect (Test 20/21 flow) will still fire the effect on mount
      because the useEffect is at the component root, not inside the body branch —
      but the speak button rendering requires expansion. If autoplay triggers
      startSpeak while collapsed, that is fine (audio plays without UI); the
      state changes still happen — test the state via button aria-label after
      expanding.
  </behavior>
  <action>
    Implement collapse-by-default for all three left-side bubble components with
    a small, additive predicate pattern. NO refactor of existing render logic.

    Shared pattern (apply consistently across all three files):

    1. Add `import { useState } from "react";` (already imported in all three).
    2. Add `const [collapsed, setCollapsed] = useState(true);` at the top of the
       component body, alongside existing state hooks.
    3. Replace the existing header `<div>` with a `<button type="button">` that
       carries the same visual classes (add `w-full text-left cursor-pointer
       bg-transparent border-0 p-0` to strip default button chrome) plus:
         - `aria-expanded={!collapsed}`
         - `aria-label={collapsed ? "Expand message" : "Collapse message"}`
         - `onClick={() => setCollapsed((v) => !v)}`
       Append a small caret glyph to the end of the header row content:
       `{" "}{collapsed ? "▶" : "▼"}`. Use a `<span aria-hidden="true">` for
       the caret so screen readers rely on aria-label instead.
    4. Wrap the existing body + footer in `{!collapsed && (<>…</>)}`.
    5. Keep the outer wrapper (`<div className="flex justify-start">…`) and the
       inner bubble chrome `<div>` untouched, so padding/border/shadow don't
       jump on toggle. The conditional gate goes AROUND the body element(s)
       only, not the outer bubble.

    File-specific notes:

    RelayInboundBubble.tsx (:150-169 header, :171-195 body, :197-205 footer):
    - Add `data-testid="relay-inbound-header"` to the header button and
      `data-testid="relay-inbound-body"` to a wrapping fragment div inside the
      `{!collapsed && …}` block (or add to the existing pointer/inline wrapper)
      so tests can assert presence/absence.
    - The `useEffect` at :101-126 fires the `/relay-pointer` fetch on mount.
      Gate it on `!collapsed`: add `if (collapsed) return;` as the first line
      inside the useEffect (before the `if (!pointer) return;` line), and add
      `collapsed` to the dep array. This preserves T-17-03-04 semantics (fires
      once per (pointerPath, hostId, first-expand)) while ensuring no network
      traffic for bubbles the user never opens.

    RelayOutboundBubble.tsx (:76-85 header, :87-131 body, :133-141 footer):
    - Add `data-testid="relay-outbound-header"` to the header button.
    - The inner `rawExpanded` state and its toggle button (:51, :94-105) are
      UNRELATED to outer collapse. Do not touch them. They live INSIDE the
      body branch, so they naturally disappear when the outer body is hidden
      and their `useState` resets to `false` on each expand cycle (the button
      unmounts and remounts). Note this remount-reset behavior in Test C4.

    ChatMessage.tsx (assistant branch gate at :88, body at :471-525):
    - The assistant bubble has NO existing header. Add a minimal collapsed
      header pill INSIDE the inner bubble `<div>` at the top, gated on `!isUser`.
    - Header pill markup (mirrors RelayInboundBubble.tsx:150-169 look):
      * A `<button type="button">` with classes:
        `flex items-center gap-1 text-xs mb-1 text-[rgba(232,_228,_216,_0.6)]
         font-[JetBrains_Mono_Variable,ui-monospace,monospace]
         w-full text-left cursor-pointer bg-transparent border-0 p-0`
      * `aria-expanded={!collapsed}`, `aria-label={collapsed ? "Expand message" : "Collapse message"}`
      * `onClick={() => setCollapsed((v) => !v)}`
      * `data-testid="chatmessage-collapsed-header"`
      * Contents: avatar-dot `<span aria-hidden="true"
        className="inline-block w-2 h-2 rounded-full flex-shrink-0"
        style={{ backgroundColor: "hsla(var(--pv-id-hue),80%,60%,1)",
        color: "hsla(var(--pv-id-hue),80%,60%,1)" }} />` followed by the text
        `assistant` and the caret span `{collapsed ? "▶" : "▼"}`.
    - Gate the assistant body content on `!collapsed`. The body content is
      everything currently rendered between the `{isQuickReply ? … : …}` block
      at :471-511 and the speak button at :526-615, PLUS the speak button
      itself. Concretely: wrap the entire existing assistant-bubble content
      (isQuickReply/injected/markdown ternary AND the `showSendingSpinner` block
      AND the speak-button block) in `{!collapsed && (<>…</>)}` — but ONLY for
      the assistant branch.
    - The USER branch (`isUser === true`) MUST be byte-identical. The cleanest
      way: keep the outer wrapper untouched, and inside the inner bubble div,
      structure as:
        ```
        {!isUser && <button …>{avatar dot} assistant {caret}</button>}
        {(isUser || !collapsed) && (<>
          {isQuickReply ? … : injected ? … : <ReactMarkdown …/>}
          {showSendingSpinner && <Loader2 …/>}
          {!isUser && <button …speak…/>}
        </>)}
        ```
      This keeps the user path always-expanded (no header, always body) and the
      assistant path collapsed-by-default with header + gated body.
    - Ensure the `useEffect` at :107-121 (unmount cleanup) is NOT moved inside
      any conditional — hooks must fire unconditionally.
    - The autoplay `useEffect` at :282-295 also stays unconditional; autoplay
      may fire while collapsed (audio-only) — this is fine per fleet no-streaming
      rule (TTS is not message streaming). Do not add a collapse gate there.

    Update failing existing tests:

    - `ChatMessage.test.tsx`: For each existing assistant-side test (Tests 11,
      G, H, I, 18, 19, 20, 21), add an initial expand step before body
      assertions:
        ```
        const expandBtn = screen.getByTestId("chatmessage-collapsed-header");
        await act(async () => { await userEvent.click(expandBtn); });
        ```
      (Wrap in act/userEvent where the test already uses userEvent; use a plain
      `fireEvent.click` from `@testing-library/react` where the test is
      synchronous.)
    - `RelayInboundBubble.test.tsx`: For each existing test that asserts body
      text or fetch behavior (Tests 1, 2, 3, and any others that touch body),
      click the header first:
        ```
        import { fireEvent } from "@testing-library/react";
        …
        fireEvent.click(screen.getByTestId("relay-inbound-header"));
        ```
    - `RelayOutboundBubble.test.tsx`: Same pattern — click
      `relay-outbound-header` first in tests that assert body/footer visibility.

    Add new colocated tests (C1–C4 for each of the three files, plus C5/C6
    regression guards for ChatMessage as described in the behavior block).

    Verify with `npx vitest run` scoped to the three test files only. The
    orchestrator (Tina) handles full-suite + deploy after executor returns.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; npx vitest run src/ui/features/pretty-view/RelayInboundBubble.test.tsx src/ui/features/pretty-view/RelayOutboundBubble.test.tsx src/ui/features/pretty-view/ChatMessage.test.tsx</automated>
  </verify>
  <done>
    - All three components (`RelayInboundBubble`, `RelayOutboundBubble`,
      `ChatMessage` assistant branch) start collapsed on mount showing only
      the header row (existing header for the two Relay bubbles; new
      `● assistant ▶` pill for ChatMessage).
    - Header row is a native `<button type="button">` with correct
      `aria-expanded` and `aria-label` toggle strings.
    - Caret glyph `▶`/`▼` appears at end of header row.
    - Click toggles collapsed state; second click re-collapses. Enter/Space
      work via native button semantics.
    - User branch of `ChatMessage` (`role="user"`) renders identically to
      pre-change: no header pill, body immediately visible, wrapper stays
      `flex justify-end`. All existing user-side tests pass unmodified.
    - RelayOutboundBubble inner `rawExpanded` state remains independent and
      untouched; it is only visible when the outer bubble is expanded, and
      naturally resets to `false` on each outer expand cycle (fresh mount).
    - RelayInboundBubble `/relay-pointer` fetch is gated on `!collapsed`
      (no network for unexpanded bubbles).
    - Scoped test run (`npx vitest run` on the three files) is green.
    - No streaming state, no auto-expand-while-streaming, no persistence
      across mounts.
  </done>
</task>

</tasks>

<verification>
- Scoped vitest run on the three touched test files is green.
- Manual sanity check (optional, not blocking on executor): open PrettyView,
  observe left-side bubbles collapsed by default, click to expand/collapse,
  right-side (Ashley) messages unchanged.
</verification>

<success_criteria>
- All three left-side bubble types render collapsed on mount showing only
  their header row.
- Header row is a keyboard-accessible native `<button>` with correct ARIA.
- Toggle round-trips (click → expand → click → collapse) work.
- Right-side user bubble is byte-identical to today (regression guard test
  passes; existing user-side tests pass unmodified).
- Scoped vitest run on the three test files passes.
- No streaming affordances added anywhere.
</success_criteria>

<output>
Executor returns summary of edits + green scoped-vitest output. Tina
orchestrator picks up full-suite + ship motion.
</output>
