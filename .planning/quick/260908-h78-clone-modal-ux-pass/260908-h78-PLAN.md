---
phase: 260908-h78-clone-modal-ux-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-conversations/PrettyConversationRow.tsx
  - src/ui/features/pretty-conversations/PrettyConversationRow.clone-menu.test.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.clone-dialog.test.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
autonomous: true
requirements:
  - SHAPE-clone-modal-ux-pass-IN-1  # row context menu relabel
  - SHAPE-clone-modal-ux-pass-IN-2  # role pre-fill wiring (already in place via chainPrefill)
  - SHAPE-clone-modal-ux-pass-IN-3  # reference sweep of retired-modal wording
  - SHAPE-clone-modal-ux-pass-IN-4  # scoped tests: label + pre-fill + editable + submit role match

must_haves:
  truths:
    - "Right-click on an agent row shows a menu item whose exact visible text is 'create new agent under this role' (lowercase)."
    - "Clicking that menu item opens the existing new-agent modal (NewSessionDialog) with the role picker pre-filled to the source row's role."
    - "The role picker in the opened modal remains visible AND editable (the user can change it before submitting)."
    - "No frontend source, test, or user-facing string still says 'Spawn under this role' or references a standalone clone-modal component."
    - "Scoped tests (only the specific files touched) run green; no full test suite, no docker, no deploy runs from this plan."
  artifacts:
    - path: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx"
      provides: "Row context menu with the new label"
      contains: 'label: "create new agent under this role"'
    - path: "src/ui/features/pretty-conversations/PrettyConversationRow.clone-menu.test.tsx"
      provides: "Scoped tests: label reads correctly + click fires onClone + gates unchanged"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.clone-dialog.test.tsx"
      provides: "Scoped tests: panel wiring seeds initialRole from source row's identity.role + brief NOT prefilled"
  key_links:
    - from: "PrettyConversationRow context menu items[] builder"
      to: "onClone prop"
      via: "menu-item onClick"
      pattern: 'label: "create new agent under this role"'
    - from: "PrettyConversationsPanel.handleRowClone"
      to: "NewSessionDialog initialHost + initialRole props"
      via: "chainPrefill state → NewSessionDialog mount"
      pattern: "setChainPrefill\\(\\{ role: identity\\.role, host: row\\.host \\}\\)"
---

<objective>
Retire the last user-visible remnant of the standalone clone flow. The
context-menu label "Spawn under this role" is renamed to exactly
"create new agent under this role" (lowercase, Alice greenlit), and
every comment/test/string that still references the retired
CloneAgentDialog by name gets swept. The underlying wiring
(handleRowClone → chainPrefill → NewSessionDialog with initialHost +
initialRole) was already stood up in Phase 80-09 and is left untouched
— NewSessionDialog already accepts a pre-filled role via initialRole
AND keeps the picker editable (verified in
NewSessionDialog.role-dropdown.test.tsx), so no modal-side change is
in scope.

Purpose: Finalize the campaign's "clone and new-under-this-role are
the same operation" stance after cosmetics-migrate-to-role. One label,
one modal, no drift.

Output: One or two atomic commits on feat/tab-title-from-tmux — label
change + scoped-test updates (Task 1), reference sweep (Task 2). No
push in this plan; the campaign ship gate is at the end of the UX-pass
campaign, not per bounty.

Discovery note (findings that reshaped this plan vs. the task-context
briefing):
- Phase 80-09 already DELETED the standalone CloneAgentDialog source
  file. There is no `CloneAgentDialog.tsx` to delete in this plan. The
  shape's "delete the clone modal source file" line was authored before
  discovering Phase 80-09 already did it; the sweep in Task 2 is the
  residual work.
- The two files named `*.clone-menu.test.tsx` /
  `*.clone-dialog.test.tsx` are NOT tests for the retired modal — they
  are the scoped tests for the CURRENT context-menu → NewSessionDialog
  wiring. Deleting them would delete the exact coverage the shape's In
  section #4 asks for. They get UPDATED (new label string + one added
  editability assertion), not deleted. Filenames are left alone (rename
  is out-of-scope creep per PrettyConversationRow.tsx L237's existing
  precedent of keeping the `onClone` prop name for the same reason).
- Backend `identity-clone` route + `cloneIdentity` API client are
  documented as "currently unused by any UI callers" (identities-api.ts
  L318). They are backend and OUT per the shape ("Backend changes to
  the create endpoint... adds none"). Not touched here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/shapes/shape-clone-modal-ux-pass.md
@.planning/shapes/shape-create-agent-modal-ux-pass.md
@src/ui/features/pretty-conversations/PrettyConversationRow.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
@src/ui/features/pretty-conversations/PrettyConversationRow.clone-menu.test.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.clone-dialog.test.tsx
@src/ui/sidebar/NewSessionDialog.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename context-menu label + update scoped tests + add editability assertion</name>
  <files>
    src/ui/features/pretty-conversations/PrettyConversationRow.tsx,
    src/ui/features/pretty-conversations/PrettyConversationRow.clone-menu.test.tsx,
    src/ui/features/pretty-conversations/PrettyConversationsPanel.clone-dialog.test.tsx
  </files>
  <behavior>
    - PrettyConversationRow.clone-menu.test.tsx Test 13 asserts the menu
      item name matches exactly `create new agent under this role`
      (case-sensitive lowercase — use `screen.getByRole("menuitem", { name: "create new agent under this role" })`,
      NOT the case-insensitive `/spawn under this role/i` regex it uses today).
    - Tests 14/15 (negative gates) update the same query string; behavior
      (onClone not provided → no item; no identity → no item) unchanged.
    - PrettyConversationsPanel.clone-dialog.test.tsx Test 1 asserts the
      menu item under the new exact label and that the mock
      NewSessionDialog receives `initialRole === "operator"` (the
      stubIdentity.role) and `initialHost === stubHost`.
    - PrettyConversationsPanel.clone-dialog.test.tsx: ADD one new test
      (Test 4) covering shape In-4's "picker remains editable"
      requirement. Since the existing suite mocks NewSessionDialog as a
      spy, the "editable" invariant is asserted at the CONTRACT level:
      Test 4 asserts that the panel forwards the pre-filled role via
      the `initialRole` prop (a seed, not a `role` lock prop) — the
      dialog's own `initialRole` semantics are already covered
      elsewhere (NewSessionDialog.role-dropdown.test.tsx confirms
      `selectedRole` starts equal to `initialRole` but is mutable via
      `setSelectedRole` and is exposed as a plain HTML `<select>`
      changeable via `fireEvent.change`). Test 4 documents this
      cross-file contract with a comment pointing at
      NewSessionDialog.role-dropdown.test.tsx and asserts the panel
      passes NO lock/readonly prop (i.e. the props the spy captures
      contain `initialRole` but do NOT contain any key matching
      `/lock|readonly|disabled/i` alongside a role field).
    - All three test files start RED (label mismatch), production
      change to the label makes them GREEN.
  </behavior>
  <action>
    Step A (RED — write failing tests first, per tdd=true):

    1. In `src/ui/features/pretty-conversations/PrettyConversationRow.clone-menu.test.tsx`,
       replace every occurrence of the regex `/spawn under this role/i`
       with the exact string `"create new agent under this role"`.
       There are 4 occurrences (Test 13 assertion, Test 14 negative,
       Test 15 negative, plus the file-header comment on line 14 that
       reads `(label "Spawn under this role", onClick fires onClone)`
       — change the comment too so grep for `Spawn under this role`
       returns zero hits in this file). Also update the file's header
       block comment (lines 5-9) that says `Phase 80-09: label renamed
       "Clone" → "Spawn under this role"` to add a trailing sentence:
       `Bounty 260908-h78: label rewritten again to "create new agent
       under this role" — this file's assertions track the current
       label verbatim.`

    2. In `src/ui/features/pretty-conversations/PrettyConversationsPanel.clone-dialog.test.tsx`,
       replace every occurrence of the regex `/spawn under this role/i`
       with the exact string `"create new agent under this role"`
       (currently at lines 285, 328, 356 per grep). Also update the
       header block comment (lines 1-20) so `Spawn under this role`
       becomes `create new agent under this role` in the human-readable
       description, and add a note that references bounty 260908-h78.
       ADD Test 4 (append inside the existing `describe(...)`) — pattern:

           it("Test 4: panel forwards initialRole as a seed (no lock/readonly prop) → picker stays editable", () => {
             // Contract test for shape-clone-modal-ux-pass In-4 "picker
             // remains editable". NewSessionDialog's own role-dropdown
             // suite (NewSessionDialog.role-dropdown.test.tsx) proves
             // that when initialRole is set, selectedRole seeds to it
             // but the <select> stays a plain, mutable dropdown. This
             // test locks the panel-side contract: we pass a SEED prop
             // (initialRole), never a lock/readonly/disabled prop.
             render(<PrettyConversationsPanel variant="desktop" hostTree={ONE_HOST_TREE} />);
             const row = screen.getByTestId(/^conv-row-/);
             fireEvent.contextMenu(row.querySelector('[role="button"]')!, { clientX: 10, clientY: 10 });
             fireEvent.click(
               screen.getByRole("menuitem", { name: "create new agent under this role" }),
             );
             expect(newSessionDialogSpy).toHaveBeenCalled();
             const lastProps = newSessionDialogSpy.mock.calls.at(-1)![0];
             expect(lastProps.initialRole).toBe(stubIdentity.role);
             // No lock prop: the panel MUST NOT be passing any prop
             // that would signal "role is fixed" to the dialog.
             for (const key of Object.keys(lastProps)) {
               expect(key).not.toMatch(/lock|readonly|disabled/i);
             }
           });

       If the existing `getByTestId(/^conv-row-/)` matcher does not fit
       the current row test-id convention (check by reading Tests 1-3
       in the same file for the exact selector they use — likely
       `container.querySelector('[data-conversation-id="conv-1"]')`
       matching the panel row rendered from `stubRow`), copy the
       selector pattern verbatim from Test 1. Keep the rest of the
       assertion shape identical.

    3. Run the two scoped test files. Confirm they now FAIL (RED) with
       "unable to find role 'menuitem' with name 'create new agent under
       this role'". If they pass, the label change (Step B) already
       happened prematurely — stop and investigate.

    Step B (GREEN — make the tests pass):

    4. In `src/ui/features/pretty-conversations/PrettyConversationRow.tsx`,
       change the label at line 1388 from `label: "Spawn under this role",`
       to `label: "create new agent under this role",` (exact
       lowercase, no capitalization on any word). Do NOT touch the
       `onClick: onClone` line above it — the prop name stays. Do
       NOT touch any other props, gates, or comment blocks except:
       - Line 1349: `"Spawn under this role" (Phase 80 — formerly labeled "Clone") only`
         → change the quoted label to `"create new agent under this role"`
         and extend the parenthetical to `(bounty 260908-h78 — formerly "Spawn under this role", originally "Clone")`.
       - Line 1375-1376: same treatment on the `// Phase 80: "Spawn under this role" item (formerly "Clone" —`
         comment.

    5. Re-run the two scoped test files. Confirm they now PASS (GREEN),
       including Test 4.

    Do NOT rename the `onClone` prop, the `handleRowClone` handler,
    the `chainPrefill` state, the `.clone-menu.test.tsx` /
    `.clone-dialog.test.tsx` filenames, or the file-scoped `describe(...)`
    strings that mention "Clone" as historical context — those are
    out-of-scope creep per the executor precedent already in the code
    (PrettyConversationRow.tsx L237 comment).

    Do NOT run the full test suite. Do NOT `docker build`. Do NOT push.
    Commit atomically at the end of the task with message shaped like:
    `feat(quick-260908-h78): rename row context-menu label to "create new agent under this role" + update scoped tests`.
  </action>
  <verify>
    <automated>
      cd /home/ubuntu/skynet-tabitha &amp;&amp; \
      npx vitest run \
        src/ui/features/pretty-conversations/PrettyConversationRow.clone-menu.test.tsx \
        src/ui/features/pretty-conversations/PrettyConversationsPanel.clone-dialog.test.tsx \
        --reporter=verbose &amp;&amp; \
      grep -c 'label: "create new agent under this role"' src/ui/features/pretty-conversations/PrettyConversationRow.tsx | grep -qx 1 &amp;&amp; \
      { grep -v '^\s*//' src/ui/features/pretty-conversations/PrettyConversationRow.tsx | grep -c '"Spawn under this role"'; } | grep -qx 0
    </automated>
  </verify>
  <done>
    - Scoped vitest run passes (both files, all tests including new Test 4) with exit 0.
    - Grep confirms exactly one occurrence of the new production label string in PrettyConversationRow.tsx.
    - Grep (excluding comment lines with `^\s*//`) confirms zero occurrences of the old `"Spawn under this role"` production string in PrettyConversationRow.tsx.
    - One atomic commit landed on feat/tab-title-from-tmux.
    - No push, no full-suite run, no docker.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sweep remaining "Spawn under this role" references in frontend + update the panel-level test comment</name>
  <files>
    src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx,
    src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx,
    src/ui/api/identities-api.ts
  </files>
  <action>
    Reference sweep to close shape's In-3 ("Grep the codebase for any
    lingering references to the retired modal (imports, dynamic loads,
    string labels, tests, docs) and remove or update each one"). The
    standalone CloneAgentDialog component was already deleted in Phase
    80-09; residual references are all COMMENTS or historical narrative
    (no live imports, no dynamic loads, no dead code paths — verified
    via the grep in the discovery pass, see PLAN objective's Discovery
    note). Update the wording so future greps for the retired label do
    not surface stale menu-name strings.

    1. `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`
       — three comment sites reference the old label in narrative form:
       - Line 127: `// "Spawn under this role" item now routes through NewSessionDialog with the`
         → change `"Spawn under this role"` to `"create new agent under this role"` and add a trailing
         phrase `(bounty 260908-h78 label rewrite)`.
       - Line 1233: `// handler. "Clone" was rebranded to "Spawn under this role" (see`
         → change `"Spawn under this role"` to `"create new agent under this role"` and add
         `(originally "Clone", then "Spawn under this role" until bounty 260908-h78)`.
       - Line 1983: `context-menu "Spawn under this role" item (formerly labeled`
         → change the quoted label in the JSX-adjacent block comment to
         `"create new agent under this role"`.

       Do NOT touch the `handleRowClone` handler name, the `onClone`
       prop names on the row-mount sites (L1748, L1780, L1809, L1918),
       the `chainPrefill` state, or the `setNewSessionDialogOpen` call.
       Wiring is already correct — this is a comment-only pass.

    2. `src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx`
       — line 2444-2445 comment says `quick-260804-uo4 inserted the
       new-window item between Clone and Deactivate; for a row without
       an identity Clone is auto-hidden` and line 2453 says `menu order
       is now Pin, Hide, Open-in-new-window (Clone hidden — row has no
       identity)`. Update `Clone` → `create-new-agent-under-this-role`
       in those three narrative mentions (kebab-cased for readability
       inside prose; NOT quoted, since the test itself does not assert
       the label — it asserts menu ORDER by array-index for a
       no-identity row where the item is auto-hidden anyway).

       Do NOT change any assertion or `expect(...)` in this file. Do
       NOT change Test (g)'s array-position expectations — the item is
       hidden on a no-identity row so its label is irrelevant to those
       positional asserts.

    3. `src/ui/api/identities-api.ts` — line 316 says `deleted; the
       "Spawn under this role" context-menu path now routes through`.
       Update the quoted label to `"create new agent under this role"`
       and add `(bounty 260908-h78 renamed the label; the underlying
       chainPrefill wiring is unchanged)`.

       Do NOT delete `cloneIdentity()` or `IdentityCloneCollisionError`
       or `CloneIdentityInput` in this task — the backend
       `POST /identities/clone` route still exists and the API client
       is still exported. Removing dead-code API clients is a separate
       call (out of scope per shape's "Backend changes to the create
       endpoint... adds none").

    After edits, verify the sweep with a single grep gate:
      `grep -rn '"Spawn under this role"' --include='*.tsx' --include='*.ts' src`
      MUST return zero lines. If a hit remains, it was not enumerated
      above — investigate and either sweep it (if it's a
      comment/label) or STOP and report (if it's a live string in
      unexpected surface area).

    Commit atomically. Message shape: `docs(quick-260908-h78): sweep
    residual "Spawn under this role" comment references in frontend
    (label rewritten in Task 1)`.

    Do NOT push. Do NOT run full test suite. Do NOT `docker build`.
  </action>
  <verify>
    <automated>
      cd /home/ubuntu/skynet-tabitha &amp;&amp; \
      { grep -rn '"Spawn under this role"' --include='*.tsx' --include='*.ts' src | wc -l; } | grep -qx 0 &amp;&amp; \
      npx vitest run \
        src/ui/features/pretty-conversations/PrettyConversationRow.clone-menu.test.tsx \
        src/ui/features/pretty-conversations/PrettyConversationsPanel.clone-dialog.test.tsx \
        --reporter=verbose &amp;&amp; \
      npx tsc --noEmit
    </automated>
  </verify>
  <done>
    - Zero occurrences of `"Spawn under this role"` (exact quoted
      double-quote form) remain anywhere under `src/**/*.ts{x}`.
    - The two scoped test files from Task 1 still pass (regression
      guard — the sweep MUST NOT break the tests that were made
      green in Task 1).
    - `npx tsc --noEmit` is clean (no type errors introduced by the
      comment sweep — should be a no-op typewise but proves the
      files still parse).
    - One atomic commit landed on feat/tab-title-from-tmux.
    - No push. No full suite. No docker.
  </done>
</task>

</tasks>

<verification>

## End-of-plan gates (scoped only — campaign constraint)

Run from `/home/ubuntu/skynet-tabitha` on branch `feat/tab-title-from-tmux`:

1. **Scoped tests green** (the two touched files + tsc):
   ```
   npx vitest run \
     src/ui/features/pretty-conversations/PrettyConversationRow.clone-menu.test.tsx \
     src/ui/features/pretty-conversations/PrettyConversationsPanel.clone-dialog.test.tsx
   npx tsc --noEmit
   ```
   Both must exit 0.

2. **Label rename landed** (production string is the new exact text):
   ```
   grep -c 'label: "create new agent under this role"' \
     src/ui/features/pretty-conversations/PrettyConversationRow.tsx
   ```
   Must print `1`.

3. **Old label swept** (excluding comment lines to avoid header-comment
   self-invalidation — Nyquist / grep-gate hygiene rule):
   ```
   grep -rn '"Spawn under this role"' --include='*.tsx' --include='*.ts' src | wc -l
   ```
   Must print `0`.

4. **Wiring untouched** — the four `onClone={... handleRowClone(row) ...}`
   mount sites and the `setChainPrefill({ role: identity.role, host: row.host })`
   handler body are unchanged from HEAD~2:
   ```
   git diff HEAD~2 -- src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx \
     | grep -E '^[+-]\s*(setChainPrefill|onClone=)' \
     | grep -vE '^\s*(\+\+\+|---)'
   ```
   Must print nothing (no additions or deletions on those lines — only
   comment edits landed).

5. **Two atomic commits** on the branch, in order (Task 1 then Task 2):
   ```
   git log --oneline HEAD~2..HEAD
   ```
   Two commits, both prefixed with `quick-260908-h78` (or equivalent
   scope tag — the exact prefix is executor discretion within the
   repo's commit-message conventions).

## NOT run in this plan (campaign constraint — Alice 2026-09-07 verbatim)

- Full `npx vitest` / `npm test` run — the ship gate lives at the END of the UX-pass campaign, not per bounty.
- `docker build`, `docker cp`, `docker compose up`.
- `git push`. The plan terminates at the second local commit.
- Deploy of any shape.

If a follow-up bounty in the campaign later wants a push before the
campaign ship gate (e.g. for a hotfix), the standing rule is
`git pull --rebase` first — origin HEAD at plan-authorship time was
`eba3eae9` (fix(86): apply code-review findings), so no fast-forward
gap existed. This plan does not push.

</verification>

<success_criteria>

Shipped when ALL of:

1. Right-clicking any agent row in the sidebar shows a context-menu
   item whose visible text is EXACTLY `create new agent under this
   role` (lowercase, no capital letters on any word).
2. Clicking that item opens the existing NewSessionDialog with the
   role dropdown pre-selected to the source row's identity.role, AND
   the dropdown is still editable (user can pick a different role).
3. Submitting the modal produces an agent whose role equals the
   dropdown's CURRENT value (i.e. if the user changed the pre-filled
   role, the new agent uses the changed role — no override from the
   seed) — asserted at the contract level via
   NewSessionDialog.role-dropdown.test.tsx's existing coverage that
   `selectedRole` (which drives submit) is `initialRole`-seeded but
   `setSelectedRole`-mutable.
4. Zero frontend production strings, comments, or test descriptions
   still say `"Spawn under this role"`.
5. Two atomic commits on `feat/tab-title-from-tmux`, neither pushed.
6. Scoped vitest run + tsc are green. No full suite run. No docker.
   No deploy.

Explicitly OUT (per shape's Out / Tempting-but-no lists):

- No changes to NewSessionDialog itself beyond what already exists —
  no header blurb, no admin split, no name auto-suggest, no
  "auto-filled from &lt;source&gt;" hint.
- No override carry-over from source identity's per-identity
  cosmetics into the new agent.
- No rename of `onClone` prop, `handleRowClone` handler,
  `chainPrefill` state, or the two `.clone-*.test.tsx` filenames.
- No backend deletion of `identity-clone` route or `cloneIdentity()`
  API client (both currently unused by UI, but their removal is a
  separate backend cleanup call).
- No adding the menu item to row types it did not live on
  (role-header rows, group rows, etc.).

</success_criteria>

<output>

Create `.planning/quick/260908-h78-clone-modal-ux-pass/260908-h78-SUMMARY.md` when both tasks are done. The SUMMARY records:
- The two commit hashes (Task 1 label + tests, Task 2 comment sweep).
- The exact final label string (`create new agent under this role`).
- The grep counts from the verification gates (label production string = 1, old-label-in-non-comment lines = 0).
- Confirmation that no push, no full suite, no docker, no deploy ran.
- Anything encountered during execution that deviated from the plan
  (e.g. an additional file surfaced by the sweep grep) with rationale.

</output>
