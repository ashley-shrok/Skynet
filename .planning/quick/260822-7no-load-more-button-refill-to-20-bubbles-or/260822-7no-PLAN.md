---
phase: quick-260822-7no
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/claude-session/claude-session-server.ts
  - src/backend/claude-session/claude-session-server.fetch-older-range.test.ts
  - src/ui/features/pretty-view/PrettyView.tsx
autonomous: true
requirements:
  - QUICK-260822-7NO-01  # backend refill loop replaces v1 no-refill policy
  - QUICK-260822-7NO-02  # structured logs on the full click → WS → response path
must_haves:
  truths:
    - "Clicking the load-more button on Sally's session (or any skip-heavy JSONL) returns 20 non-skip wire frames unless the read reached startLine=1 first."
    - "When the refill loop reaches startLine=1, the handler emits the accumulated frames (0..20) with hasMore=false and the button unmounts on the next render."
    - "Every load-more click emits one [pv-load-more] click log entry with beforeLine, currentMessagesLen, sessionHasMore, sessionTotalLines."
    - "Every fetch_older_range_batch response causes one [pv-load-more] response log entry with ok, messagesLen, oldestLine, hasMore, error, preLen, postLen."
    - "Every fetch_older_range request emits one [fetch-older-range] req entry at handler entry, one [fetch-older-range] slice entry per reader slice inside the refill loop, and one [fetch-older-range] emit entry at the final ws.send."
    - "Scoped vitest for claude-session-server, session-file-range-reader, and PrettyView is green; npm run build:backend and npm run build both pass."
  artifacts:
    - path: "src/backend/claude-session/claude-session-server.ts"
      provides: "handleFetchOlderRange with refill loop (v2 policy) + [fetch-older-range] req/slice/emit logs"
      contains: "refillIterations"
    - path: "src/backend/claude-session/claude-session-server.fetch-older-range.test.ts"
      provides: "Test 8 rewritten as v2 refill-until-20-or-top; new tests for all-skip file and top-of-file partial"
      contains: "refill"
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "[pv-load-more] click + response console.info instrumentation (behavior unchanged)"
      contains: "[pv-load-more]"
  key_links:
    - from: "handleFetchOlderRange refill loop"
      to: "readSessionFileRange"
      via: "repeated calls with current = previous startLine until accumulator.length >= 20 OR startLine === 1"
      pattern: "while.*accumulator.*length.*<.*20"
    - from: "PrettyView handleLoadOlder"
      to: "backend handleFetchOlderRange"
      via: "ws.send fetch_older_range → ws.onmessage fetch_older_range_batch"
      pattern: "\\[pv-load-more\\] (click|response)"
---

<objective>
Phase 47 v2 — replace the v1 "no refill" partial-batch policy in
`handleFetchOlderRange` with a refill loop that keeps reading older 20-line
slices from the session file until either the accumulator holds 20 non-skip
wire frames OR the read cursor reaches `startLine = 1`. Wire shape is
unchanged. Simultaneously add structured `[fetch-older-range]` (backend)
and `[pv-load-more]` (frontend) `console.info` logs across the entire
click → WS → response path so we can diagnose future load-more issues
without another live debug session.

Purpose: Sally's session file is 88% skip lines (579/657 raw lines classify
as `kind:"skip"`). Under v1 policy a single click can return 0–4 messages
even though 20 non-skip messages exist further back in the file. Symptom is
"button flashes → nothing appears (sometimes)". Diagnosing this cost an
hour of live tracing on 2026-08-22 because there are zero structured logs
on the entire path — hence the instrumentation is not "nice to have" but
part of the fix.

Output: Backend refill loop + rewritten Test 8 + two new backend tests + two
frontend `console.info` sites. No wire-shape change. No client behavior change.
Both scoped vitest suites plus backend + full builds pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/shapes/shape-load-more-button.md
@src/backend/claude-session/claude-session-server.ts
@src/backend/claude-session/session-file-range-reader.ts
@src/backend/claude-session/session-file-parser.ts
@src/backend/claude-session/claude-session-server.fetch-older-range.test.ts
@src/ui/features/pretty-view/PrettyView.tsx
@src/ui/features/pretty-view/LoadMoreOlderButton.tsx
@src/ui/features/pretty-view/PrettyView.load-more.test.tsx

## Key excerpts already read (do NOT re-read)

**Backend `handleFetchOlderRange`** lives at
`src/backend/claude-session/claude-session-server.ts` L1477–1637. The v1
no-refill body to REPLACE is the block at L1596–1636 (from the comment
`// ─ Parse + reshape each line via the SHARED helper (Hunk A output).`
through the trailing `ws.send(...)` catch). The comment on L1597–1598
literally reads `"v1 partial-batch policy, Test 8 lock — no refill"` —
that comment must be updated to reflect v2 refill semantics.

Preserve unchanged (do NOT touch):
- L1477–1553: signature, `emitErrorFrame` helper, input validation, trust boundary
- L1555–1579: rangeCount<=0 fast path (`beforeLine=1` empty-success emit)
- L1581–1594: FIRST `readSessionFileRange` call with try/catch error frame

The refill loop replaces ONLY the parse+reshape+emit block after the first
successful read.

**Reshape helper** at
`src/backend/claude-session/claude-session-server.ts` L285–360:
`reshapeParsedLineToWireFrame(parsed, rawLine, line)` returns
`StreamEvent | null` — null means `kind:"skip"` and the caller filters.
Malformed lines return a `malformed_line` frame (NOT null) — they count
toward the 20-non-skip accumulator (same as v1).

**Reader** at `src/backend/claude-session/session-file-range-reader.ts`
L144–241: `readSessionFileRange(conn, path, startLine, count)` returns
`{ lines, totalLines }`. Throws for invalid inputs. LOCAL branch (`conn===null`)
slices via `allLines.slice(startLine - 1, startLine - 1 + count)`. Past-EOF
just returns fewer lines. `startLine=1, count=20` returns lines 1..20 (or
1..totalLines if file shorter). Safe to call in a loop; refill loop never
re-reads the same range because `current` always decreases.

**Skip classifier** at `src/backend/claude-session/session-file-parser.ts`
L640–984 — nine skip subtypes including `empty`, `unknown-type`, `meta`,
`no_message`, `empty_content`, `harness_wrapper`. All produce
`{ kind:"skip", why }`. Malformed JSONL produces `{ kind:"malformed", bytes }`.

**Test file** `src/backend/claude-session/claude-session-server.fetch-older-range.test.ts`:
- L28 header comment locks v1 policy — REWRITE.
- L108 helper-doc comment references Test 8 policy lock — REWRITE.
- L137 `describe("handleFetchOlderRange", ...)` — the target block.
- Tests 1–7 remain compatible with v2 refill (their fixtures return
  20 non-skip lines from a single read → refill exits after iteration 1
  with identical output → reader-call-count assertions in Test 1
  remain `toHaveLength(1)`). Do NOT modify Tests 1–7.
- Test 5 (L275–305) uses `beforeLine=15, count=20`, reader returns 14 all-
  message lines → refill exits at startLine=1 with 14 messages → identical
  observable behavior → Test 5 assertions still pass unchanged. Do NOT
  modify Test 5.
- Test 8 (L374–415) is the ONLY existing test to REWRITE — its fixture
  (20 lines, 3 skips) currently expects `messages.length === 17`; under v2
  the handler will refill and the assertion must change.
- Add tests 9 and 10 after Test 8 (see task action).

**Frontend click handler** at
`src/ui/features/pretty-view/PrettyView.tsx` L757–783 (`handleLoadOlder`
callback). State at click time: `oldestLoadedLine` (in-scope closure),
`messages` (via ref `messagesLenRef` at L1126 or a fresh setter capture),
`sessionHasMore` (declared L377), `sessionTotalLines` (declared L374).
`console.info` goes AFTER the three defense-in-depth guards and BEFORE
`ws.send`.

**Frontend response handler** at
`src/ui/features/pretty-view/PrettyView.tsx` L1678–1726
(`fetch_older_range_batch` case in the ws.onmessage switch).
Error branch: L1696–1704. Success branch: L1706–1725. `preLen` and
`postLen` must be captured INSIDE the `setMessages(prev => ...)` setter
(prev.length for preLen, result.length for postLen) so the log lands on
the actual state transition, not a stale-closure read.

**Visibility gate** at L2559–2562: `hasOlderMessages = sessionTotalLines !=
null && sessionHasMore && sessionTotalLines > messages.length`. Not
modified; noted so the plan reader understands what `sessionHasMore=false`
from the response triggers (button unmounts on next render).

## Fleet rules in force

- No worktree — main tree only (tina / box-maintainer role).
- Executor stops at code + commit + scoped tests + backend build green.
  Do NOT run `git push`, `docker build`, `docker compose up`. Tina picks
  those up after the executor returns.
- Scoped tests only during dev:
  `npx vitest run --related src/backend/claude-session/claude-session-server.ts src/backend/claude-session/session-file-range-reader.ts src/ui/features/pretty-view/PrettyView.tsx`
  Full suite is orchestrator-only at ship-gate.
- `src/backend/**` touched → `npm run build:backend` MUST run (frontend
  tsc doesn't catch backend TS errors — patch #154 fixup lesson).
- `npm run build` also runs before commit as belt-and-suspenders.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend refill loop + Test 8 rewrite + two new tests + [fetch-older-range] logs</name>
  <files>src/backend/claude-session/claude-session-server.ts, src/backend/claude-session/claude-session-server.fetch-older-range.test.ts</files>
  <behavior>
    - Test 8 (rewritten, v2 policy): reader returns 20 lines with 3 skips on the FIRST call, then 20 more all-message lines on the SECOND call → handler accumulates 17 + 3 = 20 messages, exits loop after iteration 2, emits `{ messages.length === 20, oldestLine === startLine of the second read, hasMore === true, error undefined }`. Reader was called exactly twice.
    - Test 9 (new): reader returns all-skip lines for every call until startLine reaches 1 → handler exits loop with `messages: []`, `oldestLine: 1`, `hasMore: false`, no error. Reader called ceil((beforeLine - 1) / 20) times.
    - Test 10 (new): partial file. `beforeLine = 25, count = 20` on a file with 24 lines all messages, 5 skips scattered → refill halts at startLine=1 with 19 messages, `hasMore: false`. Reader called at most twice.
    - Test 11 (new — refill halts on 20-exact-hit): reader returns 20 lines with 0 skips on FIRST call → accumulator hits 20 after iteration 1, loop exits without a second read. Reader called exactly once. (This guards against the "always keep going" regression.)
    - Existing Tests 1–7 continue to pass unchanged (no assertion touches beyond Test 8).
  </behavior>
  <action>
    In `src/backend/claude-session/claude-session-server.ts`, REPLACE lines
    ~1596–1636 (the parse-reshape-emit block, from the `// ─ Parse + reshape…`
    header through the trailing `ws.send(...)` catch) with a refill loop.
    Preserve everything above (input validation, trust boundary, rangeCount≤0
    fast path, FIRST `readSessionFileRange` try/catch) unchanged.

    Implementation shape (name identifiers exactly as listed so tests can
    grep them from the emit-log later):
    - Add `[fetch-older-range] req beforeLine=<n> count=<n>` via
      `databaseLogger.info(...)` (or `console.info` if that matches the
      existing tag style in this file — read the surrounding logger usage
      and match it) IMMEDIATELY after the trust-boundary gate passes and
      BEFORE the rangeCount≤0 fast path so every request produces exactly
      one `req` line even on the empty-success early return.
    - Introduce `const accumulator: StreamEvent[] = []` and
      `let currentBefore = beforeLine` and `let refillIterations = 0`
      and `let lastStartLine = startLine` (seeded from the pre-existing
      variable computed at L1557).
    - Reuse the FIRST read result already in `readResult` (L1583). Do NOT
      re-issue that call — process it as the first loop iteration:
      1. Reshape each line with `reshapeParsedLineToWireFrame`, filter
         nulls, push survivors to `accumulator`.
      2. Log `[fetch-older-range] slice startLine=<lastStartLine> rawLines=<readResult.lines.length> nonSkip=<survivorsThisIter> total=<accumulator.length>`.
      3. Increment `refillIterations` to 1.
      4. Advance `currentBefore = lastStartLine`.
    - Then enter `while (accumulator.length < 20 && currentBefore > 1) { … }`:
      1. Recompute `const nextStartLine = Math.max(1, currentBefore - 20)`
         and `const nextRangeCount = Math.min(20, currentBefore - 1)`.
      2. `try { readResult = await readSessionFileRange(deps.sshConn,
         deps.currentSessionFile, nextStartLine, nextRangeCount); } catch (err)
         { emitErrorFrame(err instanceof Error ? err.message : String(err));
         return; }` — mid-loop reader failure surfaces as an error frame just
         like the FIRST read failure at L1592.
      3. Prepend-not-append semantics: because the reader returns oldest-first
         and each successive iteration reads OLDER lines, the correct final
         order is `[iteration N frames, ..., iteration 2, iteration 1]`. Use
         `accumulator.unshift(...survivors)` (spread survivors onto the FRONT)
         so wire order stays chronological oldest→newest — matching what
         Test 1 asserts for line-number monotonicity.
      4. Log `[fetch-older-range] slice startLine=<nextStartLine> rawLines=<...> nonSkip=<...> total=<accumulator.length>`.
      5. Update `lastStartLine = nextStartLine`, `currentBefore = nextStartLine`,
         `refillIterations++`.
    - After the loop: `const messages = accumulator.slice(0, 20)`. Note we
      cap at 20 because a refill iteration could push the accumulator past
      20 (e.g. accumulator at 18, next read yields 15 non-skip → 33). We
      keep the OLDEST 20? No — we keep the NEWEST 20 (`accumulator.slice(-20)`
      would be wrong direction; accumulator is oldest-first after prepends).
      Correction: after the loop the accumulator is chronological (older
      iterations at the front). We want the NEWEST 20 wire frames (closest
      to the client's cursor) so the client's prepend is contiguous with
      what it already has. Use `accumulator.slice(-20)` and update
      `oldestLine` accordingly: `const oldestLine = messages.length > 0
      && typeof messages[0].line === "number" ? messages[0].line :
      lastStartLine`. Prefer the actual first surviving frame's line
      number so `oldestLoadedLine` on the client seeks to a real message
      and NOT a skip line the next click would re-read. Fall back to
      `lastStartLine` if the accumulator is empty (all-skip file case).
    - `const hasMore = oldestLine > 1`.
    - Log `[fetch-older-range] emit oldestLine=<oldestLine> messagesLen=<messages.length> hasMore=<hasMore> refillIterations=<refillIterations>`.
    - Emit `ws.send(JSON.stringify({ type: "fetch_older_range_batch",
      messages, oldestLine, hasMore }))` inside the existing try/catch
      that logs `[ws-server] send-failed`.
    - Update the comment on L1597–1598 to describe v2 refill semantics and
      reference this quick's ID (`quick-260822-7no`). Delete the "Test 8 lock
      — no refill" phrase.

    In `src/backend/claude-session/claude-session-server.fetch-older-range.test.ts`:
    - Update the top-of-file header comment at L26–28 to describe v2 refill
      semantics (cite `quick-260822-7no`). Remove the "LOCKED v1" and "partial
      batch, no refill" phrases.
    - Update the `makeSkipLine` helper doc at L105–109 to say "Test 9 policy
      lock: all-skip range refills until startLine=1".
    - REWRITE Test 8 in-place (do NOT delete or renumber). New name:
      `"Test 8: skip frames inside range → refill loop reads next slice until
      accumulator holds 20 non-skip frames (v2 refill policy — quick-260822-7no)"`.
      Fixture: mock `readSessionFileRange` to return two batches via
      `mockResolvedValueOnce`. Batch 1 (call args startLine=101, count=20):
      20 lines with 3 skips at indices 3, 8, 15 → 17 message lines with
      eventIds evt-101, evt-102, evt-103, evt-105… Batch 2 (call args
      startLine=81, count=20): 20 all-message lines with eventIds evt-81…evt-100.
      Client request: `beforeLine=121, count=20`. Assert exactly one `sent`
      frame, `messages.length === 20`, `oldestLine === 84` (line of first
      surviving frame in the newest-20 slice: batch1 contributes 17 lines
      starting at 101, batch2 contributes 20 lines starting at 81 → total
      accumulator = 37 in chronological oldest-first order [81..100, 101, 102,
      103, 105..108, 110..115, 117..120], newest-20 slice starts at index 17
      → line 84 through line 120), `hasMore === true`, `error` undefined,
      `readSessionFileRange` called exactly twice with the expected args.
      DOUBLE-CHECK the arithmetic when writing the fixture — precompute
      the accumulator and use the actual computed value for oldestLine.
    - ADD Test 9 (`"Test 9: all-skip file → refill until startLine=1, emit
      empty success (quick-260822-7no)"`). Fixture: 5 sequential
      `mockResolvedValueOnce` returning 20 skip lines each (or fewer for
      the last call if `beforeLine=101, count=20` → refill sequence is
      startLine=81, 61, 41, 21, 1 with rangeCount 20,20,20,20,20 (last
      call: startLine=Math.max(1, 21-20)=1, rangeCount=Math.min(20, 21-1)=20).
      Assert exactly one sent frame, `messages.length === 0`,
      `oldestLine === 1`, `hasMore === false`, no error, reader called
      exactly 5 times.
    - ADD Test 10 (`"Test 10: partial refill halts at top-of-file with fewer
      than 20 messages (quick-260822-7no)"`). Fixture: `beforeLine=25, count=20`.
      Batch 1 (startLine=5, rangeCount=20): 20 lines with 3 skips → 17
      messages. Batch 2 (startLine=1, rangeCount=4): 4 all-message lines.
      Accumulator = 21. Slice(-20) → 20 messages. Assert
      `messages.length === 20`, `oldestLine === (line of first surviving
      frame in newest-20 slice)`, `hasMore === (oldestLine > 1)`,
      reader called exactly twice.
    - ADD Test 11 (`"Test 11: 20 non-skip lines in FIRST slice → no refill,
      exactly one reader call (quick-260822-7no)"`). Fixture: 20 all-message
      lines from a single `mockResolvedValueOnce`. Assert
      `messages.length === 20`, reader called exactly once. This locks the
      "don't over-fetch when the first slice satisfies" invariant.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; npx vitest run --related src/backend/claude-session/claude-session-server.ts src/backend/claude-session/session-file-range-reader.ts &amp;&amp; npm run build:backend</automated>
  </verify>
  <done>
    Backend handler `handleFetchOlderRange` refills reads until accumulator
    has ≥20 non-skip frames OR startLine reaches 1. Test 8 rewritten for v2
    semantics; Tests 9, 10, 11 added; Tests 1–7 unchanged and still passing.
    Three structured log lines (`[fetch-older-range] req`, `slice` per
    iteration, `emit` once) fire on every request. Scoped vitest and
    `npm run build:backend` both green.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Frontend [pv-load-more] click + response instrumentation (no behavior change)</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx</files>
  <action>
    Pure-instrumentation task. NO behavior change. NO new state. NO changed
    render output. Two `console.info` sites; nothing else.

    Site A — inside `handleLoadOlder` at
    `src/ui/features/pretty-view/PrettyView.tsx` L757–783. Add the log
    AFTER the three defense-in-depth guards (L764–768) and BEFORE the
    `ws.send(JSON.stringify(payload))` on L776. Exact format:

      console.info(
        `[pv-load-more] click beforeLine=${oldestLoadedLine} ` +
        `currentMessagesLen=${messages.length} ` +
        `sessionHasMore=${sessionHasMore} ` +
        `sessionTotalLines=${sessionTotalLines}`,
      );

    Note `messages` is closed-over from the outer component scope — since
    `handleLoadOlder`'s deps array is `[oldestLoadedLine]` (see L783), the
    closure captures `messages` stale between renders. This is acceptable
    for the click log because (a) the log is diagnostic not load-bearing,
    (b) each render recreates the callback so a click will use the most
    recent commit's `messages` snapshot, and (c) adding `messages` to the
    deps array would recreate the callback on every message frame —
    unacceptable performance cost. DO NOT change the deps array. If you
    prefer strictly-current values, read `messagesLenRef.current` instead
    (defined near L1126 — verify by grep before use); either is fine.

    Site B — inside the `fetch_older_range_batch` case at L1678–1726.
    Requirements:
    - `preLen` MUST be captured inside `setMessages(prev => ...)` at L1706
      (`prev.length`), not from a stale outer-closure read.
    - `postLen` MUST be captured inside the same setter after the dedup
      loop finishes (`result.length`).
    - The log MUST fire exactly once per response for BOTH the error and
      success branches.

    Restructure the case body as follows:

      case "fetch_older_range_batch": {
        // (unchanged doc-comment block L1679–1695)
        if (parsed.error != null) {
          console.info(
            `[pv-load-more] response ok=false messagesLen=0 ` +
            `oldestLine=${parsed.oldestLine} hasMore=${parsed.hasMore} ` +
            `error="${parsed.error}" preLen=${messages.length} postLen=${messages.length}`,
          );
          setLoadOlderState("error");
          setLoadOlderError(parsed.error);
          loadOlderInFlightRef.current = false;
          break;
        }
        let logPreLen = 0;
        let logPostLen = 0;
        setMessages((prev) => {
          logPreLen = prev.length;
          const combined = [...parsed.messages, ...prev];
          const seen = new Set<string>();
          const result: typeof combined = [];
          for (const m of combined) {
            if (!seen.has(m.eventId)) {
              seen.add(m.eventId);
              result.push(m);
            }
          }
          logPostLen = result.length;
          return result;
        });
        console.info(
          `[pv-load-more] response ok=true messagesLen=${parsed.messages.length} ` +
          `oldestLine=${parsed.oldestLine} hasMore=${parsed.hasMore} ` +
          `error=undefined preLen=${logPreLen} postLen=${logPostLen}`,
        );
        setOldestLoadedLine(parsed.oldestLine);
        setSessionHasMore(parsed.hasMore);
        setLoadOlderState("idle");
        setLoadOlderError(null);
        loadOlderInFlightRef.current = false;
        break;
      }

    Justification for `let logPreLen/logPostLen` in the outer scope of the
    case block: React setters are synchronous when the setter function
    receives a callback — the callback runs immediately during the setter
    call in React 18's non-concurrent code paths that this file already
    depends on (search "setMessages((prev)" — 5 pre-existing sites at
    L1582, L1616, L1632, L1648, L1665 all rely on this same synchronous
    behavior). If a lint rule complains about `let` reassignment in a
    setter callback, the pattern is idiomatic in this file and existing
    sites confirm it's accepted.

    Tag `[pv-load-more]` MUST appear verbatim (bracket-p-v-hyphen-load-hyphen-more)
    so `grep '\[pv-load-more\]'` on the browser devtools console output
    or on any collected log dump filters cleanly.

    NO other edits — do not touch `handleWake`, the visibility gate, the
    other four `setMessages` sites, or any test file. Rely on existing
    PrettyView load-more tests to catch behavior regressions; the pure-log
    addition should not break any assertion.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; npx vitest run --related src/ui/features/pretty-view/PrettyView.tsx &amp;&amp; npm run build</automated>
  </verify>
  <done>
    Two `console.info` sites added: one in `handleLoadOlder` immediately
    before `ws.send`, one in the `fetch_older_range_batch` case covering
    both error and success branches. Structured tag `[pv-load-more]`
    verbatim. `preLen` and `postLen` captured inside the `setMessages`
    prev-callback. NO behavior change — existing PrettyView load-more
    tests all pass. `npm run build` green.
  </done>
</task>

</tasks>

<verification>
Executor's green gate (per fleet rule Ashley 2026-08-20, scoped tests only):

```bash
cd /home/ubuntu/skynet-tina
npx vitest run --related \
  src/backend/claude-session/claude-session-server.ts \
  src/backend/claude-session/session-file-range-reader.ts \
  src/ui/features/pretty-view/PrettyView.tsx
npm run build:backend
npm run build
```

All four commands MUST exit 0.

Grep sanity checks (executor should run these before committing):

```bash
# Backend v1 policy comment is gone.
grep -c "no refill" src/backend/claude-session/claude-session-server.ts
# → 0 expected

# Backend logs are wired.
grep -c "\[fetch-older-range\]" src/backend/claude-session/claude-session-server.ts
# → 3 expected (req, slice, emit)

# Frontend logs are wired.
grep -c "\[pv-load-more\]" src/ui/features/pretty-view/PrettyView.tsx
# → 3 expected (click + response-ok + response-error)

# Test 8 header reflects v2.
grep -c "quick-260822-7no" src/backend/claude-session/claude-session-server.fetch-older-range.test.ts
# → ≥4 expected (Test 8 header + Tests 9, 10, 11 headers)

# Refill iterations counter present.
grep -c "refillIterations" src/backend/claude-session/claude-session-server.ts
# → ≥3 expected (declaration, increment, emit log)
```

Scope guardrails — none of these should have changed:

```bash
git diff --stat src/backend/claude-session/session-file-range-reader.ts
# → empty (this file is READ-ONLY for this quick)

git diff --stat src/backend/claude-session/session-file-parser.ts
# → empty (parser unchanged)

git diff --stat src/ui/features/pretty-view/LoadMoreOlderButton.tsx
# → empty (pure component unchanged)

git diff --stat src/ui/features/pretty-view/PrettyView.load-more.test.tsx
# → empty (no test rewrites on the frontend)
```
</verification>

<success_criteria>
1. `handleFetchOlderRange` refills reads from the session file until the
   accumulator holds 20 non-skip wire frames OR the read cursor reaches
   line 1 — verifiable by grepping the backend file for the `while`
   condition against `accumulator.length < 20` and `currentBefore > 1`.
2. Wire response shape is byte-identical to v1:
   `{ type: "fetch_older_range_batch", messages, oldestLine, hasMore, error? }`.
   No new fields, no renamed fields.
3. Tests 1–7 pass unchanged (their fixtures return ≥20 non-skip lines
   from a single read → refill loop exits after iteration 1 → observable
   behavior identical).
4. Test 8 rewritten in-place for v2 refill semantics; Tests 9, 10, 11 added.
5. Three `[fetch-older-range]` logs (req, per-slice, emit) and three
   `[pv-load-more]` logs (click, response-success, response-error) fire on
   every load-more click → WS → response cycle.
6. Frontend behavior unchanged — existing `PrettyView.load-more.test.tsx`
   passes without modification.
7. Both scoped vitest suites green + `npm run build:backend` + `npm run build`
   all exit 0.
8. Executor commits (one or two commits, executor's discretion) and STOPS.
   Deploy is tina's responsibility (fleet rule Ashley 2026-08-08).
</success_criteria>

<output>
Create `.planning/quick/260822-7no-load-more-button-refill-to-20-bubbles-or/260822-7no-SUMMARY.md`
when done, following the standard summary template. Key sections to include:
- Refill loop's actual iteration counts against Sally's session file (if
  the executor has access — otherwise cite the Python-sim numbers from the
  background section: click 1 previously returned 3 bubbles, now should
  return 20; the executor may verify by running the backend once against
  Sally's session file if it's mounted, otherwise defer to tina's post-deploy
  verification).
- Any deviation from the planned refill semantics (e.g., if `accumulator.slice(-20)`
  proved incorrect and a different slice policy was substituted).
- Exact test names and IDs added (`Test 9`, `Test 10`, `Test 11`) so tina
  can grep for them post-ship.
</output>
