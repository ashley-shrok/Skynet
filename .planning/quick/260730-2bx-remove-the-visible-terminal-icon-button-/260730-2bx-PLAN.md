---
phase: 260730-2bx-remove-the-visible-terminal-icon-button-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/ComposeBox.tsx
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/terminal/Terminal.tsx
autonomous: true
requirements:
  - QUICK-260730-2bx

must_haves:
  truths:
    - "The Terminal-icon toggle button no longer renders in the ComposeBox aux row on any viewport (previously only shown on md+ via max-md:hidden)."
    - "Ctrl+Shift+O still toggles pretty-mode off/on (escape hatch to raw xterm.js preserved via useKeyboardTogglePrettyMode in AppShell + line 869 setIsPrettyMode in Terminal.tsx)."
    - "`npx tsc --noEmit` exits 0 with no unused-import or unused-variable errors introduced."
    - "Colocated vitest files for ComposeBox and PrettyView still pass (no test asserted on the toggle button, but they exercise the surrounding aux row + prop plumbing)."
  artifacts:
    - path: "src/ui/features/pretty-view/ComposeBox.tsx"
      provides: "ComposeBox with Terminal-icon toggle JSX + onTogglePrettyMode prop removed; Terminal dropped from lucide-react import"
      contains_not: "onTogglePrettyMode"
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "PrettyView with onTogglePrettyMode prop declaration, destructure, and ComposeBox forwarding removed"
      contains_not: "onTogglePrettyMode"
    - path: "src/ui/features/terminal/Terminal.tsx"
      provides: "Terminal.tsx no longer passes onTogglePrettyMode to PrettyView (line ~3012 removed); setIsPrettyMode keyboard-shortcut path at line 869 untouched"
      contains_not: "onTogglePrettyMode"
  key_links:
    - from: "src/ui/features/terminal/Terminal.tsx (Ctrl+Shift+O handler line ~869)"
      to: "setIsPrettyMode toggle"
      via: "useKeyboardTogglePrettyMode hook (unchanged in AppShell)"
      pattern: "setIsPrettyMode\\(\\(v\\)"
---

<objective>
Remove the visible Terminal-icon button from the ComposeBox aux row and clean up the now-orphan `onTogglePrettyMode` prop chain across ComposeBox -> PrettyView -> Terminal.tsx. The Ctrl+Shift+O keyboard shortcut path (useKeyboardTogglePrettyMode -> setIsPrettyMode at Terminal.tsx line 869) is the escape hatch to raw xterm.js and MUST remain functional. UI-removal only: no docs, no build, no deploy.

Purpose: The button was already `max-md:hidden` and duplicated the keyboard shortcut; Ashley wants it gone so the aux row does not advertise a mode switch. Escape hatch stays via keybind.
Output: Three source files modified; one atomic commit; tsc + focused vitest green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md

# Source files being modified (read for exact line context, then edit surgically)
@src/ui/features/pretty-view/ComposeBox.tsx
@src/ui/features/pretty-view/PrettyView.tsx
@src/ui/features/terminal/Terminal.tsx

# Confirmed reference points (already grep'd during planning — do not re-verify):
# - ComposeBox.tsx:2       lucide-react import includes `Terminal` — safe to drop, ONLY used at line 1377
# - ComposeBox.tsx:270-275 prop comment + `onTogglePrettyMode?: () => void;`
# - ComposeBox.tsx:301     destructured `onTogglePrettyMode,`
# - ComposeBox.tsx:1352-1379  JSX block (leading comment "Toggle-pretty-off button" through closing `)}`)
# - PrettyView.tsx:126-131 prop comment + `onTogglePrettyMode?: () => void;`
# - PrettyView.tsx:174     destructured `onTogglePrettyMode,`
# - PrettyView.tsx:1349    forwarding line `onTogglePrettyMode={onTogglePrettyMode}`
# - Terminal.tsx:3012      passing line `onTogglePrettyMode={() => setIsPrettyMode((v) => !v)}`
# - Terminal.tsx:869       KEEP — Ctrl+Shift+O shortcut path calling `setIsPrettyMode((v) => !v)`
# - Terminal.tsx:235       KEEP — `const [isPrettyMode, setIsPrettyMode] = useState(false);`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete ComposeBox Terminal-button JSX + prop, drop Terminal from lucide import</name>
  <files>src/ui/features/pretty-view/ComposeBox.tsx</files>
  <action>
    Make four surgical edits in `src/ui/features/pretty-view/ComposeBox.tsx`. Do NOT reflow surrounding code, do NOT touch other lucide icons, do NOT touch neighbouring aux buttons (Paperclip, ThumbsUp, Lightbulb, etc.).

    1. Line 2: remove the `Terminal,` identifier from the `lucide-react` named import. Preserve alphabetical order of the remaining icons: `Hourglass, Lightbulb, Paperclip, RefreshCw, RotateCcw, Square, ThumbsUp, X`. The `Terminal` identifier is only referenced at the JSX being deleted (verified by grep during planning) so removal will not cause an unresolved-reference.

    2. Lines 270-275: delete the four-line comment block "Optional callback for the aux-row Terminal-icon button..." and the `onTogglePrettyMode?: () => void;` prop line. Leave the surrounding `recycleActive?` prop (ends line 269) and the `className?: string;` prop (line 276) intact.

    3. Line 301: delete the `onTogglePrettyMode,` entry from the destructured props list inside `export function ComposeBox({ ... }: ComposeBoxProps)`. Keep the trailing comma pattern consistent with the neighbouring `recycleActive,` (line 300) and `className,` (line 302).

    4. Lines 1352-1379: delete the entire block starting with the leading comment `{/* Toggle-pretty-off button — flips pretty view off, revealing ... */}` through the closing `)}` on line 1379. Result: the `<div className="flex flex-row gap-1">` aux-button group (opened on line 1351) proceeds directly to the next comment block `{/* Paperclip attach button (Phase 05 UPLOAD-03) ... */}` starting on line 1380 — no blank-line gap, no orphan comment.

    Verify by grep: `grep -n "onTogglePrettyMode\|Terminal\b" src/ui/features/pretty-view/ComposeBox.tsx` must return zero matches after the edit.
  </action>
  <verify>
    <automated>grep -c "onTogglePrettyMode\|Terminal\b" src/ui/features/pretty-view/ComposeBox.tsx | grep -qx 0 &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>ComposeBox.tsx has zero references to `onTogglePrettyMode` or `Terminal` (identifier), and `npx tsc --noEmit` still exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Remove onTogglePrettyMode plumbing from PrettyView + Terminal.tsx caller</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx, src/ui/features/terminal/Terminal.tsx</files>
  <action>
    Remove the now-orphan `onTogglePrettyMode` prop chain in two files. Do NOT modify any other prop, and specifically do NOT touch Terminal.tsx line 235 (`useState`) or line 869 (Ctrl+Shift+O keyboard-shortcut `setIsPrettyMode((v) =&gt; !v)` call) — both stay verbatim.

    A) `src/ui/features/pretty-view/PrettyView.tsx`:
       1. Lines 126-131: delete the six-line comment block "Optional callback for the ComposeBox aux-row Terminal-icon button..." plus the `onTogglePrettyMode?: () =&gt; void;` prop line. Leave the preceding `onInjectedTurnReady?` prop (line 125) and the closing `}` (line 132) intact.
       2. Line 174: delete the `onTogglePrettyMode,` entry from the destructured props list inside `export function PrettyView({ ... }: PrettyViewProps)`. Neighbouring props (`onInjectedTurnReady,` on line 173 above, closing `}` on line 175 below) stay intact.
       3. Line 1349: delete the single line `onTogglePrettyMode={onTogglePrettyMode}` from the `&lt;ComposeBox ...&gt;` prop-forwarding block. Do NOT reflow the surrounding `asideActive=...` (line 1347), `onAsideDismiss=...` (line 1348), or `onSendWithAttachments=...` (line 1350) attributes.

    B) `src/ui/features/terminal/Terminal.tsx`:
       1. Line 3012: delete the single line `onTogglePrettyMode={() =&gt; setIsPrettyMode((v) =&gt; !v)}` from the `&lt;PrettyView ...&gt;` prop-passing block. The preceding `onInjectedTurnReady={handleInjectedTurnReady}` (line 3011) and the closing `/&gt;` (line 3013) stay intact.
       2. Do NOT modify line 235 (`useState`) or line 869 (keyboard-shortcut toggle) — both are required for the Ctrl+Shift+O escape hatch that MUST STAY per task scope.

    Verify by grep: `grep -rn "onTogglePrettyMode" src/` must return zero matches after both files are edited.
  </action>
  <verify>
    <automated>grep -rn "onTogglePrettyMode" src/ | wc -l | grep -qx 0 &amp;&amp; grep -qE "setIsPrettyMode\(\(v\) =&gt; !v\)" src/ui/features/terminal/Terminal.tsx &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>Zero `onTogglePrettyMode` references remain across `src/`. Terminal.tsx still contains the `setIsPrettyMode((v) =&gt; !v)` keyboard-shortcut call (line ~869 preserved). `npx tsc --noEmit` exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Run focused vitest suites and commit</name>
  <files>(no file edits — verification + git commit only)</files>
  <action>
    Run the closest colocated vitest files that exercise the modified components. Do NOT run the full test suite (per constraints — this is a UI removal). Do NOT run `npm run build`, do NOT push, do NOT deploy.

    1. Run: `npx vitest run src/ui/features/pretty-view/ComposeBox.test.tsx src/ui/features/pretty-view/ComposeBox.aside-morph.test.tsx src/ui/features/pretty-view/ComposeBox.aside-props.test.tsx src/ui/features/pretty-view/ComposeBox.recycle-disable.test.tsx src/ui/features/pretty-view/ComposeBox.voice.test.tsx src/ui/features/pretty-view/PrettyView.test.tsx src/ui/features/pretty-view/PrettyView.aside.test.tsx`
       - All must pass. Since no existing test asserts on the removed button (verified during planning: `grep onTogglePrettyMode` in all `.test.tsx` files returned zero hits), removal should be transparent. If any test unexpectedly fails, STOP and report — do not "fix" a test by weakening an assertion.

    2. Run: `npx tsc --noEmit` once more as a final gate — must exit 0.

    3. Stage the three modified files explicitly (do NOT use `git add .` / `-A`):
       `git add src/ui/features/pretty-view/ComposeBox.tsx src/ui/features/pretty-view/PrettyView.tsx src/ui/features/terminal/Terminal.tsx`

    4. Create ONE atomic commit with the exact message below (HEREDOC form). This lands as a small UI-cleanup patch; the orchestrator will handle skynet-patches.md bookkeeping + deploy authorization separately (per plan constraints — docs are explicitly out of scope for this plan).

       ```
       chore(compose-box): remove Terminal-icon toggle button (Ctrl+Shift+O shortcut retained)

       The visible Terminal-icon button in the ComposeBox aux row was already
       max-md:hidden and duplicated the Ctrl+Shift+O keyboard shortcut wired
       via useKeyboardTogglePrettyMode in AppShell. Removed the JSX block plus
       the now-orphan onTogglePrettyMode prop across ComposeBox -&gt; PrettyView
       -&gt; Terminal.tsx (declaration, destructure, forwarding, caller).

       Escape hatch to raw xterm.js is preserved: Terminal.tsx line ~869 still
       calls setIsPrettyMode((v) =&gt; !v) from the keyboard-shortcut handler,
       and useState for isPrettyMode is unchanged.

       Also dropped Terminal from the lucide-react named import in
       ComposeBox.tsx (no other usages remained after JSX removal).

       Quick task: 260730-2bx
       ```

    5. Run `git status` after the commit to confirm the working tree is clean and the commit landed.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q "compose-box.*Terminal-icon" &amp;&amp; git status --porcelain | grep -qv .</automated>
  </verify>
  <done>Focused vitest runs green, `npx tsc --noEmit` exits 0, single atomic commit on the current branch with the specified subject, working tree clean. No push, no build, no deploy.</done>
</task>

</tasks>

<verification>
Post-plan gates (all must hold before returning control to orchestrator):

1. `grep -rn "onTogglePrettyMode" src/` returns zero matches.
2. `grep -n "Terminal\b" src/ui/features/pretty-view/ComposeBox.tsx` returns zero matches.
3. `grep -n "setIsPrettyMode((v) =&gt; !v)" src/ui/features/terminal/Terminal.tsx` returns at least one match at the Ctrl+Shift+O keyboard-handler site (~line 869) — escape hatch preserved.
4. `npx tsc --noEmit` exits 0.
5. Named vitest files (7 test files listed in Task 3) all pass.
6. Exactly one new commit exists on the current branch touching only the three modified files. No untracked artifacts.
</verification>

<success_criteria>
- Terminal-icon button visually gone from ComposeBox aux row on all viewports.
- Ctrl+Shift+O still flips pretty-mode off/on (unchanged code path).
- tsc + focused vitest green.
- One atomic commit, three files modified, no unrelated changes.
- No push, no `npm run build`, no `docker compose` — plan explicitly ends at "commit made, tests green" per constraints.
</success_criteria>

<output>
Create `.planning/quick/260730-2bx-remove-the-visible-terminal-icon-button-/260730-2bx-SUMMARY.md` when done, using the standard summary template. The orchestrator will handle skynet-patches.md bookkeeping and any deploy authorization as a separate follow-up.
</output>
