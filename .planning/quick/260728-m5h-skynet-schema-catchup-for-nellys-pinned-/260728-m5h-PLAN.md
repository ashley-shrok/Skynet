---
phase: 260728-m5h-skynet-schema-catchup-for-nellys-pinned-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/claude-session/identity-artifact-reader.ts
  - src/backend/claude-session/claude-session-server.ts
  - src/backend/claude-session/identity-artifact-reader.count-bounties.test.ts
  - src/backend/claude-session/identity-artifact-reader.write-bounty-status.test.ts
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
  - src/ui/features/pretty-conversations/pretty-conversations.css
  - "~/.claude/identities/tina/skynet-patches.md"
  - CLAUDE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Backend bounty counter counts bounties where `pinned === true`, not `status === 'pinned'`"
    - "Backend rejects writes attempting to set `status: 'pinned'` (enum no longer includes it)"
    - "Patch #167's fleet-wide pinned filter shows the correct count again (not silently 0)"
    - "Per-row pinned badge renders on bounties with `pinned:true` regardless of status"
    - "Inline status-editor pill row shows 4 options (pinned option removed)"
    - "Mobile viewports (max-md) render `.pv-bounty-badge` visibly larger; desktop unchanged"
    - "All updated tests pass (count-bounties, write-bounty-status, PrettyConversationsPanel)"
    - "Skynet-patches.md has patch #168 entry and updated header count"
  artifacts:
    - path: "src/backend/claude-session/identity-artifact-reader.ts"
      provides: "Bounty counter using `pinned === true` boolean field"
      contains: "parsed.pinned === true"
    - path: "src/ui/features/pretty-conversations/pretty-conversations.css"
      provides: "Mobile pinned-badge size bump under max-md viewport"
      contains: "max-md"
    - path: "~/.claude/identities/tina/skynet-patches.md"
      provides: "Patch #168 entry documenting the schema catchup + mobile badge bump"
      contains: "#168"
  key_links:
    - from: "src/backend/claude-session/identity-artifact-reader.ts"
      to: "PrettyConversationsPanel filter/badge rendering"
      via: "counter + returned bounty shape"
      pattern: "parsed\\.pinned === true"
    - from: "src/ui/features/pretty-conversations/pretty-conversations.css"
      to: ".pv-bounty-badge on mobile"
      via: "max-md media query / Tailwind v4 max-md:* utilities"
      pattern: "max-md"
---

<objective>
Skynet schema catchup for Nelly's fleet-wide pinned-status migration: `pinned` moved from a `status` enum value to a separate boolean field orthogonal to lifecycle. Every previously-pinned bounty is now `status:"in_progress" + pinned:true`. Skynet backend still queries `.status === "pinned"` — patch #167's filter and the per-row pinned badge silently show 0 in production. Ship the backend counter fix, drop `pinned` from the allowed-status enum, update the inline status-editor pill row (5 -> 4 options), bump the mobile `.pv-bounty-badge` size (Ashley's companion ask), update all impacted tests, and document as patch #168 inline.

Purpose: Restore #167's filter + per-row pinned badge to non-zero counts and align Skynet with fleet-wide schema.
Output: Working counter, updated enum, 4-option pill row, larger mobile badge, passing tests, and inline docs update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/backend/claude-session/identity-artifact-reader.ts
@src/backend/claude-session/claude-session-server.ts
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
@src/ui/features/pretty-conversations/pretty-conversations.css
@src/backend/claude-session/identity-artifact-reader.count-bounties.test.ts
@src/backend/claude-session/identity-artifact-reader.write-bounty-status.test.ts
@src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend schema catchup + backend tests (atomic backend unit gated by strict build)</name>
  <files>
    src/backend/claude-session/identity-artifact-reader.ts,
    src/backend/claude-session/claude-session-server.ts,
    src/backend/claude-session/identity-artifact-reader.count-bounties.test.ts,
    src/backend/claude-session/identity-artifact-reader.write-bounty-status.test.ts
  </files>
  <action>
    Read `src/backend/claude-session/identity-artifact-reader.ts` and locate the pinned counter (~line 1041 per context; verify by grepping `parsed.status === "pinned"`). Change the counter predicate from `parsed.status === "pinned"` to `parsed.pinned === true`. Update the surrounding descriptive comments at ~line 987 and ~line 996 that reference the old `status === "pinned"` query — reword to reflect the new orthogonal `pinned:boolean` field (be specific: mention that `pinned` is now an independent boolean field orthogonal to the lifecycle `status`).

    In the same file, locate the allowed-status set at ~line 312 (has comment `Quick 260727-v0b: allowed status set for bounty-status updates`). Change from the current 5-value set to exactly: `["in_progress", "waiting_on_someone_else", "done", "dropped"]`. Preserve the surrounding structure — this is a targeted enum edit only.

    Then open `src/backend/claude-session/claude-session-server.ts` at ~line 50 and update the JSDoc comment listing allowed status values to match the new 4-value set (drop `pinned`). Also update any nearby prose that names the allowed values. Do NOT touch `pinnedCount` references — those are consumers of the counter and remain valid (the field name doesn't need renaming; only the predicate changed).

    Update backend tests to match the new schema:

    In `src/backend/claude-session/identity-artifact-reader.count-bounties.test.ts`: replace fixture shapes so the "counts pinned bounties" test asserts `{status:"in_progress", pinned:true}` IS counted and `{status:"in_progress", pinned:false}` (or `pinned` absent) is NOT counted. Also add/adjust a case that shows a `done`-status bounty with `pinned:true` is still counted (orthogonality: `pinned` is independent of lifecycle). Remove any lingering `status:"pinned"` fixtures — that value no longer exists.

    In `src/backend/claude-session/identity-artifact-reader.write-bounty-status.test.ts`: add or update a test asserting that a write attempting `status: "pinned"` is rejected by the allowed-status enum validation (expect the reader's rejection path — mirror the pattern of any existing "rejects invalid status" test). Also ensure all four remaining valid statuses (`in_progress`, `waiting_on_someone_else`, `done`, `dropped`) are still accepted by existing coverage; add cases if missing.

    Per hard constraint (patch #154 lesson): Backend touches MUST run the strict local backend typecheck — `npm run build:backend && npm run build` — not just `npx tsc --noEmit`. Frontend tsconfig does not catch backend TS errors.

    DO NOT push, docker build, or docker compose up. Commit only.
  </action>
  <verify>
    <automated>npm run build:backend &amp;&amp; npm run build &amp;&amp; npx vitest run src/backend/claude-session/identity-artifact-reader.count-bounties.test.ts src/backend/claude-session/identity-artifact-reader.write-bounty-status.test.ts</automated>
  </verify>
  <done>
    - `identity-artifact-reader.ts` uses `parsed.pinned === true` for the counter, comments at ~987/~996 updated
    - Allowed-status set at ~line 312 is exactly `["in_progress", "waiting_on_someone_else", "done", "dropped"]`
    - `claude-session-server.ts` JSDoc at ~line 50 lists the 4 new allowed values
    - `identity-artifact-reader.count-bounties.test.ts` fixtures use `pinned:boolean`; all cases pass
    - `identity-artifact-reader.write-bounty-status.test.ts` rejects `status:"pinned"` and accepts the four remaining values
    - `npm run build:backend && npm run build` passes cleanly
    - Both listed vitest files pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend pill row (5→4 options), mobile pinned-badge bump, panel tests, and inline docs update (patch #168)</name>
  <files>
    src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx,
    src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx,
    src/ui/features/pretty-conversations/pretty-conversations.css,
    "~/.claude/identities/tina/skynet-patches.md",
    CLAUDE.md
  </files>
  <action>
    Locate the inline status-editor pill row that patch #160 shipped (5-option row including `pinned`). It may live in `PrettyConversationsPanel.tsx` or a nearby file — grep `src/ui/` for a literal `"pinned"` co-occurring with `"in_progress"` to confirm the exact file. If it is in `PrettyConversationsPanel.tsx`, edit in place; if the pill component was extracted to a sibling file, edit the sibling and add that file to `files_modified` mentally (still stay within the pretty-conversations feature folder). Drop the `pinned` option from the pill list so the row shows exactly 4 options: `in_progress`, `waiting_on_someone_else`, `done`, `dropped`. Preserve ordering and any keyboard/aria attributes.

    Update patch #167's filter code in `PrettyConversationsPanel.tsx` to match the new schema: any local predicate that reads `.status === "pinned"` (either as part of the filter or the per-row badge render) must switch to `.pinned === true`. Grep the file for both `"pinned"` and `pinnedCount` to find every touchpoint. The `pinnedCount` value coming from the backend now reflects the new predicate (Task 1), so its consumers do not need renaming — only the local `.status === "pinned"` reads change.

    Bump the mobile `.pv-bounty-badge` size in `src/ui/features/pretty-conversations/pretty-conversations.css` for `max-md` viewports (mobile PWA per Ashley's clarification 2026-07-28). Bump font-size + padding to roughly 1.5–1.7x the desktop value; desktop unchanged. Prefer a `@media (max-width: 767.98px)` block (Tailwind v4 md breakpoint boundary) targeting `.pv-bounty-badge`. If the file already uses Tailwind `max-md:*` utilities inline, mirror that pattern instead — pick whichever matches the existing convention in this file (read the file first, then decide).

    Per patch #164 lesson: `.pv-bounty-badge` may compete with a shadcn wrapper's variant. First try without `!important`; if `getComputedStyle` (or visual verification) shows the bump losing the cascade, add `!important` on the font-size and padding declarations (or use Tailwind v4 `!` suffix if using utility classes). Note in a brief CSS comment which path you took and why. If `.pv-bounty-badge` is fork-owned CSS with no shadcn ancestor competing, skip `!important` — do not preemptively add it.

    Update `src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx`: the 5 tests patch #167 added must use `{pinned: true}` fixture shape (in addition to any status field) instead of `{status: "pinned"}`. Also update any test fixture that used `status:"pinned"` to `status:"in_progress", pinned:true` (or leave `pinned:false` for the "not pinned" case). Confirm the panel test file compiles and passes.

    Inline docs update (patch #168) — this is a fleet rule (Ashley 2026-07-27), NOT queued for later:

    1. Open `~/.claude/identities/tina/skynet-patches.md`. Bump the header count/index (find the current total-patches count and increment; the previous patch was #167, this is #168). Add a new entry for patch #168 titled something like: `#168 — Schema catchup: pinned is now a boolean field orthogonal to lifecycle (fleet migration) + mobile pinned-badge bump`. Body should summarize: (a) fleet-wide schema change context, (b) counter predicate flipped to `parsed.pinned === true`, (c) allowed-status enum dropped `pinned` (now 4 values), (d) inline status-editor pill row 5→4, (e) patch #167's filter + per-row badge restored (were silently 0), (f) mobile `.pv-bounty-badge` bump for Ashley's PWA. Mirror the existing entries' voice and length.

    2. Grep `CLAUDE.md` (the one at repo root, `~/skynet/CLAUDE.md`) for any lingering `.status === "pinned"` references or prose that names `pinned` as a status value. Update in place to reflect the new schema. If nothing matches, leave `CLAUDE.md` untouched — do not invent content.

    DO NOT push, docker build, or docker compose up. Commit only. After all commits, the executor STOPs with the phrase: "committed on the branch; not pushed, not built, not deployed".
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx &amp;&amp; grep -q "#168" "$HOME/.claude/identities/tina/skynet-patches.md" &amp;&amp; ! grep -RIn '\.status\s*===\s*"pinned"' src/ui/features/pretty-conversations/ &amp;&amp; ! grep -RIn '\.status\s*===\s*"pinned"' src/backend/claude-session/</automated>
  </verify>
  <done>
    - Inline status-editor pill row shows exactly 4 options (`in_progress`, `waiting_on_someone_else`, `done`, `dropped`)
    - All `.status === "pinned"` reads in `PrettyConversationsPanel.tsx` replaced with `.pinned === true`
    - `.pv-bounty-badge` visibly larger on `max-md` viewports; desktop pixel-equivalent unchanged
    - `!important` / `!` suffix used only if cascade check showed a shadcn variant winning (documented in a brief CSS comment)
    - `PrettyConversationsPanel.test.tsx` tests pass with new `pinned:true` fixture shape
    - `~/.claude/identities/tina/skynet-patches.md` has patch #168 entry and header count bumped
    - `CLAUDE.md` scrubbed of any stale `.status === "pinned"` prose (if present)
    - `npm run build` passes cleanly
    - Final commit followed by executor STOP phrase: "committed on the branch; not pushed, not built, not deployed"
    - No `git push`, no `docker` commands executed
  </done>
</task>

</tasks>

<verification>
Overall phase checks:

- `npm run build:backend && npm run build` passes end-to-end
- Backend vitest (`identity-artifact-reader.count-bounties.test.ts`, `identity-artifact-reader.write-bounty-status.test.ts`) green
- Frontend vitest (`PrettyConversationsPanel.test.tsx`) green
- Zero remaining `.status === "pinned"` occurrences in `src/backend/claude-session/` or `src/ui/features/pretty-conversations/`:
  `grep -RIn '\.status\s*===\s*"pinned"' src/backend/claude-session/ src/ui/features/pretty-conversations/` returns nothing
- Allowed-status set in `identity-artifact-reader.ts` is exactly the 4 values
- `~/.claude/identities/tina/skynet-patches.md` contains a `#168` entry
- Mobile viewport (dev tools ≤767px) renders `.pv-bounty-badge` at the bumped size; desktop unchanged (spot-check via computed styles if needed)
- No `git push`, no `docker build`, no `docker compose up` was run — executor STOP phrase present in final commit summary
</verification>

<success_criteria>
- Fleet-wide schema (`pinned` boolean orthogonal to `status`) is honored end-to-end in Skynet
- Patch #167's filter and the per-row pinned badge report non-zero counts again on production data
- Inline status-editor pill row has 4 options; enum rejects writes with `status:"pinned"`
- Mobile PWA users see a larger `.pv-bounty-badge`; desktop UX unchanged
- Patch #168 documented inline in `skynet-patches.md`; CLAUDE.md schema references updated
- Work is committed on the current branch; nothing pushed, built, or deployed
</success_criteria>

<output>
Create `.planning/quick/260728-m5h-skynet-schema-catchup-for-nellys-pinned-/260728-m5h-SUMMARY.md` when done.
</output>
