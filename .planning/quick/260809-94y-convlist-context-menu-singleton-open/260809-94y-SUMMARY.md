---
phase: quick-260809-94y
plan: "01"
subsystem: pretty-conversations
tags:
  - ui
  - context-menu
  - singleton
  - pretty-conversations
dependency_graph:
  requires: []
  provides:
    - context-menu-singleton-behavior
  affects:
    - src/ui/features/pretty-conversations/PrettyConversationRow.tsx
tech_stack:
  added: []
  patterns:
    - module-scoped singleton with re-entry guard
key_files:
  modified:
    - src/ui/features/pretty-conversations/PrettyConversationRow.tsx
    - src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx
decisions:
  - Singleton lives at module scope in PrettyConversationRow.tsx (no new helper file); minimal diff, no barrel/index changes needed
  - closeSelf via useCallback with empty deps (setCtxMenu identity is stable); ensures singleton fn identity is stable across renders
  - Unmount cleanup calls notifyMenuClosed unconditionally (idempotent); simpler than mirroring ctxMenu via a ref
  - Re-entry guard: null currentClose BEFORE calling prev() so prev's own onClose wrapper does not clobber the newly registered closeFn
metrics:
  duration: "~10 minutes"
  completed: "2026-08-09"
  tasks_completed: 2
  files_modified: 2
---

# Phase quick-260809-94y Plan 01: Context-Menu Singleton Summary

Module-scoped singleton (`notifyMenuOpened`/`notifyMenuClosed`) in `PrettyConversationRow.tsx` ensures only one row's context menu is open at a time; wired at desktop right-click, mobile long-press, wrapped `onClose`, and unmount cleanup.

## Tasks Completed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | Add module-scoped singleton + wire row open/close sites + unmount cleanup | `9b33f18` | PrettyConversationRow.tsx |
| 2 | Add singleton-behavior tests (Row B closes Row A; unmount clears singleton) | `ec3ae19` | PrettyConversationRow.test.tsx |

## What Was Built

### Singleton (module scope in PrettyConversationRow.tsx)

```ts
let currentClose: (() => void) | null = null;

export function notifyMenuOpened(closeFn: () => void): void {
  if (currentClose && currentClose !== closeFn) {
    const prev = currentClose;
    currentClose = null; // re-entry guard
    prev();
  }
  currentClose = closeFn;
}

export function notifyMenuClosed(closeFn: () => void): void {
  if (currentClose === closeFn) currentClose = null;
}
```

### Row wiring (4 sites)

1. `closeSelf = useCallback(() => setCtxMenu(null), [])` — stable identity via empty deps
2. `onRowContextMenu`: `notifyMenuOpened(closeSelf)` BEFORE `setCtxMenu({x,y})`
3. Long-press 500ms timer: `notifyMenuOpened(closeSelf)` BEFORE `setCtxMenu({x,y})`
4. `<PrettyConversationContextMenu onClose>`: `() => { notifyMenuClosed(closeSelf); closeSelf(); }`
5. Unmount cleanup `useEffect`: `notifyMenuClosed(closeSelf)` in return fn

### Tests (2 new)

- **S1**: Two rows rendered side-by-side; open A → 1 menu; open B → 1 menu (A's closed); Row B's menu verified by "Unpin" presence (Row B is `pinned=true`)
- **S2**: Render A, open menu, unmount A → singleton cleared; render B, open menu → succeeds cleanly

## Verification

- `PrettyConversationContextMenu.tsx`: UNCHANGED (iOS Safari bubbling click fix preserved)
- `notifyMenuOpened` exported exactly once (single definition)
- Two occurrences of `notifyMenuOpened(closeSelf)` (desktop + long-press)
- Full suite: 133 test files, 1668 passed, 6 skipped, 0 failures
- `npx tsc --noEmit` exits 0

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check: PASSED

- `src/ui/features/pretty-conversations/PrettyConversationRow.tsx` modified with singleton + wiring
- `src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` modified with S1 + S2 tests
- Commits `9b33f18` and `ec3ae19` exist in git log
- Full suite green (133 files, 0 failures)
- TypeScript clean
