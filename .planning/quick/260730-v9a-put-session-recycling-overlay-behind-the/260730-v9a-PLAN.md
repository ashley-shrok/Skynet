---
phase: quick-260730-v9a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/SessionHoldingOverlay.tsx
  - src/ui/features/pretty-view/SessionHoldingOverlay.test.tsx
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-view/DropOverlay.tsx
autonomous: true
requirements:
  - QUICK-260730-v9a — Put session-recycle overlay BEHIND IdentityBadge so badge stays visible + clickable during a recycle
user_setup: []

must_haves:
  truths:
    - "SessionHoldingOverlay renders with a z-index BELOW IdentityBadge (z-[101])."
    - "While SessionHoldingOverlay is mounted, IdentityBadge remains visible on screen (not covered by the scrim)."
    - "While SessionHoldingOverlay is mounted, IdentityBadge remains clickable (pointer events reach it, not the scrim)."
    - "The scrim still visually dims/blurs the chat-region (messages, tasks, shells) — the geometry contract from patch #74 + quick 260729-j8l is unchanged."
    - "Error variant (patch #122) still renders warm-red at the new z-index — no regression."
    - "Motion-channel guardrail (STATIC RefreshCcw — no animate-spin) still holds — no regression."
    - "Comments referencing the old stacking claim (overlay ABOVE IdentityBadge) are updated to reflect the reversed intent, with an explicit 'supersedes patch #111 rationale' note for the historical trail."
    - "`npx tsc --noEmit` exits 0."
    - "`npx vitest run` is fully green (872 pass / 6 skip / 0 fail — matches #215 baseline)."
    - "IdentityModal.tsx L584-589 z-[110] modal-backdrop is UNTOUCHED (different concept — modal-open backdrop, legitimate)."
  artifacts:
    - path: "src/ui/features/pretty-view/SessionHoldingOverlay.tsx"
      provides: "SessionHoldingOverlay component, z-index reversed to sit below IdentityBadge"
      contains: "z-[99]"
    - path: "src/ui/features/pretty-view/SessionHoldingOverlay.test.tsx"
      provides: "Component tests — any pinned z-index assertion updated"
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "Mount-site comment updated to reflect reversed stacking"
    - path: "src/ui/features/pretty-view/DropOverlay.tsx"
      provides: "Orientation comment updated so SessionHoldingOverlay z-band reference is not stale"
  key_links:
    - from: "src/ui/features/pretty-view/SessionHoldingOverlay.tsx"
      to: "IdentityBadge stacking context (z-[101])"
      via: "z-index class on overlay root"
      pattern: "z-\\[99\\]"
    - from: "src/ui/features/pretty-view/SessionHoldingOverlay.tsx"
      to: "patch #111 historical rationale"
      via: "comment block near overlay root className"
      pattern: "supersedes patch #111"
---

<objective>
Reverse patch #111's z-index elevation on SessionHoldingOverlay. Change `z-[110]` → `z-[99]` on the overlay root so IdentityBadge (z-[101]) sits ABOVE the scrim instead of being covered by it. Ashley wants to see who's speaking and be able to click the identity avatar even while a session is recycling.

Purpose: Restore access to the IdentityBadge affordance during the 2–15s recycle window. Patch #111 deliberately covered the badge; live feedback is that this was wrong — the badge needs to stay reachable mid-recycle.

Output: A one-file behavior change (z-[110] → z-[99] on SessionHoldingOverlay's scrim), plus three comment-only updates that keep the stacking narrative honest across SessionHoldingOverlay.tsx, PrettyView.tsx (mount-site comment), and DropOverlay.tsx (orientation comment). Test assertions updated to match the new z-index. No JSX reorders, no other component changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@src/ui/features/pretty-view/SessionHoldingOverlay.tsx
@src/ui/features/pretty-view/SessionHoldingOverlay.test.tsx
@src/ui/features/pretty-view/DropOverlay.tsx
@src/ui/features/pretty-view/PrettyView.tsx

# Reference (DO NOT MODIFY — different concept, legitimate):
# src/ui/features/pretty-view/IdentityModal.tsx L584-589 also has z-[110].
# That is the modal-open backdrop intentionally covering IdentityBadge WHEN
# the identity modal is OPEN. Distinct from the recycle overlay. Leave it.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reverse SessionHoldingOverlay z-index + update stacking comments + flip test assertions</name>
  <files>src/ui/features/pretty-view/SessionHoldingOverlay.tsx, src/ui/features/pretty-view/SessionHoldingOverlay.test.tsx, src/ui/features/pretty-view/PrettyView.tsx, src/ui/features/pretty-view/DropOverlay.tsx</files>
  <behavior>
    - Assertion: rendered SessionHoldingOverlay root has class `z-[99]` (NOT `z-[110]`).
    - Assertion: rendered SessionHoldingOverlay root still has classes `absolute` and `inset-0` (geometry contract from quick 260729-j8l is unchanged — patch #74 header block, A1 invariant).
    - Assertion: error variant (patch #122) still renders warm-red glyph tint `text-[hsl(0,72%,60%)]` and the copy "Session recycle failed — refresh to check" (A2 invariant preserved after z-index change).
    - Assertion: motion-channel guardrail still holds — no `.animate-spin` on either variant (A3 invariant preserved after z-index change).
    - Grep-gate: `grep -n 'z-\[110\]' src/ui/features/pretty-view/SessionHoldingOverlay.tsx src/ui/features/pretty-view/SessionHoldingOverlay.test.tsx | grep -v '^[^:]*:[^:]*:#' | wc -l` equals `0` after the change (i.e. no live `z-[110]` references remain in SessionHoldingOverlay's own files; comments explaining the OLD value referenced only via the "supersedes patch #111" phrase, not as a live class token).
    - Grep-gate: `grep -c 'z-\[110\]' src/ui/features/pretty-view/IdentityModal.tsx` equals `2` — IdentityModal.tsx L584 (comment "bumped z-40 → z-[110]") + L589 (live class on modal-backdrop) MUST remain untouched (different concept — modal-open backdrop).
  </behavior>
  <action>
    Reverse patch #111 on SessionHoldingOverlay's z-band, and keep the stacking-narrative comments honest across the three files that mention the old claim. Per QUICK-260730-v9a.

    ### 1. `src/ui/features/pretty-view/SessionHoldingOverlay.tsx`

    - **Line ~109 (the live class string):** change `"absolute inset-0 z-[110]",` to `"absolute inset-0 z-[99]",`. Rationale: 99 sits just below IdentityBadge's z-[101], mirroring the pattern DropOverlay.tsx uses at z-[95] with its "below IdentityBadge (z-[101]) so the badge stays clickable" comment (that file's L13-15). Chose 99 over lower values so the overlay still stacks above ordinary chat content (which has no explicit z-index and defaults to auto in the local stacking context) — the intent is "below the badge, above bubbles."

    - **Lines ~100-108 (the adjacent comment block that currently reads "z-[110] sits above IdentityBadge (z-[101]) but below app-modal dialogs (z-[500])"):** rewrite to describe the reversed intent. The comment must state:
      * The overlay now sits BELOW IdentityBadge (z-[99] < z-[101]) so the badge stays visible AND clickable during a session recycle.
      * Ashley wants to see who's speaking + open the identity modal even mid-recycle (the user-facing rationale — this is the "why").
      * This REVERSES patch #111. Include the exact phrase "supersedes patch #111 rationale" so the historical trail is greppable.
      * Retain the "still below app-modal dialogs (z-[500])" orientation fact — that stacking relationship is still true and still useful.
      * Retain the "pointer-events-auto blocks clicks and typing on everything the scrim covers" sentence — that behavior is unchanged, and it's important the comment makes clear that IdentityBadge is NOT covered by the scrim (so pointer-events-auto does not affect it — it's a sibling in the same stacking context, sitting above the scrim by z-index).

    - **Do NOT touch:** the patch #74 header block (L1-73), the `error?: boolean` prop, the glass-card className, the RefreshCcw import, or any error-variant styling. Only the scrim's z-index class and its adjacent explainer comment change.

    ### 2. `src/ui/features/pretty-view/SessionHoldingOverlay.test.tsx`

    - Grep the file for `z-\[110\]`. If ANY test assertion pins that specific value, flip it to `z-\[99\]`.
    - Verify (before saving) that the three existing invariants (A1 geometry / A2 error variant / A3 motion channel) still pass unchanged — none of them pin z-index today (A1 pins `absolute` + `inset-0` only). If those three tests are the only tests in the file, add ONE new assertion inside the A1 test (or as a fourth `it()` block if that reads cleaner) that pins `z-\[99\]` on the overlay root — so that a future regression to z-[110] (or any value ≥ 101) fails the test suite. Pattern: `expect(cls).toMatch(/\bz-\[99\]\b/);`.
    - Keep the file's header docblock accurate: if the new assertion is added as an A4, extend the header's "three invariants" list to "four invariants" and describe A4 as "z-index below IdentityBadge (z-[101]) — supersedes patch #111 which had elevated the overlay to z-[110]."

    ### 3. `src/ui/features/pretty-view/PrettyView.tsx` (comment-only)

    - Lines ~1196-1198 currently read: `Sits / above IdentityBadge (z-[110] > z-[101]) but below app-modal / dialogs (z-[500]) — component-local, not an app-modal event.`
    - Rewrite to: `Sits BELOW IdentityBadge (z-[99] < z-[101]) so the badge stays visible and clickable during a recycle (Ashley wants the identity affordance reachable mid-recycle — supersedes patch #111 rationale), still below app-modal dialogs (z-[500]) — component-local, not an app-modal event.`
    - Do NOT touch the surrounding JSX (`{showOverlay && <SessionHoldingOverlay ... />}` on the next line stays exactly as-is — no reorder, no gating change).

    ### 4. `src/ui/features/pretty-view/DropOverlay.tsx` (comment-only)

    - Line ~15 currently reads: `z-band from the SessionHoldingOverlay (z-[110]) and app-modals`.
    - Update the parenthetical to `(z-[99])` so this orientation fact stays true. Do NOT touch DropOverlay's own `z-[95]` class or its behavior — it already sits below IdentityBadge per its own comment, and that's fine.

    ### DO NOT TOUCH

    - `src/ui/features/pretty-view/IdentityModal.tsx` L584-589 — that `z-[110]` is the identity-modal's OPEN-state backdrop, a different concept (covers IdentityBadge only while the modal itself is open). Leave both the comment and the live class alone.
    - `IdentityBadge` component itself — z-[101] stays.
    - `PrettyView.tsx` JSX ordering — this is a z-index tweak only, not a mount-order change.
    - Identity-side files under `~/.claude/identities/tina/` — the orchestrator (Tina) owns skynet-patches.md + bounty-archive updates after this ships.

    Commit-only. No `git push`, no `docker compose`, no build. Orchestrator handles push after verification.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run src/ui/features/pretty-view/SessionHoldingOverlay.test.tsx src/ui/features/pretty-view/PrettyView.test.tsx 2>&amp;1 | tee /tmp/v9a-colocated.log &amp;&amp; grep -Ec 'FAIL|failed|✗|Unhandled Rejection' /tmp/v9a-colocated.log | { read n; [ "$n" -le 0 ] || { echo "Found $n failure markers"; exit 1; }; } &amp;&amp; grep -c 'z-\[110\]' src/ui/features/pretty-view/SessionHoldingOverlay.tsx | { read n; [ "$n" -eq 0 ] || { echo "z-[110] still present in SessionHoldingOverlay.tsx (found $n)"; exit 1; }; } &amp;&amp; grep -c 'z-\[99\]' src/ui/features/pretty-view/SessionHoldingOverlay.tsx | { read n; [ "$n" -ge 1 ] || { echo "z-[99] not present in SessionHoldingOverlay.tsx"; exit 1; }; } &amp;&amp; grep -c 'z-\[110\]' src/ui/features/pretty-view/IdentityModal.tsx | { read n; [ "$n" -eq 2 ] || { echo "IdentityModal.tsx z-[110] count changed (expected 2, got $n) — modal backdrop MUST stay untouched"; exit 1; }; }</automated>
  </verify>
  <done>
    - `src/ui/features/pretty-view/SessionHoldingOverlay.tsx` line ~109 uses `z-[99]` (not `z-[110]`).
    - The adjacent comment block explains the reversal, cites "supersedes patch #111 rationale", retains the "below z-[500] app-modals" orientation fact, and explains that IdentityBadge remains visible + clickable during a recycle.
    - `SessionHoldingOverlay.test.tsx` has an assertion pinning `z-[99]` on the overlay root, and its docblock reflects the invariant count (three → four, if added as A4).
    - `PrettyView.tsx` L1197-area comment updated to `z-[99] &lt; z-[101]` with the reversed rationale + patch #111 supersession note. JSX unchanged.
    - `DropOverlay.tsx` L15 parenthetical updated to `(z-[99])`. DropOverlay's own z-[95] and behavior unchanged.
    - `IdentityModal.tsx` untouched — grep still finds exactly 2 hits for `z-[110]` (comment on L584 + live class on L589).
    - `npx tsc --noEmit` exits 0.
    - Colocated tests (SessionHoldingOverlay.test.tsx + PrettyView.test.tsx) pass, and captured output has 0 real failure markers.
    - Full `npx vitest run` (run as part of the phase-close review, not this task's verify — kept out of the task's automated verify to keep runtime bounded) is green at the #215 baseline (872 pass / 6 skip / 0 fail).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → SessionHoldingOverlay (DOM) | User pointer / keyboard events during recycle window |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v9a-01 | Denial of Service (UX) | IdentityBadge affordance during session recycle | mitigate | Set overlay z-index BELOW IdentityBadge (z-[99] &lt; z-[101]) so the badge stays reachable — this IS the fix. Verified by grep-gate + test assertion pinning z-[99]. |
| T-v9a-02 | Tampering (regression) | IdentityModal modal-backdrop z-[110] on L584-589 | mitigate | Grep-gate asserts IdentityModal.tsx z-[110] hit-count stays at exactly 2 after the change. If the count changes, verify fails and the change is rejected. Different concept from the recycle overlay — must not be swept up in the reversal. |
| T-v9a-03 | Information Disclosure | pointer-events-auto scrim | accept | Unchanged from patch #74 — scrim still blocks clicks/typing on the chat-region it covers. IdentityBadge sits ABOVE the scrim by z-index and is a sibling in the local stacking context, so pointer-events-auto on the scrim does not affect it. UX affordance only, not a security boundary (see T-260719-5ym-02). |
| T-v9a-SC | Tampering | package installs | accept | Zero new packages. Grep-gate: `git diff --name-only` post-change includes NO `package.json` / `package-lock.json` / `pnpm-lock.yaml` changes. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- Colocated tests pass: `npx vitest run src/ui/features/pretty-view/SessionHoldingOverlay.test.tsx src/ui/features/pretty-view/PrettyView.test.tsx`.
- Full suite (phase-close review, not per-task): `npx vitest run` — 872 pass / 6 skip / 0 fail (#215 baseline).
- Captured output failure-marker grep: `grep -Ec 'FAIL|failed|✗|Unhandled Rejection' /tmp/v9a-colocated.log` returns 0.
- Grep-gate: `grep -c 'z-\[110\]' src/ui/features/pretty-view/SessionHoldingOverlay.tsx` = 0 (live class removed).
- Grep-gate: `grep -c 'z-\[99\]' src/ui/features/pretty-view/SessionHoldingOverlay.tsx` ≥ 1 (new value present).
- Grep-gate: `grep -c 'z-\[110\]' src/ui/features/pretty-view/IdentityModal.tsx` = 2 (untouched — comment on L584 + live class on L589).
- Grep-gate: `grep -c 'supersedes patch #111' src/ui/features/pretty-view/SessionHoldingOverlay.tsx` ≥ 1 (historical trail phrase present).
</verification>

<success_criteria>
1. IdentityBadge (z-[101]) visually sits ABOVE the SessionHoldingOverlay scrim (z-[99]) during a recycle — verifiable in code by z-index comparison, verifiable in tests by the pinned `z-[99]` assertion.
2. IdentityBadge remains clickable during a recycle — the overlay is a sibling below the badge in the stacking context, so pointer-events-auto on the scrim does not block clicks that hit the badge.
3. Comments across SessionHoldingOverlay.tsx (L100-108), PrettyView.tsx (L1197-area), and DropOverlay.tsx (L15) reflect the reversed stacking claim consistently — no comment still asserts overlay-above-badge.
4. Patch #111's rationale is superseded on-record via the "supersedes patch #111 rationale" phrase in SessionHoldingOverlay.tsx — greppable.
5. IdentityModal.tsx's independent z-[110] modal-backdrop is untouched — enforced by grep-gate hit-count assertion.
6. No new dependencies. No JSX reorders. Commit-only — orchestrator handles push and any archival work under `~/.claude/identities/tina/` after.
</success_criteria>

<output>
Create `.planning/quick/260730-v9a-put-session-recycling-overlay-behind-the/260730-v9a-SUMMARY.md` when done.
</output>
