---
phase: 260723-6xb-patch-133
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
  - src/ui/AppShell.tsx
  - src/ui/index.css
  - public/manifest.webmanifest
  - public/manifest.json
  - index.html
autonomous: true
requirements:
  - BOUNTY-666704b0-TODO-02  # Delete gear DropdownMenu + all renderSettingsMenuItems usage
  - BOUNTY-666704b0-TODO-03  # Delete shadcn Tooltip usage from desktop header buttons
  - BOUNTY-666704b0-TODO-04  # Inline text-muted-foreground token replacements
  - BOUNTY-666704b0-TODO-05  # Port prototype.html visual language (backdrop, spacing, typography, header layout)
  - BOUNTY-666704b0-TODO-06  # Rows + PinAction stay largely as-is
  - BOUNTY-666704b0-TODO-10  # Pick Skynet base color from --color-pv-base-* palette (#0a0b12)
  - BOUNTY-666704b0-TODO-11  # Rebase .dark --background to Skynet base
  - BOUNTY-666704b0-TODO-12  # Update theme_color + background_color in both manifests
  - BOUNTY-666704b0-TODO-13  # Update <meta name="theme-color"> in index.html

must_haves:
  truths:
    - "PrettyConversationsPanel.tsx has ZERO imports from @/components/dropdown-menu or @/components/tooltip"
    - "PrettyConversationsPanel.tsx has ZERO imports of Settings from lucide-react and ZERO import of renderSettingsMenuItems from @/sidebar/SettingsRow"
    - "PrettyConversationsPanel.tsx has ZERO occurrences of the string 'text-muted-foreground' (all four remaining sites at lines 229/248/368/371 pre-edit are replaced with inline hex/rgba)"
    - "PrettyConversationsPanel props typedef removes onRailClick and isAdmin fields (they only fed the gear); showGear const declaration deleted; settingsLabel translation deleted"
    - "AppShell.tsx PrettyConversationsPanel call-site no longer passes onRailClick or isAdmin (lines 1422-1426 pre-edit removed); settingsRowSlot mechanism at lines 1442-1446 UNCHANGED"
    - "Header container class is `px-4 py-3` (prototype .top-strip padding: 12px 16px), not `px-3 py-2`"
    - ".dark { --background } in src/ui/index.css is `#0a0b12` (was `oklch(0.155 0.004 128.73)`)"
    - "public/manifest.webmanifest theme_color AND background_color are both `#0a0b12` (was `#080808`)"
    - "public/manifest.json theme_color AND background_color are both `#0a0b12` (was `#09090b`)"
    - "index.html <meta name=\"theme-color\" content=\"#0a0b12\"> (was `#080808`)"
    - "`npx tsc --noEmit` exits clean with no new errors introduced by these edits"
    - "`npx vitest run src/ui/features/pretty-conversations/` passes — Tests 9 and 10 rewritten to assert the gear button NEVER renders in either variant regardless of onRailClick/isAdmin absence; all other tests untouched"
    - "Panel LOC drops from ~444 to ~370 (± a handful) after gear branch + imports + prop deletions"
    - "One atomic git commit lands the five-file diff; NO deploy, NO push (Ashley bundles with prior UAT commits)"
  artifacts:
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "Shadcn-free, prototype-fidelity conversations panel"
      contains: "PrettyConversationsPanel"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx"
      provides: "Updated Vitest coverage with rewritten Tests 9/10 (gear NEVER renders)"
      contains: "PrettyConversationsPanel"
    - path: "src/ui/AppShell.tsx"
      provides: "PrettyConversationsPanel call-site without onRailClick/isAdmin props"
      contains: "PrettyConversationsPanel"
    - path: "src/ui/index.css"
      provides: "Skynet base color rebase at .dark --background"
      contains: "#0a0b12"
    - path: "public/manifest.webmanifest"
      provides: "Skynet PWA manifest with #0a0b12 theme/background"
      contains: "#0a0b12"
    - path: "public/manifest.json"
      provides: "Skynet PWA manifest.json with #0a0b12 theme/background"
      contains: "#0a0b12"
    - path: "index.html"
      provides: "index.html <meta theme-color> rebased to #0a0b12"
      contains: "content=\"#0a0b12\""
    - path: ".planning/quick/260723-6xb-patch-133-strip-shadcn-wrappers-from-pre/260723-6xb-SUMMARY.md"
      provides: "Patch #133 SUMMARY with iOS reinstall note for post-deploy safe-area verification"
      min_lines: 40
---

<objective>
Patch #133 — strip shadcn wrappers from PrettyConversationsPanel, port prototype.html visual fidelity, rebase Skynet base color to #0a0b12 across CSS + both manifests + index.html.

Purpose: Ashley 2026-07-23 called out that the shipped Phase 10 panel still has "bones of Skynet" via shadcn Tooltip + DropdownMenu wrappers + `text-muted-foreground` theme tokens even though the design was clean-slate. Companion decision: rebase the Skynet base color from the green-tinted Skynet `--background` (oklch 0.155 0.004 128.73) to `#0a0b12` — the darkest anchor from the pretty-view `--color-pv-*` palette (`--color-pv-base-end`). This fixes both (a) the visible Skynet ancestry Ashley flagged in the conversation panel, and (b) the iOS PWA safe-area seam that surfaces when body background ≠ manifest theme_color.

Output:
- PrettyConversationsPanel.tsx: shadcn Tooltip + DropdownMenu + Settings icon + renderSettingsMenuItems + `showGear` branch + `text-muted-foreground` tokens ALL removed; onRailClick/isAdmin props removed from typedef (they only fed the gear); header padding upgraded to `px-4 py-3` per prototype `.top-strip { padding: 12px 16px }`
- AppShell.tsx call-site: onRailClick + isAdmin props dropped from `<PrettyConversationsPanel>` invocation
- PrettyConversationsPanel.test.tsx: Tests 9 and 10 rewritten to assert gear NEVER renders in either variant (previous "renders when onRailClick provided" contract retired)
- src/ui/index.css `.dark --background`: `oklch(0.155 0.004 128.73)` → `#0a0b12`
- public/manifest.webmanifest: theme_color + background_color both → `#0a0b12`
- public/manifest.json: theme_color + background_color both → `#0a0b12`
- index.html: `<meta name="theme-color" content="#080808">` → `content="#0a0b12"`
- One atomic git commit; NO deploy, NO push (Ashley bundles this with the pending 4 UAT commits: 558749a, 68e4f62, b749bf1, 536d224)
- SUMMARY.md with the iOS reinstall note (manifest colors cache on install — Ashley must reinstall the PWA post-deploy to see the safe-area seam disappear)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
@src/ui/index.css
@public/manifest.webmanifest
@public/manifest.json
@index.html

# Prototypes as visual/spacing/typography source of truth
@/home/ubuntu/.claude/identities/tina/bounties/archive/pretty-conversations-panel-redesign/prototype.html
@/home/ubuntu/.claude/identities/tina/bounties/archive/pretty-conversations-panel-redesign/desktop.html

# Bounty scope + 14 todos
@/home/ubuntu/.claude/identities/tina/bounties/phase10-shadcn-strip-and-prototype-fidelity/bounty.json

# Identity file — READ § "Skynet direction — Ship of Theseus" (base-color palette rationale),
# § "Learned preferences" patch #81 shadcn `!` important cascade lesson (this patch REMOVES shadcn
# wrappers so the ! trap goes away, but the mental model is still load-bearing)
@/home/ubuntu/.claude/identities/tina/tina.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strip shadcn wrappers, port prototype visual fidelity, drop dead props from AppShell + tests</name>
  <files>
    src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx,
    src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx,
    src/ui/AppShell.tsx
  </files>
  <behavior>
    - PrettyConversationsPanel.test.tsx Test 9 (currently: "desktop variant with onRailClick renders the gear (Settings) button") is REWRITTEN to assert the inverse invariant: `expect(queryByRole("button", { name: /settings/i })).toBeNull()` even when the panel is rendered with no onRailClick/isAdmin props on desktop variant. Rename the test title to "Test 9: desktop variant does NOT render the gear button (gear removed patch #133)". The wrapping `describe` label updates from "desktop gear" to "desktop gear removed (patch #133)".
    - PrettyConversationsPanel.test.tsx Test 10 (currently: "mobile variant does NOT render the gear button even with onRailClick provided") stays semantically the same but its `render()` call MUST drop the `onRailClick={vi.fn()}` and `isAdmin={true}` props — those are removed from the typedef, passing them will TS-error. Rename to "Test 10: mobile variant does NOT render the gear button (gear removed patch #133)".
    - All OTHER tests in the file (Tests 1-8, 11-13+) are UNTOUCHED — they never referenced the gear.
    - AppShell.tsx `<PrettyConversationsPanel>` call-site (starts line ~1412) drops the `onRailClick={(view) => { handleRailClick(view); if (isMobile) setSidebarOpen(false); }}` block AND `isAdmin={isAdmin}` line. The `settingsRowSlot={isTouchDevice ? <SettingsRow onRailClick={handleRailClick} isAdmin={isAdmin} /> : undefined}` prop is UNCHANGED — it's a separate mechanism (mobile bottom-of-scroller settings row) that survives the gear removal.
    - Panel renders in both variants without runtime errors; header layout branches on `variant` verbatim (desktop shows "Conversations" title + pencil; mobile shows empty-left + pencil).
    - All 4 replaced text-color sites (formerly `text-foreground/90`, `text-foreground`, `text-muted-foreground/50` ×2) render with the inline values specified in the action; DOM-verified via computed style OR grep confirming no `text-muted-foreground` / `text-foreground` substrings remain in the panel file.
    - Header container class transitions from `px-3 py-2` to `px-4 py-3` (prototype `.top-strip { padding: 12px 16px }`).
  </behavior>
  <action>
    STEP A — PrettyConversationsPanel.tsx surgical edits (~444 LOC → ~370 LOC):

    A1. Delete these imports (lines 44 + 47-57 + 66):
        - Remove `Settings` from the lucide-react import list (line 44). Keep `MessagesSquare, Monitor, Pencil`.
        - Remove the entire `DropdownMenu`, `DropdownMenuContent`, `DropdownMenuTrigger` import block from `@/components/dropdown-menu` (lines 47-51).
        - Remove the entire `Tooltip`, `TooltipContent`, `TooltipProvider`, `TooltipTrigger` import block from `@/components/tooltip` (lines 52-57).
        - Remove `import { renderSettingsMenuItems } from "@/sidebar/SettingsRow";` (line 66).
        - Keep `import type { RailView } from "@/sidebar/AppRail";` for now — but note it is likely dead after removing onRailClick. Grep the file post-edit: if RailView has ZERO remaining references, remove the import too. If unsure, keep it (harmless).

    A2. Remove `onRailClick` and `isAdmin` fields from the component's props typedef (lines 76-77 in the function signature AND lines 90-93 in the JSDoc-typed props object). Also delete the `// Same as ConversationsPanel — route to admin destinations via ...` JSDoc paragraph immediately above the removed fields. Leave every other prop untouched.

    A3. Delete the `showGear` const declaration (line 141): `const showGear = variant === "desktop" && typeof onRailClick === "function";`. It's now dead.

    A4. Delete the `settingsLabel` translation constant (lines 206-208): `const settingsLabel = t("nav.conversations.settings", { defaultValue: "Settings & Admin" });`. It's now dead.

    A5. Delete the entire `{showGear && ( ... )}` JSX block (lines 257-291 pre-edit) — the `<TooltipProvider><Tooltip><DropdownMenu><TooltipTrigger asChild><DropdownMenuTrigger asChild><button ...><Settings ...></DropdownMenuTrigger></TooltipTrigger><TooltipContent>...</TooltipContent><DropdownMenuContent>{renderSettingsMenuItems({...})}</DropdownMenuContent></DropdownMenu></Tooltip></TooltipProvider>` sandwich. After deletion, the header's right-side icon group is JUST the pencil button.

    A6. Prototype-fidelity inline hex/rgba color swaps (four sites):
        - Line 227 pre-edit: header container class change `px-3 py-2` → `px-4 py-3`. Full class becomes: `"shrink-0 flex items-center justify-between px-4 py-3 border-b border-white/[0.06]"`. This matches prototype.html `.top-strip { padding: 12px 16px }` (16px x = px-4 with default rem 4px * 4 = 16px; 12px y = py-3).
        - Line 229 pre-edit desktop title span: replace `text-foreground/90` with `text-[rgba(240,234,224,0.9)]`. Note: prototype.html declares `--pv-text-primary: #f0eae0;` — `#f0eae0` = rgb(240,234,224). Applied at 0.9 alpha per the prototype's title-tier treatment.
        - Line 248 pre-edit pencil button: replace `text-foreground` with `text-[#f0eae0]`. Direct hex form (no alpha) matches prototype's primary-text tier.
        - Line 368 pre-edit RDP Monitor glyph: replace `text-muted-foreground/50` with `text-[#5c6070]/50`. Prototype `--pv-text-hint: #5c6070`.
        - Line 371 pre-edit RDP label span: replace `text-muted-foreground/50` with `text-[#5c6070]/50` (same hint-tier).

    A7. After edits, MANUALLY grep the panel file to verify:
        `grep -c 'text-muted-foreground\|text-foreground\|DropdownMenu\|Tooltip\|renderSettingsMenuItems\|"Settings\|Settings,\|Settings }' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`
        Should return `0` — if it returns anything, list them for verification (there may be a legitimate string mention in a comment that's fine to leave; adjust if a real code reference survived).

    STEP B — AppShell.tsx call-site prop cleanup:

    B1. In `<PrettyConversationsPanel ...>` (starts line ~1412):
        - Delete the entire `onRailClick={(view) => { handleRailClick(view); if (isMobile) setSidebarOpen(false); }}` block (lines 1422-1425 pre-edit) INCLUDING the JSDoc comment lines immediately above it that reference the gear's admin routing (if any adjacent comment references the removed gear, remove those comment lines too — leave surrounding comments about other props intact).
        - Delete the `isAdmin={isAdmin}` line (line 1426 pre-edit).
        - LEAVE UNTOUCHED: `variant`, `onConversationSelected`, `settingsRowSlot`, `hostTree`, `onCreateSession`, `onDetachedRowClick`, `onRdpRowClick` — every other prop stays as-is. The `settingsRowSlot={isTouchDevice ? <SettingsRow onRailClick={handleRailClick} isAdmin={isAdmin} /> : undefined}` line is a DIFFERENT mechanism (mobile bottom-of-scroller settings row) and MUST survive.
        - If `handleRailClick` is now unused ANYWHERE else in AppShell after this edit, do NOT touch its declaration — SettingsRow at line ~1444 still consumes it. If `isAdmin` becomes unused after this AND after settingsRowSlot: `isAdmin` at line ~1444 still consumes it, so keep its declaration too. Only remove prop wiring at the PrettyConversationsPanel call-site.

    STEP C — PrettyConversationsPanel.test.tsx test rewrites:

    C1. Test 9 (currently lines 480-493) — REWRITE:
        - Rename `describe` title from `"PrettyConversationsPanel: desktop gear"` to `"PrettyConversationsPanel: desktop gear removed (patch #133)"`.
        - Rename `it()` title from `"Test 9: desktop variant with onRailClick renders the gear (Settings) button"` to `"Test 9: desktop variant does NOT render the gear button (patch #133 removed shadcn DropdownMenu gear entirely)"`.
        - Change the `render(<PrettyConversationsPanel variant="desktop" onRailClick={vi.fn()} isAdmin={false} />)` call to `render(<PrettyConversationsPanel variant="desktop" />)` — dropping the props that no longer exist on the typedef.
        - Change the assertion from `expect(queryByRole("button", { name: /settings/i })).toBeTruthy();` to `expect(queryByRole("button", { name: /settings/i })).toBeNull();`.
        - Update the inline comment "The DropdownMenuTrigger button carries aria-label..." to something like "// Post-patch-#133: gear DropdownMenu removed entirely; no button with a settings-matching aria-label should exist in either variant."

    C2. Test 10 (currently lines 499-510) — LIGHT REWRITE:
        - Rename `describe` title from `"PrettyConversationsPanel: mobile gear suppression"` to `"PrettyConversationsPanel: mobile gear removed (patch #133)"`.
        - Rename `it()` title from `"Test 10: mobile variant does NOT render the gear button even with onRailClick provided"` to `"Test 10: mobile variant does NOT render the gear button (patch #133 removed shadcn DropdownMenu gear entirely)"`.
        - Change the `render(<PrettyConversationsPanel variant="mobile" onRailClick={vi.fn()} isAdmin={true} />)` call to `render(<PrettyConversationsPanel variant="mobile" />)`.
        - Assertion `expect(queryByRole("button", { name: /settings/i })).toBeNull();` — UNCHANGED.

    C3. Every other test in the file — UNTOUCHED. Do not modify Tests 1-8 or Tests 11+.

    STEP D — Verify PrettyConversationsPanel.test.tsx imports don't reference removed items:
    Grep the test file for `renderSettingsMenuItems`, `DropdownMenu`, `Tooltip`, `Settings` — if any survive as unused imports (e.g., a `vi.mock("@/sidebar/SettingsRow", ...)` block), leave the mock (it may be needed by other tests) but confirm no import statement references types/values that are now dead.

    Do NOT touch NewSessionDialog anywhere. Do NOT touch PrettyConversationRow.tsx, PinAction.tsx, or tokens.ts — Ashley's scope note: "Rows + PinAction stay as-is (already clean)".
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx tsc --noEmit 2>&1 | tail -30 && echo "---VITEST---" && npx vitest run src/ui/features/pretty-conversations/ 2>&1 | tail -40 && echo "---GREP-GATE-1---" && grep -c 'text-muted-foreground\|DropdownMenu\|Tooltip\|renderSettingsMenuItems' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx | grep -v '^#' && echo "---GREP-GATE-2 (Settings identifier — must be 0, allowing 'Settings' in string literals if any)---" && grep -nE '\b(DropdownMenu|Tooltip|renderSettingsMenuItems)\b|from "@/components/dropdown-menu"|from "@/components/tooltip"' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx || echo "gate-2 clean"</automated>
  </verify>
  <done>
    tsc --noEmit exits clean with no new errors attributable to these edits (pre-existing errors elsewhere in the codebase are acceptable — compare against baseline before edit if in doubt). vitest scoped to `src/ui/features/pretty-conversations/` passes all tests (including the two rewritten Tests 9 and 10). Grep gate 1 returns `0`. Grep gate 2 prints `gate-2 clean` (no import statements from @/components/dropdown-menu or @/components/tooltip, no bare identifier usage of DropdownMenu/Tooltip/renderSettingsMenuItems). AppShell.tsx no longer passes onRailClick or isAdmin to PrettyConversationsPanel. Panel LOC dropped from ~444 to ~370 (± a handful).
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebase Skynet base color to #0a0b12 across CSS + both manifests + index.html</name>
  <files>
    src/ui/index.css,
    public/manifest.webmanifest,
    public/manifest.json,
    index.html
  </files>
  <action>
    STEP A — src/ui/index.css:
    Locate line 192 in the `.dark { ... }` block:
      `--background: oklch(0.155 0.004 128.73);`
    Replace with:
      `--background: #0a0b12;`
    DO NOT modify any other line in this block. DO NOT modify the `--color-pv-base` / `--color-pv-base-mid` / `--color-pv-base-end` palette at lines 119-121 — those are the palette source we're anchoring to and MUST stay intact. DO NOT modify the light-mode `:root { --background: oklch(1 0 0); }` at line 156 — Ashley only uses dark. DO NOT touch any of the theme-specific class blocks (`.dracula`, `.catppuccin`, `.nord`, `.solarized`, `.tokyo-night`, `.one-dark`, `.gruvbox`) — those are pre-existing Skynet upstream themes and untouched by the Skynet rebase.

    STEP B — public/manifest.webmanifest:
    Change both:
      `"background_color": "#080808",` → `"background_color": "#0a0b12",`
      `"theme_color": "#080808",` → `"theme_color": "#0a0b12",`
    No other keys are modified.

    STEP C — public/manifest.json:
    Change both:
      `"theme_color": "#09090b",` → `"theme_color": "#0a0b12",`
      `"background_color": "#09090b",` → `"background_color": "#0a0b12",`
    No other keys are modified. (Note the pre-edit value is `#09090b` here, different from webmanifest's `#080808` — this is expected legacy Skynet drift; both converge to `#0a0b12` after this task.)

    STEP D — index.html (repo root, NOT src/ui/index.html — that path does not exist per Read tool result):
    Line 9 pre-edit: `<meta name="theme-color" content="#080808" />`
    Replace `content="#080808"` with `content="#0a0b12"`.
    No other line in index.html is modified.

    STEP E — Post-edit grep verification (this is the belt-and-suspenders check; executor should visually confirm all four files show the new value):
    Run: `grep -n '#0a0b12' src/ui/index.css public/manifest.webmanifest public/manifest.json index.html`
    Expected minimum matches:
      - src/ui/index.css: at least 2 (one is the pre-existing `--color-pv-base-end: #0a0b12;` at line 121; the other is the newly-inserted `--background: #0a0b12;` at line 192)
      - public/manifest.webmanifest: 2 (background_color + theme_color)
      - public/manifest.json: 2 (theme_color + background_color)
      - index.html: 1 (meta theme-color)
      TOTAL: at least 7 matches.

    Also run: `grep -n '#080808\|#09090b' src/ui/index.css public/manifest.webmanifest public/manifest.json index.html` — should return ZERO matches (the old values must be entirely rebased).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && echo "---NEW-VALUES (expect >= 7 matches, at least: 2 css, 2 webmanifest, 2 json, 1 html)---" && grep -c '#0a0b12' src/ui/index.css public/manifest.webmanifest public/manifest.json index.html && echo "---OLD-VALUES (expect 0 matches)---" && grep -cE '#080808|#09090b' src/ui/index.css public/manifest.webmanifest public/manifest.json index.html && echo "---DARK-BACKGROUND-LINE (expect: --background: #0a0b12;)---" && grep -n -- '--background:' src/ui/index.css | head -5 && echo "---TSC (must be clean — CSS/JSON/HTML changes shouldn't affect tsc but check anyway)---" && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>
    All four files updated: src/ui/index.css `.dark --background` is `#0a0b12`; public/manifest.webmanifest theme_color + background_color are `#0a0b12`; public/manifest.json theme_color + background_color are `#0a0b12`; index.html meta theme-color is `#0a0b12`. Zero grep matches for `#080808` or `#09090b` across these four files. Palette block at src/ui/index.css lines 117-146 is byte-identical to pre-edit (still has `--color-pv-base-end: #0a0b12;`). tsc still clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full-tree build verify + write SUMMARY + atomic commit (NO deploy, NO push)</name>
  <files>
    .planning/quick/260723-6xb-patch-133-strip-shadcn-wrappers-from-pre/260723-6xb-SUMMARY.md
  </files>
  <action>
    STEP A — Full-tree build verify (not just scoped):

    A1. `npx tsc --noEmit` from repo root. Capture exit code and last 30 lines of output. Any NEW type errors attributable to the patch (e.g., leftover reference to removed prop, missing import) MUST be fixed before commit — return to Task 1 to fix. Pre-existing errors elsewhere are acceptable IF they existed at the same location before the patch (compare to `git stash` baseline if in doubt).

    A2. `npx vitest run src/ui/features/pretty-conversations/` — scoped run. All tests in the pretty-conversations feature MUST pass. Capture exit code and last 40 lines of output.

    A3. `npx vitest run` — full-tree run. Capture exit code + summary line ("Tests X passed, Y failed"). Compare to the STATE.md baseline of "16/18 with 2 pre-existing patch-#124 ThumbsUp `/send 'yes'/i` failures" from the last quick task (260723-2cg patch #129). If the failure count matches baseline (same 2 pre-existing failures), that's acceptable — this patch does not touch ComposeBox. If failure count INCREASES, investigate and fix before commit.

    A4. `npm run build` — production build must succeed (Vite bundle). Capture exit code and total time.

    STEP B — Post-edit belt-and-suspenders grep gates (one final sweep, echoed into SUMMARY):
    B1. `grep -nE '\b(DropdownMenu|Tooltip|renderSettingsMenuItems)\b|from "@/components/dropdown-menu"|from "@/components/tooltip"|text-muted-foreground' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx || echo "clean"` — must print `clean`.
    B2. `grep -c '#0a0b12' src/ui/index.css public/manifest.webmanifest public/manifest.json index.html` — must show `#0a0b12` present in each file (≥1 in index.css beyond the pre-existing palette entry means ≥2 there; ≥2 in each manifest; ≥1 in index.html).
    B3. `grep -cE '#080808|#09090b' src/ui/index.css public/manifest.webmanifest public/manifest.json index.html` — must show `0` for each file.
    B4. `git diff --stat` — must show exactly 6 files touched (PrettyConversationsPanel.tsx, PrettyConversationsPanel.test.tsx, AppShell.tsx, src/ui/index.css, public/manifest.webmanifest, public/manifest.json, index.html — count 7 actually; verify no accidental extra file was modified).

    STEP C — Write SUMMARY.md at `.planning/quick/260723-6xb-patch-133-strip-shadcn-wrappers-from-pre/260723-6xb-SUMMARY.md`:

    Content structure (write as Markdown, use the Write tool):

    ```
    # Patch #133 — strip shadcn wrappers from PrettyConversationsPanel + prototype fidelity + Skynet base #0a0b12

    **Date:** 2026-07-23
    **Branch:** feat/tab-title-from-tmux
    **Bounty:** phase10-shadcn-strip-and-prototype-fidelity (666704b0-8f39-4f5d-a990-cae701a74717)
    **Commit:** <FILL-IN-AFTER-COMMIT-SHA>
    **Deploy status:** DEFERRED — Ashley bundles with prior UAT commits (558749a, 68e4f62, b749bf1, 536d224) in a single grouped deploy behind the standing 15-min deadman rollback discipline.

    ## What changed (7 files, atomic)

    **Panel strip (PrettyConversationsPanel.tsx, ~444→~370 LOC):**
    - Dropped imports: DropdownMenu*, Tooltip*, renderSettingsMenuItems, Settings icon
    - Deleted entire `showGear` JSX branch (34 LOC of TooltipProvider>Tooltip>DropdownMenu wrapper sandwich)
    - Deleted dead consts: showGear, settingsLabel
    - Removed onRailClick + isAdmin from props typedef (they only fed the gear)
    - Inlined 4 text-color sites with prototype-anchored hex/rgba per prototype.html --pv-text-* palette:
      * desktop title `text-foreground/90` → `text-[rgba(240,234,224,0.9)]` (--pv-text-primary #f0eae0 @ 0.9)
      * pencil `text-foreground` → `text-[#f0eae0]`
      * RDP monitor glyph + label `text-muted-foreground/50` → `text-[#5c6070]/50` (--pv-text-hint)
    - Header padding `px-3 py-2` → `px-4 py-3` (prototype .top-strip 12px/16px)
    - NewSessionDialog untouched (pencil is the ONLY entry point going forward per Skynet direction)
    - Rows + PinAction untouched (already clean per Wave 1 handoff)

    **AppShell call-site cleanup:**
    - `<PrettyConversationsPanel>` no longer passes onRailClick or isAdmin (dead after gear removal)
    - `settingsRowSlot` mechanism at same call-site UNCHANGED — mobile bottom-of-scroller settings row survives independently (different mechanism)

    **Test file (PrettyConversationsPanel.test.tsx):**
    - Test 9 rewritten from "renders gear when onRailClick provided" → asserts gear NEVER renders on desktop (invariant flip)
    - Test 10 title/prop-drop updated (assertion unchanged — still asserts no gear on mobile)
    - All other tests untouched (they never referenced the gear)

    **Skynet base color rebase (#080808/#09090b → #0a0b12):**
    - src/ui/index.css `.dark --background`: oklch(0.155 0.004 128.73) → #0a0b12
      * Anchors to --color-pv-base-end at line 121 (existing pretty-view palette) — no palette additions
    - public/manifest.webmanifest: theme_color + background_color → #0a0b12
    - public/manifest.json: theme_color + background_color → #0a0b12
    - index.html: <meta name="theme-color"> → #0a0b12

    ## Verify results

    - `npx tsc --noEmit`: <clean|N errors> (baseline delta: <0|+N>)
    - `npx vitest run src/ui/features/pretty-conversations/`: <X/X passing>
    - `npx vitest run` (full-tree): <X/Y passing — Z pre-existing patch-#124 failures unchanged from STATE.md baseline>
    - `npm run build`: <success in Ns>
    - Grep gate 1 (shadcn residuals in panel): clean
    - Grep gate 2 (#0a0b12 present in all 4 targets): <N matches — expected ≥7>
    - Grep gate 3 (old #080808/#09090b eliminated): 0 matches

    ## Deploy discipline

    NOT deployed. NOT pushed. Ashley owns the deploy timing and bundles this with:
    - 558749a
    - 68e4f62
    - b749bf1
    - 536d224
    - <this commit sha>

    ### iOS PWA reinstall note (LOAD-BEARING for UAT verification)

    iOS caches PWA manifest theme_color + background_color at install time. Ashley WILL NOT see the safe-area seam disappearance (Skynet-gray top/bottom bars instead of black) until she:
    1. Removes the current Skynet PWA from her home screen
    2. Loads term.gigaashley.click in Safari after deploy
    3. Re-adds to Home Screen ("Share → Add to Home Screen")
    4. Launches the reinstalled PWA

    Desktop UAT (Safari + Chrome browser tabs) will show the new #0a0b12 immediately after deploy — no reinstall needed there.

    ## Scope discipline notes

    - Bounty todo #7 ("Persistent top-left chevron stays, safe-area architecture stays post-#131") — untouched per Ashley's directive: this patch is pure REMOVAL + color rebase, no positive UI structural changes.
    - Bounty todos #1 ("side-by-side survey") — implicitly completed via prototype.html grep during planning; explicit visual diff enumeration deferred to Ashley's UAT walkthrough.
    - Bounty todo #8 ("deploy") + #9 ("patches-md #133 pin") — DEFERRED to Ashley's greenlight, tracked as follow-up in bounty timeline.
    - Bounty todo #14 ("verify seam gone on iOS PWA") — VERIFIABLE POST-DEPLOY-POST-REINSTALL only; documented above.

    ## Follow-ups (not in this patch)

    - If Ashley later wants a Skynet settings surface, build fresh — do NOT reintroduce shadcn DropdownMenu. Radix headless primitives + custom styling per the tina.md § Learned preferences shadcn `!` important lesson (patch #81); this patch REMOVED the wrapper, not just its usage.
    - Any future prop pruning across the fork that discovers dead onRailClick/isAdmin flows can now safely treat this panel as a reference example of "gear removed cleanly".
    ```

    Fill in `<FILL-IN-AFTER-COMMIT-SHA>` after Step D completes. Fill in the verify results from Step A observations before committing.

    STEP D — Atomic git commit:

    D1. `git status` to confirm exactly 7 files staged (the 6 code files + SUMMARY.md).

    D2. `git add` the specific files (do NOT use `git add -A`):
        - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
        - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
        - src/ui/AppShell.tsx
        - src/ui/index.css
        - public/manifest.webmanifest
        - public/manifest.json
        - index.html
        - .planning/quick/260723-6xb-patch-133-strip-shadcn-wrappers-from-pre/260723-6xb-SUMMARY.md

    D3. Commit with a HEREDOC message per fork discipline:

    ```
    git commit -m "$(cat <<'EOF'
    patch #133: strip shadcn wrappers from PrettyConversationsPanel + Skynet base #0a0b12

    Ashley 2026-07-23 called out the shipped Phase 10 panel still had "bones of
    Skynet" via shadcn Tooltip + DropdownMenu wrappers + text-muted-foreground
    theme tokens. This patch does the full clean-slate pass matching the
    pretty-view (phases 4-9) treatment.

    Panel strip (~444→~370 LOC, PrettyConversationsPanel.tsx):
      - Drop shadcn DropdownMenu + Tooltip wrappers + Settings icon
      - Delete entire gear branch (34 LOC of TooltipProvider>Tooltip>DropdownMenu)
      - Remove onRailClick + isAdmin from props typedef (only fed the gear)
      - Inline 4 text-color sites with prototype.html --pv-text-* palette:
        desktop title text-foreground/90 → text-[rgba(240,234,224,0.9)]
        pencil text-foreground → text-[#f0eae0]
        RDP monitor glyph + label text-muted-foreground/50 → text-[#5c6070]/50
      - Header padding px-3 py-2 → px-4 py-3 (prototype .top-strip 12px/16px)
      - NewSessionDialog untouched (pencil = only entry point per Skynet direction)
      - Rows + PinAction untouched (already clean per Wave 1 handoff)

    AppShell.tsx: drop onRailClick + isAdmin props from PrettyConversationsPanel
    call-site. settingsRowSlot mechanism UNCHANGED (mobile bottom-of-scroller
    settings row is a separate surface that survives gear removal).

    Test file: Tests 9 + 10 rewritten. Test 9 flips from "gear renders on
    desktop with onRailClick" → "gear NEVER renders on desktop" invariant.
    Test 10 unchanged assertion, prop-drop only. Every other test untouched.

    Skynet base color rebase — src/ui/index.css .dark --background:
    oklch(0.155 0.004 128.73) → #0a0b12. Anchors to --color-pv-base-end
    (existing pretty-view palette, no palette additions). Also:
      - public/manifest.webmanifest theme_color + background_color → #0a0b12
      - public/manifest.json theme_color + background_color → #0a0b12
      - index.html <meta name="theme-color"> → #0a0b12
    Fixes iOS PWA safe-area seam — Ashley must reinstall PWA post-deploy
    for manifest colors to refresh (documented in SUMMARY).

    Deploy deferred: batched with 558749a, 68e4f62, b749bf1, 536d224 pending
    Ashley greenlight. No push.
    EOF
    )"
    ```

    D4. `git status` to confirm commit succeeded and tree is clean.
    D5. `git log -1 --oneline` — capture the commit SHA and edit SUMMARY.md `<FILL-IN-AFTER-COMMIT-SHA>` placeholder with the actual SHA. Amend the commit ONLY if the SUMMARY SHA fill-in is the ONLY reason to amend — otherwise leave the placeholder and note in the return message that Ashley/Tina should manually fill it post-hoc. (Prefer: use a temp file, write the commit, then Edit SUMMARY.md and amend the same commit — one atomic result. Amend IS legitimate here because the SUMMARY SHA is part of the same logical unit, and no hook rejection has occurred.)

    D6. DO NOT push. DO NOT deploy. DO NOT run any nginx/docker command. STOP after the commit lands.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && echo "---TSC---" && npx tsc --noEmit 2>&1 | tail -10 && echo "---VITEST-SCOPED---" && npx vitest run src/ui/features/pretty-conversations/ 2>&1 | tail -20 && echo "---BUILD---" && npm run build 2>&1 | tail -10 && echo "---GIT-STATUS (must be clean after commit)---" && git status --short && echo "---GIT-LOG---" && git log -1 --oneline && echo "---SUMMARY-EXISTS---" && ls -la .planning/quick/260723-6xb-patch-133-strip-shadcn-wrappers-from-pre/260723-6xb-SUMMARY.md</automated>
  </verify>
  <done>
    tsc clean (no new errors). vitest pretty-conversations scoped 100% pass. Full-tree vitest ≤ baseline failure count (2 pre-existing patch-#124 failures acceptable). npm run build succeeds. All grep gates pass. SUMMARY.md written with all verify results filled in. One atomic git commit landed on feat/tab-title-from-tmux containing exactly 8 files (7 code/config + SUMMARY.md). `git status --short` prints nothing (clean tree). NO deploy, NO push executed.
  </done>
</task>

</tasks>

<verification>
Post-plan overall verification (executor confirms all three tasks landed):

- `git log -1 --stat` shows the single commit with 8 files (7 targets + SUMMARY.md), sensible +/- line counts (rough expectation: panel -~74 LOC, test file ~+2/-4, AppShell -~5, index.css -1/+1, both manifests -2/+2 each, index.html -1/+1, SUMMARY.md ~+80 new).
- `git diff HEAD~1 HEAD -- src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx | grep -cE '^-.*(DropdownMenu|Tooltip|renderSettingsMenuItems|text-muted-foreground|showGear|settingsLabel|Settings)'` ≥ 10 (removals visible in the diff).
- `git diff HEAD~1 HEAD -- src/ui/index.css | grep -c '^\+.*--background: #0a0b12'` = 1 (exactly one added line for the new background value).
- Ashley notified in return message that:
  1. Patch is code-complete, NOT deployed, NOT pushed
  2. Ready to bundle with 558749a / 68e4f62 / b749bf1 / 536d224 in one deploy
  3. iOS reinstall REQUIRED post-deploy for the safe-area seam disappearance to be visible (manifest cache)
  4. Bounty todos #1, #7, #8, #9, #14 remain OPEN (survey done implicitly during planning; deploy + patches-md pin + iOS UAT are Ashley-gated follow-ups)
</verification>

<success_criteria>
- Panel file no longer imports from `@/components/dropdown-menu` or `@/components/tooltip`, no longer imports `renderSettingsMenuItems` or `Settings` icon
- Panel file has zero `text-muted-foreground` / `text-foreground` substrings (all inlined to prototype hex/rgba)
- Panel props typedef no longer includes onRailClick or isAdmin
- AppShell call-site no longer passes onRailClick or isAdmin
- Test 9 asserts gear NEVER renders on desktop (was: renders); Test 10 unchanged assertion with prop-drop; all other tests untouched
- Header padding `px-4 py-3` (prototype .top-strip 12px/16px)
- src/ui/index.css `.dark --background` is `#0a0b12`
- public/manifest.webmanifest + public/manifest.json: theme_color + background_color both `#0a0b12`
- index.html `<meta name="theme-color" content="#0a0b12">`
- Zero `#080808` or `#09090b` residuals in the 4 rebased files
- tsc clean, vitest pretty-conversations scoped fully passing, full-tree vitest ≤ baseline, npm run build succeeds
- One atomic git commit on feat/tab-title-from-tmux
- SUMMARY.md with iOS reinstall note
- NO deploy, NO push
</success_criteria>

<output>
Create `.planning/quick/260723-6xb-patch-133-strip-shadcn-wrappers-from-pre/260723-6xb-SUMMARY.md` when done (as part of Task 3).
</output>
