---
task: 260718-43f-fix-pretty-view-context-false-positive-m
type: quick
status: complete
commit: 17c4079
files_modified:
  - src/backend/claude-session/claude-session-server.ts
---

# Patch #56: Context% Scrape False-Positive Fix

**What shipped:** The pretty-view context-% poller now slices `tmux capture-pane` output to the bottom 8 lines before running `matchAll`, defeating transcript pollution where `context)...NN%` strings quoted in assistant messages or old redraw fragments could win the last-match-wins race when Claude Code's footer was transiently absent mid-tool-execution. A defense-in-depth fallback regex (`/[░█]\s*(\d{1,3})%/g`) is tried when the primary `context)` regex misses, future-proofing against Claude Code ever changing its footer text while keeping the visual block-glyph bar. Build clean, one atomic commit `17c4079`.
