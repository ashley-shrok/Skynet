---
phase: quick-260814-u0w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/database/routes/voice.ts
  - src/backend/database/routes/voice.test.ts
autonomous: true
requirements:
  - QUICK-260814-U0W
must_haves:
  truths:
    - "handleTranscribe waits up to 120s (not 30s) for the STT upstream before aborting"
    - "Every incoming transcribe request writes its audio bytes to disk before the STT fetch is issued"
    - "A disk-bank write failure never propagates to the HTTP response — the STT flow still returns its normal result"
    - "The banked filename encodes timestamp, userId (or 'anon'), byte size, and mimetype-derived extension"
    - "npx vitest run exits 0 with zero failures across the full suite"
    - "npm run build:backend AND npm run build both exit 0 (backend-touching pre-commit typecheck rule)"
  artifacts:
    - path: "src/backend/database/routes/voice.ts"
      provides: "handleTranscribe with 120s AbortController + fire-and-forget bank-to-disk of file.buffer"
      contains: "120_000"
    - path: "src/backend/database/routes/voice.test.ts"
      provides: "New tests: bank-write invoked with expected filename shape + call-order-before-fetch; STT flow completes on writeFile rejection"
      contains: "voice_transcribe_bank_write"
  key_links:
    - from: "handleTranscribe entry (after req.file guard)"
      to: "fs.promises.writeFile(dir, file.buffer)"
      via: "void-prefixed fire-and-forget with .catch → databaseLogger.warn"
      pattern: "void .*writeFile"
    - from: "handleTranscribe AbortController setTimeout"
      to: "120_000 ms cap"
      via: "setTimeout(() => controller.abort(), 120_000)"
      pattern: "120_000"
    - from: "test file mock of fs.promises"
      to: "handleTranscribe execution"
      via: "vi.mock('node:fs') or vi.mock('fs') exposing writeFile + mkdir spies"
      pattern: "vi\\.mock.*fs"
---

<objective>
Fix Ashley's lost-dictation bug (2026-08-14 21:18 PT, 3.87 MB clip): bump the STT AbortController timeout from 30s to 120s so long dictations to the tailnet faster-whisper server (large-v3 on CUDA GPU) complete, AND install a fire-and-forget disk-bank of the incoming audio buffer so that even if the STT round-trip fails for any future reason, the raw bytes are recoverable from the container filesystem (scoped-down consolation prize for the parked `persist-stt-attempts-for-drop-recovery` bounty — server-side banking only, no user-facing recovery affordance).

Purpose: Ashley lost ~3.87 MB of dictation tonight because (a) our own 30s AbortController fired before the upstream returned, and (b) multer is memory-only so once the 504 returned the audio was GC'd. Both root causes need patching. Timeout bump addresses (a); disk-bank addresses (b) for future incidents.

Output: `src/backend/database/routes/voice.ts` modified (timeout + bank-write + logger lines) and `src/backend/database/routes/voice.test.ts` extended with new tests covering the bank-write happy path (with call-order-before-fetch assertion) and the bank-write-rejects best-effort behavior. Full vitest suite green; both backend and frontend typechecks green; one atomic commit on `feat/tab-title-from-tmux`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/backend/database/routes/voice.ts
@src/backend/database/routes/voice.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: voice.ts — bump STT timeout to 120s + fire-and-forget disk-bank of audio buffer</name>
  <files>src/backend/database/routes/voice.ts</files>
  <behavior>
    Behavior after this task (asserted by Task 2's new tests):

    - Timeout cap: `setTimeout(() => controller.abort(), 120_000)` (was `30_000`). Comment on the line above updated from the current "30-second STT timeout" wording to "120-second STT timeout" and notes the bump was for long-dictation support (see Ashley 2026-08-14 3.87 MB clip incident).
    - After the `if (!req.file)` guard and BEFORE the existing `databaseLogger.info(\`[voice-server] transcribe-req ...\`)` line at ~line 70, kick off a fire-and-forget disk-bank of `file.buffer`:
      - Target directory: `process.env.STT_RECORDINGS_DIR ?? "/app/stt-recordings"`.
      - Idempotent mkdir on kickoff: `fs.promises.mkdir(dir, { recursive: true })` — awaited only within the fire-and-forget chain, NOT awaited by handleTranscribe itself (the whole write pipeline is `void`-prefixed so it does not delay the STT fetch).
      - Filename shape: `${YYYYMMDDTHHMMSSZ}-${userId ?? "anon"}-${file.size}.${ext}` where `ext` comes from the existing `extFromMimetype(file.mimetype)`, timestamp is UTC compact ISO with no separators (e.g. `20260814T213704Z`), `userId` from `(req as AuthenticatedRequest).userId` following the exact pattern already used inside the slash-transform block at lines 131-132.
      - Write: `fs.promises.writeFile(fullPath, file.buffer)`.
      - Success info log emitted on kickoff (before the write resolves is fine — the log documents the ATTEMPT filename so it sits next to the `transcribe-req` line): `databaseLogger.info(\`[voice-server] transcribe-bank-write filename=${filename}\`, { operation: "voice_transcribe_bank_write", filename })`. Attach a `.catch()` to the writeFile chain that emits `databaseLogger.warn(\`[voice-server] transcribe-bank-write-failed filename=${filename} error=${err.message}\`, { operation: "voice_transcribe_bank_write_failed", filename, error: err.message })`. The `.catch()` MUST NOT rethrow — a disk-write failure must never affect the response.
    - No other behavior in handleTranscribe changes: FormData construction, fetch to STT, timeout clear, 2xx/non-2xx branches, slash-transform block, AbortError → 504, generic error → 502 all stay byte-identical outside the two edits above.
    - Test 1: mocked `fs.promises.writeFile` is invoked when `handleTranscribe` runs, with a filename that matches the expected shape (timestamp regex + userId + size + ext), the audio buffer is passed as the write data, AND the file-write mock is called BEFORE the fetch mock (call-order check on the mocks via `vi.fn().mock.invocationCallOrder`).
    - Test 2: when the mocked `fs.promises.writeFile` REJECTS, `handleTranscribe` still returns HTTP 200 with the STT transcript body — the write failure does not surface to the response.
    - Test 3 (if any existing test in voice.test.ts pins `30_000` or `30000` — grep first): update to `120_000` or loosen the pin.
  </behavior>
  <action>
    Edit `src/backend/database/routes/voice.ts` with two changes inside `handleTranscribe` (do NOT touch handleSpeak, handleSpeakStream, handleListVoices, extFromMimetype, the router wiring, or any other export):

    1) Add `import fs from "node:fs";` at the top of the file with the other imports (place it after the `multer` import for grouping).

    2) Update the timeout comment (currently at ~line 86: `// (c) AbortController: T-16-02 mitigation — 30-second STT timeout`) to reflect the 120s cap and cite the long-dictation motivation. Change the `setTimeout` call on the following line from `30_000` to `120_000`. Everything else in the AbortController block stays identical.

    3) BEFORE the existing `databaseLogger.info(\`[voice-server] transcribe-req ...\`)` line (currently at ~line 70) and AFTER `const ext = extFromMimetype(file.mimetype);`, insert a bank-to-disk block:
       - Compute `userId` via `const authReq = req as AuthenticatedRequest; const userId = authReq.userId;` (mirrors the pattern at lines 131-132; `userId` is a number when JWT is present, undefined otherwise — the `${userId ?? "anon"}` interpolation handles both).
       - Compute `dir` via `process.env.STT_RECORDINGS_DIR ?? "/app/stt-recordings"`.
       - Compute `timestamp` in UTC compact ISO with no separators: strip `-`, `:`, and `.` from `new Date().toISOString()` and truncate the fractional-second component so the result matches the `20260814T213704Z` shape. A single-line derivation like `const timestamp = new Date().toISOString().replace(/[-:]/g, "").replace(/\.\d+/, "");` produces the required form.
       - Compute `filename = \`${timestamp}-${userId ?? "anon"}-${file.size}.${ext}\``.
       - Compute `fullPath` via `path.join(dir, filename)` — add `import path from "node:path";` at the top of the file grouped with the fs import.
       - Emit the kickoff info log: `databaseLogger.info(\`[voice-server] transcribe-bank-write filename=${filename}\`, { operation: "voice_transcribe_bank_write", filename });`.
       - Fire-and-forget chain (MUST be `void`-prefixed, MUST NOT be awaited): `void fs.promises.mkdir(dir, { recursive: true }).then(() => fs.promises.writeFile(fullPath, file.buffer)).catch((err: unknown) => { const message = err instanceof Error ? err.message : String(err); databaseLogger.warn(\`[voice-server] transcribe-bank-write-failed filename=${filename} error=${message}\`, { operation: "voice_transcribe_bank_write_failed", filename, error: message }); });`.
       - The mkdir MUST be inside the fire-and-forget chain so it's idempotent per-request without blocking the STT fetch.

    4) Do NOT modify the `databaseLogger.info(\`[voice-server] transcribe-req ...\`)` line itself — the bank log sits above it. Do NOT modify the slash-transform block's own `authReq`/`userId` derivation (leave those local to that block; the new bank-write derivation is a separate local scope at the top of the handler).

    5) Do NOT touch `/opt/skynet/docker-compose.yml`, `skynet-patches.md`, any bounty file, or anything outside `src/backend/database/routes/voice.ts` in this task.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tiffany &amp;&amp; grep -c "120_000" src/backend/database/routes/voice.ts | { read n; test "$n" -ge 1; } &amp;&amp; grep -c "voice_transcribe_bank_write" src/backend/database/routes/voice.ts | { read n; test "$n" -ge 2; } &amp;&amp; grep -c "STT_RECORDINGS_DIR" src/backend/database/routes/voice.ts | { read n; test "$n" -ge 1; } &amp;&amp; grep -q "void fs.promises.mkdir" src/backend/database/routes/voice.ts &amp;&amp; ! grep -qE "setTimeout\(\(\) => controller\.abort\(\), 30_000\).*// \(c\)" src/backend/database/routes/voice.ts &amp;&amp; npm run build:backend &amp;&amp; npm run build</automated>
  </verify>
  <done>
    - `grep -c "120_000" voice.ts` &gt;= 1 AND the ONLY `setTimeout(..., 30_000)` occurrences remaining (if any) are in handleSpeak/handleSpeakStream/handleListVoices, NOT in handleTranscribe's block-(c).
    - Bank-write block sits between `const ext = extFromMimetype(file.mimetype);` and `databaseLogger.info(\`[voice-server] transcribe-req ...\`)`.
    - `void fs.promises.mkdir(...).then(() => fs.promises.writeFile(...)).catch(...)` present (verifies fire-and-forget shape).
    - `import fs from "node:fs";` and `import path from "node:path";` present at top of file.
    - Comment on the line above the 120_000 setTimeout mentions "120-second" and long-dictation.
    - `npm run build:backend` exits 0 AND `npm run build` exits 0 (backend-touching pre-commit rule — plain `tsc --noEmit` is NOT sufficient per fleet directive).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: voice.test.ts — mock fs.promises + add 2 new tests (bank-write invoked with call-order-before-fetch; bank-write rejection is best-effort)</name>
  <files>src/backend/database/routes/voice.test.ts</files>
  <behavior>
    - The existing test file structure, imports, mocks, describe blocks, and all pre-existing tests are preserved byte-identical except for the new additions below.
    - New mock at the top of the file (adjacent to the existing `vi.mock("../../voice/skill-catalog.js", ...)` block): `vi.mock("node:fs", ...)` exposing `promises.writeFile` and `promises.mkdir` as `vi.fn()` spies that resolve by default. The mock must be structured so individual tests can override the writeFile/mkdir resolutions via `vi.mocked(fs.promises.writeFile).mockRejectedValueOnce(new Error("EACCES"))` etc. Follow the same vi.mock-hoisting pattern already used in the file for `skill-catalog.js` — mocks declared at module scope before imports.
    - New test 1 inside the existing `describe("handleTranscribe", ...)` block, placed after the existing tests but before any nested describes: **"Test bank-write-1: writes audio buffer to disk before issuing STT fetch, with expected filename shape"**.
      - Arrange: fetch mock returns a normal 200 STT response (`{ text: "hello" }`); fs.promises.mkdir + writeFile default to resolving.
      - Build req with `file.buffer = Buffer.from("audio-bytes-fixture")`, `file.mimetype = "audio/mp4"`, `file.size = 19` (matches "audio-bytes-fixture".length).
      - Optionally pass a `userId` if the test wants to assert non-'anon' — either approach acceptable; the simpler shape uses no userId and asserts the filename contains `-anon-`.
      - Act: `await handleTranscribe(req, res)`.
      - Assert:
        - `fs.promises.writeFile` was called exactly once.
        - The first argument (path) matches `/^.*\/\d{8}T\d{6}Z-(anon|\d+)-19\.mp4$/` (timestamp compact ISO shape + userId-or-anon + size + ext).
        - The second argument equals `Buffer.from("audio-bytes-fixture")` (buffer identity via `.equals()` or `Uint8Array.from`).
        - Call-order: `fs.promises.writeFile.mock.invocationCallOrder[0] < fetchMock.mock.invocationCallOrder[0]` — the write kicks off BEFORE the fetch.
        - Response is still 200 with the STT text (bank-write does not disturb the normal response).
    - New test 2 inside the same describe block: **"Test bank-write-2: bank-write rejection does not affect STT response (best-effort)"**.
      - Arrange: fetch mock returns 200 with `{ text: "still works" }`; `vi.mocked(fs.promises.writeFile).mockRejectedValueOnce(new Error("EACCES: permission denied"))`.
      - Act: `await handleTranscribe(req, res)`.
      - Assert:
        - Response status is 200.
        - Response body's `text` equals `"still works"`.
        - `databaseLogger.warn` was invoked with `operation: "voice_transcribe_bank_write_failed"` (import + spy the logger following whatever pattern is already used in the test file — if the test file doesn't currently spy the logger, add a `vi.mock("../../utils/logger.js", ...)` mock that exposes `databaseLogger.info/warn/error` as vi.fn() spies). Alternatively, if a logger mock is already set up elsewhere and asserting the warn call is awkward, this assertion can be omitted PROVIDED the test still asserts that the writeFile rejection is caught (i.e. handleTranscribe returns normally and does not throw).
        - Give the pending microtask a chance to settle before assertions (e.g. `await new Promise(r => setImmediate(r))` or the equivalent vi.runAllTimersAsync approach used elsewhere in the file) — the `.catch()` runs on a microtask after handleTranscribe returns.
    - Grep-and-patch: if the pre-existing test file contains any literal `30_000` or `30000` that pins the OLD STT timeout (grep confirms none exist as of read, but this task explicitly re-greps in case of drift), update to `120_000` or loosen the pin.
    - Full-suite `npx vitest run` MUST exit 0 with zero failures after both new tests are added.
  </behavior>
  <action>
    Edit `src/backend/database/routes/voice.test.ts` (extend, do NOT rewrite):

    1) Add a `vi.mock("node:fs", ...)` block at module scope, adjacent to the existing `vi.mock("../../voice/skill-catalog.js", ...)` block (before the imports). The mock exposes `promises.writeFile` and `promises.mkdir` as `vi.fn()` that resolve by default. Use the vi.mock factory shape:
       ```
       vi.mock("node:fs", () => ({
         default: {
           promises: {
             writeFile: vi.fn(async () => undefined),
             mkdir: vi.fn(async () => undefined),
           },
         },
         promises: {
           writeFile: vi.fn(async () => undefined),
           mkdir: vi.fn(async () => undefined),
         },
       }));
       ```
       (Both `default` and named-`promises` exports covered because voice.ts uses `import fs from "node:fs"` which resolves to `default`. If TS/vitest resolver prefers one or the other, the executor may simplify — the load-bearing requirement is that `import fs from "node:fs"` in voice.ts sees vi.fn-backed `fs.promises.writeFile` and `fs.promises.mkdir`.)

    2) Add the corresponding import in the test file so the tests can reach the mocks: `import fs from "node:fs";` — grouped with the other imports.

    3) In `beforeEach`, reset the fs.promises spies so tests are isolated: `vi.mocked(fs.promises.writeFile).mockClear(); vi.mocked(fs.promises.mkdir).mockClear(); vi.mocked(fs.promises.writeFile).mockResolvedValue(undefined); vi.mocked(fs.promises.mkdir).mockResolvedValue(undefined);` — placed inside the existing beforeEach so it runs alongside `vi.useFakeTimers()`.

    4) Add the two new tests (`Test bank-write-1`, `Test bank-write-2`) inside `describe("handleTranscribe", ...)` after the existing tests, following the exact same makeReq/makeRes/vi.stubGlobal("fetch", ...) pattern used by Tests 1-6. Use fetch as a `vi.fn()` so `.mock.invocationCallOrder` is available for the call-order check in Test bank-write-1 — the existing tests use inline arrow functions for stubGlobal, but for Test bank-write-1 the executor can hoist to `const fetchMock = vi.fn(async () => makeFetchResponse(200, { text: "hello" })); vi.stubGlobal("fetch", fetchMock);` so both fetchMock and fs.promises.writeFile expose `.mock.invocationCallOrder`.

    5) If (and only if) a grep for `30_000` or `30000` inside `voice.test.ts` returns a match that pins the STT timeout specifically, update to `120_000` or loosen. Do NOT change TTS-timeout pins (300_000) or the list-voices 30_000 pin — those are unrelated caps.

    6) If asserting `databaseLogger.warn` for Test bank-write-2 requires mocking `../../utils/logger.js`, add a `vi.mock("../../utils/logger.js", () => ({ databaseLogger: { info: vi.fn(), warn: vi.fn(), error: vi.fn() } }));` block at module scope. If the test file already has such a mock (grep first), reuse it. If neither the mock nor the assertion are wanted, the executor may drop the databaseLogger.warn assertion PROVIDED Test bank-write-2 still verifies the 200 response body — the load-bearing behavior is that the rejection is swallowed.

    7) Do NOT alter or delete any existing test in the file. Do NOT change the makeReq/makeRes/makeFetchResponse helpers (they can be reused as-is).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tiffany &amp;&amp; grep -c "bank-write-1\|bank-write-2\|voice_transcribe_bank_write" src/backend/database/routes/voice.test.ts | { read n; test "$n" -ge 2; } &amp;&amp; grep -q "vi.mock.*node:fs" src/backend/database/routes/voice.test.ts &amp;&amp; npx vitest run src/backend/database/routes/voice.test.ts 2>&amp;1 | tail -20 &amp;&amp; npx vitest run 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    - `voice.test.ts` contains a `vi.mock("node:fs", ...)` block and the two new tests (`Test bank-write-1`, `Test bank-write-2`) with the assertions specified in `<behavior>`.
    - Targeted `npx vitest run src/backend/database/routes/voice.test.ts` exits 0 with all pre-existing tests + both new tests passing.
    - Full-suite `npx vitest run` exits 0 with zero failures. Never leave tests failing (fleet directive).
    - `npm run build:backend` AND `npm run build` both still exit 0 after the test-file additions.
    - One atomic commit prepared on `feat/tab-title-from-tmux` combining Task 1 + Task 2 (both files modified in the same logical change; execute-plan may commit as one commit or two — executor's choice). No push, no docker build, no deploy — subagents don't do deploys (fleet directive).
  </done>
</task>

</tasks>

<verification>
Full-plan verification checklist (executor runs before considering done):

1. **Targeted test file green:** `npx vitest run src/backend/database/routes/voice.test.ts` exits 0.
2. **Full suite green:** `npx vitest run` exits 0 with zero failures.
3. **Backend + frontend typecheck green:** `npm run build:backend && npm run build` both exit 0. **`npx tsc --noEmit` alone is NOT sufficient** — the frontend tsconfig doesn't compile backend files the same way, so subtle backend TS errors slip past `tsc --noEmit` and only surface at docker build time (fleet standing directive).
4. **File-level grep gates:**
   - `grep -q "120_000" src/backend/database/routes/voice.ts` (timeout bumped)
   - `grep -q "voice_transcribe_bank_write" src/backend/database/routes/voice.ts` (kickoff log)
   - `grep -q "voice_transcribe_bank_write_failed" src/backend/database/routes/voice.ts` (warn log)
   - `grep -q "STT_RECORDINGS_DIR" src/backend/database/routes/voice.ts` (env var respected)
   - `grep -q "void fs.promises.mkdir" src/backend/database/routes/voice.ts` (fire-and-forget shape)
   - `grep -q "vi.mock.*node:fs" src/backend/database/routes/voice.test.ts` (fs mock installed)
5. **Branch invariant:** `git branch --show-current` returns `feat/tab-title-from-tmux`.
6. **No forbidden edits:** `git status` shows NO changes to `/opt/skynet/docker-compose.yml`, `skynet-patches.md`, or anything under `~/.claude/roles/box-maintainer/bounties/`. Only `voice.ts` + `voice.test.ts` (plus PLAN.md + SUMMARY.md if `commit_docs: true`).
7. **No deploy motion:** executor did NOT run `git push`, `docker build`, `docker compose up`, or any deploy shell — subagents don't do deploys (fleet directive; orchestrator handles ship separately).
</verification>

<success_criteria>
- `src/backend/database/routes/voice.ts` has the 30s → 120s timeout bump with an updated comment on the preceding line.
- `src/backend/database/routes/voice.ts` has a fire-and-forget `void fs.promises.mkdir(...).then(writeFile).catch(warn)` bank-to-disk block between the `req.file` guard and the existing `transcribe-req` info log, honoring `STT_RECORDINGS_DIR` env with `/app/stt-recordings` fallback, producing filenames of shape `${YYYYMMDDTHHMMSSZ}-${userId ?? "anon"}-${file.size}.${ext}`, and never affecting the HTTP response on write failure.
- `src/backend/database/routes/voice.test.ts` gains two new tests (bank-write happy path with call-order-before-fetch assertion + bank-write rejection is best-effort) and a `vi.mock("node:fs", ...)` block. No existing test is deleted or altered beyond a timeout pin loosen (only if grep confirms one exists).
- Full-suite `npx vitest run` exits 0. Both `npm run build:backend` and `npm run build` exit 0. Branch `feat/tab-title-from-tmux`. No push, no docker, no compose motion. Committed locally (one or two atomic commits — executor's choice).
</success_criteria>

<output>
Create `.planning/quick/260814-u0w-voice-transcribe-bump-stt-timeout-30s-12/260814-u0w-SUMMARY.md` when done, following the standard summary template. Include: exact lines changed in voice.ts, the two new test names + their assertions, the full-suite vitest exit code + pass/fail counts, both typecheck exit codes, the commit SHA(s), and confirmation that no push/build/deploy motion was performed.
</output>
