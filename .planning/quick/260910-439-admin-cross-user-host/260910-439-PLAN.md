---
phase: quick-260910-439
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/database/routes/host.ts
  - src/backend/database/routes/credentials.ts
  - src/backend/database/routes/host.test.ts
autonomous: true
requirements:
  - QUICK-260910-439-A  # Admin cross-user READ (host + credentials)
  - QUICK-260910-439-B  # Admin cross-user CREATE/UPDATE on host (targetUserId, sensitive-field block)
  - QUICK-260910-439-C  # Unit tests + live post-fix smoke linking rows 21/22/23
tags:
  - admin
  - authz
  - host
  - credentials
  - cross-user

must_haves:
  truths:
    - "Admin GET /host/db/host returns rows owned by OTHER users (not just their own + shared)"
    - "Admin GET /host/db/host/:id succeeds against a row admin does not own"
    - "Admin GET /host/db/host/:id/export succeeds against a row admin does not own"
    - "Admin GET /host/db/hosts/export returns all users' rows, not just admin's"
    - "Admin GET /credentials returns rows owned by other users (or an admin-scope endpoint returns them)"
    - "Admin GET /credentials/:id succeeds against a row admin does not own"
    - "Non-admin GET on all four host read endpoints still returns ONLY their own userId rows (regression)"
    - "Non-admin GET on credentials read endpoints still returns ONLY their own userId rows (regression)"
    - "Admin POST /host/db/host with body.targetUserId=<other-user> inserts hosts.userId = other-user (not admin.id)"
    - "Admin PUT /host/db/host/:id succeeds on a row whose hosts.userId != admin.id when no sensitive inline creds in body"
    - "Non-admin POST or PUT with body.targetUserId set → 403 with a clear 'Only admin users can set targetUserId' error (NOT silently ignored)"
    - "Admin PUT with body.targetUserId != admin.id AND body contains any of {password, key, keyPassword, rdpPassword, vncPassword, telnetPassword, sudoPassword} → 400 with a clear 'Cross-user writes cannot include inline sensitive credentials — use credentialId instead' error"
    - "Non-admin POST/PUT for their own hosts (no targetUserId in body) still succeeds (regression)"
    - "After Task 3 smoke: skynet container logs show zero 'Unsupported authType.*fleetHostId:(21|22|23)' warnings in the 60s window after the sweep tick following the linkage PUTs"
  artifacts:
    - path: "src/backend/database/routes/host.ts"
      provides: "Admin read bypass on 4 GET endpoints; targetUserId param on POST + PUT with sensitive-field-block guard"
      contains: "isAdmin"
    - path: "src/backend/database/routes/credentials.ts"
      provides: "Admin read bypass on credentials list + get-by-id"
      contains: "isAdmin"
    - path: "src/backend/database/routes/host.test.ts"
      provides: "New test group A1-A6 covering admin cross-user read + write + non-admin regression + sensitive-field-block guard"
      contains: "targetUserId"
  key_links:
    - from: "src/backend/database/routes/host.ts (each of 4 read endpoints + POST + PUT)"
      to: "src/backend/database/db/schema.ts (users table, isAdmin column)"
      via: "await db.select().from(users).where(eq(users.id, userId)) then read row[0].isAdmin"
      pattern: "isAdmin"
    - from: "src/backend/database/routes/credentials.ts (list + get-by-id)"
      to: "src/backend/database/db/schema.ts (users table, isAdmin column)"
      via: "same DB-lookup pattern"
      pattern: "isAdmin"
---

<objective>
Extend Skynet's host + credentials READ endpoints with an admin bypass so an admin (Ashley) can see and manage rows owned by other users. Add an optional `targetUserId` body param to host POST + PUT — admin-only — so admin can create hosts under, and update hosts owned by, other users without needing the target's session or data key. Sensitive-inline-credential writes to another user's row are explicitly blocked at the API boundary (encryption of another user's row requires target's data key, which isn't held server-side unless target is logged in). Non-admins passing `targetUserId` are rejected with 403, not silently ignored — probing for admin behavior must be observable.

After the code lands, do a live smoke test that exercises the newly-admin-capable PUT: link `joe`/`zoey`'s existing host rows 21, 22, 23 to Ashley's shared `sshCredentials` (workstation-ssh, thenasty-ssh, ZoeyBattlestation-ssh) and confirm the 30s fleet-status sweep stops emitting the "Unsupported authType" warning for those three fleetHostIds. This proves the endpoint works end-to-end against the live production API, closing the loop that motivated this quick.

Purpose: Ashley admins the whole fleet — she needs to be able to fix other users' host configs (including linking them to shared credentials she owns) via Skynet's own API without SSH-ing in and hand-editing the DB. Preserves the encryption invariant by refusing cross-user writes that would need the target's data key.

Output: 3 patched files + updated test file + a SUMMARY.md documenting smoke-test result (log grep count = 0 = success).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# Route files under modification
@src/backend/database/routes/host.ts
@src/backend/database/routes/credentials.ts
@src/backend/database/routes/host.test.ts

# Existing admin-read patterns to mirror (DO NOT invent a new one)
# users.ts:442 → "Read caller's isAdmin from DB (not JWT) — defends against mid-session revocation"
# user-session-routes.ts:154, :221 → own-or-admin gate + targetUserId semantics
# relay-registry-backfill.ts:102 → admin gate returning 403 when !adminRow[0].isAdmin
@src/backend/database/routes/users.ts
@src/backend/database/routes/user-session-routes.ts
@src/backend/database/db/schema.ts

# Encryption boundary — confirms cross-user SENSITIVE writes require target's data key
@src/backend/utils/simple-db-ops.ts
@src/backend/utils/lazy-field-encryption.ts

# JWT middleware — confirms authenticateJWT sets req.userId but does NOT populate req.user.isAdmin
# so isAdmin MUST be read from DB in each handler (mirror users.ts:442 pattern)
@src/backend/utils/auth-manager.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Admin READ bypass on host.ts (4 endpoints) + credentials.ts (2 endpoints)</name>
  <files>src/backend/database/routes/host.ts, src/backend/database/routes/credentials.ts</files>
  <behavior>
    - Given: admin (userRecord.isAdmin === true) calls GET /host/db/host → response includes hosts.userId rows for OTHER users (not just admin's own + shared). Query stops filtering by `eq(hosts.userId, userId)` when isAdmin.
    - Given: admin calls GET /host/db/host/:id where hosts.userId != admin.id → returns 200 with row (currently returns 404 due to the AND-eq filter).
    - Given: admin calls GET /host/db/host/:id/export where hosts.userId != admin.id → returns 200 with export payload (currently returns 404).
    - Given: admin calls GET /host/db/hosts/export → returns ALL users' rows (currently returns admin's only).
    - Given: admin calls GET /credentials → response includes sshCredentials rows for other users.
    - Given: admin calls GET /credentials/:id where sshCredentials.userId != admin.id → returns 200 with row (currently 404).
    - Regression: non-admin (userRecord.isAdmin === false) still gets the pre-existing filter behavior on all 6 endpoints — their own userId rows only.
    - Cross-user reads for hosts: pass caller's userId to SimpleDBOps.select as before. Host metadata is cleartext-at-rest (per scope prompt + lazy-field-encryption sensitiveFields list — hosts sensitive fields are password/key/keyPassword/rdp*/vnc*/telnet*/sudoPassword only). Sensitive fields on cross-user rows will decrypt to empty string via LazyFieldEncryption.safeGetFieldValue's fallback (line 148, lazy-field-encryption.ts) — that's acceptable for a read that primarily surfaces metadata; the admin's use case is linking rows to shared credentials via credentialId, not viewing other users' inline passwords.
    - Cross-user reads for credentials: sshCredentials uses SystemCrypto.getCredentialSharingKey system-key encryption (see data-crypto.ts:477 encryptRecordWithSystemKey + simple-db-ops.ts:32-44 dual encryption on insert). Admin can decrypt via the system key path — but the existing SimpleDBOps.select path uses the caller's user data key. For this quick, do NOT change SimpleDBOps.select behavior; accept that cross-user cred READS return the ROW with sensitive fields as empty strings (same fallback as hosts). Admin's use case is knowing that a shared cred *exists* for another user and linking hosts to it — not exfiltrating the secret. Document this in a code comment where the isAdmin bypass is applied in credentials.ts.
  </behavior>
  <action>
    Mirror the users.ts:442 "read caller's isAdmin from DB (not JWT)" pattern. Add a small helper at the top of each file (or import from a shared spot if one already exists — grep first; do NOT create a new utils file if a similar helper already exists in the codebase):

    ```
    async function callerIsAdmin(userId: string): Promise<boolean> {
      const rows = await db.select().from(users).where(eq(users.id, userId));
      return !!(rows[0]?.isAdmin);
    }
    ```

    Import `users` from `../db/schema.js` at the top of each file (host.ts already imports several schema symbols on line 7-20; add `users` to that import list. credentials.ts imports schema on line 4-9; add `users` there. Do NOT create a second `users` import elsewhere in the file).

    **host.ts changes (6 endpoints — 4 read now, POST + PUT in Task 2):**

    1. GET /db/host (registered line 1226, handler starts line 1230): read `const isAdmin = await callerIsAdmin(userId);` early in the handler. When isAdmin, change the `.where(...)` clause on the main select (currently line ~1341-1358 with the `or(eq(hosts.userId, userId), ...hostAccess...)` filter) so that admin sees ALL rows — pass `sql\`true\`` or drop the `where` clause entirely. Keep the `leftJoin` on hostAccess intact so isShared / permissionLevel / sharedExpiresAt still populate for the admin's own shared rows. The ownership-check filter that produces `ownHosts` (line 1360, `rawData.filter((row) => row.userId === userId)`) should also be flipped for admin — for admin, treat ALL rows as "own" for decryption purposes (they'll try to decrypt with admin's data key; foreign rows will surface empty sensitive fields, which is fine per behavior notes above).

    2. GET /db/host/:id (registered line 1440, handler starts line 1444): read isAdmin. Change the `.where(and(eq(hosts.id, ...), eq(hosts.userId, userId)))` on line 1463 to conditionally drop the userId filter when isAdmin — e.g. `.where(and(eq(hosts.id, Number(hostId)), isAdmin ? undefined : eq(hosts.userId, userId)))`. drizzle's `and()` gracefully ignores `undefined` per its docs; verify with a small grep of the codebase for `and(.*undefined` first to see if this pattern is already used — if not, use `isAdmin ? eq(hosts.id, Number(hostId)) : and(eq(hosts.id, Number(hostId)), eq(hosts.userId, userId))` instead. Whichever matches the surrounding code style wins.

    3. GET /db/host/:id/export (registered line 1586, handler starts line 1590): same treatment as GET /db/host/:id — drop the userId filter on line 1605 when isAdmin.

    4. GET /db/hosts/export (registered line 1764, handler starts line 1768): drop the `.where(eq(hosts.userId, userId))` on line 1777 when isAdmin (query becomes `db.select().from(hosts)` with no where when isAdmin).

    Do NOT touch: POST /db/host, PUT /db/host/:id, /quick-connect, /db/host/:id/password (out of scope — no filter drop there), or any endpoint outside the four listed above.

    **credentials.ts changes (2 endpoints):**

    1. GET / (registered line 234, handler starts line 238): read isAdmin. When isAdmin, drop the `.where(eq(sshCredentials.userId, userId))` on line 251. Keep the `.orderBy(desc(sshCredentials.updatedAt))` intact.

    2. GET /:id (registered line 341, handler starts line 345): drop the `.where(and(eq(sshCredentials.id, parseInt(id)), eq(sshCredentials.userId, userId)))` userId filter (line 360-363) when isAdmin. Same drizzle pattern as GET /host/db/host/:id above.

    Do NOT touch:
    - GET /folders (line 281) — folders is a projection over user's own creds; adjacent, but scope-creep. Skip.
    - Any POST/PUT/DELETE — WRITE bypass on sshCredentials is explicitly out of scope per scope prompt.
    - Any endpoint that recently got the SimpleDBOps raw-db-update fix (scope prompt says "do NOT touch those — they're WRITE endpoints deliberately out of scope").

    **Logging:** Add a `authLogger.info` (credentials.ts) / `databaseLogger.info` (host.ts) log line inside the isAdmin branch identifying the admin bypass: `operation: "admin_cross_user_read", table: "hosts"|"ssh_credentials", callerUserId: userId`. This creates an audit trail for cross-user reads. Match the existing log style in each file (they use `sshLogger.warn/info/success` + structured payload).

    **CLAUDE.md nginx caveat** does NOT apply — this quick touches EXISTING routes, not new ones. No nginx.conf edit required.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; npx tsc --noEmit -p tsconfig.json 2&gt;&amp;1 | grep -E "src/backend/database/routes/(host|credentials)\.ts" | head -20 | (grep . &amp;&amp; exit 1 || echo "typecheck clean on host.ts + credentials.ts")</automated>
  </verify>
  <done>
    - callerIsAdmin helper (or equivalent DB-read of users.isAdmin) present in both host.ts and credentials.ts, mirroring users.ts:442 pattern
    - `users` symbol added to the existing schema import block in each file (no duplicate imports)
    - 4 host read endpoints + 2 credentials read endpoints have their userId filter conditionally dropped when isAdmin
    - `stripSensitiveFields`, `transformHostResponse`, `resolveHostCredentials`, `formatCredentialOutput` call sites remain unchanged (we're only relaxing the WHERE clause, not the response shape)
    - Cross-user admin-read log line added inside each of the 6 modified handlers with `operation: "admin_cross_user_read"`
    - tsc --noEmit clean on the two modified route files
    - Nothing else in host.ts or credentials.ts modified (verify with `git diff --stat src/backend/database/routes/host.ts src/backend/database/routes/credentials.ts` and eyeball)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Admin targetUserId on host.ts POST + PUT (with sensitive-field-block guard)</name>
  <files>src/backend/database/routes/host.ts</files>
  <behavior>
    - Given: admin POSTs /db/host with `body.targetUserId = "T4C1p6jcQgUZT9y6o3xGo"` → row inserted with hosts.userId = "T4C1p6jcQgUZT9y6o3xGo" (not admin's id). Response body reflects the target ownership.
    - Given: admin PUTs /db/host/:id where the existing row's userId != admin.id AND body has no sensitive inline creds → update succeeds. `permissionManager.canAccessHost` isOwner check must be bypassed for admin.
    - Given: non-admin POST or PUT with `body.targetUserId` present (even matching their own id) → 403 with body `{ error: "Only admin users can set targetUserId" }`. Reject explicitly rather than silently ignore, so non-admins can't probe for admin behavior.
    - Given: admin PUT/POST with `body.targetUserId` != admin.id AND body includes ANY of {password, key, keyPassword, rdpPassword, vncPassword, telnetPassword, sudoPassword} that is non-null/non-empty → 400 with body `{ error: "Cross-user writes cannot include inline sensitive credentials — use credentialId instead" }`. Explicitly enumerated because encryption of these fields requires the target's data key, which Skynet only holds when the target is actively logged in — refusing at the API boundary is safer than a runtime encryption failure.
    - Regression: non-admin POST/PUT for their own hosts (no targetUserId key in body) works exactly as before. No behavior change to the happy path.
    - Substrate D-08 guard (`runsFleetSubstrate:true` requires credentialId) still fires BEFORE the cross-user checks so the ordering of error responses stays predictable.
    - The fire-and-forget substrate sweep on POST (line 459-485) and PUT (line 1151-1194) uses the response's `effectiveName` / `effectiveUserId` context; these must still resolve correctly when admin is acting on behalf of the target.
  </behavior>
  <action>
    In host.ts POST /db/host handler (starts line 102):

    1. Right after `const userId = (req as AuthenticatedRequest).userId;` on line 108 and after `hostData` parse (line 108-136), extract `const { targetUserId } = hostData;` alongside the other destructured fields (add to the destructure block starting line 138, position it near `runsFleetSubstrate` for locality).

    2. After hostData validation (right before the `sshDataObj` construction on line 249), add the admin-gate + sensitive-field-block guard:
       - If `targetUserId` is present AND typeof === "string" AND !== userId:
         - Look up isAdmin via the Task 1 helper (`callerIsAdmin(userId)`). If !isAdmin → return `res.status(403).json({ error: "Only admin users can set targetUserId" })`. Log via `sshLogger.warn` with `operation: "host_create_target_user_rejected_not_admin"` + userId + attemptedTargetUserId.
         - If admin AND any of the sensitive fields is non-empty (check `password || key || keyPassword || rdpPassword || vncPassword || telnetPassword || sudoPassword` — the destructured names from hostData) → return `res.status(400).json({ error: "Cross-user writes cannot include inline sensitive credentials — use credentialId instead" })`. Log via `sshLogger.warn` with `operation: "host_create_target_user_rejected_sensitive_fields"` + which fields were present.
       - Compute `const effectiveUserId = (targetUserId && typeof targetUserId === "string") ? targetUserId : userId;` — after the guards above have passed.

    3. Replace `userId: userId,` in the `sshDataObj` block (line 250) with `userId: effectiveUserId,`.

    4. Replace the `SimpleDBOps.insert(hosts, "ssh_data", sshDataObj, userId)` call on line 407-412 with `SimpleDBOps.insert(hosts, "ssh_data", sshDataObj, effectiveUserId)`. This is CRITICAL — if effectiveUserId != userId AND userId is admin, we're inserting a row that will be decrypted under effectiveUserId's data key later. Since we've guarded sensitive fields to be absent, the encryption pass will only touch cleartext metadata fields and effectively no-op the per-user encryption. But SimpleDBOps.insert calls `DataCrypto.validateUserAccess(userId)` at line 20 which THROWS if the target user isn't logged in / has no data key cached. Wrap in try/catch and return 400 with `{ error: "Target user not logged in — Skynet cannot encrypt on their behalf. Ask them to log in first." }` if validateUserAccess throws. This is discoverable and actionable for the admin.

    5. Any subsequent code that references `userId` for logging/response should still use the caller's `userId` (audit: who initiated the write); anything that references the ROW OWNER should use `effectiveUserId`. Concretely: `notifyStatsHostUpdated` (line 438-442) and the substrate on-add sweep (line 459-485) do NOT need to change (they read hostId from the returned row, not userId). Logging: add a `databaseLogger.info` with `operation: "host_create_admin_cross_user", callerUserId: userId, effectiveUserId, hostId`  after successful insert when `effectiveUserId !== userId`.

    In host.ts PUT /db/host/:id handler (starts line 709):

    1. After `const userId = (req as AuthenticatedRequest).userId;` on line 718 and hostData parse (through line 820), destructure `targetUserId` from hostData in the big destructure block (line 750-820).

    2. Before the `permissionManager.canAccessHost` call on line 1004, run the same admin-gate + sensitive-field-block guard as in POST:
       - If `targetUserId` present AND !== userId → require isAdmin (403 otherwise). If admin, require no sensitive inline creds in body (400 otherwise).
    
    3. Compute `const effectiveUserId = (targetUserId && typeof targetUserId === "string") ? targetUserId : userId;`.

    4. When !accessInfo.isOwner (line 1019): the current code returns 403 "Only the host owner can modify host configuration". Change this to ALSO allow admin (isAdmin === true, previously looked up) to pass through — else keep the 403. Read the caller's isAdmin ONCE at the top of the handler and reuse (avoid a second DB round-trip inside the try block).

    5. The `ownerId = hostRecord[0].userId` (line 1053) already points at the row owner. The subsequent `SimpleDBOps.update(hosts, "ssh_data", eq(hosts.id, Number(hostId)), sshDataObj, ownerId)` (line 1099-1105) already passes ownerId — good. But note: if admin is targeting via `body.targetUserId` and the row's actual owner differs, `body.targetUserId` should CHANGE the row's userId. Add `sshDataObj.userId = effectiveUserId;` right before the SimpleDBOps.update call — this lets admin re-assign the row's owner if desired. Guard: only set this when admin AND targetUserId present (don't allow non-admin to spoof).

    6. Same try/catch around SimpleDBOps.update — if `DataCrypto.validateUserAccess(effectiveUserId)` throws, return 400 with the same "Target user not logged in" message.

    7. Logging: `databaseLogger.info` with `operation: "host_update_admin_cross_user", callerUserId: userId, effectiveUserId, hostId` when admin + effectiveUserId !== userId + write succeeds.

    **On the smoke-test PUT payload shape** (referenced in Task 3): the PUT handler validates `isNonEmptyString(ip)` and `isValidPort(port)` (line 828-831), so the smoke PUT body MUST include `ip`, `port`, `username`, `authType`. Task 3 sends a full-record replace shape — this is by design of the PUT endpoint (not a patch).

    **openapi doc comments** on the POST + PUT handlers (line 87-101 and line 685-708): add a note in the description that `targetUserId` is admin-only. Just prose in the JSDoc, don't invent a new response schema — the 403/400 error shapes match existing ones.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; npx tsc --noEmit -p tsconfig.json 2&gt;&amp;1 | grep -E "src/backend/database/routes/host\.ts" | head -20 | (grep . &amp;&amp; exit 1 || echo "typecheck clean on host.ts")</automated>
  </verify>
  <done>
    - POST /db/host destructures targetUserId from hostData, gates on isAdmin, blocks sensitive inline creds for cross-user writes, and inserts with effectiveUserId
    - PUT /db/host/:id destructures targetUserId, gates on isAdmin, blocks sensitive inline creds for cross-user writes, allows admin to bypass the "only owner" 403, and updates sshDataObj.userId to effectiveUserId when admin re-assigns
    - Both endpoints return 403 (non-admin with targetUserId) and 400 (admin cross-user with sensitive inline creds) with the exact error messages listed in the behavior section
    - Both endpoints wrap SimpleDBOps.insert / .update in try/catch that catches DataCrypto.validateUserAccess throw and returns 400 with "Target user not logged in" message
    - Substrate D-08 guard still fires (order: D-08 first, then admin-gate)
    - No behavior change when body has no targetUserId key (regression preserved)
    - tsc --noEmit clean on host.ts
    - openapi JSDoc mentions targetUserId is admin-only
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Unit tests (extend host.test.ts) + live smoke linking rows 21/22/23</name>
  <files>src/backend/database/routes/host.test.ts</files>
  <behavior>
    Add a new describe group `describe("Admin cross-user extensions (A1-A6)", ...)` to host.test.ts. Six tests, all against the extracted postHandler / putHandler with mocked db + SimpleDBOps + auth. Mock the `db.select().from(users).where(...)` chain to return `[{ isAdmin: true }]` when the caller is a designated admin id, else `[{ isAdmin: false }]`.

    - A1: Admin GET-list (route.get "/db/host") — mock db.select for hosts to return a mix of rows across userIds. Test: response includes rows where userId != caller when isAdmin true; excludes them when isAdmin false. (If extracting the GET handler is too fiddly given the test's current POST/PUT-only handler extraction on line 259-271, extend the loadHandlers() switch to also capture the GET handlers via `route.methods["get"]`).
    - A2: Non-admin POST with body.targetUserId = "other-user" → status 403, body.error matches "Only admin users can set targetUserId", SimpleDBOps.insert NOT called.
    - A3: Admin POST with body.targetUserId = "other-user" AND body.password = "x" → status 400, body.error matches "Cross-user writes cannot include inline sensitive credentials", SimpleDBOps.insert NOT called.
    - A4: Admin POST with body.targetUserId = "other-user" AND credentialId = 42 AND no inline creds AND runsFleetSubstrate = false → status 200, SimpleDBOps.insert called with argument #4 (userId param) === "other-user".
    - A5: Non-admin PUT with body.targetUserId = "other-user" → 403 as above.
    - A6: Admin PUT on a row where mock hostRecord[0].userId = "other-user" (admin does not own), body.targetUserId absent, no inline creds → 200, SimpleDBOps.update called and permissionManager.canAccessHost's isOwner:false branch is BYPASSED (mock canAccessHost to return `{hasAccess: true, isOwner: false}` and confirm no 403).

    Mocking strategy: extend the existing beforeEach db.select().from().where() mock (line 318-324) so it can return either the users-lookup shape `[{isAdmin: true/false}]` OR the hosts-lookup shape based on which schema table is being queried. Simplest: use a `.mockImplementation((tableArg) => ...)` on `.from` that switches on the arg identity — inspect what `users` vs `hosts` symbols look like after the mock (`_colName` etc.) and branch there. If that gets fiddly, add a per-test `db.select.mockReturnValueOnce(...)` chain for the admin-lookup call before the primary chain.

    Do NOT run the full vitest suite — run only the extended file:
    `npx vitest run src/backend/database/routes/host.test.ts --reporter=default`

    **Live post-fix smoke test** (INCLUDED — run AFTER unit tests pass, BEFORE writing SUMMARY.md, per scope prompt):

    1. `curl -sSf https://term.example.com/ -o /dev/null -w "%{http_code}\n"` — expect 200 or 3xx (site reachable).
    2. Check existing session cookie: `curl -sS -b /tmp/sk-cj -o /tmp/sk-me -w "%{http_code}\n" https://term.example.com/users/me` — if 200, reuse. Else re-login via runbook flow `~/.claude/roles/box-maintainer/runbooks/skynet-admin/skynet-admin.md § Get an admin cookie` (uses `~/.claude/roles/box-maintainer/skynet-creds.json` + TOTP, writes fresh cookie to `/tmp/sk-cj`).
    3. Fetch credentials list as ashley: `curl -sS -b /tmp/sk-cj https://term.example.com/credentials | jq '.[] | {id, name, userId}'` — record IDs for `workstation-ssh`, `thenasty-ssh`, `ZoeyBattlestation-ssh`. Prior session indicated ids 2, 5, 3 — re-verify from live response.
    4. Look up existing host rows 21, 22, 23 to preserve their existing ip/port/username in the PUT body (PUT is full-record replace, so we can't blindly send only the fields we want to change):
       `curl -sS -b /tmp/sk-cj https://term.example.com/host/db/host/21 | jq '{ip,port,username,name,connectionType,enableSsh,enableRdp,enableTerminal}'`
       Repeat for 22, 23.
    5. For each host 21/22/23, PUT the full-record body with the newly-admin-capable endpoint. Example for row 21 (target ownership stays whatever it currently is — DO NOT set targetUserId if not needed; instead we're just exercising the "admin can update a row they don't own" path):

       ```
       curl -sS -b /tmp/sk-cj -X PUT \
         -H "Content-Type: application/json" \
         https://term.example.com/host/db/host/21 \
         -d '{"ip":"<from step 4>","port":<from step 4>,"username":"<from step 4>","name":"<from step 4>","connectionType":"ssh","authType":"credential","credentialId":<workstation-ssh id>,"key":null,"password":null,"keyPassword":null,"enableSsh":true,"enableRdp":false,"enableTerminal":true,"runsFleetSubstrate":true}'
       ```

       Note: `runsFleetSubstrate:true` + credentialId satisfies the D-08 guard. Set the other enable* flags to whatever the row already had (from step 4) to avoid inadvertently disabling something. `key:null,password:null,keyPassword:null` explicitly clears any old inline creds — since authType is now "credential", these should be nulled per the "if effectiveAuthType === 'credential'" branch in the PUT handler (host.ts:940+ handles password/key branches; the credential branch falls through the else on line 991-996 which nulls all three).

       Expect HTTP 200 with the updated row in response.
    6. Same PUT for row 22 → thenasty-ssh, row 23 → ZoeyBattlestation-ssh.
    7. Wait 30s for the next sweep tick: `sleep 32`.
    8. Grep for the specific warning in the last 60s of container logs:
       `sudo docker logs skynet --since 60s 2>&amp;1 | grep -cE "Unsupported authType.*fleetHostId:(21|22|23)"`
       Expected result: `0`.
    9. Capture the smoke-test result (each curl status + the grep count) in SUMMARY.md. If any PUT returns non-200 (e.g. encryption error, or targetUserId gate misfired), capture the exact error body — a failing smoke test with actionable error text is still useful data per scope prompt.

    Rebase discipline (from scope prompt constraints): before ANY `git push`, run `git pull --rebase origin feat/tab-title-from-tmux` — the multi-identity role means peer branches move under us. Do NOT force-push. Do NOT commit .planning/ docs from inside the executor — orchestrator handles the docs commit.
  </behavior>
  <action>
    1. Read the existing host.test.ts loadHandlers() function (line 249-273) to understand the router.stack walk pattern. Extend it to also capture GET handlers for the 4 read routes if the A1 test needs them, else keep A1 as a plain assertion against the mocked select chain output (simpler).

    2. Add the new `describe` block after the existing PUT tests block. Use the existing `makePostReq` / `makePutReq` / `makeMockRes` helpers. Add an admin id constant `const ADMIN_ID = "admin-user-1";` and configure the db mock in beforeEach (or per-test) so the users-lookup returns `[{isAdmin: true}]` for ADMIN_ID and `[{isAdmin: false}]` for "user-1".

    3. Run unit tests: `npx vitest run src/backend/database/routes/host.test.ts --reporter=default 2>&amp;1 | tee /tmp/quick-439-vitest.log`. If any test fails, fix and re-run. Do NOT run the full suite.

    4. Run the live smoke test steps 1-9 above. For each PUT, capture the HTTP status code AND response body. For the final docker logs grep, print the exact grep output line.

    5. Write SUMMARY.md at `.planning/quick/260910-439-admin-cross-user-host/260910-439-SUMMARY.md` following the standard summary template with a `## Smoke Test` section that includes: session cookie source (reused vs re-login), cred IDs looked up, PUT results per row (status + brief response summary), sleep duration, docker logs grep command + exact numeric output (must be 0), and whether the "Unsupported authType" warning fully cleared.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; npx vitest run src/backend/database/routes/host.test.ts --reporter=default 2&gt;&amp;1 | tail -30 | grep -E "Test Files.*passed|failed" | grep -v "failed"</automated>
  </verify>
  <done>
    - New describe group A1-A6 added to host.test.ts, all 6 tests pass
    - Existing P1-P6 and U1-U5 test groups still pass (no regression)
    - Live smoke test executed end-to-end: session cookie valid, cred IDs resolved, PUT 21/22/23 all returned 200 (or if any returned non-200, exact error captured)
    - Post-sweep docker logs grep for "Unsupported authType.*fleetHostId:(21|22|23)" returns 0
    - SUMMARY.md written with smoke-test section documenting each step's outcome
    - NO changes to .planning/ files are committed from the executor (orchestrator handles that)
    - NO hand-patch of DB rows or peer settings.json (smoke uses Skynet's OWN newly-admin-capable PUT)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Skynet HTTPS | Authenticated JWT cookie crosses here; body params (targetUserId) untrusted |
| Skynet → SQLite | isAdmin lookup + hosts/creds writes cross here; DB-side integrity is trusted |
| Skynet → Peer via SSH | Not directly touched by this quick — indirect via fleet-status sweep after PUT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-Q439-01 | Elevation of Privilege | non-admin sets body.targetUserId to attempt cross-user write | mitigate | Explicit 403 with clear error message before any DB write; isAdmin read from DB (not JWT) per users.ts:442 pattern, defends against mid-session role revocation |
| T-Q439-02 | Information Disclosure | non-admin reads via /db/host — inadvertently sees another user's row via a bug in the conditional filter | mitigate | Regression tests A1 (admin=true sees others, admin=false does not) + explicit `isAdmin ? undefined : eq(hosts.userId, userId)` pattern; non-admin path is byte-identical to pre-change |
| T-Q439-03 | Tampering | admin PUT includes inline password/key targeting another user's row — encryption falls back to caller's data key, corrupting the row | mitigate | Explicit 400 with enumerated sensitive-field-list before SimpleDBOps.update; SimpleDBOps.validateUserAccess try/catch as backstop |
| T-Q439-04 | Information Disclosure | admin READ of another user's ssh_credentials row returns decrypted secrets | accept | Sensitive fields fall through LazyFieldEncryption fallback to empty string (line 148 lazy-field-encryption.ts); ssh_credentials also uses SystemCrypto sharing-key encryption but SimpleDBOps.select doesn't use that path — so admin gets metadata, not the raw secret. Documented in Task 1 action notes. |
| T-Q439-05 | Repudiation | admin acts on other users' hosts without audit trail | mitigate | New `operation: "admin_cross_user_read"` / `"host_create_admin_cross_user"` / `"host_update_admin_cross_user"` structured log lines with callerUserId + effectiveUserId + hostId |
| T-Q439-06 | Denial of Service | admin PUT triggers substrate fire-and-forget sweep on a foreign row, sweep fails, admin sees no error | accept | Fire-and-forget already has defense-in-depth try/catch (host.ts line 476-483, 1184-1192) that never surfaces to the response; existing behavior preserved |

Package legitimacy gate: N/A — no npm installs in this quick.
</threat_model>

<verification>
- tsc --noEmit clean on host.ts, credentials.ts (Task 1 + Task 2)
- vitest run against host.test.ts only — all P1-P6 + U1-U5 + A1-A6 pass (Task 3)
- Live smoke: HTTP PUT to rows 21/22/23 returns 200; post-sweep docker log grep count for the "Unsupported authType" warning on those fleetHostIds = 0 (Task 3)
</verification>

<success_criteria>
- Admin cross-user reads work on the 4 host read endpoints + 2 credentials read endpoints, verified by A1 unit test and by the live PUT which itself required an implicit admin read to fetch the row via GET before PUT-ing
- Admin cross-user writes work on host POST + PUT via `body.targetUserId` (admin-only) AND on PUT for rows admin does not own even without targetUserId
- Non-admin attempts to use targetUserId are rejected with 403 (not silently ignored)
- Admin cross-user writes with sensitive inline creds are rejected with 400 (safety guard for encryption boundary)
- Skynet's own API is sufficient to link joe/zoey's rows 21/22/23 to Ashley's shared credentials — no hand-patch of DB rows or SSH-in required (this is the whole point of the quick, per scope prompt)
- Post-fix smoke: `Unsupported authType` warnings for fleetHostId 21/22/23 stop appearing in skynet container logs after the next fleet-status sweep
</success_criteria>

<output>
Create `.planning/quick/260910-439-admin-cross-user-host/260910-439-SUMMARY.md` when done, including the mandatory `## Smoke Test` section with per-row PUT results and the final `grep -cE` numeric output.
</output>
