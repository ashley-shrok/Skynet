---
quick_id: 260918-52n
type: quick
autonomous: true
files_modified:
  - src/backend/database/routes/identity-birth-orchestrator.ts
  - src/backend/database/routes/identity-birth-orchestrator.test.ts
  - src/backend/spawn-requests/worker.ts
---

<objective>
Derive the identity folder name from the MXID localpart (already-uniqueness-guaranteed via `deriveMxidWithOrdinal`) instead of from the short pool name, so `~/fleet/identities/<mxid-localpart>/` collisions are structurally impossible.

Purpose: kill two production bugs verified live tonight (2026-09-18):
  1. Claude Code auto-resume of the OLD JSONL at `~/.claude/projects/-home-ubuntu-fleet-identities-<name>-workspace/` when a pool name is reused (anthem/piano/ballad/lullaby all resumed prior Sep 13-16 sessions; anthem executed unauthorized `git push` + `CLAUDE.md` edits, piano ran `tmux kill-server` killing 74 sessions).
  2. agent-supervisor's `retire_identity` "State 3" archive-name collision (`identities-archive/<name>/` already exists → `mv` refuses → archive silently fails, identity stuck).

Output: birth-forward-only change. Existing short-name-folder identities are NOT migrated. Folder path shifts from `~/fleet/identities/anthem/` to `~/fleet/identities/anthem-box-maintainer-2/` on the next fresh birth of a `poolPicked` identity. `displayName` still derives from `opts.name` so UI badges keep showing "Anthem", not "Anthem-box-maintainer-2".

Scope discipline (per full design brief — locked, do NOT re-decide):
  - Keep `opts.name` for displayName derivation ONLY (~L1465-1468).
  - Introduce a new derived value `identityFolderName` = MXID localpart with `@` and `:server` suffix stripped. Use it for ALL folder/file path construction downstream of MXID derivation.
  - Move MXID derivation OUT of Step 6's `runStep` block into Step 1 so the folder-existence probe (~L1319 LOCAL, ~L1331 REMOTE) runs against the derived path.
  - Preserve external `step:N` SSE event contract — no new step numbers. Step 1 grows an internal sub-order (derive MXID → derive folder name → existence probe → role-folder probe → avatar candidate check). Step 6 keeps its admin-mint + login-as-user logic but reads the already-computed mxid from closure.
  - Preserve every "no rm / no rm -rf" anti-rollback assertion in the test file — Q2 no-rollback locks are load-bearing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/backend/database/routes/identity-birth-orchestrator.ts
@src/backend/database/routes/identity-birth-orchestrator.test.ts
@src/backend/spawn-requests/worker.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorder MXID derivation to Step 1 and thread `identityFolderName` through the orchestrator</name>
  <files>src/backend/database/routes/identity-birth-orchestrator.ts, src/backend/spawn-requests/worker.ts</files>
  <action>
Refactor `identity-birth-orchestrator.ts` so folder/file paths derive from the MXID localpart instead of `opts.name`, and MXID derivation runs in Step 1 before the folder-existence probe. Preserve the external `step:N` SSE event contract exactly — no new step numbers, same started/completed/failed framing for steps 1, 2, 6, 7, 8.

1. Extract MXID derivation into a reusable helper in the same file (unexported, module-scope), or an inline block usable from both `birthIdentity` and `runRelayMintAndWrite`. Signature-wise, given `{ name, role, poolPicked }` + `deps.matrixServerName`, `deps.matrixHomeserver`, `deps.matrixCountUsersMatching`, it must:
   - Compute `serverName` via the same rule that currently lives at L886-889 (`deps.matrixServerName != null ? deps.matrixServerName : extractServerName(deps.matrixHomeserver)`).
   - Compute the MXID via the SAME branching that currently lives inside `runRelayMintAndWrite`'s Step 6 (L914-950): when `poolPicked === true` AND `typeof role === "string"`, try `composeMxidLocalpart(name, role)` + `deriveMxidWithOrdinal(baseHandle, serverName, deps.matrixCountUsersMatching)`, silent-fallback to `@<name>:<serverName>` ONLY on `mxid_name_not_pool_shape`, rethrow all other errors. Legacy branch: `@<name>:<serverName>`.
   - Return `{ mxid, identityFolderName }` where `identityFolderName` is the mxid with the leading `@` stripped and the `:<serverName>` suffix stripped (a plain slice, since we control the composition — do NOT regex-strip; use `mxid.slice(1, mxid.length - serverName.length - 1)` or equivalent). Assert (via `IDENTITY_KEY_RE.test(identityFolderName)`) that the derived folder name still passes the identity-key regex; throw if it does not (defense-in-depth — `composeMxidLocalpart` output is already lowercase-kebab so this should never fire, but a future change to the localpart shape must not silently break folder-safety).

2. Inside `birthIdentity`:
   - Run this derivation at the TOP of `runStep(1, ...)`, BEFORE the role-folder probe (which currently is the first substantive check at L1247-1283). Order inside Step 1 becomes: (a) derive mxid + identityFolderName, (b) role-folder probe, (c) avatar candidate check, (d) on-disk collision probe using the derived path.
   - On the LOCAL collision probe (L1319): replace `path.join(getLocalIdentitiesRoot(), opts.name)` with `path.join(getLocalIdentitiesRoot(), identityFolderName)`.
   - On the REMOTE collision probe (L1331): replace `$HOME/fleet/identities/${opts.name}` with `$HOME/fleet/identities/${identityFolderName}`.
   - Hoist `mxid` and `identityFolderName` into `try` block scope (declared with `let` before `runStep(1)` starts) so Steps 2, 6, 7, 8 and the supervisor-wait block can all reference them.

3. Inside Step 2 (`runStep(2, ...)`, ~L1375-1499):
   - L1415 LOCAL `identityDir`: use `${getLocalIdentitiesRoot()}/${identityFolderName}` (NOT `opts.name`).
   - L1421 REMOTE `identityDir`: use `${remoteHome}/fleet/identities/${identityFolderName}` (NOT `opts.name`).
   - L1423 `identityFilePath`: use `${identityDir}/${identityFolderName}.md` — the on-disk identity file basename matches the folder name (agent-supervisor uses `basename` on the folder path to compute the identity key, so this stays consistent).
   - L1457 `avatarFilename`: `${identityFolderName}.${avatarExt}` — matches folder + basename convention.
   - L1497 `deps.writeAvatarSiblingFile(conn, opts.name, ...)`: replace `opts.name` with `identityFolderName` (the primitive uses its second arg for the target-path folder key — verify by reading `writeAvatarSiblingFile`'s signature in `src/backend/claude-session/identity-artifact-reader.ts` before the swap; if it treats that arg as the folder key, swap; if it treats it as a display label, leave alone). Do the same audit for `deps.writeMarkdownFileAtomic` at L1478 — `identityFilePath` is already the fully-composed path from step (3), so it should just work.
   - L1465-1468 `displayName`: KEEP using `opts.name` (per design brief lock). This is the sole surviving `opts.name` consumer downstream of Step 1 and is intentional so "Anthem" (not "Anthem-box-maintainer-2") shows in UI badges.
   - `buildIdentityFileBody`'s `# ${opts.name}` H1 heading at L581: KEEP using `opts.name` — the H1 is a human-readable title, not a filesystem identifier. This preserves grep/reading affordances (`# anthem` inside `anthem-box-maintainer-2.md`).

4. Refactor `runRelayMintAndWrite` (L807-1069):
   - Delete the in-helper MXID derivation block (L914-950) and the `let mxid: string` at L893.
   - Extend the helper's `opts` param to accept `mxid: string` and `identityFolderName: string` (both required). The caller (birthIdentity) passes what it derived in Step 1; the retry route (identity-birth.ts POST /retry/:key) must be updated to derive+pass both — see step 5 below.
   - Step 8's `writeIdentityFile(opts.name, "relay.json", ...)` at L1063: replace `opts.name` with `opts.identityFolderName`. `writeIdentityFile`'s first arg is the identity-key used to build the target path (`fleet/identities/<key>/relay.json`).
   - Delete the local `serverName` derivation at L886-889 (moved into the shared helper in step 1).

5. Update the sole caller inside `birthIdentity` (L1520-1541) to pass the new fields:
   ```
   await runRelayMintAndWrite({
     name: opts.name,            // still passed — used for displayName default only
     displayName,
     hostId: opts.hostId,
     role: opts.role,
     poolPicked: opts.poolPicked,
     mxid,                       // derived in Step 1
     identityFolderName,         // derived in Step 1
   }, emit, deps, conn);
   ```

   Then find the OTHER caller of `runRelayMintAndWrite` — the retry route in `src/backend/database/routes/identity-birth.ts` (search for `runRelayMintAndWrite\s*\(` in that file). Update it to derive mxid + identityFolderName using the same shared helper before invoking. The retry route currently takes the legacy `@<name>:<server>` branch always (poolPicked undefined) — under the new shape, the retry route derives `mxid = @${opts.name}:${serverName}` and `identityFolderName = opts.name` (since retry operates on an already-on-disk folder whose name IS `opts.name`). Confirm by reading identity-birth.ts's retry handler before editing — if the retry path uses `runRelayMintAndWrite` differently than assumed, adjust accordingly, but the invariant "retry uses the folder-name that already exists on disk" must hold.

6. `identityId` at L1172 (`const identityId = opts.name;`) + emitted `ended{ok:true, identityId, sessionName: opts.name}` at L1642: change `identityId` to `identityFolderName` and `sessionName` to `identityFolderName`. The design brief says the identity IS its folder name on disk; downstream consumers (`spawn-requests/worker.ts` at L487 checks `.ok`, uses `lastPickedName.toLowerCase()` for the success payload, NOT the emit's identityId/sessionName) will continue to work because worker's SuccessResponse builds `name` from its own `lastPickedName`, not from the emit — verify by re-reading worker.ts L513-537.

   Wait — worker.ts DOES use `lastPickedName.toLowerCase()` for the success payload name field (L535), so downstream coord consumers see the SHORT pool name in success responses. That's fine and intentional per the coord dispatch contract. But the SSE-consumer (frontend PrettyView) may key off `sessionName` or `identityId` from the ended event to route to the identity's page. Grep for consumers before flipping: `grep -rn "sessionName\|identityId" src/ui/` and `src/backend/` for `ended.identityId`, `ended.sessionName`. If any consumer keys off these fields to open a session by URL, `identityFolderName` (not `opts.name`) is the correct value since the folder name is the tmux session name that agent-supervisor uses. If ANY consumer expects the short pool name, add a comment explaining the flip and update the consumer.

7. Add a short code comment in `src/backend/spawn-requests/worker.ts` near the pool-name-collision retry loop (~L436-506) noting that folder uniqueness now comes from mxid uniqueness (deriveMxidWithOrdinal iterates ordinals against Synapse), so the COLLISION_REASON "identity already exists on this host" branch is expected to be rare — it now only fires when an orphaned folder from a prior failed birth still occupies the derived path (e.g. Step 8 crashed after Step 2's mkdir but before the supervisor picked the folder up, and the ordinal search happened to converge on the same suffix). No functional change.

Build gate — must pass BEFORE committing:
  npm run build:backend && npm run build

Common-mistake watchlist:
  - Do NOT rebuild `identityFolderName` from mxid inside downstream steps — hoist and reuse the value computed in Step 1. Recomputing invites drift.
  - `extractServerName` is currently a private function (L630). If the shared MXID-derivation helper needs it AND lives outside `runRelayMintAndWrite`'s closure, no visibility change is needed — same file. Do NOT export it if not required.
  - `IDENTITY_KEY_RE` at L480 must accept the derived folder name. `anthem-box-maintainer-2` matches `[a-z0-9._=/+-]+` — safe. `TMUX_SAFE_NAME_RE` at L490 (`[a-z][a-z0-9_-]*`) also matches — safe.
  - `opts.name` gate at L1095-1108 (IDENTITY_KEY_RE + TMUX_SAFE_NAME_RE) stays. The identityFolderName gate (added in step 1) is defense-in-depth on the DERIVED value.
  - Do NOT touch `# ${opts.name}` in `buildIdentityFileBody` — that's the H1 markdown heading, human-facing.
  - Do NOT add rm/rm -rf/unlink/rmdir anywhere. Q2 no-rollback lock stands.
  </action>
  <verify>
    <automated>cd /home/ubuntu/fleet/identities/cove/workspace/skynet && npm run build:backend && npm run build</automated>
  </verify>
  <done>
  - `identityFolderName` is derived from mxid localpart inside Step 1's `runStep`, BEFORE the folder-existence probe.
  - LOCAL + REMOTE folder-existence probes at L1319 / L1331 use `identityFolderName`.
  - Step 2's `identityDir`, `identityFilePath`, `avatarFilename` all use `identityFolderName`.
  - `writeAvatarSiblingFile`'s folder-key arg uses `identityFolderName` (or is left alone if that arg is not the folder key — audited).
  - `runRelayMintAndWrite` no longer derives MXID internally; accepts `mxid` and `identityFolderName` as opts.
  - `writeIdentityFile(...)` at L1063 uses `identityFolderName`.
  - The other caller of `runRelayMintAndWrite` in `identity-birth.ts` (retry route) is updated consistently.
  - `identityId` and `sessionName` in the ended event use `identityFolderName`.
  - `displayName` (L1465-1468) still uses `opts.name` — deliberately unchanged.
  - `buildIdentityFileBody`'s H1 `# ${opts.name}` unchanged.
  - Worker.ts has a new short comment near L436-506 explaining folder-collision retry is now rare.
  - `npm run build:backend && npm run build` clean (backend + frontend TypeScript compile).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update tests to reflect mxid-derived folder naming + add a new test asserting derivation</name>
  <files>src/backend/database/routes/identity-birth-orchestrator.test.ts, src/backend/spawn-requests/worker.test.ts</files>
  <action>
Update `identity-birth-orchestrator.test.ts` and `worker.test.ts` to reflect the new folder-naming semantics. Preserve every "no rm / no rm -rf" anti-rollback assertion — those are Q2 no-rollback locks and MUST stay green.

1. Systematically grep the orchestrator test file for hard-coded folder-name expectations that are now stale:
   ```
   grep -n "fleet/identities/agent1\|fleet/identities/agent92\|/testkey\|/mock\|\.md\|relay.json" src/backend/database/routes/identity-birth-orchestrator.test.ts
   ```
   Test setup uses `name: "testkey"`, `role: "box-maintainer"`, `poolPicked` undefined (default) in `makeOpts`. Because `poolPicked` is undefined, the legacy MXID branch fires → mxid becomes `@testkey:<serverName>` → `identityFolderName === "testkey"`. So MOST existing tests should be UNCHANGED for their path assertions (folder still resolves to `testkey/`). Verify this by re-running the tests after Task 1 and adjusting only the failures.

2. For tests that DO explicitly exercise the `poolPicked: true` + role-triggered MXID-derivation path (grep for `poolPicked: true` in the test file), update expected folder paths:
   - `pool-picked birth with existing account → folder becomes <pool>-<role>-2/`
   - Adjust any `expect(...).toContain("fleet/identities/<oldname>/")` or similar to reflect the derived name.

3. Grep for tests using `agent1`, `agent92` (test-fixture names appearing in relay.json path assertions like L1379, L2024, L2245) — those tests use makeOpts with `name: "agent1"` (or similar) and default `poolPicked` undefined, so `identityFolderName === "agent1"`. Existing assertions like `fleet/identities/agent1/relay.json` should still pass. If any test explicitly sets `poolPicked: true` with a custom name, the assertion needs updating to the derived form. Enumerate failing tests via a run before edits.

4. Add ONE new test asserting the derived-folder-name behavior. Place after the existing MXID-derivation tests (search for `deriveMxidWithOrdinal` or `poolPicked` blocks in the test file for insertion locale). Concrete test spec:

   Title: "birth with poolPicked=true + role + existing account creates folder at derived mxid-localpart path (not at bare pool name)"

   Setup:
     - `makeOpts({ name: "anthem", role: "box-maintainer", poolPicked: true })`
     - `deps.matrixCountUsersMatching` mock: returns `{ ok: true, total: 1 }` for `@anthem-box-maintainer:...` (simulates the base handle already exists) and `{ ok: true, total: 0 }` for `@anthem-box-maintainer-2:...` (simulates the ordinal-2 slot free).
     - `deps.isLocalHostId` returns `true` (LOCAL branch — fs paths are easier to assert than SSH exec strings).
     - `getLocalIdentitiesRoot` mock returns `/tmp/test-fleet/identities` (already the default in makeDeps).

   Assertions:
     - LOCAL folder-existence probe path is `/tmp/test-fleet/identities/anthem-box-maintainer-2` (NOT `/tmp/test-fleet/identities/anthem`).
     - Step 2's mkdir exec command contains `fleet/identities/anthem-box-maintainer-2/wakeups` (NOT `fleet/identities/anthem/wakeups`).
     - `deps.writeMarkdownFileAtomic` is called with a path ending in `/anthem-box-maintainer-2/anthem-box-maintainer-2.md` (NOT `/anthem/anthem.md`).
     - `writeIdentityFile` (module-level `writeMarkdownFileAtomic` for LOCAL) is called with `fleet/identities/anthem-box-maintainer-2/relay.json` for the relay.json write.
     - Ended event on success: `ended{ok:true, identityId: "anthem-box-maintainer-2", sessionName: "anthem-box-maintainer-2"}`.
     - Zero `rm` / `rm -rf` invocations across all execCommand + execLocal calls (Q2 no-rollback anti-rollback assertion — re-use the helper pattern from tests C1/C2/B).

5. Preserve every existing anti-rollback assertion. Do NOT delete or weaken tests whose names or bodies mention "Q2 agent-supervisor race", "no-rollback", or "rm -rf". If a test asserted `fleet/identities/agent1/` and it's now `agent1-box-maintainer-2/` because Task 1 changed the fixture's `poolPicked`, update the path — but the `expect(...).not.toContain("rm")` (or equivalent) assertion stays.

6. `worker.test.ts` audit — the worker sets `poolPicked: true` always (L468) and doesn't currently mock `matrixCountUsersMatching` outputs precisely, so existing worker tests that assert success paths may need their birth-deps mock to return `{ ok: true, total: 0 }` for the bare handle so the mxid stays at `@<name>-<role>:server` (no ordinal suffix). Grep for `poolPicked` and `birthIdentity` in worker.test.ts. If no assertion in worker.test.ts inspects the derived folder path (worker.ts talks to birthIdentity via the emit callback and success-file payload, not directly to folder paths), no changes should be needed. Minimal churn — only touch what breaks.

Test gate — must pass:
  cd /home/ubuntu/fleet/identities/cove/workspace/skynet && npx vitest related --run src/backend/database/routes/identity-birth-orchestrator.ts src/backend/spawn-requests/worker.ts

Do NOT run the full vitest suite — per fleet rule (2026-08-20 + 2026-09-07), full suite is a per-deployment gate, not per-commit.

Common-mistake watchlist:
  - Do NOT delete anti-rollback tests to make them pass — update the folder path assertion, keep the "no rm" assertion.
  - Do NOT change the existing default `poolPicked: undefined` behavior in `makeOpts` — that's what keeps most existing tests green.
  - The new test MUST use `poolPicked: true` explicitly AND set `matrixCountUsersMatching` to return `total: 1` for the base handle so the ordinal search actually iterates to `-2`. A `total: 0` on first call would give `anthem-box-maintainer` (no suffix), which is also valid but doesn't prove the ordinal-suffix path.
  - When adding new mocks, put them next to existing ones in `makeDeps` overrides so future readers see the pattern.
  </action>
  <verify>
    <automated>cd /home/ubuntu/fleet/identities/cove/workspace/skynet && npx vitest related --run src/backend/database/routes/identity-birth-orchestrator.ts src/backend/spawn-requests/worker.ts 2>&1 | tail -30</automated>
  </verify>
  <done>
  - All orchestrator tests pass. Tests that exercise `poolPicked: true` + role have path assertions matching the derived form (`<pool>-<role>[-N]/`).
  - All worker tests pass; changes limited to what breaks (minimal churn).
  - Every "no rm" / "no rollback" / "Q2 agent-supervisor race" anti-rollback assertion is intact and still green.
  - New test asserts: (a) LOCAL folder probe path is derived, (b) Step 2 mkdir uses derived path, (c) writeMarkdownFileAtomic identity file path uses derived name, (d) writeIdentityFile relay.json path uses derived name, (e) ended event identityId + sessionName are the derived name, (f) zero `rm` invocations.
  - `npx vitest related --run` on the two files exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Atomic commits</name>
  <files>(git)</files>
  <action>
Split the work from Tasks 1 and 2 into atomic commits with clear messages. Suggested granularity (adjust to actual diff shape if needed):

  Commit A (Task 1 core refactor):
    "birth: derive identity folder name from mxid localpart

     Move MXID derivation from Step 6 into Step 1, introduce
     identityFolderName threaded through Steps 1/2/6/7/8, and switch
     folder/file paths from opts.name to identityFolderName.

     Fixes:
       - Claude Code session-resume via workspace path when pool name is
         reused (JSONL at ~/.claude/projects/-home-ubuntu-fleet-identities-
         <name>-workspace/ auto-resumed the OLD identity's transcript on
         a fresh birth with the same short name).
       - agent-supervisor's retire_identity 'State 3' archive-name
         collision (identities-archive/<name>/ already exists → archive
         silently fails, identity stuck).

     opts.name is still used for displayName + H1 heading (human-readable).
     External step:N SSE event contract preserved.

     Birth-forward-only. Existing short-name-folder identities are NOT
     migrated. Verified live 2026-09-18: anthem/piano/ballad/lullaby
     freshly-spawned identities auto-resumed prior Sep 13-16 sessions;
     anthem executed unauthorized git push + CLAUDE.md edits, piano ran
     tmux kill-server killing 74 sessions."

  Commit B (Task 2 tests):
    "birth tests: cover mxid-derived folder naming

     Update expected folder paths for poolPicked-triggered MXID derivation.
     Add a new test asserting that birth with pool-name 'anthem' + role
     'box-maintainer' + an existing @anthem-box-maintainer:server account
     creates folder at anthem-box-maintainer-2/, not anthem/. Preserve
     every Q2 no-rollback anti-rollback assertion."

  Commit C (Task 1 worker comment) — optional; if the worker.ts comment is a single-hunk change consider folding into Commit A:
    "spawn-requests worker: note folder-collision retry is now rare

     Folder uniqueness now comes from mxid uniqueness (deriveMxidWithOrdinal
     iterates ordinals against Synapse). The collision retry loop only
     fires when an orphaned folder from a prior failed birth still
     occupies the derived path."

Gates before committing:
  - `npm run build:backend && npm run build` clean.
  - `npx vitest related --run src/backend/database/routes/identity-birth-orchestrator.ts src/backend/spawn-requests/worker.ts` exits 0.

Do NOT push. Do NOT deploy. Executor stops at commits + tests green per fleet rule (2026-08-08 — deploy motion is orchestrator-only).
  </action>
  <verify>
    <automated>cd /home/ubuntu/fleet/identities/cove/workspace/skynet && git log --oneline -5 && git status --short</automated>
  </verify>
  <done>
  - Two (or three) atomic commits on the current branch (`feat/tab-title-from-tmux`).
  - Each commit message references the design brief's problem framing (session-resume bug, archive-collision bug).
  - `git status` clean (or shows only intentionally-uncommitted files).
  - No push. No deploy.
  </done>
</task>

</tasks>

<verification>
End-to-end sanity checks (all must pass):
  1. `npm run build:backend && npm run build` — clean.
  2. `npx vitest related --run src/backend/database/routes/identity-birth-orchestrator.ts src/backend/spawn-requests/worker.ts` — all tests green.
  3. Anti-rollback grep: `grep -n "rm -rf\|rmdir\|unlink" src/backend/database/routes/identity-birth-orchestrator.ts` returns ZERO new hits vs baseline (Q2 no-rollback lock preserved).
  4. Grep for stale `opts.name` folder-path uses inside the orchestrator: `grep -n "identities/\${opts.name}\|opts\.name.*\.md\|opts\.name.*avatar" src/backend/database/routes/identity-birth-orchestrator.ts` — should only match the displayName / H1 heading contexts, not folder-path composition.
  5. The new test's assertions include the string `"anthem-box-maintainer-2"` in expected folder paths.
</verification>

<success_criteria>
- Identity folder name derives from mxid localpart (Step 1), NOT from `opts.name`.
- `opts.name` still drives displayName (`Anthem`) + H1 heading (`# anthem`) — unchanged.
- External `step:N` SSE event contract unchanged (steps 1, 2, 6, 7, 8; started/completed/failed framing).
- Q2 no-rollback lock preserved — no new `rm` / `rmdir` / `unlink` sites.
- Tests green (build:backend + build + scoped vitest).
- Atomic commits on `feat/tab-title-from-tmux`.
- Both production bugs (session-resume + archive-name collision) are structurally fixed for birth-forward identities.
</success_criteria>

<output>
Return a short summary of what was changed:
  - Files touched (with line-range highlights).
  - Commit hashes + subject lines.
  - Which tests were added / adjusted.
  - Any deviations from the plan and why.
</output>
