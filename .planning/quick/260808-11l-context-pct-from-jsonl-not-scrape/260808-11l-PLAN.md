---
phase: quick-260808-11l-context-pct-from-jsonl-not-scrape
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/backend/claude-session/context-pct-from-jsonl.ts
  - src/backend/claude-session/context-pct-from-jsonl.test.ts
  - src/backend/claude-session/claude-session-server.ts
autonomous: true
requirements:
  - QUICK-260808-11l-01
  - QUICK-260808-11l-02
  - QUICK-260808-11l-03

must_haves:
  truths:
    - "Pretty-view context-% meter reads from the JSONL session file (authoritative source) whenever state.currentSessionFile is resolved, not from tmux capture-pane."
    - "When JSONL read fails or returns null (no sessionFile resolved, tail empty, no assistant turn yet, malformed lines, SSH exec error), the poll falls back to the legacy parseContextPct(tmux capture-pane) path so nothing regresses on fresh sessions or unmapped panes."
    - "Displayed integer matches the number rendered in Claude Code's statusline within ±1 (the empirical 5/8 exact + 3/8 off-by-1 observed 2026-08-08 on session e222905c is acceptable — sub-1% precision is well below the 80% context-watch nudge threshold)."
    - "Frontend WebSocket contract is unchanged: same {type: \"context_pct\", pct} message shape at the same 3s cadence."
    - "The narrow-pane mobile PWA failure mode (pane collapses to ~2 chars, statusline bar chars truncated, parseContextPct returns null, meter freezes) is eliminated for any session where state.currentSessionFile has been resolved (which is the steady-state case after connect via discoverClaudeSession)."
  artifacts:
    - path: "src/backend/claude-session/context-pct-from-jsonl.ts"
      provides: "readContextPctFromJsonl(conn, sessionFile) helper — tails 10KB of the JSONL, reverse-scans for the last assistant turn with usage, computes autocompact-normalized displayed pct."
      exports: ["readContextPctFromJsonl"]
      min_lines: 40
    - path: "src/backend/claude-session/context-pct-from-jsonl.test.ts"
      provides: "Vitest coverage for the 8 required cases (happy path, no assistant turn, missing usage, exec throw, boundary at 16.5%, over-full clamp, multiple-assistant last-wins, malformed-line skip)."
      contains: "readContextPctFromJsonl"
    - path: "src/backend/claude-session/claude-session-server.ts"
      provides: "Poll callback around line 3441 now tries JSONL first (when state.currentSessionFile is set) and falls back to parseContextPct(tmux capture-pane) only on null."
      contains: "readContextPctFromJsonl"
  key_links:
    - from: "src/backend/claude-session/claude-session-server.ts"
      to: "src/backend/claude-session/context-pct-from-jsonl.ts"
      via: "static import + call inside the CONTEXT_PCT_INTERVAL_MS setInterval callback"
      pattern: "readContextPctFromJsonl"
    - from: "src/backend/claude-session/claude-session-server.ts"
      to: "state.currentSessionFile"
      via: "read at poll-fire time (not captured up-front) so session recycles are picked up on the next tick"
      pattern: "state\\.currentSessionFile"
    - from: "src/backend/claude-session/claude-session-server.ts"
      to: "src/backend/claude-session/context-pct-parser.ts"
      via: "fallback call when JSONL path returns null — parseContextPct(output) preserved verbatim"
      pattern: "parseContextPct"
    - from: "src/backend/claude-session/context-pct-from-jsonl.ts"
      to: "src/backend/ssh/tmux-helper.ts"
      via: "import { execCommand } — same import path session-file-discovery.ts uses"
      pattern: "from \"\\.\\./ssh/tmux-helper\\.js\""
---

<objective>
Switch Skynet's pretty-view context-% meter from tmux-pane scraping to reading the JSONL session file directly. Pane scrape is unreliable — when Ashley's mobile PWA collapses the pane to ~2 chars wide, the bar glyphs the parser AND-anchors against get truncated and parseContextPct returns null → meter freezes.

The JSONL file already resolved by discoverClaudeSession (state.currentSessionFile) is the authoritative source Claude Code itself writes token usage to. Reading it eliminates the pane-width dependency entirely.

Purpose: fix the frozen-meter regression on narrow mobile PWA panes, and future-proof the meter against any statusline-format changes upstream (JSONL is Claude Code's own wire format, pane scrape is a display-surface hack).

Output:
- NEW helper `context-pct-from-jsonl.ts` exporting `readContextPctFromJsonl(conn, sessionFile): Promise<number | null>`
- NEW test file covering 8 required cases (mirrors session-file-discovery.test.ts shape)
- MODIFIED poll callback in `claude-session-server.ts` around line 3441 — JSONL first, tmux-scrape as fallback, all existing guards preserved
- `context-pct-parser.ts` and its tests UNCHANGED (they remain the fallback path)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

# Existing modules the new helper must mirror the shape of (do NOT modify):
@src/backend/claude-session/context-pct-parser.ts
@src/backend/claude-session/session-file-discovery.ts
@src/backend/claude-session/session-file-discovery.test.ts
@src/backend/ssh/tmux-helper.ts

# Call-site (only the region around line 3400-3631 is relevant — poll setup + callback + finally):
@src/backend/claude-session/claude-session-server.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write failing tests for readContextPctFromJsonl (all 8 cases)</name>
  <files>src/backend/claude-session/context-pct-from-jsonl.test.ts</files>
  <behavior>
    Mirror the mock shape of `session-file-discovery.test.ts` exactly: vi.mock the `../ssh/tmux-helper.js` module (only `execCommand` needs a working mock; include `queryPanePid` and `queryPaneCurrentCommand` in the factory to match module contract), then `import { execCommand } from "../ssh/tmux-helper.js"` after the mock, then use `vi.mocked(execCommand).mockImplementation(...)` or `.mockResolvedValue(...)` / `.mockRejectedValue(...)` per case. Use `const fakeConn = {} as import("ssh2").Client` as the connection stub. Add `afterEach` that resets the execCommand mock.

    All 8 cases from the planning context, verbatim:

    - Case (a) HAPPY PATH: mock execCommand resolves to a tail containing ONE JSONL line: `JSON.stringify({type: "assistant", message: {usage: {input_tokens: 1, cache_creation_input_tokens: 2182, cache_read_input_tokens: 196359, output_tokens: 1599}}})`. Expect return `24`. This is the empirical measurement for session e222905c (2026-08-08): sum=198542, remaining_pct=80.1458, usable_remaining=76.2225, displayed=round(100-76.2225)=24.
    - Case (b) NO ASSISTANT TURN: tail contains only lines with `type: "user"` or `type: "tool_result"` (no assistant turn at all). Include at least 2 non-assistant lines to prove reverse-scan iterates the full tail. Expect return `null`.
    - Case (c) ASSISTANT WITHOUT USAGE: tail contains one line `{type: "assistant", message: {content: "hi"}}` — assistant type present but `message.usage` is missing/undefined. Expect return `null`. (Reverse-scan must NOT count a usage-less assistant turn as a match.)
    - Case (d) EXEC THROWS: `vi.mocked(execCommand).mockRejectedValue(new Error("SSH exec channel error"))`. Expect return `null` (no throw escapes the helper).
    - Case (e) BOUNDARY AT 16.5%: pick token sum such that `remaining_pct === 16.5` exactly. `sum = 835_000` gives remaining_pct=16.5 → usable_remaining=max(0, ((16.5-16.5)/83.5)*100)=0 → displayed=round(100-0)=100. Use one assistant line with `input_tokens=835000, cache_creation_input_tokens=0, cache_read_input_tokens=0`. Expect return `100`.
    - Case (f) OVER-FULL CLAMP: token sum > 1_000_000 so `remaining_pct` goes negative. Use `input_tokens=1_500_000` → remaining_pct=-50 → (before clamp) usable_remaining=((-50-16.5)/83.5)*100=-79.64… → `Math.max(0, ...)` clamps to 0 → displayed=round(100-0)=100. Expect return `100`.
    - Case (g) MULTIPLE ASSISTANT TURNS — LAST WINS: tail contains TWO assistant lines with different usage; the SECOND (later in the file) must win. Suggested: line 1 usage sum = 100_000 (would give a different pct), line 2 usage sum = 198_542 (the case-a numbers, expect 24). Expect return `24`. This proves the reverse-scan short-circuits on the FIRST match encountered while iterating from the end.
    - Case (h) MALFORMED JSON LINES DON'T BREAK PARSING: tail contains, in order: valid assistant turn A (older, sum=100_000), a garbage line (e.g. `"{not valid json"`), valid assistant turn B (newer, the case-a numbers, sum=198_542). Reverse iteration must skip the garbage line via try/catch on JSON.parse and land on turn B. Expect return `24`.

    Notes for the mock:
    - The helper's SSH exec is a single call: `execCommand(conn, "tail -c 10000 '<sessionFile>'")`. The mock does not need to inspect the script beyond returning the intended stdout (or rejecting for case d).
    - For all cases, invoke `readContextPctFromJsonl(fakeConn, "/home/ubuntu/.claude/projects/-fake-slug/fake-session.jsonl")`. The path string is opaque to the helper's arithmetic — only the tail output matters for the return value.
    - Each JSONL "line" in the mock output should be a single `JSON.stringify(...)` on its own, joined with `\n`. Trailing newline optional; the split-on-`\n` reverse-scan must tolerate a trailing empty string.

    Run the tests now — they MUST all fail with "readContextPctFromJsonl is not defined" (or the import itself must fail). This is the RED anchor. Do not create the helper source file in this task.
  </behavior>
  <action>
    Create `src/backend/claude-session/context-pct-from-jsonl.test.ts` following the mock shape and case list in `<behavior>`. Import `readContextPctFromJsonl` from `./context-pct-from-jsonl.js` (the .js suffix matches project ESM convention — see how `session-file-discovery.test.ts` imports its module). Do NOT create `context-pct-from-jsonl.ts` in this task — the test must fail on import to prove RED.

    Header comment on the test file: 3-4 lines explaining why this exists (JSONL is the authoritative source; pane-scrape becomes unreliable at narrow pane widths on mobile PWA; this helper replaces the primary path and parseContextPct becomes fallback).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx vitest run src/backend/claude-session/context-pct-from-jsonl.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All 8 test cases FAIL (import-not-found or ReferenceError — the module doesn't exist yet). Test file compiles as TypeScript. No other tests were touched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement readContextPctFromJsonl and make all 8 tests pass</name>
  <files>src/backend/claude-session/context-pct-from-jsonl.ts</files>
  <behavior>
    Signature (exact): `export async function readContextPctFromJsonl(conn: Client, sessionFile: string): Promise<number | null>`.

    Imports:
    - `import type { Client } from "ssh2"` (type-only import, matches session-file-discovery.ts line 1)
    - `import { execCommand } from "../ssh/tmux-helper.js"` (same specifier session-file-discovery.ts uses)

    File-top constants:
    - `const AUTO_COMPACT_BUFFER_PCT = 16.5;` — mirrors the same default in ~/.claude/hooks/gsd-statusline.js:312-314. Do NOT read the CLAUDE_CODE_AUTO_COMPACT_WINDOW env var here (that's the hook's concern; this helper stays on the fleet default per Ashley: "every model available in the harness is a million tokens window" + statusline default of 16.5% covers all fleet identities).
    - `const MODEL_CONTEXT_WINDOW = 1_000_000;` — Ashley 2026-08-08 lock: every model in the harness is 1M window.
    - `const TAIL_BYTES = 10_000;` — 10KB tail is plenty for the last few turns; small enough to keep the SSH round-trip fast (well under the 3s poll interval).
    - `const EXEC_TIMEOUT_MS = 3000;` — matches session-file-discovery's DISCOVERY_EXEC_TIMEOUT_MS. Used to race the execCommand against a timeout (see behavior below).

    Behavior:
    1. Build the tail command: `` `tail -c ${TAIL_BYTES} '${sessionFile}'` ``. Single-quote wrap matches how session-file-discovery.ts wraps paths — sessionFile is validated upstream by discoverClaudeSession, so this is sufficient (see session-file-discovery.ts:220-230 for the same escape convention).
    2. Run `execCommand(conn, tailCmd)` inside a `Promise.race` against a 3000ms timeout that rejects with a timeout error (mirror the exact `Promise.race + setTimeout + reject` pattern used at session-file-discovery.ts:122-132 so error handling stays uniform).
    3. Wrap the race in try/catch; on ANY throw (SSH error, timeout, empty output) return `null` (case d — helper never throws).
    4. If the tail output is empty string after `.trim()`, return `null`.
    5. Split the tail on `\n`. Iterate the array in REVERSE (for i = arr.length - 1; i >= 0; i--). For each line:
       - If the line is empty/whitespace-only, skip (continue).
       - Wrap `JSON.parse(line)` in try/catch; on parse error, skip (continue) — do NOT return null (case h: garbage lines don't kill the scan).
       - If parsed is not an object (`typeof parsed !== "object" || parsed === null`), skip.
       - If `parsed.type !== "assistant"`, skip.
       - Extract `usage = parsed?.message?.usage`. If `!usage || typeof usage !== "object"`, skip (case c: assistant without usage doesn't count).
       - Extract `input = Number(usage.input_tokens) || 0`, `cc = Number(usage.cache_creation_input_tokens) || 0`, `cr = Number(usage.cache_read_input_tokens) || 0`. `Number(undefined) || 0` = 0 handles missing fields safely.
       - `sum = input + cc + cr`. Break out of the loop (short-circuit — cases g/h both rely on last-wins == first-match-in-reverse).
    6. If the loop completes without finding a match (no assistant turn with usage anywhere in the tail), return `null` (case b).
    7. Compute:
       - `remaining_pct = 100 - (sum / MODEL_CONTEXT_WINDOW) * 100`
       - `usable_remaining = Math.max(0, ((remaining_pct - AUTO_COMPACT_BUFFER_PCT) / (100 - AUTO_COMPACT_BUFFER_PCT)) * 100)`
       - `displayed = Math.round(100 - usable_remaining)`
       - `return Math.max(0, Math.min(100, displayed))` — final range clamp defensive against float edge cases.

    Header docblock: 12-20 lines explaining (a) WHY this exists (mobile PWA narrow-pane bar-glyph truncation broke parseContextPct; JSONL is Claude Code's own wire format for token accounting = authoritative); (b) WHERE the 16.5% autocompact-buffer normalization comes from (mirror of gsd-statusline.js:306-318, keeping meters aligned); (c) THE ROUNDING NOTE (this helper normalizes floats first then rounds only at display; CC's statusline rounds `remaining_percentage` to integer before normalizing — so the two can differ by ±1 in the last digit; empirical 2026-08-08 = 5/8 exact + 3/8 off-by-1 across 8 live Opus-4.7 sessions; sub-1% precision is way inside the 80% context-watch nudge threshold); (d) NOTE that context-pct-parser.ts remains the fallback path for hosts with no resolved sessionFile or fresh sessions with no assistant turn yet.
  </behavior>
  <action>
    Create `src/backend/claude-session/context-pct-from-jsonl.ts` per the behavior spec above. Then re-run the test file created in Task 1 — all 8 cases must PASS. If any case fails, adjust the helper (not the tests) until GREEN.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx vitest run src/backend/claude-session/context-pct-from-jsonl.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All 8 test cases PASS. Test file and helper both compile cleanly. No other tests touched or affected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire helper into the poll callback (JSONL first, tmux-scrape fallback); typecheck + full-suite green</name>
  <files>src/backend/claude-session/claude-session-server.ts</files>
  <behavior>
    Modify ONLY the `setInterval` callback body at `claude-session-server.ts:3441-3631` (the `contextPctTimer = setInterval(() => { ... }, CONTEXT_PCT_INTERVAL_MS)` block). Every other part of the file — including the constant declaration `const CONTEXT_PCT_INTERVAL_MS = 3000` (line 3436), the `captureCmd` construction (line 3440), the timer teardown `clearInterval(contextPctTimer)` (line 3115), the timer/in-flight state declarations (lines 980-981), and every guard variable — MUST remain byte-for-byte identical.

    Add exactly one static import at the top of the file, alongside the existing `import { parseContextPct } from "./context-pct-parser.js"` at line 11:
    ```
    import { readContextPctFromJsonl } from "./context-pct-from-jsonl.js";
    ```

    New callback shape (replaces the `.then(...).catch(...).finally(...)` chain currently at lines 3447-3630 — the plan_pending logic that lives INSIDE the `.then(...)` from lines 3463-3621 MUST stay working; wrap it into the same async closure and preserve its position relative to the pct emit):

    ```
    contextPctTimer = setInterval(() => {
      if (stopped || ws.readyState !== WebSocket.OPEN) return;
      if (!sshConn) return;
      if (contextPctInFlight) return; // guard against slow SSH pileups
      contextPctInFlight = true;
      const connSnapshot = sshConn;
      // Read state.currentSessionFile at poll-fire time (not up-front) so a
      // session recycle after connect is picked up on the next tick — matches
      // how other state.* fields are consulted inside this callback.
      const sessionFileSnapshot = state.currentSessionFile;
      (async () => {
        try {
          // PRIMARY: JSONL read (authoritative, pane-width-independent).
          let pct: number | null = null;
          if (sessionFileSnapshot) {
            pct = await readContextPctFromJsonl(connSnapshot, sessionFileSnapshot);
          }
          // Get pane output once — needed for parseContextPct fallback AND for
          // isPlanPending / parsePlanFilePath below (they still key off the
          // pane's overlay text, not the JSONL).
          let output = "";
          try {
            output = await execCommand(connSnapshot, captureCmd);
          } catch {
            // Silent — scrape failure is nice-to-have, not load-bearing.
            // pct stays whatever the JSONL path produced; plan-pending simply
            // sees "" and reports null.
          }
          // FALLBACK: only invoke the scrape parser if JSONL yielded no value.
          if (pct === null && output !== "") {
            pct = parseContextPct(output);
          }
          if (stopped || ws.readyState !== WebSocket.OPEN) return;
          if (pct !== null) {
            try {
              ws.send(JSON.stringify({ type: "context_pct", pct }));
            } catch {
              /* ws may be mid-close */
            }
          }
          // ---- PRESERVE VERBATIM: the entire plan-pending block that lives
          // ---- at lines 3463-3621 of the current file, keyed off `output`.
          // ---- The `output` variable above IS the same capture-pane payload
          // ---- the old code used, so isPlanPending(output) / parsePlanFilePath(output)
          // ---- and every de-dup / CR-01 / SFTP-fetch guard downstream keep
          // ---- working unchanged. Do not rename `output` and do not move
          // ---- the plan-pending block into a helper — this task is a swap
          // ---- of the pct-source only.
        } finally {
          contextPctInFlight = false;
        }
      })();
    }, CONTEXT_PCT_INTERVAL_MS);
    ```

    Critical invariants (verify each one after the swap):
    - `contextPctInFlight = true` set BEFORE the async closure fires; cleared in `finally` inside the closure — same ordering as the current `.finally(() => { contextPctInFlight = false; })`.
    - All four guards preserved at the top of the callback: `stopped || readyState !== OPEN`, `!sshConn`, `contextPctInFlight`, and the post-await `stopped || readyState !== OPEN` re-check before ws.send (this is the "connection tore down while we were awaiting SSH" guard — matches the current `if (stopped || ws.readyState !== WebSocket.OPEN) return;` at line 3449 inside the old .then).
    - The `output` capture MUST happen every tick regardless of the JSONL path outcome — the plan-pending logic downstream depends on it. If capture-pane throws, treat `output` as empty string (silent) and let the JSONL-only pct flow through; isPlanPending("") will simply report false and plan-pending will emit null, matching the current silent-on-scrape-failure behavior at line 3623.
    - Do NOT modify or delete `parseContextPct` import, `context-pct-parser.ts`, or its test file.
    - Do NOT change `CONTEXT_PCT_INTERVAL_MS`, `captureCmd`, timer variable names, or the teardown at line 3115.
    - The `ws.send({type: "context_pct", pct})` message shape MUST be byte-identical — frontend is untouched.

    OPTIONAL micro-optimization to consider but NOT to implement in this pass (write a one-line TODO comment in the callback noting it exists): once JSONL is confirmed working in production, the capture-pane call could be skipped in the pct-only path when plan-pending is disabled. Not this pass — keep behavior symmetric while validating the swap.

    After the edit, run the full backend build (not just `tsc --noEmit`) per the constraint "backend-only change → pre-push typecheck MUST be `npm run build:backend && npm run build`". Then run the FULL test suite — `npx vitest run` must exit 0 with zero failures. Any regression in claude-session-server tests, context-pct-parser tests, or elsewhere blocks this task.
  </behavior>
  <action>
    Apply the swap described in `<behavior>` to `src/backend/claude-session/claude-session-server.ts` — one import at the top, one setInterval callback body replaced, everything else byte-identical. Then run typecheck (both backend + frontend build steps per patch-#154 rule) and the full test suite. Fix any failures before marking done. Do NOT commit yet — the commit is Task 4.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npm run build:backend 2>&1 | tail -10 && npm run build 2>&1 | tail -10 && npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <done>Backend build: zero TS errors. Frontend build: zero TS errors. `npx vitest run` exits 0 with zero failures. `grep -n "readContextPctFromJsonl" src/backend/claude-session/claude-session-server.ts` returns 2 lines (the import + the call in the callback). `grep -n "parseContextPct" src/backend/claude-session/claude-session-server.ts` still returns the same lines as before (import + fallback call) — parser NOT removed. `wc -l src/backend/claude-session/context-pct-parser.ts` unchanged from pre-plan value (125 lines). `git status` shows exactly 3 modified/new files: the new helper, the new test, the server edit.</done>
</task>

<task type="auto">
  <name>Task 4: Atomic commit + write SUMMARY</name>
  <files>.planning/quick/260808-11l-context-pct-from-jsonl-not-scrape/260808-11l-SUMMARY.md</files>
  <action>
    ONE atomic commit on the current branch (`feat/tab-title-from-tmux` — Ashley's fleet rule: no worktrees, work in main tree). Stage exactly the 3 changed files by name (no `git add -A`, no `git add .`):

    - `src/backend/claude-session/context-pct-from-jsonl.ts` (new)
    - `src/backend/claude-session/context-pct-from-jsonl.test.ts` (new)
    - `src/backend/claude-session/claude-session-server.ts` (modified)

    Plus the SUMMARY file (added in the same commit so history stays clean):
    - `.planning/quick/260808-11l-context-pct-from-jsonl-not-scrape/260808-11l-SUMMARY.md` (new)

    Commit message shape (subject + body, via heredoc):
    ```
    feat(context-pct): read from JSONL session file instead of tmux capture-pane

    Pane-scrape context-% failed on Ashley's mobile PWA when the browser
    collapsed the pane to ~2 chars wide — the statusline pct chars survived
    but the AND-anchored bar glyphs (█/░) were truncated so parseContextPct
    returned null and the meter froze.

    New helper readContextPctFromJsonl tails 10KB of state.currentSessionFile
    (already resolved by discoverClaudeSession), reverse-scans for the last
    assistant turn with usage, sums input+cache_creation+cache_read, applies
    the same 16.5% autocompact-buffer normalization the statusline hook uses,
    and returns the displayed integer. Matches CC's rendered number within
    ±1 (5/8 exact + 3/8 off-by-1 across 8 live Opus-4.7 sessions 2026-08-08 —
    sub-1% precision, well below the 80% context-watch nudge threshold).

    Poll callback in claude-session-server.ts now: JSONL first (when
    state.currentSessionFile is set), tmux-scrape fallback (parseContextPct
    unchanged, still used when no sessionFile or JSONL returns null — e.g.
    fresh session with no assistant turn yet). Frontend WS contract
    unchanged ({type: "context_pct", pct} at 3s cadence).

    context-pct-parser.ts intentionally preserved as fallback path.

    Full vitest green. NOT pushed / NOT deployed — awaiting Ashley greenlight.
    ```

    SUMMARY.md content: use the standard summary template. Include:
    - What changed (3 files + one-line each)
    - Why (mobile PWA narrow-pane bar-glyph truncation → frozen meter)
    - Empirical validation reference (2026-08-08 tina, session e222905c, sum=198542 → displayed=24)
    - Test count delta (8 new cases added; full-suite total pre → post)
    - Explicit note: NOT pushed, NOT deployed, awaiting greenlight
    - Follow-ups: monitor for one full 24h cycle on Ashley's fleet; if JSONL path proves stable, consider retiring context-pct-parser.ts in a follow-up (out of scope for this task per the "don't modify parser" constraint)

    Do NOT push. Do NOT run `npm run build && docker compose up`. Do NOT deploy. Ashley greenlights ship separately.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && git log -1 --stat --format="%s%n%n%b" 2>&1 | head -40 && git status 2>&1 | head -10</automated>
  </verify>
  <done>Single commit on `feat/tab-title-from-tmux` containing exactly the 3 code files + SUMMARY.md. `git status` reports a clean tree (no uncommitted / untracked files). SUMMARY.md written to the plan directory. No push, no build-and-deploy, no worktree created.</done>
</task>

</tasks>

<verification>
End-to-end verification of the fix (post-plan, before ship — Ashley will run these when she greenlights):

1. `npx vitest run src/backend/claude-session/context-pct-from-jsonl.test.ts` → 8/8 pass.
2. `npx vitest run src/backend/claude-session/context-pct-parser.test.ts` → all pre-existing cases pass (parser untouched).
3. `npx vitest run` → full suite green, zero failures (per constraint).
4. `npm run build:backend && npm run build` → both exit 0 (per patch #154 backend TS coverage rule).
5. `grep -c "readContextPctFromJsonl" src/backend/claude-session/claude-session-server.ts | grep -v '^#'` → 2 (import + call site).
6. `grep -c "parseContextPct" src/backend/claude-session/claude-session-server.ts | grep -v '^#'` → same count as before the plan (import + fallback call — parser still present).
7. Manual smoke on Ashley's fleet after ship (out of plan scope): open pretty-view on mobile PWA, resize to narrow pane width, meter should keep updating (was: frozen at last-known value; now: updates every 3s from JSONL).
</verification>

<success_criteria>
- 3 code files landed in one atomic commit on `feat/tab-title-from-tmux`: new helper, new test, server swap. Plus SUMMARY.md in the plan dir.
- Helper `readContextPctFromJsonl` compiles and its 8 tests all pass.
- `context-pct-parser.ts` and its test file byte-identical to pre-plan state (fallback preserved).
- Server callback preserves all guards (stopped/readyState/sshConn/contextPctInFlight) and the post-await re-check before ws.send.
- Frontend WS contract byte-identical ({type: "context_pct", pct} at 3s cadence).
- `npm run build:backend && npm run build` both exit 0.
- `npx vitest run` exits 0 with zero failures.
- Not pushed, not built-for-deploy, not deployed — commit is the terminal action.
</success_criteria>

<output>
Create `.planning/quick/260808-11l-context-pct-from-jsonl-not-scrape/260808-11l-SUMMARY.md` when done (as part of Task 4's commit).
</output>
