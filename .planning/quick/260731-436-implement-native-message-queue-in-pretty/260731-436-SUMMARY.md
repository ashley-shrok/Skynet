---
task: 260731-436
subsystem: pretty-view/compose
tags: [bounty, message-queue, persistence, voice, tdd]
key-files:
  created: []
  modified:
    - src/backend/database/db/schema.ts
    - src/backend/database/db/index.ts
    - src/backend/database/routes/compose-drafts.ts
    - src/backend/database/routes/compose-drafts.test.ts
    - src/ui/api/compose-drafts-api.ts
    - src/ui/features/pretty-view/ComposeBox.tsx
    - src/ui/features/pretty-view/ComposeBox.test.tsx
decisions:
  - slot textarea renders ABOVE primary textarea in DOM (queue-slot stack between Row 1 and Row 2)
  - slot index 0 in getAllByRole("textbox") = first slot, not primary
  - act() wrapping required for fake-timer tests in React 18
  - queueSlots optional on PUT (omit preserves existing column; pass overrides)
  - two ON CONFLICT branches: with/without queue_slots in SET clause
metrics:
  duration: ~45min
  completed: 2026-07-31
  tasks_completed: 3
  files_changed: 7
---

# Quick 260731-436: Native Message Queue in Pretty View

Per-pane message queue stack wired end-to-end: schema, route, client, ComposeBox UI, full persistence (debounce autosave + localStorage mirror + pagehide keepalive + 10s retry), per-slot voice mic, and 30 new passing tests (20 backend + 10 frontend).

## What Was Built

### Task 1 (Backend) — commit `9c324a8`

**Schema extension** (`schema.ts`): Added `queueSlots: text("queue_slots").notNull().default("[]")` to the `composeDrafts` Drizzle table.

**Migration** (`db/index.ts`): Added `queue_slots TEXT NOT NULL DEFAULT '[]'` to `CREATE TABLE IF NOT EXISTS compose_drafts` and `addColumnIfNotExists("compose_drafts", "queue_slots", "TEXT NOT NULL DEFAULT '[]'")` in `migrateSchema()`.

**Route** (`compose-drafts.ts`): Full rewrite exporting `handleGetDraft`, `handlePutDraft`, `parseQueueSlotsFromStorage`, `validateQueueSlotsFromRequest`. Two raw SQL branches in PUT: with `queueSlots` (ON CONFLICT updates `queue_slots = excluded.queue_slots`) and without (body-only, preserves existing column).

**Tests** (`compose-drafts.test.ts`): 20 tests — BQ 1-11 (handler behavior), PARSE 1-5, VALIDATE 1-4. In-memory Map mock simulates SQLite upsert with `_isQueueSlotsUpsert` flag on mocked drizzle sql template result.

### Task 2 (Frontend) — commit `fe1bc28`

**API types** (`compose-drafts-api.ts`): `ComposeDraft` interface with `queueSlots`, `putComposeDraft` and `flushComposeDraftKeepalive` accept optional `queueSlots`.

**ComposeBox** (`ComposeBox.tsx`):
- `queueSlots` state + `latestQueueSlotsRef` mirror (stale-closure guard)
- `micTarget` state for routing voice to correct slot
- `dirtyQueueSlotsRef` alongside `dirtyBodyRef` (persistence dirty tracking)
- `makeSlotId()` using `crypto.randomUUID()` with fallback
- `flushDirty()` extended: includes queueSlots in every PUT; localStorage extended to `{body, queueSlots}`
- `scheduleAutosave(nextBody, nextSlots?)` marks both dirty, writes extended LS payload
- Mount effect: hydrates `queueSlots` from server response; LS fallback handles legacy bare-string entries
- `clearAfterSend()`: does NOT clear queueSlots (slot sends clear individually); persists `{body: "", queueSlots: currentSlots}`
- `handleQueueSlotSend(slotId)`: D-50 newline collapse, COMPOSE-04 hard-lock, removes slot on success, keeps + sets errorMessage on failure
- Target-aware voice handlers: `handleVoiceAppend(target)`, `handleVoiceSend(target)`, `beginRecord(target)`
- Plus button in Row 1 aux group
- Queue-slot stack: each slot has Textarea (controlled), X delete button (absolute left-1 bottom-0.5), Send button (absolute right-1 bottom-0.5, disabled when empty), MicButton (right-11 bottom-0.5 when idle), RecordingControls (when recording)
- Primary RecordingControls guarded by `micTarget === "primary"`

**Tests** (`ComposeBox.test.tsx`): 10 new QS tests (QS 1-10).

### Task 3 (Verification)

- `npm run build:backend` → exit 0
- `npm run build` → exit 0
- `npm test -- --run` → 917 passed | 6 skipped | 0 failed (up from 887 baseline; 30 new tests)
- `grep -r "Add queued message textarea" dist/` → found in `Terminal-BN3i1wZE.js`
- `grep -r "queue_slots" dist/` → found in backend and frontend bundles

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Slot textarea DOM order was opposite to test assumption**
- **Found during:** Task 2 test debugging (QS 4-8 failures)
- **Issue:** Tests assumed `getAllByRole("textbox")[last]` = slot textarea. In the DOM, the queue-slot stack renders ABOVE Row 2 (between Row 1 and Row 2), so slot = index 0 and primary = last index. Tests were typing into the primary textarea instead of the slot.
- **Fix:** Changed all slot textarea references from `allTextareas[allTextareas.length - 1]` to `allTextareas[0]`. Updated comments.
- **Files modified:** `ComposeBox.test.tsx`
- **Commit:** `fe1bc28`

**2. [Rule 1 - Bug] TypeScript narrowing error in compose-drafts.ts**
- **Found during:** Task 1 backend implementation
- **Issue:** `Property 'error' does not exist on type union` when accessing `validation.error` after discriminant check
- **Fix:** Cast to specific union member: `(validation as { ok: false; error: string }).error`
- **Files modified:** `compose-drafts.ts`
- **Commit:** `9c324a8`

**3. [Rule 3 - Blocker] React 18 act() wrapping required for fake-timer tests**
- **Found during:** Task 2 test debugging (QS 4-8 still failing after index fix)
- **Issue:** With `vi.useFakeTimers()`, `fireEvent` doesn't automatically flush React state updates in @testing-library/react v16
- **Fix:** Wrapped `fireEvent.click(plusBtn)`, `fireEvent.change(slotTextarea)`, and `fireEvent.click(slotSendBtn)` in `await act(async () => {...})`
- **Files modified:** `ComposeBox.test.tsx`
- **Commit:** `fe1bc28`

## Self-Check

- [x] `src/backend/database/db/schema.ts` modified (queueSlots column)
- [x] `src/backend/database/routes/compose-drafts.ts` exists and exports handlers
- [x] `src/backend/database/routes/compose-drafts.test.ts` exists with 20 tests
- [x] `src/ui/api/compose-drafts-api.ts` has ComposeDraft with queueSlots
- [x] `src/ui/features/pretty-view/ComposeBox.tsx` has Plus button + slot stack
- [x] `src/ui/features/pretty-view/ComposeBox.test.tsx` has 10 new QS tests
- [x] Commits: `9c324a8` (backend), `fe1bc28` (frontend) both exist
- [x] 917 tests pass, 0 fail
- [x] Both builds exit 0
- [x] Queue marker found in dist

## Self-Check: PASSED
