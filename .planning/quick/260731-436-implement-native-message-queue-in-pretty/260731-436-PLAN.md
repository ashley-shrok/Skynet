---
phase: 260731-436-implement-native-message-queue-in-pretty
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/database/db/schema.ts
  - src/backend/database/db/index.ts
  - src/backend/database/routes/compose-drafts.ts
  - src/backend/database/routes/compose-drafts.test.ts
  - src/ui/api/compose-drafts-api.ts
  - src/ui/features/pretty-view/ComposeBox.tsx
  - src/ui/features/pretty-view/ComposeBox.test.tsx
autonomous: true
requirements:
  - message-queue-in-pretty-view
tags:
  - pretty-view
  - compose-box
  - message-queue
  - persistence

must_haves:
  truths:
    - "Clicking the Plus (+) button in the ComposeBox Row 1 aux-button row appends a new empty queue-slot textarea below any existing slots, stacked above Row 2 (primary compose bar)."
    - "Typing into a queue slot updates that slot's text; slots are independent of each other and of the primary."
    - "Each queue slot's X (delete) button removes only that slot."
    - "Clicking a queue slot's Send button routes the slot's text through the same onSend(text) prop the primary uses; on onSend===true the slot disappears; on onSend===false the slot persists and the ComposeBox errorMessage surfaces (mirroring primary handleSend)."
    - "A queue slot's Send button is disabled when slot.text.trim() === '' (mirrors primary)."
    - "Clicking a queue slot's mic button sets micTarget to that slot's id, calls voice.start() synchronously in the tap handler (D-16-02), and RecordingControls render ONLY on that slot; transcript from voice.endAppend/endSend routes to that slot; mic Send routes through the same onSend(text) path and clears the slot on success."
    - "Queue slots persist across page reloads and device switches: hydrated on mount from GET /compose-drafts, autosaved via 400ms debounce PUT, flushed on blur, sent keepalive on pagehide/visibilitychange, retried by the 10s dirty loop, AND mirrored to localStorage under the same key namespace as body (extended LS payload {body, queueSlots})."
    - "compose_drafts table has a queue_slots TEXT column, NOT NULL DEFAULT '[]', added via addColumnIfNotExists in migrateSchema() for existing installs and included in the CREATE TABLE IF NOT EXISTS for fresh installs."
    - "GET /compose-drafts returns { body, queueSlots } — queueSlots defaults to [] on missing row or JSON parse failure."
    - "PUT /compose-drafts accepts queueSlots; on invalid shape (not an array of {id: string, text: string}) responds 400; otherwise persists the JSON-encoded array."
    - "npm run build:backend and npm run build both exit 0."
    - "npm test -- --run has 0 new failures relative to the pre-change baseline (887 passed / 6 skipped / 0 failed across 79 files per patch #220)."
  artifacts:
    - path: "src/backend/database/db/schema.ts"
      provides: "composeDrafts Drizzle definition includes queueSlots column"
      contains: "queueSlots"
    - path: "src/backend/database/db/index.ts"
      provides: "CREATE TABLE and addColumnIfNotExists for queue_slots"
      contains: "queue_slots"
    - path: "src/backend/database/routes/compose-drafts.ts"
      provides: "GET/PUT extended for queueSlots with validation"
      contains: "queueSlots"
    - path: "src/backend/database/routes/compose-drafts.test.ts"
      provides: "backend tests: GET default, GET populated, PUT persists, PUT invalid → 400"
      contains: "queueSlots"
    - path: "src/ui/api/compose-drafts-api.ts"
      provides: "ComposeDraft wire type includes queueSlots; putComposeDraft accepts queueSlots; flushComposeDraftKeepalive sends queueSlots"
      contains: "queueSlots"
    - path: "src/ui/features/pretty-view/ComposeBox.tsx"
      provides: "Plus button + queue-slot stack + per-slot send/delete/mic + full persistence wiring for queueSlots"
      contains: "queueSlots"
    - path: "src/ui/features/pretty-view/ComposeBox.test.tsx"
      provides: "frontend tests covering all queue-slot behaviors listed in CONTEXT §Tests"
      contains: "queueSlots"
  key_links:
    - from: "src/ui/features/pretty-view/ComposeBox.tsx"
      to: "src/ui/api/compose-drafts-api.ts"
      via: "getComposeDraft / putComposeDraft / flushComposeDraftKeepalive now round-trip queueSlots"
      pattern: "queueSlots"
    - from: "src/backend/database/routes/compose-drafts.ts"
      to: "compose_drafts.queue_slots column"
      via: "SQL upsert reads/writes JSON-encoded queue_slots alongside body"
      pattern: "queue_slots"
    - from: "src/ui/features/pretty-view/ComposeBox.tsx (queue-slot Send button + mic Send)"
      to: "onSend(text) prop"
      via: "same handleSend routing (D-50 newline collapse, COMPOSE-04 hard-lock, errorMessage-on-failure)"
      pattern: "onSend"
---

<objective>
Implement the native message-queue feature in `src/ui/features/pretty-view/ComposeBox.tsx` per bounty `message-queue-in-pretty-view`. Ship the full form: real send wiring, per-slot mic support, server-side persistence, tests. Extends the existing `compose_drafts` table with a `queue_slots` TEXT column (JSON-encoded array of `{id, text}`) and threads `queueSlots` through the full persistence stack (backend route validation, wire types, hydrate-on-mount, 400ms debounced autosave, onBlur flush, pagehide/visibilitychange keepalive, 10s retry loop, localStorage mirror). Design is LOCKED in CONTEXT.md — do not revisit decisions.

Purpose: Deliver the bounty exactly as validated in-app via scratch rounds. This IS the shipped form — no scaffolding, no URL toggles, no gates.
Output: One or more commits landing schema + backend route + wire types + ComposeBox integration + tests. Code motion only — no push, no build-and-deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260731-436-implement-native-message-queue-in-pretty/260731-436-CONTEXT.md
@src/backend/database/db/schema.ts
@src/backend/database/db/index.ts
@src/backend/database/routes/compose-drafts.ts
@src/ui/api/compose-drafts-api.ts
@src/ui/features/pretty-view/ComposeBox.tsx
@src/ui/features/pretty-view/ComposeBox.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend — extend compose_drafts with queue_slots (schema + route + tests)</name>
  <files>
    src/backend/database/db/schema.ts,
    src/backend/database/db/index.ts,
    src/backend/database/routes/compose-drafts.ts,
    src/backend/database/routes/compose-drafts.test.ts
  </files>
  <behavior>
    Backend contract for queueSlots per CONTEXT §Tests (backend):
    - GET /compose-drafts?hostId=X (no existing row) → 200, body: { body: "", queueSlots: [] }.
    - GET /compose-drafts?hostId=X after PUT with queueSlots [{id:"a",text:"hi"},{id:"b",text:"bye"}] → 200, body: { body: "...", queueSlots: [{id:"a",text:"hi"},{id:"b",text:"bye"}] }.
    - PUT /compose-drafts with body {hostId, tmuxSession, body, queueSlots: [{id:"x",text:"t"}]} → 204; subsequent GET reflects the array.
    - PUT with queueSlots that is NOT an array → 400 with error message.
    - PUT with queueSlots array item missing id or text (or non-string) → 400.
    - GET when a corrupt row has invalid JSON in queue_slots column → 200 with queueSlots: [] (defensive parse, no crash).
    - Existing body-only PUT (no queueSlots in request) still works and does not clobber a previously-persisted queueSlots array (route must handle queueSlots as OPTIONAL on PUT, defaulting to preserve-existing OR to empty per LOCKED decision — see action).
  </behavior>
  <action>
    Implements bounty `message-queue-in-pretty-view` (LOCKED design in CONTEXT.md §Implementation approach items 1–2).

    Follow TDD: write/extend tests in `src/backend/database/routes/compose-drafts.test.ts` FIRST (RED), then implement. Mirror the existing test-harness style used in that folder (e.g. `voice.test.ts`, `user-preferences.test.ts`, `debug.test.ts`) — supertest-against-express-router, in-memory or temp SQLite per that harness's convention. If `compose-drafts.test.ts` does not yet exist, create it using the closest neighbor (`user-preferences.test.ts`) as the scaffold model.

    (1) `src/backend/database/db/schema.ts` — add `queueSlots: text("queue_slots").notNull().default("[]")` to the `composeDrafts` sqliteTable definition (right after `body`). Import type-safe helpers already present in file; do NOT introduce a new Drizzle helper.

    (2) `src/backend/database/db/index.ts` —
      - In the `CREATE TABLE IF NOT EXISTS compose_drafts (...)` block (around line 497), add `queue_slots TEXT NOT NULL DEFAULT '[]',` after the `body` line so fresh installs get the column.
      - In `migrateSchema()` (around line 670), add `addColumnIfNotExists("compose_drafts", "queue_slots", "TEXT NOT NULL DEFAULT '[]'");` so existing installs pick up the column. Place it grouped near any related compose_drafts entries; if none exist, add a new section-comment above.

    (3) `src/backend/database/routes/compose-drafts.ts` — extend the GET and PUT handlers:
      - Introduce `parseQueueSlotsFromStorage(raw: string | null | undefined): Array<{id: string; text: string}>` helper that JSON.parses the column; on parse failure OR non-array OR any item lacking string `id`/`text` returns `[]` (never throws). Log-and-swallow on parse error (use `databaseLogger.warn` with `operation: "load_compose_draft_queue_slots_corrupt"`).
      - GET: `select` must also pull `queueSlots: composeDrafts.queueSlots`; return `{ body: row?.body ?? "", queueSlots: parseQueueSlotsFromStorage(row?.queueSlots) }`.
      - PUT: introduce `validateQueueSlotsFromRequest(raw: unknown): { ok: true; value: Array<{id:string;text:string}> } | { ok: false; error: string }`. Rules per LOCKED spec: MUST be an array; each item MUST be a plain object with string `id` AND string `text`; empty array is valid. Reject with 400 `{ error: "invalid queueSlots shape" }` when validation fails.
      - PUT semantics for missing `queueSlots` key: LOCKED — treat "queueSlots omitted from request" as "no change to queueSlots" i.e. do NOT overwrite the existing column value. Implement by branching the upsert SQL: when validated queueSlots is present, include `queue_slots = excluded.queue_slots` in the ON CONFLICT DO UPDATE SET clause; when absent, omit it (so ON CONFLICT preserves the existing column). For the INSERT branch (new row), default to `'[]'` when omitted. This preserves backward-compat with any body-only PUT paths.
      - When queueSlots is present and validated, `JSON.stringify(value)` before binding into the SQL.

    (4) `src/backend/database/routes/compose-drafts.test.ts` — cover all `<behavior>` cases above. Do NOT weaken the shape validator to make tests pass; if a test fails, the implementation is wrong, not the test.

    Keep the raw ON CONFLICT SQL style already used in the file (per the comment on line 100 about drizzle-orm onConflictDoUpdate typing). Do NOT introduce a new SQL builder or switch to drizzle's upsert helper.

    Do NOT create files under `src/backend/database/migrations/` — that folder does not exist in this repo; CONTEXT.md's reference to it is superseded by the actual pattern in `db/index.ts` (inline `CREATE TABLE IF NOT EXISTS` + `addColumnIfNotExists` in `migrateSchema()`). This is the migration primitive this codebase uses.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npm test -- --run src/backend/database/routes/compose-drafts.test.ts &amp;&amp; npm run build:backend</automated>
  </verify>
  <done>
    All new backend tests pass, existing compose_drafts tests still pass, `npm run build:backend` exits 0, `grep -n "queue_slots" src/backend/database/db/index.ts src/backend/database/db/schema.ts src/backend/database/routes/compose-drafts.ts` returns hits in all three files.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Frontend — wire types + ComposeBox Plus button, queue-slot stack, per-slot send/delete/mic, full persistence extension (+ tests)</name>
  <files>
    src/ui/api/compose-drafts-api.ts,
    src/ui/features/pretty-view/ComposeBox.tsx,
    src/ui/features/pretty-view/ComposeBox.test.tsx
  </files>
  <behavior>
    Frontend contract per CONTEXT §Tests (frontend) — extend `ComposeBox.test.tsx` (do NOT create a new file; add a `describe("queue slots", () => {...})` block):
    - Add slot: clicking the Plus button appends a queue slot with empty text; the slot's textarea is rendered above the primary Row 2.
    - Edit slot: typing into a queue slot's textarea updates that slot's `text` in state (verify by re-render + accessible value).
    - Delete slot: clicking the slot's X button removes only that slot (other slots and primary body unchanged).
    - Send slot success: with a stub `onSend` returning `true`, clicking the slot's Send button calls `onSend` with the slot's text (post D-50 newline collapse), then the slot is removed from the DOM.
    - Send slot failure: with a stub `onSend` returning `false`, the slot remains and the ComposeBox errorMessage surfaces (same errorMessage element the primary handleSend uses).
    - Send slot disabled when empty: `slot.text.trim() === ""` → Send button has `disabled` attribute.
    - Mic per-slot wiring: clicking the mic button on a specific slot causes `voice.start` to be called (mocked), that slot's RecordingControls to render, and the primary's MicButton/RecordingControls to NOT render for that slot's target. Simulating a `voice.endSend` return routes the transcript through the same onSend path, clearing the slot on success.
    - Persistence round-trip: mock `getComposeDraft` to resolve with `{ body: "", queueSlots: [{id:"a",text:"seeded"}] }` on mount → the seeded slot appears. Mutate (add/edit/delete/send), advance the debounce timer past 400ms, assert `putComposeDraft` was called with the current queueSlots array. On unmount / pagehide, `flushComposeDraftKeepalive` receives the current queueSlots.
    - localStorage mirror: after a successful autosave, `localStorage.getItem(composeDraftLsKey(...))` returns the extended payload `{body, queueSlots}` (JSON-stringified). On mount, if server seed differs from LS payload, LS is used and a debounced save is scheduled (matches existing patch #119 behavior for body).

    Follow TDD: write RED tests in ComposeBox.test.tsx first, then implement in ComposeBox.tsx. Test mocks should follow the patterns already used in ComposeBox.test.tsx and ComposeBox.voice.test.tsx (mock `compose-drafts-api`, mock voice hook, use `vi.useFakeTimers()` for the 400ms debounce).
  </behavior>
  <action>
    Implements bounty `message-queue-in-pretty-view` (LOCKED design in CONTEXT.md §Implementation approach items 3–4).

    (A) `src/ui/api/compose-drafts-api.ts`:
      - Change `ComposeDraft` interface to `{ body: string; queueSlots: Array<{id: string; text: string}> }`.
      - `getComposeDraft`: parse `response.data?.queueSlots` — if it's an array of `{id: string, text: string}` use it, otherwise default to `[]` (client-side defensive parse mirrors server-side; belt-and-suspenders).
      - `putComposeDraft(hostId, tmuxSession, body, queueSlots)`: extend signature to accept queueSlots, include in the PUT body.
      - `flushComposeDraftKeepalive(hostId, tmuxSession, body, queueSlots)`: extend signature identically, include in the JSON.stringify payload.
      - Update the JSDoc block at top of file to note the queueSlots addition (per bounty `message-queue-in-pretty-view`).

    (B) `src/ui/features/pretty-view/ComposeBox.tsx` — LOCKED design from CONTEXT §Implementation approach item 4:
      - Import `Plus` from `lucide-react` (add to existing lucide-react import line).
      - Add state near the existing `text`/`errorMessage` state: `const [queueSlots, setQueueSlots] = useState&lt;Array&lt;{id: string; text: string}&gt;&gt;([])` and `const [micTarget, setMicTarget] = useState&lt;"primary" | string&gt;("primary")`.
      - Helper `makeSlotId()` returning a unique short id (use `crypto.randomUUID()` if available, fallback to `Date.now().toString(36) + Math.random().toString(36).slice(2)` — pick one; document the choice with a one-line comment).
      - Plus button: add to the Row 1 aux-button row (leftmost — insert BEFORE the existing ListPlus button block around line ~1473). Same styling template as the neighboring aux buttons (warm-neutral gradient Glass treatment, `size-4` icon, `max-md:size-12 [&amp;_svg]:max-md:size-6` mobile bump per patch #165). `title="Add queued message textarea"`, `aria-label="Add queued message textarea"`. `onClick={() => setQueueSlots(prev => [...prev, {id: makeSlotId(), text: ""}])}`.
      - Queue-slot stack: render a new `<div className="flex flex-col gap-2">...</div>` BETWEEN the closing `</div>` of Row 1 and the opening of Row 2. Map over `queueSlots`; per slot render a Textarea styled to match primary (`bg-[rgba(10,12,20,0.5)]!` etc — copy the primary Textarea's className), plus positioned children:
        * Delete X at `absolute left-1 bottom-0.5` (lucide `X` icon, `onClick={() => setQueueSlots(prev => prev.filter(s => s.id !== slot.id))}`, `aria-label="Delete queued message"`).
        * MicButton at `right-11 bottom-0.5` when `voice.state === "idle" && micTarget !== slot.id` OR when the slot is idle-target-eligible. Show ONLY the SAME conditions the primary uses, but scoped: mic hidden while `voice.state === "recording"` AND `micTarget === slot.id`.
        * RecordingControls at `right-1 bottom-0.5` when `voice.state === "recording" && micTarget === slot.id` (mutex: hides both MicButton and Send while owning the slot).
        * Send button at `right-1 bottom-0.5` when NOT showing RecordingControls; `disabled={slot.text.trim() === ""}`.
      - `handleQueueSlotSend(slotId)`:
        * Read the slot's current text; apply the SAME newline collapse used by primary handleSend (per D-50 — inspect existing handleSend around line 795 for the exact transform; reuse it, do NOT duplicate the logic — extract a small helper `collapseNewlinesForSend(s: string): string` if not already present, and call it from both sites).
        * Call `onSend(payload)`; if `true` → `setQueueSlots(prev => prev.filter(s => s.id !== slotId))`; if `false` → keep the slot and set `errorMessage` to the SAME message primary handleSend surfaces on onSend failure.
        * Mirror the COMPOSE-04 hard-lock behavior primary handleSend uses (if primary gates via a ref/state, the queue-slot path must respect the same gate).
      - Voice handlers become target-aware. Extend/rename the existing `handleVoiceAppend` / `handleVoiceSend` to accept a `target: "primary" | string` argument (default `"primary"` to keep existing primary call sites working). Route the transcript:
        * `target === "primary"` → existing behavior (append to `text` on append; call `handleSend(glued)` on send).
        * `target === slotId` → append: update the slot's text via `setQueueSlots(prev => prev.map(s => s.id === target ? {...s, text: s.text + glued} : s))`. Send: call `handleQueueSlotSend` with the glued payload (route through the SAME onSend path).
      - Slot mic tap handler `beginRecord(target)`: `setMicTarget(target); voice.start();` — plain function, NO `await` before `voice.start()` (D-16-02 iOS Safari constraint; `setMicTarget` is a synchronous React setState — no microtask boundary — safe to precede `voice.start()`).
      - Persistence — extend EVERY existing site that reads/writes body to also read/write queueSlots:
        * Hydrate on mount: current `getComposeDraft(hostId, tmuxSessionKey).then(...)` around line ~516 — seed `setQueueSlots(response.queueSlots ?? [])` in the same success handler.
        * Debounced 400ms autosave: current `putComposeDraft(hostId, tmuxSessionKey, body)` call around line ~462 — pass `queueSlots` as fourth arg. Dependency array of the effect that schedules the autosave must include `queueSlots` so slot mutations trigger a save.
        * Introduce a `dirtyQueueSlotsRef` mirroring `dirtyBodyRef` (or extend the existing dirty-tracking to a single `dirtyRef: {body?: string; queueSlots?: [...]}` object — pick whichever fits the current shape cleanest; the rule is queue-slot mutations count as "dirty" for the 10s retry loop).
        * onBlur flush: if the code has a per-textarea onBlur that force-flushes, extend it (or add a shared flush that runs on primary blur AND on any slot blur).
        * pagehide/visibilitychange keepalive: current `flushComposeDraftKeepalive(hostId, tmuxSessionKey, body)` calls around lines ~584/~600 — pass `queueSlots` as fourth arg. Use the `queueSlotsRef` mirror pattern (mirror `queueSlots` into a ref so the pagehide handler reads the latest value without stale-closure issues; mirror `text` uses this pattern already).
        * 10s retry loop: current `setInterval(...)` around line ~625 — retry both body AND queueSlots when dirty. If the existing loop only checks one dirty flag, extend it to check both and PUT the merged payload.
        * localStorage mirror (patch #119): extend the LS payload from a bare string body to a JSON object `{body: string, queueSlots: [...]}`. Change `composeDraftLsKey` usage points:
          - Successful autosave (line ~466): `localStorage.setItem(key, JSON.stringify({body, queueSlots}))`.
          - Pre-save best-effort mirror (line ~487): same shape.
          - On-clear (line ~656 `localStorage.removeItem`): unchanged (clears everything).
          - Hydrate-compare (lines ~533/~543): parse the LS string as JSON. If parse fails, treat as legacy (assume the string IS the body, queueSlots is []). Compare BOTH fields against server seed; if either differs, use LS and schedule an autosave. Add a one-line comment noting the legacy fallback covers pre-bounty LS entries.
        * Successful primary-send clear of `body` (existing behavior around line ~656): DO NOT clear queueSlots; queue-slot sends clear THAT slot individually (which the autosave then persists).

    (C) `src/ui/features/pretty-view/ComposeBox.test.tsx`: add a `describe("queue slots (bounty: message-queue-in-pretty-view)", () => {...})` block covering every case in `<behavior>`. Reuse the existing render helper / mock setup already used at the top of the file. Mock `compose-drafts-api` for hydrate and autosave assertions. Use `vi.useFakeTimers()` and `vi.advanceTimersByTime(400)` for debounce assertions.

    Do NOT introduce a "v1" / "static" / "hardcoded" version of any of this. LOCKED design is what ships.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npm test -- --run src/ui/features/pretty-view/ComposeBox.test.tsx src/ui/features/pretty-view/ComposeBox.voice.test.tsx &amp;&amp; npm run build</automated>
  </verify>
  <done>
    All new frontend tests pass; existing ComposeBox tests (including `ComposeBox.voice.test.tsx`, `ComposeBox.aside-morph.test.tsx`, `ComposeBox.aside-props.test.tsx`, `ComposeBox.recycle-disable.test.tsx`) still pass; `npm run build` exits 0; grep of built assets under `dist/` (or the frontend build output dir this project uses) for `"Add queued message textarea"` returns at least one hit (corroborates the string landed).
  </done>
</task>

<task type="auto">
  <name>Task 3: Full-suite verification and grep corroboration</name>
  <files>
    (verification only — no code changes)
  </files>
  <action>
    Run the full verification per CONTEXT §Verification. If any check fails, return to Task 1 or Task 2 to fix — do NOT paper over test failures by weakening assertions.

    Steps:
    1. `cd /home/ubuntu/skynet && npm run build:backend` — must exit 0.
    2. `cd /home/ubuntu/skynet && npm run build` — must exit 0.
    3. `cd /home/ubuntu/skynet && npm test -- --run` — baseline was 887 passed / 6 skipped / 0 failed across 79 files (patch #220). New totals should be: (887 + N_new_tests) passed / 6 skipped / 0 failed / (79 or 79+1 if a new compose-drafts.test.ts file was created) files. Zero pre-existing failures acceptable ONLY if git-diffing the failing file shows the failure was pre-existing on the base commit; otherwise fix.
    4. Grep corroboration (built assets):
       - `grep -r "queue_slots" dist/ 2>/dev/null | head` — expect at least one hit (backend build output).
       - `grep -r "Add queued message textarea" dist/ 2>/dev/null | head` — expect at least one hit (frontend build output).
       If the build output directory in this repo is not `dist/`, use the correct one (check `package.json` build scripts / `vite.config.*` / `tsconfig.*` `outDir`).
    5. `git status` — confirm no files outside the whitelist in `files_modified` frontmatter are modified. If ANY file under `~/.claude/identities/tina/` or `skynet-patches.md` shows up, revert those changes immediately (out-of-scope per CONTEXT §Constraints).

    Do NOT push, do NOT `docker compose up`, do NOT deploy. Code motion only.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npm run build:backend &amp;&amp; npm run build &amp;&amp; npm test -- --run 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    Both build commands exit 0. Full test run reports 0 failed. Grep of built assets shows `queue_slots` and `Add queued message textarea` present. `git status` shows only files in the plan's `files_modified` list as modified.
  </done>
</task>

</tasks>

<verification>
- `npm run build:backend` exits 0.
- `npm run build` exits 0.
- `npm test -- --run` reports 0 failed, ≥ 887 passed (baseline maintained + new tests added).
- `grep -c "queue_slots" src/backend/database/db/index.ts src/backend/database/db/schema.ts src/backend/database/routes/compose-drafts.ts` returns non-zero for all three.
- `grep -c "queueSlots" src/ui/api/compose-drafts-api.ts src/ui/features/pretty-view/ComposeBox.tsx src/ui/features/pretty-view/ComposeBox.test.tsx src/backend/database/routes/compose-drafts.test.ts` returns non-zero for all four.
- `git status --short` shows ONLY paths in the frontmatter `files_modified` list.
</verification>

<success_criteria>
- All must-have truths in frontmatter are demonstrable.
- Every LOCKED decision in CONTEXT.md §Implementation approach items 1–4 is implemented as specified — no scope reduction, no "v1", no "static for now".
- Persistence round-trips queueSlots across page reloads via server (compose_drafts.queue_slots) AND localStorage (extended {body, queueSlots} payload with legacy-string fallback).
- Per-slot mic wiring routes recording to exactly the tapped slot (micTarget state) with no `await` before `voice.start()` (D-16-02).
- Zero files modified outside `~/skynet`; zero pushes/builds-and-deploys performed.
</success_criteria>

<output>
Create `.planning/quick/260731-436-implement-native-message-queue-in-pretty/260731-436-SUMMARY.md` when done, following the summary template.
</output>
