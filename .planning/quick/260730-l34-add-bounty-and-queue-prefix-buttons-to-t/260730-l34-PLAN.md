---
phase: quick-260730-l34
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/ComposeBox.tsx
  - src/ui/features/pretty-view/ComposeBox.test.tsx
  - src/ui/features/pretty-view/ComposeBox.aside-morph.test.tsx
autonomous: true
requirements:
  - BOUNTY-QUEUE-01
user_setup: []

must_haves:
  truths:
    - "Ashley sees a Target-icon button in the ComposeBox aux row, positioned after Lightbulb and before Hourglass"
    - "Ashley sees a ListPlus-icon button in the ComposeBox aux row, positioned after Target and before Hourglass"
    - "Clicking Target with text in the textarea dispatches onSend with payload '/bounty <text>' (single space between prefix and text) and clears the textarea"
    - "Clicking ListPlus with text in the textarea dispatches onSend with payload '/queue <text>' (single space between prefix and text) and clears the textarea"
    - "Both new buttons render disabled when textarea is empty (text.trim() === '')"
    - "Both new buttons render disabled when asideActive === true (matches aux-row aside-morph disable contract)"
    - "Hovering Target shows tooltip 'Send with /bounty prefix'; hovering ListPlus shows tooltip 'Send with /queue prefix'"
    - "Full vitest suite still passes (73 files, 801 tests baseline from prior quick task 260730-jes)"
    - "npx tsc --noEmit is clean"
  artifacts:
    - path: "src/ui/features/pretty-view/ComposeBox.tsx"
      provides: "Two new aux-row prefix buttons (Target = /bounty, ListPlus = /queue) between Lightbulb and Hourglass"
      contains: "Send with /bounty prefix"
    - path: "src/ui/features/pretty-view/ComposeBox.tsx"
      provides: "ListPlus button with /queue prefix wiring"
      contains: "Send with /queue prefix"
    - path: "src/ui/features/pretty-view/ComposeBox.test.tsx"
      provides: "Payload dispatch tests for the two new prefix buttons"
      contains: "/bounty"
    - path: "src/ui/features/pretty-view/ComposeBox.aside-morph.test.tsx"
      provides: "asideActive disable coverage for the two new prefix buttons (matches Task 1 pattern)"
      contains: "bounty"
  key_links:
    - from: "Target button onClick"
      to: "handleSend"
      via: "overridePayload argument at ComposeBox.tsx:795"
      pattern: "handleSend\\(\"/bounty "
    - from: "ListPlus button onClick"
      to: "handleSend"
      via: "overridePayload argument at ComposeBox.tsx:795"
      pattern: "handleSend\\(\"/queue "
    - from: "lucide-react import"
      to: "new button icons"
      via: "named imports on line 2"
      pattern: "ListPlus.*Target|Target.*ListPlus"
---

<objective>
Add two new ComposeBox aux-row buttons that send the current textarea contents prefixed with a slash-command. Target icon fires `/bounty <text>`; ListPlus icon fires `/queue <text>`. Both wire through the existing `handleSend(overridePayload)` seam (ComposeBox.tsx:795), so D-50 newline→space collapse, COMPOSE-04 hard-lock (no optimistic bubble), and attachment branching all apply automatically. Positioned after Lightbulb, before Hourglass, clustering visually with the other prefix-send affordances (Reset, ThumbsUp, Lightbulb).

Purpose: Close bounty #7 of the pinned-slate working batch. Ashley wants first-class one-click access to two fleet-wide slash commands — `/bounty` (file a bounty at target agent) and `/queue` (file a task in the target agent's harness task list) — without typing the prefix each time. Ships as patch #204 in the pinned-slate stack. NO variant gate: this is a positive addition, not a UI restructure, so straight to unconditional default.

Output: Two Button elements inserted in ComposeBox.tsx aux row between Lightbulb and Hourglass, matching the ThumbsUp/Lightbulb warm-neutral Glass treatment. Two new lucide-react imports (`Target`, `ListPlus`). Test coverage split across `ComposeBox.test.tsx` (payload dispatch + empty-text disable) and `ComposeBox.aside-morph.test.tsx` (asideActive disable — matches Task 1 pattern for ThumbsUp/Hourglass).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/ui/features/pretty-view/ComposeBox.tsx
@src/ui/features/pretty-view/ComposeBox.test.tsx
@src/ui/features/pretty-view/ComposeBox.aside-morph.test.tsx

Key ComposeBox.tsx locations (verified during planning):
- Line 2: `import { Hourglass, Lightbulb, Paperclip, RefreshCw, RotateCcw, Square, ThumbsUp, X } from "lucide-react";` — add `Target` and `ListPlus` here.
- Line 795: `function handleSend(overridePayload?: string)` — the seam. Passing a string here bypasses the current `text` state but keeps every other guard (queue cancel, D-50 collapse, attachment branch, COMPOSE-04 hard lock). Perfect for prefix-and-send.
- Lines 1449-1471: existing Lightbulb button — mirror this structure exactly (size, variant, className, mobile size-bump). Insert new buttons AFTER Lightbulb's closing `</Button>` on line 1471.
- Line 1472 (`{/* Patch #84: Queue button ...`): the Hourglass button's leading comment. New buttons must precede this comment.
- Lines 1199-1204: existing `Send with /id reset prefix` button — precedent for the "Send with /X prefix" aria-label naming convention.
- Line 1423: `onClick={() => { onGoodToGo?.(); handleQuickSend("let's go"); }}` — reference for onClick pattern (note handleQuickSend sends canned text INDEPENDENT of textarea; the new buttons DIFFER — they prefix the CURRENT textarea contents).

Existing aux-row disable predicate (ThumbsUp line 1424, Lightbulb line 1453, Hourglass line 1487):
  `disabled={canSend === false || asideActive === true || recycleActive === true}`

Standard aux-row className block (copy verbatim, do not restyle):
  `rounded-md cursor-pointer` + `border-white/10` + warm-neutral gradient bg + text-[#e8e4d8] + shadow + hover: variants + `max-md:size-12 [&_svg]:max-md:size-6` mobile bump.

Test conventions found in `ComposeBox.test.tsx`:
  - `screen.getByLabelText(/send 'let's go'/i)` for label lookup
  - `expect(onSend).toHaveBeenCalledWith("hi")` for payload assertion
  - `expect((btn as HTMLButtonElement).disabled).toBe(true|false)` for disable state
  - `fireEvent.click(...)` for button activation
  - `localStorage.clear()` in `beforeEach` (patch #129 test-hygiene)

Test conventions found in `ComposeBox.aside-morph.test.tsx`:
  - Discrete `describe` block per Task with numbered `Task 1 Test N` names
  - Renders with `asideActive: true` and asserts button `.disabled === true`
  - Complementary "stays enabled when asideActive is undefined" backward-compat test

Naming collision to comment on (per task spec) but NOT to solve:
  The Hourglass button is ALSO called "queue" internally (queue-until-idle, patch #84). The new ListPlus `/queue` is a fleet-wide agent-side skill (files a task in target agent's harness task list), semantically different from Hourglass's local queue-until-idle. Disambiguation lives in distinct icons + tooltips; a rename is deliberately OUT of scope for this task. Add a 3-4 line comment above the ListPlus button explaining the collision so future readers don't collapse them.

Decision (planner discretion): The task spec calls out `text.trim() === "" OR asideActive === true` as the disable rule. The codebase-wide aux-row pattern also includes `canSend === false` (transport health) and `recycleActive === true` (session-recycle overlay, patch 260729-j8l). These are ORTHOGONAL to the spec's rule — they're transport/lifecycle safety, not scope reduction. The final predicate MUST be:
  `disabled={text.trim() === "" || canSend === false || asideActive === true || recycleActive === true}`
The spec doesn't preclude these; it's silent on them. Following the established pattern for every other WS-side-effecting aux button is the safer default. If the executor sees a reason to drop `canSend`/`recycleActive`, STOP and ask — do not silently reduce.

Deploy state (from STATE.md and task spec): pinned-slate stack rides on `feat/tab-title-from-tmux`. Stop at commit boundary — NO push, NO docker build, NO deploy. Two atomic commits (impl + tests) matching the 260730-jes / 260730-2nl pattern from prior quick tasks.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Target (/bounty) and ListPlus (/queue) buttons to ComposeBox aux row</name>
  <files>src/ui/features/pretty-view/ComposeBox.tsx</files>
  <action>
Extend the lucide-react named import on line 2 to include `Target` and `ListPlus` (alphabetize with the existing list; the current order is roughly alphabetical). Insert TWO new Button elements between the Lightbulb button's closing `</Button>` (line 1471) and the leading `{/* Patch #84: Queue button ...` comment (line 1472). Order: Target FIRST, then ListPlus (per spec: after Lightbulb, before Hourglass; Target for /bounty, ListPlus for /queue).

For BOTH buttons, mirror the Lightbulb button (lines 1449-1471) structure verbatim EXCEPT for these fields:
  - `onClick`: Target → `() => handleSend("/bounty " + text)`; ListPlus → `() => handleSend("/queue " + text)`. NOTE: single space between prefix and text. This uses the `handleSend(overridePayload)` seam at line 795, which internally trims, collapses newlines to spaces (D-50), applies COMPOSE-04 hard lock, and clears the textarea on success. The current `text` state variable is in scope at the render site (already used by other buttons via `text.trim()`).
  - `disabled`: `text.trim() === "" || canSend === false || asideActive === true || recycleActive === true`. See planner-discretion note in `<context>` — spec mandates the first two clauses; the last two match the established aux-row pattern for every other WS-side-effecting button (ThumbsUp/Lightbulb/Hourglass). Do NOT drop `canSend` or `recycleActive` without asking.
  - `aria-label` and `title`: Target → `"Send with /bounty prefix"`; ListPlus → `"Send with /queue prefix"` (mirrors the existing `Send with /id reset prefix` convention at line 1203).
  - Icon child: Target → `<Target className="size-4" />`; ListPlus → `<ListPlus className="size-4" />` (matches Lightbulb's `<Lightbulb className="size-4" />` sizing).

Copy the Lightbulb button's className block VERBATIM (rounded-md cursor-pointer + border-white/10 + warm-neutral bg gradient + text-[#e8e4d8] + shadow + hover variants + `max-md:size-12 [&_svg]:max-md:size-6` mobile bump). No restyle; VISUAL-08 HARD LOCK applies — these are quiet warm-neutral aux buttons, not saturated attention grabs.

Add a leading block comment above the Target button explaining: (a) it's a prefix-send button that fires the current textarea contents with a `/bounty ` prepend via the handleSend override seam; (b) semantically clusters with the Reset (line 1199) and future ListPlus buttons as "prefix-send" affordances distinct from ThumbsUp/Lightbulb's canned-text quick-replies.

Add a leading block comment above the ListPlus button ACKNOWLEDGING the naming collision with Hourglass (which is also called "queue" internally, patch #84). Content: Hourglass = local queue-until-idle; ListPlus /queue = fleet-wide agent-side skill (files a task in target agent's harness task list). Distinct icons + tooltips are the disambiguation; a rename is deliberately out of scope. Reference this task by name (260730-l34) so the collision context is git-blame-recoverable.

Do NOT alter any other button, the handleSend function, or the disable predicates on existing buttons. Do NOT alter the textarea. Do NOT touch PrettyView.tsx or any parent — this is a pure additive change inside ComposeBox.tsx.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30 &amp;&amp; grep -q "Send with /bounty prefix" src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; grep -q "Send with /queue prefix" src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; grep -q 'handleSend("/bounty "' src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; grep -q 'handleSend("/queue "' src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; grep -E "^import .*Target.*ListPlus|^import .*ListPlus.*Target" src/ui/features/pretty-view/ComposeBox.tsx | grep -q "lucide-react"</automated>
  </verify>
  <done>
    - `npx tsc --noEmit` exits 0.
    - Both new aria-labels (`Send with /bounty prefix`, `Send with /queue prefix`) are present in ComposeBox.tsx.
    - Both handleSend prefix calls (`handleSend("/bounty " + text)`, `handleSend("/queue " + text)`) are present with the exact single-space payload shape.
    - lucide-react import line contains both `Target` and `ListPlus`.
    - Target button appears BEFORE ListPlus button in source order (line number of Target &lt; line number of ListPlus).
    - Both new buttons appear AFTER the Lightbulb `</Button>` and BEFORE the `{/* Patch #84: Queue button` comment.
    - No other button, no handleSend body, no textarea disabled predicate was modified (`git diff --stat` shows only ComposeBox.tsx changed with a small line-count delta ~40-55 lines added).
  </done>
</task>

<task type="auto">
  <name>Task 2: Append payload + empty-text disable tests to ComposeBox.test.tsx and asideActive disable tests to ComposeBox.aside-morph.test.tsx</name>
  <files>src/ui/features/pretty-view/ComposeBox.test.tsx, src/ui/features/pretty-view/ComposeBox.aside-morph.test.tsx</files>
  <action>
Split tests across the two files following the existing convention (payload/behavior tests → `ComposeBox.test.tsx`; asideActive disable tests → `ComposeBox.aside-morph.test.tsx`, matching the Task 1 Test 4/Test 5 pattern that already covers ThumbsUp and Hourglass).

In `src/ui/features/pretty-view/ComposeBox.test.tsx`, append a new `describe` block at the END of the file titled `"ComposeBox — quick 260730-l34: /bounty and /queue prefix buttons"`. Include `beforeEach` with `vi.clearAllMocks()` + `localStorage.clear()` (matches the file's existing hygiene pattern). Add these four tests:

  1. `"renders Target (bounty) button with aria-label 'Send with /bounty prefix'"` — render ComposeBox with `baseProps({ canSend: true })`; assert `screen.getByLabelText(/send with \/bounty prefix/i)` is truthy.
  2. `"renders ListPlus (queue) button with aria-label 'Send with /queue prefix'"` — same setup; assert `screen.getByLabelText(/send with \/queue prefix/i)` is truthy.
  3. `"bounty button click with text present calls onSend with '/bounty <text>' payload"` — const `onSend = vi.fn(() => true)`; render with `baseProps({ onSend, canSend: true })`; use `fireEvent.change` on the textarea to set text to `"hello"`; `fireEvent.click(screen.getByLabelText(/send with \/bounty prefix/i))`; assert `expect(onSend).toHaveBeenCalledWith("/bounty hello")`. Also assert the textarea's `.value === ""` after the click (COMPOSE-04 clear-on-success contract).
  4. `"queue button click with text present calls onSend with '/queue <text>' payload"` — mirror of test 3 but with `handleSend("/queue " + text)` producing `"/queue hello"`. Same clear-on-success assertion.
  5. `"bounty and queue buttons are disabled when textarea is empty"` — render with `baseProps({ canSend: true })`; assert both buttons' `.disabled === true` when text is untouched (empty).
  6. `"bounty and queue buttons become enabled when text is present"` — render, type into textarea, then assert both `.disabled === false`.

For tests 3 and 4, use the same textarea-lookup pattern already in the file: `screen.getByRole("textbox")` or `screen.getByPlaceholderText(/message/i)` (whichever matches the file's existing convention — grep the file for `getByPlaceholderText\|getByRole..textbox` and mirror). For `fireEvent.change`, use `target: { value: "hello" }`.

In `src/ui/features/pretty-view/ComposeBox.aside-morph.test.tsx`, add two new `it(...)` blocks INSIDE the existing `describe("ComposeBox — Phase 14 Wave 4 aside morph (Task 1: aux button disable)", ...)` block, matching the numbering scheme (`Task 1 Test 7`, `Task 1 Test 8`). These MUST also set a non-empty text — otherwise the `text.trim() === ""` clause would ALSO disable the button and the test wouldn't be proving the asideActive clause. Use the file's existing `baseProps({ asideActive: true })` + `fireEvent.change` on the textarea to set text, THEN assert `.disabled === true`.

  7. `"Task 1 Test 7: bounty (/bounty) button becomes disabled when asideActive=true even with text present"` — render `baseProps({ asideActive: true, canSend: true })`; type "hello" into textarea; assert `screen.getByLabelText(/send with \/bounty prefix/i).disabled === true`.
  8. `"Task 1 Test 8: queue (/queue) button becomes disabled when asideActive=true even with text present"` — same pattern, ListPlus button.

Do NOT edit any existing tests. Do NOT alter `baseProps` in either file. Do NOT modify the vi.mock('@/api/compose-drafts-api', ...) block.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/ComposeBox.test.tsx src/ui/features/pretty-view/ComposeBox.aside-morph.test.tsx 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    - `npx vitest run` on both test files exits 0.
    - The new describe block `"ComposeBox — quick 260730-l34: /bounty and /queue prefix buttons"` reports 6 passing tests.
    - The aside-morph `Task 1` describe block reports 2 additional passing tests (Task 1 Test 7 and Task 1 Test 8), for a total of 8 passing tests in that block (was 6).
    - The full ComposeBox test suite (all `ComposeBox*.test.tsx` files) runs green.
    - No existing test regressed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Ashley confirms full-suite verification and stops at commit boundary</name>
  <what-built>
Two new ComposeBox aux-row buttons: Target icon (`/bounty` prefix) and ListPlus icon (`/queue` prefix), positioned between Lightbulb and Hourglass. Both send the current textarea contents prefixed with a slash-command via the existing `handleSend(overridePayload)` seam. Disabled when empty text, when asideActive, when canSend is false, or during session recycle. Full test coverage split across `ComposeBox.test.tsx` (payload + empty-text disable, 6 new tests) and `ComposeBox.aside-morph.test.tsx` (asideActive disable, 2 new tests matching Task 1 pattern).

Naming-collision comment added above the ListPlus button acknowledging Hourglass's internal "queue" name (patch #84 = local queue-until-idle; /queue slash = fleet-wide agent-side skill). No rename attempted here — out of scope per spec.
  </what-built>
  <how-to-verify>
    1. Run `npx tsc --noEmit` from repo root — must exit 0.
    2. Run `npx vitest run` (full suite) from repo root — must show 73+ files, 803+ tests passing, 0 failures (baseline from 260730-jes was 801 passing; expect +8 new tests from Task 2).
    3. (Optional visual check via `npm run dev`) Open the pretty-view ComposeBox and confirm:
       - Target icon appears between the Lightbulb and Hourglass icons in the aux row.
       - ListPlus icon appears between Target and Hourglass.
       - Both buttons are visually indistinguishable from Lightbulb/Hourglass (same warm-neutral Glass treatment, same size).
       - Hovering shows tooltips `"Send with /bounty prefix"` and `"Send with /queue prefix"`.
       - Both are visibly disabled (dimmed cursor-not-allowed) when the textarea is empty.
       - Typing "hi there" into the textarea enables both buttons.
       - Clicking Target sends `/bounty hi there` to the attached session; clicking ListPlus sends `/queue hi there`. Textarea clears on both.
    4. Confirm two atomic commits on `feat/tab-title-from-tmux` — one for impl (Task 1), one for tests (Task 2) — following the 260730-jes / 260730-2nl commit-pair pattern.
    5. Confirm STOP at commit boundary: NO `git push`, NO `docker compose up`, NO deploy. This ships as patch #204 in the pinned-slate stack alongside #198–#203; the full slate deploys together when Ashley greenlights the batch.
  </how-to-verify>
  <resume-signal>Type "approved" to close the task, or describe issues to iterate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→ComposeBox | Textarea input (already validated / newline-collapsed by handleSend D-50 policy) |
| ComposeBox→onSend | Payload dispatched to WS-side session-file bridge (COMPOSE-04 hard lock: no optimistic local render) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-l34-01 | Tampering | prefix payload concatenation | mitigate | Prefix is a compile-time string literal (`"/bounty "` / `"/queue "`) concatenated with user text; user cannot inject a different prefix. `handleSend` still applies D-50 newline→space collapse and empty-trim guards. No template interpolation, no eval. |
| T-l34-02 | Denial of Service | rapid clicks while transport is down | mitigate | `canSend === false` clause in the disable predicate prevents dispatch when WS is unhealthy; `recycleActive === true` blocks during session recycle. Matches every other aux-row button's contract. |
| T-l34-03 | Information Disclosure | naming collision Hourglass vs /queue button | accept | Two buttons both semantically "queue" but different scopes (local queue-until-idle vs fleet-wide task file). Distinct icons + tooltips are the mitigation; user confusion risk is low and rename is out of scope per spec. Comment in source makes the collision git-blame-recoverable. |
| T-l34-SC | Tampering | npm/pip/cargo installs | N/A | No new packages installed. `Target` and `ListPlus` are additional named imports from the ALREADY-installed `lucide-react` (see line 2 of ComposeBox.tsx). No package.json change, no supply-chain surface. |
</threat_model>

<verification>
Overall phase verification:
- `npx tsc --noEmit` clean (exit 0).
- `npx vitest run` full suite green — expect ~73 files / ~809 tests passing (baseline 801 + 8 new).
- `grep -c "Send with /bounty prefix" src/ui/features/pretty-view/ComposeBox.tsx` returns 1 (button aria-label + title share the same string, so this grep should match a raw occurrence count — adjust if you also put it in a comment).
- `grep -c "Send with /queue prefix" src/ui/features/pretty-view/ComposeBox.tsx` returns 1.
- `grep -c 'handleSend("/bounty ' src/ui/features/pretty-view/ComposeBox.tsx` returns 1.
- `grep -c 'handleSend("/queue ' src/ui/features/pretty-view/ComposeBox.tsx` returns 1.
- `git diff --stat` shows exactly 3 files changed: ComposeBox.tsx (~40-55 lines added), ComposeBox.test.tsx (~60-80 lines added), ComposeBox.aside-morph.test.tsx (~20-30 lines added).
- Two atomic commits on `feat/tab-title-from-tmux` matching the 260730-jes / 260730-2nl pair pattern.
- Stopped at commit boundary — no push, no docker build, no deploy.
</verification>

<success_criteria>
- Two new prefix-send buttons (Target for /bounty, ListPlus for /queue) render in the ComposeBox aux row between Lightbulb and Hourglass, matching the warm-neutral Glass treatment of Lightbulb/ThumbsUp/Hourglass.
- Clicking each button sends the current textarea contents prefixed with `/bounty ` or `/queue ` respectively, via the existing `handleSend(overridePayload)` seam (so D-50 collapse and COMPOSE-04 hard-lock apply automatically).
- Both buttons are disabled when: textarea is empty, `asideActive === true`, `canSend === false`, or `recycleActive === true`.
- Aria-labels and tooltips read `"Send with /bounty prefix"` and `"Send with /queue prefix"`.
- 8 new tests pass across `ComposeBox.test.tsx` (6) and `ComposeBox.aside-morph.test.tsx` (2). Full vitest suite green.
- `npx tsc --noEmit` clean.
- Naming-collision comment above ListPlus button explains Hourglass/queue-until-idle vs /queue slash-command distinction.
- Two atomic commits on `feat/tab-title-from-tmux`. NO push, NO docker build, NO deploy — closes at commit boundary per fleet rule.
- Closes pinned bounty `~/.claude/identities/tina/bounties/bounty-and-queue-buttons/` in one shot (no variant gate needed for positive additions).
</success_criteria>

<output>
Create `.planning/quick/260730-l34-add-bounty-and-queue-prefix-buttons-to-t/260730-l34-SUMMARY.md` when done, recording:
- Which files changed and how many lines.
- The two commit SHAs on `feat/tab-title-from-tmux`.
- Final vitest count (files / tests passing).
- Confirmation of stop-at-commit-boundary (no push, no deploy).
- Patch number this ships as (#204) and pinned-slate stack position (7th of 7).
</output>
