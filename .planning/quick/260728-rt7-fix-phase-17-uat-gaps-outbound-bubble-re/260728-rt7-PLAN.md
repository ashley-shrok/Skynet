---
phase: quick-260728-rt7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/claude-session/session-file-parser.ts
  - src/backend/claude-session/session-file-parser.test.ts
  - src/backend/claude-session/claude-session-server.ts
  - src/ui/api/claude-session-api.ts
  - src/ui/features/pretty-view/RelayOutboundBubble.tsx
  - src/ui/features/pretty-view/RelayOutboundBubble.test.tsx
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-view/relay-pointer-detect.ts
  - src/ui/features/pretty-view/RelayInboundBubble.test.tsx
  - src/backend/database/routes/relay-pointer.ts
  - src/backend/database/routes/relay-pointer.test.ts
autonomous: true
requirements:
  - UAT-GAP-17-BUG1-OUTBOUND-RENDER
  - UAT-GAP-17-BUG2-POINTER-PATH-SHAPE

must_haves:
  truths:
    - "Outbound bubble in pretty-view renders the actual Bash command (mono block) with room-id header — no ⚠ extraction-failure fallback anywhere in the pipeline."
    - "detectRelayOutbound return shape is exactly { room, rawCommand } — no body, no extractError."
    - "Wire type RelayOutboundEvent and WS emit in claude-session-server.ts carry { room, rawCommand } only."
    - "Frontend POINTER_REGEX matches ~/.claude/identities/<id>/relay-state/messages/<eventid>.txt paths bounded by whitespace (recv.sh preview line format)."
    - "Backend WHITELIST_REGEX in relay-pointer.ts matches the same identity-dir path shape and rejects /tmp/relay-msg-*.txt (no longer valid) plus traversal/metachars."
    - "All vitest suites pass (npm test)."
    - "Backend TS compile clean via npm run build:backend && npm run build."
  artifacts:
    - path: "src/backend/claude-session/session-file-parser.ts"
      provides: "detectRelayOutbound with simplified { room, rawCommand } return shape; body-extraction code deleted"
      contains: "detectRelayOutbound"
    - path: "src/ui/features/pretty-view/RelayOutboundBubble.tsx"
      provides: "Outbound bubble that renders rawCommand as a pre/mono block with room-id header; no extractError branch"
      contains: "rawCommand"
    - path: "src/ui/api/claude-session-api.ts"
      provides: "RelayOutboundEvent wire type with { type, room, rawCommand, eventId, ts } only"
      contains: "RelayOutboundEvent"
    - path: "src/ui/features/pretty-view/relay-pointer-detect.ts"
      provides: "POINTER_REGEX matching identity-dir messages path"
      contains: "identities"
    - path: "src/backend/database/routes/relay-pointer.ts"
      provides: "WHITELIST_REGEX matching identity-dir messages path"
      contains: "identities"
  key_links:
    - from: "src/backend/claude-session/session-file-parser.ts"
      to: "src/backend/claude-session/claude-session-server.ts"
      via: "RelayOutboundMessage type flowed into WS emit payload"
      pattern: "relay_outbound"
    - from: "src/backend/claude-session/claude-session-server.ts"
      to: "src/ui/api/claude-session-api.ts"
      via: "WS relay_outbound frame shape mirrored by RelayOutboundEvent wire type"
      pattern: "RelayOutboundEvent"
    - from: "src/ui/api/claude-session-api.ts"
      to: "src/ui/features/pretty-view/RelayOutboundBubble.tsx"
      via: "Props destructured from RelayOutboundEvent via Pick<>"
      pattern: "RelayOutboundEvent"
    - from: "src/ui/features/pretty-view/PrettyView.tsx"
      to: "src/ui/features/pretty-view/RelayOutboundBubble.tsx"
      via: "JSX props: room, rawCommand"
      pattern: "RelayOutboundBubble"
    - from: "src/ui/features/pretty-view/relay-pointer-detect.ts"
      to: "src/backend/database/routes/relay-pointer.ts"
      via: "Both regexes MUST match the same path shape; client is defence-in-depth, backend is authoritative"
      pattern: "relay-state/messages"
---

<objective>
Close two Phase 17 UAT gaps Ashley surfaced 2026-07-28.

Bug 1 (outbound): Delete the body-extractor in `detectRelayOutbound` (per Ashley's Option D). Render the raw Bash command as the outbound bubble body. Fleet-standard sends use `curl -d "$(jq -n --arg b "$MSG" ...)"` — literal-JSON extraction cannot succeed on those, and no shell-recipe parser is worth building. Faithful record of what happened > guessed body.

Bug 2 (inbound pointer): Swap POINTER_REGEX (frontend detector) and WHITELIST_REGEX (backend SSRF gate) from the obsolete `/tmp/relay-msg-*.txt` shape to the actual recv.sh output shape: `/home/<user>/.claude/identities/<id>/relay-state/messages/<eventid>.txt`. Both regexes must stay in lockstep.

Purpose: Phase 17 through-line ("relay bubbles work in Skynet") is not truly closed until real fleet sends/receives render correctly. Currently every outbound bubble shows ⚠ and every long inbound bubble shows raw pointer text.

Output: Two atomic commits on branch `feat/tab-title-from-tmux`. No push, no docker build, no deploy — Tina handles ship separately per Ashley's rule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@~/.claude/identities/tina/bounties/phase-17-uat-gap-outbound-render-plus-pointer-path/bounty.json

@src/backend/claude-session/session-file-parser.ts
@src/backend/claude-session/session-file-parser.test.ts
@src/backend/claude-session/claude-session-server.ts
@src/ui/api/claude-session-api.ts
@src/ui/features/pretty-view/RelayOutboundBubble.tsx
@src/ui/features/pretty-view/RelayOutboundBubble.test.tsx
@src/ui/features/pretty-view/PrettyView.tsx
@src/ui/features/pretty-view/relay-pointer-detect.ts
@src/ui/features/pretty-view/RelayInboundBubble.tsx
@src/ui/features/pretty-view/RelayInboundBubble.test.tsx
@src/backend/database/routes/relay-pointer.ts
@src/backend/database/routes/relay-pointer.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bug 1 — Delete outbound extractor; render command as bubble body</name>
  <files>
    src/backend/claude-session/session-file-parser.ts,
    src/backend/claude-session/session-file-parser.test.ts,
    src/backend/claude-session/claude-session-server.ts,
    src/ui/api/claude-session-api.ts,
    src/ui/features/pretty-view/RelayOutboundBubble.tsx,
    src/ui/features/pretty-view/RelayOutboundBubble.test.tsx,
    src/ui/features/pretty-view/PrettyView.tsx
  </files>
  <behavior>
    Parser (session-file-parser.ts):
    - `detectRelayOutbound` return type is `{ room: string | null; rawCommand: string } | null` (drop `body`, drop `extractError`).
    - Detection logic UNCHANGED — three-way conjunction (curl + -X PUT + rooms/.../send/m.room.message/... URL) stays byte-for-byte per prototype 6/6 acceptance. Do NOT loosen.
    - Delete the body-extraction block (current lines ~181-197: `dSingle`/`dDouble`/`dArg`/`JSON.parse`).
    - `RelayOutboundMessage` type drops `body` and `extractError` fields.
    - `parseSessionLine` outbound branch (~lines 447-473) drops `body` and `extractError` from the returned object.
    - Add a code comment near detectRelayOutbound noting that a follow-up bounty could add opportunistic body-var grep (MSG=/BODY=/TEXT=) as a "nice-to-have preview above command" — explicitly out of scope for this fix per Ashley 2026-07-28.

    Parser tests (session-file-parser.test.ts):
    - Test 1 (outbound-happy-path, ~L527): drop `expect(parsed.body).toBe("hello")` and `expect(parsed.extractError).toBeNull()`. Keep room, rawCommand, eventId assertions.
    - Test 4 (outbound-extract-fail-shellvar, ~L560): rewrite. Assertion changes to: detection STILL fires (`kind === "relay_outbound"`), room is extracted, `rawCommand === cmd`. Remove all `body`/`extractError` assertions.
    - Test 5 (outbound-extract-fail-dataraw, ~L571): same rewrite as Test 4 — detection fires + rawCommand preserved, no body/extractError assertions.
    - Rename these tests to reflect new semantics — suggested: "Test 4: outbound-shellvar — shell-var -d arg still emits relay_outbound with rawCommand" / "Test 5: outbound-dataraw — --data-raw variant still emits relay_outbound with rawCommand".
    - All other tests (heredoc/grep false-positives, inbound tests, regression) UNCHANGED.

    WS emit (claude-session-server.ts, ~L1248-1270):
    - `relay_outbound` case: WS frame carries `{ type, room, rawCommand, eventId, ts }` only. Delete `body` and `extractError` from the JSON.stringify object.
    - Update the comment block above (~L1249-1254) to reflect new shape — remove ⚠-fallback language, describe as "faithful command record".

    Wire type (claude-session-api.ts, ~L122-144):
    - `RelayOutboundEvent` becomes `{ type: "relay_outbound"; room: string | null; rawCommand: string; eventId: string; ts: number }`.
    - Update the doc comment: strip `body:` and `extractError:` bullets; add note that rawCommand IS the body per Option D 2026-07-28.

    Bubble component (RelayOutboundBubble.tsx):
    - Props type Pick<> drops `body` and `extractError` — becomes `Pick<RelayOutboundEvent, "room" | "rawCommand">`.
    - Delete the `showSource`/`setShowSource` useState (no longer needed — command is always visible).
    - Delete the entire `extractError !== null ? (…) : body !== null ? (…) : null` conditional block.
    - Replace body area with a single always-rendered `<pre>` or `<div>` block:
      - Renders `{rawCommand}` as React children (auto-escaped — same T-17-03-01 security posture).
      - Classes: `whitespace-pre` (preserve newlines) + `overflow-x-auto` (long single lines scroll instead of overflowing the bubble) + `max-h-[24rem] overflow-y-auto` (bounded vertical space).
      - Uses existing mono font stack `font-[JetBrains_Mono_Variable,ui-monospace,monospace]` and the existing dark-panel treatment `bg-black/40 rounded p-2 text-xs`.
    - Header UNCHANGED: `▸ relay send → {room ?? "unknown room"}` (existing 8-char-ish room slug pattern is already what Ashley wants).
    - Footer "via curl" UNCHANGED.
    - Remove the now-unused `useState` and `cn` import lines if genuinely unused after edit (verify — cn is still used elsewhere in file, keep it).

    Bubble tests (RelayOutboundBubble.test.tsx):
    - Test 1: rewrite as "renders room-id header + rawCommand in mono block". Assert command text present, header contains room, wrapper is justify-end. Drop body/extractError props from the render call.
    - Test 2: DELETE the extractError/showSource flow entirely — that state no longer exists.
    - Test 3: keep "room null → unknown room" — remove body/extractError props.
    - Add NEW Test 2: "long command with newlines preserves them" — render with `rawCommand="curl \\\n  -X PUT \\\n  https://…"`, assert the rendered text contains the newline-preserving structure (query by text match of a multi-line snippet, or assert `whitespace-pre` class on the container).
    - Add NEW Test 4: "very long single-line command does not overflow bubble" — render with a 500-char single-line rawCommand, assert the container has `overflow-x-auto` class (structural assertion, since jsdom won't measure layout).

    PrettyView.tsx JSX (~L1200-1206):
    - `<RelayOutboundBubble>` receives `room={m.room}` and `rawCommand={m.rawCommand}` only. Delete `body` and `extractError` props from the JSX.
  </behavior>
  <action>
    Implement the changes described in `<behavior>` in TDD order:

    Step A (RED): Update the parser test file first to reflect the new shape (drop body/extractError assertions from Test 1, 4, 5; rename 4 and 5). Update the RelayOutboundBubble.test.tsx to match the new prop shape (delete old Test 2, rewrite Tests 1/3, add new Tests 2/4). Run `npm test -- session-file-parser.test.ts RelayOutboundBubble.test.tsx` — MUST fail (production code still emits body/extractError, still branches on them).

    Step B (GREEN): Make the production changes:
    1. session-file-parser.ts: shrink RelayOutboundMessage type; shrink detectRelayOutbound return type; delete body-extraction block (~L181-197); shrink parseSessionLine outbound return object (~L464-472); add follow-up-bounty comment.
    2. claude-session-server.ts (~L1248-1270): drop body + extractError from WS emit; update comment.
    3. claude-session-api.ts (~L122-144): shrink RelayOutboundEvent type; update doc comment.
    4. RelayOutboundBubble.tsx: delete useState + extractError branch + showSource button + fallback state; render rawCommand as always-visible pre/mono block with `whitespace-pre overflow-x-auto max-h-[24rem] overflow-y-auto` styling; keep header + footer.
    5. PrettyView.tsx (~L1200-1206): remove body + extractError JSX props from `<RelayOutboundBubble>`.

    Do NOT add opportunistic body-var grep (MSG=/BODY=/TEXT=) — Ashley explicitly deferred it. Just leave a comment stub.

    Step C (VERIFY): Run backend TS gate `npm run build:backend && npm run build` — MUST compile clean. Any leftover reference to `body`/`extractError` in the outbound path will surface as a TS error here. Then run full `npm test` — all tests must pass.

    Step D (COMMIT): Single atomic commit:
    ```
    git add src/backend/claude-session/session-file-parser.ts \
            src/backend/claude-session/session-file-parser.test.ts \
            src/backend/claude-session/claude-session-server.ts \
            src/ui/api/claude-session-api.ts \
            src/ui/features/pretty-view/RelayOutboundBubble.tsx \
            src/ui/features/pretty-view/RelayOutboundBubble.test.tsx \
            src/ui/features/pretty-view/PrettyView.tsx
    ```
    Commit message: `fix(phase-17): render outbound bubble as command block (delete extractor, close UAT Bug 1)`. Body: reference bounty id `3f0369dd-01fc-4e0e-a89c-b45597877e9b` and Ashley's Option D 2026-07-28 decision.

    Do NOT push. Do NOT docker build. Do NOT docker compose up. STOP after commit — Task 2 continues on same branch.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npm test -- --run src/backend/claude-session/session-file-parser.test.ts src/ui/features/pretty-view/RelayOutboundBubble.test.tsx 2>&1 | tail -40 && npm run build:backend 2>&1 | tail -20 && npm run build 2>&1 | tail -20 && grep -n "extractError\|\.body[^A-Za-z]" src/backend/claude-session/session-file-parser.ts src/ui/api/claude-session-api.ts src/ui/features/pretty-view/RelayOutboundBubble.tsx src/ui/features/pretty-view/PrettyView.tsx 2>&1 | grep -v '^#' | grep -vE 'msg\.body|source\.data|resolved\.body|res\.body' || echo "OK: no stray extractError/body refs in outbound path"</automated>
  </verify>
  <done>
    - Parser tests pass with body/extractError assertions removed from Tests 1/4/5.
    - RelayOutboundBubble tests: Test 1 (rawCommand renders), Test 2 (multiline preserved), Test 3 (unknown room), Test 4 (overflow-x-auto class present) all pass. Old extractError/showSource test is deleted.
    - `npm run build:backend && npm run build` both succeed with zero TS errors.
    - Grep confirms zero `extractError` references and no `.body` references in the outbound path (parser, wire type, bubble, PrettyView JSX for outbound).
    - Single commit on branch feat/tab-title-from-tmux with message starting `fix(phase-17): render outbound bubble as command block`.
    - No push, no docker build, no deploy performed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bug 2 — Regex swap on both sides (client detector + backend whitelist) to match recv.sh identity-dir path</name>
  <files>
    src/ui/features/pretty-view/relay-pointer-detect.ts,
    src/ui/features/pretty-view/RelayInboundBubble.test.tsx,
    src/backend/database/routes/relay-pointer.ts,
    src/backend/database/routes/relay-pointer.test.ts
  </files>
  <behavior>
    New path shape (both regexes MUST use the identical inner path pattern):
    ```
    /home/[a-z0-9_-]+/\.claude/identities/[a-z0-9_-]+/relay-state/messages/[A-Za-z0-9_-]+\.txt
    ```
    - Char classes: POSIX-safe user + identity names (lowercase kebab), event-id can include uppercase since Matrix event ids do (see reproducer `_j14UxhqP0NpJXLReeXBR0qPGh04JwNXDGneCrEyarWw`).
    - Rejects `..`, `/` inside components, shell metacharacters — SSRF-safe by construction.
    - Note: character class REMOVES `.` (dot) from the eventId segment vs the old `/tmp/relay-msg-[A-Za-z0-9._-]+\.txt` — recv.sh eventids never contain `.` (they are base64url-like), and dropping dot from the character class strengthens the whitelist. If the executor discovers otherwise while writing tests, restore `.` to the eventId class ONLY (do NOT loosen the identity-name class).

    Backend WHITELIST_REGEX (relay-pointer.ts:33):
    ```typescript
    export const WHITELIST_REGEX =
      /^\/home\/[a-z0-9_-]+\/\.claude\/identities\/[a-z0-9_-]+\/relay-state\/messages\/[A-Za-z0-9_-]+\.txt$/;
    ```
    Anchored `^...$`. Used in isolation on the query-param `path` value, so no surrounding-boundary variance needed.

    Frontend POINTER_REGEX (relay-pointer-detect.ts:30):
    ```typescript
    export const POINTER_REGEX =
      /(?:^|\s)(\/home\/[a-z0-9_-]+\/\.claude\/identities\/[a-z0-9_-]+\/relay-state\/messages\/[A-Za-z0-9_-]+\.txt)(?:\s|$)/;
    ```
    - Preserves the existing `(?:^|\s)(<path>)(?:\s|$)` surrounding-whitespace shape.
    - Em-dash concern check: the recv.sh preview line format is `[long message, N chars — full text at <path> — Read it] «...»`. The `<path>` is bounded by ` — ` on both sides — that is space + em-dash + space. JS `\s` DOES match the ASCII space adjacent to the em-dash, so the existing `\s` boundaries still work. NO regex boundary changes needed. Add a test asserting this exact reproducer string matches.

    Backend tests (relay-pointer.test.ts, ~L156-180):
    - Test 1 (whitelist accept, ~L157): REPLACE `/tmp/relay-msg-abc123.txt` positive cases with the new identity-dir shape:
      - `/home/ubuntu/.claude/identities/molly/relay-state/messages/_j14UxhqP0NpJXLReeXBR0qPGh04JwNXDGneCrEyarWw.txt` (Ashley's reproducer path — MUST match).
      - `/home/thenasty/.claude/identities/tina/relay-state/messages/AbCdEf-_1234.txt`.
    - Test 2 (whitelist reject dot-dot): change payload to `/home/ubuntu/.claude/identities/../../etc/passwd` — MUST still reject.
    - Test 3 (whitelist reject non-tmp / arbitrary path): keep `/etc/passwd`, add `/home/ubuntu/other/file.txt` (missing identities/... segment).
    - Test 4 (whitelist reject wrong suffix): change to `/home/ubuntu/.claude/identities/tina/relay-state/messages/abc.sh` — MUST reject.
    - Add Test 4b (whitelist reject uppercase user): `/home/UBUNTU/.claude/identities/tina/relay-state/messages/abc.txt` — MUST reject (POSIX-safe lowercase enforced).
    - Add Test 4c (whitelist reject path traversal in identity name): `/home/ubuntu/.claude/identities/../evil/relay-state/messages/abc.txt` — MUST reject.
    - Tests 5-7 (unauthorized-host, file-not-found, happy-path) — update the `/tmp/relay-msg-abc.txt` argument in each to the new shape (e.g. `/home/ubuntu/.claude/identities/tina/relay-state/messages/abc.txt`). Behavior assertions UNCHANGED.
    - Any additional tests below L200 that use the old path: update the path arg to the new shape.

    Frontend tests (RelayInboundBubble.test.tsx):
    - Locate any test that constructs a body with `/tmp/relay-msg-*.txt` and update to the new identity-dir path shape.
    - Add a dedicated test named "detectFilePointer matches recv.sh preview line format with em-dash boundaries" that feeds the EXACT reproducer string:
      ```
      "[long message, 1960 chars — full text at /home/ubuntu/.claude/identities/molly/relay-state/messages/_j14UxhqP0NpJXLReeXBR0qPGh04JwNXDGneCrEyarWw.txt — Read it] «Huge. Signal received before I invested…»"
      ```
      Import `detectFilePointer` directly from `./relay-pointer-detect` and assert the returned `pointerPath` equals the identity-dir path (proves the em-dash-bounded space still triggers the `\s` boundary).
    - Old positive cases for `/tmp/relay-msg-*.txt` must be REMOVED (that path is no longer a valid pointer per this fix).

    Comment updates:
    - relay-pointer-detect.ts header comment (~L8): change `/tmp/relay-msg-<id>.txt` example to `~/.claude/identities/<id>/relay-state/messages/<eventid>.txt`. Update "Character class matches plan 17-02's WHITELIST_REGEX" line to reference the new shape.
    - relay-pointer.ts WHITELIST_REGEX comment (~L29-33): change to describe the new identity-dir shape; keep T-17-02-01 tag.
  </behavior>
  <action>
    Implement the changes in TDD order:

    Step A (RED): Update the test files first:
    1. relay-pointer.test.ts: rewrite Tests 1-4 with new path shape; add Tests 4b/4c for stricter negative cases; update path args in Tests 5-7.
    2. RelayInboundBubble.test.tsx: swap any old `/tmp/relay-msg-*.txt` bodies for new identity-dir paths; add the em-dash-boundary reproducer test that imports `detectFilePointer` directly.

    Run `npm test -- --run src/backend/database/routes/relay-pointer.test.ts src/ui/features/pretty-view/RelayInboundBubble.test.tsx` — MUST fail (old regexes still in place).

    Step B (GREEN):
    1. relay-pointer.ts line 33: replace WHITELIST_REGEX with the new shape (exact regex in `<behavior>`).
    2. relay-pointer-detect.ts line 30: replace POINTER_REGEX with the new shape (preserve `(?:^|\s)(...)(?:\s|$)` boundaries).
    3. Update comments in both files per `<behavior>`.

    Step C (VERIFY): `npm test -- --run src/backend/database/routes/relay-pointer.test.ts src/ui/features/pretty-view/RelayInboundBubble.test.tsx` — MUST pass. Then run full `npm test` — no regressions elsewhere. Then `npm run build:backend && npm run build` — MUST compile clean.

    Sanity check with the exact reproducer via a one-liner (belt-and-suspenders):
    ```
    node -e "const {detectFilePointer} = require('./src/ui/features/pretty-view/relay-pointer-detect.ts'); console.log(detectFilePointer('[long message, 1960 chars — full text at /home/ubuntu/.claude/identities/molly/relay-state/messages/_j14UxhqP0NpJXLReeXBR0qPGh04JwNXDGneCrEyarWw.txt — Read it] «...»'))"
    ```
    Skip the node-eval if `.ts` require doesn't resolve without a loader — the vitest test covers it. The vitest test is the authoritative gate.

    Step D (COMMIT): Second atomic commit on same branch:
    ```
    git add src/ui/features/pretty-view/relay-pointer-detect.ts \
            src/ui/features/pretty-view/RelayInboundBubble.test.tsx \
            src/backend/database/routes/relay-pointer.ts \
            src/backend/database/routes/relay-pointer.test.ts
    ```
    Commit message: `fix(phase-17): file-pointer regex matches recv.sh identity-dir path (close UAT Bug 2)`. Body: reference bounty id `3f0369dd-01fc-4e0e-a89c-b45597877e9b`, note both frontend detector and backend whitelist swap in lockstep.

    Do NOT push. Do NOT docker build. Do NOT docker compose up.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npm test -- --run src/backend/database/routes/relay-pointer.test.ts src/ui/features/pretty-view/RelayInboundBubble.test.tsx 2>&1 | tail -40 && npm test 2>&1 | tail -20 && npm run build:backend 2>&1 | tail -20 && npm run build 2>&1 | tail -20 && grep -n "relay-msg" src/ui/features/pretty-view/relay-pointer-detect.ts src/backend/database/routes/relay-pointer.ts | grep -v '^#' | grep -c relay-msg | awk '{if ($1==0) print "OK: no /tmp/relay-msg refs in production code"; else print "FAIL: stale /tmp/relay-msg refs remain"; exit ($1==0)?0:1}'</automated>
  </verify>
  <done>
    - Both regexes swapped to `/^\/home\/[a-z0-9_-]+\/\.claude\/identities\/[a-z0-9_-]+\/relay-state\/messages\/[A-Za-z0-9_-]+\.txt$/` (with the `(?:^|\s)(...)(?:\s|$)` wrapper preserved on the frontend detector).
    - Backend tests: whitelist accepts Ashley's exact reproducer path; rejects traversal, wrong suffix, uppercase user, non-identity paths. Existing UNAUTHORIZED_HOST / FILE_NOT_FOUND / happy-path tests still pass with updated path args.
    - Frontend tests: em-dash-bounded reproducer string yields the correct pointerPath via detectFilePointer.
    - Full `npm test` passes with no regressions.
    - Backend TS gate `npm run build:backend && npm run build` succeeds.
    - Zero `relay-msg` references remain in the production code paths (relay-pointer-detect.ts and relay-pointer.ts) — grep confirms.
    - Second atomic commit on branch feat/tab-title-from-tmux with message starting `fix(phase-17): file-pointer regex matches recv.sh identity-dir path`.
    - No push, no docker build, no deploy performed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code session file → backend parser | Untrusted `tool_use.input.command` string ingested from JSONL and rendered downstream. |
| Frontend WS client → PrettyView render | `relay_outbound.rawCommand` string rendered into DOM — must not evade React auto-escape. |
| Client detector body → backend `/relay-pointer?path=…` | Frontend hands whatever POINTER_REGEX captured to the backend as a query param; backend must independently gate. |
| Backend SSH exec channel | `path` interpolated into `head -c N "${path}"` shell command — regex whitelist is the pre-condition that keeps this safe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-rt7-01 | Information disclosure | `rawCommand` in RelayOutboundBubble | accept | Same disclosure surface as the tmux pane and existing session viewer — Ashley already accepted this in T-17-01-02. Comment carried forward in session-file-parser.ts header. |
| T-rt7-02 | XSS via bubble render | RelayOutboundBubble rawCommand text node | mitigate | Render `{rawCommand}` as React children (NOT `dangerouslySetInnerHTML`); React auto-escapes. Preserve T-17-03-01 posture. Task 1 assertion: no `dangerouslySetInnerHTML` introduced. |
| T-rt7-03 | SSRF via arbitrary path fetch | backend `/relay-pointer` WHITELIST_REGEX | mitigate | Anchored `^...$` regex, char classes reject `..`, `/`, shell metachars. Identity-dir path shape is more restrictive than the previous `/tmp/relay-msg-*.txt` (character class dropped `.` from the eventId segment). T-17-02-01 mitigation carried forward. Test 4c specifically asserts identity-name traversal is rejected. |
| T-rt7-04 | Elevation of privilege (cross-user host access) | backend `/relay-pointer` handler | mitigate | Unchanged — `resolveHostById(hostId, userId)` gate stays in place (Task 2 does not touch it). T-17-02-02 mitigation carried forward. |
| T-rt7-05 | Shell command injection via `path` interpolation | `head -c N "${path}"` in readRelayPointerFile | mitigate | WHITELIST_REGEX char classes guarantee no shell metacharacters can reach the interpolation (same rationale as T-17-02-07). Task 2 verifies with negative tests. |
| T-rt7-06 | Regex-drift: client and backend disagree on valid paths | POINTER_REGEX vs WHITELIST_REGEX | mitigate | Both regexes swapped in the same commit with the identical inner path pattern; must_haves.key_links locks the "must match" invariant; audit at review time. |
</threat_model>

<verification>
Overall phase checks (executor should run before final commit for the plan):

1. `npm test` — full vitest suite passes, no regressions in parser, RelayOutboundBubble, RelayInboundBubble, or relay-pointer suites.
2. `npm run build:backend && npm run build` — both compile clean (project-mandated backend TS gate order per skynet-patches.md #154).
3. Grep audit for stale references:
   - `grep -rn "extractError" src/` → zero hits.
   - `grep -rn "/tmp/relay-msg" src/backend/ src/ui/features/pretty-view/relay-pointer-detect.ts src/backend/database/routes/relay-pointer.ts` → zero hits in production files (test files may keep them as negative-case rejections but this plan expects them removed).
4. Two atomic commits on branch feat/tab-title-from-tmux, in order:
   - `fix(phase-17): render outbound bubble as command block (delete extractor, close UAT Bug 1)`
   - `fix(phase-17): file-pointer regex matches recv.sh identity-dir path (close UAT Bug 2)`
5. NO push, NO docker build, NO docker compose up performed.
</verification>

<success_criteria>
- Task 1 done: extractor deleted end-to-end (parser → wire type → WS emit → bubble → PrettyView JSX); outbound bubble renders rawCommand in a scrollable mono block; all related tests pass; backend + frontend TS compile clean.
- Task 2 done: both POINTER_REGEX and WHITELIST_REGEX swapped to identity-dir path shape; Ashley's exact reproducer string (with em-dash boundaries) matches on the frontend; backend whitelist accepts the same path and rejects traversal/wrong-suffix/uppercase-user variants; all related tests pass; backend + frontend TS compile clean.
- Two atomic commits authored (one per task), on the current branch, no push, no build, no deploy.
- Bounty `3f0369dd-01fc-4e0e-a89c-b45597877e9b` referenced in both commit bodies for provenance.
</success_criteria>

<output>
Create `.planning/quick/260728-rt7-fix-phase-17-uat-gaps-outbound-bubble-re/260728-rt7-SUMMARY.md` when done.
Include:
- Files changed per task (git diff --stat per commit).
- Both commit SHAs and messages.
- Test run summary (pass counts, failed → passing transitions).
- Explicit confirmation: no push, no docker build, no docker compose up performed.
- Callout for Tina/orchestrator: ready to bundle-ship alongside kiro-cli fix + Phase 17 patch #169 pin per bounty timeline item 2.
</output>
