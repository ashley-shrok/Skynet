---
phase: 260722-mp6-patch-126-pwa-safe-area-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/index.css
  - src/ui/AppShell.tsx
  - src/ui/sidebar/ConversationsPanel.tsx
autonomous: true
requirements:
  - PATCH-126-BLEND-BODY-BG
  - PATCH-126-DROP-SHELL-BOTTOM-PAD
  - PATCH-126-SIDEBAR-SCROLL-INSET

must_haves:
  truths:
    - "Top safe-area region on Ashley's iPhone renders Skynet-gray (not browser-default black), seamless with AppShell bg"
    - "Bottom safe-area region on Ashley's iPhone renders Skynet-gray (not browser-default black), seamless with AppShell bg"
    - "Status-bar clock/battery still readable — content pushed below the notch (paddingTop preserved)"
    - "Last item in ConversationsPanel scroll list rests above the iOS home-indicator black-bar region without requiring a pull-up gesture"
    - "Desktop rendering unchanged — env(safe-area-inset-*) resolves to 0"
    - "npx tsc --noEmit passes with no new errors"
    - "git diff shows exactly 3 files touched: src/ui/index.css, src/ui/AppShell.tsx, src/ui/sidebar/ConversationsPanel.tsx"
  artifacts:
    - path: "src/ui/index.css"
      provides: "body bg blended to --background"
      contains: "background-color: var(--background)"
    - path: "src/ui/AppShell.tsx"
      provides: "AppShell outer div with top-only safe-area padding"
      contains: "paddingTop"
    - path: "src/ui/sidebar/ConversationsPanel.tsx"
      provides: "ConversationsPanel scroll container with bottom safe-area inset"
      contains: "pb-[env(safe-area-inset-bottom)]"
  key_links:
    - from: "src/ui/index.css body{}"
      to: ".dark { --background }"
      via: "CSS cascade — body inherits --background from .dark ancestor"
      pattern: "background-color: var\\(--background\\)"
    - from: "src/ui/AppShell.tsx outer div"
      to: "body bg"
      via: "removing paddingBottom lets shell bg extend to viewport bottom edge; body bg is fallback for any residual gap"
      pattern: "paddingTop.*safe-area-inset-top"
---

<objective>
Patch #126: PWA safe-area polish. Three tiny edits that resolve Ashley's UAT feedback on the patch #125 iOS install:
1. Black bars in the top/bottom safe-area regions (body bg leaking through) → blend body bg with Skynet-gray.
2. ConversationsPanel scroll content hiding behind the iOS home indicator → add bottom safe-area inset to the scroll container.

Purpose: Ship the two Ashley-reported polish issues without expanding scope (no root-cause chase for 100dvh-vs-flex, no preemptive padding on other scroll containers, no rebrand).
Output: 3-file diff, commits cleanly, tsc-clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@src/ui/index.css
@src/ui/AppShell.tsx
@src/ui/sidebar/ConversationsPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the three safe-area polish edits</name>
  <files>src/ui/index.css, src/ui/AppShell.tsx, src/ui/sidebar/ConversationsPanel.tsx</files>
  <action>
Make exactly these three edits — no scope expansion, no touching other files, no re-styling unrelated code.

EDIT 1 — src/ui/index.css (body block at lines 39–41):
Add `background-color: var(--background);` to the existing body rule. Final block:
  body {
    overscroll-behavior: none;
    background-color: var(--background);
  }
Rationale: The `.dark` class (defined at line 190) is the standard shadcn dark-mode class typically applied on `<html>` or `<body>` in shadcn setups. `--background` is set inside `.dark` as an `oklch()` value (line 191: `--background: oklch(0.155 0.004 128.73);`) — NOT an H/S/L triplet — so `var(--background)` resolves directly without an `hsl()` wrapper. If after visual verification the body still renders black in dark mode, the `.dark` class is not an ancestor of `<body>`; in that case fall back to hardcoding `background-color: oklch(0.155 0.004 128.73);` in the body rule (Skynet-dark constant matching line 191). Do NOT touch any other CSS rules, tokens, or variants.

EDIT 2 — src/ui/AppShell.tsx (outer div style block at lines 1738–1742):
DROP the `paddingBottom` line. KEEP `height: "100dvh"` and `paddingTop: "max(env(safe-area-inset-top), 0px)"`. Final style block:
  style={{
    height: "100dvh",
    paddingTop: "max(env(safe-area-inset-top), 0px)",
  }}
Rationale per task_shape: status-bar clock/battery need content pushed down (keep paddingTop). The home-indicator region is fine to extend under now that body bg matches (edit 1) and the interior scroll padding (edit 3) keeps interactive content above it. Do NOT touch the className, the `height: "100dvh"` value, the outer fragment, the child components, or add any new style properties.

EDIT 3 — src/ui/sidebar/ConversationsPanel.tsx (scroll container div at line 229):
Append `pb-[env(safe-area-inset-bottom)]` to the existing className. Final line:
  <div className="flex-1 min-h-0 overflow-y-auto pb-[env(safe-area-inset-bottom)]">
Rationale: Desktop no-op (env resolves to 0). iOS pushes last item above home indicator. Do NOT touch other classes on this div, other divs in this file, SessionsPanel, dashboard cards, pretty view, or any other scroll container — Ashley only reported hitting this one, and other containers are explicitly non-goals for this patch.

After the three edits, run `npx tsc --noEmit` from repo root and confirm no new errors. Then `git diff --stat` to confirm exactly 3 files touched.

Do NOT push, build, or deploy — Tina handles that behind Ashley's greenlight per task_shape non-goals. Do NOT touch manifest.webmanifest, index.html head, icons, or anything else.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; test "$(git diff --name-only | sort)" = "$(printf 'src/ui/AppShell.tsx\nsrc/ui/index.css\nsrc/ui/sidebar/ConversationsPanel.tsx')" &amp;&amp; grep -q 'background-color: var(--background)' src/ui/index.css &amp;&amp; ! grep -q 'paddingBottom.*safe-area-inset-bottom' src/ui/AppShell.tsx &amp;&amp; grep -q 'pb-\[env(safe-area-inset-bottom)\]' src/ui/sidebar/ConversationsPanel.tsx</automated>
  </verify>
  <done>
- `npx tsc --noEmit` exits 0 with no new errors
- `git diff --name-only` lists exactly: src/ui/AppShell.tsx, src/ui/index.css, src/ui/sidebar/ConversationsPanel.tsx
- src/ui/index.css body block contains `background-color: var(--background);`
- src/ui/AppShell.tsx outer div style has no `paddingBottom` line; `paddingTop` preserved
- src/ui/sidebar/ConversationsPanel.tsx line 229 div className contains `pb-[env(safe-area-inset-bottom)]`
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean
- `git diff --stat` shows exactly 3 files, small line counts
- Ready to commit as: `feat(pwa): patch #126 — safe-area polish (blend body bg, drop shell bottom pad, sidebar scroll-inset)`
</verification>

<success_criteria>
- All three edits applied precisely as specified
- tsc-clean, 3-file diff
- No scope creep: no root-cause chase for 100dvh-vs-flex, no preemptive safe-area padding on other scroll containers, no touching paddingTop/manifest/index.html/icons, no push/build/deploy
- Tina can commit + deploy behind the 15-min deadman rollback timer once Ashley greenlights
</success_criteria>

<output>
Create `.planning/quick/260722-mp6-patch-126-pwa-safe-area-polish-blend-bod/260722-mp6-SUMMARY.md` when done, capturing:
- The 3 edits made (file + line + before/after snippet)
- tsc + git diff verification results
- Any deviation from the plan (e.g., if body bg fallback to hardcoded oklch was needed)
- Commit message ready for Tina: `feat(pwa): patch #126 — safe-area polish (blend body bg, drop shell bottom pad, sidebar scroll-inset)`
</output>
