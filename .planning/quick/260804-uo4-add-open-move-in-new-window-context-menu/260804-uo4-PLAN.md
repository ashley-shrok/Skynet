---
phase: 260804-uo4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-conversations/PrettyConversationRow.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx
autonomous: true
requirements:
  - UO4-01  # Add "Open in new window" / "Move to new window" context-menu item (label bifurcates on inActiveSet; desktop-only)
  - UO4-02  # Relax the three row-level !isRdp gates so RDP rows get the full context menu
  - UO4-03  # Wire onDeactivate for RDP rows in PrettyConversationsPanel (drop isRdp condition in threading)

must_haves:
  truths:
    - "Ashley desktop-right-clicks any non-RDP row NOT in active-set → a menu item labeled 'Open in new window' renders."
    - "Ashley desktop-right-clicks any non-RDP row IN active-set → the same slot renders 'Move to new window'."
    - "Ashley desktop-right-clicks any RDP row → the context menu opens at all (no more silent !isRdp gate), and 'Open/Move in new window' is present with the same bifurcating label."
    - "Ashley long-presses an RDP row on mobile → the same PrettyConversationContextMenu opens (touch handlers relaxed)."
    - "Clicking 'Move to new window' fires window.open on the encoded workspace URL AND calls onDeactivate — but only when window.open returned a non-null Window handle (popup-blocker safety)."
    - "Clicking 'Open in new window' fires window.open and does NOT call onDeactivate (row was never in active-set)."
    - "The item does NOT render on the mobile variant (variant==='mobile'), for ANY row type."
    - "Deactivating an RDP row via its context menu invokes the same panel handler (handleRowDeactivate) as non-RDP rows — no RDP-specific variant introduced."
  artifacts:
    - path: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx"
      provides: "New items[] entry between Clone and Deactivate; three !isRdp row-level gates dropped; comments updated"
      contains: "Move to new window"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "onDeactivate now threaded for RDP rows in the __rdp__ group render site"
      contains: "onDeactivate={() => handleRowDeactivate(row)}"
    - path: "src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx"
      provides: "New tests covering label bifurcation, popup-blocker safety, RDP menu opens, mobile suppression"
      contains: "Open in new window"
  key_links:
    - from: "PrettyConversationRow.tsx items[] builder"
      to: "src/ui/lib/tab-url.ts specForTab + encodeWorkspaceSpec"
      via: "onClick handler that builds TabSpec from row, encodes workspace {tabs:[spec],activeIndex:0,only:true}, calls window.open('#'+payload, '_blank', 'noopener')"
      pattern: "specForTab.*encodeWorkspaceSpec.*window\\.open"
    - from: "PrettyConversationsPanel.tsx __rdp__ group render"
      to: "handleRowDeactivate(row)"
      via: "onDeactivate prop now threaded on RDP rows"
      pattern: "data-rdp-group.*onDeactivate"
---

<objective>
Add a context-menu item "Open in new window" / "Move to new window" (label bifurcates on inActiveSet) to sidebar rows, applying to ALL row types INCLUDING RDP. To make it reachable on RDP rows, drop the three row-level `!isRdp` gates in PrettyConversationRow.tsx that currently prevent RDP rows from opening the menu at all. Thread `onDeactivate` for RDP rows in PrettyConversationsPanel.tsx so the "Move to new window" click path (which tears down the current row after opening the new window) works uniformly across row types.

Purpose: Ashley needs multi-window screen real estate for parallel sessions on desktop. The new item reuses the exact URL scheme (`#tab=…&active=0&only=1`) already implemented in `src/ui/lib/tab-url.ts`. RDP rows previously had no context menu at all — this quick unlocks the full menu for them while keeping the mobile variant suppression (multi-window is meaningless in a PWA).

Output: Two source files modified (Row + Panel), one test file augmented with 6 new tests. `npx tsc --noEmit` clean and `npm test -- src/ui/features/pretty-conversations/` all pass. No deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/ui/features/pretty-conversations/PrettyConversationRow.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
@src/ui/features/pretty-conversations/PrettyConversationContextMenu.tsx
@src/ui/lib/tab-url.ts
@src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx
@src/ui/state/conversation-store.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add "Open/Move in new window" item + relax !isRdp gates in PrettyConversationRow.tsx</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationRow.tsx</files>
  <behavior>
    Executor MUST write the six new tests in Task 3 (or at least the assertions/skeletons)
    conceptually first, then modify Row + Panel to make them pass. If pragmatic ordering is
    preferred, edit source + tests together, but do NOT mark the task done until every new test in
    Task 3 passes green. Behavior contract locked by locked_scope:
      - Desktop non-mobile, inActiveSet=true, non-RDP → items[] includes an entry with label
        "Move to new window", positioned between Clone and Deactivate.
      - Desktop non-mobile, inActiveSet=false, non-RDP → same slot, label "Open in new window".
      - Desktop non-mobile, RDP row → menu opens at all (was previously suppressed by three
        !isRdp gates), and same label bifurcation applies.
      - Mobile variant (variant==='mobile' / isMobile===true) → item does NOT render (desktop-only).
      - Click handler builds TabSpec via specForTab({type: row.type, host: row.host,
        targetTmuxSession: row.targetTmuxSession}), bails to a no-op if specForTab returns null,
        encodes payload via encodeWorkspaceSpec({tabs: [spec], activeIndex: 0, only: true}),
        opens window.open("#" + payload, "_blank", "noopener"), and ONLY IF the returned window
        handle is non-null AND inActiveSet===true calls onDeactivate?.().
      - Mobile RDP row long-press → menu opens (proves touch handlers relaxed).
  </behavior>
  <action>
Modify `src/ui/features/pretty-conversations/PrettyConversationRow.tsx` in three surgical passes. Do NOT touch PrettyConversationContextMenu.tsx (contract is items[]-agnostic).

Pass A — Relax the three row-level `!isRdp` gates (locked_scope Change 2):

  1. Line ~416: `onContextMenu={!isMobile && !isRdp ? onRowContextMenu : undefined}`
     → `onContextMenu={!isMobile ? onRowContextMenu : undefined}` (drop `&& !isRdp`).
  2. Lines ~417-420: four touch props currently gated on `isMobile && !isRdp`
     (onTouchStart / onTouchMove / onTouchEnd / onTouchCancel) → drop the `&& !isRdp` conjunct
     on ALL FOUR. Long-press on RDP rows must open the same menu.
  3. Inside onTouchStart / onTouchMove / onTouchEnd early-returns (lines ~273, ~296, ~314):
     drop the `if (isRdp || !isMobile) return;` isRdp check — leave only the `!isMobile` gate.
     Rationale: with the JSX prop-level gate relaxed, the handlers now fire on RDP mobile rows;
     the early-return guard was a defense-in-depth mirror that must be relaxed in lockstep or the
     handler bodies immediately no-op and the menu never arms.
  4. Line ~565: `{ctxMenu !== null && !isRdp && (` → `{ctxMenu !== null && (` (drop `&& !isRdp`).

  Update the header comment on the mobile long-press block (lines ~240-243) to say
  "mobile-only" instead of "mobile + non-RDP-only". Update the comment above the items[] builder
  (lines ~561-564, ~577-578, ~594): the "RDP rows never open the menu" claim is now wrong.
  Rewrite to: "RDP rows now open the menu. Items filter by row eligibility: Pin renders for any
  row; Hide/Show only when onToggleHide is provided; Clone only when onClone AND identity resolve
  (RDP rows have no identity → Clone auto-hidden); Deactivate only when inActiveSet && onDeactivate;
  Open/Move in new window renders on desktop for any row where specForTab produces a spec."

Pass B — Add the "Open/Move in new window" items[] entry (locked_scope Change 1):

  In the items[] builder inside the `<PrettyConversationContextMenu items={((): PrettyContextMenuItem[] => { … })()}>` block, insert a NEW push AFTER the Clone push (`if (onClone && identity) items.push(…)`) and BEFORE the Deactivate push (`if (inActiveSet && onDeactivate) items.push(…)`), so the final order is: Pin/Unpin, Hide/Show, Clone, Open/Move-in-new-window, Deactivate.

  The new entry is gated: `if (!isMobile) { const spec = specForTab({ type: row.type, host: row.host, targetTmuxSession: row.targetTmuxSession }); if (spec !== null) { items.push({ label: inActiveSet ? "Move to new window" : "Open in new window", onClick: () => { const payload = encodeWorkspaceSpec({ tabs: [spec], activeIndex: 0, only: true }); const w = window.open("#" + payload, "_blank", "noopener"); if (w !== null && inActiveSet) { onDeactivate?.(); } } }); } }`

  Add `import { specForTab, encodeWorkspaceSpec } from "@/ui/lib/tab-url";` at the top of the file
  (adjust path alias to match existing imports — the file uses `@/lib/utils`, `@/state/...`,
  `@/shell/tabUtils`, `@/features/terminal/session-hue`, so the correct alias is `@/lib/tab-url`).
  Verify by grepping the file: `grep -n "^import" src/ui/features/pretty-conversations/PrettyConversationRow.tsx` to see the existing alias convention; the actual on-disk path is `src/ui/lib/tab-url.ts` and the codebase's `@/` typically maps to `src/ui/`, so the import path is `@/lib/tab-url`.

Pass C — Sanity checks before finishing:

  1. `grep -n "!isRdp" src/ui/features/pretty-conversations/PrettyConversationRow.tsx` — the three
     specific sidebar-row-context-menu / touch / mouse handler gates in the JSX + the three
     handler-body early-returns must be gone. Other `isRdp` uses (className composition on line
     ~372, isAmbient derivation on line ~215, ambient exemption comment, RDP data attribute) MUST
     survive.
  2. `grep -n "Move to new window\|Open in new window" src/ui/features/pretty-conversations/PrettyConversationRow.tsx` — exactly one occurrence of each label string.
  3. Confirm no changes outside PrettyConversationRow.tsx in this task.

Do NOT introduce any new URL parameter. Do NOT ship a mobile variant. Do NOT touch PrettyConversationContextMenu.tsx.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | tee /tmp/uo4-tsc-task1.log; test ! -s /tmp/uo4-tsc-task1.log || grep -qi "no errors\|^$" /tmp/uo4-tsc-task1.log; grep -c "!isRdp" src/ui/features/pretty-conversations/PrettyConversationRow.tsx | (read n; test "$n" = "0" || (echo "Still $n !isRdp gates in PrettyConversationRow.tsx; expected 0 (all six sidebar row-level gates dropped)"; exit 1)); grep -c "Move to new window" src/ui/features/pretty-conversations/PrettyConversationRow.tsx | (read n; test "$n" = "1" || (echo "Expected exactly 1 'Move to new window' occurrence, got $n"; exit 1)); grep -c "Open in new window" src/ui/features/pretty-conversations/PrettyConversationRow.tsx | (read n; test "$n" = "1" || (echo "Expected exactly 1 'Open in new window' occurrence, got $n"; exit 1))</automated>
  </verify>
  <done>
    `npx tsc --noEmit` clean. PrettyConversationRow.tsx compiles. Six `!isRdp` gates (3 JSX + 3 handler early-returns) removed. Import of specForTab + encodeWorkspaceSpec present. Exactly one occurrence of each label string. No changes to PrettyConversationContextMenu.tsx.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread onDeactivate for RDP rows in PrettyConversationsPanel.tsx</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx, src/ui/features/pretty-conversations/PrettyConversationRow.tsx</files>
  <behavior>
    - Panel-level: the __rdp__ group render site (currently omits `onDeactivate`) now threads
      `onDeactivate={() => handleRowDeactivate(row)}` — same handler used by every other tier.
    - No RDP-specific handler introduced. `handleRowDeactivate` at lines ~605-611 already does
      `removeFromActiveSet(row.id)` + optional fleet-id purge + `onDeactivateRow(row)`; verify
      this is safe for RDP rows (row.host is defined for RDP-sentinel rows because they're
      synthesized from Host records; `row.targetTmuxSession === null` for RDP rows so the fleet-id
      purge branch cleanly no-ops). If AppShell's `onDeactivateRow` cannot handle an RDP row via
      the existing tab-close path, adapt the SHARED handler at the panel/AppShell boundary — do
      NOT fork a new one.
    - JSDoc on PrettyConversationRow.tsx line ~126 (`MUST be provided by the panel whenever
      inActiveSet === true && !isRdp`) updated to drop the `&& !isRdp` clause.
  </behavior>
  <action>
Modify `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`:

  1. Locate the __rdp__ group render block (around lines ~899-946, guarded by
     `if (group.hostId === "__rdp__")`). Inside the `group.rows.map((row) => (`
     `<PrettyConversationRowLive ... />`) at approximately lines 931-943, the current props are:
     `key`, `row`, `selected`, `pinned={false}`, `variant`, `onSelect`, `onTogglePin={rdpNoopTogglePin}`, `inActiveSet`, `sessionKey`.
     Add exactly one new prop: `onDeactivate={() => handleRowDeactivate(row)}`.

     Do NOT thread `onToggleHide` (RDP rows are ineligible for hiding — see the note at
     conversation-store.ts line ~537 "NOT eligible for hiding — their id shape (rdp-host::${host.id})
     never..."). Do NOT thread `onClone` (RDP rows have no identity → items[] Clone gate already
     filters it out via `onClone && identity`). Do NOT change `subtitleMode` (RDP rows stay on
     "hostname" default — locked_scope non-goal). Do NOT touch the `rdpNoopTogglePin` wiring.

  2. Verify `handleRowDeactivate(row)` at lines ~605-611 works for RDP rows without modification:
     `removeFromActiveSet(row.id)` — safe for any row id shape (idempotent). The `if (row.host && row.targetTmuxSession)` branch skips the fleet-id purge for RDP rows (targetTmuxSession is null), which is correct. `onDeactivateRow(row)` is delegated to AppShell — if AppShell's implementation cannot handle an RDP-sentinel row shape, the fix is at that boundary layer, NOT here. Run the test suite in Task 3; if AppShell-side breakage surfaces, extend Task 2's file scope to include the AppShell hook, but only if the test explicitly proves it. Do NOT preemptively touch AppShell.

Modify `src/ui/features/pretty-conversations/PrettyConversationRow.tsx`:

  3. Update the JSDoc comment on the `onDeactivate` prop declaration at lines ~124-130. Current
     text: "fired when Ashley clicks the red-tinted Deactivate menu item (desktop right-click OR
     mobile long-press). MUST be provided by the panel whenever inActiveSet === true && !isRdp —
     otherwise the menu item is filtered out at items[] build time."
     New text (drop the `&& !isRdp`): "fired when Ashley clicks the red-tinted Deactivate menu
     item (desktop right-click OR mobile long-press). MUST be provided by the panel whenever
     inActiveSet === true — otherwise the menu item is filtered out at items[] build time. As of
     quick-260804-uo4, RDP rows also receive this prop (RDP context menu is now enabled)."

  4. Also update the tail comment inside `handleRowDeactivate` in
     PrettyConversationsPanel.tsx (lines ~578-604) — the block currently states
     "quick-260727-gm3: fired when Ashley clicks the red-tinted X on an active-set non-RDP row".
     Drop "non-RDP" from that sentence.

Sanity checks before finishing:
  - `grep -n "data-rdp-group=\"true\"" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` — locate the RDP group JSX. The nested `PrettyConversationRowLive` MUST now have `onDeactivate={() => handleRowDeactivate(row)}`.
  - `grep -c "handleRowDeactivate" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` — count should INCREASE by exactly 1 vs pre-change (three existing threading sites — active-set, pinned tier, non-RDP grouped — plus the new RDP site = 4, plus the handler definition itself = 5 total).
  - No other panel changes; specifically, do NOT touch handleRowSelect, handleTogglePin, handleToggleHide, handleRowClone, or any of the four other RowLive render sites.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | tee /tmp/uo4-tsc-task2.log; test ! -s /tmp/uo4-tsc-task2.log || grep -qi "no errors\|^$" /tmp/uo4-tsc-task2.log; awk '/data-rdp-group="true"/,/<\/div>/' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx | grep -c "onDeactivate={() => handleRowDeactivate(row)}" | (read n; test "$n" = "1" || (echo "RDP group block must contain exactly 1 onDeactivate threading line; got $n"; exit 1))</automated>
  </verify>
  <done>
    `npx tsc --noEmit` clean. The __rdp__ group render site now threads `onDeactivate={() => handleRowDeactivate(row)}`. JSDoc on PrettyConversationRow.tsx onDeactivate prop updated to drop `&& !isRdp`. Panel handleRowDeactivate comment updated. No AppShell changes (unless a Task 3 test explicitly forces one — in which case adapt the shared handler at the AppShell/panel boundary, NOT via an RDP-specific variant).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add 6 tests to PrettyConversationRow.test.tsx covering the new item + RDP menu + mobile suppression</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx</files>
  <behavior>
    Six new tests, appended after the existing TL5b test at the tail of the file. All tests use
    the existing `makeRow` / `makeHost` / `makeIdentity` fixture helpers + the existing `currentIdentity`
    per-test override. Follow the file's existing `it("Test <ID>: <sentence>", ...)` naming
    convention — use IDs `UO1` through `UO6` (or `19`-`24` if the file's numeric progression is
    preferred; check the tail for the last numeric ID and pick the pattern consistent with the
    file's existing style). All new tests must run standalone under
    `npm test -- src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx`.

    Tests:

    UO1 — Desktop non-mobile, inActiveSet=true, non-RDP → contextmenu opens a menu containing
      a menuitem labeled /move to new window/i. Clicking it calls a stubbed window.open with
      a URL that starts with "#" and contains "tab=" AND "only=1" AND "active=0". After the
      click, the passed onDeactivate mock was called exactly once.
      Setup: `vi.stubGlobal('open', vi.fn(() => ({} as Window)))` (return non-null Window
      handle so the popup-blocker branch of the handler doesn't short-circuit). Use
      `afterEach(vi.unstubAllGlobals)` to restore.

    UO2 — Desktop non-mobile, inActiveSet=false, non-RDP → menu contains /open in new window/i
      (NOT /move/). Clicking it calls window.open. onDeactivate mock was NOT called (row was
      never in active-set → the inActiveSet branch inside the handler doesn't fire).

    UO3 — Desktop non-mobile, RDP row (rdpHostRow=true, targetTmuxSession=null),
      inActiveSet=true → contextmenu opens the menu AT ALL (proves the row-body-level !isRdp gate
      is relaxed — this used to fail with `screen.queryByRole("menu")` returning null). Menu
      contains /move to new window/i. Clicking it calls window.open + onDeactivate.
      Note: this test regresses the existing Test 7b assertion "queryByRole('menu') is null on
      RDP contextmenu". Delete / update Test 7b as part of this task — swap its assertion to
      the NEW invariant: `expect(screen.getByRole("menu")).toBeTruthy()`. Leave the "no
      PinAction in .pv-meta" assertion in Test 7b intact (that CSS-column contract is unchanged).

    UO4 — Desktop non-mobile, inActiveSet=true, `window.open` stubbed to return `null`
      (popup blocked) → menu item click still calls window.open, but onDeactivate was NOT called.
      Locks the popup-blocker-safety branch (user must not lose their tab if the new window was
      blocked).

    UO5 — Mobile variant (variant="mobile") → contextmenu on the row body is not wired
      (existing invariant), and long-press → menu opens BUT the /open in new window/i and
      /move to new window/i menuitems do NOT render. Use the same long-press helper pattern as
      TL1/TL5 (vi.useFakeTimers, touchStart with `touches:[{clientX,clientY}]`, `act(() =>
      vi.advanceTimersByTime(500))`, then query the portal-mounted menu via `screen`).
      Assert both queries return null: `expect(screen.queryByRole("menuitem", { name: /new window/i })).toBeNull();`.
      Use `screen.queryByRole("menuitem", { name: /pin/i })` as a positive control that the menu
      itself did open — if that returns null, the test setup is wrong (menu never opened) and
      the assertion is a false pass.

    UO6 — Mobile variant + RDP row long-press → the menu opens at all (proves the four
      touch-handler !isRdp gates AND the three handler-body isRdp early-returns are relaxed).
      Assert `screen.getByRole("menu")` is truthy. Item filter: with default Row props
      (no onDeactivate provided, no onToggleHide provided, no onClone provided, no identity),
      the menu will contain ONLY the Pin item on mobile (Open-in-new-window is desktop-only).
      Assert `screen.getByRole("menuitem", { name: /pin/i })` is truthy. Note: this test
      supersedes / regresses TL4's "mobile RDP row long-press does NOT open the menu" invariant —
      DELETE TL4 or rewrite it to assert the NEW invariant (menu opens). The old TL4 comment
      "isRdp guard" is stale.

    IMPORTANT for UO1/UO2/UO3/UO4: to compare the URL passed to `window.open`, decode with
    URLSearchParams(url.slice(1)) and assert:
      - params.getAll("tab").length === 1
      - params.get("tab") starts with the correct protocol prefix for the fixture (default
        `makeRow()` yields type="terminal" + targetTmuxSession="nelly" + host.name="thenasty",
        so `specForTab` produces `{protocol:"tmux", host:"thenasty", session:"nelly"}` →
        encoded as "tmux:thenasty:nelly"). Do the encodeURIComponent-aware compare
        (`decodeURIComponent(...)` on the second/third parts) or just assert the substring
        "tmux:thenasty:nelly" appears in the raw string.
      - params.get("active") === "0"
      - params.get("only") === "1"
      For UO3 (RDP), swap the fixture to `makeRow({ rdpHostRow: true, targetTmuxSession: null,
      type: "rdp" })` so `specForTab` produces `{protocol:"rdp", host:"thenasty"}` — the encoded
      spec is "rdp:thenasty".
  </behavior>
  <action>
Append 6 new tests to `src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` following the file's existing describe / it convention. Model on TL1 (mobile long-press setup + fake-timers + portal-menu query) and Test 18d (desktop contextmenu + `screen.getByRole('menu')` + `within(menu).getByRole('menuitem', {name: /.../i})`).

Delete or rewrite exactly TWO existing invariants that the new behavior invalidates:

  (a) Test 7b (line ~291) — the tail `fireEvent.contextMenu(body, ...); expect(screen.queryByRole("menu")).toBeNull();` assertion is now inverted (menu MUST open for RDP rows). Rewrite the assertion to `fireEvent.contextMenu(body, ...); expect(screen.getByRole("menu")).toBeTruthy();`. Update the test's describe/it text to reflect the new invariant. LEAVE the `no PinAction in .pv-meta` + `rdp className present` + `data-rdp-host-row === "true"` assertions intact.

  (b) TL4 (search for `it("TL4: mobile RDP row + 500ms hold does NOT open the menu`) — the invariant is now inverted (menu MUST open for mobile RDP long-press). Delete this `it` block OR rewrite it to assert `expect(screen.getByRole("menu")).toBeTruthy();` after the 500ms `act(() => vi.advanceTimersByTime(500))` step. Update the it-string to reflect the new behavior (drop the "does NOT open").

New tests structure (append to end of file, after TL5b):

```
describe("PrettyConversationRow: Open/Move-in-new-window context-menu item (quick-260804-uo4)", () => {
  let originalOpen: typeof window.open;
  beforeEach(() => {
    originalOpen = window.open;
  });
  afterEach(() => {
    window.open = originalOpen;
    vi.restoreAllMocks();
  });

  // UO1: desktop, inActiveSet, non-RDP → "Move to new window" + window.open + onDeactivate
  // UO2: desktop, !inActiveSet, non-RDP → "Open in new window" + window.open + NO onDeactivate
  // UO3: desktop, RDP row, inActiveSet → menu opens (proves gate relaxed) + "Move to new window"
  // UO4: desktop, inActiveSet, window.open returns null → onDeactivate NOT called (popup-blocker safety)
  // UO5: mobile → item does NOT render (long-press menu opens but no new-window items)
  // UO6: mobile RDP long-press → menu opens (proves touch/handler gates relaxed)
});
```

Each test:
  1. Stub `window.open` via direct assignment (`window.open = vi.fn(() => ({} as Window));`) — DO
     NOT use `vi.stubGlobal` if the file's existing tests use direct assignment (check for prior
     `window.` mutation patterns; the file's TL5a/TL5b already use direct navigator.vibrate
     mutation via delete + restore).
  2. Render with the appropriate row + props.
  3. For desktop tests, `fireEvent.contextMenu(body, { clientX: 100, clientY: 100 })`; for
     mobile tests, use the TL1-style long-press pattern with `vi.useFakeTimers()` in
     beforeEach + `vi.useRealTimers()` in afterEach + `act(() => vi.advanceTimersByTime(500))`.
  4. Query the portal-mounted menu via `screen.getByRole("menu")` (NOT `container.querySelector`).
  5. Query the menuitem via `within(menu).getByRole("menuitem", { name: /move to new window/i })`
     (or `/open in new window/i` for UO2).
  6. `fireEvent.click(item)` and assert (a) `window.open` was called exactly once, (b) the URL
     argument decodes to the correct workspace spec, and (c) `onDeactivate` mock call count
     matches expectation.

Do NOT modify any test other than 7b and TL4. Do NOT change the shared mocks (react-i18next,
session-hue, identities-store, use-is-touch-device) at the top of the file.
  </action>
  <verify>
    <automated>npm test -- src/ui/features/pretty-conversations/ 2>&amp;1 | tee /tmp/uo4-vitest.log; grep -qE "Test Files.*passed" /tmp/uo4-vitest.log &amp;&amp; ! grep -qE "failed|FAIL" /tmp/uo4-vitest.log</automated>
  </verify>
  <done>
    All existing tests in `src/ui/features/pretty-conversations/` still pass (except Test 7b and TL4 which are rewritten in-place to assert the new inverted invariants). Six new tests (UO1-UO6) pass green. `npx tsc --noEmit` still clean. No changes outside the three files declared in `files_modified`.
  </done>
</task>

</tasks>

<verification>
Overall phase gates — run once after all three tasks land:

1. `npx tsc --noEmit` clean (no new errors introduced).
2. `npm test -- src/ui/features/pretty-conversations/` — all suites pass including the six new UO1-UO6 tests and the rewritten Test 7b + TL4.
3. `grep -c "!isRdp" src/ui/features/pretty-conversations/PrettyConversationRow.tsx` — Six sidebar row-level gates dropped: three at the JSX props (`onContextMenu`, four `onTouch*` gates merged conceptually into three touch props but the file has four touch props; three inside the handler bodies (`onTouchStart`, `onTouchMove`, `onTouchEnd` early-returns). Remaining `isRdp` uses (className composition, isAmbient derivation, RDP data attribute, ambient-exemption comment) — those are NOT `!isRdp` gates and are counted separately by the grep. The invariant is: zero occurrences of the substring `!isRdp` remain in the file.
4. `git diff --name-only src/ui/features/pretty-conversations/` — exactly three files modified: `PrettyConversationRow.tsx`, `PrettyConversationsPanel.tsx`, `PrettyConversationRow.test.tsx`. Nothing else.
5. `git diff src/ui/features/pretty-conversations/PrettyConversationContextMenu.tsx` — empty (component untouched).

No push. No build. No deploy. User handles deploy separately.
</verification>

<success_criteria>
- Ashley right-clicks a non-RDP idle row on desktop → menu shows "Open in new window" between Clone and Deactivate. Click → new browser tab opens showing just that session, current row untouched.
- Ashley right-clicks a non-RDP row that IS in her active-set on desktop → same slot shows "Move to new window". Click → new browser tab opens with the session, original row deactivates + tab closes (via `handleRowDeactivate` → `onDeactivateRow(row)` → AppShell's `closeTab(row.id)` — same code path as clicking Deactivate directly).
- Ashley right-clicks any RDP row on desktop → menu opens for the first time (previously suppressed entirely). Shows Pin/Unpin + Open-or-Move-in-new-window + (if active-set) Deactivate. Clone is absent because RDP rows have no identity. Hide is absent because the panel deliberately doesn't thread onToggleHide for RDP rows.
- Ashley long-presses an RDP row on mobile → menu opens (same gate relaxation on the touch axis). No "Open/Move in new window" item because mobile is a PWA.
- Popup blocked (window.open returns null) → no data loss: original row/tab is untouched.
- `npx tsc --noEmit` clean, all pretty-conversations tests pass, no source changes outside the three declared files.
</success_criteria>

<output>
Create `.planning/quick/260804-uo4-add-open-move-in-new-window-context-menu/260804-uo4-SUMMARY.md` when done, following the standard SUMMARY template. Include:
- Files modified (verbatim list matching frontmatter)
- Key wiring diff snippets (items[] insertion + three JSX gate deletions + three handler-body early-return deletions + RDP-group onDeactivate threading + JSDoc updates)
- Test additions / rewrites (UO1-UO6 added; Test 7b + TL4 inverted)
- Verification command outputs (tsc clean, vitest green, grep counts)
- Any unexpected behavior discovered (e.g. AppShell adaptation, if any was needed for RDP handleRowDeactivate)
</output>
