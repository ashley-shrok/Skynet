---
phase: quick-260829-f9l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/IdentityModal.tsx
  - src/ui/features/pretty-view/IdentityModal.bounties-filter.test.tsx
autonomous: true
requirements:
  - QUICK-260829-f9l
tags:
  - frontend
  - identity-modal
  - bounties

must_haves:
  truths:
    - "Ashley sees a search input at the top of the Bounties tab when the modal opens."
    - "Typing in the input filters visible bounty cards in the open partitions (pinned / in_progress / rest / other) by case-insensitive substring match against title, premise, keywords, and slug."
    - "When the archive is already loaded (archivedLoadState === 'loaded'), the same filter applies to cards inside the Archive accordion contents."
    - "Typing does NOT force a fetch of the archive — if the archive is still collapsed/unloaded, no identity:list-bounties WS request fires as a side effect of typing."
    - "When the query is non-empty and no open partition matches AND the archive is loaded-and-empty (or has no matches), Ashley sees 'no matches for \"<query>\"'."
    - "When the query is non-empty, no open partition matches, AND the archive is NOT yet loaded, Ashley sees the archive-not-loaded hint appended to the empty-state message."
    - "Ashley can clear the query in one gesture via a small X button inside the input OR by pressing Escape while the input is focused; the full unfiltered list returns."
    - "Switching to the Bounties tab does NOT autofocus the search input — Ashley's keyboard focus is not stolen."
  artifacts:
    - path: "src/ui/features/pretty-view/IdentityModal.tsx"
      provides: "Bounties-tab search input + client-side filter predicate + empty-state hint"
      contains: "bountyQuery"
    - path: "src/ui/features/pretty-view/IdentityModal.bounties-filter.test.tsx"
      provides: "vitest coverage for the filter (per-field matches, empty state, archive-not-loaded hint, filter into loaded archive, clear via X and Escape, no autofocus)"
      contains: "describe"
  key_links:
    - from: "sticky <Input value={bountyQuery} …/> at the top of TabsContent value=\"bounties\""
      to: "useMemo filter predicate that reduces grouped[statusKey] and sortedArchive"
      via: "case-insensitive substring test on (title | premise | keywords[] joined | slug)"
      pattern: "bountyQuery"
    - from: "Escape keydown on the search Input"
      to: "setBountyQuery(\"\")"
      via: "onKeyDown handler"
      pattern: "e\\.key === \"Escape\""
---

<objective>
Add a client-side search box at the top of the Bounties tab in `IdentityModal` that
filters the list of bounty cards as Ashley types.

Purpose: Ashley's `tina` role has grown a long bounty list (both open and archived);
scanning by eye is now the bottleneck. A no-debounce, `useMemo`-backed substring
filter over four fields (title, premise, keywords, slug) is enough because the
lists are small (< 100 items per role) and typing feels instant.

Output: One new stateful control in the Bounties tab, one filter predicate applied
to both `OPEN_STATUS_ORDER.map` groups and `sortedArchive.map` (only when archive is
loaded), a query-aware empty-state message, and a dedicated vitest suite proving all
of the above. NO backend changes, NO wire-type changes, NO auto-fetch of archive.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md

# Component under edit (~1765 lines — Bounties tab lives at lines 1488-1692)
@src/ui/features/pretty-view/IdentityModal.tsx

# Bounty type and card shape (read-only reference — do NOT modify)
@src/ui/features/pretty-view/BountyCard.tsx

# Wire type — confirms field names: slug, title, premise, keywords: string[]
# (definition at line 524)
@src/ui/api/claude-session-api.ts

# Test-file patterns for a sibling suite (WS stub, module mocks, renderModal helper)
@src/ui/features/pretty-view/IdentityModal.role-tab.test.tsx
@src/ui/features/pretty-view/IdentityModal.test.tsx
</context>

<truths_from_read>
1. `Bounty` at `src/ui/api/claude-session-api.ts:524` HAS exactly the four searchable
   fields Ashley locked in scope: `slug: string`, `title: string`, `premise: string`,
   `keywords: string[]`. No field renaming or type extraction is needed — implement
   the predicate directly against these names.
2. The Bounties tab renders bounty cards through two structurally distinct paths:
   - Open groups: `OPEN_STATUS_ORDER.map((statusKey) => { const group = grouped[statusKey]; … group.map((b) => <BountyCard …/>) })` (line 1532-1604).
   - Archive: `sortedArchive.map((b) => <BountyCard …/>)` inside `AccordionContent` (line 1644-1684).
   The filter predicate MUST be applied to BOTH sites — filtering only one path
   silently omits half the matches.
3. `archivedLoadState` is the correct gate for "is the archive contents actually
   known": values are `"idle" | "loading" | "loaded"` with a companion `archivedError`
   (line 190). `sortedArchive` is derived from `archivedBounties` (line 1030); when
   `archivedLoadState !== "loaded"` we don't know whether archive would match — that's
   the case where the "archive not loaded — expand to include it" hint applies.
4. Existing empty-state branch at line 1514 (`!hasOpen && !hasArchive && archivedLoadState === "loaded"`) is the "truly nothing" branch and must NOT be repurposed —
   the new query-driven empty state is a separate, additional branch that only
   fires when `bountyQuery.trim() !== ""` and all filtered groups render zero cards.
5. `useMemo`, `useState`, `useEffect`, `useCallback`, `useRef` are already imported
   at line 1. `Input` from `@/components/input` is NOT yet imported in this file
   (`BountyCard.tsx` uses it — line 6 there); the new plan adds the import at the top.
6. Existing code style uses `// Patch #NNN:` or `// Quick 260NNN-XXX:` header
   comments above new blocks — reuse `// Quick 260829-f9l:` as the label for
   every new block so history-diving is greppable.
</truths_from_read>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add search state, filter predicate, and query-aware empty-state to the Bounties tab</name>
  <files>src/ui/features/pretty-view/IdentityModal.tsx</files>
  <behavior>
    - When `bountyQuery === ""`, cards render identically to before (byte-shape parity with the pre-change render).
    - When `bountyQuery` is non-empty, only cards whose `title`, `premise`, `keywords[]` (joined or iterated), or `slug` contain the query as a case-insensitive substring survive.
    - Filter is applied to BOTH the OPEN groups (`OPEN_STATUS_ORDER.map` region) and the Archive accordion body (`sortedArchive.map` region).
    - The Archive filter only runs when `archivedLoadState === "loaded"` — typing MUST NOT trigger `loadArchivedBounties()` or any other WS side effect.
    - When query is non-empty and every open group renders zero cards AND (archive is loaded AND has zero filtered matches, OR archive is NOT loaded), a query-driven empty state renders `no matches for "<query>"`; when archive is unloaded, an additional hint line `archive not loaded — expand to include it` appears.
    - Search input renders sticky at the top of the Bounties tab's scroll region so it stays visible while scrolling the list. It does NOT autofocus on tab open.
    - Input has a small X clear button (visible only when `bountyQuery !== ""`) that resets the query to `""` on click; Escape keydown on the input does the same.
  </behavior>
  <action>
    Edit `src/ui/features/pretty-view/IdentityModal.tsx`. All changes prefixed with the comment banner `// Quick 260829-f9l:` matching the file's existing `// Patch #NNN:` / `// Quick 260NNN-XXX:` header style.

    1. Add the `Input` import to the existing `@/components/input` import group. If no such import exists yet in this file, add `import { Input } from "@/components/input";` next to the existing `Button` import at line 17. Also add `X` to the existing `lucide-react` import at line 2 (if not already imported — the file already imports `X` at line 2, so no change needed there).

    2. Introduce `const [bountyQuery, setBountyQuery] = useState("")` alongside the other component-scope state in `IdentityModal`. Place it near `archivedLoadState`/`archivedError` (~line 190) so all Bounties-tab-owned state clusters together.

    3. Add a memoized normalized-query helper: `const bountyQueryNorm = useMemo(() => bountyQuery.trim().toLowerCase(), [bountyQuery]);`. Use this in the predicate so the predicate itself does NOT recompute the trim/lowercase per card.

    4. Add the filter predicate as a stable function scoped in the component (not inside the render return): `const bountyMatchesQuery = useCallback((b: Bounty): boolean => { if (!bountyQueryNorm) return true; const hay = [b.title, b.premise, b.slug, b.keywords.join(" ")].join(" ").toLowerCase(); return hay.includes(bountyQueryNorm); }, [bountyQueryNorm]);`. Rationale for keywords `.join(" ")` (rather than `.some(...)`): a keyword like `"web-ui"` never straddles the space boundary, and joining collapses the branch predicate so `hay.includes` is one call — faster and cleaner for < 100-item lists.

    5. In the Bounties `TabsContent` (the one at `value="bounties"`, currently at ~line 1489), wrap its inner children with a flex column so the search input can be sticky. The current structure `<TabsContent value="bounties" className="flex-1 min-h-0 overflow-y-auto px-6 py-4">` mixes scroll + padding on the tab pane; change the pane to `className="flex-1 min-h-0 flex flex-col"` and move the `overflow-y-auto px-6 py-4` (plus `pt-0` — the sticky bar owns the top padding) onto an INNER `<div>` that contains the existing conditional block (loading / error / empty / OPEN groups + Archive). Emit a sticky `<div>` sibling that lives INSIDE the scroll container so it sticks against the top of the scrolling region: `<div className="sticky top-0 z-10 bg-[…]/… backdrop-blur … pb-2 pt-4">` containing the `<Input …/>` with the clear X. Ashley's ask "sticky at the top of the Bounties tab's scroll region so it stays visible while scrolling" is what drives placing the sticky element INSIDE the scroll container, not above it.

    6. The `<Input>` element: `value={bountyQuery}`, `onChange={(e) => setBountyQuery(e.target.value)}`, `onKeyDown={(e) => { if (e.key === "Escape") { e.preventDefault(); setBountyQuery(""); } }}`, `placeholder="Search bounties…"`, `aria-label="Search bounties"`. NO `autoFocus` prop (locked scope #3). Style with the muted glass-token classes already used elsewhere in the modal (see the title editor at ~line 1001 for the shape — `text-sm bg-white/5 border-white/20 text-[#f0ebe0]` — reuse those literal class strings). Render the X button as a positioned overlay child of a wrapping `<div className="relative">`: `{bountyQuery !== "" && (<button type="button" onClick={() => setBountyQuery("")} aria-label="Clear search" className="absolute right-2 top-1/2 -translate-y-1/2 cursor-pointer text-[var(--color-pv-fg-muted)] hover:text-[#e8e4d8]"><X className="h-3.5 w-3.5" /></button>)}`. The existing `X` lucide icon is already imported at line 2 — reuse it, do NOT add a duplicate import.

    7. Filter the OPEN groups: inside the `OPEN_STATUS_ORDER.map((statusKey) => { const group = grouped[statusKey]; if (!group || group.length === 0) return null; … })` block (line 1532), introduce `const filteredGroup = group.filter(bountyMatchesQuery);` and change `group.map((b) => <BountyCard …/>)` to `filteredGroup.map((b) => <BountyCard …/>)`. Change `if (!group || group.length === 0) return null;` to `if (!group || filteredGroup.length === 0) return null;` — hides an entire partition header when its post-filter card count is zero.

    8. Filter the Archive: inside the accordion content, change `sortedArchive.map((b) => <BountyCard …/>)` (line 1644) to `sortedArchive.filter(bountyMatchesQuery).map((b) => <BountyCard …/>)`. Do NOT change the archive accordion trigger label or count — that stays as the total archive count, not the filtered count (Ashley's ask centers on scanning bounties, and the archive count `Archive (${sortedArchive.length})` is already the "how big is the archive drawer" signal; filtered count would conflict with that reading).

    9. Add the query-aware empty state. Compute two derived booleans (memoized): `const hasOpenAfterFilter = useMemo(() => OPEN_STATUS_ORDER.some((s) => (grouped[s] ?? []).some(bountyMatchesQuery)), [grouped, bountyMatchesQuery]);` and `const hasArchiveAfterFilter = useMemo(() => sortedArchive.some(bountyMatchesQuery), [sortedArchive, bountyMatchesQuery]);`. When `bountyQueryNorm !== ""` and `!hasOpenAfterFilter` and ((archivedLoadState === "loaded" && !hasArchiveAfterFilter) || archivedLoadState !== "loaded"), render the query-driven empty-state block: `<div className="flex flex-col gap-1 text-sm text-[var(--color-pv-fg-muted)] mt-4"><p>no matches for &ldquo;{bountyQuery}&rdquo;</p>{archivedLoadState !== "loaded" && (<p className="text-xs">archive not loaded — expand to include it</p>)}</div>`. Place this block AFTER the OPEN groups map and BEFORE the Archive accordion — that way expanding the archive still works (the accordion is still rendered per its existing gate at line 1611) and Ashley can click through to trigger the lazy load.

    10. Ensure the existing "truly nothing" empty-state at line 1514 (`!hasOpen && !hasArchive && archivedLoadState === "loaded"`) still fires only when `bountyQuery === ""` — otherwise the query-empty state takes precedence. Gate by adding `&& bountyQueryNorm === ""` to that branch's condition. Result: when Ashley opens an identity with genuinely no bounties, the existing empty message still shows; when she types and there are no matches, the query-driven one shows.

    11. Do NOT touch any backend file, WS payload shape, or `IdentityBountiesEvent` handler. Do NOT introduce debouncing (locked scope #6). Do NOT change autofocus behavior on tab switch — the existing `Tabs` `value=…` change must remain a no-op for the search input's focus (locked scope #3).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "IdentityModal|BountyCard|claude-session-api" | head -20 ; echo "---" ; npx vitest run src/ui/features/pretty-view/IdentityModal 2>&1 | tail -40</automated>
  </verify>
  <done>
    - `IdentityModal.tsx` type-checks with no new errors involving the Bounties tab, `Bounty`, or the filter predicate.
    - The search Input renders at the top of the Bounties tab scroll region (visible via existing IdentityModal renders in the test file's DOM output).
    - No test that previously passed regresses (the existing IdentityModal.test.tsx and IdentityModal.role-tab.test.tsx suites still pass).
    - Locked scope items #1-#6 all reflected in the diff; NO backend / wire-type files modified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add IdentityModal.bounties-filter.test.tsx covering all locked filter behaviors</name>
  <files>src/ui/features/pretty-view/IdentityModal.bounties-filter.test.tsx</files>
  <behavior>
    New vitest suite next to the existing `IdentityModal.role-tab.test.tsx` covering:
    - Test A: Typing "wire" into the input hides non-matching cards; matching card visible.
    - Test B: Match by TITLE — fixture bounty with title "Wire the RDP relay" matches query "rdp"; other cards hidden.
    - Test C: Match by PREMISE — fixture bounty with premise "Investigate the guacd freeze on reconnect." matches query "guacd"; other cards hidden.
    - Test D: Match by KEYWORDS — fixture bounty with `keywords: ["freerdp", "network"]` matches query "freerdp"; other cards hidden.
    - Test E: Match by SLUG — fixture bounty with slug "260828-a1b-fix-caddy-conf" matches query "caddy"; other cards hidden.
    - Test F: Empty-state renders `no matches for "…"` when query has no matches AND archive IS loaded (fixture archive loaded with zero matches too).
    - Test G: Archive-not-loaded hint renders when open has no matches AND `archivedLoadState !== "loaded"` (fixture: never send `identity:bounties` with `archivedBounties` populated, i.e. archive stays unloaded); WS spy asserts NO new `identity:list-bounties includeArchived: true` request fires as a side effect of typing.
    - Test H: Filter applies to expanded archive too — archive is loaded (mock response includes `archivedBounties: [<archived bounty>]`), user expands the accordion, then types a query that matches ONLY the archived card; only that card renders in the accordion body and open partitions render zero visible cards.
    - Test I: X-button clear — after typing "wire", the X inside the input is visible; clicking it resets `bountyQuery` to `""` and full unfiltered list returns.
    - Test J: Escape clears — after typing "wire", pressing Escape while the input is focused resets to `""` and full list returns.
    - Test K: No autofocus — after switching to the Bounties tab, `document.activeElement` is NOT the search input.
  </behavior>
  <action>
    Create `src/ui/features/pretty-view/IdentityModal.bounties-filter.test.tsx`. Follow the exact test scaffolding shape used in `IdentityModal.role-tab.test.tsx` (WS stub + module mocks + `renderModal` helper + `findSocketForRequestType` helper). Copy that file's `WsStub`, `openedSockets`, `makeFakeWs`, and `findSocketForRequestType` helpers verbatim as the setup boilerplate — Ashley's fleet rule is one WS stub pattern across all IdentityModal tests, and that file is canonical.

    Key differences vs the role-tab test file:
    - Focus the modal onto the Bounties tab BEFORE running assertions. The role-tab test showed this via `Array.from(document.querySelectorAll(".shrink-0.flex.items-stretch button")).find((b) => b.textContent?.includes("Role"))`; do the same but match "Bounties" instead. Note: 2026-08-05 default is already `"bounties"` (see role-tab test comment) — a click may not be strictly necessary, but keeping it explicit makes the test intent obvious and immune to future default-tab flips.
    - The bounties socket is the one that sends `identity:list-bounties`. After the modal mounts, feed it a scripted `identity:bounties` response via `.onmessage` carrying a fixture `bounties: [...]` and (for Test H only) `archivedBounties: [...]`.

    Fixture bounties (minimum 4 for OPEN + 1 for archive in Test H). Each bounty must be a complete `Bounty` — copy shape from `src/ui/api/claude-session-api.ts:524`. Concrete fixture set:
    - Bounty 1: `title: "Wire the RDP relay"`, `premise: "Route RDP traffic through the guacd bridge."`, `keywords: ["rdp", "guacd"]`, `slug: "260827-w1r-wire-rdp"`, `status: "in_progress"`, `priority: "high"`.
    - Bounty 2: `title: "Fix cert renewal"`, `premise: "Investigate the guacd freeze on reconnect."`, `keywords: ["cert", "tls"]`, `slug: "260827-crt-fix-cert-renewal"`, `status: "waiting_on_someone_else"`, `priority: "medium"`.
    - Bounty 3: `title: "Ship dashboard"`, `premise: "Move the sessions dashboard behind the /sessions/ prefix."`, `keywords: ["freerdp", "network"]`, `slug: "260827-dsh-ship-dashboard"`, `status: "in_progress"`, `priority: "urgent"`.
    - Bounty 4: `title: "Sweep old configs"`, `premise: "Remove stale VNC entries from the manager."`, `keywords: ["vnc"]`, `slug: "260828-a1b-fix-caddy-conf"`, `status: "in_progress"`, `priority: "low"`.
    - Archived bounty (for Test H only): `title: "Purge legacy tunnels"`, `premise: "Old SSM ports."`, `keywords: ["ssm"]`, `slug: "260701-old-purge-legacy"`, `status: "done"`, `priority: "unprioritized"`.

    Populate all other required `Bounty` fields (`id`, `pinned: false`, `needs_desk: false`, `requested_by: null`, `created_at`, `updated_at`, `timeline: []`, `todos: []`, `source_links: []`, `deadline: null`, `meeting_questions: []`) with harmless defaults.

    For each of Tests B/C/D/E, the assertion pattern is: after wiring the fixture and typing the query into the search input (find it via `screen.getByRole("textbox", { name: /search bounties/i })` — the `aria-label="Search bounties"` from Task 1 makes this stable), assert `screen.getByText(<expected match title>)` is truthy AND `screen.queryByText(<non-matching title>)` is null.

    For Test G, the key assertion: after typing, iterate `openedSockets` and verify NO socket has sent a payload with `type === "identity:list-bounties"` AND `includeArchived === true` that wasn't already there before typing (snapshot the socket list length + sent-payload lengths before and after `fireEvent.change` on the input; deltas must be zero).

    For Test H, the expansion step: `fireEvent.click(screen.getByRole("button", { name: /Archive/i }))` (the accordion trigger button carries the label from IdentityModal.tsx:1634-1640), then feed the archived bounties into the same bounties socket, then type the query.

    For Test I: after typing "wire", `screen.getByRole("button", { name: /clear search/i })` should exist (from Task 1's `aria-label="Clear search"` on the X button); `fireEvent.click(...)` it, then assert unfiltered bounties are all visible again.

    For Test J: after typing "wire", find the input, `fireEvent.keyDown(input, { key: "Escape" })`, then assert unfiltered bounties are all visible again.

    For Test K: `renderModal()` (default tab is bounties per 2026-08-05), then `await new Promise((r) => setTimeout(r, 0))` to let mount effects settle, then assert `document.activeElement` is NOT the input (`expect(document.activeElement).not.toBe(input)`).

    Do NOT add a debounce test — locked scope #6 says no debounce. Do NOT test highlight rendering — that's an explicit non-goal.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina && npx vitest run src/ui/features/pretty-view/IdentityModal.bounties-filter.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <done>
    - New test file at `src/ui/features/pretty-view/IdentityModal.bounties-filter.test.tsx` exists.
    - All 11 tests (A-K) pass under `npx vitest run …bounties-filter.test.tsx`.
    - Running `npx vitest run` at the repo root produces zero failures (fleet rule: never leave tests failing). Existing `IdentityModal.test.tsx` and `IdentityModal.role-tab.test.tsx` still green.
    - No backend or wire-type files modified in this task.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full-suite green + atomic commit</name>
  <files>—</files>
  <action>
    Run the full vitest suite and confirm zero failures across the whole tree (fleet rule: `npx vitest run` must be zero-failure before any commit). If failures surface OUTSIDE `IdentityModal.*.test.tsx` and are traceable to this change, fix them; if they're pre-existing (unrelated) failures on this branch, STOP and surface them to Tina — do NOT paper over pre-existing red tests.

    Once green, make ONE atomic commit containing:
    - `src/ui/features/pretty-view/IdentityModal.tsx`
    - `src/ui/features/pretty-view/IdentityModal.bounties-filter.test.tsx`

    Commit message (heredoc form):

    ```
    feat(quick-260829-f9l): add search box to Bounties tab in IdentityModal

    Sticky client-side search input at the top of the Bounties tab that
    filters open partitions AND (when loaded) the Archive accordion body by
    case-insensitive substring match against title, premise, keywords, and
    slug. No debounce (list is small, useMemo is enough). Escape or X-button
    clears; no autofocus on tab open. Typing does not force an archive
    fetch — when archive is unloaded and open has no matches, the empty
    state carries a hint to expand.

    Frontend-only: no backend, no wire types, no WS message shapes touched.

    11 new tests in IdentityModal.bounties-filter.test.tsx (per-field matches,
    empty state, archive-not-loaded hint, filter into loaded archive, X-clear,
    Escape-clear, no autofocus). Full vitest run is zero-failure.
    ```

    Do NOT ship (no docker build, no compose up, no HTTPS verify, no push, no
    patch-list entry, no coord-room post — Ashley's fleet rule: executors do
    not deploy; the orchestrator handles ship after executor returns).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina && npx vitest run 2>&1 | tail -20 ; echo "---" ; git log -1 --stat</automated>
  </verify>
  <done>
    - `npx vitest run` at repo root reports zero failures.
    - Exactly one new commit on `feat/tab-title-from-tmux` with the message above and only the two files listed in `files`.
    - No unrelated files staged or modified.
    - No push, no docker actions, no ROADMAP/skynet-patches.md edits.
  </done>
</task>

</tasks>

<verification>
Zero-failure `npx vitest run` at repo root — no exceptions.
The 11 new tests in `IdentityModal.bounties-filter.test.tsx` all pass.
The existing `IdentityModal.test.tsx` and `IdentityModal.role-tab.test.tsx` suites still pass.
Diff touches ONLY `IdentityModal.tsx` and the new test file — no `src/backend/`, no wire-type file, no docker/nginx.
</verification>

<success_criteria>
- Sticky search input renders at the top of Bounties tab scroll region.
- Typing filters both OPEN partitions AND (when archive loaded) archive contents by case-insensitive substring match on title + premise + keywords + slug.
- Empty state message shows "no matches for '<query>'" when non-empty query returns zero cards, with archive-not-loaded hint when applicable.
- X button + Escape both clear the query.
- No autofocus on tab switch.
- No WS side effect from typing (archive fetch not triggered).
- Full vitest suite green; one atomic commit.
</success_criteria>

<output>
Create `.planning/quick/260829-f9l-add-search-box-to-bounties-tab-in-identi/260829-f9l-SUMMARY.md` when done, following the standard summary template.
</output>
