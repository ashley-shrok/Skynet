---
phase: quick-260810-ia4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-view/ImageBubble.tsx
  - src/ui/features/pretty-view/PrettyView.estimateSize.test.tsx
  - src/ui/features/pretty-view/PrettyView.virtualization.test.tsx
autonomous: true
requirements:
  - pv-auto-scroll-redesign (partial down-payment — jitter class only; H1/H2/M5/M7 remain parked)
user_setup: []

must_haves:
  truths:
    - "Ashley's scroll gesture upward is no longer displaced by re-measure churn as new estimates converge on real heights."
    - "Image bubbles reserve their vertical space before decode, so an image landing in the viewport does not pop bubble height 0 -> N and shove content below the fold."
    - "Browser native scroll anchoring no longer competes with the virtualizer for scroll-position ownership through re-measure adjustments."
    - "Bounded-DOM invariant from Phase 27/28 still holds: rendered [data-pv-bubble] count stays bounded regardless of total messages."
    - "Executor stops at 'code committed + tests green' — no deploy motion, no coord-room posts, no skynet-patches.md entries (orchestrator-only per box-maintainer standing directive)."
  artifacts:
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "type-aware estimateSize wired via estimatePvBubbleSize helper; overflow-anchor: none on outer scroll container"
      contains: "estimatePvBubbleSize"
    - path: "src/ui/features/pretty-view/ImageBubble.tsx"
      provides: "aspect-ratio layout reservation on image wrapper (default 4/3, no metadata available on ImageBlock)"
      contains: "aspect-ratio"
    - path: "src/ui/features/pretty-view/PrettyView.estimateSize.test.tsx"
      provides: "unit test proving estimatePvBubbleSize returns distinct values for image / code-block / short-text / long-text cases"
      min_lines: 40
  key_links:
    - from: "src/ui/features/pretty-view/PrettyView.tsx (useVirtualizer options)"
      to: "estimatePvBubbleSize"
      via: "estimateSize: (i) => estimatePvBubbleSize(messages[i])"
      pattern: "estimateSize:\\s*\\(i\\)\\s*=>\\s*estimatePvBubbleSize"
    - from: "src/ui/features/pretty-view/PrettyView.tsx (composeScrollRefs div ~L1957/1963)"
      to: "browser scroll-anchoring subsystem"
      via: "overflow-anchor: none on the outer scroll container"
      pattern: "overflow-anchor.*none|overflowAnchor"
---

<objective>
Reduce PrettyView virtualizer jitter (the "jumpy sometimes" + "sometimes snapping back to bottom as I try to scroll up" class of symptom Ashley reported) with three coordinated cheap fixes: (1) a type-aware estimateSize closure factored into an exported helper so estimates are ~90% correct instead of ~30%, shrinking re-measure correction deltas that displace visible content under the scroll gesture; (2) aspect-ratio reservation on ImageBubble so images do not pop 0 -> decoded-height post-mount and trigger a measureElement re-fire mid-scroll; (3) overflow-anchor: none on the outer scroll container so the browser's native scroll-anchoring stops competing with the virtualizer's own re-measure adjustments for position authority.

Purpose: down-payment on the pv-auto-scroll-redesign bounty. This is the jitter-focused slice only — H1/H2/M5/M7 from the review, and the full auto-scroll redesign, are explicitly parked. No library swap; @tanstack/react-virtual stays.

Output: three edits + one new unit test, all inside src/ui/features/pretty-view/. Tests green (npm run build:backend, npm run build, npx vitest run). Executor commits and stops. Deploy motion belongs to the orchestrator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md

# Files being modified — read enough to preserve surrounding invariants (Phase 27/28 comments, aesthetic contract, etc.)
@src/ui/features/pretty-view/PrettyView.tsx
@src/ui/features/pretty-view/ImageBubble.tsx
@src/ui/features/pretty-view/PrettyView.virtualization.test.tsx

# StreamEvent shape — union of ChatMessageEvent | ImageEvent | RelayOutboundEvent | RelayInboundEvent | MalformedLineEvent.
# ChatMessageEvent has .content (string). ImageEvent has .type === "image" and .images (ImageBlock[]) + optional .text.
# ImageBlock is { data, mediaType, toolUseId? } — NO natural width/height metadata. The Fix-2 default-aspect-ratio path is the ONLY path; there is no metadata path to prefer.
@src/ui/api/claude-session-api.ts

# Parent bounty context — this plan is a partial down-payment, not the full redesign
@$HOME/.claude/roles/box-maintainer/bounties/pv-auto-scroll-redesign/bounty.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Type-aware estimateSize — extract estimatePvBubbleSize helper and wire it, with unit test</name>
  <files>
    src/ui/features/pretty-view/PrettyView.tsx,
    src/ui/features/pretty-view/PrettyView.estimateSize.test.tsx
  </files>
  <behavior>
    Export a pure helper `estimatePvBubbleSize(m: StreamEvent): number` from PrettyView.tsx with these rules (per source-artifact D-01, quick task scope_detail Fix 1):

    - If `m.type === "image"` return 400 (image bubbles are tall by default; overshooting slightly is fine — measureElement corrects downward with less visible displacement than correcting upward).
    - Else, derive the message text: for ChatMessageEvent use `m.content`; for RelayOutboundEvent / RelayInboundEvent / MalformedLineEvent use whatever text-carrying field the union member exposes (grep the type to confirm — likely `.content` or `.text`); if none, fall back to empty string.
    - If the derived text contains a fenced code block (regex `/```/`), return `Math.max(120, lineCount * 22 + 40)` where `lineCount = (text.match(/\n/g)?.length ?? 0) + 1`.
    - Otherwise return `Math.max(80, Math.min(400, textLength * 0.4))` where `textLength = text.length`.

    Unit tests (in PrettyView.estimateSize.test.tsx, dedicated file, unit-level — do NOT mount the component):

    - Image event returns 400.
    - Short text (~20 chars, no newlines, no fences) returns 80 (clamped by the Math.max floor).
    - Long text (~600 chars, no fences) returns 240 (bounded by textLength * 0.4, still under 400 cap).
    - Text with a fenced code block spanning 10 newlines returns Math.max(120, 11 * 22 + 40) = 282.
    - Very long text (~2000 chars, no fences) clamps at 400 (Math.min upper bound).
    - Distinct-value sanity check: image estimate, short-text estimate, and code-block estimate are all different from each other (this is the "estimates now discriminate by shape" contract).
  </behavior>
  <action>
    Read PrettyView.tsx around lines 712-717 and the StreamEvent union at line 169 first. Confirm which fields carry text on RelayOutbound / RelayInbound / MalformedLine (grep the ClaudeSessionServerEvent members in src/ui/api/claude-session-api.ts).

    Define and export `estimatePvBubbleSize(m: StreamEvent): number` in PrettyView.tsx (module scope, above the PrettyView component). Implement the rules described in <behavior>. Extract a small internal `getMessageText(m: StreamEvent): string` if it cleans up the union narrowing — but keep it module-local (unexported) unless the test genuinely needs it (it does not). Do NOT introduce a per-message ref cache — the measurement cache is TanStack Virtual's job; this helper only produces the initial ESTIMATE.

    Replace the useVirtualizer `estimateSize: () => 80` at line 717 with `estimateSize: (i) => estimatePvBubbleSize(messages[i])`. Update the adjacent comment (currently "Rough default; measureElement corrects per-item once real DOM heights are known.") to note the estimate is now type-aware and cite this quick task's ID (260810-ia4) for archaeology.

    Do NOT change `overscan: 5`, `scrollMargin: 12`, `getItemKey`, `initialRect`, or `observeElementRect`. Those are Phase 27/28 invariants; touching them is out of scope. In particular the `initialRect.height: 600` and `observeElementRect` fallback `600` literal stay put — the rationale for those (bounded first-paint DOM budget in JSDOM) is orthogonal to the estimate change. If the estimate change causes a test-window slice-size shift, the fix belongs in Task 3's test-tolerance step, NOT in these virtualizer options.

    Create src/ui/features/pretty-view/PrettyView.estimateSize.test.tsx with the six assertions above. Import `estimatePvBubbleSize` and the StreamEvent-member types directly; construct plain object literals for each case. Do NOT render PrettyView. Do NOT import testing-library — this is a pure-function test.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx vitest run src/ui/features/pretty-view/PrettyView.estimateSize.test.tsx</automated>
  </verify>
  <done>
    estimatePvBubbleSize is exported from PrettyView.tsx and covers all StreamEvent variants (image, message, relay in/out, malformed). The useVirtualizer options at line ~717 call `estimatePvBubbleSize(messages[i])` instead of returning constant 80. PrettyView.estimateSize.test.tsx exists and all its assertions pass. No changes to Phase 27/28 virtualizer invariants (overscan / scrollMargin / getItemKey / initialRect / observeElementRect).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ImageBubble aspect-ratio reservation (kill the 0 -> decoded-height pop)</name>
  <files>src/ui/features/pretty-view/ImageBubble.tsx</files>
  <behavior>
    Post-change behavior: an image bubble reserves its final layout height BEFORE the `<img>` element decodes, so TanStack Virtual's measureElement does not observe a 0 -> N grow after mount. The reservation is per-image and uses `aspect-ratio: 4 / 3` (default fallback) — `ImageBlock` in src/ui/api/claude-session-api.ts carries only { data, mediaType, toolUseId? }, so there is NO natural-dimension metadata to prefer; the fallback IS the only path. Non-image bubbles (ChatMessage, relay, malformed, etc.) are untouched — this file is ImageBubble-only.
  </behavior>
  <action>
    Read src/ui/features/pretty-view/ImageBubble.tsx and src/ui/api/claude-session-api.ts:44-57 first to CONFIRM ImageBlock has no natural width/height (as of this plan's authoring pass it does not — { data, mediaType, toolUseId? } only). If a natural-dimension field HAS been added since, prefer it and use inline width/height attributes on `<img>`; otherwise use the default fallback below.

    Fallback path (assume this path unless the read above proves otherwise): wrap each `<img>` in a `<div>` (or set the aspect-ratio directly on a wrapper spanning the image) that has `style={{ aspectRatio: "4 / 3" }}` AND the same `max-w-full max-h-[480px]` sizing constraints the current img carries so the reservation matches the final layout box. Keep the existing className shape on the `<img>` (`max-w-full max-h-[480px] object-contain rounded`) so the decoded image renders inside the reserved box. The wrapper is what tanstack observes for height; the img fills it via `w-full h-full object-contain` if needed.

    Concretely: change the current `{images.map((img, i) => ( <img ... /> ))}` block so each iteration produces a wrapper carrying the aspect-ratio + max-h reservation, with the `<img>` inside. Preserve `key={i}` (per this file's existing comment #2, index keys are safe here). Preserve `alt={`image ${i + 1}`}` and the `data:${img.mediaType};base64,${img.data}` src exactly.

    Idempotency guards: do NOT add a loading spinner or skeleton (explicitly out of scope per quick task). Do NOT touch the caption/timestamp divs, the outer bubble div, the aesthetic contract (gradient/border/shadow classes), or the `role`-driven identity-hue treatment. Add a short comment above the wrapper naming this quick task ID (260810-ia4 Fix 2) and citing the reason ("reserve layout before decode; kills 0->N pop that fires TanStack measureElement mid-scroll").

    If the read proves ImageBlock HAS acquired natural-dimension fields since this plan was authored, use those instead: set `width={img.naturalWidth}` and `height={img.naturalHeight}` on the `<img>` element (which sets the aspect ratio implicitly via HTML attributes) rather than the CSS aspect-ratio fallback. Document the choice inline.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx vitest run src/ui/features/pretty-view/ 2>&1 | tail -30</automated>
  </verify>
  <done>
    Each `<img>` in ImageBubble is wrapped in (or has inline style setting) an aspect-ratio reservation that establishes final layout height before decode. Existing PrettyView test suite still passes (the image-bubble re-measure test intentionally does NOT exist per the Phase 27 Wave 3 file header — Ashley's manual smoke is the check-in-production path — but no OTHER test regresses). Non-image bubbles untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: overflow-anchor: none on outer scroll container + full-tree verification + bounded-DOM tolerance retune</name>
  <files>
    src/ui/features/pretty-view/PrettyView.tsx,
    src/ui/features/pretty-view/PrettyView.virtualization.test.tsx
  </files>
  <action>
    Locate the composeScrollRefs div (grep confirms it's at src/ui/features/pretty-view/PrettyView.tsx:1957, className at :1963: `flex-1 min-h-0 overflow-y-auto overflow-x-hidden overscroll-contain px-4 py-3`). Add `overflow-anchor: none` to that element. Preferred form: Tailwind arbitrary variant `[overflow-anchor:none]` appended to the existing className string (matches the surrounding Tailwind-shaped classes and keeps the div style-attribute-free, which the surrounding comment tradition preserves for stacking-context reasons). Fallback if the arbitrary variant fails to compile in this project's Tailwind config: use `style={{ overflowAnchor: "none" }}` inline. The div currently has no `style` prop so adding one is safe if needed.

    Add a short inline comment naming this quick task ID (260810-ia4 Fix 3) and citing the reason: "browser native scroll-anchoring competes with TanStack Virtual's re-measure adjustments for scroll-position authority; the virtualizer should be the sole authority through measurement changes."

    Then run the full frontend suite and both backend + frontend builds. If PrettyView.virtualization.test.tsx Test 1 (the `bubbles.length <= 30` assertion at line ~380) fails because the type-aware estimate from Task 1 shifts the JSDOM visible-slice size, adjust the upper bound in that assertion to accommodate the new size — but preserve the "bounded regardless of message count" property (D-01 constraint from the quick task):

    - Keep the assertion shape `expect(bubbles.length).toBeLessThanOrEqual(N)`.
    - Do NOT change the assertion to a per-message-count-relative bound.
    - Do NOT weaken it to a large sentinel (e.g. 200) — pick the smallest N that passes for the 120-message fixture plus a small safety margin (e.g. new_observed + 5).
    - Update the comment at line ~375 ("Assert the bounded DOM cap: bubbles.length <= 30 (CONTEXT.md ...)") to note the retune, cite this quick task ID, and record the new N.

    If Test 1 STILL passes at 30, do NOT touch the test file. The retune is a contingency, not a required edit.

    Also run `npm run build:backend && npm run build` — the backend build catches TS errors the frontend tsc pass sometimes misses (box-maintainer preference; both must be clean).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npm run build:backend &amp;&amp; npm run build &amp;&amp; npx vitest run</automated>
  </verify>
  <done>
    The composeScrollRefs div carries `overflow-anchor: none` (either via Tailwind arbitrary variant or inline style). `npm run build:backend`, `npm run build`, and `npx vitest run` all exit 0. If PrettyView.virtualization.test.tsx Test 1's upper bound was retuned, the new bound is the tightest passing value (not a loose sentinel), the "bounded regardless of message count" property is preserved, and the test comment records the retune with quick task ID 260810-ia4. Executor STOPS here — no deploy motion, no docker commands, no git push, no coord-room posts, no skynet-patches.md entries.
  </done>
</task>

</tasks>

<verification>
Executor-run checks (all three must pass):

- `cd /home/ubuntu/skynet && npm run build:backend` exits 0.
- `cd /home/ubuntu/skynet && npm run build` exits 0.
- `cd /home/ubuntu/skynet && npx vitest run` exits 0 with zero test failures.

Grep sanity (executor confirms):

- `grep -n "estimatePvBubbleSize" src/ui/features/pretty-view/PrettyView.tsx` shows both the export/definition AND the useVirtualizer call site.
- `grep -n "aspect-ratio\|aspectRatio" src/ui/features/pretty-view/ImageBubble.tsx` shows the reservation.
- `grep -n "overflow-anchor\|overflowAnchor" src/ui/features/pretty-view/PrettyView.tsx` shows the outer-scroll-container fix.

Human smoke (deferred to orchestrator/Ashley post-deploy — NOT part of executor's done state):

- Open a live pane with a scrolled-back conversation containing mixed text + code blocks + images. Scroll up during streaming. Confirm the scroll gesture no longer snaps back to bottom and content does not visibly displace under the cursor.
</verification>

<success_criteria>
- estimatePvBubbleSize helper exists, is exported, is unit-tested for all four estimate branches (image / short-text / long-text / code-block) plus a distinct-values sanity assertion.
- The useVirtualizer options in PrettyView.tsx call `estimatePvBubbleSize(messages[i])` instead of returning constant 80.
- ImageBubble reserves per-image layout box via aspect-ratio (4/3 fallback, or natural-dimension attributes if ImageBlock has since gained them) before decode.
- The composeScrollRefs div (outer scroll container) has `overflow-anchor: none`.
- `npm run build:backend`, `npm run build`, `npx vitest run` all pass.
- If the bounded-DOM cap in PrettyView.virtualization.test.tsx was retuned, the new bound is tight, preserves the "bounded regardless of message count" property, and the test comment records the change.
- Scope guardrails held: changes restricted to src/ui/features/pretty-view/; no package.json changes; no backend changes; no changes to useAutoScroll; no deploy motion in the executor's work.
</success_criteria>

<output>
Create `.planning/quick/260810-ia4-pv-virtual-jitter-cheap-fixes/260810-ia4-SUMMARY.md` when done, following the template. Include: (1) what shipped in each of the three fixes, (2) whether the bounded-DOM test upper bound was retuned and to what value, (3) the exact commit SHA(s) produced, (4) explicit note that this is a partial down-payment on `pv-auto-scroll-redesign` and H1/H2/M5/M7 review findings remain parked.
</output>
