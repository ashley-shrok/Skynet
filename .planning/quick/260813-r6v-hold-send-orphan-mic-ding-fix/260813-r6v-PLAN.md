---
phase: quick-260813-r6v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/useVoiceRecording.ts
  - src/ui/features/pretty-view/useHoldToRecord.ts
  - src/ui/features/pretty-view/ComposeBox.tsx
  - src/ui/features/pretty-view/useVoiceRecording.test.ts
  - src/ui/features/pretty-view/useHoldToRecord.test.tsx
autonomous: true
requirements:
  - quick-260813-r6v-B1  # Orphaned MediaRecorder on fast-tap-past-getUserMedia
  - quick-260813-r6v-B2  # Start-mic ding plays on every quick send-button tap

must_haves:
  truths:
    - "A fast send-button tap where getUserMedia resolves BEFORE useVoiceRecording.cancel() runs leaves NO orphaned MediaRecorder — recorderRef, streamRef, chunksRef all cleared, MediaStream tracks all stopped, state === 'idle'"
    - "A fast send-button tap where useVoiceRecording.cancel() runs BEFORE getUserMedia resolves continues to work via the existing pendingCancelRef path (Test PC-A regression preserved)"
    - "A quick send-button tap (elapsed < 250ms) does NOT play start.mp3 — start.mp3 fires ONLY when the hold gesture crosses the 250ms threshold via voice.commitStartVisibility()"
    - "A long-press hold (elapsed >= 250ms) plays start.mp3 exactly once, at threshold, and voice.state transitions to 'recording' at that same moment (consumer-visible)"
    - "During the 0–250ms grey zone (voice.start called but threshold not yet crossed), voice.state remains 'idle' from the consumer's perspective, but a second pointerdown on the send button does NOT double-arm getUserMedia (re-entrance is gated internally)"
    - "The mic-tap path (MicButton onClick → beginRecord → voice.start) plays start.mp3 immediately as before — mic-tap UX is unchanged"
    - "Full vitest suite (npx vitest run) exits 0; no pre-existing failures left behind"
  artifacts:
    - path: "src/ui/features/pretty-view/useVoiceRecording.ts"
      provides: "Layer-1 stateRef + post-recorder.start() pendingCancel re-check; Layer-2 commitStartVisibility() split + starting-state (or startingRef) gate"
      exports: ["useVoiceRecording", "VoiceRecordingState", "UseVoiceRecordingReturn"]
    - path: "src/ui/features/pretty-view/useHoldToRecord.ts"
      provides: "Threshold-timer callback calls voice.commitStartVisibility() alongside setHoldCommitted(true)"
    - path: "src/ui/features/pretty-view/ComposeBox.tsx"
      provides: "beginRecord() calls voice.commitStartVisibility() immediately after voice.start() (mic-tap paths unchanged UX)"
    - path: "src/ui/features/pretty-view/useVoiceRecording.test.ts"
      provides: "New tests for post-getUserMedia cancel orphan-guard, commitStartVisibility gating of start.mp3 + state transition, re-entrance gate during grey zone"
    - path: "src/ui/features/pretty-view/useHoldToRecord.test.tsx"
      provides: "New tests for commitStartVisibility call timing (short-tap = never called, long-press = called at threshold, slide-off = called)"
  key_links:
    - from: "src/ui/features/pretty-view/useVoiceRecording.ts"
      to: "src/ui/features/pretty-view/useHoldToRecord.ts"
      via: "UseHoldToRecordVoice type extended with commitStartVisibility"
      pattern: "commitStartVisibility"
    - from: "src/ui/features/pretty-view/useHoldToRecord.ts"
      to: "src/ui/features/pretty-view/useVoiceRecording.ts"
      via: "holdTimerRef setTimeout callback invokes voice.commitStartVisibility()"
      pattern: "voice\\.commitStartVisibility"
    - from: "src/ui/features/pretty-view/ComposeBox.tsx"
      to: "src/ui/features/pretty-view/useVoiceRecording.ts"
      via: "beginRecord() calls voice.commitStartVisibility() after voice.start()"
      pattern: "voice\\.commitStartVisibility"
---

<objective>
Fix two shipped Phase 37 hold-to-send defects (patch #436):
- B-1 (orphaned MediaRecorder): on a fast send-button tap, if getUserMedia resolves BEFORE useVoiceRecording.cancel() runs, cancel() reads stale `state="idle"` from React's useState closure, takes the pending-cancel-arming branch, but pendingCancelRef is only checked ONCE (line 322) BEFORE recorder construction — the recorder starts and stays running forever with the mic hot.
- B-2 (mic-ding on every quick tap): playSound(startAudioRef) fires unconditionally inside .then() (line 352) before the short-tap branch has a chance to bail — every quick tap emits the start-mic sound.

Purpose: Restore the hold-to-send UX invariant — a quick tap should be silent (no mic ding, no lingering mic) and a hold should ding + engage recording at 250ms.

Output: Two-layer fix in useVoiceRecording.ts (correctness + UX), matching threshold-timer wiring in useHoldToRecord.ts, mic-tap parity in ComposeBox.tsx, and 4 new useVoiceRecording tests + 3 new useHoldToRecord tests. Full vitest suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/ui/features/pretty-view/useVoiceRecording.ts
@src/ui/features/pretty-view/useHoldToRecord.ts
@src/ui/features/pretty-view/useVoiceRecording.test.ts
@src/ui/features/pretty-view/useHoldToRecord.test.tsx

# ComposeBox: only the two hold sites + beginRecord + MicButton props. Do NOT
# read the whole file — grep for `voice\.start\|voice\.commitStartVisibility`
# to find call sites (already audited: line 1393 beginRecord, plus MicButton
# uses at 2525 for primary and 2945 for slot both go through beginRecord).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Two-layer fix in useVoiceRecording — Layer 1 (orphan-guard) + Layer 2 (commitStartVisibility split)</name>
  <files>
    src/ui/features/pretty-view/useVoiceRecording.ts,
    src/ui/features/pretty-view/useVoiceRecording.test.ts
  </files>
  <behavior>
    Layer 1 (correctness — kills the orphaned MediaRecorder):
    - Add a state-mirror mechanism so cancel() reads the CURRENT state, not the useState closure snapshot. Implementer picks between (a) stateRef + useEffect(() => { stateRef.current = state; }, [state]), or (b) a setStateSync helper that writes to stateRef.current synchronously BEFORE calling setState. Either works — (a) is the smaller diff, (b) is racier-safe. Recommend (a).
    - cancel() replaces `state !== "recording"` with `stateRef.current !== "recording"` (and the state check inside endAppend/endSend if the executor chooses to unify, though the primary requirement is cancel()).
    - Inside start()'s .then() callback, AFTER `recorder.start()` fires (currently line 349) and BEFORE `setState("recording")` / `playSound(startAudioRef)` (currently 351-352), re-check `pendingCancelRef.current`. If it flipped true between the initial check at line 322 and now: call `recorder.stop()`, iterate `stream.getTracks().forEach(t => t.stop())`, clear `recorderRef.current = null`, `streamRef.current = null`, `chunksRef.current = []`, then `return`. Do NOT setState (leave it as consumer-visible "idle"/"starting"). Do NOT playSound. Do NOT clear pendingCancelRef (the cancel's semantics are "returned to idle").

    Layer 2 (UX — kills the mic-ding-on-every-tap):
    - Introduce a new hidden intermediate state `"starting"` in the `VoiceRecordingState` union: `"idle" | "starting" | "recording" | "transcribing"`. The `"starting"` state is set inside the .then() callback IN PLACE OF `setState("recording")` at line 351. `playSound(startAudioRef)` at line 352 is REMOVED from this call site. `setErrorMessage(null)` STAYS at line 353 (housekeeping clear). The `console.info(`[voice] recording-started ...`)` diagnostic log STAYS at line 350 (always want the diagnostic).
    - New public method `commitStartVisibility(): void`. Behavior: `if (stateRef.current === "starting") { setState("recording"); playSound(startAudioRef.current); }` — if not in "starting", no-op (idempotent, safe to call twice, safe to call after cancel).
    - Export `commitStartVisibility` from `UseVoiceRecordingReturn`.
    - The existing `start()` early guard `if (state !== "idle") return;` becomes `if (stateRef.current !== "idle") return;` — this now correctly gates re-entrance during the "starting" grey zone: a second pointerdown finds state = "starting", not "idle", and short-circuits. No separate startingRef needed.
    - `cancel()` handling of the "starting" state: treat "starting" like "recording" for teardown purposes IF the recorder has been constructed (recorderRef.current != null), else route to the existing pending-cancel-arming path. Concretely: `if (stateRef.current !== "recording" && stateRef.current !== "starting")` for the arming-branch condition; when state IS "starting" AND recorderRef.current != null, run the same stopRecording()+setState("idle") teardown as the "recording" branch (this is the same-tick partner of Layer 1's post-recorder.start() re-check — one of them will handle every fast-tap ordering).
    - `endAppend`/`endSend` continue to gate on `stateRef.current !== "recording"` (unchanged behavior — you cannot end-send during the "starting" grey zone; the consumer only reaches end-send after long-press commits which flips state to "recording").
    - Update `VoiceRecordingState` JSDoc / type comment to describe the four-state machine.

    UseVoiceRecordingReturn interface add:
    - `commitStartVisibility: () => void`

    New tests appended to useVoiceRecording.test.ts (mirror existing style; keep existing tests intact):
    - **Test PC-D**: fast tap where getUserMedia resolves BEFORE cancel() — call start(), await one microtask so getUserMedia resolves and .then() runs to completion (recorder constructed, state === "starting"), THEN call cancel() with state === "starting". Assert: MockMediaRecorder.instances[0].stop called, mockStream tracks stopped, recorderRef cleared (indirectly via a follow-up start() call: assert a fresh MockMediaRecorder is constructed on the next start()), state returns to "idle", NO commitStartVisibility, NO start.mp3 played.
    - **Test PC-E**: post-recorder.start() re-check path (the exact log Ashley captured) — flip `pendingCancelRef.current = true` MANUALLY inside a `.then()` callback race by: mock getUserMedia to return a Promise the test controls, call start(), call cancel() (pendingCancelRef armed via the "starting" branch), then resolve the getUserMedia promise, await. Assert: MockMediaRecorder was constructed AND stop() was called on it (the post-.then() re-check tore it down), stream tracks stopped, state stays "idle", start.mp3 NEVER played. This is the smoking-gun Ashley bug regression guard.
    - **Test COMMIT-A**: happy path via commitStartVisibility — start(), await state === "starting", assert start.mp3 NOT yet played AND state is "starting" (not "recording") from consumer view; then call commitStartVisibility(), assert state flips to "recording" AND start.mp3 played exactly once.
    - **Test COMMIT-B**: commitStartVisibility called BEFORE getUserMedia resolves (race — .then() hasn't run) — assert commitStartVisibility is a no-op (state stays "idle"), no start.mp3. Then let getUserMedia resolve; assert state transitions to "starting" (not straight to "recording"). This proves commit must be called AFTER threshold AND AFTER .then() has run to be effective; the threshold timer is 250ms which is longer than most getUserMedia resolves in practice, so this is a defensive test not a normal path.
    - **Test COMMIT-C** (regression guard for existing behavior): re-entrance during grey zone — call start(), await state === "starting", call start() again SYNCHRONOUSLY (no commit yet). Assert: getUserMedia called ONLY ONCE total, MockMediaRecorder.instances.length === 1.

    Preserve existing tests unchanged. PC-A (cancel BEFORE getUserMedia resolves) and PC-B (cancel AFTER recording started) MUST still pass — Test PC-A's assertion "MediaRecorder NEVER constructed" is preserved by the pending-cancel-arming branch running first when state === "idle" (before .then()). Test PC-B's assertion "recorder.stop called" is preserved because state === "recording" post-commit takes the same teardown path.

    Do NOT touch any other test file in this task. Do NOT touch ComposeBox tests (they use their own MockMediaRecorder infrastructure and will need Task 2's ComposeBox change + potentially updated fakes; that is Task 2's concern).
  </behavior>
  <action>
Modify src/ui/features/pretty-view/useVoiceRecording.ts to implement the Layer 1 + Layer 2 fixes described in <behavior> above. Use recommended approach: (a) stateRef via useRef + useEffect(() => { stateRef.current = state; }, [state]); (b) "starting" intermediate state in VoiceRecordingState union; (c) commitStartVisibility() as a new public method. Update the type doc-comment for VoiceRecordingState to describe the four-state machine and the intent of "starting" (hidden-from-consumer, hold-to-record grey zone before threshold commit; mic-tap consumers should call commitStartVisibility() immediately after start()). Update the docstring of start() to note the new state machine and the two-layer race defenses (initial pendingCancel check + post-recorder.start re-check). Update the docstring of cancel() to note the new "starting"-state handling.

Append the four new tests (PC-D, PC-E, COMMIT-A, COMMIT-B, COMMIT-C) to src/ui/features/pretty-view/useVoiceRecording.test.ts, mirroring the existing test style (MockMediaRecorder, makeMockStream, controllable getUserMedia via vi.fn().mockImplementation(() => new Promise(...)) for race tests, waitFor for state transitions, act() wrappers for state-changing calls, getAudioBySrc('start.mp3') for playSound assertions). Add them at the end of the describe block, after Test F. Do NOT modify or reorder existing tests. Verify all existing tests still pass — if any legitimately break because of the state-machine change (e.g., a test that asserted `state === "recording"` immediately after `await waitFor` following start()), update that ONE test to await commitStartVisibility() first OR to accept "starting" and note the change in a comment.

Run `npx vitest run src/ui/features/pretty-view/useVoiceRecording.test.ts` after each editing pass. Iterate until green.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/useVoiceRecording.test.ts</automated>
  </verify>
  <done>All useVoiceRecording.test.ts tests pass (existing + 5 new: PC-D, PC-E, COMMIT-A, COMMIT-B, COMMIT-C). VoiceRecordingState union includes "starting". useVoiceRecording returns commitStartVisibility. Layer 1 (post-recorder.start pendingCancel re-check + stateRef) implemented. Layer 2 (setState("recording") + playSound(startAudioRef) moved from .then() into commitStartVisibility) implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire commitStartVisibility from consumers — useHoldToRecord threshold timer + ComposeBox mic-tap parity</name>
  <files>
    src/ui/features/pretty-view/useHoldToRecord.ts,
    src/ui/features/pretty-view/useHoldToRecord.test.tsx,
    src/ui/features/pretty-view/ComposeBox.tsx
  </files>
  <behavior>
    useHoldToRecord.ts changes:
    - Extend `UseHoldToRecordVoice` type: `Pick<UseVoiceRecordingReturn, "state" | "start" | "cancel" | "commitStartVisibility">`.
    - Inside `onPointerDown`'s `holdTimerRef` setTimeout callback (currently line 244-246, single `setHoldCommitted(true)` call), ADD `voice.commitStartVisibility();` as a new line inside the same callback. Order within the callback: call `voice.commitStartVisibility()` FIRST (so the state transition to "recording" and the mic-ding fire at the exact threshold instant), then `setHoldCommitted(true)`. Both are synchronous.
    - No other logic changes. The B-3 holdInitiatedRef ordering, the D-16-02 sync-gesture invariant (voice.start still called synchronously in pointerdown), the reset/cleanup, and all handler callbacks stay identical.

    ComposeBox.tsx changes (surgical — grep-audited two mic-tap sites):
    - `beginRecord(target)` at line 1391-1394: add `voice.commitStartVisibility();` as a new line immediately AFTER `voice.start();`. This preserves the mic-tap UX — mic-tap immediately transitions to "recording" and plays start.mp3 as before, because commitStartVisibility runs synchronously after start() (and is idempotent — if called during "idle" before .then() runs, it no-ops; when .then() then flips state to "starting", the next React tick's mic-tap consumer would see "starting" briefly, but since beginRecord is single-shot and .then() → "starting" transitions happen in a microtask, followed by an eventual commit... actually no: beginRecord's commitStartVisibility runs SYNCHRONOUSLY while state is still "idle" (before .then()), so it no-ops. That is wrong for mic-tap parity. **Correct approach for mic-tap parity**: wire the commit into the .then() success path or use a "commit on start" flag. **Simpler correct approach**: make beginRecord register a one-shot "auto-commit-on-starting" intent via a hook-exposed method like `voice.startAndCommitImmediately()` — but that duplicates surface area. **Simplest correct approach**: extend voice.start() to accept an optional `{ autoCommit?: boolean }` parameter — when true, the .then() flips state directly to "recording" and plays start.mp3 (bypassing the "starting" intermediate). When false/omitted, the current "starting" behavior applies. **Recommended approach (LOCKED IN by planner for executor)**: pick the `{ autoCommit?: boolean }` parameter on voice.start(). This keeps commitStartVisibility surface clean for the hold-consumer's threshold-timer path AND keeps the mic-tap path (beginRecord) using its single existing call. Update Task 1's useVoiceRecording change accordingly if executor is combining tasks; or Task 2 must edit useVoiceRecording.ts to add the parameter.

    **PLANNER DECISION**: To keep Task 1 focused on the useVoiceRecording refactor and avoid ping-pong, MOVE the `voice.start({ autoCommit: true })` overload design INTO Task 1's scope. Task 1's `start()` gains a `{ autoCommit?: boolean }` parameter (default false). When true: the .then() success path calls setState("recording") + playSound(startAudioRef) directly, skipping the "starting" intermediate. When false (or omitted): the .then() sets state to "starting" and requires commitStartVisibility() to advance to "recording". Update Task 1 tests to cover the autoCommit path (Test COMMIT-D: `start({ autoCommit: true })` flips straight to "recording" and plays start.mp3 without commitStartVisibility being called).

    ComposeBox.tsx concrete edit set (with the autoCommit approach):
    - Line 1393 `voice.start();` → `voice.start({ autoCommit: true });` — mic-tap parity preserved: state goes idle → recording, start.mp3 plays, no external commit needed.
    - No commitStartVisibility call anywhere in ComposeBox.tsx (the primaryHold and slotHold useHoldToRecord instances handle it via their threshold-timer callback in useHoldToRecord.ts).
    - Verify: `grep -n 'voice\.start\|voice\.commitStartVisibility' src/ui/features/pretty-view/ComposeBox.tsx` should show line 1393 as `voice.start({ autoCommit: true })` and NO other voice.start calls (the two MicButton call sites at 2525 and 2945 both route through beginRecord).

    useHoldToRecord.test.tsx new tests (append after Test 10, in the same describe block, mirroring style — vi.useFakeTimers, TestConsumer, fireEvent.pointerDown/Up, installBoundsShim):
    - **Test 11**: short tap (elapsed < 250ms) — assert voice.commitStartVisibility NOT called. Extend the makeMockVoice helper OR inline it in this test to add a `commitStartVisibility: vi.fn()` spy. Sequence: pointerDown → advance 200ms → pointerUp → await microtasks. Assert commitStartVisibility.mock.calls.length === 0, voice.cancel called 1x, onShortTap called 1x.
    - **Test 12**: hold at exactly threshold — pointerDown → `vi.advanceTimersByTime(HOLD_THRESHOLD_MS)`. This fires the timer callback SYNCHRONOUSLY inside advanceTimersByTime. Assert voice.commitStartVisibility called 1x. Then pointerUp at timeStamp=HOLD_THRESHOLD_MS → assert onLongPressSend called 1x, voice.cancel NOT called (long-press-inside branch).
    - **Test 13**: hold past threshold then slide-off then release outside — pointerDown → advance 300ms → assert commitStartVisibility called 1x → pointerLeave (mark out-of-bounds) → pointerUp outside rect. Assert voice.cancel called 1x, onLongPressSend NOT called. This test also proves commitStartVisibility fires ONCE at the threshold (not again on release).

    Update the shared `makeMockVoice` helper at the top of useHoldToRecord.test.tsx to include the new `commitStartVisibility: vi.fn().mockReturnValue(undefined)` spy in the returned object. This keeps Test 11/12/13 clean and makes the mock match the new UseHoldToRecordVoice type.

    Also update the makeMockVoice helper's return-type comment/annotation to `Pick<UseVoiceRecordingReturn, "state" | "start" | "cancel" | "commitStartVisibility">` so TypeScript stays happy.

    Do NOT modify Tests 1-10 (they don't call commitStartVisibility, so adding the spy to the helper is invisible to them).
  </behavior>
  <action>
1. Modify src/ui/features/pretty-view/useHoldToRecord.ts:
   - Extend `UseHoldToRecordVoice` type to include `commitStartVisibility` (per Task 1's new useVoiceRecording API).
   - In `onPointerDown`, inside the `holdTimerRef = setTimeout(() => { ... }, effectiveThreshold)` callback (currently just `setHoldCommitted(true)`), add `voice.commitStartVisibility();` FIRST, then `setHoldCommitted(true);`.

2. Modify src/ui/features/pretty-view/ComposeBox.tsx:
   - Line 1393: change `voice.start();` to `voice.start({ autoCommit: true });`.
   - Update the beginRecord() JSDoc comment (currently at line 1387-1390) to note the autoCommit:true semantics — mic-tap parity requires immediate state=recording + start.mp3, whereas the hold-to-record path uses the default (starting → commit-at-threshold) behavior.
   - Verify no other `voice.start` call sites exist: `grep -n 'voice\.start\b' src/ui/features/pretty-view/ComposeBox.tsx` should return only line 1393.

3. Modify src/ui/features/pretty-view/useHoldToRecord.test.tsx:
   - Update `makeMockVoice` helper: add `commitStartVisibility: vi.fn().mockReturnValue(undefined)` to the returned object. Update its return-type annotation to include `"commitStartVisibility"` in the Pick.
   - Append Test 11, Test 12, Test 13 (per <behavior>) at the end of the describe block after Test 10.

4. Run these tests in order:
   - `npx vitest run src/ui/features/pretty-view/useHoldToRecord.test.tsx` — must pass
   - `npx vitest run src/ui/features/pretty-view/ComposeBox.hold-to-send.test.tsx` — must pass (this integration test exercises the wired stack)
   - `npx vitest run src/ui/features/pretty-view/ComposeBox.voice.test.tsx` — must pass (mic-tap paths — autoCommit:true guarantees UX parity)
   - `npx vitest run` (FULL SUITE) — must exit 0. If any pre-existing failures surface, fix them in-session per the constraint. If any test broke because of the API change (e.g., a mock voice missing commitStartVisibility), update the mock to add the spy.

5. Fix any TypeScript errors surfaced by the new `UseHoldToRecordVoice` shape. Grep `grep -rn 'UseHoldToRecordVoice\|Pick<UseVoiceRecordingReturn' src/ui/features/pretty-view/` to find all consumers of the narrowed type and update as needed.

6. Commit on current branch (feat/tab-title-from-tmux). NO deploy, NO push, NO merge. Committing includes all changes across Task 1 + Task 2 (single or dual commit, executor's choice — prefer two commits per Layer 1 / Layer 2 semantics, but a single squash commit is acceptable if the diff is coherent).

DO NOT modify files outside src/ui/features/pretty-view/. DO NOT modify any backend/server code. DO NOT modify ROADMAP.md or phase docs (this is a quick task; STATE.md's quick-tasks table is updated by the summary step post-execution, not here).
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>Full vitest suite exits 0. useHoldToRecord.ts calls voice.commitStartVisibility() inside the 250ms threshold-timer callback. ComposeBox.tsx line 1393 uses voice.start({ autoCommit: true }). useHoldToRecord.test.tsx has 3 new tests (11, 12, 13) all passing. makeMockVoice helper updated with commitStartVisibility spy. All existing tests still green. Changes committed on feat/tab-title-from-tmux.</done>
</task>

</tasks>

<verification>
Full-suite green + defect-specific manual sanity via test coverage:

- **B-1 orphan-guard**: useVoiceRecording.test.ts Tests PC-D and PC-E cover both fast-tap orderings (getUserMedia-first, cancel-first). PC-E specifically reproduces the Ashley log scenario — pendingCancelRef armed AFTER recorder construction, teardown via post-recorder.start re-check.
- **B-2 mic-ding**: useVoiceRecording.test.ts Test COMMIT-A proves start.mp3 does NOT play until commitStartVisibility() fires. useHoldToRecord.test.tsx Test 11 proves a short-tap NEVER calls commitStartVisibility. Test 12 proves a hold at exactly 250ms DOES call it.
- **Mic-tap parity**: ComposeBox.voice.test.tsx (existing, unchanged) exercises the mic-tap path via beginRecord — passes because `voice.start({ autoCommit: true })` preserves the immediate state=recording + start.mp3 behavior.
- **iOS Safari D-16-02 invariant**: useHoldToRecord.test.tsx Test 4 (existing) still passes — voice.start is called synchronously in pointerdown (unchanged); commitStartVisibility is called from a setTimeout callback which does NOT need to respect the invariant (the getUserMedia call has already happened by then).
- **Re-entrance safety**: useVoiceRecording.test.ts Test COMMIT-C proves a second start() during the grey zone is a no-op — no double getUserMedia.
</verification>

<success_criteria>
- Ashley's shipped bug reproduction (fast tap on send button after typing "hi" → orphaned mic hot indefinitely) is eliminated: verified by Test PC-E asserting recorder.stop() is called + tracks stopped even when cancel arms AFTER recorder is constructed.
- Ashley's shipped bug reproduction (every quick send tap plays start-mic ding) is eliminated: verified by Test COMMIT-A asserting start.mp3 is not played until commitStartVisibility() and by Test 11 asserting commitStartVisibility is NEVER called on a short tap.
- Mic-tap ding preserved: verified by ComposeBox.voice.test.tsx passing with `voice.start({ autoCommit: true })` in beginRecord().
- `npx vitest run` exits 0. Pre-existing failures (if any) fixed in-session.
- Changes committed on branch `feat/tab-title-from-tmux`. NO deploy, NO push.
</success_criteria>

<output>
Create `.planning/quick/260813-r6v-hold-send-orphan-mic-ding-fix/260813-r6v-SUMMARY.md` when done (following @$HOME/.claude/get-shit-done/templates/summary.md).

Update STATE.md's quick-tasks table to mark 260813-r6v-hold-send-orphan-mic-ding-fix as complete.
</output>
