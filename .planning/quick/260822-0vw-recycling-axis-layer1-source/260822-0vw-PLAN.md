---
phase: quick-260822-0vw-recycling-axis-layer1-source
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/fleet-status/ssh-poll-orchestrator.ts
  - src/backend/fleet-status/ssh-poll-orchestrator.test.ts
autonomous: true
requirements:
  - QT-260822-0vw
must_haves:
  truths:
    - "When source A processes a live-PID identity whose current session file's most-recent real user turn is `/id reset`, `SessionState.recycling` is published as true — regardless of whether `.recycled-at` exists."
    - "When source A processes a live-PID identity whose `.recycled-at` sentinel exists, `SessionState.recycling` is published as true — regardless of what the session file's last user turn is (behavior preserved)."
    - "When both signals are true, `SessionState.recycling` is true (OR is idempotent). When both are false, `SessionState.recycling` is false."
    - "When the JSONL tail read returns null (SSH hiccup), the Layer 1 signal falls back to its cached value on the PidCacheEntry — the composed recycling axis is `cached-layer1 || derivedSentinel` (fail-open, same shape as the sentinel's own fail-open)."
    - "Source B (dormant-only, no live PID) behavior is UNCHANGED — no JSONL to check, `.recycled-at` sentinel remains the only source B recycling input (Phase 53-CR fix preserved)."
    - "The three consumer sites (`SessionHoldingOverlay`, `ComposeBox isHolding + recycleActive`, row spinner via `useSessionIsRecycling`) are NOT touched — they read `useSessionIsRecycling(sessionWorkingKey)` and pick up the source change for free (Phase 53 one-axis architecture preserved)."
  artifacts:
    - path: src/backend/fleet-status/ssh-poll-orchestrator.ts
      provides: "OR-composed recycling axis (Layer 1 tail predicate || .recycled-at sentinel stat) for source A; PidCacheEntry.layer1RecyclingCached field"
      contains: "detectIdReset"
    - path: src/backend/fleet-status/ssh-poll-orchestrator.test.ts
      provides: "5 new tests in a new describe block covering the Layer-1 OR composition (both signals independent, OR truth table, JSONL fail-open)"
      contains: "quick-260822-0vw"
  key_links:
    - from: "src/backend/fleet-status/ssh-poll-orchestrator.ts (processPid tail-scan block, ~line 869)"
      to: "src/backend/claude-session/session-file-parser.ts (detectIdReset)"
      via: "import + per-line invocation while iterating the existing `tail -c 262144 ${jsonlPath}` buffer"
      pattern: "import.*detectIdReset|detectIdReset\\("
    - from: "src/backend/fleet-status/ssh-poll-orchestrator.ts (source A publish site, ~line 995)"
      to: "SessionState.recycling wire field"
      via: "derivedRecycling = layer1Recycling || derivedSentinelRecycling (single composed value stamped into SessionState)"
      pattern: "recycling:\\s*derivedRecycling"
---

<objective>
OR the Layer 1 `/id reset` detector's signal into source A's composition of `SessionState.recycling` inside `ssh-poll-orchestrator.ts`, so the recycling axis arms at t=0 (user presses reset → `/id reset` lands in the session file as the last user turn) rather than only at t=N (after `/id save` completes and the caretaker renames `.recycle-requested` → `.recycled-at`).

Purpose: Phase 53-03 swapped SessionHoldingOverlay + ComposeBox + row spinner to a single working-store Axis E (`useSessionIsRecycling`) fed exclusively by the backend `.recycled-at` sentinel stat. That sentinel is placed at the END of the save flow, not at the start of the reset gesture — so the recycling axis misses the entire save phase (t=0..N, typically ~5-15s). Extending the backend composition to also read the current session file's last user turn (Layer 1 predicate: `detectIdReset`) closes that window without touching any consumer site, preserving the "one axis" architecture and the backend-authoritative property (works for identities whose PrettyView is not mounted, so the row spinner also arms at t=0).

Output:
- `ssh-poll-orchestrator.ts` gains a Layer 1 tail-scan pass (reuses the existing `tail -c 262144 ${jsonlPath}` buffer at line 878 — ONE exec, TWO scans, no new SSH round-trip), OR's the result into `derivedRecycling`, caches the derived Layer 1 value on `PidCacheEntry.layer1RecyclingCached` for fail-open on SSH hiccup, publishes on fingerprint delta.
- 5 new co-located tests confirming the OR truth table + fail-open behavior.
- Zero changes to `wire-protocol.ts`, `session-file-parser.ts`, `layer1-detect.ts`, `pane-state-emitter.ts`, any store, or any consumer component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/53-backend-authoritative-recycling-signal-one-wire-axis-two-con/53-CONTEXT.md
@.planning/phases/53-backend-authoritative-recycling-signal-one-wire-axis-two-con/53-01-SUMMARY.md
@.planning/phases/53-backend-authoritative-recycling-signal-one-wire-axis-two-con/53-03-SUMMARY.md
@src/backend/fleet-status/ssh-poll-orchestrator.ts
@src/backend/claude-session/session-file-parser.ts
@src/backend/claude-session/layer1-detect.ts
@src/backend/fleet-status/wire-protocol.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OR Layer 1 /id reset detector into source A's derivedRecycling composition (+ 5 co-located tests)</name>
  <files>src/backend/fleet-status/ssh-poll-orchestrator.ts, src/backend/fleet-status/ssh-poll-orchestrator.test.ts</files>

  <behavior>
    Write the 5 new tests FIRST in a new describe block titled
    `describe("quick-260822-0vw — Layer 1 /id reset OR composition into source A recycling", () => { ... })`
    appended to the end of `ssh-poll-orchestrator.test.ts` (after the existing Phase-53-CR describe that ends around line 3700+). Model the fixture wiring on the existing `Phase 53 Plan 01 — source A recycling stat + fingerprint` describe (starts at ~line 3365) — reuse `MockSshChannel`, `buildDeps`, `makeSessionJson`, `makeStatContents`, `makeValidPayload`, `jsonlMessageLine`, `buildDiscoveryFixture` (the last three are local helpers inside the Phase 53 Plan 01 describe — copy them into the new describe or hoist; do NOT rewrite discovery-fixture shape). Each test drives the orchestrator ONCE via `orchestrator.start()` on a wired `MockSshChannel` and asserts `deps.registry.publishedStates[0].state.recycling`.

    Test IDs and expectations (co-located style — mirror `P53-01-T1-i` naming exactly):

    - **Test QT-260822-0vw-T1-i — Layer 1 alone triggers recycling.**
      Wire the session file discovery fixture + tail response so the discovered JSONL's tail contains a JSONL line that satisfies `detectIdReset`. Use this exact user-turn shape (mirrors `session-file-parser.id-reset.test.ts` Test 1):
      ```json
      {"type":"user","message":{"role":"user","content":"<command-name>/id</command-name><command-args>reset</command-args>"},"timestamp":"...","uuid":"..."}
      ```
      Override `.recycled-at` stat to `"no\n"` (sentinel absent). Assert `deps.registry.publishedStates[0].state.recycling === true`.

    - **Test QT-260822-0vw-T1-ii — Sentinel alone triggers recycling (regression guard for Phase 53-01 behavior).**
      Wire the tail to contain a plain assistant/user message (no `/id reset` markup). Override `.recycled-at` stat to `"yes\n"`. Assert `recycling === true`. This proves the OR does not accidentally AND — the sentinel path still fires when Layer 1 is quiet.

    - **Test QT-260822-0vw-T1-iii — Both signals true → still recycling (OR is idempotent).**
      Wire the tail to contain a `/id reset` line AND the sentinel to `"yes\n"`. Assert `recycling === true` and only ONE publish (fingerprint identity — do NOT expect two frames).

    - **Test QT-260822-0vw-T1-iv — Neither signal → not recycling.**
      Wire the tail to contain a plain assistant/user message (no `/id reset`). Override `.recycled-at` to `"no\n"`. Assert `recycling === false`.

    - **Test QT-260822-0vw-T1-v — SSH failure on session file tail → Layer 1 falls back to cached value (fail-open).**
      Wire the discovery response normally, then set the JSONL tail response (matched via the `discovered.jsonl` substring, same pattern as Phase 53 Plan 01 tests) to `null` — simulating a mid-tick SSH hiccup on the `tail -c 262144` exec. Also override `.recycled-at` to `"no\n"` (sentinel absent). Assert `orchestrator.start()` resolves without throwing AND `recycling === false` (cold-start cache default is false; matches the sentinel's own fail-open contract at Test P53-01-T1-iii). Do NOT assert anything about the second-tick behavior — one tick is enough to prove fail-open.

    Every test uses `wireBaseResponses` as a starting fixture and then overrides one or two channel responses. Do NOT introduce new mocks, new module boundaries, or new fixture helpers beyond what's needed to override the discovery + tail response for the `/id reset` case.
  </behavior>

  <action>
    **STEP 1 — RED (write failing tests):**

    Append a new describe block to `src/backend/fleet-status/ssh-poll-orchestrator.test.ts` after the existing Phase 53 CR describe. Copy or hoist the `jsonlMessageLine` and `buildDiscoveryFixture` helpers from the Phase 53 Plan 01 describe (~lines 3371-3401) into the new describe (or a shared position immediately above it) so both describes stay self-contained. Extend `wireBaseResponses` locally (or add a `wireBaseResponsesForLayer1` variant) so tests can override the `discovered.jsonl` tail response WITHOUT re-wiring the full fixture stack. Model on the existing pattern where `channel.setResponse("discovered.jsonl", jsonlContents)` sets the tail response — the same pattern lets Test T1-v pass `null` for a fail-open simulation.

    Write all 5 tests exactly per the `<behavior>` block above. Run `npx vitest run --related src/backend/fleet-status/ssh-poll-orchestrator.ts src/backend/fleet-status/ssh-poll-orchestrator.test.ts` and confirm all 5 new tests FAIL (Layer 1 signal not yet wired). Commit as `test(quick-260822-0vw): add failing tests for Layer 1 /id reset OR into source A recycling`.

    **STEP 2 — GREEN (wire the composition):**

    Edit `src/backend/fleet-status/ssh-poll-orchestrator.ts`:

    (a) **Add import** at the top with the existing `parseSessionLine` import (line 44):
    ```
    import { parseSessionLine, detectIdReset } from "../claude-session/session-file-parser.js";
    ```
    (Merge into the existing import statement; do NOT create a second import line for the same module.)

    (b) **Extend `PidCacheEntry`** (interface at line 109) with a new `layer1RecyclingCached` field placed IMMEDIATELY AFTER the existing `recycling: boolean;` field (line 180). Mirror the Phase 53 Plan 01 comment style — brief block comment explaining: cached derived boolean result of scanning the JSONL tail for the most-recent real user turn's `/id reset` status; `false` at cold-start; fail-open (preserve cache) when the tail exec returns null; participates as an INPUT to `derivedRecycling` (which is `cached-layer1 || sentinel`), NOT its own fingerprint axis (the axis stays `derivedRecycling` — one composed boolean).

    (c) **Layer 1 scan** — extend the existing tail-scan block at lines 869-933. Inside the `if (jsonlPath !== null)` branch, AFTER the `tail -c 262144 ${jsonlPath}` exec (line 878-880) and the `if (tailRaw !== null && tailRaw.trim() !== "")` guard that already runs `scanTailForNewestMessageAt` and `scanTailForLatestAiTitle` (lines 883-887), add a THIRD scan against the same `tailRaw` buffer that computes the most-recent real-user-turn `/id reset` status. Signature (add near the sibling scanners around line 276-344):
    ```
    function scanTailForLayer1RecyclingSignal(tailContents: string): boolean | null
    ```
    Returns `true` if the last non-null-parse user turn in the tail satisfies `detectIdReset`, `false` if the last user turn does NOT, `null` if the tail contained ZERO parseable user turns (no signal — caller preserves cache). Implementation: iterate lines in order (matches existing `scanTailForNewestMessageAt` shape), for each non-empty line try `JSON.parse` inside try/catch (skip malformed silently — matches the module's existing tolerance), skip if `parsed.type !== "user"`, otherwise call `detectIdReset(parsed)` and REMEMBER the result on each user-turn hit (last-wins — most-recent user turn wins, mirrors `layer1-detect.applyLineToLayer1State`'s reducer semantics). Return the last-remembered value or `null` if zero user turns were seen.

    (d) **Compose `derivedRecycling`**: at the existing sentinel-stat block (lines 818-834), leave the sentinel derivation exactly as-is (rename its local variable to `derivedSentinelRecycling` for clarity — or leave `derivedRecycling` and use a separate `derivedLayer1Recycling` local; either works, but the FINAL composed value stamped into SessionState must be the OR). After the tail-scan block (post-line 933), compute:
    ```
    let derivedLayer1Recycling: boolean = cached?.layer1RecyclingCached ?? false;
    if (jsonlPath !== null && tailRaw !== null && tailRaw.trim() !== "") {
      const scannedLayer1 = scanTailForLayer1RecyclingSignal(tailRaw);
      if (scannedLayer1 !== null) {
        derivedLayer1Recycling = scannedLayer1;
      }
      // scannedLayer1 === null → tail had zero user turns → keep cached value
    }
    // tail exec failed (null) OR tail empty → keep cached value (fail-open)
    const derivedRecyclingComposed = derivedLayer1Recycling || derivedSentinelRecycling;
    ```
    (Adjust variable names to fit the surrounding style; the load-bearing structure is: cache-default, sentinel derived independently, then OR both booleans into the composed value stamped into `SessionState.recycling`.)

    NOTE: `tailRaw` is currently declared inside the `if (jsonlPath !== null)` scope (line 878). You will need to either (i) hoist it up so the composition block after the branch can see it, OR (ii) compute `derivedLayer1Recycling` INSIDE the same `if (jsonlPath !== null)` block (right after the two existing scans at lines 885-886). Prefer (ii) — it colocates the scan with the exec that produced its input, matches the sibling scanners' locality, and avoids scope hoisting.

    (e) **Stamp both livenessMap.set sites** (lines 1013-1024 fresh-publish branch, 1027-1038 same-fingerprint update branch) with the new cache field:
    ```
    layer1RecyclingCached: derivedLayer1Recycling,
    ```
    Placed immediately after the existing `recycling: derivedRecycling,` field in both sites.

    (f) **SessionState stamp** (lines 982-996): change the `recycling:` field to stamp the composed value:
    ```
    recycling: derivedRecyclingComposed,
    ```
    (or whatever local name step (d) produced). This is the load-bearing wire change — the composed OR flows onto the wire under the existing `recycling` axis.

    (g) **computeFingerprint** — do NOT touch. The recycling axis in the fingerprint (line 444) reads `state.recycling`, which is now the composed value. Fingerprint delta detection continues to fire on any change to the composed axis — no schema change, no version bump.

    Do NOT touch: `wire-protocol.ts`, `session-file-parser.ts` (already exports `detectIdReset`), `layer1-detect.ts`, `pane-state-emitter.ts`, `claude-session-server.ts`, any file under `src/frontend/`, any store, any consumer component. This is a single-file backend composition change plus its tests.

    Source B (`pollDormantOnlyIdentities`, lines 596-703) is UNCHANGED — dormant-only identities have no live claude PID, therefore no session file to tail-scan; source B's existing `.recycled-at` stat remains its sole recycling input (Phase 53-CR preserved).

    Run `npx vitest run --related src/backend/fleet-status/ssh-poll-orchestrator.ts src/backend/fleet-status/ssh-poll-orchestrator.test.ts` and confirm all 5 new tests PASS + all pre-existing tests in the file still PASS. If any Phase 52 / Phase 53-01 / Phase 53-CR test regresses, the composition is wrong — the sentinel path must remain independently sufficient (Test QT-260822-0vw-T1-ii is the guard).

    Commit as `feat(quick-260822-0vw): OR Layer 1 /id reset detector into source A recycling composition`.

    **STEP 3 — verify TypeScript is clean:**

    Run `npx tsc --noEmit` restricted to the touched files (`npx tsc --noEmit` on the full project is fine if the incremental cache is warm; otherwise scope via `tsc --noEmit -p tsconfig.json` and read only the errors matching the touched paths). Zero new errors.

    Do NOT run the full test suite, do NOT run `npm run build`, do NOT run any docker command, do NOT include any ship or deploy step. Executor's remit ends at scoped tests green + typescript clean + commits pushed to the working branch.
  </action>

  <verify>
    <automated>npx vitest run --related src/backend/fleet-status/ssh-poll-orchestrator.ts src/backend/fleet-status/ssh-poll-orchestrator.test.ts 2>&amp;1 | tail -40</automated>
    <automated>grep -c "QT-260822-0vw-T1-" src/backend/fleet-status/ssh-poll-orchestrator.test.ts</automated>
    <automated>grep -c "detectIdReset" src/backend/fleet-status/ssh-poll-orchestrator.ts</automated>
    <automated>grep -c "layer1RecyclingCached" src/backend/fleet-status/ssh-poll-orchestrator.ts</automated>
    <automated>npx tsc --noEmit 2>&amp;1 | grep -E "ssh-poll-orchestrator\.(ts|test\.ts)" | grep -v "^$" | wc -l</automated>
  </verify>

  <done>
    - 5 new tests in the `quick-260822-0vw` describe block all pass; every pre-existing test in `ssh-poll-orchestrator.test.ts` still passes.
    - `grep -c "QT-260822-0vw-T1-" src/backend/fleet-status/ssh-poll-orchestrator.test.ts` returns at least 10 (5 tests × 2 references each — describe title + it name, matching the Phase 53 Plan 01 count-of-10 self-check convention).
    - `grep -c "detectIdReset" src/backend/fleet-status/ssh-poll-orchestrator.ts` returns at least 2 (import + call site).
    - `grep -c "layer1RecyclingCached" src/backend/fleet-status/ssh-poll-orchestrator.ts` returns at least 4 (interface field + cache read + 2 livenessMap.set stamps).
    - `npx tsc --noEmit` reports zero errors touching `ssh-poll-orchestrator.ts` or `ssh-poll-orchestrator.test.ts`.
    - Two commits on the working branch: RED (failing tests) + GREEN (composition wire-up), both scoped to the two files above.
    - Zero changes to: `wire-protocol.ts`, `session-file-parser.ts`, `layer1-detect.ts`, `pane-state-emitter.ts`, `claude-session-server.ts`, any frontend file, any store file, any consumer component.
    - Source B path (`pollDormantOnlyIdentities`) is byte-identical to pre-change.
  </done>
</task>

</tasks>

<verification>
- `grep -n "derivedRecyclingComposed\|derivedLayer1Recycling\|scanTailForLayer1RecyclingSignal" src/backend/fleet-status/ssh-poll-orchestrator.ts` shows the composition wired in exactly one place (the source A processPid pipeline).
- `git diff HEAD~2 -- src/backend/fleet-status/ssh-poll-orchestrator.ts src/backend/fleet-status/ssh-poll-orchestrator.test.ts` is the ONLY file diff (paired with the RED test commit).
- `git diff HEAD~2 -- src/backend/fleet-status/wire-protocol.ts src/backend/claude-session/ src/frontend/` returns empty output — no wire schema change, no parser change, no consumer change, no frontend change.
</verification>

<success_criteria>
- `SessionState.recycling` for a live-PID identity is true at t=0 (user presses reset → `/id reset` lands in the session file as the most-recent real user turn), BEFORE `.recycled-at` is created by the caretaker at t=N.
- `SessionState.recycling` for a live-PID identity is true throughout the save phase (t=0..N) and continues to be true at t=N when the sentinel appears (OR is idempotent, single publish per fingerprint delta).
- `SessionState.recycling` for a live-PID identity is false when neither Layer 1 nor sentinel fire.
- Source B (dormant identities without live PIDs) behavior is byte-identical to pre-change.
- Zero consumer sites touched — the three PrettyView + row-spinner sites read `useSessionIsRecycling` unchanged and inherit the earlier signal arrival for free.
- Scoped test run + tsc are clean; full test suite / build / deploy are the orchestrator's problem, not the executor's.
</success_criteria>

<output>
Create `.planning/quick/260822-0vw-recycling-axis-layer1-source/260822-0vw-01-SUMMARY.md` when done — brief (10-30 lines), matching the Phase 53 Plan 01 SUMMARY style: one-liner, what was built (with concrete line ranges + the composed local's exact name), any deviations from plan, test count, commit shas.
</output>
