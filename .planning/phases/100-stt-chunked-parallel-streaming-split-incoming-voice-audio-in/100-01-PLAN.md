---
phase: 100-stt-chunked-parallel-streaming
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/voice/semaphore.ts
  - src/backend/voice/semaphore.test.ts
  - src/backend/voice/audio-chunker.ts
  - src/backend/voice/audio-chunker.test.ts
  - src/backend/voice/audio-transcode.ts
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-09
  - D-10
user_setup: []

must_haves:
  truths:
    - "createSemaphore(N) admits exactly N concurrent async fn invocations; the (N+1)th call waits until one active call resolves"
    - "sliceFlac(flacBuf, start, dur) returns a valid FLAC buffer covering [start, start+dur) seconds of the input"
    - "scanSilenceGaps(flacBuf) returns SilenceGap[] with only fully-closed pairs (start+end+duration); unpaired trailing silence_start is discarded"
    - "probeDuration(flacBuf) returns the audio's duration in seconds as a float"
    - "computeChunkBoundaries produces boundaries that (a) start at 0, (b) end covering totalDurationSec, (c) extend each cut by overlapSec, (d) prefer silence-gap midpoints within the target window and fall back to fixed 8s cuts"
    - "runFfmpeg is exported from audio-transcode.ts as a reusable utility with the same spawn/pipe body as before"
  artifacts:
    - path: "src/backend/voice/semaphore.ts"
      provides: "createSemaphore(limit) counting-semaphore factory"
      exports: ["createSemaphore"]
    - path: "src/backend/voice/semaphore.test.ts"
      provides: "Unit tests for createSemaphore concurrency invariant"
    - path: "src/backend/voice/audio-chunker.ts"
      provides: "sliceFlac, scanSilenceGaps, probeDuration, parseSilenceGaps, computeChunkBoundaries; SilenceGap + ChunkBoundary types"
      exports: ["sliceFlac", "scanSilenceGaps", "probeDuration", "parseSilenceGaps", "computeChunkBoundaries", "SilenceGap", "ChunkBoundary"]
    - path: "src/backend/voice/audio-chunker.test.ts"
      provides: "Unit tests for chunker: spawn argv, parse edge cases, boundary math"
    - path: "src/backend/voice/audio-transcode.ts"
      provides: "Existing webmToOggOpus + webmToFlac PLUS newly-exported runFfmpeg"
      exports: ["webmToOggOpus", "webmToFlac", "runFfmpeg"]
  key_links:
    - from: "src/backend/voice/audio-chunker.ts"
      to: "src/backend/voice/audio-transcode.ts"
      via: "import { runFfmpeg } from './audio-transcode.js'"
      pattern: "import.*runFfmpeg.*from.*audio-transcode"
    - from: "src/backend/voice/audio-chunker.ts"
      to: "node:child_process (via runFfmpeg for stdout AND separately for stderr-collecting spawns)"
      via: "spawn ffmpeg / ffprobe stdin→stdout OR stdin→stderr"
      pattern: "spawn"
---

<objective>
Wave 1 pure kernels for Phase 100 chunked STT: (1) module-level counting semaphore (D-09/D-10), (2) FLAC audio chunker with ffmpeg silencedetect + ffmpeg slicing + ffprobe duration (D-01/D-02/D-03/D-04), (3) export the existing `runFfmpeg` helper from audio-transcode.ts so both the chunker and the transcode module share ONE spawn implementation.

Purpose: These are the dependency-free building blocks the Wave 2 orchestrator composes. They have no interdependencies on each other (semaphore.ts is standalone; audio-chunker.ts depends only on the already-shipping audio-transcode.ts). By landing them in Wave 1 with full unit-test coverage, Wave 2 can be pure assembly.

Output: Three source files (semaphore.ts, audio-chunker.ts, +1-line export change in audio-transcode.ts) and two test files (semaphore.test.ts, audio-chunker.test.ts) with `npx vitest run --related` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/100-stt-chunked-parallel-streaming-split-incoming-voice-audio-in/100-CONTEXT.md
@.planning/phases/100-stt-chunked-parallel-streaming-split-incoming-voice-audio-in/100-RESEARCH.md
@.planning/phases/100-stt-chunked-parallel-streaming-split-incoming-voice-audio-in/100-PATTERNS.md
@src/backend/voice/audio-transcode.ts
@src/backend/voice/audio-transcode.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Export runFfmpeg from audio-transcode.ts and create semaphore module + tests</name>
  <files>src/backend/voice/audio-transcode.ts, src/backend/voice/semaphore.ts, src/backend/voice/semaphore.test.ts</files>
  <read_first>
    - src/backend/voice/audio-transcode.ts (current runFfmpeg body at lines 80-103 — the shape being exported)
    - src/backend/voice/audio-transcode.test.ts (existing test suite — MUST stay green after export change; no runFfmpeg-specific tests added here since it's tested through webmToOggOpus/webmToFlac)
    - .planning/phases/100-stt-chunked-parallel-streaming-split-incoming-voice-audio-in/100-RESEARCH.md (Section 5 — inline semaphore pattern, verbatim source)
    - .planning/phases/100-stt-chunked-parallel-streaming-split-incoming-voice-audio-in/100-PATTERNS.md (semaphore.ts section — full module content to copy exactly)
  </read_first>
  <behavior>
    - semaphore Test 1: createSemaphore(2) — call 3 async fns each resolving after a promise; assert exactly 2 run concurrently and the 3rd waits until one resolves (use a shared counter incremented on entry / decremented on resolve to observe max concurrency = 2)
    - semaphore Test 2: acquire() returns the wrapped fn's resolved value (identity passthrough)
    - semaphore Test 3: acquire() rejects with the wrapped fn's rejection (error passthrough, releases slot for next waiter)
    - semaphore Test 4: FIFO queue order — when limit=1 and 3 calls are made in order A,B,C, they resolve in order A,B,C
    - semaphore Test 5: releases the slot even when the wrapped fn throws (subsequent acquire() succeeds; no slot leak on rejection)
    - audio-transcode: existing 6-scenario test file must remain green (webmToOggOpus + webmToFlac happy paths + failure paths). No new audio-transcode tests in THIS task.
  </behavior>
  <action>
    Step A (audio-transcode.ts, ONE-LINE change): Change the function declaration on line 80 from `function runFfmpeg(webmBuffer: Buffer, args: readonly string[]): Promise&lt;Buffer&gt;` to `export function runFfmpeg(inputBuffer: Buffer, args: readonly string[]): Promise&lt;Buffer&gt;`. Rename the parameter from `webmBuffer` to `inputBuffer` inside the body (it's used exactly once: `ff.stdin.end(webmBuffer)` becomes `ff.stdin.end(inputBuffer)`). Do NOT modify the body logic, do NOT touch webmToOggOpus / webmToFlac. Update the private-helper comment on lines 71-79 to reflect that runFfmpeg is now exported for reuse by audio-chunker.ts (keep the T-98-04-04 argv-safety note verbatim — argv is still hardcoded literals in every caller). Rationale for exporting rather than duplicating: RESEARCH.md § Open Question 1 recommendation ("avoid duplication; the 20-line helper is the same shape whether it emits FLAC or Ogg"). The wildcard filter Pitfall 3 gate stays intact — this task does NOT touch transcribe-adapter.ts.

    Step B (semaphore.ts, NEW file): Copy the pattern verbatim from 100-PATTERNS.md § semaphore.ts (which itself lifted verbatim from 100-RESEARCH.md § Section 5). File content is a single exported factory function `createSemaphore(limit: number)` returning `async function acquire&lt;T&gt;(fn: () =&gt; Promise&lt;T&gt;): Promise&lt;T&gt;`. Body maintains `let active = 0` and `const queue: Array&lt;() =&gt; void&gt; = []`, acquire path: if active &gt;= limit, `await new Promise&lt;void&gt;((resolve) =&gt; queue.push(resolve))`; then increment active, run fn in try/finally, decrement + `queue.shift()?.()` in finally. Include a top-of-file docblock: "D-09 (N=5 concurrent Transcribe streams, under 25-stream account quota); D-10 (module-level singleton mirrors TranscribeStreamingClient at transcribe-adapter.ts:93). This module is dep-free — no imports."

    Step C (semaphore.test.ts, NEW file): Import `vitest` and `createSemaphore` from `./semaphore.js`. Implement the 5 behavior cases above using `let running = 0; let maxSeen = 0;` observer pattern. For FIFO test, resolve promises in a controlled order via `Promise.withResolvers()` or manually-constructed resolvers; assert the completion-order matches submission-order. All tests must be deterministic — use awaited resolvers, no setTimeout races.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tabitha &amp;&amp; npx vitest run --related src/backend/voice/audio-transcode.ts src/backend/voice/semaphore.ts 2&gt;&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -n "^export function runFfmpeg" src/backend/voice/audio-transcode.ts` returns a match on the exact declaration `export function runFfmpeg(inputBuffer: Buffer, args: readonly string[]): Promise&lt;Buffer&gt; {`
    - Source: `grep -n "webmBuffer" src/backend/voice/audio-transcode.ts` returns ONLY hits inside webmToOggOpus / webmToFlac docblocks/params — NOT inside runFfmpeg body (parameter was renamed to inputBuffer)
    - Source: `grep -c "^export function createSemaphore" src/backend/voice/semaphore.ts` == 1
    - Source: `grep -c "let active = 0" src/backend/voice/semaphore.ts` == 1 AND `grep -c "queue.shift()" src/backend/voice/semaphore.ts` == 1
    - Source: `grep -c "^import" src/backend/voice/semaphore.ts` == 0 (no imports — dep-free module)
    - Test: `npx vitest run --related src/backend/voice/semaphore.ts src/backend/voice/audio-transcode.ts` reports zero failures across both suites (semaphore's 5 new tests PASS; audio-transcode's existing tests PASS unchanged)
    - Behavior: semaphore.test.ts contains at least the 5 behavior cases enumerated in &lt;behavior&gt; (grep for `it("...concurrent"`, `it("...passthrough"`, `it("...rejection"`, `it("...FIFO"`, `it("...slot leak"` — exact wording flexible but each case verifiable)
  </acceptance_criteria>
  <done>runFfmpeg is exported from audio-transcode.ts with its parameter renamed to inputBuffer; audio-transcode.test.ts remains fully green. semaphore.ts exists as a zero-dependency exported factory. semaphore.test.ts covers all 5 behavior cases (concurrency limit, value passthrough, rejection passthrough, FIFO order, slot-release-on-throw) and all pass under vitest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create audio-chunker module (sliceFlac, scanSilenceGaps, probeDuration, parseSilenceGaps, computeChunkBoundaries) + tests</name>
  <files>src/backend/voice/audio-chunker.ts, src/backend/voice/audio-chunker.test.ts</files>
  <read_first>
    - src/backend/voice/audio-transcode.ts (runFfmpeg shape being imported; also the T-98-04-04 argv comment on lines 58-62 for the security-note pattern to mirror)
    - src/backend/voice/audio-transcode.test.ts (FakeChildProcess pattern on lines 34-47 — audio-chunker.test.ts uses this exact class)
    - .planning/phases/100-stt-chunked-parallel-streaming-split-incoming-voice-audio-in/100-RESEARCH.md (Sections 2, 3, 4, 6, 8 and Pitfalls 2, 3, 4 — verified argv shapes, regex, boundary algorithm, min-duration guard)
    - .planning/phases/100-stt-chunked-parallel-streaming-split-incoming-voice-audio-in/100-PATTERNS.md (audio-chunker.ts + audio-chunker.test.ts sections — analog structure and test shape)
  </read_first>
  <behavior>
    - sliceFlac Test 1: calls runFfmpeg with argv exactly `["-f", "flac", "-i", "pipe:0", "-ss", "2.500", "-t", "8.000", "-f", "flac", "pipe:1"]` for `sliceFlac(buf, 2.5, 8.0)` (Pitfall 3 verified: `-f flac` MUST precede `-i pipe:0`)
    - sliceFlac Test 2: startSec and durationSec are formatted with `.toFixed(3)` (e.g. `sliceFlac(buf, 2, 8)` produces `-ss 2.000 -t 8.000`)
    - scanSilenceGaps Test 1: spawns ffmpeg with argv exactly `["-f", "flac", "-i", "pipe:0", "-af", "silencedetect=n=-40dB:d=0.2", "-f", "null", "-"]` and resolves with the stderr text on close code 0
    - scanSilenceGaps Test 2: rejects on non-zero close code (stderr included in error message)
    - probeDuration Test 1: spawns `ffprobe` with argv exactly `["-f", "flac", "-i", "pipe:0", "-v", "quiet", "-show_entries", "format=duration", "-of", "default=noprint_wrappers=1:nokey=1"]`, parses stdout as float (`parseFloat("8.000000\n") === 8`)
    - probeDuration Test 2: rejects when stdout is not a parseable float (returns NaN or empty)
    - parseSilenceGaps Test 1: extracts 2 gaps from stderr `[silencedetect @ 0x] silence_start: 5\n[silencedetect @ 0x] silence_end: 5.40006 | silence_duration: 0.400062\n[silencedetect @ 0x] silence_start: 12.4\n[silencedetect @ 0x] silence_end: 12.7001 | silence_duration: 0.300063\n` → `[{startSec:5, endSec:5.40006, durationSec:0.400062}, {startSec:12.4, endSec:12.7001, durationSec:0.300063}]`
    - parseSilenceGaps Test 2: discards unpaired trailing `silence_start` (Pitfall 2) — stderr with only `silence_start: 30.5\n` at end produces zero gaps (or omits that unpaired entry from a set of otherwise-complete gaps)
    - parseSilenceGaps Test 3: returns `[]` when stderr contains no silence lines
    - computeChunkBoundaries Test 1: silence-aware — silenceGap at [7.5, 8.1] with targetChunkSec=8, overlapSec=2.5 produces boundaries where cut lands at the gap midpoint (7.8) and endSec = 10.3 (7.8 + 2.5 overlap)
    - computeChunkBoundaries Test 2: fixed-window fallback — no silenceGaps in the target window produces a cut at the fixed target (8.0) with endSec = 10.5
    - computeChunkBoundaries Test 3: last-chunk terminator — final chunk has startSec at cursor and endSec = totalDurationSec + overlapSec (no overshoot loop past the end)
    - computeChunkBoundaries Test 4: total coverage — for totalDuration=20, targetChunkSec=8, overlapSec=2.5, no silence: produces 3 boundaries with startSecs 0, 8, 16 (or similar deterministic split) that COVER [0, 20]
    - computeChunkBoundaries Test 5: minimum-chunk guard — if the trailing chunk would be shorter than MIN_CHUNK_SEC (1.0), it merges into the previous chunk instead of dispatching (Pitfall 8 / Domain item 8 mitigation)
  </behavior>
  <action>
    Create `src/backend/voice/audio-chunker.ts` as a new module. Module-level docblock cites CONTEXT D-01 through D-04 and D-06, RESEARCH Sections 2/3/4/6/8, PATTERNS audio-chunker.ts section. Imports: `import { spawn } from "node:child_process"` (for ffprobe AND for silencedetect — since those need stderr collection, not stdout) and `import { runFfmpeg } from "./audio-transcode.js"` (for sliceFlac which needs stdout capture).

    Define and export these functions and types:

    (1) `export interface SilenceGap { startSec: number; endSec: number; durationSec: number; }`

    (2) `export interface ChunkBoundary { startSec: number; endSec: number; }` (endSec exclusive; endSec extends past the logical cut by overlapSec)

    (3) `export function sliceFlac(flacBuf: Buffer, startSec: number, durationSec: number): Promise&lt;Buffer&gt;` — delegates to `runFfmpeg(flacBuf, ["-f", "flac", "-i", "pipe:0", "-ss", startSec.toFixed(3), "-t", durationSec.toFixed(3), "-f", "flac", "pipe:1"])`. Add T-98-04-04 comment: "Argv contains ONLY literal strings and numeric-formatted values; user data flows through stdin only."

    (4) `export function scanSilenceGaps(flacBuf: Buffer, thresholdDb = -40, minGapSec = 0.2): Promise&lt;string&gt;` — spawn ffmpeg directly (NOT via runFfmpeg because we need stderr, not stdout). Argv: `["-f", "flac", "-i", "pipe:0", "-af", `silencedetect=n=${thresholdDb}dB:d=${minGapSec}`, "-f", "null", "-"]`. Collect stderr chunks in an array; on `close(0)` resolve with `Buffer.concat(errChunks).toString()`; on non-zero close reject with `new Error(`ffmpeg exited ${code}: ${stderrText}`)`; on `error` event reject. Push input buffer via `ff.stdin.end(flacBuf)`.

    (5) `export function probeDuration(flacBuf: Buffer): Promise&lt;number&gt;` — spawn `ffprobe` with argv `["-f", "flac", "-i", "pipe:0", "-v", "quiet", "-show_entries", "format=duration", "-of", "default=noprint_wrappers=1:nokey=1"]`. Collect stdout chunks; on close(0) parse `parseFloat(Buffer.concat(chunks).toString().trim())`; if `Number.isFinite(duration)` resolve with it, else reject with error including the raw stdout. Reject on non-zero close or error event. Push input via `ff.stdin.end(flacBuf)`.

    (6) `export function parseSilenceGaps(stderrOutput: string): SilenceGap[]` — pure function. Iterate over `stderrOutput.split("\n")`. Use `const START_RE = /silence_start:\s*([\d.]+)/;` and `const END_RE = /silence_end:\s*([\d.]+)\s*\|\s*silence_duration:\s*([\d.]+)/;`. Track `pendingStart: number | null`. On start-match, set pendingStart. On end-match AND pendingStart !== null, push `{startSec: pendingStart, endSec: parseFloat(endMatch[1]), durationSec: parseFloat(endMatch[2])}` then clear pendingStart. Do NOT push on unpaired trailing start (Pitfall 2). Return the array.

    (7) `export function computeChunkBoundaries(totalDurationSec: number, silenceGaps: SilenceGap[], targetChunkSec = 8, overlapSec = 2.5, minChunkSec = 1.0): ChunkBoundary[]` — algorithm from RESEARCH Section 6 code example, with the additional Pitfall 8 minimum-chunk guard: track cursor from 0. While cursor &lt; totalDurationSec: compute targetEnd = cursor + targetChunkSec. If targetEnd &gt;= totalDurationSec, push final chunk `{startSec: cursor, endSec: totalDurationSec + overlapSec}` and break. Else find candidate silenceGap where `gap.startSec &gt;= cursor + (targetChunkSec * 0.5)` AND `gap.endSec &lt;= targetEnd + 2`; cutAt = candidate ? (candidate.startSec + candidate.endSec) / 2 : targetEnd. Push `{startSec: cursor, endSec: cutAt + overlapSec}`; advance cursor = cutAt. AFTER the loop, apply the minChunkSec guard: if the final chunk's actual audio-length (endSec - overlapSec - startSec) &lt; minChunkSec AND boundaries.length &gt; 1, merge it into the previous chunk by removing the last boundary and extending the second-to-last's endSec to totalDurationSec + overlapSec.

    Create `src/backend/voice/audio-chunker.test.ts`. Copy the vi.hoisted + vi.mock("node:child_process") pattern from audio-transcode.test.ts lines 24-47 verbatim (change import to `./audio-chunker.js` and destructure `sliceFlac, scanSilenceGaps, probeDuration, parseSilenceGaps, computeChunkBoundaries`). Use the FakeChildProcess class verbatim (stdin.end vi.fn, stdout/stderr EventEmitters). Implement all 12 behavior tests above using deterministic FakeChildProcess drives (emit data + emit close(0)). For pure-function tests (parseSilenceGaps, computeChunkBoundaries) no mock is needed — direct assertion on return values.

    IMPORTANT: Because sliceFlac delegates to the mocked runFfmpeg-via-node-child_process-spawn, the spawn mock will observe the child process from runFfmpeg's internal call. Simplest test: assert `spawnMock.mock.calls[0][1]` matches the expected argv from runFfmpeg's invocation. Do NOT try to double-mock runFfmpeg — mocking spawn is sufficient because runFfmpeg is a thin wrapper.

    IMPORTANT (probeDuration): the spawnMock is shared across ffmpeg AND ffprobe calls in the test file (since both go through `spawn` from node:child_process). Tests assert `spawnMock.mock.calls[0][0]` == "ffprobe" for probeDuration tests, == "ffmpeg" for sliceFlac and scanSilenceGaps tests.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tabitha &amp;&amp; npx vitest run --related src/backend/voice/audio-chunker.ts 2&gt;&amp;1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "^export function sliceFlac" src/backend/voice/audio-chunker.ts` == 1
    - Source: `grep -c "^export function scanSilenceGaps" src/backend/voice/audio-chunker.ts` == 1
    - Source: `grep -c "^export function probeDuration" src/backend/voice/audio-chunker.ts` == 1
    - Source: `grep -c "^export function parseSilenceGaps" src/backend/voice/audio-chunker.ts` == 1
    - Source: `grep -c "^export function computeChunkBoundaries" src/backend/voice/audio-chunker.ts` == 1
    - Source: `grep -c "^export interface SilenceGap" src/backend/voice/audio-chunker.ts` == 1
    - Source: `grep -c "^export interface ChunkBoundary" src/backend/voice/audio-chunker.ts` == 1
    - Source: `grep -n 'import { runFfmpeg } from "./audio-transcode.js"' src/backend/voice/audio-chunker.ts` returns exactly one match (chunker consumes the newly-exported helper from Task 1)
    - Source: `grep -n '"-f", "flac", "-i", "pipe:0"' src/backend/voice/audio-chunker.ts` returns matches (Pitfall 3 argv ordering) inside both sliceFlac AND scanSilenceGaps AND probeDuration
    - Source: `grep -n 'silencedetect=n=' src/backend/voice/audio-chunker.ts` returns at least one match with the -40dB:d=0.2 default parameters
    - Source: `grep -c "pendingStart" src/backend/voice/audio-chunker.ts` &gt;= 2 (used inside parseSilenceGaps to gate unpaired-trailing-start; Pitfall 2)
    - Test: `npx vitest run --related src/backend/voice/audio-chunker.ts` reports zero failures. Expected test count &gt;= 12 (behavior list above).
    - Test coverage grep: `grep -c "it(" src/backend/voice/audio-chunker.test.ts` &gt;= 12
    - Regression: `npx vitest run --related src/backend/voice/audio-transcode.ts` still green (import change in Task 1 did not break the module's own tests)
  </acceptance_criteria>
  <done>audio-chunker.ts exports the 5 functions (sliceFlac, scanSilenceGaps, probeDuration, parseSilenceGaps, computeChunkBoundaries) and 2 types (SilenceGap, ChunkBoundary). audio-chunker.test.ts covers the 12 behavior cases above (argv shapes, edge cases, boundary math including silence-aware, fixed fallback, and minimum-chunk guard) — all pass under vitest. audio-transcode.test.ts remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audio buffer → ffmpeg stdin | FLAC bytes originating from browser upload; already piped through the existing Phase 98 webmToFlac transcode |
| ffmpeg argv → subprocess exec | ALL argv strings are hardcoded literals + numeric `.toFixed(3)` values in this plan; NO user-controlled string interpolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-01-01 | Tampering | audio-chunker.ts argv construction | mitigate | Inherits T-98-04-04 — argv contains ONLY literal strings and `toFixed(3)` numerics; ONLY the audio buffer flows through stdin. Grep gate: `grep -E '\$\{[^}]*\}' src/backend/voice/audio-chunker.ts | grep -v 'toFixed\|thresholdDb\|minGapSec'` returns no user-input interpolation |
| T-100-01-02 | Denial of Service | scanSilenceGaps on adversarial audio | accept | Bounded by upstream 25 MB multer cap on voice.ts; ffmpeg is single-shot per request, subprocess auto-terminates on stdin end. No new attack surface vs Phase 98. |
| T-100-01-03 | Tampering | Malformed FLAC crashes ffmpeg subprocess | mitigate | Subprocess isolation (existing) — a crashed ffmpeg emits nonzero close code, chunker rejects the promise, caller (Wave 2 orchestrator) either retries the chunk or fails the whole request. No code exec into Node. Inherited from audio-transcode.ts pattern. |
| T-100-01-04 | Information Disclosure | ffmpeg stderr leaked into thrown Error message | accept | Same disposition as audio-transcode.ts — stderr text goes only to the Error passed up to voice.ts, which logs it server-side and returns fixed `{error:"STT error", status:502}` to clients (T-98-06-03). No client leak. |
</threat_model>

<verification>
- `npx vitest run --related src/backend/voice/semaphore.ts src/backend/voice/audio-chunker.ts src/backend/voice/audio-transcode.ts` — all three suites pass
- `npx tsc --noEmit` — zero errors introduced (the export change to runFfmpeg + new modules type-check clean)
- `grep -R "runFfmpeg" src/backend/voice/` — audit that audio-chunker.ts is the ONLY new consumer (voice.ts and transcribe-adapter.ts do NOT import runFfmpeg in this plan)
</verification>

<success_criteria>
- Wave 1 kernels are ready for Wave 2 assembly: createSemaphore is importable and thread-safe under async, audio-chunker exposes the 5 functions the orchestrator will call, runFfmpeg is a shared utility not a duplicated body.
- All new tests pass; no existing tests break.
- Zero user-input interpolation into any argv (T-100-01-01 grep gate satisfied).
</success_criteria>

<output>
Create `.planning/phases/100-stt-chunked-parallel-streaming-split-incoming-voice-audio-in/100-01-SUMMARY.md` when done. Summary must include: exact files created / modified, test count added (semaphore: 5, audio-chunker: 12+), any deviations from the plan (there should be none — this plan is pure kernel work), and a one-line handoff note to Plan 03 confirming `runFfmpeg` is now importable from `./audio-transcode.js`.
</output>
