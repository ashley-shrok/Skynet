---
phase: 100-stt-chunked-parallel-streaming
plan: 04
type: execute
wave: 3
depends_on:
  - 100-03
files_modified:
  - src/backend/database/routes/voice.ts
  - src/backend/database/routes/voice.test.ts
autonomous: true
requirements:
  - D-05
  - D-06
  - D-12
  - D-13
  - D-14
user_setup: []

must_haves:
  truths:
    - "Short clips (below the fast-path threshold) still take the EXISTING transcribeBuffer call (D-05) — unchanged code path, unchanged behavior"
    - "Long clips (at or above the threshold) route to transcribeBufferChunked instead, returning the same string type"
    - "The threshold is a byte-size proxy on the transcoded FLAC buffer (D-06 planner choice: byte-proxy over ffprobe for zero-overhead branch decision)"
    - "The threshold constant is 80_000 bytes (~10 s of clean speech FLAC — per RESEARCH § Section 4 recommendation)"
    - "The response body shape is preserved EXACTLY (D-12): {text: string} on success, {text: transformedText} when slash-transform fires, {error, status} on failure — no new fields, no per-chunk metadata"
    - "The slash-command transform (D-13) still runs on the STITCHED transcript from the chunked path — same code, same behavior"
    - "The disk-bank write of raw WebM (D-14, voice.ts:106-127) still fires BEFORE any transcode/chunk/dispatch — unchanged position"
    - "AccessDenied from the chunked path returns 503 via the existing catch block (same as single-stream path)"
    - "The existing 24 tests in voice.test.ts remain green; new tests EXTEND the suite"
  artifacts:
    - path: "src/backend/database/routes/voice.ts"
      provides: "handleTranscribe with fast-path gate branching to transcribeBufferChunked for long clips, transcribeBuffer for short clips; all other behavior unchanged"
    - path: "src/backend/database/routes/voice.test.ts"
      provides: "Existing 24 tests preserved + new tests for fast-path branching, chunked-path success, chunked-path AccessDenied handling"
  key_links:
    - from: "src/backend/database/routes/voice.ts"
      to: "src/backend/voice/transcribe-orchestrator.ts"
      via: "import { transcribeBufferChunked } from '../../voice/transcribe-orchestrator.js'"
      pattern: "import.*transcribeBufferChunked.*from.*transcribe-orchestrator"
    - from: "src/backend/database/routes/voice.ts"
      to: "src/backend/voice/transcribe-adapter.ts (existing)"
      via: "existing import of transcribeBuffer preserved for the fast-path branch"
      pattern: "import.*transcribeBuffer.*from.*transcribe-adapter"
---

<objective>
Wave 3 wiring for Phase 100. Add a fast-path gate to `handleTranscribe` in voice.ts that routes clips at/above `CHUNKED_THRESHOLD_BYTES` (80_000 bytes ≈ 10 s of speech FLAC) to the new `transcribeBufferChunked` orchestrator, while preserving the existing single-stream `transcribeBuffer` call for short clips (D-05 explicitly requires the fast path to be unchanged). Extend voice.test.ts with tests for both branches without breaking the 24 existing tests.

Purpose: This is the ONE small change that flips the switch. All heavy lifting shipped in Plans 01-03. This plan is intentionally minimal — a 4-line import + branch inside handleTranscribe plus 3 new test cases.

Output: 2 modified files (voice.ts + voice.test.ts). `npx vitest run src/backend/database/routes/voice.test.ts` passes with 24 existing + 3 new tests all green. Executor scope stops here — the deploy motion (push, force-recreate skynet, verify live) is orchestrator-owned per fleet 2026-08-08 rule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/100-stt-chunked-parallel-streaming-split-incoming-voice-audio-in/100-CONTEXT.md
@.planning/phases/100-stt-chunked-parallel-streaming-split-incoming-voice-audio-in/100-RESEARCH.md
@.planning/phases/100-stt-chunked-parallel-streaming-split-incoming-voice-audio-in/100-PATTERNS.md
@src/backend/database/routes/voice.ts
@src/backend/database/routes/voice.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fast-path gate to handleTranscribe (voice.ts) + extend voice.test.ts</name>
  <files>src/backend/database/routes/voice.ts, src/backend/database/routes/voice.test.ts</files>
  <read_first>
    - src/backend/database/routes/voice.ts (existing handleTranscribe at lines 97-206 — the ONLY function modified in this plan; slash-transform block at 149-184 UNTOUCHED; disk-bank write at 106-127 UNTOUCHED; catch block at 190-205 UNTOUCHED)
    - src/backend/database/routes/voice.test.ts (existing 24-test suite — new tests EXTEND the file, existing tests remain byte-for-byte unchanged; helper factories makeReq/makeRes on lines 123-216 are REUSED)
    - src/backend/voice/transcribe-orchestrator.ts (from Plan 03: transcribeBufferChunked signature `(flacBuf, sampleRateHz) =&gt; Promise&lt;string&gt;`)
    - .planning/phases/100-stt-chunked-parallel-streaming-split-incoming-voice-audio-in/100-CONTEXT.md (D-05 fast-path, D-06 threshold choice, D-12/D-13/D-14 preservation rules)
    - .planning/phases/100-stt-chunked-parallel-streaming-split-incoming-voice-audio-in/100-RESEARCH.md (Section 4 — WHY byte-proxy over ffprobe: overhead-avoidance for the branch decision; 80_000 bytes is the safe threshold for "almost certainly &gt; 10 s at any FLAC compression ratio for clean speech")
    - .planning/phases/100-stt-chunked-parallel-streaming-split-incoming-voice-audio-in/100-PATTERNS.md (voice.ts + voice.test.ts MODIFY sections — the exact insertion points and test shapes)
  </read_first>
  <behavior>
    - Test 1 (fast-path preserved): webmToFlac mock returns a Buffer of length 10_000 bytes (well below threshold). handleTranscribe called with a webm mock request. Assert transcribeSendMock (the existing single-stream mock) was called; transcribeBufferChunked mock was NOT called; response status 200; response body `{text: "hello world"}`. All existing 24 tests already assert this shape for short clips — they must continue to pass without modification.
    - Test 2 (chunked-path routing): webmToFlac mock returns a Buffer of length 100_000 bytes (above threshold). transcribeBufferChunked mock returns "chunked transcript from parallel path". Assert transcribeBufferChunked was called with the FLAC buffer AND sampleRateHz=16000; transcribeSendMock was NOT called (single-stream path bypassed); response 200; response body `{text: "chunked transcript from parallel path"}`.
    - Test 3 (chunked-path AccessDenied → 503): webmToFlac returns 100_000 bytes; transcribeBufferChunked mock rejects with an AccessDenied error (constructed via existing makeAccessDeniedError factory). Assert response 503; response body `{error: "voice STT unavailable", status: 503}`; databaseLogger.info called with `operation: "voice_transcribe_access_denied"` (same log op as the single-stream path — reuses the existing catch block).
    - Test 4 (chunked-path generic failure → 502): webmToFlac returns 100_000 bytes; transcribeBufferChunked rejects with `new Error("network timeout")`. Assert response 502; response body `{error: "STT error", status: 502}` (existing shape from voice.ts lines 200-204).
    - Test 5 (D-13 slash-transform still fires on chunked output): webmToFlac returns 100_000 bytes; transcribeBufferChunked returns "slash test hello"; hostId is provided in req.body; fetchSkillCatalog mock returns a matching skill. Assert the returned text has been transformed via applyServerSlashTransform (existing test pattern — extend the existing slash-transform test scenario to also cover the chunked-path input).
    - Test 6 (D-14 disk-bank write still fires for chunked path): webmToFlac returns 100_000 bytes; assert fs.promises.mkdir and fs.promises.writeFile mocks were called BEFORE transcribeBufferChunked (assertion via mock.invocationCallOrder or by asserting the disk-bank happens regardless of path taken). Existing tests already verify this for the single-stream path — the chunked path must preserve it.
    - Test 7 (existing 24 tests remain green): explicit regression check — after adding Tests 1-6, `npx vitest run src/backend/database/routes/voice.test.ts` reports 24 + 6 = 30 tests all passing (or whatever the actual existing count is; ensure zero regressions).
  </behavior>
  <action>
    Edit `src/backend/database/routes/voice.ts`:

    Step A: Add import AFTER the existing `import { transcribeBuffer } from "../../voice/transcribe-adapter.js";` (line 17). New line: `import { transcribeBufferChunked } from "../../voice/transcribe-orchestrator.js";`

    Step B: Add a module-level constant AFTER the existing constants block (after `SAMPLE_PHRASE` on line 38). New constant: `export const CHUNKED_THRESHOLD_BYTES = 80_000;` with an inline comment: `// D-05/D-06 fast-path threshold. ~10 s of clean-speech FLAC at 16 kHz mono (see 100-RESEARCH.md § Section 4 for the byte-proxy rationale — chosen over ffprobe to avoid a ~10 ms spawn on every request; conservative upper bound at 80 KB catches any clip that could reasonably benefit from parallel dispatch).`. Exported so tests can reference the exact value.

    Step C: Modify handleTranscribe (lines 97-206). The ONLY change is in the `try` block at lines 131-142. Replace the single unconditional call to `transcribeBuffer` with a fast-path branch. Current shape (lines 137-142):
    ```
    // (c) Push the buffer through Amazon Transcribe streaming.
    const transcript = await transcribeBuffer(
      transcodeResult.buffer,
      transcodeResult.mediaEncoding,
      transcodeResult.sampleRateHz,
    );
    ```

    New shape:
    ```
    // (c) Dispatch to chunked orchestrator for long clips; single-stream for short.
    // D-05: below-threshold clips take the existing single-stream path unchanged.
    // D-06: byte-proxy avoids ffprobe overhead on every request.
    const transcript =
      transcodeResult.buffer.length >= CHUNKED_THRESHOLD_BYTES
        ? await transcribeBufferChunked(
            transcodeResult.buffer,
            transcodeResult.sampleRateHz,
          )
        : await transcribeBuffer(
            transcodeResult.buffer,
            transcodeResult.mediaEncoding,
            transcodeResult.sampleRateHz,
          );
    ```

    Update the log line at 144-147 to include which path was taken for observability:
    ```
    databaseLogger.info(
      `[voice-server] transcribe-ok textLen=${transcript.length} mediaEncoding=${transcodeResult.mediaEncoding} path=${transcodeResult.buffer.length >= CHUNKED_THRESHOLD_BYTES ? "chunked" : "single"}`,
      { operation: "voice_transcribe", path: transcodeResult.buffer.length >= CHUNKED_THRESHOLD_BYTES ? "chunked" : "single" },
    );
    ```

    DO NOT touch:
    - The disk-bank write block at lines 106-127 (D-14 preservation)
    - The slash-transform block at lines 149-184 (D-13 preservation — runs on `transcript` regardless of path)
    - The response shape at lines 186-189 (D-12 preservation — same `{text}` and `{text: transformedText}`)
    - The catch block at lines 190-205 (AccessDenied → 503, else → 502; reused for BOTH paths)
    - The transcodeForTranscribe function (lines 79-93) — unchanged
    - handleSpeak, handleSpeakStream, rejectBridgeServiceOnSpeak, the router registration — all unchanged (Phase 100 is voice-in only)

    Edit `src/backend/database/routes/voice.test.ts`:

    Step D: Add a NEW vi.mock block AFTER the existing `vi.mock("../../voice/audio-transcode.js", ...)` (line 72). New mock:
    ```
    vi.mock("../../voice/transcribe-orchestrator.js", () =&gt; ({
      transcribeBufferChunked: vi.fn(),
    }));
    ```

    Step E: Add an import AFTER the existing `import { webmToFlac } from "../../voice/audio-transcode.js";` (line 116):
    ```
    import { transcribeBufferChunked } from "../../voice/transcribe-orchestrator.js";
    ```
    Also import the exported `CHUNKED_THRESHOLD_BYTES` from `./voice.js` alongside the existing named imports at line 108-114 so tests can size buffers against the real threshold.

    Step F: Add a NEW describe block at the END of the file (after the last existing test): `describe("handleTranscribe — Phase 100 chunked path routing", () =&gt; { ... })`. Inside, implement Tests 1-6 above using the existing `makeReq` / `makeRes` factories. For Test 5, either extend the existing slash-transform test OR duplicate its setup into a chunked-input variant. For Test 6, use `vi.mocked(fs.promises.writeFile).mock.invocationCallOrder[0]` compared to `vi.mocked(transcribeBufferChunked).mock.invocationCallOrder[0]` to assert ordering; alternatively assert both were called during the request (weaker but sufficient).

    Ensure `beforeEach` resets ALL mocks including the new `transcribeBufferChunked` mock — add `vi.mocked(transcribeBufferChunked).mockReset()` to whichever existing beforeEach applies to this describe block, or add a fresh beforeEach inside the new describe.

    IMPORTANT: The existing `beforeEach` in the file already runs `vi.clearAllMocks()` or similar — verify by reading voice.test.ts. If it does, no explicit reset needed. If it uses per-mock resets, add the new mock to that list.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tabitha &amp;&amp; npx vitest run src/backend/database/routes/voice.test.ts 2&gt;&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -n 'import { transcribeBufferChunked } from "../../voice/transcribe-orchestrator.js"' src/backend/database/routes/voice.ts` returns exactly one match
    - Source: `grep -n "^export const CHUNKED_THRESHOLD_BYTES = 80_000" src/backend/database/routes/voice.ts` returns exactly one match
    - Source: `grep -c "transcribeBufferChunked(" src/backend/database/routes/voice.ts` == 1 (called exactly once, inside handleTranscribe's branch)
    - Source: `grep -c "transcribeBuffer(" src/backend/database/routes/voice.ts` &gt;= 1 (single-stream path preserved for short clips)
    - Source: `grep -n "CHUNKED_THRESHOLD_BYTES" src/backend/database/routes/voice.ts` returns at least 3 matches (declaration + branch condition + log)
    - Source: `git diff src/backend/database/routes/voice.ts` shows changes ONLY in: (a) the import block near line 17, (b) the constants block after line 38, (c) inside handleTranscribe's try-block replacing the single transcribeBuffer call, (d) the log line — NO changes to disk-bank write, slash-transform, catch block, handleSpeak, handleSpeakStream, or router registration
    - Source (D-14 preservation): `grep -c "voice_transcribe_bank_write" src/backend/database/routes/voice.ts` &gt;= 1 (disk-bank log op still present)
    - Source (D-13 preservation): `grep -c "applyServerSlashTransform" src/backend/database/routes/voice.ts` == 1 (slash-transform still invoked)
    - Source (D-12 preservation): `grep -c 'res.status(200).json({ text' src/backend/database/routes/voice.ts` == 2 (both the transformedText path AND the raw-transcript path unchanged)
    - Test: `npx vitest run src/backend/database/routes/voice.test.ts` reports ZERO failures. Expected test count &gt;= 27 (24 existing + at least 3 new: Test 1 may map onto an existing test if renaming works). Grep sanity check: `grep -c "it(" src/backend/database/routes/voice.test.ts` &gt;= 27.
    - Regression: `npx vitest run --related src/backend/voice/transcribe-orchestrator.ts src/backend/voice/transcribe-adapter.ts src/backend/voice/audio-chunker.ts src/backend/voice/word-stitcher.ts src/backend/voice/semaphore.ts src/backend/voice/audio-transcode.ts` — ALL Wave 1 + Wave 2 tests remain green (this plan does NOT modify those files)
    - Type check: `npx tsc --noEmit` — zero new errors
    - Scope check: this plan does NOT include a deploy step, does NOT run the full test suite, and does NOT include any `[BLOCKING]` gates for skynet container recreation. Deploy is orchestrator territory per fleet 2026-08-08 rule.
  </acceptance_criteria>
  <done>voice.ts's handleTranscribe now dispatches long clips (buffer.length &gt;= 80_000) to transcribeBufferChunked and short clips to the existing transcribeBuffer. CHUNKED_THRESHOLD_BYTES is exported for tests. The disk-bank write, slash-transform, response shape, and catch block are byte-for-byte preserved (D-12/D-13/D-14). voice.test.ts extends with 3+ new tests for the branching, chunked success, chunked AccessDenied, and chunked generic-failure — all existing 24 tests remain green. `npx tsc --noEmit` clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| multipart file → handleTranscribe | Already authenticated (JWT before multer per T-16-04); size-capped at 25 MB per T-16-01 |
| transcript → response body | D-12: only `{text: string}` or `{text: transformedText}` — no per-chunk metadata leaked |
| chunked-path error → response | D-12: AccessDenied → fixed `{error: "voice STT unavailable", status: 503}`; else → fixed `{error: "STT error", status: 502}` — same shapes as the single-stream path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-04-01 | Information Disclosure | Log line reveals path taken to unauthorized log-viewers | accept | The `path=chunked` field is diagnostic; server-side log only; no client leak. Same disclosure profile as existing `mediaEncoding=flac` log field. |
| T-100-04-02 | Denial of Service | Attacker uploads exactly-at-threshold clips to force chunked-path overhead on every request | accept | The chunked path is FASTER than single-stream for &gt;=10 s audio and only marginally slower for clips right at the threshold (bounded ffprobe + silencedetect ~50 ms overhead). Semaphore of 5 caps total concurrent Transcribe streams regardless. Multer 25 MB cap upstream. No new cost amplification. |
| T-100-04-03 | Tampering | Modification breaks the D-14 disk-bank write invariant | mitigate | Acceptance criteria explicitly grep for `voice_transcribe_bank_write` log op preservation. The disk-bank block at lines 106-127 is untouched by this plan's diff — verifiable via `git diff`. |
| T-100-04-04 | Tampering | Modification silently drops the slash-transform for the chunked path | mitigate | Acceptance criteria greps for `applyServerSlashTransform` still called exactly once (D-13). Test 5 explicitly asserts the slash-transform fires for chunked-path output. |
</threat_model>

<verification>
- `npx vitest run src/backend/database/routes/voice.test.ts` — all tests (existing 24 + new 3+) pass
- `npx vitest run --related src/backend/voice/` — full voice module suite green
- `npx tsc --noEmit` — zero new type errors
- `git diff src/backend/database/routes/voice.ts` — diff is confined to the import, constant, one branch inside handleTranscribe's try-block, and one log-line update; NO changes elsewhere in the file
</verification>

<success_criteria>
- The fast-path gate is wired: short clips take the existing single-stream path unchanged (D-05), long clips route to the Wave 2 orchestrator.
- All D-12/D-13/D-14 preservation rules verified via grep + test.
- The 24 existing voice.test.ts tests remain green; 3+ new tests cover the new branching.
- Executor scope ends here. Deploy (skynet-tools push, force-recreate, verify) is orchestrator-owned.
</success_criteria>

<output>
Create `.planning/phases/100-stt-chunked-parallel-streaming-split-incoming-voice-audio-in/100-04-SUMMARY.md` when done. Summary must include: exact diff summary of voice.ts (line ranges added/modified), exact test count in voice.test.ts before and after, confirmation that `npx tsc --noEmit` is clean, and an explicit "READY FOR DEPLOY" note listing the files the orchestrator will need to push (all files under src/backend/voice/ plus src/backend/database/routes/voice.ts and voice.test.ts). Do NOT include deploy commands in the summary — that's the orchestrator's job.
</output>
