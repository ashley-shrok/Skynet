---
phase: 100-stt-chunked-parallel-streaming
plan: 03
type: execute
wave: 2
depends_on:
  - 100-01
  - 100-02
files_modified:
  - src/backend/voice/transcribe-adapter.ts
  - src/backend/voice/transcribe-adapter.test.ts
  - src/backend/voice/transcribe-orchestrator.ts
  - src/backend/voice/transcribe-orchestrator.test.ts
autonomous: true
requirements:
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
user_setup: []

must_haves:
  truths:
    - "transcribeBufferWithItems is a NEW export from transcribe-adapter.ts that mirrors transcribeBuffer's stream-loop but ALSO collects alt.Items into a returned array; transcribeBuffer itself is UNMODIFIED"
    - "transcribeBufferChunked is the exported entry point; it probes duration, scans silence, computes boundaries, slices FLAC per boundary, fans out N Transcribe calls under a module-level semaphore of 5, retries each chunk once, inserts [...] on double failure, and stitches the results via word-stitcher"
    - "AccessDenied on ANY chunk propagates out of transcribeBufferChunked WITHOUT being caught by the retry loop (Pitfall 5)"
    - "The module-level createSemaphore(5) singleton is constructed ONCE at module load (D-10, mirrors client singleton at transcribe-adapter.ts:93)"
    - "Every dispatched chunk emits structured logs: voice_transcribe_chunk_start (before send), voice_transcribe_chunk_ok (on success), voice_transcribe_chunk_retry (after first failure), voice_transcribe_chunk_failed_gap (after second failure), voice_transcribe_stitch_complete (once at end with textLen and chunkCount)"
    - "The existing 24-test voice.test.ts suite AND the transcribe-adapter.test.ts suite remain green — new tests EXTEND, they do not replace"
  artifacts:
    - path: "src/backend/voice/transcribe-adapter.ts"
      provides: "Existing transcribeBuffer UNCHANGED + NEW export transcribeBufferWithItems returning {transcript, items}"
      exports: ["transcribeBuffer", "transcribeBufferWithItems", "TranscribeChunkResult"]
    - path: "src/backend/voice/transcribe-adapter.test.ts"
      provides: "Existing tests preserved + new tests for transcribeBufferWithItems (Items collected, IsPartial still filtered, same singleton client used)"
    - path: "src/backend/voice/transcribe-orchestrator.ts"
      provides: "transcribeBufferChunked entry point; module-level semaphore; per-chunk retry with gap marker; Items-collecting fan-out; stitch via word-stitcher"
      exports: ["transcribeBufferChunked"]
    - path: "src/backend/voice/transcribe-orchestrator.test.ts"
      provides: "Unit tests: fan-out concurrency respects N=5, single-failure retries, double-failure yields [...], AccessDenied propagates unchanged, stitch is invoked with correct chunk offsets"
  key_links:
    - from: "src/backend/voice/transcribe-orchestrator.ts"
      to: "src/backend/voice/transcribe-adapter.ts"
      via: "import { transcribeBufferWithItems } from './transcribe-adapter.js'"
      pattern: "import.*transcribeBufferWithItems.*from.*transcribe-adapter"
    - from: "src/backend/voice/transcribe-orchestrator.ts"
      to: "src/backend/voice/audio-chunker.ts"
      via: "import { probeDuration, scanSilenceGaps, parseSilenceGaps, computeChunkBoundaries, sliceFlac } from './audio-chunker.js'"
      pattern: "import.*(probeDuration|scanSilenceGaps|sliceFlac).*from.*audio-chunker"
    - from: "src/backend/voice/transcribe-orchestrator.ts"
      to: "src/backend/voice/word-stitcher.ts"
      via: "import { stitchChunks, type ChunkResult } from './word-stitcher.js'"
      pattern: "import.*stitchChunks.*from.*word-stitcher"
    - from: "src/backend/voice/transcribe-orchestrator.ts"
      to: "src/backend/voice/semaphore.ts"
      via: "import { createSemaphore } from './semaphore.js'"
      pattern: "import.*createSemaphore.*from.*semaphore"
    - from: "src/backend/voice/transcribe-orchestrator.ts"
      to: "src/backend/voice/aws-errors.ts"
      via: "import { isAwsAccessDenied } from './aws-errors.js'"
      pattern: "import.*isAwsAccessDenied.*from.*aws-errors"
---

<objective>
Wave 2 assembly for Phase 100 chunked STT: (1) extend transcribe-adapter.ts with a NEW `transcribeBufferWithItems` export that mirrors the existing `transcribeBuffer` loop but ALSO collects `alt.Items`, and (2) create transcribe-orchestrator.ts which composes the Wave 1 kernels (semaphore, audio-chunker, word-stitcher) with the new adapter function to deliver the chunked-parallel entry point `transcribeBufferChunked(flacBuf, sampleRateHz)`.

Purpose: This is the plan that ACTUALLY delivers the 5-10× speedup. Wave 1 gave us the parts; this plan assembles them into the working chunked path. The result is a drop-in replacement for `transcribeBuffer` (same string return type) that voice.ts will branch to in Wave 3 (Plan 04).

Output: 2 modified files (transcribe-adapter.ts extended with new export + tests) and 2 new files (transcribe-orchestrator.ts + its tests). All existing tests remain green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/100-stt-chunked-parallel-streaming-split-incoming-voice-audio-in/100-CONTEXT.md
@.planning/phases/100-stt-chunked-parallel-streaming-split-incoming-voice-audio-in/100-RESEARCH.md
@.planning/phases/100-stt-chunked-parallel-streaming-split-incoming-voice-audio-in/100-PATTERNS.md
@src/backend/voice/transcribe-adapter.ts
@src/backend/voice/transcribe-adapter.test.ts
@src/backend/voice/aws-errors.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add transcribeBufferWithItems export to transcribe-adapter.ts + tests</name>
  <files>src/backend/voice/transcribe-adapter.ts, src/backend/voice/transcribe-adapter.test.ts</files>
  <read_first>
    - src/backend/voice/transcribe-adapter.ts (existing transcribeBuffer at lines 142-172 — the shape being cloned + Items-extended; the module-level client at line 93 is REUSED)
    - src/backend/voice/transcribe-adapter.test.ts (existing test suite: mock harness lines 24-47, fakeTranscriptStream at 53-74 — new tests use these existing helpers and add an Items-aware variant)
    - .planning/phases/100-stt-chunked-parallel-streaming-split-incoming-voice-audio-in/100-RESEARCH.md (Section 1 — SDK type declaration for Item + Alternative; Pitfall 8 — WHY a parallel function, not a modification)
    - .planning/phases/100-stt-chunked-parallel-streaming-split-incoming-voice-audio-in/100-PATTERNS.md (transcribe-adapter.ts MODIFY section — the exact code block to add after line 173)
  </read_first>
  <behavior>
    - Test 1 (Items collection): a fakeTranscriptStreamWithItems yielding one final result with Alternatives=[{Transcript:"hello world", Items:[{Type:"pronunciation",Content:"hello",StartTime:0,EndTime:0.5},{Type:"pronunciation",Content:"world",StartTime:0.5,EndTime:1.0}]}] returns from transcribeBufferWithItems as `{transcript:"hello world", items:[<2 items with the exact Content/StartTime/EndTime]}`.
    - Test 2 (IsPartial filter preserved / Pitfall 3): a stream yielding two events — first `{IsPartial:true, transcript:"hel", items:[<hel-partial-items>]}` and then `{IsPartial:false, transcript:"hello world", items:[<final items>]}` — returns ONLY the final's transcript and items. The partial's items are DROPPED (same guard as the existing transcribeBuffer).
    - Test 3 (empty Items array): a final result with `Alternatives=[{Transcript:"hmm", Items:[]}]` returns `{transcript:"hmm", items:[]}` — no crash, empty items array preserved.
    - Test 4 (missing Items field): a final result with `Alternatives=[{Transcript:"hmm"}]` (no Items property) returns `{transcript:"hmm", items:[]}` — defensive `alt.Items ?? []` guard.
    - Test 5 (missing TranscriptResultStream): same throw behavior as transcribeBuffer — `throw new Error("Transcribe returned no TranscriptResultStream")`.
    - Test 6 (module-level singleton reuse / D-10): transcribeBufferWithItems and transcribeBuffer share the SAME `client` module-level singleton — the ctor counter stays at 1 after both are imported (existing test line 77 asserts count is 1 after just transcribe-adapter import; adding transcribeBufferWithItems must NOT construct another client).
    - Test 7 (existing transcribeBuffer tests unchanged): the existing suite's 8+ tests continue to pass verbatim. No regressions.
  </behavior>
  <action>
    Edit `src/backend/voice/transcribe-adapter.ts`. Do NOT touch the existing transcribeBuffer function (lines 142-172) or the module-level singleton client (line 93) — this is an ADD-ONLY change that appends a parallel function AFTER the existing exports.

    Step A: Add `type Item` to the existing SDK import block at lines 78-84. Change from `import { TranscribeStreamingClient, StartStreamTranscriptionCommand, type AudioStream, type LanguageCode, type MediaEncoding } from "@aws-sdk/client-transcribe-streaming";` to include `type Item`. Result: `import { TranscribeStreamingClient, StartStreamTranscriptionCommand, type AudioStream, type Item, type LanguageCode, type MediaEncoding } from "@aws-sdk/client-transcribe-streaming";`.

    Step B: AFTER the existing `transcribeBuffer` function (append after line 173), add:

    - `export interface TranscribeChunkResult { transcript: string; items: Item[]; }` — with a docblock explaining `items` timestamps are stream-relative (per Pitfall 1); consumers must add chunk startOffsetSec before cross-chunk comparison.

    - `export async function transcribeBufferWithItems(audioBuffer: Buffer, mediaEncoding: "flac" | "ogg-opus" | "pcm" = "flac", sampleRateHz: number = 16000): Promise&lt;TranscribeChunkResult&gt;` — body mirrors transcribeBuffer's structure but with an additional `const items: Item[] = []` accumulator. Uses the SAME module-level `client` and SAME `audioChunkGenerator`. The for-await loop uses IDENTICAL structure to transcribeBuffer (IsPartial filter preserved verbatim) but adds `if (alt.Items) items.push(...alt.Items);` alongside the existing `if (alt.Transcript) transcripts.push(alt.Transcript);`. Return `{transcript: transcripts.join(" "), items}`.

    Docblock for the new function: reference D-11 (word-timestamp stitching), Pitfall 3 (IsPartial filter — same as transcribeBuffer), and Pitfall 8 (WHY a parallel function: transcribeBuffer's `Promise&lt;string&gt;` signature is load-bearing for 24 existing tests + the fast-path caller; adding Items requires a widened return type, so we introduce a sibling export instead of mutating the existing one).

    Update transcribe-adapter.test.ts (do NOT delete or modify any existing test):

    Step C: Add a new `fakeTranscriptStreamWithItems` helper AFTER the existing `fakeTranscriptStream` helper (line 74). Same shape but each yielded event's Alternatives entry accepts an optional `Items` field. Signature: `function fakeTranscriptStreamWithItems(results: Array&lt;{IsPartial: boolean; transcript: string; items?: unknown[]}&gt;): AsyncIterable&lt;unknown&gt;`.

    Step D: Add a new top-level describe block "transcribe-adapter — transcribeBufferWithItems (Phase 100)" implementing tests 1-6 above. Import `transcribeBufferWithItems` from `./transcribe-adapter.js` (extend the existing `const { transcribeBuffer } = await import(...)` line to also destructure `transcribeBufferWithItems`).

    Verify the existing "constructs TranscribeStreamingClient exactly once at module load" test (line 77) continues to pass with `transcribeClientCtor.mock.calls.length === 1` — meaning the new function did NOT introduce a second client construction.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tabitha &amp;&amp; npx vitest run --related src/backend/voice/transcribe-adapter.ts 2&gt;&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "^export async function transcribeBuffer[A-Z]" src/backend/voice/transcribe-adapter.ts` == 2 (transcribeBuffer + transcribeBufferWithItems)
    - Source: `grep -c "^export interface TranscribeChunkResult" src/backend/voice/transcribe-adapter.ts` == 1
    - Source: `grep -n "type Item" src/backend/voice/transcribe-adapter.ts` returns one hit in the SDK import block
    - Source: `grep -n "new TranscribeStreamingClient" src/backend/voice/transcribe-adapter.ts` returns exactly ONE match (D-10 singleton preserved — the new function does NOT construct another client)
    - Source: existing transcribeBuffer function body (lines 142-172 approximately) has NOT been altered — `git diff src/backend/voice/transcribe-adapter.ts` shows changes ONLY in the import block AND appended after line 173, NOT inside the existing function
    - Source: `grep -c "if (result.IsPartial) continue" src/backend/voice/transcribe-adapter.ts` == 2 (Pitfall 3 preserved in BOTH functions)
    - Test: `npx vitest run --related src/backend/voice/transcribe-adapter.ts` reports zero failures. The existing test count remains and NEW tests for transcribeBufferWithItems (&gt;= 5, covering the 6 behaviors above minus the singleton-reuse assertion which is already covered by the existing ctor-count test) are green.
    - Test coverage grep: `grep -c 'transcribeBufferWithItems' src/backend/voice/transcribe-adapter.test.ts` &gt;= 5 (referenced in test bodies)
    - Regression: `npx vitest run --related src/backend/voice/audio-transcode.ts` still green (no unintended coupling)
  </acceptance_criteria>
  <done>transcribe-adapter.ts exports transcribeBufferWithItems and TranscribeChunkResult alongside the untouched transcribeBuffer. The module-level TranscribeStreamingClient singleton is REUSED (single ctor call at module load). transcribe-adapter.test.ts contains the new describe block with 5+ tests covering Items collection, IsPartial filter, empty-Items, missing-Items, and missing-stream error path. All existing tests remain green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create transcribe-orchestrator module (transcribeBufferChunked) + tests</name>
  <files>src/backend/voice/transcribe-orchestrator.ts, src/backend/voice/transcribe-orchestrator.test.ts</files>
  <read_first>
    - src/backend/voice/transcribe-adapter.ts (POST-Task-1: transcribeBufferWithItems is the primary dependency)
    - src/backend/voice/audio-chunker.ts (from Plan 01: probeDuration, scanSilenceGaps, parseSilenceGaps, computeChunkBoundaries, sliceFlac, SilenceGap, ChunkBoundary types)
    - src/backend/voice/word-stitcher.ts (from Plan 02: stitchChunks, ChunkResult type)
    - src/backend/voice/semaphore.ts (from Plan 01: createSemaphore factory)
    - src/backend/voice/aws-errors.ts (isAwsAccessDenied classifier — CRITICAL for Pitfall 5)
    - src/backend/database/utils/logger.ts (databaseLogger — for structured op logs)
    - .planning/phases/100-stt-chunked-parallel-streaming-split-incoming-voice-audio-in/100-RESEARCH.md (Sections 5, 7, 10; Pitfall 5)
    - .planning/phases/100-stt-chunked-parallel-streaming-split-incoming-voice-audio-in/100-PATTERNS.md (transcribe-orchestrator.ts + transcribe-orchestrator.test.ts sections — full assembly pattern with mock setup)
    - .planning/phases/100-stt-chunked-parallel-streaming-split-incoming-voice-audio-in/100-CONTEXT.md (D-05 threshold, D-07 retry, D-08 gap marker, D-09 N=5, D-10 module-level singleton, D-11 stitcher)
  </read_first>
  <behavior>
    - Test 1 (happy path — 3 chunks, all succeed): probeDuration mocked to return 24 (seconds), scanSilenceGaps returns stderr with 2 gaps at [7.5, 8.1] and [15.5, 16.1], sliceFlac mocked to return distinct buffers per call, transcribeBufferWithItems mocked to return distinct {transcript, items} per chunk, stitchChunks mocked to return "stitched result". Expected: transcribeBufferChunked returns "stitched result"; transcribeBufferWithItems is called exactly 3 times; stitchChunks is called with 3 ChunkResult items whose startOffsetSec values match the computed boundaries.
    - Test 2 (single chunk edge case — duration below or equal to threshold within chunked path): probeDuration returns 6 (seconds — smaller than targetChunkSec of 8), boundaries produces exactly 1 boundary. Orchestrator dispatches ONE transcribeBufferWithItems call and returns its transcript directly (stitchChunks may be called with length-1 input which returns the single transcript).
    - Test 3 (retry on transient failure — D-07): 5 chunks, chunk index 2 fails on first call (throws non-AccessDenied error), succeeds on retry. Expected: transcribeBufferWithItems is called 6 times total (5 initial + 1 retry for chunk 2); databaseLogger.warn is called with `operation: "voice_transcribe_chunk_retry"` and `chunkIndex: 2`; final result contains chunk 2's retry-transcript in position.
    - Test 4 (double failure → gap marker `[...]` — D-08): chunk index 3 fails both attempts. Expected: databaseLogger.warn called with `operation: "voice_transcribe_chunk_failed_gap"` and `chunkIndex: 3`; stitchChunks is called with ChunkResult items where index 3 has `transcript: "[...]"` and `items: []`. Final output contains `[...]` in the appropriate position (via the word-stitcher's gap passthrough behavior tested in Plan 02).
    - Test 5 (AccessDenied propagates / Pitfall 5): chunk index 1 throws AccessDeniedException on FIRST attempt. Expected: transcribeBufferChunked's promise REJECTS with the AccessDenied error unchanged; NO retry is attempted for chunk 1; other chunks may still be in flight but their results are discarded (Promise.all rejects on first rejection).
    - Test 6 (semaphore respects N=5 concurrency): dispatch 8 chunks where each transcribeBufferWithItems mock is a controllable promise (Promise.withResolvers). Assert that at any point in time, at most 5 promises are pending. Increment a `pending` counter in the mock impl, decrement on resolve, track `maxSeen` — assert `maxSeen === 5`.
    - Test 7 (structured logging op names): after a happy-path run with N chunks, assert databaseLogger.info was called with `operation: "voice_transcribe_chunk_start"` N times, `operation: "voice_transcribe_chunk_ok"` N times, and `operation: "voice_transcribe_stitch_complete"` exactly once (with a `chunkCount` and `textLen` field).
    - Test 8 (chunk offset assignment): each ChunkResult passed to stitchChunks has `startOffsetSec` matching the corresponding boundary's `startSec` from computeChunkBoundaries. Verify by inspecting the args to stitchChunks (mocked).
  </behavior>
  <action>
    Create `src/backend/voice/transcribe-orchestrator.ts`. Module docblock cites CONTEXT D-07, D-08, D-09, D-10, D-11; RESEARCH Sections 5+7+10 and Pitfalls 1, 5, 6, 8; PATTERNS transcribe-orchestrator.ts section.

    Imports (all `.js` suffixed for NodeNext ESM):
    - `import { transcribeBufferWithItems, type TranscribeChunkResult } from "./transcribe-adapter.js";`
    - `import { probeDuration, scanSilenceGaps, parseSilenceGaps, computeChunkBoundaries, sliceFlac, type SilenceGap, type ChunkBoundary } from "./audio-chunker.js";`
    - `import { stitchChunks, type ChunkResult } from "./word-stitcher.js";`
    - `import { createSemaphore } from "./semaphore.js";`
    - `import { isAwsAccessDenied } from "./aws-errors.js";`
    - `import { databaseLogger } from "../database/utils/logger.js";`

    Constants:
    - `const CONCURRENCY_LIMIT = 5;` — D-09 (well under 25-stream account quota)
    - `const TARGET_CHUNK_SEC = 8;` — D-03
    - `const OVERLAP_SEC = 2.5;` — D-04 (midpoint of the 2–3 s planner-picked range)
    - `const MIN_CHUNK_SEC = 1.0;` — Pitfall 8 minimum-duration guard

    Module-level singleton (D-10): `const transcribeSemaphore = createSemaphore(CONCURRENCY_LIMIT);` — constructed ONCE at import; docblock explains this mirrors the TranscribeStreamingClient singleton at transcribe-adapter.ts:93.

    Internal type: `interface ChunkInput { index: number; startSec: number; endSec: number; buf: Buffer; }` — bundles the boundary metadata with the sliced FLAC buffer that transcribeChunkOnce needs.

    Internal helper `async function transcribeChunkOnce(chunk: ChunkInput, sampleRateHz: number): Promise&lt;ChunkResult&gt;`:
    - Log `voice_transcribe_chunk_start` with `{chunkIndex: chunk.index, startSec: chunk.startSec, endSec: chunk.endSec}` via databaseLogger.info.
    - Call `const {transcript, items} = await transcribeBufferWithItems(chunk.buf, "flac", sampleRateHz);`.
    - Log `voice_transcribe_chunk_ok` with `{chunkIndex: chunk.index, textLen: transcript.length, itemCount: items.length}`.
    - Return `{startOffsetSec: chunk.startSec, endOffsetSec: chunk.endSec, transcript, items}` — the ChunkResult shape expected by stitchChunks.

    Internal helper `async function transcribeChunkWithRetry(chunk: ChunkInput, sampleRateHz: number): Promise&lt;ChunkResult&gt;`:
    - Try transcribeChunkOnce once. On error: check `isAwsAccessDenied(err)` FIRST — if true, re-throw immediately (Pitfall 5 — do NOT catch/swallow into retry). Otherwise, log `voice_transcribe_chunk_retry` at warn level with `{chunkIndex: chunk.index, error: (err instanceof Error) ? err.message : String(err)}` and attempt transcribeChunkOnce ONCE MORE (per D-07: retry exactly once, no backoff). On the second failure: check isAwsAccessDenied again (re-throw if so), else log `voice_transcribe_chunk_failed_gap` at warn level with `{chunkIndex: chunk.index}` and return the sentinel `ChunkResult` for gap-marker: `{startOffsetSec: chunk.startSec, endOffsetSec: chunk.endSec, transcript: "[...]", items: []}`.

    Exported entry point `export async function transcribeBufferChunked(flacBuf: Buffer, sampleRateHz: number = 16000): Promise&lt;string&gt;`:

    Step 1: `const totalDurationSec = await probeDuration(flacBuf);`

    Step 2: `const stderrText = await scanSilenceGaps(flacBuf);` then `const silenceGaps: SilenceGap[] = parseSilenceGaps(stderrText);`

    Step 3: `const boundaries: ChunkBoundary[] = computeChunkBoundaries(totalDurationSec, silenceGaps, TARGET_CHUNK_SEC, OVERLAP_SEC, MIN_CHUNK_SEC);`

    Step 4: Slice FLAC per boundary — `const chunks: ChunkInput[] = await Promise.all(boundaries.map(async (b, index) =&gt; ({ index, startSec: b.startSec, endSec: b.endSec, buf: await sliceFlac(flacBuf, b.startSec, Math.min(b.endSec - b.startSec, totalDurationSec - b.startSec)) })));` — note the Math.min guard so a boundary whose endSec (including overlap) exceeds totalDurationSec is clamped to the audio end.

    Step 5: Fan out with semaphore + retry — `const chunkResults: ChunkResult[] = await Promise.all(chunks.map((c) =&gt; transcribeSemaphore(() =&gt; transcribeChunkWithRetry(c, sampleRateHz))));` — Promise.all propagates any AccessDenied from Pitfall 5 immediately.

    Step 6: Stitch — `const finalText = stitchChunks(chunkResults, OVERLAP_SEC);`

    Step 7: Log stitch completion — `databaseLogger.info(`[voice-server] voice_transcribe_stitch_complete chunkCount=${chunks.length} textLen=${finalText.length}`, { operation: "voice_transcribe_stitch_complete", chunkCount: chunks.length, textLen: finalText.length });`

    Step 8: `return finalText;`

    IMPORTANT for gap-marker via stitcher (D-08): when a chunk returned the sentinel {transcript:"[...]", items:[]}, the word-stitcher's empty-Items fallback (Plan 02 Test 4) makes `[...]` appear verbatim in the merged output. The orchestrator does NOT need to do any additional gap-marker splicing.

    Create `src/backend/voice/transcribe-orchestrator.test.ts`. Copy the vi.hoisted + vi.mock pattern from transcribe-adapter.test.ts. Mock ALL sibling modules:
    - `vi.mock("./transcribe-adapter.js", () =&gt; ({ transcribeBufferWithItems: vi.fn() }))`
    - `vi.mock("./audio-chunker.js", () =&gt; ({ probeDuration: vi.fn(), scanSilenceGaps: vi.fn(), parseSilenceGaps: vi.fn(), computeChunkBoundaries: vi.fn(), sliceFlac: vi.fn() }))`
    - `vi.mock("./word-stitcher.js", () =&gt; ({ stitchChunks: vi.fn() }))`
    - `vi.mock("./aws-errors.js", () =&gt; ({ isAwsAccessDenied: vi.fn() }))`
    - `vi.mock("../database/utils/logger.js", () =&gt; ({ databaseLogger: { info: vi.fn(), warn: vi.fn(), error: vi.fn() } }))`
    - Do NOT mock `./semaphore.js` — use the real semaphore so Test 6 can verify concurrency behavior end-to-end.

    Implement the 8 behavior tests above. For Test 5 (AccessDenied), use the makeAccessDeniedError() factory pattern from voice.test.ts lines 249-256. Import `transcribeBufferChunked` from `./transcribe-orchestrator.js` AFTER vi.mock. Use `beforeEach` to reset all mocks.

    For Test 6 (semaphore concurrency), implement transcribeBufferWithItems mock as:
    ```
    let pending = 0; let maxSeen = 0;
    transcribeBufferWithItemsMock.mockImplementation(async () =&gt; {
      pending++; if (pending &gt; maxSeen) maxSeen = pending;
      await new Promise(resolve =&gt; setImmediate(resolve));  // yield the event loop
      pending--;
      return { transcript: "ok", items: [] };
    });
    ```
    Then assert `expect(maxSeen).toBe(5)` after Promise.all completes. Configure boundaries mock to return 8 boundaries so 8 chunks are dispatched.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tabitha &amp;&amp; npx vitest run --related src/backend/voice/transcribe-orchestrator.ts src/backend/voice/transcribe-adapter.ts 2&gt;&amp;1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "^export async function transcribeBufferChunked" src/backend/voice/transcribe-orchestrator.ts` == 1
    - Source: `grep -n "const transcribeSemaphore = createSemaphore(" src/backend/voice/transcribe-orchestrator.ts` returns exactly one match at MODULE level (D-10 singleton)
    - Source: `grep -c "isAwsAccessDenied" src/backend/voice/transcribe-orchestrator.ts` &gt;= 2 (checked BEFORE retry attempt AND before second-failure gap; Pitfall 5)
    - Source: `grep -c 'operation: "voice_transcribe_chunk_start"' src/backend/voice/transcribe-orchestrator.ts` == 1
    - Source: `grep -c 'operation: "voice_transcribe_chunk_ok"' src/backend/voice/transcribe-orchestrator.ts` == 1
    - Source: `grep -c 'operation: "voice_transcribe_chunk_retry"' src/backend/voice/transcribe-orchestrator.ts` == 1
    - Source: `grep -c 'operation: "voice_transcribe_chunk_failed_gap"' src/backend/voice/transcribe-orchestrator.ts` == 1
    - Source: `grep -c 'operation: "voice_transcribe_stitch_complete"' src/backend/voice/transcribe-orchestrator.ts` == 1
    - Source: `grep -n '"\\[\\.\\.\\.\\]"' src/backend/voice/transcribe-orchestrator.ts` returns exactly one match (the D-08 sentinel string emitted in the gap path). Filter comments/docblocks: `grep -v '^ \\*\\|^//' src/backend/voice/transcribe-orchestrator.ts | grep -c '"\\[\\.\\.\\.\\]"'` == 1.
    - Source: `grep -n "transcribeSemaphore(" src/backend/voice/transcribe-orchestrator.ts` returns one match (the fan-out wraps each chunk via the semaphore)
    - Source: `grep -c "^import" src/backend/voice/transcribe-orchestrator.ts` == 6 (transcribe-adapter, audio-chunker, word-stitcher, semaphore, aws-errors, databaseLogger)
    - Test: `npx vitest run --related src/backend/voice/transcribe-orchestrator.ts` reports zero failures. Expected test count &gt;= 8 (matches behavior list).
    - Test coverage grep: `grep -c "it(" src/backend/voice/transcribe-orchestrator.test.ts` &gt;= 8
    - Behavior — concurrency: Test 6 explicitly asserts `expect(maxSeen).toBe(5)` — grep the test file: `grep -c 'toBe(5)' src/backend/voice/transcribe-orchestrator.test.ts` &gt;= 1
    - Regression: `npx vitest run --related src/backend/voice/transcribe-adapter.ts src/backend/voice/audio-chunker.ts src/backend/voice/word-stitcher.ts src/backend/voice/semaphore.ts` — all Wave 1 + Task 1 tests remain green
  </acceptance_criteria>
  <done>transcribe-orchestrator.ts exports transcribeBufferChunked as a `(flacBuf, sampleRateHz) =&gt; Promise&lt;string&gt;` — the drop-in shape voice.ts will branch to. Module-level semaphore singleton at N=5. Per-chunk retry with AccessDenied bypass. Gap-marker `[...]` on double failure. Structured logs at all 5 op names. transcribe-orchestrator.test.ts covers all 8 behaviors including the semaphore-concurrency assertion (maxSeen === 5). All Wave 1 and transcribe-adapter tests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flacBuf → orchestrator | Already-transcoded audio from voice.ts's transcodeForTranscribe; not user text |
| Per-chunk buffer → AWS SDK | Same trust boundary as Phase 98 transcribeBuffer — inherits T-98 threat model |
| AWS response → local stitcher | Server-to-server; assumes AWS response is well-formed per SDK types |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-03-01 | Denial of Service | Adversarial audio triggers N→∞ chunks | mitigate | computeChunkBoundaries has an implicit ceiling (bounded by totalDurationSec / targetChunkSec); voice.ts inherits the existing 25 MB multer cap so totalDurationSec cannot exceed ~7 hours of FLAC. Semaphore of 5 caps concurrent AWS spend at 5 streams even for a 100-chunk request. |
| T-100-03-02 | Elevation of Privilege | AccessDenied swallowed by retry loop → wrong 502 instead of 503 | mitigate | Pitfall 5 explicitly asserted: isAwsAccessDenied is checked BEFORE the retry branch AND before the gap-marker branch. If AccessDenied, re-throw unchanged so voice.ts's existing catch returns 503. Grep gate + Test 5 verify this. |
| T-100-03-03 | Information Disclosure | Chunk-level error messages leaked into transcript | mitigate | Errors go to databaseLogger only (server-side). The transcript surface is either the successful `.transcript` string or the sentinel `[...]`. No error text leaks into the user-facing transcript. |
| T-100-03-04 | Tampering | Malicious FLAC produces an item whose Content is a shell metacharacter injected into a downstream log format | accept | databaseLogger emits structured JSON, not string interpolation. Item content is transcribed audio, not machine-generated; existing slash-transform reads `.text` only. No new injection surface. |
</threat_model>

<verification>
- `npx vitest run --related src/backend/voice/transcribe-orchestrator.ts src/backend/voice/transcribe-adapter.ts src/backend/voice/audio-chunker.ts src/backend/voice/word-stitcher.ts src/backend/voice/semaphore.ts` — full Wave 1 + Wave 2 suite passes
- `npx tsc --noEmit` — zero type errors (all Wave 1 exports type-check; the assembly correctly threads types through)
- `grep -R "transcribeBufferChunked" src/backend/` — voice.ts does NOT import it yet (Plan 04's job); only the orchestrator's own file + its test reference the name
</verification>

<success_criteria>
- Chunked path is READY for wiring: `transcribeBufferChunked` accepts a FLAC buffer and returns a stitched transcript string, identical shape to `transcribeBuffer`.
- All new tests pass; ALL existing tests (including the 24-test voice.test.ts suite and the transcribe-adapter suite) remain green.
- The 5 structured log op names are emitted correctly (verifiable via test mock assertions).
- AccessDenied propagates unchanged (Pitfall 5 preserved).
- Semaphore holds concurrency at 5 (Test 6 assertion).
</success_criteria>

<output>
Create `.planning/phases/100-stt-chunked-parallel-streaming-split-incoming-voice-audio-in/100-03-SUMMARY.md` when done. Summary must include: exact test counts added (transcribe-adapter: 5+, orchestrator: 8+), any deviations from RESEARCH.md's suggested shape (with justification), the exact `transcribeBufferChunked(flacBuf, sampleRateHz)` signature ready for Plan 04 to consume, and a note confirming voice.ts has NOT been touched in this plan.
</output>
