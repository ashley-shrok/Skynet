---
phase: 86-cosmetics-migrate-to-role
plan: 04
type: execute
wave: 2
depends_on: [86-01]  # 86-01 owns identities-api.ts in Wave 1; this plan touches a DIFFERENT exported symbol in the same file (BirthRequest, not createRole) as a small field-relaxation on top of 86-01's landed edits — sequence is clean.
files_modified:
  - src/ui/sidebar/NewSessionDialog.tsx
  - src/ui/api/identities-api.ts  # BirthRequest field-widening only (title, avatarCandidateId → optional); does NOT overlap textually with 86-01 Task 3's createRole widening
  - src/backend/database/routes/identity-birth.ts  # relax the title-required and avatarCandidateId-required gates at L116-124 so identities can be born with empty cosmetics that inherit from role
  - src/backend/database/routes/identity-birth-orchestrator.ts  # extends the identity-birth backend surface — Option A resolution of the Task 1 Step 4(iv) STOP-and-surface for the empty-avatarCandidateId path; orchestrator-approved 2026-09-08. Adds absent-avatar branches at Step 1 candidate lookup, Step 2.5 sibling-file write, and buildIdentityFileBody's avatar frontmatter emission (absent-⇒-omit invariant matching existing title/voice/task pattern).
autonomous: true
requirements: [D-CTX-86-surface-4]
must_haves:
  truths:
    - "NewSessionDialog identity-mode branch no longer renders Title input, Brief textarea, VoicePicker, ColorPicker, or avatar generator UI per D-CTX-86-surface-4"
    - "Birth stream call passes empty/absent cosmetic fields — identity is born wearing the role's face on landing"
    - "Non-identity-mode (raw shell) branch unchanged (never rendered cosmetic controls)"
    - "Identity name, host, role dropdown, path field, identity-mode checkbox, task field ALL still render (D-CTX-86-surface-4 explicit: cosmetic strip is orthogonal to these)"
    - "Associated state variables (title, brief, voice, colorHue, candidates, pickedCandidateId, manualPreviewUrl, gen/upload loading+error) removed from the component"
    - "BirthRequest type in src/ui/api/identities-api.ts widened: `title` and `avatarCandidateId` become optional (previously required strings) so the birthed identity inherits its face from the role per D-CTX-86-inherit"
    - "identity-birth.ts POST /identities/birth handler no longer 400-rejects requests with absent/empty `title` or absent/empty `avatarCandidateId` (existing L116-124 gates relaxed; downstream code that consumes those fields must handle absence gracefully — see step 4 in the action)"
  artifacts:
    - path: src/ui/sidebar/NewSessionDialog.tsx
      provides: "identity-mode branch stripped of cosmetic controls + associated state + handlers; birth stream call adapted"
    - path: src/ui/api/identities-api.ts
      provides: "BirthRequest field-relaxation: `title?: string` and `avatarCandidateId?: string` (both were required-string in the pre-Phase-85 shape); no other change to this file — 86-01 Task 3 owns the createRole widening on the same file"
    - path: src/backend/database/routes/identity-birth.ts
      provides: "POST /identities/birth handler no longer requires `title` or `avatarCandidateId` in the request body; downstream orchestrator invocation + candidate-consume paths adjusted for absence (see action step 4)"
  key_links:
    - from: src/ui/sidebar/NewSessionDialog.tsx handleBirth
      to: openBirthStream (BirthRequest)
      via: "cosmetic fields (title, voice, colorHue, avatarCandidateId) become empty/absent — backend accepts identity born with role-inherited cosmetics"
      pattern: "openBirthStream|BirthRequest"
---

<objective>
Strip cosmetic authoring controls from `NewSessionDialog.tsx` per D-CTX-86-surface-4. The face is now defined at role level (Plan 86-03 adds it to CreateRoleDialog); creating a new identity under an existing role no longer needs to author cosmetics — the identity wears its role's face on landing. Per-identity overrides remain possible later via IdentityModal (Plan 86-05).

Purpose: Wave-2 frontend delivery of D-CTX-86-surface-4. Runs in parallel with Plan 86-03 (different file). Does NOT depend on Plan 86-01 or 86-02 backend work — this is purely a deletion + call-site adaptation on the frontend.

Output: Slimmer `NewSessionDialog.tsx` with the identity-mode branch stripped of cosmetic UI + state + handlers; `openBirthStream` call passes absent/empty cosmetic fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/86-cosmetics-migrate-to-role-move-title-hue-voice-avatar-from-i/85-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip cosmetic controls, state, and handlers from NewSessionDialog</name>
  <files>src/ui/sidebar/NewSessionDialog.tsx</files>
  <read_first>
    - src/ui/sidebar/NewSessionDialog.tsx (entire 1416-line file — this is a delete operation across multiple regions; read the whole file so you understand what NOT to touch)
    - Specifically: L60-70 (imports to prune), L124-149 (NewSessionOnCreateOpts type — cosmetic fields need dropping from the identityMode: true branch), L313-345 (state to remove), L653-704 (handleGenerate + handleManualUpload to remove), L720-802 (handleBirth — remove cosmetic fields from openBirthStream call AND from onCreate at L790-801), L820-843 (canOpen — remove title/brief/avatarReady gates from the identityMode ON branch), L1186-1366 (JSX to remove: Title / Brief / Voice / Color / Avatar sections)
    - src/ui/api/identities-api.ts L410-431 (BirthRequest type — the birth endpoint accepts absent title/voice/colorHue; verify this by reading the endpoint at src/backend/database/routes/identity-birth.ts if needed)
  </read_first>
  <action>
    In `src/ui/sidebar/NewSessionDialog.tsx`:

    1. Delete these JSX blocks from the identity-mode branch:
       - Title input block (L1186-1202)
       - Brief textarea block (L1204-1222)
       - VoicePicker block (L1224-1239)
       - ColorPicker block (L1241-1255)
       - Avatar section: entire outer div starting at L1257 through its closing `</div>` at L1366 (Generate button, Upload button, gen/upload error strips, candidates row, manual preview)

    2. Delete these state declarations from around L315-336:
       - `title`, `setTitle` (L317)
       - `brief`, `setBrief` (L318)
       - `voice`, `setVoice` (L319)
       - `colorHue`, `setColorHue` (L320)
       - `candidates`, `setCandidates` (L327)
       - `pickedCandidateId`, `setPickedCandidateId` (L328)
       - `genLoading`, `setGenLoading`, `genError`, `setGenError` (L329-330)
       - `manualPreviewUrl`, `setManualPreviewUrl` (L333)
       - `uploadLoading`, `setUploadLoading`, `uploadError`, `setUploadError` (L334-335)
       - `manualUrlRef` (L337)

    3. Delete `handleGenerate` (L653-676) and `handleManualUpload` (L678-704).

    4. Update the `handleBirth` call at L724-751, the `BirthRequest` type at src/ui/api/identities-api.ts L410-431, AND the backend `identity-birth.ts` endpoint. Path is LOCKED (no alternatives — the checker-verified preflight below confirms this is the only viable shape):

       **Frontend `BirthRequest` type** (src/ui/api/identities-api.ts L410-431): relax `title: string` → `title?: string` and `avatarCandidateId: string` → `avatarCandidateId?: string`. Leave `colorHue: number | null` and `voice: string | null` as-is (already nullable — no change needed on those two). This is a strictly-widening type change — no existing caller breaks because passing a value still typechecks.

       **Frontend `handleBirth` call** at L724-751: omit `title`, `colorHue`, `voice`, and `avatarCandidateId` from the `openBirthStream` args object entirely. The birth stream will send a request body without those four fields, and the widened backend accepts absence.

       **Backend `identity-birth.ts`** — three edits:
       (i) Delete the `title` required gate at L116-119 (`if (typeof title !== "string" || !title.trim()) { res.status(400)...; return; }` — the whole 4-line block).
       (ii) Delete the `avatarCandidateId` required gate at L121-124 (same shape as (i)).
       (iii) Update the `birthIdentity(...)` invocation at L293-316 to pass `title: typeof title === "string" ? title.trim() : ""` (empty-string fallback when absent — orchestrator writes empty title to identity frontmatter; per Plan 86-01 merge semantics, absent-in-identity + present-in-role → resolved value comes from role). Update `avatarCandidateId: typeof avatarCandidateId === "string" && avatarCandidateId.trim() ? avatarCandidateId.trim() : ""` similarly, and add a guard around the `consumeCandidateForBirth(userId, avatarCandidateId as string)` call at L328-329: skip when `avatarCandidateId` is absent/empty.
       (iv) Verify the orchestrator (`birthIdentity` from identity-birth-orchestrator.ts) handles empty `title` + empty `avatarCandidateId` gracefully. If the orchestrator hard-crashes on empty avatarCandidateId (candidate-cache lookup returns undefined), add a branch: empty avatarCandidateId → skip the identity-side avatar write step entirely (the role's avatar file will be served via Plan 86-01's GET /:key/avatar role-folder fallback). If the orchestrator lives in a file this plan does NOT list in files_modified, STOP and surface — the file-ownership graph needs to be extended before this edit proceeds. (Preflight check: read `src/backend/database/routes/identity-birth-orchestrator.ts` before touching the endpoint; if the change surface extends there, revise the plan's files_modified before continuing.)

       Add a test case to `src/backend/database/routes/identity-birth.test.ts` (extend the existing file — do not create a new one): POST /identities/birth with a body containing only `{hostId, name, path, role, task}` (no title, no avatarCandidateId, no colorHue, no voice) succeeds with SSE step-1 emitted and ended{ok:true}. If the orchestrator invocation adjustment above required a `consumeCandidateForBirth` skip branch, add a second test asserting no consume-call fires when avatarCandidateId was absent.

    5. Update the `onCreate` call at L790-801 (identity-mode branch success path) to drop `title`, `brief`, `avatarCandidateId`, `voice`, `colorHue` from the passed options. This means the `NewSessionOnCreateOpts` type discriminated union's `identityMode: true` variant at L126-137 needs those fields removed too.

       **Preflight consumer-sweep finding (verified at plan-check time — planner-level grep confirmed):** `src/ui/AppShell.tsx` and `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` are the only downstream consumers wiring `NewSessionDialog`'s `onCreate` callback. Neither destructures the cosmetic fields (`title`, `brief`, `voice`, `colorHue`, `avatarCandidateId`) from the callback opts — both forward `opts` opaquely or extract only non-cosmetic fields (`host`, `sessionName`, `path`, `identityMode`, `name`). Consequence: **no edits required in AppShell.tsx or PrettyConversationsPanel.tsx** for the `NewSessionOnCreateOpts` type narrowing — the type change is strictly-narrowing and both callers already ignore the fields being removed. The executor may skip touching those two files defensively; if TypeScript flags any post-narrowing error at them, it is a bug in this preflight — surface it and stop.

       Do NOT list AppShell.tsx or PrettyConversationsPanel.tsx in `files_modified` for this plan — they are unaffected by design.

    6. Update `canOpen` at L820-843 (identity-mode ON branch): remove `title.trim().length > 0`, `brief.trim().length > 0`, `avatarReady` gates. Preserve: `selectedHost !== null && nameValid && !skynetCollision && !hostCollision && !collisionChecking && selectedRole !== ""`. The `avatarReady` local at L829-830 becomes dead code — delete it.

    7. Delete imports that become unused: `ColorPicker` (L63), `VoicePicker` (L62), `postGenerateAvatarBatch`, `postManualAvatarCandidate`, `AvatarCandidate` (grep for these imports and remove), `Loader2` (only if no other usage — grep to confirm; the BirthProgress subcomponent at L221-222 uses it, so it stays).

    8. Update the file's header comment block at L1-30 to reflect the new shape: identity-mode branch no longer authors cosmetics; note "Cosmetics moved to role level per Phase 86 — new identities wear their role's face on landing until per-identity override via IdentityModal."

    Do NOT touch: the non-identity-mode branch (path field at L305-309, session-name at L302, host picker), the role dropdown (L353-359), the identity-mode checkbox (L313), the task field (L322-323), the name field with collision checks (L1150-1183), the BirthProgress subcomponent (L191-266), the birth stream + progress state (L361-367), the openBirthStream call structure.
  </action>
  <acceptance_criteria>
    - `grep -c "ColorPicker" src/ui/sidebar/NewSessionDialog.tsx | grep -v '^#'` reports 0 (import + usage both removed).
    - `grep -c "VoicePicker" src/ui/sidebar/NewSessionDialog.tsx | grep -v '^#'` reports 0.
    - `grep -c "postGenerateAvatarBatch\\|postManualAvatarCandidate\\|handleGenerate\\|handleManualUpload" src/ui/sidebar/NewSessionDialog.tsx | grep -v '^#'` reports 0.
    - `grep -c "^  const \\[title" src/ui/sidebar/NewSessionDialog.tsx | grep -v '^#'` reports 0 (state removed).
    - `grep -c "^  const \\[brief\\|^  const \\[voice\\|^  const \\[colorHue\\|^  const \\[candidates\\|^  const \\[pickedCandidateId\\|^  const \\[manualPreviewUrl" src/ui/sidebar/NewSessionDialog.tsx | grep -v '^#'` reports 0.
    - `grep -c "avatarReady" src/ui/sidebar/NewSessionDialog.tsx | grep -v '^#'` reports 0.
    - `grep -n "new-identity-title\\|new-identity-brief\\|new-identity-voice\\|new-identity-color" src/ui/sidebar/NewSessionDialog.tsx` returns 0 matches (dead ids gone).
    - `grep -n "new-identity-name" src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 1 match (name input preserved — this must NOT be stripped).
    - `grep -n "path=\\|setPath\\|selectedRole\\|identityMode" src/ui/sidebar/NewSessionDialog.tsx | wc -l` reports ≥ 10 (name / path / role / identity-mode fields all preserved).
    - `npx tsc -p tsconfig.json --noEmit 2>&1 | grep -c "NewSessionDialog\\.tsx\\|PrettyConversationsPanel\\.tsx" | tr -d '\n' | { read c; test "$c" = "0" && echo "typecheck clean" || echo "typecheck errors: $c"; }` reports 0.
    - File line count reduced by ≥ 200 lines: `wc -l src/ui/sidebar/NewSessionDialog.tsx` reports ≤ 1216 (was 1416 pre-plan).
    - `grep -n "title?:.*string\|avatarCandidateId?:.*string" src/ui/api/identities-api.ts` returns ≥ 2 matches (BirthRequest field relaxations).
    - `grep -c "title is required\|avatarCandidateId is required" src/backend/database/routes/identity-birth.ts | grep -v '^#'` reports 0 (both required gates deleted per action step 4).
    - `src/backend/database/routes/identity-birth.test.ts` contains ≥ 1 new `it(` block asserting the birth flow succeeds with a body omitting title / avatarCandidateId / colorHue / voice.
    - `npx vitest run src/backend/database/routes/identity-birth.test.ts` exits 0.
    - `grep -c "opts\.title\|opts\.brief\|opts\.voice\|opts\.colorHue\|opts\.avatarCandidateId" src/ui/AppShell.tsx src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx | grep -v ':0$' | wc -l` reports 0 (preflight confirms no destructuring; regression guard that this stays true post-plan).
  </acceptance_criteria>
  <verification>
    <automated>npx vitest run src/backend/database/routes/identity-birth.test.ts && npx tsc -p tsconfig.json --noEmit 2>&1 | grep -c "NewSessionDialog\\.tsx\\|PrettyConversationsPanel\\.tsx\\|identity-birth\\.ts\\|identities-api\\.ts" | tr -d '\n' | { read c; test "$c" = "0" && echo "typecheck clean" || echo "typecheck errors: $c"; }</automated>
  </verification>
  <done>NewSessionDialog identity-mode branch renders only name/host/role/path/identity-mode checkbox/task — no cosmetic controls; associated state, handlers, and imports all removed; birth stream call adapted (with backend BirthRequest change if needed); downstream consumers updated; typecheck clean. Test failures from stale NewSessionDialog cosmetic tests are expected and will be addressed in Plan 86-06.</done>
</task>

</tasks>

<threat_model>
Deletion + call-site adaptation plan — reduces surface area, does not add any. No new user-input paths. No new external service integrations. No STRIDE entries required beyond noting: birth stream endpoint accepts fewer required fields (widening tolerance, not restricting).
</threat_model>

<verification>
- Grep gate acceptance criteria above enforce complete removal of the five UI blocks and associated state.
- Typecheck gate ensures downstream consumers (PrettyConversationsPanel.tsx wire-up) also updated.
- Line count reduction gate confirms the strip is real, not just symbolic.
</verification>

<success_criteria>
- NewSessionDialog identity-mode branch shows: name, host picker, role dropdown, path, identity-mode checkbox, task field. Nothing else.
- Birth stream still creates identity successfully (backend accepts absent cosmetic fields).
- Downstream callers of the onCreate callback compile without cosmetic-field references.
</success_criteria>

<output>
Create `.planning/phases/86-cosmetics-migrate-to-role-move-title-hue-voice-avatar-from-i/86-04-SUMMARY.md` when done. Summary captures: (1) which BirthRequest widening path was chosen (option a vs b) and any accompanying backend edit, (2) list of downstream callers of NewSessionOnCreateOpts that were updated, (3) list of NewSessionDialog test assertions that will break (for Plan 86-06), (4) final file line count.
</output>
