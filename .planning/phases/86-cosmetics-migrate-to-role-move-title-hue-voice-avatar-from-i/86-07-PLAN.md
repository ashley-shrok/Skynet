---
phase: 86-cosmetics-migrate-to-role
plan: 07
type: execute
wave: 3
depends_on: [86-06]
files_modified: []
autonomous: true
requirements: [D-CTX-86-doc-7, D-CTX-86-doc-8]
must_haves:
  truths:
    - "`~/.claude/roles/box-maintainer/id-skill-handoff.md` cosmetic-frontmatter section reflects the new inheritance model (role defaults, identity optional overrides, per-field `identity ?? role ?? null` rule, displayName-stays-per-identity) per D-CTX-86-doc-7"
    - "`~/.claude/roles/box-maintainer/runbooks/avatar-flow.md` no longer mentions Matrix-homeserver upload per D-CTX-86-doc-8"
    - "Terminal push: `git pull --rebase origin feat/tab-title-from-tmux` (multi-identity rule) followed by `git push` — no deploy, no full suite, no coord post per campaign constraint"
    - "The two role-owned docs are OUTSIDE the git tree — they are edits to files under `~/.claude/roles/`, NOT tracked in the skynet-tabitha repo. Do not attempt to git add them."
  artifacts: []
  key_links:
    - from: ~/.claude/roles/box-maintainer/id-skill-handoff.md
      to: Phase 86 cosmetic inheritance model
      via: "new section describing role-defaults + identity-overrides + `displayName` exception"
      pattern: "role.default|identity.override|inheritance"
    - from: ~/.claude/roles/box-maintainer/runbooks/avatar-flow.md
      to: pre-Phase-85 shape
      via: "deletion of Matrix upload step; no other content changes required unless collateral references break"
      pattern: "matrix\\|homeserver"
---

<objective>
Update the two role-owned documents that carry stale references to the pre-Phase-85 model per D-CTX-86-doc-7 and D-CTX-86-doc-8, then push the phase's commits per the campaign constraint's terminal step.

Purpose: Wave-3 close-out. The docs live outside the git tree (`~/.claude/roles/box-maintainer/`) but must land alongside the code so future maintainers reading the stale docs don't believe the old model still holds (per the shape file's "What would make it wrong" list).

Output: Two doc files updated on disk; git commit history rebased against origin and pushed. Phase execution ends here per campaign constraint — no deploy, no full-suite gate, no coord-room ship post.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/86-cosmetics-migrate-to-role-move-title-hue-voice-avatar-from-i/85-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update role-owned id-skill-handoff.md with new inheritance model</name>
  <files>~/.claude/roles/box-maintainer/id-skill-handoff.md</files>
  <read_first>
    - ~/.claude/roles/box-maintainer/id-skill-handoff.md (entire file — locate the section describing identity frontmatter cosmetic fields; this is what needs the update)
    - .planning/phases/86-cosmetics-migrate-to-role-move-title-hue-voice-avatar-from-i/85-CONTEXT.md § decisions (the LOCKED inheritance model to describe verbatim)
    - .planning/shapes/shape-cosmetics-migrate-to-role.md § Shape + § Philosophy (source-of-truth prose for the "why" of the new model — reuse phrasing where sensible so the doc reads consistently with the shape)
  </read_first>
  <action>
    In `~/.claude/roles/box-maintainer/id-skill-handoff.md`:

    1. Locate the existing section describing identity frontmatter (cosmetic fields specifically: `title`, `colorHue`, `voice`, `avatar`, and `displayName`). If a heading exists like "Cosmetic frontmatter" / "Identity cosmetics" / similar, edit in place. If no such section exists yet, add one under the existing frontmatter-description area.

    2. Rewrite / add the section to describe the Phase 86 inheritance model:
       - **Role file frontmatter** now carries `title`, `colorHue`, `voice`, `avatar` as DEFAULTS for all identities of that role.
       - **Identity file frontmatter** may still carry any of the four cosmetic fields, but their meaning is now OVERRIDE — "this is the face I picked instead of my role's default."
       - **Per-field resolution**: `resolved_value = identity_frontmatter_value ?? role_frontmatter_value ?? null`. Absent from identity → inherit from role. Present in identity → override role's value (presence = override, regardless of whether the value matches numerically).
       - **`displayName` is EXCLUDED** from this inheritance — it stays per-identity always (it IS the per-identity unique name).
       - **Avatar file location**: role's avatar file lives ALONGSIDE the role file at `~/.claude/roles/<role>/<avatar-filename>` (mirrors identity-side sibling-file convention). Identity's optional avatar override lives at `~/.claude/identities/<key>/<key>.<ext>`.
       - **Migration** is a manual per-deploy motion (whoever deploys picks role defaults and wipes redundant identity frontmatter fields). This box's motion is on the deployer's plate, not something the substrate performs automatically.

    3. Do NOT edit the id skill body itself (`~/.claude/skills/id/SKILL.md`) — the CONTEXT is explicit: id-skill BODY updates are Nicole/Alice's fleet-substrate authorship territory, coordinated separately with the Skynet distributor phase. This plan updates the role-owned handoff doc only.

    4. If the file has a "last updated" or version header, bump it to `2026-09-07` (today's date per the current-date context).
  </action>
  <acceptance_criteria>
    - `~/.claude/roles/box-maintainer/id-skill-handoff.md` exists and is not empty after the edit.
    - `grep -c "role.default\\|role frontmatter\\|inheritance\\|override" ~/.claude/roles/box-maintainer/id-skill-handoff.md | grep -v '^#'` reports ≥ 3.
    - `grep -c "displayName" ~/.claude/roles/box-maintainer/id-skill-handoff.md | grep -v '^#'` reports ≥ 1 (the exclusion clause).
    - `grep -c "identity ?? role\\|identity_frontmatter_value" ~/.claude/roles/box-maintainer/id-skill-handoff.md | grep -v '^#'` reports ≥ 1 (the per-field resolution rule).
    - `~/.claude/skills/id/SKILL.md` was NOT modified — `git status --porcelain -- ~/.claude/skills/id/SKILL.md 2>/dev/null || echo not-tracked` shows either not-tracked (typical — outside repo) or no modification (if for some reason it were tracked, no changes).
  </acceptance_criteria>
  <verification>
    <automated>grep -c "role.default\\|role frontmatter\\|inheritance\\|override" ~/.claude/roles/box-maintainer/id-skill-handoff.md | tr -d '\n' | { read c; test "$c" -ge 3 && echo "handoff doc updated" || echo "handoff doc gates missing: $c matches"; }</automated>
  </verification>
  <done>Role-owned id-skill-handoff doc updated in place; id skill body untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Strip Matrix-upload step from avatar-flow runbook</name>
  <files>~/.claude/roles/box-maintainer/runbooks/avatar-flow.md</files>
  <read_first>
    - ~/.claude/roles/box-maintainer/runbooks/avatar-flow.md (entire file — locate the Matrix-upload step)
  </read_first>
  <action>
    In `~/.claude/roles/box-maintainer/runbooks/avatar-flow.md`:

    1. Locate the step or paragraph mentioning "avatars also get uploaded to your Matrix homeserver" (Alice 2026-09-07 verbatim — the exact phrasing per D-CTX-86-doc-8). This is the section to delete.

    2. Delete that step, its preamble if it's tightly coupled, and any subsequent references that only make sense if the Matrix-upload step existed.

    3. If deletion leaves a numbered-step gap (e.g. "Step 3: … Step 5: …"), re-number the remaining steps sequentially.

    4. If the runbook has a "last updated" or version header, bump it to `2026-09-07`.

    5. No other content changes required per D-CTX-86-doc-8. Do NOT expand the runbook or add unrelated cleanup — the shape is scope-limited.
  </action>
  <acceptance_criteria>
    - `grep -ic "matrix" ~/.claude/roles/box-maintainer/runbooks/avatar-flow.md` reports 0 (case-insensitive to catch Matrix / matrix / MATRIX).
    - `grep -ic "homeserver" ~/.claude/roles/box-maintainer/runbooks/avatar-flow.md` reports 0.
    - File remains non-empty (deletion did not empty the runbook — other steps preserved).
  </acceptance_criteria>
  <verification>
    <automated>test "$(grep -ic 'matrix\\|homeserver' ~/.claude/roles/box-maintainer/runbooks/avatar-flow.md)" = "0" && echo "runbook cleaned" || echo "matrix/homeserver refs remain"</automated>
  </verification>
  <done>Runbook no longer references Matrix homeserver upload; other steps preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Terminal push — rebase against origin, push commits</name>
  <files></files>
  <read_first>
    - .planning/phases/86-cosmetics-migrate-to-role-move-title-hue-voice-avatar-from-i/86-01-SUMMARY.md
    - .planning/phases/86-cosmetics-migrate-to-role-move-title-hue-voice-avatar-from-i/86-02-SUMMARY.md
    - .planning/phases/86-cosmetics-migrate-to-role-move-title-hue-voice-avatar-from-i/86-03-SUMMARY.md
    - .planning/phases/86-cosmetics-migrate-to-role-move-title-hue-voice-avatar-from-i/86-04-SUMMARY.md
    - .planning/phases/86-cosmetics-migrate-to-role-move-title-hue-voice-avatar-from-i/86-05-SUMMARY.md
    - .planning/phases/86-cosmetics-migrate-to-role-move-title-hue-voice-avatar-from-i/86-06-SUMMARY.md
  </read_first>
  <action>
    Terminal step per campaign constraint (Alice 2026-09-07 LOCKED — the farthest phase execute goes):

    1. Verify current branch is `feat/tab-title-from-tmux`. If not, STOP and surface — this branch is the campaign's landing zone per the multi-identity rule.

    2. Verify all Wave 1-3 SUMMARY.md files exist (six summaries: 86-01 through 86-06). If any are missing, STOP and surface — the phase is incomplete.

    3. Run `git status` to enumerate uncommitted changes. If ANY uncommitted work exists in files owned by Plans 86-01 through 86-06, STOP and surface — each code plan is responsible for its own commit(s), not this terminal task.

    4. Run `git log --oneline origin/feat/tab-title-from-tmux..HEAD` to list the local commits about to push. Confirm the list matches the phase's expected commit shape (one or more commits per code plan; no accidental cross-contamination from other work).

    5. Run `git pull --rebase origin feat/tab-title-from-tmux` (multi-identity rule per CONTEXT § Test discipline). If rebase encounters conflicts, STOP and surface — DO NOT auto-resolve.

    6. Run `git push origin feat/tab-title-from-tmux`. This is the terminal action of the phase.

    7. Verify push succeeded via `git log --oneline origin/feat/tab-title-from-tmux..HEAD` returning zero lines (all local commits now on origin).

    DO NOT:
    - Run `docker build`, `docker cp`, `docker compose up`, `docker compose --force-recreate`.
    - Run the full test suite (`npx vitest run` bare) — scoped runs from Plan 86-06 Task 3 are the phase's test gate.
    - Post a coord-room BEFORE/AFTER ship notice — the campaign constraint says no ship posts (no ship — just push + wait for campaign gate).
    - Update `.planning/roles/` bounty JSON — that is orchestrator-owned per phase brief.
    - Update `~/.claude/roles/box-maintainer/bounties/cosmetics-migrate-to-role/bounty.json` — orchestrator-owned per phase brief.
  </action>
  <acceptance_criteria>
    - `git status --porcelain` reports zero uncommitted changes (all Wave 1-3 code committed by their own plans; docs from Tasks 1-2 above are outside the git tree so they don't show up in status).
    - `git log --oneline origin/feat/tab-title-from-tmux..HEAD` returns zero lines after Task 3 push (all commits landed on origin).
    - `git branch --show-current` reports `feat/tab-title-from-tmux`.
    - No `docker` command executed during this task (verifiable by shell history if audit is needed).
    - `~/.claude/roles/box-maintainer/bounties/cosmetics-migrate-to-role/bounty.json` unchanged (orchestrator-owned).
  </acceptance_criteria>
  <verification>
    <automated>test "$(git status --porcelain | wc -l)" = "0" && test "$(git log --oneline origin/feat/tab-title-from-tmux..HEAD | wc -l)" = "0" && echo "phase push complete" || echo "push incomplete: uncommitted or unpushed commits remain"</automated>
  </verification>
  <done>Both role-owned docs updated; local branch rebased against origin and pushed; no deploy, no full suite, no coord post. Phase execution ends.</done>
</task>

</tasks>

<threat_model>
Doc-only + git-push plan. No production code changes. No STRIDE entries.

Push discipline: multi-identity rebase rule enforced (Task 3 step 5) — prevents overwriting concurrent commits from other identity workers on the same branch.
</threat_model>

<verification>
- Task 1-2 grep gates confirm both role-owned docs updated in place.
- Task 3 verification command confirms push succeeded and no uncommitted work remains.
</verification>

<success_criteria>
- Role-owned docs (id-skill-handoff, avatar-flow runbook) reflect the Phase 86 landings.
- Local branch pushed to origin.
- No deploy, no coord post, no full suite — campaign constraint honored.
</success_criteria>

<output>
Create `.planning/phases/86-cosmetics-migrate-to-role-move-title-hue-voice-avatar-from-i/86-07-SUMMARY.md` when done. Summary captures: (1) confirmation both docs updated with grep-verified content, (2) `git log --oneline` output of the pushed commits (as of the push), (3) confirmation of no docker / no full-suite / no coord-post actions taken.
</output>
