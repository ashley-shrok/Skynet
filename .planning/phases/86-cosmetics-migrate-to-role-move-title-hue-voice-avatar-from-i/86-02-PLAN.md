---
phase: 86-cosmetics-migrate-to-role
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/database/routes/roles-create.ts
autonomous: true
requirements: [D-CTX-86-surface-1, D-CTX-86-surface-3]
must_haves:
  truths:
    - "POST /roles accepts multipart/form-data with a `data` JSON field carrying {name, description, hostId, cosmetics: {title, colorHue, voice, avatar}} and an optional `avatar` file per D-CTX-86-surface-3 (LOCKED to identity endpoint pattern)"
    - "On successful POST /roles, the role file's YAML frontmatter carries the four cosmetic fields locked in D-CTX-86-surface-1 (title, colorHue, voice, avatar) when supplied by the caller"
    - "On successful POST /roles with an avatar file part, the file is written to `~/.claude/roles/<name>/<avatar-filename>` at the same sibling-file convention used for identities per D-CTX-86-surface-1"
    - "Absent cosmetic fields in the request produce a role file with NO cosmetic frontmatter keys (existing kebab-case role name + description behavior preserved)"
  artifacts:
    - path: src/backend/database/routes/roles-create.ts
      provides: "POST /roles multipart handler with cosmetic frontmatter emission + avatar file SFTP write to role folder"
  key_links:
    - from: caller (Plan 86-01 Task 3 widens the createRole client; Plan 86-03 wires the dialog)
      to: src/backend/database/routes/roles-create.ts POST /
      via: "multipart/form-data with `data` JSON field carrying {name, description, hostId, cosmetics: {title, colorHue, voice, avatar}} + optional `avatar` file part"
      pattern: "multipart/form-data|parseMultipartRolePayload|req.file"
    - from: src/backend/database/routes/roles-create.ts
      to: writeMarkdownFileAtomic (role .md) + writeAvatarSiblingFile-like helper (role .webp/.png/.jpg)
      via: "SFTP tmp+rename after mkdir -p"
      pattern: "writeMarkdownFileAtomic|writeAvatarSiblingFile"
---

<objective>
Extend the role-creation endpoint to accept + persist the four cosmetic frontmatter fields (title, colorHue, voice, avatar) locked in D-CTX-86-surface-1, and to accept + write an avatar sibling file into the role folder. Match the identity-endpoint multipart-with-`data` convention (LOCKED per canonical_refs learned preference: "Skynet `/identities/:id` PUT is multipart/form-data with field name `data` — a JSON body silently no-ops with a 200. Same trap likely applies to any `/roles/:id` PUT extension") so future consumers hit a loud 415 instead of a silent no-op if they send raw JSON with cosmetics.

Purpose: Wave-1 backend groundwork for the create-role UI cosmetic authoring flow (Plan 86-03 will call this widened endpoint). Independent of Plan 86-01 — different file, different route, different tests — runs in parallel.

Output: Widened `POST /roles` handler with multipart body parsing, cosmetic frontmatter emission, and role-folder avatar write; widened `createRole()` frontend client sending multipart/form-data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/86-cosmetics-migrate-to-role-move-title-hue-voice-avatar-from-i/85-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen POST /roles to multipart with cosmetics + role-folder avatar write</name>
  <files>
    - src/backend/database/routes/roles-create.ts
    - src/backend/database/routes/roles-create.test.ts
  </files>
  <read_first>
    - src/backend/database/routes/roles-create.ts (entire file — 368 lines; the existing JSON-only handler at L148-347 that we widen)
    - src/backend/database/routes/identities.ts L38-70 (multer config + parseMultipartMetadata pattern to mirror), L286-560 (the PUT handler that writes multipart with `data` JSON + optional avatar file + `writeMarkdownFileAtomic` + `writeAvatarSiblingFile` — the endpoint pattern locked in canonical_refs)
    - src/backend/claude-session/identity-artifact-reader.ts (existing `writeMarkdownFileAtomic` export used by current roles-create at L86; existing `writeAvatarSiblingFile` for the sibling-write pattern; `AvatarExt` type + `MIME_TO_AVATAR_EXT` map)
  </read_first>
  <behavior>
    - Test 1: POST /roles with valid multipart body `data={name:"box-maintainer", description:"...", hostId:1}` (no cosmetics, no avatar) writes a role file whose body matches the existing `# name\n\n## Role\n\n<description>\n\n<SEED COMMENT>\n` shape with NO YAML frontmatter block (identical to today's behavior — regression guard).
    - Test 2: POST /roles with `data={name, description, hostId, cosmetics: {title: "Box maintainer", colorHue: 190, voice: "Kate.wav"}}` writes a role file whose body begins with `---\n` frontmatter containing exactly those three keys (no `avatar:` since none supplied), followed by `---\n` then the standard body.
    - Test 3: POST /roles with multipart `data` + `avatar` file (image/webp) writes BOTH the .md (with `avatar: "box-maintainer.webp"` in frontmatter) AND `~/.claude/roles/box-maintainer/box-maintainer.webp` via the sibling-file writer.
    - Test 4: POST /roles with malformed cosmetic values in `data` (e.g. `colorHue: 400`, `voice: "wrong-format"`) returns 400 with a field-specific error message (mirror the identity PUT gates at L332-349).
    - Test 5: POST /roles with a raw JSON body (application/json instead of multipart) returns 415 with error "roles create requires multipart/form-data with `data` field" — LOUD failure per the learned preference, no silent no-op.
    - Test 6: POST /roles with an avatar file exceeding 2 MiB returns 413 (mirrors multer LIMIT_FILE_SIZE handling).
    - Test 7: POST /roles with an avatar file whose mimetype is not one of image/png|jpeg|webp returns 415.
    - Test 8: POST /roles success round-trip returns 201 with `{name, description, cosmetics}` echoing the persisted cosmetic frontmatter (empty object when none supplied).
    - Test 9 (regression): POST /roles path/name validation (existing kebab-case, description length cap, hostId positive integer, cross-user host 404, collision 409, SSH connect 502) all still fire — do NOT loosen any existing gate.
  </behavior>
  <action>
    In `src/backend/database/routes/roles-create.ts`:

    1. Replace the `express.json({ limit: "32kb" })` body parser at L150 with a `multer` upload configured identically to `identities.ts` L38-45 (memoryStorage, 2 MB fileSize limit, mimetype whitelist png/jpeg/webp). Import multer at the top of the file.

    2. Add a `parseMultipartRolePayload(req)` helper that returns `{name, description, hostId, cosmetics}` where `cosmetics: {title?, colorHue?, voice?, avatar?}`. `avatar` is the FILENAME the frontend will use if an avatar file part is present (e.g. `<name>.webp` — derived server-side from the file's mimetype so the client cannot pick a mismatched extension). Return `null` on malformed `data` JSON (→ 400).

    3. In the route handler, keep every existing validation (Steps 1-4 at L155-256) unchanged: name/description/hostId gates, host resolution, SSH connect, collision probe.

    4. Add cosmetic validation BEFORE any provisioning: `title` must be non-empty string ≤128 chars; `colorHue` must be integer 0-359; `voice` must match `/^[A-Z][A-Za-z]+\.wav$/` (same regex identities.ts uses at L47). Reject with 400 + field-specific error on any failure.

    5. If a multipart `avatar` file was uploaded, derive `avatarFilename = <name>.<ext>` where ext comes from `MIME_TO_AVATAR_EXT[req.file.mimetype]`. Set `cosmetics.avatar = avatarFilename`.

    6. Modify the stub markdown builder at L311 to prepend a YAML frontmatter block IF `cosmetics` has any keys: `---\n<yaml.dump(cosmetics)>---\n\n` followed by the existing `# ${name}\n\n## Role\n\n${description}\n\n${SEED_COMMENT}\n` body. When cosmetics is empty, emit the existing frontmatter-less body verbatim (regression guard for Test 1). Import `yaml` from `js-yaml` (already used by identities.ts at L7).

    7. After the `writeMarkdownFileAtomic` call at L317 succeeds, if `req.file` is present, write the avatar bytes to `${remoteHome}/.claude/roles/${name}/${avatarFilename}` via an **inlined SFTP writeFile** (LOCKED — not a new export in identity-artifact-reader.ts, which is owned by Plan 86-01 in the same wave; keeps wave-1 file ownership clean). Shape: `await new Promise<void>((resolve, reject) => { conn.sftp((sftpErr, sftp) => { if (sftpErr) return reject(sftpErr); sftp.writeFile(targetPath, req.file!.buffer, (writeErr) => { sftp.end(); if (writeErr) return reject(writeErr); resolve(); }); }); });` — mirrors the private `sftpReadFile` helper pattern at identity-artifact-reader.ts L2301-2318. Add a code comment: `// Inlined role-folder avatar write per Plan 86-02 (wave-1 file-ownership constraint keeps identity-artifact-reader.ts out of this plan). Follow-up refactor may extract writeAvatarSiblingFileForRole(conn, roleName, ext, bytes) if a third caller emerges — per the same 'extract later if a third caller emerges' precedent CONTEXT.md applies to the avatar generator (see D-CTX-86-surface-3)`. On write failure → 502 with `roles_create_sftp_avatar_write` operation tag (mirrors the log discipline at L318-329).

    8. Return 201 with `{name, description, cosmetics}` (echo cosmetics — empty object when none).

    9. Add an error middleware at the end of the router (mirror identities.ts L650-667) handling multer LIMIT_FILE_SIZE → 413 "Avatar exceeds 2 MB limit" and unsupported mimetypes → 415.

    Create colocated test file `src/backend/database/routes/roles-create.test.ts` if it does not exist (grep to check). Use the same scaffold as `identities.put-disk.test.ts` (bare Express + Node http.request + `vi.mock` of AuthManager, ssh-one-shot, tmux-helper, identity-artifact-reader). Add the 9 tests above. If a test file already exists, extend it — do not clobber existing tests.

  </action>
  <acceptance_criteria>
    - `grep -n "upload.single\\|multer\\|memoryStorage" src/backend/database/routes/roles-create.ts` returns ≥ 3 matches (import + upload config + .single("avatar") on the route).
    - `grep -n "parseMultipartRolePayload\\|multipart/form-data" src/backend/database/routes/roles-create.ts` returns ≥ 2 matches.
    - `grep -n "cosmetics\\." src/backend/database/routes/roles-create.ts` returns ≥ 4 matches (title/colorHue/voice/avatar handling paths).
    - `grep -n "yaml.dump" src/backend/database/routes/roles-create.ts` returns ≥ 1 match (frontmatter emission).
    - `grep -c "res\\.status(400)" src/backend/database/routes/roles-create.ts | grep -v '^#'` reports ≥ 5 (existing name/description/hostId + new title/colorHue/voice cosmetic gates).
    - `grep -n "res\\.status(415)" src/backend/database/routes/roles-create.ts` returns ≥ 1 match (JSON-only reject + avatar mimetype reject).
    - `src/backend/database/routes/roles-create.test.ts` contains ≥ 9 `it(` blocks.
    - `npx vitest run src/backend/database/routes/roles-create.test.ts` exits 0.
  </acceptance_criteria>
  <verification>
    <automated>npx vitest run src/backend/database/routes/roles-create.test.ts</automated>
  </verification>
  <done>POST /roles accepts multipart body with cosmetics + optional avatar file, writes both to the role folder atomically, rejects raw JSON with 415, echoes cosmetics on 201; frontend createRole() client sends multipart; all new tests + existing regression-guard tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated admin → POST /roles | JWT-authenticated; body validated pre-SSH |
| POST /roles → SFTP write | name pre-validated by ROLE_NAME_PATTERN before shell interpolation (existing gate L163-175); avatarFilename derived server-side from mimetype (client cannot spoof ext) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-02-01 | Injection | avatar filename shell-injection into SFTP path | mitigate | avatarFilename = `<name>.<ext>` where name passed ROLE_NAME_PATTERN and ext ∈ {webp,png,jpg} via MIME_TO_AVATAR_EXT lookup — client cannot control either half |
| T-86-02-02 | DoS | oversized avatar upload | mitigate | multer 2 MB fileSize limit (matches identities.ts L40) |
| T-86-02-03 | Tampering | client sends JSON with cosmetics and gets silent no-op | mitigate | LOUD 415 on non-multipart (learned preference from canonical_refs — never silently accept the wrong content type) |
| T-86-02-04 | Injection | frontmatter YAML injection via cosmetic field values | mitigate | js-yaml `dump` sanitizes values (existing pattern at identities.ts L478-483); reject non-string/non-number values before dump via field-specific validators |
</threat_model>

<verification>
- `npx vitest run src/backend/database/routes/roles-create.test.ts` exits 0.
- `git diff src/backend/database/routes/roles-create.ts` shows the multipart migration + cosmetic frontmatter emission + avatar sibling write.
- `grep -n "cosmetics" src/backend/database/routes/roles-create.ts` returns ≥ 6 matches confirming end-to-end wiring.
</verification>

<success_criteria>
- POST /roles accepts multipart with cosmetics + optional avatar; writes both atomically.
- Raw JSON with cosmetics gets 415 (loud fail, not silent no-op).
- Existing role-name-only creation still works (frontmatter-less body preserved).
- Frontend createRole() client widening lives in Plan 86-01 Task 3 (moved there to resolve the wave-1 file-ownership conflict on identities-api.ts). CreateRoleDialog.tsx wire happens in Plan 86-03.
</success_criteria>

<output>
Create `.planning/phases/86-cosmetics-migrate-to-role-move-title-hue-voice-avatar-from-i/86-02-SUMMARY.md` when done. Summary captures: (1) endpoint contract before/after, (2) whether a new role-folder-scoped avatar-write helper was added or the sibling-write was inlined (with rationale), (3) frontmatter emission logic (with-cosmetics vs without-cosmetics path), (4) exact scoped test command that went green.
</output>
