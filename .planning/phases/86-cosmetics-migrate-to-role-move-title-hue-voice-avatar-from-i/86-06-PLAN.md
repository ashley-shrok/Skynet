---
phase: 85-cosmetics-migrate-to-role
plan: 06
type: execute
wave: 3
depends_on: [85-03, 85-04, 85-05]
files_modified:
  - src/ui/sidebar/CreateRoleDialog.test.tsx
  - src/ui/sidebar/NewSessionDialog.test.tsx
autonomous: true
requirements: [D-CTX-85-surface-6]
must_haves:
  truths:
    - "CreateRoleDialog.test.tsx assertions align to the Plan 85-03 landings — the previously-passing 'no cosmetic controls' assertions flip to 'has cosmetic controls' per D-CTX-85-surface-6"
    - "NewSessionDialog.test.tsx test cases for cosmetic-in-identity-mode UI are DELETED — the surface no longer exists per D-CTX-85-surface-6"
    - "Non-cosmetic identity-mode tests in NewSessionDialog.test.tsx are PRESERVED (name, host, role dropdown, path, checkbox, task) per D-CTX-85-surface-4"
    - "All scoped test runs (identity-artifact-reader + identities.get-disk + identities.put-disk + roles-create + identities-api role-cosmetics + IdentityModal.inherit-override + IdentityModal.voice + CreateRoleDialog + NewSessionDialog) go green"
    - "Backend loader/response-merge tests + IdentityModal.inherit-override tests were already added in Plans 85-01 and 85-05 — this plan does not duplicate them"
  artifacts:
    - path: src/ui/sidebar/CreateRoleDialog.test.tsx
      provides: "Grown test surface covering cosmetic controls render / cosmetic values persist / avatar generator flow / cosmetic-missing gates Create"
    - path: src/ui/sidebar/NewSessionDialog.test.tsx
      provides: "Slimmer test surface — cosmetic-in-identity-mode tests removed; non-cosmetic tests preserved"
  key_links:
    - from: src/ui/sidebar/CreateRoleDialog.test.tsx
      to: Plan 85-03 landings in src/ui/sidebar/CreateRoleDialog.tsx
      via: "test assertions target the new Title/Voice/Color/Avatar UI + widened createRole call"
      pattern: "cosmetics|Title|Voice|Color"
    - from: src/ui/sidebar/NewSessionDialog.test.tsx
      to: Plan 85-04 deletions in src/ui/sidebar/NewSessionDialog.tsx
      via: "removed test cases match removed UI"
      pattern: "identity-mode.*(title|brief|voice|color|avatar)"
---

<objective>
Realign the test surface across CreateRoleDialog and NewSessionDialog to match the Plan 85-03 (dialog grows cosmetic controls) and Plan 85-04 (dialog strips cosmetic controls) landings. Per D-CTX-85-surface-6 LOCKED — this plan closes the test-side of the six surface changes; backend loader tests (85-01), response-merge tests (85-01), roles-create endpoint tests (85-02), createRole client tests (85-02), and IdentityModal inherit/override tests (85-05) were already colocated with their respective code plans.

Purpose: Wave-3 test-side realignment for D-CTX-85-surface-6. Follows the sister Phase 84 shape (test plan runs in its own wave after the code lands). Depends on Plans 85-03, 85-04, 85-05 all landing.

Output: Updated CreateRoleDialog.test.tsx (grown), updated NewSessionDialog.test.tsx (stripped), all scoped test runs green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/85-cosmetics-migrate-to-role-move-title-hue-voice-avatar-from-i/85-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grow CreateRoleDialog.test.tsx to cover cosmetic controls</name>
  <files>src/ui/sidebar/CreateRoleDialog.test.tsx</files>
  <read_first>
    - src/ui/sidebar/CreateRoleDialog.test.tsx (entire 513-line file — the shape this task extends; see Test 11 header block explaining the Phase 84 delta and preserving comments)
    - src/ui/sidebar/CreateRoleDialog.tsx (post-Plan-85-03 shape — the new cosmetic controls to assert against)
    - Plan 85-03's SUMMARY.md (from .planning/phases/85-cosmetics-migrate-to-role-move-title-hue-voice-avatar-from-i/85-03-SUMMARY.md — carries the list of Phase 84 test assertions that will break and any deviation from D-CTX-85-empty-not-scenario)
    - src/ui/sidebar/NewSessionDialog.test.tsx L1-200 for mock patterns for ColorPicker + VoicePicker + postGenerateAvatarBatch — CreateRoleDialog.test.tsx will need the same mocks now that the dialog imports these
  </read_first>
  <action>
    In `src/ui/sidebar/CreateRoleDialog.test.tsx`:

    1. Add missing mocks at the top of the file:
       - Mock `@/features/pretty-view/pickers/ColorPicker` with a passthrough component that exposes `data-testid="color-picker"` and forwards `onChange` on a button click at a fixed hue value (matches NewSessionDialog test mock).
       - Mock `@/features/pretty-view/pickers/VoicePicker` similarly with `data-testid="voice-picker"` and an onChange callback.
       - Mock `postGenerateAvatarBatch` and `postManualAvatarCandidate` from `@/api/identities-api` with default fake candidate responses.
       - Mock `createRole` — the widened multipart signature from Plan 85-02.

    2. Update Test 11 (per the existing header block that documents Phase 84 landings): change from "asserts no cosmetic controls" to "asserts cosmetic controls render (Title input, VoicePicker, ColorPicker, Generate button, Upload button)". Preserve the Phase 84 assertions (header blurb text, no required-caption, "New role" title) in the same test.

    3. Update Test 12-14 (name/description/host validation) to also account for the new cosmetic fields being required: Create button remains disabled until all cosmetic fields + picked avatar are set.

    4. Update Test 15 (auto-select single host) — the single-host suppression logic from Phase 84 is unchanged; verify test still passes as-is.

    5. Update Test 16 (successful submit calls createRole with correct args): assert `createRole` is called with the new widened shape `({name, description, hostId, cosmetics: {title, colorHue, voice}}, avatarFile)` where avatarFile is a File object.

    6. Update Test 17 (onChainToCreateIdentity invoked on success) — the callback shape is unchanged, so test body stays the same; only the setup differs (must populate title/voice/color/avatar before hitting Create).

    7. Delete or update Test 18 (which currently asserts on the deleted checkbox from Phase 84 — this may already be deleted per Plan 84-03; grep to confirm). If already deleted, skip.

    8. Update Test 19 (409 collision) — no logic change; setup must populate cosmetic fields to reach the submit path.

    9. Update Test 20 (state reset on close) — extend to assert that title/voice/colorHue/candidates/pickedCandidateId all reset to initial values on close.

    10. Add NEW Test 22 (Cosmetic gates):
        - Test 22a: With name + description + host all valid but title EMPTY → Create disabled.
        - Test 22b: With everything valid but voice EMPTY → Create disabled.
        - Test 22c: With everything valid but no avatar picked → Create disabled.

    11. Add NEW Test 23 (Avatar generator flow):
        - Click Generate → asserts `postGenerateAvatarBatch` called with `{name, title, brief: description, colorHue}` (seed mapping per D-CTX-85-surface-3).
        - Three candidate images appear.
        - Clicking a candidate sets `aria-selected=true` on that button.

    12. Add NEW Test 24 (Manual upload):
        - Trigger file input change with a mock File → asserts `postManualAvatarCandidate` called.
        - Preview image renders.
        - Any generated candidate carousel is cleared (mutual exclusion).

    Ensure the file's top-level test-count docblock is updated to reflect the new test roster.
  </action>
  <acceptance_criteria>
    - `grep -c "^it\\.\\|^  it(\\|test(" src/ui/sidebar/CreateRoleDialog.test.tsx | grep -v '^#'` reports ≥ 12 (existing tests + at least 3 new: 22, 23, 24 — subtests count as separate `it` blocks).
    - `grep -n "color-picker\\|voice-picker" src/ui/sidebar/CreateRoleDialog.test.tsx` returns ≥ 2 matches (mock testids).
    - `grep -n "postGenerateAvatarBatch\\|postManualAvatarCandidate" src/ui/sidebar/CreateRoleDialog.test.tsx` returns ≥ 2 matches (mocked).
    - `grep -n "cosmetics: \\{\\|cosmetics:{" src/ui/sidebar/CreateRoleDialog.test.tsx` returns ≥ 1 match (asserts createRole called with widened shape).
    - `grep -n "brief: description\\|brief:.*description" src/ui/sidebar/CreateRoleDialog.test.tsx` returns ≥ 1 match (seed mapping assertion).
    - `grep -n "A role is what an agent does" src/ui/sidebar/CreateRoleDialog.test.tsx` returns ≥ 1 match (Phase 84 blurb regression guard preserved).
    - `npx vitest run src/ui/sidebar/CreateRoleDialog.test.tsx` exits 0.
  </acceptance_criteria>
  <verification>
    <automated>npx vitest run src/ui/sidebar/CreateRoleDialog.test.tsx</automated>
  </verification>
  <done>All existing Phase 84 regression assertions preserved; cosmetic-controls render / cosmetic-fields-gate-Create / avatar-generator-flow / manual-upload-flow all covered; scoped test run green.</done>
</task>

<task type="auto">
  <name>Task 2: Strip cosmetic-in-identity-mode tests from NewSessionDialog.test.tsx</name>
  <files>src/ui/sidebar/NewSessionDialog.test.tsx</files>
  <read_first>
    - src/ui/sidebar/NewSessionDialog.test.tsx (entire 1603-line file — read the whole thing so you know which tests to keep vs delete)
    - src/ui/sidebar/NewSessionDialog.tsx (post-Plan-85-04 shape — the reduced surface to assert against)
    - Plan 85-04's SUMMARY.md (carries the list of NewSessionDialog test assertions that will break)
  </read_first>
  <action>
    In `src/ui/sidebar/NewSessionDialog.test.tsx`:

    1. Grep for all tests that assert on cosmetic-in-identity-mode UI. Candidate patterns to search (adjust based on actual test names):
       - `new-identity-title`, `new-identity-brief`, `new-identity-voice`, `new-identity-color`
       - `Generate`, `Regenerate`, `Upload…`, `pickedCandidateId`, `candidate`, `avatarBatch`
       - Any `it(` block with a description containing "title", "brief", "voice", "color", "avatar" in an identity-mode setup.

    2. DELETE those tests entirely. Do NOT leave them as `it.skip` — surface must go with the tests.

    3. PRESERVE:
       - All tests targeting name field, name-collision precheck, host picker, role dropdown, identity-mode checkbox, path field, task field.
       - All tests for non-identity-mode (raw shell) behavior — cosmetic controls never rendered there.
       - Tests targeting the "New agent" title conform (Phase 84 landing).
       - Tests targeting single-host suppression (Phase 84 landing).
       - Tests targeting the birth stream + progress steps (steps still fire; the request body just has fewer fields).

    4. Update any test that asserts on `onCreate` callback opts for `identityMode: true` — remove `.title`, `.brief`, `.voice`, `.colorHue`, `.avatarCandidateId` from the expected args object (per Plan 85-04's `NewSessionOnCreateOpts` type change).

    5. Remove any orphaned mocks that are no longer used: `postGenerateAvatarBatch`, `postManualAvatarCandidate`, `AvatarCandidate` type imports. Preserve `openBirthStream` mock (birth stream still runs).

    6. Update the file's top-level test-count docblock (if present) to reflect the reduced roster.
  </action>
  <acceptance_criteria>
    - `grep -c "postGenerateAvatarBatch\\|postManualAvatarCandidate" src/ui/sidebar/NewSessionDialog.test.tsx | grep -v '^#'` reports 0.
    - `grep -c "new-identity-title\\|new-identity-brief\\|new-identity-voice\\|new-identity-color" src/ui/sidebar/NewSessionDialog.test.tsx | grep -v '^#'` reports 0.
    - `grep -c "candidate\\|avatar batch" src/ui/sidebar/NewSessionDialog.test.tsx | grep -iv '^#\\|birth\\|candidate ]' | wc -l` reports 0 (avatar-batch UI test references removed; string 'candidate' may appear in unrelated context — spot-check remaining matches manually).
    - `grep -c "new-identity-name" src/ui/sidebar/NewSessionDialog.test.tsx | grep -v '^#'` reports ≥ 1 (name field tests preserved).
    - `grep -c "selectedRole\\|role dropdown\\|New agent\\|identity-mode checkbox\\|identityMode" src/ui/sidebar/NewSessionDialog.test.tsx | grep -v '^#'` reports ≥ 5 (non-cosmetic identity-mode tests preserved).
    - `npx vitest run src/ui/sidebar/NewSessionDialog.test.tsx` exits 0.
    - File line count reduced by ≥ 100 lines: `wc -l src/ui/sidebar/NewSessionDialog.test.tsx` reports ≤ 1500 (was 1603 pre-plan).
  </acceptance_criteria>
  <verification>
    <automated>npx vitest run src/ui/sidebar/NewSessionDialog.test.tsx</automated>
  </verification>
  <done>Cosmetic-in-identity-mode test cases deleted; non-cosmetic identity-mode + birth stream + Phase 84 tests all preserved and green; scoped test run green.</done>
</task>

<task type="auto">
  <name>Task 3: Full scoped-suite check across all Phase 85 touched surfaces</name>
  <files></files>
  <read_first>
    - .planning/phases/85-cosmetics-migrate-to-role-move-title-hue-voice-avatar-from-i/85-01-SUMMARY.md
    - .planning/phases/85-cosmetics-migrate-to-role-move-title-hue-voice-avatar-from-i/85-02-SUMMARY.md
    - .planning/phases/85-cosmetics-migrate-to-role-move-title-hue-voice-avatar-from-i/85-03-SUMMARY.md
    - .planning/phases/85-cosmetics-migrate-to-role-move-title-hue-voice-avatar-from-i/85-04-SUMMARY.md
    - .planning/phases/85-cosmetics-migrate-to-role-move-title-hue-voice-avatar-from-i/85-05-SUMMARY.md
  </read_first>
  <action>
    Run the union of all scoped test paths this phase touched — a single `npx vitest run` invocation with explicit file arguments. This is the "scoped surface sweep" the campaign constraint permits (no full `npx vitest run` bare):

    ```
    npx vitest run \
      src/backend/claude-session/identity-artifact-reader.role-cosmetics.test.ts \
      src/backend/claude-session/identity-artifact-reader.role-file.test.ts \
      src/backend/claude-session/identity-artifact-reader.avatar-read.test.ts \
      src/backend/database/routes/identities.get-disk.test.ts \
      src/backend/database/routes/identities.put-disk.test.ts \
      src/backend/database/routes/roles-create.test.ts \
      src/ui/api/identities-api.role-cosmetics.test.ts \
      src/ui/features/pretty-view/IdentityModal.inherit-override.test.tsx \
      src/ui/features/pretty-view/IdentityModal.voice.test.tsx \
      src/ui/sidebar/CreateRoleDialog.test.tsx \
      src/ui/sidebar/NewSessionDialog.test.tsx
    ```

    Also run `npx tsc -p tsconfig.json --noEmit` to confirm the whole tree still typechecks after all Wave 1-2 landings. Since Wave 1-2 touched cross-cutting types (`Identity` interface, `NewSessionOnCreateOpts`), any latent breakage lives here.

    If ANY test fails, DO NOT patch it silently — investigate whether the test needs realignment (add to CreateRoleDialog.test.tsx / NewSessionDialog.test.tsx per Tasks 1-2) or the corresponding source plan (85-01 through 85-05) has a bug that needs a return-to-planner call. If in doubt, STOP and surface via the SUMMARY.

    Do NOT run the full test suite (`npx vitest run` bare). Do NOT run `docker build` / `docker cp` / `docker compose up`.
  </action>
  <acceptance_criteria>
    - The exact 11-file scoped `npx vitest run` command above exits 0.
    - `npx tsc -p tsconfig.json --noEmit` exits 0 (whole-tree typecheck clean).
    - No new test files created in this task (all colocations were done by their respective code plans).
  </acceptance_criteria>
  <verification>
    <automated>npx vitest run src/backend/claude-session/identity-artifact-reader.role-cosmetics.test.ts src/backend/claude-session/identity-artifact-reader.role-file.test.ts src/backend/claude-session/identity-artifact-reader.avatar-read.test.ts src/backend/database/routes/identities.get-disk.test.ts src/backend/database/routes/identities.put-disk.test.ts src/backend/database/routes/roles-create.test.ts src/ui/api/identities-api.role-cosmetics.test.ts src/ui/features/pretty-view/IdentityModal.inherit-override.test.tsx src/ui/features/pretty-view/IdentityModal.voice.test.tsx src/ui/sidebar/CreateRoleDialog.test.tsx src/ui/sidebar/NewSessionDialog.test.tsx && npx tsc -p tsconfig.json --noEmit</automated>
  </verification>
  <done>Full scoped surface green; whole-tree typecheck clean.</done>
</task>

</tasks>

<threat_model>
Test-only plan for Tasks 1-2; scoped sweep for Task 3. No production code changes → no new attack surface, no STRIDE entries.
</threat_model>

<verification>
- Task 3's scoped `npx vitest run` command with 11 file paths is the phase-wide green gate.
- Whole-tree `npx tsc --noEmit` confirms no lingering type breakage from cross-cutting Identity / NewSessionOnCreateOpts type changes.
</verification>

<success_criteria>
- CreateRoleDialog test surface grown to cover new cosmetic UI + gates + avatar flows.
- NewSessionDialog test surface stripped of cosmetic-in-identity-mode cases.
- Full scoped test set + whole-tree typecheck both green.
</success_criteria>

<output>
Create `.planning/phases/85-cosmetics-migrate-to-role-move-title-hue-voice-avatar-from-i/85-06-SUMMARY.md` when done. Summary captures: (1) exact list of CreateRoleDialog tests added/updated, (2) exact list of NewSessionDialog tests deleted, (3) the passing scoped vitest command with test-count total, (4) any test failures encountered during Task 3 and how they were resolved (surface plan bugs or test realignment).
</output>
