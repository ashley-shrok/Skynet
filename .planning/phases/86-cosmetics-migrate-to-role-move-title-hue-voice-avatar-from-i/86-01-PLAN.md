---
phase: 86-cosmetics-migrate-to-role
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/claude-session/identity-artifact-reader.ts
  - src/backend/database/routes/identities.ts
  - src/ui/api/identities-api.ts
autonomous: true
requirements: [D-CTX-86-inherit, D-CTX-86-surface-2, D-CTX-86-surface-3-client]
must_haves:
  truths:
    - "GET /identities returns each identity's cosmetics resolved per `identity ?? role ?? null` per D-CTX-86-inherit"
    - "GET /identities/:key/avatar serves the identity's sibling avatar if present, else the role folder's sibling avatar if present, else 404"
    - "Role file frontmatter parses via the same extractor used for identity frontmatter (title/colorHue/voice/avatar narrowing) per D-CTX-86-inherit"
    - "Each per-host fanout reads any given role file at most once, even when multiple identities on that host share the role"
    - "Frontend Identity type surfaces roleDefaults so IdentityModal can label inherited vs overridden fields per D-CTX-86-surface-5"
    - "Frontend createRole() client sends multipart/form-data with cosmetics + optional avatar File (pulled forward from what would have been 86-02 Task 2 to resolve the wave-1 file-ownership conflict on identities-api.ts — 86-01 owns the full file this wave)"
  artifacts:
    - path: src/backend/claude-session/identity-artifact-reader.ts
      provides: "extractCosmeticsFromRoleMarkdown (or generalized extractCosmeticsFromFrontmatter reused on role markdown); readRoleFileByName(conn, roleName) that reads role .md WITHOUT requiring an identity two-step; role-side readAvatarSiblingFileByRole(conn, roleName, avatarFilename)"
    - path: src/backend/database/routes/identities.ts
      provides: "publicIdentity(identityKey, hostId, identityCosmetics, roleCosmetics, role) — per-field merge with identity overriding role; GET /identities per-host role-read memo; GET /:identityKey/avatar role-folder fallback"
    - path: src/ui/api/identities-api.ts
      provides: "Identity.roleDefaults?: { title, colorHue, voice, avatar } — nullable per-field object surfacing the role's raw cosmetic values so the modal can show inherit-vs-override affordances; widened createRole(input, avatar?) sending multipart/form-data with cosmetics + optional avatar File per D-CTX-86-surface-3-client"
  key_links:
    - from: src/backend/database/routes/identities.ts GET /
      to: identity-artifact-reader extractCosmeticsFromFrontmatter on role markdown
      via: "per-host role-read memo (Map<roleName, roleCosmetics>) built once per fanout call"
      pattern: "roleReadCache\\.get|Map<string"
    - from: src/backend/database/routes/identities.ts GET /:identityKey/avatar
      to: role-folder avatar sibling read
      via: "cascade after identity-side readAvatarSiblingFile returns null"
      pattern: "readAvatarSiblingFile.*null"
    - from: src/ui/api/identities-api.ts Identity
      to: publicIdentity JSON shape
      via: "roleDefaults optional field"
      pattern: "roleDefaults"
    - from: src/ui/api/identities-api.ts createRole
      to: src/backend/database/routes/roles-create.ts POST / (widened in Plan 86-02)
      via: "multipart/form-data with `data` JSON + optional `avatar` File (mirrors buildUpdateFormData L55-64)"
      pattern: "multipart/form-data|new FormData|RoleCosmeticInput"
---

<objective>
Teach the backend to read role-file cosmetic frontmatter, merge it under identity frontmatter per the `identity ?? role ?? null` inheritance rule locked in D-CTX-86-inherit, and surface both the resolved values AND the raw role defaults to the frontend. Extend the avatar sibling-file resolver to fall back to the role folder when the identity has no sibling image. Update the `Identity` TypeScript shape so IdentityModal can render inherit-vs-override affordances (Plan 86-05) without a second round-trip.

Purpose: Wave-1 backend groundwork for the four surface changes locked in `85-CONTEXT.md § Implementation Decisions`. Downstream plans (86-03 role-creation UI, 86-05 identity-edit inherit/override affordances) both depend on this data flowing through the API.

Output: Extended `identity-artifact-reader.ts` extractors + role-name-keyed readers; updated `publicIdentity()` doing per-field identity/role merge; role-fallback branch in the avatar endpoint; new `roleDefaults` field on the frontend `Identity` type.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/86-cosmetics-migrate-to-role-move-title-hue-voice-avatar-from-i/85-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add role-cosmetic extractor + role-by-name readers to identity-artifact-reader.ts</name>
  <files>src/backend/claude-session/identity-artifact-reader.ts</files>
  <read_first>
    - src/backend/claude-session/identity-artifact-reader.ts (existing extractCosmeticsFromFrontmatter at L2095-2157 — the narrowing rules to preserve; readRoleFile at L539-575 for the identity-driven role-read pattern; readAvatarSiblingFile at L2179-2290 for the sibling-cascade pattern to mirror on the role side)
    - src/backend/database/routes/roles-list-for-host.ts L1-50 (existing role-name-keyed enumeration proves `getLocalRolesRoot()` + `~/.claude/roles/<role>/<role>.md` shape and shell-escape discipline)
  </read_first>
  <behavior>
    - Test 1 (LOCAL): given a temp roles dir containing `box-maintainer/box-maintainer.md` with frontmatter `title: "Box maintainer"\ncolorHue: 190\nvoice: "Kate.wav"\navatar: "box-maintainer.webp"`, `readRoleFileByName(null, "box-maintainer")` returns `{markdown: "<full file>"}`.
    - Test 2 (LOCAL): given the same file, `extractCosmeticsFromFrontmatter(markdown)` (the EXISTING function, reused unchanged on role markdown) returns `{title: "Box maintainer", colorHue: 190, voice: "Kate.wav", avatar: "box-maintainer.webp"}`.
    - Test 3 (LOCAL): role file with subset frontmatter (only `title` + `colorHue`) yields extractor output containing only those two keys — absent fields must NOT appear in the returned object (matches existing narrowing contract at L2119-2148).
    - Test 4 (LOCAL): role file with no frontmatter → extractor returns `{}` (existing L2110-2117 behavior; regression guard).
    - Test 5 (LOCAL): `readRoleFileByName(null, "does-not-exist")` returns `{markdown: ""}` on ENOENT (mirrors readRoleFile L557-565).
    - Test 6 (REMOTE): `readRoleFileByName(mockConn, "box-maintainer")` execs the exact shell command `cat "$HOME/.claude/roles/box-maintainer/box-maintainer.md" 2>/dev/null || true` (mirrors readRoleFile L572).
    - Test 7 (invalid): `readRoleFileByName(null, "../etc/passwd")` throws (must reject any role name failing `ROLE_NAME_PATTERN = /^[a-z0-9-]+$/` — imported from `identity-birth-orchestrator.ts` and already used by roles-create.ts).
    - Test 8 (LOCAL): given a role folder containing `box-maintainer.webp`, `readAvatarSiblingFileByRole(null, "box-maintainer", "box-maintainer.webp")` returns `{bytes, mime: "image/webp", ext: "webp"}`.
    - Test 9 (LOCAL): given a role folder containing NO sibling image, `readAvatarSiblingFileByRole(null, "box-maintainer", "box-maintainer.webp")` returns `null`.
    - Test 10 (LOCAL): `readAvatarSiblingFileByRole` rejects bytes exceeding `IDMEDIT_MAX_AVATAR_BYTES` (mirrors L2219-2221 defense).
  </behavior>
  <action>
    Add three exports to `src/backend/claude-session/identity-artifact-reader.ts`, sited alongside their identity-side siblings:

    1. Export `readRoleFileByName(conn: SSHClientType | null, roleName: string): Promise<{ markdown: string }>` — implements the role name → role `.md` read WITHOUT the identity two-step. Same LOCAL/REMOTE branch shape as the existing `readRoleFile` (L539-575) but takes `roleName` directly. Validate `roleName` against `ROLE_NAME_PATTERN` (imported from `./identity-birth-orchestrator.js` — already imported by `roles-create.ts` per its L87) at the top of the function; throw `new Error("invalid roleName")` on failure. LOCAL branch reads from `getLocalRolesRoot()` (already exported) joined with `<roleName>/<roleName>.md`. REMOTE branch execs `cat "$HOME/.claude/roles/${roleName}/${roleName}.md" 2>/dev/null || true` via `execWithTimeout`. ENOENT → `{markdown: ""}` on LOCAL; empty stdout → `{markdown: ""}` on REMOTE.

    2. Do NOT create a new `extractCosmeticsFromRoleMarkdown` — the existing `extractCosmeticsFromFrontmatter` (L2095-2157) is already shape-compatible (YAML frontmatter, same field names, same narrowing rules). Reuse it as-is against role markdown. Add a JSDoc note to that function stating it is now also called against role frontmatter as of Phase 86.

    3. Export `readAvatarSiblingFileByRole(conn: SSHClientType | null, roleName: string, avatarFilename: string): Promise<{ bytes: Buffer; mime: string; ext: AvatarExt } | null>` — mirrors `readAvatarSiblingFile` (L2179-2290) but rooted at `~/.claude/roles/<roleName>/<avatarFilename>` and takes the filename as an explicit argument (already known from role frontmatter). Validate `roleName` via `ROLE_NAME_PATTERN`; extract `ext` from `avatarFilename` via regex `^[a-z0-9-]+\\.(webp|png|jpg|gif|svg)$` and reject invalid on both ends. Enforce the `IDMEDIT_MAX_AVATAR_BYTES` cap on returned bytes (matches L2219-2221 / L2286-2288).

    Create colocated test file `src/backend/claude-session/identity-artifact-reader.role-cosmetics.test.ts` — copy scaffolding from `identity-artifact-reader.role-file.test.ts` (mock `../ssh/tmux-helper.js`, LOCAL branch mkdtemps `ROLES_HOST_DIR` via env var); add the 10 tests above.

    DO NOT modify `extractCosmeticsFromFrontmatter` narrowing rules — the shape is load-bearing for existing identity reads. DO NOT change `readRoleFile` (the identity-two-step version) — Plan 22-06 code depends on its signature.
  </action>
  <acceptance_criteria>
    - `grep -n "^export function readRoleFileByName" src/backend/claude-session/identity-artifact-reader.ts` returns exactly one line.
    - `grep -n "^export function readAvatarSiblingFileByRole" src/backend/claude-session/identity-artifact-reader.ts` returns exactly one line.
    - `grep -n "^export function extractCosmeticsFromFrontmatter" src/backend/claude-session/identity-artifact-reader.ts` returns exactly one line AND is unchanged in signature (accepts `markdown: string`, returns the union shape at L2095-2109).
    - `grep -c "ROLE_NAME_PATTERN" src/backend/claude-session/identity-artifact-reader.ts | grep -v '^#'` reports ≥ 2 (import + at least two usages in the new readers).
    - `src/backend/claude-session/identity-artifact-reader.role-cosmetics.test.ts` exists and contains 10 `it(` blocks.
    - No production behavior change to `readRoleFile`, `readAvatarSiblingFile`, or `extractCosmeticsFromFrontmatter` — `git diff` on those three functions shows only comment/JSDoc additions (no code changes inside function bodies).
  </acceptance_criteria>
  <verification>
    <automated>npx vitest run src/backend/claude-session/identity-artifact-reader.role-cosmetics.test.ts src/backend/claude-session/identity-artifact-reader.role-file.test.ts</automated>
  </verification>
  <done>New readers + colocated test file exist, all tests green, existing role-file tests still green (regression guard on unchanged readRoleFile).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Merge role cosmetics into publicIdentity + GET /identities, and add role-avatar fallback in GET /:identityKey/avatar</name>
  <files>
    - src/backend/database/routes/identities.ts
    - src/backend/database/routes/identities.get-disk.test.ts
  </files>
  <read_first>
    - src/backend/database/routes/identities.ts L86-151 (existing publicIdentity signature to extend), L189-272 (GET / fanout that constructs the per-host read), L288-559 (PUT handler that also re-emits publicIdentity — must be updated), L560-648 (GET /:identityKey/avatar — needs the role fallback branch)
    - src/backend/claude-session/identity-artifact-reader.ts (the three new/reused exports from Task 1)
    - src/backend/database/routes/identities.put-disk.test.ts L1-40 (test scaffolding shape — bare Express + Node http.request + vi.mock pattern)
    - src/backend/database/routes/identities.get-disk.test.ts if it exists (extend it in place); otherwise create parallel file next to identities.put-disk.test.ts using the same scaffold
  </read_first>
  <behavior>
    - Test PUB-1: `publicIdentity(key, hostId, {title: "id-title", colorHue: 200}, {title: "role-title", voice: "Kate.wav", colorHue: 190, avatar: "role.webp"}, "box-maintainer")` returns `{title: "id-title", colorHue: 200, voice: "Kate.wav", role: "box-maintainer", roleDefaults: {title: "role-title", colorHue: 190, voice: "Kate.wav", avatar: "role.webp"}}` — identity title/colorHue win, voice inherits from role, roleDefaults echoes the role's raw values.
    - Test PUB-2: `publicIdentity(key, hostId, {}, {title: "role-title", colorHue: 190}, "role-x")` returns resolved `title: "role-title", colorHue: 190, voice: null` (voice absent on both), plus `roleDefaults: {title: "role-title", colorHue: 190}` (no voice/avatar keys — absent on role).
    - Test PUB-3: `publicIdentity(key, hostId, {}, {}, "role-x")` returns resolved title/colorHue/voice all `null` (safe defaults preserved) AND `roleDefaults: {}` (empty object surfaces "role has no defaults" — the frontend distinguishes this from "no role" via the `role` field being non-null).
    - Test PUB-4: `publicIdentity(key, hostId, {title: "id-title"}, null, null)` returns resolved title, `roleDefaults: null`, `role: null` — an identity with no resolvable role still gets safe-default identity shape.
    - Test PUB-5: `identity.avatarUrl` stays `/identities/${key}/avatar?hostId=${hostId}` unchanged — url shape must not shift under the merge (existing frontend consumers ride on the URL as-is).
    - Test GET-1 (route integration): GET /identities where the on-disk identity file has `title: "id-title"` and the on-disk role file has `title: "role-title", voice: "Kate.wav"` returns the identity with resolved `title: "id-title", voice: "Kate.wav"` and `roleDefaults: {title: "role-title", voice: "Kate.wav"}`.
    - Test GET-2 (route integration): GET /identities where the same role is shared by TWO identities on the same host reads the role file ONCE — verify via mock spy count on `readRoleFileByName`.
    - Test AVATAR-1: GET /:identityKey/avatar where identity's sibling file exists returns those bytes (existing behavior preserved).
    - Test AVATAR-2: GET /:identityKey/avatar where identity has NO sibling file but the role has `avatar: "box-maintainer.webp"` in frontmatter AND `~/.claude/roles/box-maintainer/box-maintainer.webp` exists returns the role's bytes with `Content-Type: image/webp`.
    - Test AVATAR-3: GET /:identityKey/avatar where neither identity nor role has an avatar returns 404 with `{error: "no avatar on disk for this identity"}` (existing L616-619 behavior preserved).
  </behavior>
  <action>
    In `src/backend/database/routes/identities.ts`:

    1. Extend `publicIdentity()` (L109-151) signature to accept a fifth parameter `roleCosmetics: { title?: string; colorHue?: number; voice?: string; avatar?: string } | null = null`. Compute the resolved values per the `identity ?? role ?? null` rule locked in D-CTX-86-inherit: for each of `title`, `colorHue`, `voice`, use `cosmetics.<field> ?? roleCosmetics?.<field> ?? null` (with the existing type-narrowing guards preserved). Emit a new response field `roleDefaults` — echo `roleCosmetics` verbatim when non-null (only include fields that were present); emit `null` when `roleCosmetics` is null. DO NOT change `avatarUrl` shape (`/identities/${identityKey}/avatar?hostId=${hostId}` stays identical) — the URL is what the frontend renders, and the backend does the fallback internally.

    2. In GET / (L189-272), build a per-host `roleReadCache: Map<string, ReturnType<typeof extractCosmeticsFromFrontmatter>>` inside the per-host `try` block. When iterating identityKeys, extract the role from the identity's markdown via `extractRoleFromMarkdown`, then look up the role's cosmetics via `roleReadCache.get(role)`; if absent, call `readRoleFileByName(conn, role)` + `extractCosmeticsFromFrontmatter`, store in the cache, and use the value. Pass the cached role cosmetics as the fourth arg to `publicIdentity`. If role read throws → treat as `{}` (role has no defaults; identity's own values still surface). If identity has no role → pass `null` as fourth arg.

    3. Update the PUT handler's response echo at L540 to include a fresh role-cosmetic read: after the post-write re-read of the identity markdown, also do `const roleName = extractRoleFromMarkdown(postWriteMd)` and `readRoleFileByName(conn, roleName)` + `extractCosmeticsFromFrontmatter` if roleName is non-null, then pass those role cosmetics as the fourth arg to `publicIdentity`. On any error in the role-read chain → pass `{}` (do not fail the whole PUT).

    4. In GET /:identityKey/avatar (L560-648), after `readAvatarSiblingFile` returns `null` (existing L615), do NOT immediately 404. Instead: read the identity's markdown, resolve its role via `extractRoleFromMarkdown`; if role present, `readRoleFileByName(conn, role)` + `extractCosmeticsFromFrontmatter` → if `cos.avatar` is a valid filename, call new `readAvatarSiblingFileByRole(conn, role, cos.avatar)` and return those bytes with matching Content-Type + ETag. Only 404 if both branches return null.

    5. Import the new helpers (`readRoleFileByName`, `readAvatarSiblingFileByRole`) alongside existing `identity-artifact-reader.js` imports at L11-23.

    Create or extend `src/backend/database/routes/identities.get-disk.test.ts` with the 5 PUB tests, 2 GET-integration tests, and 3 AVATAR tests. If the file exists (grep for it first), APPEND new tests preserving existing ones. If it does not exist, create it using the same scaffold as `identities.put-disk.test.ts` (bare Express + Node http.request + `vi.mock` of AuthManager, identity-artifact-reader, ssh-one-shot). Export `publicIdentity` remains exported (unchanged at the export declaration) so PUB tests can unit-test the shape directly.
  </action>
  <acceptance_criteria>
    - `grep -n "^export function publicIdentity" src/backend/database/routes/identities.ts` returns one line AND the immediately-following signature contains a parameter named `roleCosmetics`.
    - `grep -c "roleDefaults" src/backend/database/routes/identities.ts | grep -v '^#'` reports ≥ 3 (return-object key + PUT echo + type annotation).
    - `grep -n "readRoleFileByName\\|readAvatarSiblingFileByRole" src/backend/database/routes/identities.ts` returns ≥ 3 matches (import + GET / fanout + GET /:key/avatar fallback + PUT re-read).
    - `grep -n "roleReadCache\\|Map<string" src/backend/database/routes/identities.ts` shows the per-host role-read memo lives inside the GET / handler (not a module-level global).
    - `src/backend/database/routes/identities.get-disk.test.ts` contains at least 10 `it(` blocks covering PUB-1..PUB-5, GET-1..GET-2, AVATAR-1..AVATAR-3.
    - `src/ui/api/identities-api.ts` `Identity` interface contains `roleDefaults?:` field with the four optional cosmetic fields.
    - `npx vitest run src/backend/database/routes/identities.get-disk.test.ts src/backend/database/routes/identities.put-disk.test.ts` exits 0.
    - Existing `identities.put-disk.test.ts` still passes — signature extension is backward-compatible (fourth+fifth params default to `null`).
  </acceptance_criteria>
  <verification>
    <automated>npx vitest run src/backend/database/routes/identities.get-disk.test.ts src/backend/database/routes/identities.put-disk.test.ts src/backend/claude-session/identity-artifact-reader.role-cosmetics.test.ts</automated>
  </verification>
  <done>publicIdentity merges identity + role cosmetics; GET / caches role reads per host; GET /:key/avatar falls back to role folder; PUT echo reflects the merge; Identity type surfaces roleDefaults for downstream UI; all backend tests green.</done>
</task>


<task type="auto" tdd="true">
  <name>Task 3: Widen createRole() client to multipart with cosmetics + avatar (pulled forward from what would have been Plan 86-02 Task 2)</name>
  <files>
    - src/ui/api/identities-api.ts
    - src/ui/api/identities-api.role-cosmetics.test.ts
  </files>
  <read_first>
    - src/ui/api/identities-api.ts L38-64 (buildFormData / buildUpdateFormData pattern — the exact multipart shape to mirror for role creation), L94-136 (createIdentity / updateIdentity call sites — Content-Type header override precedent), L357-391 (existing createRole to widen)
    - .planning/phases/86-cosmetics-migrate-to-role-move-title-hue-voice-avatar-from-i/86-02-PLAN.md (the backend endpoint contract this client will speak to — Plan 86-02 owns the endpoint; this task owns the client but ships in Wave 1 alongside the endpoint plan so both land as a shippable unit)
  </read_first>
  <behavior>
    - Test C-1: `createRole({name, description, hostId})` (no cosmetics, no avatar) sends multipart/form-data with `data` field containing `{name, description, hostId}` JSON and NO `avatar` file part.
    - Test C-2: `createRole({name, description, hostId, cosmetics: {title, colorHue, voice}}, undefined)` sends multipart with `data` field containing `{name, description, hostId, cosmetics: {title, colorHue, voice}}` and no avatar part.
    - Test C-3: `createRole({name, description, hostId, cosmetics: {title, colorHue, voice}}, avatarFile)` sends multipart with `data` field AND `avatar` file part carrying the File.
    - Test C-4: 409 response still throws `RoleAlreadyExistsError(name)` (existing behavior).
    - Test C-5: TypeScript compile — the widened signature is backward-compatible: existing single-argument call sites `createRole({name, description, hostId})` still typecheck without change.
  </behavior>
  <action>
    In `src/ui/api/identities-api.ts`:

    1. Add a new exported type `RoleCosmeticInput = { title?: string; colorHue?: number; voice?: string }` (avatar filename is derived server-side from the uploaded File's mimetype per Plan 86-02 — client does not send `avatar` in the JSON).

    2. Change `createRole` signature to `createRole(input: { name: string; description: string; hostId: number; cosmetics?: RoleCosmeticInput }, avatar?: File | null): Promise<{ name: string; description: string; cosmetics: RoleCosmeticInput }>`.

    3. Rebuild the request body: use `FormData` with `fd.append("data", JSON.stringify({name, description, hostId, cosmetics}))` and `if (avatar) fd.append("avatar", avatar)`. Send with `headers: { "Content-Type": "multipart/form-data" }` (matches createIdentity L103, updateIdentity L130 — explicit override suppresses axios's JSON transform).

    4. Preserve the 409 → `RoleAlreadyExistsError` branch unchanged.

    5. Update the return type shape to include `cosmetics` (echoed from the server per Plan 86-02 Task 1 step 8).

    Create colocated test file `src/ui/api/identities-api.role-cosmetics.test.ts`. Mock `authApi.post` and assert the FormData contents. The existing `identities-api` test suite (if any) — grep to confirm — should not need changes; if `createRole` is currently tested there, extend those tests instead of duplicating.

    Note on cross-plan sequencing: Plan 86-02 owns the backend endpoint that this client speaks to. Both plans land in Wave 1 in parallel; the client's `identities-api.role-cosmetics.test.ts` mocks `authApi` so it does NOT require the real backend endpoint to run. Integration between the two happens in Wave 2 (Plan 86-03) when CreateRoleDialog actually calls the widened client against the widened endpoint.
  </action>
  <acceptance_criteria>
    - `grep -n "export type RoleCosmeticInput\\|RoleCosmeticInput" src/ui/api/identities-api.ts` returns ≥ 2 matches (export + usage in createRole signature).
    - `grep -n "^export async function createRole" src/ui/api/identities-api.ts` shows the signature accepts an `avatar` argument.
    - `grep -n "new FormData\\|FormData()" src/ui/api/identities-api.ts` returns ≥ 3 matches (existing 2 + new createRole build).
    - `grep -n "multipart/form-data" src/ui/api/identities-api.ts` returns ≥ 3 matches (existing 2 + new createRole header).
    - `src/ui/api/identities-api.role-cosmetics.test.ts` exists and contains ≥ 5 `it(` blocks.
    - `npx vitest run src/ui/api/identities-api.role-cosmetics.test.ts` exits 0.
    - `npx tsc -p tsconfig.json --noEmit` completes with no NEW errors on `src/ui/api/identities-api.ts` or any file importing `createRole` (CreateRoleDialog.tsx at L51, PrettyConversationsPanel.tsx). NOTE: backward-compat matters — existing call sites pass a single-arg object; the widened signature must remain callable that way.
  </acceptance_criteria>
  <verification>
    <automated>npx vitest run src/ui/api/identities-api.role-cosmetics.test.ts</automated>
  </verification>
  <done>createRole client sends multipart with cosmetics + optional avatar File; RoleAlreadyExistsError preserved; existing call site in CreateRoleDialog.tsx L178-182 still typechecks (widened arg is optional); new colocated tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /identities | Authenticated admin caller; identityHosts query param already validated by parseIdentityHosts |
| identities.ts → SSH exec | roleName interpolated into `cat "$HOME/.claude/roles/${roleName}/..."` — must pass ROLE_NAME_PATTERN before reaching shell |

## STRIDE Threat Register

Skynet is single-tenant, single-operator per phase brief. This plan extends existing admin-authenticated endpoints; no new attack surface beyond widening the role-file read path.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-01-01 | Injection | readRoleFileByName SSH interpolation | mitigate | ROLE_NAME_PATTERN /^[a-z0-9-]+$/ gate at function entry (defense-in-depth: identity file's role: frontmatter is already the source, but this reader takes roleName as a direct arg so re-validate) |
| T-86-01-02 | Injection | readAvatarSiblingFileByRole SSH interpolation | mitigate | ROLE_NAME_PATTERN gate + explicit ext regex `^[a-z0-9-]+\.(webp\|png\|jpg\|gif\|svg)$` on avatarFilename |
| T-86-01-03 | DoS | Multiple identities sharing a role trigger redundant SSH reads | mitigate | per-host roleReadCache inside GET / fanout — role file read at most once per host per request |
| T-86-01-04 | Info disclosure | Role file contents leak via SSH stderr | accept | existing per-host silent-swallow pattern preserved (L243-247) |
</threat_model>

<verification>
- `npx vitest run src/backend/claude-session/identity-artifact-reader.role-cosmetics.test.ts src/backend/database/routes/identities.get-disk.test.ts src/backend/database/routes/identities.put-disk.test.ts src/backend/claude-session/identity-artifact-reader.role-file.test.ts src/ui/api/identities-api.role-cosmetics.test.ts` exits 0.
- `git diff src/backend/claude-session/identity-artifact-reader.ts` shows the new readers added at the end of existing sections and no changes to `readRoleFile` / `readAvatarSiblingFile` / `extractCosmeticsFromFrontmatter` bodies.
- `git diff src/ui/api/identities-api.ts` shows (a) the `roleDefaults` field addition to the `Identity` interface, (b) a new `RoleCosmeticInput` type export, (c) a widened `createRole()` signature accepting optional cosmetics + optional avatar File and sending multipart/form-data.
</verification>

<success_criteria>
- Backend surfaces role cosmetics merged with identity overrides on GET /identities.
- Backend serves role-folder avatar as fallback when identity has none.
- Frontend `Identity` type carries `roleDefaults` for Plan 86-05.
- Per-host role-read memo prevents redundant SSH reads.
- All new + existing backend tests pass under scoped run.
</success_criteria>

<output>
Create `.planning/phases/86-cosmetics-migrate-to-role-move-title-hue-voice-avatar-from-i/86-01-SUMMARY.md` when done. Summary captures: (1) the new reader exports and their exact signatures, (2) the publicIdentity signature extension and merge semantics, (3) the avatar fallback branch site + test count, (4) any deviation from the plan with rationale, (5) the exact scoped `npx vitest run` command that went green.
</output>
