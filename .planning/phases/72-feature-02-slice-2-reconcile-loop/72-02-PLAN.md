---
phase: 72-feature-02-slice-2-reconcile-loop
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/database/db/index.ts
  - src/backend/database/db/schema.ts
  - src/backend/database/db/index.migration.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "The ssh_data table gains a new BOOLEAN column runs_fleet_substrate defaulting to false"
    - "The migration is idempotent: safe on fresh installs (column added via addColumnIfNotExists no-op after first add), safe on upgraded installs (column added once on first successful boot)"
    - "The direct schema mutation persists to the encrypted SQLite file via DatabaseSaveTrigger.forceSave wrapped in try/catch that warns-not-throws on failure"
    - "Drizzle schema in schema.ts declares the new column so Plan 04's helper can read the flag via typed query"
  artifacts:
    - path: "src/backend/database/db/index.ts"
      provides: "addColumnIfNotExists('ssh_data', 'runs_fleet_substrate', 'INTEGER NOT NULL DEFAULT 0') call in migrateSchema"
      contains: "runs_fleet_substrate"
    - path: "src/backend/database/db/schema.ts"
      provides: "runsFleetSubstrate column on the hosts drizzle table"
      contains: "runs_fleet_substrate"
    - path: "src/backend/database/db/index.migration.test.ts"
      provides: "Migration test asserting the column lands on both OLD-schema and NEW-schema installs"
  key_links:
    - from: "src/backend/database/db/index.ts migrateSchema"
      to: "DatabaseSaveTrigger.forceSave"
      via: "phase-72-add-runs-fleet-substrate reason string"
      pattern: "forceSave.*phase-72"
    - from: "src/backend/database/db/schema.ts hosts table"
      to: "runs_fleet_substrate column"
      via: "drizzle integer/boolean mode declaration"
      pattern: "runsFleetSubstrate"
---

<objective>
Add the `runs_fleet_substrate BOOLEAN NOT NULL DEFAULT false` opt-in column to the ssh_data table (Skynet's host table, aliased as `hosts` in Drizzle) via the existing idempotent addColumnIfNotExists migration path. Wrap the direct SQLite mutation in DatabaseSaveTrigger.forceSave per the in-memory-SQLite invariant so the schema mutation actually persists to the encrypted file. Declare the column on the Drizzle schema too so Plan 04's identity-hosting-host filter can read the flag via typed query without a raw SQL escape hatch.

Purpose: The opt-in flag is the narrower filter that lets a Skynet operator opt an identity-hosting host OUT of distributor push (per the shape doc's "A per-host opt-in flag on the host record" section). Default false means no host is touched by the sweep unless an operator explicitly flips it — the shape doc's "The sweep runs when the flag is off" failure mode is impossible without this column existing first.

Output: Column persists across container restart; migration test covers the idempotent-add path (existing schema + fresh schema); Drizzle type available for Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-feature-02-slice-2-reconcile-loop/72-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add runs_fleet_substrate migration + Drizzle column + test coverage</name>
  <files>src/backend/database/db/index.ts, src/backend/database/db/schema.ts, src/backend/database/db/index.migration.test.ts</files>
  <read_first>
    - /home/ubuntu/skynet-tiffany/.planning/phases/72-feature-02-slice-2-reconcile-loop/72-CONTEXT.md (Code context section — migration site + forceSave invariant)
    - /home/ubuntu/skynet-tiffany/src/backend/database/db/index.ts (whole file if practical, otherwise the following ranges: lines 195–240 for the CREATE TABLE ssh_data block; lines 634–659 for the addColumnIfNotExists helper; lines 774–830 for the migrateSchema function structure and the phase-68 forceSave pattern that this task mirrors; lines 800–821 for the exact try/catch wrap around DatabaseSaveTrigger.forceSave — copy that wrapping shape verbatim, adjusting the reason string)
    - /home/ubuntu/skynet-tiffany/src/backend/database/db/schema.ts (lines 65–170 — the `hosts = sqliteTable("ssh_data", { ... })` block. Note the pattern for boolean columns: `enableSsh: integer("enable_ssh", { mode: "boolean" }).notNull().default(true)` — mirror for runsFleetSubstrate with default(false))
    - /home/ubuntu/skynet-tiffany/src/backend/database/db/index.migration.test.ts (the whole file — this is the test-authoring pattern to follow. Mirror the OLD_IDENTITIES_CREATE_SQL / NEW_IDENTITIES_CREATE_SQL two-way test shape but for the ssh_data table, adding a runs_fleet_substrate assertion via PRAGMA table_info)
    - /home/ubuntu/skynet-tiffany/CLAUDE.md § "In-memory SQLite pattern" IF PRESENT (may not exist; the invariant is authoritative in the CONTEXT.md canonical refs pointing to box-maintainer role file § "Load-bearing invariants")
  </read_first>
  <behavior>
    - Test A (OLD ssh_data schema, no runs_fleet_substrate): create the pre-slice-2 CREATE TABLE, run addColumnIfNotExists("ssh_data", "runs_fleet_substrate", "INTEGER NOT NULL DEFAULT 0") → PRAGMA table_info(ssh_data) contains a runs_fleet_substrate column
    - Test B (NEW ssh_data schema, column already present): CREATE TABLE with runs_fleet_substrate column already in the DDL, run the same addColumnIfNotExists → column is unchanged (no duplicate, no throw), existing rows retain their runs_fleet_substrate value
    - Test C (existing rows default): CREATE TABLE (OLD schema) with a seed ssh_data row inserted BEFORE the migration → after migration, the seed row's runs_fleet_substrate value is 0 (SQLite's ALTER TABLE ADD COLUMN with NOT NULL DEFAULT 0 backfills existing rows with the default)
    - Test D (Drizzle typed query): after migration, a `db.select({ runsFleetSubstrate: hosts.runsFleetSubstrate }).from(hosts).limit(1)` compiles + runs (this is a compile-time proof via a small runtime harness — the test file imports `hosts` from schema.ts and asserts the property `hosts.runsFleetSubstrate` is defined and non-null)
  </behavior>
  <action>
    Three edits in one commit:

    **Edit 1 — src/backend/database/db/schema.ts**: inside the `hosts = sqliteTable("ssh_data", { ... })` block (lines 67–170ish), append a new column declaration after the `enableTelnet` line (around line 135) or in any consistent position within the boolean-flag cluster:
    `runsFleetSubstrate: integer("runs_fleet_substrate", { mode: "boolean" }).notNull().default(false),`
    Mirror the punctuation + `.notNull().default(false)` shape used by `enableRdp`, `enableVnc`, `enableTelnet` on the same table.

    **Edit 2 — src/backend/database/db/index.ts**: NOTE — migration site is `src/backend/database/db/index.ts` (CONTEXT.md's canonical-refs paragraph says `src/backend/database/index.ts` which is missing the `db/` segment — verified: `CREATE TABLE IF NOT EXISTS ssh_data` lives at L199-236 of the correct file). Inside `migrateSchema()` (starting around line 774), append the new addColumnIfNotExists call AFTER the existing addColumnIfNotExists cluster (around lines 823–837 where `user_preferences.theme`, `compose_drafts.queue_slots`, `users.is_admin` etc. are added). The exact line:
    `addColumnIfNotExists("ssh_data", "runs_fleet_substrate", "INTEGER NOT NULL DEFAULT 0");`

    IMMEDIATELY after that call, add a try/catch wrapping `await DatabaseSaveTrigger.forceSave("phase-72-add-runs-fleet-substrate")` — VERBATIM STRUCTURE of the phase-68 wrapper at lines 809–821, but with:
    - reason string: `"phase-72-add-runs-fleet-substrate"`
    - warn message: `"[phase-72] forceSave failed post-add (non-fatal — addColumnIfNotExists is idempotent, next boot retries)"`
    - operation: `"schema_migration_force_save_post_add"`
    Preserve the exact catch semantics of phase-68 (warn-not-throw; propagation would crash boot).

    Add a docblock comment ABOVE the addColumnIfNotExists line (2–5 lines) referencing this phase and stating the invariant: opt-in flag for the fleet-substrate sweep; default 0 means no host is swept until an operator explicitly flips it; migration is idempotent via addColumnIfNotExists.

    **Edit 3 — src/backend/database/db/index.migration.test.ts**: append a new describe block for the phase-72 migration. Reuse the test file's existing helpers (`columnNames(db, "ssh_data")`). The new describe block adds four tests matching the <behavior> block. Test D imports `hosts` from `./schema.js` and asserts `hosts.runsFleetSubstrate !== undefined` — no need for a real SQLite roundtrip on Test D; it is a static-shape proof that the Drizzle column exists.

    Explicit DO-NOT: Do NOT alter the CREATE TABLE ssh_data literal in db/index.ts (lines 199–236). The upgrade path is addColumnIfNotExists; the CREATE TABLE literal is legacy-schema history and staying as-is means fresh installs get the column via the same code path as upgraded installs (idempotent no-op on the addColumnIfNotExists call — the SELECT probes for column presence). Modifying the CREATE TABLE literal would create two ways for the column to appear, complicating the migration story. Leave it alone.

    Explicit DO-NOT: Do NOT introduce a new export like `runFleetSubstrateColumnAdd()` mirroring `runIdentitiesTableDrop`. The addColumnIfNotExists helper already covers this pattern in-line; only the drop-column and drop-table paths needed dedicated exported functions (they required SQLite version preflight + were more elaborate).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tiffany &amp;&amp; npx vitest run src/backend/database/db/index.migration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'runs_fleet_substrate' src/backend/database/db/index.ts` >= 1 (migration call added)
    - Source assertion: `grep -c 'runsFleetSubstrate' src/backend/database/db/schema.ts` equals 1 (Drizzle column added exactly once)
    - Source assertion: `grep -c 'phase-72-add-runs-fleet-substrate' src/backend/database/db/index.ts` equals 1 (forceSave reason string appears exactly once)
    - Source assertion: `grep -c 'DatabaseSaveTrigger.forceSave.*phase-72' src/backend/database/db/index.ts` equals 1 (forceSave call for this phase appears exactly once)
    - Source assertion (regression guard): `git diff src/backend/database/db/index.ts | grep -E '^\-.*CREATE TABLE IF NOT EXISTS ssh_data' | wc -l` equals 0 (the CREATE TABLE literal for ssh_data is unchanged)
    - Behavior assertion: `npx vitest run src/backend/database/db/index.migration.test.ts` reports all tests passing, including the new phase-72 describe block with 4 tests
    - Behavior assertion (typed query proof): the migration test file successfully imports `hosts` from `./schema.js` and asserts `hosts.runsFleetSubstrate` is defined
  </acceptance_criteria>
  <done>
    New Drizzle column `runsFleetSubstrate` declared on hosts table with boolean/default-false shape matching sibling enable_* columns. Migration adds `runs_fleet_substrate INTEGER NOT NULL DEFAULT 0` to ssh_data via addColumnIfNotExists, followed by wrapped forceSave with warn-not-throw catch. Migration test covers both OLD-schema-add path and NEW-schema-idempotent path plus row-backfill + Drizzle typed access. All three files staged for commit.
  </done>
</task>

</tasks>

<verification>
Plan-level verification:
1. `cd /home/ubuntu/skynet-tiffany &amp;&amp; npx vitest run src/backend/database/db/` runs green (existing tests + new phase-72 describe block)
2. `cd /home/ubuntu/skynet-tiffany &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -E "runs_fleet_substrate|runsFleetSubstrate" | wc -l` equals 0 (no type errors involving the new column)
3. Confirm the CREATE TABLE ssh_data literal is untouched: `git diff src/backend/database/db/index.ts | grep -c '^-.*CREATE TABLE IF NOT EXISTS ssh_data'` equals 0
</verification>

<success_criteria>
- runs_fleet_substrate column added on both OLD and NEW schemas via idempotent migration path
- Drizzle schema mirrors the DB column with boolean/default-false semantics
- forceSave wrapped in try/catch that warns-not-throws (matches phase-68 pattern exactly)
- Existing rows backfilled to 0 by SQLite's ALTER TABLE ADD COLUMN NOT NULL DEFAULT 0
- Migration test green including 4 new phase-72 tests
- Zero regression to CREATE TABLE ssh_data literal
</success_criteria>

<output>
Create `.planning/phases/72-feature-02-slice-2-reconcile-loop/72-02-SUMMARY.md` when done, capturing:
- Exact line numbers of the three edits
- Confirmation that the CREATE TABLE literal is unchanged
- Migration test output tail
- Drizzle column declaration line as it appears in schema.ts
</output>
</content>
</invoke>