---
phase: 117-projects-sidebar-section-with-drag-and-drop-membership-per-p
plan: 07
type: execute
wave: 3
depends_on:
  - 117-06
files_modified:
  - src/ui/state/conversation-store.ts
  - src/ui/state/conversation-store.projects.test.ts
  - src/ui/state/use-collapsed-project-slugs.ts
  - src/ui/state/use-collapsed-project-slugs.test.ts
  - src/ui/AppShell.tsx
  - src/backend/claude-session/identity-artifact-reader.ts
  - src/backend/fleet-status/identity-appearance.ts
  - src/backend/database/routes/identities.ts
  - src/ui/api/identities-api.ts
  - src/backend/database/routes/relay-room-project-tags-list.ts
  - src/backend/database/routes/relay-room-project-tags-list.test.ts
  - src/backend/database/database.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "conversation-store.ts grows a ProjectRow slice: setProjects(rows), upsertProjectRow (optional), useProjects() hook mirroring useArchivedFleetRows byte-shape from :391-432 + :1888-1967."
    - "The projects-derived selector produces buckets: {pinnedUnassigned, projectSections: Array<{slug, displayName, rows}>, middle, rdp} where each projectSection has its conversations sorted alphabetically by display name (D-15/D-16), and pinned-in-project rows float to the top of THAT section (D-19)."
    - "Pinned + unassigned conversations continue to float to the top pinnedUnassigned zone (existing behavior — D-19 preserves it)."
    - "The selector reads each conversation's project assignment via a helper that: (a) for identity-associated rows, checks the identity's frontmatter `project:` field; (b) for relay-room rows, checks the room's u.project.<slug> tag; (c) for RDP/terminal rows, always treats as unassigned (D-08 exclusion)."
    - "Missing/dangling project references (a slug pointing to a nonexistent project) degrade gracefully — the conversation falls into the flat middle, no crash (D-07)."
    - "use-collapsed-project-slugs.ts is a localStorage-backed hook exporting `useCollapsedProjectSlugs(): {collapsed: ReadonlySet<string>, toggle: (slug: string) => void}` — hydrate on mount, persist on toggle, silent try/catch on all reads/writes (mobile Safari private mode)."
    - "AppShell.tsx wires the fleet-status client's `onProjectListChanged` option to conversation-store's setProjects, so live project state flows from backend → wire → store → selector → sidebar."
    - "AppShell boot-time hydration triggers listProjects(hostId) for each managed host and merges results into setProjects so the initial render has the project list without waiting for the first WS snapshot."
  artifacts:
    - path: src/ui/state/conversation-store.ts
      provides: "ProjectRow type + projects slice + setProjects + useProjects hook + roomProjectAssignments slice + setRoomProjectAssignments + projects-derived selector integrated into the snapshot builder."
      exports: ["ProjectRow", "setProjects", "useProjects", "setRoomProjectAssignments"]
    - path: src/ui/state/use-collapsed-project-slugs.ts
      provides: "localStorage-backed collapse-state hook (D-40)."
      exports: ["useCollapsedProjectSlugs"]
    - path: src/ui/AppShell.tsx
      provides: "onProjectListChanged wired to setProjects + boot-time listProjects + listRelayRoomProjectTags hydration."
    - path: src/backend/claude-session/identity-artifact-reader.ts
      provides: "extractCosmeticsFromFrontmatter extended to surface project?: string in its return type and narrowing body."
    - path: src/backend/fleet-status/identity-appearance.ts
      provides: "Cosmetics + resolveIdentityAppearance return object extended with project (per-identity, not inherited from role)."
    - path: src/backend/database/routes/identities.ts
      provides: "publicIdentity cosmetics param + return object extended with project."
    - path: src/ui/api/identities-api.ts
      provides: "Frontend Identity interface extended with project: string | null."
    - path: src/backend/database/routes/relay-room-project-tags-list.ts
      provides: "GET /relay-rooms/project-tags?hostId=<n> endpoint returning {assignments: Array<{roomId, slug}>} by walking the user's rooms and filtering m.tag account_data for u.project.* tags."
      exports: ["default (Router)"]"
  key_links:
    - from: src/ui/AppShell.tsx
      to: src/ui/state/conversation-store.ts
      via: "onProjectListChanged → setProjects; boot-time listProjects → setProjects"
      pattern: "setProjects\\("
    - from: src/ui/state/conversation-store.ts
      to: identity frontmatter project field / relay-room u.project.<slug> tag
      via: "conversation → project assignment resolver used by the derived selector"
      pattern: "project"
    - from: src/ui/AppShell.tsx
      to: fleet-status-client onProjectListChanged option
      via: "createFleetStatusClient({...options, onProjectListChanged: setProjects})"
      pattern: "onProjectListChanged"
---

<objective>
Grow the frontend store with the projects slice + projects-derived selector + collapse-state hook, wire the fleet-status-client's project-list-changed frame to `setProjects` in AppShell, and hydrate the project list at boot via listProjects. This closes the loop from backend project state → wire event → store → derived selector → sidebar buckets.

The derived selector implements the load-bearing pinning-contextual-scope semantic from D-19: pinned + in-project rows float to the top of their project section (not the global pinned zone); pinned + unassigned rows continue to float to the global pinned zone. Missing/dangling project references degrade gracefully per D-07 (conversation falls into flat middle).

Purpose: this is the state layer the sidebar renders from. Wave 4's sidebar (117-08) reads the derived buckets from useProjects + the selector; the modal (117-09) reads useProjects to enumerate for boot-time UI state; the collapse-state hook drives the chevron indicators on each section header.

Output: extended conversation-store.ts, new use-collapsed-project-slugs.ts hook, extended AppShell.tsx wiring.
</objective>

<decision_coverage>
This plan implements the following CONTEXT.md locked decisions: D-05, D-07, D-08, D-11, D-15, D-16, D-18, D-19, D-20, D-37, D-39, D-40. Each cited decision is either directly enacted by a task's action step or implicitly enforced by the plan's structural choices (e.g., D-31 by writeSessionProjectField only firing on explicit clear; D-18/D-20 by the pinning-contextual-scope selector not touching the pin sentinel; D-17 by the absence of any drag-to-reorder handler in the panel). Missing D-XX citation ≠ missing implementation — the D-list here is the authoritative coverage map.
</decision_coverage>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-CONTEXT.md
@.planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-RESEARCH.md
@.planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-PATTERNS.md
@.planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ProjectRow slice + setProjects + useProjects + projects-derived selector + tests</name>
  <files>src/ui/state/conversation-store.ts, src/ui/state/conversation-store.projects.test.ts</files>
  <read_first>
    - src/ui/state/conversation-store.ts — READ IN FULL (2000+ lines; use section-anchored reads via grep + Read with offset+limit for each region). Anchor on: state type declaration (grep for `type State`, `interface State`), `archivedFleetRows: ArchivedFleetRow[]` field at :402, the ArchivedFleetRow type at :406-424, initial state block at :427-432, setArchivedFleetRows at :1903-1925, upsertArchivedFleetRow at :1927-1945, getArchivedFleetRowsSnapshot at :1947-1955, useArchivedFleetRows hook at :1957-1965, ACTIVE_SET_STORAGE_KEY + hydrateActiveSetFromStorage at :270-290 (localStorage discipline), computeSnapshot memoization at :1028 (where the derived selector integrates), the existing snapshot builder that produces {pinned, middle, rdp} — grep for `useConversations` or the exported snapshot getter to find it.
    - src/ui/state/conversation-store.test.ts — existing test file for the snapshot builder. Anchor on the tests that verify pinned promotion + row ordering — those are the templates for the projects-selector tests.
    - .planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-PATTERNS.md § "conversation-store.ts (extension)" for the exact ProjectRow shape + setter + hook + selector integration.
    - .planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-CONTEXT.md § D-05 (identity vs relay-room membership carriers), § D-15/D-16 (alphabetical by display name), § D-19 (pinning contextual scope), § D-07 (graceful degradation on dangling refs), § D-39 (derived selector), § D-08 (terminals excluded).
  </read_first>
  <behavior>
    - Test 1 (ProjectRow shape): setProjects([{slug:"a", displayName:"Alpha", hostId:"1", hostname:"t1000", archived:false}]) then useProjects() returns `[{slug:"a", ...}]`.
    - Test 2 (setProjects identity-equal skip): call setProjects twice with the same array; second call must NOT bump snapshotVersion or notify subscribers (byte-identity check like setArchivedFleetRows).
    - Test 3 (setProjects delta triggers notify): call setProjects([]) then setProjects([{slug:"a"...}]) — subscribers see 2 notifications total (initial state + one delta).
    - Test 4 (derived selector: pinned unassigned floats to pinnedUnassigned zone): mock 2 conversations, one pinned + no project, one unpinned + no project; assert the selector's output has the pinned one in `pinnedUnassigned` and the unpinned in `middle`.
    - Test 5 (derived selector: pinned + in-project floats to top of that section — D-19): 3 conversations, all with project="alpha", 2 unpinned + 1 pinned; assert the pinned one is FIRST in projectSections[0].rows, other two follow alphabetically by displayName. The pinned row is NOT in `pinnedUnassigned`.
    - Test 6 (derived selector: project sections sorted alphabetically by displayName): projects [{slug:"z", displayName:"Alpha"}, {slug:"a", displayName:"Zebra"}]; conversations distributed 1 each; assert projectSections order is [Alpha (slug=z), Zebra (slug=a)] — sorted by displayName, NOT by slug (D-15).
    - Test 7 (derived selector: dangling project ref falls to middle — D-07): conversation with project="ghost" but useProjects has no such slug; assert the conversation lands in `middle`, not in any projectSection. No crash.
    - Test 8 (derived selector: RDP row never in project section — D-08): conversation with rdpHostRow=true AND project="alpha" set (this shouldn't normally happen but defense-in-depth); assert the row stays in `rdp` bucket, NOT in projectSections. This prevents the terminal-with-project-poisoning bug (RESEARCH § Pitfall 3).
    - Test 9 (derived selector: empty project still shows up as a section — D-11): projects has {slug:"empty", displayName:"Empty"} but no conversation has project="empty"; assert projectSections contains that entry with rows=[].
    - Test 10 (identity conversation project resolution — mock frontmatter reader): a conversation with row.host=1, row.tmuxSession/identityKey="wren" — the selector needs to read the identity's frontmatter `project:` field. Since this data lives on disk and isn't in the wire snapshot, the selector reads from a passed-in map (e.g., `identityProjectAssignments: Map<string, string>` keyed by hostId::identityKey), which is hydrated from the identity list (existing useIdentities().byKey?) or from a new derived read. If the byKey structure already carries the frontmatter, use it; if not, extend the identity type to carry `project?: string | null` (this may require a Phase 25-style plumbing extension of `publicIdentity()`. Grep `publicIdentity` in the backend — if it already exposes arbitrary frontmatter fields, verify `project` is included; if not, add `project?: string | null` to the emitted shape). Test: mock the identity map with `wren → project="alpha"`; conversation for wren lands in the "alpha" projectSection.
    - Test 11 (relay-room conversation project resolution): a conversation with row.matrixRoomId="!abc:host" — the selector needs the room's u.project.<slug> tag. This is read from a separate map `roomProjectAssignments: Map<roomId, string>` hydrated from a new list-relay-room-tags endpoint OR from the wire event's projects payload (planner picks — recommend piggy-backing on the wire event's projects if it carries roomId; else add a boot-time enumerator). Test: mock the room map with `!abc:host → "alpha"`; assert the row lands in the "alpha" section.
    - Test 12 (empty state — no projects, no assignments): selector output has `projectSections: []`, all conversations in pinnedUnassigned or middle. Sanity check that the selector is not zero-crashy when useProjects is empty.
  </behavior>
  <action>
    Edit `src/ui/state/conversation-store.ts`:

    (1) Add the `ProjectRow` type near ArchivedFleetRow at :406-424:
       ```
       /**
        * Phase 117 Plan 07 (D-37, D-39): shape of a project entry in the
        * frontend. Mirrors the wire event payload from 117-03.
        */
       export type ProjectRow = {
         slug: string;
         displayName: string;
         hostId: string;
         hostname: string;
         archived: boolean;
       };
       ```

    (2) Add the state field:
       - In the State type: `projects: ProjectRow[];`
       - In initial state: `projects: [],`

    (3) Add setter + hook mirroring archivedFleetRows exactly (:1903-1965 shape):
       ```
       export function setProjects(rows: readonly ProjectRow[]): void {
         const cur = state.projects;
         if (rows.length === cur.length) {
           let equal = true;
           for (let i = 0; i < rows.length; i++) {
             const a = rows[i], b = cur[i];
             if (a.slug !== b.slug || a.displayName !== b.displayName || a.hostId !== b.hostId ||
                 a.hostname !== b.hostname || a.archived !== b.archived) { equal = false; break; }
           }
           if (equal) return;
         }
         state = { ...state, projects: rows.slice() };
         notify();  // (whatever the existing notify helper name is)
       }

       function getProjectsSnapshot(): readonly ProjectRow[] {
         return state.projects;
       }

       export function useProjects(): readonly ProjectRow[] {
         return useSyncExternalStore(subscribe, getProjectsSnapshot, getProjectsSnapshot);
       }
       ```
       (Match the existing subscribe/notify helper names — grep for them in the archivedFleetRows setter body.)

    (4) Add the derived-selector integration. Extend the snapshot builder (grep for the existing computeSnapshot / build-buckets logic at ~:1028+). The existing shape produces something like `{pinned, middle, rdp}`; extend it to `{pinnedUnassigned, projectSections, middle, rdp}` where:
       - `pinnedUnassigned = <rows that are pinned AND have no project assignment>`
       - `projectSections = <for each project in useProjects, alphabetical by displayName, an entry {slug, displayName, rows: [<pinned first, then rest alphabetical by displayName>]}>`
       - `middle = <rows that are not pinned, not RDP, and either have no project OR reference a nonexistent project (D-07 graceful degradation)>`
       - `rdp = <rows with rdpHostRow=true — unchanged, never in a project section per D-08>`

       For each conversation row, compute its project assignment via:
       - If `row.rdpHostRow === true` → assignment = null (D-08 terminal exclusion, defense-in-depth).
       - Else if `row.matrixRoomId` set → look up `roomProjectAssignments.get(row.matrixRoomId) ?? null`.
       - Else (identity-associated) → derive key from `${row.host}::${row.tmuxSession || row.identityKey}` and look up `identityProjectAssignments.get(key) ?? null`.
       - If assignment is a slug but NOT in the projects list, treat as null (graceful degradation D-07).

    (5) **Extend the backend Identity public shape to carry the `project:` frontmatter field** (confirmed necessary via grep at planning time — `extractCosmeticsFromFrontmatter` at identity-artifact-reader.ts:2487 is field-narrowing to 7 keys {displayName, title, colorHue, voice, avatar, coordinator, task}; `publicIdentity` at identities.ts:153 accepts those 7 cosmetics plus role/pinned; the frontend `Identity` interface at identities-api.ts:3-70 carries all 7 but no `project`). Four file edits, each additive:

       (5a) `src/backend/claude-session/identity-artifact-reader.ts` — extend `extractCosmeticsFromFrontmatter`'s return-type declaration AND its narrowing body to include `project?: string`. Follow the same non-empty-string narrowing pattern used for `task` at :2573-2576: `if (typeof src.project === "string" && src.project.length > 0) out.project = src.project;`. The regex-strict validation (PROJECT_SLUG_RE) is NOT applied here — the extractor is permissive (dangling / mistyped slugs are handled at render time by the selector's graceful-degradation branch per D-07). The strict slug regex fires only at the WRITE path (writeSessionProjectField from 117-01 Task 1).

       (5b) `src/backend/fleet-status/identity-appearance.ts` — extend the `Cosmetics` input type (grep for the type declaration around :36) to include `project?: string`, and extend `resolveIdentityAppearance`'s return object (grep for the return statement around :193-194 where `task` and `coordinator` are surfaced) to include `project: typeof cosmetics.project === "string" ? cosmetics.project : null`. Not inherited from role (project is per-identity, same discipline as `task` per D-05 write-once semantic).

       (5c) `src/backend/database/routes/identities.ts` — extend `publicIdentity`'s `cosmetics` parameter type (:156-168) to include `project?: string`, and extend the returned object literal (:205-243) to include `project: resolved.project` (from the resolveIdentityAppearance output extended in 5b). Position the field alongside `task` and `coordinator` for readability.

       (5d) `src/ui/api/identities-api.ts` — extend the frontend `Identity` interface (:3-70) with `project: string | null;` — non-optional to match the emitter's always-emit posture (backend emits `null` when absent, per identity-appearance's fallback in 5b). Position alongside `task` at :34.

    (6) **Add relay-room project-tag hydration endpoint** — the wire event's `projects` payload from 117-03 carries the project LIST only (slug/displayName/hostId/hostname/archived), NOT per-room memberships. A boot-time enumerator is needed so `roomProjectAssignments` has data.

       Create `src/backend/database/routes/relay-room-project-tags-list.ts`:
       - Route: `GET /relay-rooms/project-tags?hostId=<n>` — auth via authenticateJWT, resolveHostById-404 gate, then walks the user's Matrix rooms and calls `getRoomTags(userMxid, roomId)` (from 117-02) for each room, filtering the returned tags for keys matching `/^u\.project\./` and mapping to `{roomId, slug: key.slice("u.project.".length)}`. Response: `{assignments: Array<{roomId: string, slug: string}>}`.
       - **How to enumerate the user's rooms**: grep `src/backend/matrix/matrix-admin-client.ts` for existing room-list helpers (`listUserRooms`, `listJoinedRooms`, or equivalent). If a helper exists, reuse it. If NOT, use the Matrix Client-Server API `GET /_matrix/client/v3/joined_rooms` via the same `ensureUserToken`-based per-user token flow that 117-02 already established — add a small `listJoinedRooms(userMxid)` function to matrix-room-tag-client.ts as an additive extension.
       - Mount in `src/backend/database/database.ts` alongside the 117-05 relay-rooms mount: `app.use("/relay-rooms", relayRoomProjectTagsListRoutes);` (chains cleanly with the existing project-tag write route because they have non-overlapping sub-paths: `/project-tags` vs `/:roomId/project`).
       - Test file `relay-room-project-tags-list.test.ts` covering: happy path (2 rooms, 1 tagged) → returns 1 assignment; empty rooms → returns []; 404 on cross-user host; a room whose `getRoomTags` returns AdminErr is skipped (logged, not fatal — partial hydration is better than none).

       Frontend consumer: extend the boot-time hydration in Task 3 (AppShell) to fire `Promise.all(hosts.map(h => listRelayRoomProjectTags(h.id)))` in the same wave as the existing `listProjects` call. Aggregate the per-host {roomId, slug} pairs into a `Map<roomId, slug>` and pass into the store via a new setter `setRoomProjectAssignments(map)` (or, cleaner, embed the map as another slice on the store — planner picks; recommend a plain `Map` state field mirroring the ProjectRow slice). Add `src/ui/api/project-list-api.ts` a companion export `listRelayRoomProjectTags(hostId: number)` (extends 117-06's module — one-line fetch wrapper following the identity-archive-api byte-shape).

    (7) Create colocated `conversation-store.projects.test.ts` with the 12 tests. Follow the existing conversation-store.test.ts pattern for setting up store state and asserting snapshot output.

    **Relay-room hydration is NOT deferred**: D-05 explicitly names relay-room membership as one of two project-membership carriers (identity file frontmatter is the other). Deferring would be scope reduction. If context budget becomes tight during execution, the executor should surface the constraint back to the orchestrator — but attempt integrated first and only escalate on measurable overrun.

    Do NOT disturb existing state fields or hooks. All additions are additive; the existing snapshot consumers (Wave <117 sidebar rendering) must continue to work unchanged.
  </action>
  <acceptance_criteria>
    - `grep -c "^export type ProjectRow" src/ui/state/conversation-store.ts` outputs `1`.
    - `grep -c "^export function \\(setProjects\\|useProjects\\)" src/ui/state/conversation-store.ts` outputs `2`.
    - `grep -c "projects:" src/ui/state/conversation-store.ts | grep -v '^#'` shows the field is added to State type + initial state (≥ 2 hits in structural positions).
    - The derived-selector output type is extended: the type/interface of the snapshot builder's return value now includes `pinnedUnassigned`, `projectSections`, `middle`, `rdp` fields — verify via `grep -n "projectSections" src/ui/state/conversation-store.ts` shows ≥ 2 hits (type declaration + selector computation).
    - Backend Identity project extension is present:
      - `grep -c "project?: string" src/backend/claude-session/identity-artifact-reader.ts` outputs ≥ 1 (extractCosmeticsFromFrontmatter return type extended).
      - `grep -n "out.project = src.project" src/backend/claude-session/identity-artifact-reader.ts` outputs ≥ 1 line (narrowing branch added).
      - `grep -c "project" src/backend/fleet-status/identity-appearance.ts | grep -v '^#'` outputs ≥ 2 (Cosmetics input + resolveIdentityAppearance return object).
      - `grep -c "project: resolved.project\|project:" src/backend/database/routes/identities.ts | grep -v '^#'` outputs ≥ 1 (publicIdentity return object has project field).
      - `grep -c "^  project: string \| null" src/ui/api/identities-api.ts` outputs `1` (frontend Identity type has non-optional project field).
    - Relay-room project-tag hydration endpoint is present:
      - `grep -c "^router\.get("/project-tags"\|/relay-rooms/project-tags" src/backend/database/routes/relay-room-project-tags-list.ts` outputs ≥ 1.
      - `grep -c "relayRoomProjectTagsListRoutes\|relay-room-project-tags-list" src/backend/database/database.ts` outputs ≥ 2 (import + mount).
      - `npx vitest run src/backend/database/routes/relay-room-project-tags-list.test.ts` exits 0 with 4+ tests passing.
    - Task 1's selector test suite includes at least one test that hydrates BOTH identity project assignments AND relay-room project assignments and asserts both kinds of rows land in the correct project section (Test 10 + Test 11 in the behavior list — both must pass, not just one).
    - `npx vitest run src/ui/state/conversation-store.projects.test.ts src/ui/state/conversation-store.test.ts` exits 0 with all pre-existing tests still green AND the 12 new tests passing.
    - `npx tsc --noEmit -p tsconfig.json` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/ui/state/conversation-store.projects.test.ts src/ui/state/conversation-store.test.ts</automated>
  </verify>
  <done>ProjectRow slice + useProjects hook + derived selector all land; pinning-contextual-scope (D-19), alphabetical-by-displayName (D-15), graceful degradation (D-07), terminal exclusion (D-08), empty-section (D-11) all proven by tests. Existing conversation-store snapshot tests remain green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create use-collapsed-project-slugs.ts hook + tests</name>
  <files>src/ui/state/use-collapsed-project-slugs.ts, src/ui/state/use-collapsed-project-slugs.test.ts</files>
  <read_first>
    - src/ui/state/conversation-store.ts:270 — the `ACTIVE_SET_STORAGE_KEY` + `hydrateActiveSetFromStorage` pattern. The byte-shape template for the new hook.
    - .planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-PATTERNS.md § "use-collapsed-project-slugs.ts".
    - .planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-CONTEXT.md § D-40 (localStorage-backed, not server-synced).
  </read_first>
  <behavior>
    - Test 1 (hydrate empty): localStorage empty → `useCollapsedProjectSlugs()` returns `{collapsed: <empty Set>, toggle: fn}`.
    - Test 2 (hydrate from valid stored value): localStorage["pv-collapsed-project-slugs"] = `["alpha","beta"]`; hook returns collapsed set containing both.
    - Test 3 (hydrate silent on corrupt JSON): localStorage["pv-collapsed-project-slugs"] = `"not-json{"`; hook returns empty set (no throw).
    - Test 4 (hydrate silent on non-array): localStorage value = `'{"a":1}'`; hook returns empty set.
    - Test 5 (hydrate filters non-string entries): localStorage value = `'["a", 42, "b"]'`; hook returns set containing only "a" and "b".
    - Test 6 (toggle adds slug): initial empty; call toggle("alpha"); collapsed contains "alpha"; localStorage value becomes `["alpha"]`.
    - Test 7 (toggle removes slug): initial set contains "alpha"; call toggle("alpha"); set is empty; localStorage value becomes `[]`.
    - Test 8 (toggle silent on localStorage write failure): mock localStorage.setItem to throw (quota exceeded); call toggle; hook state still updates in-memory (no throw propagates).
  </behavior>
  <action>
    Create `src/ui/state/use-collapsed-project-slugs.ts`:

    (1) File header comment citing Phase 117 Plan 07 + D-40.

    (2) Constants:
       ```
       const COLLAPSED_PROJECT_SLUGS_STORAGE_KEY = "pv-collapsed-project-slugs";
       ```

    (3) hydrateFromStorage helper — mirrors conversation-store.ts:270 shape verbatim.

    (4) persistToStorage helper — try/catch silent.

    (5) `export function useCollapsedProjectSlugs()` — useState initialized via hydrateFromStorage, useCallback for toggle. Return `{collapsed: <Set>, toggle}`.

    (6) Create colocated test file. Mock localStorage via `Object.defineProperty(window, "localStorage", {value: mockLocalStorage})` or similar (vitest-jsdom canonical). Render the hook via `renderHook` from `@testing-library/react`.
  </action>
  <acceptance_criteria>
    - `grep -c "^export function useCollapsedProjectSlugs" src/ui/state/use-collapsed-project-slugs.ts` outputs `1`.
    - `grep -c "COLLAPSED_PROJECT_SLUGS_STORAGE_KEY = \"pv-collapsed-project-slugs\"" src/ui/state/use-collapsed-project-slugs.ts` outputs `1` (exact key literal for cross-reference by Wave 4 UI code).
    - `grep -c "try\\|catch" src/ui/state/use-collapsed-project-slugs.ts | grep -v '^#'` outputs ≥ 2 (silent-fail discipline for hydrate + persist).
    - `npx vitest run src/ui/state/use-collapsed-project-slugs.test.ts` exits 0 with 8+ tests passing.
    - `npx tsc --noEmit -p tsconfig.json` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/ui/state/use-collapsed-project-slugs.test.ts</automated>
  </verify>
  <done>Hook exists, localStorage-backed, silent on failure, 8+ tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire AppShell.tsx (onProjectListChanged + boot-time hydration)</name>
  <files>src/ui/AppShell.tsx</files>
  <read_first>
    - src/ui/AppShell.tsx:626-660 — the `createFleetStatusClient({url, onSnapshot, onUpdate, onIdentityArchived, onGone})` wiring block (Phase 115 template). Anchor on the exact call shape and the `upsertArchivedFleetRow` import for the identity-archived callback.
    - src/ui/state/conversation-store.ts — verify setProjects import path.
    - src/ui/api/project-list-api.ts (from 117-06) — verify listProjects signature.
    - .planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-PATTERNS.md § "AppShell.tsx (extension)".
  </read_first>
  <behavior>
    - Test (integration — via existing AppShell test file if present, else via a new one): after AppShell mounts, if the fleet-status-client receives a project-list-changed frame, setProjects is called with the frame's projects array. Also: AppShell fires listProjects(hostId) for each managed host during boot; the aggregated results are passed to setProjects.
    - Since AppShell has many side effects, this task's primary verification is that the WIRING is present (grep-verified) plus tsc + a scoped test if reasonable.
  </behavior>
  <action>
    Edit `src/ui/AppShell.tsx`:

    (1) Import `setProjects` from `@/state/conversation-store` alongside `upsertArchivedFleetRow`.

    (2) Import `listProjects` from `@/api/project-list-api`.

    (3) Extend the `createFleetStatusClient({...})` options object at ~:626 by adding `onProjectListChanged: (projects) => setProjects(projects)`. Insert alongside `onIdentityArchived`.

    (4) Add boot-time hydration: after AppShell resolves the list of managed hosts (grep for where the hosts list is materialized — likely inside a useEffect that also does listIdentities), fire `Promise.all(hosts.map(async (h) => listProjects(h.id).catch(() => ({projects: []}))))`. Aggregate the per-host results into a flat ProjectRow array (each host's listProjects returns `{projects: ProjectSummary[]}` — enrich each with hostId + hostname to become a ProjectRow). Call `setProjects(aggregated)` once.

    (5) The wire event will subsequently update setProjects when the backend fires publishProjectListChanged. Boot hydration and wire updates are unified through setProjects's identity-equal-skip semantic — if the boot-time list and the first WS snapshot are byte-identical, no re-render happens.

    (6) If AppShell already has a hosts-hydration flow, tuck the listProjects call into the same useEffect. Do NOT introduce a new useEffect if an existing one covers the same trigger.

    Do NOT modify the createFleetStatusClient signature or any other callback wiring. All changes are additive to the options object + one new hydration call.
  </action>
  <acceptance_criteria>
    - `grep -c "onProjectListChanged" src/ui/AppShell.tsx` outputs ≥ 1 (the option is passed).
    - `grep -c "setProjects\|listProjects\|listRelayRoomProjectTags\|setRoomProjectAssignments" src/ui/AppShell.tsx` outputs ≥ 5 (imports + wire callback + boot hydration for both projects AND relay-room tags).
    - `grep -c "listRelayRoomProjectTags" src/ui/AppShell.tsx` outputs ≥ 1 (Fix 1 gate — relay-room hydration is NOT deferred).
    - `npx vitest run src/ui/AppShell.test.tsx` (if it exists) exits 0. If no test file exists for AppShell today, this acceptance criterion becomes: `npx tsc --noEmit -p tsconfig.json` shows no new errors.
    - `npx tsc --noEmit -p tsconfig.json` exits 0.
    - Manual smoke: `grep -n "createFleetStatusClient" src/ui/AppShell.tsx` shows a single call site (regression guard — the wiring shouldn't have been duplicated).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>onProjectListChanged is wired to setProjects; boot-time listProjects + listRelayRoomProjectTags hydration both fire; tsc passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS server frame → setProjects | frame validated by wire-protocol.ts schema before reaching setProjects; input is trusted after parse |
| localStorage → useCollapsedProjectSlugs | user's own browser storage; no privilege boundary crossed |
| Frontend selector → user conversation buckets | pure client-side derivation; no data crosses trust boundary here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-07-01 | Tampering | Malformed frame reaches setProjects with garbage projects field | mitigate | FrontendOutboundFrame.parse (existing gate from 117-03) rejects malformed frames BEFORE dispatch; fleet-status-client's onProjectListChanged is only invoked with a validated ProjectRow[] shape. |
| T-117-07-02 | Denial of Service | Massive projects array crashes the client | accept | Realistic project count is small (< 20 per the user's fleet); the ~O(N × M) selector complexity is bounded. RESEARCH § Pitfall 4 verified. |
| T-117-07-03 | Info Disclosure | Sensitive data in localStorage collapse state | accept | Only slugs (kebab-case identifiers) stored; slugs are not sensitive. |
| T-117-07-04 | Tampering | Dangling project slug in identity frontmatter poisons UI | mitigate | D-07 graceful-degradation: dangling refs → row falls to middle; no crash. Test 7 asserts. |
| T-117-07-05 | Tampering | Terminal row assigned a project poisons the UI (id-skill would try to load a project on an RDP row) | mitigate | Frontend selector defensively drops project assignment for `rdpHostRow=true` rows (Test 8). Backend does not enforce this today (documented in 117-05 threat model as ACCEPTED), so this frontend gate is the primary defense. |
| T-117-07-SC | Tampering | npm installs | accept | No new packages installed. |
</threat_model>

<verification>
- `npx vitest run src/ui/state/conversation-store.projects.test.ts src/ui/state/conversation-store.test.ts src/ui/state/use-collapsed-project-slugs.test.ts` — all pass; existing coverage green.
- `npx tsc --noEmit -p tsconfig.json` — 0 errors.
- `grep -n "onProjectListChanged\\|setProjects\\|listProjects" src/ui/AppShell.tsx` — wiring present.
</verification>

<success_criteria>
Wave 4 sidebar (117-08) can `useProjects()` for the projects list and consume the derived selector's `projectSections` for rendering. The collapse-state hook is available for chevron toggles. AppShell wires wire events + boot-time hydration through setProjects.
</success_criteria>

<output>
Create `.planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-07-SUMMARY.md` when done. Include: (1) ProjectRow type + setProjects/useProjects signatures verbatim, (2) the derived selector's output type (with new fields), (3) whether the identity `project` field was added to the backend publicIdentity emitter and the frontend Identity type (yes/no + file paths), (4) whether relay-room project assignments are hydrated (yes/no; if no, which endpoint or gap remains for follow-up), (5) test counts.
</output>
