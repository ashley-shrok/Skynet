---
phase: 117-projects-sidebar-section-with-drag-and-drop-membership-per-p
plan: 10
type: execute
wave: 6
depends_on:
  - 117-09
files_modified:
  - substrate/skills/id/SKILL.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "substrate/skills/id/SKILL.md contains a new step in § 2 (Loading an existing identity) placed AFTER the existing 'Read any per-identity specialization' step and BEFORE the runbooks-enumeration step."
    - "The new step reads the identity file's frontmatter for a `project:` key; if present, reads `~/fleet/projects/<slug>/project.md` into context and silently enumerates the top-level contents of the project directory."
    - "Graceful no-op: if the frontmatter has no `project:` field, OR the slug points to a nonexistent directory, OR the slug points to an archived project at `~/fleet/projects/archive/<slug>/`, the step continues without loading (D-33)."
    - "The step matches the shape of the existing runbooks-enumeration step in the same file (silent enumerate, on-demand read via a normal Read tool call)."
    - "Existing runbooks enumeration step is renumbered from step 4 to step 5 (or whatever the number shift is) to accommodate the insertion."
    - "The coordinator-mode block in the SKILL.md is UNCHANGED — coordinators skip the role-file-load path per D-32 note and do not trigger the project-load clause."
    - "distributor/catalog.ts is UNCHANGED — SKILL.md is already a 4-row catalog entry at :216-244; the byte-compare sweep picks up the body edit automatically on the next tick."
  artifacts:
    - path: substrate/skills/id/SKILL.md
      provides: "id-skill body with the project-awareness clause inserted at § 2 step 4."
  key_links:
    - from: substrate/skills/id/SKILL.md new step
      to: ~/fleet/projects/<slug>/project.md and its directory listing
      via: "Read tool + Glob/LS on the project directory — same shape as the existing runbooks enumeration"
      pattern: "project:"
    - from: src/backend/distributor/catalog.ts
      to: substrate/skills/id/SKILL.md
      via: "byte-compare sweep detects the body change and pushes to every host on next tick"
      pattern: "id/SKILL.md"
---

<objective>
Edit `substrate/skills/id/SKILL.md` to add the project-awareness clause per D-32/D-33. Inserted into § 2 "Loading an existing identity" between the existing step that reads per-identity specialization and the existing runbooks-enumeration step. New step: check identity frontmatter for `project:`, if present read `~/fleet/projects/<slug>/project.md` + enumerate directory silently; graceful no-op on missing/dangling/archived.

Purpose: this is the substrate half of the phase. Everything Waves 1-5 built lets the sidebar assign projects; this step lets the AGENT know it's IN a project when it loads. The amendment ships via the fleet-substrate distributor's next sweep (no code change — SKILL.md is already a distributor catalog entry).

Ships LAST (Wave 6) so we UAT the sidebar first via 117-09's human-verify, then land the substrate change once the sidebar is proven. This ordering matches the phase-scope-hints guidance: "the id-skill amendment lands last so we can UAT the sidebar first."

Output: single-file body edit to SKILL.md; no code change; distributor picks up automatically.
</objective>

<decision_coverage>
This plan implements the following CONTEXT.md locked decisions: D-32, D-33, D-34, D-35. Each cited decision is either directly enacted by a task's action step or implicitly enforced by the plan's structural choices (e.g., D-31 by writeSessionProjectField only firing on explicit clear; D-18/D-20 by the pinning-contextual-scope selector not touching the pin sentinel; D-17 by the absence of any drag-to-reorder handler in the panel). Missing D-XX citation ≠ missing implementation — the D-list here is the authoritative coverage map.
</decision_coverage>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-CONTEXT.md
@.planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-RESEARCH.md
@.planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert project-awareness clause into substrate/skills/id/SKILL.md</name>
  <files>substrate/skills/id/SKILL.md</files>
  <read_first>
    - substrate/skills/id/SKILL.md — READ IN FULL. Anchor on: § 2 "Loading an existing identity" section header; the numbered-list within it; identify the exact numbered step that says "Read any per-identity specialization from the slim identity file..." (or equivalent — this is the step that reads the identity file's frontmatter for role/task/etc.); identify the NEXT numbered step (the runbooks-enumeration step). The insertion goes BETWEEN these two. Also read the § "Coordinator mode" block at the end of § 2 to verify it's structurally SEPARATE from the numbered list (so renumbering the runbooks step doesn't propagate into coordinator mode).
    - src/backend/distributor/catalog.ts:216-244 — verify the 4-row catalog entry for the id-skill (SKILL.md + 3 companions: actor-status-prompt.md, clone-picker-prompt.md, coordinator-instructions.md). No change needed here; just confirm the entry exists so the sweep picks up SKILL.md's body change.
    - .planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-CONTEXT.md § D-32, § D-33, § D-34, § D-35.
    - .planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-RESEARCH.md § "id-skill Amendment Draft" for the exact text to insert.
    - .planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-PATTERNS.md § "substrate/skills/id/SKILL.md (body edit — one insertion)" for the insertion-point specification.
  </read_first>
  <behavior>
    - Behavior (not tests — this is a documentation edit): after this task, when an agent runs `/id <name>`, the SKILL.md instructions direct it to:
      1. Read the role file (existing step).
      2. Read any handoff (existing step).
      3. Read the identity file frontmatter for role/task specialization (existing step).
      4. **NEW: Read the project file, if any.** Check the identity file's frontmatter for a `project: <slug>` key. If present, read `~/fleet/projects/<slug>/project.md` into context and silently enumerate `~/fleet/projects/<slug>/`'s top-level contents. If missing/dangling/archived, graceful no-op.
      5. Silently enumerate the runbooks directory (existing step, renumbered).
    - Backward compat: identities WITHOUT a `project:` frontmatter field load exactly as they do today (D-35).
    - The step's language matches the runbooks-enumeration step's style (silent enumerate, on-demand read).
  </behavior>
  <action>
    Edit `substrate/skills/id/SKILL.md`:

    (1) Locate the insertion point in § 2 "Loading an existing identity". Find the numbered list. Identify the two adjacent steps:
       - The step that ends with reading role/task/frontmatter from the identity file (likely numbered step 3 per RESEARCH.md, but VERIFY by direct read of the current file — the numbering may have shifted since RESEARCH.md was written; if so, adjust accordingly).
       - The following step that enumerates runbooks (likely numbered step 4).

    (2) Insert the new step BETWEEN them. The new step's text (draft per RESEARCH.md § id-skill Amendment Draft):

       ```
       N. **Read the project file, if any.** Check the identity file's frontmatter for a
          `project: <slug>` key. If present:
          - Read `~/fleet/projects/<slug>/project.md` into context — it names what this
            project is for, plus any shared conventions or references the project needs.
          - Silently enumerate the top-level contents of `~/fleet/projects/<slug>/`. Hold
            the names in context. Each file's contents load on demand via a normal Read
            tool call — same shape as the runbooks enumeration below.
          - If the frontmatter has no `project:` field, OR the slug points to a directory
            that doesn't exist on disk, OR points to an archived project at
            `~/fleet/projects/archive/<slug>/`, this step is a graceful no-op — continue
            without it.
       ```

       Where `N` is the correct step number based on the current SKILL.md numbering. **If the existing steps use flat integers (`1.`, `2.`, `3.`, `4.`)**, insert at the appropriate position and RENUMBER the runbooks-enumeration step and any subsequent numbered steps in § 2 by +1. **If the numbering restarts per subsection or uses non-integer labels**, adjust accordingly — the executor should read the current numbering style and match it verbatim.

    (3) Do NOT modify any other section of the file. In particular:
       - The § "Coordinator mode" block MUST remain byte-identical (D-32 note: coordinators skip the role-file-load path and do not trigger this clause).
       - The § companions (references to actor-status-prompt.md, clone-picker-prompt.md, coordinator-instructions.md) MUST remain byte-identical.
       - The YAML frontmatter at the top of SKILL.md (if any — including `distributed: true` or similar) MUST remain byte-identical.

    (4) Do NOT edit any of the 3 companion files (actor-status-prompt.md, clone-picker-prompt.md, coordinator-instructions.md) — the amendment is body-only per D-34.

    (5) Do NOT edit `src/backend/distributor/catalog.ts` — the entry at :216-244 already covers SKILL.md; the sweep picks up body changes via byte-compare.

    (6) After the edit, verify the numbering is coherent by re-reading § 2 as a whole.
  </action>
  <acceptance_criteria>
    - `grep -c "Read the project file, if any" substrate/skills/id/SKILL.md` outputs `1` (the new step's exact opening line).
    - `grep -c "fleet/projects/<slug>/project.md" substrate/skills/id/SKILL.md` outputs ≥ 1 (the eager-read path is documented).
    - `grep -c "fleet/projects/archive/<slug>" substrate/skills/id/SKILL.md` outputs ≥ 1 (the archived-project no-op clause is documented).
    - `grep -c "graceful no-op" substrate/skills/id/SKILL.md` outputs ≥ 1.
    - Companion files unchanged: `git diff substrate/skills/id/actor-status-prompt.md substrate/skills/id/clone-picker-prompt.md substrate/skills/id/coordinator-instructions.md` shows no output.
    - Distributor catalog unchanged: `git diff src/backend/distributor/catalog.ts` shows no output.
    - `git diff --name-only substrate/skills/id/` shows exactly one file: `substrate/skills/id/SKILL.md`.
    - The numbering in § 2 is coherent post-edit: reading it top-to-bottom, the numbers are sequential with no gaps.
    - `npx tsc --noEmit -p tsconfig.json` exits 0 (no code touched, but run as a regression guard).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'grep -c "Read the project file, if any" substrate/skills/id/SKILL.md | grep -q "^1$" && grep -c "fleet/projects/archive/<slug>" substrate/skills/id/SKILL.md | grep -qE "^[1-9]" && test $(git diff --name-only substrate/skills/id/ | wc -l) -eq 1'</automated>
  </verify>
  <done>SKILL.md § 2 has a new project-awareness step inserted between the identity-file-specialization step and the runbooks-enumeration step. Companion files + distributor catalog + coordinator-mode block are all unchanged. Numbering coherent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md body → distributor sweep → managed hosts | body content ships to every managed host on next sweep; substrate repo is the authority |
| Agent loading /id <name> → project.md read | new instruction reads a file from disk; slug value is trusted (comes from the identity's own frontmatter, which the user authored) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-10-01 | Tampering | Agent following the instruction reads a project file with malicious markdown content | accept | The `project:` slug in an identity's frontmatter is controlled by the same user who owns the identity file; agent trusts its own configuration. This is the same trust model already applied to role files and runbooks. |
| T-117-10-02 | Info Disclosure | Agent silently enumerates a project directory containing sensitive files | accept | Enumeration is name-only (like runbooks); contents load on demand. The user is the only party who put files there. No cross-user boundary. |
| T-117-10-03 | Denial of Service | Very large project.md eagerly loaded into every /id load | accept | User controls the size of their own project.md; if it becomes unwieldy, they trim it. No enforcement here. |
| T-117-10-04 | Tampering | SKILL.md edit breaks the numbered-list structure of § 2 in a way that confuses agents | mitigate | Acceptance criterion asserts numbering is coherent (sequential, no gaps). Executor manually re-reads § 2 post-edit. |
| T-117-10-05 | Tampering | Body edit accidentally clobbers § "Coordinator mode" block or other sections | mitigate | Acceptance criterion asserts git diff scope is limited to § 2's numbered-list region. |
| T-117-10-SC | Tampering | npm installs | accept | No new packages installed (no code touched). |
</threat_model>

<verification>
- Grep gates in acceptance criteria confirm the new step is present with the correct text.
- `git diff --name-only substrate/skills/id/` shows exactly one file changed.
- `git diff --name-only src/backend/distributor/` shows no output.
- `npx tsc --noEmit -p tsconfig.json` exits 0 (regression guard on unrelated code).
</verification>

<success_criteria>
The next fleet-substrate distributor sweep after deploy will push the updated SKILL.md body to every managed host. From that point onward, agents loading `/id <name>` will check for a `project:` frontmatter field and load the project's context file + silent directory enumeration if present. Backward-compatible for identities without a `project:` field.

**Post-deploy notification**: Per shape file's Vehicle notes and RESEARCH § Pitfall 6, peer identities need a relay DM one turn after successful deploy so they know projects is a live substrate concept they can start using. This notification is orchestrator-owned (not executor scope).
</success_criteria>

<output>
Create `.planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-10-SUMMARY.md` when done. Include: (1) the exact insertion location (step number, line range in SKILL.md), (2) the number of subsequent steps renumbered (if any), (3) confirmation that companion files and catalog.ts are byte-identical, (4) the exact numbering scheme in § 2 post-edit (list of step numbers as a sanity check).
</output>
