---
phase: 117-projects-sidebar-section-with-drag-and-drop-membership-per-p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/claude-session/identity-artifact-reader.ts
  - src/backend/claude-session/identity-artifact-reader.projects.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "PROJECT_SLUG_RE constant matches /^[a-z0-9-]{1,64}$/ and is exported (per D-04 kebab-case lowercased)."
    - "getLocalProjectsRoot() returns process.env.PROJECTS_HOST_DIR when set, else os.homedir()/fleet/projects."
    - "readSessionProjectField(conn, identityKey) returns the string in the identity file's frontmatter `project:` key, or null if missing / empty / non-string / no frontmatter."
    - "writeSessionProjectField(conn, identityKey, slug|null) sets or deletes the frontmatter `project:` key via a FULL yaml.load (not extractCosmeticsFromFrontmatter) round-trip that preserves ALL other frontmatter keys, and writes atomically via writeMarkdownFileAtomic."
    - "writeSessionProjectField with slug=null DELETES the key from the frontmatter dict (absent-⇒-omit invariant); never emits `project: null` or `project: ''`."
    - "listProjects(conn) enumerates $HOME/fleet/projects/ excluding the archive/ subdirectory (per D-30) and returns [{slug, displayName}] where displayName comes from each project.md's frontmatter, falling back to the slug if displayName is absent."
    - "readProjectFile(conn, slug) reads $HOME/fleet/projects/<slug>/project.md via the LOCAL/REMOTE branch, returning {markdown: string} (empty string on ENOENT per readIdentityFile precedent)."
    - "createProject(conn, slug, displayName) creates the directory + writes a bare project.md with frontmatter `displayName: <value>` and empty body via writeMarkdownFileAtomic; returns 'exists' sentinel-error (or throws distinguishable EEXIST) if the slug directory already exists."
    - "archiveProject(conn, slug) moves $HOME/fleet/projects/<slug>/ to $HOME/fleet/projects/archive/<slug>/ (mirrors identity-archive tree destination pattern)."
    - "Every function branches on conn===null (LOCAL: fs.promises) vs conn!==null (REMOTE: SSH exec + SFTP) — same discipline as readIdentityFile at :441-475."
    - "All slug interpolations into remote shell commands pass through the slug regex gate BEFORE interpolation; slug injection is guarded (belt-and-suspenders per runbooks-editor.ts:49-51)."
  artifacts:
    - path: src/backend/claude-session/identity-artifact-reader.ts
      provides: "PROJECT_SLUG_RE + 7 new exported functions: getLocalProjectsRoot, readSessionProjectField, writeSessionProjectField, listProjects, readProjectFile, createProject, archiveProject."
      exports: ["PROJECT_SLUG_RE", "getLocalProjectsRoot", "readSessionProjectField", "writeSessionProjectField", "listProjects", "readProjectFile", "createProject", "archiveProject"]
    - path: src/backend/claude-session/identity-artifact-reader.projects.test.ts
      provides: "Vitest coverage for all 7 new methods: happy LOCAL, happy REMOTE (mocked SSH conn), slug-regex reject, missing-file behavior, frontmatter preservation on round-trip, directory-move on archive."
  key_links:
    - from: src/backend/claude-session/identity-artifact-reader.ts
      to: js-yaml
      via: "yaml.load(frontmatterRaw) full parse + yaml.dump with sortKeys:false, lineWidth:-1, noRefs:true, forceQuotes:false"
      pattern: "yaml\\.(load|dump)"
    - from: writeSessionProjectField
      to: writeMarkdownFileAtomic
      via: "atomic tmp+rename write for the identity file frontmatter rewrite"
      pattern: "writeMarkdownFileAtomic\\("
    - from: createProject
      to: writeMarkdownFileAtomic
      via: "atomic write of bare project.md after mkdir -p"
      pattern: "writeMarkdownFileAtomic\\("
---

<objective>
Extend `src/backend/claude-session/identity-artifact-reader.ts` with the on-disk project primitives called by the route layer (Wave 2/3). Adds PROJECT_SLUG_RE (per D-04 lowercase-kebab-64), getLocalProjectsRoot, readSessionProjectField, writeSessionProjectField (full-frontmatter-round-trip per Pitfall 5), listProjects (excluding archive/ per D-30), readProjectFile, createProject (with dupe-slug detection per D-25), and archiveProject (folder-move to archive/ subdir per D-30). Every method follows the existing LOCAL/REMOTE branch discipline verbatim, uses writeMarkdownFileAtomic for durability, and validates slug BEFORE any I/O.

Purpose: this is the substrate layer. Every route in Wave 2 (117-04) and Wave 3 (117-05), plus the archive-cascade in 117-09, calls into these functions. Without them, nothing else in the phase can land.

Output: 7 new exported functions in identity-artifact-reader.ts, PROJECT_SLUG_RE constant, colocated vitest test file with LOCAL + REMOTE branch coverage.
</objective>

<decision_coverage>
This plan implements the following CONTEXT.md locked decisions: D-01, D-02, D-03, D-04, D-05, D-25, D-30, D-31, D-32, D-36. Each cited decision is either directly enacted by a task's action step or implicitly enforced by the plan's structural choices (e.g., D-31 by writeSessionProjectField only firing on explicit clear; D-18/D-20 by the pinning-contextual-scope selector not touching the pin sentinel; D-17 by the absence of any drag-to-reorder handler in the panel). Missing D-XX citation ≠ missing implementation — the D-list here is the authoritative coverage map.
</decision_coverage>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-CONTEXT.md
@.planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-RESEARCH.md
@.planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PROJECT_SLUG_RE + getLocalProjectsRoot + readSessionProjectField + writeSessionProjectField + tests</name>
  <files>src/backend/claude-session/identity-artifact-reader.ts, src/backend/claude-session/identity-artifact-reader.projects.test.ts</files>
  <read_first>
    - src/backend/claude-session/identity-artifact-reader.ts (open the full file — this task is a self-extension). Specifically anchor on: `IDENTITY_KEY_RE` at :175, `getLocalIdentitiesRoot` at :218, `readIdentityFile` at :441-475, `writeMarkdownFileAtomic` at :1945-2073, `extractCosmeticsFromFrontmatter` at :2487 (understand what it does — DO NOT call it from the new writer per Pitfall 5), `writeIdentityFile` at :2861 (long-form atomic-file variant — use this for identity-md rewrites).
    - src/backend/database/routes/identity-birth-orchestrator.ts:495-586 (buildIdentityFileBody — canonical `yaml.dump` options with `sortKeys:false, lineWidth:-1, noRefs:true, forceQuotes:false` and the absent-⇒-omit invariant).
    - .planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-RESEARCH.md § Common Pitfalls #5 (frontmatter write clobbers other fields — MUST use full yaml.load, NOT extractCosmeticsFromFrontmatter) and § Code Examples "Reading + Rewriting an Identity File's `project:` Frontmatter".
    - .planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-PATTERNS.md § "identity-artifact-reader.ts (utility extension)" for the exact snippet of writeSessionProjectField and § "Shared Patterns — YAML frontmatter emit — absent-⇒-omit invariant".
    - src/backend/database/routes/identity-archive.test.ts (test-file structure reference — how to mock SSH conn, how to structure describe/it, how vi.mock('node:fs/promises') is used elsewhere).
  </read_first>
  <behavior>
    - Test 1 (PROJECT_SLUG_RE valid): "abc", "a-b-c", "proj-1", "0-9", "a"*64 all match; length 65 does not; "A" does not; "abc_def" does not (underscore rejected — narrower than IDENTITY_KEY_RE per D-04); "abc/def" does not; empty string does not.
    - Test 2 (getLocalProjectsRoot): returns process.env.PROJECTS_HOST_DIR when set to "/tmp/x"; when unset, returns path.join(os.homedir(), "fleet", "projects").
    - Test 3 (readSessionProjectField LOCAL happy): mock fs.readFile to return `---\nrole: worker\nproject: alpha\ndisplayName: 'W'\n---\nbody`; assert returns "alpha".
    - Test 4 (readSessionProjectField LOCAL missing key): mock frontmatter with `role: worker\ndisplayName: 'W'` (no project); assert returns null.
    - Test 5 (readSessionProjectField LOCAL no frontmatter): mock file body with no `---` fence; assert returns null (graceful, not throw).
    - Test 6 (readSessionProjectField LOCAL empty file): mock ENOENT via readIdentityFile fallback; assert returns null.
    - Test 7 (writeSessionProjectField LOCAL set): starting file has frontmatter `role: worker\ndisplayName: 'W'\ntask: 'foo'`; call with slug="alpha"; assert final markdown contains `project: alpha`, still contains role/displayName/task in same relative order, and body-after is byte-identical.
    - Test 8 (writeSessionProjectField LOCAL update): starting file has `project: alpha`; call with slug="beta"; assert final has `project: beta`, no `alpha` string remains in frontmatter.
    - Test 9 (writeSessionProjectField LOCAL clear): starting file has `project: alpha`; call with slug=null; assert final has NO `project:` key at all, and role/displayName/task all preserved.
    - Test 10 (writeSessionProjectField reject bad slug): call with slug="Alpha" (uppercase); assert throws with message including "invalid project slug".
    - Test 11 (writeSessionProjectField reject bad identity key): call with "BADKEY!"; assert throws with "invalid identityKey".
    - Test 12 (writeSessionProjectField unknown-key preservation): starting file has custom frontmatter key `custom: foo` alongside role/displayName; call with slug="alpha"; assert `custom: foo` survives the round-trip (Pitfall 5 regression guard — proves the writer is NOT going through extractCosmeticsFromFrontmatter).
    - Test 13 (writeSessionProjectField REMOTE branch): pass a mock SSHClientType conn (execWithTimeout mocked); assert readIdentityFile's REMOTE cat command is invoked and writeMarkdownFileAtomic REMOTE branch fires with the expected target path `$HOME/fleet/identities/<key>/<key>.md`.
  </behavior>
  <action>
    Add to `identity-artifact-reader.ts` at appropriate insertion points (do not renumber existing line-anchored comments):

    (1) Export new constant `PROJECT_SLUG_RE = /^[a-z0-9-]{1,64}$/` — colocate near `IDENTITY_KEY_RE` at :175. Add a JSDoc comment noting D-04 (kebab-case lowercased, narrower than IDENTITY_KEY_RE which allows underscore) and citing Phase 117 CONTEXT.

    (2) Export `getLocalProjectsRoot(): string` — colocate near `getLocalIdentitiesRoot` at :218. Body: `return process.env.PROJECTS_HOST_DIR ?? path.join(os.homedir(), "fleet", "projects");`. JSDoc cites D-01 (projects live at `~/fleet/projects/<slug>/`) and matches getLocalIdentitiesRoot signature/return-type shape.

    (3) Export `readSessionProjectField(conn: SSHClientType | null, identityKey: string): Promise<string | null>`:
       - Validate identityKey against IDENTITY_KEY_RE; throw on mismatch.
       - Call the existing `readIdentityFile(conn, identityKey)` — returns `{markdown: string}`. If markdown is empty string, return null (identity file missing).
       - Match frontmatter regex `/^---\r?\n([\s\S]*?)\r?\n---\r?\n[\s\S]*$/`. If no match, return null (no frontmatter block).
       - `yaml.load(frontmatterRaw)` — wrap in try/catch, return null on parse failure (do NOT throw — a broken identity file should not break every consumer).
       - Read the `project` key from the parsed dict; return null unless typeof === "string" AND length > 0.
       - JSDoc cites D-05, D-32, and the fact that this reads FROM the IDENTITY file (`~/fleet/identities/<key>/<key>.md`), NOT any session JSONL file — Pitfall 2 in RESEARCH.md.

    (4) Export `writeSessionProjectField(conn: SSHClientType | null, identityKey: string, projectSlug: string | null): Promise<void>`:
       - Validate identityKey via IDENTITY_KEY_RE (throw "invalid identityKey" on mismatch).
       - Validate projectSlug — if !== null, must match PROJECT_SLUG_RE (throw "invalid project slug").
       - Call `readIdentityFile(conn, identityKey)`. If markdown is empty (ENOENT), throw with "identity file missing".
       - Match frontmatter regex — if no match, throw with "identity has no frontmatter".
       - `yaml.load(frontmatterRaw)` as `Record<string, unknown>` — full parse, NOT extractCosmeticsFromFrontmatter. Fall back to `{}` on null. Throw on parse error (this is a write path — cannot proceed on unparseable input).
       - If projectSlug === null → `delete parsed.project` (absent-⇒-omit invariant per identity-birth-orchestrator.ts:495-586). Else → `parsed.project = projectSlug`.
       - `yaml.dump(parsed, {sortKeys: false, lineWidth: -1, noRefs: true, forceQuotes: false})` — same options as identity-birth-orchestrator.ts:575.
       - Concatenate `` `---\n${yamlBody}---\n${bodyAfter}` ``.
       - Call `writeMarkdownFileAtomic(conn, ` `$HOME/fleet/identities/${identityKey}/${identityKey}.md` `, newContents)` — the $HOME-prefix form is resolved inside writeMarkdownFileAtomic (LOCAL: IDENTITIES_HOST_DIR / os.homedir fallback; REMOTE: sftp.realpath("."). See :1968-2005).
       - JSDoc cites D-05, Pitfall 5 (full-parse discipline), and lists which fields survive round-trip (all — this is the whole point).

    (5) Create colocated test file `identity-artifact-reader.projects.test.ts`. Mock `fs.readFile`, `fs.writeFile`, `fs.rename`, `fs.mkdir` via `vi.mock("node:fs/promises")`. For REMOTE-branch tests, construct a minimal mock `SSHClientType` object and stub `execWithTimeout` via `vi.mock("./exec-with-timeout")` OR whichever module houses it (grep the file for its import path; do not guess). Follow the describe/it layout from identity-archive.test.ts. Every test's assertion targets exact field values or exact command strings — no "reasonable-looking" checks.

    Do NOT modify any existing exports; PROJECT_SLUG_RE and the new functions must be additive. If any existing test breaks after adding constants (unlikely), stop and diagnose — do not "fix" the existing tests to accommodate.
  </action>
  <acceptance_criteria>
    - `grep -c "^export const PROJECT_SLUG_RE" src/backend/claude-session/identity-artifact-reader.ts` outputs `1`.
    - `grep -c "^export function \\(getLocalProjectsRoot\\|readSessionProjectField\\|writeSessionProjectField\\)" src/backend/claude-session/identity-artifact-reader.ts` outputs `3`.
    - `grep -c "extractCosmeticsFromFrontmatter" src/backend/claude-session/identity-artifact-reader.ts | grep -v '^#'` count for the new methods MUST be `0` inside the function bodies of `readSessionProjectField` and `writeSessionProjectField` (Pitfall 5 gate — verify by inspecting the two function bodies with `grep -n -A 40 "^export function writeSessionProjectField"`).
    - `npx vitest run src/backend/claude-session/identity-artifact-reader.projects.test.ts` exits 0 with at least 13 passing tests covering the behaviors above.
    - `npx tsc --noEmit -p tsconfig.json` exits 0 with no new errors introduced.
    - PROJECT_SLUG_RE regex literal in the source file is byte-identical to `/^[a-z0-9-]{1,64}$/` (no case-insensitive `i` flag, no `_` in character class).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/backend/claude-session/identity-artifact-reader.projects.test.ts</automated>
  </verify>
  <done>13+ tests pass covering slug regex, root resolver, read/write happy paths, clear semantics, unknown-key preservation (Pitfall 5 guard), invalid-input rejects, and REMOTE branch invocation. TypeScript compiles cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add listProjects + readProjectFile + createProject + archiveProject + tests</name>
  <files>src/backend/claude-session/identity-artifact-reader.ts, src/backend/claude-session/identity-artifact-reader.projects.test.ts</files>
  <read_first>
    - src/backend/claude-session/identity-artifact-reader.ts §§ listIdentityKeysOnHost at :506-540 (list-directory template, LOCAL fs.readdir vs REMOTE find pattern), writeMarkdownFileAtomic at :1945 (atomic write template — reuse verbatim for project.md creation), execWithTimeout usage (grep for import). Also anchor on the shellEscape helper (search the file for `shellEscape` / `shellSingleQuote` — likely near the SSH exec helpers).
    - src/backend/database/routes/runbooks-editor.ts:107-158 (RUNBOOK_NAME_RE + shellEscape shape — reference for the belt-and-suspenders absPath.startsWith guard; and :251-386 for the list-directory find command shape).
    - .planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-PATTERNS.md § "identity-artifact-reader.ts (utility extension)" adaptations 5-8 (listProjects find command, readProjectFile path, createProject mkdir-then-atomic-write, archiveProject folder-move).
    - .planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-RESEARCH.md § Security Domain (path-traversal via slug, shell injection — PROJECT_SLUG_RE gate BEFORE I/O + shellEscape belt-and-suspenders for any user-supplied value in exec).
  </read_first>
  <behavior>
    - Test 1 (listProjects LOCAL happy): mock fs.readdir on $HOME/fleet/projects/ to return three dirents: `alpha` (dir), `beta` (dir), `archive` (dir); each with a `project.md` mocked containing `---\ndisplayName: 'Alpha One'\n---\n`. Call listProjects(null); assert returns `[{slug:"alpha", displayName:"Alpha One"}, {slug:"beta", displayName:"Beta"}]` sorted by slug alphabetically. The `archive` entry is EXCLUDED.
    - Test 2 (listProjects displayName fallback): if a project.md has no displayName in frontmatter (or the file is missing), the returned displayName equals the slug. Also: a project directory with no project.md at all should still appear in the list (best-effort — the directory exists, so it's a project).
    - Test 3 (listProjects LOCAL missing root): mock fs.readdir to throw ENOENT on $HOME/fleet/projects/; assert returns [] (graceful, per readIdentityFile pattern).
    - Test 4 (listProjects REMOTE happy): mock execWithTimeout to return `alpha\nbeta\n` for the find command; assert returns two entries sorted, and the find command string contains `! -name archive` (archive-exclusion gate).
    - Test 5 (listProjects REMOTE only-slug-valid entries): mock execWithTimeout to return `alpha\nBADENTRY!\nbeta\n`; assert `BADENTRY!` is filtered out (PROJECT_SLUG_RE gate on returned entries).
    - Test 6 (readProjectFile LOCAL happy): mock fs.readFile on $HOME/fleet/projects/alpha/project.md → returns `---\ndisplayName: 'Alpha'\n---\nbody`; assert readProjectFile(null, "alpha") returns `{markdown: "---\ndisplayName: 'Alpha'\n---\nbody"}`.
    - Test 7 (readProjectFile LOCAL missing): fs.readFile ENOENT; assert returns `{markdown: ""}` (per readIdentityFile pattern).
    - Test 8 (readProjectFile invalid slug): call with slug="Alpha"; assert throws.
    - Test 9 (createProject LOCAL happy): call createProject(null, "alpha", "Alpha One"); assert fs.mkdir called with $HOME/fleet/projects/alpha and {recursive:true}, then writeMarkdownFileAtomic called with target $HOME/fleet/projects/alpha/project.md and contents starting `---\ndisplayName: Alpha One\n---\n` (or single-quoted per yaml.dump defaults) and ending with a newline. Body after frontmatter is empty (per D-25).
    - Test 10 (createProject rejects dupe slug): mock a probe (fs.stat or fs.access) that indicates $HOME/fleet/projects/alpha already exists; assert createProject throws with a distinguishable error (e.g., message containing "exists" or code === "EEXIST") so the route layer can 409 it.
    - Test 11 (createProject rejects invalid slug): call with slug "Alpha"; assert throws before any fs call fires.
    - Test 12 (createProject REMOTE happy): mock execWithTimeout for the `test -d` probe returning "missing" (or non-zero exit); then mock `mkdir -p` exec success; then writeMarkdownFileAtomic REMOTE branch fires with target `$HOME/fleet/projects/alpha/project.md`. Assert the `test -d` probe's shell command contains the slug through shellEscape (single-quoted), not raw interpolation.
    - Test 13 (archiveProject LOCAL happy): call archiveProject(null, "alpha"); assert fs.mkdir called on $HOME/fleet/projects/archive with {recursive:true}, then fs.rename called from $HOME/fleet/projects/alpha to $HOME/fleet/projects/archive/alpha.
    - Test 14 (archiveProject REMOTE happy): mock execWithTimeout for `mkdir -p ... && mv ...`; assert single exec call whose shell command builds `mkdir -p "$HOME/fleet/projects/archive" && mv "$HOME/fleet/projects/<escaped-slug>" "$HOME/fleet/projects/archive/<escaped-slug>"` with slug interpolated through shellEscape.
    - Test 15 (archiveProject invalid slug): call with slug="../etc"; assert throws before any I/O fires (path-traversal defense per Security Domain).
  </behavior>
  <action>
    Add to `identity-artifact-reader.ts`:

    (1) Export `listProjects(conn: SSHClientType | null): Promise<Array<{slug: string; displayName: string}>>`:
       - LOCAL branch: `fs.readdir(getLocalProjectsRoot(), {withFileTypes: true})`, filter for dirs matching PROJECT_SLUG_RE and NAME !== "archive", for each read the child project.md via `readProjectFile(null, slug)`, parse frontmatter with `yaml.load`, extract `displayName` (fallback to slug). Sort ascending by slug. Catch ENOENT on the root readdir → return [].
       - REMOTE branch: build find command exactly `` `find "$HOME/fleet/projects" -mindepth 1 -maxdepth 1 -type d ! -name archive -printf '%f\\n' 2>/dev/null || true` `` (verbatim from PATTERNS.md), execute via execWithTimeout, split on \n, trim, filter PROJECT_SLUG_RE, sort. For each, call readProjectFile(conn, slug) and extract displayName (fallback to slug).
       - Never throw on missing displayName — fallback to slug is the invariant.

    (2) Export `readProjectFile(conn: SSHClientType | null, slug: string): Promise<{markdown: string}>`:
       - Validate slug against PROJECT_SLUG_RE; throw on mismatch.
       - LOCAL branch: path.join(getLocalProjectsRoot(), slug, "project.md"), fs.readFile with utf-8; catch ENOENT → `{markdown: ""}`.
       - REMOTE branch: `cat "$HOME/fleet/projects/${slug}/project.md" 2>/dev/null || true` via execWithTimeout (slug is regex-validated so direct interpolation is safe per identity-artifact-reader.ts:441-475 patch #95 comment — but keep it inside double quotes as-is).

    (3) Export `createProject(conn: SSHClientType | null, slug: string, displayName: string): Promise<void>`:
       - Validate slug against PROJECT_SLUG_RE; throw on mismatch.
       - Validate displayName: typeof string, trimmed length between 1 and 80. Throw on mismatch (length cap per Security Domain V5).
       - Build frontmatter body via yaml.dump({displayName}, {sortKeys:false, lineWidth:-1, noRefs:true, forceQuotes:false}); concatenate `` `---\n${yamlBody}---\n` `` (empty body per D-25).
       - LOCAL branch: probe with fs.stat on `$HOME/fleet/projects/<slug>/`; if it exists, throw `Object.assign(new Error("project slug already exists"), {code: "EEXIST"})`. Else `fs.mkdir` with recursive:true, then `writeMarkdownFileAtomic(null, absPath, body)`.
       - REMOTE branch: probe with `test -d ${shellEscape(remotePath)} && echo ok || echo missing` via execWithTimeout; on "ok" throw the EEXIST-shaped error. Else exec `mkdir -p ${shellEscape(remotePath)}` and then writeMarkdownFileAtomic(conn, `$HOME/fleet/projects/${slug}/project.md`, body).
       - The "dupe rejection" happens BEFORE any write — matches D-25 semantics and gives the route layer a clean 409 signal.

    (4) Export `archiveProject(conn: SSHClientType | null, slug: string): Promise<void>`:
       - Validate slug against PROJECT_SLUG_RE (path-traversal defense — the belt-and-suspenders guard against `../` is the regex itself since `.` and `/` aren't in the charset).
       - LOCAL branch: fs.mkdir on `$HOME/fleet/projects/archive/` with recursive:true, then fs.rename from `$HOME/fleet/projects/<slug>/` to `$HOME/fleet/projects/archive/<slug>/`. Do NOT overwrite an existing archived slug (fs.rename onto an existing directory is a fs error — let it propagate).
       - REMOTE branch: single exec command `mkdir -p "$HOME/fleet/projects/archive" && mv ${shellEscape("$HOME/fleet/projects/" + slug)} ${shellEscape("$HOME/fleet/projects/archive/" + slug)}`. (Use whichever shellEscape helper the file already uses — grep for `shellEscape\\|shellSingleQuote` and reuse verbatim. Do NOT define a new escape helper.)

    (5) Extend `identity-artifact-reader.projects.test.ts` with the 15 tests listed above (Task 1 already created the file for tests 1-13 of Task 1; append these). Mock fs.stat, fs.mkdir, fs.rename in addition to fs.readFile/writeFile. For REMOTE-branch tests, spy on execWithTimeout and assert the exact command string is passed.

    Do not touch existing exports. All 4 new functions follow the LOCAL/REMOTE conn===null discipline verbatim.
  </action>
  <acceptance_criteria>
    - `grep -c "^export function \\(listProjects\\|readProjectFile\\|createProject\\|archiveProject\\)" src/backend/claude-session/identity-artifact-reader.ts` outputs `4`.
    - The find command used inside listProjects REMOTE branch, when extracted via grep, contains the literal string `! -name archive` (verifies archive-exclusion per D-30 — check with `grep -n "! -name archive" src/backend/claude-session/identity-artifact-reader.ts` which must output ≥ 1).
    - `npx vitest run src/backend/claude-session/identity-artifact-reader.projects.test.ts` exits 0 with 28+ passing tests (Task 1's 13 + Task 2's 15).
    - `npx tsc --noEmit -p tsconfig.json` exits 0.
    - createProject's dupe-rejection path is proven by a passing test that asserts throw BEFORE mkdir/write fires (fs.mkdir spy call count === 0 in the dupe case).
    - archiveProject's rename target path is `$HOME/fleet/projects/archive/<slug>/` (verified by both LOCAL test assertion on fs.rename destination and REMOTE test assertion on the mv command string).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/backend/claude-session/identity-artifact-reader.projects.test.ts</automated>
  </verify>
  <done>All 4 new functions land in identity-artifact-reader.ts with LOCAL/REMOTE branches, slug-regex validated inputs, atomic writes via writeMarkdownFileAtomic, and the archive/ subdir exclusion in listProjects. 28+ tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route handler → identity-artifact-reader | slug + identityKey inputs cross from JSON-body-parsed user input into filesystem/exec paths |
| identity-artifact-reader → local filesystem | LOCAL branch writes to $HOME/fleet/projects/ via fs.promises |
| identity-artifact-reader → remote host via SSH | REMOTE branch interpolates slug + identityKey into shell commands over SSH |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-01-01 | Tampering | createProject / archiveProject slug argument | mitigate | PROJECT_SLUG_RE = /^[a-z0-9-]{1,64}$/ gate BEFORE any I/O — `.` and `/` are not in the charset so path traversal is impossible by construction. Redundant `absPath.startsWith(root + "/")` belt-and-suspenders per runbooks-editor.ts:49-51 not needed here because the regex is stricter, but the pattern is available if a future call site widens the charset. |
| T-117-01-02 | Tampering | archiveProject REMOTE mv command shell interpolation | mitigate | Every user-supplied value passes through shellEscape (single-quote wrap with `'"'"'` escape sequence) before shell interpolation. Even though PROJECT_SLUG_RE excludes shell metacharacters, the double-defense against future regex loosening is cheap. |
| T-117-01-03 | Info Disclosure | listProjects on directories not owned by the requesting user | accept | Skynet backend runs as a single system user; per-user host isolation gate lives at the ROUTE layer (`resolveHostById`), not at this artifact-reader layer. This layer trusts its caller. |
| T-117-01-04 | Tampering | writeSessionProjectField frontmatter round-trip clobbers custom user fields | mitigate | Full `yaml.load` parse (NOT `extractCosmeticsFromFrontmatter` narrowing extractor). Test 12 in Task 1 is a regression guard — a custom `custom: foo` field must survive the round-trip. |
| T-117-01-05 | Denial of Service | writeSessionProjectField on a corrupt frontmatter block | mitigate | yaml.load parse errors throw with a message including the identity key — caller can 500 and log; does not attempt "repair" which could damage data. |
| T-117-01-06 | Tampering | createProject/archiveProject race on same slug | accept | Skynet is single-user single-writer per the user's fleet model; race is theoretical. The mkdir + probe pattern is not atomic but the exposure window is microseconds and the failure mode (409 vs 500) is benign. |
| T-117-01-SC | Tampering | npm installs | accept | This plan installs ZERO new packages. All dependencies (js-yaml, express, ssh2) are pre-existing. No slopcheck required. |
</threat_model>

<verification>
- `npx vitest run src/backend/claude-session/identity-artifact-reader.projects.test.ts` — all 28+ tests pass.
- `npx tsc --noEmit -p tsconfig.json` — 0 errors introduced.
- Manual `grep -n "^export" src/backend/claude-session/identity-artifact-reader.ts` shows the 7 new exports plus PROJECT_SLUG_RE constant.
- Manual `grep -n "extractCosmeticsFromFrontmatter" src/backend/claude-session/identity-artifact-reader.ts` returns only the existing call sites — no new call sites added inside the new writer functions.
</verification>

<success_criteria>
All 7 new exports available and working end-to-end (LOCAL and REMOTE branches). Wave 2 route plans (117-04, 117-05) can import PROJECT_SLUG_RE, readSessionProjectField, writeSessionProjectField, listProjects, readProjectFile, createProject, archiveProject with no missing symbols. Test suite gate is green.
</success_criteria>

<output>
Create `.planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-01-SUMMARY.md` when done, listing: (1) the 7 exports added + their signatures verbatim, (2) test count and pass status, (3) any deviation from the pattern (e.g., if execWithTimeout lives in a module different than expected, note the actual path so Wave 2 knows where to import from).
</output>
