---
phase: 117-projects-sidebar-section-with-drag-and-drop-membership-per-p
plan: 06
type: execute
wave: 2
depends_on:
  - 117-03
files_modified:
  - src/ui/api/project-list-api.ts
  - src/ui/api/project-list-api.test.ts
  - src/ui/api/session-project-api.ts
  - src/ui/api/session-project-api.test.ts
  - src/ui/api/fleet-status-client.ts
  - src/ui/api/fleet-status-client.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "project-list-api.ts exports listProjects(hostId), createProject(hostId, displayName), archiveProject(hostId, slug) — all thin fetch wrappers using authApi + handleApiError, mirroring identity-archive-api.ts byte-shape."
    - "session-project-api.ts exports setSessionProject(hostId, identityKey, projectSlug|null) and setRelayRoomProject(roomId, userMxid, projectSlug|null) — same fetch-wrapper shape."
    - "fleet-status-client.ts grows a new option `onProjectListChanged?: (projects: ProjectRow[]) => void` and a `case 'project-list-changed':` in the ws.onmessage switch dispatching to it."
    - "createProject sends body {hostId, displayName} to POST /projects and returns the parsed {ok: true, slug} on success; on 409, it surfaces a distinguishable error (via handleApiError) so the calling modal can detect duplicate-slug."
    - "archiveProject sends body {hostId} to POST /projects/:slug/archive and returns {ok: true}."
    - "setSessionProject sends body {hostId, project: slug | null} to POST /identities/:key/project and returns {ok: true}."
    - "setRelayRoomProject sends body {userMxid, project: slug | null} to POST /relay-rooms/:roomId/project and returns {ok: true}."
    - "fleet-status-client emits a structured console.info log entry on receipt of project-list-changed frame (same shape as identity-archived logging), then invokes the optional callback."
  artifacts:
    - path: src/ui/api/project-list-api.ts
      provides: "listProjects, createProject, archiveProject fetch wrappers."
      exports: ["listProjects", "createProject", "archiveProject", "ProjectSummary"]
    - path: src/ui/api/session-project-api.ts
      provides: "setSessionProject, setRelayRoomProject fetch wrappers."
      exports: ["setSessionProject", "setRelayRoomProject"]
    - path: src/ui/api/fleet-status-client.ts
      provides: "onProjectListChanged option + WS dispatch for project-list-changed frames."
      exports: []
  key_links:
    - from: src/ui/api/fleet-status-client.ts
      to: src/backend/fleet-status/wire-protocol.ts
      via: "consumes the `project-list-changed` frame kind that 117-03 added to FrontendOutboundFrame"
      pattern: "project-list-changed"
    - from: src/ui/api/project-list-api.ts
      to: /projects endpoints (117-04)
      via: "HTTP GET/POST to /projects, POST /projects/:slug/archive"
      pattern: "authApi\\.(get|post)"
---

<objective>
Add the three frontend API clients that Wave 3 store hydration + Wave 4 UI components consume:

1. `project-list-api.ts` — listProjects, createProject, archiveProject fetch wrappers hitting 117-04's /projects endpoints.
2. `session-project-api.ts` — setSessionProject (identity route from 117-05) and setRelayRoomProject (Matrix route from 117-05) fetch wrappers.
3. `fleet-status-client.ts` extension — new `onProjectListChanged` option and WS `case "project-list-changed":` dispatch consuming 117-03's frame.

All follow the byte-shape of `identity-archive-api.ts` (authApi + handleApiError) and mirror the fleet-status-client's existing `onIdentityArchived` extension pattern from Phase 115.

Purpose: this is the wire between the frontend store slices (117-07) and the backend routes (117-04, 117-05) + wire (117-03). Wave 3+4 cannot mutate or hydrate project state without these clients.

Output: 2 new API modules + 1 extension to fleet-status-client, all with colocated vitest coverage.
</objective>

<decision_coverage>
This plan implements the following CONTEXT.md locked decisions: D-05, D-05a, D-25, D-28, D-30, D-37. Each cited decision is either directly enacted by a task's action step or implicitly enforced by the plan's structural choices (e.g., D-31 by writeSessionProjectField only firing on explicit clear; D-18/D-20 by the pinning-contextual-scope selector not touching the pin sentinel; D-17 by the absence of any drag-to-reorder handler in the panel). Missing D-XX citation ≠ missing implementation — the D-list here is the authoritative coverage map.
</decision_coverage>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-CONTEXT.md
@.planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-RESEARCH.md
@.planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-PATTERNS.md
@.planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create project-list-api.ts + session-project-api.ts + tests</name>
  <files>src/ui/api/project-list-api.ts, src/ui/api/project-list-api.test.ts, src/ui/api/session-project-api.ts, src/ui/api/session-project-api.test.ts</files>
  <read_first>
    - src/ui/api/identity-archive-api.ts — READ IN FULL. The exact byte-shape template. Anchor on: import shape (`authApi, handleApiError` from @/main-axios), thin fetch wrapper pattern (:1-33), the `try { const response = await authApi.post(...); return response.data; } catch (error) { handleApiError(error, "..."); }` skeleton.
    - src/ui/api/identity-archive-api.test.ts — the test-file structure for these thin wrappers (grep for the describe/it layout; how the axios adapter is mocked; how handleApiError is asserted to have been called).
    - src/ui/api/main-axios.ts (or wherever authApi + handleApiError live) — verify import path and function signatures. Common location: `@/main-axios`. Grep the ui/api/ directory for `from "@/main-axios"` to confirm.
    - .planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-PATTERNS.md § "project-list-api.ts" and § "session-project-api.ts" for the exact wrapper shapes.
    - 117-04-SUMMARY.md (endpoint paths) and 117-05-SUMMARY.md (endpoint paths + body shapes) — the fetch wrapper URL and body shapes must match verbatim.
  </read_first>
  <behavior>
    - **project-list-api.ts tests:**
    - Test 1 (listProjects happy): axios mock GET → returns `{projects: [{slug:"a", displayName:"Alpha", archived: false}]}`. Call listProjects(1); assert URL is `/projects?hostId=1`, method GET, returns the parsed body.
    - Test 2 (listProjects error): axios mock rejects with 500; assert handleApiError called with (error, "list projects").
    - Test 3 (createProject happy): axios mock POST → returns `{ok: true, slug: "my-project"}`. Call createProject(1, "My Project"); assert URL `/projects`, body `{hostId: 1, displayName: "My Project"}`, returns parsed body.
    - Test 4 (createProject 409): axios mock POST rejects with 409 response; assert handleApiError called with (error, "create project"). The error object propagated must retain the response status/body so the caller (modal) can detect 409.
    - Test 5 (archiveProject happy): axios mock POST → `{ok: true}`. Call archiveProject(1, "alpha"); assert URL `/projects/alpha/archive`, body `{hostId: 1}`.
    - Test 6 (archiveProject slug URL-encoded): call archiveProject(1, "abc-123"); assert URL segment doesn't double-encode (slug is already URL-safe).
    - **session-project-api.ts tests:**
    - Test 7 (setSessionProject assign): call setSessionProject(1, "wren", "alpha"); assert URL `/identities/wren/project`, body `{hostId: 1, project: "alpha"}`.
    - Test 8 (setSessionProject clear): call setSessionProject(1, "wren", null); assert body `{hostId: 1, project: null}`.
    - Test 9 (setSessionProject identity key URL-encoded): call with key containing colon `"id:1"`; assert URL is `/identities/id%3A1/project`.
    - Test 10 (setSessionProject error): axios rejects; assert handleApiError(error, "set session project").
    - Test 11 (setRelayRoomProject assign): call setRelayRoomProject("!room:host", "@ash:host", "alpha"); assert URL `/relay-rooms/%21room%3Ahost/project`, body `{userMxid: "@ash:host", project: "alpha"}`.
    - Test 12 (setRelayRoomProject clear): call with null third arg; assert body `.project === null`.
  </behavior>
  <action>
    Create `src/ui/api/project-list-api.ts`:

    (1) Imports:
       ```
       import { authApi, handleApiError } from "@/main-axios";
       ```
       (Verify path via grep; adjust if actual is different.)

    (2) Export a `ProjectSummary` TypeScript type:
       ```
       export type ProjectSummary = {
         slug: string;
         displayName: string;
         archived: boolean;
       };
       ```

    (3) Add file header comment citing Phase 117 Plan 06 + D-25, D-28, D-30 (rationale for the 3 endpoints).

    (4) Export three fetch wrappers, byte-parallel to identity-archive-api.ts:

       ```
       export async function listProjects(hostId: number): Promise<{ projects: ProjectSummary[] }> {
         try {
           const response = await authApi.get(`/projects?hostId=${encodeURIComponent(String(hostId))}`);
           return response.data as { projects: ProjectSummary[] };
         } catch (error) {
           handleApiError(error, "list projects");
         }
       }

       export async function createProject(hostId: number, displayName: string): Promise<{ ok: true; slug: string }> {
         try {
           const response = await authApi.post("/projects", { hostId, displayName });
           return response.data as { ok: true; slug: string };
         } catch (error) {
           handleApiError(error, "create project");
         }
       }

       export async function archiveProject(hostId: number, slug: string): Promise<{ ok: true }> {
         try {
           const url = `/projects/${encodeURIComponent(slug)}/archive`;
           const response = await authApi.post(url, { hostId });
           return response.data as { ok: true };
         } catch (error) {
           handleApiError(error, "archive project");
         }
       }
       ```

    (5) Create `src/ui/api/session-project-api.ts`:
       ```
       import { authApi, handleApiError } from "@/main-axios";

       // Phase 117 Plan 06 (D-05, D-05a): frontend wrappers for the session-
       // project-write (identity route) and relay-room-project-tag (Matrix
       // route) endpoints from 117-05. Both take slug=null to CLEAR.

       export async function setSessionProject(
         hostId: number,
         identityKey: string,
         projectSlug: string | null,
       ): Promise<{ ok: true }> {
         try {
           const url = `/identities/${encodeURIComponent(identityKey)}/project`;
           const response = await authApi.post(url, { hostId, project: projectSlug });
           return response.data as { ok: true };
         } catch (error) {
           handleApiError(error, "set session project");
         }
       }

       export async function setRelayRoomProject(
         roomId: string,
         userMxid: string,
         projectSlug: string | null,
       ): Promise<{ ok: true }> {
         try {
           const url = `/relay-rooms/${encodeURIComponent(roomId)}/project`;
           const response = await authApi.post(url, { userMxid, project: projectSlug });
           return response.data as { ok: true };
         } catch (error) {
           handleApiError(error, "set relay room project");
         }
       }
       ```

    (6) Create colocated test files following identity-archive-api.test.ts's exact structure. Mock `@/main-axios` with `authApi` as an object having `.get` and `.post` methods (vi.fn()) plus a `handleApiError` mock. Assert on the mock call arguments per test.

    Do NOT introduce any new networking abstraction. authApi + handleApiError is the ONLY shape used across the entire ui/api/ surface — respect it.
  </action>
  <acceptance_criteria>
    - `grep -c "^export async function \\(listProjects\\|createProject\\|archiveProject\\)" src/ui/api/project-list-api.ts` outputs `3`.
    - `grep -c "^export async function \\(setSessionProject\\|setRelayRoomProject\\)" src/ui/api/session-project-api.ts` outputs `2`.
    - `grep -c "handleApiError" src/ui/api/project-list-api.ts` outputs ≥ 3 (once per wrapper).
    - `grep -c "handleApiError" src/ui/api/session-project-api.ts` outputs ≥ 2 (once per wrapper).
    - `grep -c "encodeURIComponent" src/ui/api/project-list-api.ts src/ui/api/session-project-api.ts` outputs ≥ 4 (safe URL segment building for all path params).
    - `npx vitest run src/ui/api/project-list-api.test.ts src/ui/api/session-project-api.test.ts` exits 0 with 12+ tests passing.
    - `npx tsc --noEmit -p tsconfig.json` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/ui/api/project-list-api.test.ts src/ui/api/session-project-api.test.ts</automated>
  </verify>
  <done>5 fetch wrappers land; identity-archive-api byte-shape mirrored verbatim; 12+ tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend fleet-status-client.ts with onProjectListChanged + WS dispatch + tests</name>
  <files>src/ui/api/fleet-status-client.ts, src/ui/api/fleet-status-client.test.ts</files>
  <read_first>
    - src/ui/api/fleet-status-client.ts — READ IN FULL (it's a switch-based WS message dispatcher). Anchor on: FleetStatusClientOptions interface (grep for `interface FleetStatusClientOptions`), `onIdentityArchived?:` callback declaration (:71-82 per PATTERNS.md), the `switch (parsed.type)` dispatch (grep for `switch \\(parsed`), the existing `case "identity-archived":` block (:216-231) — this is the template to mirror.
    - src/ui/api/fleet-status-client.test.ts — existing tests exercising the switch, especially the identity-archived case. Mirror this test pattern.
    - src/backend/fleet-status/wire-protocol.ts (already extended in 117-03) — verify the FrontendProjectListChangedFrameSchema shape. Note: the type import into fleet-status-client should come from the SHARED wire-protocol module (if there's a shared @/wire or similar; grep the frontend for the wire types import).
    - .planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-PATTERNS.md § "fleet-status-client.ts (extension)".
  </read_first>
  <behavior>
    - Test 1 (onProjectListChanged callback fires): construct a client with `onProjectListChanged: vi.fn()`; feed a `{type: "project-list-changed", projects: [{slug:"a", ...}]}` frame to ws.onmessage; assert the callback was invoked once with the projects array.
    - Test 2 (missing callback is a no-op): construct client without onProjectListChanged; feed the same frame; assert no throw, no crash, structured console.info still logs.
    - Test 3 (existing dispatchers untouched): feed an `identity-archived` frame; assert onIdentityArchived (mocked) fires. Feed a `snapshot` frame; assert onSnapshot fires. Regression guard on Phase 115 wiring.
    - Test 4 (malformed project-list-changed rejected by zod parse before dispatch): feed a frame with `type: "project-list-changed"` but `projects: "not-an-array"`; assert onProjectListChanged NOT called (the existing FrontendOutboundFrame.parse guard rejects the frame).
    - Test 5 (structured log entry): spy on console.info; feed a valid project-list-changed frame; assert one call to console.info with an object containing `operation: "fleet_status_client_project_list_changed"` and the projects count (or the array reference — planner's discretion; match the existing identity-archived log-entry style).
  </behavior>
  <action>
    Edit `src/ui/api/fleet-status-client.ts`:

    (1) Extend the FleetStatusClientOptions interface. Add the callback declaration alongside `onIdentityArchived?:`:
       ```
       /**
        * Phase 117 Plan 06 (D-37): fire when a `project-list-changed`
        * frame arrives from the backend. Consumer (AppShell) forwards
        * to conversation-store's setProjects(...) so the sidebar
        * re-derives buckets.
        */
       onProjectListChanged?: (
         projects: Array<{slug: string; displayName: string; hostId: string; hostname: string; archived: boolean}>,
       ) => void;
       ```

    (2) Extend the switch (parsed.type) block. Add a new case immediately after the existing `case "identity-archived":`:
       ```
       case "project-list-changed":
         console.info({
           operation: "fleet_status_client_project_list_changed",
           url,
           projectCount: parsed.projects.length,
         });
         onProjectListChanged?.(parsed.projects);
         break;
       ```

    (3) Extend `fleet-status-client.test.ts` with the 5 tests. Follow the existing describe/it layout. Frame construction: use the same shape the existing identity-archived tests use (JSON.stringify a valid frame + set on `event.data` of the mock ws message).

    Do NOT modify any other dispatch branch. Do NOT change the ws.onmessage outer wrapping.
  </action>
  <acceptance_criteria>
    - `grep -c "onProjectListChanged" src/ui/api/fleet-status-client.ts` outputs ≥ 2 (interface declaration + dispatch call).
    - `grep -c "case \"project-list-changed\":" src/ui/api/fleet-status-client.ts` outputs `1`.
    - `grep -c "operation: \"fleet_status_client_project_list_changed\"" src/ui/api/fleet-status-client.ts` outputs `1`.
    - `npx vitest run src/ui/api/fleet-status-client.test.ts` exits 0 with all pre-existing tests still green AND the 5 new tests passing (regression guard on Phase 115 wiring).
    - `npx tsc --noEmit -p tsconfig.json` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/ui/api/fleet-status-client.test.ts</automated>
  </verify>
  <done>New callback option + switch case land; existing dispatch branches untouched; 5 new tests + all pre-existing green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React component → project-list-api / session-project-api | callers pass user-typed values (displayName, slug), auth handled by authApi |
| WS server → fleet-status-client | frames pass through zod schema validation (FrontendOutboundFrame.parse) before dispatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-06-01 | Tampering | URL segment injection via slug / identityKey / roomId | mitigate | encodeURIComponent on every path segment (verified by grep gate in Task 1 acceptance). |
| T-117-06-02 | Info Disclosure | 500 error details leaked to caller | mitigate | handleApiError already normalizes error shape (existing surface — Phase 115 verified). Caller (modal, DnD handler) sees the same ApiError type as all other endpoints. |
| T-117-06-03 | Tampering | WS frame malformed → dispatch on garbage | mitigate | FrontendOutboundFrame.parse (existing gate) rejects malformed frames before switch dispatch. Test 4 asserts. |
| T-117-06-04 | Denial of Service | Bulk drag → 20 rapid POSTs → 20 wire fanouts | mitigate | Registry's idempotent-skip (117-03) absorbs no-op fanouts. Frontend batches renders via useSyncExternalStore memoization (Wave 3 concern). |
| T-117-06-SC | Tampering | npm installs | accept | No new packages installed. |
</threat_model>

<verification>
- `npx vitest run src/ui/api/project-list-api.test.ts src/ui/api/session-project-api.test.ts src/ui/api/fleet-status-client.test.ts` — all pass, regressions clean.
- `npx tsc --noEmit -p tsconfig.json` — 0 errors.
</verification>

<success_criteria>
Wave 3 store hydration (117-07) can `import { listProjects } from "@/api/project-list-api"` for boot-time hydration, and can wire `onProjectListChanged` in the fleet-status-client options. The DnD drop handler (117-08) can `import { setSessionProject, setRelayRoomProject }`. The create-project modal (117-09) can `import { createProject }`. The archive-cascade (117-09) can `import { archiveProject }`.
</success_criteria>

<output>
Create `.planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-06-SUMMARY.md` when done. Include: (1) 5 exports and their signatures verbatim, (2) the new fleet-status-client option name and the console.info operation string, (3) test counts (new + pre-existing).
</output>
