---
phase: 117-projects-sidebar-section-with-drag-and-drop-membership-per-p
plan: 08
type: execute
wave: 4
depends_on:
  - 117-06
  - 117-07
files_modified:
  - src/ui/features/pretty-conversations/PrettyProjectSectionHeader.tsx
  - src/ui/features/pretty-conversations/PrettyProjectSectionHeader.test.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "PrettyProjectSectionHeader renders as a single row with a FolderOpen icon (or planner-chosen lucide-react folder-shape glyph), 'Project:' literal prefix text, display name, small new-conversation button on the right (SquarePen icon), and a ChevronDown that rotates on expand/collapse — same visual language as the existing Archived section header at PrettyConversationsPanel.tsx:1995-2033."
    - "Each project section wraps its rows in a per-section coral drop lane: type-gated on `application/x-skynet-row` ONLY (not badge drags, not OS file drops), hover-only coral (no baseline coral), bounding-rect stateless dragleave guard against child boundary crossings, window-level dragend for Escape-cancel, verbatim palette rgba(255,184,150,0.22) bg / rgba(255,184,150,0.60) border / zIndex 30 / isolation: isolate on wrapper (D-23)."
    - "Dropping a row onto a project section fires setSessionProject (for identity rows) or setRelayRoomProject (for relay-room rows) with the target section's slug; RDP rows (rdpHostRow=true) are REFUSED at the drop handler (D-08)."
    - "Panel renders project sections BETWEEN the pinned zone and the flat middle (D-09), each project header standing on its own without a super-section wrapping label 'Projects' (D-10 explicit rejection). Empty projects still show up as header-only sections (D-11)."
    - "Panel's flat-middle container has a drop handler that CLEARS the project field when a currently-in-a-project row is dropped there (D-22 gesture #2)."
    - "Panel's header button cluster (pencil / drama / globe / etc.) gains a 'Create project' button that opens the CreateProjectModal (implementation in 117-09; wire only the state toggle here — the modal opening lives in the panel but the modal component itself is 117-09's concern)."
    - "Project sections' rows are the same PrettyConversationRow rows used elsewhere (no new row component). Only the wrapping section header + drop-lane is new."
    - "Collapse state per project persists via use-collapsed-project-slugs (from 117-07): clicking the header toggles the collapsed set; when collapsed the section renders only the header (no rows in DOM)."
  artifacts:
    - path: src/ui/features/pretty-conversations/PrettyProjectSectionHeader.tsx
      provides: "Reusable project section component: header + per-section drop lane + collapsible rows region."
      exports: ["PrettyProjectSectionHeader"]
    - path: src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
      provides: "Extended panel rendering: project sections inserted between pinned zone and flat middle; flat-middle clear-project drop handler; create-project button toggling modal state."
  key_links:
    - from: PrettyConversationsPanel
      to: PrettyProjectSectionHeader
      via: "one section per project in useProjects(), sorted alphabetically by displayName (derived selector from 117-07)"
      pattern: "PrettyProjectSectionHeader"
    - from: PrettyProjectSectionHeader onDrop
      to: setSessionProject / setRelayRoomProject (from 117-06)
      via: "row payload → determine identity vs relay-room kind → dispatch to appropriate setter"
      pattern: "(setSessionProject|setRelayRoomProject)\\("
    - from: PrettyConversationsPanel flat-middle drop handler
      to: setSessionProject / setRelayRoomProject with slug=null
      via: "drop-in-middle clears the project assignment"
      pattern: "(setSessionProject|setRelayRoomProject)\\(.*null"
---

<objective>
Render the projects zone in the sidebar. Insert per-project section headers between the pinned zone and the flat middle (per D-09), each with the visual language of the existing Archived section header (D-12: icon + "Project:" prefix + display name + new-conversation button + chevron). Wrap each project's rows in a per-section coral drop lane implementing D-22 gesture #1 (drop onto section → assigns project) and D-23 verbatim overlay palette. Extend the panel with the flat-middle drop handler for D-22 gesture #2 (drop clears project). Wire the create-project header button state toggle (the modal itself is 117-09).

Purpose: this is the visible sidebar change. Everything the user actually SEES — project sections between pinned and middle, drag-drop landing on them, collapse chevrons — happens here.

Output: new PrettyProjectSectionHeader component (with its own tests) + extended PrettyConversationsPanel (with per-section rendering + flat-middle drop + create-project button state).
</objective>

<decision_coverage>
This plan implements the following CONTEXT.md locked decisions: D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-14, D-15, D-21, D-22, D-23, D-27. Each cited decision is either directly enacted by a task's action step or implicitly enforced by the plan's structural choices (e.g., D-31 by writeSessionProjectField only firing on explicit clear; D-18/D-20 by the pinning-contextual-scope selector not touching the pin sentinel; D-17 by the absence of any drag-to-reorder handler in the panel). Missing D-XX citation ≠ missing implementation — the D-list here is the authoritative coverage map.
</decision_coverage>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-CONTEXT.md
@.planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-RESEARCH.md
@.planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-PATTERNS.md
@.planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-06-SUMMARY.md
@.planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create PrettyProjectSectionHeader.tsx with header + drop lane + tests</name>
  <files>src/ui/features/pretty-conversations/PrettyProjectSectionHeader.tsx, src/ui/features/pretty-conversations/PrettyProjectSectionHeader.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx:1995-2033 — the Archived section render (button + Archive icon + "Archived" prefix + chevron rotate on collapse). Byte-shape template for the new header.
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx:1636-1647 — the coral overlay verbatim palette (rgba values, zIndex, 2px border). Reuse verbatim.
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx:1490-1600 — the drag-state machinery (isBadgeDragOver + bounding-rect dragleave guard + window-level dragend for Escape-cancel). Byte-shape template for the per-section drag state.
    - src/ui/shell/CollapsedPanelCloseLane.tsx — canonical example of a hover-only-no-baseline drop lane WITH type-gate on a specific MIME. Anchor on: the dragover type-gate (only accept `application/x-skynet-*` MIME), the dragleave bounding-rect check, the window-level dragend listener useEffect, the drop handler payload extraction + validation pipeline.
    - src/ui/features/pretty-conversations/PrettyConversationRow.tsx:949-973 — the DnD source contract (`text/plain` = row.id, `application/x-skynet-row` = JSON blob {id, host, targetTmuxSession, fleetOnly, rdpHostRow}). This is what the drop handler PARSES.
    - src/ui/api/session-project-api.ts (from 117-06) — verify setSessionProject and setRelayRoomProject signatures.
    - src/ui/state/conversation-store.ts — the selector output shape from 117-07 (projectSection rows include the row's kind — identity vs relay-room).
    - .planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-PATTERNS.md § "PrettyProjectSectionHeader.tsx".
    - .planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-CONTEXT.md § D-12 (header shape), § D-22 (drop gestures), § D-23 (coral palette), § D-08 (terminal exclusion).
    - .planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-RESEARCH.md § Common Pitfalls #7 (type-gate on `application/x-skynet-row` only).
  </read_first>
  <behavior>
    - Test 1 (render happy — collapsed=false): renders header with FolderOpen icon (or the chosen glyph), "Project:" prefix, displayName text, chevron NOT rotated (aria-expanded=true). Rows region has children (the passed-in `rows` prop's contents).
    - Test 2 (render collapsed=true): header renders, chevron IS rotated (aria-expanded=false), rows region is NOT in the DOM.
    - Test 3 (click header toggles collapse): fires onToggleCollapse callback with the section's slug.
    - Test 4 (new-conversation button fires callback): the SquarePen button fires onNewConversationClick(slug); does NOT toggle collapse.
    - Test 5 (drop-lane baseline is NEUTRAL — no coral): initial render has NO overlay div matching the coral palette selector (`isDragOver=false` — the overlay only appears during hover).
    - Test 6 (dragover with x-skynet-row MIME sets isDragOver): dispatch a synthetic DragEvent with dataTransfer.types containing `application/x-skynet-row`; assert the coral overlay div appears with the verbatim style values (bg rgba(255,184,150,0.22), border rgba(255,184,150,0.60), zIndex 30). preventDefault should be called on the event.
    - Test 7 (dragover with WRONG MIME does NOT set isDragOver): dispatch a DragEvent with only `application/x-skynet-badge` in types; assert NO overlay appears, no preventDefault.
    - Test 8 (dragleave with cursor STILL inside bounding rect does NOT clear): mock the section's getBoundingClientRect to return a rect containing the event's clientX/Y; dispatch dragleave; assert overlay STAYS visible.
    - Test 9 (dragleave with cursor outside bounding rect DOES clear): dispatch dragleave with clientX outside the mocked rect; assert overlay hides.
    - Test 10 (window-level dragend clears overlay — Escape-cancel path): dispatch a `dragend` event on window (`window.dispatchEvent(new Event("dragend"))`); assert overlay hides regardless of dragleave.
    - Test 11 (drop on identity row → setSessionProject called): dispatch drop with `application/x-skynet-row` = JSON `{id: "wren", host: 1, targetTmuxSession: "wren-session", rdpHostRow: false}`; assert setSessionProject called with (1, "wren", "alpha"). Overlay clears on drop.
    - Test 12 (drop on relay-room row → setRelayRoomProject called): dispatch drop with JSON containing `matrixRoomId` field (mock the row payload shape); assert setRelayRoomProject called with (roomId, userMxid, "alpha"). Getting userMxid: verify the payload includes it, or pull from a hook (grep for existing user-mxid resolver in AppShell). If neither exists, the drop handler surfaces the roomId to a higher-level callback (`onDropRow(slug, payload)`) which the panel resolves; the panel is where the userMxid is available.
    - Test 13 (drop on RDP row REFUSED — D-08): dispatch drop with payload `{rdpHostRow: true, id: "..."}`; assert setSessionProject NOT called; assert onDropRow NOT called with the RDP payload; overlay clears.
    - Test 14 (drop with missing MIME is a no-op): dispatch drop with empty dataTransfer.types; assert no callback fires, no throw, overlay clears.
    - Test 15 (isolation isolate on wrapper): render and query the outer div; assert its inline style OR class contains `isolation: isolate` (CollapsedPanelCloseLane.tsx:40 discipline for z-index sandboxing).
  </behavior>
  <action>
    Create `src/ui/features/pretty-conversations/PrettyProjectSectionHeader.tsx`:

    (1) Props type:
       ```
       type Props = {
         slug: string;
         displayName: string;
         rows: React.ReactNode;   // pre-rendered rows children; the section is a display container
         collapsed: boolean;
         onToggleCollapse: (slug: string) => void;
         onNewConversationClick: (slug: string) => void;
         onDropRow: (slug: string, payload: {id: string; host?: number | null; targetTmuxSession?: string | null; matrixRoomId?: string | null; rdpHostRow?: boolean}) => void;
       };
       ```

    (2) Component body:
       - useState `isDragOver` boolean (initial false).
       - useRef sectionRef for the outer div.
       - useEffect: window.addEventListener("dragend", () => setIsDragOver(false)); cleanup on unmount.
       - onDragOver: type-gate FIRST (`e.dataTransfer?.types && Array.from(e.dataTransfer.types).includes("application/x-skynet-row")`). Only on match: `e.preventDefault(); e.stopPropagation(); setIsDragOver(true)`.
       - onDragLeave: same type-gate; bounding-rect check via `e.currentTarget.getBoundingClientRect()` — if cursor still inside, return; else `setIsDragOver(false)`.
       - onDrop: `setIsDragOver(false)`; `raw = e.dataTransfer?.getData("application/x-skynet-row") ?? ""`; if empty, return; JSON.parse in try/catch (return on parse failure). Validate parsed is object with id-string, drop if `rdpHostRow === true`. Call `props.onDropRow(slug, parsed)`. e.preventDefault() + e.stopPropagation() on successful drop.

    (3) JSX structure:
       ```
       <div
         ref={sectionRef}
         className="pv-panel-group pv-project-section relative"
         style={{isolation: "isolate"}}
         data-project-slug={slug}
         onDragOver={onDragOver}
         onDragLeave={onDragLeave}
         onDrop={onDrop}
       >
         {isDragOver && (
           <div className="absolute inset-0 pointer-events-none"
                data-testid="pv-project-drop-overlay"
                style={{background: "rgba(255, 184, 150, 0.22)",
                        border: "2px solid rgba(255, 184, 150, 0.60)",
                        zIndex: 30}} />
         )}
         <button type="button"
                 onClick={() => onToggleCollapse(slug)}
                 className="flex items-center gap-2 px-4 pt-3 pb-1.5 w-full text-left"
                 data-testid={`pv-project-section-header-${slug}`}
                 aria-expanded={!collapsed}
                 aria-controls={`pv-project-section-content-${slug}`}>
           <FolderOpen className="size-3 text-[#5c6070]/85 shrink-0" aria-hidden="true" />
           <span className="text-[13px] font-semibold uppercase tracking-[0.08em] text-[#5c6070]/85 shrink-0">
             Project: {displayName}
           </span>
           <span aria-hidden="true" className="flex-1 h-px bg-[linear-gradient(90deg,rgba(255,255,255,0.06),transparent)]" />
           <button type="button"
                   onClick={(e) => { e.stopPropagation(); onNewConversationClick(slug); }}
                   className="..." aria-label={`New conversation in ${displayName}`}>
             <SquarePen className="size-3 ..." aria-hidden="true" />
           </button>
           <ChevronDown className={`size-3 text-[#5c6070]/85 shrink-0 transition-transform ${collapsed ? "" : "rotate-180"}`}
                        aria-hidden="true" />
         </button>
         {!collapsed && (
           <div id={`pv-project-section-content-${slug}`}>{rows}</div>
         )}
       </div>
       ```
       (Byte-parallel to :1995-2033 with the additions for slug + drop lane.)

    (4) Import icons from lucide-react: `FolderOpen`, `SquarePen`, `ChevronDown`.

    (5) Create colocated test file `PrettyProjectSectionHeader.test.tsx`. Use @testing-library/react's `render` + `fireEvent`. Use the DnD synthetic-event pattern from `PrettyConversationsPanel.test.tsx` and `CollapsedPanelCloseLane.test.tsx` — they demonstrate `Object.defineProperty` on the event for clientX/Y + `dataTransfer` mocking with `.types` and `.getData`.

    Do NOT bundle the section's rows children INSIDE this component — the panel passes them as a prop. This keeps the section a display wrapper, not a business-logic container.
  </action>
  <acceptance_criteria>
    - `grep -c "^export function PrettyProjectSectionHeader" src/ui/features/pretty-conversations/PrettyProjectSectionHeader.tsx` outputs `1`.
    - `grep -c "rgba(255, 184, 150, 0.22)" src/ui/features/pretty-conversations/PrettyProjectSectionHeader.tsx` outputs ≥ 1 (verbatim palette per D-23).
    - `grep -c "rgba(255, 184, 150, 0.60)" src/ui/features/pretty-conversations/PrettyProjectSectionHeader.tsx` outputs ≥ 1.
    - `grep -c "zIndex: 30" src/ui/features/pretty-conversations/PrettyProjectSectionHeader.tsx | grep -v '^#'` outputs ≥ 1.
    - `grep -c "application/x-skynet-row" src/ui/features/pretty-conversations/PrettyProjectSectionHeader.tsx` outputs ≥ 2 (dragover type-gate + drop payload extraction).
    - `grep -c "application/x-skynet-badge" src/ui/features/pretty-conversations/PrettyProjectSectionHeader.tsx | grep -v '^#'` outputs `0` (badge drags are NOT accepted — Pitfall 7).
    - `grep -c "rdpHostRow" src/ui/features/pretty-conversations/PrettyProjectSectionHeader.tsx | grep -v '^#'` outputs ≥ 1 (D-08 terminal exclusion).
    - `grep -c "Project:" src/ui/features/pretty-conversations/PrettyProjectSectionHeader.tsx` outputs ≥ 1 (literal prefix text per D-12).
    - `grep -c "isolation.*isolate" src/ui/features/pretty-conversations/PrettyProjectSectionHeader.tsx | grep -v '^#'` outputs ≥ 1.
    - `npx vitest run src/ui/features/pretty-conversations/PrettyProjectSectionHeader.test.tsx` exits 0 with 15+ tests passing.
    - `npx tsc --noEmit -p tsconfig.json` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-conversations/PrettyProjectSectionHeader.test.tsx</automated>
  </verify>
  <done>Component renders header + drop lane; type-gate + hover-only-coral + bounding-rect dragleave + window-dragend + RDP-refuse all proven; 15+ tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend PrettyConversationsPanel with project sections + flat-middle clear-drop + create-project button state + tests</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx, src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx — READ IN FULL for structural context. Anchor on: the top comment block (:1-60) for the three-zone-layout invariants, the snapshot consumption (grep for `useConversations\\|useProjects\\|snapshot`), the current rendered structure at :1616-2033 (pinned zone → flat middle → RDP → Archived), the header button cluster (:1701-1745), the coral overlay + drag-state machinery (:1490-1647), the handleArchive handler at :1395-1425 (byte-shape reference for the archive-project handler in 117-09; not directly needed here but useful context).
    - src/ui/state/conversation-store.ts (extended in 117-07) — verify the derived selector's output includes `projectSections: Array<{slug, displayName, rows: Row[]}>` + the pinnedUnassigned/middle/rdp fields. Verify how the panel currently reads the snapshot.
    - src/ui/state/use-collapsed-project-slugs.ts (from 117-07) — verify hook signature.
    - src/ui/features/pretty-conversations/PrettyProjectSectionHeader.tsx (freshly created in Task 1) — verify Props shape.
    - src/ui/api/session-project-api.ts (from 117-06) — verify setSessionProject and setRelayRoomProject signatures.
    - src/ui/features/pretty-conversations/PrettyConversationRow.tsx — verify the row shape carries all fields the drop handler needs (host, matrixRoomId, targetTmuxSession/identityKey, rdpHostRow).
    - .planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-CONTEXT.md § D-09 (order), § D-10 (no super-section label), § D-11 (empty projects visible), § D-22 gesture #2 (flat-middle clear), § D-27 (new-conversation flow — modal-open state only in this plan; the modal itself is 117-09), § D-14 (right-click → edit + archive), § Claude's Discretion (icon choice, button placement).
    - .planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-PATTERNS.md § "PrettyConversationsPanel.tsx (extension)".
  </read_first>
  <behavior>
    - Test 1 (project sections render between pinned and flat middle — D-09): mock the derived-selector snapshot with `{pinnedUnassigned: [rowP], projectSections: [{slug:"alpha", displayName:"Alpha", rows:[rowA]}], middle: [rowM], rdp: []}`. Assert DOM order: rowP (in pinned zone chip block) → project section header "Project: Alpha" → rowA (inside alpha section) → rowM (in flat middle).
    - Test 2 (no "Projects" super-section label — D-10): render with 3 projectSections; assert NO element with text "Projects" exists in the DOM as a wrapping super-section header. Each project has its own "Project: <name>" header.
    - Test 3 (empty project section renders as header-only — D-11): mock projectSections with `[{slug:"empty", displayName:"Empty", rows:[]}]`; assert the section header appears but no row is inside; the section is DROPPABLE (i.e., the section container is still in the DOM with the drop-lane wrapping).
    - Test 4 (collapse state hides rows): mock useCollapsedProjectSlugs to return `{collapsed: new Set(["alpha"]), toggle}`; render; assert the alpha section's rows are NOT in the DOM (only header visible). Clicking header calls `toggle("alpha")`.
    - Test 5 (create-project button in header cluster): assert a button with aria-label like "Create project" exists in the panel's header. Click it; assert component state `createProjectModalOpen` toggles to true (state observable via a rendered mock modal or a data-attribute; the actual modal component is 117-09).
    - Test 6 (drop on project section fires setSessionProject for identity row): mock the DnD event on the project section wrapper (via `PrettyProjectSectionHeader`'s onDropRow — which the panel supplies as its `handleProjectDrop`). Fire the drop with payload `{id: "wren", host: 1, targetTmuxSession: "wren-sess"}`; assert setSessionProject(1, "wren", "alpha") is called. (The panel's handleProjectDrop resolves the row-shape to the identityKey.)
    - Test 7 (drop on project section fires setRelayRoomProject for relay-room row): payload `{id: "!abc:host", matrixRoomId: "!abc:host"}`; assert setRelayRoomProject(roomId, userMxid, "alpha") — with userMxid resolved from the auth context.
    - Test 8 (drop in flat middle clears identity row's project — D-22 gesture #2): the flat-middle container (grep for what wraps the middle rows today) has an onDrop that fires setSessionProject(hostId, key, null) when a payload with a currently-assigned project is dropped there. Test: drop payload for a row currently in "alpha" onto the flat middle; assert setSessionProject called with third arg = null.
    - Test 9 (drop in flat middle on already-unassigned row is a no-op): payload for a row NOT in any project dropped on flat middle; assert setSessionProject NOT called (no round-trip needed).
    - Test 10 (pinned zone is NOT a project drop target — D-21): drop a row on the pinned zone container; assert setSessionProject NOT called (dropping into the pinned zone is not a project-DnD gesture).
    - Test 11 (right-click on project header — D-14 hint): the section header (or PrettyProjectSectionHeader's outer div) has a `onContextMenu` handler that renders a context menu with "Edit project file" and "Archive project" items. The actual menu implementation may live in Task 1 or here; if the menu items' actual handlers (opening the file editor, firing the archive cascade) live in 117-09, wire them as callbacks passed from the panel. This test asserts the context menu is at least *visible* on right-click; the archive handler wire is in 117-09.
    - Test 12 (project sections rendered in ORDER from the selector): if selector returns projectSections in a specific alphabetical order, the DOM order matches. This is a regression guard on the 117-07 selector's ordering behavior surviving through the panel render.
  </behavior>
  <action>
    Edit `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`:

    (1) Imports (add at top):
       ```
       import { useProjects, setProjects } from "@/state/conversation-store";
       // ^ or wherever the derived selector is consumed
       import { useCollapsedProjectSlugs } from "@/state/use-collapsed-project-slugs";
       import { setSessionProject, setRelayRoomProject } from "@/api/session-project-api";
       import { PrettyProjectSectionHeader } from "./PrettyProjectSectionHeader";
       import { FolderOpen } from "lucide-react";  // for the create-project header button
       ```

    (2) Inside the panel function body:
       - `const projects = useProjects();`
       - `const {collapsed, toggle: toggleCollapse} = useCollapsedProjectSlugs();`
       - `const [createProjectModalOpen, setCreateProjectModalOpen] = useState(false);`
       - The snapshot now returns `{pinnedUnassigned, projectSections, middle, rdp}` — use the new shape; verify the derived selector call site.

    (3) In the render tree at the position between pinned zone and flat middle (D-09):
       - For each entry in `snapshot.projectSections`, render:
         ```
         <PrettyProjectSectionHeader
           key={section.slug}
           slug={section.slug}
           displayName={section.displayName}
           collapsed={collapsed.has(section.slug)}
           onToggleCollapse={toggleCollapse}
           onNewConversationClick={handleNewConversationInProject}
           onDropRow={handleProjectDrop}
           rows={section.rows.map((row) => <PrettyConversationRowLive key={row.id} row={row} {...rowProps} />)}
         />
         ```
         (Match the existing rowProps threading — grep for how PrettyConversationRow is invoked today. Do NOT rebuild the row props from scratch.)

    (4) Add `handleProjectDrop(slug: string, payload: {id, host?, matrixRoomId?, targetTmuxSession?, rdpHostRow?})`:
       - Guard against `rdpHostRow === true` (defense-in-depth; the section already guards, but the panel also guards).
       - If `payload.matrixRoomId` is present → resolve userMxid from AppShell's auth context (grep for how existing panels reach userMxid — likely a `viewingUser` store or a `getAuthUser()` helper) → call `setRelayRoomProject(payload.matrixRoomId, userMxid, slug).catch(err => console.error(err))`.
       - Else if `payload.host` and `payload.targetTmuxSession` are present → call `setSessionProject(payload.host, identityKeyFromRow, slug).catch(...)`. The identityKey is derived from the row — this is either `payload.targetTmuxSession` OR a separate `payload.identityKey` field. Grep the DnD source in PrettyConversationRow.tsx:949-973 to see which fields the row DnD payload actually carries; use whichever field carries the identity key.
       - Fire-and-forget (no await); errors log via console.error. On success, the WS event will eventually propagate the state change; on failure, log locally (a toast/banner is out of scope for v1 per shape D-07 graceful-degradation).

    (5) Add `handleFlatMiddleDrop(e: React.DragEvent)`:
       - Type-gate on `application/x-skynet-row` first.
       - Extract payload; guard rdpHostRow.
       - Only fire clear if the payload's row IS currently assigned to a project (check via projectSections membership OR by consulting a lookup on the payload — the derived selector or the row row itself carries this info). If not currently assigned, no-op.
       - If assigned + identity → `setSessionProject(host, key, null)`. If assigned + relay-room → `setRelayRoomProject(roomId, userMxid, null)`.
       - Wire this handler to the flat-middle container in the render tree (grep for the existing middle-container div; add onDragOver + onDragLeave + onDrop with the same type-gate discipline used in project sections). NOTE: the flat middle already has DnD handlers today (for split-view drag — see `SplitView.tsx:340`); take care to NOT clobber or shadow them. If both handlers should coexist, ensure your type-gate is strict (`application/x-skynet-row` ONLY, ignore other MIMEs) so the existing split-view badge drag still works.

    (6) Add `handleNewConversationInProject(slug: string)` — for now (before 117-09), just record the intent so the modal can pick it up: `setCreateProjectModalOpen(true)` + set a pending-project-slug state variable. The actual pre-filling of the frontmatter in the new-conversation flow is a 117-09 concern; here we just wire the button. Note in the SUMMARY that `handleNewConversationInProject` needs a follow-up wire in 117-09.

    (7) Add the "Create project" button to the header button cluster at :1701-1745. Use `FolderOpen` icon (Claude's Discretion per CONTEXT), placed alongside the pencil/drama/globe icons. onClick: `setCreateProjectModalOpen(true)`. Preserve the existing header buttons' order and styling — insert the new button at a visually-appropriate position (recommend: after the pencil-edit button, before the filter icon).

    (8) Extend `PrettyConversationsPanel.test.tsx` with the 12 tests. Reuse the store-mock pattern from existing tests. For DnD tests, use the same synthetic-event pattern as PrettyProjectSectionHeader.test.tsx (Task 1).

    Do NOT create the actual CreateProjectModal component here — 117-09 owns it. Just render a placeholder for now (e.g., a data-testid marker that becomes the modal component in 117-09). OR, if it's cleaner, add the modal component in 117-09 and pass its rendered form via a simple `{createProjectModalOpen && <div data-testid="create-project-modal-placeholder" />}` block that 117-09 replaces.

    Do NOT modify the existing pinned zone / RDP / Archived rendering. Insert the projects zone strictly BETWEEN pinned and flat middle.
  </action>
  <acceptance_criteria>
    - `grep -c "PrettyProjectSectionHeader" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` outputs ≥ 2 (import + at least one render usage).
    - `grep -c "useProjects\\|useCollapsedProjectSlugs" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` outputs ≥ 2.
    - `grep -c "setSessionProject\\|setRelayRoomProject" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` outputs ≥ 3 (imports + at least 2 call sites: assign-to-section + clear-in-middle).
    - `grep -c "handleProjectDrop\\|handleFlatMiddleDrop\\|handleNewConversationInProject" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` outputs ≥ 3.
    - Verify the projects zone is inserted BETWEEN pinned and flat middle via a manual read of the JSX region — the executor should paste the relevant JSX snippet into SUMMARY.md for audit. Automated grep gate: `grep -n "Project:\\|pv-project-section" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` shows the project rendering block; a companion grep for `Pinned` or `pv-pinned` in the same file confirms the pinned zone comes BEFORE (lower line number).
    - `grep -c "Projects\"\\|>Projects<" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx | grep -v '^#'` outputs `0` (no "Projects" super-section header per D-10). Careful: this shouldn't match "Project:" (colon after) — the grep is anchored on the closing quote or bracket.
    - `npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` exits 0 with all pre-existing tests still green AND the 12 new tests passing.
    - `npx tsc --noEmit -p tsconfig.json` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx src/ui/features/pretty-conversations/PrettyProjectSectionHeader.test.tsx</automated>
  </verify>
  <done>Sidebar renders project sections between pinned and flat middle; drop-onto-section assigns; drop-in-middle clears; create-project button state toggles; empty sections still visible; existing tests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser drag source → drop handler | dataTransfer payload is user-controlled (via row DnD source); parsed as JSON in a try/catch |
| Drop handler → session-project-api | validated payload passes to network API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-08-01 | Tampering | Malformed drag payload (unparseable JSON, wrong shape) | mitigate | try/catch on JSON.parse + shape validation (`typeof p.id === "string"` etc.) before invoking the API. Test 14 asserts drop with empty types is a no-op. |
| T-117-08-02 | Tampering | Badge drag misrouted to project drop lane | mitigate | Type-gate on `application/x-skynet-row` ONLY (grep gate confirms no `application/x-skynet-badge` reads). Test 7 asserts wrong-MIME drag does not activate coral. |
| T-117-08-03 | Tampering | RDP row assigned a project via drag | mitigate | Two-level defense: PrettyProjectSectionHeader refuses `rdpHostRow=true` payloads at the drop; the panel's handleProjectDrop also guards. Test 13 in Task 1 asserts the section-level guard. |
| T-117-08-04 | Denial of Service | Rapid drag-drop cycle spams the wire | mitigate | Fire-and-forget with `.catch` on the API call; each drop is a single POST; backend registry idempotent-skip (117-03) absorbs no-op wire events. |
| T-117-08-05 | Info Disclosure | Coral overlay exposes drag target info to unauthorized viewers | accept | Coral is a UI affordance visible only within the user's own browser; no cross-user leakage. |
| T-117-08-06 | Elevation of Privilege | User drops another user's conversation into their own project section | accept | Route layer (117-05) enforces per-user host isolation via resolveHostById; the drop request would 404. |
| T-117-08-SC | Tampering | npm installs | accept | No new packages installed (lucide-react + React already present). |
</threat_model>

<verification>
- `npx vitest run src/ui/features/pretty-conversations/PrettyProjectSectionHeader.test.tsx src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` — all pass; no regressions in pre-existing panel tests.
- `npx tsc --noEmit -p tsconfig.json` — 0 errors.
- Manual dev-server smoke: after 117-08, running `npm run dev` and opening the sidebar with a seeded project should show the project section between pinned and middle, and dragging a row onto it should activate the coral overlay. This smoke check is documented in SUMMARY but not automated in the executor gate.
</verification>

<success_criteria>
Sidebar renders the projects zone. Drag-and-drop assigns and clears project membership. Empty project sections still show up. Existing pinned + flat-middle + RDP + Archived rendering unchanged. Create-project button toggles a state that 117-09 will hook the modal onto.
</success_criteria>

<output>
Create `.planning/phases/117-projects-sidebar-section-with-drag-and-drop-membership-per-p/117-08-SUMMARY.md` when done. Include: (1) PrettyProjectSectionHeader exports + Props verbatim, (2) the panel's new handleProjectDrop / handleFlatMiddleDrop / handleNewConversationInProject signatures, (3) the exact insertion point (line number range) of the projects zone in the panel JSX, (4) userMxid resolution mechanism used (which hook/context), (5) whether the identityKey field in the row DnD source needed to be added or was already present, (6) test counts (new + pre-existing panel tests).
</output>
