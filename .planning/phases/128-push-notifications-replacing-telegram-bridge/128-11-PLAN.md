---
phase: 126-push-notifications-replacing-telegram-bridge
plan: 11
type: execute
wave: 5
depends_on: [128-01, 128-02, 128-05, 128-06, 128-08, 128-09, 128-10]
files_modified:
  - src/backend/database/db/index.integration.test.ts  # NEW or extended integration test
autonomous: true
requirements: [D-11, D-13, D-18]
tags: [schema-push-equivalent, verification, integration-test]

must_haves:
  truths:
    - "Booting the backend in dev mode (no docker) with the new schema succeeds — the initializeDatabase path creates push_subscriptions with its UNIQUE index and runs runTelegramBotTokensTableDrop"
    - "An integration test asserts: (a) push_subscriptions table exists after initializeDatabase; (b) the UNIQUE(user_id, endpoint) constraint is enforced (duplicate INSERT returns changes=0); (c) telegram_bot_tokens does not exist post-init; (d) the drop-migration is idempotent (running init twice does not error)"
    - "The full scoped verification sweep passes across the phase's touched files (all module test files from Plans 01-10)"
  artifacts:
    - path: "src/backend/database/db/index.integration.test.ts"
      provides: "Integration test exercising initializeDatabase in an in-memory SQLite DB and asserting on the resulting schema state"
      exports: []
  key_links:
    - from: "src/backend/database/db/index.integration.test.ts"
      to: "src/backend/database/db/index.ts (initializeDatabase + runTelegramBotTokensTableDrop)"
      via: "direct call in test setup"
      pattern: "initializeDatabase"
    - from: "this plan's verification sweep"
      to: "every file touched by Plans 01-10"
      via: "npx vitest related --run <all files>"
      pattern: "vitest related --run"
---

<objective>
The `[BLOCKING]` schema-push equivalent + a phase-wide integration verification. Skynet does NOT use `drizzle-kit push` — schema changes ship via hand-written CREATE TABLE + drop-migration functions in src/backend/database/db/index.ts (mirroring runPinColumnDrop / runHiddenColumnDrop). The equivalent "schema is queryable in dev" gate is: (a) call initializeDatabase in an in-memory SQLite DB and (b) assert the resulting schema state matches expectations.

Purpose: The schema_push_requirement in this phase's planning brief specifies a BLOCKING task that "verifies the schema is queryable in dev." This plan is that task. It also acts as the phase-wide integration checkpoint: all module-level tests are re-run scoped across every file the phase touched.

Actual container-side migration execution (Skynet's in-memory-then-persist model applies on boot) is an ORCHESTRATOR step at deploy time, not this executor's remit — the plan's job is the pre-deploy build+test-time verification that the schema motion is correct.

Output: One new integration test file + a full scoped verification sweep across Plans 01-10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-push-notifications-replacing-telegram-bridge/128-CONTEXT.md
@.planning/phases/128-push-notifications-replacing-telegram-bridge/128-RESEARCH.md
@.planning/phases/128-push-notifications-replacing-telegram-bridge/128-PATTERNS.md
@.planning/phases/128-push-notifications-replacing-telegram-bridge/128-01-SUMMARY.md
@.planning/phases/128-push-notifications-replacing-telegram-bridge/128-08-SUMMARY.md
@.planning/phases/128-push-notifications-replacing-telegram-bridge/128-09-SUMMARY.md
@.planning/phases/128-push-notifications-replacing-telegram-bridge/128-10-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build integration test exercising initializeDatabase (schema-push equivalent) — the [BLOCKING] task</name>
  <files>src/backend/database/db/index.integration.test.ts</files>
  <read_first>
    - src/backend/database/db/index.ts (whole file — understand initializeDatabase's boot sequence + the CREATE TABLE path for push_subscriptions from Plan 01 + the runTelegramBotTokensTableDrop from Plan 01)
    - src/backend/database/db/index.test.ts (existing test file — mirror its shape; understand how it constructs an in-memory better-sqlite3 DB for unit-level testing)
    - src/backend/database/db/schema.ts (confirm pushSubscriptions is exported + telegramBotTokens is NOT exported post-Plan 09)
    - .planning/phases/128-push-notifications-replacing-telegram-bridge/128-01-SUMMARY.md (Plan 01's schema changes + drop migration)
    - .planning/phases/128-push-notifications-replacing-telegram-bridge/128-09-SUMMARY.md (Plan 09's telegramBotTokens export deletion)
    - .planning/phases/128-push-notifications-replacing-telegram-bridge/128-CONTEXT.md D-11 (subscription lifecycle needs persistence gate), D-18 (teardown gate — telegram_bot_tokens must be gone)
    - `schema_push_requirement` in this phase's planning brief: this is the mandatory task that verifies the schema motion is correct pre-deploy
  </read_first>
  <behavior>
    - New integration test file at src/backend/database/db/index.integration.test.ts (`.integration.test.ts` suffix follows any existing convention this repo uses for integration tests; if none exists, `index.test.ts` is fine — the executor picks based on grep of existing `.integration.test.ts` files in the repo).
    - Test uses a fresh in-memory better-sqlite3 DB (not a shared module-level DB) — construct via `new Database(":memory:")` per better-sqlite3 API, or however initializeDatabase accepts a target DB in the existing test surface.
    - Test 1: after initializeDatabase runs, `SELECT sql FROM sqlite_master WHERE type='table' AND name='push_subscriptions'` returns exactly one row with SQL that includes the expected columns (id, user_id, endpoint, p256dh, auth, created_at, last_delivered_at).
    - Test 2: after initializeDatabase runs, `SELECT sql FROM sqlite_master WHERE type='index' AND name='push_subscriptions_user_endpoint_unique'` returns exactly one row.
    - Test 3: given a users row exists, INSERT INTO push_subscriptions (id, user_id, endpoint, p256dh, auth) VALUES (?, 'test-user', 'https://push.example.com/abc', ?, ?); a SECOND INSERT with the SAME (user_id, endpoint) returns changes=0 (UNIQUE constraint enforcement — mirrors the ON CONFLICT DO NOTHING pattern used by the route in Plan 05).
    - Test 4: given telegram_bot_tokens existed before initializeDatabase (executor manually CREATEs it in the test setup as a pre-existing row), after initializeDatabase runs the table does NOT exist (runTelegramBotTokensTableDrop worked on hit).
    - Test 5: given telegram_bot_tokens does NOT exist before initializeDatabase (fresh DB), initializeDatabase does NOT throw (runTelegramBotTokensTableDrop no-op on miss).
    - Test 6: idempotency — calling initializeDatabase twice on the same DB does not throw and leaves the schema in the expected state (all IF NOT EXISTS / DROP IF EXISTS discipline holds).
    - Test 7: cascade — INSERT users row + INSERT push_subscriptions row referencing it + DELETE users row + assert push_subscriptions is empty (FK ON DELETE CASCADE from Plan 01 works).
    - Every test uses a fresh in-memory DB to avoid cross-test pollution.
  </behavior>
  <action>
    Create src/backend/database/db/index.integration.test.ts. Use vitest's `describe` + `it` + `expect` + `beforeEach` (per repo conventions). Import initializeDatabase and any helpers from ./index. Import Database from better-sqlite3.

    Structure:
    ```
    describe("Phase 128 schema integration", () => {
      let db;
      beforeEach(() => {
        db = new Database(":memory:");
        // Optionally pre-create users table + telegram_bot_tokens depending on the test
      });
      it("Test 1: push_subscriptions table exists after initializeDatabase", () => { ... });
      it("Test 2: UNIQUE index exists", () => { ... });
      it("Test 3: UNIQUE constraint enforced", () => { ... });
      it("Test 4: telegram_bot_tokens drop on hit", () => { ... });
      it("Test 5: telegram_bot_tokens drop no-op on miss", () => { ... });
      it("Test 6: idempotent", () => { ... });
      it("Test 7: FK cascade", () => { ... });
    });
    ```

    Each test writes clear assertion messages that make the failure mode obvious.

    NOTE: this task does NOT boot the FULL Skynet backend or run docker or hit Matrix or send push. It only exercises the DB module in isolation with an in-memory SQLite. Real container-side migration on deploy is orchestrator work; this test proves the migration is CORRECT before it ever runs on the deployed DB.
  </action>
  <verify>
    <automated>npm run build:backend &amp;&amp; npm run build &amp;&amp; npx vitest related --run src/backend/database/db/index.integration.test.ts src/backend/database/db/index.ts src/backend/database/db/schema.ts</automated>
  </verify>
  <acceptance_criteria>
    - File src/backend/database/db/index.integration.test.ts exists.
    - `grep -c "push_subscriptions" src/backend/database/db/index.integration.test.ts` returns >= 3 (multiple tests reference the table).
    - `grep -c "telegram_bot_tokens" src/backend/database/db/index.integration.test.ts` returns >= 2 (drop-on-hit + drop-on-miss tests).
    - `grep -c "user_endpoint_unique\\|UNIQUE" src/backend/database/db/index.integration.test.ts` returns >= 1 (uniqueness test present).
    - `grep -c "ON DELETE CASCADE\\|cascade" src/backend/database/db/index.integration.test.ts` returns >= 1 (cascade test present).
    - `npm run build:backend && npm run build` exits 0.
    - `npx vitest related --run src/backend/database/db/index.integration.test.ts` exits 0 with all seven tests passing.
  </acceptance_criteria>
  <done>Integration test file present, all seven schema-verification tests green, backend build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-wide scoped verification sweep — run vitest related across every file the phase touched</name>
  <files>(no files created — this task is a verification-only sweep)</files>
  <read_first>
    - Every previous plan's SUMMARY.md (128-01-SUMMARY.md through 128-10-SUMMARY.md) — the SUMMARYs list the files each plan touched. The union of those files is what this sweep must cover.
    - The frontmatter `files_modified` of every plan (128-01 through 128-10) — canonical source of the file list
    - .planning/phases/128-push-notifications-replacing-telegram-bridge/128-PATTERNS.md § S7 (backend TS build gate — required after any src/backend/ patch)
    - Fleet rule from planning_context: SCOPED tests during dev, NEVER full-suite (`npx vitest run` alone) — this sweep uses `related --run <file list>`, NOT bare `run`
  </read_first>
  <behavior>
    - `npm run build:backend && npm run build` exits 0 (full backend + frontend TS build).
    - `npx vitest related --run <union of every file touched by Plans 01-10>` exits 0.
    - No test regressions from any plan's changes surface at the union level.
  </behavior>
  <action>
    Compute the file list. Sources:
    - Plan 01: src/backend/database/db/schema.ts, src/backend/database/db/index.ts, src/backend/database/db/schema.test.ts, src/backend/database/db/index.test.ts
    - Plan 02: package.json, package-lock.json, src/backend/notifications/vapid-config.ts, src/backend/notifications/vapid-config.test.ts, src/backend/notifications/push-sender.ts, src/backend/notifications/push-sender.test.ts
    - Plan 03: src/backend/notifications/preview-text.ts, src/backend/notifications/preview-text.test.ts, src/backend/notifications/resolve-agent-display-name.ts, src/backend/notifications/resolve-agent-display-name.test.ts
    - Plan 04: public/sw.js (no test file — SW is served literally)
    - Plan 05: src/backend/database/routes/push-subscriptions.ts, src/backend/database/routes/push-subscriptions.test.ts, docker/nginx.conf, docker/nginx-https.conf (conf files not vitest-scoped)
    - Plan 06: src/backend/notifications/push-trigger-loop.ts, src/backend/notifications/push-trigger-loop.test.ts, src/backend/notifications/push-trigger-starter.ts, src/backend/notifications/push-trigger-starter.test.ts
    - Plan 07: src/ui/features/notifications/EnableNotificationsButton.tsx, src/ui/features/notifications/EnableNotificationsButton.test.tsx, src/ui/features/notifications/push-subscription-api.ts, src/ui/features/notifications/push-subscription-api.test.ts, src/ui/AppShell.tsx
    - Plan 08: src/backend/starter.ts, src/backend/database/database.ts
    - Plan 09: src/backend/database/routes/voice.ts, src/backend/database/db/schema.ts, src/backend/matrix/matrix-admin-client.ts (deletions covered by preserved test files that formerly referenced the deleted modules — if any test file was left dangling, it should have been deleted alongside its source in Plan 09)
    - Plan 10: docker/docker-compose.yml (not vitest-scoped)
    - Plan 11 Task 1: src/backend/database/db/index.integration.test.ts

    Build the sweep command:
    ```
    npx vitest related --run \
      src/backend/database/db/schema.ts \
      src/backend/database/db/index.ts \
      src/backend/database/db/index.integration.test.ts \
      src/backend/notifications/vapid-config.ts \
      src/backend/notifications/push-sender.ts \
      src/backend/notifications/preview-text.ts \
      src/backend/notifications/resolve-agent-display-name.ts \
      src/backend/notifications/push-trigger-loop.ts \
      src/backend/notifications/push-trigger-starter.ts \
      src/backend/database/routes/push-subscriptions.ts \
      src/backend/database/routes/voice.ts \
      src/backend/matrix/matrix-admin-client.ts \
      src/backend/starter.ts \
      src/backend/database/database.ts \
      src/ui/features/notifications/EnableNotificationsButton.tsx \
      src/ui/features/notifications/push-subscription-api.ts \
      src/ui/AppShell.tsx
    ```

    Run the sweep. Expected: exit 0. If any test fails:
    - Do NOT bypass or skip. Investigate the failure inline.
    - Common causes: (a) a plan's test-doubles drift out of sync with a downstream module signature change (fix the test-double); (b) a deletion missed a reference (fix by removing the reference); (c) a plan's assertion is too strict/loose about the joint state (widen or narrow with rationale in SUMMARY).

    Run `npm run build:backend && npm run build` — exit 0 confirms the full TS project (backend + frontend) type-checks with all phase changes cumulative.

    NOTE: do NOT run bare `npx vitest run` (the full suite). Per fleet test discipline (planning_context), full-suite runs are a DEPLOY-GATE, not a dev-time gate. The orchestrator handles full-suite at deploy time.
  </action>
  <verify>
    <automated>npm run build:backend &amp;&amp; npm run build &amp;&amp; npx vitest related --run src/backend/notifications/ src/backend/database/routes/push-subscriptions.ts src/backend/database/routes/voice.ts src/backend/matrix/matrix-admin-client.ts src/backend/database/db/schema.ts src/backend/database/db/index.ts src/backend/database/db/index.integration.test.ts src/backend/starter.ts src/backend/database/database.ts src/ui/features/notifications/</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build:backend && npm run build` exits 0.
    - The vitest related sweep above exits 0.
    - Executor's SUMMARY notes any test-double drift or shared-mock changes encountered during the sweep.
    - Executor confirms NO invocation of bare `npx vitest run` (full suite) was used.
  </acceptance_criteria>
  <done>Full backend+frontend build clean, phase-wide scoped test sweep green, SUMMARY notes any shared-mock adjustments.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-time in-memory SQLite → production in-memory-with-DiskFileEncryption path | Boot codepath differs slightly (encryption layer); integration test covers the schema motion only |
| Phase-wide test sweep vs cross-file mock drift | Independently-tested modules can accumulate joint incompatibilities |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-56 | Availability | Schema motion works in-memory-test but fails against DatabaseFileEncryption at deploy | mitigate | Orchestrator's deploy-time boot log is the ultimate gate; the in-memory test proves the SQL is correct; deploy adds only the encryption layer which is independently tested by existing DatabaseFileEncryption test coverage |
| T-126-57 | Availability | Phase-wide sweep passes but full suite fails at deploy | accept | Full suite is orchestrator's deploy-gate concern per fleet rule; scoped sweeps are executor's job, and the sweep covers every touched file's related tests transitively |
| T-126-58 | Repudiation | Integration test's UNIQUE-constraint assertion drifts from Plan 05's ON CONFLICT DO NOTHING assumption | mitigate | Test 3 explicitly asserts changes===0 on duplicate — same invariant Plan 05's route relies on |
| T-126-59 | Denial of Service | Cascade delete cascades unexpectedly to production users | accept | Test 7 exercises the exact cascade motion; if the test passes, the production motion is correct (ON DELETE CASCADE is a schema-level guarantee) |
</threat_model>

<verification>
- Integration test file exists + all seven tests green
- Phase-wide scoped test sweep exit 0
- Backend + frontend build exit 0
- No bare `npx vitest run` invocations
</verification>

<success_criteria>
The schema motion is proven correct at test-time. Every module in Plans 01-10 remains testable and green in the joint context. The phase is ready for the orchestrator's deploy step (which runs container build + docker compose up + HTTPS smoke + docker logs check + manual iOS UAT).
</success_criteria>

<output>
Create `.planning/phases/128-push-notifications-replacing-telegram-bridge/128-11-SUMMARY.md` when done. SUMMARY MUST include:
1. Confirmation that the [BLOCKING] schema integration test file exists and all seven tests are green.
2. The exact `npx vitest related --run <file list>` command run and its exit code.
3. Any test-double drift or cross-file adjustments made during the sweep (with rationale).
4. Explicit statement that no `npx vitest run` (bare, full-suite) was invoked — per fleet test discipline.
5. Handoff note for the orchestrator's deploy step listing: (a) VAPID env vars needed at deploy time; (b) `docker volume rm skynet_tg-bridge-state` step; (c) `docker image prune -f` step for the orphaned tg-bridge:local image; (d) reminder that manual iOS PWA UAT is deploy-side, not executor-side.
</output>
