---
phase: 126-push-notifications-replacing-telegram-bridge
plan: 09
type: execute
wave: 4
depends_on: [128-08]
files_modified:
  - src/backend/telegram/  # entire directory DELETED
  - substrate/services/tg-bridge/  # entire directory DELETED
  - src/backend/database/routes/voice.ts
  - src/backend/database/db/schema.ts
  - src/backend/matrix/matrix-admin-client.ts  # conditional: only if grep confirms zero non-telegram callers of getSharedDMRoom
autonomous: true
requirements: [D-18, D-19]
tags: [telegram-teardown, source-deletion, destructive]

must_haves:
  truths:
    - "All 22 files under src/backend/telegram/ are deleted"
    - "All 4 files under substrate/services/tg-bridge/ are deleted"
    - "The rejectBridgeServiceOnSpeak block in src/backend/database/routes/voice.ts is deleted"
    - "The telegramBotTokens export in src/backend/database/db/schema.ts is deleted (the runtime CREATE was already removed in Plan 01)"
    - "getSharedDMRoom is deleted from matrix-admin-client.ts ONLY IF grep confirms zero non-telegram callers (per D-19); if any non-telegram caller exists, getSharedDMRoom is kept and a note added to SUMMARY.md"
    - "Backend build + related tests remain green (no dangling references from surviving source)"
  artifacts:
    - path: "src/backend/telegram/"
      provides: "DELETED — directory does not exist post-plan"
    - path: "substrate/services/tg-bridge/"
      provides: "DELETED — directory does not exist post-plan"
    - path: "src/backend/database/routes/voice.ts"
      provides: "rejectBridgeServiceOnSpeak block removed (was at ~L420-435)"
    - path: "src/backend/database/db/schema.ts"
      provides: "telegramBotTokens export removed"
  key_links:
    - from: "src/backend/database/routes/voice.ts (post-deletion)"
      to: "existing voice-route logic"
      via: "no more tg-bridge-service special case"
      pattern: "tg-bridge-service"
    - from: "src/backend/matrix/matrix-admin-client.ts"
      to: "getSharedDMRoom (conditional)"
      via: "removed if zero non-telegram callers"
      pattern: "getSharedDMRoom"
---

<objective>
The bulk of the tg-bridge teardown from D-18/D-19: delete all telegram source directories, the voice.ts special case, the drizzle schema export, and conditionally the getSharedDMRoom helper. This plan is the second-largest teardown surface after Plan 10 (compose + nginx). The backend must remain buildable and tests must remain green — no dangling references from surviving code.

Purpose: D-18 (teardown scope — code side) and D-19 (getSharedDMRoom conditional deletion). This plan runs in Wave 3 in PARALLEL with Plan 10 (docker/nginx teardown) because file scopes are disjoint. Both depend on Plan 08 (route mount + starter dispatches removed) — with those gone, deleting the source code cannot break the running server.

Output: 22 backend telegram files deleted, 4 substrate tg-bridge files deleted, voice.ts special-case removed, schema.ts telegramBotTokens export removed, getSharedDMRoom conditionally removed. Backend build + related tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-push-notifications-replacing-telegram-bridge/128-CONTEXT.md
@.planning/phases/128-push-notifications-replacing-telegram-bridge/128-RESEARCH.md
@.planning/phases/128-push-notifications-replacing-telegram-bridge/128-PATTERNS.md
@.planning/phases/128-push-notifications-replacing-telegram-bridge/128-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete src/backend/telegram/ (22 files) + substrate/services/tg-bridge/ (4 files); confirm no dangling references</name>
  <files>src/backend/telegram/, substrate/services/tg-bridge/</files>
  <read_first>
    - .planning/phases/128-push-notifications-replacing-telegram-bridge/128-PATTERNS.md § Deletion Manifest (canonical enumeration of files to delete)
    - .planning/phases/128-push-notifications-replacing-telegram-bridge/128-RESEARCH.md § Runtime State Inventory (understand what state disappears — nothing on the running-Skynet side needs migration; docker volume + BotFather regs are Plan 10 + operator concerns)
    - .planning/phases/128-push-notifications-replacing-telegram-bridge/128-CONTEXT.md D-18 (teardown scope), D-20 (destructive shutdown accepted)
    - Run `ls src/backend/telegram/` before deleting to confirm scope matches PATTERNS.md
    - Run `ls substrate/services/tg-bridge/` before deleting to confirm scope
  </read_first>
  <behavior>
    - src/backend/telegram/ directory does not exist post-task.
    - substrate/services/tg-bridge/ directory does not exist post-task.
    - No code outside these directories still imports from them (grep confirms zero surviving references).
    - Backend build + related tests remain green.
  </behavior>
  <action>
    Confirm scope with `ls src/backend/telegram/` — expected files per PATTERNS.md § Deletion Manifest: bot-token-file-writer.{ts,test.ts}, bridge-config-writer.{ts,test.ts}, bridge-service-token.{ts,test.ts}, getme-proxy.{ts,test.ts}, human-token-writer.{ts,test.ts}, reconcile-dead-tokens.{ts,test.ts}, reconcile-pending-chat-ids.{ts,test.ts}, registry-writer.{ts,test.ts}, routes.{ts,test.ts}, shared-volume.{ts,test.ts}, tokens-store.{ts,test.ts} = 22 files.

    Confirm scope with `ls substrate/services/tg-bridge/` — expected: bridge.sh, Dockerfile.tg-bridge, README.md, cursor-persistence-repro.sh = 4 files.

    Pre-deletion safety grep: run `grep -rn 'from "\.\./telegram/\|from "\.\./\.\./telegram/' src/backend/ src/ui/ 2>&1 | grep -v "^src/backend/telegram/"` — expected output: empty (Plan 08 removed the last surviving import from starter.ts and database.ts). If any hit surfaces, STOP and log the reference — the plan needs revisiting; do NOT delete until the caller is handled.

    Delete `src/backend/telegram/` entirely with `rm -rf src/backend/telegram/`.
    Delete `substrate/services/tg-bridge/` entirely with `rm -rf substrate/services/tg-bridge/`.

    Post-deletion: run backend build (`npm run build:backend && npm run build`) — MUST exit 0. If any TS error surfaces, it means Plan 08 missed a reference; fix inline by removing the offending import + any transitive dangling call, then re-run build.

    Run `npx vitest related --run src/backend/database/database.ts src/backend/starter.ts src/backend/database/db/schema.ts` to confirm no surviving test files reference the deleted modules.
  </action>
  <verify>
    <automated>test ! -d src/backend/telegram &amp;&amp; test ! -d substrate/services/tg-bridge &amp;&amp; npm run build:backend &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -d src/backend/telegram` exits 0 (directory gone).
    - `test ! -d substrate/services/tg-bridge` exits 0 (directory gone).
    - `grep -rn 'from "\.\./telegram/\|from "\.\./\.\./telegram/' src/backend/ src/ui/ 2>&1 | grep -v "^src/backend/telegram/" | wc -l` returns 0 (no dangling imports anywhere).
    - `grep -rn "src/backend/telegram" .planning/ 2>&1 | grep -v "phases/126-" | wc -l` returns 0 OR only counts historical references in older phase SUMMARYs (historical references are fine — planning docs are chronological record).
    - `npm run build:backend && npm run build` exits 0.
    - `npx vitest related --run src/backend/database/database.ts src/backend/starter.ts src/backend/database/db/schema.ts` exits 0.
  </acceptance_criteria>
  <done>Both directories gone, no surviving code references, backend build + related tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Delete rejectBridgeServiceOnSpeak block from voice.ts + telegramBotTokens export from schema.ts + conditionally getSharedDMRoom from matrix-admin-client.ts</name>
  <files>src/backend/database/routes/voice.ts, src/backend/database/db/schema.ts, src/backend/matrix/matrix-admin-client.ts</files>
  <read_first>
    - src/backend/database/routes/voice.ts lines 415-440 (the rejectBridgeServiceOnSpeak block at ~L420-435 per PATTERNS.md §16 — DELETE the whole block)
    - src/backend/database/db/schema.ts (find the telegramBotTokens export around line 759 — DELETE the whole export block)
    - src/backend/matrix/matrix-admin-client.ts around line 710 (getSharedDMRoom definition) AND lines 460-475, 615-625, 685-710 which have supporting comments referencing it
    - .planning/phases/128-push-notifications-replacing-telegram-bridge/128-PATTERNS.md §16 + § Deletion Manifest (voice + schema deletions) + § D-19 conditional (getSharedDMRoom check-before-delete)
    - .planning/phases/128-push-notifications-replacing-telegram-bridge/128-CONTEXT.md D-19 (getSharedDMRoom deletion IS conditional on zero non-telegram callers)
  </read_first>
  <behavior>
    - voice.ts:
      - rejectBridgeServiceOnSpeak function definition + any adjacent early-return call inside voice route handlers → both DELETED
      - Any `if (userId === "tg-bridge-service") ...` special-case guards inside voice route handlers → DELETED
      - No behavioral change to voice route for real users (the special-case rejected tg-bridge-service; that identity no longer exists post-teardown)
    - schema.ts:
      - `export const telegramBotTokens = sqliteTable(...)` block DELETED entirely
      - Any adjacent doc comments referencing the table for future readers → DELETED
    - matrix-admin-client.ts:
      - FIRST: run the D-19 grep gate to determine whether getSharedDMRoom has any non-telegram callers:
        `grep -rn "getSharedDMRoom" src/ 2>&1 | grep -v "src/backend/telegram/" | grep -v "src/backend/matrix/matrix-admin-client.ts:"`
        - If the grep returns zero lines (only definition + telegram callers exist, and telegram/ is already deleted per Task 1 so all telegram callers are gone) → DELETE the getSharedDMRoom export, the definition, and the supporting helper comments that reference it (lines ~465-475 and ~615-625 per read_first; keep only the underlying primitive that was extracted out for non-getSharedDMRoom use)
        - If the grep returns >= 1 line → KEEP getSharedDMRoom, log in SUMMARY.md that D-19's condition was NOT met and name the non-telegram caller
    - Backend build + related tests green after all three deletions.
  </behavior>
  <action>
    In src/backend/database/routes/voice.ts:
    - Locate the rejectBridgeServiceOnSpeak function at ~L420-435. DELETE the entire block (function definition + any docblock).
    - Search the file for any call to `rejectBridgeServiceOnSpeak(...)` or any `if (userId === "tg-bridge-service")` guards. Delete each. Preserve surrounding voice route handler logic exactly (additive-inverse — remove the guard, keep the surrounding flow).

    In src/backend/database/db/schema.ts:
    - Locate the `export const telegramBotTokens = sqliteTable("telegram_bot_tokens", { ... });` block (~L759 per RESEARCH.md).
    - DELETE the entire export block, plus any adjacent doc comments that reference it (e.g. "// Phase 79 Plan 01 telegram_bot_tokens ..." comments).
    - Do NOT touch other schema exports.

    In src/backend/matrix/matrix-admin-client.ts:
    - Run the D-19 grep gate: `grep -rn "getSharedDMRoom" src/ 2>&1 | grep -v "src/backend/telegram/" | grep -v "src/backend/matrix/matrix-admin-client.ts:"`
    - If empty:
      - DELETE the `export async function getSharedDMRoom(...) { ... }` at ~L710 (whole function body).
      - DELETE the file-scope comments at ~L460-475 and ~L615-625 that reference getSharedDMRoom as their motivation. If any code they doc extracted primitives that have OTHER callers, keep the primitives + delete only the getSharedDMRoom-specific narrative comments.
      - Grep-verify post-deletion: `grep -c "getSharedDMRoom" src/backend/matrix/matrix-admin-client.ts` returns 0.
    - If non-empty:
      - KEEP getSharedDMRoom untouched.
      - Log in the eventual 128-09-SUMMARY.md: "D-19 condition NOT MET — getSharedDMRoom retained; non-telegram callers found: <list of file:line hits>"
      - Note in the SUMMARY that the user may want a follow-up phase to migrate/remove those callers or accept the retained helper as long-lived.

    Post-modification: run `npm run build:backend && npm run build` — MUST exit 0. Run `npx vitest related --run src/backend/database/routes/voice.ts src/backend/database/db/schema.ts src/backend/matrix/matrix-admin-client.ts` — MUST exit 0.
  </action>
  <verify>
    <automated>grep -c "rejectBridgeServiceOnSpeak\|tg-bridge-service" src/backend/database/routes/voice.ts &amp;&amp; grep -c "telegramBotTokens" src/backend/database/db/schema.ts &amp;&amp; npm run build:backend &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rejectBridgeServiceOnSpeak" src/backend/database/routes/voice.ts` returns 0.
    - `grep -c "tg-bridge-service" src/backend/database/routes/voice.ts` returns 0.
    - `grep -c "export const telegramBotTokens" src/backend/database/db/schema.ts` returns 0.
    - `grep -c "telegram_bot_tokens" src/backend/database/db/schema.ts` returns 0.
    - For getSharedDMRoom: EITHER `grep -c "getSharedDMRoom" src/backend/matrix/matrix-admin-client.ts` returns 0 (deletion path) OR the executor's 128-09-SUMMARY.md contains an explicit "D-19 condition NOT MET" paragraph naming the surviving callers (keep-path). Executor confirms which path was taken via a note in the task output.
    - `npm run build:backend && npm run build` exits 0.
    - `npx vitest related --run src/backend/database/routes/voice.ts src/backend/database/db/schema.ts src/backend/matrix/matrix-admin-client.ts` exits 0.
  </acceptance_criteria>
  <done>voice.ts special case gone, schema.ts telegramBotTokens export gone, getSharedDMRoom conditionally gone with SUMMARY note on the decision; backend build + related tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source code deletion → surviving imports | Any dangling import breaks the backend build; strictly gated by grep + backend-build verify |
| Destructive drop of tg-bridge-service userId special case | Per D-20 accepted; no rollback surface |
| getSharedDMRoom conditional retention | D-19 explicit rule — grep gate protects against unintended removal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-45 | Availability | Dangling import from a surviving file breaks backend build | mitigate | Task 1 has a pre-deletion grep gate + a post-deletion `npm run build:backend` check; Task 2 grep-verifies via acceptance criteria |
| T-126-46 | Repudiation | getSharedDMRoom deleted despite a live non-telegram caller | mitigate | D-19 grep gate is EXPLICITLY in the task action; deletion is conditional on gate result |
| T-126-47 | Availability | voice.ts logic breaks for real users after guard removed | accept | The guard was tg-bridge-service-specific; no real user matches that userId (bridge account is deleted upstream). Post-deletion `npx vitest related --run src/backend/database/routes/voice.ts` confirms no test regressions |
| T-126-48 | Denial of Service | Substrate/tg-bridge/ still referenced by distributor catalog | mitigate | Grep `substrate/services/tg-bridge/` inside src/ and substrate/ post-deletion; if any reference in the distributor catalog exists, it must be removed as part of Task 1 |
| T-126-49 | Repudiation | Historical planning docs reference deleted files | accept | Historical .planning/phases/NN-*/... SUMMARYs are a chronological record; not touched |
</threat_model>

<verification>
- Both directories gone
- voice.ts + schema.ts changes applied
- getSharedDMRoom handled per D-19
- Backend build clean
- Related tests green
- No dangling imports anywhere in src/
</verification>

<success_criteria>
The tg-bridge source code is gone from the repo (backend + substrate). voice.ts no longer special-cases tg-bridge-service. The drizzle schema no longer exports telegramBotTokens. getSharedDMRoom either deleted (grep-confirmed zero non-telegram callers) or explicitly retained with SUMMARY-noted rationale.
</success_criteria>

<output>
Create `.planning/phases/128-push-notifications-replacing-telegram-bridge/128-09-SUMMARY.md` when done. If getSharedDMRoom was RETAINED, the SUMMARY MUST include a paragraph naming the non-telegram callers found and the user's follow-up options.
</output>
