---
phase: 126-push-notifications-replacing-telegram-bridge
plan: 08
type: execute
wave: 3
depends_on: [128-02, 128-05, 128-06]
files_modified:
  - src/backend/starter.ts
  - src/backend/database/database.ts
autonomous: true
requirements: [D-06, D-11, D-13, D-18]
tags: [push-notifications, wiring, boot-integration, telegram-teardown-partial]

must_haves:
  truths:
    - "starter.ts calls assertVapidConfigAtBoot() (from Plan 02) BEFORE any route mounts — fail-fast at boot on missing/malformed VAPID env"
    - "starter.ts dispatches startPushTriggerLoopOnBoot() (from Plan 06) fire-and-forget after the observation-loop-starter dispatch, using the same two-layer catch (dynamic-import catch + promise catch) discipline"
    - "starter.ts NO LONGER dispatches the three telegram-loop bootstrap blocks (bridge-config-writer at ~L361-378, reconcile-dead-tokens at ~L398-419, reconcile-pending-chat-ids at ~L421-443) — all three DELETED"
    - "database.ts mounts pushSubscriptionsRoutes at /push-subscriptions"
    - "database.ts NO LONGER imports telegramRoutes NOR mounts /telegram (both lines DELETED)"
  artifacts:
    - path: "src/backend/starter.ts"
      provides: "VAPID boot assertion + push-trigger loop dispatch; three telegram-loop dispatches removed"
      contains: "assertVapidConfigAtBoot\\|startPushTriggerLoopOnBoot"
    - path: "src/backend/database/database.ts"
      provides: "app.use('/push-subscriptions', pushSubscriptionsRoutes); no /telegram mount"
      contains: "push-subscriptions"
  key_links:
    - from: "src/backend/starter.ts"
      to: "src/backend/notifications/vapid-config.ts"
      via: "assertVapidConfigAtBoot() called BEFORE route mounts (mirroring assertBrandingConfigAtBoot placement)"
      pattern: "assertVapidConfigAtBoot"
    - from: "src/backend/starter.ts"
      to: "src/backend/notifications/push-trigger-starter.ts"
      via: "void import + startPushTriggerLoopOnBoot dispatch (mirroring observation-loop-starter block)"
      pattern: "startPushTriggerLoopOnBoot"
    - from: "src/backend/database/database.ts"
      to: "src/backend/database/routes/push-subscriptions.ts"
      via: "app.use('/push-subscriptions', ...)"
      pattern: "push-subscriptions"
---

<objective>
The wiring plan: connect the pieces built in Plans 02, 05, and 06 to the running server, and remove the three telegram-loop bootstrap blocks from starter.ts + the /telegram route mount from database.ts. After this plan, a fresh backend boot:
1. Fails fast if VAPID env is missing/malformed
2. Mounts the /push-subscriptions route
3. Starts the push-trigger loop for users with mxids
4. Does NOT try to boot any telegram-bridge machinery
5. Does NOT expose the /telegram HTTP prefix

Purpose: This is where the phase becomes real — the six preceding plans build components in isolation, this one connects them + starts the teardown. D-06 (standard web push wired), D-11 (route active), D-13 (subscription lifecycle end-to-end), D-18 (partial teardown — Plans 09-10 handle the rest of teardown scope).

Output: Two files modified; no new files created. Deep-touch to a load-bearing surface (starter.ts + database.ts) so care with additive-only changes is critical.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-push-notifications-replacing-telegram-bridge/128-CONTEXT.md
@.planning/phases/128-push-notifications-replacing-telegram-bridge/128-RESEARCH.md
@.planning/phases/128-push-notifications-replacing-telegram-bridge/128-PATTERNS.md
@.planning/phases/128-push-notifications-replacing-telegram-bridge/128-02-SUMMARY.md
@.planning/phases/128-push-notifications-replacing-telegram-bridge/128-05-SUMMARY.md
@.planning/phases/128-push-notifications-replacing-telegram-bridge/128-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire assertVapidConfigAtBoot + startPushTriggerLoopOnBoot into starter.ts; DELETE three telegram-loop dispatches</name>
  <files>src/backend/starter.ts</files>
  <read_first>
    - src/backend/starter.ts (WHOLE FILE, but especially lines 355-472 which contain: the three telegram-loop dispatches at ~L361-378, L398-419, L421-443 to be DELETED; the observation-loop-starter block at L455-472 which is the copy-paste template for the push-trigger dispatch; and the assertBrandingConfigAtBoot placement at L474-481 which is the copy-paste template for the VAPID assertion placement)
    - src/backend/branding/assert-boot.ts (understand the fail-fast throw shape — assertVapidConfigAtBoot from Plan 02 mirrors this)
    - src/backend/notifications/vapid-config.ts (Plan 02 artifact — confirm the assertVapidConfigAtBoot signature)
    - src/backend/notifications/push-trigger-starter.ts (Plan 06 artifact — confirm the startPushTriggerLoopOnBoot signature)
    - .planning/phases/128-push-notifications-replacing-telegram-bridge/128-PATTERNS.md §14 (starter.ts modifications — includes the exact void import block to copy) + § S3 (fire-and-forget two-layer catch)
    - .planning/phases/128-push-notifications-replacing-telegram-bridge/128-CONTEXT.md D-06 (standard machinery), D-18 (teardown scope — starter loses the three telegram dispatches here)
  </read_first>
  <behavior>
    - assertVapidConfigAtBoot() is called inside starter.ts synchronously at the same placement as assertBrandingConfigAtBoot (~L474-481) — BEFORE HTTP route mounts. Missing/malformed VAPID env aborts boot with a structured operator-facing error.
    - startPushTriggerLoopOnBoot dispatch appears as a fire-and-forget block AFTER the observation-loop-starter dispatch (starter.ts ~L455-472), using the exact same two-layer catch pattern (S3): outer catch on the void import promise, inner catch on the startPushTriggerLoopOnBoot() call inside the .then. Log lines use systemLogger.warn with operation strings "push_trigger_bootstrap_error" and "push_trigger_bootstrap_module_load_failed".
    - Lines ~L361-378 (bridge-config-writer telegram bootstrap block) DELETED — every line in that block goes.
    - Lines ~L398-419 (reconcile-dead-tokens bootstrap block) DELETED.
    - Lines ~L421-443 (reconcile-pending-chat-ids bootstrap block) DELETED.
    - No dangling imports of ../telegram/... remain in starter.ts (any imports used exclusively by the three deleted blocks get removed too — grep for `from "../telegram/` after deletion; should be zero hits).
    - starter.ts still boots cleanly (`npm run build:backend && npm run build` exits 0).
  </behavior>
  <action>
    In src/backend/starter.ts:

    ADD (import section, alongside other adjacent imports):
    - `import { assertVapidConfigAtBoot } from "./notifications/vapid-config.js";` — this is a direct static import because assertVapidConfigAtBoot is a synchronous throw-at-boot gate, not a fire-and-forget module load.

    ADD (adjacent to assertBrandingConfigAtBoot call at ~L474-481):
    - A call to `assertVapidConfigAtBoot()` in the same insertion pattern. If assertBrandingConfigAtBoot is wrapped in a try/catch that re-throws with context, mirror that; if it's a bare call that just lets throws propagate, mirror that. Match the ambient style exactly.

    ADD (after the observation-loop-starter void-import block at ~L455-472):
    - Copy the block VERBATIM from PATTERNS.md §14 (the "// Copy this exact shape from starter.ts:455-472" snippet — the entire void import + two-layer catch block).
    - The block dispatches `./notifications/push-trigger-starter.js` → `m.startPushTriggerLoopOnBoot()` with systemLogger.warn on both catch layers. Operation strings: "push_trigger_bootstrap_error" and "push_trigger_bootstrap_module_load_failed".

    DELETE (three blocks):
    1. Lines ~L361-378: the bridge-config-writer dispatch block. Delete the entire void-import + .then + .catch structure, and delete any adjacent comment lines that reference "bridge-config-writer" or "telegram bridge config".
    2. Lines ~L398-419: the reconcile-dead-tokens dispatch block. Same shape, entire block goes.
    3. Lines ~L421-443: the reconcile-pending-chat-ids dispatch block. Same shape, entire block goes.

    AFTER deletion, grep for any `from "../telegram/` imports in starter.ts. Any imports left orphaned by the three deletions must ALSO be deleted. Common candidates: import for shared-volume, tokens-store, bridge-config-writer, reconcile helpers. Remove each import statement that has zero remaining references.

    Verify preservation: the observation-loop-starter dispatch at ~L455-472 stays intact. The assertBrandingConfigAtBoot call at ~L474-481 stays intact. No other starter code touched.
  </action>
  <verify>
    <automated>npm run build:backend &amp;&amp; npm run build &amp;&amp; npx vitest related --run src/backend/starter.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "assertVapidConfigAtBoot" src/backend/starter.ts returns >= 2 (import + call).
    - grep -c "startPushTriggerLoopOnBoot" src/backend/starter.ts returns >= 1 (dispatched inside the void-import block).
    - grep -c "push_trigger_bootstrap_error\\|push_trigger_bootstrap_module_load_failed" src/backend/starter.ts returns >= 2 (both operation strings present).
    - grep -c "bridge-config-writer\\|reconcile-dead-tokens\\|reconcile-pending-chat-ids" src/backend/starter.ts returns 0 (all three telegram-loop references gone).
    - grep -c 'from "../telegram/' src/backend/starter.ts returns 0 (no orphaned telegram imports).
    - grep -c "assertBrandingConfigAtBoot" src/backend/starter.ts returns >= 1 (preserved — do not remove the sibling assertion).
    - grep -c "startObservationLoopOnBoot" src/backend/starter.ts returns >= 1 (observation-loop-starter preserved).
    - `npm run build:backend && npm run build` exits 0.
    - `npx vitest related --run src/backend/starter.ts` exits 0.
  </acceptance_criteria>
  <done>starter.ts fails-fast on VAPID, starts the push-trigger loop, and does not attempt any telegram-bridge bootstrap; backend build + related tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Mount /push-subscriptions in database.ts; DELETE telegramRoutes import + /telegram mount</name>
  <files>src/backend/database/database.ts</files>
  <read_first>
    - src/backend/database/database.ts lines 40-80 (import section — line 56 is `import telegramRoutes from "../telegram/routes.js";` per PATTERNS.md §15 — DELETE this)
    - src/backend/database/database.ts lines 1940-2130 (route mount region — line 1950 is `app.use("/telegram", telegramRoutes);` per PATTERNS.md §15 — DELETE this; also find an adjacent app.use for the new /push-subscriptions mount)
    - .planning/phases/128-push-notifications-replacing-telegram-bridge/128-PATTERNS.md §15 (database.ts modifications) + § S6 (dual-conf caveat reminder — nginx blocks already handled by Plan 05)
    - src/backend/database/routes/push-subscriptions.ts (Plan 05 artifact — the router being mounted)
    - .planning/phases/128-push-notifications-replacing-telegram-bridge/128-CONTEXT.md D-11 (route activation completes subscription persistence path)
  </read_first>
  <behavior>
    - `import pushSubscriptionsRoutes from "./routes/push-subscriptions.js";` present in database.ts imports.
    - `app.use("/push-subscriptions", pushSubscriptionsRoutes);` present in the route-mount region (adjacent to other app.use calls per PATTERNS.md §15).
    - `import telegramRoutes from "../telegram/routes.js";` REMOVED from database.ts imports.
    - `app.use("/telegram", telegramRoutes);` REMOVED from database.ts route mounts.
    - No other lines in database.ts changed (this is a targeted diff — add 2 lines, delete 2 lines).
    - Backend build + related tests green.
  </behavior>
  <action>
    In src/backend/database/database.ts:

    ADD (import section, adjacent to other route imports):
    - `import pushSubscriptionsRoutes from "./routes/push-subscriptions.js";`

    ADD (route-mount region, adjacent to other app.use calls):
    - `app.use("/push-subscriptions", pushSubscriptionsRoutes);`
    - Place this alphabetically among the mounts if the file has an ordering convention, or adjacent to the block where the /telegram mount lived (so the diff is visually paired: delete /telegram, add /push-subscriptions).

    DELETE:
    - Line 56 (or wherever the current line lives): `import telegramRoutes from "../telegram/routes.js";`
    - Line 1950 (or wherever it lives): `app.use("/telegram", telegramRoutes);`

    Do NOT touch any other route mounts, nginx-caveat comments, or shared middleware. The change is +2 lines / -2 lines (four-line diff total).

    Verify no other file in the codebase imports `telegramRoutes` from this file — grep `grep -rn "telegramRoutes" src/backend/` should show only zero (or only occurrences inside src/backend/telegram/ which are deleted in Plan 09).
  </action>
  <verify>
    <automated>npm run build:backend &amp;&amp; npm run build &amp;&amp; npx vitest related --run src/backend/database/database.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'app.use("/push-subscriptions"' src/backend/database/database.ts returns exactly 1.
    - grep -c 'pushSubscriptionsRoutes' src/backend/database/database.ts returns >= 2 (import + mount).
    - grep -c 'telegramRoutes' src/backend/database/database.ts returns 0 (both import + mount removed).
    - grep -c 'app.use("/telegram"' src/backend/database/database.ts returns 0.
    - grep -c 'from "../telegram/routes' src/backend/database/database.ts returns 0.
    - `npm run build:backend && npm run build` exits 0.
    - `npx vitest related --run src/backend/database/database.ts` exits 0.
  </acceptance_criteria>
  <done>Route mounted, /telegram gone; backend build + related tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Boot-time environment (VAPID env) → assertVapidConfigAtBoot | Fail-fast on missing/malformed = operator sees the issue |
| starter.ts void imports → push-trigger loop | Load failure is absorbed to warn, boot continues (best-effort discipline) |
| HTTP request lifecycle → /push-subscriptions route | Now reachable at runtime; route-level auth from Plan 05 gates access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-40 | Availability | VAPID missing at boot → server runs degraded without push | mitigate | assertVapidConfigAtBoot throws — server refuses to start; operator sees error and provides env vars |
| T-126-41 | Availability | push-trigger-starter dynamic-import fails silently | mitigate | Two-layer catch (S3) — outer catches import failure, inner catches startPushTriggerLoopOnBoot rejection; both log .warn with distinct operation strings for grep-ability |
| T-126-42 | Denial of Service | Orphaned telegram imports cause TS build failure | mitigate | Acceptance criteria greps for zero telegram imports after deletion; backend build gate confirms |
| T-126-43 | Elevation of Privilege | /telegram route still reachable after mount removed | mitigate | Route mount deleted from database.ts; nginx /telegram block remains until Plan 10 but proxies to a nothing (404 from Express); Plan 10 removes the nginx side + deletes the source code |
| T-126-44 | Repudiation | Route mount added but tests don't cover it | accept | Route-level tests live in Plan 05's push-subscriptions.test.ts; this plan's tests are structural (starter/database patch correctness), route behavior is Plan 05's coverage |
</threat_model>

<verification>
- starter.ts changes green
- database.ts changes green
- No orphaned telegram imports anywhere in the touched files
- Backend build clean
- Related tests pass
</verification>

<success_criteria>
A fresh backend boot: (1) throws if VAPID env missing; (2) mounts /push-subscriptions; (3) starts push-trigger loop for real users; (4) does NOT run any telegram-bridge boot code; (5) does NOT expose /telegram HTTP prefix from Express.
</success_criteria>

<output>
Create `.planning/phases/128-push-notifications-replacing-telegram-bridge/128-08-SUMMARY.md` when done.
</output>
