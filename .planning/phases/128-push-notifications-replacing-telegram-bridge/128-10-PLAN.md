---
phase: 126-push-notifications-replacing-telegram-bridge
plan: 10
type: execute
wave: 4
depends_on: [128-08]
files_modified:
  - docker/docker-compose.yml
  - docker/nginx.conf
  - docker/nginx-https.conf
autonomous: true
requirements: [D-17, D-18]
tags: [telegram-teardown, docker-compose, nginx, deploy-surface]

must_haves:
  truths:
    - "The tg-bridge service block (docker-compose.yml ~L194-216) is DELETED"
    - "The tg-bridge-state named volume block (docker-compose.yml ~L227-232) is DELETED"
    - "The tg-bridge build stanza (docker-compose.yml ~L26-31, if present as a separate stanza) is DELETED"
    - "The /telegram location block in docker/nginx.conf (~L252-262) is DELETED"
    - "The /telegram location block in docker/nginx-https.conf (~L263-273) is DELETED"
    - "The /push-subscriptions location blocks added by Plan 05 REMAIN in both nginx confs"
    - "docker-compose YAML is valid — `docker compose config` (or equivalent syntax-check) exits 0"
  artifacts:
    - path: "docker/docker-compose.yml"
      provides: "tg-bridge service + volume + build stanza REMOVED; other services unchanged"
    - path: "docker/nginx.conf"
      provides: "/telegram block removed; /push-subscriptions block (Plan 05) retained; other routing unchanged"
    - path: "docker/nginx-https.conf"
      provides: "/telegram block removed; /push-subscriptions block (Plan 05) retained; other routing unchanged"
  key_links:
    - from: "docker/docker-compose.yml"
      to: "the substrate/services/tg-bridge/Dockerfile.tg-bridge that no longer exists (Plan 09 deleted it)"
      via: "removing the compose build stanza that referenced it"
      pattern: "tg-bridge"
    - from: "docker/nginx.conf + nginx-https.conf"
      to: "backend :30001 Express router"
      via: "removing /telegram (mount already gone per Plan 08); preserving /push-subscriptions"
      pattern: "location.*(telegram|push-subscriptions)"
---

<objective>
The infrastructure-side half of the tg-bridge teardown: remove the Docker service + volume + build stanza + the two nginx `/telegram` location blocks. This is the disjoint-file counterpart to Plan 09 and runs in Wave 3 in parallel with it.

Purpose: D-17 (bridge leaves in the same shipping unit as push arrives — infrastructure side) and D-18 (teardown scope — infrastructure side). This is the surface the orchestrator's deploy step (out of executor scope) will act on when running `docker compose up --force-recreate skynet` — with tg-bridge gone from the compose file, the container disappears; the volume orphaned by this file change is removed by the orchestrator's separate `docker volume rm skynet_tg-bridge-state` step at deploy time.

Output: Three files modified — docker-compose.yml + both nginx confs — all deletions of tg-bridge / /telegram surfaces. The /push-subscriptions blocks added by Plan 05 are preserved. YAML remains valid.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-push-notifications-replacing-telegram-bridge/128-CONTEXT.md
@.planning/phases/128-push-notifications-replacing-telegram-bridge/128-RESEARCH.md
@.planning/phases/128-push-notifications-replacing-telegram-bridge/128-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete tg-bridge service + tg-bridge-state volume + build stanza from docker/docker-compose.yml</name>
  <files>docker/docker-compose.yml</files>
  <read_first>
    - docker/docker-compose.yml lines 20-40 (find the tg-bridge build stanza if it exists as a separate top-level `x-build` or similar around L26-31 per PATTERNS.md § Deletion Manifest)
    - docker/docker-compose.yml lines 190-220 (the `tg-bridge:` service definition at ~L194-216 per PATTERNS.md — includes image, build, depends_on, environment, volumes, restart policy)
    - docker/docker-compose.yml lines 225-240 (the `volumes:` section at the bottom, `tg-bridge-state:` volume declaration at ~L227-232)
    - .planning/phases/128-push-notifications-replacing-telegram-bridge/128-PATTERNS.md § Deletion Manifest (canonical enumeration of docker-compose.yml deletions) + §13 (also confirms compose changes)
    - .planning/phases/128-push-notifications-replacing-telegram-bridge/128-CONTEXT.md D-17 (one-motion shipping), D-18 (compose service in scope), D-20 (destructive drop of tg-bridge-state accepted)
  </read_first>
  <behavior>
    - The `services.tg-bridge:` block is removed entirely from docker-compose.yml.
    - The `volumes.tg-bridge-state:` block is removed entirely.
    - The tg-bridge build stanza (if present as a separate stanza at ~L26-31 per PATTERNS.md — verify by reading the file first) is removed.
    - No other services (skynet, caddy, matrix-synapse, whatever else is defined) are touched.
    - Volume references from OTHER services to tg-bridge-state (if any exist) are also removed — grep before deleting.
    - docker-compose.yml remains syntactically valid YAML. If `docker compose config` is available in the executor's environment, run it and confirm exit 0; if not available (executor is not on a docker-installed host), skip that check with a note in acceptance_criteria — the YAML validity is confirmed by lint or a hand-check (yaml.safe_load in a python one-liner also fine).
    - Environment variables that were tg-bridge-only (SKYNET_BRIDGE_TOKEN, SKYNET_BASE-for-bridge, MATRIX_ROOT-for-bridge as referenced by the tg-bridge environment block) disappear because their consumer disappears — no separate env-file surgery needed since they were declared inline in the tg-bridge service block.
  </behavior>
  <action>
    Read docker/docker-compose.yml (whole file) to understand its layout and confirm the exact line ranges (PATTERNS.md gives estimates; actual line numbers may drift).

    Locate the `tg-bridge:` service block under `services:`. It typically has the form:
    ```
      tg-bridge:
        build:
          context: ...
          dockerfile: substrate/services/tg-bridge/Dockerfile.tg-bridge
        image: tg-bridge:local
        depends_on:
          - skynet
        environment:
          - SKYNET_BASE=...
          - SKYNET_BRIDGE_TOKEN=...
          - MATRIX_ROOT=...
        volumes:
          - tg-bridge-state:/state
        restart: unless-stopped
    ```
    DELETE the entire block including its leading indentation and trailing blank line.

    Locate the `volumes:` section at the bottom of the file. DELETE the `tg-bridge-state:` entry (typically 1-3 lines depending on whether options are set).

    Grep the remaining file for any `tg-bridge` references (`grep -n "tg-bridge" docker/docker-compose.yml`). Expected result after deletion: zero hits. If any surviving references exist (e.g. a shared network alias, a depends_on from another service), DELETE those too.

    If a separate build stanza exists at ~L26-31 (per PATTERNS.md — possibly an x-build anchor or a variable-definition YAML anchor), delete that too. Not all compose files use anchors; verify by reading before deleting.

    Post-deletion: verify YAML validity. Preferred: `docker compose -f docker/docker-compose.yml config --quiet` exits 0. Fallback (if docker not available in executor env): run a python YAML parse check: `python3 -c "import yaml; yaml.safe_load(open('docker/docker-compose.yml'))"` exits 0.
  </action>
  <verify>
    <automated>grep -c "tg-bridge" docker/docker-compose.yml &amp;&amp; python3 -c "import yaml; yaml.safe_load(open('docker/docker-compose.yml'))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tg-bridge" docker/docker-compose.yml` returns 0.
    - `grep -c "tg-bridge-state" docker/docker-compose.yml` returns 0.
    - `grep -c "Dockerfile.tg-bridge" docker/docker-compose.yml` returns 0.
    - `grep -c "SKYNET_BRIDGE_TOKEN" docker/docker-compose.yml` returns 0 (the bridge-only env-var declaration is inside the deleted service block).
    - `python3 -c "import yaml; yaml.safe_load(open('docker/docker-compose.yml'))"` exits 0 (YAML is still parseable).
    - OTHER services still present: `grep -c "skynet:" docker/docker-compose.yml` returns >= 1 (the skynet service itself is preserved).
  </acceptance_criteria>
  <done>tg-bridge service + volume gone from compose; YAML valid; other services untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the /telegram location block from docker/nginx.conf AND docker/nginx-https.conf (dual-conf caveat)</name>
  <files>docker/nginx.conf, docker/nginx-https.conf</files>
  <read_first>
    - docker/nginx.conf lines 250-270 (the /telegram location block at ~L252-262 per PATTERNS.md § Deletion Manifest)
    - docker/nginx-https.conf lines 260-280 (the /telegram location block at ~L263-273)
    - .planning/phases/128-push-notifications-replacing-telegram-bridge/128-PATTERNS.md § Deletion Manifest (nginx blocks) + § S6 (dual-conf discipline — delete from BOTH)
    - .planning/phases/128-push-notifications-replacing-telegram-bridge/128-RESEARCH.md § Common Pitfalls § Pitfall 5 (dual-conf caveat — deletion must land in both to keep the two configs symmetric)
    - Confirm Plan 05's /push-subscriptions blocks are still present in BOTH files (`grep -c "push-subscriptions" docker/nginx.conf docker/nginx-https.conf` should each return >= 1 before this task starts — if not, Plan 05 hasn't run yet and this task should not run)
  </read_first>
  <behavior>
    - `location ~ ^/telegram(/.*)?$ { ... }` block is removed from docker/nginx.conf.
    - Same block is removed from docker/nginx-https.conf.
    - `/push-subscriptions` block from Plan 05 remains in BOTH files.
    - No other location blocks touched.
    - Both nginx conf files remain syntactically valid nginx configs (if `nginx -t` is available with an accessible config path, it's the strongest check; if not, a substring balance check for `{` and `}` counts remaining equal is a fallback).
  </behavior>
  <action>
    In docker/nginx.conf: locate the `location ~ ^/telegram(/.*)?$ { ... }` block at ~L252-262 (per PATTERNS.md; verify actual line range by reading). DELETE the whole block including its opening `location` line, all proxy_pass + proxy_set_header lines, and the closing `}`.

    In docker/nginx-https.conf: locate the equivalent /telegram block at ~L263-273. DELETE it the same way.

    Grep-verify preservation: `grep -c "location.*push-subscriptions" docker/nginx.conf` should still return 1, same for nginx-https.conf. The /push-subscriptions blocks Plan 05 added must survive this task untouched.

    Do NOT touch any other location blocks (/sw.js, /manifest.webmanifest, /api/*, /matrix/*, root fallback, etc.). Additive-inverse edit only.

    Nginx-syntax check (best-effort): if the executor's environment has `nginx` binary, run `nginx -t -c docker/nginx.conf` — this will complain about missing includes / paths that don't resolve at test-time, so it may exit non-zero for reasons unrelated to this diff. If it exits non-zero, do a manual balance check: `grep -c "^}" docker/nginx.conf` and `grep -c "^\\s*}" docker/nginx.conf` counts, and confirm the file structure is still recognizable. Perfect nginx syntax validation happens at deploy time (orchestrator's docker compose build reveals any real config errors).
  </action>
  <verify>
    <automated>grep -c "location.*telegram" docker/nginx.conf &amp;&amp; grep -c "location.*telegram" docker/nginx-https.conf &amp;&amp; grep -c "location.*push-subscriptions" docker/nginx.conf &amp;&amp; grep -c "location.*push-subscriptions" docker/nginx-https.conf</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "location.*telegram" docker/nginx.conf` returns 0.
    - `grep -c "location.*telegram" docker/nginx-https.conf` returns 0.
    - `grep -c "location.*push-subscriptions" docker/nginx.conf` returns exactly 1 (Plan 05 block preserved).
    - `grep -c "location.*push-subscriptions" docker/nginx-https.conf` returns exactly 1 (Plan 05 block preserved).
    - `grep -c "location.*/sw.js\\|location.*manifest" docker/nginx.conf` returns >= 1 (other blocks preserved).
    - `grep -c "location.*/sw.js\\|location.*manifest" docker/nginx-https.conf` returns >= 1.
  </acceptance_criteria>
  <done>Both nginx confs' /telegram blocks gone; /push-subscriptions preserved; other blocks untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docker-compose.yml → deploy-time container reconciliation | Removing a service from the compose file is destructive on next `docker compose up` |
| nginx config → HTTP routing surface | Both HTTP + HTTPS confs must stay symmetric (Pitfall 5) |
| YAML syntax | Malformed YAML would break the deploy entirely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-50 | Availability | Malformed docker-compose.yml breaks the deploy | mitigate | YAML parse check via python3 in acceptance_criteria; orchestrator's `docker compose config` at deploy time is the second gate |
| T-126-51 | Availability | Only one nginx conf touched → HTTPS + HTTP diverge | mitigate | Dual acceptance_criteria greps enforce zero-count in BOTH files |
| T-126-52 | Availability | /push-subscriptions block accidentally deleted alongside /telegram | mitigate | Acceptance_criteria greps enforce /push-subscriptions block remains in both files (>= 1 each) |
| T-126-53 | Repudiation | Volume tg-bridge-state removed from compose but the named volume persists on host | accept | Per D-20 and RESEARCH.md § Runtime State Inventory, host-side `docker volume rm skynet_tg-bridge-state` is an orchestrator deploy step, not this executor's remit |
| T-126-54 | Denial of Service | Other services depended_on tg-bridge (unlikely but possible) | mitigate | Task 1's `grep -c "tg-bridge"` returns 0 confirms no surviving reference anywhere in the compose file |
| T-126-55 | Availability | Dockerfile.tg-bridge referenced from compose after Plan 09 deleted the substrate directory | mitigate | Task 1's post-deletion grep on `Dockerfile.tg-bridge` returns 0; compose no longer tries to build the image |
</threat_model>

<verification>
- docker-compose.yml: no tg-bridge references, YAML valid
- Both nginx confs: no /telegram blocks, /push-subscriptions preserved
- No other services or blocks touched
</verification>

<success_criteria>
Post-deploy: `docker compose up` (orchestrator's step, not this executor) will not create or start tg-bridge; nginx routing at both HTTP and HTTPS layers no longer forwards /telegram; /push-subscriptions still reaches the backend.
</success_criteria>

<output>
Create `.planning/phases/128-push-notifications-replacing-telegram-bridge/128-10-SUMMARY.md` when done. Note in SUMMARY that the following orchestrator-scope steps are pending at deploy time (NOT executor's responsibility, but visible in the deploy runbook): `docker volume rm skynet_tg-bridge-state` (removes the orphaned named volume), `docker image prune -f` (removes the now-dangling tg-bridge:local image), operator-side BotFather cleanup (revoke Telegram bots if desired — external to Skynet, entirely the user's call per D-20).
</output>
