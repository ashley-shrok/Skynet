---
phase: 120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-
plan: 06
type: execute
wave: 3
depends_on:
  - 120-04
files_modified:
  - src/ui/shell/AppPane.tsx
  - src/ui/shell/tabUtils.tsx
  - src/ui/shell/tabUtils.test.tsx
autonomous: true
requirements:
  - D-03
  - D-04
  - D-05
  - D-19
  - D-20
  - D-21
must_haves:
  truths:
    - "A new `<AppPane>` component renders an `<iframe>` pointing at `/apps/${encodeURIComponent(hostId)}/${encodeURIComponent(slug)}/pane/`."
    - "The iframe has no `sandbox` attribute (fully-permissive per anti-patterns); `referrerPolicy=\"no-referrer\"` (D-20); `loading=\"eager\"`; `className=\"h-full w-full border-0\"`; `title={`App ${slug}`}`."
    - "`tabIcon` in `src/ui/shell/tabUtils.tsx` is refactored from a switch to `const TAB_ICONS: Record<TabType, React.ElementType>` lookup — six arms including `app: AppWindow`."
    - "`renderTabContent` in `src/ui/shell/tabUtils.tsx` is refactored from a switch to `const RENDERERS: Record<TabType, Renderer>` lookup — six entries; `rdp`/`vnc`/`telnet` become three explicit rows pointing at the same `renderGuacamoleTab` helper (replacing the fall-through pattern)."
    - "The new `app` renderer calls `isAppTab(tab)` narrowing before accessing `tab.app.hostId` and `tab.app.slug`; returns `null` on narrowing failure."
    - "Snapshot tests over the five existing kinds (`dashboard`, `terminal`, `rdp`, `vnc`, `telnet`) demonstrate byte-equivalent output before and after the refactor (D-21 client-dispatch layer test)."
    - "A new test asserts `<AppPane hostId={1} slug=\"todo\" tabId=\"t1\" isVisible={true}>` renders an iframe with `src=/apps/1/todo/pane/`."
  artifacts:
    - path: "src/ui/shell/AppPane.tsx"
      provides: "The iframe wrapper React component"
      exports: ["AppPane", "AppPaneProps"]
    - path: "src/ui/shell/tabUtils.tsx"
      provides: "Refactored dispatch: TAB_ICONS lookup + RENDERERS lookup + new app renderer"
      contains: "TAB_ICONS"
    - path: "src/ui/shell/tabUtils.test.tsx"
      provides: "Snapshot tests over five existing kinds + one new app-kind mount test"
  key_links:
    - from: "src/ui/shell/AppPane.tsx"
      to: "src/backend/apps/app-pane-router.ts (Plan 05)"
      via: "iframe.src points at the proxy route"
      pattern: "/apps/\\$\\{encodeURIComponent\\(hostId\\)\\}"
    - from: "src/ui/shell/tabUtils.tsx"
      to: "src/types/ui-types.ts (Plan 04)"
      via: "Record<TabType, ...> exhaustive-check on the six-arm union + isAppTab predicate"
      pattern: "Record<TabType"
---

<objective>
Land the client-side dispatch refactor (D-03 + D-04) + the new `<AppPane>` iframe wrapper (D-05) + the D-21 client-dispatch-layer tests. This plan turns the wave-1 type extension (Plan 04) into consumable UI: every existing `switch(tab.type)` in `tabUtils.tsx` becomes a `Record<TabType, ...>` lookup, the sixth arm renders the new `<AppPane>`, and byte-equivalent snapshot tests over the five existing kinds ensure the refactor is safe.

Purpose: This is the "small in-place unification" the shape file locked. The refactor is byte-equivalent for existing kinds — snapshot tests are the green gate. The new AppPane component establishes the pattern for "iframe as pane content" and is the surface the wave-2 backend router serves into.

Output: One new file (`AppPane.tsx`), two files modified (`tabUtils.tsx` + its test file — or a new test file if none exists).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-CONTEXT.md
@.planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-RESEARCH.md
@.planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-PATTERNS.md
@.planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-04-SUMMARY.md
@src/ui/shell/tabUtils.tsx
@src/types/ui-types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create `src/ui/shell/AppPane.tsx` iframe wrapper</name>
  <files>src/ui/shell/AppPane.tsx</files>
  <read_first>
    - src/ui/shell/tabUtils.tsx (lines 394-403 — the `GuacamoleApp` mount pattern inside a tab renderer, closest analog for "leaf-scoped mount that receives hostId/tabId/isVisible")
    - .planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-RESEARCH.md § Code Examples "AppPane iframe wrapper" (the full component excerpt including iframe attribute discipline)
    - .planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-RESEARCH.md § Anti-Patterns (sandbox anti-pattern rationale)
    - .planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-CONTEXT.md § D-05 (component role: iframe IS the entire in-pane view; Skynet draws no chrome inside the leaf beyond the leaf title bar)
    - .planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-CONTEXT.md § D-20 (no signal to the app — referrerpolicy discipline)
    - .planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-PATTERNS.md § AppPane.tsx (verbatim JSX shape + attribute discipline)
    - src/ui/features/pretty-conversations/AppTile.tsx (Phase 119 shipped tile — verify `encodeURIComponent` MEDIUM-1 fix pattern for defensive URL construction)
  </read_first>
  <behavior>
    - Test 1: `<AppPane hostId={1} slug="todo" tabId="t1" isVisible={true}>` renders exactly one `<iframe>` element.
    - Test 2: the iframe's `src` attribute is `/apps/1/todo/pane/` (encodeURIComponent applied to both hostId and slug, though `1` and `"todo"` are already safe).
    - Test 3: `<AppPane hostId={5} slug="my app" ...>` — slug is URL-encoded → `src` contains `/apps/5/my%20app/pane/`.
    - Test 4: `<AppPane hostId={5} slug="a/b" ...>` — slug forward-slash is URL-encoded → `src` contains `/apps/5/a%2Fb/pane/`.
    - Test 5: iframe has NO `sandbox` attribute (assert `iframe.getAttribute("sandbox") === null`).
    - Test 6: iframe has `referrerPolicy="no-referrer"` (assert exact string).
    - Test 7: iframe has `loading="eager"` (or the React attribute equivalent).
    - Test 8: iframe has `title` prop containing the slug (assert `title === `App ${slug}``).
    - Test 9: iframe has data attributes `data-app-hostid={hostId}`, `data-app-slug={slug}`, `data-tab-id={tabId}` for downstream test/debug identification.
    - Test 10: iframe has className `"h-full w-full border-0"` (Tailwind class shape matches PATTERNS.md excerpt).
  </behavior>
  <action>
    Create `src/ui/shell/AppPane.tsx` mirroring the verbatim JSX shape from `120-PATTERNS.md § AppPane.tsx` and `120-RESEARCH.md § Code Examples`:

    (a) Exported interface `AppPaneProps { hostId: number; slug: string; tabId: string; isVisible: boolean; }`. Do NOT add `onClose` or `onFocus` or any callback — the component receives no callbacks per D-05 (Skynet draws no chrome inside the leaf).

    (b) Exported function component `AppPane({ hostId, slug, tabId, isVisible }: AppPaneProps): React.ReactElement`. The body:
        i. Compute `const src = `/apps/${encodeURIComponent(hostId)}/${encodeURIComponent(slug)}/pane/`;` — the `encodeURIComponent` on hostId is defensive (mirrors the MEDIUM-1 fix pattern in `AppTile.tsx`; a number cast to string cannot contain URL-unsafe chars, but the consistency is worth it).
        ii. Return a single `<iframe>` with the exact set of attributes named in the behavior tests: `src={src}`, `title={`App ${slug}`}`, `referrerPolicy="no-referrer"`, `loading="eager"`, `className="h-full w-full border-0"`, `data-app-hostid={hostId}`, `data-app-slug={slug}`, `data-tab-id={tabId}`. Do NOT set `sandbox`. Do NOT set `allow` (no need to expose additional feature permissions). The `isVisible` prop is INTENTIONALLY UNUSED in this component (mirror the GuacamoleApp pattern where the parent Pane manages visibility via CSS `display:none` one layer up); a `// isVisible is honoured by the parent Pane one layer up — matches GuacamoleApp integration` comment above the JSX makes this explicit.

    (c) At the top of the file, import `React` (or `type { ReactElement }` if the codebase uses the newer conventions — verify from an existing shell component). Do NOT import from any Phase 120 file other than the type (no import from `AppTile.tsx`).

    (d) JSDoc block above the component naming D-05, D-19 (title comes from the app's static metadata via `tab.label` — the AppPane itself just reflects the slug in the iframe's `title` attribute), and D-20 (no signal to app — referrerPolicy).

    Do NOT wrap the iframe in Suspense (the iframe loads independently of React; Suspense adds no value here). Do NOT add a loading placeholder / spinner (No message streaming ever — fleet rule). Do NOT add a "app is loading" text (D-17 delegates all failure/loading states to Phase 103's interstitial via the proxy).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-vision &amp;&amp; SKYNET_COOKIE_DOMAIN=https://skynet.test npx vitest related --run src/ui/shell/AppPane.tsx 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `src/ui/shell/AppPane.tsx` exists and exports `AppPane` and `AppPaneProps`.
    - `grep -c 'encodeURIComponent(hostId)' src/ui/shell/AppPane.tsx` returns exactly 1.
    - `grep -c 'encodeURIComponent(slug)' src/ui/shell/AppPane.tsx` returns exactly 1.
    - `grep -c 'sandbox' src/ui/shell/AppPane.tsx` returns 0 (no sandbox attribute — anti-pattern per RESEARCH.md).
    - `grep -c 'referrerPolicy="no-referrer"' src/ui/shell/AppPane.tsx` returns exactly 1.
    - `grep -c 'loading="eager"' src/ui/shell/AppPane.tsx` returns exactly 1.
    - `grep -c 'data-app-hostid' src/ui/shell/AppPane.tsx` returns exactly 1.
    - `grep -c 'data-app-slug' src/ui/shell/AppPane.tsx` returns exactly 1.
    - `grep -c 'data-tab-id' src/ui/shell/AppPane.tsx` returns exactly 1.
    - `grep -c '/apps/\\$\\{encodeURIComponent(hostId)\\}/\\$\\{encodeURIComponent(slug)\\}/pane/' src/ui/shell/AppPane.tsx` returns exactly 1 (the exact src template).
    - No message-streaming primitives — `grep -c 'skeleton\\|spinner\\|Suspense\\|loading placeholder' src/ui/shell/AppPane.tsx` returns 0.
    - `tsc --noEmit` reports zero errors originating from `AppPane.tsx`.
  </acceptance_criteria>
  <done>
    The component compiles cleanly, the iframe attribute discipline matches RESEARCH.md verbatim, and no sandboxing / message-streaming / auxiliary chrome is introduced.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor `tabUtils.tsx` dispatch: `tabIcon` + `renderTabContent` to `Record<TabType, ...>` lookup tables + add `app` entries</name>
  <files>src/ui/shell/tabUtils.tsx</files>
  <read_first>
    - src/ui/shell/tabUtils.tsx (the ENTIRE file — read lines 1-410 minimum; specifically the two switches at lines 97-110 (`tabIcon`) and 318-405 (`renderTabContent`); note the fall-through pattern for `rdp`/`vnc`/`telnet` in the second switch)
    - src/types/ui-types.ts (as modified by Plan 04 — TabType six-arm + Tab.app + isAppTab)
    - src/ui/shell/AppPane.tsx (Task 1 output — the component this task's new renderer mounts)
    - .planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-PATTERNS.md § tabUtils.tsx (both refactor patterns + exhaustiveness note + test discipline)
    - .planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-CONTEXT.md § D-03 + D-04 (the exact lookup pattern + how `rdp`/`vnc`/`telnet` split into three explicit rows)
    - .planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-RESEARCH.md § Pattern 2 (dispatch table refactor + exhaustiveness guarantee)
  </read_first>
  <action>
    In `src/ui/shell/tabUtils.tsx`:

    (a) At the top of the file, add `AppWindow` to the existing `lucide-react` import block (or verify it's already imported — `grep -c 'AppWindow' src/ui/shell/tabUtils.tsx` may already return 1 if used elsewhere; if not, add it to the existing multi-name lucide import). Add `import { AppPane } from "./AppPane";` and `import { isAppTab } from "../../types/ui-types";` (verify the correct relative path from `tabUtils.tsx`'s location — `src/ui/shell/` to `src/types/` is `../../types/ui-types`).

    (b) **Refactor `tabIcon`** (per D-03; replace lines 97-110 or thereabouts): remove the switch statement. Above the function, declare `const TAB_ICONS: Record<TabType, React.ElementType> = { dashboard: LayoutDashboard, terminal: Terminal, rdp: Monitor, vnc: Monitor, telnet: Terminal, app: AppWindow };`. The function body reads `TAB_ICONS[type]` and returns `<Icon className="size-3.5" />` — see the verbatim excerpt in `120-PATTERNS.md § tabUtils.tsx`. The compile-time exhaustiveness is preserved via TypeScript's `Record<TabType, ...>` — if `TabType` grows, tsc surfaces missing entries.

    (c) **Refactor `renderTabContent`** (per D-04; replace lines 318-405 or thereabouts): DO NOT inline the full replacement in this action — verify the exact excerpt in `120-PATTERNS.md § tabUtils.tsx` before writing.

        Steps:
        i. Define a local `type RendererDeps = { ... }` capturing the closure of dependencies the current switch consumes: `onOpenSingletonTab`, `onOpenTab`, `onCloseTab`, `isVisible`, `shouldAttach`, `onTmuxSessionChange`, `onTmuxSessionMissing`. Match the current switch's parameter list exactly — verify by reading the current function signature.
        ii. Define `type Renderer = (tab: Tab, deps: RendererDeps) => React.ReactNode`.
        iii. Extract each existing switch case body into a module-scope arrow function `Renderer`:
            - `renderDashboard: Renderer = () => <PrettyLandingCard />;` (or whatever the current dashboard case renders).
            - `renderTerminalTab: Renderer = (tab, deps) => { ... }` — verbatim body from the current terminal case, threaded via `deps`.
            - `renderGuacamoleTab: Renderer = (tab, deps) => { ... }` — verbatim body from the current `rdp | vnc | telnet` fall-through, threaded via `deps`. The runtime type check on `tab.type` inside remains: `protocol={tab.type as "rdp" | "vnc" | "telnet"}` is fine.
            - `renderAppTab: Renderer = (tab, deps) => { if (!isAppTab(tab)) return null; return <AppPane hostId={tab.app.hostId} slug={tab.app.slug} tabId={tab.id} isVisible={deps.isVisible} />; };` — the `isAppTab` narrowing is defensive; upstream type-narrowing should prevent the null branch from firing.
        iv. Declare `const RENDERERS: Record<TabType, Renderer> = { dashboard: renderDashboard, terminal: renderTerminalTab, rdp: renderGuacamoleTab, vnc: renderGuacamoleTab, telnet: renderGuacamoleTab, app: renderAppTab };` — three explicit rows for `rdp`/`vnc`/`telnet` all pointing at the same helper (replacing the fall-through per D-04).
        v. The `renderTabContent` function body becomes: pack the passed arguments into a `RendererDeps` object then `return RENDERERS[tab.type](tab, deps);`. The function's signature (name, parameter list order + types) stays IDENTICAL to the current signature — this is byte-equivalent from every call site's perspective.

    (d) Do NOT reorder the existing five arms in either `TAB_ICONS` or `RENDERERS` — the new `app` entry is appended (matches the TabType order from Plan 04).

    (e) The refactor is byte-equivalent for existing kinds — the extracted renderer bodies must be VERBATIM COPIES of the current switch case bodies (including any imports, JSX shapes, prop wiring, and inline logic). The only structural change is dispatch shape (switch → lookup) — the OUTPUT for each existing kind must be identical before and after.

    Do NOT rename the exported function `renderTabContent` or `tabIcon`. Do NOT change their parameter lists. Do NOT change any prop wiring inside the extracted renderer bodies. Do NOT delete comments explaining the current dispatch logic — either preserve them near the appropriate renderer or lift them into JSDoc on `RENDERERS`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-vision &amp;&amp; SKYNET_COOKIE_DOMAIN=https://skynet.test npx vitest related --run src/ui/shell/tabUtils.tsx 2>&amp;1 | tail -30; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -c 'tabUtils.tsx' || echo 'tsc: no errors from tabUtils.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'TAB_ICONS: Record<TabType' src/ui/shell/tabUtils.tsx` returns exactly 1.
    - `grep -c 'RENDERERS: Record<TabType' src/ui/shell/tabUtils.tsx` returns exactly 1.
    - Both lookup tables contain all six arms — `grep -oE 'dashboard:|terminal:|rdp:|vnc:|telnet:|app:' src/ui/shell/tabUtils.tsx | sort -u | wc -l` returns 6.
    - `grep -c 'app: AppWindow' src/ui/shell/tabUtils.tsx` returns exactly 1 (in TAB_ICONS).
    - `grep -c 'app: renderAppTab' src/ui/shell/tabUtils.tsx` returns exactly 1 (in RENDERERS).
    - The `rdp`/`vnc`/`telnet` entries in RENDERERS all point at `renderGuacamoleTab` — `grep -oE '(rdp|vnc|telnet): renderGuacamoleTab' src/ui/shell/tabUtils.tsx | wc -l` returns 3 (three explicit rows per D-04 cleanup).
    - `grep -c 'switch (tab.type)\\|switch(tab.type)' src/ui/shell/tabUtils.tsx` returns 0 (the two switches are gone).
    - `grep -c 'switch (type)\\|switch(type)' src/ui/shell/tabUtils.tsx` returns 0 (the tabIcon switch is gone).
    - `grep -c 'isAppTab' src/ui/shell/tabUtils.tsx` returns at least 1 (used inside renderAppTab).
    - `grep -c 'from "./AppPane"' src/ui/shell/tabUtils.tsx` returns exactly 1 (the AppPane import).
    - `tsc --noEmit` reports zero errors originating from `tabUtils.tsx`.
    - No new external dependency added — the `AppWindow` icon comes from the existing `lucide-react` package that's already imported.
  </acceptance_criteria>
  <done>
    Both dispatch tables are `Record<TabType, ...>` lookups with all six arms, the three Guacamole protocols are three explicit rows, the new `app` renderer uses `isAppTab` narrowing to mount `<AppPane>`, tsc compiles clean, and no external deps were added.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add snapshot tests for the five existing kinds + a mount test for the new `app` kind (D-21 client-dispatch layer)</name>
  <files>src/ui/shell/tabUtils.test.tsx</files>
  <read_first>
    - src/ui/shell/tabUtils.tsx (as modified by Task 2 — the refactored dispatch tables)
    - src/ui/shell/AppPane.tsx (Task 1 output — the target of the app-kind mount test)
    - src/ui/features/pretty-conversations/AppTile.test.tsx (existing frontend test scaffold — `vi.mock` + `render` + `screen` pattern that this test file mirrors)
    - .planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-CONTEXT.md § D-21 (client dispatch layer test requirements: snapshot each existing kind before/after; add one new test for the app kind)
    - .planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-PATTERNS.md § tabUtils.tsx test discipline note (byte-equivalent for existing kinds; snapshot test as green gate)
  </read_first>
  <behavior>
    - Test 1: `tabIcon("dashboard")` renders a `<LayoutDashboard>` element with `className="size-3.5"` (snapshot).
    - Test 2: `tabIcon("terminal")` renders a `<Terminal>` element with `className="size-3.5"` (snapshot).
    - Test 3: `tabIcon("rdp")` renders a `<Monitor>` element with `className="size-3.5"` (snapshot).
    - Test 4: `tabIcon("vnc")` renders a `<Monitor>` element with `className="size-3.5"` (snapshot).
    - Test 5: `tabIcon("telnet")` renders a `<Terminal>` element with `className="size-3.5"` (snapshot).
    - Test 6: `tabIcon("app")` renders an `<AppWindow>` element with `className="size-3.5"` (new).
    - Test 7: `renderTabContent({ type: "dashboard", ... }, ...)` renders the expected dashboard component (snapshot — verify against the current output).
    - Test 8: `renderTabContent({ type: "terminal", host: mockHost, ... }, ...)` renders the terminal tab content (snapshot).
    - Test 9: `renderTabContent({ type: "rdp", host: mockHost, ... }, ...)` renders the Guacamole wrapper with `protocol="rdp"` (snapshot).
    - Test 10: `renderTabContent({ type: "vnc", host: mockHost, ... }, ...)` renders the Guacamole wrapper with `protocol="vnc"` (snapshot).
    - Test 11: `renderTabContent({ type: "telnet", host: mockHost, ... }, ...)` renders the Guacamole wrapper with `protocol="telnet"` (snapshot).
    - Test 12 (new — D-21): `renderTabContent({ type: "app", app: { hostId: 1, slug: "todo" }, id: "t1", label: "Todo", ... }, ..., isVisible=true, ...)` renders exactly one `<iframe>` element with `src="/apps/1/todo/pane/"`.
    - Test 13 (defensive): `renderTabContent({ type: "app", app: undefined, ... }, ...)` returns `null` (isAppTab predicate fires).
    - Test 14 (exhaustiveness): the number of entries in `TAB_ICONS` and `RENDERERS` equals `6` (assert via `Object.keys(...).length`).
  </behavior>
  <action>
    Create or extend `src/ui/shell/tabUtils.test.tsx`:

    (a) If the file already exists: read it fully, understand the existing test structure and mocks, and add the new tests as additional `it`s in the existing `describe` blocks (or new `describe` blocks alongside).

    (b) If the file does NOT exist: create it with the vi.hoisted + vi.mock + `render` + `screen` scaffolding pattern from `src/ui/features/pretty-conversations/AppTile.test.tsx`. Mock the heavy React components that get mounted (`GuacamoleApp`, `TerminalOrIdentitySessionPane`, `PrettyLandingCard`) as `vi.fn(() => <div data-testid="...">...</div>)` stubs so the tests focus on the DISPATCH shape, not the mounted component internals. Mock `AppPane` as `vi.fn(({hostId, slug, tabId, isVisible}) => <iframe data-testid="app-pane-mock" data-hostid={hostId} data-slug={slug} data-tabid={tabId} data-visible={isVisible} src={`/apps/${hostId}/${slug}/pane/`} />)` — the mock renders a real iframe with the same `src` so the app-mount test can assert on src.

    (c) Build a `mockDeps: RendererDeps` factory that returns a fully-populated deps object with `vi.fn()` for each callback and defaults `isVisible: true, shouldAttach: false`.

    (d) Snapshot approach: use vitest's `expect(...).toMatchInlineSnapshot()` for the five existing kinds — this captures the CURRENT rendered output (post-refactor). Because the refactor is byte-equivalent, if any snapshot deviates from expected DOM shape, the test fails immediately and the executor knows the refactor broke something. Do NOT commit stale snapshots.

    (e) The app-kind mount test (Test 12) asserts on the mocked `<AppPane>` invocation: assert `AppPane` was called with `expect.objectContaining({ hostId: 1, slug: "todo", tabId: "t1", isVisible: true })`. Also assert `screen.getByTestId("app-pane-mock").getAttribute("src") === "/apps/1/todo/pane/"`.

    (f) The exhaustiveness test (Test 14) imports (or accesses via the module) the two lookup tables — if they are not exported, the executor MAY export `TAB_ICONS` and `RENDERERS` as named module exports (planner discretion — a small ambient-typed re-export is safer than adding a test-only hook). Alternatively, assert exhaustiveness indirectly by running `tabIcon(...)` and `renderTabContent(...)` for all six kinds and asserting each returns a non-null value.

    (g) Do NOT test the internal implementation of `<AppPane>` here — that's Task 1's test. This test file tests DISPATCH, not the target components.

    Do NOT run full component tests (rendering GuacamoleApp for real would pull in the whole Guacamole SDK — mock it). Do NOT rely on file-based snapshots that live outside the test file (use inline snapshots for stability and reviewability).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-vision &amp;&amp; SKYNET_COOKIE_DOMAIN=https://skynet.test npx vitest related --run src/ui/shell/tabUtils.tsx src/ui/shell/tabUtils.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/ui/shell/tabUtils.test.tsx` exists.
    - At least 14 test cases execute — `grep -c "^\\s*it(" src/ui/shell/tabUtils.test.tsx` returns at least 14.
    - The app-mount test asserts on `src="/apps/1/todo/pane/"` — `grep -c '/apps/1/todo/pane/' src/ui/shell/tabUtils.test.tsx` returns at least 1.
    - The defensive test for `isAppTab` narrowing (Test 13) exists — `grep -c 'isAppTab\\|app: undefined' src/ui/shell/tabUtils.test.tsx` returns at least 1.
    - All six tab kinds appear in test names or scenarios — `grep -oE '"dashboard"|"terminal"|"rdp"|"vnc"|"telnet"|"app"' src/ui/shell/tabUtils.test.tsx | sort -u | wc -l` returns 6.
    - Snapshots use inline form — `grep -c 'toMatchInlineSnapshot' src/ui/shell/tabUtils.test.tsx` returns at least 5 (one per existing kind).
    - The scoped vitest run reports all tests passing (exit 0).
    - The refactor is byte-equivalent for existing kinds: if the executor introduced any DOM shape drift in Task 2, this suite fails and the executor MUST fix Task 2 (not merely regenerate snapshots).
  </acceptance_criteria>
  <done>
    All 14 tests pass. Snapshot equivalence is asserted for the five existing kinds. The new app kind mounts `<AppPane>` with the correct props. Exhaustiveness on the six-arm dispatch tables is asserted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skynet pane → iframe (same-origin) | Iframe inherits Skynet's session cookie; app-side XSS lands in Skynet origin (accepted trade-off per shape file philosophy). |
| Iframe src URL construction | User-controlled `slug` from `Tab.app` flows into `iframe.src` via `encodeURIComponent`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-32 | Tampering | Slug injection into iframe.src (e.g. `slug="../../evil"`) | mitigate | `encodeURIComponent(slug)` unconditionally applied in `AppPane.tsx`. `../` becomes `%2E%2E%2F` — cannot break out of the URL path segment. Test 3 (spaces) + Test 4 (forward-slash) exercise. Defence-in-depth: the proxy's `APP_SLUG_RE` refuses malformed slugs at the backend. |
| T-120-33 | Information disclosure | Referer header leaking Skynet page identity to the app | mitigate | `referrerPolicy="no-referrer"` on the iframe — matches D-20 "no signal to the app". Test 6 asserts the exact string. |
| T-120-34 | Elevation of privilege | Sandbox misconfiguration allowing embedded content to access parent | accept | Sandbox INTENTIONALLY unset — the app is same-origin + fully authenticated + trusted at the pane level per shape file philosophy. This is the accepted trade-off. If a future attack surface justifies revisiting, it becomes a deferred idea. |
| T-120-35 | Denial of service | Malicious app crashes iframe → parent React tree unaffected | mitigate | iframe is a separate document — its crash does not propagate to the React tree. No new mitigation needed; browser DOM isolation covers this. |
| T-120-36 | Tampering | Byte-drift in refactor changes existing tab-kind output | mitigate | Snapshot tests over five existing kinds catch any DOM output drift. If snapshots differ, Task 2 is incorrect and must be fixed (NOT regenerate snapshots to make them pass — that would be a test-antipattern). |
| T-120-37 | Information disclosure | Third-party page embeds Skynet iframe → clickjacks the app inside | mitigate | Skynet's edge `X-Frame-Options: SAMEORIGIN` (or CSP `frame-ancestors 'self'`) covers this at the response layer — verified at Plan 05 execution time (Assumption A2). If not present, add response header at Plan 05. |
| T-120-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed. `AppWindow` icon is from `lucide-react` already in the lockfile. |
</threat_model>

<verification>
Plan 04's tsc-error surface (from the unresolved `switch(tab.type)` in tabUtils.tsx) is closed by Task 2's refactor. The scoped vitest run at Task 3 exercises all 14 tests green. `grep`-based structural assertions confirm all six arms in both dispatch tables and no residual switch statements.
</verification>

<success_criteria>
- Wave 4 (Plan 07 AppTile + SplitView + AppShell wiring) can call `openTab(null, "app", ..., { app: { hostId, slug } })` and the resulting Tab renders through the new `<AppPane>` iframe when placed in a leaf.
- No changes to `<GuacamoleApp>` or `<PrettyLandingCard>` or `<TerminalOrIdentitySessionPane>` — those are read-only.
- Byte-equivalent rendering for the five existing kinds is asserted (snapshot tests).
</success_criteria>

<output>
Create `.planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-06-SUMMARY.md` when done, listing:
- The lines removed (the two switches) and the lines added (the two lookup tables + the new renderers + the AppPane component).
- Confirmation that snapshot tests over the five existing kinds pass unchanged (byte-equivalence proof).
- The exhaustiveness guarantee (six entries in each Record) demonstrated by test 14.
- Any tsc surface still open from Plan 04 (should be zero after Task 2).
</output>
