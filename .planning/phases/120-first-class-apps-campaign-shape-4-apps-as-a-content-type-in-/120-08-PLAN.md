---
phase: 120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-
plan: 08
type: execute
wave: 6
depends_on:
  - 120-05
  - 120-07
files_modified:
  - substrate/skills/app-development/templates/app-starter/svelte.config.js
autonomous: true
requirements:
  - D-14
  - D-23
  - D-22
must_haves:
  truths:
    - "The `csrf.checkOrigin: false` LINE ITSELF is UNCHANGED in the starter template (Pitfall 4 — removing it breaks proxied POSTs)."
    - "The comment above `csrf: { checkOrigin: false }` in the starter template is UPDATED to state 'the check is off BECAUSE Skynet's reverse-proxy at /apps/:hostId/:slug/pane/* enforces same-origin at its boundary, not as a shortcut', with a reference to `src/backend/apps/app-proxy-csrf-check.ts`."
    - "The comment additionally notes that apps served directly (bypassing the pane proxy — e.g. the `.serve.` fresh-tab affordance) rely on the tailnet perimeter + Skynet's edge auth."
    - "The D-23 UAT procedure is DOCUMENTED (in this plan's SUMMARY.md at plan close) — NOT executed here per campaign-close deploy-hold policy. UAT execution defers to campaign close."
    - "Nothing else in this plan issues `git push`, `docker build`, or `docker compose up`. Executor scope is committing the starter template comment change to the held branch only."
  artifacts:
    - path: "substrate/skills/app-development/templates/app-starter/svelte.config.js"
      provides: "Updated comment explaining proxy-enforced same-origin check discipline"
      contains: "app-proxy-csrf-check.ts"
  key_links:
    - from: "substrate/skills/app-development/templates/app-starter/svelte.config.js"
      to: "src/backend/apps/app-proxy-csrf-check.ts (Plan 01)"
      via: "The comment references the file path of the proxy-side enforcement"
      pattern: "app-proxy-csrf-check"
---

<objective>
Land the D-14 starter-template comment update (the ONLY substrate-side change in Phase 120) and document the D-23 UAT procedure for execution at campaign close (per the campaign hold policy). The `csrf.checkOrigin: false` LINE stays disabled — only the comment above it changes to point at the proxy as the enforcement site. No `git push`, no `docker build`, no substrate distributor sweep at this executor scope — those are campaign-close orchestrator-only motions.

Purpose: With the comment update, future app authors reading the starter template understand WHY the check is disabled (proxy enforces it) and where the enforcement lives (`src/backend/apps/app-proxy-csrf-check.ts`). Without this update, a future maintainer might delete the disable line thinking it was a shortcut and break every proxied POST.

Output: One file modified (a comment update). One documented UAT procedure written into the plan's SUMMARY.md for campaign-close use.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-CONTEXT.md
@.planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-RESEARCH.md
@.planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-PATTERNS.md
@substrate/skills/app-development/templates/app-starter/svelte.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the starter template's `svelte.config.js` comment above `csrf.checkOrigin: false`</name>
  <files>substrate/skills/app-development/templates/app-starter/svelte.config.js</files>
  <read_first>
    - substrate/skills/app-development/templates/app-starter/svelte.config.js (the ENTIRE file — it's ~19 lines; find the current comment block at approximately lines 9-12; the `csrf: { checkOrigin: false }` block underneath)
    - .planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-CONTEXT.md § D-14 (verbatim wording is Claude's discretion; intent is "check is off BECAUSE proxy enforces it, not as shortcut")
    - .planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-PATTERNS.md § svelte.config.js (verbatim shape of the updated comment)
    - .planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-RESEARCH.md § Common Pitfalls Pitfall 4 (CRITICAL: the `csrf.checkOrigin: false` LINE ITSELF stays; only the comment changes)
    - .planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-RESEARCH.md § Code Examples "D-14 comment update" (a working-draft wording — planner picks the exact final wording)
  </read_first>
  <action>
    Read the current file. Locate the comment block above `csrf: { checkOrigin: false }` (approximately lines 9-12 per PATTERNS.md — verify by reading).

    Replace the current comment with a new multi-line comment that captures ALL of the following load-bearing points:
    1. Skynet's reverse-proxy at `/apps/:hostId/:slug/pane/*` enforces the same-origin (CSRF) check at its boundary.
    2. The enforcement site is `src/backend/apps/app-proxy-csrf-check.ts` in the Skynet repo (Plan 01 output).
    3. The built-in SvelteKit `checkOrigin` check stays disabled because a proxied POST arrives at 127.0.0.1:PORT with an Origin header naming Skynet's own domain (not `127.0.0.1:PORT`), and the default check would refuse it. This is NOT a shortcut — it's the load-bearing reason (per Pitfall 4).
    4. Apps served directly (bypassing the pane proxy — e.g. the `.serve.` per-port URL for a fresh-tab open) rely on the tailnet perimeter plus Skynet's edge auth.

    The comment style matches the surrounding SvelteKit `svelte.config.js` conventions (`/** ... */` JSDoc or `//` line comments — verify by reading the current file's comment style). Match the current indentation (nested inside the `kit: { ... }` object, so likely 8-space or 4-space indent — verify).

    The exact wording is Claude's discretion per D-14 — pick prose that reads naturally in this file. The RESEARCH.md § Code Examples has a working draft; use it as a starting point but adjust to match the file's existing comment style. Include the phrase "see src/backend/apps/app-proxy-csrf-check.ts" or similar so future maintainers can find the enforcement site.

    Do NOT remove the `csrf: { checkOrigin: false }` line — this is the LOAD-BEARING rule per Pitfall 4. Do NOT change any other line in `svelte.config.js` (adapter import, preprocess line, export statement all stay untouched). Do NOT add a `paths.base` or any other kit-config option (out of scope per D-11 resolution + Pitfall 6). Do NOT add a README.md file separately — this task's scope is limited to the one config file.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-vision &amp;&amp; grep -c 'app-proxy-csrf-check' substrate/skills/app-development/templates/app-starter/svelte.config.js &amp;&amp; grep -c 'checkOrigin: false' substrate/skills/app-development/templates/app-starter/svelte.config.js &amp;&amp; node --check substrate/skills/app-development/templates/app-starter/svelte.config.js 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'checkOrigin: false' substrate/skills/app-development/templates/app-starter/svelte.config.js` returns exactly 1 (line preserved).
    - `grep -c 'app-proxy-csrf-check' substrate/skills/app-development/templates/app-starter/svelte.config.js` returns at least 1 (comment references the file).
    - `grep -c '/apps/:hostId/:slug/pane\\|apps/.*/pane' substrate/skills/app-development/templates/app-starter/svelte.config.js` returns at least 1 (comment mentions the proxy path).
    - `grep -c 'tailnet\\|edge auth' substrate/skills/app-development/templates/app-starter/svelte.config.js` returns at least 1 (comment mentions the fresh-tab fallback trust source).
    - The file parses as valid JavaScript — `node --check substrate/skills/app-development/templates/app-starter/svelte.config.js` exits 0.
    - No changes outside the comment block — `git diff substrate/skills/app-development/templates/app-starter/svelte.config.js` shows changes ONLY inside the comment block above `csrf: { checkOrigin: false }` (executor verifies via `git diff --stat` and `git diff` inspection).
    - No new dependencies added (`import` statements unchanged).
  </acceptance_criteria>
  <done>
    The comment update lands, `checkOrigin: false` line stays, the file parses valid, and no other lines in the file changed. The reason for the disabled check is now discoverable from the file itself.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document the D-23 UAT procedure for campaign-close execution</name>
  <files>.planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-08-SUMMARY.md</files>
  <read_first>
    - .planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-CONTEXT.md § D-23 (the full UAT procedure: create scratch app, exercise gestures, verify persistence, exercise failure paths, cleanup)
    - .planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-CONTEXT.md § campaign hold policy (UAT converges at campaign close, NOT this phase's individual close)
    - .planning/campaigns/first-class-apps/campaign-first-class-apps.md (campaign artifact — cross-shape sequencing + campaign deploy hold)
    - .planning/phases/119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c/119-CONTEXT.md (prior phase UAT-defer policy pattern for reference)
  </read_first>
  <action>
    Write `.planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-08-SUMMARY.md` containing (in addition to the standard SUMMARY sections):

    A section titled `## D-23 UAT Procedure (defer to campaign close)` documenting the exact step sequence per CONTEXT.md § D-23:

    1. **Setup on t1000:** Create scratch app folder at `~/fleet/apps/scratch-shape-4-test/` with a real `app.json` (title, slug, port), a real systemd `--user` unit, and real serving content (a minimal SvelteKit starter or Express server with at least one form POST route + one GET route).
    2. **Trigger sweep:** Ensure Phase 118's fleet-status sweep picks up the new app frame (wait one sweep interval or trigger manually).
    3. **Click gesture (D-06):** Open Skynet UI in a browser session logged in as the same user that owns the app's home box. Locate the new app tile in the sidebar Apps section. Click it. Verify: a new pane leaf appears with the app rendering inside via the iframe; the leaf title bar shows the app's static metadata title.
    4. **Form POST (D-13 CSRF pass path):** Inside the app's leaf, submit a form POST. Verify: the request succeeds (200 OK); NO 403 CSRF-failure body. This validates that the Origin header matches Skynet's `PRIMARY_DOMAIN`.
    5. **Drag gesture (D-07):** Drag the same tile from the sidebar into a split edge (left/right/top/bottom). Verify: a new leaf appears at that edge with a second instance of the same app rendering (D-15 multi-instance).
    6. **Internal navigation (D-11 base-tag injection):** Inside leaf 1, click a link that navigates to a sub-path within the app (e.g. `/about` inside the app). Verify: the URL bar stays on Skynet's origin (proxying works); the sub-page renders via the base-tag-injected absolute-path resolution.
    7. **Reload persistence (D-16):** Reload the whole browser page. Verify: both app leaves restore automatically; both iframes reconnect and render.
    8. **App stopped (D-17 interstitial):** On t1000, stop the scratch app's systemd unit (`systemctl --user stop <unit>`). Refresh both leaves. Verify: existing leaves show Phase 103's tunnel-error interstitial; opening a new leaf via a fresh click shows the same interstitial state.
    9. **App deleted (folder removed):** Delete the app folder + `systemctl --user stop <unit>` + `systemctl --user disable <unit>` + `systemctl --user daemon-reload`. Verify: on the next sweep, the tile disappears from the sidebar; existing leaves show the connection-refused state.
    10. **Cleanup:** Confirm scratch folder is gone; systemd user unit is gone; no residual app-frame entry in fleet-status.

    Additional documentation in the SUMMARY:

    - A section titled `## Deploy hold` reiterating that Phase 120's commits stack on Phase 119's held commits on branch `feat/tab-title-from-tmux`. NO `git push`, NO `docker build`, NO `docker compose up --force-recreate` at this executor scope. Campaign-close orchestrator runs the deploy + substrate distributor sweep after all four shapes' UAT converges.
    - A section titled `## Substrate distributor sweep note` noting that if `substrate/skills/app-development/templates/app-starter/svelte.config.js`'s byte count or line count materially changed, the campaign-close deploy step must update the distributor catalog row-count entry for this file (mirror how Phase 116 handled that). Include the current file's line count via a note (executor runs `wc -l substrate/skills/app-development/templates/app-starter/svelte.config.js` at plan close and records both pre- and post-edit counts).

    Do NOT execute the UAT procedure. Do NOT push commits. Do NOT run docker or systemd commands. Do NOT create scratch apps at plan-execution time — the entire D-23 procedure is a campaign-close artifact.
  </action>
  <verify>
    <automated>test -f /home/ubuntu/skynet-vision/.planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-08-SUMMARY.md &amp;&amp; grep -c 'D-23 UAT Procedure' /home/ubuntu/skynet-vision/.planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-08-SUMMARY.md &amp;&amp; grep -c 'Deploy hold' /home/ubuntu/skynet-vision/.planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-08-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `120-08-SUMMARY.md` exists in the phase directory.
    - `grep -c 'D-23 UAT Procedure' 120-08-SUMMARY.md` returns at least 1.
    - `grep -c 'Deploy hold' 120-08-SUMMARY.md` returns at least 1.
    - `grep -c 'campaign close\\|campaign-close' 120-08-SUMMARY.md` returns at least 1.
    - `grep -c 'no git push\\|NO git push\\|No git push' 120-08-SUMMARY.md` returns at least 1 (explicit statement of the hold).
    - The ten UAT steps are all present — `grep -oE '^\\s*[0-9]+\\.' 120-08-SUMMARY.md | wc -l` returns at least 10.
    - The substrate distributor sweep note is present — `grep -c 'distributor catalog\\|distributor sweep' 120-08-SUMMARY.md` returns at least 1.
    - No commands were actually executed against t1000 or the container fleet (verify: no evidence in git history of scratch-shape-4-test folder existing).
  </acceptance_criteria>
  <done>
    The UAT procedure is fully documented for later use at campaign close. The deploy hold and distributor sweep policies are recorded. No fleet-mutation commands were executed at this executor scope.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Starter template (substrate) → future apps (built from template) | The comment steers future authors away from removing the disable line; there is no runtime attack surface introduced by a comment change. |
| Deploy hold → production | No production changes at this executor scope; campaign-close orchestrator manages the deploy boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-44 | Tampering | Future maintainer removes `csrf.checkOrigin: false` misreading the comment | mitigate | Comment explicitly states "the check is off BECAUSE the proxy enforces it, not as a shortcut" and references the enforcement file. Pitfall 4 explanation embedded directly in the comment. |
| T-120-45 | Elevation of privilege | Deploy motion runs before all four shapes' UAT converges | mitigate | Deploy hold explicitly reiterated in this plan's SUMMARY.md; orchestrator's pre-deploy checklist references this. Executor scope explicitly excludes deploy motions. |
| T-120-46 | Information disclosure | UAT procedure documentation reveals internal file paths + component names | accept | The SUMMARY.md is internal planning artifact; no external exposure. Documentation is intended to be discoverable by future planners. |
| T-120-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed. |
</threat_model>

<verification>
- The starter template comment update lands with the `csrf.checkOrigin: false` line preserved.
- The file parses as valid JavaScript.
- The D-23 UAT procedure is documented in the SUMMARY.md with all ten steps.
- No deploy commands executed.
- No fleet-mutation commands executed.
- The commit stacks locally on branch `feat/tab-title-from-tmux` per campaign hold policy.
</verification>

<success_criteria>
- Future app authors reading `svelte.config.js` in the starter template understand WHY the CSRF check is disabled and WHERE the enforcement lives.
- The D-23 UAT procedure is available for the campaign-close orchestrator to execute.
- The campaign hold is preserved: NO git push, NO docker build, NO docker compose up.
- The substrate distributor sweep note captures the file-count change (if any) so the campaign-close deploy step can update the distributor catalog row-count.
</success_criteria>

<output>
Create `.planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-08-SUMMARY.md` when done, containing:
- The exact updated comment content that landed in `svelte.config.js` (verbatim).
- The pre- and post-edit line counts of `svelte.config.js` (for the distributor catalog update at campaign close).
- The full 10-step D-23 UAT procedure (per Task 2 action).
- The deploy hold reiteration + substrate distributor sweep note.
- Cross-reference to Plan 05 SUMMARY's Caddyfile-forward assumption verification (if it flagged a gap, this SUMMARY should acknowledge and defer to campaign-close orchestrator).
</output>
