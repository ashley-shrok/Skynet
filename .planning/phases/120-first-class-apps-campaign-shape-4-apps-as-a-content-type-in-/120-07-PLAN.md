---
phase: 120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-
plan: 07
type: execute
wave: 4
depends_on:
  - 120-03
  - 120-04
  - 120-06
files_modified:
  - src/ui/features/pretty-conversations/AppTile.tsx
  - src/ui/features/pretty-conversations/AppTile.test.tsx
  - src/ui/features/pretty-conversations/pretty-conversations.css
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/shell/SplitView.tsx
  - src/ui/AppShell.tsx
autonomous: true
requirements:
  - D-06
  - D-07
  - D-15
  - D-16
  - D-18
  - D-19
  - D-21
must_haves:
  truths:
    - "Left-click on an `AppTile` calls `onOpenApp(hostId, slug, title)` which resolves to `openTab(null, \"app\", ..., { app: { hostId, slug }, label: title })` (D-06)."
    - "Left-click on an unhealthy tile still calls `onOpenApp` — no health gate on the click handler (D-18)."
    - "`AppTile` cursor style is `cursor: pointer` (removes Phase 119's `cursor: default` override — D-06)."
    - "Dragging an `AppTile` sets `dataTransfer.setData(\"application/x-skynet-app-tile\", JSON.stringify({ hostId, slug, title }))` and effectAllowed=copy (D-07)."
    - "Dragging an `AppTile` does NOT set `text/plain` (Phase 64 closure — RESEARCH.md anti-pattern)."
    - "`SplitView.hasSkynetDragPayload` gate accepts the third MIME `application/x-skynet-app-tile`."
    - "`SplitView`'s onDrop dispatch has a new branch reading `application/x-skynet-app-tile` and calling a new callback `onDropAppTileInTree(payload, path, edge)`."
    - "`AppShell`'s `openTab` accepts an `app?: { hostId: number; slug: string }` option; when provided the created Tab carries it (D-02); when `PERSISTENT_TAB_TYPES.includes(\"app\")` the persist body sends `appSlug` (D-16)."
    - "`AppShell`'s `restoredTabs` block reconstructs `Tab.app` from `saved.appSlug + saved.hostId` when `saved.tabType === \"app\"` (D-16)."
    - "`AppShell` wires `onDropAppTileInTree` to `openTab(null, \"app\", ...)` + `insertLeafAtEdge` (mirror of `onDropRowInTree`)."
    - "URL-fragment round-trip: `specForTab` emits `{ protocol: \"app\", hostId, slug }` for app tabs; `parseTabParam` reconstructs Tab.app on restore (D-16 via Plan 04 TabSpec extension)."
    - "D-21 client-sidebar-wiring layer tests exercise the click handler + drag-payload emit + drop dispatch."
  artifacts:
    - path: "src/ui/features/pretty-conversations/AppTile.tsx"
      provides: "onClick + drag handlers + updated cursor style"
      contains: "onOpenApp"
    - path: "src/ui/shell/SplitView.tsx"
      provides: "hasSkynetDragPayload accepts third MIME + onDrop dispatch reads app-tile payload"
      contains: "application/x-skynet-app-tile"
    - path: "src/ui/AppShell.tsx"
      provides: "openTab accepts app tuple; restoredTabs reconstructs Tab.app; onDropAppTileInTree callback + AppTile prop wiring"
      contains: "onDropAppTileInTree"
    - path: "src/ui/features/pretty-conversations/AppTile.test.tsx"
      provides: "D-21 client-sidebar-wiring tests: click handler + drag payload emit"
  key_links:
    - from: "src/ui/features/pretty-conversations/AppTile.tsx"
      to: "src/ui/AppShell.tsx"
      via: "onOpenApp prop wired through PrettyConversationsPanel"
      pattern: "onOpenApp"
    - from: "src/ui/shell/SplitView.tsx"
      to: "src/ui/AppShell.tsx"
      via: "onDropAppTileInTree callback prop"
      pattern: "onDropAppTileInTree"
    - from: "src/ui/AppShell.tsx"
      to: "src/backend/database/routes/open-tabs.ts (Plan 03)"
      via: "addOpenTab persists appSlug + hostId"
      pattern: "appSlug"
    - from: "src/ui/AppShell.tsx"
      to: "src/ui/lib/tab-url.ts (Plan 04)"
      via: "URL-fragment round-trip via specForTab / parseTabParam"
      pattern: "specForTab\\|parseTabParam"
---

<objective>
Wire the two shape-4 gestures (D-06 left-click, D-07 drag) end-to-end from the sidebar tile through the pane's split-drop machinery to the AppShell's `openTab` factory and persistence layer, so that clicking or dragging an app tile actually creates an app leaf in the pane. Cover the D-21 client-sidebar-wiring test layer (click + drag emit + drop dispatch) and the D-16 reload persistence contract (DB round-trip + URL-fragment round-trip).

Purpose: This is the wave-4 integration plan — every wave-1-through-3 leaf plugs together here. After this plan lands, the shape-4 goal is achievable end-to-end at the code level: click a tile → get an app pane leaf → reload the page → the leaf restores.

Output: Three files modified across UI (AppTile, SplitView, AppShell) + one test file (AppTile drag/drop tests). No new files created (the AppShell.tsx changes are extensions to existing multi-hundred-line factories; splitting AppShell across plans would violate file ownership).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-CONTEXT.md
@.planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-RESEARCH.md
@.planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-PATTERNS.md
@.planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-04-SUMMARY.md
@.planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-06-SUMMARY.md
@src/ui/features/pretty-conversations/AppTile.tsx
@src/ui/features/pretty-conversations/PrettyConversationRow.tsx
@src/ui/shell/SplitView.tsx
@src/ui/AppShell.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire `AppTile.tsx` — onClick + drag handlers + cursor style + prop plumbing</name>
  <files>src/ui/features/pretty-conversations/AppTile.tsx, src/ui/features/pretty-conversations/AppTile.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-conversations/AppTile.tsx (the entire file — read all 257 lines; specifically Phase 119's `cursor: default` styling and the reserved-no-op click handler + the long-press logic; the `suppressNextClickRef` pattern; the MEDIUM-1 encodeURIComponent fix pattern)
    - src/ui/features/pretty-conversations/PrettyConversationRow.tsx (lines 940-1014 — the `onRowDragStart` template; specifically the `dataTransfer.setData("application/x-skynet-row", JSON.stringify(...))` pattern + the effectAllowed setting; note: this template DOES set `text/plain` but Phase 120 MUST NOT — RESEARCH.md anti-pattern)
    - .planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-PATTERNS.md § AppTile.tsx (verbatim shape of the onClick + onDragStart handlers + JSX wiring + cursor-style removal note)
    - .planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-CONTEXT.md § D-06 (left-click semantics: same behavior chats + terminals use; no-op if same app already in focused leaf; new leaf if in different leaf; unhealthy tile still clickable per D-18)
    - .planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-CONTEXT.md § D-07 (drag semantics: new leaf via existing split-drop machinery; payload shape)
    - .planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-RESEARCH.md § Anti-Patterns (the "do NOT set text/plain" rule + Phase 64 closure reference in SplitView.tsx:596-608)
    - src/backend/fleet-status/wire-protocol.ts (lines 640-720 — verify: `AppState.hostId` is a STRING on the wire per `fleet-status-types.ts:136`; the drag payload must Number(hostId) at the client boundary to unify types)
  </read_first>
  <behavior>
    - Test 1: mount `<AppTile app={mockApp} onOpenApp={mockFn} />`, `fireEvent.click(tile)` → `mockFn` called with args `(Number(mockApp.hostId), mockApp.slug, mockApp.title)`.
    - Test 2: mount `<AppTile app={{...mockApp, healthy: false}} onOpenApp={mockFn} />` (unhealthy variant), `fireEvent.click(tile)` → `mockFn` called (health does NOT gate click — D-18).
    - Test 3: mount `<AppTile app={mockApp} onOpenApp={mockFn} />`, trigger a long-press then a click (the `suppressNextClickRef` pattern from Phase 119) → `mockFn` NOT called (long-press suppresses the synthesized click).
    - Test 4: `fireEvent.dragStart(tile, { dataTransfer: mockDataTransfer })` → `mockDataTransfer.setData` called with `"application/x-skynet-app-tile"` and a JSON payload whose parsed shape is `{ hostId: Number(mockApp.hostId), slug: mockApp.slug, title: mockApp.title }`.
    - Test 5: `mockDataTransfer.setData` NOT called with `"text/plain"` at any point during drag (Phase 64 closure — assert via `mockDataTransfer.setData.mock.calls.every(([mime]) => mime !== "text/plain")`).
    - Test 6: `mockDataTransfer.effectAllowed` set to `"copy"` (not `"move"` — matches the drag-and-drop metaphor: dragging a tile CREATES a new leaf, does NOT MOVE the tile).
    - Test 7: cursor CSS is `cursor: pointer` (assert via computed style OR via a specific className presence — depends on how the file uses CSS: inline style vs Tailwind vs a separate CSS module. Verify by reading the file).
    - Test 8 (D-15 multi-instance — POSITIVE assertion per checker WARNING 2): mount `<AppTile app={mockApp} onOpenApp={mockFn} />`; call `fireEvent.click(tile)` TWICE in sequence → `mockFn.mock.calls.length` equals 2, and both calls carry args `(Number(mockApp.hostId), mockApp.slug, mockApp.title)`. No dedupe at the tile layer. This is the tile-half of D-15 multi-instance. The AppShell-half (that `openTab` creates two distinct Tab entries with matching `(hostId, slug)` tuples) is exercised at Plan 08's D-23 UAT — AppShell state is not practically unit-testable in isolation from the full component tree.
  </behavior>
  <action>
    In `src/ui/features/pretty-conversations/AppTile.tsx`:

    (a) Extend the component props type: add `onOpenApp?: (hostId: number, slug: string, title: string) => void` to the `AppTileProps` interface (or the equivalent props type — verify by reading the file's current props definition).

    (b) Locate the existing reserved-no-op click handler (Phase 119 D-13 shipped this as `cursor: default` + click handler that does nothing). Replace the body with the onTileClick pattern from `120-PATTERNS.md § AppTile.tsx`: check `suppressNextClickRef.current` — if true, reset it and return (long-press suppression preserved). Otherwise call `onOpenApp?.(Number(app.hostId), app.slug, app.title)` — `Number(...)` cast unifies the wire-protocol string hostId to the number the frontend uses per Plan 04's `Tab.app.hostId: number`. Keep the existing `useCallback` shape + dep array (add `onOpenApp`, `app.hostId`, `app.slug`, `app.title` to the dep array).

    (c) Add a new `onTileDragStart` handler above (or near) the existing handlers using `useCallback`. Body per `120-PATTERNS.md § AppTile.tsx` verbatim: `e.dataTransfer.setData("application/x-skynet-app-tile", JSON.stringify({ hostId: Number(app.hostId), slug: app.slug, title: app.title }))` and `e.dataTransfer.effectAllowed = "copy"`. Deps: `[app.hostId, app.slug, app.title]`. Do NOT `setData("text/plain", ...)` — this is the Phase 64 closure guardrail.

    (d) In the JSX of the tile element (a `<div>` with `role="button"` per Phase 119; verify by reading), add `draggable={true}` + `onDragStart={onTileDragStart}` + `onClick={onTileClick}` (the click handler existed in Phase 119 as a no-op; verify by reading). Preserve `onContextMenu={onRowContextMenu}` and all existing touch handlers unchanged.

    (e) Cursor style update per D-06 (UNCONDITIONAL — the override lives in `pretty-conversations.css`, VERIFIED by AppTile.tsx line 26's own comment which reads "cursor: default in the CSS (pretty-conversations.css `.pv-app-tile`)"). In `src/ui/features/pretty-conversations/pretty-conversations.css`, locate the `.pv-app-tile` rule block (approximately lines 1405-1445; verify by `grep -n '.pv-app-tile {' src/ui/features/pretty-conversations/pretty-conversations.css`). Inside that block, the current declaration reads `cursor: default;` (approximately line 1424 with a preceding comment about D-13). REPLACE that declaration with `cursor: pointer;` and update the preceding comment: change `/* D-13 override: ... replaced with cursor: default here ... */` to `/* D-06 (Phase 120): revert Phase 119's cursor: default override. Left-click now opens the app in a pane per D-06 — cursor communicates the interactive affordance. */`. Do NOT touch any other CSS rules in the file. AppTile.tsx does NOT set `cursor: default` inline (verified by reading the file — line 26 is a JSDoc comment only, not a style declaration), so no AppTile.tsx CSS edit is needed.

    In `src/ui/features/pretty-conversations/AppTile.test.tsx`:

    (f) Extend (or create) the test file with the seven behavior cases above. Use the existing test scaffold's `render` + `fireEvent` + `screen` + `vi.fn()` conventions (mirror the sibling test file `PrettyConversationRow.test.tsx` if AppTile.test.tsx doesn't exist yet).

    (g) The drag test builds a `mockDataTransfer` object with `setData: vi.fn()` and passes it in the `fireEvent.dragStart` options. Assert on `mockDataTransfer.setData.mock.calls` shape.

    (h) The long-press test uses `fireEvent.touchStart` → wait → `fireEvent.touchEnd` → `fireEvent.click` sequence per the existing `suppressNextClickRef` flow (verify shape by reading the file).

    Do NOT change the tile's rendering (title, second-line, icon, unhealthy variant styling). Do NOT gate click or drag on healthy status. Do NOT introduce new context-menu items. Do NOT set text/plain in the drag payload.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-vision &amp;&amp; SKYNET_COOKIE_DOMAIN=https://skynet.test npx vitest related --run src/ui/features/pretty-conversations/AppTile.tsx src/ui/features/pretty-conversations/AppTile.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'onOpenApp' src/ui/features/pretty-conversations/AppTile.tsx` returns at least 3 (props type + handler body + JSX wiring).
    - `grep -c 'application/x-skynet-app-tile' src/ui/features/pretty-conversations/AppTile.tsx` returns exactly 1.
    - `grep -c '"text/plain"' src/ui/features/pretty-conversations/AppTile.tsx` returns 0 (Phase 64 closure).
    - `grep -c 'effectAllowed = "copy"' src/ui/features/pretty-conversations/AppTile.tsx` returns exactly 1.
    - `grep -c 'Number(app.hostId)' src/ui/features/pretty-conversations/AppTile.tsx` returns at least 2 (click handler + drag payload).
    - `grep -c 'draggable={true}' src/ui/features/pretty-conversations/AppTile.tsx` returns exactly 1.
    - `grep -c 'onDragStart={onTileDragStart}' src/ui/features/pretty-conversations/AppTile.tsx` returns exactly 1.
    - `grep -c 'onClick={onTileClick}' src/ui/features/pretty-conversations/AppTile.tsx` returns exactly 1.
    - `grep -c 'cursor: default\\|cursor-default' src/ui/features/pretty-conversations/AppTile.tsx` returns 0 (AppTile.tsx never had an inline override — sanity).
    - Within the `.pv-app-tile` block of `pretty-conversations.css`, the ACTIVE `cursor:` declaration is `pointer` (comments not counted) — the executor runs `awk '/\.pv-app-tile \{/,/^\}/' src/ui/features/pretty-conversations/pretty-conversations.css | grep -v '^\s*/\*\|^\s*\*' | grep 'cursor:'` and confirms the ONE match is `cursor: pointer;`.
    - The `.pv-app-tile` block contains `cursor: pointer` — `awk '/\.pv-app-tile \{/,/^\}/' src/ui/features/pretty-conversations/pretty-conversations.css | grep -c 'cursor: pointer'` returns at least 1 (D-06 pointer cursor now in effect on the tile).
    - The `suppressNextClickRef` gate is preserved — `grep -c 'suppressNextClickRef' src/ui/features/pretty-conversations/AppTile.tsx` returns at least 2 (declaration + check).
    - The test file has at least 8 test cases — `grep -c "^\\s*it(" src/ui/features/pretty-conversations/AppTile.test.tsx` returns at least 8 (per checker WARNING 2: includes the D-15 multi-instance positive-assertion test).
    - The drag test asserts on the `application/x-skynet-app-tile` MIME — `grep -c 'application/x-skynet-app-tile' src/ui/features/pretty-conversations/AppTile.test.tsx` returns at least 1.
    - The text/plain-negative assertion exists — `grep -c 'text/plain' src/ui/features/pretty-conversations/AppTile.test.tsx` returns at least 1 (used as a negative assertion).
    - The scoped vitest run reports all tests passing (exit 0).
  </acceptance_criteria>
  <done>
    AppTile emits the app-tile drag payload without text/plain, click and drag work independently, click respects long-press suppression, cursor is pointer, D-15 multi-instance is preserved at the tile layer, and all eight tests pass green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend `SplitView.tsx` — `hasSkynetDragPayload` + onDrop app-tile branch + `onDropAppTileInTree` prop</name>
  <files>src/ui/shell/SplitView.tsx</files>
  <read_first>
    - src/ui/shell/SplitView.tsx (specifically the `hasSkynetDragPayload` gate at approximately lines 187-192; the onDrop dispatch handler at approximately lines 596-668 including the Phase 64 text/plain-fallback closure at 596-608 and the row branch at 619-634; the drop-target native listener pattern per patch #514)
    - .planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-PATTERNS.md § SplitView.tsx (verbatim extension patterns for hasSkynetDragPayload + drop-dispatch branch + center-drop discipline note)
    - .planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-RESEARCH.md § Pattern 4 (drag MIME extension + drop dispatch)
    - .planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-CONTEXT.md § D-07 (existing split-drop machinery accepts general leaf-source payload; extending is additive)
  </read_first>
  <action>
    In `src/ui/shell/SplitView.tsx`:

    (a) Locate `hasSkynetDragPayload` (approximately lines 187-192; verify by grep). Extend the OR-chain to include `dt?.types.includes("application/x-skynet-app-tile")` as a third disjunct (append after the existing `x-skynet-row` check). Preserve exact existing formatting.

    (b) Extend the SplitView component's props to accept a new callback: find the existing `onDropRowInTree` prop declaration on the SplitView component's props type (verify by grep). Immediately after it, add `onDropAppTileInTree?: (payload: { hostId: number; slug: string; title: string }, path: <PathType>, edge: <EdgeType>) => void;` — use the same `path` and `edge` type shape as `onDropRowInTree`.

    (c) Locate the onDrop dispatch handler (approximately lines 596-668; the branch structure has: text/plain-fallback (closed), row branch at 619-634, and possibly other branches). Add a NEW branch AFTER the row branch, mirroring its shape: read `const appTileJson = e.dataTransfer?.getData("application/x-skynet-app-tile") ?? "";`. If `appTileJson && onDropAppTileInTree` are both truthy, JSON.parse the payload as `{ hostId: number; slug: string; title: string }`, invoke `onDropAppTileInTree(parsed, path, edge)`, and `return`. On parse failure log a `console.warn` with prefix `[pv-split-drop] app-tile parse failed:` — verbatim shape from `120-PATTERNS.md § SplitView.tsx`.

    (d) Center-drop discipline (per PATTERNS.md § SplitView.tsx center-drop note): an app-tile center-drop is ALWAYS a fresh open (no replace-in-place). If the file has a center-drop branch parallel to the edge-drop branch (verify by grep for `centerDrop` or `center.*drop`), mirror the app-tile branch there too, wiring to the SAME `onDropAppTileInTree` callback. If no center-drop branch exists, the edge-drop branch alone suffices.

    (e) Preserve the Phase 64 text/plain-fallback closure at lines 596-608. Do NOT reintroduce a text/plain fallback for the app-tile source.

    Do NOT modify the native-listener registration for the drop target (patch #514 stability). Do NOT change the drop-target visual feedback logic (coral overlay stays as Phase 117 shipped it).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-vision &amp;&amp; SKYNET_COOKIE_DOMAIN=https://skynet.test npx vitest related --run src/ui/shell/SplitView.tsx 2>&amp;1 | tail -20; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -c 'SplitView.tsx' || echo 'tsc: no errors from SplitView.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'application/x-skynet-app-tile' src/ui/shell/SplitView.tsx` returns at least 2 (once in `hasSkynetDragPayload`, once in `onDrop` dispatch).
    - `grep -c 'onDropAppTileInTree' src/ui/shell/SplitView.tsx` returns at least 2 (prop type declaration + call in dispatch handler).
    - `hasSkynetDragPayload` still includes both prior MIMEs — `grep -c 'application/x-skynet-badge' src/ui/shell/SplitView.tsx` and `grep -c 'application/x-skynet-row' src/ui/shell/SplitView.tsx` each return at least 1.
    - Phase 64 text/plain closure preserved — `grep -c 'text/plain' src/ui/shell/SplitView.tsx` returns the SAME count as before this task (executor records the pre-task count and asserts post-task equality; alternatively use `git diff` to confirm no new text/plain reads were added).
    - `grep -c 'pv-split-drop.*app-tile' src/ui/shell/SplitView.tsx` returns at least 1 (parse-failure console.warn).
    - `tsc --noEmit` produces zero new errors originating from `SplitView.tsx`.
  </acceptance_criteria>
  <done>
    The gate accepts the third MIME, the dispatch handler has a new branch for app-tile drops calling `onDropAppTileInTree`, no text/plain reads were introduced, and tsc compiles cleanly.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire `AppShell.tsx` — `openTab` extension + `restoredTabs` reconstruction + `onDropAppTileInTree` callback + AppTile `onOpenApp` prop wiring + URL-fragment round-trip</name>
  <files>src/ui/AppShell.tsx</files>
  <read_first>
    - src/ui/AppShell.tsx (specifically lines 1185-1263 for URL fragment sync; lines 1505-1631 for `restoredTabs` handling; lines 1905-2000 for `openTab` factory; lines 2553-2604 for drop-target callback wiring; lines 2847+ for `PrettyConversationsPanel` invocation site — verify by reading each range)
    - src/types/ui-types.ts (as modified by Plan 04 — TabType six-arm + Tab.app + isAppTab)
    - src/ui/lib/tab-url.ts (as modified by Plan 04 — TabSpec seventh variant + parseTabParam + specForTab)
    - src/ui/features/pretty-conversations/AppTile.tsx (as modified by Task 1 — the `onOpenApp` prop shape)
    - src/ui/shell/SplitView.tsx (as modified by Task 2 — the `onDropAppTileInTree` prop shape)
    - .planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-PATTERNS.md § AppShell.tsx (verbatim extension patterns for openTab options, addOpenTab persist body, restoredTabs.push block, drop-target dispatch, URL-fragment persistence)
    - .planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-CONTEXT.md § D-06, D-07, D-15, D-16 (multi-instance is the default fall-through, reload persistence via existing machinery)
  </read_first>
  <behavior>
    - Test scaffolding: this file is >2000 lines and has heavy component dependencies; running a targeted vitest suite here is impractical without significant mocking. Rely on tsc-clean-compile + the existing AppShell test scaffold (if any) + downstream integration coverage from Plan 06 (tabUtils tests) and Plan 05 (backend integration tests). The D-21 client-sidebar-wiring layer already covered by Task 1's AppTile tests and Task 2's SplitView state; AppShell-level wiring is validated at Plan 08's D-23 UAT step.
  </behavior>
  <action>
    In `src/ui/AppShell.tsx`:

    (a) **`openTab` extension** (approximately lines 1905-2000; verify by `grep -n 'openTab = useCallback' src/ui/AppShell.tsx`). Extend the `options` parameter type to include an optional `app?: { hostId: number; slug: string }` field. Inside the useCallback body, extract `const appTuple = options?.app;`. When building the new Tab object inside `setTabs`, conditionally spread `...(appTuple !== undefined ? { app: appTuple } : {})` — mirror the existing `sessionKind` and `relayRoomId` conditional-spread pattern.

    (b) **`addOpenTab` persist call** (locate by `grep -n 'addOpenTab' src/ui/AppShell.tsx` or `grep -n 'PERSISTENT_TAB_TYPES' src/ui/AppShell.tsx`; approximately lines 2030-2044). When `PERSISTENT_TAB_TYPES.includes(type)` and the tab type is `"app"`, pass `appSlug: appTuple?.slug ?? null` in the persist body. Also thread `hostId: host ? parseInt(host.id) : (appTuple ? appTuple.hostId : null)` — app tabs carry the hostId via the tuple, not via a resolved `host` object. Verify by reading the existing addOpenTab call signature and mirror the field-add exactly. Ensure `PERSISTENT_TAB_TYPES` includes `"app"` — grep for its declaration; if it does not include `"app"`, add it (this is required for D-16 DB persistence).

    (c) **`restoredTabs.push` reconstruction** (approximately lines 1510-1552; verify by `grep -n 'restoredTabs.push' src/ui/AppShell.tsx`). When `saved.tabType === "app" && saved.hostId != null && saved.appSlug != null`, conditionally spread `{ app: { hostId: saved.hostId, slug: saved.appSlug } }` into the pushed Tab object. Mirror the existing sessionKind conditional-spread pattern. Note: `saved.appSlug` field is a new nullable field on the saved-row type per Plan 03 — verify by reading the type import for the saved-row shape (probably `SavedTab` or similar); the type will already have `appSlug?: string | null` after Plan 03's row shape flows through Drizzle's inferred types.

    (d) **`onDropAppTileInTree` callback** (approximately lines 2553-2604; the block that wires drop-target callbacks to SplitView; verify by `grep -n 'onDropRowInTree' src/ui/AppShell.tsx`). Define a new `onDropAppTileInTree` callback via `useCallback`. Body: call `const tabId = openTab(null, "app", undefined, { label: payload.title, app: { hostId: payload.hostId, slug: payload.slug } });` then `insertLeafAtEdge(tabId, path, edge);` — mirror the existing `onDropRowInTree` shape exactly. Wire it through the SplitView JSX at the SplitView invocation site (find `<SplitView ...`; verify by grep).

    (e) **`AppTile` `onOpenApp` prop wiring — UNCONDITIONAL, THREE-FILE COORDINATION** (approximately line 2847+; the `PrettyConversationsPanel` invocation site; verify by `grep -n 'PrettyConversationsPanel' src/ui/AppShell.tsx`). Phase 119 shipped AppTile.tsx with a deliberate NO-OP click handler and did NOT plumb an `onOpenApp` prop through the panel — this is a known gap (see AppTile.tsx line 26's own comment describing the Phase 119 deferral). Wire it end-to-end (all three touch points required, no conditional):

        (i) In `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` (declared in `files_modified` frontmatter — BLOCKER 3 fix), locate the component's props type near the top (verify by `grep -n 'export.*function PrettyConversationsPanel\|Props =' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`). Extend the props type with `onOpenApp?: (hostId: number, slug: string, title: string) => void;`. Then locate the `<AppTile key={...} app={app} />` invocation at approximately line 2440 (verify by grep for `<AppTile`) and thread the prop: `<AppTile key={...} app={app} onOpenApp={props.onOpenApp} />` (or use destructured `onOpenApp` if the component destructures props). That is the ONLY code edit needed in PrettyConversationsPanel.tsx.

        (ii) In `src/ui/AppShell.tsx`, define an `onOpenApp` useCallback (colocate near the other tab-creation callbacks — grep for `onDropRowInTree` for a placement anchor). Body: `const tabId = openTab(null, "app", undefined, { label: title, app: { hostId, slug } });`. Deps: `[openTab]`. Then at the `<PrettyConversationsPanel ... />` invocation site, add `onOpenApp={onOpenApp}` to the props.

        (iii) AppTile.tsx already received the `onOpenApp` prop declaration in Task 1(a) and the click-handler wiring in Task 1(b); no additional AppTile edit needed here.

    (f) **URL-fragment persistence** (approximately lines 1185-1263 and 2553-2604; verify by `grep -n 'encodeSplitTreeToUrl\\|specForTab\\|parseTabParam' src/ui/AppShell.tsx`). The URL-fragment serialiser/deserialiser dispatches via `specForTab`/`parseTabParam` from `src/ui/lib/tab-url.ts` (Plan 04 extended both). Per Plan 04 Task 2(d), `specForTab`'s input-parameter type now carries `app?: { hostId: number; slug: string }`. Locate every `specForTab({ ... })` call site in AppShell and, for app tabs, thread `app: tab.app` in the input object literal alongside the existing `host` / `sessionKind` / `relayRoomId` fields — same object, one new key. Concretely: at each callsite, extend the argument to `specForTab({ type: tab.type, host: tab.host, sessionKind: tab.sessionKind, relayRoomId: tab.relayRoomId, app: tab.app })` (or match the existing local variable naming). Do NOT introduce a separate `if (tab.type === "app")` branch — dispatch is generic via the widened input type.

    For the reverse direction (`parseTabParam` → construct `Tab`): AppShell has a code path that turns a `TabSpec` into a `Tab` for restore (grep for `spec.protocol === "relay"` or `parseTabParam` consumers). Add an `app` branch that reads `spec.hostId` + `spec.slug` and constructs `Tab.app = { hostId: Number(spec.hostId), slug: spec.slug }` — mirror the shape of the existing relay-room branch.

    (g) **Multi-instance is the free-fall behavior (D-15)** — no code needed. The existing tuple-keyed layout allows duplicates by default; no dedupe logic exists to defeat. Verify by NOT introducing any check like `if (existingTab.app?.hostId === newTab.app.hostId && existingTab.app?.slug === newTab.app.slug) return;` — such a check would BREAK D-15.

    Do NOT change the URL-fragment grammar. Do NOT change the persisted-tab-layout schema beyond adding the new field flow-through. Do NOT introduce a "focused-leaf same-app dedupe" behaviour (D-15 forbids). Do NOT alter the behaviour of any of the five existing tab types.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-vision &amp;&amp; SKYNET_COOKIE_DOMAIN=https://skynet.test npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -c 'AppShell.tsx' || echo 'tsc: no errors from AppShell.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'appTuple' src/ui/AppShell.tsx` returns at least 2 (extraction from options + conditional spread).
    - `grep -c 'onDropAppTileInTree' src/ui/AppShell.tsx` returns at least 2 (callback definition + SplitView prop wiring).
    - `grep -c 'onOpenApp' src/ui/AppShell.tsx` returns at least 2 (callback definition + panel prop wiring).
    - `grep -c 'app: appTuple\\|app: { hostId: saved.hostId, slug: saved.appSlug }' src/ui/AppShell.tsx` returns at least 2 (openTab conditional spread + restoredTabs conditional spread).
    - `grep -c 'appSlug' src/ui/AppShell.tsx` returns at least 2 (addOpenTab persist body + restoredTabs check).
    - The tabType `"app"` is in `PERSISTENT_TAB_TYPES` — `grep -A5 'PERSISTENT_TAB_TYPES' src/ui/AppShell.tsx | grep -c '"app"'` returns at least 1.
    - No dedupe check on `(hostId, slug)` was introduced — `grep -c 'existing.*\\.app\\?\\.\\(hostId\\|slug\\)' src/ui/AppShell.tsx` returns 0 (D-15 multi-instance preserved).
    - `tsc --noEmit` produces zero new errors originating from `AppShell.tsx`.
    - `PrettyConversationsPanel.tsx` extended per Task 3(e)(i) — `grep -c 'onOpenApp' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns at least 2 (props-type declaration + AppTile prop-pass-through). File is declared in `files_modified` frontmatter.
    - AppShell's `specForTab` callsites for app tabs thread `app: tab.app` in the input object literal — grep for the callsite and confirm the field is present when the source Tab is an app tab.
    - AppShell's URL-fragment restore path constructs `Tab.app = { hostId: Number(spec.hostId), slug: spec.slug }` when `spec.protocol === "app"` — `grep -c 'spec.protocol === "app"' src/ui/AppShell.tsx` returns at least 1.
  </acceptance_criteria>
  <done>
    AppShell's openTab accepts + persists the app tuple; restoredTabs reconstructs Tab.app from the saved row; onDropAppTileInTree is wired to SplitView; onOpenApp is wired to the sidebar panel; URL-fragment round-trip works via Plan 04's tab-url.ts; multi-instance is preserved (no dedupe introduced); tsc compiles cleanly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sidebar tile drag payload (browser → SplitView drop) | User-controlled hostId+slug flow into a new tab; downstream proxy request re-validates via checkHostAccess. |
| URL fragment (browser history → AppShell restore) | User-controlled TabSpec fragment flows into openTab; downstream proxy validation gates the eventual request. |
| DB-persisted tab records (user_open_tabs → restoredTabs) | User's own saved records only (WHERE userId = ? gate at open-tabs.ts); trust boundary is per-user, not per-request. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-38 | Tampering | Drag payload JSON contains crafted hostId/slug that fails downstream `APP_SLUG_RE` | mitigate | The proxy route (Plan 05) validates every request via `APP_SLUG_RE` + positive-integer hostId at entry; a malicious payload results in 400 at the proxy, not client-side crash. Frontend does NOT re-validate the payload beyond `JSON.parse` structural check. |
| T-120-39 | Denial of service | Malformed JSON in `application/x-skynet-app-tile` payload | mitigate | The SplitView dispatch try/catch (Task 2) logs a console.warn and returns without crashing the pane. Existing browsers gracefully handle malformed drag data. |
| T-120-40 | Data integrity | Legacy persisted app tabs (from a mid-rollout state where Plan 03 shipped but Plan 07 didn't) restore without Tab.app | mitigate | The restoredTabs.push conditional-spread reads `saved.appSlug` — if null (legacy) the tab restores WITHOUT `app` field, `isAppTab` narrowing returns false in tabUtils render, and the `renderAppTab` returns null (empty leaf). Non-fatal degradation. |
| T-120-41 | Information disclosure | URL-fragment specForTab emits hostId + slug in URL bar | accept | The URL is user-visible + shareable per D-16's design (workspace-share flow). This is the intended behaviour, not a leak — access control gates the eventual proxy request, not the URL. |
| T-120-42 | Elevation of privilege | Multi-instance (D-15) allowing duplicate open leaves | accept | Two independent iframes to the same app is the SPEC (D-15). Each leaf's requests are independently gated by checkHostAccess + CSRF. No new attack surface. |
| T-120-43 | Denial of service | User opens many app leaves → memory exhaustion | accept | Each iframe holds one WebSocket + one HTTP session; a user can create at most O(dozens) leaves manually. No practical DoS via automation because click and drag are user-initiated. |
| T-120-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed. |
</threat_model>

<verification>
- All grep-based acceptance criteria for all three tasks pass.
- Scoped vitest for AppTile passes (7 test cases green).
- `tsc --noEmit` reports zero errors originating from AppTile.tsx, SplitView.tsx, or AppShell.tsx.
- The D-21 client-sidebar-wiring layer test cases (click, drag emit, drop dispatch) are all exercised in Task 1's AppTile.test.tsx.
- The full end-to-end gesture flow (tile click → openTab → AppPane iframe → proxy hit → interstitial or byte-forwarding) can be exercised at the D-23 UAT step in Plan 08.
</verification>

<success_criteria>
- A user clicking an AppTile in the sidebar sees a new app leaf appear in the focused pane, with the app rendering inside via the iframe.
- A user dragging an AppTile into a split-edge sees a new leaf appear at that edge holding the app.
- On page reload, previously-open app leaves are restored from both the URL fragment (immediate) and the DB (async) paths.
- Multi-instance is preserved: dragging the same tile twice creates two independent leaves.
- No changes required to the five existing tab types' behavior.
</success_criteria>

<output>
Create `.planning/phases/120-first-class-apps-campaign-shape-4-apps-as-a-content-type-in-/120-07-SUMMARY.md` when done, listing:
- The line ranges modified in each of the three source files.
- Whether the `PrettyConversationsPanel` (or equivalent) required a prop-hole extension (with the file path if yes).
- Whether `PERSISTENT_TAB_TYPES` needed `"app"` added (it likely did — record the change).
- The seven AppTile test-case labels + their pass/fail state.
- Confirmation that no multi-instance dedupe check was introduced (D-15 preserved).
- Any tsc warnings or errors from touching AppShell.tsx (should be zero — surface any residual).
</output>
