---
phase: 95-pv-context-pct-batch-sweep-drop-capture-pane-phase-92-sibling
plan: 05
type: execute
wave: 3
depends_on: ["95-04"]
files_modified:
  - src/backend/claude-session/claude-session-server.ts
  - src/backend/claude-session/claude-session-server.pv-sweep.test.ts
autonomous: false
requirements: []
must_haves:
  truths:
    - "The contextPctTimer callback in claude-session-server.ts dispatches batch-first / legacy-fallback per Phase 92's pattern. On a peer where `~/.local/bin/pv-context-pct-sweep` is present + healthy, ONE sweep exec fires per WS per 3s tick instead of today's up-to-4 tail-expansion execs. On a peer without the script (mid-rollout / opt-out), the LEGACY path fires — the current readContextPctFromJsonl call preserved verbatim."
    - "Presence probe (`test -x ~/.local/bin/pv-context-pct-sweep`) fires once per SSH-channel lifetime per WS, cached in the per-WS closure. Cleared implicitly by the WS closure resetting on WS-close."
    - "Null sweep-exec return → this-tick legacy fallback + force-re-probe next tick (transient recovery). Sweep exec returning schema-mismatched JSONL → this-tick legacy fallback + latch mismatch for this WS's SSH channel lifetime (permanent lockout until channel re-establish)."
    - "Regression tests prove: (a) batch path fires exactly 1 sweep exec + 0 tail execs per tick under presence-probe-yes, (b) legacy path fires today's per-identity tail expansion under presence-probe-no, (c) presence probe fires once per SSH-channel-lifetime (not per tick), (d) null-exec recovery re-probes next tick, (e) schema-mismatch latch stays latched, (f) parity between batch and legacy `context_pct` values for the same input state."
    - "The Ashley UAT checklist at end of Wave 3 confirms: zero `tail -c 10000/50000/200000/512000` execs from the context-pct pipeline in a 60s window under normal PV load; zero `tmux capture-pane` execs from the context-pct pipeline (the aside-subsystem capture-pane calls are out of scope and stay); the batch sweep script fires as expected per WS per tick."
    - "Existing tests remain green through the caller rewire (fleet rule)."
  artifacts:
    - path: "src/backend/claude-session/claude-session-server.ts"
      provides: "contextPctTimer callback with sweep-first / legacy-fallback dispatch + per-WS-per-channel-lifetime presence probe cache + null-exec re-probe + schema-mismatch latch"
    - path: "src/backend/claude-session/claude-session-server.pv-sweep.test.ts"
      provides: "6 regression tests covering batch dispatch, legacy fallback, probe caching, null-exec recovery, schema-mismatch latch, batch-vs-legacy parity"
  key_links:
    - from: "claude-session-server.ts contextPctTimer"
      to: "pv-sweep-schema.ts parseSweepJsonl"
      via: "import + call in batch dispatch branch"
      pattern: "parseSweepJsonl|PV_SWEEP_SCHEMA_VERSION"
    - from: "claude-session-server.ts contextPctTimer"
      to: "~/.local/bin/pv-context-pct-sweep on managed box"
      via: "channel.exec of installed script path + --identities argv"
      pattern: "pv-context-pct-sweep"
    - from: "claude-session-server.ts contextPctTimer"
      to: "existing readContextPctFromJsonl (legacy fallback branch)"
      via: "extracted-to-helper + reused in fallback branch"
      pattern: "readContextPctFromJsonl"
---

<objective>
Load-bearing task of Wave 3. Rewire the contextPctTimer callback in claude-session-server.ts to dispatch sweep-first / legacy-fallback per Phase 92's proven pattern. Add 6 regression tests. Prepare + execute an Ashley-owned UAT checklist that closes Phase 95.

Purpose: Plans 01–04 shipped the substrate; Plan 05 is where the exec-count collapse actually delivers. Also the highest-risk task in the phase — the callback surface is small (~40 lines post-Plan-02 simplification per RESEARCH §4f) but the failure modes are subtle (batch produces wrong pct + no fallback → PrettyView shows wrong percent silently; presence probe never re-runs after a false negative → stuck on legacy forever). Regression tests are the safety net.

**Topology confirmed as Option 3 (per-WS) from Plan 04's decision lock.** The sweep exec runs on the same per-WS SSH connection as today's per-tail path. No new coordination surface. Presence probe + schema-mismatch latch state lives in the per-WS closure — same lifetime as `sshConn`, `currentSessionFile`, etc. When the WS closes and re-opens, the closure regenerates and the probe re-fires.

Output: Modified claude-session-server.ts with the rewired contextPctTimer, new pv-sweep.test.ts with 6 regressions, an Ashley UAT gate that closes Phase 95.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/95-pv-context-pct-batch-sweep-drop-capture-pane-phase-92-sibling/95-CONTEXT.md
@.planning/phases/95-pv-context-pct-batch-sweep-drop-capture-pane-phase-92-sibling/95-RESEARCH.md
@.planning/phases/95-pv-context-pct-batch-sweep-drop-capture-pane-phase-92-sibling/95-04-SUMMARY.md

# Phase 92-04 + 92-05 patterns to mirror — read in full:
@.planning/phases/92-fleet-status-poller-batch-sweep-one-exec-per-host-not-one-pe/92-04-PLAN.md
@.planning/phases/92-fleet-status-poller-batch-sweep-one-exec-per-host-not-one-pe/92-05-PLAN.md

# The file being rewired — read L7569–L7793 (the simplified contextPctTimer callback post-Plan-02) + closure state area:
@src/backend/claude-session/claude-session-server.ts

# The schema module from Plan 04:
@src/backend/claude-session/pv-sweep-schema.ts

# The legacy path being preserved verbatim as fallback:
@src/backend/claude-session/context-pct-from-jsonl.ts

# Test-pattern reference — the seam pattern for regression testing:
@src/backend/claude-session/dormant-poll.test.ts
@src/backend/claude-session/claude-session-server.contextpct-dual-write.test.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewire contextPctTimer with batch-first / legacy-fallback dispatch + per-WS presence probe cache</name>
  <files>src/backend/claude-session/claude-session-server.ts</files>
  <action>
    Read Phase 92-04's caller-rewire plan end-to-end BEFORE editing. The mechanic ports 1:1 with two simplifications: (a) per-WS closure state (not PerHostState), (b) single-tier PvSweepLine (not two-tier).

    Read the current contextPctTimer callback at L7569–L7793 (post-Plan-02 simplified state) to confirm the surface. Expected surface per RESEARCH §4f: ~40 lines — read sessionFileSnapshot, call readContextPctFromJsonl, guard stopped/readyState, setContextPct + ws.send.

    **Step 1 — Extract legacy readContextPctFromJsonl callsite into a helper.**

    Currently the contextPctTimer calls `pct = await readContextPctFromJsonl(connSnapshot, sessionFileSnapshot)`. Extract this into a named local function `computeContextPctLegacy(connSnapshot, sessionFileSnapshot): Promise<number | null>` co-located near the callback. Body is: return `await readContextPctFromJsonl(connSnapshot, sessionFileSnapshot)`. Rationale: the batch path AND the legacy path both need to compute pct; extracting to a helper keeps the dispatch logic in the callback readable + gives Plan 05 tests a mockable seam.

    **Step 2 — Add per-WS closure state for the presence probe cache + schema-mismatch latch.**

    Immediately after the WS connection setup (probably near where `sshConn`, `currentSessionFile`, etc. are declared — grep for `let sshConn` to find the region), add:

    ```
    // Phase 95 Part C — per-WS closure state for the pv-context-pct-sweep
    // batch dispatch. sweepScriptPresent is `null` before the first probe;
    // becomes true/false after probe. Cleared naturally on WS close (whole
    // closure regenerates). sweepSchemaMismatch latches true on the first
    // schema-mismatched sweep response and stays latched — the caller falls
    // back to legacy for the remainder of the WS lifetime rather than
    // re-attempting a schema the peer's script version doesn't support.
    let sweepScriptPresent: boolean | null = null;
    let sweepSchemaMismatch: boolean = false;
    ```

    **Step 3 — Add the batch dispatch function.**

    Also near the contextPctTimer callback, add:

    ```
    async function computeContextPctBatch(connSnapshot: SSHClientType, tmuxSessionSnapshot: string): Promise<{ ok: true; pct: number | null } | { ok: false; reason: "null-exec" | "schema-mismatch" | "identity-missing" }> {
      // G6 belt-and-suspenders safe-char guard.
      if (!/^[a-zA-Z0-9_-]+$/.test(tmuxSessionSnapshot)) {
        return { ok: false, reason: "identity-missing" };
      }
      const sweepCmd = `~/.local/bin/pv-context-pct-sweep --identities ${tmuxSessionSnapshot} 2>/dev/null`;
      const raw = await execCommand(connSnapshot, sweepCmd);
      if (raw === null) return { ok: false, reason: "null-exec" };
      const parsed = parseSweepJsonl(raw);
      if (parsed.schemaMismatch) return { ok: false, reason: "schema-mismatch" };
      const line = parsed.lines.find(l => l.identity === tmuxSessionSnapshot);
      if (!line) return { ok: false, reason: "identity-missing" };
      return { ok: true, pct: line.context_pct };
    }
    ```

    Note the sweep is invoked with `--identities <tmuxSessionSnapshot>` — a single-identity sweep from this WS. Per topology decision Option 3 (Plan 04), each WS runs its own sweep for its own identity. If Ashley later chooses Option 1 or 2 (per-host coordination), the sweep would batch multiple identities into the argv; the schema already supports that via the `lines[]` array. No forward-compat cost.

    Import `parseSweepJsonl` from `../claude-session/pv-sweep-schema.js` at the top of the file.

    **Step 4 — Rewrite the contextPctTimer body with the dispatch.**

    New body (replaces the current ~40-line simplified body from Plan 02):

    ```
    // Snapshot state to protect against concurrent closure mutations.
    const sessionFileSnapshot = currentSessionFile;
    const connSnapshot = sshConn;
    const tmuxSessionSnapshot = activeTmuxSession;
    if (!connSnapshot || !sessionFileSnapshot || !tmuxSessionSnapshot) return;

    let pct: number | null = null;

    // Presence probe on first tick per SSH-channel lifetime.
    if (sweepScriptPresent === null) {
      const probeRaw = await execCommand(connSnapshot, "test -x ~/.local/bin/pv-context-pct-sweep 2>/dev/null && echo yes || echo no");
      sweepScriptPresent = probeRaw !== null && probeRaw.trim() === "yes";
      systemLogger.info("PV context-pct sweep-script presence probed", {
        operation: "pv_context_pct_sweep_probe",
        present: sweepScriptPresent,
        identity: tmuxSessionSnapshot,
      });
    }

    // Batch-first dispatch.
    if (sweepScriptPresent && !sweepSchemaMismatch) {
      const result = await computeContextPctBatch(connSnapshot, tmuxSessionSnapshot);
      if (result.ok) {
        pct = result.pct;
      } else {
        systemLogger.warn("PV context-pct batch fell back to legacy", {
          operation: "pv_context_pct_batch_fallback",
          reason: result.reason,
          identity: tmuxSessionSnapshot,
        });
        // Null-exec: force re-probe next tick (transient recovery).
        if (result.reason === "null-exec") sweepScriptPresent = null;
        // Schema-mismatch: latch for connection lifetime.
        if (result.reason === "schema-mismatch") sweepSchemaMismatch = true;
        // This-tick fallback:
        pct = await computeContextPctLegacy(connSnapshot, sessionFileSnapshot);
      }
    } else {
      // Legacy path (script absent OR schema mismatch latched).
      pct = await computeContextPctLegacy(connSnapshot, sessionFileSnapshot);
    }

    // Emit — null pct also emits (accepted UX regression per CONTEXT.md).
    if (stopped || ws.readyState !== WebSocket.OPEN) return;
    setContextPct(activeHostId, activeTmuxSession, pct);
    ws.send(JSON.stringify({ type: "context_pct", pct }));
    ```

    **Step 5 — Preserve unrelated closure state.**

    The dormancy stat block (D1, D2, D3) at L7796+, the sentinel probe at L7892+, and the aside timer at L8014 stay UNCHANGED. Per RESEARCH §3b these are OUT of Phase 95 scope. Same for the aside subsystem's `capture-pane -p -S -200` calls at L7982 + L8013.

    **Step 6 — Compile + typecheck + existing test regression.**

    - `npx tsc --noEmit` — zero new errors.
    - `npx vitest run src/backend/claude-session/` — all existing tests still pass. The extraction to `computeContextPctLegacy` is a rename+move that preserves behavior for the sweep-absent path; `contextpct-dual-write.test.ts` should still be green (Test 8's regex was already updated in Plan 02 to match the simplified emit block).

    **Do NOT** modify pv-sweep-schema.ts (Plan 04 owns it). **Do NOT** modify substrate/scripts/pv-context-pct-sweep.py. **Do NOT** touch the fleet-status ssh-poll-orchestrator (that's Phase 92's subsystem).

    **Commit shape:** `feat(claude-session): rewire contextPctTimer to batch-first sweep with legacy fallback (Phase 95 Part C-4)`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; npx tsc --noEmit &amp;&amp; grep -c "computeContextPctBatch\|computeContextPctLegacy\|sweepScriptPresent\|pv_context_pct_sweep_probe\|pv_context_pct_batch_fallback" src/backend/claude-session/claude-session-server.ts | grep -v '^0$' &amp;&amp; grep -c "parseSweepJsonl" src/backend/claude-session/claude-session-server.ts | grep -v '^0$' &amp;&amp; npx vitest run src/backend/claude-session/</automated>
  </verify>
  <done>
    claude-session-server.ts imports parseSweepJsonl from pv-sweep-schema.js. contextPctTimer dispatches sweep-first / legacy-fallback with a per-WS presence probe cache. computeContextPctBatch + computeContextPctLegacy helper functions exist. `sweepScriptPresent` + `sweepSchemaMismatch` closure state added. Null-exec re-probes; schema-mismatch latches. `pv_context_pct_sweep_probe` + `pv_context_pct_batch_fallback` structured log ops present. `npx tsc --noEmit` green. Existing claude-session tests still pass. Atomic commit landed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Six regression tests in claude-session-server.pv-sweep.test.ts</name>
  <files>src/backend/claude-session/claude-session-server.pv-sweep.test.ts</files>
  <behavior>
    Reuse test-fixture patterns from src/backend/claude-session/claude-session-server.contextpct-dual-write.test.ts (MockSshChannel-equivalent for the per-WS `sshConn`, exec-call capture list, contextpct-store mock, ws.send mock). If the existing test file uses a `__applyContextPctTimerTickForTests` seam or similar — use it. If not, expose one from Task 1's edits: `export const __applyPvContextPctBatchTickForTests = { computeContextPctBatch, computeContextPctLegacy, ... };` similar to Phase 92's seam pattern. Task 1's action block does not spec this seam explicitly — add it as part of Task 2's TDD RED-gate if the existing tests can't drive the callback via the current seam.

    Six regression tests (each in its own `it()` under a `describe("Phase 95 Part C — pv-context-pct-sweep batch dispatch", () => { ... })`):

    - **Test 1 — batch path fires exactly 1 sweep exec per tick + 0 tail-expansion execs.**
        - Setup: mock sshConn's exec to respond `"yes\n"` to the presence probe command AND to respond with a valid JSONL blob (one line, matching PvSweepLine) for the `--identities <session>` sweep command.
        - Drive one contextPctTimer tick.
        - Assert: `execCalls.filter(c => c.match(/test -x .*pv-context-pct-sweep/)).length === 1` (probe fired).
        - Assert: `execCalls.filter(c => c.match(/~\/.local\/bin\/pv-context-pct-sweep --identities/)).length === 1` (sweep fired).
        - Assert: `execCalls.filter(c => c.match(/tail -c/)).length === 0` (NO legacy tail).
        - Assert: `wsSendCalls.filter(c => c.type === "context_pct").length === 1` (context_pct emitted).

    - **Test 2 — legacy path fires today's tail-expansion when probe returns no.**
        - Setup: mock probe response `"no\n"`. Register legacy tail-expansion responses (readContextPctFromJsonl's execs — inspect that file to identify the exact command patterns; likely `tail -c 10000 <jsonl_path>` etc.).
        - Drive one tick.
        - Assert: probe fired once; sweep exec fired ZERO times.
        - Assert: legacy tail expansion fired at least once (`execCalls.filter(c => c.match(/tail -c/)).length >= 1`).
        - Assert: context_pct emitted with the pct value the legacy path computed.

    - **Test 3 — presence probe fires once per SSH-channel lifetime, not per tick.**
        - Setup: same as Test 1 (probe returns yes, sweep returns valid JSONL).
        - Drive THREE ticks in sequence.
        - Assert: probe fired exactly ONCE across all 3 ticks (probeCalls === 1).
        - Assert: sweep exec fired exactly THREE times (sweepCalls === 3, one per tick).

    - **Test 4 — sweep returning null triggers this-tick legacy fallback + re-probe next tick.**
        - Setup: probe returns yes on tick 1. Sweep returns `null` on tick 1's sweep exec (simulate SSH transport error). Sweep returns valid JSONL on tick 2. Legacy tail-expansion fixtures registered for tick 1 fallback.
        - Drive tick 1: assert sweep attempted (once), sweep returned null, legacy tail-expansion fired, `pv_context_pct_batch_fallback` log op observable with `reason: "null-exec"`, context_pct emitted with legacy value.
        - Drive tick 2: assert probe re-fired (probeCalls === 2 total), sweep fired successfully, NO legacy tail-expansion this tick.

    - **Test 5 — schema-version mismatch triggers this-tick legacy + connection-lifetime latch.**
        - Setup: probe returns yes. Sweep returns valid JSONL BUT with `schema_version: 999`. Legacy fixtures for fallback.
        - Drive tick 1: assert sweep fired (once), `pv_context_pct_batch_fallback` log op with `reason: "schema-mismatch"` fires, legacy tail-expansion fires, context_pct emitted with legacy value.
        - Drive tick 2: assert sweep did NOT fire (latched off — sweepSchemaMismatch === true), legacy tail-expansion fires, context_pct emitted with legacy value.

    - **Test 6 — parity: batch path and legacy path produce equal context_pct for equivalent inputs.**
        - Build TWO copies of the test harness: (A) batch-path (probe yes, sweep JSONL emits `context_pct: 42`), (B) legacy-path (probe no, tail-expansion fixture produces a JSONL that readContextPctFromJsonl computes to `context_pct: 42`).
        - Drive one tick each.
        - Assert: `A.wsSend.calls.filter(c => c.type === "context_pct")[0].pct === B.wsSend.calls.filter(c => c.type === "context_pct")[0].pct`. Both === 42.
        - This is the "batch and legacy return same value" guarantee. If Task 1's Python sweep script emits a pct that differs from readContextPctFromJsonl for the same input, this test catches the drift.

    Colocate a helper `makePvSweepJsonl({ identity, contextPct, schemaVersionOverride? })` at the top of the test file. Mirrors `makeSweepJsonl` from Phase 92-05 Task 1.
  </behavior>
  <action>
    Write the test file FIRST (RED gate). Confirm all 6 tests FAIL before any implementation of the seam that lets tests drive the callback (may require adding a small `__applyPvContextPctBatchTickForTests` export to claude-session-server.ts — if so, add it to Task 1's file surgically as a follow-on edit + commit that alongside Task 2's test file).

    If Task 1 already exposed a suitable seam or the existing test file's `__applyContextPctTimerTickForTests` seam covers the dispatch, use it. Prefer minimal seam surface.

    Then run tests to confirm the 6 new ones now PASS. Run the full claude-session test suite to confirm zero regressions on the existing tests.

    Commit shape: `test(claude-session): regression suite for pv-context-pct-sweep batch dispatch (Phase 95 Part C-5)`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; npx vitest run src/backend/claude-session/claude-session-server.pv-sweep.test.ts</automated>
  </verify>
  <done>
    claude-session-server.pv-sweep.test.ts contains 6 new tests inside a Phase 95 Part C describe block. Tests exercise: batch dispatch, legacy fallback, probe caching, null-exec recovery, schema-mismatch latch, batch-vs-legacy parity. `npx vitest run src/backend/claude-session/claude-session-server.pv-sweep.test.ts` passes. Full claude-session test suite still green. Atomic commit landed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Ashley UAT gate — end-of-Phase-95 verification against t1000</name>
  <files>N/A — human verification checkpoint; no files modified</files>
  <what-built>
    Wave 1 (Plan 01) shipped Part A: distributor settings-patch that disables plan mode fleet-wide.

    Wave 2 (Plans 02 + 03) shipped Part B: deleted the entire plan-pending backend + frontend stack. `tmux capture-pane -p -t <session>` in the contextPctTimer callback is gone. parseContextPct pane-scrape fallback dropped.

    Wave 3 (Plans 04 + 05) shipped Part C: pv-sweep-schema.ts + pv-context-pct-sweep.py + distributor catalog row + contextPctTimer batch-first/legacy-fallback dispatch + 6 regression tests.

    Wave 3 close = this UAT gate. Ashley executes the checklist below against t1000 (post-deploy) to confirm the exec-count collapse is real + plan-pending code paths are gone + the frontend still works.
  </what-built>
  <how-to-verify>
    **Prerequisite:** Wave 3 commits (Plans 04 + 05) merged to `feat/tab-title-from-tmux`. Skynet container rebuilt with the new image AND `docker compose up -d --force-recreate skynet` on t1000. Wait 60 seconds for the distributor sweep to push `pv-context-pct-sweep.py` to every reachable peer AND for existing WS connections to churn (or reload the browser tabs to force fresh WS closures + reconnects; the presence probe caches per-WS-connection-lifetime, so a fresh WS gets a fresh probe).

    **Check 1 — every peer got the sweep script.**

    For each reachable fleet-substrate peer:

    `ssh <peer> "test -x ~/.local/bin/pv-context-pct-sweep && echo INSTALLED || echo MISSING"`

    Expect INSTALLED on every `runsFleetSubstrate=true` peer. MISSING on opt-out peers is expected + legacy fallback covers.

    **Check 2 — sweep-probe log op fired for every open PV tab post-deploy.**

    `docker logs skynet-backend --since 5m 2>&1 | grep '"operation":"pv_context_pct_sweep_probe"'`

    Expect: N log lines where N ≈ number of open PV tabs (each tab = one WS = one probe on first tick). Each line should show `present: true` for peers with the script installed.

    **Check 3 — no `tail -c 10000/50000/200000/512000` execs from the context-pct pipeline under normal PV load.**

    First verify the assumption: `docker logs skynet-backend --since 60s 2>&1 | grep -c "exec command="` should be non-zero (proving exec commands ARE in the docker stdout stream). If zero, `execCommand` in this build is not logging to docker stdout — this check needs a different observation surface (e.g. instrumented log, network-level packet capture). Halt + adjust.

    Then open 5 PV tabs in the Skynet frontend against 5 different identities. Wait 60 seconds. Run:

    `docker logs skynet-backend --since 60s 2>&1 | grep -cE "tail -c (10000|50000|200000|512000)"`

    Expect: ZERO or very low single-digit (allowing for legacy-fallback triggered by mid-rollout peers). If the count is comparable to pre-phase (hundreds per minute), the batch dispatch isn't taking effect — investigate whether the presence probe is returning false, whether the sweep script is emitting schema-mismatched JSON, or whether the executor missed a Task 1 wiring step.

    **Check 4 — `pv_context_pct_batch_fallback` log ops are rare.**

    `docker logs skynet-backend --since 5m 2>&1 | grep -c '"operation":"pv_context_pct_batch_fallback"'`

    Expect: near-zero after the first minute of deployment. Persistent non-zero here means the sweep script is failing or returning schema-mismatched output on some peer — investigate.

    **Check 5 — context_pct values match between batch and legacy on cross-checked identities.**

    Sanity check: for one identity, note the context_pct shown in the Skynet PV UI. Then SSH to the peer and run:

    `python3 ~/.local/bin/pv-context-pct-sweep --identities <identity>`

    Expect: the emitted `context_pct` value in the JSONL matches the UI display within +/-1% (accounts for the 3s tick timing). Do this for 3 identities on 3 different peer hosts. If any mismatch is >1%, the Python port has a parity bug — HALT + investigate.

    **Check 6 — no frontend regressions.**

    Load the Skynet frontend at https://term.gigaashley.click. Open multiple PV tabs. Confirm:
    - Every tab's context-pct meter renders (a number OR the loading placeholder — both are valid; recall the "first few seconds of fresh session shows null" accepted UX regression per CONTEXT.md).
    - No plan-pending bubbles appear anywhere (they can't — the code is deleted).
    - ComposeBox is usable in every state — sending, receiving, pane-switching all work.
    - Frontend console has zero errors related to `plan_pending`, `PlanPendingBubble`, or `raw_keystrokes`.

    **Reply to close this gate + close Phase 95:** either `all 6 checks PASS — Phase 95 complete` OR describe which check(s) failed with specific evidence. If any check fails, the phase reopens for gap-closure planning.
  </how-to-verify>
  <verify>
    <human-check>Ashley executes checks 1–6 against t1000, reports pass/fail per step.</human-check>
  </verify>
  <done>
    Ashley has replied `all 6 checks PASS — Phase 95 complete` OR the failures are documented for gap-closure planning. Under no circumstance does this task self-approve — it MUST wait for a human reply. Phase 95 does not close until this gate closes.
  </done>
  <resume-signal>Reply `all 6 checks PASS — Phase 95 complete` OR describe failure(s) and Wave 3 reopens for gap-closure planning.</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — zero new type errors.
- `npx vitest run src/backend/claude-session/claude-session-server.pv-sweep.test.ts` — all 6 new tests pass.
- `npx vitest run src/backend/claude-session/` — all existing claude-session tests still pass.
- `npx vitest run` — full suite green.
- `grep -c "computeContextPctBatch\|computeContextPctLegacy\|sweepScriptPresent\|parseSweepJsonl" src/backend/claude-session/claude-session-server.ts` returns positive counts for all four symbols.
- `grep -c "tail -c" src/backend/claude-session/claude-session-server.ts` UNCHANGED from post-Plan-02 count — the legacy tail-expansion code lives inside readContextPctFromJsonl (unchanged file) invoked via computeContextPctLegacy helper (still fires on the sweep-absent branch).
- Ashley UAT gate (Task 3) closes with `all 6 checks PASS — Phase 95 complete`.
</verification>

<success_criteria>
- The contextPctTimer collapses from up-to-4 tail execs per WS per tick to 1 sweep exec per WS per tick on peers with the script; legacy path preserved on peers without.
- Presence probe cached correctly per SSH-channel lifetime — no per-tick probe overhead.
- Null-exec recovery + schema-mismatch latch both work per Phase 92's proven pattern.
- Regression tests prove the exec-count collapse + parity between batch and legacy.
- Ashley UAT confirms the per-tick tail-c exec pressure from the context-pct pipeline is eliminated and no frontend regressions.
- Phase 95 closes with the L7602-block capture-pane elimination (Plan 02) AND the Part C batch collapse (Plan 05) both live in prod. The remaining unrelated poll consumers (aside subsystem, dormancy stat, sentinel probe, SFTP pool) are OUT of scope and unchanged.
</success_criteria>

<output>
Create `.planning/phases/95-pv-context-pct-batch-sweep-drop-capture-pane-phase-92-sibling/95-05-SUMMARY.md` when done. Include:
- Line count of the new contextPctTimer callback body (post-rewire).
- Line count of computeContextPctBatch + computeContextPctLegacy helpers.
- List of the 6 new test names + which seam they drove (existing __applyContextPctTimerTickForTests, new __applyPvContextPctBatchTickForTests, or another).
- Confirmation that Test 6 (batch-vs-legacy parity) uses strict `===` equality on emitted pct.
- UAT execution results (per-check pass/fail after Ashley replies).
- Any observed regression or drift from expected behavior.
- Two atomic commit hashes (Task 1 rewire + Task 2 tests).
- **Final phase-close signal** — whether Phase 95 is closing green (all 7 UAT checks PASS) or reopening for gap-closure planning.
</output>
