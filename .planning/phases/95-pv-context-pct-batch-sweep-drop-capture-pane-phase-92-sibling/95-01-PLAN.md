---
phase: 95-pv-context-pct-batch-sweep-drop-capture-pane-phase-92-sibling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/fleet-status/remote-hook-install.ts
  - src/backend/fleet-status/remote-hook-install.test.ts
autonomous: false
requirements: []
must_haves:
  truths:
    - "A new pure helper `readAndMergePermissionDeny(currentSettings, toolName)` exists in remote-hook-install.ts with the same shape and shallow-copy discipline as readAndMergeHookSettings — accepts settings + a bare tool name, returns { merged, alreadyInstalled }, never mutates input."
    - "`installStopHook` merges `EnterPlanMode` + `ExitPlanMode` into `permissions.deny` alongside the six existing hook-entry merges, using the same idempotency AND-fold pattern."
    - "The merge helper handles all three settings.json shapes: (a) no `permissions` key at all, (b) `permissions` exists but no `deny` array, (c) `permissions.deny` already contains other entries — new entries appended, never clobbering."
    - "Structured `plan_mode_deny_applied` log op fires on install completion so container logs show a fleet-wide inventory of which hosts got patched."
    - "Skynet container restart in prod causes every fleet-substrate peer host's next SSH-client acquire (or existing lifecycle re-acquire) to re-fire installStopHook, which idempotently merges the two new deny entries into `~/.claude/settings.json` on every reachable peer."
    - "After Wave 1 UAT gate closes, grep-of-settings-json on every reachable peer confirms both `EnterPlanMode` and `ExitPlanMode` appear inside `permissions.deny` — Part B (Wave 2) is safe to start."
  artifacts:
    - path: "src/backend/fleet-status/remote-hook-install.ts"
      provides: "readAndMergePermissionDeny helper + two-entry deny merge inside installStopHook"
      exports: ["readAndMergePermissionDeny", "installStopHook", "readAndMergeHookSettings"]
      contains: "EnterPlanMode"
    - path: "src/backend/fleet-status/remote-hook-install.test.ts"
      provides: "unit tests for readAndMergePermissionDeny across the three settings shapes + integration test for installStopHook idempotency across 8 total merges"
  key_links:
    - from: "remote-hook-install.ts"
      to: "~/.claude/settings.json on every peer box"
      via: "installStopHook's atomic .tmp + mv write path"
      pattern: "EnterPlanMode|ExitPlanMode"
    - from: "starter.ts L643 installStopHook call"
      to: "remote-hook-install.ts installStopHook export"
      via: "existing per-host SSH-client-lifecycle fire-and-forget call"
      pattern: "installStopHook"
---

<objective>
Extend the existing fleet-substrate distributor's `installStopHook` in remote-hook-install.ts to merge two new `permissions.deny` entries — `EnterPlanMode` and `ExitPlanMode` — into every managed box's `~/.claude/settings.json`. This is the source-side kill for Claude Code's plan mode across the fleet. Ships as ONE atomic commit and MUST be verified in prod BEFORE Wave 2 (Plans 02–05) touches the plan-pending code path.

Purpose: Wave-ordering hard-dependency per RESEARCH §5. If Wave 2 ships first (backend deletes plan-pending detection code) while an un-patched peer box's Claude Code can still enter plan mode, the user gets stuck at a live Ink plan-approval prompt with zero Skynet UI feedback (ComposeBox is no longer gated by `planPendingActive`; keystrokes route to a pane waiting for a "1"/"2" plan response). Blast-radius asymmetric per CLAUDE.md — Ashley loses visibility into her fleet's active state. This plan closes the window by ensuring plan mode is disallowed at the source everywhere BEFORE the detection code deletes.

Output: A modified `remote-hook-install.ts` (~40 new lines: the pure helper + two invocations inside `installStopHook`), an extended `remote-hook-install.test.ts` (~4–6 new test cases covering the three settings.json shapes + idempotency across the now-8-merge plan), one atomic commit, and one Ashley-owned prod ship + UAT gate.

Answers **MEDIUM-confidence decision #1 (installStopHook re-invocation cadence)** — CONFIRMED via `starter.ts` L554–L559 + L635–L644 + L672–L676 read: `hookInstallAttempted` Set guards per-host-per-SSH-client-lifecycle; cleared on channel end/close/error + on `releaseSshChannel` + on `onLastUnsubscriber`. **Rollout path:** Skynet container restart re-fires `installStopHook` on every host as new SSH clients acquire. Steady-state churn (dead-channel reap + reconnect) also re-fires it. Ashley's deploy discipline (container restart post-merge) is the reliable Wave 1 rollout mechanism — no new plumbing required. The helper's idempotency guarantees no duplicate entries on re-fire.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-pv-context-pct-batch-sweep-drop-capture-pane-phase-92-sibling/95-CONTEXT.md
@.planning/phases/95-pv-context-pct-batch-sweep-drop-capture-pane-phase-92-sibling/95-RESEARCH.md

# The file being modified — read the full 864 lines. Especially:
# - L339–L393 (readAndMergeHookSettings — the pattern to mirror)
# - L640–L678 (the mergePlan SIX-merge block — where the two new deny merges land)
# - L1–L60 (module docstring — extend with the two new merge entries)
@src/backend/fleet-status/remote-hook-install.ts
@src/backend/fleet-status/remote-hook-install.test.ts

# Confirms cadence — L554–L676 area is the definitive read
@src/backend/starter.ts

# Live production example of permissions.deny shape working correctly
# (~/.claude/settings.json on Skynet host currently has {"permissions":{"deny":["AskUserQuestion"]}}
# — read this to confirm the exact key path per RESEARCH §1c)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add readAndMergePermissionDeny helper + wire two merges into installStopHook</name>
  <files>src/backend/fleet-status/remote-hook-install.ts, src/backend/fleet-status/remote-hook-install.test.ts</files>
  <behavior>
    - New export `readAndMergePermissionDeny(currentSettings: Record<string, unknown>, toolName: string): { merged: Record<string, unknown>; alreadyInstalled: boolean }` — pure function, NEVER mutates input, shallow-copies at every level (mirrors readAndMergeHookSettings L339–L393 discipline).
    - Idempotency check: walk `currentSettings.permissions.deny` (if present + array). If it contains `toolName` as a string entry, return `{ merged: currentSettings, alreadyInstalled: true }` unchanged.
    - Three shape cases the helper must handle:
        1. `currentSettings.permissions` absent → create `{ permissions: { deny: [toolName] } }` merged with existing top-level keys.
        2. `currentSettings.permissions` exists but `deny` absent → create `deny: [toolName]` alongside existing `permissions.*` keys.
        3. `currentSettings.permissions.deny` exists as a non-empty array → append `toolName`, preserving every existing entry (order-preserving append at end).
    - Defensive shape: if `currentSettings.permissions` exists but is NOT a plain object (e.g. someone put an array there), treat as case 1 (overwrite the mis-shaped value). If `deny` exists but is NOT an array (e.g. `null`, string, object), treat as case 2 (create fresh `deny` array). Log a warn via `systemLogger.warn` in both defensive-overwrite cases with operation `settings_permissions_shape_unexpected` — do not throw. Rationale: settings.json is user-editable; the merge must be resilient to any prior garbage without clobbering the user's intent unrecoverably.
    - Extend `installStopHook`'s existing `mergePlan` array-driven loop at L657–L678. Two options for wiring — pick option A:
        - Option A (recommended, minimal delta): add a SECOND array-driven loop AFTER the existing SIX-hook mergePlan loop, iterating `[{ toolName: "EnterPlanMode" }, { toolName: "ExitPlanMode" }]` through `readAndMergePermissionDeny`. Same `running` variable threading. Same `allAlreadyInstalled &&= alreadyInstalled` AND-fold — extend to cover the two new merges so `allAlreadyInstalled` is TRUE only when every one of the now-EIGHT merges reports the entry already present.
        - Option B (rejected): mutate the existing mergePlan array to accept a discriminated union — extra type complexity for no benefit given the second loop is 5 lines.
    - Extend the `allAlreadyInstalled` skip-write log op (L681–L692) — the existing message says "all six hook entries already present"; update to "all eight settings entries" (six hook + two permission-deny). Update the structured log fields to include `permissionDenyToolNames: ["EnterPlanMode", "ExitPlanMode"]` alongside the existing three `remote*HookPath` fields so grep on `fleet_status_hook_install_already_present` in container logs shows the current install shape.
    - Extend the success log op (L706–L713) similarly with a `permissionDenyToolNames` field. Also add a NEW dedicated structured log line right after the success log: `systemLogger.info("Fleet-substrate: plan-mode tools denied via permissions.deny", { operation: "plan_mode_deny_applied", permissionDenyToolNames: ["EnterPlanMode", "ExitPlanMode"], enterAlreadyInstalled: <bool>, exitAlreadyInstalled: <bool> })`. This is the Wave 1 verification breadcrumb per RESEARCH §5c — Ashley greps this op across the container log to inventory fleet coverage.
    - Extend the module-level docstring block at L1–L59 to add a bullet under "Purpose" describing the Phase 95 Part A extension: two `permissions.deny` merges added alongside the existing six hook merges. Cross-reference RESEARCH §1a–§1c for the exact tool names + verified schema. Note the canonical names are `EnterPlanMode` and `ExitPlanMode` — NOT `EnterPlanModeV2Tool` / `ExitPlanModeV2Tool` (those are internal Ink implementation names per RESEARCH §1b + G13).
    - New tests in remote-hook-install.test.ts (colocated with existing `readAndMergeHookSettings` tests — mirror that describe-block's shape):
        - **Test P1 — helper: empty settings.** Input `{}`, toolName `"EnterPlanMode"`. Expect `merged.permissions.deny = ["EnterPlanMode"]`, `alreadyInstalled: false`. Assert input `{}` is unchanged (no mutation).
        - **Test P2 — helper: settings with only `hooks` key.** Input `{ hooks: { Stop: [{ hooks: [{ command: "/foo" }] }] } }`, toolName `"EnterPlanMode"`. Expect `merged.hooks` preserved byte-identical (deep-equal), `merged.permissions.deny = ["EnterPlanMode"]`, `alreadyInstalled: false`.
        - **Test P3 — helper: settings with `permissions` but no `deny`.** Input `{ permissions: { allow: ["Read"] } }`, toolName `"ExitPlanMode"`. Expect `merged.permissions.allow = ["Read"]` (preserved), `merged.permissions.deny = ["ExitPlanMode"]`, `alreadyInstalled: false`.
        - **Test P4 — helper: settings with existing `deny` containing other entries.** Input `{ permissions: { deny: ["AskUserQuestion"] } }`, toolName `"EnterPlanMode"`. Expect `merged.permissions.deny = ["AskUserQuestion", "EnterPlanMode"]` (append, order preserved), `alreadyInstalled: false`.
        - **Test P5 — helper: idempotency (entry already present).** Input `{ permissions: { deny: ["EnterPlanMode", "AskUserQuestion"] } }`, toolName `"EnterPlanMode"`. Expect `merged === currentSettings` (same reference, since alreadyInstalled shortcut), `alreadyInstalled: true`.
        - **Test P6 — helper: defensive shape recovery — `permissions` is an array.** Input `{ permissions: ["not", "an", "object"] }`, toolName `"EnterPlanMode"`. Expect warn logged with op `settings_permissions_shape_unexpected`, `merged.permissions = { deny: ["EnterPlanMode"] }` (overwrites the array), `alreadyInstalled: false`. Verify original input unchanged.
        - **Test P7 — helper: defensive shape recovery — `deny` is a string.** Input `{ permissions: { deny: "not-an-array" } }`, toolName `"EnterPlanMode"`. Expect warn logged, `merged.permissions.deny = ["EnterPlanMode"]` (overwrites the string), `alreadyInstalled: false`.
        - **Test P8 — integration: installStopHook fires all 8 merges on fresh box.** Set up MockSshChannel per existing test file's shape. Have the settings.json read return `{}`. Invoke `installStopHook`. Assert the write command's heredoc payload parses back to JSON containing BOTH `hooks.Stop`/`hooks.UserPromptSubmit`/…/`hooks.PermissionRequest` (six event arrays, per existing shape) AND `permissions.deny = ["EnterPlanMode", "ExitPlanMode"]`. Assert `settingsUpdated: true`. Assert the new `plan_mode_deny_applied` log op fires with `enterAlreadyInstalled: false, exitAlreadyInstalled: false`.
        - **Test P9 — integration: installStopHook second run is no-op (idempotency across all 8).** Set up MockSshChannel returning a settings.json that ALREADY contains the six hook entries AND `permissions.deny = ["EnterPlanMode", "ExitPlanMode"]`. Invoke `installStopHook`. Assert NO settings write command was issued (grep the mock's captured commands). Assert `settingsUpdated: false`. Assert `fleet_status_hook_install_already_present` log op fires with the extended fields (`permissionDenyToolNames`). Assert `plan_mode_deny_applied` still fires (breadcrumb belt-and-suspenders — even on idempotent no-op we want the fleet inventory op).
        - **Test P10 — integration: installStopHook merges two new deny entries into a box that has only the six hooks pre-installed** (mid-rollout scenario — box has old shape, this is the first ship of the new shape). Settings.json has all six hook entries, no `permissions` key at all. Invoke `installStopHook`. Assert the write command's payload contains the new `permissions.deny = ["EnterPlanMode", "ExitPlanMode"]` AND preserves all six existing hook entries byte-for-byte. Assert `settingsUpdated: true` (because the two new deny entries triggered `!allAlreadyInstalled`).
  </behavior>
  <action>
    Read src/backend/fleet-status/remote-hook-install.ts in full (864 lines) before editing. Especially internalize the `readAndMergeHookSettings` pattern at L339–L393 — the shallow-copy discipline (`{ ...currentSettings }`, `{ ...existingHooks }`, spread-based array construction) is load-bearing for idempotency + non-mutation guarantees per the module's contract.

    Read src/backend/fleet-status/remote-hook-install.test.ts to inventory the existing describe-block layout, MockSshChannel patterns, fixture builders, and `sshLoggerSpy`/`systemLoggerSpy` (or equivalent) infrastructure. Reuse verbatim — do not fork new patterns.

    Read starter.ts L554–L676 to confirm the `installStopHook` invocation site — the fire-and-forget `maybeInstallStopHook(host.id, channelAdapter, hookInstallAttempted, { installStopHook, systemLogger })` call at L635–L644, gated by the `hookInstallAttempted: Set<string>` at L554–L559 (per-host per-SSH-client-lifecycle guard; cleared on `releaseSshChannel` at L676 + on channel close events at L616–L618). Note this cadence in your SUMMARY — this is MEDIUM-confidence decision #1 resolved: Skynet container restart triggers Wave 1 rollout on every fleet peer's next SSH client acquire, plus steady-state channel churn triggers it too. Idempotent by design — a re-fire on an already-patched box just skips the write.

    **Write the tests FIRST** (RED gate). Tests P1–P10 per the behavior block above, added to remote-hook-install.test.ts. Run `npx vitest run src/backend/fleet-status/remote-hook-install.test.ts` and confirm P1–P10 all FAIL (the helper doesn't exist yet, and installStopHook doesn't merge deny entries yet). Save RED output to a scratchpad — the SUMMARY records this.

    **Then implement** (GREEN gate) — add `readAndMergePermissionDeny` alongside `readAndMergeHookSettings` in remote-hook-install.ts (co-located, same section comment style). Extend `installStopHook`'s merge loop with the second array-driven pass over `[{ toolName: "EnterPlanMode" }, { toolName: "ExitPlanMode" }]`. Extend the log ops per the behavior spec. Extend the module docstring.

    Re-run tests. All P1–P10 should now PASS. Run the FULL remote-hook-install.test.ts suite (including pre-existing tests) to confirm zero regressions in the existing hook-merge behavior — the second merge pass MUST NOT affect the six hook-merge results in any way.

    Do NOT modify starter.ts (the caller). Do NOT modify any file outside remote-hook-install.ts + remote-hook-install.test.ts. Do NOT modify the other 6 hook-merge entries — only ADD the two deny merges alongside.

    Grep the codebase for pre-existing references to `EnterPlanModeV2Tool` or `ExitPlanModeV2Tool` (RESEARCH §1b + G13 flags these as canonical-name traps): `grep -rn "EnterPlanModeV2\|ExitPlanModeV2Tool" src/`. Any hits are wrong per docs.claude.com — this plan uses `EnterPlanMode` + `ExitPlanMode` bare names EXACTLY. Do not perpetuate the V2 misnomer.

    Commit shape: ONE atomic commit `feat(fleet-substrate): deny EnterPlanMode/ExitPlanMode via distributor settings-patch (Phase 95 Part A) per D-CTX Part A`. Run pre-commit hooks; do NOT use `--no-verify`. If a hook fails, fix + re-stage + commit anew (never amend).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; npx vitest run src/backend/fleet-status/remote-hook-install.test.ts &amp;&amp; grep -c "readAndMergePermissionDeny\|EnterPlanMode\|ExitPlanMode\|plan_mode_deny_applied" src/backend/fleet-status/remote-hook-install.ts | grep -v '^0$' &amp;&amp; ! grep -q "EnterPlanModeV2\|ExitPlanModeV2Tool" src/backend/fleet-status/remote-hook-install.ts</automated>
  </verify>
  <done>
    remote-hook-install.ts exports `readAndMergePermissionDeny` alongside `readAndMergeHookSettings`. `installStopHook` merges `EnterPlanMode` + `ExitPlanMode` into `permissions.deny` after the existing six-hook merge. Both merges use the same idempotency AND-fold. `plan_mode_deny_applied` log op fires on every install-completion path (both write-happened and already-installed paths). Test file has 10 new tests P1–P10 all passing; existing tests unchanged. `npx vitest run src/backend/fleet-status/remote-hook-install.test.ts` green. Grep confirms zero `EnterPlanModeV2Tool` / `ExitPlanModeV2Tool` references. One atomic commit landed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Ashley UAT gate — Wave 1 prod ship + fleet-wide settings.json verification</name>
  <files>N/A — human verification checkpoint; no files modified</files>
  <what-built>
    Task 1 shipped the readAndMergePermissionDeny helper + wired two `permissions.deny` merges into `installStopHook`. Local test suite green. Atomic commit ready.

    What Ashley must now do (Wave 1's ship motion is orchestrator-owned per CLAUDE.md § Deploy convention): (a) push + build + deploy the Skynet container, (b) let the fleet-substrate distributor's per-host SSH-client-acquire path re-fire `installStopHook` on every reachable peer host, (c) grep-verify the two new deny entries landed on every peer's `~/.claude/settings.json`, (d) confirm zero real `tool_use` invocations of `EnterPlanMode`/`ExitPlanMode` in newly-created session JSONLs post-deploy. Only after (a)–(d) close does Wave 2 (Plan 02 onwards) begin.

    Wave 1 close = every reachable peer's settings.json has both deny entries + fleet-wide JSONL grep confirms plan mode is dead. Blast-radius reminder: peer-box confirmation was DEFERRED in RESEARCH (DNS resolution failure during pre-plan verification); this UAT is the concrete peer-box confirmation gate.
  </what-built>
  <how-to-verify>
    Ashley: execute these six steps in order against the current fleet. Report per-step pass/fail. Do NOT approve until steps 1–5 all pass. Step 6 is the fleet-wide inventory check that decides whether Wave 2 is safe.

    **Prerequisite:** The Task 1 commit is on `feat/tab-title-from-tmux` (or the phase's active branch). Skynet container has been pushed + built + `docker compose up -d --force-recreate skynet` on t1000 with the new image tag. Wait 60 seconds for the distributor's per-host SSH-client acquire to re-fire on every peer.

    **Check 1 — container log confirms plan_mode_deny_applied fired on every reachable peer host.**

    `docker logs skynet-backend --since 5m 2>&1 | grep '"operation":"plan_mode_deny_applied"'`

    Expected: N log lines where N = the number of reachable fleet-substrate peer hosts. Each line shows the host being patched + `enterAlreadyInstalled` + `exitAlreadyInstalled` flags. On first ship both flags should be `false`; on subsequent Skynet container restarts they'll be `true` (idempotency confirmed live).

    **Check 2 — grep every reachable peer's settings.json for the two deny entries.**

    For each peer host in the fleet's `runsFleetSubstrate=true` set (query the Skynet DB or use Ashley's mental map — the fleet is small):

    `ssh <peer-host> "grep -E 'EnterPlanMode|ExitPlanMode' ~/.claude/settings.json"`

    Expected: exactly TWO lines per peer, one for each tool name, both inside the `permissions.deny` array (visible by JSON context). If a peer returns zero lines → that peer is UN-PATCHED (distributor sweep didn't reach it for some reason — SSH transport, container hadn't restarted its client to that host, etc.). Investigate before proceeding.

    **Unreachable-peer policy — hard-defined.** If a peer is unreachable at UAT time (DNS, tailnet issue, offline), that peer's Claude Code CAN still enter plan mode post-Wave-2 with no Skynet UI feedback (backend detection code deleted in Wave 2 — the exact failure window the wave-ordering rule exists to prevent). Acceptable Wave 2 authorization requires ONE of: (a) 100% reachable-peer coverage verified via Check 2 grep; (b) any unreachable peers hand-patched via SSM / direct SSH before Wave 2 authorization; (c) Ashley explicitly names the specific unreachable peers she's accepting as knowingly-Skynet-blind until they reconnect. Do NOT accept partial-fleet coverage silently as a default.

    **Check 3 — verify the `permissions.deny` merge preserved existing entries.**

    On any peer that had pre-existing `permissions.deny` entries (e.g. Skynet host itself which has `AskUserQuestion`):

    `ssh <peer-host> "cat ~/.claude/settings.json | jq -r '.permissions.deny[]'"`

    Expected: pre-existing entries (like `AskUserQuestion`) STILL PRESENT alongside the two new ones. If any pre-existing entry is missing, the merge helper had a shallow-copy bug — HALT + investigate.

    **Check 4 — verify existing hook entries preserved.**

    On any peer, sanity-check the six hook merges are still intact after the deny extension:

    `ssh <peer-host> "cat ~/.claude/settings.json | jq '.hooks | keys'"`

    Expected: `["PermissionRequest", "PreToolUse", "Stop", "StopFailure", "UserPromptSubmit"]` (existing per Phase 62 shape). Six merge targets, five event keys (Stop takes two entries).

    **Check 5 — grep session JSONLs for actual plan-mode tool_use invocations (post-deploy).**

    Give the fleet 30 minutes of normal use post-deploy so peer boxes accumulate fresh session JSONLs. Then:

    `ssh <peer-host> "find ~/.claude/projects -name '*.jsonl' -mmin -30 -print0 | xargs -0 grep -l '\"type\":\"tool_use\"' | xargs grep -c 'EnterPlanMode\\|ExitPlanMode'"`

    (Adjust find flags per peer's actual JSONL layout — the RESEARCH sample was at `~/.claude/projects/-home-ubuntu-skynet-tabitha/*.jsonl`.)

    Expected: zero matches across all peers. Per RESEARCH §G13, `deferred_tools_delta` still LISTS these names in `addedNames` (harmless schema-registration noise) — do NOT grep for `deferred_tools_delta`; grep specifically for `"type":"tool_use"` blocks with those names. Zero matches = plan mode is truly dead fleet-wide = Wave 2 is safe.

    **Check 6 — startup-warning sanity check.**

    Spawn a fresh Claude Code session on any patched peer:

    `ssh <peer-host> "claude --help 2>&1 | head -30"` (or invoke however Ashley's usual harness spawns Claude).

    Expected: NO startup warning about unknown deny-rule tool names. Per RESEARCH §1a docs.claude.com note, unknown tool names emit a startup warning as a typo safety net; `EnterPlanMode` + `ExitPlanMode` are known canonical names so no warning should fire. Any warning = names are wrong = HALT + investigate.

    **Reply to close this gate:** either `all 6 checks PASS — Wave 2 authorized` OR describe which check(s) failed with the specific host + evidence. If any check fails, the gate stays open — Wave 2 does NOT start.
  </what-built>
  <how-to-verify>
    (See what-built block above for the six-step checklist.)
  </how-to-verify>
  <verify>
    <human-check>Ashley executes checks 1–6 against t1000 + peer hosts, reports pass/fail per step.</human-check>
  </verify>
  <done>
    Ashley has replied `all 6 checks PASS — Wave 2 authorized`, OR the failures are documented in the phase's issue log for gap-closure planning. Under no circumstance does this task self-approve — it MUST wait for a human reply. Wave 2 (Plans 02–05) does not start until this gate closes.
  </done>
  <resume-signal>Reply `all 6 checks PASS — Wave 2 authorized` to open Wave 2, OR describe failure(s) and Wave 1 reopens for gap-closure planning.</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run src/backend/fleet-status/remote-hook-install.test.ts` — all tests pass, includes 10 new Phase 95 Part A cases (P1–P10).
- `npx vitest run` — full suite green, zero regressions in unrelated modules.
- `grep -c readAndMergePermissionDeny src/backend/fleet-status/remote-hook-install.ts` returns positive.
- `grep -c "plan_mode_deny_applied" src/backend/fleet-status/remote-hook-install.ts` returns at least 1 (the new log op exists).
- No changes to any file outside remote-hook-install.ts + remote-hook-install.test.ts.
- No `EnterPlanModeV2` / `ExitPlanModeV2Tool` string appears anywhere in the diff.
- Ashley UAT gate (Task 2) closes with `all 6 checks PASS` before Wave 2 starts.
</verification>

<success_criteria>
- readAndMergePermissionDeny helper mirrors readAndMergeHookSettings' contract byte-for-byte in idempotency + shallow-copy discipline.
- installStopHook now merges 8 total entries (6 hooks + 2 permission-deny) with a single AND-fold controlling the write-skip decision.
- Rollout mechanism confirmed: MEDIUM-confidence decision #1 resolved — `installStopHook` re-fires on every SSH-client acquire per host, cleared on channel close / releaseSshChannel / onLastUnsubscriber. Container restart is the reliable Wave 1 trigger; steady-state channel churn also picks it up. Documented in SUMMARY.
- Ashley UAT gate confirms every reachable peer's settings.json has both new deny entries + zero real tool_use invocations of the plan-mode tools in fresh JSONLs. Only then is Wave 2 authorized.
- Wave-ordering hard-dependency achieved: no Wave 2 code lands in prod until Wave 1's fleet-wide UAT closes.
</success_criteria>

<output>
Create `.planning/phases/95-pv-context-pct-batch-sweep-drop-capture-pane-phase-92-sibling/95-01-SUMMARY.md` when done. Include:
- MEDIUM-confidence decision #1 resolution: installStopHook cadence — confirmed one-shot per host per SSH-client lifecycle, guarded by `hookInstallAttempted` Set; cleared on channel end/close/error, on `releaseSshChannel`, on `onLastUnsubscriber`. Container restart re-fires on every peer.
- Test count added (10 new) + total remote-hook-install.test.ts count post-change.
- Exact commit hash of the atomic ship commit.
- Any defensive-shape recovery cases encountered on real peer settings.json during UAT (if any peer had a mis-shaped `permissions` key that the helper had to fix up — expected to be rare).
- Ashley UAT gate result — per-check pass/fail + un-patched peer set (if any).
- Wave 2 authorization signal (yes/no + timestamp).
</output>
