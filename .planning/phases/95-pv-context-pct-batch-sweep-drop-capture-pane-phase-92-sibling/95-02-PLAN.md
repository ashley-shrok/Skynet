---
phase: 95-pv-context-pct-batch-sweep-drop-capture-pane-phase-92-sibling
plan: 02
type: execute
wave: 2
depends_on: ["95-01"]
files_modified:
  - src/backend/claude-session/plan-pending-parser.ts
  - src/backend/claude-session/plan-pending-parser.test.ts
  - src/backend/ssh/plan-file-fetch.ts
  - src/backend/ssh/plan-file-fetch.test.ts
  - src/backend/claude-session/context-pct-parser.ts
  - src/backend/claude-session/context-pct-parser.test.ts
  - src/backend/claude-session/claude-session-server.ts
  - src/backend/database/routes/pretty-view-fetch-host-file.ts
  - src/backend/matrix/matrix-admin-narrow.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Three whole source files are deleted (plan-pending-parser.ts, plan-file-fetch.ts, context-pct-parser.ts) alongside their three co-located test files. Zero surviving imports of any of the three modules or their exports (`isPlanPending`, `parsePlanFilePath`, `fetchPlanFile`, `MAX_PLAN_BYTES`, `utf8SafeCutoff`, `__resetHomeDirCacheForTest`, `parseContextPct`) anywhere in `src/`."
    - "claude-session-server.ts drops its three imports of these modules (L38 context-pct-parser, L49 plan-pending-parser, L50 plan-file-fetch) — imports section cleaner by 3 lines."
    - "claude-session-server.ts drops the L4796–L4897 JSONL-scanner plan-pending emit branch (~102 lines) and the L4311–L4366 declarations block (~55 lines) — this is the JSONL-based `{type:\"plan_pending\", pending}` state machine, orthogonal to the L7569+ pane-scrape path which Plan 03 handles."
    - "claude-session-server.ts drops the L4509–L4519 + L5353–L5359 teardown-reset stanzas that clear the JSONL-scanner state (the plan-pending state variables no longer exist)."
    - "Comment references in pretty-view-fetch-host-file.ts (L116–L119, L246) and matrix-admin-narrow.ts (L7) that cite `plan-file-fetch.ts` as a pattern precedent are updated to cite `pretty-view-fetch-host-file.ts` itself (per RESEARCH G5+G6) — no compile break, no behavior change, just docblock accuracy."
    - "`npx tsc --noEmit` passes after Plan 02's atomic backend commit. The commit deletes the three whole modules + all import references + the L4311+/L4509+/L5353+/L4796+ backend site groups + the L7569+ pane-scrape block simultaneously — no intermediate state exists where a deleted module is still referenced from surviving backend code. Frontend + wire-type deletions land in Plan 03; the intermediate state between Plan 02 and Plan 03 has a green backend build and a red frontend build (expected, closed by Plan 03 within the same Wave 2)."
    - "Existing tests remain green through the mass deletion (fleet rule — never leave tests failing). Regression risk: `context-pct-from-jsonl.test.ts`, `dormant-poll.test.ts`, and `claude-session-server.contextpct-dual-write.test.ts` MUST still pass — they exercise infrastructure Plan 02 does not touch."
  artifacts:
    - path: "src/backend/claude-session/claude-session-server.ts"
      provides: "backend with all plan-pending state machine + capture-pane exec + plan-file SFTP fetch surgically removed; L7620+ context_pct emit block simplified per RESEARCH §4f (null pct now emits, no guard)"
      contains: "readContextPctFromJsonl"
    - path: "src/backend/database/routes/pretty-view-fetch-host-file.ts"
      provides: "comment refs updated to self-reference the SFTP wrapper pattern (no code changes)"
    - path: "src/backend/matrix/matrix-admin-narrow.ts"
      provides: "comment ref updated to cite pretty-view-fetch-host-file.ts as inline-cast precedent (no code changes)"
  key_links:
    - from: "claude-session-server.ts contextPctTimer body"
      to: "readContextPctFromJsonl (unchanged, still primary)"
      via: "single tail-based JSONL read per WS per 3s"
      pattern: "readContextPctFromJsonl"
    - from: "claude-session-server.ts"
      to: "no plan-pending code path anywhere"
      via: "grep 0 hits for plan-pending / planPending / plan_pending / fetchPlanFile / MAX_PLAN_BYTES"
      pattern: "planPending|plan_pending|fetchPlanFile|MAX_PLAN_BYTES"
---

<objective>
Delete every backend plan-pending code path and the capture-pane exec + parseContextPct fallback from claude-session-server.ts. Delete three whole source modules (plan-pending-parser.ts, plan-file-fetch.ts, context-pct-parser.ts) + their three co-located test files. Update two comment-only references elsewhere in the codebase (RESEARCH G5+G6). Ship as one atomic commit — the entire backend plan-pending stack goes to zero in a single change so the build never enters a broken half-state.

**Split rationale between Plan 02 (backend) and Plan 03 (frontend):**

The RESEARCH §5d 9-commit map proposes plan-pending backend and frontend as separate commits. Plan 02/03 preserve that atomicity in commit history — this plan produces TWO atomic commits inside one PLAN.md (permitted by executor when Task 1 and Task 2 form a natural pair): Task 1 = whole-module deletions + claude-session-server.ts surgery; Task 2 = comment-reference sanity edits.

The frontend deletions (PrettyView.tsx, ComposeBox.tsx, claude-session-api.ts, PlanPendingBubble.tsx, docblocks) go to Plan 03 because they touch a different subsystem (React UI) with its own regression surface and warrant their own atomic commit. Plan 02 (backend) + Plan 03 (frontend) can execute back-to-back in Wave 2 because they modify disjoint file sets — Plan 03's `files_modified` list contains no files touched by Plan 02.

**Why not delete backend + frontend in one plan?** Two reasons: (a) commit atomicity — RESEARCH §5d wants a clean per-file-group git-log narrative, (b) context budget — plan-pending sites span 95 backend + 15 PrettyView + 14 ComposeBox + 6 api sites (130 total); combining pushes the executor past 50% context and quality degrades on a delete-heavy plan where mechanical accuracy matters.

Purpose: With plan mode disallowed at the source (Wave 1 shipped + verified), every plan-pending detection code path is dead. Deleting it means no more per-tick `tmux capture-pane -p -t <session>` exec on the shared Skynet-host SSH connection (that alone is measurable exec-pressure relief) AND no more up-to-315-line SFTP side-channel maintenance burden AND no more `parseContextPct` pane-scrape fallback (JSONL is authoritative — accepted per CONTEXT.md § Verification and RESEARCH §3g).

Output: One atomic commit that produces a backend with zero plan-pending code. All existing tests still green (fleet rule). Wave 2 build stays healthy — no half-state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/95-pv-context-pct-batch-sweep-drop-capture-pane-phase-92-sibling/95-CONTEXT.md
@.planning/phases/95-pv-context-pct-batch-sweep-drop-capture-pane-phase-92-sibling/95-RESEARCH.md
@.planning/phases/95-pv-context-pct-batch-sweep-drop-capture-pane-phase-92-sibling/95-01-SUMMARY.md

# Files to DELETE entirely — read once each to confirm no exports are consumed
# outside the enumerated sites in RESEARCH §3a:
@src/backend/claude-session/plan-pending-parser.ts
@src/backend/ssh/plan-file-fetch.ts
@src/backend/claude-session/context-pct-parser.ts

# The 95-line-precise deletion inventory site — read line ranges L1–L60, L153–L165,
# L4166, L4311–L4366, L4509–L4519, L4796–L4897, L5238, L5353–L5359, L7046–L7098,
# L7407–L7417, L7569–L7793. Total ~430 lines targeted for surgical deletion in this
# plan (plus L7569–L7793 which drops another ~152 lines via RESEARCH §3b).
@src/backend/claude-session/claude-session-server.ts

# Files with comment-only references to update (RESEARCH G5+G6):
@src/backend/database/routes/pretty-view-fetch-host-file.ts
@src/backend/matrix/matrix-admin-narrow.ts

# Tests that MUST stay green through the deletion — DO NOT touch these files but
# DO run them post-deletion as regression gate:
@src/backend/claude-session/claude-session-server.contextpct-dual-write.test.ts
@src/backend/claude-session/context-pct-from-jsonl.test.ts
@src/backend/claude-session/dormant-poll.test.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Backend plan-pending stack deletion — three whole modules + surgical claude-session-server.ts edits</name>
  <files>src/backend/claude-session/plan-pending-parser.ts, src/backend/claude-session/plan-pending-parser.test.ts, src/backend/ssh/plan-file-fetch.ts, src/backend/ssh/plan-file-fetch.test.ts, src/backend/claude-session/context-pct-parser.ts, src/backend/claude-session/context-pct-parser.test.ts, src/backend/claude-session/claude-session-server.ts</files>
  <action>
    **Step 0 — Pre-flight grep confirmations (do these EXACTLY as written; RESEARCH §3a caution flags depend on these being zero-hits).**

    Run each of these and record the count in your scratchpad:
    - `grep -rn "plan-pending-parser\|isPlanPending\|parsePlanFilePath" src/ | grep -v claude-session-server.ts | grep -v plan-pending-parser`
      Expected: this counts callers OTHER than claude-session-server.ts + the module itself. Should be ZERO — anything else is scope creep and must be resolved before deletion (surface back to orchestrator).
    - `grep -rn "plan-file-fetch\|fetchPlanFile\|MAX_PLAN_BYTES\|utf8SafeCutoff" src/ | grep -v claude-session-server.ts | grep -v plan-file-fetch | grep -v pretty-view-fetch-host-file.ts | grep -v matrix-admin-narrow.ts`
      Expected: ZERO. `pretty-view-fetch-host-file.ts` + `matrix-admin-narrow.ts` are the comment-only references (Task 2 handles), and they're intentionally excluded here.
    - `grep -rn "context-pct-parser\|parseContextPct" src/ | grep -v claude-session-server.ts | grep -v context-pct-parser`
      Expected: ZERO. If context-pct-parser.ts has any other consumer, HALT — do not delete blindly.

    If ANY of these grep counts is non-zero for the wrong file, you've discovered a caller RESEARCH missed. Do NOT proceed with deletion — return control to the orchestrator with the discovered caller so the plan can widen scope safely.

    **Step 1 — Delete the six files whole.**

    - `rm src/backend/claude-session/plan-pending-parser.ts`
    - `rm src/backend/claude-session/plan-pending-parser.test.ts`
    - `rm src/backend/ssh/plan-file-fetch.ts`
    - `rm src/backend/ssh/plan-file-fetch.test.ts`
    - `rm src/backend/claude-session/context-pct-parser.ts`
    - `rm src/backend/claude-session/context-pct-parser.test.ts`

    Rationale per RESEARCH §3a: three whole source modules + three co-located test suites. Plan mode is dead post-Wave-1; parseContextPct pane-scrape fallback is dropped per CONTEXT.md accepted UX regression (RESEARCH §3g); all callers are confined to claude-session-server.ts.

    **Step 2 — Surgical edits in claude-session-server.ts (94 hit sites per RESEARCH §3b enumeration).**

    Read the file's L1–L165 header block FIRST to internalize the WS-frame-types docblock at L153–L165 — RESEARCH G12 flags this as the wire contract; deletions here must be coherent, not just mechanically hole-punched.

    Then execute these edits in file-position order (top-down; keeps line numbers stable in your head):

    - **Imports (L38, L49, L50):** DELETE these three import lines. All three import from now-deleted modules; without deletion the build breaks.

    - **WS-frame docblock (L163–L164):** DELETE the two lines documenting `{type: "plan_pending", pending}` and the raw_keystrokes client→server frame. Re-flow the surrounding numbered list so the enumeration stays coherent — do NOT leave a gap or an orphaned bullet. Per RESEARCH G12: this docblock IS the wire contract; leave it clean.

    - **L4166 comment edit:** the `dormantLastEmitted` comment says "mirrors planPendingLastSerialized" — EDIT to remove the reference. Keep the "change-only emit guard" phrase; drop the specific name. Suggested rewrite: `let dormantLastEmitted: boolean | null = null;       // change-only emit guard`.

    - **Declarations block L4311–L4366 (~55 lines):** DELETE all of it. This is the JSONL-scanner state (`pendingPlans`, `pendingPlansLastSerialized`, `planPendingLastSerialized`, `planPendingContentByPath`, `planPendingFetchInFlightForPath`, `planPendingWindowToken`) + all its docblocks. Preserve any UNRELATED declarations that happen to be interleaved — read carefully, this region has 55 lines of docblocks + 6 declaration lines; delete surgically. If in doubt, re-read RESEARCH §3b's line-precise breakdown before deleting.

    - **Teardown reset L4509–L4519 (~11 lines):** DELETE. This block clears the plan-pending state on `teardownPane()`. All 6 `.clear() / = "null" / += 1` operations reference now-deleted state. Preserve any UNRELATED teardown ops in the surrounding function — this is a surgical excision, not a scorched-earth pass.

    - **JSONL scanner emit block L4796–L4897 (~102 lines):** DELETE. Per RESEARCH §3b:
        - L4796–L4816 docblock
        - L4817–L4826 scan preamble
        - L4827–L4862 the `if (obj?.type === "assistant" ...) { for block of content } else if (obj?.type === "user" ...) { for block }` branch that maintains `pendingPlans` Map
        - L4863–L4867 "Only one ExitPlanMode can be pending" docblock
        - L4868 `pendingIter = pendingPlans.values().next()`
        - L4869–L4876 shape-widening comment
        - L4877–L4883 `currentPending` compose
        - L4884–L4897 `planSerialized` compare + `ws.send({type: "plan_pending", ...})` emit
      Delete the entire ~102-line block. Surrounding JSONL-scanner logic (context_pct handling, dormant handling, etc.) MUST be preserved byte-for-byte.

    - **L5238 comment edit:** `// / backgroundedAgents / plan_pending / asideText — false-alarm recovery` → EDIT to drop the `/ plan_pending` mention; keep `backgroundedAgents` and `asideText`.

    - **Second teardown site L5353–L5359 (~7 lines):** DELETE the 3 `.clear() / = "null" / += 1` lines that reference plan-pending state, EDIT the intervening comment to drop plan-pending refs. Preserve UNRELATED teardown ops.

    - **raw_keystrokes handler L7046–L7098 (~53 lines):** DELETE. Per RESEARCH G3, post-Part-A this handler has zero client senders (PlanPendingBubble was the only source, and it deletes in Plan 03 — but the frontend can't send raw_keystrokes to the deleted handler after Plan 02 ships alone in Wave 2 either, so backend-first deletion is safe). Delete the L7046–L7058 docblock + the entire `if (msg.type === "raw_keystrokes") { ... }` block at L7059–L7097. Any surrounding `if (msg.type === ...) { ... }` message handlers stay unchanged.

    - **Docblock L7407–L7417 closure-state summary:** L7414–L7416 references six plan-pending state variables in a state-summary comment. EDIT to drop all six references. Leave the surrounding comment structure intact.

    - **contextPctTimer callback L7569–L7793 (~215 lines targeted; ~152 lines to delete):**
        - **L7580–L7589 preamble:** EDIT — keep the `sessionFileSnapshot = currentSessionFile;` line; DELETE the L7585–L7588 TODO comment referencing plan-pending.
        - **L7602–L7612 capture-pane exec:** DELETE. This is the `output = await execCommand(connSnapshot, captureCmd)` block where captureCmd is `tmux capture-pane -p -t '<session>'`. Per CONTEXT.md § Verification and D-CTX Part B "tmux capture-pane -p -t <session> in the contextPctTimer callback → delete." Note: G8 clarifies that ASIDE subsystem's independent capture-pane calls at L7982 and L8013 (with `-S -200` flag) STAY — they are OUT of scope. Grep to confirm before deletion: `grep -n "capture-pane" src/backend/claude-session/claude-session-server.ts` — expect this block at L7602-ish and the aside subsystem at L7982+/L8013+. Delete ONLY the L7602-ish block.
        - **L7613–L7619 parseContextPct fallback:** DELETE. `if (pct === null && output !== "") pct = parseContextPct(output);` — the pane-scrape fallback the researcher confirmed is safe to drop (RESEARCH §3g). JSONL becomes sole source; null pct emits per Step below.
        - **L7620–L7635 context_pct emit block:** KEEP but SIMPLIFY per RESEARCH §4f. Remove the `if (pct !== null)` guard so a null pct also emits. New shape:
            ```
            setContextPct(activeHostId, activeTmuxSession, pct);
            if (!stopped && ws.readyState === WebSocket.OPEN) {
              ws.send(JSON.stringify({ type: "context_pct", pct }));
            }
            ```
          The stopped/readyState guard stays. The null-pct passthrough is the intentional behavior change — frontend renders a loading placeholder for the first-few-seconds-of-fresh-session case (accepted UX regression per CONTEXT.md).
          **G10 warning:** `contextpct-dual-write.test.ts` Test 8 grep-verifies the emit byte pattern. After this edit, the test's regex may fail. Anticipate + fix Test 8 in Step 3 (regression validation) — do NOT touch the test in Task 1 speculatively.
        - **L7636–L7717 pane-scrape plan-pending block:** DELETE entirely. All `isPending / planFilePath / currentPending / pendingSerialized / ws.send({type: "plan_pending"})` code goes.
        - **L7719–L7793 async SFTP fetch dispatch:** DELETE entirely. `if (isPending && planFilePath && sshConn && !cached && !inFlight) { planPendingFetchInFlightForPath.add(); const targetPath = ...; fetchPlanFile(...).then(...) }` — all of it.
        - **L7796+ dormancy stat block (D1, D2, D3):** LEAVE UNCHANGED. RESEARCH §3b explicitly flags this as OUT OF SCOPE for Phase 95. Preserve verbatim. Same for the sentinel probe at L7892+ and the aside timer at L8014.

    **Step 3 — Immediate build + test regression validation.**

    Run:
    - `cd /home/ubuntu/skynet-tina && npx tsc --noEmit` — expect zero new type errors. If any surface, the deletion missed a caller — inspect and either add to Step 2 or return to orchestrator.
    - `npx vitest run src/backend/claude-session/` — expect all remaining tests green EXCEPT possibly `contextpct-dual-write.test.ts` Test 8 (per G10). If Test 8 fails on the byte-pattern regex for the simplified emit block, update Test 8's regex to match the new shape — the test's INTENT is to grep-verify context_pct emission still exists; that intent is preserved, only the byte pattern near the emit line changed. Update the regex minimally (do NOT weaken the assertion; do NOT delete the test).
    - `npx vitest run src/backend/` — full backend regression. Expect zero unrelated failures.
    - `npx vitest run src/ui/` — full frontend regression. Expected to FAIL because frontend still imports `PlanPendingEvent` from claude-session-api.ts + still references `plan_pending` frames in PrettyView.tsx + still has PlanPendingBubble. These failures are EXPECTED and get fixed in Plan 03. Record the specific failing test names in your scratchpad — Plan 03's SUMMARY confirms they all turn green.

    **BUT** the build must still SUCCEED — a red test suite is different from a red compile. `npx tsc --noEmit` covering src/ui/ MUST pass. If it doesn't (e.g. a UI file directly imports a now-deleted backend module — which RESEARCH did not surface), HALT + investigate.

    **Step 4 — Atomic commit.**

    Commit shape: `refactor(claude-session): delete plan-pending stack + drop capture-pane exec + parseContextPct fallback (Phase 95 Part B backend)`

    Longer body: enumerate the 3 whole-module deletions + the 3 test-file deletions + the ~450 lines removed from claude-session-server.ts. Reference CONTEXT.md § Decisions Part B + RESEARCH §3a–§3b.

    Run pre-commit hooks. Fix any failure + re-stage + commit anew (never amend).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; test ! -f src/backend/claude-session/plan-pending-parser.ts &amp;&amp; test ! -f src/backend/ssh/plan-file-fetch.ts &amp;&amp; test ! -f src/backend/claude-session/context-pct-parser.ts &amp;&amp; npx tsc --noEmit &amp;&amp; grep -v '^[[:space:]]*//\|^[[:space:]]*\*' src/backend/claude-session/claude-session-server.ts | grep -cE "planPending|plan_pending|fetchPlanFile|MAX_PLAN_BYTES|isPlanPending|parsePlanFilePath|parseContextPct" | grep -qE "^0$" &amp;&amp; npx vitest run src/backend/</automated>
  </verify>
  <done>
    Three modules deleted (plan-pending-parser.ts, plan-file-fetch.ts, context-pct-parser.ts) + three test files deleted. claude-session-server.ts has zero non-comment references to planPending/plan_pending/fetchPlanFile/MAX_PLAN_BYTES/isPlanPending/parsePlanFilePath/parseContextPct. Zero surviving `tmux capture-pane -p -t '<session>'` exec in the contextPctTimer callback (aside subsystem's `-S -200` capture-pane calls at L7982+/L8013+ preserved). Simplified context_pct emit block passes null through. `npx tsc --noEmit` green. `npx vitest run src/backend/` green (Test 8 in contextpct-dual-write.test.ts regex updated if needed). Atomic commit landed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update comment-only cross-references in pretty-view-fetch-host-file.ts + matrix-admin-narrow.ts</name>
  <files>src/backend/database/routes/pretty-view-fetch-host-file.ts, src/backend/matrix/matrix-admin-narrow.ts</files>
  <action>
    Per RESEARCH G5 + G6: two files carry stale comment cross-references to `plan-file-fetch.ts` after Task 1 deletes that module. Update the references so future readers land on a valid precedent file.

    **File 1: src/backend/database/routes/pretty-view-fetch-host-file.ts**

    Read L110–L130 and L240–L250 to see the comment context. Existing comments (verified by RESEARCH):
    - L116–L119: "SFTP promise wrappers (mirrors plan-file-fetch.ts L122-163 verbatim). Copied locally rather than exported from plan-file-fetch.ts because the plan-file-fetch SftpLike type is module-private."
    - L246: another plan-file-fetch reference (RESEARCH flagged L246 exists but didn't quote it — inspect and update).

    Edit L116–L119 to (adjust exact wording to match the surrounding tone):
    "SFTP promise wrappers (self-contained here — this file is now the canonical SFTP-wrapper precedent in the codebase; the predecessor plan-file-fetch.ts was retired in Phase 95 Part B alongside plan-mode deprecation)."

    Edit L246 similarly if it also references plan-file-fetch.ts — cite this file's own patterns (or `matrix-admin-narrow.ts`) as the surviving precedent.

    Behavior unchanged. The SFTP wrapper functions themselves stay — they're used by an unrelated feature (host-file fetch, the other SFTP consumer per RESEARCH G5).

    **File 2: src/backend/matrix/matrix-admin-narrow.ts**

    Read L1–L20 to see the docblock. Existing comment (verified by RESEARCH G6): L7 comment says "See `plan-file-fetch.ts:297` for the inline-cast precedent."

    Edit L7 to cite a surviving precedent. Recommended: point at `src/backend/database/routes/pretty-view-fetch-host-file.ts` as the surviving SFTP-wrapper + inline-cast precedent — Task 1 above just made that file the canonical surviving example. Suggested rewrite:
    `// See src/backend/database/routes/pretty-view-fetch-host-file.ts for the inline-cast + SFTP-wrapper precedent (Phase 95 Part B retired plan-file-fetch.ts).`

    **Grep sanity check across the rest of the codebase.**

    `grep -rn "plan-file-fetch" src/ --include='*.ts' --include='*.tsx'` — expect ZERO hits after Task 1 + Task 2. Any lingering hit is a stale ref RESEARCH missed; update it to cite the same alternative precedent.

    **Commit shape (second atomic commit for Plan 02):** `refactor: update comment cross-references after plan-file-fetch retirement (Phase 95 Part B tidy)`.

    Per Plan 02's split rationale (see objective), this ships as a SEPARATE atomic commit from Task 1 for clean git-log narrative — Task 1's commit is the substantive deletion; Task 2's is the housekeeping edit that keeps the codebase's internal cross-references accurate.

    Verify build still green: `npx tsc --noEmit` — expect zero diffs from Task 1's state.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; ! grep -rn "plan-file-fetch" src/ --include='*.ts' --include='*.tsx' &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>
    pretty-view-fetch-host-file.ts + matrix-admin-narrow.ts comment refs updated. Zero occurrences of `plan-file-fetch` string anywhere in `src/`. `npx tsc --noEmit` green. Second atomic commit landed.
  </done>
</task>

</tasks>

<verification>
- All three source module files gone: `test ! -f src/backend/claude-session/plan-pending-parser.ts && test ! -f src/backend/ssh/plan-file-fetch.ts && test ! -f src/backend/claude-session/context-pct-parser.ts`.
- All three co-located test files gone.
- `grep -rn "plan-pending-parser\|plan-file-fetch\|context-pct-parser\|isPlanPending\|parsePlanFilePath\|fetchPlanFile\|MAX_PLAN_BYTES\|parseContextPct" src/ --include='*.ts' --include='*.tsx'` returns ZERO hits after both tasks.
- `npx tsc --noEmit` — zero new type errors (existing type errors from unrelated files, if any, unchanged).
- `npx vitest run src/backend/` — full backend suite passes.
- `grep -c "tmux capture-pane -p -t '\\\\\\${activeTmuxSession}'" src/backend/claude-session/claude-session-server.ts` returns 0 (the contextPctTimer capture-pane exec is gone). Aside subsystem capture-pane calls at L7982+/L8013+ preserved (verify by `grep -c "capture-pane -p -S -200" src/backend/claude-session/claude-session-server.ts` still returns 2 or the appropriate pre-plan count).
- Two atomic commits landed (Task 1 = substantive deletion; Task 2 = comment refs).
</verification>

<success_criteria>
- Backend has zero plan-pending code paths — the entire subsystem is retired from the codebase.
- `tmux capture-pane -p -t <session>` exec eliminated from the per-3s contextPctTimer callback (measurable exec-pressure relief on the shared Skynet-host SSH connection even before Plan 04's Part C batch collapse ships).
- parseContextPct pane-scrape fallback dropped — JSONL is now authoritative for context_pct (accepted UX regression per CONTEXT.md § Verification and RESEARCH G4).
- Wave 2 build stays healthy — no half-state; the frontend regression tests will fail (expected) but the backend + typecheck are green.
- Frontend deletion (Plan 03) can now proceed without inter-dependency concerns.
</success_criteria>

<output>
Create `.planning/phases/95-pv-context-pct-batch-sweep-drop-capture-pane-phase-92-sibling/95-02-SUMMARY.md` when done. Include:
- List of deleted files (6 total) + their pre-deletion line counts.
- Total lines removed from claude-session-server.ts (approximate).
- Any callers RESEARCH §3a missed that the pre-flight grep surfaced (expected: zero).
- Confirmation that the aside subsystem's `capture-pane -p -S -200` calls at L7982+/L8013+ are UNCHANGED (grep count before + after).
- Whether Test 8 in `contextpct-dual-write.test.ts` needed regex adjustment (yes/no + minimal edit description).
- Explicitly-recorded failing frontend tests to be resolved in Plan 03 (name each + line).
- Two atomic commit hashes (Task 1 substantive deletion + Task 2 comment refs).
</output>
