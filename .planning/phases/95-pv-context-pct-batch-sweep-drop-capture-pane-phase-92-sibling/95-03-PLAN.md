---
phase: 95-pv-context-pct-batch-sweep-drop-capture-pane-phase-92-sibling
plan: 03
type: execute
wave: 2
depends_on: ["95-02"]
files_modified:
  - src/ui/features/pretty-view/PlanPendingBubble.tsx
  - src/ui/features/pretty-view/ComposeBox.plan-pending-disable.test.tsx
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-view/ComposeBox.tsx
  - src/ui/api/claude-session-api.ts
  - src/ui/features/pretty-view/PrettyViewLoadingOverlay.tsx
  - src/ui/features/pretty-view/RelayInboundBubble.tsx
  - src/ui/features/pretty-view/AsideBubble.tsx
  - src/ui/features/pretty-view/SessionHoldingOverlay.tsx
  - src/ui/features/pretty-view/WaitingBubble.tsx
  - src/ui/features/pretty-view/use-auto-scroll.ts
  - src/ui/features/relay-room-pane/AgentBadgeWithAppendage.tsx
  - src/ui/AppShell.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "PlanPendingBubble.tsx is deleted whole (only rendered from PrettyView.tsx L3751; no other consumers per RESEARCH §3a)."
    - "ComposeBox.plan-pending-disable.test.tsx is deleted whole (tests the planPendingActive OR-in prop chain which this plan removes)."
    - "PrettyView.tsx drops the 15 line-precise plan-pending sites enumerated in RESEARCH §3c."
    - "ComposeBox.tsx drops the 14 planPendingActive prop-chain sites enumerated in RESEARCH §3d plus duplicates at L3219+."
    - "claude-session-api.ts drops the PlanPendingEvent type + docblock + union-member + the raw_keystrokes wire type."
    - "Nine files carry comment-only plan-pending references (per RESEARCH §3e); each is edited to drop the reference without breaking the surrounding docblock's meaning."
    - "npx tsc --noEmit passes after this plan (frontend was red at Plan 02 end; Plan 03 closes it)."
    - "The full frontend test suite is green after this plan — every test flagged in Plan 02's SUMMARY as expected-to-fail turns green."
  artifacts:
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "PrettyView with zero plan-pending state, zero plan_pending case handler, zero PlanPendingBubble render, zero planPendingActive prop-drill"
    - path: "src/ui/features/pretty-view/ComposeBox.tsx"
      provides: "ComposeBox with zero planPendingActive prop, zero OR-in guards referencing it"
    - path: "src/ui/api/claude-session-api.ts"
      provides: "wire-type file with PlanPendingEvent + raw_keystrokes types removed"
  key_links:
    - from: "PrettyView.tsx"
      to: "no PlanPendingBubble"
      via: "conditional render deleted; import deleted"
      pattern: "PlanPendingBubble"
    - from: "ComposeBox.tsx"
      to: "no planPendingActive prop"
      via: "prop + destructure + all OR-in usages removed"
      pattern: "planPendingActive"
    - from: "claude-session-api.ts"
      to: "no PlanPendingEvent in discriminated union"
      via: "type export removed; union-member removed"
      pattern: "PlanPendingEvent|plan_pending"
---

<objective>
Frontend companion to Plan 02. Delete the entire plan-pending UI stack — PlanPendingBubble.tsx and its co-located test file (whole), the PrettyView.tsx state + handlers + conditional render, the ComposeBox.tsx planPendingActive prop-chain, the claude-session-api.ts wire types, and 9 comment-only references in sibling files. After Plan 03 lands, tsc --noEmit and the full frontend test suite are both green.

Purpose: Plan 02 shipped the backend deletion but the wire-type file (claude-session-api.ts) is still shared with the frontend; PrettyView.tsx's plan_pending case is still trying to consume a frame the backend no longer emits; PlanPendingBubble.tsx still renders when the (now-always-null) state is checked. This plan closes the loop — the frontend has zero references to plan-pending anywhere. Wave 2 build is fully green.

Output: Two atomic commits. Task 1 = substantive UI deletion (one commit). Task 2 = docblock housekeeping across 9 sibling files (one commit). Split keeps the git-log narrative clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/95-pv-context-pct-batch-sweep-drop-capture-pane-phase-92-sibling/95-CONTEXT.md
@.planning/phases/95-pv-context-pct-batch-sweep-drop-capture-pane-phase-92-sibling/95-RESEARCH.md
@.planning/phases/95-pv-context-pct-batch-sweep-drop-capture-pane-phase-92-sibling/95-02-SUMMARY.md

# Files being modified — read in full (or the enumerated line ranges) before editing:
@src/ui/features/pretty-view/PlanPendingBubble.tsx
@src/ui/features/pretty-view/PrettyView.tsx
@src/ui/features/pretty-view/ComposeBox.tsx
@src/ui/api/claude-session-api.ts

# Comment-only reference files — read the specific line to see the comment context:
@src/ui/features/pretty-view/PrettyViewLoadingOverlay.tsx
@src/ui/features/pretty-view/RelayInboundBubble.tsx
@src/ui/features/pretty-view/AsideBubble.tsx
@src/ui/features/pretty-view/SessionHoldingOverlay.tsx
@src/ui/features/pretty-view/WaitingBubble.tsx
@src/ui/features/pretty-view/use-auto-scroll.ts
@src/ui/features/relay-room-pane/AgentBadgeWithAppendage.tsx
@src/ui/AppShell.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete PlanPendingBubble + gut plan-pending from PrettyView, ComposeBox, and wire-types</name>
  <files>src/ui/features/pretty-view/PlanPendingBubble.tsx, src/ui/features/pretty-view/ComposeBox.plan-pending-disable.test.tsx, src/ui/features/pretty-view/PrettyView.tsx, src/ui/features/pretty-view/ComposeBox.tsx, src/ui/api/claude-session-api.ts</files>
  <action>
    Step 1 — Delete two files whole.

    - rm src/ui/features/pretty-view/PlanPendingBubble.tsx
    - rm src/ui/features/pretty-view/ComposeBox.plan-pending-disable.test.tsx

    RESEARCH G7 asks whether to preserve the "Ink Plan Mode split-send" lesson embedded in PlanPendingBubble.tsx L14–L21 docblock. Recommendation: capture the lesson verbatim in Plan 03's SUMMARY.md under a "Lessons preserved from deleted file PlanPendingBubble.tsx" section, so the phase summary retains the knowledge. Lightweight preservation; if Alice wants a heavier record (dedicated `.planning/lessons-learned/` file), that's a follow-up.

    Step 2 — Edit src/ui/api/claude-session-api.ts (wire types — do this FIRST because PrettyView + ComposeBox reference the types being deleted).

    Per RESEARCH §3f:
    - L148–L156: DELETE the PlanPendingEvent docblock.
    - L156–L165: DELETE the PlanPendingEvent type export (the whole `export type PlanPendingEvent = { type: "plan_pending"; ... }` block).
    - L174: EDIT the "preserved-verbatim WS frames" comment — remove plan_pending from the enumeration.
    - L395: DELETE `| PlanPendingEvent` from the discriminated union. Union goes from N members to N-1.
    - L480–L483: DELETE the raw_keystrokes docblock (referring to PlanPendingBubble Approve/Feedback).
    - L491: DELETE the `type: "raw_keystrokes";` union member. RESEARCH §3f confirms PlanPendingBubble was the ONLY client-side sender of raw_keystrokes.

    Grep to verify: `grep -n "PlanPendingEvent\|raw_keystrokes" src/ui/api/claude-session-api.ts` — expected ZERO after edits.

    Step 3 — Edit src/ui/features/pretty-view/PrettyView.tsx (15 line-precise sites per RESEARCH §3c).

    Read L1–L50 imports + L620–L680 state + L2470–L2650 WS-frame case handler + L3640–L3990 render before editing.

    Top-down edits (stable line numbers):
    - L35: DELETE `import { PlanPendingBubble } from "./PlanPendingBubble";`.
    - L184: EDIT the bubble-type enumeration comment — remove PlanPendingBubble.
    - L632–L659: DELETE the planPending state block (`const [planPending, setPlanPending] = useState<...>(null);` plus its docblock). All ~28 lines.
    - L1878: DELETE the `setPlanPending(null);` reset.
    - L2125: DELETE the `setPlanPending(null);` reset.
    - L2491–L2494: DELETE the `case "plan_pending": { setPlanPending(parsed.pending); break; }` in the WS-frame switch. Discriminated union type change from Plan 02 (backend no longer emits `plan_pending`) makes the case unreachable; deletion is safe.
    - L2554: EDIT the `/ plan_pending / asideText` comment — drop `/ plan_pending`.
    - L2596: DELETE the `setPlanPending(null);` in session_changed handler.
    - L2646: EDIT the wire_boot handler docblock — drop the planPending mention; keep dormantRef and draft.
    - L2942: EDIT the "backgroundedShells/planPending/asideText/isHolding are NOT touched" comment — drop `planPending/`.
    - L3646: EDIT the bubble-type enumeration comment — drop PlanPendingBubble.
    - L3740–L3747: EDIT this comment block — remove PlanPendingBubble references; preserve pre-Phase-43 rendering semantic.
    - L3751–L3756: DELETE the entire `{planPending && (<PlanPendingBubble ... />)}` conditional render block.
    - L3972–L3982: DELETE the `planPendingActive={planPending !== null}` prop on ComposeBox + surrounding comment. Since Step 4 removes the ComposeBox prop entirely, the prop assignment MUST go too — TS error otherwise.

    Step 4 — Edit src/ui/features/pretty-view/ComposeBox.tsx (14 planPendingActive sites per RESEARCH §3d + duplicates at L3219+).

    Read L400–L600 (prop declarations + destructure), L1700–L1760 (first OR-in cluster), L2000–L2100 (send guards), L2100–L2250 (button-disabled ORs), L2350–L2650 (aux + prop-drill), L3100–L3450 (the L3219+ duplicate cluster — inspect: sibling render function or duplicate prop chain; treat consistently).

    Edits:
    - L415–L422: DELETE the planPendingActive prop docblock.
    - L426: EDIT the ThumbsUp+Recap+Queue disable chain comment — drop planPendingActive.
    - L584: DELETE `planPendingActive,` from props destructure.
    - L1726, L1742: EDIT `if (!recycleActive && !planPendingActive && !reconnectingActive)` → drop `!planPendingActive`.
    - L2031: EDIT `if (recycleActive || planPendingActive || reconnectingActive) return;` → drop `|| planPendingActive`.
    - L2122: EDIT the OR-chain `planPendingActive === true ||` → drop.
    - L2203: EDIT the AND-chain `!planPendingActive &&` → drop.
    - L2356: EDIT the button disabled prop OR-chain `planPendingActive === true` → drop.
    - L2555, L2588: EDIT the aux button disabled OR-chains `|| planPendingActive === true` → drop.
    - L2639: DELETE the `planPendingActive={planPendingActive}` prop-drill entirely.
    - L3219–L3220, L3262, L3353, L3421: RESEARCH-flagged duplicate cluster. Apply SAME edits (drop prop / drop destructure / drop OR-in guards). Consistency is the rule.

    Also: the PROP TYPE declaration for planPendingActive (in ComposeBoxProps near the top) MUST go. Grep after: `grep -n "planPendingActive" src/ui/features/pretty-view/ComposeBox.tsx` — expect ZERO.

    Step 5 — Validation.

    - `grep -c "planPending\|plan_pending\|PlanPendingBubble\|planFilePath\|planContent" src/ui/` recursive — expect ZERO in the files this Task touched (comment-only refs in Task 2 files still exist temporarily).
    - `npx tsc --noEmit` — zero errors. All Plan-02-expected-to-fail errors resolved.
    - `npx vitest run src/ui/` — full frontend suite green.
    - Sibling regression: `npx vitest run src/ui/features/pretty-view/PrettyView` and `npx vitest run src/ui/features/pretty-view/ComposeBox` — all green.

    Step 6 — Atomic commit.

    Commit shape: `refactor(pretty-view): delete PlanPendingBubble + planPendingActive prop chain + wire-type (Phase 95 Part B frontend)`.

    Body: enumerate deleted files (2), 15 PrettyView.tsx edits, 14 ComposeBox.tsx edits, 6 claude-session-api.ts edits. Reference CONTEXT.md § Decisions Part B + RESEARCH §3c–§3f.

    Pre-commit hooks; do NOT `--no-verify`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; test ! -f src/ui/features/pretty-view/PlanPendingBubble.tsx &amp;&amp; test ! -f src/ui/features/pretty-view/ComposeBox.plan-pending-disable.test.tsx &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run src/ui/features/pretty-view/</automated>
  </verify>
  <done>
    PlanPendingBubble.tsx + ComposeBox.plan-pending-disable.test.tsx deleted. PrettyView.tsx has zero planPending state, no PlanPendingBubble import, no plan_pending case, no conditional render. ComposeBox.tsx has zero planPendingActive references (prop, destructure, guards). claude-session-api.ts has no PlanPendingEvent + no raw_keystrokes type. `npx tsc --noEmit` green. `npx vitest run src/ui/features/pretty-view/` green. Atomic commit landed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Comment-only reference cleanup across 9 sibling files</name>
  <files>src/ui/features/pretty-view/PrettyViewLoadingOverlay.tsx, src/ui/features/pretty-view/RelayInboundBubble.tsx, src/ui/features/pretty-view/AsideBubble.tsx, src/ui/features/pretty-view/SessionHoldingOverlay.tsx, src/ui/features/pretty-view/WaitingBubble.tsx, src/ui/features/pretty-view/use-auto-scroll.ts, src/ui/features/relay-room-pane/AgentBadgeWithAppendage.tsx, src/ui/AppShell.tsx</files>
  <action>
    Per RESEARCH §3e: 9 files carry comment-only references to PlanPendingBubble as pattern precedent, semantic sibling, or aesthetic anchor. None are load-bearing behavior; all are docblock housekeeping.

    Approach: for each file, read the specific line + ~10 lines of context. Edit minimally — remove the plan-pending reference while preserving the surrounding sentence's meaning. Do NOT do a scorched-earth sed pass — RESEARCH G11 flags this: `sed -i s/plan/foo/g` has enormous blast radius since "plan" appears in project docs, adverbs, role names. Grep explicitly for `PlanPendingBubble` / `plan-pending` / `plan_pending` / `planPending` — those are the safe target strings.

    Edits per RESEARCH §3e:

    - PrettyViewLoadingOverlay.tsx L95: aesthetic PlanPendingBubble reference. Drop; retain the aesthetic-anchor phrasing pointing at a surviving bubble (WaitingBubble is the natural replacement).

    - RelayInboundBubble.tsx L18: reference to PlanPendingBubble in a bubble-type enumeration. Drop from enumeration.

    - AsideBubble.tsx L6: short PlanPendingBubble reference. Drop.

    - SessionHoldingOverlay.tsx L29 and L140: two aesthetic references. Drop both; retain "aesthetic sibling of WaitingBubble" or equivalent.

    - WaitingBubble.tsx L11, L21–L38, L69, L83, L100: the docblock's purpose is to explain "WaitingBubble is NOT PlanPendingBubble because X, Y, Z." With PlanPendingBubble deleted, the distinction is moot. Rewrite the docblock section that references PlanPendingBubble to be a self-standing description of WaitingBubble's purpose, WITHOUT the contrastive framing. Preserve non-contrastive info (why WaitingBubble exists, when it renders, what it wraps). Drop "as opposed to PlanPendingBubble which..." comparisons. Post-edit test: WaitingBubble's docblock should stand alone without dangling references to a nonexistent sibling. ~10–30 line docblock rewrite depending on how deep the cross-referencing goes. Read L11 through L100 in full before writing.

    - use-auto-scroll.ts L341: comment reference. Drop.

    - AgentBadgeWithAppendage.tsx L215: comment reference. Drop.

    - AppShell.tsx L2923: comment reference. Drop.

    After all 9 edits:

    - `grep -rn "PlanPendingBubble\|plan-pending\|planPending\|plan_pending" src/ui/ --include='*.ts' --include='*.tsx'` — expect ZERO hits.
    - `npx tsc --noEmit` — zero errors.
    - `npx vitest run src/ui/` — full frontend suite still green.

    Second atomic commit: `refactor(pretty-view): comment cleanup after plan-pending removal (Phase 95 Part B tidy)`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; RESULT=$(grep -rn "PlanPendingBubble\|plan-pending\|planPending\|plan_pending" src/ui/ --include=*.ts --include=*.tsx | wc -l) &amp;&amp; test "$RESULT" = "0" &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run src/ui/</automated>
  </verify>
  <done>
    All 9 comment-only references updated. `grep -rn "PlanPendingBubble\|plan-pending\|planPending\|plan_pending" src/ui/` returns zero hits. `npx tsc --noEmit` green. Full frontend suite green. Second atomic commit landed.
  </done>
</task>

</tasks>

<verification>
- PlanPendingBubble.tsx + ComposeBox.plan-pending-disable.test.tsx deleted (test with `test ! -f`).
- `grep -rn "PlanPendingBubble\|plan-pending\|planPending\|plan_pending" src/ui/ --include='*.ts' --include='*.tsx'` returns ZERO hits after both tasks.
- `grep -rn "PlanPendingEvent\|raw_keystrokes" src/ui/api/claude-session-api.ts` returns ZERO hits.
- `npx tsc --noEmit` — zero new type errors, resolving all Plan 02 expected-to-fail states.
- `npx vitest run src/ui/` — full frontend suite passes.
- `npx vitest run` — full-suite regression check green (backend + frontend, no un-addressed failures).
- Two atomic commits landed (Task 1 substantive UI deletion; Task 2 comment refs).
</verification>

<success_criteria>
- The entire plan-pending UI stack is gone from the frontend — no state, no handlers, no bubble render, no compose-box guards, no wire types.
- Wave 2 build is fully green — both backend (Plan 02) and frontend (Plan 03) side of the plan-pending deletion is now complete.
- The RESEARCH G7 Ink Plan Mode split-send lesson is captured in the SUMMARY.md if the executor deems it worth preserving (Alice's call whether to promote to `.planning/lessons-learned/`).
- Wave 3 (Plans 04–05) can now proceed cleanly — Part C batch collapse builds on a clean pre-existing backend + frontend with zero plan-pending residue.
</success_criteria>

<output>
Create `.planning/phases/95-pv-context-pct-batch-sweep-drop-capture-pane-phase-92-sibling/95-03-SUMMARY.md` when done. Include:
- Verbatim capture of the PlanPendingBubble.tsx L14–L21 "Ink Plan Mode split-send" lesson (RESEARCH G7 preservation).
- Total lines removed across the 5 primary files (PlanPendingBubble deleted + PrettyView.tsx + ComposeBox.tsx + claude-session-api.ts + ComposeBox.plan-pending-disable.test.tsx deleted).
- Whether the L3219+ ComposeBox.tsx duplicate cluster turned out to be a sibling render function or duplicate prop chain in the same file (recorded for future readers who might puzzle over it).
- Post-plan grep count for plan-pending strings across `src/` (must be zero).
- List of the 9 comment-cleanup edits with a one-line description of what was said before / after.
- Two atomic commit hashes.
</output>
