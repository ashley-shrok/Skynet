---
phase: 32-hold-to-send-gesture-on-send-button
plan: 03
type: execute
wave: 3
depends_on:
  - 32-01
  - 32-02
files_modified:
  - src/ui/features/pretty-view/ComposeBox.hold-to-send.test.tsx
autonomous: true
requirements:
  - HOLD-SEND-11
  - HOLD-SEND-12
  - HOLD-SEND-13
must_haves:
  truths:
    - "New test file src/ui/features/pretty-view/ComposeBox.hold-to-send.test.tsx exists with integration tests exercising ComposeBox rendered end-to-end (not just the hook)"
    - "Tests cover all 9 behaviors from CONTEXT.md § specifics test cases (L120-129): short-tap-sends, long-press-starts-recording-in-place (send button stays with data-hold-active=true, RecordingControls does NOT swap in per B-3), release-inside-sends-glued-with-typed-text, slide-off-cancels, aside-morph inertness (long-press does nothing; short-tap on X dismisses via preserved onClick per B-2), disabled-state inertness, voice.state !== 'idle' guard, iOS Safari sync-gesture invariant, both-paths-coexist regression"
    - "Test suite uses the same MockMediaRecorder + getUserMedia + fetch mock scaffold as ComposeBox.voice.test.tsx (structural convention)"
    - "npx vitest run exits 0 with 0 failures (full suite green — fleet directive)"
  artifacts:
    - path: "src/ui/features/pretty-view/ComposeBox.hold-to-send.test.tsx"
      provides: "Integration test suite for the hold-to-send gesture on the primary send button (Phase 32)"
      contains: "describe(\"ComposeBox — Phase 32 hold-to-send gesture\""
      min_lines: 300
  key_links:
    - from: "src/ui/features/pretty-view/ComposeBox.hold-to-send.test.tsx"
      to: "src/ui/features/pretty-view/ComposeBox.tsx"
      via: "renders ComposeBox with onSend + asideActive + baseProps mock; drives pointer events via fireEvent.pointerDown / .pointerUp with clientX/Y coords"
      pattern: "import \\{ ComposeBox"
    - from: "src/ui/features/pretty-view/ComposeBox.hold-to-send.test.tsx"
      to: "src/ui/features/pretty-view/useVoiceRecording.ts"
      via: "mocks navigator.mediaDevices.getUserMedia + globalThis.MediaRecorder + fetch — the same triple ComposeBox.voice.test.tsx uses so the internal voice singleton wires up correctly"
      pattern: "MockMediaRecorder"
---

<objective>
Add integration tests that exercise the full ComposeBox surface (rendered end-to-end via React Testing Library) with the hold-to-send gesture wired up per Plans 32-01 + 32-02. Unit tests on the hook (Plan 32-01) cover the gesture layer in isolation with a mocked voice object; these integration tests cover the wiring — that the hook's callbacks are actually connected to handleSend / handleVoiceSend / voice.start / voice.cancel in ComposeBox, that the paper-plane button in the DOM responds to pointer events correctly, that the coexistence with the mic-tap path (RecordingControls) works, and that the aside-morph / disabled / voice.state-guard branches all short-circuit as expected in a real component render.

Test surface scope: primary send button only. The slot-mode send button is functionally identical in wiring (same hook, symmetric callbacks per Plan 32-02) and adding a full queue-slot integration harness would double the test scaffolding for marginal coverage — the hook's own unit tests (Plan 32-01) already prove the gesture logic works on any consumer; Plan 32-02 wires the slot symmetrically. If Ashley later reports a slot-specific bug, add a targeted slot test at that time.

Purpose: prove end-to-end that Phase 32 delivers the hold-to-send gesture without regressing the tap-mic path, aside-morph, or disabled semantics. This is the phase's Nyquist regression net.

Output: new test file with 10 tests, all passing; full-suite vitest still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-hold-to-send-gesture-on-send-button/32-CONTEXT.md
@.planning/phases/32-hold-to-send-gesture-on-send-button/32-01-PLAN.md
@.planning/phases/32-hold-to-send-gesture-on-send-button/32-02-PLAN.md
@src/ui/features/pretty-view/useVoiceRecording.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ComposeBox.hold-to-send.test.tsx with 10 integration tests</name>
  <files>src/ui/features/pretty-view/ComposeBox.hold-to-send.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/ComposeBox.voice.test.tsx (entire file — this is the canonical structural template for voice-related ComposeBox integration tests. Copy the top-of-file setup block verbatim: vi.mock("@/api/compose-drafts-api") before importing ComposeBox, baseProps helper, MockMediaRecorder class, getUserMedia mock, fetch mock, beforeEach/afterEach lifecycle. Deviations from this template break the isolation from real browser APIs.)
    - src/ui/features/pretty-view/ComposeBox.aside-morph.test.tsx (structural reference for how the file tests aside-morph behavior — the hold-inert-when-asideActive test in this plan mirrors that file's setup pattern)
    - src/ui/features/pretty-view/useHoldToRecord.ts (the hook — imported to reference HOLD_THRESHOLD_MS in threshold-boundary tests)
    - src/ui/features/pretty-view/ComposeBox.tsx L2380-2437 (the wired primary button — tests target the button with aria-label "Send" for typed sends OR the paper-plane path; button with aria-label "Resume" for the asideActive branch)
    - src/ui/features/pretty-view/ComposeBox.tsx L1603 (the B-3 `showRecordingControls = isPrimaryRecording && !primaryHold.holdInitiatedRef.current` gate — Test 2 asserts this predicate's effect: during a hold-initiated recording, RecordingControls does NOT appear; the send button stays with data-hold-active="true")
    - .planning/phases/32-hold-to-send-gesture-on-send-button/32-CONTEXT.md § specifics test cases L120-129 (the 9 canonical test cases — this plan implements all 9 as tests 1-9, plus adds test 10 for the threshold-boundary regression)
  </read_first>
  <behavior>
    Ten tests, each independently runnable, each asserting exact call counts on mocked side effects (onSend, fetch, getUserMedia) and exact DOM state assertions. All assertions are deterministic — no "executor picks the assertion after running the test once" clauses. If a test fails, the failure indicates a real defect in Plans 32-01 / 32-02 wiring and must be fixed there (not by weakening the assertion).
  </behavior>
  <action>
    Create a new file src/ui/features/pretty-view/ComposeBox.hold-to-send.test.tsx. Copy the file-header + setup block from ComposeBox.voice.test.tsx L1-140 verbatim (adapt only the top-of-file comment to describe Phase 32 hold-to-send). The tests then diverge.

    File structure (in order):

    1. File header comment (~20 lines) explaining:
       - Phase 32 hold-to-send integration tests for the ComposeBox primary send button.
       - Covers the 9 canonical CONTEXT.md § specifics test cases plus 1 threshold-boundary regression guard.
       - Uses the same MockMediaRecorder + getUserMedia + fetch scaffold as ComposeBox.voice.test.tsx.
       - The hold gesture invokes voice.start synchronously inside pointerdown (D-16-02 iOS Safari invariant); Test 8 explicitly asserts this call-order.

    2. Imports: from vitest — describe, it, expect, vi, beforeEach, afterEach. From @testing-library/react — render, screen, fireEvent, act, waitFor. From ./ComposeBox — the ComposeBox component and its ComposeBoxProps type. From ./useHoldToRecord — HOLD_THRESHOLD_MS.

    3. vi.mock for "@/api/compose-drafts-api" identical to ComposeBox.voice.test.tsx L31-35.

    4. baseProps helper identical to ComposeBox.voice.test.tsx L43-50 (returns { onSend: vi.fn(() => true), hostId: 1, tmuxSession: "s1", ...overrides }).

    5. Copy MockMediaRecorder + makeMockStream helpers verbatim from ComposeBox.voice.test.tsx L57-94.

    6. beforeEach: mirror ComposeBox.voice.test.tsx L100-134 (clear mocks, install MockMediaRecorder on globalThis, install navigator.mediaDevices.getUserMedia stub, stub fetch returning { text: "hello world" }). Additionally: install vi.useFakeTimers({ shouldAdvanceTime: false }) — the hold threshold is time-based and tests must control the clock deterministically.

    7. afterEach: vi.useRealTimers(); vi.restoreAllMocks(); vi.unstubAllGlobals();

    8. Helper: `getSendButton()` — returns `screen.getByRole("button", { name: "Send" })` (the primary paper-plane button when not in aside-morph mode). Test 5 uses `screen.getByRole("button", { name: "Resume" })` for the aside-morph state.

    9. Helper: `installBoundsShim(button, rect = { left: 0, right: 40, top: 0, bottom: 40 })` — installs a `getBoundingClientRect` shim on the button element via `Object.defineProperty(button, "getBoundingClientRect", { value: () => rect, configurable: true })`. jsdom returns zero-width rect by default; the hook's bounds check needs a real rect.

    10. Helper: `queryRecordingControlsSendButton()` — returns `screen.queryByRole("button", { name: /^Send$/i })` filtered to buttons INSIDE the RecordingControls component. Since RecordingControls has three buttons (Cancel / Append / Send), and the paper-plane button also has aria-label "Send", the test needs to distinguish. Use a testid instead if RecordingControls exposes one — grep RecordingControls.tsx for `data-testid` before writing the helper. If no testid, use structural selectors: `container.querySelector('[data-recording-controls]')` or find all buttons with aria-label matching /Cancel|Append|Send/ and assert their presence/absence collectively. Simplest: assert `screen.queryByRole("button", { name: "Cancel recording" })` (or whatever the RecordingControls Cancel button's aria-label is — grep RecordingControls.tsx to confirm) is null during a hold-initiated recording.

    11. describe block: "ComposeBox — Phase 32 hold-to-send gesture (primary send button)".

    Tests (each an it() block):

    - `it("Test 1 (CONTEXT.md L122): short tap under threshold fires normal handleSend with typed text; rollback via voice.cancel")`: render ComposeBox with typed text "hello world" (fireEvent.change on the textarea to set it). Install bounds shim. fireEvent.pointerDown on the send button with pointerId 1. `act(() => vi.advanceTimersByTime(200))`. fireEvent.pointerUp with `{clientX: 20, clientY: 20}` (inside bounds). Wait for the awaited cancel + subsequent onShortTap: `await waitFor(() => expect(baseProps.onSend).toHaveBeenCalledWith("hello world"))`. Assert `navigator.mediaDevices.getUserMedia.mock.calls.length === 1` (Shape 1 optimistic start called it) AND `fetch.mock.calls.length === 0` (rollback tore down before endSend) AND `baseProps.onSend.mock.calls.length === 1`. This test also implicitly verifies the useVoiceRecording pendingCancelRef fix from Plan 32-01 Task 1 works end-to-end: the short-tap-rollback cancel() may fire before or after getUserMedia resolves; either way the mic must be torn down and no MediaRecorder constructed. Assert `MockMediaRecorder.instances.length === 0` — no MediaRecorder was ever constructed because the pending-cancel branch tore down the stream before construction.

    - `it("Test 2 (CONTEXT.md L123): long press >= 250ms starts recording IN PLACE — send button stays with data-hold-active=true, RecordingControls does NOT swap in (B-3)")`: render ComposeBox with empty text (WAIT — empty text means sendDisabled=true, which makes the hook inert. Use typed text "hi" instead so sendDisabled=false and the hook can arm). Install bounds shim. `const button = getSendButton()`. Snapshot `button.getAttribute("data-hold-active")` — expect "false". fireEvent.pointerDown on the button with pointerId 1. Wait for the getUserMedia promise resolution: `await waitFor(() => expect(navigator.mediaDevices.getUserMedia).toHaveBeenCalledTimes(1))`. Advance to post-threshold: `await act(async () => { vi.advanceTimersByTime(300); })`. Now assert THREE things deterministically (this is the B-3 assertion — no "executor picks" clause):

      1. `expect(button.getAttribute("data-hold-active")).toBe("true")` — the send button has the hold-active tint.
      2. `expect(screen.queryByRole("button", { name: "Cancel recording" })).toBeNull()` — RecordingControls did NOT swap in. (Adjust the aria-label to match whatever RecordingControls.tsx actually uses for its Cancel button — grep RecordingControls.tsx during test authoring to confirm the exact label; if it uses "Cancel" without qualifier and that collides with anything else, use a testid instead.)
      3. `expect(getSendButton()).toBe(button)` — the same button element is still in the DOM (identity preserved; the button was not unmounted and replaced).

      Then to clean up the test (leave the recording in a defined state so afterEach can tear down): fireEvent.pointerUp with `{clientX: 200, clientY: 200}` (outside — slide-off + release cancels) so voice.cancel runs and state returns to idle.

    - `it("Test 3 (CONTEXT.md L124): release inside bounds after long-press with typed text sends the glued transcript")`: render ComposeBox with typed text "hello". Install bounds shim. fireEvent.pointerDown. Await getUserMedia. Advance timers: `await act(async () => { vi.advanceTimersByTime(300); })`. Push a data chunk: `MockMediaRecorder.instances[0].emitData(new Blob(["audio"], { type: "audio/webm" }))`. fireEvent.pointerUp with `{clientX: 20, clientY: 20}` (inside). `await waitFor(() => expect(baseProps.onSend).toHaveBeenCalledWith("hello hello world"))` — the pre-typed text "hello" glued to the STT-returned "hello world" via applyGlue's single-space rule since "hello" does not end in whitespace. Assert `fetch.mock.calls[0][0] === "/voice/transcribe"`. Assert `baseProps.onSend.mock.calls.length === 1`. This is the D-16-05 same-path assertion — the hold-send routes through the same handleSend + voice.endSend as the mic-tap -> RecordingControls Send.

    - `it("Test 4 (CONTEXT.md L125): slide off + release cancels — no send, no fetch, textarea unchanged")`: render ComposeBox with typed text "hello". Install bounds shim. fireEvent.pointerDown. Await getUserMedia. `await act(async () => { vi.advanceTimersByTime(300); })`. fireEvent.pointerLeave. fireEvent.pointerUp with `{clientX: 200, clientY: 200}` (outside the shimmed {left:0,right:40,top:0,bottom:40} rect). Await a microtask to let voice.cancel resolve: `await waitFor(() => expect(screen.getByRole("button", { name: "Send" })).toBeTruthy())`. Assert `fetch.mock.calls.length === 0` (voice.cancel path taken, no /voice/transcribe request). Assert `baseProps.onSend.mock.calls.length === 0` (no send). Assert `(screen.getByRole("textbox") as HTMLTextAreaElement).value === "hello"` (textarea unchanged).

    - `it("Test 5 (CONTEXT.md L126): aside-morph inertness — hold on X does NOT call getUserMedia; short-tap on X still dismisses via preserved onClick (B-2)")`: render ComposeBox with `asideActive={true}` and `onAsideDismiss={vi.fn()}`. The send button now renders as the X (aria-label "Resume"). Install bounds shim on the "Resume" button. **Deterministic assertion sequence (no branching, no "executor discovers"):**

      1. fireEvent.pointerDown on the Resume button with pointerId 1.
      2. `await act(async () => { vi.advanceTimersByTime(500); })` — well past the 250ms threshold.
      3. `expect(navigator.mediaDevices.getUserMedia).not.toHaveBeenCalled()` — the hook's asideActive guard prevented voice.start (which prevents getUserMedia). This is a mock-assertion — deterministic.
      4. fireEvent.pointerUp with `{clientX: 20, clientY: 20}` (inside bounds).
      5. `await waitFor(() => expect(baseProps.onAsideDismiss).toHaveBeenCalledTimes(1))` — the preserved `onClick={asideActive ? () => onAsideDismiss?.() : undefined}` from Plan 32-02 Task 1 Step 4 fires from the synthetic click event generated by a valid pointerdown/pointerup pair inside the button bounds. This is a mock-assertion — deterministic.
      6. `expect(navigator.mediaDevices.getUserMedia).not.toHaveBeenCalled()` (assert again post-pointerup to confirm no late call from a delayed timer).

      This test proves the B-2 fix: even though the useHoldToRecord hook is inert when asideActive=true (its onShortTap does NOT fire), the aside-dismiss still works because the button's native onClick is preserved and served by the browser's synthetic click event.

    - `it("Test 6 (CONTEXT.md L127): disabled-state inertness — hold on send button when sendDisabled fires nothing")`: render ComposeBox with empty text (sendDisabled=true because text.trim()==="" and no attachments). Install bounds shim. fireEvent.pointerDown. `await act(async () => { vi.advanceTimersByTime(500); })`. Assert `navigator.mediaDevices.getUserMedia` NOT called (hook guard on disabled=true short-circuits). fireEvent.pointerUp inside bounds. Assert `baseProps.onSend.mock.calls.length === 0`, `fetch.mock.calls.length === 0`. Note: because the button element also carries `disabled={true}` when sendDisabled is true, jsdom may not fire pointerdown at all on the disabled button — that is fine; the assertion is that getUserMedia was NOT called, which holds in both cases.

    - `it("Test 7 (CONTEXT.md L128): voice.state !== 'idle' guard — while mic-tap is recording, primary send button is not visible AND no additional getUserMedia call fires")`: render ComposeBox. First tap the MicButton: `fireEvent.click(screen.getByRole("button", { name: "Record voice" }))`. Await getUserMedia + MockMediaRecorder construction: `await waitFor(() => expect(navigator.mediaDevices.getUserMedia).toHaveBeenCalledTimes(1)); await waitFor(() => expect(MockMediaRecorder.instances.length).toBe(1))`. Await voice.state === "recording" (which triggers the RecordingControls swap): `await waitFor(() => expect(screen.queryByRole("button", { name: "Send" })).toBeNull())` — proves the primary send button is not visible (replaced by RecordingControls). Assert `navigator.mediaDevices.getUserMedia.mock.calls.length === 1` — no double-arm. Clean up: click the RecordingControls Cancel button (grep RecordingControls.tsx for the exact aria-label — likely "Cancel recording" or similar).

    - `it("Test 8 (CONTEXT.md L129): iOS Safari sync-gesture invariant — getUserMedia is called synchronously within the pointerdown handler")`: render ComposeBox with typed text "hi" (so sendDisabled=false). Install bounds shim. Immediately BEFORE fireEvent.pointerDown, snapshot `navigator.mediaDevices.getUserMedia.mock.calls.length` (should be 0). fireEvent.pointerDown on the send button. In the SAME synchronous tick (no await, no timer advance), assert `expect(navigator.mediaDevices.getUserMedia).toHaveBeenCalledTimes(1)`. This proves voice.start was called synchronously inside the pointerdown handler, which in turn called getUserMedia synchronously per useVoiceRecording L261. The chain: fireEvent.pointerDown -> hook onPointerDown -> voice.start() -> getUserMedia({audio:true}). If any await had been inserted anywhere in that chain, the getUserMedia call would happen in a microtask AFTER the pointerdown handler returned, and this test would fail — the iOS Safari runtime uses this exact detection to gate the mic-permission prompt. Clean up: fireEvent.pointerUp outside bounds so voice.cancel runs.

      Comment above the assertion (~4 lines): "D-16-02 iOS Safari sync-gesture invariant assertion — if this test fails, the mic-permission prompt will silently die on iOS Safari and hold-to-record will not work on Ashley's iPhone. Do NOT insert an `await` between the fireEvent.pointerDown and the expect below — the whole point of this test is that the assertion is synchronous."

    - `it("Test 9 (CONTEXT.md L130): both paths coexist — after hold-send, mic-tap still works")`: render ComposeBox with typed text "test1". Install bounds shim. Do a full hold-send cycle: fireEvent.pointerDown, await getUserMedia, `await act(async () => { vi.advanceTimersByTime(300); })`, emit data chunk, fireEvent.pointerUp inside -> `await waitFor(() => expect(baseProps.onSend).toHaveBeenCalledWith("test1 hello world"))`. Await state return to idle: `await waitFor(() => expect(screen.getByRole("button", { name: "Send" })).toBeTruthy())`. Now do a mic-tap cycle: fireEvent.click on button with aria-label "Record voice". Await RecordingControls (send button gone). Click the Cancel button in RecordingControls. Await MicButton reappearance. Assert `navigator.mediaDevices.getUserMedia.mock.calls.length === 2` (once per cycle). This proves neither cycle poisons the other. Reverse order not tested (Test 7 already covers the "mic-tap recording -> send button not visible" invariant).

    - `it("Test 10: threshold boundary — 249ms tap-sends, exactly HOLD_THRESHOLD_MS ms hold-records")`: render ComposeBox with typed text "hi". Case A (short-tap boundary): fireEvent.pointerDown, `await act(async () => { vi.advanceTimersByTime(HOLD_THRESHOLD_MS - 1); })` (i.e., 249ms), fireEvent.pointerUp inside -> `await waitFor(() => expect(baseProps.onSend).toHaveBeenCalledWith("hi"))` (short-tap-send branch, typed text only), assert `fetch.mock.calls.length === 0`. Fresh render for Case B (long-press boundary): render ComposeBox with typed text "hi", install bounds shim, fireEvent.pointerDown, `await waitFor(() => expect(navigator.mediaDevices.getUserMedia).toHaveBeenCalledTimes(1))`, `await act(async () => { vi.advanceTimersByTime(HOLD_THRESHOLD_MS); })` (i.e., 250ms exactly), emit data chunk, fireEvent.pointerUp inside -> `await waitFor(() => expect(baseProps.onSend).toHaveBeenCalledWith("hi hello world"))` (long-press-send + glue branch), assert `fetch.mock.calls.length === 1`.

    Notes on test structure:
    - Every test that involves an async voice cycle MUST await both (a) the getUserMedia promise resolution (via `await waitFor` on getUserMedia mock call count) AND (b) the fetch resolution or final DOM state (via `await waitFor` on onSend call OR screen state). Missing (a) leads to flaky tests where the MockMediaRecorder was never constructed.
    - Use `act(async () => { vi.advanceTimersByTime(ms); })` — NOT bare `vi.advanceTimersByTime`, because the hook's setTimeout callback wraps a React setState (setHoldCommitted) and needs to be flushed inside act to avoid warnings.
    - Do NOT modify any existing test file (ComposeBox.voice.test.tsx, ComposeBox.aside-morph.test.tsx, ComposeBox.test.tsx, etc.). This plan is purely additive.
    - If any test reveals a wiring bug in Plans 32-01 or 32-02, that is a defect in those plans — the executor must return to the correct plan, fix, then re-run this plan. Do NOT paper over the wiring bug by weakening the test assertion. All 10 tests have deterministic assertions with no "executor picks at runtime" clauses.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/ComposeBox.hold-to-send.test.tsx --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - File `src/ui/features/pretty-view/ComposeBox.hold-to-send.test.tsx` exists. Verified by ls.
    - The file contains exactly 10 it() blocks. Verified by `grep -v '^\s*//' src/ui/features/pretty-view/ComposeBox.hold-to-send.test.tsx | grep -c '^\s*it('` returns 10.
    - No `it.skip()` or `describe.skip()` blocks. Verified by `grep -c '\.skip(' src/ui/features/pretty-view/ComposeBox.hold-to-send.test.tsx` returns 0.
    - Test 2 (B-3 in-place recording) contains three deterministic assertions after the 300ms timer advance: (a) `data-hold-active` === "true" on the send button, (b) RecordingControls Cancel button is NOT in the DOM (queryByRole returns null), (c) getSendButton() returns the SAME element as the pre-pointerdown snapshot. Verified by reading Test 2's body — no `if/else`, no "executor picks", all assertions run unconditionally.
    - Test 5 (B-2 aside-morph inertness + preserved onClick) contains two deterministic assertions: (a) `getUserMedia not.toHaveBeenCalled()` after the 500ms hold on the Resume button, (b) `onAsideDismiss.toHaveBeenCalledTimes(1)` after the pointerup. Verified by reading Test 5's body — no branching, no "executor discovers", both assertions run unconditionally.
    - Test 8 (iOS sync-gesture invariant) asserts `navigator.mediaDevices.getUserMedia` was called synchronously within the pointerdown handler — the assertion appears immediately after the `fireEvent.pointerDown` call with no intervening await, timer advance, or waitFor. Verified by reading the file: within the Test 8 it() body, find the `fireEvent.pointerDown` line — the very next non-blank, non-comment line must be `expect(navigator.mediaDevices.getUserMedia).toHaveBeenCalledTimes(1)`.
    - The file imports `HOLD_THRESHOLD_MS` from `"./useHoldToRecord"` and uses it in Test 10. Verified by `grep -c "HOLD_THRESHOLD_MS" src/ui/features/pretty-view/ComposeBox.hold-to-send.test.tsx` returns at least 2 (import + at least one usage).
    - `npx vitest run src/ui/features/pretty-view/ComposeBox.hold-to-send.test.tsx` exits 0 with 10 passing tests and 0 failures. Verified by the final summary line showing all tests passed.
    - The full-suite `npx vitest run` exits 0. Verified by the final summary line showing 0 failing tests across the entire codebase.
    - No previously-passing test in ComposeBox.voice.test.tsx or ComposeBox.aside-morph.test.tsx became failing. Verified by running those files specifically and confirming exit 0.
  </acceptance_criteria>
  <done>
    Integration test file exists, all 10 tests pass, iOS sync-gesture invariant is asserted in Test 8, B-3 in-place recording is asserted deterministically in Test 2, B-2 aside-dismiss preserved-onClick is asserted deterministically in Test 5, threshold boundary is asserted in Test 10 using HOLD_THRESHOLD_MS, and the full-suite `npx vitest run` still exits green. Any wiring defects discovered by these tests are fixed in Plan 32-01 or 32-02 (executor returns and re-runs) rather than papered over by weakening assertions.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/ui/features/pretty-view/ComposeBox.hold-to-send.test.tsx` exits 0 with 10 tests passing.
- `npx vitest run` (full suite) exits 0 with zero failures.
- Test 8 (iOS sync-gesture invariant) has the getUserMedia call-count assertion synchronously placed immediately after fireEvent.pointerDown — no intervening await.
- Test 2 (B-3) has three deterministic assertions: data-hold-active=true, RecordingControls not in DOM, same button element.
- Test 5 (B-2) has two deterministic assertions: getUserMedia never called, onAsideDismiss called once.
- ComposeBox.voice.test.tsx and ComposeBox.aside-morph.test.tsx both still exit 0 (no regression).
</verification>

<success_criteria>
Phase 32 hold-to-send gesture is proven end-to-end by the integration test suite. The 9 canonical CONTEXT.md § specifics test cases plus 1 threshold-boundary regression guard all pass. iOS Safari sync-gesture invariant is asserted programmatically. B-3 in-place recording gate is asserted deterministically (RecordingControls does NOT swap in during hold-initiated recording). B-2 preserved onClick for aside-dismiss is asserted deterministically. Both-paths-coexist regression sentinel is in place. Full-suite green — the fleet directive "never leave tests failing" is honored.
</success_criteria>

<output>
Create .planning/phases/32-hold-to-send-gesture-on-send-button/32-03-SUMMARY.md when done. Include: test count, per-test result (pass/fail), any wiring bugs discovered that required a return to Plan 32-01 or 32-02 (list the fix), full-suite green confirmation, and a paste of the final vitest summary line.

**Phase-level completion note:** This is the last plan of Phase 32. After 32-03-SUMMARY.md is created, the executor's remit is complete. The orchestrator (Tanya) picks up from here for the ship/deploy step — do NOT include a ship/deploy task in this plan. Do NOT run docker build. Do NOT push. The executor stops at code + commit + tests green.
</output>
