---
phase: 106-birth-flow-rework-chunk-3-retire-skynet-tmux-claude-launch-s
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/sidebar/NewSessionDialog.tsx
autonomous: true
requirements: [D-13, D-14, D-15, D-16, D-17, D-18, D-19]

must_haves:
  truths:
    - "The 5-step BirthProgress checklist no longer renders on any code path"
    - "During birthing===true, the Create button's text is replaced by a Loader2 spinner (no button-label text)"
    - "During birthing===true, form fields are disabled and the modal's onOpenChange close affordance no-ops"
    - "On birth success, `refreshIdentities()` still fires between ended:ok and onCreate/onClose (D-18)"
    - "On any birth failure (Skynet-side or supervisor-wait timeout), `window.alert('agent creation failed')` fires, then onClose"
    - "The shell-only branch of the modal is byte-identical to pre-phase behavior (regression guard — same fields, same submit path, same close-on-submit)"
    - "AppShell.tsx onCreateSession auto-route mechanic is UNTOUCHED (D-16 — mechanic already correct)"
  artifacts:
    - path: "src/ui/sidebar/NewSessionDialog.tsx"
      provides: "Refactored identity-mode modal with spinner-in-button + locked-modal + alert-on-failure"
      contains: "Loader2"
  key_links:
    - from: "src/ui/sidebar/NewSessionDialog.tsx"
      to: "src/ui/AppShell.tsx:2236"
      via: "onCreate → onCreateSession callback contract (identityMode: true payload shape unchanged)"
      pattern: "onCreate\\("
    - from: "src/ui/sidebar/NewSessionDialog.tsx"
      to: "src/state/identities-store.ts"
      via: "refreshIdentities() call between ended:ok and onCreate"
      pattern: "refreshIdentities\\(\\)"
---

<objective>
Delete the 5-step BirthProgress inline component + labels + blurbs + failed-step
state, and replace them with a spinner-in-Create-button UX. Lock the modal
during birthing (fields disabled, close affordance no-ops). On success: keep
the existing refreshIdentities → onCreate → onClose chain (which triggers
AppShell's auto-route into the new agent's chat surface). On failure of any
kind: fire `window.alert("agent creation failed")` then onClose.

Purpose: the frontend half of Chunk 3 — the sole-spawner architecture means
Skynet can no longer report per-step launch beats (they happen on the
supervisor's timeline now), so the 5-step checklist has nothing to tick
against. The modal collapses to a single undifferentiated spinner and reports
one generic failure surface, matching what the backend can actually observe.

Output: modified `NewSessionDialog.tsx`. No other frontend files change. The
`AppShell.tsx:2236` onCreateSession handler is UNTOUCHED per D-16 (auto-route
mechanic already correct — the party that made the tmux session differs
[supervisor instead of Skynet] but the frontend attach-not-create semantics
are identical).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-birth-flow-rework-chunk-3-retire-skynet-tmux-claude-launch-s/106-CONTEXT.md
@.planning/shapes/shape-birth-flow-supervisor-sole-spawner.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete BirthProgress + step-checklist state; add spinner-in-button + modal lock + alert-on-failure</name>
  <files>src/ui/sidebar/NewSessionDialog.tsx</files>
  <read_first>
    - src/ui/sidebar/NewSessionDialog.tsx (full file — see BIRTH_STEP_LABELS :190-196, BIRTH_STEP_BLURBS :201-207, BirthStepState :211-215, INITIAL_BIRTH_PROGRESS :217-223, BirthProgress component :226-301, birthProgress + birthFailedStep state :424-427, resetBirthProgress :456-460, formDisabled :869, anyStepActive + showBirthProgress :865-866, handleBirth :690-817, BirthProgress render :1274-1282, Dialog onOpenChange :915-917, Cancel button :1287-1291, Create button :1292-1340)
    - src/ui/AppShell.tsx:2225-2270 (onCreateSession handler — do NOT modify; only verify the identityMode:true branch sets sessionName=opts.name and allowCreateTmux=false)
    - src/state/identities-store.ts (refreshIdentities signature — must survive per D-18)
    - .planning/phases/106-birth-flow-rework-chunk-3-retire-skynet-tmux-claude-launch-s/106-CONTEXT.md § D-13..D-19
    - .planning/shapes/shape-birth-flow-supervisor-sole-spawner.md § "Shape" bullet 2 (modal lock) + § "Simplify the failure surface where the user has nothing to act on"
  </read_first>
  <action>
Make eight coordinated edits inside `src/ui/sidebar/NewSessionDialog.tsx`:

1) **Delete the BirthProgress inline component and its associated tables/types** (D-13):
   - Delete `const BIRTH_STEP_LABELS = [...]` block at :190-196 (with its preceding comment header at :186-189).
   - Delete `const BIRTH_STEP_BLURBS = [...]` block at :201-207 (with its preceding comment header at :198-200) — all five failure-blurb strings go per D-19.
   - Delete `type StepStatus = ...` at :209.
   - Delete `interface BirthStepState { ... }` at :211-215.
   - Delete `const INITIAL_BIRTH_PROGRESS: BirthStepState[] = [...]` at :217-223.
   - Delete the entire `function BirthProgress({...}) { ... }` component at :226-301.
   - Prune imports on line 89: remove `Check, XCircle, Circle` (no longer used); KEEP `Search` (used by host search) and `Loader2` (used by new spinner-in-button).

2) **Delete BirthProgress-related state hooks** (D-13):
   - Delete `const [birthProgress, setBirthProgress] = useState<BirthStepState[]>(INITIAL_BIRTH_PROGRESS.map((s) => ({ ...s })));` at :424-426.
   - Delete `const [birthFailedStep, setBirthFailedStep] = useState<number | null>(null);` at :427.
   - Delete `const abortControllerRef = useRef<AbortController | null>(null);` at :428 — keep the ref usage if it's referenced by the useEffect cleanup at :534, but note that under the new shape the abort-on-close semantics still apply (see edit 5 close-disabled-during-birth); after the modal-lock lands, abortControllerRef is only useful for the component-unmount cleanup effect at :533-537. Keep the ref AND that cleanup effect intact.
   - KEEP `const [birthing, setBirthing] = useState(false);` at :423 — sole survivor per D-15.

3) **Delete `resetBirthProgress` helper and its call sites** (D-13):
   - Delete `function resetBirthProgress() { setBirthing(false); setBirthProgress(...); setBirthFailedStep(null); }` at :456-460.
   - Replace its call at :521 (inside the open-effect else branch) with a direct `setBirthing(false);`.

4) **Simplify `formDisabled` and delete `anyStepActive` / `showBirthProgress` derivations** (D-13, D-15):
   - At :865-866 delete both `const anyStepActive = ...` and `const showBirthProgress = ...` derivations.
   - At :869 change `const formDisabled = birthing || birthFailedStep !== null;` to `const formDisabled = birthing;` — no failure-persist state under the new shape (failure now fires alert and immediately closes the modal per D-17).

5) **Lock the modal's close affordance during birthing** (D-15):
   - At the `<Dialog>` `onOpenChange` handler (:915-917), change from `onOpenChange={(next) => { if (!next) onClose(); }}` to a version that no-ops the close when `birthing === true`: `onOpenChange={(next) => { if (!next && !birthing) onClose(); }}`.
   - Keep the Cancel button rendering block at :1287-1291, but change its render gate from `{!birthing && (<Button ...`  to render the Cancel button ALWAYS but pass `disabled={birthing}` (mirrors form-field disabled pattern). Rationale: keeps visual continuity so the Cancel button doesn't jump-vanish during the spin — matches the shape file's "modal is fully locked from click to resolution" instruction (D-15 verbatim) which locks close, not visibility.

6) **Replace the Create button label with a spinner during birthing** (D-14):
   - At :1338-1339 the current button body is `{birthing ? "Creating..." : openLabel}`. Change to render `<Loader2 className="size-4 animate-spin" />` when `birthing === true`, and `openLabel` (the existing `"Create"` string) otherwise. Use exactly `size-4` for parity with the existing Loader2 sizing convention elsewhere in the file (e.g. :257 in the deleted BirthProgress body used `size-3.5`; the button variant should be `size-4` to match Button component's default text-line-height affordance — verify against Button component if in doubt).
   - Do NOT change the `disabled={!canOpen}` gate — `canOpen` already includes `!birthing`.
   - Do NOT change the outer onClick or any of the effectiveShellOnly/isAdmin logic in the onClick handler at :1296-1337. Shell-only branch behavior stays byte-identical.

7) **Delete the BirthProgress render block at :1273-1282** — remove the entire `{showBirthProgress && (<BirthProgress .../>)}` JSX block. The `<div className="flex flex-col gap-3">` container closes normally after the identity-cluster block at :1271.

8) **Rewrite `handleBirth`'s SSE loop + terminal handling** (D-11, D-14, D-15, D-17, D-18):
   - At `handleBirth` :690-817:
     - Delete the `setBirthProgress(INITIAL_BIRTH_PROGRESS.map((s) => ({ ...s })));` at :693 and `setBirthFailedStep(null);` at :694 — those state hooks are gone.
     - Keep `setBirthing(true);` at :692 and `abortControllerRef.current = new AbortController();` at :695.
     - Inside the `for await (const evt of stream)` loop at :753-771: DELETE the entire `if (evt.type === "step") { ... }` branch at :754-766 (frontend discards step events per D-12). Keep only the `else if (evt.type === "ended") { endedEvent = evt; break; }` clause; simplify to `if (evt.type === "ended") { endedEvent = evt; break; }`.
     - Success path at :773-802 STAYS: refreshIdentities → onCreate → setBirthing(false) → onClose (D-18 + D-16 — do not touch this chain).
     - Failure `else` branch at :803-806: replace `setBirthing(false);` with the D-17 chain: `window.alert("agent creation failed"); setBirthing(false); onClose();`.
     - Outer `catch (_e)` block at :807-816: replace the whole body (setBirthProgress + setBirthFailedStep + setBirthing) with `window.alert("agent creation failed"); setBirthing(false); onClose();` — same failure surface as the ended:ok:false branch per D-17 (Skynet-side stream error OR any thrown fetch/network error routes through the same alert).
   - Note on refresh timing (D-18 preservation): `await refreshIdentities()` must fire AFTER the ended:ok event is consumed and BEFORE `onCreate()` — the existing order at :783 (`await refreshIdentities();`) then :794-800 (`onCreate({...});`) is correct; do not reorder.
  </action>
  <verify>
    <automated>grep -c "BirthProgress" src/ui/sidebar/NewSessionDialog.tsx | grep -q "^0$" &amp;&amp; grep -c "BIRTH_STEP_LABELS" src/ui/sidebar/NewSessionDialog.tsx | grep -q "^0$" &amp;&amp; grep -c "BIRTH_STEP_BLURBS" src/ui/sidebar/NewSessionDialog.tsx | grep -q "^0$" &amp;&amp; grep -c "BirthStepState" src/ui/sidebar/NewSessionDialog.tsx | grep -q "^0$" &amp;&amp; grep -c "INITIAL_BIRTH_PROGRESS" src/ui/sidebar/NewSessionDialog.tsx | grep -q "^0$" &amp;&amp; grep -c "birthFailedStep" src/ui/sidebar/NewSessionDialog.tsx | grep -q "^0$" &amp;&amp; grep -c "birthProgress" src/ui/sidebar/NewSessionDialog.tsx | grep -q "^0$" &amp;&amp; grep -c "resetBirthProgress" src/ui/sidebar/NewSessionDialog.tsx | grep -q "^0$" &amp;&amp; grep -c "window.alert(\"agent creation failed\")" src/ui/sidebar/NewSessionDialog.tsx | grep -q -v "^0$" &amp;&amp; grep -c "Loader2" src/ui/sidebar/NewSessionDialog.tsx | grep -q -v "^0$" &amp;&amp; grep -c "refreshIdentities" src/ui/sidebar/NewSessionDialog.tsx | grep -q -v "^0$" &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'BirthProgress' src/ui/sidebar/NewSessionDialog.tsx` returns 0
    - Source assertion: `grep -c 'BIRTH_STEP_LABELS' src/ui/sidebar/NewSessionDialog.tsx` returns 0
    - Source assertion: `grep -c 'BIRTH_STEP_BLURBS' src/ui/sidebar/NewSessionDialog.tsx` returns 0
    - Source assertion: `grep -c 'BirthStepState' src/ui/sidebar/NewSessionDialog.tsx` returns 0
    - Source assertion: `grep -c 'INITIAL_BIRTH_PROGRESS' src/ui/sidebar/NewSessionDialog.tsx` returns 0
    - Source assertion: `grep -c 'birthFailedStep' src/ui/sidebar/NewSessionDialog.tsx` returns 0
    - Source assertion: `grep -c 'birthProgress' src/ui/sidebar/NewSessionDialog.tsx` returns 0
    - Source assertion: `grep -c 'setBirthProgress' src/ui/sidebar/NewSessionDialog.tsx` returns 0
    - Source assertion: `grep -c 'resetBirthProgress' src/ui/sidebar/NewSessionDialog.tsx` returns 0
    - Source assertion: `grep -c 'evt.type === \"step\"' src/ui/sidebar/NewSessionDialog.tsx` returns 0 (frontend discards step events per D-12)
    - Source assertion: `grep -c 'window.alert("agent creation failed")' src/ui/sidebar/NewSessionDialog.tsx` returns 2 or more (ended:ok:false branch + outer catch)
    - Source assertion: `grep -c 'Loader2' src/ui/sidebar/NewSessionDialog.tsx` returns 2 or more (import + Create button render)
    - Source assertion: `grep -c 'refreshIdentities' src/ui/sidebar/NewSessionDialog.tsx` returns 2 or more (import + call site — D-18 preserved)
    - Source assertion: `grep -c '!birthing' src/ui/sidebar/NewSessionDialog.tsx` returns at least 1 match near the Dialog onOpenChange handler (modal close lock — D-15)
    - Source assertion: `grep -c 'const \\[birthing, setBirthing\\]' src/ui/sidebar/NewSessionDialog.tsx` returns 1 (survivor state per D-15)
    - Source assertion (shell-only regression guard): `grep -c 'identityMode: false' src/ui/sidebar/NewSessionDialog.tsx` returns 1 (still emitted by the shell-only submit branch — untouched)
    - Behavior assertion (D-16 non-touch): `git diff src/ui/AppShell.tsx` shows zero changes
    - Behavior assertion: `npm run build` exits 0 (frontend Vite/TSC build passes)
  </acceptance_criteria>
  <done>
    BirthProgress component + all step-checklist tables/types/state deleted; Loader2 spinner replaces the Create button label during birthing; modal close is disabled during birthing (both Cancel button and Dialog onOpenChange); success path preserves refreshIdentities → onCreate → onClose chain; both failure paths (ended:ok:false and outer catch) call `window.alert("agent creation failed")` then onClose; shell-only branch is byte-identical to pre-phase; AppShell.tsx untouched; frontend build passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend SSE stream → frontend NewSessionDialog | Browser consumes `event: birth\ndata: {...}` frames. This phase does not add a new trust boundary; it collapses the frontend's per-step trust model into a terminal-only trust model (frontend only trusts `ended` events; `step` events are discarded per D-12). |
| User → modal (close attempt) | New modal-lock gate: `onOpenChange` no-ops while `birthing===true`. Not a security boundary — a user attempting to close mid-birth is not adversarial; the gate exists to prevent partial-UX-state per shape §"Modal is fully locked from click to resolution". |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-07 | Information Disclosure | `window.alert` message text ("agent creation failed") reveals no sensitive information | accept | Message is a fixed literal string with no interpolation of identity name, host, error text, or reason field. D-11 explicitly locks the frontend to ignore the reason string. |
| T-106-08 | Denial of Service | User cannot close modal during 120s birthing window | accept | Bounded by backend's 120s timeout (Plan 106-01). Component-unmount cleanup at :533-537 aborts the stream if the user navigates away entirely (e.g. tab close, route change) — abortControllerRef still fires on unmount. |
| T-106-09 | Repudiation | User claims "I clicked Create but no agent was created" after alert | mitigate | Backend structured log at `identity_birth_supervisor_wait_timeout` (Plan 106-01) provides the audit trail. Frontend intentionally does NOT surface a distinct error class per D-17 — the log is the diagnosis surface. |
| T-106-10 | Elevation of Privilege | Non-admin using dev tools to force `shellOnly=true` | mitigate | Pre-existing defense-in-depth: `effectiveShellOnly = isAdmin && shellOnly` at :849, plus `handleBirth` submit-onclick invariant at :1314. This phase does NOT weaken those gates — the shell-only branch of the onClick handler stays byte-identical per D-15 regression guard. |

**Package Legitimacy Gate:** N/A — this plan installs zero new npm packages.
</threat_model>

<verification>
- All source assertions in Task 1's `acceptance_criteria` pass.
- `git diff src/ui/AppShell.tsx` returns empty (D-16 non-touch invariant).
- `npm run build` exits 0.
- Manual visual smoke-check (executor performs): open the New Agent modal, pick host+role+name, click Create — Create button shows spinner, other buttons/fields disabled, X in dialog header does nothing. When backend closes SSE (this plan doesn't run backend end-to-end — see Plan 106-03 for tests), the modal either auto-closes into the new tab (success) or fires the alert then closes (failure).
- Existing frontend tests (`npx vitest run src/ui/sidebar/NewSessionDialog.test.tsx src/ui/sidebar/NewSessionDialog.chain.test.tsx`) EXPECTED to fail in this plan (they assert on BirthProgress rendering) — Plan 106-04 updates the tests. Do NOT block plan-close on those failing.
</verification>

<success_criteria>
- BirthProgress component and all associated tables/types/state are deleted from NewSessionDialog.tsx.
- Create button renders `<Loader2 />` while `birthing===true`; renders normal "Create" text otherwise.
- Dialog's `onOpenChange` no-ops the close when `birthing===true`; Cancel button is disabled during birthing (not hidden).
- On success (ended:ok:true): `await refreshIdentities()` → `onCreate({...identityMode:true...})` → `setBirthing(false)` → `onClose()` chain fires (unchanged from pre-phase).
- On failure (ended:ok:false OR outer catch): `window.alert("agent creation failed")` → `setBirthing(false)` → `onClose()` chain fires — no fields preserved, user re-opens fresh.
- Shell-only branch submit path (`identityMode: false` at :1330-1335) is byte-identical to pre-phase behavior.
- AppShell.tsx is untouched.
- Frontend `npm run build` exits 0.
</success_criteria>

<output>
Create `.planning/phases/106-birth-flow-rework-chunk-3-retire-skynet-tmux-claude-launch-s/106-02-SUMMARY.md` when done. Note in the summary: (a) whether Loader2 sizing chosen was size-4 or another value (with rationale if different), (b) whether Cancel button was made `disabled` or hidden during birthing (spec: disabled), (c) any deviations from action steps + rationale.
</output>
