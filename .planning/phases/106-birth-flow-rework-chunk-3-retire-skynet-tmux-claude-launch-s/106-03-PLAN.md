---
phase: 106-birth-flow-rework-chunk-3-retire-skynet-tmux-claude-launch-s
plan: 03
type: execute
wave: 2
depends_on: [106-01]
files_modified:
  - src/backend/database/routes/identity-birth-orchestrator.test.ts
  - src/backend/database/routes/identity-birth-orchestrator.role-frontmatter.test.ts
  - src/backend/database/routes/identity-birth-orchestrator.mxid-derivation.test.ts
  - src/backend/database/routes/identity-birth.test.ts
autonomous: true
requirements: [D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-20]

must_haves:
  truths:
    - "All step:3, step:4, step:5 assertions are removed from all four backend test files"
    - "identity-birth-orchestrator.test.ts includes a success-case test where mock discoverIdentitySessionFile returns non-null on Nth poll and asserts final `ended:ok:true` event"
    - "identity-birth-orchestrator.test.ts includes a timeout-case test where mock discoverIdentitySessionFile returns null throughout and asserts final `ended:ok:false, reason:'supervisor_wait_timeout'` event"
    - "identity-birth-orchestrator.test.ts includes an SSH-error-during-poll test (mock throws or returns null due to error) and asserts the timeout path fires"
    - "identity-birth-orchestrator.test.ts still asserts step:6, step:7, step:8 events emit on the mint block (log-forensic breadcrumbs preserved per D-12)"
    - "identity-birth.test.ts SSE endpoint stream-shape test consumes the new ended event with optional reason field"
    - "identity-harness-start.test.ts is UNCHANGED (helper is still called by clone)"
    - "All updated tests pass under `npx vitest run` scoped to the four touched files"
  artifacts:
    - path: "src/backend/database/routes/identity-birth-orchestrator.test.ts"
      provides: "Updated tests: no step:3/4/5 assertions; new wait-poll success/timeout/error tests"
      contains: "supervisor_wait_timeout"
    - path: "src/backend/database/routes/identity-birth-orchestrator.role-frontmatter.test.ts"
      provides: "Step 2.5 identity-file body assertions preserved; step:3+ assertions removed"
    - path: "src/backend/database/routes/identity-birth-orchestrator.mxid-derivation.test.ts"
      provides: "Step 6 MXID derivation assertions preserved; step:3+ assertions removed"
    - path: "src/backend/database/routes/identity-birth.test.ts"
      provides: "SSE endpoint stream-shape test updated for new event contract"
  key_links:
    - from: "test files"
      to: "src/backend/database/routes/identity-birth-orchestrator.ts"
      via: "BirthDeps.discoverIdentitySessionFile mock injection"
      pattern: "discoverIdentitySessionFile:"
---

<objective>
Update the four backend test files that pin the birth orchestrator's behavior
so they match the new wire contract shipped in Plan 106-01: no step:3/4/5
events, wait-for-supervisor poll block, ended event with optional reason
string. Add three new tests for the wait-poll's success/timeout/error paths
plus verify Steps 6/7/8 still emit their forensic breadcrumbs.

Purpose: prevent regression on the sole-spawner architecture. The step:3/4/5
retirement is the load-bearing change; the tests that today count those events
would silently fail-open (or worse, silently over-assert) without this
update. The three new wait-poll tests pin the D-06/07/08 contract into
executable form so any future refactor that changes cadence or timeout gets
caught.

Output: modified test files. `identity-harness-start.test.ts` is byte-untouched
(helper is still called by clone flow per D-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-birth-flow-rework-chunk-3-retire-skynet-tmux-claude-launch-s/106-CONTEXT.md
@.planning/shapes/shape-birth-flow-supervisor-sole-spawner.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update identity-birth-orchestrator.test.ts — remove step:3/4/5 assertions; add wait-poll success/timeout/error tests</name>
  <files>src/backend/database/routes/identity-birth-orchestrator.test.ts</files>
  <read_first>
    - src/backend/database/routes/identity-birth-orchestrator.test.ts (FULL file — locate every assertion mentioning `n: 3`, `n: 4`, `n: 5`, `startHarnessOnIdentity`, `step: 3`, `step: 4`, `step: 5`; locate the existing BirthDeps mock builder pattern used across tests; locate the vi.useFakeTimers / vi.advanceTimersByTime helper usage if any)
    - src/backend/database/routes/identity-birth-orchestrator.ts (Plan 106-01 output — BirthDeps.discoverIdentitySessionFile signature; WAIT_FOR_SUPERVISOR_POLL_MS=2000; WAIT_FOR_SUPERVISOR_TIMEOUT_MS=120000; new BirthEvent.ended.reason field)
    - src/backend/claude-session/discover-identity-session-file.ts (return type: Promise&lt;string | null&gt;; null-return-on-error contract)
    - .planning/phases/106-birth-flow-rework-chunk-3-retire-skynet-tmux-claude-launch-s/106-CONTEXT.md § D-20 (exact test-update requirements)
  </read_first>
  <action>
Make four coordinated edits inside `src/backend/database/routes/identity-birth-orchestrator.test.ts`:

1) **Delete all assertions referring to step:3, step:4, step:5 events.** Grep the file for `n: 3`, `n: 4`, `n: 5`, `phase: "started"` in the context of step 3+, and `phase: "completed"` in the context of step 3+. Every `expect(emitted).toContainEqual({ type: "step", n: 3, ... })` and similar assertions for n=4,5 goes. Any test whose primary purpose was asserting the step-3 harness-launch behavior (e.g. Test 14 per the CONTEXT reference) needs its assertion body rewritten around the new contract — the test's `it("...")` name may need updating to reflect what it now pins (e.g. "step 3 harness launch failure" becomes "supervisor wait timeout").

2) **Delete all mocks of `startHarnessOnIdentity`.** The helper is no longer imported by the orchestrator (per Plan 106-01 edit 4). Any `vi.mock('./identity-harness-start', ...)` or explicit stub replacement of that dep goes. If the test file's BirthDeps mock builder had a `startHarnessOnIdentity` field, delete it.

3) **Extend every existing BirthDeps mock instance with a `discoverIdentitySessionFile` field.** Default implementation for existing "happy path" tests: a mock that returns `"~/.claude/projects/mock/session.jsonl"` on the FIRST call (so the wait-poll exits immediately with success). Signature per Plan 106-01: `(conn, identityName) => Promise<string | null>`. Suggested vitest pattern: `discoverIdentitySessionFile: vi.fn().mockResolvedValue("/mock/path.jsonl")`.

4) **Add three new tests** inside the same describe block that already tests `birthIdentity`. Suggested test names + body sketches (prose — actual test code goes in the file):

   Test A: `"emits ended:ok:true after discoverIdentitySessionFile returns non-null on 3rd poll"`
   - Setup: mock `discoverIdentitySessionFile` with `vi.fn()` that returns `null` twice then `"/found/path.jsonl"` on the third call.
   - Use `vi.useFakeTimers()` + `vi.advanceTimersByTimeAsync(WAIT_FOR_SUPERVISOR_POLL_MS * 3)` (import the constant from the orchestrator module) to fast-forward through the poll cadence.
   - Assert final emitted event equals `{ type: "ended", ok: true, identityId: <name>, sessionName: <name> }`.
   - Assert mock was called 3 times.
   - Assert step:6, step:7, step:8 emits happened before the ended event (log-forensic breadcrumbs per D-12).

   Test B: `"emits ended:ok:false reason:supervisor_wait_timeout after 120s of null returns"`
   - Setup: mock `discoverIdentitySessionFile` with `vi.fn().mockResolvedValue(null)` — always returns null.
   - Use `vi.useFakeTimers()` + `vi.advanceTimersByTimeAsync(WAIT_FOR_SUPERVISOR_TIMEOUT_MS + 1000)` to blow past the timeout.
   - Assert final emitted event equals `{ type: "ended", ok: false, reason: "supervisor_wait_timeout" }`.
   - Assert mock was called approximately `Math.floor(WAIT_FOR_SUPERVISOR_TIMEOUT_MS / WAIT_FOR_SUPERVISOR_POLL_MS)` times (allow off-by-one — the assertion should be `>= 59 && <= 61` for 60 expected calls).
   - Assert `databaseLogger.warn` was called with `operation: "identity_birth_supervisor_wait_timeout"` — mock the logger via `vi.mock('../../utils/logger.js', ...)` if not already mocked in the test file.

   Test C: `"emits ended:ok:false reason:supervisor_wait_timeout when discoverIdentitySessionFile throws SSH errors throughout"`
   - Setup: mock `discoverIdentitySessionFile` to `vi.fn().mockRejectedValue(new Error("SSH channel closed"))` OR return `null` (matches the module's fail-safe null-return contract at discover-identity-session-file.ts:319-321) — either shape simulates the SSH-error path. Prefer null-return per the module's actual behavior.
   - Fast-forward same as Test B.
   - Assert same terminal event shape as Test B.
   - Rationale: this test proves the wait-poll block treats SSH errors identically to real "signal not found" — matches the fail-safe contract.

   Test D (bonus, ensures D-12 breadcrumb preservation): `"still emits step:6, step:7, step:8 events on happy path"`
   - Setup: happy-path BirthDeps + discoverIdentitySessionFile returning non-null on first call.
   - Assert `emitted` array (in order) contains `{ type: "step", n: 6, phase: "started" }`, `{ type: "step", n: 6, phase: "completed" }`, `{ type: "step", n: 7, phase: "started" }`, `{ type: "step", n: 7, phase: "completed" }`, `{ type: "step", n: 8, phase: "started" }`, `{ type: "step", n: 8, phase: "completed" }`, then the final ended event.

5) **Preserve all Step 1, Step 2, Step 2.5, Step 6, Step 7, Step 8 tests.** These pin behavior that this phase does not change. If any of those tests were coupled to the step:3+ event chain via shared setup, decouple them (extract shared fixtures if useful).
  </action>
  <verify>
    <automated>grep -c "n: 3" src/backend/database/routes/identity-birth-orchestrator.test.ts | grep -q "^0$" &amp;&amp; grep -c "n: 4" src/backend/database/routes/identity-birth-orchestrator.test.ts | grep -q "^0$" &amp;&amp; grep -c "n: 5" src/backend/database/routes/identity-birth-orchestrator.test.ts | grep -q "^0$" &amp;&amp; grep -c "startHarnessOnIdentity" src/backend/database/routes/identity-birth-orchestrator.test.ts | grep -q "^0$" &amp;&amp; grep -c "supervisor_wait_timeout" src/backend/database/routes/identity-birth-orchestrator.test.ts | grep -q -v "^0$" &amp;&amp; grep -c "discoverIdentitySessionFile" src/backend/database/routes/identity-birth-orchestrator.test.ts | grep -q -v "^0$" &amp;&amp; npx vitest run src/backend/database/routes/identity-birth-orchestrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'n: 3' src/backend/database/routes/identity-birth-orchestrator.test.ts` returns 0
    - Source assertion: `grep -c 'n: 4' src/backend/database/routes/identity-birth-orchestrator.test.ts` returns 0
    - Source assertion: `grep -c 'n: 5' src/backend/database/routes/identity-birth-orchestrator.test.ts` returns 0
    - Source assertion: `grep -c 'startHarnessOnIdentity' src/backend/database/routes/identity-birth-orchestrator.test.ts` returns 0
    - Source assertion: `grep -c 'supervisor_wait_timeout' src/backend/database/routes/identity-birth-orchestrator.test.ts` returns 2 or more (present in both the timeout test AND the SSH-error test)
    - Source assertion: `grep -c 'discoverIdentitySessionFile' src/backend/database/routes/identity-birth-orchestrator.test.ts` returns 4 or more (mock declarations across multiple tests + assertions)
    - Source assertion: at least one test uses `WAIT_FOR_SUPERVISOR_TIMEOUT_MS` and at least one uses `WAIT_FOR_SUPERVISOR_POLL_MS` — `grep -c 'WAIT_FOR_SUPERVISOR' src/backend/database/routes/identity-birth-orchestrator.test.ts` returns 2 or more
    - Source assertion (D-12 forensics preserved): `grep -c 'n: 6' src/backend/database/routes/identity-birth-orchestrator.test.ts` returns 1 or more (Test D above)
    - Source assertion (D-12 forensics preserved): `grep -c 'n: 8' src/backend/database/routes/identity-birth-orchestrator.test.ts` returns 1 or more
    - Test command: `npx vitest run src/backend/database/routes/identity-birth-orchestrator.test.ts` exits 0
  </acceptance_criteria>
  <done>
    All step:3/4/5 assertions removed; all `startHarnessOnIdentity` mocks removed; every BirthDeps mock instance gains a `discoverIdentitySessionFile` field; three new wait-poll tests (success on Nth poll, timeout, SSH-error path) added + one D-12 forensics test; step:6/7/8 assertions preserved; file passes vitest run.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update the three sibling backend test files (role-frontmatter, mxid-derivation, identity-birth SSE route)</name>
  <files>src/backend/database/routes/identity-birth-orchestrator.role-frontmatter.test.ts, src/backend/database/routes/identity-birth-orchestrator.mxid-derivation.test.ts, src/backend/database/routes/identity-birth.test.ts</files>
  <read_first>
    - src/backend/database/routes/identity-birth-orchestrator.role-frontmatter.test.ts (FULL file — locate BirthDeps mock builder; locate any step:3+ assertions)
    - src/backend/database/routes/identity-birth-orchestrator.mxid-derivation.test.ts (FULL file — same)
    - src/backend/database/routes/identity-birth.test.ts (FULL file — locate SSE endpoint stream-shape test; locate any step:3/4/5 assertions in the stream-shape test)
    - src/backend/database/routes/identity-birth.ts (Plan 106-01 Task 2 output — new keepalive interval + wired discoverIdentitySessionFile dep)
    - src/backend/database/routes/identity-birth-orchestrator.ts (Plan 106-01 Task 1 output — new BirthEvent.ended.reason field)
    - .planning/phases/106-birth-flow-rework-chunk-3-retire-skynet-tmux-claude-launch-s/106-CONTEXT.md § D-20 (exact per-file requirements)
  </read_first>
  <action>
Make three coordinated updates, one per file:

1) **`identity-birth-orchestrator.role-frontmatter.test.ts`**:
   - Delete every assertion mentioning `n: 3`, `n: 4`, `n: 5`, `step: 3`, `step: 4`, `step: 5`. Grep to confirm exhaustive removal.
   - Delete any `startHarnessOnIdentity` mocks (the import is gone from the orchestrator).
   - Add `discoverIdentitySessionFile: vi.fn().mockResolvedValue("/mock/path.jsonl")` to every BirthDeps mock instance in the file — mirrors Task 1 edit 3. Without this the tests will hang on the wait-poll loop.
   - PRESERVE every Step 2.5 identity-file-body assertion — this file's core purpose is pinning frontmatter round-tripping (title/colorHue/voice/avatar/task absent-⇒-omit invariants). Those tests do NOT touch step:3+ and stay byte-identical.

2) **`identity-birth-orchestrator.mxid-derivation.test.ts`**:
   - Same three edits as file 1 (remove step:3+ assertions; remove startHarnessOnIdentity mocks; add discoverIdentitySessionFile to every BirthDeps mock).
   - PRESERVE every Step 6 MXID derivation assertion — this file's core purpose is pinning `composeMxidLocalpart` + `deriveMxidWithOrdinal` behavior + Step 6 error propagation. Those tests do NOT touch step:3+.

3) **`identity-birth.test.ts`** (SSE endpoint tests, not the pure orchestrator):
   - Locate the stream-shape assertion (the test that consumes the SSE stream and asserts the sequence of events emitted).
   - Delete step:3/step:4/step:5 assertions from that sequence.
   - Add an assertion that failure-path terminal events carry the new `reason` field on the `ended` event (e.g. an ended event on a matrix-503 fail-early path can be checked to include a reason, though the 503 fires BEFORE SSE opens so may not apply — pick a test path where a step failure happens after SSE opens and assert the ended event contains a `reason` key). If no such existing failure-after-SSE test exists in this file, add ONE new test that provokes a Step 6 failure via a mocked `matrixCreateOrUpdateUser: vi.fn().mockResolvedValue({ ok: false, status: 500, error: "test failure" })` and asserts the ended event shape includes `{ type: "ended", ok: false, failedStep: 6, reason: <string> }`.
   - Add discoverIdentitySessionFile to any BirthDeps or dep wiring that runs a happy-path birth through the SSE route in this file (if the file mocks the whole birthIdentity function instead of BirthDeps, this may be a no-op — inspect first).
   - Add ONE new keepalive-behavior test if the file has infrastructure for SSE consumption assertions: assert that during a slow birthIdentity call, the SSE response body includes at least one `:keepalive` line (the leading colon = SSE comment). This is optional-if-hard-to-write — the file's existing test infrastructure may not support timing-based SSE payload inspection cleanly. If the shape is easy (there's already a mock timer + payload-collection helper), add it; otherwise skip and note in the SUMMARY.

4) **`identity-harness-start.test.ts`** — UNCHANGED. Do NOT touch this file per D-20. Verify by `git diff --stat src/backend/database/routes/identity-harness-start.test.ts` returning empty.
  </action>
  <verify>
    <automated>grep -c "n: 3" src/backend/database/routes/identity-birth-orchestrator.role-frontmatter.test.ts | grep -q "^0$" &amp;&amp; grep -c "n: 3" src/backend/database/routes/identity-birth-orchestrator.mxid-derivation.test.ts | grep -q "^0$" &amp;&amp; grep -c "n: 3" src/backend/database/routes/identity-birth.test.ts | grep -q "^0$" &amp;&amp; grep -c "startHarnessOnIdentity" src/backend/database/routes/identity-birth-orchestrator.role-frontmatter.test.ts | grep -q "^0$" &amp;&amp; grep -c "startHarnessOnIdentity" src/backend/database/routes/identity-birth-orchestrator.mxid-derivation.test.ts | grep -q "^0$" &amp;&amp; npx vitest run src/backend/database/routes/identity-birth-orchestrator.role-frontmatter.test.ts src/backend/database/routes/identity-birth-orchestrator.mxid-derivation.test.ts src/backend/database/routes/identity-birth.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion (role-frontmatter): `grep -c 'n: 3' src/backend/database/routes/identity-birth-orchestrator.role-frontmatter.test.ts` returns 0
    - Source assertion (role-frontmatter): `grep -c 'n: 4' src/backend/database/routes/identity-birth-orchestrator.role-frontmatter.test.ts` returns 0
    - Source assertion (role-frontmatter): `grep -c 'n: 5' src/backend/database/routes/identity-birth-orchestrator.role-frontmatter.test.ts` returns 0
    - Source assertion (role-frontmatter): `grep -c 'discoverIdentitySessionFile' src/backend/database/routes/identity-birth-orchestrator.role-frontmatter.test.ts` returns 1 or more
    - Source assertion (mxid-derivation): `grep -c 'n: 3' src/backend/database/routes/identity-birth-orchestrator.mxid-derivation.test.ts` returns 0
    - Source assertion (mxid-derivation): `grep -c 'discoverIdentitySessionFile' src/backend/database/routes/identity-birth-orchestrator.mxid-derivation.test.ts` returns 1 or more
    - Source assertion (birth-route): `grep -c 'n: 3' src/backend/database/routes/identity-birth.test.ts` returns 0
    - Source assertion (birth-route): `grep -c 'reason' src/backend/database/routes/identity-birth.test.ts` returns 1 or more (new failure-shape assertion)
    - Source assertion (harness): `git diff --stat src/backend/database/routes/identity-harness-start.test.ts` returns empty (untouched per D-20)
    - Test command: `npx vitest run src/backend/database/routes/identity-birth-orchestrator.role-frontmatter.test.ts src/backend/database/routes/identity-birth-orchestrator.mxid-derivation.test.ts src/backend/database/routes/identity-birth.test.ts` exits 0
  </acceptance_criteria>
  <done>
    Three sibling backend test files have step:3/4/5 assertions removed and discoverIdentitySessionFile wired into every BirthDeps mock; Step 2.5 identity-file assertions and Step 6 MXID derivation assertions preserved intact; SSE endpoint stream-shape test consumes the new ended.reason field on a failure-after-SSE path; identity-harness-start.test.ts is byte-untouched; all three files pass vitest run.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Test files are not on the runtime trust boundary — they are the assurance-that-boundaries-hold layer. No new attack surface introduced by this plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-11 | Repudiation | Tests could be over-loose and let regression slip through | mitigate | Explicit source assertions in acceptance_criteria pin the shape (no step:3/4/5 mentions, ≥ N supervisor_wait_timeout mentions, ≥ N discoverIdentitySessionFile mentions). If the executor writes weak tests, the acceptance_criteria greps flag the gap. |
| T-106-12 | Tampering | Fake-timers misuse could cause tests to hang or false-pass | mitigate | Test A/B/C prescribe explicit `vi.advanceTimersByTimeAsync` values tied to the exported constants (WAIT_FOR_SUPERVISOR_POLL_MS × N, WAIT_FOR_SUPERVISOR_TIMEOUT_MS + 1000). Vitest's fake-timer semantics are well-understood; the existing test file already uses them (existing STEP_2_SLEEP_MS / ENTER_TRAIN_SPACING_MS tests). |

**Package Legitimacy Gate:** N/A — this plan installs zero new npm packages.
</threat_model>

<verification>
- All source assertions from Task 1 and Task 2 pass.
- `npx vitest run src/backend/database/routes/identity-birth-orchestrator.test.ts src/backend/database/routes/identity-birth-orchestrator.role-frontmatter.test.ts src/backend/database/routes/identity-birth-orchestrator.mxid-derivation.test.ts src/backend/database/routes/identity-birth.test.ts` exits 0.
- `npx vitest run src/backend/database/routes/identity-harness-start.test.ts` still exits 0 (harness helper still tested for clone-flow contract; file untouched by this plan per D-20).
- `npm run build:backend` still exits 0 (test-file TS changes don't break backend build).
</verification>

<success_criteria>
- Four backend test files reflect the new wire contract.
- Three new wait-poll tests (success on Nth poll, timeout with reason, SSH-error null-return path) added to `identity-birth-orchestrator.test.ts`.
- Step 2.5 identity-file assertions in `role-frontmatter.test.ts` intact.
- Step 6 MXID derivation assertions in `mxid-derivation.test.ts` intact.
- SSE stream-shape test in `identity-birth.test.ts` asserts the new ended.reason field on a failure-after-SSE path.
- `identity-harness-start.test.ts` is byte-untouched (D-03 + D-20 non-touch invariant).
- Scoped vitest run passes for all four touched files.
</success_criteria>

<output>
Create `.planning/phases/106-birth-flow-rework-chunk-3-retire-skynet-tmux-claude-launch-s/106-03-SUMMARY.md` when done. Note in the summary: (a) whether the optional keepalive-behavior test in `identity-birth.test.ts` was implemented or skipped (with rationale if skipped), (b) exact test count added per file, (c) any deviations from action steps + rationale.
</output>
