---
phase: 106-birth-flow-rework-chunk-3-retire-skynet-tmux-claude-launch-s
plan: 04
type: execute
wave: 2
depends_on: [106-02]
files_modified:
  - src/ui/sidebar/NewSessionDialog.test.tsx
  - src/ui/sidebar/NewSessionDialog.chain.test.tsx
autonomous: true
requirements: [D-13, D-14, D-15, D-16, D-17, D-18, D-21]

must_haves:
  truths:
    - "All BirthProgress-render assertions removed from NewSessionDialog.test.tsx"
    - "NewSessionDialog.test.tsx includes spinner-in-Create-button assertion for birthing===true state"
    - "NewSessionDialog.test.tsx includes close-disabled-during-birthing assertion (Cancel button disabled + Dialog onOpenChange no-op)"
    - "NewSessionDialog.test.tsx includes window.alert-on-failure assertion (both ended:ok:false and outer-catch paths)"
    - "NewSessionDialog.chain.test.tsx verifies birth+auto-route chain still fires after the ended:ok event (refreshIdentities → onCreate → onClose)"
    - "NewSessionDialog.role-dropdown.test.tsx is UNCHANGED (unrelated axis per D-21)"
    - "NewSessionDialog.task-input.test.tsx is UNCHANGED (unrelated axis per D-21)"
    - "All updated tests pass under `npx vitest run` scoped to the two touched files"
  artifacts:
    - path: "src/ui/sidebar/NewSessionDialog.test.tsx"
      provides: "Updated tests: no BirthProgress assertions; new spinner + close-lock + alert assertions"
      contains: "agent creation failed"
    - path: "src/ui/sidebar/NewSessionDialog.chain.test.tsx"
      provides: "birth+auto-route chain test updated for new ended event shape"
  key_links:
    - from: "test files"
      to: "src/ui/sidebar/NewSessionDialog.tsx"
      via: "openBirthStream mock returning new event shape"
      pattern: "openBirthStream"
---

<objective>
Update the two frontend test files that pin `NewSessionDialog`'s
identity-mode-branch behavior so they match the new UX shipped in Plan 106-02:
no BirthProgress checklist, spinner-in-Create-button during birthing,
modal-lock during birthing, `window.alert("agent creation failed")` on any
failure path. Verify the birth-success auto-route chain
(refreshIdentities → onCreate → onClose) still fires.

Purpose: prevent regression on the frontend half of the sole-spawner
architecture. The BirthProgress deletion is the load-bearing frontend change;
existing tests that assert the checklist rendering must be updated to assert
the new spinner-in-button UX. The failure-path alert assertion pins D-17's
"one generic surface" contract into executable form.

Output: modified two test files. `NewSessionDialog.role-dropdown.test.tsx`
and `NewSessionDialog.task-input.test.tsx` are byte-untouched (unrelated axes
per D-21).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-birth-flow-rework-chunk-3-retire-skynet-tmux-claude-launch-s/106-CONTEXT.md
@.planning/shapes/shape-birth-flow-supervisor-sole-spawner.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update NewSessionDialog.test.tsx — remove BirthProgress assertions; add spinner + close-lock + alert assertions</name>
  <files>src/ui/sidebar/NewSessionDialog.test.tsx</files>
  <read_first>
    - src/ui/sidebar/NewSessionDialog.test.tsx (FULL file — locate every mention of `BirthProgress`, `BIRTH_STEP_LABELS`, `Birth Progress`, `birthProgress`, `Check identity name is available`, `Open tmux session`, `Launch Claude CLI`, `Bootstrap dance`, `Send /id`, `ssh <host> tmux attach`; locate the mock builder for `openBirthStream` and any `refreshIdentities` mock)
    - src/ui/sidebar/NewSessionDialog.tsx (Plan 106-02 output — new spinner-in-button render, new modal-lock onOpenChange, new alert-on-failure flow, new evt.type === "ended"-only stream consumer)
    - src/api/identities-api.ts (search for `openBirthStream` — its return-type shape; the mocks must produce the new `BirthEvent` shape including `ended.reason`)
    - .planning/phases/106-birth-flow-rework-chunk-3-retire-skynet-tmux-claude-launch-s/106-CONTEXT.md § D-21 (exact per-file requirements)
  </read_first>
  <action>
Make four coordinated edits inside `src/ui/sidebar/NewSessionDialog.test.tsx`:

1) **Delete all assertions about BirthProgress rendering.** Grep the file for the strings that only appear in the deleted BIRTH_STEP_LABELS / BIRTH_STEP_BLURBS: `Check identity name`, `Open tmux session`, `Launch Claude CLI`, `Bootstrap dance`, `Send /id`, `Birth Progress`, `ssh <host> tmux attach`, `BirthProgress`. Every test whose primary purpose is asserting the 5-step checklist appearance goes — or gets rewritten to assert the new spinner UX.

2) **Delete tests that assert per-step tick behavior.** Any test that dispatches step:1/2/3/4/5 events through the openBirthStream mock and then asserts specific in-progress/done/failed icons per row → delete. Replace with a single test that asserts: while the SSE stream is open (no ended event yet), the Create button shows a Loader2 spinner and no step labels are visible.

3) **Add new tests for the new UX** (per D-17 + D-14 + D-15). Suggested test names + body sketches (prose):

   Test A: `"during birthing, Create button shows spinner and modal cannot be closed"`
   - Setup: render NewSessionDialog with `open={true}`, mock `openBirthStream` to return an async iterable that yields nothing (simulating a stuck stream — never resolves to ended).
   - Fill in host + role + name; click Create.
   - Wait for `birthing===true` (React state flush).
   - Assert: the Create button no longer has text content "Create"; instead its child DOM contains an element with class matching `animate-spin` (Loader2's telltale) — e.g. `expect(createButton.querySelector('.animate-spin')).toBeTruthy()`.
   - Assert: Cancel button has `disabled` attribute.
   - Assert: attempting to close the dialog (e.g. `fireEvent.keyDown(document, { key: 'Escape' })` or invoking the dialog's close mechanism) does NOT fire the `onClose` prop callback — `expect(onCloseMock).not.toHaveBeenCalled()`.

   Test B: `"on ended:ok:true, refreshIdentities fires then onCreate fires then onClose fires"`
   - Setup: mock `openBirthStream` to yield one event: `{ type: "ended", ok: true, identityId: "alicia", sessionName: "alicia" }`.
   - Mock `refreshIdentities` (import from `@/state/identities-store` — use `vi.mock`).
   - Fill in host + role + name="alicia"; click Create.
   - Wait for stream to resolve.
   - Assert: `refreshIdentities` called at least once.
   - Assert: `onCreate` prop called with an object matching `expect.objectContaining({ identityMode: true, name: "alicia" })`.
   - Assert: `onClose` prop called once.
   - Assert: refreshIdentities was called BEFORE onCreate (use `mock.invocationCallOrder` OR sequence-track manually).

   Test C: `"on ended:ok:false, window.alert('agent creation failed') fires then onClose fires"`
   - Setup: mock `window.alert` via `vi.spyOn(window, 'alert').mockImplementation(() => {})`.
   - Mock `openBirthStream` to yield one event: `{ type: "ended", ok: false, reason: "supervisor_wait_timeout" }`.
   - Fill in host + role + name; click Create.
   - Wait for stream to resolve.
   - Assert: `window.alert` called exactly once with the string `"agent creation failed"`.
   - Assert: `onClose` called once.
   - Assert: `onCreate` NOT called.

   Test D: `"on stream throw (network error / fetch reject), window.alert('agent creation failed') fires then onClose fires"`
   - Same shape as Test C but mock `openBirthStream` to throw immediately (or the AsyncIterable rejects on first iteration).
   - Same assertions as Test C — outer catch routes through the same alert per Plan 106-02 edit 8.

4) **Preserve tests unrelated to birth flow.** Any test asserting host-list rendering, host filter, name-input validation, collision-check debounce, shell-only-branch behavior, admin-gate render, chain-prefill from initialHost/initialRole — all stay intact.

5) **Regression guard for shell-only branch** (D-15 "shell-only branch untouched" clause): if the file has a shell-only submit test, verify it still passes without modification. If it doesn't have one, add ONE minimal test: `"shell-only branch submits with identityMode:false without opening birth stream"` — asserts `onCreate` called with `identityMode: false` and `openBirthStream` NEVER called.
  </action>
  <verify>
    <automated>grep -c "BirthProgress" src/ui/sidebar/NewSessionDialog.test.tsx | grep -q "^0$" &amp;&amp; grep -c "Open tmux session" src/ui/sidebar/NewSessionDialog.test.tsx | grep -q "^0$" &amp;&amp; grep -c "Launch Claude CLI" src/ui/sidebar/NewSessionDialog.test.tsx | grep -q "^0$" &amp;&amp; grep -c "agent creation failed" src/ui/sidebar/NewSessionDialog.test.tsx | grep -q -v "^0$" &amp;&amp; grep -c "animate-spin\|Loader2" src/ui/sidebar/NewSessionDialog.test.tsx | grep -q -v "^0$" &amp;&amp; npx vitest run src/ui/sidebar/NewSessionDialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'BirthProgress' src/ui/sidebar/NewSessionDialog.test.tsx` returns 0
    - Source assertion: `grep -c 'Open tmux session' src/ui/sidebar/NewSessionDialog.test.tsx` returns 0
    - Source assertion: `grep -c 'Launch Claude CLI' src/ui/sidebar/NewSessionDialog.test.tsx` returns 0
    - Source assertion: `grep -c 'Bootstrap dance' src/ui/sidebar/NewSessionDialog.test.tsx` returns 0
    - Source assertion: `grep -c 'Send /id' src/ui/sidebar/NewSessionDialog.test.tsx` returns 0
    - Source assertion: `grep -c 'ssh <host> tmux attach' src/ui/sidebar/NewSessionDialog.test.tsx` returns 0
    - Source assertion: `grep -c 'agent creation failed' src/ui/sidebar/NewSessionDialog.test.tsx` returns 2 or more (Tests C + D + optionally the assertion string)
    - Source assertion: `grep -Ec 'animate-spin|Loader2' src/ui/sidebar/NewSessionDialog.test.tsx` returns 1 or more (Test A spinner check)
    - Source assertion: `grep -c 'window.alert' src/ui/sidebar/NewSessionDialog.test.tsx` returns 2 or more (spy + assertion)
    - Source assertion (D-18 preserved): `grep -c 'refreshIdentities' src/ui/sidebar/NewSessionDialog.test.tsx` returns 1 or more (Test B)
    - Test command: `npx vitest run src/ui/sidebar/NewSessionDialog.test.tsx` exits 0
  </acceptance_criteria>
  <done>
    All BirthProgress-render assertions removed; four new UX tests added (spinner + close-lock + alert-on-ended:ok:false + alert-on-stream-throw); refreshIdentities-then-onCreate ordering preserved; shell-only branch regression guard in place; file passes vitest run.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update NewSessionDialog.chain.test.tsx — verify birth+auto-route chain still fires after ended:ok event</name>
  <files>src/ui/sidebar/NewSessionDialog.chain.test.tsx</files>
  <read_first>
    - src/ui/sidebar/NewSessionDialog.chain.test.tsx (FULL file — locate the birth+auto-route chain test; locate the openBirthStream mock builder; locate any BirthProgress-related assertions)
    - src/ui/sidebar/NewSessionDialog.tsx (Plan 106-02 output — for reference on the success-path call order)
    - src/ui/AppShell.tsx:2225-2270 (onCreateSession handler — for confirmation the auto-route mechanic is untouched)
    - .planning/phases/106-birth-flow-rework-chunk-3-retire-skynet-tmux-claude-launch-s/106-CONTEXT.md § D-16 + D-18 + D-21
  </read_first>
  <action>
Make two coordinated edits inside `src/ui/sidebar/NewSessionDialog.chain.test.tsx`:

1) **Update the birth-flow mock event shape.** Any place where the test's `openBirthStream` mock previously yielded step:1..5 events followed by ended → replace with the new contract: yield ONLY a single `{ type: "ended", ok: true, identityId: <name>, sessionName: <name> }` event (or optionally interleave 1-2 step:6/7/8 events for realism — those are ignored by the frontend per Plan 106-02 edit 8 but do not break the test either). Do NOT emit step:1/2/3/4/5 events.

2) **Delete or update any assertions on intermediate step-event rendering.** If the existing chain test asserted the BirthProgress checklist ticked visibly during the chain flow, remove those assertions. The core assertion this test file must still make: after the ended:ok:true event is consumed, the auto-route chain fires (refreshIdentities → onCreate with identityMode:true payload → onClose). If the file already asserts that chain, the test may only need mock-event-shape updates.

3) **Add or preserve an assertion for the AppShell chain-fire** — the test file's name suggests it verifies the full birth→auto-route flow end-to-end. If the file mounts AppShell (not just NewSessionDialog) and asserts that a tab opens for the new identity, keep that assertion. If it only mounts NewSessionDialog with a mocked onCreate, assert onCreate was called with `expect.objectContaining({ identityMode: true, name: <name> })` (matching Plan 106-02's D-16 payload preservation).

4) **Regression guard: no touch to `NewSessionDialog.role-dropdown.test.tsx` or `NewSessionDialog.task-input.test.tsx`** per D-21.
  </action>
  <verify>
    <automated>grep -c "BirthProgress" src/ui/sidebar/NewSessionDialog.chain.test.tsx | grep -q "^0$" &amp;&amp; grep -c "type: \"step\", n: 3\|type: \"step\", n: 4\|type: \"step\", n: 5" src/ui/sidebar/NewSessionDialog.chain.test.tsx | grep -q "^0$" &amp;&amp; grep -c "type: \"ended\", ok: true" src/ui/sidebar/NewSessionDialog.chain.test.tsx | grep -q -v "^0$" &amp;&amp; git diff --stat src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx src/ui/sidebar/NewSessionDialog.task-input.test.tsx | wc -l | grep -q "^0$" &amp;&amp; npx vitest run src/ui/sidebar/NewSessionDialog.chain.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'BirthProgress' src/ui/sidebar/NewSessionDialog.chain.test.tsx` returns 0
    - Source assertion: `grep -Ec 'type: \"step\", n: 3|type: \"step\", n: 4|type: \"step\", n: 5' src/ui/sidebar/NewSessionDialog.chain.test.tsx` returns 0
    - Source assertion: `grep -c 'type: \"ended\", ok: true' src/ui/sidebar/NewSessionDialog.chain.test.tsx` returns 1 or more (updated mock event)
    - Source assertion (D-16 payload preservation): `grep -c 'identityMode: true' src/ui/sidebar/NewSessionDialog.chain.test.tsx` returns 1 or more
    - Source assertion (D-21 non-touch): `git diff --stat src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx` returns empty
    - Source assertion (D-21 non-touch): `git diff --stat src/ui/sidebar/NewSessionDialog.task-input.test.tsx` returns empty
    - Test command: `npx vitest run src/ui/sidebar/NewSessionDialog.chain.test.tsx` exits 0
  </acceptance_criteria>
  <done>
    Chain test updated to consume the new single-ended-event contract; auto-route chain assertion preserved; role-dropdown + task-input test files byte-untouched; file passes vitest run.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Test files do not sit on the runtime trust boundary. No new attack surface introduced by this plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-13 | Repudiation | Tests could pass without actually exercising the failure-alert path | mitigate | Explicit source assertions in acceptance_criteria pin the shape (≥ 2 mentions of `agent creation failed`, ≥ 1 `window.alert` mock/assertion). Tests C + D in Task 1 exercise both failure surfaces (ended:ok:false AND stream-throw). |
| T-106-14 | Tampering | window.alert mock could leak past test boundaries | mitigate | Standard vitest cleanup via `vi.restoreAllMocks()` in afterEach (should already exist in the test file — verify at read-time). |

**Package Legitimacy Gate:** N/A — this plan installs zero new npm packages.
</threat_model>

<verification>
- All source assertions from Task 1 and Task 2 pass.
- `npx vitest run src/ui/sidebar/NewSessionDialog.test.tsx src/ui/sidebar/NewSessionDialog.chain.test.tsx` exits 0.
- `npx vitest run src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx src/ui/sidebar/NewSessionDialog.task-input.test.tsx` still exits 0 (unrelated axes, untouched).
- `npm run build` still exits 0 (test-file changes should not break frontend build).
</verification>

<success_criteria>
- `NewSessionDialog.test.tsx` no longer asserts BirthProgress rendering; includes four new UX tests (spinner-in-button, close-disabled-during-birthing, alert-on-ended:ok:false, alert-on-stream-throw).
- `NewSessionDialog.chain.test.tsx` consumes the new single-ended-event contract and preserves the D-16 auto-route chain assertion.
- `NewSessionDialog.role-dropdown.test.tsx` and `NewSessionDialog.task-input.test.tsx` are byte-untouched (D-21 non-touch invariant).
- Scoped vitest run passes for both touched files.
- Shell-only branch regression guard test present in `NewSessionDialog.test.tsx`.
</success_criteria>

<output>
Create `.planning/phases/106-birth-flow-rework-chunk-3-retire-skynet-tmux-claude-launch-s/106-04-SUMMARY.md` when done. Note in the summary: (a) exact test count added per file, (b) whether Test D (stream-throw path) was implemented as an outer-catch reject vs an AsyncIterable rejection (both are valid — pick whichever mock pattern is idiomatic in the existing test file), (c) any deviations from action steps + rationale.
</output>
