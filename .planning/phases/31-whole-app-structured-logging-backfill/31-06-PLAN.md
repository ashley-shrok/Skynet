---
phase: 31-whole-app-structured-logging-backfill
plan: 06
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - src/ui/lib/diag-registry.ts
  - src/ui/features/pretty-view/PrettyView.tsx
autonomous: true
requirements:
  - D-01
  - D-02
  - D-11
  - D-13
  - D-14
  - D-17
  - D-18
must_haves:
  truths:
    - "DIAG-REPORT tick already routes through the [render] prefix (renamed in plan 31-01 § diag-emitter.ts)"
    - "Per-pane mount/unmount lifecycle events emit [render] lines complementing the periodic DIAG-REPORT snapshot"
    - "Virtualization events (if diagnostic) emit [render] lines only at meaningful transitions (not per-frame)"
    - "PrettyView.tsx `case \"pane_state\":` handler emits `[pane-state] received phase=<...> sessionId=<...> hostId=<...>` on every frame — pairs with backend `[pane-state-emitter]` from plan 31-08 for D-16 cross-side correlation"
    - "Any state-machine transition driven by the pane_state frame emits `[pane-state] state-transition from=<prev> to=<next> trigger=pane-state-frame`"
  artifacts:
    - path: "src/ui/lib/diag-registry.ts"
      provides: "instrumented DIAG-REPORT registry with lifecycle event lines"
      contains: "[render]"
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "instrumented pane mount/unmount lifecycle + pane_state frame receive-side logging"
      contains: "[render]"
  key_links:
    - from: "PrettyView.tsx mount effect"
      to: "[render] pane-mount line"
      via: "structured pane-id + host-id extraction"
      pattern: "\\[render\\] pane-mount"
    - from: "PrettyView.tsx unmount effect"
      to: "[render] pane-unmount line"
      via: "structured pane-id + reason attribution"
      pattern: "\\[render\\] pane-unmount"
    - from: "PrettyView.tsx `case \"pane_state\":` handler (line ~1121)"
      to: "backend pane-state-emitter (plan 31-08)"
      via: "matching sessionId field enables emit↔receive correlation on the log timeline (D-16)"
      pattern: "\\[pane-state\\] received phase="
---

<objective>
Complete the `[render]` and frontend `[pane-state]` subsystem coverage per D-02:
- Plan 31-01 already remapped `[DIAG-REPORT]` → `[render]` in `diag-emitter.ts` (the sole callsite).
- This plan adds EVENT-BASED lifecycle lines that complement the periodic tick-based DIAG-REPORT snapshot: per-pane mount/unmount in PrettyView (and in Terminal.tsx if the mount effect is instrumentable without conflicting with plan 31-02's edits).
- This plan ALSO instruments the frontend `[pane-state]` receive-side handler in PrettyView.tsx (the `case "pane_state":` switch arm at ~L1121 — this is where the backend's authoritative `pane_state` frame lands and drives `setPaneState()` which flows into `usePaneResolvingMachine`). This is the D-16 cross-side correlation partner for plan 31-08's backend `[pane-state-emitter]` — WITHOUT this receive-side log, the backend can be seen emitting `pane_state:holding` but not whether/when the frontend received it or what the resolver did next.

Purpose: even when the DIAG-REPORT snapshot period doesn't line up with a bug window, mount/unmount events give edge-triggered visibility. And the pane_state receive-side log pairs with the emitter side for full cross-side observability of Phase 30's authoritative pane-state channel (patch #383). Bias overdo per D-01.

Note on placement: the revision-mode checker suggested the frontend pane_state handler lives in Terminal.tsx. It does not — the `case "pane_state":` switch arm is in PrettyView.tsx (line ~1121). Terminal.tsx has no pane_state case (verified by grep `msg.type === "pane_state"` in Terminal.tsx → 0 hits). Placing the receive-side instrumentation here (31-06) is correct because 31-06 already owns PrettyView.tsx, and this is where the handler actually lives.

Output: 2 files instrumented.

Note: this plan grew from the smallest of the wave-2 plans to a mid-sized one after the revision-mode checker asked for pane-state receive-side coverage. It ships alongside 31-02/31-03/31-04/31-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-whole-app-structured-logging-backfill/31-CONTEXT.md
@.planning/phases/31-whole-app-structured-logging-backfill/31-PATTERNS.md
@.planning/phases/31-whole-app-structured-logging-backfill/31-01-SUMMARY.md
@src/ui/features/pretty-view/useVoiceRecording.ts
@src/ui/lib/diag-emitter.ts
@src/ui/lib/diag-registry.ts
@src/ui/features/pretty-view/PrettyView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instrument diag-registry.ts + PrettyView.tsx per-pane mount/unmount events with [render] prefix</name>
  <files>src/ui/lib/diag-registry.ts, src/ui/features/pretty-view/PrettyView.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/useVoiceRecording.ts (golden-copy shape)
    - src/ui/lib/diag-emitter.ts (post-plan-31-01 — verifies [render] tick line was remapped)
    - src/ui/lib/diag-registry.ts (existing pane registry pattern — grep for `register`, `unregister`, `subscribe` methods)
    - src/ui/features/pretty-view/PrettyView.tsx (top of file — imports + mount effect; look for existing `useEffect(() => { ...; return () => { ...; };}, [...])` where the mount body registers a pane and cleanup unregisters)
    - .planning/phases/31-whole-app-structured-logging-backfill/31-CONTEXT.md § D-02 (pretty-view render lifecycle — extend existing DIAG-REPORT), D-18 (no per-render logs by default; move to lifecycle-only mount/unmount/meaningful-prop-change)
    - .planning/phases/31-whole-app-structured-logging-backfill/31-PATTERNS.md § "src/ui/lib/diag-emitter.ts" (canonical envelope shape)
  </read_first>
  <action>
    A) **diag-registry.ts** — add lifecycle event emissions on register/unregister:
    - In the `register(paneId, snapshotFn)` method (or equivalent — read the file to find the exact name), add BEFORE storing the pane in the registry map:
      * `console.info(\`[render] pane-register paneId=${paneId}\`)`
    - In the `unregister(paneId)` method, add BEFORE removing from the map:
      * `console.info(\`[render] pane-unregister paneId=${paneId}\`)`
    - If the file has a `getAll()` / snapshot-composition method that's called on every tick, do NOT log per-call (D-18 avoids per-render noise). The periodic DIAG-REPORT tick line from diag-emitter.ts already covers the snapshot cadence.

    B) **PrettyView.tsx mount/unmount effect** — instrument the top-level pane mount:
    - Find the useEffect at the top of PrettyView that runs on mount (grep `useEffect.*\\[\\]` for empty-deps effects, or the effect where a pane is registered with diag-registry). Add as the FIRST line inside the effect body:
      * `console.info(\`[render] pane-mount paneId=${paneId} paneType=pretty-view hostId=${hostId ?? 'null'} sessionKey=${sessionKey ?? 'null'}\`)`
    - Add as the FIRST line inside the cleanup return function:
      * `console.info(\`[render] pane-unmount paneId=${paneId} paneType=pretty-view hostId=${hostId ?? 'null'} sessionKey=${sessionKey ?? 'null'}\`)`
    - The paneId variable naming follows the file's existing convention (may be `paneKey`, `tabId`, etc. — use what the file uses).

    C) **Virtualization events (opt-in only if genuinely load-bearing)** — Phase 27 landed virtualization on PrettyView; Phase 28 fixed correctness issues. If the virtualizer's `scrollElement` re-bind path (H4 fix from Phase 28) currently has debug prints or comments about diagnostic hooks, ADD a structured line at the re-bind boundary:
    - `console.info(\`[render] virtualizer-rebind paneId=${paneId} scrollElementPresent=${scrollElement != null}\`)`

    If no such hook exists cleanly, do NOT add it — Phase 27/28 code is stable and this plan should not touch it just to add logs. Prioritize mount/unmount which is universally valuable.

    D) **Do NOT** touch:
    - The DIAG-REPORT tick emission in diag-emitter.ts (already handled by plan 31-01).
    - Any per-frame render effect (D-18 forbids per-render logging).
    - The virtualizer internals (Phase 27/28 already-stable code — logging touches inside there is high risk-reward-ratio).

    No fenced code blocks in this action.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/ui/features/pretty-view/PrettyView</automated>
  </verify>
  <acceptance_criteria>
    - Grep assertion: `git grep -c '\[render\] pane-register' src/ui/lib/diag-registry.ts` returns >=1.
    - Grep assertion: `git grep -c '\[render\] pane-unregister' src/ui/lib/diag-registry.ts` returns >=1.
    - Grep assertion: `git grep -c '\[render\] pane-mount' src/ui/features/pretty-view/PrettyView.tsx` returns >=1.
    - Grep assertion: `git grep -c '\[render\] pane-unmount' src/ui/features/pretty-view/PrettyView.tsx` returns >=1.
    - Grep assertion: `git grep -v '^#' src/ui/lib/diag-registry.ts | grep -c '\[DIAG-REPORT\]'` returns 0 (verify no lingering old prefix).
    - Grep assertion: `git grep -v '^#' src/ui/features/pretty-view/PrettyView.tsx | grep -c '\[DIAG-REPORT\]'` returns 0.
    - Behavior assertion: `npx tsc --noEmit` exits 0.
    - Behavior assertion: `npx vitest run src/ui/features/pretty-view/PrettyView` exits 0 (all PrettyView tests including virtualization + aside + phase29/phase30 tests still green).
  </acceptance_criteria>
  <done>
    diag-registry pane register/unregister emissions added; PrettyView mount/unmount lifecycle emissions added. All complementary to the existing periodic DIAG-REPORT tick from diag-emitter.ts (renamed in plan 31-01).
  </done>
</task>


<task type="auto">
  <name>Task 2: Instrument PrettyView.tsx `case "pane_state":` receive-side handler with [pane-state] structured logs (D-16 cross-side correlation partner for plan 31-08 emitter)</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/useVoiceRecording.ts (golden-copy shape — template-literal `[subsystem] event key=val` form)
    - src/ui/features/pretty-view/PrettyView.tsx lines 1078-1140 (the WS `onmessage` handler; `switch (parsed.type) { case "pane_state": ... setPaneState(parsed.state); ...}` at ~L1121)
    - src/ui/features/pretty-view/PrettyView.tsx lines 690-705 (the `paneState` React state slot declaration + the surrounding comments explaining Phase 30's authoritative-frame design)
    - src/ui/features/pretty-view/PrettyView.tsx lines 970-985 (the `usePaneResolvingMachine({ wsTransportState, paneState })` call site — the destination that consumes the paneState mutation)
    - src/ui/features/pretty-view/usePaneResolvingMachine.ts (the trivial 2-input resolver — its `renderedState` output is what a transition surfaces to the UI)
    - .planning/phases/31-whole-app-structured-logging-backfill/31-CONTEXT.md § D-13 ([pane-state] is the canonical prefix for the patch #383 emitter/parser), § D-16 (backend + frontend log BOTH sides so cross-correlation works), § D-12 (standard fields: hostId, sessionId, paneId when available)
    - .planning/phases/31-whole-app-structured-logging-backfill/31-PATTERNS.md § "pane-state" references + the Phase 30 patch #383 background
  </read_first>
  <action>
    Instrument the frontend receive-side of the `pane_state` WS frame in `src/ui/features/pretty-view/PrettyView.tsx`. Do NOT change behavior — instrumentation only.

    A) **Receive-side log at the `case "pane_state":` handler (line ~1121)** — add IMMEDIATELY AT THE TOP of the case body, BEFORE the existing `setPaneState(parsed.state)` call:
    - `console.info(\`[pane-state] received phase=${parsed.state} reason="${parsed.reason ?? ''}" sessionId=${sessionKey ?? 'null'} hostId=${hostId ?? 'null'} paneId=${paneId ?? 'null'}\`)`

    Field extraction rules (D-05 / D-12):
    - `parsed.state` is the authoritative pane phase string per Phase 30 pane-state-emitter — one of `"active"|"holding"|"dormant"|"inactive"|"error"`.
    - `parsed.reason` is optional diagnostic string (may be undefined per the existing threat-model comment at line 1131-1135 which flags future UI surface as an information-disclosure concern — this log line only PRINTS the reason to the console-forward stream, not the UI, so falls outside T-30-03-03; still, quote it defensively with `?? ''` fallback so `undefined` doesn't render as literal `reason=undefined` noise).
    - `sessionKey`, `hostId`, `paneId` variable names may differ in the file's local scope — read the file to see which of these are in scope at the WS onmessage closure and use the actual identifiers. If a field is truly not in scope (e.g. no `paneId` variable at this point), OMIT that specific `key=val` pair rather than fabricating one. Do NOT invent variables.
    - Use the useVoiceRecording golden-copy template-literal shape (no fenced code in this action — this snippet is a D-11 shape reference).

    B) **State-transition log** — the pane_state frame drives `setPaneState(parsed.state)` which will feed into `usePaneResolvingMachine`. Capture the from→to transition of the local `paneState` React state slot AT THE HANDLER SITE (not in the resolver) because the resolver runs on every render and D-18 forbids per-render logs. Read the CURRENT paneState via a ref (mirror the ref-of-state pattern already used elsewhere in PrettyView.tsx — e.g. `dormantRef`, `isVisibleRef`, `autoplayArmedRef`) or via the existing `paneState` closure if it's stable enough. IMMEDIATELY BEFORE the `setPaneState(parsed.state)` call add:
    - `if (paneState !== parsed.state) console.info(\`[pane-state] state-transition from=${paneState ?? 'null'} to=${parsed.state} trigger=pane-state-frame sessionId=${sessionKey ?? 'null'} hostId=${hostId ?? 'null'}\`)`
    - The `paneState !== parsed.state` guard is the D-18 discipline anchor — no-op writes (same-value setState calls, which React coalesces anyway) do NOT emit a transition log; noise-only lines are forbidden.
    - If reading `paneState` from the closure has stale-closure risk (the onmessage handler captures the paneState from mount time), fall back to introducing a small `paneStateRef` mirror using the same `useEffect(() => { paneStateRef.current = paneState }, [paneState])` pattern this file already uses for `dormantRef`. Prefer this ref pattern for correctness — a stale from= value would be worse than adding one ref.

    C) **No dedup on this line** — per D-17 opt-out discipline: pane_state frames are LOW frequency (fire on WS attach + on every actual state change from the backend, roughly 1-10 per session) and each one is diagnostically valuable individually. Do NOT wrap in `createLogDedup`.

    D) **Do NOT** touch:
    - The `case "session":`, `case "message":`, `case "image":`, or any other switch arm — those are covered by different plans (31-05 for session, no need for message body logging per D-05 privacy discipline).
    - The `setPaneState` call itself — instrumentation only, no behavior change.
    - The `usePaneResolvingMachine` hook — it's a pure derivation, adding a log inside it would fire per-render (D-18 violation).

    E) **Correlation guarantee for D-16** — the backend `[pane-state-emitter]` line from plan 31-08 MUST carry a `sessionId=<...>` field matching the value the frontend logs here. Verify plan 31-08's shipped emitter log line includes `sessionId=` — if plan 31-08 shipped a different field name (e.g. `sessionKey=` or `session=`), file a revision back to 31-08 to align. The whole point of this task is a matching field for grep-side correlation.

    No fenced code blocks in this action.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/ui/features/pretty-view/PrettyView</automated>
  </verify>
  <acceptance_criteria>
    - Grep assertion: `git grep -c '\[pane-state\] received phase=' src/ui/features/pretty-view/PrettyView.tsx` returns >=1.
    - Grep assertion: `git grep -c '\[pane-state\] state-transition' src/ui/features/pretty-view/PrettyView.tsx` returns >=1.
    - Grep assertion (D-16 correlation partner exists): `git grep -c '\[pane-state-emitter\]' src/backend/` returns >=1 (verifies plan 31-08 shipped the emitter side; if 0, block until 31-08 ships).
    - Grep assertion: `git grep -c 'setPaneState' src/ui/features/pretty-view/PrettyView.tsx` returns the SAME count as before this task (behavior unchanged — no new state writes introduced by the log).
    - Behavior assertion: `npx tsc --noEmit` exits 0.
    - Behavior assertion: `npx vitest run src/ui/features/pretty-view/PrettyView` exits 0 (all Phase 29/30 PrettyView tests, virtualization tests, aside tests still green).
  </acceptance_criteria>
  <done>
    PrettyView.tsx `case "pane_state":` handler emits a receive-side `[pane-state] received phase=` line per frame, and a `[pane-state] state-transition from=X to=Y trigger=pane-state-frame` line whenever the paneState value actually changes. D-16 cross-side correlation with plan 31-08's `[pane-state-emitter]` unlocked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pane lifecycle → log line | paneId + paneType + hostId + sessionKey; no user content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-15 | Denial of Service | mount/unmount log volume | accept | Pane mount/unmount is inherently low-frequency (max ~1-10 per session). No dedup needed. |
| T-31-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed. package.json unchanged. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run src/ui/features/pretty-view/PrettyView` exits 0.
- All `[DIAG-REPORT]` occurrences eliminated from src/ (verified in plan 31-09 final grep pass).
- `git grep -c '\[pane-state\] received phase=' src/ui/features/pretty-view/PrettyView.tsx` returns >=1 (D-16 correlation partner for plan 31-08 backend `[pane-state-emitter]`).
- `git grep -c '\[pane-state\] state-transition' src/ui/features/pretty-view/PrettyView.tsx` returns >=1.
</verification>

<success_criteria>
- Pane lifecycle has edge-triggered logs (mount/unmount) complementing the periodic DIAG-REPORT snapshot.
- Frontend `[pane-state] received` and `[pane-state] state-transition` lines emit at the PrettyView.tsx `case "pane_state":` handler, providing the D-16 cross-side correlation partner for plan 31-08's backend `[pane-state-emitter]` — for any observed post-Phase-31 pane-state bug I can grep both sides and match on `sessionId=`.
- D-18 per-render discipline honored: transitions log ONLY on value change (guard: `paneState !== parsed.state`); the resolver hook itself never logs.
- No behavior change; existing tests green.
</success_criteria>

<output>
Create `.planning/phases/31-whole-app-structured-logging-backfill/31-06-SUMMARY.md` when done. Include: files touched line-count deltas; confirmation that Phase 27/28 virtualizer internals were NOT edited.
</output>
