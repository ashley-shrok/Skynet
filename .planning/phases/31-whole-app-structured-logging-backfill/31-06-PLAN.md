---
phase: 31-whole-app-structured-logging-backfill
plan: 06
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - src/ui/lib/diag-registry.ts
  - src/ui/features/pretty-view/PrettyView.tsx
autonomous: true
requirements:
  - D-01
  - D-02
  - D-11
  - D-13
  - D-14
  - D-17
must_haves:
  truths:
    - "DIAG-REPORT tick already routes through the [render] prefix (renamed in plan 31-01 § diag-emitter.ts)"
    - "Per-pane mount/unmount lifecycle events emit [render] lines complementing the periodic DIAG-REPORT snapshot"
    - "Virtualization events (if diagnostic) emit [render] lines only at meaningful transitions (not per-frame)"
  artifacts:
    - path: "src/ui/lib/diag-registry.ts"
      provides: "instrumented DIAG-REPORT registry with lifecycle event lines"
      contains: "[render]"
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "instrumented pane mount/unmount lifecycle"
      contains: "[render]"
  key_links:
    - from: "PrettyView.tsx mount effect"
      to: "[render] pane-mount line"
      via: "structured pane-id + host-id extraction"
      pattern: "\\[render\\] pane-mount"
    - from: "PrettyView.tsx unmount effect"
      to: "[render] pane-unmount line"
      via: "structured pane-id + reason attribution"
      pattern: "\\[render\\] pane-unmount"
---

<objective>
Complete the `[render]` subsystem coverage per D-02:
- Plan 31-01 already remapped `[DIAG-REPORT]` → `[render]` in `diag-emitter.ts` (the sole callsite).
- This plan adds EVENT-BASED lifecycle lines that complement the periodic tick-based DIAG-REPORT snapshot: per-pane mount/unmount in PrettyView (and in Terminal.tsx if the mount effect is instrumentable without conflicting with plan 31-02's edits).

Purpose: even when the DIAG-REPORT snapshot period doesn't line up with a bug window, mount/unmount events give edge-triggered visibility. Bias overdo per D-01.

Output: 2 files instrumented.

Note: this is the SMALLEST of the wave-2 plans and has the LOWEST risk. It ships alongside 31-02/31-03/31-04/31-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-whole-app-structured-logging-backfill/31-CONTEXT.md
@.planning/phases/31-whole-app-structured-logging-backfill/31-PATTERNS.md
@.planning/phases/31-whole-app-structured-logging-backfill/31-01-SUMMARY.md
@src/ui/features/pretty-view/useVoiceRecording.ts
@src/ui/lib/diag-emitter.ts
@src/ui/lib/diag-registry.ts
@src/ui/features/pretty-view/PrettyView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instrument diag-registry.ts + PrettyView.tsx per-pane mount/unmount events with [render] prefix</name>
  <files>src/ui/lib/diag-registry.ts, src/ui/features/pretty-view/PrettyView.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/useVoiceRecording.ts (golden-copy shape)
    - src/ui/lib/diag-emitter.ts (post-plan-31-01 — verifies [render] tick line was remapped)
    - src/ui/lib/diag-registry.ts (existing pane registry pattern — grep for `register`, `unregister`, `subscribe` methods)
    - src/ui/features/pretty-view/PrettyView.tsx (top of file — imports + mount effect; look for existing `useEffect(() => { ...; return () => { ...; };}, [...])` where the mount body registers a pane and cleanup unregisters)
    - .planning/phases/31-whole-app-structured-logging-backfill/31-CONTEXT.md § D-02 (pretty-view render lifecycle — extend existing DIAG-REPORT), D-18 (no per-render logs by default; move to lifecycle-only mount/unmount/meaningful-prop-change)
    - .planning/phases/31-whole-app-structured-logging-backfill/31-PATTERNS.md § "src/ui/lib/diag-emitter.ts" (canonical envelope shape)
  </read_first>
  <action>
    A) **diag-registry.ts** — add lifecycle event emissions on register/unregister:
    - In the `register(paneId, snapshotFn)` method (or equivalent — read the file to find the exact name), add BEFORE storing the pane in the registry map:
      * `console.info(\`[render] pane-register paneId=${paneId}\`)`
    - In the `unregister(paneId)` method, add BEFORE removing from the map:
      * `console.info(\`[render] pane-unregister paneId=${paneId}\`)`
    - If the file has a `getAll()` / snapshot-composition method that's called on every tick, do NOT log per-call (D-18 avoids per-render noise). The periodic DIAG-REPORT tick line from diag-emitter.ts already covers the snapshot cadence.

    B) **PrettyView.tsx mount/unmount effect** — instrument the top-level pane mount:
    - Find the useEffect at the top of PrettyView that runs on mount (grep `useEffect.*\\[\\]` for empty-deps effects, or the effect where a pane is registered with diag-registry). Add as the FIRST line inside the effect body:
      * `console.info(\`[render] pane-mount paneId=${paneId} paneType=pretty-view hostId=${hostId ?? 'null'} sessionKey=${sessionKey ?? 'null'}\`)`
    - Add as the FIRST line inside the cleanup return function:
      * `console.info(\`[render] pane-unmount paneId=${paneId} paneType=pretty-view hostId=${hostId ?? 'null'} sessionKey=${sessionKey ?? 'null'}\`)`
    - The paneId variable naming follows the file's existing convention (may be `paneKey`, `tabId`, etc. — use what the file uses).

    C) **Virtualization events (opt-in only if genuinely load-bearing)** — Phase 27 landed virtualization on PrettyView; Phase 28 fixed correctness issues. If the virtualizer's `scrollElement` re-bind path (H4 fix from Phase 28) currently has debug prints or comments about diagnostic hooks, ADD a structured line at the re-bind boundary:
    - `console.info(\`[render] virtualizer-rebind paneId=${paneId} scrollElementPresent=${scrollElement != null}\`)`

    If no such hook exists cleanly, do NOT add it — Phase 27/28 code is stable and this plan should not touch it just to add logs. Prioritize mount/unmount which is universally valuable.

    D) **Do NOT** touch:
    - The DIAG-REPORT tick emission in diag-emitter.ts (already handled by plan 31-01).
    - Any per-frame render effect (D-18 forbids per-render logging).
    - The virtualizer internals (Phase 27/28 already-stable code — logging touches inside there is high risk-reward-ratio).

    No fenced code blocks in this action.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/ui/features/pretty-view/PrettyView</automated>
  </verify>
  <acceptance_criteria>
    - Grep assertion: `git grep -c '\[render\] pane-register' src/ui/lib/diag-registry.ts` returns >=1.
    - Grep assertion: `git grep -c '\[render\] pane-unregister' src/ui/lib/diag-registry.ts` returns >=1.
    - Grep assertion: `git grep -c '\[render\] pane-mount' src/ui/features/pretty-view/PrettyView.tsx` returns >=1.
    - Grep assertion: `git grep -c '\[render\] pane-unmount' src/ui/features/pretty-view/PrettyView.tsx` returns >=1.
    - Grep assertion: `git grep -v '^#' src/ui/lib/diag-registry.ts | grep -c '\[DIAG-REPORT\]'` returns 0 (verify no lingering old prefix).
    - Grep assertion: `git grep -v '^#' src/ui/features/pretty-view/PrettyView.tsx | grep -c '\[DIAG-REPORT\]'` returns 0.
    - Behavior assertion: `npx tsc --noEmit` exits 0.
    - Behavior assertion: `npx vitest run src/ui/features/pretty-view/PrettyView` exits 0 (all PrettyView tests including virtualization + aside + phase29/phase30 tests still green).
  </acceptance_criteria>
  <done>
    diag-registry pane register/unregister emissions added; PrettyView mount/unmount lifecycle emissions added. All complementary to the existing periodic DIAG-REPORT tick from diag-emitter.ts (renamed in plan 31-01).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pane lifecycle → log line | paneId + paneType + hostId + sessionKey; no user content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-15 | Denial of Service | mount/unmount log volume | accept | Pane mount/unmount is inherently low-frequency (max ~1-10 per session). No dedup needed. |
| T-31-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed. package.json unchanged. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run src/ui/features/pretty-view/PrettyView` exits 0.
- All `[DIAG-REPORT]` occurrences eliminated from src/ (verified in plan 31-09 final grep pass).
</verification>

<success_criteria>
- Pane lifecycle has edge-triggered logs (mount/unmount) complementing the periodic DIAG-REPORT snapshot.
- No behavior change; existing tests green.
</success_criteria>

<output>
Create `.planning/phases/31-whole-app-structured-logging-backfill/31-06-SUMMARY.md` when done. Include: files touched line-count deltas; confirmation that Phase 27/28 virtualizer internals were NOT edited.
</output>
