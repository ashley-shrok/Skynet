---
phase: 31-whole-app-structured-logging-backfill
plan: 04
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - src/ui/features/pretty-view/useVoiceRecording.ts
autonomous: true
requirements:
  - D-01
  - D-02
  - D-05
  - D-07
  - D-11
  - D-12
  - D-13
  - D-14
must_haves:
  truths:
    - "The `[voice-diag]` prefix is remapped to `[voice]` throughout useVoiceRecording.ts"
    - "Log levels are normalized per D-14: info for expected transitions, warn for unexpected, error for real failures"
    - "MediaRecorder events (start, stop, error, dataavailable) all emit structured lines with explicit .error.name / .error.message extraction"
    - "The patch #382 feedback-playback ordering fix has explicit boundary logs so an iOS AudioSession race re-appearance is diagnosable"
    - "The hook accepts an optional logContext prop for hostId/sessionId threading per D-12"
  artifacts:
    - path: "src/ui/features/pretty-view/useVoiceRecording.ts"
      provides: "instrumented voice recording pipeline with canonical [voice] prefix"
      contains: "[voice] "
  key_links:
    - from: "useVoiceRecording.ts MediaRecorder error handler"
      to: "[voice] recorder-error line"
      via: "explicit .error.name + .error.message extraction (D-05)"
      pattern: "\\[voice\\] recorder-error.*errName="
    - from: "useVoiceRecording.ts patch #382 feedback-playback ordering site"
      to: "[voice] feedback-playback-order line with sequence attribution"
      via: "before/after teardown boundary logs"
      pattern: "\\[voice\\] feedback-playback-order"
---

<objective>
Complete the golden-copy retrofit for `src/ui/features/pretty-view/useVoiceRecording.ts`: rename `[voice-diag]` → `[voice]` per D-13, normalize log levels per D-14 (currently everything is `console.warn`), extract explicit .error.name/.error.message on MediaRecorder events per D-05, and add boundary logs around the patch #382 feedback-playback ordering fix so if the iOS Safari AudioSession race re-surfaces we see it in logs (D-07 style — the going-forward rule applied to a known gotcha).

This file is already the golden-copy for shape (per PATTERNS.md § "Model / Golden-Copy File"); the retrofit is prefix rename + level normalization + explicit error-field extraction + D-07 boundary logs. NOT a from-scratch instrumentation.

Purpose: bring the golden-copy fully into the D-11/D-13/D-14 canonical form so it can be pointed at as the authoritative example in AGENTS.md / role-file directives for future patches (D-04 going-forward rule).

Output: instrumented useVoiceRecording.ts. No new test file — the file's existing test coverage (if any — verify with `ls src/ui/features/pretty-view/useVoiceRecording*.test.*`) covers behavior; instrumentation is shape-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-whole-app-structured-logging-backfill/31-CONTEXT.md
@.planning/phases/31-whole-app-structured-logging-backfill/31-PATTERNS.md
@.planning/phases/31-whole-app-structured-logging-backfill/31-01-SUMMARY.md
@src/ui/features/pretty-view/useVoiceRecording.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename [voice-diag] → [voice], normalize log levels, add explicit MediaRecorder error extraction + patch #382 boundary logs</name>
  <files>src/ui/features/pretty-view/useVoiceRecording.ts</files>
  <read_first>
    - src/ui/features/pretty-view/useVoiceRecording.ts (full 395 lines — this IS the golden-copy)
    - .planning/phases/31-whole-app-structured-logging-backfill/31-CONTEXT.md § D-05, D-07, D-11, D-13, D-14
    - .planning/phases/31-whole-app-structured-logging-backfill/31-PATTERNS.md § "src/ui/features/pretty-view/useVoiceRecording.ts (hook, streaming)" — enumerates the 3 changes: prefix rename, level normalization, hostId/sessionId threading
    - Recent commit `3d30878 fix(voice-recording): play stop/cancel feedback AFTER recorder teardown to stop iOS Safari AudioSession switch dropping MediaRecorder onstop` (git log — this is patch #382, the AudioSession ordering fix whose boundary this plan instruments per D-07)
  </read_first>
  <action>
    Perform all edits in `src/ui/features/pretty-view/useVoiceRecording.ts`.

    A) **Prefix rename** — every `[voice-diag]` occurrence in this file:
    - Use a whole-file substitution: `[voice-diag] ` → `[voice] ` (with trailing space).
    - Verify count: `grep -c '\[voice-diag\]' src/ui/features/pretty-view/useVoiceRecording.ts` should return 0 after edit; the equivalent `[voice]` count should match the pre-edit `[voice-diag]` count (approximately 27 lines per PATTERNS.md metadata).

    B) **Log level normalization per D-14** — the file currently uses `console.warn` for EVERYTHING (per PATTERNS.md's "single level throughout for grepability of a single subsystem" observation). Reclassify:
    - **info** (expected transitions): recording start success, recording stop success (blob produced), transcribe POST fired, transcribe response ok=true, feedback playback started, feedback playback ended, cancel entry+exit success.
      * Example: line 152 `[voice] stopRecording: onstop fired, blob size=... type=...` → `console.info(...)`
      * Example: line 196 `[voice] transcribeBlob: POST ...` → `console.info(...)`
      * Example: line 210 `[voice] transcribeBlob: fetch resolved status=... ok=true` → `console.info(...)`
    - **warn** (unexpected but not fatal): recorder null early-returns, recorder state != recording rejections, WATCHDOG onstop-never-fired timeout, transcribe response ok=false, dedup fallbacks.
      * Example: line 124 `[voice] stopRecording: recorderRef null, resolving null` → stays `console.warn`
      * Example: line 136 `[voice] stopRecording: recorder.state=... (not recording), resolving null` → stays `console.warn`
      * Example: line 165 `[voice] stopRecording: WATCHDOG onstop never fired after 8s — forcing cleanup` → stays `console.warn`
    - **error** (real failures): mic permission denied (line ~284 setErrorMessage without any current console log — ADD one), MediaRecorder onerror event, fetch threw (transcribe network error), transcribe response status >= 500.
      * ADD at the mic denied path (~line 284 per PATTERNS.md): `console.error(\`[voice] mic-denied errName="${err instanceof Error ? err.name : 'unknown'}" errMessage="${err instanceof Error ? err.message : String(err)}"\`)`

    C) **MediaRecorder event explicit field extraction per D-05** — the MediaRecorder API emits `onerror` events with an `error` property that is a DOMException-shaped object. Any existing `onerror` handler or ondataavailable/onstart/onstop that logs the event needs explicit extraction:
    - `recorder.onerror = (event) => { const err = event.error; console.error(\`[voice] recorder-error errName="${err?.name ?? 'unknown'}" errMessage="${err?.message ?? 'no message'}" recorderState=${recorder.state}\`); ... existing behavior preserved ...}`
    - If `recorder.onstart` currently exists without a log, add: `console.info(\`[voice] recorder-start mimeType=${recorder.mimeType} state=${recorder.state}\`)`.
    - If `recorder.onstop` is where the blob-producing branch lives, ensure the existing blob-size log stays but MOVE it inside the onstop handler explicitly (do not depend on subsequent stopRecording resolution alone for observability of the raw event).
    - If `recorder.ondataavailable` is present and currently logs, restructure: `console.info(\`[voice] recorder-data-available size=${event.data.size} type=${event.data.type}\`)`.
    - If any handler is currently missing (grep the file for `recorder.on*` bindings), ADD it with a structured info line for start / warn line for stop-without-blob / error line for onerror.

    D) **Patch #382 feedback-playback ordering boundary logs (D-07)** — the recent commit fixed the ordering: stop/cancel feedback plays AFTER recorder teardown. The gotcha per PATTERNS.md is iOS Safari's AudioSession shared between MediaRecorder and Audio drops the MediaRecorder onstop if audio starts BEFORE teardown. Add BOUNDARY logs so a regression is diagnosable:
    - Immediately BEFORE the recorder teardown call in the stop/cancel path: `console.info(\`[voice] feedback-playback-order phase=before-teardown\`)`
    - Immediately AFTER the recorder teardown resolves (right before the feedback audio.play call): `console.info(\`[voice] feedback-playback-order phase=after-teardown-before-feedback\`)`
    - Immediately AFTER the feedback audio.play() promise resolves: `console.info(\`[voice] feedback-playback-order phase=feedback-play-resolved\`)`
    - In the feedback audio.play() catch: `console.warn(\`[voice] feedback-playback-order phase=feedback-play-rejected errName="${err instanceof Error ? err.name : 'unknown'}" errMessage="${err instanceof Error ? err.message : String(err)}"\`)`

    The point: if the AudioSession bug reappears (onstop stops firing after audio.play), the phase progression in the logs will show whether feedback fired BEFORE or AFTER teardown, immediately narrowing the diagnosis.

    E) **Optional logContext for hostId/sessionId threading (D-12)** — the hook currently has no access to hostId/sessionId. Extend the hook's argument signature (or create a new one) to accept an optional `logContext?: { hostId?: number; sessionId?: string }` parameter. In every emission, append ` hostId=${logContext?.hostId ?? 'n/a'} sessionId=${logContext?.sessionId ?? 'n/a'}` to the msg tail.

    Update the ONE known caller (grep `useVoiceRecording(` in `src/` — likely ComposeBox.tsx per the voice-input attach in patch #382 vicinity) to pass the context if it has hostId/sessionId in scope. If the caller doesn't have that context handy, pass `{}` — do NOT wire hostId/sessionId synthetically. Threading is best-effort; missing fields fall back to 'n/a'.

    F) **File header comment** — since this file is being pointed at as the CANONICAL golden-copy for the D-11 shape, prepend/update the module JSDoc block with a one-paragraph note:
    - "Canonical golden-copy for the [subsystem] event key=value log-line shape used across the app (D-11). All structured logs in this hook use the `[voice]` prefix (per D-13 taxonomy), template-literal-composed `msg` strings, explicit field extraction from DOM events (D-05), and level semantics per D-14."

    No fenced code blocks in this action; every snippet is a shape reference following useVoiceRecording's own existing pattern (this file's the model — the retrofit stays in-shape).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/ui/features/pretty-view/</automated>
  </verify>
  <acceptance_criteria>
    - Smoke assertion (checker-added, per revision blocker #4 warning): `git grep -c '\[voice-diag\]' src/` returns 0 (src-wide sweep; not just the golden-copy file — the rename must land globally, no stragglers).
    - Smoke assertion (floor): `git grep -c '\[voice\]' src/ui/features/pretty-view/useVoiceRecording.ts` returns >=10 (PATTERNS.md documents 27 pre-existing [voice-diag] lines in this file; >=10 [voice] lines post-rename is the floor — if the count drops below 10, the rename either failed or scope shrank materially).
    - Grep assertion: `git grep -v '^#' src/ui/features/pretty-view/useVoiceRecording.ts | grep -c '\[voice-diag\]'` returns 0.
    - Grep assertion: `git grep -c '\[voice\] ' src/ui/features/pretty-view/useVoiceRecording.ts` returns >=20 (majority of previous 27 [voice-diag] lines remapped; some may be combined).
    - Grep assertion: `git grep -c 'feedback-playback-order' src/ui/features/pretty-view/useVoiceRecording.ts` returns >=3 (before-teardown, after-teardown-before-feedback, feedback-play-resolved OR rejected).
    - Grep assertion: `git grep -c 'recorder-error' src/ui/features/pretty-view/useVoiceRecording.ts` returns >=1.
    - Grep assertion: `git grep -c 'mic-denied' src/ui/features/pretty-view/useVoiceRecording.ts` returns >=1.
    - Grep assertion: `git grep -c 'console.info' src/ui/features/pretty-view/useVoiceRecording.ts` returns >=5 (level normalization introduced info lines for expected transitions).
    - Grep assertion: `git grep -c 'console.error' src/ui/features/pretty-view/useVoiceRecording.ts` returns >=2 (real-failure paths promoted).
    - Grep assertion: `git grep -c 'logContext' src/ui/features/pretty-view/useVoiceRecording.ts` returns >=1 (optional context param wired).
    - Behavior assertion: `npx tsc --noEmit` exits 0.
    - Behavior assertion: `npx vitest run src/ui/features/pretty-view/` exits 0 (existing voice recording tests, ChatMessage tests, and other pretty-view tests still pass — instrumentation is shape-only, no behavior change).
    - Source assertion: the file's module JSDoc header names it as "Canonical golden-copy" for the D-11 shape.
  </acceptance_criteria>
  <done>
    useVoiceRecording.ts fully retrofit to canonical D-11/D-13/D-14 shape: prefix remapped, levels normalized, MediaRecorder errors explicit, patch #382 boundary logs added per D-07. Optional logContext threaded. Module JSDoc marks the file as the golden-copy.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MediaRecorder events → log lines | Recorder error events extracted explicitly (D-05 — no JSON.stringify on DOMException-shaped .error). |
| Recorded audio blob → log line | Only blob.size (numeric) and blob.type (mimetype string) logged; audio bytes NEVER logged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-10 | Information Disclosure | voice recording log lines | mitigate | Log lines carry: blob.size (numeric), blob.type (mimetype), MediaError name/message (browser-provided), fetch status/ok, optional hostId/sessionId. Explicitly NOT logged: recorded audio bytes, transcript text, user's speech content. |
| T-31-11 | Denial of Service | log volume from recorder events | accept | MediaRecorder events fire once per state change (per recording session, ~1-10 events). No dedup needed per D-17 opt-out. |
| T-31-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed. package.json unchanged. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run src/ui/features/pretty-view/` exits 0 (all pretty-view tests including any voice recording tests still green).
- `git grep -c '\[voice-diag\]' src/` returns 0 (only occurrences allowed are in docs/comments, not code).
- Level distribution across the file: `git grep -c 'console.info' src/ui/features/pretty-view/useVoiceRecording.ts` >=5, `git grep -c 'console.warn' src/ui/features/pretty-view/useVoiceRecording.ts` >=3, `git grep -c 'console.error' src/ui/features/pretty-view/useVoiceRecording.ts` >=2.
</verification>

<success_criteria>
- The golden-copy is officially at D-11 shape, with the module header naming it as the reference. Future patches per D-04 can cite this file as "follow useVoiceRecording.ts shape."
- Patch #382 iOS AudioSession gotcha has diagnostic boundary logs so a regression is visible in the log trail without code archaeology.
- No behavior change; existing tests green.
</success_criteria>

<output>
Create `.planning/phases/31-whole-app-structured-logging-backfill/31-04-SUMMARY.md` when done. Include:
- Exact old→new prefix count (should be identical if nothing was reshaped).
- Final level distribution (info/warn/error counts).
- The caller sites updated to pass logContext (or note "no callers wired — context unavailable in current call sites").
</output>
