---
phase: 31-whole-app-structured-logging-backfill
plan: 05
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - src/ui/features/pretty-view/ComposeBox.tsx
  - src/main.tsx
autonomous: true
requirements:
  - D-01
  - D-02
  - D-11
  - D-12
  - D-13
  - D-14
  - D-20
must_haves:
  truths:
    - "Compose/draft save/load prefix `[compose-draft]` is remapped to `[compose]` and normalized from printf-placeholder to template-literal shape"
    - "Tap-diag prefix `[tap-diag]` is remapped to `[tap]` and normalized from object-literal to key=value shape"
    - "Compose submission (send handler entry + result) emits structured [compose] lines currently absent"
    - "Main.tsx PWA lifecycle handlers (visibilitychange, pagehide, pageshow) emit [pwa] lines currently absent"
    - "Session-open flow surfaces (new session dialog + first WS connect trigger) emit [session] lines"
  artifacts:
    - path: "src/ui/features/pretty-view/ComposeBox.tsx"
      provides: "instrumented compose/draft/tap with canonical prefixes"
      contains: "[compose]"
    - path: "src/main.tsx"
      provides: "instrumented PWA lifecycle at page-level"
      contains: "[pwa]"
  key_links:
    - from: "main.tsx visibilitychange handler"
      to: "[pwa] visibility-change line"
      via: "explicit document.visibilityState + document.hidden extraction"
      pattern: "\\[pwa\\] visibility-change"
    - from: "ComposeBox.tsx submit handler"
      to: "[compose] submit line"
      via: "explicit hostId + tmuxSession + bodyLen + result= attribution"
      pattern: "\\[compose\\] submit"
---

<objective>
Cover the "compose/draft + PWA visibility + tap" cluster per D-02:

1. **ComposeBox.tsx** — rename existing `[compose-draft]` → `[compose]` and `[tap-diag]` → `[tap]` per D-13; normalize the two existing partial shapes to canonical D-11 template-literal form; add currently-silent instrumentation on the compose SUBMIT path (send handler entry, submission result, aside-morph transitions per patch #382 Aside surrounding).
2. **main.tsx** — add PWA lifecycle instrumentation (visibilitychange, pagehide, pageshow, PWA install prompt if present) — currently emits NOTHING at page-level lifecycle per PATTERNS.md § main.tsx.
3. **Session-open flow** — thin instrumentation on the ONE session-open trigger site in main.tsx / snapshotPendingTab boot path so the "did the user's fresh-open action get processed" question has a log answer.

Purpose: cover the "everywhere a bug could conceivably need diagnosis" surfaces per D-01 that are not otherwise handled by 31-02/31-03/31-04. Bias overdo per D-01.

Output: 2 files instrumented; no new test file (D-20 light testing — smoke tests already cover the highest-value critical lines in plans 31-02/31-03; compose/tap/pwa lines are dedup-friendly volume additions rather than singular-critical-line-of-record).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-whole-app-structured-logging-backfill/31-CONTEXT.md
@.planning/phases/31-whole-app-structured-logging-backfill/31-PATTERNS.md
@.planning/phases/31-whole-app-structured-logging-backfill/31-01-SUMMARY.md
@src/ui/features/pretty-view/useVoiceRecording.ts
@src/ui/features/pretty-view/ComposeBox.tsx
@src/main.tsx
@src/ui/lib/console-forwarder.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instrument ComposeBox.tsx — rename [compose-draft]→[compose] + [tap-diag]→[tap], normalize shapes, add compose submit + aside-morph logs</name>
  <files>src/ui/features/pretty-view/ComposeBox.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/useVoiceRecording.ts (golden-copy shape — template-literal + key=value)
    - src/ui/features/pretty-view/ComposeBox.tsx (2878 lines — existing [compose-draft] sites near lines 626 and 765; existing [tap-diag] sites at 897/906/910/915/919/924 per PATTERNS.md; the send/submit handler location varies — grep `onSend\\|handleSubmit\\|handleSend` to locate)
    - .planning/phases/31-whole-app-structured-logging-backfill/31-CONTEXT.md § D-11, D-13
    - .planning/phases/31-whole-app-structured-logging-backfill/31-PATTERNS.md § "src/ui/features/pretty-view/ComposeBox.tsx (component, CRUD + event-driven)" — exact old→new shape mappings
  </read_first>
  <action>
    Perform all edits in `src/ui/features/pretty-view/ComposeBox.tsx`.

    A) **[compose-draft] → [compose] prefix rename + shape normalization** (~lines 626, 765 per PATTERNS.md):
    - Existing pattern (printf-placeholder):
      `console.warn("[compose-draft] save hostId=%s tmuxSession=%s bodyLen=%d slotsLen=%d", hostId, tmuxSession, body.length, slots.length)`
    - New shape (template-literal):
      `console.warn(\`[compose] draft-save hostId=${hostId} tmuxSession=${tmuxSession} bodyLen=${body.length} slotsLen=${slots.length}\`)`
    - Do the analogous rename+reshape at line 765 (the draft-load site). Grep `\[compose-draft\]` for the exact count and cover all sites.

    B) **[tap-diag] → [tap] prefix rename + shape normalization** (~lines 897, 906, 910, 915, 919, 924 per PATTERNS.md):
    - Existing pattern (object-literal):
      `console.log("[tap-diag] pointerdown", { client: { x: ..., y: ... }, pointerType: ..., ... })`
    - New shape (flattened key=value template-literal):
      `console.log(\`[tap] pointerdown x=${Math.round(e.clientX)} y=${Math.round(e.clientY)} pointerType=${e.pointerType} targetTag=${targetTag} selfTarget=${e.target === el}\`)`
    - Do the analogous rename+reshape for each of the 6+ tap-diag sites. Preserve every field the object-literal was carrying; add hostId if available in scope.
    - Log level per D-14: pointerdown/pointerup/pointermove tap-diag lines stay at `console.log` (they're diagnostic-only, dev-tier). If any is currently `console.warn`, keep the level unchanged (rename is prefix + shape, not level reshuffle).

    C) **Compose SUBMIT instrumentation** (currently silent per PATTERNS.md):
    - Locate the send/submit handler (grep `onSend`, `handleSubmit`, `handleSend`, `sendMessage` — pick the actual send trigger). At its ENTRY:
      * `console.info(\`[compose] submit-entry hostId=${hostId} tmuxSession=${tmuxSession ?? 'null'} bodyLen=${body.length} attachmentCount=${attachments?.length ?? 0} trigger=${trigger ?? 'unknown'}\`)`
      * Where `trigger` is `"enter-key"` or `"send-button"` or `"queue-item"` depending on the invocation site.
    - At the send SUCCESS boundary (either after the WS send returns success OR after the split-send Enter delay has fired both events):
      * `console.info(\`[compose] submit-success hostId=${hostId} tmuxSession=${tmuxSession ?? 'null'} bodyLen=${body.length}\`)`
    - At the send FAILURE boundary (if the WS send throws or the split-send timeout hits):
      * `console.error(\`[compose] submit-failed hostId=${hostId} tmuxSession=${tmuxSession ?? 'null'} err="${err instanceof Error ? err.message : String(err)}"\`)`

    D) **Aside-morph transitions** (patch #382 Aside surrounding — if the file has aside-active state, log its transitions):
    - Locate the aside-active state (grep `asideActive`, `isAside`, or the ComposeBox aside-morph props). At every transition (false→true and true→false):
      * `console.info(\`[compose] aside-morph edge=${prev}→${next} hostId=${hostId} tmuxSession=${tmuxSession ?? 'null'}\`)`
    - If the state lives in a parent component and ComposeBox just consumes it as prop, use the useEffect-with-prop-diff pattern to log transitions on prop change (standard React idiom).

    E) **Existing console.warn/error sites in this file** — grep `console\\.` in ComposeBox.tsx for any bare/unstructured lines and reshape to `[compose] event ...` per D-11. Do NOT preserve unstructured lines; every remaining one gets a `[compose]` prefix.

    F) **Dedup opt-in for tap-diag** (D-17 hot path — tap events fire frequently on scroll/pointer flicker):
    - Import `createLogDedup` from `src/ui/lib/log-dedup` at the file top.
    - Create a module-scoped or component-scoped `tapDedup = createLogDedup({N: 5, W: 3000})` (slightly higher N than default because tap volume is chatter, and shorter W because taps cluster in bursts).
    - Wrap the 6 `[tap]` emissions in `if (tapDedup.shouldEmit(...).emit) console.log(...)`. Use the event name (e.g. `"tap-pointerdown"`) as the key.

    Do NOT dedup `[compose] draft-save`, `[compose] submit-*`, or `[compose] aside-morph` — those are per-user-action events, not hot loops (opt-out per D-17).

    No fenced code blocks in this action; template-literal shapes are shape references, not code to paste verbatim.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/ui/features/pretty-view/ComposeBox</automated>
  </verify>
  <acceptance_criteria>
    - Grep assertion: `git grep -v '^#' src/ui/features/pretty-view/ComposeBox.tsx | grep -c '\[compose-draft\]'` returns 0.
    - Grep assertion: `git grep -v '^#' src/ui/features/pretty-view/ComposeBox.tsx | grep -c '\[tap-diag\]'` returns 0.
    - Grep assertion: `git grep -c '\[compose\] draft-save' src/ui/features/pretty-view/ComposeBox.tsx` returns >=1.
    - Grep assertion: `git grep -c '\[compose\] draft-load' src/ui/features/pretty-view/ComposeBox.tsx` returns >=1 (renamed from the second [compose-draft] site).
    - Grep assertion: `git grep -c '\[compose\] submit-entry' src/ui/features/pretty-view/ComposeBox.tsx` returns >=1.
    - Grep assertion: `git grep -c '\[compose\] submit-success' src/ui/features/pretty-view/ComposeBox.tsx` returns >=1.
    - Grep assertion: `git grep -c '\[compose\] submit-failed' src/ui/features/pretty-view/ComposeBox.tsx` returns >=1.
    - Grep assertion: `git grep -c '\[tap\] pointerdown' src/ui/features/pretty-view/ComposeBox.tsx` returns >=1.
    - Grep assertion: `git grep -c 'createLogDedup' src/ui/features/pretty-view/ComposeBox.tsx` returns >=1 (dedup imported for [tap]).
    - Grep assertion: `git grep -c '%s\\|%d' src/ui/features/pretty-view/ComposeBox.tsx | head -5` — the 2 lines that formerly used printf-placeholders in [compose-draft] should be gone from that context (may still have other %s uses in unrelated code — grep-verify no printf-placeholders remain on `[compose]` or `[tap]` prefix lines specifically).
    - Behavior assertion: `npx tsc --noEmit` exits 0.
    - Behavior assertion: `npx vitest run src/ui/features/pretty-view/ComposeBox` exits 0 (10+ existing ComposeBox tests still pass).
  </acceptance_criteria>
  <done>
    ComposeBox.tsx: both existing prefixes remapped + shape-normalized; compose submit (entry + result branches) and aside-morph transitions instrumented; tap-diag dedup opt-in wired. No behavior change; existing tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Instrument main.tsx PWA lifecycle handlers + session-open trigger boundary</name>
  <files>src/main.tsx</files>
  <read_first>
    - src/main.tsx (246 lines — the app bootstrap; find snapshotPendingTab call from console-forwarder init at top and any visibilitychange/pagehide/pageshow existing bindings)
    - src/ui/lib/console-forwarder.ts (existing visibilitychange handler pattern at lines 168-173 — model the main.tsx additions on the same shape but with structured msg output rather than just a flush trigger)
    - .planning/phases/31-whole-app-structured-logging-backfill/31-PATTERNS.md § "src/main.tsx (bootstrap, event-driven — PWA visibility)" — exact new-line shapes
    - .planning/phases/31-whole-app-structured-logging-backfill/31-CONTEXT.md § D-02 (PWA visibility handler + service worker events)
  </read_first>
  <behavior>
    - Test 1: Firing `document.dispatchEvent(new Event("visibilitychange"))` with `document.visibilityState="hidden"` produces `console.info` matching `/^\[pwa\] visibility-change state=hidden hidden=true/`.
    - Test 2: Firing pageshow with `event.persisted=true` produces `console.info` matching `/^\[pwa\] pageshow persisted=true/`.
    - Test 3: Firing pagehide with `event.persisted=false` produces `console.info` matching `/^\[pwa\] pagehide persisted=false/`.
  </behavior>
  <action>
    Perform all edits in `src/main.tsx`.

    A) **PWA visibility handler** — currently console-forwarder.ts has a visibilitychange handler that only calls `flushBeacon()` on hidden. This plan ADDS a page-level structured log at main.tsx level (distinct from console-forwarder's transport-only handler — different concerns):
    - Add near the top of main.tsx (after console-forwarder init, before app render):
      * `window.addEventListener("visibilitychange", () => { console.info(\`[pwa] visibility-change state=${document.visibilityState} hidden=${document.hidden}\`); });`
    - Add pagehide + pageshow bindings:
      * `window.addEventListener("pagehide", (e: PageTransitionEvent) => { console.info(\`[pwa] pagehide persisted=${e.persisted}\`); });`
      * `window.addEventListener("pageshow", (e: PageTransitionEvent) => { console.info(\`[pwa] pageshow persisted=${e.persisted}\`); });`

    B) **PWA install / beforeinstallprompt event** — if the app currently registers a `beforeinstallprompt` listener (grep for it), instrument its fire and outcome:
    - `window.addEventListener("beforeinstallprompt", (e) => { console.info(\`[pwa] beforeinstallprompt-fired\`); ... existing behavior ...})`
    - If the user prompt is presented and outcome captured, log the outcome: `console.info(\`[pwa] install-prompt-outcome outcome=${outcome}\`)` (where outcome is `accepted`|`dismissed`).
    - If no `beforeinstallprompt` handler currently exists in main.tsx, do NOT invent one — that's out of scope for a logging-only phase.

    C) **Boot / snapshotPendingTab boundary** — main.tsx currently calls `initConsoleForwarder()` and `snapshotPendingTab()`. Instrument the app boot:
    - After `initConsoleForwarder()`, before `snapshotPendingTab()`:
      * `console.info(\`[pwa] boot ts=${Date.now()} ua="${navigator.userAgent.slice(0,80)}" pathname=${window.location.pathname}\`)`
    - After `snapshotPendingTab()` returns (if it returns a value indicating pending tab restore state):
      * `console.info(\`[pwa] snapshot-tab-restore result=${snapshotResult ?? 'no-pending'}\`)`

    D) **Session-open flow entry (D-02)** — if main.tsx has any code that triggers a fresh session open (e.g. URL-fragment routing, `#session=...` handling), instrument it at the entry:
    - `console.info(\`[session] frontend-open-trigger source=${source} sessionKey=${sessionKey ?? 'null'}\`)`
    - Source could be `"url-fragment"`, `"conversations-panel-click"`, etc. If the session-open flow lives elsewhere (e.g. AppShell), just cover the main.tsx entry point; the broader flow is enumerated in D-02 and gets instrumentation in whatever file owns the click handler (frontend session-open surfaces — plan-05 covers only the main.tsx entry point; anything deeper is out of scope of this plan and follows the D-01 "bias overdo" rule — the executor may extend to AppShell if the wiring is trivially reachable, but is NOT required to).

    E) **Optional test file** — a minimal main.tsx test may not exist because main.tsx is the app boot; if a test would require heavy JSDOM setup skip it. Instead the 3 tests in the <behavior> block above can live in a NEW file `src/main.instrumentation.test.tsx` using a simple vitest setup that imports main.tsx side effects (or extracts the handlers into a named export first). If extracting for testability requires refactoring main.tsx significantly, downgrade the test to a manual verification note in the plan SUMMARY and mark this task as no-test (D-20 tests are opt-in per subsystem; PWA lifecycle is not on the critical-line list).

    Preferred order: (1) try to extract the handler registrations into a named `registerPwaLifecycleLogs()` function in main.tsx; (2) unit-test that function; (3) if extraction is invasive, skip the test file and note in SUMMARY.

    No fenced code blocks in this action.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/main.instrumentation.test.tsx 2>&1 || echo "test file skipped per D-20 discretion"</automated>
  </verify>
  <acceptance_criteria>
    - Grep assertion: `git grep -c '\[pwa\] visibility-change' src/main.tsx` returns >=1.
    - Grep assertion: `git grep -c '\[pwa\] pagehide' src/main.tsx` returns >=1.
    - Grep assertion: `git grep -c '\[pwa\] pageshow' src/main.tsx` returns >=1.
    - Grep assertion: `git grep -c '\[pwa\] boot' src/main.tsx` returns >=1.
    - Behavior assertion: `npx tsc --noEmit` exits 0.
    - Behavior assertion (optional test): If test file was created, `npx vitest run src/main.instrumentation.test.tsx` exits 0. Otherwise SUMMARY notes the manual-verification-only path.
    - Behavior assertion: `npx vitest run` (full frontend suite) exits 0 — no regressions from main.tsx edits.
  </acceptance_criteria>
  <done>
    main.tsx PWA lifecycle handlers instrumented (visibilitychange, pagehide, pageshow, boot). Optional session-open-trigger boundary added if reachable in main.tsx. Test file created if extraction feasible, otherwise skipped per D-20.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User pointer/keyboard input → tap-diag logs | Coordinates + pointerType only; no text body, no clipboard content. |
| Compose text body → submit log | Only bodyLen (numeric) — NEVER text content itself. |
| navigator.userAgent → boot log | UA truncated to 80 chars (already public, sent on every HTTP request). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-12 | Information Disclosure | compose submit log | mitigate | submit-entry logs bodyLen (numeric character count), NEVER the compose body text. attachmentCount (numeric) only; no filenames or content. Confirmed by acceptance-criteria grep. |
| T-31-13 | Denial of Service | tap-diag volume | mitigate | Wrapped in `createLogDedup({N:5, W:3000})` per D-17. Bursty tap loops collapse to summary lines. |
| T-31-14 | Information Disclosure | UA in boot log | accept | UA is already publicly transmitted with every HTTP request; logging it locally adds no exposure. |
| T-31-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed. package.json unchanged. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run src/ui/features/pretty-view/ComposeBox` exits 0.
- `git grep -c '\[compose-draft\]' src/` returns 0.
- `git grep -c '\[tap-diag\]' src/` returns 0.
- `git grep -c '\[pwa\] ' src/main.tsx` returns >=4 (visibility-change, pagehide, pageshow, boot).
</verification>

<success_criteria>
- Old zoo prefixes `[compose-draft]` and `[tap-diag]` fully eliminated from ComposeBox.tsx.
- Compose submit path (entry + success + failure) has structured log lines that let post-Phase-31 diagnosis answer "did the send fire, did it succeed, what error if any."
- PWA lifecycle at main.tsx level emits structured lines for visibilitychange / pagehide / pageshow / boot so a bug like "app hangs on page-restore" can be correlated with the pageshow persisted=true event.
- No behavior change; existing tests green.
</success_criteria>

<output>
Create `.planning/phases/31-whole-app-structured-logging-backfill/31-05-SUMMARY.md` when done. Include:
- Counts of new/remapped log lines per subsystem ([compose], [tap], [pwa]).
- Whether main.instrumentation.test.tsx was created or skipped per D-20 discretion, with justification.
- Any surfaces originally in D-02 ("session-open flow from user-click through terminal render") that the executor discovered ARE reachable from main.tsx AND instrumented, vs. those left for other files.
</output>
