---
phase: 31-whole-app-structured-logging-backfill
plan: 09
type: execute
wave: 4
depends_on:
  - 31-02
  - 31-03
  - 31-04
  - 31-05
  - 31-06
  - 31-07
  - 31-08
files_modified:
  - .planning/phases/31-whole-app-structured-logging-backfill/31-COVERAGE-REPORT.md
  - .planning/phases/31-whole-app-structured-logging-backfill/31-MANUAL-VERIFICATION.md
autonomous: false
requirements:
  - D-01
  - D-04
  - D-05
  - D-11
  - D-13
  - D-20
  - D-21
  - D-22
must_haves:
  truths:
    - "No JSON.stringify(event) remains in any touched frontend file (D-05 anti-pattern eliminated app-wide)"
    - "All old prefixes ([WebSocket], [voice-diag], [compose-draft], [tap-diag], [DIAG-REPORT], [SW], [postSpeakStream]) return 0 grep hits in src/"
    - "All Phase 31 smoke tests + full pre-existing test suite pass with npx vitest run exit 0"
    - "Frontend (npm run build) and backend (npm run build:backend) compile clean"
    - "A COVERAGE-REPORT.md enumerates every subsystem in D-02+D-03 with its shipped log-line inventory"
    - "A MANUAL-VERIFICATION.md documents the post-ship Ashley-reproduces-a-symptom procedure for verifying the WS-cycle log trail lands as expected on cellular"
    - "D-04 going-forward rule delivery verified: the Standing directive block \"Logging is cheap and batched to the console-forward server...\" is present in ~/.claude/roles/box-maintainer/box-maintainer.md § Standing directives (banked 2026-08-11 during discuss-phase); the phase does NOT ship if this directive block is absent"
  artifacts:
    - path: ".planning/phases/31-whole-app-structured-logging-backfill/31-COVERAGE-REPORT.md"
      provides: "Final coverage report: per-subsystem line inventory + gap notes"
      contains: "COVERAGE REPORT"
    - path: ".planning/phases/31-whole-app-structured-logging-backfill/31-MANUAL-VERIFICATION.md"
      provides: "Post-ship manual verification procedure for Ashley to reproduce a WS-cycle event on cellular"
      contains: "MANUAL VERIFICATION"
  key_links:
    - from: "final grep pass"
      to: "coverage report"
      via: "systematic enumeration of every prefix + assertion counts"
      pattern: "grep -c"
    - from: "smoke tests + build gates"
      to: "quality gate"
      via: "npx vitest run + npm run build + npm run build:backend all exit 0"
      pattern: "exit 0"
---

<objective>
Final Phase 31 verification and gap surface:

1. **Grep pass — anti-patterns eliminated**: `JSON.stringify(event)` returns 0 in any file touched; old prefixes all return 0.
2. **Grep pass — coverage inventory**: enumerate every prefix and count sites in every D-02+D-03 subsystem; write a COVERAGE-REPORT.md.
3. **Build + test gates**: `npm run build` clean, `npm run build:backend` clean, `npx vitest run` all green (including the new Phase 31 smoke tests + full pre-existing suite).
4. **Manual verification procedure**: write a MANUAL-VERIFICATION.md describing what Ashley reproduces post-ship (one WS-cycle event on cellular) and what log lines to expect — so the two deferred bounties (D-21) have a clear "did the instrumentation land" acceptance signal.
5. **Human checkpoint**: pause for user (Ashley/orchestrator) to review the coverage report + manual verification procedure BEFORE the phase is closed as ready-to-ship. This is `checkpoint:human-verify` because a purely-automated verification can't judge whether the instrumentation MEANS what we hope it means for post-Phase-31 diagnosis.

Purpose: leave the phase in a state where the going-forward rule (D-04) is inherit-able and the deferred bounties (D-21) have a clear "reproduce → confirm log-shape → open diagnosis phase" ramp.

Output: 2 verification documents + human-approved coverage sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-whole-app-structured-logging-backfill/31-CONTEXT.md
@.planning/phases/31-whole-app-structured-logging-backfill/31-PATTERNS.md
@.planning/phases/31-whole-app-structured-logging-backfill/31-01-SUMMARY.md
@.planning/phases/31-whole-app-structured-logging-backfill/31-02-SUMMARY.md
@.planning/phases/31-whole-app-structured-logging-backfill/31-03-SUMMARY.md
@.planning/phases/31-whole-app-structured-logging-backfill/31-04-SUMMARY.md
@.planning/phases/31-whole-app-structured-logging-backfill/31-05-SUMMARY.md
@.planning/phases/31-whole-app-structured-logging-backfill/31-06-SUMMARY.md
@.planning/phases/31-whole-app-structured-logging-backfill/31-07-SUMMARY.md
@.planning/phases/31-whole-app-structured-logging-backfill/31-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run grep coverage sweep + build gates + full test suite, write 31-COVERAGE-REPORT.md</name>
  <files>.planning/phases/31-whole-app-structured-logging-backfill/31-COVERAGE-REPORT.md</files>
  <read_first>
    - All 8 prior 31-*-SUMMARY.md files (they enumerate what each plan shipped — the coverage report cross-references against them)
    - .planning/phases/31-whole-app-structured-logging-backfill/31-CONTEXT.md § D-02 + D-03 (the enumerated subsystems that MUST be covered)
    - .planning/phases/31-whole-app-structured-logging-backfill/31-PATTERNS.md § "File Classification" (17 files enumerated — the coverage report walks these)
  </read_first>
  <action>
    Execute the following verification commands, capture their outputs, and compose `31-COVERAGE-REPORT.md`. Use absolute paths in every command.

    A) **Anti-pattern elimination grep pass**:
    - `grep -rn "JSON.stringify(event)" /home/ubuntu/skynet-tiffany/src/` — MUST return 0 hits (D-05 anti-pattern eliminated).
    - `grep -rn "JSON.stringify(e)" /home/ubuntu/skynet-tiffany/src/ui/` — MUST return 0 hits where `e` is a DOM Event (context-check each hit; if hits exist and refer to non-Event `e` like accumulator objects, note explicitly in report).
    - `grep -rn '\[WebSocket\]' /home/ubuntu/skynet-tiffany/src/` — MUST return 0 hits.
    - `grep -rn '\[voice-diag\]' /home/ubuntu/skynet-tiffany/src/` — MUST return 0 hits.
    - `grep -rn '\[compose-draft\]' /home/ubuntu/skynet-tiffany/src/` — MUST return 0 hits.
    - `grep -rn '\[tap-diag\]' /home/ubuntu/skynet-tiffany/src/` — MUST return 0 hits.
    - `grep -rn '\[DIAG-REPORT\]' /home/ubuntu/skynet-tiffany/src/` — MUST return 0 hits.
    - `grep -rn '\[SW\]' /home/ubuntu/skynet-tiffany/src/ui/hooks/` — MUST return 0 hits (only under hooks/ where the SW is used).
    - `grep -rn '\[postSpeakStream\]' /home/ubuntu/skynet-tiffany/src/` — MUST return 0 hits.

    B) **Canonical prefix inventory grep pass** — capture the count for each D-13 prefix in src/:
    - `grep -rc '\[ws\] ' /home/ubuntu/skynet-tiffany/src/ | grep -v ':0$'` — record per-file hit counts.
    - Same for: `[ws-server]`, `[ws-msg]`, `[pause-gate]`, `[reopen]`, `[session]`, `[session-server]`, `[session-parser]`, `[tts]`, `[voice]`, `[voice-server]`, `[pwa]`, `[compose]`, `[tap]`, `[render]`, `[pane-state]`, `[pane-state-emitter]`, `[host-db]`, `[tmux-helper]`.

    C) **D-02 subsystem coverage crosswalk** — for each of the following D-02-enumerated subsystems, verify at LEAST one log line ships:
    | Subsystem (D-02) | Prefix expected | Grep hit? |
    |---|---|---|
    | WS lifecycle (open/close/error/message) | [ws] | expect >=5 in Terminal.tsx |
    | Pause-gate (patches #367-#369) | [pause-gate] | expect >=2 in Terminal.tsx |
    | 4 reopen paths | [reopen] with path= attribution | expect 4 distinct path= values |
    | Session-open flow | [session] or [session-server] | expect >=1 |
    | TTS/speak (fetch/decode/play/media events) | [tts] | expect >=10 in ChatMessage.tsx |
    | Voice recording | [voice] | expect >=20 in useVoiceRecording.ts |
    | PWA visibility + SW | [pwa] | expect >=4 in main.tsx + >=4 in use-service-worker.ts |
    | Compose/draft | [compose] | expect >=3 in ComposeBox.tsx |
    | Keyboard/tap | [tap] | expect >=6 in ComposeBox.tsx |
    | Pretty-view render | [render] | expect >=3 (pane-mount + pane-unmount + tick) |
    | Pane-state emit (backend) | [pane-state-emitter] | expect >=2 |
    | Session-file parser (backend) | [session-parser] | expect >=3 |
    | Host CRUD + session-kill | [host-db] | expect >=20 |
    | Voice transcribe/speak (backend) | [voice-server] | expect >=4 |
    | tmux-helper SSH execs | [tmux-helper] | expect >=3 |
    | Frontend pane_state receive | [pane-state] | expect >=1 line matching /^\[pane-state\] received/ in PrettyView.tsx (paired with backend [pane-state-emitter] for D-16 cross-side correlation) |
    | WS message router (backend→client) | [ws-msg] | expect >=1 line matching /^\[ws-msg\] received type=/ in Terminal.tsx (dedup-collapsed for hot types per D-13/D-17) |

    For each row, mark COVERED / GAP based on actual grep counts.

    D) **Build gates**:
    - Run `cd /home/ubuntu/skynet-tiffany && npm run build:backend` — capture exit code + last 20 lines of output.
    - Run `cd /home/ubuntu/skynet-tiffany && npm run build` — capture exit code + last 20 lines of output.
    - BOTH must exit 0.

    E) **Full test suite**:
    - Run `cd /home/ubuntu/skynet-tiffany && npx vitest run` — capture exit code + summary line.
    - MUST exit 0 with all tests passing. If failures exist, DO NOT proceed — file them as blockers in the report and stop.

    F) **Compose `31-COVERAGE-REPORT.md`** with sections:
    1. "Anti-pattern elimination" — grep results for the 7 old prefixes + JSON.stringify(event); each MUST be 0.
    2. "Canonical prefix inventory" — table of D-13 prefixes with per-file counts (from step B).
    3. "D-02 subsystem coverage crosswalk" — the table from step C with COVERED/GAP marks.
    4. "Build gates" — exit codes for backend and frontend builds.
    5. "Test suite" — vitest exit code + pass/fail count.
    6. "Suspected bugs surfaced" — aggregated list from all 8 prior SUMMARYs (per D-22 discipline). These are candidates for follow-up bounties AFTER Phase 31 ships.
    7. "Gaps / follow-up" — any subsystem where GAP was marked; any file where instrumentation was consciously deferred; the D-17 backend rate-limit-silent-drop behavior noted in plan 31-08 § task 2 (if surfaced as confusing during testing).

    G) **If any grep in section A returns >0 or any subsystem in section C is GAP**:
    - The plan does NOT pass. File a revision request back to the responsible earlier plan (e.g. if `[voice-diag]` still returns >0, revise plan 31-04). Do not proceed to task 2 until section A + C are clean.

    No fenced code blocks in this action. Report content is prose + markdown tables.
  </action>
  <verify>
    <automated>
    grep -rn 'JSON.stringify(event)' /home/ubuntu/skynet-tiffany/src/ | wc -l | grep -q '^0$' &&
    grep -rn '\[WebSocket\]' /home/ubuntu/skynet-tiffany/src/ | wc -l | grep -q '^0$' &&
    grep -rn '\[voice-diag\]' /home/ubuntu/skynet-tiffany/src/ | wc -l | grep -q '^0$' &&
    grep -rn '\[compose-draft\]' /home/ubuntu/skynet-tiffany/src/ | wc -l | grep -q '^0$' &&
    grep -rn '\[tap-diag\]' /home/ubuntu/skynet-tiffany/src/ | wc -l | grep -q '^0$' &&
    grep -rn '\[DIAG-REPORT\]' /home/ubuntu/skynet-tiffany/src/ | wc -l | grep -q '^0$' &&
    grep -rn '\[postSpeakStream\]' /home/ubuntu/skynet-tiffany/src/ | wc -l | grep -q '^0$' &&
    cd /home/ubuntu/skynet-tiffany && npm run build:backend &&
    cd /home/ubuntu/skynet-tiffany && npm run build &&
    cd /home/ubuntu/skynet-tiffany && npx vitest run
    </automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/31-whole-app-structured-logging-backfill/31-COVERAGE-REPORT.md` exists with the 7 sections named above.
    - All 7 anti-pattern grep counts = 0.
    - All 17 D-02 subsystem rows in section C are COVERED (no GAP marks) — includes the revision-added [pane-state] (frontend receive) and [ws-msg] (dispatch) rows.
    - Both build commands exit 0.
    - vitest exit 0 with all tests passing.
    - Suspected-bugs list in section 6 is either populated (specific candidates for follow-up bounties) or explicitly notes "no bugs surfaced during instrumentation" — do not leave the section empty.
  </acceptance_criteria>
  <done>
    Coverage report written; all gates green; anti-patterns eliminated app-wide.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write 31-MANUAL-VERIFICATION.md — post-ship procedure for Ashley to reproduce a WS-cycle event and confirm log-trail lands</name>
  <files>.planning/phases/31-whole-app-structured-logging-backfill/31-MANUAL-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/31-whole-app-structured-logging-backfill/31-CONTEXT.md § D-21 (deferred bounties: ws-pause-gate-stuck-connect-cycling, speak-button-broken-on-cellular)
    - .planning/phases/31-whole-app-structured-logging-backfill/31-02-SUMMARY.md (WS lifecycle instrumentation summary from plan 31-02)
    - .planning/phases/31-whole-app-structured-logging-backfill/31-03-SUMMARY.md (TTS instrumentation summary from plan 31-03)
    - .planning/phases/31-whole-app-structured-logging-backfill/31-07-SUMMARY.md (backend transport summary from plan 31-07)
    - ~/.claude/roles/box-maintainer/bounties/ws-pause-gate-stuck-connect-cycling/bounty.json — the 08:03-08:07 UTC 2026-08-11 empirical baseline (Ashley's cellular reproduction)
  </read_first>
  <action>
    Compose `.planning/phases/31-whole-app-structured-logging-backfill/31-MANUAL-VERIFICATION.md` with the following sections:

    1. **Purpose**: Phase 31 shipped instrumentation only (D-22 — no bugs fixed). The deferred bounties (D-21) are unblocked once Ashley reproduces and confirms the new log trail lands as expected. This document is the post-ship procedure.

    2. **Prerequisites**:
       - Skynet deployed with all Phase 31 commits (per box-maintainer § "Subagents don't do deploys" — deploy is orchestrator-only, not part of this plan).
       - iPhone PWA opened at term.gigaashley.click.
       - Cellular connection (WiFi disabled) — matches the observed empirical baseline.
       - Terminal SSH access to skynet-ec2 for tailing the log file.

    3. **Reproduction A — WS reconnect cycle** (targets ws-pause-gate-stuck-connect-cycling bounty):
       - Open Skynet PWA on iPhone.
       - Attempt to open a new session (fresh tmux session on a fleet host).
       - Wait for the symptom to reproduce (either the "Waiting for connection logs..." hang or the reconnect cycle).
       - On skynet-ec2: `sudo tail -F /opt/skynet/console-forward-logs/console-forward.log | grep -E '(\\[ws\\]|\\[ws-msg\\]|\\[pause-gate\\]|\\[reopen\\]|\\[ws-server\\]|\\[pane-state\\]|\\[pane-state-emitter\\])'`
       - Confirm the log stream contains (at minimum):
         * `[ws] close code=<N> reason="<...>" wasClean=<true|false>` — NOT `[WebSocket] Error: {"isTrusted":true}` (the old anti-pattern is gone).
         * `[ws] wasConnectedRef-transition edge=<X→Y> trigger=<what>` on every ref flip.
         * `[reopen] fired path=<setup-effect|onclose-retry|visibilitychange|direct-caller>` on every reconnect attempt.
         * `[pause-gate] blocked-<caller> reason=hidden` if the pause-gate fires while hidden.
         * A `[ws-server] accept` and `[ws-server] close` line pairing each frontend `[ws] open`/`[ws] close` (from backend, source=backend).
         * `[ws-msg] received type=<msg.type>` per WS frame (dedup-collapsed for hot types like `data`). If the reconnect cycle NEVER produces messages, this line is absent and the log correlation makes the "connect-then-nothing" symptom visible.
         * `[pane-state] received phase=<state> sessionId=<...>` on the frontend PrettyView.tsx side, PAIRED WITH `[pane-state-emitter]` on the backend with matching `sessionId=<...>` field — D-16 cross-side correlation. If backend emits but frontend never receives, the log trail names the exact WS gap.
       - Confirmation criteria: within ONE reproduction, the log stream unambiguously names WHICH refs flipped, WHICH reopen path fired, and WHAT the close code was.

    4. **Reproduction B — TTS speak silence** (targets speak-button-broken-on-cellular bounty):
       - Open Skynet PWA on iPhone on cellular.
       - Open a pretty-view conversation with a long assistant message.
       - Tap the speak-button on the message.
       - If no audio plays (the reproduced symptom), scan the log tail: `sudo tail -F /opt/skynet/console-forward-logs/console-forward.log | grep '\\[tts\\]'`
       - Confirm the log stream contains (at minimum):
         * `[tts] speak-start owner=<N> textLen=<N> voice="..." trigger=user-click`.
         * `[tts] fetch-start` followed by `[tts] fetch-resolved status=<N> ok=<true|false>`.
         * `[tts] play-attempt owner=<N>` followed by `[tts] play-attempt result=<success|error|blocked>`.
         * If play-attempt result != success: `[tts] player-error errCode=<N> errMessage="..."` or `[tts] play-attempt result=blocked errName="NotAllowedError"`.
         * Media events (canplay/playing/stalled/suspend/error) if the audio element progressed at all.
       - Confirmation criteria: the log stream unambiguously names WHICH stage failed (fetch never resolved / play-attempt rejected / media error / stalled forever).

    5. **What to do with the observations**:
       - If Reproduction A/B confirms the log trail lands as expected → Phase 31 is verified; open follow-up bounty(-ies) to diagnose + fix the root cause using the new log data.
       - If the log trail is MISSING an expected line (e.g. `[pause-gate] blocked-*` never fires despite the symptom being a pause-gate stall) → Phase 31 has a coverage gap; file a revision back to the responsible plan (31-02 for pause-gate, 31-03 for TTS).
       - If the log trail's lines are structurally malformed (e.g. code=undefined) → file a revision back to fix the field extraction.

    6. **Ashley's role**:
       - Reproduce ONE instance of each symptom on her iPhone PWA on cellular (Phase 31's whole point of ambient-signal collection).
       - Grep-verify the log lines land per section 3 and 4 criteria — 5 minutes of `sudo tail -F ... | grep` on skynet-ec2.
       - Report back with either "log trail lands as expected — opening bounty for diagnosis" or "log trail missing X — revising Phase 31."

    7. **Silence-is-success rule**: per box-maintainer.md, if Ashley doesn't report back within the reasonable window, Phase 31 is assumed verified. No blocking-on-UAT loops.

    Format the file as a runbook — clear step numbering, exact commands (with absolute paths where relevant), and explicit "confirmation criteria" checkpoints. Use markdown but no fenced code (per plan-execute discipline); wrap commands in inline backticks.
  </action>
  <verify>
    <automated>test -f /home/ubuntu/skynet-tiffany/.planning/phases/31-whole-app-structured-logging-backfill/31-MANUAL-VERIFICATION.md && grep -q 'Reproduction A' /home/ubuntu/skynet-tiffany/.planning/phases/31-whole-app-structured-logging-backfill/31-MANUAL-VERIFICATION.md && grep -q 'Reproduction B' /home/ubuntu/skynet-tiffany/.planning/phases/31-whole-app-structured-logging-backfill/31-MANUAL-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/31-whole-app-structured-logging-backfill/31-MANUAL-VERIFICATION.md` exists with all 7 sections named above.
    - Section 3 (Reproduction A) contains ≥5 specific log-line-shape checks with regex-shaped patterns.
    - Section 4 (Reproduction B) contains ≥5 specific log-line-shape checks.
    - The document is under 300 lines (runbooks stay concise).
  </acceptance_criteria>
  <done>
    Manual verification runbook written; explicitly ties each deferred bounty (D-21) to its expected log trail; provides Ashley a 5-minute grep-verify workflow.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify D-04 going-forward rule delivery — assert the box-maintainer Standing directive block is present in the role file</name>
  <files>.planning/phases/31-whole-app-structured-logging-backfill/31-COVERAGE-REPORT.md</files>
  <read_first>
    - ~/.claude/roles/box-maintainer/box-maintainer.md § Standing directives (the "Logging is cheap and batched to the console-forward server..." block banked 2026-08-11 during discuss-phase — this task verifies its presence)
    - .planning/phases/31-whole-app-structured-logging-backfill/31-CONTEXT.md § D-04 (the going-forward rule: "Every future patch that touches an interaction/lifecycle/effect boundary MUST add structured logs at that boundary as part of the patch — no exceptions")
    - .planning/phases/31-whole-app-structured-logging-backfill/31-CONTEXT.md § D-07 (Phase 31 does the initial pass across all D-02+D-03 surfaces AND establishes the going-forward rule — both, not one)
  </read_first>
  <action>
    D-04 (the going-forward rule) is a POLICY deliverable of Phase 31, not a code deliverable. It exists as a Standing directive in `~/.claude/roles/box-maintainer/box-maintainer.md`. This task verifies the directive is present in the role file and that the phase cannot ship without it.

    A) **Assert the directive block exists** — run:
    - `grep -q "Logging is cheap and batched to the console-forward server" ~/.claude/roles/box-maintainer/box-maintainer.md`
    - Exit code MUST be 0. If exit code is 1 (grep didn't find the string), the directive was never banked or was removed — the phase acceptance FAILS and this task's acceptance_criteria fails.

    B) **Confirm placement under § Standing directives** — run:
    - `awk '/## Standing directives|^## Standing directives|^### Standing directives/,/^##[^#]/' ~/.claude/roles/box-maintainer/box-maintainer.md | grep -c "Logging is cheap and batched to the console-forward server"`
    - Output MUST be >=1. Verifies the directive lives under the Standing directives section, not just anywhere in the file (a stray comment elsewhere doesn't count).

    C) **Append the verification result to 31-COVERAGE-REPORT.md** — add a new section 8 to the coverage report titled "D-04 going-forward rule delivery" that contains:
    - The exact matching line from the role file (via `grep -n "Logging is cheap and batched to the console-forward server" ~/.claude/roles/box-maintainer/box-maintainer.md`).
    - A one-line confirmation: "D-04 going-forward rule delivered as Standing directive at ~/.claude/roles/box-maintainer/box-maintainer.md § Standing directives (banked 2026-08-11 per discuss-phase, verified present at ship time)."
    - If EITHER grep in A or B fails, the report section instead records the FAILURE and this task's acceptance_criteria fails — Phase 31 does not close.

    D) **Do NOT** modify the role file from this task — this task only READS and VERIFIES. If the directive is missing, the correct response is:
    - Emit a revision-mode request back through the orchestrator: "Phase 31 cannot close — D-04 Standing directive missing from ~/.claude/roles/box-maintainer/box-maintainer.md. Re-run discuss-phase or bank the directive manually per ~/.claude/skills/id/SKILL.md role-update discipline, then re-run this task."
    - Do NOT auto-inject the directive from this task (~/.claude/skills/id/SKILL.md governs role-file updates; adding it here would bypass the id discipline).

    No fenced code blocks in this action.
  </action>
  <verify>
    <automated>grep -q "Logging is cheap and batched to the console-forward server" ~/.claude/roles/box-maintainer/box-maintainer.md</automated>
  </verify>
  <acceptance_criteria>
    - Command assertion: `grep -q "Logging is cheap and batched to the console-forward server" ~/.claude/roles/box-maintainer/box-maintainer.md` exits 0.
    - Command assertion: `awk '/^## Standing directives/,/^## [^S]/' ~/.claude/roles/box-maintainer/box-maintainer.md | grep -c "Logging is cheap and batched to the console-forward server"` returns >=1 (directive is inside the Standing directives section, not stray).
    - File assertion: `.planning/phases/31-whole-app-structured-logging-backfill/31-COVERAGE-REPORT.md` contains a section titled "D-04 going-forward rule delivery" with the verification result recorded.
    - If EITHER grep fails: this task's acceptance_criteria FAILS and Phase 31 does not close. This is a HARD gate on D-07 (phase does both: instrumentation + going-forward rule) — a phase that shipped instrumentation but forgot the rule leaves future patches uninstrumented, which defeats the phase's whole purpose.
  </acceptance_criteria>
  <done>
    D-04 going-forward rule delivery verified: the "Logging is cheap and batched to the console-forward server" Standing directive is present in ~/.claude/roles/box-maintainer/box-maintainer.md § Standing directives; verification result recorded as section 8 of 31-COVERAGE-REPORT.md; phase acceptance not blocked on this dimension.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human review of coverage report + manual verification runbook</name>
  <what-built>
    - `.planning/phases/31-whole-app-structured-logging-backfill/31-COVERAGE-REPORT.md` documenting all 17 D-02 subsystems (including revision-added [pane-state] and [ws-msg] rows) with COVERED marks + anti-pattern elimination confirmation + section 8 D-04 delivery verification.
    - `.planning/phases/31-whole-app-structured-logging-backfill/31-MANUAL-VERIFICATION.md` documenting the Reproduction A + B procedures for the two deferred bounties.
    - All 8 prior plan SUMMARYs available at `.planning/phases/31-whole-app-structured-logging-backfill/31-*-SUMMARY.md`.
    - Full frontend + backend test suites green; both builds clean.
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/31-whole-app-structured-logging-backfill/31-COVERAGE-REPORT.md`:
       - Confirm all 7 anti-pattern grep counts are 0.
       - Confirm all 17 D-02 subsystem rows are COVERED (no GAP marks) — includes the two revision-mode-added rows for Frontend pane_state receive ([pane-state] in PrettyView.tsx per plan 31-06 Task 2) and WS message router ([ws-msg] in Terminal.tsx per plan 31-02 Task 3).
       - Confirm section 8 "D-04 going-forward rule delivery" exists and records the Standing directive as present in ~/.claude/roles/box-maintainer/box-maintainer.md.
       - Skim the suspected-bugs-surfaced list — decide which are worth filing as follow-up bounties AFTER Phase 31 ships.
    2. Read `.planning/phases/31-whole-app-structured-logging-backfill/31-MANUAL-VERIFICATION.md`:
       - Confirm the Reproduction A + B procedures make sense and match how Ashley reproduces (iPhone PWA on cellular).
       - Confirm the log-line-shape checks are specific enough to fail LOUDLY if the instrumentation missed a subsystem.
    3. Spot-check ONE or TWO instrumented files (e.g. `src/ui/features/terminal/Terminal.tsx`, `src/backend/claude-session/claude-session-server.ts`) and confirm the log shapes visually match what the coverage report claims.
    4. Optionally: run `grep -rc '\\[ws\\] ' /home/ubuntu/skynet-tiffany/src/` and eyeball the distribution.

    **Approve if**: the coverage is credible, the manual verification runbook is executable, and the suspected-bugs list has reasonable candidates for follow-up.

    **Ask for revision if**: any subsystem GAP mark; anti-pattern grep >0; runbook missing critical log-line-shape check; a suspected bug looks like it should have been caught by the instrumentation but wasn't logged.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 31, OR describe revisions needed (which plan to revise + what change).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coverage report writing → filesystem | Report is written under .planning/; git-tracked. No secret exposure. |
| grep output → report content | grep results captured verbatim; may include file paths only. No credential exposure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-24 | Information Disclosure | coverage report content | mitigate | Report captures grep counts and file paths only. No actual log-line CONTENT is dumped into the report (the log lines' fields could include hostId etc. but the coverage report only counts, doesn't inspect). |
| T-31-25 | Denial of Service | full-suite test run duration | accept | `npx vitest run` full suite may take several minutes; this is the standard verification cost. Not a concern. |
| T-31-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed. package.json unchanged. |
</threat_model>

<verification>
- Coverage report exists with all 7 sections + 15-row D-02 crosswalk.
- Manual verification runbook exists with both Reproduction A and B procedures.
- `npm run build:backend` exit 0.
- `npm run build` exit 0.
- `npx vitest run` exit 0.
- Anti-pattern grep sweep: all 7 old-prefix greps return 0.
- Human checkpoint receives "approved" from reviewer.
- D-04 going-forward rule delivery: `grep -q "Logging is cheap and batched to the console-forward server" ~/.claude/roles/box-maintainer/box-maintainer.md` exits 0 (Task 3 hard gate).
</verification>

<success_criteria>
- Every D-02+D-03 subsystem covered per the crosswalk in the coverage report.
- Every anti-pattern (D-05 + old prefixes) eliminated from src/.
- Full test suite + both builds green.
- Deferred bounties (D-21) have a clear, executable verification runbook.
- Human reviewer approves the coverage + runbook, closing Phase 31 as ready-to-ship.

**Phase 31's contribution to D-04 (going-forward rule) is DELIVERED**: after this plan, all interaction/lifecycle/effect boundaries in the app follow the D-11 shape / D-13 taxonomy / D-14 levels codified in useVoiceRecording.ts's canonical golden-copy. Future patches per the Standing directive on box-maintainer.md 2026-08-11 have a concrete pattern to copy.
</success_criteria>

<output>
Create `.planning/phases/31-whole-app-structured-logging-backfill/31-09-SUMMARY.md` when done. Include:
- Final grep counts for every D-13 prefix (frontend + backend).
- Confirmation of both build exit codes.
- Total test count (X passed / 0 failed).
- Human reviewer's approval message (verbatim, if approved) OR the revision request (if not).
- Any candidate follow-up bounties surfaced during the phase (from the aggregated D-22 suspected-bugs list) — these become the immediate post-Phase-31 work.
</output>
