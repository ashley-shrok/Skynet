---
phase: 31-whole-app-structured-logging-backfill
plan: 02
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - src/ui/features/terminal/Terminal.tsx
  - src/ui/features/terminal/Terminal.instrumentation.test.tsx
autonomous: true
requirements:
  - D-01
  - D-02
  - D-05
  - D-06
  - D-11
  - D-12
  - D-13
  - D-14
  - D-15
  - D-17
  - D-20
  - D-21
  - D-22
must_haves:
  truths:
    - "Every WS event handler in Terminal.tsx extracts .code/.reason/.wasClean explicitly (no JSON.stringify(event) remaining)"
    - "All 5 load-bearing WS refs (wasConnectedRef, isConnectingRef, isAttachingSessionRef, isVisibleRef, shouldNotReconnectRef) emit edge=old→new trigger=... lines on transition"
    - "All 4 reopen paths (setup-effect, onclose-retry, visibilitychange, direct-caller) log with path= attribution"
    - "The pause-gate boundaries (patches #367-#369, !isVisibleRef sites) emit [pause-gate] blocked-... lines with reason=hidden"
    - "The misleading 'Connection closed unexpectedly … reverse proxy or SSL configuration issue' copy either goes or is paired with a structured [ws] close line carrying the real code/reason"
    - "A smoke test asserts a WS close event produces a line matching /\\[ws\\] close .* code=\\d+ reason=\"[^\"]*\" wasClean=(true|false)/"
  artifacts:
    - path: "src/ui/features/terminal/Terminal.tsx"
      provides: "instrumented WS lifecycle + pause-gate + reopen ladder"
      contains: "[ws] "
    - path: "src/ui/features/terminal/Terminal.instrumentation.test.tsx"
      provides: "smoke tests for the load-bearing WS lifecycle log lines"
      contains: "describe"
  key_links:
    - from: "Terminal.tsx ws.addEventListener('close')"
      to: "[ws] close line"
      via: "explicit event.code/reason/wasClean extraction"
      pattern: "\\[ws\\] close .*code=\\$\\{event\\.code\\}"
    - from: "Terminal.tsx wasConnectedRef mutation sites"
      to: "[ws] wasConnectedRef-transition line"
      via: "edge=false→true trigger=... structured field"
      pattern: "wasConnectedRef-transition edge="
    - from: "Terminal.tsx 4 reopen sites"
      to: "[reopen] fired line"
      via: "path=setup-effect|onclose-retry|visibilitychange|direct-caller attribution"
      pattern: "\\[reopen\\] fired.*path="
---

<objective>
Instrument the WS lifecycle, pause-gate, load-bearing ref transitions, and 4-path reopen ladder in `src/ui/features/terminal/Terminal.tsx` per D-02 § "Frontend surfaces confirmed in scope" + D-06/D-15.

This is THE plan that directly enables the `ws-pause-gate-stuck-connect-cycling` bounty (deferred per D-21) to be diagnosed post-Phase-31. The current logs are useless (`[WebSocket] Error: {"isTrusted":true}` is the D-05 anti-pattern verbatim); after this plan they become actionable.

Purpose: replace the D-05 anti-pattern in every WS/DOM-event handler; add structured lines at all 5 load-bearing ref transitions per D-06/D-15; attribute all 4 reopen-ladder paths per D-02; add pause-gate lines at the patch #367-#369 `!isVisible` boundaries.

Output: instrumented Terminal.tsx + one smoke test file asserting the critical [ws] close line shape.

**PROHIBITED per D-22:** Do NOT diagnose or fix `ws-pause-gate-stuck-connect-cycling` or `speak-button-broken-on-cellular` inline, even if the root cause becomes visually obvious while adding logs. If you notice a suspected bug, add a `[ws]` warn line that would expose it and MOVE ON. File a follow-up bounty note in the SUMMARY, do NOT patch the underlying code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-whole-app-structured-logging-backfill/31-CONTEXT.md
@.planning/phases/31-whole-app-structured-logging-backfill/31-PATTERNS.md
@.planning/phases/31-whole-app-structured-logging-backfill/31-01-SUMMARY.md
@src/ui/features/pretty-view/useVoiceRecording.ts
@src/ui/features/terminal/Terminal.tsx
@src/ui/lib/log-dedup.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Instrument WS event handlers (open/close/error/message) with explicit field extraction + pause-gate lines + rewrite misleading copy</name>
  <files>src/ui/features/terminal/Terminal.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/useVoiceRecording.ts (GOLDEN COPY — model every emission on the `[voice-diag] X: field=value field=value` template-literal shape at lines 124/136/152/165/196/210; RENAMING happens in plan 31-04)
    - src/ui/features/terminal/Terminal.tsx lines 1250-1400 (WS setup + open + pong-timeout + patch #367 pause gate near lines 400-420 + close near lines 2040-2115 + error near lines 2110-2120)
    - src/ui/features/terminal/Terminal.tsx lines 320-345 (ref declarations: isAttachingSessionRef@216, isVisibleRef@321, shouldNotReconnectRef@337, isConnectingRef@339, wasConnectedRef@340)
    - src/ui/features/terminal/Terminal.tsx lines 400-420 (visibilitychange handler + pause-gate `if (!isVisibleRef.current) return` at line 405)
    - src/ui/features/terminal/Terminal.tsx lines 1075-1160 (attemptReconnection + connectToHost — reopen paths)
    - .planning/phases/31-whole-app-structured-logging-backfill/31-CONTEXT.md § D-05 (NEVER JSON.stringify DOM Event), D-06 (5 refs), D-11 (msg shape), D-12 (standard fields), D-13 ([ws] [pause-gate] [reopen] prefixes), D-14 (info/warn/error semantics), D-15 (edge=x→y trigger=z shape)
    - .planning/phases/31-whole-app-structured-logging-backfill/31-PATTERNS.md § "Terminal.tsx" (specific line refs, current anti-patterns, exact new-line shapes)
  </read_first>
  <behavior>
    - Test 1 (in task 2): A simulated WS close event with `code=1006 reason="abnormal" wasClean=false` produces a line matching `/^\[ws\] close hostId=\d+ .*code=1006 reason="abnormal" wasClean=false/`.
    - Test 2 (in task 2): A simulated WS close event with `code=1000 reason="" wasClean=true` when `wasConnectedRef=false && !isAttachingSessionRef` produces a `[ws] connection-rejected` line (renamed from misleading "Connection closed unexpectedly" copy).
    - Test 3 (in task 2): Toggling `wasConnectedRef.current = true` at the WS open handler emits a line matching `/^\[ws\] wasConnectedRef-transition edge=false→true trigger=ws-open/`.
    - Test 4 (in task 2): visibilitychange to hidden then back to visible produces 2 lines: `[pwa] visibility-change` (from main.tsx — separate plan) AND `[ws] isVisibleRef-transition edge=true→false trigger=visibilitychange` AND `[ws] isVisibleRef-transition edge=false→true trigger=visibilitychange`.
  </behavior>
  <action>
    Perform all edits in `src/ui/features/terminal/Terminal.tsx` following the useVoiceRecording golden-copy shape. Use `console.info` for expected transitions, `console.warn` for unexpected-but-recoverable, `console.error` for real failures (per D-14). Every line MUST include `hostId=${hostId}` and `sessionId=${sessionId ?? 'null'}` (D-12); pull hostId from the existing `hostId` prop / context; sessionId from `tmuxSession` where available.

    A) **WS event handlers** — replace the D-05 anti-pattern sites:
    - Line 2115 `console.error("[WebSocket] Error:", event)`: replace with `console.error(\`[ws] error hostId=${hostId} sessionId=${sessionId ?? 'null'} type=${event.type} isTrusted=${event.isTrusted} readyState=${ws.readyState}\`)`. Note: WS ErrorEvent has NO code/reason (those are on CloseEvent); do not fabricate them.
    - Line 1353 `console.warn("[WebSocket] Pong timeout ...")`: rewrite as `console.warn(\`[ws] pong-timeout hostId=${hostId} sessionId=${sessionId ?? 'null'} readyState=${ws.readyState}\`)`.
    - Line 2047 `console.warn("[WebSocket] Abnormal closure detected ...")`: rewrite as `console.warn(\`[ws] abnormal-close hostId=${hostId} sessionId=${sessionId ?? 'null'} code=${event.code} reason="${event.reason}" wasClean=${event.wasClean} wasConnected=${wasConnectedRef.current}\`)`.
    - Line 2065 `console.error("WebSocket authentication failed:", event.reason)`: rewrite as `console.error(\`[ws] auth-failed hostId=${hostId} sessionId=${sessionId ?? 'null'} code=${event.code} reason="${event.reason}"\`)`.
    - Line 2084 `console.error("[WebSocket] Connection rejected by server")`: rewrite as `console.error(\`[ws] connection-rejected hostId=${hostId} sessionId=${sessionId ?? 'null'} code=${event.code} reason="${event.reason}" wasClean=${event.wasClean}\`)`. This is the misleading copy that the reverse-proxy/SSL translation surfaces from — the STRUCTURED line replaces the misleading branch. The user-facing `t("terminal.connectionRejected")` toast can stay (that's UI copy, not diagnostic). Do NOT touch the i18n string in this plan (out of scope).
    - Line 2008 `console.error("WebSocket message handler error:", error)`: rewrite as `console.error(\`[ws] msg-handler-error hostId=${hostId} sessionId=${sessionId ?? 'null'} err="${error instanceof Error ? error.message : String(error)}"\`)`.
    - Line 2046 (the "may be due to reverse proxy or SSL configuration issue" copy referenced in planning direction) — if present as user-facing i18n only, leave the i18n key alone; if present as a `console.warn` with that literal text, replace with a structured `[ws] close code=... reason=... wasClean=...` line and remove the code-side misleading interpretation.

    Additionally, at the `ws.addEventListener("open", ...)` block near line 1264, ADD as the FIRST line inside the handler:
    - `console.info(\`[ws] open hostId=${hostId} sessionId=${sessionId ?? 'null'} wsUrl=${wsUrl} readyState=${ws.readyState}\`)`

    At the `ws.addEventListener("close", ...)` block near line 2040, ADD as the FIRST line inside the handler (BEFORE the code=1006/1008/1000-1005 branches):
    - `console.info(\`[ws] close hostId=${hostId} sessionId=${sessionId ?? 'null'} code=${event.code} reason="${event.reason}" wasClean=${event.wasClean} isVisible=${isVisibleRef.current} wasConnected=${wasConnectedRef.current}\`)`

    B) **Load-bearing ref transitions** (D-06/D-15) — 5 refs, every mutation site:
    Locate every assignment to `wasConnectedRef.current`, `isConnectingRef.current`, `isAttachingSessionRef.current`, `isVisibleRef.current`, `shouldNotReconnectRef.current`. Each mutation gets ONE new line IMMEDIATELY BEFORE (to capture old→new) the assignment. Shape:
    - `console.info(\`[ws] ${refName}-transition edge=${prev}→${next} trigger=${triggerString} hostId=${hostId} sessionId=${sessionId ?? 'null'}\`)`

    Where `refName` is `wasConnectedRef` etc. (no quotes, just the identifier as string), `prev` is the current value of the ref BEFORE mutation, `next` is the new value, and `triggerString` names the code path (e.g. `ws-open`, `ws-close-abnormal`, `ws-close-1006`, `visibilitychange-hidden`, `visibilitychange-visible`, `connectToHost-entry`, `setup-effect-mount`, `unmount-cleanup`). Refer to PATTERNS.md § Terminal.tsx for the enumerated mutation sites (lines 345/347/349/350/406/408/410/597/954/990/992/995/1094/1161/1163/1252/1289/1303/1317).

    Where prev is `false` and next is `true` (or vice versa), emit the line. Where next equals prev, SKIP the line (no-op writes are noise). Use `oldValue !== newValue` as the guard.

    Only skip the emission for the initial `useRef(false)` initializations at declaration time — those are not mutations.

    C) **Pause-gate lines** (patches #367-#369 boundary — D-02) — at every `if (!isVisibleRef.current) return;` site (lines ~405, ~1080, and any others discovered by grepping `!isVisibleRef.current`), add IMMEDIATELY INSIDE the if-body BEFORE the return:
    - `console.warn(\`[pause-gate] blocked-${caller} hostId=${hostId} sessionId=${sessionId ?? 'null'} reason=hidden\`)`

    Where `caller` names the outer function (e.g. `reconnect`, `visibilitychange-handler`, `setup-effect`, `heartbeat`). Grep `!isVisibleRef` in the file to enumerate all sites; if more than the two mentioned exist, instrument each.

    D) **Reopen ladder attribution** (D-02) — the 4 paths:
    - **setup-effect deps**: the useEffect at ~L2903 (or wherever the top-level WS setup effect lives — find via grep for `connectToHost` calls inside a useEffect body without a visibilitychange or onclose context). At the site that calls `connectToHost(...)`, add IMMEDIATELY BEFORE the call: `console.info(\`[reopen] fired hostId=${hostId} path=setup-effect wsUrl=${wsUrl}\`)`.
    - **onclose retry scheduler**: at the reconnect-scheduling site inside the close handler (attemptReconnection call at ~L2101 / ~L2107 / wherever `attemptReconnection()` is invoked from within the onclose branch), add immediately BEFORE the call: `console.info(\`[reopen] fired hostId=${hostId} path=onclose-retry code=${event.code} wasClean=${event.wasClean}\`)`.
    - **visibilitychange handler**: at the visibilitychange handler's connectToHost call (~L415 in the `if (!isVisibleRef.current) return` block that later calls connectToHost), add immediately BEFORE: `console.info(\`[reopen] fired hostId=${hostId} path=visibilitychange\`)`.
    - **direct connectToHost callers**: at every OTHER connectToHost call site (grep `connectToHost(` and enumerate — expect ~4-6 hits after excluding the 3 above), add: `console.info(\`[reopen] fired hostId=${hostId} path=direct-caller callSite="${functionNameOrLineContext}"\`)`. Use a short static string identifying the caller (e.g. `"attemptReconnection"`, `"handleReconnectClick"`).

    E) **Other bare console.error/warn sites in this file** — rewrite to `[ws] op-failed` shape:
    - Line 520 `console.error("Failed to load command history:", error)` → `console.error(\`[ws] load-history-failed hostId=${hostId} err="${error instanceof Error ? error.message : String(error)}"\`)`
    - Line 541 `console.error("Failed to load autocomplete history:", error)` → analogous shape with `event=load-autocomplete-failed`
    - Line 589 `console.warn("Failed to log terminal activity:", err)` → analogous shape with `event=log-activity-failed`
    - Line 770 `console.error("Terminal operation failed:", error)` and Line 1055 similar → `console.error(\`[ws] op-failed hostId=${hostId} sessionId=${sessionId ?? 'null'} err="${error instanceof Error ? error.message : String(error)}"\`)`
    - Line 1579 `console.warn("Failed to execute startup snippet:", err)` → `console.warn(\`[ws] snippet-failed hostId=${hostId} sessionId=${sessionId ?? 'null'} err="${err instanceof Error ? err.message : String(err)}"\`)`
    - Line 2248 `console.error("Failed to delete command from history:", error)` → analogous shape with `event=delete-history-failed`

    Leave Lines 1198 and 1209 (Electron server URL) as-is — those aren't in the WS scope; they'll be addressed by plan 31-05 if session-open flow covers them.

    F) **Dedup opt-in for hot paths** — Import `createLogDedup` from `../../lib/log-dedup` at the top of Terminal.tsx. Create a MODULE-scoped dedup instance for `[ws] isVisibleRef-transition` transitions ONLY (visibility flap is the confirmed hot path):
    - `const visibilityDedup = createLogDedup({ N: 3, W: 5000 })` at module scope OR inside the component (whichever pattern matches existing file conventions — read the file to see how module-scope helpers are declared).
    - At the `isVisibleRef` transition emission site, wrap the console.info in `if (visibilityDedup.shouldEmit(\`ws-isVisibleRef-\${next}\`, () => msg).emit) console.info(msg)`.
    - Do NOT dedup any of `[ws] close`, `[ws] error`, `[reopen] fired`, or ref transitions OTHER than isVisibleRef — those are opt-out per D-17.

    No fenced code blocks in this action. All snippets above are D-11 shape references (template literal expressions), not code to paste verbatim.

    Because this file is enormous (3582 lines), work in careful edit passes: first pass = WS event handler rewrites (section A); second pass = ref transitions (section B); third = pause-gate + reopen ladder (C+D); fourth = misc console rewrites (E); fifth = dedup import + wrap (F). After each pass, `npx tsc --noEmit` to catch typo regressions before piling on.

    **D-22 discipline reminder**: if you find a bug (e.g. a ref that should be reset but isn't, a reopen path that fires when it shouldn't), do NOT fix it. Add a log that would surface it. Note the finding in the SUMMARY under "suspected bugs surfaced for follow-up bounty."
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/ui/features/terminal/</automated>
  </verify>
  <acceptance_criteria>
    - Grep assertion: `git grep -v '^#' src/ui/features/terminal/Terminal.tsx | grep -c 'JSON.stringify(event)'` returns 0 (D-05 anti-pattern eliminated for DOM Events in this file).
    - Grep assertion: `git grep -v '^#' src/ui/features/terminal/Terminal.tsx | grep -c '\[WebSocket\]'` returns 0 (all old prefixes remapped).
    - Grep assertion: `git grep -c '\[ws\] close' src/ui/features/terminal/Terminal.tsx` returns >=1.
    - Grep assertion: `git grep -c '\[ws\] error' src/ui/features/terminal/Terminal.tsx` returns >=1.
    - Grep assertion: `git grep -c 'wasConnectedRef-transition' src/ui/features/terminal/Terminal.tsx` returns >=2 (at least one true→false, one false→true).
    - Grep assertion: `git grep -c 'isVisibleRef-transition' src/ui/features/terminal/Terminal.tsx` returns >=2.
    - Grep assertion: `git grep -c 'isConnectingRef-transition' src/ui/features/terminal/Terminal.tsx` returns >=2.
    - Grep assertion: `git grep -c 'isAttachingSessionRef-transition' src/ui/features/terminal/Terminal.tsx` returns >=2.
    - Grep assertion: `git grep -c 'shouldNotReconnectRef-transition' src/ui/features/terminal/Terminal.tsx` returns >=2.
    - Grep assertion: `git grep -c '\[pause-gate\] blocked-' src/ui/features/terminal/Terminal.tsx` returns >=2.
    - Grep assertion: `git grep -c 'path=setup-effect' src/ui/features/terminal/Terminal.tsx` returns >=1.
    - Grep assertion: `git grep -c 'path=onclose-retry' src/ui/features/terminal/Terminal.tsx` returns >=1.
    - Grep assertion: `git grep -c 'path=visibilitychange' src/ui/features/terminal/Terminal.tsx` returns >=1.
    - Grep assertion: `git grep -c 'path=direct-caller' src/ui/features/terminal/Terminal.tsx` returns >=1.
    - Grep assertion: `git grep -c 'createLogDedup' src/ui/features/terminal/Terminal.tsx` returns >=1 (dedup imported + used for visibility flap).
    - Behavior assertion: `npx tsc --noEmit` exits 0.
    - Behavior assertion: `npx vitest run src/ui/features/terminal/` exits 0 (pre-existing tests still pass — no behavior regressions).
  </acceptance_criteria>
  <done>
    Terminal.tsx WS event handlers, load-bearing refs (5), pause-gate boundaries, and reopen ladder (4 paths) all instrumented per D-05/D-06/D-11/D-12/D-13/D-14/D-15. Dedup opt-in wired for visibilityRef flap only. Misleading "Connection rejected" copy paired with structured [ws] close line. tsc clean + existing tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Terminal.instrumentation.test.tsx smoke suite asserting critical [ws] close + ref-transition line shapes</name>
  <files>src/ui/features/terminal/Terminal.instrumentation.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/useVoiceRecording.ts (golden-copy shape the tests assert against)
    - src/ui/features/terminal/Terminal.tsx (post-task-1 instrumented file — read the emitted line shapes to write matching regexes)
    - Any existing Terminal.tsx test file (e.g. Terminal.test.tsx if present) for the harness pattern (WebSocket mock, ResizeObserver polyfill, etc.) — use the same test-harness idiom
    - .planning/phases/31-whole-app-structured-logging-backfill/31-CONTEXT.md § D-20 (smoke tests for MOST critical lines: WS close event structured fields, pause-gate transitions — this plan's coverage)
  </read_first>
  <behavior>
    - Test 1: Given a mock CloseEvent with `code=1006, reason="abnormal", wasClean=false`, dispatching it to Terminal's WS produces a `console.info` call whose first argument matches `/^\[ws\] close hostId=\d+ .*code=1006 reason="abnormal" wasClean=false/`.
    - Test 2: Given a mock CloseEvent with `code=1000, reason="", wasClean=true` when wasConnectedRef was never set true, produces a `console.error` call matching `/^\[ws\] connection-rejected .*code=1000/`.
    - Test 3: Given a mock ErrorEvent (not CloseEvent) with `type="error"`, produces a `console.error` call matching `/^\[ws\] error .*type=error isTrusted=/` AND NOT containing `JSON.stringify` OR `{"isTrusted":true}` verbatim.
    - Test 4: Given a WS open event, produces a `console.info` matching `/^\[ws\] wasConnectedRef-transition edge=false→true trigger=ws-open/`.
    - Test 5: Toggling document.visibilityState to "hidden" then "visible" produces the `[ws] isVisibleRef-transition edge=true→false` line THEN `edge=false→true` line (may be preceded by `[pause-gate] blocked-*` if the reopen fires while still hidden).
  </behavior>
  <action>
    Create `src/ui/features/terminal/Terminal.instrumentation.test.tsx`. Use vitest + @testing-library/react conventions matching the rest of the codebase — read one existing Terminal.tsx test (or nearby test file like `src/ui/features/pretty-view/ChatMessage.speak.test.tsx`) for the setup pattern.

    Setup harness needs:
    - A MockWebSocket class exposing `.addEventListener(type, cb)` + `.dispatch(type, event)` + `.readyState` gettable + `.close()`. Global window.WebSocket mocked to return this.
    - `vi.spyOn(console, 'info')`, `vi.spyOn(console, 'warn')`, `vi.spyOn(console, 'error')` — capture and assert against calls[i][0] as a string.
    - Render `<Terminal hostId={42} ...minimal-props-to-mount />` — mirror the props from the closest existing Terminal.tsx test.

    For each of the 5 behavior tests above, mount Terminal, dispatch the relevant event on the mock WS or trigger the visibilitychange, wait for effects, then assert `expect(console.info.mock.calls.map(c=>c[0])).toEqual(expect.arrayContaining([expect.stringMatching(/pattern/)]))`.

    IMPORTANT — this is a SMOKE test per D-20, not a contract test. The tests assert the SHAPE of the critical lines (regex-match) — NOT the exact set of every log line. Additional log lines in the output are fine; missing the critical ones fails the test.

    If mounting Terminal proves prohibitively hard due to its 3582-line surface area (many context providers, refs, ResizeObserver deps), fall back to a UNIT-scoped variant: extract-and-test only the log-emission helpers by importing the module and stubbing dependencies. But prefer the integration approach if it works in <100 lines of test setup.

    Follow useVoiceRecording.ts test style for `console.warn` spy assertions — that pattern is already-known-good in this codebase.

    No fenced code blocks in this action; the test file itself uses standard vitest/RTL syntax.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/terminal/Terminal.instrumentation.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/ui/features/terminal/Terminal.instrumentation.test.tsx` exists with at least 5 `it(...)` blocks matching the 5 behaviors.
    - Test assertion: `npx vitest run src/ui/features/terminal/Terminal.instrumentation.test.tsx` exits 0.
    - Grep assertion: `git grep -c 'expect.stringMatching' src/ui/features/terminal/Terminal.instrumentation.test.tsx` returns >=5 (one per behavior).
    - Behavior assertion: The [ws] close test regex `/\[ws\] close .*code=\d+ reason="[^"]*" wasClean=(true|false)/` matches at least one captured console.info call.
  </acceptance_criteria>
  <done>
    Smoke test file exists and passes; asserts the 5 most-critical instrumentation shapes from task 1; runs in <30s.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS server → Terminal.tsx | WS frames arrive from backend; DOM Event objects cross into log lines. D-05 mandates explicit field extraction; JSON.stringify(event) is the specific anti-pattern this plan eliminates. |
| Log line content → console-forwarder → backend log file | New lines carry hostId (numeric) + sessionId (tmux session name string). No credentials, no session tokens, no user input body content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | Information Disclosure | Terminal.tsx log lines | mitigate | Log lines carry only: hostId (numeric), sessionId (tmux session name, opaque string), WS close code/reason (server-provided), readyState (numeric). Explicitly NOT logged: WS message BODIES, user keystrokes, snippet contents, auth tokens. Task-1 action's rewrite of line 520 (command history load error) uses `error.message` only — history rows themselves are never logged. |
| T-31-05 | Denial of Service | log volume from visibility flap | mitigate | isVisibleRef-transition (the confirmed hot path per PATTERNS.md § dedup discipline) is wrapped in a `createLogDedup({N:3, W:5000})` from plan 31-01 per section F of task 1. Other refs are per-transition (low volume by construction — WS lifecycle events are rare). |
| T-31-06 | Tampering | log-line shape drift | mitigate | Task 2's smoke test asserts the critical `[ws] close` line regex — future refactors that break the shape fail the test. Additional lines added by future work must also match D-11 template-literal shape (grep-verified in plan 31-09). |
| T-31-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed. package.json unchanged. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 after all edits.
- `npx vitest run src/ui/features/terminal/` exits 0 (full terminal test suite green).
- `git grep -c 'JSON.stringify(event)' src/ui/features/terminal/` returns 0.
- `git grep -c '\[WebSocket\]' src/ui/features/terminal/` returns 0.
- `git grep -c '\[ws\] close' src/ui/features/terminal/Terminal.tsx` returns >=1.
- All 5 ref-transition patterns present (grep counts documented in task 1 acceptance criteria).
</verification>

<success_criteria>
- The `ws-pause-gate-stuck-connect-cycling` bounty (deferred per D-21) can be diagnosed from post-Phase-31 logs alone: for any observed reconnect loop, the log trail names WHICH refs flipped, WHICH pause-gate blocked, WHICH reopen path fired, and WHAT the WS close code+reason were.
- Zero JSON.stringify(event) on DOM Events remaining in Terminal.tsx.
- Smoke test locks in the critical [ws] close line shape against future regressions.
- No behavior change to Terminal.tsx — instrumentation only. Existing terminal test suite still green.
</success_criteria>

<output>
Create `.planning/phases/31-whole-app-structured-logging-backfill/31-02-SUMMARY.md` when done. Include:
- Count of new log lines added, grouped by prefix ([ws], [pause-gate], [reopen]).
- List of ref mutation sites instrumented (line numbers).
- List of reopen-path sites attributed (line numbers + which path=).
- Any "suspected bugs surfaced" list per D-22 discipline (bugs noticed but NOT fixed — capture as bounty candidates for post-Phase-31 follow-up).
</output>
