---
phase: 45-fix-forward-on-phase-43-restore-correct-architecture-for-win
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/api/claude-session-api.ts
  - src/ui/api/claude-session-api.test.ts
autonomous: true
requirements: []
tags: [pretty-view, frontend, wire-types, revert, phase-43-fix-forward]

must_haves:
  truths:
    - "Frontend claude-session-api module has no fetch_older / historyWindow wire types or helpers"
    - "openClaudeSessionSocket() accepts ZERO arguments and constructs a URL with no query string"
    - "src/ui/api/claude-session-api.test.ts does not exist (whole-file delete because 100% of its specs cover deleted Phase 43 helpers)"
    - "grep -R 'fetch_older|historyWindow|sendFetchOlder|isFetchOlderBatchEvent|FetchOlder' src/ui/ returns hits ONLY inside PrettyView.tsx (which Wave 2 Plan 45-03 cleans up)"
    - "npx vitest run src/ui/api/ exits 0 with zero failures and zero .todo markers"
  artifacts:
    - path: "src/ui/api/claude-session-api.ts"
      provides: "WS client with 4-arg openClaudeSessionSocket() (no opts), no fetch_older types, no send helpers for the deleted feature"
      exports: ["openClaudeSessionSocket", "ClaudeSessionServerEvent", "ConnectToPanePayload"]
  key_links:
    - from: "src/ui/api/claude-session-api.ts"
      to: "WebSocket URL"
      via: "no query string appended to /claude-session/websocket/"
      pattern: "return new WebSocket\\(url\\)"
    - from: "grep -c countIdentityBounties src/ui/api/claude-session-api.ts"
      to: "positive integer (sibling one-shot helper canary preserved)"
      via: "delete-only-Phase-43 surgery"
      pattern: "countIdentityBounties"
---

<objective>
Delete every Phase 43 addition inside `src/ui/api/claude-session-api.ts` — the `historyWindow?: number` opts parameter on `openClaudeSessionSocket`, the `FetchOlderPayload` type, the `FetchOlderBatchEvent` type, the `sendFetchOlder` helper, and the `isFetchOlderBatchEvent` type-guard. Delete the entire `src/ui/api/claude-session-api.test.ts` file — all 21 spec assertions reference `sendFetchOlder`, `isFetchOlderBatchEvent`, `fetch_older`, or `fetch_older_batch` (verified by pattern-mapper via grep count = 21 hits in 174 lines).

Purpose: Removes the client-side wire types Plan 45-03 (Wave 2) will strip out of `PrettyView.tsx`. Splitting the wire cleanup from the component cleanup keeps files single-owner per plan and lets Wave 1 run in parallel with backend revert (Plan 45-01). No behavior change on its own — until Wave 2 lands, `PrettyView.tsx` still imports these deleted names and will fail to compile, which is the correct signal that Wave 2 is the dependent step. (The wave-gated executor blocks 45-03 on 45-01 + 45-02, so 45-03 will always run after these two are done.)

Output: The client-facing WS API is a pre-Phase-43-shaped surface. Wave 2 removes the last consumer (`PrettyView.tsx`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-CONTEXT.md
@.planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-PATTERNS.md
@.planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-05-PLAN.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete claude-session-api.test.ts (whole file — 100% Phase 43 coverage)</name>
  <files>src/ui/api/claude-session-api.test.ts</files>
  <read_first>
    - src/ui/api/claude-session-api.test.ts (whole file, 174 lines — pattern-mapper confirmed every one of the ~20 specs references `sendFetchOlder` OR `isFetchOlderBatchEvent` OR `fetch_older` / `fetch_older_batch` type literals; grep count = 21 hits)
    - .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-PATTERNS.md § 8 (whole-file delete rationale + grep evidence in § Metadata)
    - .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-CONTEXT.md § "Test strategy" (delete Phase 43 stale specs, do not tolerate skipped or .todo markers)
  </read_first>
  <behavior>
    - Post-deletion: file does not exist on disk.
    - Post-deletion: `git grep -q 'claude-session-api.test' src/ui/` returns exit code 1 (no consumers — vitest discovers by convention, not by path reference).
    - Post-deletion: `npx vitest run src/ui/api/` runs zero tests from this file (file gone) and exits 0.
  </behavior>
  <action>
    Confirm the pattern-mapper's finding before deletion — run `grep -cE 'sendFetchOlder|isFetchOlderBatchEvent|fetch_older|FetchOlder' src/ui/api/claude-session-api.test.ts` and verify the result is positive AND that the total spec count matches (each `it(` should reference one of these Phase 43 names). If any `it(` block covers behavior UNRELATED to Phase 43 (e.g. tests for `openClaudeSessionSocket` URL construction without the historyWindow opt-in, or tests for `ClaudeSessionServerEvent` shape independent of fetch_older_batch), STOP and switch to surgical edit-in-place instead of whole-file delete.

    Assuming the pattern-mapper's finding holds: delete via `git rm src/ui/api/claude-session-api.test.ts`. No content preservation — the two remaining exports (`openClaudeSessionSocket` post-revert, `ClaudeSessionServerEvent`, `ConnectToPanePayload`) are exercised at integration level by `PrettyView.plain-dom.test.tsx` and the replacement test in Plan 45-03. Unit-level coverage of the API surface is not required for a 4-line factory function; deletion is the cleaner audit trail.
  </action>
  <verify>
    <automated>test ! -f src/ui/api/claude-session-api.test.ts &amp;&amp; npx vitest run src/ui/api/ &amp;&amp; git grep -q claude-session-api.test src/ui/ &amp;&amp; echo FAIL || echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/ui/api/claude-session-api.test.ts` returns exit code 1 (file gone).
    - `git status --porcelain src/ui/api/claude-session-api.test.ts` shows `D ` (staged deletion).
    - `npx vitest run src/ui/api/` exits 0 (any remaining test files pass; the deleted one doesn't get discovered).
    - Before deletion: `grep -cE 'sendFetchOlder|isFetchOlderBatchEvent|fetch_older|FetchOlder' src/ui/api/claude-session-api.test.ts` returned ≥ 21 (recorded in commit message as evidence of Phase-43-only coverage).
  </acceptance_criteria>
  <done>File deleted via git rm. If any non-Phase-43 spec found during the pre-deletion audit, task pivots to surgical edit and preserves those specs — otherwise clean whole-file delete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Revert claude-session-api.ts to pre-Phase-43 4-line factory + delete Phase 43 wire-type block</name>
  <files>src/ui/api/claude-session-api.ts</files>
  <read_first>
    - src/ui/api/claude-session-api.ts L1-45 (openClaudeSessionSocket function region — L14-42 is the current Phase 43 opts-with-JSDoc + `hw` / `qp` construction to strip)
    - src/ui/api/claude-session-api.ts L876-1001 (entire Phase 43 wire-type + helper block: section-header comment L876-901, FetchOlderPayload L903-921, FetchOlderBatchEvent L923-949, sendFetchOlder L951-981, isFetchOlderBatchEvent L983-1001)
    - src/ui/api/claude-session-api.ts L1017-1059 (sibling `countIdentityBounties` — MUST stay byte-for-byte intact; canary that surgery didn't over-reach)
    - .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-PATTERNS.md § 7 (revert block content) and § "One-shot backend-write-then-frontend-consume pattern is the SIBLING of what's deleted" (canary rule)
  </read_first>
  <behavior>
    - Post-edit `openClaudeSessionSocket` signature is exactly `export function openClaudeSessionSocket(): WebSocket` — zero parameters, zero JSDoc referencing historyWindow.
    - Post-edit `openClaudeSessionSocket` body constructs the URL as `${scheme}//${host}/claude-session/websocket/` with no query-string interpolation (no `?historyWindow=`).
    - Post-edit file has ZERO occurrences of: `historyWindow`, `FetchOlderPayload`, `FetchOlderBatchEvent`, `sendFetchOlder`, `isFetchOlderBatchEvent`, `fetch_older`, `fetch_older_batch`.
    - Post-edit file preserves `countIdentityBounties` (~L1017-1059 pre-edit; may shift after Phase 43 block deleted) byte-for-byte.
    - Post-edit file compiles clean via `npx tsc --noEmit` for the frontend (CLAUDE.md notes tsc --noEmit only covers frontend; backend was covered in Plan 45-01 Task 3).
    - The dependent consumer file `src/ui/features/pretty-view/PrettyView.tsx` will BREAK compile after this edit (it still imports the deleted names). This is expected — Plan 45-03 in Wave 2 fixes it. Wave-2 executor ordering guarantees 45-03 runs after both 45-01 and 45-02 land, so the broken state is a transient hop between waves.
  </behavior>
  <action>
    Two discrete edits, applied top-down (edits don't affect each other's line numbers):

    Edit 1 — `openClaudeSessionSocket` at current L14-42. Delete: (a) the opts parameter `opts?: { historyWindow?: number }` from the signature, (b) the 13-line JSDoc comment L15-26 describing the historyWindow opt-in, (c) the `hw` local at L35, (d) the `qp` local at L36-39, (e) the query-string interpolation on the URL. Result — restore the pre-Phase-43 shape:

    Function signature: `export function openClaudeSessionSocket(): WebSocket {` (no opts param, no JSDoc other than any surviving pre-Phase-43 doc — check `git show 4e4da2c6~1:src/ui/api/claude-session-api.ts` for the pre-P43 top-of-file JSDoc, if any, and preserve it verbatim).

    Function body: three consts (`scheme`, `host`, `url`) constructing `${scheme}//${host}/claude-session/websocket/` and `return new WebSocket(url);` — exactly the pre-Phase-43 shape.

    Edit 2 — delete the entire Phase 43 block at current L876-1001 (~125 lines). Includes: 26-line section-header comment L876-901 starting with "─── Phase 43: fetch_older WS wire types (scaffolding — no consumers here) ──"; the `FetchOlderPayload` type L903-921; the `FetchOlderBatchEvent` type L923-949; the `sendFetchOlder` function L951-981 (with its 3-paragraph JSDoc); the `isFetchOlderBatchEvent` type-guard L983-1001 (with JSDoc).

    After both edits: run `grep -nE 'fetch_older|historyWindow|sendFetchOlder|isFetchOlderBatchEvent|FetchOlder' src/ui/api/claude-session-api.ts` — expect ZERO hits. Then `grep -c countIdentityBounties src/ui/api/claude-session-api.ts` — expect positive (canary preserved).

    Also run `npx tsc --noEmit --project tsconfig.json` — frontend compile MUST fail with errors ONLY inside `src/ui/features/pretty-view/PrettyView.tsx` (which is Wave 2's dependency signal). Errors anywhere else mean the deletion over-reached — investigate + narrow before shipping.
  </action>
  <verify>
    <automated>grep -qE 'fetch_older|historyWindow|sendFetchOlder|isFetchOlderBatchEvent|FetchOlder' src/ui/api/claude-session-api.ts &amp;&amp; echo FAIL_HAS_HITS || (grep -q countIdentityBounties src/ui/api/claude-session-api.ts &amp;&amp; grep -q 'export function openClaudeSessionSocket(): WebSocket' src/ui/api/claude-session-api.ts &amp;&amp; echo PASS)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'fetch_older|historyWindow|sendFetchOlder|isFetchOlderBatchEvent|FetchOlder|FetchOlderPayload|FetchOlderBatchEvent' src/ui/api/claude-session-api.ts` returns 0.
    - `grep -c 'export function openClaudeSessionSocket(): WebSocket' src/ui/api/claude-session-api.ts` returns exactly 1.
    - `grep -c countIdentityBounties src/ui/api/claude-session-api.ts` returns a positive integer (sibling canary preserved).
    - `grep -c 'return new WebSocket(url);' src/ui/api/claude-session-api.ts` returns exactly 1.
    - `npx tsc --noEmit 2>&amp;1 | grep -cE 'src/ui/features/pretty-view/PrettyView.tsx' | awk '{ if ($1 > 0) print "EXPECTED"; else print "UNEXPECTED_ELSEWHERE"; }'` prints `EXPECTED` (only PrettyView.tsx errors, indicating the wave-2 dependency signal is clean).
  </acceptance_criteria>
  <done>openClaudeSessionSocket reverted to no-arg pre-Phase-43 shape. Phase 43 wire-type block (types + helpers) removed. `countIdentityBounties` canary intact. Only compile errors remaining are in PrettyView.tsx (Plan 45-03 dependency signal, expected).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser JS → WS URL construction | client-side URL builder; the deleted `?historyWindow=N` query param is REMOVED from the client's ability to influence backend tail behavior. |
| Client wire-type surface → runtime type-check | `isFetchOlderBatchEvent` runtime guard is DELETED — no code path parses `fetch_older_batch` frames from the WS. Backend Plan 45-01 already deletes the frame emission. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-02-01 | Tampering | client-controlled `?historyWindow=N` on WS URL | mitigate | DELETED — opts param on openClaudeSessionSocket removed; URL construction no longer accepts a query-string input from the client. Deletion is the mitigation. |
| T-45-02-02 | Information Disclosure | client-side sendFetchOlder helper | mitigate | DELETED — no code path can construct a `fetch_older` payload. Backend also deleted the receiving handler (Plan 45-01). |
| T-45-02-03 | Denial of Service | isFetchOlderBatchEvent runtime guard | accept | DELETED — no `fetch_older_batch` frames arrive from backend post-45-01. If a malformed frame with that shape somehow arrived, the normal event-type switch in PrettyView.tsx would fall through to the default case (no-op). |

**Net effect:** All three threats mitigated by DELETION. Phase 45-02 removes client-side attack surface rather than adding it. No new trust boundaries introduced. Package Legitimacy Gate not applicable (no npm installs; changes are pure deletion + revert).
</threat_model>

<verification>
1. Task 1 deletion: `test ! -f src/ui/api/claude-session-api.test.ts` returns exit 0.
2. Task 2 zero-hit sweep: `grep -nE 'fetch_older|historyWindow|sendFetchOlder|isFetchOlderBatchEvent|FetchOlder' src/ui/api/claude-session-api.ts` returns exit code 1.
3. Sibling canary preserved: `grep -c countIdentityBounties src/ui/api/claude-session-api.ts` returns positive.
4. Function signature reverted: `grep -c 'export function openClaudeSessionSocket(): WebSocket' src/ui/api/claude-session-api.ts` returns 1.
5. Compile signal: `npx tsc --noEmit 2>&1 | grep -E 'src/ui/features/pretty-view/PrettyView.tsx' | wc -l` returns a positive integer (Wave 2 dependency signal — PrettyView.tsx now has unresolved imports, exactly as expected pre-Plan 45-03).
6. Vitest for the API dir passes: `npx vitest run src/ui/api/` exits 0 (deleted file gone; any surviving api-dir tests still green).
</verification>

<success_criteria>
- Client-side `openClaudeSessionSocket()` accepts zero args and constructs the URL with no query string — matches pre-Phase-43 shape byte-for-byte.
- All Phase 43 wire types (`FetchOlderPayload`, `FetchOlderBatchEvent`) and helpers (`sendFetchOlder`, `isFetchOlderBatchEvent`) deleted from `claude-session-api.ts`.
- `claude-session-api.test.ts` deleted whole-file (100% Phase 43 coverage per pattern-mapper grep count of 21 hits in 174 lines).
- Sibling `countIdentityBounties` one-shot helper preserved as canary that surgery didn't over-reach.
- Compile errors remaining are localized to `src/ui/features/pretty-view/PrettyView.tsx` — Wave 2 (Plan 45-03) resolves them by deleting the fetch_older client wiring inside that file.
</success_criteria>

<output>
Create `.planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-02-SUMMARY.md` when done. Include: file deleted (1), file surgically reverted (1), grep zero-hit evidence for the 5 Phase 43 identifiers, `countIdentityBounties` canary count, `npx tsc --noEmit` output showing only-PrettyView.tsx errors as the Wave 2 hand-off signal.
</output>
