---
phase: 45-fix-forward-on-phase-43-restore-correct-architecture-for-win
plan: 04
type: execute
wave: 3
depends_on: [45-01, 45-02, 45-03]
files_modified:
  - src/ui/features/pretty-view/ComposeBox.tsx
  - src/ui/features/pretty-view/commandTags.ts
  - src/ui/AppShell.tsx
autonomous: false
requirements: []
tags: [pretty-view, bug-investigation, phase-43-fix-forward, bug-3]

must_haves:
  truths:
    - "A fresh minified stack line for the `.replace()` crash has been captured from a dev build (source-map-available) after Bugs #1 + #2 fixes are live"
    - "Exactly ONE of the 3 candidate `.replace()` sites has been confirmed as the crash thrower via source-map trace"
    - "Exactly ONE targeted undefined-guard has been added to the confirmed site (not 3 speculative guards)"
    - "The guard is annotated with a source comment linking to Phase 45 for future archaeology"
    - "Post-guard: send-path exercised in dev container without the `.replace()` crash reappearing"
    - "If the crash does NOT reproduce after Bugs #1 + #2 land, the plan closes with 'resolved incidentally' evidence — NO speculative guard shipped"
  artifacts:
    - path: "the-confirmed-crash-site (one of: ComposeBox.tsx:1194 collapseNewlinesForSend, AppShell.tsx:1239 tab-dedup filter, commandTags.ts:53 preprocessCommandTriplets)"
      provides: "Undefined-safe entry so `.replace()` cannot be called on undefined"
      contains: "Phase 45 Bug #3"
    - path: ".planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-04-EVIDENCE.md"
      provides: "Fresh minified stack line + source-map trace + guard rationale + repro-post-guard result"
  key_links:
    - from: "confirmed-crash-site .replace() call"
      to: "typeof input !== 'string' early-return"
      via: "targeted guard"
      pattern: "Phase 45 Bug #3"
---

<objective>
Investigate and fix Bug #3 — `TypeError: Cannot read properties of undefined (reading 'replace')` at `bi` in `AppShell-BjR3_4Qj.js:1:33664` on send, called by `Zi` at `AppShell-BjR3_4Qj.js:5:13582`. Ashley's UAT-locked instruction (CONTEXT § "Bug #3 `.replace()` crash"): DO NOT guard all 3 candidate sites blindly. Reproduce the crash AFTER Bugs #1 + #2 fixes are live (or in the dev container), read the FRESH minified stack line, source-map it against the current dev build to pin down the ONE thrower, add ONE targeted `typeof x !== "string" ? "" : x.replace(...)` guard on THAT site with a source comment linking to Phase 45.

Two possible outcomes:
1. **Crash reproduces post-Bug-#1+#2:** confirm site via source-map trace → add one targeted guard → verify crash no longer reproduces → close plan with the guard shipped.
2. **Crash does NOT reproduce post-Bug-#1+#2:** Bug #1 or #2 fix incidentally unblocked the crash (e.g., the malformed frame that was starving the render pipeline was itself a downstream consequence of the observation-channel starvation). Close plan with "resolved incidentally" evidence — NO speculative guard shipped. The "no defensive code for scenarios that can't happen" rule in CONTEXT § Deferred forbids blanket guards.

This is a `type: execute` plan with `autonomous: false` because it contains a `checkpoint:human-verify` step where Ashley must attempt the send action in a fresh dev build after Bugs #1 + #2 have landed. That human-verify step CANNOT be automated — we cannot programmatically reproduce a user's send in the exact browser session that originally crashed.

Purpose: Close Bug #3 with the smallest possible defensive change (one guard, one file, one comment) or with a documented no-change if the fix landed incidentally.

Output: Either a targeted guard on the confirmed crash site + evidence file, OR an evidence-file-only close-out with the repro-not-reproducing proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-CONTEXT.md
@.planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-PATTERNS.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build fresh dev bundle + prepare source-map access + confirm plan 45-01/02/03 changes are in the dev build</name>
  <files>
    (no source files modified; produces build artifacts in dist/ + prepares .planning/phases/45-.../45-04-EVIDENCE.md scaffold)
  </files>
  <read_first>
    - .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-CONTEXT.md § "Bug #3 `.replace()` crash" (workflow: reproduce → read stack → source-map → target ONE site → guard once → comment)
    - .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-PATTERNS.md § 11 (three candidate sites: `ComposeBox.tsx:1194 collapseNewlinesForSend`, `AppShell.tsx:1239 tab-dedup filter`, `commandTags.ts:53 preprocessCommandTriplets`; guard style patterns for each)
    - ./CLAUDE.md (deploy discipline — subagents don't do deploys; the executor builds artifacts + hands off to Ashley for the actual dev-container spin-up if a running container isn't already available)
  </read_first>
  <behavior>
    - `npm run build` exits 0 producing dist/ with fresh minified JS + source maps.
    - dist/AppShell-*.js exists AND dist/AppShell-*.js.map exists AND dist/index.html references the fresh hash (grep the hash in dist/index.html and match against the file on disk).
    - The fresh dist reflects the state after Plans 45-01, 45-02, 45-03: `grep -c 'appendDedupWithCap' dist/AppShell-*.js` returns positive (client-side cap present), `grep -c 'fetch_older' dist/AppShell-*.js` returns 0 (fetch_older wiring gone).
    - Evidence file scaffold `.planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-04-EVIDENCE.md` created with headers for: "Fresh minified stack line", "Source-map trace", "Confirmed crash site", "Guard shipped OR resolved-incidentally rationale", "Post-fix repro result".
  </behavior>
  <action>
    Run `npm run build` from the repo root. Expect exit 0. Capture the fresh AppShell hash from `dist/index.html` (`grep -oE 'AppShell-[A-Za-z0-9_-]+\.js' dist/index.html`) — this is the hash Ashley's dev browser session will download and (if it crashes) reference in the fresh minified stack.

    Confirm the fresh dist reflects the three prior plans landed: `grep -c 'appendDedupWithCap' dist/AppShell-*.js` (expect ≥ 1), `grep -c 'sendFetchOlder' dist/AppShell-*.js` (expect 0), `grep -c 'paddingBottom:9' dist/AppShell-*.js` (expect ≥ 1 — Vite minifies whitespace around `:`; also try `paddingBottom: 9`).

    Note the source-map inspection tool for later: `node -e 'const sm = require("source-map-js"); const m = require("fs").readFileSync("./dist/AppShell-XXXXXX.js.map", "utf8"); sm.SourceMapConsumer.with(JSON.parse(m), null, c => { const p = c.originalPositionFor({line: LINE_FROM_STACK, column: COLUMN_FROM_STACK}); console.log(p); });'` (replace `XXXXXX`, `LINE_FROM_STACK`, `COLUMN_FROM_STACK` when Ashley provides the fresh stack). Do NOT run this yet — need the fresh stack line first.

    Create the evidence-file scaffold at `.planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-04-EVIDENCE.md` with the following section headers (empty bodies, filled in by later tasks):
    - `## Fresh dev-build hash` — record the AppShell hash for this dev build.
    - `## Fresh minified stack line` — populated in Task 2 by Ashley.
    - `## Source-map trace to src/` — populated in Task 3.
    - `## Confirmed crash site` — one of the three candidates OR "did not reproduce (see repro-not-reproducing evidence)".
    - `## Fix action` — either "guard shipped at file:line" OR "no guard shipped (resolved incidentally)".
    - `## Post-fix repro result` — populated in Task 5 by Ashley.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; test -f dist/index.html &amp;&amp; grep -oE 'AppShell-[A-Za-z0-9_-]+\.js' dist/index.html | head -1 &amp;&amp; grep -c appendDedupWithCap dist/assets/AppShell-*.js 2>/dev/null || grep -c appendDedupWithCap dist/AppShell-*.js &amp;&amp; test -f .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-04-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `test -f dist/index.html` returns exit 0.
    - `grep -oE 'AppShell-[A-Za-z0-9_-]+\.js' dist/index.html | head -1` prints a hash.
    - `grep -c 'appendDedupWithCap' dist/**/AppShell-*.js` (via `find dist -name 'AppShell-*.js' -exec grep -c appendDedupWithCap {} +`) returns ≥ 1 (Plan 45-03 landed).
    - `grep -c 'sendFetchOlder' dist/**/AppShell-*.js` returns 0 (Plan 45-03 stripped the client wiring).
    - `test -f .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-04-EVIDENCE.md` returns exit 0.
  </acceptance_criteria>
  <done>Fresh dev bundle produced with Plans 45-01/02/03 code present; AppShell hash recorded; evidence-file scaffold created.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Ashley reproduces the send action in a fresh dev browser session against the new dist</name>
  <files>.planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-04-EVIDENCE.md</files>
  <what-built>Plans 45-01, 45-02, 45-03 land in dev bundle. Backend: `-n +1` full-file emission, no fetch_older handler, no historyWindow parse. Client: no fetch_older wiring, client-side cap on messages[], 9px paddingBottom on bubbles.</what-built>
  <how-to-verify>
    1. Ashley deploys the fresh dev build (subagent does NOT run this — CLAUDE.md rule + fleet standing directive). Runbook: `docker compose up -d --force-recreate skynet` behind the 15-min deadman rollback timer (see CLAUDE.md § Deploy safety), coord-room announcements per role file protocol.
    2. Once deployed, Ashley opens the deployed Skynet in her browser (term.gigaashley.click or wherever the dev instance lives) with DevTools console open.
    3. Ashley navigates to a pretty-view pane and attempts to send a message — same action shape that triggered the #465 crash.
    4. Ashley reports back with EITHER:
       (a) The full browser-console minified stack line and adjacent frames if the `.replace()` TypeError STILL fires. Copy-paste the stack verbatim into the evidence file's `## Fresh minified stack line` section.
       (b) "Did not reproduce — send worked, bubble appeared, no crash." Also record this outcome in the evidence file.
    5. Also test: paste multi-line content into ComposeBox and send, send an /id command, send an empty message (auto-blocked), send a message on an aside-active session — exercise all 6 `collapseNewlinesForSend` call sites at ComposeBox.tsx L1012/L1217/L1257/L1272/L1362/L1465 to maximize crash-surface exposure.
    6. Also open a new pane / duplicate an existing tab — exercise the AppShell.tsx:1239 tab-dedup filter under load.
  </how-to-verify>
  <resume-signal>
    Ashley types EITHER "crash reproduced: `<paste stack line>`" (then Task 3 source-maps and Task 4 ships one guard) OR "did not reproduce" (then Task 3+4 are skipped and Task 5 documents the incidental fix).
  </resume-signal>
  <action>Human-verify checkpoint (blocking gate) — the executor pauses and prompts Ashley to perform the how-to-verify steps above against the fresh dev build produced in Task 1. Ashley's report (crash-reproduced-with-stack OR did-not-reproduce) is copied verbatim into the `## Fresh minified stack line` and/or `## Post-fix repro result` sections of 45-04-EVIDENCE.md before the executor resumes with Task 3.</action>
  <verify><human-check>Ashley confirms via resume-signal (either "crash reproduced: &lt;stack&gt;" or "did not reproduce"); the executor appends the response to 45-04-EVIDENCE.md before advancing.</human-check></verify>
  <done>Ashley's resume-signal captured verbatim in 45-04-EVIDENCE.md `## Fresh minified stack line` section. Downstream tasks (3, 4) branch on this signal.</done>
</task>

<task type="auto">
  <name>Task 3: Source-map the fresh minified stack line back to src/</name>
  <files>
    .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-04-EVIDENCE.md
  </files>
  <read_first>
    - Fresh minified stack line from Task 2's evidence file (populated by Ashley).
    - dist/**/AppShell-*.js.map (or dist/AppShell-*.js.map) — the source map for the current dev build.
    - PATTERNS.md § 11 — the 3 candidate sites with their src line numbers.
    - source-map-js documentation (via node_modules if installed, or via `npm ls source-map-js`) — the API for `SourceMapConsumer.with(...)` and `originalPositionFor({line, column})`.
    - src/ui/features/pretty-view/ComposeBox.tsx L1193-1195 (Candidate 1 — collapseNewlinesForSend body)
    - src/ui/AppShell.tsx L1239 (Candidate 2 — tab-dedup filter t.label.replace)
    - src/ui/features/pretty-view/commandTags.ts L52-55 (Candidate 3 — preprocessCommandTriplets body)
  </read_first>
  <behavior>
    - Task ONLY runs if Task 2 resume-signal was "crash reproduced" with a stack line. If Task 2 signal was "did not reproduce", Task 3 SKIPS (records a "skip: no crash to trace" line in evidence file and exits).
    - Post-Task-3: `## Source-map trace to src/` section in evidence file records: the raw minified stack line, the source-map lookup result (source file + line + column + name), and a verdict "confirmed thrower: <src file>:<line> — <function name>".
    - The verdict source file MUST be one of: `src/ui/features/pretty-view/ComposeBox.tsx`, `src/ui/AppShell.tsx`, `src/ui/features/pretty-view/commandTags.ts`. If the source map resolves to a DIFFERENT file (a 4th unexpected site), STOP and escalate to Ashley — the investigation scope expanded and the candidate list from PATTERNS.md § 11 is incomplete.
  </behavior>
  <action>
    If Task 2 signal was "did not reproduce": append `Source-map trace: SKIPPED — no crash to trace, see § "Post-fix repro result" for the did-not-reproduce evidence` to the evidence file's `## Source-map trace to src/` section. Exit task.

    If Task 2 signal was "crash reproduced" with a stack line:

    1. Parse the fresh stack line for the minified filename + line + column. Typical shape: `AppShell-XXXXXX.js:1:33664` means line 1, column 33664 (Vite compiles into one long minified line; the "column" is really the byte offset).

    2. Locate the source map: `find dist -name 'AppShell-*.js.map' | head -1` — should match the hash in the fresh stack line. If it doesn't match (Ashley reproduced on an older build), rerun Task 1 with the fresh sources first.

    3. Query the source map. Two options:
       (a) Programmatic: `node -e 'const sm = require("source-map-js"); const fs = require("fs"); const m = JSON.parse(fs.readFileSync("<path-to-map>", "utf8")); sm.SourceMapConsumer.with(m, null, c => { const p = c.originalPositionFor({line: LINE, column: COLUMN}); console.log(JSON.stringify(p)); });'` — replace `<path-to-map>`, `LINE`, `COLUMN` with the actual values. Prints `{source, line, column, name}`.
       (b) CLI: `npx source-map-cli resolve <path-to-map> LINE COLUMN` if the `source-map-cli` npm package is available locally.

    4. Record the result in evidence file's `## Source-map trace to src/` section — verbatim JSON output + one-sentence interpretation.

    5. Verdict — cross-reference the `source` field to the three candidates:
       - If `source` ends in `ComposeBox.tsx` and `line` is ~1194 → Candidate 1 confirmed (`collapseNewlinesForSend`).
       - If `source` ends in `AppShell.tsx` and `line` is ~1239 → Candidate 2 confirmed (tab-dedup filter).
       - If `source` ends in `commandTags.ts` and `line` is ~53 → Candidate 3 confirmed (`preprocessCommandTriplets`).
       - If `source` matches something else → STOP, escalate to Ashley, do NOT proceed to Task 4 (the site is outside the pre-planned candidate set).

    6. Also examine the frame ABOVE the throw (the `Zi` frame at `:5:13582` per CONTEXT.md) — source-map that too. It reveals the CALLER, which helps confirm the site is reachable from the send path (e.g., Candidate 1 caller should be one of ComposeBox's send handlers; Candidate 3 caller should be `ChatMessage` render).

    7. Record the verdict in `## Confirmed crash site` section: `<src file>:<line> — <function name>. Caller: <src file>:<line>. Evidence: source-map lookup above.`
  </action>
  <verify>
    <automated>grep -q "^## Confirmed crash site" .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-04-EVIDENCE.md &amp;&amp; grep -A5 "^## Confirmed crash site" .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-04-EVIDENCE.md | grep -qE 'ComposeBox\.tsx|AppShell\.tsx|commandTags\.ts|did not reproduce'</automated>
  </verify>
  <acceptance_criteria>
    - Evidence file `## Source-map trace to src/` section is populated with either the source-map lookup output OR a "SKIPPED — no crash to trace" note.
    - Evidence file `## Confirmed crash site` section names EXACTLY ONE of the three candidate sites OR records "did not reproduce".
    - If a 4th unexpected site emerges from the source-map lookup, the plan HALTS with an escalation note in the evidence file; Task 4 does NOT run without Ashley's explicit direction on the expanded scope.
  </acceptance_criteria>
  <done>Fresh minified stack line traced to one of the 3 candidate src sites (or "did not reproduce" documented). Evidence file updated with source-map JSON + verdict.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add ONE targeted undefined-guard at the confirmed crash site (or skip if no reproduction)</name>
  <files>
    (one of, depending on Task 3 verdict:)
    src/ui/features/pretty-view/ComposeBox.tsx,
    src/ui/AppShell.tsx,
    src/ui/features/pretty-view/commandTags.ts
  </files>
  <read_first>
    - .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-04-EVIDENCE.md § "Confirmed crash site" (from Task 3)
    - PATTERNS.md § 11 (guard style for each of the 3 candidates)
    - src/ui/features/pretty-view/ComposeBox.tsx L1193-1195 (Candidate 1 body — if verdict = Candidate 1)
    - src/ui/AppShell.tsx L1235-1245 (Candidate 2 body — if verdict = Candidate 2, need context around L1239)
    - src/ui/features/pretty-view/commandTags.ts L50-58 (Candidate 3 body — if verdict = Candidate 3)
  </read_first>
  <behavior>
    - Task ONLY runs if Task 3 verdict was one of the 3 known candidates. If Task 3 said "did not reproduce" or "SKIPPED", Task 4 skips with evidence-file annotation.
    - Post-Task-4: EXACTLY ONE file modified. The modified file contains EXACTLY ONE new `if (typeof <arg> !== "string") return "";` (or contextually-equivalent shape per candidate) line + one adjacent source-comment line referencing Phase 45.
    - The other TWO candidate files remain UNTOUCHED (verified via git diff — canary that surgery targeted only the confirmed site).
    - The guard's early-return value is contextually appropriate for the function (empty string for `collapseNewlinesForSend` since it returns string; empty string for `preprocessCommandTriplets` since it returns string; falsey for the AppShell tab-dedup filter narrows the `.filter(...)` predicate).
    - A companion RED test locking the guard behavior added to the corresponding test file (or the closest existing sibling test file if none exists specifically for that function).
    - `npx vitest run <targeted-test-file>` exits 0 including the new RED-turned-GREEN test.
    - `npx vitest run` (full suite) exits 0.
    - `npx tsc --noEmit` exits 0.
  </behavior>
  <action>
    If Task 3 verdict was "did not reproduce" or Task 3 halted with unexpected site: append `Fix action: NO GUARD SHIPPED — see Task 5 evidence for repro-not-reproducing rationale (or Task 3 escalation)` to `## Fix action` in evidence file. Exit task.

    Otherwise, apply the guard for the confirmed site. Each candidate has a pre-specified guard shape per PATTERNS.md § 11:

    **If verdict = Candidate 1 (`ComposeBox.tsx:1194 collapseNewlinesForSend`):**
    RED first — add a test to `src/ui/features/pretty-view/ComposeBox.test.tsx` (or the nearest send-path test file) that calls `collapseNewlinesForSend(undefined as unknown as string)` and asserts it returns `""` without throwing. Run — expect TypeError (the current code will crash). GREEN — edit `src/ui/features/pretty-view/ComposeBox.tsx` L1193-1195:
    Current:
    ```
    function collapseNewlinesForSend(s: string): string {
      return s.replace(/\r?\n/g, " ");
    }
    ```
    Target:
    ```
    // Phase 45 Bug #3 defense — see .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-04-EVIDENCE.md for the crash-stack evidence and source-map trace confirming this site.
    function collapseNewlinesForSend(s: string): string {
      if (typeof s !== "string") return "";
      return s.replace(/\r?\n/g, " ");
    }
    ```
    Run the RED test — expect GREEN.

    **If verdict = Candidate 2 (`AppShell.tsx:1239 tab-dedup filter`):**
    RED first — add a test to `src/ui/AppShell.test.tsx` (or the nearest tab-dedup test file — if none exists, create one) that exercises the tab-dedup filter with a tab whose `label` is `undefined` and asserts no crash. Run — expect TypeError. GREEN — narrow the filter guard at L1239:
    Current shape: `t.type === type && t.label.replace(/ \(\d+\)$/, "") === host.name`
    Target: `t.type === type && typeof t.label === "string" && t.label.replace(/ \(\d+\)$/, "") === host.name`
    Add source comment line above the enclosing filter expression: `// Phase 45 Bug #3 defense — typeof-guard on t.label to prevent the .replace-on-undefined crash confirmed at this site. See .planning/phases/45-.../45-04-EVIDENCE.md.`
    Run RED test — expect GREEN.

    **If verdict = Candidate 3 (`commandTags.ts:53 preprocessCommandTriplets`):**
    RED first — add a test to `src/ui/features/pretty-view/commandTags.test.ts` (or nearest test file) that calls `preprocessCommandTriplets(undefined as unknown as string)` and asserts it returns `""` without throwing. Run — expect TypeError. GREEN — edit `src/ui/features/pretty-view/commandTags.ts` L52-55:
    Current:
    ```
    export function preprocessCommandTriplets(text: string): string {
      return text.replace(COMMAND_BLOCK_RE, (block) => { /* ... */ });
    }
    ```
    Target:
    ```
    // Phase 45 Bug #3 defense — see .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-04-EVIDENCE.md for the crash-stack evidence and source-map trace confirming this site.
    export function preprocessCommandTriplets(text: string): string {
      if (typeof text !== "string") return "";
      return text.replace(COMMAND_BLOCK_RE, (block) => { /* ... */ });
    }
    ```
    Run RED test — expect GREEN.

    DO NOT touch the other two candidate files. DO NOT add speculative guards to any other `.replace()` sites in the codebase. DO NOT widen the guard to a `try/catch` or a `throw new Error(...)` — the goal is to prevent the crash, not to improve error reporting. (Per CONTEXT § Deferred: "do NOT sweep and guard all 25 sites — that violates the 'no error handling for scenarios that can't happen' rule.")

    Update `## Fix action` in evidence file: `Guard shipped at <src file>:<line> — <function name>. Test at <test file> locks the behavior. Other two candidates left untouched (see git diff canary).`

    Run full-suite verification: `npx tsc --noEmit && npx vitest run && npm run build` — all must exit 0.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run &amp;&amp; npm run build &amp;&amp; grep -q "^## Fix action" .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-04-EVIDENCE.md &amp;&amp; grep -A3 "^## Fix action" .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-04-EVIDENCE.md | grep -qE 'Guard shipped|NO GUARD SHIPPED'</automated>
  </verify>
  <acceptance_criteria>
    - If Task 3 verdict was one of the 3 candidates: EXACTLY ONE of the 3 candidate source files has a diff (verify via `git diff --name-only src/ui/features/pretty-view/ComposeBox.tsx src/ui/AppShell.tsx src/ui/features/pretty-view/commandTags.ts | wc -l` returns 1).
    - The guarded file contains a `Phase 45 Bug #3` source comment (`grep -c 'Phase 45 Bug #3' <guarded-file>` returns ≥ 1).
    - The two non-guarded candidate files are byte-identical to pre-Task-4 state (git diff empty).
    - A new/updated test file exists asserting `.replace()` no longer throws on undefined for the confirmed site.
    - `npx tsc --noEmit`, `npx vitest run`, and `npm run build` all exit 0.
    - Evidence file `## Fix action` section populated with either "Guard shipped at …" or "NO GUARD SHIPPED — …".
    - No new guard added to files OUTSIDE the confirmed candidate (grep for `Phase 45 Bug #3` across `src/` should hit exactly one non-comment location — the confirmed guard).
  </acceptance_criteria>
  <done>Either one targeted guard shipped at the confirmed site with test + comment + full-suite green, OR no guard shipped with evidence-file note of "did not reproduce". Other two candidate files untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Ashley re-verifies the crash is gone in a fresh dev build post-guard (or confirms did-not-reproduce)</name>
  <files>.planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-04-EVIDENCE.md</files>
  <what-built>
    Either: one targeted undefined-guard on the confirmed `.replace()` crash site (per Task 4) + fresh dev bundle rebuilt.
    Or: no code change (crash did not reproduce post-Bugs #1+#2).
  </what-built>
  <how-to-verify>
    1. If Task 4 shipped a guard: subagent runs `npm run build` again (produces a NEW AppShell hash in dist/) and appends the new hash to evidence file's `## Fresh dev-build hash` (marked as "post-guard build"). Ashley redeploys the dev container per CLAUDE.md deploy discipline (15-min deadman rollback + coord-room announcements).
    2. If Task 4 skipped: no rebuild needed; Ashley re-tests against the existing dev build (still Task-1's hash).
    3. Ashley re-runs the same send workflow that triggered the original crash (multi-line paste + send, /id command + send, empty message + send, aside-active send, tab-open dedup exercise). Reports back whether the `.replace()` TypeError fires or does not.
    4. Result recorded in evidence file's `## Post-fix repro result` section verbatim: either "confirmed fixed — 5 send scenarios exercised, no crash" or "still crashing — <fresh stack>" (which would escalate to a re-open plan since the guard didn't catch the site).
  </how-to-verify>
  <resume-signal>
    Ashley types "confirmed fixed" (plan closes; move to Plan 45-05 verification) OR "still crashing: `<fresh stack>`" (plan escalates — this would be surprising because Task 4 shipped a guard on the confirmed site; if it happens, the source-map trace in Task 3 was misleading and a follow-up investigation is needed).
  </resume-signal>
  <action>Human-verify checkpoint (blocking gate). If Task 4 shipped a guard: the executor runs `npm run build` first to produce a fresh dist with the guard in place, then pauses and prompts Ashley to redeploy + re-exercise the send scenarios. If Task 4 skipped (did-not-reproduce path): the executor prompts Ashley to re-exercise the same scenarios against the existing dev build to confirm the crash truly is gone. Ashley's report ("confirmed fixed" or "still crashing: &lt;stack&gt;") is copied verbatim into 45-04-EVIDENCE.md `## Post-fix repro result` before the plan closes.</action>
  <verify><human-check>Ashley confirms via resume-signal ("confirmed fixed" or "still crashing: &lt;stack&gt;"); the executor appends the response to 45-04-EVIDENCE.md `## Post-fix repro result` before closing the plan.</human-check></verify>
  <done>Ashley's post-fix resume-signal captured verbatim in 45-04-EVIDENCE.md. Plan 45-04 closes on "confirmed fixed" or "did not reproduce"; a "still crashing" response escalates to a new plan (Task 3 source-map was misleading).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS message frame → ChatMessage render (Candidate 3 path) | frame's `content` reaches `preprocessCommandTriplets(text)`; if `content === undefined`, `.replace()` on undefined crashes render. |
| ComposeBox textarea → send-path (Candidate 1 path) | textarea value or derived string reaches `collapseNewlinesForSend(s)`; if any caller passes undefined, `.replace()` on undefined crashes send. |
| Tab-list source → tab-dedup filter (Candidate 2 path) | tab records may have `label: undefined` in edge cases; `.replace()` on undefined crashes tab render. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-04-01 | Denial of Service | one of the 3 `.replace()` sites (whichever Task 3 confirms) | mitigate | One targeted `typeof x !== "string" ? "" : x.replace(...)` early-return guard on the confirmed site with a Phase 45 source comment. NOT a blanket sweep — the two unconfirmed sites remain unguarded per the no-defensive-code-for-scenarios-that-can't-happen rule (CONTEXT § Deferred). |
| T-45-04-02 | Denial of Service | the two UNCONFIRMED `.replace()` sites | accept | Deliberately left unguarded. If a future crash confirms a second site, a follow-up phase adds a second targeted guard. Blanket defensive guards masked bugs in prior fleet history and are forbidden by role-file learned preferences. |
| T-45-04-03 | Tampering | source-map lookup output | accept | source-map-js reads a static file from dist/; no user-controlled input. |

**Net effect:** One mitigation, two accept-with-rationale. No new trust boundaries introduced. Package Legitimacy Gate: `source-map-js` is a mature package (weekly downloads in tens of millions, `npmjs.com/package/source-map-js`); if it's not already in package.json, this plan does not install it — the executor either uses an existing devDependency or does the source-map lookup manually via the .map file's SourceMappingURL comment.
</threat_model>

<verification>
1. Task 1: `npm run build` exit 0 + dist has fresh AppShell + evidence-file scaffold present.
2. Task 2: Ashley resume-signal captured verbatim in evidence file.
3. Task 3: `## Confirmed crash site` names ONE of 3 candidates OR records "did not reproduce".
4. Task 4: EXACTLY ONE candidate file diffed (or none if no repro); guard has Phase 45 source comment; test exercises the guard; full suite + tsc + build all exit 0.
5. Task 5: Ashley resume-signal captured; plan closes on "confirmed fixed" or "did not reproduce"; escalates on "still crashing" (would require a new plan).
</verification>

<success_criteria>
- Bug #3 CLOSED with one of two outcomes:
  (a) One targeted undefined-guard shipped at the confirmed crash site (one of 3 candidates) with source comment + test + full-suite green. Two other candidates untouched. Ashley re-verifies crash gone in fresh dev build.
  (b) Crash did not reproduce post-Bugs #1+#2 (incidentally fixed by earlier plans). No guard shipped. Evidence file documents the repro-not-reproducing outcome with Ashley's confirmation. No speculative defensive code added anywhere.
- Evidence file `.planning/phases/45-.../45-04-EVIDENCE.md` populated with: fresh dev-build hash, fresh minified stack line (or "did not reproduce" note), source-map trace, confirmed site (or "did not reproduce"), fix action (or "no guard shipped"), post-fix repro result.
- Zero speculative defensive guards added to non-confirmed sites (grep `Phase 45 Bug #3` across `src/` returns at most 1 non-comment hit).
- Full-suite `npx vitest run`, `npx tsc --noEmit`, `npm run build` all exit 0.
</success_criteria>

<output>
Create `.planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-04-SUMMARY.md` when done. Include: Task 1 build evidence (fresh hash, Plans 45-01/02/03 present in dist), Task 2 Ashley signal verbatim, Task 3 source-map trace + confirmed site (or "did not reproduce"), Task 4 fix action (guard-shipped-at-file OR no-guard-shipped rationale) + test file added + git-diff canary showing other 2 candidates untouched, Task 5 Ashley re-verify signal, full-suite exit codes. Reference `.planning/phases/45-.../45-04-EVIDENCE.md` for the raw stack + source-map JSON.
</output>
