---
phase: 45-fix-forward-on-phase-43-restore-correct-architecture-for-win
plan: 05
type: execute
wave: 3
depends_on: [45-01, 45-02, 45-03, 45-04]
files_modified: []
autonomous: true
requirements: []
tags: [pretty-view, verification, ship-gate, phase-43-fix-forward, patch-466-prep]

must_haves:
  truths:
    - "Full-suite `npx vitest run` exits 0 with zero failures, zero .todo, zero .skip markers"
    - "`npm run build:backend` exits 0 (backend TS compiles clean per CLAUDE.md rule — tsc --noEmit does NOT catch backend)"
    - "`npm run build` exits 0 (Vite frontend build clean)"
    - "`npx tsc --noEmit` exits 0 (frontend TS clean)"
    - "Zero Phase 43 identifiers remain in src/backend/claude-session/ or src/ui/api/ or src/ui/features/pretty-view/ (grep-verified sweep across the fix-forward surface)"
    - "Sibling canaries (handleIdentityCountBounties + countIdentityBounties) remain present (proof surgery didn't over-reach)"
    - "Bug #1 (backend observation channel starvation): CLOSED per Plan 45-01 SUMMARY (tail restored to -n +1)"
    - "Bug #2 (9px inter-bubble padding lost): CLOSED per Plan 45-03 SUMMARY (paddingBottom: 9 present on bubble wrapper)"
    - "Bug #3 (.replace crash on send): CLOSED per Plan 45-04 SUMMARY + 45-04 EVIDENCE (targeted guard OR did-not-reproduce)"
    - "Ship-time bookkeeping delegated to orchestrator (Tina): docker build + force-recreate + 15-min deadman rollback + coord-room announcements + skynet-patches.md #466 entry with #465 REVERTED marker — this plan does NOT execute the deploy motion (fleet standing directive: subagents don't do deploys)"
  artifacts:
    - path: ".planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-05-SHIP-READINESS.md"
      provides: "Full-suite verification proof + phase-wide grep sweep proof + 3-bugs-closed proof + explicit hand-off note to orchestrator for deploy motion"
  key_links:
    - from: "phase-wide grep sweep"
      to: "zero hits for 10 Phase 43 identifiers across src/"
      via: "final verification before ship"
      pattern: "ZERO hits"
    - from: "full-suite vitest"
      to: "exit 0 with no todo/skip markers"
      via: "fleet standing directive: never leave tests failing"
      pattern: "exit code 0"
---

<objective>
Final verification gate before Ashley ships Phase 45 as patch #466. Runs the full test suite, both build commands, and a phase-wide grep sweep to confirm every Phase 43 identifier is gone from the reverted surface + sibling canaries preserved + all three bugs closed per prior plans' evidence.

This plan does NOT execute the deploy motion. Fleet standing directive (CLAUDE.md + role file): subagents' remit stops at code + commit + tests green; the orchestrator (Tina) does docker build + `docker compose up -d --force-recreate skynet` + 15-min deadman rollback + coord-room BEFORE+AFTER announcements + `skynet-patches.md` #466 entry (with a #465 REVERTED marker per CONTEXT § Deferred).

Purpose: Guarantee the orchestrator hits zero surprises at ship time. If any check fails here, the failure is caught in the same session as the code work — not at deploy time where the 15-minute deadman clock is running against the fleet gateway (see CLAUDE.md § Blast radius: "a bad deploy loses Ashley access to her whole fleet").

Output: `.planning/phases/45-.../45-05-SHIP-READINESS.md` — the ship-readiness manifest the orchestrator reads before running `docker compose up`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-CONTEXT.md
@.planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-PATTERNS.md
@.planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-01-SUMMARY.md
@.planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-02-SUMMARY.md
@.planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-03-SUMMARY.md
@.planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-04-SUMMARY.md
@.planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-04-EVIDENCE.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite verification — vitest, tsc, backend build, frontend build</name>
  <files>
    .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-05-SHIP-READINESS.md
  </files>
  <read_first>
    - .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-01-SUMMARY.md (Plan 45-01 evidence: backend build + backend tests green)
    - .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-02-SUMMARY.md (Plan 45-02 evidence: frontend wire cleanup done)
    - .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-03-SUMMARY.md (Plan 45-03 evidence: PrettyView.tsx surgery + hydration-cap test file green)
    - .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-04-SUMMARY.md (Plan 45-04 evidence: Bug #3 outcome — guard shipped OR resolved incidentally)
    - .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-04-EVIDENCE.md (Plan 45-04 raw evidence)
    - ./CLAUDE.md § Nginx caveat + § Deploy safety + § Blast radius (fleet-standing constraints)
  </read_first>
  <behavior>
    - `npx vitest run` exits 0 across the ENTIRE repo, not just per-directory. Reports positive test count, zero failing, zero skipped, zero `.todo`.
    - `npx tsc --noEmit` exits 0.
    - `npm run build:backend` exits 0.
    - `npm run build` exits 0 (Vite frontend build produces dist/).
    - Post-task: `.planning/phases/45-.../45-05-SHIP-READINESS.md` contains a Verification section with exit codes + test counts for each of the 4 checks + the fresh AppShell hash from the dist/index.html.
  </behavior>
  <action>
    Run four verification commands sequentially, capturing exit codes and any summary output:

    1. `npx vitest run 2>&1 | tail -30` — record full-suite test count and exit code. If any test fails, STOP. Do NOT proceed to grep sweep or ship-readiness manifest until the suite is green. Failures at this stage likely mean Plans 45-01..04 missed a cleanup (e.g., a test file still references a deleted identifier); investigation loops back to the offending plan.

    2. `npx tsc --noEmit 2>&1 | tail -20` — record exit code and any error output.

    3. `npm run build:backend 2>&1 | tail -20` — record exit code. CRITICAL per CLAUDE.md: frontend tsc does NOT catch backend TS errors; this is the only backend compile gate.

    4. `npm run build 2>&1 | tail -20` — record exit code + fresh AppShell hash via `grep -oE 'AppShell-[A-Za-z0-9_-]+\.js' dist/index.html | head -1`.

    Create `.planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-05-SHIP-READINESS.md` with the following template (fill Verification now; leave the other 3 sections as headers-only for Tasks 2/3/4):

    ```
    # Phase 45 Ship Readiness — Plan 45-05

    **Verified date:** <YYYY-MM-DD>
    **Fresh AppShell hash:** <hash from dist/index.html>

    ## Verification

    | Command | Exit code | Summary |
    |---------|-----------|---------|
    | `npx vitest run` | 0 | <N passing / 0 failing / 0 skipped / 0 todo> |
    | `npx tsc --noEmit` | 0 | (no errors) |
    | `npm run build:backend` | 0 | (backend TS compiles clean) |
    | `npm run build` | 0 | (Vite frontend built; dist AppShell = <hash>) |

    ## Phase 43 identifier sweep

    (populated by Task 2)

    ## Three bugs closed

    (populated by Task 3)

    ## Hand-off to orchestrator

    (populated by Task 4)
    ```
  </action>
  <verify>
    <automated>npx vitest run &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build:backend &amp;&amp; npm run build &amp;&amp; test -f .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-05-SHIP-READINESS.md &amp;&amp; grep -q "^## Verification" .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-05-SHIP-READINESS.md</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 with a positive test count and zero failing / zero `.todo` / zero `.skip`.
    - `npx tsc --noEmit` exits 0.
    - `npm run build:backend` exits 0.
    - `npm run build` exits 0 AND `test -f dist/index.html` returns 0.
    - `.planning/phases/45-.../45-05-SHIP-READINESS.md` exists AND contains a `## Verification` table with four rows (all exit code 0).
    - The Verification table records the fresh AppShell hash from dist/index.html.
  </acceptance_criteria>
  <done>Full-suite green across all 4 gates. Ship-readiness manifest scaffold created with Verification section populated.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-wide grep sweep for Phase 43 identifiers + sibling canaries</name>
  <files>
    .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-05-SHIP-READINESS.md
  </files>
  <read_first>
    - The 45-05-SHIP-READINESS.md scaffold from Task 1.
    - .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-PATTERNS.md § "Delete-file over surgical-remove" + § "One-shot backend-write-then-frontend-consume pattern is the SIBLING of what's deleted" (defines the identifier list to sweep for + the canary sibling that MUST remain).
  </read_first>
  <behavior>
    - Ten Phase 43 identifiers grep-verified as ZERO-hits across the reverted surface (src/backend/claude-session/ + src/ui/api/ + src/ui/features/pretty-view/):
      1. `fetch_older`
      2. `historyWindow`
      3. `readSessionFileRange`
      4. `resolveEventIdToLine`
      5. `handleFetchOlder`
      6. `parseHistoryWindow`
      7. `sendFetchOlder`
      8. `isFetchOlderBatchEvent`
      9. `FetchOlderPayload` / `FetchOlderBatchEvent`
      10. `session-file-range`
    - Two canary identifiers grep-verified as POSITIVE hits (proof surgery didn't over-reach):
      1. `handleIdentityCountBounties` in `src/backend/claude-session/claude-session-server.ts` (sibling backend handler pattern the deleted `handleFetchOlder` mimicked)
      2. `countIdentityBounties` in `src/ui/api/claude-session-api.ts` (sibling frontend one-shot helper pattern the deleted `sendFetchOlder` mimicked)
    - Post-task: `## Phase 43 identifier sweep` section in 45-05-SHIP-READINESS.md populated with two tables — zero-hit sweep + canary sweep.
    - Grep scope EXCLUDES `.planning/` (planning docs still reference Phase 43 identifiers by design — those are historical audit records, not live code).
  </behavior>
  <action>
    Run twelve grep commands and record results:

    Zero-hit sweep (all 10 must return 0 across `src/backend/claude-session/`, `src/ui/api/`, and `src/ui/features/pretty-view/`):
    - `grep -rc 'fetch_older' src/backend/claude-session/ src/ui/api/ src/ui/features/pretty-view/`
    - `grep -rc 'historyWindow' src/backend/claude-session/ src/ui/api/ src/ui/features/pretty-view/`
    - `grep -rc 'readSessionFileRange' src/`
    - `grep -rc 'resolveEventIdToLine' src/`
    - `grep -rc 'handleFetchOlder' src/`
    - `grep -rc 'parseHistoryWindow' src/`
    - `grep -rc 'sendFetchOlder' src/`
    - `grep -rc 'isFetchOlderBatchEvent' src/`
    - `grep -rc 'FetchOlderPayload\|FetchOlderBatchEvent' src/`
    - `grep -rc 'session-file-range' src/`

    Canary sweep (both must return positive):
    - `grep -c handleIdentityCountBounties src/backend/claude-session/claude-session-server.ts`
    - `grep -c countIdentityBounties src/ui/api/claude-session-api.ts`

    Append to `.planning/phases/45-.../45-05-SHIP-READINESS.md` under `## Phase 43 identifier sweep`:

    ```
    ## Phase 43 identifier sweep

    ### Zero-hit sweep (all must be 0)

    | Identifier | Hits in src/ | Status |
    |------------|--------------|--------|
    | fetch_older | 0 | PASS |
    | historyWindow | 0 | PASS |
    | readSessionFileRange | 0 | PASS |
    | resolveEventIdToLine | 0 | PASS |
    | handleFetchOlder | 0 | PASS |
    | parseHistoryWindow | 0 | PASS |
    | sendFetchOlder | 0 | PASS |
    | isFetchOlderBatchEvent | 0 | PASS |
    | FetchOlderPayload / FetchOlderBatchEvent | 0 | PASS |
    | session-file-range | 0 | PASS |

    ### Canary sweep (both must be positive)

    | Canary | Hits | Status |
    |--------|------|--------|
    | handleIdentityCountBounties (backend sibling) | <N> | PASS (>0) |
    | countIdentityBounties (frontend sibling) | <N> | PASS (>0) |
    ```

    If ANY zero-hit-sweep row is non-zero: STOP. Do NOT declare ship-readiness. Loop back to the offending plan's SUMMARY.md and identify the missed cleanup. This gate is what protects the ship from a half-reverted state.

    If either canary is zero: STOP. Something in Plans 45-01 or 45-02 over-reached and deleted the sibling handler; investigation is required before ship.
  </action>
  <verify>
    <automated>ZH=$(grep -rE 'fetch_older|historyWindow|readSessionFileRange|resolveEventIdToLine|handleFetchOlder|parseHistoryWindow|sendFetchOlder|isFetchOlderBatchEvent|FetchOlderPayload|FetchOlderBatchEvent|session-file-range' src/backend/claude-session/ src/ui/api/ src/ui/features/pretty-view/ 2>/dev/null | wc -l); C1=$(grep -c handleIdentityCountBounties src/backend/claude-session/claude-session-server.ts); C2=$(grep -c countIdentityBounties src/ui/api/claude-session-api.ts); test "$ZH" -eq 0 &amp;&amp; test "$C1" -gt 0 &amp;&amp; test "$C2" -gt 0 &amp;&amp; grep -q "^## Phase 43 identifier sweep" .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-05-SHIP-READINESS.md</automated>
  </verify>
  <acceptance_criteria>
    - Combined zero-hit sweep across the 10 identifiers over `src/backend/claude-session/`, `src/ui/api/`, `src/ui/features/pretty-view/` returns ZERO lines.
    - `grep -c handleIdentityCountBounties src/backend/claude-session/claude-session-server.ts` returns a positive integer.
    - `grep -c countIdentityBounties src/ui/api/claude-session-api.ts` returns a positive integer.
    - `.planning/phases/45-.../45-05-SHIP-READINESS.md` contains a `## Phase 43 identifier sweep` section with the two tables above, each row marked PASS.
  </acceptance_criteria>
  <done>Zero-hit sweep clean across the 10 identifiers; canary sweep positive for both sibling patterns. Ship-readiness manifest updated with grep-sweep tables.</done>
</task>

<task type="auto">
  <name>Task 3: Three-bugs-closed proof from prior plan SUMMARYs</name>
  <files>
    .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-05-SHIP-READINESS.md
  </files>
  <read_first>
    - .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-01-SUMMARY.md (Bug #1 backend half)
    - .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-02-SUMMARY.md (Bug #1 wire half)
    - .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-03-SUMMARY.md (Bug #1 client half + Bug #2 padding)
    - .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-04-SUMMARY.md + 45-04-EVIDENCE.md (Bug #3 outcome)
    - .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-CONTEXT.md § "Bugs to close" (the three UAT bugs to close)
  </read_first>
  <behavior>
    - Post-task `## Three bugs closed` section in 45-05-SHIP-READINESS.md contains a table with one row per bug, each pointing to the SUMMARY and specific evidence that closes it.
    - Bug #1 closure evidence spans THREE plans (backend + wire + client) — cite all three.
    - Bug #2 closure evidence points to Plan 45-03 Task 1's `paddingBottom: 9` line + inline source comment.
    - Bug #3 closure evidence points to Plan 45-04's outcome — either the guarded file+line OR the did-not-reproduce record in 45-04-EVIDENCE.md.
    - If ANY bug lacks closure evidence in the SUMMARYs, STOP — bounty cannot close, ship cannot proceed.
  </behavior>
  <action>
    Read each SUMMARY and 45-04-EVIDENCE.md. Extract the specific evidence line per bug:

    - **Bug #1 (backend tail -F -n 50 starves observation channel):** Cross-reference three SUMMARYs. From 45-01-SUMMARY.md: byte-shape equality with pre-P43 `f60514b5~1` on session-file-tail.ts + zero grep hits for `fetch_older`, `historyWindow`, etc. in backend + `npm run build:backend` exit 0. From 45-02-SUMMARY.md: `openClaudeSessionSocket()` zero-arg + zero wire-type hits. From 45-03-SUMMARY.md: client `openClaudeSessionSocket()` zero-arg call + `appendDedupWithCap` preserved for client-side cap + 8-test hydration-cap test file green. Together: the backend now emits `-n +1` (unstarved observation), the client caps drop-oldest without any fetch_older mechanism.

    - **Bug #2 (9px inter-bubble padding lost):** From 45-03-SUMMARY.md: grep count ≥ 1 for `paddingBottom: 9` in PrettyView.tsx, present on the bubble wrapper `<div key={m.eventId} data-pv-bubble ...>` in the plain-DOM `.map`, with an inline source comment citing Ashley's verbatim quote. Value exactly 9 (not 8, not 10). Padding not margin.

    - **Bug #3 (.replace TypeError on send):** From 45-04-SUMMARY.md + 45-04-EVIDENCE.md: either "Guard shipped at <file>:<line>" with post-guard repro-confirmed-fixed by Ashley in Task 5, OR "Did not reproduce — see 45-04-EVIDENCE.md" with Ashley's confirmation.

    Append to `.planning/phases/45-.../45-05-SHIP-READINESS.md` under `## Three bugs closed`:

    ```
    ## Three bugs closed

    | Bug | Closure evidence | Source |
    |-----|------------------|--------|
    | #1 backend observation channel starved | Backend `tail -F -n +1` byte-shape-equality with pre-P43; frontend `openClaudeSessionSocket()` zero-arg; client-side cap in appendDedupWithCap enforces WORKING_SET_CAP=150 during hydration+live-tail with no fetch_older mechanism | 45-01-SUMMARY.md + 45-02-SUMMARY.md + 45-03-SUMMARY.md |
    | #2 9px inter-bubble padding lost | Inline `style={{ paddingBottom: 9 }}` on bubble wrapper at PrettyView.tsx `<div key={m.eventId} data-pv-bubble ...>`, verified via `grep -c 'paddingBottom: 9' src/ui/features/pretty-view/PrettyView.tsx` ≥ 1 and absence of 8/10 variants | 45-03-SUMMARY.md Task 1 § "Part (c)" |
    | #3 .replace crash on send | <One of: "Targeted guard at <file>:<line> with Phase 45 source comment + post-guard Ashley re-verify confirmed fixed" OR "Did not reproduce post-Bugs-#1+#2 (evidence in 45-04-EVIDENCE.md); no guard shipped"> | 45-04-SUMMARY.md + 45-04-EVIDENCE.md |
    ```

    Fill the bracketed placeholders from the actual SUMMARY content. If any of the three bugs lacks closure evidence: STOP, do NOT declare ship-readiness, and note the missing evidence in the manifest with a "BLOCKING: needs re-run of Plan XX-YY" note for the orchestrator.
  </action>
  <verify>
    <automated>grep -q "^## Three bugs closed" .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-05-SHIP-READINESS.md &amp;&amp; grep -A20 "^## Three bugs closed" .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-05-SHIP-READINESS.md | grep -qE '#1' &amp;&amp; grep -A20 "^## Three bugs closed" .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-05-SHIP-READINESS.md | grep -qE '#2' &amp;&amp; grep -A20 "^## Three bugs closed" .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-05-SHIP-READINESS.md | grep -qE '#3'</automated>
  </verify>
  <acceptance_criteria>
    - `## Three bugs closed` section exists in 45-05-SHIP-READINESS.md.
    - The section contains ONE table row per bug (#1, #2, #3), each with concrete closure evidence pointing to a specific SUMMARY (or EVIDENCE) file + evidence line.
    - Bug #3's row correctly reflects the actual Plan 45-04 outcome (either "Guard shipped at <file>:<line>" or "Did not reproduce"). Do not fabricate.
    - If any bug lacks closure evidence, the section explicitly says "BLOCKING: needs re-run of Plan XX-YY" and the plan halts before Task 4.
  </acceptance_criteria>
  <done>Three-bugs-closed table populated from prior SUMMARYs. Any missing evidence surfaced as a BLOCKING note for the orchestrator instead of silently declaring ship-readiness.</done>
</task>

<task type="auto">
  <name>Task 4: Hand-off note to orchestrator (Tina) — deploy motion is out-of-scope for this plan</name>
  <files>
    .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-05-SHIP-READINESS.md
  </files>
  <read_first>
    - The 45-05-SHIP-READINESS.md at post-Task-3 state (Verification + Phase 43 sweep + Three bugs closed already populated).
    - ./CLAUDE.md § Deploy safety (15-min deadman rollback) + § Nginx caveat (both nginx.conf + nginx-https.conf need matching location blocks for any new backend route — Phase 45 DELETES a route, so this is not applicable, but confirm nothing was added).
    - .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-CONTEXT.md § "Deferred Ideas" (skynet-patches.md #465 REVERTED marker + #466 pointer is ship-time bookkeeping — orchestrator handles).
  </read_first>
  <behavior>
    - Post-task `## Hand-off to orchestrator` section in 45-05-SHIP-READINESS.md contains explicit instructions the orchestrator (Tina) MUST perform for the ship motion, plus explicit NON-actions (things the subagent already handled that the orchestrator should NOT redo).
    - Section reminds orchestrator of the fleet standing directive: BEFORE + AFTER coord-room announcements around any docker build + force-recreate, 15-min deadman rollback timer per CLAUDE.md.
    - Section reminds orchestrator that this is a DELETION of routes (fetch_older + historyWindow), so the CLAUDE.md § Nginx caveat is INVERTED here — no new location block is needed, but the orchestrator should verify no stale `location /claude-session/fetch-older` or similar was accidentally added by Plans 45-01..04 (grep both nginx configs).
    - Section names the exact patch number to use (#466 per ROADMAP entry; collision-check at ship time by grepping `~/.claude/roles/box-maintainer/skynet-patches.md` for #466 before landing).
  </behavior>
  <action>
    Append to `.planning/phases/45-.../45-05-SHIP-READINESS.md` under `## Hand-off to orchestrator`:

    ```
    ## Hand-off to orchestrator

    ### Actions ready for orchestrator (Tina)

    1. **Grep verify nginx configs are unchanged.** Phase 45 DELETED a backend route (fetch_older WS handler). No new location block was added. Run:
       ```
       git diff HEAD~<N> -- docker/nginx.conf docker/nginx-https.conf | wc -l
       ```
       where <N> covers Plans 45-01..04 commits. Expect ZERO changes to both files. If non-zero, investigate — someone added a stale location block that ships bloat.

    2. **Coord-room BEFORE announcement.** Post to box-maintainer coord room per role-file protocol: "Deploying Phase 45 patch #466 — fix-forward on #465 UAT-failed. Reverts backend tail to -n +1 (Bug #1), restores 9px inter-bubble padding (Bug #2), <guard shipped at X:Y | Bug #3 resolved incidentally>. Fresh AppShell hash: <hash>."

    3. **Ship.** `docker compose up -d --force-recreate skynet` under the 15-min deadman rollback timer (per CLAUDE.md § Deploy safety — no exceptions, even with Ashley at the keyboard).

    4. **Coord-room AFTER announcement.** Post: "Phase 45 patch #466 deployed. Deadman rollback armed for 15 min. Waiting for Ashley UAT signal."

    5. **skynet-patches.md bookkeeping.** Add #466 entry to `~/.claude/roles/box-maintainer/skynet-patches.md` with reference to the 3 bugs closed + link to this plan's SUMMARY. Amend the #465 entry with a REVERTED marker pointing at #466 (per CONTEXT § Deferred).

    6. **Ashley UAT.** Ashley walks the 3 UAT scenarios: (a) send a message on a long-session pane — messages appear (Bug #1 verify), (b) inter-bubble spacing is visually 9px (Bug #2 verify), (c) send does not crash (Bug #3 verify — either the guard held OR the crash was already gone).

    7. **Bounty close.** After Ashley's UAT-passed signal + 15-min deadman clears, run `/close replace-pv-virtualization-with-windowed-pagination` (closes bounty of record) AND `/close pretty-view-message-list-virtualization` (closes parent bounty per CONTEXT § Domain).

    ### Non-actions (already done by Plans 45-01..05 subagents)

    - Code changes, test additions, `git commit`s per plan — done in each plan's execute step.
    - Full-suite vitest + tsc + backend build + frontend build — proven green above in § Verification.
    - Phase 43 identifier zero-hit sweep — proven above in § Phase 43 identifier sweep.
    - Bug #1 + #2 + #3 closure evidence — proven above in § Three bugs closed.
    - Fresh AppShell hash in dist/ — recorded above.

    ### Fleet standing directives (must be respected)

    - **git pull --rebase before every push** — HEAD is on `feat/tab-title-from-tmux` per CONTEXT.md; if the orchestrator commits any ship-bookkeeping (skynet-patches.md), rebase first.
    - **Subagents don't do deploys** — this plan intentionally does NOT run `docker compose up`; the orchestrator does.
    - **No worktrees** — all work happens in the main tree.
    - **Never leave tests failing** — proven above via `npx vitest run` exit 0 + zero .todo/.skip.
    ```
  </action>
  <verify>
    <automated>grep -q "^## Hand-off to orchestrator" .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-05-SHIP-READINESS.md &amp;&amp; grep -A80 "^## Hand-off to orchestrator" .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-05-SHIP-READINESS.md | grep -qE '15-min deadman' &amp;&amp; grep -A80 "^## Hand-off to orchestrator" .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-05-SHIP-READINESS.md | grep -qE 'coord[- ]room' &amp;&amp; grep -A80 "^## Hand-off to orchestrator" .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-05-SHIP-READINESS.md | grep -qE 'patch #466|patch-466'</automated>
  </verify>
  <acceptance_criteria>
    - `## Hand-off to orchestrator` section exists in 45-05-SHIP-READINESS.md.
    - Section names the 7 orchestrator actions (nginx-verify, coord-BEFORE, ship, coord-AFTER, skynet-patches.md, Ashley UAT, bounty close).
    - Section names the 5 non-actions (things subagents already did).
    - Section includes the 4 fleet standing directives (rebase, no-subagent-deploys, no-worktrees, tests-green).
    - Section mentions the 15-min deadman rollback + coord-room protocol + patch #466 explicitly.
  </acceptance_criteria>
  <done>Ship-readiness manifest complete with all four sections populated. Orchestrator can read this one file and know exactly what to do next without opening the four sub-SUMMARYs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (n/a) | Plan 45-05 is verification + bookkeeping only. No source code modified. No new trust boundaries introduced. Deploy motion (which crosses the ship boundary) is explicitly out-of-scope for this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-05-01 | Denial of Service | premature ship declaration | mitigate | Full-suite `vitest run` gate in Task 1 catches broken tests before deploy; Task 2 grep sweep catches half-reverted state; Task 3 catches missing bug-closure evidence. If ANY gate fails, the manifest surfaces a BLOCKING note rather than silently declaring ready. |
| T-45-05-02 | Repudiation | orchestrator ship without pre-check | mitigate | Task 4 hand-off note explicitly lists the orchestrator's required actions (nginx-verify, coord-room announcements, deadman timer) so no protocol step is skipped by omission. |
| T-45-05-03 | Tampering | (n/a) | accept | No source modifications; no attacker-controlled inputs in the verification commands (all commands are fixed-shape npm/npx/grep). |

**Net effect:** Two mitigations (both gate-enforcement flavored), one N/A-accept. Plan 45-05 is a bookkeeping + verification gate; the threats it addresses are process-flavored (premature ship, skipped protocol), not code-flavored. No new trust boundaries introduced. Package Legitimacy Gate: no new installs.
</threat_model>

<verification>
1. Task 1 full-suite green: `npx vitest run && npx tsc --noEmit && npm run build:backend && npm run build` all exit 0.
2. Task 2 zero-hit sweep: `grep -rE '<10 identifiers>' src/backend/claude-session/ src/ui/api/ src/ui/features/pretty-view/` returns zero lines.
3. Task 2 canary preserved: both `handleIdentityCountBounties` (backend) and `countIdentityBounties` (frontend) grep-positive.
4. Task 3 three-bugs-closed table populated with concrete evidence pointers per bug.
5. Task 4 hand-off note lists the 7 orchestrator actions + 5 non-actions + 4 fleet standing directives.
6. Ship-readiness manifest fully populated (all 4 sections present).
</verification>

<success_criteria>
- Full-suite `npx vitest run` exits 0 with zero failures / zero `.todo` / zero `.skip` markers.
- `npx tsc --noEmit`, `npm run build:backend`, `npm run build` all exit 0.
- Phase-wide grep sweep confirms zero hits for 10 Phase 43 identifiers across `src/backend/claude-session/`, `src/ui/api/`, `src/ui/features/pretty-view/`.
- Sibling canaries (`handleIdentityCountBounties` + `countIdentityBounties`) grep-positive as proof surgery didn't over-reach.
- All three UAT bugs have closure evidence in prior plan SUMMARYs (Bug #1 spans 45-01/02/03, Bug #2 in 45-03 Task 1 Part (c), Bug #3 in 45-04 + 45-04-EVIDENCE.md).
- Ship-readiness manifest `.planning/phases/45-.../45-05-SHIP-READINESS.md` contains all four sections (Verification, Phase 43 sweep, Three bugs closed, Hand-off to orchestrator).
- Deploy motion NOT executed by this plan (subagents don't do deploys per fleet standing directive; orchestrator Tina picks it up from the hand-off note).
</success_criteria>

<output>
Create `.planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-05-SUMMARY.md` when done. Include: exit codes for all 4 verification commands, grep counts for 10 zero-hit + 2 canary identifiers, three-bugs-closed evidence per bug with SUMMARY pointers, explicit "handed off to orchestrator; deploy motion not executed" note.
</output>
