---
phase: 45-fix-forward-on-phase-43-restore-correct-architecture-for-win
plan: 03
type: execute
wave: 2
depends_on: [45-01, 45-02]
files_modified:
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-view/PrettyView.windowed-pagination.test.tsx
  - src/ui/features/pretty-view/PrettyView.hydration-cap.test.tsx
autonomous: true
requirements: []
tags: [pretty-view, frontend, revert, hydration-cap, padding, phase-43-fix-forward]

must_haves:
  truths:
    - "PrettyView.tsx no longer imports sendFetchOlder / isFetchOlderBatchEvent / FetchOlderPayload from claude-session-api"
    - "PrettyView.tsx no longer contains fireFetchOlder / composedScrollRef / near-top-scroll effect / loading-hint mount / fetch_older_batch case"
    - "PrettyView.tsx PRESERVES appendDedupWithCap<T> helper + WORKING_SET_CAP=150 + all 5 live-append call sites byte-for-byte"
    - "PrettyView.tsx bubble wrapper at the plain-DOM messages.map render has style={{ paddingBottom: 9 }} (verbatim Ashley UAT-locked value)"
    - "PrettyView.tsx calls openClaudeSessionSocket() with ZERO arguments (no opts object)"
    - "PrettyView.windowed-pagination.test.tsx does not exist (deleted); replaced by PrettyView.hydration-cap.test.tsx locking client-side drop-oldest + no-fetch_older behavior"
    - "npx vitest run src/ui/features/pretty-view/ exits 0 with zero failures and zero .todo markers"
    - "npm run build exits 0 (frontend Vite build clean)"
  artifacts:
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "PrettyView with client-side-only drop-oldest cap on messages[], no fetch_older client wiring, 9px paddingBottom on bubble wrapper"
      contains: "appendDedupWithCap"
    - path: "src/ui/features/pretty-view/PrettyView.hydration-cap.test.tsx"
      provides: "Client-side hydration cap + no-fetch_older behavior specs (replacement for deleted windowed-pagination.test.tsx)"
  key_links:
    - from: "PrettyView.tsx WS connect"
      to: "openClaudeSessionSocket()"
      via: "no args, matches Plan 45-02 revert"
      pattern: "openClaudeSessionSocket\\(\\)"
    - from: "PrettyView.tsx setMessages call sites (5 of them)"
      to: "appendDedupWithCap(prev, parsed, WORKING_SET_CAP)"
      via: "drop-oldest enforced client-side during both hydration and live-tail"
      pattern: "appendDedupWithCap"
    - from: "PrettyView.tsx bubble wrapper in messages.map"
      to: "inline paddingBottom: 9 style"
      via: "restored 9px inter-bubble gap (Bug #2 fix)"
      pattern: "paddingBottom: 9"
---

<objective>
Three-part surgery on `src/ui/features/pretty-view/PrettyView.tsx`, plus delete-and-recreate the paired test file:

**Part (a) — DELETE Phase 43 fetch_older client:** Strip imports of `sendFetchOlder` / `isFetchOlderBatchEvent` / `FetchOlderPayload`; delete `INITIAL_WINDOW`, `REFETCH_BATCH_SIZE`, `NEAR_TOP_TRIGGER_PX`, `LOAD_OLDER_DEBOUNCE_MS`, `LOADING_HINT_THRESHOLD_MS` constants; delete `scrollEl`/`reachedBeginningRef`/`fetchInFlightRef`/`loadingOlder`/`loadingHintTimerRef`/`debounceTimerRef` state + refs; delete the `messagesRef` live-mirror (sole consumer is `fireFetchOlder`); delete the `composedScrollRef` useCallback; delete the `fireFetchOlder` useCallback + near-top-scroll effect + cleanup effect; delete the `case "fetch_older_batch":` dispatch; delete the loading-hint mount + the composed-ref binding on the outer scroll container (revert to `ref={scrollRef}` from `useAutoScroll`); delete the `openClaudeSessionSocket({ historyWindow: INITIAL_WINDOW })` opt-in (revert to zero-arg call).

**Part (b) — KEEP client-side drop-oldest cap:** `appendDedupWithCap<T>` at L221-238, `WORKING_SET_CAP = 150` at L109, all 5 `setMessages((prev) => appendDedupWithCap(prev, parsed, WORKING_SET_CAP))` call sites at L1354/L1374/L1380/L1386/L1393 stay byte-for-byte. The 6-line `appendDedup` helper at L213-219 also stays (documented pair per plan 43-07b key-decisions; scope-creep to delete). Update the const-block header comment L94-107 to describe the new architecture (client-side cap only, server sends everything) since the current comment ("backend caps tail -F to -n INITIAL_WINDOW") is a lie post-Plan 45-01 revert.

**Part (c) — ADD 9px paddingBottom to bubble wrapper (Bug #2 fix):** Add `style={{ paddingBottom: 9 }}` to the `<div key={m.eventId} data-pv-bubble data-event-id={m.eventId}>` inside the current `messages.map()` at L2478-2483. Value locked by Ashley UAT quote: *"the margin between the message bubbles before was nine pixels ... so that we didn't have to try to re-derive a good looking version of that."* Padding, not margin. Inline style, not a class.

**Part (d) — DELETE-AND-RECREATE test file:** `PrettyView.windowed-pagination.test.tsx` (888 lines locking behaviors that no longer exist post-revert). Replace with `PrettyView.hydration-cap.test.tsx` (renamed per PATTERNS.md § 10 recommendation) locking client-side cap behavior. New file uses the identical WS-stub + polyfill infrastructure verbatim from `PrettyView.plain-dom.test.tsx` (untouched sibling, per PATTERNS.md § 10 "infrastructure verbatim reuse").

Purpose: Ashley's UAT-locked fix for Bug #1 client half (move cap authority to client) + Bug #2 (9px padding lost during plain-DOM conversion). After this plan, Bug #1 + Bug #2 are closed; Plan 45-04 handles Bug #3 investigation.

Output: PrettyView with pre-Phase-43-shaped WS wiring + client-side-only drop-oldest cap + restored inter-bubble gap. `npm run build` + full-suite vitest green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-CONTEXT.md
@.planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-PATTERNS.md
@.planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-07b-PLAN.md
@.planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-07b-SUMMARY.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete Phase 43 fetch_older client wiring from PrettyView.tsx (Part a + Part b comment update + Part c padding add)</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/PrettyView.tsx L1-30 (imports region — L9-13 has the 5 Phase 43 imports to delete: `sendFetchOlder`, `isFetchOlderBatchEvent`, `type FetchOlderPayload`; the two other imports on those lines — `type ClaudeSessionServerEvent`, `type ConnectToPanePayload` — are pre-Phase-43 and MUST be preserved).
    - src/ui/features/pretty-view/PrettyView.tsx L90-120 (const-block region — L102 REFETCH_BATCH_SIZE, L103 NEAR_TOP_TRIGGER_PX, L104 LOAD_OLDER_DEBOUNCE_MS, L105 LOADING_HINT_THRESHOLD_MS, L108 INITIAL_WINDOW = 50 all delete; L109 WORKING_SET_CAP = 150 STAYS; L94-107 comment block must be rewritten to match post-revert reality).
    - src/ui/features/pretty-view/PrettyView.tsx L200-245 (appendDedup + appendDedupWithCap region — both helpers PRESERVED byte-for-byte per PATTERNS.md § 9 Part (b); also update the L225 comment referring to "fetch_older path re-hydrates" — that path is deleted).
    - src/ui/features/pretty-view/PrettyView.tsx L760-890 (fetch_older refs/state/callbacks/effects region: L783-802 refs + state + composedScrollRef; L804-811 messagesRef live-mirror; L813-849 fireFetchOlder useCallback; L851-887 near-top-scroll effect + cleanup — ALL deleted).
    - src/ui/features/pretty-view/PrettyView.tsx L1240-1260 (WS connect region — L1244-1247 has `openClaudeSessionSocket({ historyWindow: INITIAL_WINDOW })` to revert to `openClaudeSessionSocket()`).
    - src/ui/features/pretty-view/PrettyView.tsx L1345-1400 (live-append call sites — all 5 setMessages((prev) => appendDedupWithCap(...)) at L1354/L1374/L1380/L1386/L1393 PRESERVED byte-for-byte).
    - src/ui/features/pretty-view/PrettyView.tsx L1390-1450 (fetch_older_batch case region — L1396-1442 the entire `case "fetch_older_batch": { ... }` block DELETED).
    - src/ui/features/pretty-view/PrettyView.tsx L2425-2490 (outer scroll container + loading-hint + bubble map region — L2437 revert `ref={composedScrollRef}` to `ref={scrollRef}`; L2430-2436 comment update; L2449-2466 loading-hint mount DELETED; L2478-2483 bubble wrapper — ADD `style={{ paddingBottom: 9 }}`).
    - .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-PATTERNS.md § 9 all three parts (a/b/c) — the surgical map.
    - .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-CONTEXT.md § "Client architecture" (Ashley's verbatim UAT: cap moves to client) and § "9px inter-bubble padding" (verbatim value + medium).
    - Pre-Phase-43 bubble wrapper byte-shape target: `git show 5bc24f49~1:src/ui/features/pretty-view/PrettyView.tsx` L2380-2405 (source-of-truth for `paddingBottom: 9`).
  </read_first>
  <behavior>
    - `grep -c 'sendFetchOlder\|isFetchOlderBatchEvent\|FetchOlderPayload\|fetchInFlightRef\|reachedBeginningRef\|loadingOlder\|fireFetchOlder\|composedScrollRef\|messagesRef\|LOAD_OLDER_DEBOUNCE_MS\|LOADING_HINT_THRESHOLD_MS\|NEAR_TOP_TRIGGER_PX\|REFETCH_BATCH_SIZE\|INITIAL_WINDOW\|fetch_older_batch\|pv-loading-older' src/ui/features/pretty-view/PrettyView.tsx` returns 0.
    - `grep -c 'openClaudeSessionSocket()' src/ui/features/pretty-view/PrettyView.tsx` returns exactly 1 (the WS connect line).
    - `grep -c 'openClaudeSessionSocket({' src/ui/features/pretty-view/PrettyView.tsx` returns 0 (no opts object).
    - `grep -c 'appendDedupWithCap' src/ui/features/pretty-view/PrettyView.tsx` returns AT LEAST 6 (1 helper definition + 5 call sites).
    - `grep -c 'WORKING_SET_CAP' src/ui/features/pretty-view/PrettyView.tsx` returns at least 6 (1 const decl + 5 usages).
    - `grep -c 'paddingBottom: 9' src/ui/features/pretty-view/PrettyView.tsx` returns at least 1 (bubble wrapper — the value MUST be exactly 9, not 8, not 10, per Ashley UAT lock).
    - `grep -n 'data-pv-bubble' src/ui/features/pretty-view/PrettyView.tsx` shows the plain-DOM bubble wrapper still present with `paddingBottom: 9` on the same `<div>`.
    - `grep -c 'ref={scrollRef}' src/ui/features/pretty-view/PrettyView.tsx` returns positive (outer scroll container ref reverted from composedScrollRef).
    - `npx tsc --noEmit` exits 0 (frontend compile clean; consumes Plan 45-02 API surface).
    - Post-edit file line count is ~200-300 lines smaller than pre-edit (rough sanity check — 74 lines from fireFetchOlder region + 47 lines from fetch_older_batch case + 18 lines from loading-hint mount + ~30 lines of refs/state/effects/comments = ~170+ lines deleted; the paddingBottom line is +1; net ≥ ~150 lines shrink).
  </behavior>
  <action>
    Apply edits bottom-up (highest line number first) to keep line references stable across edits. Line numbers are current-HEAD per PATTERNS.md § 9.

    **Edit 8 (Part c — ADD paddingBottom on bubble wrapper) — approx L2478-2483:**
    Current shape:
    ```tsx
    {messages.map((m) => (
      <div
        key={m.eventId}
        data-pv-bubble
        data-event-id={m.eventId}
      >
    ```
    Add `style={{ paddingBottom: 9 }}` prop on the `<div>` — insertion point after `data-event-id={m.eventId}` line and before the closing `>`. Add a JSX comment line above the style prop: `{/* Phase 45 Bug #2: 9px inter-bubble padding restore (Ashley UAT-locked value; padding not margin) — see .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-CONTEXT.md § "9px inter-bubble padding" */}`. Do NOT re-derive the number, do NOT use CSS class, do NOT use margin.

    **Edit 7 (Part a — DELETE loading-hint mount) — approx L2449-2466:**
    Delete the entire block starting with the JSX comment `{/* Phase 43 (plan 43-07b): loading-hint element. Silent-when-fast ... */}` and ending after the `{loadingOlder && (…)}` conditional-render block. After deletion, the surrounding JSX flows directly from whatever preceded the comment to whatever followed the conditional-render.

    **Edit 6 (Part a — REVERT outer scroll container ref binding) — approx L2437:**
    Change `ref={composedScrollRef}` back to `ref={scrollRef}` (the ref from `useAutoScroll` — grep the file for `const { scrollRef` to confirm the exact destructured name and use it verbatim). Update the multi-line comment block above (approx L2430-2436) which explains the composed-ref pattern — it becomes a lie post-revert. Replace with a 1-2 line comment: `{/* Outer scroll container. useAutoScroll's scrollRef drives pinned-follow behavior. */}` (or match the pre-Phase-43-07b comment shape if a shorter one existed).

    **Edit 5 (Part a — DELETE fetch_older_batch case) — approx L1396-1442:**
    Delete the entire `case "fetch_older_batch": { … }` block. After deletion, the msg-type switch flows to the next case unaffected.

    **Edit 4 (Part a — REVERT openClaudeSessionSocket call) — approx L1244-1247:**
    Replace the 4-line `openClaudeSessionSocket({ historyWindow: INITIAL_WINDOW })` call + its 3-line "Phase 43 Plan 43-07b" comment with the single line `const ws = openClaudeSessionSocket();`. No opts object. No Phase 43 comment.

    **Edit 3 (Part a — DELETE fireFetchOlder + near-top-scroll effect + cleanup) — approx L813-887:**
    Delete the entire block from `const fireFetchOlder = useCallback((): void => {` through the closing brace of the cleanup effect. Approximately 74 lines. After deletion, the surrounding useCallbacks / useEffects flow through unaffected.

    **Edit 2 (Part a — DELETE refs/state/messagesRef/composedScrollRef) — approx L783-811:**
    Delete: `scrollEl` state + `setScrollEl`, `reachedBeginningRef`, `fetchInFlightRef`, `loadingOlder` + `setLoadingOlder`, `loadingHintTimerRef`, `debounceTimerRef`, the reset-on-pane-change useEffect that mutates `reachedBeginningRef` and `fetchInFlightRef`, `composedScrollRef` useCallback, `messagesRef` + its live-mirror useEffect. Grep-verify before deleting `messagesRef` that its only consumer is `fireFetchOlder` (per PATTERNS.md § 9 Part (a) footnote): `grep -n messagesRef src/ui/features/pretty-view/PrettyView.tsx` should show ONLY L808/L810/L816 — L816 is inside `fireFetchOlder` which is being deleted in Edit 3.

    **Edit 1 (Part a — DELETE constants + rewrite const-block header comment; Part b — leave WORKING_SET_CAP + comment update) — approx L94-113:**
    Delete: L108 `const INITIAL_WINDOW = 50;`, L110 `const REFETCH_BATCH_SIZE = 50;`, L111 `const NEAR_TOP_TRIGGER_PX = 500;`, L112 `const LOAD_OLDER_DEBOUNCE_MS = 250;`, L113 `const LOADING_HINT_THRESHOLD_MS = 150;`.
    Preserve: L109 `const WORKING_SET_CAP = 150;` byte-for-byte.
    Rewrite: the multi-line const-block header comment L94-107 currently describes the Phase 43 architecture (server-cap + client-cap). Post-revert reality is client-cap only. New comment (4-6 lines) should read approximately: `// WORKING_SET_CAP — max messages in DOM at once; drop-oldest fires past this. // Server emits every JSONL line on connect (tail -F -n +1); client caps the // sustained message array to this size to bound memory on long sessions. // Loss of scroll-back-forever is explicit trade-off per Phase 45 UAT — see // .planning/phases/45-*/45-CONTEXT.md § "Client architecture" for rationale.`

    **Edit 0 (Part a — DELETE Phase 43 imports; Part b — leave appendDedup + appendDedupWithCap) — approx L1-30 + L200-245:**
    In the import block L9-13, delete these 3 names: `sendFetchOlder`, `isFetchOlderBatchEvent`, `type FetchOlderPayload`. Preserve `type ClaudeSessionServerEvent` and `type ConnectToPanePayload` — those are pre-Phase-43 imports on the same lines. Post-edit the import list is smaller by 3 entries; the remaining `openClaudeSessionSocket` import + the 2 preserved types stay verbatim.
    In the L200-245 region: leave `appendDedup` (L213-219) and `appendDedupWithCap<T>` (L221-238) byte-for-byte. Update the L225 comment referring to "fetch_older path re-hydrates" — the fetch_older path is gone. Rewrite the comment to describe the current reality: `// Live-append with drop-oldest cap. Enforced during both initial hydration // (as the server drains its full-file emission the client drops-oldest as it // grows past CAP) AND live-tail (post-hydration frames continue to enforce // the same cap so long-lived sessions bound their memory).`

    After all edits, run `grep -nE 'sendFetchOlder|isFetchOlderBatchEvent|FetchOlderPayload|fetchInFlightRef|reachedBeginningRef|loadingOlder|fireFetchOlder|composedScrollRef|messagesRef|LOAD_OLDER_DEBOUNCE_MS|LOADING_HINT_THRESHOLD_MS|NEAR_TOP_TRIGGER_PX|REFETCH_BATCH_SIZE|INITIAL_WINDOW|fetch_older_batch|pv-loading-older' src/ui/features/pretty-view/PrettyView.tsx` — expect ZERO hits. Then `grep -c 'appendDedupWithCap' src/ui/features/pretty-view/PrettyView.tsx` — expect ≥ 6. Then `grep -c 'paddingBottom: 9' src/ui/features/pretty-view/PrettyView.tsx` — expect ≥ 1. Then `npx tsc --noEmit` — expect exit 0.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; grep -qE 'sendFetchOlder|isFetchOlderBatchEvent|FetchOlderPayload|fetchInFlightRef|reachedBeginningRef|loadingOlder|fireFetchOlder|composedScrollRef|messagesRef|LOAD_OLDER_DEBOUNCE_MS|LOADING_HINT_THRESHOLD_MS|NEAR_TOP_TRIGGER_PX|REFETCH_BATCH_SIZE|INITIAL_WINDOW|fetch_older_batch|pv-loading-older' src/ui/features/pretty-view/PrettyView.tsx &amp;&amp; echo FAIL_HAS_HITS || (grep -q 'appendDedupWithCap' src/ui/features/pretty-view/PrettyView.tsx &amp;&amp; grep -q 'paddingBottom: 9' src/ui/features/pretty-view/PrettyView.tsx &amp;&amp; grep -q 'const ws = openClaudeSessionSocket();' src/ui/features/pretty-view/PrettyView.tsx &amp;&amp; echo PASS)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'sendFetchOlder|isFetchOlderBatchEvent|FetchOlderPayload|fetchInFlightRef|reachedBeginningRef|loadingOlder|fireFetchOlder|composedScrollRef|messagesRef|LOAD_OLDER_DEBOUNCE_MS|LOADING_HINT_THRESHOLD_MS|NEAR_TOP_TRIGGER_PX|REFETCH_BATCH_SIZE|INITIAL_WINDOW|fetch_older_batch|pv-loading-older' src/ui/features/pretty-view/PrettyView.tsx` returns 0.
    - `grep -c 'const ws = openClaudeSessionSocket();' src/ui/features/pretty-view/PrettyView.tsx` returns exactly 1.
    - `grep -c 'openClaudeSessionSocket({' src/ui/features/pretty-view/PrettyView.tsx` returns 0.
    - `grep -c 'appendDedupWithCap' src/ui/features/pretty-view/PrettyView.tsx` returns ≥ 6 (1 def + 5 call sites).
    - `grep -c 'const WORKING_SET_CAP = 150;' src/ui/features/pretty-view/PrettyView.tsx` returns exactly 1.
    - `grep -c 'paddingBottom: 9' src/ui/features/pretty-view/PrettyView.tsx` returns ≥ 1 AND `grep -c 'paddingBottom: 8\|paddingBottom: 10\|paddingBottom:8\|paddingBottom:10' src/ui/features/pretty-view/PrettyView.tsx` returns 0 (only the Ashley-locked value present).
    - `grep -B2 -A5 'data-pv-bubble' src/ui/features/pretty-view/PrettyView.tsx | grep -c 'paddingBottom: 9'` returns ≥ 1 (padding is on the bubble wrapper, not somewhere unrelated).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>PrettyView.tsx stripped of all Phase 43 fetch_older client wiring; `appendDedupWithCap` + `WORKING_SET_CAP` + 5 live-append sites preserved; 9px `paddingBottom` restored on bubble wrapper with Ashley-quote source comment; frontend `tsc --noEmit` clean. Test file replacement handled by Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete PrettyView.windowed-pagination.test.tsx and recreate as PrettyView.hydration-cap.test.tsx</name>
  <files>
    src/ui/features/pretty-view/PrettyView.windowed-pagination.test.tsx,
    src/ui/features/pretty-view/PrettyView.hydration-cap.test.tsx
  </files>
  <read_first>
    - src/ui/features/pretty-view/PrettyView.windowed-pagination.test.tsx (current 888 lines — verify all 11 tests reference fetch_older / historyWindow / loading-hint / reachedBeginning — whole file deletion candidate).
    - src/ui/features/pretty-view/PrettyView.plain-dom.test.tsx (full file, 658 lines — verbatim source of WS-stub scaffolding L52-88, mocks L70-102, ResizeObserver polyfill, HTMLElement.prototype.offsetHeight override on [data-pv-bubble], fireMessageBatch helper — copy-paste the infrastructure sections verbatim into the new test file).
    - src/ui/features/pretty-view/PrettyView.aside.test.tsx (widened ResizeObserver polyfill referenced by pattern-mapper as the pattern to lift for offsetHeight overrides).
    - src/ui/features/pretty-view/PrettyView.tsx (post-Task-1 shape — confirms `openClaudeSessionSocket()` accepts no args and no fetch_older code path exists to test).
    - .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-PATTERNS.md § 10 (delete-and-recreate rationale + 8 new spec descriptions + rename-for-git-log-clarity recommendation).
    - .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-CONTEXT.md § "Test strategy" (no skipped or .todo tolerated).
  </read_first>
  <behavior>
    - `PrettyView.windowed-pagination.test.tsx` does not exist post-task.
    - `PrettyView.hydration-cap.test.tsx` exists and contains 8 test cases (Test A through Test H per PATTERNS.md § 10) covering: (A) initial-hydration cap enforces WORKING_SET_CAP=150 after 200 frames arrive; (B) live-append respects the cap after cap reached; (C) cap applies uniformly across all 5 wire-frame types; (D) dedup within cap; (E) `openClaudeSessionSocket` called with zero args (locks Plan 45-02 wire contract); (F) auto-scroll pinned-follow still works after drop-oldest (regression carry-over); (G) no yank when scrolled up (regression carry-over — load-bearing); (H) no fetch_older payload ever sent (scroll-to-top produces zero `ws.send({type:"fetch_older"})` calls).
    - `npx vitest run src/ui/features/pretty-view/PrettyView.hydration-cap.test.tsx` exits 0 with 8 passing tests, 0 failing, 0 skipped, 0 `.todo`.
    - `npx vitest run src/ui/features/pretty-view/` (whole pretty-view dir) exits 0 (Task 1 + Task 2 together: no test file references deleted Phase 43 constructs, no stale mocks, no dangling imports).
    - `PrettyView.plain-dom.test.tsx` remains byte-identical (untouched sibling per PATTERNS.md § 10 preserve-list).
  </behavior>
  <action>
    RED first: run `npx vitest run src/ui/features/pretty-view/PrettyView.windowed-pagination.test.tsx` on the CURRENT (post-Task-1) file. Because Task 1 removed the Phase 43 constructs that this file tests, the 11 tests will crash on unresolved imports / references. Confirm the failure mode (unresolved symbol / TypeError / can't-mount) — this is the signal that the file is obsolete and must be replaced.

    Delete `src/ui/features/pretty-view/PrettyView.windowed-pagination.test.tsx` via `git rm`. Do not preserve any content.

    Create `src/ui/features/pretty-view/PrettyView.hydration-cap.test.tsx` from scratch. Structure:

    Header comment (10-20 lines): describe the file's purpose — "Locks post-Phase-45 client-side hydration cap behavior. Test infrastructure verbatim from PrettyView.plain-dom.test.tsx (WS stub + mocks + ResizeObserver polyfill + HTMLElement.prototype.offsetHeight override). Replaces the deleted PrettyView.windowed-pagination.test.tsx (Phase 43 windowed-pagination behavior that no longer exists post-Phase-45 revert)."

    Imports section: copy verbatim from `PrettyView.plain-dom.test.tsx` L45-100 (React Testing Library imports, vi from vitest, WsStub type + wsStubs array + getCurrentWs helper, all 3 vi.mock calls for @/api/claude-session-api, @/api/compose-drafts-api, @/session-hue etc., useSessionIdentityMock scaffolding). The mock for `@/api/claude-session-api` MUST provide the post-Phase-45 shape: `openClaudeSessionSocket: vi.fn(() => ws)` with NO opts parameter accepted (matches Plan 45-02 revert; if the mock accepted opts, tests would falsely pass against a code path that no longer exists).

    ResizeObserver polyfill + HTMLElement.prototype.offsetHeight override: copy from PrettyView.plain-dom.test.tsx (search for `ResizeObserver` and `offsetHeight` in that file and lift the setup blocks byte-for-byte).

    fireMessageBatch helper: copy from PrettyView.plain-dom.test.tsx or PrettyView.windowed-pagination.test.tsx (existing helper shape: fires N message events through the mocked WS's `onmessage` handler in sequence, awaits act() wrapping).

    Test suite: one `describe("PrettyView — hydration cap (Phase 45)", () => { ... })` block containing 8 `it(...)` cases:

    - **Test A** (initial hydration cap): render PrettyView with an identity-attached session. Await WS.onopen. Fire 200 message frames via fireMessageBatch (unique eventIds 0..199, all `type: "message"`). Assert `container.querySelectorAll("[data-pv-bubble]").length === 150`. Assert first surviving bubble's `data-event-id === "50"`. Assert last bubble's `data-event-id === "199"`. Locks Ashley's UAT-locked client cap.

    - **Test B** (live-append respects cap): after Test A's fill-to-cap, fire ONE more frame (eventId 200). Assert `querySelectorAll("[data-pv-bubble]").length === 150` (still 150). Assert first bubble's `data-event-id === "51"` (oldest shifted forward by 1). Locks sustained cap on live-tail.

    - **Test C** (uniform across frame types): fire 30 `type: "message"` + 40 `type: "image"` + 30 `type: "relay_outbound"` + 30 `type: "relay_inbound"` + 30 `type: "malformed_line"` (160 total, unique eventIds). Assert 150 bubbles remain. Assert the surviving distribution matches drop-oldest math (first 10 message frames dropped from the front). Locks that the cap is type-agnostic.

    - **Test D** (dedup within cap): fire eventId "X" twice back-to-back. Assert exactly ONE bubble with `data-event-id="X"` (appendDedupWithCap dedups; cap unaffected by would-be-duplicates). Locks the dedup gate.

    - **Test E** (openClaudeSessionSocket zero-arg contract): after mount + WS connect, assert `openCalls[0]` was invoked with NO argument (or the mock was called with `undefined`). Locks Plan 45-02 revert byte-for-byte: no opts object ever passed. Assertion shape: `expect(openMock).toHaveBeenCalledTimes(1); expect(openMock.mock.calls[0].length).toBe(0);`.

    - **Test F** (auto-scroll pinned-follow after drop-oldest): fire 200 frames (cap kicks in at 150). Assert the scroll container's `scrollTop + clientHeight >= scrollHeight - 8` (pinned to bottom within slop). Locks that drop-oldest during hydration does not break the pin-to-bottom behavior. Regression carry-over from Phase 43 Test 10.

    - **Test G** (no yank when scrolled up): render, fire 30 frames, simulate user scroll to the top (`scrollEl.scrollTop = 0; fireEvent.scroll(scrollEl)`), then fire 30 more frames. Assert `scrollEl.scrollTop` remains near 0 (within slop) — the arrival of new frames does NOT yank the view back to the bottom. LOAD-BEARING per PATTERNS.md § 10; regression carry-over from Phase 43 Test 11.

    - **Test H** (no fetch_older payload ever): render, fire 50 frames, scroll to top, wait 500ms (in fake-timer mode or real setTimeout), fire 10 more frames. Assert `getCurrentWs().send.mock.calls` contains ZERO calls where the payload includes `"type":"fetch_older"` (grep the serialized send arguments). Locks that the fetch_older client path is truly gone even under scenarios that used to trigger it.

    Every test MUST run under real timers UNLESS specifically needing fake timers (Test H's 500ms wait — use `vi.useFakeTimers()` then `vi.advanceTimersByTime(500)`). Every test MUST clean up its mocks in `afterEach` (`vi.clearAllMocks(); wsStubs.length = 0;`).

    Zero `it.todo(...)` markers, zero `it.skip(...)` markers, zero `.only()` markers. Full-suite `npx vitest run src/ui/features/pretty-view/` must exit 0 after this task.
  </action>
  <verify>
    <automated>test ! -f src/ui/features/pretty-view/PrettyView.windowed-pagination.test.tsx &amp;&amp; test -f src/ui/features/pretty-view/PrettyView.hydration-cap.test.tsx &amp;&amp; npx vitest run src/ui/features/pretty-view/PrettyView.hydration-cap.test.tsx &amp;&amp; ! grep -qE '\.todo\(|\.skip\(|\.only\(' src/ui/features/pretty-view/PrettyView.hydration-cap.test.tsx &amp;&amp; npx vitest run src/ui/features/pretty-view/</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/ui/features/pretty-view/PrettyView.windowed-pagination.test.tsx` returns exit code 1 (file gone).
    - `test -f src/ui/features/pretty-view/PrettyView.hydration-cap.test.tsx` returns exit code 0 (file exists).
    - `grep -cE '^\s*it\(' src/ui/features/pretty-view/PrettyView.hydration-cap.test.tsx` returns 8.
    - `grep -cE '\.todo\(|\.skip\(|\.only\(' src/ui/features/pretty-view/PrettyView.hydration-cap.test.tsx` returns 0.
    - `npx vitest run src/ui/features/pretty-view/PrettyView.hydration-cap.test.tsx` exits 0, reports 8 passing tests.
    - `npx vitest run src/ui/features/pretty-view/` exits 0 (no test in the pretty-view dir fails or is skipped).
    - `diff -q <(git show HEAD:src/ui/features/pretty-view/PrettyView.plain-dom.test.tsx) src/ui/features/pretty-view/PrettyView.plain-dom.test.tsx` returns empty (sibling untouched — canary that Task 2 didn't over-reach).
  </acceptance_criteria>
  <done>Windowed-pagination test file deleted. Hydration-cap test file created with 8 tests (A-H) locking client-side cap + no-fetch_older + zero-arg WS-open + regressions carried over. Full pretty-view test dir green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS message frame → PrettyView.tsx render pipeline | client parses `ClaudeSessionServerEvent` from WS. Deleting fetch_older_batch parser + loading-hint mount reduces surface area. |
| User scroll → client-side effect trigger | previously scroll-to-top fired `sendFetchOlder`. Deletion of `fireFetchOlder` + near-top listener removes this trigger entirely. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-03-01 | Denial of Service | fetch_older client debouncer | mitigate | DELETED — `fireFetchOlder`, near-top-scroll effect, and debounce timer all removed. No client-side amplification vector remains. |
| T-45-03-02 | Information Disclosure | loading-hint mount ("loading older messages…") | mitigate | DELETED — no user-facing signal reveals the deleted fetch_older code path. |
| T-45-03-03 | Tampering | 9px paddingBottom inline style | accept | Inline style with a hardcoded integer literal — no user-controlled data flows into the style value. Ashley-locked constant, not derived from any input. |
| T-45-03-04 | Denial of Service | client-side cap on messages[] | accept | Pre-existing behavior from Phase 43 (Plan 43-07b) preserved byte-for-byte. Cap = 150 sustained; drop-oldest is O(1) amortized via slice+concat. No new attack surface. |

**Net effect:** Threats 01+02 mitigated by DELETION. Threats 03+04 are accepted (pre-existing safe patterns, no attacker-controlled inputs). No new trust boundaries introduced. Package Legitimacy Gate not applicable (no new installs).
</threat_model>

<verification>
1. Task 1 zero-hit sweep on PrettyView.tsx: `grep -cE 'sendFetchOlder|isFetchOlderBatchEvent|FetchOlderPayload|fetchInFlightRef|reachedBeginningRef|loadingOlder|fireFetchOlder|composedScrollRef|messagesRef|LOAD_OLDER_DEBOUNCE_MS|LOADING_HINT_THRESHOLD_MS|NEAR_TOP_TRIGGER_PX|REFETCH_BATCH_SIZE|INITIAL_WINDOW|fetch_older_batch|pv-loading-older' src/ui/features/pretty-view/PrettyView.tsx` returns 0.
2. Task 1 preserve check: `grep -c appendDedupWithCap src/ui/features/pretty-view/PrettyView.tsx` returns ≥ 6.
3. Task 1 zero-arg WS call: `grep -c 'const ws = openClaudeSessionSocket();' src/ui/features/pretty-view/PrettyView.tsx` returns 1.
4. Task 1 padding: `grep -c 'paddingBottom: 9' src/ui/features/pretty-view/PrettyView.tsx` returns ≥ 1.
5. Task 2 file swap: `test ! -f src/ui/features/pretty-view/PrettyView.windowed-pagination.test.tsx && test -f src/ui/features/pretty-view/PrettyView.hydration-cap.test.tsx` returns exit 0.
6. Task 2 test count: `grep -cE '^\s*it\(' src/ui/features/pretty-view/PrettyView.hydration-cap.test.tsx` returns 8.
7. Task 2 no-skips: `grep -cE '\.todo\(|\.skip\(|\.only\(' src/ui/features/pretty-view/PrettyView.hydration-cap.test.tsx` returns 0.
8. Frontend build: `npx tsc --noEmit` exits 0.
9. Full pretty-view test dir: `npx vitest run src/ui/features/pretty-view/` exits 0.
10. Vite build: `npm run build` exits 0.
</verification>

<success_criteria>
- Bug #1 CLIENT half CLOSED: PrettyView.tsx has zero fetch_older client wiring; client-side drop-oldest cap on `messages[]` enforced during both hydration and live-tail via preserved `appendDedupWithCap` + 5 call sites.
- Bug #2 CLOSED: 9px inline paddingBottom restored on the bubble wrapper at the plain-DOM `messages.map` `<div key={m.eventId} data-pv-bubble …>`. Value exactly 9 (Ashley UAT-locked verbatim quote), medium is padding (not margin), inline style (not CSS class).
- `openClaudeSessionSocket()` called with zero args (matches Plan 45-02 revert).
- Windowed-pagination test file deleted; replaced by hydration-cap test file with 8 tests (A-H) covering: initial-cap enforcement, live-cap enforcement, uniform-across-frame-types, dedup, zero-arg WS-open, auto-scroll-pinned-follow regression, no-yank-when-scrolled-up regression, no-fetch_older-under-any-scroll-scenario.
- Zero `.todo` / `.skip` / `.only` markers anywhere in the new test file.
- `PrettyView.plain-dom.test.tsx` byte-identical to pre-Plan-45 state (untouched sibling canary).
- `npx tsc --noEmit`, `npm run build`, and `npx vitest run src/ui/features/pretty-view/` all exit 0.
</success_criteria>

<output>
Create `.planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-03-SUMMARY.md` when done. Include: PrettyView.tsx delete-region evidence (grep zero-hit for 16 identifiers), preserve-region evidence (appendDedupWithCap count ≥ 6, WORKING_SET_CAP intact, paddingBottom: 9 present, ref={scrollRef} restored), test-file swap evidence (old deleted, new created, 8 passing tests), plain-dom test unchanged canary, full vitest + build exit codes.
</output>
