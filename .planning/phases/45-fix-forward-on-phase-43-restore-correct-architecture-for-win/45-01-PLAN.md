---
phase: 45-fix-forward-on-phase-43-restore-correct-architecture-for-win
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/claude-session/session-file-tail.ts
  - src/backend/claude-session/session-file-tail.test.ts
  - src/backend/claude-session/session-file-range.ts
  - src/backend/claude-session/session-file-range.test.ts
  - src/backend/claude-session/claude-session-server.ts
  - src/backend/claude-session/claude-session-server.fetch-older.test.ts
  - src/backend/claude-session/claude-session-server.history-window.test.ts
autonomous: true
requirements: []
tags: [pretty-view, backend, revert, phase-43-fix-forward]

must_haves:
  truths:
    - "Backend WS emits every JSONL line from line 1 on connect (tail -F -n +1) regardless of session length"
    - "Backend has no fetch_older WS handler, no historyWindow handshake parsing, and no session-file-range module"
    - "The observation channel fan-out (layer1-detect / context-pct / plan-pending / backgroundedAgents / backgroundedShells / id-reset) is byte-identical to pre-Phase-43 behavior"
    - "npm run build:backend exits 0"
    - "npx vitest run src/backend/claude-session/ exits 0 with zero failures and zero .todo markers"
  artifacts:
    - path: "src/backend/claude-session/session-file-tail.ts"
      provides: "SSH tail helper with 4-arg signature emitting `tail -F -n +1 <path>` unconditionally"
      contains: "tail -F -n +1"
    - path: "src/backend/claude-session/claude-session-server.ts"
      provides: "WS server without fetch_older handler / parseHistoryWindow / handleFetchOlder / session-file-range imports"
    - path: "src/backend/claude-session/session-file-tail.test.ts"
      provides: "Backcompat tests locking `tail -F -n +1 <path>` command shape + path escaping"
  key_links:
    - from: "src/backend/claude-session/claude-session-server.ts"
      to: "src/backend/claude-session/session-file-tail.ts"
      via: "tailSessionFile(sshConn, path, onLine, onError) — 4-arg only, no 5th param"
      pattern: "tailSessionFile\\(sshConn"
    - from: "grep -R 'fetch_older|historyWindow|readSessionFileRange|resolveEventIdToLine|handleFetchOlder|parseHistoryWindow|session-file-range' src/backend/"
      to: "zero hits"
      via: "delete-verification"
      pattern: "ZERO hits"
---

<objective>
Revert every Phase 43 backend change so the tail stream restores its pre-Phase-43 `tail -F -n +1` full-file emission. Deletes the freestanding `session-file-range.ts` module, its test file, both Phase-43-born backend test files (`claude-session-server.fetch-older.test.ts` + `claude-session-server.history-window.test.ts`), the `initialLines` parameter on `tailSessionFile`, and all four Phase 43 regions inside `claude-session-server.ts` (imports, handleFetchOlder + parseHistoryWindow block, historyWindowParsed binding, both 5-arg tailSessionFile calls, fetch_older msg-switch case). Trims `session-file-tail.test.ts` back to the backcompat + path-escaping-default-branch tests. Result: the OBSERVATION channel (layer1-detect / context-pct / plan-pending / backgroundedAgents/Shells / id-reset fan-out inside `onLine`) receives every JSONL line, unstarved.

Purpose: Ashley's UAT-locked fix for Bug #1 (backend `tail -F -n 50` starved the observation channel — the single tail stream serves both emission and observation, so capping it capped observation too). Reverting to `-n +1` restores byte-for-byte pre-Phase-43 observation behavior; Wave 2 moves the cap authority entirely to the client.

Output: Backend revert commit sequence. Wave 2 (Plan 45-03) can then safely delete client-side fetch_older wiring without a client/backend contract mismatch (Plan 45-02 handles the wire-type cleanup in parallel with this).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-CONTEXT.md
@.planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-PATTERNS.md
@.planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-04-PLAN.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete Phase-43-born standalone modules and test files</name>
  <files>
    src/backend/claude-session/session-file-range.ts,
    src/backend/claude-session/session-file-range.test.ts,
    src/backend/claude-session/claude-session-server.fetch-older.test.ts,
    src/backend/claude-session/claude-session-server.history-window.test.ts
  </files>
  <read_first>
    - src/backend/claude-session/session-file-range.ts (177 lines — Phase-43-born, sole exports: readSessionFileRange, resolveEventIdToLine)
    - src/backend/claude-session/session-file-range.test.ts (336 lines — sole consumer of session-file-range.ts exports outside claude-session-server.ts)
    - src/backend/claude-session/claude-session-server.fetch-older.test.ts (327 lines — tests __handleFetchOlderForTests seam being deleted in Task 3)
    - src/backend/claude-session/claude-session-server.history-window.test.ts (251 lines — tests __parseHistoryWindowForTests seam being deleted in Task 3)
    - .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-PATTERNS.md §§ 2, 3, 5, 6 (whole-file-delete rationale + Phase-43-born commit evidence)
  </read_first>
  <behavior>
    - After deletion: `git grep -n 'session-file-range' src/` returns ZERO hits (planning docs may still reference).
    - After deletion: `git grep -n 'readSessionFileRange\|resolveEventIdToLine' src/` returns ZERO hits.
    - After deletion: `git grep -n '__handleFetchOlderForTests\|__parseHistoryWindowForTests' src/` returns ZERO hits (seam consumers gone; the seam exports themselves die in Task 3).
    - After deletion: `git grep -n 'claude-session-server.fetch-older\|claude-session-server.history-window' src/` returns ZERO hits.
    - `npx vitest run src/backend/claude-session/session-file-range.test.ts` fails with "no test files found" (file does not exist).
  </behavior>
  <action>
    Delete four files entirely — no surgical edits, no content preservation:
    1. `src/backend/claude-session/session-file-range.ts` — Phase-43-born via commit `1a02ef04 feat(43-02)`. Two exports (`readSessionFileRange`, `resolveEventIdToLine`) consumed ONLY by `claude-session-server.ts` (imports L14-17, call sites L817, L859) and this file's own test. Task 3 removes those consumers.
    2. `src/backend/claude-session/session-file-range.test.ts` — 336 lines, sole test consumer of session-file-range.ts. Dies with the module.
    3. `src/backend/claude-session/claude-session-server.fetch-older.test.ts` — 327 lines, 100% coverage of `handleFetchOlder` which Task 3 deletes. Test seam `__handleFetchOlderForTests` also disappears with the handler.
    4. `src/backend/claude-session/claude-session-server.history-window.test.ts` — 251 lines, 100% coverage of `parseHistoryWindow` which Task 3 deletes. Test seam `__parseHistoryWindowForTests` also disappears with the parse helper.

    Use `git rm` (not filesystem rm) so deletions land as tracked changes. Do NOT preserve any content into a "history-only" file; the .planning artifacts and git history are the audit trail.
  </action>
  <verify>
    <automated>test ! -f src/backend/claude-session/session-file-range.ts && test ! -f src/backend/claude-session/session-file-range.test.ts && test ! -f src/backend/claude-session/claude-session-server.fetch-older.test.ts && test ! -f src/backend/claude-session/claude-session-server.history-window.test.ts &amp;&amp; git grep -q 'session-file-range\|readSessionFileRange\|resolveEventIdToLine\|__handleFetchOlderForTests\|__parseHistoryWindowForTests' src/ &amp;&amp; echo FAIL || echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/backend/claude-session/session-file-range.ts` returns 1 (file not present).
    - `test -f src/backend/claude-session/session-file-range.test.ts` returns 1.
    - `test -f src/backend/claude-session/claude-session-server.fetch-older.test.ts` returns 1.
    - `test -f src/backend/claude-session/claude-session-server.history-window.test.ts` returns 1.
    - `git grep -n 'session-file-range\|readSessionFileRange\|resolveEventIdToLine' src/` returns exit code 1 (no matches).
    - `git status --porcelain` shows all four files with `D ` (staged for deletion) status.
  </acceptance_criteria>
  <done>Four Phase-43-born files removed via git rm; grep verification zero-hit; the last consumers (imports + call sites inside claude-session-server.ts) still remain — Task 3 removes those.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Revert session-file-tail.ts to pre-Phase-43 4-arg signature and trim its test file</name>
  <files>
    src/backend/claude-session/session-file-tail.ts,
    src/backend/claude-session/session-file-tail.test.ts
  </files>
  <read_first>
    - src/backend/claude-session/session-file-tail.ts (current 168 lines — reads whole file: header, shellEscape, TailHandle type, function body with Phase 43 additions)
    - src/backend/claude-session/session-file-tail.test.ts (current 177 lines — reads whole file: makeStreamStub, makeConnStub, Tests 1-4)
    - Pre-Phase-43 byte-shape target: `git show f60514b5~1:src/backend/claude-session/session-file-tail.ts` (must actually run this git command inside the task and diff against post-edit HEAD to confirm byte-shape equality for the reverted regions)
    - .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-PATTERNS.md § 1 (revert-target byte-shape + preserve-list) and § 8 (test-seam co-deletion pattern applied to Tests 2-3-4-override-branch)
  </read_first>
  <behavior>
    - Post-edit `session-file-tail.ts` has NO 5th parameter on `tailSessionFile` — signature is exactly `(conn: Client, absolutePath: string, onLine: (line: string) => void, onError: (err: Error) => void): TailHandle`.
    - Post-edit `session-file-tail.ts` command construction is exactly `const command = "tail -F -n +1 " + shellEscape(absolutePath);` (one line, no ternary, no `boundedN` variable, no `initialLines` mention).
    - Post-edit `session-file-tail.ts` has zero occurrences of `initialLines`, `boundedN`, `Phase 43`, `1_000_000` (all Phase 43 additions).
    - Post-edit `session-file-tail.test.ts` has ONE describe block containing exactly TWO `it()` calls: the backcompat test (`tail -F -n +1 <path>`) and the path-escaping default-branch test. Tests 2 (`tail -F -n 50`), Test 3 (invalid initialLines fallback), and Test 4's override branch are gone.
    - `npx vitest run src/backend/claude-session/session-file-tail.test.ts` exits 0 with 2 passing tests, 0 failures, 0 skipped.
    - `git diff f60514b5~1:src/backend/claude-session/session-file-tail.ts HEAD:src/backend/claude-session/session-file-tail.ts` returns EMPTY output (byte-shape equality with pre-Phase-43 file).
  </behavior>
  <action>
    RED first: run `npx vitest run src/backend/claude-session/session-file-tail.test.ts` — expect Tests 2+3+4 to already pass against the current source (they lock Phase 43 behavior that still exists). We will DELETE those test bodies, not make them fail then pass. Rewrite the test file to keep only Test 1 (backcompat, current L66-77) and Test 4's DEFAULT-branch inner block (current L152-158 — the `tail -F -n +1 ${expectedEscaped}` assertion for the tricky-quote path). Delete Test 2 (current L85-113), Test 3 (current L115-143), and Test 4's OVERRIDE-branch inner block (current L160-175 — the `-n 50` assertion). Preserve `makeStreamStub()` + `makeConnStub()` helpers verbatim; the header comment must be rewritten to describe the current-file reality: the test file locks the unconditional `-n +1` shape + path escaping, no reference to `initialLines` or Phase 43.

    GREEN: with test file trimmed and still passing (they're testing what already works), now revert the source:

    Edit `src/backend/claude-session/session-file-tail.ts`:
    1. Remove the `initialLines?: number` parameter and its 9-line comment block (current L40-48). Function signature becomes exactly `export function tailSessionFile(conn: Client, absolutePath: string, onLine: (line: string) => void, onError: (err: Error) => void): TailHandle {`.
    2. Remove the `boundedN` validation block and its 6-line comment (current L89-101).
    3. Replace the current ternary command construction (current L103-106) with the single line `const command = "tail -F -n +1 " + shellEscape(absolutePath);`.
    4. Preserve every other byte: header comment L1-23, `shellEscape` helper L27-29, `TailHandle` type L31, `STDERR_ACCUMULATION_LIMIT_BYTES` const L33, all locals (`stopped`, `stream`, `buffer`, `stderrBuf`, `anyStdout`), the `stop` closure, the `conn.exec` callback body with stdout/stderr handlers.

    Verify byte-shape equality with pre-Phase-43: `git show f60514b5~1:src/backend/claude-session/session-file-tail.ts > /tmp/pre-p43-tail.ts && diff -u /tmp/pre-p43-tail.ts src/backend/claude-session/session-file-tail.ts` — expect empty output. If the diff is non-empty, iterate on the edit until byte-equal.
  </action>
  <verify>
    <automated>npx vitest run src/backend/claude-session/session-file-tail.test.ts &amp;&amp; git show f60514b5~1:src/backend/claude-session/session-file-tail.ts > /tmp/pre-p43-tail.ts &amp;&amp; diff -q /tmp/pre-p43-tail.ts src/backend/claude-session/session-file-tail.ts &amp;&amp; grep -v '^#' src/backend/claude-session/session-file-tail.ts | grep -c 'initialLines\|boundedN\|Phase 43' | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/backend/claude-session/session-file-tail.test.ts` exits 0, reports 2 passing tests, 0 failing, 0 skipped.
    - `diff -q <(git show f60514b5~1:src/backend/claude-session/session-file-tail.ts) src/backend/claude-session/session-file-tail.ts` prints nothing (files are byte-identical).
    - `grep -n 'initialLines\|boundedN\|1_000_000' src/backend/claude-session/session-file-tail.ts` returns exit code 1 (no matches).
    - `grep -n 'tail -F -n +1' src/backend/claude-session/session-file-tail.ts` returns EXACTLY ONE match (the command construction line).
    - `grep -n 'tail -F -n' src/backend/claude-session/session-file-tail.ts` returns exit code 0 with EXACTLY ONE hit (excludes any `-n <number>` branch).
    - `grep -cE "^\\s*it\\(" src/backend/claude-session/session-file-tail.test.ts` returns 2.
  </acceptance_criteria>
  <done>tailSessionFile signature back to 4 args, command back to unconditional `tail -F -n +1 <escaped>`, byte-shape matches pre-P43 commit `f60514b5~1`. Test file trimmed to backcompat + path-escape-default-branch (2 tests), passing green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Revert five discrete Phase 43 regions from claude-session-server.ts</name>
  <files>src/backend/claude-session/claude-session-server.ts</files>
  <read_first>
    - src/backend/claude-session/claude-session-server.ts L1-25 (imports region — L14-17 is the session-file-range import block to delete)
    - src/backend/claude-session/claude-session-server.ts L722-955 (handleFetchOlder + __handleFetchOlderForTests + parseHistoryWindow + __parseHistoryWindowForTests region to delete)
    - src/backend/claude-session/claude-session-server.ts L1909-1930 (historyWindowParsed binding + its 18-line comment block to delete)
    - src/backend/claude-session/claude-session-server.ts L3080-3105 (session-rotation branch's tailSessionFile 5-arg call to reshape to 4-arg)
    - src/backend/claude-session/claude-session-server.ts L4520-4545 (fetch_older msg-switch case to delete)
    - src/backend/claude-session/claude-session-server.ts L5560-5580 (fresh-connect branch's tailSessionFile 5-arg call to reshape to 4-arg)
    - src/backend/claude-session/claude-session-server.ts L5690-5710 (comment-only reference to tailSessionFile — check for stale Phase 43 comment updates needed)
    - src/backend/claude-session/claude-session-server.ts L1620-1710 (JWT-URL-parse pattern parseHistoryWindow mirrored — MUST stay intact, only parseHistoryWindow is deleted)
    - src/backend/claude-session/claude-session-server.ts L560-720 (extant handleIdentityCountBounties handler + seam — the pattern handleFetchOlder mirrored, MUST stay intact byte-for-byte)
    - src/backend/claude-session/claude-session-server.dormant-tail.test.ts (locks the 4-arg tailSessionFile call shape on the dormant path — Task 3 edits MUST preserve this test green)
    - .planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-PATTERNS.md § 4 (5 delete-block regions with exact byte content to remove) and § "Do NOT touch the observation channel" (fan-out invariant preservation rule)
  </read_first>
  <behavior>
    - Post-edit file has ZERO occurrences of: `fetch_older`, `historyWindow`, `readSessionFileRange`, `resolveEventIdToLine`, `handleFetchOlder`, `parseHistoryWindow`, `__handleFetchOlderForTests`, `__parseHistoryWindowForTests`, `historyWindowParsed`, `FETCH_OLDER_MAX_COUNT`, `HISTORY_WINDOW_MAX`, `session-file-range`.
    - Post-edit file has EXACTLY TWO `tailSessionFile(` call sites (down from current 2), both with 4 arguments (`sshConn`, path, `onLine`, `onError`), no 5th `historyWindowParsed` arg.
    - Post-edit file preserves `handleIdentityCountBounties` handler + `__handleIdentityCountBountiesForTests` seam byte-for-byte (canary — that pattern was the sibling handleFetchOlder mimicked; its untouched-ness proves surgery didn't over-reach).
    - Post-edit file preserves the observation-channel fan-out inside the `onLine` handler for both branches (session-rotation ~L3086-3098 and fresh-connect ~L5567-5576): layer1-detect, context-pct, plan-pending, backgroundedAgents/Shells, id-reset call sites remain byte-identical.
    - `npm run build:backend` exits 0 (backend TypeScript compiles — CLAUDE.md rule: `tsc --noEmit` on frontend does NOT catch backend TS errors, `npm run build:backend` is the required check).
    - `npx vitest run src/backend/claude-session/` exits 0 (dormant-tail.test.ts + any remaining backend tests green; the two deleted test files were already removed in Task 1 so they cannot fail).
  </behavior>
  <action>
    Five discrete delete/reshape regions inside `src/backend/claude-session/claude-session-server.ts`. Line numbers below are current-HEAD line numbers per PATTERNS.md § 4 evidence — after each deletion the line numbers below shift, so apply edits from BOTTOM (highest line number) to TOP to keep line references stable.

    Region 5 (delete) — `case "fetch_older":` msg-switch dispatch at approx L4526-4540. Includes the 9-line preceding comment block. After deletion the msg-switch continues to the next `if (msg.type === ...)` branch without a gap.

    Region 4b (reshape) — fresh-connect tailSessionFile call at approx L5567-5576. Current form is a 6-line multi-line call with `historyWindowParsed,` as the 5th arg + a 3-line preceding "Phase 43 Plan 43-04" comment. Reshape to the single line `tailHandle = tailSessionFile(sshConn!, sessionFile, onLine, onError);` — no 5th arg, no Phase 43 comment. Preserve any surrounding non-Phase-43 comments and logging calls untouched.

    Region 4a (reshape) — session-rotation tailSessionFile call at approx L3086-3098. Current form is an 8-line multi-line call with `historyWindowParsed,` as the 5th arg + a 5-line preceding "Phase 43 Plan 43-04" comment. Reshape to `tailHandle = tailSessionFile(sshConn, newSessionFile, onLine, onError);` — no 5th arg, no Phase 43 comment. Preserve any surrounding non-Phase-43 comments untouched.

    Region 3 (delete) — historyWindowParsed binding at approx L1909-1926. Includes the entire 18-line comment block above the `const historyWindowParsed = parseHistoryWindow(req);` binding. Delete both the comment block AND the binding line. Preserve the JWT-URL fallback pattern at L1618-1622 that parseHistoryWindow mirrored — it is pre-Phase-43 and stays intact.

    Region 2 (delete) — handleFetchOlder + seams + parseHistoryWindow + seams at approx L722-954. Delete every line in this range including the 18-line "Phase 43 Plan 43-04" section header comment L722-739, `FETCH_OLDER_MAX_COUNT` const L745, `handleFetchOlder` function L747-917, `__handleFetchOlderForTests` seam L921, second "Phase 43 Plan 43-04" section header L923-934, `HISTORY_WINDOW_MAX` const L935, `parseHistoryWindow` function L937-951, `__parseHistoryWindowForTests` seam L954. After deletion the file continues from whatever was at L722 pre-Phase-43 into whatever was at L955 post-Phase-43 (verify by grepping the surrounding function names to confirm no accidental over-delete).

    Region 1 (delete) — imports at L14-17: the `import { resolveEventIdToLine, readSessionFileRange } from "./session-file-range.js";` block. After deletion the import list continues with whatever import statement follows the closing brace + semicolon.

    Also scan comments L180 (`tailSessionFile runs 'tail -F -n +1'`) and L932 (`caller passes NO initialLines override to tailSessionFile`) — the L180 comment is now accurate again and stays; the L932 comment references initialLines and dies with Region 2. Also update any surviving comment or log message that still references `initialLines`, `historyWindow`, `fetch_older`, or `session-file-range` (grep the file after all deletions and prune stale references).

    After all edits: run `grep -nE 'fetch_older|historyWindow|readSessionFileRange|resolveEventIdToLine|handleFetchOlder|parseHistoryWindow|FETCH_OLDER_MAX_COUNT|HISTORY_WINDOW_MAX|session-file-range|initialLines' src/backend/claude-session/claude-session-server.ts` — expect ZERO hits. Then `grep -nE 'handleIdentityCountBounties|__handleIdentityCountBountiesForTests' src/backend/claude-session/claude-session-server.ts` — expect POSITIVE hits (the sibling pattern MUST remain intact).
  </action>
  <verify>
    <automated>npm run build:backend &amp;&amp; npx vitest run src/backend/claude-session/ &amp;&amp; grep -qE 'fetch_older|historyWindow|readSessionFileRange|resolveEventIdToLine|handleFetchOlder|parseHistoryWindow|FETCH_OLDER_MAX_COUNT|HISTORY_WINDOW_MAX|session-file-range' src/backend/claude-session/claude-session-server.ts &amp;&amp; echo FAIL_HAS_HITS || (grep -q handleIdentityCountBounties src/backend/claude-session/claude-session-server.ts &amp;&amp; echo PASS)</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build:backend` exits 0 (no backend TS errors).
    - `npx vitest run src/backend/claude-session/` exits 0 with zero failing tests and zero `.todo` markers.
    - `grep -cE 'fetch_older|historyWindow|readSessionFileRange|resolveEventIdToLine|handleFetchOlder|parseHistoryWindow|FETCH_OLDER_MAX_COUNT|HISTORY_WINDOW_MAX|session-file-range|__handleFetchOlderForTests|__parseHistoryWindowForTests' src/backend/claude-session/claude-session-server.ts` returns 0.
    - `grep -cE 'tailSessionFile\(' src/backend/claude-session/claude-session-server.ts` returns 4 or fewer (2 for the actual call sites, plus the DI-typedef inside `deps.tailSessionFile:` if present — verify no extra 5-arg leftovers with `grep -A3 'tailSessionFile(' src/backend/claude-session/claude-session-server.ts | grep -c historyWindowParsed | grep -qx 0`).
    - `grep -c handleIdentityCountBounties src/backend/claude-session/claude-session-server.ts` returns a positive integer (canary preserved).
    - `grep -c "tail -F -n +1" src/backend/claude-session/claude-session-server.ts` returns a positive integer (comment at approx L180 preserved).
  </acceptance_criteria>
  <done>Five discrete Phase 43 regions removed from claude-session-server.ts; imports, handler+parse block, historyWindowParsed binding, two 5-arg tailSessionFile calls (reshaped to 4-arg), and fetch_older msg-switch case all deleted. Sibling handleIdentityCountBounties + JWT-URL parse pattern preserved as canaries. Backend build + tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS client → backend claude-session-server | client sends `{ type: "connect_to_pane", ... }` and formerly `{ type: "fetch_older", ... }` — the fetch_older handler + historyWindow URL param deletion REDUCES this surface area. |
| SSH exec channel → target host tail process | backend spawns `tail -F -n +1 <path>` on target — the reverted 4-arg signature restores the fixed unbounded command shape (no caller-injected `-n N`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01-01 | Tampering | WS `fetch_older` handler | mitigate | DELETED — handler no longer exists. No input to validate, no code path to exploit. Deletion is the mitigation. |
| T-45-01-02 | Denial of Service | historyWindow handshake URL param | mitigate | DELETED — parseHistoryWindow no longer reads `?historyWindow=N` off the WS URL. No caller-injected initial-slice bound. |
| T-45-01-03 | Denial of Service | tail command construction | accept | Reverted to unconditional `tail -F -n +1 <escaped-path>` — same bounded surface as pre-Phase-43 (months of production without incident). Path is already `shellEscape`-guarded (unchanged). |
| T-45-01-04 | Information Disclosure | session-file-range one-shot read | mitigate | DELETED — readSessionFileRange no longer exists. No range-read code path to serve arbitrary line ranges from JSONL. |

**Net effect:** All four threats mitigated by DELETION. Phase 45-01 removes surface area rather than adding it; no new trust boundaries introduced. Package Legitimacy Gate not applicable (no new npm/pip/cargo installs; changes are pure deletion + revert).
</threat_model>

<verification>
1. All four files from Task 1 are gone: `for f in src/backend/claude-session/session-file-range.ts src/backend/claude-session/session-file-range.test.ts src/backend/claude-session/claude-session-server.fetch-older.test.ts src/backend/claude-session/claude-session-server.history-window.test.ts; do test ! -f "$f" || echo "STILL EXISTS: $f"; done` prints nothing.
2. Task 2 byte-shape equality: `diff -q <(git show f60514b5~1:src/backend/claude-session/session-file-tail.ts) src/backend/claude-session/session-file-tail.ts` returns empty (byte-identical).
3. Task 3 zero-hit sweep: `grep -rE 'fetch_older|historyWindow|readSessionFileRange|resolveEventIdToLine|handleFetchOlder|parseHistoryWindow|FETCH_OLDER_MAX_COUNT|HISTORY_WINDOW_MAX|session-file-range' src/backend/` returns exit code 1.
4. Sibling canary preserved: `grep -c handleIdentityCountBounties src/backend/claude-session/claude-session-server.ts` returns positive.
5. Backend build: `npm run build:backend` exits 0.
6. Backend test suite: `npx vitest run src/backend/claude-session/` exits 0, no failures, no `.todo`.
</verification>

<success_criteria>
- Backend WS on connect emits every JSONL line via `tail -F -n +1` regardless of session length (behavior identical to pre-Phase-43 months of production).
- Backend has no fetch_older WS handler, no historyWindow handshake consumer, no session-file-range module, no `initialLines` parameter anywhere.
- Observation channel fan-out inside `onLine` (layer1-detect, context-pct-from-jsonl, plan-pending-parser, backgroundedAgents/Shells sets, id-reset detection) is byte-identical to pre-Phase-43 — every downstream derivation continues to see every line.
- `npm run build:backend` exits 0; `npx vitest run src/backend/claude-session/` exits 0 with zero failures and zero `.todo` markers.
- Sibling `handleIdentityCountBounties` handler + `__handleIdentityCountBountiesForTests` seam remain untouched (canary that surgery didn't over-reach).
- Wave 2 (Plan 45-03) can now safely delete the client-side fetch_older wire without a client/backend contract mismatch (Plan 45-02 handles frontend wire types in parallel).
</success_criteria>

<output>
Create `.planning/phases/45-fix-forward-on-phase-43-restore-correct-architecture-for-win/45-01-SUMMARY.md` when done. Include: files deleted (4), files reverted (2 surgical), byte-shape diff evidence for session-file-tail.ts, grep zero-hit evidence for the 8 Phase 43 identifiers, `npm run build:backend` exit code, `npx vitest run src/backend/claude-session/` test count + exit code.
</output>
