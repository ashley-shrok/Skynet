---
phase: 52-convo-list-filter-restyle-popover-add-ready-toggle
plan: 04
type: execute
wave: 3
depends_on: ["52-01", "52-02", "52-03"]
files_modified:
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "A test verifies the popover renders 3 menu-item buttons in Ready → Pinned → Needs desk order with the outlined-square check affordance and inline-SVG path"
    - "A test verifies the popover chrome is applied via inline style on PopoverContent with the exact chrome tokens from CONTEXT.md AND the width:auto override for shadcn w-72 (plan-checker W-1 alignment)"
    - "A test verifies clicking each of the three menu items toggles aria-checked and (for Pinned + Needs desk) preserves the existing predicate behavior"
    - "A test verifies the Ready toggle filters out working rows: readyOnly=on + fixture row seeded with isWorking=true → row hidden"
    - "A test verifies the Ready toggle filters out dormant rows: readyOnly=on + fixture row seeded with isDormant=true → row hidden"
    - "A test verifies the Ready toggle admits idle-not-dormant rows WITH a seeded wire signal: readyOnly=on + fixture row seeded with isWorking=false + isDormant=false → row visible"
    - "A test verifies the Ready toggle's fail-CLOSED default (plan-checker W-3 alignment): readyOnly=on + fixture row with NO working-store seed (rowState undefined) → row HIDDEN"
    - "A test verifies anyFilterOn extends to readyOnly: Ready-toggle-on with Pinned + Needs-desk off → .pv-filter-dot is present"
    - "A test verifies RDP-group rows continue to pass through unfiltered when Ready is on"
    - "A test verifies AND-intersection: Ready-on + Pinned-on rejects a row that is idle-not-dormant but has pinnedCount=0"
  artifacts:
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx"
      provides: "Phase 52 filter-popover coverage: chrome (including width:auto), item order, checkbox affordance, Ready predicate (all 4 branches including fail-CLOSED default), dot extension, RDP pass-through, AND-intersection"
      contains: "Phase 52"
  key_links:
    - from: "Test fixtures for isWorking + isDormant"
      to: "Panel's rowSessionStates map (via mock spies)"
      via: "mockIsWorkingByKey / mockIsDormantByKey / mockWorkingSnapshot module-scoped Maps consumed by extended vi.mock factory"
      pattern: "mockIsDormantByKey"
---

<objective>
Add Panel-level coverage for the Phase 52 filter changes. Tests go into the existing
`PrettyConversationsPanel.test.tsx` under a new `describe` block titled
"Phase 52 — filter popover restyle + Ready toggle". Do NOT modify the existing Phase 26
tests (Test 23, 24, 25, 25b) beyond the minimal adaptation Plan 02 already did to keep
them passing (aria-checked instead of data-state).

Coverage matrix per CONTEXT.md § success_criteria + plan-checker W-3 fail-CLOSED lock-in:
- Chrome (inline style tokens present, INCLUDING width:auto shadcn w-72 override)
- Menu-item shape (3 items, order, roles, testids, SVG path)
- Ready predicate — 4 branches:
  - working (seeded isWorking=true) → hidden
  - dormant (seeded isDormant=true) → hidden
  - idle+not-dormant WITH seed → visible
  - **no seed at all (rowState undefined) → hidden** (fail-CLOSED default per Plan 03's W-3 fix)
- anyFilterOn extension (dot lights on Ready-only)
- RDP pass-through
- AND-intersection with new Ready + old Pinned/Needs-desk

Backend already has ssh-poll-orchestrator (source A + source B) + wire-protocol + working-store coverage from
Plan 01's TDD tasks. This plan does NOT duplicate backend tests — it focuses on the
Panel integration surface.

**Stub-setup baseline (Plan 03's B-2 fix already landed):** The vi.mock factory at
PrettyConversationsPanel.test.tsx:312 already exports the default stubs for
`getSessionWorkingSnapshot: () => new Map()` and `useSessionIsDormant: () => false`
per Plan 03 Task 1 Part A. This plan REPLACES those defaults with spy-based variants
that read from module-scoped Maps (mirroring the existing `mockAiTitleByKey` +
`useSessionAiTitleSpy` pattern at :307-311) so per-test seeding works. See action
step 0 below.

Purpose: freeze the Phase 52 contract as executable behavior specs so future refactors
(and Ashley's UAT) have a mechanical safety net.
Output: A new describe block in PrettyConversationsPanel.test.tsx with 10 tests (was 9 in the prior draft;
+1 for the fail-CLOSED default test per plan-checker W-3 lock-in) + spy-based stub upgrades to the vi.mock factory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-convo-list-filter-restyle-popover-add-ready-toggle/52-CONTEXT.md
@.planning/phases/52-convo-list-filter-restyle-popover-add-ready-toggle/52-01-SUMMARY.md
@.planning/phases/52-convo-list-filter-restyle-popover-add-ready-toggle/52-02-SUMMARY.md
@.planning/phases/52-convo-list-filter-restyle-popover-add-ready-toggle/52-03-SUMMARY.md
@src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add "Phase 52 — filter popover restyle + Ready toggle" describe block to PrettyConversationsPanel.test.tsx with 10 tests + upgrade the vi.mock factory to spy-based per-test seeding</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx (lines 300-340 — CRITICAL: the vi.mock factory Plan 03 Task 1 Part A extended with getSessionWorkingSnapshot + useSessionIsDormant defaults. This plan upgrades those defaults to spy variants. Also lines 1774-1900 — existing Phase 26 filter tests + the fixture setup pattern for identitiesByKey + bountyCounts + setSnapshot; the makeConversationRow + makeHost helpers)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (Plan 02 + Plan 03 output — for exact testids and behavior contracts, particularly the fail-CLOSED predicate `rowState !== undefined && !rowState.isWorking && !rowState.isDormant`)
    - src/ui/state/session-working-store.ts (Plan 01 output — for the exact WorkingRecord shape returned by getSessionWorkingSnapshot: `{ isWorking, lastMessageAt, aiTitle, dormant }`)
    - .planning/phases/52-convo-list-filter-restyle-popover-add-ready-toggle/52-01-SUMMARY.md (for source B publish frame shape — matters for how test P50-4b seeds a dormant-only fixture)
    - .planning/phases/52-convo-list-filter-restyle-popover-add-ready-toggle/52-03-SUMMARY.md (for the exact fail-CLOSED predicate shape and the vi.mock factory current state)
    - .planning/phases/52-convo-list-filter-restyle-popover-add-ready-toggle/52-CONTEXT.md § success_criteria
  </read_first>
  <behavior>
    - New describe block "Phase 52 — filter popover restyle + Ready toggle" appended to the end of PrettyConversationsPanel.test.tsx.
    - 10 tests total per the coverage matrix above (up from 9 in prior draft; +1 for fail-CLOSED default per W-3 lock-in).
    - Tests DO NOT modify or replace any of the existing Phase 26 tests (they remain intact).
    - Working-store seeding strategy: extend the vi.mock factory (Plan 03 Task 1 Part A already added the default stubs) to use module-scoped mutable Maps + spy functions, mirroring the existing `mockAiTitleByKey` + `useSessionAiTitleSpy` pattern at :307-311. Each test seeds the Maps in its own arrange step then clears them in `afterEach`.
  </behavior>
  <action>
    ### Step 0 — Upgrade the vi.mock factory to spy-based per-test seeding (mirrors existing mockAiTitleByKey pattern)

    Locate the module-scoped `mockAiTitleByKey` Map + `useSessionAiTitleSpy` at PrettyConversationsPanel.test.tsx:307-311. Add three sibling declarations ABOVE the `vi.mock("@/state/session-working-store", ...)` block:

    ```
    // Phase 52 Plan 04 — per-test seeding for the Ready toggle test coverage.
    // Mirrors the mockAiTitleByKey pattern at :307. Tests seed these Maps in
    // arrange steps; afterEach clears them.
    let mockIsWorkingByKey: Map<string | null, boolean> = new Map();
    let mockIsDormantByKey: Map<string | null, boolean> = new Map();
    let mockWorkingSnapshot: Map<string, { isWorking: boolean; lastMessageAt: number | null; aiTitle: string | null; dormant: boolean }> = new Map();

    const useSessionIsWorkingSpy = vi.fn((sessionKey: string | null) => {
      if (sessionKey === null) return false;
      return mockIsWorkingByKey.get(sessionKey) ?? false;
    });
    const useSessionIsDormantSpy = vi.fn((sessionKey: string | null) => {
      if (sessionKey === null) return false;
      return mockIsDormantByKey.get(sessionKey) ?? false;
    });
    const getSessionWorkingSnapshotSpy = vi.fn(() => mockWorkingSnapshot as ReadonlyMap<string, { isWorking: boolean; lastMessageAt: number | null; aiTitle: string | null; dormant: boolean }>);
    ```

    Then REPLACE the vi.mock factory that Plan 03 Task 1 Part A landed. Old shape (Plan 03 result):
    ```
    vi.mock("@/state/session-working-store", () => ({
      useSessionIsWorking: () => false,
      useSessionLastMessageAt: () => null,
      getSessionLastMessageAt: () => null,
      subscribeSessionWorkingStore: (_cb: () => void) => () => {},
      useSessionAiTitle: (sessionKey: string | null) => useSessionAiTitleSpy(sessionKey),
      getSessionWorkingSnapshot: () => new Map(),
      useSessionIsDormant: () => false,
    }));
    ```

    New shape (this plan's Step 0 result):
    ```
    vi.mock("@/state/session-working-store", () => ({
      useSessionIsWorking: (sessionKey: string | null) => useSessionIsWorkingSpy(sessionKey),
      useSessionLastMessageAt: () => null,
      getSessionLastMessageAt: () => null,
      subscribeSessionWorkingStore: (_cb: () => void) => () => {},
      useSessionAiTitle: (sessionKey: string | null) => useSessionAiTitleSpy(sessionKey),
      getSessionWorkingSnapshot: () => getSessionWorkingSnapshotSpy(),
      useSessionIsDormant: (sessionKey: string | null) => useSessionIsDormantSpy(sessionKey),
    }));
    ```

    Add `afterEach` in the new describe block that clears all three Maps: `mockIsWorkingByKey.clear(); mockIsDormantByKey.clear(); mockWorkingSnapshot.clear();`. If a suite-wide `afterEach` already exists earlier in the file, extend it; otherwise add a local one inside the new describe.

    NOTE: The `useSessionIsWorking` upgrade to spy-based (from prior `() => false` default) is required for tests P50-4 (seed working=true) — this is a low-risk change because the default (empty Map → returns false) matches the prior behavior for existing tests.

    ### Step 1-10 — Append the new describe block at the END of PrettyConversationsPanel.test.tsx

    1. **Test P50-1 (chrome tokens present on PopoverContent inline style, INCLUDING width:auto override for shadcn w-72):**
       - Render Panel, click filter button to open popover.
       - Query the popover element by testId `pv-filter-toggles-popover`.
       - Read `.getAttribute("style")` (or use `.style` accessors).
       - Assert style contains substrings: `linear-gradient(160deg`, `rgba(20, 21, 32, 0.94)` (with or without space normalization — normalize whitespace before compare), `rgba(255, 240, 215, 0.12)`, `blur(20px)`, `saturate(1.6)`, `#e8e4d8`, `min-width: 200px` (or `min-width:200px` — normalize), `width: auto` (plan-checker W-1 alignment — verifies the shadcn w-72 override landed).
       - Rationale: This locks the chrome tokens against accidental drift AND locks the width-override fix.

    2. **Test P50-2 (menu-item count + order):**
       - Open popover.
       - Query `screen.getAllByRole("menuitemcheckbox")` → assert length 3.
       - Assert order by text content: first item textContent contains "Ready", second contains "Pinned", third contains "Needs desk".

    3. **Test P50-3 (leading outlined-square check affordance with inline SVG):**
       - Open popover.
       - Query the Ready button by testId `pv-filter-toggle-ready`.
       - Query descendant `.pv-filter-check` element → assert present with `data-checked="false"` initially.
       - Query descendant `svg > path` element → assert `d` attribute equals `M3.5 8.5 L7 12 L13 5` (verbatim from CONTEXT.md § decisions § Menu items).
       - Click the button → assert `data-checked="true"` on the `.pv-filter-check` element.
       - Rationale: Locks the inline-SVG check (T-52-02-01 mitigation) and the check path.

    4. **Test P50-4 (Ready toggle filters out working rows):**
       - Seed identitiesByKey with tina-session → identityKey "tina"; bountyCounts optional (Ready is orthogonal to bounty counts).
       - setSnapshot with one grouped row `{ id: "tina-row", targetTmuxSession: "tina-session", host: makeHost("1", "hostA") }`.
       - Seed working-store: `mockWorkingSnapshot.set("1:tina-session", { isWorking: true, lastMessageAt: null, aiTitle: null, dormant: false });`. Also seed `mockIsWorkingByKey.set("1:tina-session", true);` (used by PrettyConversationRowLive's per-row hook).
       - Render Panel → assert row present pre-filter (`[data-conversation-id="tina-row"]` in DOM).
       - Open popover, click `pv-filter-toggle-ready`.
       - Assert row absent (`querySelector('[data-conversation-id="tina-row"]')` returns null).

    5. **Test P50-5 (Ready toggle filters out dormant rows):**
       - Same fixture as P50-4 but seed: `mockWorkingSnapshot.set("1:tina-session", { isWorking: false, lastMessageAt: null, aiTitle: null, dormant: true });` and `mockIsDormantByKey.set("1:tina-session", true);`.
       - Render → row present pre-filter.
       - Open popover, click Ready.
       - Assert row absent.

    6. **Test P50-6 (Ready toggle admits idle-not-dormant rows WITH seeded wire signal):**
       - Same fixture but seed: `mockWorkingSnapshot.set("1:tina-session", { isWorking: false, lastMessageAt: null, aiTitle: null, dormant: false });` (BOTH `isWorking` and `dormant` false, rowState DEFINED — this is the fail-CLOSED-compatible admit case).
       - Render → row present pre-filter.
       - Open popover, click Ready.
       - Assert row still present.

    7. **Test P50-6b (NEW — plan-checker W-3 fail-CLOSED default lock-in):**
       - Same fixture BUT do NOT seed mockWorkingSnapshot at all (leave it empty for the tina-session key). Row is truly rowState-undefined.
       - Render → row present pre-filter (Ready toggle off, so filter is not applied).
       - Open popover, click Ready.
       - Assert row now HIDDEN (fail-CLOSED default: no wire signal → row filtered out even though nothing indicates it's working or dormant).
       - Rationale: Locks the plan-checker W-3 fix. If a future refactor accidentally reverts to fail-OPEN (`!rowState?.isWorking && !rowState?.isDormant`) this test fails immediately.
       - Test-name suffix: include the string "fail-CLOSED" verbatim in the test description so a grep locates it — e.g. `it("P50-6b — Ready toggle fail-CLOSED default: row with no wire signal is HIDDEN when readyOnly is on", ...)`.

    8. **Test P50-7 (anyFilterOn extends to readyOnly — filter-icon dot lights on Ready-only):**
       - Render Panel with empty snapshot.
       - Assert no `.pv-filter-dot` in DOM initially.
       - Open popover, click `pv-filter-toggle-ready`.
       - Assert `.pv-filter-dot` NOW present in DOM.
       - Assert filter button's `data-active="true"`.

    9. **Test P50-8 (RDP-group rows pass through unfiltered when Ready is on):**
       - Fixture: setSnapshot with `rdpGroup: { hostId: "__rdp__", hostName: "rdp-hosts", rows: [makeConversationRow({ id: "rdp-row", targetTmuxSession: null, host: makeHost("2", "rdpBox"), rdpHostRow: true })] }`. Middle + pinned empty.
       - Render → RDP row present.
       - Open popover, click Ready.
       - Assert RDP row STILL present (Ready toggle does NOT touch RDP zone per CONTEXT.md § decisions).

    10. **Test P50-9 (AND-intersection: Ready + Pinned reject a row that satisfies only Ready):**
       - Fixture: tina identity, tina-session, `bountyCounts = new Map([["tina:1", { pinnedCount: 0, needsDeskCount: 0 }]])`.
       - Working-store seed: `mockWorkingSnapshot.set("1:tina-session", { isWorking: false, lastMessageAt: null, aiTitle: null, dormant: false });` (row is READY-with-wire-signal, admissible under Ready-only per fail-CLOSED-compatible case).
       - Row is: idle + not-dormant + rowState-defined (passes Ready) + pinnedCount=0 (fails Pinned).
       - Render → row present.
       - Open popover, click Ready → row still present (Ready alone passes).
       - Click Pinned → row now hidden (AND-intersection: Ready passes but Pinned fails).
       - Assert `.pv-filter-dot` present throughout.

    Do NOT add any tests beyond these 10. Do NOT touch any test outside this new describe block.

    Cross-reference note for anyone editing later: the `mockWorkingSnapshot` values MUST match the shape returned by `getSessionWorkingSnapshot()` post-Plan-01 — `{ isWorking, lastMessageAt, aiTitle, dormant }`. If Plan 01's WorkingRecord grows a new axis in a future phase, this test-file's mock Map value shape must grow in lock-step.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 52 — filter popover restyle + Ready toggle" src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` returns 1.
    - `grep -c "P50-[1-9]\|P50-6b" src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` returns ≥ 10 (one per test tag — 9 numbered + 6b variant).
    - `grep -c "pv-filter-toggle-ready" src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` returns ≥ 4 (used across P50-2, P50-3, P50-4, P50-6, P50-6b, P50-7, P50-9 at minimum).
    - `grep -q "M3.5 8.5 L7 12 L13 5" src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` exits 0 (P50-3 asserts the exact path).
    - `grep -q "menuitemcheckbox" src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` exits 0 (P50-2 asserts role).
    - `grep -c "dormant" src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` returns ≥ 3 (P50-5 seeds dormant true, P50-6 seeds dormant false, mockIsDormantByKey declaration).
    - `grep -q "width: auto\|width:auto" src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` exits 0 (P50-1 asserts the shadcn w-72 override — plan-checker W-1 alignment).
    - `grep -q "fail-CLOSED" src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` exits 0 (P50-6b test description contains this string — plan-checker W-3 alignment).
    - `grep -c "mockIsWorkingByKey\|mockIsDormantByKey\|mockWorkingSnapshot" src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` returns ≥ 6 (three Map declarations + at least three seed calls across P50-4/5/6).
    - `grep -c "useSessionIsWorkingSpy\|useSessionIsDormantSpy\|getSessionWorkingSnapshotSpy" src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` returns ≥ 6 (three spy declarations + three factory references).
    - `npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel 2>&1 | grep -E "Tests.*passed|Tests.*failed"` shows 0 failures.
    - `npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel 2>&1 | grep -oE "[0-9]+ passed"` shows total passing count is (existing count + 10).
    - `npx tsc --noEmit 2>&1 | grep -c "error TS"` returns 0.
    - Existing Phase 26 tests (Test 23, 24, 25, 25b) MUST still pass. The spy upgrade preserves the empty-Map default so behavior is unchanged for tests that don't seed.
    - Full suite: `npx vitest run 2>&1 | grep -E "^Tests\s+"` shows 0 failures across the whole project.
  </acceptance_criteria>
  <done>
    10 Phase 52 tests added to PrettyConversationsPanel.test.tsx (9 original coverage tests + 1 fail-CLOSED default test per W-3 lock-in), all passing. vi.mock factory upgraded from Plan 03's default stubs to spy-based per-test seeding via three module-scoped Maps. Existing Phase 26 tests still passing. Full vitest suite green. tsc clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → Panel component | Trusted test environment; no external inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-04-01 | Repudiation | Test coverage of Phase 52 contract | mitigate | 10 tests exercise every success-criteria bullet from CONTEXT.md § success_criteria PLUS the fail-CLOSED default lock-in from Plan 03's W-3 fix. Future refactors that break any of the 10 fail loudly in vitest before ship. |
| T-52-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs — reuses existing vitest + testing-library setup. Package legitimacy gate N/A. |
</threat_model>

<verification>
- `npx vitest run 2>&1 | tail -10` shows 0 failed tests across the whole project.
- `grep -c "P50-" src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` returns ≥ 10 (test names + describe title mentions).
- `grep -q "fail-CLOSED" src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` exits 0 (W-3 lock-in gate).
- `grep -q "width: auto\|width:auto" src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` exits 0 (W-1 alignment gate).
- `npx tsc --noEmit 2>&1 | grep -c "error TS"` returns 0.
</verification>

<success_criteria>
- 10 tests cover every CONTEXT.md § success_criteria bullet PLUS the fail-CLOSED default from Plan 03's W-3 fix.
- All existing tests still green (Phase 26 filter tests + everything else).
- Full vitest suite passes.
- vi.mock factory upgraded to spy-based per-test seeding via mockIsWorkingByKey / mockIsDormantByKey / mockWorkingSnapshot Maps.
- No new package installs; no new HTTP routes.
</success_criteria>

<output>
Create `.planning/phases/52-convo-list-filter-restyle-popover-add-ready-toggle/52-04-SUMMARY.md` when done. Summary MUST include: exact test names (P50-1 through P50-9 plus P50-6b), which store-seeding strategy was used (spy-based via mockIsWorkingByKey / mockIsDormantByKey / mockWorkingSnapshot — confirmed by including the final vi.mock factory shape), and the final test counts (before/after per suite).
</output>
