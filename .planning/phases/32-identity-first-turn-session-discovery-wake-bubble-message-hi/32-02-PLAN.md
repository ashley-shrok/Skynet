---
phase: 32-identity-first-turn-session-discovery-wake-bubble-message-hi
plan: 02
type: execute
wave: 2
depends_on: [32-01]
files_modified:
  - src/backend/claude-session/claude-session-server.ts
  - src/backend/claude-session/claude-session-server.dormant-tail.test.ts
autonomous: true
requirements: [D-01, D-02, D-05, D-07, D-08, D-09]
must_haves:
  truths:
    - "When the dormant branch at claude-session-server.ts:4675 enters (identity-shape probe = yes, .dormant sentinel present), it ALSO calls discoverIdentitySessionFile(conn, currentTmuxSession) and — if the return is non-null — opens a tailSessionFile(sshConn, discoveredFile, onLine, onError) whose handle is stored in the pre-existing closure-scoped `tailHandle` variable."
    - "The dormant tail uses the SAME onLine + onError closures the active flow uses (defined at claude-session-server.ts:1601 and 2102 respectively) — no new lambda; the tail runs `tail -F -n +1` per session-file-tail.ts:80, so the browser receives the full historical message stream through the existing appendDedup + eventId pipeline (D-08)."
    - "If discoverIdentitySessionFile returns null (brand-new identity, fresh box, no prior sessions), the dormant branch degrades gracefully to today's behavior: dormant frame sent, no tail opened, no messages. No exception thrown; a single sshLogger.info structured log is emitted noting the null-discovery outcome."
    - "The wake→active handoff safely closes the dormant tail BEFORE opening the active tail. Specifically: in the `startActiveFlow` callback at claude-session-server.ts:4728-4743 (the callback passed into __applyDormantPollWithRediscoveryForTests), tailHandle?.stop() + tailHandle = null are called BEFORE startActiveSessionFlow runs — which in turn assigns tailHandle = tailSessionFile(...) at claude-session-server.ts:4634."
    - "No eventId is emitted twice across the wake handoff window — the safe-close ordering above prevents dormant-file + active-file tails from briefly overlapping."
    - "The dormant tail is also stopped on WS close: the pre-existing `teardownPane()` function invoked from `ws.on(\"close\")` at L2484 already calls `tailHandle.stop()` + `tailHandle = null` at L1557-1564. Because the new dormant-tail assignment reuses that same closure-scoped `tailHandle` variable, WS-close cleanup for the dormant tail comes for free — no new code needed in ws.on(\"close\")."
    - "Existing dormant-poll.test.ts tests (A-K, all 11 cases) pass byte-unchanged. New dormant-tail integration coverage is added to a NEW test file `claude-session-server.dormant-tail.test.ts` rather than extending dormant-poll.test.ts, to keep test files at single-responsibility per the existing repo convention (layer1 tests are separate from repoll tests are separate from dormant-poll tests)."
    - "The active-flow discovery at claude-session-server.ts:4634 + startActiveSessionFlow is UNCHANGED — D-09 out-of-scope enforced."
    - "A new `__applyDormantBranchTailOpenForTests` test seam is exported from claude-session-server.ts, alongside the existing `__applyDormantPollTickForTests` and `__applyDormantPollWithRediscoveryForTests` seams (approximately L960-1033). It encapsulates the discovery + tail-open + logging logic in a pure, dependency-injected form so CASE-DT1..DT3 + DT6 are directly assertable without a live WebSocketServer."
  artifacts:
    - path: "src/backend/claude-session/claude-session-server.ts"
      provides: "Dormant branch (L4675-4799) calls discoverIdentitySessionFile + tailSessionFile after emitting the dormant frame + starting dormantPollTimer. Wake-handoff safe-close ordering added in the startActiveFlow callback (L4728-4743). New `__applyDormantBranchTailOpenForTests` seam exported alongside existing dormant seams. File header + inline comments updated."
      contains: "discoverIdentitySessionFile"
    - path: "src/backend/claude-session/claude-session-server.dormant-tail.test.ts"
      provides: "Integration coverage for (a) dormant branch opens a tail on the discovered file + replays messages, (b) null-discovery path preserves today's behavior, (c) wake handoff closes dormant tail before opening active tail, (d) no eventId double-emit across the handoff window, (e) helper-throw fallback, (f) WS-close cleanup path stops the dormant tail."
      min_lines: 220
  key_links:
    - from: "src/backend/claude-session/claude-session-server.ts (dormant branch, L4675-4799)"
      to: "src/backend/claude-session/discover-identity-session-file.ts"
      via: "await discoverIdentitySessionFile(conn, currentTmuxSession!)"
      pattern: "discoverIdentitySessionFile\\("
    - from: "src/backend/claude-session/claude-session-server.ts (dormant branch)"
      to: "src/backend/claude-session/session-file-tail.ts (tailSessionFile)"
      via: "tailHandle = tailSessionFile(sshConn!, discoveredFile, onLine, onError)"
      pattern: "tailSessionFile\\(sshConn"
    - from: "src/backend/claude-session/claude-session-server.ts (startActiveFlow callback L4728-4743)"
      to: "closure-scoped tailHandle variable at L1279"
      via: "tailHandle?.stop(); tailHandle = null; BEFORE startActiveSessionFlow(...)"
      pattern: "tailHandle\\?\\.stop\\(\\)"
    - from: "src/backend/claude-session/claude-session-server.ts (ws.on(\"close\") at L2480)"
      to: "closure-scoped tailHandle stop at L1557-1564 (inside teardownPane)"
      via: "ws.on(\"close\") → teardownPane() → tailHandle.stop() + tailHandle = null"
      pattern: "teardownPane"
---

<objective>
Wire the Wave 1 helper (`discoverIdentitySessionFile`) into the dormant branch of `src/backend/claude-session/claude-session-server.ts` at the exact site identified in 32-CONTEXT.md (L4675-4799), open a tail on the discovered file so the wake-bubble's message list is populated with the identity's historical conversation, and add the safe-close ordering at the wake→active handoff so a dormant tail is never open simultaneously with an active tail on a different file.

This closes Ashley's verbatim complaint from 2026-08-12: *"the bubble looks good, but unfortunately, the rest of the messages that would be in that session are not showing up."*

Purpose: end-to-end delivery of the phase's primary user-visible outcome — a dormant identity pane's wake bubble is now backed by the tail of the conversation Ashley is deciding whether to wake, streamed through the existing message-emission pipeline.

Output: `claude-session-server.ts` gains the dormant-branch wiring + wake-transition safe-close ordering + a new `__applyDormantBranchTailOpenForTests` test seam, plus a new integration test file covering both the happy path (tail opens + messages replay) and the fallback path (null discovery preserves status quo) and the handoff-safety property (no eventId double-emit) and the WS-close-cleanup path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-identity-first-turn-session-discovery-wake-bubble-message-hi/32-CONTEXT.md
@.planning/phases/32-identity-first-turn-session-discovery-wake-bubble-message-hi/32-01-SUMMARY.md
@src/backend/claude-session/discover-identity-session-file.ts
@src/backend/claude-session/dormant-poll.test.ts
@src/backend/claude-session/session-file-tail.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire discoverIdentitySessionFile + dormant tail-open into the dormant branch</name>
  <files>src/backend/claude-session/claude-session-server.ts</files>
  <behavior>
    Edit the dormant branch at `src/backend/claude-session/claude-session-server.ts` starting around L4675 (where `dormantProbeOut.trim() === "yes"` gates dormancy entry). AFTER the block that:
    - seeds closure state (`currentHostId = hostId; currentTmuxSession = tmuxSession; dormantLastEmitted = true;`),
    - emits the `{type:"dormant",dormant:true,wakingSince}` frame,
    - fires `paneStateEmitter.emit("dormant")`,
    - starts the `dormantPollTimer = setInterval(...)` block (L4704-4797),

    AND BEFORE `enteredDormantPoll = true;` (L4798),

    INSERT the new dormant-tail-open sequence:

    1. Import `discoverIdentitySessionFile` at the top of the file (add to the existing `./discover-identity-session-file.js` import line — module doesn't exist yet in claude-session-server.ts, so add a fresh `import { discoverIdentitySessionFile } from "./discover-identity-session-file.js";` grouped with the other `./` imports around L10-15 where `parseSessionLine` and `detectIdReset` are imported). ALSO import `basename` from `node:path` (or use existing `path` import if already present) — needed for W-5 log-payload downgrade in step 3.

    2. Call `const discoveredFile = await discoverIdentitySessionFile(conn, tmuxSession);` (per D-01; use the same `conn` variable already in scope inside the connection callback — NOT `sshConn`, since inside this branch `sshConn` is assigned but `conn` is the exec-scope reference — verify by inspection).

    3. If `discoveredFile !== null`:
       - Emit ONE structured log using ONLY `path.basename(discoveredFile)` in the payload (NOT the absolute path — see T-32-05 mitigation): `sshLogger.info("Dormant tail discovered", { operation: "claude_session_dormant_tail_discovered", userId, sessionId, hostId, tmuxSession, discoveredFileBasename: basename(discoveredFile) });`. The basename is the JSONL's session UUID (e.g. `16da4efa-....jsonl`), which is already discoverable via other session-scoped logs; the encoded project-dir path segment is dropped from log output. Follow the exact structured-logging shape already used at L4681-4686 (dormant-state-entered log) so Phase 31's taxonomy is preserved.
       - Assign `tailHandle = tailSessionFile(sshConn!, discoveredFile, onLine, onError);` — SAME closure-scoped `tailHandle` variable declared at L1279, SAME `onLine` closure defined at L1601, SAME `onError` closure defined at L2102. Do NOT create a new lambda; do NOT wrap `onLine`/`onError`. The dormant tail's line-emission path IS the active-flow line-emission path — that is the whole point (D-08 latency parity).
       - The `tailSessionFile` runs `tail -F -n +1` per `session-file-tail.ts:80`, so the historical stream replays from line 1 through the exact same `appendDedup` + `eventId` pipeline the active flow uses. No new frame types; no wire-protocol changes.

    4. If `discoveredFile === null`:
       - Emit ONE structured log: `sshLogger.info("Dormant tail not discovered — no matching identity session file", { operation: "claude_session_dormant_tail_no_match", userId, sessionId, hostId, tmuxSession });`. Purpose: post-deploy visibility for the fallback path (D-05 cold-start diagnosis; distinguishes "no /id turn in any JSONL" from "SSH failure" without leaking JSONL contents per T-32-02). No path is in this payload — there is nothing to disclose.
       - Do NOT open a tail. Do NOT emit any new WS frames. Dormant branch continues as before (fallback preservation per invariant 1).

    5. The `try { ... } catch { isIdentityShapedCached = false; }` block at L4801-4804 already wraps the entire dormancy-probe block. `discoverIdentitySessionFile` is INSIDE that try block, so any throw from the helper is caught by the same fallback (which per Wave 1 shouldn't happen — the helper returns null on SSH-throw — but the try wrapper is defense-in-depth). If the executor determines the enclosing try must be widened OR narrowed to cover the new call correctly, document the choice in the SUMMARY.

    Update the block comment at L4646-4700 (dormant-poll setup) to reflect the new tail-open sequence. Do NOT modify the L145-150 FALLBACK-01 comment — the fallback rule is UNCHANGED: `inactive` STILL means "no tail from a prior file for the ACTIVE path". This branch is dormant + identity-attributed, which is a DIFFERENT contract from the FALLBACK-01 "inactive-and-stop" rule (add a sentence to the dormant-branch comment noting this distinction so a future reader isn't confused).

    D-09 GUARD: do NOT modify the active-flow discovery at L3898 (`startActiveSessionFlow = ...`) OR at L4634 (`tailHandle = tailSessionFile(sshConn!, sessionFile, ...)` inside startActiveSessionFlow). Live path stays on `discoverClaudeSession`. Only the dormant branch is touched.

    Invariant 3 GUARD: `git status --porcelain src/ui/` MUST be empty after this task's diff. Verify.
  </behavior>
  <action>
    Edit `src/backend/claude-session/claude-session-server.ts` to add the dormant-branch tail-open sequence per the `<behavior>` above. All changes are within the dormant branch (approximately L4675-4805) plus one new import at the top of the file (and a `basename` import from `node:path` if not already imported). Preserve every existing comment, log line, and control-flow branch verbatim except where explicitly noted (block-comment updates at L4646-4700 to reflect the new sequence).

    Do NOT touch `session-file-discovery.ts`, `session-file-tail.ts`, `layer1-detect.ts`, or `discover-identity-session-file.ts`. Do NOT touch any file under `src/ui/`.

    Cite D-01 (byte-pattern match delegated to helper), D-05 (fallback preserved on null), D-08 (latency parity via SAME onLine/onError closures + SAME tail-follow mechanism), and D-09 (active flow untouched) in the block-comment update at the wire site so future readers can trace back to the locked decisions.

    Do NOT embed fenced code inside this action block; the executor writes the exact edit hunks against the live file at edit time.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "claude-session-server\|discover-identity" | head -5; echo "---"; grep -n "discoverIdentitySessionFile" src/backend/claude-session/claude-session-server.ts | head -5; echo "---"; grep -n "discoveredFileBasename\|basename(discoveredFile)" src/backend/claude-session/claude-session-server.ts | head -3; echo "---"; git status --porcelain src/ui/ | head -3</automated>
  </verify>
  <done>
    (1) `import { discoverIdentitySessionFile } from "./discover-identity-session-file.js";` present at top of `claude-session-server.ts`.
    (2) `basename` is imported from `node:path` (or accessible via an existing `path` import) at the top of `claude-session-server.ts`.
    (3) `discoverIdentitySessionFile(` appears at exactly ONE call site in `claude-session-server.ts` (grep count = 1, or 2 if the import line is counted — executor confirms via grep output that only one CALL exists, distinct from the import line).
    (4) The dormant branch at L~4675-4800 has the new call + tail-open sequence + two new structured logs (`claude_session_dormant_tail_discovered` and `claude_session_dormant_tail_no_match`).
    (5) The `claude_session_dormant_tail_discovered` log payload emits `discoveredFileBasename` (NOT `discoveredFile`) — enforced by grep gate in `<verify>`: `grep -c 'discoveredFile:' src/backend/claude-session/claude-session-server.ts | grep -v '^#'` returns 0, `grep -c 'discoveredFileBasename' src/backend/claude-session/claude-session-server.ts` returns ≥ 1.
    (6) `npx tsc --noEmit` reports zero errors.
    (7) `git status --porcelain src/ui/` returns empty (invariant 3).
    (8) `startActiveSessionFlow` at L3898 and the active-flow tail-open at L4634 are byte-unchanged (D-09).
    (9) Full pre-existing test suite still green: `npx vitest run 2>&1 | tail -5` reports 0 failures (dormant-poll.test.ts tests A-K unchanged since the ordering additions in Task 2 are the only ones that touch the wake-handoff path).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add wake-handoff safe-close ordering + new test seam + full integration test coverage</name>
  <files>src/backend/claude-session/claude-session-server.ts, src/backend/claude-session/claude-session-server.dormant-tail.test.ts</files>
  <behavior>
    **Part A — safe-close ordering** (edit `claude-session-server.ts`):

    In the `startActiveFlow` callback passed into `__applyDormantPollWithRediscoveryForTests` at approximately L4728-4743 (the `startActiveFlow: (pid, sessionFile) => { ... }` arrow function), INSERT the safe-close sequence:

    1. Immediately AFTER `if (dormantPollTimer) { clearInterval(dormantPollTimer); dormantPollTimer = null; }` at L4731 and BEFORE `wakeTriggerTs = null;` at L4734:
       - Add `if (tailHandle) { tailHandle.stop(); tailHandle = null; }`
       - Add a structured log: `sshLogger.info("Dormant tail stopped for wake handoff", { operation: "claude_session_dormant_tail_stopped_for_wake", userId, sessionId, hostId: currentHostId, tmuxSession: currentTmuxSession });`.

    The invariant this establishes: `startActiveSessionFlow` at L3898 reassigns `tailHandle = tailSessionFile(sshConn!, sessionFile, onLine, onError)` at L4634. If `tailHandle` is not stopped + nulled BEFORE that reassignment, the old dormant-file tail's `s.on("data")` callback continues to fire lines from the PRIOR file into the SAME `onLine` closure, briefly interleaving with lines from the NEW active-file tail. The `appendDedup` layer downstream uses `eventId` to dedupe, but interleaving lines from two different files may produce duplicate emissions (an eventId from file A followed by a distinct eventId from file B for the same logical position) OR out-of-order emissions (the dormant file's tail's buffered lines arrive AFTER the active file's tail's initial replay begins).

    The safe-close ordering above eliminates this window: `tailHandle.stop()` synchronously flips the `stopped` closure flag inside session-file-tail.ts (see L54-78), so any subsequent `s.on("data")` callback immediately early-returns and never invokes `onLine`. The stream close itself (`s.close()` / `s.signal("KILL")`) is async but idempotent-with-stopped-flag, so the ordering is safe even if the SSH channel takes hundreds of ms to actually tear down.

    Cite the invariant + rationale as an inline comment above the new lines (mirroring the comment density style at L4700-4703 and L4707-4716).

    **Part B — new test seam `__applyDormantBranchTailOpenForTests`** (add to `claude-session-server.ts`):

    The dormant-branch tail-open logic added in Task 1 lives INSIDE a deeply-nested closure chain (`ws.on("connection")` → per-conn state setup → message handler → `if (result.status === "inactive")` → dormancy probe). Every state variable it touches is closure-scoped (`ws`, `sshConn`, `currentHostId`, `currentTmuxSession`, `dormantLastEmitted`, `wakeTriggerTs`, `tailHandle`, `paneStateEmitter`, `stopped`, `dormantPollTimer`, `dormantPollInFlight`, plus `onLine` / `onError` closures at L1601/L2102). This branch is NOT reachable via any pre-existing seam. `vi.mock('./discover-identity-session-file.js')` + `vi.mock('./session-file-tail.js')` alone (Option B from the plan-checker review) will NOT work because there is no entry point that drives the branch through those mocks without spinning up a real WebSocketServer + SSH client stub — an integration surface out of scope for this test file.

    Therefore: introduce a NEW exported test seam `__applyDormantBranchTailOpenForTests` at the BOTTOM of `claude-session-server.ts`, alongside the existing `__applyDormantPollTickForTests` (L984-1033) and `__applyDormantPollWithRediscoveryForTests` (search for it near the same region). Follow the EXACT injectable-helpers shape those seams use: dependency-injected `deps` object + mutable `state` object. Document the seam with the same docblock style used at L973-983 (WHAT does one call do, what mutates in `state`, what side effects fire via `deps`).

    Concrete seam signature (executor implements this verbatim modulo type refinements):

    ```
    // TYPES — near the other __*ForTests type exports (approximately L960)
    export type __DormantBranchTailOpenDepsForTests = {
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      conn: any;                                          // SSH conn used by discoverIdentitySessionFile
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      sshConn: any;                                       // SSH conn used by tailSessionFile
      tmuxSession: string;
      discoverIdentitySessionFile: (
        conn: unknown, identityName: string,
      ) => Promise<string | null>;
      tailSessionFile: (
        conn: unknown, file: string,
        onLine: (line: string) => void,
        onError: (err: Error) => void,
      ) => { stop: () => void };
      onLine: (line: string) => void;
      onError: (err: Error) => void;
      wsSend: (data: string) => void;                     // for parity with __applyDormantPollTickForTests; may be unused
      logger: {
        info: (msg: string, meta: Record<string, unknown>) => void;
      };                                                  // sshLogger stub
    };

    export type __DormantBranchTailOpenStateForTests = {
      setTailHandle: (h: { stop: () => void } | null) => void;
    };

    // FUNCTION — at the bottom of the file, alongside the other __*ForTests exports
    /**
     * Apply the dormant-branch tail-open sequence for tests.
     *
     * Encapsulates the discovery + tail-open + logging logic that lives inline
     * inside the dormant branch at claude-session-server.ts:~L4675 so it is
     * directly assertable without spinning up a WebSocketServer.
     *
     * Called ONCE per dormant-branch entry.
     *
     * - Calls deps.discoverIdentitySessionFile(deps.conn, deps.tmuxSession).
     * - On non-null return: emits deps.logger.info("Dormant tail discovered", ...)
     *   with `discoveredFileBasename: path.basename(discoveredFile)` (T-32-05
     *   mitigation), then calls deps.tailSessionFile(deps.sshConn, discoveredFile,
     *   deps.onLine, deps.onError) and hands the resulting handle to
     *   state.setTailHandle.
     * - On null return: emits deps.logger.info("Dormant tail not discovered — no
     *   matching identity session file", ...); does NOT call setTailHandle.
     * - On discoverIdentitySessionFile throw: emits deps.logger.info(...
     *   dormant_tail_no_match ...) with the same shape as the null-return
     *   branch; does NOT call setTailHandle; does NOT re-throw (fail-safe
     *   mirrors the Wave 1 caller contract).
     */
    export async function __applyDormantBranchTailOpenForTests(
      deps: __DormantBranchTailOpenDepsForTests,
      state: __DormantBranchTailOpenStateForTests,
    ): Promise<void> {
      // implementation extracted from the dormant-branch inline code
      // (lines corresponding to Task 1 steps 2-4)
    }
    ```

    Then, in the production dormant branch (the code added in Task 1 at L~4675), REFACTOR the inline discovery+tail-open steps to call this seam:

    ```
    // Production call site (inside the dormant branch, after paneStateEmitter.emit("dormant"))
    await __applyDormantBranchTailOpenForTests(
      {
        conn,
        sshConn: sshConn!,
        tmuxSession,
        discoverIdentitySessionFile,
        tailSessionFile,
        onLine,
        onError,
        wsSend: (data) => { try { ws.send(data); } catch { /* logged elsewhere */ } },
        logger: {
          info: (msg, meta) => sshLogger.info(msg, { userId, sessionId, hostId, ...meta }),
        },
      },
      {
        setTailHandle: (h) => { tailHandle = h; },
      },
    );
    ```

    This gives tests a direct entry point without instantiating a WebSocketServer, without a real SSH conn, and without the deep closure chain. The seam is the SINGLE production call site (matches D-09's "one call site" invariant).

    **Part C — new integration test file** (create `claude-session-server.dormant-tail.test.ts`):

    Mirror the shape of `dormant-poll.test.ts` (`makeState`, `makeExec`, seam usage via the `__*ForTests` exports, no real WebSocketServer). NEW file — do NOT extend `dormant-poll.test.ts` (single-responsibility per repo convention).

    Import both seams from `./claude-session-server.js`:
    - `__applyDormantBranchTailOpenForTests` (new, from Part B) — drives CASE-DT1, DT2, DT3, DT6.
    - `__applyDormantPollWithRediscoveryForTests` (pre-existing) — drives CASE-DT4, DT5 (wake-handoff path).

    Fixture builders at the top:
    - `makeDeps(overrides?)` — returns a fully-populated `__DormantBranchTailOpenDepsForTests` with `vi.fn()` stubs for `discoverIdentitySessionFile`, `tailSessionFile` (default returns `{ stop: vi.fn() }`), `onLine`, `onError`, `wsSend`, and `logger.info`.
    - `makeTailStateBox()` — returns `{ tailHandle: null as { stop: () => void } | null, setTailHandle(h) { this.tailHandle = h; } }`.

    Test cases required (each REQUIRED as an `it` block):

    - CASE-DT1: **dormant branch opens tail on discovered file** — `discoverIdentitySessionFile` mock returns `"/home/ubuntu/.claude/projects/-home-ubuntu-skynet-tanya/abc-123.jsonl"`. Invoke `__applyDormantBranchTailOpenForTests(deps, state)`. Assert `tailSessionFile` was called with `(deps.sshConn, "/home/ubuntu/.claude/projects/-home-ubuntu-skynet-tanya/abc-123.jsonl", deps.onLine, deps.onError)` and `state.tailHandle` is non-null. Assert `logger.info` was called with `"Dormant tail discovered"` and a payload containing `discoveredFileBasename: "abc-123.jsonl"` (NOT the full absolute path) — asserts the T-32-05 log downgrade.

    - CASE-DT2: **null-discovery preserves today's behavior** — `discoverIdentitySessionFile` stub returns `null`. Invoke seam. Assert `tailSessionFile` was NOT called (`expect(deps.tailSessionFile).not.toHaveBeenCalled()`); assert `state.tailHandle` is still null; assert `logger.info` was called with `"Dormant tail not discovered — no matching identity session file"`.

    - CASE-DT3: **onLine + onError closures are passed through unwrapped** — assert that the `onLine` and `onError` arguments passed to `tailSessionFile` are the SAME function references passed into `deps.onLine` / `deps.onError` (i.e., `expect(deps.tailSessionFile).toHaveBeenCalledWith(expect.anything(), expect.any(String), deps.onLine, deps.onError)` using `Object.is`-tight matchers, NOT structural equality). This is the D-08 latency-parity truth — proves the seam does not re-wrap the closures.

    - CASE-DT4: **wake handoff closes dormant tail before opening active tail** — this test exercises `__applyDormantPollWithRediscoveryForTests` (the pre-existing seam). Set up: (a) create a `stopSpy = vi.fn()` and mount it on a `dormantTailHandle = { stop: stopSpy }`; (b) inject a `startActiveFlow` callback that, when called, first checks `state.tailHandle` and calls `.stop()` + nulls it (mirrors the safe-close ordering added in Part A), THEN calls a second `secondTailSpy = vi.fn()` representing `startActiveSessionFlow`'s tailSessionFile call; (c) drive the seam with a mock `discoverSession` returning `{ status: "active", pid: 123, sessionFile: "/new/session.jsonl" }`. Assert via `vi.fn()` invocation-order tracking: `stopSpy` was called BEFORE `secondTailSpy`. Use `stopSpy.mock.invocationCallOrder[0] < secondTailSpy.mock.invocationCallOrder[0]`.

    - CASE-DT5: **no eventId double-emit across the handoff window** — most-critical test for the safe-close ordering. Set up: create a shared `emittedEventIds: string[] = []`; the dormant tail's `onLine` closure pushes eventIds from lines it processes into that array; simulate a queued dormant-file line that "arrives" AFTER `stopSpy` is called by invoking the captured onLine argument POST-stop. Assert that after `stopSpy` fires, invoking the captured onLine argument STILL runs (since the seam doesn't guard onLine itself — the guard is inside session-file-tail.ts's `stopped` flag) — BUT for the test's purposes, model the guard by having `stopSpy` also flip a local `stopped` flag; assert that no eventId appears twice in `emittedEventIds`. Include a comment in the test explaining WHY this test is load-bearing — it is the sole guardrail against the class of bug described in Task 2 Part A's rationale comment.

    - CASE-DT6: **helper-throw fallback** — `discoverIdentitySessionFile` stub rejects (SSH throw). Invoke seam. Assert (a) no throw propagates (test does not fail with unhandled rejection), (b) `tailSessionFile` was NOT called, (c) `state.tailHandle` is still null, (d) `logger.info` was called with a fallback-log message (the `dormant_tail_no_match` operation code).

    - CASE-DT7 (NEW — addresses W-4 bonus recommendation): **WS-close cleanup path stops the dormant tail** — this test does NOT go through the `__applyDormantBranchTailOpenForTests` seam; instead, it verifies the invariant that the pre-existing `teardownPane()` function stops `tailHandle` if set. Assertion strategy: since `teardownPane` is a closure-scoped function (not exported), the test asserts the STRUCTURAL invariant via a grep-style check that lives inside the test body (`expect(readFileSync('src/backend/claude-session/claude-session-server.ts', 'utf8')).toMatch(/tailHandle\.stop\(\)/)` — proves the pattern is present in the file). Complementary grep gate in `<verify>` (below) asserts the same at the CLI level. Together these enforce: "the closure-scoped tailHandle is stopped somewhere in the file's teardown path" without needing to reach through 3 layers of closure to assert it dynamically.

    Use `vi.mock` sparingly — the seam's dependency injection makes `vi.mock` largely unnecessary for CASE-DT1..DT3 + DT6 (deps object is the seam). For CASE-DT4/DT5, `vi.mock('./session-file-tail.js', () => ({ tailSessionFile: vi.fn(() => ({ stop: vi.fn() })) }))` may still be useful IF the pre-existing `__applyDormantPollWithRediscoveryForTests` seam has an entry point for injecting the tail helper; otherwise use the seam's existing deps injection surface.

    If the existing `__applyDormantPollWithRediscoveryForTests` seam's dep shape does NOT allow injecting the second `tailSessionFile` call needed for CASE-DT4/DT5, the executor may (a) EXTEND that seam's deps to accept an optional `tailSessionFile` injection (backward-compatible — default to the module import), OR (b) simulate the handoff purely at the callback level (the seam invokes `startActiveFlow`; the test's `startActiveFlow` mock does the `stop` + secondary tail call directly, without actually reaching a real tailSessionFile). Option (b) is cleaner and preferred.
  </behavior>
  <action>
    Three edits:

    (1) Modify `src/backend/claude-session/claude-session-server.ts` to add the safe-close ordering + inline comment in the `startActiveFlow` callback per Part A.

    (2) Add the new `__applyDormantBranchTailOpenForTests` seam (+ its exported types) at the bottom of `claude-session-server.ts` per Part B, and refactor the Task-1 production dormant-branch code to call this seam as its single implementation entry point.

    (3) Create `src/backend/claude-session/claude-session-server.dormant-tail.test.ts` per Part C with all 7 cases (DT1-DT7). Use the new seam for CASE-DT1..DT3 + DT6; use the pre-existing `__applyDormantPollWithRediscoveryForTests` seam (or its extended-with-tail-injection form) for CASE-DT4 + DT5; use the file-inspection assertion for CASE-DT7.

    Do NOT modify `dormant-poll.test.ts` — keep its 11 existing tests byte-unchanged; the new test file lives beside it.

    Do NOT touch any file under `src/ui/`.

    Cite D-08 (safe-close ordering rationale) and D-05 (null-return fallback) in the inline comment above the new safe-close block and in the docblock of the new seam + test file.
  </action>
  <verify>
    <automated>npx vitest run src/backend/claude-session/claude-session-server.dormant-tail.test.ts 2>&1 | tail -10; echo "---"; npx vitest run src/backend/claude-session/dormant-poll.test.ts 2>&1 | tail -5; echo "---"; npx vitest run 2>&1 | tail -5; echo "---"; grep -B2 -A30 'ws\.on("close"' src/backend/claude-session/claude-session-server.ts | grep -c 'teardownPane' | head -1; echo "---"; grep -c 'tailHandle\.stop()' src/backend/claude-session/claude-session-server.ts; echo "---"; grep -c '__applyDormantBranchTailOpenForTests' src/backend/claude-session/claude-session-server.ts; echo "---"; git status --porcelain src/ui/ | head -3</automated>
  </verify>
  <done>
    (1) `claude-session-server.ts` has `tailHandle?.stop(); tailHandle = null;` (or equivalent `if (tailHandle) { tailHandle.stop(); tailHandle = null; }`) in the startActiveFlow callback. Combined with the pre-existing `tailHandle.stop()` calls at L1559 (inside teardownPane) and L2330 (transitionToActiveNew), `grep -c 'tailHandle\.stop()' src/backend/claude-session/claude-session-server.ts` returns ≥ 3 (was 2 pre-change; incremented by 1 from the Part A addition).
    (2) `__applyDormantBranchTailOpenForTests` is exported from `claude-session-server.ts` and appears at ≥ 2 grep hits (one export + at least one call site inside the production dormant branch).
    (3) `claude-session-server.dormant-tail.test.ts` exists with 7+ test cases (DT1-DT7) and all pass.
    (4) `dormant-poll.test.ts` still passes byte-unchanged (11/11).
    (5) Full vitest suite green: `npx vitest run 2>&1 | tail -5` reports 0 failures.
    (6) `git status --porcelain src/ui/` returns empty (invariant 3).
    (7) UI-side test regression check: `npx vitest run src/ui/features/pretty-view/DormancyOverlay.test.tsx src/ui/features/pretty-view/PrettyView.test.tsx 2>&1 | tail -5` reports 0 failures (ROADMAP § 6 verbatim).
    (8) TypeScript clean: `npx tsc --noEmit` reports 0 errors.
    (9) WS-close cleanup gate (W-4): `grep -B2 -A30 'ws\.on("close"' src/backend/claude-session/claude-session-server.ts | grep -c 'teardownPane'` returns ≥ 1 (proves the ws-close handler invokes teardownPane, which in turn stops tailHandle per L1557-1564). This gate is TRUE against the pre-change baseline (ws.on("close") already calls teardownPane at L2484) — the invariant being asserted is "the wire-up is present," and it must remain present post-edit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skynet backend → remote host (SSH exec) | New call to `discoverIdentitySessionFile` inside the dormant branch. Already covered in Plan 01's threat model (T-32-01, T-32-02, T-32-03). |
| Dormant tail → WS client | Dormant-file lines flow through the SAME onLine → parseSessionLine → appendDedup → ws.send pipeline the active flow uses. No new wire-protocol surface. |
| Wake handoff window | Brief window where dormant tail closes and active tail opens. Attacker with control of the JSONL file could theoretically inject duplicate-eventId lines during the window; safe-close ordering eliminates the overlap. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-04 | Tampering | Wake-handoff dormant/active tail overlap | mitigate | `tailHandle.stop(); tailHandle = null;` sequenced BEFORE `startActiveSessionFlow` re-assigns tailHandle. Test CASE-DT4 asserts the call order; test CASE-DT5 asserts no eventId is emitted twice across the window. `session-file-tail.ts:54-78`'s synchronous `stopped` flag means post-stop callbacks early-return, so the safety guarantee holds even if the underlying SSH channel close is async. |
| T-32-05 | Information Disclosure | New log emissions | mitigate | Log payload downgrade (W-5 resolution): the `claude_session_dormant_tail_discovered` log emits `discoveredFileBasename: path.basename(discoveredFile)` — the JSONL's session UUID (e.g. `abc-123.jsonl`), which is already discoverable via existing session-scoped logs — NOT the absolute path. The encoded project-dir path segment (e.g. `-home-ubuntu-skynet-tanya`) is dropped from log output, shrinking the disclosure surface to the session UUID alone. The `claude_session_dormant_tail_no_match` and `claude_session_dormant_tail_stopped_for_wake` logs contain NO path payload at all. Do NOT log JSONL first-line contents — that would leak conversation body per T-32-02. |
| T-32-06 | Denial of Service | Dormant tail runs for full duration of dormancy (until wake or WS close) | accept | `tail -F -n +1` on a stable-length file is negligible bandwidth once the initial replay completes. `session-file-tail.ts:126-132` already caps stderr accumulation. No new resource risk vs the existing active-flow tail. Dormant tail is cleaned up on wake handoff (Task 2 Part A) AND on WS close (via the pre-existing `teardownPane()` function invoked from `ws.on("close")` at L2484, which stops `tailHandle` at L1557-1564 — the new dormant-tail assignment reuses the same closure-scoped `tailHandle` variable, so this cleanup path applies unchanged). W-4 grep gate in Task 2 `<verify>` block asserts the `ws.on("close") → teardownPane` wiring remains intact; CASE-DT7 asserts the structural invariant that `tailHandle.stop()` is present in the file. |
| T-32-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies. Both tasks reuse existing imports (`discoverIdentitySessionFile` from Plan 01, `tailSessionFile` + `TailHandle` already imported, `sshLogger` + `databaseLogger` already imported, `basename` from `node:path` stdlib). No package legitimacy audit needed. |
</threat_model>

<verification>
1. `npx tsc --noEmit` — zero errors.
2. `npx vitest run src/backend/claude-session/claude-session-server.dormant-tail.test.ts` — all cases (DT1-DT7) pass.
3. `npx vitest run src/backend/claude-session/dormant-poll.test.ts` — all 11 pre-existing cases pass byte-unchanged.
4. `npx vitest run src/ui/features/pretty-view/DormancyOverlay.test.tsx src/ui/features/pretty-view/PrettyView.test.tsx` — pass unchanged (ROADMAP § 6 UI regression check).
5. `npx vitest run` — full suite green.
6. Grep gate: `grep -c 'discoverIdentitySessionFile' src/backend/claude-session/claude-session-server.ts` returns exactly 2 (one import, one call site inside `__applyDormantBranchTailOpenForTests`) — enforces the D-09 out-of-scope invariant (helper is called only from one place).
7. `git status --porcelain src/ui/` returns empty.
8. W-4 WS-close cleanup gate: `grep -B2 -A30 'ws\.on("close"' src/backend/claude-session/claude-session-server.ts | grep -c 'teardownPane' | head -1` returns ≥ 1 — asserts the ws-close handler invokes teardownPane (which in turn stops tailHandle at L1557-1564). This gate is TRUE against the pre-change baseline (invariant we're gating is "the pattern is present," not "we're introducing it").
9. W-5 log-payload downgrade gate: `grep -c 'discoveredFileBasename' src/backend/claude-session/claude-session-server.ts` returns ≥ 1 (the downgraded log payload is present); `grep -E 'discoveredFile\s*:' src/backend/claude-session/claude-session-server.ts | grep -v '^#' | grep -v 'discoveredFileBasename' | wc -l` returns 0 (the absolute-path payload is NOT present anywhere in log emissions).
10. New-seam gate: `grep -c '__applyDormantBranchTailOpenForTests' src/backend/claude-session/claude-session-server.ts` returns ≥ 2 (export declaration + production call site).
</verification>

<success_criteria>
1. Dormant identity panes in production emit historical messages via the WS pipeline (Ashley UAT will confirm after this plan ships as part of the phase deploy).
2. Wake→active handoff never produces a duplicate eventId or an out-of-order message on the WS (CASE-DT4 + DT5 guard the code-level invariant; production observability will confirm at scale).
3. Fallback to today's behavior on null-discovery — no regression to any pre-Phase-32 dormant behavior.
4. Zero UI-side diff (invariant 3).
5. Full vitest suite green.
6. `claude_session_dormant_tail_*` structured logs surface in production for post-deploy diagnosability (D-05 cold-start / D-07 cost observability), with T-32-05-safe payload shape (session UUID only, no encoded project-dir path).
7. WS-close teardown continues to stop the dormant tail via the pre-existing `teardownPane()` invocation — no leaked tail process after WS drop.
</success_criteria>

<output>
On completion, write `.planning/phases/32-identity-first-turn-session-discovery-wake-bubble-message-hi/32-02-SUMMARY.md` following `@$HOME/.claude/get-shit-done/templates/summary.md`. Include:
- The exact line numbers of the dormant-branch edit + safe-close-ordering edit + new seam export in the final file (line numbers may shift from the planning-time L4675 baseline after the edit).
- The final signature and location of `__applyDormantBranchTailOpenForTests` + its `__DormantBranchTailOpenDepsForTests` / `__DormantBranchTailOpenStateForTests` type exports.
- Confirmation that `tailHandle` cleanup on WS close is preserved via the pre-existing `teardownPane()` path (no new ws.on("close") code needed).
- Confirmation that the `claude_session_dormant_tail_discovered` log emits `discoveredFileBasename` only (T-32-05 mitigation).
- D-nnn coverage matrix mapping each test case (DT1-DT7) to at least one D-nnn.
- Any deviation from the planned edit shape (e.g., the enclosing try/catch needed widening, or `__applyDormantPollWithRediscoveryForTests` needed to accept an optional tailSessionFile injection for CASE-DT4/DT5).
</output>
