---
phase: 32-identity-first-turn-session-discovery-wake-bubble-message-hi
plan: 03
type: execute
wave: 3
depends_on: []
files_modified:
  - "~/.claude/roles/box-maintainer/box-map.md (OUTSIDE the repo — edited via the Edit tool, NEVER git-added to skynet-tiffany)"
autonomous: true
requirements: [D-05]
must_haves:
  truths:
    - "~/.claude/roles/box-maintainer/box-map.md documents that the served static tree served by nginx inside the Skynet container is `/app/html/`, NOT `/app/dist/`, so future `docker cp` deploys land bytes where nginx will actually read them."
    - "~/.claude/roles/box-maintainer/box-map.md contains a served-bundle-hash-match check as an explicit step in the standard deploy runbook: `curl -sS https://term.gigaashley.click/ | grep -oE 'assets/index-[^\"]+\\.js'` MUST match `grep -oE 'assets/index-[^\"]+\\.js' dist/index.html` before the deploy can be declared verified."
    - "The correction includes a preserved reference to the 2026-08-12 incident (quick 260812-ma8 first-ship-attempt no-op deploy) so future readers can trace WHY the runbook was updated."
    - "The runbook change is edited via the Edit tool. It is NEVER staged/committed to skynet-tiffany git — box-map.md lives at ~/.claude/roles/box-maintainer/box-map.md, OUTSIDE the repo tree."
    - "The change is small (10-30 lines added/edited); no wholesale re-org of box-map.md."
  artifacts:
    - path: "~/.claude/roles/box-maintainer/box-map.md"
      provides: "Corrected deploy-path documentation (/app/html/ not /app/dist/) + new hash-match verify step in the standard deploy runbook."
      contains: "/app/html/"
  key_links:
    - from: "~/.claude/roles/box-maintainer/box-map.md deploy section"
      to: "the quick 260812-ma8 incident narrative"
      via: "an inline reference in the new/updated runbook block citing the incident date + the no-op `docker cp` root cause"
      pattern: "260812-ma8|no-op.*cp|hash-match"
---

<objective>
Small, wave-independent runbook hardening: correct the deploy-path documentation in `~/.claude/roles/box-maintainer/box-map.md` (which is outside the skynet-tiffany repo tree) so future `docker cp` deploys don't silently no-op by copying bytes to `/app/dist/` when nginx inside the container is actually serving from `/app/html/`. Codify a served-bundle-hash-match check into the standard deploy runbook so the failure mode caught by hand on 2026-08-12 (quick 260812-ma8 first-ship-attempt: `docker cp` to `/app/dist/` returned exit 0, HTTPS smoke returned 200, but the served bundle was Tina's older build from the correct path — hash mismatch would have caught this in seconds) is impossible to repeat silently.

This plan is INDEPENDENT of Wave 1 + Wave 2 — it does not touch any file in `src/backend/claude-session/` and can run in parallel with either. It is scheduled as Wave 3 for orchestration simplicity; the executor may run it first, last, or interleaved with Waves 1-2 as time permits.

Purpose: eliminate a known deploy-loop foot-gun that already burned one ship attempt this month. The runbook is the ONE artifact that governs future deploys of this fork; getting it right closes the loop on the immediate real-world lesson.

Output: `~/.claude/roles/box-maintainer/box-map.md` gains a corrected deploy-path note + a hash-match verify step. No source diffs to skynet-tiffany; no git commits to this repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-identity-first-turn-session-discovery-wake-bubble-message-hi/32-CONTEXT.md
@.planning/quick/260812-ma8-dormancy-bubble-in-flow/260812-ma8-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the served-path documentation in box-map.md</name>
  <files>~/.claude/roles/box-maintainer/box-map.md</files>
  <action>
    Read `~/.claude/roles/box-maintainer/box-map.md` to locate the section documenting the Skynet container's served static tree. Empirical observation from the planning read: the file currently says the Express backend runs as `node dist/backend/backend/starter.js on 127.0.0.1:30001` and describes nginx templating from `docker/nginx.conf` in the fork. What it does NOT currently document: the STATIC BUNDLE tree that nginx serves is `/app/html/`, not `/app/dist/`. This is the miss that produced the 2026-08-12 no-op deploy incident.

    Add a new subsection (or extend the "In-container nginx layer" block at approximately L75-92) with the following content:

    (a) A one-paragraph note stating: the Vite build's static output goes to `/app/html/` inside the container, NOT to `/app/dist/`. `dist/` is the BUILD OUTPUT tree from the host-side `npm run build`; inside the container that build gets copied to `/app/html/` during image build (or via `docker cp /path/to/dist/. <container>:/app/html/` for hot-patch deploys). Explicitly warn that `docker cp` to `/app/dist/` will succeed with exit 0 but the bytes will land where nginx does not read them — the served bundle will remain the pre-existing one.

    (b) A cross-reference to the 2026-08-12 quick 260812-ma8 incident: "First observed on 2026-08-12 during the initial ship attempt of quick 260812-ma8 (DormancyOverlay bubble-in-flow refactor); `docker cp` to `/app/dist/` returned exit 0, HTTPS smoke returned 200, but Ashley's browser still rendered the pre-patch bundle because nginx was serving from `/app/html/`. Discovered by comparing served bundle hash against `dist/index.html` — hash mismatch was the smoking gun."

    (c) A hash-match verify step that MUST be run before any deploy is declared verified. Verbatim command block:

    ```
    # After docker cp completes, before declaring the deploy verified:
    SERVED=$(curl -sS https://term.gigaashley.click/ | grep -oE 'assets/index-[^"]+\.js' | head -1)
    LOCAL=$(grep -oE 'assets/index-[^"]+\.js' dist/index.html | head -1)
    [ "$SERVED" = "$LOCAL" ] && echo "OK: served=$SERVED local=$LOCAL" || echo "MISMATCH: served=$SERVED local=$LOCAL"
    ```

    A `MISMATCH` result means the `docker cp` copied to the wrong path (or otherwise didn't propagate) — the deploy is NOT verified and the operator must either: (i) redo the copy against the correct destination (`/app/html/`), OR (ii) fall back to a full `docker build` + container recycle.

    Place the new content adjacent to or inside the existing "In-container nginx layer" block (around L75-92) so it groups semantically with the existing nginx-vs-Express prose. Use the same formatting conventions as the surrounding text (Markdown headers if the existing block uses them, bold + prose otherwise; single-space indent under bullet lists; wrap at ~80 cols).

    Do NOT reflow or re-organize the surrounding sections — this is a targeted addition, not a rewrite. Preserve every existing line above and below the insertion point verbatim.

    CRITICAL: `~/.claude/roles/box-maintainer/box-map.md` is OUTSIDE the skynet-tiffany repo tree. Do NOT run `git add`, `git commit`, or any other git command against this file. Do NOT list it in any `git status --porcelain` output expected from the skynet-tiffany working tree. It is edited in place via the Edit tool ONLY.

    Cite D-05 (cold-start / fallback path) as the LOOSE motivating decision — this runbook hardening is a downstream consequence of the phase's lesson-learned; not a direct D-nnn implementation.
  </action>
  <verify>
    <automated>grep -n "/app/html/" ~/.claude/roles/box-maintainer/box-map.md | head -5; echo "---"; grep -n "260812-ma8\|hash-match\|MISMATCH" ~/.claude/roles/box-maintainer/box-map.md | head -5; echo "---"; cd /home/ubuntu/skynet-tiffany && git status --porcelain 2>&1 | grep -c 'box-map' | head -1</automated>
  </verify>
  <done>
    (1) `grep -c '/app/html/' ~/.claude/roles/box-maintainer/box-map.md` returns ≥ 1 (new content added).
    (2) `grep -c '260812-ma8' ~/.claude/roles/box-maintainer/box-map.md` returns ≥ 1 (incident cross-reference present).
    (3) `grep -c 'MISMATCH\|hash-match' ~/.claude/roles/box-maintainer/box-map.md` returns ≥ 1 (verify-step codified).
    (4) `cd /home/ubuntu/skynet-tiffany && git status --porcelain | grep -c 'box-map'` returns 0 (file OUTSIDE repo, never staged).
    (5) The 2026-08-12 incident narrative is preserved in the runbook prose so a future reader hitting a similar symptom can trace back to the root cause.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator-runbook boundary | The runbook is the interface between "an intended deploy" and "an actually-served bundle." A silent failure at this boundary (no-op `docker cp`) is exactly what this plan closes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-07 | Repudiation | Silent-no-op deploy | mitigate | Hash-match check codified as a required verify step — any post-deploy mismatch fails LOUDLY (`MISMATCH: served=... local=...`) and blocks "deploy verified" declaration. This closes the class of bug where `docker cp` + HTTPS smoke both report success but the served bytes are stale. |
| T-32-08 | Information Disclosure | Runbook cites term.gigaashley.click in a public-facing-way | accept | The public URL is already documented elsewhere in box-map.md (Caddy edge section at L43); adding a curl example against it is not a new disclosure. |
| T-32-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (pure documentation edit). No audit needed. |
</threat_model>

<verification>
1. `grep -c '/app/html/' ~/.claude/roles/box-maintainer/box-map.md` ≥ 1.
2. `grep -c '260812-ma8' ~/.claude/roles/box-maintainer/box-map.md` ≥ 1.
3. `grep -c 'MISMATCH\|hash-match\|hash match' ~/.claude/roles/box-maintainer/box-map.md` ≥ 1.
4. `cd /home/ubuntu/skynet-tiffany && git status --porcelain` — box-map.md MUST NOT appear (it's OUTSIDE the repo tree; if it appears something has gone wrong).
5. `head -100 ~/.claude/roles/box-maintainer/box-map.md` shows the surrounding sections (nginx layer, Caddy edge, Operating) preserved verbatim — no re-org.
</verification>

<success_criteria>
1. Future deploy attempts against the Skynet fork use the corrected `/app/html/` path OR fall through the hash-match check LOUDLY if the wrong path is used.
2. Runbook change is isolated to `~/.claude/roles/box-maintainer/box-map.md` — zero source diffs to skynet-tiffany, zero git commits from this plan.
3. 2026-08-12 incident cross-reference preserved as institutional memory.
</success_criteria>

<output>
On completion, write `.planning/phases/32-identity-first-turn-session-discovery-wake-bubble-message-hi/32-03-SUMMARY.md` following `@$HOME/.claude/get-shit-done/templates/summary.md`. Include:
- The exact section header (or bullet list position) where the new content was inserted in box-map.md.
- The exact hash-match snippet as landed (may differ trivially from the plan draft if the executor tightened the shell syntax).
- Confirmation that `cd /home/ubuntu/skynet-tiffany && git status --porcelain` shows no box-map.md entry (file OUTSIDE the repo).
</output>
