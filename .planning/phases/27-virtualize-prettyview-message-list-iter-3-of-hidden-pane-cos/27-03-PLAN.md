---
phase: 27-virtualize-prettyview-message-list-iter-3-of-hidden-pane-cos
plan: 03
type: execute
wave: 3
depends_on:
  - 27-02
files_modified:
  - src/ui/features/pretty-view/PrettyView.aside.test.tsx
  - src/ui/features/pretty-view/PrettyView.virtualization.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "PrettyView.aside.test.tsx assertions still pass after AsideBubble moves out of the flex-column contentRef wrapper into a sibling of the sized virtualizer container"
    - "A new integration test empirically proves the ≤ 30 bubble-subtree cap on a 100+ msg conversation (must_have #2 from CONTEXT.md § Success criteria)"
    - "A new integration test proves scrolling up-then-down keeps the bubble-subtree count bounded (must_have #3)"
    - "A new integration test proves auto-scroll-to-bottom-when-pinned still works over the virtualized DOM (must_have #4)"
    - "A new integration test proves don't-yank-when-scrolled-up still works over the virtualized DOM (must_have #5)"
    - "A new integration test proves the initial-slice-from-bottom hydration lands at the last message (must_have #6)"
    - "A new integration test proves getItemKey identity by inspecting `data-event-id` DOM attribute on each virtualized item wrapper (must_have — SURPRISE #2 measurement-cache stability lock-in via observable DOM attribute, per checker Warning 4)"
    - "A new integration test proves WipBubble / PlanPendingBubble / AsideBubble render in the DOM tree OUTSIDE the sized virtualizer container but INSIDE the outer scroll container (must_have #8 layout invariant)"
    - "`npx vitest run` on the full frontend suite exits 0 with zero failures (must_have #9 / CONTEXT.md § Success criteria item 9)"
    - "Image-bubble grow (must_have #7) is verified via the manual dev-instance smoke check landed in Plan 27-02 Step B, NOT via a JSDOM test in this file (per checker Warning 3 — the no-op / single-slot ResizeObserver stubs available make a JSDOM implementation unreliable, and widening the polyfill is out of scope for this plan)"
  artifacts:
    - path: "src/ui/features/pretty-view/PrettyView.aside.test.tsx"
      provides: "Updated aside subsystem assertions matching the new sibling-of-scroller layout"
      contains: "aside_ready"
    - path: "src/ui/features/pretty-view/PrettyView.virtualization.test.tsx"
      provides: "New virtualization-focused integration test file"
      contains: "data-pv-bubble"
  key_links:
    - from: "src/ui/features/pretty-view/PrettyView.virtualization.test.tsx"
      to: "src/ui/features/pretty-view/PrettyView.tsx virtualized item DOM"
      via: "container.querySelectorAll('[data-pv-bubble]') and el.getAttribute('data-event-id')"
      pattern: "data-pv-bubble|data-event-id"
    - from: "src/ui/features/pretty-view/PrettyView.virtualization.test.tsx"
      to: "src/ui/features/pretty-view/use-auto-scroll.ts scroll-anchor behavior"
      via: "assertions on scrollEl.scrollTop / scrollEl.scrollHeight after WS frame batches"
      pattern: "scrollTop"
---

<objective>
Update the existing PrettyView test suite to match Wave 2's layout change (AsideBubble et al. moved out of the sized virtualizer container into a sibling block), and add a new file `PrettyView.virtualization.test.tsx` that empirically covers the virtualization-specific must-haves (bounded DOM, initial-slice-from-bottom, scroll-anchor preservation across sizes, getItemKey identity via `data-event-id`, accessory-sibling placement).

**Scope adjustment from checker feedback:** must_have #7 (image-bubble grow) is NOT verified via a JSDOM test in this file — the available ResizeObserver stubs (no-op stub from `PrettyView.aside.test.tsx:122-127` that discards constructor callbacks; single-slot CapturingResizeObserver from `use-auto-scroll.test.ts:1-16` that TanStack Virtual's per-item RO constructions would overwrite; see PATTERNS.md SURPRISE #3) make a reliable JSDOM implementation infeasible, and widening the polyfill is explicitly out of scope for this plan (Warning 3). Instead, image-bubble grow is verified by the manual dev-instance smoke check that lands in Plan 27-02 Step B, with observations recorded in 27-02-SUMMARY.

Purpose: without these tests, must-haves #2-#6, #8, #9 from CONTEXT.md are unverifiable inside CI and any future refactor could silently regress the DOM-cap guarantee this whole phase exists to deliver. must_have #7 (image-bubble grow) remains covered — just via a manual smoke check documented in 27-02-SUMMARY, not automated CI.

Output: `PrettyView.aside.test.tsx` with updated DOM-tree assertions where needed; new `PrettyView.virtualization.test.tsx` with 6-7 focused tests using the same WS-stub / CapturingResizeObserver / makeScrollEl patterns already established in `use-auto-scroll.test.ts` and `PrettyView.test.tsx`. Full-suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-virtualize-prettyview-message-list-iter-3-of-hidden-pane-cos/27-CONTEXT.md
@.planning/phases/27-virtualize-prettyview-message-list-iter-3-of-hidden-pane-cos/27-PATTERNS.md
@.planning/phases/27-virtualize-prettyview-message-list-iter-3-of-hidden-pane-cos/27-02-SUMMARY.md
@src/ui/features/pretty-view/PrettyView.tsx
@src/ui/features/pretty-view/PrettyView.aside.test.tsx
@src/ui/features/pretty-view/PrettyView.test.tsx
@src/ui/features/pretty-view/use-auto-scroll.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update PrettyView.aside.test.tsx to reflect accessory-sibling layout, and reconfirm full aside test suite passes</name>
  <files>src/ui/features/pretty-view/PrettyView.aside.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/PrettyView.aside.test.tsx (whole file — 10+ tests use `container.querySelector('[role="note"]')` or similar to find the AsideBubble; some may traverse .parentElement paths that broke when Wave 2 Step B moved AsideBubble out of the flex-column wrapper into a sibling block)
    - src/ui/features/pretty-view/PrettyView.tsx (post-Wave-2 state — inspect the new accessory-block placement to know the exact DOM path the tests need to assert against)
    - .planning/phases/27-virtualize-prettyview-message-list-iter-3-of-hidden-pane-cos/27-02-SUMMARY.md § "tests that broke after Step B" list (Wave 2 Step B executor records the specific failing tests + assertions here)
    - .planning/phases/27-virtualize-prettyview-message-list-iter-3-of-hidden-pane-cos/27-PATTERNS.md § "Tests: use-auto-scroll.test.ts and PrettyView*.test.tsx" (resizeObserverStub pattern at :122-127)
  </read_first>
  <behavior>
    - Every existing aside test still passes after adjusting DOM-tree assertions to match the new layout.
    - If a test asserted "AsideBubble is the last child of the flex column at contentRef" (paraphrased), it must be updated to "AsideBubble is a sibling of the virtualizer container inside the scroll container." Do NOT weaken the assertion — swap the tree-path selector, keep the semantic (still-in-flow, still below the messages).
    - Tests continue to use the existing `resizeObserverStub` / `wsStubs` / `flipToStreaming` / `fireWsMessage` helpers verbatim — no infrastructure churn.
  </behavior>
  <action>
    1. Run `npx vitest run src/ui/features/pretty-view/PrettyView.aside.test.tsx` first to identify which specific tests (if any) broke as a side effect of Wave 2 Step B's DOM re-parenting. Cross-check against the Wave 2 SUMMARY's "tests that broke after Step B" list.

    2. For each broken test, inspect the failing assertion. There are two probable failure modes:
       a) `container.querySelector('[role="note"]')` still works because it's a global selector — no fix needed.
       b) `note.parentElement` or `note.closest(some-flex-selector)` or a specific DOM-path traversal broke because the parent chain changed (`AsideBubble` no longer sits inside `<div className="flex flex-col gap-[18px]">`, it now sits as a direct child of the outer `overflow-y-auto` scroll container).

    3. For (b)-class failures: update the DOM-path assertion to match the new structure. Do NOT weaken the semantic — if the old assertion proved "AsideBubble is inside the scroll container," change the selector but keep proving the same thing. If the old assertion proved "AsideBubble is last child of the flex column," change it to prove "AsideBubble is a sibling of the sized virtualizer container inside the same scroll container" (still an in-flow layout invariant, which is what ASIDE-05 requires).

    4. Do NOT add new tests in this task — new tests go in Task 2's new file.

    5. Do NOT modify the `resizeObserverStub` / WS-mock infrastructure. TanStack Virtual internally uses ResizeObserver too; the no-op stub already covers it (does nothing = virtualizer falls back to `estimateSize` for measurement in JSDOM, which is exactly what these tests want — deterministic sizing).

    6. Run `npx vitest run src/ui/features/pretty-view/PrettyView.aside.test.tsx` again and confirm exit 0.

    Exit-code hygiene (standing directive #3): the `<verify>` block below issues the vitest command as an unpiped invocation with a JSON-reporter check chained via `&&`, so exit codes are preserved. Do NOT pipe test commands through `| tail` / `| head` even during interactive debugging — use `--reporter=verbose` for granularity.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/PrettyView.aside.test.tsx --reporter=json 2>/dev/null | node -e "let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>{try{const j=JSON.parse(d); if(j.numFailedTests>0) { console.error('FAIL:', j.numFailedTests, 'failed of', j.numTotalTests, 'total'); process.exit(1); } console.log('OK:', j.numPassedTests, 'passed'); }catch(e){console.error('parse fail:', e.message); process.exit(1);}})"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/ui/features/pretty-view/PrettyView.aside.test.tsx` exits 0 with zero failed tests.
    - No test was DELETED (only assertion selectors adjusted to match new layout).
    - No test was WEAKENED — an old "is last child of X" assertion becomes a "is a sibling of Y in the same scroll container" assertion; the underlying in-flow layout invariant is still enforced.
    - Zero new dependencies added; zero infrastructure changes to `resizeObserverStub` / WS helpers.
    - Diff limited to `src/ui/features/pretty-view/PrettyView.aside.test.tsx` (no source-file edits).
  </acceptance_criteria>
  <done>
    Aside test file adapted to the new sibling-of-scroller AsideBubble placement, all its tests green, no scope creep into infrastructure.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create PrettyView.virtualization.test.tsx covering bounded-DOM, scroll-anchor preservation, getItemKey identity via data-event-id, and accessory-sibling placement</name>
  <files>src/ui/features/pretty-view/PrettyView.virtualization.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/PrettyView.test.tsx (whole file — reuse the `wsStubs` array + `getCurrentWs()` helper + the `vi.mock("@/api/claude-session-api", () => ...)` factory verbatim; this file is your infrastructure template)
    - src/ui/features/pretty-view/PrettyView.aside.test.tsx :100-135 (the `fireWsMessage(ws, payload)` helper + `resizeObserverStub` pattern in `beforeEach`)
    - src/ui/features/pretty-view/use-auto-scroll.test.ts :22-90 (the `makeScrollEl` + `makeMutableScrollEl` helpers with `Object.defineProperty` mocks for `scrollHeight` / `clientHeight` / `scrollTop` — TanStack Virtual reads these three properties and JSDOM returns 0 without mocks per PATTERNS)
    - src/ui/features/pretty-view/PrettyView.tsx (post-Wave-2 state — inspect the new virtualized JSX for the exact `data-pv-bubble`, `data-index`, and `data-event-id` markers + item wrapper style so test selectors are correct)
    - .planning/phases/27-virtualize-prettyview-message-list-iter-3-of-hidden-pane-cos/27-CONTEXT.md § "Success criteria to bake into must_haves" (items 2-6, 8, 9 — this file's tests cover those; item 7 image-bubble grow is deliberately out of scope per Warning 3 and covered by the Wave 2 Step B manual smoke check; items 1 and 10 are external to this file)
    - .planning/phases/27-virtualize-prettyview-message-list-iter-3-of-hidden-pane-cos/27-PATTERNS.md § "New test angles to add" (bounded DOM, auto-scroll-to-bottom, don't-yank) and SURPRISE #3 (double-RO / single-slot capturing polyfill limitation that motivates dropping the image-grow JSDOM test)
    - .planning/phases/27-virtualize-prettyview-message-list-iter-3-of-hidden-pane-cos/27-02-SUMMARY.md § "Image-bubble grow smoke check" (confirmation that must_have #7 was verified manually in Wave 2 Step B — this file relies on that verification instead of a JSDOM test)
  </read_first>
  <behavior>
    New file `PrettyView.virtualization.test.tsx` covers these test cases (one `it(...)` per):

    1. `Test: bounded DOM on a 100-msg conversation` — mount PrettyView, fire 100+ chat_message WS frames, assert `container.querySelectorAll('[data-pv-bubble]').length <= 30` after all frames flush. Uses the ResizeObserver no-op stub so TanStack Virtual falls back to `estimateSize` = 80 in JSDOM, which combined with a mocked `clientHeight` on the scroll container produces a deterministic small visible slice.

    2. `Test: scroll-anchor preserved when pinned to bottom` — after mount + N frames, simulate the scroll container being at bottom (`scrollTop === scrollHeight - clientHeight`), fire one more frame, assert `scrollTop` was updated to the new bottom (i.e. useAutoScroll's ResizeObserver-driven jump-to-bottom still fires against the virtualized sized container).

    3. `Test: don't-yank-when-scrolled-up` — after mount + N frames, simulate the scroll container scrolled up (`scrollTop` set to a value well above `scrollHeight - clientHeight`, then dispatch a `wheel` event with `deltaY: -100` to exit sticky), fire a new frame, assert `scrollTop` was NOT changed to the new bottom.

    4. `Test: getItemKey uses eventId (verified via data-event-id DOM attribute)` — mount + fire frames with known `eventId` values, query `container.querySelectorAll('[data-pv-bubble]')` and for each rendered item read `el.getAttribute('data-event-id')` and `el.getAttribute('data-index')`. Assert that for every rendered item, `parseInt(el.getAttribute('data-index'))` maps into the expected `messages` array position and `el.getAttribute('data-event-id')` equals the `eventId` of the frame at that position (as fed to the WS stub). This locks in SURPRISE #2 — regression here would cause measurement-cache invalidation on dedup/reorder. **Per checker Warning 4:** inspects the observable `data-event-id` DOM attribute (added in Wave 2 Step A) rather than React's internal `key` prop (which is stripped before commit and not observable through the DOM).

    5. **Test 5 (image-bubble grow re-measure) is DELETED per checker Warning 3.** The available ResizeObserver stubs cannot reliably drive TanStack Virtual's per-item measureElement ROs (no-op stub discards constructor callbacks; single-slot CapturingResizeObserver would be overwritten by TanStack Virtual's many per-item RO constructions per SURPRISE #3), and widening the polyfill is out of scope for this plan. Image-bubble grow (must_have #7) is verified by the Wave 2 Step B manual smoke check documented in 27-02-SUMMARY. Do NOT re-add this test in this plan; a follow-up plan could widen the polyfill and add it, but that is not required for phase closure.

    6. `Test: WipBubble is a sibling of the sized virtualizer container, not a child of it` — mount, fire a frame that produces `isWorking === true` (typically a `working` or `spinning` WS event; check PrettyView.tsx for the exact frame type; may need to send a message that sets `isWorking`), then query the WipBubble element and assert `wipEl.parentElement !== sizedContainer` and `wipEl.parentElement === outerScrollContainer` (or the outer container is a further ancestor with the sized container being a sibling).

    7. `Test: PlanPendingBubble is a sibling of the sized virtualizer container` — analogous to test 6 but fire a `plan_pending` WS frame (check PrettyView.tsx for exact frame shape) and assert the same sibling-not-child invariant.

    8. `Test: AsideBubble is a sibling of the sized virtualizer container` — analogous to tests 6-7, using `aside_ready` frame (already covered by `PrettyView.aside.test.tsx` behaviorally, but this test locks in the DOM-tree layout invariant specifically for the virtualization refactor context).

    NOTE: tests 6-8 may be collapsed into a single `it(...)` test that asserts all three accessory-sibling invariants OR kept as three separate `it(...)` cases OR expressed as `it.each([WipBubble, PlanPendingBubble, AsideBubble])(...)` — the important thing is the DOM-tree invariant is proven for all three accessories.

    Final test count: 7 distinct behaviors (1, 2, 3, 4, 6, 7, 8 — behaviour #5 dropped per Warning 3). Actual `it(...)` count MINIMUM = 5 (behaviors 1/2/3/4 always separate + at least one accessory test that asserts all three). If executor uses `it.each` for behaviors 6/7/8, vitest counts each iteration → 7 passed tests. If executor collapses 6/7/8 into a single `it(...)` with three internal assertions, vitest counts 5 passed tests. The `<automated>` gate below enforces `numPassedTests<5` as the correctness floor — either shape satisfies it.
  </behavior>
  <action>
    Create the new test file `src/ui/features/pretty-view/PrettyView.virtualization.test.tsx`. Structure:

    1. Copy the WS-stub infrastructure from `PrettyView.test.tsx` (the `wsStubs` array, `getCurrentWs` helper, the `vi.mock("@/api/claude-session-api", ...)` factory that pushes a fresh stub per call, the `flipToStreaming` / `fireWsMessage` helpers). This can be a verbatim copy — do not extract to a shared helper file in this plan (scope creep).

    2. In a top-level `beforeEach`, set up the no-op `resizeObserverStub` from `PrettyView.aside.test.tsx:122-127`. Also mock the scroll container's `clientHeight` to a known value (e.g. 800px) — one option is to use `HTMLElement.prototype`-level `Object.defineProperty` overrides in `beforeAll`; another is to walk into the mounted DOM tree post-render and apply `Object.defineProperty` per `use-auto-scroll.test.ts:29-46`. Prefer the latter — surgical and doesn't leak across tests.

    3. Write the tests described in `<behavior>` — behaviors 1, 2, 3, 4 always separate `it(...)` cases, plus at least one accessory-sibling test (behaviors 6, 7, 8 may collapse into one `it(...)` with three internal assertions, or use `it.each` for three iterations, or stay as three separate `it(...)`). Minimum `it(...)` count is 5. Reuse helpers verbatim from PrettyView.test.tsx / use-auto-scroll.test.ts wherever possible.

    4. For test 4 specifically: read `data-event-id` and `data-index` attributes via `el.getAttribute('data-event-id')` / `el.getAttribute('data-index')`. Do NOT attempt to inspect React's `key` prop directly — it is not observable via the DOM. The `data-event-id` attribute is present on every virtualized item wrapper as of Wave 2 Step A and serves as the observable identity witness for `getItemKey`.

    5. **Do NOT include a test for image-bubble grow re-measure.** This is deliberately dropped per checker Warning 3. must_have #7 is covered by the Wave 2 Step B manual smoke check (see 27-02-SUMMARY § "Image-bubble grow smoke check"). If the smoke check was not recorded in the Step B SUMMARY, escalate to the orchestrator before finishing this task — must_have #7 has no verification path without it.

    6. Do NOT add any new mocking of `@tanstack/react-virtual` itself — test the real virtualizer against real DOM. In JSDOM the virtualizer's `measureElement` will find `clientHeight` = 0 unless mocked, and `getBoundingClientRect` returns zeros; that's fine for the bounded-DOM test (virtualizer will render its overscan default), but the scroll-anchor tests need the mocks per (2).

    7. Do NOT delete or modify `PrettyView.test.tsx` (Task 1 owns aside; existing PrettyView.test.tsx tests should keep passing untouched since the pretty-view core rendering path is now virtualization-aware but not virtualization-blocked).

    8. Run the full-suite sanity: `npx vitest run` (whole frontend suite) must exit 0 with zero failed tests before this task is done. If any test outside the pretty-view directory broke, root-cause it (probably won't happen — the refactor is scoped to PrettyView.tsx — but verify).

    Exit-code hygiene (standing directive #3, per checker Blocker 2): the `<verify>` block below runs vitest exactly TWICE — once for the new file, once for the full suite — using `--reporter=json` piped only into a `node -e` exit-code gate. No `| tail` / `| head` masking anywhere. Both invocations preserve vitest's exit code via the JSON-reporter node gate. Do NOT add diagnostic `| tail` prefixes even during interactive debugging — run separate commands if you need verbose output.
  </action>
  <verify>
    <automated>test -f src/ui/features/pretty-view/PrettyView.virtualization.test.tsx && npx vitest run src/ui/features/pretty-view/PrettyView.virtualization.test.tsx --reporter=json 2>/dev/null | node -e "let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>{try{const j=JSON.parse(d); if(j.numFailedTests>0) { console.error('NEW-FILE FAIL:', j.numFailedTests, 'failed of', j.numTotalTests); process.exit(1); } if(j.numPassedTests<5) { console.error('NEW-FILE FAIL: only', j.numPassedTests, 'passed, need >=5 (behaviors 1/2/3/4 always separate + at least one accessory-sibling test)'); process.exit(1); } console.log('NEW-FILE OK:', j.numPassedTests, 'passed'); }catch(e){console.error('parse fail:', e.message); process.exit(1);}})" && npx vitest run --reporter=json 2>/dev/null | node -e "let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>{try{const j=JSON.parse(d); if(j.numFailedTests>0) { console.error('FULL-SUITE FAIL:', j.numFailedTests, 'failed of', j.numTotalTests, 'total'); process.exit(1); } console.log('FULL-SUITE OK:', j.numPassedTests, 'passed,', j.numTotalTests, 'total'); }catch(e){console.error('parse fail:', e.message); process.exit(1);}})"
  </verify>
  <acceptance_criteria>
    - File `src/ui/features/pretty-view/PrettyView.virtualization.test.tsx` exists.
    - It contains at least 5 `it(...)` cases covering behaviors 1, 2, 3, 4 (always separate) + at least one accessory-sibling test (behaviors 6/7/8 may collapse into 1 case with 3 internal assertions, or use `it.each` with 3 iterations, or stay as 3 separate cases — minimum 5 `it(...)` bodies, all 7 behaviors covered).
    - **Test 4 uses `el.getAttribute('data-event-id')` — NOT React key inspection — to prove getItemKey identity (per Warning 4).**
    - **No test attempts to verify image-bubble grow re-measure (per Warning 3 — that must_have is covered by Wave 2 Step B's manual smoke check documented in 27-02-SUMMARY).**
    - `npx vitest run src/ui/features/pretty-view/PrettyView.virtualization.test.tsx` exits 0 with zero failed tests and at least 6 passing.
    - `npx vitest run` (full frontend suite) exits 0 with zero failed tests — this is CONTEXT.md § Success criteria item 9.
    - The file uses the pre-existing `resizeObserverStub` no-op pattern from PrettyView.aside.test.tsx and the `makeScrollEl` / `makeMutableScrollEl` pattern from use-auto-scroll.test.ts (or an inline equivalent) — no new mocking framework introduced.
    - The bounded-DOM test asserts `container.querySelectorAll('[data-pv-bubble]').length <= 30` on a 100+ msg fixture. This is the empirical proof for the phase's core must-have.
    - No source files under `src/ui/features/pretty-view/` (other than tests) modified.
    - `npx tsc --noEmit` exits 0.
    - Exit-code hygiene preserved: vitest runs at most TWICE in `<automated>` — once for the new file, once for the full suite — both gated purely by `--reporter=json` piped to a `node -e` exit-code check. Zero `| tail` / `| head` masking (per checker Blocker 2).
  </acceptance_criteria>
  <done>
    New virtualization test file exists, covers all in-scope virtualization-specific must-haves empirically (must_haves #2-#6 and #8), test 4 proves getItemKey identity via observable `data-event-id` DOM attribute, full frontend suite green, `tsc --noEmit` green. must_have #7 (image-bubble grow) is covered by Wave 2 Step B's manual smoke check documented in 27-02-SUMMARY, NOT by a JSDOM test here (per Warning 3). Phase 27 is code-complete and test-verified — ready for the orchestrator's deploy motion.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run` exit 0 across full frontend suite with zero failures — this is the primary phase-level acceptance.
- `npx tsc --noEmit` exit 0.
- `git diff --stat` shows exactly two changed test files: `PrettyView.aside.test.tsx` (Task 1) and new `PrettyView.virtualization.test.tsx` (Task 2). No source file diffs in this plan.
- The new test file contains a literal assertion on `<= 30` (the empirical bounded-DOM cap) and can be inspected by grep: `grep -c "<= 30\\|<=30" src/ui/features/pretty-view/PrettyView.virtualization.test.tsx | grep -v '^0$'`.
- The new test file contains at least one `getAttribute('data-event-id')` call (the Warning 4 fix witness).
- The new test file contains NO reference to an image-bubble grow test (grep confirmation: `grep -c "image.*grow\\|grow.*image\\|measureElement.*image" src/ui/features/pretty-view/PrettyView.virtualization.test.tsx` should be 0).
- The `<automated>` block in Task 2's `<verify>` runs vitest at most twice (once for the new file, once for the full suite), each via `--reporter=json | node -e "..."` with no `| tail` / `| head` prefixes. Verify by re-reading the `<automated>` string and confirming zero `tail` or `head` invocations (per checker Blocker 2 fix).
</verification>

<success_criteria>
- All Plan-03 must_haves satisfied.
- Phase 27 must_haves #2-#6, #8, #9 empirically enforced by CI via `PrettyView.virtualization.test.tsx`.
- Phase 27 must_have #7 (image-bubble grow) verified via Wave 2 Step B manual smoke check documented in 27-02-SUMMARY. If the smoke-check section is missing from that SUMMARY, Wave 3 executor MUST surface this to the orchestrator before finishing — no shipping without a must_have #7 verification path.
- Phase code-complete: code refactor (Wave 2, split into Step A + Step B) + tests (Wave 3) ship together, both green, on `feat/tab-title-from-tmux`.
- Post-ship verification path preserved: diag emitter untouched (must_have #10 preserved by Wave 2), Wave 3 does not touch it either.
- Orchestrator can now execute the DEPLOY motion (docker build + coord-room announce + recreate + HTTPS 200 verify + push + skynet-patches.md entry) per standing directive #2 — that's out of scope for executor plans.
</success_criteria>

<output>
Create `.planning/phases/27-virtualize-prettyview-message-list-iter-3-of-hidden-pane-cos/27-03-SUMMARY.md` when done. Record:
- Which pre-existing aside tests needed selector updates (if any) — cross-reference against the Wave 2 Step B SUMMARY's "tests that broke" list.
- The list of new tests added to `PrettyView.virtualization.test.tsx` with a one-line description each.
- The final `npx vitest run` pass count.
- Explicit confirmation that Test 4 uses `getAttribute('data-event-id')` (per Warning 4).
- Explicit confirmation that no image-bubble grow test was added, with a cross-reference to the Wave 2 Step B SUMMARY's image-bubble smoke-check section (per Warning 3).
- A note reminding the orchestrator that the post-ship diag comparison (pre-iter3-baseline-diag.jsonl vs. a fresh post-ship snapshot on a 200-msg conversation) is the empirical closing check for the parent hidden-pane-cost-mitigation-empirical-rotation bounty.
</output>
</task>
</content>
</invoke>