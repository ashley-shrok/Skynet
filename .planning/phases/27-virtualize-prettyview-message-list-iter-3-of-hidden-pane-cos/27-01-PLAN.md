---
phase: 27-virtualize-prettyview-message-list-iter-3-of-hidden-pane-cos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "@tanstack/react-virtual is installed and resolvable from src/ui code"
    - "The package sits in dependencies, not devDependencies"
    - "No pre-existing package is downgraded or removed as a side effect"
  artifacts:
    - path: "package.json"
      provides: "@tanstack/react-virtual dependency entry"
      contains: "@tanstack/react-virtual"
    - path: "package-lock.json"
      provides: "resolved lockfile entry for @tanstack/react-virtual"
      contains: "@tanstack/react-virtual"
  key_links:
    - from: "src/ui/features/pretty-view/PrettyView.tsx (Wave 2)"
      to: "@tanstack/react-virtual"
      via: "import { useVirtualizer } from '@tanstack/react-virtual'"
      pattern: "@tanstack/react-virtual"
---

<objective>
Add `@tanstack/react-virtual` as a runtime dependency so Wave 2 can import `useVirtualizer` from it. This plan is a pure config change with an install-and-verify motion — no source files touched, no behavior change.

Purpose: this repo has zero `@tanstack/*` packages installed today (pattern-mapper confirmed: `node_modules/@tanstack/` does not exist, `grep "@tanstack" package.json` returns zero). The virtualization refactor in Wave 2 depends on this dep being resolvable.

Output: `package.json` with `@tanstack/react-virtual` under `dependencies`, `package-lock.json` updated, `node_modules/@tanstack/react-virtual` populated, install verified via `npm ls`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-virtualize-prettyview-message-list-iter-3-of-hidden-pane-cos/27-CONTEXT.md
@.planning/phases/27-virtualize-prettyview-message-list-iter-3-of-hidden-pane-cos/27-PATTERNS.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @tanstack/react-virtual to package.json dependencies and install</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (lines 43-90 in dependencies block to identify alphabetical insertion point between "@fontsource-variable/inter" and "@tailwindcss/typography")
    - .planning/phases/27-virtualize-prettyview-message-list-iter-3-of-hidden-pane-cos/27-CONTEXT.md § Library choice (locked: @tanstack/react-virtual) and § Success criteria item 1
    - .planning/phases/27-virtualize-prettyview-message-list-iter-3-of-hidden-pane-cos/27-PATTERNS.md § "package.json — add @tanstack/react-virtual to dependencies" (guidance on alphabetical placement)
  </read_first>
  <action>
    Edit `package.json` to add `"@tanstack/react-virtual"` to the `"dependencies"` block (NOT `"devDependencies"`). Insert alphabetically between `"@fontsource-variable/inter"` (currently line 44) and `"@tailwindcss/typography"` (currently line 45). Use the latest stable major version — as of 2026-08-09 the current stable is `^3.x`; check `npm view @tanstack/react-virtual version` first and pin `^` to that major.

    Then run `npm install` (NOT `npm install @tanstack/react-virtual` on the CLI, since that would rewrite the caret/version choice; edit the file first, then `npm install` to sync the lockfile). This regenerates `package-lock.json` and populates `node_modules/@tanstack/react-virtual/`.

    Verify install with `npm ls @tanstack/react-virtual` — must show exactly one resolved copy at the top of the tree, no `UNMET DEPENDENCY` / `deduped` weirdness.

    Do NOT touch any source files in this plan. Do NOT modify `devDependencies`. Do NOT run `npm audit fix` or any other side-effect motions.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies['@tanstack/react-virtual']) { console.error('FAIL: not in dependencies'); process.exit(1); } if(p.devDependencies && p.devDependencies['@tanstack/react-virtual']) { console.error('FAIL: also in devDependencies'); process.exit(1); } console.log('OK: version', p.dependencies['@tanstack/react-virtual']);" && npm ls @tanstack/react-virtual && node -e "require('@tanstack/react-virtual'); console.log('OK: resolves at runtime');"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `dependencies` contains a key `"@tanstack/react-virtual"` with a semver value (e.g. `"^3.10.0"`).
    - `package.json` `devDependencies` does NOT contain `"@tanstack/react-virtual"`.
    - `package-lock.json` contains at least one `"node_modules/@tanstack/react-virtual"` entry.
    - `node_modules/@tanstack/react-virtual/package.json` exists on disk.
    - `npm ls @tanstack/react-virtual` exit 0 with a printed version and zero `UNMET DEPENDENCY` lines.
    - `node -e "require('@tanstack/react-virtual')"` exit 0 (module resolves from CJS harness).
    - No source files under `src/` modified in this task.
  </acceptance_criteria>
  <done>
    `@tanstack/react-virtual` is installed as a runtime dep. Wave 2 can `import { useVirtualizer } from "@tanstack/react-virtual"` without a resolution error. Zero source-file diffs; only `package.json` + `package-lock.json` changed.
  </done>
</task>

</tasks>

<verification>
- `npm ls @tanstack/react-virtual` prints a version with zero warnings.
- `git diff --stat` shows exactly two changed files: `package.json`, `package-lock.json`.
- `npx tsc --noEmit` still exits 0 (no source changes → no type regressions possible; sanity check only).
</verification>

<success_criteria>
- Plan-01 must-haves all satisfied.
- Wave 2 unblocked: `import { useVirtualizer } from "@tanstack/react-virtual"` will resolve at build time.
- Zero behavior change in the running app (dep added but not yet imported).
</success_criteria>

<output>
Create `.planning/phases/27-virtualize-prettyview-message-list-iter-3-of-hidden-pane-cos/27-01-SUMMARY.md` when done. Record: the exact version installed, the alphabetical insertion position in package.json, and the `npm ls` output line for reference.
</output>
