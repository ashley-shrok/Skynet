---
phase: 27-virtualize-prettyview-message-list-iter-3-of-hidden-pane-cos
plan: 02
type: execute
wave: 2
depends_on:
  - 27-01
files_modified:
  - src/ui/features/pretty-view/PrettyView.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "The PrettyView scrollable message column renders only viewport-visible (~5-15) messages + overscan buffer at any moment, not all N messages"
    - "On a 100+ msg conversation, DOM contains no more than 30 message-bubble subtrees at initial render (measured by `document.querySelectorAll('[data-pv-bubble]').length`)"
    - "Scrolling up N screens then back down keeps the bubble subtree count bounded (no more than 30 at any observed moment)"
    - "Auto-scroll-to-bottom-when-pinned still works: sending a message snaps the viewport to the bottom"
    - "Don't-yank-when-scrolled-up still works: a user who has scrolled up stays put when new messages arrive"
    - "Initial paint lands at the BOTTOM of the conversation (recent messages visible), not the top"
    - "Image-bearing bubbles grow to their loaded height without visible jitter and without displacing subsequent items off-screen (verified manually via dev-instance smoke check in Step B)"
    - "WipBubble, PlanPendingBubble, AsideBubble still render below the message list — they are physically OUTSIDE the virtualizer's sized-height container but still INSIDE the scroll container, in-flow, per ASIDE-05 (not overlay / sticky / fixed)"
    - "useAutoScroll's scroll-container-side effects (overflow-anchor mutation, ResizeObserver on contentEl, wheel/touchmove/scroll listeners) still observe the correct DOM element after refactor"
    - "Message key remains `eventId` (used as virtualizer getItemKey — required for measurement-cache stability across dedup / reorders)"
    - "Each virtualized item wrapper exposes `data-event-id` matching `messages[data-index].eventId` so Wave 3 tests can prove getItemKey identity without inspecting React's internal key prop"
  artifacts:
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "Virtualized message list rendering"
      contains: "useVirtualizer"
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "Stable data attribute for post-ship DOM counting"
      contains: "data-pv-bubble"
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "Stable per-item event-id attribute for getItemKey testability"
      contains: "data-event-id"
  key_links:
    - from: "src/ui/features/pretty-view/PrettyView.tsx"
      to: "@tanstack/react-virtual"
      via: "import { useVirtualizer } from '@tanstack/react-virtual'"
      pattern: "useVirtualizer\\("
    - from: "src/ui/features/pretty-view/PrettyView.tsx"
      to: "src/ui/features/pretty-view/use-auto-scroll.ts"
      via: "composed callback ref on the outer scroll container (scrollElRef.current = el; scrollRef(el))"
      pattern: "scrollRef\\("
    - from: "src/ui/features/pretty-view/PrettyView.tsx virtualized items"
      to: "messages[i].eventId"
      via: "getItemKey: (i) => messages[i].eventId + data-event-id attribute on each item wrapper"
      pattern: "getItemKey|data-event-id"
---

<objective>
Refactor the PrettyView scrollable message column at `PrettyView.tsx:1715-1817` to render messages via `@tanstack/react-virtual`'s `useVirtualizer`, so DOM stays constant (~30 nodes worth of bubble subtrees) regardless of conversation length. Preserve every behavior of `useAutoScroll` by composing its `scrollRef` onto the same outer scroll container the virtualizer measures. Preserve initial-slice-from-bottom hydration by trusting the existing `useAutoScroll` rAF-chain jump (Option A from 27-PATTERNS.md § use-auto-scroll analog — the "do nothing" outcome for use-auto-scroll.ts). Handle image-bubble grow via TanStack Virtual's `measureElement` ResizeObserver, with a manual dev-instance smoke check to prove it works on a real image-bearing conversation (per CONTEXT.md § "Image bubble height unknown until load" gate). Physically MOVE `WipBubble` / `PlanPendingBubble` / `AsideBubble` OUT of the sized virtualizer container into a sibling in-flow block inside the same scroll container, per pattern-mapper SURPRISE #1.

**Task splitting (from checker Warning 5):** Task 1 is decomposed into two atomic steps (Step A and Step B) executed as two separate commits within this plan. Step A adds the virtualizer while keeping accessories INSIDE the contentRef container — a pure add, no test failures expected. Step B then moves accessories OUT to a sibling block — the ONE change that will break `PrettyView.aside.test.tsx`'s DOM-tree assertions in a scoped, single-cause way that Wave 3 addresses cleanly. This split isolates the "add virtualization" concern from the "restructure DOM tree" concern so that any failing test in Wave 3 has an unambiguous root cause.

Purpose: today a 200-msg conversation renders 2,500-3,200 DOM nodes and this scales linearly forever. Post-refactor: DOM stays flat (~300 nodes for the pretty-view root) regardless of message count. This is iter 3 of the hidden-pane-cost-mitigation empirical rotation — the final iter needed to close the parent bounty.

Output: `PrettyView.tsx` with a virtualized message list, `data-pv-bubble` + `data-event-id` + `data-index` marker attributes on each virtualized item wrapper (for empirical DOM counting in tests + getItemKey verification + post-ship diag verification), and accessories physically moved to a sibling block. Zero other files touched. `use-auto-scroll.ts` is NOT modified. Two atomic commits: one for Step A, one for Step B.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-virtualize-prettyview-message-list-iter-3-of-hidden-pane-cos/27-CONTEXT.md
@.planning/phases/27-virtualize-prettyview-message-list-iter-3-of-hidden-pane-cos/27-PATTERNS.md
@.planning/phases/27-virtualize-prettyview-message-list-iter-3-of-hidden-pane-cos/27-01-SUMMARY.md
@src/ui/features/pretty-view/PrettyView.tsx
@src/ui/features/pretty-view/use-auto-scroll.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Virtualize PrettyView message list + move accessories out — TWO ATOMIC STEPS (Step A then Step B, each committed separately)</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/PrettyView.tsx (full read of lines 1-100 for imports, 500-700 for useAutoScroll call site around line 608 + paneKey reset block around 644-684, 1290-1320 for diag registry pvRootRef snapshot at line 1311-1312, and 1500-1820 for the render tree including pvRootRef bind at line 1516 and the message-map block at 1715-1817)
    - src/ui/features/pretty-view/use-auto-scroll.ts (whole file — MUST NOT be modified in this plan; verify the scrollRef / contentRef callback contracts and the ResizeObserver at :137-159 that observes contentEl)
    - .planning/phases/27-virtualize-prettyview-message-list-iter-3-of-hidden-pane-cos/27-CONTEXT.md § "Scroll anchor preservation" (locked API), § "Initial-slice-from-bottom hydration", § "Image bubble height unknown until load" (has the "Plan MUST verify this actually works on a real image-bearing conversation before shipping" gate — Step B smoke check addresses it), § "Below-list accessories stay unvirtualized" (with the layout-change correction)
    - .planning/phases/27-virtualize-prettyview-message-list-iter-3-of-hidden-pane-cos/27-PATTERNS.md § "PrettyView.tsx — replace messages.map with virtualizer" (the concrete target pattern), § "use-auto-scroll.ts — verify anchor logic still works over virtualized DOM" (Option A: do-nothing), all 8 SURPRISES (especially #1 accessories layout change, #2 getItemKey=eventId, #3 double-RO, #8 flex gap becomes padding)
  </read_first>
  <behavior>
    Behaviors this task must produce (Wave 3 tests will assert these; write the tests there):
    - On a session where 100+ WS message frames arrive, `container.querySelectorAll('[data-pv-bubble]').length` is <= 30 at any observed moment.
    - Message key stability: virtualizer receives `getItemKey: (i) => messages[i].eventId` so that dedup / reorder / prepend paths do not invalidate the measure cache.
    - Each item wrapper exposes `data-event-id={messages[virtualRow.index]?.eventId}` alongside `data-pv-bubble` and `data-index` so Wave 3 tests can prove getItemKey identity by inspecting a real DOM attribute (React's internal `key` prop is stripped before commit and NOT observable through the DOM — this attribute is the observable substitute).
    - The outer scroll container has BOTH `useAutoScroll.scrollRef` and the virtualizer's `getScrollElement` reader pointing at the same DOM element (via a composed callback ref).
    - The virtualizer's sized container has `contentRef` from `useAutoScroll` (so the existing ResizeObserver still fires when total-size changes on image load / message add).
    - Each virtualized item is wrapped in an absolute-positioned `<div data-pv-bubble data-index={virtualRow.index} data-event-id={messages[virtualRow.index]?.eventId} ref={rowVirtualizer.measureElement} style={{ position:'absolute', top:0, left:0, width:'100%', transform:'translateY({start}px)', paddingBottom:18 }}>` — the 18px paddingBottom replaces the removed `gap-[18px]` flex rhythm (SURPRISE #8).
    - WipBubble, PlanPendingBubble, AsideBubble render OUTSIDE the virtualizer's sized container but INSIDE the scroll container, as siblings immediately after the sized container. They remain in-flow (no `position: sticky`, no `position: absolute`, no overlay) per ASIDE-05. **This layout change lands in Step B only.** Step A leaves them inside the contentRef container as-is.
    - The jump-to-bottom pill (`sticky bottom-2`) still renders as a sibling inside the scroll container, unchanged.
    - Initial-slice-from-bottom hydration: no new imperative scroll call added; trust `useAutoScroll`'s existing paneKey-change rAF-chain jump (Option A). Verify empirically that after mount the last message is visible.
    - Bubble internals (ChatMessage / ImageBubble / RelayOutboundBubble / RelayInboundBubble) unchanged — they are opaque children of the virtualizer.
    - Image bubble grow re-measures without visible jitter on a real dev-instance session with image-bearing messages (verified via the Step B manual smoke check — the JSDOM equivalent is not implementable with the existing no-op / single-slot ResizeObserver stub infra, per checker Warning 3 and PATTERNS.md SURPRISE #3).
  </behavior>
  <action>
    Edit `PrettyView.tsx` in a surgical diff across TWO ATOMIC STEPS. Do NOT drive-by-refactor any of the surrounding ~1,900 lines. The affected surface is small (~150 lines net churn per PATTERNS SURPRISE #5).

    ────────────────────────────────────────────────────────────────
    STEP A — Add virtualizer, keep accessories INSIDE contentRef container
    ────────────────────────────────────────────────────────────────

    Goal: pure add — after Step A, every pre-existing pretty-view test MUST still pass unchanged. If any test fails after Step A, STOP and root-cause it before proceeding to Step B (a failure here means the virtualizer wiring itself regressed a behavior, not that the DOM tree shape changed).

    Step A actions:

    A1. Add `useVirtualizer` import from `@tanstack/react-virtual` at the top of the file, grouped with the other third-party imports.

    A2. Locate the `useAutoScroll(paneKey)` call at approximately line 608. Immediately AFTER it (order matters per SURPRISE #3 — construct virtualizer AFTER useAutoScroll so any CapturingResizeObserver polyfill in tests captures useAutoScroll's RO first), add:
       - A local ref: `scrollElRef` of type `useRef<HTMLDivElement | null>(null)`.
       - The virtualizer hook: `useVirtualizer({ count: messages.length, getScrollElement: () => scrollElRef.current, estimateSize: () => 80, overscan: 5, getItemKey: (i) => messages[i]?.eventId ?? i })`. Bind to a const named `rowVirtualizer`. The `?? i` fallback protects against the transient race where `messages` shrinks between renders and `count` hasn't caught up yet — TanStack Virtual will call `getItemKey` with the currently-cached count.
       - A composed callback ref: `composeScrollRefs = useCallback((el: HTMLDivElement | null) => { scrollElRef.current = el; scrollRef(el); }, [scrollRef])`. This is the ref that gets bound to the outer scroll container in the JSX so BOTH useAutoScroll AND the virtualizer read from the same DOM element.

    A3. Rewrite the message-list rendering block at lines 1715-1817 to use the virtualizer, BUT keep WipBubble / PlanPendingBubble / AsideBubble as siblings INSIDE the contentRef container (their current position). Target structure:
       - Outer `<div>` at line 1717-1720 keeps its `className` and `overflow-y-auto` behavior. Change its `ref={scrollRef}` to `ref={composeScrollRefs}`.
       - Inner content wrapper at line 1725 changes from `<div ref={contentRef} className="flex flex-col gap-[18px]">` to `<div ref={contentRef} style={{ height: rowVirtualizer.getTotalSize() + 'px', position: 'relative', width: '100%' }}>`. Remove `flex flex-col gap-[18px]` — flexbox does nothing on absolutely-positioned children (SURPRISE #8). Keep `contentRef` binding.
       - Replace `{messages.map((m) => (<div key={m.eventId}>…</div>))}` with `{rowVirtualizer.getVirtualItems().map((virtualRow) => { const m = messages[virtualRow.index]; return (<div key={virtualRow.key} data-pv-bubble data-index={virtualRow.index} data-event-id={m?.eventId} ref={rowVirtualizer.measureElement} style={{ position:'absolute', top:0, left:0, width:'100%', transform:`translateY(${virtualRow.start}px)`, paddingBottom: 18 }}> …same type-switch on m.type as before, unchanged bubble children… </div>); })}`. The `data-pv-bubble` attribute is the empirical hook Wave 3 tests / post-ship diag count. `data-event-id` is the getItemKey identity witness (Warning 4 fix). `data-index` is the row-index witness for measurement/positioning tests. Bubble children (ImageBubble / RelayOutboundBubble / RelayInboundBubble / ChatMessage) MUST render with the exact same props they receive today — do not add / rename / omit any prop.
       - KEEP `{isWorking && <WipBubble />}`, `{planPending && (<PlanPendingBubble .../>)}`, and `{asideText !== null && <AsideBubble text={asideText} />}` INSIDE the `<div ref={contentRef}>` wrapper, positioned as absolute children of the sized container. Since the sized container is now `position: relative`, they need to be given `position: absolute; top: {rowVirtualizer.getTotalSize()}px; left: 0; width: 100%` so they render immediately below the virtualized items without overlapping. This is a temporary shape ONLY for Step A to keep tests green — Step B will restructure them into a sibling block and drop this absolute-positioning workaround.
       - The jump-to-bottom pill block at 1788-1817 is unchanged — it remains a sibling inside the scroll container, its `sticky bottom-2` positioning still anchors to the viewport correctly.

    A4. Run `npx tsc --noEmit && npx vitest run src/ui/features/pretty-view/` — BOTH must exit 0 before proceeding to Step B. If any pretty-view test fails at Step A, ROOT-CAUSE the failure — Step A is a pure add and should not break tests. Do NOT proceed to Step B until Step A is green.

    A5. **Commit Step A** with message: `refactor(pretty-view): virtualize message list (accessories still inside sized container, Step A of 27-02)`. This commit represents a stable green-tests intermediate state.

    ────────────────────────────────────────────────────────────────
    STEP B — Move accessories OUT to sibling block below sized container
    ────────────────────────────────────────────────────────────────

    Goal: restructure the DOM tree so WipBubble / PlanPendingBubble / AsideBubble move from being absolute-positioned children of the sized virtualizer container to being in-flow siblings of the sized container inside the same scroll container. This step WILL break `PrettyView.aside.test.tsx`'s DOM-tree assertions in a scoped, single-cause way that Wave 3 will address.

    Step B actions:

    B1. Restructure the contentRef container in `PrettyView.tsx` (the block Step A left with accessories absolute-positioned inside it):
       - The contentRef `<div>` (now `position: relative; height: {rowVirtualizer.getTotalSize()}px`) contains ONLY the virtualized `getVirtualItems().map(...)` output. Remove the accessory children from inside it.
       - Immediately AFTER the closing `</div>` of the contentRef container but still INSIDE the outer `composeScrollRefs`-bound scroll container, add a plain sibling block that renders the accessories:
         - `{isWorking && <WipBubble />}`
         - `{planPending && (<PlanPendingBubble planFilePath={...} planContent={...} contentError={...} onApprove={...} onFeedback={...} />)}` — preserve ALL props verbatim.
         - `{asideText !== null && <AsideBubble text={asideText} />}`
       - These siblings are in-flow (no `position: sticky`, no `position: absolute`, no overlay) per ASIDE-05. Since they follow the sized virtualizer container in normal document flow, the outer scroll container's `scrollHeight` naturally includes both the virtualizer's `totalSize` AND the accessory heights — useAutoScroll's "pin to bottom" logic continues to work uniformly.
       - Update the AsideBubble comment (currently PrettyView.tsx:1776-1779) to reflect the new structure: "AsideBubble mounts as an in-flow sibling immediately after the sized virtualizer container inside the scroll container. Post-refactor (Phase 27), it is no longer a child of the flex column that holds the messages — that column became the virtualizer's absolute-positioned sized container. AsideBubble stays visually below the message list and still watched by useAutoScroll's contentRef-side ResizeObserver via the shared scroll container's scrollHeight."

    B2. Do NOT modify `use-auto-scroll.ts`. Do NOT modify the `paneKey`-change reset block at :644-684. Do NOT add any imperative `rowVirtualizer.scrollToIndex(...)` call — trust Option A from PATTERNS.

    B3. Do NOT extract a wrapper hook (`use-pv-virtualizer.ts`) — inline is fine for a first pass; PATTERNS classifies it as "optional planner choice." Keeping it inline minimizes diff.

    B4. Do NOT touch the diag emitter or `pvRootRef` (line 538 / 1516). The diag-registry snapshot at :1311-1312 (`pvRootRef.current.querySelectorAll("*").length`) is the post-ship measurement path and must remain unchanged — this is must_have #10 from CONTEXT.md.

    B5. Run `npx tsc --noEmit && npx vitest run src/ui/features/pretty-view/`. `tsc` must exit 0. Some pretty-view tests are EXPECTED to fail here — specifically `PrettyView.aside.test.tsx` assertions that traverse the DOM tree expecting AsideBubble to be a child of the flex column. Wave 3 (Plan 27-03 Task 1) owns those test updates.
       - For each failing test, verify the break is purely a "DOM tree shape changed, assertion needs the new selector path" break, NOT a behavior regression. Record failing test names + the specific failing assertion + a one-line diagnosis in the SUMMARY.
       - If any test fails for a reason OTHER than the accessory-sibling layout change (e.g. a behavior regression in useAutoScroll, a virtualizer-wiring bug, a bubble prop-drilling regression), STOP and fix the root cause before finishing Step B.

    B6. **Manual smoke check (Warning 3 replacement for the unimplementable image-bubble JSDOM test):** Before committing Step B, load a session with at least one image-bearing message in a local dev instance (`npm run dev` on this working tree, open a conversation that contains an ImageBubble entry). Confirm all THREE of the following observations by direct visual inspection:
       - (i) The image bubble renders at its placeholder height first, then grows to the full image height when the image loads, WITHOUT visible jitter of surrounding items (no flicker, no stuttering re-layout).
       - (ii) Items that were rendered BELOW the image bubble at estimate-size time stay correctly positioned after the image grow — no items get pushed off-screen, no items overlap.
       - (iii) Scrolling up past the image bubble then back down produces the same stable layout (the virtualizer's measurement cache is stable across the round-trip).
       - Record the observation verbatim in `27-02-SUMMARY.md` under a new heading `## Image-bubble grow smoke check (Step B, per checker Warning 3)`. Include: conversation ID or file path used, number of image bubbles observed, and a plain-language pass/fail statement for each of (i)/(ii)/(iii). If ANY observation fails, this constitutes a phase-blocking regression on must_have #7 — root-cause before shipping and do NOT commit until resolved.

    B7. **Commit Step B** with message: `refactor(pretty-view): move accessories out of sized virtualizer container (Step B of 27-02)`.

    ────────────────────────────────────────────────────────────────
    Common hygiene rules for both steps
    ────────────────────────────────────────────────────────────────

    - Exit-code hygiene: NEVER pipe build commands through `| tail` / `| head` (standing directive #3). The `<verify>` block below chains every gate with bare `&&` — no piping — so any tsc or node-invariant-check failure short-circuits the chain with a non-zero exit. If you need to inspect verbose output during debugging, run the command separately (not chained). All grep-style pattern verification is folded into the single `node -e "..."` invariant check at the end of the chain, which reads the source file with `fs.readFileSync` and throws on any missing marker — no unpipeable grep+head diagnostic prefix needed.
  </action>
  <verify>
    <automated>npx tsc --noEmit && echo "---TSC OK---" && node -e "const fs=require('fs'); const src=fs.readFileSync('src/ui/features/pretty-view/PrettyView.tsx','utf8'); if(!src.includes('useVirtualizer')) throw new Error('missing useVirtualizer import/call'); if(!src.includes('data-pv-bubble')) throw new Error('missing data-pv-bubble marker'); if(!src.includes('data-event-id')) throw new Error('missing data-event-id marker (Warning 4 fix)'); if(!src.includes('getItemKey')) throw new Error('missing getItemKey'); if(!src.includes('composeScrollRefs')) throw new Error('missing composeScrollRefs composed ref'); if(src.match(/gap-\\[18px\\]/g)) throw new Error('flex gap-[18px] still present — should be replaced with paddingBottom'); console.log('OK: all Wave-2 markers present, no forbidden gap-[18px] class remaining');"</automated>
  </verify>
  <acceptance_criteria>
    - `PrettyView.tsx` contains a top-level `import { useVirtualizer } from "@tanstack/react-virtual"`.
    - `PrettyView.tsx` calls `useVirtualizer({ count: messages.length, ..., getItemKey: (i) => messages[i]?.eventId ?? i, overscan: 5, estimateSize: () => 80 })`.
    - The outer scroll container's ref prop is a composed callback (bound name: `composeScrollRefs`) that assigns both `scrollElRef.current` and calls `scrollRef(el)`.
    - The virtualizer's sized container is bound with `ref={contentRef}` and its inline style sets `height: {rowVirtualizer.getTotalSize()}px`, `position: 'relative'`.
    - Each virtualized item wrapper has attributes `data-pv-bubble`, `data-index={virtualRow.index}`, `data-event-id={messages[virtualRow.index]?.eventId}`, and prop `ref={rowVirtualizer.measureElement}`.
    - Each virtualized item wrapper inline style contains `position: 'absolute'`, `transform: translateY(...)`, and `paddingBottom: 18`.
    - `PrettyView.tsx` no longer contains the literal string `gap-[18px]` (removed with the flex column).
    - After Step B, WipBubble / PlanPendingBubble / AsideBubble JSX is rendered OUTSIDE the `contentRef`-bound sized container but still INSIDE the outer `composeScrollRefs`-bound scroll container, as in-flow siblings (no `position: sticky`, no `position: absolute` on the accessory wrappers).
    - `src/ui/features/pretty-view/use-auto-scroll.ts` is unchanged (byte-for-byte: `git diff src/ui/features/pretty-view/use-auto-scroll.ts` prints nothing).
    - `src/ui/lib/diag-emitter.ts` and `src/ui/lib/diag-registry.ts` are unchanged (`git diff` prints nothing for both).
    - `npx tsc --noEmit` exits 0.
    - Step A commit landed with all pre-existing pretty-view tests GREEN. Step B commit landed with only "DOM-tree-shape assertion" failures in `PrettyView.aside.test.tsx` — no behavior regressions. Failing tests recorded in SUMMARY for Wave 3 pickup.
    - Manual image-bubble smoke check completed and results recorded in SUMMARY under `## Image-bubble grow smoke check (Step B, per checker Warning 3)`. All three observations (i)/(ii)/(iii) marked PASS. If any observation failed, the commit was blocked and root-caused before proceeding.
    - Git log shows TWO commits from this task: one Step A commit ("virtualize message list (accessories still inside sized container, Step A of 27-02)") and one Step B commit ("move accessories out of sized virtualizer container (Step B of 27-02)").
    - `<automated>` verify block contains ZERO stdout-masking pipes (`| tail`, `| head`) — every gate either runs unpiped or pipes into a language-level exit-code gate (`node -e "…process.exit(1)…"`) that preserves the source command's failure signal (per checker Blocker 1 fix). Note: piping INTO `node -e` for a JSON-reporter parse is legitimate — that's a gate, not a mask. Piping INTO `tail`/`head` is a mask (stdout redirect that consumes the source's exit code) and is what's banned.
  </acceptance_criteria>
  <done>
    Virtualized message list replaces the raw `messages.map`, landed across TWO atomic commits (Step A: add virtualizer with accessories still inside; Step B: move accessories out to sibling block). Each item wrapper exposes `data-pv-bubble`, `data-index`, and `data-event-id`. `useAutoScroll` untouched, `pvRootRef` untouched, diag emitter untouched. `tsc --noEmit` green. Manual image-bubble smoke check on a real dev instance PASSED all three observations (per Warning 3, this replaces the unimplementable JSDOM test) — result recorded in SUMMARY. Wave 3 owns test updates for the layout change; only "DOM-tree-shape assertion" failures expected in aside tests after Step B, and those failing test names are recorded in SUMMARY.
  </done>
</task>

</tasks>

<verification>
- `git diff --stat` shows exactly one changed file across the two commits: `src/ui/features/pretty-view/PrettyView.tsx`.
- Post-refactor, `PrettyView.tsx` has exactly ONE new import (`useVirtualizer`) and NO removed imports (all existing bubble imports still needed).
- `git diff src/ui/features/pretty-view/use-auto-scroll.ts` prints nothing.
- `git diff src/ui/lib/diag-emitter.ts src/ui/lib/diag-registry.ts` prints nothing.
- `npx tsc --noEmit` exit 0.
- `git log --oneline -3` shows the two Step A + Step B commits with subjects that match the pattern in the acceptance criteria.
- After Step A commit, `npx vitest run src/ui/features/pretty-view/` exit 0 (all pretty-view tests green — Step A is a pure add).
- After Step B commit, any pretty-view test failure is a DOM-tree-shape mismatch in `PrettyView.aside.test.tsx` (e.g. AsideBubble now DOM-parented to the scroll container instead of the flex column), not a behavior regression. Confirmed by inspecting the failing assertion and cross-checking that the underlying user-visible behavior still works.
- SUMMARY contains the `## Image-bubble grow smoke check (Step B, per checker Warning 3)` section with all three observations recorded PASS.
</verification>

<success_criteria>
- All Plan-02 must_haves observable via a manual run: opening a long conversation and inspecting `document.querySelectorAll('[data-pv-bubble]').length` yields <= 30. Scrolling up and down keeps the count bounded. Sending a message auto-scrolls to bottom. Accessories still render (as in-flow siblings below the sized container post-Step-B).
- Wave 3 unblocked with a concrete surface (the `data-pv-bubble` + `data-event-id` + `data-index` markers + the accessory-as-scroll-container-sibling structure) to write empirical tests against.
- Zero regression in `useAutoScroll` (byte-for-byte identical file).
- Two-commit shape gives Wave 3 an unambiguous test-failure root cause: any failing aside test after Step B lands is a DOM-tree assertion that needs the new selector path, NOT a virtualizer-wiring behavior regression.
- Image-bubble grow behavior (must_have #7) empirically verified on a real dev instance via the Step B manual smoke check, per the CONTEXT.md gate. The JSDOM equivalent (checker Warning 3) is dropped from the automated CI surface in Plan-03 in favor of this manual proof.
</success_criteria>

<output>
Create `.planning/phases/27-virtualize-prettyview-message-list-iter-3-of-hidden-pane-cos/27-02-SUMMARY.md` when done. Record:
- Exact line ranges of the refactored block (Step A intermediate state and Step B final state).
- Git SHAs of the Step A commit and Step B commit.
- List of any pretty-view tests that broke after Step B (with test-name + specific failing assertion + one-line diagnosis) for Wave 3 pickup.
- The `## Image-bubble grow smoke check (Step B, per checker Warning 3)` section with observations (i)/(ii)/(iii) recorded PASS or FAIL, plus conversation ID/path used, number of image bubbles observed, and any anomalies noted.
- A note on the observed post-refactor DOM node count if the executor did an additional manual smoke test on a long conversation.
</output>
</task>
</content>
</invoke>