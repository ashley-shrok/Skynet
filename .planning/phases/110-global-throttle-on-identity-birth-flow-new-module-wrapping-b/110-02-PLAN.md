---
phase: 110-global-throttle-on-identity-birth-flow-new-module-wrapping-b
plan: 02
type: execute
wave: 2
depends_on:
  - 110-01
files_modified:
  - src/backend/database/routes/identity-birth.ts
  - src/backend/database/routes/identity-birth.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "POST /identities/birth handler acquires a birth slot before invoking birthIdentity(...); releases in a finally"
    - "POST /identities/birth handler responds 429 JSON {error:'identity_birth_queue_full', retry_after_ms} on ThrottleRejectedError — SSE stream NOT opened"
    - "POST /identities/birth/retry/:key handler acquires a birth slot before invoking runRelayMintAndWrite(...); releases in a finally"
    - "POST /identities/birth/retry/:key handler responds 429 JSON {error, retry_after_ms} on ThrottleRejectedError — SSE stream NOT opened"
    - "Three concurrent POST /identities/birth requests with default maxConcurrent=1 serialize (birthIdentity called with A then B then C in order — B does not start until A's release fires)"
    - "Fourth POST /identities/birth when maxConcurrent=1 + maxQueueDepth=2 fires 429 with the ThrottleRejectedError.retryAfterMs value in the body"
    - "Acquire happens BEFORE res.flushHeaders() / SSE header set — 429 lands as application/json, not as a text/event-stream frame"
    - "npx vitest run src/backend/database/routes/identity-birth.test.ts passes"
  artifacts:
    - path: "src/backend/database/routes/identity-birth.ts"
      provides: "Both HTTP handlers (POST / and POST /retry/:key) wrap orchestrator calls in acquire/release"
      contains: "acquireBirthSlot"
    - path: "src/backend/database/routes/identity-birth.test.ts"
      provides: "Extended integration tests for throttle serialization + 429-on-cap"
      min_lines: 1100
  key_links:
    - from: "src/backend/database/routes/identity-birth.ts"
      to: "src/backend/identity-birth/global-throttle.ts"
      via: "import { acquireBirthSlot, ThrottleRejectedError } from '../../identity-birth/global-throttle.js'"
      pattern: "acquireBirthSlot"
    - from: "src/backend/database/routes/identity-birth.test.ts"
      to: "src/backend/identity-birth/global-throttle.ts"
      via: "vi.mock or __resetForTests import"
      pattern: "global-throttle"
---

<objective>
Wire the global-throttle module (delivered by Plan 110-01) into the HTTP entry point at `src/backend/database/routes/identity-birth.ts` — both `POST /identities/birth` and `POST /identities/birth/retry/:key` — and extend `identity-birth.test.ts` with the two integration tests CONTEXT.md § "Test strategy" specifies: three-concurrent-request serialization and 429-at-cap.

Purpose: Deliver must-haves 3, 4, 7. The 429-response semantics must be exact: JSON body `{error: "identity_birth_queue_full", retry_after_ms: <number>}`, `Content-Type: application/json`, and — critically — the acquire happens BEFORE the SSE headers are flushed, so a rejected caller sees a proper HTTP 429 not an SSE frame after the stream has already opened.

Output: Edits to two files. File-disjoint from Plan 110-03 (which touches `spawn-requests/worker.ts` and its test), so this plan and 110-03 can run in Wave 2 in parallel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-global-throttle-on-identity-birth-flow-new-module-wrapping-b/110-CONTEXT.md
@.planning/phases/110-global-throttle-on-identity-birth-flow-new-module-wrapping-b/110-01-PLAN.md
@src/backend/database/routes/identity-birth.ts
@src/backend/database/routes/identity-birth.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire acquireBirthSlot into POST / handler + POST /retry/:key handler in identity-birth.ts</name>
  <read_first>
    - src/backend/database/routes/identity-birth.ts (both handlers in full — POST / at L90-512, POST /retry/:key at L539-704 — you're inserting the acquire before res.flushHeaders() and the release inside the existing finally block)
    - src/backend/identity-birth/global-throttle.ts (the module you're wiring — confirms the `AcquireContext` shape and `ThrottleRejectedError.retryAfterMs` field)
    - .planning/phases/110-global-throttle-on-identity-birth-flow-new-module-wrapping-b/110-CONTEXT.md § "Integration surface" bullet 1 (HTTP path)
  </read_first>
  <files>src/backend/database/routes/identity-birth.ts</files>
  <behavior>
    - When maxConcurrent slots are free, the handler flow is unchanged from today (SSE opens, orchestrator runs, connection closes). Latency-neutral on the happy path.
    - When all slots are busy but the queue has room, the handler awaits its slot (log line `identity_birth_throttle_wait` fires from the throttle module) before flushing SSE headers.
    - When the queue is full, the handler catches `ThrottleRejectedError` BEFORE `res.flushHeaders()` and responds `res.status(429).json({ error: "identity_birth_queue_full", retry_after_ms: err.retryAfterMs })`. Content-Type is application/json (no SSE headers set), and the client sees a normal HTTP error response (not an event-stream frame).
    - `POST /retry/:key` mirrors the same discipline: acquire slot BEFORE `res.flushHeaders()`, release inside the existing `finally` block that already tears down the keepalive interval and the SSH conn.
    - Release is idempotent by design (guarded inside `global-throttle.ts`), so calling `release()` from the outer `finally` even after an early exit path is safe.
  </behavior>
  <action>
    Import at the top of the file (near the existing orchestrator imports at L41-49):

    ```
    import {
      acquireBirthSlot,
      ThrottleRejectedError,
    } from "../../identity-birth/global-throttle.js";
    ```

    (Use `../../identity-birth/global-throttle.js` — one dot-dot to escape `routes/`, one to escape `database/`, then into `identity-birth/`.)

    **POST / handler edits** (currently starts at L90):

    Insert the acquire call AFTER the existing `getMatrixAdminCreds()` fail-early gate at L314-321 (503 on missing creds) and BEFORE the SSE header block at L326-330. Rationale: 503 is a hard "the box isn't ready" signal that should short-circuit even the throttle; the throttle only gates on load, not on foundational readiness.

    Concretely, insert this block right before the `res.setHeader("Content-Type", "text/event-stream")` line:

    ```
    // Phase 110: acquire a global-birth-throttle slot before opening SSE. If
    // the queue is at capacity, reject with 429 JSON (Content-Type
    // application/json) — do NOT flush SSE headers, because a caller that
    // receives text/event-stream frames after a 429 status has no clean way
    // to route the response. The 429 body carries retry_after_ms so the
    // frontend can back off with a hint.
    let release: (() => void) | null = null;
    try {
      release = await acquireBirthSlot({ source: "http" });
    } catch (err) {
      if (err instanceof ThrottleRejectedError) {
        res
          .status(429)
          .json({ error: "identity_birth_queue_full", retry_after_ms: err.retryAfterMs });
        return;
      }
      throw err;
    }
    ```

    Then, wrap the existing `try { await birthIdentity(...) } catch (err) { ... } finally { clearInterval(keepAliveInterval); if (parsedAvatarCandidateId) { ... } res.end(); }` block (currently L441-510) so the `release()` call fires in the outermost finally. Concretely: leave the existing try/catch/finally structure intact, and inside the `finally` block, AFTER `res.end();`, add:

    ```
    // Phase 110: release the throttle slot LAST — after res.end() has flushed
    // pending frames but before the handler returns to Express. The
    // global-throttle module guards release() with a local `released` flag,
    // so a stray double-call is a no-op.
    if (release) release();
    ```

    **POST /retry/:key handler edits** (currently at L539-704):

    Same discipline. Insert the acquire call AFTER the existing `resolveHostById` 404 gate (L586-589) and BEFORE the SSE header block at L592-596. The retry route already has a `let conn ... let endedEmitted ... try { ... } catch { ... } finally { clearInterval(keepAliveInterval); if (conn) { conn.end(); } res.end(); }` block spanning L643-702. Add the acquire block before the SSE headers, mirroring the POST / handler shape, and add `if (release) release();` inside the finally AFTER `res.end();`.

    Do NOT change any argument shape or dep-wiring inside the `birthIdentity(...)` or `runRelayMintAndWrite(...)` calls. Do NOT modify `identity-birth-orchestrator.ts` (must-have 11). Do NOT touch the keepalive interval, the abortController, or the SSE emit function — just wrap the whole flow in an acquire/release pair.

    Retain all existing comments that predate this phase; the file's git-blame archaeology matters for future readers.
  </action>
  <verify>
    <automated>grep -q 'import {.*acquireBirthSlot.*ThrottleRejectedError.*} from "../../identity-birth/global-throttle.js";' src/backend/database/routes/identity-birth.ts &amp;&amp; test $(grep -c 'acquireBirthSlot({' src/backend/database/routes/identity-birth.ts) -eq 2 &amp;&amp; test $(grep -c 'if (release) release();' src/backend/database/routes/identity-birth.ts) -eq 2 &amp;&amp; grep -q 'error: "identity_birth_queue_full"' src/backend/database/routes/identity-birth.ts &amp;&amp; grep -q 'retry_after_ms: err.retryAfterMs' src/backend/database/routes/identity-birth.ts &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "identity-birth.ts" | grep -v "^$" | head -5; echo "tsc-scan-ok"</automated>
  </verify>
  <acceptance_criteria>
    - Single import statement pulls both `acquireBirthSlot` and `ThrottleRejectedError` from `../../identity-birth/global-throttle.js`.
    - `acquireBirthSlot({` appears exactly twice in the file (once per handler).
    - `if (release) release();` appears exactly twice (once per handler's finally).
    - `error: "identity_birth_queue_full"` literal appears at least once (the 429 body).
    - `retry_after_ms: err.retryAfterMs` literal appears at least once.
    - Acquire is inserted BEFORE any `res.setHeader("Content-Type", "text/event-stream")` call (grep line-order check: `sed -n '/acquireBirthSlot/,/text\\/event-stream/p'` shows acquire lines coming first).
    - `npx tsc --noEmit -p tsconfig.json` reports no NEW type errors attributable to `identity-birth.ts`.
    - No modification to `identity-birth-orchestrator.ts` (git status confirms).
  </acceptance_criteria>
  <done>Both handlers wrap their orchestrator calls in an acquire/release pair with 429 fast-fail on `ThrottleRejectedError` before SSE headers flush.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend identity-birth.test.ts with throttle-serialization + 429-at-cap integration tests</name>
  <read_first>
    - src/backend/database/routes/identity-birth.test.ts L1-350 (understand the vi.mock pattern for orchestrator, the makeServer/httpPost helper shape, and where new tests should land)
    - src/backend/database/routes/identity-birth.test.ts L350-end (see how tests await the orchestrator's emit callback + assert SSE frames — mirror this pattern for the concurrency test)
    - src/backend/identity-birth/global-throttle.ts (import `__resetForTests` for a beforeEach reset so throttle state doesn't leak across tests)
  </read_first>
  <files>src/backend/database/routes/identity-birth.test.ts</files>
  <behavior>
    Two new tests appended to the existing suite:

    - Test — three concurrent POST /identities/birth requests serialize under default maxConcurrent=1:
      - `beforeEach` ensures `process.env.IDENTITY_BIRTH_MAX_CONCURRENT` is unset (or `"1"`) and calls `__resetForTests()` from `global-throttle.js`.
      - Mock `birthIdentity` with a manually-resolvable Promise (deferred pattern from `queue.test.ts` L84-90) so the test controls when each birth "finishes."
      - Fire three concurrent `httpPost(port, "/identities/birth", { ...VALID_BODY, name: "a"|"b"|"c" }, {...})` calls via `Promise.all([...])`.
      - Assert only ONE birthIdentity call has been made after a short microtask flush (`await new Promise(r => setTimeout(r, 20))`).
      - Resolve the first deferred → assert the second birthIdentity call fires (still only 2 total, not 3).
      - Resolve the second → assert the third fires.
      - Resolve the third → all three POSTs return 200 with SSE frames.
      - Assert `birthIdentity.mock.calls` ordering matches the enqueue order (a, b, c).

    - Test — 429 fires at cap under maxConcurrent=1 + maxQueueDepth=2:
      - `beforeEach` sets `process.env.IDENTITY_BIRTH_MAX_CONCURRENT = "1"` and `process.env.IDENTITY_BIRTH_MAX_QUEUE_DEPTH = "2"` and calls `__resetForTests()`.
      - Mock `birthIdentity` with a never-resolving Promise so the first request holds the slot indefinitely.
      - Fire request #1 (holds the slot), request #2 and #3 (fill the queue), request #4 (should 429).
      - Since request #1 never resolves, its Promise stays pending — do NOT await it (`.catch(() => {})` and hold the reference).
      - Await request #4's response: assert `status === 429`, `Content-Type` is `application/json` (not `text/event-stream`), body parses to `{ error: "identity_birth_queue_full", retry_after_ms: <number> }` with `retry_after_ms > 0`.
      - Cleanup: `afterEach` resolves the deferred so pending requests don't leak into other tests.

    Both tests must import `__resetForTests` from `../../identity-birth/global-throttle.js` and call it in `beforeEach`.
  </behavior>
  <action>
    Extend the existing `src/backend/database/routes/identity-birth.test.ts` file (do NOT create a new test file — the coverage lives alongside the existing tests to share the vi.mock scaffolding).

    Add at the top-of-file imports section (next to the existing `import { birthIdentity } from "./identity-birth-orchestrator.js";`):

    ```
    import { __resetForTests as __resetThrottleForTests } from "../../identity-birth/global-throttle.js";
    ```

    (Do NOT vi.mock the throttle module — we want the real module under test to prove the wiring works. `__resetForTests` is the module's own test-cleanup hook.)

    Add to the existing `beforeEach(async () => { ... })` block: `__resetThrottleForTests();`. And save/restore `process.env.IDENTITY_BIRTH_MAX_CONCURRENT` + `IDENTITY_BIRTH_MAX_QUEUE_DEPTH` + `IDENTITY_BIRTH_MIN_INTERVAL_MS` if they were set, so per-test env mutations don't leak. The pattern: capture them once in a module-scope const at file-top, restore in `afterEach`.

    Add the two new `it("...")` blocks near the end of the existing describe (after Test 5 but before the retry-route tests, or grouped in a new nested `describe("throttle integration", ...)` if that reads cleaner):

    - `it("Test 110-A: three concurrent POST /identities/birth serialize under maxConcurrent=1")` — use the deferred-Promise + `callOrder` pattern from `queue.test.ts` L82-127. Assert `mockBirthIdentity.mock.calls[0][0].name === "a"`, `[1][0].name === "b"`, `[2][0].name === "c"`.

    - `it("Test 110-B: 429 at queue-depth cap with maxConcurrent=1 + maxQueueDepth=2")` — before firing the 4 requests, `process.env.IDENTITY_BIRTH_MAX_CONCURRENT = "1"`, `process.env.IDENTITY_BIRTH_MAX_QUEUE_DEPTH = "2"`, then `__resetThrottleForTests()` to make the module re-read the env. After the test, `delete process.env.IDENTITY_BIRTH_MAX_QUEUE_DEPTH` (or restore prior value).

    Use `httpPost` (already exists at L225-260) for firing requests. Use `Promise.all` with `.catch(() => ({status: -1, ...}))` on the in-flight requests so a never-resolving mock doesn't block the test forever. The 429 request DOES resolve (Express writes and closes), so a plain await works for it.

    Do NOT change any of the existing 5 tests. The two new tests are purely additive.

    Do NOT add a mock of the throttle module — the test uses the real module. This is what "integration test" means in the phase context.
  </action>
  <verify>
    <automated>npx vitest run src/backend/database/routes/identity-birth.test.ts --reporter=default 2>&amp;1 | tee /tmp/110-02-vitest.log; grep -E "^Tests +[0-9]+ passed" /tmp/110-02-vitest.log &amp;&amp; grep -q "Test 110-A" /tmp/110-02-vitest.log &amp;&amp; grep -q "Test 110-B" /tmp/110-02-vitest.log &amp;&amp; ! grep -E "failed|FAIL" /tmp/110-02-vitest.log</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/backend/database/routes/identity-birth.test.ts` reports all tests passing (exit 0). Test count is the previous count + 2.
    - Test names "Test 110-A" and "Test 110-B" appear in the vitest output.
    - Test file imports `__resetForTests` from `../../identity-birth/global-throttle.js` under a local alias to avoid collision with any existing `__resetForTests` symbol.
    - The 429 test's response body parses to a JSON object containing `error === "identity_birth_queue_full"` and `typeof retry_after_ms === "number"`.
    - No pre-existing test breaks (vitest run reports 0 failed).
  </acceptance_criteria>
  <done>Two integration tests land; the full identity-birth.test.ts suite passes.</done>
</task>

</tasks>

<verification>
Final scoped test command (must be green before commit):

```
npx vitest run src/backend/database/routes/identity-birth.test.ts --reporter=default
```

Expected: all pre-existing tests + Test 110-A + Test 110-B pass. Exit 0.

Manual code-review checks:
- The acquire call sits BEFORE `res.flushHeaders()` in both handlers.
- The release call sits INSIDE the outermost `finally` block AFTER `res.end()` in both handlers.
- No modification to `identity-birth-orchestrator.ts` (must-have 11): `git diff --name-only` shows only `identity-birth.ts` and `identity-birth.test.ts`.
- No modification to `spawn-requests/queue.ts` (must-have 12): file not touched by this plan.
- Banned-strings gate: no alternate spelling of the user's name (see planner constraint — "user" is correct).
</verification>

<success_criteria>
- POST `/identities/birth` handler wraps `birthIdentity(...)` in acquire/release with 429-on-reject (must-have 3).
- POST `/identities/birth/retry/:key` handler wraps `runRelayMintAndWrite(...)` in acquire/release with 429-on-reject (must-have 4).
- Integration test asserts three-concurrent-request serialization + 429-at-cap (must-have 7).
- `npx vitest run src/backend/database/routes/identity-birth.test.ts` reports all tests passing.
- No changes to `identity-birth-orchestrator.ts` or `spawn-requests/queue.ts` (must-haves 11, 12).
</success_criteria>

<output>
Create `.planning/phases/110-global-throttle-on-identity-birth-flow-new-module-wrapping-b/110-02-SUMMARY.md` when done. SUMMARY should document:
- Where the acquire call was inserted in each handler (line-range references — 503-gate proximity for POST /, host-resolver-404-gate proximity for POST /retry/:key).
- Why the acquire lives BEFORE `res.flushHeaders()` (429 needs application/json, not an event-stream frame after headers are flushed).
- Where the release call was inserted (outermost finally, after `res.end()`, guarded by the `if (release)` null-check because acquire may throw before assigning it).
- Test count change (previous count → previous + 2).
- Any edge case discovered during implementation (e.g. env-var restore order, or a subtle race in the never-resolving-mock test).
</output>
