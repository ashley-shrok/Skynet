---
phase: 110-global-throttle-on-identity-birth-flow-new-module-wrapping-b
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - substrate/skills/id/coordinator-instructions.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "coordinator-instructions.md § 'Wait for the response file' (or a nearby drop-mechanics section) now contains a batch-drop-safe note referencing Phase 110"
    - "The note reads roughly: 'Batch drops are safe — the Skynet backend paces births internally (see Phase 110). No manual spacing needed.'"
    - "No other content of coordinator-instructions.md is modified (single additive edit)"
  artifacts:
    - path: "substrate/skills/id/coordinator-instructions.md"
      provides: "Coordinator-facing documentation with batch-drop-safe note"
      contains: "Batch drops are safe"
  key_links:
    - from: "substrate/skills/id/coordinator-instructions.md"
      to: "src/backend/identity-birth/global-throttle.ts"
      via: "documentation cross-reference to Phase 110"
      pattern: "Phase 110"
---

<objective>
Add a one-paragraph batch-drop-safe note to `substrate/skills/id/coordinator-instructions.md` — the coordinator-facing companion doc that the fleet substrate distributor pushes to every managed host under `~/.claude/skills/id/coordinator-instructions.md` on its next sweep — telling coordinators that batch spawn-request drops are safe because the Skynet backend paces births internally via Phase 110.

Purpose: Deliver must-have 9. The doc update is structurally independent of the code (no imports, no test dependencies), so this plan sits at wave 2 with `depends_on: []` and can run in parallel with any code plan. Landing it in the same phase closes the feedback loop: coordinators reading the current doc might be spacing their drops manually to be polite — the throttle makes that unnecessary, and the doc should say so.

Output: Single edit to one markdown file, then commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-global-throttle-on-identity-birth-flow-new-module-wrapping-b/110-CONTEXT.md
@substrate/skills/id/coordinator-instructions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add batch-drop-safe note to coordinator-instructions.md</name>
  <read_first>
    - substrate/skills/id/coordinator-instructions.md L240-300 (the "Drop the request file" and "Wait for the response file" sections — this is where the note lands. The doc's other 400+ lines cover pool-picking, dispatch, escalation — unrelated to the throttle addition.)
    - .planning/phases/110-global-throttle-on-identity-birth-flow-new-module-wrapping-b/110-CONTEXT.md § "Documentation update" for the exact suggested wording.
  </read_first>
  <files>substrate/skills/id/coordinator-instructions.md</files>
  <behavior>
    A coordinator reading the "Drop the request file" section (starting around L241) sees a callout paragraph indicating that dropping multiple request files simultaneously is safe — the Skynet backend paces births internally, and no manual spacing between drops is needed. The doc otherwise reads identically to today.
  </behavior>
  <action>
    Locate the "### Drop the request file" section (currently at L241) and the closing paragraph of that section before "### Wait for the response file" (currently at L269 — the `mkdir -p ~/fleet/spawn-requests` line).

    Insert a new paragraph AFTER the `mkdir -p ~/fleet/spawn-requests` sentence at L269 and BEFORE the `### Wait for the response file` heading at L271. The paragraph text:

    ```
    ⚠️ **Batch drops are safe.** The Skynet backend paces births internally
    via a global throttle (Phase 110 — `src/backend/identity-birth/global-throttle.ts`)
    that both entry points funnel through, so dropping many request files at
    once will not overwhelm the homeserver, the SSH channel budget, or the
    orchestrator. No manual spacing between drops is needed — write the files
    as quickly as your role logic decides they are needed, and the backend
    will drain them in order at a safe pace.
    ```

    (The ⚠️ marker matches the existing callout style in the same section, e.g. L258 `⚠️ **Write atomically**` and L262 `⚠️ **Never pretty-print the JSON.**`.)

    Do NOT modify any other content in the file. Do NOT rewrite the surrounding paragraphs. Do NOT change the section heading levels or the surrounding examples. This is a single additive paragraph insertion.

    Constraints reminders:
    - Planner constraint reminder — "user" is the correct user name throughout; the current doc already respects this. Do not introduce alternate spellings.
    - No em-dash replacement wars — the surrounding paragraphs use em-dashes (`—`) freely; match the local style.
    - Use single-line paragraph or gently-wrapped prose consistent with the surrounding paragraphs (existing lines around L241-270 wrap at ~90 chars — match that).
  </action>
  <verify>
    <automated>grep -q "Batch drops are safe" substrate/skills/id/coordinator-instructions.md &amp;&amp; grep -q "Phase 110" substrate/skills/id/coordinator-instructions.md &amp;&amp; grep -q "global-throttle.ts" substrate/skills/id/coordinator-instructions.md &amp;&amp; grep -q "No manual spacing" substrate/skills/id/coordinator-instructions.md &amp;&amp; test $(wc -l &lt; substrate/skills/id/coordinator-instructions.md) -ge 442 &amp;&amp; test $(wc -l &lt; substrate/skills/id/coordinator-instructions.md) -le 460; echo "line-count-in-range"</automated>
  </verify>
  <acceptance_criteria>
    - `substrate/skills/id/coordinator-instructions.md` contains the string "Batch drops are safe" at least once.
    - The file contains "Phase 110" and "global-throttle.ts" as references.
    - The new paragraph sits inside the "Drop the request file" section, before the "Wait for the response file" section — verified by `sed -n '/### Drop the request file/,/### Wait for the response file/p' substrate/skills/id/coordinator-instructions.md | grep -q "Batch drops are safe"`.
    - The file line count is between 442 and 460 (baseline was 442; new paragraph adds ~7 lines including surrounding blank lines).
    - No other content edits — `git diff --stat substrate/skills/id/coordinator-instructions.md` shows a single-hunk insertion.
    - No alternate spelling of the user's name in the new paragraph (planner constraint — "user" is correct).
  </acceptance_criteria>
  <done>The batch-drop-safe note lands in the correct section with the correct wording and no collateral edits.</done>
</task>

</tasks>

<verification>
Final scoped check (must be true before commit):

```
sed -n '/### Drop the request file/,/### Wait for the response file/p' substrate/skills/id/coordinator-instructions.md | grep -q "Batch drops are safe"
```

Expected: exit 0 (grep matched). The new paragraph is in the right section.

```
git diff --stat substrate/skills/id/coordinator-instructions.md
```

Expected: shows only `substrate/skills/id/coordinator-instructions.md | +N -0` where N is around 7-10 lines. No deletions.

Manual code-review:
- Wording is faithful to CONTEXT.md § "Documentation update" (the suggested phrasing is a floor, not a ceiling — the executor may tighten but not weaken).
- No accidental sentence rewrites in surrounding paragraphs.
- Banned-strings gate: no alternate spelling of the user's name (see planner constraint).
</verification>

<success_criteria>
- Batch-drop-safe note lands in `substrate/skills/id/coordinator-instructions.md` (must-have 9).
- Note is in the correct section (between "Drop the request file" and "Wait for the response file").
- Note references Phase 110 and `global-throttle.ts` for future readers who want to trace the mechanism.
- No collateral edits to the surrounding doc.
</success_criteria>

<output>
Create `.planning/phases/110-global-throttle-on-identity-birth-flow-new-module-wrapping-b/110-04-SUMMARY.md` when done. SUMMARY should document:
- Exact line range where the new paragraph landed.
- Confirmation that no surrounding content was rewritten.
- Note that the substrate distributor picks this up on its next sweep (no manual per-host push needed — orchestrator territory at ship time).
</output>
