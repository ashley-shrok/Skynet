---
phase: 110-global-throttle-on-identity-birth-flow-new-module-wrapping-b
plan: 03
type: execute
wave: 2
depends_on:
  - 110-01
files_modified:
  - src/backend/spawn-requests/worker.ts
  - src/backend/spawn-requests/worker.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "processBirth() acquires a birth slot at the top of the function with {source:'spawn-request', bypassQueueDepth:true, requestId:item.uuid}"
    - "processBirth() releases the slot in a finally block that also encloses ALL response-file writes (success + failure paths)"
    - "Spawn-request path never rejects on queue-depth overflow — bypassQueueDepth:true is passed"
    - "Two parallel processBirth() calls under maxConcurrent=1 serialize (second waits for first's release)"
    - "npx vitest run src/backend/spawn-requests/worker.test.ts passes"
  artifacts:
    - path: "src/backend/spawn-requests/worker.ts"
      provides: "processBirth wraps entire birth flow + response-file drop in throttle acquire/release"
      contains: "acquireBirthSlot"
    - path: "src/backend/spawn-requests/worker.test.ts"
      provides: "Extended tests for parallel serialization + bypass semantics"
      min_lines: 850
  key_links:
    - from: "src/backend/spawn-requests/worker.ts"
      to: "src/backend/identity-birth/global-throttle.ts"
      via: "import { acquireBirthSlot } from '../identity-birth/global-throttle.js'"
      pattern: "acquireBirthSlot"
    - from: "src/backend/spawn-requests/worker.test.ts"
      to: "src/backend/identity-birth/global-throttle.ts"
      via: "__resetForTests import"
      pattern: "global-throttle"
---

<objective>
Wire the global-throttle module (delivered by Plan 110-01) into the spawn-request worker at `src/backend/spawn-requests/worker.ts` — specifically into `processBirth()`, at the top of the function, with `{source:"spawn-request", bypassQueueDepth:true, requestId:item.uuid}`. Extend `worker.test.ts` with the parallel-serialization + bypass-semantics tests CONTEXT.md § "Test strategy" specifies.

Purpose: Deliver must-haves 5, 8. The spawn-request path never rejects on queue-depth overflow — disk-drop origin means "already durable, silently dropping is worse UX than piling up in memory" (CONTEXT.md § "Integration surface" bullet 2). The release() must enclose ALL response-file writes (success + every failure branch), so a birth that returns early on a pre-flight check still frees its slot cleanly.

Output: Edits to two files. File-disjoint from Plan 110-02 (which touches `identity-birth.ts` and its test), so this plan and 110-02 can run in Wave 2 in parallel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-global-throttle-on-identity-birth-flow-new-module-wrapping-b/110-CONTEXT.md
@.planning/phases/110-global-throttle-on-identity-birth-flow-new-module-wrapping-b/110-01-PLAN.md
@src/backend/spawn-requests/worker.ts
@src/backend/spawn-requests/worker.test.ts
@src/backend/spawn-requests/queue.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire acquireBirthSlot into processBirth() with bypassQueueDepth:true and finally-release around the entire birth flow</name>
  <read_first>
    - src/backend/spawn-requests/worker.ts L262-527 (the full `processBirth` function — every early-return path must be enclosed by the finally so the slot is always released)
    - src/backend/identity-birth/global-throttle.ts (confirm `AcquireContext.bypassQueueDepth` optional field + `AcquireContext.requestId` optional field are the wire keys)
    - .planning/phases/110-global-throttle-on-identity-birth-flow-new-module-wrapping-b/110-CONTEXT.md § "Integration surface" bullet 2 (spawn-request path)
  </read_first>
  <files>src/backend/spawn-requests/worker.ts</files>
  <behavior>
    - Every early-return path in `processBirth` (malformed short-circuit L293-299, pool_exhausted L305-308, matrix_creds_missing L311-315, host-owner null L320-324, pool-exhausted-locally L479-482, no-ended-event L485-492, and both terminal success/failure writes at L495-526) fires its response-file write BEFORE `release()` is called by the finally.
    - When maxConcurrent=1 and two parallel `processBirth(itemA, deps)` + `processBirth(itemB, deps)` calls race, B's `birthIdentity` invocation MUST NOT start until A's finally block has fired `release()`.
    - `bypassQueueDepth:true` ensures no ThrottleRejectedError ever propagates out — the spawn-request path treats disk-drop overflow as "pile up in memory, drain in order" rather than "reject and lose work."
    - The `requestId: item.uuid` field flows through into the throttle module's log calls, so `console-forward.log` can correlate throttle-grant lines with worker-process-start lines.
  </behavior>
  <action>
    Import at the top of the file (add after the existing `import { birthIdentity, ROLE_NAME_PATTERN, SSH_CONNECT_TIMEOUT_MS, ... } from "../database/routes/identity-birth-orchestrator.js";` line at L26):

    ```
    import { acquireBirthSlot } from "../identity-birth/global-throttle.js";
    ```

    (Path: `../identity-birth/global-throttle.js` — one dot-dot escapes `spawn-requests/`, then into `identity-birth/`.)

    Note: only `acquireBirthSlot` is needed — the spawn-request path never catches `ThrottleRejectedError` because `bypassQueueDepth:true` prevents rejection.

    **Modify `processBirth` (L278-527):**

    Restructure the function body so the entire flow — from the very first log line at L280-286 all the way through both the success-response-file write at L516-521 and every failure-file write — sits inside a `try/finally` where the `finally` calls `release()`. The acquire happens at the very top, BEFORE the "processing birth" log line.

    Concretely:

    Before the existing `systemLogger.info("spawn-request worker: processing birth", { ... });` line at L280, insert:

    ```
    // Phase 110: acquire a global-birth-throttle slot. Spawn-request-origin
    // callers set bypassQueueDepth:true — disk-drop items are already
    // durable, so silently dropping them on queue overflow is worse UX than
    // piling up in memory. The throttle is orthogonal to spawn-requests/queue.ts
    // (which stays as the FIFO consumer of disk-drop files); the throttle is
    // the broader construct both entry points share.
    const release = await acquireBirthSlot({
      source: "spawn-request",
      bypassQueueDepth: true,
      requestId: item.uuid,
    });
    try {
    ```

    Then, at the END of the function (after the L522-526 else branch that writes the failure file), add a matching:

    ```
    } finally {
      release();
    }
    ```

    Indentation of the wrapped body will shift by one level; that's fine — reviewers understand indentation-only diffs. If indentation churn makes the diff noisy, an acceptable alternative is to hoist the entire body into a helper `const doBirth = async (): Promise<void> => { <existing L280-527 body> };` and call `await doBirth();` inside the try. Executor's call — pick whichever produces a cleaner code-review diff for the actual working tree state.

    Do NOT modify:
    - `identity-birth-orchestrator.ts` (must-have 11)
    - `spawn-requests/queue.ts` (must-have 12)
    - Any dep signature in `WorkerDeps` (the throttle is a module-scope side effect, not injected — mirrors how `spawn-requests/queue.ts` doesn't inject its own module state)
    - The `buildProductionDeps` function or `WorkerDeps` interface
    - The `parseRequestBody`, `mapEndedEventToReason`, `getHostOwnerUserId` exports

    The `release` variable does NOT need to be optional — since `acquireBirthSlot({ bypassQueueDepth: true })` never rejects, we can assume the acquire succeeds and always have a release fn to call. (Contrast with the HTTP path in Plan 110-02 where acquire CAN throw and we need the `let release: (() => void) | null = null` shape.)

    Retain all existing comments; the file's git-blame archaeology matters.
  </action>
  <verify>
    <automated>grep -q 'import { acquireBirthSlot } from "../identity-birth/global-throttle.js";' src/backend/spawn-requests/worker.ts &amp;&amp; grep -q 'source: "spawn-request"' src/backend/spawn-requests/worker.ts &amp;&amp; grep -q 'bypassQueueDepth: true' src/backend/spawn-requests/worker.ts &amp;&amp; grep -q 'requestId: item.uuid' src/backend/spawn-requests/worker.ts &amp;&amp; grep -q 'release();' src/backend/spawn-requests/worker.ts &amp;&amp; test $(grep -c 'acquireBirthSlot({' src/backend/spawn-requests/worker.ts) -eq 1 &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "spawn-requests/worker.ts" | grep -v '^$' | head -5; echo "tsc-scan-ok"</automated>
  </verify>
  <acceptance_criteria>
    - Import of `acquireBirthSlot` from `../identity-birth/global-throttle.js` present at file top.
    - Exactly one `acquireBirthSlot({` call site in the file (inside `processBirth`).
    - Call includes all three fields: `source: "spawn-request"`, `bypassQueueDepth: true`, `requestId: item.uuid` (grepped as three separate literals).
    - A `release();` invocation exists (in the finally block).
    - The acquire happens BEFORE the "processing birth" log line — verified by `grep -n` showing `acquireBirthSlot` on a lower line number than `spawn-request worker: processing birth`.
    - `npx tsc --noEmit -p tsconfig.json` reports no NEW type errors attributable to `worker.ts`.
    - No modification to `identity-birth-orchestrator.ts` or `spawn-requests/queue.ts` (git status confirms).
  </acceptance_criteria>
  <done>processBirth wraps its entire body in throttle acquire/release with bypassQueueDepth:true.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend worker.test.ts with parallel-serialization + bypass-semantics tests</name>
  <read_first>
    - src/backend/spawn-requests/worker.test.ts L1-160 (understand the existing `buildTestDeps` helper + `makePendingBirth` helper + the vi.mock setup for logger and drizzle-orm)
    - src/backend/spawn-requests/worker.test.ts L160-end (see how existing processBirth tests inject a mocked birthIdentity via `deps.birthIdentity` — mirror this for the concurrency test)
    - src/backend/spawn-requests/queue.test.ts L82-127 (the deferred-Promise + `callOrder` pattern you'll reuse for the parallel-serialization test)
    - src/backend/identity-birth/global-throttle.ts (import `__resetForTests` for a beforeEach reset)
  </read_first>
  <files>src/backend/spawn-requests/worker.test.ts</files>
  <behavior>
    Two new tests appended to the existing worker.test.ts suite:

    - Test 110-W-A — two parallel processBirth calls serialize under maxConcurrent=1:
      - `beforeEach`: `process.env.IDENTITY_BIRTH_MAX_CONCURRENT = "1"` (or leave default), then `__resetThrottleForTests()`.
      - Build a `callOrder: string[]` array.
      - Mock `deps.birthIdentity` (via `buildTestDeps`) with a deferred-Promise implementation: on itemA's uuid, push "A-birth-start", await deferredA, push "A-birth-end", emit ended{ok:true}. On itemB's uuid, push "B-birth-start", push "B-birth-end", emit ended{ok:true}.
      - Fire `Promise.all([processBirth(itemA, deps), processBirth(itemB, deps)])`.
      - After a short microtask flush: assert `callOrder` contains "A-birth-start" but NOT "B-birth-start" (B is queued in the throttle).
      - Resolve deferredA. After a microtask flush: assert `callOrder` is `["A-birth-start", "A-birth-end", "B-birth-start", "B-birth-end"]`.
      - Assert `deps.birthIdentity` was called twice, first with itemA, then with itemB.

    - Test 110-W-B — bypassQueueDepth honored (10 parallel calls never reject):
      - `beforeEach`: `process.env.IDENTITY_BIRTH_MAX_CONCURRENT = "1"`, `process.env.IDENTITY_BIRTH_MAX_QUEUE_DEPTH = "2"` (deliberately tiny), then `__resetThrottleForTests()`.
      - Mock `deps.birthIdentity` with a fast-resolving implementation (emits ended{ok:true} immediately).
      - Fire 10 parallel `processBirth(items[i], deps)` calls via `Promise.all(items.map(item => processBirth(item, deps)))`.
      - Assert none reject — all 10 promises settle successfully.
      - Assert `deps.birthIdentity` was called exactly 10 times (all births ran, none were dropped on queue overflow).
      - Assert `deps.writeMarkdownFileAtomic` was called at least 10 times (each birth produces a response file — matches must-have "release() encloses ALL response-file writes").
  </behavior>
  <action>
    Extend the existing `src/backend/spawn-requests/worker.test.ts` file (do NOT create a new test file).

    Add at the top-of-file imports (next to the existing `import { parseRequestBody, mapEndedEventToReason, processBirth, type WorkerDeps } from "./worker.js";`):

    ```
    import { __resetForTests as __resetThrottleForTests } from "../identity-birth/global-throttle.js";
    ```

    (Do NOT vi.mock the throttle module — we want the real module under test to prove the wiring works. The throttle's own `__resetForTests` is the test-cleanup hook.)

    Add to the existing `beforeEach(() => { vi.clearAllMocks(); })` block: `__resetThrottleForTests();`. And save/restore `process.env.IDENTITY_BIRTH_MAX_CONCURRENT` + `IDENTITY_BIRTH_MAX_QUEUE_DEPTH` per-test using module-scope backup consts (capture prior values in `beforeAll`, restore in `afterAll` or after each mutating test).

    Add the two new `it("...")` blocks near the end of the existing `describe("spawn-request worker", ...)` block (grouping them in a nested `describe("throttle integration (Phase 110)", ...)` reads cleanly and lets a reader locate them by grep).

    - `it("Test 110-W-A: two parallel processBirth calls serialize under maxConcurrent=1")` — deferred-Promise pattern from `queue.test.ts` L82-127. Override `deps.birthIdentity` in `buildTestDeps({ birthIdentity: ... })` with the deferred implementation.

    - `it("Test 110-W-B: bypassQueueDepth honored — 10 parallel calls never reject")` — before the test body: `process.env.IDENTITY_BIRTH_MAX_CONCURRENT = "1"`, `process.env.IDENTITY_BIRTH_MAX_QUEUE_DEPTH = "2"`, then `__resetThrottleForTests()`. Build 10 items with distinct uuids (e.g. `Array.from({length: 10}, (_, i) => makePendingBirth({ uuid: \`test-uuid-${String(i).padStart(4, "0")}-...\` }))`). After the test: restore or delete the env vars.

    Do NOT change any of the existing 20 tests. The two new tests are purely additive.

    Note about test-file mocking of `../utils/logger.js`: the existing mock at L60-74 already stubs `systemLogger` — the throttle module imports the same `systemLogger` from the same path, so the mock covers both. No new mock needed.
  </action>
  <verify>
    <automated>npx vitest run src/backend/spawn-requests/worker.test.ts --reporter=default 2>&amp;1 | tee /tmp/110-03-vitest.log; grep -E "^Tests +[0-9]+ passed" /tmp/110-03-vitest.log &amp;&amp; grep -q "Test 110-W-A" /tmp/110-03-vitest.log &amp;&amp; grep -q "Test 110-W-B" /tmp/110-03-vitest.log &amp;&amp; ! grep -E "failed|FAIL" /tmp/110-03-vitest.log</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/backend/spawn-requests/worker.test.ts` reports all tests passing (exit 0). Test count is the previous count + 2.
    - Test names "Test 110-W-A" and "Test 110-W-B" appear in the vitest output.
    - Test file imports `__resetForTests` from `../identity-birth/global-throttle.js` under a local alias.
    - No pre-existing test breaks (vitest run reports 0 failed).
    - Test 110-W-A's callOrder assertion asserts exact order `["A-birth-start", "A-birth-end", "B-birth-start", "B-birth-end"]` — not just membership.
    - Test 110-W-B asserts `deps.birthIdentity` was called exactly 10 times.
  </acceptance_criteria>
  <done>Two integration tests land; the full worker.test.ts suite passes.</done>
</task>

</tasks>

<verification>
Final scoped test command (must be green before commit):

```
npx vitest run src/backend/spawn-requests/worker.test.ts --reporter=default
```

Expected: all pre-existing tests + Test 110-W-A + Test 110-W-B pass. Exit 0.

Additional cross-check (throttle module still green — sanity after wiring):

```
npx vitest run src/backend/identity-birth/global-throttle.test.ts src/backend/spawn-requests/worker.test.ts --reporter=default
```

Expected: both suites green.

Manual code-review checks:
- The acquire call in `worker.ts` sits at the very top of `processBirth`, before the "processing birth" log.
- The release call sits in the outermost finally, after every response-file write path.
- No modification to `spawn-requests/queue.ts` (must-have 12): file not touched.
- No modification to `identity-birth-orchestrator.ts` (must-have 11): file not touched.
- Banned-strings gate: no alternate spelling of the user's name (see planner constraint — "user" is correct).
</verification>

<success_criteria>
- `processBirth` acquires at top with `{source:'spawn-request', bypassQueueDepth:true, requestId:item.uuid}` and releases in finally (must-have 5).
- Extended tests assert parallel-serialization and bypass semantics (must-have 8).
- `npx vitest run src/backend/spawn-requests/worker.test.ts` reports all tests passing.
- No changes to `identity-birth-orchestrator.ts` or `spawn-requests/queue.ts` (must-haves 11, 12).
</success_criteria>

<output>
Create `.planning/phases/110-global-throttle-on-identity-birth-flow-new-module-wrapping-b/110-03-SUMMARY.md` when done. SUMMARY should document:
- Whether the wrap-in-try/finally used the inline-shift-indentation approach or the hoist-into-doBirth-helper approach (and why).
- The two test-file env-var mutations and the restore discipline used.
- Any subtle race observed with the deferred-Promise pattern (e.g. how many microtask flushes were needed before "A-birth-start" appeared).
- Test count change (previous count → previous + 2).
</output>
