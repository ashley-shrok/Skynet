---
phase: 76-optimistic-bubbles-must-survive-the-wait-when-the-agent-was-
plan: 02
type: tdd
wave: 2
depends_on:
  - 76-01
files_modified:
  - src/ui/features/pretty-view/ChatMessage.tsx
  - src/ui/features/pretty-view/ChatMessage.test.tsx
autonomous: true
requirements:
  - D-06
tags:
  - failed-bubble-visual
  - whole-bubble-red
  - chat-message
  - pretty-view
  - semantic-upgrade

must_haves:
  truths:
    - "D-06: A user bubble in `pendingState === \"failed\"` renders with a WHOLE-BUBBLE red fill (saturated red background overrides the base blue-gray gradient), not just a red border + faint tint. Visual reads as \"truly failed, worth being loud about\" per Alice 2026-09-06 verbatim."
    - "`ChatMessage.tsx` `showFailedBubble` inline style at line 402-408 replaces the current muted `hsla(0, 60%, 55%, 0.4)` border + `hsla(0, 40%, 50%, 0.08)` background with a saturated red pair: `background` (opaque enough to override the base `linear-gradient(160deg,rgba(45,55,80,0.55),rgba(28,35,55,0.6))` on the className at line 451) + saturated red `borderColor`."
    - "The `data-pv-bubble-failed` attribute at ChatMessage.tsx:415 is preserved unchanged — existing Tests 4, 5, 6 in ChatMessage.test.tsx that assert on the attribute continue to pass."
    - "Assistant bubbles still ignore `pendingState === \"failed\"` (existing Test 5 invariant preserved)."
    - "Failed still supersedes sending (existing Test 6 invariant preserved) — spinner and failed remain mutually exclusive."
    - "New pin test asserts the exact CSS values (`background` or `backgroundColor` and `borderColor` from `bubbleInlineStyle` when `pendingState === \"failed\"`) match saturated-red target values — prevents silent regression of the visual."
  artifacts:
    - path: "src/ui/features/pretty-view/ChatMessage.tsx"
      provides: "Whole-bubble red inline style for pendingState=failed user bubbles"
      contains: "showFailedBubble"
      required_removed_pattern: "hsla(0, 40%, 50%, 0.08)"
    - path: "src/ui/features/pretty-view/ChatMessage.test.tsx"
      provides: "Pin test for whole-bubble red inline style values"
      contains: "whole-bubble red"
      min_added_lines: 15
  key_links:
    - from: "ChatMessage.tsx bubbleInlineStyle branch when showFailedBubble"
      to: "saturated red background + borderColor"
      via: "inline style overrides base className gradient"
      pattern: "showFailedBubble"
    - from: "ChatMessage.test.tsx new pin test"
      to: "computed style / inline style assertion"
      via: "querySelector('[data-pv-bubble-failed]').style"
      pattern: "data-pv-bubble-failed"
---

<objective>
Upgrade the failed-bubble visual from "muted red border + faint tint" to WHOLE-BUBBLE red fill per D-06 — Alice's original mental model of a failed message.

**Purpose:** Post-Plan 01, a failed pending-send bubble is a truly-failed event (backend spent its full 210s ceiling, gave up, client's 220s cap fired). Alice 2026-09-06 verbatim: *"hopefully after we do this work a failed bubble will be a truly failed bubble and that is worth being that loud about."* The current visual (muted border, 8%-alpha tint layered over the base blue-gray gradient) reads as "just a red border" — undersells the semantic weight. Change to a whole-bubble red fill that decisively overrides the base gradient.

**Output:** `src/ui/features/pretty-view/ChatMessage.tsx` `bubbleInlineStyle` (lines 402-408) uses saturated-red values that override the base gradient. Existing failed-bubble tests (ChatMessage.test.tsx Test 4, Test 5, Test 6) pass unchanged; a new pin test asserts the CSS values so silent drift is caught.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-optimistic-bubbles-must-survive-the-wait-when-the-agent-was-/76-CONTEXT.md
@.planning/phases/76-optimistic-bubbles-must-survive-the-wait-when-the-agent-was-/76-RESEARCH.md
</context>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure CSS/inline-style change to a leaf component. No new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-02-01 | Information Disclosure | Failed bubble visual color values | accept | Colors are hard-coded constants; no user-controlled or session-derived data influences the style. No PII surface. |
| T-76-02-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero new packages (RESEARCH.md § Standard Stack). No slopcheck gate needed. |

ASVS L1: N/A (V2-V6 all N/A — this is a CSS value change).

</threat_model>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — add ChatMessage.test.tsx pin test asserting whole-bubble red inline style values</name>
  <files>src/ui/features/pretty-view/ChatMessage.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/ChatMessage.test.tsx (existing Test 4 at L387-391 — failed styling assertion; Test 5 at L393-401 — assistant ignores pendingState; Test 6 at L403+ — mutual exclusivity)
    - src/ui/features/pretty-view/ChatMessage.tsx L390-408 (bubbleInlineStyle branch — the modification target), L410-416 (bubble div — `data-pv-bubble-failed` attribute site), L451-455 (user-bubble base className with gradient — what the whole-bubble red must defeat)
    - .planning/phases/76-optimistic-bubbles-must-survive-the-wait-when-the-agent-was-/76-CONTEXT.md § D-06 (Alice verbatim on visual intent)
    - .planning/phases/76-optimistic-bubbles-must-survive-the-wait-when-the-agent-was-/76-RESEARCH.md § "Whole-Bubble Red Visual — Current Failure State Rendering" (options analysis and target color rationale)
  </read_first>
  <behavior>
    - New test added to ChatMessage.test.tsx immediately after Test 4 (or grouped with Test 4/5/6 in the failed-bubble describe block), named `"Test 4b — whole-bubble red fill on pendingState=failed (D-06: saturated red overrides base gradient)"`
    - Test renders `<ChatMessage role="user" content="hello" pendingState="failed" />`
    - Query the bubble via `document.querySelector('[data-pv-bubble-failed]')` — must be non-null (mirrors Test 4 pattern)
    - Assert the bubble's inline style contains a saturated red background: read `bubble.style.backgroundColor` or `bubble.style.background` and assert it matches one of the target values. Suggested target values (executor picks the exact tuple in Task 2, test asserts the chosen tuple):
      - Option i: `background: "hsla(0, 60%, 35%, 0.90)"` — opaque enough to defeat the base gradient
      - Option ii: `backgroundColor: "hsl(0, 55%, 30%)"` — flat solid red
      - Either way: hue in the red range (0deg or 355-5deg), saturation >= 50%, alpha >= 0.85 (or opaque)
    - Assert the border color is a saturated red (contrast with the fill): e.g., `borderColor: "hsla(0, 70%, 50%, 0.8)"` or similar — hue 0deg range, saturation >= 60%, alpha >= 0.7
    - The exact numeric values are Task-2 discretion — Task 1's test is written AFTER a decision on the tuple (documented at the top of the test file or in the test's comment) so the assertion is deterministic
    - Under current (pre-Task-2) ChatMessage.tsx, this test MUST FAIL because the current values are `hsla(0, 60%, 55%, 0.4)` border and `hsla(0, 40%, 50%, 0.08)` background — the alpha 0.08 fails the "saturated" / high-alpha check
    - Existing Tests 4, 5, 6 continue to pass (not touched)
  </behavior>
  <action>
    Read the existing ChatMessage.test.tsx Test 4 at L387-391 as the structural template. Decide the target tuple BEFORE writing the test — pick one saturated-red combination and document it in a comment at the top of the new test. Recommended concrete values (executor may adjust with justification but must commit to specific numbers so the test is deterministic):
    - `background`: `"hsla(0, 60%, 35%, 0.90)"` — opaque-enough red that defeats the `linear-gradient(160deg,rgba(45,55,80,0.55),rgba(28,35,55,0.6))` base className gradient at ChatMessage.tsx:451.
    - `borderColor`: `"hsla(0, 70%, 50%, 0.85)"` — saturated red border that contrasts with the fill without looking like a separate outline.

    Add the new test as `it("Test 4b — whole-bubble red fill on pendingState=failed (D-06)", () => { ... })`. In the test: render, `const bubble = document.querySelector('[data-pv-bubble-failed]') as HTMLElement`, assert `bubble` not null, then assert BOTH `bubble.style.background` (or `bubble.style.backgroundColor` — whichever the executor chose in Task 2's implementation) matches the target value AND `bubble.style.borderColor` matches the target value. Use `expect(bubble.style.background).toBe("hsla(0, 60%, 35%, 0.9)")` — note that jsdom normalizes hsla by dropping trailing zeros (`0.90` → `0.9`). Test both `.style.background` and `.style.backgroundColor` (use whichever the implementation ends up setting).

    Run `npx vitest run src/ui/features/pretty-view/ChatMessage.test.tsx -t "Test 4b"` and capture the failing log to `/tmp/76-02-task1-red.log`. The expected failure: the assertion on `.style.background` (or `.style.backgroundColor`) fails because the current `bubbleInlineStyle` sets `backgroundColor: "hsla(0, 40%, 50%, 0.08)"` which does not match the target. Do NOT modify ChatMessage.tsx in this task.

    Commit with message `test(76-02): RED — add Test 4b pin test for whole-bubble red per D-06`. Commit body: reference Alice 2026-09-06 verbatim intent and the target CSS tuple decided.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/ChatMessage.test.tsx -t "Test 4b" 2>&amp;1 | tee /tmp/76-02-task1-red.log; grep -qE "expected .* to (be|equal)" /tmp/76-02-task1-red.log &amp;&amp; echo "RED CONFIRMED" || echo "RED NOT CONFIRMED — check log"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Test 4b" src/ui/features/pretty-view/ChatMessage.test.tsx` >= 1
    - `grep -cE "hsla\(0, 60%, 35%" src/ui/features/pretty-view/ChatMessage.test.tsx` >= 1 (or the executor-chosen equivalent saturated-red value, documented in commit body)
    - Test 4b exits non-zero: `npx vitest run src/ui/features/pretty-view/ChatMessage.test.tsx -t "Test 4b"` exit != 0
    - `/tmp/76-02-task1-red.log` contains a failing style-comparison assertion (either `expected "hsla(0, 40%, 50%, 0.08)" to be "hsla(0, 60%, 35%, 0.9)"` or equivalent — evidence that current inline style values do not match target)
    - Existing Tests 4, 5, 6 in ChatMessage.test.tsx still pass: `npx vitest run src/ui/features/pretty-view/ChatMessage.test.tsx -t "Test 4 " src/ui/features/pretty-view/ChatMessage.test.tsx -t "Test 5 " src/ui/features/pretty-view/ChatMessage.test.tsx -t "Test 6 "` exit 0
    - ChatMessage.tsx has ZERO diff in this task's commit (`git show HEAD -- src/ui/features/pretty-view/ChatMessage.tsx | wc -l` == 0)
    - Commit landed with prefix `test(76-02):`
  </acceptance_criteria>
  <done>Test 4b added; runs; fails on style-value assertion proving current bubble is not whole-bubble red; commit landed; ChatMessage.tsx unchanged in this commit.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — update ChatMessage.tsx bubbleInlineStyle to whole-bubble red per D-06</name>
  <files>src/ui/features/pretty-view/ChatMessage.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/ChatMessage.tsx L390-416 (the modification site: bubbleInlineStyle branch and bubble div with data-pv-bubble-failed attribute), L444-456 (the user-bubble className gradient that the inline style must override)
    - src/ui/features/pretty-view/ChatMessage.test.tsx Test 4b added in Task 1 — the test this task must turn GREEN
    - .planning/phases/76-optimistic-bubbles-must-survive-the-wait-when-the-agent-was-/76-CONTEXT.md § D-06 verbatim intent
    - .planning/phases/76-optimistic-bubbles-must-survive-the-wait-when-the-agent-was-/76-RESEARCH.md § "Whole-Bubble Red Visual" § "CSS approach options for the planner" (approach 1: inline-style `background` override — recommended because inline-style specificity beats the className's gradient)
  </read_first>
  <behavior>
    - `bubbleInlineStyle` when `showFailedBubble` returns an object with saturated-red `background` (or `backgroundColor` — see Action note below) AND saturated-red `borderColor`. The exact values MUST match what Task 1's Test 4b asserts.
    - The `position: "relative"` field is preserved (existing rendering depends on it).
    - The `data-pv-bubble-failed` attribute at L415 is UNCHANGED.
    - When `pendingState !== "failed"` (or role !== "user" per existing gate), `bubbleInlineStyle` continues to return `{ position: "relative" }` — no visual regression on non-failed bubbles.
    - After the change:
      - Test 4b passes (was RED in Task 1)
      - Test 4 (existing) passes — `data-pv-bubble-failed` attribute assertion unchanged
      - Test 5 (existing) passes — assistant bubbles still ignore `pendingState`
      - Test 6 (existing) passes — mutual exclusivity intact (spinner vs failed)
      - All tests in ChatMessage.test.tsx pass
  </behavior>
  <action>
    In `src/ui/features/pretty-view/ChatMessage.tsx` at lines 402-408, change the `showFailedBubble` branch of `bubbleInlineStyle`. Replace the current `borderColor: "hsla(0, 60%, 55%, 0.4)"` and `backgroundColor: "hsla(0, 40%, 50%, 0.08)"` with the saturated-red pair chosen in Task 1 and asserted by Test 4b.

    **Recommended concrete change (matches Task 1 recommended target):**
    - Use `background: "hsla(0, 60%, 35%, 0.90)"` instead of `backgroundColor` — this is critical: the `background` shorthand overrides the base className's `bg-[linear-gradient(...)]` because inline-style specificity beats Tailwind's utility class, AND `background` (shorthand) overrides `background-image` (which is what the gradient sets). Using `backgroundColor` alone would layer over the gradient rather than replace it — that's what the current implementation does and why the current visual reads as "just a border with tint."
    - Use `borderColor: "hsla(0, 70%, 50%, 0.85)"` — saturated red border that reads coherent with the fill.
    - Preserve `position: "relative"` as the first property.

    Update the comment header at ChatMessage.tsx:390-401 to reflect the new intent — replace the "muted-red palette per D-06 Discretion" wording (which is stale from a prior D-06 iteration) with a comment citing Phase 76 D-06 and Alice's 2026-09-06 verbatim intent: whole-bubble red on flip-to-failed, because a failed bubble post-Phase-76 is a truly-failed event (backend fully exhausted its ceiling, agent gave up).

    Do NOT change the `data-pv-bubble-failed` attribute at L415. Do NOT change the base user-bubble className at L444-455 (the gradient stays as-is for non-failed states). Do NOT touch the assistant-bubble path. Do NOT modify the `showFailedBubble = isUser && pendingState === "failed"` gate at L395.

    Run tests. Confirm Test 4b passes (Task 1's RED turns GREEN). Confirm Test 4, Test 5, Test 6 still pass. Run `npx vitest run src/ui/features/pretty-view/ChatMessage.test.tsx` — all tests green. Then run `npx tsc --noEmit`. Then run scoped sibling tests: `npx vitest run src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx` (per Phase 62 SUMMARY §Deviation — executor scope stops at scoped-tests-green).

    Commit with message `feat(76-02): GREEN — whole-bubble red fill on flip-to-failed per D-06`. Commit body: cite Alice verbatim intent, the exact CSS values chosen (background + borderColor), and reference the visual-language upgrade from "muted red border" to "decisive whole-bubble red."
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/ChatMessage.test.tsx 2>&amp;1 | tail -15; echo "---"; npx tsc --noEmit 2>&amp;1 | tail -5; echo "---"; echo "background-shorthand-present:"; grep -cE 'background: "hsla\(0,' src/ui/features/pretty-view/ChatMessage.tsx; echo "old-tint-removed:"; grep -c 'hsla(0, 40%, 50%, 0.08)' src/ui/features/pretty-view/ChatMessage.tsx; echo "data-attribute-preserved:"; grep -c 'data-pv-bubble-failed' src/ui/features/pretty-view/ChatMessage.tsx</automated>
    <human-check>End-of-phase UAT (per `.planning/config.json` `human_verify_mode: end-of-phase` — bundled into orchestrator's post-ship UAT, NOT a mid-phase pause): Alice on next post-Phase-76 dormant-wake failure (or synthetic force-fail via DevTools console delivering `paste_send_failed` frame) confirms the failed bubble reads as WHOLE-BUBBLE RED — the entire bubble surface is saturated red, not "blue-gray gradient with a red outline." If visual is still ambiguous, file a follow-up bounty and adjust the CSS values (hue/saturation/alpha). Reference CSS tuple shipped in SUMMARY.</human-check>
  </verify>
  <acceptance_criteria>
    - Test 4b passes (was RED in Task 1, now GREEN): `npx vitest run src/ui/features/pretty-view/ChatMessage.test.tsx -t "Test 4b"` exit 0
    - Test 4 still passes: `npx vitest run src/ui/features/pretty-view/ChatMessage.test.tsx -t "Test 4 "` exit 0
    - Test 5 still passes: `npx vitest run src/ui/features/pretty-view/ChatMessage.test.tsx -t "Test 5 "` exit 0
    - Test 6 still passes: `npx vitest run src/ui/features/pretty-view/ChatMessage.test.tsx -t "Test 6 "` exit 0
    - All tests in ChatMessage.test.tsx pass: `npx vitest run src/ui/features/pretty-view/ChatMessage.test.tsx` exit 0
    - PrettyView optimistic tests still pass (regression check): `npx vitest run src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx` exit 0
    - Typecheck clean: `npx tsc --noEmit` exit 0
    - Grep: old tint absent — `grep -c 'hsla(0, 40%, 50%, 0.08)' src/ui/features/pretty-view/ChatMessage.tsx` == 0
    - Grep: `data-pv-bubble-failed` attribute still present — `grep -c 'data-pv-bubble-failed' src/ui/features/pretty-view/ChatMessage.tsx` >= 1
    - Grep: saturated-red hue in red range (0deg) present in inline style block: `awk '/showFailedBubble/,/position: "relative" \}/' src/ui/features/pretty-view/ChatMessage.tsx | grep -cE 'hsla?\(0,\s*[5-9][0-9]%'` >= 1
    - Commit landed with prefix `feat(76-02):`
    - SUMMARY records the exact CSS values shipped so Alice's end-of-phase UAT has a deterministic reference tuple
  </acceptance_criteria>
  <done>Whole-bubble red fill shipped; Test 4b green; existing failed-bubble tests unchanged; sibling PrettyView optimistic tests unchanged; typecheck clean; commit landed; end-of-phase human-check marker embedded for orchestrator UAT bundle.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/ui/features/pretty-view/ChatMessage.test.tsx` — all tests pass including new Test 4b
- `npx vitest run src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx` — no regression (Plan 01's Test 5c stays green)
- `npx tsc --noEmit` — exit 0
- `grep -c 'hsla(0, 40%, 50%, 0.08)' src/ui/features/pretty-view/ChatMessage.tsx` == 0 (old tint removed)
- `grep -c 'data-pv-bubble-failed' src/ui/features/pretty-view/ChatMessage.tsx` >= 1 (attribute preserved)
- Git log shows commits in order: `test(76-02):` before `feat(76-02):` (TDD RED-then-GREEN)
</verification>

<success_criteria>
- **D-06** whole-bubble red on flip-to-failed: `bubbleInlineStyle` for failed user bubbles uses `background` (shorthand — beats the base className gradient) with saturated red hue + alpha >= 0.85, and saturated red borderColor. Alice's 2026-09-06 intent honored: "the whole bubble would just turn red instead of the blue hue that normal messages have."
- Existing failed-bubble tests (ChatMessage.test.tsx Test 4, Test 5, Test 6) unchanged and green.
- New pin test (Test 4b) locks the exact CSS values so a future refactor that accidentally regresses the visual catches it in CI.
- Plan 01's Test 5c stays green — no cross-plan regression.
- End-of-phase Alice UAT confirms the whole-bubble red visual matches the mental model (per `human-check` in Task 2's verify block; bundled into orchestrator's post-ship UAT bundle per `human_verify_mode: end-of-phase`).
</success_criteria>

<output>
Create `.planning/phases/76-optimistic-bubbles-must-survive-the-wait-when-the-agent-was-/76-02-SUMMARY.md` when done. SUMMARY must include:
- Exact CSS tuple shipped (`background`, `borderColor` values) — REQUIRED for Alice's end-of-phase UAT reference
- TDD evidence: RED log at `/tmp/76-02-task1-red.log`, GREEN test output
- Screenshot reference or DOM inspector note for Alice's UAT
- Note that the visual UAT is bundled into the end-of-phase orchestrator UAT (not a mid-phase pause)
</output>
