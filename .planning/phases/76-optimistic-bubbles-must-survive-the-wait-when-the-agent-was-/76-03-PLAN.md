---
phase: 76-optimistic-bubbles-must-survive-the-wait-when-the-agent-was-
plan: 03
type: tdd
wave: 2
depends_on:
  - 76-01
files_modified:
  - src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx
autonomous: true
requirements:
  - D-07
tags:
  - multi-send-verification
  - dormancy
  - reconnect-mid-dormancy
  - phase-62-claim-verification
  - pretty-view

must_haves:
  truths:
    - "D-07: Multi-send-during-wake is verified under real conditions in an in-process test — Phase 62's implementation claim (multiple pending sends during a wake all deliver in order when wake completes) is now proven, not assumed."
    - "The multi-send scenario includes reconnect-mid-dormancy setup (WS close + fresh WS attach on a dormant session, only `pane_state:dormant` re-delivered on the new WS) — exactly the failure mode Plan 01 fixed. Two sends land back-to-back, both arm the 220s branch (proving Plan 01's authoritative source works for the second send too), both clear in order when their matching user-role frames arrive."
    - "The test asserts: (1) after two `typeAndEnter` calls during dormancy-after-reconnect, `countPendingBubbles(container) === 2`; (2) both pending bubbles are in `state === \"sending\"` (spinner on the newest per D-04 latest-only rule); (3) delivering the FIRST matching `{type:\"message\", role:\"user\", content:\"message-one\"}` frame clears the FIRST pending in FIFO order; (4) delivering the SECOND matching frame clears the SECOND pending; (5) no failed bubbles at any point; (6) final state: two confirmed bubbles, zero pending bubbles."
    - "The test is a first-class deliverable, not a subordinate to Plan 01. If it fails, Phase 62's multi-send claim was wrong and the failure must be fixed before ship (loops back to a follow-up plan; do NOT ship a phase where D-07 is red)."
    - "This test is distinct from Plan 01's Test 5c — Test 5c proves the single-send reconnect-mid-dormancy scenario; this plan's Test 5d proves the multi-send version."
  artifacts:
    - path: "src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx"
      provides: "Test 5d — multi-send-during-wake verification with reconnect-mid-dormancy setup"
      contains: "Test 5d"
      min_added_lines: 50
  key_links:
    - from: "Test 5d"
      to: "two typeAndEnter calls during dormancy-after-reconnect"
      via: "sequential sends during widened-timeout window"
      pattern: "typeAndEnter\\(container, \"message-one\"\\)[\\s\\S]{0,2000}?typeAndEnter\\(container, \"message-two\"\\)"
    - from: "Test 5d"
      to: "two sendWsFrame calls delivering matching user-role message frames in order"
      via: "FIFO clear verification"
      pattern: "type: \"message\", role: \"user\""
---

<objective>
Verify Phase 62's multi-send-during-wake claim under real conditions in an in-process test — including the reconnect-mid-dormancy setup that Plan 01 fixed. D-07 is a first-class deliverable, not a nice-to-have.

**Purpose:** Ashley 2026-09-06 verbatim: *"that exact model was agreed upon when we first tried to implement this. So I imagine there's an attempt to have that already be happening in the current code. Although I don't think I've ever tried to send a follow-up message, so I can't really confirm if it works or not."* Verify, not assume. The intended user model is: two spinning bubbles in flight during a widened wait, both deliver in order when wake completes, no bubbles lost or reordered, no dedup collision. Plan 01 fixed the authoritative-source race for a single send; this plan proves the fix scales to multiple sends AND catches any latent Phase 62 multi-send bug (bounty tracker `pv-queue-op-dedup-doesnt-survive-wake-recycle` is a related-but-separate concern — this test does NOT exercise the wake-recycle case, only wake-completion).

**Output:** Test 5d added to `PrettyView.optimistic-bubbles.test.tsx` immediately after Test 5c (from Plan 01). Test drives: reconnect-mid-dormancy → two back-to-back sends during widened wait → two matching user-role frames arrive in order → both pendings clear in FIFO. If any assertion fails on current code (proving Phase 62 multi-send claim was wrong), Plan 03 escalates to a follow-up fix before ship.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-optimistic-bubbles-must-survive-the-wait-when-the-agent-was-/76-CONTEXT.md
@.planning/phases/76-optimistic-bubbles-must-survive-the-wait-when-the-agent-was-/76-RESEARCH.md
@.planning/phases/76-optimistic-bubbles-must-survive-the-wait-when-the-agent-was-/76-01-PLAN.md
</context>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Test-only file addition. No production trust boundary changed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-03-01 | N/A | Test scaffolding | accept | Pure test file addition; no runtime code, no attacker surface. |
| T-76-03-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero new packages. |

ASVS L1: N/A (test file only).

</threat_model>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Add Test 5d — multi-send during widened wait with reconnect-mid-dormancy setup (D-07 verification)</name>
  <files>src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx (existing Test 5b at L391 dormant-branch defer, Test 5c added by Plan 01 for reconnect-mid-dormancy single send, Test 4 at L322 FIFO tiebreaker pattern for two-pending scenarios, Test 12 at L646 WS close pattern for reconnect drive)
    - src/ui/features/pretty-view/PrettyView.tsx L1225-1300 (handleOptimisticSend arm + pendingSends state machine — pendingSends is FIFO, matched by head content equality in Test 3 pattern), L1840-1860 (case "message" role:"user" cleanup with `matched_by=fifo-head-match` diagnostic — the FIFO-head match path)
    - .planning/phases/76-optimistic-bubbles-must-survive-the-wait-when-the-agent-was-/76-CONTEXT.md § D-07 (Ashley verbatim on multi-send model)
    - .planning/phases/76-optimistic-bubbles-must-survive-the-wait-when-the-agent-was-/76-RESEARCH.md § "Multi-Send-During-Wake Test Scenario Feasibility" (harness supports wsStubs multi-WS pattern)
    - .planning/phases/76-optimistic-bubbles-must-survive-the-wait-when-the-agent-was-/76-01-PLAN.md § Task 1 behavior (Test 5c pattern to extend)
  </read_first>
  <behavior>
    - Test 5d added to PrettyView.optimistic-bubbles.test.tsx immediately after Test 5c (the reconnect-mid-dormancy single-send test from Plan 01)
    - Test name: `"Test 5d: multi-send during widened wait after reconnect-mid-dormancy — two sends arm 220s branch, both clear in FIFO order when matching frames arrive (D-07 verification of Phase 62 multi-send claim)"`
    - Uses `vi.useFakeTimers()`
    - Sequence:
      1. Mount PrettyView, get `ws1 = getCurrentWs()`, call `flipToStreaming(ws1)`
      2. Establish dormancy: `sendWsFrame(ws1, { type: "pane_state", state: "dormant" })` AND `sendWsFrame(ws1, { type: "dormant", dormant: true })`
      3. `await act(async () => { await Promise.resolve(); })`
      4. Simulate WS close: `act(() => { ws1.readyState = 3; ws1.onclose?.(); })`
      5. `await act(async () => { await Promise.resolve(); })` — new WS pushed
      6. `ws2 = getCurrentWs()`; assert `ws2 !== ws1`; call `flipToStreaming(ws2)`
      7. Deliver ONLY `sendWsFrame(ws2, { type: "pane_state", state: "dormant" })` — Signal B path per Plan 01 authoritative source
      8. `await act(async () => { await Promise.resolve(); })`
      9. First send: `typeAndEnter(container, "message-one")`; `await act(async () => { await Promise.resolve(); })`; assert `countPendingBubbles(container) === 1`
      10. Second send: `typeAndEnter(container, "message-two")`; `await act(async () => { await Promise.resolve(); })`; assert `countPendingBubbles(container) === 2`
      11. Assert no failed bubbles: `expect(container.querySelectorAll("[data-pv-bubble-failed]").length).toBe(0)`
      12. Advance past normal 20s ceiling: `vi.advanceTimersByTime(20001)`; assert BOTH pendings still exist (`countPendingBubbles === 2`) and no failed bubbles — proves BOTH sends armed the 220s branch (D-08 unified for both)
      13. Deliver first matching frame: `sendWsFrame(ws2, { type: "message", role: "user", content: "message-one", eventId: 1001, line: 1 })`; `await act(async () => { await Promise.resolve(); })`
      14. Assert `countPendingBubbles === 1` — first pending cleared in FIFO order (Test 3 pattern)
      15. Assert `countConfirmedBubbles === 1` — first message now confirmed
      16. Deliver second matching frame: `sendWsFrame(ws2, { type: "message", role: "user", content: "message-two", eventId: 1002, line: 2 })`; `await act(async () => { await Promise.resolve(); })`
      17. Assert `countPendingBubbles === 0` — both cleared
      18. Assert `countConfirmedBubbles === 2` — both confirmed
      19. Assert `container.querySelectorAll("[data-pv-bubble-failed]").length === 0` — no failure anywhere
    - After Plan 01 lands, this test MUST PASS. If it FAILS on Plan 01's HEAD, that means Phase 62's multi-send claim is wrong — escalate to a follow-up plan before the phase ships.
    - The eventId + line values (1001/1, 1002/2) are arbitrary but must be unique per Test 3 dedup patterns and different from any prior test's IDs to avoid cross-test dedup collisions.
  </behavior>
  <action>
    Read Test 5c (added in Plan 01 Task 1) as the reconnect-mid-dormancy structural template. Extend it with a SECOND `typeAndEnter` call and a SECOND `sendWsFrame` matching-message delivery, following the FIFO cleanup pattern from Test 3 at L252 and Test 4 at L322. The two sends use content strings `"message-one"` and `"message-two"` — chosen to be distinct (unlike Test 4 which uses identical content to exercise the tiebreaker); this test exercises the ordered-clear path where the head-match content equality distinguishes them clearly.

    The eventId/line values for the two matching frames must be unique and non-colliding with any other test in the file. Search for existing `eventId:` values with `grep -oE 'eventId:\s*[0-9]+' src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx | sort -u` — pick two values above the highest existing one (e.g., 1001 and 1002 if nothing is above 999).

    Run the test: `npx vitest run src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx -t "Test 5d"`.

    **Expected outcome:** Test PASSES on the HEAD that includes Plan 01's `feat(76-01)` commit. If Test 5d passes, D-07 verification is complete — Phase 62's multi-send claim holds under the reconnect-mid-dormancy scenario, and this plan's execution is straightforward.

    **If Test 5d FAILS:** Capture the failure log to `/tmp/76-03-task1-failure.log` and analyze. Common failure modes to expect:
    - Second send arms 20s branch (not 220s) — would mean Plan 01's fix works for the FIRST send only; investigation needed on why the second send in the same widened wait window sees a different `dormantRef.current` value. Possibly a race where the first `handleOptimisticSend` transiently updates state that clears `dormant`. Report the mechanism.
    - Second matching frame doesn't clear the second pending — FIFO head-match logic bug. Would be a real Phase 62 defect that requires a follow-up fix.
    - Both pendings clear but out of order — FIFO discipline bug.
    - Dedup Map suppresses the second matching frame — the sister bounty `pv-queue-op-dedup-doesnt-survive-wake-recycle` mechanism biting. Note this is different from the deferred wake-recycle case; it would be a dedup gap on ordinary wake-completion delivery.

    In the FAILURE case: do NOT modify PrettyView.tsx to make the test pass — that would exceed this plan's scope (behavior fix would be a new plan). Instead:
    1. Commit the failing test as evidence: `test(76-03): D-07 FAILURE — multi-send during reconnect-mid-dormancy does not deliver in order (mechanism: <describe>)`
    2. Halt execution and return `## D-07 VERIFICATION FAILED` to the orchestrator with the specific assertion that failed and the mechanism hypothesis
    3. Orchestrator surfaces to the user + spawns a follow-up plan to fix the underlying bug

    In the PASS case: commit with message `test(76-03): D-07 verification — multi-send during reconnect-mid-dormancy delivers in FIFO order (Phase 62 claim upheld)`. No production code changes in this plan.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx -t "Test 5d" 2>&amp;1 | tee /tmp/76-03-task1-run.log; echo "---"; grep -c "Test 5d" src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx; echo "---"; grep -cE 'typeAndEnter\(container, "message-(one|two)"\)' src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Test 5d" src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx` >= 1
    - `grep -cE 'typeAndEnter\(container, "message-one"\)' src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx` >= 1
    - `grep -cE 'typeAndEnter\(container, "message-two"\)' src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx` >= 1
    - `grep -cE 'type: "message", role: "user", content: "message-one"' src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx` >= 1
    - `grep -cE 'type: "message", role: "user", content: "message-two"' src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx` >= 1
    - `grep -c 'countPendingBubbles(container)).toBe(2)' src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx` >= 1 (two pendings after both sends)
    - Test 5d runs to completion: `npx vitest run src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx -t "Test 5d"` — exit 0 if PASS (D-07 verified) OR exit != 0 if FAIL (evidence captured for follow-up)
    - IF Test 5d PASSES: `/tmp/76-03-task1-run.log` shows `1 pass` for Test 5d; commit landed with prefix `test(76-03):` and message notes D-07 verification passed
    - IF Test 5d FAILS: `/tmp/76-03-task1-failure.log` captures the specific assertion failure; commit landed with prefix `test(76-03):` and message names the failure mechanism; orchestrator alerted via `## D-07 VERIFICATION FAILED` structured return
    - No production code (src/ui/features/pretty-view/PrettyView.tsx, src/ui/features/pretty-view/ChatMessage.tsx) modified in Plan 03's commit: `git show HEAD -- 'src/ui/features/pretty-view/PrettyView.tsx' 'src/ui/features/pretty-view/ChatMessage.tsx' | wc -l` == 0
    - No regression to prior tests: `npx vitest run src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx` — all prior tests (5, 5b, 5c) still exit 0
  </acceptance_criteria>
  <done>Test 5d added; runs on Plan 01's HEAD; either PASSES (D-07 verified — commit lands with verification message) or FAILS with mechanism documented (evidence commit lands, orchestrator alerted). Zero production code changes.</done>
</task>

</tasks>

<verification>
- Test 5d exists in PrettyView.optimistic-bubbles.test.tsx
- Test 5d runs to completion (pass or fail — outcome is the verification result)
- `npx vitest run src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx` — Test 5 / 5b / 5c still pass (regression check)
- No production code modified in this plan's commit
- If Test 5d fails: orchestrator receives `## D-07 VERIFICATION FAILED` structured return with mechanism hypothesis
</verification>

<success_criteria>
- **D-07 verification:** Multi-send-during-wake with reconnect-mid-dormancy is proven under real conditions in an in-process test. Phase 62's claim is either UPHELD (test passes → verification complete → ship) or FALSIFIED (test fails → mechanism documented → follow-up plan spawns before ship).
- No production code changes — this plan is a verification-only artifact. If a production fix is needed, it lives in a follow-up plan spawned from this plan's failure evidence.
</success_criteria>

<output>
Create `.planning/phases/76-optimistic-bubbles-must-survive-the-wait-when-the-agent-was-/76-03-SUMMARY.md` when done. SUMMARY must include:
- Test 5d outcome (PASS or FAIL)
- If FAIL: specific assertion that failed + mechanism hypothesis + escalation notice
- If PASS: D-07 verification confirmed, Phase 62 multi-send claim upheld under reconnect-mid-dormancy conditions
- Test count delta and evidence log paths
</output>
