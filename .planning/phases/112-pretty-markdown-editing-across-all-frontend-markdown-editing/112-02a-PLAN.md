---
phase: 111-pretty-markdown-editing-across-all-frontend-markdown-editing
plan: 02a
type: execute
wave: 2
depends_on:
  - 112-01
files_modified:
  - src/ui/features/pretty-view/GlobalFileTab.tsx
  - src/ui/features/pretty-view/SkillFileTab.tsx
  - src/ui/features/pretty-view/GlobalFilesModal.tsx
  - src/ui/features/pretty-view/SkillsEditorModal.tsx
  - src/ui/features/pretty-view/RunbookEditorModal.tsx
  - src/ui/features/pretty-view/EditableFileModal.tsx
  - src/ui/features/pretty-view/GlobalFileTab.test.tsx
  - src/ui/features/pretty-view/SkillFileTab.test.tsx
autonomous: true
requirements:
  - D-06
  - D-08
  - D-15
  - D-16

must_haves:
  truths:
    - "Global and Skill file-tab surfaces (the filetype-gated pair) are thin wrappers over the shared MarkdownEditor (per D-08)"
    - "GlobalFileTab and SkillFileTab accept a new required `filename: string` prop (per D-06 plumbing)"
    - "The four modals that mount these two tabs (GlobalFilesModal, SkillsEditorModal, RunbookEditorModal, EditableFileModal) pass filename to their tab child (per D-06 threading)"
    - "GlobalFileTab.test.tsx has new integration cases asserting filetype gate: .md filename → mocked MDXEditor renders; .json filename → textarea renders (per D-15)"
    - "SkillFileTab.test.tsx has the same new integration cases (per D-15)"
    - "The mtime-reseed effect, onDraftChange divergence effect, handleSave optimistic-concurrency signature, loading/error branches, and save-button chrome in Global/Skill tabs are unchanged"
    - "Existing vitest suites for the Global/Skill file tabs still pass after refactor (per D-16)"
  artifacts:
    - path: "src/ui/features/pretty-view/GlobalFileTab.tsx"
      provides: "Thin wrapper — new required filename prop threaded to MarkdownEditor"
    - path: "src/ui/features/pretty-view/SkillFileTab.tsx"
      provides: "Thin wrapper — new required filename prop threaded to MarkdownEditor"
  key_links:
    - from: "src/ui/features/pretty-view/GlobalFilesModal.tsx"
      to: "src/ui/features/pretty-view/GlobalFileTab.tsx"
      via: "filename={file.path} prop"
      pattern: "filename=\\{file\\.path\\}"
    - from: "src/ui/features/pretty-view/SkillsEditorModal.tsx"
      to: "src/ui/features/pretty-view/SkillFileTab.tsx"
      via: "filename={file.path} prop"
      pattern: "filename=\\{file\\.path\\}"
    - from: "src/ui/features/pretty-view/RunbookEditorModal.tsx"
      to: "src/ui/features/pretty-view/SkillFileTab.tsx"
      via: "filename={file.path} prop"
      pattern: "filename=\\{file\\.path\\}"
    - from: "src/ui/features/pretty-view/EditableFileModal.tsx"
      to: "src/ui/features/pretty-view/GlobalFileTab.tsx"
      via: "filename={filename} prop"
      pattern: "filename=\\{filename\\}"
    - from: "src/ui/features/pretty-view/GlobalFileTab.tsx"
      to: "src/ui/features/pretty-view/MarkdownEditor.tsx"
      via: "named import + JSX with threaded filename"
      pattern: "<MarkdownEditor[^>]*filename=\\{filename\\}"
    - from: "src/ui/features/pretty-view/SkillFileTab.tsx"
      to: "src/ui/features/pretty-view/MarkdownEditor.tsx"
      via: "named import + JSX with threaded filename"
      pattern: "<MarkdownEditor[^>]*filename=\\{filename\\}"
---

<objective>
Adopt the shared `MarkdownEditor` (built in Plan 01) into the two FILETYPE-GATED file-tab surfaces (Global + Skill) and thread `filename` down from the four hosting modals. This is the D-06 half of the D-08 consolidation — the side where the extension determines whether the pretty editor or the plain textarea renders.

Purpose: Deliver the pretty-editor experience to the load-bearing file-editing surfaces where mixed content types coexist (Global files can be `.md` / `.json` / etc.; Skill files can be `.md` / `.sh` / etc.), while preserving the TabState<T> contract, the mtime optimistic-concurrency shape, the onDraftChange divergence signalling, and every existing test's intent.

Split note: this plan is 02a of a 02a/02b pair. 02b covers the always-`.md` Identity + Role tab side. Both shards target wave 2 and run in parallel with Plan 03 (BountyCard + AddWakeupDialog). Shard boundary chosen so each shard modifies a disjoint file set and can be executor-parallelised if desired.

Output: Two modified tab files, four modified modal call sites, updated tests for Global/Skill tabs, all committed atomically. `EditableFileModal` (delegates through GlobalFileTab) and `RunbookEditorModal` (delegates through SkillFileTab) ride along automatically.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/112-pretty-markdown-editing-across-all-frontend-markdown-editing/112-CONTEXT.md
@.planning/phases/112-pretty-markdown-editing-across-all-frontend-markdown-editing/112-RESEARCH.md
@.planning/phases/112-pretty-markdown-editing-across-all-frontend-markdown-editing/112-PATTERNS.md
@.planning/phases/112-pretty-markdown-editing-across-all-frontend-markdown-editing/112-01-SUMMARY.md
@src/ui/features/pretty-view/GlobalFileTab.tsx
@src/ui/features/pretty-view/SkillFileTab.tsx
@src/ui/features/pretty-view/GlobalFilesModal.tsx
@src/ui/features/pretty-view/SkillsEditorModal.tsx
@src/ui/features/pretty-view/RunbookEditorModal.tsx
@src/ui/features/pretty-view/EditableFileModal.tsx
@src/ui/features/pretty-view/GlobalFileTab.test.tsx
@src/ui/features/pretty-view/SkillFileTab.test.tsx
@src/ui/features/pretty-view/MarkdownEditor.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor GlobalFileTab + SkillFileTab (add `filename` prop, swap textarea for MarkdownEditor), thread filename from GlobalFilesModal / SkillsEditorModal / RunbookEditorModal / EditableFileModal, extend tests</name>
  <files>
    src/ui/features/pretty-view/GlobalFileTab.tsx, src/ui/features/pretty-view/SkillFileTab.tsx, src/ui/features/pretty-view/GlobalFilesModal.tsx, src/ui/features/pretty-view/SkillsEditorModal.tsx, src/ui/features/pretty-view/RunbookEditorModal.tsx, src/ui/features/pretty-view/EditableFileModal.tsx, src/ui/features/pretty-view/GlobalFileTab.test.tsx, src/ui/features/pretty-view/SkillFileTab.test.tsx
  </files>
  <read_first>
    - .planning/phases/112-pretty-markdown-editing-across-all-frontend-markdown-editing/112-CONTEXT.md (D-06, D-07, D-08, D-15, D-16 — the load-bearing decisions)
    - .planning/phases/112-pretty-markdown-editing-across-all-frontend-markdown-editing/112-PATTERNS.md (§GlobalFileTab.tsx MOD; §SkillFileTab.tsx MOD; §GlobalFilesModal.tsx MOD; §SkillsEditorModal.tsx MOD; §RunbookEditorModal.tsx MOD; §EditableFileModal.tsx MOD; §Shared Pattern F vitest style)
    - .planning/phases/112-pretty-markdown-editing-across-all-frontend-markdown-editing/112-01-SUMMARY.md (learn what Plan 01 chose for the key= remount strategy)
    - src/ui/features/pretty-view/GlobalFileTab.tsx (current shape — L22-27 props destructure; L45-51 mtime reseed effect; L57-61 onDraftChange effect; L63-74 handleSave; L77-93 loading/error; L106-111 textarea to swap; L115-124 save button chrome)
    - src/ui/features/pretty-view/SkillFileTab.tsx (current shape — mirror of GlobalFileTab; L100-112 isText placeholder branch to preserve; L120-125 textarea to swap; L129-137 Trash2 delete button to preserve)
    - src/ui/features/pretty-view/GlobalFilesModal.tsx (call site — L316-321)
    - src/ui/features/pretty-view/SkillsEditorModal.tsx (call site — L570-579)
    - src/ui/features/pretty-view/RunbookEditorModal.tsx (call site — L444 mirror comment; L456-465)
    - src/ui/features/pretty-view/EditableFileModal.tsx (call site — L194 filename in scope; L487-491 GlobalFileTab call)
    - src/ui/features/pretty-view/GlobalFileTab.test.tsx (test patterns to mirror + extend)
    - src/ui/features/pretty-view/SkillFileTab.test.tsx (test patterns to mirror + extend)
    - src/ui/features/pretty-view/MarkdownEditor.tsx (the component being adopted — confirm the exact prop names and named-export shape)
  </read_first>
  <behavior>
    New test cases added to GlobalFileTab.test.tsx and SkillFileTab.test.tsx (RED → GREEN):
    - Test A (both files): rendered with filename="notes.md" and status="ready" → the rendered subtree contains the mocked MDXEditor test-id (import the same vi.mock strategy from MarkdownEditor.test.tsx for `@mdxeditor/editor`), NOT a raw textarea.
    - Test B (both files): rendered with filename="settings.json" and status="ready" → the rendered subtree contains a `<textarea>` (getByRole('textbox')), NOT the mocked MDXEditor testid.
    - Test C (both files): existing tests that call `getByRole('textbox')` on `.md` files must be updated to work with the mocked MDXEditor OR the test fixtures must be given non-.md filenames so they continue to hit the textarea branch. Preserve the ORIGINAL test intent (save flow, dirty tracking, etc.) — the mock stub should surface `data-testid="mdxeditor"` and echo `props.markdown`, so tests can still assert content passthrough without exercising Lexical.
    Existing tests: MUST still pass with their original assertions (subject to the fixture-filename adjustment above).
  </behavior>
  <action>
    Step 1 — Extend GlobalFileTab.test.tsx and SkillFileTab.test.tsx with the `@mdxeditor/editor` mock. Copy the `vi.mock("@mdxeditor/editor", () => ({...}))` block from `MarkdownEditor.test.tsx` (created in Plan 01) verbatim — same stub shape. Place it at the top of the test file, before the `import GlobalFileTab from "./GlobalFileTab"` / `import SkillFileTab from "./SkillFileTab"` line. Add the new Test A and Test B cases inside a new `describe("filetype gate integration (D-06)", () => { ... })` block. Also audit existing tests: any fixture that passes `state: { status: 'ready', data: { content: '...', mtime: ... } }` WITHOUT rendering the tab with a filename prop — those tests now need a `filename={"…"}` prop passed. Pick `filename="fixture.md"` for tests that want to exercise the pretty branch (mocked MDXEditor accepts markdown prop), OR `filename="fixture.txt"` for tests that want the textarea. Adjust each existing test's expectations accordingly — the mocked MDXEditor renders a `<div data-testid="mdxeditor">…</div>` not a `<textarea>`, so `getByRole('textbox')` won't find it. Keep the change minimal: for tests whose intent is state-management (dirty, save-disabled), the textarea branch is the natural fit; for tests whose intent is the visual editor renders, use the .md branch.

    Run: `npx vitest run src/ui/features/pretty-view/GlobalFileTab.test.tsx src/ui/features/pretty-view/SkillFileTab.test.tsx` — expect failures on the new Test A / Test B because production code doesn't yet accept `filename`.

    Step 2 — Modify `GlobalFileTab.tsx`. Add `filename: string` to the props interface at L22-27. Insert `filename` into the destructure at L21. Import `MarkdownEditor` from `"./MarkdownEditor"` at the top. Replace the textarea at L106-111 with `<MarkdownEditor filename={filename} content={draft} onChange={setDraft} disabled={saving} />`. Preserve everything else verbatim: the mtime-reseed effect (L45-51), the onDraftChange effect (L57-61), handleSave (L63-74), loading/error branches (L77-93), and the save button chrome (L115-124). Do NOT move the Save button styling into MarkdownEditor.

    Step 3 — Modify `SkillFileTab.tsx`. Same shape as Step 2 — add `filename: string` to props, thread it through, import MarkdownEditor, swap L120-125 textarea for `<MarkdownEditor filename={filename} content={draft} onChange={setDraft} disabled={saving} />`. Preserve two SkillFileTab-specific branches unchanged: (a) the isText placeholder branch at L100-112 that fires BEFORE the editor when `data.isText === false` (do NOT move this into MarkdownEditor — it's a tab-level policy about non-text files), and (b) the Trash2 delete-file button at L129-137.

    Step 4 — Modify `GlobalFilesModal.tsx` at L316-321. Add `filename={file.path}` as a JSX attribute to the `<GlobalFileTab …>` call. `file.path` is already in scope (used at L312 key, L313 value, L319 onSave arg). One-line change.

    Step 5 — Modify `SkillsEditorModal.tsx` at L570-579. Add `filename={file.path}` to the `<SkillFileTab …>` call. Same pattern as Step 4.

    Step 6 — Modify `RunbookEditorModal.tsx` at L456-465. Add `filename={file.path}` to the `<SkillFileTab …>` call. Same pattern as Step 4.

    Step 7 — Modify `EditableFileModal.tsx` at L487-491. Add `filename={filename}` to the `<GlobalFileTab …>` call. `filename` is already in scope at the component-prop level (L194). One-line change.

    Step 8 — Run the extended pretty-view scoped test suite (Global/Skill-side only — plan 02b handles Identity/Role): `npx vitest run src/ui/features/pretty-view/GlobalFileTab.test.tsx src/ui/features/pretty-view/SkillFileTab.test.tsx`. All tests MUST pass. If existing tests fail because they render a tab without a filename prop, revisit Step 1's test adjustments — the fix is at the TEST call site, not by making filename optional (D-06 makes it required).

    Step 9 — Type-check: `npx tsc --noEmit -p tsconfig.app.json` MUST exit 0. TypeScript will catch any missing `filename={…}` at a call site that we missed.

    Note on shard boundary: because IdentityFileTab (plan 02b) also imports MarkdownEditor once 02b lands, the type-check in this plan tolerates 02b not yet being present — MarkdownEditor already exists from plan 01, so filename-threading here is self-consistent. If 02a and 02b run in parallel, the shared file boundary is `MarkdownEditor.tsx` (read-only from both) — no write conflict.

    Step 10 — Commit as atomic units:
    (a) `test(112-02a): extend Global/Skill FileTab tests for filetype-gate integration`
    (b) `refactor(112-02a): thread filename into GlobalFileTab + SkillFileTab, adopt MarkdownEditor`
    (c) `refactor(112-02a): pass filename={file.path} from Global/Skills/Runbook/EditableFile modals`
    (Merge (b) and (c) into one commit if the wiring is trivially co-dependent and splitting would leave one commit broken at TypeScript check.)
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/GlobalFileTab.test.tsx src/ui/features/pretty-view/SkillFileTab.test.tsx</automated>
    <automated>npx tsc --noEmit -p tsconfig.app.json</automated>
    <automated>grep -q "filename: string" src/ui/features/pretty-view/GlobalFileTab.tsx</automated>
    <automated>grep -q "filename: string" src/ui/features/pretty-view/SkillFileTab.tsx</automated>
    <automated>grep -q "filename={file.path}" src/ui/features/pretty-view/GlobalFilesModal.tsx</automated>
    <automated>grep -q "filename={file.path}" src/ui/features/pretty-view/SkillsEditorModal.tsx</automated>
    <automated>grep -q "filename={file.path}" src/ui/features/pretty-view/RunbookEditorModal.tsx</automated>
    <automated>grep -q "filename={filename}" src/ui/features/pretty-view/EditableFileModal.tsx</automated>
    <automated>grep -q "import { MarkdownEditor } from \"./MarkdownEditor\"" src/ui/features/pretty-view/GlobalFileTab.tsx</automated>
    <automated>grep -q "import { MarkdownEditor } from \"./MarkdownEditor\"" src/ui/features/pretty-view/SkillFileTab.tsx</automated>
    <automated>! grep -q "min-h-\[400px\]" src/ui/features/pretty-view/GlobalFileTab.tsx</automated>
    <automated>! grep -q "min-h-\[400px\]" src/ui/features/pretty-view/SkillFileTab.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `GlobalFileTab.tsx` and `SkillFileTab.tsx` no longer contain a raw `<textarea>` element for the editor body — the verbatim class string `min-h-[400px]` (part of the pre-refactor textarea markup) is gone from both files.
    - Both files import `{ MarkdownEditor }` from `"./MarkdownEditor"` and use it in the ready branch.
    - Both files' props interfaces list `filename: string` as required.
    - `GlobalFilesModal`, `SkillsEditorModal`, `RunbookEditorModal`, `EditableFileModal` all pass `filename` prop to their child tab.
    - Existing test suites for Global/Skill file tabs pass with the fixture-filename adjustment.
    - New `describe("filetype gate integration (D-06)", …)` block adds ≥2 tests each in GlobalFileTab.test.tsx and SkillFileTab.test.tsx.
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0.
    - Save-flow, dirty-tracking, mtime-reseed, and onDraftChange behaviours untouched (verified by original tests passing).
    - NO file listed under Plan 02b's `files_modified` (IdentityFileTab.tsx, RoleFileTab.tsx, RoleFileTab.test.tsx) is modified by this plan.
  </acceptance_criteria>
  <done>
    Four call sites thread filename to Global/Skill file tabs; the tabs adopt MarkdownEditor; existing behaviours preserved; tests green. EditableFileModal and RunbookEditorModal ride along automatically. Identity/Role side is handled by 02b.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Modal → tab → MarkdownEditor | filename propagates from `file.path` (backend-sourced, trusted-side) through UI props to the editor gate; not a user-input attack surface |
| Tab draft state → save handler → backend PUT | UNCHANGED from pre-refactor — the shared component doesn't alter the save contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-02a-01 | Tampering | filename prop injection | accept | `file.path` comes from the backend's own file-listing endpoints (globalListDirectory, skillsListFiles). No user-controlled path enters the gate. Existing backend path-traversal defences (unchanged per D-10) remain the authority. |
| T-112-02a-02 | Information Disclosure | onSave signature preservation | mitigate | Preserving the exact `(content, expectedMtime)` signature for Global/Skill tabs means optimistic-concurrency check on the backend still fires; TypeScript checks (part of `<verify>`) enforce the signature at every call site. |
| T-112-02a-03 | Denial of Service | MarkdownEditor lazy-chunk load failure on modal open | accept | Handled by the Suspense fallback in Plan 01 (renders the raw textarea style). If the chunk 404s, the user sees a read-only textarea, not a crash. Pitfall 4 in RESEARCH suggests an ErrorBoundary but Skynet doesn't yet have an established one — deferred to a future polish pass. |
</threat_model>

<verification>
Plan-level verification:
- Filetype gate hit at each of the four hosting call sites (integration tests A and B for Global/Skill).
- TabState<T> contract preserved (imported from IdentityFileTab.tsx — plan 02b keeps the export byte-identical).
- Existing test suites green — the regression contract per D-16.
- TypeScript strict check clean — filename prop is required, so any missed call site fails compilation.
</verification>

<success_criteria>
- Opening a `.md` file in the global-files modal shows the pretty MDXEditor.
- Opening a `.json` file in the global-files modal shows the plain textarea (unchanged from today).
- Opening a skill `.md` file in the skills-editor modal shows the pretty editor.
- Opening a skill `.sh` file shows the plain textarea.
- Save flows behave exactly as before (mtime concurrency, onDraftChange, error surfacing).
- Global/Skill test suites pass.
</success_criteria>

<output>
Create `.planning/phases/112-pretty-markdown-editing-across-all-frontend-markdown-editing/112-02a-SUMMARY.md` capturing:
- Which existing tests needed fixture-filename adjustments (list them by file + test name).
- Any TypeScript strict check surprises (missing prop at a call site not in the plan?).
- Total lines removed from GlobalFileTab.tsx + SkillFileTab.tsx (should be significant — the verbatim textarea + duplicate class strings collapse).
- Whether the shard ran in parallel with 02b or sequentially, and if any file-boundary friction surfaced.
</output>
</content>
