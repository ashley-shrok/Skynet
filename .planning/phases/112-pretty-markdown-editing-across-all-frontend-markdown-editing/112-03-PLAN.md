---
phase: 111-pretty-markdown-editing-across-all-frontend-markdown-editing
plan: 03
type: execute
wave: 2
depends_on:
  - 112-01
files_modified:
  - src/ui/features/pretty-view/BountyCard.tsx
  - src/ui/features/pretty-view/AddWakeupDialog.tsx
  - src/ui/features/pretty-view/AddWakeupDialog.test.tsx
autonomous: true
requirements:
  - D-06
  - D-07
  - D-08
  - D-16

must_haves:
  truths:
    - "BountyCard premise field renders <MarkdownEditor filename='premise.md' …> in edit mode (per Claude's Discretion — synthetic filename forces always-pretty per D-06/D-07)"
    - "AddWakeupDialog instruction field renders <MarkdownEditor filename='wakeup.md' …> (synthetic filename, always-pretty)"
    - "AddWakeupDialog min-height on the instruction field is raised to accommodate MDXEditor's ~40px toolbar (per RESEARCH Open Question 2 recommendation)"
    - "BountyCard's premise save/cancel/Escape/Cmd+Enter shortcuts still work"
    - "Existing AddWakeupDialog test suite still passes (per D-16)"
  artifacts:
    - path: "src/ui/features/pretty-view/BountyCard.tsx"
      provides: "Premise <Textarea> replaced by <MarkdownEditor filename='premise.md' …>"
    - path: "src/ui/features/pretty-view/AddWakeupDialog.tsx"
      provides: "Instruction <textarea> replaced by <MarkdownEditor filename='wakeup.md' …> inside a min-h-[160px] wrapper"
  key_links:
    - from: "src/ui/features/pretty-view/BountyCard.tsx"
      to: "src/ui/features/pretty-view/MarkdownEditor.tsx"
      via: "named import + JSX with synthetic filename"
      pattern: "<MarkdownEditor[^>]*filename=\"premise\\.md\""
    - from: "src/ui/features/pretty-view/AddWakeupDialog.tsx"
      to: "src/ui/features/pretty-view/MarkdownEditor.tsx"
      via: "named import + JSX with synthetic filename"
      pattern: "<MarkdownEditor[^>]*filename=\"wakeup\\.md\""
---

<objective>
Adopt the shared `MarkdownEditor` in the two inline markdown-content fields — BountyCard's premise and AddWakeupDialog's instruction. These are NOT files but they're markdown content by contract (per the shape file's scope enumeration); the synthetic filename forces the pretty-editor branch of the D-06 gate.

Purpose: Deliver the remaining two in-scope surfaces (per CONTEXT.md domain paragraph). Ride-along cleanup of the AddWakeupDialog min-height issue (RESEARCH Open Question 2) that the ~40px MDXEditor toolbar creates.

Output: Two modified files, one modified test file (AddWakeupDialog), all committed atomically. Runs in parallel with Plan 02 because it touches disjoint files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/112-pretty-markdown-editing-across-all-frontend-markdown-editing/112-CONTEXT.md
@.planning/phases/112-pretty-markdown-editing-across-all-frontend-markdown-editing/112-RESEARCH.md
@.planning/phases/112-pretty-markdown-editing-across-all-frontend-markdown-editing/112-PATTERNS.md
@.planning/phases/112-pretty-markdown-editing-across-all-frontend-markdown-editing/112-01-SUMMARY.md
@src/ui/features/pretty-view/BountyCard.tsx
@src/ui/features/pretty-view/AddWakeupDialog.tsx
@src/ui/features/pretty-view/AddWakeupDialog.test.tsx
@src/ui/features/pretty-view/MarkdownEditor.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Swap BountyCard premise field + AddWakeupDialog instruction field to MarkdownEditor with synthetic filenames, raise AddWakeup min-height, update AddWakeupDialog tests</name>
  <files>
    src/ui/features/pretty-view/BountyCard.tsx, src/ui/features/pretty-view/AddWakeupDialog.tsx, src/ui/features/pretty-view/AddWakeupDialog.test.tsx
  </files>
  <read_first>
    - .planning/phases/112-pretty-markdown-editing-across-all-frontend-markdown-editing/112-CONTEXT.md (§Phase Boundary — confirms BountyCard premise + AddWakeupDialog instruction are in scope; §Deferred Ideas — confirms other bounty fields are OUT of scope)
    - .planning/phases/112-pretty-markdown-editing-across-all-frontend-markdown-editing/112-RESEARCH.md (§Open Question 2 recommendation on AddWakeupDialog min-height; §Recommended Project Structure line 207 on synthetic filename rationale)
    - .planning/phases/112-pretty-markdown-editing-across-all-frontend-markdown-editing/112-PATTERNS.md (§BountyCard.tsx MOD — L441-478 with the load-bearing surrounding-code notes about onKeyDown shortcuts; §AddWakeupDialog.tsx MOD — L480-521 with min-height guidance and the wrapper div rationale)
    - .planning/phases/112-pretty-markdown-editing-across-all-frontend-markdown-editing/112-01-SUMMARY.md (Plan 01's chosen remount strategy — same synthetic filename in both places means one mount per open, matches the strategy)
    - src/ui/features/pretty-view/BountyCard.tsx (L7 Textarea import; L470-500 premise handlers; L502-505 onPremiseKeyDown; L1061-1069 premise Textarea to swap; L1071-1091 Save/Cancel buttons; L1092-1094 premiseError display)
    - src/ui/features/pretty-view/AddWakeupDialog.tsx (L408-414 label; L415-426 instruction textarea to swap; the instructionDraft state declaration and validateForm predicate — grep for both)
    - src/ui/features/pretty-view/AddWakeupDialog.test.tsx (existing test shape — mirror mocking approach used in Plan 02)
    - src/ui/features/pretty-view/MarkdownEditor.tsx (confirm prop shape one final time — filename, content, onChange, disabled?, placeholder?)
  </read_first>
  <behavior>
    AddWakeupDialog.test.tsx behaviour after refactor:
    - Existing tests that assert the instruction field accepts input, that validateForm blocks empty instruction, that Save is enabled/disabled based on validity — MUST still pass. Instrument via the same `vi.mock("@mdxeditor/editor", …)` pattern used in Plan 02. Adjust any test that uses `getByRole('textbox')` on the instruction field — the field now renders the mocked MDXEditor div instead of a textarea.
    - New: assert the wrapper div's min-height class raised to `min-h-[160px]` (RESEARCH Open Question 2 recommendation).
    BountyCard: no dedicated test file for this — coverage lives in higher-level tests. Existing coverage should continue to work; if a test breaks on the premise flow specifically, note it and adjust. Do NOT invent net-new coverage in this task.
  </behavior>
  <action>
    Step 1 — Extend `AddWakeupDialog.test.tsx` with the `vi.mock("@mdxeditor/editor", () => ({...}))` block used in Plan 02 (copy verbatim). Audit existing tests: any that call `screen.getByRole('textbox', { name: /instruction/i })` or similar — the field is now a mocked-MDXEditor div, so those queries won't find a textbox. Adjust to `screen.getByTestId("mdxeditor")` or use the label + a helper. Preserve original test intent (validation, save flow). Add ONE new test asserting the outer wrapper `<div>` has class `min-h-[160px]` — this is the raised min-height (RESEARCH Open Question 2). Run `npx vitest run src/ui/features/pretty-view/AddWakeupDialog.test.tsx` — expect failures where existing tests reach for `getByRole('textbox')`.

    Step 2 — Modify `AddWakeupDialog.tsx`. Import `MarkdownEditor` from `"./MarkdownEditor"` at the top of the file. Locate the `<textarea id="add-wakeup-instruction" …>` at L415-426. Replace the entire textarea (11 lines) with:
    ```
    <div className="min-h-[160px]">
      <MarkdownEditor
        filename="wakeup.md"
        content={instructionDraft}
        onChange={setInstructionDraft}
        placeholder="What should the agent do when this fires?"
      />
    </div>
    ```
    The wrapper `<div className="min-h-[160px]">` accommodates MDXEditor's ~40px toolbar — RESEARCH Open Question 2 chose option (b) over dropping the toolbar or skipping the surface. Preserve everything else in the dialog untouched: the label at L408-414, the `instructionDraft` useState declaration (find via grep — usually near the top of the component body), `validateForm` predicate (unchanged — it checks `!instructionDraft.trim()`), Save/Cancel handlers, and the Dialog chrome.

    Step 3 — Modify `BountyCard.tsx`. Import `MarkdownEditor` from `"./MarkdownEditor"` at the top (add after other feature-local imports per Shared Pattern A). Locate the `<Textarea …>` at L1061-1069. Replace with:
    ```
    <MarkdownEditor
      filename="premise.md"
      content={premiseDraft}
      onChange={setPremiseDraft}
      disabled={savingPremise}
    />
    ```
    Preserve everything else:
    - `startEditPremise` / `cancelEditPremise` / `savePremise` handlers at L470-500.
    - Save + Cancel `<Button>`s at L1071-1091.
    - `premiseError` display at L1092-1094.

    Special handling for the Escape / Cmd+Enter shortcuts (previously wired via `onKeyDown={onPremiseKeyDown}` on the `<Textarea>`): MarkdownEditor doesn't expose an `onKeyDown` passthrough. Two options:
    (a) Move the shortcut listener to the outer wrapper `<div>` surrounding MarkdownEditor + the Save/Cancel row (a `<div onKeyDown={onPremiseKeyDown}>` around both). Keydown bubbles from the focused editor to the wrapper, so Escape and Cmd+Enter fire as long as the outer div is in the bubble path.
    (b) Accept that MDXEditor's own Escape/Enter behaviours take precedence inside the editor and the shortcuts only fire when focus is outside.
    Recommendation: implement (a) — wrap the current premise-edit block (MarkdownEditor + save/cancel row + premiseError) in a `<div onKeyDown={onPremiseKeyDown}>`. Verify manually in UAT (Plan 04) that Escape cancels and Cmd+Enter saves from within the editor. If (a) creates a conflict with MDXEditor's own keybindings (e.g. Escape closes a link dialog), fall back to (b) and note it in the summary.

    Check the Textarea import removal: after swapping, grep BountyCard.tsx for other `<Textarea>` usages. If none remain, remove the `import { Textarea } from "@/components/textarea";` line at L7. If BountyCard has other Textarea uses (bounty title, todos, keywords, source links, meeting questions — all in Deferred per CONTEXT), leave the import in place. Grep confirms: pre-refactor, only L1061 references `<Textarea>` — so the import can be removed. Verify with grep after edit.

    Step 4 — Run `npx vitest run src/ui/features/pretty-view/AddWakeupDialog.test.tsx src/ui/features/pretty-view/BountyCard*.test.tsx 2>/dev/null` — all tests pass.

    Step 5 — Run the wider scoped suite: `npx vitest run src/ui/features/pretty-view/` — all pretty-view tests green (no regression in Plan 02's work).

    Step 6 — Type-check: `npx tsc --noEmit -p tsconfig.app.json` MUST exit 0.

    Step 7 — Commit as atomic units:
    (a) `test(112-03): adjust AddWakeupDialog tests for MarkdownEditor adoption`
    (b) `refactor(112-03): adopt MarkdownEditor in BountyCard premise + AddWakeupDialog instruction`
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/AddWakeupDialog.test.tsx</automated>
    <automated>npx vitest run src/ui/features/pretty-view/</automated>
    <automated>npx tsc --noEmit -p tsconfig.app.json</automated>
    <automated>grep -q "filename=\"premise.md\"" src/ui/features/pretty-view/BountyCard.tsx</automated>
    <automated>grep -q "filename=\"wakeup.md\"" src/ui/features/pretty-view/AddWakeupDialog.tsx</automated>
    <automated>grep -q "min-h-\[160px\]" src/ui/features/pretty-view/AddWakeupDialog.tsx</automated>
    <automated>grep -q "import { MarkdownEditor } from \"./MarkdownEditor\"" src/ui/features/pretty-view/BountyCard.tsx</automated>
    <automated>grep -q "import { MarkdownEditor } from \"./MarkdownEditor\"" src/ui/features/pretty-view/AddWakeupDialog.tsx</automated>
    <automated>! grep -q "<Textarea" src/ui/features/pretty-view/BountyCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `BountyCard.tsx` premise editor block renders `<MarkdownEditor filename="premise.md" content={premiseDraft} onChange={setPremiseDraft} disabled={savingPremise} />` and no longer contains a `<Textarea>` element.
    - `BountyCard.tsx` no longer imports `Textarea` from `@/components/textarea` (removed once no other usage remains).
    - `AddWakeupDialog.tsx` instruction editor block renders `<MarkdownEditor filename="wakeup.md" content={instructionDraft} onChange={setInstructionDraft} placeholder="…" />` inside a `<div className="min-h-[160px]">` wrapper.
    - `AddWakeupDialog.tsx` no longer contains the pre-refactor `<textarea id="add-wakeup-instruction"` element.
    - `AddWakeupDialog.test.tsx` all tests pass; existing intent preserved.
    - `npx vitest run src/ui/features/pretty-view/` exits 0.
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0.
    - Escape and Cmd+Enter shortcuts on BountyCard's premise field are wired to the outer wrapper `<div onKeyDown={onPremiseKeyDown}>` (Option A) OR the summary explicitly documents the fallback to (B).
  </acceptance_criteria>
  <done>
    BountyCard premise + AddWakeupDialog instruction adopt MarkdownEditor with synthetic filenames; min-height raised to accommodate the toolbar; all existing tests green; type-check clean; two atomic commits landed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-typed premise / instruction text → in-memory state → save handler → backend | UNCHANGED — MarkdownEditor is a controlled input, save handlers are untouched |
| Synthetic filename ("premise.md", "wakeup.md") → gate | Deterministic string constants; not a user-controlled attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-03-01 | Tampering | premise field markdown link scheme | mitigate | Inherits T-112-01 mitigation (Plan 01 Task 2 test 8): javascript: URIs stripped by MDXEditor/Lexical, verified by unit test. |
| T-112-03-02 | Denial of Service | AddWakeupDialog toolbar overflow on narrow viewport | accept | The `min-h-[160px]` wrapper accommodates the toolbar vertically. Horizontal overflow (many toolbar buttons on a narrow modal) is a UX concern for the dark-theme UAT in Plan 04, not a security issue. |
| T-112-03-03 | Repudiation | BountyCard onKeyDown shortcut behaviour change | accept | The Escape/Cmd+Enter shortcuts move from the textarea to the outer wrapper `<div onKeyDown>`. This is a behaviour-preservation concern, verified by manual UAT in Plan 04. Not a security threat. |
</threat_model>

<verification>
Plan-level verification:
- BountyCard premise edit mode: shows MDXEditor toolbar + editable content area.
- AddWakeupDialog instruction: shows MDXEditor toolbar + editable content area with sufficient height.
- Escape/Cmd+Enter on BountyCard premise still cancel/save (verified in UAT — Plan 04).
- validateForm still gates the AddWakeupDialog Save button when instruction is empty.
- No regression in Plan 02's tab refactor.
</verification>

<success_criteria>
- BountyCard premise WYSIWYG editable with dark theme.
- AddWakeupDialog instruction WYSIWYG editable with dark theme + adequate space for toolbar.
- Existing test suites all green.
- No Textarea leftovers in BountyCard's premise path.
</success_criteria>

<output>
Create `.planning/phases/112-pretty-markdown-editing-across-all-frontend-markdown-editing/112-03-SUMMARY.md` capturing:
- Whether Option A (wrapper onKeyDown) or Option B (accept behaviour change) was chosen for BountyCard's Escape/Cmd+Enter shortcuts, and why.
- Whether the `Textarea` import was removed from BountyCard.tsx.
- Which existing AddWakeupDialog tests required fixture adjustments.
- Any surprises with min-height at very narrow modal widths (surface for UAT).
</output>
</content>
</invoke>