---
phase: 111-pretty-markdown-editing-across-all-frontend-markdown-editing
plan: 02b
type: execute
wave: 2
depends_on:
  - 112-01
files_modified:
  - src/ui/features/pretty-view/IdentityFileTab.tsx
  - src/ui/features/pretty-view/RoleFileTab.tsx
  - src/ui/features/pretty-view/RoleFileTab.test.tsx
autonomous: true
requirements:
  - D-08
  - D-16

must_haves:
  truths:
    - "Identity and Role file-tab surfaces (the always-.md pair) are thin wrappers over the shared MarkdownEditor (per D-08)"
    - "IdentityFileTab passes synthetic filename='identity.md' to MarkdownEditor (always-pretty, per Claude's Discretion)"
    - "RoleFileTab passes synthetic filename='role.md' to MarkdownEditor (always-pretty)"
    - "The TabState<T> type export from IdentityFileTab.tsx is unchanged (per D-08 contract preservation — every downstream file tab depends on this)"
    - "The read/edit toggle, ReactMarkdown read-mode preview, and toolbar Edit/Save/Cancel buttons in Identity/Role tabs are unchanged"
    - "Existing vitest suite for RoleFileTab still passes after refactor (per D-16); Identity coverage lives in IdentityModal integration tests, adjusted only if the refactor breaks them"
  artifacts:
    - path: "src/ui/features/pretty-view/IdentityFileTab.tsx"
      provides: "Thin wrapper — edit-mode textarea replaced by <MarkdownEditor filename='identity.md' …>; TabState<T> export intact"
    - path: "src/ui/features/pretty-view/RoleFileTab.tsx"
      provides: "Thin wrapper — edit-mode textarea replaced by <MarkdownEditor filename='role.md' …>"
  key_links:
    - from: "src/ui/features/pretty-view/IdentityFileTab.tsx"
      to: "src/ui/features/pretty-view/MarkdownEditor.tsx"
      via: "named import + JSX with synthetic filename"
      pattern: "<MarkdownEditor[^>]*filename=\"identity\\.md\""
    - from: "src/ui/features/pretty-view/RoleFileTab.tsx"
      to: "src/ui/features/pretty-view/MarkdownEditor.tsx"
      via: "named import + JSX with synthetic filename"
      pattern: "<MarkdownEditor[^>]*filename=\"role\\.md\""
---

<objective>
Adopt the shared `MarkdownEditor` (built in Plan 01) into the two ALWAYS-.md file-tab surfaces (Identity + Role). This is the D-08 half of the consolidation for the tabs where content type is fixed by contract — no filetype gate is needed; the synthetic filename constant just forces the pretty branch of the D-06 gate.

Purpose: Deliver the WYSIWYG edit experience to identity and role file editing, replacing the raw textarea inside the existing read/edit toggle, while preserving the `TabState<T>` contract exported from `IdentityFileTab.tsx` (byte-identical — every downstream file tab depends on this) and every existing test's intent.

Split note: this plan is 02b of a 02a/02b pair. 02a covers the filetype-gated Global + Skill tab side. Both shards target wave 2 and run in parallel with Plan 03. Shard boundary is exact: 02a modifies GlobalFileTab / SkillFileTab / their modals / their tests; 02b modifies IdentityFileTab / RoleFileTab / RoleFileTab.test — disjoint sets, no write conflicts.

Output: Two modified tab files, one modified test file (RoleFileTab), all committed atomically.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/112-pretty-markdown-editing-across-all-frontend-markdown-editing/112-CONTEXT.md
@.planning/phases/112-pretty-markdown-editing-across-all-frontend-markdown-editing/112-RESEARCH.md
@.planning/phases/112-pretty-markdown-editing-across-all-frontend-markdown-editing/112-PATTERNS.md
@.planning/phases/112-pretty-markdown-editing-across-all-frontend-markdown-editing/112-01-SUMMARY.md
@src/ui/features/pretty-view/IdentityFileTab.tsx
@src/ui/features/pretty-view/RoleFileTab.tsx
@src/ui/features/pretty-view/RoleFileTab.test.tsx
@src/ui/features/pretty-view/MarkdownEditor.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor IdentityFileTab + RoleFileTab (synthetic filename, swap edit-mode textarea for MarkdownEditor), extend RoleFileTab tests</name>
  <files>
    src/ui/features/pretty-view/IdentityFileTab.tsx, src/ui/features/pretty-view/RoleFileTab.tsx, src/ui/features/pretty-view/RoleFileTab.test.tsx
  </files>
  <read_first>
    - .planning/phases/112-pretty-markdown-editing-across-all-frontend-markdown-editing/112-CONTEXT.md (D-08, D-16 — Identity/Role tabs' role in the consolidation)
    - .planning/phases/112-pretty-markdown-editing-across-all-frontend-markdown-editing/112-PATTERNS.md (§IdentityFileTab.tsx MOD; §RoleFileTab.tsx MOD)
    - src/ui/features/pretty-view/IdentityFileTab.tsx (current shape — L21-24 TabState<T> export MUST NOT change; L33-36 useState block; L38-62 handleSave/handleCancel; L64-88 loading/error/empty branches; L93-131 toolbar Edit/Save/Cancel; L135-148 edit-mode textarea to swap; L149-177 read-mode ReactMarkdown)
    - src/ui/features/pretty-view/RoleFileTab.tsx (byte-shape mirror of IdentityFileTab; L131-144 edit-mode textarea to swap)
    - src/ui/features/pretty-view/RoleFileTab.test.tsx (test patterns to mirror + extend)
    - src/ui/features/pretty-view/MarkdownEditor.tsx (confirm the prop shape one more time)
  </read_first>
  <behavior>
    RoleFileTab.test.tsx new cases (RED → GREEN):
    - Test D: in edit mode with a ready state, the rendered subtree contains the mocked MDXEditor testid (Identity/Role always pass synthetic filename="role.md" / "identity.md" which hit the .md branch).
    - Test E: existing save-flow tests that click "Edit" then interact with the textarea must be updated — since the .md branch now uses the mocked MDXEditor, use `screen.getByTestId("mdxeditor")` or drive setDraft via a different affordance. Simplest strategy: keep the tests focused on the toolbar Edit/Save/Cancel button interactions (which don't touch the editor), and assert the mocked MDXEditor testid appears when editing.
    IdentityFileTab: no test file exists (PATTERNS.md L26 confirms — coverage is via IdentityModal.*.test.tsx integration tests). Do NOT create one in this task; if an integration test breaks, adjust it separately in this same plan (integration test files are outside `files_modified`; if adjustment is needed, escalate rather than expand scope silently).
    Existing RoleFileTab tests: MUST still pass with fixture adjustments.
  </behavior>
  <action>
    Step 1 — Extend RoleFileTab.test.tsx with the same `vi.mock("@mdxeditor/editor", () => ({...}))` block used in Plan 01's MarkdownEditor.test.tsx. Add a new `describe("MarkdownEditor adoption (D-08)", () => { ... })` block with Test D + Test E. Audit existing tests that switch to edit mode and try to interact with the textarea — either (a) mock MDXEditor to expose an `onChange` callable via `data-testid` + a helper, or (b) change assertions to focus on button state (Save enabled/disabled) rather than the input surface. Preserve original test intent.

    Run: `npx vitest run src/ui/features/pretty-view/RoleFileTab.test.tsx` — expect the new tests to fail because production code still has the textarea.

    Step 2 — Modify `IdentityFileTab.tsx`. Import `MarkdownEditor` from `"./MarkdownEditor"` at the top. Locate the edit-mode textarea at L135-148. Replace the `<textarea …>` element with:
    - `<MarkdownEditor filename="identity.md" content={draft} onChange={setDraft} disabled={saving} />`
    Preserve everything else verbatim:
    - `TabState<T>` export at L21-24 (byte-identical — every downstream file tab depends on this).
    - useState declarations at L33-36.
    - handleSave / handleCancel at L38-62.
    - Loading / error / empty branches at L64-88.
    - Toolbar Edit/Save/Cancel at L93-131.
    - Read-mode ReactMarkdown block at L149-177.
    Keep the surrounding `<div className="flex flex-col flex-1 min-h-0">` wrapper — MarkdownEditor takes the same slot.

    Step 3 — Modify `RoleFileTab.tsx`. Same shape as Step 2 — import MarkdownEditor, replace L131-144 textarea with `<MarkdownEditor filename="role.md" content={draft} onChange={setDraft} disabled={saving} />`. Preserve everything else (RoleFileTab is a byte-shape mirror of IdentityFileTab per its own header comment).

    Step 4 — Run: `npx vitest run src/ui/features/pretty-view/RoleFileTab.test.tsx` — must pass.

    Step 5 — Run the wider pretty-view scoped suite (Identity/Role-focused, but sees Plan 02a's Global/Skill work too when 02a has landed): `npx vitest run src/ui/features/pretty-view/`. All tests MUST pass. If an IdentityModal integration test (`src/ui/features/pretty-view/IdentityModal.*.test.tsx`) tries to `getByRole('textbox')` in edit mode, it will need the same fixture-adjustment. Because those files are NOT in this plan's `files_modified`, if an integration test breaks: STOP and escalate to the orchestrator; do NOT silently expand scope. The expected outcome is either (i) integration tests already pass because they don't drill into the edit-mode textarea, or (ii) a scope revision is triggered.

    Step 6 — Type-check: `npx tsc --noEmit -p tsconfig.app.json` MUST exit 0.

    Step 7 — Commit as atomic units:
    (a) `test(112-02b): extend RoleFileTab tests for MarkdownEditor adoption`
    (b) `refactor(112-02b): adopt MarkdownEditor in Identity/Role FileTabs (synthetic .md filename)`
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/RoleFileTab.test.tsx</automated>
    <automated>npx vitest run src/ui/features/pretty-view/</automated>
    <automated>npx tsc --noEmit -p tsconfig.app.json</automated>
    <automated>grep -q "filename=\"identity.md\"" src/ui/features/pretty-view/IdentityFileTab.tsx</automated>
    <automated>grep -q "filename=\"role.md\"" src/ui/features/pretty-view/RoleFileTab.tsx</automated>
    <automated>grep -q "export type TabState" src/ui/features/pretty-view/IdentityFileTab.tsx</automated>
    <automated>grep -q "import { MarkdownEditor } from \"./MarkdownEditor\"" src/ui/features/pretty-view/IdentityFileTab.tsx</automated>
    <automated>grep -q "import { MarkdownEditor } from \"./MarkdownEditor\"" src/ui/features/pretty-view/RoleFileTab.tsx</automated>
    <automated>! grep -q "min-h-\[400px\]" src/ui/features/pretty-view/IdentityFileTab.tsx</automated>
    <automated>! grep -q "min-h-\[400px\]" src/ui/features/pretty-view/RoleFileTab.tsx</automated>
    <automated>grep -c "TabState" src/ui/features/pretty-view/IdentityFileTab.tsx | awk '$1 >= 1 { exit 0 } { exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `IdentityFileTab.tsx` edit-mode branch renders `<MarkdownEditor filename="identity.md" content={draft} onChange={setDraft} disabled={saving} />` and no longer contains the pre-refactor `min-h-[400px]` textarea markup.
    - `RoleFileTab.tsx` edit-mode branch renders `<MarkdownEditor filename="role.md" …>` with the same pattern.
    - `export type TabState<T> = …` at IdentityFileTab.tsx:21-24 is byte-identical to pre-refactor (no rename, no shape change).
    - Read-mode ReactMarkdown preview, toolbar Edit/Save/Cancel, handleSave/Cancel, and loading/error/empty branches are unchanged in both files.
    - `RoleFileTab.test.tsx` gains a `describe("MarkdownEditor adoption (D-08)", …)` block with ≥2 tests.
    - `npx vitest run src/ui/features/pretty-view/` exits 0.
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0.
    - NO file listed under Plan 02a's `files_modified` (GlobalFileTab.tsx, SkillFileTab.tsx, their modals, their tests) is modified by this plan.
  </acceptance_criteria>
  <done>
    Identity + Role file tabs adopt MarkdownEditor with synthetic filenames; edit-mode is now WYSIWYG; TabState<T> contract intact; tests green; type-check clean. Global/Skill side is handled by 02a.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Synthetic filename ("identity.md" / "role.md") → gate | Deterministic string constants baked into the component; not a user-controlled attack surface |
| Tab draft state → save handler → backend PUT | UNCHANGED from pre-refactor — the shared component doesn't alter the save contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-02b-01 | Tampering | TabState<T> contract drift breaking downstream file tabs | mitigate | Acceptance criterion requires byte-identical `export type TabState<T>`; grep verifies the export exists; TypeScript strict check across the whole app (`tsc --noEmit`) fails compilation if any downstream call site drifts. |
| T-112-02b-02 | Denial of Service | MarkdownEditor lazy-chunk load failure in Identity/Role edit mode | accept | Handled by the Suspense fallback in Plan 01 (renders the raw textarea style). Same disposition as 02a. |
</threat_model>

<verification>
Plan-level verification:
- Identity + Role edit mode renders the pretty MDXEditor via the .md branch of the gate.
- TabState<T> contract preserved — verified via grep + full-repo TypeScript strict check.
- Existing RoleFileTab test suite green — the regression contract per D-16.
</verification>

<success_criteria>
- Editing an identity file (always .md by contract) shows the pretty editor.
- Editing a role file (always .md by contract) shows the pretty editor.
- Save flows behave exactly as before.
- RoleFileTab test suite passes.
</success_criteria>

<output>
Create `.planning/phases/112-pretty-markdown-editing-across-all-frontend-markdown-editing/112-02b-SUMMARY.md` capturing:
- Which existing RoleFileTab tests needed fixture-filename adjustments (list them by name).
- Whether any IdentityModal integration tests required updates (if yes, note whether they were adjusted in-scope or escalated).
- Total lines removed from IdentityFileTab.tsx + RoleFileTab.tsx.
- Whether the shard ran in parallel with 02a or sequentially, and if any file-boundary friction surfaced (expected: none — file sets are disjoint).
</output>
</content>
