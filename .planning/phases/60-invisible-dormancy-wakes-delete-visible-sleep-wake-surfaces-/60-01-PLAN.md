---
phase: 60-invisible-dormancy-wakes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/claude-session/claude-session-server.ts
  - src/backend/claude-session/dormant-poll.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "When a WS `input` frame arrives at a dormant pane, the send-path drops `.dormant`, waits for `.resume-complete` newer than send-time (or the 90s MARKER_FALLBACK_MS window), then dispatches `tmux send-keys` normally."
    - "The backend `{type:\"dormant\", dormant:true}` frame no longer carries `wakingSince` at any of the three emission sites."
    - "Two sends arriving during dormancy land at the harness in send order (sentinel-drop is idempotent; the `.resume-complete` wait serializes the second behind the first)."
    - "Existing natural-resume paths (Matrix DM, scheduled fire — `wakeTriggerTs === null`) are unchanged: no marker freshness check, no send-triggered sentinel drop, no watchdog widening."
    - "Sends into an awake pane behave byte-identically to today — no dormant-branch code runs, no widened watchdog arms."
    - "Backend still owns `wakeTriggerTs` internally as the marker-freshness gate (deleting it would break the natural-user-wake path today, and Plan 01 must set it when the send-path drops the sentinel so its own marker-freshness check works)."
  artifacts:
    - path: "src/backend/claude-session/claude-session-server.ts"
      provides: "Send-while-dormant branch inside __applyInputMessageForTests + suppressed wakingSince at all three dormant-emit sites + retained internal wakeTriggerTs bookkeeping"
      contains: "dormant"
    - path: "src/backend/claude-session/dormant-poll.test.ts"
      provides: "Test coverage for the new send-while-dormant path (marker-fresh success, MARKER_FALLBACK_MS success, ordering of two sends, natural-resume unchanged)"
      contains: "send-while-dormant"
  key_links:
    - from: "src/backend/claude-session/claude-session-server.ts::__applyInputMessageForTests"
      to: "MARKER_FALLBACK_MS at L770-773"
      via: "reused freshness contract, same shape as __applyDormantPollWithRediscoveryForTests"
      pattern: "MARKER_FALLBACK_MS"
    - from: "src/backend/claude-session/claude-session-server.ts::__applyInputMessageForTests dormant branch"
      to: "cat ~/.claude/identities/'${name}'/.resume-complete"
      via: "markerCommand helper, mirrors L6991-6996"
      pattern: "\\.resume-complete"
    - from: "input WS handler L5750-5780"
      to: "__applyInputMessageForTests"
      via: "existing deps object; new deps.dormantLastEmitted getter + deps.setWakeTriggerTs setter + deps.markerCommand + deps.now injectables"
      pattern: "__applyInputMessageForTests"
---

<objective>
Teach the backend send-path (`__applyInputMessageForTests` at `src/backend/claude-session/claude-session-server.ts:2023-2250`) to invisibly wake a dormant pane on send: on entry, if the connection's `dormantLastEmitted` closure var is `true`, drop the `.dormant` sentinel (same `rm -f ~/.claude/identities/'${escapedName}'/.dormant` exec that today's wake handler at L2472-2492 does), record `wakeTriggerTs = Date.now()` in the connection closure (so the existing dormant-poll's marker-freshness gate at L2515-2648 fires correctly), poll `.resume-complete` until its timestamp exceeds send-time OR the `MARKER_FALLBACK_MS = 90_000` window (L770-773) elapses, then proceed with the normal `tmux send-keys` split-send delivery unchanged. Also suppress `wakingSince` at all three emission sites in `claude-session-server.ts` so the frontend can't reconstruct a waking UI.

Purpose: The visible dormancy overlay + Wake button + progress bar disappear in Plan 03. Send becomes the new invisible wake trigger. This plan owns the entire backend side of that: the send-path teach, the `wakingSince` suppression, and the audit of what remains on the `{type:"dormant"}` frame.

Output: `claude-session-server.ts` with (a) new dormant-branch inside `__applyInputMessageForTests` that drops sentinel + waits for marker + delivers send-keys; (b) `wakingSince` deleted from all three emit sites (L1934, L2577, L6899); (c) `wakeTriggerTs` set by the send-path when it drops the sentinel (mirrors the wake-handler write at L5828). Plus `dormant-poll.test.ts` extended with the send-while-dormant test cases below.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60-invisible-dormancy-wakes-delete-visible-sleep-wake-surfaces-/60-CONTEXT.md
@.planning/shapes/shape-invisible-dormancy.md
@.planning/STATE.md
@src/backend/claude-session/pv-send-watchdog.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS client -> backend input handler | Untrusted client sends `{type:"input", data, messageQueueItemId}`. Body/mqid attacker-controlled; hostId/tmuxSession IGNORED — backend derives target from connection-scoped `currentTmuxSession` set at connectToPane discovery. (Mirrors T-cd6-01 from the current wake handler at L5806.) |
| backend send-path -> agent-supervisor via `.dormant` sentinel | Backend calls `rm -f` on a sentinel file inside the identity folder. The identity folder path is derived from connection-scoped `currentTmuxSession`, single-quote-wrapped for shell safety (mirrors L2487 in `__applyWakeMessageForTests`). |
| agent-supervisor -> backend via `.resume-complete` marker | Marker file is written by `~/vms-apps/apps/home/agent-supervisor.sh` at the end of the `/id <name>` load sequence. Same-box trust — no network exposure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01-01 | Spoofing | send-path dormant branch — target pane authority | mitigate | Sentinel `rm -f` path derived EXCLUSIVELY from connection-scoped `currentTmuxSession` (set at connectToPane discovery success). Any hostId/tmuxSession in the `input` frame payload is IGNORED. Mirrors T-cd6-01 mitigation from the existing wake handler at claude-session-server.ts:5799-5804 comment block. The `__applyInputMessageForTests` seam already takes `currentTmuxSession` as an explicit dep (L2026); the new dormant branch inside it reuses that same parameter — no new dep from the WS payload is introduced. |
| T-60-01-02 | Tampering | `.resume-complete` marker source | accept | Marker file is written locally on each fleet box by `agent-supervisor.sh` (owned by Ashley), read via a same-box `cat` over the existing SSH connection. No network path. Same trust posture as the existing dormant-poll marker read at L6991-6996 which has shipped since quick 260808-fgf. |
| T-60-01-03 | Race | send arrives BETWEEN the dormant check and the tmux send-keys | mitigate | Race is bounded by the marker wait: even if `dormantLastEmitted` was `true` when the send-path entered but the pane naturally resumed before the sentinel drop lands, the sentinel `rm -f` is idempotent (`-f` swallows ENOENT), and the marker poll will fall through fast because the pre-existing `.resume-complete` timestamp is already newer than the wake-trigger the send-path just set (natural resume wrote it during its own wake). Worst case: an extra ~1s of poll before send-keys fires. Log at ENTRY (`sshLogger.info` — path taken) and at EXIT (marker-fresh vs fallback vs timeout) so this is forensically observable. |
| T-60-01-04 | Denial of Service | attacker floods `input` frames at a dormant pane | mitigate | Existing `MAX_INPUT_BYTES = 16*1024` cap at L2044-2057 unchanged. Idempotent sentinel drop means N concurrent sends produce ONE `rm -f` + N marker polls, not N sentinel drops. Sentinel drop is a single `rm -f` (constant cost); marker poll is a repeated `cat` bounded by MARKER_FALLBACK_MS. Watchdog widening (Plan 02) prevents red-bubble false-fire under load. |
| T-60-01-05 | Info Disclosure | logging includes hostId + tmuxSession + mqid + elapsed-ms | accept | Existing `sshLogger.info` posture at L2072-2083 already logs `hostId`, `tmuxSession`, `mqid`, `dataLen`. New dormant-branch logs add elapsed-ms + path-taken (marker-fresh vs fallback) which are forensic — no additional PII beyond what the input handler already logs. |

Ancestry: mitigations T-cd6-01/02/03 documented at `claude-session-server.ts:5799-5806` are the trust-boundary template this branch reuses.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Teach __applyInputMessageForTests to drop sentinel + wait for marker on send-while-dormant, then dispatch normally</name>
  <files>src/backend/claude-session/claude-session-server.ts</files>
  <read_first>
    src/backend/claude-session/claude-session-server.ts (specifically L770-773 for MARKER_FALLBACK_MS, L2023-2250 for __applyInputMessageForTests, L2472-2492 for __applyWakeMessageForTests as the sentinel-drop shape template, L2515-2648 for __applyDormantPollWithRediscoveryForTests as the marker-freshness contract template, L5750-5780 for the WS input handler call site, L5806-5831 for the wake handler that sets wakeTriggerTs at L5828, L6360-6370 for the production `dormantState: __DormantStateForTests = {...}` construction that piggybacks wakeTriggerTs into the tick seam — the caller-side deletion this task must land, L6991-6996 for the markerCommand read template)
    .planning/phases/60-invisible-dormancy-wakes-delete-visible-sleep-wake-surfaces-/60-CONTEXT.md
    .planning/shapes/shape-invisible-dormancy.md
    src/backend/claude-session/pv-send-watchdog.ts (context — Plan 02 will consume the new deps.dormantSend flag added by this task so callers know what shape to expect)
  </read_first>
  <action>
    Extend the `__applyInputMessageForTests` seam signature (starts at L2023) with four new OPTIONAL deps so old test call sites continue to work:
      • `dormantLastEmitted?: () => boolean | null` — getter for the connection-scoped `dormantLastEmitted` closure var. Same accessor pattern as `state.dormantLastEmitted` in `__applyDormantPollWithRediscoveryForTests` at L2555-2559.
      • `setWakeTriggerTs?: (ts: number) => void` — setter that writes into the connection-scoped `wakeTriggerTs` closure var. Mirrors the assignment at L5828 (`wakeTriggerTs = Date.now();`).
      • `markerCommand?: (conn: unknown, escapedName: string) => Promise<string | null>` — same signature and semantics as `deps.markerCommand` in `__applyDormantPollWithRediscoveryForTests` (L2543) and the production wire-up at L6991-7003. Returns the trimmed body of `.resume-complete`, or null.
      • `now?: () => number` — injectable clock, defaults to `Date.now`. Same shape as L2544, L7004-7005.

    At the top of the function body (after the `if (!sshConn || !currentTmuxSession) return;` guard at L2041, before the `MAX_INPUT_BYTES` cap at L2044), insert the dormant branch:

      1. Read `const wasDormant = deps.dormantLastEmitted?.() === true;`. If `wasDormant === false`, fall through to the existing normal-send code path with zero change.
      2. If `wasDormant === true` AND all four new deps are present (`deps.setWakeTriggerTs && deps.markerCommand && deps.now`):
         a. Emit `sshLogger.info("[pv-input] send received while pane dormant, dropping sentinel", { operation: "pv_input_dormant_send_start", hostId: currentHostId, tmuxSession: currentTmuxSession, mqid: mqid.length > 0 ? mqid : "none" });` (use the same `hostId=... tmuxSession=... mqid=...` shape as L2072-2083).
         b. Record `const triggerTs = deps.now();` and call `deps.setWakeTriggerTs(triggerTs);` — this writes the connection-scoped `wakeTriggerTs` closure var so the existing dormant-poll's marker-freshness gate (L2604-2626) can hold this pane's `dormant:true` frame in place while the wake completes.
         c. Drop the sentinel: `await exec(sshConn, \`rm -f ~/.claude/identities/'${currentTmuxSession}'/.dormant\`);`. Byte-identical to the exec at L2487 inside `__applyWakeMessageForTests` — same single-quote wrap, same path, same connection. Wrap in try/catch; on catch, emit `sshLogger.warn("[pv-input] sentinel drop failed during dormant send", { operation: "pv_input_dormant_sentinel_drop_failed", ... })` and STILL fall through to normal send (do not abort — the pane may still be usable; the pre-existing send_keys_error frame at L2216-2249 will surface any tmux-side failure).
         d. Emit `sshLogger.info("[pv-input] sentinel dropped, waiting for .resume-complete marker (or MARKER_FALLBACK_MS)", { operation: "pv_input_dormant_wait_marker", triggerTs, mqid: ... });`.
         e. Poll the marker in a bounded loop. Use a 500ms interval (`await new Promise(r => setTimeout(r, 500))`) between checks. Each iteration: call `const body = await deps.markerCommand(sshConn, currentTmuxSession);`. If `body !== null`, parse via `const markerTs = Date.parse(body.trim());` and check `Number.isFinite(markerTs) && markerTs > triggerTs` — that is the "marker fresh" success condition. Break out of the loop and set `markerFresh = true`. Also break if `deps.now() - triggerTs >= MARKER_FALLBACK_MS` — that is the fallback success condition (set both `markerFresh = true` and `fellBack = true`). Semantics MUST byte-for-byte match the freshness check at L2604-2626.
         f. On loop exit, emit `sshLogger.info` with either `operation: "pv_input_dormant_marker_fresh"` (with the `elapsedMs`) or `operation: "pv_input_dormant_marker_fallback"` (with `elapsedMs` and note `fellBack: true`).
         g. Fall through to the existing normal-send code (the split-send branch at L2089+ handles the actual `tmux send-keys`).

    At the production input-handler call site at L5758-5779, wire the four new deps by adding them to the object literal:
      • `dormantLastEmitted: () => dormantLastEmitted,` — reads the connection closure var already declared elsewhere in the file (search for `let dormantLastEmitted` — it's set inside the tail-watcher setup). Do NOT create a new closure var; reuse the existing one.
      • `setWakeTriggerTs: (ts: number) => { wakeTriggerTs = ts; },` — mirrors the assignment at L5828 in the wake handler. `wakeTriggerTs` is the same connection-scoped closure var the wake handler writes to (search for `let wakeTriggerTs` — declared at ~L1264 per L6896 comment).
      • `markerCommand: async (conn: unknown, name: string): Promise<string | null> => { ... }` — copy the exact function-body from L6992-7003 (or better, extract it once into a module-level `readResumeCompleteMarker(execCommand, conn, name)` helper that both the wire-up at L6992-7003 AND the new input-handler wire-up call, so the two never drift). Use `execCommand` from the closure (same identifier the wire-up at L6994 uses).
      • `now: () => Date.now(),` — literal, same as L7005.

    Suppress `wakingSince` at ALL THREE dormant-emit sites — search the file for the exact string `wakingSince` and delete both the key AND its value from each of these emissions:
      • L1934 (inside `__applyDormantPollTickForTests`): change `wsSend(JSON.stringify({ type: "dormant", dormant: true, wakingSince }));` to `wsSend(JSON.stringify({ type: "dormant", dormant: true }));`. Also delete the surrounding branch at L1932-1937 that computes `wakingSince` from `state.wakeTriggerTs?.() ?? null` — collapse to a single unconditional `wsSend(JSON.stringify({ type: "dormant", dormant: isDormant }));`. Delete the `wakeTriggerTs?: () => number | null;` field from `__DormantStateForTests` at L1881 since no emission consumes it any more (internal `wakeTriggerTs` still lives in the connection closure — this delete is only removing the state-box optional getter that piped it to the emit).
      • **Also delete the sole remaining CALLER of that field at claude-session-server.ts L6367-6369** — the production `const dormantState: __DormantStateForTests = { ..., wakeTriggerTs: () => wakeTriggerTs, };` construction. Remove the `wakeTriggerTs: () => wakeTriggerTs,` line entirely (L6369) along with its two-line comment at L6367-6368 (`// quick 260809-ha3: pipe closure-scoped wakeTriggerTs through the / state box so the tick seam can emit wakingSince on dormant:true.`). Without this deletion the type-field removal above fails `npm run build:backend` with a TS excess-property / never-used error on the object literal. The connection-closure `let wakeTriggerTs` var itself STAYS — it is still (a) written by the wake handler at L5828 (until Plan 03 deletes that handler), (b) written by Plan 01's new `setWakeTriggerTs` writer via the input-handler wire-up, and (c) read by the rediscovery-seam wire-up at ~L7013 which targets the DIFFERENT `state.wakeTriggerTs` getter signature on `__applyDormantPollWithRediscoveryForTests` (L2555-2559). Do NOT touch that L7013 wire-up.
      • L2577 (inside `__applyDormantPollWithRediscoveryForTests`): change `wsSend(JSON.stringify({ type: "dormant", dormant: true, wakingSince: state.wakeTriggerTs() }));` to `wsSend(JSON.stringify({ type: "dormant", dormant: true }));`. Keep the surrounding comment updated to reflect that `wakingSince` no longer ships (rewrite the comment at L2573-2576 to note "Phase 60: wakingSince no longer emitted — backend keeps wakeTriggerTs internally only for the marker-freshness gate at L2604-2626."). Keep `state.wakeTriggerTs` getter on the state signature (L2558) because the marker-freshness gate at L2604 still reads it — do not delete the getter itself, only its use in the emit.
      • L6899 (inside the connection-scope initial-discovery-dormant path): change `ws.send(JSON.stringify({ type: "dormant", dormant: true, wakingSince: wakeTriggerTs }));` to `ws.send(JSON.stringify({ type: "dormant", dormant: true }));`. Update the comment at L6895-6898 similarly.

    DO NOT touch the `msg.type === "wake"` handler at L5806-5831 in this plan — that deletion (or gutting) happens in Plan 03 after the frontend cleanup renders it consumer-less. This plan preserves it so any in-flight frontend can still call it during the deploy window if needed.

    DO NOT touch `__applyWakeMessageForTests` at L2472-2492 — same rationale as above.

    DO NOT touch `pane-state-emitter.ts` — it already emits `"dormant"` as a state name with no timestamp or waking metadata (verified: no `wakingSince` or `waking` reference in that file).
  </action>
  <verify>
    <automated>
      # Task 1 verification — grep-gated so a self-invalidating header comment can't false-pass.
      # (a) All three wakingSince emissions gone:
      test "$(grep -v '^ *//' src/backend/claude-session/claude-session-server.ts | grep -v '^ *\*' | grep -c 'wakingSince')" -eq 0 || (echo "FAIL: wakingSince still present in a non-comment line" && exit 1)
      # (b) New send-while-dormant path present:
      grep -q "pv_input_dormant_send_start" src/backend/claude-session/claude-session-server.ts || (echo "FAIL: dormant-send start log missing" && exit 1)
      grep -q "pv_input_dormant_wait_marker" src/backend/claude-session/claude-session-server.ts || (echo "FAIL: marker-wait log missing" && exit 1)
      grep -q "pv_input_dormant_marker_fresh\|pv_input_dormant_marker_fallback" src/backend/claude-session/claude-session-server.ts || (echo "FAIL: marker-outcome log missing" && exit 1)
      # (c) Same-shape sentinel drop:
      grep -q "rm -f ~/.claude/identities/'" src/backend/claude-session/claude-session-server.ts || (echo "FAIL: sentinel drop path missing/changed" && exit 1)
      # (d) BLOCKER-fix guard: sole remaining `wakeTriggerTs: () => wakeTriggerTs` line is the L7013 rediscovery-seam wire-up; the L6369 __DormantStateForTests caller is gone:
      test "$(grep -c 'wakeTriggerTs: () => wakeTriggerTs' src/backend/claude-session/claude-session-server.ts)" -eq 1 || (echo "FAIL: expected exactly 1 remaining 'wakeTriggerTs: () => wakeTriggerTs' (the L7013 wire-up); the L6369 __DormantStateForTests caller must be deleted or the type-field removal breaks TS build" && exit 1)
      # (e) Backend build + full app build clean (per CONTEXT.md fleet rule at L129 — frontend `tsc --noEmit` misses backend TS errors; MUST run both):
      npm run build:backend && npm run build
    </automated>
  </verify>
  <acceptance_criteria>
    - Zero non-comment occurrences of `wakingSince` in `src/backend/claude-session/claude-session-server.ts` (`grep -v '^ *//' | grep -v '^ *\*' | grep -c wakingSince` returns 0).
    - New non-comment log-operation identifiers present: `pv_input_dormant_send_start`, `pv_input_dormant_wait_marker`, and at least one of `pv_input_dormant_marker_fresh` / `pv_input_dormant_marker_fallback`.
    - `__applyInputMessageForTests` signature includes four new optional deps: `dormantLastEmitted`, `setWakeTriggerTs`, `markerCommand`, `now` — confirmable via `grep -E 'dormantLastEmitted\?|setWakeTriggerTs\?|markerCommand\?|now\?' src/backend/claude-session/claude-session-server.ts` returning ≥4 hits inside the seam signature block.
    - Production WS input handler at ~L5758 passes all four deps at the call site — confirmable via `grep -A 30 'if (msg.type === "input")' src/backend/claude-session/claude-session-server.ts | grep -c 'dormantLastEmitted:\|setWakeTriggerTs:\|markerCommand:\|now:'` returning ≥4.
    - Exactly ONE remaining `wakeTriggerTs: () => wakeTriggerTs` line in the file (the L7013 rediscovery-seam wire-up); the L6369 `__DormantStateForTests` construction site's identical line was deleted alongside the type-field removal at L1881. Confirmable via `grep -c 'wakeTriggerTs: () => wakeTriggerTs' src/backend/claude-session/claude-session-server.ts` returning 1.
    - `__applyWakeMessageForTests` at L2472-2492 is BYTE-FOR-BYTE unchanged (regression check: `git diff src/backend/claude-session/claude-session-server.ts | grep -A 20 '__applyWakeMessageForTests' | grep -c '^[-+]'` returns 0 for lines inside that function's body).
    - `msg.type === "wake"` handler at L5806 is BYTE-FOR-BYTE unchanged (defer to Plan 03).
    - `npm run build:backend && npm run build` exits 0 — mandatory per CONTEXT.md L129 fleet rule (frontend `tsc --noEmit` misses backend TS errors; both commands MUST run).
  </acceptance_criteria>
  <done>
    Send-path teach lands + `wakingSince` suppression lands at all three sites + L6369 caller-side deletion lands + backend + full-app TS build clean. No test extension yet — that's Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add send-while-dormant test coverage in dormant-poll.test.ts</name>
  <files>src/backend/claude-session/dormant-poll.test.ts</files>
  <read_first>
    src/backend/claude-session/dormant-poll.test.ts (specifically L1-100 for imports and setup, L191-260 for the existing `__applyWakeMessageForTests` test-block pattern to mirror, L448-500 for the ok:true test, L659-750 for the wakingSince-carrying-dormant tests that will need adjustment given Task 1's suppression)
    src/backend/claude-session/claude-session-server.ts (specifically the modified `__applyInputMessageForTests` from Task 1 — the new signature deps are what these tests exercise)
    .planning/phases/60-invisible-dormancy-wakes-delete-visible-sleep-wake-surfaces-/60-CONTEXT.md
    .planning/shapes/shape-invisible-dormancy.md
  </read_first>
  <action>
    Add a new `describe` block near the end of `dormant-poll.test.ts` titled exactly `Phase 60: send-while-dormant path (invisible wake trigger)` containing these four `it` cases:

      1. `it("Test SWD-1: send while dormant with fresh marker → sentinel dropped, wakeTriggerTs recorded, tmux send-keys fires after marker becomes fresh", async () => { ... })`.
         Setup: mock `dormantLastEmitted` getter to return `true`. Stub `markerCommand` to return `null` on first two calls, then a timestamp string strictly greater than the `triggerTs` (use a small `now()` counter that advances 500ms per call so `triggerTs = 0`, first two polls at 500ms and 1000ms return null, third poll at 1500ms returns `new Date(2000).toISOString()` — comfortably > triggerTs). Stub `execCommand` to record which commands ran. Call `__applyInputMessageForTests` with `data: "hello\r"`, `messageQueueItemId: "mqid-1"`, `sshConn: {}`, `currentTmuxSession: "test-agent"`, and all four new deps wired to the stubs.
         Assertions:
           • `execCommand` was called with a command containing `rm -f ~/.claude/identities/'test-agent'/.dormant` (sentinel drop).
           • `setWakeTriggerTs` was called exactly once with a numeric arg equal to the mocked `now()` at entry.
           • `markerCommand` was called at least twice (poll loop).
           • `execCommand` was called with a command containing `tmux send-keys -l -t 'test-agent' 'hello'` (split-send body) AND a command containing `tmux send-keys -t 'test-agent' Enter` (split-send Enter).
           • Assertion order: sentinel-drop command appears BEFORE any `tmux send-keys` command in the recorded call list.
           • No exception thrown.

      2. `it("Test SWD-2: send while dormant with marker never appearing → falls back at MARKER_FALLBACK_MS and still fires send-keys", async () => { ... })`.
         Setup: same as SWD-1 but `markerCommand` always returns `null`. Stub `now()` to jump from `triggerTs` (0) to `MARKER_FALLBACK_MS` (90_000) after the FIRST poll returns null — i.e. loop iterates once, sees null, next `now()` returns 90_000, so `deps.now() - triggerTs >= MARKER_FALLBACK_MS` → fallback branch fires. Note: import `MARKER_FALLBACK_MS` from claude-session-server.ts if it's not already exported; if not exported, hard-code `90_000` and add a comment `// mirrors MARKER_FALLBACK_MS at claude-session-server.ts:773`.
         Assertions:
           • Sentinel drop still fired (same command grep as SWD-1).
           • Send-keys still fired (both body and Enter commands present).
           • Assert `sshLogger.info` was called with an object whose `operation` field equals `"pv_input_dormant_marker_fallback"` (use vi.mock or a spy on sshLogger — mirror how `dormant-poll.test.ts` already spies on log output; check the file for existing `vi.spyOn(sshLogger` or equivalent patterns).

      3. `it("Test SWD-3: two sends into a dormant pane in rapid succession — both land in send order, sentinel drop idempotent", async () => { ... })`.
         Setup: mock `dormantLastEmitted` to return `true` throughout. Fire `__applyInputMessageForTests` TWICE in sequence with `data: "first\r", messageQueueItemId: "mqid-1"` and `data: "second\r", messageQueueItemId: "mqid-2"`. `markerCommand` returns a fresh timestamp on second poll for BOTH invocations (so each send waits then dispatches).
         Assertions:
           • `execCommand` was called with `rm -f ...` at least twice (once per send — idempotent because `-f` swallows ENOENT on the second call; the test proves the second call doesn't throw).
           • The recorded `execCommand` calls contain, in order: (a) sentinel-drop for first send, (b) body send-keys for "first", (c) Enter for "first", (d) sentinel-drop for second send, (e) body send-keys for "second", (f) Enter for "second". Assert `indexOf("'first'") < indexOf("'second'")` in the recorded call list so ordering is proven.

      4. `it("Test SWD-4: send into an awake pane (dormantLastEmitted=false) — no sentinel drop, no marker poll, no wakeTriggerTs write, normal path unchanged", async () => { ... })`.
         Setup: mock `dormantLastEmitted` to return `false`. Same send as SWD-1.
         Assertions:
           • `execCommand` was NEVER called with any command matching `rm -f ~/.claude/identities/`.
           • `markerCommand` was NEVER called.
           • `setWakeTriggerTs` was NEVER called.
           • Body send-keys + Enter WERE called (normal split-send unchanged).

    Additionally, adjust the existing wakingSince-carrying tests in the file (search for `wakingSince` in `dormant-poll.test.ts` — there are hits around L659+ per the CONTEXT.md prior-context notes) so they now assert the OPPOSITE: `dormant:true` frames must NOT carry `wakingSince`. Specifically, find any assertion like `expect(sent).toContain('wakingSince')` or `expect(parsed.wakingSince).toBe(...)` and either delete the test if its entire purpose was the wakingSince round-trip, or flip the assertion to `expect(parsed).not.toHaveProperty('wakingSince')`. Add a comment above each flipped/deleted test: `// Phase 60: wakingSince removed — frontend no longer needs to reconstruct waking state (DormancyOverlay deleted).`
  </action>
  <verify>
    <automated>
      # Scoped test run — per fleet rule "scoped tests during dev, full suite at ship-gate":
      npx vitest run src/backend/claude-session/dormant-poll.test.ts --reporter=verbose
      # Grep-gate: four new SWD tests present:
      test "$(grep -c 'Test SWD-[1-4]:' src/backend/claude-session/dormant-poll.test.ts)" -ge 4 || (echo "FAIL: not all four SWD tests present" && exit 1)
      # Grep-gate: no assertions expect wakingSince present:
      test "$(grep -c 'toContain(.wakingSince.)\|toHaveProperty(.wakingSince..*, [^)]' src/backend/claude-session/dormant-poll.test.ts)" -eq 0 || (echo "FAIL: a test still asserts wakingSince is present" && exit 1)
      # Backend + full app build clean (per CONTEXT.md L129 fleet rule):
      npm run build:backend && npm run build
    </automated>
  </verify>
  <acceptance_criteria>
    - `dormant-poll.test.ts` contains four `it()` blocks whose names match the regex `^\s*it\("Test SWD-[1-4]:` (grep confirms count >= 4).
    - Zero occurrences of positive `wakingSince` assertions remain (all such assertions either deleted or flipped to `.not.toHaveProperty('wakingSince')`).
    - `npx vitest run src/backend/claude-session/dormant-poll.test.ts` exits 0 with all four SWD tests passing.
    - `npm run build:backend && npm run build` exits 0 (Task 1's + Task 2's combined diff still typechecks end-to-end).
  </acceptance_criteria>
  <done>
    Backend send-while-dormant path has test coverage for: marker-fresh success, MARKER_FALLBACK_MS fallback, ordered two-send burst, and awake-pane no-op. All existing tests either updated or deleted for the wakingSince suppression. Scoped test run green.
  </done>
</task>

</tasks>

<verification>
- `npm run build:backend && npm run build` exits 0 (mandatory per CONTEXT.md L129 fleet rule — frontend `tsc --noEmit` misses backend TS errors; both commands are load-bearing).
- `npx vitest run src/backend/claude-session/dormant-poll.test.ts` exits 0 with the four new `Test SWD-*` cases passing.
- Manual read: the three `wakingSince` emission sites (L1934, L2577, L6899 pre-change) now emit `{type:"dormant", dormant:true|false}` with no `wakingSince` field.
- Manual read: `__applyWakeMessageForTests` (L2472-2492) and `msg.type === "wake"` handler (L5806-5831) are byte-for-byte unchanged from `main`.
- Zero touches to `pane-state-emitter.ts` (verified: no `wakingSince`/`waking` field in emit).
- Zero touches to `agent-supervisor.sh` on any box (out of scope per shape file).
- BLOCKER guard: exactly ONE `wakeTriggerTs: () => wakeTriggerTs` line remains in `claude-session-server.ts` — the L7013 rediscovery-seam wire-up; the L6369 `__DormantStateForTests` caller was deleted alongside the type-field removal.
</verification>

<success_criteria>
- Backend send-path drops `.dormant`, sets `wakeTriggerTs`, waits for `.resume-complete` (or MARKER_FALLBACK_MS), then dispatches `tmux send-keys` normally — proven by SWD-1/SWD-2 tests.
- Multiple sends during dormancy serialize in send order — proven by SWD-3 test.
- Awake-pane sends are byte-identical to today — proven by SWD-4 test.
- Frontend cannot reconstruct waking UI: `{type:"dormant"}` frames carry no timestamp — proven by grep-gate + test assertion flips.
- Backend + full app TS build passes end-to-end (`npm run build:backend && npm run build`).
- Zero regressions in the natural-resume path (Matrix DM, scheduled fire): `wakeTriggerTs` stays null for those, marker-freshness check stays skipped exactly as it does today.
</success_criteria>

<output>
Create `.planning/phases/60-invisible-dormancy-wakes-delete-visible-sleep-wake-surfaces-/60-01-SUMMARY.md` when done, covering: files modified, new deps added to `__applyInputMessageForTests` signature, three `wakingSince` deletion sites, the L6369 `__DormantStateForTests` caller-side deletion (BLOCKER fix from plan-checker revision), four new SWD tests, and the interface contract Plan 02 + Plan 03 need to consume (specifically: `{type:"dormant"}` no longer has `wakingSince`; `msg.type === "wake"` handler still present but no longer receives frontend traffic after Plan 03 lands; new logging operations for forensic diagnostics).
</output>
