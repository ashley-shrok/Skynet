---
phase: 133-role-archival
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/api/role-archive-api.ts
  - src/ui/api/role-archive-api.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "`archiveRole(hostId: number, roleName: string): Promise<{ ok: true }>` exists as a thin authApi.post wrapper mirroring `archiveIdentity(hostId, identityKey)` (D-01: single API call, no fan-out from the frontend)."
    - "The call POSTs to `/roles/${encodeURIComponent(roleName)}/archive` with body `{ hostId }` and returns the parsed JSON body on 200."
    - "Non-2xx responses surface through `handleApiError` with the exact same shape sibling API modules produce — so the RolesListModal caller's catch-and-log path is uniform with the identity-archive caller's."
    - "URL-encoding is applied to the role name so any future edge (e.g. a role name containing a character the current ROLE_NAME_PATTERN allows but URLs treat specially) doesn't double-encode or break — mirrors `archiveIdentity`'s `encodeURIComponent(identityKey)` discipline."
  artifacts:
    - path: src/ui/api/role-archive-api.ts
      provides: "archiveRole(hostId, roleName) → POST /roles/:name/archive. Thin authApi wrapper mirroring archiveIdentity byte-for-byte."
      exports: ["archiveRole"]
    - path: src/ui/api/role-archive-api.test.ts
      provides: "vitest coverage: happy path + POST-shape lock, 400 rejection, 404 rejection, 500 rejection, URL-encoding smoke."
  key_links:
    - from: src/ui/api/role-archive-api.ts
      to: src/main-axios
      via: "authApi.post + handleApiError (same helpers archiveIdentity uses)"
      pattern: "authApi\\.post|handleApiError"
---

<objective>
Wave A frontend API-wrapper half of Phase 133. Deliver `archiveRole(hostId, roleName)` — a thin `authApi.post` wrapper that the Wave D RolesListModal integration calls once both `window.confirm` dialogs are OK'd.

Purpose: give the frontend UI a single named function to fire-and-forget when the operator confirms Archive. No fan-out (D-01: cascade complexity lives in the supervisor); no un-archive companion (D-19: one-way, no un-archive gesture); no batch shape.

Output: `archiveRole(hostId, roleName)` importable from `@/api/role-archive-api`, tested against a mocked `authApi`.

**This plan is parallel with 133-01** — they touch disjoint files (`src/ui/api/*` vs `src/backend/*`) and neither depends on the other's runtime behavior for the test-green gate. The two plans land in Wave 1 side-by-side.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-role-archival/133-CONTEXT.md
@.planning/phases/133-role-archival/133-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create `role-archive-api.ts` client + tests</name>
  <files>src/ui/api/role-archive-api.ts, src/ui/api/role-archive-api.test.ts</files>
  <read_first>
    - src/ui/api/identity-archive-api.ts (~32 lines — the byte-shape template. `archiveRole` is a mechanical clone of `archiveIdentity` with the identity → role concept substitution. Read it in full; the new file should look almost identical, changing only the endpoint path, the parameter names, and the docstring header.)
    - src/ui/api/identity-archive-api.test.ts (test template — read the module-level `vi.mock("@/main-axios", …)` block and the 4-5 test cases it covers. Mirror the mocking strategy verbatim. The reset-and-assert flow in beforeEach/afterEach is the same.)
    - src/main-axios (source of `authApi` + `handleApiError`; verify the helper shapes match what identity-archive-api.ts uses — no drift expected).
    - .planning/phases/133-role-archival/133-RESEARCH.md § "Code Examples" #3 (verbatim shape sketch of role-archive-api.ts).
    - .planning/phases/133-role-archival/133-CONTEXT.md § D-01 (single call, no fan-out) and § D-19 (one-way, no un-archive — DO NOT add an un-archive companion function).
  </read_first>
  <behavior>
    - Test 1 (happy path): `archiveRole(42, "box-maintainer")` calls `authApi.post` exactly once with URL `/roles/box-maintainer/archive` and body `{ hostId: 42 }`. On a mocked 200 response with data `{ ok: true }`, returns `{ ok: true }`.
    - Test 2 (400 rejection): `authApi.post` rejects with an axios-shape error carrying status 400 + a body error message. `archiveRole` propagates through `handleApiError("archive role", …)` and throws with a message containing `archive role` (operation label) and the underlying message.
    - Test 3 (404 rejection): same shape as Test 2 but 404. Assert throw.
    - Test 4 (500 rejection): same shape as Test 2 but 500. Assert throw.
    - Test 5 (URL-encoding): `archiveRole(1, "role-with-space")` — although ROLE_NAME_PATTERN wouldn't allow spaces, verify `encodeURIComponent` is applied at the frontend layer so a defense-in-depth character never double-encodes. Assert the URL passed to `authApi.post` is exactly `/roles/role-with-space/archive` (space encoded as `%20` — actually `encodeURIComponent(" ")` → `%20`, so URL becomes `/roles/role-with-space/archive`; the hyphenated fixture avoids that ambiguity but the assertion still pins the shape). Better fixture: `archiveRole(1, "a b")` → URL is `/roles/a%20b/archive`. The point of this test is to lock `encodeURIComponent` into the call site so it can't silently regress into raw string interpolation.
  </behavior>
  <action>
    Create `src/ui/api/role-archive-api.ts`. Direct clone of `identity-archive-api.ts` with concept substitution.

    (a) Module docstring: 10-15 lines noting this is the Phase 133 D-01 archiveRole helper — mirrors archiveIdentity byte-for-byte. State: "No un-archive companion by design (D-19: one-way, no un-archive gesture). No batch shape (D-01: single sentinel drop; cascade complexity lives in the supervisor). If the operator wants to archive N roles, N clicks, N calls."

    (b) Body — verbatim shape from identity-archive-api.ts with `identity → role`, `identityKey → roleName`, `.../identities/... → .../roles/...`, `"archive identity" → "archive role"`:
    ```
    import { authApi, handleApiError } from "@/main-axios";

    export async function archiveRole(
      hostId: number,
      roleName: string,
    ): Promise<{ ok: true }> {
      try {
        const url = `/roles/${encodeURIComponent(roleName)}/archive`;
        const response = await authApi.post(url, { hostId });
        return response.data as { ok: true };
      } catch (error) {
        handleApiError(error, "archive role");
      }
    }
    ```

    (c) Do NOT add `unarchiveRole` or any batch helper. Do NOT export additional helpers beyond `archiveRole`.

    Create `src/ui/api/role-archive-api.test.ts`. Mirror identity-archive-api.test.ts structure verbatim:
      - `vi.mock("@/main-axios", () => ({ authApi: { post: vi.fn(), get: vi.fn(), put: vi.fn() }, handleApiError: (err, op) => { throw new Error(`${op}: ${err instanceof Error ? err.message : String(err)}`); } }));`
      - `beforeEach(() => vi.mocked(authApi.post).mockReset());`
      - `afterEach(() => vi.clearAllMocks());`
      - Cover all 5 behavior cases above.
      - For Test 1 (happy): `vi.mocked(authApi.post).mockResolvedValueOnce({ data: { ok: true } });` then assert `authApi.post` was called once with `("/roles/box-maintainer/archive", { hostId: 42 })` and the result equals `{ ok: true }`.
      - For Tests 2-4 (rejections): `vi.mocked(authApi.post).mockRejectedValueOnce(new Error("400: bad request"));` and assert `await expect(archiveRole(...)).rejects.toThrow(/archive role.*400/)`.
      - For Test 5 (encoding): call `archiveRole(1, "a b")` and assert URL was `/roles/a%20b/archive`.
  </action>
  <verify>
    <automated>npx vitest run src/ui/api/role-archive-api.test.ts</automated>
  </verify>
  <done>
    - `src/ui/api/role-archive-api.ts` exports `archiveRole(hostId, roleName)`.
    - `npx vitest run src/ui/api/role-archive-api.test.ts` reports all 5 tests green.
    - `grep -c 'archiveRole\|archiveIdentity' src/ui/api/role-archive-api.ts` — only `archiveRole` appears (no accidental copy of the identity name).
    - `grep -c 'unarchive\|batchArchive' src/ui/api/role-archive-api.ts` returns 0 — no un-archive or batch helper accidentally added.
    - Pre-commit typecheck passes: `npm run build` exits 0.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/ui/api/role-archive-api.test.ts` — all 5 tests green.
- `npm run build` — exit 0 (frontend tsc pass).
- The exported surface is exactly one function: `archiveRole`. No other exports.
- The file is a direct-shape clone of `identity-archive-api.ts` — line counts should be comparable (~30-35 lines including docstring).
</verification>

<success_criteria>
- `archiveRole` is importable from `@/api/role-archive-api` and matches the shape sibling API modules produce.
- 5 tests green.
- No accidental un-archive companion, no batch helper.
- `encodeURIComponent` is applied to `roleName` at the call site (defense-in-depth, mirrors sibling).
</success_criteria>

<output>
Create `.planning/phases/133-role-archival/133-02-SUMMARY.md` when done, including:
- Confirmation that the function is a direct clone of `archiveIdentity` (line-count comparison; note any deviations)
- List of the 5 tests with pass/fail (all should be pass)
- Confirmation that no un-archive or batch helper was added
</output>
