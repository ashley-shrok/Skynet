---
phase: 133-role-archival
plan: 04
type: execute
wave: 3
depends_on:
  - 133-03
files_modified:
  - substrate/scripts/agent-supervisor.sh
  - substrate/scripts/tests/agent-supervisor-role-archive.test.sh
autonomous: true
requirements: []
must_haves:
  truths:
    - "New constants `ROLES_DIR` (default `$HOME/fleet/roles`, override via `AGENT_ROLES_DIR`) and `ROLES_ARCHIVE_DIR` (default `$HOME/fleet/roles-archive`, override via `AGENT_ROLES_ARCHIVE_DIR`) exist in `agent-supervisor.sh` alongside the existing IDENTITIES_DIR / IDENTITIES_ARCHIVE_DIR (mirror the same env-overridable pattern for test hermeticity) — per D-16 archive location + D-18 per-box scope."
    - "New helper function `identity_has_role(<identity_file>, <role_name>)` exists and matches `role:` frontmatter between the first two `---` delimiters. Handles unquoted (Skynet convention) and quoted (double-quote / single-quote) variants; logs a WARN when a quoted variant is encountered so fleet-drift surfaces — per RESEARCH § Pitfall 2 + Assumption A2."
    - "New scanner function `scan_role_archive_requested_sentinels()` exists. Walks `$ROLES_DIR/*/` for `.archive-requested` sentinels. On each sentinel: fresh-enumerate identities holding the role (per D-07), fail-soft cascade `retire_identity` on each (per D-08), guards bypassed uniformly (per D-10 — the scanner does not check .pinned / .no-dormancy / coordinator), move the role folder to `$ROLES_ARCHIVE_DIR/<name>/` ONLY if every identity retired cleanly (per D-09), delete the sentinel regardless of cascade outcome (per D-06)."
    - "Empty cascade (zero identities hold the role) is a degenerate case of the same code path — no identities to retire, folder move happens immediately (per D-12)."
    - "On partial failure the supervisor emits a LOUD `ERROR:` log line naming each failed identity + which step failed + the underlying error — mirror the D-15 silent-by-design carve-out that the existing retire-stuck logging uses (per D-09 + shape philosophy)."
    - "The scanner is wired into `reconcile()` at agent-supervisor.sh:~L2327 IMMEDIATELY AFTER `scan_archive_requested_sentinels` — same reconcile-tick cadence as the user-initiated identity archive scanner (~15s)."
    - "New bash test file `substrate/scripts/tests/agent-supervisor-role-archive.test.sh` covers: happy-path cascade (2 identities + folder move), fail-soft partial cascade (1 identity fails matrix-deactivate, other retires cleanly, role folder retained, sentinel deleted, LOUD log fires), empty cascade (0 identities, folder moves immediately), guard-bypass (pinned + coordinator identity gets cascade-retired regardless), sentinel-always-deleted (delete-on-fail confirmed), scanner is a no-op when no `.archive-requested` sentinels exist."
    - "`bash substrate/scripts/tests/agent-supervisor-role-archive.test.sh` passes green."
  artifacts:
    - path: substrate/scripts/agent-supervisor.sh
      provides: "New ROLES_DIR / ROLES_ARCHIVE_DIR constants (near the existing IDENTITIES_DIR block ~L38-42); new identity_has_role() helper (near is_coordinator ~L324); new scan_role_archive_requested_sentinels() function (sibling of scan_archive_requested_sentinels ~L951); reconcile-loop wire-up at ~L2327."
    - path: substrate/scripts/tests/agent-supervisor-role-archive.test.sh
      provides: "New hermetic test file for the role-archival scanner. Reuses the harness conventions from agent-supervisor-archive-scan.sh (setup_scratch, _source_supervisor, fixture_identity, assert_*, run_test, stub_homeserver)."
  key_links:
    - from: substrate/scripts/agent-supervisor.sh scan_role_archive_requested_sentinels()
      to: substrate/scripts/agent-supervisor.sh retire_identity()
      via: "for each enumerated identity holding the role, invoke retire_identity <name> — fail-soft accumulation of failures"
      pattern: "retire_identity"
    - from: substrate/scripts/agent-supervisor.sh reconcile()
      to: scan_role_archive_requested_sentinels
      via: "one call per reconcile tick, immediately after scan_archive_requested_sentinels at ~L2327"
      pattern: "scan_role_archive_requested_sentinels"
    - from: substrate/scripts/agent-supervisor.sh identity_has_role()
      to: awk-between-first-two-fences frontmatter parser (mirrors is_coordinator shape)
      via: "awk-with-var-passing that matches `role: <name>` (and single/double-quoted variants) inside first `---`-fenced block"
      pattern: "awk -v w="
---

<objective>
Wave C — the role cascade scanner in the substrate supervisor. This is the load-bearing new bash logic that consumes the sentinel dropped by Wave A's HTTP endpoint (Plan 133-01) and cascades `retire_identity()` (freshly refactored in Wave B / Plan 133-03) over every identity holding the archived role.

Depends on Plan 133-03 (Wave B) — the cascade caller MUST consume the clean atomic-retire semantic from the refactored `retire_identity()`. Wiring this scanner into a pre-refactor `retire_identity()` would give the new cascade caller inherit-legacy-counter behavior at birth, defeating the D-15 shared-clean-semantic contract.

Purpose (from the shape file): "The supervisor's reconcile loop, on its next tick, finds the sentinel on the role folder. On that tick it does the entire retirement inline: it walks the live identity folders and picks out every identity whose frontmatter names this role as its role; for each such identity it runs the identity retirement process end-to-end … After every identity has been attempted, if all of them retired cleanly the supervisor moves the role folder itself to a roles archive sibling and deletes the sentinel."

Output:
- `ROLES_DIR` / `ROLES_ARCHIVE_DIR` constants (env-overridable).
- `identity_has_role()` helper (frontmatter-aware role match).
- `scan_role_archive_requested_sentinels()` scanner function.
- Reconcile-loop wire-up.
- New test file exercising happy-path, partial-failure, empty-cascade, guard-bypass, sentinel-always-deleted, and no-op scenarios.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-role-archival/133-CONTEXT.md
@.planning/phases/133-role-archival/133-RESEARCH.md
@.planning/shapes/shape-role-archival.md
@.planning/phases/133-role-archival/133-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ROLES_DIR / ROLES_ARCHIVE_DIR constants + identity_has_role() helper + scan_role_archive_requested_sentinels() function + reconcile wire-up</name>
  <files>substrate/scripts/agent-supervisor.sh</files>
  <read_first>
    - substrate/scripts/agent-supervisor.sh L30-140 (constants block — where IDENTITIES_DIR + IDENTITIES_ARCHIVE_DIR + DORMANCY_STATE_DIR are declared; the new ROLES_DIR + ROLES_ARCHIVE_DIR mirror these patterns).
    - substrate/scripts/agent-supervisor.sh L319-328 (`is_coordinator()` — the awk-between-first-two-fences frontmatter parser. The new `identity_has_role()` mirrors this shape verbatim, changing only the pattern matched. Read the whole function including comments about YAML-shape variance.)
    - substrate/scripts/agent-supervisor.sh L928-998 (post-refactor `scan_archive_requested_sentinels` — the sibling scanner. The new `scan_role_archive_requested_sentinels` mirrors its outer walk shape but has additional inner logic for cascade enumeration + partial-failure accumulation + conditional folder move.)
    - substrate/scripts/agent-supervisor.sh L2322-2331 (reconcile() dispatch — where the existing scanner is wired at L2327; the new scanner wires IMMEDIATELY AFTER on its own line).
    - substrate/scripts/agent-supervisor.sh L590-825 (post-refactor `retire_identity()` — the cascade caller. Verify it now returns atomic 0/1 from a caller's POV, no cross-tick state.)
    - .planning/phases/133-role-archival/133-CONTEXT.md § D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-16, D-17, D-18 (the entire cascade semantic locked here).
    - .planning/phases/133-role-archival/133-RESEARCH.md § "Code Examples" #4 (verbatim scanner code sketch) and § "Pitfall 2" (identity_has_role frontmatter-parse pitfall) and § "Pitfall 5" (sentinel-delete order safety) and § "Pitfall 6" (cascade preview vs live enumeration race — design-level accept, no code mitigation).
  </read_first>
  <action>
    Edit `substrate/scripts/agent-supervisor.sh` at four sites — in this order (top-down through the file to keep line numbers monotonic during editing):

    ### Part A — Add ROLES_DIR / ROLES_ARCHIVE_DIR constants (~L42 area, immediately after IDENTITIES_ARCHIVE_DIR)

    Add the two new constants adjacent to `IDENTITIES_ARCHIVE_DIR`:
    ```
    # Phase 133 D-16 + D-18: role folder + role-archive sibling. Roles are per-box
    # (id-skill invariant), so the archival gesture is scoped to this box's disk.
    # Env-overridable for test hermeticity (same discipline as AGENT_IDENTITIES_DIR /
    # AGENT_IDENTITIES_ARCHIVE_DIR — see substrate/scripts/tests/*.test.sh for
    # scratch-dir setup patterns).
    ROLES_DIR="${AGENT_ROLES_DIR:-$HOME/fleet/roles}"
    ROLES_ARCHIVE_DIR="${AGENT_ROLES_ARCHIVE_DIR:-$HOME/fleet/roles-archive}"
    ```

    ### Part B — Add identity_has_role() helper (~L328 area, immediately after is_coordinator)

    Add the helper sibling of `is_coordinator`:
    ```
    # Phase 133 D-07: identity_has_role <identity_file_path> <role_name>
    # Returns 0 IFF the identity file has `role: <role_name>` on its own line
    # BETWEEN the first two `---` frontmatter delimiters. Handles unquoted
    # (Skynet convention — verified across the fleet 2026-09-24), plus double-
    # and single-quoted YAML variants; logs a WARN when a quoted variant is
    # encountered so fleet-drift surfaces (per Assumption A2).
    # Mirrors is_coordinator's awk-between-fences shape — same delimiter-sensitivity
    # discipline (Pitfall 6 lock from Phase 94).
    identity_has_role() {
      local identity_file="$1" want_role="$2"
      [ -f "$identity_file" ] || return 1
      awk -v w="$want_role" '
        /^---$/{f++}
        f==1 && ($0 == "role: " w){found=1; exit}
        f==1 && ($0 == "role: \"" w "\"" || $0 == "role: '\''" w "'\''"){quoted=1; found=1; exit}
        END {
          if (quoted) {
            print "WARN: quoted role frontmatter detected — Skynet convention is unquoted (fleet-drift signal)" > "/dev/stderr"
          }
          exit !found
        }
      ' "$identity_file"
    }
    ```
    Note: `awk`'s stderr redirect inside END emits the WARN — it lands on the supervisor's stderr channel which flows into the log via the surrounding log-capture, matching the D-15 discipline (WARN is diagnostic; only fleet-drift surfaces trigger it, not routine paths). Verify by reading a nearby log site to confirm the log-capture pattern the file uses; if the file logs via a custom `log()` function that captures stdout only, the awk stderr won't be captured — adjust by having awk print to /dev/stdout with a WARN: prefix and having the caller `log` it, or simpler: emit the WARN from the bash caller after checking the awk return status via a side-channel. Planner's call on the exact mechanism, but SOME warn signal MUST fire when quoted frontmatter is seen so fleet-drift is observable.

    Simpler alternative (recommended if the stderr-in-awk approach is fragile): keep the awk purely as a match/no-match test, and add a separate quick grep sanity-check ONLY if awk succeeds — if the identity file contains a quoted role line, log the WARN via the bash `log` function:
    ```
    identity_has_role() {
      local identity_file="$1" want_role="$2"
      [ -f "$identity_file" ] || return 1
      # Awk match (unquoted OR quoted — Skynet convention is unquoted but tolerate both)
      awk -v w="$want_role" '
        /^---$/{f++}
        f==1 && ($0 == "role: " w || $0 == "role: \"" w "\"" || $0 == "role: '\''" w "'\''") { found=1; exit }
        END { exit !found }
      ' "$identity_file" || return 1
      # Fleet-drift signal: warn on quoted variants after a match confirmed.
      # (Only fires when a match was found + the shape is quoted — cheap post-hoc grep.)
      if grep -qE "^role: [\"'].*[\"']$" "$identity_file" 2>/dev/null; then
        log "WARN: '$identity_file' has quoted role frontmatter — Skynet convention is unquoted (fleet-drift signal)"
      fi
      return 0
    }
    ```

    ### Part C — Add scan_role_archive_requested_sentinels() function (immediately after scan_archive_requested_sentinels, ~L998 area)

    Add the scanner. Full body per RESEARCH § "Code Examples" #4, adapted to the exact log conventions of the file:
    ```
    # ---- user-initiated role archive scanner (Phase 133 D-01/D-05/D-06/D-07/D-08/D-09/D-10/D-12) ----
    # scan_role_archive_requested_sentinels()
    #
    # Sibling of scan_archive_requested_sentinels. Walks $ROLES_DIR/*/ every
    # reconcile tick (~15s cadence). For each role folder carrying a
    # `.archive-requested` sentinel:
    #
    #   1. FRESH ENUMERATION (D-07): walk $IDENTITIES_DIR/*/ and collect every
    #      identity whose frontmatter carries `role: <role_name>` via
    #      identity_has_role(). No snapshot; the disk walk IS the state.
    #
    #   2. FAIL-SOFT CASCADE (D-08): invoke retire_identity(name) on each
    #      enumerated identity. Do NOT abort on individual failures — try every
    #      identity, accumulate failures. Guards (.pinned / .no-dormancy /
    #      coordinator: true) are BYPASSED (D-10) — the scanner doesn't check
    #      them, uniform with the user-initiated identity archive path.
    #
    #   3. CONDITIONAL FOLDER MOVE (D-09): if ALL enumerated identities retired
    #      cleanly, mv $ROLES_DIR/<name>/ to $ROLES_ARCHIVE_DIR/<name>/ and
    #      clean up the sentinel that travelled with the folder. If ANY failed,
    #      keep the role folder in the live tree; emit a LOUD ERROR: log line
    #      naming each failed identity + which step failed + the underlying error
    #      (mirrors the D-15 carve-out that the removed retire-stuck logging
    #      used to occupy).
    #
    #   4. SENTINEL ALWAYS DELETED (D-06): whether cascade succeeded, partially
    #      succeeded, or fully failed, remove the sentinel at end-of-tick. No
    #      cross-tick persistence of the archival intent. Operator retries via a
    #      fresh UI click (D-11), which drops a fresh sentinel and the scanner
    #      naturally picks up only identities not-yet-retired (D-11).
    #
    # D-12: empty cascade is the same code path — zero identities → for-loop is
    # zero-iteration → all_clean stays true → folder move happens immediately.
    #
    # D-18: per-box scope — no cross-box coordination. Scanner runs on the box
    # holding the role.
    #
    # D-15 silent discipline exception: the LOUD ERROR: log lines on partial
    # failure are the ONE explicit carve-out (same status as the removed
    # retire-stuck logging in the D-14 refactor).
    scan_role_archive_requested_sentinels() {
      local d role_name identity_dir identity_name
      for d in "$ROLES_DIR"/*/; do
        [ -d "$d" ] || continue                          # nullglob-miss guard
        role_name="$(basename "$d")"
        [ -f "$d/.archive-requested" ] || continue

        log "role '$role_name' archive: .archive-requested detected — cascading"

        # D-07: fresh enumeration on every scan.
        local to_retire=()
        for identity_dir in "$IDENTITIES_DIR"/*/; do
          [ -d "$identity_dir" ] || continue
          identity_name="$(basename "$identity_dir")"
          [ -f "$identity_dir/$identity_name.md" ] || continue
          if identity_has_role "$identity_dir/$identity_name.md" "$role_name"; then
            to_retire+=("$identity_name")
          fi
        done

        log "role '$role_name' cascade: ${#to_retire[@]} identities hold this role"

        # D-08 fail-soft cascade + D-10 guards bypassed (scanner doesn't check them).
        local failed=0 total=${#to_retire[@]} failed_names=""
        local ident
        for ident in "${to_retire[@]}"; do
          if retire_identity "$ident"; then
            log "role '$role_name' cascade: identity '$ident' retired cleanly"
          else
            failed=$((failed + 1))
            failed_names="$failed_names $ident"
            log "ERROR: role '$role_name' cascade: identity '$ident' FAILED to retire (see per-step retire logs above)"
          fi
        done

        # D-09: folder moves only if ALL identities retired cleanly.
        # D-06: sentinel deleted regardless of cascade outcome.
        # D-12: empty cascade — failed=0 total=0 all_clean is true — folder move fires immediately.
        if [ "$failed" -eq 0 ]; then
          mkdir -p "$ROLES_ARCHIVE_DIR" 2>/dev/null
          if mv "$d" "$ROLES_ARCHIVE_DIR/$role_name" 2>/dev/null; then
            # Sentinel travelled with the folder into the archive — clean it up there.
            rm -f "$ROLES_ARCHIVE_DIR/$role_name/.archive-requested" 2>/dev/null
            log "role '$role_name' cascade complete: $total identities retired, folder moved to archive"
          else
            # All-clean cascade but move failed — anomaly. Log LOUD, still delete the live sentinel (D-06).
            log "ERROR: role '$role_name' cascade: all identities retired but folder move FAILED — role folder retained in live tree"
            rm -f "$d/.archive-requested" 2>/dev/null
          fi
        else
          # D-09 partial failure — role folder stays live; sentinel still deleted (D-06).
          log "ERROR: role '$role_name' cascade PARTIAL: $failed/$total identities failed:${failed_names}. Role folder retained in live tree. Retry via UI."
          rm -f "$d/.archive-requested" 2>/dev/null
        fi
      done
    }
    ```

    Notes:
      - Preserve the D-15 silent-by-design discipline: routine `log` lines (sentinel detected, cascade count, per-identity retired cleanly) are diagnostic plumbing — they do NOT carry `ERROR:` prefix. Only failure lines (partial cascade, mv failure, per-identity retire failure) get the `ERROR:` prefix, matching the carve-out precedent from the removed retire-stuck logging.
      - The "which step failed and underlying error" details LIVE inside the per-step log lines already emitted by `retire_identity()` (from Task 1 Part A/B of Plan 133-03 — `ERROR: '<name>' retire step 1 (matrix deactivate) FAILED …` etc.). The cascade's partial-failure log line references those upstream per-step lines rather than re-emitting the underlying error text — this keeps the log DRY. Log line explicitly says "see per-step retire logs above" so an operator scrolling back finds them.
      - The `for ident in "${to_retire[@]}";` loop MUST handle the empty-array case gracefully. In bash 4+, iterating an empty array is zero iterations — the loop body simply doesn't execute. Verify by testing manually or trusting bash semantics; add a defensive `if [ "${#to_retire[@]}" -eq 0 ]` short-circuit if the planner prefers explicit.
      - The `mkdir -p "$ROLES_ARCHIVE_DIR"` is unconditional (per D-16 — first archival on any box creates the archive sibling on demand, mirroring the identity-archive precedent at retire_identity() step 4b area).

    ### Part D — Wire into reconcile() at ~L2327

    Find the existing `scan_archive_requested_sentinels` line at ~L2327. Add the new scanner IMMEDIATELY AFTER it, with a comment:
    ```
    scan_archive_requested_sentinels                 # Phase 115 D-10: user-initiated identity archive (every tick, no gate, bypasses guards)
    scan_role_archive_requested_sentinels            # Phase 133 D-01/D-05: user-initiated ROLE archive (every tick, no gate, cascades retire_identity per D-08, moves role folder per D-09 if all-clean)
    ```

    Do NOT introduce a new gate (like the 24h `run_archive_scan_if_due` gate) — role archives are user-initiated and every-tick per D-20 (no automated role archival) + D-11 (retry semantic is fresh-click).
  </action>
  <verify>
    <automated>shellcheck substrate/scripts/agent-supervisor.sh && grep -c 'scan_role_archive_requested_sentinels\|identity_has_role\|ROLES_DIR\|ROLES_ARCHIVE_DIR' substrate/scripts/agent-supervisor.sh</automated>
  </verify>
  <done>
    - `shellcheck substrate/scripts/agent-supervisor.sh` exits 0 (no new warnings).
    - `grep -c 'scan_role_archive_requested_sentinels' substrate/scripts/agent-supervisor.sh` returns >= 2 (function definition + reconcile wire-up call).
    - `grep -c 'identity_has_role' substrate/scripts/agent-supervisor.sh` returns >= 2 (function definition + usage in scanner).
    - `grep -c '^ROLES_DIR=\|^ROLES_ARCHIVE_DIR=' substrate/scripts/agent-supervisor.sh` returns exactly 2 (the two new constant declarations).
    - `grep -c 'AGENT_ROLES_DIR\|AGENT_ROLES_ARCHIVE_DIR' substrate/scripts/agent-supervisor.sh` returns >= 2 (env-override references in the constant defaults).
    - The reconcile wire-up line for `scan_role_archive_requested_sentinels` appears IMMEDIATELY AFTER the `scan_archive_requested_sentinels` line — verify by reading lines 2325-2331 of the file post-edit.
    - Full test suite (both existing + new): `bash substrate/scripts/tests/agent-supervisor-archive-scan.sh` still passes green (no regressions from adding the new constants/functions/wire-up).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create new bash test file `agent-supervisor-role-archive.test.sh`</name>
  <files>substrate/scripts/tests/agent-supervisor-role-archive.test.sh</files>
  <read_first>
    - substrate/scripts/tests/agent-supervisor-archive-scan.sh (~1834 lines — the reference harness. Use its convention verbatim: shellcheck header block; `assert_eq / assert_neq / assert_gte / assert_file / assert_nofile / assert_grep / safe_grep_count / setup_scratch / teardown_scratch / fixture_identity / fixture_relay_json / start_stub_homeserver / stop_stub_homeserver / _source_supervisor / _retire_test_preamble / run_test`. Read the file's first ~320 lines end-to-end to see the harness. Read the `_114_setup_identity` convenience wrapper at ~L829-840 for the sentinel-drop pattern.)
    - substrate/scripts/agent-supervisor.sh (post-Task-1 state — the file under test).
    - .planning/phases/133-role-archival/133-CONTEXT.md § D-05 through D-12 (test-relevant locked semantics).
    - .planning/phases/133-role-archival/133-RESEARCH.md § "Pitfall 2" (identity_has_role — the test must exercise the substring-safety case: two roles `foo` and `foo-bar`, identities holding each, cascade of `foo` must NOT touch the `foo-bar` identities).
  </read_first>
  <behavior>
    - Test A (`test_role_cascade_happy_path_2_identities`): Fixture: role folder `$scratch-roles/box-maintainer/` with `.archive-requested` sentinel; two identity folders `alpha` + `beta` both with `role: box-maintainer` in frontmatter; both with working 200-stub relay.json. Invoke `scan_role_archive_requested_sentinels`. Assert (post-scan):
      - Both `alpha` and `beta` folders are gone from `$scratch/`.
      - Both `alpha` and `beta` folders exist under `$scratch-archive/` (identities archive).
      - Role folder `$scratch-roles/box-maintainer/` is gone.
      - Role folder exists at `$scratch-roles-archive/box-maintainer/`.
      - The sentinel `.archive-requested` does NOT exist anywhere (neither in live nor archive location — it travelled with the mv, then cleanup deleted it).
      - The scanner's log output contains `cascade: 2 identities hold this role` and `cascade complete: 2 identities retired, folder moved to archive`.

    - Test B (`test_role_cascade_fail_soft_partial`): Fixture: role folder with sentinel; two identities `gamma` + `delta` both holding the role; `gamma` has a 200-stub relay (retires cleanly), `delta` has a 500-stub relay (retire fails all 3 attempts at step 1). Invoke scanner. Assert:
      - `gamma` folder is gone from live, present in identities-archive.
      - `delta` folder still exists in live tree, `.archive-requested` sentinel still present INSIDE `delta` (step 4a not reached since step 1 exhausted retries).
      - Role folder is STILL in the live tree (D-09: partial cascade → role stays).
      - The `.archive-requested` sentinel on the ROLE folder is DELETED (D-06: sentinel always deleted).
      - Log output contains `ERROR: role '<role>' cascade PARTIAL: 1/2 identities failed: delta. Role folder retained in live tree. Retry via UI.` (or the exact wording chosen in Task 1 — align).
      - Log output contains `ERROR: role '<role>' cascade: identity 'delta' FAILED to retire`.

    - Test C (`test_role_cascade_empty_immediate_folder_move`): Fixture: role folder with sentinel; ZERO identities hold the role (empty identities dir OR identities exist but none carry the role in frontmatter). Invoke scanner. Assert:
      - Role folder moved to roles-archive immediately (D-12 degenerate empty cascade).
      - Sentinel deleted.
      - Log: `cascade: 0 identities hold this role` and `cascade complete: 0 identities retired, folder moved to archive`.

    - Test D (`test_role_cascade_guard_bypass`): Fixture: role folder with sentinel; two identities `pinned-one` (with `.pinned` sentinel) and `coord-one` (with `coordinator: true` in frontmatter). Both hold the role. Both have 200-stub relay. Invoke scanner. Assert:
      - Both identities retired cleanly (guards BYPASSED per D-10 — the scanner does not check .pinned or coordinator).
      - Role folder moved to archive.
      - Log: cascade complete with 2 identities retired.

    - Test E (`test_role_cascade_sentinel_deleted_on_full_failure`): Fixture: role folder with sentinel; one identity `epsilon` with 500-stub relay (retire fails). Invoke scanner. Assert:
      - `epsilon` folder still in live tree.
      - Role folder still in live tree (D-09 partial → stays).
      - Sentinel on role folder DELETED (D-06 always).
      - Second invocation of the scanner (without dropping a fresh sentinel) is a no-op — no cascade attempted because no sentinel — mirrors the "operator retries via fresh UI click" semantic (D-11).

    - Test F (`test_scanner_noop_when_no_sentinel`): Fixture: role folder exists at `$scratch-roles/some-role/` but NO `.archive-requested` sentinel; a handful of identities also exist. Invoke scanner. Assert:
      - Role folder untouched.
      - No identities retired.
      - Log output does NOT contain `cascade` or `.archive-requested detected`.

    - Test G (`test_identity_has_role_substring_safety`): Direct unit test of the `identity_has_role` helper — this is the Pitfall 2 lock. Create two identity files: one with `role: foo` in frontmatter, another with `role: foo-bar`. Then assert:
      - `identity_has_role <foo-identity-file> "foo"` returns 0.
      - `identity_has_role <foo-bar-identity-file> "foo"` returns 1 (must NOT match — substring safety).
      - `identity_has_role <foo-identity-file> "foo-bar"` returns 1 (must NOT match).
      - `identity_has_role <foo-bar-identity-file> "foo-bar"` returns 0.

    - Test H (`test_identity_has_role_quoted_variant_warns`): Direct unit test — create an identity file with `role: "quoted-role"` (double-quoted YAML variant). Assert:
      - `identity_has_role <file> "quoted-role"` returns 0 (matches quoted variant).
      - The stderr output OR the captured log contains `WARN` + `quoted role frontmatter` (fleet-drift signal fires).

    - Test I (`test_identity_has_role_body_line_ignored`): Identity file with `role: foo` NOT in the frontmatter but in the body prose (e.g. after the second `---`). Assert `identity_has_role <file> "foo"` returns 1 — the awk-between-first-two-fences discipline blocks body mentions.

    - Test manifest (bottom of file): all 9 tests wired via `run_test` invocations.
  </behavior>
  <action>
    Create `substrate/scripts/tests/agent-supervisor-role-archive.test.sh`. Model verbatim after `agent-supervisor-archive-scan.sh`:

    (a) Shebang + shellcheck header block. Copy the first ~10 lines of the sibling file (bash shebang, shellcheck directives, the SC2317 suppress note about indirect run_test invocation).

    (b) Locate the supervisor script the same way the sibling does — via `SUPERVISOR="${SUPERVISOR:-$(dirname "$0")/../agent-supervisor.sh}"`.

    (c) Test harness setup: reuse the harness by SOURCING it. Two options:
      - Option 1: Extract the harness (helpers + fixture_identity + start_stub_homeserver + assertion macros) into a separate `substrate/scripts/tests/lib/test-harness.sh` file that both test files source. This is cleaner but adds a refactor scope to this task.
      - Option 2: Copy the harness verbatim into the new file (duplication). Less clean but keeps the plan scope tight.
      - Recommendation: Option 2 for this phase (tight scope). Note in a TODO comment at the top of the new file that the harness would benefit from an extract-shared-lib refactor in a future phase. The duplication is bounded (~300 lines) and doesn't drift because the sibling file is stable.
    
    Alternative Option 3: source the sibling test file conditionally (`AGENT_SUPERVISOR_TEST_LIB_ONLY=1 source agent-supervisor-archive-scan.sh` if the sibling supports lib-only mode). Read the sibling to check — if it doesn't, don't add that mode as part of this task (out of scope).

    (d) Add a `setup_role_scratch()` helper that extends `setup_scratch` — creates the identities scratch dir, the identities-archive sibling, PLUS a roles scratch dir `$scratch-roles/` and a roles-archive sibling `$scratch-roles-archive/`. Also set the two new env vars for the supervisor source:
    ```
    _source_supervisor_with_roles() {
      local scratch="${1:-/tmp}"
      export AGENT_IDENTITIES_DIR="$scratch"
      export AGENT_IDENTITIES_ARCHIVE_DIR="${scratch}-archive"
      export AGENT_ROLES_DIR="${scratch}-roles"
      export AGENT_ROLES_ARCHIVE_DIR="${scratch}-roles-archive"
      export DORMANCY_STATE_DIR="$scratch/.state"
      export AGENT_SUPERVISOR_LIB_ONLY=1
      # shellcheck disable=SC1090
      source "$SUPERVISOR" 2>/dev/null || true
    }

    setup_role_scratch() {
      local s; s=$(mktemp -d)
      mkdir -p "$s/.state" "${s}-archive" "${s}-roles" "${s}-roles-archive"
      printf '%s' "$s"
    }

    teardown_role_scratch() {
      local s="$1"
      [ -n "$s" ] && [ -d "$s" ] && rm -rf "$s"
      [ -n "$s" ] && [ -d "${s}-archive" ] && rm -rf "${s}-archive"
      [ -n "$s" ] && [ -d "${s}-roles" ] && rm -rf "${s}-roles"
      [ -n "$s" ] && [ -d "${s}-roles-archive" ] && rm -rf "${s}-roles-archive"
    }
    ```

    (e) Add a `fixture_role_folder(<scratch>, <role_name>, [--with-sentinel])` helper that creates `$scratch-roles/<role_name>/` and optionally drops the `.archive-requested` sentinel.

    (f) Enhance `fixture_identity` (if not already flexible enough) to accept a `--role <role_name>` flag that writes `role: <role_name>` in the frontmatter instead of the default `role: test`. Read the existing fixture at ~L188-215 of the sibling file — the current fixture hardcodes `role: test` in the frontmatter emission. Add the flag inline or wrap it with a `fixture_identity_with_role` sibling. Planner's call.

    (g) Write the 9 test functions per the behavior block above. Each test:
      - Calls `setup_role_scratch` → captures the scratch path.
      - Creates role folder(s) via `fixture_role_folder`.
      - Creates identity folder(s) via `fixture_identity` + `fixture_relay_json`.
      - Starts stub homeserver (if any identity's step 1 will fire) via `start_stub_homeserver` — potentially with sequence-code support from Plan 133-03 Task 2 (needed for the fail-then-succeed pattern but Test B in this file only needs a single fixed 500 for delta, so may or may not need the enhanced stub).
      - Invokes the scanner: `out=$( _source_supervisor_with_roles "$scratch" ; scan_role_archive_requested_sentinels 2>&1 ) || true`.
      - Runs assertions on filesystem state + captured log output.
      - Calls `stop_stub_homeserver` + `teardown_role_scratch`.

    (h) At the bottom of the file: invocation manifest.
    ```
    # ─── Phase 133 D-01/D-05/D-06/D-07/D-08/D-09/D-10/D-12: role cascade scanner ───
    run_test test_role_cascade_happy_path_2_identities
    run_test test_role_cascade_fail_soft_partial
    run_test test_role_cascade_empty_immediate_folder_move
    run_test test_role_cascade_guard_bypass
    run_test test_role_cascade_sentinel_deleted_on_full_failure
    run_test test_scanner_noop_when_no_sentinel

    # ─── identity_has_role helper (Pitfall 2 substring-safety + quoted-variant handling) ───
    run_test test_identity_has_role_substring_safety
    run_test test_identity_has_role_quoted_variant_warns
    run_test test_identity_has_role_body_line_ignored

    printf '\n===============================\n'
    printf 'PASS: %s  FAIL: %s\n' "$PASS" "$FAIL"
    if [ "$FAIL" -gt 0 ]; then
      printf 'Failures:\n'
      for _f in "${FAILURES[@]}"; do
        printf '  - %s\n' "$_f"
      done
      exit 1
    fi
    exit 0
    ```
  </action>
  <verify>
    <automated>bash substrate/scripts/tests/agent-supervisor-role-archive.test.sh</automated>
  </verify>
  <done>
    - The file exists at `substrate/scripts/tests/agent-supervisor-role-archive.test.sh` and is executable (`chmod +x` if the sibling has +x).
    - `bash substrate/scripts/tests/agent-supervisor-role-archive.test.sh` exits 0.
    - Final line of output: `PASS: 9  FAIL: 0`.
    - `shellcheck substrate/scripts/tests/agent-supervisor-role-archive.test.sh` exits 0.
    - `grep -c '^run_test ' substrate/scripts/tests/agent-supervisor-role-archive.test.sh` returns >= 9.
    - All 9 test-function names from the behavior block appear as `<name>()` function definitions AND as `run_test <name>` invocations.
    - The existing test file `agent-supervisor-archive-scan.sh` continues to pass green (no regression from the Task 1 supervisor edits).
  </done>
</task>

</tasks>

<verification>
- `bash substrate/scripts/tests/agent-supervisor-role-archive.test.sh` reports `PASS: 9 FAIL: 0`.
- `bash substrate/scripts/tests/agent-supervisor-archive-scan.sh` continues to pass green (no regression on the sibling tests from Task 1 edits — this is important because the new constants + wire-up sit in the same script).
- `shellcheck substrate/scripts/agent-supervisor.sh substrate/scripts/tests/agent-supervisor-role-archive.test.sh` both exit 0.
- All new functions declared with valid bash syntax: `scan_role_archive_requested_sentinels`, `identity_has_role`.
- Constants `ROLES_DIR` + `ROLES_ARCHIVE_DIR` declared adjacent to `IDENTITIES_DIR` block with env-override support.
- Reconcile wire-up line is the LAST scanner call in the pre-`resolve_identities` block (reads intuitively: identity user-initiated archive first, then role archive).
</verification>

<success_criteria>
- New scanner cascades cleanly on happy path (Test A), fail-softs on partial (Test B), handles empty degenerate case (Test C), bypasses guards (Test D), always deletes sentinel (Test E), no-ops when sentinel absent (Test F).
- `identity_has_role` frontmatter parser is substring-safe (Test G — Pitfall 2 lock), warns on quoted variants (Test H — Assumption A2 lock), ignores body-line mentions (Test I — Pitfall 2 frontmatter-only lock).
- Test file green + shellcheck clean.
- No regression on the existing sibling test file.
- Reconcile wired at the correct site.
</success_criteria>

<output>
Create `.planning/phases/133-role-archival/133-04-SUMMARY.md` when done, including:
- Confirmation that all 9 tests are green (list them with pass/fail)
- Confirmation that `agent-supervisor-archive-scan.sh` still passes green (no regression)
- Which approach was taken for harness reuse (Option 1 shared lib / Option 2 duplication / Option 3 source-sibling) and rationale
- Whether the WARN-on-quoted-role stderr mechanism worked as designed OR the alternative post-hoc grep approach was used, with rationale
- Any surprises during scanner-vs-retire-refactor integration
</output>
