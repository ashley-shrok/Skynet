---
phase: 133-role-archival
plan: 06
type: execute
wave: 4
depends_on:
  - 133-04
files_modified:
  - substrate/skills/id/SKILL.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "A new section `## On archiving a role` exists in `substrate/skills/id/SKILL.md`, sibling to the existing `## On archiving an identity` section (~L790 in the current file)."
    - "The new section documents: (a) how the operator triggers a role archive (right-click a role in RolesListModal → Archive → double confirm), (b) the sentinel mechanism (`.archive-requested` at `~/fleet/roles/<name>/`), (c) the cascade semantics (fresh enumeration each scan, fail-soft, guard-bypass, role-folder moves only if all identities retired cleanly), (d) the failure semantics (partial cascade leaves role folder in live tree, sentinel deleted regardless, LOUD ERROR log), (e) the not-reversible-yet note (D-19), (f) an explicit user-initiated-only rule mirroring the identity-archive USER-INITIATED ONLY block."
    - "The new section explicitly notes that role archival is a NEW gesture in Phase 133; the identity-archive mechanism it composes over is unchanged (except for the retire-fail-count / retire-stuck removal in Phase 133 D-14) and callers of `/id archive` behavior are unaffected."
    - "The section notes the click-vs-scan enumeration race honestly (RESEARCH Pitfall 6 + D-11): the frontend dialog shows the identity list AT CLICK TIME; the supervisor freshly enumerates AT SCAN TIME; if an identity spawns/despawns between click and scan, the operator sees discrepancy in the log lines. This is design, not a bug — document it."
    - "The section makes clear that this is USER-INITIATED ONLY (no automated role archival, D-20) with a callout mirroring the identity-archive USER-INITIATED ONLY block at ~L800-806."
  artifacts:
    - path: substrate/skills/id/SKILL.md
      provides: "New `## On archiving a role` section, ~30-60 lines of body prose covering trigger + sentinel mechanism + cascade + failure semantics + user-initiated-only + not-reversible-yet."
  key_links:
    - from: substrate/skills/id/SKILL.md `## On archiving a role`
      to: substrate/skills/id/SKILL.md `## On archiving an identity` (existing, ~L790)
      via: "sibling section — same structure + heading level"
      pattern: "^## On archiving a role"
---

<objective>
Wave D documentation update. Add the id-skill docs section describing role archival, sibling to the existing "On archiving an identity" section. Text-content only; no code changes.

Depends on Plan 133-04 (Wave C — the scanner must exist as the described mechanism so the docs are describing shipped behavior, not aspirational behavior). Does NOT depend on Plan 133-05 (the UI half) because the docs describe the mechanism from the identity-agent's perspective (what to know if you're a fleet identity whose role gets archived) rather than the operator's perspective (which lives in-app as tooltip / confirmation copy). If Ashley wants an operator-facing readme somewhere, that's a separate scope.

Purpose (from the shape file's Prior context section): "Identity archival already exists. When an operator archives an identity today, a sentinel drops on the identity folder, the supervisor sees it on its next reconcile tick, and it runs a five-step retire … That mechanism is the direct model for role archival and its identity-retirement building block is reused verbatim by the role cascade." The id-skill's identity-archive section is the counterpart on the identity-agent side; adding a role-archive sibling gives identities a reference for what happens to them when their role gets archived above them.

Output: `substrate/skills/id/SKILL.md` has a new `## On archiving a role` section adjacent to the existing `## On archiving an identity` section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-role-archival/133-CONTEXT.md
@.planning/shapes/shape-role-archival.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `## On archiving a role` section to `substrate/skills/id/SKILL.md`</name>
  <files>substrate/skills/id/SKILL.md</files>
  <read_first>
    - substrate/skills/id/SKILL.md L790-836 (the existing `## On archiving an identity` section — read the full section: preamble (what/why), USER-INITIATED ONLY callout, When invoked numbered steps, the terminal-not-reversible note. The new section mirrors this structure adapted for the role-cascade semantic.)
    - substrate/skills/id/SKILL.md L836-865 (the `## File locations` section that follows — the new role-archive section is inserted BEFORE this file-locations section, so the ordering of the two archive sections is: identity-archive → ROLE-ARCHIVE (new) → file-locations.)
    - .planning/phases/133-role-archival/133-CONTEXT.md § D-01 through D-21 (the full locked semantic — the docs describe all locked decisions in operator/agent-facing terms).
    - .planning/shapes/shape-role-archival.md (the philosophy section — the docs pick up the "operator gesture, supervisor cascade, honest failure mode, sentinel is the memory" framing).
    - .planning/phases/133-role-archival/133-RESEARCH.md § "Pitfall 6" (click-vs-scan enumeration race — must be documented honestly per D-11).
  </read_first>
  <action>
    Insert a new section `## On archiving a role` into `substrate/skills/id/SKILL.md`, positioned AFTER the existing `## On archiving an identity` section (which ends at ~L834) and BEFORE the existing `## File locations` section (which starts at ~L836).

    Suggested structure — mirror the identity-archive section's shape while adapting content for the role cascade. Target: ~40-60 lines of body prose.

    ```
    ## On archiving a role

    Role archival is a NEW gesture in Phase 133 (adjacent to but distinct
    from identity archival — the two are compose-over-each-other, not
    duplicate paths). Where identity archival tears down a single fleet
    identity, ROLE archival tears down the ROLE FOLDER at
    `~/fleet/roles/<name>/` plus cascade-retires every identity that
    currently holds that role. The operator clicks once in Skynet; the
    supervisor on the box holding the role does the entire cascade.

    ⚠️ **USER-INITIATED ONLY — an agent NEVER drops
    `~/fleet/roles/<name>/.archive-requested` on its own initiative,
    under any circumstances.** Same rule and same reasons as `/id reset`
    and `/id archive`. If you *think* a role archive would make sense
    (task is done, the whole line of work has wrapped up, no future need
    for this role), OFFER — don't self-execute. The trigger belongs to
    the human operator.

    ### How it works (mechanism)

    1. Operator right-clicks a role in Skynet's roles list, picks
       `Archive` from the context menu.
    2. Two confirmation dialogs follow. The first names the role and
       lists every identity that will be cascade-archived (one line per
       identity, using the identity's task field with a fallback to its
       display name — same fallback Skynet's sidebar uses). The list is
       complete; nothing is truncated. If zero identities hold the role,
       the first dialog says so plainly.
    3. Both dialogs OK'd, Skynet POSTs `/roles/<name>/archive` to the box
       holding the role.
    4. The backend drops a `.archive-requested` sentinel at
       `~/fleet/roles/<name>/`.
    5. The supervisor's next reconcile tick (~15s) finds the sentinel
       and runs the cascade inline on that tick:
       - Fresh-enumerates identities holding the role by walking
         `~/fleet/identities/*/*.md` and matching `role:` frontmatter.
       - Runs the full identity retire (matrix deactivate → graceful
         `/exit` → tmux kill → sentinel delete + workspace cleanup →
         folder move to `~/fleet/identities-archive/<name>/`) on each,
         fail-soft: every identity is attempted regardless of individual
         failures.
       - If ALL identities retired cleanly, the role folder moves to
         `~/fleet/roles-archive/<name>/`.
       - If ANY identity failed to retire, the role folder STAYS in the
         live tree; the supervisor logs LOUDLY which identities failed
         and at which step.
       - The sentinel is deleted at end-of-tick regardless of outcome.

    ### Failure semantics

    Role archival is honest about partial failure. When the cascade
    fails partway:

    - Successfully-retired identities are already gone from the live
      tree (moved to identities-archive).
    - Failed identities remain in the live tree with their
      `.archive-requested` sentinel still present (a retry-on-next-tick
      signal for the identity-archive path).
    - The role folder remains in the live tree (D-09: folder moves only
      on all-clean cascade).
    - The role sentinel is deleted (D-06: one-shot signal — subsequent
      supervisor ticks do NOT re-cascade automatically).
    - The operator retries by clicking Archive again in Skynet. Because
      the scanner freshly enumerates the disk on every scan, a retry
      naturally picks up only the identities that weren't already
      archived — the ones that succeeded on the first pass are gone
      from the live tree and get silently skipped.

    ### Guard bypass

    Identities holding a role being cascade-archived have their
    `.pinned`, `.no-dormancy`, and `coordinator: true` guards
    **bypassed** during the cascade. Those guards exist to protect
    against AUTOMATED retirement (e.g. the 180-day daily dormancy sweep
    won't retire a pinned identity). A deliberate operator click on
    Archive is not automated — the click means intent, the intent is
    honored.

    ### Click-vs-scan enumeration race (design, not bug)

    The frontend confirmation dialog shows the identity list at CLICK
    TIME. The supervisor freshly enumerates at SCAN TIME (~15s later
    typically). If a new identity spawns holding the role between click
    and scan, the supervisor will archive it too, even though the
    operator's dialog didn't name it. Conversely, if an identity is
    manually re-assigned to another role between click and scan, the
    supervisor won't include it in the cascade. The operator sees this
    discrepancy in the log lines. This is honest failure-mode surfacing,
    not a bug — the shape philosophy is "sentinel is the memory,
    supervisor freshly reads disk on every tick."

    ### Not reversible (yet)

    Role archival is one-way for now. The archived role folder is at
    `~/fleet/roles-archive/<name>/` on disk and can be moved back by
    hand if desired, but there is no gesture in Skynet to un-archive a
    role. Reversibility is a separate future concern (would require
    settling rehydrate semantics for the cascade of identities that
    were retired alongside the role).

    ### What travels with the archive

    Everything in `~/fleet/roles/<name>/` at the moment of archival
    travels verbatim into `~/fleet/roles-archive/<name>/`: the role
    file, history, runbooks, reference documents, wakeups, bounties,
    any ad-hoc content that identities working the role had accumulated.
    Roles should not hold credentials as a hygiene matter, but this
    archival gesture is not the place to introduce scrubbing (D-17).

    ### Per-box scope

    A role and every identity holding it live on the same box (id-skill
    invariant: "Identities and roles are strictly 1:1 with a host").
    The archival gesture is per-box: the sentinel drops on that box,
    the scanner runs on that box, the cascade retires that box's
    identities. There is no cross-box coordination.
    ```

    Notes on the section:
      - Keep the ⚠️ USER-INITIATED ONLY callout early (mirrors the identity-archive section's ordering — puts the safety rule where the reader will see it first).
      - Bullet points inside the mechanism steps are OK — the identity-archive section uses numbered steps at ~L810 similarly.
      - Do NOT go into implementation code details (grep patterns, awk snippets, etc.) — the docs describe operator/agent-facing behavior, not the substrate implementation. If a fleet identity wants that detail they read `agent-supervisor.sh` directly.
      - Do NOT link to the phase-plan directory (`.planning/`) from the SKILL.md — that's project meta, not fleet-agent-facing content. The section references D-numbers inline where it's helpful for a future maintainer, but doesn't require the reader to have `.planning/phases/133-role-archival/` open.
      - The section is inserted BEFORE the existing `## File locations` section (at ~L836 today). After the insertion, the file structure order is:
        ```
        ## On archiving an identity   (existing, unchanged)
        ---                           (existing horizontal rule if present)
        ## On archiving a role         (NEW)
        ---                           (add matching horizontal rule)
        ## File locations              (existing, unchanged)
        ```
        Verify the horizontal-rule presence by reading L836 area first; match the surrounding formatting.

    Do NOT modify the existing `## On archiving an identity` section — the identity mechanism's caller-facing behavior is unchanged (the D-13/D-14 refactor in Plan 133-03 is caller-transparent: retire_identity() still returns 0/1 the same way, just without the cross-tick counter side-effects). If the existing section's step 3 references a "supervisor retry mechanism" or "retire-stuck sentinel," update ONLY that specific mention to describe the new inline-retry semantic — but a fresh read of L810-827 suggests it doesn't reference retire-stuck at all, so likely no edit needed to the identity-archive section.

    Verify final state: `grep -c '^## On archiving' substrate/skills/id/SKILL.md` returns exactly 2 (identity + role sections both present, both at H2).
  </action>
  <verify>
    <automated>grep -c '^## On archiving' substrate/skills/id/SKILL.md</automated>
  </verify>
  <done>
    - `grep -c '^## On archiving' substrate/skills/id/SKILL.md` returns exactly 2 (identity + role).
    - `grep -c 'USER-INITIATED ONLY' substrate/skills/id/SKILL.md` returns >= 2 (the identity-archive section's callout stays; the new role-archive callout adds a second occurrence).
    - `grep -F 'roles-archive' substrate/skills/id/SKILL.md` returns >= 1 (the archive location is documented).
    - `grep -F '.archive-requested' substrate/skills/id/SKILL.md` returns >= 2 (identity sentinel path already referenced in the identity-archive section; role sentinel path now added).
    - `grep -F 'guard bypass\|Guard bypass' substrate/skills/id/SKILL.md` returns >= 1 (D-10 semantic documented).
    - `grep -F 'fail-soft\|fail soft' substrate/skills/id/SKILL.md` returns >= 1 (D-08 semantic documented).
    - `grep -F 'no un-archive\|not reversible\|One-way\|one-way' substrate/skills/id/SKILL.md` returns >= 1 (D-19 documented).
    - No changes to the existing `## On archiving an identity` section (compare pre/post diff — should be empty for that section, unless a specific corrective edit was made per the plan's guidance).
    - No accidental references to `retire-stuck` or `retire-fail-count-*` in the new section (those mechanisms are gone in Phase 133 D-14; documenting them would be misleading).
    - Manual read of the new section post-write: language is clear, matches the shape file's philosophy tone, is friendly to a fleet identity reading it cold.
  </done>
</task>

</tasks>

<verification>
- `grep -c '^## On archiving' substrate/skills/id/SKILL.md` — exactly 2.
- File is committable as-is (no broken markdown, no half-finished sections).
- All D-decisions from CONTEXT.md that touch operator-facing semantics are represented (D-01, D-02, D-03, D-04 briefly; D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-16, D-17, D-18, D-19, D-20 in the section body).
- No claims that reference removed mechanism (`retire-stuck`, cross-tick counters — those are gone per D-14 and shouldn't appear in the new section).
</verification>

<success_criteria>
- New `## On archiving a role` section exists, sibling to `## On archiving an identity`.
- Section covers: USER-INITIATED ONLY, mechanism (5-step numbered), failure semantics, guard bypass, click-vs-scan race, not-reversible-yet, what-travels-with-archive, per-box scope.
- Existing identity-archive section is UNCHANGED (or minimally corrected only if it referenced removed mechanism).
- File remains valid markdown, all H2 headings render correctly.
</success_criteria>

<output>
Create `.planning/phases/133-role-archival/133-06-SUMMARY.md` when done, including:
- Confirmation of the new section's line count and location in the file
- Whether the pre-existing identity-archive section needed any edits (it likely did not)
- Confirmation that no removed-mechanism (retire-stuck / retire-fail-count) references landed in the new section
- Any deviations from the suggested section outline and why
</output>
