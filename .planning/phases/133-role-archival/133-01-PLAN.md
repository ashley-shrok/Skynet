---
phase: 133-role-archival
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/claude-session/per-role-file.ts
  - src/backend/claude-session/per-role-file.test.ts
  - src/backend/database/routes/role-archive.ts
  - src/backend/database/routes/role-archive.test.ts
  - src/backend/database/database.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "POST /roles/:name/archive with a valid ROLE_NAME_PATTERN name + hostId owned by the caller drops `.archive-requested` at ~/fleet/roles/<name>/ and returns 200 { ok: true } (per D-01, D-05, D-16)."
    - "POST /roles/:name/archive with a name that fails ROLE_NAME_PATTERN /^[a-z0-9-]+$/ returns 400 without touching disk."
    - "POST /roles/:name/archive with a hostId the caller does not own returns 404 (not 403 — no probe info leak, mirrors T-115-03-01)."
    - "POST /roles/:name/archive against an unreachable REMOTE host returns 504 without partial writes."
    - "POST /roles/:name/archive on SFTP/fs write failure returns 500 { error: \"failed to drop archive sentinel\" } without leaking underlying error text; conn.end() still fires on the REMOTE branch."
    - "The route is mounted in database.ts BEFORE the three existing `/roles` mounts (rolesListForHostRoutes, rolesCreateRoutes, rolesRoutes) so `/roles/:name/archive` is not shadowed by any future generic `/:roleName` handler — mirrors the identity-archive precedent at database.ts:2033."
    - "writeRoleFile(name, relPath, contents, opts) primitive exists as a NEW sibling of writeIdentityFile (NOT extended into writeIdentityFile), scoped to role paths under ~/fleet/roles/<name>/ with a bounded ALLOWED_ROLE_REL_PATHS whitelist initially containing only `.archive-requested`."
  artifacts:
    - path: src/backend/claude-session/per-role-file.ts
      provides: "New per-role-file primitive: writeRoleFile(name, relPath, contents, opts). LOCAL branch (isLocalHostId true) does tmp+rename via node fs against `${ROLES_HOST_DIR || ${os.homedir()}/fleet/roles}/<name>/<relPath>`. REMOTE branch delegates to writeMarkdownFileAtomic (SFTP tmp+atomic-rename) at relative path `fleet/roles/<name>/<relPath>`. Gates: ROLE_NAME_PATTERN + ALLOWED_ROLE_REL_PATHS whitelist, both fire BEFORE any I/O."
      exports: ["writeRoleFile", "ALLOWED_ROLE_REL_PATHS"]
    - path: src/backend/claude-session/per-role-file.test.ts
      provides: "vitest coverage: LOCAL write, REMOTE write via writeMarkdownFileAtomic mock, bad-name rejection, bad-relPath rejection, missing-conn on REMOTE rejection."
    - path: src/backend/database/routes/role-archive.ts
      provides: "New route module exporting Express router with POST /:name/archive. Auth + ROLE_NAME_PATTERN gate + resolveHostById filter + LOCAL/REMOTE branch + writeRoleFile sentinel drop + audit log."
      exports: ["default"]
    - path: src/backend/database/routes/role-archive.test.ts
      provides: "supertest-driven coverage: happy LOCAL, happy REMOTE, bad-name 400, missing-hostId 400, wrong-host 404, host-unreachable 504, disk-write-failure 500, unauthenticated 401."
    - path: src/backend/database/database.ts
      provides: "Mount site: `app.use(\"/roles\", roleArchiveRoutes)` mounted BEFORE rolesListForHostRoutes / rolesCreateRoutes / rolesRoutes at ~L2051 (mirror identity-archive mount at L2033)."
  key_links:
    - from: src/backend/database/routes/role-archive.ts
      to: src/backend/claude-session/per-role-file.ts
      via: "writeRoleFile(name, '.archive-requested', '', { hostId, conn })"
      pattern: "writeRoleFile.*\\.archive-requested"
    - from: src/backend/database/database.ts
      to: src/backend/database/routes/role-archive.ts
      via: "app.use('/roles', roleArchiveRoutes) mounted BEFORE the other three /roles mounts"
      pattern: "roleArchiveRoutes"
    - from: src/backend/claude-session/per-role-file.ts
      to: src/backend/claude-session/identity-artifact-reader.ts
      via: "writeMarkdownFileAtomic (REMOTE branch) + isLocalHostId + getLocalIdentitiesRoot-equivalent pattern"
      pattern: "writeMarkdownFileAtomic"
---

<objective>
Wave A backend half of Phase 133. Deliver:

1. `POST /roles/:name/archive` HTTP route on the Skynet backend that drops `.archive-requested` on the role folder — the single sentinel that triggers the supervisor's role cascade (D-01, D-05, D-16).
2. `writeRoleFile()` primitive — a NEW sibling of `writeIdentityFile` in a new module `per-role-file.ts`, NOT an extension of the identity primitive (per RESEARCH §5 recommendation — the H1 write⇔read parity lock on `writeIdentityFile` is identity-scoped by design; widening it would blur the invariant).
3. Route mounted BEFORE the existing three `/roles` mounts in `database.ts` (mirrors Phase 115 Plan 115-03 precedent at database.ts:2033).

Purpose: the frontend (Wave D) needs a well-defined POST endpoint to call when the operator confirms Archive on a role. The supervisor scanner (Wave C) picks up the sentinel that this endpoint drops. This plan is the write path from web layer to disk.

Output: `POST /roles/:name/archive` returns 200 with an empty `.archive-requested` file present on the role folder afterward.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-role-archival/133-CONTEXT.md
@.planning/phases/133-role-archival/133-RESEARCH.md
@.planning/shapes/shape-role-archival.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create `per-role-file.ts` primitive + tests</name>
  <files>src/backend/claude-session/per-role-file.ts, src/backend/claude-session/per-role-file.test.ts</files>
  <read_first>
    - src/backend/claude-session/per-identity-file.ts (the reference module this new file mirrors — read the whole file, ~399 lines. Copy the module docstring, the isEnoent helper, the shellSingleQuote helper if you keep chmod, the LOCAL tmp+rename shape, and the REMOTE `writeMarkdownFileAtomic` delegation verbatim in structure. This is the primary template.)
    - src/backend/claude-session/identity-artifact-reader.ts (imports source for `isLocalHostId`, `writeMarkdownFileAtomic`, and `getLocalIdentitiesRoot`. Verify the equivalent role-root env var pattern before writing localTargetPath — RESEARCH §5 suggests `ROLES_HOST_DIR` env override falling back to `${os.homedir()}/fleet/roles`; verify no existing helper `getLocalRolesRoot` exists via grep before hand-rolling.)
    - src/backend/utils/role-name-pattern.ts (source of ROLE_NAME_PATTERN /^[a-z0-9-]+$/ — import from here directly, do NOT redefine).
    - src/backend/database/routes/roles-create.ts (for observing how ROLE_NAME_PATTERN is used at HTTP layer today — informs the assertValidRoleName error message shape).
    - .planning/phases/133-role-archival/133-RESEARCH.md §5 "New writeRoleFile() sibling module" code example (verbatim shape to mirror — the file you write should look very close to this, except no chmod (roles don't hold credentials that need locking down as a hygiene invariant, per D-17 discussion — but do NOT add scrubbing logic either)).
    - .planning/phases/133-role-archival/133-CONTEXT.md § "Claude's Discretion" item 2: "Whether to introduce a per-role-file writer primitive as a separate module or extend `writeIdentityFile` to cover role paths — planner's call after checking whether an existing helper already covers the role-folder write path." Planner has decided: NEW sibling module per RESEARCH §5 rationale (H1 lock preservation).
  </read_first>
  <behavior>
    - Test 1 (LOCAL happy path): `writeRoleFile("box-maintainer", ".archive-requested", "", { hostId: <local>, conn: null })` writes an empty file at `<ROLES_HOST_DIR-or-homedir-fallback>/box-maintainer/.archive-requested` using tmp+rename. Assert `fs.stat` on the final path succeeds and file contents are empty string. Assert the tmp file (`.archive-requested.tmp`) does NOT exist after the rename.
    - Test 2 (REMOTE happy path): `writeRoleFile("box-maintainer", ".archive-requested", "", { hostId: <remote>, conn: <mockConn> })` calls `writeMarkdownFileAtomic(mockConn, "fleet/roles/box-maintainer/.archive-requested", "")`. Mock `writeMarkdownFileAtomic` and assert exact args (path is RELATIVE — no leading `/`, no `$HOME/` prefix — same discipline as per-identity-file.ts:143 remoteTargetPath).
    - Test 3 (bad role name rejected before I/O): `writeRoleFile("BadName!", ".archive-requested", "", { hostId: 1, conn: null })` throws with a message containing `invalid role name` and the ROLE_NAME_PATTERN source. Assert `fs.stat` on any file path was never called (spy).
    - Test 4 (bad relPath rejected before I/O): `writeRoleFile("box-maintainer", "malicious-file", "", { hostId: 1, conn: null })` throws with a message containing `invalid relPath` and the allowed list. Assert no fs write happened.
    - Test 5 (REMOTE with null conn throws): `writeRoleFile("box-maintainer", ".archive-requested", "", { hostId: <remote>, conn: null })` throws with message `conn required for remote host` (verbatim match — per-identity-file uses this exact string).
    - Test 6 (ALLOWED_ROLE_REL_PATHS starts minimal): `expect([...ALLOWED_ROLE_REL_PATHS]).toEqual([".archive-requested"])` — this pins the whitelist at exactly ONE entry so a future phase adding more entries has to update this test deliberately (bounded-scope discipline).
    - Test 7 (path shape LOCAL — env-honoring): with `process.env.ROLES_HOST_DIR = "/tmp/test-roles-root"`, `writeRoleFile("box-maintainer", ".archive-requested", "", { hostId: <local>, conn: null })` writes to `/tmp/test-roles-root/box-maintainer/.archive-requested`. Cleanup: `delete process.env.ROLES_HOST_DIR` in afterEach. (This is the containerized-Skynet bind-mount case — mirror the fix at per-identity-file.ts:108-118.)
  </behavior>
  <action>
    Create `src/backend/claude-session/per-role-file.ts`. Structure mirrors `per-identity-file.ts` — treat that file as a byte-shape template, changing only the identity → role concept substitution.

    (a) Module docstring: 30-40 lines explaining this is the per-ROLE (not per-identity) sibling primitive introduced in Phase 133. Note: NEW MODULE (not extension of writeIdentityFile) per RESEARCH §5 (H1 write⇔read parity lock on writeIdentityFile is identity-scoped by design; role paths get their own primitive to preserve the invariant). Cite D-01 (bounded whitelist) and D-16 (roles live at `~/fleet/roles/<name>/`, sibling of identities).

    (b) Imports:
      - `os`, `path`, `fs from "node:fs/promises"`
      - `type { Client as SSHClientType } from "ssh2"`
      - `type SFTPWrapper = import("ssh2").SFTPWrapper` (only if needed for future remove/exists helpers — this plan is write-only, so skip if unused)
      - `{ isLocalHostId, writeMarkdownFileAtomic } from "./identity-artifact-reader.js"`
      - `{ ROLE_NAME_PATTERN } from "../utils/role-name-pattern.js"`

    (c) Constants:
      - `export const ALLOWED_ROLE_REL_PATHS: ReadonlySet<string> = new Set([".archive-requested"]);` — start with exactly one entry per D-01 bounded-scope discipline. Comment: "Bounded whitelist; add entries only via a deliberate CONTEXT-locked decision in a future phase."

    (d) Path helpers:
      - `function localRoleTargetPath(name: string, relPath: string): string` — returns `path.join(process.env.ROLES_HOST_DIR ?? path.join(os.homedir(), "fleet", "roles"), name, relPath)`. Match the env-honoring pattern from per-identity-file.ts:108-118 (containerized-Skynet bind-mount fix). Add the same 2026-09-11 rationale comment adapted for roles.
      - `function remoteRoleTargetPath(name: string, relPath: string): string` — returns `` `fleet/roles/${name}/${relPath}` `` (RELATIVE path — no leading `/`, no `$HOME/`). Copy the docstring paragraph from per-identity-file.ts:120-142 explaining why this is relative (SFTP home-resolution behavior; do NOT insert `$HOME/` literal — it would ENOENT silently).

    (e) Gate helpers:
      - `function assertValidRoleName(name: string): void` — throws if `!ROLE_NAME_PATTERN.test(name)` with message including `ROLE_NAME_PATTERN.source`.
      - `function assertValidRoleRelPath(relPath: string): void` — throws if `!ALLOWED_ROLE_REL_PATHS.has(relPath)` with message including the allowed list.

    (f) Public interface:
      ```
      export interface WriteRoleFileOpts {
        hostId: number;
        conn: SSHClientType | null;
      }
      ```
      NO `chmod?` field — roles don't hold credentials as a hygiene invariant (per shape philosophy) and this phase adds no chmod site.

    (g) Public function:
      - `export async function writeRoleFile(name: string, relPath: string, contents: string, opts: WriteRoleFileOpts): Promise<void>`
      - Body: assertValidRoleName → assertValidRoleRelPath → branch on isLocalHostId(opts.hostId):
        - LOCAL: `const finalPath = localRoleTargetPath(name, relPath); const tmp = finalPath + ".tmp"; await fs.writeFile(tmp, contents, "utf-8"); await fs.rename(tmp, finalPath);`
        - REMOTE: `if (opts.conn === null) throw new Error("conn required for remote host"); await writeMarkdownFileAtomic(opts.conn, remoteRoleTargetPath(name, relPath), contents);`
      - Both gate helpers MUST run BEFORE any fs/SFTP work (belt-and-suspenders against T-133-01-02 path-traversal via role name — the reader gate is the primary defense at the HTTP layer, this is the write-layer defense).
      - DO NOT parent-mkdir the role directory here — role folder creation is roles-create.ts's job. If the role folder doesn't exist yet, this write SHOULD fail (invariant: archive-requested only makes sense on an existing role folder).

    (h) Do NOT add `removeRoleFile` or `roleFileExists` in this plan — write-only for Phase 133. Extension can happen in a future phase.

    Create `src/backend/claude-session/per-role-file.test.ts`. Model after `per-identity-file.test.ts` (read its describe/beforeEach/afterEach + fs.mkdtemp fixture pattern, ~600+ lines — take the same fixture setup + mocking strategy):
      - Use `vitest`. Mock `writeMarkdownFileAtomic` and `isLocalHostId` from `identity-artifact-reader.js`. For LOCAL tests, create a real tmp directory via `fs.mkdtemp` and set `process.env.ROLES_HOST_DIR` to it in beforeEach; clean up in afterEach.
      - Use a mocked hostId scheme: e.g. `mockIsLocalHostId.mockReturnValue(true)` for LOCAL tests, `false` for REMOTE tests.
      - Cover all 7 behavior cases above.
      - Assert exact `writeMarkdownFileAtomic` positional args in REMOTE tests (path is `"fleet/roles/box-maintainer/.archive-requested"` — no leading `/`).
  </action>
  <verify>
    <automated>npx vitest run src/backend/claude-session/per-role-file.test.ts</automated>
  </verify>
  <done>
    - `src/backend/claude-session/per-role-file.ts` exists and exports `writeRoleFile` + `ALLOWED_ROLE_REL_PATHS`.
    - `npx vitest run src/backend/claude-session/per-role-file.test.ts` reports 7 tests green with zero skipped.
    - `grep -F 'ROLE_NAME_PATTERN' src/backend/claude-session/per-role-file.ts` returns at least 2 hits (import + use in assertValidRoleName).
    - `grep -F 'writeMarkdownFileAtomic' src/backend/claude-session/per-role-file.ts` returns exactly 1 hit (REMOTE branch delegation — same audit surface discipline as per-identity-file.ts:237).
    - The whitelist has exactly one entry: `grep -c '"\\.archive-requested"' src/backend/claude-session/per-role-file.ts` returns >= 1 AND the ALLOWED_ROLE_REL_PATHS array literal contains no other string entries (grep the file for `new Set(\[` and confirm the enclosing set has one element).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create `role-archive.ts` route module + tests</name>
  <files>src/backend/database/routes/role-archive.ts, src/backend/database/routes/role-archive.test.ts</files>
  <read_first>
    - src/backend/database/routes/identity-archive.ts (~176 lines — the reference module. Structure, threat model, error handling, LOCAL/REMOTE branch, try/finally conn cleanup are all mirrored. Do NOT deviate from this shape — the byte-for-byte substitution `key → name`, `IDENTITY_KEY_RE → ROLE_NAME_PATTERN`, `writeIdentityFile → writeRoleFile`, `/identities/... → /roles/...` is the intended clone.)
    - src/backend/database/routes/identity-archive.test.ts (~396 lines — the test module template. Uses supertest against a minimal Express app; mocks `writeIdentityFile`, `resolveHostById`, `connectOneShot`, `isLocalHostId`. Mirror the exact test structure with role concepts.)
    - src/backend/utils/role-name-pattern.ts (source of ROLE_NAME_PATTERN — direct import).
    - src/backend/ssh/host-resolver.ts (resolveHostById — same as identity-archive uses; do NOT redefine).
    - src/backend/ssh/ssh-one-shot.ts (connectOneShot + timeout — verify import path matches identity-archive.ts:48).
    - src/backend/utils/auth-manager.ts (AuthManager — same import path as identity-archive.ts:45).
    - src/backend/utils/logger.ts (databaseLogger — same import path as identity-archive.ts:46).
    - src/backend/claude-session/identity-artifact-reader.ts (isLocalHostId — same import path as identity-archive.ts:50-52; ROLE_NAME_PATTERN comes from role-name-pattern.ts NOT this file).
    - src/backend/claude-session/per-role-file.ts (from Task 1 — the writeRoleFile function this route calls).
    - .planning/phases/133-role-archival/133-RESEARCH.md § "Code Examples" #1 — direct code sketch of role-archive.ts (verbatim shape). Copy the structure; adapt the exact import paths from the reference sibling.
    - .planning/phases/133-role-archival/133-CONTEXT.md § D-01, D-05, D-16 (the endpoint's semantic contract).
  </read_first>
  <behavior>
    - Test 1 (happy LOCAL): with `isLocalHostId` mocked to return true and `writeRoleFile` mocked to resolve, POST `/roles/box-maintainer/archive` with body `{ hostId: <local> }` returns 200 `{ ok: true }`. Assert `writeRoleFile` was called with (`"box-maintainer"`, `".archive-requested"`, `""`, `{ hostId: <local>, conn: null }`). Assert `databaseLogger.info` was called with a string containing `role archive requested` + `userId=` + `hostId=` + `name=box-maintainer` (audit log per T-133-01-05 mirrored from T-115-03-05).
    - Test 2 (happy REMOTE): with `isLocalHostId=false`, `connectOneShot` resolving to a fake conn (with `.end()` spy), and `writeRoleFile` succeeding, POST returns 200. Assert the fake conn was passed to `writeRoleFile` and `.end()` was called on the conn afterward (in the finally block).
    - Test 3 (invalid role name via ROLE_NAME_PATTERN): POST `/roles/Bad_Name/archive` (underscore is NOT in `/^[a-z0-9-]+$/` — role names use hyphens only) returns 400 with error message containing `role name` or `[a-z0-9-]`. Assert `writeRoleFile` was NOT called. Test uppercase, underscore, and `../` traversal — all reject.
    - Test 4 (missing hostId): POST with body `{}` returns 400 (`hostId is required`). Assert no disk touch.
    - Test 5 (malformed hostId): POST with body `{ hostId: "not-a-number" }` and `{ hostId: -1 }` and `{ hostId: 3.14 }` — all return 400 (`hostId must be a positive integer`).
    - Test 6 (hostId not owned by caller): `resolveHostById` returns null; POST returns 404 (`Host not found`). Assert no disk touch. NOT 403 — no probe info leak per T-133-01-01 (mirrored from T-115-03-01).
    - Test 7 (host unreachable REMOTE): `connectOneShot` throws; POST returns 504 (`Host unreachable`). Assert no `writeRoleFile` call.
    - Test 8 (disk write failure): mocked `writeRoleFile` throws; POST returns 500 with `{ error: "failed to drop archive sentinel" }` — do NOT leak the underlying error text (T-133-01-03 info-disclosure mitigation). Assert `databaseLogger.error` was called with the underlying error text (server-side only). In the REMOTE branch, assert `conn.end()` is still called via the `finally` block.
    - Test 9 (unauthenticated): request without a JWT is rejected by `authenticateJWT` before reaching the handler (401 per the middleware). Assert `writeRoleFile` not called. This test uses the same middleware mock pattern identity-archive.test.ts uses — read it and mirror.
  </behavior>
  <action>
    Create `src/backend/database/routes/role-archive.ts` mirroring `identity-archive.ts` byte-for-byte in structure. Substitutions:
      - `IDENTITY_KEY_RE` → `ROLE_NAME_PATTERN`
      - `writeIdentityFile` → `writeRoleFile`
      - `/:key/archive` → `/:name/archive`
      - `key` variable → `name` variable
      - "identity key" error strings → "role name" error strings
      - Module docstring: adapt to Phase 133 Plan 133-01 with role-archive semantic contract per D-01/D-05/D-16 (per D-01 single sentinel drop; per D-05 sentinel filename `.archive-requested`; per D-16 archive location `~/fleet/roles-archive/<name>/` handled by the supervisor, not this endpoint — endpoint only drops the sentinel at `~/fleet/roles/<name>/.archive-requested`).

    (a) Imports: mirror identity-archive.ts:42-53 with role-appropriate paths:
      - `import type { AuthenticatedRequest } from "../../../types/index.js";`
      - `import express from "express";`
      - `import type { Request, Response } from "express";`
      - `import { AuthManager } from "../../utils/auth-manager.js";`
      - `import { databaseLogger } from "../../utils/logger.js";`
      - `import { resolveHostById } from "../../ssh/host-resolver.js";`
      - `import { connectOneShot } from "../../ssh/ssh-one-shot.js";`
      - `import { isLocalHostId } from "../../claude-session/identity-artifact-reader.js";`
      - `import { ROLE_NAME_PATTERN } from "../../utils/role-name-pattern.js";`
      - `import { writeRoleFile } from "../../claude-session/per-role-file.js";`

    (b) Route: `router.post("/:name/archive", authenticateJWT, async (req, res) => { ... })`.

    (c) Handler flow (mirror identity-archive.ts step-for-step):
      1. `const userId = (req as AuthenticatedRequest).userId;`
      2. Parse + validate hostId from body. Same three-branch check identity-archive uses (undefined/null/empty → 400 "hostId is required"; non-integer/non-positive → 400 "hostId must be a positive integer").
      3. Validate `name = String(req.params.name ?? "")` against `ROLE_NAME_PATTERN`. If it fails, return 400 `{ error: "role name must match [a-z0-9-]" }`.
      4. `const host = await resolveHostById(hostId, userId);` — if null, return 404 `{ error: "Host not found" }`.
      5. Branch on `isLocalHostId(hostId)`:
         - LOCAL: `conn = null`.
         - REMOTE: `try { conn = await connectOneShot(host, 3000); } catch { return 504 "Host unreachable"; }`.
      6. In a try/finally block:
         - `await writeRoleFile(name, ".archive-requested", "", { hostId, conn });`
         - `databaseLogger.info(\`role archive requested: userId=${userId}, hostId=${hostId}, name=${name}\`);`
         - `return res.json({ ok: true });`
         - On throw: log server-side via `databaseLogger.error`, return 500 `{ error: "failed to drop archive sentinel" }` (do NOT leak underlying error text to client).
         - finally: `if (conn) { try { conn.end(); } catch { /* ignore */ } }`.

    (d) Threat-model comments in the module docstring: cite T-133-01-01 through T-133-01-05 mirroring identity-archive.ts's T-115-03-01 through T-115-03-05 with role concepts substituted.

    (e) Add the same generic 500 fallback error handler at the bottom that identity-archive.ts:162-173 has (mirror verbatim).

    (f) Export: `export default router;`

    Create `src/backend/database/routes/role-archive.test.ts` mirroring `identity-archive.test.ts` structure verbatim:
      - Use `supertest` against a minimal Express app that mounts the router.
      - Mock `writeRoleFile`, `resolveHostById`, `connectOneShot`, `isLocalHostId`, and the authenticateJWT middleware using the same mocking strategy identity-archive.test.ts uses.
      - Cover all 9 behavior cases above.
      - Assert exact HTTP status codes and exact `writeRoleFile` argument shapes.
      - Test file naming: role names use `box-maintainer` (hyphenated, matches ROLE_NAME_PATTERN) as the primary fixture. Add invalid-name cases with underscore (`bad_name`), uppercase (`BadName`), and traversal (`../etc/passwd`) all in Test 3.
  </action>
  <verify>
    <automated>npx vitest run src/backend/database/routes/role-archive.test.ts</automated>
  </verify>
  <done>
    - `src/backend/database/routes/role-archive.ts` exists and exports a default Express router.
    - The router registers exactly one route: `POST /:name/archive`.
    - `npx vitest run src/backend/database/routes/role-archive.test.ts` reports all 9 tests green with zero skipped.
    - `grep -c 'writeRoleFile' src/backend/database/routes/role-archive.ts` returns >= 2 (import + call).
    - `grep -F '.archive-requested' src/backend/database/routes/role-archive.ts` returns >= 1 match (the sentinel filename passed to `writeRoleFile`).
    - `grep -F 'ROLE_NAME_PATTERN' src/backend/database/routes/role-archive.ts` returns >= 2 hits (import + `.test()` gate).
    - No un-archive / delete / DELETE verb registered — POST-only, one-way (grep-check: `grep -cE '(delete|DELETE|router\\.(get|delete|put|patch))' src/backend/database/routes/role-archive.ts | grep -v '^#'` returns 0).
  </done>
</task>

<task type="auto">
  <name>Task 3: Mount `role-archive.ts` in `database.ts` BEFORE the three existing `/roles` mounts</name>
  <files>src/backend/database/database.ts</files>
  <read_first>
    - src/backend/database/database.ts lines 2020-2065 (the route mounting section — locate the three `/roles` mounts at L2051 (rolesListForHostRoutes), L2056 (rolesCreateRoutes), L2061 (rolesRoutes), and the identity-archive precedent at L2033 with its comment).
    - .planning/phases/133-role-archival/133-RESEARCH.md § "Pattern 3: Multi-router mount on same base path (Express)" and "Pitfall 1: Route mount order — role-archive shadowed by generic roles handler" — spells out why the new mount MUST go BEFORE the three existing mounts.
  </read_first>
  <action>
    In `src/backend/database/database.ts`, near the existing three `/roles` mounts at ~L2051-2061:

    (a) Add import at the top of the file, alphabetically-sorted or grouped with sibling route imports (match the import style already used in the file — read the identity-archive import for the exact pattern): `import roleArchiveRoutes from "./routes/role-archive";` (adjust extension to match sibling — likely `.js` extension in the source for ESM compat, matching `import identityArchiveRoutes from "./routes/identity-archive";` if that exists — read the exact identity-archive import site and mirror it).

    (b) Add mount line IMMEDIATELY BEFORE the existing `app.use("/roles", rolesListForHostRoutes);` at L2051. Add a comment matching the identity-archive precedent at L2027-2033 that explains WHY the mount ordering matters (per Pitfall 1: mount before the three siblings so `POST /:name/archive` is never shadowed by a future generic `/:roleName` handler in `rolesRoutes` or elsewhere).

    (c) No other changes to `database.ts`. Do NOT interleave with unrelated mounts. Do NOT alter the three existing `/roles` mounts.

    Suggested diff (approximate — adjust to match the exact file state):
    ```
    // Phase 133 D-01: role archive route — mounts BEFORE the three /roles mounts
    // so POST /:name/archive is not shadowed by any generic /:roleName handler
    // in rolesRoutes (mirrors identity-archive mount discipline at ~L2033).
    app.use("/roles", roleArchiveRoutes);
    app.use("/roles", rolesListForHostRoutes);   // EXISTING
    app.use("/roles", rolesCreateRoutes);        // EXISTING
    app.use("/roles", rolesRoutes);              // EXISTING (has /:name/avatar)
    ```
  </action>
  <verify>
    <automated>grep -n 'roleArchiveRoutes\|role-archive\|app.use("/roles"' src/backend/database/database.ts</automated>
  </verify>
  <done>
    - `grep -c 'roleArchiveRoutes' src/backend/database/database.ts` returns exactly 2 (one import + one `app.use`).
    - The line number of the `app.use("/roles", roleArchiveRoutes)` line is LESS than every other `app.use("/roles", ...)` line in the file. Verification command: `grep -n 'app.use("/roles"' src/backend/database/database.ts` — first line in output MUST be the roleArchiveRoutes mount.
    - Pre-commit typecheck passes: run `npm run build:backend && npm run build` from the repo root. Both must exit 0. (Per orchestrator ownership note: backend TS crossover — the frontend tsc pass does NOT catch backend TS errors; run both.)
    - Manual smoke (optional, developer's discretion): with a running dev backend + a valid JWT + local hostId, `curl -X POST -H "Authorization: Bearer <jwt>" -H "Content-Type: application/json" -d '{"hostId":<local>}' http://localhost:<port>/roles/box-maintainer/archive` returns 200 `{"ok":true}` AND `~/fleet/roles/box-maintainer/.archive-requested` exists afterward.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → backend HTTP | JWT-authenticated user posts a role name + hostId. Untrusted input crosses into filesystem write and SSH-remote-write. |
| Backend Express handler → per-role disk write | Handler passes user-provided role name straight to `writeRoleFile`; the allowlist gate (ALLOWED_ROLE_REL_PATHS) + ROLE_NAME_PATTERN gate are the defenses. |
| Backend Express handler → SSH remote host | On REMOTE branch, `writeRoleFile` delegates to `writeMarkdownFileAtomic` (SFTP tmp+rename). ssh2 SFTP primitives never shell-interpolate untrusted strings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-01-01 | Elevation of Privilege | Unauthenticated caller drops `.archive-requested` on another user's role | mitigate | `authenticateJWT` middleware runs first; `resolveHostById(hostId, userId)` filters hosts to the caller's own. Test 6 + Test 9 in the behavior block cover both defenses. Response is 404 (not 403) on cross-user hostId — same no-probe-info-leak discipline as T-115-03-01. |
| T-133-01-02 | Tampering | `name` param containing shell metacharacters, `/`, `..`, or NUL | mitigate | `ROLE_NAME_PATTERN = /^[a-z0-9-]+$/` gate at the top of the handler (Task 2 step c.3). Any name that fails the regex is rejected with 400 before any disk or SSH activity. Primitive-layer `writeRoleFile` also re-validates via `assertValidRoleName` (belt-and-suspenders — Task 1 step e). Test 3 exercises uppercase, underscore, and `../` traversal. |
| T-133-01-03 | Information Disclosure | 500 error response leaking `writeRoleFile` error text (filesystem paths, ssh error strings, stack traces) | mitigate | Return generic `{ error: "failed to drop archive sentinel" }` on any thrown error — log the detail server-side via `databaseLogger.error`. Test 8 verifies the client response is generic and the server log contains the underlying error text. |
| T-133-01-04 | Denial of Service | Attacker floods the endpoint with archive requests, causing many `.archive-requested` sentinels on roles they own | accept | Rate limiting is cross-cutting concern owned elsewhere (Skynet Express layer's existing middleware). Each archive gesture is idempotent — dropping `.archive-requested` twice writes the same empty file. The supervisor's cascade consumes the sentinel within one reconcile tick regardless. |
| T-133-01-05 | Repudiation | User claims they didn't archive a role but the role is now archived | accept | JWT auth + server-side audit log (`databaseLogger.info("role archive requested: userId=X, hostId=Y, name=Z")` on success — Task 2 step c.6). Sufficient for solo-developer trust model. |
| T-133-01-06 | Tampering | Two concurrent archive requests for the same role race the `writeRoleFile` primitive | accept | Both writes produce the same result (empty file at the same path). The `tmp+rename` sequence in the LOCAL branch and `writeMarkdownFileAtomic` in the REMOTE branch are both atomic-swap primitives; there's no partial-file exposure window. The supervisor's sentinel consumption is single-threaded per tick — one cascade per role per reconcile pass regardless of how many concurrent frontend clicks. |
| T-133-01-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies added — this plan uses only existing modules (`express`, `writeMarkdownFileAtomic`, `resolveHostById`, `connectOneShot`, `AuthManager`, `databaseLogger`, `ROLE_NAME_PATTERN`). Per RESEARCH § "Package Legitimacy Audit": Phase 133 installs zero external packages. |
</threat_model>

<verification>
- `npx vitest run src/backend/claude-session/per-role-file.test.ts src/backend/database/routes/role-archive.test.ts` — all tests green.
- `npm run build:backend && npm run build` — both exit 0 (per orchestrator ownership note: backend TS crossover check).
- `grep -n 'app.use("/roles"' src/backend/database/database.ts` — the `roleArchiveRoutes` mount MUST be the first `/roles` mount line in the file.
- The three existing `/roles` mounts (rolesListForHostRoutes, rolesCreateRoutes, rolesRoutes) still exist unchanged.
- No new external packages added to `package.json` (grep before/after diff — must be empty).
- Manual smoke (optional): `curl` POST as documented in Task 3's `<done>` block returns 200 and creates the sentinel file.
</verification>

<success_criteria>
- New primitive `writeRoleFile` in `per-role-file.ts` mirrors the shape of `writeIdentityFile` in `per-identity-file.ts` (RESEARCH §5 precedent).
- New route `role-archive.ts` mirrors the shape of `identity-archive.ts` (RESEARCH §Code-Example-1 precedent).
- Route mounted BEFORE the three existing `/roles` routers.
- All 7 primitive tests + 9 route tests green.
- Sentinel filename passed to `writeRoleFile` is exactly `".archive-requested"` (per D-05).
- No new external packages installed.
- `<threat_model>` STRIDE register present with T-133-01-01 through T-133-01-06 + T-133-01-SC, each with disposition + mitigation citing test or code site.
</success_criteria>

<output>
Create `.planning/phases/133-role-archival/133-01-SUMMARY.md` when done, including:
- Final route path (`POST /roles/:name/archive`) and mount line number in `database.ts` (before/after ordering confirmed)
- Confirmation that `writeRoleFile` was added as a NEW module (not extension of `writeIdentityFile`) with RESEARCH §5 rationale
- Confirmation that `ALLOWED_ROLE_REL_PATHS` contains exactly one entry (`.archive-requested`)
- List of the 7 primitive tests + 9 route tests with pass/fail (all should be pass)
- Any deviations from the identity-archive shape and why
- Note whether the optional manual `curl` smoke was performed and its outcome
</output>
