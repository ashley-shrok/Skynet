---
phase: 113-skill-creation-in-the-edit-skills-modal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/api/skills-api.ts
  - src/ui/features/pretty-view/SkillFileTab.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "skills-api.ts exports a new `createSkill(hostId, name, description)` function that POSTs to `/skills-editor/skill` and returns `Promise<{ slug: string; mtime: number }>`"
    - "skills-api.ts exports a new `SkillAlreadyExistsError` class thrown when the endpoint returns 409 with error `\"skill exists\"`"
    - "SkillFileTab.tsx renders its Trash2 delete button ONLY when `filename !== 'SKILL.md'` — no delete affordance for the SKILL.md sentinel tab"
  artifacts:
    - path: "src/ui/api/skills-api.ts"
      provides: "createSkill function + SkillAlreadyExistsError class"
      exports: ["createSkill", "SkillAlreadyExistsError"]
    - path: "src/ui/features/pretty-view/SkillFileTab.tsx"
      provides: "SKILL.md-guarded delete affordance"
      contains: "filename !== \"SKILL.md\""
  key_links:
    - from: "src/ui/api/skills-api.ts createSkill"
      to: "POST /skills-editor/skill (backend endpoint from plan 113-01)"
      via: "authApi.post + 409 recognition (status === 409 && data.error === 'skill exists')"
      pattern: "authApi\\.post\\(\"/skills-editor/skill\""
    - from: "src/ui/api/skills-api.ts SkillAlreadyExistsError"
      to: "existing SkillFileAlreadyExistsError shape at skills-api.ts:57-62"
      via: "byte-shape mirror — extends Error, super() message, name property"
      pattern: "class SkillAlreadyExistsError extends Error"
    - from: "src/ui/features/pretty-view/SkillFileTab.tsx delete-button conditional"
      to: "D-10 frontend invariant layer + backend guard from plan 113-01 task 2"
      via: "hide-affordance-when-filename-is-SKILL.md; backend is authoritative"
      pattern: "filename !== \"SKILL.md\""
---

<objective>
Wire the frontend up to the new backend contract from plan 113-01 by adding a
`createSkill` API function + `SkillAlreadyExistsError` class to `skills-api.ts`
(per D-24), and enforce the frontend layer of the SKILL.md invariant by hiding
the delete affordance on the SKILL.md tab in `SkillFileTab.tsx` (per D-10
frontend layer).

Purpose: The modal restructure in plan 113-03 consumes both artifacts —
`handleNewSkill` calls `createSkill` and catches `SkillAlreadyExistsError`, and
the tab strip renders `SkillFileTab` for every file including `SKILL.md`. These
two touches are trivial in isolation but load-bearing for plan 03.

Output: Two new exports at the end of `skills-api.ts` (function + error class),
and a one-line conditional wrap around the Trash2 button in `SkillFileTab.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-skill-creation-in-the-edit-skills-modal/113-CONTEXT.md
@.planning/phases/113-skill-creation-in-the-edit-skills-modal/113-RESEARCH.md
@.planning/phases/113-skill-creation-in-the-edit-skills-modal/113-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add createSkill + SkillAlreadyExistsError to skills-api.ts</name>
  <files>src/ui/api/skills-api.ts</files>
  <read_first>
    - src/ui/api/skills-api.ts (entire file — need the imports at L1 that expose `authApi` + `handleApiError` from `@/main-axios`, the existing `SkillFileAlreadyExistsError` class at L52-62 as the byte-shape mirror, and the existing `createSkillFile` function at L163-197 as the shape for the new `createSkill` — same 409 recognition pattern, same `handleApiError` fallthrough)
    - .planning/phases/113-skill-creation-in-the-edit-skills-modal/113-PATTERNS.md (§ "src/ui/api/skills-api.ts — new createSkill function + SkillAlreadyExistsError class" for the exact snippet form)
    - .planning/phases/113-skill-creation-in-the-edit-skills-modal/113-CONTEXT.md (D-24 for the signature contract: `createSkill(hostId, name, description) → Promise<{ slug: string; mtime: number }>`)
    - .planning/phases/113-skill-creation-in-the-edit-skills-modal/113-01-PLAN.md (§ "output" contract — the backend returns `{ slug, mtime }` on 200 and `{ error: "skill exists" }` on 409; the 409 recognition must match `data.error === "skill exists"` exactly)
  </read_first>
  <behavior>
    - Function `createSkill(hostId: number, name: string, description: string): Promise<{ slug: string; mtime: number }>` exists and is exported.
    - On successful 200 response, `createSkill` returns the parsed `response.data` cast to `{ slug: string; mtime: number }`.
    - On 409 with `response.data.error === "skill exists"`, `createSkill` throws a `SkillAlreadyExistsError` (typed 409 recognition — do NOT recognize any other 409 shape).
    - On any other error shape (400, 404, 500, 502, network), `createSkill` calls `handleApiError(error, "create skill")` and rethrows so the return type stays `Promise<{ slug: string; mtime: number }>`.
    - `SkillAlreadyExistsError` is a subclass of `Error` with `name === "SkillAlreadyExistsError"` and `super()` message `"skill exists"` (matches the backend error string).
    - No modifications to any existing export — the file's existing surface is preserved byte-for-byte.
  </behavior>
  <action>
    Append two new exports at the END of `src/ui/api/skills-api.ts` (after the existing `deleteSkill` function at L226-238). Follow PATTERNS.md § "src/ui/api/skills-api.ts" verbatim.

    (A) Add `export class SkillAlreadyExistsError extends Error` — byte-shape mirror of `SkillFileAlreadyExistsError` at L52-62. Constructor calls `super("skill exists")` and sets `this.name = "SkillAlreadyExistsError"`. Add a JSDoc block above the class noting: "Typed 409 skill-exists error. Thrown by createSkill when the backend returns 409 with { error: 'skill exists' }. Byte-shape mirror of SkillFileAlreadyExistsError (Phase 44 SKILLED-05)."

    (B) Add `export async function createSkill(hostId: number, name: string, description: string): Promise<{ slug: string; mtime: number }>`. Body:
    - Try block: `const response = await authApi.post("/skills-editor/skill", { hostId, skill: name, description });` then `return response.data as { slug: string; mtime: number };`.
    - Catch block: `const err = error as { response?: { status?: number; data?: { error?: string } } };`. If `err?.response?.status === 409 && err.response.data?.error === "skill exists"` throw `new SkillAlreadyExistsError()`. Otherwise call `handleApiError(error, "create skill")` and `throw error;` (unreachable-but-satisfies-TS-return-type).
    - JSDoc above: "POST /skills-editor/skill. Creates a new skill folder plus a seed SKILL.md file with YAML frontmatter carrying the name (slug) and description. Backend composes and writes the seed server-side in the same call (D-08 — no round-trip through PUT /write). If the skill folder already exists, throws SkillAlreadyExistsError (409)."

    Note the request body field name is `skill` (not `name`) — the backend contract from plan 113-01 uses `{ hostId, skill, description }` where `skill` is the already-slugified string. The function parameter is named `name` for caller-side ergonomics but sent to the backend as `skill`.

    Do NOT add any new imports — `authApi` and `handleApiError` are already imported at L1. Do NOT modify any existing function, class, or type export.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-apollo && npx vitest related --run src/ui/api/skills-api.ts 2>&1 | tail -30 && npx tsc --noEmit 2>&1 | grep -E "skills-api|SkillAlreadyExistsError|createSkill" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export class SkillAlreadyExistsError extends Error" src/ui/api/skills-api.ts` returns exactly `1`.
    - `grep -c "export async function createSkill" src/ui/api/skills-api.ts` returns exactly `1`.
    - `grep -c 'this.name = "SkillAlreadyExistsError"' src/ui/api/skills-api.ts` returns exactly `1`.
    - `grep -c 'super("skill exists")' src/ui/api/skills-api.ts` returns exactly `1`.
    - `grep -c 'authApi.post("/skills-editor/skill"' src/ui/api/skills-api.ts` returns exactly `1`.
    - `grep -c 'hostId, skill: name, description' src/ui/api/skills-api.ts` returns exactly `1` — confirms the parameter-to-backend-field mapping is correct (`name` param → `skill` backend field).
    - `grep -c 'data\\?.error === "skill exists"' src/ui/api/skills-api.ts` returns exactly `1`.
    - `grep -c 'handleApiError(error, "create skill")' src/ui/api/skills-api.ts` returns exactly `1`.
    - The line number of `export class SkillAlreadyExistsError` is GREATER than the line number of the existing `export async function deleteSkill` (both new exports are at the END of the file).
    - `npx tsc --noEmit 2>&1 | grep -c "skills-api.ts"` returns `0` (no TS errors on the modified file).
    - Pre-existing exports (`listSkills`, `enumerateSkillFiles`, `readSkillFile`, `writeSkillFile`, `createSkillFile`, `deleteSkillFile`, `deleteSkill`, `SkillFileMtimeConflictError`, `SkillFileAlreadyExistsError`, `SkillEntry`, `SkillFileEntry`) are ALL still present — verify via `grep -c "^export" src/ui/api/skills-api.ts` shows the count increased by exactly 2 vs baseline.
  </acceptance_criteria>
  <done>
    `skills-api.ts` exports `createSkill` and `SkillAlreadyExistsError` at end of file; both mirror the existing `createSkillFile` / `SkillFileAlreadyExistsError` shapes; no existing exports modified; `tsc --noEmit` clean on this file; scoped vitest passes any pre-existing tests that import from `skills-api.ts` (frontend test extensions land in plan 04).
  </done>
</task>

<task type="auto">
  <name>Task 2: Guard SkillFileTab.tsx delete affordance behind filename !== "SKILL.md"</name>
  <files>src/ui/features/pretty-view/SkillFileTab.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/SkillFileTab.tsx (entire file — the delete affordance is at L150-158 (the `<button title="Delete this file"><Trash2 /></button>` block), the `filename` prop is already threaded through the component at L64-66 and used by `MarkdownEditor` at L142, so no prop-plumbing is needed. Confirm the exact classes on the Trash2 button before wrapping so the wrap preserves surrounding sibling layout — the button is inside a `<div className="flex justify-end gap-2 shrink-0 items-center">` container that also holds the Save button.)
    - .planning/phases/113-skill-creation-in-the-edit-skills-modal/113-PATTERNS.md (§ "src/ui/features/pretty-view/SkillFileTab.tsx" for the exact wrap form)
    - .planning/phases/113-skill-creation-in-the-edit-skills-modal/113-CONTEXT.md (D-10 for the two-layer enforcement rule — this is the frontend layer; backend guard from plan 113-01 task 2 is authoritative)
  </read_first>
  <behavior>
    - When `filename === "SKILL.md"`: the delete-file `<button>` (Trash2 icon) is NOT rendered at all — the DOM contains no `title="Delete this file"` element on the SKILL.md tab.
    - When `filename !== "SKILL.md"` (e.g., `"README.md"`, `"tests/basic.py"`, `"SKILL.md.bak"`): the delete-file `<button>` renders exactly as before — same classes, same onClick, same Trash2 icon size 16.
    - The Save button (sibling to the delete button in the same `<div className="flex justify-end gap-2 shrink-0 items-center">` container) is UNCHANGED — same classes, same onClick, same layout regardless of `filename`.
    - No prop changes, no state changes, no effect changes — this is a one-line conditional wrap.
  </behavior>
  <action>
    In `src/ui/features/pretty-view/SkillFileTab.tsx`, locate the delete `<button>` at L150-158 (the one carrying `title="Delete this file"` and rendering `<Trash2 size={16} />`). Wrap that button ONLY (not the surrounding `<div>`, not the Save button) in a `{filename !== "SKILL.md" && ( ... )}` conditional. The existing `<div className="flex justify-end gap-2 shrink-0 items-center">` container stays as-is and continues to hold the Save button unconditionally.

    Do NOT change:
    - The `filename` prop signature (already threaded at L64-66).
    - The `onRequestDelete` prop signature or its wiring (still called on click when the button renders).
    - The Save button below the delete button.
    - The button's className, title, onClick handler, or icon size when it does render.
    - Any imports.

    Post-wrap, the container's JSX should read (semantically):
    - `<div className="flex justify-end gap-2 shrink-0 items-center">`
    - `  {filename !== "SKILL.md" && (<button title="Delete this file" ... ><Trash2 size={16} /></button>)}`
    - `  <button /* existing Save button */ />`
    - `</div>`
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-apollo && npx vitest related --run src/ui/features/pretty-view/SkillFileTab.tsx 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'filename !== "SKILL.md"' src/ui/features/pretty-view/SkillFileTab.tsx` returns exactly `1`.
    - `grep -c 'title="Delete this file"' src/ui/features/pretty-view/SkillFileTab.tsx` returns exactly `1` (the button element still exists, just conditionally rendered).
    - `grep -c "<Trash2" src/ui/features/pretty-view/SkillFileTab.tsx` returns exactly `1` (icon reference preserved).
    - The line number of the `filename !== "SKILL.md"` conditional is LESS than the line number of the sole `<Trash2` element (guard wraps the button, not sits after it).
    - `grep -c "onRequestDelete" src/ui/features/pretty-view/SkillFileTab.tsx` shows the same count as before the edit (prop usage unchanged).
    - `npx tsc --noEmit 2>&1 | grep -c "SkillFileTab.tsx"` returns `0` (no TS errors).
    - Save button remains unconditional — inspect the same container `<div>` and confirm a second `<button>` (the Save button — check for its distinguishing attribute like `title="Save"` or its onClick reference) is NOT wrapped in any conditional.
  </acceptance_criteria>
  <done>
    `SkillFileTab.tsx` delete-file button is wrapped in `filename !== "SKILL.md"` conditional; Save button is untouched; no prop or handler changes. `tsc --noEmit` clean. The frontend test asserting "SKILL.md tab has NO delete affordance" (which lands in plan 113-04) will pass once plan 04 wires up the surrounding modal.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (React component) → Skynet backend (`POST /skills-editor/skill`) | `createSkill` sends `{ hostId, skill, description }` — validation is enforced backend-side (plan 113-01); the client trusts the backend's 409 recognition contract. |
| React DOM rendering (`SkillFileTab`) | The delete-file button visibility on the `SKILL.md` sentinel is a UX layer only — the backend guard from plan 113-01 task 2 is the load-bearing enforcement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-11 | Tampering | `createSkill`: response body misinterpretation | mitigate | Type cast `response.data as { slug: string; mtime: number }` is a compile-time contract only; the backend's fixed response shape from plan 113-01 (with tests asserting the exact shape) is the runtime guarantee. Any drift is caught by plan 113-04's frontend tests. |
| T-113-12 | Information Disclosure | `createSkill`: leaked error message | mitigate | Error messages surfaced to `window.alert` are recognized-error-typed (SkillAlreadyExistsError) OR generic `Error.message` — no backend stderr or path leakage (backend already sanitized in plan 113-01). |
| T-113-13 | Integrity | Frontend-only SKILL.md delete-guard could be bypassed via curl | accept (backend authoritative) | D-10 explicitly requires BOTH layers. The backend guard from plan 113-01 task 2 is the load-bearing floor; this task's `SkillFileTab` change is UX polish so users never see a button that would 400. Documented and intentional. |
| T-113-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced this phase. |

</threat_model>

<verification>
- `grep -c "export async function createSkill" src/ui/api/skills-api.ts` == `1`
- `grep -c "export class SkillAlreadyExistsError extends Error" src/ui/api/skills-api.ts` == `1`
- `grep -c 'filename !== "SKILL.md"' src/ui/features/pretty-view/SkillFileTab.tsx` == `1`
- `npx vitest related --run src/ui/api/skills-api.ts src/ui/features/pretty-view/SkillFileTab.tsx` — exit code 0
- `npx tsc --noEmit` — no errors on either touched file
</verification>

<success_criteria>
- `skills-api.ts` exports `createSkill(hostId, name, description)` returning `Promise<{ slug: string; mtime: number }>` and `SkillAlreadyExistsError` class per D-24.
- `createSkill` recognizes the exact backend 409 contract (`status === 409 && data.error === "skill exists"`) and throws the typed error; all other errors go through `handleApiError`.
- `SkillFileTab.tsx` hides its Trash2 delete button when `filename === "SKILL.md"` per D-10 frontend layer.
- No pre-existing behavior of either file is changed.
- Scoped vitest is green on both touched files.
</success_criteria>

<output>
Create `.planning/phases/113-skill-creation-in-the-edit-skills-modal/113-02-SUMMARY.md` when done. Include:
- New exports added to `skills-api.ts` with exact signatures (for plan 113-03 to import).
- `SkillFileTab.tsx` guard: what renders differently for SKILL.md vs other filenames.
- Files touched with line-count deltas.
</output>
