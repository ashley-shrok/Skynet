---
phase: 113-skill-creation-in-the-edit-skills-modal
plan: 04
type: execute
wave: 3
depends_on:
  - 113-01
  - 113-02
  - 113-03
files_modified:
  - src/ui/features/pretty-view/SkillsEditorModal.test.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "SkillsEditorModal.test.tsx has a passing test asserting `+ New skill` button is visible + enabled when a host is picked, hidden/disabled when no host is picked"
    - "SkillsEditorModal.test.tsx has a passing test for the chained-prompt happy path (name → description → `createSkill` called with slugified name and trimmed description → refetch → auto-select)"
    - "SkillsEditorModal.test.tsx has passing tests for cancellation on prompt 1 and prompt 2 (no createSkill call in either case)"
    - "SkillsEditorModal.test.tsx has a passing test for the empty-description re-prompt that verifies the name prompt fires exactly once while the description prompt fires twice, and the alert fires once"
    - "SkillsEditorModal.test.tsx has passing tests for `+ New file` tab position (last child of tab strip) and `+ New file` click NOT changing activeTab"
    - "SkillsEditorModal.test.tsx has passing tests for single-host hides picker and multi-host shows picker"
    - "SkillsEditorModal.test.tsx has a passing test for `SKILL.md` tab having NO delete affordance and non-`SKILL.md` tabs retaining delete affordance"
    - "The pre-existing delete-file confirm test is updated to target a non-`SKILL.md` filename so it does not conflict with the D-10 frontend guard"
  artifacts:
    - path: "src/ui/features/pretty-view/SkillsEditorModal.test.tsx"
      provides: "Extended frontend RTL coverage of every D-28 behavior + companion edit"
      contains: "createSkill"
  key_links:
    - from: "SkillsEditorModal.test.tsx new tests"
      to: "SkillsEditorModal.tsx post-plan-03 DOM structure"
      via: "RTL queries (getByRole, getByTitle, getByText, queryByRole)"
      pattern: "getByRole|queryByRole"
    - from: "SkillsEditorModal.test.tsx `createSkill` mock"
      to: "src/ui/api/skills-api.ts createSkill (from plan 113-02)"
      via: "vi.mock('@/api/skills-api') hoisted block extension"
      pattern: "createSkill: vi.fn"
---

<objective>
Extend `SkillsEditorModal.test.tsx` with frontend RTL coverage for every D-28
test case, plus the companion edit to the pre-existing delete-file confirm test
whose target `SKILL.md` is now guarded by plan 113-02's `SkillFileTab.tsx`
change. This is the frontend green-gate for the whole phase — the execution of
this plan proves the modal wiring from plan 113-03 behaves correctly under
click/prompt inputs.

Purpose: D-28 explicitly lists 10+ frontend test cases that must land. The
scoped-test discipline (D-29) means the executor's green gate is `npx vitest
related --run` on the modified test file — running this plan produces the
executable proof that the whole frontend touch is correct.

Output: Extended `vi.mock` hoisted block adding `createSkill` mock, ~10-12 new
`it` cases across `+ New skill` (visibility, chained-prompt happy path, cancels,
empty-description re-prompt), `+ New file` (position, no-activeTab-change),
single-host (hide/show picker), and `SKILL.md` (no delete affordance, non-SKILL
retains). Companion edit to the pre-existing delete-file test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-skill-creation-in-the-edit-skills-modal/113-CONTEXT.md
@.planning/phases/113-skill-creation-in-the-edit-skills-modal/113-RESEARCH.md
@.planning/phases/113-skill-creation-in-the-edit-skills-modal/113-PATTERNS.md
@.planning/phases/113-skill-creation-in-the-edit-skills-modal/113-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend vi.mock('@/api/skills-api') block with createSkill mock</name>
  <files>src/ui/features/pretty-view/SkillsEditorModal.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/SkillsEditorModal.test.tsx (lines 1-100 including the imports at L1-22 and the `vi.mock("@/api/skills-api", async (importOriginal) => { ... })` hoisted block at L23-51 that mocks 8 existing API functions; the `HOST_TREE` test fixture around L60-90 which supplies the single-host case for D-17 tests)
    - src/ui/api/skills-api.ts (post-plan-02 — confirm `createSkill` signature and `SkillAlreadyExistsError` class shape so the mock's resolved value type-checks)
    - .planning/phases/113-skill-creation-in-the-edit-skills-modal/113-PATTERNS.md (§ "Frontend test mock hoisting pattern" for the extend-vi.mock pattern)
  </read_first>
  <action>
    Extend the existing `vi.mock("@/api/skills-api", async (importOriginal) => { ... })` block at L23-51 in `src/ui/features/pretty-view/SkillsEditorModal.test.tsx` to add a `createSkill` mock. Preserve every existing mocked function (`listSkills`, `enumerateSkillFiles`, `readSkillFile`, `writeSkillFile`, `createSkillFile`, `deleteSkillFile`, `deleteSkill`) and the `...orig` spread that keeps error classes accessible.

    Add: `createSkill: vi.fn().mockResolvedValue({ slug: "new-skill", mtime: 1_700_000_200 }),` alongside the other function mocks.

    Do NOT restructure the block — insert the new line in the natural sort order (near `createSkillFile`). Do NOT touch the `...orig` spread — it already exposes `SkillAlreadyExistsError` because the class is a real named export from `skills-api.ts` post-plan-02.

    If the existing block uses a different hoisting style (e.g., `vi.mock(...)` without `importOriginal`), preserve that style and just add `createSkill` to whatever object the mock resolves to.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-apollo && grep -c "createSkill: vi.fn" src/ui/features/pretty-view/SkillsEditorModal.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "createSkill: vi.fn" src/ui/features/pretty-view/SkillsEditorModal.test.tsx` returns exactly `1`.
    - `grep -c "listSkills: vi.fn" src/ui/features/pretty-view/SkillsEditorModal.test.tsx` returns exactly `1` (existing mock preserved).
    - `grep -c "createSkillFile: vi.fn" src/ui/features/pretty-view/SkillsEditorModal.test.tsx` returns exactly `1` (existing mock preserved).
    - `grep -c "deleteSkill: vi.fn" src/ui/features/pretty-view/SkillsEditorModal.test.tsx` returns exactly `1` (existing mock preserved).
    - `grep -c "importOriginal" src/ui/features/pretty-view/SkillsEditorModal.test.tsx` returns the same count as before this task (harness structure preserved).
    - `npx tsc --noEmit 2>&1 | grep -c "SkillsEditorModal.test.tsx"` returns `0`.
  </acceptance_criteria>
  <done>
    The `vi.mock('@/api/skills-api')` block includes a `createSkill: vi.fn().mockResolvedValue(...)` entry; all pre-existing mocks are preserved; TS clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add `+ New skill` behavior tests (visibility, chained-prompt, cancels, empty-desc re-prompt)</name>
  <files>src/ui/features/pretty-view/SkillsEditorModal.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/SkillsEditorModal.test.tsx (post-Task-1 — the existing `+ Add file` chained-prompt test at L283-323 is the byte-shape mirror for the new `+ New skill` chained-prompt happy path; the RDP-filter test at L392-408 is the mirror for the visibility test; the HOST_TREE fixture around L60-90 supplies the single-host tree)
    - src/ui/features/pretty-view/SkillsEditorModal.tsx (post-plan-03 — the `+ New skill` button's `disabled={selectedHostId == null}` attribute; the `handleNewSkill` two-loop flow so the tests match its exact behavior)
    - .planning/phases/113-skill-creation-in-the-edit-skills-modal/113-PATTERNS.md (§ "src/ui/features/pretty-view/SkillsEditorModal.test.tsx" for the exact snippet-by-snippet form of every `+ New skill` test case)
    - .planning/phases/113-skill-creation-in-the-edit-skills-modal/113-CONTEXT.md (D-28 for the required test cases)
  </read_first>
  <behavior>
    - Test 1 (chained-prompt happy path): with `prompt` mocked to return "My Cool Skill" then "A cool skill.", clicking `+ New skill` calls `createSkill(1, "my-cool-skill", "A cool skill.")` — the first argument is the hostId, the second is the SLUGIFIED name (verifying `slugifyRoleName` is applied client-side), the third is the trimmed description.
    - Test 2 (visibility when host picked vs not): with `defaultHostId={1}` on a multi-host tree, `+ New skill` button exists and is not disabled; with `defaultHostId={null}` on a multi-host tree (host auto-select skipped), `+ New skill` button exists but IS disabled.
    - Test 3 (cancel on prompt 1): `prompt` returns `null` on first call; `createSkill` is never called; only one prompt fires.
    - Test 4 (cancel on prompt 2): `prompt` returns "My Skill" then `null`; `createSkill` is never called; exactly two prompts fire.
    - Test 5 (empty-description re-prompt): `prompt` returns "My Skill" then "   " then "Fine desc"; `createSkill` is called with `(1, "my-skill", "Fine desc")`; the name prompt fires exactly ONCE; the description prompt fires exactly TWICE; `window.alert` fires at least once with a message matching `/description is required/i`.
  </behavior>
  <action>
    Append the following `it` blocks inside the top-level `describe` in `src/ui/features/pretty-view/SkillsEditorModal.test.tsx` (after the existing `+ Add file` tests around L283-323 is a natural home). Follow PATTERNS.md § "src/ui/features/pretty-view/SkillsEditorModal.test.tsx" for the exact snippet form.

    (A) `it("+ New skill: chained prompt (name → description) calls createSkill and auto-selects", async () => { ... })` — spy `window.prompt` returning `"My Cool Skill"` then `"A cool skill."`; override `(skillsApi.createSkill as ReturnType<typeof vi.fn>).mockResolvedValueOnce({ slug: "my-cool-skill", mtime: 1_700_000_200 })`; override `listSkills` for the two calls (initial list without new skill; post-create list with new skill). Render the modal with `defaultHostId={1}`. Await `getByRole("button", { name: /\\+ new skill/i })`; click it. Assert `createSkill` was called with `(1, "my-cool-skill", "A cool skill.")`. Restore the prompt spy.

    (B) `it("+ New skill button is disabled when defaultHostId is null (host not auto-picked)", async () => { ... })` — render with `defaultHostId={null}` and a multi-host tree fixture. Assert `screen.getByRole("button", { name: /\\+ new skill/i }).disabled === true`.

    Alternate acceptable form of test (B): render with `defaultHostId={1}` and assert the button is NOT disabled. Both variations map to D-01's disabled-when-no-host contract; pick whichever pairs cleanly with the existing HOST_TREE fixture.

    (C) `it("+ New skill: cancel on name prompt → no createSkill call, no state change", async () => { ... })` — spy `window.prompt` with `.mockReturnValueOnce(null)`. Render with `defaultHostId={1}`. Await + click the button. Assert `promptSpy.mock.calls.length === 1` and `skillsApi.createSkill` was NOT called.

    (D) `it("+ New skill: cancel on description prompt → no createSkill call", async () => { ... })` — spy `window.prompt` with `.mockReturnValueOnce("My Skill").mockReturnValueOnce(null)`. Render and click. Assert `promptSpy.mock.calls.length === 2` and `skillsApi.createSkill` was NOT called.

    (E) `it("+ New skill: empty description re-prompts description ONLY; name is retained", async () => { ... })` — spy `window.alert` (silent implementation) and `window.prompt` with `.mockReturnValueOnce("My Skill").mockReturnValueOnce("   ").mockReturnValueOnce("Fine desc")`. Render and click. Assert `createSkill` was called with `(1, "my-skill", "Fine desc")`. Count prompt calls whose first arg matches `/skill name/i` (must be exactly 1) and whose first arg matches `/Description/` (must be exactly 2). Assert `alertSpy` was called at least once with a message matching `/description is required/i`. Restore both spies.

    Use `fireEvent.click`, `screen.getByRole`, `screen.getByText`, `waitFor` from `@testing-library/react` as already imported at L19-20 of the test file. If any new helper is needed (e.g., `within`), add it to the existing testing-library import.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-apollo && npx vitest related --run src/ui/features/pretty-view/SkillsEditorModal.test.tsx 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"+ New skill: chained prompt' src/ui/features/pretty-view/SkillsEditorModal.test.tsx` returns exactly `1`.
    - `grep -c 'createSkill.*toHaveBeenCalledWith(1, "my-cool-skill"' src/ui/features/pretty-view/SkillsEditorModal.test.tsx` returns at least `1`.
    - `grep -c '"+ New skill: cancel on name prompt' src/ui/features/pretty-view/SkillsEditorModal.test.tsx` returns exactly `1`.
    - `grep -c '"+ New skill: cancel on description prompt' src/ui/features/pretty-view/SkillsEditorModal.test.tsx` returns exactly `1`.
    - `grep -c '"+ New skill: empty description re-prompts description ONLY' src/ui/features/pretty-view/SkillsEditorModal.test.tsx` returns exactly `1`.
    - `grep -c '"+ New skill button is disabled\\|"+ New skill button is\\|new skill.*disabled' src/ui/features/pretty-view/SkillsEditorModal.test.tsx` returns at least `1` (visibility test present).
    - `grep -c 'my-skill' src/ui/features/pretty-view/SkillsEditorModal.test.tsx` returns at least `1` (empty-desc re-prompt test asserts the slugified name).
    - `npx vitest related --run src/ui/features/pretty-view/SkillsEditorModal.test.tsx --testNamePattern="\\+ New skill" 2>&1 | grep -E "Tests|passed|failed" | tail -5` shows the new tests passing (5 tests matching the pattern, exit code 0).
  </acceptance_criteria>
  <done>
    Five new `it` cases cover `+ New skill` visibility, chained-prompt happy path, both cancel branches, and the empty-description re-prompt with name retention. `npx vitest related --run` on the test file is green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add `+ New file` + single-host + `SKILL.md` no-delete tests + companion edit</name>
  <files>src/ui/features/pretty-view/SkillsEditorModal.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/SkillsEditorModal.test.tsx (post-Task-2 — the existing delete-file confirm test at L325-357 which currently targets `SKILL.md` and MUST be updated to target `tests/basic.py` or another non-SKILL.md filename; the RDP-filter test at L392-408 which is the byte-shape mirror for the single-host-hides-picker test; the existing `selectSkill` helper if present (grep for `selectSkill` — it may be a helper for clicking through the skill picker to a specific skill))
    - src/ui/features/pretty-view/SkillFileTab.tsx (post-plan-02 — confirm the delete button carries `title="Delete this file"` so `getByTitle` / `queryByTitle` selectors work in the new tests)
    - src/ui/features/pretty-view/SkillsEditorModal.tsx (post-plan-03 — confirm the `+ New file` `<button>`'s accessible name via its text content `New file`, its position as last-child of the tab strip, and that it never renders `data-state="active"` or `font-semibold`)
    - .planning/phases/113-skill-creation-in-the-edit-skills-modal/113-PATTERNS.md (§ "src/ui/features/pretty-view/SkillsEditorModal.test.tsx" — sections on `+ New file` tab position, `+ New file` no-activeTab-change, single-host tests, and the SKILL.md no-delete tests)
    - .planning/phases/113-skill-creation-in-the-edit-skills-modal/113-CONTEXT.md (D-13 through D-18 and D-28)
  </read_first>
  <behavior>
    - Test 1 (`+ New file` tab position): after selecting a skill with files, the last button whose text is `New file` OR matches a file path (`SKILL.md`, `tests/basic.py`) is the `New file` button — it is the LAST child of the tab strip regardless of file count.
    - Test 2 (`+ New file` no activeTab change): clicking `+ New file` fires `createSkillFile` (via `handleAddFile`) but does NOT change `activeTab` — the button itself has no `font-semibold` or active-state styling; the previously-active tab remains active.
    - Test 3 (single-host hides picker): with the existing HOST_TREE (single SSH host), the host `<combobox>` is NOT in the DOM (only the skill combobox remains).
    - Test 4 (multi-host shows picker): with a synthesized 2-host tree fixture, the host `<combobox>` IS in the DOM.
    - Test 5 (`SKILL.md` no delete affordance): after selecting a skill and confirming `SKILL.md` is the auto-selected first tab, `queryByTitle(/delete this file/i)` returns null.
    - Test 6 (non-`SKILL.md` retains delete affordance): after switching to a non-`SKILL.md` tab (e.g., `tests/basic.py`), `queryByTitle(/delete this file/i)` returns the button element.
    - Companion edit: the pre-existing delete-file confirm test at L325-357 (which currently targets `SKILL.md`) is updated to first click a non-`SKILL.md` tab (e.g., `tests/basic.py`) and then trigger the delete flow — assertions updated to `deleteSkillFile(1, "build", "tests/basic.py")`.
  </behavior>
  <action>
    Extend `src/ui/features/pretty-view/SkillsEditorModal.test.tsx` with six new `it` cases and one companion edit. Follow PATTERNS.md § "src/ui/features/pretty-view/SkillsEditorModal.test.tsx" for each snippet.

    (A) `it("+ New file tab is the LAST child of the tab strip", ...)` — render with `defaultHostId={1}`; if a `selectSkill(...)` helper exists in the file, use it to select `"build"`; otherwise reproduce its behavior (click the skill combobox, select the option). Await a textbox to appear (indicates a file tab loaded). Query all buttons whose text matches `/new file|skill\\.md|tests\\/basic\\.py/i` — assert the last one's `textContent` matches `/new file/i`.

    (B) `it("+ New file tab click does NOT change activeTab (existing file tab stays selected)", ...)` — spy `window.prompt` returning `"new-file.md"`. Render + select `"build"`. Await the textbox. Query `screen.getByRole("button", { name: /new file/i })` and click. `waitFor` on `skillsApi.createSkillFile` having been called (proves `handleAddFile` was invoked). Verify the button's `className` does NOT include `font-semibold` (asserting the button was never selected). Restore the prompt spy.

    (C) `it("single-host: host picker <select> is hidden entirely", ...)` — the existing HOST_TREE fixture has exactly one SSH host (verify via `flatHosts.length === 1`). Render with `defaultHostId={1}`. Query `screen.queryAllByRole("combobox")` — assert length is exactly 1 (only the skill picker). `screen.queryByRole("combobox", { name: /host/i })` returns null.

    (D) `it("multi-host (2+ SSH hosts): host picker <select> is visible", ...)` — synthesize a multi-host fixture: `const multi: HostFolder = { name: "root", children: [HOST_TREE.children[0], { ...HOST_TREE.children[0], id: "2", name: "second-host" }] };` (adjust type import and shape to match the existing HOST_TREE type — if HOST_TREE is not exported/named `HostFolder`, use the type it actually uses). Render with `hostTree={multi}` and `defaultHostId={null}`. Assert `screen.queryByRole("combobox", { name: /host/i })` is truthy.

    (E) `it("SKILL.md tab has NO delete (Trash2) affordance", ...)` — render + select `"build"`. Await textbox (indicates SKILL.md file is loaded; it auto-selects as the first tab because the mocked `enumerateSkillFiles` returns `[{ path: "SKILL.md" }, { path: "tests/basic.py" }]` and SKILL.md sorts first alphabetically). Assert `screen.queryByTitle(/delete this file/i)` is `null`.

    (F) `it("Non-SKILL.md tab retains delete affordance", ...)` — render + select `"build"`. Await textbox. Click the tab button whose text matches `/tests\\/basic\\.py/i` to switch to the non-SKILL.md tab. `waitFor` on `screen.queryByTitle(/delete this file/i)` becoming truthy.

    (G) **Companion edit** — locate the pre-existing delete-file confirm test at L325-357 (the one that currently clicks the delete button and asserts `deleteSkillFile` was called with `(1, "build", "SKILL.md")`). Modify it as follows:
    - Before triggering the delete flow, first click the `tests/basic.py` tab button (or whatever non-SKILL.md tab the mocked `enumerateSkillFiles` provides). Await the textbox re-render.
    - Update the assertion to `expect(skillsApi.deleteSkillFile).toHaveBeenCalledWith(1, "build", "tests/basic.py")`.
    - Preserve every other line of the test — only the tab-switch prelude and the final assertion's third argument change.

    Preserve every other pre-existing test in the file — only the delete-file test needs the companion edit.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-apollo && npx vitest related --run src/ui/features/pretty-view/SkillsEditorModal.test.tsx 2>&1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"+ New file tab is the LAST child' src/ui/features/pretty-view/SkillsEditorModal.test.tsx` returns exactly `1`.
    - `grep -c '"+ New file tab click does NOT change activeTab' src/ui/features/pretty-view/SkillsEditorModal.test.tsx` returns exactly `1`.
    - `grep -c '"single-host: host picker' src/ui/features/pretty-view/SkillsEditorModal.test.tsx` returns exactly `1`.
    - `grep -c '"multi-host' src/ui/features/pretty-view/SkillsEditorModal.test.tsx` returns exactly `1`.
    - `grep -c '"SKILL.md tab has NO delete' src/ui/features/pretty-view/SkillsEditorModal.test.tsx` returns exactly `1`.
    - `grep -c '"Non-SKILL.md tab retains delete affordance"' src/ui/features/pretty-view/SkillsEditorModal.test.tsx` returns exactly `1`.
    - `grep -c 'deleteSkillFile.*toHaveBeenCalledWith(1, "build", "SKILL.md")' src/ui/features/pretty-view/SkillsEditorModal.test.tsx` returns `0` (companion edit removed the SKILL.md target).
    - `grep -c 'deleteSkillFile.*toHaveBeenCalledWith(1, "build", "tests/basic.py")' src/ui/features/pretty-view/SkillsEditorModal.test.tsx` returns at least `1` (companion edit updated to non-SKILL.md target).
    - `npx vitest related --run src/ui/features/pretty-view/SkillsEditorModal.test.tsx` completes with exit code 0.
    - Post-edit test file's `it(...)` count is greater than the pre-edit count by exactly `11` (5 from Task 2 + 6 from Task 3). Verify via `grep -c "  it(" src/ui/features/pretty-view/SkillsEditorModal.test.tsx` before and after tasks 2+3 in the executor SUMMARY.
  </acceptance_criteria>
  <done>
    Six new `it` cases cover `+ New file` position + no-activeTab-change, single-host hide + multi-host show, and SKILL.md no-delete + non-SKILL.md retain-delete. Companion edit updates the pre-existing delete-file test to target `tests/basic.py`. `npx vitest related --run src/ui/features/pretty-view/SkillsEditorModal.test.tsx` is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runtime → mocked API layer | `vi.mock` swaps the real `@/api/skills-api` for stubs; the tests never touch the backend. |
| Test runtime → mocked `window.prompt` / `window.alert` | Spies must be restored after each test to avoid cross-test contamination. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-18 | Repudiation | Spy leaks between tests | mitigate | Every `vi.spyOn(window, ...)` call is followed by `.mockRestore()` inside the same test's teardown path (or via `afterEach` if the file already has one). |
| T-113-19 | Tampering | Test mock resolves stale after plan 02 API surface change | mitigate | `...orig` spread on the `vi.mock` block ensures newly-added named exports (`SkillAlreadyExistsError`) resolve to the real class; `createSkill` mock has an explicit `.mockResolvedValue(...)` shape. |
| T-113-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced this phase. |

</threat_model>

<verification>
- `grep -c 'createSkill: vi.fn' src/ui/features/pretty-view/SkillsEditorModal.test.tsx` == `1`
- `grep -c '"+ New skill:' src/ui/features/pretty-view/SkillsEditorModal.test.tsx` >= `4`
- `grep -c '"+ New file tab' src/ui/features/pretty-view/SkillsEditorModal.test.tsx` >= `2`
- `grep -c '"single-host: host picker' src/ui/features/pretty-view/SkillsEditorModal.test.tsx` == `1`
- `grep -c '"SKILL.md tab has NO delete' src/ui/features/pretty-view/SkillsEditorModal.test.tsx` == `1`
- `grep -c '"build", "SKILL.md"' src/ui/features/pretty-view/SkillsEditorModal.test.tsx` == `0` (companion edit landed)
- `npx vitest related --run src/ui/features/pretty-view/SkillsEditorModal.test.tsx` — exit code 0
- `npx tsc --noEmit` — no errors on the modified test file
</verification>

<success_criteria>
- All 10-11 new `it` cases from D-28 pass under `npx vitest related --run`.
- Companion edit removes the SKILL.md target from the pre-existing delete-file test so the D-10 frontend guard from plan 113-02 does not break it.
- Frontend suite is green — the executor's D-29 green gate is met for the phase.
- `tsc --noEmit` clean on the test file.
</success_criteria>

<output>
Create `.planning/phases/113-skill-creation-in-the-edit-skills-modal/113-04-SUMMARY.md` when done. Include:
- Number of new `it` cases added (target: 11).
- Companion edit summary (before/after state of the delete-file test).
- `it(...)` count before + after so plan 113's phase-close can report a delta.
- Any pre-existing tests that broke due to plan 113-03's DOM restructure and how they were fixed (or noted as needing follow-up).
- Files touched with line-count deltas.
</output>
