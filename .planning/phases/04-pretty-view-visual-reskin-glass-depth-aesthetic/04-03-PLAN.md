---
phase: 04-pretty-view-visual-reskin-glass-depth-aesthetic
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified: []
autonomous: true
requirements:
  - VISUAL-01
  - VISUAL-02
  - VISUAL-03
  - VISUAL-04
  - VISUAL-05
  - VISUAL-06
  - VISUAL-07
  - VISUAL-08
  - VISUAL-09
  - VISUAL-10
must_haves:
  truths:
    - "`docker compose build skynet` on the EC2's `/opt/skynet` (via the fork's `skynet-patches/build-skynet.sh` mechanism) produces a clean `skynet-patched:local` image from the current branch state — verified via a REBUILD command that exits 0 and produces the expected image tag in `docker image ls`."
    - "`npm run build` inside the container-build step OR run standalone via `cd ~/skynet && npm run build` completes with no TypeScript errors, no unresolved imports, no missing tokens (Tailwind v4 warns loudly when arbitrary-value classes reference an undefined CSS custom property — a green build means every `var(--color-pv-*)` / `var(--pv-id-hue)` reference resolves)."
    - "Zero source file diffs on this plan — Plan 04-03 is a verification-and-preparation-only wave. It runs the build, records observations, and prepares the AGENTS.md documentation writeup content that will be pasted in at deploy time. It does NOT commit anything."
    - "Nyquist UAT checklist is generated as `04-UAT-CHECKLIST.md` in the phase directory covering all VISUAL-01 through VISUAL-10 requirements, each with a specific in-app verification step Ashley can walk through post-deploy. The checklist is written from Ashley's perspective (visual/functional UAT, not code inspection)."
    - "AGENTS.md documentation writeup content is drafted as `04-AGENTS-MD-ENTRY.md` in the phase directory — a ready-to-paste block for patch #NN (the executor determines the current-highest patch number from AGENTS.md; will most likely be #69 since patches #61-#68 are already documented). The block follows the existing AGENTS.md fork-patch entry style: numbered patch header with feat/fix commit prefix; body describing scope, mechanism, design rationale, iteration lessons, verify-post-deploy invariants, rebase risk; the WHOLE-BATCH deploy note for Phase 4 (whether Phase 4 batches with Phase 3 patches #61/#62/#63 or deploys standalone depends on Phase 3's deploy status — draft covers both scenarios with a clear pick-one instruction). The doc block is NOT written to AGENTS.md yet — that happens at deploy PIN time per the fork's standing 'AGENTS.md write-up at PIN, not deferred' rule from tina.md."
    - "No deploy is performed. Ashley's separate green-light triggers the mandatory 15-min deadman + `docker compose up -d --force-recreate skynet` cycle per AGENTS.md, not this plan."
    - "Frontend build artifacts (`html/assets/*`) exist under `dist/` or wherever Vite emits them; grep verifies that at least one output CSS/JS file references `--pv-id-hue` and at least one references `--color-pv-base` — proof that the Phase 4 tokens made it through Vite + Tailwind v4 tree-shaking."
  artifacts:
    - path: ".planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-UAT-CHECKLIST.md"
      provides: "Nyquist UAT checklist covering VISUAL-01 through VISUAL-10 as walk-through steps Ashley can run post-deploy"
      contains: "VISUAL-01"
    - path: ".planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-AGENTS-MD-ENTRY.md"
      provides: "Ready-to-paste AGENTS.md entry for the phase 4 patch — inserted at deploy PIN time"
      contains: "feat(pretty-view)"
  key_links:
    - from: "Plan 04-01 + 04-02 committed source"
      to: "Verified-clean local build via npm run build + optional docker compose build"
      via: "verify-build task"
      pattern: "built in"
    - from: "REQUIREMENTS.md VISUAL-01..10"
      to: "UAT walk-through steps in 04-UAT-CHECKLIST.md"
      via: "Task 2 generates the checklist by mapping each requirement to a specific in-app verify step"
      pattern: "VISUAL-"
    - from: "Fork's AGENTS.md patch-entry convention"
      to: "Draft entry for the Phase 4 reskin patch"
      via: "Task 3 generates 04-AGENTS-MD-ENTRY.md as a paste-ready block"
      pattern: "AGENTS.md"
---

<objective>
Verify the Phase 4 reskin builds cleanly, generate the Nyquist UAT checklist Ashley walks through post-deploy, and draft the AGENTS.md patch-entry content for the eventual PIN commit. Zero source-code changes. Three tasks:

1. **Build verification** — `npm run build` succeeds; grep confirms Phase 4 tokens made it through the Vite + Tailwind v4 tree-shaking pipeline; optional `docker compose build skynet` if the executor can run it (may require sudo — if unavailable, `npm run build` is sufficient because the docker build is exactly `npm run build` inside a container).

2. **Nyquist UAT checklist generation** — write `04-UAT-CHECKLIST.md` in the phase directory covering VISUAL-01 through VISUAL-10 as concrete walk-through steps Ashley runs post-deploy. Each step names the in-app action + expected visual outcome + failure indicator.

3. **AGENTS.md entry draft** — write `04-AGENTS-MD-ENTRY.md` in the phase directory as a ready-to-paste block matching the fork's existing patch-entry style (see AGENTS.md patches #43 through #68 for the format). Includes: patch number (executor determines from `grep -c '^   [0-9]' /home/ubuntu/AGENTS.md` — likely #69), commit prefix, body describing scope + mechanism + design rationale + iteration lessons + verify-post-deploy invariants + rebase risk, and a whole-batch deploy note.

Purpose: Complete the Phase 4 execution wave through committed-and-verified state, without deploying. Ashley's separate green-light plus the mandatory 15-min deadman + `docker compose up -d --force-recreate skynet` cycle happens as a distinct action per fleet standing rule (patch #35 tina.md lesson: BLANKET PRE-AUTHORIZATION ≠ PER-DEPLOY GREEN LIGHT).

Output: Two new files in the phase directory (UAT checklist + AGENTS.md draft); no source-code diffs; confirmed-green build.

**Requirement mapping this plan closes:**
All ten VISUAL-01 through VISUAL-10 requirements are already technically closed by Plans 04-01 + 04-02; this plan verifies they hold end-to-end and prepares the UAT + documentation surface Ashley uses to confirm at deploy time.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-CONTEXT.md
@.planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-01-SUMMARY.md
@.planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-02-SUMMARY.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify clean build of the Phase 4 reskin</name>
  <read_first>
    - Plan 04-01-SUMMARY.md + Plan 04-02-SUMMARY.md — the executor's prior notes may flag adjustments made during implementation that Task 1 should re-verify.
  </read_first>
  <files></files>
  <action>
Run the frontend build and verify Phase 4 tokens survived the pipeline.

**Step 1 — Clean build:**

```bash
cd ~/skynet
npm run build 2>&1 | tail -30
```

Expected: `built in Xs`, no errors. If the build fails on TypeScript errors, missing imports, or Tailwind v4 unresolved-arbitrary-value warnings, STOP HERE and revert to Plan 04-01 or 04-02 execution to fix the specific issue named in the build output.

**Step 2 — Grep for Phase 4 tokens in the output bundles:**

Vite writes output to `dist/` (or `html/assets/` inside the container build — depends on the compose Dockerfile). Locate the CSS + JS bundles:

```bash
find ~/skynet/dist -type f \( -name '*.css' -o -name '*.js' \) 2>/dev/null | head -20
```

If `dist/` doesn't exist post-build, check `~/skynet/html/` or `~/skynet/public/` — the fork's build.sh may output elsewhere. Once bundles are located, verify Phase 4 tokens survived:

```bash
grep -l '\-\-pv-id-hue' ~/skynet/dist/**/*.{css,js} 2>/dev/null | head -5
grep -l '\-\-color-pv-base' ~/skynet/dist/**/*.{css,js} 2>/dev/null | head -5
grep -l 'pv-identity-breathe' ~/skynet/dist/**/*.{css,js} 2>/dev/null | head -5
```

Each grep should return at least one file. If any grep returns nothing, that token isn't being emitted — investigate: usually means Tailwind v4's JIT didn't see the class because it's constructed via string concatenation or is otherwise unreachable. Ideally: the token appears in the CSS bundle for `--color-pv-*` + `--pv-id-hue` (they're used as CSS variables) and the keyframes name appears in the CSS bundle too.

**Step 3 — TypeScript-only pass (belt and braces):**

```bash
cd ~/skynet
npx tsc --noEmit 2>&1 | tail -20
```

Expected: no output (or only warnings from node_modules/*.d.ts that aren't from our source). If any error mentions a file under `src/ui/features/pretty-view/` or `src/ui/features/terminal/IdentityBadge.tsx` or `src/ui/index.css`, investigate.

**Step 4 (OPTIONAL, if sudo is granted) — Docker image build:**

```bash
sudo bash /opt/skynet/skynet-patches/build-skynet.sh 2>&1 | tail -40
```

Expected: successful build producing `skynet-patched:local` image; verify via:
```bash
sudo docker image ls skynet-patched:local
```

If sudo isn't granted or the docker build fails for infra reasons (e.g. registry pull errors unrelated to our source), skip this step — `npm run build` verification is authoritative for the source-level cleanness of the phase.

**Step 5 — Terminal.tsx untouched sanity check:**

```bash
cd ~/skynet
git diff --stat src/ui/features/terminal/Terminal.tsx
```

Expected: empty output. Terminal.tsx is the highest rebase-risk file on the fork (patches #1/#3/#6/#13/#17/#24/#26/#28/#33/#39/#40/#41/#44/#50/#51/#52/#60); Phase 4 must leave it entirely untouched.

**Step 6 — Backend / nginx / docker configs untouched sanity check:**

```bash
cd ~/skynet
git diff --stat src/backend/ docker/ package.json package-lock.json
```

Expected: empty output. Phase 4 is a CSS-only frontend reskin — no backend, no nginx, no dependency changes.

**Record observations in the summary:**
- What `git diff --stat` shows for the whole repo (only the 12 files enumerated across Plans 04-01 + 04-02 should appear).
- Any Vite build output warnings that surfaced (even non-fatal warnings — e.g. "chunk larger than 500kB" is expected but noteworthy).
- Whether the docker build ran + what the image size delta was (if run).
  </action>
  <verify>
    <automated>cd ~/skynet &amp;&amp; npm run build 2>&amp;1 | tail -5 | grep -q 'built in' &amp;&amp; test -z "$(git diff --stat src/ui/features/terminal/Terminal.tsx)" &amp;&amp; test -z "$(git diff --stat src/backend/ docker/ package.json package-lock.json)"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds with `built in Xs` in the output.
    - `git diff --stat src/ui/features/terminal/Terminal.tsx` returns empty (Terminal.tsx is UNTOUCHED).
    - `git diff --stat src/backend/ docker/ package.json package-lock.json` returns empty (no backend / nginx / docker / dependency changes).
    - `git diff --stat` over the entire repo shows only the 12 files enumerated in the Plan 04-01 + Plan 04-02 files_modified lists (with IdentityBadge.tsx counted once).
    - `npx tsc --noEmit` returns no errors from files under `src/ui/features/pretty-view/`, `src/ui/features/terminal/IdentityBadge.tsx`, or `src/ui/index.css`.
    - At least one output bundle file references `--pv-id-hue` and one references `--color-pv-base` (via grep on the emitted CSS/JS).
  </acceptance_criteria>
  <done>The Phase 4 reskin builds cleanly. All Phase 4 tokens survived Vite + Tailwind v4 tree-shaking. Terminal.tsx and all backend/nginx/docker files are untouched. Ready for Task 2 to generate the UAT checklist Ashley walks through at deploy time.</done>
</task>

<task type="auto">
  <name>Task 2: Generate Nyquist UAT checklist as 04-UAT-CHECKLIST.md</name>
  <read_first>
    - .planning/REQUIREMENTS.md — the VISUAL-01 through VISUAL-10 requirement texts are the source of truth for what each UAT step verifies.
    - Plan 04-CONTEXT.md § 6 (Decisions — UAT criteria) — Ashley's UAT probes at design time.
    - AGENTS.md — for context on how Ashley's fleet interacts with pretty view (which identities have avatars, which panes are the natural UAT candidates, e.g. tina pane for the amber color check).
  </read_first>
  <files>.planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-UAT-CHECKLIST.md</files>
  <action>
Write a Nyquist UAT checklist Ashley can walk through post-deploy to confirm the Phase 4 reskin works end-to-end. Every VISUAL-01 through VISUAL-10 requirement gets a concrete walk-through step naming: (a) the in-app action to take, (b) the expected visual outcome, (c) the failure indicator (what she'd see if the requirement is broken).

Also include prerequisite state ("open Ctrl+Shift+O on a tina pane", "have a bella pane available with colorHue set", etc.) so Ashley isn't hunting for setup mid-check.

Write to `.planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-UAT-CHECKLIST.md`. Format:

```markdown
# Phase 4 — Pretty View Visual Reskin — UAT Checklist

**Prerequisites (do these once before starting the checklist):**

- Confirm the fresh build is running: `docker exec skynet cat /app/dist/backend/backend/starter.js | head -3` (any nonempty output means the container is running the current patch).
- Have at least two identity-tagged tmux panes available in Skynet — ideally one with `colorHue` set (tina at hue 35 = amber, if Ashley's set it; else pick whichever identity has a colorHue) and one without.
- Have at least one non-identity pane (e.g. a fresh `ssh` session with no matched identity).
- Sanity-check terminal panes are visually unchanged by opening a terminal-only tab (any host, tmux mode). All identity badges + panel chrome + terminal styling should look identical to pre-deploy. **This is the VISUAL-10 canary — if terminal chrome looks different, STOP and revert.**

**Checklist (walk through in order; check each box):**

### VISUAL-10 — Terminal chrome untouched (safety canary)

- [ ] Open any terminal tab (tmux mode, not pretty). IdentityBadge (if identity-matched) appears in top-right at the same size/position/style as before deploy. Terminal content styling, tab bar, sidebar, AppRail all unchanged.
- [ ] Open RDP tab (e.g. thenasty-RDP). Guacamole canvas + IdentityBadge (if applicable) styled identically to pre-deploy.
- [ ] Open a VNC or file-manager tab. Chrome unchanged.

**Failure indicator:** any visual difference in terminal / RDP / VNC / dashboard / sidebar / tab bar / AppRail. If yes, revert immediately per AGENTS.md deploy runbook.

### VISUAL-01 — Atmospheric warm-neutral base

- [ ] Flip to pretty view on any pane (Ctrl+Shift+O). The background reads as a warm off-black atmosphere, not flat black, not cool navy-black. Subtle radial-gradient overlays imply an ambient light source from the top-left corner.
- [ ] Scroll through the message list — the background gradient stays consistent (no banding, no visible seams).

**Failure indicator:** flat black/dark background, cool blue-black tint, or visible gradient banding.

### VISUAL-02 — Bubbles as raised physical objects

- [ ] Both assistant and user bubbles have soft rounded corners (`~14px`), a visible drop shadow underneath, a thin bright rim highlight along the top edge, and backdrop-blur behind (visible when a bubble overlaps the atmospheric gradient).
- [ ] Bubbles look like they float above the surface, not painted flat on it.

**Failure indicator:** bubbles look flat, no shadow, no rim highlight, or no visible backdrop-blur when overlapping the atmospheric background.

### VISUAL-03 — Identity-hue color chain end-to-end

- [ ] Open pretty view on a pane matched to an identity WITH `colorHue` set (e.g. tina → amber). User bubble accents (bg gradient, border, outer glow), context-bar fill, send-button gradient, focused-textarea border and glow, and (in BackgroundedAgentsPanel if agents are running) the subagentType tag pill all read the same identity color.
- [ ] Open pretty view on a pane matched to a different-colorHue identity (e.g. bella if her colorHue is set to a distinct value like 320 for pink). The color chain shifts to that hue. **This is the key cross-pane test.**
- [ ] Open pretty view on a pane whose identity has NO colorHue set. Color chain falls back to a neutral warm amber (hue 35) — no visual break, no black rings, no missing accents.
- [ ] Open pretty view on a pane with NO matching identity (e.g. fresh ssh session). Color chain still uses the neutral warm amber fallback; the IdentityBadge simply doesn't render (matches terminal-pane semantic).

**Failure indicator:** identity-hue elements stay a fixed color regardless of pane, OR unmatched-identity panes render a broken/missing color chain.

### VISUAL-04 — Bigger identity badge with breathing

- [ ] Pretty view identity badge is ~56px avatar with name + title stacked to the RIGHT of the avatar (not below). Uses the identity's colorHue for the rim + outer glow. Subtle breathing brightness animation over ~5s.
- [ ] Hover the badge — it fades to transparent within ~150ms (patch #38 hover-fade preserved). Move cursor away — it fades back.
- [ ] Enable "prefer reduced motion" in the OS/browser accessibility settings — the badge stops breathing (animation disabled) but stays visible with everything else intact.
- [ ] Flip back to tmux mode. The terminal-pane IdentityBadge is UNCHANGED — smaller, avatar+name+title stacked vertically at 120px width. This is the size="md" default preserving patch #17/#38 behavior.

**Failure indicator:** badge is too small in pretty view, name/title stacked below the avatar (wrong direction), or the breathing runs when reduced-motion is on, or the terminal-pane badge changed size.

### VISUAL-05 — Ambient panels shelf reads as one quiet floating card treatment

- [ ] Run a `TaskCreate` in the pane's Claude session so at least one harness task appears. Also spawn a backgrounded Agent (Explore or general-purpose with run_in_background:true) and a backgrounded Bash (run_in_background:true) so all three panels can be visible together.
- [ ] The three panels (Tasks, Agents, Shells) stack ABOVE ComposeBox, each with subtle translucent warm-tint bg + backdrop-blur + soft drop shadow + rounded corners. They read as a "shelf" of three related cards, distinct from the messages above but not shouting for attention.
- [ ] The BackgroundedAgentsPanel's subagentType tag pill (e.g. "Explore") is the identity color of the pane.

**Failure indicator:** panels look like flat gray boxes with a top border (pre-deploy styling remnant), or the tag pill is gray instead of identity-hue.

### VISUAL-06 — Compose surround intentionally low-prominence

- [ ] The compose area does NOT have a card treatment. No bright top rim, no drop shadow around the whole area, no visible border separating it from the panels above.
- [ ] The only visual cue distinguishing the compose from the messages above is a very subtle inset shadow shading the bottom strip.
- [ ] Compose reads as "quiet — go there when you're ready." Does NOT compete with chat for attention.

**Failure indicator:** compose looks like a raised card with hard edges, bright top rim, or a bordered panel treatment.

### VISUAL-07 — Textarea lightest-touch outline + identity-hue focus ring

- [ ] Textarea's default state: barely-there warm-white outline (1px, ~9% opacity), soft warm-dark background, muted placeholder text. Findable as a receptacle for typing but not visually loud.
- [ ] Click into the textarea. Focus ring lights up: identity-hue border + soft outer glow + inset shadow depth increases. Transition ~200ms.
- [ ] Click away. Focus ring fades back to the barely-there outline.

**Failure indicator:** textarea has a bright bordered box treatment when unfocused (looks like a hole punched in the compose), OR the focus ring uses a fixed blue/gray instead of the identity hue.

### VISUAL-08 — Send button saturated identity-hue glow as ONE grab-point

- [ ] Send button (paper-airplane icon at the bottom of the icon column): saturated identity-hue gradient background, warm rim highlight, outer glow. Reads as the ONE intentional attention-grab-point in the compose area. Deep dark text/icon color for contrast against the saturated bg.
- [ ] Hover: gradient brightens, outer glow intensifies.
- [ ] Disabled state (empty text OR canSend===false): faded to ~40% opacity, cursor changes to not-allowed.
- [ ] Reset (RotateCcw) + Go-Ahead (ThumbsUp) buttons ABOVE the send button take the QUIETER treatment: subtle warm-dark gradient, faint white rim, no outer glow at rest. Hover reveals a subtle hue-tinted rim and outer glow.

**Failure indicator:** send button doesn't glow at all (looks like the reset/go-ahead buttons), OR the reset/go-ahead buttons are as saturated as send (breaks the ONE-grab-point semantic).

### VISUAL-09 — All existing behavior preserved

- [ ] Chat rendering works: markdown, inline code, preformatted blocks, bullet lists, tables all render (patch #47 + #48 preserved).
- [ ] Type into ComposeBox and hit Enter: message sends via the split-send WS input events; the sent message appears in the chat when the session file confirms (COMPOSE-04 HARD LOCK).
- [ ] Shift-Enter in ComposeBox inserts a newline.
- [ ] Reset button sends `/id reset (<body>)` when text present, or bare `/id reset` when empty (patches #52a + #58).
- [ ] Go-ahead button sends "go ahead" without touching the textarea's current content (patches #50 + #52a).
- [ ] Context-window fill bar animates when contextPct changes; ≥80% branch is RED, not identity-hue (semantic HARD LOCK).
- [ ] HarnessTasksPanel populates when Claude Code has tasks; disappears when empty.
- [ ] BackgroundedAgentsPanel populates when subagents run; disappears when done (patch #61/#66).
- [ ] BackgroundedShellsPanel populates when BG Bash runs; disappears on task-notification completion (patch #68).
- [ ] WipBubble appears when Claude is working (patch #51 PTY-idle signal); disappears when quiet.
- [ ] PlanPendingBubble appears when ExitPlanMode is pending (patch #63/#67); disappears on tool_result close.
- [ ] SessionHoldingBanner (if Phase 3 deployed) appears at the top of the scroll region during `/id reset` recycle; dismisses on `session_changed`. WebSocket does NOT drop.
- [ ] ComposeBox draft persistence: type into textarea, refresh the page. Draft returns (patches #57 + #49 + #55).
- [ ] Ctrl+Shift+O flips between pretty and tmux; Ctrl+Shift+; toggles message-queue drawer; Ctrl+Shift+L closes tab; all keyboard chords work.
- [ ] Message-queue drawer coexists with pretty view — opens below, doesn't disrupt pretty view's layout.

**Failure indicator:** any of the above behaviors is broken. If any is, revert per AGENTS.md deploy runbook.

## Sign-off

- [ ] All ten VISUAL-01 through VISUAL-10 requirements verified as PASSING via the above checks.
- [ ] Terminal / RDP / dashboard / sidebar / AppRail / tab bar / file manager chrome verified as UNCHANGED.
- [ ] All existing pretty-view functionality verified as PRESERVED end-to-end.
- [ ] Ashley signs off: "Phase 4 UAT green — pin the deploy."

If any item fails, the standard 15-min deadman timer + `/opt/skynet/.tmp-revert.sh` runbook handles rollback. Otherwise pin with:
```bash
sudo touch /tmp/skynet-keep-patched && sudo pkill -f 'sleep 900; \[ ! -f /tmp/skynet-keep-patched'
```
(narrow pkill pattern per AGENTS.md — do NOT use `pkill -f "sleep 900"` which would kill the guacd-zombie sentinel too).
```

**Verify by grepping for the 10 VISUAL requirement IDs in the generated file:**

```bash
for i in 01 02 03 04 05 06 07 08 09 10; do
  grep -q "VISUAL-$i" /home/ubuntu/skynet/.planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-UAT-CHECKLIST.md && echo "VISUAL-$i present" || echo "MISSING VISUAL-$i"
done
```

Expected: all 10 present.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-UAT-CHECKLIST.md &amp;&amp; for i in 01 02 03 04 05 06 07 08 09 10; do grep -q "VISUAL-$i" .planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-UAT-CHECKLIST.md || exit 1; done &amp;&amp; grep -q 'Sign-off' .planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-UAT-CHECKLIST.md &amp;&amp; grep -q 'Prerequisites' .planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-UAT-CHECKLIST.md &amp;&amp; grep -q 'Failure indicator' .planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-UAT-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-UAT-CHECKLIST.md` exists.
    - All ten VISUAL-01 through VISUAL-10 requirements are represented as walk-through sections with concrete in-app steps.
    - Every section has an expected outcome AND a failure indicator.
    - Prerequisites section names required setup state (identity panes with/without colorHue).
    - Sign-off section names the pin-deploy command (`sudo touch /tmp/skynet-keep-patched && sudo pkill ...` with the narrow pattern per AGENTS.md).
    - VISUAL-10 (terminal untouched) is called out FIRST as the safety canary.
    - VISUAL-09 (behavior preservation) enumerates ALL existing pretty-view features that must still work (chat, ComposeBox all 3 send paths, all 3 panels, WipBubble, PlanPendingBubble, SessionHoldingBanner from Phase 3, draft persistence, keyboard chords, message-queue drawer coexistence).
  </acceptance_criteria>
  <done>Ashley now has a concrete walk-through checklist to verify Phase 4 at deploy time. Every VISUAL requirement has a specific verification step; every failure has a named indicator.</done>
</task>

<task type="auto">
  <name>Task 3: Draft the AGENTS.md patch entry as 04-AGENTS-MD-ENTRY.md</name>
  <read_first>
    - /home/ubuntu/AGENTS.md — read the last three patch entries (#66, #67, #68) as style templates. Note: entries are numbered `NN. ` (2-digit) with a `feat(...):` or `fix(...):` prefix; body describes scope + mechanism + design rationale + iteration lessons + verify-post-deploy invariants + rebase risk in a specific structural pattern that Ashley expects.
    - Plan 04-CONTEXT.md — for the design spec references + iteration lessons that shaped the final mock.
    - Plans 04-01-SUMMARY.md + 04-02-SUMMARY.md — for empirical adjustments made during implementation that belong in the patch entry.
  </read_first>
  <files>.planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-AGENTS-MD-ENTRY.md</files>
  <action>
Write a ready-to-paste AGENTS.md entry for the Phase 4 patch. This file is a DRAFT — it is NOT written to AGENTS.md during Plan 04-03 execution. It gets pasted into AGENTS.md at deploy PIN time per the fork's standing rule (tina.md: "AGENTS.md write-up at PIN, not deferred").

**Step 1 — Determine the current-highest patch number:**

```bash
grep -E '^   [0-9]+\. `feat|^   [0-9]+\. `fix' /home/ubuntu/AGENTS.md | tail -3
```

The next patch number is one higher than the last. As of 2026-07-18, #68 is the last — so Phase 4 will most likely be #69. Confirm via the grep above; use whatever the actual next number is.

**Step 2 — Determine deploy-batch scenario:**

Check whether Phase 3's queued patches (#61/#62/#63 or whatever the current-pending list is per `/home/ubuntu/.claude/identities/tina/bounties/pending-patch-batch-post-60/`) are still undeployed:

```bash
ls /home/ubuntu/.claude/identities/tina/bounties/pending-patch-batch-post-60/ 2>/dev/null | head -5
```

If the bounty directory exists AND has active files, Phase 4 batches with Phase 3 (single deploy for #61/#62/#63 + all Phase 4 changes). If Phase 3 has already been deployed (bounty archived), Phase 4 deploys standalone.

**Step 3 — Write the draft entry to `.planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-AGENTS-MD-ENTRY.md`:**

```markdown
# AGENTS.md Draft Entry — Phase 4 (Patch #NN, executor determines NN)

**Insertion point in AGENTS.md**: After the last existing patch entry (currently #68 as of 2026-07-18). Follow the same indentation and formatting as the surrounding entries.

**Deploy-batch scenario** (executor picks one at PIN time based on Step 2 of Task 3):
- Scenario A: Phase 3 patches (#61/#62/#63) still queued → batch with them; write the deploy note as "shipped as part of the pending-batch-post-60 combined deploy 2026-07-XX."
- Scenario B: Phase 3 already deployed → standalone deploy; write "shipped standalone 2026-07-XX."

---

**Draft body (paste into AGENTS.md, adjust NN + date + scenario as needed):**

   NN. `feat(pretty-view): Glass depth reskin — atmospheric base + physical
       bubbles + per-pane identity-hue color chain + bigger IdentityBadge` —
       three commits shipped as one Phase 4 execution wave (04-01 tokens +
       IdentityBadge size variant + PrettyView root hue plumbing; 04-02
       bubbles + panels + compose reskin; no source diffs on 04-03 —
       build-verify + UAT prep + this doc entry drafted). CSS-only across
       12 files under `src/ui/features/pretty-view/`, plus
       `src/ui/features/terminal/IdentityBadge.tsx` (add-a-prop with
       default preserving patch #17/#38 behavior byte-identically), plus
       `src/ui/index.css` (Phase 4 tokens + breathing keyframes). Terminal
       / RDP / VNC / file manager / dashboard / sidebar / tab bar /
       AppRail chrome COMPLETELY unchanged.
       * **Aesthetic goal**: pretty view was flat-brutalist to match the
         rest of Skynet; Ashley wanted a distinct visual identity — a
         "warm dark Glass depth aesthetic with real physical
         dimensionality" — that reads as a themed island for the surface
         where she coordinates parallel Claude Code sessions. Design
         iterated live in-turn over 8 rounds of feedback (bounty
         `pretty-view-visual-overhaul/` reference-mock at
         `~/.claude/identities/tina/bounties/pretty-view-visual-overhaul/mock/index.html`,
         Glass tab final state).
       * **Iteration dead-ends** (each was a real recovery moment,
         preserved in `.planning/phases/04-.../04-CONTEXT.md` § design_spec
         iteration lessons):
         - Round 3: made compose a raised card with bright top rim +
           drop shadow. Ashley: "compose shouldn't draw as much attention
           as it does because it's never something I want to draw
           attention." Recovery: compose lost its card treatment
           entirely — VISUAL-06 HARD LOCK.
         - Round 4: bright brightness contrast + hard top-edge
           separators between message area / panels / compose. Ashley:
           "as much as it does improve the issue of them blending
           together, I like it less so." Recovery: switched to
           shadow-based separation, same visual language as bubbles
           above (subtle floating cards), not stepped chrome.
         - Round 5-7: textarea iteration. Near-black hole → too bright
           → halfway between; final `rgba(255,255,255,0.03)` bg with
           1px warm-white 9% outline as the ONLY affordance making it
           findable, focus ring identity-hue glow — VISUAL-07 HARD LOCK.
       * **Token layer** (`src/ui/index.css` additive to `@theme inline
         {}`): `--color-pv-base` warm off-black gradient triple,
         `--color-pv-surface-quiet` ambient-panel tint,
         `--color-pv-fg` warm off-white text, `--color-pv-code-fg` warm
         coral for inline code, `--radius-pv-bubble` 14px,
         `--radius-pv-card` 10px, multi-layer shadow tokens
         `--shadow-pv-raised-md` (bubbles) + `--shadow-pv-quiet-card`
         (panels) + `--shadow-pv-root`. Plus `@keyframes pv-identity-
         breathe` (5s brightness cycle) with `prefers-reduced-motion:
         reduce` override that disables it for accessibility. All Phase
         4 values are theme-independent (hardcoded from the mock, not
         `oklch()` derivations from theme vars) — pretty view is a
         themed island (VISUAL-10) that keeps its Glass aesthetic
         regardless of which Skynet theme is active.
       * **Identity-hue plumbing**: PrettyView reads the pane's
         identity via the existing `useSessionIdentity` hook
         (patch #30 lifted from Terminal.tsx), gets `identityHue`
         (0-360 from patch #17's identities.colorHue field), sets it
         as a CSS custom property `--pv-id-hue` on the root div's
         inline style with a fallback of hue 35 (warm amber, matches
         mock's Tina). Downstream components read via Tailwind
         arbitrary-value classes like `hsla(var(--pv-id-hue), 75%, 52%,
         0.18)`. Fallback for unmatched panes: hue 35 across the whole
         chain (user bubble accents, context-bar fill, send-button
         glow, textarea focus ring, subagentType tag pill in
         BackgroundedAgentsPanel). NO hash-based fallback like patch
         #26's terminal-pane tint — pretty view is deliberately more
         restrained: identity or neutral, no third path.
       * **IdentityBadge size variant** (`size?: "md" | "lg"`): the
         `md` default preserves patch #17 (120px pill + 80px avatar +
         name-below + title-below) AND patch #38 (`transition-opacity
         duration-150 hover:opacity-0`) byte-identically for the
         terminal-pane call site in `Terminal.tsx:2888` (which passes
         NO size prop so defaults to `md`). The `lg` treatment used
         only in pretty view: 56px avatar on LEFT, name+title stacked
         to the RIGHT, backdrop-filter blur, warm-dark glass base
         gradient, identity-hue rim + outer glow, subtle 5s breathing
         brightness (respects prefers-reduced-motion via
         `.pv-identity-breathe` class marker + the `@media` rule).
         Patch #38 hover-fade PRESERVED in both size branches — the
         terminal-content-showing-through affordance is load-bearing
         even in pretty view (in case Claude's chat output ever crowds
         the top-right corner).
       * **Bubble treatment** (ChatMessage assistant + user + WipBubble
         + PlanPendingBubble): rounded 14px + `backdrop-blur-xl
         saturate-150` + `-webkit-backdrop-filter` fallback + 3-layer
         shadow stack (ambient drop + inset rim highlight + hairline
         outer stroke) + translucent gradient bg. Assistant is
         mid-blue-gray; user is identity-hue-tinted with an outer
         hue-tinted glow — VISUAL-02 + VISUAL-03 delivered in one
         cascade.
       * **Ambient panels shelf** (HarnessTasksPanel +
         BackgroundedAgentsPanel + BackgroundedShellsPanel): each panel
         wraps in the SAME quiet-card treatment (translucent warm-tint
         gradient bg + backdrop-blur-md + `--shadow-pv-quiet-card`
         subtle drop + rounded card + subtle border). Reads as a
         shelf of three related cards distinct from the messages
         above but visually calm — VISUAL-05. Motion channel
         preserved: STILL NO SPINNERS in any of the three panels
         (patch #53 discipline extended to Phase 4). BackgroundedAgentsPanel
         subagentType tag pill gets identity-hue accents per mock.
       * **Compose surround intentionally quiet** (VISUAL-06 HARD
         LOCK from Round-3 recovery): no card, no bright top rim,
         no visible separator from the panels above. ONE cue:
         subtle inset shadow shading the bottom strip (`inset 0 2px
         10px rgba(0,0,0,0.35)`). Compose goes to it when you're
         ready, not because it demands attention.
       * **Textarea lightest-touch** (VISUAL-07 HARD LOCK from Round-7
         pivot): 1px warm-white 9% opacity outline, subtle inset
         shadow, warm-mid-dark bg (`rgba(255,255,255,0.03)`),
         placeholder text warm-dim. Focus reveals identity-hue border
         + focus ring (thin ring + soft outer glow), transition ~200ms
         on box-shadow + border-color.
       * **Send button ONE grab-point** (VISUAL-08 HARD LOCK):
         saturated identity-hue gradient bg + warm-cream rim + outer
         glow, deep-dark text for contrast. Reset (RotateCcw) and
         Go-Ahead (ThumbsUp) buttons above it get the QUIETER
         treatment: subtle warm-dark gradient + faint rim, hover
         reveals hue-tinted rim + glow. Preserves the "compose has
         ONE intentional attention-grab-point" attention hierarchy.
       * **Context bar identity-hue** (with red-at-80 semantic HARD
         LOCK): <50 = identity-hue full-strength gradient + glow;
         50-79 = same hue but reduced glow alpha; ≥80 = RED
         (`hsla(0,75%,50%,X)`) — approaching-full stays alarming
         regardless of pane identity because "context is about to
         run out" is a semantic that overrides per-pane color signal.
         Track becomes `bg-black/55` with inset shadow.
       * **SessionHoldingBanner** (Phase 3, if deployed): adopts the
         ambient-panel treatment (translucent warm bg + backdrop-blur
         + soft shadow), NOT the raised-bubble treatment — it's chrome,
         not content. The Plan 03-02 sticky-positioning mechanism
         (`sticky top-0 z-10 -mx-4 -mt-3 mb-3 px-4 py-2`) preserved
         byte-identically alongside the FRAGILITY WARNING code comment.
         PrettyView's outer div INTENTIONALLY does NOT gain
         `backdrop-filter` / `transform` / `will-change` / `filter` /
         `perspective` — any of those would establish a new containing
         block and break the sticky banner's positioning per the Plan
         03-02 warning. Root atmospheric depth is `background` +
         `background-image` (both safe for sticky-descendant
         positioning).
       * **Fleet asks that fed into this reskin**: Ashley's
         many-parallel-Claude workflow benefits from per-pane color
         identification at a glance (patch #26 tint on terminal panes,
         patch #17 identity badges, patch #30 shared row helpers).
         Phase 4 extends the "which agent is this?" grounding into
         pretty view via the full color chain + bigger badge.
       * **Verify post-deploy invariants** (for future rebase smoke
         checks):
         - `docker exec skynet ls /app/html/assets/` includes at least
           one CSS bundle referencing `--pv-id-hue` and one referencing
           `--color-pv-base` (Vite + Tailwind v4 tree-shake didn't drop
           them).
         - `docker exec skynet grep -l 'pv-identity-breathe' /app/html/assets/*.css`
           should return ≥1.
         - Live smoke: flip to pretty view on any identity-matched pane
           → bigger badge appears in top-right with breathing + hue rim;
           chat bubbles read as raised objects with backdrop-blur; user
           bubble carries pane's identity hue; compose is quiet chrome;
           textarea focus ring is identity-hue.
         - Terminal panes (tmux mode) visually UNCHANGED — the safety
           canary.
       * **Deploy note**: shipped [Scenario A OR B — pick one]:
         - Scenario A: part of the combined pending-batch-post-60
           deploy alongside Phase 3 patches #61 through #NN, YYYY-MM-DD.
         - Scenario B: shipped standalone via mandatory 15-min deadman +
           `docker compose up -d --force-recreate skynet`, pinned after
           Ashley's UAT green-light on `.planning/phases/04-.../04-UAT-CHECKLIST.md`, YYYY-MM-DD.
       * **Rebase risk**: LOW-to-MEDIUM.
         - `src/ui/features/pretty-view/*` — all fork-only files (created
           in patches #43/#44/#45/#47/#48/#50/#51/#52/#57/#61/#63/#68).
           No upstream conflict surface exists.
         - `src/ui/features/terminal/IdentityBadge.tsx` — patch #17 +
           #38 territory. Phase 4 is the first patch after #38 to touch
           it (add-a-prop with default). If a future rebase drops the
           `size` prop, terminal panes still render correctly because
           `size` defaults to `"md"` (byte-identical to patch #17/#38
           behavior). Pretty view would degrade to no-badge-in-pretty
           until IdentityBadge is patched back — but this is a graceful
           degradation, not a broken deploy.
         - `src/ui/index.css` — patches #47/#48 added `@plugin
           @tailwindcss/typography` and `@import @fontsource-variable/
           inter` at the top. Phase 4 adds tokens INSIDE the existing
           `@theme inline {}` block (additive, no reordering) + keyframes
           + reduced-motion rule at the bottom (in the neighborhood of
           the existing `@keyframes blink` + `.session-tint`). Upstream
           rarely touches this file so conflict risk is LOW.
         - `Terminal.tsx` — the highest-rebase-risk file on the fork
           (patches #1/#3/#6/#13/#17/#24/#26/#28/#33/#39/#40/#41/#44/
           #50/#51/#52/#60) — was DELIBERATELY LEFT UNTOUCHED. IdentityBadge
           default `size="md"` guaranteed the terminal-pane call site
           `<IdentityBadge identityKey={identityKey} />` renders
           byte-identically.
         - **Design mock preserved** at
           `~/.claude/identities/tina/bounties/pretty-view-visual-overhaul/mock/index.html`
           for reference in future revisions. If a future Phase-5+ wants
           to iterate the aesthetic, the mock's 4 tabs (Glass / Chip /
           Print / [4th if added]) are ready as design-conversation
           starters.
```

**Save the file. Do NOT paste into AGENTS.md yet.**
  </action>
  <verify>
    <automated>test -f .planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-AGENTS-MD-ENTRY.md &amp;&amp; grep -q 'feat(pretty-view)' .planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-AGENTS-MD-ENTRY.md &amp;&amp; grep -q 'IdentityBadge size variant' .planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-AGENTS-MD-ENTRY.md &amp;&amp; grep -q 'VISUAL-06' .planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-AGENTS-MD-ENTRY.md &amp;&amp; grep -q 'VISUAL-07' .planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-AGENTS-MD-ENTRY.md &amp;&amp; grep -q 'VISUAL-08' .planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-AGENTS-MD-ENTRY.md &amp;&amp; grep -q 'FRAGILITY WARNING' .planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-AGENTS-MD-ENTRY.md &amp;&amp; grep -q 'Rebase risk' .planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-AGENTS-MD-ENTRY.md &amp;&amp; grep -q 'Scenario' .planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-AGENTS-MD-ENTRY.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-AGENTS-MD-ENTRY.md` exists.
    - Draft follows the fork's AGENTS.md patch-entry format (patch number, feat/fix prefix, structured body with mechanism, design rationale, iteration lessons, verify invariants, rebase risk, deploy note).
    - Every major reskin surface is covered: token layer, identity-hue plumbing, IdentityBadge size variant, bubble treatment, ambient panels shelf, compose surround, textarea, send button, context bar, SessionHoldingBanner.
    - Iteration lessons from Round 3 / Round 4 / Round 5-7 captured (VISUAL-06 / VISUAL-07 HARD LOCKS explained).
    - FRAGILITY WARNING acknowledgment present (why PrettyView root does NOT gain backdrop-filter / transform / etc.).
    - Rebase risk section names Terminal.tsx as UNTOUCHED and explains the graceful-degradation posture of the IdentityBadge default prop.
    - Deploy scenario A vs B guidance present.
    - Verify-post-deploy invariants section names `docker exec` commands + live smoke tests.
    - File is a DRAFT — not written to AGENTS.md. Writeup at PIN time is the standard workflow.
  </acceptance_criteria>
  <done>The Phase 4 documentation is drafted and ready to paste into AGENTS.md at deploy PIN time. Ashley walks the UAT checklist, greenlights, and then the draft entry gets pasted in as patch #NN. Phase 4 is complete on the source side; deploy is the standing separate green-light.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds cleanly (Task 1).
- Terminal.tsx UNTOUCHED throughout the whole phase (`git diff --stat src/ui/features/terminal/Terminal.tsx` empty; Task 1 verifies).
- Backend / nginx / docker configs UNTOUCHED (Task 1 verifies).
- Only the 12 files enumerated in Plans 04-01 + 04-02 files_modified lists appear in `git diff --stat` for the whole repo (Task 1 confirms).
- `04-UAT-CHECKLIST.md` exists with all 10 VISUAL requirements as walk-through sections (Task 2).
- `04-AGENTS-MD-ENTRY.md` exists as a ready-to-paste draft (Task 3).
- No source-code diffs from Plan 04-03 (it's verification + doc-prep only).
- No commits from Plan 04-03 either — the phase's atomic feat(pretty-view) commit spans Plans 04-01 + 04-02 (executor commits at end of Plan 04-02); Plan 04-03 verifies and preps docs; the AGENTS.md doc entry is applied as a separate docs commit at deploy PIN time.
</verification>

<success_criteria>
- All ten VISUAL-01 through VISUAL-10 requirements are technically closed by Plans 04-01 + 04-02, verified as passing by the build + Task 2 UAT prep.
- Phase 4 is ready for Ashley's separate deploy green-light: source is on `feat/tab-title-from-tmux`, build is clean, UAT checklist exists for post-deploy walk-through, AGENTS.md entry is drafted for PIN-time paste.
- Terminal / RDP / VNC / file manager / dashboard / sidebar / tab bar / AppRail chrome COMPLETELY unchanged (VISUAL-10).
- Every existing pretty-view behavior preserved end-to-end (VISUAL-09).
- The FRAGILITY WARNING from Plan 03-02 (SessionHoldingBanner sticky positioning) preserved throughout the reskin.
- The mandatory 15-min deadman + `docker compose up -d --force-recreate skynet` deploy cycle runs at deploy time (not part of any Plan 04 execution wave).
</success_criteria>

<output>
Create `.planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-03-SUMMARY.md` when done. Note in the summary:
- `npm run build` result (exit 0, output tail).
- `git diff --stat` result for the whole repo (should show exactly the 12 files touched in Plans 04-01 + 04-02).
- Whether Terminal.tsx / backend / nginx / docker are UNTOUCHED (via targeted `git diff --stat` invocations).
- Whether the Vite output bundles contain the Phase 4 tokens (via grep on emitted CSS/JS).
- Whether docker compose build ran (only if sudo granted; otherwise skip).
- The current highest patch number found in AGENTS.md at time of Task 3 (so the phase 4 patch number is unambiguous at PIN time).
- The deploy scenario (A vs B) as detected in Task 3 (Phase 3 batch vs standalone).
- Confirmation that `04-UAT-CHECKLIST.md` and `04-AGENTS-MD-ENTRY.md` are created and non-empty.
- Reminder: NEXT STEP is Ashley's separate deploy green-light. Do NOT paste `04-AGENTS-MD-ENTRY.md` content into AGENTS.md yet — that happens at PIN time.
</output>
</content>
</invoke>