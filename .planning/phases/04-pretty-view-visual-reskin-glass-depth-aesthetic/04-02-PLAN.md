---
phase: 04-pretty-view-visual-reskin-glass-depth-aesthetic
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-view/ChatMessage.tsx
  - src/ui/features/pretty-view/ComposeBox.tsx
  - src/ui/features/pretty-view/HarnessTasksPanel.tsx
  - src/ui/features/pretty-view/BackgroundedAgentsPanel.tsx
  - src/ui/features/pretty-view/BackgroundedShellsPanel.tsx
  - src/ui/features/pretty-view/WipBubble.tsx
  - src/ui/features/pretty-view/PlanPendingBubble.tsx
  - src/ui/features/pretty-view/SessionHoldingBanner.tsx
autonomous: true
requirements:
  - VISUAL-01
  - VISUAL-02
  - VISUAL-03
  - VISUAL-05
  - VISUAL-06
  - VISUAL-07
  - VISUAL-08
  - VISUAL-09
must_haves:
  truths:
    - "PrettyView's root surface reads as a warm-neutral dark atmosphere: `linear-gradient(160deg, var(--color-pv-base), var(--color-pv-base-mid), var(--color-pv-base-end))` base + two low-opacity radial-gradient overlays (one warm from top-left tinted with `hsla(var(--pv-id-hue),...)`, one cool from bottom-right in a fixed muted-violet) implying an ambient light source. Body text color is `var(--color-pv-fg)` (warm off-white)."
    - "ChatMessage bubbles read as raised physical objects: `rounded-[var(--radius-pv-bubble)]`, `backdrop-blur-xl saturate-150` + `-webkit-backdrop-filter` fallback, `shadow-[var(--shadow-pv-raised-md)]` (multi-layer ambient + inset rim + hairline stroke), border `border-white/8`. Assistant bubble uses a mid-blue-gray gradient background; user bubble uses an identity-hue gradient (`hsla(var(--pv-id-hue),60%,45%,0.55)` → `hsla(var(--pv-id-hue),55%,30%,0.6)`) with a hue-tinted outer glow (`0 0 30px hsla(var(--pv-id-hue),75%,52%,0.18)`) and hue-tinted border. `prose-code` inline color changes to `text-[var(--color-pv-code-fg)]` (warm coral, mock #ffb896) with a subtle border rectangle. `prose-pre` gets an inner shadow for depth."
    - "ComposeBox has a QUIET surround (no card treatment per VISUAL-06): outer div swaps `border-t border-border bg-background` for `bg-gradient-to-b from-black/15 to-black/30` + `shadow-[inset_0_2px_10px_rgba(0,0,0,0.35)]` — a subtle inset shadow that shades the bottom strip without introducing a bright rim or hard separator."
    - "Textarea has the lightest-touch outline per VISUAL-07: `bg-white/[0.03]`, `border border-[rgba(255,240,215,0.09)]`, `rounded-[10px]`, plus a soft inset shadow. Focused: `border-[hsla(var(--pv-id-hue),65%,55%,0.35)]` + focus-ring `shadow-[inset_0_3px_10px_rgba(0,0,0,0.55),_0_0_0_1px_hsla(var(--pv-id-hue),65%,55%,0.3),_0_0_22px_hsla(var(--pv-id-hue),65%,55%,0.18)]`. Placeholder color `var(--color-pv-fg-dim)`. Transition on box-shadow + border-color."
    - "Send button retains saturated identity-hue glow (VISUAL-08 ONE attention grab-point): `bg-gradient-to-b from-[hsla(var(--pv-id-hue),75%,66%,0.85)] to-[hsla(var(--pv-id-hue),75%,42%,0.9)]`, `border-[hsla(var(--pv-id-hue),80%,72%,0.5)]`, `shadow-[0_4px_12px_rgba(0,0,0,0.5),_inset_0_1px_0_rgba(255,235,190,0.4),_0_0_24px_hsla(var(--pv-id-hue),75%,52%,0.35)]`, text color deep dark for contrast. The reset (RotateCcw) and go-ahead (ThumbsUp) buttons take the quieter icon-button treatment (mock's `.pv-icon-btn` sans `.send`): `bg-gradient-to-b from-[rgba(70,66,58,0.5)] to-[rgba(38,34,28,0.6)]`, `border-white/10`, muted hover with hue-tinted rim on hover."
    - "Context-% fill bar (ComposeBox left column): the track becomes `bg-black/55` + inner shadow; the fill's ≥50/<80/≥80 branches all get identity-hue treatment (`bg-gradient-to-t from-[hsla(var(--pv-id-hue),75%,42%,X)] to-[hsla(var(--pv-id-hue),75%,66%,X)]` + `shadow-[0_0_10px_hsla(var(--pv-id-hue),75%,52%,0.6),_inset_0_1px_0_rgba(255,220,150,0.4)]`) — the color-threshold logic (<50 = full-strength hue, 50-79 = same hue but reduced glow alpha, ≥80 = red-tinted variant `hsla(0,75%,50%,X)` — approaching-full is the ONE breakout from identity-hue because it needs to be alarming regardless of pane identity) preserves the semantic Ashley expects. Guard: the fill's `height: X%` inline style and the color-threshold branches stay driven by `contextPct` — CSS-only change here."
    - "HarnessTasksPanel + BackgroundedAgentsPanel + BackgroundedShellsPanel form ONE visually-coherent ambient panels shelf per VISUAL-05: EACH panel container gets `bg-[linear-gradient(160deg,var(--color-pv-surface-quiet),var(--color-pv-surface-quiet-alt))]`, `border border-[var(--color-pv-border-quiet)]`, `rounded-[var(--radius-pv-card)]`, `shadow-[var(--shadow-pv-quiet-card)]`, `backdrop-blur-md`. Header (`TASKS` / `AGENTS` / `SHELLS`) stays uppercase-tracked but color shifts to `text-[var(--color-pv-fg-muted)]`. In-progress task rows still use static ArrowRight glyph per patch #53 (motion channel stays owned by WipBubble)."
    - "Panel row TAGS (subagentType tag in BackgroundedAgentsPanel) shift to identity-hue: `bg-[hsla(var(--pv-id-hue),75%,52%,0.14)]`, `border-[hsla(var(--pv-id-hue),75%,52%,0.22)]`, `text-[hsla(var(--pv-id-hue),40%,72%,1)]`. Matches mock's `.theme-glass .pv-panel-row .tag`. The arrow-glyph (`text-primary`) is unchanged for consistency across all three panels."
    - "WipBubble adopts the same bubble treatment as ChatMessage assistant bubble (rounded, backdrop-blur, multi-layer shadow, translucent mid-blue-gray gradient background), keeping `Loader2 + animate-spin` in the mock's `rgba(150,180,220,0.9)` border-top color. This preserves the motion-channel semantic while the visual belongs to the Glass aesthetic. Reduced-motion: no change — Tailwind's `motion-reduce:animate-none` already covers spinners at the framework level (verify that patch #51's spinner respects reduced motion; if not, add the modifier)."
    - "PlanPendingBubble adopts the same bubble treatment as ChatMessage assistant bubble (rounded, backdrop-blur, multi-layer shadow, translucent gradient background). ClipboardList glyph unchanged (static per patch #63 correct semantic — motion channel owned by WipBubble). Copy unchanged."
    - "SessionHoldingBanner adopts a translucent muted treatment matching the ambient-panels-shelf visual language (subtle bg + backdrop-blur + hue-tinted border), NOT the raised-bubble treatment (this is chrome, not content). Static RefreshCcw or RotateCcw glyph unchanged per Plan 03-02. The `bg-background/95 backdrop-blur-sm border-b border-border` sticky-positioning wrapper in PrettyView.tsx stays (Phase 3 sticky mechanism is unchanged), but the wrapper's `bg-background/95` value shifts to a warmer variant so it blends with the Phase 4 aesthetic when it appears."
    - "PrettyView's own error inline strip (`errorMessage && status === streaming`) gets a subtle Glass-style hue-tinted destructive treatment (rather than the flat `bg-destructive/10 text-destructive`) so it reads consistent with the surface without losing semantic urgency."
    - "PrettyView's connecting/inactive/error state renders (`Connecting…`, `no active Claude session`, `errorMessage` full-region) adopt the Phase 4 fg colors so the empty-state chrome doesn't jar against the atmospheric base."
    - "Zero behavior changes across all files. Every event handler, ref, effect, prop signature, state hook, WebSocket handler, useCallback, useMemo, computed value (e.g. `wipActive`, `sendDisabled`, `rows`), and JSX conditional gate is byte-preserved. The reskin is CSS-only: className swaps, added inline style properties for identity-hue values that Tailwind's arbitrary-value classes can't express directly, and the atmospheric background gradient on PrettyView's root."
    - "Terminal.tsx is UNTOUCHED. The IdentityBadge md-branch is unchanged, so terminal-pane rendering is byte-identical."
    - "Deploy is NOT part of this plan. Plan 04-03 handles build verification + Nyquist UAT prep + AGENTS.md documentation notes for the eventual deploy at Ashley's separate green-light."
  artifacts:
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "Atmospheric root surface with radial-gradient overlays + Phase 4 fg colors on connecting/inactive/error branches + reskinned inline error strip; SessionHoldingBanner sticky-wrapper bg shifted to warmer variant"
      contains: "radial-gradient"
    - path: "src/ui/features/pretty-view/ChatMessage.tsx"
      provides: "Raised-object bubble treatment with backdrop-blur + multi-layer shadow + hue-tinted user-bubble accents; inline code + preformatted-block Glass styling"
      contains: "backdrop-blur"
    - path: "src/ui/features/pretty-view/ComposeBox.tsx"
      provides: "Quiet compose surround + lightest-touch textarea outline + identity-hue focus ring + saturated send-button glow + identity-hue context-% fill; icon buttons match mock's `.pv-icon-btn` treatment"
      contains: "--pv-id-hue"
    - path: "src/ui/features/pretty-view/HarnessTasksPanel.tsx"
      provides: "Ambient-panel-shelf treatment (Glass-style translucent card + backdrop-blur + subtle shadow)"
      contains: "--color-pv-surface-quiet"
    - path: "src/ui/features/pretty-view/BackgroundedAgentsPanel.tsx"
      provides: "Ambient-panel treatment + identity-hue subagentType tags"
      contains: "--color-pv-surface-quiet"
    - path: "src/ui/features/pretty-view/BackgroundedShellsPanel.tsx"
      provides: "Ambient-panel treatment"
      contains: "--color-pv-surface-quiet"
    - path: "src/ui/features/pretty-view/WipBubble.tsx"
      provides: "Assistant-bubble Glass treatment matching ChatMessage; Loader2 animation preserved"
      contains: "backdrop-blur"
    - path: "src/ui/features/pretty-view/PlanPendingBubble.tsx"
      provides: "Assistant-bubble Glass treatment matching ChatMessage; ClipboardList glyph preserved"
      contains: "backdrop-blur"
    - path: "src/ui/features/pretty-view/SessionHoldingBanner.tsx"
      provides: "Ambient-treatment banner matching the panels-shelf visual language rather than a bubble"
      contains: "backdrop-blur"
  key_links:
    - from: "PrettyView root `--pv-id-hue` (set by Plan 04-01)"
      to: "All hue-consuming components (ChatMessage user bubble, ComposeBox send button, focus ring, context-bar fill, panel tags)"
      via: "CSS custom-property inheritance; Tailwind arbitrary-value classes `hsla(var(--pv-id-hue),X%,Y%,Z)`"
      pattern: "hsla(var(--pv-id-hue)"
    - from: "Phase 4 tokens in index.css (set by Plan 04-01)"
      to: "All 8 pretty-view components' Tailwind arbitrary-value classes (`bg-[var(--color-pv-base)]`, `shadow-[var(--shadow-pv-raised-md)]`, `rounded-[var(--radius-pv-bubble)]`, etc.)"
      via: "Tailwind v4 arbitrary-value class syntax"
      pattern: "var(--color-pv"
    - from: "identity registry's colorHue (patch #17)"
      to: "Per-pane color chain via `--pv-id-hue` set by useSessionIdentity in PrettyView (Plan 04-01)"
      via: "existing useSessionIdentity hook"
      pattern: "useSessionIdentity"
---

<objective>
Apply the Phase 4 Glass depth reskin to all 8 pretty-view components (PrettyView root surface + ChatMessage + ComposeBox + HarnessTasksPanel + BackgroundedAgentsPanel + BackgroundedShellsPanel + WipBubble + PlanPendingBubble + SessionHoldingBanner). CSS-only. Every behavior — event handlers, refs, effects, prop signatures, state hooks, WebSocket dispatching, useCallback / useMemo / computed values, conditional mount gates — byte-preserved. Consumes the tokens + hue plumbing from Plan 04-01.

Grouped into 3 tasks that can be executed sequentially within a single execute-plan session:
1. **Bubbles + banner** — ChatMessage, WipBubble, PlanPendingBubble, SessionHoldingBanner. Shared bubble/pill treatment; batching them lets the executor keep the shared classNames mentally in one place.
2. **Ambient panels shelf** — HarnessTasksPanel, BackgroundedAgentsPanel, BackgroundedShellsPanel. Shared ambient-card treatment; the identity-hue tag treatment is only in BackgroundedAgentsPanel.
3. **PrettyView root + ComposeBox** — the atmospheric base + all compose ergonomics. This is the biggest single-component change (ComposeBox is 454 lines of props + effects + 3 send paths + context-bar + icon column + 3 buttons + inline error strip).

**Requirement mapping this plan closes:**
- VISUAL-01 (atmospheric warm-neutral base)
- VISUAL-02 (bubbles as raised physical objects with backdrop-blur + shadows)
- VISUAL-03 (identity-hue color chain end-to-end — user bubble + ctxbar + send button + focus ring + panel tags)
- VISUAL-05 (ambient panels shelf as one quiet floating card treatment)
- VISUAL-06 (compose surround is quiet — no card, blends into atmosphere)
- VISUAL-07 (textarea lightest-touch outline + identity-hue focus ring)
- VISUAL-08 (send button retains saturated identity-hue glow — ONE grab-point)
- VISUAL-09 (all existing pretty-view behaviour preserved — CSS-only)

VISUAL-04 + VISUAL-10 were already closed by Plan 04-01.

**Rebase-risk drift caveat (preserve this in AGENTS.md at documentation time — Plan 04-03 handles the doc writeup prep):**

- Every file touched is FORK-ONLY (created by patches #43/#44/#45/#47/#48/#50/#51/#52/#57/#61/#63/#68 and #Phase-3). No upstream conflict surface exists for any of these files.
- Terminal.tsx is UNTOUCHED. This is load-bearing per Plan 04-01's discussion — Termix's most-touched file (patches #1/#3/#6/#13/#17/#24/#26/#28/#33/#39/#40/#41/#44/#50/#51/#52/#60) stays out of the diff.
- PrettyView.tsx WILL have already been touched by Plan 04-01 (root `--pv-id-hue` wiring + IdentityBadge lg mount). Plan 04-02 layers additional changes on top: the atmospheric background gradient, the connecting/inactive/error state chrome tweaks, and the inline error strip reskin. Every existing Plan 04-01 change is preserved. Executor: do NOT undo `data-pv-root`, `--pv-id-hue` style, or the IdentityBadge mount from Plan 04-01.
- The SessionHoldingBanner sticky-positioning wrapper in PrettyView.tsx from Plan 03-02 (lines 366-370 area — sticky top-0 z-10 -mx-4 -mt-3 mb-3 px-4 py-2 bg-background/95 backdrop-blur-sm border-b border-border) stays STRUCTURALLY the same but its `bg-background/95` and `border-b border-border` shift to warmer Phase 4 variants. The FRAGILITY WARNING comment about ancestors gaining transform/will-change/filter/perspective/backdrop-filter STAYS intact — do NOT remove.
- **Warning:** Plan 04-02 makes PrettyView's outer div wrap a `backdrop-filter` (via a `bg-clip-*` or `backdrop-blur` class on the root or via inline style). This IS the ancestor-gains-backdrop-filter case the Plan 03-02 FRAGILITY WARNING names — however: the FRAGILITY comment names ancestors OF the sticky element, which includes ancestors WITHIN PrettyView. The scroll region and its `sticky top-0` banner are DESCENDANTS of PrettyView's root, so PrettyView's root gaining any of those properties WOULD break the banner. **HARD LOCK:** the atmospheric depth on PrettyView's root is applied via `background` (linear-gradient + radial-gradient composited), NOT via `backdrop-filter`, `filter`, `transform`, `will-change`, or `perspective`. `background-image` is safe — it does NOT establish a new containing block. If the executor is tempted to add `backdrop-blur` to PrettyView's root, DO NOT — apply blur only to bubble/card leaf elements or update the FRAGILITY comment + option-(b) restructure the banner mount site.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-CONTEXT.md
@.planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reskin bubbles + banner — ChatMessage, WipBubble, PlanPendingBubble, SessionHoldingBanner</name>
  <read_first>
    - src/ui/features/pretty-view/ChatMessage.tsx (76 lines) — full read.
    - src/ui/features/pretty-view/WipBubble.tsx (35 lines) — full read.
    - src/ui/features/pretty-view/PlanPendingBubble.tsx (49 lines) — full read.
    - src/ui/features/pretty-view/SessionHoldingBanner.tsx (~50 lines from Plan 03-02) — full read.
    - Design spec mock lines 297-320 (glass-theme bubble treatment) + lines 333-355 (ambient-panels shelf treatment for the banner analogy).
  </read_first>
  <files>src/ui/features/pretty-view/ChatMessage.tsx, src/ui/features/pretty-view/WipBubble.tsx, src/ui/features/pretty-view/PlanPendingBubble.tsx, src/ui/features/pretty-view/SessionHoldingBanner.tsx</files>
  <action>
Reskin all four bubble/pill components together — they share the assistant-bubble treatment; ChatMessage adds the user-bubble variant on top.

**Shared assistant-bubble class recipe** (use across ChatMessage assistant branch, WipBubble, PlanPendingBubble). Compose via `cn()`:

```
"rounded-[var(--radius-pv-bubble)] px-3 py-2 leading-relaxed"
"backdrop-blur-xl saturate-150"
"[-webkit-backdrop-filter:blur(20px)_saturate(1.6)]"
"bg-[linear-gradient(160deg,rgba(45,55,80,0.5),rgba(28,35,55,0.55))]"
"text-[#dfe3ee]"
"border border-white/[0.08]"
"shadow-[0_8px_24px_rgba(0,0,0,0.5),_0_1px_0_rgba(255,255,255,0.12)_inset,_0_0_0_0.5px_rgba(255,255,255,0.05)]"
```

(The `-webkit-backdrop-filter` fallback goes as an arbitrary-value class since Tailwind v4 doesn't ship a first-class utility for the vendor-prefixed form; the standard `backdrop-blur-xl saturate-150` covers `backdrop-filter` in modern browsers.)

**Edit 1 — ChatMessage.tsx:**

Reskin both branches. Bubble className stays composed via the existing `cn(...)` call — the current call has ~14 discrete arguments; keep the style but swap the content of each argument for Phase 4 values. Specifically:

- Outer flex `justify-end` / `justify-start` unchanged (layout stays).
- Inner bubble className: replace the current shared classes with the shared assistant-bubble recipe above, PLUS the prose-* overrides (kept from patch #47/#48), PLUS the font-family override (kept from patch #48).
- Preserve the `max-w-[85%]` cap.
- Preserve `break-words text-sm leading-relaxed`.
- Preserve `[&>*:first-child]:mt-0 [&>*:last-child]:mb-0`.
- Preserve `prose prose-sm max-w-[85%]` for typography.
- Change `prose-code:*` classes to swap `prose-code:font-[JetBrains...]` (keep) plus a NEW `prose-code:bg-white/[0.08]` + `prose-code:text-[var(--color-pv-code-fg)]` + `prose-code:border prose-code:border-white/[0.06]` for the warm-coral code color per mock line 321-325.
- Change `prose-pre:*` classes: keep `prose-pre:my-2 prose-pre:p-2 prose-pre:rounded prose-pre:font-[JetBrains...]`, ADD `prose-pre:bg-[rgba(10,12,20,0.6)] prose-pre:border prose-pre:border-white/[0.06] prose-pre:shadow-[inset_0_2px_8px_rgba(0,0,0,0.4)]` per mock line 326-330.
- User-branch (`isUser ? ...`): swap `bg-primary text-primary-foreground prose-invert` for the identity-hue gradient recipe:
  ```
  "bg-[linear-gradient(160deg,hsla(var(--pv-id-hue),60%,45%,0.55),hsla(var(--pv-id-hue),55%,30%,0.6))]"
  "text-[#fbf5e8]"
  "border-[hsla(var(--pv-id-hue),75%,52%,0.28)]"
  "shadow-[0_8px_24px_rgba(0,0,0,0.5),_0_1px_0_rgba(255,220,170,0.18)_inset,_0_0_0_0.5px_hsla(var(--pv-id-hue),75%,52%,0.18),_0_0_30px_hsla(var(--pv-id-hue),75%,52%,0.18)]"
  "prose-invert"
  ```
  This REPLACES the outer `border border-white/[0.08]` and the outer shadow — the user branch overrides both. Compose via cn() so later classes win.

- Assistant-branch (else): apply the shared assistant-bubble recipe (already includes border + shadow + bg gradient), and add `dark:prose-invert` for the prose contrast (kept from patch #48).

**Preserve exactly:**
- `role` prop signature (user | assistant).
- `content` prop signature.
- ReactMarkdown + remarkGfm + the `components={{a: ...target="_blank"...}}` override (patch #62).
- The `isUser = role === "user"` computation.
- Every JSX structure and nesting.

**Edit 2 — WipBubble.tsx:**

Adopt the shared assistant-bubble recipe on the inner bubble div. Loader2 spinner + `animate-spin` PRESERVED. Add `motion-reduce:animate-none` to the Loader2 className to respect reduced motion (patch #51 does NOT gate this today — verify by grep and add if missing; this is a defensive add, doesn't break the WIP semantic since a paused Loader2 still reads as "waiting"). Icon color shifts from default to `text-[rgba(150,180,220,0.9)]` per mock line 331. aria-label preserved.

**Edit 3 — PlanPendingBubble.tsx:**

Adopt the shared assistant-bubble recipe on the inner bubble div. ClipboardList glyph PRESERVED as static (patch #63 semantic). aria-label + role + copy PRESERVED (patch #67 corrected copy stays).

**Edit 4 — SessionHoldingBanner.tsx:**

This is DIFFERENT — banner is chrome (matches ambient-panels-shelf language), not a raised bubble. Recipe:

```
"flex items-center gap-2"
"rounded-[var(--radius-pv-card)] px-3 py-1.5"
"bg-[linear-gradient(160deg,var(--color-pv-surface-quiet),var(--color-pv-surface-quiet-alt))]"
"text-[var(--color-pv-fg-muted)]"
"border border-[var(--color-pv-border-quiet)]"
"backdrop-blur-md"
"[-webkit-backdrop-filter:blur(12px)]"
"text-xs"
"shadow-[var(--shadow-pv-quiet-card)]"
```

Preserve: `role="status"`, `aria-label="Session recycling — reconnecting"`, `RefreshCcw` (or `RotateCcw` if Plan 03-02 shipped that) icon, exact copy string, single-line layout. shrink-0 on icon preserved.

**Verification for this task:**

```
npm run build 2>&1 | tail -10
```

Expected: `built in Xs`, no TypeScript errors.

Additionally: grep to confirm no behavior-level code (event handlers, props, imports of React state utilities) was changed in any of the 4 files. Every file should still be a pure functional component with the same props + return statement, only className content changed.
  </action>
  <verify>
    <automated>grep -q 'backdrop-blur-xl' src/ui/features/pretty-view/ChatMessage.tsx &amp;&amp; grep -q 'hsla(var(--pv-id-hue)' src/ui/features/pretty-view/ChatMessage.tsx &amp;&amp; grep -q 'rounded-\[var(--radius-pv-bubble)\]' src/ui/features/pretty-view/ChatMessage.tsx &amp;&amp; grep -q 'prose-code:text-\[var(--color-pv-code-fg)\]' src/ui/features/pretty-view/ChatMessage.tsx &amp;&amp; grep -q 'ReactMarkdown' src/ui/features/pretty-view/ChatMessage.tsx &amp;&amp; grep -q 'backdrop-blur-xl' src/ui/features/pretty-view/WipBubble.tsx &amp;&amp; grep -q 'animate-spin' src/ui/features/pretty-view/WipBubble.tsx &amp;&amp; grep -q 'motion-reduce:animate-none' src/ui/features/pretty-view/WipBubble.tsx &amp;&amp; grep -q 'backdrop-blur-xl' src/ui/features/pretty-view/PlanPendingBubble.tsx &amp;&amp; grep -q 'ClipboardList' src/ui/features/pretty-view/PlanPendingBubble.tsx &amp;&amp; grep -q 'Plan proposed' src/ui/features/pretty-view/PlanPendingBubble.tsx &amp;&amp; grep -q 'backdrop-blur-md' src/ui/features/pretty-view/SessionHoldingBanner.tsx &amp;&amp; grep -q 'Session recycling' src/ui/features/pretty-view/SessionHoldingBanner.tsx &amp;&amp; ! grep -q 'bg-card' src/ui/features/pretty-view/ChatMessage.tsx &amp;&amp; ! grep -q 'bg-card' src/ui/features/pretty-view/WipBubble.tsx &amp;&amp; ! grep -q 'bg-card' src/ui/features/pretty-view/PlanPendingBubble.tsx &amp;&amp; npm run build 2>&amp;1 | tail -5 | grep -q 'built in'</automated>
  </verify>
  <acceptance_criteria>
    - ChatMessage.tsx: bubble className uses `backdrop-blur-xl saturate-150`, `rounded-[var(--radius-pv-bubble)]`, hue-driven user branch (with `hsla(var(--pv-id-hue),...)`), Phase 4 shadow stack, warm-coral prose-code color; no `bg-card` / `bg-primary` remnants; ReactMarkdown + remarkGfm + anchor-target override preserved; role/content props unchanged.
    - WipBubble.tsx: bubble className adopts shared assistant recipe (backdrop-blur, gradient, multi-layer shadow); Loader2 + animate-spin + motion-reduce:animate-none present; aria-label preserved.
    - PlanPendingBubble.tsx: bubble className adopts shared assistant recipe; ClipboardList glyph static; copy exact "Plan proposed — awaiting your approval" preserved; role + aria-label preserved.
    - SessionHoldingBanner.tsx: pill className adopts ambient-card recipe (linear-gradient bg, backdrop-blur-md, warmer border, quiet-card shadow); role="status" + aria-label + copy + static icon preserved; text-xs preserved.
    - No behavior code changes in any of the 4 files — every file's exports, imports, and JSX structure unchanged aside from className content.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>All four bubble/banner components now render with the Phase 4 Glass aesthetic. Bubbles read as physical raised objects; the holding banner reads as calm chrome. Identity-hue accents thread through the user bubble as designed. Next task is the ambient-panels shelf.</done>
</task>

<task type="auto">
  <name>Task 2: Reskin ambient panels shelf — HarnessTasksPanel, BackgroundedAgentsPanel, BackgroundedShellsPanel</name>
  <read_first>
    - src/ui/features/pretty-view/HarnessTasksPanel.tsx (86 lines) — full read.
    - src/ui/features/pretty-view/BackgroundedAgentsPanel.tsx (75 lines) — full read.
    - src/ui/features/pretty-view/BackgroundedShellsPanel.tsx (76 lines) — full read.
    - Design spec mock lines 333-355 (`.theme-glass .pv-panels` + `.pv-panel` + `.pv-panel-row .tag`).
  </read_first>
  <files>src/ui/features/pretty-view/HarnessTasksPanel.tsx, src/ui/features/pretty-view/BackgroundedAgentsPanel.tsx, src/ui/features/pretty-view/BackgroundedShellsPanel.tsx</files>
  <action>
Apply the same ambient-panel treatment to all three panels. They currently share `"border-t border-border bg-muted/30 shrink-0 max-h-40 overflow-y-auto"` — swap to a shared Phase 4 recipe.

**Shared panel-container recipe** (composed via `cn()` on the outer `<div aria-label="...">`):

```
"shrink-0 max-h-40 overflow-y-auto"
"mx-3 my-1 px-3 py-1.5"
"bg-[linear-gradient(160deg,var(--color-pv-surface-quiet),var(--color-pv-surface-quiet-alt))]"
"border border-[var(--color-pv-border-quiet)]"
"rounded-[var(--radius-pv-card)]"
"shadow-[var(--shadow-pv-quiet-card)]"
"backdrop-blur-md"
"[-webkit-backdrop-filter:blur(12px)]"
```

Rationale:
- Drop the top-border-only separator (`border-t border-border`) — Phase 4 uses per-panel full-border cards, not stepped edges (mock has each panel as a card in a shelf).
- Drop the `bg-muted/30` — replaced by the warm-tint linear gradient.
- `mx-3 my-1` — 12px horizontal breathing room from the sides + 4px vertical between stacked panels (BackgroundedShellsPanel + BackgroundedAgentsPanel + HarnessTasksPanel all mount above ComposeBox, potentially all three at once). This yields the "quiet floating card shelf" reading per VISUAL-05.
- `px-3 py-1.5` on the panel outer: reduce padding a hair since the border now defines the boundary (previously the panel had chrome only from its header row, now the border cost is absorbed by reducing internal pad).
- backdrop-blur-md gives it the glass-plane reading over PrettyView's atmospheric depth.

**Shared header-row treatment:**

Existing header row is:
```
<div className="px-3 py-1.5 text-xs font-medium text-muted-foreground uppercase tracking-wide flex items-center gap-2">
```

Swap `text-muted-foreground` for `text-[var(--color-pv-fg-muted)]` and drop the `px-3 py-1.5` (parent panel now provides padding). Result:
```
<div className="py-1 text-xs font-medium text-[var(--color-pv-fg-muted)] uppercase tracking-wide flex items-center gap-2">
```

The count-span currently uses `text-muted-foreground/70`; keep that pattern by swapping to `text-[var(--color-pv-fg-dim)]`.

**Body list treatment:**

Existing body list is:
```
<ul className="px-3 pb-2 flex flex-col gap-1">
```

Drop `px-3 pb-2` (parent handles padding); keep the flex-col + gap-1:
```
<ul className="pb-1 flex flex-col gap-1">
```

**Row-level tweaks:**

Row body text currently uses `text-foreground/90` and `text-foreground font-medium` (HarnessTasksPanel in-progress); swap to `text-[var(--color-pv-fg)]/90` and `text-[var(--color-pv-fg)] font-medium` respectively so rows use warm off-white.

**BackgroundedAgentsPanel-specific:** the subagentType tag (currently `text-xs font-medium text-muted-foreground uppercase tracking-wide shrink-0` — line ~62-65). Give it identity-hue accents per mock line 351-354:

```
"text-[10px] font-medium uppercase tracking-wide shrink-0"
"px-1.5 py-0.5 rounded-sm"
"bg-[hsla(var(--pv-id-hue),75%,52%,0.14)]"
"border border-[hsla(var(--pv-id-hue),75%,52%,0.22)]"
"text-[hsla(var(--pv-id-hue),40%,72%,1)]"
```

This makes the "Explore" / "general-purpose" tag pill the identity color — the panel visually ties to the pane it belongs to.

**BackgroundedShellsPanel-specific:** the `<code>` line for the command (mock: `.pv-panel-row .row-main` uses `#c8ccd6`). Swap:
- code line: `text-xs text-muted-foreground/80 font-mono break-all` → `text-xs text-[var(--color-pv-fg-dim)] font-mono break-all`. No other change.

**Static icon glyphs preserved unchanged**: ArrowRight in HarnessTasks (in-progress) + Circle (pending) + CircleDot (completed defensive) + ArrowRight in BackgroundedAgents + Terminal in BackgroundedShells. All keep `text-primary` for the primary-status glyphs and `text-muted-foreground` for the pending/completed variants. Patch #53 semantic HARD LOCK: NO SPINNERS in these panels.

**Preserve exactly (all three files):**
- Component signatures (props types, exports).
- List item keys (`t.id`, `a.toolUseId`, `s.toolUseId`).
- Filter/render logic (no `.filter()` additions — the parent gates on non-empty already).
- Title attributes for tooltips.
- `activeForm || subject` fallback in HarnessTasksPanel.
- `subagentType || "Agent"` and `description || subagentType || "Agent"` fallbacks in BackgroundedAgentsPanel.
- `description || command || "Shell"` primary logic in BackgroundedShellsPanel.
- `showCommandLine` condition in BackgroundedShellsPanel.

**Verification for this task:**

```
npm run build 2>&1 | tail -10
```

Expected: `built in Xs`, no TypeScript errors.
  </action>
  <verify>
    <automated>grep -q 'backdrop-blur-md' src/ui/features/pretty-view/HarnessTasksPanel.tsx &amp;&amp; grep -q 'var(--color-pv-surface-quiet)' src/ui/features/pretty-view/HarnessTasksPanel.tsx &amp;&amp; ! grep -q 'bg-muted/30' src/ui/features/pretty-view/HarnessTasksPanel.tsx &amp;&amp; grep -q 'backdrop-blur-md' src/ui/features/pretty-view/BackgroundedAgentsPanel.tsx &amp;&amp; grep -q 'hsla(var(--pv-id-hue)' src/ui/features/pretty-view/BackgroundedAgentsPanel.tsx &amp;&amp; ! grep -q 'bg-muted/30' src/ui/features/pretty-view/BackgroundedAgentsPanel.tsx &amp;&amp; grep -q 'backdrop-blur-md' src/ui/features/pretty-view/BackgroundedShellsPanel.tsx &amp;&amp; ! grep -q 'bg-muted/30' src/ui/features/pretty-view/BackgroundedShellsPanel.tsx &amp;&amp; grep -q 'ArrowRight' src/ui/features/pretty-view/HarnessTasksPanel.tsx &amp;&amp; grep -q 'ArrowRight' src/ui/features/pretty-view/BackgroundedAgentsPanel.tsx &amp;&amp; grep -q 'Terminal' src/ui/features/pretty-view/BackgroundedShellsPanel.tsx &amp;&amp; ! grep -q 'animate-spin' src/ui/features/pretty-view/HarnessTasksPanel.tsx &amp;&amp; ! grep -q 'animate-spin' src/ui/features/pretty-view/BackgroundedAgentsPanel.tsx &amp;&amp; ! grep -q 'animate-spin' src/ui/features/pretty-view/BackgroundedShellsPanel.tsx &amp;&amp; npm run build 2>&amp;1 | tail -5 | grep -q 'built in'</automated>
  </verify>
  <acceptance_criteria>
    - All three panels use the shared ambient-panel recipe: linear-gradient bg via `--color-pv-surface-quiet`, `border-[var(--color-pv-border-quiet)]`, `rounded-[var(--radius-pv-card)]`, `shadow-[var(--shadow-pv-quiet-card)]`, `backdrop-blur-md`.
    - Header row `text-muted-foreground` swapped for `text-[var(--color-pv-fg-muted)]`; count-span uses `text-[var(--color-pv-fg-dim)]`.
    - BackgroundedAgentsPanel tag pill uses identity-hue accents (`hsla(var(--pv-id-hue),...)` × 3 for bg + border + text).
    - Static icon glyphs (ArrowRight, Circle, Terminal) preserved unchanged; NO spinners in any of the three panels (`animate-spin` grep returns nothing).
    - Component signatures, props types, list item keys, filter/render logic all byte-preserved.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>The three ambient panels now render as a coherent floating-card shelf above ComposeBox, with the subagentType tag in BackgroundedAgentsPanel picking up the per-pane identity hue. Motion channel remains owned by WipBubble (no spinners here). Next task is PrettyView root + ComposeBox — the biggest single change of Plan 04-02.</done>
</task>

<task type="auto">
  <name>Task 3: Reskin PrettyView root surface + ComposeBox — atmospheric depth, quiet compose surround, textarea outline, identity-hue focus ring + send-button glow + context-bar</name>
  <read_first>
    - src/ui/features/pretty-view/PrettyView.tsx — full read (post-Plan-04-01 state, ~470 lines).
    - src/ui/features/pretty-view/ComposeBox.tsx — full read (454 lines). Focus on the return statement (~ line 358-454) — that's the JSX to reskin. Preserve every event handler / effect / ref / useCallback / useState / import.
    - `src/components/textarea.tsx` — read to know what className is composed onto the underlying `<textarea>`. The `<Textarea ref={textareaRef} className="resize-none flex-1" ... />` in ComposeBox will need its className extended to carry the Phase 4 outline + focus-ring — verify that the Textarea shadcn-style wrapper forwards className and doesn't override background/border.
    - Design spec mock lines 283-296 (root gradient + shadow) + lines 357-408 (compose + ctxbar + textarea + icon buttons + send).
  </read_first>
  <files>src/ui/features/pretty-view/PrettyView.tsx, src/ui/features/pretty-view/ComposeBox.tsx</files>
  <action>
**Edit 1 — PrettyView.tsx root surface + connecting/inactive/error chrome + inline-error strip + banner sticky wrapper:**

Locate the return statement's outermost div (the one Plan 04-01 already added `data-pv-root` + `--pv-id-hue` style to). Update its className from:

```
cn("h-full w-full flex flex-col bg-background", className)
```

To Phase 4 atmospheric base:

```
cn(
  "h-full w-full flex flex-col relative overflow-hidden",
  "text-[var(--color-pv-fg)]",
  "font-[Inter_Variable,ui-sans-serif,system-ui,sans-serif]",
  className,
)
```

**Add background via `style`** (compose with the existing `--pv-id-hue` style prop and the incoming `style` prop). The atmospheric depth is `linear-gradient` base + TWO `radial-gradient` overlays — one warm-hue-tinted from top-left, one fixed cool-violet from bottom-right. Since we need to REFERENCE `--pv-id-hue` in the warm radial gradient, and CSS custom properties are LIVE in `background-image` values, this Just Works with `hsla(var(--pv-id-hue),...)`:

```tsx
style={
  {
    "--pv-id-hue": String(pvHue),
    backgroundImage: `
      radial-gradient(ellipse 800px 400px at 20% 0%, hsla(${pvHue}, 60%, 45%, 0.14), transparent 60%),
      radial-gradient(ellipse 700px 500px at 90% 100%, rgba(90, 60, 120, 0.15), transparent 60%),
      linear-gradient(160deg, var(--color-pv-base) 0%, var(--color-pv-base-mid) 50%, var(--color-pv-base-end) 100%)
    `,
    ...style,
  } as React.CSSProperties
}
```

Note: use `${pvHue}` in the radial gradient (numeric fallback of 35), NOT `hsla(var(--pv-id-hue),...)`. Why: the radial gradient's outer stops need to resolve at style compute time; inline style computes the string once per render, and `${pvHue}` is already the resolved number. The inner components' `hsla(var(--pv-id-hue),...)` classes ARE fine because they resolve at paint time (CSS custom props are dynamic per-cascade). Both patterns work — using `${pvHue}` here avoids browser edge cases where CSS custom props in gradients sometimes need `@property` declarations to interpolate.

**HARD LOCK:** do NOT add `backdropFilter`, `filter`, `transform`, `willChange`, or `perspective` to this root div's style. Doing so would break the SessionHoldingBanner sticky positioning per Plan 03-02's FRAGILITY WARNING. `background` + `background-image` are safe — they do NOT establish a new containing block for sticky descendants.

**Update the connecting state render** (line 312-314 area):
```
<div className="p-4 text-sm text-[var(--color-pv-fg-muted)]">Connecting…</div>
```

**Update the inactive state render** (line 316-320 area):
```
<div className="flex-1 flex items-center justify-center p-4 text-sm text-[var(--color-pv-fg-muted)]">
  no active Claude session
</div>
```

**Update the error state render** (line 322-324 area) — keep destructive intent but softer:
```
<div className="p-4 text-sm text-destructive">{errorMessage}</div>
```
(Leave `text-destructive` — Ashley needs this to READ as error. Do NOT swap for a hue-tinted variant.)

**Update the inline error strip** (line 404-408 area):
```
<div className="border-t border-white/[0.08] bg-[hsla(var(--pv-id-hue),75%,52%,0.06)] text-destructive text-xs px-3 py-1">
  {errorMessage}
</div>
```
The subtle hue-tint keeps the strip visually consistent with the surface; `text-destructive` preserves the semantic that this is an error.

**Update the SessionHoldingBanner sticky wrapper** (Plan 03-02's mount site, line 366-370 area). Change:
```
"sticky top-0 z-10 -mx-4 -mt-3 mb-3 px-4 py-2 bg-background/95 backdrop-blur-sm border-b border-border"
```
To a warmer Phase 4 variant:
```
"sticky top-0 z-10 -mx-4 -mt-3 mb-3 px-4 py-2 bg-[rgba(20,18,14,0.92)] backdrop-blur-sm border-b border-[var(--color-pv-border-quiet)]"
```
- Preserve `sticky top-0 z-10 -mx-4 -mt-3 mb-3 px-4 py-2` positioning byte-identically (Plan 03-02's FRAGILITY WARNING mechanism).
- Preserve `backdrop-blur-sm`.
- The FRAGILITY WARNING code comment PRESERVED IN FULL — do not delete or truncate.

**Preserve exactly:**
- All existing state hooks, useAutoScroll, WS effect + dispatcher, message handling, session-changeover cases from Plan 03-02, IdentityBadge lg mount from Plan 04-01.
- `useSessionIdentity` + `sessionMatchKey` + `pvHue` + `pvIdentityKey` from Plan 04-01.
- Scroll region JSX structure (scrollRef div wrapping contentRef div wrapping message.map + WipBubble + PlanPendingBubble).
- Jump-to-latest pill.
- ComposeBox + HarnessTasksPanel + BackgroundedAgentsPanel + BackgroundedShellsPanel mount conditionals + props.
- `false && inactiveReason` dead branch (FALLBACK-01 defense in depth).

**Edit 2 — ComposeBox.tsx return statement reskin:**

Locate the outer container div (line 359-364 area):
```tsx
<div
  className={cn(
    "border-t border-border bg-background flex flex-col gap-1 px-3 py-2 shrink-0",
    className,
  )}
>
```

Replace with the QUIET compose-surround per VISUAL-06 (no card, no bright top rim, inset shadow at the bottom):
```tsx
<div
  className={cn(
    "flex flex-col gap-1 px-3 py-3 shrink-0",
    "bg-[linear-gradient(180deg,rgba(0,0,0,0.15),rgba(0,0,0,0.3))]",
    "shadow-[inset_0_2px_10px_rgba(0,0,0,0.35)]",
    className,
  )}
>
```
- Drops the `border-t border-border` (VISUAL-06 explicit: no hard separator).
- Drops the `bg-background` (adopts the linear-gradient darken instead — reads as continuous atmosphere with a subtle deepening at the bottom strip).
- Adds inset top-shadow (mock's `inset 0 2px 10px rgba(0,0,0,0.35)`) — the ONLY visual cue distinguishing compose from the messages above.

**Update the context-bar (~ lines 372-396):**

The `<div className="w-1.5 self-stretch bg-muted/40 rounded-sm relative overflow-hidden">` outer becomes:
```tsx
<div
  className="w-1.5 self-stretch rounded-sm relative overflow-hidden bg-black/55 shadow-[inset_0_0_4px_rgba(0,0,0,0.7),_0_1px_0_rgba(255,240,215,0.08)]"
  role="meter"
  ...
>
```

The inner `<div className="absolute bottom-0 left-0 right-0 transition-[height] duration-300 ..." style={{height: `${...}%`}}>` fill gets a hue-driven treatment for <50 and 50-79 branches, and a red-tinted variant for ≥80 (approaching-full stays alarming regardless of pane identity — semantic HARD LOCK: red-at-80 is what Ashley reads for "context is filling up dangerously fast", overriding it with the pane identity hue would degrade the signal):

```tsx
className={cn(
  "absolute bottom-0 left-0 right-0 transition-[height] duration-300",
  contextPct < 50
    ? "bg-[linear-gradient(180deg,hsla(var(--pv-id-hue),75%,66%,1),hsla(var(--pv-id-hue),75%,42%,1))] shadow-[0_0_10px_hsla(var(--pv-id-hue),75%,52%,0.6),_inset_0_1px_0_rgba(255,220,150,0.4)]"
    : contextPct < 80
      ? "bg-[linear-gradient(180deg,hsla(var(--pv-id-hue),70%,60%,0.9),hsla(var(--pv-id-hue),70%,40%,0.9))] shadow-[0_0_8px_hsla(var(--pv-id-hue),70%,50%,0.45),_inset_0_1px_0_rgba(255,220,150,0.3)]"
      : "bg-[linear-gradient(180deg,hsla(0,75%,60%,1),hsla(0,75%,40%,1))] shadow-[0_0_10px_hsla(0,75%,50%,0.6),_inset_0_1px_0_rgba(255,220,150,0.4)]",
)}
```

(Yes, the color-threshold logic stays; the branches now emit hue-driven gradients rather than flat `bg-green-500`/`bg-yellow-500`/`bg-red-500`. The ≥80 branch stays red because the color's semantic is "approaching-full = alarming", which overrides the per-pane identity signal.)

Guard: `style={{height: ...%}}` inline style + the `contextPct` conditional gate + `role="meter"` + aria attributes + title tooltip all PRESERVED byte-identically.

**Update the Textarea (~ line 397-409):**

The `<Textarea ...>` element gets an override className that carries the Phase 4 outline + focus-ring. Since `<Textarea>` is a shadcn wrapper that forwards className with cn merge (verify by reading textarea.tsx), extend it:

```tsx
<Textarea
  ref={textareaRef}
  value={text}
  onChange={(e) => handleTextChange(e.target.value)}
  onBlur={handleBlur}
  onKeyDown={handleKeyDown}
  placeholder="Message Claude…"
  rows={rows}
  className={cn(
    "resize-none flex-1",
    "bg-white/[0.03] text-[#f0ebe0]",
    "border border-[rgba(255,240,215,0.09)]",
    "rounded-[10px] px-4 py-3",
    "placeholder:text-[var(--color-pv-fg-dim)]",
    "shadow-[inset_0_2px_6px_rgba(0,0,0,0.4),_0_1px_0_rgba(255,240,215,0.04)]",
    "transition-[box-shadow,border-color] duration-200",
    "focus:border-[hsla(var(--pv-id-hue),65%,55%,0.35)]",
    "focus:shadow-[inset_0_3px_10px_rgba(0,0,0,0.55),_inset_0_1px_2px_rgba(0,0,0,0.35),_0_1px_0_rgba(255,240,215,0.07),_0_0_0_1px_hsla(var(--pv-id-hue),65%,55%,0.3),_0_0_22px_hsla(var(--pv-id-hue),65%,55%,0.18)]",
    "focus-visible:ring-0 focus-visible:outline-none",
  )}
/>
```

- `focus-visible:ring-0` overrides any shadcn default focus ring (the identity-hue focus ring is our own via `focus:shadow-[...]`).
- The transition on box-shadow + border-color makes the focus reveal feel deliberate.
- Preserve the `onChange`, `onBlur`, `onKeyDown`, `ref`, `value`, `placeholder`, `rows` props unchanged.
- **If** the shadcn Textarea overrides our styling: fall back to using a native `<textarea>` inside a wrapper div, adopting the same event handlers + ref forwarding. Note this fallback in the summary. Most likely the shadcn wrapper composes className via `cn(defaults, className)` and our overrides win — verify by grepping the textarea.tsx file for its default classes.

**Update the icon column (~ line 417-447):**

The three buttons (RotateCcw reset, ThumbsUp go-ahead, Send) currently use shadcn Button with `size="icon-sm"` and `variant="outline"` (for reset + go-ahead) and default variant (for Send). Reskin:

- RotateCcw + ThumbsUp: adopt the mock's `.pv-icon-btn` quiet treatment. Override via className:
  ```tsx
  className={cn(
    "border-white/10",
    "bg-[linear-gradient(180deg,rgba(70,66,58,0.5),rgba(38,34,28,0.6))]",
    "text-[#e8e4d8]",
    "shadow-[0_2px_4px_rgba(0,0,0,0.4),_inset_0_1px_0_rgba(255,240,210,0.12)]",
    "hover:bg-[linear-gradient(180deg,rgba(100,85,55,0.7),rgba(60,50,32,0.8))]",
    "hover:border-[hsla(var(--pv-id-hue),75%,55%,0.35)]",
    "hover:shadow-[0_4px_8px_rgba(0,0,0,0.5),_inset_0_1px_0_rgba(255,240,210,0.2),_0_0_20px_hsla(var(--pv-id-hue),75%,52%,0.22)]",
  )}
  ```
- Send: adopt the mock's `.pv-icon-btn.send` saturated identity-hue treatment. Override via className:
  ```tsx
  className={cn(
    "border-[rgba(255,220,170,0.5)]",
    "bg-[linear-gradient(180deg,hsla(var(--pv-id-hue),75%,66%,0.85),hsla(var(--pv-id-hue),75%,42%,0.9))]",
    "text-[#1a0f04]",
    "shadow-[0_4px_12px_rgba(0,0,0,0.5),_inset_0_1px_0_rgba(255,235,190,0.4),_0_0_24px_hsla(var(--pv-id-hue),75%,52%,0.35)]",
    "hover:bg-[linear-gradient(180deg,hsla(var(--pv-id-hue),80%,72%,0.9),hsla(var(--pv-id-hue),80%,48%,0.95))]",
    "hover:shadow-[0_6px_16px_rgba(0,0,0,0.6),_inset_0_1px_0_rgba(255,235,190,0.5),_0_0_32px_hsla(var(--pv-id-hue),75%,52%,0.45)]",
    "disabled:opacity-40 disabled:cursor-not-allowed",
  )}
  ```

Preserve the `variant="outline"` on reset + go-ahead so shadcn's default variant styling composes as the base (our overrides win via cn ordering); alternatively drop `variant` entirely if shadcn's defaults fight the overrides (verify empirically). Preserve `size="icon-sm"`. Preserve `onClick` handlers, `disabled`, `aria-label`, `title`, and the Lucide icons themselves (`<RotateCcw className="size-4" />` etc.).

**Update the inline error text (line 449-451):**

```tsx
{errorMessage && (
  <div className="text-xs text-destructive">{errorMessage}</div>
)}
```

Preserved — same as before. `text-destructive` reads correctly across all themes and remains the correct semantic for a send failure.

**Preserve exactly (ComposeBox.tsx):**
- All imports.
- `ComposeBoxProps` interface signature.
- All useState + useRef + useEffect + useCallback hooks.
- `DEBOUNCE_MS` constant.
- `dirtyBodyRef` + `latestBodyRef` + `debounceTimerRef` + `textareaRef` refs.
- `tmuxSessionKey` normalization.
- `clearDebounce`, `flushDirty`, `scheduleAutosave` useCallbacks.
- Load-on-mount effect with cancelled flag + `getComposeDraft` + `flushComposeDraftKeepalive` cleanup.
- pagehide + visibilitychange keepalive effect.
- 10s retry setInterval effect.
- Auto-focus on mount effect.
- Auto-grow rows formula.
- `clearAfterSend`.
- `handleTextChange` + `handleBlur` + `handleSend` + `handleResetSend` + `handleQuickSend` + `handleKeyDown` handlers — every line byte-preserved.
- `sendDisabled` computed value.
- All ARIA attributes on buttons.
- All button `onClick` handlers.
- All Lucide icon imports + JSX (RotateCcw, ThumbsUp, Send).
- All conditional gates (`typeof contextPct === "number"`, `errorMessage &&`).
- The comment blocks at the top of the file documenting Design Decisions.

**Verify build:**
```
npm run build 2>&1 | tail -10
```
Expected: `built in Xs`, no TypeScript errors.
  </action>
  <verify>
    <automated>grep -q 'radial-gradient' src/ui/features/pretty-view/PrettyView.tsx &amp;&amp; grep -q 'text-\[var(--color-pv-fg)\]' src/ui/features/pretty-view/PrettyView.tsx &amp;&amp; ! grep -q '"bg-background"' src/ui/features/pretty-view/PrettyView.tsx &amp;&amp; grep -q 'FRAGILITY WARNING' src/ui/features/pretty-view/PrettyView.tsx &amp;&amp; grep -q 'data-pv-root' src/ui/features/pretty-view/PrettyView.tsx &amp;&amp; grep -q '<IdentityBadge identityKey={pvIdentityKey} size="lg" />' src/ui/features/pretty-view/PrettyView.tsx &amp;&amp; ! grep -q 'backdropFilter' src/ui/features/pretty-view/PrettyView.tsx &amp;&amp; ! grep -q '"transform"' src/ui/features/pretty-view/PrettyView.tsx &amp;&amp; grep -q 'linear-gradient(180deg,rgba(0,0,0,0.15)' src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; ! grep -q 'border-t border-border bg-background' src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; grep -q 'focus:border-\[hsla(var(--pv-id-hue)' src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; grep -q 'bg-\[linear-gradient(180deg,hsla(var(--pv-id-hue),75%,66%,0.85)' src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; grep -q 'hsla(0,75%,60%,1)' src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; grep -q 'contextPct' src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; grep -q 'handleResetSend' src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; grep -q 'handleQuickSend' src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; grep -q 'flushComposeDraftKeepalive' src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; npm run build 2>&amp;1 | tail -5 | grep -q 'built in'</automated>
  </verify>
  <acceptance_criteria>
    - PrettyView root div has: `text-[var(--color-pv-fg)]`, `font-[Inter...]`, `relative overflow-hidden`; incoming `bg-background` REMOVED; inline `backgroundImage` uses `radial-gradient` × 2 + `linear-gradient(160deg, var(--color-pv-base), var(--color-pv-base-mid), var(--color-pv-base-end))`; NO `backdropFilter`, `transform`, `willChange`, `filter`, or `perspective` in the root's style prop.
    - Plan 04-01's `data-pv-root`, `--pv-id-hue`, and `<IdentityBadge size="lg">` mount all preserved.
    - Plan 03-02's SessionHoldingBanner sticky wrapper preserved structurally (sticky top-0 z-10 -mx-4 -mt-3 mb-3 px-4 py-2 backdrop-blur-sm) with only bg/border classes swapped to Phase 4 warmer variants; FRAGILITY WARNING comment PRESERVED.
    - Connecting / inactive / error / inline-error strip renders use Phase 4 fg colors.
    - ComposeBox outer container drops `border-t border-border bg-background`, adopts `bg-[linear-gradient(180deg,rgba(0,0,0,0.15),rgba(0,0,0,0.3))]` + `shadow-[inset_0_2px_10px_rgba(0,0,0,0.35)]`.
    - Context bar track and both non-alarming fill branches use `hsla(var(--pv-id-hue),...)`; ≥80 branch uses `hsla(0,75%,...)` (red — semantic HARD LOCK).
    - Textarea className carries the Phase 4 outline (`bg-white/[0.03]`, `border-[rgba(255,240,215,0.09)]`, `rounded-[10px]`) + focus ring via identity hue + `focus-visible:ring-0` (shadcn defaults disabled).
    - RotateCcw + ThumbsUp buttons adopt mock's `.pv-icon-btn` treatment; Send button adopts saturated identity-hue glow.
    - All existing ComposeBox behavior byte-preserved: refs, hooks, useCallbacks, effects, event handlers, `handleSend`/`handleResetSend`/`handleQuickSend`/`handleKeyDown`, ARIA, disabled logic, Lucide icons, comment blocks at file top.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>The Phase 4 reskin is complete end-to-end. Pretty view now reads as a warm-neutral atmospheric space with physical bubbles, a quiet ambient panels shelf, a calm compose surround, a lightest-touch textarea outline that lights up with the identity hue on focus, and a saturated identity-hue send button as the ONE grab-point. Every existing pretty-view feature — chat rendering, ComposeBox all three send paths, HarnessTasksPanel, BackgroundedAgentsPanel, BackgroundedShellsPanel, WipBubble, PlanPendingBubble, SessionHoldingBanner + all Plan 03-02 session-changeover handlers, IdentityBadge lg — works byte-identically. Terminal panes are visually unchanged. Plan 04-03 handles the final build verification + Nyquist UAT prep + AGENTS.md documentation notes.</done>
</task>

</tasks>

<verification>
- All 9 files touched (ChatMessage, WipBubble, PlanPendingBubble, SessionHoldingBanner, HarnessTasksPanel, BackgroundedAgentsPanel, BackgroundedShellsPanel, PrettyView, ComposeBox) reskinned per plan.
- Every file's behavior code (props signatures, hooks, refs, effects, event handlers, JSX conditionals, computed values) byte-preserved.
- Plan 04-01's Plan-specific changes preserved: `data-pv-root`, `--pv-id-hue`, `<IdentityBadge size="lg">` mount in PrettyView.
- Plan 03-02's SessionHoldingBanner sticky wrapper preserved structurally with only bg/border warmed to Phase 4 palette; FRAGILITY WARNING comment intact.
- PrettyView root has NO `backdrop-filter`, `filter`, `transform`, `will-change`, or `perspective` (would break sticky banner per Plan 03-02 FRAGILITY WARNING).
- No spinners in any of the three panels (motion channel HARD LOCKED to WipBubble per patch #53 discipline).
- Send button (VISUAL-08 ONE grab-point) is the ONLY compose element with saturated identity-hue glow; reset + go-ahead icon buttons take the quieter treatment.
- Context bar's ≥80% branch is red (semantic HARD LOCK — alarming beats identity-hue for approaching-full).
- Terminal.tsx UNTOUCHED (verify with `git diff --name-only src/ui/features/terminal/Terminal.tsx` returns nothing).
- `npm run build` succeeds.
</verification>

<success_criteria>
- VISUAL-01 (warm-neutral atmospheric base with radial-gradient depth cues) — closed.
- VISUAL-02 (bubbles as raised physical objects with multi-layer shadow + backdrop-blur) — closed.
- VISUAL-03 (identity-hue color chain: user bubble + ctxbar + send button + focus ring + panel tags) — closed. Falls back to hue 35 (neutral amber) for identities with no colorHue or panes with no identity.
- VISUAL-05 (ambient panels shelf as one quiet floating card treatment) — closed.
- VISUAL-06 (compose surround is quiet, no card, blends into atmosphere) — closed.
- VISUAL-07 (textarea lightest-touch outline + identity-hue focus ring) — closed.
- VISUAL-08 (send button saturated identity-hue glow — ONE grab-point) — closed.
- VISUAL-09 (all existing pretty-view functionality preserved — CSS-only) — verified via byte-preservation of every event handler / hook / effect / prop / state / computed value across all 9 files.
- `npm run build` clean.
- Zero visual change to Terminal / RDP / VNC / file manager / dashboard / sidebar / tab bar / AppRail (Terminal.tsx untouched; IdentityBadge md branch byte-identical to patch #17/#38).
- Zero regression to any pretty-view behavior (chat rendering, all three ComposeBox send paths, all three ambient panels, WipBubble motion, PlanPendingBubble static, SessionHoldingBanner + session-changeover handlers from Plan 03-02, empty state, error states, keyboard chords, message-queue drawer coexistence).
- NOT deployed. Plan 04-03 handles build verification + Nyquist UAT prep + AGENTS.md documentation for the eventual deploy at Ashley's separate green-light per fleet standing rule.
</success_criteria>

<output>
Create `.planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-02-SUMMARY.md` when done. Note in the summary:

- Confirmation that all 9 files reskinned (list them with a one-line change summary per file).
- Confirmation that Terminal.tsx is UNTOUCHED (via `git diff --name-only src/ui/features/terminal/Terminal.tsx` returning nothing).
- Confirmation that no `backdrop-filter`, `filter`, `transform`, `will-change`, `perspective` appears in PrettyView's ROOT div's style (grep for these ancestor-hostile properties in the outermost div).
- Confirmation that Plan 04-01's `data-pv-root`, `--pv-id-hue`, and `<IdentityBadge size="lg">` mount are preserved.
- Confirmation that Plan 03-02's SessionHoldingBanner sticky wrapper structure + FRAGILITY WARNING comment are preserved.
- Any surprises from the shadcn Textarea integration (if the wrapper overrode Phase 4 focus styling, note the workaround — most likely `focus-visible:ring-0` sufficed).
- Any surprises from Tailwind arbitrary-value class generation (e.g. if a class like `bg-[linear-gradient(160deg,hsla(var(--pv-id-hue),60%,45%,0.55),hsla(var(--pv-id-hue),55%,30%,0.6))]` didn't survive the JIT and needed to be moved to an inline `style` prop, note the swap).
- Confirmation that the ≥80% context-bar branch is red (`hsla(0,75%,...)`), NOT identity-hue (semantic HARD LOCK).
- Confirmation that motion is ONLY in WipBubble Loader2 + IdentityBadge lg breathing (with prefers-reduced-motion + motion-reduce:animate-none respected respectively); no other component has motion.
- Confirmation that `npm run build` succeeds.
- Any deferred polish (e.g. if the mock's `.pv-identity` border-radius 12px vs 36px pill readability warranted a change from Plan 04-01's badge shape, note the empirical decision).
- Reminder that Plan 04-03 is the next step and handles UAT prep + AGENTS.md doc writeup + deploy readiness.
</output>
</content>
</invoke>