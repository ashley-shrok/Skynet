---
phase: 02-toggle-compose-and-native-web-ergonomics
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/ui/sidebar/UserProfilePanel.tsx
  - src/ui/locales/en.json
autonomous: false
requirements:
  - RENDER-04
  - RENDER-05
  - TOGGLE-01
  - TOGGLE-02
  - TOGGLE-03
  - COMPOSE-01
  - COMPOSE-02
  - COMPOSE-03
  - COMPOSE-04
  - COMPOSE-05
must_haves:
  truths:
    - "The Skynet User Profile settings panel exposes a fourth keyboard-chord toggle (`keyboardTogglePrettyModeEnabled`), matching the three existing SettingRow toggles for Tab Switching, Close Tab, and Message Queue chords (D-104)"
    - "en.json carries the `keyboardTogglePrettyMode` + `keyboardTogglePrettyModeDesc` i18n key pair, matching the three existing `keyboard*`/`keyboard*Desc` key pairs (D-105)"
    - "Native browser text selection works in the compose textarea AND across the conversation bubbles — no highlight-then-Enter dance, no user-select:none regression (RENDER-04, verified in checkpoint)"
    - "Clicking anywhere in the pretty view focuses it without accidentally starting a selection — the compose textarea receives focus on click, the conversation area accepts click without initiating a phantom selection (RENDER-05, verified in checkpoint)"
    - "All Phase 1 features (backend session tail, WS bridge on 30011, PrettyView, ChatMessage, auto-scroll, FALLBACK-01/02) still work end-to-end; no regressions to session-tint (patch #26), IdentityBadge (patch #17), message queue drawer (patches #39-41), tmux-target-session (patch #33), or any RDP/VNC/tab-switch/close/session-list surface (verified in checkpoint)"
  artifacts:
    - path: "src/ui/sidebar/UserProfilePanel.tsx"
      provides: "Fourth keyboard-chord SettingRow with FakeSwitch, localStorage-backed state, and window-event dispatch"
      contains: "keyboardTogglePrettyModeEnabled"
    - path: "src/ui/locales/en.json"
      provides: "i18n keys under newUi.sidebar.userProfile.keyboardTogglePrettyMode and keyboardTogglePrettyModeDesc"
      contains: "keyboardTogglePrettyMode"
  key_links:
    - from: "UserProfilePanel FakeSwitch"
      to: "use-keyboard-toggle-pretty-mode hook enabledRef"
      via: "localStorage.setItem + window.dispatchEvent(new Event('keyboardTogglePrettyModeEnabledChanged'))"
      pattern: "keyboardTogglePrettyModeEnabledChanged"
    - from: "Deploy checkpoint"
      to: "Production browser at term.gigaashley.click"
      via: "build-skynet.sh + docker compose up -d --force-recreate skynet, behind the 15-min deadman rollback"
      pattern: "term.gigaashley.click"
---

<objective>
Land the two small remaining code deltas (settings toggle + i18n keys) and run the mandatory end-to-end deploy verification for the whole Phase 2 slice.

Purpose: Give Ashley runtime control over the new chord (disable it if her OS bindings collide), i18n plumbing so the chord row reads correctly in the settings panel, and — critically — a rigorous UAT that verifies EVERY Phase 2 requirement (both the code-shipped ones from Plans 01–02 and the two verify-only ones RENDER-04/RENDER-05) in production behind Skynet's mandatory deadman rollback timer. Also verify no regression to any Phase 1 or prior-patch feature the compose + toggle refactor could have disturbed.

The plan is deliberately checkpoint-heavy: two small autonomous code tasks (settings + i18n) followed by one comprehensive deploy-and-UAT checkpoint. This mirrors Plan 01-05 of Phase 1 (the deploy smoke plan) and is where the whole Phase 2 slice becomes real.

Output: A production deploy of Phase 2 with human confirmation that the chord works, the mode flip preserves the drawer, native selection and click-focus work as expected, sends land only when the tail confirms them, large pastes stay readable end-to-end, and no other Skynet feature is regressed.

Deploy discipline reminder (STANDING CONSTRAINT, not a task): the mandatory 15-min deadman rollback is armed before every deploy per Ashley 2026-07-03. The pre-authorized code work from Plans 01–02 does NOT authorize the deploy; per Ashley 2026-07-12, the executor MUST get explicit deploy authorization before running `sudo bash /opt/skynet/skynet-patches/build-skynet.sh` + `docker compose up -d --force-recreate skynet`. The deploy step lives inside Task 3's checkpoint and includes the deadman arm/disarm sequence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-toggle-compose-and-native-web-ergonomics/02-CONTEXT.md
@.planning/shapes/shape-pretty-session-view.md
@.planning/phases/02-toggle-compose-and-native-web-ergonomics/02-01-SUMMARY.md
@.planning/phases/02-toggle-compose-and-native-web-ergonomics/02-02-SUMMARY.md
@.planning/phases/01-live-session-stream-to-browser-read-only-pretty-view/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add UserProfilePanel SettingRow + FakeSwitch for the new chord</name>
  <read_first>
    - src/ui/sidebar/UserProfilePanel.tsx — read the neighborhood of lines 493–510 (the three `useState` blocks initializing keyboardTabNavEnabled, keyboardCloseTabEnabled, keyboardMessageQueueEnabled from localStorage; the new fourth state block goes here) and lines 1034–1083 (the three existing SettingRow blocks for those chord toggles — the new fourth SettingRow goes immediately after keyboardMessageQueue at line 1083 and before the `reopenTabsOnLogin` SettingRow at line 1084).
  </read_first>
  <files>src/ui/sidebar/UserProfilePanel.tsx</files>
  <action>
Two localized edits in `src/ui/sidebar/UserProfilePanel.tsx`, following the existing three-chord SettingRow pattern verbatim.

**Edit 1 — Add the state initializer** (around line 501, after the `keyboardMessageQueueEnabled` state block):

Locate the block:

    const [keyboardMessageQueueEnabled, setKeyboardMessageQueueEnabled] =
      useState(() => {
        const v = localStorage.getItem("keyboardMessageQueueEnabled");
        return v === null ? true : v === "true";
      });

Immediately after it, add the fourth state block:

    const [keyboardTogglePrettyModeEnabled, setKeyboardTogglePrettyModeEnabled] =
      useState(() => {
        const v = localStorage.getItem("keyboardTogglePrettyModeEnabled");
        return v === null ? true : v === "true";
      });

Default value `true` when the key is absent — matches all three siblings and matches the hook's own default (`enabledRef.current = v === null ? true : v === "true"`).

**Edit 2 — Add the fourth SettingRow** (after line 1083's `</SettingRow>` closing the keyboardMessageQueue row, before line 1084's `<SettingRow` opening the reopenTabsOnLogin row):

    <SettingRow
      label={t("newUi.sidebar.userProfile.keyboardTogglePrettyMode")}
      description={t(
        "newUi.sidebar.userProfile.keyboardTogglePrettyModeDesc",
      )}
    >
      <FakeSwitch
        checked={keyboardTogglePrettyModeEnabled}
        onChange={(v) => {
          setKeyboardTogglePrettyModeEnabled(v);
          localStorage.setItem(
            "keyboardTogglePrettyModeEnabled",
            v.toString(),
          );
          window.dispatchEvent(
            new Event("keyboardTogglePrettyModeEnabledChanged"),
          );
        }}
      />
    </SettingRow>

Shape is byte-for-byte parallel to the keyboardMessageQueue SettingRow at lines 1064–1083 — same t() calls, same FakeSwitch pattern, same localStorage+dispatchEvent sequence.

Do NOT reorder any existing SettingRow. Do NOT add any additional switches, toggles, or fields. The four chord SettingRows now stack in a group; this pattern matches the fork convention that all keyboard-chord toggles live adjacent.

Reminder for the executor: the `<key>EnabledChanged` window event name MUST match the hook's window.addEventListener call in `src/ui/hooks/use-keyboard-toggle-pretty-mode.ts`. If Plan 01 named the event differently (which the plan spec says it should not), fix it in ONE place — the hook — not here.
  </action>
  <verify>
    <automated>grep -q "keyboardTogglePrettyModeEnabled" src/ui/sidebar/UserProfilePanel.tsx &amp;&amp; grep -q "keyboardTogglePrettyModeEnabledChanged" src/ui/sidebar/UserProfilePanel.tsx &amp;&amp; grep -q "newUi.sidebar.userProfile.keyboardTogglePrettyMode" src/ui/sidebar/UserProfilePanel.tsx &amp;&amp; grep -q "newUi.sidebar.userProfile.keyboardTogglePrettyModeDesc" src/ui/sidebar/UserProfilePanel.tsx &amp;&amp; test $(grep -c "SettingRow" src/ui/sidebar/UserProfilePanel.tsx) -ge 8 &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -v -E "(node_modules|\.d\.ts)" | grep "UserProfilePanel\.tsx" | grep -i error; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - A `useState` for `keyboardTogglePrettyModeEnabled` exists, defaulting to true when localStorage is absent.
    - A new SettingRow with label + description i18n keys and a FakeSwitch bound to the state exists.
    - The FakeSwitch onChange calls localStorage.setItem, setKeyboardTogglePrettyModeEnabled, AND window.dispatchEvent with the exact event name `keyboardTogglePrettyModeEnabledChanged`.
    - The new SettingRow sits immediately after the existing keyboardMessageQueue SettingRow.
    - No pre-existing SettingRow is modified or reordered.
    - TypeScript compiles cleanly.
  </acceptance_criteria>
  <done>UserProfilePanel exposes the fourth chord toggle; Ashley can disable Ctrl+Shift+O at runtime if it collides with an OS binding.</done>
</task>

<task type="auto">
  <name>Task 2: Add the i18n key pair to en.json</name>
  <read_first>
    - src/ui/locales/en.json — read lines 2085–2095 to see the three existing keyboard-chord i18n key pairs (`keyboardTabNav`/`keyboardTabNavDesc`, `keyboardCloseTab`/`keyboardCloseTabDesc`, `keyboardMessageQueue`/`keyboardMessageQueueDesc`). The new fourth pair goes immediately after keyboardMessageQueueDesc.
  </read_first>
  <files>src/ui/locales/en.json</files>
  <action>
Add one new i18n key pair to `src/ui/locales/en.json`, immediately after the `keyboardMessageQueueDesc` line at ~line 2093.

Locate:

    "keyboardMessageQueue": "Message Queue Shortcut",
    "keyboardMessageQueueDesc": "Ctrl+Shift+; to show/hide the message queue drawer",

Insert immediately after:

    "keyboardTogglePrettyMode": "Pretty Mode Toggle Shortcut",
    "keyboardTogglePrettyModeDesc": "Ctrl+Shift+O to flip the top pane between tmux and pretty modes",

Match the JSON indentation and trailing-comma pattern of the surrounding keys exactly. If the executor's file has different indentation (this file is user-edited over time), match whatever the file uses locally — the important part is that the JSON stays valid and both new keys land under the same object as the three keyboard-chord siblings.

CRITICAL:
- Ensure trailing comma on the line AFTER the new pair if that following line is another JSON key (it is — `keyboardMessageQueueDesc` is currently followed by other newUi.sidebar.userProfile keys).
- Ensure the new pair uses `"keyboardTogglePrettyMode"` (matches the SettingRow's t() call keys) — a typo here silently shows the raw key string in the UI.
- Do NOT touch any other locale file (`translated/id_ID.json`, etc.). The fork convention (patches #37, #39 also followed this) is to let translated files fall back to English. Ashley does not use translated locales.
  </action>
  <verify>
    <automated>grep -q '"keyboardTogglePrettyMode":' src/ui/locales/en.json &amp;&amp; grep -q '"keyboardTogglePrettyModeDesc":' src/ui/locales/en.json &amp;&amp; python3 -c "import json; json.load(open('src/ui/locales/en.json'))" 2>&amp;1 | grep -qv "error"</automated>
  </verify>
  <acceptance_criteria>
    - The two new JSON keys exist under `newUi.sidebar.userProfile`.
    - The file is valid JSON (parses cleanly with `python3 -c "import json; json.load(open(...))"`).
    - The three existing keyboard-chord i18n key pairs are unchanged.
    - No other locale file is modified.
  </acceptance_criteria>
  <done>The settings panel row renders the correct human-readable label and description; the runtime toggle is fully wired end-to-end.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Deploy Phase 2 behind mandatory deadman + full UAT covering every requirement</name>
  <what-built>
    Phase 2 in aggregate: the keyboard-chord mode toggle (Ctrl+Shift+O) that flips the top pane between tmux xterm and pretty mode (Plan 01), the compose box in pretty mode with Enter-sends / Shift-Enter-newlines and split-send WebSocket delivery matching the queue drawer's proven pattern (Plan 02), the removal of the Phase 1 `#pretty=1` URL-fragment scaffolding (Plan 01), the runtime chord-disable toggle in User Profile settings + i18n keys (Tasks 1–2 above).

    Together these deliver every Phase 2 requirement — TOGGLE-01, TOGGLE-02, TOGGLE-03, RENDER-04 (verify-only), RENDER-05 (verify-only), COMPOSE-01, COMPOSE-02, COMPOSE-03, COMPOSE-04, COMPOSE-05.
  </what-built>
  <how-to-verify>
    This checkpoint has TWO gates: (a) explicit deploy authorization from Ashley (per D-116 Ashley 2026-07-12 rule: pre-authorized code work does NOT authorize the deploy), and (b) the six-part UAT below.

    **Gate A — Get explicit deploy authorization.**

    Before running any build or deploy command, PAUSE and ask Ashley:

        Phase 2 code is ready to deploy: keyboard chord Ctrl+Shift+O for mode
        flip, compose box in pretty mode, and the Phase 1 URL-fragment
        cleanup. This is a production Skynet deploy behind the 15-min
        deadman rollback. Ready to build and deploy?

    Wait for explicit "yes" / "deploy" / "go". Do NOT proceed on silence, "sounds good", or ambiguous replies. This is Ashley's rule.

    **Deploy sequence (once authorized):**

    1. **Arm the deadman FIRST** per CLAUDE.md's "Auto-revert helper for deploys" section:

           nohup sudo -b bash -c 'sleep 900; [ ! -f /tmp/skynet-keep-patched ] && bash /opt/skynet/.tmp-revert.sh' > /tmp/skynet-revert-bg.log 2>&1

       Do NOT skip. Ashley's rule is explicit: NO EXCEPTIONS on any deploy.

    2. **Build the patched image:**

           sudo bash /opt/skynet/skynet-patches/build-skynet.sh

       If build fails, `git checkout -- .` to reset any accidental worktree changes, investigate, retry. Report the error to Ashley before attempting a fix.

    3. **Deploy:**

           cd /opt/skynet && sudo docker compose up -d --force-recreate skynet

    4. **Sanity check the container is healthy:**

           sudo docker logs --tail 50 skynet 2>&1 | grep -iE "error|listening"

       Expect: "listening on 30011" (claude-session WS from Phase 1) and no fresh errors related to Terminal.tsx, PrettyView, ComposeBox, or AppShell.

    **Gate B — UAT covering all Phase 2 requirements.**

    Open a Chrome tab to https://term.gigaashley.click, log in, then run through EVERY item below. Report pass/fail on each with a one-line note; screenshots or console output for anything unexpected.

    **UAT-1 (TOGGLE-01): Chord flips modes.**
    - Open a terminal tab to a host running a Claude Code tmux session (thenasty is the reliable choice; any autoTmux SSH host running claude works).
    - Baseline: verify the tab shows xterm with the tmux pane content as usual.
    - Press Ctrl+Shift+O.
    - Expect: top pane instantly shows PrettyView (chat bubbles) instead of xterm. No page reload. No URL change.
    - Press Ctrl+Shift+O again.
    - Expect: top pane returns to xterm with the terminal still connected (SSH connection did not drop; tmux still attached; scrollback intact).

    **UAT-2 (TOGGLE-02): No memory of mode choice across tab opens.**
    - With one tab in pretty mode, close that tab.
    - Reopen the SAME session (via the session-list dashboard or command palette).
    - Expect: the newly-opened tab is in tmux mode. Not pretty.
    - Also verify: opening a NEW browser tab or reloading the whole page with existing tabs (`reopenTabsOnLogin` OFF for Ashley) lands every restored tab in tmux mode regardless of what mode it was in before.
    - Also verify: any bookmark or URL still containing `#pretty=1` in its fragment silently loads in tmux mode — the fragment gate is gone.

    **UAT-3 (TOGGLE-03): Queue drawer preserved across flips.**
    - In tmux mode on an active tab, press Ctrl+Shift+; to open the message queue drawer. Type text into a compose row (do NOT send) and blur the textarea (click elsewhere) so the text persists.
    - Press Ctrl+Shift+O to flip to pretty mode.
    - Expect: the queue drawer is still visible at the bottom of the tab, with the same open/closed state and the typed text still present in its row.
    - Press Ctrl+Shift+O to flip back to tmux.
    - Expect: the queue drawer is STILL open with the same typed text.
    - Also verify: the drawer's scroll position and per-row expansion state (if applicable) survives the flip.

    **UAT-4 (RENDER-04): Native browser text selection in pretty mode.**
    - Flip to pretty mode on an active Claude tab.
    - Click-and-drag your cursor across the text of one of Claude's chat replies.
    - Expect: normal browser selection highlight appears. Text is selected as expected.
    - Press Ctrl+C (or Cmd+C on macOS).
    - Expect: text lands in the OS clipboard. Paste it into another app to confirm.
    - Verify: there was NO highlight-then-Enter dance — just click, drag, Ctrl+C.
    - Also verify: text selection works INSIDE the compose textarea (browser default), and across compose + conversation regions (though cross-region selection may be limited by DOM structure; single-region is what matters).

    **UAT-5 (RENDER-05): Click-to-focus without accidental selection.**
    - Flip to pretty mode on an active tab.
    - Click ONCE on the conversation area (not the compose textarea).
    - Expect: no text is selected as a result of the click. No character was accidentally highlighted.
    - Click ONCE on the compose textarea.
    - Expect: cursor lands in the textarea; no selection artifact.

    **UAT-6 (COMPOSE-01/02/03/04/05): Full compose-and-send round trip.**
    - Flip to pretty mode on an active Claude tab.
    - Verify: the compose textarea is present at the bottom of the pretty view, directly below the conversation.
    - Type a short message: e.g., "hi from pretty view test".
    - Press Enter.
    - Expect: (a) the compose textarea CLEARS immediately (COMPOSE-04: no optimistic bubble — the message does NOT appear in the conversation yet). (b) The message appears in the conversation within ~1–2 seconds as the session-file tail bubbles it back (COMPOSE-04 confirmation).
    - Type a multi-line message using Shift-Enter to insert newlines: "line one" + Shift+Enter + "line two" + Shift+Enter + "line three".
    - Verify: the textarea shows three visible lines (Shift-Enter did NOT send prematurely — COMPOSE-02 half 2).
    - Press Enter (plain).
    - Expect: the message is sent (COMPOSE-02 half 1). Newlines are collapsed to spaces per D-50 policy; the arriving message reads "line one line two line three" in Claude's REPL and in the session file / pretty view.
    - Paste a large multi-line block into the compose textarea (200+ lines from a source file — Ashley can pick something suitable; e.g., a section of AGENTS.md or a long log).
    - Verify: the full paste is visible in the textarea (COMPOSE-05 half 1). No "[pasted N lines]" collapse in the textarea itself.
    - Press Enter to send.
    - Expect: Claude Code's Ink REPL receives the message as typed input, NOT a bracketed-paste block. Verify by observing Claude's response context — Claude sees the full readable content, not "[pasted N lines]" (COMPOSE-05 half 2). This is the payoff for D-60.
    - Then flip to tmux mode and verify the sent message is present in the tmux pane's history exactly as it landed in Claude Code (should match what pretty view showed).
    - Finally: with the WebSocket healthy, verify a send with WS disconnected surfaces an inline error and preserves the text. Reliable disconnect trigger: `sudo docker restart skynet` (which will drop the WS), then within the reconnect window try to send. Expect: inline error near compose box, typed text unchanged in textarea. Once reconnected (a few seconds later), a fresh send works.

    **UAT-7 (REGRESSION — Phase 1 features):**
    - On a tab where NO Claude Code process is running (e.g., a bash prompt on ashley-beelink), press Ctrl+Shift+O.
    - Expect: pretty view shows "no active Claude session" text; nothing else (FALLBACK-01 preserved).
    - Verify: auto-scroll behavior in pretty mode on an active Claude session — scroll to top, new messages should NOT yank you down; scroll to bottom, new messages should follow (RENDER-03 preserved).
    - Verify: an active Claude session on a fresh open flips to pretty mode and immediately shows the full session from the start (RENDER-02 preserved).

    **UAT-8 (REGRESSION — prior patches):**
    - Verify the session-tint overlay (patch #26) still shows on identity-tagged panes in both tmux AND pretty mode.
    - Verify the IdentityBadge (patch #17) still renders in the top-right corner of both tmux AND pretty mode when the session name matches a registered identity.
    - Verify the message queue drawer (patch #39-41) still opens/closes with Ctrl+Shift+;, adds/deletes/edits rows, and sends via the queue button (its own onSend was untouched by Phase 2).
    - Verify tab switching (Ctrl+Shift+[/]) still works (patch #31).
    - Verify Close Tab (Ctrl+Shift+L) still works (patch #37).
    - Verify at least one RDP tab still connects and renders (workstation-RDP or thenasty-RDP — patch #10 keep-alive, patch #23 takeover machinery, guacd wiring all untouched by Phase 2, but any AppShell / hook / Terminal change is a blast-radius risk worth spot-checking).
    - Verify at least three other terminal tabs (non-Claude, plain SSH) still connect normally.
    - Verify the session-list dashboard (patch #7) still lists sessions correctly.

    **UAT-9 (REGRESSION — chord non-collision with Ashley's OS bindings):**
    - Ashley, please confirm: pressing Ctrl+Shift+O with your live macOS/Windows/whatever OS-level binding profiles active does NOT trigger any other application, system, or accessibility shortcut. If it collides (like Ctrl+Shift+K did with speech-to-text in patch #37), we swap chords now — pick an alternate (Ctrl+Shift+P? Ctrl+Shift+U? Ctrl+Shift+.?) and I'll ship a follow-up commit before disarming the deadman.

    **If ALL of UAT-1 through UAT-9 pass**, disarm the deadman:

        sudo touch /tmp/skynet-keep-patched && sudo pkill -f 'sleep 900; \[ ! -f /tmp/skynet-keep-patched'

    Use the NARROW pkill pattern per CLAUDE.md (a bare `pkill -f "sleep 900"` matches the guacd-zombie sentinel's poll loop too).

    **If ANY UAT fails**:
    - Small fix within the deadman window (< 12 min after deploy) — fix, rebuild, redeploy, re-verify.
    - Larger fix or ambiguity — let the deadman fire (it restores `docker-compose.yml.pre-tabtitle.bak` via `/opt/skynet/.tmp-revert.sh`). Then in the calm, diagnose and plan a v2.
    - Explicit revert-now needed: `sudo bash /opt/skynet/.tmp-revert.sh`.

    Report back with:
    - Pass/fail status on each of UAT-1 through UAT-9.
    - Whether the deadman was disarmed (all green) or fired / manually reverted (any red).
    - Any surprises worth capturing in the eventual AGENTS.md patch #43 write-up (which is a bounty follow-up, not part of this plan).
  </how-to-verify>
  <resume-signal>Type "approved" (all UAT green, deadman disarmed) or describe issues (with a note on whether the deadman fired / was manually reverted / is still live so we know the current image state)</resume-signal>
  <done>Phase 2 is live in production behind mandatory deadman; every requirement is verified end-to-end including the two verify-only ones (RENDER-04, RENDER-05); no regression to Phase 1 or prior-patch features; Ashley has confirmed the chord does not collide with her OS bindings.</done>
</task>

</tasks>

<verification>
- UserProfilePanel exposes the fourth keyboard-chord SettingRow with the correct localStorage key + window event.
- en.json has both new i18n keys and remains valid JSON.
- Post-deploy: EVERY Phase 2 requirement is confirmed via UAT — TOGGLE-01, TOGGLE-02, TOGGLE-03, RENDER-04, RENDER-05, COMPOSE-01, COMPOSE-02, COMPOSE-03, COMPOSE-04, COMPOSE-05.
- Post-deploy: no regression to Phase 1 (BACKEND-01..04, RENDER-01..03, FALLBACK-01..02).
- Post-deploy: no regression to prior fork patches (session-tint #26, IdentityBadge #17, message queue #39-41, tab-switch #31, close-tab #37, session-list #7, RDP #10/#23, tmux target session #33).
- Chord does not collide with Ashley's OS bindings (patch #37 lesson).
</verification>

<success_criteria>
- All ten Phase 2 requirements verified in production behind the mandatory 15-min deadman rollback.
- The Skynet fork's blast-radius discipline (patch #43 or the eventual patch number) preserves reliable browser SSH/RDP access to Ashley's fleet.
- Runtime chord-disable toggle exists in User Profile settings; if a future OS-binding collision surfaces, Ashley can disable Ctrl+Shift+O without a code change or redeploy.
- The five phase Success Criteria from ROADMAP.md are met:
  1. Active-tab chord flips top pane; every fresh tab opens tmux with no memory.
  2. Message queue drawer stays at the bottom in the same position and behavior across flips.
  3. Native browser text selection with normal OS copy shortcut works; click-to-focus does not accidentally select.
  4. Compose box accepts typed and pasted text with full readable content; Enter sends via split-send; Shift-Enter newlines.
  5. Sent messages appear in the conversation only after the session file confirms them.
</success_criteria>

<output>
Create `.planning/phases/02-toggle-compose-and-native-web-ergonomics/02-03-SUMMARY.md` when done. Include: the actual UAT pass/fail summary for each of UAT-1 through UAT-9; whether the shipping chord was Ctrl+Shift+O or an alternate (if OS-collision forced a change during UAT-9); the deadman disarm confirmation timestamp; a follow-up note that the AGENTS.md patch #43 write-up (dense per-patch documentation matching the existing #37/#39-41 entries) is a bounty follow-up separate from this plan.
</output>
