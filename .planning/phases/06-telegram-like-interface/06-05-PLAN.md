---
phase: 06-telegram-like-interface
plan: 05
type: execute
wave: 5
depends_on:
  - 06-02
  - 06-03
  - 06-04
files_modified:
  - .planning/phases/06-telegram-like-interface/06-UAT-CHECKLIST.md
  - .planning/phases/06-telegram-like-interface/06-PATCHES-MD-ENTRY.md
  - .planning/phases/06-telegram-like-interface/06-05-BUILD-VERIFY-LOG.md
autonomous: false
requirements:
  - TG-01
  - TG-02
  - TG-03
  - TG-04
  - TG-05
  - TG-06
  - TG-07
  - TG-08
  - TG-09
  - TG-10
  - TG-11
must_haves:
  truths:
    - "npm run build completes clean (no TypeScript errors, no bundler warnings blocking) and the resulting dist/ contains the Phase 6 client-side artifacts (conversation-store, ConversationsPanel + ConversationRow, NewSessionButton + NewSessionDialog, SettingsRow, mobile-flow) AND excludes the deleted artifacts (TabBar.js, MobileBottomBar.js not present in the frontend bundle)."
    - "docker exec against the newly-built termix container confirms the removed surfaces are gone from dist (grep for TabBar / MobileBottomBar in dist/assets returns zero) and the new ones are present (grep for ConversationsPanel / mobile-flow / mv=1 handling returns ≥ 1 per token)."
    - "All load-bearing prior-patch bytes still present in dist: patch #25 (`#tab=` URL scheme handling — grep for `snapshotPendingTab` OR `consumePendingWorkspace`), patch #35 (tabNodesRef DOM-move mechanism — verify by grepping for `tabNodesRef` in dist/assets), patch #57 (compose-drafts), patch #60 (message_queue_delete_on_send), patch #100 (ssh_input_delayed_enter), patch #102 (pointer: coarse — useIsTouchDevice), patch #103-or-similar (whichever mobile-bottom-nav gate was — the gate itself is gone but the useIsTouchDevice consumer is now the mobile-flow branch)."
    - "A UAT checklist walking TG-01..11 exists for Ashley to run post-deploy; a termix-patches.md entry draft exists ready for pin-time paste following the fork's canonical multi-file-patch style (see Phase 5's 05-PATCHES-MD-ENTRY.md as reference — the fork's `~/.claude/identities/tina/termix-patches.md` patch #60 is the exemplar for load-bearing multi-file patches)."
    - "This plan produces ZERO production source-code diffs — everything is verification, artifact preparation, and the deploy checkpoint itself."
    - "The deploy checkpoint's how-to-verify section explicitly references `~/.claude/identities/tina/deploy-runbook.md` and enforces the mandatory 15-min deadman rollback (STANDING CONSTRAINT per Ashley 2026-07-03, NO EXCEPTIONS) — this is a substantial user-visible change so the deadman + narrow-pkill-disarm-on-Ashley-engagement pattern is especially important."
  artifacts:
    - path: ".planning/phases/06-telegram-like-interface/06-UAT-CHECKLIST.md"
      provides: "Nyquist UAT checklist walking every TG-01..11 requirement with expected observable behavior per viewport (desktop + mobile); happy-path first, failure-recovery + edge cases last. Blocking items marked 🚨 (regression = revert immediately); nice-to-have polish marked ✨. Explicitly includes: switch-and-back persistence check for a mounted pretty-view pane's scroll position (T-06-02-01 end-to-end proof); regression smoke for patches #25, #35, #57, #60, #100, #102 (they must all still work); mobile back-gesture + browser-back-from-list behavior; new-session flow end-to-end (button → picker → open → auto-navigate)."
      contains: "TG-01"
    - path: ".planning/phases/06-telegram-like-interface/06-PATCHES-MD-ENTRY.md"
      provides: "Draft entry for ~/.claude/identities/tina/termix-patches.md — numbered patch entry (patch # placeholder for pin time), following the fork's convention of full motivation + code paths + gotchas + rebase risk + verify-post-deploy invariants (see Phase 5's 05-PATCHES-MD-ENTRY.md, patch #60 for the canonical multi-file style)."
      contains: "Files touched"
    - path: ".planning/phases/06-telegram-like-interface/06-05-BUILD-VERIFY-LOG.md"
      provides: "Paper trail of build verification output (npm run build, dist artifact greps, load-bearing prior-patch byte greps, git diff --stat for scope-fence enforcement)."
      contains: "npm run build"
  key_links:
    - from: "Ashley's post-deploy UAT walk"
      to: "TG-01..11 pass/fail status"
      via: ".planning/phases/06-telegram-like-interface/06-UAT-CHECKLIST.md"
      pattern: "TG-"
    - from: "Deploy green-light window"
      to: "Mandatory 15-min deadman rollback"
      via: "sudo touch /tmp/termix-keep-patched && sudo pkill -f 'sleep 900; \\[ ! -f /tmp/termix-keep-patched'"
      pattern: "termix-keep-patched"
    - from: "This plan's checkpoint"
      to: "~/.claude/identities/tina/deploy-runbook.md"
      via: "explicit reference; MUST follow the runbook step-by-step"
      pattern: "deploy-runbook"
---

<objective>
Land the deploy checkpoint for Phase 6. Follows the exact pattern established by Phase 5's 05-04-PLAN.md — build verification (Task 1, autonomous), UAT + patches-md-entry artifacts (Tasks 2-3, autonomous), and a single blocking human-verify checkpoint (Task 4) that owns the mandatory 15-minute deadman deploy flow per `~/.claude/identities/tina/deploy-runbook.md`.

Purpose: Phase 6 is a substantial user-visible change — the tab strip is gone, the mobile bottom nav is gone, navigation model is a Telegram-style conversation list. This is exactly the kind of change where the mandatory deadman is most important: if it breaks Ashley's flow, she has 15 minutes to walk away and the fleet gateway continues to work.

Output: Three markdown artifacts under `.planning/phases/06-telegram-like-interface/` (UAT checklist + patches-md entry draft + build-verify log) plus the deploy checkpoint being run to green. Zero touch to `src/`, `docker/`, `package.json`, or any nginx config (Phase 6 does NOT add backend routes — it's purely a frontend chrome reshape). The nginx-location-block trap (fork's DEPLOY DISCIPLINE Nginx caveat) does NOT apply here.

Deploy discipline reminder (STANDING CONSTRAINT, not a task): the mandatory 15-min deadman rollback is armed before every deploy per Ashley 2026-07-03. Pre-authorized code work from Plans 06-01..04 does NOT authorize the deploy per Ashley 2026-07-12. The deploy step lives inside Task 4's checkpoint and includes the deadman arm/disarm sequence exactly as spelled out in deploy-runbook.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-telegram-like-interface/06-CONTEXT.md
@.planning/shapes/shape-telegram-like-interface.md
@.planning/phases/06-telegram-like-interface/06-01-SUMMARY.md
@.planning/phases/06-telegram-like-interface/06-02-SUMMARY.md
@.planning/phases/06-telegram-like-interface/06-03-SUMMARY.md
@.planning/phases/06-telegram-like-interface/06-04-SUMMARY.md
@.planning/phases/05-pretty-view-file-upload-support/05-04-PLAN.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local build tree → production dist | If the build produces an artifact that references a deleted module (dangling import), Vite either fails the build OR silently ships a broken chunk. Task 1's grep gates catch both. |
| Termix container → Ashley's browser | The deploy replaces the running container. If the deadman-armed 15-min window expires without Ashley's approval, the container reverts. This is the safety asset. |
| Documented invariants (patches-md entry) → future rebase | If the entry misdescribes what shipped, future rebase reviews will use bad ground truth. Task 3 sources from actual shipped code (git diff), not from plan text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Tampering (dangling import to deleted TabBar.tsx) | Vite build | mitigate | Task 1 runs `npx tsc --noEmit --skipLibCheck` (catches TS-level dangling imports) AND `npm run build` (catches Vite-level dangling references). Also greps dist/assets for `TabBar` and `MobileBottomBar` strings — expect zero. If either grep returns > 0, a dangling reference or dead code sneaked into the bundle; fix in whichever Plan owns the source. |
| T-06-05-02 | Denial of Service (deadman fires but rollback fails) | tmp-revert.sh script | mitigate | Not a Phase-6 concern — deploy-runbook.md and `/opt/termix/.tmp-revert.sh` are pre-existing fork infra. Task 4's how-to-verify references the runbook verbatim; no new deploy-mechanism surface in this plan. |
| T-06-05-03 | Information Disclosure (patches-md entry contains secrets) | 06-PATCHES-MD-ENTRY.md draft | mitigate | The entry describes shape + code paths + grep gates only — no credentials, no host names, no tokens. Verify at write time via `git diff` review before Task 3 commits. |
| T-06-05-04 | Regression to load-bearing prior patches | Phase-6 code interacts with #25 / #35 / #57 / #60 / #100 / #102 territory | mitigate | Task 1's grep gates verify each prior-patch marker byte is present in dist. Task 4's UAT (Task 2 output) has an explicit "regression smoke — pre-existing patches must still work" section that Ashley walks post-deploy. Both are belt-and-suspenders. |
| T-06-05-SC | Tampering | npm/pip/cargo installs | mitigate | Task 1 verifies `git diff --stat package.json package-lock.json` returns empty — Phase 6 added zero new dependencies. |

## Assets Protected
- **Fleet access continuity** — the mandatory deadman is the primary asymmetric-risk defense; if Phase 6 breaks Ashley's browser session, she has 15 min to abandon and the container reverts.
- **Load-bearing prior-patch integrity** (T-06-05-04) — belt-and-suspenders grep gates + Ashley's regression smoke walk.
- **Ground truth for future rebases** — patches-md entry sourced from actual git diff, not from plan text.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build verification — npm run build clean, Phase 6 artifacts survive Vite tree-shake, deletions actually removed, load-bearing prior-patch bytes intact</name>
  <read_first>
    - .planning/phases/05-pretty-view-file-upload-support/05-04-PLAN.md Task 1 — the template for this build-verification task. Same pattern: clean build, grep for new artifacts in dist, grep for deleted-artifact absence in dist, grep for load-bearing prior-patch markers, git diff --stat for scope-fence enforcement, write output to a paper-trail markdown.
    - src/ui/state/conversation-store.ts — for the exact export names to grep for in dist (`selectConversationDeferred`, `useConversations`, `updateOpenTabs`, `pendingSelectId` — although the last is module-scoped, its presence in a minified bundle is unreliable; grep for exported names instead).
    - src/ui/sidebar/ConversationsPanel.tsx — for the exact function name to grep (`ConversationsPanel`).
    - src/ui/lib/mobile-flow.ts — for `useMobileScreen`, `navigateToView`, `navigateToList`.
    - src/ui/lib/tab-url.ts — for `mobileView` (may be minified; grep for the string literal `"mv"` in dist which is preserved even after minification).
    - Phase 6 SUMMARYs — for any deviations from the plans that would change the grep targets.
  </read_first>
  <files>.planning/phases/06-telegram-like-interface/06-05-BUILD-VERIFY-LOG.md</files>
  <action>
Run the build + verify sequence. This task does NOT modify any files in `src/` — it runs commands and captures output for the paper trail.

**Step A — Clean build:**
```
cd /home/ubuntu/termix && npm run build 2>&1 | tail -50
```
Expected: `built in Xs` and NO error output. TypeScript errors surface bugs in Plans 06-01..04 (do NOT fix here — surface for revision).

**Step B — Verify frontend Phase 6 artifacts in dist:**
```
grep -c 'ConversationsPanel\|ConversationRow' dist/assets/*.js
grep -c 'NewSessionButton\|NewSessionDialog' dist/assets/*.js
grep -c 'selectConversationDeferred\|useConversations\|updateOpenTabs\|pinConversation' dist/assets/*.js
grep -c 'useMobileScreen\|navigateToView\|navigateToList' dist/assets/*.js
grep -c '"mv"' dist/assets/*.js
```
Expected: each of the five greps returns ≥ 1 total across all frontend chunks (Vite may mangle names; if names come back as 0, verify by grepping for characteristic body strings like `pinnedIds` or the exact regex pattern from NewSessionDialog OR by grepping the ORIGINAL identifier before Vite minification — check the `dist/assets` output size and use a source-map-aware grep as a fallback documented in the log).

**Step C — Verify deletions actually landed in dist:**
```
grep -c 'from.*TabBar' dist/assets/*.js
grep -c 'MobileBottomBar' dist/assets/*.js
```
Expected: BOTH return 0. If > 0, dead code sneaked in — investigate before deploy.

**Step D — Verify load-bearing prior-patch bytes are UNCHANGED in dist:**
```
grep -c 'snapshotPendingTab\|consumePendingWorkspace\|writeWorkspaceToUrl' dist/assets/*.js
grep -c 'tabNodesRef\|getTabNode' dist/assets/*.js  # (patch #35 — may be minified; check for characteristic string 'position: absolute' near a Map<string' or fallback to source-map inspection)
grep -c 'putComposeDraft\|flushComposeDraftKeepalive' dist/assets/*.js  # patch #57
grep -c 'message_queue_delete_on_send' dist/backend/backend/ssh/terminal.js  # patch #60
grep -c 'ssh_input_delayed_enter' dist/backend/backend/ssh/terminal.js  # patch #100
grep -c 'pointer: coarse' dist/assets/*.js  # patch #102 (useIsTouchDevice)
```
Expected: each grep returns ≥ 1. Patch #35's tabNodesRef marker is the most likely to be minified beyond recognition; use the fallback: `grep -c 'appendChild' dist/assets/*.js` and confirm the count is >= what it was pre-deploy (capture pre-deploy count if possible; else document that a subjective read of the diff is required).

**Step E — Verify no source diff creep:**
```
git diff --stat src/ui/features/pretty-view/
git diff --stat src/ui/features/terminal/Terminal.tsx
git diff --stat src/ui/features/guacamole/
git diff --stat src/backend/
git diff --stat docker/nginx.conf docker/nginx-https.conf
git diff --stat package.json package-lock.json
```
Expected: ALL zero. If any returns non-zero, a scope-fence violation happened in Plans 01-04.

**Step F — Sanity-check the deletions:**
```
ls src/ui/shell/TabBar.tsx 2>&1  # expect "No such file"
ls src/ui/shell/MobileBottomBar.tsx 2>&1  # expect "No such file"
grep -rn "from ['\"]@/shell/TabBar" src/ui 2>&1 | wc -l  # expect 0
grep -rn "from ['\"]@/shell/MobileBottomBar" src/ui 2>&1 | wc -l  # expect 0
```

Record ALL outputs in `.planning/phases/06-telegram-like-interface/06-05-BUILD-VERIFY-LOG.md`. Include a `date -u` timestamp at the top and the git SHA at the bottom (`git rev-parse HEAD`). Structure:

```
# Phase 6 Build Verify Log
Timestamp: <date -u output>
Commit: <git rev-parse HEAD output>

## Step A — Clean build
<output>

## Step B — Phase 6 artifacts in dist
<output per grep>

## Step C — Deletions confirmed
<output>

## Step D — Load-bearing prior-patch bytes intact
<output>

## Step E — No source-diff creep
<output>

## Step F — Deletion sanity
<output>

## Verdict
CLEAN / DIRTY (with rationale)
```
  </action>
  <verify>
    <automated>cd /home/ubuntu/termix && npm run build 2>&1 | tail -20 | grep -E "(error|failed|built in)"; echo "---presence---"; grep -c 'ConversationsPanel' dist/assets/*.js 2>/dev/null | awk -F: '{sum+=$2} END {print "ConversationsPanel total:", sum}'; grep -c '"mv"' dist/assets/*.js 2>/dev/null | awk -F: '{sum+=$2} END {print "mv total:", sum}'; echo "---deletions---"; ls src/ui/shell/TabBar.tsx 2>&1; ls src/ui/shell/MobileBottomBar.tsx 2>&1; grep -c 'from.*TabBar' dist/assets/*.js 2>/dev/null | awk -F: '{sum+=$2} END {print "TabBar leak:", sum}'; echo "---load-bearing---"; grep -c 'message_queue_delete_on_send' dist/backend/backend/ssh/terminal.js; grep -c 'ssh_input_delayed_enter' dist/backend/backend/ssh/terminal.js; echo "---scope-fence---"; git diff --stat src/ui/features/pretty-view/ src/ui/features/terminal/Terminal.tsx src/backend/ docker/ package.json; echo "---log-exists---"; ls -la .planning/phases/06-telegram-like-interface/06-05-BUILD-VERIFY-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` output contains `built in` and does NOT contain `error TS` or `[vite]` error markers
    - `src/ui/shell/TabBar.tsx` does NOT exist (`ls` returns "No such file")
    - `src/ui/shell/MobileBottomBar.tsx` does NOT exist
    - Zero imports of `@/shell/TabBar` or `@/shell/MobileBottomBar` in `src/ui/` (grep returns 0 for each)
    - `dist/assets/*.js` contains `ConversationsPanel` (grep ≥ 1)
    - `dist/assets/*.js` contains `"mv"` literal (grep ≥ 1) — mobile-flow URL fragment key present
    - `dist/assets/*.js` does NOT contain `MobileBottomBar` (grep returns 0)
    - `dist/backend/backend/ssh/terminal.js` still contains `message_queue_delete_on_send` (patch #60 marker, ≥ 1)
    - `dist/backend/backend/ssh/terminal.js` still contains `ssh_input_delayed_enter` (patch #100 marker, ≥ 1)
    - `dist/assets/*.js` still contains `pointer: coarse` (patch #102 marker, ≥ 1)
    - `git diff --stat src/ui/features/pretty-view/` returns empty (scope-fence)
    - `git diff --stat src/ui/features/terminal/Terminal.tsx` returns empty
    - `git diff --stat src/ui/features/guacamole/` returns empty
    - `git diff --stat src/backend/` returns empty (Phase 6 is frontend-only)
    - `git diff --stat docker/` returns empty (no nginx changes — Phase 6 doesn't add backend routes)
    - `git diff --stat package.json package-lock.json` returns empty (zero new deps)
    - `.planning/phases/06-telegram-like-interface/06-05-BUILD-VERIFY-LOG.md` exists with full command outputs, UTC timestamp, git SHA
  </acceptance_criteria>
  <done>Build is verified clean. Phase 6 artifacts landed. Deletions actually happened. Prior-patch load-bearing bytes intact. Zero unintended scope creep. Task 4 (deploy checkpoint) can proceed with confidence.</done>
</task>

<task type="auto">
  <name>Task 2: Generate 06-UAT-CHECKLIST.md — Nyquist walk of TG-01..11 + persistence smoke + regression smoke</name>
  <read_first>
    - .planning/REQUIREMENTS.md § "Telegram-like Interface (Phase 6)" (lines 89-99) — exact text of TG-01..11 quoted verbatim in the checklist so Ashley reads the exact contract.
    - .planning/phases/05-pretty-view-file-upload-support/05-UAT-CHECKLIST.md — reference for the Nyquist UAT format the fork established. Structure: Setup → happy-path grouped by requirement id → failure-recovery + edge cases → mobile-specific → regression smoke → sign-off (deadman disarm or let-fire).
    - .planning/phases/06-telegram-like-interface/06-CONTEXT.md `<success_criteria>` (lines 198-214) — the phase-level "must be TRUE from Ashley's perspective" list; each becomes a checklist section.
    - .planning/phases/06-telegram-like-interface/06-CONTEXT.md `<scope_fence>` (lines 180-196) — for the negative-space checks (things that MUST NOT be present: no tab strip, no bottom nav, no activity-indicator placeholder chrome, no toggle to bring tabs back).
    - .planning/shapes/shape-telegram-like-interface.md `## What would make it wrong` (lines 41-46) — for the "would violate the spirit" items that become UAT negative-space checks.
    - .planning/phases/06-telegram-like-interface/06-02-SUMMARY.md — for the actual persistence-test methodology chosen; the UAT step for "switch away and back preserves scroll" should mirror the same assertion Ashley can perform manually.
  </read_first>
  <files>.planning/phases/06-telegram-like-interface/06-UAT-CHECKLIST.md</files>
  <action>
Create `.planning/phases/06-telegram-like-interface/06-UAT-CHECKLIST.md`. Follow the shape below; the executor fills verbatim requirement quotes at write time.

```
# Phase 6 UAT Checklist — Telegram-like Interface

Post-deploy walk-through for Ashley. Every TG-NN requirement gets an
observable check. Groupings: desktop happy-path first, mobile flow next,
persistence + failure recovery, then negative-space scope-fence checks,
then regression smoke against pre-existing patches. Blocking items marked
🚨 (regression = revert immediately). Nice-to-have polish marked ✨.

## Setup — one time
1. Open https://term.gigaashley.click in Chrome on desktop AND on your phone.
2. Have at least 3 hosts configured in Termix (any mix of SSH/RDP is fine).
3. Have at least 2 sessions active on distinct hosts before starting (one identity-attached Claude session in a tmux window, one plain shell — this covers TG-04 unchanged internals).

## Desktop happy-path (TG-01, TG-02, TG-03, TG-08, TG-09, TG-10, TG-11)
- [ ] 🚨 **TG-11 tab strip gone**: no tab strip visible along the top of the main content area. Nowhere in the UI is there a chrome element that switches between multiple simultaneous tabs. There is NO user-facing toggle to restore the tab view.
- [ ] 🚨 **TG-01 list layout**: sidebar shows a flat scrollable list. Sessions are grouped visually by host with separators. Order below any pinned items matches the existing sidebar host-tree order (RDP hosts fall out at the position they occupy in the host tree — no new sort rule).
- [ ] 🚨 **TG-01 session-end vanishes**: kill a tmux session on the receiving box (`tmux kill-session -t <name>`). Refresh the list (or wait a few seconds for the poll). Verify: the row for that session vanishes from the list. No tombstone, no "recently closed" section, no re-open gesture.
- [ ] 🚨 **TG-02 pin float**: right-click (or use whichever pin affordance the plan wound up shipping — planner discretion per 06-01) on a session row to pin it. Verify: the row floats to the top of the list ABOVE the host-grouped rows.
- [ ] 🚨 **TG-02 unpin drops back**: unpin the same session. Verify: it returns to its host group in the original host-tree position (NOT appended at the end).
- [ ] 🚨 **TG-02 pin is per-session**: pin session A on hostX. Verify: session B on hostX is NOT also pinned (pinning is per-row, not per-host).
- [ ] 🚨 **TG-02 session-end clears pin**: pin a session, then kill it. Verify: the pin is gone along with the row (no orphan pin state, no ghost row).
- [ ] 🚨 **TG-03 single view + no side-by-side**: only ONE conversation is visible in the main area at a time. No tab strip, no split-view indicator on multiple rows (except for split-screen mode which is a separate SplitScreenPanel feature — clicking a row when NOT in split-screen mode swaps the visible conversation, does not add a second).
- [ ] 🚨 **TG-08 desktop sidebar collapse preserved**: collapse the sidebar (chevron-left in the sidebar header). Verify: the sidebar collapses to a thin clickable strip; clicking the strip re-expands it. Refresh the page; the collapse state is REMEMBERED (persisted preference).
- [ ] 🚨 **TG-09 visible new-session button**: at the top of the conversation list (above any pinned items), a VISIBLE button labeled "New session" (or similar) with a Plus icon.
- [ ] 🚨 **TG-09 host picker flow**: click the new-session button. A modal opens with a filterable host list. Pick a host. Optionally type a session name (leave blank first to test auto-fill). Click Open. Verify: (a) new tab appears in the list; (b) the new conversation view is displayed in the main area (auto-navigate); (c) if you did NOT type a name, the session gets a default label (tmux window title auto-fills once the session materializes).
- [ ] 🚨 **TG-09 session-name validation**: click new-session, pick a host, type "bad;name`with backticks". Verify: the Open button is disabled AND/OR the input shows a validation error.
- [ ] 🚨 **TG-10 desktop gear icon**: locate the small gear icon in the sidebar header (or the ConversationsPanel header — whichever position was chosen). Click it. A dropdown menu offers routes to: Host Manager, Credentials, User Profile, Admin (if admin), plus the More menu items from the deleted bottom nav (Quick Connect, SSH Tools, Snippets, History, Split Screen).
- [ ] 🚨 **TG-04 unchanged internals — identity Claude session**: click a row whose session is an identity-attached Claude Code session. Verify: the pretty view opens (same appearance and behavior as before Phase 6 — chat bubbles, WipBubble, PlanPendingBubble, compose box with 3D orb WIP indicator, ambient panels shelf, everything preserved end-to-end).
- [ ] 🚨 **TG-04 unchanged internals — plain SSH**: click a row for a plain shell session. Verify: the terminal opens (unchanged xterm.js rendering, message queue drawer at the bottom, keyboard chords preserved).
- [ ] 🚨 **TG-04 unchanged internals — RDP host**: open an RDP session. Verify: the remote desktop canvas renders normally; guacd behavior unchanged.

## Persistence contract (TG-05) — load-bearing correctness
- [ ] 🚨 **TG-05 instant switch — no reconnect**: open conversation A (identity Claude session). Wait for the pretty-view to fully load messages. Scroll UP in the message list to some earlier position. Click conversation B in the list. Then click conversation A again. Verify: (a) the switch back to A is INSTANTANEOUS (no loading indicator, no reconnect message); (b) A's scroll position is preserved exactly where you left it; (c) no session-changeover holding banner appears (WebSocket was not torn down).
- [ ] 🚨 **TG-05 terminal buffer preserved**: open a plain terminal (conversation C). Type something like `ls -la /tmp | head -20`. Note the last visible line. Switch to conversation B, then back to C. Verify: the terminal buffer is intact — the `ls` output is still there, cursor position preserved, no reconnect flash.
- [ ] 🚨 **TG-05 refresh resets everything**: hit Ctrl+Shift+R (hard refresh). Verify: all conversations reset — the list re-populates from the URL fragment restore, but any transient scroll positions / composed drafts survive only per each conversation's own persistence (composebox drafts persist per patch #57; message list re-loads from the session file). Persistence is page-load scoped ONLY.

## Mobile flow (TG-06, TG-07) — requires touch device
Ashley: this section requires a touch device (phone / tablet). Verify on your phone.

- [ ] 🚨 **TG-06 list vs view — two distinct screens**: open Termix on phone. Verify: the initial view is the conversation list, occupying the whole viewport. Tap a conversation. Verify: the list is fully replaced by the conversation view (list NOT visible during view). A back button in the top-left of the view returns to the list, fully replacing the view.
- [ ] 🚨 **TG-06 browser back gesture**: from the conversation view, hit the browser back button (or use the phone's system back gesture). Verify: returns to the list. From the list, hit browser back again. Verify: leaves Termix (navigates back in browser history to whatever page was before).
- [ ] 🚨 **TG-06 URL survives Chrome window-restore**: on the phone, open a specific conversation view. Note the URL fragment (should contain `mv=1` alongside `tab=`). Close the Chrome tab. Reopen via Ctrl+Shift+T (Chrome tab-restore) or from the tab-history menu. Verify: the URL survives and Termix reopens ON THE VIEW SCREEN for that specific conversation, not on the list. (This is patch #25's lesson extended to mobile — if it fails, the mv=1 fragment did not survive; investigate before pinning.)
- [ ] 🚨 **TG-07 bottom nav deleted**: on the mobile viewport, verify the bottom navigation bar is GONE. No bottom-of-screen strip with icons. The full viewport height is either list (with SettingsRow somewhere below the conversation rows) or view (with back button + conversation content).
- [ ] 🚨 **TG-07 destinations reachable via SettingsRow**: on the mobile list screen, scroll to find the SettingsRow (positioned somewhere that does NOT compete with pinned or active rows for attention — bottom of the list per Plan 06-03). Tap it. Verify: a menu offers all the destinations the bottom nav used to reach (Host Manager, Credentials, Quick Connect, SSH Tools, Snippets, History, Split Screen, User Profile, Admin).
- [ ] 🚨 **Mobile new-session button**: verify the new-session button is visible AND tappable on mobile — same top-of-list position as desktop.
- [ ] 🚨 **Mobile new-session auto-navigates to view**: tap new-session, complete the picker, verify: the new session opens AND the mobile flow auto-navigates to the view screen (list is replaced by the new conversation's view). Not "created on the list; tap to enter" — auto-navigate is TG-09 + Plan 06-04 explicit behavior.
- [ ] 🚨 **Mobile — stranded-user defense (T-06-03-06)**: open a conversation on mobile view. Kill that tmux session on the receiving box. Refresh the list on the phone or wait for the store's session-end coercion. Verify: mobile automatically returns to the list screen (not stuck on an empty view).

## Negative-space (scope-fence) checks
Ashley MUST verify that certain things are NOT present. These catch scope creep.

- [ ] 🚨 **NO activity/unread indicators anywhere**: no dots, no badges, no numbers, no motion signaling "new activity" on any conversation row. Deferred-to-v2 per shape lock. If ANY appear, a plan violated the deferred-items list — surface for revision.
- [ ] 🚨 **NO cross-conversation search**: no global search bar anywhere in the list chrome. (Out entirely per shape.)
- [ ] 🚨 **NO folder / nested-grouping above host separators**: the list has ONE grouping (by host). No folder hierarchy on top.
- [ ] 🚨 **NO drag-to-reorder for pins**: attempting to drag a pinned row does nothing (or is refused). Pin order is simple; drag reorder is out.
- [ ] 🚨 **NO history / scrollback for ended sessions**: after a session ends, there is NO "recently closed" section, NO way to see what was said in that session.
- [ ] 🚨 **NO alternate-mode toggle**: nowhere in the UI is there a setting to restore the tab strip. Full replacement per TG-11.
- [ ] 🚨 **NO tab strip in any window state**: even when the sidebar is collapsed, even when split-screen is active, even in any RailView, the tab strip is GONE. If it appears in any state, that state's rendering path missed the TabBar deletion.

## Regression smoke — pre-existing patches must still work
- [ ] 🚨 **Patch #25 URL scheme**: from a specific conversation, note the URL fragment (should contain `tab=<type>:<host>[:<session>]`). Close the tab, use Chrome's Ctrl+Shift+T to restore. Verify: the exact same conversation opens.
- [ ] 🚨 **Patch #35 DOM node stability**: the persistence contract (TG-05 above) is the direct test of this.
- [ ] 🚨 **Patch #57 compose-drafts**: type into a pretty-view compose box, close the tab, reopen. Text is restored.
- [ ] 🚨 **Patch #60 atomic delete-on-send**: open the message queue drawer, add a message, send it. Verify: the drawer row disappears atomically.
- [ ] 🚨 **Patch #100 split-and-delay Enter**: send a message via pretty-view compose. Verify: Claude Code REPL treats the input as typed (not paste — patch #100 preserved).
- [ ] 🚨 **Patch #102 useIsTouchDevice**: on desktop, the mobile-specific UI (mobile list-vs-view, SettingsRow bottom placement, mobile back button) does NOT render. On phone, it does.
- [ ] 🚨 **Terminal / RDP / VNC / file manager / dashboard / SplitScreenPanel / AppRail / Command Palette (double-shift)** — all render + behave normally.
- [ ] 🚨 **Identity badge on pretty-view**: still visible with the Phase-4 Glass treatment (once Phase 4 lands; if Phase 4 hasn't shipped yet, patches #17/#38 baseline treatment).
- [ ] 🚨 **3D orb WIP indicator**: still renders (Phase 6 does not touch WipBubble; the orb was awaiting deploy at the time Phase 6 was planned — verify it survived).
- [ ] 🚨 **Pretty-view file upload (Phase 5)**: drag a file onto a pretty-view surface. Verify: it uploads and injects an inline chip message. Phase 5 continues to work.

## Sign-off
- [ ] All 🚨 items pass → disarm deadman (`sudo touch /tmp/termix-keep-patched && sudo pkill -f 'sleep 900; \[ ! -f /tmp/termix-keep-patched'`)
- [ ] Any 🚨 item fails → let the deadman fire OR run `sudo bash /opt/termix/.tmp-revert.sh` immediately

Post-sign-off:
- Pin the patch: number it (next available in termix-patches.md), paste the entry from `06-PATCHES-MD-ENTRY.md`, commit the pin
- Close the bounty at `~/.claude/identities/tina/bounties/telegram-like-interface/` via `/close telegram-like-interface`
```

Fill in the exact requirement text for each TG-NN by copying verbatim from `.planning/REQUIREMENTS.md` lines 89-99 (do not paraphrase — Ashley walks the exact contract). The above template already shows the structure; the executor fills in the verbatim contract quotes at write time.
  </action>
  <verify>
    <automated>cd /home/ubuntu/termix && test -f .planning/phases/06-telegram-like-interface/06-UAT-CHECKLIST.md && for req in TG-01 TG-02 TG-03 TG-04 TG-05 TG-06 TG-07 TG-08 TG-09 TG-10 TG-11; do echo "$req:" $(grep -c "$req" .planning/phases/06-telegram-like-interface/06-UAT-CHECKLIST.md); done; echo "---blocking-items---"; grep -c '🚨' .planning/phases/06-telegram-like-interface/06-UAT-CHECKLIST.md; echo "---prior-patches---"; grep -c 'patch #25\|patch #35\|patch #57\|patch #60\|patch #100\|patch #102' .planning/phases/06-telegram-like-interface/06-UAT-CHECKLIST.md; echo "---deadman-disarm---"; grep -c 'termix-keep-patched' .planning/phases/06-telegram-like-interface/06-UAT-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/06-telegram-like-interface/06-UAT-CHECKLIST.md` exists
    - Every TG-01..11 appears at least once (11 grep counts, each ≥ 1)
    - Blocking items marked 🚨 appear ≥ 15 times (roughly one per requirement + persistence + negative-space + regression)
    - Regression smoke section names patches #25, #35, #57, #60, #100, #102 (grep for each ≥ 1)
    - The disarm command is present (grep for `termix-keep-patched` returns ≥ 1)
    - The "negative-space (scope-fence) checks" section is present (grep for `Negative-space` OR `NO activity/unread` returns ≥ 1)
    - The persistence-contract section explicitly walks the TG-05 scroll-position preservation smoke test (grep for `scroll position is preserved` OR `Scroll UP` returns ≥ 1)
    - Mobile section covers TG-06 URL fragment survives Chrome window-restore explicitly (grep for `mv=1` or `window-restore` returns ≥ 1)
  </acceptance_criteria>
  <done>Ashley has a clean UAT walkthrough ready to run the moment Task 4 turns her green-light into a live container. Every load-bearing correctness contract has an observable check.</done>
</task>

<task type="auto">
  <name>Task 3: Generate 06-PATCHES-MD-ENTRY.md — draft entry for the fork's patch catalog</name>
  <read_first>
    - ~/.claude/identities/tina/termix-patches.md — the ENTIRE file's structure at a high level. Specifically:
      - Patch #60 entry (around offset 3309, ~130 lines) — the CANONICAL entry style for a load-bearing multi-file patch. Structure: motivation → root cause → fix (with sub-bullets per file / per behavior) → files touched → rebase risk → verify-post-deploy invariants → deploy note.
      - Patch #100 entry (around offset 6235, ~25 lines) — CANONICAL for a smaller surgical patch.
      - Patch #102 entry (around offset 6274, ~40 lines) — reference for how the fork explains a NEW hook + call sites.
      - The count near the top of termix-patches.md ("ONE HUNDRED FOUR numbered patches" or similar) — bump instructions.
    - .planning/phases/06-telegram-like-interface/06-01-SUMMARY.md, 06-02-SUMMARY.md, 06-03-SUMMARY.md, 06-04-SUMMARY.md — synthesize the actual shipped shapes into the entry (do NOT re-guess based on plans; use what actually landed).
    - .planning/phases/06-telegram-like-interface/06-CONTEXT.md `<decisions>` block — for the philosophical framing quotes that belong in the "motivation" section.
    - .planning/shapes/shape-telegram-like-interface.md — for the "philosophy" language Ashley approved on 2026-07-21.
    - The actual git diff of Plans 06-01..04 (`git log --oneline` for those SHAs; `git diff` for the shipped code) — for accurate "files touched" lines.
    - .planning/phases/05-pretty-view-file-upload-support/05-PATCHES-MD-ENTRY.md — reference for the fork's entry style at scale (Phase-5 shipped a multi-file feature; Phase 6 is similar scope).
  </read_first>
  <files>.planning/phases/06-telegram-like-interface/06-PATCHES-MD-ENTRY.md</files>
  <action>
Create `.planning/phases/06-telegram-like-interface/06-PATCHES-MD-ENTRY.md`. Structure follows patch #60 + Phase-5 entry as templates. The patch number is `NNN.` (numeric placeholder; Ashley fills at pin time — likely 105+ depending on interstitial pins).

Structure:

```
   NNN. `feat(navigation): telegram-like interface — sidebar conversation
       list, tab strip removed, mobile list-vs-view flow, session
       persistence across switches` — Phase 6, shipped 2026-MM-DD (fill in at pin time).
       Reshapes Termix's top-level navigation model around a Telegram-style
       single-select conversation list. Sidebar becomes a flat list of
       currently-active sessions (grouped by host with separators, per-
       session pins float on top). Tab strip removed entirely. Mobile:
       list-vs-view two-screen flow with top-left back button; bottom
       navigation bar deleted. Admin destinations retreat to a gear icon
       on desktop and a settings row on mobile. Selection persistence
       preserved within a page-load — switching conversations is hide-not-
       unmount (WebSocket, terminal buffer, pretty-view scroll, ambient
       panels all preserved across switches; refresh resets from scratch).

       * **Motivation** (from shape file). Termix has been drifting toward
         Telegram-shape for months (pretty view, expanding sidebar,
         mobile-bottom-nav gate). Ashley uses it like a chat client to
         talk to agents on remote boxes, not like a tab manager. This
         work names the drift and completes it — deletes the tab strip,
         collapses the mental model into "conversations," and moves
         admin surfaces out of the way. Shape file:
         `.planning/shapes/shape-telegram-like-interface.md`.

       * **New conversation-store (in-memory).** Module-scoped store
         (`src/ui/state/conversation-store.ts`) mirrors identities-store
         pattern (useSyncExternalStore + EventTarget + module-scoped
         Map/Set). Derives a `{pinned, grouped}` view from AppShell's
         live `tabs` + `realHostTree`. Order below pins is the existing
         sidebar host-tree order (no new sort rule, no recency shuffle).
         Pins are per-session; session-end clears pin along with the
         row. Single-select via `selectedId` with stale-pointer defense
         (coerces to null when the id is no longer in tabs). No
         localStorage / sessionStorage — persistence is page-load only
         per shape lock.

       * **Tab strip DELETED.** `src/ui/shell/TabBar.tsx` removed from
         the repo entirely. NO feature flag, NO user-facing toggle to
         restore, NO conditional rendering. AppShell's DOM-node-stability
         mechanism (patch #35's `tabNodesRef` + `normalViewRef` +
         DOM-move effect) UNCHANGED in structure — only the "visible-
         inline" tab-id selector changes from `activeTabId` to
         `effectiveSelectedTabId = selectedConversationId ?? activeTabId`.
         Result: switching conversations is instant with zero reconnect
         because the DOM nodes stay mounted throughout, hidden via
         visibility toggle. The `isVisible` signal passed to each mounted
         pane continues to fire under the same semantics (WipBubble,
         PlanPendingBubble, MessageQueueDrawer, SessionHoldingOverlay
         all unaffected).

       * **Mobile flow.** `src/ui/lib/mobile-flow.ts` — small state
         machine keyed on a new `#mv=1` URL fragment key layered over
         patch #25's `#tab=`. On touchscreen viewports (useIsTouchDevice
         from patch #103) the tree renders list-full-screen OR view-full-
         screen based on `useMobileScreen()`. Top-left back button
         (MobileViewHeader) calls navigateToList(); browser back gesture
         also works via popstate. `mv=1` survives Chrome window-restore
         for the same reason `tab=` does — it lives in the fragment,
         not the query string. `MobileBottomBar.tsx` DELETED; its
         destinations (host manager, credentials, quick-connect, ssh-
         tools, snippets, history, split-screen, user-profile, admin-
         settings) migrated to a SettingsRow component mounted at the
         bottom of the mobile ConversationsPanel (position chosen so it
         doesn't compete with pinned or active rows for attention).

       * **Settings surface (desktop).** Small gear icon in the
         ConversationsPanel header (unobtrusive per shape lock). Dropdown
         routes via the existing handleRailClick to admin-settings /
         user-profile / host-manager / credentials — same destinations
         the mobile bottom nav used to reach. Admin-settings entry is
         gated on the existing `isAdmin` check (T-06-02-04 preserved;
         no bypass).

       * **New-session button.** `src/ui/sidebar/NewSessionButton.tsx` +
         `NewSessionDialog.tsx` — visible button at top of list (above
         pins). Click opens a modal host picker: filterable host list
         from realHostTree, optional session name (empty = auto-fill
         via the fork's feat/tab-title-from-tmux mechanism). Client-side
         name validation `/^[\w-]{0,64}$/` as belt-and-suspenders
         (backend tmux path unchanged, remains the security boundary).
         On create: openTab(host, "terminal", ...{targetTmuxSession,
         label, allowCreateTmux:true}) then selectConversationDeferred
         (new store action) to auto-navigate the new session — the
         `pendingSelectId` state defends against the React setState-
         then-select race.

       * **Threat model addressed.** Path-traversal / injection surfaces
         are all indirect (session names → tmux → shell); client-side
         input validation + existing backend sanitization handle. URL
         scheme collision handled by keeping `mv=` short and reserved.
         Stale-selection defense (T-06-01-01) prevents "phantom active
         row" UX. Stranded-user defense (T-06-03-06) auto-returns mobile
         users to the list when their viewed conversation ends. Race
         defense (T-06-04-04) via pendingSelectId.

       * **What we DIDN'T do** (deferred-to-v2 per shape lock):
         - Any activity/unread signal (dots, badges, counts, motion, sound)
         - Cross-conversation search
         - Folder / nested-grouping above host separators
         - Drag-to-reorder for pins
         - History / scrollback for ended sessions
         - Persisting selected conversation across browser refreshes
         - Auto-restoring "last set of open conversations"

       * **Verify post-deploy invariants** (for future rebase smoke checks):
         - `docker exec termix grep -c 'ConversationsPanel' /app/dist/assets/*.js` should return ≥ 1
         - `docker exec termix grep -c '"mv"' /app/dist/assets/*.js` should return ≥ 1 (mobile-flow URL fragment key survived tree-shake)
         - `docker exec termix grep -c 'MobileBottomBar\|TabBar' /app/dist/assets/*.js` should return 0 (deletions confirmed)
         - `docker exec termix grep -c 'message_queue_delete_on_send' /app/dist/backend/backend/ssh/terminal.js` should return ≥ 1 (patch #60 preserved)
         - `docker exec termix grep -c 'ssh_input_delayed_enter' /app/dist/backend/backend/ssh/terminal.js` should return ≥ 1 (patch #100 preserved)
         - `docker exec termix grep -c 'pointer: coarse' /app/dist/assets/*.js` should return ≥ 1 (patch #102 preserved)
         - `docker exec termix grep -c 'snapshotPendingTab\|consumePendingWorkspace' /app/dist/assets/*.js` should return ≥ 1 (patch #25 preserved)

       * **Files touched** (fill from actual `git log` at pin time):
         - `src/ui/state/conversation-store.ts` — NEW module, in-memory store
         - `src/ui/state/conversation-store.test.ts` — NEW test coverage
         - `src/ui/sidebar/ConversationsPanel.tsx` — NEW panel + list rendering
         - `src/ui/sidebar/ConversationRow.tsx` — NEW row component
         - `src/ui/sidebar/NewSessionButton.tsx` — NEW button component
         - `src/ui/sidebar/NewSessionDialog.tsx` — NEW picker modal
         - `src/ui/sidebar/NewSessionDialog.test.tsx` — NEW test coverage
         - `src/ui/sidebar/SettingsRow.tsx` — NEW (or exported from ConversationsPanel) — mobile settings-row
         - `src/ui/lib/mobile-flow.ts` — NEW mobile screen state machine + fragment routing
         - `src/ui/lib/mobile-flow.test.ts` — NEW test coverage
         - `src/ui/lib/tab-url.ts` — extended: WorkspaceSpec.mobileView + mv=1 encode/parse
         - `src/ui/sidebar/AppRail.tsx` — added `conversations` RailView + rail button
         - `src/ui/AppShell.tsx` — wired conversation-store + mobile-flow + gear-icon routing; deleted TabBar + MobileBottomBar mount sites; extended URL-sync effect for mobileView; added stranded-user defense; changed default RailView from "hosts" to "conversations"
         - `src/ui/AppShell.persistence.test.tsx` — NEW integration test for DOM-node-stability contract (or fallback per Plan 06-02 summary)
         - `src/ui/shell/TabBar.tsx` — DELETED
         - `src/ui/shell/MobileBottomBar.tsx` — DELETED

       * **Rebase risk**: MEDIUM on `src/ui/AppShell.tsx` (single largest
         edit surface — DOM-move effect + createPortal loop + URL-sync
         + JSX tree; upstream restructuring of any of these requires
         careful re-apply). LOW on all new files (fork-only, no
         upstream conflict surface). LOW-MEDIUM on `src/ui/lib/tab-url.ts`
         (small surgical extension; upstream doesn't have this file).
         The MobileBottomBar deletion is trivial to re-apply if the file
         reappears upstream (delete again).

       * **Deploy note**: shipped behind the mandatory 15-min deadman with
         explicit per-deploy green light per deploy-runbook.md. Zero new
         npm dependencies. Zero new nginx location blocks (Phase 6 is
         frontend-only; no backend routes added). Standard
         `sudo bash /opt/termix/termix-patches/build-termix.sh` +
         `sudo docker compose up -d --force-recreate termix`.

       Related bounty: `~/.claude/identities/tina/bounties/telegram-like-interface/`.
```

At pin time, Ashley:
1. Numbers the patch (next available integer)
2. Copies the entry into `~/.claude/identities/tina/termix-patches.md` at the appropriate ordinal position
3. Bumps the "ONE HUNDRED FOUR numbered patches" count near the top of that file
4. Commits the pin
  </action>
  <verify>
    <automated>cd /home/ubuntu/termix && test -f .planning/phases/06-telegram-like-interface/06-PATCHES-MD-ENTRY.md && grep -c "Files touched" .planning/phases/06-telegram-like-interface/06-PATCHES-MD-ENTRY.md; grep -c "Verify post-deploy invariants" .planning/phases/06-telegram-like-interface/06-PATCHES-MD-ENTRY.md; grep -c "Rebase risk" .planning/phases/06-telegram-like-interface/06-PATCHES-MD-ENTRY.md; grep -c "Deploy note" .planning/phases/06-telegram-like-interface/06-PATCHES-MD-ENTRY.md; grep -c "patch #25\|patch #35\|patch #57\|patch #60\|patch #100\|patch #102" .planning/phases/06-telegram-like-interface/06-PATCHES-MD-ENTRY.md; grep -c "conversation-store\|ConversationsPanel\|mobile-flow\|NewSessionDialog" .planning/phases/06-telegram-like-interface/06-PATCHES-MD-ENTRY.md; grep -c "TabBar.tsx\|MobileBottomBar.tsx" .planning/phases/06-telegram-like-interface/06-PATCHES-MD-ENTRY.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/06-telegram-like-interface/06-PATCHES-MD-ENTRY.md` exists
    - Contains canonical section headings: `Files touched`, `Verify post-deploy invariants`, `Rebase risk`, `Deploy note` (grep each ≥ 1)
    - References the six load-bearing prior patches Phase 6 inherits from / preserves: #25, #35, #57, #60, #100, #102 (grep total ≥ 6)
    - Names the four new-file categories: conversation-store, ConversationsPanel, mobile-flow, NewSessionDialog (grep total ≥ 4)
    - Names both deleted files: TabBar.tsx, MobileBottomBar.tsx (grep total ≥ 2)
    - Mentions the "what we DIDN'T do" deferred-items list explicitly
    - Mentions the mandatory 15-min deadman
  </acceptance_criteria>
  <done>The patch-catalog entry is drafted following the fork's canonical multi-file-patch style. Ready to paste into `~/.claude/identities/tina/termix-patches.md` at pin time.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: End-to-end deploy checkpoint — build, deadman-armed deploy, Ashley's UAT walk, disarm-or-fire</name>
  <what-built>
    Plans 06-01..04 shipped:
    - Plan 06-01: `src/ui/state/conversation-store.ts` (module-scoped store, in-memory only, pins + single-select + host-tree derivation) + tests. `src/ui/sidebar/ConversationsPanel.tsx` + `ConversationRow.tsx` (identity avatar + hue tint from TabBar's idiom reused). Zero user-visible change on production yet (foundation only).
    - Plan 06-02: `src/ui/shell/TabBar.tsx` DELETED. `AppShell.tsx` rewired to consume conversation-store as selection source of truth; DOM-node-stability mechanism (patch #35) preserved verbatim in structure; only the "visible-inline" tab-id selector changed. ConversationsPanel extended with desktop gear icon settings surface. SettingsRow component created for Plan 06-03's mobile mount. AppRail's default view changed to `conversations`.
    - Plan 06-03: `src/ui/shell/MobileBottomBar.tsx` DELETED. `src/ui/lib/mobile-flow.ts` NEW (fragment-based list-vs-view state machine). `src/ui/lib/tab-url.ts` extended with `mobileView`/`mv=1`. AppShell branches mobile viewport into list vs view screen; MobileViewHeader with top-left back button; SettingsRow mounted at bottom of mobile list. Stranded-user defense in place.
    - Plan 06-04: `src/ui/sidebar/NewSessionButton.tsx` + `NewSessionDialog.tsx` NEW (visible new-session button at top of ConversationsPanel; host picker modal; client-side session-name validation). Conversation-store extended with `selectConversationDeferred` + `pendingSelectId` (race defense). AppShell's ConversationsPanel mount site provides `hostTree` + `onCreateSession` that wires openTab + selectConversationDeferred + (mobile) navigateToView.
    - Plan 06-05 Task 1: build clean, Phase 6 artifacts survive Vite tree-shake, deletions actually gone from dist, load-bearing prior-patch bytes intact.
    - Plan 06-05 Task 2: `06-UAT-CHECKLIST.md` walking TG-01..11 + persistence + negative-space + regression smoke.
    - Plan 06-05 Task 3: `06-PATCHES-MD-ENTRY.md` drafted for termix-patches.md pin.

    Together: the full Telegram-shape navigation — flat conversation list with pins-on-top + host-grouped-below, single visible view with instant switch (no reconnect), mobile list-vs-view flow with back button, unobtrusive settings surface, visible new-session CTA. Tab strip and mobile bottom nav are gone.
  </what-built>
  <how-to-verify>
    Follow this exact sequence from `~/.claude/identities/tina/deploy-runbook.md` step-by-step. The mandatory 15-min deadman rollback is a STANDING CONSTRAINT per Ashley 2026-07-03, NO EXCEPTIONS.

    **CRITICAL — Get explicit per-deploy green light from Ashley BEFORE running step 1.** The pre-authorization for the code work does NOT authorize the deploy per Ashley 2026-07-12. This checkpoint is where you ask: "Plans 06-01..04 are shipped in-tree, build verifies clean per Task 1, UAT checklist + patches entry drafted per Tasks 2-3. May I deploy Phase 6 now? This is a substantial user-visible change — the tab strip and mobile bottom nav are gone."

    After green light:

    1. **Apply + commit + push + build.**
       - Working copy already has Plans 06-01..04 committed atomically per each Plan's summary. Verify: `cd ~/termix && git status` should show a clean tree (or only the plan-artifact .md files uncommitted).
       - **`git push` BEFORE build.** The build script clones from GitHub — local-only commits cache-hit the frontend-builder layer per deploy-runbook.md's patches #43 + #69 trap. Verify: `git log --oneline origin/feat/tab-title-from-tmux..HEAD` should return empty.
       - Build: `sudo bash /opt/termix/termix-patches/build-termix.sh` (typical duration ~9-12s on this box).
       - If build fails: `git checkout -- .` to reset any accidental worktree changes, investigate, retry.

    2. **Clear the stale sentinel from the prior deploy.**

           sudo rm -f /tmp/termix-keep-patched

       ⚠️ From this moment until step 5, the sentinel is ABSENT. A `sleep 900` process from a PRIOR deploy's deadman may still be ticking; DO NOT try to "clean it up" during this window per deploy-runbook.md's "never kill a deadman's sleep child" rule.

    3. **Arm the new deadman.**

           nohup sudo -b bash -c 'sleep 900; [ ! -f /tmp/termix-keep-patched ] \
             && bash /opt/termix/.tmp-revert.sh' > /tmp/termix-revert-bg.log 2>&1

    4. **Force-recreate the container.**

           cd /opt/termix && sudo docker compose up -d --force-recreate termix

       Watch for healthy state (`sudo docker ps | grep termix` shows healthy after ~5-10s).

    5. **Tell Ashley: "Deployed. Please run the UAT checklist at `.planning/phases/06-telegram-like-interface/06-UAT-CHECKLIST.md`."**

       Ashley walks the checklist. She reports either "all 🚨 green" or specific failures. Because this is a substantial UX change, expect that some UAT items surface subjective feedback ("the pin toggle affordance is confusing" — that kind of thing is normal for a first ship, doesn't necessarily mean revert).

    6. **On her "worked" / "pin it":** disarm the deadman.

           sudo touch /tmp/termix-keep-patched
           sudo pkill -f 'sleep 900; \[ ! -f /tmp/termix-keep-patched'

       ⚠️ Use the NARROW pkill pattern per deploy-runbook.md.

       Then help Ashley pin the patch:
       - Assign next available patch number (check `grep -n "^\s*[0-9]\+\." ~/.claude/identities/tina/termix-patches.md | tail -3` for current top)
       - Paste `.planning/phases/06-telegram-like-interface/06-PATCHES-MD-ENTRY.md` content into termix-patches.md at the appropriate ordinal position
       - Bump the "ONE HUNDRED FOUR numbered patches" count near the top of termix-patches.md
       - Commit the pin

    7. **If she doesn't respond within 15 min:** the deadman fires and rolls the container back to `ghcr.io/lukegus/termix:latest`. She still has Termix access. If deadman fires:
       - Do NOT panic
       - Diagnose (browser console, `sudo docker logs termix | tail -100`)
       - Fix the specific issue in a follow-up plan (do NOT re-deploy Phase 6 as-is without addressing)

    8. **If UAT surfaces a specific 🚨 failure:** either
       - Quick-fix inline within the deadman window (small chrome fix, small logic bug), re-deploy → re-arm → re-verify — OR
       - Let the deadman fire and iterate in a follow-up plan

    9. **On any 🚨 failure that requires source diffs beyond a trivial one-liner:** the fix belongs in whichever Plan 06-01..04 the bug lives in — surface via `/gsd-plan-phase --gaps 6` for a proper gap-closure plan.

    Report back with:
    - Whether the deadman was disarmed (Ashley's UAT green) or fired (rollback)
    - Screenshot or brief description of at least ONE end-to-end happy-path (open list → click a conversation → see it → click another → click back → instant with no reconnect)
    - Confirmation of TG-05 persistence (switch away + back preserves scroll position on a pretty-view pane)
    - Confirmation of TG-06 mobile (open on phone, list-view flow works, back button works)
    - Confirmation of TG-07 (no bottom nav on mobile)
    - If pinned: the assigned patch number and the commit sha of the termix-patches.md pin commit
  </how-to-verify>
  <resume-signal>Type "pinned as patch #NNN commit <sha>" if deployed + verified + pinned. Type "reverted, deadman fired, issue: ..." if rollback occurred with root cause. Type "held, iterating on ..." if a fix is in progress within the deadman window.</resume-signal>
  <done>Phase 6 is deployed to production behind the mandatory 15-min deadman, Ashley has walked the UAT checklist end-to-end and confirmed all 🚨 items green (or the deadman fired and rollback occurred with a documented root cause for follow-up). Either way the ship state is CLEAR — no ambiguity about whether the reshape is live.</done>
</task>

</tasks>

<verification>
- `npm run build` completes with no errors (Task 1)
- All Phase 6 frontend artifacts appear in `dist/` after tree-shake; deletions confirmed gone (Task 1)
- All load-bearing prior-patch grep gates pass (#25, #35, #57, #60, #100, #102 markers still in dist — Task 1)
- Zero diff on `src/ui/features/pretty-view/`, `src/ui/features/terminal/Terminal.tsx`, `src/ui/features/guacamole/`, `src/backend/`, `docker/`, `package.json`, `package-lock.json` (Task 1)
- `06-UAT-CHECKLIST.md` exists and covers all 11 TG-NN requirements + persistence + negative-space + regression (Task 2)
- `06-PATCHES-MD-ENTRY.md` exists in the canonical fork-catalog style (Task 3)
- Deploy checkpoint (Task 4) reaches green: either Ashley pins OR the deadman fires with a documented root cause
</verification>

<success_criteria>
- All 11 TG-NN requirements verified end-to-end in production per the UAT checklist
- Mandatory 15-min deadman rollback armed before deploy per Ashley 2026-07-03 and disarmed after successful UAT (or allowed to fire on failure)
- Zero regression to pre-existing patches — patches #25, #35, #57, #60, #100, #102 all continue to work
- Zero regression to unrelated Termix surfaces — pretty-view, terminal, RDP/VNC, file manager, dashboard, command palette, split-screen (via SplitScreenPanel rail) all render + behave normally
- Patch pinned in `~/.claude/identities/tina/termix-patches.md` with the drafted entry from Task 3, or rollback documented with clear root cause for follow-up
- Bounty at `~/.claude/identities/tina/bounties/telegram-like-interface/` closed via `/close telegram-like-interface` upon successful UAT (bounty lifecycle housekeeping — done outside this plan by Ashley)
</success_criteria>

<output>
Create `.planning/phases/06-telegram-like-interface/06-05-SUMMARY.md` when done. Summary must record: deploy outcome (disarmed vs fired), assigned patch number if pinned, commit sha of pin commit, any 🚨 items that failed and how they were resolved, any deltas between drafted `06-PATCHES-MD-ENTRY.md` and actually-pinned entry (Ashley may edit at pin time), and any UAT items Ashley surfaced as subjective feedback (worth capturing for a v2 follow-up bounty).
</output>
