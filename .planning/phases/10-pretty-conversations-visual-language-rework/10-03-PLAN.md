---
phase: 10-pretty-conversations-visual-language-rework
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - src/ui/AppShell.tsx
  - src/ui/sidebar/NewSessionDialog.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "AppShell mounts PrettyConversationsPanel on BOTH viewports (desktop inline sidebar, small-window desktop Sheet, mobile full-screen list) — the old `<ConversationsPanel .../>` mount site is replaced with `<PrettyConversationsPanel variant={isTouchDevice ? 'mobile' : 'desktop'} .../>` verbatim prop shape (per Wave 2 contract)"
    - "No dual-mode ship — both viewports swap in the same commit (shape file 'What would make it wrong' §4)"
    - "Persistent top-left sidebar-toggle button: fixed-positioned 32x32 chevron at `top: 8px, left: 8px, z-index: 30` inside the AppShell root; renders at ALL widths (desktop, small-window desktop, mobile touch); toggles `sidebarOpen` (existing state)"
    - "Chevron rotates 180° when sidebarOpen === true (transition-transform); direction always points 'toward the action' — collapsed = points open, open = points closed"
    - "The existing narrow-window thin-strip affordance at AppShell:1844-1852 is either REMOVED (single canonical toggle) or KEPT as a secondary discovery path (planner's call — recommendation: REMOVE to avoid two overlapping affordances at narrow widths). Whichever chosen, document in SUMMARY"
    - "Phase 6/7 tests in NewSessionDialog.test.tsx that reference the old `ConversationsPanel` (Test 10 in that file) are updated to reference `PrettyConversationsPanel` OR the specific 'DOM order' assertion is retargeted to the new panel — this is the only Phase 6/7 test that couples to the panel; conversation-store.test.ts continues to reference the OLD panel in comments only (no import — leave those alone)"
  artifacts:
    - path: "src/ui/AppShell.tsx"
      provides: "Single-panel cutover: ConversationsPanel → PrettyConversationsPanel; persistent top-left sidebar toggle added"
      contains: "PrettyConversationsPanel"
    - path: "src/ui/sidebar/NewSessionDialog.test.tsx"
      provides: "Test 10 (Panel DOM-order) retargeted to PrettyConversationsPanel"
      contains: "PrettyConversationsPanel"
  key_links:
    - from: "src/ui/AppShell.tsx"
      to: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      via: "import + mount in sidebarPanelContent"
      pattern: "PrettyConversationsPanel"
    - from: "src/ui/AppShell.tsx (persistent toggle)"
      to: "sidebarOpen state (existing useState line 232)"
      via: "onClick sets sidebarOpen(!sidebarOpen)"
      pattern: "setSidebarOpen"
---

<objective>
Cut over AppShell.tsx from the old `ConversationsPanel` to the new `PrettyConversationsPanel` on both viewports in a single commit (no dual-mode ship — shape file lock). Add the persistent top-left 32x32 chevron sidebar-toggle button that renders at all window widths and toggles `sidebarOpen` — this is the fix for the small-window desktop sidebar-affordance regression Ashley called out. Resolve the existing narrow-window thin-strip (AppShell:1844-1852) so there aren't two competing affordances; recommendation is to remove it (single canonical toggle) but planner may keep it for wide-window discovery — document the choice in the SUMMARY. Retarget the one panel-coupled test in Phase 6/7 that would otherwise break (NewSessionDialog.test.tsx Test 10). The old files stay on disk — Wave 4 deletes them; this plan ensures they are UNREFERENCED after cutover.

Purpose: Ship the visual change to production (behind Ashley's morning greenlight — no deploy in this phase, just a code-complete cutover). Every Ashley experience on both viewports becomes the new panel. The persistent toggle closes the "how do I get the sidebar back when the window is narrow" bug she surfaced.

Output: AppShell.tsx cutover complete; NewSessionDialog.test.tsx Test 10 updated; existing Vitest suite still green; tsc-clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-pretty-conversations-visual-language-rework/10-01-SUMMARY.md
@.planning/phases/10-pretty-conversations-visual-language-rework/10-02-SUMMARY.md

# Design source-of-truth for persistent toggle (LOCKED)
@/home/ubuntu/.claude/identities/tina/bounties/pretty-conversations-panel-redesign/desktop.html

# Files to modify
@src/ui/AppShell.tsx
@src/ui/sidebar/NewSessionDialog.test.tsx

# Files to reference (do NOT modify in this plan)
@src/ui/sidebar/ConversationsPanel.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cut over AppShell.tsx from ConversationsPanel to PrettyConversationsPanel</name>
  <files>src/ui/AppShell.tsx</files>
  <action>
    Locate the current mount site at `src/ui/AppShell.tsx` line 1403 (`<ConversationsPanel ... />` inside `sidebarPanelContent`).

    Change:
      1. Line 60 import: replace `import { ConversationsPanel } from "@/sidebar/ConversationsPanel";` with `import { PrettyConversationsPanel } from "@/features/pretty-conversations/PrettyConversationsPanel";`.
      2. Line 1403 JSX opening tag: change `<ConversationsPanel` to `<PrettyConversationsPanel`.
      3. Line 1534 JSX closing/self-close: change to match (`/>`).
      4. Add ONE new prop to the mount: `variant={isTouchDevice ? "mobile" : "desktop"}`. Everything else stays byte-identical:
         - `onRailClick={...}` unchanged
         - `isAdmin={isAdmin}` unchanged
         - `onConversationSelected={isTouchDevice ? () => navigateToView() : undefined}` unchanged
         - `settingsRowSlot={isTouchDevice ? <SettingsRow .../> : undefined}` unchanged
         - `hostTree={realHostTree}` unchanged
         - `onCreateSession={...}` unchanged
         - `onDetachedRowClick={...}` unchanged (BUT: remove the `[F3-diag] onDetachedRowClick: row.host missing` console.warn at AppShell:1483 — the panel-side F3-diag is going away, this AppShell-side one is orphaned telemetry with no companion; delete it and keep a silent early-return `if (!host) return;` per fork's cleanup discipline)
         - `onRdpRowClick={...}` unchanged

    The mount site's location inside `sidebarPanelContent` means both desktop-inline (line 1777) + narrow-window Sheet (line 1801) + mobile full-screen (line 1823) all get the new panel in a single change — no per-viewport branching needed. This is the "single cutover, both viewports" the shape file requires.

    Verify by grep that AppShell.tsx contains ZERO references to `ConversationsPanel` (as a component import or JSX) after the edit. Comment references are fine to keep (Phase 6/7 plan notes throughout AppShell.tsx that name "ConversationsPanel" — those describe historical mount points and can be left; only IMPORTS + JSX ELEMENTS need updating).

    Also remove the `[F3-diag] onDetachedRowClick` console.warn at line ~1483 as noted above. The whole F3-diag telemetry was scoped to patch #111e (see STATE.md 2026-07-22 decision context) and is being retired with the old panel.

    Do NOT modify anywhere else in AppShell.tsx in this task — this task is a surgical mount-site swap. Persistent toggle is Task 2. Thin-strip resolution is Task 3.
  </action>
  <verify>
    <automated>cd /home/ubuntu/termix && grep -c "ConversationsPanel" src/ui/AppShell.tsx && grep -c "PrettyConversationsPanel" src/ui/AppShell.tsx && npx tsc --noEmit 2>&1 | grep -c "src/ui/AppShell.tsx"</automated>
  </verify>
  <done>AppShell.tsx imports and JSX-mounts `PrettyConversationsPanel` instead of `ConversationsPanel`; the `variant={isTouchDevice ? "mobile" : "desktop"}` prop is present; the F3-diag console.warn at AppShell:1483 is removed; tsc-clean at AppShell.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Add persistent top-left sidebar-toggle chevron; resolve narrow-window thin-strip</name>
  <files>src/ui/AppShell.tsx</files>
  <action>
    At the top of the AppShell root JSX return (right after the opening `<div className="flex w-screen bg-background" ...>` at line 1736), insert a fixed-positioned chevron button as the FIRST child of the root flex container.

    Structure:
      - Absolute positioning: `position: fixed; top: max(env(safe-area-inset-top), 8px); left: max(env(safe-area-inset-left), 8px); z-index: 30;`. Fixed (not absolute) so the button is anchored to the viewport corner regardless of AppShell's internal scroll containers — Ashley's small-window use case has scrollable areas that would otherwise absorb an absolutely-positioned button.
      - Dimensions: 32x32 rounded 8px.
      - Visual: glass treatment mirroring desktop.html lines 228-251 — `background: rgba(20,22,28,0.85); backdrop-filter: blur(10px) saturate(1.6); border: 1px solid rgba(255,255,255,0.08); box-shadow: 0 2px 8px rgba(0,0,0,0.35);`. Inline as arbitrary-value Tailwind classes (`bg-[rgba(20,22,28,0.85)] backdrop-blur-[10px] backdrop-saturate-150 border border-white/[0.08] rounded-lg shadow-[0_2px_8px_rgba(0,0,0,0.35)]`).
      - Icon: lucide `ChevronLeft` size-4 (already imported at AppShell.tsx line ~30; verify — if not, add). Wrap in `<span style={{ transform: sidebarOpen ? "rotate(180deg)" : undefined, transition: "transform 220ms" }}>` so the chevron rotates when the state flips. Direction: sidebarOpen=false → chevron points RIGHT (default `ChevronLeft` rotated 180° reads as "click to open →"). Wait — desktop.html rotates on `.open`, so open state = rotated. Match desktop.html exactly: use ChevronLeft, default (unrotated) reads as "sidebar is open, click to close" per desktop.html's mental model. Verify with the design mock — desktop.html line 251 says `.app-shell.open .persistent-toggle svg { transform: rotate(180deg); }`, which means when the sidebar is OPEN, the chevron rotates 180° (from ← to →, i.e. "click to close and content will extend right"). Copy that exact semantic: `sidebarOpen === true → rotate(180deg)`.
      - onClick: `() => setSidebarOpen(!sidebarOpen)`.
      - aria-label: `t("nav.sidebar.toggle", { defaultValue: "Toggle sidebar" })` (add this i18n key if not present, defaultValue is the fallback).
      - Conditional rendering: render UNCONDITIONALLY at all widths on non-touch devices. On touchscreens where the mobile flow uses a different navigation model (list-vs-view screens), still render at all times so Ashley has one consistent affordance — the sidebar isn't used as a peek on touchscreens (it IS the list screen), so the toggle acts as a "collapse for more reading room in view screen" affordance. Same button, same state, same visual.

    Reason button is fixed to the ROOT (not the AppShell outer flex): the AppShell outer div (line 1736) already carries `paddingTop: max(env(safe-area-inset-top), 0px)` (patch #125). Position:fixed anchors to the viewport, which sits ABOVE that padding — inject the button INSIDE the root div so its position:fixed context is the root's stacking context (which will honor the z-index correctly against the sidebar's z-30 resize handle at line 1783).

    Actually — position:fixed always anchors to the viewport regardless of parent (unless a parent has `transform` set). None of the AppShell parents have transform. So the button can live anywhere in the JSX tree, but for stacking-context clarity, place it as the FIRST child of the root flex div at line 1736.

    Resolve the existing narrow-window thin-strip at AppShell.tsx lines 1844-1852:
      - RECOMMENDED: REMOVE the entire `{!isMobile && !sidebarOpen && !isTouchDevice && (<button>...ChevronRight...</button>)}` block. Rationale: with the persistent top-left toggle rendering unconditionally, the thin-strip is redundant AND the two overlap in position (both left-edge) at narrow widths, causing a visual conflict. Single canonical toggle > two competing affordances.
      - Also remove the `pl-6` padding on the main content wrapper at line 1836 that reserved space for the thin-strip: change `className={\`... ${!isMobile && !sidebarOpen && !isTouchDevice ? "pl-6" : ""}\`}` to just the base class string without the ternary. The main content now uses the full width when sidebar is closed.
      - Add left padding to the main-content region to reserve room for the persistent toggle. Options:
        (a) Add `pl-12` (48px) unconditionally to the main content wrapper — clean but wastes 48px of horizontal real estate when sidebar is open (toggle sits over the sidebar's header per desktop.html sidebar-header line 188 `padding-left: 52px`).
        (b) Only reserve space when sidebar is closed: `${!sidebarOpen ? "pl-12" : ""}`. When open, the toggle sits over the sidebar's own header (which already has `padding-left: 52px` room baked in per desktop.html — but the production sidebarHeader at AppShell.tsx line 1687 does NOT have that padding baked in yet; verify and either add `pl-12` to the sidebarHeader OR let the toggle overlap the sidebar title text). Choose option (b) + add `pl-12` to sidebarHeader's title `<span>` at line 1689 so the toggle has room to sit over the header at all times without covering text. This matches desktop.html line 188 exactly.

    Grep-verifiable outputs:
      - `grep -c "persistent-toggle\|PersistentSidebarToggle\|top-left sidebar toggle" src/ui/AppShell.tsx` — expect ≥ 1 comment marker labeling the block
      - `grep -c "ChevronLeft\|ChevronRight" src/ui/AppShell.tsx` — should show ChevronLeft present, ChevronRight either removed or reduced by one (the thin-strip's use goes away)
      - `grep -c "!isMobile && !sidebarOpen && !isTouchDevice" src/ui/AppShell.tsx` — the thin-strip conditional is removed; should be 0 uses (or if the pattern appears elsewhere e.g. transition classes, verify that use is unrelated)

    Add an inline comment above the button naming it as "Phase 10: persistent top-left sidebar-toggle — the fix for Ashley's small-window sidebar-affordance regression. Renders at all widths; replaces the narrow-window thin-strip that used to disappear below some breakpoint."
  </action>
  <verify>
    <automated>cd /home/ubuntu/termix && grep -c "ChevronLeft" src/ui/AppShell.tsx && npx tsc --noEmit 2>&1 | grep -c "src/ui/AppShell.tsx"</automated>
  </verify>
  <done>Persistent 32x32 top-left chevron toggle renders inside the AppShell root; onClick toggles sidebarOpen; chevron rotates 180° when sidebarOpen; the narrow-window thin-strip at AppShell.tsx:1844-1852 is removed AND its companion `pl-6` main-content padding is removed; sidebarHeader title has `pl-12` room baked in for the toggle overlay; tsc-clean.</done>
</task>

<task type="auto">
  <name>Task 3: Retarget NewSessionDialog.test.tsx Test 10 to PrettyConversationsPanel</name>
  <files>src/ui/sidebar/NewSessionDialog.test.tsx</files>
  <action>
    Locate `describe("ConversationsPanel: NewSessionButton DOM order", ...)` at line 327 of `src/ui/sidebar/NewSessionDialog.test.tsx`.

    Options:
      (a) DELETE this describe block entirely. Rationale: it asserts a DOM-order constraint (NewSessionButton before ConversationRow) that no longer applies — the new PrettyConversationsPanel uses a compact pencil-icon button in the header (not a full-width NewSessionButton), and the "button before rows" constraint is trivially true by header vs scroll-region structure. Test provides ~zero regression coverage post-cutover.
      (b) RETARGET to PrettyConversationsPanel by replacing the ConversationsPanel import + render call. Rationale: keeps some coverage of "button appears before rows in DOM order" as a smoke test.

    RECOMMENDED: option (b) — retarget, don't delete. Cheap to keep, provides a regression net for future refactors of the panel header.

    Steps:
      1. Change import at line 39: `import { ConversationsPanel } from "./ConversationsPanel";` → `import { PrettyConversationsPanel } from "@/features/pretty-conversations/PrettyConversationsPanel";`.
      2. Change describe title at line 327: `"ConversationsPanel: NewSessionButton DOM order"` → `"PrettyConversationsPanel: header pencil renders before rows"`.
      3. Change the JSX at line 363 from `<ConversationsPanel` to `<PrettyConversationsPanel` and add the `variant="desktop"` prop.
      4. Update the DOM query to match the new component's affordance:
         - Was querying: `getByRole("button", { name: /new session/i })` (the labeled NewSessionButton).
         - Now query: `getByRole("button", { name: /new session/i })` — same aria-label carries into the pencil-icon button per Wave 2 spec.
         - The assertion "button precedes first ConversationRow in DOM" becomes "pencil precedes first `[data-conversation-id]` element in DOM".
      5. Update the assertion using `compareDocumentPosition` (Following bit = 4) instead of whatever the old test used.

    Keep beforeEach + mock block intact. Adjust any expectation that references a specific class or role name from the old NewSessionButton.

    Grep-verify: `grep -c "ConversationsPanel" src/ui/sidebar/NewSessionDialog.test.tsx` — expect 0 references to the OLD component (excluding filename `./ConversationsPanel` string in comments if any). `grep -c "PrettyConversationsPanel" src/ui/sidebar/NewSessionDialog.test.tsx` — expect ≥ 2 (import + JSX).

    Also verify the other tests in this file (Tests 1-9) still work — they test NewSessionDialog + NewSessionButton in isolation; NewSessionButton is still on disk (Wave 4 deletes it), and NewSessionDialog is reused verbatim by the new panel. Both remain importable in this test file until Wave 4. So Tests 1-9 stay untouched.
  </action>
  <verify>
    <automated>cd /home/ubuntu/termix && npx vitest run src/ui/sidebar/NewSessionDialog.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>Test 10 (the panel-coupled test) references PrettyConversationsPanel with `variant="desktop"`; all tests in NewSessionDialog.test.tsx pass; grep shows zero remaining references to the old `ConversationsPanel` in this test file (except possibly in comments).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Both viewports now render `PrettyConversationsPanel` (Wave 1 + Wave 2 output) via the AppShell cutover in Task 1. The persistent top-left 32x32 chevron toggle from Task 2 lives at `top: 8px, left: 8px` and toggles sidebarOpen at all widths — this is the fix for the small-window sidebar-affordance regression. Old ConversationsPanel/ConversationRow files are still on disk (Wave 4 deletes them) but unreferenced by AppShell.
  </what-built>
  <how-to-verify>
    Ashley does a live smoke check on the desktop dev server (or the last-deploy production site, whichever is easier), specifically:
      1. Open the app in a wide desktop window. Confirm the new panel renders in the sidebar with the chunky rows + hue-ring avatars + label-only-primary-line (no IdentityBadge chip) + host-name secondary line.
      2. Hover a row — pin button should appear on the right; hovering off should hide it. Click the pin — row moves to top with a filled pin glyph.
      3. Click any row — selected treatment matches the ChatMessage assistant-bubble hue gradient (verify against ChatMessage.tsx or against a live pretty-view side-by-side).
      4. Click the pencil in the sidebar header — NewSessionDialog opens (same dialog as before).
      5. Click the gear — settings dropdown opens (same items as before).
      6. Click the top-left chevron toggle — sidebar collapses; chevron rotates. Click again — sidebar expands; chevron rotates back.
      7. Shrink the window to ~480px width. The persistent toggle stays visible. Sidebar collapse/expand still works.
      8. Open in a mobile browser (or use DevTools mobile emulation). Confirm the mobile variant loads with the empty-text header + compact pencil only (no gear in header). Swipe-left on a row — pin action strip reveals. Swipe past 40px — snaps open. Tap the pin button — row pin state toggles. Tap outside — closes. Verify RDP rows at bottom cannot be swiped.
      9. Verify the empty-state renders the PlanPendingBubble-style idle glass card when no conversations exist (log out + log in to trigger, or manually clear openTabs via devtools).
    If any of 1-9 fail visually or functionally, capture the mismatch and route back to Wave 1 (row) or Wave 2 (panel) — this checkpoint is BEFORE deletion of the old files, so a route-back keeps the old panel available as an emergency swap-back.
  </how-to-verify>
  <resume-signal>Type "approved" if all 9 checks pass. Otherwise describe the specific failure(s) — the orchestrator will route back to the appropriate wave for revision.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ConversationsPanel → PrettyConversationsPanel mount swap | prop shape preserved verbatim; no new attack surface introduced |
| Persistent toggle click → sidebarOpen state | trusted local state mutation; same setSidebarOpen path the existing thin-strip uses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Denial of Service | position:fixed toggle covering critical UI | mitigate | 32x32 button in the top-left corner does not cover any interactive region; sidebarHeader is padded (pl-12) to leave room; verified in Ashley's human-verify checkpoint (visual test 8) |
| T-10-03-02 | Tampering | AppShell mount site swap | mitigate | prop shape preserved verbatim per Wave 2 contract; grep gate `grep -c PrettyConversationsPanel` confirms swap; tsc-clean at AppShell.tsx confirms no signature drift |
| T-10-03-SC | Tampering | npm installs | mitigate | zero new dependencies |
</threat_model>

<verification>
- AppShell.tsx imports and mounts `PrettyConversationsPanel` (not `ConversationsPanel`) with `variant={isTouchDevice ? "mobile" : "desktop"}` and preserved verbatim prop shape
- Persistent top-left 32x32 chevron toggle renders unconditionally inside AppShell root; toggles sidebarOpen on click; chevron rotates 180° when sidebarOpen === true
- Narrow-window thin-strip at AppShell:1844-1852 REMOVED; its companion `pl-6` main-content padding REMOVED; sidebarHeader title has `pl-12` room for the toggle overlay
- F3-diag console.warn at AppShell:1483 REMOVED (silent early-return preserved for defensive `if (!host)` handling)
- NewSessionDialog.test.tsx Test 10 retargeted to PrettyConversationsPanel with `variant="desktop"`; all tests in that file pass
- Full existing Vitest suite still passes (no regressions in Phase 6/7 tests)
- `npx tsc --noEmit` clean
- Ashley human-verify checkpoint APPROVED before Wave 4 executes
</verification>

<success_criteria>
1. Both viewports render `PrettyConversationsPanel` in production (behind Ashley's deferred greenlight)
2. Persistent top-left chevron toggle survives at all widths (480/720/1024/1400 all reachable)
3. No dual-mode ship — old panel unreferenced from AppShell.tsx after cutover
4. NewSessionDialog.test.tsx Test 10 retargeted; all tests still pass
5. tsc-clean; full existing test suite green
6. Human-verify checkpoint APPROVED — 9-step visual/functional walkthrough passes
</success_criteria>

<output>
Create `.planning/phases/10-pretty-conversations-visual-language-rework/10-03-SUMMARY.md` when done. Summary: what shipped (AppShell mount-site swap + persistent toggle + thin-strip resolution + test retarget), the specific choice made for the thin-strip (removed vs kept — Ashley reads this at deploy-time), any UAT feedback from the human-verify checkpoint that required revision, and a "handoff to Wave 4" section confirming the old ConversationsPanel + ConversationRow + NewSessionButton files can now be safely deleted (no remaining production references).
</output>
