---
phase: 10-pretty-conversations-visual-language-rework
plan: 05
type: execute
wave: 5
depends_on: ["10-04"]
files_modified:
  - .planning/phases/10-pretty-conversations-visual-language-rework/10-UAT-CHECKLIST.md
  - .planning/phases/10-pretty-conversations-visual-language-rework/10-BUILD-VERIFY-LOG.md
  - .planning/phases/10-pretty-conversations-visual-language-rework/10-PATCHES-MD-ENTRY.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "`npm run build` succeeds — Vite output has no errors, no TypeScript errors"
    - "`npx vitest run` reports all-green modulo pre-existing failures documented in STATE.md (patch #121 Send-button residual, patch #124 ThumbsUp aria-label residual); new-panel tests all green"
    - "`npx tsc --noEmit` reports zero errors"
    - "10-UAT-CHECKLIST.md exists and walks Ashley through the 9-step visual/functional check post-deploy"
    - "10-PATCHES-MD-ENTRY.md exists as a paste-ready draft for the patch #128 entry"
    - "10-BUILD-VERIFY-LOG.md captures tsc + vitest + npm-run-build outputs for provenance"
    - "NO deploy attempt in this phase — deferred to Ashley's separate morning greenlight per fork DEPLOY DISCIPLINE + the batched #123-#128 hold on the branch"
    - "The patch #128 draft references itself as batched onto the existing #123-#127 pending stack on feat/tab-title-from-tmux — no push, no image build, no deploy"
  artifacts:
    - path: ".planning/phases/10-pretty-conversations-visual-language-rework/10-BUILD-VERIFY-LOG.md"
      provides: "tsc + vitest + npm run build output for provenance"
    - path: ".planning/phases/10-pretty-conversations-visual-language-rework/10-UAT-CHECKLIST.md"
      provides: "Ashley post-deploy walkthrough — 9-step check adapted for production"
    - path: ".planning/phases/10-pretty-conversations-visual-language-rework/10-PATCHES-MD-ENTRY.md"
      provides: "Paste-ready patch #128 entry draft for ~/.claude/identities/tina/skynet-patches.md"
  key_links:
    - from: "10-UAT-CHECKLIST.md"
      to: "prototype.html + desktop.html design source-of-truth"
      via: "checklist items reference specific design details (48/40px avatars, hue-ring color, chevron rotation, RDP monitor icon)"
      pattern: "48px|40px|hue-ring|chevron|monitor"
    - from: "10-PATCHES-MD-ENTRY.md"
      to: "existing skynet-patches.md format"
      via: "matches the multi-commit-under-one-pin convention from patches #104 and #105"
      pattern: "patch #128"
---

<objective>
Close out Phase 10 as code-complete on the feat/tab-title-from-tmux branch, ready for Ashley's morning greenlight on the batched #123-#128 deploy. Runs the full build/test suite (logged for provenance), authors the UAT checklist Ashley walks after deploy, and drafts the patch #128 entry for skynet-patches.md. NO deploy in this phase — that is Ashley's separate call per fork DEPLOY DISCIPLINE.

Purpose: Deliver the last mile — verification, docs, pin-ready draft — so when Ashley greenlights the deploy in the morning, deploy is mechanical (run build-skynet.sh + docker compose up + arm deadman + Ashley UATs) rather than "write the docs first". Everything Ashley needs post-deploy lives in 10-UAT-CHECKLIST.md; everything Tina needs to pin lives in 10-PATCHES-MD-ENTRY.md.

Output: 3 markdown files in `.planning/phases/10-pretty-conversations-visual-language-rework/`. No code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-pretty-conversations-visual-language-rework/10-01-SUMMARY.md
@.planning/phases/10-pretty-conversations-visual-language-rework/10-02-SUMMARY.md
@.planning/phases/10-pretty-conversations-visual-language-rework/10-03-SUMMARY.md
@.planning/phases/10-pretty-conversations-visual-language-rework/10-04-SUMMARY.md

# Reference patterns for the deliverables
@.planning/phases/07-fleet-native-conversation-list/07-03-PLAN.md
@.planning/phases/07-fleet-native-conversation-list/07-UAT-CHECKLIST.md
@.planning/phases/07-fleet-native-conversation-list/07-03-BUILD-VERIFY-LOG.md

# Design source-of-truth (checklist references these visual details)
@/home/ubuntu/.claude/identities/tina/bounties/pretty-conversations-panel-redesign/prototype.html
@/home/ubuntu/.claude/identities/tina/bounties/pretty-conversations-panel-redesign/desktop.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: tsc + vitest + npm run build with output captured</name>
  <files>.planning/phases/10-pretty-conversations-visual-language-rework/10-BUILD-VERIFY-LOG.md</files>
  <action>
    Run each command via the bash tool and capture last ~50 lines of output into `10-BUILD-VERIFY-LOG.md`:

    1. `npx tsc --noEmit` — expect: no errors.
    2. `npx vitest run 2>&1 | tail -80` — expect: all-green modulo the pre-existing failures documented in STATE.md (patch #121 ComposeBox Tests 7-8 stale Send-button refs, patch #124 Phase 9 Layout stale "yes" refs). The new Phase 10 tests MUST all pass: PrettyConversationRow.test.tsx 11/11, PrettyConversationsPanel.test.tsx 15/15, retargeted NewSessionDialog.test.tsx (post-prune) 9/9.
    3. `npm run build 2>&1 | tail -50` — expect: Vite success, dist/ populated, zero TS errors.

    Log file structure (paste this template and fill in):
      ```
      # Phase 10 Build-Verify Log

      **Date:** [today]
      **Branch:** feat/tab-title-from-tmux (post patch #128 code-complete)
      **Verifier:** Claude (Wave 5 automation)
      **Verdict:** PASS / FAIL

      ## 1. TypeScript check (`npx tsc --noEmit`)
      [captured tail output — max 50 lines]
      **Result:** clean / N errors
      **Analysis:** [prose describing any errors and disposition]

      ## 2. Vitest run (`npx vitest run`)
      [captured tail output — max 80 lines]
      **Result:** X/Y passing
      **New Phase 10 tests:** PrettyConversationRow 11/11, PrettyConversationsPanel 15/15, NewSessionDialog (post-prune) 9/9
      **Pre-existing failures (out-of-scope):** [list explicitly, cross-reference STATE.md]

      ## 3. Vite build (`npm run build`)
      [captured tail output — max 50 lines]
      **Result:** success / failure
      **Bundle size delta:** [if easily measurable via ls -la dist/ vs prior build — else "not measured"]

      ## Verdict summary
      [PASS if all three green (modulo documented pre-existing) — else FAIL with route-back target wave]
      ```

    If ANY of the three commands produce unexpected failures (not the STATE.md-documented pre-existing), STOP and log the failure with a "route-back to Wave X" note. Do NOT proceed to Task 2/3 until resolved.

    If all three succeed with only documented pre-existing failures, mark **Verdict: PASS** at the top and continue.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && test -f .planning/phases/10-pretty-conversations-visual-language-rework/10-BUILD-VERIFY-LOG.md &amp;&amp; grep -c "PASS\|Verdict" .planning/phases/10-pretty-conversations-visual-language-rework/10-BUILD-VERIFY-LOG.md</automated>
  </verify>
  <done>10-BUILD-VERIFY-LOG.md exists with tsc + vitest + build outputs captured; "Verdict: PASS" recorded; new-panel tests confirmed 11+15+9 = 35 passing.</done>
</task>

<task type="auto">
  <name>Task 2: Author 10-UAT-CHECKLIST.md — Ashley's post-deploy walkthrough</name>
  <files>.planning/phases/10-pretty-conversations-visual-language-rework/10-UAT-CHECKLIST.md</files>
  <action>
    Author `10-UAT-CHECKLIST.md` modeled on the Phase 7 UAT format (`.planning/phases/07-fleet-native-conversation-list/07-UAT-CHECKLIST.md`) — same header + item shape + expected-behavior structure, updated for Phase 10 scope.

    Structure:
      ```
      # Phase 10 UAT Checklist — Pretty-Conversations visual-language rework

      **Deploy:** Patches #123-#128 batched behind 15-min deadman rollback
      **UAT anchor:** term.gigaashley.click (production) — post-deploy
      **Rollback trigger:** any RED item below or any visual defect Ashley flags
      **Design source-of-truth (locked):**
      - Mobile: ~/.claude/identities/tina/bounties/pretty-conversations-panel-redesign/prototype.html
      - Desktop: ~/.claude/identities/tina/bounties/pretty-conversations-panel-redesign/desktop.html

      ## Non-negotiable checks (any RED = rollback)

      ### Desktop (wide window — 1400px+)
      1. **New panel loads.** Open term.gigaashley.click. Sidebar shows the new chunky rows, not the old compact ones. Each row has a 40px hue-ring avatar disc, primary label = session name (= identity name, per Ashley convention), secondary line = Server-glyph + host name.
      2. **Selected row treatment.** Click any identity row. The row background gets the ChatMessage.tsx assistant-bubble hue gradient — verify by pulling up a pretty-view pane side-by-side and comparing the hue-tinted glass card treatment (they should visually match).
      3. **Pin toggle — hover-reveal.** Hover an unpinned row. Pin button appears on the right column, 24x24 rounded. Click it. Row jumps to the top of the list with a filled pin glyph in the identity hue. Hover another row and confirm the button reappears there too.
      4. **Header pencil = new session.** Click the compact pencil icon in the top-right of the sidebar header. NewSessionDialog opens (same dialog as before). Cancel and confirm dialog closes.
      5. **Header gear = settings.** Click the gear icon next to the pencil. Same dropdown menu as before (Host Manager, Credentials, Connections, Quick Connect, SSH Tools, Snippets, History, Split Screen, User Profile, Admin Settings).
      6. **Persistent top-left toggle.** Click the 32x32 chevron button fixed to the top-left of the app shell (position `top: 8px, left: 8px`). Sidebar collapses. Chevron rotates 180°. Click again. Sidebar expands. Chevron rotates back.
      7. **Small-window regression fix.** Shrink the browser window to ~480px width. The persistent top-left toggle STAYS VISIBLE. Sidebar can still be collapsed/expanded. This is the fix for the small-window sidebar-affordance regression.

      ### Mobile (touchscreen — iPhone/iPad)
      8. **Mobile variant loads.** Open the PWA-installed Skynet app (or the site in Mobile Safari). List screen shows the new chunky rows with 48px hue-ring avatar discs. Header shows the compact pencil button ONLY (no gear, no title text). Rows appear larger than desktop (72px vs 62px min-height).
      9. **Swipe-left pin.** Swipe any identity row leftward. A 48x48 hue-tinted pin action button reveals on the right. Swipe past ~40px past neutral to snap it open. Tap the button. Row jumps to top with a filled pin glyph. Swipe again to unpin.
      10. **Swipe coordination.** Swipe row A open, then swipe row B open. Row A closes automatically. Tap outside the swiped row. Row closes.
      11. **Vertical scroll not hijacked.** Try to scroll vertically starting from an identity row. Scroll proceeds normally (no swipe-open triggered) — the >12px vertical tolerance yields correctly.
      12. **RDP rows can't be swiped.** Scroll down to the RDP section (bottom, below the "Remote desktop" divider chip with monitor glyph). Try to swipe-left on an RDP row. No swipe-open. No pin action reveals. (T-Test-34 preserved.)
      13. **Mobile settings row at bottom.** Scroll to the bottom of the mobile list. SettingsRow with gear icon is present below the last row (above home-indicator safe area).
      14. **Empty state.** Log out and log back in without any active sessions (or verify against a scratch account). Empty state renders a PlanPendingBubble-style idle glass card centered in the scroll region with "No conversations yet" text.
      15. **Mobile header pencil.** Tap the compact pencil in the mobile header. NewSessionDialog opens. Complete the flow (pick host, name session). New session is created + auto-selected + auto-navigates to the view screen (existing Phase 6 mobile behavior preserved).

      ### Cross-viewport regression checks (all preserved from Phase 6/7)
      16. **Pretty-view internals unchanged.** Open any identity session. ChatMessage bubbles, ComposeBox, WipBubble, PlanPendingBubble, HarnessTasksPanel — all render identically to pre-Phase-10.
      17. **Session persistence.** Click identity A. Click identity B. Click back to A. A's live connection is preserved (no reconnect). Terminal buffer intact, ComposeBox draft intact.
      18. **RDP tab lifecycle.** Click an RDP row. RDP session opens as before. Click back to an identity row. Click back to RDP. Same behavior as pre-Phase-10.
      19. **Fleet-native rows.** On a fresh page-load with no local tabs, verify identity rows for fleet-discovered tmux sessions appear (Phase 7 behavior). Clicking transparently attaches.

      ## Optional polish checks (YELLOW = deploy but log for follow-up)
      20. **Selected-row hue matches identity chip color.** For 2-3 identities, verify the row's selected-state glow color visually matches the identity's colorHue (same hue driving the pretty-view assistant bubble).
      21. **Chevron animation smoothness.** Toggle sidebar rapidly. Chevron rotation is smooth, no jank.
      22. **Empty-state glass card sits vertically centered** in the scroll region regardless of viewport height.

      ## Sign-off

      | Item | Status | Ashley notes |
      |------|--------|--------------|
      | 1-19 (non-negotiable) | ⬜ | |
      | 20-22 (polish) | ⬜ | |

      **Ashley signature:** ______________  **Date:** ______________
      **Rollback triggered?** Y / N
      ```

    File name: `10-UAT-CHECKLIST.md` in the phase directory.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && test -f .planning/phases/10-pretty-conversations-visual-language-rework/10-UAT-CHECKLIST.md &amp;&amp; grep -c "Non-negotiable\|persistent top-left\|Swipe-left pin\|Empty state\|Ashley signature" .planning/phases/10-pretty-conversations-visual-language-rework/10-UAT-CHECKLIST.md</automated>
  </verify>
  <done>10-UAT-CHECKLIST.md exists with 19 non-negotiable items + 3 polish items + sign-off table; references the specific design details (48/40px, hue-ring, chevron rotation, swipe threshold, RDP no-swipe).</done>
</task>

<task type="auto">
  <name>Task 3: Draft 10-PATCHES-MD-ENTRY.md — patch #128 pin-ready draft</name>
  <files>.planning/phases/10-pretty-conversations-visual-language-rework/10-PATCHES-MD-ENTRY.md</files>
  <action>
    Author `10-PATCHES-MD-ENTRY.md` in the format Tina uses for skynet-patches.md entries. Model the shape on the recent patch #123 entry (`260722-g2x` quick task's paragraph in STATE.md is a close reference) — dense paragraph, mechanically specific, ends with deploy status.

    Structure:
      ```
      # Patch #128 — Pretty-Conversations visual-language rework (Phase 10)

      **Paste target:** ~/.claude/identities/tina/skynet-patches.md
      **Paste timing:** After Ashley greenlights the batched #123-#128 deploy AND UAT passes AND deadman disarms
      **Batch context:** Stacks on the pending #123-#127 stack (drafts belt-and-suspenders localStorage mirror, compose-box stop button, send-button removal, meter reset polish, paperclip decouple, ThumbsUp rename, Skynet rebrand + PWA, PWA safe-area polish). Single build/deploy behind one 15-min deadman rollback.

      ---

      ## Draft (paste-ready)

      **Patch #128 — Pretty-Conversations visual-language rework.** New component tree `src/ui/features/pretty-conversations/` (mirrors Phase 4 pretty-view pattern) — `PrettyConversationRow.tsx` (~[TBD] LOC) + `PrettyConversationsPanel.tsx` (~[TBD] LOC) + shared `PinAction` + tokens.ts — replaces the retiring shadcn-derived `ConversationsPanel` + `ConversationRow` + `NewSessionButton` from Phase 6/7 on BOTH viewports in a single cutover (shape-file lock: no dual-mode ship). Chunky Telegram-style rows: 48px mobile / 40px desktop hue-ring avatar disc (radial-gradient with `hsla(colorHue, 65%, 55%, 0.45)` hue-ring, matches prototype.html design source-of-truth), primary label = session name = identity name (Ashley convention, so no separate IdentityBadge chip), secondary line = Server-glyph + host name. Selected-row treatment mirrors ChatMessage.tsx assistant-bubble class strings VERBATIM (`hsla(hue,50%,38%,0.30)` → `hsla(hue,45%,24%,0.35)` gradient + hue-border + inner glow) adapted for row geometry — inline hsla per Ashley's naming rule (no new CSS vars unless a value is reused 3+ times). Flat list, no "Pinned" or per-host section headers (pin glyph on row IS the marker). RDP-sentinel HostGroup (id `__rdp__`) still renders at the bottom with a subtle "Remote desktop" divider chip + Monitor-glyph avatars, no hue, no pin (T-Test-34 preserved). Empty state uses a PlanPendingBubble-style idle glass card. Mobile pin mechanism: swipe-left on a row past 40px reveals a hue-tinted 48x48 pin/unpin action strip (vertical-swipe tolerance 12px yields to scroll; only one row swiped-open at a time; RDP rows skip the touch listener entirely). Desktop pin: hover-reveal 24x24 button in the right column, always visible on pinned rows (matches production ConversationRow.tsx pattern). New-session affordance downgraded to a compact 34x34 pencil icon in the panel header (no more full-width labeled CTA); NewSessionDialog reused verbatim on submit. AppShell mount site swapped in place (`<ConversationsPanel .../>` → `<PrettyConversationsPanel variant={isTouchDevice ? "mobile" : "desktop"} .../>`) — same prop shape (hostTree, onCreateSession, onConversationSelected, onRailClick, isAdmin, onDetachedRowClick, onRdpRowClick, settingsRowSlot) plus the new `variant` prop. Persistent top-left 32x32 chevron sidebar-toggle added to AppShell root (fixed `top: 8px, left: 8px, z-index: 30`, glass-treatment matching desktop.html mock, rotates 180° when sidebarOpen) — fixes the small-window sidebar-affordance regression Ashley called out; the old narrow-window thin-strip at AppShell:1844-1852 removed (single canonical toggle at all widths). Old files DELETED post-cutover: `ConversationsPanel.tsx` (~430 LOC), `ConversationRow.tsx` (~150 LOC), `NewSessionButton.tsx`. Patch #111e F3-diag console.log spew fully retired (lived inside the deleted panel + one AppShell console.warn at :1483 removed). New test coverage: PrettyConversationRow.test.tsx (11 cases: swipe state machine including angle-tolerance-bails-to-scroll, RDP-no-swipe, hover-reveal desktop pin, selected-state hue interpolation, avatar fallback, e.stopPropagation on pin click, no IdentityBadge in DOM), PrettyConversationsPanel.test.tsx (15 cases: empty state, pinned-first ordering, no "Pinned"/host section headers, RDP-sentinel-at-bottom, variant-based header, header pencil opens dialog, gear desktop-only, settingsRowSlot mobile bottom, row dispatcher routes RDP/fleetOnly/plain correctly, onConversationSelected fires every branch). NewSessionDialog.test.tsx pruned (deleted Test 1 NewSessionButton-in-isolation; Test 10 retargeted to PrettyConversationsPanel). conversation-store.ts + conversation-store.test.ts UNCHANGED (data source, ordering rules, pin persistence, session-persistence contract, RDP-sentinel emission, fleetOnly marker — all preserved verbatim; presentation-only rework). Zero touches to pretty-view / terminal / RDP / guacamole / message-queue / identity-store internals (Phase 10 scope-fence). npm run build clean, tsc clean, N/Y vitest passing (new Phase 10 tests 35/35 green; the 3-4 pre-existing failures documented in STATE.md — patch #121 stale Send-button test refs + patch #124 stale ThumbsUp aria-label refs — remain out of Phase 10 scope per GSD SCOPE BOUNDARY rule). No push, no build image, no deploy — BATCHED with #123-#127 behind Tina's 15-min deadman rollback pending Ashley greenlight on the visual behavior. Design source-of-truth: `/home/ubuntu/.claude/identities/tina/bounties/pretty-conversations-panel-redesign/{prototype.html,desktop.html}` (v0.3 mobile + v0.1 desktop, Ashley signed off 2026-07-22). Commits: [pending; will populate at commit time — expected: one commit per wave x 4 waves + one docs commit, or a single squash — Tina's call at commit time].

      ---

      ## Fill-in placeholders (before pinning)

      Before pasting into skynet-patches.md, replace:
      - `[TBD]` LOC counts — get from `wc -l` on the two new files
      - `N/Y` vitest count — from 10-BUILD-VERIFY-LOG.md's Vitest section
      - Commit SHA(s) — from `git log --oneline` after Wave 5 commits

      ## Batched deploy checklist (Ashley's morning greenlight)

      1. Confirm all commits in the #123-#128 stack are on `feat/tab-title-from-tmux`
      2. `cd /opt/skynet && bash skynet-patches/build-skynet.sh`
      3. `docker compose up -d --force-recreate skynet`
      4. `/opt/skynet/.tmp-revert.sh` armed for 15 minutes (mandatory per DEPLOY DISCIPLINE)
      5. Ashley walks 10-UAT-CHECKLIST.md items 1-19 (mandatory) + 20-22 (optional)
      6. If PASS: disarm deadman + paste the draft above into ~/.claude/identities/tina/skynet-patches.md (with fill-ins) + close bounty `pretty-conversations-panel-redesign`
      7. If FAIL on any item 1-19: deadman auto-rolls at T+15m OR trigger manual rollback; route feedback back to Wave 1/2/3 per failure detail
      ```

    File name: `10-PATCHES-MD-ENTRY.md` in the phase directory.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && test -f .planning/phases/10-pretty-conversations-visual-language-rework/10-PATCHES-MD-ENTRY.md &amp;&amp; grep -c "Patch #128\|Pretty-Conversations\|persistent top-left\|BATCHED with #123-#127\|deadman" .planning/phases/10-pretty-conversations-visual-language-rework/10-PATCHES-MD-ENTRY.md</automated>
  </verify>
  <done>10-PATCHES-MD-ENTRY.md exists as a paste-ready draft; references patch #128, Phase 10 scope, the persistent top-left toggle, the new tests, the deleted files, the F3-diag retirement, the batched #123-#128 hold behind the 15-min deadman; fill-in placeholders documented for LOC counts + vitest count + commit SHAs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A — this phase produces docs only | no code diffs, no attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05-01 | Information Disclosure | patches-md draft leaks operational detail | accept | patches-md is Tina's private identity log; not published; matches existing patch entry format |
| T-10-05-02 | Denial of Service | UAT checklist steps miss a critical regression | mitigate | 19 non-negotiable items cover every user-facing behavior called out in the phase brief non-negotiables + shape-file non-negotiables; polish items are additive |
| T-10-05-SC | Tampering | N/A | accept | no code, no installs |
</threat_model>

<verification>
- 10-BUILD-VERIFY-LOG.md exists with tsc + vitest + build outputs captured, Verdict: PASS
- 10-UAT-CHECKLIST.md exists with 19 non-negotiable items + 3 polish items + sign-off block
- 10-PATCHES-MD-ENTRY.md exists as paste-ready patch #128 draft with fill-in placeholders documented
- NO code changes (this plan modifies only .planning/phases/10-*/*.md)
- NO deploy attempt (deferred to Ashley's morning greenlight per fork DEPLOY DISCIPLINE)
- git diff shows only markdown adds/modifies under .planning/phases/10-*/
</verification>

<success_criteria>
1. Full build + test + tsc PASS logged to 10-BUILD-VERIFY-LOG.md
2. 10-UAT-CHECKLIST.md ready for Ashley's post-deploy walkthrough (19 non-negotiable + 3 polish)
3. 10-PATCHES-MD-ENTRY.md ready to paste into skynet-patches.md at pin time
4. Phase 10 is code-complete on feat/tab-title-from-tmux, batched behind Ashley's morning greenlight on the #123-#128 stack
5. No deploy performed
</success_criteria>

<output>
Create `.planning/phases/10-pretty-conversations-visual-language-rework/10-05-SUMMARY.md` when done. Summary: what shipped (build-verify log + UAT checklist + patches-md draft), the verdict from Task 1 (PASS/FAIL), and a "phase closeout" section confirming Phase 10 is ready for the batched #123-#128 morning deploy greenlight. If Task 1 verdict was FAIL, list the route-back target wave(s) so the orchestrator knows where to loop.
</output>
