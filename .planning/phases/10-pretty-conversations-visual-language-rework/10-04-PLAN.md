---
phase: 10-pretty-conversations-visual-language-rework
plan: 04
type: execute
wave: 4
depends_on: ["10-03"]
files_modified:
  - src/ui/sidebar/ConversationsPanel.tsx
  - src/ui/sidebar/ConversationRow.tsx
  - src/ui/sidebar/NewSessionButton.tsx
  - src/ui/sidebar/NewSessionDialog.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "src/ui/sidebar/ConversationsPanel.tsx DELETED"
    - "src/ui/sidebar/ConversationRow.tsx DELETED"
    - "src/ui/sidebar/NewSessionButton.tsx DELETED"
    - "Any test files exclusively covering the deleted components are also deleted"
    - "Repo-wide grep for imports of the deleted paths returns ZERO source-code references"
    - "The patch #111e F3-diag console.log spew is fully removed — it lived inside ConversationsPanel.tsx (deleted along with the file); this task confirms no F3-diag stragglers survive elsewhere in the repo"
    - "SettingsRow.tsx + renderSettingsMenuItems SURVIVE (reused by PrettyConversationsPanel Wave 2)"
    - "NewSessionDialog.tsx SURVIVES (reused verbatim by PrettyConversationsPanel Wave 2)"
    - "conversation-store.ts + conversation-store.test.ts SURVIVE UNCHANGED"
  artifacts:
    - path: "src/ui/sidebar/ConversationsPanel.tsx"
      provides: "DELETED"
      contains: "MUST NOT EXIST"
    - path: "src/ui/sidebar/ConversationRow.tsx"
      provides: "DELETED"
      contains: "MUST NOT EXIST"
    - path: "src/ui/sidebar/NewSessionButton.tsx"
      provides: "DELETED"
      contains: "MUST NOT EXIST"
  key_links:
    - from: "repo-wide grep"
      to: "zero source imports of deleted files"
      via: "post-deletion verification"
      pattern: "@/sidebar/ConversationsPanel|@/sidebar/ConversationRow|@/sidebar/NewSessionButton"
---

<objective>
Delete the retired ConversationsPanel + ConversationRow + NewSessionButton files, prune the one NewSessionButton-in-isolation test from NewSessionDialog.test.tsx, then verify repo-wide that no source imports the deleted paths and that no F3-diag telemetry survives. Runs ONLY after Wave 3's human-verify checkpoint approves the cutover.

Purpose: Enforce the "delete old files after cutover" non-negotiable from the phase brief. Prevents the "two panels living in the sidebar/ tree" confusion tax on future refactors. Fully retires patch #111e's diagnostic telemetry.

Output: Three source files removed; NewSessionDialog.test.tsx pruned of the NewSessionButton-in-isolation test; grep confirms no remaining references; tsc-clean; existing test suite still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-pretty-conversations-visual-language-rework/10-03-SUMMARY.md

# Files being deleted
@src/ui/sidebar/ConversationsPanel.tsx
@src/ui/sidebar/ConversationRow.tsx
@src/ui/sidebar/NewSessionButton.tsx

# Files being PRESERVED (verify not accidentally deleted)
@src/ui/sidebar/NewSessionDialog.tsx
@src/ui/sidebar/SettingsRow.tsx
@src/ui/state/conversation-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the retired files</name>
  <files>src/ui/sidebar/ConversationsPanel.tsx, src/ui/sidebar/ConversationRow.tsx, src/ui/sidebar/NewSessionButton.tsx</files>
  <action>
    Execute the deletions via the bash tool:
      1. `rm src/ui/sidebar/ConversationsPanel.tsx`
      2. `rm src/ui/sidebar/ConversationRow.tsx`
      3. `rm src/ui/sidebar/NewSessionButton.tsx`

    Before each `rm`, run `ls` on the file to verify it exists. If any is already gone (previous partial run), skip that specific `rm` and continue with the rest.

    Check for exclusive test files with `find src/ui/sidebar -name "ConversationsPanel.test.*" -o -name "ConversationRow.test.*" -o -name "NewSessionButton.test.*" 2>/dev/null`. If any results appear, delete them too (Wave 0 discovery confirmed none exist, but this is defensive).

    Do NOT delete:
      - `src/ui/sidebar/NewSessionDialog.tsx` — reused verbatim by the new panel (Wave 2 imports it)
      - `src/ui/sidebar/NewSessionDialog.test.tsx` — Tests 2-9 still cover the reused dialog; Test 10 was retargeted in Wave 3; only Test 1 (NewSessionButton in isolation) will be pruned in Task 2 below
      - `src/ui/sidebar/SettingsRow.tsx` — `renderSettingsMenuItems` + `SettingsRow` both consumed by the new panel + AppShell mobile mount
      - Any file under `src/ui/state/conversation-store*`
      - Any file under `src/ui/features/pretty-conversations/` (Wave 1 + Wave 2 output)
  </action>
  <verify>
    <automated>cd /home/ubuntu/termix && ls src/ui/sidebar/ConversationsPanel.tsx src/ui/sidebar/ConversationRow.tsx src/ui/sidebar/NewSessionButton.tsx 2>&1 | grep -c "No such file"</automated>
  </verify>
  <done>All 3 files no longer exist on disk; `ls` returns "No such file" for each (grep count = 3); SettingsRow.tsx + NewSessionDialog.tsx + conversation-store.ts all still exist.</done>
</task>

<task type="auto">
  <name>Task 2: Prune NewSessionButton-in-isolation test from NewSessionDialog.test.tsx</name>
  <files>src/ui/sidebar/NewSessionDialog.test.tsx</files>
  <action>
    Locate the `describe("NewSessionButton", ...)` block at line 108-116 of `src/ui/sidebar/NewSessionDialog.test.tsx` (post-Wave-3 line numbers may drift by a few lines).

    Changes:
      1. Delete the `import { NewSessionButton } from "./NewSessionButton";` line at line 37.
      2. Delete the entire `describe("NewSessionButton", ...)` block (Test 1) — starts at line 108, ends at the closing `});` around line 116.
      3. Delete the "Test 1:" section header comment block just above the describe (lines ~105-107).

    Verify surviving Vitest test count in this file is still ≥ 8 (Tests 2-9 + retargeted Test 10 = 9 total). Rationale for keeping Test 10 (Wave 3): "pencil precedes rows in DOM" is meaningful smoke coverage for future panel refactors.

    Do not touch Tests 2-9 (they cover NewSessionDialog directly, which still exists and is reused by the new panel).

    Rationale for pruning Test 1 (NOT retargeting it): the inline pencil-icon button in the new panel is trivial glue code (onClick prop → local setState) already covered by `PrettyConversationsPanel.test.tsx` Test 5 (Wave 2). Adding a fourth "pencil click fires dialog open" assertion in a file dedicated to the dialog+button pair is redundant.
  </action>
  <verify>
    <automated>cd /home/ubuntu/termix && grep -c "NewSessionButton" src/ui/sidebar/NewSessionDialog.test.tsx && npx vitest run src/ui/sidebar/NewSessionDialog.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <done>grep for `NewSessionButton` in NewSessionDialog.test.tsx returns 0 (import + describe removed); Vitest run reports ≥ 9 tests, all passing.</done>
</task>

<task type="auto">
  <name>Task 3: Repo-wide grep verification — no stragglers, no F3-diag survivors</name>
  <files>N/A (verification only, no file modifications unless a straggler is found)</files>
  <action>
    Run each of the following greps and capture the output:
      1. `grep -rn "from \"@/sidebar/ConversationsPanel\"" src` — expect 0 matches
      2. `grep -rn "from \"@/sidebar/ConversationRow\"" src` — expect 0 matches
      3. `grep -rn "from \"@/sidebar/NewSessionButton\"" src` — expect 0 matches
      4. `grep -rn "F3-diag" src` — expect 0 matches (all F3-diag spew was inside the deleted panel + Wave 3 removed the AppShell one)
      5. `grep -rn "\"@/sidebar/ConversationsPanel\"\\|\"@/sidebar/ConversationRow\"\\|\"@/sidebar/NewSessionButton\"" src` — catches alternate import styles (`import "..."`, `import type ... from "..."`, dynamic `import("...")`), expect 0 matches

    If ANY grep returns non-zero source-code hits, do NOT proceed:
      - Investigate the straggler.
      - If it's a stale import in a test/dev file: update it to reference the new PrettyConversationsPanel / PrettyConversationRow equivalents, OR delete the stale test if the coverage moved to `src/ui/features/pretty-conversations/*.test.tsx`.
      - Re-run the grep suite until all four report 0.

    Comment-only references (`//` or `/* */` mentioning the retired component names inside surviving files) are ACCEPTABLE — code is the source of truth; comments are historical annotation. Example acceptable straggler: `src/ui/state/conversation-store.ts` comments referencing "ConversationsPanel special-cases `__rdp__`" — this describes the panel's contract with the store; the new PrettyConversationsPanel honors the same contract; comment stays as history.

    Final verification: run the full test suite once (`npx vitest run 2>&1 | tail -20`) and confirm no unexpected failures. Pre-existing failures documented in STATE.md (patch #121 stale Send-button refs, patch #124 stale "yes"→"let's go" ThumbsUp refs) are ACCEPTABLE and out of scope for this phase — do not attempt to fix them here (planner scope-boundary rule).

    Run `npx tsc --noEmit 2>&1 | tail -10` and confirm no new type errors introduced by the deletions.
  </action>
  <verify>
    <automated>cd /home/ubuntu/termix && grep -rn "from \"@/sidebar/ConversationsPanel\"\|from \"@/sidebar/ConversationRow\"\|from \"@/sidebar/NewSessionButton\"" src | grep -v '^#' | wc -l && grep -rn "F3-diag" src | wc -l && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>All 4 greps (imports of the 3 deleted files + F3-diag) return 0 source-code hits; tsc-clean; full test suite either green OR fails only on pre-existing failures already documented in STATE.md (patch #121, patch #124 residual).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file deletion → build system | tsc + vitest verify no orphaned imports; grep gates confirm no source references |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Denial of Service | orphaned import causing build failure | mitigate | grep verification in Task 3 catches any straggler before commit; tsc check confirms no dangling references |
| T-10-04-02 | Information Disclosure | F3-diag console.log leaking row identifiers to browser console | mitigate | F3-diag telemetry fully retired with deleted panel; Task 3 grep confirms zero survivors |
| T-10-04-SC | Tampering | npm installs | mitigate | zero installs; only file deletions |
</threat_model>

<verification>
- `src/ui/sidebar/ConversationsPanel.tsx` does not exist on disk
- `src/ui/sidebar/ConversationRow.tsx` does not exist on disk
- `src/ui/sidebar/NewSessionButton.tsx` does not exist on disk
- `src/ui/sidebar/NewSessionDialog.tsx` STILL exists (Wave 2 imports it)
- `src/ui/sidebar/SettingsRow.tsx` STILL exists (Wave 2 imports it)
- `src/ui/state/conversation-store.ts` STILL exists unchanged
- Grep confirms zero production imports of the deleted paths
- Grep confirms zero F3-diag references anywhere in src/
- Full test suite green (modulo pre-existing failures documented in STATE.md)
- tsc-clean
</verification>

<success_criteria>
1. Three retired files removed from disk
2. NewSessionDialog.test.tsx pruned of NewSessionButton isolation test (Tests 2-10 survive)
3. Repo-wide grep returns zero source-code references to the deleted paths
4. F3-diag telemetry fully retired (grep for "F3-diag" returns 0 in src/)
5. Preserved files (NewSessionDialog, SettingsRow, conversation-store, PrettyConversationsPanel, PrettyConversationRow) all still exist and function
6. tsc-clean and existing test suite green (modulo pre-existing failures)
</success_criteria>

<output>
Create `.planning/phases/10-pretty-conversations-visual-language-rework/10-04-SUMMARY.md` when done. Summary: what shipped (3 deletions + test prune + grep verification), any stragglers found and how they were resolved, confirmation that F3-diag is fully retired, and a "handoff to Wave 5" section noting the code-complete state ready for build-verify + UAT + patch #128 draft.
</output>
