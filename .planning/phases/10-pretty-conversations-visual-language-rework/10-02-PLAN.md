---
phase: 10-pretty-conversations-visual-language-rework
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "PrettyConversationsPanel renders a flat list from conversation-store's derived output (useConversations + useSelectedConversationId + usePinnedIds) — no store reshape"
    - "Pinned rows sit at the top with pin glyph as the only marker (NO 'Pinned' section header)"
    - "Grouped identity-tmux rows follow (NO per-host semibold headers — flat list per Ashley/prototype lock)"
    - "RDP-sentinel HostGroup (hostId '__rdp__') renders at the bottom with a subtle 'Remote desktop' divider chip; RDP rows render as PrettyConversationRow with `rdpHostRow: true` so pin/swipe are skipped by the row itself (Wave 1 contract)"
    - "Empty state uses a PlanPendingBubble-style idle glass card centered in the scroll region (no more MessagesSquare icon + 'No active conversations' minimal text)"
    - "Header carries the desktop label ('Conversations') + compact pencil-icon button + gear (desktop-only) OR just the compact pencil (mobile). Pencil opens the existing NewSessionDialog verbatim"
    - "Mobile variant coordinates swipe-open state across rows: only one row swiped-open at a time; opening a new one auto-closes the previous; selecting any row auto-closes the current swiped row"
    - "Panel accepts the same prop shape ConversationsPanel had — hostTree, onCreateSession, onConversationSelected, onRailClick, isAdmin, onDetachedRowClick, onRdpRowClick, settingsRowSlot — plus one NEW prop: `variant: 'mobile' | 'desktop'` (drives row variant + header layout branching)"
  artifacts:
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "Flat conversation-list panel wrapping PrettyConversationRow; header with pencil/gear; empty state; RDP-sentinel handling; NewSessionDialog wiring"
      exports: ["PrettyConversationsPanel"]
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx"
      provides: "Vitest coverage — empty state, pinned-first ordering, RDP-sentinel-at-bottom, variant-based header, swipe-coordination across rows, header pencil opens dialog"
      contains: "describe(\"PrettyConversationsPanel"
  key_links:
    - from: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      to: "src/ui/state/conversation-store.ts"
      via: "useConversations, useSelectedConversationId, usePinnedIds, selectConversation, togglePinConversation imports (VERBATIM store consumption — no reshape)"
      pattern: "useConversations|useSelectedConversationId|usePinnedIds|selectConversation|togglePinConversation"
    - from: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      to: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx"
      via: "renders one row per pinned + grouped entry (except RDP sentinel), passing variant prop"
      pattern: "PrettyConversationRow"
    - from: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      to: "src/ui/sidebar/NewSessionDialog.tsx"
      via: "reused VERBATIM for the pencil-icon new-session flow (no redesign of the dialog itself)"
      pattern: "NewSessionDialog"
    - from: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      to: "src/ui/sidebar/SettingsRow.tsx"
      via: "renderSettingsMenuItems export reused VERBATIM for the desktop gear dropdown"
      pattern: "renderSettingsMenuItems"
---

<objective>
Build the flat-list panel that composes Wave 1's `PrettyConversationRow` into a full conversation list matching the prototype's visual language: flat rows (no "Pinned" header, no per-host headers), RDP-sentinel divider at bottom, PlanPendingBubble-style empty state, compact pencil-icon header (mobile) OR label + pencil + gear header (desktop). Preserves the entire prop contract of the retiring `ConversationsPanel` so the Wave 3 AppShell cutover is a mount-site swap, not a rewire. Adds a `variant: "mobile" | "desktop"` prop that drives both the header layout AND the child rows' pin mechanism. Consumes the conversation-store output verbatim — pinned + grouped + selected + pinnedIds — no store reshape.

Purpose: The panel is the composition layer. Wave 1 owns the visual language of the row; this plan owns the visual language of the list: flat, section-header-less, RDP at bottom with a subtle chip, warm idle empty state, one canonical NewSessionDialog. The panel is NOT yet mounted in AppShell — Wave 3 does that.

Output: `PrettyConversationsPanel.tsx` + `PrettyConversationsPanel.test.tsx`. tsc-clean. All tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-pretty-conversations-visual-language-rework/10-01-SUMMARY.md

# Design source-of-truth (LOCKED)
@/home/ubuntu/.claude/identities/tina/bounties/pretty-conversations-panel-redesign/prototype.html
@/home/ubuntu/.claude/identities/tina/bounties/pretty-conversations-panel-redesign/desktop.html

# Files to model against / reuse verbatim
@src/ui/sidebar/ConversationsPanel.tsx
@src/ui/sidebar/NewSessionDialog.tsx
@src/ui/sidebar/SettingsRow.tsx
@src/ui/features/pretty-view/PlanPendingBubble.tsx
@src/ui/state/conversation-store.ts
@src/ui/features/pretty-conversations/PrettyConversationRow.tsx
@src/ui/features/pretty-conversations/PinAction.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build PrettyConversationsPanel with variant prop and swipe coordination</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx</files>
  <behavior>
    Props (drop-in for ConversationsPanel's shape + `variant`):
      - `variant: "mobile" | "desktop"` — NEW, drives header + row-variant
      - `onRailClick?: (view: RailView) => void`
      - `isAdmin?: boolean`
      - `onConversationSelected?: (id: string) => void`
      - `settingsRowSlot?: ReactNode`
      - `hostTree?: HostFolder | null`
      - `onCreateSession?: (opts: { host: Host; sessionName?: string }) => void`
      - `onDetachedRowClick?: (row: ConversationRowShape) => void`
      - `onRdpRowClick?: (row: ConversationRowShape) => void`

    Store subscriptions (VERBATIM from ConversationsPanel.tsx lines 122-125):
      - `const { pinned, grouped } = useConversations();`
      - `const selectedId = useSelectedConversationId();`
      - `const pinnedIds = usePinnedIds();`

    Header rules:
      - `variant === "desktop"`: title text "Conversations" (i18n `nav.conversations.title` verbatim from existing i18n) on the left; right side = pencil-icon button + gear dropdown. Gear is gated on `typeof onRailClick === "function"` (same shape as ConversationsPanel.tsx line 138, but the touch-device gate is now inherent in `variant === "desktop"`).
      - `variant === "mobile"`: no title text (prototype has an empty-text top-strip on mobile per bounty); right side = pencil-icon button ONLY. NO gear (mobile settings live in `settingsRowSlot` at the bottom of the scroller).
      - Pencil button: 34x34 circular button with lucide `Pencil` icon size-4, hover:bg-white/[0.08]. Style mirrors prototype.html lines 111-131 (`.pencil-btn` block). Only rendered when `typeof onCreateSession === "function"` (matches ConversationsPanel.tsx line 139 gate); opens local `newSessionDialogOpen` state.
      - Gear (desktop only): 34x34 rounded button with lucide `Settings` icon, wraps `<DropdownMenu>` from `@/components/dropdown-menu` containing `renderSettingsMenuItems({ onRailClick, isAdmin, t })` (VERBATIM reuse from SettingsRow.tsx export). Tooltip wrapper matches ConversationsPanel.tsx lines 193-223.

    Row-click dispatcher (VERBATIM behavior from ConversationsPanel.tsx lines 153-183, MINUS the `[F3-diag]` console.log spew which is being retired):
      - `handleRowSelect(row)`:
        1. If `row.rdpHostRow && onRdpRowClick` → `onRdpRowClick(row); onConversationSelected?.(row.id); return;`
        2. Else if `row.fleetOnly && onDetachedRowClick` → `onDetachedRowClick(row); onConversationSelected?.(row.id); return;`
        3. Else → `selectConversation(row.id); onConversationSelected?.(row.id);`
      - NO console.log — the F3-diag telemetry is being deleted along with the old panel (Wave 4).

    Swipe coordination (mobile variant only):
      - Local state `currentlySwipedId: string | null`.
      - Every row receives an `onSwipeOpenChange?: (open: boolean) => void` callback. When a row reports `open === true`, panel sets `currentlySwipedId = row.id`. Panel force-closes any previously-open row via a `forceClosed?: boolean` prop (Wave 1 must expose this — noted in 10-01-SUMMARY handoff; if not present, panel falls back to a key-based remount which is uglier).
      - When `handleRowSelect` fires, always set `currentlySwipedId = null` (tap-body-closes-swipe semantic — but the row's OWN onSelect handler already suppresses onSelect when swipedOpen; this panel-side reset is defense-in-depth for the case where two rows are open due to a race).
      - Alternative if Wave 1 chose a different coordination surface: use whatever API the row exposes; contract is "only one row open at a time, panel is the coordinator".

    Row rendering — flat list, no section headers:
      - `pinned.map(row => <PrettyConversationRow key={row.id} row={row} selected={row.id === selectedId} pinned={true} variant={variant} onSelect={() => handleRowSelect(row)} onTogglePin={() => togglePinConversation(row.id)} ... />)`.
      - `grouped.forEach(group => {...})` — for each group, if `group.hostId === "__rdp__"`, emit the RDP divider chip + RDP rows; else emit the group's rows FLAT (no per-host header at all).
      - RDP divider chip: small `<div>` above the RDP row cluster with lucide `Monitor` icon size-3 + "Remote desktop" text (i18n `nav.conversations.rdpSection` with defaultValue `"Remote desktop"`) + a gradient rule (prototype.html lines 346-366 `.rdp-divider` block). Rendered ONCE above the RDP rows regardless of how many RDP rows exist.
      - RDP rows are still rendered via `<PrettyConversationRow ... />` — the row's `data-rdp-host-row="true"` attribute (from Wave 1) suppresses the pin swipe/hover intrinsically. The row STILL uses variant + selected + host + label. The `pinned` prop is always `false` for RDP rows.

    Empty state (when `pinned.length === 0 && grouped.length === 0`):
      - PlanPendingBubble-style glass card centered in the scroll region. Adapt PlanPendingBubble's className (PlanPendingBubble.tsx lines 44-54) — use the neutral no-identity variant (blue-gray gradient like ChatMessage.tsx user bubble lines 105-115) since there's no identity hue to reference. Contents: small `MessagesSquare` glyph + text "No conversations yet" (i18n `nav.conversations.empty` with new defaultValue — update from existing "No active conversations" copy to match the pretty-view idle-card tone).
      - Vertically centered in the scroll region (flex-1 min-h-0 items-center justify-center wrapper).

    NewSessionDialog mount:
      - VERBATIM from ConversationsPanel.tsx lines 358-368 — dialog controlled by local `newSessionDialogOpen`, gated on `typeof onCreateSession === "function"`, `onCreate` fires `onCreateSession(opts)` + closes dialog.

    Container shape:
      - Outer: `<div className="relative flex flex-col flex-1 min-h-0 overflow-hidden">` (same size contract as ConversationsPanel.tsx line 186 so it drops into the existing sidebar-panel-content slot).
      - Header: `<div className="shrink-0 flex items-center justify-between px-3 py-2 border-b border-white/[0.06]">` (variant-agnostic hairline treatment matching prototype.html `.top-strip`).
      - Scroll region: `<div className="flex-1 min-h-0 overflow-y-auto pb-[env(safe-area-inset-bottom)]">` (patch #126's safe-area padding preserved).
      - settingsRowSlot rendered at BOTTOM of scroll region (below the last group), same position as ConversationsPanel.tsx line 348.
  </behavior>
  <action>
    Create `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`.

    Imports: `useState`, `type ReactNode` from `react`; `MessagesSquare`, `Monitor`, `Pencil`, `Settings` from `lucide-react`; `useTranslation` from `react-i18next`; `DropdownMenu`, `DropdownMenuContent`, `DropdownMenuTrigger` from `@/components/dropdown-menu`; `Tooltip`, `TooltipContent`, `TooltipProvider`, `TooltipTrigger` from `@/components/tooltip`; `useConversations`, `useSelectedConversationId`, `usePinnedIds`, `selectConversation`, `togglePinConversation`, `type ConversationRow as ConversationRowShape` from `@/state/conversation-store`; `renderSettingsMenuItems` from `@/sidebar/SettingsRow`; `NewSessionDialog` from `@/sidebar/NewSessionDialog`; `PrettyConversationRow` from `./PrettyConversationRow`; `type RailView` from `@/sidebar/AppRail`; `type Host, HostFolder` from `@/types/ui-types`.

    Do NOT import `NewSessionButton` — the pencil-icon button is inline (compact 34x34 icon button, not a full-width labeled CTA). Do NOT import from `@/sidebar/ConversationsPanel` or `@/sidebar/ConversationRow` — those are being retired in Wave 4.

    Implementation ordering:
      1. Destructure props + variant + local state (`newSessionDialogOpen`, `currentlySwipedId` for mobile).
      2. Store subscriptions (identical shape to ConversationsPanel).
      3. `isEmpty`, `showGear` (variant === "desktop" && typeof onRailClick === "function"), `showNewSessionButton` (typeof onCreateSession === "function") derived flags.
      4. `handleRowSelect` (dispatcher, NO console.log spew).
      5. `handleSwipeOpenChange(rowId, open)` — coordinates `currentlySwipedId`.
      6. Return JSX: outer wrapper → header (variant-branched) → scroll region → (empty state OR pinned+grouped rows) → settingsRowSlot → sibling NewSessionDialog.

    Selected-row coloring is entirely the row's responsibility (Wave 1); this panel does not apply any hue treatment to the row-list wrapper.

    RDP row: verify that the row's `pinned` prop is always passed as `false` (RDP rows are never pinned). Verify `onTogglePin` is a no-op arrow (defensive — should never fire from an RDP row per Wave 1's contract, but pass a no-op anyway).

    The panel should have NO console.log or console.warn statements. Patch #111e F3-diag was scoped to the old panel; Wave 4 removes it, this plan does not port it forward.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx tsc --noEmit 2>&1 | grep "src/ui/features/pretty-conversations/PrettyConversationsPanel" | head -5</automated>
  </verify>
  <done>PrettyConversationsPanel.tsx compiles clean; grep for `console.log\|console.warn` in the file returns zero (no F3-diag spew ported forward); grep confirms `useConversations` + `useSelectedConversationId` + `usePinnedIds` all imported from `@/state/conversation-store`; grep confirms `NewSessionDialog` imported from `@/sidebar/NewSessionDialog` (verbatim reuse); grep confirms zero references to `NewSessionButton` (retired affordance replaced by inline compact pencil).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vitest coverage — empty state, ordering, RDP-sentinel, variant header, swipe coordination, header pencil</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx</files>
  <behavior>
    Test suite `PrettyConversationsPanel`. Model the mock block on `NewSessionDialog.test.tsx` verbatim: mock react-i18next passthrough, mock session-hue + identities-store to inert defaults, mock use-is-touch-device (unused by this panel — variant is a prop — but the row imports it).

    Fixture helpers:
      - `makeHost(id, name, overrides)` — reuse the pattern from NewSessionDialog.test.tsx lines 47-79.
      - `makeConversationRow(overrides)` — builds a `ConversationRowShape` for direct panel-store fixtures.
      - `setStoreSnapshot({ pinned, grouped, selectedId, pinnedIds })` — helper that calls `updateOpenTabs` + `updateFleetSessions` + `updateHostTree` + `pinConversation` from `@/state/conversation-store` to produce the desired derived output. OR, cleaner: mock `useConversations`/`useSelectedConversationId`/`usePinnedIds` at module level with `vi.mock("@/state/conversation-store", ...)` so tests inject snapshots directly. Choose the mock-at-module approach — it's what NewSessionDialog.test.tsx does for adjacent modules and it's dramatically simpler than driving the real store.

    Tests:
      1. **Test 1 — Empty state renders idle glass card.** Mock store to `{ pinned: [], grouped: [] }`. Render desktop variant. Assert queryByText(/no conversations yet/i) exists. Assert the card wrapper has class matching a gradient background (glass-card indicator). Assert NO PrettyConversationRow instances rendered.
      2. **Test 2 — Pinned rows render at top, grouped rows below.** Mock store to `{ pinned: [row("a"), row("b")], grouped: [{ hostId: "h1", hostName: "hostA", rows: [row("c"), row("d")] }], selectedId: null, pinnedIds: new Set(["a","b"]) }`. Render desktop. Query all `[data-conversation-id]` in DOM order; assert order is `["a","b","c","d"]`.
      3. **Test 3 — No 'Pinned' section header rendered.** Same fixture as Test 2. Grep container.innerHTML for the string "Pinned" (case-insensitive header text). Assert absent (pin glyph on the row is the only marker). Also assert no per-host semibold header rendered — grep container.innerHTML for "hostA" — assert absent (host name only appears as row-secondary-line text, not as a section header).
      4. **Test 4 — RDP-sentinel HostGroup renders at the bottom with a 'Remote desktop' divider.** Mock store `grouped: [{ hostId: "h1", hostName: "hostA", rows: [row("a")] }, { hostId: "__rdp__", hostName: "", rows: [row("r1", { rdpHostRow: true, label: "GIGAASHLEYPC" })] }]`. Render desktop. Assert queryByText(/remote desktop/i) exists. Assert the RDP row `[data-rdp-host-row="true"]` appears AFTER the identity-tmux row in DOM order.
      5. **Test 5 — Header pencil opens NewSessionDialog.** Render with `onCreateSession={vi.fn()}` + hostTree fixture. Click the pencil button (query by aria-label /new session/i). Assert NewSessionDialog is visible (queryByRole("dialog") — the actual dialog opens via the local newSessionDialogOpen state).
      6. **Test 6 — Header pencil not rendered when onCreateSession is undefined.** Render without onCreateSession. Assert pencil button is absent (queryByRole("button", { name: /new session/i }) returns null).
      7. **Test 7 — Desktop header shows title 'Conversations'.** Render desktop variant. Assert queryByText(/^conversations$/i) exists in the header region.
      8. **Test 8 — Mobile header omits title text.** Render mobile variant. Assert no visible "Conversations" title text in the header; still assert pencil button present when onCreateSession is wired.
      9. **Test 9 — Desktop gear renders when onRailClick provided.** Render desktop with onRailClick={vi.fn()}, isAdmin=false. Assert gear button (aria-label /settings/i) exists.
      10. **Test 10 — Mobile gear is never rendered even when onRailClick is provided.** Render mobile with onRailClick={vi.fn()}. Assert queryByRole("button", { name: /settings/i }) is null. Mobile settings live in the settingsRowSlot.
      11. **Test 11 — settingsRowSlot renders at bottom of scroll region on mobile.** Render mobile with `settingsRowSlot={<div data-testid="settings-slot">SETTINGS</div>}`. Query by testid; assert present; also assert it appears AFTER all row entries in DOM order (compare compareDocumentPosition against any [data-conversation-id]).
      12. **Test 12 — Row click routes RDP → onRdpRowClick.** Fixture with one RDP row. Click it. Assert onRdpRowClick called with the row; onDetachedRowClick NOT called; selectConversation NOT called.
      13. **Test 13 — Row click routes fleetOnly → onDetachedRowClick.** Fixture with one fleetOnly row. Click it. Assert onDetachedRowClick called with the row; onRdpRowClick NOT called.
      14. **Test 14 — Row click on plain row calls selectConversation.** Fixture with one plain openTabs-derived row. Mock `selectConversation` via `vi.mock("@/state/conversation-store", ...)`. Click. Assert selectConversation called with row.id.
      15. **Test 15 — onConversationSelected fires after every branch of the dispatcher.** Wire a spy for onConversationSelected. Click a plain row → spy called. Click a fleetOnly row → spy called. Click an RDP row → spy called. Each with the row's own id.
  </behavior>
  <action>
    Create `src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx`.

    Use `vi.mock("@/state/conversation-store", () => ({ ... }))` at module level with a mutable `let snapshot = { pinned: [], grouped: [], selectedId: null, pinnedIds: new Set() }` and expose a `setSnapshot` helper. Mock hooks return the current snapshot. Mock `selectConversation`, `togglePinConversation` as `vi.fn()`. Re-export the `ConversationRow` type via `type ConversationRow = { id, type, label, host, targetTmuxSession, fleetOnly?, rdpHostRow? }`.

    Import `render`, `fireEvent`, `within` from `@testing-library/react`; `describe`, `it`, `expect`, `vi`, `beforeEach` from `vitest`. Import `PrettyConversationsPanel` from `./PrettyConversationsPanel`. Import `type Host, HostFolder` from `@/types/ui-types` (matching NewSessionDialog.test.tsx pattern).

    For Test 4's DOM-order assertion, use `compareDocumentPosition` on the two elements (Following bit = 4).

    For Test 5's dialog-open assertion, note the NewSessionDialog uses shadcn's Dialog which renders inside a portal — `queryByRole("dialog")` should still find it since testing-library queries the body.

    `beforeEach(() => { vi.clearAllMocks(); setSnapshot({ pinned: [], grouped: [], selectedId: null, pinnedIds: new Set() }); });`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>All 15 tests pass; tsc-clean; test file does not import from `@/sidebar/ConversationsPanel` or `@/sidebar/ConversationRow` (those files are still on disk, but the new panel MUST NOT link to them).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conversation-store output → panel render | store output is trusted (existing invariant from Phase 6/7) |
| user click on pencil → local dialog state → NewSessionDialog | dialog itself is retired-file-untouched surface (Wave 4 does not touch NewSessionDialog) — reused verbatim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Denial of Service | many rows in scroll region | accept | conversation-store output has been performance-tested through Phases 6+7; no new perf surface introduced |
| T-10-02-02 | Information Disclosure | RDP row rendering | mitigate | row.host.name is the only host-identifying data emitted; matches existing ConversationsPanel.tsx RDP row rendering — no new exposure |
| T-10-02-SC | Tampering | npm installs | mitigate | zero new dependencies; all imports come from existing installed packages |
</threat_model>

<verification>
- tsc clean in `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`
- All 15 Vitest cases pass
- Grep confirms `useConversations`, `useSelectedConversationId`, `usePinnedIds`, `selectConversation`, `togglePinConversation` all imported from `@/state/conversation-store` (no store reshape)
- Grep confirms zero `console.log` / `console.warn` in the new panel (F3-diag not ported)
- Grep confirms `NewSessionDialog` imported from `@/sidebar/NewSessionDialog` (verbatim reuse)
- Grep confirms `renderSettingsMenuItems` imported from `@/sidebar/SettingsRow` (verbatim reuse)
- Panel does NOT import `NewSessionButton` from `@/sidebar/NewSessionButton` (inline compact pencil replaces the labeled CTA)
- No changes to `src/ui/AppShell.tsx` or `src/ui/sidebar/*` in this plan
</verification>

<success_criteria>
1. `PrettyConversationsPanel.tsx` renders a flat list — pinned rows first, grouped rows next (no per-host semibold header), RDP sentinel with "Remote desktop" divider chip at bottom
2. Empty state renders as a PlanPendingBubble-style glass card ("No conversations yet")
3. Desktop header: title "Conversations" + pencil + gear (when onRailClick provided)
4. Mobile header: no title text; pencil only
5. Header pencil opens the same NewSessionDialog used by the old panel (verbatim reuse)
6. Row-click dispatcher matches ConversationsPanel.tsx behavior for rdpHostRow / fleetOnly / plain rows — MINUS the F3-diag console.log
7. Swipe coordination: only one row swiped-open at a time in mobile variant (tested via row events + panel state)
8. `npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` reports 15/15 green
9. `npx tsc --noEmit` reports no new errors
</success_criteria>

<output>
Create `.planning/phases/10-pretty-conversations-visual-language-rework/10-02-SUMMARY.md` when done. Summary: what shipped (panel + flat-list rendering + variant header + swipe coordination + tests), any deviations, and a "handoff to Wave 3" section listing the AppShell.tsx call sites Wave 3 will change (the `<ConversationsPanel ... />` at line 1403 in each mount branch — 3 mount branches total per lines 1777/1801/1823).
</output>
