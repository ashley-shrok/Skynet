---
phase: 10-pretty-conversations-visual-language-rework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-conversations/tokens.ts
  - src/ui/features/pretty-conversations/PinAction.tsx
  - src/ui/features/pretty-conversations/PrettyConversationRow.tsx
  - src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "PrettyConversationRow renders a chunky Telegram-style row with an identity-hue avatar disc and hue-ring, primary label + host-name secondary line"
    - "Selected rows carry the ChatMessage.tsx assistant-bubble hue-gradient + hue-border + inner glow treatment (verbatim class strings, adapted for row geometry)"
    - "The same component drives mobile (~72px min-h, 48px avatar) and desktop (~62px min-h, 40px avatar) via a `variant: 'mobile' | 'desktop'` prop"
    - "Mobile variant: swipe-left past 40px reveals a right-anchored 48x48 hue-tinted PinAction; snap-shut on tap-body, swipe-right, vertical gesture (>12px), or when another row opens"
    - "Desktop variant: hover-reveal 24x24 pin button in right column (always visible for pinned rows) with e.stopPropagation() on click so row-select doesn't fire"
    - "RDP rows (row.rdpHostRow === true) have NO swipe binding and NO pin button in either variant (T-Test-34: RDP rows can't be pinned)"
    - "Rows with no identity fall back to a neutral tab-icon avatar (no hue) — identity carry-through via sessionMatchKey + useIdentities preserved from ConversationRow.tsx"
    - "No IdentityBadge chip on the row (Ashley: session name = identity name; label carries identity presence)"
  artifacts:
    - path: "src/ui/features/pretty-conversations/tokens.ts"
      provides: "Shared row-height + row-gutter tokens (used only where a value repeats 3+ times per Ashley naming rule); inline hsla(...) preferred elsewhere"
      contains: "PC_ROW_MIN_H_MOBILE"
    - path: "src/ui/features/pretty-conversations/PinAction.tsx"
      provides: "Shared internal <PinAction /> helper — hue-tinted pin/unpin button used by both mobile swipe strip and desktop hover-reveal column"
      exports: ["PinAction"]
    - path: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx"
      provides: "Chunky Telegram-style row with variant-based pin mechanism (swipe vs hover), identity avatar, selected-row ChatMessage-verbatim treatment"
      exports: ["PrettyConversationRow"]
    - path: "src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx"
      provides: "Vitest coverage — swipe state machine, pin toggle, RDP-no-swipe, selected-state, avatar fallback, hover-reveal desktop pin"
      contains: "describe(\"PrettyConversationRow"
  key_links:
    - from: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx"
      to: "src/ui/features/terminal/session-hue.ts"
      via: "sessionMatchKey + useIdentities import (identity carry-through)"
      pattern: "sessionMatchKey|useIdentities"
    - from: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx"
      to: "src/ui/features/pretty-view/ChatMessage.tsx"
      via: "verbatim copy of assistant-bubble gradient/border/shadow class strings for selected-row treatment (adapted for row geometry)"
      pattern: "hsla\\(var\\(--pv-id-hue"
    - from: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx"
      to: "src/ui/state/conversation-store.ts"
      via: "ConversationRow type import for row shape"
      pattern: "ConversationRow as ConversationRowShape"
---

<objective>
Build the foundation of the new pretty-conversations component tree: a single `PrettyConversationRow` component that drives both mobile and desktop viewports via a `variant` prop, plus its shared `PinAction` helper and a token file for the couple of values shared 3+ times. Delivers the chunky Telegram-style row layout (48/40px identity-hue avatar disc, hue-ring, primary label + host-name secondary line, no IdentityBadge chip), the ChatMessage.tsx-verbatim selected-row hue treatment, and both pin mechanisms (mobile swipe-left, desktop hover-reveal). No AppShell wiring, no panel yet — this plan lands the row in isolation and proves the swipe state machine + selected treatment with Vitest.

Purpose: The row IS the visual language of Phase 10. Every visual decision Ashley made in the 2026-07-22 prototype review (chunky rows, hue-ring avatars, ChatMessage-mirrored selected treatment, no IdentityBadge chip, swipe-left pin) lives inside this component. Wave 2's panel is a thin container around this row; Wave 3's AppShell cutover is a mount-site swap. Get the row right and everything downstream is straightforward.

Output: `src/ui/features/pretty-conversations/` directory with 4 files. tsc-clean. All tests green. No changes to `src/ui/sidebar/`, no changes to `src/ui/AppShell.tsx`, no changes to conversation-store.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/shapes/shape-telegram-like-interface.md
@.planning/shapes/shape-fleet-native-conversation-list.md

# Design source-of-truth (LOCKED, Ashley signed off 2026-07-22)
@/home/ubuntu/.claude/identities/tina/bounties/pretty-conversations-panel-redesign/prototype.html
@/home/ubuntu/.claude/identities/tina/bounties/pretty-conversations-panel-redesign/desktop.html

# Files to model against (do NOT modify in this plan)
@src/ui/sidebar/ConversationRow.tsx
@src/ui/features/pretty-view/ChatMessage.tsx
@src/ui/features/terminal/session-hue.ts
@src/ui/state/conversation-store.ts
@src/ui/hooks/use-is-touch-device.ts
@src/ui/sidebar/NewSessionDialog.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tokens.ts + PinAction.tsx shared helpers</name>
  <files>src/ui/features/pretty-conversations/tokens.ts, src/ui/features/pretty-conversations/PinAction.tsx</files>
  <behavior>
    - tokens.ts exports named constants: `PC_ROW_MIN_H_MOBILE = 72` (px), `PC_ROW_MIN_H_DESKTOP = 62` (px), `PC_SWIPE_REVEAL = 88` (px), `PC_SWIPE_THRESHOLD = 40` (px), `PC_SWIPE_ANGLE_TOLERANCE = 12` (px). Rationale (Ashley naming rule): each value reused 3+ times across swipe handler + PinAction geometry + row height CSS. Everything else stays inline hsla(...).
    - PinAction.tsx exports `<PinAction hue={number|null} pinned={boolean} size={"mobile"|"desktop"} onClick={(e) => void} />`. Renders a lucide Pin (unpinned) or PinOff (pinned) glyph inside a circular button. Mobile size = 48x48 circle with hue-tinted background `hsla(hue, 60%, 42%, 0.95)` + hue-border `hsla(hue, 70%, 55%, 0.6)` (prototype.html lines 210-223); desktop size = 24x24 rounded rect with transparent bg + hue-colored icon when pinned, muted when unpinned (desktop.html lines 366-385). When hue is null: mobile = neutral fallback `rgba(90,105,140,0.9)` (prototype.html line 226-228); desktop = `text-muted-foreground/60`. onClick prop is what fires when the button itself is clicked; NO stopPropagation inside PinAction — caller (row) attaches its own stopPropagation.
    - Both files are pure — no store subscription, no hooks besides useTranslation for aria-label (Pin/Unpin from `nav.conversations.pin` + `nav.conversations.unpin` — reuse existing i18n keys from ConversationRow.tsx lines 129-138 verbatim).
  </behavior>
  <action>
    Create `src/ui/features/pretty-conversations/tokens.ts` with the 5 named constants above and a top-of-file comment explaining the "3+ times" rule so a future planner does not add new tokens for one-off values (per Ashley naming rule from planner context).

    Create `src/ui/features/pretty-conversations/PinAction.tsx` exporting `<PinAction />`. Imports: `Pin`, `PinOff` from `lucide-react`; `useTranslation` from `react-i18next`. Props exactly as spec'd above. Class strings for mobile: hue-tinted circular button with box-shadow `0 3px 10px rgba(0,0,0,0.35), inset 0 1px 0 rgba(255,255,255,0.15)` (prototype.html line 221). Class strings for desktop: `size-6 rounded-md` with `opacity-*` control REMOVED — opacity/visibility is the ROW's responsibility (Task 2), not the button's. Reuse i18n keys `nav.conversations.pin` and `nav.conversations.unpin` verbatim.

    No IdentityBadge, no external gesture library, no new npm deps.
  </action>
  <verify>
    <automated>cd /home/ubuntu/termix && npx tsc --noEmit 2>&1 | grep -E "src/ui/features/pretty-conversations/(tokens|PinAction)" | head -5</automated>
  </verify>
  <done>tokens.ts + PinAction.tsx exist with the 5 tokens and PinAction default export; tsc-clean at the two new files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build PrettyConversationRow with variant-based pin mechanism</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationRow.tsx</files>
  <behavior>
    Props (deliberate deviation from ConversationRow.tsx: adds `variant` — everything else stays):
      - `row: ConversationRowShape` (from `@/state/conversation-store`)
      - `selected: boolean`
      - `pinned: boolean`
      - `variant: "mobile" | "desktop"`
      - `onSelect: () => void`
      - `onTogglePin: () => void`

    Behavior:
      1. **Identity resolution.** Same as ConversationRow.tsx lines 41-47: `key = sessionMatchKey(row.targetTmuxSession)`, `identity = key ? identitiesByKey.get(key) : null`, `hue = identity?.colorHue ?? null`. Identity avatar renders `identity.avatarUrl` OR an initial letter (first char of identity.name uppercased) inside the hue-ring disc; no-identity path renders `tabIcon(row.type)` (from `@/shell/tabUtils`) inside a neutral fallback disc.
      2. **Avatar disc geometry.** Mobile: 48x48 circle with radial-gradient background `radial-gradient(circle at 30% 25%, hsla(hue, 80%, 62%, 1) 0%, hsla(hue, 65%, 40%, 1) 60%, hsla(hue, 55%, 28%, 1) 100%)` + box-shadow `0 0 0 2px hsla(hue, 70%, 55%, 0.45)` (prototype.html lines 246-265). Desktop: 40x40 circle with same radial gradient + reduced 1.5px hue-ring (desktop.html lines 279-297). No-identity: neutral linear-gradient + neutral ring per prototype.html lines 267-274.
      3. **Layout.** `flex items-center gap-3` (mobile) / `gap-2.5` (desktop). Row height: mobile `min-h-[72px]` px-4 py-3; desktop `min-h-[62px]` px-3 py-2.5. Body column: primary label `text-[15.5px]` (mobile) / `text-[13.5px]` (desktop) font-medium, secondary line = Server-icon glyph (lucide `Server` or `Cpu` — pick `Server` for symmetry with tabIcon vocabulary) + `row.host?.name` in muted color (`text-[12.5px]` mobile / `text-[11.5px]` desktop). NO IdentityBadge chip. Right meta column: pin glyph (14px mobile / 12px desktop) when `pinned === true`.
      4. **Selected-row treatment — VERBATIM from ChatMessage.tsx lines 118-129 assistant branch, adapted for row geometry.** When `selected === true` AND `hue !== null`, apply background `bg-[linear-gradient(160deg,hsla(<hue>,50%,38%,0.30),hsla(<hue>,45%,24%,0.35))]` + border `border-[hsla(<hue>,65%,55%,0.32)]` + box-shadow `inset 0 1px 0 rgba(255,220,170,0.10), inset 0 0 0 0.5px hsla(<hue>,70%,55%,0.16), 0 0 24px hsla(<hue>,70%,52%,0.08)` (adapted alpha per prototype.html lines 231-239). Interpolate the runtime hue into the string (inline hsla per Ashley naming rule — do NOT create a shared class). When `selected && hue === null`: use `bg-[linear-gradient(160deg,rgba(45,55,80,0.55),rgba(28,35,55,0.6))]` + user-blue-gray border/shadow (prototype.html no-identity handling). When `!selected`: `hover:bg-white/[0.03]` (desktop) or `active:bg-white/[0.03]` (mobile).
      5. **Pin mechanism — `variant === "mobile"`: swipe state machine.**
         - Local state: `dx: number` (translateX offset, 0..-88), `swipedOpen: boolean`.
         - Touch handlers on the row wrapper: `onTouchStart` records `startX`, `startY`, `baseDx = swipedOpen ? -88 : 0`, `active: true`. `onTouchMove` computes `dy = Math.abs(t.clientY - startY)`; if `dy > PC_SWIPE_ANGLE_TOLERANCE`, bail out (set `active: false`, do not preventDefault, let the browser scroll). Otherwise `raw = t.clientX - startX`; `dx = Math.max(-PC_SWIPE_REVEAL, Math.min(0, baseDx + raw))`; apply as inline `transform: translateX(${dx}px)` via style prop (NOT className to avoid re-render churn). `onTouchEnd`: `shouldOpen = dx < -PC_SWIPE_THRESHOLD`; setState accordingly; clear inline transform (let className transition handle it via `translate-x-[-88px]` when open).
         - RDP rows (row.rdpHostRow === true): SKIP swipe handler wiring entirely. No touchStart/Move/End listeners attached; no PinAction rendered in the swipe strip.
         - Coordination across rows (only one row swiped-open at a time): expose an optional callback prop `onSwipeOpenChange?: (open: boolean) => void` that fires on state change. The panel (Wave 2) will track `currentlySwipedId` and force-close others by re-rendering with a `forceClosed` prop — this task exposes the API surface; wiring lives in Wave 2. Also close on `onSelect` firing (tap-body semantic per prototype.html lines 509-518: if swipedOpen, tap closes it INSTEAD of selecting).
         - Swipe-action strip: right-anchored 88px-wide absolutely-positioned strip behind the row containing `<PinAction hue={hue} pinned={pinned} size="mobile" onClick={(e) => { e.stopPropagation(); onTogglePin(); }} />`. Revealed by row transform-left; strip stays absolutely positioned regardless of transform.
      6. **Pin mechanism — `variant === "desktop"`: hover-reveal.**
         - Right meta column contains `<PinAction hue={hue} pinned={pinned} size="desktop" onClick={(e) => { e.stopPropagation(); onTogglePin(); }} />`.
         - Visibility: `pinned ? "opacity-100" : "opacity-0 group-hover/pcrow:opacity-100 focus-visible:opacity-100"` (verbatim pattern from ConversationRow.tsx lines 65-72, class name `pcrow` to avoid collision with sidebar's `convrow`).
         - No swipe listeners on desktop variant.
         - RDP rows: no PinAction rendered (T-Test-34 preserved).
      7. **Row-select click.** Click anywhere on the row body fires `onSelect()`. Mobile: if `swipedOpen === true`, click closes it instead (do NOT fire onSelect). Desktop: always fires onSelect. Keyboard: Enter/Space on the row wrapper fires onSelect (a11y — matches ConversationRow.tsx lines 79-84).
      8. **data-* attributes for tests + AppShell integration:** `data-conversation-id={row.id}`, `data-selected={selected ? "true" : "false"}`, `data-pinned={pinned ? "true" : "false"}`, `data-variant={variant}`, `data-rdp-host-row={row.rdpHostRow ? "true" : undefined}`, `data-swiped-open={swipedOpen ? "true" : undefined}` (mobile variant only).
  </behavior>
  <action>
    Create `src/ui/features/pretty-conversations/PrettyConversationRow.tsx`. Imports: `useState`, `useRef`, `useCallback`, `type CSSProperties` from `react`; `Server`, `Pin` from `lucide-react`; `tabIcon` from `@/shell/tabUtils`; `sessionMatchKey` + `useIdentities` from `@/features/terminal/session-hue` (session-hue.ts exports useIdentities indirectly — actually it lives in `@/state/identities-store`; confirm and adjust import path per session-hue.ts source); `ConversationRow as ConversationRowShape` from `@/state/conversation-store`; `PinAction` from `./PinAction`; tokens from `./tokens`.

    Implementation ordering: (a) identity resolution + hue; (b) variant-based dimensions (avatar size, min-h, padding, gap); (c) selected-row class string interpolation; (d) mobile swipe state machine as `useSwipeState` inline (or extract to hook — planner's discretion); (e) render tree — outer wrapper (relative, overflow-hidden on mobile) → swipe strip (mobile only, non-RDP) → row body (role=button, tabIndex=0, click/keydown/touch handlers, style={{ transform: ... }} on mobile only) → avatar disc → body column → meta column with pin glyph (when pinned) + desktop PinAction.

    Follow ConversationRow.tsx idioms verbatim wherever unchanged: identity avatar shape, tabIcon fallback, e.stopPropagation() on pin click, aria-labels via useTranslation with existing `nav.conversations.pin` / `nav.conversations.unpin` keys. Do NOT reuse the `bg-accent-brand/10 text-accent-brand` selected treatment from ConversationRow.tsx line 62 — that's the shadcn/AppRail idiom being explicitly replaced by the ChatMessage-verbatim hue treatment.

    Class strings for the selected background/border/shadow MUST be inline (`bg-[linear-gradient(...)]` etc. per Tailwind arbitrary-values idiom) with runtime hue interpolated via template literal into the arbitrary-value bracket. Follows ChatMessage.tsx lines 105-129 pattern exactly. Do NOT introduce new CSS custom properties — Ashley naming rule prefers inline hsla.

    No IdentityBadge import, no chip render, no `identity.name` displayed as chrome — the row's `row.label` IS the identity name (Ashley: session name = identity name).
  </action>
  <verify>
    <automated>cd /home/ubuntu/termix && npx tsc --noEmit 2>&1 | grep "src/ui/features/pretty-conversations/PrettyConversationRow" | head -5</automated>
  </verify>
  <done>PrettyConversationRow.tsx compiles clean; grep for `hsla(${hue}` (interpolated hue in inline hsla) returns matches; grep for `IdentityBadge` in the new file returns zero (chip NOT rendered); grep for `variant === "mobile"` returns matches (branch logic present).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Vitest coverage — swipe state machine, pin toggle, RDP-no-swipe, selected-state, avatar fallback, hover-reveal</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx</files>
  <behavior>
    Test suite named `PrettyConversationRow`. Model the fixture pattern on `src/ui/sidebar/NewSessionDialog.test.tsx` lines 14-35 verbatim: mock react-i18next passthrough, mock `@/features/terminal/session-hue` to controlled outputs, mock `@/state/identities-store` to inert `byKey: new Map()`, mock `@/hooks/use-is-touch-device` to `false` (irrelevant — this row takes variant as a prop, not from the hook).

    Fixture helper `makeRow(overrides)` builds a `ConversationRowShape` with id="conv-1", type="terminal", label="nelly", targetTmuxSession="nelly", host={ name: "thenasty", ... }.

    Tests (each a top-level `it()`):
      1. **Test 1 — Desktop variant renders selected-row hue treatment.** Render `<PrettyConversationRow row={makeRow()} selected={true} pinned={false} variant="desktop" onSelect={vi.fn()} onTogglePin={vi.fn()} />` with a mocked identity `{ colorHue: 30 }`. Query the row wrapper by `data-conversation-id="conv-1"`. Assert its className contains `hsla(30,` (interpolated hue) or `linear-gradient(160deg,hsla(30`. Assert `data-selected="true"`.
      2. **Test 2 — Mobile variant swipe-left past threshold opens strip.** Render mobile variant. Fire touchStart at clientX=200, touchMove at clientX=140 (dx=-60 past threshold), touchEnd. Assert `data-swiped-open="true"`. Verify PinAction is queryable inside the swipe strip (by role=button, aria-label /pin/i).
      3. **Test 3 — Mobile swipe under threshold snaps closed.** touchStart clientX=200, touchMove clientX=175 (dx=-25 under 40px threshold), touchEnd. Assert `data-swiped-open` is absent / not "true".
      4. **Test 4 — Vertical gesture > 12px yields to scroll.** touchStart clientX=200, touchMove clientX=150 clientY=(startY+20) (dy=20 > tolerance), touchEnd. Assert no `data-swiped-open`. Assert onSelect NOT called (touch ended without either open or select).
      5. **Test 5 — Tap on swiped-open row closes it, does NOT fire onSelect.** Open the row via a swipe (Test 2 sequence), then fire a plain click. Assert `data-swiped-open` becomes absent AND onSelect was called zero times.
      6. **Test 6 — Tap on closed row fires onSelect.** Fire click without any prior swipe. Assert onSelect called exactly once.
      7. **Test 7 — RDP row has no swipe strip and no pin button.** `makeRow({ rdpHostRow: true })`, mobile variant. Query for any PinAction button by role — should be zero. Fire touch sequence that would otherwise open swipe — assert no `data-swiped-open` transition.
      8. **Test 8 — Desktop pin button e.stopPropagation() — clicking pin does not fire onSelect.** Desktop variant, pinned=false. Query pin button by role=button aria-label=/pin/i. Fire click. Assert `onTogglePin` called once, `onSelect` called zero times.
      9. **Test 9 — Avatar fallback for no-identity row.** Mock session-hue to return `{ identity: null, identityHue: null }`. Render. Query the row and assert the avatar column contains a Server/Terminal icon (svg matching tabIcon("terminal") output) — a data-testid on the avatar container or a query for the icon by role="img" works.
      10. **Test 10 — Pinned row on desktop always shows pin button (not hover-gated).** Desktop variant, pinned=true. Query pin button by role. Assert its computed style is NOT `opacity: 0` — inspect className for `opacity-100` presence.
      11. **Test 11 — No IdentityBadge chip in DOM.** Render either variant. Grep `container.innerHTML` for the string `IdentityBadge` OR query by any known IdentityBadge testid. Assert zero occurrences.
  </behavior>
  <action>
    Create `src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx`. Model the mock-setup block byte-for-byte on `NewSessionDialog.test.tsx` lines 14-35, adjusting the identity mock so tests can override `useSessionIdentity` return per test (use `vi.mocked(useSessionIdentity).mockReturnValue({...})` inside each `it`). Import `render`, `fireEvent` from `@testing-library/react`; `describe`, `it`, `expect`, `vi`, `beforeEach` from `vitest`.

    Use `fireEvent.touchStart(el, { touches: [{ clientX, clientY }] })` for touch events (jsdom supports this shape). For click assertions, `fireEvent.click(el)`. For pin-button queries: `getByRole("button", { name: /pin/i })` — but note there's a wrapper role="button" for the row itself; scope the query to inside the pin column via `queryByLabelText(/^pin$/i)` or a data-testid on the PinAction.

    Add a data-testid="pin-action" to PinAction if the role query is ambiguous (Task 1 revision — call out in commit message). Alternatively narrow via `within(pinColumn).getByRole(...)`.

    For Test 9's icon assertion: tabIcon returns a `<Terminal>` lucide component; query by `svg` inside the avatar column via a data-testid="avatar" on the disc div.

    Ensure `beforeEach(() => vi.clearAllMocks())`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/termix && npx vitest run src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>All 11 tests pass; tsc-clean; no console warnings from react-testing-library about missing act() wrappers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| touch input → row state | Touch coordinates are attacker-controllable (user input) but can only mutate local swipe state; no privilege escalation |
| DOM className → visual output | Class strings interpolate runtime `hue: number` from identity store — hue is a numeric field validated at identity ingress (existing) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Tampering | className hue interpolation | accept | hue is a number 0-360 from identity-store; not user-controllable at this layer; existing identity-ingress validation covers upstream |
| T-10-01-02 | Denial of Service | swipe touch handlers | mitigate | passive touch listeners (no preventDefault) so the browser can always scroll; vertical-gesture bail-out at 12px yields to native scroll instead of blocking it |
| T-10-01-03 | Information Disclosure | avatar image src | accept | `identity.avatarUrl` sourced from the same identities-store used in production ConversationRow.tsx — no new attack surface |
| T-10-01-SC | Tampering | npm installs | mitigate | zero new dependencies; only existing `lucide-react` + `react-i18next` — no package legitimacy audit needed |
</threat_model>

<verification>
- tsc clean at every file in `src/ui/features/pretty-conversations/`
- All 11 Vitest cases pass
- Grep confirms zero `IdentityBadge` references in new files
- Grep confirms `hsla(` inline interpolation (not new CSS vars) for selected-row treatment
- Grep confirms `variant === "mobile"` branch in PrettyConversationRow.tsx
- No touches to `src/ui/sidebar/*` or `src/ui/AppShell.tsx` (scope-fence)
- No new npm deps in package.json
</verification>

<success_criteria>
1. `src/ui/features/pretty-conversations/{tokens.ts, PinAction.tsx, PrettyConversationRow.tsx, PrettyConversationRow.test.tsx}` all exist
2. `PrettyConversationRow` renders a chunky row with 48px (mobile) / 40px (desktop) hue-ring avatar disc, primary label + host secondary line, no IdentityBadge chip
3. Selected-row treatment uses ChatMessage.tsx-verbatim hue gradient + border + shadow (grep-verifiable inline hsla)
4. Mobile variant swipe state machine passes all touch-sequence tests (open, close, vertical-yield, tap-body-closes)
5. Desktop variant hover-reveal pin button with e.stopPropagation() confirmed by test
6. RDP rows (rdpHostRow=true) skip swipe listener and skip pin button in both variants
7. `npx vitest run src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` reports 11/11 green
8. `npx tsc --noEmit` reports no errors in the new files
</success_criteria>

<output>
Create `.planning/phases/10-pretty-conversations-visual-language-rework/10-01-SUMMARY.md` when done. Summary format: brief prose recap of what shipped (row component + variant prop + swipe state machine + hover-reveal desktop + tests), any deviations from the plan (e.g. added data-testid attributes for test queries), and a "handoff to Wave 2" section noting the panel-side coordination surface (`onSwipeOpenChange` prop or `forceClosed` prop shape) that Wave 2 will wire.
</output>
