---
phase: 70-branding-config
plan: 04
type: execute
wave: 3
depends_on:
  - 70-03
files_modified:
  - src/ui/AppShell.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/auth/Auth.tsx
  - src/ui/locales/en.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "grep -c '\"SKYNET\"' src/ui/AppShell.tsx returns 0 — the three hardcoded 'SKYNET' fallback strings at L232, L616, L1569 are all replaced with brandingConfig.appName"
    - "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx L1489-1497 no longer contains <SkynetLogo — the SVG component reference is replaced with an <img src={brandingConfig.iconPath}> element; the wordmark <img src='/skynet-wordmark.png'> is replaced with <img src={brandingConfig.wordmarkPath}>"
    - "The SkynetLogo import at PrettyConversationsPanel.tsx L135 is removed IF SkynetLogo is not referenced elsewhere in the file (grep confirms zero remaining SkynetLogo tokens before removing the import)"
    - "src/ui/auth/Auth.tsx L1131 no longer contains <img src=\"/icon.png\" — replaced with <img src={brandingConfig.iconPath}>; L1138 no longer contains <img src=\"/skynet-wordmark.png\" — replaced with <img src={brandingConfig.wordmarkPath}> and alt={brandingConfig.appName}"
    - "src/ui/auth/Auth.tsx L1314's use of t('auth.loginTitle') is either replaced with 'Login to {brandingConfig.appName}' (dynamic wiring) OR the underlying locale value in en.json is changed from 'Login to SKYNET' to something operator-neutral like 'Login' — the fifth surface is addressed per 70-RESEARCH.md Pitfall 8 recommendation (b)"
    - "useBrandingFavicon() (from Plan 70-03) is called exactly once inside AppShell so favicon updates apply pre-login and post-login — grep for 'useBrandingFavicon' in src/ui/AppShell.tsx returns 1"
    - "npx tsc --noEmit exits 0 after all edits"
  artifacts:
    - path: "src/ui/AppShell.tsx"
      provides: "Three 'SKYNET' string replacements at L232 (initial tab label), L616 (document.title fallback), L1569 (tab reset on close); one call to useBrandingConfig() near the top of AppShell function; one call to useBrandingFavicon() inside the component body"
      contains: "brandingConfig.appName"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "Conversation-list header (L1489-1497) rewired: <SkynetLogo> → <img src={brandingConfig.iconPath}>; static wordmark <img src=\"/skynet-wordmark.png\"> → <img src={brandingConfig.wordmarkPath}> alt={brandingConfig.appName}; useBrandingConfig() call added at top of component; SkynetLogo import at L135 removed if no other consumers remain in the file"
      contains: "brandingConfig.iconPath"
    - path: "src/ui/auth/Auth.tsx"
      provides: "Login screen header (L1131-1142) rewired to brandingConfig.iconPath + wordmarkPath; useBrandingConfig() call added in the component that owns that render; L1314's t('auth.loginTitle') addressed via one of the two options in the action (dynamic wire OR locale-neutralize)"
      contains: "brandingConfig.iconPath"
    - path: "src/ui/locales/en.json"
      provides: "IF the locale-neutralize option is chosen: L914 'loginTitle' value changed from 'Login to SKYNET' to 'Login' (or equivalent stack-neutral phrasing). IF the dynamic-wire option is chosen: L914 stays as-is and Auth.tsx L1314 templating handles the substitution — in which case en.json is unchanged and this artifact drops from files_modified."
  key_links:
    - from: "src/ui/AppShell.tsx"
      to: "src/ui/branding/branding-store.ts useBrandingConfig()"
      via: "hook call near top of AppShell function; brandingConfig.appName consumed in the three fallback chains"
      pattern: "useBrandingConfig\\(\\)"
    - from: "src/ui/AppShell.tsx"
      to: "src/ui/branding/apply-favicon.ts useBrandingFavicon()"
      via: "single call inside AppShell body so favicon updates apply for both auth and post-auth UI (AppShell is mounted throughout the lifecycle)"
      pattern: "useBrandingFavicon\\(\\)"
    - from: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx header render"
      to: "backend GET /branding/icon.png + /branding/wordmark.png (Plan 70-01)"
      via: "brandingConfig.iconPath and .wordmarkPath resolve to /branding/... URLs that nginx (Plan 70-02) routes to the backend /branding/* handler with per-file fallback"
      pattern: "src=\\{brandingConfig\\.(iconPath|wordmarkPath)\\}"
    - from: "src/ui/auth/Auth.tsx login header render"
      to: "backend GET /branding/icon.png + /branding/wordmark.png (Plan 70-01)"
      via: "same as conversation header — unified via D-08's 'same icon+wordmark' constraint"
      pattern: "src=\\{brandingConfig\\.(iconPath|wordmarkPath)\\}"
---

<objective>
Wire the four (five, counting the Auth.tsx loginTitle discovery) branding-driven UI surfaces to the branding store from Plan 70-03. All edits are consumers of the useBrandingConfig() hook and useBrandingFavicon() hook — no new files are created, no new dependencies added. This is the plan that turns Plans 70-01 through 70-03 into visible user behavior.

Purpose: Implements D-08 (four surfaces: tab title, favicon, conversation list header, login screen header) plus the fifth surface flagged in 70-RESEARCH.md finding #2 (Auth.tsx L1314 `t("auth.loginTitle")` = "Login to SKYNET"). The four base surfaces are non-negotiable per CONTEXT.md; the fifth is a scope-expansion decision resolved by the planner (per Open Question #1 in 70-RESEARCH.md) — this plan adopts recommendation (b) from Pitfall 8: neutralize the loginTitle so it doesn't hardcode "SKYNET" on AI+ deploys.

Output: (a) src/ui/AppShell.tsx: three "SKYNET" literal replacements, one useBrandingConfig() call, one useBrandingFavicon() call. (b) src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx: header <SkynetLogo> + static wordmark img → dynamic <img> tags reading brandingConfig; import cleanup for SkynetLogo if no other consumers remain. (c) src/ui/auth/Auth.tsx: L1131-1142 login header rewired; L1314 loginTitle addressed per action-step decision. (d) src/ui/locales/en.json: L914 loginTitle value neutralized (only if the locale-neutralize option is chosen; otherwise this file is unchanged).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-branding-config/70-CONTEXT.md
@.planning/phases/70-branding-config/70-RESEARCH.md
@.planning/phases/70-branding-config/70-PATTERNS.md

@src/ui/AppShell.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
@src/ui/features/pretty-conversations/SkynetLogo.tsx
@src/ui/auth/Auth.tsx
@src/ui/locales/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire AppShell.tsx (three SKYNET replacements + useBrandingConfig + useBrandingFavicon) and PrettyConversationsPanel.tsx (conversation header icon+wordmark)</name>
  <files>src/ui/AppShell.tsx, src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx</files>
  <read_first>
    - .planning/phases/70-branding-config/70-PATTERNS.md § "src/ui/AppShell.tsx (modify — three 'SKYNET' replacements)" — exact target sites at L232, L616, L1569; hook-add site near L225
    - .planning/phases/70-branding-config/70-PATTERNS.md § "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (modify — header wire-through)" — target site L1489-1497; import at L135; useBrandingConfig hook add at top of component
    - .planning/phases/70-branding-config/70-CONTEXT.md § D-08 surfaces 1 (tab title in AppShell) and 3 (conversation list header)
    - .planning/phases/70-branding-config/70-RESEARCH.md § "Code Examples > Existing document.title assignment" (L297-305) and § "Code Examples > Existing conversation header" (L314-340)
    - src/ui/AppShell.tsx L225-240, L610-624, L1560-1580 (three target sites — grep for '"SKYNET"' to confirm exact line numbers in the current tree; L232/L616/L1569 are from PATTERNS.md but may drift on recent edits)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx L130-140, L1483-1502 (import site + header target)
    - src/ui/features/pretty-conversations/SkynetLogo.tsx (whole file — to understand what's being replaced; the component may need to stay in the tree if it's still consumed elsewhere after the header edit)
  </read_first>
  <action>
    STEP A — src/ui/AppShell.tsx edits:

    1. Add import at top of file alongside existing hook imports. Use the same import path style as sibling imports in AppShell (grep for `from "@/state/` or `from "./` to determine which the project uses).
       - Import useBrandingConfig from `@/branding/branding-store` (or the equivalent relative path).
       - Import useBrandingFavicon from `@/branding/apply-favicon`.
    2. Inside the AppShell function body, near the top (~L225 — after existing hook calls, before any early returns), add:
       - `const brandingConfig = useBrandingConfig();`
       - `useBrandingFavicon();`
       Placement: after the other useX() hook calls but before any conditional early return. useBrandingFavicon() has no return value — call for side-effect only.
    3. Find the three "SKYNET" literal replacements. Use grep to locate exact line numbers (they may drift from PATTERNS.md's cited L232/L616/L1569 if recent edits shifted the file):
       - Site 1 (initial dashboard tab label, ~L232): `label: "SKYNET",` → `label: brandingConfig.appName,`
       - Site 2 (document.title fallback chain, ~L616): the pattern is `identity?.displayName || tmux || activeTab?.label || "SKYNET"` — replace the final `"SKYNET"` with `brandingConfig.appName`.
       - Site 3 (tab reset on close, ~L1569): `label: "SKYNET",` → `label: brandingConfig.appName,`
    4. Do NOT introduce a hook call inside a callback / event handler / effect. `brandingConfig` should be captured once at the top of the component and closed over by the fallback chains. If the tab-init or tab-reset code lives inside a callback that doesn't have access to brandingConfig from closure (e.g., if it's a module-level constant), you'll need to move the constant into the component body or use useCallback / useMemo — grep the exact context of each L232/L1569 site before editing.
    5. Verify: `grep -c '"SKYNET"' src/ui/AppShell.tsx` returns 0. `grep -c "brandingConfig.appName" src/ui/AppShell.tsx` returns >= 3. `grep -c "useBrandingConfig" src/ui/AppShell.tsx` returns 2 (import + call). `grep -c "useBrandingFavicon" src/ui/AppShell.tsx` returns 2 (import + call).

    STEP B — src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx edits:

    1. Add import: `import { useBrandingConfig } from "@/branding/branding-store";` (or matching relative path — match the extension convention used by the file's other imports).
    2. Inside the PrettyConversationsPanel component body (top of function, after existing hook calls), add: `const brandingConfig = useBrandingConfig();`
    3. Find the header render around L1489-1497. Current shape (verify exact line numbers via grep for "SkynetLogo"):
       <SkynetLogo
         aria-hidden="true"
         className="pv-header-logo"
       />
       <img
         src="/skynet-wordmark.png"
         alt="SKYNET"
         className="pv-header-wordmark"
       />
    4. Replace with:
       <img
         src={brandingConfig.iconPath}
         alt=""
         aria-hidden="true"
         className="pv-header-logo"
       />
       <img
         src={brandingConfig.wordmarkPath}
         alt={brandingConfig.appName}
         className="pv-header-wordmark"
       />
    5. Preserve the CSS classes `pv-header-logo` and `pv-header-wordmark` verbatim (research L340 — styling continuity).
    6. After the header edit, grep the whole file for `SkynetLogo` — if it returns 0 tokens (import gone, no residual usage), delete the import line at ~L135: `import SkynetLogo from "./SkynetLogo";`. If any consumer remains (unlikely but possible), leave the import intact and note it in the SUMMARY.
    7. Do NOT delete src/ui/features/pretty-conversations/SkynetLogo.tsx itself in this plan — the file may still be imported elsewhere (grep across src/ before deleting; if zero consumers, planner discretion whether to delete or defer as harmless-dead-code).
    8. Verify: `grep -c "brandingConfig.iconPath" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns >= 1. `grep -c 'src="/skynet-wordmark.png"' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns 0.

    STEP C — Type check:

    - `npx tsc --noEmit 2>&1 | tail -30` MUST exit 0.
    - If TSC complains about a missing prop (e.g., <img> requires alt), fix it in place — the alt should be "" for the icon (aria-hidden) and brandingConfig.appName for the wordmark.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | tail -30 &amp;&amp; grep -c '"SKYNET"' src/ui/AppShell.tsx &amp;&amp; grep -c "brandingConfig.appName" src/ui/AppShell.tsx &amp;&amp; grep -c "useBrandingFavicon" src/ui/AppShell.tsx &amp;&amp; grep -c "brandingConfig.iconPath" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx &amp;&amp; grep -c 'src="/skynet-wordmark.png"' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx</automated>
  </verify>
  <done>
    - `grep -c '"SKYNET"' src/ui/AppShell.tsx` returns 0 (three hardcoded fallbacks gone)
    - `grep -c "brandingConfig.appName" src/ui/AppShell.tsx` returns >= 3 (three replacement sites)
    - `useBrandingConfig()` is called exactly once in AppShell (grep for occurrences excluding the import line)
    - `useBrandingFavicon()` is called exactly once in AppShell (grep for occurrences excluding the import line)
    - `grep -c 'src="/skynet-wordmark.png"' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns 0
    - `grep -c '<SkynetLogo' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns 0 (JSX consumption gone)
    - If `grep -c "SkynetLogo" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns 0 after the JSX edit, the import at L135 is also removed
    - `npx tsc --noEmit` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewire Auth.tsx login header (L1131-1142) and address the fifth 'Login to SKYNET' surface (L1314 t('auth.loginTitle'))</name>
  <files>src/ui/auth/Auth.tsx, src/ui/locales/en.json</files>
  <read_first>
    - .planning/phases/70-branding-config/70-PATTERNS.md § "src/ui/auth/Auth.tsx (modify — login header wire-through)" — exact target site L1131-1142; note that /icon.png bundled default now unifies conversation and login icons per D-08 "same icon+wordmark" language
    - .planning/phases/70-branding-config/70-RESEARCH.md § "Summary" finding #2 (fifth surface discovered — Auth.tsx L1314 t("auth.loginTitle") = "Login to SKYNET" from en.json L914; LoginPage.tsx is DEAD CODE — do not touch)
    - .planning/phases/70-branding-config/70-RESEARCH.md § "Pitfalls > Pitfall 8" (fifth surface options a/b/c; recommendation is (b) — fold into branding surfaces, options: dynamic-wire `Login to ${brandingConfig.appName.toUpperCase()}` OR locale-neutralize to plain "Login")
    - .planning/phases/70-branding-config/70-RESEARCH.md § "Open Questions" #1 (recommendation to add the fifth surface to the phase; single-line change either way)
    - .planning/phases/70-branding-config/70-CONTEXT.md § D-08 surface 4 (login screen header — same icon+wordmark as conversation header)
    - src/ui/auth/Auth.tsx L1125-1145 (login header render — the img tags to rewire) and L1305-1320 (the `t("auth.loginTitle")` usage site)
    - src/ui/locales/en.json L910-920 (the auth.loginTitle key at L914 — value is "Login to SKYNET")
    - src/ui/auth/LoginPage.tsx (ONLY read the top of the file to CONFIRM it is dead code per Pitfall 10; then IGNORE it — do NOT edit)
  </read_first>
  <action>
    STEP A — Rewire login header at Auth.tsx L1131-1142:

    1. Identify which component/function inside Auth.tsx renders L1131-1142 (grep for surrounding text like `className="h-12 w-12 shrink-0"` or `className="h-8 w-auto"` to locate; the file is large with multiple components — pick the one that owns the L1131 render).
    2. Add at the top of that component's function body (after existing hook calls, before any early return): `const brandingConfig = useBrandingConfig();`
    3. If not already imported at the file top: `import { useBrandingConfig } from "@/branding/branding-store";` (or matching relative path — check other Auth.tsx imports for the convention).
    4. Replace the two <img> tags at L1131-1142:
       - `<img src="/icon.png" alt="" className="h-12 w-12 shrink-0" draggable={false} />` → `<img src={brandingConfig.iconPath} alt="" className="h-12 w-12 shrink-0" draggable={false} />`
       - `<img src="/skynet-wordmark.png" alt="Skynet" className="h-8 w-auto" draggable={false} />` → `<img src={brandingConfig.wordmarkPath} alt={brandingConfig.appName} className="h-8 w-auto" draggable={false} />`
    5. Preserve the exact className strings, the draggable={false} attribute, and the h-12/w-12/h-8/w-auto sizing so login screen visuals are unchanged on a no-config deploy (bundled default iconPath = /branding/icon.png which is a copy of public/icon.png per Plan 70-01).
    6. Do NOT introduce a Skynet-icon-vs-Skynet-wordmark alignment change — the login screen currently sits side-by-side and that layout is preserved.

    STEP B — Address the fifth surface (Auth.tsx L1314 loginTitle):

    Pick ONE of the two options below and apply consistently. This plan RECOMMENDS Option 1 (locale-neutralize) because it's the smaller diff and CONTEXT.md's <specifics> notes the i18n key "does not need to be touched" — that guidance held for the FOUR surfaces in D-08 but the fifth surface was not enumerated, so the locale value going neutral is the least-invasive fix that stays within the "no i18n rewire" spirit while removing the operator-inappropriate string.

    OPTION 1 (RECOMMENDED — locale-neutralize):
    - Edit src/ui/locales/en.json L914: change `"loginTitle": "Login to SKYNET"` to `"loginTitle": "Login"`.
    - Leave Auth.tsx L1314 unchanged — `t("auth.loginTitle")` continues to work, just resolves to "Login" instead of "Login to SKYNET".
    - Rationale: operator-neutral phrasing works for both t1000 (was "Login to SKYNET", now just "Login" — mild simplification, no operator-specific string in either state) and AI+ (previously would have shown "Login to SKYNET" which is wrong; now shows "Login" which is generic and correct).
    - Verify: `grep -c "loginTitle.*SKYNET" src/ui/locales/en.json` returns 0.

    OPTION 2 (ALTERNATIVE — dynamic template):
    - Leave en.json L914 as-is: `"loginTitle": "Login to SKYNET"` (or change to `"loginTitle": "Login to {{appName}}"` with a placeholder).
    - Edit Auth.tsx L1314: change `t("auth.loginTitle")` to `\`Login to ${brandingConfig.appName.toUpperCase()}\`` (template literal) OR to `t("auth.loginTitle", { appName: brandingConfig.appName.toUpperCase() })` if the i18n library supports interpolation and en.json is updated to use a placeholder.
    - Rationale: Preserves the "Login to X" phrasing wherever it's used; substitutes the operator's brand name.
    - Verify: `grep -c "brandingConfig.appName" src/ui/auth/Auth.tsx` returns >= 3 (login-header wordmark alt + login-header icon (indirectly via iconPath) + this new loginTitle site).

    Pick Option 1. Do NOT do both. If a follow-up UAT wants "Login to Aither Intelligence Plus" instead of just "Login", the next phase can revisit — this MVP just needs to remove the hardcoded "SKYNET" from the login flow.

    STEP C — Confirm LoginPage.tsx is untouched:

    - `git diff src/ui/auth/LoginPage.tsx` MUST be empty. LoginPage.tsx is dead code (per Pitfall 10 in 70-RESEARCH.md — confirmed 2026-09-03 that no live import path exists). Modifying it would waste effort and add review surface.

    STEP D — Type check + i18n key sanity:

    - `npx tsc --noEmit 2>&1 | tail -30` MUST exit 0.
    - If Option 1 chosen: `grep -c "\"loginTitle\": \"Login\"" src/ui/locales/en.json` returns 1.
    - If Option 2 chosen: `grep -c "brandingConfig.appName" src/ui/auth/Auth.tsx` returns >= 3.
    - `grep -c 'src="/icon.png"' src/ui/auth/Auth.tsx` returns 0 (the login-header icon replacement landed).
    - `grep -c 'src="/skynet-wordmark.png"' src/ui/auth/Auth.tsx` returns 0.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | tail -30 &amp;&amp; grep -c 'src="/icon.png"' src/ui/auth/Auth.tsx &amp;&amp; grep -c 'src="/skynet-wordmark.png"' src/ui/auth/Auth.tsx &amp;&amp; grep -c "brandingConfig.iconPath" src/ui/auth/Auth.tsx &amp;&amp; grep -c "loginTitle.*SKYNET" src/ui/locales/en.json</automated>
  </verify>
  <done>
    - `grep -c 'src="/icon.png"' src/ui/auth/Auth.tsx` returns 0 (login-header icon rewired to brandingConfig.iconPath)
    - `grep -c 'src="/skynet-wordmark.png"' src/ui/auth/Auth.tsx` returns 0 (login-header wordmark rewired to brandingConfig.wordmarkPath)
    - `grep -c "brandingConfig.iconPath" src/ui/auth/Auth.tsx` returns >= 1
    - `grep -c "brandingConfig.wordmarkPath" src/ui/auth/Auth.tsx` returns >= 1
    - Fifth surface addressed: EITHER en.json L914 no longer contains "SKYNET" OR Auth.tsx L1314 substitutes brandingConfig.appName dynamically (verify one of these is true — the automated verify's grep for "loginTitle.*SKYNET" in en.json returns 0 under Option 1; under Option 2, en.json may still have SKYNET but the Auth.tsx grep for brandingConfig.appName is >= 3)
    - src/ui/auth/LoginPage.tsx is unchanged (dead code — do not touch per Pitfall 10)
    - `npx tsc --noEmit` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| branding store → JSX src/alt attributes | brandingConfig.iconPath/wordmarkPath/appName flow into <img src>, <img alt>, and label prop values. Store contents are backend-sourced (Plan 70-01), inline-validated (Plan 70-03 shape guard), and treated as opaque display strings by consumers. |
| useBrandingFavicon() → document.head | Hook writes href attributes on existing <link rel="icon"> elements. No element creation — only attribute rewriting on elements already present in index.html. |
| i18n key value change (Option 1) | en.json changes are visible to every user who loads the app — not per-instance-configurable. Fifth surface uses a neutral value ("Login") that is correct for any operator. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-04-01 | Injection | Malicious operator config with iconPath = "javascript:alert(1)" rendered into <img src> | mitigate | Handled at Plan 70-03 boundary (isBrandingConfig shape guard); further defense could restrict to "starts with /branding/" (deferred as low-value MVP — operator has full deploy control anyway). Same disposition as T-70-03-04. |
| T-70-04-02 | Tampering | Consumer accidentally invokes useBrandingConfig() inside a callback or conditional, causing hook-order violations | mitigate | <action> steps explicitly call out "add at top of component function body, after existing hook calls, before early return" — same pattern as every other useX() call in the component. TSC-level React linting would catch actual hook-order violations. |
| T-70-04-03 | Compatibility | Removing the SkynetLogo import while a stale consumer still references it in the same file | mitigate | <action> STEP B point 6 requires grep to return 0 for the SkynetLogo token in the file BEFORE deleting the import. TSC would also catch a stray reference (unused-import warning becomes an error under strict mode). |
| T-70-04-04 | Compatibility | Locale change breaks a snapshot test that asserts on "Login to SKYNET" | mitigate | Task 2 STEP D + <done> gate runs npx tsc --noEmit. If Option 1 is chosen and a snapshot test breaks, the fix is to update the snapshot (the change is intentional). Note in the SUMMARY if any snapshot updates were needed. |
| T-70-04-05 | Compatibility | Fifth-surface handling accidentally re-adds a hardcoded "SKYNET" in AI+ deploys via Option 2's template using .toUpperCase() | accept | Option 1 (recommended) avoids this entirely by neutralizing to "Login". Under Option 2, the fallback appName = "Skynet" (from Plan 70-03 defaults sentinel) would render "Login to SKYNET" — but that's the CURRENT behavior on t1000, so no regression. |
| T-70-04-06 | Compatibility | Preservation of visual layout (h-12, w-12, h-8, w-auto, pv-header-logo, pv-header-wordmark class names) | mitigate | <action> STEP A point 5 and STEP B point 5 explicitly preserve className strings. End-of-phase human verify (Plan 70-05) confirms visuals unchanged on t1000. |
| T-70-04-SC | Tampering | Frontend package installs | mitigate | NO NEW PACKAGES. Verify at task done gate by checking git diff of package.json is empty. |
</threat_model>

<verification>
Type-check + surface grep:
```bash
npx tsc --noEmit 2>&1 | tail -30
grep -c '"SKYNET"' src/ui/AppShell.tsx                                     # 0
grep -c "brandingConfig.appName" src/ui/AppShell.tsx                       # >= 3
grep -c "useBrandingConfig" src/ui/AppShell.tsx                            # 2 (import + call)
grep -c "useBrandingFavicon" src/ui/AppShell.tsx                           # 2 (import + call)
grep -c 'src="/skynet-wordmark.png"' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx   # 0
grep -c '<SkynetLogo' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx   # 0
grep -c 'src="/icon.png"' src/ui/auth/Auth.tsx                             # 0
grep -c 'src="/skynet-wordmark.png"' src/ui/auth/Auth.tsx                  # 0
grep -c "brandingConfig.iconPath" src/ui/auth/Auth.tsx                     # >= 1
grep -c "loginTitle.*SKYNET" src/ui/locales/en.json                        # 0 (under Option 1)
git diff src/ui/auth/LoginPage.tsx | wc -l                                  # 0 (dead code untouched)
git diff package.json | wc -l                                               # 0 (no new deps)
```

Scoped tests:
```bash
npx vitest run --related src/ui/AppShell.tsx src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx src/ui/auth/Auth.tsx 2>&1 | tail -40
```

Ship-gate (orchestrator, NOT executor): full npx vitest run green; visual verification in Plan 70-05 human-verify checkpoint.
</verification>

<success_criteria>
- All three "SKYNET" hardcoded strings in AppShell.tsx replaced with brandingConfig.appName
- useBrandingConfig() and useBrandingFavicon() called exactly once each inside AppShell
- Conversation-list header (PrettyConversationsPanel L1489-1497) renders <img src={brandingConfig.iconPath}> and <img src={brandingConfig.wordmarkPath}>
- SkynetLogo import removed from PrettyConversationsPanel.tsx if no other file-local consumers remain
- Login screen header (Auth.tsx L1131-1142) rewired to brandingConfig.iconPath + wordmarkPath with alt={brandingConfig.appName}
- Fifth surface addressed: en.json loginTitle neutralized to "Login" (Option 1) OR Auth.tsx L1314 dynamically templated (Option 2)
- src/ui/auth/LoginPage.tsx is unchanged (dead code)
- TypeScript compiles clean
- Scoped test suite passes
- No new package.json dependencies
</success_criteria>

<output>
Create `.planning/phases/70-branding-config/70-04-SUMMARY.md` capturing:
- Exact line numbers hit in the current tree for each edit (AppShell.tsx L232/L616/L1569 or the drifted equivalents; PrettyConversationsPanel L135 import + L1489-1497 header; Auth.tsx L1131-1142 header + L1314 loginTitle)
- Whether SkynetLogo import in PrettyConversationsPanel was removed (grep result before/after)
- Whether SkynetLogo.tsx file was left in the tree or flagged as dead code for future cleanup (grep across src/ for other consumers)
- Which fifth-surface option was chosen (Option 1 locale-neutralize vs Option 2 dynamic template) and rationale
- Any snapshot tests updated (list the snapshot file paths + one-line description of the update)
- Handoff note to Plan 70-05: end-to-end verify surfaces to hit — (1) new PrettyConversationsPanel header (icon+wordmark); (2) login screen header (icon+wordmark); (3) browser tab title on close of last tab; (4) favicon after page reload; (5) login screen title text ("Login" or "Login to X" depending on option chosen).
</output>
</content>
