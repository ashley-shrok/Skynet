---
phase: 68-compose-send-funnel
plan: 03
type: execute
wave: 3
depends_on:
  - 68-02
files_modified:
  - src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx
autonomous: true
requirements: [D-02, D-03, D-04, D-05, D-06]
must_haves:
  truths:
    - "Test 2 locks queue-slot send: 1 pending bubble seeded with the slot's collapsed payload, slot removed from state on success, onSend called with mqid matching pv-optim regex"
    - "Test 3 locks thumbs-up: 1 pending bubble whose visible text contains '👍' (NOT 'thumbs up'), onSend called with literal payload 'thumbs up' and well-formed mqid, button is NOT disabled when the session state would have been the pre-refactor dormant-disable state"
    - "Test 4 locks recap: 1 pending bubble whose visible text contains '/explain the current situation' (no override), onSend called with the same string + well-formed mqid, button is NOT disabled when dormant"
    - "Test 5 locks reset (the CONTEXT.md hypothesis verification): 0 pending bubbles visible after reset click, onSend called with payload matching /^\\/id reset/, onSend called with a well-formed mqid, sendInput's outgoing WS frame carries messageQueueItemId (verified via getCurrentWs().send.mock.calls parse)"
    - "Test suite runs green: 5 tests total in ComposeBox.send-funnel.test.tsx (Test 1 from Plan 68-01 + Tests 2-5 from this plan)"
  artifacts:
    - path: "src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx"
      provides: "Per-trigger locking tests: queue-slot, thumbs-up, recap, reset"
      contains: "queue-slot"
  key_links:
    - from: "Test 5 (reset)"
      to: "the messageQueueItemId presence check on the outgoing WS input frame"
      via: "getCurrentWs().send.mock.calls parse + JSON.parse of frame payload"
      pattern: "messageQueueItemId"
    - from: "Test 3 (thumbs-up)"
      to: "the 👍 override bubble rendering"
      via: "querySelector('[data-event-id^=pending-]').textContent.includes('👍')"
      pattern: "👍"
---

<objective>
Add per-trigger locking tests to `src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx` (created in Plan 68-01 with Test 1 for main-textarea) — one test per remaining trigger: queue-slot, thumbs-up, recap, reset. Each test verifies: (a) bubble seeded correctly (or blacklisted correctly for reset), (b) wake fires when dormant (verified indirectly by asserting the WS frame carries `messageQueueItemId` — the backend wake gate keys on this shape), (c) dispatch reaches Claude (onSend called with expected payload). Includes a specific WS-shape assertion for the reset-wake fix (Test 5) that verifies the CONTEXT.md hypothesis: reset's post-refactor WS frame carries messageQueueItemId, satisfying the Phase 56 backend wake gate's pretty-view submit shape check.

Purpose: Fulfill D-06 — one in-process test per trigger locking new behavior. Prevents any regression that would silently drop bubbles, disable buttons when dormant, or omit the mqid on any send. Test 5 is the executable specification for the "reset stops falling into bare bash on dormant sessions" fix.

Output: `ComposeBox.send-funnel.test.tsx` grows from 1 → 5 tests, all passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/68-compose-send-funnel-universal-send-funnel-for-prettyview-com/68-CONTEXT.md
@.planning/phases/68-compose-send-funnel-universal-send-funnel-for-prettyview-com/68-PATTERNS.md
@.planning/phases/68-compose-send-funnel-universal-send-funnel-for-prettyview-com/68-01-SUMMARY.md
@.planning/phases/68-compose-send-funnel-universal-send-funnel-for-prettyview-com/68-02-SUMMARY.md
@.planning/shapes/shape-compose-send-funnel.md
@src/ui/features/pretty-view/ComposeBox.tsx
@src/ui/features/pretty-view/PrettyView.tsx
@src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx
@src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Test 2 (queue-slot) and Test 3 (thumbs-up) to ComposeBox.send-funnel.test.tsx</name>
  <files>src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx (current state — Test 1 from Plan 68-01, mount/harness helpers)
    - src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx (Test 1 at L188-207 as the assertion-shape template; the WS stub + mqid capture pattern)
    - src/ui/features/pretty-view/ComposeBox.tsx (post-Plan-68-02: the queue-slot text-only branch after rewire, the thumbs-up wire site with `handleQuickSend("thumbs up", { bubbleTextOverride: "👍" })`, the relaxed disable predicate at L2379)
    - src/ui/features/pretty-view/ComposeBox.queued-slot-paste.test.tsx (existing test for queue-slot behavior — reference for how queue-slot rows are surfaced in the DOM + how their textareas are addressed)
    - .planning/phases/68-compose-send-funnel-universal-send-funnel-for-prettyview-com/68-02-SUMMARY.md (exact line numbers of the rewired sites; exact bubbleTextOverride byte value for the 👍 codepoint)
  </read_first>
  <behavior>
    - Test 2 "queue-slot send routes through funnel — 1 bubble, slot removed, mqid present": create a queue slot with text "slot payload" (via the same UI path queued-slot-paste.test.tsx uses — programmatically populate a slot before the send, likely by driving the "Queue" or "+" affordance and typing into the resulting slot textarea, OR by seeding `queueSlots` through the compose-drafts hydration mock's `queueSlots: [{ id: "s1", text: "slot payload" }]` return). Fire the slot's Send button. Assert: `countPendingBubbles(container) === 1`; pending bubble's text contains "slot payload"; `onSendMqidCapture` matches `/^pv-optim-\\d+-[0-9a-z]{8}$/`; `onSendMock` was called with `("slot payload", <mqid>)`; the queue slot row is no longer in the DOM after the send resolves (waitFor on the slot's removal).
    - Test 3 "thumbs-up routes through funnel — 1 bubble showing 👍, onSend receives 'thumbs up', button clickable when dormant": mount + flipToStreaming. Locate the thumbs-up button by aria-label `"Send 'thumbs up'"` (matches ComposeBox L2380 `aria-label="Send 'thumbs up'"`). Assert `button.disabled === false` — this asserts the D-05 relaxation. Fire click. Assert: `countPendingBubbles(container) === 1`; pending bubble's text contains "👍" (the codepoint from 68-02-SUMMARY.md); pending bubble's text does NOT contain "thumbs up"; `onSendMqidCapture` matches the pv-optim regex; `onSendMock` was called with `("thumbs up", <mqid>)` — the LITERAL string goes to onSend, NOT the override.
  </behavior>
  <action>
    Open `src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx`. After Test 1's closing brace (the existing `it("Test 1: main textarea Enter routes through useComposeSend...")` block), add Test 2 and Test 3 as sibling `it(...)` blocks inside the same `describe`. For Test 2, use the compose-drafts hydration mock to seed a queue slot: change the beforeEach's `vi.mock("@/api/compose-drafts-api", ...)` to `getComposeDraft: vi.fn().mockResolvedValue({ body: "", queueSlots: [{ id: "s1", text: "slot payload" }] })` for THIS test only (either scope the mock per-test with `vi.mocked(getComposeDraft).mockResolvedValueOnce(...)` inside Test 2, OR restructure the file's mock to be overridable per test). After hydration, find the queue-slot row's Send button (grep the ComposeBox render for how queue-slot Send buttons are rendered — the queue-slot map renders a Button with an aria-label containing "Send" that fires `handleQueueSlotSend(slot.id)`; identify the correct selector by inspecting the DOM after mount, e.g., using `container.querySelector` and inspecting textContent, or by using testing-library's `getByRole('button', { name: /send/i })` scoped to the slot row). Fire the button click. Assert per the <behavior> block. For Test 3, click the button matched by `container.querySelector('button[aria-label="Send \\'thumbs up\\'"]')` — assert `button.disabled === false` BEFORE clicking (the D-05 lockdown assertion), then fire click and assert per the <behavior>. Emoji byte in the assertion MUST be the same U+1F44D codepoint used in ComposeBox.tsx (copy from 68-02-SUMMARY.md); do NOT re-type the character — use string literal `"\\uD83D\\uDC4D"` or paste the codepoint verbatim from the SUMMARY to avoid mojibake. Do NOT add Tests 4-5 in this task — they're Task 2.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tiffany && npx vitest run src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx` exits 0 with `3 passed` (Test 1 from Plan 68-01 + Test 2 + Test 3).
    - Vitest output includes both `Test 2: queue-slot send routes through funnel` and `Test 3: thumbs-up routes through funnel` as PASS lines.
    - `grep -c 'it("Test [123]:' src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx` == 3.
    - `grep -c '"thumbs up"' src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx` >= 2 (both the aria-label match and the onSendMock call assertion mention it).
    - `grep -cE 'button\\.disabled\\s*===?\\s*false|expect\\(button\\.disabled\\)\\.toBe\\(false\\)' src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx` >= 1 (Test 3 asserts the D-05 relaxation).
    - `npx tsc --noEmit` reports zero errors on the test file.
  </acceptance_criteria>
  <done>Tests 2 and 3 landed and passing; 3/3 tests green in the file so far.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Test 4 (recap) and Test 5 (reset + WS-shape verification)</name>
  <files>src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx (post-Task-1 state — Tests 1, 2, 3)
    - src/ui/features/pretty-view/ComposeBox.tsx (post-Plan-68-02: recap wire site at L2411 with `handleQuickSend("/explain the current situation")` — no override; reset button wiring — find via `grep -n "handleResetClick\\|onResetClicked\\|onClick.*Reset\\|onClick=\\{handleReset" ComposeBox.tsx`; recap aria-label at L2413 `aria-label="Recap the current situation"`)
    - src/ui/features/pretty-view/PrettyView.tsx L996-1011 (`sendInput` — the WS frame construction with the `messageQueueItemId` conditional; Test 5 asserts on this frame's shape)
    - src/ui/features/pretty-view/PrettyView.tsx L1146-1210 (post-Plan-68-02: handleOptimisticSend with the isIdCommand early-return — the mechanism that suppresses reset's bubble)
    - .planning/phases/68-compose-send-funnel-universal-send-funnel-for-prettyview-com/68-CONTEXT.md § domain § "Reset button drops `/id reset` into bare bash on dormant sessions instead of waking first. Root-cause hypothesis... the reset send omits the `messageQueueItemId` field... Routing reset through the funnel means it inherits mqid generation and the payload shape matches." — Test 5 is the executable verification of this hypothesis.
    - .planning/phases/68-compose-send-funnel-universal-send-funnel-for-prettyview-com/68-CONTEXT.md § claude_discretion § "How the reset-wake fix is verified — via the in-process test alone, or by an additional targeted assertion on the WS message shape. Planner's call." — planner's decision: BOTH. Test 5 asserts on the WS message shape directly because it's the load-bearing surface the backend keys on.
    - .planning/phases/68-compose-send-funnel-universal-send-funnel-for-prettyview-com/68-02-SUMMARY.md (recap wire site line number; reset button wire location)
  </read_first>
  <behavior>
    - Test 4 "recap routes through funnel — 1 bubble with /explain text, no override, button clickable when dormant": mount + flipToStreaming. Locate the recap button by aria-label `"Recap the current situation"`. Assert `button.disabled === false` (D-05 relaxation on recap). Fire click. Assert: `countPendingBubbles(container) === 1`; pending bubble's text contains "/explain the current situation"; `onSendMqidCapture` matches pv-optim regex; `onSendMock` was called with `("/explain the current situation", <mqid>)` — the SAME string in both bubble and onSend (recap has no override, per D-02).
    - Test 5 "reset routes through funnel — 0 bubbles (render-blacklist honored) BUT WS frame carries messageQueueItemId (wake gate fires)": mount + flipToStreaming. Locate the reset button by whatever DOM signature the reset control uses — grep ComposeBox for `handleResetClick` / `handleResetSend` wire site (likely the meter-well reset cell rendered inside the segmented meter component; find via `container.querySelector` with the aria-label or icon match). If no aria-label is present on the reset control, use `container.querySelector('[data-testid="reset"]')` — and if no data-testid exists, add one as a test hook in this task (add `data-testid="pv-compose-reset"` on the reset button in ComposeBox.tsx and cite it in the test — this is a legitimate test-affordance edit permitted within this plan's scope). Fire click. Assert: `countPendingBubbles(container) === 0` (render-blacklist honored — reset produces no visible bubble); `onSendMock` was called with a payload matching `/^\\/id reset/` (bare `/id reset` when the textarea was empty at click time); the CAPTURED SECOND ARGUMENT to onSendMock IS defined and matches the pv-optim regex (funnel still generated an mqid per D-03); PARSE the outgoing WS frame from `getCurrentWs().send.mock.calls` — find the call whose JSON.parse'd body has `type === "input"` AND `data.startsWith("/id reset")`, then assert that the parsed body has property `messageQueueItemId` equal to the captured mqid. This last assertion is the executable verification of CONTEXT.md's reset-wake hypothesis: post-refactor, reset's WS frame IS in the pretty-view submit shape the backend wake gate needs.
  </behavior>
  <action>
    Open `src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx`. After Test 3's closing brace, add Test 4 and Test 5 as sibling `it(...)` blocks. Test 4: click the button matched by `container.querySelector('button[aria-label="Recap the current situation"]')`; assert `button.disabled === false`; fire click; assert per <behavior>. Test 5: identify the reset button — first pass: `grep -n "handleResetClick\\|Reset\\|meter-reset" src/ui/features/pretty-view/ComposeBox.tsx` to find the reset control's render; if it lacks a stable selector, add `data-testid="pv-compose-reset"` to the reset Button element in ComposeBox.tsx as a minimal test-affordance edit (do NOT add any behavior — just the data-testid attribute); locate via `container.querySelector('[data-testid="pv-compose-reset"]')`. Fire click. Assertions per <behavior>: (a) `expect(countPendingBubbles(container)).toBe(0)` — the render-blacklist test; (b) `expect(onSendMock).toHaveBeenCalledWith(expect.stringMatching(/^\\/id reset/), expect.stringMatching(/^pv-optim-\\d+-[0-9a-z]{8}$/))` — payload + mqid shape; (c) locate the WS frame via `const ws = getCurrentWs(); const sentCalls = ws.send.mock.calls.map(c => JSON.parse(c[0] as string)); const inputFrame = sentCalls.find(f => f.type === "input" && typeof f.data === "string" && f.data.startsWith("/id reset")); expect(inputFrame).toBeDefined(); expect(inputFrame.messageQueueItemId).toBe(onSendMqidCapture);` — the load-bearing wake-gate-shape assertion. Set `voice.state === "idle"` in the test setup so the reset button router `handleResetClick` takes the `handleResetSend` branch (not `handleVoiceResetSend`) — the existing beforeEach setup should already produce idle voice state; verify by inspection.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tiffany && npx vitest run src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx --reporter=verbose 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx` exits 0 with `5 passed`.
    - Vitest output includes all five test names as PASS: `Test 1: main textarea`, `Test 2: queue-slot send`, `Test 3: thumbs-up routes`, `Test 4: recap routes`, `Test 5: reset routes through funnel — 0 bubbles`.
    - `grep -c 'it("Test [12345]:' src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx` == 5.
    - Test 5 body contains an assertion on `messageQueueItemId` in the WS frame: `grep -c "messageQueueItemId" src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx` >= 1 (Test 5 executes the reset-wake shape check).
    - If a data-testid was added to ComposeBox.tsx for reset: `grep -c 'data-testid="pv-compose-reset"' src/ui/features/pretty-view/ComposeBox.tsx` == 1 AND `grep -c 'pv-compose-reset' src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx` >= 1.
    - `npx tsc --noEmit` reports zero errors.
    - Full pretty-view test suite still green: `npx vitest run src/ui/features/pretty-view/` — no new failures introduced anywhere else.
    - Regression scan for the D-05 lockdown: `grep -cE 'button\\.disabled\\s*===?\\s*false|expect\\(button\\.disabled\\)\\.toBe\\(false\\)' src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx` >= 2 (Tests 3 + 4 both assert the disable-predicate relaxation).
  </acceptance_criteria>
  <done>5/5 tests green; D-06 satisfied (one test per trigger); Test 5 executes the reset-wake hypothesis verification via WS-frame shape assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → real ComposeBox / PrettyView code paths | Tests exercise the funnel end-to-end through the real WS stub scaffold. |
| Test WS stub → sendInput | Tests parse outgoing WS frames from the mock's calls array — this is the primary verification surface for the wake-shape gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-09 | Tampering | Test-only data-testid attribute added to ComposeBox.tsx for the reset button | mitigate | If a `data-testid="pv-compose-reset"` is added, it MUST NOT alter any runtime behavior — it is a DOM attribute with zero React state or handler binding. Verify by acceptance criteria that adding it does not change any other test's pass/fail count. Alternative: use existing DOM affordances if the reset button already has a stable selector (aria-label / icon component name / test-visible text) — planner's grep guidance in Task 2 <action> covers this. |
| T-68-10 | Tampering | Test 5's WS-shape assertion could false-positive if the mock's send.mock.calls includes non-input frames that happen to satisfy the filter | mitigate | Test 5's filter is `f.type === "input" && typeof f.data === "string" && f.data.startsWith("/id reset")` — a triple conjunction that only matches the specific reset input frame. The `data.startsWith("/id reset")` filter excludes control frames (`session`, `resize`, etc.) and other input frames (main-textarea sends of different text). If reset is fired only ONCE per test, the filter yields exactly one frame. Test asserts `expect(inputFrame).toBeDefined()` before dereferencing `messageQueueItemId` so a missing frame produces a clear failure rather than a null deref. |
| T-68-11 | Information Disclosure | Test emoji codepoint mismatch (👍 vs. another U+1F44D encoding) causing false test failures | mitigate | Task 1's <action> mandates copying the exact byte from 68-02-SUMMARY.md rather than re-typing. If the assertion uses `String.fromCodePoint(0x1F44D)` or the JS escape `"\\uD83D\\uDC4D"`, the byte value is canonical. Verify: `grep -c "👍\\|\\\\uD83D\\\\uDC4D\\|fromCodePoint(0x1F44D)" src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx` >= 1. |
| T-68-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs. Verify: `git diff package.json package-lock.json` == 0 lines. |
</threat_model>

<verification>
- All 5 tests pass: `npx vitest run src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx` reports `5 passed, 0 failed`.
- Full pretty-view suite green: `npx vitest run src/ui/features/pretty-view/` — no new failures introduced.
- `npx tsc --noEmit` clean.
- Test 5's WS-shape assertion executes: the parsed inputFrame has `messageQueueItemId` equal to the captured mqid — this is the load-bearing verification of the CONTEXT.md reset-wake hypothesis.
- Grep audit for D-06 coverage: `grep -c 'it("Test [12345]:' src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx` == 5 — one test per user-driven trigger (main-textarea, queue-slot, thumbs-up, recap, reset), matching the shape file's five affordances exactly.
</verification>

<success_criteria>
- Each of the 5 user-driven send-affordances has a dedicated in-process test (D-06 satisfied).
- Test 5 is the executable specification for the reset-wake fix — if a future refactor re-introduces the mqid-less reset send, this test fails immediately with a specific message about the WS frame shape.
- Tests 3 and 4 lock the D-05 disable-predicate relaxation — if a future refactor re-adds `canSend === false` to the thumbs-up or recap disabled expression, the `button.disabled === false` assertion fails.
- Test 3 locks the D-02 bubbleTextOverride behavior — if a future refactor drops the override, the "bubble contains 👍" + "bubble does NOT contain 'thumbs up'" assertions fail.
- Test 2 locks the queue-slot flow — if a future refactor omits mqid on queue-slot or forgets to remove the slot on success, the test fails.
- All shape-file "what would make it wrong" invariants are now guarded by a test: (a) characters ending up in bare bash → Test 5's messageQueueItemId assertion; (b) thumbs-up/recap silently doing nothing when dormant → Tests 3+4's `button.disabled === false` assertion; (c) main-textarea missing bubble → Test 1 (Plan 68-01); (d) reset showing a bubble → Test 5's `countPendingBubbles === 0`.
</success_criteria>

<output>
Create `.planning/phases/68-compose-send-funnel-universal-send-funnel-for-prettyview-com/68-03-SUMMARY.md` when done capturing: (a) final test count (should be 5), (b) whether a test-affordance `data-testid` was added to ComposeBox.tsx for the reset button (yes/no + rationale), (c) any DOM-affordance discovery notes (e.g., how the queue-slot Send button was located, whether an existing test file's helper pattern was reused), (d) executable time for the test file (from vitest reporter output) so future maintainers know the baseline for regression tracking, (e) confirmation that Test 5's WS-frame parse asserts `messageQueueItemId === capturedMqid`.
</output>
