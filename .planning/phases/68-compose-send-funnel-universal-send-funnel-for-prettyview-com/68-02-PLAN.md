---
phase: 68-compose-send-funnel
plan: 02
type: execute
wave: 2
depends_on:
  - 68-01
files_modified:
  - src/ui/features/pretty-view/ComposeBox.tsx
  - src/ui/features/pretty-view/PrettyView.tsx
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05]
must_haves:
  truths:
    - "Queue-slot send text-only branch (L1391-1404) invokes the funnel; on dispatched=true the slot is removed + scheduleAutosave fires; on dispatched=false the slot is preserved (D-20 posture unchanged)"
    - "Thumbs-up button routes through the funnel with bubbleTextOverride='👍' so the seeded bubble renders 👍 not 'thumbs up' (D-02); onSend still receives the literal 'thumbs up' payload"
    - "Recap button routes through the funnel with NO bubbleTextOverride so the seeded bubble renders '/explain the current situation' verbatim (D-02: recap does not use override)"
    - "Reset (dispatchResetPayload) routes through the funnel — every reset send carries a well-formed mqid (D-03), backend Phase 56 wake gate fires for dormant reset just like it does for main-textarea"
    - "Reset render-blacklist honored downstream — the /id command's optimistic bubble does NOT render as a visible pv-bubble to the user"
    - "Thumbs-up wire-site disabled expression at L2379 no longer contains the term `canSend === false` (D-05); the other terms (asideActive, recycleActive, planPendingActive, reconnectingActive) are preserved verbatim"
    - "Recap wire-site disabled expression at L2412 no longer contains the term `canSend === false` (D-05); the other terms are preserved verbatim"
    - "The 4 other-site canSend uses (L1926 send-button gate, L2158 unrelated, L2458 prop passthrough, L2618 comment) are NOT touched"
  artifacts:
    - path: "src/ui/features/pretty-view/ComposeBox.tsx"
      provides: "handleQueueSlotSend + handleQuickSend + dispatchResetPayload all rewired through funnel; L2379 + L2412 disable predicates relaxed"
      contains: "funnel.send"
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "handleOptimisticSend render-blacklist branch that suppresses pv-bubble render for /id command payloads"
      contains: "isIdCommand"
  key_links:
    - from: "src/ui/features/pretty-view/ComposeBox.tsx dispatchResetPayload"
      to: "src/ui/features/pretty-view/PrettyView.tsx sendInput"
      via: "onSend(payload, mqid) — mqid now always present"
      pattern: "funnel\\.send\\("
    - from: "src/ui/features/pretty-view/PrettyView.tsx handleOptimisticSend"
      to: "the pendingSends render at L3146"
      via: "conditional record-suppression on /id command payloads"
      pattern: "isIdCommand"
    - from: "thumbs-up wire site (L2378-2379)"
      to: "handleQuickSend"
      via: "onClick handler passes bubbleTextOverride to funnel through handleQuickSend"
      pattern: "handleQuickSend"
---

<objective>
Rewire the remaining 4 send-triggers in `ComposeBox.tsx` through the `useComposeSend` hook created in Plan 68-01: (a) `handleQueueSlotSend` text-only branch (L1391-1404), (b) `handleQuickSend` for thumbs-up + recap wire sites (L1790-1816 handler; L2378-2379 thumbs-up wire; L2411-2412 recap wire), (c) `dispatchResetPayload` (L1735-1747). Add a `bubbleTextOverride` parameter through `handleQuickSend` so the thumbs-up caller can seed a "👍" bubble (D-02). Relax the disable predicate on thumbs-up (L2379) and recap (L2412) by removing the `canSend === false ||` term (D-05). Add render-blacklist logic in `PrettyView.tsx`'s `handleOptimisticSend` that suppresses the pending-bubble seed for `/id` command payloads so reset's mqid still fires the backend wake gate (D-03) without producing a visible bubble.

Purpose: Close the shape file's four bug bundles — (1) missing bubbles on queue-slot/thumbs-up/recap, (2) thumbs-up/recap disabled-when-dormant legacy artifact, (3) reset dropping `/id reset` into bare bash on dormant sessions, (4) uniform WS `input` frame shape carrying `messageQueueItemId` on every send.

Output: Every send-trigger in ComposeBox routes through the funnel; `messageQueueItemId` is present on 100% of `input` WS frames from the pretty view; thumbs-up + recap enabled when dormant with wake-then-send routing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/68-compose-send-funnel-universal-send-funnel-for-prettyview-com/68-CONTEXT.md
@.planning/phases/68-compose-send-funnel-universal-send-funnel-for-prettyview-com/68-PATTERNS.md
@.planning/phases/68-compose-send-funnel-universal-send-funnel-for-prettyview-com/68-01-SUMMARY.md
@.planning/shapes/shape-compose-send-funnel.md
@src/ui/features/pretty-view/ComposeBox.tsx
@src/ui/features/pretty-view/PrettyView.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire handleQueueSlotSend + dispatchResetPayload through the funnel</name>
  <files>src/ui/features/pretty-view/ComposeBox.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/ComposeBox.tsx (post-68-01 state: the useComposeSend hook body, the main-textarea handleSend rewire — use them as the canonical pattern; the untouched handleQueueSlotSend at L1391-1404, dispatchResetPayload at L1735-1747, handleResetSend at L1749-1752, handleVoiceResetSend at L1761-1767, fireResetSyncFx at L1691-1731)
    - .planning/phases/68-compose-send-funnel-universal-send-funnel-for-prettyview-com/68-01-SUMMARY.md (the hook signature landed by Plan 68-01 — call site pattern to copy)
    - .planning/phases/68-compose-send-funnel-universal-send-funnel-for-prettyview-com/68-CONTEXT.md § decisions D-01, D-03; § deferred § "Bubble render-blacklist logic — stays as-is in the render layer"
    - .planning/phases/68-compose-send-funnel-universal-send-funnel-for-prettyview-com/68-PATTERNS.md § "Call site A: handleQueueSlotSend"; § "Call site C: dispatchResetPayload"
  </read_first>
  <behavior>
    - `handleQueueSlotSend` text-only branch (L1391-1404): `setErrorMessage(null)`, `const payload = collapseNewlinesForSend(trimmed)`, `const dispatched = funnel.send(payload, { trigger: "queue-item" })`, on true: `setQueueSlots(nextSlots)` + `scheduleAutosave(latestBodyRef.current, nextSlots)`, on false: `setErrorMessage("Not connected — try again in a moment")` + slot preserved (D-20 posture). Attachment branch at L1360-1389 UNTOUCHED (out of scope per shape file).
    - `dispatchResetPayload(body)` (L1735-1747): construct payload exactly as today — `const trimmed = body.trim(); const payload = trimmed ? \`/id reset (\${collapseNewlinesForSend(trimmed)})\` : "/id reset";` — then `const dispatched = funnel.send(payload, { trigger: "reset" })` instead of `const dispatched = onSend(payload)`. On dispatched=true: `setText("")` + `clearAfterSend()`. On dispatched=false: `setErrorMessage("Not connected — try again in a moment")`. `fireResetSyncFx()` at L1691-1731 is UNTOUCHED — it runs before dispatchResetPayload in both handleResetSend and handleVoiceResetSend. `handleResetSend` and `handleVoiceResetSend` bodies are UNTOUCHED — they still call `fireResetSyncFx()` then `dispatchResetPayload(text)` / await `voice.endSend` then `dispatchResetPayload(body)`.
    - Post-rewire: every WS `input` frame from queue-slot and reset now carries `messageQueueItemId` (funnel always generates mqid). Backend Phase 56 wake gate at `src/backend/claude-session/claude-session-server.ts` L2070-2076 fires on dormant sessions for BOTH triggers.
    - Log-format preservation: the funnel's submit-entry/success/failed logs use `trigger=queue-item` for queue-slot and `trigger=reset` for reset. Existing sibling tests do NOT assert on these specific trigger labels; if any do (grep to confirm), keep the existing label instead of introducing "reset" as a new one — planner's escape hatch is "reuse whatever handleSend's caller uses today for this trigger."
  </behavior>
  <action>
    Locate `handleQueueSlotSend` at L1391-1404 (post-attachment-branch text-only tail). Replace lines L1391-1404 with a call to the funnel: keep `setErrorMessage(null);` and `const payload = collapseNewlinesForSend(trimmed);`; change `const dispatched = onSend(payload);` to `const dispatched = funnel.send(payload, { trigger: "queue-item" });`; keep the existing `if (dispatched) { const nextSlots = queueSlots.filter((s) => s.id !== slotId); setQueueSlots(nextSlots); scheduleAutosave(latestBodyRef.current, nextSlots); } else { setErrorMessage("Not connected — try again in a moment"); }` body verbatim. Locate `dispatchResetPayload` at L1735-1747. Keep the payload construction verbatim (`const trimmed = body.trim(); const payload = trimmed ? \`/id reset (\${collapseNewlinesForSend(trimmed)})\` : "/id reset";`); change `const dispatched = onSend(payload);` to `const dispatched = funnel.send(payload, { trigger: "reset" });`; keep the `if (dispatched) { setText(""); clearAfterSend(); } else { setErrorMessage("Not connected — try again in a moment"); }` body verbatim. Do NOT touch handleResetSend, handleVoiceResetSend, fireResetSyncFx — they compose dispatchResetPayload and inherit the rewire transitively. Do NOT touch the queue-slot attachment branch at L1360-1389 (out of scope per shape file § Deferred). Do NOT touch handleQuickSend in this task — Task 2 owns it.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tiffany && npx tsc --noEmit 2>&1 | grep -E "ComposeBox\\.tsx" | head -5 ; echo "---" ; grep -c "funnel\\.send(" src/ui/features/pretty-view/ComposeBox.tsx ; echo "---" ; grep -cE "^\\s*const dispatched = onSend\\(" src/ui/features/pretty-view/ComposeBox.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` reports zero errors on `src/ui/features/pretty-view/ComposeBox.tsx`.
    - `grep -c "funnel\\.send(" src/ui/features/pretty-view/ComposeBox.tsx` >= 3 (main-textarea from 68-01 + queue-slot + reset).
    - `grep -cE "^\\s*const dispatched = onSend\\(" src/ui/features/pretty-view/ComposeBox.tsx` == 1 (only handleQuickSend still calls onSend directly at this task boundary; Task 2 rewires it).
    - `grep -c "trigger: \"queue-item\"" src/ui/features/pretty-view/ComposeBox.tsx` >= 1.
    - `grep -c "trigger: \"reset\"" src/ui/features/pretty-view/ComposeBox.tsx` >= 1.
    - The reset payload construction still produces the exact strings `/id reset` and `/id reset (<collapsed body>)`: `grep -c '/id reset' src/ui/features/pretty-view/ComposeBox.tsx` >= 2 (payload literals preserved).
    - Existing tests still green: `npx vitest run src/ui/features/pretty-view/ComposeBox.test.tsx src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx src/ui/features/pretty-view/PrettyView.compose-send.test.tsx` — all previously-green tests remain green.
  </acceptance_criteria>
  <done>Queue-slot and reset dispatch tails call `funnel.send(...)`; attachment branches and fireResetSyncFx untouched; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire handleQuickSend + add bubbleTextOverride for thumbs-up + relax L2379/L2412 disable predicates</name>
  <files>src/ui/features/pretty-view/ComposeBox.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/ComposeBox.tsx (post-Task-1 state: handleQuickSend at L1790-1816, thumbs-up wire site at L2375-2396, recap wire site at L2404-2429; the useComposeSend hook body — API for bubbleTextOverride landed in Plan 68-01)
    - .planning/phases/68-compose-send-funnel-universal-send-funnel-for-prettyview-com/68-CONTEXT.md § decisions D-02, D-04, D-05; § specifics § "User's thumbs-up-render argument" (load-bearing rationale)
    - .planning/phases/68-compose-send-funnel-universal-send-funnel-for-prettyview-com/68-PATTERNS.md § "Call site B: handleQuickSend"; § "D-02 bubble-text override"; § "D-04/D-05 disable-predicate relaxation"
  </read_first>
  <behavior>
    - `handleQuickSend(quickText, options?)`: signature widens to accept an optional second parameter `{ bubbleTextOverride?: string }`. Body: `setErrorMessage(null)`, `const dispatched = funnel.send(quickText, { trigger: "quick-reply", bubbleTextOverride: options?.bubbleTextOverride })`, on true: `clearAfterSend()` (intentionally does NOT setText("") — user's typed draft stays visible per L1799 comment), on false: `setErrorMessage("Not connected — try again in a moment")`. Post-dispatch focus restore at L1815 (`if (isTouchDevice !== true) textareaRef.current?.focus();`) is UNTOUCHED.
    - Thumbs-up wire site (currently L2378): `onClick={() => { onGoodToGo?.(); handleQuickSend("thumbs up", { bubbleTextOverride: "👍" }); }}` — funnel seeds a "👍" bubble (D-02 rationale: rendered bubble transforms to 👍 icon in the shipped ChatMessage; optimistic bubble should match the final form from the start, not flash "thumbs up" then flip). The literal string "thumbs up" still goes to onSend so the backend receives what it expects.
    - Recap wire site (currently L2411): `onClick={() => { onGoodToGo?.(); handleQuickSend("/explain the current situation"); }}` — NO bubbleTextOverride (recap's rendered bubble text IS the send text; no transformation).
    - Disable predicate at L2379 (thumbs-up): change from `disabled={canSend === false || asideActive === true || recycleActive === true || planPendingActive === true || reconnectingActive === true}` to `disabled={asideActive === true || recycleActive === true || planPendingActive === true || reconnectingActive === true}` — the `canSend === false ||` term is DELETED (D-05); the other four terms are PRESERVED verbatim.
    - Disable predicate at L2412 (recap): identical relaxation — delete only the `canSend === false ||` term.
    - The 4 other `canSend` sites in ComposeBox.tsx (L1926 send-button gate, L2158 mic-adjacent, L2458 prop passthrough, L2618 comment) are NOT touched — this refactor is scoped to L2379 and L2412 only.
  </behavior>
  <action>
    Locate `handleQuickSend` at L1790-1816. Widen its signature to `function handleQuickSend(quickText: string, options?: { bubbleTextOverride?: string })`. Body: keep `setErrorMessage(null);` at L1796; change `const dispatched = onSend(quickText);` at L1797 to `const dispatched = funnel.send(quickText, { trigger: "quick-reply", bubbleTextOverride: options?.bubbleTextOverride });`; keep the `if (dispatched) { clearAfterSend(); }` at L1798-1805 verbatim (the intentional "does NOT setText" and the comment block explaining why STAY); keep the `else { setErrorMessage("Not connected — try again in a moment"); }` at L1806-1808 verbatim; keep the touch-device focus-restore at L1815 verbatim. Locate the thumbs-up wire site at L2378 (`onClick={() => { onGoodToGo?.(); handleQuickSend("thumbs up"); }}`) — change to `onClick={() => { onGoodToGo?.(); handleQuickSend("thumbs up", { bubbleTextOverride: "👍" }); }}`. Locate the recap wire site at L2411 (`onClick={() => { onGoodToGo?.(); handleQuickSend("/explain the current situation"); }}`) — do NOT change the onClick (recap has no override). Locate the disable predicates at L2379 (thumbs-up) and L2412 (recap) — each currently reads `disabled={canSend === false || asideActive === true || recycleActive === true || planPendingActive === true || reconnectingActive === true}`. Change each to `disabled={asideActive === true || recycleActive === true || planPendingActive === true || reconnectingActive === true}` — delete the leading `canSend === false || ` term ONLY. Do NOT touch any other `canSend` usage in the file (grep to confirm — 4 other sites are load-bearing and out of scope for D-05).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tiffany && npx tsc --noEmit 2>&1 | grep -E "ComposeBox\\.tsx" | head -5 ; echo "---" ; grep -cE "^\\s*const dispatched = onSend\\(" src/ui/features/pretty-view/ComposeBox.tsx ; echo "---" ; grep -c "funnel\\.send(" src/ui/features/pretty-view/ComposeBox.tsx ; echo "---" ; grep -c 'bubbleTextOverride: "👍"' src/ui/features/pretty-view/ComposeBox.tsx ; echo "---" ; grep -c "canSend === false ||" src/ui/features/pretty-view/ComposeBox.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` reports zero errors on `src/ui/features/pretty-view/ComposeBox.tsx`.
    - `grep -cE "^\\s*const dispatched = onSend\\(" src/ui/features/pretty-view/ComposeBox.tsx` == 0 (all 5 send-triggers now route through `funnel.send`; direct `onSend(...)` calls at the dispatch level are gone).
    - `grep -c "funnel\\.send(" src/ui/features/pretty-view/ComposeBox.tsx` >= 4 (main-textarea, queue-slot, reset, quick-reply). Note: `handleQuickSend` is called from TWO wire sites (thumbs-up + recap) but there is only ONE `funnel.send(...)` call inside `handleQuickSend` itself.
    - `grep -c 'bubbleTextOverride: "👍"' src/ui/features/pretty-view/ComposeBox.tsx` == 1 (thumbs-up wire site passes the override).
    - `grep -c "canSend === false ||" src/ui/features/pretty-view/ComposeBox.tsx` == 1 (the L1926 send-button gate reads `(canSend === false && !hasAttachments)` not `canSend === false ||`, so that site does not match; the ONLY remaining `canSend === false ||` after L2379 and L2412 relaxation is... check by inspection — planner expected count is 0 or 1. If 1 remains, verify it's not L2379 or L2412; if 0, both L2379 + L2412 successfully relaxed).
    - `grep -nE "disabled=\\{.*canSend === false" src/ui/features/pretty-view/ComposeBox.tsx` — the reported line numbers MUST NOT include L2379 or L2412 (both must be relaxed; any other sites keeping `canSend === false` are load-bearing and out of scope).
    - The thumbs-up onClick still passes the literal string `"thumbs up"` as the first arg (not "👍"): `grep -c 'handleQuickSend("thumbs up"' src/ui/features/pretty-view/ComposeBox.tsx` == 1 (onSend receives the literal command; only the bubble renders 👍).
    - The recap onClick is unchanged: `grep -c 'handleQuickSend("/explain the current situation")' src/ui/features/pretty-view/ComposeBox.tsx` == 1 (single-arg form — no override object).
    - Existing tests still green: `npx vitest run src/ui/features/pretty-view/ComposeBox.test.tsx src/ui/features/pretty-view/ComposeBox.send-funnel.test.tsx src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx src/ui/features/pretty-view/PrettyView.compose-send.test.tsx` — all previously-green tests remain green.
  </acceptance_criteria>
  <done>All 5 send-triggers route through `funnel.send`; thumbs-up bubble text overridden to 👍; L2379+L2412 disable predicates relaxed; other canSend sites untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add reset render-blacklist in PrettyView.handleOptimisticSend</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/PrettyView.tsx (whole handleOptimisticSend at L1140-1210; pendingSends render at L3146-3167; the pre-existing `isIdCommand` module-scoped helper at L425-427 that already recognizes both invocation forms of /id)
    - src/ui/features/pretty-view/ComposeBox.tsx (post-Task-2 state: the reset caller now sends `/id reset` or `/id reset (...)` with mqid — the render-blacklist trigger the funnel plumbs through)
    - .planning/phases/68-compose-send-funnel-universal-send-funnel-for-prettyview-com/68-CONTEXT.md § D-03; § code_context § "Render-blacklist for reset — already exists somewhere in the render layer (needs research to locate exactly). Funnel does NOT know about it; render layer honors it."
    - .planning/shapes/shape-compose-send-funnel.md § "What would make it wrong" § "Reset shows a bubble — violates the render blacklist"
  </read_first>
  <behavior>
    - `handleOptimisticSend` at L1146-1210 gets a leading blacklist check: if the collapsed payload matches `isIdCommand` (the existing L425-427 helper that recognizes both `content.trimStart().startsWith("/id ")` AND the harness XML-wrapper `<command-name>/id</command-name>` form), the function RETURNS EARLY without adding to `pendingSends` and without arming the 20s timer.
    - The early-return applies to BOTH the immediateFailure=true branch (L1150-1181) AND the immediateFailure=false branch (L1182-1207) — reset never seeds a visible bubble regardless of dispatch outcome. Rationale: reset is a control-plane operation; the pretty-view doesn't render its own bubble for `/id reset` because the JSONL tail's post-reset session_change signal is the source of truth for what the user sees.
    - Critical invariant: this suppression happens ONLY at the pretty-view bubble render layer. The mqid is still generated by the funnel and still travels through `onSend` → `sendInput` → the WS `input` frame's `messageQueueItemId` field. The backend Phase 56 wake gate at `claude-session-server.ts` L2070-2076 sees the pretty-view submit shape and fires wake — the render-blacklist does NOT bypass the wake path.
    - The blacklist check uses the SAME `isIdCommand` helper the JSONL parser at L2710-ish already uses so the "hide user's /id turn" rule is applied consistently across the pending-bubble surface AND the confirmed-message surface.
  </behavior>
  <action>
    Locate `handleOptimisticSend` at L1146-1210 in `src/ui/features/pretty-view/PrettyView.tsx`. Immediately AFTER `const { payload, mqid, immediateFailure } = args;` (L1148) and BEFORE `const collapsed = collapseNewlinesForMatch(payload);` (L1149), insert a blacklist check: `if (isIdCommand(payload)) { return; }`. This one-line guard covers both `immediateFailure` branches. Verify `isIdCommand` (declared at L425-427) is in scope at the callback body — it is a module-scoped `const`, so no import change is needed; if TypeScript flags the reference for some hoist-order reason, move the `isIdCommand` declaration to earlier in the file (before `PrettyView` function declaration). Do NOT touch the pendingSends render at L3146 (no marker needed on the render side — the record is never added in the first place, so nothing renders). Do NOT touch `flipToFailed`, `clearAllPendingSends`, or the WS-frame match logic — reset never appears in `pendingSends` so those never see reset mqids. Do NOT touch the `sendInput` callback at L996-1011 — the WS `input` frame construction is unchanged and still conditionally attaches `messageQueueItemId` when the funnel provides mqid.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tiffany && npx tsc --noEmit 2>&1 | grep -E "PrettyView\\.tsx" | head -5 ; echo "---" ; grep -nE "if \\(isIdCommand\\(payload\\)\\) \\{ return" src/ui/features/pretty-view/PrettyView.tsx ; echo "---" ; grep -c "isIdCommand" src/ui/features/pretty-view/PrettyView.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` reports zero errors on `src/ui/features/pretty-view/PrettyView.tsx`.
    - `grep -nE "if \\(isIdCommand\\(payload\\)\\) \\{ return" src/ui/features/pretty-view/PrettyView.tsx` returns exactly one match, inside `handleOptimisticSend` (line number between L1146 and L1210).
    - `grep -c "isIdCommand" src/ui/features/pretty-view/PrettyView.tsx` >= 3 (declaration at L425-427 = 2 mentions in the arrow-fn body + 1 new call site = 3+).
    - `sendInput` at L996-1011 is byte-identical to pre-refactor: `git diff src/ui/features/pretty-view/PrettyView.tsx | grep -A1 -B1 "sendInput"` shows no changes to that callback body.
    - Existing tests still green: `npx vitest run src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx src/ui/features/pretty-view/PrettyView.compose-send.test.tsx src/ui/features/pretty-view/PrettyView.test.tsx` — all previously-green tests remain green.
    - Behavioral verify (adhoc — Plan 68-03 formalizes as a test): the sequence `mount → dispatchResetPayload("")` produces zero pending bubbles in the DOM (0 elements matching `[data-event-id^="pending-"]`) but the `onSend` mock IS called with a well-formed mqid.
  </acceptance_criteria>
  <done>Reset payload no longer seeds a pending bubble on the render layer; mqid still flows through onSend/sendInput → WS frame; tsc clean; all existing tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ComposeBox handlers → WS `input` frame | Same as Plan 68-01. Now uniformly carries `messageQueueItemId` on 100% of sends including reset. |
| Hook → PrettyView.handleOptimisticSend | New: reset payloads reach handleOptimisticSend where they get filtered by isIdCommand. |
| Thumbs-up bubbleTextOverride → rendered bubble text | The override string ("👍") is user-visible pretty-view content. Must not be attacker-controllable. |
| Backend Phase 56 wake gate | Now receives mqid on every pretty-view send including reset — expected to fire wake for dormant reset. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-05 | Tampering | Render-blacklist bypass — /id command payload rendering as a bubble | mitigate | The `isIdCommand` helper at PrettyView.tsx L425-427 already handles both invocation forms (raw `/id ` prefix + harness XML-wrapper). Task 3's guard reuses this same helper so the blacklist check matches the existing parser's semantics exactly — no new parser divergence. Any future /id form (e.g. `<command-name>/id</command-name>` with a different wrapper) would need to update isIdCommand in ONE place, not two. Verify by grep: `isIdCommand` is the sole gate for "is this a /id command" across the pretty-view render surface. |
| T-68-06 | Information Disclosure | Sensitive command text leaking into rendered bubble via bubbleTextOverride | mitigate | Only Task 2 introduces a bubbleTextOverride usage, and its value is the hardcoded string literal `"👍"` (not user-controllable, not derived from any dynamic source). The recap caller does NOT pass an override — recap's bubble text is the slash-command payload itself, which is user-initiated by clicking a button (Ashley chose this canned prompt). Verify by grep: `bubbleTextOverride` is set from a string literal only, never from user-typed input or from a variable holding untrusted content. |
| T-68-07 | Elevation of Privilege | Wake path authorization on the reset/thumbs-up/recap paths | accept | The funnel is a pure frontend refactor. Authorization on wake-then-send is enforced backend-side (same auth middleware `sendInput` was already going through). Pre-refactor, reset went through `onSend` → `sendInput` → WS with NO mqid; post-refactor, reset goes through `onSend` → `sendInput` → WS WITH mqid. The backend auth surface (JWT / cookie identity) is unchanged; the only shape difference is the presence of `messageQueueItemId` in the input frame, which the Phase 56 wake gate treats as a shape check for "this is a pretty-view send" — not as an authorization token. Verified by inspection: `sendInput` at PrettyView.tsx L996-1011 body is unchanged; the WS URL, headers, and outer auth flow are all pre-existing. |
| T-68-08 | Denial of Service | Wake-then-send from unauthorized clicks flooding backend wake gate | accept | Thumbs-up and recap buttons are UI affordances — Ashley initiates the click. No new automated wake path is introduced. Rate limiting on the backend session-server (pre-existing) applies uniformly to funnel sends and pre-refactor sends. The disable-predicate relaxation (D-05) removes the `canSend === false` term only — the other four terms (aside/recycle/plan-pending/reconnecting) still prevent clicks during genuinely non-sendable UI states. |
| T-68-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan. Verify: `git diff package.json package-lock.json` produces zero lines. |
</threat_model>

<verification>
- `npx tsc --noEmit` produces zero new errors.
- All 5 send-triggers route through `funnel.send`: `grep -cE "^\\s*const dispatched = onSend\\(" src/ui/features/pretty-view/ComposeBox.tsx` == 0.
- Thumbs-up L2379 and recap L2412 disable predicates no longer contain `canSend === false ||`: `grep -nE "^\\s*disabled=\\{.*canSend === false" src/ui/features/pretty-view/ComposeBox.tsx` must NOT report L2379 or L2412.
- Reset render-blacklist active: `grep -c "if (isIdCommand(payload)) { return" src/ui/features/pretty-view/PrettyView.tsx` == 1.
- Existing test suite regression check: `npx vitest run src/ui/features/pretty-view/` — no new failures.
- Manual end-to-end (Plan 68-03 formalizes as tests): (a) dormant session + main-textarea send → 1 pending bubble + WS frame carries messageQueueItemId + backend wakes; (b) dormant session + thumbs-up click → 1 pending bubble showing 👍 + WS frame carries messageQueueItemId + backend wakes + button was NOT disabled; (c) dormant session + reset click → 0 pending bubbles + WS frame carries messageQueueItemId + backend wakes (the hypothesis in CONTEXT.md verified).
</verification>

<success_criteria>
- Every user-driven send-affordance in ComposeBox (main textarea, queue-slot, thumbs-up, recap, reset) routes through `useComposeSend` (shape file's rule of thumb satisfied).
- Thumbs-up bubble renders "👍" not "thumbs up" (D-02 satisfied for thumbs-up).
- Recap bubble renders "/explain the current situation" verbatim (D-02 respected — recap has no override).
- Reset produces no visible bubble but the WS frame carries `messageQueueItemId` so the backend Phase 56 wake gate fires (D-03 satisfied; the hypothesis in CONTEXT.md's "fixes bundled by this phase" #4 addressed).
- Thumbs-up and recap remain clickable when the session is dormant (D-05 satisfied); the wake-then-send routing covers the ~1-3s wake window with an immediate optimistic bubble (D-04 satisfied for thumbs-up + recap).
- No regression on the L1926 send-button, L2158, L2458, L2618 canSend sites (scope discipline).
- `tsc --noEmit` clean; existing pretty-view test suite unchanged in pass/fail count.
</success_criteria>

<output>
Create `.planning/phases/68-compose-send-funnel-universal-send-funnel-for-prettyview-com/68-02-SUMMARY.md` when done capturing: (a) the exact line numbers of each rewired call site (queue-slot / thumbs-up / recap / reset dispatch tails) so Plan 68-03 can point its per-trigger tests at stable coordinates, (b) the exact `bubbleTextOverride` value(s) used (verbatim string — "👍" is a specific codepoint; the SUMMARY records it byte-for-byte so 68-03's test can assert on it), (c) any deviation from the plan's action, (d) confirmation of the reset render-blacklist mechanism landed (guard placement in handleOptimisticSend).
</output>
