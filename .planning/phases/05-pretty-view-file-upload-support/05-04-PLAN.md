---
phase: 05-pretty-view-file-upload-support
plan: 04
type: execute
wave: 4
depends_on:
  - 05-01
  - 05-02
  - 05-03
files_modified:
  - .planning/phases/05-pretty-view-file-upload-support/05-UAT-CHECKLIST.md
  - .planning/phases/05-pretty-view-file-upload-support/05-PATCHES-MD-ENTRY.md
autonomous: false
requirements:
  - UPLOAD-01
  - UPLOAD-02
  - UPLOAD-03
  - UPLOAD-04
  - UPLOAD-05
  - UPLOAD-06
  - UPLOAD-07
  - UPLOAD-08
  - UPLOAD-09
  - UPLOAD-10
  - UPLOAD-11
  - UPLOAD-12
  - UPLOAD-13
  - UPLOAD-14
must_haves:
  truths:
    - "npm run build completes clean (no TypeScript errors, no bundler warnings blocking) and the resulting dist/ contains the Phase 5 client-side artifacts (chunk-pump hook, chip strip component, drop overlay, injected-turn parser)"
    - "docker exec against the newly-built skynet container confirms the backend upload handler is wired (grep for the three new WS case labels in dist/backend/backend/ssh/terminal.js) and the pretty-view-upload orchestrator module ships (dist/backend/backend/ssh/pretty-view-upload.js exists)"
    - "Terminal.tsx / MessageQueueDrawer.tsx / backend terminal.ts input case / patch #57 compose-drafts / patch #60 atomic delete-on-send / patch #100 split-and-delay Enter / patch #102 useIsTouchDevice — all BYTE-IDENTICAL in behavior; grep gates verify the load-bearing bytes are still present"
    - "A UAT checklist walking UPLOAD-01..14 exists for Ashley to run post-deploy; a skynet-patches.md entry draft exists ready for pin-time paste"
    - "This plan produces ZERO production source-code diffs — everything is verification, artifact preparation, and the deploy checkpoint itself"
  artifacts:
    - path: ".planning/phases/05-pretty-view-file-upload-support/05-UAT-CHECKLIST.md"
      provides: "Nyquist UAT checklist walking every UPLOAD-01..14 requirement with expected observable behavior per pane; groups desktop vs touch-device paths; groups happy-path vs failure recovery"
      contains: "UPLOAD-01"
    - path: ".planning/phases/05-pretty-view-file-upload-support/05-PATCHES-MD-ENTRY.md"
      provides: "Draft entry for ~/.claude/identities/tina/skynet-patches.md — numbered patch entry (patch #104 or next available at pin time), following the fork's convention of full motivation + code paths + gotchas + rebase risk + verify-post-deploy invariants"
      contains: "Files touched"
  key_links:
    - from: "Ashley's post-deploy UAT walk"
      to: "UPLOAD-01..14 pass/fail status"
      via: "05-UAT-CHECKLIST.md"
      pattern: "UPLOAD-"
    - from: "Deploy green-light window"
      to: "Mandatory 15-min deadman rollback"
      via: "sudo touch /tmp/skynet-keep-patched && sudo pkill -f 'sleep 900; \\[ ! -f /tmp/skynet-keep-patched'"
      pattern: "skynet-keep-patched"
---

<objective>
Land the deploy checkpoint for Phase 5. Follows the exact pattern established by Phase 1's 01-05-PLAN.md and Phase 2's 02-03-PLAN.md — build verification (Tasks 1-2, autonomous), UAT + patches-md-entry artifacts (Task 3, autonomous), and a single blocking human-verify checkpoint (Task 4) that owns the mandatory 15-minute deadman deploy flow per `~/.claude/identities/tina/deploy-runbook.md`.

Purpose: This plan produces ZERO production source-code diffs. Its whole job is to prove Plans 01-03 shipped correctly in-tree, prepare the documents Ashley needs to run UAT and pin the patch, and then hand her a clean deploy checkpoint. Deploy is her separate green-light per fleet rule (blanket pre-authorization ≠ per-deploy green light, per deploy-runbook.md).

Output: Two new markdown artifacts under `.planning/phases/05-pretty-view-file-upload-support/` (UAT checklist + patches-md entry draft) plus the deploy checkpoint being run to green. Zero touch to `src/`, zero touch to `docker/`, zero touch to `package.json`, zero touch to any nginx config (Phase 5 rides existing `/ssh/websocket/` — no new backend HTTP or WS route was added, so the fork's "location block on both nginx configs" trap does NOT apply here).

Deploy discipline reminder (STANDING CONSTRAINT, not a task): the mandatory 15-min deadman rollback is armed before every deploy per Ashley 2026-07-03. Pre-authorized code work from Plans 01-03 does NOT authorize the deploy per Ashley 2026-07-12. The deploy step lives inside Task 4's checkpoint and includes the deadman arm/disarm sequence exactly as spelled out in deploy-runbook.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-pretty-view-file-upload-support/05-CONTEXT.md
@.planning/phases/05-pretty-view-file-upload-support/05-01-SUMMARY.md
@.planning/phases/05-pretty-view-file-upload-support/05-02-SUMMARY.md
@.planning/phases/05-pretty-view-file-upload-support/05-03-SUMMARY.md
@.planning/phases/01-live-session-stream-to-browser-read-only-pretty-view/01-05-SUMMARY.md
@.planning/phases/02-toggle-compose-and-native-web-ergonomics/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification — npm run build clean, Phase 5 artifacts survive Vite tree-shake, load-bearing bytes intact</name>
  <read_first>
    - .planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-03-PLAN.md (if it exists) — reference for the Vite-tree-shake verification pattern the fork established. If not present, use Phase 1's 01-05-PLAN.md Task 3 (deploy smoke) and Phase 2's 02-03-PLAN.md checkpoint task as the pattern reference.
    - ~/.claude/identities/tina/skynet-patches.md offset 3309, patch #60 — for the "Verify post-deploy invariants" pattern to include in the patches-md entry (Task 3 output). Patch #60 explicitly documents `docker exec skynet grep -c 'message_queue_delete_on_send' /app/dist/backend/backend/ssh/terminal.js` should return ≥1 — that's the grep-gate style Phase 5 emulates.
    - src/backend/ssh/terminal.ts — for the exact case labels the grep gates check for: `case "upload_start":`, `case "upload_chunk":`, `case "upload_abort":`.
    - src/ui/api/pretty-view-upload-protocol.ts — the tokens `INJECTED_DELIMITER` and `formatInjectedUserTurn` must survive tree-shake into `dist/`. Both should appear in the bundled frontend + backend outputs.
  </read_first>
  <files>(no source files modified — this task produces console output the executor records)</files>
  <action>
Run the build + verify sequence. This task does NOT modify any files in the repo — it runs commands and captures their output for the summary.

**Step A — Clean build:**

```
cd /home/ubuntu/skynet && npm run build 2>&1 | tail -50
```

Expected: build completes with `built in Xs` and NO error output. If TypeScript errors appear, they belong to Plans 01-03 and this task should surface them for revision (do NOT attempt to fix from within this plan — fixes belong in whichever Plan owns the affected file).

**Step B — Verify backend Phase 5 artifacts in dist:**

```
grep -c 'case "upload_start":' dist/backend/backend/ssh/terminal.js
grep -c 'case "upload_chunk":' dist/backend/backend/ssh/terminal.js
grep -c 'case "upload_abort":' dist/backend/backend/ssh/terminal.js
grep -c 'handleUploadStart' dist/backend/backend/ssh/terminal.js
ls -la dist/backend/backend/ssh/pretty-view-upload.js
grep -c 'sanitizeFilenameForUpload\|handleUploadStart\|handleUploadChunk' dist/backend/backend/ssh/pretty-view-upload.js
```

Expected: each `case "upload_..."` grep returns 1; `handleUploadStart` grep returns ≥ 1; `pretty-view-upload.js` exists; the orchestrator's key exports are present.

**Step C — Verify frontend Phase 5 artifacts in dist:**

```
grep -c '\-\-\- attached files \-\-\-' dist/assets/*.js
grep -c 'usePrettyViewUploads\|useIsTouchDevice\|AttachmentChipStrip\|DropOverlay' dist/assets/*.js
grep -c 'parseInjectedUserTurn\|formatInjectedUserTurn' dist/assets/*.js
```

Expected: the INJECTED_DELIMITER literal survives in the bundled frontend (grep ≥ 1); the four Phase 5 component/hook symbols survive (grep ≥ 1 across all matches — Vite may mangle names, so also check for characteristic strings like `webkitGetAsEntry` or `upload_chunk` or `Drop files here`); parser + formatter names or their bodies survive (grep ≥ 1 across all matches — allow name-mangling by checking for the delimiter constant OR characteristic body strings like `attached files` in the source).

**Step D — Verify load-bearing prior-patch bytes are UNCHANGED in dist:**

```
grep -c 'message_queue_delete_on_send' dist/backend/backend/ssh/terminal.js
grep -c 'ssh_input_delayed_enter' dist/backend/backend/ssh/terminal.js
grep -c 'pointer: coarse' dist/assets/*.js
grep -c 'putComposeDraft\|flushComposeDraftKeepalive' dist/assets/*.js
```

Expected: patch #60 (message_queue_delete_on_send) ≥ 1 — atomic delete-on-send byte intact; patch #100 (ssh_input_delayed_enter) ≥ 1 — split-and-delay Enter byte intact; patch #102 (pointer: coarse) ≥ 1 — useIsTouchDevice byte intact; patch #57 (compose-drafts helpers) ≥ 1 — caption persistence byte intact.

**Step E — Verify no source diff creep:**

```
git diff --stat src/ui/features/terminal/Terminal.tsx
git diff --stat src/backend/ssh/terminal.ts
git diff --stat docker/nginx.conf docker/nginx-https.conf
git diff --stat package.json package-lock.json
```

Expected:
- Terminal.tsx diff shows fewer than 40 lines changed (Plan 03 Task 1)
- backend terminal.ts diff shows new case blocks + import line only (Plan 01 Task 2), input case region UNCHANGED
- nginx configs: ZERO changes (Phase 5 rides existing WS)
- package.json: ZERO changes (zero new dependencies)

Record every command's output in `.planning/phases/05-pretty-view-file-upload-support/05-04-BUILD-VERIFY-LOG.md` — this is a NEW file created by this task purely as a paper trail. Include the timestamp of the build (`date -u`) at the top.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npm run build 2>&1 | tail -20 | grep -E "(error|failed|built in)" ; echo "---backend---" ; grep -c 'case "upload_start":' dist/backend/backend/ssh/terminal.js ; grep -c 'case "upload_chunk":' dist/backend/backend/ssh/terminal.js ; grep -c 'case "upload_abort":' dist/backend/backend/ssh/terminal.js ; ls dist/backend/backend/ssh/pretty-view-upload.js 2>&1 ; echo "---frontend---" ; grep -c '\-\-\- attached files \-\-\-' dist/assets/*.js 2>/dev/null | awk -F: '{sum+=$2} END {print "delimiter total:", sum}' ; echo "---load-bearing---" ; grep -c 'message_queue_delete_on_send' dist/backend/backend/ssh/terminal.js ; grep -c 'ssh_input_delayed_enter' dist/backend/backend/ssh/terminal.js ; echo "---diff-scope---" ; git diff --stat docker/nginx.conf docker/nginx-https.conf package.json ; echo "---log---" ; ls -la .planning/phases/05-pretty-view-file-upload-support/05-04-BUILD-VERIFY-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` output contains `built in` and does NOT contain `error TS` or `[vite]` errors
    - `dist/backend/backend/ssh/terminal.js` contains exactly one occurrence each of `case "upload_start":`, `case "upload_chunk":`, `case "upload_abort":` (grep -c each returns 1)
    - `dist/backend/backend/ssh/pretty-view-upload.js` exists
    - `dist/backend/backend/ssh/terminal.js` still contains `message_queue_delete_on_send` (patch #60 marker) — grep ≥ 1
    - `dist/backend/backend/ssh/terminal.js` still contains `ssh_input_delayed_enter` (patch #100 marker) — grep ≥ 1
    - `dist/assets/*.js` contains the literal `--- attached files ---` — grep across all frontend chunks returns ≥ 1
    - `dist/assets/*.js` still contains `pointer: coarse` (patch #102 marker) — grep ≥ 1
    - `dist/assets/*.js` still contains `putComposeDraft` or `flushComposeDraftKeepalive` (patch #57 markers) — grep ≥ 1
    - `git diff --stat docker/nginx.conf docker/nginx-https.conf` returns empty (Phase 5 does NOT touch nginx)
    - `git diff --stat package.json` returns empty (Phase 5 adds ZERO new dependencies)
    - `git diff --stat package-lock.json` returns empty
    - `.planning/phases/05-pretty-view-file-upload-support/05-04-BUILD-VERIFY-LOG.md` exists with the full command outputs and a UTC timestamp
  </acceptance_criteria>
  <done>Build is verified clean, Phase 5 artifacts made it into dist, prior-patch load-bearing bytes are still present, no unintended scope creep into nginx or dependencies. Task 4 (deploy checkpoint) can proceed with confidence that what's about to ship IS what Plans 01-03 built.</done>
</task>

<task type="auto">
  <name>Task 2: Generate 05-UAT-CHECKLIST.md — Nyquist walk of UPLOAD-01..14 for Ashley post-deploy</name>
  <read_first>
    - .planning/REQUIREMENTS.md — the "File Upload Support (Phase 5)" section lines 57-72 for the EXACT text of UPLOAD-01..14. Each requirement gets its own checklist item, quoted verbatim in the checklist so Ashley reads the exact contract.
    - .planning/phases/04-pretty-view-visual-reskin-glass-depth-aesthetic/04-UAT-CHECKLIST.md (if it exists) — reference for the Nyquist UAT format the fork established for visual/behavior phases.
    - .planning/phases/05-pretty-view-file-upload-support/05-CONTEXT.md `<decisions>` block — for the concrete expected behaviors that instantiate each UPLOAD-NN requirement (e.g., "chip strip mounts only when at least one attachment is staged" instantiates UPLOAD-04).
    - .planning/shapes/shape-pretty-view-file-upload-support.md `## What would make it wrong` section (lines 164-189) — for the failure modes Ashley checks against; each "would make it wrong" item becomes a negative-space check in the UAT ("paperclip should NOT be visible on desktop", etc.).
  </read_first>
  <files>.planning/phases/05-pretty-view-file-upload-support/05-UAT-CHECKLIST.md</files>
  <action>
Create `.planning/phases/05-pretty-view-file-upload-support/05-UAT-CHECKLIST.md`. Structure:

```
# Phase 5 UAT Checklist — pretty-view file upload support

Post-deploy walk-through for Ashley. Every UPLOAD-NN requirement gets an
observable check. Groupings: desktop happy-path first, mobile paperclip
next, failure-recovery + edge cases last. Blocking-severity gates are marked
🚨 (regression = revert immediately). Nice-to-have polish items are marked ✨.

## Setup — one time
1. Open https://term.gigaashley.click in Chrome on desktop
2. Open a pretty-view pane on a host where you can write to `~/pretty-view-uploads/` — thenasty is fine; the tmux session doesn't need to be Claude Code (UPLOAD-14: works on any pane)
3. Have a small test file ready on your local Desktop (e.g. `test.txt`, a few hundred bytes)
4. Have a screenshot in your clipboard (or take one now)

## Desktop happy-path (UPLOAD-01, 04, 05, 06, 09, 10, 11, 13)

- [ ] 🚨 **UPLOAD-01** Drag `test.txt` FROM Desktop OVER the pretty-view surface (top-pane conversation area). While dragging, a full-surface "Drop files here" overlay appears. Drop anywhere on the surface — the file stages as a chip in a strip that appears ABOVE the compose textarea.
- [ ] 🚨 **UPLOAD-04** Chip renders: filename `test.txt` + human-readable size + a × control on the right. The strip is absent when no attachments are staged (verify by removing this chip via × and confirming the strip disappears — no empty chrome).
- [ ] 🚨 Re-drag `test.txt` on. Type a short caption like `check this out`. Click Send.
- [ ] 🚨 **UPLOAD-05** During transfer, the chip shows a progress ring or bar filling as bytes flow.
- [ ] 🚨 **UPLOAD-06** The injected message appears in the pretty-view stream ONLY after the upload completes (never sees a message referencing a not-yet-landed file).
- [ ] 🚨 **UPLOAD-11** The sent message renders as a single bubble containing your caption text and an inline chip for the file (filename + size only, no thumbnail).
- [ ] 🚨 **UPLOAD-09** The bubble's underlying content (visible if you inspect the tail) contains the caption + a delimited `--- attached files ---` block + a line naming the file with its landing path (no inlined file bytes).
- [ ] 🚨 **UPLOAD-10** ssh into the receiving box (`ssh thenasty` or whatever pane you tested on) and verify: `ls ~/pretty-view-uploads/$(date +%Y-%m-%d)/` shows a file named `<hhmmss>-test.txt`. `cat` that file shows your original content.
- [ ] **UPLOAD-13** Attach 3 files at once (drop multiple), type ONE caption, send. Verify all 3 chips render, one caption input is shared, and after send the bubble contains all 3 chips.
- [ ] **UPLOAD-13** Attach a file, leave caption EMPTY, click Send. Verify the send goes through (Send button was enabled even with empty caption).

## Clipboard paste (UPLOAD-02)

- [ ] 🚨 **UPLOAD-02** Copy a screenshot to your clipboard (screenshot tool, or select an image and copy). Click into the compose textarea. Paste (Cmd/Ctrl+V). The screenshot stages as a chip through the SAME landing path (test upload completes, file lands in `~/pretty-view-uploads/<date>/` with an image mimetype in the metadata block).
- [ ] Paste plain text — verify normal textarea behavior (text inserted, no chip staged).

## Failure recovery + retry (UPLOAD-06 negative-space, UPLOAD-07)

- [ ] Stage a file. In DevTools Network tab, offline the connection. Click Send. Verify the chip shows a failure state (red border) and the message stays in staging (not fired to the tail).
- [ ] Re-online the connection. Click Retry (or Send again — whichever affordance the plan wound up shipping). Verify the batch re-uploads and completes successfully.
- [ ] **UPLOAD-07** Close the tab while a file is staged (attachment bytes lost) and the caption typed (draft persisted). Reopen the pane. Caption is restored (via patch #57). Chip strip is empty (attachment bytes DID NOT persist — UPLOAD-08 asymmetry).

## Folder rejection (UPLOAD-12)

- [ ] 🚨 **UPLOAD-12** Try to drag a FOLDER from your Desktop onto the pretty-view surface. Verify the nudge `please attach files or zip first` appears momentarily. Verify NO attachments are staged (chip strip stays empty). Verify the recursive folder upload never happens (check `~/pretty-view-uploads/<date>/` afterwards — no folder-derived files present).

## Draft persistence asymmetry (UPLOAD-08)

- [ ] 🚨 **UPLOAD-08** Type a caption, do NOT attach anything, close the tab, reopen the pane. Verify caption is restored (patch #57 behavior preserved).
- [ ] 🚨 Repeat with a file attached: close the tab, reopen. Verify caption is restored, chip strip is EMPTY (attachment bytes are ephemeral by design).

## Mobile paperclip (UPLOAD-03) — requires touch device

Ashley: this section requires a touch device (phone / tablet). If you don't have one handy, skip and mark N/A.

- [ ] 🚨 **UPLOAD-03** On a touchscreen device (any width — verify via `matchMedia("(pointer: coarse) and (hover: none)").matches` in DevTools console), open a pretty-view pane. Verify a paperclip button appears in the compose area's icon column. Tap → native file picker opens. Select a file → stages as a chip. Send flow works identically to desktop.
- [ ] 🚨 **UPLOAD-03 negative-space** On desktop (any window width, including narrow), verify the paperclip button is NOT rendered. Match `matchMedia("(pointer: coarse) and (hover: none)").matches === false` in DevTools console.

## Works-on-any-pane (UPLOAD-14)

- [ ] 🚨 **UPLOAD-14** Test on a plain shell pane (not Claude Code). Attach a file, send. Verify: file lands in `~/pretty-view-uploads/<date>/`, injected message appears in the tail as human-readable text (caption + metadata block), a shell user could `cat` the landing path and see the file.

## Regression smoke — pre-existing patches must still work

- [ ] 🚨 Message queue drawer: open with Ctrl+Shift+;. Type a message, hit Send. Verify it lands in the terminal, drawer row disappears (patch #60 atomic delete-on-send).
- [ ] 🚨 Split-and-delay Enter: send a message via the pretty-view compose (no attachments). Verify text arrives + Enter fires as separate keystrokes (Claude Code REPL treats as typed input, not paste — patch #100).
- [ ] 🚨 Compose-drafts: type in the compose textarea, close the tab, reopen. Text is restored (patch #57).
- [ ] 🚨 useIsTouchDevice: mobile bottom nav appears on touchscreens, absent on desktop (patch #102).
- [ ] 🚨 Terminal / RDP / VNC / file manager / dashboard / sidebar / tab bar / AppRail: all render + behave normally. Open at least one RDP tab (e.g. workstation-RDP) and one file-manager surface — verify no unexpected changes.
- [ ] 🚨 Identity badge on pretty-view: still visible with the same treatment (Phase 4 Glass — patches #17, #38, #86, and Phase 4 all intact).
- [ ] 🚨 The 3D orb WIP indicator (patch pending pin, awaiting deploy): still renders — Phase 5 does not touch WipBubble.

## Sign-off

- [ ] All 🚨 items pass → disarm deadman (`sudo touch /tmp/skynet-keep-patched && sudo pkill -f 'sleep 900; \[ ! -f /tmp/skynet-keep-patched'`)
- [ ] Any 🚨 item fails → let the deadman fire OR run `bash /opt/skynet/.tmp-revert.sh` immediately

Post-sign-off:
- Pin the patch: number it (next available in skynet-patches.md), paste the entry from `05-PATCHES-MD-ENTRY.md`, commit the pin
- Close the bounty at `~/.claude/identities/tina/bounties/pretty-view-file-upload-support/` via `/close pretty-view-file-upload-support`
```

Fill in the exact requirement text for each UPLOAD-NN by copying verbatim from `.planning/REQUIREMENTS.md` lines 57-72 (do not paraphrase — Ashley walks the exact contract). The above template already shows the structure; the executor fills in the verbatim contract quotes at write time.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && test -f .planning/phases/05-pretty-view-file-upload-support/05-UAT-CHECKLIST.md && for req in UPLOAD-01 UPLOAD-02 UPLOAD-03 UPLOAD-04 UPLOAD-05 UPLOAD-06 UPLOAD-07 UPLOAD-08 UPLOAD-09 UPLOAD-10 UPLOAD-11 UPLOAD-12 UPLOAD-13 UPLOAD-14 ; do echo "$req:" $(grep -c "$req" .planning/phases/05-pretty-view-file-upload-support/05-UAT-CHECKLIST.md) ; done</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/05-pretty-view-file-upload-support/05-UAT-CHECKLIST.md` exists
    - Every UPLOAD-01..14 requirement id appears at least once in the checklist (14 grep counts, each ≥ 1)
    - The checklist is structured with `- [ ]` items (grep for `- \[ \]` returns ≥ 14, roughly one per requirement)
    - Blocking items are marked 🚨 (grep for the emoji returns ≥ 8 — the load-bearing UPLOADs)
    - The regression-smoke section names patches #57, #60, #100, #102 (grep for each returns ≥ 1)
    - The disarm command is present (grep for `skynet-keep-patched` returns ≥ 1)
  </acceptance_criteria>
  <done>Ashley has a clean UAT walkthrough ready to run the moment the deploy checkpoint (Task 4) turns her green light into a live container.</done>
</task>

<task type="auto">
  <name>Task 3: Generate 05-PATCHES-MD-ENTRY.md — draft entry for the fork's patch catalog</name>
  <read_first>
    - ~/.claude/identities/tina/skynet-patches.md offset 3309 (patch #60 entry, lines 3309-3434) — the CANONICAL entry style for a load-bearing multi-file patch. Note: motivation → root cause → fix (with sub-bullets per file / per behavior) → files touched → rebase risk → verify-post-deploy invariants → deploy note. Emulate exactly.
    - ~/.claude/identities/tina/skynet-patches.md offset 6235 (patch #100 entry, lines 6235-6258) — the CANONICAL entry style for a smaller surgical patch. Reference the more compact form.
    - ~/.claude/identities/tina/skynet-patches.md offset 6274 (patch #102 entry, lines 6274-6311) — reference for how the fork explains a NEW hook + call sites.
    - .planning/phases/05-pretty-view-file-upload-support/05-01-SUMMARY.md, 05-02-SUMMARY.md, 05-03-SUMMARY.md — synthesize the actual shipped shapes into the entry (do NOT re-guess based on plans; use what actually landed).
    - .planning/phases/05-pretty-view-file-upload-support/05-CONTEXT.md `<decisions>` block — for the philosophical framing quotes (path-only injection, atomic transfer, cognitively-free paperclip) that belong in the "motivation" section.
    - src/backend/ssh/terminal.ts and src/backend/ssh/pretty-view-upload.ts as-shipped — read the diffs / current state at task time to write accurate "files touched" lines.
  </read_first>
  <files>.planning/phases/05-pretty-view-file-upload-support/05-PATCHES-MD-ENTRY.md</files>
  <action>
Create `.planning/phases/05-pretty-view-file-upload-support/05-PATCHES-MD-ENTRY.md`. Structure follows the patch #60 entry as a template. The patch number is `NNN.` (numeric placeholder for pin time — do NOT hardcode; Ashley will fill in the next available number when pinning, likely 104 or higher depending on any patches pinned between now and Phase 5 deploy).

```
   NNN. `feat(pretty-view): file upload support — drag/drop/paste/mobile
       paperclip, atomic transfer, path-only injection` — Phase 5,
       shipped 2026-MM-DD (fill in at pin time).
       Adds a cognitively-free "attach a file" affordance to pretty view.
       Drag-and-drop anywhere on the pretty-view surface, clipboard paste,
       and (mobile only, gated by patch #102 useIsTouchDevice) a paperclip
       button. Files transfer atomically over the EXISTING per-pane SSH
       WebSocket to `~/pretty-view-uploads/<yyyy-mm-dd>/<hhmmss>-<name>` on
       the receiving box, then an injected user turn carries path-only-with-
       metadata (never inlined bytes) — a 100MB attachment costs zero session
       context until the agent chooses to read it.

       * **Motivation** (from shape / CONTEXT.md). Moving a file to an agent
         used to require per-box transfer paths — different for each box,
         none convenient, always broke the flow of talking. The paperclip
         should feel cognitively free: never think "which box, which path,
         will this eat context, will it work next reboot." Path-only injection
         with atomic transfer collapses those questions.

       * **Transport — the existing terminal WS.** Every pretty-view pane
         already has an authenticated SSH connection to the receiving box
         (the same connection carrying send/receive today). Uploads ride
         that channel; no new WS, no new HTTP endpoint, no nginx changes.
         Three new message types on the existing WS:
         - `upload_start` (client→server): batch metadata + per-file
           {tempId, filename, size, mimetype}
         - `upload_chunk` (client→server): {tempId, offset, base64 bytes},
           64KB per chunk, sequential per file, up to 3 files in parallel
         - `upload_abort` (client→server): {tempId?} — omit tempId to abort
           the whole batch
         Plus four server→client event types: `upload_progress`,
         `upload_complete`, `upload_failed`, `upload_ready_to_inject`.

       * **Atomic transfer via extended patch #60 lifecycle.** Each staged
         batch gets a `messageQueueItemId` (crypto.randomUUID). The injected
         user turn (fired only after ALL files have `upload_complete`) rides
         the SAME `case "input"` handler patch #60 established, carrying the
         same mqid — inheriting split-and-delay Enter (patch #100) and
         atomic delete-on-send (patch #60) for free. No parallel send path.

       * **Landing convention.** `$HOME/pretty-view-uploads/<yyyy-mm-dd>/
         <hhmmss>-<sanitized-filename>` under the receiving user's home.
         HOME resolved once per batch via `sftp.realpath('.')` — NEVER
         hardcoded `/home/...`. Day subdir created on demand. Filename
         collisions suffixed `-2`, `-3` up to 10 attempts. Write goes to a
         `.<name>.partial` temp file with `wx` flag (fail-if-exists → no
         symlink write); rename-on-complete gives atomicity at the FS
         layer. **No auto-cleanup, ever.** Uploads persist until the agent
         or user deletes them.

       * **Injected metadata block.** After the caption text:
         ```
         --- attached files ---
         1. {filename} ({human size}, {mimetype}) → {landing path}
            uploaded {box-local ISO timestamp}
         2. ...
         ```
         Delimiter is LOCKED at `--- attached files ---` — sanitizeFilename
         rejects any filename containing that substring OR any newline-then-
         `--- ` sequence (delimiter-collision defense). Human-legible at
         a shell (`cat`/`less`), trivially machine-parseable (agent
         `@`-references landing path). Source machine NOT included per
         shape's lock.

       * **Threat model addressed at ingress.** Filename sanitization
         (strip `/`, `\`, null bytes; reject `.`/`..`/leading-dot/paths >
         200 chars); size limits (500MB per file, 2GB per batch, enforced
         at upload_start before any bytes flow); size overflow during
         chunking → temp file unlinked; chunk-out-of-order → temp file
         unlinked; SFTP write uses `flags: 'wx'` (no symlink follow);
         delimiter-collision blocked at upload_start. Attacker who owns
         the browser session can only upload up to their SSH-authenticated
         write capacity on the receiving box (same trust boundary that
         already exists for any pretty-view send).

       * **Frontend UX.** Drop-overlay on the whole pretty-view surface
         (patch #74 verified `data-pv-root` positioning still works with
         `absolute inset-0` children). Chip strip mounts above the compose
         textarea ONLY when at least one attachment is staged (no empty
         chrome). Per-chip progress ring driven by `upload_progress`
         events. × control to remove. Failure → chip turns red, message
         stays in staging, retry button appears (retry = fresh
         messageQueueItemId + fresh upload_start with all still-staged
         files). Folder drops refused with an in-surface nudge "please
         attach files or zip first"; recursive uploads never happen.
         Mobile paperclip gated on `useIsTouchDevice` from patch #102
         (one-line consumer — no touch re-detection).

       * **Caption + attachment persistence asymmetry.** Caption text
         inherits patch #57's compose-drafts persistence — survives tab
         close, restored on reload. Attachment bytes DO NOT persist —
         user re-drags the file from their desktop (where it still
         lives). No localStorage / IndexedDB / sessionStorage of file
         bytes; staging state is React-only. Rationale: raw file bytes
         client-side is a footgun (storage limits, memory bloat,
         security surface).

       * **Sender-side render.** In the sender's own pretty-view stream,
         the just-sent user turn renders as a single bubble containing
         the caption text and inline chips for each attachment
         (filename + size only, no thumbnails, consistent across
         mimetypes per shape lock). ChatMessage detects the injected
         format via `parseInjectedUserTurn` (round-trip inverse of
         `formatInjectedUserTurn`); non-matching content renders as
         plain markdown, byte-identical to pre-patch behavior for the
         vast majority of messages.

       * **Works on any pane.** Feature works on Claude Code panes AND
         plain shell panes — the receiving box only needs SSH + writable
         `$HOME`. No dependency on Claude Code running, no dependency on
         any identity being loaded. The injected metadata block is
         meaningful to a human at a shell (cat/less the landing path)
         as much as to an agent (`@`-reference the path).

       * **Verify post-deploy invariants** (for future rebase smoke checks):
         - `docker exec skynet grep -c 'case "upload_start":' /app/dist/backend/backend/ssh/terminal.js` should return 1
         - `docker exec skynet grep -c 'case "upload_chunk":' /app/dist/backend/backend/ssh/terminal.js` should return 1
         - `docker exec skynet grep -c 'case "upload_abort":' /app/dist/backend/backend/ssh/terminal.js` should return 1
         - `docker exec skynet ls /app/dist/backend/backend/ssh/pretty-view-upload.js` should exist
         - `docker exec skynet grep -c 'message_queue_delete_on_send' /app/dist/backend/backend/ssh/terminal.js` should return ≥ 1 (patch #60 preserved)
         - `docker exec skynet grep -c 'ssh_input_delayed_enter' /app/dist/backend/backend/ssh/terminal.js` should return ≥ 1 (patch #100 preserved)
         - `docker exec skynet grep -c '\-\-\- attached files \-\-\-' /app/dist/assets/*.js` should return ≥ 1 (delimiter constant survived tree-shake)

       * **Files touched** (fill from actual `git log` at pin time):
         - `src/backend/ssh/terminal.ts` — three new `case` blocks + import;
           `case "input"` UNCHANGED byte-for-byte (verified via awk-extract
           + diff, see 05-01-SUMMARY.md)
         - `src/backend/ssh/pretty-view-upload.ts` — NEW module, orchestrator
         - `src/ui/api/pretty-view-upload-protocol.ts` — NEW module, shared
           types + constants + sanitizeFilenameForUpload + formatHumanSize +
           formatInjectedUserTurn + parseInjectedUserTurn
         - `src/ui/features/pretty-view/use-pretty-view-uploads.ts` — NEW,
           orchestrator hook (staging state + chunk pump + WS event handling)
         - `src/ui/features/pretty-view/AttachmentChipStrip.tsx` — NEW,
           chip strip component (used both in ComposeBox for staging AND
           in ChatMessage in read-only mode for sender-side render)
         - `src/ui/features/pretty-view/DropOverlay.tsx` — NEW, full-surface
           drop overlay + folder-nudge
         - `src/ui/features/pretty-view/ComposeBox.tsx` — chip strip mount,
           paperclip button (gated on showPaperclip), paste handler,
           onSendWithAttachments callback branch; existing textarea + meter
           well + icon column + queue-armed overlay UNCHANGED
         - `src/ui/features/pretty-view/PrettyView.tsx` — drop-overlay
           mount + drag/drop event handlers on data-pv-root + hook wiring
           + new terminalWs/onInjectedTurnReady props
         - `src/ui/features/pretty-view/ChatMessage.tsx` — sender-side
           chip render branch when parseInjectedUserTurn matches;
           non-matching content renders unchanged
         - `src/ui/features/terminal/Terminal.tsx` — new
           handleInjectedTurnReady useCallback + two new JSX attributes
           on the PrettyView mount; existing onSend / MessageQueueDrawer
           onSend UNCHANGED byte-for-byte

       * **Rebase risk**: MEDIUM on `terminal.ts` (`case "input"`
         neighborhood — patches #13/#24/#33/#51/#60/#100 all live here;
         the three new upload cases go IMMEDIATELY after the input case
         and BEFORE `case "ping"`; if upstream restructures the input
         handler, resolve by preserving all patch #60 + #100 behavior
         then re-inserting the upload cases at the same position).
         LOW-MEDIUM on `Terminal.tsx` (busiest file — Plan 03 added
         only a useCallback + two JSX attrs; localized in the PrettyView
         mount region at line ~2841). LOW on all new files (fork-only,
         no upstream conflict surface). LOW on `ComposeBox.tsx` +
         `PrettyView.tsx` + `ChatMessage.tsx` (fork-only files).

       * **Deploy note**: shipped behind the mandatory 15-min deadman
         with explicit per-deploy green light per deploy-runbook.md.
         Zero new npm dependencies (ssh2 SFTP subsystem already in
         dep tree from file-manager routes). Zero new nginx location
         blocks (rides existing `/ssh/websocket/`). Standard
         `sudo bash /opt/skynet/skynet-patches/build-skynet.sh` +
         `sudo docker compose up -d --force-recreate skynet`.

       Related bounty: `~/.claude/identities/tina/bounties/pretty-view-file-upload-support/`.
```

At pin time, Ashley:
1. Numbers the patch (next available integer, e.g. `104.`)
2. Copies the entry above (with the number filled in and the date filled in) into `~/.claude/identities/tina/skynet-patches.md` at the appropriate ordinal position
3. Bumps the "ONE HUNDRED THREE numbered patches" count near the top of that file to the new count
4. Commits the pin
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && test -f .planning/phases/05-pretty-view-file-upload-support/05-PATCHES-MD-ENTRY.md && grep -c "Files touched" .planning/phases/05-pretty-view-file-upload-support/05-PATCHES-MD-ENTRY.md && grep -c "Verify post-deploy invariants" .planning/phases/05-pretty-view-file-upload-support/05-PATCHES-MD-ENTRY.md && grep -c "Rebase risk" .planning/phases/05-pretty-view-file-upload-support/05-PATCHES-MD-ENTRY.md && grep -c "Deploy note" .planning/phases/05-pretty-view-file-upload-support/05-PATCHES-MD-ENTRY.md && grep -c "patch #60\|patch #100\|patch #57\|patch #102" .planning/phases/05-pretty-view-file-upload-support/05-PATCHES-MD-ENTRY.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/05-pretty-view-file-upload-support/05-PATCHES-MD-ENTRY.md` exists
    - Contains all the canonical section headings the fork's catalog uses: `Files touched`, `Verify post-deploy invariants`, `Rebase risk`, `Deploy note` (grep each ≥ 1)
    - References the four load-bearing prior patches Phase 5 depends on / inherits from: #57, #60, #100, #102 (grep for these strings — total ≥ 4)
    - Mentions the three new backend WS case labels (grep for `upload_start`, `upload_chunk`, `upload_abort` — ≥ 1 each)
    - Mentions the landing convention `~/pretty-view-uploads/` (grep ≥ 1)
    - Mentions the delimiter `--- attached files ---` (grep ≥ 1)
  </acceptance_criteria>
  <done>The patch-catalog entry is drafted and ready to paste into `~/.claude/identities/tina/skynet-patches.md` at pin time, following the fork's canonical multi-file-patch style.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: End-to-end deploy checkpoint — build, deadman-armed deploy, Ashley's UAT walk, disarm-or-fire</name>
  <what-built>
    Plans 01-03 shipped:
    - Plan 01: backend upload orchestrator (`src/backend/ssh/pretty-view-upload.ts`) + three new WS cases in `src/backend/ssh/terminal.ts` (upload_start / upload_chunk / upload_abort) + shared protocol types in `src/ui/api/pretty-view-upload-protocol.ts` (formatInjectedUserTurn, sanitizeFilenameForUpload, formatHumanSize, MAX_PER_FILE_BYTES, MAX_PER_BATCH_BYTES, CHUNK_SIZE_BYTES, INJECTED_DELIMITER). `case "input":` in terminal.ts byte-identical (patches #60 and #100 preserved).
    - Plan 02: `useIsTouchDevice`-gated mobile paperclip + drag/drop overlay + clipboard paste + chip strip staging + per-chip progress + orchestrator hook (`use-pretty-view-uploads.ts`) + `AttachmentChipStrip` + `DropOverlay`. All within `src/ui/features/pretty-view/`. Compose-drafts (patch #57) preserved; attachment bytes are React-only (UPLOAD-08 asymmetry).
    - Plan 03: Terminal.tsx threads `webSocketRef.current` + `handleInjectedTurnReady` into PrettyView. handleInjectedTurnReady emits a two-event split-send matching the MessageQueueDrawer pattern at line 2869 — patches #60 and #100 fire automatically for the injected turn. ChatMessage detects injected-turn user messages via `parseInjectedUserTurn` and renders sender-side chips inside the same user bubble (UPLOAD-11).
    - Plan 04 Task 1: build clean, Phase 5 artifacts survive Vite tree-shake, load-bearing prior-patch bytes intact, zero unintended scope creep into nginx / package.json.
    - Plan 04 Task 2: `05-UAT-CHECKLIST.md` walking UPLOAD-01..14 for Ashley post-deploy.
    - Plan 04 Task 3: `05-PATCHES-MD-ENTRY.md` drafted for skynet-patches.md pin.

    Together: the full "cognitively-free paperclip" feature — drop / paste / (mobile) tap → chip strip → atomic upload with per-chip progress → path-only injected user turn → sender-side chip render — end-to-end.
  </what-built>
  <how-to-verify>
    Follow this exact sequence from `~/.claude/identities/tina/deploy-runbook.md` step-by-step. The mandatory 15-min deadman rollback is a STANDING CONSTRAINT per Ashley 2026-07-03, NO EXCEPTIONS.

    **CRITICAL — Get explicit per-deploy green light from Ashley BEFORE running step 1.** The pre-authorization for the code work does NOT authorize the deploy per Ashley 2026-07-12. This checkpoint is where you ask: "Plans 01-03 are shipped in-tree, build verifies clean per Task 1, UAT checklist + patches entry drafted per Tasks 2-3. May I deploy now?"

    After green light:

    1. **Apply + commit + push + build.**
       - Working copy already has Plans 01-03 committed atomically per each Plan's summary. Verify: `cd ~/skynet && git status` should show a clean tree (or only these plan-artifact .md files uncommitted).
       - **`git push` BEFORE build.** The build script clones from GitHub — local-only commits cache-hit the frontend-builder layer per deploy-runbook.md's patches #43 + #69 trap. Verify all commits are on remote: `git log --oneline origin/feat/tab-title-from-tmux..HEAD` should return empty.
       - Build: `sudo bash /opt/skynet/skynet-patches/build-skynet.sh` (typical duration ~9-12s on this box).
       - If build fails: `git checkout -- .` to reset any accidental worktree changes, investigate, retry.

    2. **Clear the stale sentinel from the prior deploy.**

           sudo rm -f /tmp/skynet-keep-patched

       ⚠️ From this moment until step 5, the sentinel is ABSENT. A `sleep 900` process from a PRIOR deploy's deadman may still be ticking in the background. DO NOT try to "clean it up" during this window per deploy-runbook.md's "never kill a deadman's sleep child" rule. Either let the old sleep expire naturally OR kill its PARENT bash only (not the child sleep).

    3. **Arm the new deadman.**

           nohup sudo -b bash -c 'sleep 900; [ ! -f /tmp/skynet-keep-patched ] \
             && bash /opt/skynet/.tmp-revert.sh' > /tmp/skynet-revert-bg.log 2>&1

    4. **Force-recreate the container.**

           cd /opt/skynet && sudo docker compose up -d --force-recreate skynet

       Watch for the container to become healthy (`sudo docker ps | grep skynet` shows healthy after ~5-10s).

    5. **Tell Ashley: "Deployed. Please run the UAT checklist at `.planning/phases/05-pretty-view-file-upload-support/05-UAT-CHECKLIST.md`."**

       Ashley runs through the checklist. She will report either "all 🚨 green" or specific failures.

    6. **On her "worked" / "pin it":** disarm the deadman.

           sudo touch /tmp/skynet-keep-patched
           sudo pkill -f 'sleep 900; \[ ! -f /tmp/skynet-keep-patched'

       ⚠️ Use the NARROW pkill pattern per deploy-runbook.md — bare `pkill -f "sleep 900"` matches other sentinels.

       Then help Ashley pin the patch:
       - Assign next available patch number (check `grep -n "^\s*[0-9]\+\." ~/.claude/identities/tina/skynet-patches.md | tail -3` for the current top)
       - Paste the content of `.planning/phases/05-pretty-view-file-upload-support/05-PATCHES-MD-ENTRY.md` into skynet-patches.md at the appropriate ordinal position
       - Bump the "ONE HUNDRED THREE numbered patches" count near the top of skynet-patches.md
       - Commit the pin

    7. **If she doesn't respond within 15 min:** the deadman fires and rolls the container back to `ghcr.io/lukegus/skynet:latest`. She still has Skynet access to iterate. If deadman fires:
       - Do NOT panic
       - Diagnose the failure (browser console errors, `sudo docker logs skynet | tail -100`)
       - Fix the specific issue in a follow-up plan (do NOT re-deploy Phase 5 as-is without addressing the failure)

    8. **If UAT surfaces a specific 🚨 failure:** either
       - Quick-fix inline within the deadman window (small css / small logic bug), re-deploy → re-arm → re-verify — OR
       - Let the deadman fire and iterate in a follow-up plan

    9. **On any 🚨 failure that requires source diffs beyond a trivial one-liner:** the fix belongs in whichever Plan 01-03 the bug lives in — surface via `/gsd-plan-phase --gaps` for a proper gap-closure plan.

    Report back with:
    - Whether the deadman was disarmed (Ashley's UAT green) or fired (rollback)
    - Screenshot or console-log evidence of at least ONE happy-path attachment sent + received end-to-end (drag → chip → progress → completion → bubble with chip in sender-side render)
    - Confirmation of the landing path: `ssh <pane host> "ls ~/pretty-view-uploads/$(date +%Y-%m-%d)/"` shows the uploaded file(s)
    - Confirmation that at least the pre-existing message queue drawer + terminal / RDP / compose-drafts / mobile paperclip on touch still work (regression smoke items from UAT checklist)
    - If pinned: the assigned patch number and the commit sha of the skynet-patches.md pin commit
  </how-to-verify>
  <resume-signal>Type "pinned as patch #NNN commit <sha>" if deployed + verified + pinned. Type "reverted, deadman fired, issue: ..." if rollback occurred with root cause. Type "held, iterating on ..." if a fix is in progress within the deadman window.</resume-signal>
  <done>Phase 5 is deployed to production behind the mandatory 15-min deadman, Ashley has walked the UAT checklist end-to-end and confirmed all 🚨 items green (or the deadman fired and rollback occurred with a documented root cause for follow-up). Either way the ship state is CLEAR — no ambiguity about whether the patch is live.</done>
</task>

</tasks>

<verification>
- `npm run build` completes with no errors (Task 1)
- All Phase 5 backend and frontend artifacts appear in `dist/` after tree-shake (Task 1)
- All load-bearing prior-patch grep gates pass (patches #57, #60, #100, #102 markers still in dist — Task 1)
- Zero diff on `docker/nginx.conf`, `docker/nginx-https.conf`, `package.json`, `package-lock.json` (Task 1)
- `05-UAT-CHECKLIST.md` exists and covers all 14 UPLOAD-NN requirements (Task 2)
- `05-PATCHES-MD-ENTRY.md` exists in the canonical fork-catalog style (Task 3)
- Deploy checkpoint (Task 4) reaches green: either Ashley pins OR the deadman fires with a documented root cause
</verification>

<success_criteria>
- All 14 UPLOAD-NN requirements verified end-to-end in production per the UAT checklist
- Mandatory 15-min deadman rollback was armed before deploy per Ashley 2026-07-03 and disarmed after successful UAT (or allowed to fire on failure)
- Zero regression to pre-existing patches — message queue drawer, compose-drafts, split-and-delay Enter, atomic delete-on-send, useIsTouchDevice-gated mobile bottom nav, identity badge, WipBubble, ambient panels, session-holding overlay all continue to work
- Zero regression to unrelated Skynet surfaces — Terminal / RDP / VNC / file manager / dashboard / sidebar / tab bar / AppRail all render + behave normally
- Patch pinned in `~/.claude/identities/tina/skynet-patches.md` with the drafted entry from Task 3, or rollback documented with clear root cause for follow-up
- Bounty at `~/.claude/identities/tina/bounties/pretty-view-file-upload-support/` closed via `/close pretty-view-file-upload-support` upon successful UAT (bounty lifecycle housekeeping — done outside this plan by Ashley)
</success_criteria>

<output>
Create `.planning/phases/05-pretty-view-file-upload-support/05-04-SUMMARY.md` when done. Summary must record: the deploy outcome (disarmed vs fired), the assigned patch number if pinned, the commit sha of the pin commit, any 🚨 items that failed and how they were resolved, and any deltas between the drafted `05-PATCHES-MD-ENTRY.md` and the actually-pinned entry (Ashley may edit at pin time — capture the delta for future reference).
</output>
