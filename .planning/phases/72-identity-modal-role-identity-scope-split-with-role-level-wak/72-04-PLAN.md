---
phase: 72-identity-modal-role-identity-scope-split-with-role-level-wak
plan: 04
type: execute
wave: 4
depends_on:
  - 72-03
files_modified:
  - src/ui/features/pretty-view/IdentityModal.tsx
  - src/ui/features/pretty-view/WakeupsTab.tsx
  - src/ui/features/pretty-view/HandoffTab.tsx
  - src/ui/features/pretty-view/IdentityModal.coordinator-empty.test.tsx
autonomous: false
requirements: []
must_haves:
  truths:
    - "When a coordinator identity opens the modal, switches to Identity view, and taps the Wakeups tab, the tab shows an informative empty-state caption: 'Coordinators use role-scope wakeups only. Switch to Role view to manage.' — with NO Add-wakeup pill (coordinators create wakeups from Role scope only)."
    - "When any user (actor OR coordinator) opens the modal, switches to Role view, and taps the Wakeups tab on an EMPTY role-scope list, the tab shows 'No scheduled wake-ups.' + the Add-wakeup pill (so the first role-scope wakeup can be created from a blank slate)."
    - "When an actor identity opens the modal on the Identity Wakeups tab with an empty identity-scope list, the tab shows 'No scheduled wake-ups.' + the Add-wakeup pill (parity with role-scope empty branch)."
    - "The Add-wakeup pill renders in BOTH the data-branch (non-empty list, above the wakeup rows) AND the empty-branch (non-coordinator-identity case) — verified by a testid appearing in each code path."
    - "When a coordinator identity opens the modal, switches to Identity view, and taps the Handoff tab, the tab shows an informative empty-state caption: 'Coordinators are stateless routers — no handoff to display.'"
    - "The coordinator Identity file tab renders normally (coordinators DO have a slim identity file)."
    - "Both empty-state captions render with the pretty-view muted-fg palette (matches existing empty states in the modal — same visual language as 'No open bounties for {displayName}.' in the Bounties pane)."
    - "The add-wakeup pill button uses the mid-gradient hue-tinted palette (matches the sticky search bar in the Bounties pane per CONTEXT.md § Add-wakeup UX)."
    - "Ashley visually confirms via a browser walk-through that the scope switch reads as intentional for both actor and coordinator identities, and that the design matches sketch variant D."
  artifacts:
    - path: "src/ui/features/pretty-view/WakeupsTab.tsx"
      provides: "coordinator-empty branch in WakeupsTab that fires when scope='identity' AND wakeup list is empty AND identity is a coordinator (via new isCoordinator prop). Add-wakeup pill renders in both data-branch and non-coordinator-identity empty-branch."
    - path: "src/ui/features/pretty-view/HandoffTab.tsx"
      provides: "coordinator-empty branch that fires when identity.coordinator === true (via new isCoordinator prop)"
    - path: "src/ui/features/pretty-view/IdentityModal.tsx"
      provides: "Threads identity.coordinator down as isCoordinator prop to WakeupsTab (identity-scope pane only) and HandoffTab"
    - path: "src/ui/features/pretty-view/IdentityModal.coordinator-empty.test.tsx"
      provides: "5 tests: coordinator Identity-view Wakeups shows caption (no pill), coordinator Identity-view Handoff shows caption, coordinator Identity-file renders normally, actor Identity empty-branch shows pill, actor role-scope empty-branch shows pill"
  key_links:
    - from: "src/ui/features/pretty-view/WakeupsTab.tsx"
      to: "isCoordinator + scope='identity' + empty-list branch"
      via: "conditional rendering guard before the default empty-state fallback"
      pattern: "isCoordinator && scope === \"identity\""
    - from: "src/ui/features/pretty-view/WakeupsTab.tsx data-branch"
      to: "Add-wakeup pill above wakeup rows"
      via: "pill renders as a permanent top-of-list affordance"
      pattern: "data-testid=\"wakeup-add-button\""
    - from: "src/ui/features/pretty-view/WakeupsTab.tsx empty-branch (non-coordinator-identity case)"
      to: "Add-wakeup pill alongside 'No scheduled wake-ups.' caption"
      via: "pill renders inside the empty-state div so first-wakeup flow is reachable"
      pattern: "data-testid=\"wakeup-add-button\""
    - from: "src/ui/features/pretty-view/HandoffTab.tsx"
      to: "isCoordinator empty caption"
      via: "conditional rendering guard at the top of the render body"
      pattern: "isCoordinator"
    - from: "src/ui/features/pretty-view/IdentityModal.tsx"
      to: "isCoordinator prop threading"
      via: "identity.coordinator passed to WakeupsTab (identity-scope pane) and HandoffTab"
      pattern: "isCoordinator=\\{identity\\.coordinator\\}"
---

<objective>
Land the coordinator-Identity-view informative empty states + do the visual polish pass against sketch variant D + get Ashley's real-browser sign-off. This closes Phase 72.

Purpose: The shape's coordinator-legibility failure mode ("Coordinator identities open the modal and see mostly-empty regions with no explanation") gets closed here. Without this, a coordinator in Identity view sees "No scheduled wake-ups." + a blank handoff — which reads as broken. With the informative captions, the same absence reads as intentional ("this coordinator is a router; there's nothing on the identity side because there isn't supposed to be").

Output: Coordinator-Identity-view Wakeups and Handoff tabs render the two locked empty-state captions from CONTEXT.md. Non-coordinator empty branches KEEP the Add-wakeup pill so first-wakeup flow is reachable. Visual polish pass on the add-wakeup pill button matches the sketch's hue-tint. Ashley browser walk-through checkpoint gates the ship.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/72-identity-modal-role-identity-scope-split-with-role-level-wak/72-CONTEXT.md
@.planning/phases/72-identity-modal-role-identity-scope-split-with-role-level-wak/72-01-SUMMARY.md
@.planning/phases/72-identity-modal-role-identity-scope-split-with-role-level-wak/72-02-SUMMARY.md
@.planning/phases/72-identity-modal-role-identity-scope-split-with-role-level-wak/72-03-SUMMARY.md
@src/ui/features/pretty-view/WakeupsTab.tsx
@src/ui/features/pretty-view/HandoffTab.tsx
@src/ui/features/pretty-view/IdentityModal.tsx
@.planning/sketches/001-identity-modal-role-vs-identity-split/index.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add coordinator-aware empty states to WakeupsTab + HandoffTab; thread isCoordinator prop from IdentityModal; visual polish pass on add-wakeup pill</name>
  <files>src/ui/features/pretty-view/WakeupsTab.tsx, src/ui/features/pretty-view/HandoffTab.tsx, src/ui/features/pretty-view/IdentityModal.tsx, src/ui/features/pretty-view/IdentityModal.coordinator-empty.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/WakeupsTab.tsx (Plan 02 output — the current empty state at the equivalent of L302-308: "No scheduled wake-ups." + Add-wakeup pill; the coordinator branch prepends BEFORE this default; Plan 02 already established the Add-wakeup pill rendering both above the row list AND inside the empty state)
    - src/ui/features/pretty-view/HandoffTab.tsx (existing handoff renderer — where the empty markdown state lives; the coordinator branch prepends BEFORE any markdown render)
    - src/ui/features/pretty-view/IdentityModal.tsx (Plan 03 output — the two TabsContent panes for identity-wakeups and handoff, plus how they receive props; add `isCoordinator={identity.coordinator}` to both)
    - src/ui/features/pretty-view/IdentityModal.tsx:1616-1651 (sticky search bar hue-tint pattern — grep for the exact `hsla(${hue}, 45%, 25%, 0.82)` string; the add-wakeup pill in WakeupsTab.tsx should reference the same palette shape for consistency)
    - .planning/sketches/001-identity-modal-role-vs-identity-split/index.html:1360-1400 (variant D coordinator column mockup — the "Add role wakeup" button style: `hsla(${hue}, 60%, 45%, 0.25)` background + `hsla(${hue}, 60%, 55%, 0.4)` border + rounded 6px radius + font-size 11px + font-weight 600; the pill from Plan 02 should match this treatment)
  </read_first>
  <action>
    (1) **WakeupsTab.tsx — add isCoordinator prop + three-branch empty state (actor-empty-with-pill / coordinator-role-scope-empty-with-pill / coordinator-identity-scope-empty-WITHOUT-pill):**

    - Extend the WakeupsTab prop signature to add `isCoordinator: boolean;` (required, non-optional; caller always knows this).
    - In the empty-list branch (currently returns `<div>No scheduled wake-ups.</div>` + Add-wakeup pill per Plan 02's updated component), split into three code paths as follows. This is EXACT JSX for all three branches so there's no interpretation about pill visibility:

      ```
      if (state.data.length === 0) {
        // Branch 1: coordinator + Identity scope → caption only, NO pill.
        // Coordinators create wakeups from Role scope; hiding the pill here
        // prevents them from accidentally creating identity-scope wakeups.
        if (isCoordinator && scope === "identity") {
          return (
            <div
              className="text-sm text-[var(--color-pv-fg-muted)]"
              data-testid="wakeups-coordinator-empty-identity"
            >
              Coordinators use role-scope wakeups only. Switch to Role view to manage.
            </div>
          );
        }

        // Branch 2: coordinator + Role scope (empty role-wakeup list) → caption
        //           AND the Add-wakeup pill so the first role-scope wakeup can
        //           be created from a blank slate.
        // Branch 3: actor + Identity scope (empty identity-wakeup list) →
        //           SAME shape as branch 2, different scope context. Pill is
        //           the same component in both cases.
        return (
          <div className="flex flex-col gap-3">
            <button
              type="button"
              data-testid="wakeup-add-button"
              onClick={() => setAddDialogOpen(true)}
              style={{
                background: `hsla(${hue}, 60%, 45%, 0.25)`,
                border: `1px solid hsla(${hue}, 60%, 55%, 0.4)`,
                color: "#f0ebe0",
                borderRadius: "9999px",
                padding: "6px 14px",
                fontSize: "11px",
                fontWeight: 600,
                alignSelf: "flex-start",
              }}
            >
              <Plus size={12} className="inline mr-1" />Add wakeup
            </button>
            <div className="text-sm text-[var(--color-pv-fg-muted)]">
              No scheduled wake-ups.
            </div>
          </div>
        );
      }
      ```

    - The data-branch (state.data.length > 0) STILL renders the Add-wakeup pill above the wakeup row list (that's the Plan 02 baseline behavior — do not remove it). The pill's `data-testid="wakeup-add-button"` appears in TWO code paths: (a) the data-branch top-of-list position, (b) the empty-branch (non-coordinator-identity case) inside the empty-state div. The `data-testid="wakeup-add-button"` grep asserts a total of `>=2` across the file.

    - The `<AddWakeupDialog>` element continues to render at the tab-pane level (unchanged from Plan 02 placement) so BOTH pill code paths can open it via shared `addDialogOpen` state.

    - Also thread `isCoordinator` down to `WakeupRow` if any coordinator-specific rendering emerges (currently none — just future-proof; if not needed, only touch WakeupsTab's top-level component).

    (2) **WakeupsTab.tsx — visual polish on Add-wakeup pill (data-branch position):**

    - The data-branch pill (top of the wakeup row list, from Plan 02) uses the same style block as the empty-branch pill (both defined in section 1 above). Grep the current Plan 02 pill styling with testid `wakeup-add-button`. Compare to the sketch's "Add role wakeup" button at .planning/sketches/001-identity-modal-role-vs-identity-split/index.html:1374 which uses `hsla(${hue}, 60%, 45%, 0.25)` background + `hsla(${hue}, 60%, 55%, 0.4)` border + rounded-6px + font-size 11px + font-weight 600.
    - Update BOTH pill instances to use the shape from section 1 (identical style + identical testid). If Plan 02 already used a different palette (e.g. the sticky search bar's `hsla(${hue}, 45%, 25%, 0.82)`), replace it with the sketch's polished style. Keep the lucide `Plus` icon before the "Add wakeup" text. Hover state: brighten background to `hsla(${hue}, 65%, 50%, 0.35)`.

    (3) **HandoffTab.tsx — add isCoordinator prop + empty branch:**

    - Extend HandoffTab prop signature to add `isCoordinator: boolean;`.
    - At the TOP of the render body (before any markdown-empty check), add:
      ```
      if (isCoordinator) {
        return (
          <div className="text-sm text-[var(--color-pv-fg-muted)]" data-testid="handoff-coordinator-empty">
            Coordinators are stateless routers — no handoff to display.
          </div>
        );
      }
      ```
    - This intentionally short-circuits BEFORE the existing empty-markdown fallback so even a coordinator with a stray handoff.md file on disk gets the coordinator caption (the caption is more informative than raw markdown content, and coordinator handoffs are semantically undefined per the shape).

    (4) **IdentityModal.tsx — thread isCoordinator to both tabs:**

    - Find the identity-wakeups TabsContent (added/renamed in Plan 03 at what was L1902): update the `<WakeupsTab ...>` call to include `isCoordinator={identity.coordinator}`.
    - Find the role-wakeups TabsContent (added in Plan 03): update the `<WakeupsTab ...>` call to include `isCoordinator={identity.coordinator}` (even though role-scope coordinator behavior is normal — for prop consistency; the coordinator branch in WakeupsTab only fires under scope='identity').
    - Find the handoff TabsContent (at what was L1909): update the `<HandoffTab state={handoffState} onSave={updateHandoff} />` call to include `isCoordinator={identity.coordinator}`.

    (5) **Create test file `IdentityModal.coordinator-empty.test.tsx`:**

    Cover 5 tests (adds C5 to explicitly guard the pill-in-both-branches invariant):
    - Test C1: coordinator (coordinator=true) + Identity scope + empty wakeups list → `data-testid="wakeups-coordinator-empty-identity"` present with expected caption text "Coordinators use role-scope wakeups only. Switch to Role view to manage." The Add-wakeup pill is NOT rendered in this branch (assert `queryByTestId("wakeup-add-button")` inside the Identity-scope Wakeups pane is null).
    - Test C2: coordinator + Handoff tab (under Identity scope) → `data-testid="handoff-coordinator-empty"` present with caption "Coordinators are stateless routers — no handoff to display." No markdown editor rendered even if handoffState.data has content (short-circuit).
    - Test C3: coordinator + Identity file tab → renders normally via IdentityFileTab (no coordinator short-circuit here; assert the markdown editor or its skeleton renders as usual).
    - Test C4: actor (coordinator=false) + Identity scope + empty wakeups list → shows the NORMAL "No scheduled wake-ups." + Add-wakeup pill (regression guard — coordinator-branch does not affect actors). Assert BOTH: the "No scheduled wake-ups." caption text is present AND `getByTestId("wakeup-add-button")` returns the pill button in the same DOM subtree.
    - Test C5 (pill-in-both-branches invariant): mount WakeupsTab standalone (or via IdentityModal) with `scope="role"`, `isCoordinator=false`, and (a) an EMPTY wakeup list → assert `getByTestId("wakeup-add-button")` renders inside the empty-state div; then re-mount with (b) a NON-empty wakeup list → assert `getByTestId("wakeup-add-button")` renders above the wakeup row list. The pill must appear in BOTH data-branch and empty-branch code paths.

    Use `renderModal({coordinator: true})` from IdentityModal.test.tsx (import BASE_IDENTITY + renderModal helper). Mock WS reads returning empty wakeups arrays via the same mocking pattern used in IdentityModal.test.tsx.
  </action>
  <acceptance_criteria>
    - grep confirms isCoordinator on WakeupsTab: `grep -cE 'isCoordinator:\\s*boolean' src/ui/features/pretty-view/WakeupsTab.tsx` returns >=1
    - grep confirms coordinator-empty caption: `grep -c "Coordinators use role-scope wakeups only. Switch to Role view to manage." src/ui/features/pretty-view/WakeupsTab.tsx` returns 1
    - grep confirms isCoordinator on HandoffTab: `grep -cE 'isCoordinator:\\s*boolean' src/ui/features/pretty-view/HandoffTab.tsx` returns >=1
    - grep confirms handoff coordinator caption: `grep -c "Coordinators are stateless routers — no handoff to display." src/ui/features/pretty-view/HandoffTab.tsx` returns 1
    - grep confirms threading in IdentityModal: `grep -cE 'isCoordinator=\\{identity\\.coordinator\\}' src/ui/features/pretty-view/IdentityModal.tsx` returns 3 (role-wakeups WakeupsTab + identity-wakeups WakeupsTab + HandoffTab)
    - grep confirms Add-wakeup pill hue-tint: `grep -cE "hsla\\(\\\\\\\$\\{hue\\},\\s*60%,\\s*45%" src/ui/features/pretty-view/WakeupsTab.tsx` returns >=1
    - grep confirms pill-in-both-branches invariant (pill testid appears in >=2 code paths — data-branch above rows, and non-coordinator-identity empty-branch inside empty div): `grep -cE 'data-testid=["'\'']wakeup-add-button["'\'']' src/ui/features/pretty-view/WakeupsTab.tsx` returns >=2
    - grep confirms coordinator-empty testid: `grep -cE 'data-testid=["'\'']wakeups-coordinator-empty-identity["'\'']' src/ui/features/pretty-view/WakeupsTab.tsx` returns 1 AND `grep -cE 'data-testid=["'\'']handoff-coordinator-empty["'\'']' src/ui/features/pretty-view/HandoffTab.tsx` returns 1
    - Scoped tests pass: `npx vitest run --related src/ui/features/pretty-view/WakeupsTab.tsx src/ui/features/pretty-view/HandoffTab.tsx src/ui/features/pretty-view/IdentityModal.tsx` — all existing 7 IdentityModal.*.test.tsx files + WakeupsTab.test.tsx + AddWakeupDialog.test.tsx + new IdentityModal.coordinator-empty.test.tsx (5 tests C1-C5) green
    - TypeScript: `npx tsc --noEmit` exits 0 (isCoordinator is required on both tabs; all callers must pass it)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/IdentityModal.coordinator-empty.test.tsx src/ui/features/pretty-view/WakeupsTab.test.tsx</automated>
  </verify>
  <done>Coordinator-Identity-view Wakeups + Handoff tabs render the two locked captions from CONTEXT.md, Add-wakeup pill matches sketch's hue-tint palette AND renders in BOTH the data-branch (above the row list) AND the non-coordinator-identity empty-branch (inside the empty state div, so first-wakeup flow is reachable). Only the coordinator-identity-scope-empty branch omits the pill. isCoordinator prop threaded to both consumers. 5 new tests (C1-C5) + all existing tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Ashley browser walk-through — validate Phase 72 ship</name>
  <action>Human-verify checkpoint. All automation from Task 1 is complete; this task pauses execution so Ashley can walk through the shipped modal in a real browser (dev, staging, or prod as appropriate to where the orchestrator has deployed by the time this checkpoint fires) and confirm the seven walk-throughs below. No code changes fire during this task — resume-signal is Ashley's typed "approved" or a description of issues to fold into a follow-up quick / Wave 4b.</action>
  <what-built>
    All four Phase 72 waves shipped:
    - Backend CRUD parity for role-scope + identity-scope wakeups (6 new WS handlers, 6 new writer/reader functions, 12 new wire types).
    - WakeupsTab.tsx is scope-aware with Add-wakeup pill + trash-with-confirm + scope pill on each row.
    - IdentityModal.tsx has a segmented Role/Identity switch, per-scope conditional NAV_SECTIONS + TabsContent, per-scope Wakeups panes wired to their respective WS handlers.
    - Zustand-shaped modal-scope-store remembers scope per-identityKey across opens within a browser session.
    - Coordinator Identity-view Wakeups + Handoff tabs render informative empty captions instead of raw "no data" states.
    - All test files updated / added; scoped tests green.

    Ashley browser walk-through validates that the design matches the sketch variant D, the scope split reads legibly for both actor and coordinator identities, and no existing modal behavior regressed.
  </what-built>
  <how-to-verify>
    1. **Wait for orchestrator ship** — this checkpoint fires BEFORE deploy but AFTER all code is committed. The box-maintainer directive says executors don't do deploys; the orchestrator handles push/build/deploy. So Ashley walks through against the DEV build first (or the newly-deployed staging URL if orchestrator has run the pre-checkpoint deploy).

    2. **Open Skynet in browser at https://term.gigaashley.click** (or the dev/staging URL if not yet shipped to prod).

    3. **Actor identity walk-through (use "tina" or any actor):**
       - Tap Tina's badge in a chat to open the modal.
       - EXPECT: the modal opens with the segmented Role/Identity control at the top, Identity is highlighted (aria-pressed), the bottom icon-bar shows 3 tabs: Identity file / Wakeups / Handoff, and the Identity file tab is active.
       - Tap the Role scope switch button (left).
       - EXPECT: the bottom icon-bar reshuffles to 4 tabs: Role file / Bounties / History / Wakeups. Role file tab is active.
       - Tap the Wakeups tab under Role scope.
       - EXPECT: the list shows role-scope wakeups (or "No scheduled wake-ups." + Add-wakeup pill if none exist). Every row's scope pill reads "role".
       - Tap the Add-wakeup pill. EXPECT: a sub-modal opens with Name / Schedule type / per-type params / Timezone (optional IANA) / Instruction / Enabled Switch / Save / Cancel. Sub-modal title reads "Add role-scope wakeup".
       - Cancel the sub-modal.
       - Tap the trash icon on any wakeup row. EXPECT: an AlertDialog confirm opens: "Delete '<slug>'? This cannot be undone." with Cancel and Delete buttons.
       - Cancel the confirm.
       - Switch back to Identity scope. Tap the Wakeups tab. EXPECT: shows identity-scope wakeups (should be non-empty since Tina has identity-scope wakeups today). Every row's scope pill reads "identity".
       - Close the modal, reopen it (same identity). EXPECT: it opens on the LAST scope you had selected (Identity, since that was the last tap).

    4. **Coordinator identity walk-through (use a known coordinator identity):**
       - Tap the coordinator's badge in a chat.
       - EXPECT: the modal opens with the Role scope switch highlighted, bottom icon-bar shows 4 tabs (Role file / Bounties / History / Wakeups), Role file tab is active.
       - Tap the Identity scope switch.
       - EXPECT: bottom icon-bar reshuffles to 3 tabs (Identity file / Wakeups / Handoff), Identity file tab is active.
       - Tap the Wakeups tab.
       - EXPECT: the caption reads "Coordinators use role-scope wakeups only. Switch to Role view to manage." No Add-wakeup pill visible.
       - Tap the Handoff tab.
       - EXPECT: the caption reads "Coordinators are stateless routers — no handoff to display."
       - Tap the Identity file tab.
       - EXPECT: renders the coordinator's identity.md file normally (or the standard empty-file behavior if the file doesn't exist).

    5. **Regression sweep (each takes 30s):**
       - Under an actor's Role scope, tap Bounties. EXPECT: bounty search input at top, priority-sorted bounty cards render, Archive accordion at bottom, all the existing bounty operations work (priority change, status change, pin toggle, etc).
       - Type in the bounty search input. EXPECT: filter fires, results narrow, escape clears the input.
       - Expand the Archive accordion. EXPECT: lazy load fires (spinner or content), archived bounties render.
       - Under Identity scope, edit an identity's title (inline editor in title bar). EXPECT: Save persists, badge/avatar refreshes.
       - Toggle the "Stays awake" switch. EXPECT: persists.

    6. **Sketch fidelity check:**
       - Compare the modal against `.planning/sketches/001-identity-modal-role-vs-identity-split/index.html` variant D (Top Scope Switch).
       - EXPECT: segmented control positioning, hue tint, bottom-bar tab arrangement, add-wakeup pill visual language all match the sketch (allowing minor pixel differences).

    Type "approved" if all seven walkthroughs pass. Type a description of what's off if not.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found. If issues surface, they get folded into a follow-up quick or a Wave 4b task before ship.</resume-signal>
</task>

</tasks>

<verification>
  Overall phase-4 verification:
  - Scoped tests: `npx vitest run --related src/ui/features/pretty-view/WakeupsTab.tsx src/ui/features/pretty-view/HandoffTab.tsx src/ui/features/pretty-view/IdentityModal.tsx` — all green
  - TypeScript: `npx tsc --noEmit` exits 0
  - Ashley human-verify checkpoint: approved
</verification>

<success_criteria>
- Coordinator Identity-view Wakeups tab renders the CONTEXT.md-locked caption instead of "No scheduled wake-ups."
- Coordinator Identity-view Handoff tab renders the CONTEXT.md-locked caption instead of raw markdown / empty
- Coordinator Identity-file tab renders normally
- Non-coordinator-identity empty branches (actor Identity empty + any-user Role empty) show "No scheduled wake-ups." + the Add-wakeup pill (first-wakeup flow reachable)
- Add-wakeup pill appears in BOTH the data-branch (above the row list) AND the non-coordinator-identity empty-branch (inside the empty-state div) — grep-audited
- Add-wakeup pill matches sketch variant D's hue-tint palette
- Ashley confirms via real-browser walk-through that the design is legible + no regressions
- All scoped tests pass
</success_criteria>

<output>
Create `.planning/phases/72-identity-modal-role-identity-scope-split-with-role-level-wak/72-04-SUMMARY.md` when done. Include:
- Exact caption strings landed (both wakeups + handoff coordinator empty branches)
- Add-wakeup pill final styling (background + border + hue) with the exact hsla values
- Confirmation that the pill testid `wakeup-add-button` appears in >=2 code paths in WakeupsTab.tsx (data-branch above rows + non-coordinator-identity empty-branch inside empty div); coordinator-identity-scope-empty branch omits the pill
- Test coverage summary for IdentityModal.coordinator-empty.test.tsx (which 5 tests C1-C5 passed)
- Ashley's checkpoint verdict + any UAT tweaks that landed as follow-up commits in this task
- Confirmation that the phase's spirit-violation guardrail ("any state where the reader can't tell whether the thing they're looking at is a role thing or an identity thing") is closed by the combination of top scope switch + per-row scope pills + coordinator empty captions
</output>
</output>
