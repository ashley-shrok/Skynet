---
phase: 72-identity-modal-role-identity-scope-split-with-role-level-wak
plan: 02
type: execute
wave: 2
depends_on:
  - 72-01
files_modified:
  - src/ui/features/pretty-view/WakeupsTab.tsx
  - src/ui/features/pretty-view/WakeupsTab.test.tsx
  - src/ui/features/pretty-view/AddWakeupDialog.tsx
  - src/ui/features/pretty-view/AddWakeupDialog.test.tsx
  - src/ui/features/pretty-view/IdentityModal.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "WakeupsTab accepts a `scope: 'role' | 'identity'` prop and routes list/create/update/delete callbacks to the caller-supplied handlers — WakeupsTab itself is scope-agnostic beyond an 'each wakeup wears its scope' visible pill."
    - "Users can add a new wakeup via a hue-tinted 'Add wakeup' pill button at the top of the tab; clicking it opens a Radix Dialog-in-Dialog sub-modal with Name / Schedule-type / per-type params / optional Timezone (IANA text input, applies to Daily/Weekly/One-shot only) / Instruction / Enabled form fields — 6 fields total per CONTEXT.md § Add-wakeup UX."
    - "Users can delete a wakeup via a trash icon on each row; clicking it opens a Radix AlertDialog confirm; on confirm the delete callback fires and the list refreshes."
    - "The Save button in the add-wakeup dialog is disabled until Name and Instruction are non-empty and the schedule params validate."
    - "Every wakeup row visibly declares its scope via a small pill/badge — reader can tell at a glance whether they're looking at a role-scope or identity-scope wakeup."
    - "IdentityModal L1905 WakeupsTab call site passes stub values (scope='identity', no-op onCreate/onDelete) with a TODO Wave 3 comment — TypeScript stays clean without any @ts-expect-error suppression (avoids TS2578 / TS2741 diagnostic instability)."
  artifacts:
    - path: "src/ui/features/pretty-view/WakeupsTab.tsx"
      provides: "WakeupsTab component with scope prop + add/delete affordances + scope pill on each row"
      contains: "scope: \"role\" | \"identity\""
    - path: "src/ui/features/pretty-view/AddWakeupDialog.tsx"
      provides: "Radix Dialog-in-Dialog sub-modal component for creating a new wakeup"
      contains: "AddWakeupDialog"
    - path: "src/ui/features/pretty-view/WakeupsTab.test.tsx"
      provides: "Extended test coverage: scope prop routes to the right callbacks, add button opens dialog, trash icon opens confirm, delete callback fires on confirm"
    - path: "src/ui/features/pretty-view/AddWakeupDialog.test.tsx"
      provides: "Unit coverage for the add-wakeup form: field validation, per-type params render, Timezone override wiring, Save disabled until valid, Cancel closes without emitting"
  key_links:
    - from: "src/ui/features/pretty-view/WakeupsTab.tsx"
      to: "src/ui/features/pretty-view/AddWakeupDialog.tsx"
      via: "sub-component with open state controlled by WakeupsTab"
      pattern: "<AddWakeupDialog"
    - from: "WakeupsTab component props"
      to: "IdentityModal-supplied onCreate/onUpdate/onDelete callbacks"
      via: "callback props (no direct WS knowledge in WakeupsTab)"
      pattern: "onCreate: \\(spec: WakeupSpecWire\\)"
    - from: "WakeupRow trash icon"
      to: "Radix AlertDialog"
      via: "controlled open state per-row"
      pattern: "AlertDialog"
    - from: "IdentityModal.tsx L1905 WakeupsTab call site"
      to: "stub scope + no-op onCreate/onDelete (Wave 3 will replace with real wiring)"
      via: "inline stub values + TODO comment (no @ts-expect-error suppression)"
      pattern: "TODO Wave 3: replace stubs with real wiring"
---

<objective>
Extend WakeupsTab.tsx to be scope-aware and to expose full CRUD affordances (add + delete alongside existing list + update). No modal-shell / IdentityModal changes yet beyond a one-line stub-values patch at the WakeupsTab call site to keep TypeScript green — this plan produces a self-contained component that Wave 3 will wire into two scoped tabs in IdentityModal.

Purpose: The current WakeupsTab does list + update only; the shape's "both scopes get full CRUD parity" locks add + delete as required. This plan lands the UI for both new affordances at the component level, plus adds the scope prop that Wave 3 uses to route to either role-scope or identity-scope wire calls. Splitting this from Wave 3 keeps the WakeupsTab diff and the IdentityModal diff independently reviewable.

Output: WakeupsTab.tsx accepts `scope`, `onCreate`, `onDelete` (in addition to existing `state`, `hue`, `onUpdate`), renders an "Add wakeup" pill + a trash icon per row, and passes tests covering both new flows plus a scope-visibility pill on each row. AddWakeupDialog.tsx exposes the 6 CONTEXT.md-locked form fields (including optional Timezone). IdentityModal L1905 call site is patched with stub values + a TODO Wave 3 comment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/72-identity-modal-role-identity-scope-split-with-role-level-wak/72-CONTEXT.md
@.planning/phases/72-identity-modal-role-identity-scope-split-with-role-level-wak/72-01-SUMMARY.md
@src/ui/features/pretty-view/WakeupsTab.tsx
@src/ui/api/claude-session-api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract AddWakeupDialog sub-modal component + add its test file</name>
  <files>src/ui/features/pretty-view/AddWakeupDialog.tsx, src/ui/features/pretty-view/AddWakeupDialog.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/WakeupsTab.tsx:44-192 (existing FormSchedule discriminated union + hydrateFormSchedule + buildSchedule + validateForm + normalizeDays + detectBrowserTimezone + toIsoWithOffset — REUSE these functions; either move them to a shared module OR export them from WakeupsTab and import here)
    - src/ui/features/pretty-view/WakeupsTab.tsx:167-192 (buildSchedule — currently accepts a single `tz` param used for daily/weekly/one_shot; the AddWakeupDialog Timezone override piggybacks on this by passing the user's override (or the detected default) into the same param)
    - src/ui/features/pretty-view/WakeupsTab.tsx:214-273 (RestrictToDaysChips — REUSE for daily/weekly variants in the add form; either export or duplicate the component)
    - src/ui/features/pretty-view/WakeupsTab.tsx:275-322 (WakeupsTab shape — mirror the loading/error/empty states in the add dialog for consistency)
    - src/ui/features/pretty-view/WakeupsTab.tsx:600-830 (WakeupRow edit-mode form region — the Name / Schedule-type / per-type params / Instruction / Enabled field cluster; the add dialog builds the SAME field set but from empty defaults, not from a hydrated existing spec, and adds the CONTEXT.md-locked optional Timezone field between per-type params and Instruction)
    - src/ui/components/dialog.tsx (Radix Dialog wrapper — check if the project's Dialog primitive supports nested dialogs; if not, the add form uses radix-ui/react-dialog directly as the modal already does at src/ui/features/pretty-view/IdentityModal.tsx:3)
    - src/ui/features/pretty-view/IdentityModal.tsx:1-10 (DialogPrimitive import + how Radix Dialog is wired in the parent — the add dialog mounts inside the same portal region and the parent modal must not close when the sub-dialog closes)
    - src/ui/api/claude-session-api.ts (search for `WakeupSpecWire` — the type that Task 2 of Plan 01 added to represent the spec payload)
    - .planning/phases/72-identity-modal-role-identity-scope-split-with-role-level-wak/72-CONTEXT.md § "Add-wakeup UX (LOCKED — decision)" — locks the 6-field form (Name / Schedule type / Schedule params / Timezone (optional IANA) / Instruction / Enabled). Timezone override applies to Daily/Weekly/One-shot only; empty = box-local default.
  </read_first>
  <action>
    Create `src/ui/features/pretty-view/AddWakeupDialog.tsx` exporting a `AddWakeupDialog` component with props:

    ```
    type AddWakeupDialogProps = {
      open: boolean;
      onOpenChange: (next: boolean) => void;
      hue: number;
      scope: "role" | "identity";
      onSubmit: (spec: WakeupSpecWire) => Promise<void>;  // parent handles WS call
    };
    ```

    Component shape:
    - Radix Dialog-in-Dialog: use `DialogPrimitive.Root` + `DialogPrimitive.Portal` + `DialogPrimitive.Content` (radix-ui/react-dialog). Nested dialogs are legal in Radix; the parent modal stays mounted per Radix's stacked-modal semantics.
    - Title: `"Add {scope === "role" ? "role" : "identity"}-scope wakeup"` — the scope word is redundant with the top switch but reinforces the "no scope confusion" spirit-violation rule from CONTEXT.md.
    - Fields (mirror WakeupRow's edit-mode form region byte-for-byte, but starting from empty state; 6 fields total per CONTEXT.md § "Add-wakeup UX"):
      1. `Name` — controlled text input, required. Placeholder: `"e.g. morning-standup"`. Label prose: `"Name (becomes filename slug)"`.
      2. `Schedule type` — segmented select (buttons row, aria-pressed) with 4 options: Interval / Daily / Weekly / One-shot. Default: `"daily"`.
      3. `Schedule params` — dynamic per selected type; reuse the SAME per-type form fields from WakeupRow's edit-mode:
         - Interval: `every` numeric input + unit dropdown (`s` / `m` / `h` / `d`), no timezone shown, RestrictToDaysChips available.
         - Daily: `at` HH:MM time input, detectedTz hint, RestrictToDaysChips.
         - Weekly: `day` mon..sun dropdown + `at` HH:MM + detectedTz hint + RestrictToDaysChips.
         - One-shot: `at` datetime-local input, detectedTz hint, no days chips.
      4. `Timezone (optional)` — controlled text input, IANA name (e.g. `America/New_York`, `Europe/London`, `UTC`). Placeholder shows the DETECTED tz from `detectBrowserTimezone()`: e.g. `"America/New_York (leave blank to use)"`. Help text below the input: `"Leave blank to use box-local. Applies to Daily/Weekly/One-shot."`. Rendered ONLY when schedule type is daily / weekly / one_shot (hidden for interval — matches the existing behavior that buildSchedule OMITS timezone for interval, per L167-172). No validation of the IANA string itself (backend accepts whatever the browser writes; invalid tz is a run-time schedule-fire error, not a form-submit error). `data-testid="add-wakeup-tz-input"`.
      5. `Instruction` — controlled multiline textarea, required. Placeholder: `"What should the agent do when this fires?"`.
      6. `Enabled` — Switch component (default: true).
    - Buttons: `Save` (primary, hue-tinted) + `Cancel` (outline).
    - Save disabled when: name empty OR name normalizes to empty slug (use same `.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "")` regex from writeRoleWakeupCreate) OR instruction empty OR `validateForm(formSchedule)` returns non-null.
    - On Save click:
      1. Compute effective timezone: `const effectiveTz = tzDraft.trim() !== "" ? tzDraft.trim() : detectedTz;` — falls back to the auto-detected browser tz when the user leaves the field blank.
      2. Build spec: `{name: nameDraft, enabled: enabledDraft, schedule: buildSchedule(formSchedule, effectiveTz), instruction: instructionDraft}`. buildSchedule OMITS timezone for interval per L167-172 — user's tzDraft is dropped silently for interval, matching CONTEXT.md.
      3. Set `saving=true`, call `await onSubmit(spec)`.
      4. On success (no throw): call `onOpenChange(false)` + reset form state to defaults (including tzDraft to "").
      5. On throw: surface `err.message` in an inline `<div className="text-xs text-rose-300">` under the buttons, keep dialog open.
      6. Set `saving=false` in finally.
    - On Cancel click: `onOpenChange(false)` without emitting.

    **Refactor decision:** Move `FormSchedule`, `Weekday`, `WEEKDAY_VALUES`, `hydrateFormSchedule`, `buildSchedule`, `validateForm`, `normalizeDays`, `detectBrowserTimezone`, `toIsoWithOffset`, `pad2`, `cap`, `isWeekday`, and the `RestrictToDaysChips` component from `WakeupsTab.tsx` into a NEW shared module `src/ui/features/pretty-view/wakeup-form.ts` (and `wakeup-form.tsx` if RestrictToDaysChips comes along — but since it's a component, put it in a `.tsx` file: `src/ui/features/pretty-view/WakeupFormShared.tsx`). Update WakeupsTab.tsx imports to consume from the shared module. AddWakeupDialog imports from the same module. This is a NON-BEHAVIOR-CHANGE refactor — the existing WakeupsTab.test.tsx must still pass unchanged (the moved functions are identical in behavior).

    Use the mid-gradient hue-tint palette from IdentityModal.tsx:1201 for the dialog background: `background: linear-gradient(160deg, hsla(${hue}, 45%, 25%, 0.82), hsla(${hue}, 40%, 15%, 0.88))`. Use the same border/boxShadow shape from IdentityModal.tsx:1204-1205 for consistency with the parent modal's visual language.

    Add `data-testid="add-wakeup-dialog"` to the dialog Content root. Add `data-testid="add-wakeup-save"` and `data-testid="add-wakeup-cancel"` to the buttons. Add `data-testid="add-wakeup-tz-input"` to the Timezone text input.

    Create `AddWakeupDialog.test.tsx` covering:
    - Test A: dialog renders when open=true, does not render when open=false
    - Test B: switching schedule type re-renders the per-type param fields (interval → daily → weekly → one_shot)
    - Test C: Save button disabled when name is empty
    - Test D: Save button disabled when instruction is empty
    - Test E: Save button disabled when name normalizes to empty slug (e.g. name = "!!!")
    - Test F: Save fires onSubmit with correctly-shaped WakeupSpecWire payload (spec.schedule matches buildSchedule output for the selected type + form values)
    - Test G: Cancel calls onOpenChange(false) without calling onSubmit
    - Test H: onSubmit rejection surfaces error inline; dialog stays open; Save re-enables
    - Test I: Timezone input is HIDDEN when schedule type is Interval; VISIBLE when Daily / Weekly / One-shot
    - Test J: Timezone input placeholder text contains the auto-detected tz (mock `detectBrowserTimezone` to return `"Europe/London"`; assert placeholder contains `"Europe/London"`)
    - Test K: When Timezone input is left empty and schedule type is Daily, Save fires onSubmit with `spec.schedule.timezone === detectedTz` (fallback to browser default)
    - Test L: When Timezone input is filled (e.g. `"UTC"`) and schedule type is Daily, Save fires onSubmit with `spec.schedule.timezone === "UTC"` (user override wins)

    Mock `onSubmit` with `vi.fn().mockResolvedValue(undefined)` (and `vi.fn().mockRejectedValue(new Error("collision"))` for Test H).
  </action>
  <acceptance_criteria>
    - grep confirms component exists: `grep -c "^export function AddWakeupDialog\|^export const AddWakeupDialog" src/ui/features/pretty-view/AddWakeupDialog.tsx` returns 1
    - grep confirms shared module exists: `test -f src/ui/features/pretty-view/WakeupFormShared.tsx && test -f src/ui/features/pretty-view/wakeup-form.ts` — one or both, depending on placement decision (RestrictToDaysChips needs .tsx; the pure helpers can live in .ts)
    - grep confirms shared imports: `grep -c "from ['\"]./WakeupFormShared['\"]\\|from ['\"]./wakeup-form['\"]" src/ui/features/pretty-view/WakeupsTab.tsx` returns >=1
    - grep confirms dialog uses hue-tint: `grep -c "hsla(\\\${hue}" src/ui/features/pretty-view/AddWakeupDialog.tsx` returns >=1
    - grep confirms 6 form-field affordances (Name / Schedule type / Schedule params / Timezone / Instruction / Enabled): `grep -cE 'add-wakeup-tz-input' src/ui/features/pretty-view/AddWakeupDialog.tsx` returns 1 (Timezone testid confirms field #4 exists)
    - grep confirms Timezone help text: `grep -c "Applies to Daily/Weekly/One-shot" src/ui/features/pretty-view/AddWakeupDialog.tsx` returns 1
    - grep confirms testids: `grep -cE 'data-testid=["'\'']add-wakeup-(dialog|save|cancel|tz-input)["'\'']' src/ui/features/pretty-view/AddWakeupDialog.tsx` returns 4
    - Scoped tests pass: `npx vitest run --related src/ui/features/pretty-view/AddWakeupDialog.tsx src/ui/features/pretty-view/WakeupsTab.tsx` — the new AddWakeupDialog.test.tsx passes (12 tests A-L) AND the existing WakeupsTab.test.tsx passes unchanged (the refactor preserves behavior)
    - No TypeScript errors: `npx tsc --noEmit` from project root exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/AddWakeupDialog.test.tsx src/ui/features/pretty-view/WakeupsTab.test.tsx</automated>
  </verify>
  <done>AddWakeupDialog component + test file land with all 6 CONTEXT.md-locked form fields (Timezone optional override wired to buildSchedule with browser-tz fallback), shared form helpers extracted into a co-located module, WakeupsTab.test.tsx passes unchanged, AddWakeupDialog.test.tsx passes 12 tests (A-L).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add scope prop + onCreate/onDelete callbacks + Add-wakeup pill + trash-with-confirm to WakeupsTab.tsx; patch IdentityModal L1905 call site with stub values</name>
  <files>src/ui/features/pretty-view/WakeupsTab.tsx, src/ui/features/pretty-view/WakeupsTab.test.tsx, src/ui/features/pretty-view/IdentityModal.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/WakeupsTab.tsx:275-322 (WakeupsTab function signature + top-level rendering — where to add the Add-wakeup pill, where to thread props, how to plumb scope down to WakeupRow)
    - src/ui/features/pretty-view/WakeupsTab.tsx:324-848 (WakeupRow — where to add the trash icon in the header row alongside the enabled Switch, and how to open the AlertDialog confirm)
    - src/ui/features/pretty-view/AddWakeupDialog.tsx (component built in Task 1 — WakeupsTab wraps this with its open-state controller)
    - src/ui/components/alert-dialog.tsx or the Radix alert-dialog primitive (search in src/ui/components — if not present, use `radix-ui/react-alert-dialog` directly)
    - src/ui/features/pretty-view/IdentityModal.tsx:1616-1651 (hue-tinted sticky search bar pattern — the "Add wakeup" pill uses the SAME hue-tint gradient shape at the top of the tab)
    - src/ui/features/pretty-view/IdentityModal.tsx:1900-1910 (the WakeupsTab call site at ~L1905 — this task patches it with stub values + a TODO Wave 3 comment. Plan 03 Task 2a replaces the stubs with real wiring.)
    - src/ui/features/pretty-view/WakeupsTab.test.tsx (existing 412-line test file — the new coverage lives inside this file, not a new file, per the planner's consolidation decision noted in the top-level plan set)
  </read_first>
  <action>
    Update `WakeupsTab.tsx`:

    (1) Extend the component's prop signature at the top of `export function WakeupsTab({...})`:
    ```
    {
      state: TabState<Wakeup[]>;
      hue: number;
      scope: "role" | "identity";                             // NEW
      onUpdate: OnUpdate;                                     // existing
      onCreate: (spec: WakeupSpecWire) => Promise<void>;      // NEW
      onDelete: (slug: string) => Promise<void>;              // NEW
    }
    ```

    (2) At the top of the rendered output (inside the loading/error/data guards — data branch specifically), before the `.map` over `state.data`, add:

    - A hue-tinted "Add wakeup" pill button. Style: match IdentityModal's sticky search bar hue-tint (L1618: `hsla(${hue}, 45%, 25%, 0.82)`) but as a rounded-full pill button. Icon: lucide `Plus`. Label: `"Add wakeup"`. On click: sets `addDialogOpen=true` state. Add `data-testid="wakeup-add-button"`.

    - An `<AddWakeupDialog>` element with `open={addDialogOpen}`, `onOpenChange={setAddDialogOpen}`, `hue={hue}`, `scope={scope}`, `onSubmit={onCreate}`. This mounts inside the tab pane; on submit success it closes itself via `onOpenChange(false)`.

    Also render the button in the empty-state branch (currently `<div>No scheduled wake-ups.</div>` at L303) so the user can add the FIRST wakeup — otherwise the empty state is a dead end.

    (3) Thread `scope` and `onDelete` down to `WakeupRow`. Extend `WakeupRow` props:
    ```
    {
      wakeup: Wakeup;
      hue: number;
      scope: "role" | "identity";     // NEW
      onUpdate: OnUpdate;             // existing
      onDelete: (slug: string) => Promise<void>;   // NEW
    }
    ```

    (4) In `WakeupRow` header row (near the existing enabled Switch), add:

    - A scope pill/badge: small rounded-full text pill reading `"role"` or `"identity"`, hue-tinted using the same palette. `data-testid="wakeup-scope-pill"`. This satisfies the CONTEXT.md "every wakeup visibly declares its scope" requirement and the spirit-violation guardrail.

    - A trash icon button (lucide `Trash2`, size 14) positioned to the RIGHT of the enabled Switch. On click, opens a Radix AlertDialog (Radix `AlertDialogPrimitive` from `radix-ui` or the project's own `<AlertDialog>` wrapper in `src/ui/components/alert-dialog.tsx` if present). AlertDialog title: `"Delete wakeup?"`. AlertDialog description: `"Delete '${wakeup.slug}'? This cannot be undone."`. Buttons: Cancel (closes dialog) + Delete (destructive-styled, calls `await onDelete(wakeup.slug)` then closes on success). While the delete promise is in-flight, disable both buttons. On rejection, surface error inline in the AlertDialog body. `data-testid` on the trash icon: `wakeup-delete-icon`. `data-testid` on the confirm button: `wakeup-delete-confirm`.

    (5) Extend `src/ui/features/pretty-view/WakeupsTab.test.tsx` — do NOT create a new file. Add the following test cases (mirror existing test structure and mocking approach):

    - Test: scope='role' — Add-wakeup button click opens AddWakeupDialog with scope='role' prop (assert via testid presence + Dialog title text)
    - Test: scope='identity' — same but with scope='identity'
    - Test: onCreate callback fires with correctly-shaped spec when the sub-dialog Save button is clicked (fill in Name + Instruction, click Save, assert onCreate called once with spec.name/enabled/schedule/instruction)
    - Test: trash icon renders on every wakeup row (assert count of `wakeup-delete-icon` testids matches wakeups.length)
    - Test: clicking trash icon opens AlertDialog confirm; slug appears in the description text
    - Test: clicking confirm calls onDelete with the row's slug
    - Test: clicking cancel in the AlertDialog does NOT call onDelete
    - Test: onDelete rejection surfaces error text in the AlertDialog and does not close it
    - Test: scope='role' — every wakeup row's scope pill reads "role"
    - Test: scope='identity' — every wakeup row's scope pill reads "identity"
    - Test: empty-state branch still renders "No scheduled wake-ups." AND renders the Add-wakeup button (so first-wakeup flow is reachable)

    Mock onCreate/onDelete with `vi.fn().mockResolvedValue(undefined)` and (for rejection test) `vi.fn().mockRejectedValue(new Error("nope"))`.

    (6) **IdentityModal L1905 call-site patch (interim — replaced by Plan 03 Task 2a):**

    Do NOT modify any other part of IdentityModal.tsx. Only patch the existing WakeupsTab call site at ~L1905 with STUB values. Rationale for stubs (not `@ts-expect-error`): TypeScript emits ONE `TS2741` diagnostic per missing required prop; adding three required props (`scope`, `onCreate`, `onDelete`) would need three `@ts-expect-error` lines, and extra directives trigger `TS2578` (unused suppression) if TS coalesces them. Stubs are simpler + typecheck-stable.

    BEFORE:
    ```
    <WakeupsTab state={wakeupsState} hue={hue} onUpdate={updateWakeup} />
    ```

    AFTER:
    ```
    {/* TODO Wave 3: replace stubs with real wiring (scope + onCreate + onDelete land in Plan 03 Task 2a) */}
    <WakeupsTab
      state={wakeupsState}
      hue={hue}
      onUpdate={updateWakeup}
      scope="identity"
      onCreate={async () => {}}
      onDelete={async () => {}}
    />
    ```

    Stub semantics:
    - `scope="identity"` — matches pre-72 behavior (the existing wakeupsState is identity-scope; scope stub matches so the row-level scope pill reads "identity" during the interim between Waves 2 and 3).
    - `onCreate={async () => {}}` — no-op resolves immediately. If Ashley opens the modal in the pre-Wave-3 interim and clicks Add + Save, the dialog closes but nothing writes to disk. Wave 3 replaces with the real WS-plumbing.
    - `onDelete={async () => {}}` — same shape as above; interim delete-confirm click is a no-op.

    Plan 03 Task 2a replaces the entire JSX block with the real wiring (removes the TODO comment, splits into `<TabsContent value="identity-wakeups">` + `<TabsContent value="role-wakeups">` panes each with real callbacks).
  </action>
  <acceptance_criteria>
    - grep confirms scope prop: `grep -cE 'scope:\\s*"role"\\s*\\|\\s*"identity"' src/ui/features/pretty-view/WakeupsTab.tsx` returns >=2 (WakeupsTab + WakeupRow signatures)
    - grep confirms onCreate + onDelete props: `grep -cE '\\bonCreate\\b|\\bonDelete\\b' src/ui/features/pretty-view/WakeupsTab.tsx` returns >=6 (2 destructures + threading + call sites)
    - grep confirms Add-wakeup pill + testid: `grep -cE 'data-testid=["'\'']wakeup-add-button["'\'']' src/ui/features/pretty-view/WakeupsTab.tsx` returns 1
    - grep confirms trash icon: `grep -cE 'data-testid=["'\'']wakeup-delete-icon["'\'']' src/ui/features/pretty-view/WakeupsTab.tsx` returns 1
    - grep confirms scope pill: `grep -cE 'data-testid=["'\'']wakeup-scope-pill["'\'']' src/ui/features/pretty-view/WakeupsTab.tsx` returns 1
    - grep confirms AddWakeupDialog usage: `grep -c "<AddWakeupDialog" src/ui/features/pretty-view/WakeupsTab.tsx` returns 1
    - grep confirms stub-values patch in IdentityModal (interim state — NOT @ts-expect-error): `grep -c "TODO Wave 3: replace stubs with real wiring" src/ui/features/pretty-view/IdentityModal.tsx` returns 1
    - grep confirms NO @ts-expect-error suppression was introduced: `grep -c '@ts-expect-error.*scope/onCreate/onDelete' src/ui/features/pretty-view/IdentityModal.tsx` returns 0
    - grep confirms stub props landed on the call site: `grep -cE 'scope="identity"|onCreate=\\{async \\(\\) => \\{\\}\\}|onDelete=\\{async \\(\\) => \\{\\}\\}' src/ui/features/pretty-view/IdentityModal.tsx` returns >=3
    - Scoped tests pass: `npx vitest run --related src/ui/features/pretty-view/WakeupsTab.tsx` — all existing + all new tests green
    - TypeScript compiles: `npx tsc --noEmit` exits 0 (WakeupsTab call site at IdentityModal.tsx ~L1905 uses stub scope/onCreate/onDelete values with TODO comment — real wiring lands in Plan 03. If any stub value is missing, tsc fails with TS2741 "Property 'X' is missing".)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/WakeupsTab.test.tsx</automated>
  </verify>
  <done>WakeupsTab accepts scope + onCreate + onDelete props, renders Add-wakeup pill + AddWakeupDialog + per-row trash-with-confirm + per-row scope pill, extended test file covers both scopes with 10+ new assertions. IdentityModal L1905 call site patched with stub values (scope="identity", no-op async onCreate/onDelete) + a `TODO Wave 3` comment. NO @ts-expect-error suppression introduced (avoids TS2578/TS2741 diagnostic instability). Plan 03 Task 2a replaces the stubs with real wiring.</done>
</task>

</tasks>

<verification>
  Overall phase-2 verification:
  - Scoped tests: `npx vitest run --related src/ui/features/pretty-view/WakeupsTab.tsx src/ui/features/pretty-view/AddWakeupDialog.tsx` — all green
  - TypeScript: `npx tsc --noEmit` exits 0 (stub values on the interim call site cover the WakeupsTab call-site prop requirements)
  - Visual smoke check (deferred to Wave 4 human-verify): the Add-wakeup pill uses the same hue-tint palette as the sticky search bar in the Bounties tab
</verification>

<success_criteria>
- WakeupsTab is scope-agnostic: routes list/create/update/delete calls to caller-supplied handlers based on `scope` prop
- Users can open the add-wakeup sub-modal from either scope's Wakeups tab
- Users can delete a wakeup via trash-icon + AlertDialog confirm
- Every wakeup row visibly declares its scope (satisfies the "no scope confusion" spirit-violation guardrail)
- The add-wakeup sub-modal exposes all 6 CONTEXT.md-locked form fields, including the optional Timezone IANA text input (hidden for Interval, visible for Daily/Weekly/One-shot; blank falls back to auto-detected browser tz)
- Existing WakeupsTab behavior (list + inline update, editable schedule form, enabled toggle) unchanged — no regression
- Shared form helpers extracted into a reusable module (dedupe with AddWakeupDialog)
- IdentityModal L1905 interim call site uses stub values (not @ts-expect-error) to keep tsc green; Plan 03 replaces stubs with real wiring
</success_criteria>

<output>
Create `.planning/phases/72-identity-modal-role-identity-scope-split-with-role-level-wak/72-02-SUMMARY.md` when done. Include:
- New AddWakeupDialog.tsx component signature + WakeupsTab.tsx prop signature changes (before/after)
- Location of the shared wakeup-form helpers module + which functions/components got extracted
- Where the "Add wakeup" pill lives (line range) + where the trash icon lives (line range) + where the scope pill lives (line range) + where the Timezone input lives in AddWakeupDialog.tsx
- Confirmation that the IdentityModal.tsx ~L1905 call site uses the stub-values pattern (scope="identity", no-op async onCreate/onDelete) with the TODO Wave 3 comment — NOT @ts-expect-error
- Test file changes: exact count of new tests added to WakeupsTab.test.tsx + exact count of tests in the new AddWakeupDialog.test.tsx (should be 12: A-L, with I-L covering Timezone visibility + placeholder + fallback + override)
</output>
</output>
