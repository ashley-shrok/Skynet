---
phase: 81-optimistic-bubble-for-every-compose-box-send
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/ComposeBox.tsx
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-view/ChatMessage.tsx
  - src/ui/features/pretty-view/ChatMessage.test.tsx
autonomous: true
requirements:
  - D-05
  - D-06
  - D-07
  - D-08
  - D-14
  - D-15
  - D-16
user_setup: []

must_haves:
  truths:
    - "ChatMessage accepts an optional attachments prop with shape Array<{filename,size,mimetype}>"
    - "When ChatMessage renders as a pending user bubble with attachments present, the render shows caption above AttachmentChipStrip readOnly"
    - "When caption is empty and attachments are present, the render shows chip strip only, no caption element"
    - "Existing settled injected branch continues to fire first when parseInjectedUserTurn returns non-null (no regression)"
    - "Existing text-only pending branch (no attachments prop) continues to render ReactMarkdown fallback (no regression)"
    - "Failed pending bubble with attachments inherits Phase 76 whole-bubble-red style via bubbleInlineStyle on outer div"
    - "PendingSend record type accepts optional attachments field"
    - "handleOptimisticSend signature accepts optional attachments field and copies it into the seeded record"
    - "onOptimisticSend prop callback signature accepts optional attachments field (backward-compat for text-only funnel callsites)"
  artifacts:
    - path: "src/ui/features/pretty-view/ChatMessage.tsx"
      provides: "New optional attachments prop + new pending-with-attachments render branch after injected branch"
      contains: "attachments?: Array<{"
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "Widened PendingSend type + widened handleOptimisticSend signature"
      contains: "attachments?: Array<{"
    - path: "src/ui/features/pretty-view/ComposeBox.tsx"
      provides: "Widened onOptimisticSend prop-type"
      contains: "attachments?: Array<{"
    - path: "src/ui/features/pretty-view/ChatMessage.test.tsx"
      provides: "Unit tests for new render branch"
      contains: "pending-with-attachments"
  key_links:
    - from: "src/ui/features/pretty-view/ChatMessage.tsx"
      to: "src/ui/features/pretty-view/AttachmentChipStrip.tsx"
      via: "readOnly=true prop"
      pattern: "readOnly={true}"
    - from: "src/ui/features/pretty-view/PrettyView.tsx (PendingSend type)"
      to: "src/ui/features/pretty-view/ChatMessage.tsx (attachments prop)"
      via: "prop drilling at pendingSends.map render site (wired in Plan 02)"
      pattern: "attachments\\?: Array<\\{"
---

<objective>
Land the type-contract widening and the new ChatMessage render branch that Plan 02 will wire together. This plan produces the interface surface (widened `PendingSend`, widened `handleOptimisticSend`, widened `onOptimisticSend`, widened `ChatMessage` prop) and the new pending-with-attachments render branch inside `ChatMessage`. All widenings are backward-compatible optional fields — the existing text-only pending machinery (Phase 50 + 68 + 76) continues to work with zero behavior change. Also includes the ChatMessage-level unit tests that lock the new render branch shape.

Purpose: Establish the contracts before wiring. Plan 02 does the seed-call wiring against these contracts. Per Phase 50 D-01 and RESEARCH.md Option B, the seed call must live inside PrettyView's existing `onUploadReadyToInject` closure and pass attachments into `handleOptimisticSend`; that wiring lands in Plan 02.

Output: Type widenings + one new render branch + ChatMessage unit tests locking the new branch's shape. No behavior change yet — the new branch never fires until Plan 02 wires the seed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/81-optimistic-bubble-for-every-compose-box-send/81-CONTEXT.md
@.planning/phases/81-optimistic-bubble-for-every-compose-box-send/81-RESEARCH.md
@.planning/phases/81-optimistic-bubble-for-every-compose-box-send/81-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen type contracts — PendingSend, handleOptimisticSend, onOptimisticSend</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx, src/ui/features/pretty-view/ComposeBox.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/PrettyView.tsx (L1110-1290 for PendingSend type + handleOptimisticSend, and L3310-3358 for pending render callsite so you see what Plan 02 will wire)
    - src/ui/features/pretty-view/ComposeBox.tsx (L175-204 for onOptimisticSend prop shape + surrounding prop doc block; L440-496 for the useComposeSend funnel that CALLS onOptimisticSend at L471 and L487 — verify those two callsites do NOT need to pass attachments per D-16 / PATTERNS.md § ComposeBox anti-pattern)
    - .planning/phases/81-optimistic-bubble-for-every-compose-box-send/81-PATTERNS.md (§ ComposeBox.tsx type-widening + § PrettyView.tsx Site A + Site B)
    - .planning/phases/81-optimistic-bubble-for-every-compose-box-send/81-RESEARCH.md (§ Pattern 1 + Pattern 2 + § "PendingSend + onOptimisticSend Backward-Compatibility Surface")
  </read_first>
  <behavior>
    - PendingSend type (currently at PrettyView.tsx L1118-1124) gains optional attachments field of shape Array&lt;{filename: string; size: number; mimetype: string}&gt; per D-14. Field order in the type: after timer.
    - handleOptimisticSend (currently at PrettyView.tsx L1182-1258) accepts args.attachments (same optional shape). Destructures attachments alongside payload, mqid, immediateFailure. Copies attachments into BOTH the primary seed record spread (L1246-1255) AND the immediateFailure fallback spread (L1211-1220) when attachments is present and non-empty. When absent, records seed without the field (same as today).
    - handleOptimisticSend diagnostic log line at L1240 ([diag-dormant-send] arm ...) gains attachmentCount=${attachments?.length ?? 0} marker per RESEARCH.md § Open Questions #1.
    - ComposeBox onOptimisticSend prop-type (L181-192) gains optional attachments field with the same triple shape per D-16. The two existing funnel callsites at L471 (immediateFailure:false) and L487 (immediateFailure:true) do NOT change — they omit the field (backward-compat verified in RESEARCH.md § "onOptimisticSend callback consumers").
    - Type-only widening. Zero runtime behavior change in this task — no site currently PASSES attachments, so the widened path is dormant until Plan 02 wires it.
    - Test 1a (in PrettyView.tsx via existing PrettyView.optimistic-bubbles.test.tsx): existing text-only pending bubble test at L188-207 stays green — Plan 02 is where new tests for the wired path land. This task's TDD scope is limited to type-safety and existing-tests-still-green.
    - Test 1b: `npx tsc -p tsconfig.json --noEmit` exits 0 across the widened surface.
  </behavior>
  <action>
    Widen three type surfaces to accept an optional attachments field of exact shape `Array&lt;{filename: string; size: number; mimetype: string}&gt;`, per CONTEXT.md D-14/D-15/D-16 and PATTERNS.md § PrettyView Site A/Site B + § ComposeBox.

    Site 1 — PrettyView.tsx `PendingSend` type at L1118-1124: append `attachments?: Array&lt;{filename: string; size: number; mimetype: string}&gt;` after the `timer` field. Keep the type-alias style, semicolon-terminated fields, matches the existing shape at that site.

    Site 2 — PrettyView.tsx `handleOptimisticSend` at L1182-1258: widen the args typing so args includes optional attachments (same shape). Destructure attachments alongside payload/mqid/immediateFailure. In the primary seed record spread near L1246-1255, include `attachments` in the new record when `attachments && attachments.length > 0` (omit the field otherwise so text-only records match the current shape byte-for-byte). Apply the same conditional include in the immediateFailure fallback record spread near L1211-1220 (for the future case where an immediateFailure attachment seed is wanted — not used in this phase but stays consistent so consumers don't have to special-case). Extend the existing `[diag-dormant-send] arm` log line at L1240 to append `attachmentCount=${attachments?.length ?? 0}` as an additional space-separated key=value pair (do NOT restructure the log line; append at end).

    Site 3 — ComposeBox.tsx `onOptimisticSend` prop-type at L181-192: append `attachments?: Array&lt;{filename: string; size: number; mimetype: string}&gt;` to the args object type. Preserve the surrounding Phase 50 D-01/D-03/D-20 doc block verbatim. The two existing funnel callsites at L471 and L487 stay unchanged — they omit the optional field (backward-compat).

    Do NOT introduce a new prop or callback. Do NOT change ComposeBox behavior. Do NOT touch use-pretty-view-uploads.ts. Per RESEARCH.md Option B, the seed call for attachment sends lives in PrettyView's `onUploadReadyToInject` closure (Plan 02), not in ComposeBox. Any change to ComposeBox beyond the prop-type widening is out of scope.

    Add a code comment at the widened `handleOptimisticSend` args site referencing the invariant Plan 02 depends on: `// Phase 81 D-14/D-16: attachments carried on the pending record so ChatMessage renders caption+chip-strip via the new render branch (mqid === batchId === messageQueueItemId — RESEARCH.md Pitfall #2).`
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tabitha &amp;&amp; npx tsc -p tsconfig.json --noEmit 2>&amp;1 | tail -20</automated>
    <automated>cd /home/ubuntu/skynet-tabitha &amp;&amp; npx vitest run src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx src/ui/features/pretty-view/ComposeBox.test.tsx 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "attachments?: Array<{" src/ui/features/pretty-view/PrettyView.tsx | wc -l` returns at least 2 (PendingSend type + handleOptimisticSend args)
    - `grep -n "attachments?: Array<{" src/ui/features/pretty-view/ComposeBox.tsx | wc -l` returns at least 1 (onOptimisticSend prop)
    - `grep -c "attachmentCount=" src/ui/features/pretty-view/PrettyView.tsx` returns at least 1 (diagnostic log extension)
    - `npx tsc -p tsconfig.json --noEmit` exits 0
    - `npx vitest run src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx` — all existing tests pass (behavior-preserving)
    - `npx vitest run src/ui/features/pretty-view/ComposeBox.test.tsx` — all existing tests pass (behavior-preserving)
    - No new prop added to ComposeBoxProps beyond the widened `onOptimisticSend` field
    - `use-pretty-view-uploads.ts` is UNCHANGED (git diff shows zero touches)
  </acceptance_criteria>
  <done>Types widened backward-compatibly. TypeScript compiles clean. All existing pretty-view tests still green. Zero runtime behavior change. Ready for Plan 02 to seed the widened record.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ChatMessage new pending-with-attachments render branch + unit tests</name>
  <files>src/ui/features/pretty-view/ChatMessage.tsx, src/ui/features/pretty-view/ChatMessage.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/ChatMessage.tsx (L1-120 for prop shape + L280-520 for the render branches — especially L297-311 parseInjectedUserTurn call, L406-414 whole-bubble-red inline style, L420 outer bubble div where the style applies, and L479-509 settled injected render branch to mirror)
    - src/ui/features/pretty-view/AttachmentChipStrip.tsx (full file — 194 lines; confirm the readOnly prop contract at L36-49, the data-readonly="true" attribute at L107, and the styling gates at L114-134)
    - src/ui/features/pretty-view/ChatMessage.test.tsx (full file — see existing render-branch test patterns, mount factory, prop passing conventions)
    - .planning/phases/81-optimistic-bubble-for-every-compose-box-send/81-PATTERNS.md (§ ChatMessage.tsx new render branch — full pattern block including the exact JSX shape to copy)
    - .planning/phases/81-optimistic-bubble-for-every-compose-box-send/81-RESEARCH.md (§ Pattern 3 for the concrete JSX shape, § "ChatMessage Render Branch — Share vs Duplicate" for the ordering + duplicate-inline rationale, and § Pitfall #4 for the caption-collides-with-delimiter defense, § Pitfall #6 for the readOnly requirement)
  </read_first>
  <behavior>
    - ChatMessage accepts a new optional prop `attachments?: Array&lt;{filename: string; size: number; mimetype: string}&gt;`. Optional — text-only pending bubbles and settled bubbles omit it (backward-compat).
    - New render branch fires when: (a) existing `injected` branch condition (`parseInjectedUserTurn(content) !== null`) is FALSE, AND (b) attachments prop is defined and non-empty. Ordering: place AFTER the existing `injected ?` branch, BEFORE the ReactMarkdown fallback. Rationale: preserves settled-bubble semantics unchanged when a settled injected message arrives.
    - Render output when caption present (content.length > 0): a `&lt;div className="pv-injected-caption whitespace-pre-wrap mb-2"&gt;{content}&lt;/div&gt;` above an `&lt;AttachmentChipStrip readOnly={true} onRemove={()=>{}} attachments={...}/&gt;`. Chip strip receives synthesized `tempId` values of shape `pending-${f.filename}-${f.size}` (pending records have no landingPath — synthetic id is React-key-only, never surfaced), file objects of shape `{name: f.filename, size: f.size, type: f.mimetype}`, `status: "complete"`, `bytesUploaded: f.size`, `error: null`.
    - Render output when caption absent (content.length === 0, D-06): chip strip only, no caption div. Mirrors settled bubble's `injected.caption.length > 0 &&` gate at L488.
    - When `pendingState === "failed"` (Phase 76 D-06 / D-08), the outer bubble div's `bubbleInlineStyle` at L420 applies whole-bubble red. This is inherited for free — nothing new to add. Chips render `status:"complete"` regardless of pendingState (uploads succeeded by definition; failure is the harness echo not arriving, not the upload).
    - Do NOT extract a shared render helper — duplicate inline per RESEARCH.md § "Share vs Duplicate" (different caption source, different tempId source, different file-shape mapping, 8 lines).
    - Do NOT modify AttachmentChipStrip.
    - Do NOT synthesize a fake formatInjectedUserTurn string in the pending path (RESEARCH.md § Anti-Patterns).

    Unit test cases (all in ChatMessage.test.tsx, one describe block: `describe("ChatMessage pending-with-attachments (Phase 81)", ...)`):
    - Test A: renders caption above chip strip when both are present
    - Test B: renders chip strip only when content is empty string (D-06)
    - Test C: chip strip has data-readonly="true" attribute (Pitfall #6)
    - Test D: pending-with-attachments branch does NOT fire when attachments prop is undefined (existing text render fires)
    - Test E: pending-with-attachments branch does NOT fire when attachments prop is empty array
    - Test F: settled injected branch FIRES FIRST when content contains valid `---attached files---` delimited body — verifies branch ordering (settled wins over pending)
    - Test G: caption text containing the literal string "---attached files---" (without valid file lines) renders as pending-with-attachments (Pitfall #4 — parseInjectedUserTurn returns null on caption without well-formed file lines)
    - Test H: when pendingState === "failed", outer bubble div carries the whole-bubble-red inline style (Phase 76 D-06 inheritance — check for `background: hsla(0, 60%, 35%, 0.90)` or the substring "hsla(0, 60%" on the bubble root)
    - Test I: chip filenames render inside a `&lt;span&gt;` (React default text-node escaping — no dangerouslySetInnerHTML) — verify with a filename containing HTML metacharacters like `&lt;script&gt;evil.txt`
  </behavior>
  <action>
    Add the new render branch to ChatMessage.tsx per PATTERNS.md § ChatMessage.tsx and RESEARCH.md § Pattern 3. Add the corresponding unit tests to ChatMessage.test.tsx.

    Prop widening: extend the ChatMessage props declaration (around L52-87 area — see existing `pendingState?: "sending" | "failed" | null;` for the optional-prop pattern to match) with `attachments?: Array&lt;{filename: string; size: number; mimetype: string}&gt;`. Destructure in the component's parameter object.

    Render branch: locate the existing ternary chain at L479-509 (the `injected ?` branch that renders the settled attachment bubble). Insert a new branch AFTER the `injected ?` branch's closing `)` and BEFORE the fallback `<ReactMarkdown>` branch. Branch condition: `(attachments && attachments.length > 0)`. Branch body: mirrors the settled shape from L479-509 but sources data from the `attachments` prop directly (no `parseInjectedUserTurn` call). See PATTERNS.md § ChatMessage.tsx new render branch — the exact JSX pattern block is there to copy from. Use synthetic tempId `pending-${f.filename}-${f.size}` (comment: `// synthetic — pending records have no landingPath; used only as React key`). Always pass `readOnly={true}` (Pitfall #6). Always pass `onRemove={() => {}}` (readOnly means it never fires).

    Add a code comment above the new branch: `// Phase 81 D-15: pending-with-attachments render. Fires when the pending seed carried attachments (from PrettyView's onUploadReadyToInject closure, Plan 02). Sources from the `attachments` prop directly — no parseInjectedUserTurn call because the pending record's `content` field carries only the caption. See RESEARCH.md § Pattern 3 for shape rationale.`

    Add the unit test block in ChatMessage.test.tsx. Use the existing mount/render helpers from the file. For Test H (whole-bubble-red inheritance), render with `pendingState="failed"` + `attachments={[{filename: "a.txt", size: 1, mimetype: "text/plain"}]}` and assert the bubble root's inline style contains `hsla(0, 60%` (partial substring match tolerates React's style serialization). For Test I (XSS escape), pass filename `"<script>alert(1)</script>evil.txt"` in attachments and assert `container.innerHTML` does NOT contain a live `&lt;script&gt;` element (only the text-escaped variant).

    Test naming: `it("Test A: ...", ...)` through `it("Test I: ...", ...)` — mirror the numbering convention used by other pretty-view test files (PrettyView.optimistic-bubbles.test.tsx).

    Do NOT add integration tests here — those live in Plan 02. This task's tests are unit-level: they render `<ChatMessage>` directly with props, no PrettyView mount, no WS harness.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tabitha &amp;&amp; npx tsc -p tsconfig.json --noEmit 2>&amp;1 | tail -20</automated>
    <automated>cd /home/ubuntu/skynet-tabitha &amp;&amp; npx vitest run src/ui/features/pretty-view/ChatMessage.test.tsx 2>&amp;1 | tail -40</automated>
    <automated>cd /home/ubuntu/skynet-tabitha &amp;&amp; npx vitest run src/ui/features/pretty-view/ChatMessage.autoplay.test.tsx src/ui/features/pretty-view/ChatMessage.speak.test.tsx src/ui/features/pretty-view/ChatMessage.editable-file.test.tsx src/ui/features/pretty-view/ChatMessage.instrumentation.test.tsx 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "attachments?: Array<{" src/ui/features/pretty-view/ChatMessage.tsx` returns at least 1 match
    - `grep -n "Phase 81 D-15" src/ui/features/pretty-view/ChatMessage.tsx` returns 1 match (the branch comment)
    - `grep -c "pending-with-attachments" src/ui/features/pretty-view/ChatMessage.test.tsx | grep -v '^#'` returns at least 1 (describe block name)
    - `grep -c "^  it(\"Test [A-I]:" src/ui/features/pretty-view/ChatMessage.test.tsx` returns exactly 9 (Tests A-I)
    - `npx vitest run src/ui/features/pretty-view/ChatMessage.test.tsx` — ALL tests pass including the 9 new ones
    - `npx vitest run src/ui/features/pretty-view/ChatMessage.autoplay.test.tsx src/ui/features/pretty-view/ChatMessage.speak.test.tsx src/ui/features/pretty-view/ChatMessage.editable-file.test.tsx src/ui/features/pretty-view/ChatMessage.instrumentation.test.tsx` — all existing ChatMessage-family tests stay green (no regression to speak/autoplay/instrumentation/editable-file paths)
    - `npx vitest run src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx` — existing text-only pending tests still green (branch-ordering preserved: new branch doesn't fire when attachments prop is absent)
    - `npx tsc -p tsconfig.json --noEmit` exits 0
    - No changes to `src/ui/features/pretty-view/AttachmentChipStrip.tsx` (git diff shows zero touches)
    - No changes to `src/ui/features/pretty-view/PrettyView.tsx` beyond Task 1's type widening (git diff shows only the type + log-line edits)
    - Test H assertion checks the bubble root outer div's inline style for the substring `hsla(0, 60%` (Phase 76 red-fill inheritance)
    - Test I confirms filename `&lt;script&gt;` is escaped (React default text-node escaping — no live script element in rendered output)
  </acceptance_criteria>
  <done>ChatMessage has the new prop and the new render branch. Nine unit tests lock the branch's shape, ordering, empty-caption behavior, readOnly requirement, red-fill inheritance, and XSS-escape safety. All existing ChatMessage-family tests stay green. Ready for Plan 02 to wire the seed call that feeds this branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS `upload_ready_to_inject` frame → PrettyView state | Backend-authored file metadata (filename, size, mimetype) crosses into React render. Batch-id gate at use-pretty-view-uploads.ts:378 already filters unowned batches; this phase only adds a render surface for the already-filtered data. |
| Attacker-typed caption / filename → ChatMessage render | Caption comes from Ashley's textarea input; filenames come from backend after sanitizeFilenameForUpload (Phase 05 V5-verified). Both flow into React text nodes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-01 | Tampering / Info Disclosure | ChatMessage attachment-chip filename render | mitigate | Chip renders `{file.name}` inside a `<span>` (React default text-node escaping) — no `dangerouslySetInnerHTML`. Test I asserts a filename containing `<script>` renders as escaped text, no live script element. |
| T-81-02 | Tampering | ChatMessage caption render for pending-with-attachments branch | mitigate | Caption renders inside a `<div className="pv-injected-caption whitespace-pre-wrap mb-2">{content}</div>` — React default text-node escaping. No `dangerouslySetInnerHTML`. Mirrors settled-injected branch's identical treatment (Phase 05 V5-verified). |
| T-81-03 | Spoofing | Cross-batch id confusion in seed record | accept | The `PendingSend.attachments` field is populated in Plan 02 from `event.files` inside `onUploadReadyToInject`. The batchId gate at use-pretty-view-uploads.ts:378 already prevents cross-batch spoofing before the callback fires. This plan (types + render only) has no attack surface for this threat. |
| T-81-04 | DoS | Unbounded attachment count blows up React render | accept | Attachment count is bounded upstream by user's compose-side staging capacity (Phase 05 chip-strip cap). No pathological case introduced by this phase. |
</threat_model>

<verification>
- `npx tsc -p tsconfig.json --noEmit` exits 0 across the widened files
- `npx vitest run src/ui/features/pretty-view/ChatMessage.test.tsx src/ui/features/pretty-view/ChatMessage.autoplay.test.tsx src/ui/features/pretty-view/ChatMessage.speak.test.tsx src/ui/features/pretty-view/ChatMessage.editable-file.test.tsx src/ui/features/pretty-view/ChatMessage.instrumentation.test.tsx src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx src/ui/features/pretty-view/ComposeBox.test.tsx` — all green
- No changes to `src/ui/features/pretty-view/AttachmentChipStrip.tsx` (verify with `git diff --name-only src/ui/features/pretty-view/AttachmentChipStrip.tsx` returning empty)
- No changes to `src/ui/features/pretty-view/use-pretty-view-uploads.ts` (verify with `git diff --name-only src/ui/features/pretty-view/use-pretty-view-uploads.ts` returning empty)
</verification>

<success_criteria>
- PendingSend, handleOptimisticSend, onOptimisticSend widened backward-compatibly (optional attachments field, exact shape `{filename,size,mimetype}[]`)
- ChatMessage accepts optional attachments prop and renders the new pending-with-attachments branch when set (with correct ordering: after injected branch, before ReactMarkdown fallback)
- Whole-bubble red inheritance from Phase 76 D-06 works for the new branch without any style edits (unit test H asserts this)
- Empty caption + attachments renders chip-strip-only (D-06 unit test B asserts)
- Chips render readOnly with `data-readonly="true"` (Pitfall #6 unit test C asserts)
- XSS-safe filename render (T-81-01 unit test I asserts)
- All existing ChatMessage-family + optimistic-bubbles + ComposeBox tests stay green (zero regression)
- Diagnostic log line at handleOptimisticSend gains `attachmentCount=...` marker
- `use-pretty-view-uploads.ts` and `AttachmentChipStrip.tsx` are completely untouched
</success_criteria>

<output>
Create `.planning/phases/81-optimistic-bubble-for-every-compose-box-send/81-01-SUMMARY.md` when done. Include:
- Files modified with concrete line-range citations
- Test additions (which describe block, how many tests, all passing)
- Explicit note that no runtime behavior changed — Plan 02 lights up the new branch by wiring the seed call
- Any pitfalls encountered and how resolved (Pitfall #4 caption-delimiter defense, Pitfall #6 readOnly)
</output>
