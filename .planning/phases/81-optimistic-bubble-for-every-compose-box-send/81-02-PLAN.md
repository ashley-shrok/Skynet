---
phase: 81-optimistic-bubble-for-every-compose-box-send
plan: 02
type: execute
wave: 2
depends_on:
  - 81-01
files_modified:
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx
  - src/ui/features/pretty-view/PrettyView.compose-send.test.tsx
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-10
  - D-11
  - D-12
  - D-13
  - D-17
  - D-18
user_setup: []

must_haves:
  truths:
    - "Every compose-box send that produces an attachment message also produces a pending bubble (primary Send, queue-slot Send, cadence auto-fire, voice-into-slot — all four attachment triggers converge through the seed call)"
    - "The pending attachment bubble seeds AT upload_ready_to_inject time, NOT at Send press (D-02) — compose chips continue to own the upload phase unchanged"
    - "The pending attachment bubble's mqid equals the messageQueueItemId from the upload_ready_to_inject event (== batchId from startBatch); no fresh pv-optim-* id is minted (RESEARCH.md Pitfall #2)"
    - "Superseded batches never seed a pending bubble (D-13) — batchId + readyFiredRef gates in the uploads hook already prevent this; verified by regression test"
    - "Upload failures (server-rejected, network drop, WS-not-open at startBatch, upload_failed event) never seed a pending bubble (D-12) — verified by regression test"
    - "The pending attachment bubble is cleared by FIFO head-match on the harness's user-role echo (D-10) — same mechanism as text-only pending bubbles at PrettyView.tsx L1900"
    - "Voice-submit + attachments (both primary path and queue-slot path) produces a pending bubble via the same seed call (D-17 covers voice-primary text; voice-slot attachment is covered by Option B convergence)"
    - "Text-only pending bubbles (Phase 50 baseline) continue to work with zero behavior change"
    - "Settled attachment bubbles (Phase 05 baseline) continue to render via parseInjectedUserTurn branch (branch ordering preserved)"
  artifacts:
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "Seed call inside onUploadReadyToInject closure; attachments prop pass-through at pending render site"
      contains: "handleOptimisticSend({"
    - path: "src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx"
      provides: "10 new test cases for attachment pending bubbles inside nested describe block"
      contains: "attachment pending bubbles (Phase 81)"
    - path: "src/ui/features/pretty-view/PrettyView.compose-send.test.tsx"
      provides: "One integration test verifying seed fires from onUploadReadyToInject before sendInput"
      contains: "seed pending bubble"
  key_links:
    - from: "src/ui/features/pretty-view/PrettyView.tsx (onUploadReadyToInject closure at L1477-1486)"
      to: "src/ui/features/pretty-view/PrettyView.tsx (handleOptimisticSend at L1182-1258)"
      via: "direct function call — seed before dispatch"
      pattern: "handleOptimisticSend\\(\\{[^}]*attachments"
    - from: "src/ui/features/pretty-view/PrettyView.tsx (pendingSends.map at L3317-3338)"
      to: "src/ui/features/pretty-view/ChatMessage.tsx (attachments prop from Plan 01)"
      via: "prop drilling"
      pattern: "attachments=\\{p\\.attachments\\}"
    - from: "harness user-role echo frame"
      to: "PrettyView pending bubble cleanup at L1900"
      via: "existing FIFO head-match (unchanged)"
      pattern: "oldestSendingIdx"
---

<objective>
Wire the seed call for attachment pending bubbles. This plan lights up the new pending-with-attachments render branch created in Plan 01 by (a) adding a `handleOptimisticSend` call inside PrettyView's existing `onUploadReadyToInject` closure (RESEARCH.md § Option B), and (b) passing the widened `attachments` field from each PendingSend record through to ChatMessage at the render callsite. All four attachment-carrying trigger sites (primary Send, queue-slot Send, cadence auto-fire, voice-into-slot) converge into this single seed site because they all end in `uploads.startBatch(...)` which resolves via the same WS `upload_ready_to_inject` event that fires the callback.

Purpose: Close the D-18 attachment-branch seed gaps identified in RESEARCH.md § "State-of-Play Correction" (4 sites, not 6 — queue-slot text and cadence text already seed via Phase 68 funnel routing). This is the load-bearing wiring plan.

Output: One new statement in PrettyView (seed call inside onUploadReadyToInject); one new line in the pending render site (attachments prop pass-through); 10 new integration tests in PrettyView.optimistic-bubbles.test.tsx; one integration test in PrettyView.compose-send.test.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/81-optimistic-bubble-for-every-compose-box-send/81-CONTEXT.md
@.planning/phases/81-optimistic-bubble-for-every-compose-box-send/81-RESEARCH.md
@.planning/phases/81-optimistic-bubble-for-every-compose-box-send/81-PATTERNS.md
@.planning/phases/81-optimistic-bubble-for-every-compose-box-send/81-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire seed call inside onUploadReadyToInject + attachments prop pass-through at pending render</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/PrettyView.tsx (L1110-1290 for the widened handleOptimisticSend from Plan 01; L1467-1490 for the existing `onUploadReadyToInject` closure to modify; L1880-1918 for the FIFO head-match cleanup at L1900 that clears the pending bubble on harness echo; L3310-3358 for the pending render `pendingSends.map` at L3317-3338)
    - src/ui/features/pretty-view/use-pretty-view-uploads.ts (L80-120 for BatchOutcome type + L440-475 for the upload_ready_to_inject handler that fires onReadyRef — confirm the event payload shape includes messageQueueItemId, files, caption)
    - src/ui/api/pretty-view-upload-protocol.ts (L115-142 for UploadReadyToInjectFileSummary — confirm files array elements have filename, size, mimetype fields that map 1:1 to the seed's attachments shape)
    - .planning/phases/81-optimistic-bubble-for-every-compose-box-send/81-PATTERNS.md (§ PrettyView.tsx Site D — full pattern block with the exact JSX to insert)
    - .planning/phases/81-optimistic-bubble-for-every-compose-box-send/81-RESEARCH.md (§ "Plumbing Options for Seed-at-Inject-Time" — recommendation is Option B, seed inside existing closure; § Pitfall #2 for mqid invariant; § Pitfall #1 for retry-double-seed defense; § Pitfall #3 for cadence-fire IIFE — Option B avoids it by placing seed on receiving side)
    - .planning/phases/81-optimistic-bubble-for-every-compose-box-send/81-01-SUMMARY.md (confirms Plan 01's landing site — Site A/B/C for the type widening, so this task can spread attachments into the record)
  </read_first>
  <behavior>
    - Inside PrettyView's `onUploadReadyToInject` callback closure (existing site at L1477-1486, wired into `usePrettyViewUploads({ ws, onUploadReadyToInject, getBufferedAmount })`), insert a NEW statement as the FIRST body statement: `handleOptimisticSend({payload: caption, mqid: messageQueueItemId, immediateFailure: false, attachments: files.map(...)})`.
    - Ordering invariant (Phase 50 D-01): seed MUST fire BEFORE `formatInjectedUserTurn` + `onInjectedTurnReady?.(...)`. Rationale: FIFO head-match at L1900 clears pending records when the harness echo returns. If the pending record doesn't exist yet when the echo arrives, the echo appends as a normal user message and the pending bubble never seeds. Seed-before-dispatch is the whole reason Option B was chosen over Option A.
    - mqid invariant (RESEARCH.md Pitfall #2): The mqid passed to handleOptimisticSend MUST equal `messageQueueItemId` from the event payload. This is the same id as the upload batchId. Do NOT mint a fresh `pv-optim-*` id (that pattern is copy-pasted from useComposeSend.send at ComposeBox.tsx:465 — do NOT copy it here). Add a code comment naming this invariant.
    - attachments mapping: map each file in `event.files` to `{filename: f.filename, size: f.size, mimetype: f.mimetype}` (strip landingPath and uploadTimestamp — those are settled-bubble-only fields; the pending bubble's chip render doesn't display them).
    - At the pending render callsite (L3317-3338 `pendingSends.map((p) => ...` returning `<ChatMessage role="user" content={p.content} pendingState={computedPendingState} />`), add ONE new prop pass: `attachments={p.attachments}`. When `p.attachments` is undefined (text-only pending), ChatMessage's Plan-01-added branch condition `attachments && attachments.length > 0` is false — ReactMarkdown fallback fires. Zero regression on text-only pendings.
    - Superseded / upload-failure / WS-not-open paths (D-12, D-13, RESEARCH.md Pitfall #5): the `readyFiredRef` guard at use-pretty-view-uploads.ts:450 short-circuits `onUploadReadyToInject` for these cases — the seed is naturally gated. NO cleanup logic needed. Verified by the regression tests in Task 2.
    - Existing behavior unchanged: `formatInjectedUserTurn`, `onInjectedTurnReady`, `uploads.resetBatch()` all continue to fire in their existing order after the seed call.

    Test expectations (validated by Task 2):
    - Seed fires exactly once per successful `upload_ready_to_inject` event.
    - Seed carries the correct mqid (== messageQueueItemId).
    - Seed carries the correct attachment metadata array.
    - Superseded batch never fires seed.
    - Upload-failed / WS-not-open batches never fire seed.
    - FIFO head-match clears the pending attachment bubble on harness echo.
  </behavior>
  <action>
    Modify PrettyView.tsx's existing `onUploadReadyToInject` closure to seed the pending bubble before dispatching the injected turn.

    Locate the closure at L1477-1486 — the one passed to `usePrettyViewUploads({ ws: uploadWs, onUploadReadyToInject: ({messageQueueItemId, files, caption}) => {...}, getBufferedAmount: ... })`. Insert the seed call as the FIRST statement inside the callback body, BEFORE the existing `formatInjectedUserTurn` line.

    Seed call shape (per PATTERNS.md § Site D and RESEARCH.md § Option B recommendation):
    - Call `handleOptimisticSend` with args: payload = caption, mqid = messageQueueItemId, immediateFailure = false, attachments = files.map to the `{filename, size, mimetype}` triple.
    - Add a code comment on the line above the call: `// Phase 81 D-02: seed pending bubble at upload_ready_to_inject time (post-upload, pre-echo). mqid === messageQueueItemId === batchId — RESEARCH.md Pitfall #2. Seed MUST precede formatInjectedUserTurn + onInjectedTurnReady so FIFO head-match at L1900 finds the pending record when the harness echo returns (Phase 50 D-01).`

    Do NOT modify `formatInjectedUserTurn`, `onInjectedTurnReady?.(...)`, or `uploads.resetBatch()` — those existing statements stay verbatim, in their existing order, after the new seed call.

    At the pending render callsite (L3317-3338), the existing `pendingSends.map` builds a `<ChatMessage role="user" content={p.content} pendingState={computedPendingState} />`. Add ONE new prop line inside the JSX element: `attachments={p.attachments}`. Preserve indentation and order-of-props consistent with the surrounding style. When `p.attachments` is undefined (text-only pendings), the prop passes `undefined` — ChatMessage's Plan-01-added branch condition safely returns false and the existing render path fires.

    Do NOT touch use-pretty-view-uploads.ts (RESEARCH.md § Option B locks the hook as unchanged).
    Do NOT touch ComposeBox.tsx beyond Plan 01's type widening (RESEARCH.md § Option B locks ComposeBox as unaware of the seed).
    Do NOT touch any queue-slot or cadence branches in ComposeBox (RESEARCH.md § "Queue-Slot Text Sends — Already Handled" — they route through funnel; adding a seed there would produce a duplicate).
    Do NOT add a new prop/callback to ComposeBoxProps or PrettyViewProps — the seed lives inside PrettyView's local closure.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tabitha &amp;&amp; npx tsc -p tsconfig.json --noEmit 2>&amp;1 | tail -20</automated>
    <automated>cd /home/ubuntu/skynet-tabitha &amp;&amp; npx vitest run src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx src/ui/features/pretty-view/PrettyView.compose-send.test.tsx src/ui/features/pretty-view/ComposeBox.test.tsx src/ui/features/pretty-view/ComposeBox.queued-attachment.test.tsx src/ui/features/pretty-view/use-pretty-view-uploads.test.ts src/ui/features/pretty-view/ChatMessage.test.tsx 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Phase 81 D-02" src/ui/features/pretty-view/PrettyView.tsx` returns 1 match (invariant comment above seed call)
    - `grep -nE "handleOptimisticSend\(\{[^)]*attachments" src/ui/features/pretty-view/PrettyView.tsx` returns at least 1 match inside onUploadReadyToInject closure
    - `grep -n "attachments={p.attachments}" src/ui/features/pretty-view/PrettyView.tsx` returns exactly 1 match (the render pass-through)
    - `grep -c "formatInjectedUserTurn" src/ui/features/pretty-view/PrettyView.tsx | grep -v '^#'` returns unchanged from pre-plan count (existing statements preserved)
    - `git diff --name-only src/ui/features/pretty-view/use-pretty-view-uploads.ts` returns empty (hook untouched)
    - `git diff --name-only src/ui/features/pretty-view/AttachmentChipStrip.tsx` returns empty (component untouched)
    - Verify (manual grep) that the seed call precedes formatInjectedUserTurn line-order-wise inside the closure: `awk '/onUploadReadyToInject:/,/getBufferedAmount:/' src/ui/features/pretty-view/PrettyView.tsx | grep -n -E "handleOptimisticSend|formatInjectedUserTurn"` — handleOptimisticSend appears BEFORE formatInjectedUserTurn
    - `npx tsc -p tsconfig.json --noEmit` exits 0
    - All existing tests in the SCOPED test set stay green (regression check)
    - ComposeBox.tsx has NO new logic changes (only Plan 01's type widening at L181-192)
  </acceptance_criteria>
  <done>Seed call wired inside PrettyView's onUploadReadyToInject closure with the correct mqid invariant + ordering. attachments prop passes through at the pending render callsite. All existing tests still green. Ready for Task 2 to add the new integration tests that verify the wiring end-to-end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests — 10 new cases in PrettyView.optimistic-bubbles.test.tsx + 1 in PrettyView.compose-send.test.tsx</name>
  <files>src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx, src/ui/features/pretty-view/PrettyView.compose-send.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx (L1-210 for the mount factory + WS stub + typeAndEnter + flipToStreaming + countPendingBubbles helpers; L188-207 for Test 1 as the exact seed+render pattern to mirror; the last existing test block (near L1000+) to see where to append the new describe block)
    - src/ui/features/pretty-view/PrettyView.compose-send.test.tsx (L241-322 for Test 3 handleInjectedTurnReady two-event pattern — this is the analog for the new integration test)
    - src/ui/features/pretty-view/use-pretty-view-uploads.ts (L440-475 for the upload_ready_to_inject event handler — confirms the event payload shape you'll inject via the WS stub in tests)
    - src/ui/api/pretty-view-upload-protocol.ts (L115-142 for UploadReadyToInjectFileSummary — the shape of `event.files[i]` you'll construct in test fixtures)
    - .planning/phases/81-optimistic-bubble-for-every-compose-box-send/81-PATTERNS.md (§ PrettyView.optimistic-bubbles.test.tsx and § PrettyView.compose-send.test.tsx — describe block outline + trigger mechanism note)
    - .planning/phases/81-optimistic-bubble-for-every-compose-box-send/81-RESEARCH.md (§ "Suggested new test cases for PrettyView.optimistic-bubbles.test.tsx" — the 10-item list; § "Test File Layout" — one-integration-test rationale)
  </read_first>
  <behavior>
    - New describe block appended at end of PrettyView.optimistic-bubbles.test.tsx: `describe("PrettyView — attachment pending bubbles (Phase 81)", () => { ... })`. Reuse the existing beforeEach/afterEach (or duplicate them if nested-describe scoping requires). Reuse mount factory, WS stub, countPendingBubbles.
    - Test trigger mechanism: to fire an `upload_ready_to_inject` event at the mounted PrettyView, use the existing WS stub — invoke `ws.onmessage?.({ data: JSON.stringify({type: "upload_ready_to_inject", messageQueueItemId: "batch-abc", files: [{filename: "a.txt", size: 42, mimetype: "text/plain", landingPath: "/home/u/pretty-view-uploads/2026-09-07/000000-a.txt", uploadTimestamp: "2026-09-07T00:00:00Z"}], caption: "here you go"}) })`. Ashley's typing / Send-press is NOT required — the seed fires when the event arrives, regardless of trigger.
    - Ten test cases (one per must-have concern per RESEARCH.md § "Suggested new test cases"):
      - Test A1: `upload_ready_to_inject event with caption "hi" and one file → pending bubble seeds with data-event-id="pending-batch-abc", contains "hi" text, contains a chip with filename "a.txt"` (D-02, D-11)
      - Test A2: `upload_ready_to_inject event with empty caption + files → pending bubble contains chip strip only, no .pv-injected-caption div` (D-06)
      - Test A3: `pending attachment bubble → chips render with data-readonly="true" attribute on the strip` (Pitfall #6, D-07)
      - Test A4: `pending attachment bubble → outer bubble div inline style contains "hsla(0, 60%" AFTER the 20-second timer flips state to failed` (Phase 76 D-06 / D-08 inheritance; use fake timers + vi.advanceTimersByTime)
      - Test A5: `superseded batch (fire upload_ready_to_inject for batch-A followed by upload_start / upload_ready_to_inject for batch-B where batch-A is superseded by resetBatch) → only batch-B's pending bubble is present in the DOM, batch-A never appears` (D-13 regression) — NOTE: since Option B's seed is inside the callback and the callback is gated by readyFiredRef, the test can simply verify that a "superseded" outcome never triggers the callback. Simulate by never firing the ready event for batch-A while firing it for batch-B.
      - Test A6: `simulate upload_failed WS frame → NO pending bubble ever seeds` (D-12 regression)
      - Test A7: `simulate ws=null then call onSendWithAttachments via the PrettyView instance → uploads.startBatch would fail with ws_not_open outcome and NO upload_ready_to_inject event fires → NO pending bubble seeds` (D-12 regression)
      - Test A8: `after upload_ready_to_inject fires → simulate incoming user-role message frame with parsed.role === "user" → FIFO head-match clears the pending attachment bubble` (D-10 — mirrors Test 1's cleanup verification but for the attachment path)
      - Test A9: `assert the pending bubble's data-event-id equals "pending-" + messageQueueItemId (not "pending-pv-optim-*")` — locks Pitfall #2 mqid invariant
      - Test A10: `caption containing literal string "---attached files---" (without valid file lines below it) → pending bubble renders as pending-with-attachments branch, NOT as settled injected branch` (Pitfall #4 defense)
    - One integration test appended after Test 3 in PrettyView.compose-send.test.tsx: `Test 4: handleInjectedTurnReady flow ALSO seeds a pending bubble via handleOptimisticSend before dispatching WS write frames`. Setup: mount PrettyView, drive the injection via the same ref/hook path Test 3 uses, then assert BOTH (a) the pending bubble appears in the DOM immediately (before microtask flush) AND (b) the WS write frames dispatch afterward. Order-of-operations assertion: use vi.spyOn / an order tracker to verify seed-then-WS.
  </behavior>
  <action>
    Append the new integration tests. This task assumes Task 1 has landed the seed wiring — the tests validate that wiring end-to-end via WS-frame injection at the mounted PrettyView.

    File 1: `src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx`
    - Append a new describe block at end-of-file: `describe("PrettyView — attachment pending bubbles (Phase 81)", () => { ... })`.
    - Reuse existing beforeEach setup (WS stub, mount factory). If needed, duplicate the beforeEach into the new describe scope — jsdom cleanup between nested tests matters.
    - Add ten it-blocks: Test A1 through Test A10 per behavior list. Use existing helpers: `mount`, `getCurrentWs`, `flipToStreaming`, `countPendingBubbles`. Add a NEW helper if needed: `fireUploadReadyToInject(ws, {messageQueueItemId, files, caption})` that wraps the `ws.onmessage?.({data: JSON.stringify({type: "upload_ready_to_inject", ...})})` call.
    - For Test A4 (timer flip to failed): follow the existing test-file's fake-timers pattern (search for `vi.useFakeTimers` or `advanceTimersByTime` in the file for the exact incantation).
    - For Test A8 (FIFO cleanup): fire the upload_ready_to_inject event first, then fire an incoming user-role message via the same WS stub with `{type: "message", role: "user", content: "some echoed body"}` (or the exact frame shape the existing Test 1 uses for its user-role cleanup — mirror it).

    File 2: `src/ui/features/pretty-view/PrettyView.compose-send.test.tsx`
    - Insert a new test after Test 3: `it("Test 4: onUploadReadyToInject seeds pending bubble BEFORE dispatching WS write frames", async () => { ... })`.
    - Mount PrettyView with the same setup Test 3 uses. Instead of using the sendInputRef directly, fire an `upload_ready_to_inject` WS frame at the mount's WS stub. Assert order:
      1. Query DOM for `[data-event-id^="pending-"]` — should be present immediately after the event fires (before awaiting any Promise or timer).
      2. Then verify WS write frames were dispatched (via the existing WS stub's write-capture).
    - The assertion order matters: pending-bubble-first, WS-writes-second. Use `getAllWrittenFrames()` (or the file's equivalent) BEFORE and AFTER the event to capture the delta.

    Do NOT modify any production code in this task. If a test surfaces a defect in Task 1's wiring, fix it in Task 1 (re-run Task 1's acceptance criteria) rather than accommodating the defect in the test.

    Do NOT add tests to `ComposeBox.test.tsx` or `ComposeBox.queued-attachment.test.tsx` — RESEARCH.md § "Test File Layout" locks those as unchanged under Option B (ComposeBox is unaware of the seed).
    Do NOT add tests to `use-pretty-view-uploads.test.ts` — the hook is unchanged.
    Do NOT create a sibling `PrettyView.optimistic-bubbles-attachments.test.tsx` — RESEARCH.md § "Test File Layout" recommends inline extension (200+ lines of scaffolding avoided).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tabitha &amp;&amp; npx tsc -p tsconfig.json --noEmit 2>&amp;1 | tail -20</automated>
    <automated>cd /home/ubuntu/skynet-tabitha &amp;&amp; npx vitest run src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx src/ui/features/pretty-view/PrettyView.compose-send.test.tsx 2>&amp;1 | tail -60</automated>
    <automated>cd /home/ubuntu/skynet-tabitha &amp;&amp; npx vitest run src/ui/features/pretty-view/ 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'describe("PrettyView — attachment pending bubbles (Phase 81)"' src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx` returns 1
    - `grep -cE '^  it\("Test A[0-9]+:' src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx` returns exactly 10 (indent may vary — use `grep -cE 'it\("Test A[0-9]+:' | grep -v '^#'` if leading-whitespace grep fails; adjust regex to match the file's actual indentation)
    - `grep -cE 'it\("Test 4: onUploadReadyToInject seeds pending bubble' src/ui/features/pretty-view/PrettyView.compose-send.test.tsx` returns 1
    - `npx vitest run src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx` — ALL tests pass including the 10 new ones AND all existing text-only tests (regression check)
    - `npx vitest run src/ui/features/pretty-view/PrettyView.compose-send.test.tsx` — ALL tests pass including Test 4 AND all existing tests (regression check)
    - `npx vitest run src/ui/features/pretty-view/` — entire pretty-view test suite green (regression check across all sibling tests: ChatMessage.*, ComposeBox.*, AttachmentChipStrip.*, use-pretty-view-uploads.*, PrettyView.*)
    - Test A9 explicitly asserts the DOM attribute pattern `pending-batch-*` (or whatever id shape you feed into the event) — NOT `pending-pv-optim-*`. Failure mode: if this test passes with a `pv-optim-*` id, Task 1's wiring has the mqid invariant wrong and needs a fix.
    - Test 4 in compose-send test file asserts SEED-BEFORE-WS ordering — not just presence
    - No sibling test file created (`ls src/ui/features/pretty-view/PrettyView.optimistic-bubbles-attachments.test.tsx` returns "No such file")
    - No changes to ComposeBox.test.tsx, ComposeBox.queued-attachment.test.tsx, use-pretty-view-uploads.test.ts, AttachmentChipStrip.test.tsx (git diff shows zero touches to these files)
  </acceptance_criteria>
  <done>Ten new tests in PrettyView.optimistic-bubbles.test.tsx locking the attachment seed + render + FIFO cleanup + failure regressions. One new integration test in PrettyView.compose-send.test.tsx locking the seed-before-WS ordering. Entire pretty-view test suite green. Phase 81 executor-scope complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS `upload_ready_to_inject` frame → PrettyView.onUploadReadyToInject closure | Backend-authored batch-completion event. Batch-id gate at use-pretty-view-uploads.ts:378 (`event.messageQueueItemId !== ourBatch → drop`) already filters unowned batches — the callback fires only for batches this client started. Seed inherits the same filter. |
| WS user-role echo frame → PrettyView.FIFO-head-match cleanup at L1900 | Existing trust boundary; unchanged by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-05 | Spoofing | Attacker sends fake `upload_ready_to_inject` with attacker-chosen mqid | mitigate | Existing batchId gate at use-pretty-view-uploads.ts:378 rejects events where messageQueueItemId doesn't match the client's own startBatch mint. Seed only fires from inside the callback, which is gated. No new attack surface — unchanged from pre-Phase-80. |
| T-81-06 | Repudiation | User cannot tell if their attachment send seeded correctly (log correlation) | mitigate | Plan 01 Task 1 adds `attachmentCount=${attachments?.length ?? 0}` marker to the `[diag-dormant-send] arm` log line. Post-ship grep can distinguish attachment seeds from text-only seeds by scanning for attachmentCount != 0. |
| T-81-07 | Info Disclosure | Client-side pending record leak across session rotations | accept | Existing `clearAllPendingSends("session-rotation")` at PrettyView.tsx:1880 fires on session_changed WS frame — attachment pending records clear along with text pending records because they use the same array. No new leak vector. |
| T-81-08 | Tampering | Retry-double-seed (RESEARCH.md Pitfall #1) — same batchId fires upload_ready_to_inject twice | mitigate | Existing `readyFiredRef` guard at use-pretty-view-uploads.ts:450 prevents double-fire per batchId lifecycle. Retry with `reuseIdOnRetry=true` resets the flag at L683 — one-fire-per-successful-attempt semantics preserved. Test A5 covers superseded case; Test A6/A7 covers upload-failure cases. |
| T-81-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase. |
</threat_model>

<verification>
- `npx tsc -p tsconfig.json --noEmit` exits 0 across the touched files
- `npx vitest run src/ui/features/pretty-view/` — ENTIRE pretty-view test suite green (regression check across all sibling tests)
- Every one of D-01, D-02, D-03, D-04, D-10, D-11, D-12, D-13, D-17 has at least one asserting test in Task 2's additions
- `git diff --stat src/ui/features/pretty-view/use-pretty-view-uploads.ts` returns empty (hook unchanged)
- `git diff --stat src/ui/features/pretty-view/ComposeBox.tsx` shows only Plan 01's type-widening edits — NO logic changes to funnel or handleSend branches
- `git diff --stat src/ui/features/pretty-view/AttachmentChipStrip.tsx` returns empty (component unchanged)
</verification>

<success_criteria>
- Seed call fires inside onUploadReadyToInject BEFORE formatInjectedUserTurn (verifiable by line-order grep in acceptance criteria)
- Seed uses messageQueueItemId as mqid (Pitfall #2 invariant) — Test A9 locks this
- attachments prop passes through at pending render callsite (verifiable by grep)
- 10 new tests in optimistic-bubbles.test.tsx cover seed + render + empty-caption + readOnly + red-fill + superseded + upload-failed + ws-not-open + FIFO cleanup + Pitfall-#4 defense
- 1 new integration test in compose-send.test.tsx locks seed-before-WS ordering
- Zero regression across the pretty-view test surface (text-only pending, settled attachment bubbles, ComposeBox tests, use-pretty-view-uploads tests, ChatMessage-family tests all green)
- use-pretty-view-uploads.ts, ComposeBox.tsx (beyond Plan 01 type widening), AttachmentChipStrip.tsx are unchanged
</success_criteria>

<output>
Create `.planning/phases/81-optimistic-bubble-for-every-compose-box-send/81-02-SUMMARY.md` when done. Include:
- Files modified with concrete line-range citations (especially the seed insertion location inside onUploadReadyToInject)
- Test additions (10 in optimistic-bubbles.test.tsx + 1 in compose-send.test.tsx, all passing)
- Confirmation that use-pretty-view-uploads.ts is byte-unchanged (git diff empty)
- Confirmation that ComposeBox.tsx has NO logic changes (only Plan 01's type widening)
- Coverage table: which decision (D-01 through D-17) is covered by which test
- Any pitfall encountered during wiring or test authoring
</output>
