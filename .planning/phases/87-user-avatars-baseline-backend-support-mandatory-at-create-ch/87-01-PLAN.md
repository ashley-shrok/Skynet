---
phase: 85-user-avatars-baseline-backend-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/database/db/schema.ts
  - src/backend/database/db/index.ts
  - src/backend/database/db/index.migration.test.ts
autonomous: true
requirements: [D-04, D-05, D-06, D-13, D-17, D-18]
tags: [phase-87, user-avatars, schema, migration]

must_haves:
  truths:
    - "D-04: users table has a new nullable text column carrying a pointer to the on-disk avatar file (NOT bytes, NOT absolute path, NOT external URL)"
    - "D-05: the pointer choice is deterministic — given a row, the file location under DATA_DIR is unambiguous"
    - "D-06: column is nullable at the schema level so pre-existing users remain valid"
    - "D-13: no backfill — existing user rows keep null pointer until a downstream mechanism sets one"
    - "D-17/D-18: the addColumnIfNotExists call is paired with DatabaseSaveTrigger.forceSave using a labeled reason 'phase-87-user-avatar-schema'"
  artifacts:
    - path: "src/backend/database/db/schema.ts"
      provides: "Drizzle avatarPath field on users table"
      contains: "avatarPath: text(\"avatar_path\")"
    - path: "src/backend/database/db/index.ts"
      provides: "boot-time addColumnIfNotExists('users', 'avatar_path', 'TEXT') + labeled forceSave"
      contains: "addColumnIfNotExists(\"users\", \"avatar_path\", \"TEXT\")"
    - path: "src/backend/database/db/index.migration.test.ts"
      provides: "Phase 87 mxid-style migration test proving avatar_path is idempotently added and queryable"
      contains: "Phase 87"
  key_links:
    - from: "src/backend/database/db/schema.ts"
      to: "src/backend/database/db/index.ts"
      via: "column name string 'avatar_path' must match between Drizzle mirror and migration DDL"
      pattern: "avatar_path"
---

<objective>
Add the users.avatar_path column (D-04) via the Skynet-standard addColumnIfNotExists mechanism (D-06 nullable, D-05 deterministic, D-13 no backfill), pair the migration with DatabaseSaveTrigger.forceSave (D-17/D-18), and ship a mirrored-from-Phase-75 migration test proving idempotency and queryability.

Purpose: Every downstream plan depends on this column existing. Everything that reads or writes an avatar pointer needs the row shape in place first.

Output: Drizzle mirror at schema.ts, boot-time migration line at db/index.ts, migration test at db/index.migration.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/85-user-avatars-baseline-backend-support-mandatory-at-create-ch/87-CONTEXT.md
@.planning/phases/85-user-avatars-baseline-backend-support-mandatory-at-create-ch/87-RESEARCH.md
@.planning/phases/85-user-avatars-baseline-backend-support-mandatory-at-create-ch/87-PATTERNS.md
@src/backend/database/db/schema.ts
@src/backend/database/db/index.ts
@src/backend/database/db/index.migration.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add avatarPath column to Drizzle users schema (D-04, D-05, D-06)</name>
  <files>src/backend/database/db/schema.ts</files>
  <read_first>
    - src/backend/database/db/schema.ts (full file — the target and self-analog for column shape at lines 27-32 where Phase 75 mxid lives)
    - .planning/phases/85-user-avatars-baseline-backend-support-mandatory-at-create-ch/87-PATTERNS.md § "src/backend/database/db/schema.ts — add avatarPath column" (verbatim comment block + column pattern)
    - .planning/phases/85-user-avatars-baseline-backend-support-mandatory-at-create-ch/87-RESEARCH.md § 3 "Existing users row conventions" (rationale for genuine-null vs empty-string sentinel)
  </read_first>
  <behavior>
    - Test 1 (existing tests still pass): running the existing migration test suite does not regress after the schema mirror is added
    - Test 2 (type-shape): TypeScript compilation of the file succeeds and `users.avatarPath` is typed as `string | null`
    - Test 3 (naming): the on-disk snake_case column name in the Drizzle call is exactly `avatar_path` (matches the DDL in Task 2)
  </behavior>
  <action>
    Add the new column as the last field of the `users` table definition (immediately after the Phase 75 `mxid: text("mxid")` line at 32, inside the closing `});` at line 33). Use exactly:
    - Drizzle field name (camelCase): `avatarPath`
    - Drizzle argument (snake_case on-disk column name): `"avatar_path"`
    - Type: `text(...)` with NO `.notNull()` chain (nullable per D-06)
    - No default clause (do NOT copy the OIDC empty-string sentinel pattern from lines 12-19 per RESEARCH.md § 3 — the avatar column must be genuinely NULL when absent)

    Precede the field with a 6-line comment block matching the Phase 75 mxid style (lines 27-31). The block MUST cite:
    1. Phase 87 + locked decision D-04
    2. That the value is a pointer under `${DATA_DIR}/user-avatars/`, NOT bytes, NOT absolute path, NOT external URL (D-01/D-04)
    3. That the pointer must be resolvable from DATA_DIR alone (D-05)
    4. That nullability is intentional: pre-existing rows stay null (D-13 defers backfill)
    5. That mandatoriness for new rows is enforced at POST /users/create per D-07, not the schema (D-06)
    6. That the endpoint that mutates it is `PUT /users/:id/avatar` (D-10 — populated in Plan 04)

    Do not touch any other line of the file. Do not reorder existing columns.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina && npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "schema\.ts|error TS" | head -20; grep -n 'avatarPath: text("avatar_path")' src/backend/database/db/schema.ts</automated>
  </verify>
  <done>
    - `grep -n 'avatarPath: text("avatar_path")' src/backend/database/db/schema.ts` prints exactly one hit inside the users table def
    - `grep -n -B6 'avatarPath: text' src/backend/database/db/schema.ts` shows a 6-line comment block citing D-04, D-05, D-06, D-13 above the field
    - `npx tsc --noEmit` shows zero new errors relative to a pre-edit run in the schema.ts file
    - The field is the LAST field before the closing `});` of the users table def (grep -A2 shows `});` immediately after)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add addColumnIfNotExists migration + labeled forceSave for avatar_path (D-04, D-17, D-18)</name>
  <files>src/backend/database/db/index.ts</files>
  <read_first>
    - src/backend/database/db/index.ts lines 668-693 (addColumnIfNotExists helper definition — already in place, do not modify)
    - src/backend/database/db/index.ts lines 889-922 (Phase 75 mxid migration + forceSave block — the verbatim template)
    - src/backend/database/db/index.ts lines 150-168 (CREATE TABLE users legacy shape — do NOT edit this per RESEARCH.md § 2)
    - .planning/phases/85-user-avatars-baseline-backend-support-mandatory-at-create-ch/87-PATTERNS.md § "src/backend/database/db/index.ts — add addColumnIfNotExists + forceSave"
  </read_first>
  <behavior>
    - Test 1: server boot idempotently adds the `avatar_path` TEXT column to users on first boot AND on subsequent boots (no ALTER-TABLE-already-exists throw)
    - Test 2: after the addColumnIfNotExists call, `DatabaseSaveTrigger.forceSave("phase-87-user-avatar-schema")` is invoked (grep-able label per D-17)
    - Test 3: forceSave failure surfaces as a `databaseLogger.warn` with the labeled reason and does NOT crash boot (mirrors Phase 75 line 911-922)
  </behavior>
  <action>
    Insert a new migration block IMMEDIATELY AFTER the Phase 75 mxid block ending around line 922 (find the closing `}` of the mxid `try/catch (saveError) { … }` block). Follow the exact shape of `.planning/phases/85-user-avatars-baseline-backend-support-mandatory-at-create-ch/87-PATTERNS.md § "src/backend/database/db/index.ts — add addColumnIfNotExists + forceSave"`.

    Content:
    1. A 5-6 line comment block citing Phase 87, locked decision D-04, purpose (pointer to on-disk avatar under DATA_DIR/user-avatars per D-01/D-02), nullability rationale (D-06/D-13 backfill defer), and pointer to the Drizzle mirror at `schema.ts users.avatarPath`.
    2. The call: `addColumnIfNotExists("users", "avatar_path", "TEXT");` (verbatim — exact string, exact quoting, exact snake_case column name matching Task 1's Drizzle arg).
    3. A labeled forceSave block mirroring lines 911-922:
       - `await DatabaseSaveTrigger.forceSave("phase-87-user-avatar-schema")` inside a try
       - catch block that logs via `databaseLogger.warn` with:
         - message referencing "[phase-87] forceSave failed post-schema (non-fatal — addColumnIfNotExists is idempotent, next boot retries)"
         - context object with `operation: "schema_migration_force_save_post_add"`, `reason: "phase-87-user-avatar-schema"`, `error: saveError`

    Do NOT edit the CREATE TABLE IF NOT EXISTS users statement at lines 150-168 — new columns land ONLY via addColumnIfNotExists per RESEARCH.md § 2.

    Do NOT reuse the existing Phase 75 forceSave label — the Phase 87 block gets its own labeled forceSave for grep-ability per D-17.
  </action>
  <verify>
    <automated>grep -n 'addColumnIfNotExists("users", "avatar_path", "TEXT")' src/backend/database/db/index.ts; grep -n 'phase-87-user-avatar-schema' src/backend/database/db/index.ts; grep -n 'phase_85_user_avatar' src/backend/database/db/index.ts; cd /home/ubuntu/skynet-tina && npx tsc --noEmit 2>&amp;1 | grep -E "db/index\.ts|error TS" | head -20</automated>
  </verify>
  <done>
    - `grep -c 'addColumnIfNotExists("users", "avatar_path", "TEXT")' src/backend/database/db/index.ts` returns exactly 1
    - `grep -c '"phase-87-user-avatar-schema"' src/backend/database/db/index.ts` returns exactly 2 (once inside forceSave, once inside the warn context.reason)
    - `grep -n 'operation: "schema_migration_force_save_post_add"' src/backend/database/db/index.ts` shows the warn context matches Phase 75 shape
    - `grep -B1 'addColumnIfNotExists("users", "avatar_path"' src/backend/database/db/index.ts` shows the new block comes AFTER (higher line number than) the mxid block
    - `npx tsc --noEmit` reports zero new errors in db/index.ts
    - The CREATE TABLE IF NOT EXISTS users at lines 150-168 is UNCHANGED (`git diff src/backend/database/db/index.ts` shows no diff in that range)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Phase 87 migration test mirroring Phase 75-2 mxid test</name>
  <files>src/backend/database/db/index.migration.test.ts</files>
  <read_first>
    - src/backend/database/db/index.migration.test.ts lines 466-483 (local `addColumnIfNotExistsMxid` reproduction — copy verbatim, rename to `addColumnIfNotExistsAvatarPath`)
    - src/backend/database/db/index.migration.test.ts lines 486-506 (USERS_CREATE_SQL_PRE_MXID fixture — add a sibling `USERS_CREATE_SQL_PRE_AVATAR_PATH` that INCLUDES `mxid TEXT` since Phase 75 has already shipped by Phase 87)
    - src/backend/database/db/index.migration.test.ts lines 523-591 (Phase 75-2 mxid describe/it block — the template to swap `mxid` → `avatar_path`)
    - .planning/phases/85-user-avatars-baseline-backend-support-mandatory-at-create-ch/87-PATTERNS.md § "src/backend/database/db/index.migration.test.ts — Phase 87 migration test"
  </read_first>
  <behavior>
    - Test 1 (fresh DB): SELECT of avatar_path throws before addColumnIfNotExists runs; after it runs, avatar_path is in the column list
    - Test 2 (idempotency): re-running addColumnIfNotExistsAvatarPath after boot 1 does NOT throw
    - Test 3 (no duplicate): after boot 2, there is exactly one avatar_path column
    - Test 4 (queryable): `SELECT avatar_path FROM users LIMIT 1` executes without throwing after migration
  </behavior>
  <action>
    Add a new `describe("Phase 87-01 migration — users.avatar_path column", () => { ... })` block at the end of the file (after the Phase 75-2 block ending around line 591).

    Content:
    1. A local `addColumnIfNotExistsAvatarPath(db, table, column, definition)` function — verbatim copy of the `addColumnIfNotExistsMxid` at lines 466-481, renamed only.
    2. A `USERS_CREATE_SQL_PRE_AVATAR_PATH` constant modeled on `USERS_CREATE_SQL_PRE_MXID` at lines 486-506, BUT this fixture INCLUDES `mxid TEXT` in the CREATE TABLE columns (since by Phase 87 the Phase 75 mxid migration has already shipped — the "pre" state for Phase 87 is post-Phase-75).
    3. Two `it(...)` blocks mirroring the Phase 75-2 test at lines 566-591:
       - `it("Test P85-1: users.avatar_path TEXT column added exactly once across two boots and is queryable without throwing", ...)`:
         - Create `:memory:` Database
         - Exec `USERS_CREATE_SQL_PRE_AVATAR_PATH`
         - Assert pre-migration `avatar_path` absent from column list
         - Boot 1: call the local helper, assert `avatar_path` present in column list
         - Boot 2: call again, assert no throw
         - Assert exactly one `avatar_path` column (grep-count on `columnNames(db, "users").filter((c) => c === "avatar_path").length === 1`)
         - Assert `db.prepare("SELECT avatar_path FROM users LIMIT 1").get()` does not throw
       - `it("Test P85-2: users.avatar_path is nullable (INSERT without providing value succeeds)", ...)`:
         - Fresh `:memory:` DB
         - Exec fixture + helper
         - Prepare an INSERT that omits avatar_path (mirrors what pre-Phase-85 code would do for a legacy user row)
         - Run it — assert no throw
         - SELECT the row back and assert `avatar_path` column value is `null` (not `""`, not any sentinel — this proves D-06's nullability guarantee)

    Reuse the `columnNames(db, table)` helper already defined at the top of the file (search for `function columnNames`). Do not redefine it locally.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina && npx vitest run src/backend/database/db/index.migration.test.ts -t "Phase 87" 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    - `npx vitest run src/backend/database/db/index.migration.test.ts -t "Phase 87"` reports 2 passing tests
    - `grep -c 'Phase 87-01 migration' src/backend/database/db/index.migration.test.ts` returns 1
    - `grep -c 'addColumnIfNotExistsAvatarPath' src/backend/database/db/index.migration.test.ts` returns at least 3 (definition + 2 calls in the two tests)
    - `grep -c 'USERS_CREATE_SQL_PRE_AVATAR_PATH' src/backend/database/db/index.migration.test.ts` returns at least 3 (const decl + 2 usages)
    - The Phase 75-2 mxid tests still pass: `npx vitest run src/backend/database/db/index.migration.test.ts -t "Phase 75"` shows no regressions
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| boot process → SQLite | in-memory SQLite trusts the DDL passed in via addColumnIfNotExists; no external input at this layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan — Phase 87 § "Package Legitimacy Audit" in RESEARCH.md confirms `not applicable`. All libraries (`drizzle-orm`, `better-sqlite3`) are already in-tree. Legitimacy checkpoint waived (nothing to check). |
| T-85-DB-1 | Denial of Service | boot-time migration | accept | `addColumnIfNotExists` is idempotent (SELECT-probe then ALTER on throw); worst case is a re-run that no-ops. No user-controlled input reaches DDL — the column name and type are hard-coded string literals. Analogous to Phase 75 mxid migration, shipped clean. |
| T-85-DB-2 | Tampering | forceSave label collision | accept | Labels are grep-able strings, not access-control tokens. The `"phase-87-user-avatar-schema"` label is unique across the codebase (verifiable via `grep -r "phase-87-user-avatar-schema"`). Collision would only cause log-line ambiguity, not data corruption. |
</threat_model>

<verification>
- Run: `npx tsc --noEmit` — zero new errors in schema.ts or db/index.ts
- Run: `npx vitest run src/backend/database/db/index.migration.test.ts` — all migration tests pass (Phase 75 tests still green + 2 new Phase 87 tests pass)
- Grep: `grep -n 'avatarPath: text("avatar_path")' src/backend/database/db/schema.ts` returns exactly 1 hit
- Grep: `grep -n 'addColumnIfNotExists("users", "avatar_path", "TEXT")' src/backend/database/db/index.ts` returns exactly 1 hit
- Grep: `grep -c '"phase-87-user-avatar-schema"' src/backend/database/db/index.ts` returns 2 (forceSave call + warn context reason field)
- Grep: `grep -c 'CREATE TABLE IF NOT EXISTS users' src/backend/database/db/index.ts` unchanged from pre-edit (should still be 1, not 2 — did NOT edit the CREATE TABLE)
</verification>

<success_criteria>
- users.avatar_path column exists in the running DB after boot (verifiable via `SELECT sql FROM sqlite_master WHERE name='users'` showing the column)
- Both Phase 87 migration tests pass; Phase 75 tests do not regress
- TypeScript compiles clean with zero new errors in the two edited source files
- Downstream plans (02, 03, 04, 05, 06) can now assume `users.avatarPath` exists in the Drizzle types AND the column exists at runtime
- forceSave label `phase-87-user-avatar-schema` is grep-able across the codebase (unique)
</success_criteria>

<output>
Create `.planning/phases/85-user-avatars-baseline-backend-support-mandatory-at-create-ch/87-01-SUMMARY.md` when done, listing:
- Which files were modified (schema.ts, db/index.ts, index.migration.test.ts)
- The exact column name chosen (`avatar_path` on disk, `avatarPath` in Drizzle)
- The forceSave label used (`phase-87-user-avatar-schema`)
- Test run output confirming both new Phase 87 migration tests pass
- Confirmation that the CREATE TABLE at db/index.ts:150-168 was NOT edited
</output>
