---
phase: 85-user-avatars-baseline-backend-support
plan: 06
type: execute
wave: 4
depends_on: []
files_modified:
  - docker/nginx.conf
  - docker/nginx-https.conf
autonomous: true
requirements: [D-19, D-20, D-21]
tags: [phase-87, user-avatars, nginx, edge-config]

must_haves:
  truths:
    - "D-19: all three new endpoints (create/change/serve avatar) live under /users/… and are already covered by the existing regex block `location ~ ^/users(/.*)?$` — no new location blocks required"
    - "D-20: the existing /users block has NO client_max_body_size directive → nginx inherits its 1 MB compile-time default → 5 MB avatar uploads 413 at nginx BEFORE reaching Express. MUST add `client_max_body_size 6M;` (5 MB payload cap per D-15 + ~1 MB headroom for multipart framing) to the existing /users block."
    - "D-21: BOTH docker/nginx.conf AND docker/nginx-https.conf get the edit — missing the second file is the recurring gotcha per CLAUDE.md § Nginx caveat and RESEARCH.md § Pitfall 5"
    - "The edit sizing (6M) is derived from the /identities/avatar precedent at docker/nginx.conf:286-296 which uses 8M for a 2 MB payload cap — Phase 87's 6M for 5 MB payload leaves proportionally tighter but industry-standard multipart-framing headroom (~20%)"
  artifacts:
    - path: "docker/nginx.conf"
      provides: "existing /users location block extended with client_max_body_size 6M"
      contains: "client_max_body_size 6M"
    - path: "docker/nginx-https.conf"
      provides: "existing /users location block extended with client_max_body_size 6M"
      contains: "client_max_body_size 6M"
  key_links:
    - from: "docker/nginx.conf /users block"
      to: "docker/nginx-https.conf /users block"
      via: "MUST be lockstep — RESEARCH.md § Pitfall 5 and CLAUDE.md § Nginx caveat"
      pattern: "client_max_body_size 6M"
---

<objective>
Add `client_max_body_size 6M;` to the existing `location ~ ^/users(/.*)?$` block in BOTH docker/nginx.conf AND docker/nginx-https.conf (D-20, D-21) so 5 MB avatar uploads don't 413 at the nginx edge before reaching Express.

Purpose: Without this plan, the multer + Express-side upload validation from Plans 02/03/04 is unreachable — nginx will 413 with a plaintext HTML body for any request body >1 MB, and the user will see a broken create form with no useful error signal. This is the second of the two "Skynet-specific traps" the CONTEXT.md flagged (the first being the D-17 save-trigger pairing, mitigated by Plans 01/03/04).

Output: 2 small directive additions, one per file, in the same commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/85-user-avatars-baseline-backend-support-mandatory-at-create-ch/87-CONTEXT.md
@.planning/phases/85-user-avatars-baseline-backend-support-mandatory-at-create-ch/87-RESEARCH.md
@.planning/phases/85-user-avatars-baseline-backend-support-mandatory-at-create-ch/87-PATTERNS.md
@docker/nginx.conf
@docker/nginx-https.conf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add client_max_body_size 6M to /users block in docker/nginx.conf (D-19, D-20)</name>
  <files>docker/nginx.conf</files>
  <read_first>
    - docker/nginx.conf lines 160-180 (find the existing `location ~ ^/users(/.*)?$` block — around line 166-175 per RESEARCH.md § Pitfall 1)
    - docker/nginx.conf lines 280-300 (the /identities/avatar block at 286-296 with `client_max_body_size 8M;` — the sizing precedent to model on)
    - .planning/phases/85-user-avatars-baseline-backend-support-mandatory-at-create-ch/87-PATTERNS.md § "docker/nginx.conf — bump client_max_body_size on /users"
    - .planning/phases/85-user-avatars-baseline-backend-support-mandatory-at-create-ch/87-RESEARCH.md § Pitfall 1 (the 1 MB default trap + why 6M leaves right headroom for 5 MB payload)
  </read_first>
  <behavior>
    - Behavior 1: after edit, the /users location block contains `client_max_body_size 6M;` (with the exact trailing semicolon)
    - Behavior 2: no other directives in the /users block are modified (proxy_pass, proxy_set_header lines all preserved)
    - Behavior 3: no other location block in the file is modified
    - Behavior 4: nginx config file remains syntactically valid (nginx -t would pass; verifiable via a docker exec if available, otherwise a grep for balanced braces)
  </behavior>
  <action>
    Locate the existing `location ~ ^/users(/.*)?$` block in docker/nginx.conf. Per RESEARCH.md § Pitfall 1, it currently reads (around lines 166-175):

    ```
    location ~ ^/users(/.*)?$ {
        proxy_pass http://127.0.0.1:30001;
        proxy_http_version 1.1;
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $proxy_x_forwarded_proto;
        proxy_set_header X-Forwarded-Port $proxy_x_forwarded_port;
        proxy_set_header X-Forwarded-Host $proxy_x_forwarded_host;
    }
    ```

    Add the client_max_body_size directive as the LAST line inside the block, immediately before the closing `}`. Preface with a 3-line comment block anchoring the Phase 87 provenance:

    ```
            # Phase 87 (D-20/D-21) — POST /users/create + PUT /users/:id/avatar
            # carry up to 5 MB avatar payloads (D-15). 6M leaves ~1 MB headroom
            # for multipart framing. See docker/nginx-https.conf for the sister
            # edit (D-21). Without this directive, nginx inherits its 1 MB
            # default and 413s at the edge before Express sees the request.
            client_max_body_size 6M;
    ```

    Preserve exact indentation (matches the surrounding proxy_set_header lines — 8 spaces per the existing file style; verify by reading the surrounding lines first).

    Do NOT touch any other line in the file. Do NOT modify the /identities/avatar block at lines 286-296 (that's the SIZING PRECEDENT, not the edit target — Phase 87 does not change identity-side behavior per CONTEXT.md deferred list).

    Do NOT add a new more-specific location block above the /users regex — CONTEXT.md D-20 explicitly locks preference (a): "add client_max_body_size to the existing /users block". Simpler, one directive, no ordering hazard.
  </action>
  <verify>
    <automated>grep -n 'client_max_body_size 6M' docker/nginx.conf; grep -c 'Phase 87 (D-20/D-21)' docker/nginx.conf; awk '/location ~ \^\/users/,/^        }/{print NR": "$0}' docker/nginx.conf | tail -15</automated>
  </verify>
  <done>
    - `grep -c 'client_max_body_size 6M' docker/nginx.conf` returns exactly 1
    - `grep -c 'Phase 87 (D-20/D-21)' docker/nginx.conf` returns exactly 1 (the comment anchor)
    - The directive appears INSIDE the `location ~ ^/users(/.*)?$ {` block (verified by awk range extraction — the directive line is BETWEEN the opening `{` and closing `}` of the /users block)
    - No other `client_max_body_size` directive in the file is newly added (`git diff docker/nginx.conf | grep -c '^+.*client_max_body_size' == 1`)
    - The /identities/avatar block at line 286-296 is UNCHANGED (`git diff` shows no hits in that line range)
    - `nginx -t` (if docker/nginx container is available: `docker exec skynet-nginx nginx -t`) reports "syntax is ok" — if the container isn't running in the dev environment, verify visually that braces are balanced
  </done>
</task>

<task type="auto">
  <name>Task 2: Add same client_max_body_size 6M to /users block in docker/nginx-https.conf (D-19, D-20, D-21)</name>
  <files>docker/nginx-https.conf</files>
  <read_first>
    - docker/nginx-https.conf lines 170-195 (find the existing /users block — around line 177-186 per RESEARCH.md § Pitfall 1)
    - docker/nginx.conf (Task 1 output — verify the exact edit committed there so this file gets the IDENTICAL directive + comment)
    - CLAUDE.md § Constraints "Nginx caveat" (the recurring gotcha: missing this file causes deploy-time silent failures)
    - .planning/phases/85-user-avatars-baseline-backend-support-mandatory-at-create-ch/87-RESEARCH.md § Pitfall 5 (only-one-file is the recurring failure mode)
  </read_first>
  <behavior>
    - Behavior 1: after edit, the /users location block contains `client_max_body_size 6M;` — EXACT same shape as Task 1
    - Behavior 2: diff between the /users block in nginx.conf vs nginx-https.conf shows only trivial differences (surrounding proxy headers may vary, but the client_max_body_size directive + comment are identical)
    - Behavior 3: no other directive in the file is modified
  </behavior>
  <action>
    Locate the existing `location ~ ^/users(/.*)?$` block in docker/nginx-https.conf (around line 177-186 per RESEARCH.md § Pitfall 1).

    Add the same 6-line comment block + `client_max_body_size 6M;` directive as Task 1, positioned as the LAST line inside the block immediately before the closing `}`.

    Use EXACTLY the same comment text (Phase 87 provenance anchor) and EXACTLY the same directive (`client_max_body_size 6M;`). The two files should differ ONLY in whatever proxy header lines were already different between them pre-edit — the new directive + comment should be byte-identical.

    Do NOT touch any other line in the file.

    After both edits, run a diff comparing the two /users blocks to confirm they are consistent for the phase-87 additions:
    `diff <(awk '/location ~ \^\/users/,/^        }/' docker/nginx.conf) <(awk '/location ~ \^\/users/,/^        }/' docker/nginx-https.conf) | grep -E "client_max_body_size|Phase 87"`
    Expected: EMPTY output (no diff hits) for the Phase 87 additions — pre-existing differences in other directives may still show up but are out of scope.
  </action>
  <verify>
    <automated>grep -n 'client_max_body_size 6M' docker/nginx-https.conf; grep -c 'Phase 87 (D-20/D-21)' docker/nginx-https.conf; diff <(awk '/location ~ \^\/users/,/^        }/' docker/nginx.conf | grep -E "client_max_body_size|Phase 87") <(awk '/location ~ \^\/users/,/^        }/' docker/nginx-https.conf | grep -E "client_max_body_size|Phase 87"); echo "diff exit: $?"</automated>
  </verify>
  <done>
    - `grep -c 'client_max_body_size 6M' docker/nginx-https.conf` returns exactly 1
    - `grep -c 'Phase 87 (D-20/D-21)' docker/nginx-https.conf` returns exactly 1
    - The diff command above returns EMPTY (exit 0) for the client_max_body_size + Phase 87 lines — proves both files got the byte-identical addition per D-21
    - `grep -c 'client_max_body_size 6M' docker/nginx.conf docker/nginx-https.conf` returns 2 total (1 per file)
    - `git diff docker/nginx-https.conf | grep -c '^+.*client_max_body_size' == 1` (only 1 new occurrence, not duplicate)
    - Both files' /users blocks now have the directive as the last inside-brace line (verified via awk range extraction showing `client_max_body_size 6M;` immediately before the closing `}`)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → nginx | untrusted HTTP body crosses here; nginx enforces client_max_body_size cap BEFORE proxying to Express |
| nginx → Express upstream | trusted internal loopback; multer enforces its own size cap (5 MB per Plan 02) as defense-in-depth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-EDGE-413 | DoS / user-facing failure | nginx 1 MB default 413s the /users/create request | mitigate | Both nginx configs get `client_max_body_size 6M;` — 5 MB payload cap (D-15) + ~1 MB multipart framing headroom. Verified by Task 1 and Task 2 grep + diff assertions. |
| T-85-EDGE-DOS | DoS | oversized body attack past 6M | mitigate | Client_max_body_size hard-caps request body at nginx; requests exceeding 6M are rejected at the edge with 413 before consuming Express memory. Multer (Plan 02) adds a second layer at 5 MB — layered defense. |
| T-85-SISTER-FILE-MISS | Config drift | edit lands in one nginx file but not the other | mitigate | Task 2's verification uses a `diff` between the two files' /users blocks to catch this drift class. `grep -c` across both files verifies presence in each. This is the specific gotcha CLAUDE.md § Nginx caveat calls out and RESEARCH.md § Pitfall 5 documents historically. |
| T-85-EDGE-NEW-PATH | Config regression | new endpoint path 200s the app shell | accept | All 3 new endpoint paths (POST /users/create, PUT /users/:id/avatar, GET /users/:id/avatar) live UNDER `/users` (D-19) — already matched by the existing regex block. No new location blocks needed. If Plan 04 accidentally chose a path outside `/users` (e.g., `/user-avatars/:id`), this defense fails — but Plan 04's PATTERNS.md-locked route names all start with `/users/` so this is not a concern in practice. |
| T-85-SC | Tampering | npm/docker installs | mitigate | No new packages, no docker image changes — pure config edit inside existing files. |
</threat_model>

<verification>
- Grep: `grep -c 'client_max_body_size 6M' docker/nginx.conf docker/nginx-https.conf` returns 2 (1 per file, verifies D-21 both-files-edited)
- Diff assertion: awk-extracted /users blocks from both files show byte-identical `client_max_body_size 6M;` + `Phase 87 (D-20/D-21)` comment lines
- Manual smoke (deferred to Plan 07 integration test): POST a 3 MB PNG via curl to a running dev container — should reach Express (verifiable via server logs); POST a 6 MB PNG — should 413 (either at nginx if body >6M or at Express if body between 5-6M)
- `git diff docker/nginx.conf docker/nginx-https.conf` shows changes ONLY inside the /users location blocks; no other directives touched
- If nginx container is reloadable: `docker exec skynet-nginx nginx -t` reports "syntax is ok" (config is valid post-edit)
</verification>

<success_criteria>
- Both nginx configs have `client_max_body_size 6M;` inside the `/users` regex location block (T-85-EDGE-413 mitigated)
- The two files carry byte-identical additions (T-85-SISTER-FILE-MISS mitigated; diff exits 0 on the Phase 87 lines)
- Nginx syntactic validity preserved (braces balanced, valid directive placement inside a `location` block)
- Downstream Plan 07 integration test can successfully POST a 3 MB PNG through the full nginx → Express → multer → helper → DB chain without hitting a 413 at the edge
- The recurring "only-one-file" failure mode called out in CLAUDE.md § Nginx caveat is closed for this phase
</success_criteria>

<output>
Create `.planning/phases/85-user-avatars-baseline-backend-support-mandatory-at-create-ch/87-06-SUMMARY.md` when done, listing:
- The exact lines added to each file (verbatim comment + directive)
- The line numbers where the additions landed in each file
- Confirmation of the diff-empty check (both files got byte-identical additions)
- Confirmation that no other directives in either file were touched (verified via git diff)
- The specific value chosen (6M) and its derivation (5 MB payload cap + ~1 MB multipart framing headroom, sourced from RESEARCH.md § Pitfall 1 + § Assumption A3)
</output>
