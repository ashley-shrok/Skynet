---
phase: 85-user-avatars-baseline-backend-support
plan: 05
type: execute
wave: 4
depends_on: ["87-01", "87-02", "87-03", "87-04"]
files_modified:
  - src/backend/database/routes/delete-user-data.ts
  - src/backend/database/routes/users.ts
  - src/backend/database/db/schema.ts
  - src/backend/database/routes/delete-user-data.test.ts
autonomous: true
requirements: [D-22]
tags: [phase-87, user-avatars, delete-cleanup]

must_haves:
  truths:
    - "D-22: when a user is deleted, that user's avatar file is removed from disk (ENOENT-tolerant — missing file is not an error)"
    - "All 3 user-deletion call sites identified in RESEARCH.md § 1 are wired: (a) delete-user-data.ts:91 (canonical bulk-delete helper — covers admin delete + OIDC-merge), (b) users.ts:2007 (self-serve DELETE /users/delete-account), (c) users.ts:198 (create-endpoint encryption-failure rollback — already wired in Plan 03, verified here)"
    - "OIDC-callback rollback at users.ts:1052 is documented as intentional no-op (RESEARCH.md § 1 Assumption A1) — no code change, comment already added by Plan 03"
    - "The canonical delete helper fetches avatar_path from users row BEFORE deleting the row (row is the source of the filename pointer; delete-then-unlink would lose the pointer)"
  artifacts:
    - path: "src/backend/database/routes/delete-user-data.ts"
      provides: "extended delete pipeline that unlinks avatar file before the users row DELETE"
      contains: "unlinkUserAvatar"
    - path: "src/backend/database/routes/users.ts"
      provides: "DELETE /users/delete-account wired with pre-DELETE unlinkUserAvatar"
      contains: "unlinkUserAvatar"
    - path: "src/backend/database/routes/delete-user-data.test.ts"
      provides: "unit test asserting the helper unlinks the avatar file (fake file created → helper invoked → file absent)"
      min_lines: 60
  key_links:
    - from: "delete-user-data.ts:91 delete site"
      to: "user-avatar-storage.unlinkUserAvatar"
      via: "fetch avatar_path from users row THEN unlink THEN delete row"
      pattern: "avatarPath.*unlinkUserAvatar\\|unlinkUserAvatar.*avatarPath"
    - from: "users.ts DELETE /users/delete-account (~line 2007)"
      to: "user-avatar-storage.unlinkUserAvatar"
      via: "self-delete path independent of deleteUserAndRelatedData"
      pattern: "unlinkUserAvatar"
---

<objective>
Wire the D-22 avatar-file cleanup into every user-deletion call site enumerated by RESEARCH.md § 1:
1. `delete-user-data.ts:91` — the canonical bulk-delete helper called from admin delete + OIDC-merge (ONE edit here covers 2 downstream sites)
2. `users.ts:2007` — DELETE /users/delete-account (self-serve; does NOT go through deleteUserAndRelatedData, needs its own wire-up)
3. `users.ts:198` — create-endpoint encryption-failure rollback (already wired by Plan 03 Task 1 Step 7; this plan verifies the wiring exists and adds a code comment marking Phase 87 provenance)

Add a delete-user-data.test.ts (or extend an existing test file) proving the helper actually unlinks the avatar file.

Purpose: Without this plan, a deleted user leaves an orphan avatar file on disk forever. Over time this fills the encrypted volume. It also violates the D-22 locked decision.

Output: modified delete-user-data.ts (unlink before row delete), modified users.ts (unlink in delete-account), new/extended delete-user-data.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/85-user-avatars-baseline-backend-support-mandatory-at-create-ch/87-CONTEXT.md
@.planning/phases/85-user-avatars-baseline-backend-support-mandatory-at-create-ch/87-RESEARCH.md
@.planning/phases/85-user-avatars-baseline-backend-support-mandatory-at-create-ch/87-PATTERNS.md
@.planning/phases/85-user-avatars-baseline-backend-support-mandatory-at-create-ch/87-02-SUMMARY.md
@.planning/phases/85-user-avatars-baseline-backend-support-mandatory-at-create-ch/87-03-SUMMARY.md
@.planning/phases/85-user-avatars-baseline-backend-support-mandatory-at-create-ch/87-04-SUMMARY.md
@src/backend/database/routes/delete-user-data.ts
@src/backend/database/routes/users.ts
@src/backend/database/routes/user-avatar-storage.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire unlinkUserAvatar into delete-user-data.ts:91 (D-22 — covers 2 downstream call sites)</name>
  <files>src/backend/database/routes/delete-user-data.ts</files>
  <read_first>
    - src/backend/database/routes/delete-user-data.ts full file (32-104 is the helper body; line 91 is the insertion point immediately before `await db.delete(users).where(eq(users.id, userId))`)
    - src/backend/database/routes/user-avatar-storage.ts (Plan 02 output — unlinkUserAvatar signature)
    - src/backend/database/routes/user-oidc-account-routes.ts line 178 (indirect caller of deleteUserAndRelatedData — RESEARCH.md § 1 confirms this is covered by the single edit here)
    - src/backend/database/routes/users.ts line 2229 (DELETE /users/delete-user admin site — RESEARCH.md § 1 confirms this is also covered by this single edit because it calls deleteUserAndRelatedData)
    - .planning/phases/85-user-avatars-baseline-backend-support-mandatory-at-create-ch/87-RESEARCH.md § 1 "User-delete pipeline discovery" (the 5-site enumeration with wiring recommendations)
    - .planning/phases/85-user-avatars-baseline-backend-support-mandatory-at-create-ch/87-PATTERNS.md § "src/backend/database/routes/delete-user-data.ts — wire avatar unlink"
  </read_first>
  <behavior>
    - Test 1 (helper unlinks file before deleting row): create a user via direct DB insert + write a fake avatar file → call deleteUserAndRelatedData(userId) → assert file no longer exists AND users row no longer exists
    - Test 2 (helper is ENOENT-tolerant): create a user with null avatar_path (no file on disk) → call helper → assert row deleted without throwing
    - Test 3 (helper is ENOENT-tolerant when pointer exists but file was manually removed): create user with avatar_path="ghost.png" but do NOT create the file → call helper → assert row deleted without throwing (unlinkUserAvatar swallows ENOENT per Plan 02)
    - Test 4 (order — SELECT happens BEFORE DELETE): mock scenario where DELETE somehow succeeds but the SELECT was somehow skipped would leave orphan file. Verify code ordering visually via grep: SELECT of avatar_path appears BEFORE db.delete(users) in the source.
  </behavior>
  <action>
    Edit src/backend/database/routes/delete-user-data.ts. Find the line `await db.delete(users).where(eq(users.id, userId));` at line 91 (or wherever it now lives — grep to confirm current line number if the file has shifted).

    Step 1 (imports at top of file):
    Add `import { unlinkUserAvatar } from "./user-avatar-storage.js";` alongside the other route-level imports.

    Also verify `users` is imported from the schema (should be already — the file already references `users` in the db.delete call).

    Step 2 (insert avatar-cleanup block IMMEDIATELY BEFORE the `await db.delete(users)` line):
    ```
    // Phase 87 (D-22) — remove this user's avatar file from disk BEFORE deleting
    // the row. We fetch the pointer first because the row is our source of truth
    // for the filename; deleting the row first would lose the pointer forever.
    // unlinkUserAvatar is ENOENT-tolerant per Plan 02 — a null pointer or a
    // missing file on disk both no-op cleanly. If the unlink fails for a
    // non-ENOENT reason (permission, EBUSY), the throw propagates and the
    // outer try/catch at the top of this helper surfaces it — the DELETE
    // will not run and the operator sees the error rather than a silent
    // orphan-file leak.
    const avatarRow = await db.select({ avatarPath: users.avatarPath })
      .from(users)
      .where(eq(users.id, userId))
      .limit(1);
    if (avatarRow.length > 0) {
      await unlinkUserAvatar(avatarRow[0].avatarPath);
    }
    ```

    Then the existing `await db.delete(users).where(eq(users.id, userId));` line follows immediately.

    Step 3 (do NOT modify) any other part of the helper. Do NOT touch the user_preferences delete, the settings LIKE delete, or the surrounding try/catch structure.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -E "delete-user-data\.ts|error TS" | head -20; grep -n 'unlinkUserAvatar' src/backend/database/routes/delete-user-data.ts; grep -n 'avatarPath: users.avatarPath' src/backend/database/routes/delete-user-data.ts; awk '/unlinkUserAvatar\(avatarRow/{a=NR} /db\.delete\(users\)/{b=NR} END{print "SELECT+unlink line:", a, "DELETE line:", b, (a<b ? "OK ordering" : "WRONG ordering")}' src/backend/database/routes/delete-user-data.ts</automated>
  </verify>
  <done>
    - `grep -c 'unlinkUserAvatar' src/backend/database/routes/delete-user-data.ts` returns at least 1 (in the helper body)
    - `grep -c 'avatarPath: users.avatarPath' src/backend/database/routes/delete-user-data.ts` returns exactly 1 (the SELECT projection)
    - `grep -c 'from "./user-avatar-storage' src/backend/database/routes/delete-user-data.ts` returns exactly 1 (import added)
    - The SELECT of avatar_path comes BEFORE the db.delete(users) line (verified by awk in <verify>)
    - `grep -c 'Phase 87 (D-22)' src/backend/database/routes/delete-user-data.ts` returns 1 (comment block anchors the D-22 provenance)
    - `npx tsc --noEmit` reports zero new errors in delete-user-data.ts
    - `git diff src/backend/database/routes/delete-user-data.ts` shows changes ONLY in the imports block and the single insertion before db.delete(users)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire unlinkUserAvatar into users.ts DELETE /users/delete-account (self-serve delete — line ~2007)</name>
  <files>src/backend/database/routes/users.ts</files>
  <read_first>
    - src/backend/database/routes/users.ts lines 2000-2050 (DELETE /users/delete-account handler — verify current line number, may have shifted after Plan 03 + Plan 04 edits)
    - src/backend/database/routes/user-avatar-storage.ts (unlinkUserAvatar import already available from Plans 03/04 edits — grep to confirm import is already at top of users.ts, don't add a duplicate)
    - .planning/phases/85-user-avatars-baseline-backend-support-mandatory-at-create-ch/87-RESEARCH.md § 1 site #4 (users.ts:2007 rationale — does NOT call deleteUserAndRelatedData, needs its own wire-up)
    - .planning/phases/85-user-avatars-baseline-backend-support-mandatory-at-create-ch/87-PATTERNS.md § "src/backend/database/routes/delete-user-data.ts — wire avatar unlink" → "Also wire in users.ts:2007"
  </read_first>
  <behavior>
    - Test 1 (self-delete unlinks avatar): authenticated user Alice with existing avatar → DELETE /users/delete-account → user row deleted AND avatar file removed from disk
    - Test 2 (self-delete ENOENT-tolerant): user Alice with null avatar_path → DELETE succeeds without throwing
  </behavior>
  <action>
    Edit src/backend/database/routes/users.ts. Find the DELETE /users/delete-account handler (current line ~2007; grep for `router.delete("/delete-account"` to locate — line number may have shifted from Plans 03/04 additions).

    Step 1 (verify import already present):
    Plans 03 and 04 already added `import { userAvatarUpload, userAvatarMulterErrorHandler, writeUserAvatar, unlinkUserAvatar } from "./user-avatar-storage.js";`. Confirm this import line exists at the top of users.ts. If for some reason `unlinkUserAvatar` is not in the import list, add it.

    Step 2 (find the `db.delete(users).where(eq(users.id, userId))` call inside the delete-account handler):
    Grep for the raw `db.delete(users)` calls in users.ts; RESEARCH.md § 1 says line 2007 is the delete-account site. Distinguish from the delete-user (admin) site at line 2229 which goes through `deleteUserAndRelatedData` (already covered by Task 1).

    Step 3 (insert avatar cleanup BEFORE the db.delete in the self-delete handler):
    Add the same pattern as Task 1, but scoped to the self-delete branch:
    ```
    // Phase 87 (D-22) — remove this user's avatar file before deleting the row.
    // This path (DELETE /users/delete-account) does NOT go through
    // deleteUserAndRelatedData, so the cleanup wiring in delete-user-data.ts
    // does not cover it (RESEARCH.md § 1 site #4). ENOENT-tolerant per Plan 02.
    try {
      const avatarRow = await db.select({ avatarPath: users.avatarPath })
        .from(users)
        .where(eq(users.id, userId))
        .limit(1);
      if (avatarRow.length > 0) {
        await unlinkUserAvatar(avatarRow[0].avatarPath);
      }
    } catch (unlinkErr) {
      authLogger.warn(
        "[phase-87] avatar unlink failed on delete-account (non-fatal — proceeding with row DELETE)",
        { operation: "delete_account_avatar_unlink_failed", userId, error: unlinkErr },
      );
    }
    ```

    Note the wrap-in-try-catch here differs from Task 1's pattern: for delete-account we prefer to log-and-continue on unlink failure so a broken filesystem doesn't block the user from deleting their own account. For the admin/OIDC delete path (Task 1), we let the throw propagate so an operator sees the issue. Rationale: self-service delete is user-facing (must not fail on operational issues); helper is called from admin flows (operator should see failures).

    Step 4 (verify the encryption-failure rollback at users.ts:198 is already wired by Plan 03):
    Grep for `unlinkUserAvatar(avatarFilename)` in users.ts within the create handler (around line 195-210). Plan 03 Task 1 Step 7 added this. If for any reason it's missing, add it — this task is the belt-and-suspenders check.

    Step 5 (add a schema-level anchoring comment near the users table def) — OPTIONAL, but RESEARCH.md § Open Question 3 recommends it:
    In src/backend/database/db/schema.ts (already touched by Plan 01), immediately above the users table def (around line 4), add a comment:
    ```
    // Phase 87 (D-22) — INVARIANT: any code that deletes a users row MUST call
    // unlinkUserAvatar(avatarPath) first (via the shared helper from
    // routes/user-avatar-storage.ts). Wired sites: delete-user-data.ts:91,
    // users.ts DELETE /users/delete-account, users.ts create-rollback (Plan 03).
    // OIDC-callback rollback (users.ts ~line 1052) is deliberately exempt per
    // Plan 03 Task 1 Step 10 (OIDC create bypasses mandatoriness).
    ```

    (If Plan 01 already added this comment, verify it's still there and skip; if not, add it now — cross-plan hygiene for future editors.)
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -E "users\.ts|schema\.ts|error TS" | head -20; grep -n 'unlinkUserAvatar' src/backend/database/routes/users.ts; grep -n 'delete-account_avatar_unlink_failed\|delete_account_avatar_unlink_failed' src/backend/database/routes/users.ts; grep -n 'Phase 87 (D-22)' src/backend/database/db/schema.ts</automated>
  </verify>
  <done>
    - `grep -c 'unlinkUserAvatar' src/backend/database/routes/users.ts` returns at least 4 (Plan 03: 2 usages in create-endpoint rollbacks + Plan 04: 2 usages in change-endpoint rollback/old-file + Plan 05 Task 2: 1 usage in delete-account = at least 5; use `>=4` as lower bound)
    - `grep -c 'delete_account_avatar_unlink_failed' src/backend/database/routes/users.ts` returns exactly 1 (Task 2 log operation)
    - `grep -c 'Phase 87 (D-22)' src/backend/database/routes/users.ts` returns at least 1 (the delete-account comment block)
    - `grep -c 'Phase 87 (D-22)' src/backend/database/db/schema.ts` returns 1 (the invariant comment at top of users table def)
    - `npx tsc --noEmit` reports zero new errors
    - `git diff src/backend/database/routes/users.ts` shows changes ONLY in the delete-account handler (no unintended edits elsewhere)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create delete-user-data.test.ts proving the helper unlinks avatar files (D-22 verification)</name>
  <files>src/backend/database/routes/delete-user-data.test.ts</files>
  <read_first>
    - src/backend/database/routes/delete-user-data.ts (Task 1 output — the helper under test)
    - src/backend/database/routes/user-avatar-storage.ts (writeUserAvatar / USER_AVATARS_DIR for test setup)
    - src/backend/database/routes/user-avatar-storage.test.ts (Plan 02 output — the tmpdir + DATA_DIR reset pattern to mirror)
    - .planning/phases/85-user-avatars-baseline-backend-support-mandatory-at-create-ch/87-PATTERNS.md § "src/backend/database/routes/delete-user-data.test.ts (CREATE)"
  </read_first>
  <behavior>
    - Test 1 (helper unlinks avatar file): seed a user row (id="alice") with avatar_path="alice.png"; write actual bytes to USER_AVATARS_DIR/alice.png; invoke `deleteUserAndRelatedData("alice")`; assert `fs.access(path.join(USER_AVATARS_DIR, "alice.png"))` throws (file gone) AND SELECT of the user returns empty
    - Test 2 (null pointer is a no-op): seed user with avatar_path=null; invoke helper; assert row gone, no error, no throw
    - Test 3 (missing file is ENOENT-tolerant): seed user with avatar_path="ghost.png" but DO NOT create the file; invoke helper; assert row gone, no error
    - Test 4 (source-of-truth ordering): manually inspect by grepping the source that SELECT precedes DELETE (structural test — mirrors the awk check in Task 1's verify block, but as a test-file grep assertion using `fs.readFile` of delete-user-data.ts and a regex)
  </behavior>
  <action>
    Create src/backend/database/routes/delete-user-data.test.ts.

    Imports:
    - `describe, it, expect, beforeEach, afterEach, vi` from "vitest"
    - `* as fs` from "node:fs/promises"
    - `* as path` from "node:path"
    - `* as os` from "node:os"

    Test harness (mirror user-avatar-storage.test.ts pattern from Plan 02):
    - `beforeEach`:
      - Fresh tmp dir → set `process.env.DATA_DIR`
      - `vi.resetModules()` then `await import(...)` of both the helper module AND the users DB module so the DATA_DIR is captured fresh
      - Bootstrap in-memory DB with users table (call the Skynet DB init OR run the same `CREATE TABLE users (id TEXT PRIMARY KEY, ..., avatar_path TEXT)` fixture used by index.migration.test.ts USERS_CREATE_SQL_PRE_AVATAR_PATH — Plan 01 Task 3 defined this)
    - `afterEach`: close DB, `fs.rm(tmpDir, recursive)`, restore env, `vi.resetModules()`

    Test cases:
    1. `it("deleteUserAndRelatedData unlinks avatar file before deleting row", async () => { ... })`
       - Insert a user row: `INSERT INTO users (id, username, password_hash, is_admin, ..., avatar_path) VALUES ('alice_id', 'alice', 'hash', 0, ..., 'alice_id.png')` (mirror the raw SQL shape used by users.ts:143-163)
       - Write bytes: `await fs.mkdir(USER_AVATARS_DIR, {recursive: true}); await fs.writeFile(path.join(USER_AVATARS_DIR, 'alice_id.png'), Buffer.from([1,2,3]));`
       - `await deleteUserAndRelatedData('alice_id');`
       - Assert file gone: `await expect(fs.access(path.join(USER_AVATARS_DIR, 'alice_id.png'))).rejects.toThrow();`
       - Assert row gone: query and assert no rows
    2. `it("deleteUserAndRelatedData no-ops on null avatar pointer", async () => { ... })`
       - Insert user with `avatar_path=NULL`
       - No file write
       - Invoke helper → assert no throw, row gone
    3. `it("deleteUserAndRelatedData is ENOENT-tolerant when pointer is set but file was manually removed", async () => { ... })`
       - Insert user with `avatar_path="ghost.png"` but do NOT create the file
       - Invoke helper → assert no throw, row gone
    4. `it("SOURCE ASSERTION: delete-user-data.ts fetches avatar_path BEFORE db.delete(users)", async () => { ... })`
       - `const src = await fs.readFile("src/backend/database/routes/delete-user-data.ts", "utf-8");`
       - `const selectIdx = src.indexOf("avatarPath: users.avatarPath");`
       - `const deleteIdx = src.indexOf("db.delete(users).where(eq(users.id, userId))");`
       - `expect(selectIdx).toBeGreaterThan(-1); expect(deleteIdx).toBeGreaterThan(-1); expect(selectIdx).toBeLessThan(deleteIdx);`
       - This is a structural test — mirror Task 1's awk check but committed as a repeatable assertion so future edits can't accidentally invert the order.

    Grouping: wrap in `describe("delete-user-data (Phase 87 avatar cleanup — D-22)", () => { ... })`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; npx vitest run src/backend/database/routes/delete-user-data.test.ts 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    - `npx vitest run src/backend/database/routes/delete-user-data.test.ts` reports 4 passing tests
    - Zero failures, zero skipped
    - `wc -l src/backend/database/routes/delete-user-data.test.ts` shows at least 60 lines
    - `grep -c 'it(' src/backend/database/routes/delete-user-data.test.ts` returns at least 4
    - Test 4 (SOURCE ASSERTION) is present and passes — proves the source-ordering invariant
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| helper caller → SELECT + unlink + DELETE | trusted internal callers only (admin flow, self-delete flow, OIDC merge); no external input reaches the file-touching code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-09 | Integrity | orphan file after user delete | mitigate | Task 1 wires unlinkUserAvatar into delete-user-data.ts:91 (covers admin delete + OIDC-merge — 2 sites via 1 edit); Task 2 wires it into DELETE /users/delete-account (self-serve, distinct code path); Plan 03 already wired the create-endpoint rollback at users.ts:198. Task 3's SOURCE ASSERTION test locks the SELECT-before-DELETE ordering so a future refactor can't silently regress. |
| T-85-DEL-BLOCK | DoS / user-facing failure | filesystem error blocks self-delete | mitigate | Task 2 wraps the delete-account unlink in try/catch with WARN log (log-and-continue) so a broken FS doesn't prevent a user from deleting their own account. Admin-side (Task 1) let-throw so operators see FS issues. This is a deliberate policy split documented in Task 2 Step 3. |
| T-85-OIDC-DEL | Integrity | OIDC-user delete leaves orphan | accept | RESEARCH.md § 1 Assumption A1: OIDC users are created without avatars per Phase 87 scope (D-07 mandatoriness is endpoint-specific to POST /users/create); no avatar file ever exists for OIDC users, so no orphan is possible. The users.ts:1052 comment (added by Plan 03) documents the deliberate no-op. |
| T-85-RACE-DEL | Integrity | concurrent delete + serve request | accept | Serve reads users.avatar_path inside its own request; if the row is deleted between the caller starting the serve request and the SELECT running, the SELECT returns empty → 404 (T-85-DELETE-MID-FLIGHT in Plan 04 threat model). No stale bytes leak. |
| T-85-SC | Tampering | npm installs | mitigate | No new packages. |
</threat_model>

<verification>
- Run: `npx vitest run src/backend/database/routes/delete-user-data.test.ts` — 4 passing tests
- Run: `npx tsc --noEmit` — zero new errors
- Grep: `grep -c 'unlinkUserAvatar' src/backend/database/routes/delete-user-data.ts` >= 1
- Grep: `grep -c 'unlinkUserAvatar' src/backend/database/routes/users.ts` >= 5 (Plan 03 rollbacks + Plan 04 rollback + Plan 04 old-file cleanup + Plan 05 self-delete)
- Grep: `grep -c 'Phase 87 (D-22)' src/backend/database/routes/users.ts src/backend/database/routes/delete-user-data.ts src/backend/database/db/schema.ts` returns at least 3 (one anchoring comment per file that gained the wiring)
- Structural: awk verify block in Task 1 confirms SELECT-before-DELETE in delete-user-data.ts
- Test 4 (SOURCE ASSERTION in delete-user-data.test.ts) also codifies the ordering as a repeatable check
</verification>

<success_criteria>
- Every user-deletion call site enumerated by RESEARCH.md § 1 (delete-user-data.ts:91, users.ts:2007, users.ts:198, plus users.ts:2229 and user-oidc-account-routes.ts:178 via the shared helper) unlinks the avatar file before/around the row delete
- No orphan avatar files can accumulate under normal usage (T-85-09 mitigated)
- OIDC-callback rollback is documented as deliberate no-op (Plan 03 Task 1 Step 10 added the comment)
- Self-delete does not fail on FS errors (Task 2 log-and-continue) but admin delete does surface FS errors (Task 1 let-throw) — deliberate policy split
- SOURCE ASSERTION test locks the SELECT-before-DELETE ordering against future regressions
- Downstream Plan 07 integration test can exercise the full create → change → serve → delete flow and verify no orphan files at the end
</success_criteria>

<output>
Create `.planning/phases/85-user-avatars-baseline-backend-support-mandatory-at-create-ch/87-05-SUMMARY.md` when done, listing:
- The 3 wired call sites (delete-user-data.ts:91, users.ts:2007, users.ts:198 verified) + the 2 explicit-no-op sites (users.ts:1052 OIDC + acknowledgment that users.ts:2229 admin-delete is covered by helper edit)
- The policy split (Task 1 let-throw, Task 2 log-and-continue) with rationale
- The 4 test cases and their pass/fail status
- Confirmation that the SOURCE ASSERTION test passes (locks SELECT-before-DELETE ordering)
- Confirmation that `grep -c unlinkUserAvatar src/backend/database/routes/users.ts` returns the expected count
</output>
