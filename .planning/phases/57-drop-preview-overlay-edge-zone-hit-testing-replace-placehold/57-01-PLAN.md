---
phase: 57-drop-preview-overlay-edge-zone-hit-testing-replace-placehold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/lib/split-tree.ts
  - src/ui/lib/split-tree.test.ts
autonomous: true
requirements:
  - PV57-EDGE-ZONE-GEOMETRY
  - PV57-CENTER-DEAD-ZONE
  - PV57-SNAP-TO-NEAREST-EDGE
tags:
  - split-tree
  - edge-zone
  - drop-preview
  - phase-57
must_haves:
  truths:
    - "A pure function `computeEdgeZone(rect, clientX, clientY): DropZone` (where `DropZone = DropEdge | 'center'`) exports from `src/ui/lib/split-tree.ts` and is callable without any React/DOM setup — same module boundary as `computeNearestEdge` (Phase 56 Plan 03)."
    - "Given any cell rect, a cursor position within the inner ~20% × 20% rectangle (centered on the rect) returns `'center'`; a cursor position outside the center rectangle returns one of `'left' | 'right' | 'top' | 'bottom'` matching the nearest edge, matching the prototype's `pickZone` at `~/.claude/roles/box-maintainer/bounties/bring-back-split-view/prototype.html:361-370` verbatim (EDGE=0.28 normalized threshold, tie-break priority top → bottom → left → right)."
    - "Corner-tie cases (cursor equidistant from two edges — e.g. exactly (0.14, 0.14) of a rect from the top-left corner) resolve deterministically per the documented tie-break priority: `top` wins over `bottom` wins over `left` wins over `right`."
    - "The function accepts `DOMRect | { left; right; top; bottom }` at the type level (matching `computeNearestEdge`'s signature) so Vitest can call it with plain object literals and Pane can call it with `getBoundingClientRect()`."
    - "Existing `computeNearestEdge` continues to export unchanged — Phase 56 tests continue to pass. `computeEdgeZone` is ADDED alongside it, not a replacement (Phase 57's overlay uses `computeEdgeZone`; drop-time edge selection also uses `computeEdgeZone`, and the center case short-circuits before calling `insertAtEdge`)."
  artifacts:
    - path: "src/ui/lib/split-tree.ts"
      provides: "ADDS `DropZone` type alias + `computeEdgeZone` pure function alongside existing exports. No signature changes to `computeNearestEdge`, `insertAtEdge`, `removeLeaf`, etc."
      exports: [DropZone, computeEdgeZone]
      contains: "export function computeEdgeZone"
    - path: "src/ui/lib/split-tree.test.ts"
      provides: "ADDS a `describe('split-tree — Phase 57: edge-zone hit-testing')` block with 9+ cases: 4 edge-mid tests, 4 corner-tie tests (all four corners at the exact threshold), 1 dead-center test, plus explicit boundary tests at the 0.28 threshold (just inside → edge, just outside → center)."
  key_links:
    - from: "src/ui/shell/SplitView.tsx (Plan 57-02 consumer)"
      to: "src/ui/lib/split-tree.ts"
      via: "Plan 57-02's `Pane` component imports `computeEdgeZone` for dragover-time zone computation, and re-uses the same helper on drop for the center-dead-zone short-circuit"
      pattern: "computeEdgeZone"
    - from: "src/ui/lib/split-tree.test.ts (this plan's test file)"
      to: "src/ui/lib/split-tree.ts"
      via: "unit-test coverage for the pure function; runs with zero DOM setup"
      pattern: "import.*computeEdgeZone"
---

<objective>
Add the pure-function foundation Phase 57's drop-preview overlay depends on: `computeEdgeZone(rect, clientX, clientY): DropZone` in `src/ui/lib/split-tree.ts`, alongside the existing `computeNearestEdge` from Phase 56 Plan 03. Zero UI changes; zero React imports; zero behavior change on its own. This plan lands in isolation so Plan 57-02's Pane rewire can import a tested, deterministic zone-picker.

Purpose: The prototype at `~/.claude/roles/box-maintainer/bounties/bring-back-split-view/prototype.html` uses a normalized-coordinate zone picker (`pickZone` at line 361-370, `EDGE = 0.28`) that Ashley validated live. Phase 57's overlay needs the same math to compute the current zone on every `dragover`; Plan 57-02 also needs it on `drop` for the center-dead-zone short-circuit (drop in center → no `insertAtEdge` call, no tree change). Isolating the geometry here keeps Plan 57-02 focused on the React wiring.

Output: ONE modified file (`src/ui/lib/split-tree.ts` — appends `DropZone` type + `computeEdgeZone` function) + ONE modified test file (`src/ui/lib/split-tree.test.ts` — appends a new describe block). `npx tsc --noEmit` clean + `npx vitest run --related src/ui/lib/split-tree.ts` green is the observable end state. Phase 56 tests continue to pass unchanged (regression assertion, part of the scoped test run).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/57-drop-preview-overlay-edge-zone-hit-testing-replace-placehold/57-CONTEXT.md
@.planning/shapes/shape-visual-session-management.md
@CLAUDE.md
@src/ui/lib/split-tree.ts
@src/ui/lib/split-tree.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `computeEdgeZone` pure function + `DropZone` type alias to split-tree.ts, with unit tests</name>
  <files>
    src/ui/lib/split-tree.ts,
    src/ui/lib/split-tree.test.ts
  </files>
  <read_first>
    - src/ui/lib/split-tree.ts (READ IN FULL — the existing module. `DropEdge` type at :54 is what `DropZone` widens; `computeNearestEdge` at :44-69 is the sister function whose call signature `(rect, clientX, clientY)` computeEdgeZone MUST match so both are drop-in interchangeable at the Pane callsite. Follow the module's existing JSDoc style verbatim.)
    - src/ui/lib/split-tree.test.ts (READ IN FULL — the existing Vitest suite. Note the `describe("split-tree")` block wrapping every test at :55; note that the file already imports `DropEdge` type at :16 as a type-only import. Follow the existing test-naming convention "Test N: …".)
    - /home/ubuntu/.claude/roles/box-maintainer/bounties/bring-back-split-view/prototype.html (READ lines 361-370 — the reference `pickZone` implementation. The whole point of this task is to port that math to TS with the same threshold and tie-break priority. Do NOT invent new geometry.)
    - .planning/phases/57-drop-preview-overlay-edge-zone-hit-testing-replace-placehold/57-CONTEXT.md (§In-scope this phase item 1 for the geometry contract; §What would make Phase 57 wrong for the failure modes this function's determinism guards against.)
  </read_first>
  <behavior>
    - Test 1: `computeEdgeZone(rect, clientX, clientY)` at the exact rect center (50,50 in a 100×100 rect anchored at origin) returns `'center'`. This is the dead-center case.
    - Test 2: cursor at (5, 50) in a 100×100 rect (5% from left, 50% from top/bottom) → `'left'`. Normalized `zx = 0.05`, `zy = 0.5`; `min(distTop=0.5, distBottom=0.5, distLeft=0.05, distRight=0.95) = 0.05 <= 0.28` → the nearest edge wins, which is `left`.
    - Test 3: cursor at (95, 50) in a 100×100 rect (5% from right) → `'right'`. Same argument, `distRight = 0.05` wins.
    - Test 4: cursor at (50, 5) in a 100×100 rect (5% from top) → `'top'`. `distTop = 0.05` wins.
    - Test 5: cursor at (50, 95) in a 100×100 rect (5% from bottom) → `'bottom'`.
    - Test 6 (center-threshold outer boundary — just outside): cursor at (30, 50) in a 100×100 rect. `zx = 0.30`, so `distLeft = 0.30 > 0.28` — wait, the threshold check is `if (minDist > EDGE) return 'center'`. Here minDist = distLeft = 0.30, which is > 0.28, so this returns `'center'`. Test asserts `computeEdgeZone(rect, 30, 50)` returns `'center'`.
    - Test 7 (center-threshold outer boundary — just inside the edge zone): cursor at (28, 50) in a 100×100 rect. `distLeft = 0.28`; `minDist = 0.28` which is NOT `> 0.28`, so falls through to edge picking, which returns `'left'`. Test asserts `computeEdgeZone(rect, 28, 50) === 'left'`.
    - Test 8 (corner-tie tie-break priority): cursor at (14, 14) in a 100×100 rect. `distTop = distLeft = 0.14`, `distBottom = distRight = 0.86`, `minDist = 0.14`. Both `top` and `left` are candidates. Documented tie-break priority `top → bottom → left → right` (matching prototype's if-chain: `if (minDist === distTop) return 'top'; if (minDist === distBottom) return 'bottom'; if (minDist === distLeft) return 'left'; return 'right';`) → `'top'` wins. Test asserts `computeEdgeZone(rect, 14, 14) === 'top'`.
    - Test 9 (corner-tie, bottom-right): cursor at (86, 86) in a 100×100 rect. `distTop = distLeft = 0.86`, `distBottom = distRight = 0.14`, `minDist = 0.14`. `distTop !== 0.14`, `distBottom === 0.14` → `'bottom'` wins. Test asserts `computeEdgeZone(rect, 86, 86) === 'bottom'`.
    - Test 10 (non-square rect — 200×100): cursor at (150, 50) in a rect `{left:0, right:200, top:0, bottom:100}`. Normalized `zx = 0.75`, `zy = 0.5`; `distTop = 0.5, distBottom = 0.5, distLeft = 0.75, distRight = 0.25`. `minDist = 0.25 <= 0.28` → `'right'`. Test asserts `computeEdgeZone(rect, 150, 50) === 'right'`. This proves the normalization is per-axis (not raw pixels).
    - Test 11 (rect not anchored at origin): cursor at (110, 50) in a rect `{left:100, right:200, top:0, bottom:100}` (100×100 anchored at x=100). Normalized `zx = (110-100)/100 = 0.10`; expected: `'left'` (distLeft=0.10, minDist=0.10 <= 0.28, top and bottom = 0.5, right = 0.90). Test asserts `computeEdgeZone(rect, 110, 50) === 'left'`. This proves the function subtracts `rect.left`/`rect.top` correctly.
    - Test 12 (out-of-rect cursor — defensive): cursor at (-10, 50) in a 100×100 rect anchored at origin. Normalized `zx = -0.10`. The function does NOT clamp — a negative `distLeft` produces a min that's still less than 0.28, but the direction of the "nearest edge" argument still holds: left is the closest edge. Test asserts `computeEdgeZone(rect, -10, 50) === 'left'`. Rationale: dragover events with cursor outside the pane rect can happen at the pane boundary; the function must still return a defined value, not throw. Document this in a comment on the function.
    - Test 13 (regression — Phase 56 `computeNearestEdge` still exports and works): `expect(typeof computeNearestEdge).toBe('function')` and one smoke assertion `computeNearestEdge({left:0, right:100, top:0, bottom:100}, 10, 50) === 'left'`. This is a regression guard — this plan MUST NOT change `computeNearestEdge`'s signature or behavior.
  </behavior>
  <action>
    In `src/ui/lib/split-tree.ts`, ADD (do not remove or modify existing exports):

    1. A new exported type alias immediately after the existing `DropEdge` declaration (around line 54):

       `export type DropZone = DropEdge | "center"`

       With a JSDoc comment stating: the wider-than-DropEdge alias `computeEdgeZone` returns; `"center"` indicates the cursor is in the pane's inner dead-zone rectangle where no drop should register. Cite `.planning/phases/57-drop-preview-overlay-edge-zone-hit-testing-replace-placehold/57-CONTEXT.md` §In-scope item 1 and the shape file quote "center dead zone exists so there is one path to closing a session — closing is Phase 58's drag-badge-to-list".

    2. A new exported pure function `computeEdgeZone` appended after the existing `computeNearestEdge` (around line 70). Signature (mirroring `computeNearestEdge`'s signature exactly):

       `export function computeEdgeZone(rect: DOMRect | { left: number; right: number; top: number; bottom: number }, clientX: number, clientY: number): DropZone`

       Implementation (port of prototype.html:361-370):

       - Compute `width = rect.right - rect.left`, `height = rect.bottom - rect.top`.
       - Normalized coordinates: `zx = (clientX - rect.left) / width`, `zy = (clientY - rect.top) / height`.
       - Per-axis normalized distances: `distTop = zy`, `distBottom = 1 - zy`, `distLeft = zx`, `distRight = 1 - zx`.
       - `minDist = Math.min(distTop, distBottom, distLeft, distRight)`.
       - Threshold constant (module-local `const EDGE_ZONE_THRESHOLD = 0.28`): if `minDist > EDGE_ZONE_THRESHOLD` return `"center"`.
       - Tie-break priority (matches prototype line 366-369 verbatim):
         - `if (minDist === distTop) return "top"`
         - `if (minDist === distBottom) return "bottom"`
         - `if (minDist === distLeft) return "left"`
         - `return "right"`
       - Do NOT clamp `zx`/`zy` to [0,1] — an out-of-rect cursor still resolves to a defined edge (Test 12). Document this defensive property in a JSDoc line.

       JSDoc block on the function MUST cite (a) the prototype source path + line range as the authoritative reference, (b) `.planning/phases/57-drop-preview-overlay-edge-zone-hit-testing-replace-placehold/57-CONTEXT.md` §In-scope item 1 for the contract, (c) the documented tie-break priority "top → bottom → left → right (first-match-wins)", (d) the `EDGE_ZONE_THRESHOLD` value (0.28) with a one-line justification ("~28% inner band ≈ 44% × 44% center dead-zone by area; Ashley-validated in the reference prototype").

    3. Append the module-local `const EDGE_ZONE_THRESHOLD = 0.28` at the top of the function body (or as a module-level `const` above `computeEdgeZone`), NOT exported. Rationale: the threshold is a locked design decision; exporting it invites future callers to override it, which would fragment the geometry. If a future phase needs to tune it, they modify the constant in-place.

    In `src/ui/lib/split-tree.test.ts`, ADD (do not remove or modify existing tests):

    1. Add `computeEdgeZone, type DropZone` to the existing import from `"./split-tree"` at the top of the file.

    2. Append a new `describe("split-tree — Phase 57: edge-zone hit-testing", () => { ... })` block at the end of the file (after the existing `describe("split-tree")` block, before the `_typeCheck` line). Include Tests 1-13 above, each as its own `it(...)` block with the plan-item name in the test title (e.g. `it("Test 1: dead-center returns 'center'", () => { ... })`).

    3. Use a shared 100×100 origin-anchored rect fixture for Tests 1-9 + 12 + 13 (mirror the existing Plan 03 describe block's pattern at test file line 242-248). Tests 10 + 11 use their own fixtures per their spec.

    4. In Test 13's regression assertion, ensure `computeNearestEdge` is still imported from the SAME import statement (do NOT change its export path or add a separate import).

    Do NOT touch any other file. Do NOT modify `computeNearestEdge`. Do NOT add any React/DOM imports. Do NOT extend the `DropEdge` type — the "center" case is captured by `DropZone`, not by widening `DropEdge` (Phase 56 consumers of `DropEdge` — specifically `insertAtEdge` at split-tree.ts:127 — MUST NOT need to handle a `"center"` value; Plan 57-02's Pane short-circuits before calling `insertAtEdge`).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tanya && (set -o pipefail; npx vitest run src/ui/lib/split-tree.test.ts 2>&1 | tail -30)</automated>
  </verify>
  <acceptance_criteria>
    - File `src/ui/lib/split-tree.ts` exists and contains `export type DropZone = DropEdge | "center"` — verify: `grep -c '^export type DropZone = DropEdge | "center"' src/ui/lib/split-tree.ts` returns 1.
    - File `src/ui/lib/split-tree.ts` contains `export function computeEdgeZone(` — verify: `grep -c '^export function computeEdgeZone(' src/ui/lib/split-tree.ts` returns 1.
    - The module-local threshold constant is present — verify: `grep -c 'EDGE_ZONE_THRESHOLD.*=.*0.28' src/ui/lib/split-tree.ts` returns 1.
    - `computeNearestEdge` is unchanged in signature — verify: `grep -c '^export function computeNearestEdge(' src/ui/lib/split-tree.ts` returns 1 (still exactly one, same signature).
    - No React / DOM / backend imports added — verify: `grep -cE '^import.*(react|React|window|document|ssh2)' src/ui/lib/split-tree.ts` returns 0.
    - `DropEdge` type is NOT widened — verify: `grep -c '^export type DropEdge = "left" | "right" | "top" | "bottom"' src/ui/lib/split-tree.ts` returns 1 (unchanged from Phase 56).
    - `insertAtEdge`'s signature still accepts `edge: DropEdge` (not `DropZone`) — verify: `grep -c 'edge: DropEdge' src/ui/lib/split-tree.ts` returns at least 1.
    - Test file adds the new describe block — verify: `grep -c '^describe("split-tree — Phase 57: edge-zone hit-testing"' src/ui/lib/split-tree.test.ts` returns 1.
    - Test file imports the new function — verify: `grep -c 'computeEdgeZone' src/ui/lib/split-tree.test.ts` returns at least 14 (import line + 13 test bodies).
    - All 13 new tests pass — verify: `npx vitest run src/ui/lib/split-tree.test.ts 2>&1 | grep -E "✓|passed"` shows all Phase-57 tests green AND all 11 pre-existing Phase 56 tests green (no regression).
    - Scoped-related check green — verify: `cd /home/ubuntu/skynet-tanya && npx vitest run --related src/ui/lib/split-tree.ts 2>&1 | tail -5` reports pass.
    - TypeScript compiles clean — verify: `cd /home/ubuntu/skynet-tanya && npx tsc --noEmit 2>&1 | grep -c "error TS"` returns 0.
  </acceptance_criteria>
  <done>The `computeEdgeZone` pure function is exported, tested against 13 cases including the four edge midpoints, four corner-tie tie-break priorities, dead center, both sides of the 0.28 threshold, a non-square rect, an off-origin rect, and a defensive out-of-rect cursor. Phase 56's `computeNearestEdge` and `DropEdge` type remain unchanged so no downstream consumer regresses. Full split-tree test suite is green including the pre-existing 11 Phase 56 tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — pure function, no external inputs) | `computeEdgeZone` is a pure function of a DOMRect + two numbers. Both inputs come from the browser's own `getBoundingClientRect()` and `DragEvent.clientX/Y`, both browser-controlled. No user-controllable string / JSON / URL crosses this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01-NONE | (n/a) | computeEdgeZone | accept | Pure UI geometry helper. No auth surface, no network surface, no user-input parsing surface. Cursor coordinates originate from the browser's DragEvent, which the browser owns. Rect coordinates originate from `getBoundingClientRect()`, which the browser owns. There is no realistic tampering / STRIDE surface — the function is a mathematical mapping from `(rect, x, y) → enum` with no side effects. Documented explicitly here per Phase 57 security_config: "PURE FRONTEND UI ... realistic threat surface: none". |
</threat_model>

<verification>
- `npx vitest run src/ui/lib/split-tree.test.ts` — all 24 test cases pass (11 pre-existing Phase 56 + 13 new Phase 57).
- `npx vitest run --related src/ui/lib/split-tree.ts` — scoped-related run green (should also cover `split-tree-url.test.ts` and `SplitView.test.tsx` since they transitively depend on split-tree.ts; Phase 56 assertions continue to hold).
- `npx tsc --noEmit` — zero errors.
- `git diff --stat` — exactly two files touched: `src/ui/lib/split-tree.ts` (add ~20 lines: type + function + comment) and `src/ui/lib/split-tree.test.ts` (add ~80 lines: new describe block with 13 tests).
</verification>

<success_criteria>
1. `computeEdgeZone` exported as a pure function matching prototype.html:361-370 semantics (EDGE_ZONE_THRESHOLD = 0.28, tie-break top → bottom → left → right).
2. `DropZone` type alias exported as `DropEdge | "center"`; `DropEdge` unchanged.
3. All 13 new unit tests pass, covering: 4 edge-midpoints, 4 corner-tie tie-breaks, dead center, both sides of the 0.28 threshold boundary, non-square rect, off-origin rect, defensive out-of-rect cursor.
4. Zero regressions to Phase 56's 11 existing split-tree tests.
5. `npx tsc --noEmit` clean.
6. `npx vitest run --related src/ui/lib/split-tree.ts` green.
</success_criteria>

<output>
Create `.planning/phases/57-drop-preview-overlay-edge-zone-hit-testing-replace-placehold/57-01-SUMMARY.md` when done, documenting:
- Confirmation that `EDGE_ZONE_THRESHOLD = 0.28` (matching prototype.html).
- Confirmation that the tie-break priority is `top → bottom → left → right` (matching prototype's if-chain at :366-369).
- Confirmation that `computeNearestEdge` and `DropEdge` are unchanged (grep evidence: exact one-line matches on the pre-existing signatures).
- Confirmation that the exported `DropZone = DropEdge | "center"` does NOT leak into any Phase-56 consumer signature (e.g. `insertAtEdge` still takes `edge: DropEdge`).
- Concrete test-run output (line count of passing tests) as evidence for the scoped test gate.
</output>
</content>
