---
phase: 57-drop-preview-overlay-edge-zone-hit-testing-replace-placehold
plan: 02
type: execute
wave: 2
depends_on: [57-01]
files_modified:
  - src/ui/shell/SplitView.tsx
  - src/ui/shell/SplitView.test.tsx
autonomous: true
requirements:
  - PV57-DROP-PREVIEW-OVERLAY
  - PV57-FLICKER-FIX
  - PV57-CENTER-DEAD-ZONE-SHORT-CIRCUIT
  - PV57-STRUCTURED-LOGGING
  - PV57-SNAP-TO-NEAREST-EDGE
tags:
  - drop-preview
  - pane
  - flicker-fix
  - structured-logging
  - phase-57
must_haves:
  truths:
    - "Every `Pane` cell (leaf of the split tree) renders a coral-tinted rectangular overlay while a drag with `text/plain` in dataTransfer.types is hovering over its screen area. The overlay is HALF the Pane's dimensions along whichever edge zone the cursor is currently in (top/bottom/left/right — snap-to-nearest-edge per prototype.html:414-427 with t=0.5); it updates on every `dragover`; it hides immediately when the cursor enters the center dead zone AND when the drag leaves the Pane's bounding rect entirely."
    - "The overlay does NOT flicker when the cursor crosses child-DOM boundaries inside the Pane's screen area — moving over portaled PrettyView content (message bubbles, aside bubbles, compose box) does not toggle the overlay off/on. Fix mechanism: bounding-rect guard on the `dragleave` native listener — hide the overlay only when `e.clientX/e.clientY` are outside the Pane's outer div's `getBoundingClientRect()` (stateless, robust against React-portal / DOM-tree mismatches). This is CONTEXT.md §Gap (a) — the bounding-rect guard is the recommended fix per CONTEXT.md rationale."
    - "A drop event fired while the cursor is in the center dead zone short-circuits INSIDE the Pane's `drop` handler: `e.preventDefault()` + `e.stopPropagation()` still fire so the AppShell outer container's onDrop (SplitView.tsx AppShell.tsx:2265) does NOT act on the payload; but `onOpenSessionInTree` / `onDropRowInTree` are NOT called. A structured log line `[pv-split-drop] center-dead-zone ignored path=... clientX=... clientY=...` is emitted. NO error, NO toast, NO visual 'invalid drop' affordance — silent per shape file §What would make it wrong 'Center dead zone unclear where its boundaries are' and §Vehicle Phase 2 'release does nothing, no drop registered'."
    - "A drop event fired while the cursor is in an edge zone continues to call the same handlers with the same `(payload, path, edge)` signature Phase 56 established — no change to the Pane's outward contract. AppShell's `openSessionInTree` (AppShell.tsx:1555) and `onDropRowInTree` (AppShell.tsx:1714) work unchanged. The `edge` argument passed to those handlers comes from `computeEdgeZone` (Plan 57-01), narrowed to `DropEdge` because the center case has already short-circuited above."
    - "Every dragover-that-changes-zone is logged with a `[pv-split-preview]` prefix: `[pv-split-preview] pane path=[...] zone=<zone> clientX=<n> clientY=<n> rectLTRB=<l>,<t>,<r>,<b>`. Zone-unchanged dragovers do NOT log (avoid log spam — dragover fires at every pixel of cursor motion; logging every one would flood the console)."
    - "Overlay renders as a direct child of the Pane's `.flex-1.min-h-0.overflow-hidden.relative` wrapper (SplitView.tsx:296), sibling to the portal-target `[data-tab-id]` div (:297-301) — i.e. IN-PLACE replacement of the Phase 56 placeholder at :302-308 with the same parent and same DOM depth. Not portaled to document; cannot leak into unrelated surfaces. Uses inline `position: absolute; left/top/width/height: <computed>` to snap to the winning edge (half the pane along that edge, prototype.html:414-421, t=0.5). `pointer-events: none` so the overlay does not intercept the drag events beneath it (dragover fires on the Pane's outer div, not on the overlay)."
    - "State shape replaces Phase 56's `const [isDragOver, setIsDragOver] = useState(false)` with `const [dropPreview, setDropPreview] = useState<{ zone: DropZone; rect: DOMRect } | null>(null)`. Empty state = no drag over (or drag over center dead zone). The `rect` is captured at `dragover` time from `el.getBoundingClientRect()` so the overlay uses the current pane dimensions even if the layout reflows between dragovers."
    - "Phase 56 test surface continues to pass: `SplitView.test.test.tsx` Tests 1-8 (both describe blocks — recursive-tree renderer + Plan 03 nearest-edge geometry) remain green. Phase 57 tests are ADDED to the file in a new `describe('SplitView — Phase 57: drop-preview overlay + edge-zone hit-testing')` block."
  artifacts:
    - path: "src/ui/shell/SplitView.tsx"
      provides: "MODIFIED — `Pane` component (currently at :140-268). State shape swap (isDragOver → dropPreview), native-listener rewire (dragover computes zone via computeEdgeZone from Plan 57-01, dragleave has bounding-rect guard, drop short-circuits on center), coral overlay div replaces the placeholder 'Drop to split' div at :302-308."
      contains: "computeEdgeZone"
    - path: "src/ui/shell/SplitView.test.tsx"
      provides: "MODIFIED — new `describe('SplitView — Phase 57: drop-preview overlay + edge-zone hit-testing')` block appended after the existing Phase 56 Plan 03 describe block. Extends the existing `dispatchDropAt` / `findPaneOuter` helpers with `dispatchDragOverAt` / `dispatchDragLeaveAt` for the new event flow."
  key_links:
    - from: "src/ui/shell/SplitView.tsx (Pane component)"
      to: "src/ui/lib/split-tree.ts (Plan 57-01)"
      via: "Pane's native `dragover` handler imports and calls `computeEdgeZone(rect, e.clientX, e.clientY)` to pick the zone for the overlay AND for the drop short-circuit"
      pattern: "computeEdgeZone\\("
    - from: "src/ui/shell/SplitView.tsx (Pane component)"
      to: "src/ui/AppShell.tsx"
      via: "Pane's drop handler continues to invoke `onOpenSessionInTree(tabId, path, edge)` and `onDropRowInTree(payload, path, edge)` with a `DropEdge` (never `'center'`) — the outward contract is unchanged from Phase 56"
      pattern: "onOpenSessionInTree|onDropRowInTree"
    - from: "src/ui/shell/SplitView.test.tsx (new describe block)"
      to: "src/ui/shell/SplitView.tsx (Pane)"
      via: "vitest component tests render `<SplitView splitTree={leaf('aaa')} ...>` and dispatch native DragEvent-shaped events at the Pane outer div, asserting dropPreview state + overlay presence + handler-call semantics"
      pattern: "SplitView.*Phase 57"
---

<objective>
Rewire the `Pane` component in `src/ui/shell/SplitView.tsx` to deliver Phase 57's whole point: live coral drop-preview overlay that tracks the cursor via edge-zone hit-testing, with a silent center dead zone and a bounding-rect flicker fix. Consumes `computeEdgeZone` from Plan 57-01. Zero changes to the Pane's outward contract (`onOpenSessionInTree` + `onDropRowInTree` still receive `(payload, path, edge: DropEdge)` for edge-zone drops; center-dead-zone drops short-circuit silently inside the Pane and never call those handlers).

Purpose: Ashley's Phase 56 UAT confirmed drag-drop works end-to-end but surfaced two gaps this plan folds: (a) placeholder overlay flickers when the cursor crosses child-DOM boundaries inside portaled PrettyView content, (b) edge selection is unpredictable near equidistant points because it's computed only at drop-time with no visual preview. This plan replaces both with the prototype-validated interaction: live overlay showing the future split shape BEFORE release, snap-to-nearest-edge on every dragover, silent center dead zone as a "cancel" gesture. Structured `[pv-split-preview]` logging backfills observability per box-maintainer standing directive.

Output: TWO modified files — `SplitView.tsx` (Pane component rewire, ~30 lines added/changed in the useEffect and JSX) and `SplitView.test.tsx` (~120 lines added in a new describe block covering overlay presence, zone-driven overlay geometry, flicker fix, center-dead-zone drop short-circuit, edge-zone drop preserves handler contract, structured-log emission). `npx vitest run --related src/ui/shell/SplitView.tsx src/ui/lib/split-tree.ts` green + `npx tsc --noEmit` clean is the observable end state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/57-drop-preview-overlay-edge-zone-hit-testing-replace-placehold/57-CONTEXT.md
@.planning/phases/57-drop-preview-overlay-edge-zone-hit-testing-replace-placehold/57-01-PLAN.md
@.planning/shapes/shape-visual-session-management.md
@CLAUDE.md
@src/ui/shell/SplitView.tsx
@src/ui/shell/SplitView.test.tsx
@src/ui/lib/split-tree.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire Pane component — replace isDragOver with dropPreview state, add computeEdgeZone-driven overlay + bounding-rect flicker fix + center-dead-zone drop short-circuit + structured [pv-split-preview] logging</name>
  <files>
    src/ui/shell/SplitView.tsx,
    src/ui/shell/SplitView.test.tsx
  </files>
  <read_first>
    - src/ui/shell/SplitView.tsx (READ IN FULL, especially :140-311 — the `Pane` component. Note: patch #514 native DOM listener attachment at :208-268 is LOAD-BEARING and this task rewires the listener bodies but MUST keep the native-listener attachment mechanism intact. Note the `isDragOver` state at :172, the `dragover`/`dragleave`/`drop` handlers at :211-256, and the placeholder overlay JSX at :302-308.)
    - src/ui/lib/split-tree.ts (READ AFTER Plan 57-01 lands — the module now exports `computeEdgeZone` + `DropZone`. This task imports both.)
    - src/ui/shell/SplitView.test.tsx (READ IN FULL — the existing Vitest suite. Note the two existing describe blocks (Plan 02 + Plan 03) and the shared helpers `dispatchDropAt` at :276-294 and `findPaneOuter` at :298-305. This task appends a new describe block using the same helpers plus a new `dispatchDragOverAt` helper matching the same jsdom-compatible pattern.)
    - src/ui/AppShell.tsx (READ :1555-1735 — `openSessionInTree` and `onDropRowInTree` callbacks. Confirm their signature is `(tabId: string, path: SplitPath, edge: DropEdge)` and `(payload: unknown, path: SplitPath, edge: DropEdge)` respectively. Read :2265-2290 — the AppShell outer container's onDrop handler that this task's stopPropagation must continue to guard against.)
    - /home/ubuntu/.claude/roles/box-maintainer/bounties/bring-back-split-view/prototype.html (READ :177-188 for the drop-overlay CSS; :414-427 for `showOverlayForZone` — the exact geometry this task ports; :361-370 for `pickZone` — Plan 57-01 already ported this into `computeEdgeZone`, this task just uses it.)
    - .planning/phases/57-drop-preview-overlay-edge-zone-hit-testing-replace-placehold/57-CONTEXT.md (READ IN FULL — this is the plan-of-record for the whole phase. §In-scope items 2/3/4/5 and §What would make Phase 57 wrong are the acceptance-shape.)
  </read_first>
  <behavior>
    - Test 1 (state shape swap — pure structural check): The `Pane` component no longer uses `useState<boolean>(false)` for a variable named `isDragOver`. Instead it uses `useState<{ zone: DropZone; rect: DOMRect } | null>(null)` for a variable named `dropPreview`. Source-assertion, verified by grep.
    - Test 2 (dragover in an edge zone renders overlay): Mount a `<SplitView splitTree={leaf('aaa')} tabs={[tabA]} />` and mock the Pane outer div's `getBoundingClientRect()` to `{left:0, right:100, top:0, bottom:100, ...}`. Dispatch a native `dragover` event at (10, 50) (which `computeEdgeZone` returns `'left'` for). Assert: an element matching `[data-testid="pane-drop-preview-overlay"]` (or equivalent stable selector) is rendered as a child of the Pane's outer div, with computed inline styles matching the LEFT-half geometry: `left: 0, top: 0, width: 50, height: 100` (or the equivalent CSS-shorthand values applied via style prop / className).
    - Test 3 (dragover in top zone renders overlay on top half): Same setup, dispatch dragover at (50, 10). Assert overlay exists with `top: 0, height: 50, left: 0, width: 100` (top half of the pane).
    - Test 4 (dragover in center dead zone hides overlay): Same setup, dispatch dragover at (50, 50) — the exact center of a 100×100 rect. `computeEdgeZone` returns `'center'`. Assert: NO element matching the overlay selector is present in the DOM (or if the element is always present, its `data-zone` attribute is not `top|bottom|left|right` — executor picks the exact shape and asserts consistently). Assert the Pane's outer div does NOT have any ring/ring-inset drag-over affordance either — center dead zone is visually SILENT per shape file lock.
    - Test 5 (dragleave outside rect hides overlay): Setup as Test 2, dispatch a dragover at (10, 50) to establish an overlay, then dispatch a native `dragleave` at (150, 50) — outside the 100×100 rect. Assert: overlay is now hidden (element absent, or `data-zone="none"`).
    - Test 6 (flicker fix — dragleave INSIDE rect does NOT hide overlay): Setup as Test 2, dispatch dragover at (10, 50) to establish left-zone overlay. Now dispatch a native `dragleave` at (30, 50) — still INSIDE the 100×100 rect (simulates cursor crossing a child-DOM boundary while still within the Pane). Assert: overlay is STILL PRESENT with the left-zone geometry (the guard's core purpose — flicker fix from CONTEXT.md §Gap (a)).
    - Test 7 (center-dead-zone drop is silent — no handler call): Setup as Test 2, dispatch a `drop` event at (50, 50) with `dataTransfer` carrying `text/plain: "newtab"`. Assert: `onOpenSessionInTree` was NOT called; `onDropRowInTree` was NOT called. Assert: a `console.info` spy captured a log line containing `[pv-split-drop] center-dead-zone ignored`.
    - Test 8 (edge-zone drop still calls handler with correct edge — regression against Phase 56 contract): Setup as Test 2, dispatch drop at (10, 50) (left zone) with `text/plain: "newtab"`. Assert: `onOpenSessionInTree` called exactly once with `("newtab", [], "left")` — same contract Phase 56 Plan 03 Test 6 asserts, PROVING this plan does not regress the outward contract.
    - Test 9 (dragover-changing-zone emits a `[pv-split-preview]` log; dragover-not-changing-zone does not): Setup as Test 2. Spy on `console.info`. Dispatch dragover at (10, 50) → left zone → assert exactly 1 new `[pv-split-preview] pane path=[] zone=left` log line. Dispatch dragover at (15, 50) — still left zone — assert NO additional log line was emitted (the zone did not change, so no new log). Dispatch dragover at (50, 10) — now top zone — assert exactly 1 additional `[pv-split-preview] pane path=[] zone=top` log line.
    - Test 10 (overlay is pointer-events: none): Assert the overlay element has `style.pointerEvents === 'none'` (or equivalent Tailwind class `pointer-events-none`). This guarantees dragover events fire on the Pane's outer div underneath, not on the overlay — otherwise the overlay would swallow drag events and the whole interaction would freeze.
    - Test 11 (deep-tree — dragover in cell B of a 2-cell vertical split renders overlay only in cell B): Mount `<SplitView splitTree={split('vertical', leaf('aaa'), leaf('bbb'))} tabs={[tabA, tabB]} />`. Mock cell B's outer div `getBoundingClientRect` to `{left:100, right:200, top:0, bottom:100, ...}`. Dispatch dragover at (190, 50) inside cell B. Assert: an overlay exists that is a descendant of cell B's outer div, NOT of cell A's outer div. Overlay geometry: right half of cell B (`left: 50, top: 0, width: 50, height: 100` in cell-local coords; the executor picks whether coordinates are cell-local via inset/absolute or viewport-absolute — the assertion is that the overlay is inside cell B only).
    - Test 12 (Phase 56 regression — all 8 existing Plan 02 + Plan 03 tests still pass): No new assertion; this is verified by the scoped test run passing all tests in the file.
    - Test 13 (window dragend cleanup — Escape-cancel scenario): Setup as Test 2 (dragover at (10, 50) establishes left-zone overlay). Dispatch a `dragend` event on `window` (simulates Escape-cancel or drop-onto-non-Pane-target): `fireEvent(window, new Event("dragend"))`. Assert: the overlay is GONE from the DOM (`container.querySelector('[data-testid="pane-drop-preview-overlay"]')` returns null). This closes the plan-check finding #7 hole where an overlay would persist stuck after an Escape-cancel because no dragleave fires. Also acts as a regression guard against a future refactor accidentally scoping the dragend listener to the Pane's outer div (which would miss the event — dragend fires on the SOURCE).
  </behavior>
  <action>
    Modify `src/ui/shell/SplitView.tsx`:

    1. **Import update.** Update the type import at :27 from `import type { SplitNode, SplitPath, DropEdge } from "@/lib/split-tree"` to also import the new `DropZone` type AND the runtime `computeEdgeZone` function. Suggested split: keep the type-only import for the three existing types + `DropZone`, add a separate runtime `import { computeEdgeZone } from "@/lib/split-tree"`. If a bundler cost is a concern, use a single mixed import.

    2. **State shape swap in Pane.** Replace `const [isDragOver, setIsDragOver] = useState(false)` at :172 with:

       `const [dropPreview, setDropPreview] = useState<{ zone: DropZone; rect: DOMRect } | null>(null)`

       Rationale JSDoc/comment inline: cite CONTEXT.md §In-scope item 2 (live cursor-tracking overlay) and Plan 57-01's `computeEdgeZone` as the zone-source. The stored `rect` is captured once per zone-changing dragover so the overlay geometry is stable across renders even if a mid-drag layout reflow occurs.

    3. **Rewire `onDragOver` inside the useEffect at :211-216.** New body (pseudocode):

       - Preserve the type gate: `if (!e.dataTransfer?.types.includes("text/plain")) return`.
       - `e.preventDefault(); e.stopPropagation();` — unchanged from Phase 56.
       - `const rect = el.getBoundingClientRect()`.
       - `const zone = computeEdgeZone(rect, e.clientX, e.clientY)`.
       - Compare `zone` against the previous zone via a REF (COMMITTED CHOICE per plan-check finding #4 — do NOT use a functional-updater on setDropPreview for this comparison; reducers must be pure and React 18 strict mode double-invokes them, which would double-fire the structured log). Declare `const prevZoneRef = useRef<DropZone | null>(null)` INSIDE the Pane component alongside the existing `outerRef` declaration (add `useRef` to the import from `"react"` if not already imported). In the dragover handler: `if (zone !== prevZoneRef.current) { console.info(\`[pv-split-preview] pane path=${JSON.stringify(path)} zone=${zone} clientX=${Math.round(e.clientX)} clientY=${Math.round(e.clientY)} rectLTRB=${Math.round(rect.left)},${Math.round(rect.top)},${Math.round(rect.right)},${Math.round(rect.bottom)}\`); prevZoneRef.current = zone; }`. The ref-write happens synchronously inside the dragover handler (not inside a setState), guaranteeing single-fire semantics even under strict mode's double-invocation of function bodies (React does not double-invoke effect handlers, only render bodies).
       - Then unconditionally call `setDropPreview({ zone, rect })` — React bails out if the new value is structurally identical, but with a fresh DOMRect object every dragover we want React to skip re-render when the zone hasn't changed. Use a functional-updater that returns the prev object if `prev !== null && prev.zone === zone` (structural bailout — avoids re-render churn on cursor movement WITHIN a zone).

    4. **Rewire `onDragLeave` inside the useEffect at :217 — bounding-rect flicker fix.** New body:

       - **Type-gate FIRST** (mirrors dragover at :211-212): `if (!e.dataTransfer?.types.includes("text/plain")) return;`. Without this gate, unrelated drag events (browser file drags, native OS drags, drags without any payload) would clear `dropPreview` mid-drag whenever the browser fires a spurious dragleave on the pane — the type gate scopes the flicker-fix machinery to our own row-drag payload only. (Plan-check finding #3 fix.)
       - Read cursor position from the event: `const rect = el.getBoundingClientRect()`.
       - Check if cursor is still inside: `const stillInside = e.clientX >= rect.left && e.clientX <= rect.right && e.clientY >= rect.top && e.clientY <= rect.bottom`.
       - Only if `!stillInside`: `setDropPreview(null); prevZoneRef.current = null;` (reset the ref alongside the state — see step 3's ref declaration; failing to reset means the next dragover-back-into-the-pane won't emit a `[pv-split-preview]` log because the ref still holds the last zone).
       - Comment INLINE citing CONTEXT.md §Gap (a) rationale for the bounding-rect approach vs counter-based / relatedTarget-based: "Bounding-rect guard is stateless (no counter to keep in sync across concurrent enter/leave pairs) and robust against React-portal / DOM-tree mismatches — the portaled PrettyView children live in this Pane's DOM subtree but their React parent is AppShell's normal-view container, so `e.relatedTarget` + `el.contains()` can be unreliable across React's portal-boundary edge cases."
       - **jsdom test-helper note** (planning-time reference for Task step 3 of the test mods): `dragleave` events in jsdom, like `drop` events, ignore `clientX`/`clientY` passed via constructor init. The `dispatchDragLeaveAt` helper MUST replicate the exact pattern used by the Phase 56 `dispatchDropAt` at SplitView.test.tsx:276-294 — `createEvent.dragLeave(el)` + `Object.defineProperty(evt, "clientX", { value: clientX, configurable: true })` + same for clientY + `fireEvent(el, evt)` — with a `dataTransfer` stub carrying `types: ["text/plain"]` so the type gate above does not early-return in the test.

    5. **Rewire `onDrop` inside the useEffect at :218-256.** New body:

       - Preserve the type gate: `if (!e.dataTransfer?.types.includes("text/plain")) return`.
       - `e.preventDefault(); e.stopPropagation();` — unchanged.
       - `setDropPreview(null)` — clear the overlay immediately (drop always ends the drag).
       - `const rect = el.getBoundingClientRect()`.
       - `const zone = computeEdgeZone(rect, e.clientX, e.clientY)`.
       - **Center-dead-zone short-circuit** (NEW): `if (zone === "center") { console.info(\`[pv-split-drop] center-dead-zone ignored path=${JSON.stringify(path)} clientX=${Math.round(e.clientX)} clientY=${Math.round(e.clientY)}\`); return; }` — no handler call, no error, no toast, silent.
       - Otherwise (edge zone): narrow `zone` to `DropEdge` via TS assertion / type guard (executor picks the cleanest form — `const edge: DropEdge = zone;` after the center-check narrows it via TS control-flow analysis if the union is discriminated correctly; if TS doesn't narrow, use a `zone as DropEdge` cast with a comment explaining the center case is already returned above).
       - Preserve the existing Phase 56 rich-payload / text-plain fallback branching from :227-255 verbatim, substituting the new `edge` variable for the Phase 56 `computeNearestEdge(...)` call at :225. This means DELETING the `computeNearestEdge` call at :225 and using the `edge` from `computeEdgeZone` above — one edge picker per drop, driven by the same helper the overlay uses (guarantees preview and actual land match; CONTEXT.md §What would make it wrong "Overlay geometry doesn't match what actually happens on drop").
       - Update the existing `[pv-split-drop] pane path=... edge=...` log line at :229-231 to include `zone` explicitly: `[pv-split-drop] pane path=[...] zone=<edge> edge=<edge> ...` (redundant on edge-zone drops but useful for grep-diffing between edge and center paths in log postmortems).

    6. **Overlay JSX — REPLACES the existing placeholder in-place at :302-308, SAME PARENT.** Delete the current `{isDragOver && (...)}` block. Insert the new `{dropPreview !== null && dropPreview.zone !== "center" && (...)}` block at the EXACT SAME nesting depth as the placeholder it replaces — i.e. INSIDE the `<div className="flex-1 min-h-0 overflow-hidden relative">` wrapper at SplitView.tsx:296, as a SIBLING of the portal-target `<div ref={contentRef} data-tab-id={tabId} className="absolute inset-0" />` at :297-301. Do NOT re-parent it to the outer div or to a new wrapper. (Plan-check finding #2 fix: earlier draft's truth #6 said "outer div" but Task step 6 said "inside the outer div's `.absolute.inset-0` child" — the correct answer is neither of those descriptions verbatim. The overlay sits at the same DOM depth as the current placeholder: a direct child of `.flex-1.min-h-0.overflow-hidden.relative` at :296, sibling to the `[data-tab-id]` portal-target div.) Renders a coral-tinted overlay div whose CSS `left/top/width/height` inline styles come from a helper `overlayGeometryForZone(zone, rect)` returning `{ left, top, width, height }` in PANE-LOCAL coordinates (i.e. as if the `.flex-1` wrapper — which equals the outer div's non-header area, and after patch #512 removed the header chrome equals the outer div's full area — were the coordinate origin; the values map to `left: 0` for left-zone, `top: 0` for top-zone, `left: width/2` for right-zone, `top: height/2` for bottom-zone; each covers half the pane's dimension along the winning edge, `t = 0.5` matching prototype.html:415-421). Helper placement: a small module-local function `overlayGeometryForZone(zone: Exclude<DropZone, 'center'>, rect: DOMRect): { left: number; top: number; width: number; height: number }` defined inline in SplitView.tsx above the Pane component. Export it if unit-testing the geometry separately is helpful (executor's judgment — the Vitest suite can also assert via DOM inspection).

       Overlay CSS (inline style + Tailwind, matching the pane's design language):

       - `position: absolute` (via `absolute` class or inline).
       - `left, top, width, height` inline from the helper.
       - Fill: `background: 'rgba(255, 184, 150, 0.22)'` — matches the prototype's `--highlight: rgba(255,184,150,0.20)` at prototype.html:15 with a small nudge for the fork's darker base. RGB values match the app's `--color-pv-code-fg: #ffb896` at index.css:159 (CONTEXT.md's "warm coral" ballpark). If a future refactor extracts the alpha into a CSS token, do it in a follow-up; for now inline the rgba per Ashley's shape-file preference for legible-first CSS.
       - Border: `border: '2px solid rgba(255, 184, 150, 0.60)'` — matches prototype `--highlight-strong: rgba(255,184,150,0.55)` at prototype.html:16.
       - `border-radius: 0` (or `rounded-none` class) — matches the pane's clean chrome-less edges post patch #512.
       - `pointer-events: none` (Tailwind `pointer-events-none`) — CRITICAL, otherwise the overlay would swallow drag events and the whole interaction would freeze.
       - `z-index: 20` — matches the current placeholder overlay's z-index at :303.
       - `transition: 'left 80ms ease, top 80ms ease, width 80ms ease, height 80ms ease'` — matches prototype line 185 for the snap-to-edge motion feel.
       - `data-testid="pane-drop-preview-overlay"` — stable selector for the Vitest suite.
       - `data-zone={dropPreview.zone}` — assertable via testing-library queries.

    7. **Remove the ring-ring-inset drag-over class on the outer div at :275.** The `${isDragOver ? "ring-2 ring-inset ring-accent-brand" : ""}` template segment gets DELETED. Rationale: the coral overlay is now the drag-over affordance; the additional ring was Phase 56 placeholder chrome that becomes visual noise on top of the coral overlay. Preserve the `${isFocused ? "ring-1 ring-inset ring-accent-brand/30" : ""}` segment — focus rings are separate from drag affordances and stay.

    8. **useEffect dependency array update.** The current deps at :268 are `[path.join("."), onDropRowInTree, onOpenSessionInTree]`. Add nothing new — `computeEdgeZone` is a stable module-level function import (React does not need it in the deps), `setDropPreview` is stable across renders. Leave `path.join(".")` as-is (still needed because path is a fresh array per render).

    9. **Delete the unused `computeNearestEdge` import inside the Pane's useEffect at :225.** After this rewire, the Pane no longer calls `computeNearestEdge` directly — `computeEdgeZone` is the only edge picker at the drop site. `computeNearestEdge` REMAINS EXPORTED from SplitView.tsx (its function definition at :44-69 stays untouched) because Phase 56's `SplitView.test.tsx` Plan 03 describe block imports it directly for unit-testing. Do NOT delete the function itself — only stop calling it from within Pane.

    10. **Attach a window-level `dragend` cleanup listener** (Plan-check finding #7 fix — Escape-to-cancel a drag while hovering a Pane does NOT fire a `dragleave` on the Pane, since the cursor doesn't move; without a cleanup path the overlay would persist stuck after an Escape-cancel). Inside the same useEffect that attaches the Pane's native dragover/dragleave/drop listeners, ALSO add:
       - `const onDragEnd = () => { setDropPreview(null); prevZoneRef.current = null; }`
       - `window.addEventListener("dragend", onDragEnd)` (window-scoped because `dragend` fires on the drag SOURCE — the conv-list row or the badge — not on the drop TARGET; scoping to the Pane's outer div would miss it entirely).
       - In the effect's cleanup: `window.removeEventListener("dragend", onDragEnd)`.
       - Reasoning comment inline: "Escape cancels a drag WITHOUT moving the cursor, so no dragleave fires; dragend on the source is the only reliable signal that the drag has ended. Window-level attach because dragend fires on the SOURCE element (conv-list row / identity badge), not on this Pane; scoping to `el` would miss it." The listener body is idempotent (nulling already-null state is a no-op), so this is safe even when a drop already cleared the state.

    Modify `src/ui/shell/SplitView.test.tsx`:

    1. **Extend imports.** Update the import from `"./SplitView"` at :30 to include the exported `overlayGeometryForZone` helper IF Task step 6's helper is exported (executor's choice — if not exported, tests query the DOM overlay's inline styles instead of asserting the helper directly).

    2. **Append a new describe block** at the end of the file (before or after the `describe("SplitView — Phase 56 Plan 03: nearest-edge drop geometry")` block — order doesn't matter for Vitest, but grouping together the two SplitView-Pane-focused blocks reads best). Title: `describe("SplitView — Phase 57: drop-preview overlay + edge-zone hit-testing", () => { ... })`.

    3. **Add a `dispatchDragOverAt(el, clientX, clientY, dataTransfer?)` helper AND a `dispatchDragLeaveAt(el, clientX, clientY)` helper — both must replicate the FULL Phase 56 `dispatchDropAt` shape at SplitView.test.tsx:276-294 verbatim, not a simplified version.** (Plan-check finding #1 fix.) Concretely, each helper MUST:
       - Use `createEvent.dragOver(el)` / `createEvent.dragLeave(el)` (NOT `new DragEvent(...)` — jsdom's DragEvent init does not honor clientX/clientY passed via the constructor, which is exactly why Phase 56's helper takes this route).
       - `Object.defineProperty(evt, "clientX", { value: clientX, configurable: true })` and same for `clientY` — the ONLY reliable way to set these fields on a jsdom-created DragEvent for the handler to read via `e.clientX` / `e.clientY`.
       - Attach a `dataTransfer` stub that carries `types: ["text/plain"] as readonly string[]` so the handler's type-gate (`e.dataTransfer?.types.includes("text/plain")`) does not early-return in the test. Default the stub to `{ types: ["text/plain"], getData: () => "" }` for `dispatchDragOverAt`; caller can override for tests that need a specific payload (e.g. Test 8 passes a `getData` that returns `"newtab"`). `dispatchDragLeaveAt` also needs the dataTransfer stub with `types: ["text/plain"]` because the new dragleave handler (Task step 4) type-gates on `text/plain` — without the stub, dragleave would early-return before evaluating the bounding-rect guard, and Test 6's flicker-fix assertion would pass for the wrong reason.
       - `fireEvent(el, evt)` to dispatch.
       - Reference Phase 56's `dispatchDropAt` at SplitView.test.tsx:276-294 as the template; both new helpers are structurally identical modulo the `createEvent.*` verb.

    4. **Implement Tests 1-11 above** as separate `it(...)` blocks. Naming convention: `it("Test N: <one-line summary>", () => { ... })`. Use the existing `findPaneOuter(container)` helper (at :298-305) to locate the Pane's outer div, and mock its `getBoundingClientRect` via `paneOuter.getBoundingClientRect = () => ({...} as DOMRect)` per the Phase 56 Plan 03 pattern.

    5. **For Test 7 and Test 9** (console.info assertions), use `vi.spyOn(console, 'info')` in a `beforeEach` block scoped to the new describe, then assert `expect(spy).toHaveBeenCalledWith(expect.stringContaining("[pv-split-drop] center-dead-zone ignored"))` etc. Remember to `spy.mockRestore()` in an `afterEach` to avoid leaking spies into other test files.

    6. **For Test 4 (center-dead-zone renders no overlay)**, either assert `container.querySelector('[data-testid="pane-drop-preview-overlay"]')` returns null OR (if the overlay is always mounted with `data-zone` set to `'none'` or similar) assert `data-zone !== "left" && data-zone !== "right" && data-zone !== "top" && data-zone !== "bottom"`. Executor picks the DOM shape and asserts consistently.

    7. **For Test 10 (pointer-events)**, if the executor used Tailwind `pointer-events-none` class, assert the class is present via `expect(overlay.className).toContain('pointer-events-none')`. If inline style, assert `overlay.style.pointerEvents === 'none'`.

    8. **Regression check note in the file header comment**: add a one-line comment near the top of the new describe block stating: "Phase 57 tests must not regress Phase 56's Plan 02 (Tests 1-6) or Plan 03 (Tests 1-8) — those blocks above continue to pass unchanged. This describe adds Phase 57's overlay + zone + flicker + center-dead-zone coverage."

    Do NOT touch any other file. Do NOT modify `src/ui/AppShell.tsx`. Do NOT modify `src/ui/lib/split-tree.ts` (Plan 57-01 already added what this task needs). Do NOT modify `src/ui/lib/split-tree-url.ts` or `PrettyView.tsx` or `PrettyConversationRow.tsx` — those are locked. Do NOT rename the Pane component or change its props signature. Do NOT change any Phase 56 test assertion or test title — Phase 56 tests are load-bearing regression guards.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tanya && (set -o pipefail; npx vitest run --related src/ui/shell/SplitView.tsx src/ui/lib/split-tree.ts 2>&1 | tail -40)</automated>
  </verify>
  <acceptance_criteria>
    - `Pane` uses the new dropPreview state — verify: `grep -c 'useState<{ zone: DropZone; rect: DOMRect } | null>' src/ui/shell/SplitView.tsx` returns 1.
    - The old `isDragOver` state is removed from Pane — verify: `grep -v '^\s*//\|^\s*\*' src/ui/shell/SplitView.tsx | grep -c 'setIsDragOver\|isDragOver'` returns 0 in the Pane component (Note: the EmptyDropTarget at :97-134 still uses its own `isDragOver` — that is CORRECT and separate; this grep is loose but the executor MUST confirm by inspection that ONLY the EmptyDropTarget still references isDragOver, and NOT the Pane).
    - The Pane imports `computeEdgeZone` — verify: `grep -c 'computeEdgeZone' src/ui/shell/SplitView.tsx` returns at least 2 (import + at least one call site; likely 3: import + dragover + drop).
    - The Pane imports `DropZone` type — verify: `grep -c 'DropZone' src/ui/shell/SplitView.tsx` returns at least 2.
    - The overlay JSX is present and gated on non-center zone — verify: `grep -c 'dropPreview.zone !== "center"' src/ui/shell/SplitView.tsx` returns 1.
    - Overlay has the stable data-testid — verify: `grep -c 'data-testid="pane-drop-preview-overlay"' src/ui/shell/SplitView.tsx` returns 1.
    - Center-dead-zone drop short-circuit log line is present — verify: `grep -c '\[pv-split-drop\] center-dead-zone ignored' src/ui/shell/SplitView.tsx` returns 1.
    - Structured `[pv-split-preview]` log line is present — verify: `grep -c '\[pv-split-preview\] pane path=' src/ui/shell/SplitView.tsx` returns 1.
    - `computeNearestEdge` is STILL EXPORTED from SplitView.tsx (regression guard) — verify: `grep -c '^export function computeNearestEdge(' src/ui/shell/SplitView.tsx` returns 1.
    - `computeNearestEdge` is NO LONGER CALLED from within the Pane's useEffect — verify: get the byte range of the Pane useEffect via `awk '/useEffect\(\(\) => \{/{flag=1} flag; /\}, \[path.join/{flag=0}' src/ui/shell/SplitView.tsx | grep -c 'computeNearestEdge('` returns 0. (Alternate: `grep -n 'computeNearestEdge(' src/ui/shell/SplitView.tsx` should show at most 1 match — the function's own definition or export declaration line — and zero call sites; the Phase 56 call at :225 must be gone.)
    - The `ring-2 ring-inset ring-accent-brand` drag-over class on the outer div is REMOVED (isFocused ring stays) — verify: `grep -c 'isDragOver ? "ring-2 ring-inset ring-accent-brand"' src/ui/shell/SplitView.tsx` returns 0 (the Pane's className template no longer includes this segment).
    - Overlay has `pointer-events-none` — verify: `grep -c 'pointer-events-none' src/ui/shell/SplitView.tsx` returns at least 1 in the overlay JSX (also present elsewhere for the Divider at :78 — expect count ≥ 2).
    - Overlay CSS uses the coral color from the prototype/token — verify: `grep -c 'rgba(255, 184, 150' src/ui/shell/SplitView.tsx` returns at least 2 (fill + border) OR (if executor used a var reference) `grep -c 'color-pv-code-fg' src/ui/shell/SplitView.tsx` returns at least 2.
    - New describe block exists in the test file — verify: `grep -c 'describe("SplitView — Phase 57: drop-preview overlay' src/ui/shell/SplitView.test.tsx` returns 1.
    - Test file adds the new dispatch helpers — verify: `grep -c 'dispatchDragOverAt\|dispatchDragLeaveAt' src/ui/shell/SplitView.test.tsx` returns at least 4 (2 helper definitions + at least 2 call sites — likely many more).
    - All Phase 57 tests (Tests 1-11 + Test 13) pass — verify: `npx vitest run src/ui/shell/SplitView.test.tsx 2>&1 | grep -E "Test [0-9]+:"` shows the 12 new tests all passing.
    - Window-level `dragend` cleanup listener is attached — verify: `grep -c 'window.addEventListener("dragend"' src/ui/shell/SplitView.tsx` returns 1 AND `grep -c 'window.removeEventListener("dragend"' src/ui/shell/SplitView.tsx` returns 1 (matched attach + cleanup pair inside the Pane's useEffect).
    - Phase 56 regression — all pre-existing tests still pass — verify: `npx vitest run src/ui/shell/SplitView.test.tsx 2>&1 | tail -20` reports full pass (no failed/skipped from Plan 02's Tests 1-6 or Plan 03's Tests 1-8).
    - Scoped-related run green — verify: `cd /home/ubuntu/skynet-tanya && npx vitest run --related src/ui/shell/SplitView.tsx src/ui/lib/split-tree.ts 2>&1 | tail -5` reports pass.
    - TypeScript compiles clean — verify: `cd /home/ubuntu/skynet-tanya && npx tsc --noEmit 2>&1 | grep -c "error TS"` returns 0.
    - No new imports from React beyond what's already there — verify: `grep -c '^import React' src/ui/shell/SplitView.tsx` returns 1 (the existing line unchanged).
    - No AppShell / PrettyView / URL-codec files were touched — verify: `git diff --stat 2>&1 | grep -E 'AppShell|PrettyView|split-tree-url|PrettyConversationRow'` returns nothing (empty result).
  </acceptance_criteria>
  <done>Pane's drag/drop useEffect uses `computeEdgeZone` on every dragover to update `dropPreview` state; overlay renders in coral, snaps to the winning edge, hides in center dead zone and outside pane bounds; bounding-rect flicker fix prevents child-DOM boundary crossings from toggling the overlay; center-dead-zone drops short-circuit silently with a structured log line; edge-zone drops preserve the Phase 56 handler contract exactly; structured `[pv-split-preview]` log lines emit only on zone changes. All 11 Phase 57 vitest cases pass; all 14 Phase 56 vitest cases in the same file continue to pass; `npx tsc --noEmit` clean; scoped-related run green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DragEvent → Pane native listener | `dragover`/`dragleave`/`drop` events carry `clientX`, `clientY`, and `dataTransfer` from the browser. The browser owns these values; they are not user-controllable in the free-form sense (a user can move the mouse but not inject a string). The `dataTransfer.getData("text/plain")` payload IS user-controllable (row drag sets it via the conv-list-row dragstart handler), but this Plan does NOT touch payload handling — payload flows unchanged into the Phase-56 handlers `onOpenSessionInTree` / `onDropRowInTree`, which own the payload trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-02-NONE | (n/a) | Pane drop-preview overlay + edge-zone hit-testing | accept | PURE FRONTEND UI: no auth surface, no network surface, no persistence surface, no new payload-handling surface. Cursor coordinates originate from the browser's DragEvent; rect from `getBoundingClientRect()`; both browser-owned. Payload handling (the only user-controllable input in this data flow) is untouched — it continues to flow through the Phase-56 handlers `onOpenSessionInTree` (AppShell.tsx:1555) and `onDropRowInTree` (AppShell.tsx:1714) which already validated the payload trust boundary. The overlay renders via inline CSS + JSX with no `dangerouslySetInnerHTML`, no `innerHTML`, no interpolation of user-controllable strings — the only interpolated values are numeric geometry from `getBoundingClientRect()`. The `data-zone` attribute takes values from a fixed enum (`'left'|'right'|'top'|'bottom'|'center'`), never user input. Documented explicitly per Phase 57 security_config: "PURE FRONTEND UI ... realistic threat surface: none." No mitigation required beyond CI's normal TS strictness + vitest coverage. |
</threat_model>

<verification>
- **Mobile-leak guard: transitively covered by Phase 56 architecture** (plan-check finding #6 doc). SplitView is only mounted on desktop widths — see AppShell.tsx:2372's `{!isMobile && (<SplitView … />)}` gate. Since Pane is only rendered inside SplitView, and the coral overlay only renders inside Pane, the overlay cannot render on mobile. No new mobile media query, `useIsMobile` hook, or width check needed in this plan. Phase 57 preserves this invariant by design — it adds NO new mount points for the overlay, only rewires the Pane's internals.
- `npx vitest run --related src/ui/shell/SplitView.tsx src/ui/lib/split-tree.ts` — full scoped-related run green (should include: `SplitView.test.tsx`, `split-tree.test.ts`, `split-tree-url.test.ts`, and any AppShell test that transitively imports SplitView, e.g. `AppShell.split-tree.test.tsx`).
- `npx tsc --noEmit` — zero errors.
- `git diff --stat` — exactly two files modified: `src/ui/shell/SplitView.tsx` (~50 lines net change: state shape swap, listener body rewire, JSX overlay swap, one class removal, one call-site swap) and `src/ui/shell/SplitView.test.tsx` (~120 lines added: new describe block with 11 tests + 2 helpers). Zero untouched-file assertions per the acceptance-criteria grep.
- Manual visual smoke (optional, not gated in scoped-tests): open the app in dev, drag a conv-list row over a Pane, watch the coral overlay snap between edges as the cursor moves; release in dead center → no split; release near an edge → split lands where the overlay showed. This is the "Ashley can see where it will land" acceptance from CONTEXT.md §Vehicle Phase 2, verified live in orchestrator UAT, not in this executor's scope.
</verification>

<success_criteria>
1. Pane state shape is `{ zone: DropZone; rect: DOMRect } | null` instead of `boolean`.
2. `computeEdgeZone` (from Plan 57-01) is called on every native `dragover` to compute the current zone; overlay renders when zone is not `'center'`.
3. Coral overlay geometry matches prototype.html:414-421 exactly (half the pane along the winning edge, t=0.5), color values match prototype's `--highlight` + `--highlight-strong` (rgba(255,184,150,0.22) fill, rgba(255,184,150,0.60) border).
4. Bounding-rect flicker fix on `dragleave`: overlay stays visible when the cursor moves across child-DOM boundaries inside the Pane's screen area (Test 6 asserts this via a dragleave inside the mocked rect).
5. Center-dead-zone drop short-circuits silently: `e.preventDefault()` + `e.stopPropagation()` still fire (AppShell outer handler does not act); handlers not called; structured `[pv-split-drop] center-dead-zone ignored` log line emitted.
6. Edge-zone drop preserves Phase 56's Pane outward contract exactly — `onOpenSessionInTree(tabId, path, edge)` / `onDropRowInTree(payload, path, edge)` called with the correct `DropEdge`. Test 8 is the regression assertion.
7. Structured `[pv-split-preview] pane path=... zone=... clientX=... clientY=... rectLTRB=...` log line emits ONLY on zone changes, not on every dragover pixel-move. Test 9 asserts this.
8. Overlay is `pointer-events: none` and scoped inside the Pane's outer div (not portaled).
9. `computeNearestEdge` is still exported from SplitView.tsx (Phase 56 test regression guard) but no longer called from within the Pane's drop handler.
10. All 12 new Phase 57 tests (Tests 1-11 + Test 13) + all 14 pre-existing Phase 56 tests in `SplitView.test.tsx` are green.
12. Window-level `dragend` listener attached inside Pane's useEffect (Escape-cancel cleanup path) and paired with matching cleanup in the effect's return.
11. `npx tsc --noEmit` clean; scoped-related vitest run green.
</success_criteria>

<output>
Create `.planning/phases/57-drop-preview-overlay-edge-zone-hit-testing-replace-placehold/57-02-SUMMARY.md` when done, documenting:
- Exact overlay CSS shipped (fill rgba, border rgba, transition string, z-index, pointer-events value) — with grep evidence.
- Confirmation that `computeNearestEdge` is untouched (still exported, no signature change) and is NO LONGER called inside the Pane — grep evidence for both.
- The exact stopPropagation guard chain — confirm the center-dead-zone drop still calls `e.preventDefault()` and `e.stopPropagation()` before returning, so the AppShell outer container's onDrop at AppShell.tsx:2265 does not fire on the payload.
- Test-run output: line count of passing tests in `SplitView.test.tsx` (should be 26 = 6 Plan 02 + 8 Plan 03 + 12 Plan 57 [Tests 1-11 + Test 13; there is no "Test 12" because Plan 57's Test 12 is a documentation-only regression assertion satisfied by the overall pass, not a discrete `it()` block]).
- Any deviation from the recommended structure (e.g. helper function name, whether overlayGeometryForZone was exported for direct testing) and why.
- Confirm zero files outside `src/ui/shell/SplitView.tsx` + `src/ui/shell/SplitView.test.tsx` were modified via `git diff --stat`.
</output>
</content>
