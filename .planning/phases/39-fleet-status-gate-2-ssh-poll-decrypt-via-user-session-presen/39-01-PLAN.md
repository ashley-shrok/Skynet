---
phase: 39-fleet-status-gate-2-ssh-poll-decrypt-via-user-session-presen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/fleet-status/subscription-registry.ts
  - src/backend/fleet-status/subscription-registry.test.ts
  - src/backend/fleet-status/ssh-poll-orchestrator.test.ts
autonomous: true
requirements:
  - GATE2-01
  - GATE2-02
must_haves:
  truths:
    - "SubscriptionRegistry exposes onFirstSubscriber(cb) and onLastUnsubscriber(cb) methods"
    - "subscribe(sendFrame, ctx?) accepts an optional { userId } context"
    - "onFirstSubscriber callback fires exactly when subscribers.size transitions 0 -> 1"
    - "onLastUnsubscriber callback fires exactly when subscribers.size transitions 1 -> 0 via disposer"
    - "All 7 existing subscription-registry.test.ts tests still pass"
    - "MockRegistry in ssh-poll-orchestrator.test.ts compiles against the extended interface"
  artifacts:
    - path: "src/backend/fleet-status/subscription-registry.ts"
      provides: "Extended SubscriptionRegistry interface + factory with lifecycle callback registration"
      contains: "onFirstSubscriber"
    - path: "src/backend/fleet-status/subscription-registry.test.ts"
      provides: "New tests covering first-subscriber ctx delivery + last-unsubscriber fire + disposer returns"
      contains: "onFirstSubscriber"
  key_links:
    - from: "src/backend/fleet-status/subscription-registry.ts"
      to: "src/backend/fleet-status/ssh-poll-orchestrator.test.ts:93 MockRegistry"
      via: "structural TS interface satisfaction"
      pattern: "implements SubscriptionRegistry"
---

<objective>
Extend the SubscriptionRegistry with two lifecycle callback registrars — onFirstSubscriber(cb) and onLastUnsubscriber(cb) — plus thread an optional { userId } context through subscribe(). Foundation for Path C presence-driven lifecycle (D-CTX §Decisions "LOCKED by Ashley 2026-08-13"). Per D-01 (GATE2-01) and D-02 (GATE2-02): first subscriber wires poller start, last unsubscriber wires poller stop.

Purpose: Provide the presence signal that Plan 02 will wire the SSH-poll orchestrator to. Isolated, additive, testable in one plan; no runtime coupling to any consumer yet.

Output: subscription-registry.ts extended interface + factory. MockRegistry stub in ssh-poll-orchestrator.test.ts updated so downstream plans do not break TS. Two new tests in subscription-registry.test.ts covering the new hooks (first-sub fires with ctx, last-unsub fires when disposer runs and subscribers drops to 0, disposer for onFirstSubscriber/onLastUnsubscriber unregisters the callback).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/39-fleet-status-gate-2-ssh-poll-decrypt-via-user-session-presen/39-CONTEXT.md
@.planning/phases/39-fleet-status-gate-2-ssh-poll-decrypt-via-user-session-presen/39-RESEARCH.md
@src/backend/fleet-status/subscription-registry.ts
@src/backend/fleet-status/subscription-registry.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend SubscriptionRegistry interface + factory with lifecycle callbacks + ctx-carrying subscribe</name>
  <files>src/backend/fleet-status/subscription-registry.ts</files>
  <read_first>
    - src/backend/fleet-status/subscription-registry.ts (full — 135 lines; the file being modified)
    - src/backend/fleet-status/subscription-registry.test.ts (full — pattern-source-of-truth for existing test shape you must not break)
    - .planning/phases/39-fleet-status-gate-2-ssh-poll-decrypt-via-user-session-presen/39-RESEARCH.md § "Q1: SubscriptionRegistry API surface" AND § "Code Examples > Extending SubscriptionRegistry (Plan 1)" (canonical shape verbatim)
  </read_first>
  <behavior>
    - Interface adds two methods: onFirstSubscriber(cb: (ctx: { userId: string }) => void): () => void AND onLastUnsubscriber(cb: () => void): () => void. Each returns a disposer that unregisters the callback.
    - Interface subscribe signature becomes subscribe(sendFrame: SendFrame, ctx?: { userId: string }): () => void. ctx is OPTIONAL to avoid breaking the 7 existing tests that call subscribe with one argument.
    - Factory maintains two new Sets: firstSubCallbacks (Set of (ctx) => void) and lastUnsubCallbacks (Set of () => void).
    - On subscribe: capture wasEmpty = (subscribers.size === 0) BEFORE adding. Add to subscribers Set. Deliver snapshot (existing behavior unchanged). If wasEmpty AND ctx is defined, fire every firstSubCallbacks entry with ctx inside a try/catch — each throw is caught and logged via systemLogger.warn with operation "fleet_status_lifecycle_cb_failed".
    - On disposer invocation: subscribers.delete(sendFrame). If subscribers.size === 0 AFTER delete, fire every lastUnsubCallbacks entry inside try/catch, same warn op tag.
    - onFirstSubscriber(cb): firstSubCallbacks.add(cb); return () => firstSubCallbacks.delete(cb).
    - onLastUnsubscriber(cb): lastUnsubCallbacks.add(cb); return () => lastUnsubCallbacks.delete(cb).
    - publishSessionState, publishSessionGone, getSnapshot behaviors UNCHANGED.
    - Existing initial-snapshot delivery on subscribe UNCHANGED (already inside the subscribe body — do not move relative to the wasEmpty branch; snapshot delivery + first-sub callbacks can both run, order does not matter).
  </behavior>
  <action>
    Modify src/backend/fleet-status/subscription-registry.ts. Update the SubscriptionRegistry TypeScript interface (currently at lines 20-51) to add onFirstSubscriber and onLastUnsubscriber methods per behavior. Add optional ctx?: { userId: string } to subscribe. Inside createSubscriptionRegistry() (currently starting line 76), add two new Set constants (firstSubCallbacks and lastUnsubCallbacks) at the same scope as the existing subscribers Set. Rewrite the returned object's subscribe method to (a) capture wasEmpty before subscribers.add, (b) still deliver the initial snapshot exactly as today, (c) after snapshot delivery, if wasEmpty && ctx, iterate firstSubCallbacks and call each inside try/catch — on catch, systemLogger.warn("Fleet-status onFirstSubscriber callback threw", { operation: "fleet_status_lifecycle_cb_failed", error: err instanceof Error ? err.message : "unknown" }). Rewrite the disposer returned from subscribe to (a) delete from subscribers, (b) if subscribers.size === 0, iterate lastUnsubCallbacks with the same try/catch pattern. Add onFirstSubscriber and onLastUnsubscriber implementations returning Set.delete disposers. Leave publishSessionState / publishSessionGone / getSnapshot / makeKey / fanOut untouched. Preserve the FRAME_SCHEMA_VERSION re-export at bottom of file. No new imports required — systemLogger already imported at line 9. This implements D-01 (GATE2-01) and D-02 (GATE2-02) foundation per D-CTX §Path C.
  </action>
  <verify>
    <automated>npm run build:backend &amp;&amp; npm run build &amp;&amp; npx vitest run src/backend/fleet-status/subscription-registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "onFirstSubscriber" src/backend/fleet-status/subscription-registry.ts` returns >= 3 (interface decl + factory member + Set decl).
    - `grep -c "onLastUnsubscriber" src/backend/fleet-status/subscription-registry.ts` returns >= 3.
    - `grep -c "fleet_status_lifecycle_cb_failed" src/backend/fleet-status/subscription-registry.ts` returns >= 2 (one per callback try/catch).
    - `grep -c "ctx?: { userId: string }" src/backend/fleet-status/subscription-registry.ts` returns >= 1.
    - `npm run build:backend` exits 0.
    - `npm run build` exits 0.
    - `npx vitest run src/backend/fleet-status/subscription-registry.test.ts` reports 7 pass 0 fail (existing tests untouched — Task 2 adds more).
  </acceptance_criteria>
  <done>Interface exposes both lifecycle registrars; factory fires callbacks on the exact 0↔1 boundary transitions; existing 7 tests still green; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add lifecycle-hook tests + update MockRegistry in ssh-poll-orchestrator.test.ts to satisfy extended interface</name>
  <files>src/backend/fleet-status/subscription-registry.test.ts, src/backend/fleet-status/ssh-poll-orchestrator.test.ts</files>
  <read_first>
    - src/backend/fleet-status/subscription-registry.test.ts (full — the file being modified; you MUST preserve all 7 existing tests verbatim including Test 1-7 numbering)
    - src/backend/fleet-status/subscription-registry.ts (post-Task-1 shape you are testing)
    - src/backend/fleet-status/ssh-poll-orchestrator.test.ts lines 90-125 (MockRegistry class — the file being modified for interface satisfaction; do NOT touch other tests below)
    - .planning/phases/39-fleet-status-gate-2-ssh-poll-decrypt-via-user-session-presen/39-RESEARCH.md § "Q6: Test conventions" (vitest mock pattern + Set-of-subscribers convention)
  </read_first>
  <behavior>
    Tests to add to subscription-registry.test.ts (numbering continues from Test 7):
    - Test 8: onFirstSubscriber fires with ctx.userId when a subscriber joins an empty registry. Register a callback via onFirstSubscriber; call subscribe(sendFrame, { userId: "u1" }); assert the callback was invoked exactly once with { userId: "u1" }.
    - Test 9: onFirstSubscriber does NOT fire on second subscriber. Subscribe once with { userId: "u1" }; register callback; subscribe again with { userId: "u2" }; assert callback invocation count === 0 (was registered AFTER first sub arrived, so the 0→1 edge has passed).
    - Test 10: onFirstSubscriber fires again after a full teardown-and-resubscribe cycle. Subscribe with { userId: "u1" }, then dispose. Register onFirstSubscriber callback (this simulates late registration surviving cycles). Subscribe again with { userId: "u2" }. Assert callback invoked once with { userId: "u2" }.
    - Test 11: onLastUnsubscriber fires when the last disposer runs. Subscribe twice; register onLastUnsubscriber callback; dispose first — assert not fired; dispose second — assert fired exactly once.
    - Test 12: Disposer returned by onFirstSubscriber unregisters — subscribe, dispose. Register callback A via onFirstSubscriber, then invoke the returned unregister disposer. Subscribe with { userId: "u1" } to an empty registry. Assert A not called.
    - Test 13: Subscribe without ctx does not fire onFirstSubscriber (backward-compat guard). Register onFirstSubscriber callback. Call subscribe(sendFrame) with no second arg. Assert callback not invoked. (This guards the 7 existing tests that omit ctx.)
    - Test 14: Callback throws are caught and logged via systemLogger.warn. Mock throws on invocation; register via onFirstSubscriber; subscribe with ctx; assert subscribe returns normally AND vi.mocked(systemLogger.warn) was called with an object containing operation: "fleet_status_lifecycle_cb_failed".

    MockRegistry update in ssh-poll-orchestrator.test.ts:
    - Add stub methods onFirstSubscriber(cb) { return () => {}; } and onLastUnsubscriber(cb) { return () => {}; } (no-op stubs — orchestrator tests do not exercise the lifecycle events at this layer).
    - Do not modify any existing test logic in that file.
  </behavior>
  <action>
    Append Tests 8-14 to src/backend/fleet-status/subscription-registry.test.ts using the existing vi.mock("../utils/logger.js", ...) pattern already present at file top. Reuse the createSubscriptionRegistry() import + makeSnapshotFrame test doubles established in Test 1. For Test 14, extend the top-level logger vi.mock so systemLogger.warn is a vi.fn() you can inspect (it already is per lines 8-15 of the current file — verify before adding). Add two lines to the MockRegistry class body in ssh-poll-orchestrator.test.ts (~line 93-122): onFirstSubscriber(_cb: (ctx: { userId: string }) => void): () => void { return () => {}; } and onLastUnsubscriber(_cb: () => void): () => void { return () => {}; }. Prefix params with _ to silence unused-var lint. Do not add tests around these stubs — they exist solely for interface satisfaction and are covered by Test 8-14 in the registry test.
  </action>
  <verify>
    <automated>npm run build:backend &amp;&amp; npm run build &amp;&amp; npx vitest run src/backend/fleet-status/subscription-registry.test.ts src/backend/fleet-status/ssh-poll-orchestrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Test 8:\|Test 9:\|Test 10:\|Test 11:\|Test 12:\|Test 13:\|Test 14:" src/backend/fleet-status/subscription-registry.test.ts` returns >= 7.
    - `grep -c "onFirstSubscriber\|onLastUnsubscriber" src/backend/fleet-status/ssh-poll-orchestrator.test.ts` returns >= 2 (MockRegistry stubs).
    - `npx vitest run src/backend/fleet-status/subscription-registry.test.ts` reports 14 pass 0 fail (7 existing + 7 new).
    - `npx vitest run src/backend/fleet-status/ssh-poll-orchestrator.test.ts` reports all pre-existing 12+ tests still pass 0 fail.
    - `npx vitest run` (full suite) exits 0.
    - `npm run build:backend` and `npm run build` both exit 0.
  </acceptance_criteria>
  <done>Seven new lifecycle-hook tests green; MockRegistry satisfies extended interface without behavior drift; full vitest suite green; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A (in-tree module refactor) | This plan touches an in-memory registry factory + its tests. No new trust boundary is created or crossed. Existing WS auth boundary at fleet-status-server.ts:159-198 is unchanged (Plan 02 will thread the already-verified userId through subscribe). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Information Disclosure | onFirstSubscriber ctx passthrough | mitigate | ctx is only { userId } — a stable per-account identifier already logged in fleet-status-server.ts. No secret material carried. Downstream plan 02 uses it exclusively as a scope for SimpleDBOps decrypt, matching the canonical Skynet pattern per RESEARCH § V4. |
| T-39-02 | Denial of Service | Callback registration explosion | accept | firstSubCallbacks / lastUnsubCallbacks are Sets managed by trusted in-tree code (starter.ts only). No external caller can register callbacks. Growth bounded by module boot behavior. |
| T-39-03 | Tampering (Supply-Chain) | npm/pip/cargo installs | mitigate | No new packages introduced — Phase 39 is a pure in-tree refactor. RESEARCH § Package Legitimacy Audit explicitly notes "Not applicable — Phase 39 introduces no new npm/PyPI/crates packages." No installs → no legitimacy checkpoint required. |
| T-39-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] — n/a here, no installs. Preserved in threat register per template requirement. |
</threat_model>

<verification>
- All 14 subscription-registry tests pass (7 pre-existing + 7 new).
- ssh-poll-orchestrator.test.ts continues to pass with updated MockRegistry (12+ tests).
- Full-suite `npx vitest run` reports 0 failures.
- Both `npm run build:backend` and `npm run build` exit 0.
- `grep -n "onFirstSubscriber\|onLastUnsubscriber" src/backend/fleet-status/subscription-registry.ts` shows both symbols in the interface AND the factory return.
- No other consumers of the SubscriptionRegistry interface exist yet (starter.ts:181 uses the object via `registry` var but only calls subscribe/publish paths — Plan 02 will add the onFirstSubscriber/onLastUnsubscriber calls); ripgrep `registry.subscribe\|registry.onFirstSubscriber\|registry.onLastUnsubscriber` outside the three files touched here should show only the existing subscribe usage in fleet-status-server.ts.
</verification>

<success_criteria>
- SubscriptionRegistry interface publishes onFirstSubscriber(cb) and onLastUnsubscriber(cb) each returning a disposer.
- subscribe(sendFrame, ctx?) accepts optional { userId } context and fires firstSubCallbacks on the 0→1 subscriber transition when ctx is present.
- Last-unsubscriber transition (subscribers.size → 0 via disposer) fires lastUnsubCallbacks.
- Callback throws are caught and logged via systemLogger.warn with operation "fleet_status_lifecycle_cb_failed".
- 7 pre-existing subscription-registry tests continue to pass without modification.
- MockRegistry in ssh-poll-orchestrator.test.ts satisfies the extended interface.
- Full vitest suite green (per fleet directive #5).
</success_criteria>

<output>
Create `.planning/phases/39-fleet-status-gate-2-ssh-poll-decrypt-via-user-session-presen/39-01-SUMMARY.md` when done.
</output>
