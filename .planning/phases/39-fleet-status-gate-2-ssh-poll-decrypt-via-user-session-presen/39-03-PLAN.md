---
phase: 39-fleet-status-gate-2-ssh-poll-decrypt-via-user-session-presen
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/utils/logger.ts
  - src/backend/utils/logger.test.ts
autonomous: true
requirements:
  - GATE2-04
must_haves:
  truths:
    - "Logger.formatMessage surfaces the 'error' field for every warn/info/error call site that carries it"
    - "Logger.formatMessage surfaces any other non-sensitive structured context field passed via LogContext (fleetHostId, hostname, remoteIp, tick, pid, wsState, zodError, reason, etc)"
    - "Sanitized (SENSITIVE_FIELDS) values continue to be masked BEFORE the generic passthrough runs — no key/password/token leak"
    - "Existing 7-field known-ordering (op, user, host, tunnel, session, req, duration) is preserved verbatim so downstream log-parsing tools do not break"
    - "New logger.test.ts asserts error passthrough + sensitive-field masking + known-field ordering"
    - "Full vitest suite green"
  artifacts:
    - path: "src/backend/utils/logger.ts"
      provides: "Generic non-sensitive context-field passthrough in formatMessage"
      contains: "sanitizedContext"
    - path: "src/backend/utils/logger.test.ts"
      provides: "New test coverage for the passthrough behavior + masking invariant"
      contains: "formatMessage"
  key_links:
    - from: "src/backend/utils/logger.ts:formatMessage"
      to: "src/backend/utils/logger.ts:sanitizeContext"
      via: "sanitized-first, then generic print pass"
      pattern: "sanitizeContext"
    - from: "src/backend/utils/logger.ts:warn"
      to: "src/backend/utils/console-forward-transport.ts:enqueueBackendLog"
      via: "formatted string flow — same string to docker logs AND console-forward.log"
      pattern: "enqueueBackendLog"
---

<objective>
Fix the fleet-status structured-logger swallow gap: every `systemLogger.warn/info/error(msg, { error: err.message, ... })` call site currently loses the `error` field (and every other non-whitelisted field) in the human-readable output that reaches `console-forward-logs/console-forward.log` and `docker logs skynet`. Root cause is 100% localized to logger.ts formatMessage — a hard-coded 7-field whitelist per RESEARCH §Q4. Fix once, benefit at all 13 fleet-status call sites simultaneously plus every other backend log call site.

Purpose: D-04 (GATE2-04) — structured error surfacing. Also unblocks operator diagnosis for the next Gate 2-style bug where the real ssh2 error is invisible today (per D-CTX §Logger fix Ashley 2026-08-13 rationale: "This is why the Gate 2 diagnosis took as long as it did"). Ships in its own wave for reversibility per RESEARCH §Open Q3 recommendation.

Output: logger.ts formatMessage preserves the existing 7-field known ordering verbatim, then iterates all other sanitized fields and prints key:value pairs for each defined non-null non-empty entry. sanitizeContext runs unchanged BEFORE formatMessage — SENSITIVE_FIELDS list already masks password/key/token/etc via the existing pre-processor. New logger.test.ts covers the passthrough, the sensitive-field masking invariant, and the known-field ordering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/39-fleet-status-gate-2-ssh-poll-decrypt-via-user-session-presen/39-CONTEXT.md
@.planning/phases/39-fleet-status-gate-2-ssh-poll-decrypt-via-user-session-presen/39-RESEARCH.md
@src/backend/utils/logger.ts
@src/backend/utils/console-forward-transport.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add generic non-sensitive context passthrough to formatMessage + write logger.test.ts covering the invariants</name>
  <files>src/backend/utils/logger.ts, src/backend/utils/logger.test.ts</files>
  <read_first>
    - src/backend/utils/logger.ts (full — 306 lines; the file being modified. Pay attention to lines 46-64 SENSITIVE_FIELDS, lines 96-125 sanitizeContext, lines 127-162 formatMessage, lines 207-234 debug/info/warn/error emit paths.)
    - src/backend/utils/console-forward-transport.ts (full — 145 lines; downstream consumer of formatMessage output. Confirms that fixing formatMessage fixes both console.warn AND enqueueBackendLog because both receive the same pre-formatted string per RESEARCH §Q4.)
    - .planning/phases/39-fleet-status-gate-2-ssh-poll-decrypt-via-user-session-presen/39-RESEARCH.md § "Q4: Logger" AND § "Code Examples > Fixing the logger" (canonical shape verbatim, and known-order array for the ordering test).
    - src/backend/utils/permission-manager.test.ts lines 1-30 (existing vi.mock("./logger.js") pattern — reference for test conventions, not a file to modify)
  </read_first>
  <behavior>
    - `sanitizeContext(context)` remains unchanged — SENSITIVE_FIELDS masking (password, key, token, etc → "[MASKED]") and TRUNCATE_FIELDS truncation continue to run FIRST.
    - `formatMessage` retains the existing 7-field known-order block VERBATIM: op → user → host → tunnel → session → req → duration. This preserves observability parity for existing log-parsers.
    - After the known-order block, formatMessage iterates all OTHER keys in sanitizedContext (via Object.entries) and, for each key that (a) is not in the known-ordered set already printed AND (b) has a defined non-null non-empty-string value, appends a `key:valStr` entry to contextParts. For primitive values, use String coercion; for objects/arrays, use JSON.stringify.
    - Sensitive fields never leak because sanitizeContext replaced their values with "[MASKED]" or "[PRESENT]"/"[ABSENT]" BEFORE formatMessage sees them — the generic loop prints whatever the sanitizer left behind, which for sensitive fields is the redaction string.
    - Empty-string values are skipped (matches the existing 7-field truthiness check semantics).
    - Contextual bracket wrapping (`chalk.gray(` [${contextParts.join(",")}]`)`) unchanged.
    - Rate limiting, console.warn/log/error dispatch, enqueueBackendLog path — all unchanged. This is purely a contextParts construction fix.

    New logger.test.ts assertions (co-located with logger.ts to match Skynet convention — test file is CREATED, not extended):
    - Test 1: `error` field surfaces in warn output. Instantiate `new Logger("test", "T", "#fff")`. Spy on console.warn. Call `logger.warn("msg", { operation: "op1", error: "boom" })`. Assert the console.warn call arg contains both `op:op1` AND `error:boom`.
    - Test 2: Extra structured fields (fleetHostId, hostname, remoteIp) surface. Call `logger.warn("msg", { operation: "op1", fleetHostId: "5", hostname: "box1" })`. Assert the output contains `fleetHostId:5` AND `hostname:box1`.
    - Test 3: SENSITIVE_FIELDS still masked in the generic passthrough. Call `logger.warn("msg", { operation: "op1", password: "secret123", key: "PEMSTRING" })`. Assert the output contains `password:[MASKED]` AND `key:[MASKED]` AND does NOT contain the literal `secret123` or `PEMSTRING`.
    - Test 4: Known-field order preserved. Call `logger.warn("msg", { hostId: "h1", operation: "op1", userId: "u1" })` (deliberately out of natural order in the input). Assert the output ordering is `op:op1,user:u1,host:h1` (per the known-order array in RESEARCH §Code Examples).
    - Test 5: Objects serialized as JSON. Call `logger.warn("msg", { operation: "op1", zodError: { issues: [{ path: ["a"], message: "bad" }] } })`. Assert the output contains `zodError:{"issues":[...` (JSON-stringified).
    - Test 6: Undefined/null/empty-string values omitted. Call `logger.warn("msg", { operation: "op1", empty: "", nullish: null, undef: undefined })`. Assert the output contains `op:op1` and does NOT contain `empty:`, `nullish:`, or `undef:`.
    - Test 7: TRUNCATE_FIELDS still truncate. Call `logger.warn("msg", { operation: "op1", data: "a".repeat(200) })`. Assert `data` value in output ends with `...` (existing sanitizer contract preserved).
    - Rate-limiter interference guard: each test uses a unique message string OR resets the internal logCounts Map by creating a fresh Logger instance per test.
  </behavior>
  <action>
    Modify src/backend/utils/logger.ts formatMessage (currently lines 127-162). Keep the existing 7-if-block whitelist body verbatim as the first pass; add a Set constant `const KNOWN_CTX_FIELDS = new Set(["operation","userId","hostId","tunnelName","sessionId","requestId","duration"])` (either at file scope near SENSITIVE_FIELDS/TRUNCATE_FIELDS or inside formatMessage — inside is simpler and scoped). After the 7-if-block, append: `for (const [k, v] of Object.entries(sanitizedContext)) { if (KNOWN_CTX_FIELDS.has(k)) continue; if (v === undefined || v === null) continue; if (typeof v === "string" && v.length === 0) continue; const valStr = typeof v === "string" ? v : (typeof v === "number" || typeof v === "boolean") ? String(v) : JSON.stringify(v); contextParts.push(`${k}:${valStr}`); }`. Leave the `if (contextParts.length > 0)` wrap block untouched — chalk.gray decoration unchanged. This implements D-04 (GATE2-04) per D-CTX §Logger fix + RESEARCH §Q4 canonical shape.

    Create src/backend/utils/logger.test.ts. Import `describe, it, expect, vi, beforeEach` from "vitest" and `Logger` from "./logger.js". At the top of the file, `beforeEach` should `vi.restoreAllMocks()` to reset console spies between tests. Each test constructs a fresh `const logger = new Logger("test", "T", "#ffffff")` to avoid rate-limiter cross-contamination. For each of Tests 1-7 above, spy on `console.warn` via `const spy = vi.spyOn(console, "warn").mockImplementation(() => {})` (or the equivalent info/error method under test), invoke logger.warn, then extract the last call's first argument and use `expect(callArg).toContain(...)` / `expect(callArg).not.toContain(...)` for the assertions. For Test 4 (ordering), use a regex or a substring index-of comparison to assert `op:op1` appears before `user:u1` which appears before `host:h1` in the output string.

    Do NOT modify the debug/info/warn/error/success emit paths; do NOT touch enqueueBackendLog; do NOT change the rate limiter.
  </action>
  <verify>
    <automated>npm run build:backend &amp;&amp; npm run build &amp;&amp; npx vitest run src/backend/utils/logger.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "KNOWN_CTX_FIELDS" src/backend/utils/logger.ts` returns >= 2 (declaration + use).
    - `grep -c "Object.entries(sanitizedContext)" src/backend/utils/logger.ts` returns >= 1.
    - `grep -c "JSON.stringify(v)" src/backend/utils/logger.ts` returns >= 1.
    - Existing 7-field whitelist block preserved: `grep -c "op:\\${sanitizedContext.operation}" src/backend/utils/logger.ts` returns >= 1 AND the known-order pattern (op/user/host/tunnel/session/req/duration) all still appear in the file.
    - src/backend/utils/logger.test.ts exists AND `grep -c "it\\(" src/backend/utils/logger.test.ts` returns >= 7.
    - `npx vitest run src/backend/utils/logger.test.ts` reports 7+ pass 0 fail.
    - `npx vitest run` (full suite) exits 0 with 0 failures across every backend + frontend project (per fleet directive #5).
    - `npm run build:backend` and `npm run build` both exit 0 (per fleet directive #4).
    - No SENSITIVE_FIELDS value leak: `grep -c "secret123\\|PEMSTRING" src/backend/utils/logger.test.ts` returns exactly 2 (the two test inputs that assert masking — never emitted to the logger unmasked).
  </acceptance_criteria>
  <done>formatMessage surfaces the `error` field + every other non-sensitive structured field; sensitive-field masking preserved; known-field ordering preserved verbatim; new logger.test.ts covers the invariants; full test suite + both builds green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Log record → console-forward.log + docker logs | Every log line passes through formatMessage → console.warn → enqueueBackendLog per RESEARCH §Q4 downstream. This plan changes what gets INCLUDED in the string, not where it goes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-08 | Information Disclosure | Sensitive field leak via generic passthrough | mitigate | sanitizeContext runs BEFORE formatMessage (already the case per lines 138-139). SENSITIVE_FIELDS (password/passphrase/key/privateKey/publicKey/token/secret/clientSecret/keyPassword/autostartPassword/autostartKey/autostartKeyPassword/credentialId/authToken/jwt/session/cookie) are replaced with "[MASKED]" values before the generic loop sees them. Test 3 in logger.test.ts asserts this invariant. |
| T-39-09 | Information Disclosure | Log injection via crafted context values | accept | Existing logger has no output-escape; caller-supplied strings are printed verbatim today. Phase 39 does not widen this — the generic passthrough treats string values via String coercion, objects via JSON.stringify which itself escapes control chars. Fleet-status callers pass typed structured payloads (err.message, id strings) — no untrusted user input crosses this boundary. |
| T-39-10 | Information Disclosure | TRUNCATE_FIELDS bypassed if the field appears via generic passthrough | mitigate | sanitizeContext handles TRUNCATE_FIELDS (data/content/body/response/request) BEFORE formatMessage. Test 7 asserts this contract is preserved. |
| T-39-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — Phase 39 is a pure in-tree refactor. RESEARCH §Package Legitimacy Audit "Not applicable". |
</threat_model>

<verification>
- New logger.test.ts asserts: error surfaces, extra fields surface, sensitive-field masking preserved, known-field order preserved, objects JSON-stringified, undefined/null/empty skipped, TRUNCATE_FIELDS still truncate.
- Full-suite `npx vitest run` green (fleet directive #5).
- Both `npm run build:backend` and `npm run build` exit 0 (fleet directive #4).
- Grep confirms KNOWN_CTX_FIELDS + Object.entries pattern present in logger.ts.
- Existing 7-field whitelist block untouched — downstream log-parsers that key on `op:`/`user:`/`host:` prefixes see identical ordering.
</verification>

<success_criteria>
- Structured `error` field surfaces in every log line that carried it (D-04 / GATE2-04).
- Non-sensitive context fields (fleetHostId, hostname, remoteIp, tick, pid, wsState, zodError, reason, etc.) surface at every call site — the fix applies globally, not per-call-site.
- SENSITIVE_FIELDS never leak; masking behavior preserved by test invariant.
- Full vitest suite + both build commands green.
- Plan lives in its own wave for reversibility per RESEARCH §Open Q3 — Wave 1, no dependency on Plan 01 or Plan 02.
</success_criteria>

<output>
Create `.planning/phases/39-fleet-status-gate-2-ssh-poll-decrypt-via-user-session-presen/39-03-SUMMARY.md` when done.
</output>
