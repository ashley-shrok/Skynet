---
phase: 39-fleet-status-gate-2-ssh-poll-decrypt-via-user-session-presen
plan: 02
type: execute
wave: 2
depends_on:
  - 39-01
files_modified:
  - src/backend/fleet-status/fleet-status-server.ts
  - src/backend/fleet-status/fleet-status-server.test.ts
  - src/backend/starter.ts
autonomous: true
requirements:
  - GATE2-01
  - GATE2-02
  - GATE2-03
  - GATE2-06
must_haves:
  truths:
    - "fleet-status-server passes { userId } into registry.subscribe as the ctx arg on the subscribe path"
    - "starter.ts wires registry.onFirstSubscriber -> orchestrator.start() and registry.onLastUnsubscriber -> orchestrator.stop() + hostClients cleanup"
    - "starter.ts no longer calls orchestrator.start() unconditionally at boot"
    - "listIdentityHostingHosts rewritten to call resolveHostById(rowId, userId) per host and pass the DECRYPTED SSHHost record forward as _connDetails"
    - "Boot log emits fleet_status_awaiting_subscriber instead of the eager fleet_status_orchestrator_started duplicate"
    - "Full vitest suite green; both npm run build:backend and npm run build exit 0"
  artifacts:
    - path: "src/backend/starter.ts"
      provides: "Presence-driven orchestrator lifecycle wiring + resolveHostById-based decrypt path"
      contains: "onFirstSubscriber"
    - path: "src/backend/fleet-status/fleet-status-server.ts"
      provides: "subscribe call carrying { userId } ctx"
      contains: "userId: userId"
  key_links:
    - from: "src/backend/starter.ts:listIdentityHostingHosts"
      to: "src/backend/ssh/host-resolver.ts:resolveHostById"
      via: "per-row decrypt via SimpleDBOps"
      pattern: "resolveHostById"
    - from: "src/backend/starter.ts"
      to: "src/backend/fleet-status/subscription-registry.ts:onFirstSubscriber"
      via: "presence-driven lifecycle hook"
      pattern: "registry.onFirstSubscriber"
    - from: "src/backend/fleet-status/fleet-status-server.ts:subscribe path"
      to: "src/backend/fleet-status/subscription-registry.ts:subscribe"
      via: "userId ctx passthrough"
      pattern: "registry.subscribe\\("
---

<objective>
Rewire the SSH-poll orchestrator to run only while at least one fleet-status browser subscriber is connected, using the subscribing user's authenticated session for per-host decrypt via the canonical `resolveHostById(hostId, userId)` path. Implements Path C locked by Ashley 2026-08-13 (D-CTX §Decisions verbatim: "nobody needs to know if something is idle or not, or anything else that's going on here, if no user is present to want to know the information"). Covers D-01 (GATE2-01 start-on-first-sub), D-02 (GATE2-02 stop-on-last-unsub), D-03 (GATE2-03 decrypt via user session), D-06 (GATE2-06 no unreachable events during active window).

Purpose: Fix the ciphertext-passed-to-ssh2 bug (root cause per D-CTX §Root cause + RESEARCH §Common Pitfalls Pitfall 1) AND enforce the presence-driven lifecycle in one atomic wire-up. Both concerns share the same starter.ts block scope so they land together.

Output: fleet-status-server.ts subscribe call threads { userId } as ctx. starter.ts moves orchestrator.start() out of the boot path and behind onFirstSubscriber; listIdentityHostingHosts is rewritten to fetch id+name rows, then per-row invoke resolveHostById with the closure-captured currentSubscriberUserId; _connDetails becomes the decrypted SSHHost record; onLastUnsubscriber stops the orchestrator AND closes hostClients ssh2 Clients; boot log emits `fleet_status_awaiting_subscriber`; the duplicate `fleet_status_orchestrator_started` log at starter.ts:349 is removed (RESEARCH §Landmine 7). A fleet-status-server.test.ts test asserts the subscribe call carries { userId }.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/39-fleet-status-gate-2-ssh-poll-decrypt-via-user-session-presen/39-CONTEXT.md
@.planning/phases/39-fleet-status-gate-2-ssh-poll-decrypt-via-user-session-presen/39-RESEARCH.md
@.planning/phases/39-fleet-status-gate-2-ssh-poll-decrypt-via-user-session-presen/39-01-PLAN.md
@src/backend/fleet-status/fleet-status-server.ts
@src/backend/fleet-status/fleet-status-server.test.ts
@src/backend/starter.ts
@src/backend/ssh/host-resolver.ts
@src/backend/database/routes/sessions.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread { userId } through fleet-status-server subscribe call + spy-test coverage</name>
  <files>src/backend/fleet-status/fleet-status-server.ts, src/backend/fleet-status/fleet-status-server.test.ts</files>
  <read_first>
    - src/backend/fleet-status/fleet-status-server.ts lines 148-270 (handleFrontendConnection auth + subscribe path — file being modified)
    - src/backend/fleet-status/fleet-status-server.test.ts (full — must preserve all pre-existing test structure)
    - src/backend/fleet-status/subscription-registry.ts (post-Plan-01 shape — subscribe now accepts ctx?: { userId: string })
    - .planning/phases/39-fleet-status-gate-2-ssh-poll-decrypt-via-user-session-presen/39-RESEARCH.md § "Q3: How WS server threads authenticated userId" (canonical single-line change per §Q3 line 858-863)
  </read_first>
  <behavior>
    - The subscribe path in handleFrontendConnection (~line 231-242) passes { userId: userId! } as the second argument to registry.subscribe. userId is already extracted from JWT payload at line 181 and in closure scope; the `!` non-null assertion is safe because the auth branch above (line 172) already returned when userId is undefined.
    - When userId is truthy at subscribe time, registry.subscribe receives BOTH the sendFrame closure AND { userId } ctx.
    - Existing systemLogger.info at line 233 ("Fleet-status frontend subscribed") continues to log userId + sessionId — unchanged. NO new log entry added at the WS server layer for lifecycle events (that's starter.ts's job in Task 2).
    - Test: extend fleet-status-server.test.ts with a scenario that spies on registry.subscribe (via a mockRegistry object implementing SubscriptionRegistry) and asserts that after a valid auth + subscribe frame, mockRegistry.subscribe was called with a second arg matching { userId: <expected> }.
  </behavior>
  <action>
    Modify src/backend/fleet-status/fleet-status-server.ts. Locate the `if (frame.type === "subscribe" && !subscribeHandled)` block (~lines 231-242). Change `disposer = registry.subscribe((outFrame) => { ... });` to `disposer = registry.subscribe((outFrame) => { if (ws.readyState === WebSocket.OPEN) ws.send(JSON.stringify(outFrame)); }, { userId: userId! });` — the closure body stays identical, only the second argument is added. This implements D-03 (GATE2-03) userId threading per RESEARCH §Q3.

    Modify src/backend/fleet-status/fleet-status-server.test.ts. Add a new test asserting registry.subscribe receives the { userId } ctx. Mirror the existing test setup for handleFrontendConnection (grep the file for the top of the test list to find the existing pattern of authManager mock + registry mock construction). The test should: (a) construct a mockRegistry that implements the extended SubscriptionRegistry interface (subscribe as vi.fn().mockReturnValue(() => {}), publishSessionState/publishSessionGone/getSnapshot/onFirstSubscriber/onLastUnsubscriber as vi.fn() stubs), (b) drive handleFrontendConnection (or startFleetStatusServer with a mock ws pair) through the auth-passes-then-subscribe flow with userId "u-test", (c) assert vi.mocked(mockRegistry.subscribe).mock.calls[0][1] deep-equals { userId: "u-test" }. If the existing test file already exports the connection handler for direct test invocation, use that; otherwise use the wss driver path already established in the file.
  </action>
  <verify>
    <automated>npm run build:backend &amp;&amp; npm run build &amp;&amp; npx vitest run src/backend/fleet-status/fleet-status-server.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "registry.subscribe(" src/backend/fleet-status/fleet-status-server.ts` shows exactly one call site AND the second argument contains `userId`.
    - `grep -c "{ userId: userId" src/backend/fleet-status/fleet-status-server.ts` returns >= 1 (canonical shape per RESEARCH §Q3 code example).
    - `grep -c "mock.calls\\[0\\]\\[1\\]\\|calls\\[0\\]\\.arguments\\[1\\]" src/backend/fleet-status/fleet-status-server.test.ts` returns >= 1 (new spy assertion).
    - `npx vitest run src/backend/fleet-status/fleet-status-server.test.ts` reports 0 failures (all pre-existing + new test pass).
    - `npm run build:backend` and `npm run build` both exit 0.
  </acceptance_criteria>
  <done>Subscribe call site carries { userId }; test proves the ctx propagates; typecheck + tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire starter.ts orchestrator lifecycle to presence-driven + swap listIdentityHostingHosts to resolveHostById decrypt path</name>
  <files>src/backend/starter.ts</files>
  <read_first>
    - src/backend/starter.ts lines 140-360 (the fleet-status IIFE block — the file being modified)
    - src/backend/ssh/host-resolver.ts lines 1-80 (resolveHostById signature + return shape — canonical decrypt path)
    - src/backend/database/routes/sessions.ts lines 60-90 (canonical resolveHostById -> connectOneShot usage pattern — pattern-source-of-truth)
    - src/backend/fleet-status/subscription-registry.ts (post-Plan-01 shape — onFirstSubscriber/onLastUnsubscriber interface)
    - .planning/phases/39-fleet-status-gate-2-ssh-poll-decrypt-via-user-session-presen/39-RESEARCH.md § "Q2: createSshPollOrchestrator start/stop re-entrancy" AND § "Code Examples > Rewiring listIdentityHostingHosts" AND § "Code Examples > Rewiring orchestrator lifecycle in starter.ts" (canonical shape verbatim)
    - .planning/phases/39-fleet-status-gate-2-ssh-poll-decrypt-via-user-session-presen/39-RESEARCH.md § "Landmines / Watch-outs" (all 7 items — especially #4 _connDetails typing, #5 releaseSshChannel semantics, #7 duplicate fleet_status_orchestrator_started removal)
  </read_first>
  <behavior>
    - Add import: `resolveHostById` from `./ssh/host-resolver.js` at the top of the fleet-status IIFE block, alongside the existing dynamic imports.
    - Introduce a mutable `currentSubscriberUserId: string | null = null` inside the fleet-status IIFE block scope (same scope as `hostClients` Map at line 234).
    - Rewrite `listIdentityHostingHosts()` (~lines 199-231) so it: (a) reads only { id, name } columns from hostsTable filtered by enableSsh = true, (b) for each row, calls `await resolveHostById(row.id, currentSubscriberUserId!)` — if resolved is null, skip (null-filter after Promise.all), (c) returns [{ id: String(row.id), name: row.name ?? String(row.id), _connDetails: resolved }, ...]. Wrap Promise.all in try/catch that logs `fleet_status_host_list_failed` with error message (existing catch pattern preserved). If currentSubscriberUserId is null at call time (should never happen — orchestrator.start() is behind onFirstSubscriber), log a warn with operation `fleet_status_host_list_no_user` and return []. This implements D-03 (GATE2-03) per D-CTX §Root cause.
    - Replace the fire-and-forget `orchestrator.start().catch(...)` at line 340-345 and the eager `listIdentityHostingHosts().then(...)` at line 348-357 with: `registry.onFirstSubscriber(({ userId }) => { ... })` and `registry.onLastUnsubscriber(() => { ... })` blocks.
    - onFirstSubscriber body: (a) set currentSubscriberUserId = userId, (b) log `fleet_status_orchestrator_lifecycle` info with the userId ("Fleet-status orchestrator starting on first subscriber"), (c) `orchestrator.start().catch(err => systemLogger.warn("Fleet-status orchestrator start failed", { operation: "fleet_status_orchestrator_start_failed", error: err.message }))`.
    - onLastUnsubscriber body: (a) log `fleet_status_orchestrator_lifecycle` info ("Fleet-status orchestrator stopping on last unsubscriber"), (b) call `orchestrator.stop()` (synchronous), (c) iterate hostClients Map entries: for each [id, client], `try { client.end(); } catch {}` — this closes the live ssh2 TCP connections that outlive orchestrator's perHostState clear (RESEARCH §Pitfall 3 / §Landmine 5), (d) hostClients.clear(), (e) currentSubscriberUserId = null.
    - Add ONE new boot log at the end of the fleet-status IIFE block (after the onFirstSubscriber / onLastUnsubscriber registrations) — `systemLogger.info("Fleet-status orchestrator initialized (awaiting first subscriber)", { operation: "fleet_status_awaiting_subscriber", pollIntervalMs: 2000, staleSweepIntervalMs: 30000 })`. This replaces the misleading boot-time `fleet_status_orchestrator_started` log per RESEARCH §Open Q2.
    - Remove entirely the pre-existing lines 348-357 (`listIdentityHostingHosts().then(...)` block) — its `fleet_status_orchestrator_started` log duplicates the one inside orchestrator.start() at ssh-poll-orchestrator.ts:525 (RESEARCH §Landmine 7).
    - The orchestrator's deps.listIdentityHostingHosts signature is `() => Promise<HostRecord[]>` — the closure over currentSubscriberUserId is captured naturally because listIdentityHostingHosts is defined in the same enclosing scope. Do NOT change the orchestrator deps signature — keep the closure capture per RESEARCH §Q3 assumption A5.
    - Do NOT touch acquireSshChannel, releaseSshChannel, or the createSshPollOrchestrator construction — the decrypt change lives entirely inside listIdentityHostingHosts + _connDetails now carries the decrypted SSHHost record, which flows through the existing acquireSshChannel path unchanged (connectOneShot at line 278-281 already handles SSHHost-shaped input per canonical sessions.ts:73 pattern).
    - The `hostClients` Map (line 234) survives across start/stop cycles: onFirstSubscriber invokes orchestrator.start() which re-populates perHostState via tryAcquireHostChannel → acquireSshChannel → hostClients.get(id) hits the existing live client OR opens a new one. The onLastUnsubscriber .end() sweep is what fulfills Ashley's "no user watching = no work" contract per D-CTX §Path C.
  </behavior>
  <action>
    Modify src/backend/starter.ts inside the fleet-status IIFE block (~lines 154-358). Add `const { resolveHostById } = await import("./ssh/host-resolver.js");` next to the existing dynamic imports at the top of the block (~line 155-173). Declare `let currentSubscriberUserId: string | null = null;` immediately after the `const hostClients = new Map<string, import("ssh2").Client>();` line (~line 234). Rewrite the `listIdentityHostingHosts` function per behavior — the drizzle select now reads only { id: hostsTable.id, name: hostsTable.name } filtered by eq(hostsTable.enableSsh, true); each row goes through `resolveHostById(row.id, currentSubscriberUserId!)`; null resolves are filtered out; return type is `{ id: string; name: string; _connDetails: SSHHost }[]`. Include the null-userId guard log with operation `fleet_status_host_list_no_user`. Delete the fire-and-forget orchestrator.start() call block AND the trailing listIdentityHostingHosts().then(...) block (currently lines 339-357). In their place, insert the registry.onFirstSubscriber({ userId }) => { currentSubscriberUserId = userId; systemLogger.info("Fleet-status orchestrator starting on first subscriber", { operation: "fleet_status_orchestrator_lifecycle", userId }); orchestrator.start().catch(err => systemLogger.warn("Fleet-status orchestrator start failed", { operation: "fleet_status_orchestrator_start_failed", error: err instanceof Error ? err.message : "unknown" })); }) block and the registry.onLastUnsubscriber(() => { systemLogger.info("Fleet-status orchestrator stopping on last unsubscriber", { operation: "fleet_status_orchestrator_lifecycle" }); orchestrator.stop(); for (const [id, client] of hostClients) { try { client.end(); } catch { /* best-effort */ } } hostClients.clear(); currentSubscriberUserId = null; }) block. Finally add the new boot-log systemLogger.info("Fleet-status orchestrator initialized (awaiting first subscriber)", { operation: "fleet_status_awaiting_subscriber", pollIntervalMs: 2000, staleSweepIntervalMs: 30000 }) at the end of the block. This implements D-01, D-02, D-03, D-06 per D-CTX §Decisions Path C.
  </action>
  <verify>
    <automated>npm run build:backend &amp;&amp; npm run build &amp;&amp; npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "registry.onFirstSubscriber\|registry.onLastUnsubscriber" src/backend/starter.ts` returns >= 2.
    - `grep -c "resolveHostById" src/backend/starter.ts` returns >= 2 (import + call site).
    - `grep -c "currentSubscriberUserId" src/backend/starter.ts` returns >= 4 (declare + assign in onFirst + null in onLast + use in listIdentityHostingHosts).
    - `grep -c "fleet_status_awaiting_subscriber" src/backend/starter.ts` returns >= 1.
    - `grep -c "fleet_status_orchestrator_lifecycle" src/backend/starter.ts` returns >= 2.
    - `grep -c "hostClients.clear\\|client.end()" src/backend/starter.ts` returns >= 2 (one of each in onLastUnsubscriber cleanup).
    - `grep -v '^\\s*//\\|^\\s*\\*' src/backend/starter.ts | grep -c "listIdentityHostingHosts().then("` returns 0 (removed duplicate log block per RESEARCH §Landmine 7).
    - `grep -v '^\\s*//\\|^\\s*\\*' src/backend/starter.ts | grep -c "orchestrator.start().catch("` returns 1 (must live inside the onFirstSubscriber callback ONLY, not at boot-time).
    - `npm run build:backend` exits 0 AND `npm run build` exits 0 (per fleet directive #4 — backend + full build both required).
    - `npx vitest run` exits 0 with 0 failures across the full suite (per fleet directive #5).
  </acceptance_criteria>
  <done>Boot no longer starts the orchestrator; first fleet-status subscriber triggers start with their userId; last-unsub disconnects SSH clients; listIdentityHostingHosts uses resolveHostById; boot log clarifies the awaiting-subscriber state; full test suite green; both build commands green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → fleet-status WS server | Existing JWT auth at fleet-status-server.ts:159-198 (unchanged). Plan 02 threads the ALREADY-VERIFIED userId to the subscribe ctx — no new trust decisions. |
| Backend → SSH target host | Per-host TCP:22 boundary via ssh2 client. Handshake uses hostVerifier: () => true (Tailscale trust boundary — documented per RESEARCH §Security). Auth material is DECRYPTED via SimpleDBOps within the same process boundary. |
| DB record → in-memory decrypted host | SimpleDBOps.select(..., "ssh_data", userId) enforces per-user scope on decrypt. resolveHostById is the canonical wrapper — never bypass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-04 | Information Disclosure | Decrypted host credentials in memory | mitigate | Same lifetime as every other request-driven SSH consumer in Skynet (sessions.ts, identity-birth.ts, etc). No new persistence, no new log surface — the credentials pass through resolved._connDetails into acquireSshChannel and then into ssh2 Client, matching the canonical sessions.ts:70-75 pattern. |
| T-39-05 | Elevation of Privilege | userId threading via WS ctx | mitigate | userId is the JWT-verified payload.userId; not user-controlled at the subscribe path. resolveHostById calls SimpleDBOps which enforces per-user scope on decrypt — a subscriber cannot decrypt another user's hosts even if they spoofed the WS frame body (which they can't — auth happens before subscribe frame is processed at fleet-status-server.ts:171-179). |
| T-39-06 | Denial of Service | Presence-driven poller start/stop churn | accept | Poll cadence is 60s+ typical; user open/close of tabs is orders of magnitude slower. Repeated start/stop cycles are safe per RESEARCH §Q2 re-entrancy analysis. `hostClients.clear()` on last-unsub prevents resource leak — this is a MITIGATION for a DoS-adjacent leak scenario (RESEARCH §Pitfall 3). |
| T-39-07 | Tampering (ssh2 host key spoofing) | ssh2 hostVerifier: () => true | accept | Documented Tailscale trust boundary in ssh-one-shot.ts:67 — this predates Phase 39 and is preserved verbatim (RESEARCH §Security). |
| T-39-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — Phase 39 is a pure in-tree refactor. RESEARCH §Package Legitimacy Audit "Not applicable — Phase 39 introduces no new npm/PyPI/crates packages." |
</threat_model>

<verification>
- fleet-status-server.test.ts subscribe-ctx test passes (new coverage per Task 1).
- All pre-existing fleet-status-server / subscription-registry / ssh-poll-orchestrator / remote-hook-install tests still green.
- `npx vitest run` full-suite → 0 failures (fleet directive #5).
- `npm run build:backend` and `npm run build` both exit 0 (fleet directive #4 — starter.ts is under src/backend/).
- Static grep verifies presence-driven wiring: registry.onFirstSubscriber and registry.onLastUnsubscriber both present exactly once; boot-time orchestrator.start() call is gone; resolveHostById import + call site present; the duplicate `listIdentityHostingHosts().then` block is removed.
- Runtime signature (deferred to orchestrator's post-deploy verification — NOT run here): boot logs should show `fleet_status_awaiting_subscriber` at startup, and a real browser session should trigger `fleet_status_orchestrator_lifecycle` info logs (start on first sub, stop on last unsub). Executor scope stops at test-green.
</verification>

<success_criteria>
- Orchestrator no longer starts at boot; only starts when the first fleet-status browser subscriber connects (D-01/GATE2-01).
- Orchestrator stops + live ssh2 Clients close when the last subscriber disconnects (D-02/GATE2-02).
- listIdentityHostingHosts uses resolveHostById(id, userId) with the subscribing user's session — ciphertext bypass eliminated (D-03/GATE2-03, RESEARCH §Root cause).
- Boot log emits `fleet_status_awaiting_subscriber` — no more misleading eager-start log.
- Full vitest suite green + both build commands green.
- No executor-scope deploy / docker / ship / coord tasks per fleet directive #2.
</success_criteria>

<output>
Create `.planning/phases/39-fleet-status-gate-2-ssh-poll-decrypt-via-user-session-presen/39-02-SUMMARY.md` when done.
</output>
