---
phase: 40-text-editor-in-skynet
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/api/editable-file-api.ts
  - src/ui/features/pretty-view/editable-file-whitelist.ts
  - src/ui/features/pretty-view/use-editable-file-eligibility.ts
  - src/ui/api/editable-file-api.test.ts
  - src/ui/features/pretty-view/use-editable-file-eligibility.test.ts
autonomous: true
tags: [text-editor, frontend-api, eligibility-hook, phase-40]
requirements: []
must_haves:
  truths:
    - "Frontend has a fetchTailnetUrl() helper that POSTs to /pretty-view/fetch-tailnet-url via authApi (JWT auto-attached by the axios interceptor) and returns { contentBase64, sizeBytes, contentType, extension, filename, isTextByExt, isTextByBytes } per the backend contract from Plan 40-01 (D-01, D-04)"
    - "Frontend has a byte-identical copy of the extension whitelist + basename set + classifyByExtension at src/ui/features/pretty-view/editable-file-whitelist.ts with a MIRROR header comment referencing the backend twin (D-02)"
    - "useEditableFileEligibility(messageEventId, messageBody) hook returns Set<string> of eligible URLs; runs a synchronous whitelist check first (no fetch); only fires backend probe for extension-miss URLs; scan uses the TAILNET_URL_RE_CLIENT regex to extract candidate URLs from message text; per-message re-entry cancelled via a ref (D-02)"
    - "Eligibility hook DISCARDS the bytes it receives from fetchTailnetUrl — bytes are consumed only for sniffTextBytes classification and dropped when the promise resolves. No component of the hook exposes the bytes to any caller (D-04 hard rule — bytes fetched for eligibility MUST NEVER be served to the editor path)"
    - "Frontend and backend whitelist files are byte-identical for EDITABLE_EXTENSIONS + EDITABLE_BASENAMES contents; both have matching MIRROR header comments"
  artifacts:
    - path: "src/ui/api/editable-file-api.ts"
      provides: "fetchTailnetUrl(url: string): Promise<TailnetFetchResult> — thin axios wrapper mirroring global-files-api.ts pattern (authApi.post + handleApiError)"
      exports: ["fetchTailnetUrl", "TailnetFetchResult"]
    - path: "src/ui/features/pretty-view/editable-file-whitelist.ts"
      provides: "Frontend copy of EDITABLE_EXTENSIONS + EDITABLE_BASENAMES + classifyByExtension; also exports TAILNET_URL_RE_CLIENT (the /g regex used to scan message text for candidate URLs)"
      exports: ["EDITABLE_EXTENSIONS", "EDITABLE_BASENAMES", "classifyByExtension", "TAILNET_URL_RE_CLIENT"]
    - path: "src/ui/features/pretty-view/use-editable-file-eligibility.ts"
      provides: "useEditableFileEligibility(messageEventId: string | null, messageBody: string): Set<string> — per-message eligibility scan hook; extension check sync, byte-sniff async via backend"
      exports: ["useEditableFileEligibility"]
    - path: "src/ui/api/editable-file-api.test.ts"
      provides: "Unit tests: happy-path shape decode; error passthrough via handleApiError"
      exports: []
    - path: "src/ui/features/pretty-view/use-editable-file-eligibility.test.ts"
      provides: "Unit tests: whitelist-hit adds URL without calling fetchTailnetUrl; extension-miss + isTextByBytes:true adds URL; extension-miss + isTextByBytes:false omits URL; fetch failure omits URL; multiple URLs in one message classified independently; hook re-runs on messageEventId change; cancellation on unmount before fetch resolves does not update state"
      exports: []
  key_links:
    - from: "src/ui/features/pretty-view/use-editable-file-eligibility.ts"
      to: "src/ui/api/editable-file-api.ts"
      via: "import { fetchTailnetUrl } from '@/api/editable-file-api'"
      pattern: "fetchTailnetUrl"
    - from: "src/ui/features/pretty-view/use-editable-file-eligibility.ts"
      to: "src/ui/features/pretty-view/editable-file-whitelist.ts"
      via: "import { classifyByExtension, TAILNET_URL_RE_CLIENT } from './editable-file-whitelist'"
      pattern: "editable-file-whitelist"
    - from: "src/ui/api/editable-file-api.ts"
      to: "src/main-axios"
      via: "import { authApi, handleApiError } from '@/main-axios' — established fleet convention mirroring global-files-api.ts:1"
      pattern: "@/main-axios"
---

<objective>
Land the frontend classification + fetch infrastructure that Plan 40-03 (editor components) and Plan 40-04 (ChatMessage wiring) consume: a thin axios helper for the backend proxy endpoint, a byte-identical whitelist twin (so the ChatMessage `<a>` override can decide synchronously in the common case), and a per-message eligibility hook that scans message text for tailnet URLs and produces a Set<url> of files that carry an edit affordance.

Purpose: implements the frontend half of LOCKED decisions D-01 (passive URL detection at message-render time, no agent-side change), D-02 (extension whitelist sync path + byte-sniff async fallback), and D-04 frontend half (bytes returned from the eligibility fetch are used ONLY for classification and DISCARDED — the editor open path in Plan 40-03 will issue its own fresh fetch and never touch these cached bytes).

Output: three new source files + two test files. Zero new npm dependencies. No frontend UI surface is touched by this plan — that lives in Plans 40-03 (new components) and 40-04 (ChatMessage/PrettyView modifications).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-text-editor-in-skynet/40-CONTEXT.md
@.planning/phases/40-text-editor-in-skynet/40-RESEARCH.md
@.planning/phases/40-text-editor-in-skynet/40-UI-SPEC.md
@src/ui/api/global-files-api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Frontend whitelist twin + editable-file-api helper + api tests</name>
  <files>src/ui/features/pretty-view/editable-file-whitelist.ts, src/ui/api/editable-file-api.ts, src/ui/api/editable-file-api.test.ts</files>
  <read_first>
    - /home/ubuntu/skynet-tiffany/.planning/phases/40-text-editor-in-skynet/40-CONTEXT.md (LOCKED decisions — D-01 URL shape + D-02 whitelist contract + D-04 discard-cached-bytes rule)
    - /home/ubuntu/skynet-tiffany/.planning/phases/40-text-editor-in-skynet/40-RESEARCH.md § Pattern 5 (whitelist) + § Ex 2 (eligibility hook — for the TAILNET_URL_RE_CLIENT constant)
    - /home/ubuntu/skynet-tiffany/src/ui/api/global-files-api.ts (structure to mirror: authApi + handleApiError + typed error class if needed; L1 imports; L36-99 endpoint helpers)
    - /home/ubuntu/skynet-tiffany/src/main-axios.ts (confirm authApi is the JWT-attaching axios instance and handleApiError is the shared error-reshape helper — mirror the exact imports used by global-files-api.ts:1)
  </read_first>
  <behavior>
    - editable-file-whitelist.ts exports EDITABLE_EXTENSIONS (Set<string>) + EDITABLE_BASENAMES (Set<string>) + classifyByExtension(ext: string | null, filename: string): boolean + TAILNET_URL_RE_CLIENT (global-flag regex for scanning message text)
    - Frontend whitelist Sets are byte-identical to the backend copy from Plan 40-01 (same members in the same order for both sets)
    - Frontend whitelist file's header comment contains the string "MIRROR: byte-identical copy in src/backend/utils/editable-file-whitelist.ts — update both files in lockstep."
    - TAILNET_URL_RE_CLIENT === /http:\/\/100\.(?:6[4-9]|[7-9]\d|1[0-1]\d|12[0-7])\.\d{1,3}\.\d{1,3}:\d{1,5}\/[^\s)]+/g (unanchored, non-greedy up to first whitespace or closing paren — matches markdown link internals like [file.md](http://...) as well as bare URLs)
    - fetchTailnetUrl("http://100.64.0.1:8000/f.md") returns Promise<TailnetFetchResult> where TailnetFetchResult = { contentBase64: string; sizeBytes: number; contentType: string | null; extension: string | null; filename: string; isTextByExt: boolean; isTextByBytes?: boolean } — exact shape returned by the backend proxy from Plan 40-01 Task 2 step 9
    - Test 1 (happy path): axios stub returns 200 with a fixture TailnetFetchResult body → fetchTailnetUrl resolves to that object shape byte-for-byte
    - Test 2 (backend 502 upstream error): axios stub returns 502 with { error: "upstream 503" } → fetchTailnetUrl throws (handleApiError shape)
    - Test 3 (backend 504 timeout): axios stub returns 504 → fetchTailnetUrl throws
    - Test 4 (backend 413 oversized): axios stub returns 413 with { error: "file exceeds max size" } → fetchTailnetUrl throws
    - Test 5 (backend 400 invalid URL): axios stub returns 400 with { error: "invalid tailnet URL" } → fetchTailnetUrl throws
  </behavior>
  <action>
    Create src/ui/features/pretty-view/editable-file-whitelist.ts:
      - Header comment: "// Phase 40 D-02: extension + basename whitelist for eligible-to-edit tailnet-served files.\n// MIRROR: byte-identical copy in src/backend/utils/editable-file-whitelist.ts — update both files in lockstep.\n// The whitelist is intentionally a starter set that grows over time (per shape doc § Decisions — Ashley adds/removes as misses are noticed).\n"
      - export const EDITABLE_EXTENSIONS = new Set(["md","mdx","markdown","txt","rst","adoc","json","yaml","yml","toml","ini","conf","cfg","env","properties","ts","tsx","js","jsx","mjs","cjs","py","rb","go","rs","java","kt","swift","c","cc","cpp","h","hpp","cs","m","mm","sh","bash","zsh","fish","ps1","sql","graphql","gql","html","htm","css","scss","sass","less","vue","svelte","astro","csv","tsv","xml","log","patch","diff"])
      - export const EDITABLE_BASENAMES = new Set(["Dockerfile","Makefile","Rakefile","Gemfile","Procfile",".gitignore",".dockerignore",".editorconfig",".gitattributes",".env",".envrc",".nvmrc",".node-version",".python-version","README","LICENSE","CHANGELOG","AUTHORS","CONTRIBUTORS","COPYING","NOTICE","TODO","COMMIT_EDITMSG"])
      - export function classifyByExtension(extension: string | null, filename: string): boolean { if (extension && EDITABLE_EXTENSIONS.has(extension)) return true; if (EDITABLE_BASENAMES.has(filename)) return true; return false; }
      - export const TAILNET_URL_RE_CLIENT = /http:\/\/100\.(?:6[4-9]|[7-9]\d|1[0-1]\d|12[0-7])\.\d{1,3}\.\d{1,3}:\d{1,5}\/[^\s)]+/g
      - Extra header comment above TAILNET_URL_RE_CLIENT: "// D-01 URL shape: matches the id-skill's python -m http.server + tailnet-IP-bind pattern.\n// Regex terminator [^\\s)]+ handles bare URLs AND markdown-link internals like [text](url)."

    Create src/ui/api/editable-file-api.ts:
      - import { authApi, handleApiError } from "@/main-axios"
      - Export type TailnetFetchResult = { contentBase64: string; sizeBytes: number; contentType: string | null; extension: string | null; filename: string; isTextByExt: boolean; isTextByBytes?: boolean };
      - Export async function fetchTailnetUrl(url: string): Promise<TailnetFetchResult> that POSTs to "/pretty-view/fetch-tailnet-url" with body { url } via authApi.post; unwrap response.data as TailnetFetchResult; on error call handleApiError(error, "fetch tailnet URL") then throw error (unreachable, satisfies TS return type — mirrors global-files-api.ts:47-49 pattern verbatim)
      - Header docblock cites D-01 (endpoint is the backend proxy from Plan 40-01) and D-04 (this helper is the ONLY frontend fetch of tailnet content; callers who use bytes for eligibility MUST DISCARD them — see use-editable-file-eligibility.ts contract)

    Create src/ui/api/editable-file-api.test.ts:
      - Mock @/main-axios's authApi.post with vi.mock (mirroring GlobalFilesModal.test.tsx pattern L22-34)
      - Tests 1-5 above
      - Set up a shared fixture TailnetFetchResult with realistic values (contentBase64 = base64("hello"), sizeBytes = 5, contentType = "text/plain", extension = "md", filename = "notes.md", isTextByExt = true, isTextByBytes = undefined)
      - For error tests, stub authApi.post to reject with an axios-shaped error object ({ response: { status: 502, data: { error: "..." } } }) and assert the promise rejects
  </action>
  <verify>
    <automated>npx vitest run src/ui/api/editable-file-api.test.ts</automated>
  </verify>
  <done>All 5 API tests pass; classifyByExtension exported; TAILNET_URL_RE_CLIENT exported with the /g flag; frontend + backend whitelist Set members verified identical via manual diff or matching tests. `diff <(grep -oE '"[^"]+"' src/ui/features/pretty-view/editable-file-whitelist.ts | sort) <(grep -oE '"[^"]+"' src/backend/utils/editable-file-whitelist.ts | sort)` shows only comment differences, not Set-member differences.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useEditableFileEligibility hook + tests</name>
  <files>src/ui/features/pretty-view/use-editable-file-eligibility.ts, src/ui/features/pretty-view/use-editable-file-eligibility.test.ts</files>
  <read_first>
    - /home/ubuntu/skynet-tiffany/.planning/phases/40-text-editor-in-skynet/40-CONTEXT.md (D-01, D-02, D-04 — the discard-bytes rule is central to the hook's shape)
    - /home/ubuntu/skynet-tiffany/.planning/phases/40-text-editor-in-skynet/40-RESEARCH.md § Ex 2 (eligibility hook full source) + § Common Pitfalls #8 (URL parsing with query strings) + § Open Questions #2 (per-message vs batched)
    - /home/ubuntu/skynet-tiffany/src/ui/features/pretty-view/use-pretty-view-uploads.test.ts lines 1-50 (test env conventions — jsdom + @testing-library/react + renderHook usage in this codebase)
  </read_first>
  <behavior>
    - Signature: useEditableFileEligibility(messageEventId: string | null, messageBody: string): Set<string>
    - Initial return value: empty Set<string> (safe default — no affordances render until effect resolves)
    - Effect keyed on [messageEventId, messageBody]. On messageEventId === null, effect returns without doing work (safe for messages without a stable id).
    - For each match of TAILNET_URL_RE_CLIENT.exec (or messageBody.match) on messageBody:
      - Parse URL via new URL(url); extract filename via decodeURIComponent(url.pathname.split("/").pop() ?? "") — this correctly handles ?query-string cache-busters per Pitfall 8
      - Compute extension via filename.includes(".") ? filename.split(".").pop()!.toLowerCase() : null
      - SYNC PATH: if classifyByExtension(extension, filename) === true → add url to the local set; continue (no fetch fires)
      - ASYNC PATH: otherwise call fetchTailnetUrl(url); await result; if cancelledRef.current is true return early (don't touch state after unmount); if result.isTextByBytes === true add url to set
      - Wrap the fetchTailnetUrl call in try/catch — on any error skip the URL silently (other URLs in the same message continue to be classified independently). Do NOT surface fetch errors here; the editor's own fetch-at-open path (Plan 40-03) handles user-facing errors per D-04.
    - After processing all URLs, if cancelledRef.current is false, setEligibleUrls(new Set(eligibleUrls)) to commit the batch (single setState — do not commit per URL, to avoid render thrash)
    - Cleanup function sets cancelledRef.current = true; the next effect run resets it to false at effect entry
    - DISCARD-BYTES INVARIANT: the hook never stashes result.contentBase64 anywhere reachable outside the async closure. No ref, no Map, no cache — the byte payload is consumed only by classifyByExtension read AND (optionally) by inspecting isTextByBytes. contentBase64 MUST NOT be exposed on the return value or via any side effect.
    - Test 1 (whitelist hit — no fetch): message body = "here you go [notes.md](http://100.64.0.1:8000/notes.md)"; vi.mock fetchTailnetUrl to a spy; render hook with eventId="e1"; wait for effect; assert eligibleUrls has URL AND fetchTailnetUrl was called ZERO times
    - Test 2 (extensionless-but-text — fetch runs and returns true): message body = "http://100.64.0.1:8000/myscript"; stub fetchTailnetUrl to resolve { isTextByExt: false, isTextByBytes: true, ...rest }; assert eligibleUrls contains the URL AND fetchTailnetUrl called exactly once
    - Test 3 (extensionless-binary — fetch runs and returns false): stub fetchTailnetUrl to resolve { isTextByExt: false, isTextByBytes: false, ...rest }; assert eligibleUrls is empty
    - Test 4 (fetch error — silent skip): stub fetchTailnetUrl to reject; assert eligibleUrls is empty AND no unhandled promise rejection surfaces
    - Test 5 (multi-URL independence): body contains ONE whitelist-hit URL and ONE extension-miss URL; stub returns isTextByBytes: false for the miss; assert eligibleUrls contains ONLY the whitelist-hit URL AND fetchTailnetUrl called exactly once (only for the miss)
    - Test 6 (URL with query string — Pitfall 8 defense): body contains "http://100.64.0.1:8000/notes.md?nocache=1"; regex matches full string including query; assert eligibleUrls contains the full URL string AND that the internal filename extraction correctly used "notes.md" (verified by fetchTailnetUrl NOT being called — extension check hit)
    - Test 7 (null eventId): messageEventId=null; assert eligibleUrls stays empty AND fetchTailnetUrl called zero times AND no effect body ran
    - Test 8 (re-run on eventId change): render with eventId="e1", stub returns [nothing eligible]; re-render with eventId="e2" and different body; assert effect body ran twice
    - Test 9 (cancel on unmount before fetch resolves): render hook with a body containing an extension-miss URL; stub fetchTailnetUrl to return a Promise that never resolves within the test; unmount hook; wait; assert setEligibleUrls was NOT called after unmount (no React state update on unmounted component warning)
    - Test 10 (contentBase64 leak guard — architectural test): call the hook, wait for effect; assert typeof (eligibleUrls as any).contentBase64 === "undefined" AND the returned Set contains only string URLs (no object shapes). This test proves the D-04 discard-bytes invariant at the API surface.
  </behavior>
  <action>
    Create src/ui/features/pretty-view/use-editable-file-eligibility.ts following Research §Ex 2 as the source-of-truth structure:

    Imports: useEffect, useRef, useState from "react"; fetchTailnetUrl from "@/api/editable-file-api"; classifyByExtension, TAILNET_URL_RE_CLIENT from "./editable-file-whitelist".

    Function signature per behavior above.

    Effect body (inside useEffect):
      1. cancelledRef.current = false at effect entry
      2. Return cleanup that sets cancelledRef.current = true
      3. If messageEventId === null return (no work)
      4. TAILNET_URL_RE_CLIENT is /g — use .match(regex) or reset .lastIndex before .exec loop; recommend messageBody.match(TAILNET_URL_RE_CLIENT) ?? [] for simplicity per Research Ex 2
      5. If matches.length === 0 return (no work; do NOT setEligibleUrls(new Set()) — leave existing state alone because the hook may have been called with a stale body and the caller depends on the eligibility set being stable across renders that don't change eventId/body)
      6. Start async IIFE per Research Ex 2 structure
      7. On completion, if !cancelledRef.current, single setEligibleUrls(new Set(eligible))

    Header docblock cites: D-01 (URL detection is purely frontend, no agent-side change); D-02 (whitelist first, byte-sniff fallback); D-04 (bytes discarded — the return value is Set<string>, NOT any object carrying byte payloads). Include an in-code comment on the try/catch inside the loop: "Silent skip: eligibility failure does not surface to the user. The editor open path (EditableFileModal, Plan 40-03) fires its own fresh fetch and surfaces the error explicitly per D-04 'visible failure over silent maybe-stale'."

    Create src/ui/features/pretty-view/use-editable-file-eligibility.test.ts:
      - vi.mock("@/api/editable-file-api", ...) with fetchTailnetUrl exported as vi.fn() (each test resets via mockReset + mockResolvedValue/mockRejectedValue)
      - Use renderHook + waitFor from @testing-library/react (already resident — see use-pretty-view-uploads.test.ts pattern for existing renderHook usage)
      - Tests 1-10 above
      - Test 10 (contentBase64 leak guard) uses TypeScript's `as unknown as` cast + runtime typeof check to catch the invariant

    Do NOT add cross-message URL de-duplication at module scope in this plan — Research §Open Question 5 flags that as MEDIUM confidence and worth deferring until the naive impl proves too chatty. If it becomes an observable problem, that's a follow-up plan.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/use-editable-file-eligibility.test.ts</automated>
  </verify>
  <done>All 10 hook tests pass; Test 10 explicitly proves the D-04 discard-bytes invariant at the API surface; hook header docblock cites D-01, D-02, D-04; silent-skip comment inside the try/catch cites the D-04 error-surface handoff to Plan 40-03.</done>
</task>

<task type="auto">
  <name>Task 3: Frontend build verify + regression guard</name>
  <files>(no source diffs — verification only)</files>
  <read_first>
    - /home/ubuntu/skynet-tiffany/package.json (scripts section)
    - /home/ubuntu/skynet-tiffany/CLAUDE.md if present (frontend build discipline)
  </read_first>
  <acceptance_criteria>
    - `npm run build` exits 0 (vite build succeeds — the new hook + api helper are pure TS with no side effects at import time so bundle emission MUST succeed)
    - `npx tsc --noEmit` exits 0 (types are all resolvable — the TailnetFetchResult type is imported cleanly from the new api file; the Set<string> return type of the hook is inferred correctly)
    - `npx vitest run src/ui/api/editable-file-api.test.ts src/ui/features/pretty-view/use-editable-file-eligibility.test.ts` exits 0 with test count === 15 (5 api + 10 hook)
    - `npx vitest run` exits 0 (full frontend test suite regression check — total count strictly greater than pre-plan baseline by exactly 15)
    - `diff <(grep -oE '"[^"]+"' src/ui/features/pretty-view/editable-file-whitelist.ts | sort -u) <(grep -oE '"[^"]+"' src/backend/utils/editable-file-whitelist.ts | sort -u)` returns no differences EXCEPT for lines that are inside the header comment (both files' constant Set members MUST be byte-identical; verify by extracting quoted strings, sorting, deduping, and diffing)
    - `grep -c "MIRROR" src/ui/features/pretty-view/editable-file-whitelist.ts` returns >= 1
    - `grep -c "contentBase64" src/ui/features/pretty-view/use-editable-file-eligibility.ts` returns 0 — the hook file MUST NOT reference contentBase64 in any code path (belt-and-suspenders for D-04); if it does reference it, the returned Set may leak bytes via a stashed ref
  </acceptance_criteria>
  <action>
    Run the four verification commands (tsc, build, targeted vitest, full vitest) and record exit codes + test counts in SUMMARY.md. If any gate fails, fix the offending file — do NOT paper over.

    The "contentBase64 grep === 0" gate is the D-04 invariant check. If the hook needs to reference isTextByBytes (which it does per Behavior), that's fine — isTextByBytes is a bool on the response, not a byte payload. The invariant is specifically about NOT persisting result.contentBase64 anywhere.

    No commits made here; commits happen at task completion boundaries. This task is purely a gate.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build && npx vitest run src/ui/api/editable-file-api.test.ts src/ui/features/pretty-view/use-editable-file-eligibility.test.ts</automated>
  </verify>
  <done>tsc + build + vitest exit 0; frontend + backend whitelist file quoted-string diff shows no member differences; contentBase64 grep on the hook file returns 0.</done>
</task>

</tasks>

<verification>
- Frontend build: `npm run build` exits 0
- Full frontend tests: `npx vitest run` green with pre-plan baseline + 15 net-new tests
- Whitelist twin consistency: byte-identical Set members between frontend and backend copies
- D-04 discard-bytes invariant: grep for contentBase64 in the hook file returns 0
- No new npm packages
</verification>

<success_criteria>
- LOCKED D-01 frontend surface exists (URL scan runs per-message, no agent-side dependency).
- LOCKED D-02 both paths (sync whitelist + async byte-sniff via backend) both wired.
- LOCKED D-04 discard-bytes rule structurally enforced (the hook's return type is Set<string> and the source file does not reference contentBase64).
- Frontend + backend whitelist twins remain in lockstep for their Set members.
</success_criteria>

<output>
Create `.planning/phases/40-text-editor-in-skynet/40-02-SUMMARY.md`. Include: files created; test counts (pre-plan baseline + 15 new); confirmation of the frontend-backend whitelist diff (no member differences); the D-04 discard-bytes invariant assertion (grep result); and a next-plan handoff pointing Plan 40-03 at the two symbols it will import: `useEditableFileEligibility` and `fetchTailnetUrl`.
</output>
