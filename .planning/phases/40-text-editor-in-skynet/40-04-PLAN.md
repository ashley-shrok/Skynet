---
phase: 40-text-editor-in-skynet
plan: 04
type: execute
wave: 3
depends_on: [40-02, 40-03]
files_modified:
  - src/ui/features/pretty-view/ChatMessage.tsx
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-view/ChatMessage.editable-file.test.tsx
  - src/ui/features/pretty-view/PrettyView.editable-file.test.tsx
autonomous: true
tags: [text-editor, wiring, chat-message, pretty-view, phase-40]
requirements: []
must_haves:
  truths:
    - "ChatMessage's ReactMarkdown `a` component override (L395-417 seam) renders EditableFileAffordance as a FRAGMENT SIBLING of the existing <a> anchor whenever the current message's eligibleUrls Set contains this URL. Anchor's target=_blank + rel=noopener+noreferrer behavior is preserved verbatim (D-03 additive-not-replacive)"
    - "ChatMessage calls useEditableFileEligibility(eventId ?? null, content) at the top of the assistant-branch code path (never for user messages — user messages don't carry agent-served links) and threads the returned Set through to the `a` override via closure — hook fires exactly once per message-content change"
    - "ChatMessage's bubble container div (currently L305-360) gains a stable className token that the affordance's desktop hover-reveal selector depends on — resolves Assumption A5 from Research (the affordance file uses [.pv-bubble:hover_&]:opacity-100 so the bubble MUST have className including 'pv-bubble')"
    - "Filename extraction in the `a` override uses new URL(href).pathname.split('/').pop() with decodeURIComponent — handles query strings correctly per Pitfall 8"
    - "PrettyView mounts a single instance of EditableFileModal ALONGSIDE the existing IdentityModal (near L2082) with open state managed by a new local useState<EditableFileOpenState | null>({url, filename, messageEventId, agentIdentityName}) — the state is null when closed and an object when open"
    - "PrettyView provides an onOpenEditor callback down through the ChatMessage prop tree; ChatMessage calls it from the affordance's onClick with the {messageEventId, url, filename} tuple"
    - "PrettyView provides an onStageEditedFile callback to EditableFileModal that calls uploads.stageAttachments('primary', [new File([content], filename, { type: guessMimeFromFilename(filename) ?? 'text/plain' })]) — implements D-06 save deposits to ComposeBox as a fresh attachment"
    - "guessMimeFromFilename is a small helper (either co-located in EditableFileModal.tsx or a new src/ui/features/pretty-view/guess-mime.ts) that maps common extensions to text mimetypes ('md' → 'text/markdown', 'json' → 'application/json', etc.) and falls back to 'text/plain' — used only for chip UX niceness per Research Assumption A8, NOT load-bearing"
    - "agentIdentityName is best-effort — sourced from the same identity-resolution PrettyView already does (see pvIdentity at L2085 or the same-line agent-identity-name that IdentityBadge uses); when unresolvable, null is passed and the modal's sub-header line simply omits (UI-SPEC L104 fallback chain)"
  artifacts:
    - path: "src/ui/features/pretty-view/ChatMessage.tsx"
      provides: "MODIFIED — eligibility hook wired at assistant-branch top; `a` override extended to render EditableFileAffordance sibling on eligible URLs; bubble container gains pv-bubble class; new prop onOpenEditor threaded through"
      exports: []
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "MODIFIED — EditableFileModal mount alongside IdentityModal; open-state useState hook; onOpenEditor callback passed to ChatMessage in the render map at L2274; onStageEditedFile callback wired to uploads.stageAttachments('primary', [File])"
      exports: []
    - path: "src/ui/features/pretty-view/ChatMessage.editable-file.test.tsx"
      provides: "Tests: assistant message with a whitelist-hit tailnet URL renders anchor + affordance sibling; user message with a tailnet URL renders anchor only (no affordance — hook not called); anchor's href + target + rel unchanged; affordance click fires onOpenEditor with correct {messageEventId, url, filename}; message with multiple eligible URLs renders one affordance per URL; message with no tailnet URLs renders no affordances"
      exports: []
    - path: "src/ui/features/pretty-view/PrettyView.editable-file.test.tsx"
      provides: "Tests: openEditor call from a ChatMessage instance sets the modal's open state; onStageEditedFile invocation calls uploads.stageAttachments('primary', [File]) with a File carrying the correct name + content; closing the modal clears the open state to null"
      exports: []
  key_links:
    - from: "src/ui/features/pretty-view/ChatMessage.tsx"
      to: "src/ui/features/pretty-view/use-editable-file-eligibility.ts"
      via: "import { useEditableFileEligibility } from './use-editable-file-eligibility'"
      pattern: "useEditableFileEligibility"
    - from: "src/ui/features/pretty-view/ChatMessage.tsx"
      to: "src/ui/features/pretty-view/EditableFileAffordance.tsx"
      via: "import { EditableFileAffordance } from './EditableFileAffordance'; rendered as fragment sibling inside the `a` component override in the components map"
      pattern: "EditableFileAffordance"
    - from: "src/ui/features/pretty-view/PrettyView.tsx"
      to: "src/ui/features/pretty-view/EditableFileModal.tsx"
      via: "import EditableFileModal from './EditableFileModal'; mounted at ~L2082 alongside IdentityModal"
      pattern: "EditableFileModal"
    - from: "src/ui/features/pretty-view/PrettyView.tsx"
      to: "src/ui/features/pretty-view/use-pretty-view-uploads.ts"
      via: "onStageEditedFile → uploads.stageAttachments('primary', [file]) — using the existing hook API at L346 that PrettyView already instantiates at L814"
      pattern: "uploads\\.stageAttachments"
---

<objective>
Wire the components built in Plan 40-03 (EditableFileAffordance + EditableFileModal) into the two existing pretty-view files where the message-render + modal-mount seams live: ChatMessage.tsx (adds the affordance as a sibling to the anchor in the ReactMarkdown `a` override, keys the render on the per-message eligibility hook from Plan 40-02) and PrettyView.tsx (mounts the modal alongside IdentityModal, manages open state, and hands the save-deposit callback that closes the loop to the existing usePrettyViewUploads hook).

Purpose: closes LOCKED decisions D-03 (affordance renders alongside the anchor — never replacing it) at the render-tree level, D-06 (save → deposit-as-attachment) at the wiring level, and D-07 (return trip uses existing reply-with-attachment path — because the deposit lands in usePrettyViewUploads' staged set, the existing ComposeBox send flow handles the rest without any change).

Output: two files modified + two new test files covering the wiring specifically. No new npm dependencies. No new component files (all components exist from Plan 40-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-text-editor-in-skynet/40-CONTEXT.md
@.planning/phases/40-text-editor-in-skynet/40-UI-SPEC.md
@.planning/phases/40-text-editor-in-skynet/40-RESEARCH.md
@.planning/phases/40-text-editor-in-skynet/40-02-SUMMARY.md
@.planning/phases/40-text-editor-in-skynet/40-03-SUMMARY.md
@src/ui/features/pretty-view/ChatMessage.tsx
@src/ui/features/pretty-view/PrettyView.tsx
@src/ui/features/pretty-view/use-pretty-view-uploads.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ChatMessage.tsx wiring — hook call + `a` override extension + pv-bubble class</name>
  <files>src/ui/features/pretty-view/ChatMessage.tsx, src/ui/features/pretty-view/ChatMessage.editable-file.test.tsx</files>
  <read_first>
    - /home/ubuntu/skynet-tiffany/src/ui/features/pretty-view/ChatMessage.tsx L1-100 (imports + component signature — the new onOpenEditor prop goes into the props type here at ~L59-67; the useEditableFileEligibility call goes AFTER the existing prop destructure but BEFORE the isUser derived const at L69 so it can key on isUser+eventId+content)
    - /home/ubuntu/skynet-tiffany/src/ui/features/pretty-view/ChatMessage.tsx L280-418 (the render body including the `a` override at L398-404 which is the surgical extension site)
    - /home/ubuntu/skynet-tiffany/src/ui/features/pretty-view/ChatMessage.tsx L305-360 (bubble container className list — pv-bubble token must be ADDED here without disturbing the ~50 existing Tailwind classes that Phase 4 Glass style depends on; add it as the FIRST class in the cn() list so it's discoverable via grep)
    - /home/ubuntu/skynet-tiffany/.planning/phases/40-text-editor-in-skynet/40-RESEARCH.md § Pattern 1 (the extended `a` override JSX example) + § Pitfall 1 (href comes from props, not node) + § Pitfall 2 (sibling not wrapper) + § Pitfall 8 (URL constructor for filename extraction)
    - /home/ubuntu/skynet-tiffany/.planning/phases/40-text-editor-in-skynet/40-02-SUMMARY.md (confirms useEditableFileEligibility export shape from Plan 40-02)
    - /home/ubuntu/skynet-tiffany/.planning/phases/40-text-editor-in-skynet/40-03-SUMMARY.md (confirms EditableFileAffordance export shape from Plan 40-03)
    - /home/ubuntu/skynet-tiffany/src/ui/features/pretty-view/ChatMessage.test.tsx (top ~50 lines — the existing test scaffolding pattern; new tests should follow the same imports + render + expect conventions)
  </read_first>
  <behavior>
    - New optional prop onOpenEditor?: (input: { messageEventId: string; url: string; filename: string }) => void added to ChatMessage's props type (signature per D-03/D-06 wiring)
    - useEditableFileEligibility(eventId ?? null, content) is called once per render at the top of the function body (after the props destructure, alongside isUser derivation). Return value stored as const eligibleUrls.
    - IMPORTANT: because assistant-message eligibility is the only path that matters (user messages don't carry agent-served links), the hook may still fire on user messages returning empty Set — this is fine (cheap) and simpler than a conditional-hook (which would violate the Rules of Hooks). Rely on the hook's own internal early-return when the messageEventId is null OR the message has no tailnet URLs. Explicitly document this design choice inline: "// D-01: hook fires for both roles; user messages never carry tailnet URLs so the Set stays empty. Simpler than a conditional hook (which would violate Rules of Hooks)."
    - Bubble container div (currently at L305-360) — the outermost wrapping div with className cn(...) — gains "pv-bubble" as the FIRST class in the cn() argument list. Its purpose: descendant CSS selectors like `.pv-bubble:hover &` in the EditableFileAffordance can target it as the hover ancestor. Add a comment above the cn() call: "// pv-bubble: hover-target class for descendants like EditableFileAffordance. Do NOT rename without updating [.pv-bubble:hover_&] selectors in child components."
    - The `a` component override (currently L398-404) is restructured to render a fragment with two children:
      - Child 1: the existing <a {...props} target="_blank" rel="noopener noreferrer" /> — unchanged behavior per D-03
      - Child 2: conditional <EditableFileAffordance ... /> when isEligible === true
    - isEligible determination inside the `a` override closure:
      - const href = props.href (Pitfall 1 defense — destructure from props, not from node)
      - if (!href || !eventId || !onOpenEditor) return null-else-branch (no affordance)
      - Parse: let parsed; try { parsed = new URL(href); } catch { return null-else; } — invalid URL means it's not the tailnet pattern anyway
      - const filename = decodeURIComponent(parsed.pathname.split("/").pop() ?? "") (Pitfall 8 — URL constructor strips query strings before pathname)
      - const isEligible = eligibleUrls.has(href) — the eligibility hook keyed the Set on the raw matched URL string, so lookup by href matches when href is exactly what the regex captured
      - IMPORTANT: react-markdown may pass an href that differs slightly from the raw text (e.g., percent-encoding normalization). If tests reveal a mismatch, normalize both sides via new URL(href).toString() — flagged in an inline TODO comment
    - Affordance onClick calls onOpenEditor({ messageEventId: eventId, url: href, filename })
    - Test 1 (assistant message + whitelist-hit URL): mount <ChatMessage role="assistant" eventId="e1" content="here [notes.md](http://100.64.0.1:8000/notes.md)" onOpenEditor={vi.fn()} />; mock useEditableFileEligibility to return new Set(["http://100.64.0.1:8000/notes.md"]); wait; assert screen.getByRole("link", { name: /notes.md/i }) exists AND getByRole("button", { name: /Edit notes.md/i }) exists AND the button is a sibling (not a wrapper) of the link
    - Test 2 (user message with tailnet URL — hook returns empty, no affordance): mount role="user" with the same content + URL; mock hook returns empty Set; assert getByRole("link") exists AND queryByRole("button", { name: /Edit/i }) === null (no affordance)
    - Test 3 (assistant + non-eligible URL): assistant message with a random URL not in eligibleUrls Set; assert link exists, no affordance button
    - Test 4 (anchor semantics preserved — target + rel): after render, screen.getByRole("link").getAttribute("target") === "_blank" AND getAttribute("rel") === "noopener noreferrer"; assert the anchor href is unchanged from the source
    - Test 5 (affordance onClick fires onOpenEditor with correct payload): from Test 1 setup, fireEvent.click on the affordance button; expect the onOpenEditor mock to have been called with { messageEventId: "e1", url: "http://100.64.0.1:8000/notes.md", filename: "notes.md" }
    - Test 6 (multi-URL — one affordance per eligible URL): assistant message with two tailnet URLs in the body; hook returns Set with both; assert two affordance buttons render + each has the correct filename in its aria-label
    - Test 7 (bubble container has pv-bubble class): render any assistant message; querySelector('.pv-bubble') returns the outermost bubble div (not null)
    - Test 8 (onOpenEditor prop optional — no crash when omitted): render without onOpenEditor; assert no crash + no affordance renders (safe degrade)
    - Test 9 (query-string URL — Pitfall 8 defense): assistant message content contains "http://100.64.0.1:8000/notes.md?nocache=1"; hook returns Set with the full string (including query); assert the affordance renders with filename === "notes.md" (query stripped correctly) NOT "notes.md?nocache=1"
    - Test 10 (Pitfall 1 defense — href destructured from props): the affordance renders when href is present on props (test setup naturally exercises this; the hidden failure mode of reaching for node.properties.href instead is impossible if the source code correctly destructures)
  </behavior>
  <action>
    Modify src/ui/features/pretty-view/ChatMessage.tsx:

    1. Imports (add near L1-11): `import { useEditableFileEligibility } from "./use-editable-file-eligibility";` and `import { EditableFileAffordance } from "./EditableFileAffordance";`

    2. Props type (extend the object at L59-67): add `onOpenEditor?: (input: { messageEventId: string; url: string; filename: string }) => void;` — comment above the prop: "// Phase 40 D-03/D-06: opens the EditableFileModal for a specific tailnet URL. Optional so callers that don't provide it (tests, historical mount sites) safely skip the affordance render."

    3. Destructure onOpenEditor from the props destructure at ~L52-58 alongside eventId, autoplayArmed, etc.

    4. Add hook call after props destructure but before the isUser derivation (~L69): `const eligibleUrls = useEditableFileEligibility(eventId ?? null, content);` + the design-choice comment above the call: "// D-01: hook fires for both roles; user messages never carry tailnet URLs so the Set stays empty. Simpler than a conditional hook (Rules of Hooks)."

    5. Locate the bubble container div at ~L305-360 (the outermost wrapping div with className={cn(...)} that carries the flex+justify+bubble styling). Add "pv-bubble" as the FIRST string in the cn() argument list. Add the comment above the cn(): "// pv-bubble: hover-target class for descendants like EditableFileAffordance. Do NOT rename without updating [.pv-bubble:hover_&] selectors in child components."

    6. Modify the `a` component override at L398-404. New body:
       - Extract via destructure: const { node, href, ...rest } = props (Pitfall 1 defense)
       - Compute filename/isEligible inline as per Behavior
       - Return a React fragment containing the existing <a> AND the conditional <EditableFileAffordance onOpen={...} filename={filename} /> sibling
       - When onOpenEditor is provided AND isEligible: affordance renders with onOpen={() => onOpenEditor({ messageEventId: eventId!, url: href!, filename })}
       - Add comment above the override: "// D-03: affordance renders as fragment sibling — anchor semantics (target/rel/click) preserved verbatim per LOCKED additive-not-replacive."

    Create src/ui/features/pretty-view/ChatMessage.editable-file.test.tsx per Behavior tests 1-10. Follow the existing ChatMessage.test.tsx test conventions:
    - vi.mock("./use-editable-file-eligibility", () => ({ useEditableFileEligibility: vi.fn().mockReturnValue(new Set()) })) with per-test overrides via .mockReturnValueOnce
    - Do NOT mock EditableFileAffordance — the real component is small and tested independently in Plan 40-03; using the real one exercises the wiring end-to-end
    - Use @testing-library/react's render + screen + fireEvent + within
    - The Test 1 "sibling not wrapper" assertion: after render, get the anchor element (screen.getByRole("link")); get its parentElement; get all children of that parent; assert the anchor + the button are BOTH direct children of the same parent (siblings). Additionally, assert that the button.parentElement === anchor.parentElement.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/ChatMessage.editable-file.test.tsx</automated>
  </verify>
  <done>All 10 ChatMessage wiring tests pass; grep "pv-bubble" src/ui/features/pretty-view/ChatMessage.tsx returns >= 2 (class + comment); grep "useEditableFileEligibility" src/ui/features/pretty-view/ChatMessage.tsx returns >= 1; no existing ChatMessage test regresses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PrettyView.tsx wiring — modal mount + open-state + stage-attachment callback</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx, src/ui/features/pretty-view/PrettyView.editable-file.test.tsx</files>
  <read_first>
    - /home/ubuntu/skynet-tiffany/src/ui/features/pretty-view/PrettyView.tsx L800-830 (uploads hook instantiation — the `uploads` handle to reuse for the stageAttachments deposit call)
    - /home/ubuntu/skynet-tiffany/src/ui/features/pretty-view/PrettyView.tsx L2075-2100 (IdentityModal mount region — the EditableFileModal mount goes alongside this, but portal target differs — EditableFileModal does NOT pass container=chatRegionEl per Plan 40-03 Task 2 behavior; it portals to document.body)
    - /home/ubuntu/skynet-tiffany/src/ui/features/pretty-view/PrettyView.tsx L2270-2288 (the ChatMessage render call site — this is where onOpenEditor gets threaded down)
    - /home/ubuntu/skynet-tiffany/src/ui/features/pretty-view/use-pretty-view-uploads.ts L102-107 (the stageAttachments API signature; target must be "primary" per D-06 — that's the ComposeBox's primary attachment set)
    - /home/ubuntu/skynet-tiffany/.planning/phases/40-text-editor-in-skynet/40-03-SUMMARY.md (EditableFileModal props shape)
  </read_first>
  <behavior>
    - Import EditableFileModal from "./EditableFileModal" near the other pretty-view component imports (~top of file alongside ChatMessage)
    - New local state near the other useStates at the top of the component body:
      - const [editorOpenState, setEditorOpenState] = useState<null | { url: string; filename: string; messageEventId: string; agentIdentityName: string | null }>(null);
    - New callback: const handleOpenEditor = useCallback((input: { messageEventId: string; url: string; filename: string }) => { setEditorOpenState({ ...input, agentIdentityName: pvIdentity?.displayName ?? null }); }, [pvIdentity?.displayName]);
    - New callback: const handleStageEditedFile = useCallback((filename: string, content: string) => { const type = guessMimeFromFilename(filename) ?? "text/plain"; const file = new File([content], filename, { type }); uploads.stageAttachments("primary", [file]); }, [uploads]);
    - Add small helper `guessMimeFromFilename(filename: string): string | null` — either inline as a module-scope function in PrettyView.tsx OR extracted to src/ui/features/pretty-view/guess-mime.ts. Keep it simple: a Map of common ext → mime pairs (md→text/markdown, json→application/json, yaml/yml→application/yaml, ts/tsx/js/jsx→text/typescript or application/javascript, py→text/x-python, sh→application/x-sh, txt/log/env→text/plain, html→text/html, css→text/css, xml→application/xml, csv→text/csv, sql→application/sql), fallback to null. Per Research A8 this is nice-to-have not load-bearing; the chip strip renders name+size not mime. Recommend inline in PrettyView.tsx as a bare const function (avoid new-file overhead for a 15-line helper).
    - Modify the ChatMessage render call at ~L2274-2283 to thread the callback: add onOpenEditor={handleOpenEditor} as a new prop
    - Modal mount: after the IdentityModal mount block (L2081-2090), add:
      - {editorOpenState && (<EditableFileModal open={true} onOpenChange={(open) => { if (!open) setEditorOpenState(null); }} messageEventId={editorOpenState.messageEventId} url={editorOpenState.url} filename={editorOpenState.filename} agentIdentityName={editorOpenState.agentIdentityName} onStageEditedFile={handleStageEditedFile} />)}
      - Comment above: "// Phase 40 D-05/D-06: editor modal alongside IdentityModal. Portal target is document.body (see EditableFileModal — Portal has no container prop) so inset-4 covers composer too. onStageEditedFile deposits the saved File into uploads.stageAttachments('primary') — chip auto-mounts via existing AttachmentChipStrip wiring at L2511."
    - Test 1 (modal opens on affordance click — end-to-end wiring): render <PrettyView ... /> with an assistant message containing a whitelist-hit tailnet URL; stub useEditableFileEligibility to return a Set with the URL; wait; find the affordance button; fireEvent.click; assert the modal opens (editorOpenState set to non-null; look for the modal's dialog role in document.body)
    - Test 2 (modal close clears state): from Test 1 setup, click the modal's close X; assert editorOpenState becomes null (modal unmounts — dialog role no longer in document)
    - Test 3 (save deposits an attachment): from Test 1 setup, wait for the modal's textarea to appear (mock the fetch success); type new content; click Save; assert uploads.stageAttachments was called with ("primary", [expect.any(File)]) AND the File.name === the URL's filename AND (await file.text()) === the typed content
    - Test 4 (agentIdentityName passthrough): mock pvIdentity to have displayName "tanya"; open editor; assert the EditableFileModal received agentIdentityName === "tanya" in its props
    - Test 5 (multiple opens — different URLs): open editor for URL A, close, open for URL B; assert on second open the modal receives url === URL B (not stale A)
  </behavior>
  <action>
    Modify src/ui/features/pretty-view/PrettyView.tsx per Behavior spec:

    1. Add import: `import EditableFileModal from "./EditableFileModal";` at ~L21 (alongside `import { ChatMessage } from "./ChatMessage";`)

    2. Add module-scope helper `guessMimeFromFilename` near the top of the file (outside the component, alongside other helpers if any) — a plain switch or Map lookup returning string | null. Header comment: "// Phase 40 (Research A8): nice-to-have mime hint for chip UX. Not load-bearing — chip strip renders name+size, not mime."

    3. Add local state `editorOpenState` alongside other useStates near the top of the component body (line target: after the other pretty-view local state declarations; use grep to find a good landing spot near line ~800-900 where uploads hook is instantiated)

    4. Add handleOpenEditor and handleStageEditedFile useCallbacks alongside the other useCallbacks (e.g., near handleInjectedTurnReady at L2020 or near the composer callbacks around L2500). Both are pure closures over stable dependencies.

    5. Modify the ChatMessage render at ~L2274-2283: add onOpenEditor={handleOpenEditor} as a new prop line. Do NOT reorder existing props (rebase-hostile).

    6. Add the EditableFileModal mount after the IdentityModal block (L2081-2090). Structural pattern: `{editorOpenState && (<EditableFileModal ... />)}` — the null-guard means React unmounts the modal when state clears (verifies Test 2 assertion cleanly).

    7. Add a comment above the modal mount citing D-05 (reuse), D-06 (save-deposit path), Research §Pitfall 7 (portal target — this modal DOES cover the composer, unlike IdentityModal), and the D-07 handoff (deposit into uploads.stageAttachments → existing ComposeBox → existing send-with-attachments path is untouched).

    Create src/ui/features/pretty-view/PrettyView.editable-file.test.tsx per Behavior tests 1-5. This is a wiring test — mock the heavy dependencies (session-file stream, WebSocket, session store, etc.) using the same patterns as existing PrettyView.*.test.tsx files (e.g., PrettyView.compose-send.test.tsx and PrettyView.autoplay.test.tsx are good precedents to read for the mock-scaffolding shape).

    Mocking strategy:
    - vi.mock("./use-editable-file-eligibility") to return controllable Sets per test
    - vi.mock("@/api/editable-file-api") to control fetchTailnetUrl responses
    - vi.mock("sonner") toast
    - The existing tests in the pretty-view folder set up minimal store/session/WS mocks — reuse those setup helpers (import from ./test-utils or similar if it exists, else copy the minimal scaffold from the closest existing PrettyView.*.test.tsx)
    - Do NOT mock EditableFileModal — using the real component ensures the wiring truly opens the modal (Test 1 assertion of dialog-in-document.body proves end-to-end wiring)

    NOTE on test scope: this plan's PrettyView test file focuses ONLY on the editor wiring — it does NOT re-test everything PrettyView already covers. Existing PrettyView.*.test.tsx files remain untouched and continue to pass (regression check via full-suite vitest run).
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/PrettyView.editable-file.test.tsx src/ui/features/pretty-view/ChatMessage.editable-file.test.tsx</automated>
  </verify>
  <done>All 5 PrettyView wiring tests + all 10 ChatMessage wiring tests pass; grep "EditableFileModal" src/ui/features/pretty-view/PrettyView.tsx returns >= 2 (import + mount); grep "handleStageEditedFile" src/ui/features/pretty-view/PrettyView.tsx returns >= 2 (definition + prop passthrough); no existing PrettyView.*.test.tsx regresses.</done>
</task>

<task type="auto">
  <name>Task 3: Full frontend + backend build + test-suite regression guard</name>
  <files>(no source diffs — verification only)</files>
  <read_first>
    - /home/ubuntu/skynet-tiffany/package.json (scripts section)
    - /home/ubuntu/skynet-tiffany/CLAUDE.md if present
  </read_first>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (all types resolve; onOpenEditor prop chain typed correctly end-to-end)
    - `NODE_OPTIONS=--max-old-space-size=4096 npm run build:backend` exits 0 (backend unaffected by frontend wiring changes but confirm no bleed-through)
    - `npm run build` exits 0 (vite emission clean; new EditableFileModal + affordance are bundled)
    - `npx vitest run` exits 0 (FULL test suite green — the wiring tests add ~15 net-new tests: 10 ChatMessage + 5 PrettyView; total count MUST be **greater than or equal to** pre-plan-40 baseline + 70 (17 Plan 40-01 + 15 Plan 40-02 + 23 Plan 40-03 [rev-2: +6 for the confirm-gate tests — 4 in EditableFileModal + 2 in GlobalFileTab] + 15 this plan). Record the actual delta in the run summary. Do NOT paper over unexpected additional tests — if the count exceeds baseline+70, investigate whether earlier plans grew during execution and update this note.)
    - `grep -c "onOpenEditor" src/ui/features/pretty-view/ChatMessage.tsx` returns >= 3 (props type + destructure + `a` override usage)
    - `grep -c "onOpenEditor" src/ui/features/pretty-view/PrettyView.tsx` returns >= 1 (prop passthrough on ChatMessage render)
    - `grep -c "pv-bubble" src/ui/features/pretty-view/ChatMessage.tsx` returns >= 2 (class + comment)
    - `grep -c "EditableFileModal" src/ui/features/pretty-view/PrettyView.tsx` returns >= 2 (import + JSX mount)
    - `grep -c "stageAttachments" src/ui/features/pretty-view/PrettyView.tsx | tr -d ' '` — verify the new handleStageEditedFile call is present; existing PrettyView already references stageAttachments so this grep returns >=2 (existing + new)
    - `grep -c "container=" src/ui/features/pretty-view/PrettyView.tsx` — should be unchanged from pre-plan baseline (do NOT accidentally add container= to the new EditableFileModal mount — it must portal to document.body)
    - No new npm packages: `git diff package.json` shows no dependency changes
  </acceptance_criteria>
  <action>
    Run the full verification suite. If any gate fails, fix at source rather than at test — the plan's structural invariants (pv-bubble class present, onOpenEditor threaded, EditableFileModal mounted without container prop) are load-bearing for downstream deploy verification.

    Confirm the full pre-plan → post-plan test-count math holds. If the delta is off, either an existing test regressed silently OR the new plan skipped adding one of its promised tests — investigate the diff, do NOT paper over.

    No commits happen in this task. Commits per-task at Tasks 1 and 2 boundaries.
  </action>
  <verify>
    <automated>npx tsc --noEmit && NODE_OPTIONS=--max-old-space-size=4096 npm run build:backend && npm run build && npx vitest run</automated>
  </verify>
  <done>tsc + build:backend + build + full vitest suite all exit 0; all 8 grep gates satisfied; new-test count math holds; no npm dependency changes.</done>
</task>

</tasks>

<verification>
- Full frontend + backend build succeeds
- Full test suite exits green with baseline + all 64 new Phase 40 tests
- Wiring invariants: pv-bubble class present, onOpenEditor threaded end-to-end, EditableFileModal portals to document.body, save-deposit hits uploads.stageAttachments("primary")
</verification>

<success_criteria>
- LOCKED D-03 (additive-not-replacive) wired at the render tree: existing anchor semantics preserved verbatim; affordance renders alongside.
- LOCKED D-06 (save deposits fresh attachment) wired end-to-end: EditableFileModal save → PrettyView handleStageEditedFile → uploads.stageAttachments("primary", [File]) → existing chip strip auto-mounts.
- LOCKED D-07 (return trip uses existing reply-with-attachment path) satisfied by construction: the phase adds zero new backend routes for the return trip (Plan 40-01's proxy is inbound-fetch only) and zero new WebSocket message types — the deposited attachment flows through the byte-unchanged pipeline that Phase 05 shipped.
- Every reuse target from D-05 is left byte-untouched (GlobalFileTab, GlobalFilesModal, AttachmentChipStrip, ComposeBox, usePrettyViewUploads all unchanged in this plan).
</success_criteria>

<output>
Create `.planning/phases/40-text-editor-in-skynet/40-04-SUMMARY.md`. Include: files modified; test counts (baseline + all 64 new Phase 40 tests across Plans 40-01..40-04); confirmation of all 8 grep gates; explicit "reuse targets left byte-untouched" list (GlobalFileTab.tsx, GlobalFilesModal.tsx, AttachmentChipStrip.tsx, ComposeBox.tsx, use-pretty-view-uploads.ts); handoff to Plan 40-05 pointing at the artifacts needed for the deploy checkpoint (build-verify log + UAT checklist covering all seven LOCKED decisions on production Skynet + patch-entry draft).
</output>
