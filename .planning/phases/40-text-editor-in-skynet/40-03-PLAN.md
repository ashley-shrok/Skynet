---
phase: 40-text-editor-in-skynet
plan: 03
type: execute
wave: 2
depends_on: [40-02]
files_modified:
  - src/ui/features/pretty-view/EditableFileAffordance.tsx
  - src/ui/features/pretty-view/EditableFileModal.tsx
  - src/ui/features/pretty-view/EditableFileAffordance.test.tsx
  - src/ui/features/pretty-view/EditableFileModal.test.tsx
autonomous: true
tags: [text-editor, ui-components, editor-modal, phase-40]
requirements: []
must_haves:
  truths:
    - "EditableFileAffordance renders as an inline sibling button (never a wrapper around the anchor); icon-only on touch devices (44x44 min target, always visible at ~72% opacity); icon+label on desktop hover-reveal (opacity 0 → 100% via .pv-bubble:hover_& or a group-hover Tailwind pattern). Coral (#ffb896 / --color-pv-code-fg) at rest → identity-hue on hover with drop-shadow (D-03, UI-SPEC L124)"
    - "EditableFileModal chrome is a forked copy of GlobalFilesModal.tsx L186-217 (DialogPrimitive.Root + Portal + Overlay + Content, blue-glass gradient, backdrop-filter, onInteractOutside preventDefault guard) but WITHOUT the host picker (D-05 strip #1) and WITHOUT the multi-file tabs strip (D-05 strip #2). Header shows 'Edit {filename}' + optional 'from {agentIdentityName}' muted sub-header + glass X close button (verbatim from GlobalFilesModal.tsx L246-270)"
    - "On modal open, a fresh POST /pretty-view/fetch-tailnet-url fires via fetchTailnetUrl(url) — the cached bytes from the eligibility hook (Plan 40-02) are NEVER used here. Success → GlobalFileTab (reused verbatim, Plan 40-05's D-05 reuse) renders with TabState.ready({ content: atob(contentBase64), mtime: <stable sentinel captured once from Date.now()> }). Failure → in-body error state ('Can't fetch the current file.' + explanatory body + Close button) plus a parallel sonner toast.error (D-04, UI-SPEC L110)"
    - "GlobalFileTab is imported verbatim from './GlobalFileTab' — NO fork, NO prop additions; the mtime sentinel is captured once at fetch-success via a ref (initialMtimeRef.current = Date.now()) and passed stably across renders per Pitfall 6 (otherwise inline Date.now() reseeds the draft on every render tick)"
    - "Save handler (passed as GlobalFileTab's onSave prop) receives (content, expectedMtime) and calls onStageEditedFile(filename, content) — a prop supplied by the mount site (Plan 40-04 will wire this to uploads.stageAttachments('primary', [new File([content], filename, { type: guessMimeFromFilename(filename) })])). On stage success the modal calls onOpenChange(false) to close AND toast.success(`Attached ${filename} to your reply`). expectedMtime is discarded here (D-06 editor is stateless; there is no concurrent-edit conflict to resolve because the file is a snapshot at open-time, not a persistent host file)"
    - "Modal close paths — X button, Esc key, onInteractOutside e.preventDefault (composer clicks do NOT dismiss) — respect a draft-guard: if the textarea draft differs from initialFetchedContent, fire window.confirm('Discard unsaved changes?'); confirming closes, canceling keeps modal open (per UI-SPEC L167-169, mirrors IDMEDIT-01 idiom)"
    - "Modal portals to document.body (omits `container` prop on DialogPrimitive.Portal per Research Pitfall 7) so the inset-4 backdrop covers the entire viewport including the composer — this is deliberate per UI-SPEC L216 (unlike IdentityModal which portals to chatRegionEl to leave the composer visible)"
  artifacts:
    - path: "src/ui/features/pretty-view/EditableFileAffordance.tsx"
      provides: "Pencil-icon-only-on-mobile / pencil-plus-Edit-label-on-desktop button; opacity + hover-glow per UI-SPEC L146-147; onClick prop opens the modal at the mount site"
      exports: ["EditableFileAffordance"]
    - path: "src/ui/features/pretty-view/EditableFileModal.tsx"
      provides: "New modal composing GlobalFilesModal chrome (forked, minus host picker + tabs bar) around GlobalFileTab body; fetch-at-open + re-fetch-fail error branch + save-to-parent handler"
      exports: ["EditableFileModal", "EditableFileModalProps"]
    - path: "src/ui/features/pretty-view/EditableFileAffordance.test.tsx"
      provides: "Renders as button element (not wrapping a); aria-label uses 'Edit {filename}'; onClick fires the passed handler; icon-only rendered on mobile (useIsTouchDevice=true stub); icon+label on desktop"
      exports: []
    - path: "src/ui/features/pretty-view/EditableFileModal.test.tsx"
      provides: "Fetch fires exactly once on open; success renders GlobalFileTab in ready state; failure renders error body + fires toast.error; save handler receives (filename, content) via onStageEditedFile callback and closes modal on success; draft-guard confirm fires on close-with-changes; portals to document.body (no container prop)"
      exports: []
  key_links:
    - from: "src/ui/features/pretty-view/EditableFileModal.tsx"
      to: "src/ui/features/pretty-view/GlobalFileTab.tsx"
      via: "import GlobalFileTab from './GlobalFileTab' — verbatim reuse of the editor body (textarea + save button + skeleton loading + inline error) per D-05"
      pattern: "import GlobalFileTab"
    - from: "src/ui/features/pretty-view/EditableFileModal.tsx"
      to: "src/ui/api/editable-file-api.ts"
      via: "import { fetchTailnetUrl } from '@/api/editable-file-api' — the fresh-fetch call per D-04"
      pattern: "fetchTailnetUrl"
    - from: "src/ui/features/pretty-view/EditableFileModal.tsx"
      to: "sonner"
      via: "import { toast } from 'sonner'; toast.error(...) on re-fetch fail; toast.success(...) on save"
      pattern: "from ['\"]sonner['\"]"
    - from: "src/ui/features/pretty-view/EditableFileAffordance.tsx"
      to: "src/ui/hooks/use-is-touch-device.ts (or wherever useIsTouchDevice lives)"
      via: "import { useIsTouchDevice } from '@/hooks/use-is-touch-device' — same source ComposeBox paperclip + AttachmentChipStrip use"
      pattern: "useIsTouchDevice"
---

<objective>
Build the two net-new UI components the phase needs: the per-link edit affordance (Pencil glyph rendered inline as a sibling to the message-bubble anchor) and the edit modal itself (forked-from-GlobalFilesModal chrome + verbatim GlobalFileTab editor body + fetch-at-open lifecycle + re-fetch-fail branch + save-to-parent handler). Neither component is mounted or wired to real data in this plan — that's Plan 40-04's remit. This plan produces components that are self-contained and can be exercised end-to-end via their own unit tests using mocked props.

Purpose: implements LOCKED decisions D-03 (edit affordance is a SIBLING to the link, additive, never a wrapper), D-04 (fresh fetch at open time; on failure error is visible not silent — modal opens with the error body + parallel toast), D-05 (editor surface REUSES GlobalFileTab body verbatim; host picker + tabs bar dropped from the shell), D-06 (editor is stateless — the mtime sentinel is a stable ref captured once at open, no draft persistence, save produces a fresh attachment on every invocation).

Output: two new .tsx files + two new .test.tsx files. Zero new npm dependencies. No modifications to any existing pretty-view file — that boundary is enforced so this plan can be reviewed and shipped independently of Plan 40-04's wiring changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-text-editor-in-skynet/40-CONTEXT.md
@.planning/phases/40-text-editor-in-skynet/40-UI-SPEC.md
@.planning/phases/40-text-editor-in-skynet/40-RESEARCH.md
@.planning/phases/40-text-editor-in-skynet/40-02-SUMMARY.md
@src/ui/features/pretty-view/GlobalFilesModal.tsx
@src/ui/features/pretty-view/GlobalFileTab.tsx
@src/ui/features/pretty-view/AttachmentChipStrip.tsx
@src/ui/features/pretty-conversations/PinAction.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EditableFileAffordance component + tests</name>
  <files>src/ui/features/pretty-view/EditableFileAffordance.tsx, src/ui/features/pretty-view/EditableFileAffordance.test.tsx</files>
  <read_first>
    - /home/ubuntu/skynet-tiffany/.planning/phases/40-text-editor-in-skynet/40-UI-SPEC.md § Component Inventory (L124 — full spec of the affordance) + § Interaction Contract (L142-149 — per-viewport visibility + positioning)
    - /home/ubuntu/skynet-tiffany/.planning/phases/40-text-editor-in-skynet/40-RESEARCH.md § Ex 1 (EditableFileAffordance render code) + § Pitfall 2 (sibling not wrapper)
    - /home/ubuntu/skynet-tiffany/src/ui/features/pretty-conversations/PinAction.tsx (bare-icon-with-hue-drop-shadow precedent — the interaction idiom UI-SPEC L124 mirrors)
    - /home/ubuntu/skynet-tiffany/src/ui/features/pretty-view/ChatMessage.tsx L419-508 (speak button opacity/hover pattern — the .pv-speak-btn opacity=0.62 rest + hover:!opacity-100 + [@media(hover:none)]:!opacity-[0.72] precedent that mobile visibility should mirror; also assumption A5 confirmation — check whether the bubble container div at L305-360 has a `group` class that a `group-hover:` Tailwind selector could depend on)
    - /home/ubuntu/skynet-tiffany/src/ui/hooks/ (find the exact file exporting useIsTouchDevice — probably use-is-touch-device.ts; confirm the exact import path via `grep -rn "export.*useIsTouchDevice" src/ui/hooks/`)
  </read_first>
  <behavior>
    - Component signature: EditableFileAffordance({ onOpen, filename }: { onOpen: () => void; filename: string }): JSX.Element
    - Renders a single <button type="button"> element (not wrapping any anchor)
    - aria-label === `Edit ${filename}` AND title === `Edit ${filename}` (both on the button — screen readers + hover tooltip)
    - onClick fires the onOpen prop; passes no arguments
    - Icon is lucide-react's Pencil at size={16}
    - When useIsTouchDevice() === true: renders icon only (no visible label text); min-w-[44px] min-h-[44px] justify-center; opacity applied via inline style or Tailwind class at ~0.72 (target: matches speak-button `[@media(hover:none)]:!opacity-[0.72]` idiom — use the same media query approach if possible, else a JS branch); no hover-reveal (visible at rest)
    - When useIsTouchDevice() === false: renders icon + <span> "Edit" label; button is opacity-0 at rest; visible only on message-bubble hover — accomplished by either (a) a Tailwind `.pv-bubble:hover_&:opacity-100` arbitrary variant if the parent bubble div has the `.pv-bubble` class, OR (b) a `group-hover:opacity-100` if the parent bubble has the `group` class (Plan 40-04 will add whichever selector is needed — see Assumption A5); default: use `[.pv-bubble:hover_&]:opacity-100` per Research Ex 1 and add a comment that Plan 40-04 must ensure the parent bubble has the pv-bubble class
    - On hover: text color transitions from var(--color-pv-code-fg) (coral #ffb896) to hsla(var(--pv-id-hue), 80%, 65%, 1); a CSS filter drop-shadow(0 0 6px hsla(var(--pv-id-hue), 80%, 60%, 0.55)) applies to the button (mirrors PinAction.tsx idiom + UI-SPEC L124). Apply via inline onMouseEnter/onMouseLeave setting e.currentTarget.style.filter (per Research Ex 1) OR via a scoped CSS-in-JS/style block — inline handlers are the shorter path.
    - Base classes include "inline-flex items-center gap-1 align-baseline ml-1 cursor-pointer text-[color:var(--color-pv-code-fg)] transition-opacity duration-[120ms]" (align-baseline + ml-1 = the "sibling to anchor, 4px left margin" positioning per UI-SPEC L149)
    - Test 1 (renders as button): render <EditableFileAffordance onOpen={vi.fn()} filename="test.md" />; getByRole("button") returns the element
    - Test 2 (aria + title): button.getAttribute("aria-label") === "Edit test.md" AND button.getAttribute("title") === "Edit test.md"
    - Test 3 (onClick fires callback): const onOpen = vi.fn(); render + fireEvent.click(button); expect(onOpen).toHaveBeenCalledTimes(1)
    - Test 4 (icon-only on mobile — useIsTouchDevice=true): vi.mock the hook to return true; render; button text content === "" (no "Edit" label visible); button classList contains min-w-[44px] or min-h-[44px] (touch target)
    - Test 5 (icon+label on desktop — useIsTouchDevice=false): vi.mock the hook to return false; render; button textContent contains "Edit"; opacity class starts at "opacity-0" (hover-reveal — the visibility flip happens via ancestor hover which JSDOM can't easily test; the initial class-list check is sufficient)
    - Test 6 (sibling-not-wrapper anti-pattern lockdown): render <EditableFileAffordance ... />; the rendered root is a <button>, NOT a <span>/<div>/<a>-wrapping element. This encodes UI-SPEC L136 into a test so regressions are caught.
    - Test 7 (hover drop-shadow — desktop): render with mobile=false; fireEvent.mouseEnter(button); expect button.style.filter to contain "drop-shadow" and "hsla(var(--pv-id-hue)"; fireEvent.mouseLeave(button); expect button.style.filter === "" (empty)
  </behavior>
  <action>
    Create src/ui/features/pretty-view/EditableFileAffordance.tsx per Research §Ex 1 as source-of-truth with the following adjustments:
      - Exported as named export `EditableFileAffordance` (not default) per fleet naming convention for pretty-view components (see AttachmentChipStrip.tsx L52 pattern)
      - Header docblock cites D-03 (additive-not-replacive → sibling to the anchor; UI-SPEC L136), UI-SPEC L124 (visual treatment), and Research Assumption A5 (bubble container class dependency — Plan 40-04 confirms/adds .pv-bubble class)
      - useIsTouchDevice imported from the exact hook path (confirmed via read_first grep)
      - Return JSX per behavior spec: single <button> element, inline-flex layout, aria-label + title, onClick, onMouseEnter/onMouseLeave for the drop-shadow filter, conditional className based on isTouchDevice, conditional <span>Edit</span> label
      - Comment near the touch-mobile branch: "Mobile: always visible at ~72% opacity, 44x44 min touch target per Apple HIG (UI-SPEC L215). Uses the same [@media(hover:none)]:!opacity idiom as the speak button at ChatMessage.tsx:496."
      - Comment near the desktop branch: "Desktop: hover-reveal on parent bubble. Requires the parent .pv-bubble class (see Plan 40-04 Task 1). Falls back to always-visible-but-transparent if the parent lacks that class — degrades gracefully but with the affordance invisible."

    Create src/ui/features/pretty-view/EditableFileAffordance.test.tsx with vitest + @testing-library/react + fireEvent. Mock useIsTouchDevice per test via vi.mock at file top:
      - `vi.mock("@/hooks/use-is-touch-device", () => ({ useIsTouchDevice: vi.fn() }))`
      - Per test override: `(useIsTouchDevice as ReturnType<typeof vi.fn>).mockReturnValue(true|false)`
    Tests 1-7 above.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/EditableFileAffordance.test.tsx</automated>
  </verify>
  <done>All 7 EditableFileAffordance tests pass; component renders as <button> (Test 6 sibling-not-wrapper); Test 4 asserts min-w-[44px]/min-h-[44px]; Test 7 asserts drop-shadow filter application + cleanup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EditableFileModal component + tests</name>
  <files>src/ui/features/pretty-view/EditableFileModal.tsx, src/ui/features/pretty-view/EditableFileModal.test.tsx</files>
  <read_first>
    - /home/ubuntu/skynet-tiffany/.planning/phases/40-text-editor-in-skynet/40-UI-SPEC.md § Component Inventory (L126 — EditableFileModal spec) + § Copywriting Contract (L98-114 — every user-facing string; L110 error-state copy is load-bearing) + § Interaction Contract (L152-175 — modal open/save/close/refetch-fail flows)
    - /home/ubuntu/skynet-tiffany/.planning/phases/40-text-editor-in-skynet/40-RESEARCH.md § Pattern 3 flow diagram (L84-152) + § Pitfall 6 (mtime sentinel stability — CRITICAL) + § Pitfall 7 (portal target) + § Ex 3 (sonner error toast pattern)
    - /home/ubuntu/skynet-tiffany/src/ui/features/pretty-view/GlobalFilesModal.tsx (full file — the chrome to fork; L186-217 Portal+Overlay+Content assembly, L221-271 DialogHeader assembly, L246-270 glass X close button — copy these verbatim, minus host picker + tabs bar)
    - /home/ubuntu/skynet-tiffany/src/ui/features/pretty-view/GlobalFileTab.tsx (full file — reused verbatim as body; note the TabState<T> import from IdentityFileTab and the GlobalFileTabData type at L19; the onSave signature at L26 is (content, expectedMtime) => Promise<void>)
    - /home/ubuntu/skynet-tiffany/src/ui/features/pretty-view/GlobalFilesModal.test.tsx L1-45 (test scaffolding pattern — vi.mock, render, waitFor)
    - /home/ubuntu/skynet-tiffany/src/ui/components/sonner.tsx (verify Toaster export path; the tests will mock `import { toast } from 'sonner'` via vi.mock)
    - /home/ubuntu/skynet-tiffany/src/ui/features/pretty-view/GlobalFilesModal.tsx L198-202 (the onInteractOutside e.preventDefault() guard to copy verbatim)
  </read_first>
  <behavior>
    - Component signature (EditableFileModalProps): { open: boolean; onOpenChange: (open: boolean) => void; messageEventId: string; url: string; filename: string; agentIdentityName: string | null; onStageEditedFile: (filename: string, content: string) => void }
    - Component uses local state:
      - fetchState: TabState<GlobalFileTabData> — same type imported from ./IdentityFileTab as GlobalFileTab uses (initial: { status: "loading" })
      - fetchError: string | null (for re-fetch-fail branch — the copywritten error message)
      - initialMtimeRef: useRef<number>(0) — captured ONCE at fetch success via initialMtimeRef.current = Date.now(); NEVER reassigned across the modal's open lifecycle (Pitfall 6 defense)
    - useEffect keyed on [open, url] (NOT on filename — filename is derived from url and shouldn't drive an effect):
      - When open becomes true: reset fetchState to { status: "loading" } AND fetchError to null; fire fetchTailnetUrl(url); on success: initialMtimeRef.current = Date.now() (ONCE); setFetchState({ status: "ready", data: { content: atob(result.contentBase64), mtime: initialMtimeRef.current } }); on failure: setFetchError(err instanceof Error ? err.message : "unknown"); setFetchState({ status: "error", error: fetchError }); toast.error(`Couldn't fetch ${filename} — see modal.`); use cancelledRef pattern to prevent state updates after close during fetch
      - When open becomes false: reset all state (fetchState → loading, fetchError → null, initialMtimeRef.current → 0)
    - Modal chrome: DialogPrimitive.Root open={open} onOpenChange={onOpenChange} modal={false}; DialogPrimitive.Portal WITHOUT container prop (portals to document.body per Pitfall 7 defense — Editor covers composer per UI-SPEC L216); Overlay + Content copied VERBATIM from GlobalFilesModal.tsx L189-217 (inset-4, z-index [110]/[120], blue-glass gradient, backdropFilter, box-shadow, color #e8e4d8); onInteractOutside={(e) => e.preventDefault()} guard copied VERBATIM from L198-202
    - DialogTitle sr-only: `Edit {filename}`
    - DialogHeader (visible): border-bottom rgba(220,225,245,0.10); px-6 py-4; flex-row items-center gap-3; content: (a) title <div className="text-[15px] font-semibold text-[#f0ebe0] truncate" title={filename}>Edit {filename}</div>; (b) sub-header if agentIdentityName is truthy: <div className="text-xs text-[#a89a80]">from {agentIdentityName}</div>; (c) flex-1 spacer; (d) glass X close button VERBATIM from GlobalFilesModal.tsx L246-270 (same size-9, same background, same hover box-shadow)
    - Body branches:
      - fetchState.status === "loading": three Skeletons per GlobalFileTab.tsx L58-66 pattern OR delegate to GlobalFileTab in loading state (simpler — pass state directly to GlobalFileTab)
      - fetchState.status === "error": custom in-body error rendered explicitly (do NOT use GlobalFileTab's error branch — the UI-SPEC L110 error copy is different from GlobalFileTab.tsx L69-75 generic error): render a flex-col centered container with (a) heading `Can't fetch the current file.` (text-lg font-semibold text-[#f0ebe0]); (b) body copy verbatim from UI-SPEC L110: `The agent's temporary server may have shut down (they auto-kill after 30 minutes) or the network is unreachable. Ask the agent to re-share the file if you still want to edit it.` (text-sm text-[#a89a80] max-w-md text-center); (c) a single Close button styled per the glass X convention that fires onOpenChange(false)
      - fetchState.status === "ready": render GlobalFileTab imported verbatim, wrapped in the same overflow-y-auto container GlobalFilesModal uses at L310, with onSave={handleSave}
    - handleSave = async (content: string, _expectedMtime: number) => { onStageEditedFile(filename, content); toast.success(`Attached ${filename} to your reply`); onOpenChange(false); }; expectedMtime is discarded (D-06 — editor is stateless; there is no host-file to conflict-check against)
    - Draft-guard on close (UI-SPEC L167-169): when onOpenChange(false) fires via X, Esc, or (theoretically) outside-click that gets past the guard, if fetchState.status === "ready" AND the underlying textarea draft differs from initialFetchedContent, prompt window.confirm("Discard unsaved changes?"). Confirming closes; canceling aborts. Implementation note: GlobalFileTab owns the draft state internally — this modal cannot directly read it. Options: (a) hoist draft state from GlobalFileTab (invasive); (b) accept that the draft-guard only fires via a synthetic onOpenChange wrapper that GlobalFileTab does NOT know about, so the guard only fires from the X/Esc close paths (NOT from a save-success close). Choose (b) for minimal invasion: wrap onOpenChange in a local `handleOpenChange` that fires the confirm ONLY IF (a) attempting to close (open=false) AND (b) a data-lift-out ref indicates the child has unsaved changes. Since GlobalFileTab's internal draft is opaque, use a workaround: attach an onDraftChange callback via a new optional prop on GlobalFileTab (BUT — the plan says NO GlobalFileTab modifications). Correct simplification: skip the draft-guard for v1 and add an in-code TODO comment citing UI-SPEC L167-169. Rationale: the user-facing consequence is a slightly-less-forgiving close (Esc immediately closes even mid-edit), and Ashley's fleet-standing preference for shipping is "explicit gaps documented > invisible correctness debt." A follow-up plan can hoist the draft state if it becomes a real complaint.
    - Test 1 (fetch fires exactly once on open): vi.mock @/api/editable-file-api's fetchTailnetUrl to a resolved-once spy; render <EditableFileModal open={true} url="http://100.64.0.1:8000/notes.md" filename="notes.md" ...other-props />; waitFor(() => expect(fetchTailnetUrl).toHaveBeenCalledWith("http://100.64.0.1:8000/notes.md")); assert called exactly once
    - Test 2 (fetch does NOT fire when open=false): render with open={false}; wait a tick; assert fetchTailnetUrl NOT called
    - Test 3 (success renders GlobalFileTab in ready state): stub fetch to resolve { contentBase64: btoa("hello"), extension: "md", filename: "notes.md", isTextByExt: true, sizeBytes: 5, contentType: "text/plain" }; render; waitFor GlobalFileTab's textarea to appear; assert textarea value === "hello"
    - Test 4 (failure renders error body + fires toast): stub fetch to reject; vi.mock sonner's toast; render; waitFor error heading "Can't fetch the current file." to appear; assert toast.error was called with a string containing "Couldn't fetch notes.md"
    - Test 5 (error body has a Close button that fires onOpenChange(false)): from Test 4 setup, find the Close button, fireEvent.click; expect passed onOpenChange to have been called with false
    - Test 6 (save handler wiring — calls onStageEditedFile + closes + toasts): after success (Test 3 setup), type new content into the textarea via fireEvent.change; click the Save button; expect onStageEditedFile.toHaveBeenCalledWith("notes.md", <new content>); expect onOpenChange to have been called with false; expect toast.success to have been called with a string containing "Attached notes.md to your reply"
    - Test 7 (mtime sentinel stability — Pitfall 6 defense): render with open=true, resolve fetch; wait; capture the initial mtime the child GlobalFileTab receives (via querying the mocked GlobalFileTab if the test mocks it, OR by asserting draft doesn't revert after typing — indirect but valid); type new content, wait 100ms without any re-fetch; assert textarea still shows the typed content (would revert to fetched content if mtime was Date.now() inline and re-computed on any render)
    - Test 8 (portal target — Pitfall 7 defense): render open=true; find the DialogPrimitive.Portal container — assert it's document.body (via querying document.body.contains(dialog-content-element) === true); NOT contained by any inset chatRegionEl
    - Test 9 (onInteractOutside guarded — composer clicks do NOT close): render open=true, resolved fetch; simulate a mousedown event on document.body outside the modal Content; assert onOpenChange NOT called (the guard's e.preventDefault fires); assert Esc keydown DOES trigger onOpenChange(false)
    - Test 10 (state reset on close): render open=true, resolve fetch; call onOpenChange(false) via clicking X; re-render with open=true; assert fetchTailnetUrl is called AGAIN (second call — state was reset, fetch re-fires)
  </behavior>
  <action>
    Create src/ui/features/pretty-view/EditableFileModal.tsx per behavior spec:

    Imports: useEffect, useRef, useState, useCallback from "react"; { X } from "lucide-react"; { Dialog as DialogPrimitive } from "radix-ui"; { DialogHeader, DialogTitle, DialogClose } from "@/components/dialog"; { cn } from "@/lib/utils"; { toast } from "sonner"; { fetchTailnetUrl } from "@/api/editable-file-api"; GlobalFileTab from "./GlobalFileTab"; type { TabState } from "./IdentityFileTab"; type { GlobalFileTabData } from "./GlobalFileTab".

    Export EditableFileModalProps interface exactly per Behavior signature.
    Export EditableFileModal as default (mirrors GlobalFilesModal.tsx default export convention).

    Effect logic + state per behavior. Use a cancelledRef in the useEffect closure (cancelledRef.current = false at effect start; set true in cleanup; fetch's .then/.catch check the ref before setState).

    handleOpenChange callback: wraps onOpenChange; TODO comment about draft-guard deferred per behavior clause on the (b) choice — cite UI-SPEC L167-169 as the deferred spec.

    Modal JSX per behavior: DialogPrimitive.Root + Portal (no container) + Overlay (verbatim from GlobalFilesModal L189-196) + Content (verbatim structure from L197-217 minus content children) + DialogTitle sr-only + DialogHeader (custom for Phase 40 — Edit {filename} title + sub-header + X close) + Body branches (loading/error/ready).

    Header docblock cites D-03, D-04 (fresh fetch + explicit fail), D-05 (chrome fork from GlobalFilesModal minus host picker + tabs bar; body reuses GlobalFileTab verbatim), D-06 (stateless — mtime sentinel captured once; save deposits attachment).

    Create src/ui/features/pretty-view/EditableFileModal.test.tsx:
      - vi.mock("@/api/editable-file-api", () => ({ fetchTailnetUrl: vi.fn() }))
      - vi.mock("sonner", () => ({ toast: { error: vi.fn(), success: vi.fn() } }))
      - vi.mock("./GlobalFileTab", ...) — MOCK the child component to a simple placeholder that exposes the state + onSave props for observation (mirrors how deeply-nested component reuse is tested in the fleet; alternative: use the real GlobalFileTab and interact through its textarea directly — either works; pick the direct-interaction path for tests 3, 6, 7 since GlobalFileTab is already tested elsewhere and mocking it drops coverage of the wiring)
      - Prefer the direct-interaction path: import the REAL GlobalFileTab and drive it via its textarea. Test 7 becomes: after fetch resolves and textarea is populated, type new content, wait for a re-render tick, assert textarea still has the new content (proves the mtime effect at GlobalFileTab.tsx:36-42 did not reseed on a re-render)
      - Tests 1-10 above using @testing-library/react + fireEvent + waitFor
      - Test 8 asserts document.body.contains(screen.getByRole("dialog")) === true — proving portal target (radix-ui adds role="dialog" to the Content element)
      - Test 9's outside-mousedown simulation uses fireEvent.mouseDown(document.body); Test 9's Esc uses fireEvent.keyDown(document.body, { key: "Escape" })
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/EditableFileModal.test.tsx</automated>
  </verify>
  <done>All 10 EditableFileModal tests pass; Test 7 proves mtime sentinel stability; Test 8 proves document.body portal target; Test 9 proves onInteractOutside guard + Esc-close behavior; the error-body copy at UI-SPEC L110 is present verbatim.</done>
</task>

<task type="auto">
  <name>Task 3: Frontend build + full test suite verify</name>
  <files>(no source diffs — verification only)</files>
  <read_first>
    - /home/ubuntu/skynet-tiffany/package.json (scripts)
  </read_first>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (all new component types resolve — GlobalFileTab import + TabState import + GlobalFileTabData type + Dialog primitives + sonner toast + fetchTailnetUrl signature)
    - `npm run build` exits 0 (vite bundle emission succeeds)
    - `npx vitest run src/ui/features/pretty-view/EditableFileAffordance.test.tsx src/ui/features/pretty-view/EditableFileModal.test.tsx` exits 0 with 17 tests passing (7 affordance + 10 modal)
    - `npx vitest run` exits 0 (full frontend regression — total count strictly greater than pre-plan baseline by 17)
    - `grep -c "GlobalFileTab" src/ui/features/pretty-view/EditableFileModal.tsx` returns >= 2 (import + JSX usage in ready branch)
    - `grep -c "container=" src/ui/features/pretty-view/EditableFileModal.tsx` returns 0 (the Portal MUST NOT have a container prop per Pitfall 7)
    - `grep -c "initialMtimeRef" src/ui/features/pretty-view/EditableFileModal.tsx` returns >= 2 (ref creation + assignment site)
    - `grep -c "toast.error" src/ui/features/pretty-view/EditableFileModal.tsx` returns >= 1
    - `grep -c "toast.success" src/ui/features/pretty-view/EditableFileModal.tsx` returns >= 1
    - `grep -c "The agent's temporary server may have shut down" src/ui/features/pretty-view/EditableFileModal.tsx` returns 1 (UI-SPEC L110 verbatim copy)
    - `grep -c "onInteractOutside" src/ui/features/pretty-view/EditableFileModal.tsx` returns >= 1 (the guard)
    - Grep for the anti-pattern that would make the affordance a wrapper: `grep -E "<(a|span|div)[^>]*>[^<]*<EditableFileAffordance" src/ui/features/pretty-view/EditableFileAffordance.tsx` returns 0 lines (the component MUST NOT nest inside any wrapping element; it renders as a bare button — this grep gate is defensive for the anti-pattern from Pitfall 2)
    - No new npm packages: `git diff package.json` shows no dependency changes
  </acceptance_criteria>
  <action>
    Run the four verification commands. If any gate fails, fix rather than paper over. No commits happen in this task — commits are per-task at the natural boundaries in Tasks 1 and 2.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build && npx vitest run src/ui/features/pretty-view/EditableFileAffordance.test.tsx src/ui/features/pretty-view/EditableFileModal.test.tsx</automated>
  </verify>
  <done>tsc + build + vitest exit 0; all 11 grep gates satisfied; no npm dependency changes.</done>
</task>

</tasks>

<verification>
- Frontend build: `npm run build` exits 0
- Full frontend tests: `npx vitest run` green with pre-plan baseline + 17 net-new tests
- Modal correctly portals to document.body (not chatRegionEl) — Test 8 + grep gate
- Mtime sentinel stability — Test 7 + grep gate
- Error-copy verbatim from UI-SPEC L110 — grep gate
- Sibling-not-wrapper affordance — Test 6 + grep gate
</verification>

<success_criteria>
- LOCKED D-03 (additive-not-replacive) structurally enforced: affordance is a sibling <button>, tests 1-6 lock this shape.
- LOCKED D-04 (fresh fetch + visible fail) enforced: modal fetches at open, on failure renders explicit body + parallel toast (no silent cached-bytes reuse — the eligibility hook's cached bytes are already discarded in Plan 40-02).
- LOCKED D-05 (reuse GlobalFileTab body verbatim, strip host picker + tabs bar) enforced: GlobalFileTab imported verbatim, no host picker in EditableFileModal, no tabs bar.
- LOCKED D-06 (editor stateless, save = fresh attachment) enforced: state resets on every close; mtime sentinel captured once + discarded on save; onStageEditedFile called with fresh content on every save.
- Both components are independently testable and self-contained — Plan 40-04 only wires them into existing files.
</success_criteria>

<output>
Create `.planning/phases/40-text-editor-in-skynet/40-03-SUMMARY.md`. Include: files created; test counts (baseline + 17 new); explicit confirmation of the four grep gates enforcing invariants (container=0, initialMtimeRef>=2, UI-SPEC L110 error copy present verbatim, sibling-not-wrapper regex clean); handoff to Plan 40-04 pointing at the two exports it will consume: `EditableFileAffordance` (renders in ChatMessage's `<a>` override) and `EditableFileModal` (mounts in PrettyView alongside IdentityModal).
</output>
