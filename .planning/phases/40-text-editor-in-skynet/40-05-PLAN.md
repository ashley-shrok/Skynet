---
phase: 40-text-editor-in-skynet
plan: 05
type: execute
wave: 4
depends_on: [40-01, 40-02, 40-03, 40-04]
files_modified:
  - .planning/phases/40-text-editor-in-skynet/40-BUILD-VERIFY-LOG.md
  - .planning/phases/40-text-editor-in-skynet/40-UAT-CHECKLIST.md
  - .planning/phases/40-text-editor-in-skynet/40-PATCHES-MD-ENTRY.md
autonomous: false
tags: [text-editor, deploy-prep, uat, patch-entry, phase-40]
requirements: []
must_haves:
  truths:
    - "40-BUILD-VERIFY-LOG.md records the last clean state of the phase-40 code before hand-off to the maintainer for deploy: exit codes and durations for `npx tsc --noEmit`, `NODE_OPTIONS=--max-old-space-size=4096 npm run build:backend`, `npm run build`, and `npx vitest run` (total test count); grep-gate results proving all key structural invariants from Plans 40-01..40-04 hold in HEAD"
    - "40-UAT-CHECKLIST.md is a paste-ready 7-item post-deploy verification walk covering each LOCKED decision (D-01..D-07) as an observable behavior on production Skynet — Ashley walks this after deploy and reports pass/fail per item"
    - "40-PATCHES-MD-ENTRY.md is a paste-ready patch entry for ~/.claude/roles/box-maintainer/skynet-patches.md following the shape of recent entries (Phase 19's #237 entry at .planning/phases/19-.../19-PATCHES-MD-ENTRY.md is the freshest example); includes motivation, files touched, tests added, threat model summary, rebase risk assessment, and the deploy-note pointing to the maintainer as owner"
    - "Human-verify checkpoint is BLOCKING: end-of-phase config is `human_verify_mode: end-of-phase` per .planning/config.json:22, so this plan gates on Ashley's explicit UAT-approval or revision-notes; no code changes are made in this plan; no deploy motions are made"
  artifacts:
    - path: ".planning/phases/40-text-editor-in-skynet/40-BUILD-VERIFY-LOG.md"
      provides: "Objective record of the phase's build/test posture at hand-off — enables the maintainer to shortcut re-verification before running docker build"
      contains: "tsc/build:backend/build/vitest exit codes + test counts; grep-gate results table"
    - path: ".planning/phases/40-text-editor-in-skynet/40-UAT-CHECKLIST.md"
      provides: "Post-deploy verification steps mapped 1:1 to each LOCKED decision D-01..D-07 as observable behavior; Ashley walks this on production Skynet"
      contains: "7 numbered items with per-item URL / action / expected outcome / fail-mode notes"
    - path: ".planning/phases/40-text-editor-in-skynet/40-PATCHES-MD-ENTRY.md"
      provides: "Paste-ready entry for the fleet's ~/.claude/roles/box-maintainer/skynet-patches.md file; the maintainer inserts it at ship time with correct ordinal count"
      contains: "Section headings following recent-patch shape; files touched inventory; test-count summary; threat model summary; rebase risk; deploy-note"
  key_links:
    - from: ".planning/phases/40-text-editor-in-skynet/40-UAT-CHECKLIST.md"
      to: ".planning/phases/40-text-editor-in-skynet/40-CONTEXT.md"
      via: "each UAT item cites the D-XX decision it verifies"
      pattern: "D-0[1-7]"
    - from: ".planning/phases/40-text-editor-in-skynet/40-PATCHES-MD-ENTRY.md"
      to: "~/.claude/roles/box-maintainer/skynet-patches.md (fleet-external)"
      via: "paste target — the maintainer copies this content in at ship time"
      pattern: "skynet-patches.md"
---

<objective>
Produce the three deploy-prep artifacts the maintainer needs to ship Phase 40 safely: an objective build-verify log recording the last-known-clean state of the code, a UAT checklist Ashley walks post-deploy to confirm each LOCKED decision translates into working behavior on production, and a paste-ready patch-entry draft for ~/.claude/roles/box-maintainer/skynet-patches.md. Then hand off to the human via a blocking checkpoint per the fleet's `human_verify_mode: end-of-phase` config.

Purpose: closes the phase by documenting what shipped without shipping it. Per the fleet directive in the planner's downstream_consumer block, sub-agent executors do NOT do deploys — the maintainer (Tiffany) handles `git pull --rebase`, coord-room announces, `docker build`, deadman-guarded `docker compose up --force-recreate`, HTTPS 200 verify, `git push`, and the patch entry paste. This plan's remit stops at producing the three docs artifacts + the human-verify checkpoint.

Output: three new docs files in the phase directory. Zero source changes. Zero deploy motions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-text-editor-in-skynet/40-CONTEXT.md
@.planning/phases/40-text-editor-in-skynet/40-RESEARCH.md
@.planning/phases/40-text-editor-in-skynet/40-UI-SPEC.md
@.planning/phases/40-text-editor-in-skynet/40-01-SUMMARY.md
@.planning/phases/40-text-editor-in-skynet/40-02-SUMMARY.md
@.planning/phases/40-text-editor-in-skynet/40-03-SUMMARY.md
@.planning/phases/40-text-editor-in-skynet/40-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 40-BUILD-VERIFY-LOG.md — objective build/test posture snapshot</name>
  <files>.planning/phases/40-text-editor-in-skynet/40-BUILD-VERIFY-LOG.md</files>
  <read_first>
    - /home/ubuntu/skynet-tiffany/.planning/phases/19-streaming-tts-output-via-chatterbox-tts-endpoint/19-BUILD-VERIFY-LOG.md if it exists (freshest precedent for the log's shape; STATE.md line 112 references it — read for section conventions + phrasing to match); if the file does not exist at that exact path, look for any other `*-BUILD-VERIFY-LOG.md` in .planning/phases/**/ via `ls .planning/phases/*/*-BUILD-VERIFY-LOG.md`
    - /home/ubuntu/skynet-tiffany/package.json (script names)
    - /home/ubuntu/skynet-tiffany/CLAUDE.md if present (build discipline notes)
  </read_first>
  <acceptance_criteria>
    - File exists at the exact path above
    - Section: "## Build verification" with a table containing 4 rows: `npx tsc --noEmit`, `NODE_OPTIONS=--max-old-space-size=4096 npm run build:backend`, `npm run build`, `npx vitest run` — columns: Command, Exit Code, Duration (approximate), Notes (e.g., bundle size for `npm run build`, test totals for vitest)
    - Section: "## Structural invariant grep gates" with a table containing at minimum the following gates (each with an expected outcome and the actual outcome from HEAD):
      - `grep -c "app.use(\"/pretty-view\"" src/backend/database/database.ts | grep -v '^//'` — expected 1
      - `grep -c "MIRROR" src/backend/utils/editable-file-whitelist.ts` — expected >=1
      - `grep -c "MIRROR" src/ui/features/pretty-view/editable-file-whitelist.ts` — expected >=1
      - `diff <(grep -oE '"[^"]+"' src/ui/features/pretty-view/editable-file-whitelist.ts | sort -u) <(grep -oE '"[^"]+"' src/backend/utils/editable-file-whitelist.ts | sort -u)` — expected no differences on quoted strings that are Set members
      - `grep -c "contentBase64" src/ui/features/pretty-view/use-editable-file-eligibility.ts` — expected 0 (D-04 discard-bytes)
      - `grep -c "container=" src/ui/features/pretty-view/EditableFileModal.tsx` — expected 0 (Pitfall 7 portal target)
      - `grep -c "initialMtimeRef" src/ui/features/pretty-view/EditableFileModal.tsx` — expected >=2 (Pitfall 6 mtime stability)
      - `grep -c "pv-bubble" src/ui/features/pretty-view/ChatMessage.tsx` — expected >=2
      - `grep -c "onOpenEditor" src/ui/features/pretty-view/ChatMessage.tsx` — expected >=3
      - `grep -c "EditableFileModal" src/ui/features/pretty-view/PrettyView.tsx` — expected >=2
    - Section: "## New files inventory" — list every file created by Plans 40-01..40-04 with a one-line purpose annotation
    - Section: "## Files modified" — src/backend/database/database.ts, src/ui/features/pretty-view/ChatMessage.tsx, src/ui/features/pretty-view/PrettyView.tsx — each with a one-line diff summary
    - Section: "## npm dependency changes" — the string "None" (D-02 / D-05 / D-06 all satisfied via existing deps — every package is already resident per Research §Package Legitimacy Audit)
    - Section: "## Deploy status" — the string "PENDING — maintainer (Tiffany) owns docker build + docker compose up + git push. This plan's remit stops at documentation."
  </acceptance_criteria>
  <action>
    Run the four verification commands and capture exit codes + durations + relevant notes:
      1. `time npx tsc --noEmit` — record wall time from `time` output
      2. `time NODE_OPTIONS=--max-old-space-size=4096 npm run build:backend`
      3. `time npm run build` — record bundle size from the vite output tail (typically "main-*.js gzip <size>")
      4. `time npx vitest run` — record test total from the summary line ("Tests N passed" style)

    Run every grep-gate from the acceptance criteria and record the actual output alongside the expected. Any gate that fails MUST cause this task to fail — do NOT paper over. If a gate fails, return to the failing Plan (40-01..40-04) to fix at source; do not proceed to writing the deploy artifacts on a broken base.

    Write the log per the acceptance-criteria section shape. Match the tonal + heading conventions of any existing `*-BUILD-VERIFY-LOG.md` in the repo. If no precedent exists, use plain markdown headings and tables.

    Include a "## Test count reconciliation" section that lists the pre-plan-40 baseline count (record from the 40-04-SUMMARY.md handoff, or re-verify from git-log on a pre-phase-40 commit if the summary is missing) + the expected delta (17 + 15 + 17 + 15 = 64 new tests) + the actual final count from vitest.
  </action>
  <verify>
    <automated>test -f .planning/phases/40-text-editor-in-skynet/40-BUILD-VERIFY-LOG.md && grep -c "^##" .planning/phases/40-text-editor-in-skynet/40-BUILD-VERIFY-LOG.md | awk '$1 >= 5 {exit 0} {exit 1}'</automated>
  </verify>
  <done>File exists with all 5+ required sections; all grep gates PASS with recorded actual outputs; build/test commands all exit 0 with recorded durations + test totals; test count reconciliation shows baseline + 64.</done>
</task>

<task type="auto">
  <name>Task 2: 40-UAT-CHECKLIST.md — 7-item post-deploy Ashley-walks-this-live checklist</name>
  <files>.planning/phases/40-text-editor-in-skynet/40-UAT-CHECKLIST.md</files>
  <read_first>
    - /home/ubuntu/skynet-tiffany/.planning/phases/40-text-editor-in-skynet/40-CONTEXT.md § Decisions (D-01..D-07 verbatim — these are the seven items)
    - /home/ubuntu/skynet-tiffany/.planning/phases/40-text-editor-in-skynet/40-UI-SPEC.md § Interaction Contract (L142-181 — the specific behaviors Ashley will observe)
    - /home/ubuntu/skynet-tiffany/.planning/phases/19-streaming-tts-output-via-chatterbox-tts-endpoint/19-UAT-CHECKLIST.md if it exists (the freshest precedent for the checklist shape per STATE.md line 112 — mirror its per-item structure of Given/When/Then + fail-mode notes)
  </read_first>
  <acceptance_criteria>
    - File exists at the exact path above
    - Contains exactly 7 numbered UAT items, one per LOCKED decision D-01..D-07
    - Each item has 4 sub-sections: "Setup" (URL to visit / precondition on production Skynet), "Steps" (numbered actions Ashley takes), "Expected" (observable outcomes proving the decision was implemented correctly), "Fail modes" (specific things to watch out for — matches Common Pitfalls from Research)
    - Each item explicitly cites the D-XX decision it verifies in its heading (e.g., "### 1. D-01: Passive URL detection — agent-served link appears with edit affordance")
    - Item 1 (D-01): Ashley receives an agent-served tailnet URL (say, tanya serves her a notes.md via python3 -m http.server on her fleet host) → in Skynet's pretty-view she sees the link rendered AS a normal clickable link AND a pencil affordance appears next to it (hover-reveal on desktop, always visible on mobile)
    - Item 2 (D-02): Whitelist path — a link ending in .md gets the affordance immediately (no discernible delay). Byte-sniff path — a link to a file without an extension (e.g., "myscript") gets the affordance after ~200-500ms fetch (or does NOT get it if the file is binary — test with a link to a small .png that has no extension in the URL)
    - Item 3 (D-03): Clicking the link (not the affordance) still downloads/opens the file as before — the affordance did not replace the link's behavior. Fail mode: if clicking the file preview triggers the editor modal instead of a download, the sibling-not-wrapper structure regressed.
    - Item 4 (D-04): Open the editor immediately after receiving a link → editor loads, textarea populated. Wait 30+ minutes for the agent's temp server to auto-kill; open the editor for the same link again → error body renders explicitly ("Can't fetch the current file. The agent's temporary server may have shut down…") + sonner toast fires. Fail mode: silent fallback to stale bytes = a rare but critical shape violation.
    - Item 5 (D-05): Editor modal opens with the same look/feel as the Global Files edit modal Ashley uses today — BUT no host picker in the header, no bottom tabs strip. Mobile: modal covers full viewport minus 16px inset; iOS Safari safe-area padding correct.
    - Item 6 (D-06): Type an edit in the textarea; click Save → modal closes; a chip appears in the ComposeBox strip with the edited filename. Save the SAME file a second time (re-open + type different edit) → a SECOND chip appears (deliberate multi-version support). Click × on a chip → it disappears (existing ComposeBox remove affordance works uncha nged).
    - Item 7 (D-07): With one or more chips staged, type a caption, click send → the agent receives an injected user turn with the caption + the file(s) accessible at ~/pretty-view-uploads/<date>/<time>-<name> per Phase 5 UPLOAD-10. Agent can cat the file, sees the edited content. Fail mode: file lands empty, or filename mangled, or the agent doesn't see it at all — indicates the deposit-into-uploads.stageAttachments("primary") wiring didn't reach the existing send pipeline correctly.
    - Trailing "## Blocking issues found during UAT" section with placeholder text: "(fill in during walk; escalate any items marked FAIL back to the planning/executor rotation before proceeding to public shipment)"
    - Trailing "## Passed items — approved for ship" section with placeholder for Ashley to date + sign
    - Item ordering matches the D-01..D-07 numerical order; the seven items collectively cover every LOCKED decision without gaps or overlap
  </acceptance_criteria>
  <action>
    Read the 40-CONTEXT.md decisions section and transcribe each LOCKED decision into an observable UAT item. Use the Given/When/Then style (Setup / Steps / Expected / Fail modes) modeled on the Phase 19 UAT precedent — if that file doesn't exist, invent the structure and mark clearly that this is the first-of-shape.

    For each item, be concrete about the observable evidence Ashley uses to verify (specific URL patterns, specific button click paths, specific toast strings from UI-SPEC L107/L110/L112). Ashley should NOT need to open dev tools or shell into the container — every check is UI-observable.

    Include a "## Preconditions" section at top: "Skynet deployed to https://term.gigaashley.click (or the current production URL); at least one fleet agent has served Ashley a tailnet URL via python3 -m http.server (see id-skill § 'Sending files to the user'); Ashley's browser is at HEAD of feat/tab-title-from-tmux + Phase 40 patches are live."

    Include an "## Ash-side prep" section: "Ask an agent (e.g., 'tanya, please serve me the current 40-CONTEXT.md via the id skill's serve pattern so I can UAT the text editor') — the agent's response will be the message that carries the tailnet URL Ashley walks through the 7 items with."

    Do NOT script the deploy motion (that's the maintainer's remit). The UAT is post-deploy — the file's opening line states that clearly.
  </action>
  <verify>
    <automated>test -f .planning/phases/40-text-editor-in-skynet/40-UAT-CHECKLIST.md && grep -cE "^### [1-7]\. D-0[1-7]:" .planning/phases/40-text-editor-in-skynet/40-UAT-CHECKLIST.md | awk '$1 == 7 {exit 0} {exit 1}'</automated>
  </verify>
  <done>File exists with exactly 7 D-XX-tagged items; each item has 4 sub-sections; preconditions + Ash-side prep + trailing signoff sections present.</done>
</task>

<task type="auto">
  <name>Task 3: 40-PATCHES-MD-ENTRY.md — paste-ready patch entry for skynet-patches.md</name>
  <files>.planning/phases/40-text-editor-in-skynet/40-PATCHES-MD-ENTRY.md</files>
  <read_first>
    - /home/ubuntu/skynet-tiffany/.planning/phases/19-streaming-tts-output-via-chatterbox-tts-endpoint/19-PATCHES-MD-ENTRY.md if it exists (freshest paste-ready entry precedent — 85-line shape referenced in STATE.md line 112)
    - /home/ubuntu/skynet-tiffany/.planning/phases/40-text-editor-in-skynet/40-CONTEXT.md § Decisions (motivation + shape locks)
    - /home/ubuntu/skynet-tiffany/.planning/phases/40-text-editor-in-skynet/40-RESEARCH.md § Summary + § Standard Stack (files/dependencies inventory) + § Security Domain (threat model summary)
  </read_first>
  <acceptance_criteria>
    - File exists at the exact path above
    - Section: "## Patch title" — a one-line title following recent-patch phrasing (e.g., "Phase 40 — in-app text editor for agent-served tailnet files (D-01..D-07)")
    - Section: "## Motivation" — 3-5 sentences citing the shape doc's central pain (multi-agent round-trip + mobile-broken workflow) and the load-bearing mobile case
    - Section: "## What shipped" — bulleted list of the four functional pieces: (a) backend SSRF-hardened proxy POST /pretty-view/fetch-tailnet-url, (b) per-message eligibility hook + whitelist twins + api helper, (c) EditableFileAffordance + EditableFileModal components (forked GlobalFilesModal chrome), (d) ChatMessage + PrettyView wiring
    - Section: "## Files touched" — categorized by NEW / MODIFIED with a one-line purpose per file (mirrors 19-PATCHES-MD-ENTRY.md convention)
    - Section: "## Tests added" — count broken down per plan: 17 (40-01 backend), 15 (40-02 frontend api + hook), 17 (40-03 UI components), 15 (40-04 wiring) = 64 total net-new; running total mentions the pre-phase-40 baseline + post-phase-40 total
    - Section: "## Threat model summary" — bullets covering T-40-01 (SSRF: CGNAT allowlist + regex + traversal guard), T-40-02 (dir-listing HTML spoof: content-type check), T-40-03 (DoS: 8s timeout + 2MB cap), T-40-04 (auth: authenticateJWT + tailnet-as-ACL), T-40-05 (log privacy: filename omitted); reference to the Plan 40-01 `<threat_model>` block for full detail
    - Section: "## Nginx changes" — the string "None" (this phase does NOT touch nginx — the POST /pretty-view/fetch-tailnet-url endpoint responds with plain JSON that inherits the default nginx location / block; unlike Phase 19 /voice/speak-stream which needed proxy_buffering off, this endpoint is a plain request/response cycle)
    - Section: "## Rebase risk" — LOW (fork-local: additive backend route + additive frontend files + surgical extension of ChatMessage.tsx `a` override at L398-404 + surgical mount of EditableFileModal in PrettyView.tsx alongside IdentityModal; no upstream Skynet surfaces disturbed; GlobalFilesModal.tsx + GlobalFileTab.tsx + AttachmentChipStrip.tsx + ComposeBox.tsx + use-pretty-view-uploads.ts all left byte-untouched per D-05 reuse plan)
    - Section: "## Deploy note" — the string "Bundles with the current deploy queue held under the maintainer (Tiffany). No standalone ship. Deploy motion (git pull --rebase → coord-room BEFORE announce → docker build → deadman-guarded docker compose up --force-recreate → HTTPS 200 verify → coord-room AFTER announce → git push → this patch entry pasted into ~/.claude/roles/box-maintainer/skynet-patches.md with ordinal-count normalization) is the maintainer's remit — not the executor's."
    - Section: "## Ordinal-count guidance" — a paragraph like Phase 19's on how to increment the header's numeric count at paste day; something like: "skynet-patches.md's header line reads 'TWO HUNDRED AND THIRTY-N numbered patches' (line 17, verify at paste day); increment by 1 for this entry (or by the count of held patches that ship in the same wave)"
    - Section: "## Cross-references" — list adjacent bounties/plans: Phase 05 (upload pipeline this deposit rides on), Phase 23 GEFM-05 (GlobalFilesModal + GlobalFileTab reuse target), Phase 4 Glass visual language (--color-pv-* tokens the affordance + modal use)
  </acceptance_criteria>
  <action>
    Write the paste-ready entry per acceptance-criteria section shape. Use the Phase 19 precedent for tone + heading conventions if it exists; otherwise use plain markdown.

    Every claim in the entry MUST be verifiable against Plan 40-01..40-04's SUMMARY.md files or the source at HEAD. Do not invent test counts, file names, or threat mitigations — cross-reference against the summaries.

    Do NOT paste the entry into ~/.claude/roles/box-maintainer/skynet-patches.md — that file is outside the executor's scope (fleet-external, owned by the maintainer). This plan only produces the draft.
  </action>
  <verify>
    <automated>test -f .planning/phases/40-text-editor-in-skynet/40-PATCHES-MD-ENTRY.md && grep -cE "^## (Patch title|Motivation|What shipped|Files touched|Tests added|Threat model summary|Nginx changes|Rebase risk|Deploy note|Ordinal-count guidance|Cross-references)" .planning/phases/40-text-editor-in-skynet/40-PATCHES-MD-ENTRY.md | awk '$1 >= 11 {exit 0} {exit 1}'</automated>
  </verify>
  <done>File exists with all 11 required sections; every claim is verifiable against Plan 40-01..40-04 summaries or source at HEAD; ordinal-count guidance included; deploy note explicitly hands ownership to the maintainer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: End-of-phase human-verify checkpoint — Ashley signs off on the docs artifacts</name>
  <what-built>
    - `.planning/phases/40-text-editor-in-skynet/40-BUILD-VERIFY-LOG.md` — build/test posture at HEAD
    - `.planning/phases/40-text-editor-in-skynet/40-UAT-CHECKLIST.md` — 7-item post-deploy verification walk
    - `.planning/phases/40-text-editor-in-skynet/40-PATCHES-MD-ENTRY.md` — paste-ready patch entry for the maintainer
    - Zero source diffs in this plan
    - Zero deploy motion in this plan
  </what-built>
  <how-to-verify>
    1. Open each of the three docs files. Skim for completeness and shape.
    2. Confirm the build-verify log's numeric claims (test counts, exit codes) match your understanding of the phase's actual state. If any number smells off, ask the executor to re-run and re-write.
    3. Read the UAT checklist and imagine walking through it on production Skynet. Are the 7 items observable-behavior-level, or does any item require dev-tools/shell access to verify? If the latter, request a rewrite.
    4. Read the patch entry draft. Does the motivation ring true to the shape doc? Does the "Rebase risk: LOW" claim match reality (no upstream Skynet surfaces disturbed)? Any misclassifications, request a fix.
    5. Two exits from this checkpoint:
       a. "APPROVED — hand off to maintainer for ship": the executor rotation is done; the maintainer (Tiffany) picks up the deploy motion at her convenience.
       b. "REVISION — [notes]": specific edits requested; the executor loops back to Task 1, 2, or 3 to apply.
  </how-to-verify>
  <resume-signal>Reply with "APPROVED — hand off to maintainer" OR "REVISION — [inline notes on which of the 3 files needs changes and what]"</resume-signal>
</task>

</tasks>

<verification>
- Three docs files exist in the phase directory
- All acceptance criteria on each file are satisfied
- Human-verify checkpoint is BLOCKING per `human_verify_mode: end-of-phase` in .planning/config.json
- Zero source diffs
- Zero deploy motion (no `docker build`, no `docker compose up`, no `git push` — those are maintainer motions not executor motions)
</verification>

<success_criteria>
- The maintainer (Tiffany) has a complete paper trail to review before running `docker build`: build-verify log proving the code compiles + passes tests, UAT checklist enabling her post-deploy validation walk with Ashley, patch entry draft ready to paste at ship day.
- The seven LOCKED decisions D-01..D-07 are each represented as an observable UAT item on production Skynet.
- The threat model from Plan 40-01's `<threat_model>` block is summarized in the patch entry so post-hoc review of the SSRF mitigations is a one-file read.
- Deploy ownership is unambiguously the maintainer's — nothing in the docs suggests the executor should be running docker/compose/push commands.
</success_criteria>

<output>
Create `.planning/phases/40-text-editor-in-skynet/40-05-SUMMARY.md` when done. Include: the three docs files created; the human-verify outcome (APPROVED or REVISION with notes); phase-close status ("Ready for maintainer ship" if approved, "Blocked on revision" if not).

After the summary is written and the checkpoint is APPROVED, this phase's executor-scope work is COMPLETE. The maintainer picks up from here for deploy motion. No further plans need to fire under this phase number.
</output>
