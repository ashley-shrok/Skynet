---
phase: 24-plan-mode-approval-bubble-pane-tail-detection-expanded-bubbl
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/claude-session/plan-pending-parser.ts
  - src/backend/claude-session/plan-pending-parser.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Live fleet Ink plan-approval prompt is detected by isPlanPending (currently dead)"
    - "Plan file tilde-relative path is extracted from the pane footer when present"
    - "parsePlanFilePath returns null (never throws) when the footer is absent or the slug fails regex"
    - "vitest suite for plan-pending-parser passes with pinned-variant fixtures"
  artifacts:
    - path: "src/backend/claude-session/plan-pending-parser.ts"
      provides: "isPlanPending (fixed) + parsePlanFilePath (new)"
      exports: ["isPlanPending", "parsePlanFilePath"]
    - path: "src/backend/claude-session/plan-pending-parser.test.ts"
      provides: "vitest coverage for both helpers with pinned-variant fixtures"
      contains: "parsePlanFilePath"
  key_links:
    - from: "src/backend/claude-session/plan-pending-parser.ts"
      to: "src/backend/claude-session/claude-session-server.ts (Plan 03)"
      via: "named exports isPlanPending + parsePlanFilePath"
      pattern: "export function (isPlanPending|parsePlanFilePath)"
---

<objective>
Fix `plan-pending-parser.ts` fingerprint to match the pinned fleet Ink variant (currently dead
detection since patch 260802-rps landed) and add `parsePlanFilePath` as a sibling pure helper
that extracts the plan file's tilde-relative path from the pane footer. Update the vitest suite
to drive both helpers with pinned-variant synthetic pane fixtures.

Purpose: This is the foundation for the whole phase. Every downstream plan (WS frame extension,
SFTP fetch, bubble render, compose-disable) is gated on detection actually firing against a
live fleet pane. Without this fix, everything else is unreachable in production.

Output: Corrected pure-helper file + expanded vitest suite. Zero I/O, zero mocks — synthetic
pane strings in, boolean or string-or-null out.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-plan-mode-approval-bubble-pane-tail-detection-expanded-bubbl/24-CONTEXT.md
@.planning/phases/24-plan-mode-approval-bubble-pane-tail-detection-expanded-bubbl/24-PATTERNS.md
@src/backend/claude-session/plan-pending-parser.ts
@src/backend/claude-session/plan-pending-parser.test.ts
@src/backend/claude-session/context-pct-parser.ts
@src/backend/claude-session/context-pct-parser.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix isPlanPending fingerprint for pinned fleet Ink variant + add parsePlanFilePath</name>
  <files>src/backend/claude-session/plan-pending-parser.ts</files>
  <read_first>
    - src/backend/claude-session/plan-pending-parser.ts (the file being modified — full read)
    - src/backend/claude-session/context-pct-parser.ts (sibling pure-helper posture: zero imports, single named export style, docblock-heavy)
    - .planning/phases/24-plan-mode-approval-bubble-pane-tail-detection-expanded-bubbl/24-CONTEXT.md § Detection + § Plan file path extraction + § Specifics (anchor strings verbatim)
    - .planning/phases/24-plan-mode-approval-bubble-pane-tail-detection-expanded-bubbl/24-PATTERNS.md § plan-pending-parser.ts pattern assignments
  </read_first>
  <behavior>
    - isPlanPending returns TRUE when pane contains BOTH the pinned-variant header ("Claude has written up a plan and is ready to execute. Would you like to proceed?") anywhere AND the pinned-variant footer marker ("shift+tab to approve with this feedback") in the last 30 lines
    - isPlanPending returns FALSE when either the header or the bottom-slice marker is missing
    - isPlanPending returns FALSE on empty string, on prose that merely quotes the header, and on random terminal output
    - parsePlanFilePath returns the exact tilde-relative path string (e.g. "~/.claude/plans/groovy-watching-leaf.md") when the footer line matches the pattern
    - parsePlanFilePath returns null when the footer is absent, when the slug contains uppercase letters, when the slug contains characters outside [a-z0-9-], when the extension is not .md, or when the path prefix is not "~/.claude/plans/"
    - parsePlanFilePath never throws
  </behavior>
  <action>
    Edit `src/backend/claude-session/plan-pending-parser.ts`:

    1. Replace the fingerprint check inside `isPlanPending` (currently at lines ~66-79). The
       two-condition SHAPE (bottom-slice marker AND header-anywhere) STAYS. Only the strings
       change. The bottom-slice bound stays at 30 lines. Delete the OR-branch that checked
       both "Here is Claude's plan:" and "Ready to code?" — pinned fleet is single-variant so
       there is only ONE header string to check.

       New bottom-slice marker (verbatim, do NOT paraphrase): `shift+tab to approve with this feedback`
       New header-anywhere string (verbatim, do NOT paraphrase): `Claude has written up a plan and is ready to execute. Would you like to proceed?`

       Delete the now-unused strings "No, keep planning", "Here is Claude's plan:", "Ready to code?"
       from the function body.

    2. Add a new pure-helper `parsePlanFilePath(paneText: string): string | null` as a second
       named export in the same file. Same posture as `isPlanPending` — zero I/O, zero imports.
       Match the footer line format verbatim (Amelia's pane 2026-08-04):
       `ctrl-g to edit in  Vim  · ~/.claude/plans/<slug>.md` — note DOUBLE space between "in"
       and "Vim", middle-dot `·` (U+00B7) between "Vim" and the path.

       Use a regex like `/ctrl-g to edit in\s+Vim\s+·\s+(~\/\.claude\/plans\/[a-z0-9-]+\.md)/`
       and return the capture group OR null. Slug charset is `[a-z0-9-]+` — do NOT accept
       uppercase, dots (other than the .md extension), slashes, or any other characters.
       Return null on no match; do NOT throw.

       Add a JSDoc comment above `parsePlanFilePath` explaining that path validation
       (traversal, backticks, quotes, absolute-outside-plans) lives in the SFTP-fetch caller,
       NOT in this parser. This function is pure text extraction only.

    3. Update the docblock at the top of the file:
       - Preserve verbatim the WHY-A-PANE-SCRAPE-INSTEAD-OF-THE-JSONL section (patch #63
         legacy-JSONL rationale) — that decision does not change.
       - Rewrite the FINGERPRINT prose section so the specific string names refer to the new
         pinned-variant strings ("Claude has written up a plan…" and
         "shift+tab to approve with this feedback"). Remove references to
         "No, keep planning" / "Here is Claude's plan:" / "Ready to code?".
       - Add a note that Phase 24 also introduces `parsePlanFilePath` as a sibling helper.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && grep -c "shift+tab to approve with this feedback" src/backend/claude-session/plan-pending-parser.ts | grep -v '^0$'</automated>
    <automated>cd /home/ubuntu/skynet && grep -c "Claude has written up a plan and is ready to execute" src/backend/claude-session/plan-pending-parser.ts | grep -v '^0$'</automated>
    <automated>cd /home/ubuntu/skynet && grep -cE '^export function parsePlanFilePath' src/backend/claude-session/plan-pending-parser.ts | grep -v '^0$'</automated>
    <automated>cd /home/ubuntu/skynet && grep -cE '(No, keep planning|Here is Claude'"'"'s plan|Ready to code)' src/backend/claude-session/plan-pending-parser.ts | grep '^0$'</automated>
  </verify>
  <done>
    plan-pending-parser.ts exports both `isPlanPending` (with pinned-variant strings) and
    `parsePlanFilePath` (new). Old stale strings are gone. Docblock reflects the new fingerprint.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update plan-pending-parser.test.ts fixtures + add parsePlanFilePath cases</name>
  <files>src/backend/claude-session/plan-pending-parser.test.ts</files>
  <read_first>
    - src/backend/claude-session/plan-pending-parser.test.ts (the file being modified — full read)
    - src/backend/claude-session/plan-pending-parser.ts (post-Task 1 shape; needed for import + expected behavior)
    - src/backend/claude-session/context-pct-parser.test.ts (pure-helper test convention: `import { describe, it, expect } from "vitest";`, synthetic pane strings, zero mocks)
    - .planning/phases/24-plan-mode-approval-bubble-pane-tail-detection-expanded-bubbl/24-PATTERNS.md § plan-pending-parser.test.ts pattern assignments
  </read_first>
  <behavior>
    - Positive case: a synthetic pane string containing the pinned-variant header + bottom-slice marker returns TRUE from isPlanPending
    - Negative case: empty string returns FALSE
    - Negative case: a prose quote that mentions the header string but has no bottom-slice marker returns FALSE
    - Negative case: random terminal output with neither string returns FALSE
    - parsePlanFilePath happy case: footer line with exact Ink spacing (double space "in  Vim", middle-dot separator) returns the tilde-relative path string
    - parsePlanFilePath null case: pane with no footer returns null
    - parsePlanFilePath null case: footer with uppercase slug returns null
    - parsePlanFilePath null case: footer with `/` inside slug returns null (path traversal in the parser layer — even though the SFTP layer also validates)
    - parsePlanFilePath null case: footer with backtick / quote / $ in slug returns null
    - parsePlanFilePath null case: footer missing the .md extension returns null
  </behavior>
  <action>
    Rewrite `src/backend/claude-session/plan-pending-parser.test.ts`:

    1. Update the import to pull in the new export: `import { isPlanPending, parsePlanFilePath } from "./plan-pending-parser.js";`

    2. Replace the existing positive-case fixtures (currently target the 3-option
       `--dangerously-skip-permissions` variant with "No, keep planning") with a SINGLE positive
       fixture that mirrors the pinned fleet Ink variant. The fixture pane string must contain:
       - Somewhere in the middle/top: the header line `Claude has written up a plan and is ready to execute. Would you like to proceed?`
       - In the last 30 lines: the footer marker `shift+tab to approve with this feedback`
       - Optionally: the footer path line `ctrl-g to edit in  Vim  · ~/.claude/plans/groovy-watching-leaf.md`
       Assert `isPlanPending(pane)` returns `true`.

    3. Keep the existing negative-case tests (empty, prose-quote, random-terminal-output) but
       update the prose-quote fixture's inner quoted string from "No, keep planning" /
       "Here is Claude's plan:" to a paraphrase of the new header (so the assertion "prose
       quote does NOT trigger" still exercises the header-anywhere check being paired with the
       bottom-slice check). All three negatives must still return `false`.

    4. Add a new `describe("parsePlanFilePath — footer path extraction (Phase 24)", ...)` block
       with the following it-cases (mirror the shape in 24-PATTERNS.md § plan-pending-parser.test.ts):

       - "extracts the tilde-relative plans path from the footer with the exact Ink spacing"
         Fixture: pane containing `ctrl-g to edit in  Vim  · ~/.claude/plans/groovy-watching-leaf.md`.
         Expect: `parsePlanFilePath(pane)` === `"~/.claude/plans/groovy-watching-leaf.md"`.
       - "returns null when the footer is absent"
         Fixture: `"no footer here"`.
         Expect: null.
       - "returns null when the slug contains uppercase letters"
         Fixture: footer with `~/.claude/plans/FooBar.md`.
         Expect: null.
       - "returns null when the slug contains a slash (traversal attempt)"
         Fixture: footer with `~/.claude/plans/foo/bar.md`.
         Expect: null.
       - "returns null when the slug contains a backtick"
         Fixture: footer with a backtick inside the slug.
         Expect: null.
       - "returns null when the extension is not .md"
         Fixture: footer with `~/.claude/plans/foo.txt`.
         Expect: null.

    5. Preserve the top-of-file scaffolding comment (pure-helper posture, synthetic pane
       strings, no mocks) — update phase/patch tag to Phase 24.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx vitest run src/backend/claude-session/plan-pending-parser.test.ts --reporter=basic 2>&1 | tail -20</automated>
    <automated>cd /home/ubuntu/skynet && grep -cE 'parsePlanFilePath' src/backend/claude-session/plan-pending-parser.test.ts | grep -vE '^[01]$'</automated>
  </verify>
  <acceptance_criteria>
    - Vitest exits 0 for src/backend/claude-session/plan-pending-parser.test.ts
    - At least 6 `parsePlanFilePath` it-cases exist (grep for `parsePlanFilePath` in test file returns >= 6)
    - At least one positive isPlanPending fixture uses the string "shift+tab to approve with this feedback"
    - No test fixture references "No, keep planning" or "Here is Claude's plan:" or "Ready to code?" any more (except optionally as prose-quote negatives, but the header-check should not match)
  </acceptance_criteria>
  <done>
    Vitest passes for the parser test file. All positive cases use pinned-variant strings. New
    parsePlanFilePath cases cover happy path + all rejection cases (uppercase, slash, backtick,
    non-.md, missing footer).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pane text → parser | Untrusted PTY output enters the pure helpers. Bytes may contain adversarial patterns designed to trigger the fingerprint or the path regex. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01-01 | Tampering | parsePlanFilePath regex | mitigate | Slug charset restricted to `[a-z0-9-]+` at parser layer; SFTP caller (Plan 02) re-validates the full path. Defense-in-depth: even if the parser accepted `..` it would fail the SFTP-fetch layer's string-compare validation. |
| T-24-01-02 | Denial of Service | parser scan on massive pane | accept | Pane text is already bounded by tmux capture-pane output size in claude-session-server.ts. Adding another cap here would be redundant and add code without material safety benefit. |
</threat_model>

<verification>
- Vitest suite passes for the parser test file.
- Grep confirms only the pinned-variant strings appear in the parser source.
- `parsePlanFilePath` is exported and covered by >= 6 test cases.
</verification>

<success_criteria>
- isPlanPending correctly detects pinned fleet Ink plan-approval prompt using synthetic fixtures
- parsePlanFilePath extracts tilde-relative path or returns null on any invalid input
- Existing negative-case test coverage preserved
- Vitest exits 0 for plan-pending-parser.test.ts
</success_criteria>

<output>
Create `.planning/phases/24-plan-mode-approval-bubble-pane-tail-detection-expanded-bubbl/24-01-SUMMARY.md` when done.
</output>
