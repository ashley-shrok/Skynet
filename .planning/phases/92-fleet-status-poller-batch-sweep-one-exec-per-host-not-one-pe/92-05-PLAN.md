---
phase: 92-fleet-status-poller-batch-sweep-one-exec-per-host-not-one-pe
plan: 05
type: execute
wave: 4
depends_on: ["92-04"]
files_modified:
  - src/backend/fleet-status/ssh-poll-orchestrator.test.ts
autonomous: false
requirements: []
must_haves:
  truths:
    - "A vitest regression proves the sweep-present branch fires exactly ONE `channel.exec` matching `~/.local/bin/fleet-status-sweep` per host per poll cycle + zero per-identity / per-PID exec fan-out."
    - "A second vitest regression proves the sweep-absent branch fires the same per-identity + per-PID exec fan-out that exists today (byte-identical exec-count observable)."
    - "A parity regression proves the composed SessionState frames published to the registry are byte-identical between the batch path and the legacy path given equivalent input state (same identities, same PIDs, same sentinels, same JSONL tails)."
    - "The presence-probe re-probe-on-null-exec recovery path is exercised by a third regression (probe returns yes → sweep exec returns null → next tick re-probes → probe returns yes again → batch resumes)."
    - "The schema-version-mismatch fallback path is exercised by a fourth regression (sweep exec returns valid JSONL but with schema_version=999 → this-tick fallback to legacy → sweepSchemaMismatchThisConnection latched true → subsequent ticks stay on legacy until channel re-established)."
    - "The pre-existing ssh-poll-orchestrator.test.ts test suite is fully green — no test broken by the Plan 04 extraction."
    - "A pre-ship UAT checklist is written down so Ashley (or the /gsd-verify-work agent) can execute it against t1000 before merging."
  artifacts:
    - path: "src/backend/fleet-status/ssh-poll-orchestrator.test.ts"
      provides: "4 new test cases per the truths above + `makeSweepJsonl` helper matching sweep-schema.ts contract"
  key_links:
    - from: "ssh-poll-orchestrator.test.ts"
      to: "sweep-schema.ts"
      via: "shared makeSweepJsonl fixture builder using exported types"
      pattern: "SweepIdentityLine|SweepPidLine|SWEEP_SCHEMA_VERSION"
    - from: "ssh-poll-orchestrator.test.ts"
      to: "MockSshChannel"
      via: "countCallsMatching assertion pattern (RESEARCH.md § Existing test patterns)"
      pattern: "countCallsMatching"
---

<objective>
Prove the batch swap works via regression tests, and prepare the UAT checklist Ashley executes against t1000 to close the phase. Follow RESEARCH.md § Existing test patterns — reuse `MockSshChannel`, `MockRegistry`, `buildDeps`, `makeSessionJson`, `makeStatContents`, `makeValidPayload` verbatim, add one new fixture helper `makeSweepJsonl` that emits a JSONL blob matching the Plan 01 schema.

Purpose: The test suite is the durable proof that the batch swap achieved its exec-count collapse (the phase's whole point) AND preserved state-composition parity (the phase's whole non-negotiable). The UAT checklist is what closes the loop against real Skynet traffic — the vitest tests can't observe SFTP contention or `Channel open failure` log spam; only a human at a terminal watching t1000 can.

Output: 4 new vitest cases in ssh-poll-orchestrator.test.ts, plus a `checkpoint:human-verify` block at the end that pauses execution so Ashley can run the pre-ship UAT before this phase ships.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-fleet-status-poller-batch-sweep-one-exec-per-host-not-one-pe/92-CONTEXT.md
@.planning/phases/92-fleet-status-poller-batch-sweep-one-exec-per-host-not-one-pe/92-RESEARCH.md
@.planning/phases/92-fleet-status-poller-batch-sweep-one-exec-per-host-not-one-pe/92-01-SUMMARY.md
@.planning/phases/92-fleet-status-poller-batch-sweep-one-exec-per-host-not-one-pe/92-04-SUMMARY.md

@src/backend/fleet-status/ssh-poll-orchestrator.ts
@src/backend/fleet-status/ssh-poll-orchestrator.test.ts
@src/backend/fleet-status/sweep-schema.ts
@src/backend/fleet-status/sweep-schema.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 4 regression tests + makeSweepJsonl fixture helper to ssh-poll-orchestrator.test.ts</name>
  <files>src/backend/fleet-status/ssh-poll-orchestrator.test.ts</files>
  <behavior>
    - New test: **"batch path fires exactly ONE sweep exec per host per poll and zero per-identity/per-PID exec fan-out"**
        - Build a MockSshChannel that responds `"yes\n"` to the presence probe, and responds with a synthesized JSONL blob (via `makeSweepJsonl`) covering 3 identities + 2 live PIDs for the sweep exec command.
        - Run one poll tick via `buildDeps` + captured setInterval fn.
        - Assert `channel.countCallsMatching("~/.local/bin/fleet-status-sweep")` === 2 counted total (probe uses `test -x ~/.local/bin/fleet-status-sweep` and sweep uses `~/.local/bin/fleet-status-sweep 2>/dev/null` — both match the substring); OR use more precise substrings: `countCallsMatching("test -x ~/.local/bin/fleet-status-sweep")` === 1 for the probe AND `countCallsMatching("~/.local/bin/fleet-status-sweep 2>/dev/null")` === 1 for the sweep exec. Prefer the precise form.
        - Assert `channel.countCallsMatching("ls -1 ~/.claude/sessions/")` === 0 (source A driver did NOT fire).
        - Assert `channel.countCallsMatching("find ~/.claude/identities/")` === 0 (source B driver did NOT fire).
        - Assert `channel.countCallsMatching("cat /proc/")` === 0 (no per-PID stat).
        - Assert `channel.countCallsMatching("stat ~/.claude/identities/")` === 0 (no per-identity dormant/recycle stat).
        - Assert `MockRegistry.publishedStates.length` matches the expected identity+PID composition — 2 PID-derived frames + 3 identity-derived frames minus overlap per today's source-A-owns-non-recycle-when-live-PID skip semantics.

    - New test: **"legacy fallback path fires today's per-identity + per-PID exec fan-out when probe returns no"**
        - Build a MockSshChannel that responds `"no\n"` to the presence probe (script absent).
        - Register the same set of legacy per-identity + per-PID responses today's tests use — session listing, /proc/pid/stat readings, per-session stat outputs, tail outputs. Match the existing test file's fixture-registration pattern.
        - Run one poll tick.
        - Assert `channel.countCallsMatching("~/.local/bin/fleet-status-sweep 2>/dev/null")` === 0 (sweep exec never fires — batch path was skipped).
        - Assert `channel.countCallsMatching("ls -1 ~/.claude/sessions/")` === 1 (source A driver fires).
        - Assert `channel.countCallsMatching("find ~/.claude/identities/")` === 1 (source B driver fires).
        - Assert per-PID exec fan-out fires per today's counts (this is the mechanical byte-for-byte check).
        - Assert `MockRegistry.publishedStates.length` matches the same expected shape as the batch path test above — this is the parity assertion.

    - New test: **"presence probe fires once per SSH-channel lifetime, not per tick"**
        - Same setup as batch-path test.
        - Run THREE poll ticks (invoke the setInterval fn 3 times).
        - Assert `channel.countCallsMatching("test -x ~/.local/bin/fleet-status-sweep")` === 1 (only the FIRST tick probed; ticks 2 and 3 used the cached `sweepScriptPresent = true`).
        - Assert `channel.countCallsMatching("~/.local/bin/fleet-status-sweep 2>/dev/null")` === 3 (one sweep exec per tick).

    - New test: **"sweep exec returning null triggers legacy fallback this tick + re-probe next tick"**
        - Setup: presence probe returns yes, sweep exec returns `null` on tick 1 (simulate transient SSH-side error), returns valid JSONL on tick 2.
        - Register legacy per-identity + per-PID responses so tick 1's fallback has data to work with.
        - Run tick 1: assert batch attempted, batch failed (`fleet_status_batch_fallback` log op observable), legacy ran (per-identity + per-PID exec fan-out fires), publish happened.
        - Run tick 2: assert probe re-fired (probe count === 2 now — one on initial connection + one on retry-after-null), batch path ran successfully, no legacy exec fan-out on tick 2.

    - New test: **"schema-version mismatch on sweep emission triggers this-tick legacy fallback + connection-lifetime latch"**
        - Setup: presence probe returns yes; sweep exec returns valid JSONL but every line has `schema_version: 999`.
        - Register legacy fixtures so fallback has data.
        - Run tick 1: assert sweep exec fired (once), assert `schemaMismatch` was detected (implementation-wise: the fallback fires and the warn-log op `fleet_status_batch_fallback` with `reason: "schema-mismatch"` is observable), assert legacy exec fan-out fired.
        - Run tick 2: assert sweep exec did NOT fire (latched off because `sweepSchemaMismatchThisConnection === true`), assert legacy exec fan-out fired instead.

    - Fixture helper (co-located in the test file — mirrors `makeSessionJson`, `makeStatContents`, `makeValidPayload`):
        - `function makeSweepJsonl(input: { identities: Array<Partial<SweepIdentityLine> & { identity: string }>; pids: Array<Partial<SweepPidLine> & { identity: string; pid: number }>; schemaVersionOverride?: number }): string`
        - Applies sensible defaults for every field on each line (dormant: false, jsonl_path: null, layer1_recycling: null, etc.), lets tests override only what they care about, joins one JSON-per-line, returns the blob a MockSshChannel returns as sweep-exec response.
        - Uses `SWEEP_SCHEMA_VERSION` from sweep-schema.ts by default, overridable via `schemaVersionOverride` for the mismatch test.
  </behavior>
  <action>
    Read the top of ssh-poll-orchestrator.test.ts (roughly L1–L290 per RESEARCH.md § Existing test patterns) to inventory the current `MockSshChannel`, `MockRegistry`, `buildDeps`, and fixture helpers. Locate the section where fixture helpers are declared (near L171–L215 per RESEARCH.md). Add `makeSweepJsonl` there — mirror the shape of `makeSessionJson` / `makeStatContents` / `makeValidPayload` for consistency.

    Add the 4 new test cases at the END of the file (after the last existing describe block). Group them under a new `describe("Phase 92 — batch sweep dispatch", () => { ... })` block so they are easy to run in isolation via `npx vitest run src/backend/fleet-status/ssh-poll-orchestrator.test.ts -t "Phase 92"`.

    Match the file's existing style — same import shape, same `beforeEach`/`afterEach` patterns if present, same use of `deps.now()` mocks + fake timers, same MockSshChannel.setResponse pattern.

    The parity assertion in the second test is the most important — it's the concrete guarantee that batch mode and legacy mode produce identical registry frames. Prefer the strict assertion: build BOTH the batch and legacy MockSshChannel + Registry pairs from THE SAME input fixture data, run one tick each, and `expect(batchRegistry.publishedStates).toEqual(legacyRegistry.publishedStates)`. If field ordering or clock-derived fields cause spurious inequality, normalize by stripping `updatedAt` (or freezing `deps.now()` to a fixed value) — pattern already established in the existing tests.

    Do NOT modify sweep-schema.ts, ssh-poll-orchestrator.ts, or any other non-test file. Do NOT skip or delete any existing tests. If any existing test breaks due to the Plan 04 extraction (should not happen — Plan 04 preserves behavior — but if it does), fix minimally: update the exec-count expectation to reflect the new dispatch shape, do NOT weaken the assertion.

    Run the full ssh-poll-orchestrator.test.ts suite and confirm zero failures.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; npx vitest run src/backend/fleet-status/ssh-poll-orchestrator.test.ts</automated>
  </verify>
  <done>
    ssh-poll-orchestrator.test.ts contains 4 new tests inside a `Phase 92 — batch sweep dispatch` describe block. `makeSweepJsonl` helper is co-located next to existing fixture helpers. Full file passes: `npx vitest run src/backend/fleet-status/ssh-poll-orchestrator.test.ts` reports zero failures. Existing tests still pass unchanged. Parity assertion (batch vs legacy → same publishedStates) is a strict `toEqual` comparison (possibly with normalized clock fields).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Pre-ship UAT checklist against t1000</name>
  <files>N/A — human-verification checkpoint; no files modified</files>
  <action>
    Pause execution and present the UAT checklist below to Ashley. Do NOT proceed past this task without her explicit `all 5 checks PASS` reply. If any check fails, do NOT merge — surface the failure back to the orchestrator so gap-closure planning can address it.
  </action>
  <what-built>
    Plans 01–04 shipped: the v1 JSONL schema module, the Python sweep script at substrate/scripts/fleet-status-sweep.py, the distributor catalog row, and the ssh-poll-orchestrator.ts batch-first/legacy-fallback rewire. Task 1 above landed the vitest regression suite proving exec-count collapse + parity in-process.

    What remains: real-world verification against t1000's Skynet host connection under normal fleet-status poll load. Per CONTEXT.md § Verification, this cannot be automated — it requires human observation of container logs and one live SFTP-through-Skynet fetch reproduction.
  </what-built>
  <how-to-verify>
    Ashley: run these steps in order against t1000 (or whichever box is running the Skynet container being tested), and reply with results.

    **Prerequisite:** Skynet container has been rebuilt with Plans 01–05's changes AND restarted so the distributor's Phase 75 startup sweep has pushed `~/.local/bin/fleet-status-sweep` to every `runsFleetSubstrate:true` peer host. Verify this ONCE before running the checks:

    `ssh <peer-host> "test -x ~/.local/bin/fleet-status-sweep && echo OK-installed || echo MISSING"`

    Expect OK-installed on peer hosts with `runs_fleet_substrate=true`. MISSING on opt-out peers is expected and correct — legacy fallback covers them.

    **Check 1 — container log free of `Channel open failure` spam under normal poll load.**

    `docker logs skynet-backend --since 10m 2>&1 | grep -c "Channel open failure"`

    Expected: 0, or a small single-digit number attributable to non-fleet-status pool consumers (SFTP, guacd). Before this phase's fix, this count was in the tens per hour under fleet-status poll load. If the count is 0 → PASS. If <10 in 10 minutes → PASS with a note (ambient failure rate from non-fleet consumers). If >10 in 10 minutes → FAIL, investigate.

    **Check 2 — sweep exec count in the log.**

    `docker logs skynet-backend --since 5m 2>&1 | grep -c "fleet_status_poll_end.*batch"` and `docker logs skynet-backend --since 5m 2>&1 | grep -c "fleet_status_poll_end.*legacy"`

    Expected: nonzero batch count (roughly `hosts × 5 minutes × 30 ticks/minute`), legacy count only for opt-out or mid-rollout hosts. If ALL hosts landed on the batch path, legacy count should be 0.

    **Check 3 — SFTP file-fetch reliability (Tabitha's reproducer).**

    Run the SFTP file-fetch curl (the passthrough URL that failed 5/5 on 2026-09-09) 20 consecutive times, each 3 seconds apart:

    `for i in $(seq 1 20); do curl -sS -o /dev/null -w "attempt=$i http_code=%{http_code} time_total=%{time_total}\n" "https://term.gigaashley.click/<the-Skynet-passthrough-URL-Tabitha-used>"; sleep 3; done`

    (Substitute the exact URL from the 2026-09-09 issue-log.md.)

    Expected: 20/20 status 200 or 20/20 status 302 or otherwise all consistent success shapes. Zero `host_unreachable`. Zero `SSH Channel open failure`.

    **Check 4 — no fleet-status regressions in the UI.**

    Open the Skynet frontend at https://term.gigaashley.click. Wait 30 seconds for a full poll cycle. Confirm every identity's fleet-status indicator (dormant / recycling / working / waiting) shows the expected state, matches your mental model of what those identities are actually doing on their boxes. Any identity showing wrong state → note it and FAIL.

    **Check 5 — sweep script wall-clock health.**

    `docker logs skynet-backend --since 5m 2>&1 | grep "fleet_status_sweep_exec_null"`

    Expected: zero or negligible. Persistent non-zero here means the sweep is hitting the 8s timeout, indicating a slow filesystem on some peer box — investigate but do NOT block the phase's ship on this (transient recovery is built into the design).

    Reply with: `all 5 checks PASS` OR describe which checks failed and how.
  </how-to-verify>
  <verify>
    <human-check>Ashley executes the 5 UAT steps above against t1000 and replies with pass/fail per step.</human-check>
  </verify>
  <done>
    Ashley has replied `all 5 checks PASS` OR the orchestrator has captured the failure(s) for gap-closure planning. Under no circumstance does this task self-approve — it MUST wait for a human reply.
  </done>
  <resume-signal>Reply `all 5 checks PASS` to approve for merge, OR describe failure(s) and the plan reopens for gap-closure planning.</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run src/backend/fleet-status/ssh-poll-orchestrator.test.ts` — all tests pass, includes the 4 new Phase 92 cases.
- `npx vitest run` — full suite green, no regressions in unrelated files.
- Task 2 checkpoint approved by Ashley — all 5 UAT checks PASS against t1000.
</verification>

<success_criteria>
- Vitest regression proves ONE exec per host per poll under the batch path.
- Vitest regression proves legacy fallback intact and byte-identical parity with batch.
- Presence-probe cache + null-exec re-probe + schema-mismatch latch all covered by tests.
- Human UAT against t1000 confirms `Channel open failure` spam gone + SFTP file-fetch reliably succeeds 20/20.
- Frontend fleet-status indicators show expected state for every identity.
</success_criteria>

<output>
Create `.planning/phases/92-fleet-status-poller-batch-sweep-one-exec-per-host-not-one-pe/92-05-SUMMARY.md` when done. Include: full list of 4 new test names, MockSshChannel setup shape for each, parity assertion approach (strict toEqual vs normalized), UAT execution result summary (once Ashley replies with pass/fail), any observed regression or drift from expected behavior.
</output>
