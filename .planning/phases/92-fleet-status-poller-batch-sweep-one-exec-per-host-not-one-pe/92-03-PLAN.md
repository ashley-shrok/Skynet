---
phase: 92-fleet-status-poller-batch-sweep-one-exec-per-host-not-one-pe
plan: 03
type: execute
wave: 2
depends_on: ["92-02"]
files_modified:
  - src/backend/distributor/catalog.ts
  - src/backend/distributor/catalog.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "A new CatalogEntry row for the fleet-status-sweep script is appended to FLEET_SUBSTRATE_CATALOG, with correct bundledPath, installPath, and null restartHook."
    - "catalog.test.ts row-count assertion is bumped to reflect the new entry, and every existing assertion still passes."
    - "On next container rebuild + restart, the distributor sweep will ship substrate/scripts/fleet-status-sweep.py to `~/.local/bin/fleet-status-sweep` on every `runsFleetSubstrate:true` host with no additional wiring."
  artifacts:
    - path: "src/backend/distributor/catalog.ts"
      provides: "one new CatalogEntry appended to FLEET_SUBSTRATE_CATALOG"
      contains: "fleet-status-sweep"
    - path: "src/backend/distributor/catalog.test.ts"
      provides: "updated row-count expectation + slug uniqueness proof still holds"
  key_links:
    - from: "catalog.ts"
      to: "substrate/scripts/fleet-status-sweep.py (Plan 02 producer)"
      via: "bundledPath /app/fleet-substrate/scripts/fleet-status-sweep.py"
      pattern: "fleet-status-sweep"
    - from: "catalog.ts"
      to: "run-sweep.ts (existing distributor composer)"
      via: "iteration over FLEET_SUBSTRATE_CATALOG at container startup"
      pattern: "FLEET_SUBSTRATE_CATALOG"
---

<objective>
Register the fleet-status-sweep script in the fleet-substrate catalog so the existing distributor (Phase 72–75) picks it up on next container restart and pushes it to every `runsFleetSubstrate:true` host at `~/.local/bin/fleet-status-sweep`.

Purpose: Without this row, the sweep script from Plan 02 is a file in the container that never gets to any managed box. This is the "one more thing" that turns Plan 02's server-side script into a real fleet-wide asset.

Output: One appended row in `FLEET_SUBSTRATE_CATALOG`, one row-count bump in the catalog test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-fleet-status-poller-batch-sweep-one-exec-per-host-not-one-pe/92-CONTEXT.md
@.planning/phases/92-fleet-status-poller-batch-sweep-one-exec-per-host-not-one-pe/92-RESEARCH.md
@.planning/phases/92-fleet-status-poller-batch-sweep-one-exec-per-host-not-one-pe/92-02-SUMMARY.md

@src/backend/distributor/catalog.ts
@src/backend/distributor/catalog.test.ts
@src/backend/distributor/run-sweep.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append fleet-status-sweep CatalogEntry + bump catalog.test.ts row count</name>
  <files>src/backend/distributor/catalog.ts, src/backend/distributor/catalog.test.ts</files>
  <action>
    Read the current tail of `FLEET_SUBSTRATE_CATALOG` in src/backend/distributor/catalog.ts to identify the last existing entry's syntactic shape and section-comment convention. Append a new CatalogEntry at the very end of the array, under a `// --- fleet-status-sweep (1 row: python batch sweep for fleet-status poller — Phase 92) ---` section comment.

    Exact shape of the new row (per RESEARCH.md § Catalog entry shape + CONTEXT.md § Claude's Discretion):

    ```
    {
      slug: "fleet-status-sweep",
      bundledPath: "/app/fleet-substrate/scripts/fleet-status-sweep.py",
      installPath: "~/.local/bin/fleet-status-sweep",
      restartHook: null,
    },
    ```

    Note the install path drops the `.py` extension by existing convention (see agent-supervisor entry: `.sh` → no extension; wakeup-scheduler entry: `.py` → no extension).

    Update catalog.test.ts (concrete assertions to bump — verified against current file):
    - **Test 1** (`length === 21`): change to `length === 22`. The nearby docstring/comment enumerating "6 helper scripts" also needs bumping to "7".
    - **Test 6** (`scriptRows.length === 7`): change to `8`. The comment enumerating the 7 script slugs by name needs `fleet-status-sweep` added as the 8th.
    - Slug-uniqueness + path-shape iteration tests need no change — the new row's slug `fleet-status-sweep` is unique and its paths conform to the existing shape rules.
    - Any "row N has slug X" assertions against specific final entries should still hold — new row is appended at the end, existing indices unchanged.

    Do NOT reorder existing entries. Do NOT change any other row's mode, path, or hook. Do NOT modify run-sweep.ts, ssh-push.ts, sweep-logic.ts, or server-substrate-orchestrator.ts — the composer picks up the new row automatically once the catalog array is mutated.

    After editing both files, run the distributor test suite: `npx vitest run src/backend/distributor/`. All tests in the distributor package must pass. The row-count bump is the only substantive assertion change; slug uniqueness, path-shape validation, and iteration tests should pass without further modification.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; grep -c "fleet-status-sweep" src/backend/distributor/catalog.ts | grep -v '^0$' &amp;&amp; npx vitest run src/backend/distributor/</automated>
  </verify>
  <done>
    src/backend/distributor/catalog.ts contains a new entry with slug `fleet-status-sweep`, bundledPath `/app/fleet-substrate/scripts/fleet-status-sweep.py`, installPath `~/.local/bin/fleet-status-sweep`, restartHook `null`. src/backend/distributor/catalog.test.ts row-count expectation is incremented by 1. `npx vitest run src/backend/distributor/` passes. No changes to run-sweep.ts, ssh-push.ts, sweep-logic.ts, server-substrate-orchestrator.ts.
  </done>
</task>

</tasks>

<verification>
- `grep -c "fleet-status-sweep" src/backend/distributor/catalog.ts` returns at least 1.
- `npx vitest run src/backend/distributor/` reports zero failures.
- Full test suite green: `npx vitest run` no regressions in unrelated modules.
- Manually confirm section comment convention matches existing entries (open catalog.ts and scan the section comments).
</verification>

<success_criteria>
- Next container rebuild ships the sweep script to every `runsFleetSubstrate:true` host at `~/.local/bin/fleet-status-sweep` via the existing distributor mechanism, with no additional code path required.
- `restartHook: null` because the sweep is invoked on-demand by the fleet-status caller, not a long-lived daemon.
</success_criteria>

<output>
Create `.planning/phases/92-fleet-status-poller-batch-sweep-one-exec-per-host-not-one-pe/92-03-SUMMARY.md` when done. Include: new row-count value in catalog.test.ts, the exact appended CatalogEntry, confirmation `npx vitest run src/backend/distributor/` is green.
</output>
