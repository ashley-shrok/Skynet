---
phase: 92-fleet-status-poller-batch-sweep-one-exec-per-host-not-one-pe
plan: 04
type: execute
wave: 3
depends_on: ["92-01", "92-02", "92-03"]
files_modified:
  - src/backend/fleet-status/ssh-poll-orchestrator.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "On a host with the sweep script installed, `pollOneHost` fires exactly ONE `channel.exec` for the sweep + zero per-identity + zero per-PID exec fan-out per poll cycle."
    - "On a host without the sweep script (opt-out or mid-rollout), `pollOneHost` falls back to the existing legacy per-identity + per-PID exec fan-out — byte-for-byte the same code path currently executing today."
    - "The presence probe (`test -x ~/.local/bin/fleet-status-sweep`) fires once per host per SSH-connection lifetime (cached on `PerHostState`) — NOT per poll tick."
    - "On presence-probe fail-then-succeed or transient sweep-exec null return, the per-host cached-probe state is re-checked on the next tick — no permanent lockout after one hiccup."
    - "Schema-version mismatch on the parsed sweep emission triggers a one-time warn-log + fallback to legacy plumbing for this tick, then re-probes next tick."
    - "SessionState frames published to the registry via `publish` / `publishGone` are byte-identical between the batch path and the legacy path (parity)."
    - "`writeSessionFileCache` (RESEARCH.md G8) still fires with the same shape after batch-mode polls."
    - "`inFlight` guard (RESEARCH.md G5) still applies — a slow sweep does not stack ticks."
    - "The sessionId safe-char regex (RESEARCH.md G6) still applies caller-side even though the sweep script also applies it server-side (belt-and-suspenders)."
  artifacts:
    - path: "src/backend/fleet-status/ssh-poll-orchestrator.ts"
      provides: "pollOneHost with sweep-first / legacy-fallback dispatch, per-host presence-probe cache, JSONL-parse-drives-state-composition wiring"
  key_links:
    - from: "ssh-poll-orchestrator.ts"
      to: "sweep-schema.ts"
      via: "parseSweepJsonl import + SWEEP_SCHEMA_VERSION comparison"
      pattern: "import.*sweep-schema"
    - from: "ssh-poll-orchestrator.ts"
      to: "~/.local/bin/fleet-status-sweep on managed box"
      via: "channel.exec of the installed script path"
      pattern: "~/.local/bin/fleet-status-sweep"
    - from: "ssh-poll-orchestrator.ts"
      to: "existing legacy processPid + pollDormantOnlyIdentities"
      via: "extracted-into-helper + reused in fallback branch"
      pattern: "processPid|pollDormantOnlyIdentities"
---

<objective>
Rewire `pollOneHost` in ssh-poll-orchestrator.ts to invoke ONE sweep exec per host per poll cycle when the sweep script is present, and fall back to the current per-identity + per-PID exec fan-out when it's not. Preserve every downstream behavior: state composition, fingerprint delta, registry publish, `livenessMap`/`identityRecycleState` caches, `writeSessionFileCache`, `inFlight` guard, safe-char regex, transition-edge publish semantics.

Purpose: This is the load-bearing task of the phase. Every other plan feeds this one. Getting this wrong is what breaks parity and re-introduces the exec flood or (worse) silently drops SessionState frames.

Output: A modified `ssh-poll-orchestrator.ts` where `pollOneHost` is split into three coordinated pieces: (a) the presence-probe cache logic, (b) the batch path (sweep exec + parse + state compose), (c) the legacy path (the current per-PID + per-identity fan-out, minimally extracted into a helper so both branches share compose/publish infrastructure). Answers **open question #4 (presence-probe cache scope) → per-host-per-connection-lifetime, cleared on `acquireSshChannel` re-establish**.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-fleet-status-poller-batch-sweep-one-exec-per-host-not-one-pe/92-CONTEXT.md
@.planning/phases/92-fleet-status-poller-batch-sweep-one-exec-per-host-not-one-pe/92-RESEARCH.md
@.planning/phases/92-fleet-status-poller-batch-sweep-one-exec-per-host-not-one-pe/92-01-SUMMARY.md
@.planning/phases/92-fleet-status-poller-batch-sweep-one-exec-per-host-not-one-pe/92-02-SUMMARY.md

@src/backend/fleet-status/ssh-poll-orchestrator.ts
@src/backend/fleet-status/sweep-schema.ts
@src/backend/fleet-status/types.ts
@src/backend/fleet-status/session-file-cache.ts
@src/backend/starter.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add presence-probe cache + sweep-first / legacy-fallback dispatch to pollOneHost</name>
  <files>src/backend/fleet-status/ssh-poll-orchestrator.ts</files>
  <action>
    Modify pollOneHost (currently at L861 per RESEARCH.md, verify line number before edit) to implement sweep-first / legacy-fallback dispatch.

    **Step 1 — Extend PerHostState with the presence-probe cache.**

    Locate the `PerHostState` type in ssh-poll-orchestrator.ts (or wherever the type is declared — grep for `interface PerHostState` before editing). Add TWO new fields:

    ```
    /** Phase 92: cached presence of ~/.local/bin/fleet-status-sweep for this
     *  host's current SSH channel lifetime. `null` before first probe;
     *  reset to null when the SSH channel is torn down and re-acquired.
     *  On sweep-exec null return, re-probed next tick (transient recovery).
     */
    sweepScriptPresent: boolean | null;

    /** Phase 92: last-observed sweep schema version — used to detect drift
     *  and force a re-probe. Same lifetime as sweepScriptPresent. */
    sweepSchemaMismatchThisConnection: boolean;
    ```

    Initialize both to `null` / `false` at PerHostState construction site (grep the file for PerHostState construction — probably near where hostState is built after `acquireSshChannel`). Also clear both back to null/false whenever `hostClients` deletes an entry (starter.ts L615–L617 handlers) — this may require a small callback threaded through the deps or a direct reset call from the poll orchestrator on channel loss. Simplest wiring: reset happens the FIRST time a poll tick sees a new channel object identity — compare the current `hostState.channel` reference against a cached previous reference; if different, reset the cache. Cheaper than plumbing a callback through starter.ts.

    **Step 2 — Extract legacy pollOneHost body into `pollOneHostLegacy`.**

    The current pollOneHost body (L861–L925 per RESEARCH.md) has two phases: source A (`processPid` fan-out over PIDs enumerated by `ls ~/.claude/sessions/`) and source B (`pollDormantOnlyIdentities`). Extract that body verbatim into a new local function `pollOneHostLegacy(hostState: PerHostState): Promise<void>` in the same closure scope. No behavior change — this is a rename+move, not a rewrite. The extracted function reuses the existing `processPid` and `pollDormantOnlyIdentities` closures unchanged.

    **Step 3 — Add `pollOneHostBatch`.**

    Add a new local function `pollOneHostBatch(hostState: PerHostState): Promise<{ ok: true } | { ok: false; reason: "null-exec" | "schema-mismatch" | "empty-output-on-nonempty-box" }>`.

    Batch path steps:
    1. `const sweepRaw = await channel.exec("~/.local/bin/fleet-status-sweep 2>/dev/null");` — no timeout wrapper here; the top-level `Promise.race` idiom (matching `discoverIdentityJsonlPathViaChannel` at L726–L739) SHOULD wrap this. Recommend timeout constant `SWEEP_EXEC_TIMEOUT_MS = 8000` (larger than 5s discovery timeout because the sweep does per-identity fs walks that may hit cold disk cache on managed boxes with many identities; still safely under the 2s poll interval's `inFlight` skip semantics on most boxes).
    2. If `sweepRaw === null` → return `{ ok: false, reason: "null-exec" }`. Caller logs a `fleet_status_sweep_exec_null` op and falls through to legacy.
    3. `const parsed = parseSweepJsonl(sweepRaw);` (import from sweep-schema.ts).
    4. If `parsed.schemaMismatch` → return `{ ok: false, reason: "schema-mismatch" }`, set `hostState.sweepSchemaMismatchThisConnection = true`, log a warn (throttled — one per connection lifetime, not per tick).
    5. If `parsed.identityLines.length === 0 && parsed.pidLines.length === 0`: this is ambiguous — could be a truly empty box (identities dir missing) OR a broken script. Distinguish by requiring at least one identity line for the box to be "non-empty" per Plan 02's contract. If the caller has evidence from a prior legacy poll that this box has identities (`hostState.livenessMap.size > 0 || hostState.identityRecycleState.size > 0`), treat as `"empty-output-on-nonempty-box"` and fall back to legacy. Otherwise accept the empty emission — this correctly handles the freshly-provisioned or emptied box case.
    6. On success, drive state composition:
       - **⚠️ HANDLE WITH CARE — this is the highest-risk task in the phase.** `processPid` is 700+ lines (L1236 to ~L1950), pulls closure state from `hostState`, `livenessMap`, publish helpers, and the module-level `pollTickCount`. The fetch/compose split is the correct abstraction but the seam is not trivial to place. **Before editing ssh-poll-orchestrator.ts at all, do two things:** (a) run `npx vitest run src/backend/fleet-status/` and confirm existing tests are green — this is your baseline gate; (b) produce a paper sketch (comment block at the top of the plan working file, or a scratch note) of the exact `PerPidFetchedState` and `PerIdentityFetchedState` type shapes AND which existing lines move into fetch vs compose vs stay in closure. Attention points for the seam: which values pass through closure vs the fetched-state struct (e.g., cached values from `livenessMap` — read before or after fetch?); where `emitHookPayloadWarn` at L1957 fires (compose or fetch?); the Phase 62 hook-payload selection at L1770-ish (per-session vs box-wide — that's compose-layer in the batch path since per-session comes from the sweep line, but legacy-path selection depends on which fetch returned non-empty). If the seam requires internal shape changes to `PidCacheEntry` or `IdentityRecycleCacheEntry`, that's scope-creep — pause and flag back to orchestrator before proceeding.
       - **For each `SweepPidLine`:** synthesize the same intermediate values `processPid` today produces from its A1–A12 exec results, then feed them through the SAME per-PID compose + publish machinery. Refactor `processPid` (at ~L1236) to split into two functions: a `fetchPerPidState(hostState, pid): PerPidFetchedState` that runs the exec fan-out today, and a `composeAndPublishPerPid(hostState, pid, fetchedState): void` that runs everything downstream of the fetch. Batch path calls `composeAndPublishPerPid` directly with the fetched state synthesized from the SweepPidLine; legacy path calls `fetchPerPidState` then `composeAndPublishPerPid` — same downstream.
       - **For each `SweepIdentityLine`:** same refactor pattern for `pollDormantOnlyIdentities`. Split into `fetchPerIdentityState(hostState, name): PerIdentityFetchedState` (runs today's B1–B5 exec fan-out) and `composeAndPublishPerIdentity(hostState, name, fetchedState): void`. Batch path drives `composeAndPublishPerIdentity` with the SweepIdentityLine's fields; legacy path uses the fetch-then-compose sequence.
       - The `PerPidFetchedState` and `PerIdentityFetchedState` type shapes should be defined new (they're a rename of the intermediate values today's code passes via closure), and their fields should map directly to the SweepPidLine / SweepIdentityLine fields plus any purely-caller-side derived values.
    7. After both loops complete, the `livenessMap`, `identityRecycleState`, and any `writeSessionFileCache` calls SHOULD have fired identically to what the legacy path produces. **G8 check: `writeSessionFileCache` must fire from the compose layer, not the fetch layer, so it fires equally in both paths.** Verify by reading L1804 in ssh-poll-orchestrator.ts and confirming the write sits under compose, not fetch.
    8. Return `{ ok: true }`.

    **Step 4 — New pollOneHost body: probe + dispatch + fallback.**

    Replace the current pollOneHost body with:

    ```
    async function pollOneHost(hostState: PerHostState): Promise<void> {
      const { host, channel } = hostState;

      systemLogger.info("Fleet-status poll start", { operation: "fleet_status_poll_start", fleetHostId: host.id, tick: pollTickCount });

      // Reset per-connection presence cache if the SSH channel object changed.
      if (hostState.channel !== hostState.lastProbeChannelRef) {
        hostState.sweepScriptPresent = null;
        hostState.sweepSchemaMismatchThisConnection = false;
        hostState.lastProbeChannelRef = hostState.channel;
      }

      // Presence probe on first tick per SSH-channel lifetime OR if a previous
      // batch attempt returned null-exec (transient recovery).
      if (hostState.sweepScriptPresent === null) {
        const probeRaw = await channel.exec("test -x ~/.local/bin/fleet-status-sweep 2>/dev/null && echo yes || echo no");
        hostState.sweepScriptPresent = probeRaw !== null && probeRaw.trim() === "yes";
        systemLogger.info("Fleet-status: sweep-script presence probed", {
          operation: "fleet_status_sweep_probe",
          fleetHostId: host.id,
          present: hostState.sweepScriptPresent,
        });
      }

      // Dispatch: batch first, legacy fallback on failure.
      if (hostState.sweepScriptPresent && !hostState.sweepSchemaMismatchThisConnection) {
        const result = await pollOneHostBatch(hostState);
        if (result.ok) {
          systemLogger.info("Fleet-status poll end (batch)", { operation: "fleet_status_poll_end", fleetHostId: host.id, tick: pollTickCount, path: "batch" });
          return;
        }
        // Failure — log why and fall through to legacy for this tick.
        systemLogger.warn("Fleet-status: batch path failed, falling back to legacy this tick", {
          operation: "fleet_status_batch_fallback",
          fleetHostId: host.id,
          reason: result.reason,
        });
        // On null-exec, force re-probe next tick (may be transient).
        if (result.reason === "null-exec") {
          hostState.sweepScriptPresent = null;
        }
      }

      await pollOneHostLegacy(hostState);
      systemLogger.info("Fleet-status poll end (legacy)", { operation: "fleet_status_poll_end", fleetHostId: host.id, tick: pollTickCount, path: "legacy" });
    }
    ```

    (`lastProbeChannelRef: SshChannel | null` is a new PerHostState field, initialized to null. Its sole purpose is per-channel-lifetime cache invalidation.)

    **Step 5 — Confirm the legacy path is untouched.**

    `pollOneHostLegacy` (the extracted body from Step 2) should be BYTE-IDENTICAL in behavior to today's pollOneHost. The only source-level change is a wrapping function boundary. This preserves the mandatory backward-compat fallback per CONTEXT.md § Backward compat during rollout: "Do NOT hard-fail if the sweep script is absent."

    **Step 6 — Guards, log ops, and non-negotiables.**

    - `writeSessionFileCache` MUST fire in both branches with the same shape (G8).
    - The sessionId safe-char regex `/^[a-zA-Z0-9_-]+$/` MUST be applied caller-side inside `composeAndPublishPerPid` even when input comes from the sweep (G6 belt-and-suspenders). Wrap each per-session field access in the same regex guard the current code has (L1333, L1389, L1409, L1471) — do NOT drop the guard just because Plan 02 also applies it server-side.
    - The `inFlight` guard at L2034–L2066 requires no change — it operates on the whole `pollOneHost` call regardless of dispatch branch.
    - Existing state composition, fingerprint delta, publish/publishGone contract: unchanged.
    - Do NOT touch `src/backend/ssh/` (semaphore, connection pool). Do NOT change the poll interval. Do NOT change what state gets collected (parity with today).
    - Do NOT modify `sweep-schema.ts` — Plan 01 owns that file's shape.
    - Do NOT remove the semaphore (D-01 defer-forever).

    **Step 7 — Compile + typecheck.**

    Run `npx tsc --noEmit` (or the project's typecheck script) — no new type errors. Existing lint rules apply (grep the repo for eslint / biome config; run the standard `npm run lint` if it exists).

    **Step 8 — Sanity-run the existing test file to confirm the extraction didn't break anything.**

    `npx vitest run src/backend/fleet-status/ssh-poll-orchestrator.test.ts`. All existing tests must pass. If any test breaks because the extraction changed a function's arity or a mock's exec-count expectation, the test's assertion may need a minor mechanical update — but the underlying state composition should be identical. New batch-vs-legacy tests are added in Plan 05, not here.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run src/backend/fleet-status/ssh-poll-orchestrator.test.ts</automated>
  </verify>
  <done>
    ssh-poll-orchestrator.ts imports parseSweepJsonl + SWEEP_SCHEMA_VERSION from sweep-schema.ts. pollOneHost dispatches sweep-first / legacy-fallback with a per-connection presence cache. PerHostState carries sweepScriptPresent, sweepSchemaMismatchThisConnection, lastProbeChannelRef. `pollOneHostLegacy` is the extracted-verbatim previous body. `pollOneHostBatch` drives the same compose + publish downstream as legacy via the fetch/compose split. `writeSessionFileCache` still fires. Safe-char regex still applied. `npx tsc --noEmit` passes. Existing `npx vitest run src/backend/fleet-status/ssh-poll-orchestrator.test.ts` still passes.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — zero new type errors.
- `npx vitest run src/backend/fleet-status/ssh-poll-orchestrator.test.ts` — all existing tests still pass.
- `grep -c "pollOneHostBatch\|pollOneHostLegacy\|sweepScriptPresent" src/backend/fleet-status/ssh-poll-orchestrator.ts` returns positive counts for all three symbols.
- `grep -c "writeSessionFileCache" src/backend/fleet-status/ssh-poll-orchestrator.ts` returns the same count as before this plan (or one higher if a compose-layer refactor moved it) — i.e. the cache write is not lost.
- `grep -c "/proc/" src/backend/fleet-status/ssh-poll-orchestrator.ts` returns the SAME count as pre-plan (the /proc/ exec commands live in `pollOneHostLegacy` unchanged — extraction, not deletion).
- No changes to files outside `src/backend/fleet-status/ssh-poll-orchestrator.ts`.
</verification>

<success_criteria>
- Batch path lands: one sweep exec per host per poll on sweep-present hosts.
- Legacy fallback intact and byte-identical for sweep-absent hosts.
- Presence probe cached per SSH-channel lifetime (open question #4 answered).
- Every RESEARCH.md non-negotiable respected: G6 belt-and-suspenders regex, G7 dual dormant reads folded correctly, G8 writeSessionFileCache still fires, G5 inFlight guard unchanged, semaphore untouched.
</success_criteria>

<output>
Create `.planning/phases/92-fleet-status-poller-batch-sweep-one-exec-per-host-not-one-pe/92-04-SUMMARY.md` when done. Include: line count of `pollOneHostBatch`, line count of `pollOneHostLegacy`, list of new PerHostState fields, confirmation that `writeSessionFileCache` fires in both branches with the same shape, any refactoring decisions made during the `processPid` / `pollDormantOnlyIdentities` fetch/compose split.
</output>
