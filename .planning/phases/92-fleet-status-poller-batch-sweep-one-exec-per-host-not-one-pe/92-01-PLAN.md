---
phase: 92-fleet-status-poller-batch-sweep-one-exec-per-host-not-one-pe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/fleet-status/sweep-schema.ts
  - src/backend/fleet-status/sweep-schema.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "A single TypeScript module authoritatively declares the v1 JSONL schema shape the sweep script emits and the caller parses."
    - "Every RESEARCH.md-enumerated exec site (A1–A12, B1–B5) maps to a specific declared field or is annotated as intentionally-not-collected-this-phase (parity check)."
    - "Schema version is a compile-time constant importable by both sweep-caller code and regression tests."
    - "A parser function turns a raw JSONL blob into a typed per-identity + per-PID structure the caller can drive existing state composition off (no shape drift from today's per-identity fan-out output)."
  artifacts:
    - path: "src/backend/fleet-status/sweep-schema.ts"
      provides: "v1 JSONL line type, schema_version constant, parser function, parity map of exec-site → field"
      exports: ["SWEEP_SCHEMA_VERSION", "SweepIdentityLine", "SweepPidLine", "parseSweepJsonl", "isSweepLineOfCurrentSchema"]
    - path: "src/backend/fleet-status/sweep-schema.test.ts"
      provides: "unit tests for parser + schema-version detection + parity map assertion"
  key_links:
    - from: "sweep-schema.ts"
      to: "ssh-poll-orchestrator.ts (Plan 04 consumer)"
      via: "type export + parser function"
      pattern: "import.*sweep-schema"
    - from: "sweep-schema.ts"
      to: "substrate/scripts/fleet-status-sweep.py (Plan 02 producer)"
      via: "field-name and shape contract enforced by parser tests"
      pattern: "SWEEP_SCHEMA_VERSION"
---

<objective>
Lock the v1 JSONL schema for the fleet-status batch sweep. Emit a single TypeScript module the sweep script (Plan 02), the catalog wiring (Plan 03), the caller rewire (Plan 04), and the regression tests (Plan 05) all depend on. Also lock **open question #1 (schema shape)** authoritatively here so downstream plans have no room to drift.

Purpose: The sweep script's emission and the caller's parse are two ends of one wire. If those two definitions live in two files that can drift, the phase's parity guarantee (RESEARCH.md § "Everything else CONTEXT.md defers") breaks silently. Declaring the schema once in TypeScript with a parser + test suite anchors both ends and gives Plan 05 a fixture it can hand to MockSshChannel.

Output: `src/backend/fleet-status/sweep-schema.ts` (types + version constant + parser + parity map), `src/backend/fleet-status/sweep-schema.test.ts` (unit tests). No other files touched in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/92-fleet-status-poller-batch-sweep-one-exec-per-host-not-one-pe/92-CONTEXT.md
@.planning/phases/92-fleet-status-poller-batch-sweep-one-exec-per-host-not-one-pe/92-RESEARCH.md

@src/backend/fleet-status/ssh-poll-orchestrator.ts
@src/backend/fleet-status/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enumerate exec sites + lock v1 JSONL schema in sweep-schema.ts</name>
  <files>src/backend/fleet-status/sweep-schema.ts, src/backend/fleet-status/sweep-schema.test.ts</files>
  <behavior>
    - `SWEEP_SCHEMA_VERSION` exported constant equals `1`.
    - Two-tier line shape (chosen per open question #1 — see action below for rationale): parser accepts a JSONL blob containing zero or more `SweepIdentityLine` records followed by zero or more `SweepPidLine` records, joined by `identity` field. Rationale for two-tier over one-line-per-identity: source A is PID-keyed and source B is identity-keyed (RESEARCH.md G1); a one-line shape forces `null`-padding for the PID axis when an identity has no live claude process and forces array-nesting when one identity has multiple PIDs (rare but real — Alice's coordinator identities can run parallel claude sessions). Two-tier keeps each line flat, keeps the wire small, and matches the caller's own internal decomposition (`livenessMap` keyed by PID, `identityRecycleState` keyed by name). Every line carries `schema_version` and a `line_kind: "identity" | "pid"` discriminator.
    - `SweepIdentityLine` fields (source-B parity — B1, B2, B3, B4, B5 from RESEARCH.md):
        - `line_kind: "identity"`
        - `schema_version: 1`
        - `identity: string` (folder name under `~/.claude/identities/`)
        - `dormant: boolean` (B1 — `.dormant` sentinel present)
        - `recycled_at: boolean` (B2 — `.recycled-at` sentinel present)
        - `recycle_requested: boolean` (B3 — `.recycle-requested` sentinel present)
        - `jsonl_path: string | null` (B4 — Phase 32 discovery result, null when no matching JSONL exists)
        - `layer1_recycling: boolean | null` (B5 — tail-scan verdict; `null` means "tail unreadable this tick, caller preserves cached value" — matches today's fail-open contract)
    - `SweepPidLine` fields (source-A parity — A1, A2, A6, A7, A8, A9, A10, A12 from RESEARCH.md; A3/A4/A5/A11 handled by callers as noted below):
        - `line_kind: "pid"`
        - `schema_version: 1`
        - `identity: string` (join key back to `SweepIdentityLine.identity` — resolved server-side via tmux-helper on the sweep script, mirrors today's `resolvePidToTmuxSession` — replaces A4+A5 as a single server-side lookup)
        - `pid: number`
        - `session_json: string | null` (A1 — raw file contents; null means file missing or unreadable; caller runs existing `parseSessionJson` unchanged)
        - `stat_result: { ok: true; content: string } | { ok: false; reason: "enoent" | "transport" }` (A2 — mirrors existing `StatReadResult` discriminated union so caller can call `isStaleFromStat` unchanged)
        - `per_session_stop_mtime_ms: number | null` (A6 — `stat -c %Y ~/.claude/fleet-status/stop-<sid>.json`, converted to unix-ms server-side; null when file absent or `sessionId` fails the RESEARCH.md G6 safe-char regex `/^[a-zA-Z0-9_-]+$/`)
        - `activity_mtime_ms: number | null` (A7 — `stat -c %Y ~/.claude/fleet-status/hooks/<sid>/activity`, same guard as A6)
        - `stopped_mtime_ms: number | null` (A8 — `stat -c %Y ~/.claude/fleet-status/hooks/<sid>/stopped`, same guard)
        - `per_session_stop_payload: string | null` (A9 — raw file contents; caller runs existing `parseStopHookPayload` unchanged)
        - `dormant_a: boolean` (A10 — dormant sentinel probed via source-A tmuxSession-keyed path; equal to the corresponding `SweepIdentityLine.dormant` in the common case — RESEARCH.md G7 — but kept as a distinct field so the caller's dual-frame code path stays byte-for-byte unchanged in Plan 04)
        - `jsonl_tail: string | null` (A12 — up-to-256KB tail of the identity's active JSONL, null when path unknown or unreadable; caller runs existing `scanTailForLatestAiTitle` unchanged; **G3 disposition:** the tail is emitted raw for the ai-title scan because that scan is cheap and the JS predicate stays authoritative in the caller. The Layer 1 recycling scan is server-side per Plan 02 — its verdict lands in `SweepIdentityLine.layer1_recycling`. The 256KB tail transmitted per identity is bounded and acceptable for the ai-title scan alone — Plan 02 MAY compress or truncate further if wall-clock testing shows the sweep is slow.)
    - Fields **intentionally NOT collected in this phase's sweep** (parity documentation, not code — capture in the parity-map comment inline in sweep-schema.ts):
        - A3 (box-wide `last-stop-payload.json`): kept in the caller's legacy fallback path only. Per RESEARCH.md G9 the per-session A9 supersedes A3 in the current logic; A3 is a legacy fallback the batch path skips. Caller Plan 04 must confirm: if sweep is present but a PID's `per_session_stop_payload` is null, the caller does NOT fall back to A3 — it treats the PID as having no stop payload this tick (matches today's `perSessionUsable` branch when the per-session cat returned empty).
        - A11 (Phase 32 discovery for source-A tmuxSession→jsonl): folded server-side into the sweep. The sweep emits `SweepIdentityLine.jsonl_path`; source-A PIDs whose identity matches the `SweepIdentityLine.identity` reuse that same path. No separate per-PID discovery.
    - Parser: `parseSweepJsonl(raw: string): { identityLines: SweepIdentityLine[]; pidLines: SweepPidLine[]; unknownLines: number; schemaMismatch: boolean }`.
        - Split on `\n`, filter empty, `JSON.parse` each line; per-line try/catch — a single malformed line does NOT fail the batch (matches "prefer sweep, fall back to legacy" contract from RESEARCH.md § Backward-compat).
        - `schemaMismatch === true` if ANY line's `schema_version !== 1`. Caller (Plan 04) treats this as "fall back to legacy this cycle" per CONTEXT.md `## Decisions § Output schema`.
        - Discriminate lines by `line_kind`. Unknown `line_kind` values increment `unknownLines` counter (observability, not failure).
    - `isSweepLineOfCurrentSchema(obj: unknown): boolean` — narrow validator used by parser + tests.
    - Tests (in sweep-schema.test.ts):
        - Round-trip: a hand-crafted JSONL blob with one `SweepIdentityLine` + two `SweepPidLine`s parses into the expected typed structure.
        - Schema version mismatch: a blob where one line has `schema_version: 999` sets `schemaMismatch: true`.
        - Malformed line: a blob with a broken-JSON line + a good line yields the good line parsed and `unknownLines: 0` (broken JSON is silently skipped — mirrored on `unknownLines` behavior for the `line_kind` case).
        - `stat_result` discriminated-union shape survives round-trip.
        - Empty blob returns `{ identityLines: [], pidLines: [], unknownLines: 0, schemaMismatch: false }` — matches the "empty box, sweep exits 0 with empty stdout" case from RESEARCH.md § SSH constraints.
        - Every RESEARCH.md source-A / source-B enumeration row (A1–A12, B1–B5) has a corresponding assertion in a single test that reads the parity-map comment block and confirms the field is either declared on a line type or explicitly documented as skipped. Implementation: the parity map is a `const` object literal exported from sweep-schema.ts (`SWEEP_FIELD_PARITY: Record<"A1"|"A2"|...|"B5", { field: string | null; skipped_reason?: string }>`); the test walks all 17 keys and asserts every declared `field` value exists in the actual line-type keys, and every `skipped_reason` is a non-empty string.
  </behavior>
  <action>
    Create src/backend/fleet-status/sweep-schema.ts and sweep-schema.test.ts per the behavior block above.

    Open src/backend/fleet-status/ssh-poll-orchestrator.ts side-by-side and cross-reference RESEARCH.md's per-identity/per-host exec inventory tables (lines A0/A1–A12 and B0/B1–B5). For each exec site in RESEARCH.md, decide whether it becomes a schema field on `SweepIdentityLine` or `SweepPidLine`, or is documented as skipped in the parity map. The two enumeration drivers A0 (`ls ~/.claude/sessions/*.json`) and B0 (`find ~/.claude/identities/`) are handled by the sweep script's own enumeration loop server-side and are NOT emitted as fields — document them in the parity map as `{ field: null, skipped_reason: "server-side enumeration driver — no wire field needed" }`.

    Answer open question #1 (schema shape) as: **two-tier emission (SweepIdentityLine + SweepPidLine, joined by identity)**. Justification per D-01-like reasoning in the behavior block above. Write a 6–10 line comment at the top of sweep-schema.ts explaining the choice so future readers understand why the shape is not a single flat line.

    Add a `SCHEMA_VERSION_MISMATCH_FALLBACK` inline comment on the parser output block noting that Plan 04 wires the fallback branch and that this parser is deliberately lenient (never throws) because the caller — not the parser — owns the "fall back to legacy" policy per RESEARCH.md § Backward-compat.

    Do NOT modify ssh-poll-orchestrator.ts, types.ts, or any other file in this plan. Do NOT wire the parser into the caller — that's Plan 04. Do NOT create the sweep script — that's Plan 02.

    Run the new tests to confirm they pass before finishing.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; npx vitest run src/backend/fleet-status/sweep-schema.test.ts</automated>
  </verify>
  <done>
    sweep-schema.ts exports SWEEP_SCHEMA_VERSION=1, SweepIdentityLine, SweepPidLine, parseSweepJsonl, isSweepLineOfCurrentSchema, SWEEP_FIELD_PARITY. sweep-schema.test.ts covers round-trip, schema-mismatch, malformed-line, discriminated-union survival, empty blob, and full parity-map walk. `npx vitest run src/backend/fleet-status/sweep-schema.test.ts` passes. No changes to any other files.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/backend/fleet-status/sweep-schema.test.ts` passes.
- `grep -c "SWEEP_FIELD_PARITY" src/backend/fleet-status/sweep-schema.ts` returns at least 1 (parity map exists).
- Full test suite still green: `npx vitest run` reports zero regressions in unrelated modules (this plan adds files, mutates none).
</verification>

<success_criteria>
- The v1 JSONL wire contract is unambiguous and importable by Plans 02, 04, 05.
- Open question #1 (schema shape) has a locked answer (two-tier) with recorded rationale.
- Parser is lenient (never throws), fallback policy owned by the caller — matches RESEARCH.md backward-compat pattern.
- Every RESEARCH.md-enumerated exec site is mapped to a schema field or a documented skip reason.
</success_criteria>

<output>
Create `.planning/phases/92-fleet-status-poller-batch-sweep-one-exec-per-host-not-one-pe/92-01-SUMMARY.md` when done. Include: chosen schema shape (two-tier), final list of `SweepIdentityLine` fields, final list of `SweepPidLine` fields, list of skipped RESEARCH.md rows with reasons, test count, any unexpected findings.
</output>
