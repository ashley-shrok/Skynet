---
phase: 09-compose-box-redesign-2-tall-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/ComposeBox.tsx
autonomous: true
requirements:
  - COMPOSE-01
  - COMPOSE-02
  - COMPOSE-03
  - COMPOSE-04
  - COMPOSE-05
  - VISUAL-06
  - VISUAL-07
  - VISUAL-08
  - VISUAL-09
  - UPLOAD-04

must_haves:
  truths:
    - "At rest with an empty textarea, the ComposeBox renders as TWO stacked rows inside its warm-glass surround — a top instrument bar (~32px) and a bottom compose bar (~32px)"
    - "The top row contains, left-to-right: the meter well (with reset cell as its leftmost cell), a flex-1 spacer div, and the aux-button group (paperclip mobile-only, thumbs-up, hourglass)"
    - "The bottom row contains, left-to-right: the textarea (flex-1), and the Send button (icon-sm, vibrant amber, right side)"
    - "The Send button pins to the bottom edge of the textarea as the textarea auto-grows (items-end preserved on the bottom row)"
    - "When stagedAttachments.length > 0, the AttachmentChipStrip mounts as an ephemeral third row ABOVE the top instrument bar (Row 3 → Row 1 → Row 2 DOM order); when the array is empty, no chip-strip wrapper renders"
    - "Textarea auto-grows from 1 row minimum (down from 2) up to 6 rows maximum as the user enters newlines"
    - "On touch devices (proxied by showPaperclip === true), the top row's min-height is 44px; on desktop it is 32px"
    - "All send routing preserved verbatim: attachment path → onSendWithAttachments; text-only → onSend; COMPOSE-04 HARD LOCK — setText('') only on dispatched===true"
    - "All aux buttons + Send remain icon-sm (32×32); NO custom per-button sizing introduced"
    - "The retry-upload button and errorMessage div stay in-flow, in their existing positions relative to the new row order (chip strip DOM-first / retry button DOM-second — matching current source at ComposeBox.tsx L732/L741; error message below the compose bar)"
  artifacts:
    - path: "src/ui/features/pretty-view/ComposeBox.tsx"
      provides: "Restructured 2-row shell JSX layout with Row-3 chip strip, Row-1 instrument bar (meter + spacer + aux), Row-2 compose bar (textarea + send)"
      contains: "flex items-end gap-2"
  key_links:
    - from: "src/ui/features/pretty-view/ComposeBox.tsx (outer wrapper)"
      to: "Row 1 instrument bar wrapper"
      via: "sibling flex-col child; conditional showPaperclip drives min-h-[44px] vs min-h-8"
      pattern: "min-h-\\[44px\\]|min-h-8"
    - from: "src/ui/features/pretty-view/ComposeBox.tsx (Row 2)"
      to: "Send button + textarea wrapper"
      via: "same-row flex items-end so Send pins to textarea bottom"
      pattern: "flex items-end gap-2"
    - from: "src/ui/features/pretty-view/ComposeBox.tsx"
      to: "AttachmentChipStrip"
      via: "unchanged component import; only DOM position moves — chip strip is DOM-sibling of Row 1, appearing ABOVE Row 1 in flex-col order"
      pattern: "AttachmentChipStrip"
---

<objective>
Rearrange the ComposeBox JSX into a 2-tall shell: split the current single anchor `<div className="flex items-end gap-2">` at ComposeBox.tsx:770 into two stacked row anchors — Row 1 (instrument bar: meter+spacer+aux) and Row 2 (compose bar: textarea+send). Preserve every existing class value verbatim (per Ashley 2026-07-22: "we're not changing the style… this is about rearranging what we have and rotating some stuff"). Preserve every behavior end-to-end: draft persistence, queue armed pulse, thumbs-up 'yes' quick-send, chip strip mount rule, retry button, reset-send logic, COMPOSE-04 hard-lock.

**Meter well internals stay untouched in this plan** — Plan 09-02 handles the horizontal rotation. This plan ONLY moves the outer JSX shape and repositions the aux-button group, textarea wrapper, and Send button into two rows.

Purpose: Give Ashley ~1 button-height of vertical reading real-estate back and create a natural top-row seam for accumulating future buttons.

Output: A restructured `ComposeBox.tsx` whose render tree matches the 2-row shell contract in `09-UI-SPEC.md § Layout Contract`, with zero behavior regressions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-compose-box-redesign-2-tall-shell/09-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restructure ComposeBox JSX into 2-row shell (Row-3 chips, Row-1 instrument, Row-2 compose)</name>
  <files>src/ui/features/pretty-view/ComposeBox.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/ComposeBox.tsx (read end-to-end; the file IS the source of truth for every class value that must be preserved)
    - src/ui/features/pretty-view/ComposeBox.test.tsx (Tests 1, 2, 3, 4, 7, 8, 13, 15 — DOM contract these tests enforce: aria-labels /send message/, /attach file/, /retry upload/; testid `attachment-chip-strip`; strip precedes textarea in document order)
    - .planning/phases/09-compose-box-redesign-2-tall-shell/09-UI-SPEC.md § Layout Contract — 2-Row Shell (Row 1 / Row 2 / Row 3 spec) AND § Additional prototype-locked decisions (fixed-width meter with spacer, uniform icon-sm buttons)
    - src/ui/features/pretty-view/AttachmentChipStrip.tsx (returns null when list is empty — no wrapper conditional needed in ComposeBox around it)
  </read_first>
  <behavior>
    - Test 1 (existing): no chip strip when stagedAttachments empty — MUST still pass
    - Test 2 (existing): chip strip precedes textarea in document order — MUST still pass (Row 3 above Row 1+2)
    - Test 3+4 (existing): paperclip aria-label /attach file/ absent on desktop / present on touch — MUST still pass
    - Test 5 (existing): paperclip click opens hidden file input testid `compose-file-picker` — MUST still pass
    - Test 7 (existing): Send with attachments routes to onSendWithAttachments — MUST still pass
    - Test 8 (existing): Send enabled with attachments even if caption empty — MUST still pass
    - Test 13 (existing): Enter sends via onSend; Shift-Enter inserts newline — MUST still pass
    - Test 15 (existing): retry button (aria /retry upload/) appears only with errored chip — MUST still pass
    - New structural invariant (asserted via grep in verify): outer wrapper preserved verbatim; single `flex items-end gap-2` at L770 REPLACED by two sibling row anchors (`flex items-center gap-2 ...` and `flex items-end gap-2`)
    - New behavior: textarea `rows` prop uses `Math.max(1, ...)` instead of `Math.max(2, ...)` — auto-grow floor drops to 1
    - New behavior: on touch (showPaperclip===true) the top row wrapper carries `min-h-[44px]`; otherwise `min-h-8`
  </behavior>
  <action>
    Restructure the JSX inside the outer `<div className={cn("flex flex-col gap-1 px-2 py-2 shrink-0", …)}>` wrapper (ComposeBox.tsx:700-1165). Preserve the outer wrapper's className verbatim including the patch #82 `bg-[linear-gradient(180deg,rgba(28,30,40,0.55),rgba(18,20,28,0.62))]` and its `shadow-[inset_0_1px_0_rgba(220,225,245,0.06),_inset_0_2px_12px_rgba(0,0,0,0.4)]`. Preserve the outer `className={cn(..., className)}` merge and its comment block (do not delete the patch #79/#82 comments — they document why these class values must not change).

    Inside the outer wrapper, preserve in this exact top-to-bottom order:
    1. `<AttachmentChipStrip attachments={stagedAttachments ?? []} onRemove={onRemoveAttachment ?? (() => {})} />` (unchanged — component returns null when empty, so this becomes the ephemeral Row 3)
    2. The retry button block `{hasErroredChip && onRetryBatch && (…)}` UNCHANGED verbatim including its `px-1` wrapper, `size="xs" variant="outline"`, `RefreshCw className="size-3"`, aria-label "Retry upload", title "Retry failed upload", `className="gap-1 text-xs"`, and the `Retry upload` label text
    3. The hidden file input `<input ref={fileInputRef} type="file" multiple className="hidden" onChange={handleFileInputChange} data-testid="compose-file-picker" aria-hidden="true" tabIndex={-1} />` UNCHANGED verbatim (data-testid is load-bearing for Test 5)
    4. **NEW: Row 1 anchor** — replace nothing yet; this is a new sibling that sits between the hidden input and the current L770 anchor. Structure: `<div className={cn("flex items-center gap-2", showPaperclip ? "min-h-[44px]" : "min-h-8")}>` (per UI-SPEC L50 touch-target rule). Inside Row 1, in left-to-right order:
       a. The ENTIRE meter well block currently at ComposeBox.tsx:782-931 (the `<div>` with `role="meter"`, its inner segments container, the divider, the reset cell `<button>`). Move it verbatim — Plan 09-02 will rotate it. Class values on the meter well div and reset button remain UNCHANGED in this plan (Plan 09-02 mutates them).
       b. `<div className="flex-1" aria-hidden="true" />` — the future-buttons spacer (per UI-SPEC L47 "flex `<div>` spacer between the meter and the aux button group reserves that future room" and § Additional prototype-locked decisions #2).
       c. **Aux-button group** — a `<div className="flex flex-row gap-1">` wrapper (converted from the current `<div className="flex flex-col gap-1">` at L1033). Inside, in this exact order (least-used → most-used, matching the L→R rest of the row): Paperclip button (conditional on `showPaperclip`), ThumbsUp button, Hourglass (Queue) button. Move each of these three `<Button size="icon-sm" variant="outline" …>` blocks from L1042-1140 verbatim including EVERY class value on each — the aux button className expressions (`"rounded-md cursor-pointer"`, `"border-white/10"`, `"bg-[linear-gradient(180deg,rgba(70,66,58,0.5),rgba(38,34,28,0.6))]"`, `"text-[#e8e4d8]"`, `"shadow-[0_2px_4px_rgba(0,0,0,0.4),_inset_0_1px_0_rgba(255,240,210,0.12)]"`, all `hover:*` classes, all Queue armed-state classes including the `!` on `bg-[linear-gradient(180deg,hsla(38,55%,50%,0.9),hsla(38,60%,32%,0.95))]!` and `animate-pulse`) MUST be preserved verbatim — `!` suffixes are load-bearing per patch #81-fix and MUST NOT be removed. All aria-label / title / onClick / disabled bindings preserved verbatim.

       **Do NOT move Send into Row 1** — Send lives in Row 2 (see step 5). This is the "uniform buttons on top row + send" prototype decision (§ Additional prototype-locked decisions #3): the shared `icon-sm` size stays; only their placement changes.

    5. **NEW: Row 2 anchor** — replace the existing `<div className="flex items-end gap-2">` at L770 with the SAME `<div className="flex items-end gap-2">` — but this anchor now contains ONLY the textarea wrapper + Send button (the meter well and aux group have been moved to Row 1). Inside Row 2:
       a. The `<div className="relative flex-1 self-stretch">` textarea-wrapper block from L937-1023 UNCHANGED verbatim, including the `<Textarea ref={textareaRef} … />` with EVERY current class value preserved (especially `bg-[rgba(10,12,20,0.5)]!` — the trailing `!` is load-bearing per patch #81-fix) and the queue-armed pending overlay `{queueArmed && (<div className="absolute inset-0 …">…</div>)}` with its Hourglass icon and "Queued — waiting for idle" label.
       b. The Send `<Button size="icon-sm" onClick={handleSend} disabled={sendDisabled} aria-label="Send message" title="Send (Enter)" className={cn(…)}>` block from L1141-1159 UNCHANGED verbatim, including EVERY class value in the vibrant-amber cn() expression (VISUAL-08 HARD LOCK — `bg-[linear-gradient(180deg,hsla(38,90%,66%,0.92),hsla(38,90%,44%,0.94))]`, `shadow-[0_4px_12px_rgba(0,0,0,0.5),_inset_0_1px_0_rgba(255,235,190,0.5),_0_0_24px_hsla(38,90%,55%,0.42)]`, all `hover:*` and `disabled:*` classes) and its `<Send className="size-4" />` child.

    6. Delete the old aux-button column wrapper (the `<div className="flex flex-col gap-1">` at L1033) and its Send `<Button>` — those elements have moved into Row 1 (aux group, now flex-row) and Row 2 (Send) respectively. The comment blocks documenting patch #83 (RotateCcw moved OUT of the icon column) and patch #84 (queue button rationale) MAY be relocated inline with the moved buttons or trimmed; keep at least a one-line marker referencing patch #83 and patch #84 near the Row 1 aux group so future reads know where those patches landed.

    7. Keep the errorMessage div `{errorMessage && (<div className="text-xs text-destructive">{errorMessage}</div>)}` at L1162 UNCHANGED verbatim, mounted as the last child of the outer wrapper (after Row 2).

    8. Change the `rows` computation at L390 from `Math.min(6, Math.max(2, text.split("\n").length))` to `Math.min(6, Math.max(1, text.split("\n").length))`. Update the adjacent code-comment "Auto-grow rows: 2 minimum, 6 maximum" to say "1 minimum, 6 maximum" so the file reads consistently.

    Do NOT touch: the props interface (ComposeBoxProps), useState/useRef/useEffect blocks, event handlers (handleSend, handleResetSend, handleQuickSend, handleQueue, handleKeyDown, handleTextChange, handleBlur, handlePaste, handleOpenFilePicker, handleFileInputChange, fireQueuedDispatch, scheduleAutosave, flushDirty, clearDebounce, clearAfterSend, clearDrainTimers), the draft-persistence effect chain, the queue watchdog effect, the drain timer cleanup effect, the segment litCount computation (`litCount = contextPct != null ? Math.round((contextPct / 100) * SEG_COUNT) : 0`), or the `SEG_COUNT` constant declaration (Plan 09-02 changes SEG_COUNT to 12 and rewires segment iteration). The Row 3 chip strip renders via the existing `<AttachmentChipStrip>` invocation — do NOT wrap it in a conditional; the component's own return-null-when-empty behavior IS the mount rule (UPLOAD-04 preserved).

    Add or update the block comment above the outer `return` statement to document the new 2-row shell (naming Row 1 / Row 2 / Row 3 chip strip and citing UI-SPEC.md § Layout Contract). The old "textarea and send button share a single horizontal row" comment (L695-698) is stale — replace with a summary of the new structure.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && sudo npm test -- src/ui/features/pretty-view/ComposeBox.test.tsx --run 2>&1 | tail -40 &amp;&amp; grep -c 'flex items-end gap-2' src/ui/features/pretty-view/ComposeBox.tsx | grep -qE '^[12]$' &amp;&amp; grep -q 'flex items-center gap-2' src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; grep -q 'min-h-\[44px\]' src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; grep -q 'min-h-8' src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; grep -q 'Math.max(1, text.split' src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; grep -q 'flex-1" aria-hidden="true"' src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; grep -q 'flex flex-row gap-1' src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; grep -q 'bg-\[rgba(10,12,20,0.5)\]!' src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; grep -q 'bg-\[linear-gradient(180deg,rgba(28,30,40,0.55),rgba(18,20,28,0.62))\]' src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; grep -q 'bg-\[linear-gradient(180deg,hsla(38,90%,66%,0.92),hsla(38,90%,44%,0.94))\]' src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; grep -q 'bg-\[linear-gradient(180deg,hsla(38,55%,50%,0.9),hsla(38,60%,32%,0.95))\]!' src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; ! grep -qE 'Math\.max\(2,\s*text\.split' src/ui/features/pretty-view/ComposeBox.tsx</automated>
    <automated>cd /home/ubuntu/skynet &amp;&amp; sudo npm run typecheck 2>&amp;1 | tail -20 || sudo npx tsc --noEmit 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    All 10 pre-existing ComposeBox.test.tsx tests pass. The outer wrapper's warm-glass classes are preserved verbatim. The JSX contains exactly one `flex items-center gap-2` (Row 1) and one `flex items-end gap-2` (Row 2). Row 1 carries `min-h-[44px]` (touch branch) OR `min-h-8` (desktop branch) via the `showPaperclip` ternary. Row 1 contains, in order: the meter well `<div role="meter">`, the flex-1 spacer, and a `<div className="flex flex-row gap-1">` wrapping the aux buttons (paperclip conditional, ThumbsUp, Queue). Row 2 contains, in order: the `<div className="relative flex-1 self-stretch">` textarea wrapper, and the Send `<Button size="icon-sm">`. The `AttachmentChipStrip` mounts as a sibling ABOVE Row 1 (Test 2 preserves document order). Textarea `rows` uses `Math.max(1, …)`. No `!` suffix removed from any class value. Typecheck clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ashley's browser → ComposeBox | Existing keyboard + click handlers accept user input; no new input surface introduced |
| ComposeBox → onSend / onSendWithAttachments callbacks | Existing outbound callbacks; no new payload shape; identical serialization (D-50 newline collapse) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | ComposeBox layout refactor accidentally removes `!` from `bg-[rgba(10,12,20,0.5)]!` (Textarea) or `bg-[…]!` (Queue armed) | mitigate | grep verify gates in `<verify>` explicitly assert BOTH `!` suffixes survive verbatim; both classes have inline comments (patch #81-fix, patch #84) explaining why `!` is load-bearing |
| T-09-01-02 | Repudiation | COMPOSE-04 HARD LOCK regression — restructure introduces `setText("")` before dispatch confirmation, causing user's text to vanish on a failed send | mitigate | Task explicitly forbids touching event handlers; handleSend / handleQuickSend / handleResetSend / fireQueuedDispatch bodies unchanged; `setText("")` remains gated on `dispatched === true` |
| T-09-01-03 | Information Disclosure | Draft-persistence key `/compose-drafts` URL literal tree-shaken away if a bundler dead-code-eliminates the compose-drafts-api import | accept | Import stays because `putComposeDraft` / `getComposeDraft` / `flushComposeDraftKeepalive` are referenced by preserved effect handlers — no code path removed |
| T-09-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan; only source-file edit. No install step invoked. |

No HIGH severity threats. Pure client-side layout refactor with no new inputs, no new endpoints, no new stored data, no new auth surface. `security_block_on: high` — no block.
</threat_model>

<verification>
- All 10 tests in `src/ui/features/pretty-view/ComposeBox.test.tsx` pass unchanged
- Grep gates prove outer-wrapper classes, textarea `!`, Queue armed `!`, and Send amber gradient survived verbatim
- Grep gate proves `Math.max(2, text.split` no longer appears (only the new `Math.max(1, …)` form)
- `sudo npm run typecheck` clean (or `sudo npx tsc --noEmit` fallback)
- Visual verification in Skynet deferred to Plan 09-04 (UAT checklist)
</verification>

<success_criteria>
- ComposeBox renders as a 2-row shell (top instrument bar + bottom compose bar) with the AttachmentChipStrip mounting as an ephemeral Row 3 above the top row when at least one chip is present
- Every pre-existing behavior contract is preserved: draft persistence, Enter/Shift-Enter, split-send routing, COMPOSE-04 no-optimistic-bubble, queue armed-state, reset-send `/id reset` wrap, thumbs-up `yes` quick-send, retry-upload button, folder-drop refusal (still works because DropOverlay lives in PrettyView, unrelated to this refactor)
- Every locked class value in Ashley's preserve-do-not-relitigate list (patches #79, #82, #81-fix, VISUAL-08, #84, #57, #83) is byte-identical to the pre-refactor source
</success_criteria>

<output>
Create `.planning/phases/09-compose-box-redesign-2-tall-shell/09-01-SUMMARY.md` when done
</output>
