---
phase: 9
slug: compose-box-redesign-2-tall-shell
status: approved
shadcn_initialized: true
preset: radix-lyra / neutral / cssVariables
created: 2026-07-22
reviewed_at: 2026-07-22
---

# Phase 9 — UI Design Contract

> ComposeBox redesign: 2-tall shell with horizontal ctx meter.
> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn/ui |
| Preset | radix-lyra, baseColor neutral, cssVariables true (from components.json) |
| Component library | Radix UI (via shadcn) |
| Icon library | lucide-react |
| Font body (compose) | Inter Variable — `font-[Inter_Variable,ui-sans-serif,system-ui,sans-serif]` |
| Font mono (code) | JetBrains Mono Variable |

Source: `components.json` (shadcn initialized), `src/ui/index.css` (font imports), runtime context.

---

## Layout Contract — 2-Row Shell

This is the core geometric contract for Phase 9. All other sections refine it.

### Row 1 — Top Row (Instrument Bar)

```
┌──────────────────────────────────────────────────────────┐
│ [↺] [▓▓▓▓▓▓▓▓░░░░░░░░░░░░░░░░░]  [📎] [👍] [⏳]         │
│  reset cell ← horizontal ctx meter → aux buttons         │
└──────────────────────────────────────────────────────────┘
```

- **Reset cell**: leftmost cell of the meter well — same integrated-instrument semantic as patch #83. Native `<button>`, NOT shadcn Button. Same RotateCcw (size-3.5) icon. Rests as unlit-green; hover brightens to lit-green; drains right→left during drain-sweep.
- **Horizontal meter well**: fills the remaining width of top row between reset cell and aux buttons. Segments fill LEFT→RIGHT (lowest index = leftmost = lowest context %). Same color banding: green (low), amber (mid), red (high). `role="meter"` with `aria-valuenow`, `aria-valuemin=0`, `aria-valuemax=100`.
- **Aux buttons**: Paperclip (mobile-only, gated by `showPaperclip`), ThumbsUp (go-ahead quick-send), Hourglass (queue). Right-aligned. Same glass treatment as current (warm-neutral gradient, not amber — amber is reserved for Send only). Laid out as `flex-row gap-1`.
- **Row height**: 32px (2 × 16px = icon-sm button height). The whole top row must be visually compact — no taller than a single icon-sm button row.
- **Touch target**: On touch devices, the top row minimum touch-target height is 44px. Achieved via `min-h-[44px] flex items-center` on the top-row wrapper when `showPaperclip === true` (proxy for touch context).

### Row 2 — Bottom Row (Compose Bar)

```
┌──────────────────────────────────────────────────────────┐
│ [Textarea (flex-1, min 1 row, auto-grows) …   ] [Send ▸] │
└──────────────────────────────────────────────────────────┘
```

- **Textarea**: `flex-1`, `rows={Math.min(6, Math.max(1, text.split('\n').length))}`. Minimum is **1 row** (down from current 2-row minimum). Auto-grows on newline input up to 6 rows. Same recessed-well treatment (`bg-[rgba(10,12,20,0.5)]!`, `!` load-bearing per patch #81-fix).
- **Send button**: Right side, vertically aligned to bottom of textarea. Same vibrant-amber VISUAL-08 treatment (HARD LOCK). `size="icon-sm"`, same class list as current.
- **Row alignment**: `flex items-end gap-2` — Send button pins to bottom edge of textarea so it stays accessible when textarea grows.

### Row 3 — Chip Strip (Ephemeral, only when attachments staged)

```
┌──────────────────────────────────────────────────────────┐
│ [file.pdf ×] [photo.png ×] [doc.docx ×]                  │  ← pops when chips present
└──────────────────────────────────────────────────────────┘
```

- Mounts above Row 1 when `stagedAttachments.length > 0`. `AttachmentChipStrip` component unchanged (Phase 05 contract preserved).
- Retry-upload button stays in-flow immediately above the chip strip row (same as today — transient, no structural change from Phase 05).

### Row ordering in flex-col

```
[Retry button — conditional, transient]
[Row 3: Chip strip — conditional, ephemeral]
[Row 1: Instrument bar]
[Row 2: Compose bar]
[Error message — conditional]
```

Outer container: `flex flex-col gap-1 px-2 py-2 shrink-0` — same as current warm-glass surround (patches #79/#82 PRESERVED verbatim). No height change to the outer wrapper's padding or gap.

---

## Spacing Scale

Declared values (multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, segment gap in meter (2px between segments), cell padding |
| sm | 8px | Button group gaps, aux button column gap |
| md | 16px | Textarea horizontal padding (px-4), compose row gap |
| lg | 24px | Not used in ComposeBox |
| xl | 32px | Not used in ComposeBox |
| 2xl | 48px | Not used in ComposeBox |
| 3xl | 64px | Not used in ComposeBox |

Exceptions:
- Meter segment gap: 2px (sub-4 exception, matches patch #83's `gap-[2px]` — visual grain at this scale needs sub-4 precision).
- Meter cell inner padding: 3px (sub-4 exception, preserves patch #83's `p-[3px]` — empirically tuned so the segment stack + reset cell seat cleanly inside the ~28px-wide well; at 4px the well reads as too padded at this scale).
- Touch target minimum: 44px on the top row for touch devices (exception to icon-sm's natural 32px).

Source: existing ComposeBox.tsx (preserved values), touch target WCAG 2.5.5 Level AAA.

---

## Typography

| Role | Font | Size | Weight | Line Height |
|------|------|------|--------|-------------|
| Textarea body | Inter Variable | 14px (text-sm) | 400 | 1.5 (leading-relaxed) |
| Placeholder | Inter Variable | 14px | 400 | — |
| Queue-armed overlay label | Inter Variable | 14px (text-sm) | 400 | — |
| Error message | Inter Variable | 12px (text-xs) | 400 | 1.5 |

Two weights only: 400 (regular) for all compose surface text.
No heading typography in this phase (ComposeBox has no headings).

Source: current ComposeBox.tsx class lists, Inter Variable already imported in `src/ui/index.css`.

---

## Color

**Ratio (60/30/10):** ~60% compose surround (dominant/background gradient) + ~30% meter well + textarea well (secondary/recessed depth) + ~10% accent elements (the 7-item enumerated list below). Enforce by restricting accent to the enumerated list — no incidental accent bleed elsewhere in the compose surface.

**Primary focal point:** Send button. VISUAL-08 hard-lock designates it as the ONE compose attention grab-point — the only saturated warm-amber element in the compose surface at rest. All other elements MUST read as calmer (glass surround) or quieter (aux buttons in warm-neutral quiet treatment). Meter accent bands (green/amber/red) fill only when context warrants and pulse only during reset — they don't compete with Send at rest.

### Surface Colors

| Role | Value | Usage |
|------|-------|-------|
| Compose surround (dominant, ~60%) | `linear-gradient(180deg, rgba(28,30,40,0.55), rgba(18,20,28,0.62))` | Outer ComposeBox shell — patch #82 PRESERVED |
| Meter well (secondary, ~30%) | `rgba(10,12,20,0.6)` | Horizontal meter well background |
| Textarea well (recessed) | `rgba(10,12,20,0.5)!` | Textarea fill — `!` load-bearing per patch #81-fix |
| Queue overlay | `rgba(10,12,20,0.72)` with `backdrop-blur-[2px]` | Armed queue overlay on textarea |

### Accent Colors (10% — specific elements only)

Accent is reserved for EXACTLY these elements:

| Element | Color | Rationale |
|---------|-------|-----------|
| Send button | `hsla(38,90%,66%,0.92)` amber gradient | VISUAL-08 HARD LOCK — the ONE compose attention-grab-point, vibrant amber |
| Queue button (armed) | `hsla(38,55%,50%,0.9)` amber + `animate-pulse` | Patch #84 — transient status, visually distinct from Send's always-on amber |
| Meter segments lit (green) | `hsla(155,45%,52%,1)` gradient | Low context (< 45% band) |
| Meter segments lit (amber) | `hsla(38,75%,55%,1)` gradient | Mid context (45-77% band) |
| Meter segments lit (red) | `hsla(0,72%,55%,1)` gradient | High context (≥ 78% band) |
| Reset cell pulse | `hsla(155,45%,52%,1)` lit-green | During drain-sweep peak (isPulsing state) |
| Textarea focus ring | `rgba(220,225,245,0.2)` cool-cream outer glow | Patch #82 cool palette |

### Semantic Colors

| Role | Value | Usage |
|------|-------|-------|
| Destructive / error | `var(--color-destructive)` | Error message text below compose |
| Aux buttons (rest) | `linear-gradient(180deg,rgba(70,66,58,0.5),rgba(38,34,28,0.6))` | ThumbsUp, Hourglass (rest), Paperclip |
| Aux buttons (hover) | `linear-gradient(180deg,rgba(100,85,55,0.7),rgba(60,50,32,0.8))` | Hover glow on aux buttons |

Accent is NOT used on: the compose surround outer background, the queue overlay, the placeholder text, or any structural dividers.

Source: ComposeBox.tsx existing class lists (all preserved), runtime context VISUAL-08 hard-lock.

---

## Interaction Contract

### Drain-Sweep Animation (reoriented for horizontal)

**Verdict: KEEP the drain-sweep, reoriented right→left.**

Rationale: The L→R fill semantic means "leftmost = lowest, rightmost = highest." A drain (reset) empties from highest to lowest, i.e., rightmost to leftmost. This matches the fill direction's inverse and preserves the "watching the meter drain" reading.

Implementation:
- `isDraining === true` → all segments render dim.
- `transitionDelay` per segment: `(i) * 35ms` where `i=0` is leftmost (reset-adjacent, lowest). The rightmost segment (highest index) gets the LONGEST delay — it lights last and dims last, reading as the "last drop" draining. This is the inverse of the current vertical formula `(SEG_COUNT-1-i)*35ms` where i=0 was bottom (lowest).
- Wait: Vertical drain was TOP→BOTTOM (topmost = highest dims first). Translating: RIGHTMOST→LEFTMOST (highest dims first). So rightmost gets `0ms` delay, leftmost gets `(SEG_COUNT-1)*35ms`. Formula: `delay = (SEG_COUNT-1-i)*35ms` where `i=0` is LEFTMOST and `i=SEG_COUNT-1` is RIGHTMOST. This means: leftmost dims LAST (longest delay), rightmost dims FIRST. That's a LEFT→RIGHT sweep of dimming — rightmost dims first, leftmost last — which reads as "emptying from the full end back toward the reset cell." Correct semantic.
- Pulse timing unchanged: 420ms on, 770ms off, 800ms total.

### Segment Count — Prototype-First Decision

**Starting recommendation: SEG_COUNT = 16 for prototype, Ashley tunes live.**

Rationale: Horizontal space is much wider than the 28px-tall vertical well. At a 300px meter width, 16 segments = ~18.5px wide each with 2px gaps = visually readable. At wider windows, more segments would be fine. The prototype serves this as a CSS variable `--seg-count` so Ashley can bump it with DevTools without reloading. Lock the count after prototype review.

### Queue-Armed State

Unchanged from patch #84. When `queuedText !== null`:
- Textarea disabled, armed overlay mounts (Hourglass icon + "Queued — waiting for idle").
- Hourglass button turns amber + pulses.
- All send paths (Enter, Send button click, Queue button click) still function per patch #84 logic.

### Keyboard Contracts

- Enter → send (handleSend).
- Shift-Enter → newline in textarea (browser default, no prevention).
- AppShell chord Ctrl+Shift+O / Ctrl+Shift+L / Ctrl+Shift+; → NOT captured here (pass-through per COMPOSE-01 comment).
- No new keyboard contracts introduced.

### Send Routing

Unchanged from current:
1. If `hasAttachments && onSendWithAttachments` → attachment path (Phase 05).
2. Else → text-only path via `onSend(payload)`.
3. COMPOSE-04 HARD LOCK: NO optimistic bubble. Text cleared only on dispatch success.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Textarea placeholder | `Message {identityName || "Claude"}…` (identity-aware, patch #71) |
| Queue-armed overlay | `Queued — waiting for idle` (patch #84, PRESERVED) |
| Reset cell aria-label | `Send with /id reset prefix` (PRESERVED) |
| Send button aria-label | `Send message` / title `Send (Enter)` (PRESERVED) |
| Queue button aria-label (rest) | `Queue send for when session goes idle` (PRESERVED) |
| Queue button aria-label (armed) | `Cancel queued send` (PRESERVED) |
| Paperclip aria-label | `Attach file` (PRESERVED from Phase 05) |
| ThumbsUp aria-label | `Send 'yes'` (PRESERVED, patch #83) |
| Meter aria-label | `Context window` (PRESERVED from patch #83) |
| Error: disconnected on send | `Not connected — try again in a moment` (PRESERVED) |
| Error: queued send failed | `Not connected — queued send failed` (PRESERVED) |

No new copy elements introduced in Phase 9. All copy is preserved from prior patches.

Source: ComposeBox.tsx existing aria-label / title / placeholder attributes.

---

## Prototype-First Workflow

Per Ashley's fleet-standing preference (tina.md § Learned preferences, codified 2026-07-20):

**Phase 9 MUST produce a static HTML prototype before any React work.**

### Prototype location

```
/home/ubuntu/.claude/identities/tina/bounties/compose-box-redesign/prototype.html
```

Served via:
```bash
cd /home/ubuntu/.claude/identities/tina/bounties/compose-box-redesign && python3 -m http.server 8899
```

Ashley opens at: `http://100.99.149.8:8899/prototype.html` in a new browser tab (NOT embedded in Termix — mixed-content block applies).

### Prototype must exercise

1. 2-row shell at rest (top row: meter + aux buttons; bottom row: textarea + send).
2. Textarea expanding from 1 row to 6 rows as content grows.
3. Horizontal meter filling L→R at 0%, 25%, 50%, 75%, 85%, 100% — each clickable.
4. Drain-sweep animation on reset cell click — right→left dimming sweep, pulse peak at ~420-770ms.
5. Chip strip appearing as Row 3 when at least one mock chip is present.
6. Aux buttons (paperclip, thumbs-up, hourglass) rendering with glass treatment.
7. Queue-armed overlay on textarea.
8. SEG_COUNT as a CSS custom property — Ashley adjusts with DevTools.
9. Send button (amber glow) disabled state (opacity-40).
10. Mobile touch target check: simulate narrow viewport, top row stays ≥44px tall.

### Prototype iteration loop

1. Serve prototype on 8899.
2. Ashley opens in browser, pokes at it.
3. Tina edits prototype.html (fast — no build).
4. Ashley hard-refreshes, reviews.
5. Repeat until Ashley verbally locks the design ("this is it").
6. THEN and only then: translate locked design into React/TSX plan.

### What the prototype locks

- Exact `SEG_COUNT` for horizontal meter.
- Exact top-row height (whether 32px or 40px or something else feels better).
- Drain-sweep direction and timing (keep or drop).
- Whether reset cell needs a visual gap/divider from the meter body or reads cleanly as the leftmost cell.
- Whether aux buttons need labels or icons-only reads clearly at this width.

---

## Component Inventory

Components touched in Phase 9. No new shadcn components needed.

| Component | Change |
|-----------|--------|
| `ComposeBox.tsx` | Full layout restructure — top row + bottom row. Meter reoriented horizontal. |
| `AttachmentChipStrip.tsx` | No change — row 3 mount order change only in ComposeBox's JSX |
| `Textarea` (shadcn) | No change to component itself. Class list preserved: `bg-[rgba(10,12,20,0.5)]!` load-bearing |
| `Button` (shadcn) | No change to component. `variant="outline"` + `size="icon-sm"` preserved on aux buttons |

No third-party registry blocks. No new shadcn components installed.

---

## Locked Decisions (Do Not Re-Litigate)

These are pre-existing patch decisions that MUST survive Phase 9 unchanged:

| Decision | Patch | Contract |
|----------|-------|----------|
| Warm-glass compose surround | #79, #82 | `bg-[linear-gradient(180deg,rgba(28,30,40,0.55),rgba(18,20,28,0.62))]` + cool-cream inset rim. EXACT values preserved. |
| Textarea `!` important suffix | #81 | `bg-[rgba(10,12,20,0.5)]!` on Textarea — `!` is load-bearing (beats shadcn `dark:bg-input/30` 0-2-0 specificity). Do NOT remove. |
| Send button vibrant amber | VISUAL-08 | `hsla(38,90%,66%,0.92)` gradient. Never identity hue. Never changed. |
| Queue button armed amber pulse | #84 | `bg-[linear-gradient(180deg,hsla(38,55%,50%,0.9),hsla(38,60%,32%,0.95))]!` + `animate-pulse`. `!` load-bearing same reason as textarea. |
| Draft persistence | #57 | `/compose-drafts` URL literal must survive tree-shake. `putComposeDraft` / `getComposeDraft` / `flushComposeDraftKeepalive` call sites unchanged. |
| Phase 05 upload flow | Phase 05 | Chip strip (`AttachmentChipStrip`), retry button, `hasAttachments` routing in `handleSend`, `onSendWithAttachments` path. All preserved. |
| Reset-send `/id reset` wrap | #83 | `handleResetSend()` logic unchanged — text → `/id reset (text)` or `/id reset`. |
| COMPOSE-04 no optimistic bubble | COMPOSE-04 | `setText("")` only on `dispatched === true`. No local bubble. Never removed. |
| ThumbsUp quick-send payload | Phase 8 / patch #83 | `handleQuickSend("yes")`. ChatMessage.tsx recognition list includes "yes", "works for me", "good to go", "go ahead" — all recognized as ThumbsUp glyph. Do not change the payload. |

---

## Open Questions — Ashley Decides

These sub-questions from the bounty are surfaced to Ashley during prototype review, NOT blocking the prototype build:

| # | Question | Recommendation | Resolution |
|---|----------|----------------|------------|
| Q1 | Drain-sweep: right→left or drop? | KEEP, reorient right→left (highest fills dim first). Prototype-testable. | TBD via prototype |
| Q2 | Reset cell: integrated leftmost cell of horizontal well, or standalone button? | INTEGRATED (patch #83 one-instrument rationale preserved). | Integrated (specified above) |
| Q3 | Mobile touch targets: acceptable if top-row height is 44px on touch devices? | YES — use `min-h-[44px]` on top-row wrapper gated by `showPaperclip` (touch proxy). | Specified above |
| Q4 | Retry-upload button / error message: in-flow above/below (unchanged) or fold in? | UNCHANGED — both are transient and belong in-flow per Ashley's explicit statement in bounty. | Unchanged (specified above) |
| Q5 | Segment count: 16 for prototype, Ashley tunes live? | Yes — 16 start, `--seg-count` CSS var for live tuning. | TBD via prototype |

Q1 and Q5 require prototype-round decision from Ashley before finalizing React implementation. Q2/Q3/Q4 are resolved in this spec as recommendations.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Button, Textarea (existing, already installed) | not required |
| third-party | none | not applicable |

No new shadcn blocks or third-party registry components are introduced in Phase 9.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
