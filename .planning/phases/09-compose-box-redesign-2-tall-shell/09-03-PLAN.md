---
phase: 09-compose-box-redesign-2-tall-shell
plan: 03
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - src/ui/features/pretty-view/ComposeBox.test.tsx
autonomous: true
requirements:
  - COMPOSE-01
  - COMPOSE-02
  - COMPOSE-03
  - COMPOSE-04
  - COMPOSE-05
  - VISUAL-09

must_haves:
  truths:
    - "The full existing ComposeBox test suite (all 10 pre-existing tests: Tests 1, 2, 3, 4, 5, 6, 7, 8, 13, 15) passes after Plans 09-01 + 09-02 land"
    - "At least ONE new test asserts the 2-row structural contract: within the ComposeBox DOM, the aux-button group (identified by an aux button aria-label like /send 'yes'/) appears in an ancestor row that is a DOM sibling PRECEDING the row containing the Send button"
    - "At least ONE new test asserts the meter well is now horizontal by checking `role='meter'` renders as expected AND its className contains `flex-row` (not `flex-col`)"
    - "At least ONE new test asserts that when `showPaperclip=true`, the top row's wrapper has `min-h-[44px]` class present (touch target); when `showPaperclip=false`, `min-h-8` is present instead"
    - "At least ONE new test asserts textarea `rows` starts at 1 when text is empty (down from prior floor of 2)"
    - "Overall project test suite passes (472+ tests green per orchestrator runtime_context)"
  artifacts:
    - path: "src/ui/features/pretty-view/ComposeBox.test.tsx"
      provides: "Structural tests locking the 2-row shell, horizontal meter, touch target, and textarea 1-row-minimum behaviors"
      contains: "Phase 9"
  key_links:
    - from: "ComposeBox.test.tsx"
      to: "ComposeBox.tsx layout invariants"
      via: "getByRole('meter') + className inspection + document position comparison"
      pattern: "compareDocumentPosition|role: \"meter\"|min-h-\\[44px\\]"
---

<objective>
Extend `ComposeBox.test.tsx` with structural tests that lock in the Phase 9 2-row shell + horizontal meter + 1-row-minimum textarea contracts. Do not modify or delete any of the 10 pre-existing tests — they are already Phase-9-compatible (they gate on aria-labels, test-ids, and behavior, not on layout DOM structure, per the file inspection during planning).

Purpose: Prevent silent regression of the Phase 9 rearrangement in future refactors. Without these tests, someone could accidentally rotate the meter back to flex-col, or restore the 2-row textarea floor, and no automated signal would catch it.

Output: New `describe("ComposeBox — Phase 9 layout")` block appended to the test file with at least 4 new tests locking the 2-row shell, horizontal meter, touch target, and 1-row textarea floor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-compose-box-redesign-2-tall-shell/09-UI-SPEC.md
@.planning/phases/09-compose-box-redesign-2-tall-shell/09-01-SUMMARY.md
@.planning/phases/09-compose-box-redesign-2-tall-shell/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 9 structural tests to ComposeBox.test.tsx</name>
  <files>src/ui/features/pretty-view/ComposeBox.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/ComposeBox.test.tsx (existing test scaffolding: imports, `baseProps` helper, `mkAtt` helper, describe block, test patterns using `screen.getByLabelText` + `compareDocumentPosition` + `getByPlaceholderText`)
    - src/ui/features/pretty-view/ComposeBox.tsx (post-09-01+09-02 state — this is what the tests will exercise)
    - .planning/phases/09-compose-box-redesign-2-tall-shell/09-UI-SPEC.md § Layout Contract (the invariants being locked in)
  </read_first>
  <behavior>
    - New Test A ("Phase 9 Layout: aux button group renders in a row that precedes the Send button's row"): render ComposeBox with default (desktop) props. Get the ThumbsUp button via `getByLabelText(/send 'yes'/i)`. Get the Send button via `getByLabelText(/send message/i)`. Walk each up to its nearest ancestor that carries a flex class (heuristic: closest ancestor with `className` matching `/flex items-(center|end) gap-2/`). Assert the two ancestors are different AND that `thumbsUpAncestor.compareDocumentPosition(sendAncestor) & Node.DOCUMENT_POSITION_FOLLOWING` is truthy (Send's row comes after ThumbsUp's row).
    - New Test B ("Phase 9 Layout: meter is horizontal — role='meter' present with flex-row"): render ComposeBox with `contextPct={50}`. Get the meter via `getByRole('meter')`. Assert its `className` string includes the substring `flex-row` AND does NOT include the substring `flex-col`.
    - New Test C ("Phase 9 Layout: mobile touch target — top row carries min-h-[44px] when showPaperclip=true"): render ComposeBox with `showPaperclip: true`. Walk from the paperclip button (getByLabelText(/attach file/i)) up to its closest ancestor with `flex items-center gap-2` class. Assert that ancestor's className contains `min-h-[44px]` AND does NOT contain `min-h-8` (only one wins per the ternary).
    - New Test D ("Phase 9 Layout: desktop top row carries min-h-8 when showPaperclip=false"): render ComposeBox with `showPaperclip: false`. Since paperclip isn't rendered on desktop, walk from the ThumbsUp button up to its closest ancestor with `flex items-center gap-2`. Assert that ancestor's className contains `min-h-8` AND does NOT contain `min-h-[44px]`.
    - New Test E ("Phase 9 Layout: textarea rows starts at 1 with empty text"): render ComposeBox with default props (empty text). Get the textarea via `getByPlaceholderText(/message/i) as HTMLTextAreaElement`. Assert `textarea.rows === 1`.
    - All 10 pre-existing tests continue to pass unchanged.
  </behavior>
  <action>
    Append a new `describe("ComposeBox — Phase 9 layout", () => { … })` block to the END of `ComposeBox.test.tsx` (after the existing Phase 05 describe block). Reuse the existing `baseProps` and `mkAtt` helpers from the outer scope (they're defined at module top-level in the current file; if not, hoist them or duplicate the minimal `baseProps` inside the new describe — read the file first to determine).

    Implement Tests A through E per the behavior list above. Helper function `closestFlexRowAncestor(el: Element, pattern: RegExp): Element | null` — walk `el.parentElement` upward until a parent's `className` string matches the pattern, or null if root. Define this helper at the top of the new describe block.

    For Test A: use pattern `/flex items-(?:center|end) gap-2/` because both Row 1 (`flex items-center gap-2 …`) and Row 2 (`flex items-end gap-2`) match — the walker will find whichever is closer. For each button, `closestFlexRowAncestor` returns the row anchor. Assert the two are distinct nodes and Row 1 precedes Row 2 via `Node.DOCUMENT_POSITION_FOLLOWING` (0x04) bit mask.

    For Test B: `role='meter'` is exposed by the meter well div via `getByRole('meter')`. Assert `meter.className.includes('flex-row')` and `!meter.className.includes('flex-col')`.

    For Test C + D: the top-row anchor is Row 1, which has className `flex items-center gap-2 …` plus `min-h-[44px]` OR `min-h-8`. Use pattern `/flex items-center gap-2/` in `closestFlexRowAncestor` so it lands on Row 1 specifically (not Row 2 which is `items-end`).

    For Test E: read the `rows` attribute directly from the HTMLTextAreaElement. `textarea.rows === 1` proves the auto-grow floor dropped from 2 to 1 (Plan 09-01 change).

    Do NOT modify or delete any existing test. Do NOT modify `baseProps` or `mkAtt` in ways that would change what the existing tests receive. If `baseProps` needs a `contextPct` field for Test B and doesn't have one, either pass `contextPct` directly at the render site (Test B's own render call) OR extend `baseProps` in a backward-compatible way (new prop with default `undefined`).

    Preserve the file's import shape at the top (all existing imports from vitest, testing-library, StagedAttachmentLike type import from AttachmentChipStrip, ComposeBox + ComposeBoxProps import). If new imports are needed (e.g., `within`), add them to the existing testing-library import line.
  </action>
  <verify>
    <automated>cd /home/ubuntu/termix &amp;&amp; sudo npm test -- src/ui/features/pretty-view/ComposeBox.test.tsx --run 2>&1 | tail -50 &amp;&amp; grep -q 'Phase 9 layout' src/ui/features/pretty-view/ComposeBox.test.tsx &amp;&amp; grep -c '  it(' src/ui/features/pretty-view/ComposeBox.test.tsx | awk '{ if ($1 &gt;= 14) exit 0; else exit 1 }'</automated>
    <automated>cd /home/ubuntu/termix &amp;&amp; sudo npm test --run 2>&1 | tail -30</automated>
    <automated>cd /home/ubuntu/termix &amp;&amp; sudo npm run typecheck 2>&amp;1 | tail -20 || sudo npx tsc --noEmit 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    Test file contains a `describe("ComposeBox — Phase 9 layout")` block with at least 5 new `it(...)` tests (A-E), for a total of ≥14 `it(` occurrences in the file (10 pre-existing + 5 new; a bare-minimum threshold of 14 leaves room for the executor to add a bonus test if desired). All 14+ tests pass. The full project test suite (`sudo npm test --run`) is green. Typecheck clean. Zero pre-existing tests deleted or modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite → ComposeBox source | Read-only assertions via testing-library; no runtime security surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Repudiation | Structural tests become brittle if they gate on transient class values that Ashley might later adjust (e.g., asserting a specific gradient string) | mitigate | Tests gate ONLY on structural invariants specified in UI-SPEC's Layout Contract (row order, flex direction, min-h class presence, textarea rows number). No gradient / color / shadow class value is asserted — those are Ashley's aesthetic prerogative and were never part of the Phase 9 contract |
| T-09-03-02 | Tampering | Test-file edit accidentally removes or breaks a pre-existing Phase 05 test | mitigate | Task explicitly forbids modifying or deleting any existing test; verify gate counts `it(` occurrences ≥14 (10 pre-existing must survive; 5 new added; total ≥14 with slack); full test suite must pass |
| T-09-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan; only test-file edit. |

No HIGH severity threats. Test-only additions. `security_block_on: high` — no block.
</threat_model>

<verification>
- New describe block "ComposeBox — Phase 9 layout" exists in test file
- Test file has ≥14 `it(...)` invocations (10 pre-existing + 5 new)
- All tests pass: `sudo npm test -- src/ui/features/pretty-view/ComposeBox.test.tsx --run`
- Full project test suite green: `sudo npm test --run`
- Typecheck clean
</verification>

<success_criteria>
- Regression will fire if the 2-row shell is collapsed back to a single row
- Regression will fire if the meter is rotated back to flex-col
- Regression will fire if the mobile touch target is removed
- Regression will fire if the textarea auto-grow floor is restored to 2
- All pre-existing Phase 05 tests remain green
</success_criteria>

<output>
Create `.planning/phases/09-compose-box-redesign-2-tall-shell/09-03-SUMMARY.md` when done
</output>
