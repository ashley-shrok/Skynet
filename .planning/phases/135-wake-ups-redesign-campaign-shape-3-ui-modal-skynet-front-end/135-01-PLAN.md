---
phase: 129-wake-ups-redesign-campaign-shape-3-ui-modal-skynet-front-end
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/api/wakeups-api.ts
  - src/ui/features/pretty-conversations/WakeupsModal.tsx
  - src/ui/features/pretty-conversations/WakeupsModalRow.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
  - D-15
  - D-16
  - D-17
  - D-18
  - D-26
  - D-27
  - D-28

must_haves:
  truths:
    - "New AlarmClock header button renders in PrettyConversationsPanel between the Globe button and the More/kebab, gated by the showPencilButton guard (D-04)."
    - "Clicking the header button opens a WakeupsModal shell using ConversationSearchModal's glass-morphism chrome at 640x720 desktop / inset-4 mobile (D-06)."
    - "Modal fetches GET /wakeups on every open and renders the resulting rows in the list view (D-03, D-08)."
    - "Filter state (search / role / host) exists and narrows the visible rows client-side; state resets on modal close (D-10, D-17)."
    - "Row click (excluding the toggle and kebab hit-boxes) fires an edit intent that the modal exposes via internal state (D-11, D-27) — wave 1 wires the state machine; wave 2 renders the form."
    - "Loading state = three stacked Skeleton bars (matches shipped WakeupsTab / RoleFileTab pattern per Research Pitfall #1)."
    - "Empty state = centered dim helper text ('No wake-ups on any host. Click + to create one.' / 'No wake-ups match this filter.') (D-16)."
    - "Footer shows a live count of visible rows and how many are enabled (D-18)."
  artifacts:
    - path: "src/ui/api/wakeups-api.ts"
      provides: "5 API helpers (listWakeups, createWakeup, updateWakeup, toggleWakeupEnabled, deleteWakeup) + WakeupListItem + GlobalWakeupSpecWire wire types"
      exports:
        - "listWakeups"
        - "createWakeup"
        - "updateWakeup"
        - "toggleWakeupEnabled"
        - "deleteWakeup"
        - "WakeupListItem"
        - "GlobalWakeupSpecWire"
    - path: "src/ui/features/pretty-conversations/WakeupsModal.tsx"
      provides: "Radix Dialog shell + list view (rows, filter bar, footer, loading skeletons, empty state) — form view stubbed for wave 2"
      contains: "export function WakeupsModal"
    - path: "src/ui/features/pretty-conversations/WakeupsModalRow.tsx"
      provides: "Row subcomponent: name headline, metadata line (schedule + host chip), prompt body, chip row, toggle+kebab actions; onRowClick + stopPropagation on toggle/kebab"
      contains: "export function WakeupsModalRow"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "AlarmClock header button + wakeupsModalOpen state + <WakeupsModal> mount inside the guarded showPencilButton fragment"
      contains: "pv-header-wakeups-button"
  key_links:
    - from: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      to: "src/ui/features/pretty-conversations/WakeupsModal.tsx"
      via: "import { WakeupsModal } and controlled open/onOpenChange props + hostTree prop"
      pattern: "WakeupsModal.*open=\\{wakeupsModalOpen\\}"
    - from: "src/ui/features/pretty-conversations/WakeupsModal.tsx"
      to: "src/ui/api/wakeups-api.ts"
      via: "import { listWakeups } and useEffect fetch on open"
      pattern: "listWakeups\\(\\)"
    - from: "src/ui/api/wakeups-api.ts"
      to: "GET /wakeups"
      via: "authApi.get('/wakeups') returning { items: WakeupListItem[] }"
      pattern: "authApi\\.get\\(\"/wakeups\"\\)"
---

<objective>
Deliver the read path of the wake-ups modal: the wakeups-api.ts helper file (5 REST helpers + 2 wire types), the WakeupsModal shell with a fully functional list view (filter bar / rows / skeleton loading / empty state / footer count), the WakeupsModalRow subcomponent, and the new AlarmClock header button + modal mount in PrettyConversationsPanel.tsx.

Purpose: Wave 1 establishes the end-to-end read surface — one click of the new header button opens a modal that fan-outs across the fleet via shape 2's REST endpoints, renders every wake-up with filters, and closes cleanly. This proves the wire contract, the chrome recipe, the hostTree threading, the fetch-on-open + reset-on-close discipline (D-03, D-17), and the state machine that wave 2 will layer the form view onto. Zero mutation paths in wave 1 — no create, no edit, no toggle, no delete — those are wave 2.

Output: A browsable, filterable, fleet-wide list of wake-ups reachable from the conversation-list header, with all D-XX list-view decisions honored. Wave 2 fills in the form + write paths on top of this foundation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/135-wake-ups-redesign-campaign-shape-3-ui-modal-skynet-front-end/135-CONTEXT.md
@.planning/phases/135-wake-ups-redesign-campaign-shape-3-ui-modal-skynet-front-end/135-RESEARCH.md
@.planning/phases/135-wake-ups-redesign-campaign-shape-3-ui-modal-skynet-front-end/135-PATTERNS.md
@.planning/campaigns/wake-ups-redesign/shape-wake-ups-modal.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/ui/api/wakeups-api.ts with 5 REST helpers and 2 wire types</name>
  <files>src/ui/api/wakeups-api.ts</files>
  <read_first>
    - src/ui/api/identities-api.ts (analog per PATTERNS.md — read lines 1 for the authApi import, lines 299-328 for listRolesForHost + RoleSummary export pattern, and any surrounding POST/PATCH helpers)
    - src/ui/api/claude-session-api.ts lines 700-780 (the retired per-identity WakeupSpecWire — read ONLY to confirm the do-not-reuse rationale from RESEARCH Pitfall #2; do NOT modify)
    - src/backend/database/routes/wakeups-list.ts lines 75-86 (source-of-truth for the WakeupListItem row shape)
    - src/backend/database/routes/wakeups-write.ts lines 136-152 (source-of-truth for GlobalWakeupSpec) and lines 522-529, 643-822, 840 (PATCH name-slug gate, toggle-enabled semantics, DELETE-with-body)
    - src/backend/database/database.ts lines 2036-2037 (confirms the bare /wakeups prefix — no /api/ prefix)
  </read_first>
  <action>
    Create a new file src/ui/api/wakeups-api.ts. Import authApi + handleApiError from @/main-axios (verbatim shape from identities-api.ts:1). Export two wire types: `WakeupListItem` (matches wakeups-list.ts:75-86 — slug, host, hostId, name, enabled, schedule as `unknown`, scheduleHuman, prompt, roles, skills) and `GlobalWakeupSpecWire` (matches wakeups-write.ts:136-152 — name, optional enabled, prompt, schedule as `Record<string, unknown>`, optional roles and skills). Export five async helpers with the exact signatures listed in PATTERNS.md § wakeups-api.ts:

    - `listWakeups(): Promise<WakeupListItem[]>` — GET /wakeups, unwrap `(response.data as { items: WakeupListItem[] }).items`.
    - `createWakeup(host: number, spec: GlobalWakeupSpecWire)` — POST /wakeups with body `{ host, spec }`, return `{ slug, host, spec }`.
    - `updateWakeup(slug: string, host: number, spec: GlobalWakeupSpecWire)` — PATCH `/wakeups/${encodeURIComponent(slug)}` with body `{ host, spec }`, return `{ slug, host, spec }`.
    - `toggleWakeupEnabled(slug: string, host: number, enabled: boolean)` — PATCH `/wakeups/${encodeURIComponent(slug)}/toggle-enabled` with body `{ host, enabled }`, return `{ slug, host, enabled }`.
    - `deleteWakeup(slug: string, host: number): Promise<void>` — MUST use `authApi.delete(url, { data: { host } })` per RESEARCH Pitfall #4 (axios `data` config key attaches a body to DELETE). Do NOT use query params.

    Each helper wraps its call in try/catch and delegates to `handleApiError(error, "<verb> wake-up")` in the catch clause so `ApiError.status` propagates to the caller for banner mapping.

    Anti-patterns (from PATTERNS.md § Anti-Patterns): do NOT import or re-export WakeupSpecWire from claude-session-api.ts (has `instruction`, not `prompt`); do NOT add these helpers to claude-session-api.ts (that file is scoped to WS/legacy per-identity); do NOT compute the slug client-side (server derives from spec.name); do NOT use query params for DELETE.
  </action>
  <verify>
    <automated>test -f src/ui/api/wakeups-api.ts && grep -q "export async function listWakeups" src/ui/api/wakeups-api.ts && grep -q "export async function createWakeup" src/ui/api/wakeups-api.ts && grep -q "export async function updateWakeup" src/ui/api/wakeups-api.ts && grep -q "export async function toggleWakeupEnabled" src/ui/api/wakeups-api.ts && grep -q "export async function deleteWakeup" src/ui/api/wakeups-api.ts && grep -q "export type WakeupListItem" src/ui/api/wakeups-api.ts && grep -q "export type GlobalWakeupSpecWire" src/ui/api/wakeups-api.ts && grep -q "authApi.delete.*{ data: { host } }" src/ui/api/wakeups-api.ts && ! grep -q "instruction" src/ui/api/wakeups-api.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -q "export type WakeupListItem" src/ui/api/wakeups-api.ts` exits 0.
    - Source assertion: `grep -q "export type GlobalWakeupSpecWire" src/ui/api/wakeups-api.ts` exits 0.
    - Source assertion: all 5 helper function names present under `export async function` prefix.
    - Source assertion: `grep -q "authApi.delete" src/ui/api/wakeups-api.ts` matches with `{ data: { host } }` on the same or next line (DELETE-with-body pattern).
    - Source assertion: file does NOT contain the string `instruction` (guards against copy-paste of the retired WakeupSpecWire shape).
    - Source assertion: `grep -q "encodeURIComponent(slug)" src/ui/api/wakeups-api.ts` exits 0 for PATCH and DELETE URLs.
    - Source assertion: no import from `@/api/claude-session-api` in this file.
    - Type check: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    - File src/ui/api/wakeups-api.ts exists with 5 exported async helpers + 2 exported wire types, matching wakeups-list.ts + wakeups-write.ts server shapes.
    - handleApiError delegation in every catch so ApiError.status propagates for downstream banner mapping.
    - DELETE uses body via axios `data` config key, not query params (RESEARCH Pitfall #4 mitigated).
    - No re-use of retired WakeupSpecWire (RESEARCH Pitfall #2 mitigated).
    - `npx tsc --noEmit` and `npx vitest related --run src/ui/api/wakeups-api.ts` both exit 0 (the latter may produce a "no tests found" trivial pass — that's fine; the file has no test file yet and vitest treats it as pass; wave 2 adds tests).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create WakeupsModalRow subcomponent for list-row rendering with click + stopPropagation semantics</name>
  <files>src/ui/features/pretty-conversations/WakeupsModalRow.tsx</files>
  <read_first>
    - src/ui/features/pretty-conversations/ConversationSearchModal.tsx (chrome analog — read the full file, especially rows-scroll region ~lines 310-345 and the row style tokens)
    - src/ui/features/pretty-view/WakeupsTab.tsx (per-identity mount — read ONLY for the field-shape reference at lines 130-170; do NOT modify)
    - src/ui/api/wakeups-api.ts (the WakeupListItem type from Task 1)
    - .planning/phases/135-wake-ups-redesign-campaign-shape-3-ui-modal-skynet-front-end/135-PATTERNS.md § Row click + stopPropagation pattern (lines 418-446)
    - .planning/phases/135-wake-ups-redesign-campaign-shape-3-ui-modal-skynet-front-end/135-RESEARCH.md § Chrome Token Dictionary (lines 755-785 — magenta role chip, skill chip blue, host chip subtle grey-blue, muted body text, dim helper text)
    - ~/fleet/roles/box-maintainer/bounties/wake-ups-redesign/prototype.html sections A/B (row layout, chip colors) — the settled visual reference
  </read_first>
  <action>
    Create src/ui/features/pretty-conversations/WakeupsModalRow.tsx. Export a `WakeupsModalRow` functional component with props: `row: WakeupListItem`, `onRowClick: (row: WakeupListItem) => void`, `onToggleClick: (row: WakeupListItem) => void`, `onKebabClick: (row: WakeupListItem) => void`, `kebabOpen: boolean`, `onEditFromKebab: (row: WakeupListItem) => void`, `onDeleteFromKebab: (row: WakeupListItem) => void`. Import WakeupListItem from @/api/wakeups-api; import MoreHorizontal from lucide-react.

    Row structure (per D-08, prototype § B, RESEARCH § Chrome Token Dictionary):
    - Outer `<div>` with `role="button"`, `tabIndex={0}`, `data-testid={\`wakeups-modal-row-${row.slug}\`}`, `onClick={() => onRowClick(row)}`, keyboard handler for Enter/Space to invoke onRowClick, `cursor: pointer`, hover-bg `rgba(255,255,255,0.04)`, hover-border `rgba(220,225,245,0.08)`.
    - Line 1: name headline as bold `<div>{row.name}</div>` — text color `#f0ebe0`.
    - Line 2: metadata `<div>` containing (in order) `<span>{row.scheduleHuman}</span>` (dim `var(--color-pv-fg-dim)`), a subtle host chip `<span className="wakeups-host-chip" data-testid={\`wakeups-modal-row-${row.slug}-host\`}>{row.host}</span>` with tokens bg `hsla(220, 25%, 55%, 0.12)` / border `hsla(220, 25%, 60%, 0.24)` / text `hsla(220, 30%, 78%, 1)` (D-09).
    - Line 3: prompt body as `<div>{row.prompt}</div>` with 2-line clamp (`display: -webkit-box; -webkit-box-orient: vertical; -webkit-line-clamp: 2; overflow: hidden;`), muted color `var(--color-pv-fg-muted)`.
    - Line 4: chip row rendering `row.roles.map` as magenta chips (bg `hsla(285, 55%, 55%, 0.14)` / border `hsla(285, 55%, 60%, 0.30)` / text `hsla(285, 60%, 82%, 1)`) and `row.skills.map` as blue chips (bg `hsla(200, 55%, 55%, 0.12)` / border `hsla(200, 55%, 60%, 0.28)` / text `hsla(200, 60%, 82%, 1)`) — skills chip row renders only when `row.skills.length > 0` (skills chips appear only when hand-edited on disk per D-08).
    - Right-side actions column: an enable toggle button `data-testid={\`wakeups-modal-row-${row.slug}-toggle\`}` with `onClick={(e) => { e.stopPropagation(); onToggleClick(row); }}` (D-11) showing `row.enabled` state via a two-state visual (planner picks — a small pill/switch is fine; do NOT use spinners per D-28); and a kebab button `data-testid={\`wakeups-modal-row-${row.slug}-kebab\`}` rendering `<MoreHorizontal size={16} />` with `onClick={(e) => { e.stopPropagation(); onKebabClick(row); }}` (D-11). When `kebabOpen` is true render a small popover with two buttons: Edit (`data-testid={\`wakeups-modal-row-${row.slug}-edit\`}` calling onEditFromKebab) + Delete (`data-testid={\`wakeups-modal-row-${row.slug}-delete\`}` calling onDeleteFromKebab). Each popover button's onClick also stopPropagations (D-11).

    Do NOT put any API calls, useEffect, or state-mutation logic in this file — it is pure presentation. All handlers are prop callbacks so WakeupsModal owns the state machine and can be tested in isolation.

    Do NOT include a spinner on the toggle (D-28: no streaming affordances). The pessimistic-toggle semantics (D-12) live in the parent — the row just reflects `row.enabled` truthfully.
  </action>
  <verify>
    <automated>test -f src/ui/features/pretty-conversations/WakeupsModalRow.tsx && grep -q "export function WakeupsModalRow\|export const WakeupsModalRow" src/ui/features/pretty-conversations/WakeupsModalRow.tsx && grep -q "e.stopPropagation" src/ui/features/pretty-conversations/WakeupsModalRow.tsx && grep -q "wakeups-modal-row-" src/ui/features/pretty-conversations/WakeupsModalRow.tsx && grep -q "-webkit-line-clamp" src/ui/features/pretty-conversations/WakeupsModalRow.tsx && ! grep -q "listWakeups\|createWakeup\|updateWakeup\|toggleWakeupEnabled\|deleteWakeup" src/ui/features/pretty-conversations/WakeupsModalRow.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: file exports WakeupsModalRow.
    - Source assertion: `grep -c "e.stopPropagation" src/ui/features/pretty-conversations/WakeupsModalRow.tsx` returns >= 2 (toggle and kebab both stopPropagation per D-11).
    - Source assertion: contains testid patterns `wakeups-modal-row-`, `-toggle`, `-kebab`, `-edit`, `-delete` for stable test hooks.
    - Source assertion: 2-line clamp via `-webkit-line-clamp` present.
    - Source assertion: no API-helper imports (`listWakeups`, `createWakeup`, `updateWakeup`, `toggleWakeupEnabled`, `deleteWakeup`) — the row is pure presentation.
    - Source assertion: no Loader2/spinner import from lucide-react (D-28).
    - Type check: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    - WakeupsModalRow renders name / metadata / prompt / chips / actions matching prototype § B layout.
    - Toggle + kebab clicks call e.stopPropagation before invoking their callback (D-11).
    - Row body onClick invokes onRowClick without stopPropagation (parent uses this for edit-mode entry).
    - Host chip uses subtle grey-blue tokens (D-09); role chips magenta; skill chips blue and only render when non-empty.
    - No streaming/spinner affordances (D-28).
  </done>
</task>

<task type="auto">
  <name>Task 3: Create WakeupsModal shell with list view — chrome, fetch-on-open, filter bar, row rendering, skeleton loading, empty state, footer count</name>
  <files>src/ui/features/pretty-conversations/WakeupsModal.tsx</files>
  <read_first>
    - src/ui/features/pretty-conversations/ConversationSearchModal.tsx (chrome analog — the whole file; specifically lines 47-63 imports, 75-85 props, 107-137 fetch-on-open, 209-244 Dialog shell, 249-306 header + X button, 310-345 body/scroll, 349-380 footer)
    - src/ui/features/pretty-conversations/CreateProjectModal.tsx (host-tree flatten + auto-select-single-host — lines 55-69 isFolder + collectAllHosts, 113-151 useMemo flatHosts + useEffect auto-select)
    - src/ui/features/pretty-view/RoleFileTab.tsx lines 60-85 (Skeleton loading pattern — matches shipped code per RESEARCH Pitfall #1)
    - src/ui/api/wakeups-api.ts (WakeupListItem, listWakeups from Task 1)
    - src/ui/features/pretty-conversations/WakeupsModalRow.tsx (from Task 2)
    - src/ui/api/identities-api.ts lines 299-328 (listRolesForHost + RoleSummary)
    - src/types/ui-types.ts (Host, HostFolder types — grep for them)
    - .planning/phases/135-wake-ups-redesign-campaign-shape-3-ui-modal-skynet-front-end/135-PATTERNS.md § WakeupsModal.tsx (lines 168-500)
    - .planning/phases/135-wake-ups-redesign-campaign-shape-3-ui-modal-skynet-front-end/135-RESEARCH.md § Chrome Token Dictionary (lines 755-785)
    - ~/fleet/roles/box-maintainer/bounties/wake-ups-redesign/prototype.html section B (list view layout)
  </read_first>
  <action>
    Create src/ui/features/pretty-conversations/WakeupsModal.tsx. Follow the chrome verbatim from ConversationSearchModal.tsx with ONE change: `md:max-w-[640px]` instead of `md:max-w-[560px]` (per RESEARCH Chrome Token Dictionary — prototype specifies 640).

    Imports (per PATTERNS.md § WakeupsModal.tsx imports pattern):
    - `useEffect, useMemo, useState` from react
    - `Dialog as DialogPrimitive` from `radix-ui`
    - `AlarmClock, MoreHorizontal, Plus, Search, X` from `lucide-react`
    - `cn` from `@/lib/utils`
    - `Skeleton` from `@/components/skeleton`
    - `WakeupsModalRow` from `./WakeupsModalRow`
    - `listWakeups`, `type WakeupListItem` from `@/api/wakeups-api`
    - `listRolesForHost`, `type RoleSummary` from `@/api/identities-api`
    - `type Host`, `type HostFolder` from wherever the panel imports them (mirror PrettyConversationsPanel.tsx's import)

    Props (mirror ConversationSearchModal.tsx:75-85):
    ```
    interface WakeupsModalProps {
      open: boolean;
      onOpenChange: (open: boolean) => void;
      hostTree: HostFolder | null;
    }
    ```

    Internal state:
    - `items: WakeupListItem[] | null` (null = loading, [] = empty)
    - `loadError: string | null`
    - `search: string` (D-10 free-text)
    - `roleFilter: string` (dropdown value; "ALL" sentinel = no filter)
    - `hostFilter: number | "ALL"` (D-10)
    - `view: "list" | "form"` (D-27; wave 1 only renders list view — form-mode is stubbed)
    - `editingSlug: string | null` (for wave-2 edit-mode entry — wave 1 wires the state but the form renders a placeholder)
    - `kebabOpen: string | null` (which row's kebab is open)
    - `toggleError: string | null` (D-12 — banner slot at top of list; wave 2 actually flips it; wave 1 initializes to null and renders the banner slot as `{toggleError && <div role="alert" data-testid="wakeups-modal-toggle-error">{toggleError}</div>}` — no writer yet)
    - `availableRoles: string[]` — deduped role names across all hosts for the filter dropdown (RESEARCH Assumption A1). Fetch strategy: `useEffect(open)` — after listWakeups resolves, collect `Array.from(new Set(items.flatMap(i => i.roles)))`. Do NOT call listRolesForHost fleet-wide in wave 1; use the derived set from the list response instead (simpler, no per-host fan-out).

    Fetch-on-open + reset-on-close (verbatim shape from PATTERNS.md § Fetch-on-open + reset-on-close, lines 293-318). Trigger: `useEffect(() => { ... }, [open])`. On `open=false`: reset all internal state above (D-17). On `open=true`: `listWakeups()` and hydrate `items`; on catch, set `items=[]` + `loadError=<message>`. Use an AbortController to guard against stale updates when the modal reopens rapidly.

    Host list flatten (verbatim from CreateProjectModal.tsx:55-69 + useMemo pattern at :113-151):
    ```
    const flatHosts = useMemo(
      () => collectAllHosts(hostTree?.children ?? []).filter((h) => h.enableRdp !== true),
      [hostTree],
    );
    ```
    Place `isFolder` + `collectAllHosts` as top-level helpers in this same file (or extract if planner prefers; either is fine for wave 1 — inline is simpler).

    Dialog shell — VERBATIM copy from PATTERNS.md § Dialog shell pattern (lines 216-253), swapping `md:max-w-[560px]` for `md:max-w-[640px]`. Include:
    - `onInteractOutside={(e) => e.preventDefault()}` (MANDATORY per RESEARCH Pitfall #7 — X + Escape are the only close paths).
    - `<DialogPrimitive.Title className="sr-only">Wake-ups</DialogPrimitive.Title>`.

    Header (per PATTERNS.md § Header pattern, lines 257-289):
    - Title: `{view === "list" ? "Wake-ups" : editingSlug ? "Edit wake-up" : "New wake-up"}`.
    - "+" button visible only when `view === "list"`, with `data-testid="wakeups-modal-add-button"` and `onClick={() => { setView("form"); setEditingSlug(null); }}` — wave 1 renders the form-view placeholder; wave 2 renders the real form.
    - X button: `onClick={() => onOpenChange(false)}`, `data-testid="wakeups-modal-close-button"`.
    - Do NOT use `Dialog.Close asChild` for the X button (RESEARCH Anti-Pattern).

    Body — split into two conditional blocks:
    - `view === "list"`: filter bar (search input `data-testid="wakeups-modal-filter-search"`, role select `data-testid="wakeups-modal-filter-role"` populated from `availableRoles` with an "All roles" default option, host select `data-testid="wakeups-modal-filter-host"` populated from flatHosts with an "All hosts" default option). Then a scrollable rows region. Loading: three `<Skeleton className="h-24 w-full rounded-[var(--radius-pv-bubble)]" />` bars (matches shipped WakeupsTab / RoleFileTab per RESEARCH Pitfall #1). Empty: dim centered helper text — "No wake-ups on any host. Click + to create one." (fleet-wide zero) or "No wake-ups match this filter." (filtered zero) per D-16. Rows: `visibleItems.map((row) => <WakeupsModalRow ... />)`. Above the rows region, render the optional `toggleError` banner (D-12 slot — wave 2 fills the writer).
    - `view === "form"`: wave 1 renders a placeholder `<div data-testid="wakeups-modal-form-placeholder">Form view will be implemented in wave 2.</div>` — this stub keeps TypeScript honest and preserves the state-transition wiring (Cancel button in this placeholder just calls `setView("list")` + `setEditingSlug(null)`).

    Filter application (client-side): `visibleItems = items.filter(row => (search==="" || row.name.toLowerCase().includes(search.toLowerCase()) || row.prompt.toLowerCase().includes(search.toLowerCase())) && (roleFilter==="ALL" || row.roles.includes(roleFilter)) && (hostFilter==="ALL" || row.hostId === hostFilter))` per D-10.

    Footer (per D-18):
    - Only render when `view === "list"`.
    - Content: `<div data-testid="wakeups-modal-footer-count">` — when filter is unfiltered: `\`${items.length} wake-ups · ${items.filter(i => i.enabled).length} enabled\``. When filter narrows: `\`${visibleItems.length} of ${items.length} · ${visibleItems.filter(i => i.enabled).length} enabled\`` (RESEARCH Recommendation #6). Dim / small text tokens.

    Wave-1 handlers (state-only; NO API writes yet):
    - `handleRowClick(row)` — sets `view="form"`, `editingSlug=row.slug`. Wave 2 replaces the form placeholder with the real hydrated form.
    - `handleToggleClick(row)` — wave 1: no-op stub with a `// TODO(wave 2): pessimistic toggle via toggleWakeupEnabled + refetch (D-12)` comment. Wave 2 replaces this with the real handler.
    - `handleKebabClick(row)` — flips `kebabOpen` between row.slug and null.
    - `handleEditFromKebab(row)` — same as handleRowClick.
    - `handleDeleteFromKebab(row)` — wave 1: no-op stub with a `// TODO(wave 2): native confirm + deleteWakeup + refetch (D-14)` comment. Wave 2 replaces.

    Do NOT (from PATTERNS.md § Anti-Patterns):
    - Do NOT client-cache items across modal-open cycles (D-03).
    - Do NOT allow click-outside close (`onInteractOutside={e => e.preventDefault()}` mandatory).
    - Do NOT add streaming affordances (D-28).
  </action>
  <verify>
    <automated>test -f src/ui/features/pretty-conversations/WakeupsModal.tsx && grep -q "export function WakeupsModal\|export const WakeupsModal" src/ui/features/pretty-conversations/WakeupsModal.tsx && grep -q "onInteractOutside" src/ui/features/pretty-conversations/WakeupsModal.tsx && grep -q "md:max-w-\[640px\]" src/ui/features/pretty-conversations/WakeupsModal.tsx && grep -q "listWakeups" src/ui/features/pretty-conversations/WakeupsModal.tsx && grep -q "wakeups-modal-close-button" src/ui/features/pretty-conversations/WakeupsModal.tsx && grep -q "wakeups-modal-add-button" src/ui/features/pretty-conversations/WakeupsModal.tsx && grep -q "wakeups-modal-filter-search" src/ui/features/pretty-conversations/WakeupsModal.tsx && grep -q "wakeups-modal-filter-role" src/ui/features/pretty-conversations/WakeupsModal.tsx && grep -q "wakeups-modal-filter-host" src/ui/features/pretty-conversations/WakeupsModal.tsx && grep -q "wakeups-modal-footer-count" src/ui/features/pretty-conversations/WakeupsModal.tsx && grep -q "Skeleton" src/ui/features/pretty-conversations/WakeupsModal.tsx && grep -q "AbortController" src/ui/features/pretty-conversations/WakeupsModal.tsx && ! grep -q "asChild" src/ui/features/pretty-conversations/WakeupsModal.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `onInteractOutside={(e) => e.preventDefault()}` present (RESEARCH Pitfall #7).
    - Source assertion: `md:max-w-[640px]` present, NOT `md:max-w-[560px]` (matches prototype).
    - Source assertion: all 6 testids present — close-button, add-button, filter-search, filter-role, filter-host, footer-count.
    - Source assertion: `Skeleton` component used at least 3 times in loading block (matches shipped WakeupsTab/RoleFileTab per RESEARCH Pitfall #1).
    - Source assertion: `AbortController` used to guard stale fetch updates.
    - Source assertion: `asChild` NOT used for the X button (RESEARCH Anti-Pattern).
    - Behavior: modal chrome dimensions verifiable via a temporary manual render — 640×720 desktop.
    - Type check: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    - Modal shell mirrors ConversationSearchModal chrome exactly except for the 640px width.
    - Fetch-on-open + reset-on-close discipline enforced via useEffect(open) (D-03, D-17).
    - Filter bar renders search + role + host dropdowns with "ALL" defaults (D-10).
    - Loading = 3 Skeleton bars (RESEARCH Pitfall #1 mitigated).
    - Empty state helper text renders for both fleet-wide-zero and filter-narrows-to-zero (D-16).
    - Footer count reflects visible + total + enabled count (D-18).
    - Form view is a placeholder stub; wave 2 replaces it.
  </done>
</task>

<task type="auto">
  <name>Task 4: Wire AlarmClock header button + WakeupsModal mount into PrettyConversationsPanel</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx</files>
  <read_first>
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx lines 60-80 (icon imports at ~line 67), 830-905 (state hook block), 2210-2325 (header cluster), 2880-3095 (sibling modal mount block)
    - src/ui/features/pretty-conversations/CreateProjectModal.tsx (for the hostTree prop threading pattern — grep the panel for `<CreateProjectModal` and mirror it)
    - src/ui/features/pretty-conversations/WakeupsModal.tsx (from Task 3 — the component this task consumes)
    - .planning/phases/135-wake-ups-redesign-campaign-shape-3-ui-modal-skynet-front-end/135-PATTERNS.md § PrettyConversationsPanel.tsx (lines 502-557)
  </read_first>
  <action>
    Modify src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx with three edits:

    1) **Icon import addition** (lucide-react import line near line 67): add `AlarmClock` to the alphabetical import list. Before: `import { AppWindow, Archive, ... }`. After: `import { AlarmClock, AppWindow, Archive, ... }`.

    2) **Component import addition** (near existing modal imports around line 138 for CreateProjectModal): add `import { WakeupsModal } from "./WakeupsModal";`.

    3) **State hook addition** (near line 897 next to `rolesListModalOpen`): add
       ```
       // Phase 135 (shape 3, wake-ups-redesign): controlled open state for the
       // new WakeupsModal. Opened via the AlarmClock button in .pv-header-actions
       // below (inserted after Globe, before the More/kebab). No preserved query
       // state — modal refetches every open (D-03) and resets filter state on
       // close (D-17).
       const [wakeupsModalOpen, setWakeupsModalOpen] = useState(false);
       ```

    4) **Header-button insertion** (inside the `showPencilButton && (<>...</>)` guarded fragment at ~lines 2226-2286): insert the new button AFTER the Globe button (`pv-header-global-files-button`, closes at ~:2284) and BEFORE the closing `</>` at ~:2285. The insertion (verbatim shape of Globe button structure):
       ```
       <button
         type="button"
         className="pv-pencil"
         aria-label="Wake-ups"
         title="Wake-ups"
         data-testid="pv-header-wakeups-button"
         onClick={() => setWakeupsModalOpen(true)}
       >
         <AlarmClock size={18} />
       </button>
       ```
       Do NOT add a dashed-border accent variant (RESEARCH Assumption A7 — the prototype's dashed accent was a tasting artifact; v1 ships without it, matching neighbor buttons).

    5) **Modal mount insertion** (after the ConversationSearchModal mount at ~lines 2939-2945): insert
       ```
       {/* Phase 135 (shape 3, wake-ups-redesign) — WakeupsModal: portal-mounted
           sibling of ConversationSearchModal. Opened via the AlarmClock button
           in .pv-header-actions above (after Globe, before feedback + kebab).
           Owns its own list + form + refetch lifecycle; controlled open state
           only. */}
       <WakeupsModal
         open={wakeupsModalOpen}
         onOpenChange={setWakeupsModalOpen}
         hostTree={hostTree ?? null}
       />
       ```

    6) **Header docstring update** (comment block at `PrettyConversationsPanel.tsx:2213-2225`): update the "Header icon buttons, left-to-right" listing to include the new Wake-ups position between Edit-global-files and the feedback+kebab pair. Increment any count in that comment.

    Do NOT touch any other panel logic. This task is 5 surgical additions to an existing file — no restructure.
  </action>
  <verify>
    <automated>grep -q "AlarmClock," src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx && grep -q "import { WakeupsModal } from" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx && grep -q "wakeupsModalOpen" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx && grep -q "pv-header-wakeups-button" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx && grep -q "<WakeupsModal" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx && grep -q "hostTree={hostTree ?? null}" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `AlarmClock` in the lucide-react import statement.
    - Source assertion: `import { WakeupsModal } from "./WakeupsModal";` present.
    - Source assertion: `useState(false)` line binding `wakeupsModalOpen` + `setWakeupsModalOpen`.
    - Source assertion: `data-testid="pv-header-wakeups-button"` present with `className="pv-pencil"` on the same button element (verified by structural read).
    - Source assertion: `<WakeupsModal` JSX present with `open={wakeupsModalOpen}`, `onOpenChange={setWakeupsModalOpen}`, and `hostTree={hostTree ?? null}` props.
    - Behavior: header cluster shows a new AlarmClock icon button between the Globe icon and the More kebab (verifiable in a scoped panel-render test in wave 2, or by manual render).
    - Type check: `npx tsc --noEmit` exits 0.
    - Scoped test: `npx vitest related --run src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` exits 0 (existing tests still pass — no regressions from the additions).
  </acceptance_criteria>
  <done>
    - AlarmClock icon added to lucide import; WakeupsModal component imported; useState hook for wakeups modal open/close; new .pv-pencil header button between Globe and More; modal mount alongside sibling modals with hostTree threaded through.
    - Header docstring updated to reflect the new 7-button count and Wake-ups position.
    - No unrelated diffs to the panel.
    - Existing scoped panel tests still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Skynet backend (/wakeups) | User's JWT-authenticated axios calls cross into the backend fan-out layer. Backend enforces per-user host isolation via `resolveHostById(hostId, userId)` on every write and user-scoped host projection on the LIST. |
| Skynet backend → managed hosts (SSH) | Shape-2 responsibility; wave 1 is a pure consumer. Trust boundary already governed by shape 2's shipped code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-01 | Tampering | wakeups-api.ts DELETE helper | mitigate | Use axios `data:` config for body (RESEARCH Pitfall #4); server validates `host` against user's JWT via `resolveHostById(hostId, userId)`. Frontend cannot forge cross-user hostIds — server rejects. |
| T-135-02 | Information Disclosure | WakeupsModal filter-bar / host dropdown | mitigate | Host dropdown populated from panel-threaded `hostTree` (already per-user filtered upstream in AppShell). Modal cannot show hosts the user does not own. Verified pattern from CreateProjectModal's host picker (same threading). |
| T-129-03 | Tampering (XSS) | List row rendering (name, prompt, host, chip labels) | mitigate | React text-children only in WakeupsModalRow — no `dangerouslySetInnerHTML`. Prompt renders as `<div>{row.prompt}</div>` with CSS 2-line clamp. |
| T-129-04 | Spoofing (CSRF) | All write helpers in wakeups-api.ts (wave 2 uses; wave 1 defines) | mitigate | JWT via authApi (Authorization header, not cookie) — CSRF structurally impossible for cross-origin attackers. Inherited from existing Skynet axios wrapper. |
| T-129-05 | DoS | Modal fetch on every open | accept | LIST is 15s-per-host bounded on the backend (shape 2's timeout); rapid-open would just fire fan-outs the server handles idempotently. AbortController in useEffect cleans up stale updates in the browser. Not a real DoS surface. |
| T-129-SC | Tampering | npm/pip/cargo installs | mitigate | This phase adds ZERO new npm packages. RESEARCH Package Legitimacy Audit table verifies no installs required. No `[ASSUMED]`/`[SUS]` items — checkpoint is N/A. |

**Overall risk:** LOW. No new backend surface, no new secrets, no new auth pathway; wave 1 is a pure read consumer of shape 2's shipped, tested endpoints. Every threat above is already mitigated in shape 2's shipping code; wave 1 inherits by using endpoints as designed.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest related --run src/ui/api/wakeups-api.ts src/ui/features/pretty-conversations/WakeupsModal.tsx src/ui/features/pretty-conversations/WakeupsModalRow.tsx src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` exits 0 (existing tests still pass; new files have no tests yet — those land in wave 2).
- Grep gates for testids (`wakeups-modal-close-button`, `wakeups-modal-add-button`, `pv-header-wakeups-button`, etc.) all match.
- No `WakeupSpecWire` re-import (Pitfall #2 gate).
- No `Dialog.Close asChild` (Pitfall reason: consistency with ConversationSearchModal's direct-onClick).
- No streaming affordances (`Loader2` spinner not used in the modal — grep guards against it).
</verification>

<success_criteria>
- User can click the new AlarmClock button in the conversation-list header (guarded by showPencilButton) and see a Radix-Dialog modal open at 640×720 desktop / inset-4 mobile with glass-morphism chrome.
- Modal fetches GET /wakeups on open and renders the resulting rows with name / metadata (schedule + host chip) / prompt (2-line clamp) / role and skill chips / toggle + kebab actions.
- Filter bar (search + role + host) narrows visible rows client-side; filter state resets on close (D-17).
- Loading = 3 Skeleton bars; empty = dim helper text; footer = live count. All wave-1 D-XX decisions (D-01..D-11, D-15..D-18, D-26..D-28) honored.
- Row click enters a stubbed form view (wave 2 renders the real form); no create/update/toggle/delete writes yet.
- Container / deploy tasks are ORCHESTRATOR-EXCLUSIVE — this plan does not include `git push`, `docker build`, or `docker compose up`.
</success_criteria>

<output>
Create `.planning/phases/135-wake-ups-redesign-campaign-shape-3-ui-modal-skynet-front-end/135-01-SUMMARY.md` when done, capturing:
- Which testids were exposed and which are wave-2 slots (`wakeups-modal-form-*`, etc.).
- The exact placement of the header button in PrettyConversationsPanel.tsx (line number + comment identifying the neighbors).
- Any deviations from PATTERNS.md excerpts (e.g., if the alphabetical order in the lucide-react import forced a different position).
- The chosen approach for the role filter dropdown's population (recommend: derived from `items` union, not per-host fan-out).
</output>
