---
phase: 20-identity-creation-ui
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/pickers/VoicePicker.tsx
  - src/ui/features/pretty-view/pickers/ColorPicker.tsx
  - src/ui/features/pretty-view/pickers/VoicePicker.test.tsx
  - src/ui/features/pretty-view/pickers/ColorPicker.test.tsx
  - src/ui/features/pretty-view/IdentityModal.tsx
autonomous: true
requirements:
  - IDUI-10
user_setup: []

must_haves:
  truths:
    - "VoicePicker is a self-contained component that IdentityModal.tsx now consumes VERBATIM (no functional change to identity-edit voice UX)"
    - "ColorPicker is a self-contained component that IdentityModal.tsx now consumes VERBATIM (no functional change to identity-edit color UX)"
    - "Both components render identically pre- and post-extraction (visual regression protection via existing IdentityModal tests)"
    - "postSpeak(SAMPLE_PHRASE, voiceDraft) voice-preview behavior preserved on the sample button (IDUI-10 explicit requirement)"
  artifacts:
    - path: "src/ui/features/pretty-view/pickers/VoicePicker.tsx"
      provides: "Extracted voice picker component (select + sample play button)"
      exports: ["VoicePicker", "SAMPLE_PHRASE"]
    - path: "src/ui/features/pretty-view/pickers/ColorPicker.tsx"
      provides: "Extracted color hue slider component (range + swatch + degree readout)"
      exports: ["ColorPicker"]
    - path: "src/ui/features/pretty-view/pickers/VoicePicker.test.tsx"
      provides: "Unit tests: renders options from getVoices, calls onChange, sample button plays via postSpeak"
    - path: "src/ui/features/pretty-view/pickers/ColorPicker.test.tsx"
      provides: "Unit tests: renders swatch at draft hue, slider onChange fires with 0-360 range"
    - path: "src/ui/features/pretty-view/IdentityModal.tsx"
      contains: "<VoicePicker"
    - path: "src/ui/features/pretty-view/IdentityModal.tsx"
      contains: "<ColorPicker"
  key_links:
    - from: "src/ui/features/pretty-view/pickers/VoicePicker.tsx"
      to: "src/ui/api/voice-api.ts (getVoices, postSpeak)"
      via: "import"
      pattern: "from.*voice-api"
    - from: "src/ui/features/pretty-view/IdentityModal.tsx"
      to: "src/ui/features/pretty-view/pickers/VoicePicker.tsx and ColorPicker.tsx"
      via: "component import + JSX render"
      pattern: "VoicePicker|ColorPicker"
---

<objective>
Extract the voice picker (patch #223 surface, IdentityModal.tsx L186-190 + L1144-1197)
and color picker (patch #279 surface, IdentityModal.tsx L190-192 + L1200-1236) into
reusable components under `src/ui/features/pretty-view/pickers/`, then refactor
IdentityModal.tsx to consume them. This unblocks plan 05 (the new create modal)
using the same components without duplication.

Purpose: D-CONTEXT §IDUI-10 explicitly directs "Voice + color pickers are reused from
the existing identity-edit modal. Do not re-invent — read the existing modal's
implementation and lift the components." Extraction is the correct call because two
different modals (identity-edit and identity-create) will consume them AND the shape
files call out the drift-risk if we inline-copy.

Output:
- `VoicePicker` component (select + sample play button + Volume2 icon) taking props
  `{value, onChange, disabled, id?, ariaLabel?}` and internally managing voices list
  fetch, sample audio lifecycle, and postSpeak preview call.
- `ColorPicker` component (range slider + swatch + degree readout) taking props
  `{value, onChange, disabled, id?}`.
- Refactored IdentityModal.tsx replacing the inline JSX with `<VoicePicker>` and
  `<ColorPicker>` calls, preserving all existing state (`voiceDraft`, `hueDraft`,
  `saving`, etc.) — this is a REFACTOR-ONLY change with zero functional delta.
- Unit tests for each picker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/20-identity-creation-ui/20-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract VoicePicker component + write tests</name>
  <files>src/ui/features/pretty-view/pickers/VoicePicker.tsx, src/ui/features/pretty-view/pickers/VoicePicker.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/IdentityModal.tsx L186-190 (voice state declarations)
    - src/ui/features/pretty-view/IdentityModal.tsx L404-424 (voice-list fetch effect + sample audio cleanup effect)
    - src/ui/features/pretty-view/IdentityModal.tsx L780-807 (onSampleClick handler + patch #211 "NEVER bare audio.play().catch(...)" lesson at L802-803)
    - src/ui/features/pretty-view/IdentityModal.tsx L1144-1197 (voice picker JSX — the exact markup to lift)
    - src/ui/api/voice-api.ts L53-60 (getVoices signature) and L(search for postSpeak) (postSpeak(text, voice) → Blob signature)
    - src/ui/features/pretty-view/IdentityModal.voice.test.tsx (existing voice-picker tests — the extracted component must not break these)
  </read_first>
  <behavior>
    - Test 1 "renders voice options from getVoices on mount": Mock getVoices to resolve `[{display_name:"Alice", filename:"Alice.wav"}, {display_name:"Bob", filename:"Bob.wav"}]`; render `<VoicePicker value="" onChange={...} disabled={false}/>`; after promises flush, the `<select>` contains options `(default)`, `Alice`, `Bob` (3 total)
    - Test 2 "onChange fires with new filename on select change": Render + user-select `Alice`; assert onChange was called with `"Alice.wav"`
    - Test 3 "sample button calls postSpeak with SAMPLE_PHRASE and voice": Click the Volume2 button while value="Elena.wav"; assert postSpeak was called with `(SAMPLE_PHRASE, "Elena.wav")`
    - Test 4 "sample button uses undefined voice when value is empty": Click while value=""; assert postSpeak was called with `(SAMPLE_PHRASE, undefined)`
    - Test 5 "disabled prop disables select AND sample button": Render with disabled=true; assert both are `disabled`
    - Test 6 "unmount revokes any playing sample URL and pauses audio": Start playback (mock Audio), unmount component, assert audio.pause() called AND URL.revokeObjectURL called
    - Test 7 "SAMPLE_PHRASE is exported and non-empty": `expect(SAMPLE_PHRASE.length).toBeGreaterThan(0)`
  </behavior>
  <action>
    Create `src/ui/features/pretty-view/pickers/VoicePicker.tsx` exporting:
    - `export const SAMPLE_PHRASE = "..."` — copy verbatim from IdentityModal.tsx (grep IdentityModal.tsx for `SAMPLE_PHRASE = ` to find the exact string; if it lives in a different file, import from there instead of redeclaring).
    - `export function VoicePicker({value, onChange, disabled, id, ariaLabel}: {value: string; onChange: (next: string) => void; disabled?: boolean; id?: string; ariaLabel?: string;})` — component owning:
      - Internal state: `voices: {display_name, filename}[]` (fetched from getVoices on mount, cleared on unmount).
      - `sampleAudioRef`, `sampleUrlRef` (React refs mirroring IdentityModal L194-195).
      - `useEffect` for voices fetch (mirror L404-412).
      - `useEffect` for unmount cleanup of sample audio (mirror L414-424).
      - `onSampleClick` async handler (mirror L780-807 including the patch #211 `Promise.resolve(audio.play()).catch(() => {})` guard verbatim — do NOT change this line, it is load-bearing per commit history).
      - JSX: the `<select>` + Volume2 button structure from L1152-1197 verbatim, with `value={value}` `onChange={(e) => onChange(e.target.value)}` `disabled={disabled}` and using `id ?? "voice-picker"` for the id attribute.
    - Import Volume2 from `lucide-react`, getVoices + postSpeak from `@/api/voice-api`.

    Create `src/ui/features/pretty-view/pickers/VoicePicker.test.tsx`:
    - Use vitest + @testing-library/react.
    - Mock `@/api/voice-api` (`vi.mock`) to provide getVoices + postSpeak stubs.
    - Mock global `Audio` class + `URL.createObjectURL`/`revokeObjectURL` for tests 3-6.
    - Reference the existing test file `src/ui/features/pretty-view/IdentityModal.voice.test.tsx` for the mocking + shape that already works for voice UI in this codebase.

    Tests should first fail (component file empty), then pass after implementation lands in same task.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx vitest run src/ui/features/pretty-view/pickers/VoicePicker.test.tsx 2>&1 | tail -10 | grep -E "Tests.*passed" — MUST show 7 passed</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/features/pretty-view/pickers/VoicePicker.tsx` exists
    - `grep -c "export function VoicePicker\|export const VoicePicker" src/ui/features/pretty-view/pickers/VoicePicker.tsx` returns ≥ 1
    - `grep -c "SAMPLE_PHRASE" src/ui/features/pretty-view/pickers/VoicePicker.tsx` returns ≥ 1
    - `grep -c "getVoices\|postSpeak" src/ui/features/pretty-view/pickers/VoicePicker.tsx` returns ≥ 2
    - `grep -c "Promise.resolve(audio.play())" src/ui/features/pretty-view/pickers/VoicePicker.tsx` returns ≥ 1 (patch #211 guard preserved)
    - `grep -c "it(" src/ui/features/pretty-view/pickers/VoicePicker.test.tsx` returns ≥ 7
    - `npx vitest run src/ui/features/pretty-view/pickers/VoicePicker.test.tsx` exits 0
  </acceptance_criteria>
  <done>VoicePicker exists, tests green, patch #211 audio.play guard preserved verbatim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract ColorPicker component + write tests</name>
  <files>src/ui/features/pretty-view/pickers/ColorPicker.tsx, src/ui/features/pretty-view/pickers/ColorPicker.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/IdentityModal.tsx L190-192 (hueDraft/committedHue state)
    - src/ui/features/pretty-view/IdentityModal.tsx L400-402 (hueDraft reset effect)
    - src/ui/features/pretty-view/IdentityModal.tsx L1200-1236 (color picker JSX — the exact markup)
  </read_first>
  <behavior>
    - Test 1 "renders slider with initial value": `<ColorPicker value={210} onChange={...}/>` → slider input has `value="210"`
    - Test 2 "onChange fires with number when slider moves": fireEvent.change(slider, {target:{value:"280"}}) → onChange called with `280` (number, not string)
    - Test 3 "swatch background matches hue": Render with value=45; assert the swatch div has `background: hsl(45, 65%, 55%)` in style
    - Test 4 "degree readout shows value with degree symbol": Render with value=180; assert the readout text contains `"180°"`
    - Test 5 "disabled prop disables slider": Render disabled → range input is `disabled`
    - Test 6 "clamps to 0-360 range": slider min=0 max=360 in DOM
  </behavior>
  <action>
    Create `src/ui/features/pretty-view/pickers/ColorPicker.tsx` exporting:
    - `export function ColorPicker({value, onChange, disabled, id}: {value: number; onChange: (next: number) => void; disabled?: boolean; id?: string;})` — pure controlled component owning zero state.
    - JSX: the range input + swatch + readout structure from IdentityModal.tsx L1205-1235 verbatim, replacing `hueDraft` with `value` and the setter with `onChange(Number(e.target.value))`. Preserve the gradient background style on the slider AND the `hsl(${value}, 65%, 55%)` swatch style exactly.

    Create `src/ui/features/pretty-view/pickers/ColorPicker.test.tsx` covering the 6 behaviors above using vitest + @testing-library/react.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx vitest run src/ui/features/pretty-view/pickers/ColorPicker.test.tsx 2>&1 | tail -10 | grep -E "Tests.*passed" — MUST show 6 passed</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/features/pretty-view/pickers/ColorPicker.tsx` exists
    - `grep -c "export function ColorPicker\|export const ColorPicker" src/ui/features/pretty-view/pickers/ColorPicker.tsx` returns ≥ 1
    - `grep -c 'type="range"' src/ui/features/pretty-view/pickers/ColorPicker.tsx` returns ≥ 1
    - `grep -c 'hsl(' src/ui/features/pretty-view/pickers/ColorPicker.tsx` returns ≥ 2 (slider gradient + swatch)
    - `grep -c "it(" src/ui/features/pretty-view/pickers/ColorPicker.test.tsx` returns ≥ 6
    - `npx vitest run src/ui/features/pretty-view/pickers/ColorPicker.test.tsx` exits 0
  </acceptance_criteria>
  <done>ColorPicker exists, tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor IdentityModal.tsx to consume the extracted pickers</name>
  <files>src/ui/features/pretty-view/IdentityModal.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/pickers/VoicePicker.tsx (the target component signature)
    - src/ui/features/pretty-view/pickers/ColorPicker.tsx (the target component signature)
    - src/ui/features/pretty-view/IdentityModal.tsx L180-195 (state block — voices/voiceDraft/committedVoice/hueDraft/committedHue live here)
    - src/ui/features/pretty-view/IdentityModal.tsx L404-424 (voice fetch + sample cleanup effects — REMOVE these; VoicePicker now owns them)
    - src/ui/features/pretty-view/IdentityModal.tsx L780-807 (onSampleClick — REMOVE; VoicePicker now owns it)
    - src/ui/features/pretty-view/IdentityModal.tsx L1144-1197 (voice JSX to replace with `<VoicePicker>`)
    - src/ui/features/pretty-view/IdentityModal.tsx L1200-1236 (color JSX to replace with `<ColorPicker>`)
    - src/ui/features/pretty-view/IdentityModal.test.tsx (existing behavioral tests — MUST stay green post-refactor)
    - src/ui/features/pretty-view/IdentityModal.voice.test.tsx (voice-focused tests — MUST stay green)
  </read_first>
  <action>
    Refactor `src/ui/features/pretty-view/IdentityModal.tsx`:

    1. Add import: `import { VoicePicker } from "./pickers/VoicePicker";` and `import { ColorPicker } from "./pickers/ColorPicker";`.

    2. Remove state that VoicePicker now owns internally:
       - `const [voices, setVoices] = useState<...>([])` at L187 — DELETE.
       - `sampleAudioRef`, `sampleUrlRef` at L194-195 — DELETE.
       - Voice fetch useEffect at L404-412 — DELETE (VoicePicker fetches on its own mount).
       - Sample audio cleanup useEffect at L414-424 — DELETE (VoicePicker owns cleanup).
       - `onSampleClick` function at L780-807 — DELETE (VoicePicker owns the sample button).
       - Do NOT delete `voiceDraft` / `committedVoice` — those stay owned by IdentityModal (they gate onSave logic at L822-825 + L853-854).

    3. Replace voice JSX block at L1144-1197 with:
       - `<div className="mb-3">`
       - `  <label className="block text-xs text-[var(--color-pv-fg-muted)] mb-1" htmlFor="identity-voice-select">Voice</label>`
       - `  <VoicePicker id="identity-voice-select" value={voiceDraft} onChange={setVoiceDraft} disabled={saving} />`
       - `</div>`

    4. Replace color JSX block at L1200-1236 with:
       - `<div className="mb-3">`
       - `  <label className="block text-xs text-[var(--color-pv-fg-muted)] mb-1" htmlFor="identity-hue-input">Color</label>`
       - `  <ColorPicker id="identity-hue-input" value={hueDraft} onChange={setHueDraft} disabled={saving} />`
       - `</div>`

    5. Do NOT touch `onSave` / `onCancel` / hueDraft reset effect / voiceDraft reset effect at L396-402 — they stay in IdentityModal.

    6. Remove now-unused imports (Volume2 from lucide-react ONLY if it is not used elsewhere in the file — grep first before removing; getVoices, postSpeak, SAMPLE_PHRASE if no longer referenced).

    All existing IdentityModal tests (IdentityModal.test.tsx, IdentityModal.voice.test.tsx) MUST stay green.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx vitest run src/ui/features/pretty-view/IdentityModal.test.tsx src/ui/features/pretty-view/IdentityModal.voice.test.tsx src/ui/features/pretty-view/pickers/ 2>&1 | tail -15 | grep -E "Tests.*passed" — MUST show all passing</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<VoicePicker" src/ui/features/pretty-view/IdentityModal.tsx` returns ≥ 1
    - `grep -c "<ColorPicker" src/ui/features/pretty-view/IdentityModal.tsx` returns ≥ 1
    - `grep -c 'from "./pickers/VoicePicker"' src/ui/features/pretty-view/IdentityModal.tsx` returns ≥ 1
    - `grep -c 'from "./pickers/ColorPicker"' src/ui/features/pretty-view/IdentityModal.tsx` returns ≥ 1
    - `grep -c "setVoices\|useState.*voices" src/ui/features/pretty-view/IdentityModal.tsx` returns 0 (removed — now inside VoicePicker)
    - `grep -c "onSampleClick" src/ui/features/pretty-view/IdentityModal.tsx` returns 0 (removed)
    - `grep -c "sampleAudioRef" src/ui/features/pretty-view/IdentityModal.tsx` returns 0 (removed)
    - `grep -c "voiceDraft" src/ui/features/pretty-view/IdentityModal.tsx` returns ≥ 3 (still owns the state; onSave, reset effect, VoicePicker prop)
    - `grep -c "hueDraft" src/ui/features/pretty-view/IdentityModal.tsx` returns ≥ 3
    - `npx vitest run src/ui/features/pretty-view/IdentityModal.test.tsx src/ui/features/pretty-view/IdentityModal.voice.test.tsx` exits 0
    - `npx vitest run src/ui/features/pretty-view/pickers/` exits 0
    - Frontend TypeScript compiles: `npx tsc --noEmit -p src/ui/tsconfig.json 2>&1 | tail -5` shows no errors
  </acceptance_criteria>
  <done>IdentityModal uses VoicePicker + ColorPicker, all pre-existing tests green, no functional regression.</done>
</task>

</tasks>

<verification>
1. All picker + IdentityModal tests pass:
   `cd /home/ubuntu/skynet && npx vitest run src/ui/features/pretty-view/` exits 0.
2. Frontend TS compiles clean.
3. Visual: IdentityModal renders voice + color pickers in the same location as before.
4. No behavioral change to sample voice preview, hue save-with-verify guard at L836, etc.
</verification>

<success_criteria>
- VoicePicker and ColorPicker exist as standalone reusable components.
- IdentityModal.tsx line count REDUCES (removed voice fetch, sample handler, cleanup effect, inline JSX) but functional behavior is IDENTICAL.
- Plan 05 can now consume both components verbatim.
- Deploy discipline honored: no push/build/deploy.
</success_criteria>

<output>
Create `.planning/phases/20-identity-creation-ui/20-03-SUMMARY.md` when done. Include:
- Exact prop signatures of VoicePicker + ColorPicker (for plan 05 to consume).
- Where SAMPLE_PHRASE now lives (VoicePicker export vs re-imported from a shared voice-api file).
- Confirmation that patch #211 `Promise.resolve(audio.play()).catch(() => {})` guard is preserved.
- Line count delta of IdentityModal.tsx (before vs after — should shrink by ~80 lines).
- Explicit callout that no push/build/deploy was performed.
</output>
