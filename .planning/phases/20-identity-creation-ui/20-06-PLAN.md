---
phase: 20-identity-creation-ui
plan: 06
type: execute
wave: 4
depends_on:
  - 20-04
  - 20-05
files_modified:
  - src/ui/sidebar/NewSessionDialog.tsx
  - src/ui/sidebar/NewSessionDialog.test.tsx
  - src/ui/AppShell.tsx
  - src/ui/api/identities-api.ts
autonomous: true
requirements:
  - IDUI-06
  - IDUI-07
  - IDUI-08

must_haves:
  truths:
    - "Clicking Create with identityMode=true starts an SSE consumer against POST /identities/birth"
    - "Modal stays open during birth; 5-checkbox progress row shows pending/in-progress/done/failed per step"
    - "On successful birth (all 5 steps done), modal closes AND focus follows to the new session via AppShell's onCreateSession (which opens a tmux tab with the identity name as targetTmuxSession)"
    - "On failed birth at step N, modal shows a step-specific failure blurb (defaults from CONTEXT.md § 'Failure blurbs') and Ashley can close the modal — closing resets ALL state (no draft store)"
    - "NO Cancel button appears during birth; NO retry-in-place option; NO rollback (per D-CONTEXT non-negotiables)"
    - "Regular-session mode (identityMode=false) preserves the existing openTab behavior in AppShell verbatim"
  artifacts:
    - path: "src/ui/api/identities-api.ts"
      contains: "openBirthStream"
    - path: "src/ui/sidebar/NewSessionDialog.tsx"
      contains: "BirthProgress"
    - path: "src/ui/AppShell.tsx"
      contains: "identityMode"
  key_links:
    - from: "src/ui/sidebar/NewSessionDialog.tsx"
      to: "src/ui/api/identities-api.ts (openBirthStream) → POST /identities/birth SSE"
      via: "fetch + ReadableStream reader OR EventSource-with-POST equivalent"
      pattern: "openBirthStream"
    - from: "src/ui/sidebar/NewSessionDialog.tsx"
      to: "AppShell onCreateSession callback"
      via: "onSuccess payload with sessionName = identity name"
      pattern: "identityMode: true"
    - from: "src/ui/AppShell.tsx"
      to: "openTab with targetTmuxSession = identity name"
      via: "reuse existing openTab flow"
      pattern: "targetTmuxSession"
---

<objective>
Wire up the birth-loop consumer (IDUI-06 frontend half, IDUI-07 full, IDUI-08 frontend
half). When Create fires with identityMode=true, the modal opens an SSE stream against
POST /identities/birth, renders the 5-step ticking checklist, shows per-step failure
blurbs on failure, closes on success + fires focus-follow via AppShell's existing
openTab flow.

Purpose: Plan 05 built the modal state + validation + avatar loop but stopped short of
calling the birth endpoint. This plan closes the loop: SSE consumer + progress UI +
failure-blurb rendering + AppShell handler update to route successful births to a fresh
terminal tab with the identity name.

Output:
- New `openBirthStream(opts)` helper in `identities-api.ts` using fetch + ReadableStream
  (EventSource doesn't support POST; we need POST because the birth request has a body).
- Extended `NewSessionDialog.tsx` with `BirthProgress` sub-component rendering 5 rows
  (Skynet record / tmux session / Claude CLI / bootstrap dance / /id command) with
  pending/in-progress/done/failed states + failure blurb inline.
- Updated Create button handler that starts the birth stream when identityMode=true.
- Updated `AppShell.tsx` `onCreateSession` handler to accept the new payload shape and
  route successful identity-births to `openTab(host, "terminal", ...)` with
  targetTmuxSession = the identity name.
- Extended tests: SSE stream consumption, per-step render states, failure blurb per step,
  onSuccess opens new tab, close-during-birth is disabled, close-after-failure resets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/20-identity-creation-ui/20-CONTEXT.md
@.planning/phases/20-identity-creation-ui/20-04-SUMMARY.md
@.planning/phases/20-identity-creation-ui/20-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add openBirthStream helper to identities-api.ts</name>
  <files>src/ui/api/identities-api.ts</files>
  <read_first>
    - src/ui/api/identities-api.ts (post plan 05 shape)
    - src/ui/api/voice-api.ts (existing streaming pattern — look for `postSpeakStream` or the /voice/speak-stream consumer; it uses fetch + ReadableStream for streaming binary; adapt for text SSE)
    - src/ui/main-axios.ts (authApi definition + how it grabs JWT — we need to authenticate the fetch call the same way; likely via reading a JWT cookie or from localStorage/context)
    - .planning/phases/20-identity-creation-ui/20-04-SUMMARY.md (SSE event schema: event name = "birth", data = JSON of BirthEvent union)
  </read_first>
  <behavior>
    - Test 1 "openBirthStream sends POST with JSON body to /identities/birth": Mock global fetch. Call `openBirthStream({hostId, name, ...})`. Assert fetch was called with URL `/identities/birth`, method `POST`, body containing JSON of opts, headers with `Accept: text/event-stream` and `Content-Type: application/json`.
    - Test 2 "openBirthStream returns an async iterable of BirthEvents": Mock fetch to return a ReadableStream that emits 3 canned SSE frames. `for await (const evt of openBirthStream(opts))` yields the 3 parsed BirthEvents in order.
    - Test 3 "openBirthStream frame parser handles multi-line frames + partial chunks": Mock a stream that splits a frame across two chunks (`event: birth\ndata: {"type":"st` in one chunk, `ep","n":1,"phase":"started"}\n\n` in next). Consumer still gets the assembled event correctly.
    - Test 4 "openBirthStream aborts stream if consumer breaks early": Providing an AbortSignal that gets aborted mid-stream causes the returned iterator to stop cleanly.
    - Test 5 "openBirthStream propagates non-200 responses as thrown error": Mock fetch to return 400 with JSON body. Consuming the iterator throws with the error message from the JSON.
  </behavior>
  <action>
    Add `openBirthStream` helper to `src/ui/api/identities-api.ts`. Implementation:

    - Signature: `export async function* openBirthStream(opts: BirthRequest, signal?: AbortSignal): AsyncGenerator<BirthEvent>` where:
      - `BirthRequest = {hostId: number, name: string, title: string, path: string, colorHue: number | null, voice: string | null, avatarCandidateId: string}` (matches plan 04's route body validation)
      - `BirthEvent = {type: "step"; n: 1|2|3|4|5; phase: "started"|"completed"|"failed"; reason?: string} | {type: "ended"; ok: boolean; failedStep?: number; identityId?: string; sessionName?: string}` (mirrors plan 04's SSE schema)

    - Implementation:
      1. Use `fetch("/identities/birth", {method:"POST", headers:{"Content-Type":"application/json", "Accept":"text/event-stream", ...authHeaders}, body: JSON.stringify(opts), signal})`. Auth: mirror however authApi (from `@/main-axios`) attaches auth — likely a JWT from cookie (browser sends automatically) or from a token store (add via header). Read `src/ui/main-axios.ts` to determine the exact auth pattern and match it.
      2. If response.status !== 200 OR content-type is not text/event-stream, read response body as JSON and throw `new Error(json.error ?? \`birth failed: HTTP \${response.status}\`)`.
      3. Get reader: `const reader = response.body!.getReader(); const decoder = new TextDecoder();`
      4. Accumulate a `buffer = ""`. Loop: `const {done, value} = await reader.read(); if (done) break; buffer += decoder.decode(value, {stream: true});`
      5. Split buffer on `\n\n` — each complete chunk is one SSE frame. Keep any trailing partial in the buffer for next iteration.
      6. For each complete frame: parse lines, find `data: ...` line, JSON.parse the payload, `yield` the parsed BirthEvent. Ignore frames without a `data:` line.
      7. On done, if `buffer.length > 0` and contains a complete-looking frame, parse+yield it too.

    Write a test file `src/ui/api/identities-api.test.ts` (create fresh — no existing file per plan 05) covering the 5 tests above. Mock global fetch via `vi.stubGlobal("fetch", vi.fn())`. Build canned ReadableStreams for stream tests using `new ReadableStream({start(c) { c.enqueue(encoder.encode(chunk)); c.close(); }})`.

    All 5 tests should first fail (helper not implemented), then pass after implementation lands in same task.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx vitest run src/ui/api/identities-api.test.ts 2>&1 | tail -10 | grep -E "Tests.*passed" — MUST show 5 passing</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "openBirthStream" src/ui/api/identities-api.ts` returns ≥ 1
    - `grep -c "async function\\*\|AsyncGenerator\|AsyncIterable" src/ui/api/identities-api.ts` returns ≥ 1
    - `grep -c 'text/event-stream' src/ui/api/identities-api.ts` returns ≥ 1
    - `grep -c 'getReader\|TextDecoder' src/ui/api/identities-api.ts` returns ≥ 2
    - `grep -c "it(" src/ui/api/identities-api.test.ts` returns ≥ 5
    - `npx vitest run src/ui/api/identities-api.test.ts` exits 0
    - Frontend TS compiles clean
  </acceptance_criteria>
  <done>openBirthStream helper exported, 5 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend NewSessionDialog with BirthProgress + wire Create to birth stream</name>
  <files>src/ui/sidebar/NewSessionDialog.tsx, src/ui/sidebar/NewSessionDialog.test.tsx</files>
  <read_first>
    - src/ui/sidebar/NewSessionDialog.tsx (post plan 05 — the modal with all field state)
    - src/ui/sidebar/NewSessionDialog.test.tsx (post plan 05 — 21 tests + baseline)
    - src/ui/api/identities-api.ts (post Task 1 — openBirthStream + BirthEvent type)
    - .planning/phases/20-identity-creation-ui/20-CONTEXT.md § "Compound birth sequence" (step names for the 5 rows), § "Progress reporting granularity" (5-checkbox spec, pending/in-progress/done/failed states), § "Failure blurbs" (exact default text for each step's failure), § "Focus-follow on success", § "Field persistence on failure/close"
    - src/ui/features/pretty-view/IdentityModal.tsx (styling idiom for status states — mirror the pv-code-fg / green / dim palette for consistency)
  </read_first>
  <behavior>
    New tests (append to NewSessionDialog.test.tsx from plan 05):

    - Test V "clicking Create with identityMode ON starts birth stream": Mock openBirthStream to return an async generator that emits nothing then completes. Click Create with valid form. Assert openBirthStream was called with the exact payload from plan 05's onCreate contract (translated: no `identityMode` key, no `sessionName` key in the birth body — those are frontend-only).
    - Test W "5 progress rows render on birth start": After Create, the 5 rows appear with these labels (adjust exact wording only if D-CONTEXT changed):
      - "Create Skynet identity record"
      - "Open tmux session on <hostname>"
      - "Launch Claude CLI"
      - "Bootstrap dance"
      - "Send /id command"
    - Test X "row status flips pending → in-progress on step:N:started event": Emit `{type:"step",n:2,phase:"started"}` from the mocked stream. Assert row 2 has `data-status="in-progress"` (or the spinner icon rendered).
    - Test Y "row status flips in-progress → done on step:N:completed event": Emit started then completed for step 3. Assert row 3 has `data-status="done"` (green check icon).
    - Test Z "row status flips in-progress → failed on step:N:failed event AND blurb renders": Emit `{type:"step",n:2,phase:"failed",reason:"Connect timeout"}`. Assert row 2 has `data-status="failed"` AND the failure blurb text appears below the checklist matching the default from CONTEXT.md § "Failure blurbs" step 2 (contains "Skynet record created, but couldn't open a tmux session"). The reason string ("Connect timeout") is either surfaced as a subtitle or discarded — display the standard blurb, but include the reason on a second line for debugging.
    - Test AA "successful birth (all 5 completed then ended{ok:true}) closes modal AND calls onCreate": Emit all 10 progress events + `{type:"ended",ok:true,identityId:"abc",sessionName:"alicia"}`. Assert:
      - Modal `onClose` was called (or the parent-controlled `open` prop consumer receives the close event).
      - The plan-05 `onCreate` prop was called (as the "focus-follow" signal — AppShell's handler then opens the tab). Payload: `{host, sessionName:"alicia", path, identityMode: true, name:"alicia", ...}` — sessionName equals the identity name per plan 05's payload contract.
    - Test BB "failed birth keeps modal OPEN (state reset only on close)": Emit `{type:"step",n:3,phase:"failed",reason:"x"}` then `{type:"ended",ok:false,failedStep:3}`. Assert modal did NOT close. Assert `onCreate` was NOT called.
    - Test CC "close after failure resets ALL state": Continuing from BB, user closes modal. Assert on next open: identity-mode ON, name empty, title empty, brief empty, no candidates, no picked avatar (matches plan 05 Test U pattern — no draft store).
    - Test DD "birth in progress disables ALL form fields + hides Cancel button": After stream starts, name/title/brief/path inputs, host list, avatar pickers, voice, color, identity-mode checkbox all become disabled. The existing Cancel button becomes hidden (or disabled with an explanatory tooltip — either is fine, but pick one and be consistent). NO cancel-birth affordance appears — per D-CONTEXT non-negotiable "No cancel mid-birth."
    - Test EE "step:1:failed:silent-no-op surfaces the right blurb": Emit `{type:"step",n:1,phase:"failed",reason:"silent-no-op: colorHue not persisted"}`. Assert step-1 blurb text renders ("Couldn't create the Skynet identity record. Nothing was created — safe to retry." per CONTEXT.md § Failure blurbs step 1). The `reason` field is displayed as debug subtext but does NOT replace the blurb.
    - Test FF "openBirthStream error (fetch reject) surfaces as generic step-1 failure": Mock openBirthStream to throw synchronously. Assert row 1 shows failed with generic "birth failed" text; modal stays open; state does NOT reset until user closes.
    - Test GG "regular-session mode (identityMode=false) does NOT call openBirthStream": Uncheck identity-mode; click Create. Assert openBirthStream was NEVER called. onCreate was called with the identityMode=false payload from plan 05.
  </behavior>
  <action>
    Extend `src/ui/sidebar/NewSessionDialog.tsx`:

    1. New state:
       - `const [birthing, setBirthing] = useState(false);` — true from Create-click until stream ends
       - `const [birthProgress, setBirthProgress] = useState<Array<{n:1|2|3|4|5; status:"pending"|"in-progress"|"done"|"failed"; reason?:string}>>([...])` — initialized with 5 rows all pending
       - `const [birthFailedStep, setBirthFailedStep] = useState<number | null>(null);` — surfaced blurb key
       - `const abortControllerRef = useRef<AbortController | null>(null);` — for stream teardown on unmount

    2. Sub-component `BirthProgress` (defined inline in same file, unexported):
       - Props: `{steps: typeof birthProgress; failedStep: number | null; hostName: string; identityName: string}`
       - Renders a flex-col of 5 rows. Each row has an icon (pending: dim circle; in-progress: Loader2 spinning; done: Check green; failed: XCircle red), a label, and (if failed and this is the failed step) a blurb block below.
       - Import icons from lucide-react (Loader2, Check, XCircle are all available).
       - Step labels array (module-level constant):
         ```
         const BIRTH_STEP_LABELS = [
           "Create Skynet identity record",
           `Open tmux session on ${hostName}`,   // formatted at render
           "Launch Claude CLI",
           "Bootstrap dance",
           "Send /id command",
         ];
         ```
       - Failure blurbs — copy VERBATIM from D-CONTEXT § "Failure blurbs". Store as a module-level array `BIRTH_STEP_BLURBS: string[]` indexed 0-4 (step N is index N-1). Each blurb is templated with `<host>`, `<name>`, `<path>` slots — replace at render time.

    3. Update Create button `onClick` handler:
       - If identityMode === false: unchanged (calls onCreate with regular-session payload).
       - If identityMode === true:
         - `setBirthing(true); setBirthProgress(initialFivePendingRows); setBirthFailedStep(null);`
         - `abortControllerRef.current = new AbortController();`
         - Wrap in `try { const stream = openBirthStream({hostId: selectedHost!.id, name: name.toLowerCase(), title, path: normalizePath(path), colorHue, voice: voice || null, avatarCandidateId: pickedCandidateId!}, abortControllerRef.current.signal);`
         - `for await (const evt of stream) { ... }` handling each event:
           - `type:"step",phase:"started"` → set that row's status to "in-progress"
           - `type:"step",phase:"completed"` → set to "done"
           - `type:"step",phase:"failed"` → set to "failed", set birthFailedStep = evt.n
           - `type:"ended",ok:true` → break out of loop, call onCreate with the plan-05 identity payload (using stored sessionName=name), then call onClose. Reset birthing state.
           - `type:"ended",ok:false` → break out of loop, leave modal open with progress + blurb visible. `setBirthing(false);` (form fields stay disabled? no — user can't edit, they must close. Keep them disabled to prevent accidental "retry with same fields is not supported" confusion.)
         - `} catch (e) { setBirthProgress(prev => { prev[0].status="failed"; return [...prev]; }); setBirthFailedStep(1); setBirthing(false); /* stream aborted or errored pre-first-frame */ }`
       - Do NOT re-enable form fields on failure (user must close-and-reopen — matches D-CONTEXT "Field persistence on failure/close").

    4. Reset useEffect on close (from plan 05) — extend to also:
       - Abort the controller if still running: `abortControllerRef.current?.abort();`
       - `setBirthing(false); setBirthProgress(initialFivePendingRows); setBirthFailedStep(null);`

    5. Render tree change:
       - When `birthing || birthFailedStep !== null`: render `<BirthProgress ...>` BELOW the field cluster.
       - When `birthing`: disable ALL inputs (host list buttons, path input, identity-mode checkbox, name, title, brief, avatar Generate button, avatar candidate buttons, VoicePicker, ColorPicker). Hide (or disable + label with "in progress") the Cancel button.
       - Create button becomes disabled during `birthing`. Its label may change to "Creating..." for clarity.

    6. Component unmount cleanup:
       - Add a `useEffect(() => () => { abortControllerRef.current?.abort(); }, [])` so if the modal unmounts mid-birth (unlikely but possible), the stream is torn down. NOTE: birth continues on backend regardless — the SSE close just stops the frontend from consuming events. That's intentional per D-CONTEXT § "No cancel mid-birth" — even the abort doesn't abort the sequence.

    Append the 12 new tests (V through GG) to `src/ui/sidebar/NewSessionDialog.test.tsx`. Mocking:
    - `vi.mock('@/api/identities-api', ...)` — extend the existing mock from plan 05 to include openBirthStream. Provide a helper `createMockStream(events: BirthEvent[])` that returns an async generator yielding each event with awaits between (so tests can control timing).
    - Fake timers for any timing-sensitive assertions.

    All new tests must pass. Existing plan-05 tests must stay green.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx vitest run src/ui/sidebar/NewSessionDialog.test.tsx 2>&1 | tail -15 | grep -E "Tests.*passed" — MUST show all tests passing (plan-05's 21+baseline + this plan's 12)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "openBirthStream" src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 1
    - `grep -c "BirthProgress" src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 2 (declaration + usage)
    - `grep -c "BIRTH_STEP_LABELS\|Create Skynet identity record" src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 1
    - `grep -c "BIRTH_STEP_BLURBS\|Couldn't create the Skynet identity record" src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 1
    - `grep -c "AbortController" src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 1
    - `grep -c 'birthing' src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 5
    - `grep -ci "cancel.*birth\|cancelBirth" src/ui/sidebar/NewSessionDialog.tsx` returns 0 (no cancel affordance)
    - `grep -ci "retry.*birth\|retryBirth" src/ui/sidebar/NewSessionDialog.tsx` returns 0 (no retry affordance)
    - Test count grew: plan-05 baseline+21 → now +12 more (verify with `grep -c "it(" src/ui/sidebar/NewSessionDialog.test.tsx` showing that number)
    - `npx vitest run src/ui/sidebar/NewSessionDialog.test.tsx` exits 0
    - Frontend TS compiles: `npx tsc --noEmit -p src/ui/tsconfig.json 2>&1 | tail -5` empty of errors
  </acceptance_criteria>
  <done>BirthProgress renders, SSE consumed, all 5 rows tick + blurb on failure, modal closes on success + calls onCreate for focus-follow, close-after-failure resets, no cancel/retry affordances.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update AppShell onCreateSession handler to accept new payload + route to focus-follow</name>
  <files>src/ui/AppShell.tsx</files>
  <read_first>
    - src/ui/AppShell.tsx L1400-1440 (existing onCreateSession implementation — the exact place to update)
    - src/ui/sidebar/NewSessionDialog.tsx (post-Task-2 — the payload it emits)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx L985 (the callsite that forwards to onCreateSession — its onCreate signature must match)
    - .planning/phases/20-identity-creation-ui/20-CONTEXT.md § "Focus-follow on success"
  </read_first>
  <behavior>
    Since AppShell is a top-level shell without dedicated unit tests, the acceptance is functional + type-level:
    - Frontend TS compiles clean (currently BROKEN post-plan-05 because of the onCreate signature change — this task fixes it).
    - Existing regular-session flow still works: identityMode=false payload opens a terminal tab with the given sessionName + selectedHost.
    - New identity-mode-success flow: identityMode=true payload opens a terminal tab with `targetTmuxSession = payload.name` and `label = payload.name` (the identity name doubles as the tmux session name per Nelly's mechanism at step 2).
    - PrettyConversationsPanel.tsx's onCreate prop signature is compatible (either it forwards the payload verbatim to onCreateSession without inspecting fields — likely — or it also needs a type update).
  </behavior>
  <action>
    Edit `src/ui/AppShell.tsx` around L1409-1418:

    - Update the `onCreateSession` handler to accept the discriminated union from plan 05:
      ```
      onCreateSession={(opts) => {
        // opts is now either regular-session or identity-birth-success shape
        const host = opts.host;
        const sessionName = opts.identityMode
          ? opts.name                    // identity name doubles as tmux session name (Nelly mechanism)
          : opts.sessionName;
        const newTabId = openTab(host, "terminal", undefined, {
          targetTmuxSession: sessionName ?? null,
          label: sessionName ?? undefined,
          allowCreateTmux: !opts.identityMode, // for identity-birth, tmux session is already created by backend at step 2; open the tab attached to it, do NOT try to create a new one
        });
        selectConversationDeferred(newTabId);
        if (isTouchDevice) navigateToView();
        if (isMobile) setSidebarOpen(false);
      }}
      ```

    - Rationale for `allowCreateTmux: !opts.identityMode`:
      - Regular-session mode: existing behavior preserved (allowCreateTmux: true), user might type a new session name.
      - Identity-birth-success mode: backend already ran `tmux new-session -d -s <name>` at step 2 and launched claude at step 3. Frontend just needs to ATTACH to that existing session, not create another one. Setting `allowCreateTmux: false` prevents a race where the frontend also tries to create the session and either fails ("session already exists") or clobbers by creating a duplicate with an incremented suffix.

    - Check `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx:191` — the type is `onCreateSession?: (opts: { host: Host; sessionName?: string }) => void;`. Update this type to match the new discriminated union OR change to a broader union type. The cleanest fix: import the type from NewSessionDialog.tsx (export the discriminated union type there) and reuse in PrettyConversationsPanel + AppShell.

    - Also verify the `path` field: existing openTab does NOT pass a cwd/path arg. The identity-birth path is used by the backend to `mkdir -p` + `-c <path>` for tmux new-session; the frontend tab doesn't need it because it attaches to an already-configured session. Path is passed BACKEND-only. Do NOT try to plumb it into openTab.

    - Verify no other AppShell call sites depend on the old `onCreateSession` signature: `grep -n "onCreateSession" src/ui/AppShell.tsx` should show only the L1409 site.

    - Do NOT introduce any user-visible message like "opening session" — the switch happens after birth:completed which the modal already handled with the checklist.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx tsc --noEmit -p src/ui/tsconfig.json 2>&1 | tail -10 — MUST show NO errors, indicating the signature threading is complete across AppShell + PrettyConversationsPanel + NewSessionDialog</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "opts.identityMode" src/ui/AppShell.tsx` returns ≥ 1
    - `grep -c "allowCreateTmux" src/ui/AppShell.tsx` returns ≥ 1
    - `grep -c "onCreateSession" src/ui/AppShell.tsx` returns ≥ 1 (unchanged count — only the one handler; DO NOT duplicate)
    - `grep -c "onCreateSession" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` — count matches pre-change; only the type update landed, no signature-change duplication
    - Frontend TS compiles clean (this was BROKEN after plan 05, now fixed)
    - Full frontend test suite passes: `npx vitest run src/ui/` exits 0 (identify + fix any regression in PrettyConversationsPanel.test.tsx if types ripple through)
    - Regular-session flow behavior unchanged: opts.identityMode === false path calls openTab with same args + adds allowCreateTmux:true (same as before)
  </acceptance_criteria>
  <done>AppShell + PrettyConversationsPanel + NewSessionDialog signatures all coherent, TS compiles clean, full frontend test suite passes, focus-follow works via existing openTab machinery.</done>
</task>

</tasks>

<verification>
1. Full frontend test suite passes: `cd /home/ubuntu/skynet && npx vitest run src/ui/` exits 0.
2. Full backend test suite passes: `cd /home/ubuntu/skynet && npx vitest run src/backend/` exits 0.
3. Frontend + backend TS compiles clean.
4. Grep audits:
   - `grep -rc "cancel.*birth\|cancelBirth\|retry.*birth\|retryBirth" src/ui/sidebar/NewSessionDialog.tsx` returns 0 (no cancel/retry affordances)
   - `grep -rc "brief" src/ui/sidebar/NewSessionDialog.tsx` shows brief state/input/payload but no persistence-API calls (localStorage/sessionStorage/etc)
5. Nginx configs unchanged from plan 04 (this plan touches no nginx).
</verification>

<success_criteria>
- Create-with-identity-mode starts the birth SSE stream.
- 5-row progress checklist ticks in real time.
- On success: modal closes + AppShell opens a terminal tab attached to the newly-birthed tmux session; focus lands on the pane so the user is present when the fresh identity's /id create-path prints "No identity found for <name>. Creating one now."
- On failure at step N: step N shows failed + N's blurb renders; user can only close (which resets state); no cancel-during-birth affordance; no retry.
- Regular-session mode behavior preserved verbatim.
- Deploy discipline honored.
</success_criteria>

<output>
Create `.planning/phases/20-identity-creation-ui/20-06-SUMMARY.md` when done. Include:
- Chosen approach for auth-in-fetch (cookie automatic vs explicit header) — for future integrators.
- SSE frame parser edge cases handled (partial chunks, multi-line, ignored non-data-line frames).
- Whether Cancel button was hidden vs disabled during birthing (and rationale).
- Confirmation the AppShell `allowCreateTmux: !opts.identityMode` flag prevents the double-create race.
- Confirmation that ALL 33+ new NewSessionDialog tests (21 from plan 05 + 12 from plan 06) pass.
- Explicit callout that no push/build/deploy was performed.
- Handoff to Ashley: this ships as at least 2 skynet-patches.md entries (backend endpoints in plan 01+02+04; frontend in plan 05+06). Numbering will be #289 (backend avatar batch), #290 (backend collision probe), #291 (backend birth orchestrator + SSE), #292 (frontend modal extension + focus-follow) — final numbers assigned when the deploy is greenlit; do NOT edit skynet-patches.md as part of this phase (per D-CONTEXT §Ship "Deploy per Tina's held-queue posture... Do NOT push / build / recreate without Ashley's explicit greenlight").
</output>
