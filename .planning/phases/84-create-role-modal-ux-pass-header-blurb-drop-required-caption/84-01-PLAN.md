---
phase: 84-create-role-modal-ux-pass-header-blurb-drop-required-caption
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/sidebar/CreateRoleDialog.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "CreateRoleDialog renders a one-sentence header blurb 'A role is what an agent does and how it thinks — many agents can share one.' immediately below the DialogTitle, above the fields (per D-CONTEXT §Implementation Decisions item 1)"
    - "CreateRoleDialog no longer renders the 'These fields are required' / DialogDescription-based required-caption sentence — the required-caption content is deleted from source (per D-CONTEXT §Implementation Decisions item 2)"
    - "CreateRoleDialog no longer contains a 'Then create an identity/agent with this role' checkbox — the <input type='checkbox'>, its <label>, the thenCreateIdentity state hook, its default-value reset, and the chainCheckboxLabel i18n binding are all DELETED from source (not hidden via CSS) (per D-CONTEXT §Implementation Decisions item 3)"
    - "CreateRoleDialog's handleSubmit invokes onChainToCreateIdentity UNCONDITIONALLY on 201 success when the callback prop is provided — no thenCreateIdentity gate remains (per D-CONTEXT §Implementation Decisions items 4 + 5)"
    - "CreateRoleDialog's DialogTitle i18n defaultValue is 'New role' (not 'Create a role') — the t('nav.createRoleTitle', ...) call changes its defaultValue in place; no new i18n key is created (per D-CONTEXT §Implementation Decisions item 6)"
    - "CreateRoleDialog hides the host search input + host listbox when flatHosts.length === 1; the single host is still auto-picked into selectedHost by the existing open-effect (per D-CONTEXT §Implementation Decisions item 8)"
    - "CreateRoleDialog.tsx still compiles under `npx tsc --noEmit` with no NEW CreateRoleDialog-related type errors (the surface change is UI-only; the props interface and public API are unchanged)"
  artifacts:
    - path: "src/ui/sidebar/CreateRoleDialog.tsx"
      provides: "CreateRoleDialog component with header blurb, no required-caption, no chain checkbox, always-chain onChainToCreateIdentity call, 'New role' title, single-host picker suppression"
      contains: "A role is what an agent does and how it thinks"
  key_links:
    - from: "src/ui/sidebar/CreateRoleDialog.tsx handleSubmit success branch"
      to: "onChainToCreateIdentity({role, host, description}) callback prop"
      via: "unconditional call after successful POST /roles (thenCreateIdentity gate removed)"
      pattern: "if \\(onChainToCreateIdentity\\)"
    - from: "CreateRoleDialog DialogTitle"
      to: "PrettyConversationsPanel.tsx:2037 dropdown label 'New role'"
      via: "t('nav.createRoleTitle', { defaultValue: 'New role' }) — title conforms DOWN to dropdown label"
      pattern: "defaultValue: \"New role\""
    - from: "CreateRoleDialog host section"
      to: "flatHosts.length === 1 single-host detection"
      via: "conditional render — search input + listbox skipped when only one pickable host, single host still auto-picked"
      pattern: "flatHosts.length === 1"
---

<objective>
Land all seven surface changes to `src/ui/sidebar/CreateRoleDialog.tsx` as a single cohesive commit surface (items 1, 2, 3, 4, 5, 6, and 8 from D-CONTEXT §Implementation Decisions — item 7 lives in Plan 84-02, item 8 lands here for CreateRole and again in 84-02 for NewSession).

Purpose: tighten the create-role modal away from being form-shaped and into an opinionated flow — header blurb explains what a role IS (paired vocabulary with the future create-agent blurb), the "you must fill these fields" caption goes away since the fields already signal requiredness themselves, the "then create an agent" checkbox goes away entirely (DOM-deleted, not hidden) because a role without an agent is a modeling accident the UI stops advertising, the primary button always advances to the create-agent modal on success (no branching), the just-typed name + description pre-fill carry into the create-agent modal via the existing `onChainToCreateIdentity` callback fired unconditionally, the modal title conforms DOWN to the dropdown label that launched it, and the host picker + search input hide when the user only has one pickable host (single host still auto-selected by the existing open-effect).

Output:
- CreateRoleDialog renders a single short-sentence blurb below the title
- Required-caption sentence is deleted from source
- Chain checkbox + its state + its label + its reset are deleted from source
- handleSubmit's onChainToCreateIdentity call is no longer gated on `thenCreateIdentity`
- t('nav.createRoleTitle', ...) defaultValue is `"New role"`
- Host search input + host listbox are wrapped in a `flatHosts.length !== 1` conditional (still auto-picked when length === 1 by the existing open-effect)
- `npx tsc --noEmit` passes with no new CreateRoleDialog-related errors
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/84-create-role-modal-ux-pass-header-blurb-drop-required-caption/84-CONTEXT.md
@.planning/shapes/shape-create-role-modal-ux-pass.md
@src/ui/sidebar/CreateRoleDialog.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Land all 7 CreateRoleDialog.tsx surface changes (header blurb, drop required-caption, delete chain checkbox, always-chain onChainToCreateIdentity, 'New role' title, hide host picker when single host)</name>
  <files>src/ui/sidebar/CreateRoleDialog.tsx</files>
  <read_first>
    - src/ui/sidebar/CreateRoleDialog.tsx (full 402-line file — L1-27 phase-22 comment header; L29-44 imports; L46-65 isFolder + collectAllHosts helpers; L67-72 ROLE_NAME_PATTERN; L74-101 props interface with onChainToCreateIdentity JSDoc calling out `thenCreateIdentity && onChainToCreateIdentity` gate; L103-121 state hooks including `thenCreateIdentity` at L120 with CHECKED-default comment L117-120; L124-143 derived flatHosts/filteredHosts; L145-165 open-effect that auto-selects single host at L152-154 and resets `thenCreateIdentity` back to TRUE at L161; L167-173 validation; L175-209 handleSubmit — chain call site at L190-192 with `if (thenCreateIdentity && onChainToCreateIdentity)` gate; L211-244 i18n strings — title at L212-214 with `defaultValue: "Create a role"`, description at L215-218, chainCheckboxLabel at L237-239; L246-260 DialogHeader render with DialogTitle at L258 and DialogDescription at L259; L262-357 fields render — search input at L303-314, host listbox at L316-357; L359-369 chain checkbox render; L370-380 submitError inline; L382-400 DialogFooter with Cancel + Create buttons)
    - .planning/phases/84-create-role-modal-ux-pass-header-blurb-drop-required-caption/84-CONTEXT.md (all eight LOCKED decisions in §Implementation Decisions; §Escape-hatch behavior LOCKED — role stays committed if user Escapes after successful POST; §Copy-guard LOCKED — only defaultValue edits in place, no new i18n keys, deleted strings deleted from source)
    - .planning/shapes/shape-create-role-modal-ux-pass.md (§Shape — the 8 changes; §Philosophy — one short sentence, product language not engineering terms; §What would make it wrong — the failure modes to avoid)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx L2034-2039 (the KEEP-ORDER-guarded dropdown menu items — L2037 `{ label: "New role", ... }` is the source-of-truth term the modal title conforms DOWN to; L2036 `{ label: "New agent", ... }` is what Plan 84-02 conforms to)
  </read_first>
  <action>
    In `src/ui/sidebar/CreateRoleDialog.tsx`, land seven surface changes in a single edit pass. The order below matches file order — apply top-to-bottom so line numbers stay predictable within the edit session.

    ────────────────────────────────────────────────────────────────
    CHANGE A — Delete the `thenCreateIdentity` state hook (implements D-CONTEXT item 3, part 1 of 3)
    ────────────────────────────────────────────────────────────────

    Delete lines L117-120 in `src/ui/sidebar/CreateRoleDialog.tsx` — the entire comment block AND the `useState<boolean>(true)` line:

      // D-CONTEXT §UX rules: checkbox DEFAULT TRUE (Alice: "obviously going to
      // want an identity to take on the new role otherwise you'll have a role
      // without any identities").
      const [thenCreateIdentity, setThenCreateIdentity] = useState<boolean>(true);

    After deletion the state block should read (in order): `name`, `description`, `selectedHost`, `search`, `submitting`, `submitError` — no `thenCreateIdentity` line anywhere.

    ────────────────────────────────────────────────────────────────
    CHANGE B — Delete the `thenCreateIdentity` reset in the open-effect (implements D-CONTEXT item 3, part 2 of 3)
    ────────────────────────────────────────────────────────────────

    In the open-effect at L150-165, DELETE the two lines at L160-161:

      // D-CONTEXT default: checkbox back to TRUE on close.
      setThenCreateIdentity(true);

    Do NOT touch the surrounding lines. The auto-select-single-host branch at L152-154 STAYS INTACT — the flatHosts.length === 1 auto-pick is what makes CHANGE F (hide picker) safe: when the picker is hidden, the host is still auto-selected by this existing effect.

    ────────────────────────────────────────────────────────────────
    CHANGE C — Remove the `thenCreateIdentity &&` gate from the chain-hook call (implements D-CONTEXT items 4 + 5)
    ────────────────────────────────────────────────────────────────

    In `handleSubmit` at L188-192, change these three lines:

      // SRIC-05 chain hook: only fire when the checkbox is CHECKED AND the
      // callback prop is provided. Undefined-safe per Test 17b.
      if (thenCreateIdentity && onChainToCreateIdentity) {
        onChainToCreateIdentity({ role: name, host: selectedHost, description });
      }

    Replace with (Phase 84 D-CONTEXT items 4 + 5: primary button always advances; role name + description pre-fill carry via unconditional callback):

      // Phase 84 (D-CONTEXT items 4 + 5): the primary button always advances to
      // the create-agent modal on success. No branching, no gating. Role name
      // + description pre-fill carry via this callback unconditionally. The
      // checkbox that used to gate this call is gone (D-CONTEXT item 3). The
      // callback prop remains optional and undefined-safe for callers that
      // don't opt in to the chain (test-only pattern; production panel wires
      // it — see PrettyConversationsPanel.tsx chainPrefill).
      if (onChainToCreateIdentity) {
        onChainToCreateIdentity({ role: name, host: selectedHost, description });
      }

    ────────────────────────────────────────────────────────────────
    CHANGE D — Change the DialogTitle defaultValue from 'Create a role' to 'New role' (implements D-CONTEXT item 6)
    ────────────────────────────────────────────────────────────────

    At L212-214, change the block:

      const startTitle = t("nav.createRoleTitle", {
        defaultValue: "Create a role",
      });

    to:

      // Phase 84 (D-CONTEXT item 6): modal title conforms DOWN to the dropdown
      // label at PrettyConversationsPanel.tsx:2037 ("New role"). Same i18n key,
      // only the English defaultValue changes in place — no new key. Existing
      // translations continue to render "Create a role" until re-translated;
      // English is the source-of-truth locale for this bounty (per Copy-guard
      // LOCKED in 84-CONTEXT.md).
      const startTitle = t("nav.createRoleTitle", {
        defaultValue: "New role",
      });

    ────────────────────────────────────────────────────────────────
    CHANGE E — Delete the required-caption string binding + delete the chainCheckboxLabel binding (implements D-CONTEXT items 2 + 3 part 3 of 3)
    ────────────────────────────────────────────────────────────────

    In the i18n block at L211-244:

    (E.1) DELETE the entire `startDescription` binding at L215-218:

      const startDescription = t("nav.createRoleDescription", {
        defaultValue:
          "Provision a new role folder on the picked host. Name and description are required.",
      });

    (E.2) DELETE the entire `chainCheckboxLabel` binding at L237-239:

      const chainCheckboxLabel = t("nav.createRoleChainLabel", {
        defaultValue: "Then create an agent with this role",
      });

    After these deletions the i18n block should still contain: `startTitle`, `searchPlaceholder`, `nameLabel`, `namePlaceholder`, `nameErrorText`, `descriptionLabel`, `descriptionPlaceholder`, `cancelLabel`, `openLabel`, `emptyHostsLabel` — but NOT `startDescription`, NOT `chainCheckboxLabel`.

    ────────────────────────────────────────────────────────────────
    CHANGE F — Add header blurb below DialogTitle; delete DialogDescription; wrap host section in single-host guard; delete checkbox render (implements D-CONTEXT items 1, 2, 3, 8)
    ────────────────────────────────────────────────────────────────

    (F.1) In the render at L257-260, change the DialogHeader block:

      <DialogHeader>
        <DialogTitle>{startTitle}</DialogTitle>
        <DialogDescription>{startDescription}</DialogDescription>
      </DialogHeader>

    to (adds header blurb per D-CONTEXT item 1; removes DialogDescription per item 2 — the required-caption content is now deleted from source):

      <DialogHeader>
        <DialogTitle>{startTitle}</DialogTitle>
        {/*
         * Phase 84 (D-CONTEXT item 1): one-sentence header blurb explaining
         * what a role IS. Paired vocabulary with the future create-agent
         * blurb (create-agent-modal-ux-pass bounty carries the sibling copy:
         * "An agent is one specific worker doing a role, with its own name
         * and history."). Product language, not engineering terms — see
         * shape file §Philosophy.
         *
         * Phase 84 (D-CONTEXT item 2): the prior <DialogDescription> that
         * said "Provision a new role folder on the picked host. Name and
         * description are required." is DELETED — the fields themselves
         * already signal required state, no separate caption needed.
         */}
        <DialogDescription>
          A role is what an agent does and how it thinks — many agents can share one.
        </DialogDescription>
      </DialogHeader>

    Rationale for keeping `<DialogDescription>` as the wrapper element (rather than a raw `<p>` or `<span>`): the shadcn Dialog primitive expects a description slot for a11y (aria-describedby wiring on the dialog); using DialogDescription preserves that wiring for the new blurb without adding an extra a11y-only element. The visible text is the blurb; the required-caption content is gone.

    (F.2) At L303-357, wrap the entire search-input + host-listbox block in a `flatHosts.length !== 1` guard. Locate the two contiguous JSX subtrees:

      {/* Host search + picker (same shape as NewSessionDialog L638-696) */}
      <div className="flex items-center gap-2 px-2.5 h-7 ...">
        ... (search input, L303-314) ...
      </div>

      <div
        className="flex flex-col max-h-56 overflow-y-auto ..."
        role="listbox"
        aria-label={t("nav.createRoleHostList", { defaultValue: "Hosts" })}
      >
        ... (filteredHosts render, L316-357) ...
      </div>

    Wrap BOTH divs (L304-314 search + L316-357 listbox) in a single JSX fragment gated on `flatHosts.length !== 1`, with a preceding comment block explaining the LOCKED gate:

      {/*
       * Phase 84 (D-CONTEXT item 8): hide the host search input + host listbox
       * entirely when the user has exactly one pickable host. The existing
       * open-effect at L150-155 (unchanged by this phase) still auto-selects
       * that sole host into `selectedHost`, so submission still works. When
       * the user has zero or ≥2 hosts, both the search box and the listbox
       * render as before. Rationale: Aither Health users (target segment)
       * provision one dedicated VM per user and hit this case constantly;
       * Alice hits it herself. Matched by Plan 84-02 for NewSessionDialog.
       */}
      {flatHosts.length !== 1 && (
        <>
          {/* Host search + picker (same shape as NewSessionDialog L638-696) */}
          <div className="flex items-center gap-2 px-2.5 h-7 bg-[color:var(--color-pv-surface-quiet)] border border-[color:var(--color-pv-border-quiet-strong)] rounded-sm">
            ... (search input JSX EXACTLY as before, byte-preserved) ...
          </div>

          <div
            className="flex flex-col max-h-56 overflow-y-auto border border-[color:var(--color-pv-border-quiet)] rounded-sm"
            role="listbox"
            aria-label={t("nav.createRoleHostList", { defaultValue: "Hosts" })}
          >
            ... (filteredHosts render EXACTLY as before, byte-preserved) ...
          </div>
        </>
      )}

    Preserve the inner JSX byte-for-byte — do NOT change classNames, aria labels, event handlers, disabled attributes, or the filteredHosts.length === 0 empty-state branch. The ONLY change is wrapping both subtrees in the outer `{flatHosts.length !== 1 && (<>...</>)}` gate.

    (F.3) At L359-369, DELETE the entire chain-checkbox render block:

      {/* Chain-into-create-identity checkbox — CHECKED by default (D-CONTEXT §UX rules) */}
      <label className="flex items-center gap-2 text-xs text-[color:var(--color-pv-fg)] cursor-pointer">
        <input
          type="checkbox"
          checked={thenCreateIdentity}
          onChange={(e) => setThenCreateIdentity(e.target.checked)}
          disabled={submitting}
          className="size-3.5 cursor-pointer"
        />
        <span>{chainCheckboxLabel}</span>
      </label>

    The submitError block (L371-379) and DialogFooter (L382-400) STAY INTACT. After deletion, the render tree between the host-section guard and the submitError block should contain no leftover blank lines beyond one — collapse to a single blank line for readability.

    ────────────────────────────────────────────────────────────────
    CHANGE G — Update the top-of-file comment header L1-27 to reflect Phase 84 (single-line addendum)
    ────────────────────────────────────────────────────────────────

    Preserve L1-27 verbatim EXCEPT: insert a single new comment line immediately before the closing `//` line at L27 (`// Zero new npm deps. ...`). The insertion:

      // ─── Phase 84 (D-CONTEXT items 1-6, 8) ─────────────────────────────────
      // UX pass: header blurb below title, required-caption dropped,
      // "Then create an agent" checkbox deleted from DOM, primary button
      // ALWAYS advances to create-agent (chain callback unconditional),
      // modal title conforms to "New role" dropdown label, host picker
      // hidden when only one pickable host (single host still auto-picked).
      // Item 7 (sibling NewSessionDialog title conform) lives in Plan 84-02.

    Do NOT rewrite the Phase 22 SRIC-04 history above — it's still the correct origin story.

    ────────────────────────────────────────────────────────────────
    ANTI-PATTERNS TO AVOID
    ────────────────────────────────────────────────────────────────

    - Do NOT add a new i18n key (e.g. `nav.createRoleBlurb`). Per Copy-guard LOCKED in 84-CONTEXT.md, this phase does not restructure the translation vocabulary. The header blurb is a bare JSX string literal inside `<DialogDescription>`. Downstream translators pick it up on their normal cadence.

    - Do NOT hide the checkbox via `display:none` / `hidden` / `visibility:hidden` / commented-out JSX. Per D-CONTEXT item 3 LOCKED, the checkbox + its state + its label + its reset MUST be DELETED from source. `grep -c "thenCreateIdentity" src/ui/sidebar/CreateRoleDialog.tsx` must return 0 after this task.

    - Do NOT change the `POST /roles` call, the `createRole` import, the `RoleAlreadyExistsError` handling, the ROLE_NAME_PATTERN validation, or the on-open auto-select of a single host. Per shape file §Scope edges, only user-visible strings + the checkbox + the picker-suppression gate change; the atomic-role-creation-then-optional-agent shape is preserved (per D-CONTEXT §Escape-hatch behavior LOCKED: role stays committed if user Escapes after step 1; no rollback).

    - Do NOT extract a shared `useSingleHost(hostTree)` hook or a `HostPickerList` component in this phase. Per shape file §Scope edges and CreateRoleDialog.tsx L48 comment ("extracting is a scope-creep change for this plan"), the refactor is deferred. The `flatHosts.length !== 1` gate lives inline in BOTH dialogs (also in Plan 84-02) — the "shared primitive" per shape terminology is the SHARED PATTERN, not an extracted symbol. This is small enough that a code-shape convention beats an abstraction.

    - Do NOT touch `PrettyConversationsPanel.tsx:2036-2037` — the dropdown labels are the source of truth the modal titles conform DOWN to. The dropdown labels do NOT change (per D-CONTEXT §Deferred: "Changing dropdown labels themselves — they're the source of truth in this bounty").

    - Do NOT change the sizing / theming CSS on the DialogContent (`sm:max-w-md max-h-[90vh]`, the `--pv-hue: 190` custom property, the `text-[color:var(--color-pv-fg-muted)]` label colors, the `text-xs` field sizing). This is a text + control-flow pass, not a re-skin.

    - Do NOT change the escape-hatch behavior. If the user Escapes or clicks-away in the create-agent dialog AFTER a role was successfully created, the role STAYS committed (already POST'd) — this behavior is preserved by keeping `onClose()` and `onCreated(...)` calls exactly where they are today, and by NOT introducing a "cancel-then-rollback" affordance. Per shape §What would make it wrong: "If the escape-hatch behavior stops working... a functional-though-not-idiomatic state has been broken."
  </action>
  <verify>
    <automated>grep -c 'thenCreateIdentity' src/ui/sidebar/CreateRoleDialog.tsx | grep '^0$' && grep -c 'startDescription' src/ui/sidebar/CreateRoleDialog.tsx | grep '^0$' && grep -c 'chainCheckboxLabel' src/ui/sidebar/CreateRoleDialog.tsx | grep '^0$' && grep -c 'nav.createRoleDescription' src/ui/sidebar/CreateRoleDialog.tsx | grep '^0$' && grep -c 'nav.createRoleChainLabel' src/ui/sidebar/CreateRoleDialog.tsx | grep '^0$' && grep -c 'Then create an agent' src/ui/sidebar/CreateRoleDialog.tsx | grep '^0$' && grep -c 'defaultValue: "New role"' src/ui/sidebar/CreateRoleDialog.tsx | grep -v '^0$' && grep -c 'A role is what an agent does and how it thinks' src/ui/sidebar/CreateRoleDialog.tsx | grep -v '^0$' && grep -c 'flatHosts.length !== 1' src/ui/sidebar/CreateRoleDialog.tsx | grep -v '^0$' && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -v 'error' > /dev/null || true</automated>
  </verify>
  <done>
    All acceptance criteria hold:
    - `grep -c 'thenCreateIdentity' src/ui/sidebar/CreateRoleDialog.tsx` returns exactly 0 (state hook + reset + gate + all references gone)
    - `grep -c 'startDescription' src/ui/sidebar/CreateRoleDialog.tsx` returns exactly 0 (required-caption binding gone)
    - `grep -c 'chainCheckboxLabel' src/ui/sidebar/CreateRoleDialog.tsx` returns exactly 0 (checkbox i18n binding gone)
    - `grep -c 'nav.createRoleDescription' src/ui/sidebar/CreateRoleDialog.tsx` returns exactly 0 (i18n key not referenced)
    - `grep -c 'nav.createRoleChainLabel' src/ui/sidebar/CreateRoleDialog.tsx` returns exactly 0 (i18n key not referenced)
    - `grep -c 'Then create an agent' src/ui/sidebar/CreateRoleDialog.tsx` returns exactly 0 (checkbox label text gone)
    - `grep -c 'defaultValue: "New role"' src/ui/sidebar/CreateRoleDialog.tsx` returns exactly 1 (title conforms to dropdown label)
    - `grep -c 'A role is what an agent does and how it thinks' src/ui/sidebar/CreateRoleDialog.tsx` returns exactly 1 (header blurb present, exact string)
    - `grep -c 'flatHosts.length !== 1' src/ui/sidebar/CreateRoleDialog.tsx` returns exactly 1 (single-host picker suppression gate present)
    - `grep -c 'onChainToCreateIdentity' src/ui/sidebar/CreateRoleDialog.tsx` returns ≥ 3 (JSDoc reference + prop destructure + handleSubmit call — still wired)
    - `grep -c 'createRole' src/ui/sidebar/CreateRoleDialog.tsx` returns ≥ 2 (import + handleSubmit call — POST /roles still fires unchanged)
    - `grep -c 'RoleAlreadyExistsError' src/ui/sidebar/CreateRoleDialog.tsx` returns ≥ 2 (409 handling preserved)
    - `grep -c 'Phase 84' src/ui/sidebar/CreateRoleDialog.tsx` returns ≥ 3 (comment header addendum + at least two inline Phase 84 rationale comments)
    - `npx tsc --noEmit` completes without any NEW CreateRoleDialog-related errors (removed state, removed strings, and removed checkbox no longer referenced anywhere in the file)
    - Escape-hatch behavior preserved: `onClose()` is called in handleSubmit's success path exactly as before; no new rollback/undo logic added
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → CreateRoleDialog input fields | User types name + description + host pick. Same trust boundary as pre-phase-84; the field surface is unchanged (only the surrounding chrome changed: header blurb added, required-caption removed, checkbox deleted, single-host picker hidden). |
| CreateRoleDialog → POST /roles | Backend call unchanged — same `createRole(...)` from `identities-api.ts` with same request shape `{ name, description, hostId }`. No new endpoints, no new payload fields. |
| CreateRoleDialog → onChainToCreateIdentity callback | Same-origin JS callback invoked on successful 201 response. Now invoked UNCONDITIONALLY when the callback prop is provided (previously gated on the checkbox). No new data crosses this boundary — the callback payload `{ role, host, description }` is the same as before; only the trigger predicate changed from `thenCreateIdentity && cb` to just `cb`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-01-01 | Tampering | User-supplied role name/description passed to `createRole({ name, description, hostId })` | accept | No new attack surface — same `ROLE_NAME_PATTERN` validation at L168 (kebab-case-lowercase gate) and same backend re-validation at identity-birth-orchestrator.ts:72 (documented as defense-in-depth in the pre-existing L67-72 comment). The Phase 84 changes are UI chrome and control-flow — the input validation path, the network call, and the server-side validation are all unchanged. |
| T-84-01-02 | Elevation of Privilege | onChainToCreateIdentity now fires unconditionally on success | accept | The callback runs in the same origin, in the same authenticated context, and receives the same `{ role, host, description }` payload the CHECKED branch used to receive. The removed check was NOT a security gate — it was a UX opt-in. The callback's downstream consumer (NewSessionDialog via chainPrefill in PrettyConversationsPanel.tsx) already authenticates every operation it performs; it does not confer any elevated privilege on the caller. Per shape §What would make it wrong: "roles-without-agents was a modeling accident the UI stops advertising as a legitimate choice" — the checkbox was the accidental-choice UI, not a security control. |
| T-84-01-03 | Information Disclosure | Header blurb text `"A role is what an agent does and how it thinks — many agents can share one."` embedded as a JSX string literal | accept | Static English text with zero PII, zero credentials, zero configuration exposure. React JSX rendering escapes text nodes by default; no template-injection surface (the string is a compile-time literal, not user data). |
| T-84-01-SC | Tampering | Zero new npm installs in this plan | accept | Pure JSX/TSX edit — no `package.json`, no `package-lock.json`, no `npm install` invocation. Package legitimacy audit N/A. |

Overall UI-only scope for this plan: no backend changes, no auth changes, no data-flow changes, no new endpoints; all new attack surface introduced by Phase 84 is a JSX string literal for the header blurb and a control-flow change that removes a UX gate but does not touch any authentication or authorization path.
</threat_model>

<verification>
Campaign constraint (Alice 2026-09-07, 7-bounty UX-pass string): NO full-suite `npx vitest run`, NO `docker build`, NO `docker cp`, NO `docker compose up`, NO deploy. Executor runs SCOPED tests only. Verification commands for this plan:

- `grep -c 'thenCreateIdentity' src/ui/sidebar/CreateRoleDialog.tsx` returns 0
- `grep -c 'startDescription' src/ui/sidebar/CreateRoleDialog.tsx` returns 0
- `grep -c 'chainCheckboxLabel' src/ui/sidebar/CreateRoleDialog.tsx` returns 0
- `grep -c 'nav.createRoleDescription' src/ui/sidebar/CreateRoleDialog.tsx` returns 0
- `grep -c 'nav.createRoleChainLabel' src/ui/sidebar/CreateRoleDialog.tsx` returns 0
- `grep -c 'Then create an agent' src/ui/sidebar/CreateRoleDialog.tsx` returns 0
- `grep -c 'defaultValue: "New role"' src/ui/sidebar/CreateRoleDialog.tsx` returns 1
- `grep -c 'A role is what an agent does and how it thinks' src/ui/sidebar/CreateRoleDialog.tsx` returns 1
- `grep -c 'flatHosts.length !== 1' src/ui/sidebar/CreateRoleDialog.tsx` returns 1
- `grep -c 'onChainToCreateIdentity' src/ui/sidebar/CreateRoleDialog.tsx` returns ≥ 3 (JSDoc + prop destructure + handleSubmit call site)
- `grep -c 'createRole' src/ui/sidebar/CreateRoleDialog.tsx` returns ≥ 2 (import + call — POST /roles unchanged)
- `grep -c 'RoleAlreadyExistsError' src/ui/sidebar/CreateRoleDialog.tsx` returns ≥ 2 (import + instanceof check — 409 handling preserved)
- `grep -c 'Phase 84' src/ui/sidebar/CreateRoleDialog.tsx` returns ≥ 3
- `npx tsc --noEmit` completes without new CreateRoleDialog-related errors

Note: existing tests in `src/ui/sidebar/CreateRoleDialog.test.tsx` (Tests 11, 17, 18, 20) reference `Then create an agent with this role` and will FAIL after this task lands. Plan 84-03 updates those tests. This plan does NOT re-run the CreateRoleDialog test suite — the failing tests are expected until 84-03 lands.
</verification>

<success_criteria>
- CreateRoleDialog renders a one-sentence header blurb below the title (product language, paired-story vocabulary with the future create-agent blurb)
- Required-caption text is gone from source (DialogDescription content replaced by blurb; the old `nav.createRoleDescription` binding is deleted)
- The chain checkbox is deleted from DOM entirely (state hook, label binding, JSX render, reset — all gone)
- Primary button ALWAYS advances to the create-agent modal on success — the onChainToCreateIdentity callback is invoked unconditionally when the prop is provided
- Role name + description pre-fill carry into the create-agent modal via the same `{role, host, description}` payload the checkbox-gated call used to send
- Modal title reads "New role" (conforms DOWN to the PrettyConversationsPanel dropdown label — source of truth)
- Host search input + host listbox are hidden when the user has exactly one pickable host; single host is still auto-picked by the existing open-effect (so form remains submittable)
- Escape-hatch behavior preserved: if user cancels the downstream create-agent modal after successful role POST, the role stays committed on disk with no rollback
- `npx tsc --noEmit` passes with no new CreateRoleDialog-related type errors
- Item 7 (NewSessionDialog title conform) and item 8's NewSessionDialog side land in Plan 84-02
- Test-side updates for CreateRoleDialog land in Plan 84-03 (Wave 2)
</success_criteria>

<output>
Create `.planning/phases/84-create-role-modal-ux-pass-header-blurb-drop-required-caption/84-01-SUMMARY.md` when done
</output>
</content>
</invoke>