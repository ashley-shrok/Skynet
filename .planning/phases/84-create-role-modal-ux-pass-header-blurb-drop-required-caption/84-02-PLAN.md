---
phase: 84-create-role-modal-ux-pass-header-blurb-drop-required-caption
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/sidebar/NewSessionDialog.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "NewSessionDialog's DialogTitle i18n defaultValue is 'New agent' (not 'Start a new agent') — the t('nav.newSessionTitle', ...) call changes its defaultValue in place at L845-847; no new i18n key is created (per D-CONTEXT §Implementation Decisions item 7)"
    - "NewSessionDialog hides the host search input + host listbox when flatHosts.length === 1; the single host is still auto-picked into selectedHost by the existing open-effect at L413-414 (per D-CONTEXT §Implementation Decisions item 8, NewSessionDialog side)"
    - "The existing L844 `const uiTitle = t('nav.newSession', { defaultValue: 'New agent' });` and its `void uiTitle;` suppression at L871 are NOT touched — they were introduced for a different purpose (a UI title binding kept for future use) and coexist with the DialogTitle binding at L845-847"
    - "NewSessionDialog.tsx still compiles under `npx tsc --noEmit` with no NEW NewSessionDialog-related type errors (the surface change is UI-only; the props interface, formDisabled derivation, canOpen predicate, and birth-stream state are unchanged)"
  artifacts:
    - path: "src/ui/sidebar/NewSessionDialog.tsx"
      provides: "NewSessionDialog component with 'New agent' title and single-host picker suppression"
      contains: "defaultValue: \"New agent\""
  key_links:
    - from: "NewSessionDialog DialogTitle"
      to: "PrettyConversationsPanel.tsx:2036 dropdown label 'New agent'"
      via: "t('nav.newSessionTitle', { defaultValue: 'New agent' }) — title conforms DOWN to dropdown label; paired with Plan 84-01's CreateRoleDialog title-conform"
      pattern: "nav.newSessionTitle"
    - from: "NewSessionDialog host section"
      to: "flatHosts.length === 1 single-host detection"
      via: "conditional render — search input + listbox skipped when only one pickable host, single host still auto-picked by open-effect L413-414"
      pattern: "flatHosts.length === 1"
---

<objective>
Land the two NewSessionDialog surface changes required by Phase 84 as a single cohesive commit surface: (item 7) modal title conforms DOWN to the "New agent" dropdown label, and (item 8, NewSessionDialog side) host picker + search input hide when the user has exactly one pickable host (single host still auto-selected by the existing on-open effect).

Purpose: paired-alignment coherence with Plan 84-01. Both dialogs land the title-conform tweak in the same phase so the alignment intent isn't half-landed at the end of the bounty (per shape §Shape bullet 7). The host-picker suppression primitive lands here so the create-agent-modal-ux-pass follow-up bounty inherits it for free (per shape §Shape bullet 8 and §Scope edges: "The shared 'hide host picker when only one host' primitive itself — built once here, callable from both dialogs").

This plan runs in PARALLEL with Plan 84-01 (Wave 1) — different file (`NewSessionDialog.tsx` vs. `CreateRoleDialog.tsx`), zero `files_modified` overlap.

Output:
- t('nav.newSessionTitle', ...) defaultValue is `"New agent"` (line-in-place change at L845-847)
- Host search input (L890-901) + host listbox (L903-946) are wrapped in a `flatHosts.length !== 1` conditional
- `npx tsc --noEmit` passes with no new NewSessionDialog-related errors
- All existing NewSessionDialog behavior preserved: identity-mode branch, birth-stream state, path field, chain pre-fill (initialHost/initialRole/initialBrief), formDisabled gating, collision precheck — all untouched
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/84-create-role-modal-ux-pass-header-blurb-drop-required-caption/84-CONTEXT.md
@.planning/shapes/shape-create-role-modal-ux-pass.md
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change NewSessionDialog DialogTitle defaultValue to 'New agent' and wrap host search + listbox in single-host suppression guard</name>
  <files>src/ui/sidebar/NewSessionDialog.tsx</files>
  <read_first>
    - src/ui/sidebar/NewSessionDialog.tsx L1-100 (top-of-file comment history + imports — establishes the file's phase-history convention for adding a new Phase 84 addendum)
    - src/ui/sidebar/NewSessionDialog.tsx L258-350 (props destructure L258-289 including `hostTree`, `initialHost`, `initialRole`, `initialBrief`; state hooks L290-350 — do NOT modify any of these)
    - src/ui/sidebar/NewSessionDialog.tsx L359-373 (flatHosts derivation — Patch #111 F4 excludes RDP hosts; same predicate CreateRoleDialog uses; single-host auto-pick key is `flatHosts.length === 1`)
    - src/ui/sidebar/NewSessionDialog.tsx L375-382 (filteredHosts memo — search filter)
    - src/ui/sidebar/NewSessionDialog.tsx L402-469 (open-effect — L413-414 `else if (flatHosts.length === 1) { setSelectedHost(flatHosts[0]); }` is the auto-select-single-host branch that MUST stay intact; it is what makes the picker-suppression safe)
    - src/ui/sidebar/NewSessionDialog.tsx L840-870 (i18n bindings — `uiTitle` at L844, `startTitle` at L845-847 with `defaultValue: "Start a new agent"`, `startDescription` at L848-850 STAYS — do NOT touch it; other bindings unchanged)
    - src/ui/sidebar/NewSessionDialog.tsx L873-946 (DialogContent + DialogHeader at L884-887 with `<DialogTitle>{startTitle}</DialogTitle>`; L889 field container; L890-901 search-input JSX marked "Search input — same visual idiom as HostsPanel.tsx lines 331-347"; L903-946 scrollable-host-list JSX marked "Scrollable host list")
    - src/ui/sidebar/NewSessionDialog.tsx L948-980 (post-listbox JSX — regular session-name input at L948-975, path field starting at L977 — these MUST stay intact; the picker-suppression wrap ends BEFORE this block)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx L2034-2039 (KEEP-ORDER dropdown menu — L2036 `{ label: "New agent", ... }` is the source-of-truth term this dialog's title conforms DOWN to)
    - .planning/phases/84-create-role-modal-ux-pass-header-blurb-drop-required-caption/84-CONTEXT.md §Implementation Decisions items 7 + 8 (LOCKED — item 7 title change, item 8 picker-suppression detection = `collectAllHosts(...).length === 1`)
    - .planning/phases/84-create-role-modal-ux-pass-header-blurb-drop-required-caption/84-01-PLAN.md CHANGE F.2 (the exact JSX-fragment wrapping pattern used on the sibling dialog — Plan 84-02 uses the identical inline gate for byte-shape consistency across both dialogs; NOT extracted into a shared symbol per shape §Scope edges: "Extracting `collectAllHosts` into a shared utility ... — leave for a future refactor phase")
  </read_first>
  <action>
    In `src/ui/sidebar/NewSessionDialog.tsx`, land two changes:

    ────────────────────────────────────────────────────────────────
    CHANGE A — DialogTitle defaultValue: "Start a new agent" → "New agent" (implements D-CONTEXT item 7)
    ────────────────────────────────────────────────────────────────

    At L845-847, change the block:

      const startTitle = t("nav.newSessionTitle", {
        defaultValue: "Start a new agent",
      });

    to (adds Phase 84 rationale comment; changes the English defaultValue in place — no new i18n key per Copy-guard LOCKED):

      // Phase 84 (D-CONTEXT item 7): modal title conforms DOWN to the dropdown
      // label at PrettyConversationsPanel.tsx:2036 ("New agent"). Same i18n
      // key, only the English defaultValue changes in place — no new key.
      // Existing translations continue to render "Start a new agent" until
      // re-translated; English is the source-of-truth locale for this bounty
      // (per Copy-guard LOCKED in 84-CONTEXT.md). Paired with Plan 84-01's
      // sibling change on CreateRoleDialog's title.
      const startTitle = t("nav.newSessionTitle", {
        defaultValue: "New agent",
      });

    Do NOT touch the surrounding bindings:
    - `uiTitle` at L844 (`t("nav.newSession", { defaultValue: "New agent" });`) already reads "New agent" — it's a separate binding kept-for-future-use with a `void uiTitle;` suppression at L871. LEAVE IT ALONE.
    - `startDescription` at L848-850 (`"Pick a host and (optionally) name the agent."`) — LEAVE IT ALONE. Per shape §Scope edges: "The rest of the create-agent modal UX pass (its own bounty, next in the campaign)." The description-tightening is deferred to the create-agent bounty; Phase 84 only conforms the title on this dialog.
    - `searchPlaceholder`, `namePlaceholder`, `nameLabel`, `nameErrorText`, `cancelLabel`, `openLabel`, `emptyHostsLabel` at L851-868 — LEAVE THEM ALONE.

    ────────────────────────────────────────────────────────────────
    CHANGE B — Wrap search-input + scrollable-host-list in a `flatHosts.length !== 1` gate (implements D-CONTEXT item 8, NewSessionDialog side)
    ────────────────────────────────────────────────────────────────

    At L890-946, wrap the two contiguous JSX subtrees — the "Search input" div (L890-901) AND the "Scrollable host list" div (L903-946) — in a single JSX fragment gated on `flatHosts.length !== 1`.

    BEFORE (L890-946):

      <div className="flex flex-col gap-3">
        {/* Search input — same visual idiom as HostsPanel.tsx lines 331-347 */}
        <div className="flex items-center gap-2 px-2.5 h-7 bg-[color:var(--color-pv-surface-quiet)] border border-[color:var(--color-pv-border-quiet-strong)] rounded-sm">
          <Search className="size-3 ..." />
          <input value={search} onChange={...} placeholder={searchPlaceholder} ... />
        </div>

        {/* Scrollable host list */}
        <div
          className="flex flex-col max-h-72 overflow-y-auto ..."
          role="listbox"
          aria-label={t("nav.newSessionHostList", { defaultValue: "Hosts" })}
        >
          {filteredHosts.length === 0 ? (...) : (filteredHosts.map((h) => (...)))}
        </div>

        {/* Regular session-name input — only visible when identity-mode is OFF */}
        {!identityMode && ( ... )}

    AFTER (same content, both host subtrees wrapped in the single-host guard):

      <div className="flex flex-col gap-3">
        {/*
         * Phase 84 (D-CONTEXT item 8): hide the host search input + host listbox
         * entirely when the user has exactly one pickable host. The existing
         * open-effect at L413-414 (unchanged by this phase) still auto-selects
         * that sole host into `selectedHost`, so downstream birth-flow and
         * canOpen predicate at L822-833 still work. When the user has zero or
         * ≥2 pickable hosts, both the search box and the listbox render as
         * before. Rationale: Aither Health users (target segment) provision
         * one dedicated VM per user and hit this case constantly. Matched by
         * Plan 84-01's inline gate on CreateRoleDialog — same shape by design
         * (shape file §Scope edges: shared primitive, but NOT an extracted
         * symbol in this phase).
         */}
        {flatHosts.length !== 1 && (
          <>
            {/* Search input — same visual idiom as HostsPanel.tsx lines 331-347 */}
            <div className="flex items-center gap-2 px-2.5 h-7 bg-[color:var(--color-pv-surface-quiet)] border border-[color:var(--color-pv-border-quiet-strong)] rounded-sm">
              ... (Search icon + input JSX BYTE-PRESERVED from L890-901 body) ...
            </div>

            {/* Scrollable host list */}
            <div
              className="flex flex-col max-h-72 overflow-y-auto border border-[color:var(--color-pv-border-quiet)] rounded-sm"
              role="listbox"
              aria-label={t("nav.newSessionHostList", { defaultValue: "Hosts" })}
            >
              ... (filteredHosts empty-state + map JSX BYTE-PRESERVED from L903-946 body) ...
            </div>
          </>
        )}

        {/* Regular session-name input — only visible when identity-mode is OFF */}
        {!identityMode && ( ... )}
        {/* Path field ... */}

    Preserve the inner JSX byte-for-byte — do NOT change:
    - the search input's `value={search} onChange={(e) => setSearch(e.target.value)}` handlers
    - the search input's `placeholder={searchPlaceholder}`, `aria-label={searchPlaceholder}`, `disabled={formDisabled}` attributes
    - the search input's className (`flex-1 text-xs bg-transparent outline-none placeholder:text-[color:var(--color-pv-fg-dim)] text-[color:var(--color-pv-fg)] min-w-0 disabled:opacity-50`)
    - the scrollable-host-list div's className (`flex flex-col max-h-72 overflow-y-auto border border-[color:var(--color-pv-border-quiet)] rounded-sm`)
    - the `role="listbox"` and `aria-label={t("nav.newSessionHostList", { defaultValue: "Hosts" })}` attributes
    - the `filteredHosts.length === 0` empty-state branch (renders `emptyHostsLabel`)
    - the host `<button role="option" aria-selected={selected} disabled={formDisabled} onClick={() => !formDisabled && setSelectedHost(h)}>` inside the map
    - the host button's className expression (identity-hue-based `bg-[hsla(var(--pv-hue,35),45%,28%,0.42)] ...`)
    - the online-dot span, name span, or username/ip span inside the button

    The ONLY change is wrapping both subtrees in `{flatHosts.length !== 1 && (<>...</>)}` with the Phase 84 rationale comment above the guard.

    The `{!identityMode && ( ... regular session-name input ... )}` block at L948-975 STAYS OUTSIDE the guard — it is a separate field, not part of the host picker. The path field at L977+ ALSO stays outside the guard.

    ────────────────────────────────────────────────────────────────
    CHANGE C — Insert a one-line Phase 84 addendum in the top-of-file comment header
    ────────────────────────────────────────────────────────────────

    Preserve L1-100 verbatim EXCEPT: insert a Phase-84 addendum block BEFORE the `import` block at L~100 (find the first `import` after the header). The insertion:

      // ─── Phase 84 (D-CONTEXT items 7 + 8) ─────────────────────────────────
      // UX pass paired with Plan 84-01 (CreateRoleDialog): modal title
      // conforms to "New agent" dropdown label at
      // PrettyConversationsPanel.tsx:2036 (defaultValue change in place at
      // L845-847, no new i18n key). Host search + host listbox hidden when
      // the user has exactly one pickable host — single host still auto-
      // selected by the existing open-effect. Same inline gate shape as
      // Plan 84-01 (not extracted into a shared symbol; refactor deferred
      // per shape file §Scope edges).

    ────────────────────────────────────────────────────────────────
    ANTI-PATTERNS TO AVOID
    ────────────────────────────────────────────────────────────────

    - Do NOT extract a `useSingleHost(hostTree)` hook or a `HostPickerList` component. Per shape §Scope edges (LOCKED): "Extracting `collectAllHosts` into a shared utility (scope-creep noted in CreateRoleDialog.tsx L48; leave for a future refactor phase)." The "shared primitive" per shape terminology is a shared PATTERN (same inline gate byte-shape in both dialogs), not an extracted symbol. Plan 84-01 uses the exact same inline gate — this is deliberate.

    - Do NOT change `uiTitle` at L844 or its `void uiTitle;` suppression at L871. That binding predates Phase 84 and was introduced for a different purpose (a UI title kept for future use). Its `defaultValue` already reads "New agent" coincidentally; do NOT collapse it with `startTitle`.

    - Do NOT change `startDescription` at L848-850 ("Pick a host and (optionally) name the agent."). The description-tightening on this dialog is deferred to the create-agent-modal-ux-pass follow-up bounty per shape §Scope edges: "The rest of the create-agent modal UX pass (its own bounty, next in the campaign)."

    - Do NOT touch the identity-mode branch, the birth-stream state, the path field, the collision precheck, the initialHost/initialRole/initialBrief chain pre-fill, the roles-for-host effect, the avatar picker, the manual-upload flow, or the `canOpen` predicate at L822-833. Per shape §Scope edges, only user-visible strings (item 7) and the picker-suppression gate (item 8) change on THIS dialog in THIS phase.

    - Do NOT touch `PrettyConversationsPanel.tsx:2034-2039` — the dropdown labels are the source of truth the modal titles conform DOWN to. The dropdown labels do NOT change (per D-CONTEXT §Deferred).

    - Do NOT change `flatHosts` derivation at L359-373. The `.filter((h) => h.enableRdp !== true)` predicate is Patch #111 F4 (RDP-excluded new-session picker parity with the RDP-sentinel-row surface). The single-host detection is `flatHosts.length === 1` AFTER the RDP filter — if the user has one non-RDP host + one RDP host, the RDP one is excluded from `flatHosts` and this dialog treats them as having exactly one pickable host (correct: the RDP host isn't offered here, so hiding the picker is semantically right).

    - Do NOT hide the picker via CSS (`display: none`, `hidden`, `visibility: hidden`). The suppression is a conditional render — the DOM should not contain the search input or the listbox at all when `flatHosts.length === 1`, matching the byte-shape in Plan 84-01.

    - Do NOT combine this change with the create-agent modal's other pending changes (role-picker bug fix, path field admin-split, brief→description rename, TTS speed, title pre-fill unslugify). Those all belong to the create-agent-modal-ux-pass bounty (Deferred per D-CONTEXT). This phase touches ONLY title (item 7) + picker-suppression (item 8) on NewSessionDialog.
  </action>
  <verify>
    <automated>grep -c 'defaultValue: "Start a new agent"' src/ui/sidebar/NewSessionDialog.tsx | grep '^0$' && grep -c 'nav.newSessionTitle' src/ui/sidebar/NewSessionDialog.tsx | grep -v '^0$' && awk '/const startTitle = t\("nav.newSessionTitle"/,/}\);/' src/ui/sidebar/NewSessionDialog.tsx | grep -c 'defaultValue: "New agent"' | grep -v '^0$' && grep -c 'flatHosts.length !== 1' src/ui/sidebar/NewSessionDialog.tsx | grep -v '^0$' && grep -c 'Phase 84' src/ui/sidebar/NewSessionDialog.tsx | grep -v '^0$' && npx tsc --noEmit 2>&1 | grep -v 'error TS' > /dev/null || true</automated>
  </verify>
  <done>
    All acceptance criteria hold:
    - `grep -c 'defaultValue: "Start a new agent"' src/ui/sidebar/NewSessionDialog.tsx` returns exactly 0 (old title text gone)
    - The awk-extracted `startTitle` binding block contains `defaultValue: "New agent"` (the L845-847 binding was updated in place, not the L844 `uiTitle` binding)
    - `grep -c 'nav.newSessionTitle' src/ui/sidebar/NewSessionDialog.tsx` returns exactly 1 (i18n key kept, only defaultValue changed — no new key created)
    - `grep -c 'defaultValue: "Pick a host"' src/ui/sidebar/NewSessionDialog.tsx` returns exactly 1 (startDescription binding STAYS INTACT — description-tightening deferred to next bounty)
    - `grep -c 'flatHosts.length !== 1' src/ui/sidebar/NewSessionDialog.tsx` returns exactly 1 (single-host picker suppression gate present, wrapping BOTH search input and listbox in one fragment)
    - `grep -c 'Phase 84' src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 2 (top-of-file addendum block + inline rationale comment on the picker-suppression gate)
    - `grep -c "role=\"listbox\"" src/ui/sidebar/NewSessionDialog.tsx` returns exactly 1 (listbox still rendered, just now conditionally)
    - `grep -c 'nav.newSessionHostList' src/ui/sidebar/NewSessionDialog.tsx` returns exactly 1 (aria-label binding preserved)
    - `grep -c 'aria-label={searchPlaceholder}' src/ui/sidebar/NewSessionDialog.tsx` returns exactly 1 (search input a11y preserved)
    - `grep -c 'setSelectedHost(flatHosts\[0\])' src/ui/sidebar/NewSessionDialog.tsx` returns exactly 1 (open-effect auto-select-single-host branch at L413-414 preserved verbatim — this is what makes picker-suppression safe)
    - `grep -c 'onChainToCreateIdentity\|initialHost\|initialRole\|initialBrief' src/ui/sidebar/NewSessionDialog.tsx` returns unchanged from pre-phase (chain pre-fill wiring untouched)
    - `npx tsc --noEmit` completes with no NEW NewSessionDialog-related errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → NewSessionDialog input fields | User types agent name + task + brief + title, picks host + role + voice + color hue + avatar. Same trust boundary as pre-phase-84; the field surface is unchanged (only the title text changes and the host picker is conditionally hidden). |
| NewSessionDialog → identity-birth SSE stream | Backend call unchanged — same `POST /identities/birth` (or equivalent) SSE fetch, same payload shape. Phase 84 does not modify the birth call, the birth-progress state machine, or the abort-controller cleanup. |
| NewSessionDialog → onCreate callback | Same-origin JS callback invoked on successful birth. Payload shape unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-02-01 | Tampering | User-supplied agent name / title / brief / task passed to identity-birth backend | accept | No new attack surface — same validation predicates (nameValid, title.trim().length > 0, brief.trim().length > 0, skynetCollision, hostCollision, collisionChecking) in canOpen at L822-833; same backend re-validation at identity-birth-orchestrator. Phase 84 changes only the DialogTitle text and the host picker's render gate — no input path changes. |
| T-84-02-02 | Denial of Service | Hiding the host picker when only one host is present | accept | The host is STILL auto-selected into `selectedHost` by the existing open-effect at L413-414 (unchanged). The `canOpen` predicate at L822-833 requires `selectedHost !== null`, which the auto-select satisfies. No DoS: the form is submittable, just with the picker chrome hidden. If the auto-select ever fails to fire (e.g. race condition), the Create button stays disabled — a functional degradation, not a security failure. |
| T-84-02-03 | Information Disclosure | Modal title text changes from "Start a new agent" to "New agent" | accept | Static English text, zero PII. React JSX renders text nodes escaped. |
| T-84-02-SC | Tampering | Zero new npm installs in this plan | accept | Pure JSX/TSX edit — no `package.json`, no `package-lock.json`, no `npm install` invocation. Package legitimacy audit N/A. |

Overall UI-only scope for this plan: no backend changes, no auth changes, no data-flow changes, no new endpoints; new attack surface is a two-word i18n defaultValue edit + a JSX conditional wrap around a pre-existing DOM subtree.
</threat_model>

<verification>
Campaign constraint (Ashley 2026-09-07, 7-bounty UX-pass string): NO full-suite `npx vitest run`, NO `docker build`, NO `docker cp`, NO `docker compose up`, NO deploy. Executor runs SCOPED tests only. Verification commands for this plan:

- `grep -c 'defaultValue: "Start a new agent"' src/ui/sidebar/NewSessionDialog.tsx` returns 0
- The `awk '/const startTitle = t\("nav.newSessionTitle"/,/}\);/'` extract contains `defaultValue: "New agent"` (verifies the L845-847 binding was updated, not the L844 `uiTitle` binding)
- `grep -c 'nav.newSessionTitle' src/ui/sidebar/NewSessionDialog.tsx` returns 1 (same key, only defaultValue changed)
- `grep -c 'defaultValue: "Pick a host"' src/ui/sidebar/NewSessionDialog.tsx` returns 1 (startDescription NOT touched)
- `grep -c 'flatHosts.length !== 1' src/ui/sidebar/NewSessionDialog.tsx` returns 1
- `grep -c 'Phase 84' src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 2
- `grep -c 'role="listbox"' src/ui/sidebar/NewSessionDialog.tsx` returns 1 (listbox JSX preserved inside the guard)
- `grep -c 'setSelectedHost(flatHosts\[0\])' src/ui/sidebar/NewSessionDialog.tsx` returns 1 (auto-select-single-host branch at L413-414 preserved)
- `npx tsc --noEmit` completes with no new NewSessionDialog-related errors

Note: existing test `src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` Test 5 (L1259+, L1297-1298 asserts `expect(dialog!.textContent).toMatch(/start a new agent/i)`) will FAIL after this task lands. Plan 84-03 updates that assertion. This plan does NOT re-run the PrettyConversationsPanel test suite — the failing assertion is expected until 84-03 lands.
</verification>

<success_criteria>
- NewSessionDialog title reads "New agent" (conforms DOWN to the PrettyConversationsPanel dropdown label at :2036 — source of truth); paired with Plan 84-01's sibling change
- The `nav.newSessionTitle` i18n key is preserved; only its English defaultValue changed in place — no new i18n key created
- The `uiTitle` binding at L844 and the `startDescription` binding at L848-850 are untouched (both stay out of scope per shape §Scope edges)
- Host search input + host listbox are hidden when `flatHosts.length === 1`; single host is still auto-picked into `selectedHost` by the existing open-effect at L413-414 (form remains submittable)
- All other NewSessionDialog behavior preserved: identity-mode branch, birth-stream state, path field, collision precheck, initialHost/initialRole/initialBrief chain pre-fill, roles-for-host effect, avatar picker, manual upload, canOpen predicate — all untouched
- `npx tsc --noEmit` passes with no new NewSessionDialog-related type errors
- Runs in parallel with Plan 84-01 (Wave 1) — zero `files_modified` overlap
- Test-side update (PrettyConversationsPanel.test.tsx Test 5 assertion swap) lands in Plan 84-03 (Wave 2)
</success_criteria>

<output>
Create `.planning/phases/84-create-role-modal-ux-pass-header-blurb-drop-required-caption/84-02-SUMMARY.md` when done
</output>
</content>
</invoke>