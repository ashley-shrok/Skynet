---
phase: 84-create-role-modal-ux-pass-header-blurb-drop-required-caption
plan: 03
type: execute
wave: 2
depends_on:
  - 84-01
  - 84-02
files_modified:
  - src/ui/sidebar/CreateRoleDialog.test.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "PrettyConversationsPanel.test.tsx Test 5 (L1259+) asserts the NewSessionDialog title text matches /new agent/i (not /start a new agent/i) — matches the new t('nav.newSessionTitle', { defaultValue: 'New agent' }) landed in Plan 84-02"
    - "PrettyConversationsPanel.new-role-button.test.tsx Test 21c (L204-226) no longer asserts on the deleted 'then create an agent with this role' checkbox text; it asserts instead on a stable CreateRoleDialog signal (the header blurb 'A role is what an agent does' OR the 'New role' title text) that is unique to CreateRoleDialog vs NewSessionDialog"
    - "CreateRoleDialog.test.tsx Test 11 no longer asserts checkbox presence or CHECKED-by-default state; it asserts the header blurb string 'A role is what an agent does and how it thinks — many agents can share one.' renders below the title AND asserts the required-caption text 'Name and description are required' is NOT in the dialog"
    - "CreateRoleDialog.test.tsx Test 17 (previously 'checkbox CHECKED → chain fires') is updated to assert onChainToCreateIdentity fires unconditionally on success — checkbox interaction is deleted; the rerender-with-undefined-callback branch stays (undefined-safe still verified)"
    - "CreateRoleDialog.test.tsx Test 18 (previously 'checkbox UNCHECKED → chain does NOT fire') is DELETED — the behavior it verified is gone (per D-CONTEXT item 4: primary button ALWAYS advances; there is no un-chain path anymore)"
    - "CreateRoleDialog.test.tsx Test 20 (state-reset on close) no longer asserts checkbox reset-to-CHECKED; it asserts name/description/host reset to defaults only"
    - "CreateRoleDialog.test.tsx gains ONE new test (Test 22, Phase 84): when hostTree has exactly one host, the search input and the host listbox are NOT rendered; the sole host is auto-picked into selectedHost as evidenced by the Create button becoming enabled after typing valid name + description (no host click needed)"
    - "Every touched test file exits 0 under a scoped vitest run of that file only (per campaign constraint: NO full-suite vitest)"
  artifacts:
    - path: "src/ui/sidebar/CreateRoleDialog.test.tsx"
      provides: "CreateRoleDialog test suite aligned with Phase 84 changes — checkbox tests removed/updated, header blurb assertion added, single-host picker-suppression test added"
      contains: "A role is what an agent does and how it thinks"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx"
      provides: "Test 5 title assertion updated to /new agent/i (matches Plan 84-02's title conform)"
      contains: "toMatch(/new agent/i)"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx"
      provides: "Test 21c CreateRoleDialog-detection swapped from deleted checkbox text to a stable Phase-84 signal"
      contains: "A role is what an agent does"
  key_links:
    - from: "PrettyConversationsPanel.test.tsx Test 5 assertion"
      to: "NewSessionDialog's DialogTitle 'New agent' (Plan 84-02 land)"
      via: "regex /new agent/i against dialog textContent"
      pattern: "toMatch\\(/new agent/i\\)"
    - from: "CreateRoleDialog.test.tsx Test 22 (new)"
      to: "CreateRoleDialog single-host picker-suppression gate (Plan 84-01 CHANGE F.2)"
      via: "renders with makeHostTree([one host]); asserts queryByRole('listbox') is null AND Create button is enabled after typing name + description"
      pattern: "queryByRole\\(\"listbox\"\\)"
---

<objective>
Update the three test files that reference Phase 84's changed surfaces so they align with the code landed in Plans 84-01 and 84-02. This runs in Wave 2 because the assertions must match the LANDED code — running before 84-01/84-02 would either falsify green tests prematurely or update tests without a code target.

Purpose: keep the CreateRoleDialog test suite and the PrettyConversationsPanel test suite honest as source-of-truth executable specifications after the seven surface changes land. Delete assertions that no longer describe reality (checkbox tests), add assertions that describe the new reality (header blurb, single-host picker suppression), and swap dialog-detection signals to Phase-84-stable strings.

This is not a green-run gate — per campaign constraint (Alice 2026-09-07, 7-bounty UX-pass string), no full-suite vitest runs during this phase. The executor runs scoped vitest ONLY on the three test files touched here, plus the two source-of-truth files those tests import (`CreateRoleDialog.tsx`, `NewSessionDialog.tsx` — via the panel-mount transitively). Exit 0 on the three scoped runs is the acceptance signal.

Output:
- PrettyConversationsPanel.test.tsx Test 5 title regex flipped
- PrettyConversationsPanel.new-role-button.test.tsx Test 21c dialog-detection swapped to a Phase-84-stable signal
- CreateRoleDialog.test.tsx Tests 11, 17, 20 updated; Test 18 deleted; new Test 22 added
- Three scoped vitest runs all exit 0
- Test-file comment headers updated with a Phase 84 addendum noting the delta
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/84-create-role-modal-ux-pass-header-blurb-drop-required-caption/84-CONTEXT.md
@.planning/phases/84-create-role-modal-ux-pass-header-blurb-drop-required-caption/84-01-PLAN.md
@.planning/phases/84-create-role-modal-ux-pass-header-blurb-drop-required-caption/84-02-PLAN.md
@src/ui/sidebar/CreateRoleDialog.test.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PrettyConversationsPanel.test.tsx Test 5 and PrettyConversationsPanel.new-role-button.test.tsx Test 21c to match Phase 84 changes</name>
  <files>
    src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx,
    src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx
  </files>
  <read_first>
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx L1259-1300 (Test 5 — the ONLY assertion this plan touches in this file; at L1297-1298: `expect(dialog!.textContent).toMatch(/start a new agent/i)`. The rest of the file's 1200+ lines is out of scope.)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx L1-170 (test-file mocks + fixtures — establishes ONE_HOST_TREE fixture at L152-155; helpers openMenuAndClickItem at L162-168)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx L170-227 (all three tests — Test 21a menu-item-present, Test 21b menu-absent-when-no-onCreateSession, Test 21c click-through-opens-dialog with the L225 assertion `expect(dialog!.textContent).toMatch(/then create an agent with this role/i)` which references the DELETED checkbox text)
    - .planning/phases/84-create-role-modal-ux-pass-header-blurb-drop-required-caption/84-01-PLAN.md (CHANGE F.1 adds the exact blurb string `"A role is what an agent does and how it thinks — many agents can share one."` inside a `<DialogDescription>`; CHANGE D changes the title to `"New role"`)
    - .planning/phases/84-create-role-modal-ux-pass-header-blurb-drop-required-caption/84-02-PLAN.md (CHANGE A changes NewSessionDialog title to `"New agent"`)
  </read_first>
  <action>
    ────────────────────────────────────────────────────────────────
    EDIT 1 — PrettyConversationsPanel.test.tsx Test 5 title-regex flip
    ────────────────────────────────────────────────────────────────

    In `src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx`, at L1297-1298:

    BEFORE:

        // The dialog contains "Start a new agent" title text (i18n defaultValue).
        expect(dialog!.textContent).toMatch(/start a new agent/i);

    AFTER:

        // Phase 84 (Plan 84-02): NewSessionDialog title now reads "New agent"
        // (conforms DOWN to the "New agent" dropdown item at
        // PrettyConversationsPanel.tsx:2036 — dropdown is source of truth).
        // The i18n key nav.newSessionTitle is unchanged; only its English
        // defaultValue changed in place.
        expect(dialog!.textContent).toMatch(/new agent/i);

    Do NOT touch any other Test 5 lines. Do NOT touch any other test in this file — Tests 1-4 and Test 6 remain byte-untouched. The 1000+ other lines in this file are out of scope.

    Also update Test 5's describe/it title at L1259 — the `Phase 23 GEFM-01 repoint` bracket still applies (Test 5's dropdown-click flow is unchanged), so the it-title only needs a Phase-84 addendum. Change:

      it("Test 5 (Phase 23 GEFM-01 repoint): clicking the MoreVertical menu button + selecting 'New agent' opens the NewSessionDialog; menu button carries pv-pencil class + data-testid=pv-header-menu-button", async () => {

    to:

      it("Test 5 (Phase 23 GEFM-01 repoint; Phase 84 Plan 02 title-conform): clicking the MoreVertical menu button + selecting 'New agent' opens the NewSessionDialog (title now reads 'New agent'); menu button carries pv-pencil class + data-testid=pv-header-menu-button", async () => {

    ────────────────────────────────────────────────────────────────
    EDIT 2 — PrettyConversationsPanel.new-role-button.test.tsx Test 21c dialog-detection swap
    ────────────────────────────────────────────────────────────────

    In `src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx`, at L204-226 (Test 21c). The problematic assertion is at L224-225:

      // Look for the chain checkbox — unique to CreateRoleDialog vs NewSessionDialog.
      expect(dialog!.textContent).toMatch(/then create an agent with this role/i);

    This reference is now a hard failure — Plan 84-01 DELETED that checkbox from CreateRoleDialog. Swap the detection signal to one of the Phase-84-stable strings that is uniquely present in CreateRoleDialog but NOT in NewSessionDialog:

    BEFORE:

      // Look for the chain checkbox — unique to CreateRoleDialog vs NewSessionDialog.
      expect(dialog!.textContent).toMatch(/then create an agent with this role/i);

    AFTER:

      // Phase 84 (Plan 84-01): the "Then create an agent" checkbox that used
      // to be this suite's dialog-detection signal was deleted from DOM (per
      // D-CONTEXT item 3 — the checkbox goes away entirely because a role
      // without an agent is a modeling accident the UI stops advertising).
      //
      // New detection signal: the header blurb "A role is what an agent
      // does and how it thinks — many agents can share one." is unique to
      // CreateRoleDialog. NewSessionDialog's description is "Pick a host
      // and (optionally) name the agent." — no overlap in the matched
      // substring "A role is what an agent does".
      expect(dialog!.textContent).toMatch(/a role is what an agent does/i);

    Rationale for choosing the blurb over the title: NewSessionDialog's title is now "New agent" (Plan 84-02) and CreateRoleDialog's title is now "New role" (Plan 84-01). Both are two-word titles with overlapping words — the blurb string uniquely distinguishes CreateRoleDialog. `toMatch(/new role/i)` would also work but relies on the boundary between "New role" and "New agent" not appearing in the same dialog textContent — the blurb regex is more forgiving to future copy tweaks and more descriptive of what's being asserted.

    Update the it-title at L204 with a Phase 84 addendum:

    BEFORE:

      it("Test 21c (repoint): clicking 'New role' from the menu opens CreateRoleDialog", () => {

    AFTER:

      it("Test 21c (Phase 23 GEFM-01 repoint; Phase 84 Plan 01 signal swap): clicking 'New role' from the menu opens CreateRoleDialog (detection signal now the header blurb, not the deleted 'then create an agent' checkbox)", () => {

    Do NOT touch Test 21a or Test 21b. Do NOT touch the top-of-file comment header, the mocks, the ONE_HOST_TREE fixture, or the openMenuAndClickItem helper.

    ────────────────────────────────────────────────────────────────
    ANTI-PATTERNS TO AVOID
    ────────────────────────────────────────────────────────────────

    - Do NOT add new assertions to Test 5 or Test 21c beyond what's specified. Every added assertion is a potential future churn point; the goal is minimum-diff realignment to Phase 84 realities.

    - Do NOT change the regex flag `/i` — the dialog textContent contains extra whitespace and quote-mark normalization from the shadcn Dialog primitive; case-insensitive matching stays.

    - Do NOT change the dialog query selector `document.querySelector('[role="dialog"]')` — the shadcn Dialog primitive still portal-mounts with `role="dialog"`. Only the textContent regex changes.

    - Do NOT touch any test in PrettyConversationsPanel.test.tsx outside Test 5 (L1259-1300). Tests 1-4 (dialog-agnostic) and Test 6 (menu-button-gate) remain byte-untouched.

    - Do NOT run the full PrettyConversationsPanel test suite. Per campaign constraint (Alice 2026-09-07): scoped runs only. The verify block below specifies `npx vitest run <file-path>` for each of the three touched files.
  </action>
  <verify>
    <automated>grep -c 'toMatch(/start a new agent/i)' src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx | grep '^0$' && grep -c 'toMatch(/new agent/i)' src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx | grep -v '^0$' && grep -c 'then create an agent with this role' src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx | grep '^0$' && grep -c 'a role is what an agent does' src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx | grep -v '^0$' && npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx</automated>
  </verify>
  <done>
    - `grep -c 'toMatch(/start a new agent/i)' src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` returns 0
    - `grep -c 'toMatch(/new agent/i)' src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` returns ≥ 1 (Test 5 assertion flipped)
    - `grep -c 'then create an agent with this role' src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx` returns 0
    - `grep -c 'a role is what an agent does' src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx` returns ≥ 1 (Test 21c detection signal swapped)
    - `grep -c 'Phase 84' src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` returns ≥ 1 (Test 5 addendum comment present)
    - `grep -c 'Phase 84' src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx` returns ≥ 1 (Test 21c addendum comment present)
    - Scoped `npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx` exits 0 (both scoped runs green)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CreateRoleDialog.test.tsx — delete checkbox assertions, delete Test 18, add header-blurb assertion to Test 11, update Test 17 to unconditional-chain semantics, update Test 20 to drop checkbox reset, add new Test 22 for single-host picker suppression</name>
  <files>src/ui/sidebar/CreateRoleDialog.test.tsx</files>
  <read_first>
    - src/ui/sidebar/CreateRoleDialog.test.tsx L1-30 (top-of-file comment header — establishes the Test 11-20 behavior spec; this comment header MUST be updated with a Phase 84 addendum block enumerating the Test 11/17/18/20/22 deltas)
    - src/ui/sidebar/CreateRoleDialog.test.tsx L31-105 (mocks: react-i18next passthrough, `@/api/identities-api.createRole` mock via mockCreateRole, RoleAlreadyExistsError real class; makeHost + makeHostTree fixtures; beforeEach/afterEach — do NOT touch any of these)
    - src/ui/sidebar/CreateRoleDialog.test.tsx L108-138 (Test 11 — renders Name + Description + Host picker + chain checkbox CHECKED-by-default; L132-138 asserts checkbox presence and checked=true)
    - src/ui/sidebar/CreateRoleDialog.test.tsx L140-167 (Test 12 name validation — no chain-checkbox references, out of scope)
    - src/ui/sidebar/CreateRoleDialog.test.tsx L169-190 (Test 13 description validation — no chain-checkbox references, out of scope)
    - src/ui/sidebar/CreateRoleDialog.test.tsx L192-217 (Test 14 host validation — no chain-checkbox references, out of scope; but note this test uses a 2-host tree so the picker is still rendered and clicking hostA is required. Do NOT touch.)
    - src/ui/sidebar/CreateRoleDialog.test.tsx L219-231 (Test 15 auto-select single host — the behavior this tests is PRESERVED in Plan 84-01 CHANGE B — the open-effect auto-select branch stays. BUT the assertion at L230 relies on the option element being in the DOM. Under Plan 84-01 CHANGE F.2, when flatHosts.length === 1 the listbox is HIDDEN, so `screen.getByRole("option", { name: /onlyHost/ })` will throw. Test 15 needs a semantics update — see EDIT 15 below.)
    - src/ui/sidebar/CreateRoleDialog.test.tsx L233-258 (Test 16 createRole call payload — uses a 1-host tree; auto-select fires; the Create button is clicked without host click. This test's assertion is on mockCreateRole payload, not on any listbox interaction, so it CONTINUES TO PASS after Plan 84-01. Do NOT touch.)
    - src/ui/sidebar/CreateRoleDialog.test.tsx L260-301 (Test 17 chain-CHECKED-then-fires + undefined-safe rerender branch — must be split: delete the checkbox-CHECKED prose but keep the two-part semantic — chain fires when callback provided, callback undefined does not crash)
    - src/ui/sidebar/CreateRoleDialog.test.tsx L303-327 (Test 18 chain-UNCHECKED-then-not-fires — DELETE ENTIRELY; the behavior it verifies is gone per D-CONTEXT item 4)
    - src/ui/sidebar/CreateRoleDialog.test.tsx L329-356 (Test 19 409 conflict — no chain-checkbox references, out of scope)
    - src/ui/sidebar/CreateRoleDialog.test.tsx L358-418 (Test 20 state reset on close — asserts checkbox back to CHECKED at L413-416; that assertion is deleted per Plan 84-01 which removed the state hook entirely)
    - .planning/phases/84-create-role-modal-ux-pass-header-blurb-drop-required-caption/84-01-PLAN.md (CHANGE A deletes thenCreateIdentity state; CHANGE B deletes reset; CHANGE C removes gate; CHANGE F.1 adds header blurb; CHANGE F.2 wraps picker in flatHosts.length !== 1 guard; CHANGE F.3 deletes checkbox render)
  </read_first>
  <action>
    In `src/ui/sidebar/CreateRoleDialog.test.tsx`, land the following edits. Order matches file order — top-to-bottom keeps line numbers predictable.

    ────────────────────────────────────────────────────────────────
    EDIT HEADER — Add a Phase 84 addendum to the top-of-file comment header
    ────────────────────────────────────────────────────────────────

    At L1-29, immediately AFTER the existing L28-29 lines (which currently end with `// so those mocks are omitted.`), append a new block:

      //
      // ─── Phase 84 (Plan 84-03 test-side updates for Plan 84-01 code) ────────
      // The seven Plan 84-01 changes to CreateRoleDialog.tsx invalidate parts
      // of the original Phase 22 behavior spec above. Deltas:
      //   Test 11: no longer asserts checkbox; instead asserts the new
      //     header blurb ("A role is what an agent does and how it thinks —
      //     many agents can share one.") renders below the title, and asserts
      //     the deleted required-caption text ("Name and description are
      //     required") is NOT present.
      //   Test 15: auto-select-single-host behavior is preserved but the
      //     listbox is now HIDDEN when flatHosts.length === 1; the assertion
      //     shifts from "aria-selected on the option" to "no listbox, but
      //     the sole host is still selected as evidenced by canOpen".
      //   Test 17: onChainToCreateIdentity fires unconditionally on success
      //     (no more thenCreateIdentity gate). The two-part semantic split
      //     stays — cb-provided fires; cb-undefined is safe.
      //   Test 18: DELETED. The "checkbox UNCHECKED → chain does NOT fire"
      //     branch no longer exists (D-CONTEXT item 4: primary button ALWAYS
      //     advances).
      //   Test 20: state reset no longer includes the checkbox — the state
      //     hook itself was deleted (Plan 84-01 CHANGE A).
      //   Test 22 (NEW): single-host picker suppression — hostTree with one
      //     host renders no listbox + no search input, and Create is
      //     enable-able without a host click.

    ────────────────────────────────────────────────────────────────
    EDIT 11 — Test 11: swap the checkbox assertion for the header-blurb + required-caption-gone assertion
    ────────────────────────────────────────────────────────────────

    In Test 11 at L109-138. The block at L132-138 currently reads:

        // Chain-checkbox present + CHECKED by default (D-CONTEXT §UX rules)
        const checkbox = screen.getByRole("checkbox", {
          name: /then create an agent with this role/i,
        }) as HTMLInputElement;
        expect(checkbox).toBeTruthy();
        expect(checkbox.checked).toBe(true);

    Replace with:

        // Phase 84 (Plan 84-01 CHANGE F.3): the chain-checkbox is DELETED
        // from DOM entirely. Assert its ABSENCE rather than its presence.
        expect(
          screen.queryByRole("checkbox", {
            name: /then create an agent with this role/i,
          }),
        ).toBeNull();

        // Phase 84 (Plan 84-01 CHANGE F.1): the header blurb renders below
        // the title, above the fields. Exact string from D-CONTEXT item 1
        // (LOCKED for planning; user may redirect during execute).
        expect(
          screen.getByText(
            /A role is what an agent does and how it thinks — many agents can share one\./,
          ),
        ).toBeTruthy();

        // Phase 84 (Plan 84-01 CHANGE E.1 + F.1): the pre-Phase-84
        // required-caption sentence ("... Name and description are
        // required.") is DELETED from source — the DialogDescription now
        // holds the blurb instead. Assert the old caption text is gone.
        expect(
          screen.queryByText(/Name and description are required/i),
        ).toBeNull();

    Also update the it-title on L109:

    BEFORE:

      it("Test 11: renders Name input, Description textarea, Host picker, and 'Then create an agent with this role' checkbox CHECKED by default", () => {

    AFTER:

      it("Test 11 (Phase 84 Plan 03): renders Name input, Description textarea, Host picker; header blurb present below title; required-caption + chain-checkbox both DELETED from DOM", () => {

    ────────────────────────────────────────────────────────────────
    EDIT 15 — Test 15: single-host auto-select behavior preserved; assertion shifts from listbox-option to picker-suppression + canOpen
    ────────────────────────────────────────────────────────────────

    Test 15 currently at L219-231:

      it("Test 15: Auto-select single host on open (mirrors NewSessionDialog)", () => {
        render(
          <CreateRoleDialog
            open={true}
            onClose={() => {}}
            hostTree={makeHostTree([makeHost("h1", "onlyHost")])}
          />,
        );

        // Single-host tree → auto-select. The option should render aria-selected=true.
        const opt = screen.getByRole("option", { name: /onlyHost/ });
        expect(opt.getAttribute("aria-selected")).toBe("true");
      });

    Under Plan 84-01 CHANGE F.2, when `flatHosts.length === 1` the listbox is NOT rendered, so `getByRole("option")` throws. But the auto-select behavior IS preserved (CHANGE B keeps the open-effect intact). Replace Test 15 body:

      it("Test 15 (Phase 84 Plan 03): Auto-select single host on open — listbox is HIDDEN per Plan 84-01 CHANGE F.2, but sole host is still auto-picked as evidenced by canOpen predicate", () => {
        render(
          <CreateRoleDialog
            open={true}
            onClose={() => {}}
            hostTree={makeHostTree([makeHost("h1", "onlyHost")])}
          />,
        );

        // Phase 84 (Plan 84-01 CHANGE F.2): single-host picker suppression
        // means the listbox is NOT rendered. Assert its absence.
        expect(screen.queryByRole("listbox")).toBeNull();
        // The search input is inside the same guard — also absent.
        expect(screen.queryByPlaceholderText(/search hosts/i)).toBeNull();

        // The auto-select branch in the open-effect (Plan 84-01 CHANGE B kept
        // this intact) still fires. Evidence: after typing valid name + valid
        // description, the Create button becomes enabled — which requires
        // selectedHost !== null in the canOpen predicate.
        fireEvent.change(screen.getByLabelText(/name/i), {
          target: { value: "box-maintainer" },
        });
        fireEvent.change(screen.getByLabelText(/description/i), {
          target: { value: "d1" },
        });
        const createBtn = screen.getByRole("button", { name: /create/i }) as HTMLButtonElement;
        expect(createBtn.disabled).toBe(false);
      });

    ────────────────────────────────────────────────────────────────
    EDIT 17 — Test 17: rename + rebody to unconditional-chain semantics; keep the undefined-safe rerender branch
    ────────────────────────────────────────────────────────────────

    Test 17 currently at L260-301. Its two-part shape (checkbox-CHECKED → chain fires; rerender without callback → no crash) becomes a two-part shape without the checkbox interaction (chain fires unconditionally when callback provided; rerender without callback → no crash).

    Replace the entire Test 17 body:

      it("Test 17 (Phase 84 Plan 03): On successful submit, onChainToCreateIdentity is invoked UNCONDITIONALLY when the callback prop is provided (checkbox gate removed per Plan 84-01 CHANGE C); also safe when the callback prop is undefined", async () => {
        const chainSpy = vi.fn();
        const onClose = vi.fn();
        const { rerender } = render(
          <CreateRoleDialog
            open={true}
            onClose={onClose}
            hostTree={makeHostTree([makeHost("42", "hostA")])}
            onChainToCreateIdentity={chainSpy}
          />,
        );

        // Note: single-host tree means the listbox is suppressed and hostA
        // is auto-picked (Plan 84-01 CHANGE F.2 + B). No option click needed.
        fireEvent.change(screen.getByLabelText(/name/i), {
          target: { value: "box-maintainer" },
        });
        fireEvent.change(screen.getByLabelText(/description/i), {
          target: { value: "d1" },
        });
        fireEvent.click(screen.getByRole("button", { name: /create/i }));

        await waitFor(() => expect(chainSpy).toHaveBeenCalledTimes(1));
        expect(chainSpy).toHaveBeenCalledWith({
          role: "box-maintainer",
          host: expect.objectContaining({ id: "42", name: "hostA" }),
          description: "d1",
        });
        expect(onClose).toHaveBeenCalled();

        // Phase 84 (Plan 84-01 CHANGE C): the checkbox gate is gone; the
        // callback fires whenever the callback prop is provided. Below we
        // render WITHOUT the prop — the callback is undefined and the
        // handleSubmit branch `if (onChainToCreateIdentity)` guards it.
        rerender(
          <CreateRoleDialog
            open={true}
            onClose={onClose}
            hostTree={makeHostTree([makeHost("42", "hostA")])}
          />,
        );
        fireEvent.change(screen.getByLabelText(/name/i), {
          target: { value: "another-role" },
        });
        fireEvent.change(screen.getByLabelText(/description/i), {
          target: { value: "d2" },
        });
        fireEvent.click(screen.getByRole("button", { name: /create/i }));

        await waitFor(() => {
          // The second createRole call fires without throwing
          expect(mockCreateRole).toHaveBeenCalledTimes(2);
        });
      });

    ────────────────────────────────────────────────────────────────
    EDIT 18 — DELETE Test 18 entirely
    ────────────────────────────────────────────────────────────────

    Delete the entire Test 18 block at L303-327:

      it("Test 18: On successful submit with checkbox UNCHECKED, onChainToCreateIdentity is NOT invoked", async () => {
        ... whole body ...
      });

    Replace with a single-line replacement comment (so the it-block index/order is documented as intentional, not a bug):

      // Test 18 (Phase 84 Plan 03): DELETED. The behavior it verified
      // ("checkbox UNCHECKED → chain does NOT fire") no longer exists in
      // CreateRoleDialog — per D-CONTEXT item 4 LOCKED, the primary button
      // ALWAYS advances to the create-agent modal on success. There is no
      // un-chain path in the component anymore, so there is nothing to test.

    ────────────────────────────────────────────────────────────────
    EDIT 20 — Test 20: drop the checkbox reset-to-CHECKED assertion; keep the name/description/host reset assertions
    ────────────────────────────────────────────────────────────────

    Test 20 currently at L358-417. The problematic block is at L376-380 (fill: click the checkbox to UNCHECK it) and L413-416 (assert: checkbox back to CHECKED). Both must go.

    Replace the entire Test 20 body:

      it("Test 20 (Phase 84 Plan 03): On modal close, all state resets (name, description, host). The checkbox reset assertion is DELETED — the state hook itself was deleted (Plan 84-01 CHANGE A).", async () => {
        let openState = true;
        const setOpen = (v: boolean) => { openState = v; };
        const { rerender } = render(
          <CreateRoleDialog
            open={openState}
            onClose={() => setOpen(false)}
            hostTree={makeHostTree([
              makeHost("1", "hostA"),
              makeHost("2", "hostB"),
            ])}
          />,
        );

        // Fill fields — 2-host tree means the listbox IS rendered (Plan 84-01
        // CHANGE F.2 only suppresses at length === 1). Click hostB to pick it.
        fireEvent.change(screen.getByLabelText(/name/i), {
          target: { value: "box-maintainer" },
        });
        fireEvent.change(screen.getByLabelText(/description/i), {
          target: { value: "d1" },
        });
        fireEvent.click(screen.getByRole("option", { name: /hostB/ }));

        // Close (open=false)
        rerender(
          <CreateRoleDialog
            open={false}
            onClose={() => setOpen(false)}
            hostTree={makeHostTree([
              makeHost("1", "hostA"),
              makeHost("2", "hostB"),
            ])}
          />,
        );

        // Re-open — all remaining state should be reset
        rerender(
          <CreateRoleDialog
            open={true}
            onClose={() => setOpen(false)}
            hostTree={makeHostTree([
              makeHost("1", "hostA"),
              makeHost("2", "hostB"),
            ])}
          />,
        );

        // Name empty
        expect((screen.getByLabelText(/name/i) as HTMLInputElement).value).toBe("");
        // Description empty
        expect((screen.getByLabelText(/description/i) as HTMLTextAreaElement).value).toBe("");
        // Neither host selected (two hosts → no auto-select)
        expect(
          screen.getByRole("option", { name: /hostA/ }).getAttribute("aria-selected"),
        ).toBe("false");
        expect(
          screen.getByRole("option", { name: /hostB/ }).getAttribute("aria-selected"),
        ).toBe("false");

        // Phase 84 (Plan 84-01 CHANGE A): the thenCreateIdentity state hook
        // was DELETED. There is no checkbox to assert reset-to-CHECKED on.
        // Verify no such checkbox exists in the re-opened dialog at all:
        expect(
          screen.queryByRole("checkbox", {
            name: /then create an agent with this role/i,
          }),
        ).toBeNull();
      });

    ────────────────────────────────────────────────────────────────
    EDIT 22 — Add a new Test 22 for single-host picker suppression
    ────────────────────────────────────────────────────────────────

    IMMEDIATELY BEFORE the closing `});` of the outer `describe("CreateRoleDialog", () => { ... });` block (which is currently at L418), insert:

      it("Test 22 (Phase 84 Plan 03; implements Plan 84-01 CHANGE F.2): with a single-host hostTree, the search input and the host listbox are NOT rendered; the sole host is still auto-picked into selectedHost, so Create becomes enabled once name + description are valid", () => {
        render(
          <CreateRoleDialog
            open={true}
            onClose={() => {}}
            hostTree={makeHostTree([makeHost("42", "onlyHost")])}
          />,
        );

        // Picker chrome absent
        expect(screen.queryByRole("listbox")).toBeNull();
        expect(screen.queryByPlaceholderText(/search hosts/i)).toBeNull();

        // The single host has no option button either — the whole subtree
        // is guarded by flatHosts.length !== 1.
        expect(screen.queryByRole("option", { name: /onlyHost/ })).toBeNull();

        // But the sole host IS auto-picked into selectedHost (Plan 84-01
        // CHANGE B kept the open-effect's auto-select-single-host branch
        // intact). Evidence: Create button enables after valid name + desc.
        fireEvent.change(screen.getByLabelText(/name/i), {
          target: { value: "box-maintainer" },
        });
        fireEvent.change(screen.getByLabelText(/description/i), {
          target: { value: "d1" },
        });
        const createBtn = screen.getByRole("button", { name: /create/i }) as HTMLButtonElement;
        expect(createBtn.disabled).toBe(false);
      });

    ────────────────────────────────────────────────────────────────
    ANTI-PATTERNS TO AVOID
    ────────────────────────────────────────────────────────────────

    - Do NOT touch Tests 12, 13, 14, 16, 19. Their assertions are chain-checkbox-independent and remain valid after Plan 84-01. Test 14 uses a 2-host tree, so the listbox is rendered — its `getByRole("option", { name: /hostA/ })` still resolves. Test 16 uses a 1-host tree; the auto-select fires; the listbox is now hidden but the test never queries for the option — it only checks mockCreateRole payload.

    - Do NOT modify the top-of-file mocks (react-i18next passthrough, mockCreateRole, RoleAlreadyExistsError import, makeHost, makeHostTree, beforeEach). Only the top-of-file COMMENT header at L1-29 gets a Phase 84 addendum block; the executable code below is preserved except where Tests 11, 15, 17, 18, 20, 22 change.

    - Do NOT restructure the outer `describe("CreateRoleDialog", () => { ... });` block. The tests remain nested under the same describe.

    - Do NOT rename mockCreateRole, chainSpy, onClose, or any other test-local variable — the refactored bodies still use the same names.

    - Do NOT add a new i18n key or change the react-i18next mock. The blurb assertion uses the raw JSX string literal (which the mock passes through via `defaultValue ?? key` — the blurb has no i18n key, so it just renders the literal). Verify: the mock returns `opts?.defaultValue ?? key`; the blurb JSX is a bare `<DialogDescription>A role is what an agent does...</DialogDescription>` with no `t(...)` call, so the mock never intercepts it. Text-content matching just works.

    - Do NOT rely on `screen.getByText(...)` to distinguish the DialogTitle from the DialogDescription — they may share a DOM ancestor. Use the exact blurb sentence as the regex (which uniquely identifies the DialogDescription content).

    - Do NOT skip the required-caption-absence assertion in Test 11. Verifying that the DELETED string is gone is what makes Plan 84-01 CHANGE E.1 executable in this test suite.

    - Do NOT run the full vitest suite. Per campaign constraint: scoped runs only. The verify block below specifies `npx vitest run src/ui/sidebar/CreateRoleDialog.test.tsx` — that file only.
  </action>
  <verify>
    <automated>grep -c 'Test 18' src/ui/sidebar/CreateRoleDialog.test.tsx | grep -v '^0$' && grep -c 'it("Test 18' src/ui/sidebar/CreateRoleDialog.test.tsx | grep '^0$' && grep -c 'it("Test 22' src/ui/sidebar/CreateRoleDialog.test.tsx | grep -v '^0$' && grep -c 'A role is what an agent does and how it thinks' src/ui/sidebar/CreateRoleDialog.test.tsx | grep -v '^0$' && grep -c 'thenCreateIdentity' src/ui/sidebar/CreateRoleDialog.test.tsx | grep '^0$' && grep -c 'Phase 84' src/ui/sidebar/CreateRoleDialog.test.tsx | grep -v '^0$' && npx vitest run src/ui/sidebar/CreateRoleDialog.test.tsx</automated>
  </verify>
  <done>
    - `grep -c 'it("Test 18' src/ui/sidebar/CreateRoleDialog.test.tsx` returns exactly 0 (Test 18 it-block deleted)
    - `grep -c 'Test 18' src/ui/sidebar/CreateRoleDialog.test.tsx` returns ≥ 2 (comment references in the header addendum + the single-line replacement note remain — the test itself is gone, but the reasoning stays documented)
    - `grep -c 'it("Test 22' src/ui/sidebar/CreateRoleDialog.test.tsx` returns exactly 1 (new Test 22 present)
    - `grep -c 'A role is what an agent does and how it thinks' src/ui/sidebar/CreateRoleDialog.test.tsx` returns exactly 1 (Test 11 blurb assertion present with exact string from D-CONTEXT item 1)
    - `grep -c 'thenCreateIdentity' src/ui/sidebar/CreateRoleDialog.test.tsx` returns exactly 0 (all references to the deleted state hook gone — the checkbox is neither exercised nor asserted anywhere)
    - `grep -c 'Then create an agent with this role' src/ui/sidebar/CreateRoleDialog.test.tsx` returns ≥ 2 (survives ONLY in the two `queryByRole("checkbox", { name: /then create an agent with this role/i })` absence assertions in Tests 11 and 20)
    - `grep -c 'Name and description are required' src/ui/sidebar/CreateRoleDialog.test.tsx` returns ≥ 1 (Test 11 absence assertion for the deleted required-caption)
    - `grep -c 'Phase 84' src/ui/sidebar/CreateRoleDialog.test.tsx` returns ≥ 6 (top-of-file addendum block + per-test rationale comments across Tests 11, 15, 17, 20, 22)
    - `grep -c 'queryByRole("listbox")' src/ui/sidebar/CreateRoleDialog.test.tsx` returns ≥ 2 (Tests 15 and 22 both assert listbox absence at length === 1)
    - Scoped `npx vitest run src/ui/sidebar/CreateRoleDialog.test.tsx` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → CreateRoleDialog/NewSessionDialog under jsdom | Same trust boundary as pre-phase-84. Tests run in-process against mocked backends; no network, no auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-03-01 | Repudiation | Test-file audit trail after Test 18 deletion | mitigate | The single-line comment replacement in EDIT 18 records that Test 18 was intentionally deleted (not lost) and cites the D-CONTEXT decision (item 4) that makes the deleted-test's assertion vacuous. Future readers grepping for "Test 18" find the replacement note before concluding the test was orphaned. Additionally, the top-of-file Phase 84 addendum enumerates the Test 11/15/17/18/20/22 delta as a single audit anchor. |
| T-84-03-SC | Tampering | Zero new npm installs in this plan | accept | Pure test-file edit — no `package.json`, no `package-lock.json`, no `npm install`. Package legitimacy audit N/A. |

Overall test-only scope for this plan: no code changes, no backend changes, no data-flow changes. The test suite becomes an accurate executable spec for the Phase 84 landed code; no new attack surface.
</threat_model>

<verification>
Campaign constraint (Alice 2026-09-07, 7-bounty UX-pass string): NO full-suite `npx vitest run`, NO `docker build`, NO `docker cp`, NO `docker compose up`, NO deploy. Executor runs SCOPED tests only. The three scoped vitest runs specified below are the campaign-constraint-compliant verification:

- `grep -c 'it("Test 18' src/ui/sidebar/CreateRoleDialog.test.tsx` returns 0
- `grep -c 'it("Test 22' src/ui/sidebar/CreateRoleDialog.test.tsx` returns 1
- `grep -c 'A role is what an agent does and how it thinks' src/ui/sidebar/CreateRoleDialog.test.tsx` returns 1
- `grep -c 'thenCreateIdentity' src/ui/sidebar/CreateRoleDialog.test.tsx` returns 0
- `grep -c 'toMatch(/new agent/i)' src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` returns ≥ 1
- `grep -c 'toMatch(/start a new agent/i)' src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` returns 0
- `grep -c 'a role is what an agent does' src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx` returns ≥ 1
- `grep -c 'then create an agent with this role' src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx` returns 0
- `npx vitest run src/ui/sidebar/CreateRoleDialog.test.tsx` exits 0
- `npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` exits 0
- `npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx` exits 0

If any of the three scoped vitest runs is red, do NOT escalate to a full-suite run — diagnose the specific failure against the Phase 84 code landed in Plans 84-01 and 84-02, fix the test-side assertion, and re-run the SAME scoped file. Full-suite runs are orchestrator-owned per campaign constraint.
</verification>

<success_criteria>
- PrettyConversationsPanel.test.tsx Test 5 assertion flipped from `/start a new agent/i` to `/new agent/i` — matches Plan 84-02's title conform
- PrettyConversationsPanel.new-role-button.test.tsx Test 21c dialog-detection signal swapped from the deleted checkbox text to the Phase-84-stable blurb regex — resilient to future title tweaks in either dialog
- CreateRoleDialog.test.tsx Tests 11, 15, 17, 20 updated in place; Test 18 deleted with an audit-trail comment; new Test 22 added for single-host picker suppression
- The top-of-file comment header in CreateRoleDialog.test.tsx documents every Test 11/15/17/18/20/22 delta as a single audit anchor
- Every grep-based acceptance check hits its expected count
- Three scoped `npx vitest run <file>` invocations all exit 0
- Campaign constraint honored: no full-suite run, no docker build, no docker cp, no deploy
- Phase 84 test-side complete; the executable spec accurately describes the Phase 84 landed code across all three touched test files
</success_criteria>

<output>
Create `.planning/phases/84-create-role-modal-ux-pass-header-blurb-drop-required-caption/84-03-SUMMARY.md` when done
</output>
</content>
</invoke>