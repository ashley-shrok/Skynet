---
phase: 92-pin-sentinel-migration
plan: 03
type: execute
wave: 3
depends_on:
  - 92-02
files_modified:
  - src/backend/database/db/index.ts
  - src/backend/database/db/schema.ts
  - src/backend/database/db/index.migration.test.ts
autonomous: true
requirements:
  - D-02
  - D-08-migration-sequencing
must_haves:
  truths:
    - "After boot post-deploy, `PRAGMA table_info(user_preferences)` returns no row named `pinned_conversation_ids`"
    - "The `hidden_conversation_ids` column on the same user_preferences row is UNTOUCHED (out of scope per D-02)"
    - "Boot on a fresh install (no legacy column) is a silent no-op — migration is idempotent"
    - "The column drop is persisted via DatabaseSaveTrigger.forceSave labeled exactly `phase-92-pin-sentinel-migration`, so the migration survives container restart before any unrelated write fires the debounced save"
    - "Nothing in src/ still reads or writes the pinned_conversation_ids column (verified by grep — Plan 02 already retired those consumers)"
  artifacts:
    - path: "src/backend/database/db/index.ts"
      provides: "runPinColumnDrop() export + migrateSchema() invocation + labeled forceSave"
      contains: "runPinColumnDrop\\|phase-92-pin-sentinel-migration"
    - path: "src/backend/database/db/schema.ts"
      provides: "pinnedConversationIds field removed from userPreferences drizzle mirror"
    - path: "src/backend/database/db/index.migration.test.ts"
      provides: "Test 1 (OLD schema → drop → column absent) + Test 2 (NEW schema → idempotent no-op) + Test 3 (post-drop SELECT throws) + Test 4 (sibling hidden_conversation_ids preserved) mirroring Phase 66 test pattern"
  key_links:
    - from: "migrateSchema() in db/index.ts"
      to: "runPinColumnDrop(sqlite)"
      via: "runs BEFORE the addColumnIfNotExists sweep so a stale legacy install doesn't briefly re-add the column"
      pattern: "runPinColumnDrop\\(sqlite\\)"
    - from: "runPinColumnDrop"
      to: "dropColumnIfExists(sqliteDb, 'user_preferences', 'pinned_conversation_ids')"
      via: "shared idempotent probe-then-drop helper (Phase 66 pattern)"
      pattern: "dropColumnIfExists.*user_preferences.*pinned_conversation_ids"
---

<objective>
Drop the `pinned_conversation_ids` column from `user_preferences` per D-02 — no dormant column left behind. Plan 02 already severed every read/write in src/, so this drop is safe: nothing consumes the column post-Plan-02, and this migration removes the dead storage cleanly.

Purpose: Close the two-sources-of-truth window at the schema level. The shape file rejects "leaving the pin table dormant just in case" verbatim ("Drop it" — Alice 2026-09-09).

Output: `runPinColumnDrop()` exported from db/index.ts (mirroring `runIdentitiesTableDrop` byte-for-byte), migrateSchema() invocation with labeled forceSave, schema.ts drizzle mirror updated, migration test with the mandatory Test 1 (OLD schema drop) + Test 2 (NEW schema idempotent) pair.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/92-pin-sentinel-migration-move-identity-pin-state-from-skynet-d/92-CONTEXT.md
@.planning/shapes/shape-pin-sentinel-migration.md
@.planning/phases/92-pin-sentinel-migration-move-identity-pin-state-from-skynet-d/92-02-SUMMARY.md
@.planning/phases/66-skynet-reads-and-writes-identity-cosmetics-from-disk/66-04-SUMMARY.md
@.planning/phases/68-kill-the-identities-db-table-disk-becomes-sole-source-of-tru/68-SUMMARY.md

@src/backend/database/db/index.ts
@src/backend/database/db/schema.ts
@src/backend/database/db/index.migration.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add runPinColumnDrop + wire into migrateSchema with labeled forceSave; delete drizzle mirror field</name>
  <files>src/backend/database/db/index.ts, src/backend/database/db/schema.ts</files>
  <read_first>
    - src/backend/database/db/index.ts (assertSqliteSupportsDropColumn L785-804, dropColumnIfExists L814-842, runIdentitiesCosmeticDrops L851-860, runIdentitiesTableDrop L873-890 — mirror the shape verbatim, and the migrateSchema() body at L892-1200 including the labeled forceSave pattern at L928-939 / L995-1006 / L1024-1035 / L1184-1195)
    - src/backend/database/db/schema.ts:827-843 (userPreferences drizzle mirror — the field being removed)
    - .planning/phases/68-*/68-SUMMARY.md (runIdentitiesTableDrop precedent this task mirrors)
  </read_first>
  <behavior>
    - Test 1 (unit-of-refactor): calling runPinColumnDrop(inMemoryDb) against a DB seeded with the OLD user_preferences schema removes the pinned_conversation_ids column; surviving columns intact; hidden_conversation_ids preserved with any seeded value.
    - Test 2 (idempotent no-op): calling runPinColumnDrop against a NEW schema (column already absent) does not throw and does not mutate PRAGMA table_info.
    - Test 3 (post-drop SELECT throws): `db.prepare("SELECT pinned_conversation_ids FROM user_preferences").get()` after runPinColumnDrop throws — proves column is physically absent, not just hidden from PRAGMA.
    - Test 4 (sibling untouched): PRAGMA still lists hidden_conversation_ids after the drop; its value on the seeded row is preserved verbatim.
    - Test 5 (drizzle mirror parity): `userPreferences.pinnedConversationIds` is `undefined` after schema.ts edit (compile-time contract; will produce TypeScript error if any Plan-02-missed callsite still references it).
  </behavior>
  <action>
    In db/index.ts, add a new exported function `runPinColumnDrop(sqliteDb)` — byte-mirror of runIdentitiesTableDrop's shape:

      /**
       * Phase 92 D-02 — drop the `pinned_conversation_ids` column from
       * `user_preferences`. Pin state lives on disk as `~/.claude/identities/
       * <name>/.pinned` post-Phase-92 (matches `.no-dormancy` / `.recycle-
       * requested` presence-is-meaning convention). Plan 02 already retired
       * every reader/writer of this column in src/ — this drop retires the
       * dead storage.
       *
       * Idempotent via dropColumnIfExists (probes SELECT; ALTER TABLE DROP
       * COLUMN on hit; silent no-op on miss). The sibling `hidden_conversation
       * _ids` column on the same row is DELIBERATELY out of scope per D-02
       * and is not touched — regression-trapped in the migration test's Test 4.
       *
       * Exported so index.migration.test.ts can exercise Test 1 (OLD schema
       * drop) + Test 2 (NEW schema idempotent no-op) against test-owned
       * in-memory databases — parallel to runIdentitiesCosmeticDrops export
       * pattern at L851-860.
       */
      export function runPinColumnDrop(sqliteDb: Database.Database): void {
        assertSqliteSupportsDropColumn(sqliteDb);
        dropColumnIfExists(sqliteDb, "user_preferences", "pinned_conversation_ids");
      }

    Place it below runIdentitiesTableDrop (around L890) with the docblock verbatim above.

    In migrateSchema() (starts L892), add a runPinColumnDrop invocation BEFORE the addColumnIfNotExists sweep at L941+ — same ordering rationale as runIdentitiesCosmeticDrops at L901-913 (drops run BEFORE adds so a stale install doesn't briefly re-add the column). Wrap in try/catch that rethrows on preflight failure (mirrors L901-913):

      try {
        runPinColumnDrop(sqlite);
      } catch (preflightErr) {
        databaseLogger.error(
          "Phase 92 pin-column drop preflight failed",
          preflightErr,
          { operation: "schema_migration_preflight" },
        );
        throw preflightErr;
      }

    ALSO at L945, delete the line `addColumnIfNotExists("user_preferences", "pinned_conversation_ids", "TEXT");` — fresh installs no longer create the column (D-02 "no dormant column"). Keep the L946 hidden_conversation_ids line untouched.

    After the addColumnIfNotExists sweep for user_preferences completes (around L947+), add a labeled forceSave:

      try {
        await DatabaseSaveTrigger.forceSave("phase-92-pin-sentinel-migration");
      } catch (saveError) {
        databaseLogger.warn(
          "[phase-92] forceSave failed post-drop (non-fatal — dropColumnIfExists is idempotent, next boot retries)",
          {
            operation: "schema_migration_force_save_post_drop",
            reason: "phase-92-pin-sentinel-migration",
            error: saveError,
          },
        );
      }

    In schema.ts, at L838, delete the line `pinnedConversationIds: text("pinned_conversation_ids"),`. Keep hidden_conversation_ids at L839 untouched. Surrounding fields untouched.

    Post-Plan-02, no src/ file should reference `pinnedConversationIds` from the drizzle mirror. Verify with grep BEFORE running the migration test — if grep finds ANY reference outside comments, Plan 02 wasn't fully landed and this task should NOT proceed.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tanya && grep -c 'runPinColumnDrop\|phase-92-pin-sentinel-migration' src/backend/database/db/index.ts && grep -c 'pinnedConversationIds' src/backend/database/db/schema.ts</automated>
  </verify>
  <done>
    First grep >= 3 (runPinColumnDrop function definition, migrateSchema invocation, forceSave label). Second grep = 0 (drizzle field removed). `grep -c "pinned_conversation_ids" src/backend/database/db/index.ts` returns 0 (both CREATE TABLE block reference and addColumnIfNotExists line gone). No compile break across src/ (Plan 02 landed the callsite retirement).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration test — Test 1 OLD-schema drop + Test 2 NEW-schema idempotent + Test 3/4 regression traps</name>
  <files>src/backend/database/db/index.migration.test.ts</files>
  <read_first>
    - src/backend/database/db/index.migration.test.ts (Test 1 + Test 2 for Phase 66 at L67-149; the OLD_IDENTITIES_CREATE_SQL / NEW_IDENTITIES_CREATE_SQL const pattern at L30-58; the columnNames helper at L60-64; the Phase 68 table-drop test at L212-286 for the closest pattern to a column-drop migration test)
    - src/backend/database/db/index.ts (runPinColumnDrop from Task 1 — exported for use in this test)
  </read_first>
  <behavior>
    - Test P92-01 (OLD schema → drop → column absent, siblings intact, row data preserved): seed an in-memory sqlite with OLD_USER_PREFERENCES_CREATE_SQL (includes both pinned_conversation_ids AND hidden_conversation_ids). Insert a row with real values for both. Run runPinColumnDrop. Assert: PRAGMA table_info no longer contains pinned_conversation_ids; DOES contain hidden_conversation_ids; SELECT for hidden_conversation_ids on the seeded row returns the seeded value verbatim; SELECT theme returns the seeded theme.
    - Test P92-02 (NEW schema idempotent no-op): seed with NEW_USER_PREFERENCES_CREATE_SQL (WITHOUT pinned_conversation_ids). Sanity: column absent. Run runPinColumnDrop. Assert: not-throws; PRAGMA shape unchanged.
    - Test P92-03 (post-drop SELECT throws): after Test P92-01's drop, `db.prepare("SELECT pinned_conversation_ids FROM user_preferences").get()` throws — proves column is genuinely absent.
    - Test P92-04 (regression trap: hidden_conversation_ids untouched with its VALUE): mirrors Test P92-01 but explicitly asserts hidden_conversation_ids column presence AND its seeded value survives — trap against a future refactor that over-eagerly bundles both drops.
  </behavior>
  <action>
    Append a new describe block to index.migration.test.ts, below the existing Phase 85-01 avatar_path block (which ends around L869), titled `describe("Phase 92 migration — drop pinned_conversation_ids from user_preferences")`.

    Above the describe block, declare two SQL const literals — same style as OLD_IDENTITIES_CREATE_SQL / NEW_IDENTITIES_CREATE_SQL / OLD_SSH_DATA_CREATE_SQL / etc. at L30-335:

      const OLD_USER_PREFERENCES_CREATE_SQL = `
        CREATE TABLE user_preferences (
          user_id TEXT PRIMARY KEY,
          reopen_tabs_on_login INTEGER NOT NULL DEFAULT 0,
          theme TEXT,
          font_size TEXT,
          accent_color TEXT,
          language TEXT,
          pinned_conversation_ids TEXT,
          hidden_conversation_ids TEXT,
          updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
        );
      `;

      const NEW_USER_PREFERENCES_CREATE_SQL = `
        CREATE TABLE user_preferences (
          user_id TEXT PRIMARY KEY,
          reopen_tabs_on_login INTEGER NOT NULL DEFAULT 0,
          theme TEXT,
          font_size TEXT,
          accent_color TEXT,
          language TEXT,
          hidden_conversation_ids TEXT,
          updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
        );
      `;

    Update the existing import at L20-25 to also destructure `runPinColumnDrop`:

      import {
        assertSqliteSupportsDropColumn,
        dropColumnIfExists,
        runIdentitiesCosmeticDrops,
        runIdentitiesTableDrop,
        runPinColumnDrop,
      } from "./index.js";

    Test P92-01 body — mirrors the Phase 66 Test 1 pattern at L67-136:
      - `const db = new Database(":memory:")`
      - `db.exec(OLD_USER_PREFERENCES_CREATE_SQL)`
      - Insert one row via db.prepare(...).run("u1", 0, "dark", null, null, null, '["fleet::1::alice"]', '["fleet::2::bob"]', "2026-01-01T00:00:00.000Z") — user_id, reopen_tabs_on_login, theme, font_size, accent_color, language, pinned_conversation_ids, hidden_conversation_ids, updated_at.
      - Sanity: `const preCols = columnNames(db, "user_preferences")`; expect preCols to contain both pinned_conversation_ids AND hidden_conversation_ids.
      - `runPinColumnDrop(db)`
      - `const postCols = columnNames(db, "user_preferences")`
      - `expect(postCols).not.toContain("pinned_conversation_ids")`
      - `expect(postCols).toContain("hidden_conversation_ids")` — sibling untouched
      - Read the seeded row: `db.prepare("SELECT theme, hidden_conversation_ids FROM user_preferences WHERE user_id = ?").get("u1")` and assert theme === "dark" AND hidden_conversation_ids === '["fleet::2::bob"]' — Test P92-04 assertion inline.
      - `expect(() => db.prepare("SELECT pinned_conversation_ids FROM user_preferences").get()).toThrow()` — Test P92-03 assertion inline.

    Test P92-02 body — mirrors Phase 66 Test 2 pattern at L138-149:
      - `const db = new Database(":memory:")`
      - `db.exec(NEW_USER_PREFERENCES_CREATE_SQL)`
      - Sanity: `columnNames(db, "user_preferences")` does not contain pinned_conversation_ids.
      - `expect(() => runPinColumnDrop(db)).not.toThrow()`
      - Post-migration: columnNames shape unchanged; hidden_conversation_ids still present.

    Keep tests atomic — one describe block, two `it(...)` bodies, no shared state between them (each opens its own `new Database(":memory:")`).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tanya && npx vitest run src/backend/database/db/index.migration.test.ts --reporter=basic 2>&1 | tail -40</automated>
  </verify>
  <done>
    Two new Phase 92 tests pass (P92-01, P92-02, each carrying inline P92-03/P92-04 assertions). All existing Phase 66/68/72/75/79/85 tests in the same file still pass (no regression). `grep -c 'runPinColumnDrop' src/backend/database/db/index.migration.test.ts` >= 3 (import + Test 1 body + Test 2 body).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB migration boot path | Runs before request-handling; no untrusted input at this seam |
| DB file on disk | Encrypted at rest via existing DatabaseFileEncryption; forceSave persists the schema mutation atomically |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-03-01 | Denial of Service | migrateSchema() drop-column preflight | mitigate | assertSqliteSupportsDropColumn (Phase 66 preflight at L785-804) runs first — throws loudly on SQLite < 3.35 rather than silently continuing with a partially-migrated schema (T-66-04-04 precedent) |
| T-92-03-02 | Data Loss | dropping pinned_conversation_ids loses pin history | accept per D-08 | Migration order per box: (1) query current pinned_conversation_ids before deploy; (2) `touch ~/.claude/identities/<name>/.pinned` on the host; (3) deploy new code. Alice + Stacy each handle their own box per D-07. No code participates. |
| T-92-03-03 | Tampering | forceSave label mislabeled → future ops-grep broken | mitigate | Literal label `"phase-92-pin-sentinel-migration"` locked by quality-gate; presence-grepped by Task 1's verify step |
| T-92-03-04 | Data Loss | drop bundles hidden_conversation_ids as well (out-of-scope creep) | mitigate | Test P92-04 regression trap explicitly asserts hidden_conversation_ids survives the drop with its value intact |
| T-92-03-05 | Repudiation | migration failure invisible in logs | accept | Existing databaseLogger.error/warn shape at L928-939 (phase-68 precedent) is preserved; log tag `schema_migration_force_save_post_drop` grepable by ops |
</threat_model>

<verification>
- Task 1: `runPinColumnDrop` exported; migrateSchema invocation added; addColumnIfNotExists("user_preferences", "pinned_conversation_ids", "TEXT") deleted; schema.ts pinnedConversationIds field deleted; forceSave label matches quality-gate literal exactly.
- Task 2: Test P92-01 (OLD → drop) + Test P92-02 (NEW → idempotent) + inline Test P92-03 (post-drop SELECT throws) + Test P92-04 (hidden_conversation_ids untouched) all pass. Full test file green (no Phase 66/68/72/75/79/85 regression).
- Manual: `grep -c 'pinned_conversation_ids\|pinnedConversationIds' src/backend/database/` returns hits only in the migration test file (in the const SQL literals + assertions) — zero hits in db/index.ts and db/schema.ts.
</verification>

<success_criteria>
- The `pinned_conversation_ids` column is physically dropped from `user_preferences` on any deploy that boots this migration.
- The drop is idempotent — re-boots after the initial drop are silent no-ops.
- `hidden_conversation_ids` sibling column is UNTOUCHED (D-02 out-of-scope invariant).
- `phase-92-pin-sentinel-migration` forceSave label persists the schema mutation across container restart.
- Test 1 + Test 2 pair mirrors the Phase 66 pattern verbatim — same audit shape future migrations follow.
</success_criteria>

<output>
Create `.planning/phases/92-pin-sentinel-migration-move-identity-pin-state-from-skynet-d/92-03-SUMMARY.md` when done, documenting:
- The runPinColumnDrop export + its byte-mirror relationship to runIdentitiesTableDrop (Phase 68 precedent).
- The forceSave label + the try/catch fallback rationale (mirrors phase-68 / phase-75 / phase-85 pattern).
- The Test P92-04 regression trap for hidden_conversation_ids untouched (VALUE preserved, not just column presence).
- Confirmation that the drop is the LAST plumbing step — Plan 04 wires the frontend to the new source of truth AFTER the column is gone.
</output>
