---
phase: 80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/claude-session/identity-artifact-reader.ts
  - src/backend/claude-session/identity-artifact-reader.avatar-read.test.ts
  - src/backend/database/routes/identities.ts
  - src/backend/database/routes/identities.get-disk.test.ts
  - src/backend/database/routes/identity-birth-orchestrator.ts
  - src/backend/database/routes/identity-birth-orchestrator.role-frontmatter.test.ts
  - src/backend/database/routes/identity-birth.ts
  - src/backend/database/routes/identity-birth.test.ts
  - src/backend/database/routes/identity-clone.ts
  - src/backend/database/routes/identity-clone.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "The identity file frontmatter can carry a `task` string; `extractCosmeticsFromFrontmatter` narrows it exactly like `voice`/`title` (non-empty string → keep; anything else → drop) — matches Phase 66 disk-authoritative cosmetics pattern (D-05)."
    - "GET /identities responses include `task: string | null` on every identity object (via `publicIdentity`)."
    - "`birthIdentity` accepts optional `opts.task`; when present-and-non-empty, `buildIdentityFileBody` emits a `task:` key in the emitted YAML frontmatter (position after `avatar`)."
    - "POST /identities/birth accepts optional `task` in request body (validated as `string | null | undefined`, capped ≤500 chars server-side)."
    - "POST /identities/clone accepts optional `task` (does NOT inherit source.task — A3 lock: clone gets a fresh task per each new spawn)."
    - "Frontmatter YAML parse-error swallow semantic preserved (RESEARCH §5 landmine): outer try/catch in extractCosmeticsFromFrontmatter still returns `{}` on parse failure — do NOT harden to throw."
    - "Birth-orchestrator atomic-write invariant preserved (RESEARCH §12 landmine): task lands as part of the single `buildIdentityFileBody` string that goes to `writeMarkdownFileAtomic` — no separate write step."
  artifacts:
    - path: "src/backend/claude-session/identity-artifact-reader.ts"
      provides: "`extractCosmeticsFromFrontmatter` returns `task?: string`"
      contains: "task"
    - path: "src/backend/database/routes/identities.ts"
      provides: "`publicIdentity` emits `task: string | null`"
      contains: "task"
    - path: "src/backend/database/routes/identity-birth-orchestrator.ts"
      provides: "`BirthOptions` has `task?: string`; `buildIdentityFileBody` emits task after avatar"
      contains: "task"
    - path: "src/backend/database/routes/identity-birth.ts"
      provides: "POST body validates task (nullable optional string, cap 500)"
      contains: "task"
    - path: "src/backend/database/routes/identity-clone.ts"
      provides: "POST body validates task (nullable optional string, cap 500) — fresh per clone, not inherited"
      contains: "task"
    - path: "src/backend/database/routes/identity-clone.test.ts"
      provides: "Regression tests: clone body accepts task (present + absent), clone frontmatter emits task after avatar, source.task is NEVER read/copied (A3 enforced by test)"
      contains: "task"
  key_links:
    - from: "extractCosmeticsFromFrontmatter"
      to: "publicIdentity (via identities.ts:219-221 call site)"
      via: "cosmetics spread"
      pattern: "task"
    - from: "publicIdentity"
      to: "frontend Identity.task (via GET /identities response body)"
      via: "JSON serialization"
      pattern: "task"
    - from: "identity-birth.ts POST handler"
      to: "birthIdentity(opts) → buildIdentityFileBody → writeMarkdownFileAtomic"
      via: "opts.task thread-through"
      pattern: "task"
---

<objective>
Wire the `task` field end-to-end through the Skynet backend disk-read/write/emit path following the exact pattern established for `voice`/`title`/`avatar` cosmetics fields in Phase 66. **Additive extension only** — no new patterns invented. Implements **D-05** (task write-once-at-creation, disk-only, no DB caching) and unblocks the frontend (plan 80-05 delivers the frontend `Identity.task` type, plan 80-07 renders the pill, plan 80-08 renders the task-primary row).

**Scope split note (revision 2026-09-06):** MXID handle derivation and ordinal-suffix iteration (the `<PoolName>-<Role>[-N]` Matrix account localpart shape, per A1 DIVERGE lock) is scoped to sibling plan `80-03b` (Wave 2). This plan covers ONLY the task-frontmatter wiring; MXID composition is orthogonal and depends on plan 80-02's `countUsersMatching` primitive.

Purpose: Task field becomes a first-class frontmatter cosmetic scalar readable + writable + surface-able via existing code paths.
Output: Six source files + four test files extended (5 source + 1 clone endpoint + 1 clone test + 3 sibling test files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-CONTEXT.md
@.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-RESEARCH.md
@.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-PATTERNS.md
@src/backend/claude-session/identity-artifact-reader.ts
@src/backend/database/routes/identities.ts
@src/backend/database/routes/identity-birth-orchestrator.ts
@src/backend/database/routes/identity-birth.ts
@src/backend/database/routes/identity-clone.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend extractCosmeticsFromFrontmatter + publicIdentity for task field</name>
  <files>src/backend/claude-session/identity-artifact-reader.ts, src/backend/claude-session/identity-artifact-reader.avatar-read.test.ts, src/backend/database/routes/identities.ts, src/backend/database/routes/identities.get-disk.test.ts</files>
  <read_first>
    - src/backend/claude-session/identity-artifact-reader.ts:2079-2145 (extractCosmeticsFromFrontmatter: existing narrowing chain for displayName/title/colorHue/voice/avatar/coordinator; outer try/catch swallow at L2106-2110 that MUST be preserved per RESEARCH §5)
    - src/backend/claude-session/identity-artifact-reader.avatar-read.test.ts (existing describe block for extractCosmeticsFromFrontmatter narrowing tests — add task cases in the same block)
    - src/backend/database/routes/identities.ts:109-143 (publicIdentity: existing cosmetics arg + emission pattern)
    - src/backend/database/routes/identities.ts:181-264 (GET /identities handler — verify the cosmetics spread at L219-221 picks up task automatically)
    - src/backend/database/routes/identities.get-disk.test.ts (existing PUB-* tests for publicIdentity shape and GET response body)
    - .planning/phases/80-*/80-PATTERNS.md §"src/backend/claude-session/identity-artifact-reader.ts — extend extractCosmeticsFromFrontmatter" and §"src/backend/database/routes/identities.ts — extend publicIdentity"
  </read_first>
  <behavior>
    - `extractCosmeticsFromFrontmatter(markdown)` return type gains `task?: string`.
    - When frontmatter YAML has `task: "build the pool-pick endpoint"` → `.task === "build the pool-pick endpoint"`.
    - When `task: ""` (empty string) → `.task` undefined (dropped, absent-⇒-omit).
    - When `task: 42` (non-string) → `.task` undefined.
    - When `task:` key absent → `.task` undefined.
    - When frontmatter is malformed YAML → returns `{}` (existing swallow — unchanged; must be re-verified by test).
    - `publicIdentity` gains `task?: string` in the `cosmetics` arg type.
    - `publicIdentity` return object gains `task: typeof cosmetics.task === "string" ? cosmetics.task : null`.
    - GET /identities response identity objects now include `task: string | null` on every item.
  </behavior>
  <action>
    In `identity-artifact-reader.ts:2095-2145`: add `task?: string` to both the JSDoc return-type annotation and the local `out` type declaration. After the existing coordinator narrowing block (or after avatar — match existing ordering; task lives with the cosmetics group), add: `if (typeof src.task === "string" && src.task.length > 0) { out.task = src.task; }`. Do NOT modify the outer try/catch at L2106-2110 — the parse-error swallow is load-bearing (RESEARCH §5).

    In `identity-artifact-reader.avatar-read.test.ts`: add cases to the extractCosmeticsFromFrontmatter describe block: (a) task present + non-empty → returned; (b) task empty string → omitted; (c) task number → omitted; (d) task absent → omitted; (e) malformed YAML still returns `{}` (regression guard for landmine).

    In `identities.ts:109-143`: add `task?: string` to the `cosmetics` parameter type. In the return object literal, add `task: typeof cosmetics.task === "string" ? cosmetics.task : null` (match the `voice`/`title` null-fallback shape). Position the `task` field near the other cosmetics scalars (voice, title) in the emitted object.

    In `identities.get-disk.test.ts`: extend the existing `publicIdentity` PUB-* tests to assert `task` is present in emitted shape. Add a GET /identities case where the underlying frontmatter has `task: "something"` and the response body contains `task: "something"` on the returned identity. Add a GET case where task is absent → response has `task: null`.

    Commit prefix: `feat(80-03)` for source changes, `test(80-03)` for test-only files (or combine if implemented together).
  </action>
  <verify>
    <automated>npx vitest run src/backend/claude-session/identity-artifact-reader.avatar-read.test.ts src/backend/database/routes/identities.get-disk.test.ts --reporter=verbose 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "task" src/backend/claude-session/identity-artifact-reader.ts` shows the field added at least twice (type declaration + narrowing block).
    - The narrowing guard `if (typeof src.task === "string" && src.task.length > 0)` present verbatim in `identity-artifact-reader.ts` — `grep -c 'typeof src.task === "string"' src/backend/claude-session/identity-artifact-reader.ts` prints `1`.
    - `grep -c 'task: typeof cosmetics.task' src/backend/database/routes/identities.ts` prints `1`.
    - `grep -c 'task?: string' src/backend/database/routes/identities.ts` prints at least `1` (cosmetics arg type).
    - The outer try/catch at extractCosmeticsFromFrontmatter is NOT modified: `grep -A2 "yaml.load" src/backend/claude-session/identity-artifact-reader.ts | grep -c "return {}"` >= 1.
    - `npx vitest run src/backend/claude-session/identity-artifact-reader.avatar-read.test.ts -t "task"` reports >= 4 task-specific passing cases.
    - `npx vitest run src/backend/database/routes/identities.get-disk.test.ts` all cases pass; existing tests still pass.
  </acceptance_criteria>
  <done>`extractCosmeticsFromFrontmatter` narrows task per existing pattern; `publicIdentity` emits `task: string | null`; GET /identities returns task on every identity; parse-error swallow unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend BirthOptions + buildIdentityFileBody to emit task on birth</name>
  <files>src/backend/database/routes/identity-birth-orchestrator.ts, src/backend/database/routes/identity-birth-orchestrator.role-frontmatter.test.ts</files>
  <read_first>
    - src/backend/database/routes/identity-birth-orchestrator.ts:107-242 (BirthDeps + BirthOptions interface at L111-130)
    - src/backend/database/routes/identity-birth-orchestrator.ts:345-380 (buildIdentityFileBody: existing frontmatter emission — pairs push loop, absent-⇒-omit invariant)
    - src/backend/database/routes/identity-birth-orchestrator.ts:660-671 (Q2 no-rollback invariant — step-failure semantics; task emission must not break this)
    - src/backend/database/routes/identity-birth-orchestrator.role-frontmatter.test.ts (existing buildIdentityFileBody tests — add task emission cases)
    - .planning/phases/80-*/80-PATTERNS.md §"src/backend/database/routes/identity-birth-orchestrator.ts — extend BirthOptions + buildIdentityFileBody"
  </read_first>
  <behavior>
    - `BirthOptions` interface gains `task?: string` (optional). Sibling plan 80-03b also adds `poolPicked?: boolean` to the same interface — do NOT collide on that field here; leave any `poolPicked` wiring for 80-03b.
    - `buildIdentityFileBody(opts, displayName, avatarFilename)`: when `typeof opts.task === "string" && opts.task.trim().length > 0`, pushes `["task", opts.task]` into the pairs array, positioned AFTER the `["avatar", avatarFilename]` push.
    - When `opts.task` is undefined, null, empty string, or whitespace-only → no `task:` key in emitted YAML (absent-⇒-omit invariant matches title/voice pattern).
    - Emitted YAML string still ends with `---\n\n{IDENTITY_FILE_SEED_COMMENT}\n\n# ${opts.name}\n` (structure unchanged; only one pair added).
    - `yaml.dump` remains called with `{ sortKeys: false, lineWidth: -1, noRefs: true, forceQuotes: false }` — do NOT change the dump options.
    - Task strings containing colons/quotes/newlines emit correctly quoted YAML (verified by test) — relies on `yaml.dump`'s auto-quoting per T-66-01-04.
  </behavior>
  <action>
    In `identity-birth-orchestrator.ts` `BirthOptions` interface (L111-130 region): add `task?: string;` field. Add JSDoc: "Phase 80: optional task string written to frontmatter at birth time (write-once per D-05). Absent → no `task:` key emitted."

    In `buildIdentityFileBody` (L345-380 region): after the existing `pairs.push(["avatar", avatarFilename]);` line, add the emission guard: `if (typeof opts.task === "string" && opts.task.trim().length > 0) { pairs.push(["task", opts.task]); }`. Do NOT modify the yaml.dump options or the trailing string template.

    Extend `identity-birth-orchestrator.role-frontmatter.test.ts` with cases: (a) opts.task = "build the endpoint" → emitted YAML contains `task: build the endpoint` (or however yaml.dump renders it); (b) opts.task = "" → NO `task:` key in emitted YAML; (c) opts.task = "   " → NO `task:` key; (d) opts.task undefined → NO `task:` key; (e) opts.task with a colon like "fix the pool: shape gate" → emitted correctly quoted (assert via yaml.load round-trip yields the exact input string).

    Preserve landmine (RESEARCH §12): the whole `buildIdentityFileBody` returns ONE atomic string. Do NOT split task into a separate write step. Do NOT introduce any new step in the orchestrator's SSE flow.
  </action>
  <verify>
    <automated>npx vitest run src/backend/database/routes/identity-birth-orchestrator.role-frontmatter.test.ts --reporter=verbose 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'task?: string' src/backend/database/routes/identity-birth-orchestrator.ts` prints at least `1`.
    - `grep -c 'pairs.push(\\[\"task\"' src/backend/database/routes/identity-birth-orchestrator.ts` prints exactly `1`.
    - `grep -c 'opts.task.trim().length > 0' src/backend/database/routes/identity-birth-orchestrator.ts` prints exactly `1`.
    - yaml.dump options unchanged: `grep -c "forceQuotes: false" src/backend/database/routes/identity-birth-orchestrator.ts` unchanged from baseline.
    - The `task` pair-push appears AFTER the `avatar` pair-push in file source order (`grep -n 'pairs.push' src/backend/database/routes/identity-birth-orchestrator.ts` — task line number > avatar line number).
    - `npx vitest run src/backend/database/routes/identity-birth-orchestrator.role-frontmatter.test.ts -t "task"` reports >= 5 task-specific passing cases.
    - No new orchestrator step introduced: `grep -cE "step:\\s*[0-9]+:" src/backend/database/routes/identity-birth-orchestrator.ts` unchanged from baseline.
  </acceptance_criteria>
  <done>BirthOptions carries optional task; buildIdentityFileBody emits task after avatar with absent-⇒-omit; atomic-write invariant preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Accept task in identity-birth + identity-clone POST bodies (with clone-test unconditional extension)</name>
  <files>src/backend/database/routes/identity-birth.ts, src/backend/database/routes/identity-birth.test.ts, src/backend/database/routes/identity-clone.ts, src/backend/database/routes/identity-clone.test.ts</files>
  <read_first>
    - src/backend/database/routes/identity-birth.ts:84-127 (existing body validation for name/title/colorHue/voice — nullable-optional shape for colorHue/voice is the pattern to mirror)
    - src/backend/database/routes/identity-birth.ts:158-165 (getMatrixAdminCreds fail-early 503 gate — do NOT re-order this; new task validation lands BEFORE this OR after, but MUST land BEFORE flushHeaders() at L174 so 400s surface as JSON not SSE)
    - src/backend/database/routes/identity-birth.ts:232-247 (birthIdentity call site — thread task through opts)
    - src/backend/database/routes/identity-birth.test.ts (existing body-validation tests + orchestrator wire tests)
    - src/backend/database/routes/identity-clone.ts:1-160 (clone POST handler)
    - src/backend/database/routes/identity-clone.ts:619-693 (clone frontmatter-pairs assembly at L651-665 and writeMarkdownFileAtomic call at L679 — add task pair alongside voice/avatar with absent-⇒-omit; do NOT read source.task per A3)
    - src/backend/database/routes/identity-clone.test.ts (existing 981-line test file — extend the clone body-validation describe block with task cases; the file EXISTS, extension is unconditional per revision 2026-09-06 warnings 5+6)
    - .planning/phases/80-*/80-PATTERNS.md §"src/backend/database/routes/identity-birth.ts — accept task in request body" and §"src/backend/database/routes/identity-clone.ts — decide task inheritance"
  </read_first>
  <behavior>
    - POST /identities/birth: request body may include `task`. If `task` is neither `null`, `undefined`, nor a string → 400 `{ error: "task must be a string or null" }`. If `task` is a string with `.length > 500` → 400 `{ error: "task must be ≤500 chars" }`. Valid task string is `.trim()`ed and threaded to `birthIdentity({ ..., task: parsedTask ?? undefined, ... })`.
    - POST /identities/clone: identical validation shape. Task written to the cloned identity's frontmatter by pushing `["task", parsedTask]` onto `cloneFrontmatterPairs` (identity-clone.ts:651-665) AFTER the existing `["avatar", ...]` push when `typeof parsedTask === "string" && parsedTask.trim().length > 0` — mirrors the birth-orchestrator's absent-⇒-omit pattern. Source identity's `task` field is IGNORED — clone gets fresh task per each spawn (A3 lock).
    - 400 validation errors on task land BEFORE `flushHeaders()` / SSE stream open in identity-birth.ts (landmine RESEARCH §7 preserved — errors are JSON, not SSE frames).
    - `getMatrixAdminCreds()` 503 fail-early gate unchanged.
    - `birthIdentity` receives `opts.task` typed as `string | undefined` (matches BirthOptions from task 2).
  </behavior>
  <action>
    In `identity-birth.ts` around L84-127: add task extraction from `req.body` alongside other fields. Add validation block (mirror `colorHue`/`voice` nullable-optional shape): `if (task !== null && task !== undefined && typeof task !== "string") { res.status(400).json({ error: "task must be a string or null" }); return; } if (typeof task === "string" && task.length > 500) { res.status(400).json({ error: "task must be ≤500 chars" }); return; } const parsedTask = (typeof task === "string" ? task.trim() : null) as string | null;` Place this BEFORE the SSE flushHeaders call and BEFORE any long-running work opens (currently L174).

    In the `birthIdentity(...)` call site (L232-247): add `task: parsedTask ?? undefined,` to the opts object. Positional / by-key placement: near voice / colorHue in the existing shape.

    In `identity-birth.test.ts`: extend body-validation describe block with cases: (a) task="build" → 200 (or whatever birth-success shape returns), task threaded to orchestrator (assert via spy on birthIdentity); (b) task=null → 200, task not threaded (parsedTask stays null → `??` yields undefined → orchestrator sees undefined); (c) task=42 → 400 with error string; (d) task="x".repeat(501) → 400 with error string; (e) task absent from body → 200 (backward-compat with pre-Phase-80 clients).

    In `identity-clone.ts`: mirror the birth body-validation block for task (same 400 responses, same 500-char cap). In `cloneFrontmatterPairs` assembly (L651-665), after the existing `if (avatarExt !== null) { cloneFrontmatterPairs.push(["avatar", ...]); }` block, add: `if (typeof parsedTask === "string" && parsedTask.trim().length > 0) { cloneFrontmatterPairs.push(["task", parsedTask]); }`. Do NOT read `source.task` from the source identity's re-read frontmatter — clone always uses the request's `task` value (A3).

    In `identity-clone.test.ts` (UNCONDITIONAL — the file exists at `src/backend/database/routes/identity-clone.test.ts`, 981 lines; revision 2026-09-06 warning 5+6 flagged the conditional wording): extend the clone body-validation describe block with the same 5 cases as identity-birth.test.ts (task present → clone succeeds and emitted frontmatter contains `task:`; task absent → clone succeeds and emitted frontmatter has NO `task:` key; task=42 → 400; task=501-char string → 400; task=null → succeeds with no task in frontmatter). Add ONE additional A3 regression case: source identity's on-disk frontmatter has `task: "original task"`, POST body has `task: "fresh task"` (or absent) — assert the cloned frontmatter's `task:` is EITHER "fresh task" OR absent (never "original task"). This regression guards against future refactors accidentally reading source.task.
  </action>
  <verify>
    <automated>npx vitest run src/backend/database/routes/identity-birth.test.ts src/backend/database/routes/identity-clone.test.ts --reporter=verbose 2>&amp;1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'task must be a string or null' src/backend/database/routes/identity-birth.ts` prints `1`.
    - `grep -c 'task must be ≤500 chars' src/backend/database/routes/identity-birth.ts` prints `1`.
    - `grep -c 'task must be a string or null' src/backend/database/routes/identity-clone.ts` prints `1`.
    - `grep -c 'task must be ≤500 chars' src/backend/database/routes/identity-clone.ts` prints `1`.
    - `grep -c 'parsedTask' src/backend/database/routes/identity-birth.ts` prints at least `2` (declaration + call-site use).
    - `grep -c 'cloneFrontmatterPairs.push(\\[\"task\"' src/backend/database/routes/identity-clone.ts` prints exactly `1`.
    - Validation lands before flushHeaders: `grep -n 'flushHeaders\\|task must be' src/backend/database/routes/identity-birth.ts` — task validation line numbers < flushHeaders line number.
    - `npx vitest run src/backend/database/routes/identity-birth.test.ts -t "task"` reports >= 4 task-specific passing cases.
    - `npx vitest run src/backend/database/routes/identity-clone.test.ts -t "task"` reports >= 5 task-specific passing cases (5 validation + 1 A3 regression).
    - `identity-clone.ts` does NOT read `source.task` for the clone: `grep -cE 'source(Identity)?\\.task|sourceCosmetics\\.task' src/backend/database/routes/identity-clone.ts` prints `0` (A3 enforced by absence).
  </acceptance_criteria>
  <done>Both birth + clone POST endpoints accept and validate optional task; 500-char cap enforced; validation errors land as JSON before SSE opens; clone does not inherit source.task; identity-clone.test.ts extended unconditionally with task + A3 regression coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → POST /identities/birth | Task string is user-controlled — must be validated + capped |
| HTTP client → POST /identities/clone | Same |
| Identity file frontmatter → yaml.dump | Task string containing YAML metacharacters (colons, quotes, newlines) must be safely quoted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-03-01 | Tampering | Task string YAML injection (colons/newlines corrupt frontmatter) | mitigate | `yaml.dump` with `forceQuotes: false` auto-quotes strings containing YAML metacharacters (T-66-01-04 precedent). Test asserts round-trip: `yaml.load(yaml.dump({task: "fix: pool"})).task === "fix: pool"`. |
| T-80-03-02 | Denial of Service | Oversized task string (e.g., 10MB) exhausts frontmatter write / memory | mitigate | Hard cap `task.length > 500` → 400. Frontend soft-caps ~200 chars; server hard-caps 500 as defense-in-depth. |
| T-80-03-03 | Info Disclosure | Frontmatter parse errors on task field leak to frontend | mitigate | Outer try/catch swallow in `extractCosmeticsFromFrontmatter` (RESEARCH §5 landmine) returns `{}` on parse fail → publicIdentity emits `task: null` → frontend sees null gracefully. Do NOT harden read path to throw. |
| T-80-03-04 | Elevation of Privilege | POST /identities/clone body task overrides source identity permissions | accept | Task is display metadata, not auth material. Not applicable. |
| T-80-03-05 | Tampering | Task field written mid-flight leaves partial file state | mitigate | `buildIdentityFileBody` returns one atomic string → `writeMarkdownFileAtomic` (uses `ext_openssh_rename` for atomic overwrite per RESEARCH §12). Task is part of the single atomic string; no half-written state possible. |
| T-80-03-06 | Tampering | Clone accidentally inherits source.task (A3 violation) | mitigate | Test asserts source frontmatter with `task: "original"` + POST body with `task: "fresh"` yields cloned frontmatter with `task: "fresh"` (never "original"); grep gate asserts absence of `source.task` reads in clone handler. |
</threat_model>

<verification>
- All test suites listed in `<files>` pass (extension of both `identity-birth.test.ts` and `identity-clone.test.ts` is unconditional and their existence has been verified on disk).
- No new orchestrator SSE step introduced (grep gate above).
- Task field visible on GET /identities responses; task field written to frontmatter on birth + clone; task field re-read on subsequent GET /identities calls.
- Clone handler contains zero `source.task` / `sourceIdentity.task` reads (A3 regression guard).
</verification>

<success_criteria>
- `extractCosmeticsFromFrontmatter` narrows `task?: string` per existing cosmetics pattern.
- `publicIdentity` emits `task: string | null` on every returned identity.
- `BirthOptions.task?: string` optional; `buildIdentityFileBody` emits `task:` after `avatar:` when present-and-non-empty.
- POST /identities/birth + POST /identities/clone accept + validate + cap task per behavior spec above.
- `identity-clone.ts` does NOT copy `source.task` (A3); `identity-clone.test.ts` regression case guards this.
- Landmines preserved: parse-error swallow (§5), atomic-write invariant (§12), fail-early 503 gate (§7), no-rollback (§12), no new SSE step.
</success_criteria>

<output>
Create `.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-03-SUMMARY.md` when done.
</output>
