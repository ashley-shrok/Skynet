---
phase: 80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool
plan: 07
type: execute
wave: 3
depends_on:
  - 80-05
files_modified:
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-view/PrettyView.task-pill.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "The PrettyView chat surface renders a task pill centered at the top of the identity area when `pvIdentity.task` is present-and-truthy (D-02, D-06)."
    - "The pill uses the SAME glass treatment as IdentityBadge, hue-tinted from `pvIdentity.colorHue` (D-02: 'glass treatment matching the identity badge, hue-tinted from the identity's colorHue')."
    - "The pill is a fallback-safe sibling of IdentityBadge — when `pvIdentity?.task` is null/empty, NO pill renders (D-06 fallback: 'no pill on chat surface')."
    - "z-index discipline: pill sits BELOW IdentityBadge (`z-[100]` vs badge's `z-[101]`) so badge drag/click stays priority (RESEARCH §Stream 7 A5)."
    - "Text overflow handled via `whiteSpace: nowrap; overflow: hidden; textOverflow: ellipsis; maxWidth: 50%` so long tasks truncate rather than wrap or break the layout."
  artifacts:
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "New task pill JSX block, sibling to IdentityBadge, gated on pvIdentity?.task"
      contains: "task"
    - path: "src/ui/features/pretty-view/PrettyView.task-pill.test.tsx"
      provides: "Tests for pill present when task truthy, absent when null, hue-tinted from colorHue"
      min_lines: 40
  key_links:
    - from: "PrettyView task pill JSX"
      to: "pvIdentity.task + pvIdentity.colorHue (from identities-store via existing pvIdentity resolution)"
      via: "React render gate"
      pattern: "pvIdentity.task"
    - from: "PrettyView task pill glass style"
      to: "IdentityBadge glass formula (hsla(hue, ...) linear-gradient)"
      via: "inline-style copy with adjusted padding + shadow"
      pattern: "linear-gradient.*hsla"
---

<objective>
Add the task pill to the PrettyView chat surface: a hue-tinted glass-treatment pill centered at the top, rendered as a sibling to `IdentityBadge`, gated on `pvIdentity?.task` truthy (D-02, D-06). Copies the IdentityBadge glass formula with adjusted padding/shadow. Implements the chat-surface half of the phase's "task pill on chat" scope.

Purpose: Show "this identity's current task" visually on the chat surface, wearing this identity's face (per shape D-02 rationale).
Output: Additive JSX block in PrettyView.tsx + new test file. No CSS changes (all styling inline like IdentityBadge).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-CONTEXT.md
@.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-RESEARCH.md
@.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-PATTERNS.md
@src/ui/features/pretty-view/PrettyView.tsx
@src/ui/features/terminal/IdentityBadge.tsx
@src/ui/api/identities-api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add task-pill JSX sibling to IdentityBadge in PrettyView</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/PrettyView.tsx:2940-3020 (PrettyView root + IdentityBadge mount at L3011-3019 — new pill JSX lands as a sibling here)
    - src/ui/features/pretty-view/PrettyView.tsx (grep for `pvIdentity` to confirm the identity object is in scope where the pill renders; grep for `pvHue` if that variable is already defined, else use `pvIdentity?.colorHue ?? 35`)
    - src/ui/features/terminal/IdentityBadge.tsx:101-116 (glass formula — copy exact hsla values, adjust padding/shadow for pill sizing)
    - src/ui/features/terminal/IdentityBadge.tsx (grep for `z-[101]` to confirm the badge z-index; pill uses z-[100])
    - .planning/phases/80-*/80-PATTERNS.md §"src/ui/features/pretty-view/PrettyView.tsx — add task pill" (annotated JSX example)
    - .planning/phases/80-*/80-RESEARCH.md §Stream 7 (annotated JSX + z-index discipline note)
  </read_first>
  <behavior>
    - When `pvIdentity?.task` is truthy AND `pvIdentityKey` is set (matches the IdentityBadge render gate), a new pill `<div>` renders as a sibling to `<IdentityBadge>` at the same nesting level.
    - Pill positioning: `absolute top-4 left-1/2 -translate-x-1/2 z-[100]` (absolute-centered top, below badge z-index).
    - Pill styling (inline-style copy of IdentityBadge glass formula with adjusted padding/shadow):
      - `borderRadius: 20`
      - `padding: "6px 14px"`
      - `maxWidth: "50%"`
      - `background: linear-gradient(160deg, hsla(<hue>, 45%, 25%, 0.72), hsla(<hue>, 40%, 15%, 0.82))`
      - `backdropFilter: "blur(24px) saturate(1.4)"` + `WebkitBackdropFilter`
      - `border: 1px solid hsla(<hue>, 65%, 55%, 0.4)`
      - `boxShadow: 0 4px 12px rgba(0,0,0,0.4), inset 0 1px 0 rgba(255,220,170,0.14), 0 0 24px hsla(<hue>, 65%, 55%, 0.24)`
      - `color: "#e8e4d8"`, `fontSize: 13`, `fontWeight: 500`
      - `whiteSpace: "nowrap"`, `overflow: "hidden"`, `textOverflow: "ellipsis"`
      - `animation: "pv-identity-breathe 5s ease-in-out infinite"` (same animation as badge — pill breathes in sync)
    - Hue = `pvIdentity.colorHue ?? 35` (fallback matches IdentityBadge default).
    - Pill className includes `pv-identity-breathe select-none font-[Inter_Variable,ui-sans-serif,system-ui,sans-serif]` for animation hook + typography.
    - Text content: `{pvIdentity.task}` rendered as plain text child (React auto-escapes — no `dangerouslySetInnerHTML`).
    - When `pvIdentity?.task` is null/undefined/empty → the whole pill block is not rendered (D-06 fallback: no pill).
  </behavior>
  <action>
    Open `src/ui/features/pretty-view/PrettyView.tsx`. Locate the IdentityBadge mount region around L3011-3019 (the `{pvIdentityKey && (<IdentityBadge ... />)}` block). Add the new pill JSX block as a sibling — position it BEFORE or AFTER the IdentityBadge mount depending on which yields the desired DOM stacking order (executor's discretion; z-index handles paint order regardless). The pill's rendering condition is `pvIdentity?.task && pvIdentityKey`. Use the exact glass-formula values enumerated in `<behavior>`. Read the hue from `pvIdentity?.colorHue ?? 35` (or use whatever variable name PrettyView already computes for the badge's hue — grep for `pvHue` or `colorHue` in the file to match existing style).

    If PrettyView already extracts an intermediate `pvHue` variable for IdentityBadge, reuse it for consistency; otherwise inline `(pvIdentity.colorHue ?? 35)`.

    Optional extraction (executor's discretion, low priority): if the pill JSX gets unwieldy inline, extract into a colocated `src/ui/features/terminal/TaskPill.tsx` component taking `{ task: string; colorHue: number | null }` props. Extraction is NOT required for this plan; inline is acceptable.

    Do NOT modify IdentityBadge itself. Do NOT introduce CSS-file changes (all styling is inline, matching IdentityBadge's approach). Do NOT modify existing PrettyView animation or breathe keyframes. Commit prefix: `feat(80-07)`.
  </action>
  <verify>
    <automated>grep -c 'pvIdentity?.task\\|pvIdentity\\.task' src/ui/features/pretty-view/PrettyView.tsx | grep -qE '^[1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pvIdentity?.task\\|pvIdentity\\.task' src/ui/features/pretty-view/PrettyView.tsx` prints at least `1` (render gate + text content).
    - `grep -c 'z-\\[100\\]' src/ui/features/pretty-view/PrettyView.tsx` prints at least `1` (pill z-index).
    - Pill glass formula copies from IdentityBadge: `grep -c 'linear-gradient(160deg, hsla' src/ui/features/pretty-view/PrettyView.tsx` prints at least `1` (new pill inline style).
    - `grep -c 'pv-identity-breathe' src/ui/features/pretty-view/PrettyView.tsx` shows an increase (pill uses the same animation).
    - `grep -c 'top-4 left-1/2 -translate-x-1/2' src/ui/features/pretty-view/PrettyView.tsx` prints at least `1` (pill positioning).
    - Text is a plain child, not dangerouslySetInnerHTML: `grep -c 'dangerouslySetInnerHTML' src/ui/features/pretty-view/PrettyView.tsx` prints `0` (or same count as baseline — no new occurrences).
    - IdentityBadge unchanged: `git diff src/ui/features/terminal/IdentityBadge.tsx` empty (verified via `git status`).
    - No new CSS file changes: `git diff --stat src/ui/features/pretty-view/*.css` empty (or nothing listed).
    - `npx tsc --noEmit` no new errors in PrettyView.tsx.
  </acceptance_criteria>
  <done>Task pill renders as sibling to IdentityBadge with correct glass formula, hue tint, positioning, z-index, and fallback semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write PrettyView.task-pill.test.tsx — pill presence + hue + fallback</name>
  <files>src/ui/features/pretty-view/PrettyView.task-pill.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/PrettyView.tsx (locate the pill block just added; understand which props/store state is needed to render pvIdentity with task)
    - Existing PrettyView test scaffolding — search `src/ui/features/pretty-view/*.test.tsx` for the closest analog test setup (Provider wrapping, identities-store mock, etc.). If no existing PrettyView tests are present, use the closest colocated test pattern under `src/ui/features/`.
    - src/ui/state/identities-store.ts (if consumed — how to inject a test identity with task field)
  </read_first>
  <behavior>
    - Test 1: When identities-store yields `pvIdentity = { task: "build the pool endpoint", colorHue: 200, ... }`, the rendered PrettyView contains an element with text "build the pool endpoint".
    - Test 2: When `pvIdentity.task = null`, no pill element renders (assert via `queryByText` returning null or by absence of a specific data-testid if executor adds one).
    - Test 3: When `pvIdentity.task = ""` (empty string), no pill renders.
    - Test 4: Pill background contains `hsla(200,` (or matching hue-tinted linear-gradient) — assert via inspection of the element's inline `style` attribute or `getComputedStyle` string containment. If jsdom limitations prevent this, assert via `element.getAttribute('style')` containing `hsla(200,`.
    - Test 5: When `pvIdentity.colorHue = null`, pill background falls back to hue 35 (default) — assert via style string containing `hsla(35,`.
  </behavior>
  <action>
    Create `src/ui/features/pretty-view/PrettyView.task-pill.test.tsx`. Follow the exact test-setup pattern of the closest existing PrettyView-related test file (search `src/ui/features/pretty-view/*.test.tsx` and mirror). Use `@testing-library/react` render + `screen.queryByText` / `screen.getByText` for pill assertions. Mock the identities-store to yield the test identity with configurable task/colorHue. If PrettyView requires a complex Provider wrapper, factor a `renderPrettyView(identity)` helper. Enumerate the 5 test cases above. Commit prefix: `test(80-07)`.

    If the executor discovers that PrettyView's test scaffolding is too heavy for a unit test (e.g., requires terminal-emulator mocks), fall back to extracting the pill into `src/ui/features/terminal/TaskPill.tsx` first and testing THAT component in isolation. Document the choice in the plan summary.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/PrettyView.task-pill.test.tsx --reporter=verbose 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/features/pretty-view/PrettyView.task-pill.test.tsx` exists with >= 5 test cases (`grep -cE "^\\s*(it|test)\\(" src/ui/features/pretty-view/PrettyView.task-pill.test.tsx` >= 5).
    - `npx vitest run src/ui/features/pretty-view/PrettyView.task-pill.test.tsx` all cases pass.
    - Test file asserts BOTH presence and absence cases (grep for `queryByText\\|toBeNull\\|not.toBeInTheDocument` in the test file — at least one such assertion present).
    - Test file asserts hue-tint (grep for `hsla(200\\|hsla(35`) — at least one hue-color assertion.
    - No regressions in other tests: `npx vitest run --related src/ui/features/pretty-view/PrettyView.tsx` all green.
  </acceptance_criteria>
  <done>Task-pill test file ships with full coverage; pill behavior verified in jsdom.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| identity-store (task field from GET /identities) → React render | User-facing text rendered from backend-supplied string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-07-01 | Tampering | XSS via task string containing `<script>` or event handlers | mitigate | React auto-escapes text children. Task rendered as `{pvIdentity.task}` (plain child), NOT `dangerouslySetInnerHTML`. Test could add a case with `<img onerror=...>` string and assert it renders as literal text. |
| T-80-07-02 | Info Disclosure | Task string with sensitive info displayed to anyone viewing the chat surface | accept | Task is intentionally public within the Skynet UI (that's the point — visible task on the chat surface). Users must not put secrets in task strings (documented externally). |
| T-80-07-03 | Denial of Service | Malformed colorHue crashes inline-style computation | mitigate | `pvIdentity.colorHue ?? 35` fallback ensures numeric hue is always present. `hsla(35, ...)` is valid CSS. Test 5 asserts this. |
</threat_model>

<verification>
- Test file passes 5+ cases.
- Manual visual check (orchestrator ship-gate, not executor scope): opening the chat surface for an identity with `task` in frontmatter shows the pill centered at top, hue-tinted, breathing in sync with the badge.
- Fallback verified: identity without task → no pill visible.
</verification>

<success_criteria>
- Pill JSX shipped inline in PrettyView OR extracted to TaskPill.tsx (executor discretion).
- Test file covers 5+ behaviors.
- z-index discipline maintained (pill z-[100] < badge z-[101]).
- No CSS file changes; no IdentityBadge changes; no store changes.
</success_criteria>

<output>
Create `.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-07-SUMMARY.md` when done, noting whether extraction to TaskPill.tsx was performed and why.
</output>
