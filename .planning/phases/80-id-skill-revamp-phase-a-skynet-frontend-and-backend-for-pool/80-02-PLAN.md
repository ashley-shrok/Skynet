---
phase: 80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/matrix/matrix-admin-client.ts
  - src/backend/matrix/matrix-admin-client.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "The backend can query the Synapse admin API for the count of user accounts whose MXID matches a given prefix, including deactivated accounts (needed because Synapse deactivates but never deletes username reservations)."
    - "The primitive returns a discriminated union `{ok: true; total: number} | AdminErr` matching the byte-shape of existing matrix-admin-client primitives (loginAsUser, createOrUpdateUser)."
    - "All existing error taxonomy is reused (ERR_CREDS_MISSING / ERR_NON_2XX / ERR_TIMEOUT / ERR_PROXY); no new error strings introduced."
  artifacts:
    - path: "src/backend/matrix/matrix-admin-client.ts"
      provides: "New export `countUsersMatching(prefix: string): Promise<CountUsersOk | AdminErr>`"
      exports: ["countUsersMatching", "CountUsersOk"]
    - path: "src/backend/matrix/matrix-admin-client.test.ts"
      provides: "Unit tests for countUsersMatching covering happy path, missing creds, non-2xx, timeout, proxy error, malformed response"
  key_links:
    - from: "src/backend/matrix/matrix-admin-client.ts::countUsersMatching"
      to: "Synapse admin API GET /_synapse/admin/v2/users?user_id=<prefix>&deactivated=true&limit=1"
      via: "fetch with AbortController timeout"
      pattern: "_synapse/admin/v2/users.*deactivated=true"
    - from: "countUsersMatching"
      to: "getMatrixAdminCreds()"
      via: "creds gate at function entry"
      pattern: "getMatrixAdminCreds"
---

<objective>
Add the `countUsersMatching(prefix)` primitive to `src/backend/matrix/matrix-admin-client.ts`, mirroring the byte-shape of existing primitives (`loginAsUser`, `createOrUpdateUser`). This primitive is the substrate the pool-pick endpoint (plan 80-04) uses to check whether a pool-name-derived MXID is already taken on the target Synapse server, and the ordinal-derivation logic at birth-time uses to compute suffix (`Willow-Skynet-Maintainer-2`, etc.).

Purpose: Enable server-side "is this MXID handle taken?" checks against the Synapse admin API using the same auth + timeout + error taxonomy as existing primitives.
Output: One new exported primitive + comprehensive unit tests. No new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-RESEARCH.md
@.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-PATTERNS.md
@src/backend/matrix/matrix-admin-client.ts
@src/backend/matrix/matrix-admin-client.test.ts
@src/backend/matrix/matrix-admin-creds-store.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add countUsersMatching primitive + unit tests</name>
  <files>src/backend/matrix/matrix-admin-client.ts, src/backend/matrix/matrix-admin-client.test.ts</files>
  <read_first>
    - src/backend/matrix/matrix-admin-client.ts (whole file — especially loginAsUser L127-171 as byte-shape mirror; existing error taxonomy constants ERR_CREDS_MISSING / ERR_NON_2XX / ERR_TIMEOUT / ERR_PROXY; REQUEST_TIMEOUT_MS; AdminOk/AdminErr discriminated union at L40-42)
    - src/backend/matrix/matrix-admin-client.test.ts (existing primitive test patterns — mock-fetch shape, timeout assertions, error-taxonomy assertions)
    - src/backend/matrix/matrix-admin-creds-store.ts:29,163-166 (getMatrixAdminCreds shape — returns null when foundation not ingested)
    - .planning/phases/80-*/80-PATTERNS.md §"src/backend/matrix/matrix-admin-client.ts — add countUsersMatching"
    - .planning/phases/80-*/80-RESEARCH.md §Pattern 4 (annotated example) and §"Assumptions Log A1" (URL construction detail: encodeURIComponent on prefix; deactivated=true INCLUDES deactivated accounts)
  </read_first>
  <behavior>
    - `countUsersMatching(prefix: string)` returns `Promise<CountUsersOk | AdminErr>` where `CountUsersOk = AdminOk<{ total: number }>`.
    - When `getMatrixAdminCreds()` returns null → `{ ok: false, status: 500, error: ERR_CREDS_MISSING }`.
    - When Synapse returns 2xx with `{ total: 5 }` → `{ ok: true, total: 5 }`.
    - When Synapse returns 2xx with body missing `total` → `{ ok: true, total: 0 }` (safe default).
    - When Synapse returns non-2xx (e.g. 403) → `{ ok: false, status: 403, error: ERR_NON_2XX }`.
    - When fetch AbortController fires (REQUEST_TIMEOUT_MS elapsed) → `{ ok: false, status: 504, error: ERR_TIMEOUT }`.
    - When fetch throws non-abort error → `{ ok: false, status: 502, error: ERR_PROXY }` AND `databaseLogger.error("matrix admin proxy error", err, { operation: "matrix_admin_count_users" })` called once.
    - URL construction: `${creds.homeserverBase}/_synapse/admin/v2/users?user_id=${encodeURIComponent(prefix)}&deactivated=true&limit=1`.
    - `clearTimeout(timeoutId)` invoked in BOTH success and error paths.
    - Never logs the admin token, never logs the response body.
  </behavior>
  <action>
    Open `src/backend/matrix/matrix-admin-client.ts`. Add a new exported type `CountUsersOk = AdminOk<{ total: number }>` (or inline shape if that matches the file's existing type-declaration style — copy whichever pattern `loginAsUser`'s return type uses). Add the new exported async function `countUsersMatching(prefix: string): Promise<CountUsersOk | AdminErr>` following the byte-shape of `loginAsUser` (L127-171) exactly: creds gate → URL construction (with `encodeURIComponent(prefix)`, `deactivated=true`, `limit=1`) → AbortController + `REQUEST_TIMEOUT_MS` timeout → fetch(GET, Authorization Bearer + Content-Type JSON) → clearTimeout on both branches → response.ok check → JSON parse with `{ total?: number }` shape narrowing (fallback to `0`) → catch DOMException AbortError → catch other via `databaseLogger.error("matrix admin proxy error", err, { operation: "matrix_admin_count_users" })`. Do NOT introduce new error constants; reuse ERR_CREDS_MISSING / ERR_NON_2XX / ERR_TIMEOUT / ERR_PROXY.

    Extend `src/backend/matrix/matrix-admin-client.test.ts` with a new `describe("countUsersMatching", ...)` block. Test cases enumerated in `<behavior>`. Use the existing test file's fetch-mock and `getMatrixAdminCreds` mock patterns (do not invent new mocking scaffold). Assert URL shape includes `user_id=`, `deactivated=true`, and `limit=1`. Assert Authorization header contains `Bearer <token>`. Assert timeout uses `vi.useFakeTimers()` + fast-forward if the existing file uses that pattern, otherwise mirror whatever the existing timeout test uses. Commit with prefix `feat(80-02)` for source, `test(80-02)` for test (or combined).

    Executor scope stops at unit tests. Do NOT run any integration test against a live Synapse server — that is orchestrator-scope ship-gate.
  </action>
  <verify>
    <automated>npx vitest run src/backend/matrix/matrix-admin-client.test.ts --reporter=verbose 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function countUsersMatching" src/backend/matrix/matrix-admin-client.ts` prints `1`.
    - `grep -c "encodeURIComponent" src/backend/matrix/matrix-admin-client.ts` increases by at least 1 vs baseline (defense per T-75-05 pattern; new call site added).
    - `grep -c "matrix_admin_count_users" src/backend/matrix/matrix-admin-client.ts` prints `1`.
    - `grep -c "deactivated=true" src/backend/matrix/matrix-admin-client.ts` prints at least `1`.
    - `grep -c "limit=1" src/backend/matrix/matrix-admin-client.ts` prints at least `1`.
    - New error-constant strings do NOT appear (no `ERR_COUNT_*` invented) — `grep -c "ERR_COUNT" src/backend/matrix/matrix-admin-client.ts` prints `0`.
    - `npx vitest run src/backend/matrix/matrix-admin-client.test.ts -t "countUsersMatching"` reports at least 5 passing test cases.
    - No `console.log(token)` or `console.log(response.body)` — `grep -E "console\\.(log|info)" src/backend/matrix/matrix-admin-client.ts | wc -l` returns same count as baseline (no new prints).
  </acceptance_criteria>
  <done>`countUsersMatching` primitive shipped in matrix-admin-client with byte-shape parity to existing primitives; test coverage matches sibling primitives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skynet backend → Synapse admin API | Server-to-server, authenticated via bearer admin token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-02-01 | Info Disclosure | Admin bearer token leaks into logs | mitigate | Never log `creds.accessToken`; only log `{operation: "matrix_admin_count_users"}` metadata. Mirrors existing loginAsUser discipline. |
| T-80-02-02 | Info Disclosure | Synapse response body (may contain user metadata) leaks into logs | mitigate | Never pass `parsed` or `response` into `databaseLogger.error`; only log the `err` and `operation`. |
| T-80-02-03 | Tampering | User-controlled `prefix` string injects URL params (e.g., `&admin=true`) | mitigate | `encodeURIComponent(prefix)` on URL path assembly — same discipline as existing primitives (T-75-05 pattern). Unit-tested via URL-shape assertion. |
| T-80-02-04 | Denial of Service | Slow Synapse causes hung requests | mitigate | AbortController with `REQUEST_TIMEOUT_MS` — same timeout constant as existing primitives. Test asserts abort → 504 return. |
</threat_model>

<verification>
- Unit tests pass for all 5+ behavior cases.
- Byte-shape parity with `loginAsUser` verified by manual code review during executor pass (same imports, same error constants, same discriminated union return, same timeout discipline).
- No new npm dependencies introduced (`git diff package.json` should be empty for this plan).
</verification>

<success_criteria>
- `countUsersMatching` exported from matrix-admin-client and callable with a string prefix.
- Returns discriminated union `{ok: true; total: number} | AdminErr`.
- Full error taxonomy honored (creds missing → 500, non-2xx pass-through, timeout → 504, other → 502).
- All new tests pass; existing matrix-admin-client tests still pass (no regression).
</success_criteria>

<output>
Create `.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-02-SUMMARY.md` when done.
</output>
