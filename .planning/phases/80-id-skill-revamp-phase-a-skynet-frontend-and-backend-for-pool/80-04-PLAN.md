---
phase: 80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool
plan: 04
type: execute
wave: 2
depends_on:
  - 80-01
  - 80-02
files_modified:
  - src/backend/pool/pool-routes.ts
  - src/backend/pool/pool-routes.test.ts
  - src/backend/database/database.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "POST /identities/pool/pick returns a bare, lowercase pool name (e.g. `willow`) suitable for use as an identity folder key (matches IDENTITY_KEY_RE = /^[a-z0-9_-]{1,64}$/). Implements A2-locked Shape A: backend returns bare pool name; birth-orchestrator (plan 80-03b) computes ordinal-suffixed MXID at creation time inside Step 6."
    - "Picker asks the RIGHT 'is this taken' question (revision 2026-09-06 blocker 2 fix): for each pool candidate, the endpoint composes the FULL BASE HANDLE via the same casing rule as plan 80-03b's composeMxidLocalpart (`<PoolName>-<Role>` PascalCase-hyphenated, e.g. `Willow-Skynet-Maintainer`) and queries `countUsersMatching(@<BaseHandle>:<server>)`. A bare-lowercase check would return false-negatives because real MXIDs are minted with the PascalCase-hyphenated shape (see A1 DIVERGE lock in plan 80-03b)."
    - "The endpoint enforces JWT auth via existing AuthManager middleware; cross-user hostId spoofing returns 404 (not 200 with empty data)."
    - "Missing matrix-admin creds → 503 fail-early (mirrors identity-birth.ts:158-165 discipline, RESEARCH §7 landmine)."
    - "Empty pool → 503 with `pool is empty` error (deploy-time misconfiguration signal)."
    - "Malformed role (fails ROLE_NAME_RE) → 400 before ANY Synapse call — mirrors the validation that plan 80-03b's composeMxidLocalpart applies internally so the picker never queries with a doomed shape."
    - "Picker algorithm (Shape A, corrected): shuffle pool candidates, for each candidate compose `@<PascalCandidate>-<PascalHyphenatedRole>:<serverHost>` and call countUsersMatching(fullBaseHandleMxid); first with `total==0` wins and endpoint returns `{ name: candidate.toLowerCase() }` (bare lowercase pool name); all busy → fall back to first pool name (birth-orchestrator's deriveMxidWithOrdinal appends `-N` suffix at creation time — RESEARCH §8 Shape A recommendation, now unambiguously validated by A1 lock)."
    - "Route mounted at `/identities/pool` BEFORE the generic `/identities` handler in database.ts (RESEARCH §4 landmine — otherwise Express routes to `/:identityKey` and 400s because `pool` fails IDENTITY_KEY_RE)."
  artifacts:
    - path: "src/backend/pool/pool-routes.ts"
      provides: "Express router exporting `POST /pick` handler with full auth/host-resolve/creds-gate discipline; picker uses FULL base-handle MXID shape for the countUsersMatching query"
      exports: ["default (router)"]
    - path: "src/backend/pool/pool-routes.test.ts"
      provides: "Unit tests covering all validation + auth + gate + happy + fallback branches; asserts countUsersMatching is called with the full `@<PascalCandidate>-<PascalHyphenatedRole>:<server>` shape (regression guard for blocker 2)"
    - path: "src/backend/database/database.ts"
      provides: "`app.use(\"/identities/pool\", identityPoolRoutes)` mounted before generic /identities"
      contains: "/identities/pool"
  key_links:
    - from: "POST /identities/pool/pick handler"
      to: "getVettedPool() (pool-loader.ts)"
      via: "direct import + call"
      pattern: "getVettedPool"
    - from: "POST /identities/pool/pick handler"
      to: "countUsersMatching (matrix-admin-client.ts)"
      via: "direct import + call per candidate with FULL base-handle MXID"
      pattern: "countUsersMatching"
    - from: "POST /identities/pool/pick handler"
      to: "resolveHostById(hostId, userId) (host-resolver.ts)"
      via: "cross-user isolation gate"
      pattern: "resolveHostById"
    - from: "database.ts"
      to: "src/backend/pool/pool-routes.ts"
      via: "app.use(\"/identities/pool\", identityPoolRoutes)"
      pattern: "/identities/pool"
---

<objective>
Build the pool-pick backend endpoint: `POST /identities/pool/pick` that consumes the pool-loader (plan 80-01) and countUsersMatching primitive (plan 80-02) to return an unused pool name for the requested role on the requested host. Implements A2 Shape A (returns bare lowercase name; ordinal suffix derived at birth time in orchestrator plan 80-03b) with the CORRECTED handle-shape check (revision 2026-09-06 blocker 2 fix).

Purpose: Frontend NewSessionDialog (plan 80-06) calls this endpoint to prefill the name field when a role is chosen. The endpoint's "is this taken" check must use the same PascalCase-hyphenated shape that the orchestrator uses when minting the actual Matrix account — otherwise bare-lowercase checks always return `total==0` and hand back names that are already taken in their real MXID form.
Output: New router file + comprehensive tests + mount line in database.ts (positioned correctly per landmine 4).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-CONTEXT.md
@.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-RESEARCH.md
@.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-PATTERNS.md
@src/backend/database/routes/roles-list-for-host.ts
@src/backend/database/routes/identity-birth.ts
@src/backend/pool/pool-loader.ts
@src/backend/matrix/matrix-admin-client.ts
@src/backend/matrix/matrix-admin-creds-store.ts
@src/backend/ssh/host-resolver.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement POST /identities/pool/pick router with full-base-handle picker + comprehensive tests</name>
  <files>src/backend/pool/pool-routes.ts, src/backend/pool/pool-routes.test.ts</files>
  <read_first>
    - src/backend/database/routes/roles-list-for-host.ts (whole file — closest analog: same JWT auth, same hostId+resolveHostById gate, same 500 fallback handler pattern; L37-49 imports/setup, L110-127 hostId validation, L122-126 resolveHostById gate, L236-251 generic error handler)
    - src/backend/database/routes/identity-birth.ts:158-165 (getMatrixAdminCreds fail-early 503 pattern to mirror at pool-routes entry)
    - src/backend/pool/pool-loader.ts (delivered by plan 80-01 — import getVettedPool)
    - src/backend/matrix/matrix-admin-client.ts (delivered by plan 80-02 — import countUsersMatching; also existing getMatrixAdminCreds import path from matrix-admin-creds-store)
    - src/backend/matrix/matrix-admin-creds-store.ts (getMatrixAdminCreds return shape — has homeserverBase field used to derive server host for MXID construction)
    - src/backend/ssh/host-resolver.ts (resolveHostById signature and null-return semantics)
    - .planning/phases/80-*/80-PATTERNS.md §"src/backend/pool/pool-routes.ts (NEW)" and §"src/backend/pool/pool-routes.test.ts (NEW)"
    - .planning/phases/80-*/80-RESEARCH.md §Stream 3 (endpoint spec) and §Landmines 3, 7, 8, 9 (host isolation, creds gate, pool-exhaustion race, MXID divergence)
    - .planning/phases/80-*/80-03b-PLAN.md § Task 1 behavior (composeMxidLocalpart casing rule — this plan MUST use the same casing rule for its "is this taken" check, otherwise it queries the wrong shape and blocker 2 recurs)
  </read_first>
  <behavior>
    Request: `POST /identities/pool/pick` with JSON body `{ role: string, hostId: number }`.

    Validation:
    - Missing/invalid JWT → 401 (via authManager middleware).
    - `hostId` not a positive integer → 400 `{ error: "hostId must be a positive integer" }`.
    - `role` missing OR not kebab-case-lowercase (regex `/^[a-z][a-z0-9]*(-[a-z][a-z0-9]*)*$/` — SAME shape as plan 80-03b's ROLE_NAME_RE; note stricter-than-original: requires alpha leading char per segment to guarantee composeMxidLocalpart won't reject it) → 400 `{ error: "role must be kebab-case with alpha-leading segments" }`.
    - `getMatrixAdminCreds()` returns null → 503 `{ error: "matrix_admin_foundation_not_ingested", detail: "matrix admin foundation not ingested — see deploy runbook" }`.
    - `resolveHostById(hostId, userId)` returns null → 404 `{ error: "Host not found" }` (cross-user isolation defense).
    - `getVettedPool()` returns `[]` → 503 `{ error: "pool is empty — see deploy runbook" }`.

    Picker algorithm (Shape A, CORRECTED per revision 2026-09-06):
    - Extract server host from `new URL(creds.homeserverBase).host`.
    - Compose PascalCase-hyphenated role once: `const pascalRole = role.split('-').map(seg => seg[0].toUpperCase() + seg.slice(1)).join('-');`.
    - Shuffle the pool (`[...pool].sort(() => Math.random() - 0.5)`).
    - For each candidate:
      - Pool entries live in pool.json as PascalCase already (per plan 80-01 seed shape). Defense-in-depth: normalize with `const pascalCandidate = candidate[0].toUpperCase() + candidate.slice(1).toLowerCase();` so any casing drift in pool.json is corrected.
      - Compose FULL base handle MXID: `const baseHandleMxid = \`@${pascalCandidate}-${pascalRole}:${serverHost}\`;` (e.g. `@Willow-Skynet-Maintainer:matrix.example.com`).
      - Call `countUsersMatching(baseHandleMxid)`.
      - If `!result.ok` at any step → 502 `{ error: "admin API failure" }`.
      - First candidate with `result.total === 0` → return 200 `{ name: pascalCandidate.toLowerCase() }` (bare lowercase pool name — the orchestrator will re-derive the PascalCase MXID + ordinal at birth time in plan 80-03b).
    - All candidates busy → return 200 `{ name: pascalFirst.toLowerCase() }` (fallback; birth-orchestrator's deriveMxidWithOrdinal will suffix at creation time).

    Response shape: `{ name: string }` — lowercase, matches `IDENTITY_KEY_RE`. The response is ALWAYS the bare pool name, never the full handle; the orchestrator owns handle composition (A1 DIVERGE lock — folder key and MXID localpart are two distinct things).

    **Why not bare-lowercase check (blocker 2 origin, DO NOT do this):** an earlier draft of this handler queried `countUsersMatching(\`@${candidate.toLowerCase()}:${serverHost}\`)`. That check asks "is any user with MXID @willow:server registered?" — but real Matrix accounts for pool-picked identities are minted as `@Willow-Skynet-Maintainer:server` per A1. The bare-lowercase check therefore always returns `total==0` for pool names, and the picker hands back names that are ALREADY taken in their real form. The FULL-base-handle check is the only correct question to ask.

    Router MUST also install a generic error handler at the router base (mirror `roles-list-for-host.ts:236-251`).
  </behavior>
  <action>
    Create `src/backend/pool/pool-routes.ts`. Imports: `express`, `Request/Response` types, `AuthManager` from `../../utils/auth-manager.js`, `resolveHostById` from `../../ssh/host-resolver.js`, `sshLogger` from `../../utils/logger.js`, `AuthenticatedRequest` from `../../../types/index.js`, `getVettedPool` from `./pool-loader.js`, `countUsersMatching` from `../matrix/matrix-admin-client.js`, `getMatrixAdminCreds` from `../matrix/matrix-admin-creds-store.js`. Define `const ROLE_NAME_PATTERN = /^[a-z][a-z0-9]*(-[a-z][a-z0-9]*)*$/` (matches plan 80-03b's ROLE_NAME_RE so the picker never composes a handle that composeMxidLocalpart would then reject). Create router, get authManager, get authenticateJWT middleware. Register `router.post("/pick", express.json(), authenticateJWT, async (req, res) => { ... })` with the handler shape enumerated in `<behavior>`. Register the generic error handler at the end. Default export the router.

    Ordering inside the handler (STRICT — matches analog):
    1. Extract `userId` from `(req as AuthenticatedRequest).userId`.
    2. Destructure `{ role, hostId }` from `req.body`.
    3. Validate hostId (typeof === "number", Number.isInteger, > 0) → 400.
    4. Validate role (typeof === "string", ROLE_NAME_PATTERN.test(role.trim())) → 400.
    5. `getMatrixAdminCreds()` → 503 on null.
    6. `resolveHostById(hostId, userId)` → 404 on null.
    7. `getVettedPool()` → 503 on empty.
    8. Compose `pascalRole` from validated role.
    9. Shuffle + loop candidates → compose `pascalCandidate` (uppercase-first) + full `@<pascalCandidate>-<pascalRole>:<serverHost>` MXID → call countUsersMatching → 502 on any error → 200 on first `total === 0` → 200 fallback if all busy.

    Do NOT log the admin token or Synapse response bodies. Do NOT `console.log` — use `sshLogger.error` in the router-level error handler only.

    Create `src/backend/pool/pool-routes.test.ts`. Test cases (mirror shape of nearest analog test file — search for `*routes.test.ts` under `src/backend/database/routes/` to find the closest test scaffolding pattern; if the analog uses `supertest`, use `supertest` here). Mock `getMatrixAdminCreds`, `resolveHostById`, `getVettedPool`, `countUsersMatching`. Enumerate all cases:
    1. Missing JWT → 401 (may be covered by middleware — assert if analog does).
    2. Missing hostId → 400.
    3. Non-integer hostId → 400.
    4. Missing role → 400.
    5. Role uppercase/special-char → 400.
    6. Role with leading digit → 400 (`/^[a-z]/` gate).
    7. Cross-user hostId → 404 (mock resolveHostById returns null).
    8. Creds null → 503.
    9. Empty pool → 503.
    10. **Happy path (regression guard for blocker 2):** with role="skynet-maintainer", pool=["Willow", "Aster"], serverHost="matrix.example.com", mock countUsersMatching returning `{ok:true, total:0}` on ANY call — assert countUsersMatching was called with `@Willow-Skynet-Maintainer:matrix.example.com` OR `@Aster-Skynet-Maintainer:matrix.example.com` (NEVER `@willow:matrix.example.com` — the bare-lowercase shape). Assert response is `{name: "willow"}` or `{name: "aster"}` (bare lowercase).
    11. All-busy fallback: mock countUsersMatching always returns `{ok:true, total:1}`; response is `{name: <firstShuffled.toLowerCase()>}`.
    12. `countUsersMatching` returns error → 502.
    13. Multi-segment role: role="foo-bar-baz", pool=["Willow"] → countUsersMatching called with `@Willow-Foo-Bar-Baz:server` (assert exact call args).
    14. Pool.json casing drift regression: pool=["willow"] (lowercase in the JSON) → countUsersMatching STILL called with `@Willow-Skynet-Maintainer:server` (defense-in-depth normalization).

    Assert Response body is `{ name: string }` shape.

    Commit prefix: `feat(80-04)` for source, `test(80-04)` for tests (or combined).
  </action>
  <verify>
    <automated>npx vitest run src/backend/pool/pool-routes.test.ts --reporter=verbose 2>&amp;1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'router.post("/pick"' src/backend/pool/pool-routes.ts` prints `1`.
    - `grep -c 'authenticateJWT' src/backend/pool/pool-routes.ts` prints at least `2` (declaration + use).
    - `grep -c 'resolveHostById' src/backend/pool/pool-routes.ts` prints at least `1`.
    - `grep -c 'getMatrixAdminCreds' src/backend/pool/pool-routes.ts` prints at least `1`.
    - `grep -c 'matrix_admin_foundation_not_ingested' src/backend/pool/pool-routes.ts` prints `1`.
    - `grep -c 'pool is empty' src/backend/pool/pool-routes.ts` prints `1`.
    - `grep -c 'countUsersMatching' src/backend/pool/pool-routes.ts` prints at least `1`.
    - `grep -c 'ROLE_NAME_PATTERN' src/backend/pool/pool-routes.ts` prints at least `2`.
    - The regex uses the leading-alpha shape (matches plan 80-03b's ROLE_NAME_RE, prevents composeMxidLocalpart from rejecting downstream): `grep -c "\\[a-z\\]\\[a-z0-9\\]\\*(-\\[a-z\\]\\[a-z0-9\\]\\*)\\*" src/backend/pool/pool-routes.ts` prints at least `1`.
    - **Blocker 2 regression gate:** the picker does NOT compose a bare-lowercase MXID for the check — `grep -cE 'countUsersMatching\\(\\s*\`@\\$\\{[^}]*\\.toLowerCase\\(\\)\\}:' src/backend/pool/pool-routes.ts` prints `0` (bare-lowercase pattern absent from countUsersMatching call sites).
    - Picker composes the full base handle for the check: `grep -cE 'countUsersMatching\\(baseHandleMxid|countUsersMatching\\(.*pascalRole|countUsersMatching\\(.*-\\$\\{pascalRole\\}' src/backend/pool/pool-routes.ts` prints at least `1` (full-handle pattern present).
    - PascalCase composition present: `grep -c 'pascalRole\\|pascalCandidate\\|split.*\\.map.*toUpperCase' src/backend/pool/pool-routes.ts` prints at least `2`.
    - Response payload is bare lowercase (matches IDENTITY_KEY_RE): `grep -cE 'res\\.json\\(\\s*\\{\\s*name:\\s*[a-zA-Z]+\\.toLowerCase\\(\\)' src/backend/pool/pool-routes.ts` prints at least `1`.
    - `grep -c 'Math.random' src/backend/pool/pool-routes.ts` prints exactly `1` (shuffle).
    - No admin-token logging: `grep -c 'accessToken\\|creds.accessToken' src/backend/pool/pool-routes.ts` prints `0` (creds only used indirectly via countUsersMatching, which handles the auth header itself).
    - `npx vitest run src/backend/pool/pool-routes.test.ts` reports at least 13 passing test cases (`grep -cE "^\\s*(it|test)\\(" src/backend/pool/pool-routes.test.ts` >= 13).
    - Blocker 2 regression test asserts full handle: at least one test in pool-routes.test.ts contains an assertion of the form `expect(countUsersMatching).toHaveBeenCalledWith(expect.stringContaining("Willow-Skynet-Maintainer"))` or equivalent (`grep -cE 'Willow-Skynet-Maintainer|toHaveBeenCalledWith.*\\$\\{.*-' src/backend/pool/pool-routes.test.ts` >= 1).
  </acceptance_criteria>
  <done>POST /identities/pool/pick router shipped with all auth/host/creds/pool gates, Shape A picker using the CORRECT full-base-handle MXID for the countUsersMatching check (blocker 2 fix), generic error handler, and full test coverage including blocker-2 regression guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mount pool router in database.ts before generic /identities</name>
  <files>src/backend/database/database.ts</files>
  <read_first>
    - src/backend/database/database.ts:1800-1890 (route mount block; identify existing `/identities/avatar`, `/identities/birth`, `/identities/clone` mounts and the generic `/identities` mount at L1877; new mount lands with the other subpath mounts BEFORE the generic)
    - .planning/phases/80-*/80-PATTERNS.md §"src/backend/database/database.ts — mount pool router"
    - .planning/phases/80-*/80-RESEARCH.md §Landmine 4 (route mount ordering — `pool` fails IDENTITY_KEY_RE so must not fall through to generic)
  </read_first>
  <behavior>
    - `database.ts` imports the pool router from `../pool/pool-routes.js`.
    - `app.use("/identities/pool", identityPoolRoutes)` appears BEFORE `app.use("/identities", identitiesRoutes)` in file source order.
    - The mount lands in the same block as the other `/identities/xxx` sub-mounts (avatar, birth, clone) — grouped for maintainability.
    - No existing mounts are reordered or modified.
    - Server startup still succeeds (no import cycle).
  </behavior>
  <action>
    Open `src/backend/database/database.ts`. Locate the route-mount block around L1829-1877 (existing `/identities/avatar`, `/identities/birth`, `/identities/clone` mounts, ending with the generic `/identities` mount at L1877). Add an `import identityPoolRoutes from "../pool/pool-routes.js";` at the top of the file (or wherever the other route-imports live; match the existing import style). Add exactly one new line in the mount block, alongside the other `/identities/xxx` sub-mounts and BEFORE the generic mount at L1877: `app.use("/identities/pool", identityPoolRoutes);`. Add a Phase 80 comment above the mount line explaining the ordering requirement (mirror the style of the existing `Phase 20 (IDUI-04): identity avatar batch — mount BEFORE /identities` comment). Do NOT reorder any existing mounts.
  </action>
  <verify>
    <automated>grep -n '/identities/pool\\|/identities",' src/backend/database/database.ts | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'app.use("/identities/pool", identityPoolRoutes)' src/backend/database/database.ts` prints `1`.
    - `grep -c 'import identityPoolRoutes' src/backend/database/database.ts` prints `1`.
    - Mount ordering enforced: line number of `app.use("/identities/pool"` < line number of `app.use("/identities", identitiesRoutes)`.
    - Bash gate: `awk '/app.use\\("\\/identities\\/pool"/ {pool=NR} /app.use\\("\\/identities", identitiesRoutes\\)/ {gen=NR} END {exit (pool>0 && gen>0 && pool<gen) ? 0 : 1}' src/backend/database/database.ts` exits 0.
    - Existing mounts unchanged: `grep -c 'app.use("/identities/birth"' src/backend/database/database.ts` still prints `1`; same for `/identities/clone` and `/identities/avatar`.
    - No test regressions: `npx vitest run src/backend/database/database.ts --related` (or the closest existing pattern for testing database.ts wiring) reports pass (if no direct test exists, this criterion is verified by `npx vitest run --related src/backend/database/database.ts` returning green or by the pool-routes.test.ts passing since it exercises the mount indirectly).
  </acceptance_criteria>
  <done>Pool router mounted at correct position; import added; existing mounts untouched; no route-resolution regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → POST /identities/pool/pick | JWT-authed; user-controlled body |
| Skynet backend → Synapse admin API | Server-to-server; token never leaves backend |
| Skynet backend → target host DB | hostId → resolveHostById cross-user isolation |
| Picker composition → Synapse admin API | The queried MXID shape MUST match what the birth-orchestrator will actually mint (A1 lock); shape mismatch = false-negative on collision detection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-04-01 | Elevation of Privilege | Attacker-crafted `hostId` targets another user's host | mitigate | `resolveHostById(hostId, userId)` returns null on unowned; handler returns 404 (not 403, no data leak). Mirrors T-22-03-03 pattern from roles-list-for-host. |
| T-80-04-02 | Tampering | Attacker submits role="Admin" or role="../../etc" | mitigate | `ROLE_NAME_PATTERN = /^[a-z][a-z0-9]*(-[a-z][a-z0-9]*)*$/` regex gate → 400 on any non-kebab-case-lowercase input. Also gates leading-digit segments so downstream composeMxidLocalpart never rejects. |
| T-80-04-03 | Info Disclosure | Empty-pool / no-creds error responses leak deploy state | accept | 503 with `matrix_admin_foundation_not_ingested` / `pool is empty` is intentional operator-signal — required for runbook triage. Does not leak user data. |
| T-80-04-04 | Denial of Service | Attacker floods pool-pick to exhaust Synapse admin quota | mitigate | Per-request timeout on countUsersMatching (REQUEST_TIMEOUT_MS from plan 80-02); JWT gate ensures only authed users can invoke. Rate-limiting is out-of-scope for Phase 80 (existing Skynet has no rate-limit layer). |
| T-80-04-05 | Tampering | Pool-pick returns a name that fails IDENTITY_KEY_RE, causing downstream birth to 400 | mitigate | Response is `pascalCandidate.toLowerCase()` where pascalCandidate comes from pool.json entries (validated by pool-loader to be non-empty strings; normalized by uppercase-first in the picker for defense-in-depth). Since pool source is developer-controlled JSON (baked into image), the risk is a bad seed — mitigate by pool-loader validation (plan 80-01) AND by the lowercase-in-response invariant (matches IDENTITY_KEY_RE). |
| T-80-04-06 | Info Disclosure | Admin token leaks via error path when countUsersMatching fails | mitigate | Handler returns generic `{ error: "admin API failure" }` on !result.ok; does not surface the underlying error string or the creds. Test asserts response body does not contain "Bearer" or token characters. |
| T-80-04-07 | Tampering | Bare-lowercase MXID check produces false-negatives (blocker 2 origin) — picker returns names that are actually taken in their real MXID form | mitigate | Picker composes the FULL base-handle MXID `@<PascalCandidate>-<PascalHyphenatedRole>:<server>` using the SAME casing rule as plan 80-03b's composeMxidLocalpart. Regression test case 10 asserts countUsersMatching is called with a MXID containing the hyphenated PascalCase pattern, never with the bare lowercase shape. |
</threat_model>

<verification>
- pool-routes.test.ts reports >= 13 passing cases covering all validation + gate + happy + fallback + error + blocker-2 regression branches.
- database.ts mount ordering enforced by awk gate.
- Endpoint smoke-verifiable via `curl -X POST -H "Content-Type: application/json" -H "Cookie: <jwt>" -d '{"role":"skynet-maintainer","hostId":1}' http://localhost:PORT/identities/pool/pick` returning `{"name":"willow"}` (or similar bare lowercase name) — smoke test is orchestrator ship-gate; not part of executor scope.
- Blocker 2 regression: grep gate asserts absence of bare-lowercase countUsersMatching pattern; test case 10 asserts presence of full-base-handle pattern in call args.
</verification>

<success_criteria>
- POST /identities/pool/pick shipped with full gate chain (auth → hostId → role → creds → host-ownership → pool → picker).
- Picker uses FULL base-handle MXID (`@<PoolName>-<Role>:<server>` PascalCase-hyphenated) for the `countUsersMatching` "is this taken" check — matches the shape the birth-orchestrator actually mints (A1 DIVERGE lock).
- Response body always `{ name: string }` (lowercase pool name matching IDENTITY_KEY_RE).
- All 6+ error paths return correct HTTP status + JSON error object per behavior spec.
- Mounted at `/identities/pool` BEFORE generic `/identities` in database.ts.
- Zero admin-token leak surfaces.
- Blocker 2 permanently guarded by grep gate + regression test.
</success_criteria>

<output>
Create `.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-04-SUMMARY.md` when done.
</output>
