---
phase: 80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool
plan: 08
type: execute
wave: 3
depends_on:
  - 80-05
files_modified:
  - src/ui/features/pretty-conversations/PrettyConversationRow.tsx
  - src/ui/features/pretty-conversations/PrettyConversationRow.task-primary.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "When `identity?.task` is truthy, the conversation-list row renders task-primary display: top line = task string (using existing `.pv-label` class), subtitle = role prominent + `(name)` muted parens (using existing `.pv-ai-title` + `.pv-hostname-suffix` classes — D-03 lock: no new typographic vocabulary)."
    - "When `identity?.task` is null/empty, the row falls back to the CURRENT (pre-Phase-80) markup verbatim: displayName-primary + hostname-suffix + aiTitle subtitle (D-06 fallback)."
    - "The AI-generated conversation subtitle (`aiTitle`) drops entirely in the task-primary branch — not relocated to a tooltip or side panel (specifics §last bullet: 'do NOT quietly preserve it')."
    - "Existing `.pv-body`, `.pv-label`, `.pv-hostname-suffix`, `.pv-ai-title` CSS classes reused VERBATIM per D-03; no new selectors added at plan time. If pixel-tuning is discovered during executor work, a small sibling class (e.g. `.pv-role-prominent`) is allowed as an escape hatch (D-03 executor discretion)."
    - "Existing PrettyConversationRow.test.tsx tests continue passing — the fallback branch preserves the current markup verbatim so pre-Phase-80 behavior is unaffected for identities without task."
  artifacts:
    - path: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx"
      provides: ".pv-body markup swapped to gated task-primary vs fallback branches at L1273-1288 region"
      contains: "identity?.task"
    - path: "src/ui/features/pretty-conversations/PrettyConversationRow.task-primary.test.tsx"
      provides: "New test file covering task-primary render + fallback + role prominent + muted parens"
      min_lines: 40
  key_links:
    - from: "PrettyConversationRow .pv-body"
      to: "identity.task, identity.role, identity.displayName (from identities-store)"
      via: "React render gate on identity?.task truthy"
      pattern: "identity?.task"
---

<objective>
Swap the conversation-list row's `.pv-body` markup to a gated two-branch structure: task-primary display when `identity?.task` is truthy, fallback to current markup verbatim when null (D-03, D-06). Implements the phase's "task-primary conversation list rows" scope. All styling reuses existing CSS classes verbatim per D-03 — no new typographic vocabulary invented.

Purpose: Row-scannable "what is this agent working on?" surface, replacing the current "identity name + role + AI transcript summary" pattern.
Output: PrettyConversationRow.tsx `.pv-body` block rewritten with gated branches + new test file. Fallback preserved verbatim for graceful degradation on legacy identities.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-CONTEXT.md
@.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-RESEARCH.md
@.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-PATTERNS.md
@src/ui/features/pretty-conversations/PrettyConversationRow.tsx
@src/ui/features/pretty-conversations/pretty-conversations.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Swap .pv-body markup to gated task-primary vs fallback branches</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationRow.tsx</files>
  <read_first>
    - src/ui/features/pretty-conversations/PrettyConversationRow.tsx:1273-1288 (existing `.pv-body` markup — becomes the fallback branch verbatim)
    - src/ui/features/pretty-conversations/pretty-conversations.css:686-756 (existing `.pv-body`, `.pv-label`, `.pv-hostname-suffix`, `.pv-ai-title` selectors — D-03 lock: REUSE verbatim, no new selectors at plan time)
    - src/ui/features/pretty-conversations/PrettyConversationRow.tsx (grep for `identity` to confirm the identity object is in scope with `task`, `role`, `displayName` fields — should be true because plan 80-05 delivers `task` on the Identity type and role/displayName already exist)
    - .planning/phases/80-*/80-PATTERNS.md §"src/ui/features/pretty-conversations/PrettyConversationRow.tsx — task-primary body swap" (annotated JSX)
    - .planning/phases/80-*/80-RESEARCH.md §Stream 8 (annotated JSX + fallback preservation note)
  </read_first>
  <behavior>
    - The `<div className="pv-body">` block at L1273-1288 is replaced by a conditional block:
      - If `identity?.task` is truthy:
        - Top line: `<span className="pv-label">{identity.task}</span>` — no hostname suffix on top line (task stands alone).
        - Subtitle: `<span className="pv-ai-title"><strong>{identity.role}</strong>{" "}<span className="pv-hostname-suffix">({identity.displayName})</span></span>` — role prominent via `<strong>`, name in muted parens via existing `.pv-hostname-suffix` class.
        - The `aiTitle` (AI-generated transcript summary) is NOT rendered in this branch (drops entirely, per specifics §last bullet).
      - Else (fallback, D-06 — CURRENT markup verbatim):
        - `<span className="pv-label">{identity ? identity.displayName : row.label}{(identity?.title || row.host?.name) && (<span className="pv-hostname-suffix">{" "}({identity?.title || row.host?.name})</span>)}</span>`
        - `{aiTitle !== null ? (<span className="pv-ai-title">{aiTitle}</span>) : (<span className="pv-ai-title pv-ai-title--placeholder">…</span>)}`
    - No CSS file changes.
    - No `import` additions (identity already imported via existing store consumer chain).
    - No changes to any other JSX in the file (context menu, drag handles, avatar, etc. all untouched).
  </behavior>
  <action>
    Open `src/ui/features/pretty-conversations/PrettyConversationRow.tsx`. Locate the `<div className="pv-body">` block at ~L1273-1288. Replace its inner contents with a JSX ternary: `{identity?.task ? (<>{task-primary JSX}</>) : (<>{existing markup verbatim}</>)}`. The task-primary JSX matches the shape enumerated in `<behavior>` above; the else-branch is the CURRENT markup copied verbatim (executor MUST verify byte-parity of the fallback branch against pre-edit source using `git diff` — the fallback must be unchanged from before). Use `<>...</>` fragments so no extra wrapper `<div>` is introduced.

    Do NOT modify `pretty-conversations.css`. Do NOT invent new CSS selectors. If executor discovers pixel-mismatch during runtime testing (e.g., role weight inside `<strong>` renders too heavy against the existing `.pv-ai-title` weight), add a small sibling class like `.pv-role-prominent` — but ONLY if runtime testing shows a mismatch, and document the addition in the plan summary. Default assumption is verbatim reuse (D-03).

    Do NOT change the context-menu label "Clone" here — that rename is scope-owned by plan 80-06 (clone repurpose) to keep file-diff boundaries clean between plans. Commit prefix: `feat(80-08)`.
  </action>
  <verify>
    <automated>grep -c 'identity?.task ?\\|identity\\.task' src/ui/features/pretty-conversations/PrettyConversationRow.tsx | grep -qE '^[1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'identity?.task ?' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` prints at least `1` (ternary gate).
    - `grep -c '<strong>{identity.role}</strong>' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` prints at least `1` (role prominence).
    - `grep -c 'pv-hostname-suffix' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` shows an increase (subtitle uses the class again — total count >= 2: original in fallback + new in task-primary).
    - `grep -c 'pv-body' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` unchanged (still 1 — no new `.pv-body` element added).
    - Fallback branch byte-parity: the else-branch contains `{aiTitle !== null ? (` (present verbatim from pre-Phase-80 source). `grep -c 'aiTitle !== null ?' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` prints at least `1`.
    - No CSS changes: `git diff --stat src/ui/features/pretty-conversations/pretty-conversations.css` empty (or nothing listed).
    - Context menu label unchanged in this plan: `grep -c 'label: "Clone"' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` still prints `1` (plan 80-06 renames it later).
    - `npx tsc --noEmit` no new errors in this file.
  </acceptance_criteria>
  <done>.pv-body markup swapped to gated task-primary vs fallback; fallback preserved verbatim; no CSS changes; no unrelated file edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write PrettyConversationRow.task-primary.test.tsx</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationRow.task-primary.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx (existing test file for PrettyConversationRow — use its render helper and mock patterns; add task-primary tests as a new sibling test file rather than extending, to keep diff-boundaries clean)
    - src/ui/features/pretty-conversations/PrettyConversationRow.tsx (locate the ternary just added; determine what identity + row shape is needed for a render)
  </read_first>
  <behavior>
    - Test 1: When `identity.task = "build the pool endpoint"` and `identity.role = "skynet-maintainer"` and `identity.displayName = "Willow"`, the rendered row contains text "build the pool endpoint" as the top line and both "skynet-maintainer" and "(Willow)" in the subtitle. The aiTitle text does NOT appear in the row (task-primary branch drops it).
    - Test 2: When `identity.task = null`, the row falls back to the current markup: top line has `identity.displayName` and hostname suffix, subtitle has aiTitle. Regression guard for D-06 fallback.
    - Test 3: When `identity.task = ""` (empty string), fallback (task-primary requires truthy).
    - Test 4: Role appears wrapped in `<strong>` tag when task-primary — assert via `container.querySelector('strong')` presence, or by asserting the role text is inside an element with `tagName === "STRONG"`.
    - Test 5: The `.pv-hostname-suffix` span appears in the subtitle when task-primary (muted parens for name).
    - Test 6: When `identity` itself is undefined (row has no identity resolution), fallback renders `row.label` — regression guard for identity-null case (existing behavior).
  </behavior>
  <action>
    Create `src/ui/features/pretty-conversations/PrettyConversationRow.task-primary.test.tsx`. Follow the exact test-setup pattern of `PrettyConversationRow.test.tsx` (same imports, same mock helpers, same render wrapper). Use `@testing-library/react` `render` + `screen.getByText` / `screen.queryByText`. Enumerate the 6 test cases above. Commit prefix: `test(80-08)`.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-conversations/PrettyConversationRow.task-primary.test.tsx src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx --reporter=verbose 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/features/pretty-conversations/PrettyConversationRow.task-primary.test.tsx` exists with >= 6 test cases (`grep -cE "^\\s*(it|test)\\(" ...task-primary.test.tsx` >= 6).
    - Existing `PrettyConversationRow.test.tsx` still passes (no regressions).
    - New test file passes (`npx vitest run ...task-primary.test.tsx` green).
    - At least one test asserts NEGATIVE case (task=null falls back).
    - At least one test asserts the aiTitle text is NOT present in the task-primary branch (grep the test file for `queryByText.*aiTitle\\|not.toBeInTheDocument`).
  </acceptance_criteria>
  <done>Task-primary test file covers task-truthy, task-null, task-empty, role prominence, muted-parens name, and identity-null fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| identity-store (task/role/displayName from GET /identities) → React render | User-facing text rendered from backend-supplied strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-08-01 | Tampering | XSS via task/role/displayName strings containing HTML | mitigate | React auto-escapes text children. All three fields rendered as `{identity.task}`, `{identity.role}`, `{identity.displayName}` — plain children, no `dangerouslySetInnerHTML`. |
| T-80-08-02 | Denial of Service | Very long task string breaks row layout / overflows visible area | mitigate | Existing `.pv-label` CSS class already handles overflow via fade-truncation (pretty-conversations.css L686-756). Reusing the class inherits the behavior. Executor may confirm via a "long task string" test case that assertion of row height is unchanged. |
| T-80-08-03 | Info Disclosure | AI transcript summary (aiTitle) contained sensitive info now hidden by task-primary branch | accept | Alice greenlit the drop (per D-CONTEXT specifics §last bullet: "do NOT quietly preserve it"). Not a security concern; product decision. |
</threat_model>

<verification>
- Both test files pass.
- Manual visual check (orchestrator ship-gate): opening the conversation list panel for an identity with `task` in frontmatter shows task-primary display; opening for an identity without task shows the fallback (current) display.
</verification>

<success_criteria>
- `.pv-body` markup swaps to gated ternary; task-primary shows task + role-prominent-name-parens subtitle; fallback preserved verbatim.
- No CSS changes; no unrelated JSX changes.
- Test coverage: 6+ cases in the new file; no regressions in existing test file.
- D-06 fallback verified by explicit test.
</success_criteria>

<output>
Create `.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-08-SUMMARY.md` when done. If a new `.pv-role-prominent` (or similar) CSS class was added during runtime tuning, document it in the summary.
</output>
