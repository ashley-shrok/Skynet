---
phase: 80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool
plan: 06
type: execute
wave: 3
depends_on:
  - 80-04
  - 80-05
  - 80-03b
files_modified:
  - src/ui/sidebar/NewSessionDialog.tsx
  - src/ui/sidebar/NewSessionDialog.task-input.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "The unified new-agent modal has a `task` textarea input labeled 'What will this agent work on?' visible whenever `identityMode` is on; user-typed task is included in the POST /identities/birth body."
    - "When the user selects a role (and a host is chosen), the modal auto-fetches a pool-picked name via `pickPoolName(role, hostId)` and prefills the name field ONLY if the user hasn't typed a custom name yet. Pool is a suggestion source, not a restriction (D-01 shape phrase)."
    - "The modal tracks whether the currently-displayed name value came from a pool-pick prefill; when the user edits the name field, this pool-picked tracking flag flips to false. On birth submit, the request body includes `poolPicked: true` ONLY when the name value still matches the last pool-picked value (A1 lock signal — see plan 80-03b). This triggers the backend's PascalCase-hyphenated MXID derivation with silent ordinal suffix."
    - "The role-select bug (RESEARCH §2 landmine) is addressed: identityMode-on scenario no longer leaves the user staring at a modal with no role selector. Fix approach = widen host auto-pick + surface a visible 'pick a host first' affordance when host is null (see A4 recommendation)."
    - "Task input is soft-capped at 200 chars via `maxLength={200}` (D-Claude's Discretion — executor may pin exact number when badge pill renders at real widths in plan 80-07)."
    - "All existing NewSessionDialog test files continue passing (`NewSessionDialog.test.tsx`, `NewSessionDialog.chain.test.tsx`, `NewSessionDialog.role-dropdown.test.tsx`) — landmine RESEARCH §10: extend, do NOT rewrite."
    - "chain-hook mechanism intact: `initialHost`, `initialRole`, `initialBrief` still seeded on-open per existing L397-410 pattern (needed by plan 80-09 clone repurpose to reuse this modal)."
  artifacts:
    - path: "src/ui/sidebar/NewSessionDialog.tsx"
      provides: "Unified modal with task textarea + pool-name prefill + poolPicked wire signal + role-select bug fix"
      contains: "task"
    - path: "src/ui/sidebar/NewSessionDialog.task-input.test.tsx"
      provides: "New test file covering task-input presence, maxLength, task-included-in-birth-body, pool-prefill on role change, no-overwrite-if-user-typed, poolPicked-true-on-unedited-prefill, poolPicked-false-on-user-edit"
      min_lines: 80
  key_links:
    - from: "NewSessionDialog task textarea"
      to: "openBirthStream request body"
      via: "state → request body assembly"
      pattern: "task"
    - from: "NewSessionDialog useEffect on [selectedRole, selectedHost, identityMode]"
      to: "pickPoolName API call (delivered by plan 80-05)"
      via: "prefill name field + set pool-picked tracking flag"
      pattern: "pickPoolName"
    - from: "NewSessionDialog openBirthStream body"
      to: "backend BirthOptions.poolPicked (delivered by plan 80-03b)"
      via: "request body poolPicked field (true iff name unedited from pool-picked value)"
      pattern: "poolPicked"
---

<objective>
Rebuild the new-agent creation modal as a unified modal (D-01, D-04): extend the existing `NewSessionDialog.tsx` to add a task-description textarea, pool-picked name prefill on role change, `poolPicked` wire signal for the A1 MXID DIVERGE lock, and fix the role-select-not-visible-until-host-picked bug. Preserve all existing structure, tests, and the chain-hook mechanism so plan 80-09's clone repurpose can reuse this modal as the sole creation entry-point.

Purpose: Ship the user-facing creation flow that produces a task-scoped identity with a pool-picked name suggestion in one modal (shape file §Frontend creation flow). Wire the `poolPicked` signal so the backend orchestrator (plan 80-03b) can compose PascalCase-hyphenated MXIDs (`Willow-Skynet-Maintainer[-N]`) for pool-picked identities while preserving legacy behavior for manually-typed names.
Output: Extended NewSessionDialog + new test file covering task-input behavior + poolPicked tracking. **EXTEND, DO NOT REWRITE** (RESEARCH §10 landmine — 1400+ line file with multiple existing test files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-CONTEXT.md
@.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-RESEARCH.md
@.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-PATTERNS.md
@.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-03b-PLAN.md
@src/ui/sidebar/NewSessionDialog.tsx
@src/ui/sidebar/NewSessionDialog.test.tsx
@src/ui/sidebar/NewSessionDialog.chain.test.tsx
@src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx
@src/ui/api/identities-api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add task-input state + textarea + wire task + poolPicked into birth request body</name>
  <files>src/ui/sidebar/NewSessionDialog.tsx</files>
  <read_first>
    - src/ui/sidebar/NewSessionDialog.tsx (whole file — identify: task state placement alongside existing name/title/brief state; textarea placement inside the identity-cluster JSX above the Name input; reset-state block on close L440-459; openBirthStream call site to add task + poolPicked to request body)
    - src/ui/sidebar/NewSessionDialog.tsx:397-410 (chain-hook on-open effect — DO NOT modify; only add task-reset + pool-tracking-reset in the close branch at L440-459)
    - src/ui/sidebar/NewSessionDialog.tsx:961-1010 (role dropdown — gated on selectedHost; keep this gate for the role field, but Task 3 addresses the host-null affordance)
    - .planning/phases/80-*/80-PATTERNS.md §"src/ui/sidebar/NewSessionDialog.tsx — unified modal rebuild" §Phase 80 additions items 1, 2, 5
    - .planning/phases/80-*/80-03b-PLAN.md § Task 2 behavior (poolPicked wire contract — sent true when name still matches pool-picked value; sent as undefined/omitted otherwise)
  </read_first>
  <behavior>
    - New React state: `const [task, setTask] = useState<string>("");` positioned alongside existing name/title/brief state declarations.
    - New React state for pool-pick tracking: `const [poolPickedName, setPoolPickedName] = useState<string | null>(null);` — stores the last pool-picked value so the birth-submit can compare against the current name state to decide whether to send `poolPicked: true`.
    - New JSX block (gated on `identityMode`): a `<textarea>` with `id="new-identity-task"`, `aria-label="Task"`, `maxLength={200}`, `rows={2}`, placeholder "Describe the task in 15-20 words…", disabled when `formDisabled`. Uses same CSS classes as the existing name/title inputs (`rounded-sm border border-[color:var(--color-pv-border-quiet)] bg-[color:var(--color-pv-surface-quiet)] px-3 py-2 text-xs text-[color:var(--color-pv-fg)] outline-none disabled:opacity-50 resize-none`). Label above the textarea: "What will this agent work on?" using the existing `text-[10px] font-bold uppercase tracking-widest text-[color:var(--color-pv-fg-muted)]` label class. Positioned ABOVE the Name input within the identity cluster.
    - On dialog close (existing reset-state block at L440-459), add `setTask("")` and `setPoolPickedName(null)` alongside the other resets.
    - `openBirthStream` (or whatever function assembles the POST /identities/birth request body) is extended to include:
      - `task: task.trim() || undefined` (existing task field).
      - `poolPicked: (poolPickedName !== null && name.trim() === poolPickedName) ? true : undefined` — only set true when the name state still exactly matches the last pool-picked value (user didn't edit); otherwise omit the field so backend treats as legacy shape.
    - When user leaves the task field empty, `undefined` is sent for task; when user has edited the name after pool-pick (or never had a pool-pick prefill), `undefined` is sent for poolPicked. Backend's parsedTask + parsedPoolPicked logic (plans 80-03 + 80-03b) handles undefined correctly.
  </behavior>
  <action>
    Open `src/ui/sidebar/NewSessionDialog.tsx`. Add the new task state alongside existing state declarations (search for `const [name, setName] = useState` or `const [brief, setBrief] = useState` to locate the state cluster; add task-state right after; add `poolPickedName` state alongside task). Insert the task-textarea JSX block inside the identity-mode cluster ABOVE the Name input; use exactly the CSS classes enumerated in `<behavior>`; wire `value={task} onChange={(e) => setTask(e.target.value)}`. In the reset-state block (search for the else branch of `useEffect(() => { if (open) { ... } else { /* reset */ } }, [...])` around L440-459), add `setTask("");` and `setPoolPickedName(null);` alongside the other setters. Locate the `openBirthStream` call (or the request-body assembly for POST /identities/birth) and add TWO fields to the request body object:
    - `task: task.trim() || undefined,`
    - `poolPicked: (poolPickedName !== null && name.trim() === poolPickedName) ? true : undefined,`

    Do NOT touch the chain-hook on-open effect at L397-410 (Task 3 is the only place that changes host defaulting behavior). Do NOT alter any other existing state / effect / render logic. Commit prefix: `feat(80-06)`.
  </action>
  <verify>
    <automated>npx vitest run src/ui/sidebar/NewSessionDialog.test.tsx src/ui/sidebar/NewSessionDialog.chain.test.tsx src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx --reporter=verbose 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'const \\[task, setTask\\] = useState' src/ui/sidebar/NewSessionDialog.tsx` prints `1`.
    - `grep -c 'const \\[poolPickedName, setPoolPickedName\\] = useState' src/ui/sidebar/NewSessionDialog.tsx` prints `1`.
    - `grep -c 'id="new-identity-task"' src/ui/sidebar/NewSessionDialog.tsx` prints `1`.
    - `grep -c 'maxLength={200}' src/ui/sidebar/NewSessionDialog.tsx` prints at least `1` (new textarea).
    - `grep -c 'What will this agent work on?' src/ui/sidebar/NewSessionDialog.tsx` prints `1`.
    - `grep -c 'setTask("")' src/ui/sidebar/NewSessionDialog.tsx` prints at least `1` (reset).
    - `grep -c 'setPoolPickedName(null)' src/ui/sidebar/NewSessionDialog.tsx` prints at least `1` (reset).
    - `grep -c 'task: task.trim()' src/ui/sidebar/NewSessionDialog.tsx` prints `1` (birth request body).
    - `grep -c 'poolPicked:' src/ui/sidebar/NewSessionDialog.tsx` prints at least `1` (birth request body).
    - `grep -c 'name.trim() === poolPickedName' src/ui/sidebar/NewSessionDialog.tsx` prints at least `1` (edit-detection guard).
    - All three existing test files still pass (NewSessionDialog.test.tsx, chain.test.tsx, role-dropdown.test.tsx) — no regressions.
    - Chain-hook untouched: `grep -c 'initialHost\\|initialRole\\|initialBrief' src/ui/sidebar/NewSessionDialog.tsx` unchanged from baseline (grep count before and after edit).
  </acceptance_criteria>
  <done>Task state + poolPicked tracking + textarea + reset + request-body wiring shipped; no existing tests break.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auto-prefill name via pickPoolName on role change (only when user hasn't typed) + record pool-pick tracking</name>
  <files>src/ui/sidebar/NewSessionDialog.tsx</files>
  <read_first>
    - src/ui/sidebar/NewSessionDialog.tsx (locate existing useEffects — new effect keyed on `[selectedRole, selectedHost, identityMode]`; import `pickPoolName` from identities-api)
    - src/ui/api/identities-api.ts (pickPoolName signature — delivered by plan 80-05)
    - .planning/phases/80-*/80-PATTERNS.md §"src/ui/sidebar/NewSessionDialog.tsx — unified modal rebuild" item 3
    - .planning/phases/80-*/80-03b-PLAN.md § Task 2 behavior (poolPicked signal semantics)
  </read_first>
  <behavior>
    - New `useEffect` triggered on `[selectedRole, selectedHost, identityMode]` changes.
    - Effect early-returns when `!identityMode || !selectedRole || !selectedHost`.
    - Effect parses `selectedHost.id` as integer; early-returns if not finite.
    - Effect calls `pickPoolName(selectedRole, hostIdNum)` asynchronously.
    - On success: sets name ONLY IF the current name state is empty string (`name === ""`) — i.e., user hasn't typed a custom name yet. This preserves user intent per shape §Frontend creation flow ("user can override the name field"). Simultaneously calls `setPoolPickedName(poolName)` to record the pool-picked value for later edit-detection at submit time (Task 1's poolPicked flag logic).
    - On failure: silent (no toast, no error surfaced). User can type a name manually — pool is a suggestion source, not a hard requirement. `poolPickedName` stays null (or its previous value) so the submit path correctly omits `poolPicked` from the request body.
    - Uses `cancelled` flag pattern to guard against stale responses if role/host changes mid-flight.
    - Imports `pickPoolName` from the identities-api module (delivered by plan 80-05).
  </behavior>
  <action>
    Add `pickPoolName` to the existing `import { ... } from "@/api/identities-api"` line (or `src/ui/api/identities-api`, match the file's existing import path style). Add a new useEffect below the existing chain-hook effect. Effect dependency array: `[selectedRole, selectedHost, identityMode]`. Effect body semantics:
    - Early return if `!identityMode || !selectedRole || !selectedHost`.
    - Parse `hostIdNum = parseInt(String(selectedHost.id), 10)`; early return if not finite.
    - Declare `let cancelled = false;`.
    - Async IIFE: try/catch around `const { name: poolName } = await pickPoolName(selectedRole, hostIdNum);` — on success (and `!cancelled` AND `name === ""`), call BOTH `setName(poolName)` AND `setPoolPickedName(poolName)`. Catch is silent.
    - Cleanup: `return () => { cancelled = true; };`.

    Render the effect using the file's existing effect-style — do NOT paste code blocks, describe intent + reference the PATTERNS.md snippet.

    Do NOT modify the existing name-input's onChange (user-typed name still overwrites; the submit-time comparison `name === poolPickedName` handles edit-detection without needing to reset poolPickedName on every keystroke). Do NOT add a "Fetch pool name" button (auto-prefill is the whole point). Do NOT throw on pickPoolName failure. Commit prefix: `feat(80-06)`.
  </action>
  <verify>
    <automated>npx vitest run src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx --reporter=verbose 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pickPoolName' src/ui/sidebar/NewSessionDialog.tsx` prints at least `2` (import + call).
    - `grep -c '\\[selectedRole, selectedHost, identityMode\\]' src/ui/sidebar/NewSessionDialog.tsx` prints exactly `1` (new useEffect dep array).
    - The prefill guard `name === ""` present: `grep -c 'name === ""' src/ui/sidebar/NewSessionDialog.tsx` prints at least `1`.
    - `setPoolPickedName(poolName)` called on prefill success: `grep -c 'setPoolPickedName(poolName)' src/ui/sidebar/NewSessionDialog.tsx` prints at least `1`.
    - Silent failure: no new `toast\\|alert\\|throw` calls introduced in the new effect (`grep -B2 -A10 'pickPoolName(' src/ui/sidebar/NewSessionDialog.tsx | grep -c 'toast\\|throw' ` prints `0`).
    - Cancelled-flag pattern present: `grep -c 'cancelled = true' src/ui/sidebar/NewSessionDialog.tsx` prints at least `1`.
    - Existing role-dropdown tests still pass.
  </acceptance_criteria>
  <done>Auto-prefill on role change wired; user-typed names preserved; poolPickedName state tracks the last prefill for submit-time edit-detection; silent on failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix role-select bug + add task-input test file (includes poolPicked wire coverage)</name>
  <files>src/ui/sidebar/NewSessionDialog.tsx, src/ui/sidebar/NewSessionDialog.task-input.test.tsx</files>
  <read_first>
    - src/ui/sidebar/NewSessionDialog.tsx:397-410 (existing chain-hook on-open + `flatHosts.length === 1` auto-pick)
    - src/ui/sidebar/NewSessionDialog.tsx:961-1010 (role dropdown gated on `selectedHost !== null` — L961)
    - src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx (existing role-dropdown test — extend to cover pool-prefill and host-null affordance)
    - .planning/phases/80-*/80-RESEARCH.md §Landmine 2 (role-select-bug root cause) and §Assumption A4 (recommended fix)
    - .planning/phases/80-*/80-PATTERNS.md §Phase 80 additions item 4
  </read_first>
  <behavior>
    Role-select-bug fix (A4 recommendation): pick ONE of these two approaches (executor's discretion; document the choice in the plan summary):
    - **Approach A (recommended):** Add a visible hint OUTSIDE the `{selectedHost !== null && (...)}` role-cluster wrap when `identityMode` is on and host is null: a small inline text hint like "Pick a host to see available roles" using the existing muted-label CSS style. The role dropdown remains gated on host (existing behavior); the user gets a clear affordance instead of a blank space.
    - **Approach B (alternative):** Widen the host auto-pick condition — if `identityMode` is on and `selectedHost === null` after the existing `flatHosts.length === 1` auto-pick and the chain-hook on-open effect, default to the local Skynet host (if identifiable in `flatHosts`). Only choose this if a "local host" is deterministically identifiable in the existing host list — do NOT invent host-identification logic; if unclear, fall back to Approach A.

    New test file `NewSessionDialog.task-input.test.tsx`:
    - Test: task textarea present when `identityMode=true`, absent when `identityMode=false`.
    - Test: `maxLength` attribute is 200.
    - Test: user typing into task textarea + submitting → the POST /identities/birth mock is called with `task: <typed value>` in the request body.
    - Test: user leaving task empty + submitting → the POST body omits `task` (or sends `undefined`).
    - Test: when a role is selected AND host is set, `pickPoolName` mock is called with `(role, hostIdNum)` and the returned name populates the name field.
    - Test: if user has already typed a name, changing the role does NOT overwrite (name stays at user-typed value).
    - Test: **poolPicked=true wire signal** — user selects role, pickPoolName returns "willow", name field becomes "willow" (unedited), user submits → POST body contains `poolPicked: true`.
    - Test: **poolPicked-omitted after edit** — user selects role, pickPoolName returns "willow", user edits name field to "willow-my-custom", user submits → POST body does NOT contain `poolPicked` (or sends undefined).
    - Test: **poolPicked-omitted when no pool-pick occurred** — user manually types a name (no role change or pickPoolName failed), user submits → POST body does NOT contain `poolPicked`.
    - Test: host-null affordance visible when identityMode is on and host is null (either the "pick a host" hint per Approach A, OR the sole host is auto-picked per Approach B — assert whichever approach was implemented).
  </behavior>
  <action>
    Implement the chosen approach (A or B) with minimal file surface. For Approach A: add a small conditional JSX block near the role dropdown wrap, rendering the hint when `identityMode && selectedHost === null`. For Approach B: extend the on-open effect at L397-410 to add the local-host auto-pick fallback.

    Create `src/ui/sidebar/NewSessionDialog.task-input.test.tsx` following the same test-setup pattern as the existing `NewSessionDialog.role-dropdown.test.tsx` (same imports, same render helpers, same mock scaffolding). Mock `pickPoolName` and the birth-request submission. Enumerate the 10 test cases above. Commit prefix: `feat(80-06)` for source, `test(80-06)` for tests.
  </action>
  <verify>
    <automated>npx vitest run src/ui/sidebar/NewSessionDialog.task-input.test.tsx src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx src/ui/sidebar/NewSessionDialog.test.tsx src/ui/sidebar/NewSessionDialog.chain.test.tsx --reporter=verbose 2>&amp;1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/sidebar/NewSessionDialog.task-input.test.tsx` exists with >= 10 test cases (`grep -cE "^\\s*(it|test)\\(" src/ui/sidebar/NewSessionDialog.task-input.test.tsx` >= 10).
    - Test file explicitly asserts `poolPicked: true` in a birth request body: `grep -c 'poolPicked' src/ui/sidebar/NewSessionDialog.task-input.test.tsx` >= 3 (at least three assertions across the three poolPicked-related cases).
    - Chosen role-select fix present: either a "pick a host" hint literal appears in NewSessionDialog.tsx (Approach A: `grep -c 'Pick a host\\|pick a host' src/ui/sidebar/NewSessionDialog.tsx` >= 1) OR a widened auto-pick block appears in the on-open effect (Approach B — executor documents in SUMMARY).
    - All existing NewSessionDialog test files still pass (no regressions).
    - New test file passes.
    - No visual regression in the modal shell — no removed/moved existing JSX blocks (executor confirms via manual code review; not gate-testable at automated level).
  </acceptance_criteria>
  <done>Role-select bug addressed; new test file covers all task-input behaviors + poolPicked wire signal; existing tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser input → POST /identities/birth | Task string is user-controlled; validation on backend (plan 80-03) |
| Browser → POST /identities/pool/pick | Existing JWT cookie |
| Browser poolPicked flag → backend MXID branch | Wire signal for A1 DIVERGE; backend gates on shape check so a lying client can't force malformed MXID (plan 80-03b Task 2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-06-01 | Denial of Service | User types >200 chars into task textarea | mitigate | `maxLength={200}` HTML attribute enforces client-side. Backend also caps at 500 chars (plan 80-03 defense-in-depth). |
| T-80-06-02 | Tampering | User submits task with malicious YAML/JS payload | mitigate | Backend's `yaml.dump` auto-quoting handles YAML metacharacters (T-80-03-01); React auto-escapes on render (plans 80-07/08). Nothing this file needs to do additionally. |
| T-80-06-03 | Info Disclosure | pickPoolName error surface leaks server state | mitigate | Errors silent (per behavior spec); user simply types name manually. No error toast or leaked message. |
| T-80-06-04 | Tampering | Attacker races pickPoolName responses to stale-role state | mitigate | `cancelled` flag pattern in useEffect cleanup ensures late-arriving responses do not overwrite state after role change. |
| T-80-06-05 | Tampering | Client sends poolPicked=true with a mutated (non-pool-shape) name to force MXID DIVERGE | accept | Backend gates on composeMxidLocalpart shape check (plan 80-03b Task 2); mismatched name silently falls back to legacy MXID. Frontend already tracks edit-detection via `name === poolPickedName` comparison; a malicious client bypassing this check gains nothing (backend re-validates). |
</threat_model>

<verification>
- All 4 test files (existing 3 + new 1) pass.
- Modal render on load with identityMode=true shows task textarea + role dropdown (or "pick a host" hint) — verifiable via test.
- Submitting the form with task filled + name pool-prefilled → POST body assembled correctly with `poolPicked: true` (verified in task-input.test.tsx).
- Submitting after user edits the name → POST body omits `poolPicked` (verified in task-input.test.tsx).
</verification>

<success_criteria>
- Task textarea shipped, gated on identityMode, capped at 200 chars.
- Pool prefill useEffect wired to pickPoolName; respects user-typed name; records pool-picked value in `poolPickedName` state for edit-detection.
- Birth request body carries `task` when non-empty AND `poolPicked: true` iff name state still matches the last pool-picked value.
- Role-select bug addressed (Approach A or B documented in summary).
- All existing tests continue passing; new test file covers 10+ behaviors including poolPicked wire coverage.
- No file rewrite — extension only (verifiable via `git diff --stat src/ui/sidebar/NewSessionDialog.tsx` — line change count is a small fraction of file size).
</success_criteria>

<output>
Create `.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-06-SUMMARY.md` when done, including which role-select-bug approach (A or B) was chosen and why.
</output>
