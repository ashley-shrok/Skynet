---
phase: 80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool
plan: 09
type: execute
wave: 4
depends_on:
  - 80-06
  - 80-08
files_modified:
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/features/pretty-conversations/PrettyConversationRow.tsx
  - src/ui/sidebar/CloneAgentDialog.tsx
  - src/ui/sidebar/CloneAgentDialog.test.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.clone-dialog.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Clicking the row context-menu 'Spawn under this role' entry (renamed from 'Clone') opens the unified NewSessionDialog pre-set to that row's host + role (via existing `initialHost` + `initialRole` chain-hook props from plan 80-06's modal). Shape file §Frontend creation flow: clone becomes a thin entry-point into unified new-agent modal pre-set to row's role."
    - "The old CloneAgentDialog component + its dedicated modal state are DELETED (or reduced to a thin shim that opens NewSessionDialog). Recommended: delete entirely per RESEARCH §11."
    - "The task field is NOT prefilled from the source identity — clone is spawn-under-role with a fresh task per A3 lock (each new spawn describes its own why)."
    - "Context-menu label renamed from 'Clone' to 'Spawn under this role' (shape §Frontend creation flow: 'context-menu Clone gets renamed to reflect it simply spawns another agent under the same role')."
    - "Existing PrettyConversationsPanel + PrettyConversationRow behavior for non-clone paths is unchanged (pencil-icon → NewSessionDialog cold-open path stays intact)."
  artifacts:
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "handleRowClone rewired to seed NewSessionDialog with initialHost + initialRole; CloneAgentDialog mount removed"
      contains: "initialRole"
    - path: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx"
      provides: "context-menu label renamed from 'Clone' to 'Spawn under this role' at L1352"
      contains: "Spawn under this role"
    - path: "src/ui/sidebar/CloneAgentDialog.tsx"
      provides: "DELETED (or reduced to shim — executor's discretion; deletion recommended)"
  key_links:
    - from: "handleRowClone in PrettyConversationsPanel"
      to: "NewSessionDialog (via seeding initialHost + initialRole + opening dialog)"
      via: "state setter reusing existing chain-hook props"
      pattern: "initialRole"
    - from: "PrettyConversationRow context-menu label"
      to: "'Spawn under this role' string"
      via: "static JSX label"
      pattern: "Spawn under this role"
---

<objective>
Repurpose the clone entry-point: delete `CloneAgentDialog` (or reduce it to a shim) and rewire `handleRowClone` in `PrettyConversationsPanel` to open the unified NewSessionDialog (delivered by plan 80-06) with `initialHost` + `initialRole` seeded from the row. Rename the context-menu label from "Clone" to "Spawn under this role" (shape §Frontend creation flow, RESEARCH §Stream 6). Task is NOT inherited from source identity (A3).

Purpose: One creation flow, one modal. The "Clone" concept becomes "spawn another agent under this same role" — the cosmetics come from the role, so nothing is being carried from the source. Simplifies mental model and code.
Output: 3 modified files + 2 test files updated (or deleted+recreated as needed). Runs in Wave 4 because it touches PrettyConversationRow.tsx (owned by plan 80-08) and depends on NewSessionDialog chain-hook props being intact (plan 80-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-CONTEXT.md
@.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-RESEARCH.md
@.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-PATTERNS.md
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
@src/ui/features/pretty-conversations/PrettyConversationRow.tsx
@src/ui/sidebar/CloneAgentDialog.tsx
@src/ui/sidebar/NewSessionDialog.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire handleRowClone to open NewSessionDialog + remove CloneAgentDialog mount</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx</files>
  <read_first>
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx:119 (existing NewSessionDialog import — reuse)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx:125-128 (existing CloneAgentDialog import + comment — remove)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx:540-570 (state: newSessionDialogOpen at L544, initialHost/initialRole seed state at L546-551 for chain-hook, cloneDialogState at L559-567 — remove clone state; identify how the chain-hook seed state is currently set for the pencil-icon path so the clone path uses the same setter)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx:1240-1263 (handleRowClone L1248-1263 — rewrite to use NewSessionDialog chain-hook path)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (search for `<CloneAgentDialog` to find the mount site and remove it)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (search for `<NewSessionDialog` to confirm the mount and its prop interface — pass initialHost + initialRole through)
    - .planning/phases/80-*/80-PATTERNS.md §"src/ui/sidebar/CloneAgentDialog.tsx — repurpose or delete" (rewired handleRowClone example)
    - .planning/phases/80-*/80-RESEARCH.md §Landmine 11 (existing initialRole chain-prefill mechanism reused)
  </read_first>
  <behavior>
    - `handleRowClone(row)` computes `matchKey → identity → validate identity.role + row.host → parse hostId`. Same guards as current implementation.
    - On success: sets the NewSessionDialog chain-hook state (the same state currently used by pencil-icon-opens-NewSessionDialog-with-prefill path — variable name typically `newSessionSeed` or similar; executor identifies from L546-551 comment context) to `{ initialHost: row.host, initialRole: identity.role }`. Task is NOT prefilled (A3).
    - Opens NewSessionDialog (sets `newSessionDialogOpen` to true, or triggers whatever open mechanism the pencil-icon path uses).
    - Removes: `cloneDialogState` state variable + its setter (L567 region), `<CloneAgentDialog>` JSX mount (grep for it), CloneAgentDialog import at L128.
    - Removes the L125-128 comment referring to CloneAgentDialog, replacing with a Phase 80 comment: "Phase 80: clone flow repurposed into unified NewSessionDialog pre-seeded with initialHost + initialRole via chain-hook mechanism. See handleRowClone."
    - Does NOT touch the pencil-icon path.
  </behavior>
  <action>
    Open `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`. Remove the `CloneAgentDialog` import at L128. Update the L125-128 preamble comment to Phase 80 phrasing (see `<behavior>`). Remove the `cloneDialogState` state declaration (L559-567 region) and its setter — but FIRST confirm nothing else consumes `cloneDialogState` besides `handleRowClone` and the `<CloneAgentDialog>` mount (grep the file). Rewrite `handleRowClone` (L1248-1263) to seed the NewSessionDialog chain-hook state (identify the correct state setter from L546-551 comment context — likely `setNewSessionInitial({ initialHost, initialRole })` or setting individual state variables `setInitialHost` / `setInitialRole`; match the existing pencil-icon-path signature) with `{ initialHost: row.host, initialRole: identity.role }`, then open the dialog (`setNewSessionDialogOpen(true)` or whatever the existing setter is). Guard clauses (matchKey → identity → identity.role → row.host → hostIdNum) stay the same. Do NOT prefill task. Delete the `<CloneAgentDialog>` JSX mount block from the render tree.

    Preserve all other panel behavior. Commit prefix: `feat(80-09)`.
  </action>
  <verify>
    <automated>grep -c 'CloneAgentDialog' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'CloneAgentDialog' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` prints `0` (import + mount + state fully removed).
    - `grep -c 'handleRowClone' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` prints at least `2` (declaration + call sites still present — clone entry-point still wired to context menu).
    - `grep -c 'initialRole' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` prints at least `1` (handleRowClone seeds initialRole).
    - `grep -c 'cloneDialogState' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` prints `0`.
    - `grep -c 'Phase 80' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` prints at least `1` (replacement comment).
    - `npx tsc --noEmit` no new errors in this file.
    - Existing pencil-icon path (line references to `newSessionDialogOpen` / `newSessionDialogState` setter for the pencil path) still present unchanged.
  </acceptance_criteria>
  <done>handleRowClone opens NewSessionDialog with initialHost+initialRole seeded; CloneAgentDialog mount + state + import removed; pencil path unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rename context-menu label from 'Clone' to 'Spawn under this role'</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationRow.tsx</files>
  <read_first>
    - src/ui/features/pretty-conversations/PrettyConversationRow.tsx:1350-1355 (context-menu items push with `label: "Clone"` at L1352)
    - Any adjacent comment referencing the "Clone" naming (grep for `Clone` in the file — comments referring to the concept may need updating too)
    - .planning/phases/80-*/80-PATTERNS.md §"PrettyConversationRow.tsx L1352" rename
  </read_first>
  <behavior>
    - Line 1352 `label: "Clone"` → `label: "Spawn under this role"`.
    - `onClick: onClone` unchanged (still the same handler — plan 80-09 task 1 rewired what onClone does upstream in the panel).
    - Adjacent comments referring to the "Clone" concept updated to match the new naming (executor identifies via grep for `Clone` and updates any misleading comments to say "spawn under role" or similar).
    - The `onClone?: () => void` prop name at L241 is kept as `onClone` for now (renaming the prop everywhere is out-of-scope creep — the internal name stays; only the user-facing label changes). Optional: executor may add a comment above the prop declaration noting the Phase 80 semantic shift.
  </behavior>
  <action>
    Open `src/ui/features/pretty-conversations/PrettyConversationRow.tsx`. Change `label: "Clone"` at L1352 to `label: "Spawn under this role"`. Update any nearby comment blocks that reference "Clone" as the user-facing name — replace with "Spawn under this role" phrasing (do NOT change the prop name `onClone` or the internal handler names; those are internal). Do NOT modify any of the other context-menu items or the `.pv-body` markup from plan 80-08. Commit prefix: `feat(80-09)`.
  </action>
  <verify>
    <automated>grep -c 'label: "Clone"' src/ui/features/pretty-conversations/PrettyConversationRow.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'label: "Clone"' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` prints `0`.
    - `grep -c 'label: "Spawn under this role"' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` prints `1`.
    - `grep -c 'onClone' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` unchanged from baseline (prop name unchanged).
    - Task-primary body swap from plan 80-08 still present: `grep -c 'identity?.task ?' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` still >= 1 (no regression from plan 80-08).
    - `npx tsc --noEmit` no new errors.
  </acceptance_criteria>
  <done>Context-menu label renamed; onClone prop / handler wiring preserved; no collision with plan 80-08 edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Delete CloneAgentDialog + update / delete its tests</name>
  <files>src/ui/sidebar/CloneAgentDialog.tsx, src/ui/sidebar/CloneAgentDialog.test.tsx, src/ui/features/pretty-conversations/PrettyConversationsPanel.clone-dialog.test.tsx</files>
  <read_first>
    - src/ui/sidebar/CloneAgentDialog.tsx (whole file — confirming no exports beyond the dialog itself are consumed elsewhere)
    - src/ui/sidebar/CloneAgentDialog.test.tsx (whole file — tests are for the dialog itself; delete alongside dialog)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.clone-dialog.test.tsx (rewrite to assert new flow: handleRowClone opens NewSessionDialog with initialHost + initialRole seeded)
    - `grep -rn "CloneAgentDialog\\|from.*CloneAgentDialog" src/ui/` — verify NO other file imports CloneAgentDialog (if any exist, executor rewires them the same way Task 1 rewired PrettyConversationsPanel; otherwise delete is safe)
  </read_first>
  <behavior>
    - `grep -rn "CloneAgentDialog" src/` returns 0 matches after this task completes (except optionally in git-history / archived files).
    - `src/ui/sidebar/CloneAgentDialog.tsx` is deleted (removed from repo via `git rm`).
    - `src/ui/sidebar/CloneAgentDialog.test.tsx` is deleted (removed from repo — the dialog no longer exists).
    - `src/ui/features/pretty-conversations/PrettyConversationsPanel.clone-dialog.test.tsx` is rewritten to assert:
      - Test 1: Clicking the row context-menu "Spawn under this role" entry calls the handler that sets NewSessionDialog state with `initialHost` and `initialRole` matching the row.
      - Test 2: NewSessionDialog opens after clone entry-point invocation (via mock spy on the dialog's `open` prop change or via presence check).
      - Test 3: The task field is NOT prefilled — the NewSessionDialog receives no `initialTask` / no `initialBrief` from the clone path (A3 lock).
    - Optional: if the file's original test title was `PrettyConversationsPanel.clone-dialog.test.tsx`, executor may rename to `PrettyConversationsPanel.spawn-under-role.test.tsx`. Not required.
  </behavior>
  <action>
    Delete `src/ui/sidebar/CloneAgentDialog.tsx` via `git rm src/ui/sidebar/CloneAgentDialog.tsx`. Delete `src/ui/sidebar/CloneAgentDialog.test.tsx` via `git rm`. Rewrite `src/ui/features/pretty-conversations/PrettyConversationsPanel.clone-dialog.test.tsx` to assert the new flow enumerated in `<behavior>`. Use the same test-scaffolding pattern as the existing file (mock NewSessionDialog to spy on its `open` and `initialHost` / `initialRole` props; simulate a row context-menu click). Commit prefix: `feat(80-09)` for deletions and `test(80-09)` for the rewritten test.

    Before deleting, execute `grep -rn "CloneAgentDialog" src/ui/` to CONFIRM no other consumers exist. If any surface (unlikely), rewire them first the same way Task 1 rewired PrettyConversationsPanel.
  </action>
  <verify>
    <automated>test ! -f src/ui/sidebar/CloneAgentDialog.tsx &amp;&amp; test ! -f src/ui/sidebar/CloneAgentDialog.test.tsx &amp;&amp; npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.clone-dialog.test.tsx --reporter=verbose 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/ui/sidebar/CloneAgentDialog.tsx` exits 0 (file gone).
    - `test ! -f src/ui/sidebar/CloneAgentDialog.test.tsx` exits 0 (test file gone).
    - `grep -rn "CloneAgentDialog" src/` returns 0 matches (no dangling references).
    - `PrettyConversationsPanel.clone-dialog.test.tsx` has >= 3 test cases (`grep -cE "^\\s*(it|test)\\(" ...clone-dialog.test.tsx` >= 3).
    - `npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.clone-dialog.test.tsx` all cases pass.
    - `npx tsc --noEmit` returns 0 errors (no dangling imports).
  </acceptance_criteria>
  <done>CloneAgentDialog and its dedicated test deleted; the rewritten panel clone-dialog test asserts the new NewSessionDialog-based flow; no dangling references anywhere in src/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → context-menu → handleRowClone → NewSessionDialog | User action triggers state seed with row-derived initialHost + initialRole |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-09-01 | Elevation of Privilege | Row derived from stale/spoofed state seeds NewSessionDialog with wrong hostId → user creates identity on unintended host | mitigate | Existing NewSessionDialog validation (role dropdown re-fetches roles for the actually-selected host; user confirms before submit) mitigates. This plan does not weaken any existing gates. |
| T-80-09-02 | Tampering | Task inherited from source identity leaks stale task context into new spawn | mitigate | A3 lock: task NOT inherited. Test 3 asserts NewSessionDialog does not receive initialTask/initialBrief from clone path. |
| T-80-09-03 | Info Disclosure | Deleted CloneAgentDialog file references leak in bundle / source maps | accept | Standard git delete; no residual file. Vite/webpack bundling picks up the deletion automatically at next build. |
</threat_model>

<verification>
- All tests pass (rewritten clone-dialog test + all existing PrettyConversationsPanel + PrettyConversationRow tests).
- `grep -rn "CloneAgentDialog" src/` returns 0.
- Manual UI check (orchestrator ship-gate): right-click a conversation row → context menu shows "Spawn under this role" (not "Clone") → clicking it opens the unified new-agent modal with host + role pre-filled, name field pool-prefilling automatically when role settles, task field empty for user input.
</verification>

<success_criteria>
- Clone flow unified into NewSessionDialog via existing chain-hook props.
- CloneAgentDialog + its dedicated test file deleted.
- Context-menu label renamed.
- Task NOT inherited from source (A3 verified by test).
- Zero dangling references; TypeScript clean.
</success_criteria>

<output>
Create `.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-09-SUMMARY.md` when done.
</output>
