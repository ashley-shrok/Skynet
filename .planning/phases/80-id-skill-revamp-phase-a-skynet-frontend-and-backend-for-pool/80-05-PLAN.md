---
phase: 80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool
plan: 05
type: execute
wave: 2
depends_on:
  - 80-03
files_modified:
  - src/ui/api/identities-api.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Frontend `Identity` interface has `task: string | null` field so React components rendering the task pill (plan 80-07) and task-primary conversation row (plan 80-08) can read `identity.task` type-safely."
    - "Frontend `BirthRequest` interface has optional `task?: string` field so NewSessionDialog (plan 80-06) can include task in POST /identities/birth body."
    - "Frontend `BirthRequest` interface has optional `poolPicked?: boolean` field so NewSessionDialog can signal to the backend that the name field was pool-prefilled — this triggers the birth-orchestrator's MXID DIVERGE derivation (plan 80-03b A1 lock: PascalCase-hyphenated `<PoolName>-<Role>[-N]` MXID)."
    - "New `pickPoolName(role, hostId): Promise<{ name: string }>` API function hits POST /identities/pool/pick with proper JSON body; errors surface via handleApiError like all other identity-api calls."
  artifacts:
    - path: "src/ui/api/identities-api.ts"
      provides: "`Identity.task: string | null`, `BirthRequest.task?: string`, `BirthRequest.poolPicked?: boolean`, `pickPoolName` export"
      exports: ["pickPoolName"]
      contains: "task"
  key_links:
    - from: "pickPoolName"
      to: "POST /identities/pool/pick (delivered by plan 80-04)"
      via: "authApi.post"
      pattern: "/identities/pool/pick"
    - from: "Identity.task"
      to: "backend GET /identities response (delivered by plan 80-03)"
      via: "JSON deserialization"
      pattern: "task"
    - from: "BirthRequest.poolPicked"
      to: "backend POST /identities/birth body validation → birthIdentity opts.poolPicked (delivered by plan 80-03b)"
      via: "JSON serialization"
      pattern: "poolPicked"
---

<objective>
Extend the frontend `identities-api.ts` module to (a) add `task: string | null` to the `Identity` interface so components reading identity state have a typed task field, (b) add `task?: string` AND `poolPicked?: boolean` to the `BirthRequest` interface for POST /identities/birth, and (c) add the `pickPoolName(role, hostId)` API client function that hits POST /identities/pool/pick (delivered by plan 80-04).

The `poolPicked` field is the wire signal for the A1 DIVERGE lock (plan 80-03b): when the frontend pool-picks a name, it sets `poolPicked: true` in the birth request so the backend's orchestrator composes the PascalCase-hyphenated MXID (`Willow-Skynet-Maintainer[-N]`); when the field is absent/false, the legacy `@<name>:<server>` MXID shape is used (backward compat for pre-Phase-80 identities and manually-typed names).

Purpose: Unblock the three downstream frontend plans (80-06 NewSessionDialog, 80-07 task pill on chat, 80-08 task-primary conversation row) which all consume this type/API surface.
Output: Single file extended with 4 additive changes; no rewrites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-RESEARCH.md
@.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-PATTERNS.md
@.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-03b-PLAN.md
@src/ui/api/identities-api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add task + poolPicked fields to Identity/BirthRequest interfaces + pickPoolName API</name>
  <files>src/ui/api/identities-api.ts</files>
  <read_first>
    - src/ui/api/identities-api.ts:1-25 (Identity interface — add task after coordinator)
    - src/ui/api/identities-api.ts:370-390 (BirthRequest interface — add task after role, then poolPicked after task)
    - src/ui/api/identities-api.ts:245-260 (listRolesForHost — closest analog for the new pickPoolName function shape: params, authApi.post, handleApiError)
    - src/ui/api/identities-api.ts:88-105 (createIdentity — another reference for handleApiError pattern)
    - .planning/phases/80-*/80-PATTERNS.md §"src/ui/api/identities-api.ts — add task + pickPoolName"
    - .planning/phases/80-*/80-03b-PLAN.md § Task 2 behavior (poolPicked wire semantics — frontend sets true when name was pool-prefilled; backend uses it to select the MXID derivation branch)
  </read_first>
  <behavior>
    - `Identity` interface gains `task: string | null` field with JSDoc explaining Phase 80 semantics.
    - `BirthRequest` interface gains `task?: string` (optional, undefined = omit from request body).
    - `BirthRequest` interface gains `poolPicked?: boolean` (optional, undefined = omit → backend treats as false → legacy MXID shape). JSDoc must explain: "Phase 80 A1 lock: when true, backend composes MXID as `@<PoolName>-<Role>[-N]:<server>` PascalCase-hyphenated (plan 80-03b DIVERGE). When false/absent, backend uses legacy `@<name>:<server>` shape (backward compat for manually-typed names and pre-Phase-80 identities). NewSessionDialog (plan 80-06) sets true when the name field value came from `pickPoolName` prefill AND the user did not edit it further."
    - New exported async function `pickPoolName(role: string, hostId: number): Promise<{ name: string }>` calls `authApi.post("/identities/pool/pick", { role, hostId })` and returns `response.data as { name: string }`. On error, calls `handleApiError(error, "pick pool name")` (following the exact pattern of every other function in this file).
    - No breaking changes to any existing export or interface field.
  </behavior>
  <action>
    Open `src/ui/api/identities-api.ts`.

    (1) In the `Identity` interface (currently L3-23), add `task: string | null;` as the last field (after `coordinator`). Include a JSDoc line: "Phase 80: task string from identity file frontmatter. Null when absent (existing pre-Phase-80 identities and coord-spawned identities without task). Present-and-truthy gates the task-primary UI treatment on chat + list surfaces (D-06 fallback semantics)."

    (2) In the `BirthRequest` interface (currently around L374-384), add `task?: string;` as an optional field after `role`. JSDoc: "Phase 80: optional task string ('what will this agent work on?'). Frontend soft-caps ~200 chars; backend hard-caps 500 chars."

    (3) In the SAME `BirthRequest` interface, add `poolPicked?: boolean;` as an optional field after `task`. JSDoc from `<behavior>` above (A1 DIVERGE lock explanation).

    (4) Add a new exported async function near the other identity-api exports (e.g. after `listRolesForHost` or wherever fits the file's ordering — group with the other GET/POST identity-scope calls). Signature: `export async function pickPoolName(role: string, hostId: number): Promise<{ name: string }>`. Body: try/catch around `const response = await authApi.post("/identities/pool/pick", { role, hostId }); return response.data as { name: string };` — catch calls `handleApiError(error, "pick pool name");`. JSDoc: "Phase 80: fetch an unused pool name for the given role on the given host. Backend picks a bare pool name (lowercase) not currently in use as a Matrix account on the target server. User can edit thereafter (pool is a suggestion source, not a restriction). Errors surface via handleApiError."

    Do NOT change any other field, function, or interface in the file. Commit prefix: `feat(80-05)`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | grep -c "identities-api.ts" | grep -qE '^0$' &amp;&amp; grep -c "task: string | null" src/ui/api/identities-api.ts | grep -qE '^[1-9]' &amp;&amp; grep -c "pickPoolName" src/ui/api/identities-api.ts | grep -qE '^[1-9]' &amp;&amp; grep -c "poolPicked?: boolean" src/ui/api/identities-api.ts | grep -qE '^[1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "task: string | null" src/ui/api/identities-api.ts` prints exactly `1` (Identity field).
    - `grep -c "task?: string" src/ui/api/identities-api.ts` prints exactly `1` (BirthRequest field).
    - `grep -c "poolPicked?: boolean" src/ui/api/identities-api.ts` prints exactly `1` (BirthRequest field).
    - `grep -c "export async function pickPoolName" src/ui/api/identities-api.ts` prints exactly `1`.
    - `grep -c '"/identities/pool/pick"' src/ui/api/identities-api.ts` prints exactly `1`.
    - `grep -c '"pick pool name"' src/ui/api/identities-api.ts` prints exactly `1` (handleApiError context string).
    - `npx tsc --noEmit` completes without errors in `identities-api.ts` (i.e., `npx tsc --noEmit 2>&1 | grep -c "identities-api.ts"` prints `0`).
    - Existing exports unchanged: `grep -cE "^export (async function|interface)" src/ui/api/identities-api.ts` increases by exactly 1 (only pickPoolName added).
  </acceptance_criteria>
  <done>Identity + BirthRequest carry task and poolPicked; pickPoolName exported and typed; no TypeScript regressions in file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Skynet backend | pickPoolName call goes through existing authApi (JWT cookie); no new auth surface |
| Browser BirthRequest.poolPicked flag → backend orchestrator branch | A1 lock: attacker setting poolPicked=true for a manually-typed non-pool name is a no-op (backend's composeMxidLocalpart throws `mxid_name_not_pool_shape` and orchestrator falls back to legacy shape per plan 80-03b Task 2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-05-01 | Tampering | Response body parsed as `{ name: string }` — attacker-controlled backend could return unexpected shape | mitigate | Type-cast via `as { name: string }` is a compile-time assertion; runtime uses the value directly. Consumers (NewSessionDialog) already validate `name` before submission via existing name-input regex checks. |
| T-80-05-02 | Tampering | XSS via `identity.task` rendered on chat / list | mitigate | React auto-escapes text children — not this file's concern; plans 80-07/08 render as plain text child, not `dangerouslySetInnerHTML`. Documented at those plans' threat models. |
| T-80-05-03 | Tampering | Client claims poolPicked=true with a manually-typed name to trick backend | accept | Backend gates on composeMxidLocalpart shape check (plan 80-03b); mismatched name silently falls back to legacy MXID. No security impact — the claim is not a privilege boundary. |
</threat_model>

<verification>
- TypeScript compiles cleanly.
- Consumers of Identity type (plans 80-06/07/08) can read `identity.task` type-safely.
- `pickPoolName` callable from NewSessionDialog with role + hostId → returns `{ name: string }`.
- BirthRequest carries both task and poolPicked; NewSessionDialog can set them on submit.
</verification>

<success_criteria>
- Four additive changes committed to `identities-api.ts`: `Identity.task`, `BirthRequest.task`, `BirthRequest.poolPicked`, `pickPoolName`.
- Zero TypeScript regressions.
- No existing function or interface field modified.
</success_criteria>

<output>
Create `.planning/phases/80-id-skill-revamp-phase-a-skynet-frontend-and-backend-for-pool/80-05-SUMMARY.md` when done.
</output>
