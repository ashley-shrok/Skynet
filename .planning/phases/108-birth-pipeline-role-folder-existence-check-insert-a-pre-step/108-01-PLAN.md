---
phase: 108-birth-pipeline-role-folder-existence-check
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/database/routes/identity-birth-orchestrator.ts
  - src/backend/database/routes/identity-birth-orchestrator.test.ts
  - src/backend/spawn-requests/worker.ts
  - src/backend/spawn-requests/worker.test.ts
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12
  - D-13
  - D-14
  - D-15

must_haves:
  truths:
    - "Remote-branch birth: role folder MISSING on target host → orchestrator emits step:1:failed with reason matching /role not found/, then ended{ok:false, failedStep:1}, and NEVER runs Step 2 / Matrix mint / identity file write."
    - "Local-branch (self-birth) birth: role .md missing at getLocalRolesRoot()/<role>/<role>.md → same step:1:failed shape."
    - "Role folder probe runs FIRST inside runStep(1, ...), before the avatar candidate check AND before the identity-folder collision probe (D-11)."
    - "On role-folder miss, worker.ts's mapEndedEventToReason maps stepFailReason to FailureReason 'role_unknown' and writeResponseFile drops a .failure.json with body {\"reason\":\"role_unknown\"} (no message field, per D-09)."
    - "Stale comment at worker.ts:94 updated from '(Step 2.5 check in orchestrator)' to '(Step 1 check in orchestrator — Phase 108)' — forensic trail corrected (D-10)."
    - "File-level Step 1 docstring at identity-birth-orchestrator.ts:12 names the role-folder probe as the first substantive Step 1 check."
    - "Scoped vitest command (D-15) passes green."
  artifacts:
    - path: "src/backend/database/routes/identity-birth-orchestrator.ts"
      provides: "role-folder existence probe inside runStep(1, ...) — local branch via fs.access on getLocalRolesRoot() path, remote branch via exec() with test -f, throwing 'role not found on target host: <role>' on miss"
      contains: "role not found on target host"
    - path: "src/backend/database/routes/identity-birth-orchestrator.test.ts"
      provides: "5 new tests (A-E per D-13) covering remote miss, remote hit, local miss, local hit, and ordering (probe fires before avatar check)"
      contains: "role not found on target host"
    - path: "src/backend/spawn-requests/worker.ts"
      provides: "corrected comment at line ~94 pointing to Step 1 / Phase 108"
      contains: "Step 1 check in orchestrator — Phase 108"
    - path: "src/backend/spawn-requests/worker.test.ts"
      provides: "1 new processBirth-level test asserting a birthIdentity ended{ok:false, failedStep:1, reason:'role not found on target host: bogus'} produces .failure.json body {reason:'role_unknown'}"
      contains: "role_unknown"
  key_links:
    - from: "src/backend/database/routes/identity-birth-orchestrator.ts (runStep(1) role probe)"
      to: "src/backend/spawn-requests/worker.ts:95 regex /role.*not found|role.*does not exist|unknown role|invalid role/i"
      via: "throw new Error('role not found on target host: ' + opts.role) → step:failed.reason → mapEndedEventToReason"
      pattern: "role not found on target host"
    - from: "identity-birth-orchestrator.ts local branch of role probe"
      to: "getLocalRolesRoot() in src/backend/claude-session/identity-artifact-reader.ts:238"
      via: "import (getLocalRolesRoot is already exported at line 238 — reuse instead of path.join-fallback per D-04's planner check clause)"
      pattern: "getLocalRolesRoot"
---

<objective>
Land the missing orchestrator half of the role-folder existence check: insert a target-host probe as the FIRST substantive check inside runStep(1, ...) that verifies `~/fleet/roles/<role>/<role>.md` exists BEFORE any durable side effect (avatar cache mutation, identity folder mkdir, identity file write with role in frontmatter, Matrix admin-mint with role in MXID localpart, relay creds mint, relay.json SFTP write). On miss, throw a message that hits worker.ts:95's existing `/role.*not found/i` regex, which translates to a `FailureResponse{reason:"role_unknown"}` file — closing the gap identified by ivory's spawn-scan e2e (2026-09-12) where a bogus role birthed odin with a permanently-baked bogus-role MXID localpart.

Purpose: prevent bogus-role identity spawns from producing durable side effects. Fail fast, fail clean, expose failure through the same channel operators already read.

Output: probe inserted with local + remote branches; 5 orchestrator tests + 1 worker-level integration test proving behavior; stale worker.ts comment corrected; file-level Step 1 docstring updated to name the new probe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/108-birth-pipeline-role-folder-existence-check-insert-a-pre-step/108-CONTEXT.md
@src/backend/database/routes/identity-birth-orchestrator.ts
@src/backend/spawn-requests/worker.ts
@src/backend/spawn-requests/types.ts
@src/backend/claude-session/identity-artifact-reader.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Insert role-folder existence probe as the FIRST check inside runStep(1, ...) — local + remote branches</name>
  <files>src/backend/database/routes/identity-birth-orchestrator.ts</files>

  <read_first>
    - src/backend/database/routes/identity-birth-orchestrator.ts:1-52 (file-level docstring + imports — confirms getLocalIdentitiesRoot import at line 51; getLocalRolesRoot needs to be added alongside)
    - src/backend/database/routes/identity-birth-orchestrator.ts:1180-1290 (the exec() closure at 1181-1187, the runStep(1) body at 1232-1285 — model the new probe on the existing identity-collision probe at 1266-1284 byte-for-byte)
    - src/backend/claude-session/identity-artifact-reader.ts:226-260 (confirms getLocalRolesRoot() is already exported at line 238 and returns ~/fleet/roles — reuse this, do NOT add a new export; D-04's planner-check clause resolves to "use existing")
    - .planning/phases/108-birth-pipeline-role-folder-existence-check-insert-a-pre-step/108-CONTEXT.md sections `<decisions>` D-01, D-03, D-04, D-05, D-06, D-07, D-11, D-12 (probe placement, primitive, path, throw string, ordering)
  </read_first>

  <behavior>
    - Inside runStep(1, ...) at identity-birth-orchestrator.ts:1232, the FIRST substantive block (before the avatar-candidate lookup currently at line 1241) is the role-folder probe.
    - LOCAL branch (useLocal === true): try `await fs.access(path.join(getLocalRolesRoot(), opts.role, opts.role + ".md"))`. On rejection (any error — treat as missing), throw `new Error("role not found on target host: " + opts.role)`.
    - REMOTE branch (useLocal === false): invoke `exec('if [ -f "$HOME/fleet/roles/' + opts.role + '/' + opts.role + '.md" ]; then echo exists; else echo missing; fi')`. If the trimmed output is not exactly "exists", throw the same message.
    - Both branches interpolate opts.role directly — safe because ROLE_NAME_PATTERN validates it upstream (per D-05).
    - The throw propagates through runStep(1)'s existing catch → emits `{type:"step", n:1, phase:"failed", reason:"role not found on target host: <role>"}` then `{type:"ended", ok:false, failedStep:1, reason:"..."}`. No new emit calls needed — the runStep machinery handles it (per D-08).
  </behavior>

  <action>
    Per D-01 + D-03 + D-04 + D-05 + D-06 + D-07 + D-11 + D-12: modify `runStep(1, async () => { ... })` in identity-birth-orchestrator.ts (around line 1232) to insert a role-folder probe as the FIRST block, BEFORE the `if (opts.avatarCandidateId.length > 0)` avatar-candidate check.

    Add `getLocalRolesRoot` to the existing named import from `../../claude-session/identity-artifact-reader.js` at line 51-52 (it is already exported there at line 238 — do NOT add a new export, do NOT use the `path.join(getLocalIdentitiesRoot(), "..", "roles")` fallback D-04 mentions; D-04's parenthetical planner-check clause selects the existing helper).

    Local branch: `fs.access(path.join(getLocalRolesRoot(), opts.role, opts.role + ".md"))` inside a try/catch — on ANY catch, throw `new Error("role not found on target host: " + opts.role)`. Do NOT swallow-and-continue like the identity-collision probe's ENOENT branch; ANY error here means the role file cannot be confirmed, and a role file that cannot be read is functionally missing.

    Remote branch: reuse the `exec()` closure defined at line 1181-1187 (do NOT open a new SSH connection — D-03 + D-12; conn is guaranteed non-null because SSH connect completes at line 1205 before entering runStep(1)). Command shape per D-05: `if [ -f "$HOME/fleet/roles/${opts.role}/${opts.role}.md" ]; then echo exists; else echo missing; fi` (double-quoted path segment for defense-in-depth). If `probeOut.trim() !== "exists"`, throw the same Error message.

    The check must run BEFORE the `if (opts.avatarCandidateId.length > 0) { const cand = deps.getCandidateForBirth(...) }` block at line 1241-1250, and BEFORE the identity-folder collision probe at line 1266-1284 (D-11: role-probe → avatar candidate → identity collision).

    Also update the file-level docstring at lines 12-13 from:
    `Step 1: On-disk collision probe + avatar candidate check (Phase 68 rewire)`
    to something naming the role probe first, e.g.:
    `Step 1: Role-folder existence probe (Phase 108) + avatar candidate check + on-disk collision probe (Phase 68 rewire)`

    Do NOT introduce new constants, do NOT emit new SSE event types, do NOT add new deps.
  </action>

  <verify>
    <automated>grep -n "role not found on target host" src/backend/database/routes/identity-birth-orchestrator.ts | grep -v '^#' | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "role not found on target host" src/backend/database/routes/identity-birth-orchestrator.ts` returns AT LEAST 2 matches (one local branch throw, one remote branch throw). Both are inside the runStep(1, ...) body.
    - `grep -n "getLocalRolesRoot" src/backend/database/routes/identity-birth-orchestrator.ts` returns AT LEAST 2 matches (one at the import at line ~51, one at the local branch probe body).
    - `grep -n 'fleet/roles/' src/backend/database/routes/identity-birth-orchestrator.ts` returns AT LEAST 1 match (the remote-branch exec command string).
    - In the modified file, the role-folder probe (matched by `role not found on target host`) appears at a line number BEFORE the `avatarCandidateId.length > 0` line AND BEFORE the `identity already exists on this host` line. Verify via: `awk '/role not found on target host/{a=NR} /avatarCandidateId.length > 0/{b=NR} /identity already exists on this host/{c=NR} END{print a, b, c}' src/backend/database/routes/identity-birth-orchestrator.ts` — the first number must be less than the second AND less than the third.
    - The file-level docstring at lines 12-13 names the role-folder probe: `grep -n "Role-folder existence probe" src/backend/database/routes/identity-birth-orchestrator.ts` returns at least 1 match, on a line ≤ 50.
    - `npx tsc --noEmit` succeeds (no type errors introduced).
    - No new SSE event `type` values introduced: `grep -c "type: \"step\"" src/backend/database/routes/identity-birth-orchestrator.ts` and `grep -c "type: \"ended\"" src/backend/database/routes/identity-birth-orchestrator.ts` return counts unchanged vs. baseline (probe uses throw-through-runStep, per D-08).
  </acceptance_criteria>

  <done>The role-folder probe runs first inside runStep(1), local + remote branches present, throw string matches worker.ts:95 regex, docstring updated, TypeScript compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 5 orchestrator tests (D-13 A-E) covering remote miss, remote hit, local miss, local hit, and ordering</name>
  <files>src/backend/database/routes/identity-birth-orchestrator.test.ts</files>

  <read_first>
    - src/backend/database/routes/identity-birth-orchestrator.test.ts:85-105 (mock setup pattern — mockIsLocalHostId, mockExecCommand, mockExecLocal harness)
    - src/backend/database/routes/identity-birth-orchestrator.test.ts:549-585 (Test 4 — the existing "identity already exists on this host" collision test; mirror this shape byte-for-byte for Tests A + B)
    - src/backend/database/routes/identity-birth-orchestrator.test.ts:255-275 (makeOpts() shape — confirms `role: "box-maintainer"` default; new tests will override with `role: "bogus-role"` for miss cases)
    - src/backend/database/routes/identity-birth-orchestrator.test.ts:989-1010 (Test 16 — SHAPE B ordering pattern; useful for Test E's ordering assertion via call-count on deps)
    - .planning/phases/108-birth-pipeline-role-folder-existence-check-insert-a-pre-step/108-CONTEXT.md section `<decisions>` D-13 (verbatim spec for Tests A-E)
  </read_first>

  <behavior>
    - Test A (remote miss): `mockIsLocalHostId → false`, `mockExecCommand` returns "missing" when the command string contains `fleet/roles/` AND `.md`, returns "missing" (or unrelated) for the identity-folder probe path. Assert: events include `step:1:failed` with `reason` matching `/role not found/`, then `ended{ok:false, failedStep:1}`; assert `deps.writeMarkdownFileAtomic`, `deps.matrixCreateOrUpdateUser` (or whatever admin-mint dep name is), and any Step 2+ mkdir dep have been called ZERO times.
    - Test B (remote hit): `mockExecCommand` returns "exists" for role probe (command contains `fleet/roles/`) AND "missing" for identity collision probe (command contains `fleet/identities/`). Assert: no `step:1:failed` event; Step 1 completes and flow reaches Step 2 (assert some Step-2 dep, e.g. mkdir-invoking exec command, was called at least once).
    - Test C (local miss): `mockIsLocalHostId → true`; mock `fs.access` (or whatever module is used for local branch) to throw `ENOENT` when called with a path containing `roles/bogus-role/bogus-role.md`. Assert same failure shape as Test A.
    - Test D (local hit): `mockIsLocalHostId → true`; `fs.access` resolves for the role .md path AND rejects (missing) for the identity folder path. Assert Step 1 completes.
    - Test E (ordering): `mockIsLocalHostId → false`; `mockExecCommand` returns "missing" for the role probe; `opts.avatarCandidateId` set to a non-empty string. Assert `deps.getCandidateForBirth` was called ZERO times. This proves the role probe runs BEFORE the avatar-candidate lookup (D-11).
  </behavior>

  <action>
    Append 5 tests to identity-birth-orchestrator.test.ts, each in the style of Test 4 (line 552) — plain `it(...)` blocks at module scope, using the file's existing `makeDeps()`, `makeOpts()`, `collectEvents()`, and `mockExecCommand` / `mockExecLocal` harness. Name tests explicitly: `"Test 108-A: Phase 108 — role folder missing on target host (remote) → step:1:failed"`, etc. through `Test 108-E`.

    For Tests A, B, E (remote): differentiate role-probe vs identity-collision exec commands by substring match — role probe contains `fleet/roles/`, identity collision probe contains `fleet/identities/`. Do NOT match on brittle whitespace.

    For Tests C, D (local): use vitest's `vi.mock` on `node:fs/promises` (or the existing fs mock the file may already set up — check imports at top of file) to control `fs.access` per-path via a discriminating mockImplementation.

    Test A assertion for zero-side-effect: iterate the mocked deps object and assert `.mock.calls.length === 0` for every dep that represents a Step 2+ side effect. The concrete dep names come from the actual `makeDeps()` in this file — read that helper first and enumerate. At minimum assert zero calls on `deps.writeMarkdownFileAtomic`. If the file uses a `mockMatrixCreateOrUpdateUser` or similar, include it.

    Test E's assertion `deps.getCandidateForBirth.mock.calls.length === 0` proves ordering directly per D-11.

    All 5 tests use the throw message exactly `"role not found on target host: <role>"` — assert via `expect(reason).toMatch(/role not found on target host/)`.

    Do NOT introduce new test utilities; do NOT modify makeDeps/makeOpts helpers unless a new argument is genuinely required (prefer overriding via the spread pattern the file already uses).
  </action>

  <verify>
    <automated>npx vitest run src/backend/database/routes/identity-birth-orchestrator.test.ts -t "Test 108-"</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run src/backend/database/routes/identity-birth-orchestrator.test.ts -t "Test 108-"` reports EXACTLY 5 tests passing and 0 failures.
    - `grep -c "Test 108-" src/backend/database/routes/identity-birth-orchestrator.test.ts` returns 5.
    - `grep -c "role not found on target host" src/backend/database/routes/identity-birth-orchestrator.test.ts` returns at least 5 (one per test's assertion, possibly more).
    - `grep -c "fleet/roles/" src/backend/database/routes/identity-birth-orchestrator.test.ts` returns at least 2 (Tests A/B/E mockExecCommand branches).
    - Test E's body includes the string `getCandidateForBirth` AND the string `toHaveBeenCalledTimes(0)` or `.mock.calls.length` — verify: `grep -A 40 "Test 108-E" src/backend/database/routes/identity-birth-orchestrator.test.ts | grep -E "getCandidateForBirth|toHaveBeenCalledTimes\(0\)|\.mock\.calls\.length"` returns at least 2 lines.
    - Full scoped vitest command from D-15 passes: `npx vitest run src/backend/database/routes/identity-birth-orchestrator.test.ts src/backend/database/routes/identity-birth.test.ts src/backend/spawn-requests/worker.test.ts` — see final task for the composite gate.
  </acceptance_criteria>

  <done>All 5 orchestrator tests pass, cover remote-miss/remote-hit/local-miss/local-hit/ordering, exact throw string asserted, ordering test proves role probe fires before avatar-candidate lookup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add processBirth-level worker test (D-14) + update stale worker.ts comment (D-10); run D-15 scoped gate</name>
  <files>src/backend/spawn-requests/worker.ts, src/backend/spawn-requests/worker.test.ts</files>

  <read_first>
    - src/backend/spawn-requests/worker.ts:79-101 (mapEndedEventToReason and the stale comment at line 94 — target of D-10 comment fix)
    - src/backend/spawn-requests/worker.test.ts:254-310 (existing mapEndedEventToReason tests; specifically Test 10b at line 289 which already covers the regex path — do NOT duplicate; the new test is at a higher scope: processBirth-level with a mocked birthIdentity)
    - src/backend/spawn-requests/worker.test.ts:314-380 (existing processBirth describe block; specifically Test 12 at line 357 — the shape for the new role_unknown test is Test 12 with a different reason string)
    - src/backend/spawn-requests/types.ts:78-84 (confirms FailureReason enum contains "role_unknown" — no schema change needed)
    - .planning/phases/108-birth-pipeline-role-folder-existence-check-insert-a-pre-step/108-CONTEXT.md sections D-09, D-10, D-14, D-15
  </read_first>

  <behavior>
    - worker.ts line 94 comment reads exactly: `// Role folder not found on target host (Step 1 check in orchestrator — Phase 108)`. No behavior change to the mapping logic — only the comment.
    - worker.test.ts gains one new processBirth test: `"Test 108-W1: Phase 108 — birthIdentity ended{ok:false, failedStep:1, reason:'role not found on target host: bogus'} → .failure.json body {reason:'role_unknown'}"`. Test mocks `birthIdentity` via `buildTestDeps({ birthIdentity: vi.fn().mockImplementation(async (_opts, emit) => { emit({ type: "step", n: 1, phase: "failed", reason: "role not found on target host: bogus" }); emit({ type: "ended", ok: false, failedStep: 1 }); }) })`. Note the `step:failed` emit BEFORE `ended` — that's how the worker captures `lastStepFailReason` per the mapEndedEventToReason contract (worker.ts:67-72). Then calls `await processBirth(item, deps)`, then asserts `deps.writeMarkdownFileAtomic` was called with a path ending `.failure.json` AND body JSON has `reason === "role_unknown"`.
    - The D-15 scoped test command passes across all three files clean.
  </behavior>

  <action>
    Per D-10: replace the exact string at worker.ts:94 `// Role folder not found on target host (Step 2.5 check in orchestrator)` with `// Role folder not found on target host (Step 1 check in orchestrator — Phase 108)`. No other change to worker.ts.

    Per D-14: append one test to the `describe("processBirth", ...)` block in worker.test.ts (currently ends around Test 13 line 380 — insert after Test 12 or at end of block). Model the test byte-for-byte on Test 12 (line 357) — same `buildTestDeps` + `birthIdentity` mock + `processBirth` + assertion pattern — but:
    - Mock emits `{type: "step", n: 1, phase: "failed", reason: "role not found on target host: bogus"}` FIRST, then `{type: "ended", ok: false, failedStep: 1}`. Both emits — the `step:failed` is REQUIRED so the worker's step-tracking captures `lastStepFailReason` (see worker.ts near-line-94 context) which mapEndedEventToReason then regex-matches.
    - Assert `parsed.reason === "role_unknown"` (not `"birth_failed"` as Test 12).
    - Assert `parsed.message` is either absent OR very short (mirrors Test 12 lines 374-377, per D-09).

    Do NOT duplicate the existing Test 10b (line 289) — that already covers the mapEndedEventToReason unit level. The new test is strictly at processBirth scope.

    Do NOT add any new FailureReason enum values. Do NOT change types.ts.

    Final step: run the D-15 scoped vitest gate: `npx vitest run src/backend/database/routes/identity-birth-orchestrator.test.ts src/backend/database/routes/identity-birth.test.ts src/backend/spawn-requests/worker.test.ts`. All specs green.

    STOP at scoped green. Do NOT run `npx vitest run` (full suite) or `docker build` or `docker compose up --force-recreate` — those are orchestrator-only ship-gate operations per D-16 + the fleet ship-gate rule in the planning context.
  </action>

  <verify>
    <automated>npx vitest run src/backend/database/routes/identity-birth-orchestrator.test.ts src/backend/database/routes/identity-birth.test.ts src/backend/spawn-requests/worker.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "Step 1 check in orchestrator — Phase 108" src/backend/spawn-requests/worker.ts` returns exactly 1 match on line ~94.
    - `grep -c "Step 2.5 check in orchestrator" src/backend/spawn-requests/worker.ts` returns 0 (stale comment fully removed).
    - `grep -n "Test 108-W1" src/backend/spawn-requests/worker.test.ts` returns exactly 1 match.
    - `npx vitest run src/backend/spawn-requests/worker.test.ts -t "Test 108-W1"` reports 1 test passing, 0 failing.
    - Full D-15 scoped command passes: `npx vitest run src/backend/database/routes/identity-birth-orchestrator.test.ts src/backend/database/routes/identity-birth.test.ts src/backend/spawn-requests/worker.test.ts` — all specs green, 0 failures across all three files.
    - `npx tsc --noEmit` (repo root) succeeds — no type errors introduced anywhere.
    - No changes to src/backend/spawn-requests/types.ts (FailureReason enum unchanged): `git diff --stat src/backend/spawn-requests/types.ts` shows no changes.
    - Executor's remit STOPS here. No `npx vitest run` (full suite) was invoked. No docker commands were run. No `git push` was executed. Any of those actions being present in the commit log indicates a boundary violation (per D-16 + fleet ship-gate rule).
  </acceptance_criteria>

  <done>Stale worker.ts comment corrected to reference Phase 108 / Step 1; new processBirth-level test proves the full pipeline emits {reason:"role_unknown"} for a role-folder-miss ended event; D-15 scoped test gate green across all three specs.</done>
</task>

</tasks>

<verification>
Phase-level rollup (executor asserts before writing SUMMARY):

1. `grep -c "role not found on target host" src/backend/database/routes/identity-birth-orchestrator.ts` ≥ 2 (local + remote throw sites)
2. `grep -c "getLocalRolesRoot" src/backend/database/routes/identity-birth-orchestrator.ts` ≥ 2 (import + local-branch usage)
3. `grep -c "Test 108-" src/backend/database/routes/identity-birth-orchestrator.test.ts` == 5
4. `grep -c "Test 108-W1" src/backend/spawn-requests/worker.test.ts` == 1
5. `grep -c "Step 2.5 check in orchestrator" src/backend/spawn-requests/worker.ts` == 0 AND `grep -c "Step 1 check in orchestrator — Phase 108" src/backend/spawn-requests/worker.ts` == 1
6. D-15 scoped vitest command green: `npx vitest run src/backend/database/routes/identity-birth-orchestrator.test.ts src/backend/database/routes/identity-birth.test.ts src/backend/spawn-requests/worker.test.ts`
7. `npx tsc --noEmit` green
8. `git diff --stat src/backend/spawn-requests/types.ts` empty (no schema change)
9. Ordering assertion (Test 108-E) demonstrates the role probe runs before `getCandidateForBirth` — the strongest signal that D-11's "no side effect on bad role" invariant holds.
</verification>

<success_criteria>
- Role-folder probe inserted at the FIRST substantive position inside runStep(1, ...) — before avatar candidate lookup AND before identity-folder collision probe (D-11).
- Local branch uses fs.access on getLocalRolesRoot()/<role>/<role>.md (D-04, reusing the existing export at identity-artifact-reader.ts:238).
- Remote branch uses the shared exec() closure with a `test -f "$HOME/fleet/roles/<role>/<role>.md"` probe (D-05); no new SSH connection, no new deps.
- On miss, throw message is exactly `"role not found on target host: " + opts.role` (D-07), which hits worker.ts:95 regex first alternation.
- Throw propagates through runStep(1)'s existing machinery → step:1:failed + ended{ok:false, failedStep:1} → worker's writeResponseFile drops {reason:"role_unknown"} (D-08, D-09).
- Stale comment at worker.ts:94 corrected (D-10). No FailureReason enum change (D-09).
- 5 orchestrator tests (D-13 A-E) + 1 worker processBirth test (D-14) all pass under the D-15 scoped vitest command.
- File-level Step 1 docstring at identity-birth-orchestrator.ts:12-13 names the role-folder probe.
- Executor stops at scoped green: no full suite, no docker, no push (D-16 + fleet ship-gate rule).
</success_criteria>

<output>
Create `.planning/phases/108-birth-pipeline-role-folder-existence-check-insert-a-pre-step/108-01-SUMMARY.md` when done, capturing: (a) exact line numbers where the probe was inserted, (b) whether getLocalRolesRoot was imported (should be YES — reused the existing export), (c) whether the docstring update landed, (d) test counts (should be 5 orchestrator + 1 worker), (e) D-15 scoped command result, (f) explicit confirmation that no boundary-crossing commands (full vitest, docker, push) were invoked.
</output>
