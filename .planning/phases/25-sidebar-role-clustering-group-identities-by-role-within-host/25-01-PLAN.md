---
phase: 25-sidebar-role-clustering
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/database/routes/identities.ts
  - src/ui/api/identities-api.ts
autonomous: true
requirements: []
tags:
  - sidebar
  - role-clustering
  - identity-list
must_haves:
  truths:
    - "GET /identities response payload carries a `role: string | null` field per row (populated when the identity file has role: frontmatter; null when missing/unreadable)"
    - "A single identity whose role file is missing/malformed does NOT 500 the list endpoint — the bad identity gets role: null, the rest of the list returns normally"
    - "Frontend Identity type exposes `role: string | null` so downstream stores compile against the new field"
  artifacts:
    - path: src/backend/database/routes/identities.ts
      provides: "publicIdentity() gains role parameter; router.get('/') resolves role per row via Promise.all + resolveRoleForIdentity(null, identityKey) with per-row try/catch"
      contains: "role"
    - path: src/ui/api/identities-api.ts
      provides: "Identity interface gains `role: string | null` field"
      contains: "role: string | null"
  key_links:
    - from: src/backend/database/routes/identities.ts
      to: src/backend/claude-session/identity-artifact-reader.ts
      via: "resolveRoleForIdentity(null, row.identityKey) — LOCAL branch, throw caught and treated as null role"
      pattern: "resolveRoleForIdentity\\(null,"
    - from: src/ui/api/identities-api.ts
      to: src/backend/database/routes/identities.ts
      via: "wire shape mirror — role: string | null on Identity matches publicIdentity() output"
      pattern: "role: string \\| null"
---

<objective>
Enrich the `GET /identities` list endpoint with a per-row `role` field resolved fs-side at list-enumeration time, and thread the new field through the frontend wire type. This is the backend + wire foundation for Phase 25's sort behavior — no sort changes here, only data plumbing.

Purpose: The sidebar's new role-clustering sort (Plan 25-02) needs `identity.role` reachable from the frontend identities store. `resolveRoleForIdentity` already exists (Phase 22 SRIC-01, `src/backend/claude-session/identity-artifact-reader.ts:227`); this plan wires it into the list endpoint and grows the `Identity` wire type.

Output:
- `GET /identities` returns each row with `role: string | null` populated per Ashley's "resolved fs-side at list-enumeration time" decision (§Mechanism in 25-CONTEXT.md).
- `Identity` interface in `src/ui/api/identities-api.ts` gains the field — every existing consumer of `identity.role` (added later in Plan 25-02) compiles cleanly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-sidebar-role-clustering-group-identities-by-role-within-host/25-CONTEXT.md
@.planning/phases/25-sidebar-role-clustering-group-identities-by-role-within-host/25-PATTERNS.md
@src/backend/database/routes/identities.ts
@src/ui/api/identities-api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add role enrichment to GET /identities list endpoint + expand publicIdentity()</name>
  <files>src/backend/database/routes/identities.ts</files>
  <read_first>
    - src/backend/database/routes/identities.ts (lines 1-84 — current publicIdentity() at :52-66 and list handler at :68-84; import block at :1-15 needs `resolveRoleForIdentity` added)
    - src/backend/claude-session/identity-artifact-reader.ts (lines 150-244 — resolveRoleForIdentity signature, LOCAL branch semantics, throw-on-missing contract)
    - .planning/phases/25-sidebar-role-clustering-group-identities-by-role-within-host/25-PATTERNS.md (§`src/backend/database/routes/identities.ts` — enrichment pattern; §Shared Patterns → `resolveRoleForIdentity` invocation; §Shared Patterns → Logger warn pattern)
    - .planning/phases/25-sidebar-role-clustering-group-identities-by-role-within-host/25-CONTEXT.md (§Mechanism — Ashley's "resolved fs-side at list-enumeration time" decision; §Null-role handling — swallow-and-null contract)
  </read_first>
  <action>
    Add `resolveRoleForIdentity` to the imports from `../../claude-session/identity-artifact-reader.js` (join the existing import block at the top of the file — do NOT create a second block).

    Modify `publicIdentity()` (currently `src/backend/database/routes/identities.ts:52-66`) to accept a second parameter `role: string | null = null` (default null keeps existing single-arg callers compiling — inspect the file for other callers before finalizing; the create/update/get handlers below currently call `publicIdentity(row)` and MUST continue to work). Add `role,` as the LAST field in the returned object (after `updatedAt`) so the wire shape stays additive-only.

    Modify the list handler `router.get("/", authenticateJWT, ...)` (currently `:68-84`):
    - After `db.select().from(identities).where(eq(identities.userId, userId)).all()` returns `rows`, wrap the response construction in `await Promise.all(rows.map(async (row) => { ... }))` producing an `enriched` array.
    - Inside the map callback, initialize `let role: string | null = null;` then wrap `role = await resolveRoleForIdentity(null, row.identityKey);` in try/catch. `null` for the `conn` argument triggers the LOCAL fs branch (identities are user-scoped, no hostId in the DB — the canonical file lives on the LOCAL host mounting the identities dir).
    - Catch block: call `databaseLogger.warn("Failed to resolve role for identity", { operation: "list_identities_resolve_role", userId, identityKey: row.identityKey })`. Use `warn` (NOT `error`) — a missing/malformed role file is a data-integrity note, not an operational failure. Do NOT include the error object as a second positional arg to `.warn` — inspect the `databaseLogger` signature used elsewhere in this file (`.error("...", e, { ... })` at :78-81 takes error second, context third) and match `.warn`'s shape by omitting the error object OR passing it in the same slot — check `src/backend/utils/logger.ts` for the exact `.warn` overload before choosing.
    - Return `publicIdentity(row, role)`.
    - Replace `return res.json(rows.map(publicIdentity));` with `return res.json(enriched);`.
    - Keep the outer try/catch verbatim (:77-83) — a `Promise.all` failure would only surface if the DB select itself threw, and the per-row try/catch guarantees `resolveRoleForIdentity` throws never escape the map. Existing 500-on-catch semantics preserved.

    Do NOT touch the POST/PUT/DELETE handlers, `IDENTITY_KEY_RE`, `IDENTITY_VOICE_RE`, `parseMultipartMetadata`, or any avatar handling. This change is additive to the list endpoint only.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run build:backend</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "resolveRoleForIdentity" src/backend/database/routes/identities.ts` returns at least 2 hits (import + call site).
    - `grep -n "function publicIdentity" src/backend/database/routes/identities.ts` shows a signature accepting two parameters (row + role).
    - `grep -n "role," src/backend/database/routes/identities.ts` shows `role,` inside the publicIdentity return object.
    - `grep -n "Promise.all(rows.map" src/backend/database/routes/identities.ts` returns 1 hit inside the list handler.
    - `grep -n "list_identities_resolve_role" src/backend/database/routes/identities.ts` returns 1 hit (in the swallow-and-null catch).
    - `grep -c "databaseLogger.warn" src/backend/database/routes/identities.ts | grep -v '^#'` returns at least 1 (new warn call).
    - `npx tsc --noEmit` exits 0.
    - `npm run build:backend` exits 0 (backend tsconfig catches errors frontend tsc doesn't per box-maintainer fleet rules).
    - No changes to POST/PUT/DELETE handlers — `git diff src/backend/database/routes/identities.ts` shows edits confined to imports, publicIdentity(), and the list handler.
  </acceptance_criteria>
  <done>
    The list endpoint returns each identity row with a `role` field populated when its role: frontmatter parses cleanly, or null when resolveRoleForIdentity throws for that row. Backend tsc + build clean. Existing POST/PUT/DELETE handlers untouched and still compile.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend Identity wire type with role field</name>
  <files>src/ui/api/identities-api.ts</files>
  <read_first>
    - src/ui/api/identities-api.ts (lines 1-40 — current `Identity` interface at :3-15, `IdentityInput` at :17-23, `listIdentities()` at :32-39 which is a transparent GET the enriched payload flows through)
    - .planning/phases/25-sidebar-role-clustering-group-identities-by-role-within-host/25-PATTERNS.md (§`src/ui/api/identities-api.ts` — the "add one field after `voice`" pattern)
  </read_first>
  <action>
    Add a single new field `role: string | null;` to the `Identity` interface (`src/ui/api/identities-api.ts:3-15`), placed immediately after `voice: string | null;`. Do NOT add role to `IdentityInput` — role is derived server-side from the identity file's frontmatter, never accepted as client-supplied input on create/update.

    Do NOT touch `listIdentities()`, `createIdentity()`, `updateIdentity()`, `deleteIdentity()`, `getIdentityExistsOnHost()`, `postGenerateAvatarBatch()`, `RoleSummary`, `listRolesForHost()`, `cloneIdentity()`, `createRole()`, `openBirthStream()`, `BirthRequest`, `BirthEvent`, or any of the SSE stream parsing logic. This edit is a single-line insertion inside the `Identity` interface only.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "role: string | null" src/ui/api/identities-api.ts` returns exactly 1 hit inside the `Identity` interface block (does NOT appear in `IdentityInput`).
    - `grep -n "export interface Identity " src/ui/api/identities-api.ts` shows the interface start; the next 15 lines contain both `voice: string | null;` and `role: string | null;` in that order.
    - `grep -n "export interface IdentityInput" src/ui/api/identities-api.ts` shows the interface; `grep -A 8 "export interface IdentityInput" src/ui/api/identities-api.ts | grep -c '^\s*role:'` returns 0 (role NOT added to input type).
    - `npx tsc --noEmit` exits 0 (no downstream consumer breaks — the field is optional-ish semantically since it's typed as `string | null`, so existing destructures that don't mention it keep compiling).
  </acceptance_criteria>
  <done>
    `Identity.role: string | null` is present. Every downstream consumer of `identity.role` (Plan 25-02 adds them) will type-check. IdentityInput is deliberately unchanged — role is server-derived, never client-supplied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /identities | authenticated request (JWT via `authenticateJWT`), returns per-user rows |
| identities.ts → identity-artifact-reader.ts | LOCAL fs read via `resolveRoleForIdentity(null, identityKey)` — no SSH, no user-supplied path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01-01 | Tampering | resolveRoleForIdentity(null, identityKey) | accept | `identityKey` is DB-owned (columns.identityKey), never user-supplied at this call site. `resolveRoleForIdentity` internally re-validates via `IDENTITY_KEY_RE` (identity-artifact-reader.ts:238) — no new path-injection surface added by the list endpoint enrichment. Confirmed per 25-CONTEXT.md `<security_context>` block. |
| T-25-01-02 | Denial-of-Service | Promise.all fan-out over identity rows | accept | Ashley's fleet has ~dozen identities (25-CONTEXT.md §Specifics); LOCAL fs reads are ms-scale; parallelization matches the sessions.ts precedent (patterns §sessions.ts Promise.all). No throttling needed. |
| T-25-01-03 | Availability | one bad identity role file 500s the whole list | mitigate | Per-row try/catch around `resolveRoleForIdentity` swallows the throw and sets `role = null` + logs warn; the outer 500-on-catch (:77-83) only fires for DB-level failures. Verified by acceptance-criteria grep for `list_identities_resolve_role`. |
| T-25-01-04 | Information Disclosure | role field exposed on list response | accept | Role is fleet-internal metadata already visible to the authenticated user in Skynet's IdentityModal Role tab (Phase 22). Adding it to the list endpoint expands the surface by zero — same user, same auth boundary. |
</threat_model>

<verification>
- Backend list endpoint response shape: `role: string | null` on every row of the GET /identities response.
- One malformed identity → its row has `role: null` in the response, all other rows unaffected, HTTP 200.
- Existing IdentityModal + NewSessionDialog + CloneAgentDialog callers of `Identity` continue to work — the added field is additive, no removals.
- Frontend `Identity` type carries the field so Plan 25-02's `byKey.get(key)?.role` compiles.
</verification>

<success_criteria>
- `GET /identities` response payload carries per-row `role: string | null` populated from `resolveRoleForIdentity(null, identityKey)` with swallow-and-null on failure.
- `Identity.role: string | null` present on the wire type.
- Backend tsc + `npm run build:backend` both exit 0.
- No changes to non-list handlers in `identities.ts`; no changes to `IdentityInput` or non-Identity types in `identities-api.ts`.
</success_criteria>

<output>
Create `.planning/phases/25-sidebar-role-clustering-group-identities-by-role-within-host/25-01-SUMMARY.md` when done, noting:
- Where `role` enrichment is called from (line reference in `identities.ts` post-edit).
- How the swallow-and-null path is logged (databaseLogger.warn shape used).
- Confirmation that `publicIdentity()`'s new second parameter has a default so non-list callers stay wire-compatible.
</output>
