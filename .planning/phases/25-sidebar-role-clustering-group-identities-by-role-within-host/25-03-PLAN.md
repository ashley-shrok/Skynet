---
phase: 25-sidebar-role-clustering
plan: 3
type: execute
wave: 3
depends_on:
  - 25-01
  - 25-02
files_modified:
  - src/ui/state/conversation-store.test.ts
autonomous: true
requirements: []
tags:
  - sidebar
  - role-clustering
  - test
  - regression-guard
must_haves:
  truths:
    - "New sort behavior (host outer, role middle, label inner, null-role-last, case-insensitive) is regression-covered in conversation-store.test.ts across all three tiers (ActiveSet, Pinned, Tier-3)"
    - "Test scaffold uses the pre-existing makeHost/makeTab/act/__getSnapshotForTest utilities plus a new updateIdentitiesByKey helper — no new test infrastructure required"
    - "Null-role rows are proven to sort AFTER role-carrying rows within a host bucket"
    - "Existing conversation-store tests still pass — any pre-existing assertion that Phase 25 supersedes is either preserved (label-only within single host) or intentionally updated in this plan (documented in each edit)"
  artifacts:
    - path: src/ui/state/conversation-store.test.ts
      provides: "Phase 25 test block covering role-clustering within host, host-outer ordering in ActiveSet/Pinned, null-role sort-last, case-insensitivity across role AND label, same-role different-label falls to label"
      contains: "Phase 25"
  key_links:
    - from: src/ui/state/conversation-store.test.ts (Phase 25 block)
      to: src/ui/state/conversation-store.ts → compareByHostRoleLabel
      via: "act(() => updateIdentitiesByKey(new Map([...]))) drives role onto rows; __getSnapshotForTest() reads the sorted output"
      pattern: "updateIdentitiesByKey"
---

<objective>
Add Phase 25 regression coverage to `conversation-store.test.ts`. Six scenarios per 25-CONTEXT.md §Specifics point 6 — role-clustering-within-host, host-outer-in-active-and-pinned, null-role-last, case-insensitivity across role AND label, same-role-different-label falls to label, stable ordering. Use the pre-existing test scaffold — this plan adds test cases, not infrastructure.

Purpose: Lock the sort behavior against future regressions. The scaffold at `conversation-store.test.ts:1042-1067` (`quick-260730-wfy`) is the direct template — copy its shape, extend with `updateIdentitiesByKey` calls that inject role onto rows.

Output:
- One new `describe` block "conversation-store (Phase 25): role-clustering sort" containing 5-7 `it` cases covering all six scenarios.
- Updates to `beforeEach` to reset `identitiesByKey` between tests (mirrors the existing `updateHostsFlat(new Map())` reset at `conversation-store.test.ts:107`).
- Any pre-existing test that Plan 25-02 flagged as v0-superseded is either confirmed still valid or explicitly updated with a rationale comment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-sidebar-role-clustering-group-identities-by-role-within-host/25-CONTEXT.md
@.planning/phases/25-sidebar-role-clustering-group-identities-by-role-within-host/25-PATTERNS.md
@.planning/phases/25-sidebar-role-clustering-group-identities-by-role-within-host/25-02-SUMMARY.md
@src/ui/state/conversation-store.test.ts
@src/ui/state/conversation-store.ts
@src/ui/api/identities-api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 25 role-clustering test block + reset scaffold</name>
  <files>src/ui/state/conversation-store.test.ts</files>
  <read_first>
    - src/ui/state/conversation-store.test.ts (lines 1-108 for imports + fixtures + beforeEach; lines 1032-1095 for the `quick-260730-wfy` template scaffold and the adjacent Test-30c/30d patterns to follow for describe naming, act() blocks, and __getSnapshotForTest() usage)
    - src/ui/state/conversation-store.ts (post-25-02 state — compareByHostRoleLabel signature; updateIdentitiesByKey export; ConversationRow.role field; the five sort call sites for cross-reference)
    - src/ui/api/identities-api.ts (Identity interface — need shape for test-fixture Identity objects; `role: string | null` is now present)
    - .planning/phases/25-sidebar-role-clustering-group-identities-by-role-within-host/25-PATTERNS.md (§conversation-store.test.ts — test scaffolding pattern, key helpers list, new test scenarios required, and CRITICAL note on how identity-key-to-byKey injection works)
    - .planning/phases/25-sidebar-role-clustering-group-identities-by-role-within-host/25-02-SUMMARY.md (list of pre-existing tests flagged as v0-superseded — those get explicit attention here)
  </read_first>
  <action>
    (a) Update the imports at the top of `src/ui/state/conversation-store.test.ts` to add `updateIdentitiesByKey` (join the existing `import { ... } from "./conversation-store.js"` block that already carries `updateHostsFlat` at :17). Also add `import type { Identity } from "@/api/identities-api";` (join or create the appropriate type import block).

    (b) Add a helper `makeIdentity(identityKey, role, overrides?)` immediately below `makeTab` (~:57-73). Shape:
    - Takes `identityKey: string`, `role: string | null`, and optional `Partial<Identity>` overrides.
    - Returns a minimal `Identity` object with only the fields tests need: `identityKey`, `role`, plus dummy stubs for `id`, `displayName`, `title`, `colorHue`, `voice`, `avatarMime`, `avatarUrl`, `avatarEtag`, `createdAt`, `updatedAt`. Any field the store doesn't read can be `as unknown as string` or empty-string. Follow the `makeHost` pattern of typing via `as unknown as Identity` to avoid ceremony.

    (c) Add a helper `identitiesMap(...pairs: Identity[])` that builds a `Map<string, Identity>` keyed by `identityKey.toLowerCase()` — MUST match the key convention `identities-store.ts:21` uses (already documented in 25-PATTERNS.md §sessionMatchKey). Return type `Map<string, Identity>`.

    (d) Extend `beforeEach` at :80-108 by adding `updateIdentitiesByKey(new Map());` immediately after the existing `updateHostsFlat(new Map());` at :107. Add a short header comment noting Phase 25 — reset identitiesByKey so a prior test's role injection doesn't leak forward.

    (e) At the END of the file (append after the last existing `describe` block), add a new block:

    `describe("conversation-store (Phase 25): role-clustering sort", () => { ... });`

    Inside it, add these `it` cases. Each case uses `act()` to drive state via `updateHostTree`, `updateOpenTabs`, `updateFleetSessions`, `updateHostsFlat` (as needed), and `updateIdentitiesByKey`, then asserts on `__getSnapshotForTest()` output. Fixture pattern for identity injection:

    - Create tabs whose `targetTmuxSession` matches the intended identity key (case-insensitive — sessionMatchKey lowercases).
    - Build an `identitiesMap()` mapping those keys to `makeIdentity()` results with the desired role.
    - Call `updateIdentitiesByKey(map)` before `__getSnapshotForTest()`.

    Test cases:

    **1. Role-clustering within a host bucket (Tier 3)** — Given hostA with 3 openTabs whose targetTmuxSession names are "orwell", "asimov", "orwell-clone" (identities orwell + orwell-clone share role "novelist"; asimov has role "essayist"), pin nothing, active-set nothing, hostTree = { root, children: [hostA] }. Assert `snap.grouped[0].rows.map(r => r.label)` groups the two novelists adjacent: expected order is essayist role first alphabetically ("asimov"), then novelist role ("orwell" then "orwell-clone" alphabetically within role).

    **2. Host is outer sort in ActiveSet** — Given hostA + hostB (host names "alpha", "beta"), one openTab per host both added to activeSet, both with identities carrying the SAME role "architect". Assert `snap.activeSet.map(r => r.host?.name)` returns `["alpha", "beta"]` (host outer, role inner is a tie, label further inner). Same-role rows separated by host bucket even in the flat ActiveSet tier.

    **3. Host is outer sort in Pinned** — Same shape as #2 but pin the two rows instead of active-set. Assert `snap.pinned.map(r => r.host?.name)` returns `["alpha", "beta"]`.

    **4. Null-role rows sort last within their host** — Given hostA with three openTabs: two identities with roles ("architect", "builder") and one openTab with `targetTmuxSession: "unmapped-session"` (NO entry in identitiesByKey → matchKey lookup returns undefined → role is null-omitted → sorts last). Assert `snap.grouped[0].rows.map(r => r.label)` places the unmapped row LAST regardless of its label's alphabetical position vs. the mapped rows. To make the assertion meaningful, name the unmapped tab's label something that would otherwise sort FIRST (e.g. "aaa-would-be-first") so a pure label sort would put it at index 0 — Phase 25's null-role-last puts it at index 2.

    **5a. Case-insensitivity on ROLE** — Given hostA with three openTabs whose identities carry roles "Box-Maintainer", "box-maintainer", and "zeta-role" (labels chosen distinct and irrelevant to the assertion, e.g. `bm-a`, `bm-b`, `zed`). Assert `snap.grouped[0].rows.map(r => r.label)` places the two case-different-`box-maintainer` rows at positions 0 and 1 with `zed` at position 2 — i.e. the two role variants CLUSTER at the head of the bucket, not scattered around `zeta-role`. This proves role compare uses `sensitivity: "base"` (case-insensitive). Under a case-sensitive compare "Box-Maintainer" (uppercase B, codepoint 66) would sort before "box-maintainer" (lowercase b, codepoint 98) with `zeta-role` (z, 122) at the end — same output in THIS fixture but for the wrong reason. To disambiguate, add a fourth openTab with role `q-role` and label `q-tab`: under case-insensitive compare all four sort as `bm-a, bm-b, q-tab, zed` (Box-Maintainer < q-role < zeta-role); under case-sensitive compare it would be `bm-a, q-tab, zed, bm-b` (B<q<z<b). Assert the case-insensitive order — this is the load-bearing behavioral gate.

    **5b. Case-insensitivity on LABEL within a role** — Given hostA with three openTabs sharing role "novelist" and labels "Alpha", "alpha", and "Zebra". Assert `snap.grouped[0].rows.map(r => r.label.toLowerCase())` returns `["alpha", "alpha", "zebra"]` — the two case-different-`alpha` rows occupy positions 0 and 1 and "Zebra" is at position 2, proving label compare with `sensitivity: "base"` treats "Alpha" and "alpha" as equivalent for ordering (both before "Zebra"). Under a case-sensitive compare it would be `Alpha` (A=65), `Zebra` (Z=90), `alpha` (a=97). The relative order of "Alpha" vs "alpha" between positions 0 and 1 is a V8 sort-stability decision (both equal under `sensitivity: "base"`); the assertion checks POSITION of the cluster relative to "Zebra", not the internal order — deterministic without depending on sort stability.

    **6. Same-role different-label falls to label** — Given hostA with three openTabs sharing role "builder" but labels ["mike", "alice", "zed"]. Assert `snap.grouped[0].rows.map(r => r.label)` returns `["alice", "mike", "zed"]` — role tie forces the comparator to the label inner key, which sorts alphabetically case-insensitive numeric-natural (same as pre-Phase-25).

    Each `it` case gets a 1-line comment above it noting which CONTEXT.md decision it locks in (e.g. "// Locks: §Sort semantics tuple order (host outer, role middle, label inner)").

    (f) Handle any pre-existing tests flagged in 25-02-SUMMARY as v0-superseded:
    - If Plan 25-02 flagged specific tests, revisit them here. For each: read the assertion, decide if Phase 25 preserves (label-only within one host, no role → tie → label inner → same output) or supersedes (crosses hosts and relied on flat-alphabetical). If preserved, no edit. If superseded, update the assertion with an inline comment: `// Phase 25 supersedes: host is now outer sort key; expected order accordingly reordered.` Do NOT delete the test — adjust the expected order to match new semantics.
    - If Plan 25-02 flagged NONE (the existing test suite happened to only assert single-host ordering — the most likely case given the `quick-260730-wfy` scaffold at :1042-1067 uses only hostA), this step is a no-op. Document that in the SUMMARY.

    Constraints:
    - Do NOT introduce a new test file. All Phase 25 tests go into the existing `conversation-store.test.ts`.
    - Do NOT touch the mock at :8-11 (user-preferences-api mock — unrelated).
    - Do NOT add tests for the backend list endpoint enrichment — per CONTEXT.md §Claude's Discretion, no existing test scaffold for identities.ts route and adding one is out of scope for this plan.
    - Use `numeric: true` awareness in test label design — don't accidentally rely on numeric-natural ordering on ROLE (which the comparator does NOT use for role); role sort is plain case-insensitive.
    - Every `it` block asserts on `snap.activeSet`, `snap.pinned`, or `snap.grouped[N].rows` shape — do NOT peek into internal comparator behavior.
  </action>
  <verify>
    <automated>npx vitest run src/ui/state/conversation-store.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "describe.*Phase 25.*role-clustering" src/ui/state/conversation-store.test.ts | grep -v '^#'` returns exactly 1.
    - `grep -c "^    it(" src/ui/state/conversation-store.test.ts` increases by at least 5 vs. the pre-25-03 baseline (6 new it-blocks expected, plus the count also increases for any repair edits — verify baseline first via git).
    - `grep -n "updateIdentitiesByKey" src/ui/state/conversation-store.test.ts` returns at least 7 hits (import + beforeEach reset + at least one act() call per new it-block).
    - `grep -n "makeIdentity\b" src/ui/state/conversation-store.test.ts` returns at least 7 hits (definition + at least one call per it-block that injects role).
    - `grep -n "aaa-would-be-first" src/ui/state/conversation-store.test.ts` returns exactly 1 hit inside the null-role-last test (this is the load-bearing label choice that proves null-role sorts later than a lexically-first label).
    - `npx vitest run src/ui/state/conversation-store.test.ts` — ALL tests pass (existing + new). If any pre-existing test needed a repair, the repair MUST be reflected in the snapshot output with a comment starting `// Phase 25 supersedes:`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    Phase 25 sort behavior is regression-covered by dedicated tests. Any pre-existing test that Phase 25 legitimately supersedes has been updated with an inline rationale comment. All conversation-store.test.ts tests pass end-to-end. Future changes to compareByHostRoleLabel that break the tuple order, null-role-last handling, or case-insensitivity fail loudly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → conversation-store (production code) | tests exercise real store mutations via exported APIs; no fake conversation-store shim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03-01 | Repudiation | test-only mutation via updateIdentitiesByKey | accept | Tests use the real production API; no back-door mutation. Any store-shape drift shows up as a test compile error, not a false-pass. |
| T-25-03-02 | Denial-of-Service | test suite runtime cost | accept | Six new small it-blocks; vitest already runs the ~1000-line test file in seconds; delta is negligible. |
</threat_model>

<verification>
- `npx vitest run src/ui/state/conversation-store.test.ts` — every test passes, no skips, no failures.
- Six Phase 25 scenarios all covered per this plan's Task 1 (f) enumeration.
- `beforeEach` reset extended to include `updateIdentitiesByKey(new Map())` — no leak of role state across tests.
- Any pre-existing test flagged in 25-02-SUMMARY has an inline `// Phase 25 supersedes:` comment OR was confirmed still valid without edit.
</verification>

<success_criteria>
- Phase 25 test block added with 5-7 `it` cases covering role-clustering-within-host, host-outer-in-active-and-pinned, null-role-last, case-insensitivity, same-role-different-label, and stability.
- Test-suite green: `npx vitest run src/ui/state/conversation-store.test.ts` exits 0.
- Frontend tsc exits 0.
- No new test infrastructure files created (existing helpers reused, one new `makeIdentity` + `identitiesMap` helper added inline).
</success_criteria>

<output>
Create `.planning/phases/25-sidebar-role-clustering-group-identities-by-role-within-host/25-03-SUMMARY.md` when done, noting:
- Count of new it-blocks added (expected 7 — case-insensitivity split into 5a/5b per plan revision 2026-08-05).
- Whether any pre-existing tests were repaired (list them by describe block name) or all were preserved.
- Confirmation that `updateIdentitiesByKey(new Map())` was added to beforeEach.
- Full `npx vitest run src/ui/state/conversation-store.test.ts` output (pass count, fail count if any).
</output>
