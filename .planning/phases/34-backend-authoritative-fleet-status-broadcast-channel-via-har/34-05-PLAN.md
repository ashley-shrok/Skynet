---
phase: 34-backend-authoritative-fleet-status-broadcast-channel-via-har
plan: 05
type: execute
wave: 3
depends_on: [34-04]
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "Every persistent Monitor tool launched by the id-skill on-wake block has its description prefixed with '[ambient]' (recv, wakeup-scheduler, ctxwatch, and any per-identity persistent extras like tina's skynet-recv)"
    - "At least one live identity in the fleet has been reloaded (/id or fresh wake) AFTER the id-skill edit landed, so the running Monitors carry the [ambient] prefix in their Stop-hook payload"
    - "Fleet-agent-supervisor's parallel Monitor launch path (Nellie's coordination surface) also emits [ambient] description prefixes if that path exists — coordination done by orchestrator (tina) with Nellie"
    - "The companion bounty ~/.claude/roles/box-maintainer/bounties/ambient-monitor-tagging-in-id-skill/ is marked complete with a captured Stop payload from a reloaded identity showing the [ambient] prefix on every Monitor entry"
  artifacts: []
  key_links:
    - from: "id-skill on-wake block"
      to: "src/fleet-status-watcher (Plan 01 filterAmbientTasks)"
      via: "[ambient] description prefix is the filter key — task.description?.startsWith('[ambient]') drops the entry from the fleet-status backgroundTasks"
      pattern: "\\[ambient\\]"
---

<objective>
**This is an EXTERNAL, ORCHESTRATOR-MANAGED gate — NOT a Skynet-repo executor task.**

The `ambient-monitor-tagging-in-id-skill` companion bounty ships the `[ambient]` description-prefix marker on every persistent Monitor launched by the id-skill on-wake block. This must land BEFORE Plan 06 (frontend cutover) is scheduled, otherwise every identity in the fleet will look permanently busy — the persistent recv/wakeup-scheduler/ctxwatch Monitors would count as non-ambient background work and `isWorking` would be true for every session forever.

Per RESEARCH § 1, the env-var approach (`AGENT_AMBIENT=1`) is BLOCKED — env vars do not appear in the Stop-hook `background_tasks[]` payload. The `description` prefix is the correct mechanism, verified against the official docs field table.

Purpose: Sequence guard between the mechanical backend/watcher/UI plans (01-04) and the frontend cutover (06). If this gate is not closed, Plan 06 is not safe to execute.

Output: A completed companion bounty + evidence (captured Stop payload from a reloaded identity) that the id-skill change is live on every identity-hosting box.

Non-goal: This plan does NOT touch the Skynet repo. It does NOT modify any file under `/home/ubuntu/skynet/`. The only artifact this "produces" is the state of `~/.claude/skills/id/SKILL.md` on each identity's box + the evidence that at least one reload has happened.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-CONTEXT.md
@.planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-RESEARCH.md
@.planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-01-SUMMARY.md
@.planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Confirm id-skill ambient-tagging is shipped fleet-wide</name>
  <files>~/.claude/skills/id/SKILL.md (external to Skynet repo — read-only reference here; edited by companion bounty), ~/.claude/roles/box-maintainer/bounties/ambient-monitor-tagging-in-id-skill/bounty.json (external — status flip evidence)</files>
  <read_first>
    - .planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-CONTEXT.md (§ Hard dependency)
    - .planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-RESEARCH.md (§ RECOMMENDATION on Ambient-Monitor Marker Mechanism — why description prefix vs env var)
    - src/fleet-status-watcher/deploy/monitor-payload-verify.sh (Plan 04 Task 3 — the capture procedure used in step 4 below)
  </read_first>
  <action>Coordination checkpoint only — the orchestrator (tina) drives the id-skill edit in `~/.claude/skills/id/SKILL.md` and Nellie coordinates the fleet-agent-supervisor parallel path. Follow the how-to-verify steps below to confirm the tag propagates through the harness Stop payload on a reloaded identity. Zero Skynet-repo changes.</action>
  <verify>
    <human-check>Steps 1-6 in how-to-verify below all pass; captured Stop payload attached to companion bounty timeline.</human-check>
  </verify>
  <done>At least one live identity reloaded post-edit; captured Stop payload confirms every persistent Monitor entry carries a description prefix of [ambient]; companion bounty ambient-monitor-tagging-in-id-skill closed with the evidence attached; Nellie has confirmed fleet-agent-supervisor parallel path status (updated / no such path / gap identified).</done>
  <what-built>
    - Nothing in the Skynet repo. This checkpoint gates on work happening in `~/.claude/skills/id/SKILL.md` (a non-Skynet file) coordinated by the orchestrator (tina) with Nellie for the fleet-agent-supervisor parallel launch path.
  </what-built>
  <how-to-verify>
    1. **Confirm the id-skill edit landed**: read `~/.claude/skills/id/SKILL.md` and confirm the on-wake block launches every persistent Monitor with a description that starts with `[ambient]`. Specifically look for the recv Monitor, the wakeup-scheduler Monitor, the ctxwatch Monitor, and any per-identity persistent extras (e.g. tina's skynet-recv Monitor). Each should have `description: "[ambient] <what-it-monitors>"` (or the equivalent in whatever launch syntax the skill uses).
    2. **Confirm fleet-agent-supervisor coordination**: if fleet-agent-supervisor has a parallel Monitor launch path (Nellie owns this), confirm with Nellie that the tagging is applied there too. If it does not have such a path, note that in the resume signal.
    3. **Reload at least one live identity**: pick any identity currently running (tina on this box is convenient — `/id` reload from an active session). Wait for the on-wake block to complete.
    4. **Capture a Stop payload from the reloaded identity**: run the temporary stop-hook capture procedure documented in `src/fleet-status-watcher/deploy/monitor-payload-verify.sh` (Plan 04 Task 3). Confirm the captured `background_tasks[]` array contains monitor entries AND every monitor entry has a `description` field starting with `[ambient]`.
    5. **Optional but recommended**: reload a second identity (e.g. tanya or tiffany) as a spot-check that the tagging is deterministic across identities, not just tina.
    6. **Update the companion bounty**: mark `~/.claude/roles/box-maintainer/bounties/ambient-monitor-tagging-in-id-skill/bounty.json` status = `complete` and attach the captured payload as evidence in the bounty's timeline.
  </how-to-verify>
  <resume-signal>Type "approved: ambient tagging shipped" once every persistent Monitor on at least one reloaded identity carries the `[ambient]` description prefix in a live-captured Stop payload, AND the companion bounty is closed with evidence. If the id-skill edit reveals a different Monitor payload shape than RESEARCH § 1 anticipated (e.g. the description field is stripped or truncated by the harness), STOP and revise Plan 06's filter logic before flipping the frontend cutover. If Nellie's fleet-agent-supervisor launch path exists and cannot be updated in time, describe the gap — Plan 06 may need a defensive fallback filter for the interim.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This plan is a coordination checkpoint.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-17 | Repudiation | id-skill edit auditability | mitigate | Companion bounty timeline entry with captured payload provides the audit trail; the id-skill file itself is git-tracked under `~/.claude/skills/` |
</threat_model>

<verification>
- Manual verification only — this plan is a human-action checkpoint gating Plan 06
- No automated tests apply because no code in the Skynet repo is modified
</verification>

<success_criteria>
- id-skill on-wake block launches every persistent Monitor with `[ambient]` description prefix
- At least one live identity has been reloaded post-edit
- Live-captured Stop payload confirms the prefix is preserved by the harness end-to-end
- Companion bounty closed with evidence
- Fleet-agent-supervisor parallel path (if any) coordinated with Nellie
</success_criteria>

<output>
Create `.planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-05-SUMMARY.md` documenting: (a) which identities were reloaded and when, (b) the captured Stop payload evidence (attach the JSON blob or reference the bounty timeline entry), (c) any deviation from the RESEARCH § 1 expected shape (which would force Plan 06 revision before scheduling), (d) Nellie's fleet-agent-supervisor status.
</output>
