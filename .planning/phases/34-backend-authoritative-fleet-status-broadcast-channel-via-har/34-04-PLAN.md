---
phase: 34-backend-authoritative-fleet-status-broadcast-channel-via-har
plan: 04
type: execute
wave: 2
depends_on: [34-01, 34-02]
files_modified:
  - src/fleet-status-watcher/src/skynet-ws-client.ts
  - src/fleet-status-watcher/src/skynet-ws-client.test.ts
  - src/fleet-status-watcher/src/index.ts
  - src/fleet-status-watcher/deploy/fleet-status-watcher.service
  - src/fleet-status-watcher/deploy/stop-hook.sh
  - src/fleet-status-watcher/deploy/install.sh
  - src/fleet-status-watcher/deploy/discover-identity-hosts.sh
  - src/fleet-status-watcher/deploy/monitor-payload-verify.sh
  - src/fleet-status-watcher/deploy/README.md
  - docker/nginx.conf
  - docker/nginx-https.conf
autonomous: false
requirements: []
user_setup:
  - service: id-skill-ambient-tagging
    why: "Plan 05 (external, orchestrator-managed) MUST land the [ambient] description-prefix tagging in the id-skill on-wake block BEFORE Plan 06 is scheduled. This plan (04) also depends on running Monitor-payload verification (Task 3) BEFORE the ambient filter is shipped in Plan 06."
    dashboard_config:
      - task: "Confirm ~/.claude/roles/box-maintainer/bounties/ambient-monitor-tagging-in-id-skill/ is in_progress AND the id-skill edit has NOT yet landed — Task 3 (Monitor-payload verify) requires a NON-tagged Monitor to reproduce the current-behavior baseline"
        location: "orchestrator (tina) coordinates with Nellie for the id-skill edit timing"
must_haves:
  truths:
    - "Watcher publishes SessionState + session_gone frames to the Skynet backend fleet-status /fleet-status/watcher WS endpoint (Plan 02) instead of stdout"
    - "Watcher opens exactly one WS connection to Skynet at boot; on drop it retries with exponential backoff mirroring the patch #148 pattern (2s, 4s, 6s, 8s, 8s ≈ 28s window)"
    - "Watcher's `hello` handshake sends `os.hostname()` as the hostname field; Skynet backend maps that to the Skynet host record via resolveHostRecordByName (Plan 02 Task 1)"
    - "systemd user unit template installs cleanly on any Ubuntu 24.04 identity-hosting box; `systemctl --user enable --now fleet-status-watcher` starts the watcher, `systemctl --user restart fleet-status-watcher` picks up an updated binary"
    - "Stop-hook install script adds a `~/.claude/settings.json` hooks.Stop[] entry that pipes the payload into the watcher's Unix socket without blocking Claude Code (payload write is fire-and-forget with a 5ms socat/nc timeout)"
    - "install.sh discovers identity-hosting hosts DYNAMICALLY by querying ~/.claude/sessions/ on each managed box (via SSH) rather than baking a static host list — handles OQ-1 (ZoeyBattlestation) and OQ-4 (ashley-beelink) at deploy time"
    - "monitor-payload-verify.sh runs on a scratch box and produces a captured Stop payload showing a Monitor-type task with a description field — this SATISFIES RESEARCH § OQ-2 before Plan 06 wires the ambient filter"
    - "nginx.conf AND nginx-https.conf both have a new `location /fleet-status/ws` block that proxy-passes to the backend on port 30012 (PROJECT.md constraint: every new backend route needs matching blocks in BOTH configs)"
    - "Every WS lifecycle boundary + install-script step + hook write logs a structured line (JSON for the watcher, well-formatted stderr for the install scripts) with hostId, watcher pid, and the WS transition or install-step context"
  artifacts:
    - path: "src/fleet-status-watcher/src/skynet-ws-client.ts"
      provides: "WebSocket client that connects to /fleet-status/watcher, sends hello, publishes session_state + session_gone frames, retries on drop"
      exports: ["createSkynetWsClient", "SkynetWsClient"]
      min_lines: 100
    - path: "src/fleet-status-watcher/deploy/fleet-status-watcher.service"
      provides: "systemd user unit template — parameterises SKYNET_WS_URL + optional log path"
    - path: "src/fleet-status-watcher/deploy/stop-hook.sh"
      provides: "Bash Stop-hook script that pipes stdin payload into the watcher's Unix socket via `nc -U -w 0.005` (or `socat` fallback)"
    - path: "src/fleet-status-watcher/deploy/install.sh"
      provides: "Idempotent installer: scp watcher bundle + service unit + hook script to a target box, register the hook in ~/.claude/settings.json, systemctl enable + start"
    - path: "src/fleet-status-watcher/deploy/discover-identity-hosts.sh"
      provides: "Enumerates the current identity-hosting hosts by SSHing to each managed box in ~/.claude/roles/box-maintainer/box-map.md and checking whether ~/.claude/sessions/ contains any *.json files"
    - path: "src/fleet-status-watcher/deploy/monitor-payload-verify.sh"
      provides: "One-shot verification script (Task 3): installs a temporary Stop hook on a scratch identity, invokes a Monitor tool call, captures the payload, prints the Monitor-type entry shape — RESEARCH § OQ-2 evidence"
    - path: "src/fleet-status-watcher/deploy/README.md"
      provides: "Runbook — install / update / uninstall / debug / log locations, and instructions for how the orchestrator uses this after each plan execution"
    - path: "docker/nginx.conf"
      provides: "Adds a `location /fleet-status/ws { proxy_pass http://skynet:30012; ... }` block matching the existing per-pane WS blocks"
    - path: "docker/nginx-https.conf"
      provides: "Same `location /fleet-status/ws` block for the HTTPS profile"
  key_links:
    - from: "src/fleet-status-watcher/src/index.ts"
      to: "src/fleet-status-watcher/src/skynet-ws-client.ts"
      via: "index.ts constructs createSkynetWsClient({ url, hostname }) and replaces the stdout writer with client.publishSessionState + client.publishSessionGone"
      pattern: "createSkynetWsClient\\(|client\\.publish"
    - from: "src/fleet-status-watcher/deploy/install.sh"
      to: "src/fleet-status-watcher/deploy/stop-hook.sh"
      via: "install.sh scp's stop-hook.sh into ~/.local/bin/ on the target box and appends a Stop hook entry in ~/.claude/settings.json pointing at that path"
      pattern: "scp.*stop-hook.sh|hooks.Stop"
    - from: "src/fleet-status-watcher/deploy/install.sh"
      to: "src/fleet-status-watcher/deploy/discover-identity-hosts.sh"
      via: "install.sh calls discover-identity-hosts.sh at the top to build the target list, rather than reading a static file"
      pattern: "discover-identity-hosts.sh"
    - from: "docker/nginx.conf"
      to: "src/backend/fleet-status/fleet-status-server.ts (Plan 02)"
      via: "location block proxy_passes /fleet-status/ws to the backend port 30012 with WebSocket upgrade headers"
      pattern: "location /fleet-status/ws|proxy_pass.*30012"
---

<objective>
Wire the per-box watcher (Plan 01) to the Skynet backend broadcast server (Plan 02) via WebSocket transport, then produce the install/update/deploy tooling that gets the watcher running on every identity-hosting box. This plan also handles the two live-verification gates D-CTX identifies (RESEARCH § OQ-2: verify Monitor-type payload shape on a real Stop event; RESEARCH § OQ-1 + OQ-4: enumerate identity-hosting hosts dynamically instead of baking a static list). Nginx location blocks are added to both config files per PROJECT.md's mandatory-both-nginxes constraint.

This plan is `autonomous: false` because Task 4 is a `checkpoint:human-verify` that requires Ashley (or tina at orchestrator scope) to run the discover + install scripts against the real fleet and confirm the watchers are reporting to Skynet correctly. Executor code stops at "install script drafted, tested against a scratch box, ready for orchestrator to run against the real fleet."

Purpose: Turn Plans 01 + 02 from disjoint pieces into a working end-to-end pipe (minus the frontend consumer, which is Plan 06). After this plan, every identity-hosting box is streaming SessionState into the Skynet backend registry — but the frontend still consumes the old feeders (session-working-store.ts is untouched). That last swap is Plan 06, gated behind Plan 05 (id-skill ambient tagging).

Output: Watcher's Skynet WS client + retry logic + hello handshake, systemd unit template, Stop-hook script + installer, dynamic host-discovery script, OQ-2 Monitor-payload verification script, both nginx configs updated, deploy runbook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-CONTEXT.md
@.planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-RESEARCH.md
@.planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-01-SUMMARY.md
@.planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-02-SUMMARY.md
@src/fleet-status-watcher/src/index.ts
@src/backend/fleet-status/fleet-status-server.ts
@src/backend/fleet-status/wire-protocol.ts
@src/ui/features/pretty-view/PrettyView.tsx
@docker/nginx.conf
@docker/nginx-https.conf
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Skynet WS client for watcher — hello handshake + publish + retry-with-backoff</name>
  <files>src/fleet-status-watcher/src/skynet-ws-client.ts, src/fleet-status-watcher/src/skynet-ws-client.test.ts, src/fleet-status-watcher/src/index.ts</files>
  <read_first>
    - src/fleet-status-watcher/src/types.ts (Plan 01 SessionState + BackgroundTask shapes)
    - src/backend/fleet-status/wire-protocol.ts (Plan 02 WatcherInboundFrame — must be constructed to match)
    - src/ui/features/pretty-view/PrettyView.tsx lines 655-720 (patch #148 reconnect scheduler — 2s, 4s, 6s, 8s, 8s backoff pattern the watcher must mirror per D-CTX § Delivery topology)
    - src/fleet-status-watcher/src/index.ts (Plan 01's stdout writer — this task replaces it with a WS client)
  </read_first>
  <behavior>
    - Test 1: createSkynetWsClient({ url, hostname }) opens a WS to the URL and on `open` sends a single `{ schemaVersion: 1, type: 'hello', hostname }` frame
    - Test 2: client.publishSessionState(state) sends `{ schemaVersion: 1, type: 'session_state', state }` — waits for hello ACK / open before flushing (buffers if connection is not yet open)
    - Test 3: client.publishSessionGone(tmuxSession, sessionId) sends `{ schemaVersion: 1, type: 'session_gone', tmuxSession, sessionId }`
    - Test 4: When the WS closes unexpectedly, client schedules a reconnect with the backoff sequence [2000, 4000, 6000, 8000, 8000] ms; after 5 failed attempts it stops (matches patch #148 window)
    - Test 5: On successful reconnect, the attempt counter resets; a subsequent close starts backoff from 2000ms again
    - Test 6: While disconnected, publishSessionState calls are buffered in a bounded queue (max 100 entries; oldest dropped with a `queue_overflow` warn log); on reconnect the queue is flushed
    - Test 7: Every WS lifecycle transition (opening, open, close-with-code, error, retry-scheduled, retry-fired, giving-up) logs a JSON line via watcherLogger with `operation: 'skynet_ws_<transition>'` + `hostname` + `attempt` (when applicable)
    - Test 8: index.ts is refactored to construct createSkynetWsClient using `process.env.SKYNET_WS_URL` (required; throws on missing) and `os.hostname()`, wires session-json-watcher's onSessionState → client.publishSessionState and onSessionGone → client.publishSessionGone; NO stdout writes remain
  </behavior>
  <action>
    Create `src/fleet-status-watcher/src/skynet-ws-client.ts` exporting `createSkynetWsClient({ url, hostname }): SkynetWsClient`. Interface: `{ publishSessionState(state: SessionState): void; publishSessionGone(tmuxSession: string | null, sessionId: string): void; close(): void; }`. Uses the `ws` npm package client (`import WebSocket from 'ws'`). On construction, calls `connect()` which opens `new WebSocket(url)` and: `onopen` → send hello, flush buffered messages, reset attempt counter; `onclose(code, reason)` → log + schedule reconnect via `setTimeout(connect, backoff[attempt])` (backoff = [2000, 4000, 6000, 8000, 8000]) unless attempt ≥ 5 (log `operation: 'skynet_ws_gave_up'` and stop); `onerror(err)` → log with err.message ONLY (never the raw event). Buffer: `Array<WatcherInboundFrame>` bounded at 100; on overflow drop oldest + log `operation: 'skynet_ws_queue_overflow'`. publishSessionState / publishSessionGone: if `ws.readyState === WebSocket.OPEN` send immediately; else push to buffer. `close()` clears any pending retry timer and closes the socket. Refactor `src/fleet-status-watcher/src/index.ts`: at startup, read `process.env.SKYNET_WS_URL` (throw with a clear error if unset — no silent fallback); construct `createSkynetWsClient({ url, hostname: os.hostname() })`; hook the session-json-watcher's callbacks to client.publish*; remove all stdout `process.stdout.write` calls that Plan 01 emitted (they were placeholders for this refactor). Preserve every existing structured log line; add the new WS-lifecycle logs from skynet-ws-client.
  </action>
  <verify>
    <automated>cd src/fleet-status-watcher && npx vitest run src/skynet-ws-client.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test 1 fixture uses a WSS mock server; client sends exactly one hello frame within 100ms of open; hello schemaVersion === 1 and hostname matches the passed value
    - Test 4 fixture: mock server closes the connection five times in a row; captured retry timings match [2000, 4000, 6000, 8000, 8000] within ±100ms tolerance
    - Test 6 fixture: while disconnected, publish 150 states; capture buffer length caps at 100; the first 50 are dropped; log stream contains at least one `operation: 'skynet_ws_queue_overflow'` line
    - Test 8: `grep -c 'process.stdout.write' src/fleet-status-watcher/src/index.ts` returns 0
    - `grep -c 'JSON.stringify(event)' src/fleet-status-watcher/src/skynet-ws-client.ts` returns 0
    - All 8 tests exit 0
  </acceptance_criteria>
  <done>Watcher now publishes to Skynet over WS with retry-and-backoff matching patch #148. index.ts wires the transport into the Plan 01 pipeline. All watcher tests still green.</done>
</task>

<task type="auto">
  <name>Task 2: systemd user unit + Stop-hook script + install/uninstall scripts + dynamic host discovery + nginx blocks</name>
  <files>src/fleet-status-watcher/deploy/fleet-status-watcher.service, src/fleet-status-watcher/deploy/stop-hook.sh, src/fleet-status-watcher/deploy/install.sh, src/fleet-status-watcher/deploy/discover-identity-hosts.sh, src/fleet-status-watcher/deploy/README.md, docker/nginx.conf, docker/nginx-https.conf</files>
  <read_first>
    - src/fleet-status-watcher/deploy/ (empty — this task creates the directory)
    - .planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-RESEARCH.md (§ Research Target 4 — systemd user unit rationale, watcher update mechanism, log destination)
    - .planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-RESEARCH.md (§ Research Target 2 — Stop hook registration schema in ~/.claude/settings.json)
    - docker/nginx.conf + docker/nginx-https.conf (look at any existing per-pane WS location block, e.g. for port 30002 terminal or 30011 claude-session — copy the WebSocket upgrade header idiom verbatim)
    - ~/.claude/roles/box-maintainer/box-map.md (managed box inventory the discovery script iterates over)
  </read_first>
  <action>
    Create `src/fleet-status-watcher/deploy/fleet-status-watcher.service` — systemd user unit: `[Unit] Description=Skynet fleet-status watcher / After=network-online.target` ; `[Service] Type=simple / Environment=SKYNET_WS_URL=%h/... / ExecStart=/usr/bin/node %h/.local/lib/fleet-status-watcher/dist/index.js / Restart=always / RestartSec=5s / StandardOutput=append:%h/.local/var/log/fleet-status-watcher.log / StandardError=append:%h/.local/var/log/fleet-status-watcher.log` ; `[Install] WantedBy=default.target`. Use `%h` for `$HOME` expansion (systemd-specific). Create `src/fleet-status-watcher/deploy/stop-hook.sh`: `#!/bin/bash / set -eu / SOCKET="/tmp/fleet-status-hook-$(id -u).sock" / PAYLOAD=$(cat) / LEN=$(printf '%s' "$PAYLOAD" | wc -c) / # 4-byte big-endian length prefix + payload; fire-and-forget with 5ms timeout so we never block Claude Code / { printf '%b' "$(printf '\\\\x%02x' $(((LEN >> 24) & 0xff)) $(((LEN >> 16) & 0xff)) $(((LEN >> 8) & 0xff)) $((LEN & 0xff)))"; printf '%s' "$PAYLOAD"; } | timeout 0.005 nc -U -q 0 "$SOCKET" 2>/dev/null || true / exit 0`. Create `src/fleet-status-watcher/deploy/discover-identity-hosts.sh`: reads `~/.claude/roles/box-maintainer/box-map.md`, extracts every `[[boxes]]` entry's `hostname`, SSHes to each with `ssh -o ConnectTimeout=5 <host> 'ls ~/.claude/sessions/*.json 2>/dev/null | wc -l'`, prints hostnames where the count is > 0 to stdout (one per line). Any host that fails to connect prints a WARN to stderr with the hostname + reason and is SKIPPED (not treated as identity-hosting). Create `src/fleet-status-watcher/deploy/install.sh` accepting `--host <hostname>` OR `--all` (which pipes discover-identity-hosts.sh into a loop). Per host: (a) `cd src/fleet-status-watcher && npm run build`; (b) `rsync -a dist/ node_modules/ package.json <host>:.local/lib/fleet-status-watcher/`; (c) scp `deploy/fleet-status-watcher.service` → `<host>:.config/systemd/user/`; (d) scp `deploy/stop-hook.sh` → `<host>:.local/bin/` + `chmod +x`; (e) SSH: run a small Node one-liner (or `jq`-based script) that reads `~/.claude/settings.json`, merges a new entry into `hooks.Stop` pointing at `~/.local/bin/stop-hook.sh` (idempotent — check if the entry already exists before appending); (f) SSH: `systemctl --user daemon-reload && systemctl --user enable --now fleet-status-watcher`; (g) SSH: `systemctl --user status fleet-status-watcher --no-pager` → assert `active (running)`. Include a `--uninstall` mode that stops the service, disables it, removes the hook entry, and rms the installed files. Modify `docker/nginx.conf` and `docker/nginx-https.conf`: add a new `location /fleet-status/ws { proxy_pass http://skynet:30012; proxy_http_version 1.1; proxy_set_header Upgrade $http_upgrade; proxy_set_header Connection "upgrade"; proxy_set_header Host $host; proxy_read_timeout 3600; proxy_send_timeout 3600; }` block. Position it near the existing WS location blocks (grep for `proxy_pass.*30002` or `proxy_pass.*30011` to find the right neighborhood). NOTE the `/fleet-status/watcher` path is NOT exposed via nginx — watchers connect over Tailscale directly to the backend port. Write `deploy/README.md` documenting: install command, update command (`install.sh --host <name>` re-uses the same script — idempotent), uninstall command, log locations (per-host `~/.local/var/log/fleet-status-watcher.log` + `journalctl --user -u fleet-status-watcher`), how to verify a box is reporting (backend log stream contains `operation: 'fleet_status_watcher_hello'` for that hostId), and how the orchestrator (tina) is expected to run this after Plan 06 lands.
  </action>
  <verify>
    <automated>bash -n src/fleet-status-watcher/deploy/install.sh && bash -n src/fleet-status-watcher/deploy/stop-hook.sh && bash -n src/fleet-status-watcher/deploy/discover-identity-hosts.sh && grep -c 'location /fleet-status/ws' docker/nginx.conf docker/nginx-https.conf</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n` (shell syntax check) exits 0 for all three shell scripts
    - `grep -c 'location /fleet-status/ws' docker/nginx.conf` returns 1
    - `grep -c 'location /fleet-status/ws' docker/nginx-https.conf` returns 1
    - Both nginx blocks include `proxy_set_header Upgrade $http_upgrade;` and `proxy_set_header Connection "upgrade";` (WS upgrade headers verified via grep)
    - `grep -c 'proxy_pass http://skynet:30012' docker/nginx.conf docker/nginx-https.conf` returns 2
    - `grep -c '\-\-uninstall' src/fleet-status-watcher/deploy/install.sh` returns ≥ 1 (uninstall mode present)
    - `grep -c 'discover-identity-hosts.sh' src/fleet-status-watcher/deploy/install.sh` returns ≥ 1 (dynamic host discovery wired in, not a static list)
    - `deploy/README.md` exists and mentions "install", "update", "uninstall", "log", "verify" as section headings
  </acceptance_criteria>
  <done>All install artifacts drafted + syntax-clean; nginx blocks added to BOTH configs per PROJECT.md constraint; discover script handles OQ-1 (ZoeyBattlestation) + OQ-4 (ashley-beelink) dynamically at deploy time rather than at plan time.</done>
</task>

<task type="auto">
  <name>Task 3: Monitor-payload verification script — satisfies RESEARCH § OQ-2 before shipping the ambient filter</name>
  <files>src/fleet-status-watcher/deploy/monitor-payload-verify.sh</files>
  <read_first>
    - .planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-RESEARCH.md (§ Open Questions § OQ-2 — the exact contract that needs live verification)
    - .planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-RESEARCH.md (§ Research Target 2 — hook registration schema)
    - src/fleet-status-watcher/deploy/stop-hook.sh (Task 2 — the production hook; the verification script installs a DIFFERENT hook that dumps stdin to a file for inspection)
  </read_first>
  <action>
    Create `src/fleet-status-watcher/deploy/monitor-payload-verify.sh`. Purpose: dump one real Stop payload from a scratch Claude session that has just run a Monitor tool call, so the executor can visually confirm the `background_tasks[]` shape carries `type: "monitor"` + `description` + `server` + `tool` per RESEARCH § 1 field table. Script flow: (a) create `/tmp/monitor-payload-verify.$$.log` — the capture file; (b) write a temporary Stop hook shim to `/tmp/monitor-payload-hook.$$.sh` that does `cat > /tmp/monitor-payload-verify.$$.log; exit 0`; (c) modify `~/.claude/settings.json` to append this hook (BACKUP the original to `~/.claude/settings.json.pre-monitor-verify.$$` first); (d) print instructions to stdout for the operator: "In a scratch Claude Code session, run: /use Monitor description='[ambient] test monitor for verification' command='sleep 300' — then any interaction to trigger a Stop event"; (e) wait for the log file to be non-empty (poll every 1s, max 5 min); (f) restore the original settings.json from backup, remove the temp hook file; (g) print the captured payload with `jq '.background_tasks[] | select(.type == "monitor")'` — the visible output MUST include the description field with the `[ambient]` prefix, plus the type, id, status, server (if any), and tool (if any) fields. If NO monitor entry is found in the payload, exit 2 with a WARN message pointing at the raw log file. The script is idempotent + always restores the settings.json backup even on interrupt (`trap` on EXIT/INT/TERM).
  </action>
  <verify>
    <automated>bash -n src/fleet-status-watcher/deploy/monitor-payload-verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n src/fleet-status-watcher/deploy/monitor-payload-verify.sh` exits 0
    - Script contains a `trap.*EXIT` line to guarantee settings.json restore
    - Script uses `jq` to filter the `background_tasks[]` to `type == "monitor"` entries
    - Script backs up `~/.claude/settings.json` to a `.pre-monitor-verify.<pid>` file before mutating
    - Script explicitly instructs the operator to launch the test Monitor with description prefix `[ambient]` so the verification exercises the exact production filter target
  </acceptance_criteria>
  <done>Verification script ready to run; RESEARCH § OQ-2 can be closed by the orchestrator invoking this script against a scratch identity before the ambient filter ships in Plan 06.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verify — deploy watcher to real fleet + close OQ-1 / OQ-2 / OQ-4</name>
  <files>src/fleet-status-watcher/deploy/, docker/nginx.conf, docker/nginx-https.conf (verification-only — nothing modified in this task)</files>
  <read_first>
    - src/fleet-status-watcher/deploy/README.md (Task 2 runbook)
    - src/fleet-status-watcher/deploy/discover-identity-hosts.sh (Task 2)
    - src/fleet-status-watcher/deploy/monitor-payload-verify.sh (Task 3)
    - src/backend/fleet-status/fleet-status-server.ts (Plan 02 — the target the watchers connect to)
  </read_first>
  <action>Manual verification only — the orchestrator (tina) runs the discover, monitor-verify, and install scripts against the real fleet per the how-to-verify steps below. This task produces zero code diffs. If Monitor payload shape differs from RESEARCH § 1 expectations OR if any identity-hosting box fails to come online, STOP and revise Plan 06 before scheduling.</action>
  <verify>
    <human-check>Follow the numbered steps in how-to-verify below; all seven must pass.</human-check>
  </verify>
  <done>Every identity-hosting box (as enumerated dynamically by discover-identity-hosts.sh) shows an active fleet-status-watcher; backend log stream contains one hello per box within 5s of install; Monitor-payload verification confirmed the [ambient] description prefix propagates through the harness Stop payload as RESEARCH § 1 predicted.</done>
  <what-built>
    - Watcher WS client with hello handshake + retry backoff wired into the entrypoint (Task 1)
    - systemd user unit template + Stop-hook script + install/uninstall/discover scripts + nginx location blocks in both configs (Task 2)
    - Monitor-payload verification script (Task 3) — satisfies OQ-2 before Plan 06 ships the ambient filter
    - Everything is drafted + syntax-clean, but NO real deployment has happened yet. This checkpoint is where the orchestrator (tina) picks up the deploy motion.
  </what-built>
  <how-to-verify>
    1. **Sanity check the build**: `cd src/fleet-status-watcher && npm install && npm run build` — exit 0, dist/ populated.
    2. **Run the dynamic host discovery** to confirm which boxes will receive the watcher: `bash src/fleet-status-watcher/deploy/discover-identity-hosts.sh` — should print at least: `skynet-ec2`, `thenasty`, `workstation`. May or may not print `ZoeyBattlestation` (OQ-1) or `ashley-beelink` (OQ-4) depending on whether they currently host identities. Note the actual list — it is the ground truth for the install run.
    3. **Run the Monitor-payload verification (OQ-2)**: `bash src/fleet-status-watcher/deploy/monitor-payload-verify.sh` — follow its printed instructions in a scratch Claude session, confirm the captured Monitor entry has `type: "monitor"` AND a non-empty `description` field starting with `[ambient]`. If the payload shows a DIFFERENT shape than what RESEARCH § 1 expects, STOP and revise Plan 06 before flipping the filter.
    4. **Install to one canary box first** (thenasty is the standard canary per fleet convention): `bash src/fleet-status-watcher/deploy/install.sh --host thenasty`. Confirm `systemctl --user status fleet-status-watcher` reports `active (running)`. Tail the backend log (`docker compose logs -f skynet | grep fleet_status`) and confirm you see `operation: 'fleet_status_watcher_hello'` for hostId `thenasty` within 5 seconds.
    5. **Verify SessionState + session_gone frames flow end-to-end**: still tailing the backend log, kill and restart a Claude session on thenasty and confirm you see `operation: 'fleet_status_session_state'` frames with the new sessionId + the correct status transitions.
    6. **Install to remaining boxes**: `bash src/fleet-status-watcher/deploy/install.sh --all` — verify each box comes online in the backend log stream.
    7. **Verify nginx configs deployed**: `docker exec skynet nginx -t` returns 0; browser can hit `wss://term.gigaashley.click/fleet-status/ws` with a valid JWT cookie and receive a snapshot frame (raw ws-client one-shot test acceptable).
  </how-to-verify>
  <resume-signal>Type "approved" once every identity-hosting box shows an active fleet-status-watcher AND the backend log stream contains one hello per box AND the Monitor-payload verification (OQ-2) confirmed the expected shape. If ANY of these fail, describe the failure — the plan may need revision (particularly if OQ-2 reveals a different Monitor payload shape than RESEARCH assumed, which would force a change in Plan 06's filter logic).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Watcher → Skynet backend WS | Tailscale-only network path; no JWT on watcher connection (matches D-CTX watcher topology decision — box-side trust) |
| install.sh → target box via SSH | Uses existing SSH keys under Ashley's control; no new credential vector |
| Stop-hook script writes to Unix socket | Local-uid-scoped socket; write is fire-and-forget with 5ms timeout so a runaway watcher cannot block Claude Code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-13 | Spoofing | Watcher WS to backend | mitigate | Backend gate is `resolveHostRecordByName` (Plan 02) — an unknown hostname is closed with 1008; the watcher's `os.hostname()` must match a Skynet host record |
| T-34-14 | Tampering | install.sh idempotency | mitigate | Merger for ~/.claude/settings.json Stop hook entry checks for existing entry with the same command path before appending; backup created before mutation |
| T-34-15 | Denial of Service | Stop-hook blocking Claude Code | mitigate | `timeout 0.005 nc -U -q 0` — 5ms cap, fire-and-forget; hook script exits 0 unconditionally |
| T-34-16 | Tampering | nginx.conf edits | mitigate | Changes are additive (new location block); existing blocks untouched; both configs edited symmetrically per PROJECT.md rule |
| T-34-SC | Tampering | Fresh npm installs on target boxes | mitigate | `rsync -a node_modules/` from the build box — no `npm install` on the target box, so no supply-chain change vector at deploy time |
</threat_model>

<verification>
- `cd src/fleet-status-watcher && npm run build && npx vitest run` exits 0
- `grep -c 'location /fleet-status/ws' docker/nginx.conf docker/nginx-https.conf` returns exactly 2 (one per file)
- `bash -n src/fleet-status-watcher/deploy/*.sh` exits 0 for every script
- Checkpoint approval received before phase moves to Plan 05
</verification>

<success_criteria>
- Watcher publishes to Skynet backend over WS with retry-and-backoff mirroring patch #148
- Deploy artifacts (systemd unit, install/uninstall/discover scripts, Stop-hook, nginx blocks) are all in place
- OQ-1 (ZoeyBattlestation) and OQ-4 (ashley-beelink) resolved via dynamic host discovery at deploy time
- OQ-2 (Monitor payload shape) resolved via live verification script BEFORE Plan 06 ships the ambient filter
- Human checkpoint confirms watchers active on all identity-hosting boxes AND OQ-2 payload matches expected shape
</success_criteria>

<output>
Create `.planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-04-SUMMARY.md` when done. MUST document: (a) the SKYNET_WS_URL that watchers use (Tailscale-facing address of the backend), (b) the actual list of identity-hosting hosts discovered at deploy time (baseline for future re-installs), (c) the OQ-2 verification result (payload shape observed — confirms or forces a Plan 06 filter revision), (d) which nginx blocks were added and where in the config, (e) the install/update runbook step for the orchestrator to reference when a watcher needs updating post-phase.
</output>
