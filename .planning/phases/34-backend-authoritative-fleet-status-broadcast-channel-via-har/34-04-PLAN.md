---
phase: 34-backend-authoritative-fleet-status-broadcast-channel-via-har
plan: 04
type: execute
wave: 2
depends_on: [34-01, 34-02]
files_modified:
  - src/backend/fleet-status/ssh-poll-orchestrator.ts
  - src/backend/fleet-status/ssh-poll-orchestrator.test.ts
  - src/backend/fleet-status/remote-hook-install.ts
  - src/backend/fleet-status/remote-hook-install.test.ts
  - src/backend/fleet-status/stop-hook.sh
  - src/backend/fleet-status/scripts/verify-monitor-payload.sh
  - src/backend/fleet-status/scripts/README.md
  - src/backend/starter.ts
autonomous: false
requirements: []
user_setup:
  - service: id-skill-ambient-tagging
    why: "Plan 05 (external, orchestrator-managed) MUST land the [ambient] description-prefix tagging in the id-skill on-wake block BEFORE Plan 06 is scheduled. This plan (04) also depends on running Monitor-payload verification (Task 4) BEFORE the ambient filter is shipped in Plan 06 — the verify script needs at least one Monitor tool call captured in a real Stop payload so we can confirm the RESEARCH § 1 field-table expectation."
    dashboard_config:
      - task: "Coordinate with Nellie on fleet-agent-supervisor parallel Monitor launch path (if any) — same [ambient] prefix convention. Orchestrator (tina) drives the id-skill edit."
        location: "~/.claude/roles/box-maintainer/bounties/ambient-monitor-tagging-in-id-skill/bounty.json"
must_haves:
  truths:
    - "ssh-poll-orchestrator opens exactly one multiplexed SSH channel per identity-hosting host (via the existing Skynet backend SSH pool primitives — `connectOneShot` from `src/backend/ssh/ssh-one-shot.ts` OR `withConnection` from `src/backend/ssh/ssh-connection-pool.ts`, whichever fits the polling topology) and reuses that channel for the 2s poll cycle rather than opening a new connection per poll"
    - "The 2s poll cycle for each identity-hosting host: (a) list `~/.claude/sessions/*.json` over SSH, (b) for each PID cat the session-JSON file + `/proc/<pid>/stat` + `/proc/<pid>/environ` + the Stop-hook payload file at a well-known path — all in a single multiplexed exec batch where possible, (c) parse each blob via the Plan 01 pure-library modules, (d) compose a SessionState and publish it via the Plan 02 subscription registry (state deltas — publish only when the composed state differs from the last-known state for that key, to avoid registry fan-out churn)"
    - "The identity-host list comes from the Skynet backend's own DB — the orchestrator queries the `hosts` (`ssh_data`) table joined against the `identities` table (or via whatever route already exposes the identity-hosting host list; grep `src/backend/database/routes/` for the closest existing consumer) — NO static host list, NO parsing `~/.claude/roles/box-maintainer/box-map.md`, NO baking hostnames into config"
    - "Stale detection: for each polled PID, `isStaleFromStat(sessionJson.procStart, statContents)` (Plan 01) — on stale, publish `session_gone` via the registry and drop the PID from the orchestrator's local liveness map. Also do a 30s periodic sweep across all tracked PIDs (matches RESEARCH § 3 recommendation) to catch PIDs that stop appearing in the directory listing between polls"
    - "PID→tmuxSession correlation: for each new PID (first-sighting during a poll), call `resolvePidToTmuxSession(pid, { readEnviron, resolveTmuxName })` from Plan 01, where `readEnviron` is `execCommand(conn, 'cat /proc/<pid>/environ')` and `resolveTmuxName` is `execCommand(conn, \"tmux display-message -p -t '<pane>' '#{session_name}'\")` — pane is regex-validated by Plan 01's `isValidTmuxPaneId` before the tmux exec, so shell-metacharacter injection into the exec is impossible even under a compromised /proc read. Cache the (pid → tmuxSession) mapping in a per-host Map for the life of the PID"
    - "Ambient filter: `filterAmbientTasks(payload.background_tasks)` (Plan 01) runs BEFORE the SessionState is composed, so the frontend never sees ambient Monitors. Prior to Plan 05 landing, the persistent Monitors will NOT carry the `[ambient]` prefix and this filter will be effectively a no-op — the frontend cutover (Plan 06) is explicitly gated behind Plan 05 to prevent the resulting `isWorking=true` regression"
    - "Fail-open on missing hook payload file (Ashley 2026-08-13 LOCKED constraint): when polling the well-known Stop-hook payload file on a given host returns ENOENT / empty / malformed JSON / any SSH-side read error, the orchestrator MUST NOT crash, MUST NOT stop polling that host, MUST NOT mark the session as broken. It logs ONE warn line for that host (rate-limited to at most one line per host per 60s — NOT one per 2s poll) and treats the host's `background_tasks[]` as empty for that poll cycle. The session-JSON-derived state continues to publish normally. The dot may under-report background work on that host until the hook is (re-)installed, but the app stays functional"
    - "remote-hook-install.ts is a one-time-per-host install helper (NOT a persistent process): it (a) drops `stop-hook.sh` into a well-known path on the target host over SSH (`~/.claude/hooks/skynet-fleet-status-stop.sh` — inside `~/.claude/` so it survives home-dir migrations and lives alongside `settings.json`), (b) SSH-reads the current `~/.claude/settings.json`, mutates in-memory to append a `hooks.Stop[0].hooks[]` entry pointing at the well-known script path (idempotent — checks for existing entry with the same command path BEFORE appending), (c) SSH-writes the mutated settings.json back atomically (`mv new old` on the remote side after cat > new; verify write succeeded before removing backup). Handles the case where settings.json does not exist yet by creating a fresh minimal file with just the hook entry"
    - "stop-hook.sh script (dropped onto each identity-hosting box) reads stdin (the Stop hook payload) and writes it to the well-known local payload file at `~/.claude/fleet-status/last-stop-payload.json` atomically (`cat > .tmp && mv .tmp final`), exits 0 immediately — MUST NOT block Claude Code. Wraps the write in a subshell with a short timeout as belt-and-braces so a full disk cannot hang the hook indefinitely"
    - "verify-monitor-payload.sh (scripts/) is an orchestrator-run tool (NOT an executor test): the orchestrator SSHes to a scratch identity, ensures the Stop hook is installed there, launches a Monitor from that identity's Claude Code session, triggers a Stop, cats the resulting `~/.claude/fleet-status/last-stop-payload.json`, and prints the parsed `background_tasks[]` entries so the orchestrator can eyeball that a `type: 'monitor'` entry actually appears with the expected fields. Closes RESEARCH § OQ-2. Runs BEFORE Plan 06 is scheduled — this is documented in the plan's <verification> as an orchestrator-run step, not an executor test"
    - "starter.ts wires the orchestrator: when the Skynet backend boots, after the Plan 02 `startFleetStatusServer` call, spawn the ssh-poll-orchestrator with a reference to the same subscription-registry the WS server holds. Orchestrator queries the identity-host list from the DB, opens one SSH channel per host, kicks off the 2s poll loop, publishes state deltas into the registry (which fans them out to any connected frontend WS clients from Plan 06)"
    - "Every WS lifecycle boundary + orchestrator poll cycle + hook install step logs a single structured JSON line via `systemLogger` with explicit fields (`operation`, `hostId`, `pid`, `sessionId` when known, `err.message` when applicable) — never `JSON.stringify(err)`, never DOM Event objects"
  artifacts:
    - path: "src/backend/fleet-status/ssh-poll-orchestrator.ts"
      provides: "The 2s SSH-poll coordinator. Opens one multiplexed SSH channel per identity-hosting host (via existing Skynet backend SSH primitives); on each 2s tick, pulls session-JSON files + /proc/<pid>/stat + /proc/<pid>/environ + Stop-hook payload file for every session on the host; uses Plan 01's pure-library modules to parse and derive; publishes state deltas into the Plan 02 subscription registry. Handles fail-open on missing hook payload file, PID→tmux correlation with caching, and 30s stale sweep. Dependency-injected for testability: SSH pool + DB host-list query + registry + polling clock are all injected."
      exports: ["createSshPollOrchestrator", "SshPollOrchestrator", "OrchestratorDeps"]
      min_lines: 200
    - path: "src/backend/fleet-status/remote-hook-install.ts"
      provides: "One-time-per-host installer for the Stop hook. Drops stop-hook.sh at ~/.claude/hooks/skynet-fleet-status-stop.sh over SSH; atomically mutates ~/.claude/settings.json to append the hooks.Stop entry (idempotent — no-op on second run); handles missing settings.json by creating a minimal fresh file. Also exposes uninstallStopHook() for symmetry."
      exports: ["installStopHook", "uninstallStopHook", "readAndMergeStopHookSettings"]
    - path: "src/backend/fleet-status/stop-hook.sh"
      provides: "The actual bash Stop-hook script that is dropped onto each identity-hosting box. Reads stdin (the Stop payload JSON), writes atomically to ~/.claude/fleet-status/last-stop-payload.json, exits 0 unconditionally. Fire-and-forget; MUST NOT block Claude Code."
    - path: "src/backend/fleet-status/scripts/verify-monitor-payload.sh"
      provides: "Orchestrator-run one-shot verification: SSH to a scratch identity, ensure Stop hook is installed, print the parsed background_tasks[] entries from the last captured Stop payload so RESEARCH § OQ-2 can be closed by eye before Plan 06 ships the ambient filter."
    - path: "src/backend/fleet-status/scripts/README.md"
      provides: "Runbook — how the orchestrator (tina) runs verify-monitor-payload.sh, how remote-hook-install.ts is invoked (via a small `node -e` snippet or a wrapper script — see 'Orchestrator usage' section), how to interpret the outputs, how to (re-)install the hook on a new host, and the fail-open behavior expected when the hook payload file is missing."
    - path: "src/backend/starter.ts"
      provides: "Adds one call to `createSshPollOrchestrator` after the existing `startFleetStatusServer` call, passing the same subscription-registry instance. Nothing else changes in starter.ts."
  key_links:
    - from: "src/backend/fleet-status/ssh-poll-orchestrator.ts"
      to: "src/backend/fleet-status/types.ts (Plan 01)"
      via: "orchestrator imports parseSessionJson + parseStopHookPayload to validate SSH-pulled bytes before use"
      pattern: "parseSessionJson|parseStopHookPayload"
    - from: "src/backend/fleet-status/ssh-poll-orchestrator.ts"
      to: "src/backend/fleet-status/liveness-check.ts (Plan 01)"
      via: "orchestrator calls isStaleFromStat(sessionJson.procStart, statContentsFromSsh) on every polled PID; on stale publishes session_gone via the registry"
      pattern: "isStaleFromStat"
    - from: "src/backend/fleet-status/ssh-poll-orchestrator.ts"
      to: "src/backend/fleet-status/pid-to-tmux.ts (Plan 01)"
      via: "orchestrator constructs deps={readEnviron: pid => execCommand(conn, `cat /proc/${pid}/environ`), resolveTmuxName: pane => execCommand(conn, `tmux display-message -p -t '${pane}' '#{session_name}'`)} and calls resolvePidToTmuxSession(pid, deps); result cached in a per-host Map<pid, tmuxSession>"
      pattern: "resolvePidToTmuxSession"
    - from: "src/backend/fleet-status/ssh-poll-orchestrator.ts"
      to: "src/backend/fleet-status/ambient-filter.ts (Plan 01)"
      via: "orchestrator calls filterAmbientTasks(payload.background_tasks) after parseStopHookPayload succeeds, then attaches the filtered array to the composed SessionState"
      pattern: "filterAmbientTasks"
    - from: "src/backend/fleet-status/ssh-poll-orchestrator.ts"
      to: "src/backend/fleet-status/subscription-registry.ts (Plan 02)"
      via: "orchestrator publishes state deltas via `registry.publishSessionState(hostId, state)` and `registry.publishSessionGone(hostId, tmuxSession, sessionId)` — same registry instance that the fleet-status-server holds so frontend clients see the fan-out"
      pattern: "registry\\.(publishSessionState|publishSessionGone)"
    - from: "src/backend/fleet-status/ssh-poll-orchestrator.ts"
      to: "src/backend/fleet-status/host-id-resolver.ts (Plan 02)"
      via: "orchestrator uses the resolved Skynet host id (from the DB query it does at the top of every poll batch) as the hostId field on published SessionStates — MUST match the id resolveHostRecordByName returns so keys align"
      pattern: "HostRecord|hostId"
    - from: "src/backend/fleet-status/remote-hook-install.ts"
      to: "src/backend/fleet-status/stop-hook.sh"
      via: "installStopHook reads the local stop-hook.sh from the Skynet backend filesystem, SFTPs/scps it via the ssh2 client to ~/.claude/hooks/skynet-fleet-status-stop.sh on the remote box, chmod +x, then merges the settings.json entry pointing at that path"
      pattern: "stop-hook\\.sh|~/\\.claude/hooks"
    - from: "src/backend/starter.ts"
      to: "src/backend/fleet-status/ssh-poll-orchestrator.ts"
      via: "starter.ts constructs createSshPollOrchestrator({ registry, dbQueryIdentityHosts, sshPool, pollIntervalMs: 2000, staleSweepIntervalMs: 30000, hookPayloadPath: '~/.claude/fleet-status/last-stop-payload.json' }) and calls orchestrator.start() after startFleetStatusServer"
      pattern: "createSshPollOrchestrator\\("
---

<objective>
Wire the pure-library modules from Plan 01 into a live, backend-hosted SSH-poll orchestrator that pulls session-JSON files and Stop-hook payloads from every identity-hosting box every 2 seconds, composes SessionState updates, and publishes them via the Plan 02 subscription registry — closing the loop from "harness writes a status file on the box" to "backend broadcasts SessionState to any subscribed frontend." Also ship the remote Stop-hook install helper + the actual hook script + a one-shot verify script for RESEARCH § OQ-2.

Per D-CTX § PIVOT 2026-08-13 (LOCKED): NO per-box daemon, NO systemd user unit, NO install script for a persistent process. The watcher IS the Skynet backend. Delivery is 2s SSH polling over the existing SSH pool. The Stop hook is a one-time remote file drop + settings-file edit, NOT a persistent process. And fail-open on missing hook payload is a first-class LOCKED constraint (Ashley 2026-08-13 verbatim: *"just make sure that it fails open if the file that the hook is supposed to generate isn't found"*).

Purpose: This is where Plans 01 + 02 fuse into a working pipe. After Task 5 (checkpoint) approves, every identity-hosting box is being polled every 2s and the backend's subscription registry is holding a live snapshot of the fleet's session states. The frontend cutover (Plan 06) then flips consumption over to this new signal.

Output: One orchestrator module + one remote-hook-install module + the actual hook script + a verify script + a scripts README + a two-line addition to starter.ts. Everything lives under `src/backend/fleet-status/` (except the starter.ts edit). No per-box daemon anywhere. No systemd. No standalone subpackage.

Non-goal: This plan does NOT modify the frontend (that is Plan 06). Does NOT retire the old feeders (Plan 06). Does NOT touch per-pane WS lifecycle. Does NOT modify Plan 02's WS server, subscription registry, wire protocol, or host-id resolver. Does NOT modify any Plan 01 module. Does NOT deploy anything to production (the install helper is a library — the orchestrator invokes it on a per-host basis when the host first appears in the identity-host list, but the Task 5 checkpoint gates on the orchestrator having successfully verified against a scratch identity, not a full fleet rollout).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-CONTEXT.md
@.planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-RESEARCH.md
@.planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-01-SUMMARY.md
@.planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-02-SUMMARY.md
@src/backend/fleet-status/wire-protocol.ts
@src/backend/fleet-status/subscription-registry.ts
@src/backend/fleet-status/fleet-status-server.ts
@src/backend/fleet-status/host-id-resolver.ts
@src/backend/fleet-status/types.ts
@src/backend/fleet-status/liveness-check.ts
@src/backend/fleet-status/pid-to-tmux.ts
@src/backend/fleet-status/ambient-filter.ts
@src/backend/ssh/ssh-connection-pool.ts
@src/backend/ssh/ssh-one-shot.ts
@src/backend/ssh/tmux-helper.ts
@src/backend/claude-session/session-file-discovery.ts
@src/backend/starter.ts
@src/backend/database/db/schema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ssh-poll-orchestrator.ts — 2s poll loop, one multiplexed SSH channel per host, state-delta publish, 30s stale sweep, PID→tmux caching</name>
  <files>src/backend/fleet-status/ssh-poll-orchestrator.ts, src/backend/fleet-status/ssh-poll-orchestrator.test.ts</files>
  <read_first>
    - src/backend/ssh/ssh-connection-pool.ts (`withConnection` + `connectionPool` — how the Skynet backend already reuses SSH connections; the orchestrator SHOULD use `withConnection(hostKey, factory, useFn)` OR keep its own long-lived Client per host — pick whichever fits a 2s poll cadence best after reading the pool code; document the choice in the module docblock)
    - src/backend/ssh/ssh-one-shot.ts (`connectOneShot(host, timeoutMs)` — the factory pattern the pool consumes; the orchestrator can call this directly for its per-host long-lived channel if the pool's per-request lifecycle doesn't fit a 2s cadence)
    - src/backend/ssh/tmux-helper.ts (`execCommand(conn, command)` — the primitive the orchestrator uses to `cat` files on the remote box; also `queryPanePid` as a reference for how to wrap execCommand for a specific query)
    - src/backend/claude-session/session-file-discovery.ts (correlation primitive — same SSH-exec pattern; do NOT reuse `discoverClaudeSession` directly because it goes tmux→PID and we go PID→tmux, but the SSH-usage idiom is the reference)
    - src/backend/fleet-status/types.ts + src/backend/fleet-status/liveness-check.ts + src/backend/fleet-status/pid-to-tmux.ts + src/backend/fleet-status/ambient-filter.ts (Plan 01 — all four are consumed by this orchestrator)
    - src/backend/fleet-status/subscription-registry.ts (Plan 02 — `publishSessionState(hostId, state)` + `publishSessionGone(hostId, tmuxSession, sessionId)` are the two publish surfaces the orchestrator drives)
    - src/backend/fleet-status/host-id-resolver.ts (Plan 02 — reference for how the DB `hosts` table is queried; the orchestrator does its own similar query for the identity-host list)
    - src/backend/database/db/schema.ts (schema — `hosts` = `ssh_data` table, `identities` table, `messageQueueItems` table has the host_id foreign key + tmux_session column that's the closest existing "which hosts host identities" signal; the orchestrator queries this to build the poll list)
    - .planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-CONTEXT.md (§ PIVOT 2026-08-13 — LOCKED constraints, especially the fail-open rule)
    - .planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-RESEARCH.md (§ Research Target 3 — session-JSON schema + stale-file reap policy; § Research Target 5 — correlation chain)
  </read_first>
  <behavior>
    - Test 1: `createSshPollOrchestrator({ deps })` returns an orchestrator object with `.start()`, `.stop()`, `.getPollTickCount()` methods; start() does not begin polling until called; stop() halts the poll loop and clears all timers
    - Test 2: On start, the orchestrator queries `deps.listIdentityHostingHosts()` (injected async DB query) and iterates the returned host records; for each host it opens a channel via `deps.acquireSshChannel(hostRecord)` (injected — returns an object with a `.exec(command): Promise<string | null>` method modeling the SSH exec channel; returns null on SSH-side error)
    - Test 3: On each 2s poll tick (driven by injected `deps.setPollInterval` — orchestrator uses `deps.setInterval` internally so tests can inject fake timers), for each host: (a) `exec("ls -1 ~/.claude/sessions/*.json 2>/dev/null || true")` to enumerate PIDs, (b) for each PID `exec("cat ~/.claude/sessions/<pid>.json")` + `exec("cat /proc/<pid>/stat")` + (only for new PIDs) `exec("cat /proc/<pid>/environ")` (paired with the tmux-exec via `resolvePidToTmuxSession`), (c) `exec("cat ~/.claude/fleet-status/last-stop-payload.json 2>/dev/null || true")` for the Stop-hook payload
    - Test 4: For each successfully-polled PID: parse session-JSON via `parseSessionJson`; if null, WARN + skip. Call `isStaleFromStat(sessionJson.procStart, statContents)`; if stale, publish `session_gone` via the registry, drop from the per-host liveness Map. If NOT stale: parse Stop-hook payload via `parseStopHookPayload`; call `filterAmbientTasks` on `payload.background_tasks`; compose a SessionState with `{ hostId, tmuxSession (from cache or new resolvePidToTmuxSession), sessionId, pid, status, waitingFor: (status==='waiting'?sessionJson.waitingFor:undefined), backgroundTasks: filtered, updatedAt: sessionJson.updatedAt }`; if this composed state differs (via a shallow structural equality helper — compare status, waitingFor, backgroundTasks.length + each task's {id, status, description} triple, updatedAt) from the last-published state for this key, publish it via the registry; if equal, skip publish (delta semantics)
    - Test 5: Fail-open behavior — when the Stop-hook payload exec returns null (SSH read failed) OR empty string (file missing OR file was just created but not yet written) OR the string parses to non-JSON, the orchestrator: (a) treats `payload.background_tasks` as `[]` for that poll cycle, (b) still composes + publishes the SessionState with backgroundTasks=[] (so the session-JSON-derived status flows through), (c) logs at most ONE `systemLogger.warn` line per host per 60s with `operation: 'fleet_status_hook_payload_missing'` + hostId (asserted via `vi.useFakeTimers()` — advance the clock 30s and confirm no new warn line fires; advance to 65s and confirm one new warn line does)
    - Test 6: Fail-open — orchestrator does NOT throw, does NOT stop polling that host, does NOT publish `session_gone` for any session on the host — the missing hook file is a per-host WARN, not a per-session error
    - Test 7: 30s stale sweep — a periodic sweep (independently timed from the 2s poll) walks the per-host liveness Map and re-verifies each tracked PID by re-execing `cat /proc/<pid>/stat`; PIDs that go stale between polls (e.g. the ls -1 missed them because the session-JSON file was deleted mid-poll) are reaped and `session_gone` is published
    - Test 8: PID→tmuxSession cache — first sighting of a PID triggers `resolvePidToTmuxSession(pid, {readEnviron, resolveTmuxName})` where `readEnviron` = `exec("cat /proc/<pid>/environ")` and `resolveTmuxName` = `exec("tmux display-message -p -t '<pane>' '#{session_name}'")`; subsequent polls for the same PID reuse the cached tmuxSession (verified via mock exec-call count: for a PID polled 5 times, `cat /proc/<pid>/environ` is called exactly 1 time)
    - Test 9: When a PID is reaped (stale), its cache entry is cleared; if the same PID number is later reused for a different process, the orchestrator re-runs resolvePidToTmuxSession fresh
    - Test 10: `stop()` clears both timers (2s poll + 30s sweep), releases all per-host SSH channels via `deps.releaseSshChannel(hostRecord, channel)`, and does not publish anything after being called
    - Test 11: Structured logging — every poll cycle boundary (poll-start, poll-end with tick count, per-host-error, session-published, session-gone-published, stale-reap, hook-payload-missing rate-limited warn, sweep-run) logs a single JSON line via a mocked systemLogger with an explicit `operation` field; `grep -c 'JSON.stringify(err' src/backend/fleet-status/ssh-poll-orchestrator.ts` returns 0
    - Test 12: SSH connection failure for a specific host (`deps.acquireSshChannel` returns null OR throws) does NOT stop the orchestrator's poll loop for OTHER hosts; the failing host's poll is skipped for that cycle, logged as `operation: 'fleet_status_host_ssh_unreachable'`, and retried on the next tick
  </behavior>
  <action>
    Create `src/backend/fleet-status/ssh-poll-orchestrator.ts`. Docblock at the top explains: (a) the D-CTX § PIVOT 2026-08-13 mandate — no per-box daemon, 2s SSH poll over the existing pool, this module IS the watcher; (b) the fail-open rule for missing hook payload files — under-report is preferred to false-alarm; (c) the dependency-injection surface (all SSH, all DB, all timers injected so the module is unit-testable with `vi.useFakeTimers()` + `vi.fn()`).

    Interface `OrchestratorDeps`: `{ listIdentityHostingHosts(): Promise<HostRecord[]>; acquireSshChannel(host: HostRecord): Promise<SshChannel | null>; releaseSshChannel(host: HostRecord, channel: SshChannel): void; registry: SubscriptionRegistry; setInterval: (fn: () => void, ms: number) => ReturnType<typeof setInterval>; clearInterval: (h: ReturnType<typeof setInterval>) => void; now: () => number; pollIntervalMs?: number; staleSweepIntervalMs?: number; hookPayloadPath?: string; hookPayloadWarnCooldownMs?: number; }`. Defaults: `pollIntervalMs=2000`, `staleSweepIntervalMs=30000`, `hookPayloadPath='~/.claude/fleet-status/last-stop-payload.json'`, `hookPayloadWarnCooldownMs=60000`.

    Interface `SshChannel`: `{ exec(command: string): Promise<string | null> }` — a thin wrapper around the underlying ssh2 client that the concrete implementation (bound in starter.ts) provides via `execCommand(conn, cmd)` returning null on SSH-side error instead of throwing.

    Export `createSshPollOrchestrator(deps: OrchestratorDeps): SshPollOrchestrator` with `.start()`, `.stop()`, `.getPollTickCount()`. Internal state (module-scoped inside the closure returned by createSshPollOrchestrator):

    - `perHostState = Map<hostId, { host: HostRecord, channel: SshChannel, livenessMap: Map<pid, {sessionId, tmuxSession: string|null, procStart, lastPublishedFingerprint: string}>, lastHookWarnAt: number }>`
    - `pollTimer: ReturnType<typeof setInterval> | null`
    - `sweepTimer: ReturnType<typeof setInterval> | null`
    - `pollTickCount: number`
    - `stopped: boolean`

    `start()` flow: (1) query `listIdentityHostingHosts()` → for each host, acquire an SSH channel via `acquireSshChannel(host)`; on null, log `operation: 'fleet_status_host_ssh_unreachable'` + hostId and skip (the host will be retried on the next tick since the acquire happens per-tick — see below). (2) Set up `pollTimer = setInterval(() => pollAllHosts(), pollIntervalMs)`. (3) Set up `sweepTimer = setInterval(() => sweepAllHostsForStalePids(), staleSweepIntervalMs)`. (4) Fire an immediate first poll (do not wait 2s for the first data point).

    `pollAllHosts()` flow: increment tick count. Refresh the identity-host list (identities can appear/disappear at runtime — but be conservative: only re-query the DB every N ticks, e.g. every 30s worth of polls = 15 ticks, so we don't hammer the DB). For each known host: if we don't yet have an SSH channel, try acquireSshChannel(host); if still null, log + skip this tick. Then `pollOneHost(hostState)` — see below. Wrap the top level of pollAllHosts in try/catch so one host's failure never propagates and kills the timer callback.

    `pollOneHost(hostState)` flow: (a) `const listing = await hostState.channel.exec("ls -1 ~/.claude/sessions/*.json 2>/dev/null || true")` — parse into an array of PID numbers by matching `/(\d+)\.json$/` per line. (b) For each PID: parallel-launch `exec("cat ~/.claude/sessions/<pid>.json")` + `exec("cat /proc/<pid>/stat")` + `exec("cat <hookPayloadPath>")`; also, if PID is new to livenessMap OR PID's cached tmuxSession is null, `exec("cat /proc/<pid>/environ")` + `execCommand("tmux display-message ...")` via `resolvePidToTmuxSession` — but pass the pane check regex first via Plan 01's `isValidTmuxPaneId`. Await all three (or four) execs with `Promise.all`. (c) On the results: parse session-JSON via `parseSessionJson`; if null (parse failed OR file empty because we caught it mid-write) → skip this PID for this tick. Call `isStaleFromStat(sessionJson.procStart, statContents)`; if true → publish `session_gone` via registry with hostState.host.id + cached tmuxSession + sessionJson.sessionId, delete from livenessMap. If not stale: parse Stop-hook payload — if hook exec returned null/empty OR `parseStopHookPayload` returned null → treat `background_tasks` as `[]` AND emit the rate-limited hook-missing WARN if `now - hostState.lastHookWarnAt >= hookPayloadWarnCooldownMs` (then update `lastHookWarnAt`). Compose SessionState with `filterAmbientTasks(payload?.background_tasks ?? [])`. Compute a fingerprint (string like `${status}|${waitingFor??''}|${bgTasks.map(t=>t.id+t.status).join(',')}|${updatedAt}`); if fingerprint differs from `livenessMap.get(pid)?.lastPublishedFingerprint` → publish via `registry.publishSessionState(hostState.host.id, state)` + update `lastPublishedFingerprint`; if equal → skip publish (delta semantics — avoids fan-out churn).

    `sweepAllHostsForStalePids()` flow: for each known host, iterate its livenessMap PIDs and re-verify each via `exec("cat /proc/<pid>/stat")` → `isStaleFromStat`; reap stale entries with `session_gone` + delete + clear-cache. This catches PIDs that vanish between poll ticks (e.g. session-JSON file deleted mid-tick).

    `stop()`: set stopped=true, clearInterval(pollTimer + sweepTimer), for each hostState release the SSH channel, clear perHostState.

    Wire the module structure to allow the test to inject fake exec results via a `MockSshChannel` fixture whose `.exec(command)` returns a pre-programmed lookup table. Do NOT touch the real SSH primitives directly from this module — that binding happens in starter.ts (Task 4).

    Create `src/backend/fleet-status/ssh-poll-orchestrator.test.ts`. Mock systemLogger. Use `vi.useFakeTimers()` + `vi.advanceTimersByTime(...)` to drive the poll loop. Build a `MockSshChannel` class implementing `exec(command)` via a `Map<pattern, response>` fixture. Build a `MockRegistry` that captures publish calls into an array so assertions can inspect the sequence + count. Cover tests 1-12 above. Test 5 (fail-open) is the load-bearing one — assert both the "session-JSON continues to publish with backgroundTasks=[]" AND the rate-limited WARN behavior (advance timers, assert no new warn; advance past cooldown, assert one new warn).
  </action>
  <verify>
    <automated>npx vitest run src/backend/fleet-status/ssh-poll-orchestrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export function createSshPollOrchestrator' src/backend/fleet-status/ssh-poll-orchestrator.ts` returns 1
    - `grep -c 'filterAmbientTasks\|parseSessionJson\|parseStopHookPayload\|isStaleFromStat\|resolvePidToTmuxSession' src/backend/fleet-status/ssh-poll-orchestrator.ts` returns at least 5 (all Plan 01 primitives consumed)
    - `grep -c 'registry\.publishSessionState\|registry\.publishSessionGone' src/backend/fleet-status/ssh-poll-orchestrator.ts` returns at least 2 (Plan 02 registry driven)
    - `grep -c 'JSON.stringify(err\|JSON.stringify(event' src/backend/fleet-status/ssh-poll-orchestrator.ts` returns 0
    - `grep -c 'fleet_status_hook_payload_missing' src/backend/fleet-status/ssh-poll-orchestrator.ts` returns at least 1 (rate-limited warn present)
    - Test 5 (fail-open) explicitly asserts BOTH: (a) SessionState continues to publish with backgroundTasks=[]; (b) the warn is rate-limited to at most once per hookPayloadWarnCooldownMs per host
    - Test 8 asserts the PID→tmux exec-call count is exactly 1 across 5 poll cycles for the same PID (cache verified)
    - Test 12 asserts one host's SSH failure does not stop polling of other hosts
    - All 12 tests exit 0
  </acceptance_criteria>
  <done>Orchestrator is a fully-testable dependency-injected 2s poll loop; consumes all four Plan 01 pure-library modules; publishes state deltas to the Plan 02 subscription registry; implements fail-open for missing hook payloads with rate-limited WARNs; handles per-host SSH failure without global crash; PID→tmux cache verified; 30s stale sweep verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: remote-hook-install.ts + stop-hook.sh — one-time-per-host file drop + idempotent settings.json merge over SSH</name>
  <files>src/backend/fleet-status/remote-hook-install.ts, src/backend/fleet-status/remote-hook-install.test.ts, src/backend/fleet-status/stop-hook.sh</files>
  <read_first>
    - src/backend/fleet-status/ssh-poll-orchestrator.ts (Task 1 — the SshChannel abstraction; the same interface is used here so tests can share the MockSshChannel harness)
    - .planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-RESEARCH.md (§ Research Target 2 — full hook registration schema in `~/.claude/settings.json`, three-level nesting: event key → matcher-group array → hooks array; Stop hooks have NO matcher; hooks MERGE across levels, do NOT override)
    - .planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-CONTEXT.md (§ PIVOT 2026-08-13 — hook install is a ONE-TIME file drop + settings edit, NOT a persistent process)
    - src/backend/utils/logger.ts (structured-logger idiom)
  </read_first>
  <behavior>
    - Test 1: `stop-hook.sh` (linted via `bash -n`) is syntactically valid, contains `set -eu`, atomically writes stdin to `~/.claude/fleet-status/last-stop-payload.json` via `cat > .tmp && mv .tmp final`, exits 0 unconditionally (grep for `exit 0`; grep to confirm no `exit [1-9]` anywhere)
    - Test 2: `installStopHook(channel, opts?)` where `opts.remoteHookPath` defaults to `~/.claude/hooks/skynet-fleet-status-stop.sh` and `opts.remotePayloadDir` defaults to `~/.claude/fleet-status/`: (a) reads local `stop-hook.sh` from disk (path derived from `import.meta.url` sibling — same directory as the module); (b) SSH-execs `mkdir -p <remoteHookDir> <remotePayloadDir>`; (c) writes the hook script contents via `channel.exec("cat > <remoteHookPath>.tmp <<'STOPHOOK_EOF'\n<contents>\nSTOPHOOK_EOF\nmv <remoteHookPath>.tmp <remoteHookPath> && chmod +x <remoteHookPath>")` (atomic write via .tmp + mv); (d) verifies the write succeeded by execing `test -x <remoteHookPath> && echo OK`; (e) proceeds to settings.json merge (below)
    - Test 3: Settings.json merge: (a) `channel.exec("cat ~/.claude/settings.json 2>/dev/null")` → parse as JSON; if empty/missing → start with `{}`; (b) `readAndMergeStopHookSettings(currentSettings, remoteHookPath)` returns the mutated settings object with the hook entry appended EXACTLY ONCE — if an entry with the same `command === remoteHookPath` already exists in `settings.hooks.Stop[*].hooks[*]`, return the input unchanged (idempotent); otherwise ensure `settings.hooks` exists, `settings.hooks.Stop` exists, `settings.hooks.Stop[0]` exists with a `hooks` array, and append `{type:'command', command: remoteHookPath}` to that array; (c) write the mutated settings back atomically via `cat > .tmp && mv .tmp final`
    - Test 4: Idempotency — running `installStopHook(channel)` a second time on the same mocked host state (settings.json already contains the entry) results in NO settings.json write (the merge is a no-op and installStopHook detects this + skips the write); the hook script itself IS re-copied because we cannot trivially compare local vs remote file contents without a remote hash — but the copy is atomic + safe
    - Test 5: `readAndMergeStopHookSettings({}, '/path/to/hook.sh')` returns `{ hooks: { Stop: [{ hooks: [{ type: 'command', command: '/path/to/hook.sh' }] }] } }` — creates the full three-level structure from scratch
    - Test 6: `readAndMergeStopHookSettings({ hooks: { Stop: [{ hooks: [{type:'command', command:'/other/hook.sh'}] }] } }, '/path/to/hook.sh')` returns settings with BOTH hooks in the same `hooks[]` array (append, not replace — matches RESEARCH § 2 "hooks MERGE, do NOT override")
    - Test 7: `readAndMergeStopHookSettings` preserves existing keys in the settings object (e.g. `mcpServers`, `SessionStart`, `PostToolUse`, `permissions` — all unrelated keys pass through unchanged)
    - Test 8: `uninstallStopHook(channel)` reads settings.json, removes the entry with matching `command === remoteHookPath`, writes back; also `rm -f <remoteHookPath>`; does NOT remove the payload file or payload directory (in case the operator wants to inspect the last captured payload after uninstall)
    - Test 9: When the SSH exec for reading settings.json returns null (SSH error), installStopHook logs `operation: 'fleet_status_hook_install_settings_read_failed'` and throws (this is a synchronous per-host install — a hard failure here is legitimate; the orchestrator's poll loop is unaffected because installStopHook is called out-of-band, not from inside the poll)
    - Test 10: When settings.json exists but is invalid JSON, installStopHook logs `operation: 'fleet_status_hook_install_settings_invalid_json'` and throws WITHOUT overwriting the invalid settings.json (protects the user's config from being clobbered)
  </behavior>
  <action>
    Create `src/backend/fleet-status/stop-hook.sh`. Contents (exact):

    ```
    #!/bin/bash
    #
    # Skynet fleet-status Stop hook — dropped onto each identity-hosting box by
    # remote-hook-install.ts. Reads the Stop hook payload from stdin and writes
    # it atomically to the well-known payload file, which the Skynet backend
    # polls over SSH every 2s.
    #
    # MUST NOT block Claude Code — the hook fires synchronously during turn
    # completion, so we do the minimum work possible: read stdin, atomic-write
    # to disk, exit 0.
    #
    set -eu
    PAYLOAD_DIR="${HOME}/.claude/fleet-status"
    PAYLOAD_FILE="${PAYLOAD_DIR}/last-stop-payload.json"
    TMP_FILE="${PAYLOAD_FILE}.$$.tmp"
    mkdir -p "${PAYLOAD_DIR}"
    # Belt-and-braces: wrap the write in a timeout so a full disk cannot hang
    # the hook indefinitely. Fire-and-forget beyond this point.
    timeout 2 sh -c "cat > '${TMP_FILE}' && mv '${TMP_FILE}' '${PAYLOAD_FILE}'" || true
    exit 0
    ```

    (Do NOT introduce fenced code inside `<action>` — the block above is the file contents to write; the surrounding action prose is directive.) Verify `bash -n` passes and grep asserts a single `exit 0` line and no `exit [1-9]` lines.

    Create `src/backend/fleet-status/remote-hook-install.ts`. Import `SshChannel` type from `./ssh-poll-orchestrator.js`. Import `systemLogger` from `../utils/logger.js`. Import `readFileSync` from `node:fs` and `fileURLToPath` from `node:url` — needed to locate the local `stop-hook.sh` file relative to this module. Interface `InstallOpts`: `{ remoteHookPath?: string; remotePayloadDir?: string; localHookScriptPath?: string }`. Defaults: `remoteHookPath='~/.claude/hooks/skynet-fleet-status-stop.sh'`, `remotePayloadDir='~/.claude/fleet-status'`, `localHookScriptPath=<sibling stop-hook.sh>`. Export three functions:

    - `readAndMergeStopHookSettings(currentSettings: Record<string, unknown>, remoteHookPath: string): { merged: Record<string, unknown>; alreadyInstalled: boolean }` — PURE function, no SSH. Returns `{ merged, alreadyInstalled }`. If the current settings already contains a `hooks.Stop[*].hooks[*]` entry whose `command === remoteHookPath` → return `{ merged: currentSettings, alreadyInstalled: true }` (no mutation). Otherwise: ensure `hooks: {...}` exists (immutably, spread-copy), ensure `hooks.Stop: [{ hooks: [] }, ...]` — if `Stop` is missing, initialize as `[{ hooks: [] }]`; if `Stop` exists but empty, add `[{ hooks: [] }]`; then append `{ type: 'command', command: remoteHookPath }` to `hooks.Stop[0].hooks`. Preserve all other keys in the settings object verbatim (spread-copy every level of nesting we mutate).

    - `installStopHook(channel: SshChannel, opts?: InstallOpts): Promise<{ hookInstalled: boolean; settingsUpdated: boolean }>` — orchestrates the file drop + settings merge over the injected channel:
      1. Resolve `localHookScriptPath` (default: sibling `stop-hook.sh`). Read its contents via `readFileSync`.
      2. `exec("mkdir -p ~/.claude/hooks ~/.claude/fleet-status")` — mkdir before write.
      3. Write the hook script: use a heredoc `cat > .tmp <<'STOPHOOK_EOF'\n<contents>\nSTOPHOOK_EOF && mv .tmp final && chmod +x final`. Concatenate as a single exec command string.
      4. Verify `exec("test -x ~/.claude/hooks/skynet-fleet-status-stop.sh && echo OK")` returns `"OK"`; else log + throw.
      5. Read settings.json: `exec("cat ~/.claude/settings.json 2>/dev/null")`. If null (SSH error) → log `operation: 'fleet_status_hook_install_settings_read_failed'` + throw. If empty string → `currentSettings = {}`. Else `JSON.parse` in try/catch — on parse error → log `operation: 'fleet_status_hook_install_settings_invalid_json'` + throw (do NOT overwrite invalid JSON).
      6. Call `readAndMergeStopHookSettings(currentSettings, remoteHookPath)`. If `alreadyInstalled === true` → skip the write, log `operation: 'fleet_status_hook_install_already_present'`, return `{ hookInstalled: true, settingsUpdated: false }`.
      7. Otherwise: serialize `JSON.stringify(merged, null, 2)`, write atomically via heredoc: `cat > ~/.claude/settings.json.tmp.$$ <<'SETTINGS_EOF'\n<json>\nSETTINGS_EOF && mv ~/.claude/settings.json.tmp.$$ ~/.claude/settings.json`.
      8. Verify by re-reading settings.json + confirming the entry is present (defense-in-depth). Log `operation: 'fleet_status_hook_install_complete'` + hostId (if available in the channel abstraction; otherwise omit). Return `{ hookInstalled: true, settingsUpdated: true }`.

    - `uninstallStopHook(channel: SshChannel, opts?: InstallOpts): Promise<void>` — symmetric removal: read settings.json → filter out any `hooks.Stop[*].hooks[*]` entry matching `command === remoteHookPath` → write back → `exec("rm -f " + remoteHookPath)`. Payload dir + payload file left in place for post-mortem.

    Create `src/backend/fleet-status/remote-hook-install.test.ts`. Reuse the `MockSshChannel` fixture from Task 1's test file (import it directly if easy; else re-implement inline). Cover tests 1-10 above. Test 4 (idempotency) asserts that on second install, exec is NEVER called with `cat > ~/.claude/settings.json.tmp`. Test 6 (append, not replace) explicitly asserts the resulting `hooks.Stop[0].hooks[]` array has length 2 with both entries preserved.
  </action>
  <verify>
    <automated>bash -n src/backend/fleet-status/stop-hook.sh && npx vitest run src/backend/fleet-status/remote-hook-install.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n src/backend/fleet-status/stop-hook.sh` exits 0
    - `grep -c '^exit 0$' src/backend/fleet-status/stop-hook.sh` returns exactly 1
    - `grep -Ec 'exit [1-9]' src/backend/fleet-status/stop-hook.sh` returns 0
    - `grep -c 'set -eu' src/backend/fleet-status/stop-hook.sh` returns 1
    - `grep -c 'export function readAndMergeStopHookSettings' src/backend/fleet-status/remote-hook-install.ts` returns 1
    - `grep -c 'export async function installStopHook' src/backend/fleet-status/remote-hook-install.ts` returns 1
    - `grep -c 'export async function uninstallStopHook' src/backend/fleet-status/remote-hook-install.ts` returns 1
    - Test 4 asserts second-install exec call-count for `cat > ~/.claude/settings.json.tmp` is 0
    - Test 6 asserts `hooks.Stop[0].hooks.length === 2` with both entries preserved
    - Test 10 asserts settings.json is NOT overwritten when it contains invalid JSON
    - All 10 tests exit 0
  </acceptance_criteria>
  <done>Remote hook install is a testable one-time-per-host helper (NOT a persistent process); idempotent settings-file merge; atomic writes; safe on missing/invalid settings.json; symmetric uninstall. Actual bash Stop-hook script is fire-and-forget, atomic-write, exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fail-open regression pinning test — dedicated coverage for the missing-hook-payload behavior Ashley locked</name>
  <files>src/backend/fleet-status/ssh-poll-orchestrator.test.ts</files>
  <read_first>
    - src/backend/fleet-status/ssh-poll-orchestrator.ts (Task 1 — the module under test; specifically the fail-open branch)
    - .planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-CONTEXT.md (§ PIVOT 2026-08-13 — Ashley 2026-08-13 verbatim: *"just make sure that it fails open if the file that the hook is supposed to generate isn't found"*)
  </read_first>
  <behavior>
    Extend `ssh-poll-orchestrator.test.ts` with a `describe('fail-open on missing hook payload file', ...)` block covering ALL five triggers from D-CTX (ENOENT, empty file, malformed JSON, transient SSH read error, valid JSON that fails schema validation). Every scenario asserts (a) orchestrator does NOT throw, (b) orchestrator DOES continue publishing SessionState with backgroundTasks=[], (c) orchestrator does NOT publish session_gone (this is a per-host WARN, not a per-session error), (d) orchestrator does NOT stop polling that host, (e) a warn line fires ONCE and is rate-limited within the cooldown window.

    - Test F1 (ENOENT): SSH exec for `cat ~/.claude/fleet-status/last-stop-payload.json` returns null (simulating `2>/dev/null || true` on a non-existent file — bash outputs empty stdout, our MockSshChannel returns null for the "not-found" case). Assert: session-JSON-only SessionState published with backgroundTasks=[]; ONE warn line with `operation: 'fleet_status_hook_payload_missing'` + hostId; no throw; no session_gone.
    - Test F2 (empty string): SSH exec returns `""`. Assert same as F1 (empty is treated as missing).
    - Test F3 (malformed JSON): SSH exec returns `"{not:valid,json"`. Assert same as F1 — `parseStopHookPayload` returns null, orchestrator treats as missing.
    - Test F4 (transient SSH read error mid-poll): first poll returns valid payload with 2 non-ambient tasks → SessionState with 2 background tasks published; second poll returns null for the hook payload exec → SessionState with backgroundTasks=[] published (delta detected, so this IS a publish), warn fires; third poll returns null again → NO warn (rate-limited within cooldown), SessionState fingerprint unchanged so NO publish (delta semantics). Advance timers past cooldown; fourth poll returns null again → warn fires again (cooldown expired).
    - Test F5 (schema-valid JSON but wrong shape — e.g. `{"background_tasks": "not an array"}`): parseStopHookPayload returns null; orchestrator treats as missing per F1 semantics.
    - Test F6 (session-JSON is authoritative): even when the hook payload is missing for ALL poll cycles across the entire test lifetime, the session-JSON-derived `status` (busy/shell/idle/waiting) continues to propagate correctly — a state transition from busy → idle → busy still results in exactly 2 publishes to the registry (the delta fingerprint changes twice), and the WARN line fires only twice (once per cooldown window, not once per publish).
  </behavior>
  <action>
    Append the `describe('fail-open on missing hook payload file', ...)` block to `src/backend/fleet-status/ssh-poll-orchestrator.test.ts`. Reuse the MockSshChannel + MockRegistry fixtures from Task 1. Use `vi.useFakeTimers()` throughout so cooldown windows can be exercised. Explicitly ASSERT the warn cooldown by counting `mockedSystemLogger.warn.mock.calls.filter(c => c[1]?.operation === 'fleet_status_hook_payload_missing').length` at each phase of the test.

    This task adds NO new production code — it is a regression-pinning test suite for the fail-open constraint. It exists as a standalone task so the checker sees explicit coverage of Ashley's 2026-08-13 verbatim constraint separate from the orchestrator's general behavior tests.
  </action>
  <verify>
    <automated>npx vitest run src/backend/fleet-status/ssh-poll-orchestrator.test.ts -t "fail-open on missing hook payload"</automated>
  </verify>
  <acceptance_criteria>
    - The vitest `-t` filter matches at least 6 tests (F1-F6)
    - `grep -c "fleet_status_hook_payload_missing" src/backend/fleet-status/ssh-poll-orchestrator.test.ts` returns at least 6 (one assertion per fail-open scenario)
    - Test F4 explicitly advances timers past `hookPayloadWarnCooldownMs` and asserts the warn count increments AFTER the cooldown but NOT during it
    - Test F6 asserts the SessionState publish count and the warn count are DECOUPLED — session status changes drive publishes; the missing hook drives at-most-once-per-cooldown warns
    - All 6 fail-open tests exit 0
  </acceptance_criteria>
  <done>Fail-open behavior for the missing hook payload file is regression-pinned across all five failure modes Ashley's constraint anticipates (ENOENT, empty, malformed JSON, SSH read error, schema-invalid). Rate-limited WARN behavior is explicitly asserted. Session-JSON authority under a permanently-missing hook is proven.</done>
</task>

<task type="auto">
  <name>Task 4: Verify-monitor-payload.sh (scripts/) + scripts/README.md + starter.ts wire-in</name>
  <files>src/backend/fleet-status/scripts/verify-monitor-payload.sh, src/backend/fleet-status/scripts/README.md, src/backend/starter.ts</files>
  <read_first>
    - src/backend/fleet-status/ssh-poll-orchestrator.ts (Task 1 — what starter.ts wires in)
    - src/backend/fleet-status/remote-hook-install.ts (Task 2 — the verify script uses it to ensure the hook is installed on the scratch identity)
    - src/backend/starter.ts (grep for `startFleetStatusServer` to find the neighborhood where the orchestrator is spawned — Plan 02 already added `startFleetStatusServer(...)` there)
    - .planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-RESEARCH.md (§ Research Target 1 — the exact `background_tasks[]` fields the verify script prints; § Open Questions § OQ-2 — what needs verifying)
    - src/backend/ssh/ssh-one-shot.ts + src/backend/ssh/tmux-helper.ts (SSH primitives — bind these into the orchestrator's dep injection)
  </read_first>
  <action>
    Create `src/backend/fleet-status/scripts/verify-monitor-payload.sh`. Purpose: an orchestrator-run tool (invoked by tina in the Task 5 checkpoint) that verifies OQ-2 against a scratch identity. Flow:

    ```
    #!/bin/bash
    #
    # verify-monitor-payload.sh — RESEARCH § OQ-2 closer.
    #
    # Usage: bash verify-monitor-payload.sh <scratch-identity-hostname>
    #
    # Assumes: (a) SSH access to the target host is already configured,
    # (b) the target host has a Claude Code session available, (c) the
    # Skynet backend is running and will pick up the SessionState frames
    # via the ssh-poll-orchestrator once the hook is installed.
    #
    # This script does NOT install the hook — that is the orchestrator's
    # (tina's) job via a small `node -e "import('./remote-hook-install.js')..."`
    # snippet documented in scripts/README.md. This script assumes the hook
    # is ALREADY installed on the target and simply cats the last-captured
    # payload file, parses out the background_tasks[] entries, and prints
    # each entry with jq so the operator can eyeball the shape.
    #
    set -euo pipefail
    HOST="${1:?usage: verify-monitor-payload.sh <hostname>}"
    PAYLOAD_PATH="\$HOME/.claude/fleet-status/last-stop-payload.json"
    echo "Reading Stop-hook payload from ${HOST}:${PAYLOAD_PATH} ..."
    RAW=$(ssh -o ConnectTimeout=5 "${HOST}" "cat '${PAYLOAD_PATH}' 2>/dev/null || echo ''")
    if [ -z "${RAW}" ]; then
      echo "ERROR: payload file is missing or empty on ${HOST}." >&2
      echo "  Ensure remote-hook-install has been run for this host, then trigger a Stop event in a Claude Code session (any turn completion) and re-run this script." >&2
      exit 2
    fi
    echo "--- All background_tasks[] entries (raw) ---"
    echo "${RAW}" | jq '.background_tasks // []'
    echo ""
    echo "--- Monitor-type entries (RESEARCH § OQ-2 target) ---"
    MON_COUNT=$(echo "${RAW}" | jq '[.background_tasks[]? | select(.type == "monitor")] | length')
    if [ "${MON_COUNT}" = "0" ]; then
      echo "WARN: no type='monitor' entries in the captured payload." >&2
      echo "  This does NOT mean the pipeline is broken — it means the last Stop event on ${HOST} did not have any live Monitors." >&2
      echo "  Launch a Monitor from a Claude Code session on ${HOST}, trigger another Stop, and re-run." >&2
      exit 3
    fi
    echo "${RAW}" | jq '[.background_tasks[]? | select(.type == "monitor")]'
    echo ""
    echo "--- Field-presence check (RESEARCH § 1 field table) ---"
    echo "${RAW}" | jq '.background_tasks[]? | select(.type == "monitor") | { id, type, status, has_description: (.description != null), description_prefix: (.description // "" | .[0:12]), has_server: (.server != null), has_tool: (.tool != null) }'
    echo ""
    echo "OK: ${MON_COUNT} monitor-type entries captured."
    echo "If description_prefix shows '[ambient] ' for the persistent Monitors, Plan 05 ambient tagging is landed for this identity."
    ```

    Create `src/backend/fleet-status/scripts/README.md`. Sections:

    - **Purpose** — what the scripts directory is for (one-off orchestrator tools; NOT part of the running Skynet backend; NOT unit-tested; hand-run by tina as documented).
    - **Fail-open guarantee (LOCKED)** — restate Ashley's 2026-08-13 constraint verbatim; explain that if the operator ever finds the payload file missing on a host and the orchestrator is still publishing SessionStates for that host's sessions, that is CORRECT behavior, not a bug — the dot will under-report background work on that host until (re-)install.
    - **How to install the Stop hook on a new identity-hosting host** — provide a small `node -e` snippet the orchestrator can run: `node -e "import('./dist/backend/fleet-status/remote-hook-install.js').then(async m => { const { Client } = await import('ssh2'); const conn = /* connect */; const channel = { exec: (cmd) => new Promise((res) => conn.exec(cmd, (err, stream) => { let out=''; stream.on('data', d => out += d); stream.on('close', () => res(err ? null : out)); })) }; await m.installStopHook(channel); })"`. Note that the production path is: the orchestrator (tina) runs this once when adding a new identity-hosting box; the ssh-poll-orchestrator does NOT auto-install because we want the operator to be explicit about touching `~/.claude/settings.json` on a managed host.
    - **How to run verify-monitor-payload.sh** — usage, expected exit codes (0 = OK, 2 = payload file missing/empty, 3 = no monitor entries), how to interpret the output, when to run it (before flipping the Plan 06 frontend cutover so RESEARCH § OQ-2 is closed with fresh evidence).
    - **How to uninstall the Stop hook** — symmetric `node -e` snippet using `uninstallStopHook`.
    - **Log locations** — the orchestrator logs to the standard Skynet backend log destination (`/opt/skynet/console-forward-logs/console-forward.log` per D-CTX § Structured logging directive) with `operation: 'fleet_status_*'` prefixes; the hook script itself writes only to `~/.claude/fleet-status/last-stop-payload.json` on the identity-hosting box (no separate log file — the hook is fire-and-forget).
    - **What NOT to add here** — no per-box daemon scripts, no systemd unit templates, no persistent install script. The pivot from 2026-08-13 (LOCKED) forbids all of those.

    Modify `src/backend/starter.ts`. Find the `startFleetStatusServer(...)` call added by Plan 02. After it, add:
    - Construct a `SshChannel` factory that maps a Skynet host record → ssh2 Client → the `SshChannel` interface (`exec(command)` wrapping `execCommand(conn, command)` in try/catch to return null on error).
    - Construct a `listIdentityHostingHosts` async function that queries the Skynet DB for the current identity-hosting host list (grep `src/backend/database/routes/` for the closest existing "which hosts host identities" query — likely a join between `identities` and `hosts` via whatever table links them; if no direct link exists yet, initially return the set of `hosts` where `enableTerminal === true` AND at least one row in `messageQueueItems` has `hostId` matching — this is a reasonable initial approximation; document the choice and any TODOs in a code comment). Wrap in try/catch; on error return `[]` + log.
    - Call `const orchestrator = createSshPollOrchestrator({ listIdentityHostingHosts, acquireSshChannel, releaseSshChannel, registry: <same registry instance passed to startFleetStatusServer>, setInterval, clearInterval, now: () => Date.now() }); orchestrator.start();`.
    - Log `operation: 'fleet_status_orchestrator_started'` + `pollIntervalMs` + `staleSweepIntervalMs` + `identityHostCount` (from the initial DB query).
    - Do NOT wrap in a checkpoint — this is the executor's wire-in; the orchestrator will begin polling immediately on backend boot. Task 5's checkpoint verifies the running behavior after the code is in place.
  </action>
  <verify>
    <automated>bash -n src/backend/fleet-status/scripts/verify-monitor-payload.sh && grep -c 'createSshPollOrchestrator' src/backend/starter.ts && npx tsc --noEmit && npm run build:backend</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n src/backend/fleet-status/scripts/verify-monitor-payload.sh` exits 0
    - `grep -c 'jq' src/backend/fleet-status/scripts/verify-monitor-payload.sh` returns at least 3 (payload parsing via jq)
    - `grep -c 'type.*monitor' src/backend/fleet-status/scripts/verify-monitor-payload.sh` returns at least 2 (monitor-type filter present)
    - `grep -c 'exit 2\|exit 3' src/backend/fleet-status/scripts/verify-monitor-payload.sh` returns at least 2 (distinct exit codes for missing-payload vs no-monitor)
    - `grep -c 'createSshPollOrchestrator' src/backend/starter.ts` returns exactly 1 (single wire-in)
    - `grep -c 'orchestrator.start()' src/backend/starter.ts` returns exactly 1
    - `grep -c 'fleet_status_orchestrator_started' src/backend/starter.ts` returns at least 1 (structured log at wire-in)
    - `src/backend/fleet-status/scripts/README.md` exists with headings "Purpose", "Fail-open guarantee (LOCKED)", "How to install", "How to run verify-monitor-payload.sh", "How to uninstall", "Log locations", "What NOT to add here"
    - `npx tsc --noEmit` at repo root exits 0
    - `npm run build:backend` at repo root exits 0
  </acceptance_criteria>
  <done>Verify script drafted + syntax-clean; scripts/README documents orchestrator usage; starter.ts wires the orchestrator into the Skynet backend boot sequence; TypeScript + backend build both clean. Everything Plan 06 depends on is in place — checkpoint next.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Human verify — orchestrator polls a real scratch identity, RESEARCH § OQ-2 closed, fail-open observed against a host with the hook uninstalled</name>
  <files>src/backend/fleet-status/scripts/verify-monitor-payload.sh, src/backend/fleet-status/scripts/README.md (verification-only — nothing modified in this task)</files>
  <read_first>
    - src/backend/fleet-status/scripts/README.md (Task 4 — the runbook)
    - src/backend/fleet-status/scripts/verify-monitor-payload.sh (Task 4)
    - src/backend/fleet-status/remote-hook-install.ts (Task 2 — the operator uses this via the node -e snippet in README)
    - src/backend/fleet-status/ssh-poll-orchestrator.ts (Task 1 — the module that must be running post-restart)
    - .planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-RESEARCH.md (§ Open Questions § OQ-2 — the exact contract to verify)
    - .planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-CONTEXT.md (§ PIVOT 2026-08-13 fail-open constraint — the operator explicitly verifies this in step 5 below)
  </read_first>
  <action>Manual verification only — the orchestrator (tina) exercises the pipeline against a scratch identity per the how-to-verify steps below. This task produces zero code diffs. If Monitor payload shape differs from RESEARCH § 1 expectations OR the fail-open behavior does not hold when the hook is uninstalled, STOP and revise before Plan 05 / Plan 06 can proceed.</action>
  <verify>
    <human-check>Steps 1-6 in how-to-verify below all pass; the OQ-2 payload capture is attached to the phase's SUMMARY as evidence.</human-check>
  </verify>
  <done>ssh-poll-orchestrator is running inside the Skynet backend post-restart; a scratch identity has the Stop hook installed and the orchestrator successfully published at least one SessionState with `background_tasks[]` populated to the subscription registry; RESEARCH § OQ-2 closed with a captured payload showing a `type: 'monitor'` entry; fail-open behavior explicitly observed against a host whose hook was intentionally uninstalled mid-test (SessionState with backgroundTasks=[] continues to publish, one rate-limited WARN fires, no crash).</done>
  <what-built>
    - ssh-poll-orchestrator with 2s SSH-poll + 30s stale sweep + PID→tmux caching + delta-semantics publish (Task 1)
    - remote-hook-install helper + stop-hook.sh script (Task 2)
    - Fail-open regression-pinning tests (Task 3)
    - verify-monitor-payload.sh + scripts/README + starter.ts wire-in (Task 4)
    - Everything is code-complete + tests green + TypeScript clean. No real host has been touched yet — this checkpoint is where the orchestrator (tina) exercises the pipeline against a real scratch identity.
  </what-built>
  <how-to-verify>
    1. **Sanity check the build**: `npx tsc --noEmit && npm run build:backend && npx vitest run src/backend/fleet-status/` — all three exit 0.
    2. **Restart the backend (dev mode)**: `npm run dev` (or the standard dev command); confirm the backend log stream contains a `operation: 'fleet_status_orchestrator_started'` line within 5s of boot. Confirm `operation: 'fleet_status_host_ssh_unreachable'` does NOT dominate the log (a few hosts may be unreachable — that's fine and expected under fail-open).
    3. **Install the Stop hook on a scratch identity** (recommendation: tina on this box, since tina's Claude session is right here): follow the `node -e` snippet in `scripts/README.md` § "How to install the Stop hook on a new identity-hosting host". Confirm `~/.claude/settings.json` on the scratch identity now contains a `hooks.Stop[0].hooks[]` entry pointing at `~/.claude/hooks/skynet-fleet-status-stop.sh`. Confirm `~/.claude/hooks/skynet-fleet-status-stop.sh` exists and is executable.
    4. **Trigger a Stop event on the scratch identity + verify OQ-2**: in a Claude Code session on the scratch identity, run a quick tool call (e.g. `Bash echo hi`) so a Stop event fires. Then run `bash src/backend/fleet-status/scripts/verify-monitor-payload.sh <scratch-hostname>`. Expected output: the parsed `background_tasks[]` array, followed by the monitor-type entries with `type`, `status`, `has_description`, `description_prefix`, `has_server`, `has_tool` fields. Attach the captured raw JSON payload as evidence in `34-04-SUMMARY.md`. If ZERO monitor-type entries appear, launch a Monitor from the scratch identity's Claude session (e.g. `/use Monitor "sleep 60"`), trigger another Stop, and re-run. Once a monitor-type entry is captured, RESEARCH § OQ-2 is CLOSED.
    5. **Verify orchestrator publishes SessionState for the scratch identity**: tail the backend log stream (`docker compose logs -f skynet | grep fleet_status` — or the equivalent for dev-mode). Confirm you see `operation: 'fleet_status_orchestrator_started'` followed within a few 2s ticks by `operation: 'fleet_status_session_state_published'` (or equivalent — the exact operation string is whatever the orchestrator emits at publish; grep `src/backend/fleet-status/ssh-poll-orchestrator.ts` to confirm the string). Confirm the log line includes the scratch identity's hostId + sessionId + status.
    6. **Verify fail-open explicitly** (this is the LOAD-BEARING check per Ashley 2026-08-13): on the scratch identity, `rm ~/.claude/fleet-status/last-stop-payload.json` (delete the payload file the hook writes). Wait 5s (2-3 poll cycles). Tail the backend log again. Confirm: (a) SessionState frames for that identity STILL publish, with `backgroundTasks` empty; (b) at most ONE `operation: 'fleet_status_hook_payload_missing'` warn line appears within the cooldown window (default 60s); (c) the orchestrator did NOT crash, did NOT publish `session_gone` for that identity's sessions, did NOT stop polling that host. If any of those conditions fail, STOP and revise — the fail-open constraint is NOT negotiable.
  </how-to-verify>
  <resume-signal>Type "approved: orchestrator live + OQ-2 closed + fail-open confirmed" once all six steps pass and the OQ-2 payload evidence is attached to the SUMMARY. If step 4 reveals the Monitor payload shape differs from RESEARCH § 1 (e.g. description field absent, or type discriminant is not literally "monitor"), STOP and describe the deviation — Plan 06's filter logic may need revision before the frontend cutover is safe. If step 6 reveals fail-open is broken (orchestrator crashes, or publishes session_gone on missing hook file, or fires warn every 2s instead of once per cooldown), STOP and revise Task 1's fail-open branch before proceeding.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skynet backend → identity-hosting host (SSH exec) | Existing SSH pool primitives; the backend already trusts these boxes for tmux/pane plumbing; the orchestrator reuses the same trust boundary |
| Remote `~/.claude/sessions/<pid>.json` contents | Untrusted harness-written JSON pulled over SSH; every parse goes through Plan 01's `parseSessionJson` which returns null on any failure and never throws |
| Remote `/proc/<pid>/stat` and `/proc/<pid>/environ` contents | Untrusted `/proc` reads pulled over SSH; comm-field paren smuggling and environ-value injection are mitigated in Plan 01's `readProcStartField22` and `isValidTmuxPaneId` respectively |
| Remote Stop-hook payload file `~/.claude/fleet-status/last-stop-payload.json` | Untrusted contents (harness writes it); parseStopHookPayload validates via zod; ENOENT / empty / malformed / SSH-error all trigger fail-open per LOCKED constraint |
| Remote `~/.claude/settings.json` mutation | High-value user-config file; remote-hook-install reads first, mutates in-memory, writes atomically via .tmp + mv; refuses to write if the current file is invalid JSON (would rather fail than clobber user config); idempotent so re-runs are safe |
| stop-hook.sh execution during Claude Code turn completion | Runs synchronously in the harness's Stop hook context; MUST NOT block Claude Code; wrapped in `timeout 2` + `exit 0` unconditional to guarantee turn completion is never delayed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-13 | Spoofing | Identity-host DB list | mitigate | Orchestrator polls hosts read from the Skynet DB — same host records already used by tmux/pane plumbing; no cross-boundary trust introduced |
| T-34-14 | Tampering | ~/.claude/settings.json mutation | mitigate | Read-first, in-memory-merge, atomic-write via .tmp+mv; refuses to write if current file is invalid JSON (log + throw, no overwrite); idempotent (no-op on second run); backup preserved implicitly by the atomic-mv semantics until the mv completes |
| T-34-15 | Denial of Service | stop-hook.sh blocking Claude Code | mitigate | `timeout 2` on the atomic write + unconditional `exit 0` — hook cannot delay a turn completion by more than 2s in the worst case, and by construction never blocks longer than a normal disk write |
| T-34-16 | Injection | tmux display-message shell interpolation | mitigate | Plan 01's `isValidTmuxPaneId(/^%\d+$/)` gate BEFORE the SSH-exec is issued; even a compromised /proc read cannot inject shell metacharacters into the tmux command |
| T-34-17 | Injection | /proc/<pid>/stat comm-field parens | mitigate | Plan 01's `lastIndexOf(')')` anchor; comm field never passed downstream |
| T-34-18 | Denial of Service | SSH pool exhaustion under 2s polling | mitigate | Orchestrator opens ONE multiplexed channel per host and reuses it for the entire life of the orchestrator; polls run over that single channel; existing pool max-per-host limit (3) is not stressed |
| T-34-19 | Repudiation | Silent orchestrator failure | mitigate | Every poll cycle, every publish, every fail-open trigger logs a structured JSON line via systemLogger with an explicit operation field; grep-able for post-mortem |
| T-34-20 | Fail-Open Safety | Missing hook payload file | accept | LOCKED constraint (Ashley 2026-08-13). The dot may under-report background work on hosts where the hook is missing; this is preferable to false-alarm crashes. Rate-limited WARN gives visibility without spam |
| T-34-SC | Tampering | No new npm packages | accept | This plan adds no new dependencies (uses existing `ws`, `ssh2`, `zod`, drizzle); no supply-chain surface introduced |
</threat_model>

<verification>
- `npx vitest run src/backend/fleet-status/` exits 0 (all Plan 01 + Plan 02 + Plan 04 tests green)
- `npx vitest run src/backend/fleet-status/ssh-poll-orchestrator.test.ts -t "fail-open on missing hook payload"` matches at least 6 tests, all green (Ashley's LOCKED constraint pinned)
- `npx tsc --noEmit` at repo root exits 0
- `npm run build:backend` at repo root exits 0
- `bash -n src/backend/fleet-status/stop-hook.sh` exits 0
- `bash -n src/backend/fleet-status/scripts/verify-monitor-payload.sh` exits 0
- `grep -c 'createSshPollOrchestrator' src/backend/starter.ts` returns exactly 1
- `grep -rn 'src/fleet-status-watcher' src/ .planning/phases/34-*/34-04-PLAN.md 2>/dev/null` returns 0 (the reverted subpackage is not referenced anywhere)
- `grep -rn 'systemd\|fleet-status-watcher\.service' src/backend/fleet-status/` returns 0 (no per-box daemon anywhere — pivot honored)
- Task 5 checkpoint approval received before Plan 05 proceeds
</verification>

<success_criteria>
- ssh-poll-orchestrator runs inside the Skynet backend, opens one multiplexed SSH channel per identity-hosting host, polls session-JSON + /proc + Stop-hook-payload every 2s, publishes state deltas to the Plan 02 subscription registry
- Fail-open on missing hook payload file is regression-pinned across five failure modes (ENOENT, empty, malformed JSON, SSH read error, schema-invalid) with rate-limited WARNs
- remote-hook-install is a one-time-per-host helper (NOT a persistent process) with idempotent settings.json merge, atomic writes, and protection against clobbering invalid settings.json
- stop-hook.sh is a fire-and-forget bash script — 2s timeout on the atomic write, unconditional `exit 0`, cannot block Claude Code
- verify-monitor-payload.sh closes RESEARCH § OQ-2 with a captured payload attached to the SUMMARY
- No per-box daemon anywhere; no systemd unit; no standalone subpackage; no install script for a persistent process — the 2026-08-13 pivot is fully honored
- starter.ts wires the orchestrator into the backend boot sequence with a single call
- Task 5 checkpoint approves the running behavior against a real scratch identity
</success_criteria>

<output>
Create `.planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-04-SUMMARY.md` documenting: (a) which SSH primitive the orchestrator wound up using (`connectOneShot` + long-lived Client per host OR `withConnection` per poll — whichever fit the 2s cadence), (b) the OQ-2 captured payload (attach the raw JSON blob so the checker + Plan 06's ambient filter logic can reference the actual shape), (c) the identity-host list the orchestrator resolved at boot (baseline for future rollouts), (d) any hosts where the operator explicitly chose to install / not install the Stop hook and why, (e) the fail-open scenarios exercised in Task 5 step 6 (which host, how the hook was uninstalled, what the log stream showed), (f) any deviation from RESEARCH § 1 in the observed Monitor payload shape (would force Plan 06 filter revision), (g) the exact `operation` strings the orchestrator emits at publish / gone / warn boundaries so Plan 06's end-to-end integration test can key on them, (h) confirmation that no per-box daemon / systemd unit / standalone subpackage exists in the tree post-plan (the reverted `src/fleet-status-watcher/` is gone).
</output>
