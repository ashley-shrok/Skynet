---
phase: 34-backend-authoritative-fleet-status-broadcast-channel-via-har
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/WaitingBubble.tsx
  - src/ui/features/pretty-view/WaitingBubble.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "PrettyView has a new WaitingBubble component that renders an in-flow assistant-aligned bubble displaying a waitingFor reason string"
    - "WaitingBubble matches the PlanPendingBubble visual language (Phase 4 Glass identity-hue treatment, backdrop-blur, gradient, border, shadow) but is semantically distinct — it is presence-only with NO interactive controls (per D-CTX § Waiting bubble: 'no interactive controls; Ashley must switch to a terminal pane to answer')"
    - "WaitingBubble displays the waitingFor reason string exactly as the harness reports it (e.g. 'approve Bash' / 'sandbox request' / 'worker request' / 'dialog open')"
    - "WaitingBubble has no external dependencies beyond React + lucide-react + cn — the wiring into PrettyView happens in Plan 06 once the fleet-status channel is delivering waiting frames"
    - "Component is fully unit-tested: renders reason string, applies identity hue via --pv-id-hue CSS var, shows the correct glyph, has role='status' + aria-label for a11y"
  artifacts:
    - path: "src/ui/features/pretty-view/WaitingBubble.tsx"
      provides: "Presentational component for the harness-waiting state"
      exports: ["WaitingBubble", "WaitingBubbleProps"]
      min_lines: 60
    - path: "src/ui/features/pretty-view/WaitingBubble.test.tsx"
      provides: "Vitest coverage: renders reason, aria-label, identity hue"
  key_links:
    - from: "src/ui/features/pretty-view/WaitingBubble.tsx"
      to: "src/ui/features/pretty-view/PlanPendingBubble.tsx"
      via: "Copy the Phase 4 Glass className constants verbatim from PlanPendingBubble's outer container so the bubble reads as visually consistent with the plan-pending bubble"
      pattern: "rounded-\\[var\\(--radius-pv-bubble\\)\\]|hsla\\(var\\(--pv-id-hue\\)"
    - from: "src/ui/features/pretty-view/WaitingBubble.test.tsx"
      to: "src/ui/features/pretty-view/PlanPendingBubble.tsx"
      via: "Mirror the testing idiom used for PlanPendingBubble (if a test file exists) — @testing-library/react + vitest"
      pattern: "@testing-library/react|vitest"
---

<objective>
Build the new PrettyView WaitingBubble component — the UI surface D-CTX names as a bundled deliverable of Phase 34 (§ Waiting bubble UX). This is the visual affordance that surfaces when the harness needs a user decision PrettyView cannot render interactively (e.g. a file-deletion permission prompt that slips past dangerously-skip-permissions). Modeled on PlanPendingBubble.tsx — same visual language, same mount slot pattern — but semantically distinct: it is a presence-only indicator. Ashley must switch to a terminal pane to answer the prompt; the bubble tells her that.

Purpose: Ships in parallel with Plans 01 + 02 because it is pure UI with no backend dependency. Wiring into PrettyView (mount site + subscription to the fleet-status channel) happens in Plan 06 during the frontend cutover.

Output: One new component file + one test file. Zero other code paths modified.

Non-goal: This plan does NOT wire the bubble into PrettyView. It does NOT touch session-working-store.ts. It does NOT open the fleet-status WebSocket. All of that is Plan 06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-CONTEXT.md
@.planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-RESEARCH.md
@src/ui/features/pretty-view/PlanPendingBubble.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WaitingBubble component + unit tests</name>
  <files>src/ui/features/pretty-view/WaitingBubble.tsx, src/ui/features/pretty-view/WaitingBubble.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/PlanPendingBubble.tsx (visual template — copy the outer-container Phase 4 Glass className stack verbatim; understand the props-in/callbacks-out pattern even though WaitingBubble has NO callbacks)
    - .planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-CONTEXT.md (§ Waiting bubble — copy from Ashley 2026-08-13; explicit "no interactive controls" rule; reason-string examples)
    - .planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-RESEARCH.md (§ Research Target 3 — full list of waitingFor values: "approve <ToolName>", "worker request", "sandbox request", "dialog open", "input needed")
  </read_first>
  <behavior>
    - Test 1: Renders the reason string passed via props exactly (e.g. `<WaitingBubble reason="approve Bash" />` renders "approve Bash" in the DOM)
    - Test 2: Has `role="status"` and an `aria-label` that includes the word "waiting" and the reason string (e.g. `aria-label="Harness waiting on you: approve Bash"`)
    - Test 3: Container has `justify-start` (assistant-aligned) — matches PlanPendingBubble's alignment
    - Test 4: Outer bubble div includes the Phase 4 Glass className tokens: `rounded-[var(--radius-pv-bubble)]`, `backdrop-blur-xl`, `hsla(var(--pv-id-hue)` (checked as substrings in className)
    - Test 5: Renders a distinct glyph (not ClipboardList — that's PlanPendingBubble's). Use lucide-react `Hand` icon (semantically "stop, wait for me") — assert an SVG with the Hand icon's `aria-hidden="true"` is present
    - Test 6: Renders NO buttons anywhere in the tree (querySelectorAll('button') returns empty NodeList) — enforces the "no interactive controls" rule
    - Test 7: When reason prop is null or empty string, the component renders a fallback string "Waiting on you" (still status/aria-label present)
  </behavior>
  <action>
    Create `src/ui/features/pretty-view/WaitingBubble.tsx`. Import `React`, `Hand` from `lucide-react`, `cn` from `@/lib/utils`. Export `WaitingBubbleProps` interface: `{ reason: string | null }`. Export `function WaitingBubble({ reason }: WaitingBubbleProps)`. Render a `<div className={cn("flex", "justify-start")}>` outer, containing an inner `<div>` with `role="status"`, `aria-label={\`Harness waiting on you\${reason ? \`: \${reason}\` : ''}\`}`, and the Phase 4 Glass className stack copied VERBATIM from `PlanPendingBubble.tsx` lines 148-173 (the exact same rounded/backdrop/gradient/text/border/shadow classes) EXCEPT drop the `max-w-[min(720px,80vw)]` (waiting bubble is short — no need for the wide plan-file container width). Inside the bubble: a header row `<div className="flex items-center gap-2">` with a `<Hand className="h-4 w-4 shrink-0" aria-hidden="true" />` and a `<span>` displaying `reason ?? 'Waiting on you'`. NO body section, NO footer, NO buttons — this is strictly a presence indicator. Documentation block at the top of the file (matching PlanPendingBubble's docblock idiom, 20-40 lines) explaining: (a) what this bubble is for (harness waiting state Ashley can't answer from PrettyView), (b) why NO interactive controls (D-CTX lock — she must switch to the terminal pane), (c) the semantic distinction from PlanPendingBubble (plan mode has approve/feedback affordances because those DO route through raw_keystrokes; waiting is a permission modal Ashley clicks in tmux), (d) mount site is Plan 06's responsibility (this file ships without a mounter).
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/WaitingBubble.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'role="status"' src/ui/features/pretty-view/WaitingBubble.tsx` returns ≥ 1
    - `grep -c '<button' src/ui/features/pretty-view/WaitingBubble.tsx` returns 0
    - `grep -c 'Hand' src/ui/features/pretty-view/WaitingBubble.tsx` returns ≥ 1 (lucide import + usage)
    - `grep -c 'rounded-\[var(--radius-pv-bubble)\]' src/ui/features/pretty-view/WaitingBubble.tsx` returns ≥ 1 (Phase 4 Glass token reused)
    - All 7 tests exit 0
  </acceptance_criteria>
  <done>WaitingBubble is a self-contained presentational component ready for Plan 06 to mount. Visual language matches PlanPendingBubble. Presence-only semantics enforced by test 6 (no buttons in tree). All 7 tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| props → DOM render | `reason` string is rendered as text via React (auto-escaped); NEVER via dangerouslySetInnerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-11 | Injection | reason prop → DOM | mitigate | Render exclusively via React text node (auto-escaped); no `dangerouslySetInnerHTML` anywhere; grep enforces this |
| T-34-12 | Information Disclosure | reason string could contain internal detail | accept | The waitingFor field comes from Claude Code harness; content is user-facing tool names ("approve Bash") — no secret leakage risk |
</threat_model>

<verification>
- `npx vitest run src/ui/features/pretty-view/WaitingBubble.test.tsx` exits 0
- `grep -rn 'dangerouslySetInnerHTML' src/ui/features/pretty-view/WaitingBubble.tsx` returns 0 matches
- `grep -rn 'WaitingBubble' src/ui/features/pretty-view/PrettyView.tsx` returns 0 matches (mount happens in Plan 06, not here)
</verification>

<success_criteria>
- New WaitingBubble component compiles + tests green
- Visual language matches PlanPendingBubble (Phase 4 Glass identity-hue treatment)
- Presence-only semantics enforced by presence of role=status + absence of buttons
- Component is inert — not yet mounted in PrettyView
</success_criteria>

<output>
Create `.planning/phases/34-backend-authoritative-fleet-status-broadcast-channel-via-har/34-03-SUMMARY.md` when done. MUST document: (a) the WaitingBubbleProps contract (reason: string | null), (b) how Plan 06 should mount it (same slot pattern as PlanPendingBubble — sibling of WipBubble at bottom of message list; only mounted when `status === 'waiting'` for the current session), (c) the reason-string ↔ SessionState.waitingFor mapping (pass through verbatim).
</output>
