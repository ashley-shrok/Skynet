---
phase: 96-on-disk-tree-consolidation-consolidate-identity-metadata-and
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/claude-session/per-identity-file.ts
  - src/backend/claude-session/per-identity-file.test.ts
  - src/backend/claude-session/identity-artifact-reader.ts
  - src/backend/claude-session/identity-artifact-reader.two-step.test.ts
  - src/backend/claude-session/identity-artifact-reader.include-archived.test.ts
  - src/backend/claude-session/identity-artifact-reader.role-cosmetics.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "per-identity-file.ts localTargetPath and remoteTargetPath resolve to ~/fleet/identities/<name>/<relPath> — no legacy ~/.claude/identities left in the source or its comment/JSDoc surface"
    - "identity-artifact-reader.ts getLocalIdentitiesRoot() and getLocalRolesRoot() fallback path segments are 'fleet','identities' and 'fleet','roles' (env-var override seam preserved)"
    - "Every remote SSH-exec template literal in identity-artifact-reader.ts referencing $HOME/.claude/identities or $HOME/.claude/roles or ${remoteHome}/.claude/... is rewritten to the fleet-tree equivalent"
    - "All test assertions in the touched test files expect the fleet-tree paths; npm test is green for these test files"
  artifacts:
    - path: "src/backend/claude-session/per-identity-file.ts"
      provides: "Single-source-of-truth for SFTP write/remove/stat local + remote path resolution"
      contains: "path.join(os.homedir(), \"fleet\", \"identities\", name, relPath)"
    - path: "src/backend/claude-session/identity-artifact-reader.ts"
      provides: "Local + remote reader/writer helpers for identity + role artifacts"
      contains: "path.join(os.homedir(), \"fleet\", \"identities\")"
  key_links:
    - from: "src/backend/database/routes/identity-birth-orchestrator.ts"
      to: "src/backend/claude-session/per-identity-file.ts writeIdentityFile"
      via: "SFTP write for relay.json (Step 8 of birth flow)"
      pattern: "writeIdentityFile"
    - from: "src/backend/claude-session/identity-artifact-reader.ts LOCAL branches"
      to: "getLocalIdentitiesRoot / getLocalRolesRoot"
      via: "path.join(os.homedir(), \"fleet\", ...)"
      pattern: "getLocal(Identities|Roles)Root"
---

<objective>
Change the single-source-of-truth path constants for Skynet backend identity + role artifact access from the legacy `~/.claude/identities` and `~/.claude/roles` tree to the fleet tree at `~/fleet/identities` and `~/fleet/roles`. This plan is Wave 1 — every downstream backend route consumer (Plan 96-02, 96-03) either inherits the new base paths through these helpers or is fixed in the same wave for direct literals.

Purpose: Fold the fleet-tree rewrite at the lowest-level path-helper source so callers inherit — do NOT scatter the rewrite across 100+ callsites when a single source of truth exists (per D-06 no-dual-path and per RESEARCH.md's "Wave 1: Single-source path helpers" recommendation). Two files carry the majority of leverage: `per-identity-file.ts` (SFTP wire) and `identity-artifact-reader.ts` (LOCAL branch reads + REMOTE SSH strings). Together they resolve all identity/role artifact reads and writes originating from Skynet.

Output: Two source files rewritten to the fleet tree, four test files (per-identity-file.test.ts + three identity-artifact-reader tests) with expected paths bumped. All tests green under `npm test -- per-identity-file identity-artifact-reader`.

Implements D-01 (fleet tree root), D-02 (three-siblings layout — identities live at `~/fleet/identities`), D-05 (no grandfathering — new paths ONLY), D-06 (no dual-path fallback in code).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-CONTEXT.md
@.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite per-identity-file.ts (SFTP wire single-source) + its test file</name>
  <files>src/backend/claude-session/per-identity-file.ts, src/backend/claude-session/per-identity-file.test.ts</files>
  <read_first>
    - src/backend/claude-session/per-identity-file.ts (full file — small, ~200 lines)
    - src/backend/claude-session/per-identity-file.test.ts (full file)
    - .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-RESEARCH.md (`src/backend/claude-session/per-identity-file.ts` section — line 243–260 of RESEARCH.md — for the exact line list)
  </read_first>
  <action>
    In `src/backend/claude-session/per-identity-file.ts`:
    - Line 103 (`localTargetPath` return): change `path.join(os.homedir(), ".claude", "identities", name, relPath)` to `path.join(os.homedir(), "fleet", "identities", name, relPath)`.
    - Line 123 (`remoteTargetPath` return): change the template literal from `$HOME/.claude/identities/${name}/${relPath}` to `$HOME/fleet/identities/${name}/${relPath}`.
    - Lines 35, 110, 158 (JSDoc/comments referencing the old path): update in place so the doc matches source of truth. No implementation of a dual-path fallback — D-06.
    - Do NOT modify `ALLOWED_REL_PATHS` (whitelist covers `relay.json` and `.pinned` only — Phase 96 does not extend it per RESEARCH.md Trap 7).

    In `src/backend/claude-session/per-identity-file.test.ts`:
    - Update every assertion currently expecting `$HOME/.claude/identities/...` or `/home/*/\.claude/identities/...` to expect `$HOME/fleet/identities/...` / `/home/*/fleet/identities/...`. Per RESEARCH.md the hits are approximately lines 303, 354, 361, 375, 430, 540 — verify by reading the file and update every one. Use exact-string replacement, not regex, to keep the diff auditable.

    Verify no `\.claude/identities` substring remains in either file after edit.

    This implements D-01/D-05/D-06 at the SFTP write single source. Every consumer of `writeIdentityFile` and `removeIdentityFile` (identity-birth-orchestrator.ts Step 8, user-preferences.ts, identities.ts) picks up the new destination without further per-caller changes.
  </action>
  <verify>
    <automated>grep -n "\.claude/identities" src/backend/claude-session/per-identity-file.ts src/backend/claude-session/per-identity-file.test.ts | grep -v '^#' ; test $? -eq 1 &amp;&amp; npm test -- src/backend/claude-session/per-identity-file.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\.claude/identities" src/backend/claude-session/per-identity-file.ts` returns 0
    - `grep -c "\.claude/identities" src/backend/claude-session/per-identity-file.test.ts` returns 0
    - `grep -c "fleet/identities" src/backend/claude-session/per-identity-file.ts` returns ≥ 2 (localTargetPath body + remoteTargetPath body, and any updated comment)
    - `npm test -- src/backend/claude-session/per-identity-file.test.ts` exits 0 with all cases passing
  </acceptance_criteria>
  <done>
    per-identity-file.ts writes to the fleet tree on both LOCAL and REMOTE branches; its test file asserts the fleet paths and passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite identity-artifact-reader.ts (LOCAL roots + all REMOTE SSH templates) + its three path-string tests</name>
  <files>src/backend/claude-session/identity-artifact-reader.ts, src/backend/claude-session/identity-artifact-reader.two-step.test.ts, src/backend/claude-session/identity-artifact-reader.include-archived.test.ts, src/backend/claude-session/identity-artifact-reader.role-cosmetics.test.ts</files>
  <read_first>
    - src/backend/claude-session/identity-artifact-reader.ts (large — read via targeted grep first: `grep -n "\.claude/identities\|\.claude/roles" src/backend/claude-session/identity-artifact-reader.ts` to enumerate every hit line before editing)
    - src/backend/claude-session/identity-artifact-reader.two-step.test.ts
    - src/backend/claude-session/identity-artifact-reader.include-archived.test.ts
    - src/backend/claude-session/identity-artifact-reader.role-cosmetics.test.ts
    - .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-RESEARCH.md (`src/backend/claude-session/identity-artifact-reader.ts` section — lines 263–294 of RESEARCH.md — enumerates the two root functions plus the remote SSH template categories)
  </read_first>
  <action>
    In `src/backend/claude-session/identity-artifact-reader.ts`:
    - Lines 217–221 (`getLocalIdentitiesRoot`): change the fallback branch from `path.join(os.homedir(), ".claude", "identities")` to `path.join(os.homedir(), "fleet", "identities")`. The env-var override (`process.env.IDENTITIES_HOST_DIR`) STAYS — it is the vitest test-seam, not a production dual-path (per RESEARCH.md note lines 285 + Trap 6). Update any adjacent comment referencing the old default.
    - Lines 237–240 (`getLocalRolesRoot`): change the fallback branch from `path.join(os.homedir(), ".claude", "roles")` to `path.join(os.homedir(), "fleet", "roles")`. Env-var override (`process.env.ROLES_HOST_DIR`) preserved. Update adjacent comment.
    - REMOTE SSH template literals — enumerate via grep BEFORE editing, then rewrite every `$HOME/.claude/identities/...`, `$HOME/.claude/roles/...`, `${remoteHome}/.claude/identities/...`, `${remoteHome}/.claude/roles/...` to the corresponding `fleet/identities` / `fleet/roles` form. RESEARCH.md flags approximate lines: 1611, 1682, 1744, 1809, 2115, 2220, 2471, 2556, 2561, 2627, 2655, 2683, 2750, 2817 — but the grep enumeration is the authoritative list. Do NOT trust the RESEARCH.md line numbers blind; re-run grep on the current file. Use exact-string replacement per line, not a global sed on the file (some paths in comment blocks reference "the pre-migration state as history" — preserve historical comments if they are clearly historical narrative, rewrite functional templates and doc-comments describing current behavior). If in doubt whether a hit is historical or live, look at the surrounding code — if the string is inside backtick template used in an `exec`/`ssh` call, it is LIVE and must be rewritten.
    - Update remaining prose/JSDoc `.claude/identities` / `.claude/roles` mentions describing current behavior (line 226 area comment on getLocalIdentitiesRoot's default, line 246 area comment on getLocalRolesRoot's default, any doc summarizing the function contract).

    In the three test files (`identity-artifact-reader.two-step.test.ts`, `identity-artifact-reader.include-archived.test.ts`, `identity-artifact-reader.role-cosmetics.test.ts`):
    - Enumerate hits: `grep -n "\.claude/identities\|\.claude/roles" <file>` on each.
    - For each hit, decide: if it is an assertion string checking the command sent to SSH (e.g. `expect(cmd).toContain("cat \"$HOME/.claude/identities/...")`), update to the fleet path. If it is `process.env.IDENTITIES_HOST_DIR = tmpDir` setup that just injects a scratch dir, LEAVE UNCHANGED (the env seam is preserved).
    - Approximate hit locations (from RESEARCH.md): two-step.test.ts lines 172, 175, 178, 181, 201, 217, 221, 222, 247, 248, 323, 336; include-archived.test.ts lines 169, 186, 209, 221, 243, 264; role-cosmetics.test.ts lines 153, 165, 169, 174. Grep to confirm the actual list before editing.

    Do NOT touch the six identity-artifact-reader.*.test.ts files that only use `IDENTITIES_HOST_DIR` as a test-seam and contain no path-string assertions (wakeup-crud, write-bounty-pinned, delete-bounty, remote-writes, count-bounties, archive-bounty, empty-bounties-remote, role-file, role-wakeups, write-bounty-status, write-role-file-by-name — per the grep result these may contain scattered hits; audit each with grep and only rewrite files with real assertion strings on identity/role paths, leaving test-seam-only files alone). If a test-seam-only file has ONE stray comment or docstring using the old path, that is fine to leave for Plan 96-07 grep-clean to catch.
  </action>
  <verify>
    <automated>grep -cE "\.claude/(identities|roles)" src/backend/claude-session/identity-artifact-reader.ts | grep -q '^0$' &amp;&amp; npm test -- identity-artifact-reader</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\.claude/identities\|\.claude/roles" src/backend/claude-session/identity-artifact-reader.ts` returns 0 (or only counts historical-narrative comment blocks explicitly retained; if any hit remains it MUST be inside a `//` comment that describes historical state and is enumerated in the task summary)
    - `getLocalIdentitiesRoot` fallback branch in the source contains the literal `"fleet", "identities"` (grep: `grep -c "\"fleet\", \"identities\"" src/backend/claude-session/identity-artifact-reader.ts` returns ≥ 1)
    - `getLocalRolesRoot` fallback branch contains the literal `"fleet", "roles"`
    - `npm test -- identity-artifact-reader` exits 0 (all identity-artifact-reader.*.test.ts files pass with new paths)
  </acceptance_criteria>
  <done>
    identity-artifact-reader.ts LOCAL branches read from `~/fleet/identities` / `~/fleet/roles`; all REMOTE SSH template literals target the fleet tree; three path-assertion tests pass; env-var seams preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skynet backend → managed host (SSH) | Backend crafts SSH command strings interpolating identity name and role name into paths |
| Skynet backend → SFTP writer | Backend writes files (relay.json, avatars) via SFTP to a computed remote path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-01 | Tampering | remote SSH template literals in identity-artifact-reader.ts | mitigate | All interpolated identity/role names already pass through host-side quoting (`quotedName` pattern) — no new escaping needed. Verify the rewritten templates preserve existing quoting; do not simplify a `${quotedX}` to `${x}`. |
| T-96-02 | Elevation of Privilege | remoteTargetPath in per-identity-file.ts | accept | Path change alone does not change auth boundary; SFTP session still uses the identity-scoped SSH connection. Existing ALLOWED_REL_PATHS whitelist is unchanged. |
| T-96-03 | Information disclosure | historical `.claude/identities` prose left in comments | accept | Comments in this file describing historical behavior are not machine-readable and pose no runtime risk; Plan 96-07 grep-clean audits will surface any accidental live-code hits. |
</threat_model>

<verification>
- Both source files free of `\.claude/(identities|roles)` outside intentionally-retained historical comments
- npm test for both files' test suites green
- No new files created (pure edit-in-place); no ALLOWED_REL_PATHS extension; no env-var seams removed
</verification>

<success_criteria>
- per-identity-file.ts single-source SFTP wire targets `~/fleet/identities/<name>/<relPath>` for both localTargetPath and remoteTargetPath
- identity-artifact-reader.ts getLocalIdentitiesRoot()/getLocalRolesRoot() fallbacks are `~/fleet/identities` / `~/fleet/roles`; every REMOTE SSH template rewritten
- Test files for both source files pass with fleet-tree assertions
- Wave 2 (routes fan-out) can now proceed; the single-source-of-truth changes are landed
</success_criteria>

<output>
Create `.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-01-SUMMARY.md` documenting:
- Exact set of edits made in per-identity-file.ts (line numbers before/after)
- Exact set of edits made in identity-artifact-reader.ts (grouped: two root functions + count of REMOTE SSH template literals rewritten + count of prose/JSDoc updates)
- Enumerated line count of tests updated in each of the four test files
- Any historical-narrative comment left intentionally unchanged (with justification)
- Verification output from the grep and npm test commands
</output>
</content>
</invoke>