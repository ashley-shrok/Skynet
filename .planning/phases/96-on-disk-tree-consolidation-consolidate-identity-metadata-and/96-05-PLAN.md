---
phase: 96-on-disk-tree-consolidation-consolidate-identity-metadata-and
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - substrate/skills/id/SKILL.md
  - substrate/skills/id/coordinator-instructions.md
  - substrate/skills/id/clone-picker-prompt.md
  - substrate/skills/id/actor-status-prompt.md
  - substrate/skills/agent-relay/SKILL.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "substrate/skills/id/SKILL.md — every ~/.claude/identities/<name>/ and ~/.claude/roles/<role>/ prose reference is rewritten to ~/fleet/identities/<name>/ and ~/fleet/roles/<role>/ (plain prose and shell snippets embedded in the skill body)"
    - "substrate/skills/id/SKILL.md on-disk file layout section documents workspace/ as a new sub-part of every identity folder (D-04)"
    - "substrate/skills/id/SKILL.md STATE_DIR setup block writes STATE_DIR=~/fleet/identities/<name>/relay-state so recv.sh cred-path derivation works"
    - "substrate/skills/id/SKILL.md wakeup-scheduler / context-watch / role-file-watch launch commands pass the identity folder as ~/fleet/identities/<name> and role folder as ~/fleet/roles/<role>"
    - "substrate/skills/id/coordinator-instructions.md new-identity creation flow includes mkdir -p ~/fleet/identities/<name>/workspace alongside the existing wakeups mkdir (D-04 + orchestrator-resolved-Q4)"
    - "substrate/skills/id/coordinator-instructions.md every path prose reference rewritten to fleet tree"
    - "substrate/skills/id/clone-picker-prompt.md all path references rewritten"
    - "substrate/skills/id/actor-status-prompt.md all path references rewritten"
    - "substrate/skills/agent-relay/SKILL.md 6 identity-path references and 1 skynet-<name> reference at line 481 rewritten (line 481 references maintainer local working tree ~/skynet-<name>/substrate/skills/agent-relay/ — rewrite to ~/fleet/identities/<name>/workspace/substrate/skills/agent-relay/ per orchestrator-resolved-Q1)"
    - "~/.local/bin/, ~/.claude/skills/, ~/.claude/CLAUDE.md references RETAINED per D-12/D-13/D-14"
  artifacts:
    - path: "substrate/skills/id/SKILL.md"
      provides: "The id-skill body — load-bearing prose read by every identity at session-start"
      min_lines: 1500
    - path: "substrate/skills/id/coordinator-instructions.md"
      provides: "Coordinator dispatch companion — new-identity creation flow lives here"
      contains: "mkdir -p ~/fleet/identities/"
    - path: "substrate/skills/agent-relay/SKILL.md"
      provides: "agent-relay skill body — recv.sh operational prose"
  key_links:
    - from: "substrate/skills/id/coordinator-instructions.md new-identity flow (around lines 278–297)"
      to: "the 7-step identity creation block"
      via: "adding mkdir -p workspace/ alongside mkdir -p wakeups"
      pattern: "mkdir -p ~/fleet/identities/<name>/workspace"
    - from: "substrate/skills/id/SKILL.md STATE_DIR set"
      to: "recv.sh cred-path derivation"
      via: "dirname(STATE_DIR) lookup for relay.json"
      pattern: "STATE_DIR=~/fleet/identities/"
---

<objective>
Rewrite the substrate skill body plus 3 coordinator/actor companion files plus agent-relay SKILL.md — every prose and shell-snippet reference to `~/.claude/identities/`, `~/.claude/roles/`, and `~/skynet-<name>/` moves to the fleet tree. Additionally, the coordinator-instructions.md new-identity creation flow gains an `mkdir -p workspace/` step (per D-04 + orchestrator-resolved-Q4) so every newly-birthed identity has an empty workspace sub-part from day one — matching the tree shape the migration runbook produces for existing identities.

Purpose: The id-skill body is the load-bearing prose an identity reads every session; its path references are what the identity's own agent replays when it does `/id save`, wakeup handling, bounty work. Coordinator-instructions.md is the dispatch/creation companion — the mkdir workspace/ addition ensures new-identity tree shape is consistent. clone-picker and actor-status prompts are smaller sub-agent prompts with a handful of path refs. agent-relay/SKILL.md carries the recv.sh operational prose plus the substrate-editing-workflow prose (line 481) that references the maintainer's local working tree. Parallel with Plans 96-01/96-02/96-03/96-04 (disjoint files).

Output: 5 files rewritten. Grep confirms zero `.claude/identities` / `.claude/roles` / `skynet-<name>` outside the D-12/D-13/D-14 retained set (CLAUDE.md, skills/, local/bin/).

Implements D-01, D-02, D-04 (workspace sub-part introduced in coordinator instructions and documented in SKILL.md on-disk layout section), D-11 (canonical file list), D-12/D-13/D-14 (retained references).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-CONTEXT.md
@.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite substrate/skills/id/SKILL.md — path references, workspace sub-part in on-disk layout, STATE_DIR fleet path</name>
  <files>substrate/skills/id/SKILL.md</files>
  <read_first>
    - substrate/skills/id/SKILL.md (targeted — file is ~1500+ lines. First enumerate: grep -n "\\.claude/identities\\|\\.claude/roles\\|skynet-<name>" substrate/skills/id/SKILL.md. Then read each hit's surrounding 5–10 lines to preserve context)
    - .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-RESEARCH.md (substrate/skills/id/SKILL.md section — the sampled hits table with approximate lines 19, 23, 39, 65, 241, 260, 264, 271, 343–344, 389–390, 397, 418, 423, 477, 537, 566–568, 605, 636, 642/646, 667, 671, 690, 727–728, 826, 829, 972/976, 981, 1007–1008, 1019/1051, 1074/1083, 1109, 1164, 1315–1317, 1342, 1420/1424, 1454–1455)
  </read_first>
  <action>
    Enumerate hits with grep. Expect ~50 hits per RESEARCH.md.

    For every hit, rewrite the path prefix while preserving surrounding markdown, code fences, backticks, and template variables (`<name>`, `<role>`, `$name`, `${name}` etc.):
    - `~/.claude/identities/` becomes `~/fleet/identities/`
    - `~/.claude/roles/` becomes `~/fleet/roles/`
    - `$HOME/.claude/identities/` becomes `$HOME/fleet/identities/`
    - `$HOME/.claude/roles/` becomes `$HOME/fleet/roles/`

    Specific attention points:

    1. On-disk file layout section (approximately lines 1074/1083). This section documents where identity/role state lives. In addition to path rewrites, ADD `workspace/` as a documented sub-part of every identity folder, alongside `wakeups/`, `ctxwatch/`, `role-file-watch/`, `relay-state/`. Add short prose describing workspace as "the identity's working directory — generic, no repo-leaning; for maintainer identities it holds the repo they maintain, for other identities it may hold anything or nothing (D-04)." Do NOT rename this section; just extend the listed sub-parts.

    2. STATE_DIR setup block (approximately lines 566–568). Where the skill body sets up recv.sh's STATE_DIR, ensure the value written is `~/fleet/identities/<name>/relay-state`. This is what makes recv.sh's `$(dirname "$STATE_DIR")` cred-path derivation naturally land on the new tree (per RESEARCH.md Trap 8).

    3. Launch command snippets (approximate lines 605, 636, 690). These invoke wakeup-scheduler, context-watch, role-file-watch with the identity folder as an argument (e.g. `python3 ~/.local/bin/wakeup-scheduler ~/.claude/identities/<name>`). Rewrite the identity-folder argument to `~/fleet/identities/<name>` (and role-folder argument to `~/fleet/roles/<role>` where applicable). Do NOT touch `~/.local/bin/` — RETAINED per D-14.

    4. Retained references — do NOT rewrite:
       - Any `~/.local/bin/...` reference (D-14)
       - Any `~/.claude/skills/...` reference (D-13; skill install paths STAY)
       - Any `~/.claude/CLAUDE.md` reference (D-12; user-wide Claude config stays)

    Rewrite technique: use exact-string replacements. Do NOT use a global sed on the file — the density of context-sensitive edits (some references appear inside prose paragraphs, some inside fenced bash blocks, some inside YAML frontmatter examples) makes hand-audited replacements safer. If a bulk sed is used, follow with a grep-verify pass. If any prose describes "before Phase 96" state (historical narrative), LEAVE it and note in the SUMMARY.

    After edits, verify:
    - Legacy path substring count in the file (excluding retained set) reaches zero
    - The on-disk-layout section documents `workspace/` as a sub-part
    - STATE_DIR setup block writes to `~/fleet/identities/<name>/relay-state`
  </action>
  <verify>
    <automated>bash -c 'c=$(grep -c "\.claude/identities\|\.claude/roles" substrate/skills/id/SKILL.md); [ "$c" -eq 0 ] && grep -q "workspace/" substrate/skills/id/SKILL.md && grep -q "~/fleet/identities" substrate/skills/id/SKILL.md'</automated>
  </verify>
  <acceptance_criteria>
    - Grep for `\.claude/(identities|roles)` in SKILL.md returns 0 (any historical narrative is documented in SUMMARY)
    - Grep for `~/fleet/identities` returns at least 20 (roughly matching the pre-migration `~/.claude/identities` hit count)
    - The on-disk-layout section explicitly lists `workspace/` as a sub-part of the identity folder
    - STATE_DIR setup block in SKILL.md contains `~/fleet/identities/<name>/relay-state`
    - `~/.claude/CLAUDE.md`, `~/.claude/skills/`, `~/.local/bin/` references are unchanged (retained per D-12/D-13/D-14)
  </acceptance_criteria>
  <done>
    SKILL.md fully rewritten to fleet tree; workspace/ sub-part documented; STATE_DIR block aligned with recv.sh cred-path derivation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite coordinator-instructions.md + clone-picker-prompt.md + actor-status-prompt.md + agent-relay/SKILL.md — path rewrites + mkdir workspace/ step</name>
  <files>substrate/skills/id/coordinator-instructions.md, substrate/skills/id/clone-picker-prompt.md, substrate/skills/id/actor-status-prompt.md, substrate/skills/agent-relay/SKILL.md</files>
  <read_first>
    - substrate/skills/id/coordinator-instructions.md (full — file has ~430 lines; enumerate hits with grep first: `grep -n "\\.claude/identities\\|\\.claude/roles" substrate/skills/id/coordinator-instructions.md`)
    - substrate/skills/id/clone-picker-prompt.md (small — read fully)
    - substrate/skills/id/actor-status-prompt.md (small — read fully)
    - substrate/skills/agent-relay/SKILL.md (targeted — enumerate hits with `grep -n "\\.claude/identities\\|\\.claude/roles\\|skynet-<name>" substrate/skills/agent-relay/SKILL.md`; expect 7 hits per orchestrator-resolved-Q1)
    - .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-RESEARCH.md (per-file sections for each of these four skill files + orchestrator-resolved-Q1 + Trap 9 around lines 946–948)
  </read_first>
  <action>
    **substrate/skills/id/coordinator-instructions.md:**
    - Rewrite every hit per RESEARCH.md's approximate lines 94, 98, 232, 278–280, 290–291, 297, 319, 342, 386, 415, 420–421. Prefixes: `~/.claude/identities/` becomes `~/fleet/identities/`; `~/.claude/roles/` becomes `~/fleet/roles/`. Do NOT touch `~/.local/bin/wakeup-scheduler` (line 415) — the binary path stays.
    - New-identity creation flow (approximately lines 278–297) is a 7-step block that creates a new identity's on-disk shape. Immediately AFTER the `mkdir -p ~/fleet/identities/<name>/wakeups` step (currently at line ~278), ADD a new step: `mkdir -p ~/fleet/identities/<name>/workspace`. Update the surrounding numbered-step list so numbering flows correctly (i.e. the workspace mkdir becomes step 2b or a new numbered step, and subsequent steps renumber if needed). Include a brief comment/note on the step describing the workspace as "generic working directory per D-04 — empty at birth; for maintainer identities it will later contain the repo they maintain; for others it may hold anything or nothing".

    **substrate/skills/id/clone-picker-prompt.md:**
    - Rewrite hits per RESEARCH.md approximate lines 17, 19, 37. Only path rewrites; no structural changes.

    **substrate/skills/id/actor-status-prompt.md:**
    - Rewrite hits per RESEARCH.md approximate lines 13, 15, 33. Only path rewrites.

    **substrate/skills/agent-relay/SKILL.md** (orchestrator-resolved-Q1: 7 legacy references confirmed grep-clean):
    - Enumerate hits with grep. Confirmed hits from orchestrator resolution: lines 27, 110, 166, 206, 208, 209, 481.
    - Lines 27, 110, 166, 206, 208, 209: standard `~/.claude/identities/<name>/` prose rewrites to `~/fleet/identities/<name>/`.
    - Line 481: `~/skynet-<name>/substrate/skills/agent-relay/` — this references the maintainer's LOCAL working tree, which after Shape 3 lives at `~/fleet/identities/<name>/workspace/substrate/skills/agent-relay/`. Rewrite to that fleet-workspace path per orchestrator-resolved-Q1.

    Do NOT touch any `~/.claude/skills/agent-relay/` reference (that is the install location, RETAINED per D-13). Verify grep before-and-after: `grep -c "\.claude/skills/" substrate/skills/agent-relay/SKILL.md` should be unchanged.

    Post-edit verification:
    - `grep -c "\.claude/identities\|\.claude/roles" <each file>` returns 0 outside retained references.
    - `grep -c "skynet-<name>" substrate/skills/agent-relay/SKILL.md` returns 0.
    - `grep -c "mkdir -p ~/fleet/identities/<name>/workspace" substrate/skills/id/coordinator-instructions.md` returns ≥ 1.
  </action>
  <verify>
    <automated>bash -c 'for f in substrate/skills/id/coordinator-instructions.md substrate/skills/id/clone-picker-prompt.md substrate/skills/id/actor-status-prompt.md substrate/skills/agent-relay/SKILL.md; do c=$(grep -c "\.claude/identities\|\.claude/roles" "$f"); [ "$c" -eq 0 ] || { echo "FAIL: $f has $c legacy hits"; exit 1; }; done && grep -q "mkdir -p ~/fleet/identities/<name>/workspace" substrate/skills/id/coordinator-instructions.md && ! grep -q "skynet-<name>" substrate/skills/agent-relay/SKILL.md'</automated>
  </verify>
  <acceptance_criteria>
    - Grep for `\.claude/(identities|roles)` in all four files returns 0
    - coordinator-instructions.md contains the literal `mkdir -p ~/fleet/identities/<name>/workspace` step
    - agent-relay/SKILL.md line 481 area references `~/fleet/identities/<name>/workspace/substrate/skills/agent-relay/` (not `~/skynet-<name>/substrate/...`)
    - agent-relay/SKILL.md `\.claude/skills/agent-relay/` references count is unchanged (D-13 install-location retained)
    - `~/.local/bin/wakeup-scheduler` reference in coordinator-instructions.md line 415 is unchanged (D-14 retained)
  </acceptance_criteria>
  <done>
    All four companion files rewritten to fleet tree; workspace/ mkdir step added to coordinator new-identity flow; agent-relay line 481 maintainer-tree reference migrated to fleet workspace path.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill body prose → identity agent | An identity's session reads skill body verbatim and follows its shell snippets; a wrong path in the body causes the identity to write state to the wrong location |
| coordinator new-identity creation flow → filesystem | Coordinator runs mkdir/touch/write commands to bootstrap a new identity's on-disk tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-12 | Tampering | skill body path prose | mitigate | Hand-audited edits per hit; grep verify zero legacy paths post-edit; no fenced-code-block replacement uses a global regex |
| T-96-13 | Repudiation | historical narrative in skill body | accept | Any legitimate historical-narrative paragraph describing pre-migration state stays and is enumerated in SUMMARY |
| T-96-14 | Elevation of Privilege | mkdir workspace/ new step | accept | mkdir under identity's own folder tree; user-owned; no new writable location |
</threat_model>

<verification>
- All 5 files grep-clean for `\.claude/(identities|roles)` outside retained references
- coordinator-instructions.md contains the workspace mkdir step
- SKILL.md on-disk-layout section documents workspace/ sub-part
- agent-relay SKILL.md line 481 no longer references `~/skynet-<name>/`
- Retained references (CLAUDE.md, skills/, local/bin/) unchanged
</verification>

<success_criteria>
- 5 files rewritten
- workspace/ documented in SKILL.md on-disk layout section
- workspace/ mkdir step added to coordinator-instructions new-identity flow
- Zero `~/skynet-<name>/` remains anywhere in the touched set
- D-12/D-13/D-14 retained references preserved
</success_criteria>

<output>
Create `.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-05-SUMMARY.md` documenting:
- SKILL.md: count of path-rewrite edits by section (prose, shell snippets, on-disk layout, STATE_DIR setup, launch commands)
- The exact prose added for workspace/ in the on-disk-layout section
- coordinator-instructions.md: full text of the new mkdir workspace/ step and where it was inserted in the numbered flow
- clone-picker-prompt.md + actor-status-prompt.md: line counts of rewrites
- agent-relay/SKILL.md: exact rewrite for line 481 (before/after)
- Any historical-narrative paragraph preserved with legacy paths (with justification)
- Verification output from grep commands
</output>
</content>
