---
phase: 96-on-disk-tree-consolidation-consolidate-identity-metadata-and
plan: 02
type: execute
wave: 2
depends_on: ["96-01"]
files_modified:
  - src/backend/database/routes/relay-pointer.ts
  - src/backend/database/routes/relay-pointer.test.ts
  - src/backend/database/routes/runbooks-editor.ts
  - src/backend/database/routes/identity-birth-orchestrator.ts
  - src/backend/database/routes/identity-birth-orchestrator.test.ts
  - src/backend/database/routes/identity-birth-orchestrator.role-frontmatter.test.ts
  - src/backend/database/routes/identity-clone.ts
  - src/backend/database/routes/identity-clone.test.ts
  - src/backend/database/routes/roles-create.ts
  - src/backend/database/routes/roles-create.test.ts
  - src/backend/database/routes/roles-list-for-host.ts
  - src/backend/database/routes/identity-exists-on-host.ts
  - src/backend/database/routes/identity-exists-on-host.test.ts
  - src/backend/database/routes/identity-no-dormancy.ts
  - src/backend/database/routes/identity-no-dormancy.test.ts
  - src/backend/database/routes/identities.ts
  - src/backend/database/routes/identities.put-disk.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "relay-pointer.ts WHITELIST_REGEX admits ONLY fleet-tree paths (`^/home/[a-z0-9_-]+/fleet/identities/...`); legacy `.claude/identities` paths are REJECTED by the regex"
    - "Every route file in the touched set constructs its SSH command strings against `$HOME/fleet/identities/...` / `$HOME/fleet/roles/...` / `${remoteHome}/fleet/...` — no `.claude/identities` or `.claude/roles` substring remains outside intentional historical comments"
    - "runbooks-editor.ts ROLE_ROOT_REL constant is `\"fleet/roles\"`; the 7 `${remoteHome}/${ROLE_ROOT_REL}/${role}` compositions all inherit the new root"
    - "Every test file in the touched set asserts fleet-tree command strings; `npm test` for each is green"
  artifacts:
    - path: "src/backend/database/routes/relay-pointer.ts"
      provides: "SSRF-guarded relay-pointer file read; WHITELIST_REGEX is the security gate"
      contains: "fleet\\\\/identities"
    - path: "src/backend/database/routes/runbooks-editor.ts"
      provides: "Role runbook read/write; ROLE_ROOT_REL constant is the single leverage point"
      contains: "ROLE_ROOT_REL = \"fleet/roles\""
  key_links:
    - from: "src/backend/database/routes/relay-pointer.ts WHITELIST_REGEX"
      to: "relay-pointer.test.ts test cases"
      via: "regex-vs-path-string assertions"
      pattern: "/home/[a-z0-9_-]+/fleet/identities/"
    - from: "src/backend/database/routes/runbooks-editor.ts ROLE_ROOT_REL"
      to: "7 composition sites (lines 318, 477, 641, 864, 1070, 1268, 1435 per RESEARCH.md)"
      via: "${remoteHome}/${ROLE_ROOT_REL}/${role}"
      pattern: "ROLE_ROOT_REL"
---

<objective>
Rewrite every functional path literal in the 10 Skynet backend route files that carry direct `~/.claude/identities` or `~/.claude/roles` references in SSH command strings, plus update their test assertions. Includes the security-critical `relay-pointer.ts` WHITELIST_REGEX rewrite (D-06 hardening — after ship, only fleet paths are valid).

Purpose: Fan out from Wave 1's single-source helpers to the remaining direct-literal callsites. Each route file constructs its own SSH command strings (mkdir, touch, rm, test-e, stat) rather than going through per-identity-file.ts's helper. Rewrites are mechanical exact-string substitutions per RESEARCH.md's per-file enumeration.

Output: 10 route source files + 8 route test files rewritten to fleet tree. All `.claude/identities` / `.claude/roles` gone from these files. `npm test -- src/backend/database/routes/` green.

Implements D-01/D-02/D-05/D-06 across the routes fan-out surface. Locks in the D-06 no-dual-path constraint at the WHITELIST_REGEX (relay-pointer.ts) which is a security gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-CONTEXT.md
@.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-RESEARCH.md
@.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite relay-pointer.ts WHITELIST_REGEX (security gate) + all six test cases</name>
  <files>src/backend/database/routes/relay-pointer.ts, src/backend/database/routes/relay-pointer.test.ts</files>
  <read_first>
    - src/backend/database/routes/relay-pointer.ts (full — small file, focus on line 42 WHITELIST_REGEX)
    - src/backend/database/routes/relay-pointer.test.ts (full — read all test cases, esp. approximate lines 161, 163, 168, 178, 183, 188)
    - .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-RESEARCH.md (`src/backend/database/routes/relay-pointer.ts` section around line 422–433 + Trap 4 around line 921–928)
  </read_first>
  <action>
    In `src/backend/database/routes/relay-pointer.ts`:
    - Line ~42, WHITELIST_REGEX: replace the existing pattern with:
      `^/home/[a-z0-9_-]+/fleet/identities/[a-z0-9_-]+/relay-state/messages/[A-Za-z0-9_-]+\.txt$`
      — the ONLY structural change is `\.claude\/identities\/` → `fleet\/identities\/`. Preserve every other character (anchors, character classes, capture / non-capture semantics, file extension `\.txt$`, `[a-z0-9_-]+` for username segment, `[a-z0-9_-]+` for identity segment, `[A-Za-z0-9_-]+` for event id).
    - D-06: this is a HARD reject of legacy `.claude/identities` paths — no compatibility branch, no OR-alternation admitting both trees. After migration, only fleet paths are valid; the old paths on disk are deleted per D-07 step 7.
    - Update any adjacent comment describing the whitelist scope.

    In `src/backend/database/routes/relay-pointer.test.ts`:
    - Every test case that constructs an example path string (`/home/ubuntu/.claude/identities/tina/relay-state/messages/abc.txt` etc.) — rewrite to `/home/ubuntu/fleet/identities/tina/relay-state/messages/abc.txt`.
    - Approximate hit lines: 161, 163, 168, 178, 183, 188 per RESEARCH.md. Grep the file to confirm and enumerate every occurrence.
    - If a test case is EXPLICITLY testing rejection of a `.claude/identities` legacy path (i.e. the assertion is `expect(WHITELIST_REGEX.test("...claude/identities...")).toBe(false)`), KEEP that test but ADD a comment clarifying that post-Phase-96 the legacy path shape is a rejection surface. Do NOT delete such tests — they document the D-06 hard-cutover.
  </action>
  <verify>
    <automated>grep -n "\.claude/identities" src/backend/database/routes/relay-pointer.ts | grep -v '^[[:space:]]*//' ; test $? -eq 1 &amp;&amp; npm test -- src/backend/database/routes/relay-pointer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fleet/identities" src/backend/database/routes/relay-pointer.ts` returns ≥ 1 (WHITELIST_REGEX body)
    - `grep -c "\.claude/identities" src/backend/database/routes/relay-pointer.ts` returns 0 outside `//` comments (i.e. no live-regex hit; a documentation comment describing what the pre-migration regex looked like is acceptable if clearly labeled)
    - `npm test -- src/backend/database/routes/relay-pointer.test.ts` exits 0
    - Test suite includes at least one assertion that WHITELIST_REGEX.test on a fleet-tree path returns `true`
    - Test suite includes at least one assertion that WHITELIST_REGEX.test on a `.claude/identities` legacy path returns `false` (D-06 hardening) — if absent from the existing test surface, ADD one
  </acceptance_criteria>
  <done>
    relay-pointer WHITELIST_REGEX admits fleet-tree paths and rejects legacy `.claude/identities` paths; tests pass; security gate matches the D-06 hard-cutover shape.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite runbooks-editor.ts + six remaining route files with SSH-string path literals + their tests</name>
  <files>src/backend/database/routes/runbooks-editor.ts, src/backend/database/routes/identity-birth-orchestrator.ts, src/backend/database/routes/identity-birth-orchestrator.test.ts, src/backend/database/routes/identity-birth-orchestrator.role-frontmatter.test.ts, src/backend/database/routes/identity-clone.ts, src/backend/database/routes/identity-clone.test.ts, src/backend/database/routes/roles-create.ts, src/backend/database/routes/roles-create.test.ts, src/backend/database/routes/roles-list-for-host.ts, src/backend/database/routes/identity-exists-on-host.ts, src/backend/database/routes/identity-exists-on-host.test.ts, src/backend/database/routes/identity-no-dormancy.ts, src/backend/database/routes/identity-no-dormancy.test.ts, src/backend/database/routes/identities.ts, src/backend/database/routes/identities.put-disk.test.ts</files>
  <read_first>
    - .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-RESEARCH.md (per-file sections for each of these routes — grouped in the "Path-Reference Audit" table)
    - Enumerate authoritative live hits with `grep -n "\.claude/identities\|\.claude/roles" src/backend/database/routes/{runbooks-editor,identity-birth-orchestrator,identity-clone,roles-create,roles-list-for-host,identity-exists-on-host,identity-no-dormancy,identities}.ts` before starting
    - For each of the eight source files above, read a targeted window around each grep-flagged line to preserve surrounding shell-quoting semantics
    - identity-birth-orchestrator.mxid-derivation.test.ts is out of scope per orchestrator-resolved-Q3 (no fixture path assertions). Do NOT rewrite; if grep flags a hit, treat as a Plan 96-07 audit target.
    - identity-birth-orchestrator.role-frontmatter.test.ts: read + audit for fixture path assertions per open Q3 in RESEARCH.md; rewrite only if functional strings exist. If clean, leave file untouched.
  </read_first>
  <action>
    Handle each file mechanically per its RESEARCH.md line list:

    **runbooks-editor.ts** — line 122 constant: change `const ROLE_ROOT_REL = ".claude/roles"` to `const ROLE_ROOT_REL = "fleet/roles"`. The 7 composition sites (lines 318, 477, 641, 864, 1070, 1268, 1435) inherit automatically. Update any comment on the constant to reference the new tree.

    **identity-birth-orchestrator.ts**:
    - Line 1073: collision-probe `if [ -d "$HOME/.claude/identities/${opts.name}" ]...` → `if [ -d "$HOME/fleet/identities/${opts.name}" ]...`
    - Line 1133: `const identityDir = \`${remoteHome}/.claude/identities/${opts.name}\`` → `const identityDir = \`${remoteHome}/fleet/identities/${opts.name}\``
    - Do NOT add the `workspace/` mkdir here — that is Plan 96-06's scope for the backend birth-orchestrator (Wave 3, extends Step 2 mkdir to include `workspace/`), and Plan 96-05's scope for the coordinator-instructions.md new-identity flow (Wave 2, prose addition). Both landing points are per resolved-Q4 from RESEARCH.md.
    - Update comment lines 145, 180, 273, 468, 865, 1039, 1089, 1197 (prose only) to reference the fleet tree.
    - Test file `identity-birth-orchestrator.test.ts`: bump lines 1293, 1868, 1900, 2119 (approximate — grep to confirm) from `$HOME/.claude/identities/...` to `$HOME/fleet/identities/...`.
    - `identity-birth-orchestrator.role-frontmatter.test.ts`: grep first; rewrite fixture assertions only if present.

    **identity-clone.ts**:
    - Line 537: `if [ -d "$HOME/.claude/identities/${newName}" ]...` → fleet path
    - Line 569: `mkdir -p "$HOME/.claude/identities/${newName}/wakeups"` → fleet path
    - Line 573: `touch "$HOME/.claude/identities/${newName}/handoff.md"` → fleet path
    - Line 707: `` const targetPath = `${remoteHome}/.claude/identities/${newName}/${newName}.md` `` → fleet path
    - Comments at 46, 50–51, 264, 415 — update prose to fleet tree.
    - Test file `identity-clone.test.ts` line 547: `expect(writeArgs[1]).toBe("/home/ubuntu/.claude/identities/tina-2/tina-2.md")` → `.../fleet/identities/tina-2/tina-2.md`.

    **roles-create.ts**:
    - Lines 462, 486, 490, 545, 573 — rewrite each per the RESEARCH.md table (collision probe, mkdir bounties, touch history.md, targetPath for role .md, avatar write). All `.claude/roles` → `fleet/roles`.
    - Comments at 11, 25, 270 — prose update.
    - Test file `roles-create.test.ts`: grep for any path-string assertions and rewrite.

    **roles-list-for-host.ts**:
    - Line 153: `` `ls -1 "$HOME/.claude/roles" 2>/dev/null || true` `` → fleet path
    - Line 186: `` `echo "===ROLE:${r}===" && cat "$HOME/.claude/roles/${r}/${r}.md"` `` → fleet path
    - Comments 9, 106 — prose update.

    **identity-exists-on-host.ts**:
    - Line 133: `if [ -d "$HOME/.claude/identities/${name}" ]...` → fleet path
    - Comments 8, 10 — prose update.
    - Test `identity-exists-on-host.test.ts` line 346 — bump the toContain string.

    **identity-no-dormancy.ts**:
    - Line 123: `test -e "$HOME/.claude/identities/${key}/.no-dormancy"...` → fleet path
    - Line 227: `mkdir -p "$HOME/.claude/identities/${key}" && touch "$HOME/.claude/identities/${key}/.no-dormancy"` → fleet path
    - Line 228: `rm -f "$HOME/.claude/identities/${key}/.no-dormancy"` → fleet path
    - Test `identity-no-dormancy.test.ts` lines 483, 501 — bump assertions.

    **identities.ts**:
    - Line 701: `rm -f "$HOME/.claude/identities/${identityKey}/${identityKey}.${oldExt}"` → fleet path
    - Line 736: `rm -f "$HOME/.claude/identities/${identityKey}/${canonicalName}"` → fleet path
    - Comment 33 — prose update.
    - Test `identities.put-disk.test.ts` lines 619, 870 — bump toContain assertions.

    For every edit: use exact-string replacement on the specific `.claude/identities` or `.claude/roles` substring; do NOT touch any surrounding shell quoting, escapes, or template-literal expression syntax. Grep after each file to confirm zero legacy hits remain (excluding intentional historical comments).

    Do NOT rewrite `identity-birth-orchestrator.mxid-derivation.test.ts` — orchestrator-resolved-Q3 confirmed it has no fixture path assertions.
  </action>
  <verify>
    <automated>for f in src/backend/database/routes/runbooks-editor.ts src/backend/database/routes/identity-birth-orchestrator.ts src/backend/database/routes/identity-clone.ts src/backend/database/routes/roles-create.ts src/backend/database/routes/roles-list-for-host.ts src/backend/database/routes/identity-exists-on-host.ts src/backend/database/routes/identity-no-dormancy.ts src/backend/database/routes/identities.ts; do c=$(grep -v '^[[:space:]]*//' "$f" | grep -v '^[[:space:]]*\*' | grep -c "\.claude/identities\|\.claude/roles"); [ "$c" = "0" ] || { echo "FAIL: $f has $c legacy hits in non-comment code"; exit 1; }; done &amp;&amp; npm test -- src/backend/database/routes/</automated>
  </verify>
  <acceptance_criteria>
    - For each of the 8 source files, non-comment-line grep for `\.claude/(identities|roles)` returns 0
    - `runbooks-editor.ts` contains the literal `ROLE_ROOT_REL = "fleet/roles"` (or equivalent quote style)
    - `identity-birth-orchestrator.ts` line ~1133 constructs `identityDir` from `${remoteHome}/fleet/identities/${opts.name}`
    - `npm test -- src/backend/database/routes/` exits 0 (all touched route tests pass)
    - `ssh-poll-orchestrator.ts` is NOT modified in this plan (owned by Plan 96-03)
  </acceptance_criteria>
  <done>
    Every direct SSH-string path literal in the eight route files targets the fleet tree; their test assertions match; test suites pass. Wave 2 (substrate scripts in Plans 96-03/04/05) proceeds in parallel; Wave 3 (Plan 96-06 birth-orchestrator workspace/ addition) is unblocked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skynet API → managed host (SSH exec) | Route handlers construct shell command strings interpolating user-controlled `name` params into paths |
| relay-pointer.ts client → filesystem read | WHITELIST_REGEX gates which paths the endpoint may open — SSRF-guard equivalent for local file read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-04 | Tampering | relay-pointer.ts WHITELIST_REGEX rewrite | mitigate | Regex change preserves anchors (`^...$`), character classes, and file-extension suffix — only the path-prefix segment changes. Test suite includes a `.claude/identities`-shaped path → `false` case documenting D-06 hard-reject. |
| T-96-05 | Elevation of Privilege | SSH command strings in route handlers | mitigate | Existing input-validation layer (identity/role name sanitization at route entry, `quotedName` interpolation pattern) is unchanged; path prefix change does not weaken any existing check. Verify each rewrite preserves surrounding quoting. |
| T-96-06 | Information disclosure | rm -f in identities.ts | accept | Path change alone does not widen the deletion surface; the ${identityKey} interpolation continues to route through the same sanitizer. |
</threat_model>

<verification>
- Every touched source file has zero `.claude/(identities|roles)` outside intentional historical comments
- Every touched test file has assertions matching the new fleet-tree paths
- `npm test -- src/backend/database/routes/` green
- Plan 96-03 (ssh-poll-orchestrator) has NOT been touched here
</verification>

<success_criteria>
- 8 route source files rewritten
- 8 route test files rewritten (including relay-pointer test)
- relay-pointer WHITELIST_REGEX admits fleet paths, rejects legacy paths
- runbooks-editor.ts single constant fix propagates to 7 usage sites
- All tests green
</success_criteria>

<output>
Create `.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-02-SUMMARY.md` documenting:
- Per-file edit summary (source file → count of live-code path literals rewritten, count of comment/prose updates)
- Per-test-file summary (assertion count bumped)
- The relay-pointer WHITELIST_REGEX before/after (exact regex string)
- List of files audited but not modified (mxid-derivation.test.ts if clean; role-frontmatter.test.ts if clean)
- Verification output from grep + npm test
</output>
</content>
</invoke>