---
phase: 96-on-disk-tree-consolidation-consolidate-identity-metadata-and
plan: 06
type: execute
wave: 3
depends_on: ["96-01", "96-02"]
files_modified:
  - src/backend/database/routes/identity-birth-orchestrator.ts
  - src/backend/database/routes/identity-birth-orchestrator.test.ts
files_read:
  - .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-02-SUMMARY.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "identity-birth-orchestrator.ts Step 2 remote mkdir at line ~1140 creates BOTH `${identityDir}/wakeups` AND `${identityDir}/workspace` — a fresh identity is born with an empty workspace sub-part per D-04"
    - "The new workspace mkdir is verified via test assertion in identity-birth-orchestrator.test.ts (existing mkdir wakeups test extended, or a new test case for workspace/)"
    - "No dual-path behavior — the workspace mkdir is unconditional on identity creation (D-06)"
    - "The Step 8 relay.json write still routes through writeIdentityFile from per-identity-file.ts — Plan 96-01 already updated that to the fleet tree — so relay.json still lands at `${remoteHome}/fleet/identities/<name>/relay.json` (verify)"
  artifacts:
    - path: "src/backend/database/routes/identity-birth-orchestrator.ts"
      provides: "New-identity birth flow — mkdir remote tree, write .md, write relay.json"
      contains: "workspace"
  key_links:
    - from: "identity-birth-orchestrator.ts Step 2 mkdir"
      to: "the new fleet tree shape (identityDir/wakeups + identityDir/workspace)"
      via: "mkdir -p statement joining both paths in a single command"
      pattern: "mkdir -p .*/wakeups.*workspace"
---

<objective>
Extend the Skynet backend identity-birth-orchestrator's Step 2 remote-mkdir call so it creates the identity's `workspace/` sub-part at birth alongside the existing `wakeups/` mkdir — matching the tree shape the migration runbook produces for existing identities (D-04 + orchestrator-resolved-Q4). Verify the change with a test assertion. Runs in Wave 3 after Plans 96-01 (per-identity-file.ts / identity-artifact-reader.ts single-source updates) and 96-02 (routes fan-out — identity-birth-orchestrator.ts line 1133 `identityDir` already rewritten to fleet path) have landed. This plan makes the additional workspace/ mkdir cognitive addition on top of the fleet-path substrate that Plan 96-02 established.

Purpose: Per D-04, every identity's folder has a `workspace/` sub-part. The migration runbook creates it for existing identities by copying `~/skynet-<name>/` into it. For NEW identities (birthed via the Skynet UI birth flow), the birth orchestrator's Step 2 mkdir is the only place the on-disk tree gets bootstrapped remotely — so it must also create `workspace/`. Coordinator-instructions.md (rewritten in Plan 96-05) similarly gains the mkdir workspace/ step for coordinator-manual identity creation. This plan closes the loop for UI-birthed identities.

Output: identity-birth-orchestrator.ts Step 2 mkdir extended; matching test assertion added. `npm test -- identity-birth-orchestrator` green.

Implements D-04 (workspace as a new sub-part inside every identity's folder — for BIRTH flow, not migration flow). Depends on Plan 96-02 having landed the fleet-path rewrite of line 1133's identityDir.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-CONTEXT.md
@.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-RESEARCH.md
@.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Step 2 mkdir in identity-birth-orchestrator.ts to include workspace/ + add test assertion</name>
  <files>src/backend/database/routes/identity-birth-orchestrator.ts, src/backend/database/routes/identity-birth-orchestrator.test.ts</files>
  <read_first>
    - src/backend/database/routes/identity-birth-orchestrator.ts (targeted — read lines ~1120–1160 covering the identityDir declaration + mkdir command; note that line 1133's identityDir was already updated in Plan 96-02 to the fleet path)
    - src/backend/database/routes/identity-birth-orchestrator.test.ts (targeted — read the test cases around the Step 2 mkdir assertion; grep -n "mkdir.*wakeups\|mkdir.*handoff" src/backend/database/routes/identity-birth-orchestrator.test.ts to find the assertion)
    - .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-02-SUMMARY.md (verify Plan 96-02 already rewrote line 1133 identityDir to the fleet path — if not, block on 96-02 completing)
    - .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-RESEARCH.md (Skynet Backend Identity-Birth Orchestrator section + open Q4 around lines 971–975 + Trap 9 around lines 946–948)
  </read_first>
  <action>
    In `src/backend/database/routes/identity-birth-orchestrator.ts` around line 1140 (Step 2 remote mkdir):

    Current shape after Plan 96-02:
    ```
    const identityDir = `${remoteHome}/fleet/identities/${opts.name}`;
    const identityFilePath = `${identityDir}/${opts.name}.md`;
    // 1. Create the identity folder tree (mkdir wakeups also creates parent).
    await deps.execCommand(
      conn,
      `mkdir -p "${identityDir}/wakeups" && touch "${identityDir}/handoff.md"`,
    );
    ```

    Change the mkdir command to also create `${identityDir}/workspace`. The single-command form:
    ```
    `mkdir -p "${identityDir}/wakeups" "${identityDir}/workspace" && touch "${identityDir}/handoff.md"`
    ```

    Update the preceding comment to note the workspace/ addition:
    ```
    // 1. Create the identity folder tree — wakeups/ + workspace/ (generic working dir per D-04)
    //    plus touch handoff.md. Single mkdir -p covers both sub-parts and the parent.
    ```

    Do NOT modify line 1073's `if [ -d "$HOME/fleet/identities/${opts.name}" ]` collision probe (Plan 96-02 already rewrote it; probe checks whether the identity folder itself exists — workspace/ is a sub-part, not a separate collision surface).

    In `src/backend/database/routes/identity-birth-orchestrator.test.ts`:
    - Find the test case(s) that assert the mkdir command shape (grep `mkdir.*wakeups` in the test file to locate).
    - Extend the assertion(s) to expect the workspace path in the same command. If the existing assertion uses `expect(cmd).toContain("mkdir -p \"$HOME/fleet/identities/agent1/wakeups\"")` (using toContain), also add `expect(cmd).toContain("workspace")` on the same command capture — the `mkdir -p` command now contains both `wakeups` and `workspace` substrings, and both should be verifiable.
    - Alternatively add a new dedicated test case `test("Step 2 creates workspace/ sub-part per D-04", ...)` that inspects the same captured mkdir command and asserts it includes `workspace` as a mkdir target.
    - Preserve the existing test coverage of the handoff.md touch step.

    Post-edit verification:
    - `grep -c "mkdir.*workspace" src/backend/database/routes/identity-birth-orchestrator.ts` returns ≥ 1.
    - `npm test -- src/backend/database/routes/identity-birth-orchestrator.test.ts` exits 0.
  </action>
  <verify>
    <automated>bash -c 'grep -q "workspace" src/backend/database/routes/identity-birth-orchestrator.ts && npm test -- src/backend/database/routes/identity-birth-orchestrator.test.ts'</automated>
  </verify>
  <acceptance_criteria>
    - `identity-birth-orchestrator.ts` Step 2 mkdir command creates `${identityDir}/workspace` alongside `${identityDir}/wakeups` in a single `mkdir -p` invocation
    - Comment on the mkdir step references D-04 or "generic working directory"
    - `identity-birth-orchestrator.test.ts` has at least one assertion checking that the mkdir command includes the workspace path
    - `npm test -- src/backend/database/routes/identity-birth-orchestrator.test.ts` exits 0
    - Line 1133 identityDir remains at `${remoteHome}/fleet/identities/${opts.name}` (from Plan 96-02) — grep confirms no regression
  </acceptance_criteria>
  <done>
    Skynet backend birth flow creates workspace/ sub-part at identity creation time; test coverage extends to verify the addition; consistent with coordinator-instructions.md new-identity flow (Plan 96-05 Task 2).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skynet backend → managed host SSH exec | mkdir -p command interpolates opts.name into a path via identityDir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-15 | Tampering | mkdir workspace/ addition | mitigate | The workspace sub-path is a static literal (`workspace`, not user-controlled). identityDir interpolation goes through the same opts.name sanitization as the existing wakeups mkdir — no new interpolation surface |
| T-96-16 | Denial of Service | mkdir failure | accept | If the workspace mkdir fails, the entire Step 2 fails and the birth flow errors out — no partial-state risk beyond what the existing wakeups mkdir already introduced |
</threat_model>

<verification>
- Step 2 mkdir now creates workspace/ + wakeups/ atomically
- Test assertion covers workspace/ creation
- npm test green
- Line 1133 identityDir unchanged from Plan 96-02's fleet-path rewrite
</verification>

<success_criteria>
- workspace/ sub-part created on every identity birth
- Test asserts the addition
- No regression on prior Plan 96-02 changes to this file
</success_criteria>

<output>
Create `.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-06-SUMMARY.md` documenting:
- Exact before/after of the Step 2 mkdir command
- The test assertion added / extended
- Verification of no regression on Plan 96-02's line 1133 identityDir change
- npm test output
</output>
</content>
