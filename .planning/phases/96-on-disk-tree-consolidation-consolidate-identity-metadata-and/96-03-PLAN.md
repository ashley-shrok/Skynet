---
phase: 96-on-disk-tree-consolidation-consolidate-identity-metadata-and
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - src/backend/fleet-status/ssh-poll-orchestrator.ts
  - src/backend/fleet-status/ssh-poll-orchestrator.test.ts
  - src/backend/claude-session/session-file-parser.outbound-body.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "ssh-poll-orchestrator.ts SSH-exec strings target `~/fleet/identities/...` — no `.claude/identities` substring remains in live SSH command strings"
    - "ssh-poll-orchestrator.test.ts channel.setResponse() fixture keys and toContain assertions target fleet-tree paths — bulk sed rewrite is safe here"
    - "session-file-parser.outbound-body.test.ts fixture path strings match the post-migration relay-send emission shape (fleet tree)"
    - "npm test for both test files is green"
  artifacts:
    - path: "src/backend/fleet-status/ssh-poll-orchestrator.ts"
      provides: "Fleet-status enumeration + dormancy sentinel checks over SSH per host"
      contains: "fleet/identities"
    - path: "src/backend/fleet-status/ssh-poll-orchestrator.test.ts"
      provides: "Test fixtures for every SSH mock response the orchestrator expects"
      min_lines: 6000
  key_links:
    - from: "src/backend/fleet-status/ssh-poll-orchestrator.ts SSH-exec strings"
      to: "test fixtures channel.setResponse(<path substring>, ...)"
      via: "path-substring lookup"
      pattern: "~/fleet/identities/"
---

<objective>
Rewrite `ssh-poll-orchestrator.ts` (~5 functional SSH exec strings + comment prose) and its very large test fixture surface (~100+ path-string fixture keys, ~6000-line test file). Also rewrite the session-file-parser outbound-body test-corpus fixture strings so the parse test data reflects post-migration relay-send emission shape.

Purpose: `ssh-poll-orchestrator.ts` runs identity enumeration + dormancy sentinel stat commands against every managed host. Its path strings do NOT flow through Wave 1's helpers — they are direct SSH-exec template literals. The test file is the largest test surface in Phase 96; a bulk `sed` on the two path substrings is the safest tool. `session-file-parser.outbound-body.test.ts` snapshots captured relay-send shell fragments; after migration, agents emit fleet-tree paths, so the parse-test corpus must match.

Output: Three files rewritten. `npm test -- ssh-poll-orchestrator` and `npm test -- session-file-parser.outbound-body` both green. This plan runs in parallel with 96-02 (routes fan-out) since they touch disjoint files.

Implements D-01/D-02/D-05/D-06 for the fleet-status polling surface. Nothing in this plan depends on Wave 1 outputs — the orchestrator uses its own SSH strings, not per-identity-file helpers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-CONTEXT.md
@.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite ssh-poll-orchestrator.ts (5 SSH-exec strings + comments) + bulk-sed the fixture test file</name>
  <files>src/backend/fleet-status/ssh-poll-orchestrator.ts, src/backend/fleet-status/ssh-poll-orchestrator.test.ts</files>
  <read_first>
    - src/backend/fleet-status/ssh-poll-orchestrator.ts (targeted read — grep first: `grep -n "\.claude/identities\|\.claude/roles" src/backend/fleet-status/ssh-poll-orchestrator.ts` to enumerate the ~5 functional lines + comment lines)
    - src/backend/fleet-status/ssh-poll-orchestrator.test.ts (do NOT full-read — file is ~6000 lines. Use grep to enumerate hits then targeted Read on the small windows where fixtures are declared)
    - .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-RESEARCH.md (`src/backend/fleet-status/ssh-poll-orchestrator.ts` section around lines 297–311)
  </read_first>
  <action>
    In `src/backend/fleet-status/ssh-poll-orchestrator.ts`:
    - Approximate live lines from RESEARCH.md: 998, 1022, 1025, 1028, 1564. Confirm with fresh grep. Rewrite each `~/.claude/identities/...` template literal to `~/fleet/identities/...`. Preserve surrounding quoting (backticks, `${quotedName}`, `${quotedTmuxSession}` interpolations) — do not touch anything except the path prefix substring.
    - Comment lines (approximately 262, 901, 987, 996, 1541 per RESEARCH.md): update prose to reference the fleet tree.

    In `src/backend/fleet-status/ssh-poll-orchestrator.test.ts`:
    - Bulk-sed rewrite is the sanctioned tool here (RESEARCH.md line 311: "This is a very large test file (~6000+ lines) with ~100+ fixture path strings. Bulk `sed` replacement is the right tool.").
    - Execute:
      `sed -i 's|~/\.claude/identities/|~/fleet/identities/|g; s|~/\.claude/roles/|~/fleet/roles/|g; s|\$HOME/\.claude/identities/|$HOME/fleet/identities/|g; s|\$HOME/\.claude/roles/|$HOME/fleet/roles/|g; s|/home/ubuntu/\.claude/identities/|/home/ubuntu/fleet/identities/|g; s|/home/ubuntu/\.claude/roles/|/home/ubuntu/fleet/roles/|g' src/backend/fleet-status/ssh-poll-orchestrator.test.ts`
    - After sed, grep the file: `grep -c "\.claude/identities\|\.claude/roles" src/backend/fleet-status/ssh-poll-orchestrator.test.ts`. Zero hits expected. If non-zero, read the flagged lines and hand-fix any escape variant sed missed (unlikely — the four common forms above cover all documented fixture shapes).
    - Run `npm test -- src/backend/fleet-status/ssh-poll-orchestrator.test.ts`. If any test fails, read the failure output — the mismatch will point at a fixture key that setResponse() looks up by substring; the substring in the SOURCE (rewritten in the first bullet) must exactly match the substring in the TEST (rewritten by sed here). Both should now use `~/fleet/identities/` / `~/fleet/roles/`.
  </action>
  <verify>
    <automated>c=$(grep -v '^[[:space:]]*//' src/backend/fleet-status/ssh-poll-orchestrator.ts | grep -v '^[[:space:]]*\*' | grep -c "\.claude/identities\|\.claude/roles"); [ "$c" = "0" ] || { echo "FAIL: source has $c legacy hits"; exit 1; } &amp;&amp; grep -c "\.claude/identities\|\.claude/roles" src/backend/fleet-status/ssh-poll-orchestrator.test.ts | grep -q '^0$' &amp;&amp; npm test -- src/backend/fleet-status/ssh-poll-orchestrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Non-comment grep on `ssh-poll-orchestrator.ts` for `\.claude/(identities|roles)` returns 0
    - Full-file grep on `ssh-poll-orchestrator.test.ts` for `\.claude/(identities|roles)` returns 0
    - `ssh-poll-orchestrator.ts` grep for `fleet/identities` returns ≥ 5 (the five rewritten functional lines)
    - `npm test -- src/backend/fleet-status/ssh-poll-orchestrator.test.ts` exits 0
  </acceptance_criteria>
  <done>
    ssh-poll-orchestrator SSH commands + test fixtures all target fleet tree; test suite passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite session-file-parser.outbound-body.test.ts fixture path strings</name>
  <files>src/backend/claude-session/session-file-parser.outbound-body.test.ts</files>
  <read_first>
    - src/backend/claude-session/session-file-parser.outbound-body.test.ts (targeted — grep first to enumerate the ~20+ fixture hits; then read each test-case declaration to determine intent — historical-shape parse test vs. current-shape parse test)
    - .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-RESEARCH.md (`src/backend/claude-session/session-file-parser.outbound-body.test.ts` section around lines 456–464 + Trap 5 around lines 929–931)
  </read_first>
  <action>
    RESEARCH.md Trap 5 explicitly flags: "there is a subtlety: if some of these test cases are testing the PARSER's ability to handle old-format commands (compatibility test), updating them would defeat the test's purpose." Default per RESEARCH.md: update all; flag any that are explicitly testing "old format compatibility".

    Procedure:
    1. `grep -n "\.claude/identities\|\.claude/roles\|/home/ubuntu/\.claude" src/backend/claude-session/session-file-parser.outbound-body.test.ts` — enumerate every hit line.
    2. For each hit line, read the surrounding test case (~15 lines up to find the `test(` / `it(` declaration and its description string).
    3. If the test description contains any of "legacy", "old format", "pre-migration", "backwards compat", "compatibility", or similar keywords indicating it is explicitly testing rejection or handling of a legacy path — LEAVE UNCHANGED and note in the SUMMARY that this case was preserved.
    4. Otherwise (test case describes parsing a "current" or "typical" outbound relay-send fragment), rewrite the fixture path from `~/.claude/identities/...` (or `/home/ubuntu/.claude/identities/...`) to `~/fleet/identities/...` (or `/home/ubuntu/fleet/identities/...`).
    5. Approximate lines from RESEARCH.md: 313 and 341 explicitly contain `/home/ubuntu/.claude/roles/...` and `/home/ubuntu/.claude/identities/...` in Python-snippet fixtures — those are current-shape fixtures per the RESEARCH.md verdict "the corpus should reflect real commands as they will look POST-migration. Update all fixture path strings."

    After edits: `npm test -- src/backend/claude-session/session-file-parser.outbound-body.test.ts` and verify green.

    In the SUMMARY, list every test case name that was PRESERVED with legacy paths (if any) along with the description text that justified preservation.
  </action>
  <verify>
    <automated>npm test -- src/backend/claude-session/session-file-parser.outbound-body.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fleet/identities\|fleet/roles" src/backend/claude-session/session-file-parser.outbound-body.test.ts` returns ≥ 20 (majority of fixture strings rewritten)
    - Any remaining `\.claude/identities` or `\.claude/roles` hit is either inside a test case whose description explicitly signals legacy-format testing, OR justified in the SUMMARY
    - `npm test -- src/backend/claude-session/session-file-parser.outbound-body.test.ts` exits 0
  </acceptance_criteria>
  <done>
    Outbound-body parse-test corpus reflects post-migration relay-send emission shape; any explicitly-legacy test case is documented in the SUMMARY; test suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skynet backend → managed host (SSH exec) | ssh-poll-orchestrator runs SSH commands enumerating identity dirs + statting dormancy sentinels |
| session-file-parser test corpus | Parses shell-fragment outbound relay-send commands captured from real agent sessions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-07 | Tampering | ssh-poll-orchestrator SSH exec strings | mitigate | Bulk sed on the test file MUST NOT accidentally rewrite strings other than the four documented path shapes; grep post-sed catches any drift. Source file rewrites are hand-audited per-line to preserve `${quotedName}` interpolations. |
| T-96-08 | Repudiation | session-file-parser test corpus legacy captures | accept | Loss of pre-migration parse-test capture is acceptable; the corpus's job is to describe current agent emission shape. If any historical-shape test is preserved, it MUST document the preservation in code comments. |
</threat_model>

<verification>
- ssh-poll-orchestrator.ts has 0 `.claude/(identities|roles)` outside comments; ~5 fleet-path exec strings; grep-clean
- ssh-poll-orchestrator.test.ts has 0 `.claude/(identities|roles)` (bulk-sed complete); test suite green
- session-file-parser.outbound-body.test.ts: audited case-by-case; test suite green
</verification>

<success_criteria>
- All three files rewritten
- Bulk sed on ssh-poll-orchestrator.test.ts left zero legacy hits
- Test suites for both `ssh-poll-orchestrator.test.ts` and `session-file-parser.outbound-body.test.ts` are green
- Any preserved legacy fixture is enumerated in the SUMMARY with justification
</success_criteria>

<output>
Create `.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-03-SUMMARY.md` documenting:
- ssh-poll-orchestrator.ts: exact line list of live-code rewrites + comment updates
- ssh-poll-orchestrator.test.ts: bulk sed command used + post-sed grep verification count
- session-file-parser.outbound-body.test.ts: enumerated test cases rewritten vs preserved (with description-text justification for any preserved case)
- npm test output for both test suites
</output>
</content>
</invoke>