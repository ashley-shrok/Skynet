---
phase: 96-on-disk-tree-consolidation-consolidate-identity-metadata-and
plan: 07
type: execute
wave: 4
depends_on: ["96-01", "96-02", "96-03", "96-04", "96-05", "96-06"]
files_modified:
  - .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/MIGRATION.md
files_read:
  - .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-01-SUMMARY.md
  - .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-02-SUMMARY.md
  - .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-03-SUMMARY.md
  - .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-04-SUMMARY.md
  - .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-05-SUMMARY.md
  - .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-06-SUMMARY.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "MIGRATION.md exists at .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/MIGRATION.md and is a self-contained per-box runbook — Ashley can follow it on t1000 and Stacy can follow it on T800 without cross-referencing anything outside the repo"
    - "MIGRATION.md covers all 7 steps of D-07 (save → cp → shutdown → deploy → restart → verify → delete) plus the currently-archived-identities decision prompt of D-09"
    - "Repo-wide grep audit shows zero legacy `~/.claude/identities/` or `~/.claude/roles/` references outside D-12/D-13/D-14 retained set (CLAUDE.md, skills/, local/bin/) AND outside historical .planning/ prose"
    - "Repo-wide grep audit (`src/` + `substrate/`, all extensions) shows zero `~/skynet-<name>/` references outside .planning/ historical prose AND outside known-retained corpus captures (`src/backend/claude-session/session-file-parser.outbound-body.test.ts` message-body fixture data)"
    - "The retained-references audit enumerates every intentional legacy reference so the reviewer can eyeball-verify (D-15 acceptance criterion)"
  artifacts:
    - path: ".planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/MIGRATION.md"
      provides: "Per-box migration runbook — Steps 0–7 with maintainer-decision prompts at Step 0 (archived identities) and Step 7 (deletion confirmation)"
      min_lines: 100
  key_links:
    - from: "MIGRATION.md Step 4"
      to: "MIGRATION.md Step 5"
      via: "shut-down before deploy sequencing"
      pattern: "docker compose.*down.*deploy"
    - from: "MIGRATION.md Step 6"
      to: "MIGRATION.md Step 7"
      via: "verify before delete — deletion blocked until verification passes"
      pattern: "verify.*delete"
---

<objective>
Write the maintainer-facing migration runbook at `.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/MIGRATION.md` (per D-10), and run the final repo-wide grep-clean audit (D-15) to confirm every legacy path reference has either been rewritten (Plans 96-01 through 96-06) or is documented as intentionally retained (D-12/D-13/D-14) or as historical `.planning/` prose. Wave 4 — depends on all prior plans landing so the audit reflects the final code state.

Purpose: MIGRATION.md is the last unwritten artifact of Phase 96. It is a prose runbook (per D-10 planner discretion), self-contained, executable by Ashley on t1000 and by Stacy on T800 without any cross-references outside the repo. The repo-wide grep audit is Phase 96's overall acceptance criterion (D-15) — a partial audit leaves landmines in unrelated files (test fixtures, plan artifacts, docs examples). Running it as the last step catches anything Plans 96-01 through 96-06 missed.

Output: MIGRATION.md written. Repo-wide grep audit result documented in the SUMMARY with an enumerated list of retained references (D-12/D-13/D-14) for reviewer eyeball verification.

Implements D-07 (7-step per-box migration sequence), D-09 (currently-archived-identities decision prompt), D-10 (runbook location), D-15 (repo-wide grep sweep as audit floor).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-CONTEXT.md
@.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-RESEARCH.md
@.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-01-SUMMARY.md
@.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-02-SUMMARY.md
@.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-03-SUMMARY.md
@.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-04-SUMMARY.md
@.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-05-SUMMARY.md
@.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write MIGRATION.md — the per-box migration runbook</name>
  <files>.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/MIGRATION.md</files>
  <read_first>
    - .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-CONTEXT.md (D-07 for the 7-step sequence, D-09 for the archived-identities prompt, D-10 for the location + audience)
    - .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-RESEARCH.md (MIGRATION.md Content Sketch section around lines 725–847 — the researcher drafted a full sketch; use as scaffolding but the planner may refine wording)
  </read_first>
  <action>
    Create `.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/MIGRATION.md`. Audience: Ashley (t1000 maintainer), Stacy (T800 maintainer), any future box maintainer. Plain language, no assumed context outside the repo, self-contained.

    Required structure (adapt the RESEARCH.md sketch — refine but do not omit any step):

    1. **Title + preamble** — "Fleet tree migration — per-box runbook". Explain: this migration is a one-shot per box; run it during a maintenance window when no identity on this box is mid-flow. It ends with the box's on-disk state consolidated under `~/fleet/`.

    2. **Before you begin** — prerequisites: new code (Shape 3 + Shapes 1, 2, 4) already on origin/main; no identity mid-flow; a few minutes of downtime possible.

    3. **Step 0: Currently-archived identities decision (D-09)** — `ls ~/.claude/identities/archive/ 2>/dev/null`. If subdirectories exist, prompt the maintainer: (a) move to `~/fleet/identities-archive/`, (b) delete, (c) leave in place at legacy path. If empty or missing, skip this section entirely. Include the exact `cp -a` incantation for option (a) so the maintainer can execute it directly.

    4. **Step 1: Save every active identity (D-07 step 1)** — for each active identity on the box, send `/id save` in its session. Confirm every session shows completion before continuing. Rationale: flushes handoff.md, updates bounties, appends to history.

    5. **Step 2: Copy metadata + working directories to `~/fleet/` (D-07 step 2)** — commands to:
       - `mkdir -p ~/fleet/identities ~/fleet/roles ~/fleet/identities-archive`
       - Loop over `~/.claude/identities/*/` copying each to `~/fleet/identities/<name>` (skip `archive` if it exists as a subdir per D-09 branching).
       - Loop over `~/skynet-*/` working directories: for each, derive the identity name from the folder name (`skynet-tanya` → `tanya`) and copy to `~/fleet/identities/<name>/workspace/`. If no matching identity folder exists in `~/fleet/identities/`, log a warning and skip.
       - Loop over `~/.claude/roles/*/` copying each to `~/fleet/roles/<name>`.
       - If option (a) was chosen in Step 0, loop over `~/.claude/identities/archive/*/` copying to `~/fleet/identities-archive/`.

    6. **Step 3: Verify the copy** — spot-check commands: `ls ~/fleet/identities/<yourname>/` should show `<name>.md`, `handoff.md`, `relay.json`, and the sub-folders. `ls ~/fleet/identities/<yourname>/workspace/` should show the repo/working content. `ls ~/fleet/roles/` should list the role folders.

    7. **Step 4: Shut down Skynet host container + agent-supervisor sessions (D-07 step 3)** — `docker compose ... down` (adapt to the box's actual compose file location; use `/opt/skynet/docker-compose.yml` as the default path with a note "adjust if your box installs Skynet elsewhere") plus `systemctl --user stop agent-supervisor`.

    8. **Step 5: Deploy new Skynet code (D-07 step 4)** — `cd /opt/skynet && git pull origin main && docker compose -f docker/docker-compose.yml build && docker compose ... up -d`. Note: the container boot triggers the fleet-substrate distributor sweep, which pushes new substrate assets (id-skill, agent-supervisor, monitor scripts) to every managed host including this one.

    9. **Step 6: Verify the new tree is live (D-07 step 5-6)** — `systemctl --user start agent-supervisor`. Check identities load in Skynet UI within a minute or two. Check the distributor sweep completed cleanly (look for the sweep-complete log line in `docker compose logs`). Bounties visible; monitors running.

    10. **Step 7: Delete the old locations (D-07 step 7)** — ONLY after Step 6 verification passes. `rm -rf ~/.claude/identities/`, `rm -rf ~/.claude/roles/`, and for each old working directory `rm -rf ~/skynet-<name>/`. Include a "DO NOT proceed if Step 6 shows any error" gate. After this step, `~/fleet/` is the sole home for identity + role state on this box.

    11. **Rollback note** — if something goes wrong before Step 7, the old locations still exist (Steps 2–6 do not delete them). Roll back by pulling the previous Skynet code, restarting the container, and restarting agent-supervisor; identities resume on the old tree. Note: rollback after Step 7 requires restoring from backup.

    Style: prose runbook, not shape file. Executable commands in fenced bash blocks with `bash` language tag. Prompts to the maintainer for decisions (Step 0 archived-identities, Step 4 compose-file-path variance, Step 7 pre-delete verification) are explicit in-text questions with clear next-step branches.

    Fleet-substrate hand-patch anti-pattern warning: include a brief note at the end of the runbook that says "Do NOT hand-patch fleet-substrate content on this box after the migration — if the distributor doesn't push something correctly, fix the distributor OR the creds; do not SSH in and edit installed files" (matches CONTEXT.md's Anti-patterns to avoid list).

    Length: target 150–250 lines. Not longer — clarity beats completeness.
  </action>
  <verify>
    <automated>bash -c 'test -f .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/MIGRATION.md && wc -l .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/MIGRATION.md | awk "{ if (\$1 >= 100 && \$1 <= 400) exit 0; else { print \"line count out of range:\", \$1; exit 1 } }" && grep -q "Step 0" .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/MIGRATION.md && grep -q "Step 7" .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/MIGRATION.md && grep -q "workspace" .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/MIGRATION.md && grep -q "identities-archive" .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/MIGRATION.md'</automated>
  </verify>
  <acceptance_criteria>
    - MIGRATION.md exists at the specified path
    - File has all 7 numbered steps (Step 1 through Step 7) plus Step 0 (archived identities decision) plus preamble
    - Line count between ~100 and ~400 (a realistic prose runbook, neither empty nor bloated)
    - Contains a fenced bash block with `mkdir -p ~/fleet/identities ~/fleet/roles ~/fleet/identities-archive`
    - Contains the D-09 currently-archived-identities decision prompt
    - Contains the D-07 sequence in order: save → cp → shutdown → deploy → verify → delete
    - Contains a rollback note before Step 7
    - Contains the fleet-substrate hand-patch anti-pattern warning
    - Does NOT contain any `~/.claude/identities/` or `~/.claude/roles/` reference EXCEPT in the copy-from-old-location commands (where they are legitimately part of the source-side of a cp)
  </acceptance_criteria>
  <done>
    MIGRATION.md is a self-contained runbook Ashley or Stacy can follow end-to-end on their respective boxes without cross-referencing anything outside the repo.
  </done>
</task>

<task type="auto">
  <name>Task 2: Repo-wide grep-clean audit (D-15) + enumerate retained references</name>
  <files>.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-07-SUMMARY.md</files>
  <read_first>
    - All 6 prior plan SUMMARY files (listed in `<context>`)
    - .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-RESEARCH.md (`.planning/` prose files section around lines 483–493 + Validation Architecture section around lines 990–998 for the exact grep commands)
    - .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-CONTEXT.md (D-12/D-13/D-14 retained references)
  </read_first>
  <action>
    Run the D-15 repo-wide grep audit. The output of this task is documentation — the SUMMARY records what was found and confirms the ship state.

    1. **Live-code grep sweep — expect zero hits:**
       - `grep -rn "\.claude/identities" src/ substrate/ --include="*.ts" --include="*.sh" --include="*.py" --include="*.md"` — expect 0 hits (all rewritten across Plans 96-01 through 96-06). Any hit is a bug and must be triaged: is it retained (D-12/D-13/D-14 pattern)? Is it historical narrative? Or is it a genuine miss?
       - `grep -rn "\.claude/roles" src/ substrate/ --include="*.ts" --include="*.sh" --include="*.py" --include="*.md"` — expect 0 hits.
       - `grep -rn "~/skynet-" substrate/ src/ --include="*.sh" --include="*.py" --include="*.md" --include="*.ts"` — expect only known-retained corpus captures. Retained hits: `src/backend/claude-session/session-file-parser.outbound-body.test.ts` contains `~/skynet-tanya` inside captured relay-message body content (test fixtures verifying that outbound parser preserves message bodies verbatim, including agents' natural-language references to their own working dirs — these are LITERAL DATA, not code path references, and MUST NOT be rewritten). Any hit outside this file is a bug; enumerate all hits in SUMMARY Section B.

    2. **Retained references — enumerate for reviewer verification (D-12/D-13/D-14):**
       - `grep -rn "\.claude/CLAUDE\.md" src/ substrate/ --include="*.ts" --include="*.sh" --include="*.py" --include="*.md"` — expected retained per D-12; list every hit.
       - `grep -rn "\.claude/skills" src/ substrate/ --include="*.ts" --include="*.sh" --include="*.py" --include="*.md"` — expected retained per D-13; list every hit.
       - `grep -rn "~/\.local/bin" src/ substrate/ --include="*.ts" --include="*.sh" --include="*.py" --include="*.md"` — expected retained per D-14; list every hit.

    3. **Historical prose in `.planning/` — expected retained (RESEARCH.md lines 483–493):**
       - `grep -rn "\.claude/identities\|\.claude/roles" .planning/phases/*/ .planning/shapes/ .planning/quick/ --include="*.md" | wc -l` — count as a sanity check; these are historical planning artifacts (old plans, summaries, shapes) and are NOT rewritten. Report the count in the SUMMARY.

    4. **Test suite:** run `npm test` (top-level, all tests). Expect green. If any test fails, triage: is it a Plan 96-01 through 96-06 miss? A pre-existing failure unrelated to Phase 96? Document result in SUMMARY.

    5. **Phase 94 archive-scan test driver:** run `bash substrate/scripts/tests/agent-supervisor-archive-scan.sh` — expect 31/31 pass (already covered by Plan 96-04, but re-run here to confirm the ship state).

    6. **shellcheck agent-supervisor.sh** — expect clean.

    Write the audit result into `96-07-SUMMARY.md` with:
    - **Section A: Live-code sweep** — exact grep commands + hit counts (should all be 0 or explicit retained-reference categories)
    - **Section B: Retained references enumeration** — the exact list of file:line matches for CLAUDE.md, skills/, local/bin/. Reviewer eyeball-verifies this list matches D-12/D-13/D-14.
    - **Section C: Historical planning-prose count** — count of `.planning/` hits (informational only; not rewritten per RESEARCH.md guidance)
    - **Section D: Test suite results** — `npm test` output summary; Phase 94 archive-scan 31/31; shellcheck clean
    - **Section E: Any anomalies** — if any live-code hit remains that is not a retained reference, escalate here with file:line + proposed fix path

    If Section E is non-empty, the plan does NOT report DONE. The remediating file edits must land as a follow-up commit before marking Phase 96 complete.
  </action>
  <verify>
    <automated>bash -c 'c1=$(grep -rn "\.claude/identities" src/ substrate/ --include="*.ts" --include="*.sh" --include="*.py" --include="*.md" | wc -l); c2=$(grep -rn "\.claude/roles" src/ substrate/ --include="*.ts" --include="*.sh" --include="*.py" --include="*.md" | wc -l); c3=$(grep -rn "~/skynet-" substrate/ src/ --include="*.sh" --include="*.py" --include="*.md" --include="*.ts" | grep -v "^src/backend/claude-session/session-file-parser\.outbound-body\.test\.ts:" | wc -l); [ "$c1" = "0" ] && [ "$c2" = "0" ] && [ "$c3" = "0" ] || { echo "FAIL: live-code sweep found hits: identities=$c1 roles=$c2 skynet-=$c3 (excluding session-file-parser corpus retentions)"; exit 1; } && bash substrate/scripts/tests/agent-supervisor-archive-scan.sh > /tmp/archive-scan.out 2>&1 && grep -q "31/31\|31 passed\|all tests pass" /tmp/archive-scan.out'</automated>
  </verify>
  <acceptance_criteria>
    - Live-code grep on `src/` and `substrate/` for `\.claude/identities` returns 0 hits
    - Live-code grep on `src/` and `substrate/` for `\.claude/roles` returns 0 hits
    - Live-code grep on `src/` and `substrate/` for `~/skynet-` returns 0 hits outside the known-retained corpus captures (`src/backend/claude-session/session-file-parser.outbound-body.test.ts` — captured relay-message body content, LITERAL DATA not code path references)
    - Retained-reference lists for CLAUDE.md, skills/, local/bin/ enumerated in SUMMARY (reviewer-verifiable)
    - `npm test` green (or documented pre-existing failures that are not Phase 96-caused)
    - Phase 94 archive-scan test driver 31/31 pass
    - `shellcheck substrate/scripts/agent-supervisor.sh` clean
    - Section E (anomalies) of 96-07-SUMMARY.md is empty
  </acceptance_criteria>
  <done>
    Repo-wide grep audit passes; retained references enumerated; all tests green; Phase 96 has no outstanding path-migration debt; ship-state confirmed for the Shape 3 held-from-ship commit set.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MIGRATION.md → maintainer | Maintainer executes commands from the runbook verbatim; a wrong command destroys state |
| Repo grep audit → ship confidence | The audit is the D-15 acceptance floor; a missed hit ships broken code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-17 | Denial of Service | MIGRATION.md Step 7 delete before Step 6 verify | mitigate | Step 7 explicitly gated on Step 6 verification; runbook has "DO NOT proceed if Step 6 shows any error" gate |
| T-96-18 | Information disclosure | Retained-reference enumeration | accept | Retained references are already in the codebase; enumerating them in the SUMMARY does not disclose new information |
| T-96-19 | Tampering | Historical `.planning/` prose not rewritten | accept | Per RESEARCH.md line 483–493 explicit guidance; historical planning artifacts are documentation of past state and should not be rewritten |
</threat_model>

<verification>
- MIGRATION.md exists, is self-contained, covers Steps 0–7 including archived-identities prompt and rollback note
- Live-code grep sweep zero hits
- Retained references enumerated with file:line detail
- All tests green
- shellcheck clean
</verification>

<success_criteria>
- MIGRATION.md written and passes structural checks
- Repo-wide grep audit passes with zero live-code legacy hits
- Retained references enumerated for reviewer eyeball verification
- Test suite green; Phase 94 archive-scan 31/31; shellcheck clean
- Phase 96 ship-state confirmed
</success_criteria>

<output>
Create `.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-07-SUMMARY.md` with the five audit sections (A/B/C/D/E) plus MIGRATION.md structural verification. This SUMMARY doubles as Phase 96's ship-readiness affidavit.
</output>
</content>
