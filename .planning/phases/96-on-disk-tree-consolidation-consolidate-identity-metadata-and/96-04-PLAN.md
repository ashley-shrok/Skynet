---
phase: 96-on-disk-tree-consolidation-consolidate-identity-metadata-and
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - substrate/scripts/agent-supervisor.sh
  - substrate/scripts/tests/agent-supervisor-archive-scan.sh
  - substrate/scripts/role-file-watch.py
  - substrate/scripts/wakeup-scheduler.py
  - substrate/skills/agent-relay/recv.sh
autonomous: true
requirements: []
must_haves:
  truths:
    - "agent-supervisor.sh IDENTITIES_DIR default is `$HOME/fleet/identities` (env override AGENT_IDENTITIES_DIR preserved as test seam)"
    - "agent-supervisor.sh introduces `IDENTITIES_ARCHIVE_DIR=\"${AGENT_IDENTITIES_ARCHIVE_DIR:-$HOME/fleet/identities-archive}\"` as a SIBLING variable (not derived from IDENTITIES_DIR)"
    - "agent-supervisor.sh retire_identity uses IDENTITIES_ARCHIVE_DIR — no `$IDENTITIES_DIR/archive/*` composition remains"
    - "agent-supervisor.sh `archive` skip filter (line ~220 `[ \"$name\" = archive ] && continue`) is REMOVED as dead code post-sibling-move"
    - "agent-supervisor.sh convention workdir fallback uses `$IDENTITIES_DIR/$name/workspace` — no `$HOME/$name` fallback remains"
    - "agent-supervisor.sh direct-literal path references at lines ~1224, ~1279, ~1304 use `$IDENTITIES_DIR/$name` (not the old literal `$HOME/.claude/identities/$name`)"
    - "role-file-watch.py line 236 uses `~/fleet/roles/%s/%s.md` — the only hardcoded path in the Python monitors is rewritten"
    - "wakeup-scheduler.py docstring references the fleet tree (prose accuracy only)"
    - "recv.sh error message references the fleet tree (prose accuracy only)"
    - "substrate/scripts/tests/agent-supervisor-archive-scan.sh uses AGENT_IDENTITIES_ARCHIVE_DIR pointing at a SEPARATE scratch dir, not a subdir of AGENT_IDENTITIES_DIR"
    - "31/31 archive-scan test cases pass"
  artifacts:
    - path: "substrate/scripts/agent-supervisor.sh"
      provides: "Per-host supervisor: enumerate identities, launch sessions, run archive scan, reconcile"
      contains: "IDENTITIES_ARCHIVE_DIR="
    - path: "substrate/scripts/role-file-watch.py"
      provides: "Ambient monitor for role-file changes"
      contains: "~/fleet/roles/"
    - path: "substrate/scripts/tests/agent-supervisor-archive-scan.sh"
      provides: "Phase 94 archive-scan test driver — 31 test cases"
      min_lines: 800
  key_links:
    - from: "substrate/scripts/agent-supervisor.sh retire_identity"
      to: "IDENTITIES_ARCHIVE_DIR variable"
      via: "archdir=\"$IDENTITIES_ARCHIVE_DIR/$name\""
      pattern: "IDENTITIES_ARCHIVE_DIR"
    - from: "substrate/scripts/tests/agent-supervisor-archive-scan.sh test setup"
      to: "AGENT_IDENTITIES_ARCHIVE_DIR export"
      via: "separate scratch dir sibling of AGENT_IDENTITIES_DIR"
      pattern: "AGENT_IDENTITIES_ARCHIVE_DIR"
---

<objective>
Rewrite the substrate script surface — agent-supervisor.sh (largest file in this plan, with Phase 94 archive-scan implications requiring a NEW `IDENTITIES_ARCHIVE_DIR` variable since the archive is now a SIBLING at `~/fleet/identities-archive/` not a child of the identities dir), Phase 94's archive-scan test driver (needs matching separate-scratch-dir setup), and the Python + shell monitor script prose. This plan runs in parallel with Plans 96-01/96-02/96-03 (disjoint files).

Purpose: The substrate script layer is what agent sessions actually launch through. The variable indirection at agent-supervisor.sh line 32 is the single-source fix for most identity-path references, but the archive-scan action (from Phase 94) needs a completely separate variable because the archive moves from `$IDENTITIES_DIR/archive/` (nested) to `~/fleet/identities-archive/` (sibling). Direct literals at ~1224, ~1279, ~1304 need explicit rewrite (they bypass the variable). Line ~220's defensive `archive` filter becomes dead code and is REMOVED. Convention workdir fallback changes from `$HOME/$name` to `$IDENTITIES_DIR/$name/workspace`. Python monitor scripts + recv.sh are mostly path-relative; only role-file-watch.py line 236 has a functional hardcoded path — rewrite it. Docstring/error-message prose updates in the other monitors keep documentation aligned but are not load-bearing.

Output: 5 files modified. Phase 94's archive-scan test driver (31 tests) is green. Shellcheck-clean on agent-supervisor.sh.

Implements D-01, D-02 (identities-archive as sibling), D-04 (workspace as convention workdir), D-05, D-06, plus the Phase 94 archive-scan compatibility (retire_identity target updates).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-CONTEXT.md
@.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-RESEARCH.md
@.planning/phases/94-supervisor-archive-extension-daily-archive-scan-for-180-day-/94-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite substrate/scripts/agent-supervisor.sh — introduce IDENTITIES_ARCHIVE_DIR + rewrite retire_identity + convention workdir + direct-literal references + remove dead archive filter</name>
  <files>substrate/scripts/agent-supervisor.sh</files>
  <read_first>
    - substrate/scripts/agent-supervisor.sh (targeted: `grep -n "\.claude/identities\|\.claude/roles\|IDENTITIES_DIR\|\$HOME/\$name\|archdir\|archive" substrate/scripts/agent-supervisor.sh` — enumerate every hit before editing)
    - .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-RESEARCH.md ("agent-supervisor.sh Deep Read" section around lines 496–584 + Trap 1/Trap 3/Trap 9/Trap 10 around lines 902–953)
    - .planning/phases/94-supervisor-archive-extension-daily-archive-scan-for-180-day-/94-CONTEXT.md (understand D-10/D-11/D-12 of Phase 94 — retire_identity three-step action)
  </read_first>
  <action>
    Edit `substrate/scripts/agent-supervisor.sh` in place. All edits below preserve surrounding structure; do NOT reformat or reorder unrelated blocks.

    1. **Line 32 default rewrite:** change `IDENTITIES_DIR="${AGENT_IDENTITIES_DIR:-$HOME/.claude/identities}"` to `IDENTITIES_DIR="${AGENT_IDENTITIES_DIR:-$HOME/fleet/identities}"`. Preserve the env-var override — it is the test seam used by agent-supervisor-archive-scan.sh.

    2. **New IDENTITIES_ARCHIVE_DIR variable:** immediately after line 32's IDENTITIES_DIR assignment, add a parallel line:
       `IDENTITIES_ARCHIVE_DIR="${AGENT_IDENTITIES_ARCHIVE_DIR:-$HOME/fleet/identities-archive}"`
       with the same env-var override pattern. This variable is the leverage point for retire_identity — it is a SIBLING of IDENTITIES_DIR, not a subpath. Add a comment above it stating "Sibling of IDENTITIES_DIR per D-02; kept as a separate variable so retire_identity does not derive the archive path from IDENTITIES_DIR + '/archive' (which would resolve to `~/fleet/identities/archive/` — wrong tree)."

    3. **Line ~220 archive-skip filter REMOVAL:** delete the line `[ "$name" = archive ] && continue          # defensive; identity-archive is a SIBLING dir, not here` in the identity enumeration loop under `IDENTITIES=()`. The comment on that line already documents that the filter was defensive because the archive was nested. Post-Phase-96, the archive is a true sibling and this filter is unreachable dead code. Do NOT leave it as a commented-out line — remove it entirely to keep the enumeration loop clean.

    4. **retire_identity function (lines ~281–340+):**
       - Line ~284: change `local archdir="$IDENTITIES_DIR/archive/$name"` to `local archdir="$IDENTITIES_ARCHIVE_DIR/$name"`.
       - Line ~290: change `mkdir -p "$IDENTITIES_DIR/archive" 2>/dev/null` to `mkdir -p "$IDENTITIES_ARCHIVE_DIR" 2>/dev/null`.
       - Update the comment block above `mkdir -p` if it refers to "under IDENTITIES_DIR" — replace with "the archive sibling location per D-02".
       - Preserve the entire four-state idempotency case-analysis (D-13 of Phase 94) — only the path variables change, no logic changes.

    5. **Convention workdir fallback (lines ~1030–1046):** in the `if disc="$(resolve_session ...)" ... elif ... else ... fi` block:
       - Change the `elif [ -d "$HOME/$name" ]` branch to `elif [ -d "$IDENTITIES_DIR/$name/workspace" ]`.
       - Inside that branch, change `cwd="$HOME/$name"` to `cwd="$IDENTITIES_DIR/$name/workspace"`.
       - Update the log message string: replace `'$name' launch: resolve_session found no prior session — using convention workdir '$cwd'` (already parametric on $cwd — no text change needed, but ensure the message still reads coherently with the new $cwd shape).
       - Update the `else` branch's log message: replace `\$HOME/$name absent` with `$IDENTITIES_DIR/$name/workspace absent`. Preserve the "fresh /id in \$HOME" text unchanged (per RESEARCH.md Trap 10 the else-fallback stays at `\$HOME` — the identity has no workspace yet, so home is the safest default).

    6. **Direct-literal path references — rewrite to use $IDENTITIES_DIR:**
       - Line ~1224 (in `_matrix_peek_one` account discriminator): `if [ "$(dirname "$rj")" = "$HOME/.claude/identities/$name" ]` → `if [ "$(dirname "$rj")" = "$IDENTITIES_DIR/$name" ]`.
       - Line ~1279 (matrix_peek): `local idroot="$HOME/.claude/identities/$name"` → `local idroot="$IDENTITIES_DIR/$name"`.
       - Line ~1304 (matrix_peek): `local wd="$HOME/.claude/identities/$name/wakeups"` → `local wd="$IDENTITIES_DIR/$name/wakeups"`.

    7. **Post-edit shellcheck:** run `shellcheck substrate/scripts/agent-supervisor.sh` and address any new warnings introduced by the edits (typically none — variable substitutions preserve semantics).

    8. **Post-edit grep verification:**
       - `grep -c "\.claude/identities\|\.claude/roles" substrate/scripts/agent-supervisor.sh` (excluding grep of comments alone) — expect 0 outside intentionally-retained historical comments.
       - `grep -c "IDENTITIES_ARCHIVE_DIR" substrate/scripts/agent-supervisor.sh` — expect ≥ 3 (the declaration + 2 usages in retire_identity).
       - `grep -c "\$HOME/\$name" substrate/scripts/agent-supervisor.sh` — expect 0 in live code (the convention workdir was the only usage).
       - `grep -c "\$IDENTITIES_DIR/\$name/workspace" substrate/scripts/agent-supervisor.sh` — expect ≥ 1 (the new convention workdir).
  </action>
  <verify>
    <automated>shellcheck substrate/scripts/agent-supervisor.sh &amp;&amp; grep -cE "\.claude/(identities|roles)" substrate/scripts/agent-supervisor.sh | grep -q '^0$' &amp;&amp; grep -q "IDENTITIES_ARCHIVE_DIR" substrate/scripts/agent-supervisor.sh &amp;&amp; grep -q "\$IDENTITIES_DIR/\$name/workspace" substrate/scripts/agent-supervisor.sh</automated>
  </verify>
  <acceptance_criteria>
    - `shellcheck substrate/scripts/agent-supervisor.sh` clean
    - Full-file grep for `\.claude/(identities|roles)` returns 0 (post-comment count also expected 0; if any hit remains it MUST be in an intentional historical comment and enumerated in SUMMARY)
    - `IDENTITIES_ARCHIVE_DIR="${AGENT_IDENTITIES_ARCHIVE_DIR:-$HOME/fleet/identities-archive}"` present (or equivalent quoting) — grep confirms declaration
    - retire_identity function references `$IDENTITIES_ARCHIVE_DIR/$name` and `$IDENTITIES_ARCHIVE_DIR` (mkdir target) — no `$IDENTITIES_DIR/archive` composition
    - The dead `archive` skip filter at line ~220 is removed (grep: `grep -c '"\$name" = archive' substrate/scripts/agent-supervisor.sh` returns 0)
    - Convention workdir uses `$IDENTITIES_DIR/$name/workspace`
    - Direct literals at ~1224/1279/1304 all use `$IDENTITIES_DIR/$name`
  </acceptance_criteria>
  <done>
    agent-supervisor.sh fully migrated to the fleet tree with archive as sibling; shellcheck clean; ready for Phase 94 archive-scan test driver rewrite in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Phase 94 archive-scan test driver + role-file-watch.py + wakeup-scheduler.py + recv.sh prose</name>
  <files>substrate/scripts/tests/agent-supervisor-archive-scan.sh, substrate/scripts/role-file-watch.py, substrate/scripts/wakeup-scheduler.py, substrate/skills/agent-relay/recv.sh</files>
  <read_first>
    - substrate/scripts/tests/agent-supervisor-archive-scan.sh (full file — ~828 lines; understand setup_scratch pattern and fixture_relay_json usage)
    - substrate/scripts/role-file-watch.py (targeted: `grep -n "\.claude" substrate/scripts/role-file-watch.py` to enumerate the single functional hit at line 236)
    - substrate/scripts/wakeup-scheduler.py (line 11 docstring — grep to confirm)
    - substrate/skills/agent-relay/recv.sh (line ~43 error message + line ~19–25 cred-path derivation — read for context but do NOT edit the cred-path derivation, which is $(dirname $STATE_DIR)-relative and needs no change)
    - .planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-RESEARCH.md (Ambient Monitor Scripts section around lines 633–670 + Trap 2 around lines 913–915 + Trap 8 around lines 940–944)
  </read_first>
  <action>
    1. **substrate/scripts/tests/agent-supervisor-archive-scan.sh:**
       - The test driver exports `AGENT_IDENTITIES_DIR=<scratch>` to redirect IDENTITIES_DIR (see `setup_scratch()` around line 82). After Task 1's changes, agent-supervisor.sh has a SECOND variable `IDENTITIES_ARCHIVE_DIR` (with env override `AGENT_IDENTITIES_ARCHIVE_DIR`). The test driver must export this second variable pointing at a SEPARATE scratch dir (not a sub-dir of AGENT_IDENTITIES_DIR).
       - Update `setup_scratch()`:
         - Create a second scratch dir (via a second mktemp or a sibling subpath of the parent temp — planner's choice; simpler is `local archive_scratch="${1:-/tmp}-archive"` or `mktemp -d` a second time).
         - Export `AGENT_IDENTITIES_ARCHIVE_DIR="$archive_scratch"` alongside the existing AGENT_IDENTITIES_DIR export.
         - Ensure `teardown_scratch()` cleans up the archive scratch dir too.
       - Update any test-case fixture that currently constructs archive paths as `$scratch/archive/<name>` — those tests now construct paths as `$AGENT_IDENTITIES_ARCHIVE_DIR/<name>`. The specific line RESEARCH.md flags is line 559 (`fixture_relay_json "$scratch/archive/tina" ...`) — rewrite to use the archive-scratch dir.
       - Any assertion that checks the retire action moved a folder to `$scratch/archive/<name>` must update to check `$AGENT_IDENTITIES_ARCHIVE_DIR/<name>`.
       - Enumerate every hit with `grep -n 'archive' substrate/scripts/tests/agent-supervisor-archive-scan.sh` and audit case-by-case — some hits are legitimate references to the concept (comments, log messages) that stay; the ones to change are paths under `$scratch/archive/` or `$IDENTITIES_DIR/archive/`.
       - Update the header comment block (~lines 21, 66–79) that describes `AGENT_IDENTITIES_DIR=<scratch>` to also document `AGENT_IDENTITIES_ARCHIVE_DIR=<scratch>` as a companion env override.
       - Run the driver: `bash substrate/scripts/tests/agent-supervisor-archive-scan.sh` — expect 31/31 pass. If any test fails, read the error output and fix the fixture setup (usually a path substring mismatch).

    2. **substrate/scripts/role-file-watch.py line 236:**
       - Change `role_file_path = os.path.expanduser("~/.claude/roles/%s/%s.md" % (role, role))` to `role_file_path = os.path.expanduser("~/fleet/roles/%s/%s.md" % (role, role))`.
       - This is the ONLY functional path hardcode in the file (all other identity-dir references are relative to `sys.argv[1]`, per RESEARCH.md).

    3. **substrate/scripts/wakeup-scheduler.py line 11 docstring:**
       - Update the docstring line referencing `~/.claude/identities/<name>/wakeups/<slug>.json` to `~/fleet/identities/<name>/wakeups/<slug>.json`. Prose only — no functional change (the script takes the identity dir as sys.argv[1] and is path-relative after that).

    4. **substrate/skills/agent-relay/recv.sh line ~43 error message:**
       - Grep to confirm line: `grep -n "\.claude/identities" substrate/skills/agent-relay/recv.sh`.
       - Update the error message string from "STATE_DIR must point at ~/.claude/identities/<name>/relay-state" (or similar) to "STATE_DIR must point at ~/fleet/identities/<name>/relay-state".
       - Do NOT change the cred-path derivation at line ~19–25 — it uses `$(dirname "$STATE_DIR")` which is path-relative and naturally lands on the new tree once STATE_DIR is set correctly by the caller (SKILL.md, which is Plan 96-05's scope).

    5. **substrate/scripts/context-watch.py — NO CHANGES.** RESEARCH.md confirms this file has no hardcoded `~/.claude` paths inside the script (identity dir passed as sys.argv[1]).

    6. **Post-edit verification:**
       - `bash substrate/scripts/tests/agent-supervisor-archive-scan.sh` — 31/31 pass.
       - `grep -c "\.claude/identities\|\.claude/roles" substrate/scripts/role-file-watch.py substrate/scripts/wakeup-scheduler.py substrate/skills/agent-relay/recv.sh` — expect 0.
       - `python3 -c "import ast; ast.parse(open('substrate/scripts/role-file-watch.py').read())"` — syntax valid.
  </action>
  <verify>
    <automated>bash substrate/scripts/tests/agent-supervisor-archive-scan.sh &amp;&amp; grep -cE "\.claude/(identities|roles)" substrate/scripts/role-file-watch.py substrate/scripts/wakeup-scheduler.py substrate/skills/agent-relay/recv.sh | awk -F: '{s+=$2} END{exit (s==0)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - `bash substrate/scripts/tests/agent-supervisor-archive-scan.sh` reports 31/31 tests pass
    - `grep -c "\.claude" substrate/scripts/role-file-watch.py` returns 0
    - `substrate/scripts/wakeup-scheduler.py` line 11 docstring references `~/fleet/identities/...`
    - `substrate/skills/agent-relay/recv.sh` error message at line ~43 references `~/fleet/identities/...`
    - `substrate/scripts/tests/agent-supervisor-archive-scan.sh` header comment documents `AGENT_IDENTITIES_ARCHIVE_DIR` as a companion env override
    - context-watch.py NOT modified (grep confirms no diff needed)
  </acceptance_criteria>
  <done>
    Phase 94 archive-scan test driver green with the new SIBLING archive layout; Python monitor + recv.sh prose aligned with fleet tree.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-supervisor.sh → filesystem | supervisor mkdir, mv, tmux new-session — all filesystem writes derived from IDENTITIES_DIR + IDENTITIES_ARCHIVE_DIR |
| agent-supervisor.sh → tmux socket | supervisor cd-then-exec into workspace dir — path affects where Claude sessions run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-09 | Tampering | IDENTITIES_ARCHIVE_DIR — separate variable, not derived | mitigate | Explicit variable declaration means retire_identity cannot accidentally target `$IDENTITIES_DIR/../identities-archive` (RESEARCH.md Trap 1). Test driver exercises retire_identity end-to-end against a scratch archive dir. |
| T-96-10 | Denial of Service | archive-skip filter removed | accept | The line-220 filter was defensive against a nested archive; with archive as a true sibling, its removal cannot cause a self-referential enumeration loop. |
| T-96-11 | Elevation of Privilege | convention workdir change ($HOME/$name → $IDENTITIES_DIR/$name/workspace) | mitigate | Both paths are user-owned under $HOME; new path narrower (under identity's own folder tree). No new writable directory is exposed. |
</threat_model>

<verification>
- shellcheck clean on agent-supervisor.sh
- Phase 94 archive-scan test driver 31/31 pass
- role-file-watch.py + wakeup-scheduler.py + recv.sh prose all fleet-tree-accurate
- context-watch.py unchanged (no diff)
</verification>

<success_criteria>
- 5 files rewritten
- Phase 94 archive-scan test suite green after adding AGENT_IDENTITIES_ARCHIVE_DIR test seam
- No `.claude/(identities|roles)` in touched substrate script files outside intentional historical comments
- shellcheck + Python-ast validation clean
</success_criteria>

<output>
Create `.planning/phases/96-on-disk-tree-consolidation-consolidate-identity-metadata-and/96-04-SUMMARY.md` documenting:
- Exact line list of agent-supervisor.sh edits (grouped by section: variable declarations, retire_identity, convention workdir, direct-literal rewrites, dead-code removal)
- Full diff summary for substrate/scripts/tests/agent-supervisor-archive-scan.sh setup_scratch/teardown_scratch + fixture updates
- Single-line change confirmation for role-file-watch.py
- Docstring update confirmation for wakeup-scheduler.py
- Error-message update confirmation for recv.sh
- 31/31 test-suite result
- shellcheck output
</output>
</content>
</invoke>