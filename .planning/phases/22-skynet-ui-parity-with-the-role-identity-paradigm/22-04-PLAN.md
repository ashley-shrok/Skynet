---
phase: 22-skynet-ui-parity-with-the-role-identity-paradigm
plan: 04
type: execute
wave: 2
depends_on:
  - 22-02-PLAN
files_modified:
  - src/backend/database/routes/roles-create.ts
  - src/backend/database/routes/roles-create.test.ts
  - src/backend/database/database.ts
  - src/ui/api/identities-api.ts
  - src/ui/sidebar/CreateRoleDialog.tsx
  - src/ui/sidebar/CreateRoleDialog.test.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - docker/nginx.conf
  - docker/nginx-https.conf
autonomous: true
requirements:
  - SRIC-04
tags: [skynet, roles, backend, frontend, dialog, ssh, nginx]

must_haves:
  truths:
    - "New POST /roles endpoint SSHes to the picked host, creates ~/.claude/roles/<name>/ folder + bounties/ subdir + touches history.md + writes stub <name>.md with `# <Name>\\n\\n## Role\\n\\n<description>\\n`"
    - "POST /roles validates name against ROLE_NAME_PATTERN (kebab-case-lowercase) AND checks the target folder does not already exist on host — returns 409 if collision"
    - "CreateRoleDialog renders name input + description textarea + host picker + `Then create an identity with this role` checkbox (defaults CHECKED per D-CONTEXT)"
    - "CreateRoleDialog blocks submit if any required field is empty or name fails ROLE_NAME_PATTERN"
    - "`+ New role` launcher button lives in the pretty-conversations panel header next to the pencil-icon `+ New agent` button; opens CreateRoleDialog"
    - "POST /roles route mounted under both nginx configs (nginx.conf AND nginx-https.conf) with matching location blocks"
    - "On submit success, dialog closes; the chain to create-identity (SRIC-05) is a SEPARATE plan (22-05) — this plan wires the checkbox but the chain handler is a documented no-op stub referencing 22-05"
  artifacts:
    - path: "src/backend/database/routes/roles-create.ts"
      provides: "POST /roles HTTP route — SSHes to hostId, creates role folder + bounties/ + history.md + <role>.md stub"
      exports: ["default (Router)"]
    - path: "src/ui/sidebar/CreateRoleDialog.tsx"
      provides: "New dialog component — name + description + host picker + `Then create an identity with this role` checkbox"
      exports: ["CreateRoleDialog"]
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "`+ New role` launcher button placed next to existing `+ New agent` pencil-icon; owns CreateRoleDialog open state"
      contains: "CreateRoleDialog"
    - path: "docker/nginx.conf"
      provides: "location = /roles POST-serving block (existing GET /roles block from 22-02 already covers this via `~ ^/roles(/.*)?$` regex — this task VERIFIES existing block accepts POST and adds `client_max_body_size 32k` for the description-payload guard if missing)"
    - path: "docker/nginx-https.conf"
      provides: "matching block"
  key_links:
    - from: "src/ui/sidebar/CreateRoleDialog.tsx"
      to: "POST /roles"
      via: "createRole API client call in submit handler"
      pattern: "createRole"
    - from: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      to: "CreateRoleDialog"
      via: "import + mount + open-state controlled by new `+ New role` button next to pencil"
      pattern: "CreateRoleDialog"
    - from: "src/backend/database/routes/roles-create.ts"
      to: "writeMarkdownFileAtomic (identity-artifact-reader.ts:852)"
      via: "SFTP write of stub <role>.md"
      pattern: "writeMarkdownFileAtomic"
---

<objective>
Ship the create-role surface per SRIC-04: a new `POST /roles` backend endpoint that provisions `~/.claude/roles/<name>/` + `bounties/` + `history.md` + a stub `<name>.md` on the picked host via SSH; a new `CreateRoleDialog.tsx` frontend modal with name + description + host + `Then create an identity with this role` checkbox (defaults CHECKED per D-CONTEXT); and a `+ New role` launcher button placed next to the existing `+ New agent` pencil in the pretty-conversations panel header.

Purpose: SRIC-04 — gives Ashley a first-class create-role affordance. Prerequisite for Plan 22-05 (chain from create-role → create-identity). Reuses infrastructure introduced by 22-02 (host picker patterns, ROLE_NAME_PATTERN, nginx dual-config parity, SSH primitives).

Output:
- New backend route `roles-create.ts` mounted at `/roles` (extends the router introduced by 22-02).
- New `CreateRoleDialog.tsx` component + tests.
- Modified `PrettyConversationsPanel.tsx` — adds `+ New role` button in the header + CreateRoleDialog open state.
- Modified `identities-api.ts` — adds `createRole` client helper.
- Nginx blocks unchanged if 22-02's `~ ^/roles(/.*)?$` regex already covers POST (it does — regex is method-agnostic); verify + add `client_max_body_size` for description-payload guard if missing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-CONTEXT.md
@.planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-RESEARCH.md
@.planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-02-PLAN.md
@src/backend/database/routes/identity-birth.ts
@src/backend/database/routes/identity-exists-on-host.ts
@src/backend/database/routes/identity-birth-orchestrator.ts
@src/backend/claude-session/identity-artifact-reader.ts
@src/backend/ssh/ssh-one-shot.ts
@src/backend/ssh/tmux-helper.ts
@src/backend/ssh/host-resolver.ts
@src/backend/database/database.ts
@src/ui/api/identities-api.ts
@src/ui/sidebar/NewSessionDialog.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
@docker/nginx.conf
@docker/nginx-https.conf
</context>

<tasks>

<!--
REVISION 2026-08-04 (Ashley, at 22-02 checkpoint — applies HERE via same-pattern extension):
  When Skynet auto-creates the role folder + stub `<role>.md`, embed a SEED COMMENT below
  the description telling the first agent that holds this role to flesh it out on wake.
  Same rules as the identity-file seed in 22-02:
    - Do NOT say "Skynet" — the wake-up agent has no idea what that is.
    - Do NOT reference specific id-skill section numbers or the id-skill file path.
    - Say what needs to happen in plain terms.

  Concrete stub `<name>.md` content Task 1's writeMarkdownFileAtomic writes (replaces the
  original stub in Test 6 / Test 8 assertions and Action step 4's stub-build code):

    # <name-verbatim>

    ## Role

    <description>

    <!-- This role file was auto-generated with only a basic description. On first wake
    of an agent holding this role, please flesh out the role with standing directives,
    learned preferences, and a 10,000-foot view of the domain the role covers, then
    remove this comment. -->

  Test 6 / Test 8 assertions MUST now:
    (a) Assert the seed comment is present in the stub body (grep-anchored: `This role
        file was auto-generated`, `On first wake of an agent holding this role`, `flesh
        out the role`, `remove this comment`).
    (b) Assert the seed does NOT contain the word `Skynet` (case-insensitive).
    (c) Assert the seed does NOT reference `§2`, `§3`, `id skill`, or `SKILL.md`
        (case-insensitive).
    (d) Assert the description content still appears verbatim under `## Role`.

  Rationale: fewer moving parts. Skynet does minimal file setup; the first-agent-of-role
  does the semantic fleshing-out as part of their normal wake / adopt-directives flow.
  Matches the identity-file seed pattern from 22-02 Task 3.
-->
<task type="auto" tdd="true">
  <name>Task 1: Create POST /roles backend route + createRole API client + nginx block verification</name>
  <files>src/backend/database/routes/roles-create.ts, src/backend/database/routes/roles-create.test.ts, src/backend/database/database.ts, src/ui/api/identities-api.ts, docker/nginx.conf, docker/nginx-https.conf</files>
  <read_first>
    - src/backend/database/routes/roles-list-for-host.ts (the file created in 22-02 Task 1 — reuse the same auth + SSH + host-resolve + error-handler pattern verbatim; the new POST handler is a sibling in the same router file OR a separate router mounted at the same base — see Action step 1 for the decision)
    - src/backend/database/routes/identity-birth-orchestrator.ts (find `ROLE_NAME_PATTERN` from 22-02 — reuse this exact regex; DO NOT redefine)
    - src/backend/database/routes/identity-exists-on-host.ts (full file — the collision-probe pattern this route mirrors for the pre-write existence check)
    - src/backend/claude-session/identity-artifact-reader.ts (L826-906 for writeMarkdownFileAtomic signature — LOAD-BEARING per RESEARCH Pitfall 3)
    - src/backend/database/database.ts (route mount order — new POST /roles handler joins the existing 22-02 `/roles` mount; confirm mount order still puts /roles above /identities)
    - .planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-RESEARCH.md (Finding F8-B6 "Pattern for roles:create mkdir + touch + write" at L522-529; Assumption A5 for ROLE_NAME_PATTERN)
    - src/ui/api/identities-api.ts (L41-70 for createIdentity client shape to mirror; find newly-added listRolesForHost from 22-02)
  </read_first>
  <behavior>
    - Test 1: POST /roles without a body returns 400.
    - Test 2: POST /roles with body {name: "Box_Maintainer"} returns 400 (uppercase + underscore both fail ROLE_NAME_PATTERN).
    - Test 3: POST /roles with body {name: "box-maintainer", description: "", hostId: 5} returns 400 (empty description).
    - Test 4: POST /roles with body {name: "box-maintainer", description: "x", hostId: 5} (hostId=5 not owned by user) returns 404 (resolveHostById returns null).
    - Test 5: POST /roles with valid body + valid host + role folder already exists on the host (execCommand `if [ -d "$HOME/.claude/roles/box-maintainer" ]; then echo exists; fi` returns "exists") returns 409 with `{error: "role exists on host"}`.
    - Test 6: POST /roles happy path — execCommand mock returns "missing" for existence check, then execCommand for the mkdir compound + writeMarkdownFileAtomic for the stub. Response is 201 with `{name: "box-maintainer", description: "x"}`. Also asserts:
      * writeMarkdownFileAtomic was called with target path `~/.claude/roles/box-maintainer/box-maintainer.md`
      * writeMarkdownFileAtomic was called with contents matching `/^# box-maintainer\n\n## Role\n\nx\n$/` (or `/^# Box-maintainer\n\n## Role\n\nx\n$/` — planner picks; keep literal role name to match id skill template per D-CONTEXT §Claude's Discretion "Description placement under `## Role`, matches id skill template")
      * execCommand was called with `mkdir -p "$HOME/.claude/roles/box-maintainer/bounties"` (creates parent AND bounties in one atomic mkdir; -p is idempotent to survive the roles-list race per RESEARCH Security Domain "Role name collision race")
      * execCommand was called with `touch "$HOME/.claude/roles/box-maintainer/history.md"`
    - Test 7: SSH connect failure → 502 with `{error: "SSH connect failed"}`; conn NOT accessed after failure.
    - Test 8: Description with newlines preserved (multi-line textarea per RESEARCH Open Question 4) — writeMarkdownFileAtomic contents match `## Role\n\nline1\nline2\n` verbatim.
    - Test 9: Missing JWT cookie returns 401 (authenticateJWT middleware).
    - Test 10: Description with characters requiring shell escaping (backticks, `$`, quotes) is safely passed via SFTP write (not shell exec) — no shell injection surface. Test asserts the description round-trips into the SFTP contents unmodified.
  </behavior>
  <action>
    1. Extend the existing 22-02 router. Concrete: modify `src/backend/database/routes/roles-list-for-host.ts` to also export a POST handler on the SAME router — OR create a new `src/backend/database/routes/roles-create.ts` file with its own router mounted at the same `/roles` base path in database.ts. Pick the SEPARATE FILE approach (cleaner concerns: list-for-host is SSH+read; create is SSH+write; different failure modes and validation shapes).

    2. Create `src/backend/database/routes/roles-create.ts`:
       - Standard Express router with authenticateJWT + express.json() body parser.
       - Import ROLE_NAME_PATTERN from `./identity-birth-orchestrator.js` (DO NOT redefine — single source of truth per DRY rule).
       - Route: `router.post("/", authenticateJWT, express.json(), async (req, res) => { ... })`.
       - Validate body: `{name: string (must pass ROLE_NAME_PATTERN + length 1-64), description: string (non-empty, ≤4KB), hostId: positive integer}`. 400 on any failure with distinct error messages.
       - `const host = await resolveHostById(hostId, userId);` → 404 if null.
       - Open SSH: `const conn = await connectOneShot(host, 5000);` in try/finally. Wrap SSH failure → 502.
       - Collision check: exec `if [ -d "$HOME/.claude/roles/${name}" ]; then echo exists; else echo missing; fi` → parse stdout, return 409 if "exists".
       - Provision:
         * exec `mkdir -p "$HOME/.claude/roles/${name}/bounties"` (creates parent AND bounties/ atomically; -p makes it idempotent per RESEARCH Security "Role name collision race — accept the race, worst case is duplicate mkdir which is idempotent").
         * exec `touch "$HOME/.claude/roles/${name}/history.md"`.
         * Get remoteHome via `execCommand(conn, "echo $HOME").trim()`.
         * Build stub markdown: `` `# ${name}\n\n## Role\n\n${description}\n` `` — description passed VERBATIM (multi-line preserved). Placed under `## Role` per D-CONTEXT §Claude's Discretion default.
         * Call `writeMarkdownFileAtomic(conn, "${remoteHome}/.claude/roles/${name}/${name}.md", stubMarkdown)` — MUST use the SFTP helper (Pitfall 3).
       - Response 201: `res.json({name, description})`.
       - Finally: `try { conn.end(); } catch {}`.
       - Error handler: sanitize errors, log via sshLogger, return generic 500 `{error: "internal"}` per RESEARCH V7.

    3. In `src/backend/database/database.ts`: import + mount the new router at the SAME base `/roles` — Express supports multiple routers at the same base via chained `app.use("/roles", rolesListForHostRoutes); app.use("/roles", rolesCreateRoutes);`. Both are Router instances; the first router only handles GET so it falls through to the second for POST. Verify mount ordering: both /roles mounts appear BEFORE `app.use("/identities", identitiesRoutes);` at L1808.

    4. Verify existing nginx block from 22-02 handles POST /roles. The block `location ~ ^/roles(/.*)?$` is method-agnostic — POST routes through it. However: check that `client_max_body_size` is set high enough for the description payload (4KB is well within default `client_max_body_size 1M`, but be explicit). If the 22-02 block does not set `client_max_body_size` explicitly, ADD `client_max_body_size 32k;` to BOTH nginx.conf and nginx-https.conf `/roles` blocks (description guard — 4KB in requirement + JSON overhead + safety margin). Do NOT create a new location block if the regex block already covers.

    5. In `src/ui/api/identities-api.ts`: add `createRole(input: {name: string; description: string; hostId: number}): Promise<{name: string; description: string}>` — mirror `createIdentity` client shape (fetch POST with JSON body, credentials: include, throw on non-2xx with the server error message). 409 status maps to a typed error the dialog can render inline (per D-CONTEXT §Failure modes).

    6. Create `src/backend/database/routes/roles-create.test.ts` covering tests 1-10. Use supertest against a minimal express app that mounts only this router. Mock resolveHostById, connectOneShot, execCommand, writeMarkdownFileAtomic. Assert mock invocations for the mkdir/touch/write calls per test 6.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npx vitest run src/backend/database/routes/roles-create.test.ts &amp;&amp; grep -c 'app.use("/roles"' src/backend/database/database.ts</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: all 10 tests in roles-create.test.ts pass.
    - Source assertion: `grep -c 'app.use("/roles"' src/backend/database/database.ts` returns exactly 2 (list router + create router, both at /roles).
    - Route mount order: both /roles mounts appear before /identities. `awk '/app.use\("\/roles"/ {last=NR} /app.use\("\/identities", identitiesRoutes\)/ {i=NR} END {exit (last>0 && i>0 && last<i) ? 0 : 1}' src/backend/database/database.ts` returns 0.
    - Source assertion: `grep -c "writeMarkdownFileAtomic" src/backend/database/routes/roles-create.ts` returns at least 1 (SFTP helper used, not raw sftp.rename per Pitfall 3).
    - Source assertion: `grep -c "ROLE_NAME_PATTERN" src/backend/database/routes/roles-create.ts` returns at least 1 (imported, not redefined).
    - Source assertion: `grep -c "export const ROLE_NAME_PATTERN" src/backend/database/routes/roles-create.ts` returns 0 (NOT redefined — must import from orchestrator).
    - Nginx parity: `grep -c 'location.*roles' docker/nginx.conf` returns at least 1 AND `grep -c 'location.*roles' docker/nginx-https.conf` returns at least 1 (both files have the /roles block — from 22-02, verified still present + possibly updated with client_max_body_size).
    - API client: `grep -c "export.*createRole" src/ui/api/identities-api.ts` returns at least 1.
    - Cross-user isolation: `grep -c "resolveHostById" src/backend/database/routes/roles-create.ts` returns at least 1.
    - No new npm packages: `git diff package.json` returns empty.
  </acceptance_criteria>
  <done>
    POST /roles endpoint creates the role folder + provisions bounties/history/stub via SSH+SFTP. Tests green. API client available. Nginx passthrough verified. Ready for Task 2 (dialog).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create CreateRoleDialog + `+ New role` launcher button in pretty-conversations panel header</name>
  <files>src/ui/sidebar/CreateRoleDialog.tsx, src/ui/sidebar/CreateRoleDialog.test.tsx, src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx</files>
  <read_first>
    - src/ui/sidebar/NewSessionDialog.tsx (L231-350 for props/state pattern; L590-633 for host picker inline listbox render — this is the pattern CreateRoleDialog reuses inline OR extracts; L502-520 for canOpen predicate)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (L370-371 for existing `setNewSessionDialogOpen` pattern; L680-700 for the header where the pencil-icon `+ New agent` button lives — the `+ New role` button goes right next to it; L974-990 for how NewSessionDialog is mounted at the bottom of the panel — mirror for CreateRoleDialog)
    - src/ui/api/identities-api.ts (find newly-added createRole from Task 1)
    - .planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-CONTEXT.md (§Frontend surfaces "CreateRoleDialog — new component. Fields: Name (required, kebab-case-lowercase, validated), Description (required), Host picker (same set NewSessionDialog uses), checkbox `Then create an identity with this role` DEFAULT `true`"; §Claude's Discretion "Placement of + New role button: sidebar next to + New agent")
    - .planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-RESEARCH.md (F1 for host picker anatomy — Recommendation "extract into reusable HostPickerList" is Claude's Discretion; for THIS plan, keep it inline to avoid scope creep, extract later; F7 for dialog + form-handling pattern)
    - src/components/dialog (find the shadcn wrapper file — Dialog/DialogContent/DialogHeader/DialogTitle/DialogDescription/DialogFooter/DialogClose exports)
  </read_first>
  <behavior>
    - Test 11: CreateRoleDialog renders with fields Name (input), Description (textarea), Host picker, `Then create an identity with this role` checkbox (CHECKED by default per D-CONTEXT).
    - Test 12: Name validation — typing `Box_Maintainer` shows inline error "Name must be kebab-case-lowercase (a-z, 0-9, hyphen only)" and disables the Create button. Typing `box-maintainer` clears the error.
    - Test 13: Description validation — empty description disables Create.
    - Test 14: Host validation — no host picked disables Create.
    - Test 15: Auto-select single host — if hostTree has exactly one host, selectedHost is pre-set on open (mirrors NewSessionDialog L316-322 pattern).
    - Test 16: On submit, `createRole` API is called with {name, description, hostId} — mocked to return 201.
    - Test 17: On successful submit with checkbox CHECKED (default), the dialog closes AND invokes the optional `onChainToCreateIdentity({role, host})` callback prop (if provided) — this is the extension point Plan 22-05 wires. In THIS plan, the panel does NOT provide the callback yet — verify it's optional-safe (no crash if callback is undefined).
    - Test 18: On successful submit with checkbox UNCHECKED, the dialog closes and does NOT invoke onChainToCreateIdentity.
    - Test 19: On 409 conflict from server, the dialog stays open and renders inline error "A role named `<name>` already exists on <host>".
    - Test 20: On modal close, all state resets (name, description, host, checkbox back to default true).
    - Test 21 (panel): PrettyConversationsPanel renders a `+ New role` button (aria-label or visible label) in the header, positioned next to the existing `+ New agent` pencil button. Clicking it opens CreateRoleDialog. Only rendered when `onCreateSession` prop is wired (mirrors the pencil's gate at L511 — if the panel is in a mode that has no session-create affordance, the role-create button is also hidden).
  </behavior>
  <action>
    1. Create `src/ui/sidebar/CreateRoleDialog.tsx`:
       - Component props: `{open: boolean; onClose: () => void; hostTree: HostFolder | null; onCreated?: (result: {name: string; description: string; host: Host}) => void; onChainToCreateIdentity?: (opts: {role: string; host: Host}) => void;}`.
       - Use the shadcn `Dialog` wrapper (`@/components/dialog`) — same import as NewSessionDialog.
       - State (mirror NewSessionDialog patterns): name (string), description (string), selectedHost (Host | null), thenCreateIdentity (boolean, DEFAULT TRUE), submitting (boolean), submitError (string | null).
       - flatHosts memo — reuse the `collectAllHosts` helper from NewSessionDialog OR extract to a shared util. For scope: import + call the helper via `import { collectAllHosts } from "./NewSessionDialog";` if collectAllHosts is exported, else inline-duplicate the 10-line DFS with a comment "Duplicated from NewSessionDialog L87-97 pending F1 extraction — see RESEARCH F1 Recommendation".
       - Actually inspect NewSessionDialog.tsx: if `collectAllHosts` is not exported, DO NOT modify NewSessionDialog to export it (that's a scope-creep change). Instead, inline-duplicate the DFS in a private module-local helper with an explicit comment linking to F1.
       - Host picker: reuse the inline listbox shape from NewSessionDialog L590-633 — same visual pattern, same filteredHosts memo derived from flatHosts + search.
       - Name validation: `ROLE_NAME_PATTERN = /^[a-z0-9-]+$/` (declared at file top; matches backend ROLE_NAME_PATTERN by construction).
       - canOpen predicate: `selectedHost !== null && ROLE_NAME_PATTERN.test(name) && description.trim() !== ""`.
       - Submit handler: async function that calls `createRole({name, description, hostId: parseInt(selectedHost.id, 10)})`. On success:
         * If thenCreateIdentity && onChainToCreateIdentity: call `onChainToCreateIdentity({role: name, host: selectedHost})` — this is the SRIC-05 hook.
         * Call optional onCreated + onClose.
       - On 409 error: parse from response, setSubmitError("A role named …").
       - On other errors: setSubmitError(err.message).
       - Reset all state on close (useEffect keyed on `open` — mirror NewSessionDialog L318-351).

    2. Modify `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`:
       - Import CreateRoleDialog and createRole types.
       - Add state near L370: `const [createRoleDialogOpen, setCreateRoleDialogOpen] = useState(false);`.
       - Locate the header block where the pencil `+ New agent` button lives (around L680-700 based on grep). Add a sibling button (same visual style — Plus-like icon from lucide; use a distinct icon such as `UsersPlus` or plain `Plus` with tooltip "New role"). onClick: `setCreateRoleDialogOpen(true)`.
       - Gate: only render this button when the pencil `+ New agent` is also rendered (i.e., `showPencilButton` at L511, or the same `onCreateSession` prop wiring).
       - Mount CreateRoleDialog at the bottom of the panel next to the NewSessionDialog mount (L974-990):
         `<CreateRoleDialog open={createRoleDialogOpen} onClose={() => setCreateRoleDialogOpen(false)} hostTree={hostTree ?? null} onChainToCreateIdentity={undefined /* wired in Plan 22-05 SRIC-05 */} />`.
         Explicit `undefined` with an inline comment referencing Plan 22-05 makes the extension point discoverable.

    3. Create `src/ui/sidebar/CreateRoleDialog.test.tsx` covering behaviors 11-20. Mock createRole. Use @testing-library/react. Extend PrettyConversationsPanel.test.tsx (if present) OR create a lightweight companion test for behavior 21 — `PrettyConversationsPanel.new-role-button.test.tsx` — that renders the panel with a mock hostTree, asserts the `+ New role` button is present, clicks it, asserts CreateRoleDialog is visible.

    Do NOT touch:
    - CreateRoleDialog does NOT open NewSessionDialog directly. The chain is wired via `onChainToCreateIdentity` callback prop — SRIC-05 in Plan 22-05 provides the callback.
    - Existing NewSessionDialog surface (SRIC-02's work) is not changed by this plan.
    - `PrettyConversationRow.tsx` is NOT touched — Clone context menu integration is SRIC-03 in Plan 22-03.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npx vitest run src/ui/sidebar/CreateRoleDialog.test.tsx src/ui/features/pretty-conversations/ 2>&amp;1 | tail -30; npx tsc --noEmit 2>&amp;1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: all 11 tests (11-21) pass across CreateRoleDialog.test.tsx and any updated panel test file.
    - Source assertion: `grep -c "export.*CreateRoleDialog" src/ui/sidebar/CreateRoleDialog.tsx` returns at least 1.
    - Source assertion: `grep -c "thenCreateIdentity.*useState.*true\|useState<boolean>(true)" src/ui/sidebar/CreateRoleDialog.tsx` returns at least 1 (checkbox default TRUE per D-CONTEXT §UX rules).
    - Source assertion: `grep -c "ROLE_NAME_PATTERN.*=.*\\[a-z0-9-\\]" src/ui/sidebar/CreateRoleDialog.tsx` returns 1 (regex matches backend).
    - Source assertion: `grep -c "createRole" src/ui/sidebar/CreateRoleDialog.tsx` returns at least 2 (import + call site).
    - Source assertion: `grep -c "CreateRoleDialog" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns at least 2 (import + mount).
    - Chain hook is opt-in and safe: `grep -c "onChainToCreateIdentity" src/ui/sidebar/CreateRoleDialog.tsx` returns at least 2 (prop declaration + call site with optional-chaining or explicit guard).
    - Deferred wiring note: `grep -c "22-05\|SRIC-05" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns at least 1 (documenting the deferred callback).
    - Type safety: `npx tsc --noEmit` passes with no new errors.
    - No cross-modification of NewSessionDialog: `git diff src/ui/sidebar/NewSessionDialog.tsx` returns empty for this plan.
  </acceptance_criteria>
  <done>
    CreateRoleDialog is live with all validation + checkbox + host picker. `+ New role` launcher button is placed next to `+ New agent` pencil. Chain hook exposed as opt-in prop (unused in this plan, wired in 22-05). Ready for Plan 22-05 to consume the hook.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /roles | Untrusted name/description/hostId JSON body crosses to backend. Auth via JWT cookie. |
| Backend → SSH exec on target host | roleName is shell-interpolated into `mkdir` / `touch` / `if [ -d ]` commands; MUST pass ROLE_NAME_PATTERN before interpolation. |
| Backend → SFTP write | description string is written via SFTP (no shell exec), so shell metacharacters in description don't create injection surface — but path components (name) are still shell-safe by regex gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04-01 | Tampering (shell injection via role name in mkdir/touch commands) | roles-create.ts SSH execs | mitigate | ROLE_NAME_PATTERN gate at route entry (Test 2) + regex-limited character set (`/^[a-z0-9-]+$/`) blocks quotes/backticks/semicolons/spaces. Test 10 asserts round-trip safety of description via SFTP write path (no shell exec of description bytes). |
| T-22-04-02 | Tampering (path traversal via `../` in role name) | roles-create.ts | mitigate | ROLE_NAME_PATTERN blocks dots and slashes. Verified by test 2 (uppercase + underscore) and additional coverage in `roles-create.test.ts` should include `../` rejection. |
| T-22-04-03 | Access Control (cross-user role provisioning via spoofed hostId) | roles-create.ts | mitigate | `resolveHostById(hostId, userId)` gate returns null for cross-user hosts, 404 response per Test 4. Matches existing identity-birth pattern. |
| T-22-04-04 | Denial of Service (unbounded description payload) | roles-create.ts + nginx block | mitigate | Backend caps description at 4KB in body validator. Nginx block sets `client_max_body_size 32k;` guard per Task 1 Action 4. |
| T-22-04-05 | Data corruption (SFTP EEXIST trap on second create of same role name) | writeMarkdownFileAtomic call | mitigate | Uses ext_openssh_rename per Pitfall 3. Collision precheck at Test 5 blocks second creation of same name before we get to writeMarkdownFileAtomic anyway. |
| T-22-04-06 | Race condition (two clients create same role name simultaneously — one wins mkdir, other's `mkdir -p` is silently idempotent, both attempt write) | SSH-side collision check + writeMarkdownFileAtomic | accept | Per RESEARCH Security Domain "Role name collision race — Accept the race — worst case is a duplicate mkdir which is idempotent. Not worth a distributed lock for MVP." Both writes converge on same content since backend generates the stub deterministically from (name, description). |
| T-22-04-07 | Information Disclosure (SSH stderr containing tailnet paths leaks in error response) | roles-create.ts error handler | mitigate | Reuse sanitizeError pattern; 500 responses return `{error: "internal"}` — never raw stderr. Matches RESEARCH V7. |
| T-22-04-SC | Tampering (supply chain — no new packages) | package.json | accept | Zero new dependencies. |
</threat_model>

<verification>
- All new tests pass: roles-create.test.ts + CreateRoleDialog.test.tsx + updated panel test.
- All existing tests still pass.
- TypeScript type-check passes.
- Nginx dual-config parity: `/roles` regex block is present in BOTH nginx.conf and nginx-https.conf.
- Route mount ordering: both /roles routers mounted BEFORE /identities.
- No new npm packages.
- Manual smoke (post-deploy, phase-level UAT): click `+ New role` in panel header, complete the modal with checkbox CHECKED, verify role folder is created on the picked host, verify checkbox intent is set (chain to identity happens in 22-05).
</verification>

<success_criteria>
- SRIC-04 requirement met: `+ New role` launcher exists in panel header; dialog with name/description/host/checkbox works; POST /roles provisions the role folder + stub file end-to-end via SSH+SFTP.
- Chain hook (onChainToCreateIdentity) exposed as opt-in prop for Plan 22-05 to consume — no dead code in this plan (undefined callback is safe).
- Roles-list dropdown from Plan 22-02 will show newly-created roles on next host-selection (no cache invalidation needed — 22-02's useEffect refetches on host change).
</success_criteria>

<output>
Create `.planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-04-SUMMARY.md` when done.
</output>
