---
phase: 22-skynet-ui-parity-with-the-role-identity-paradigm
plan: 05
type: execute
wave: 4
depends_on:
  - 22-02-PLAN
  - 22-04-PLAN
  - 22-03-PLAN
files_modified:
  - src/ui/sidebar/NewSessionDialog.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/sidebar/NewSessionDialog.chain.test.tsx
autonomous: true
requirements:
  - SRIC-05
tags: [skynet, roles, frontend, chain, dialog, ux]

must_haves:
  truths:
    - "NewSessionDialog accepts optional `initialHost: Host | null` and `initialRole: string | null` props"
    - "When both initialHost and initialRole are provided, the dialog opens with selectedHost = initialHost AND selectedRole = initialRole pre-filled (both remain editable per D-CONTEXT §Claude's Discretion default)"
    - "PrettyConversationsPanel wires CreateRoleDialog's onChainToCreateIdentity callback to open NewSessionDialog with the new role + host pre-filled"
    - "Chain only fires when CreateRoleDialog's `Then create an identity with this role` checkbox is TRUE (default true per D-CONTEXT §UX rules)"
    - "When checkbox is FALSE, CreateRoleDialog closes and NewSessionDialog does NOT open — no chain"
    - "Pre-filled host and role are EDITABLE (not locked) in the chained NewSessionDialog per D-CONTEXT §Claude's Discretion default"
    - "Role dropdown in the chained NewSessionDialog immediately fetches roles-for-host for the pre-filled host so the pre-filled role appears in the dropdown options"
  artifacts:
    - path: "src/ui/sidebar/NewSessionDialog.tsx"
      provides: "New optional props initialHost + initialRole; on-open state seeding logic when both are provided"
      contains: "initialRole"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "Chain wiring — CreateRoleDialog.onChainToCreateIdentity opens NewSessionDialog with pre-fill state; new panel state to hold pending pre-fill payload"
      contains: "initialRole"
  key_links:
    - from: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      to: "CreateRoleDialog.onChainToCreateIdentity"
      via: "callback set by panel state, opens NewSessionDialog with pre-fill payload"
      pattern: "onChainToCreateIdentity"
    - from: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      to: "NewSessionDialog initialRole+initialHost props"
      via: "pending pre-fill payload passed into the NewSessionDialog mount"
      pattern: "initialRole"
---

<objective>
Wire the SRIC-05 chain: on CreateRoleDialog submit with the `Then create an identity with this role` checkbox TRUE (default), close CreateRoleDialog and open NewSessionDialog with the newly-created role AND the picked host pre-filled. This closes the loop that Ashley set up when she checked the box — no manual re-picking of host + role.

Purpose: SRIC-05 — completes the create-role → create-identity user journey. Depends on Plan 22-02 (which introduced the Role dropdown in NewSessionDialog) and Plan 22-04 (which introduced CreateRoleDialog with the opt-in `onChainToCreateIdentity` callback prop). This plan is pure frontend orchestration — no backend changes, no new API calls.

Output:
- Modified `NewSessionDialog.tsx` — accepts optional `initialHost` + `initialRole` props; seeds selectedHost + selectedRole on open when both are provided.
- Modified `PrettyConversationsPanel.tsx` — new panel state to hold pending pre-fill; onChainToCreateIdentity callback threaded into CreateRoleDialog; NewSessionDialog mount takes the pending pre-fill payload.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-CONTEXT.md
@.planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-RESEARCH.md
@.planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-02-PLAN.md
@.planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-04-PLAN.md
@src/ui/sidebar/NewSessionDialog.tsx
@src/ui/sidebar/CreateRoleDialog.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add initialHost + initialRole props to NewSessionDialog; seed state on open when both provided; roles dropdown refetches for the pre-filled host</name>
  <files>src/ui/sidebar/NewSessionDialog.tsx, src/ui/sidebar/NewSessionDialog.chain.test.tsx</files>
  <read_first>
    - src/ui/sidebar/NewSessionDialog.tsx (esp. L231-241 for existing props; L243-265 for state declarations; L316-322 for auto-select-single-host pattern on open; L318-351 for on-close reset; the new Role dropdown state introduced by Plan 22-02 — verify its location before editing)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (L974-990 for existing NewSessionDialog mount site)
    - .planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-CONTEXT.md (§Claude's Discretion "When chained into NewSessionDialog with role + host pre-filled, are those fields editable or locked? Default: pre-filled but editable")
    - .planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-RESEARCH.md (F1 note "Wiring the chain (SRIC-05) requires adding those two props (both optional) and using them as the seed for selectedHost and the new role state")
  </read_first>
  <behavior>
    - Test 1: NewSessionDialog rendered with open=true, initialHost=<box-a>, initialRole="box-maintainer" AND identity-mode=true → selectedHost is set to box-a AND selectedRole is set to "box-maintainer" on first render.
    - Test 2: NewSessionDialog rendered with open=true, initialHost=undefined, initialRole=undefined → existing behavior preserved (auto-select-single-host if hostTree has one, else selectedHost=null; selectedRole="").
    - Test 3: NewSessionDialog rendered with open=true, initialHost=<box-a>, initialRole=undefined → selectedHost=box-a, selectedRole="" (role dropdown still requires manual pick).
    - Test 4: NewSessionDialog rendered with open=true, initialHost=undefined, initialRole="box-maintainer" → selectedHost=null (no host to fetch roles for; selectedRole="" — role can't be seeded without a host; auto-select-single-host still runs).
    - Test 5: When the dialog opens with initialHost + initialRole set, the role dropdown fetches roles-for-host via listRolesForHost(initialHost.id) — mocked in the test — and confirms the initialRole appears in the returned options.
    - Test 6: If the initialRole is NOT in the fetched roles list (edge case — race between role creation and fetch; should not happen in normal flow), selectedRole is CLEARED and the user sees the empty dropdown state (no phantom selection of a non-existent role).
    - Test 7: Both initialHost and initialRole are EDITABLE after pre-fill (per D-CONTEXT §Claude's Discretion default). User can change either.
    - Test 8: On modal close, initialHost + initialRole seed values are NOT persisted — a subsequent open without props reverts to the default empty state (mirrors existing L318-351 reset).
    - Test 9: When identity-mode is OFF, initialRole is IGNORED (role dropdown only exists in identity-mode; matches Plan 22-02 gate).
  </behavior>
  <action>
    1. In `src/ui/sidebar/NewSessionDialog.tsx`, extend the props interface at L231-241:
       ```
       initialHost?: Host | null;
       initialRole?: string | null;
       ```
       Both optional, both nullable to align with existing prop patterns.

    2. In the on-open useEffect (currently L318-322 with the auto-select-single-host logic), extend the branch when `open === true`:
       - If `initialHost` is provided (truthy), setSelectedHost(initialHost) — takes precedence over the auto-select-single-host branch.
       - If `initialHost` is NOT provided but hostTree has exactly one host, keep the existing auto-select behavior (test 2).
       - The state seeding must happen BEFORE the roles-for-host useEffect fires (React batches; use the same useEffect to setSelectedHost and let the roles-for-host useEffect fire on the next render — mirror how existing seedings work).
       - After setting selectedHost, if `initialRole` is also provided AND identityMode is on: setSelectedRole(initialRole). The roles-for-host useEffect will fire (keyed on selectedHost.id) and populate rolesForHost; a subsequent effect keyed on `rolesForHost` should VERIFY initialRole is present in the fetched list and, if NOT, clear selectedRole (Test 6).

    3. Add a NEW useEffect keyed on `rolesForHost`:
       - If `initialRole !== null && initialRole !== undefined` AND selectedRole === initialRole AND rolesForHost is non-empty:
         * If rolesForHost has no entry with name === initialRole, setSelectedRole("") (Test 6).
       - Otherwise no-op.

    4. In the on-close reset block (currently L327-343), ensure the reset clears both selectedHost AND selectedRole back to null/empty (this is already the shape from Plan 22-02; verify + preserve). No new reset logic needed — the seed values come in through props and are re-applied on next open.

    5. Extend the roles-for-host useEffect from Plan 22-02: verify it keys on `selectedHost?.id` — that means when Task 1 seeds selectedHost via initialHost, the roles refetch fires automatically. No additional wiring needed.

    6. Create `src/ui/sidebar/NewSessionDialog.chain.test.tsx` covering behaviors 1-9. Mock listRolesForHost. Use @testing-library/react. Extend the existing NewSessionDialog test file OR create a companion file — planner's call. Prefer a companion `.chain.test.tsx` to keep the concern isolated (SRIC-05 is a discrete behavior).

    Do NOT:
    - Add lockedFields, disabled, or readOnly props to selectedHost/selectedRole — per D-CONTEXT §Claude's Discretion default "pre-filled but editable".
    - Change existing behavior when initialHost + initialRole are not provided — Test 2 is the regression gate.
    - Modify the birth stream, avatar batch, or collision precheck logic.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet ; npx vitest run src/ui/sidebar/NewSessionDialog.chain.test.tsx src/ui/sidebar/NewSessionDialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: tests 1-9 pass in NewSessionDialog.chain.test.tsx.
    - All Plan 22-02 role dropdown tests still pass (regression gate).
    - Source assertion: `grep -c "initialHost\|initialRole" src/ui/sidebar/NewSessionDialog.tsx` returns at least 6 (2 prop declarations + 2+ effect references + resets).
    - Source assertion: `grep -c "editable.*after\|editable\|readOnly\|disabled.*selectedHost\|disabled.*selectedRole" src/ui/sidebar/NewSessionDialog.tsx | head -1` — no NEW `readOnly` or `disabled` props added to the host/role controls; pre-fill is soft, not locked.
    - Type safety: `cd /home/ubuntu/skynet ; npx tsc --noEmit` passes.
    - No changes to other files: `git diff --name-only src/` returns only NewSessionDialog.tsx and the test file for this task.
  </acceptance_criteria>
  <done>
    NewSessionDialog can be pre-filled via initialHost + initialRole props. Both stay editable. Role dropdown refetches for the pre-filled host and validates the pre-filled role. Ready for Task 2 (panel wiring).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire chain in PrettyConversationsPanel — CreateRoleDialog.onChainToCreateIdentity opens NewSessionDialog with pre-fill</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx</files>
  <read_first>
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (L370-371 for existing dialog-open state pattern from Plans 22-04/22-03; the CreateRoleDialog mount from Plan 22-04 — locate it and find the `onChainToCreateIdentity={undefined}` placeholder with the 22-05 comment; the NewSessionDialog mount at L974-990)
    - src/ui/sidebar/CreateRoleDialog.tsx (Plan 22-04's implementation — verify onChainToCreateIdentity signature: `(opts: {role: string; host: Host}) => void`)
    - src/ui/sidebar/NewSessionDialog.tsx (find the new initialHost + initialRole props added in Task 1)
    - .planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-CONTEXT.md (§Specific Ideas "Chain sequence" — the four steps map directly to this task)
  </read_first>
  <behavior>
    - Test 10 (panel integration): Panel renders with CreateRoleDialog and NewSessionDialog both mounted. Simulating a CreateRoleDialog submit with checkbox TRUE (the callback fires) with payload {role: "new-role", host: <box-a>} results in: (a) CreateRoleDialog closes, (b) NewSessionDialog opens with initialHost=box-a and initialRole="new-role" props threaded through.
    - Test 11: Simulating CreateRoleDialog submit with checkbox FALSE (callback NOT invoked) results in: (a) CreateRoleDialog closes, (b) NewSessionDialog does NOT open.
    - Test 12: If NewSessionDialog is already open when the chain fires (edge: user has both open somehow), the chain still delivers the pre-fill payload — but this shouldn't happen because CreateRoleDialog and NewSessionDialog are mutually exclusive by UX. Acceptable behavior: the chain overwrites the NewSessionDialog's state with the pre-fill and continues. Test asserts no crash and that initialHost/initialRole are threaded through.
    - Test 13: After NewSessionDialog closes (either via successful submit or user cancel), the pending chain state is cleared — a second click of `+ New agent` opens NewSessionDialog WITHOUT the pre-fill (regression gate).
  </behavior>
  <action>
    1. In `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`:
       - Add new state near the existing dialog-open states (near L370-371 or wherever Plans 22-04/22-03 placed theirs):
         `const [chainPrefill, setChainPrefill] = useState<{role: string; host: Host} | null>(null);`
       - Locate the CreateRoleDialog mount (Plan 22-04 placed it near the bottom of the panel with `onChainToCreateIdentity={undefined /* wired in Plan 22-05 SRIC-05 */}`). Replace `undefined` with a real callback:
         `onChainToCreateIdentity={(opts) => { setCreateRoleDialogOpen(false); setChainPrefill(opts); setNewSessionDialogOpen(true); }}`
         (Panel-local state names must match what Plans 22-04 and 22-02 introduced. Verify `setCreateRoleDialogOpen` and `setNewSessionDialogOpen` exist; adjust identifiers if the earlier plans used different names.)
       - Locate the NewSessionDialog mount (L974-990). Add the new props:
         `initialHost={chainPrefill?.host ?? null}`
         `initialRole={chainPrefill?.role ?? null}`
       - Extend the NewSessionDialog's onClose handler to ALSO clear chainPrefill: `onClose={() => { setNewSessionDialogOpen(false); setChainPrefill(null); }}`. Same for the onCreate success path (`setNewSessionDialogOpen(false); onCreateSession!(opts); setChainPrefill(null);`).

    2. Add a panel-level test file OR extend an existing PrettyConversationsPanel test covering behaviors 10-13. Mock CreateRoleDialog + NewSessionDialog with lightweight fakes that expose their props for inspection. Alternative: use the real components with vi.mock on their API dependencies (listRolesForHost, cloneIdentity, etc. from earlier plans).

    Do NOT:
    - Change CreateRoleDialog internals (it's opt-in to chain via the callback — Plan 22-04 already wired the callback prop plumbing).
    - Change NewSessionDialog internals (Task 1 already added initialHost + initialRole).
    - Lock the pre-filled host or role (D-CONTEXT default: editable).
    - Add any backend call (chain is pure UI orchestration).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet ; npx vitest run src/ui/features/pretty-conversations/</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: tests 10-13 pass in panel test file.
    - Source assertion: `grep -c "chainPrefill\|setChainPrefill" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns at least 4 (declaration + setter uses + clears + reads for props).
    - Source assertion: `grep -c "onChainToCreateIdentity" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns at least 1 AND the callback body sets both createRoleDialogOpen=false AND newSessionDialogOpen=true AND chainPrefill.
    - Source assertion: `grep -c "initialHost=\|initialRole=" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns at least 2 (both props threaded into NewSessionDialog mount).
    - Source assertion (regression gate): `grep -c "onChainToCreateIdentity={undefined" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns 0 (Plan 22-04's placeholder is REMOVED — the callback is now real).
    - Type safety: `cd /home/ubuntu/skynet ; npx tsc --noEmit` passes.
    - No changes to CreateRoleDialog or NewSessionDialog: `git diff --name-only src/ui/sidebar/` returns empty for this task (Task 1 handled NewSessionDialog; CreateRoleDialog is untouched).
  </acceptance_criteria>
  <done>
    Chain wired end-to-end: CreateRoleDialog submit with checkbox true → dialog closes, NewSessionDialog opens with role + host pre-filled and editable. Ready for phase-level UAT.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend component chain — panel state → dialog props | Pure client-side; no untrusted input crosses a network boundary in this plan. |
| Chain pre-fill payload contents (role, host) | Come from the browser's own state (freshly-created via authenticated CreateRoleDialog submit). No cross-user leakage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05-01 | Tampering (a spoofed chainPrefill via devtools sets initialRole to a role that doesn't exist on the pre-filled host) | NewSessionDialog role dropdown validation | mitigate | Task 1 test 6: role-for-host useEffect verifies initialRole appears in the fetched rolesForHost list; if not, selectedRole is cleared. User sees empty dropdown, no phantom selection. |
| T-22-05-02 | Access Control (pre-filled host that user doesn't own from tampered chainPrefill) | NewSessionDialog + downstream birth flow | mitigate | The birth flow's backend already gates on resolveHostById(hostId, userId) — spoofed hostId is 404-rejected server-side. Frontend has no additional attack surface. |
| T-22-05-03 | Denial of Truth (chain leaves NewSessionDialog with stale pre-fill after close, next opens carry old state) | Panel chainPrefill lifecycle | mitigate | Task 2 test 13: onClose clears chainPrefill; onCreate success also clears. Regression gate asserts second manual open has NO pre-fill. |
| T-22-05-04 | UX bug (user unchecks the checkbox mid-typing and the chain still fires) | CreateRoleDialog callback trigger | mitigate | Plan 22-04's CreateRoleDialog only invokes onChainToCreateIdentity when the checkbox is TRUE at submit time (Test 17/18 of Plan 22-04). This plan does not change that gate. |
| T-22-05-SC | Tampering (no new packages) | package.json | accept | Zero new dependencies. |
</threat_model>

<verification>
- All new tests pass: NewSessionDialog.chain.test.tsx + panel-level chain tests.
- All existing tests still pass (Plan 22-02 role dropdown, Plan 22-04 CreateRoleDialog).
- TypeScript type-check passes.
- No backend changes: `git diff --name-only src/backend/` returns empty for this plan.
- No new npm packages.
- Manual smoke (post-deploy, phase-level UAT): click `+ New role`, fill in name/description/host, LEAVE checkbox checked, click Create. Expected: CreateRoleDialog closes, NewSessionDialog opens with host pre-selected and Role dropdown showing the new role as selected. Complete the identity-birth flow. Verify identity is born with the new role.
</verification>

<success_criteria>
- SRIC-05 requirement met: CreateRoleDialog submit with checkbox TRUE opens NewSessionDialog with role + host pre-filled (and editable). Chain does not fire when checkbox is FALSE.
- No backend surface changed (pure UI orchestration).
- Zero dead code — the previously-stub `onChainToCreateIdentity={undefined}` placeholder from Plan 22-04 is REMOVED and replaced with a real callback (verified by regression gate).
</success_criteria>

<output>
Create `.planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-05-SUMMARY.md` when done.
</output>
