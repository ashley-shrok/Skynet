---
phase: 22-skynet-ui-parity-with-the-role-identity-paradigm
plan: 03
type: execute
wave: 3
depends_on:
  - 22-01-PLAN
  - 22-02-PLAN
  - 22-04-PLAN
files_modified:
  - src/backend/database/routes/identity-clone.ts
  - src/backend/database/routes/identity-clone.test.ts
  - src/backend/database/database.ts
  - src/ui/api/identities-api.ts
  - src/ui/sidebar/CloneAgentDialog.tsx
  - src/ui/sidebar/CloneAgentDialog.test.tsx
  - src/ui/features/pretty-conversations/PrettyConversationRow.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - docker/nginx.conf
  - docker/nginx-https.conf
autonomous: true
requirements:
  - SRIC-03
tags: [skynet, clone, backend, frontend, dialog, context-menu, ssh, nginx]

must_haves:
  truths:
    - "Right-clicking a non-RDP conversation row shows a `Clone` item in the context menu (positioned in the items[] array after Pin/Unpin and Hide/Show but before Deactivate — cosmetic position; ordering just needs to be consistent)"
    - "Clicking `Clone` opens CloneAgentDialog with source's identityKey + host resolved from the clicked row's context (row.host.id via existing session store)"
    - "CloneAgentDialog fields: Name (required, blank OR default `<source>-2`), Title (editable, pre-filled from source identity), Voice (editable, pre-filled from source), Avatar (pre-filled from source's bytes as data-url, with a `Regenerate` button that fires postGenerateAvatarBatch seeded from source's role description)"
    - "Host + Role + Color are LOCKED — NOT shown in the modal UI (per D-CONTEXT §UX rules — plan-checker BLOCK if shown)"
    - "Clone submit calls POST /identities/clone with JSON body (NOT multipart — sidesteps Phase 20 patch #77 silent-no-op trap per RESEARCH Pitfall 2) — body: {sourceIdentityKey, hostId, newName, title, voice, avatarCandidateId?}"
    - "Backend clone endpoint reads source's fleet <name>.md for role frontmatter, verifies newName doesn't collide on target host, pre-writes new identity folder + relay-register + Skynet DB row (mirrors identity-birth-orchestrator.ts Step 2.5 + Step 1 patterns)"
    - "POST /identities/clone route mounted in nginx.conf AND nginx-https.conf ABOVE the generic /identities regex block per CLAUDE.md load-bearing rule"
    - "Clone endpoint uses avatarCandidateId JSON path (not multipart File) — sidesteps multipart silent-no-op trap per RESEARCH Pitfall 2"
  artifacts:
    - path: "src/backend/database/routes/identity-clone.ts"
      provides: "POST /identities/clone HTTP route — reads source frontmatter, validates newName collision, pre-writes new fleet folder + relay register + Skynet DB row"
      exports: ["default (Router)"]
    - path: "src/ui/sidebar/CloneAgentDialog.tsx"
      provides: "New dialog — Name/Title/Voice/Avatar editable (Avatar has Regenerate seeded from role description); Host/Role/Color LOCKED and NOT rendered"
      exports: ["CloneAgentDialog"]
    - path: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx"
      provides: "New `onClone` prop threaded through PrettyConversationRowLive; new items[] entry for Clone at L562-585 items builder"
      contains: "onClone"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "CloneAgentDialog open state; onClone callback that captures the clicked row's identityKey + host + opens the dialog"
      contains: "CloneAgentDialog"
    - path: "docker/nginx.conf"
      provides: "location = /identities/clone exact-match block, mounted ABOVE /identities regex"
    - path: "docker/nginx-https.conf"
      provides: "matching block"
  key_links:
    - from: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx"
      to: "onClone prop"
      via: "items[] builder adds Clone entry when onClone is provided AND row is non-RDP AND row has an identity"
      pattern: "onClone"
    - from: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      to: "cloneIdentity (identities-api.ts)"
      via: "CloneAgentDialog submit handler wired via panel-owned state"
      pattern: "cloneIdentity"
    - from: "src/backend/database/routes/identity-clone.ts"
      to: "resolveRoleForIdentity + writeMarkdownFileAtomic (identity-artifact-reader.ts)"
      via: "two-step to extract source role + SFTP write of new identity file with same role: frontmatter"
      pattern: "resolveRoleForIdentity|writeMarkdownFileAtomic"
---

<objective>
Ship clone-identity per SRIC-03: right-click a conversation row → Clone menu item → CloneAgentDialog (Name/Title/Voice/Avatar editable; Host/Role/Color LOCKED and NOT shown) → POST /identities/clone → backend reads source's role from the source's fleet `<name>.md` frontmatter, verifies newName has no collision on the target host (same host as source per LOCKED "same host" rule), pre-writes new fleet folder with same `role:` frontmatter + fresh relay account (mirroring 22-02's Step 2.5 shape) + copies the Skynet DB row with new name + user-edited title/voice/avatar.

Purpose: SRIC-03 — one-click clone of an existing identity while preserving its role/host/color and giving Ashley a fresh relay account + editable presentation fields. Reuses Plan 22-01's `resolveRoleForIdentity` for the role extraction, Plan 22-02's relay-register SSH block pattern, and the existing Phase 20 avatar-batch flow for regenerated candidates.

Output:
- New backend route `identity-clone.ts` mounted at `/identities/clone`.
- New `CloneAgentDialog.tsx` frontend component + tests.
- Modified `PrettyConversationRow.tsx` — adds `onClone` prop + Clone item to items[] builder.
- Modified `PrettyConversationsPanel.tsx` — owns CloneAgentDialog open state + wires onClone.
- Modified `identities-api.ts` — adds `cloneIdentity` client helper.
- New nginx `location = /identities/clone` blocks in BOTH configs mounted ABOVE the /identities regex.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-CONTEXT.md
@.planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-RESEARCH.md
@.planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-01-PLAN.md
@.planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-02-PLAN.md
@.planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-04-PLAN.md
@src/backend/database/routes/identities.ts
@src/backend/database/routes/identity-birth.ts
@src/backend/database/routes/identity-birth-orchestrator.ts
@src/backend/database/routes/identity-avatar-batch.ts
@src/backend/database/db/schema.ts
@src/backend/claude-session/identity-artifact-reader.ts
@src/backend/database/database.ts
@src/ui/api/identities-api.ts
@src/ui/features/pretty-conversations/PrettyConversationRow.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
@src/ui/features/pretty-conversations/PrettyConversationContextMenu.tsx
@src/ui/sidebar/NewSessionDialog.tsx
@docker/nginx.conf
@docker/nginx-https.conf
</context>

<tasks>

<!--
REVISION 2026-08-04 (Ashley, at 22-02 checkpoint — applies HERE via same-pattern extension):
  Skynet does NOT invoke the relay-register block via SSH from the clone endpoint. Same
  refinement as 22-02 Task 3 (revised): the new identity file gets a SEED COMMENT
  instructing the wake-up agent to register the relay account on first wake.

  Concrete new-identity `<name>.md` content the clone endpoint's writeMarkdownFileAtomic
  writes (replaces plan's original stub in Test 8 assertion + Action step's stub-build
  code around L165):

    ---
    role: <sourceRole>
    ---

    <!-- This identity has no relay account yet. On first wake, please register a
    Matrix relay account for this identity and remove this comment. -->

    # <newName>

    (cloned from <sourceIdentityKey>)

  Seed style constraints (baked into test assertions — SAME as 22-02 Task 3):
    - Do NOT include the word "Skynet" (case-insensitive)
    - Do NOT reference `§2`, `§3`, `id skill`, `SKILL.md` (case-insensitive)
    - MUST contain: `This identity has no relay account yet`, `On first wake`,
      `register a Matrix relay account`, `remove this comment`

  Changes to plan text:
    - DROP the "relay-register via SSH" step in the Action code sequence
    - DROP Test-line reference to "relay register block runs via SSH" (Test 8)
    - MODIFY Test 8's writeMarkdownFileAtomic content assertion to include seed comment
    - DROP threat-model rows related to relay-register-hang DoS or relay-creds leak
    - `must_haves` bullet re "relay-register-block" removed; Skynet's provisioning stops
      at file setup, wake-up agent handles relay registration on its own first wake

  Rationale (same as 22-02 revision): fewer moving parts in Skynet, cleaner boundary
  (Skynet does file setup, agent does identity setup), same end-state. Matches the
  identity-file seed pattern shipped in 22-02 Task 3.
-->
<task type="auto" tdd="true">
  <name>Task 1: Create POST /identities/clone backend route + nginx dual-config blocks + cloneIdentity API client (REVISED per 2026-08-04 — no SSH relay-register; seed comment in new identity file instead)</name>
  <files>src/backend/database/routes/identity-clone.ts, src/backend/database/routes/identity-clone.test.ts, src/backend/database/database.ts, src/ui/api/identities-api.ts, docker/nginx.conf, docker/nginx-https.conf</files>
  <read_first>
    - src/backend/database/routes/identity-birth.ts (L47-89 for createIdentityRecord DB-direct pattern to mirror; L137-260 for JSON body validation + resolveHostById + SSH pattern)
    - src/backend/database/routes/identity-birth-orchestrator.ts (Plan 22-02's Step 2.5 pre-write + relay-register block — clone shares this exact provisioning shape, MINUS the tmux/claude launch since clone is just prep, not spawn per RESEARCH Finding B4c)
    - src/backend/database/routes/identity-avatar-batch.ts (L385-420 for getCandidateForBirth + consumeCandidateForBirth — clone uses same candidate-cache lookup)
    - src/backend/database/routes/identities.ts (L52-70 for publicIdentity(row) response shape; L86-171 for POST /identities contract — clone's response shape mirrors this)
    - src/backend/database/db/schema.ts (L654-673 for identities table schema — no `role` or `host_id` columns per D-CONTEXT LOCKED)
    - src/backend/claude-session/identity-artifact-reader.ts (find resolveRoleForIdentity from Plan 22-01; L826-906 for writeMarkdownFileAtomic; L943-967 for writeIdentityFile call pattern)
    - src/backend/database/database.ts (L1786-1810 for route mount order — /identities/clone mount MUST go BEFORE the generic /identities mount, just like /identities/birth at L1804)
    - docker/nginx.conf (L227-238 for /identities/birth exact-match location block pattern to mirror)
    - docker/nginx-https.conf (matching block location)
    - .planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-RESEARCH.md (Example 3 at L768-844 for the full clone route sketch — port verbatim as a starting point; Pitfall 2 for the multipart trap avoidance; F6 for avatar-batch candidate-cache lookup on clone; B4c for relay-register recommendation)
    - .planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-CONTEXT.md (LOCKED: "Clone endpoint is JSON body (NOT multipart) — sidesteps Phase 20 patch #77 silent-no-op trap"; NO `host_id` column added)
  </read_first>
  <behavior>
    - Test 1: POST /identities/clone with empty body returns 400.
    - Test 2: POST /identities/clone with sourceIdentityKey failing IDENTITY_KEY_RE returns 400.
    - Test 3: POST /identities/clone with newName failing IDENTITY_KEY_RE returns 400.
    - Test 4: POST /identities/clone with hostId not owned by user returns 404 (resolveHostById returns null).
    - Test 5: POST /identities/clone when source row not found in Skynet DB (userId + sourceIdentityKey mismatch) returns 404 with error "source not found".
    - Test 6: POST /identities/clone when source's fleet name-file has no role: frontmatter returns 500 with error "source has no role frontmatter" — mirrors resolveRoleForIdentity throw. NO fallback per Pitfall 8.
    - Test 7: POST /identities/clone with newName that already exists on target host (SSH check finds folder) returns 409 with error "identity exists on host".
    - Test 8: POST /identities/clone happy path with avatarCandidateId — asserts: Skynet DB INSERT identities with new nanoid id, identityKey=newName, displayName=newName, title=user-edited, voice=user-edited, colorHue=source.colorHue (LOCKED — user cannot edit), avatarData=candidate.bytes, avatarMime="image/png", fresh etag; SSH mkdir + touch + writeMarkdownFileAtomic for new identity file with same role: frontmatter as source; relay register block runs via SSH; consumeCandidateForBirth called for the candidate ID; response 201 with publicIdentity(newRow) shape.
    - Test 9: POST /identities/clone WITHOUT avatarCandidateId (user didn't regen) — clone uses source's avatarData bytes verbatim. Verifies avatarData in the new row deep-equals source.avatarData buffer.
    - Test 10: SSH connect failure → 502; DB row NOT inserted (transaction-like: SSH prep MUST succeed before DB insert).
    - Test 11: Missing JWT cookie returns 401.
    - Test 12: Multipart requests rejected — endpoint only accepts application/json (415 status) — sidesteps Phase 20 patch #77 trap by contract.
  </behavior>
  <action>
    1. Create `src/backend/database/routes/identity-clone.ts`:
       - Standard Express router with authenticateJWT + express.json() body parser (NOT multer — MUST use JSON body per D-CONTEXT LOCKED + RESEARCH Pitfall 2).
       - Imports: IDENTITY_KEY_RE from identity-artifact-reader.ts, ROLE_NAME_PATTERN from identity-birth-orchestrator.ts (both established in earlier plans). resolveRoleForIdentity, writeMarkdownFileAtomic from identity-artifact-reader.ts. getCandidateForBirth, consumeCandidateForBirth from identity-avatar-batch.ts. resolveHostById from ssh/host-resolver. connectOneShot, execCommand from ssh primitives. createHash from node:crypto. nanoid from nanoid. db from db/index.js. identities from db/schema.js. eq, and from drizzle-orm. publicIdentity from ./identities.js (or duplicate locally with a comment referencing the source).

       Route handler: `router.post("/", authenticateJWT, express.json(), async (req, res) => { ... })`.

       Body validation:
       - Extract sourceIdentityKey, hostId, newName, title, voice, avatarCandidateId from req.body.
       - Validate: sourceIdentityKey passes IDENTITY_KEY_RE, newName passes IDENTITY_KEY_RE, hostId is positive integer, title is non-empty string (max 200 chars), voice is null-or-string (max 100 chars), avatarCandidateId is null-or-string.
       - 400 on any failure with distinct error messages.
       - Content-type gate: `if (!req.is("application/json")) return res.status(415).json({error: "clone requires application/json body"});` (defense-in-depth for Test 12).

       Handler body (mirror RESEARCH Example 3 verbatim):
       - Fetch source row via drizzle select on (userId, sourceIdentityKey). 404 if not present.
       - Fetch avatar bytes: if avatarCandidateId provided, call getCandidateForBirth(userIdNum, avatarCandidateId) — 400 if returns null (expired). Else avatarBytes = sourceRow.avatarData.
       - resolveHostById(hostId, userId) → 404 if null.
       - Open SSH via connectOneShot in try/finally.
       - Try body:
         * const sourceRole = await resolveRoleForIdentity(conn, sourceIdentityKey) — throws if source has no frontmatter (Test 6).
         * Collision check via execCommand: `if [ -d "$HOME/.claude/identities/${newName}" ]; then echo exists; else echo missing; fi` → 409 if trim() === "exists".
         * Provision new fleet folder (mirror 22-02 Step 2.5 exactly): mkdir new identity folder + wakeups subdir, touch handoff.md, writeMarkdownFileAtomic with contents `---\nrole: ${sourceRole}\n---\n\n# ${newName}\n\n(cloned from ${sourceIdentityKey})\n`.
         * Relay register via SSH (mirror 22-02 Step 2.5 relay-register block byte-for-byte — same target homeserver, same inline curl-on-target-host pattern for symmetry with 22-02).
         * Skynet DB insert mirroring identity-birth.ts:69-88 — new nanoid id, identityKey=newName, displayName=newName, title=user-edited (fallback to sourceRow.title if empty), colorHue=sourceRow.colorHue (LOCKED — user CANNOT edit), voice=user-edited (fallback to sourceRow.voice if empty), avatarMime="image/png", avatarData=avatarBytes, avatarEtag=createHash("md5").update(avatarBytes).digest("hex"), createdAt/updatedAt=now.
         * Re-select the new row for the response.
         * if (avatarCandidateId) consumeCandidateForBirth(userIdNum, avatarCandidateId).
         * res.status(201).json(publicIdentity(newRow)).
       - Finally: try conn.end() catch {}.
       - Error handler at end of route file: sanitize + log via databaseLogger.error + 500 with generic error. Matches identities.ts:316-333.

    2. In `src/backend/database/database.ts`: import + mount at `app.use("/identities/clone", identityCloneRoutes);`. CRITICAL: this mount MUST go BEFORE `app.use("/identities", identitiesRoutes);` (L1808) — mirrors the /identities/birth mount at L1804. Insert around L1800 adjacent to other /identities/* specific mounts.

    3. In `docker/nginx.conf`: add a new `location = /identities/clone` EXACT-MATCH block near the existing /identities/birth block (L227-238). Copy the shape: proxy_pass 127.0.0.1:30001, proxy_http_version 1.1, standard proxy_set_header lines (Host / X-Real-IP / X-Forwarded-For / X-Forwarded-Proto), client_max_body_size 4M, proxy_read_timeout 60s. Include a preceding comment: `# Phase 22 (SRIC-03): identity clone endpoint — exact-match mount ABOVE /identities regex so /identities/clone routes here and not to the generic handler.`

    4. In `docker/nginx-https.conf`: add IDENTICAL block. LOAD-BEARING per CLAUDE.md L43-46.

    5. In `src/ui/api/identities-api.ts`: add `cloneIdentity(input: {sourceIdentityKey: string; hostId: number; newName: string; title: string; voice: string | null; avatarCandidateId: string | null}): Promise<Identity>` — mirror createIdentity client shape (POST JSON body, credentials: include, throw on non-2xx with typed 409 error for the dialog).

    6. Create `src/backend/database/routes/identity-clone.test.ts` covering tests 1-12. Supertest against minimal express app with mounted router. Mock resolveHostById, connectOneShot, execCommand, writeMarkdownFileAtomic, resolveRoleForIdentity, getCandidateForBirth, consumeCandidateForBirth. Use a real in-memory better-sqlite3 for the identities table (mirror identity-birth test scaffolding if present).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet ; npx vitest run src/backend/database/routes/identity-clone.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: all 12 tests in identity-clone.test.ts pass.
    - Source assertion: `grep -c 'app.use("/identities/clone"' src/backend/database/database.ts` returns 1.
    - Route mount order gate: /identities/clone appears before /identities in database.ts — verified by `awk '/app.use\("\/identities\/clone"/ {c=NR} /app.use\("\/identities", identitiesRoutes\)/ {i=NR} END {exit (c>0 && i>0 && c<i) ? 0 : 1}' src/backend/database/database.ts` returns 0.
    - Nginx parity: `grep -c 'location = /identities/clone' docker/nginx.conf` returns 1 AND `grep -c 'location = /identities/clone' docker/nginx-https.conf` returns 1.
    - Nginx block ordering in nginx.conf: /identities/clone appears BEFORE `location ~ ^/identities(/.*)?$` — verified by `awk '/location = \/identities\/clone/ {c=NR} /location ~ \^\/identities\(\/.\*\)/ {i=NR} END {exit (c>0 && i>0 && c<i) ? 0 : 1}' docker/nginx.conf` returns 0. Same check on nginx-https.conf returns 0.
    - Source assertion (JSON only, no multipart): `grep -c 'multer\|multipart' src/backend/database/routes/identity-clone.ts` returns 0 (Pitfall 2 avoidance).
    - Source assertion (415 content-type gate): `grep -c '415\|application/json' src/backend/database/routes/identity-clone.ts` returns at least 1.
    - Source assertion (colorHue locked to source): `grep -c 'colorHue: sourceRow.colorHue' src/backend/database/routes/identity-clone.ts` returns 1 — user's inputs do NOT override colorHue per D-CONTEXT LOCKED.
    - Source assertion (role frontmatter preservation): `grep -c 'role: \${sourceRole}' src/backend/database/routes/identity-clone.ts` returns at least 1 — new identity file gets SAME role as source.
    - Source assertion (two-step reuse): `grep -c 'resolveRoleForIdentity' src/backend/database/routes/identity-clone.ts` returns at least 1.
    - Source assertion (SFTP helper reuse): `grep -c 'writeMarkdownFileAtomic' src/backend/database/routes/identity-clone.ts` returns at least 1.
    - No new npm packages.
    - Cross-user isolation: `grep -c 'resolveHostById' src/backend/database/routes/identity-clone.ts` returns at least 1.
    - API client: `grep -c 'export.*cloneIdentity' src/ui/api/identities-api.ts` returns at least 1.
  </acceptance_criteria>
  <done>
    Backend /identities/clone endpoint provisions new fleet folder with same role + fresh relay account + copied DB row via JSON body. Tests green. Nginx passthrough verified. API client available. Ready for Task 2 (dialog + context menu wiring).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create CloneAgentDialog + wire Clone context-menu item in PrettyConversationRow + panel-owned open state</name>
  <files>src/ui/sidebar/CloneAgentDialog.tsx, src/ui/sidebar/CloneAgentDialog.test.tsx, src/ui/features/pretty-conversations/PrettyConversationRow.tsx, src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx</files>
  <read_first>
    - src/ui/features/pretty-conversations/PrettyConversationRow.tsx (L97-181 for PrettyConversationRow prop shape; L562-586 for items[] builder inside the context menu render block; L183-198 for identity resolution via useIdentities().byKey.get(sessionMatchKey(row.targetTmuxSession)))
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (L113-140 for PrettyConversationRowLive wrapper prop shape; L740-820 for the row render sites where onTogglePin/onDeactivate/onToggleHide are threaded; L370-371 for existing dialog-open state pattern; L974-990 for how NewSessionDialog is mounted at panel bottom)
    - src/ui/features/pretty-conversations/PrettyConversationContextMenu.tsx (L23-35 for PrettyContextMenuItem shape — items array is generic, take a new entry with the same shape)
    - src/ui/sidebar/NewSessionDialog.tsx (L263-266 for AvatarCandidate state pattern + candidate rendering; L406-422 for handleGenerate avatar-batch flow that CloneAgentDialog reuses)
    - src/ui/api/identities-api.ts (find newly-added cloneIdentity from Task 1; L88-99 for postGenerateAvatarBatch pattern)
    - .planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-CONTEXT.md (§UX rules — Clone is TRUE-TO-THE-WORD; Host/Role/Color LOCKED and NOT shown; §Frontend surfaces — clone modal fields Name/Title/Voice/Avatar; §Failure modes — clone name collision surfaces inline)
    - .planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-RESEARCH.md (F4 for context-menu items[] builder location — INSIDE PrettyConversationRow.tsx L562-585, NOT PrettyConversationsPanel; F5 for row.host access; F6 for avatar-batch reuse pattern + role-description-as-brief seed recommendation)
  </read_first>
  <behavior>
    - Test 13 (row): PrettyConversationRow with onClone prop provided AND row.host !== null AND identity resolved from row.targetTmuxSession — items[] builder emits a Clone entry (label "Clone", onClick = onClone).
    - Test 14 (row): PrettyConversationRow WITHOUT onClone prop — no Clone entry in items[] (safe to omit for RDP rows / rows without identity).
    - Test 15 (row): PrettyConversationRow with onClone but row has no identity (identity=null via sessionMatchKey → null) — no Clone entry in items[] (clone requires a source identity).
    - Test 16 (panel): PrettyConversationsPanel threads onClone from panel state into PrettyConversationRowLive. Clicking Clone captures (sourceIdentityKey = identity.identityKey, hostId = parseInt(row.host.id, 10)) and opens CloneAgentDialog with those props.
    - Test 17 (dialog): CloneAgentDialog renders with Name (blank input), Title (pre-filled from source identity), Voice (pre-filled from source), Avatar preview (source's bytes as data-url).
    - Test 18 (dialog LOCKED gates): CloneAgentDialog does NOT render Host, Role, or Color pickers (grep-based UI test: query for text "host" / "role" / "color" in the rendered DOM returns no visible input controls — headings that say "cloning from tina" are OK).
    - Test 19 (dialog validation): Empty Name disables Submit. Name failing IDENTITY_KEY_RE disables Submit.
    - Test 20 (dialog regenerate): Clicking "Regenerate avatar" fires postGenerateAvatarBatch with {name: newName, title: editedTitle, brief: sourceRoleDescription} — brief is seeded from source's role description per RESEARCH F6 recommendation option (b). This means the dialog needs the source's role name AND the role description. Concrete: prop `sourceRoleDescription?: string` passed from the panel — the panel fetches the role description via listRolesForHost when the dialog opens (reuses 22-02's endpoint), OR the dialog fetches on-demand via a new prop-passed callback. Simpler: skip the fetch — pass `brief: editedTitle` as a fallback (title is the user's Ashley-facing description of the identity's purpose, which is close enough for archetype seeding). Pick the simpler approach: `brief: editedTitle`. Test asserts postGenerateAvatarBatch is called with brief = editedTitle.
    - Test 21 (dialog submit): On submit, calls cloneIdentity({sourceIdentityKey, hostId, newName, title, voice, avatarCandidateId}) — mocked to return 201 with new identity. Dialog closes on success.
    - Test 22 (dialog 409): On 409 conflict, dialog stays open and renders inline error `Name "<name>" already exists on the source host`.
    - Test 23 (dialog reset): On modal close, all state resets (name, title, voice, avatar, candidates).
  </behavior>
  <action>
    1. Create `src/ui/sidebar/CloneAgentDialog.tsx`:
       - Props: `{open: boolean; onClose: () => void; sourceIdentity: Identity | null; hostId: number | null; onCloned?: (result: Identity) => void;}`.
       - Use shadcn `Dialog` wrapper (@/components/dialog).
       - State (mirror NewSessionDialog patterns): name (string, blank), title (string, seeded from sourceIdentity?.title ?? ""), voice (string, seeded from sourceIdentity?.voice ?? ""), candidates (AvatarCandidate[] = []), pickedCandidateId (string | null), genLoading (boolean), genError (string | null), submitError (string | null), submitting (boolean).
       - Avatar preview default: sourceIdentity's avatarUrl (derive `/identities/${sourceIdentity.id}/avatar` or use the sourceIdentity.avatarEtag pattern — mirror how existing surfaces render identity avatars).
       - useEffect keyed on `open` + `sourceIdentity` — when opening, seed title/voice/name-blank from sourceIdentity; clear candidates + genState + submitError. On close, reset everything.
       - Render blocks:
         * Header: "Clone " + sourceIdentity?.displayName (breadcrumb; does NOT expose host/role/color as editable fields — read-only text is OK per D-CONTEXT).
         * Name input (required, ROLE_NAME_PATTERN-like validation using IDENTITY_KEY_RE = `/^[a-z0-9_-]{1,64}$/`).
         * Title input (editable, pre-filled).
         * Voice picker (reuse `VoicePicker` from src/ui/features/pretty-view/pickers/VoicePicker.tsx per RESEARCH F7).
         * Avatar preview + Regenerate button (renders source's avatar OR a candidate if user picked one).
         * Submit button.
         * Cancel button.
       - Explicitly do NOT render Host picker, Role picker, or Color picker (D-CONTEXT plan-checker BLOCK if present — asserted by Test 18).
       - Submit handler: call cloneIdentity(...) with body per Task 1 client shape. On 409: setSubmitError. On success: call onCloned + onClose.
       - Regenerate handler: postGenerateAvatarBatch({name, title, brief: title}) — brief seeded from title per Test 20 decision. Populates candidates. User picks one to set pickedCandidateId.

    2. Modify `src/ui/features/pretty-conversations/PrettyConversationRow.tsx`:
       - Add optional prop `onClone?: () => void` to the row's props interface (near L133 where onToggleHide is declared). Doc comment: "quick-260804-clone: fired when Ashley clicks the Clone menu item. Provided by PrettyConversationsPanel when the row has an identity AND row.host !== null. Undefined otherwise (RDP rows never get it — onRowContextMenu is not wired for isRdp)."
       - In the items[] builder at L562-585, add a new entry when `onClone` is defined AND identity is non-null:
         `if (onClone && identity) { items.push({ label: "Clone", onClick: onClone }); }`
         Position: between the Hide/Show entry (if present) and the Deactivate entry (if present). Preserves the pattern of adding items conditionally per available props.

    3. Modify `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`:
       - Import CloneAgentDialog + cloneIdentity types.
       - Add state near L370: `const [cloneDialogState, setCloneDialogState] = useState<{sourceIdentity: Identity; hostId: number} | null>(null);`.
       - Add handleRowClone helper (near L581 handleTogglePin): given (row: ConversationRowShape), resolve identity via useIdentities() (already available in the panel; if not, thread through PrettyConversationRowLive's identity resolution). If identity && row.host, setCloneDialogState({sourceIdentity: identity, hostId: parseInt(row.host.id, 10)}).
       - Extend PrettyConversationRowLive's props (L113-140) with `onClone?: () => void`. Thread through to the row.
       - At the row render sites (L740-820), pass `onClone={() => handleRowClone(row)}` alongside existing onTogglePin/onDeactivate/onToggleHide — for non-RDP rows only (mirror existing gate). Skip the prop for rows without an identity (or let handleRowClone no-op gracefully when identity is null).
       - Mount CloneAgentDialog at the bottom of the panel near the NewSessionDialog mount (L974-990):
         `<CloneAgentDialog open={cloneDialogState !== null} onClose={() => setCloneDialogState(null)} sourceIdentity={cloneDialogState?.sourceIdentity ?? null} hostId={cloneDialogState?.hostId ?? null} />`.

    4. Create `src/ui/sidebar/CloneAgentDialog.test.tsx` covering behaviors 17-23. Mock cloneIdentity, postGenerateAvatarBatch. Use @testing-library/react.

    5. Extend `PrettyConversationRow.test.tsx` (if present) or create companion test file covering behaviors 13-15.

    6. Extend `PrettyConversationsPanel.test.tsx` (if present) or create companion test file covering behavior 16.

    Do NOT:
    - Modify NewSessionDialog (avoid scope creep — clone is its own dedicated component per D-CONTEXT §Claude's Discretion Recommendation A6).
    - Add a target-host picker to CloneAgentDialog (LOCKED — plan-checker BLOCK per D-CONTEXT).
    - Add a role picker to CloneAgentDialog (LOCKED — plan-checker BLOCK).
    - Add a color picker to CloneAgentDialog (LOCKED — plan-checker BLOCK).
    - Add role-description-seeded avatar regen (deferred — Test 20 uses editedTitle as brief for simplicity per action step 1 decision; a later polish task can wire the role description if UX demands it).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet ; npx vitest run src/ui/sidebar/CloneAgentDialog.test.tsx src/ui/features/pretty-conversations/</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: all 11 tests (13-23) pass across CloneAgentDialog test + row test + panel test files.
    - Source assertion (LOCKED gates): `grep -Ei "HostPicker|HostSelect|<Host|hostPicker|selectedHost.*=.*useState|role.*picker|colorPicker|ColorPicker|hue.*picker" src/ui/sidebar/CloneAgentDialog.tsx` returns NO matches for UI-editable host/role/color picker constructs. (Reading text like "Cloning from ${sourceIdentity.displayName}" is allowed; picker components are not.)
    - Source assertion (VoicePicker reuse): `grep -c "VoicePicker" src/ui/sidebar/CloneAgentDialog.tsx` returns at least 1.
    - Source assertion (cloneIdentity call): `grep -c "cloneIdentity" src/ui/sidebar/CloneAgentDialog.tsx` returns at least 2 (import + call).
    - Source assertion (onClone prop): `grep -c "onClone" src/ui/features/pretty-conversations/PrettyConversationRow.tsx` returns at least 3 (prop declaration + items[] builder guard + items[] push).
    - Source assertion (panel wiring): `grep -c "CloneAgentDialog\|cloneDialogState\|handleRowClone" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns at least 4.
    - Type safety: `cd /home/ubuntu/skynet ; npx tsc --noEmit` passes.
    - No cross-modification of NewSessionDialog: `git diff src/ui/sidebar/NewSessionDialog.tsx` returns empty for this plan.
    - No cross-modification of CreateRoleDialog (Plan 22-04's surface): `git diff src/ui/sidebar/CreateRoleDialog.tsx` returns empty for this plan.
  </acceptance_criteria>
  <done>
    Clone context menu item is live. CloneAgentDialog renders with the 4 editable fields; Host/Role/Color are LOCKED and NOT rendered. Submit calls cloneIdentity end-to-end. 409 collisions surface inline. Ready for phase-level UAT.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /identities/clone | Untrusted JSON body (sourceIdentityKey, hostId, newName, title, voice, avatarCandidateId) crosses to backend. Auth via JWT cookie. |
| Backend → SSH exec on target host | sourceIdentityKey, newName, and (via resolveRoleForIdentity) role are shell-interpolated into `cat` / `mkdir` / `if [ -d ]` / relay-register curl commands. |
| Backend → Skynet DB insert | Copies source row with new nanoid + user-edited fields; colorHue is LOCKED to source. |
| Backend → tailnet homeserver (thenasty:8008) | Relay-register POST for the cloned identity (same as 22-02 Step 2.5). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03-01 | Tampering (shell injection via newName in mkdir/touch/collision-check commands) | identity-clone.ts SSH execs | mitigate | IDENTITY_KEY_RE gate at route entry (Test 3) blocks quotes/backticks/semicolons/spaces. Same regex used by identity-birth per Phase 20 precedent. |
| T-22-03-02 | Tampering (shell injection via role name interpolated into new identity file frontmatter) | identity-clone.ts writeMarkdownFileAtomic contents | mitigate | Role is extracted via resolveRoleForIdentity (Plan 22-01), which re-validates against IDENTITY_KEY_RE. If source's frontmatter contained a malicious role, the throw fires before write. |
| T-22-03-03 | Access Control (cross-user clone via spoofed hostId or sourceIdentityKey) | identity-clone.ts | mitigate | resolveHostById(hostId, userId) gate (404 on mismatch — Test 4). DB select filters on userId (Test 5 for source-not-found). Cross-user leakage requires bypassing both gates. |
| T-22-03-04 | Denial of Truth (multipart silent no-op silently succeeds with partial identity) | identity-clone.ts contract | mitigate | Endpoint is JSON-only. Content-type gate returns 415 for multipart (Test 12). Matches D-CONTEXT LOCKED + RESEARCH Pitfall 2. |
| T-22-03-05 | Data corruption (SFTP EEXIST trap on second clone of same target name) | writeMarkdownFileAtomic call | mitigate | Uses ext_openssh_rename per Pitfall 3. Collision precheck at Test 7 blocks the second write before it reaches SFTP. |
| T-22-03-06 | Information Disclosure (source's SSH stderr leaks in clone error response) | identity-clone.ts error handler | mitigate | sanitizeError pattern; 500 responses return `{error: "internal"}`. |
| T-22-03-07 | Denial of Service (avatar-candidate cache exhaustion via repeated regens) | postGenerateAvatarBatch reuse | accept | Existing per-user (15) + global (100) cache caps at identity-avatar-batch.ts:80-118 already gate this. Clone doesn't uniquely worsen the surface. |
| T-22-03-08 | Tampering (LOCKED fields — host/role/color — bypassed via crafted request body extras) | identity-clone.ts DB insert | mitigate | Handler pulls colorHue from sourceRow.colorHue (NOT req.body); role from resolveRoleForIdentity (NOT req.body); host from resolveHostById (NOT req.body override). Test 8 asserts colorHue === sourceRow.colorHue. Any hostId in body IS used for the SSH target (LOCKED to source's host per D-CONTEXT — since clone is called from a row context, the frontend passes source's host; backend does not re-derive from source, but that matches the spec). |
| T-22-03-09 | Race condition (two clone requests for same newName on same host) | SSH-side collision check | accept | Per RESEARCH Security Domain, accept the race. Worst case: two successful DB inserts + one successful mkdir + one duplicate-mkdir (idempotent). The SECOND SFTP write via writeMarkdownFileAtomic would overwrite the first (both writes deterministic on same inputs from Skynet-side; content converges). Ashley's UX: one duplicate identity in Skynet DB; both point at same fleet folder. Cleanup is deferred to a future "delete identity" bounty (out of scope per CONTEXT.md Deferred). |
| T-22-03-SC | Tampering (supply chain — no new packages) | package.json | accept | Zero new dependencies. |
</threat_model>

<verification>
- All new tests pass: identity-clone.test.ts + CloneAgentDialog.test.tsx + row/panel test files.
- All existing tests still pass.
- TypeScript type-check passes.
- Nginx dual-config parity: /identities/clone exact-match block present in BOTH nginx.conf and nginx-https.conf, both ordered ABOVE the generic /identities regex.
- Route mount ordering: /identities/clone mounted BEFORE /identities in database.ts.
- No multipart in the clone endpoint (Pitfall 2 avoidance verified).
- No new npm packages.
- Manual smoke (post-deploy, phase-level UAT): right-click an existing identity's conversation row, click Clone, edit name/title, submit. Verify: (a) new identity row in Skynet DB with source's colorHue; (b) new `~/.claude/identities/<newName>/<newName>.md` on source's host with SAME role: frontmatter as source; (c) new relay account registered on homeserver; (d) 409 fires cleanly on duplicate-name attempt.
</verification>

<success_criteria>
- SRIC-03 requirement met: Clone context-menu item wired at row level; CloneAgentDialog renders with 4 editable fields (Host/Role/Color LOCKED and NOT rendered); backend endpoint provisions new fleet folder with same role + fresh relay + copied DB row; sidesteps multipart silent-no-op trap via JSON-only contract.
- Reuses Plan 22-01's resolveRoleForIdentity + Plan 22-02's relay-register block pattern — proves the two-step helper generalizes to a fourth caller and the relay-register block extracts cleanly.
- Zero fallback branches added — missing source role frontmatter surfaces as clear 500 error, not silent (no-role) clone (Pitfall 8).
</success_criteria>

<output>
Create `.planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-03-SUMMARY.md` when done.
</output>
