---
phase: 22-skynet-ui-parity-with-the-role-identity-paradigm
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/database/routes/roles-list-for-host.ts
  - src/backend/database/routes/identity-birth-orchestrator.ts
  - src/backend/database/routes/identity-birth.ts
  - src/backend/database/database.ts
  - src/ui/api/identities-api.ts
  - src/ui/sidebar/NewSessionDialog.tsx
  - docker/nginx.conf
  - docker/nginx-https.conf
  - src/backend/database/routes/roles-list-for-host.test.ts
  - src/backend/database/routes/identity-birth-orchestrator.role-frontmatter.test.ts
autonomous: true
requirements:
  - SRIC-02
tags: [skynet, roles, backend, frontend, birth, dropdown, ssh, nginx]

must_haves:
  truths:
    - "NewSessionDialog shows a REQUIRED Role dropdown positioned near the host picker"
    - "Role dropdown populates from GET /roles?hostId=<n> (or equivalent) whenever host selection changes"
    - "Submit is blocked until Role has a value (dropdown non-empty AND user has picked one)"
    - "Identity-birth POST payload includes a `role` field passed through to the orchestrator"
    - "Birth orchestrator pre-writes ~/.claude/identities/<name>/<name>.md with `role: <picked>` YAML frontmatter BEFORE Step 5's /id <name> send-keys (per Assumption A4 resolution B4b(a) — LOCKED-by-planner)"
    - "Birth orchestrator ALSO pre-creates wakeups/ dir and touches handoff.md so id skill §3 (load-existing) branch fires and the interactive create-flow is skipped"
    - "Birth orchestrator invokes the relay-register block via SSH before /id <name> so on-wake receiver has real creds (per RESEARCH Open Question 3)"
    - "Zero roles on host renders `no roles on this host — create one first` link inline in dropdown (link handler is a no-op stub in this plan; wired in 22-04/22-05)"
    - "GET /roles?hostId=<n> route mounted in nginx.conf AND nginx-https.conf via matching location blocks"
  artifacts:
    - path: "src/backend/database/routes/roles-list-for-host.ts"
      provides: "GET /roles?hostId=<n> HTTP route — SSHes to host, lists ~/.claude/roles/, returns [{name, description}] where description = ## Role section content"
      exports: ["default (Router)"]
    - path: "src/backend/database/routes/identity-birth-orchestrator.ts"
      provides: "New Step 2.5 in birth flow — pre-writes identity file with role: frontmatter, mkdir wakeups, touch handoff, invoke relay-register via SSH"
      contains: "role"
    - path: "src/ui/sidebar/NewSessionDialog.tsx"
      provides: "Required Role dropdown + host-change repopulate + submit-block-if-empty + role in birth payload"
      contains: "Role"
    - path: "docker/nginx.conf"
      provides: "location ~ ^/roles(/.*)?$ block mounted ABOVE /identities regex"
      contains: "/roles"
    - path: "docker/nginx-https.conf"
      provides: "matching location ~ ^/roles(/.*)?$ block (parity with nginx.conf per CLAUDE.md load-bearing rule)"
      contains: "/roles"
  key_links:
    - from: "src/ui/sidebar/NewSessionDialog.tsx"
      to: "GET /roles?hostId=<n>"
      via: "listRolesForHost API client call in useEffect keyed on selectedHost"
      pattern: "listRolesForHost"
    - from: "src/ui/sidebar/NewSessionDialog.tsx"
      to: "POST /identities/birth"
      via: "openBirthStream body now includes role"
      pattern: "role"
    - from: "src/backend/database/routes/identity-birth-orchestrator.ts"
      to: "writeMarkdownFileAtomic (identity-artifact-reader.ts:852)"
      via: "Step 2.5 SFTP write of identity file with role: frontmatter"
      pattern: "writeMarkdownFileAtomic"
---

<objective>
Introduce role scoping into three coordinated surfaces per SRIC-02: (1) a new `GET /roles?hostId=<n>` HTTP route that SSHes to a host, lists `~/.claude/roles/`, and returns `[{name, description}]`; (2) the Phase 20 identity-birth orchestrator gains a new Step 2.5 that pre-writes the identity folder with `role:` frontmatter (per RESEARCH Assumption A4 resolution B4b(a) — the /id skill's load-existing branch then fires and the interactive create-flow is skipped); (3) NewSessionDialog gets a required Role dropdown positioned near the host picker, re-populating on host change, blocking submit if empty, and adding `role` to the birth payload.

Purpose: SRIC-02 — establishes the birth-time `role:` frontmatter that Plan 22-01's `resolveRoleForIdentity` reads, and gives Ashley a required-at-create dropdown so every new fleet identity carries a role from the moment of birth.

Output:
- New backend route `roles-list-for-host.ts` mounted at `/roles`.
- Modified `identity-birth-orchestrator.ts` with new Step 2.5 (pre-write identity file + mkdir wakeups + touch handoff + relay-register via SSH).
- Modified `identity-birth.ts` HTTP handler to accept + validate + pass through the `role` param.
- Modified `NewSessionDialog.tsx` with a Role dropdown, host-change repopulate, submit gating, and role in the birth payload.
- Modified `identities-api.ts` with `listRolesForHost` client helper + updated birth stream body type.
- Matching `location ~ ^/roles(/.*)?$` blocks in BOTH nginx configs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-CONTEXT.md
@.planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-RESEARCH.md
@src/backend/database/routes/identity-birth-orchestrator.ts
@src/backend/database/routes/identity-birth.ts
@src/backend/database/routes/identity-exists-on-host.ts
@src/backend/database/routes/identity-avatar-batch.ts
@src/backend/database/database.ts
@src/backend/claude-session/identity-artifact-reader.ts
@src/backend/ssh/ssh-one-shot.ts
@src/backend/ssh/tmux-helper.ts
@src/backend/ssh/host-resolver.ts
@src/ui/api/identities-api.ts
@src/ui/sidebar/NewSessionDialog.tsx
@docker/nginx.conf
@docker/nginx-https.conf
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create GET /roles?hostId=<n> route + nginx dual-config blocks + listRolesForHost API client</name>
  <files>src/backend/database/routes/roles-list-for-host.ts, src/backend/database/routes/roles-list-for-host.test.ts, src/backend/database/database.ts, src/ui/api/identities-api.ts, docker/nginx.conf, docker/nginx-https.conf</files>
  <read_first>
    - src/backend/database/routes/identity-exists-on-host.ts (full file — mirror this shape verbatim for auth + host-resolve + SSH + regex-gate pattern; ~90 lines)
    - src/backend/database/routes/identity-avatar-batch.ts (L1-100 for authenticateJWT + express.Router + error-handler pattern)
    - src/backend/database/database.ts (L1786-1810 for route mount order — the new mount goes ABOVE the generic /identities routes so /roles wins path resolution when a URL happens to start with /r)
    - src/backend/ssh/ssh-one-shot.ts (L19-99 for connectOneShot signature)
    - src/backend/ssh/tmux-helper.ts (L21-50 for execCommand signature)
    - src/backend/ssh/host-resolver.ts (full file for resolveHostById signature + cross-user isolation)
    - src/ui/api/identities-api.ts (L88-99 for postGenerateAvatarBatch client shape to mirror; L101-115 for getIdentityExistsOnHost pattern)
    - docker/nginx.conf (L208-260 for Phase 20 /identities/avatar + /identities/birth + /identities/exists-on-host location blocks — copy the shape for the new /roles block)
    - docker/nginx-https.conf (L221-260 for the mirrored blocks — the parity requirement is load-bearing per CLAUDE.md L43-46)
    - .planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-RESEARCH.md (F8-B6 section for `roles:list-for-host` SSH command pattern; F10 for nginx template; Assumption A2 for description-source decision)
  </read_first>
  <behavior>
    - Test 1: GET /roles without hostId query param returns 400 with `{error: "hostId is required"}`.
    - Test 2: GET /roles?hostId=abc returns 400 (non-integer hostId).
    - Test 3: GET /roles?hostId=99999 returns 404 when resolveHostById returns null (cross-user isolation via existing helper).
    - Test 4: GET /roles?hostId=<valid> with successful SSH: connectOneShot mock returns a conn that execCommand-yields `["box-maintainer\ntina\nnelly"]` for the ls call, then per-role cat calls yield markdown containing `## Role\n<description>`; response is `[{name: "box-maintainer", description: "..."}, {name: "tina", description: "..."}, {name: "nelly", description: "..."}]` — 200 OK.
    - Test 5: Description extraction pulls the content between `## Role` heading and the next `##` heading or EOF. Whitespace-trimmed.
    - Test 6: When `## Role` section is absent from a role file, description falls back to empty string `""` (NOT null) — dropdown still renders that role by name.
    - Test 7: SSH failure (connectOneShot rejects) → 502 with `{error: "SSH connect failed"}`, conn.end() NOT called on a null conn.
    - Test 8: SSH exec success but no roles directory (ls returns empty) → 200 with `[]`.
    - Test 9: Route mounts under authenticateJWT — request without JWT cookie returns 401.
    - Test 10: Role name from SSH ls is regex-validated against `/^[a-z0-9_-]+$/` — entries failing the gate are silently dropped from the response (prevents malformed dir names from reaching the frontend).
  </behavior>
  <action>
    1. Create `src/backend/database/routes/roles-list-for-host.ts` — new Express Router:
       - Import: `express`, `authenticateJWT` from AuthManager pattern (mirror identities.ts:15 shape), `resolveHostById` from `../../ssh/host-resolver.js`, `connectOneShot` from `../../ssh/ssh-one-shot.js`, `execCommand` from `../../ssh/tmux-helper.js`, `sshLogger` + `databaseLogger` from the existing logger module used by identity-exists-on-host.ts.
       - Define `const ROLE_NAME_PATTERN = /^[a-z0-9_-]+$/;` (stricter than IDENTITY_KEY_RE — no dots, slashes, plus, equals; but keep underscores for compat with existing fleet roles like `box-maintainer` and `box_maintainer` if any exist).
       - Actually per D-CONTEXT §Backend endpoints (new) and RESEARCH F7: role validation should match the CreateRoleDialog validation which is kebab-case-lowercase per user constraint. Use `/^[a-z0-9-]+$/` (strict kebab-case, no underscores) for role names read from disk too — silently drop entries that don't match (defense in depth; existing fleet roles are kebab-case per Ashley's paradigm).
       - Route: `router.get("/", authenticateJWT, async (req, res) => { ... })` — the mount at `/roles` in database.ts means this handles `GET /roles`.
       - Parse `hostId` from `req.query`, validate integer > 0, 400 on invalid.
       - Call `resolveHostById(hostId, userId)`; 404 if null.
       - Wrap in try/finally: `const conn = await connectOneShot(host, 5000);` then `try { ... } finally { try { conn.end(); } catch {} }` (matches claude-session-server.ts:1866-1880 SSH resource cleanup pattern from RESEARCH Security Domain).
       - Exec `ls -1 "$HOME/.claude/roles" 2>/dev/null || true` → split lines → filter non-empty → filter ROLE_NAME_PATTERN gate → sort alphabetically.
       - For each role name, exec `cat "$HOME/.claude/roles/${roleName}/${roleName}.md" 2>/dev/null || true` (roleName is regex-safe post gate). Parse the `## Role` section: match `/^##\s+Role\s*\n([\s\S]*?)(?=\n##\s|$)/m` on the markdown. Trim whitespace. Fall back to empty string if section absent (Test 6).
       - For efficiency (avoid N SSH round-trips): use the batched delimiter pattern from RESEARCH F8-B6:
         `echo "===ROLE:box-maintainer===" && cat ".../box-maintainer.md" 2>/dev/null || true ; echo "===ROLE:tina===" && cat ".../tina.md" 2>/dev/null || true ; ...`
         then split the stdout on `===ROLE:<name>===\n` delimiters to get per-role markdown blocks in one SSH round-trip.
       - Return `res.json(roles)` where `roles = [{name, description}]`.
       - Error handler at end of file mirroring identities.ts L316-333 (log via sshLogger.error, return 500 with `{error: "internal"}` — do NOT leak stderr into response body per V7 sanitization).

    2. Add SFTP/SSH error handling: connectOneShot failure → 502 `{error: "SSH connect failed"}`. exec timeout / non-zero exit → log via sshLogger.error but still return `[]` (empty result — Ashley sees "no roles" not a crash; matches Test 8).

    3. In `src/backend/database/database.ts`: import the new router (`import rolesListForHostRoutes from "./routes/roles-list-for-host.js";`) and mount it at `app.use("/roles", rolesListForHostRoutes);` — CRITICAL: this mount MUST be BEFORE `app.use("/identities", identitiesRoutes);` (currently at L1808) to preserve match precedence. Insert around L1797 next to the /identities/avatar/birth mounts for locality.

    4. In `docker/nginx.conf`: add a new `location ~ ^/roles(/.*)?$` block placed near the Phase 20 blocks (around L208-238). Copy the exact shape of the /identities/avatar block: proxy_pass to 127.0.0.1:30001, standard proxy_set_header lines (Host / X-Real-IP / X-Forwarded-For / X-Forwarded-Proto), `proxy_read_timeout 15s;` (roles list is a bounded SSH round-trip). Add a preceding comment: `# Phase 22 (SRIC-02): roles:list-for-host — mount BEFORE /identities regex to win match precedence for /roles/... standalone paths.`

    5. In `docker/nginx-https.conf`: add the IDENTICAL block (copy-paste verbatim from the nginx.conf edit). CLAUDE.md L43-46 makes this parity LOAD-BEARING — missing this file means /roles 200-returns index.html and crashes the frontend on `.map` in production.

    6. In `src/ui/api/identities-api.ts`: add `listRolesForHost(hostId: number): Promise<{name: string; description: string}[]>` — mirror `getIdentityExistsOnHost` client shape (fetch, credentials: include, error-throw on non-2xx). Export the RoleSummary type `export type RoleSummary = { name: string; description: string };`.

    7. Create the vitest test file `src/backend/database/routes/roles-list-for-host.test.ts` covering tests 1-10. Mock `resolveHostById`, `connectOneShot`, `execCommand`. Use supertest + express app harness (mirror the pattern used in `src/backend/database/routes/identity-exists-on-host.test.ts` if it exists; else scaffold minimal express app in the test).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npx vitest run src/backend/database/routes/roles-list-for-host.test.ts &amp;&amp; grep -c '/roles' docker/nginx.conf &amp;&amp; grep -c '/roles' docker/nginx-https.conf</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: all 10 tests in roles-list-for-host.test.ts pass.
    - Source assertion: `grep -c "app.use(\"/roles\", rolesListForHostRoutes)" src/backend/database/database.ts` returns 1 (mount present).
    - Route mount order: line number of `app.use("/roles", ...)` in database.ts is LESS than line number of `app.use("/identities", identitiesRoutes)` — verified by `awk '/app.use\("\/roles"/ {r=NR} /app.use\("\/identities", identitiesRoutes\)/ {i=NR} END {exit (r < i) ? 0 : 1}' src/backend/database/database.ts` returning 0.
    - Nginx parity: `grep -c 'location ~ \^/roles' docker/nginx.conf` returns 1 AND `grep -c 'location ~ \^/roles' docker/nginx-https.conf` returns 1 (both files have matching block per CLAUDE.md L43-46).
    - Nginx block ordering in nginx.conf: the /roles location appears BEFORE `location ~ ^/identities(/.*)?$` — verified by `awk '/location ~ \^\/roles/ {r=NR} /location ~ \^\/identities\(\/.\*\)/ {i=NR} END {exit (r>0 && i>0 && r<i) ? 0 : 1}' docker/nginx.conf` returning 0. Same check on nginx-https.conf returning 0.
    - Source assertion: `grep -c "export.*listRolesForHost\|export type RoleSummary" src/ui/api/identities-api.ts` returns at least 2 (client helper + type both exported).
    - No new npm packages installed: `git diff package.json` returns empty.
    - Cross-user isolation: `grep -c "resolveHostById" src/backend/database/routes/roles-list-for-host.ts` returns at least 1 (route uses the helper, not raw SQL — matches Security Domain V4).
    - SSH resource cleanup: `grep -c "conn.end()" src/backend/database/routes/roles-list-for-host.ts` returns at least 1 (try/finally cleanup).
  </acceptance_criteria>
  <done>
    Backend route + API client + nginx parity all shipped. Route callable end-to-end with proper auth + host isolation. Tests green. Ready for Task 2 to wire the frontend dropdown into this endpoint.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (checkpoint): Confirm Assumption A4 resolution B4b(a) for birth orchestrator (LOAD-BEARING per RESEARCH)</name>
  <what-built>
    RESEARCH.md Finding B4 and Assumption A4 identify a LOAD-BEARING conflict: Skynet's birth orchestrator Step 5 (`/id <name>`) currently triggers the id skill on the box to interactively PROMPT the human for a role — so Skynet cannot inject `role:` frontmatter into a file the id skill hasn't yet created.

    The researcher recommends resolution **B4b(a)** (Skynet-contained): the orchestrator adds a new Step 2.5 that pre-writes `~/.claude/identities/<name>/<name>.md` with `role:` frontmatter + creates `wakeups/` + touches `handoff.md` + invokes the relay-register block via SSH BEFORE Step 5. Then Step 5's `/id <name>` sees an existing file and takes the id-skill "load existing" branch (§3), skipping the interactive create.

    The alternative B4b(b) — modify the fleet-wide id skill to accept `--role <role>` — is CROSS-BOUNDARY (fleet-side work managed by Nelly, not Skynet) and explicitly OUT of scope for Phase 22.

    Per the planning_context "Load-bearing resolution from research", B4b(a) is treated as LOCKED with HIGH researcher confidence. This checkpoint confirms Ashley agrees before Task 3 lands ~40 lines of new orchestrator code on this basis.
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-RESEARCH.md` §B4 (starting around L609) and Common Pitfalls §1 for the full picture.
    2. Read `~/.claude/skills/id/SKILL.md` §2 (create) and §3 (load existing) to confirm the id skill takes the load-existing branch when the file exists — L268 of the skill: "If the file **exists**: load it (see §3)".
    3. Confirm you're comfortable with Skynet pre-writing the identity folder + relay account via SSH BEFORE the /id skill sees it. Alternative is a cross-boundary change to the fleet id skill (out of scope).
  </how-to-verify>
  <resume-signal>
    Type "approved B4b(a)" to proceed. If you prefer B4b(b) (fleet-side change), type "defer B4b(b)" and this phase will need a separate cross-boundary work item first (out of Phase 22 scope).
  </resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Step 2.5 (pre-write identity file with role: frontmatter + mkdir wakeups + touch handoff + SSH-invoke relay register) to identity-birth-orchestrator.ts; thread `role` through identity-birth.ts HTTP handler</name>
  <files>src/backend/database/routes/identity-birth-orchestrator.ts, src/backend/database/routes/identity-birth.ts, src/backend/database/routes/identity-birth-orchestrator.role-frontmatter.test.ts</files>
  <read_first>
    - src/backend/database/routes/identity-birth-orchestrator.ts (full file — esp. L1-100 for constants + types, L340-450 for Step 2 / 3 / 4 / 5 boundaries and shell-escape helpers)
    - src/backend/database/routes/identity-birth.ts (L137-260 for request validation + deps assembly + birthIdentity invocation shape)
    - src/backend/claude-session/identity-artifact-reader.ts (L826-906 for writeMarkdownFileAtomic signature — the load-bearing SFTP tmp+rename helper that MUST be used per RESEARCH Pitfall 3)
    - ~/.claude/skills/id/SKILL.md (§2 CREATE branch L200-350; §3 LOAD-EXISTING branch; L317-346 for the inline register block shape to invoke via SSH per RESEARCH Open Question 3)
    - .planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-RESEARCH.md (Finding B4 at L609-642 for the full B4b(a) resolution; Open Question 3 for relay-register handling)
  </read_first>
  <behavior>
    - Test 11: `birthIdentity` fails 400-equivalent (ended{ok:false, failedStep:1}) when opts.role is absent or fails ROLE_NAME_PATTERN.
    - Test 12: After Step 2 (tmux new-session) succeeds, Step 2.5 fires: writeMarkdownFileAtomic mock is called with target path `~/.claude/identities/<name>/<name>.md` and contents matching `/^---\nrole: <role>\n---/`.
    - Test 13: Step 2.5 also execs `mkdir -p "$HOME/.claude/identities/<name>/wakeups"` and `touch "$HOME/.claude/identities/<name>/handoff.md"` via the execCommand dep.
    - Test 14: Step 2.5 emits step{n:2.5-or-2, phase:"started"} / step{n:2.5-or-2, phase:"completed"} events. (Numbering choice: emit a "step 2.5" event with `n: 2` reason `pre-write-identity-file` OR expand the enum. Prefer expanding BirthEvent step type to `1 | 2 | 3 | 4 | 5 | 6` and renumbering — the FIRST option is more brittle. Actually keep it simple: expand the step enum to include the new phase as its own numbered step. Concrete: reuse the existing step:N event shape with n:2 phase:started/completed already covering "tmux new-session" — insert the new work as a subsequent numbered step 3 with a semantically-named `phase` field. To avoid churn, spec: introduce a discrete new SSE event `step: 2.5, phase: completed, reason: "identity file pre-written with role: <role>"` by widening BirthEvent.step.n to `number` type — but keep step 3/4/5 numbering untouched so the frontend BirthProgress checklist keys still match. Alternative simpler pattern: do the pre-write silently inside Step 2's completed path (no new event) and add a comment "Step 2 now includes B4b(a) pre-write". Choose the silent-inside-Step-2 approach — no frontend SSE consumer change needed.
    - Test 15: Step 5's send-keys still fires `/id <name>` unchanged — Step 2.5 does not replace or reshape Step 5. (id skill sees existing file, takes §3 load-existing branch on box side.)
    - Test 16: Relay register runs before Step 5 via a new SSH exec call. Implementation: exec the register block per id skill L317-346 (POST to http://thenasty:8008/_matrix/client/v3/register via curl inline on the target host; write result to `~/.claude/identities/<name>/relay.json` via SFTP). On register failure, emit failed for the containing step and STOP (per orchestrator's "no rollback, no retry" policy at L17-18). Actually per RESEARCH Open Question 3 recommendation: prefer Skynet-side POST to the homeserver (backend controls it, cleaner error handling). But the SSH-invoke path is also acceptable. Task action locks the SSH-invoke path for symmetry with the identity file write which is already SSH-side.
    - Test 17: Identity file contents include the slim id-skill template body (see id skill §2 for template — `# <Name>\n\n## Role\n\n<role file content will be loaded from role folder>\n\n## Handoff\n<empty>\n`). The exact template body is Claude's Discretion — keep it minimal: `---\nrole: <role>\n---\n\n# <name>\n`. That's enough for the /id skill's §3 load-existing branch to fire (it only checks file existence + frontmatter presence per skill L268).
    - Test 18: identity-birth.ts POST /identities/birth validates `role` field: 400 on missing string or non-matching ROLE_NAME_PATTERN.
    - Test 19: identity-birth.ts passes `role` through to `birthIdentity` opts.
  </behavior>
  <action>
    In `src/backend/database/routes/identity-birth-orchestrator.ts`:

    1. Add `role: string` to `BirthOptions` interface (currently L72-81) — required field.
    2. Add a new constant near existing constants (L44+): `export const ROLE_NAME_PATTERN = /^[a-z0-9-]+$/;` (kebab-case-lowercase per D-CONTEXT §Frontend surfaces "kebab-case-lowercase, required, validated").
    3. Add a new dep to `BirthDeps` interface: `writeMarkdownFileAtomic: (conn: SSHClient, targetPath: string, contents: string) => Promise<void>;` — the injected version of the existing `writeMarkdownFileAtomic` from identity-artifact-reader.ts. This keeps the orchestrator pure/testable.
    4. Immediately AFTER Step 2 completes successfully (after the tmux new-session at L360, before Step 3's hasTrustDialogAccepted write at L367): add the new pre-write block. Wrap in the same "if step 2 failed, don't run" gate (matches current step-progression pattern):

       ```
       // Phase 22 SRIC-02 / B4b(a): pre-write identity folder with role: frontmatter
       // so Step 5's /id <name> takes the load-existing branch (id skill §3) instead
       // of the interactive create branch (§2). See RESEARCH Finding B4.
       ```
       Then:
       - Validate opts.role against ROLE_NAME_PATTERN — throw with clear message if invalid. Emit step failed and ended{ok:false} then return (matches existing failure pattern).
       - Get remoteHome: `const remoteHome = (await deps.execCommand(conn, "echo $HOME")).trim();` (or use existing `execCommand` dep pattern already in scope).
       - Build path: `${remoteHome}/.claude/identities/${opts.name}/${opts.name}.md`.
       - Build stub markdown: `---\nrole: ${opts.role}\n---\n\n# ${opts.name}\n` (minimal — role: frontmatter is what the two-step in Plan 22-01 reads; body is placeholder).
       - Exec `mkdir -p "${remoteHome}/.claude/identities/${opts.name}/wakeups" && touch "${remoteHome}/.claude/identities/${opts.name}/handoff.md"` via deps.execCommand.
       - Call `await deps.writeMarkdownFileAtomic(conn, path, contents)` — MUST use the SFTP helper (NOT raw sftp.rename) per RESEARCH Pitfall 3.
       - Invoke the relay register block per id skill L317-346: exec a compound command that POSTs to `http://thenasty:8008/_matrix/client/v3/register` via inline curl on the target host, captures the response, and writes result to `~/.claude/identities/<name>/relay.json`. Model as a self-contained `deps.execCommand(conn, registerScript)` call — build the script inline; role and name are already regex-validated so shell-interpolation is safe.
       - On any exec/write failure, `emit({ type: "step", n: 2, phase: "failed", reason: sanitizeError(err) }); emit({ type: "ended", ok: false, failedStep: 2 }); return;` (piggyback on Step 2's number so the frontend BirthProgress checklist doesn't need any wiring changes — the pre-write is part of Step 2's contract now).
    5. Do NOT emit new SSE event types — the frontend NewSessionDialog.tsx birth stream consumer (L448-500) does not need updating.
    6. Do NOT change Step 3, 4, or 5 logic. Step 5's `/id <name>` remains unchanged; the id skill on the box will now take the §3 load-existing branch because the file exists.

    In `src/backend/database/routes/identity-birth.ts`:

    7. At L147-155 destructure, add `role` to the body extraction: `const { hostId, name, title, path, colorHue, voice, avatarCandidateId, role } = req.body as Record<string, unknown>;`.
    8. Add validation right after avatarCandidateId validation (~L179): `if (typeof role !== "string" || !ROLE_NAME_PATTERN.test(role.trim())) { res.status(400).json({ error: "role is required and must be kebab-case-lowercase" }); return; }`. Import ROLE_NAME_PATTERN from the orchestrator module.
    9. In the birthIdentity invocation at L239-249, add `role: role.trim()` to the opts.
    10. In the deps object at L217-233, add `writeMarkdownFileAtomic: async (conn, path, contents) => writeMarkdownFileAtomic(conn, path, contents)` — import the helper from `../../claude-session/identity-artifact-reader.js`.

    In test file `identity-birth-orchestrator.role-frontmatter.test.ts`:

    11. Cover behaviors 11-19. Mock all deps (writeMarkdownFileAtomic, execCommand, connectOneShot, createIdentityRecord, etc.). Use `vi.fn()` to capture calls and assert on target paths + markdown content.
    12. Include one integration-flavor test that runs birthIdentity end-to-end with all mocked deps and asserts the CALL ORDER: (1) createIdentityRecord (Step 1), (2) tmux new-session exec (Step 2), (3) writeMarkdownFileAtomic (Step 2.5 pre-write), (4) mkdir+touch execs (Step 2.5), (5) relay register exec (Step 2.5), (6) hasTrustDialogAccepted write (Step 3), (7) Enter train (Step 4), (8) /id name send-keys (Step 5). Use vi.fn().mock.invocationCallOrder to verify ordering.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npx vitest run src/backend/database/routes/identity-birth-orchestrator.role-frontmatter.test.ts src/backend/database/routes/identity-birth-orchestrator.test.ts 2>/dev/null || npx vitest run src/backend/database/routes/identity-birth-orchestrator.role-frontmatter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: tests 11-19 pass.
    - Source assertion: `grep -c "^export const ROLE_NAME_PATTERN" src/backend/database/routes/identity-birth-orchestrator.ts` returns 1.
    - Source assertion: `grep -c "role: string" src/backend/database/routes/identity-birth-orchestrator.ts` returns at least 1 (added to BirthOptions).
    - Source assertion: `grep -c "writeMarkdownFileAtomic" src/backend/database/routes/identity-birth-orchestrator.ts` returns at least 2 (dep declaration + call site).
    - Source assertion: `grep -c "role: \${opts.role}\|role: \\\${opts.role}" src/backend/database/routes/identity-birth-orchestrator.ts` returns at least 1 (frontmatter is embedded correctly in the pre-write template).
    - Source assertion: identity-birth.ts validates role — `grep -c "ROLE_NAME_PATTERN" src/backend/database/routes/identity-birth.ts` returns at least 1.
    - Source assertion: identity-birth.ts passes role — `grep -c "role: role.trim()" src/backend/database/routes/identity-birth.ts` returns 1.
    - Call ordering: the integration-flavor test in test 12/13/16 asserts writeMarkdownFileAtomic is called AFTER tmux new-session exec and BEFORE the send-keys `/id <name>` exec — vi.fn().mock.invocationCallOrder-based.
    - No changes to frontend birth stream consumer (NewSessionDialog.tsx L448-500): `git diff src/ui/sidebar/NewSessionDialog.tsx | grep -c "handleBirth\|birthProgress"` returns 0 for THIS task (Task 4 handles the role dropdown wiring separately).
  </acceptance_criteria>
  <done>
    Birth orchestrator now pre-writes the identity file with role: frontmatter per B4b(a). Relay register invoked before Step 5. Tests green. /id <name> on the box will take the load-existing branch.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add required Role dropdown to NewSessionDialog + host-change repopulate + submit gate + role in birth payload</name>
  <files>src/ui/sidebar/NewSessionDialog.tsx</files>
  <read_first>
    - src/ui/sidebar/NewSessionDialog.tsx (full file — esp. L231-350 for props/state; L284-310 for flatHosts pattern; L318-351 for open/close reset; L316-322 for auto-select-single-host; L406-500 for handleBirth; L502-520 for canOpen predicate; L590-633 for host picker inline listbox render; L502-520 for submit gating logic)
    - src/ui/api/identities-api.ts (find newly-added listRolesForHost from Task 1)
    - .planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-CONTEXT.md (§Frontend surfaces: "NewSessionDialog — new REQUIRED Role dropdown, positioned near host picker. Populates via roles:list-for-host; re-populates when host changes. Blocks submit if empty. Submit payload adds role field." + §Failure modes for zero-roles UX)
    - .planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-RESEARCH.md (F1 for NewSessionDialog host picker anatomy)
  </read_first>
  <behavior>
    - Test 20 (component test): open NewSessionDialog with identity-mode ON, select a host → `listRolesForHost` is called with the host's id. Test uses vi.mock for the API client to control the return.
    - Test 21: When the API returns `[{name: "box-maintainer", description: "..."}, {name: "tina", description: "..."}]`, the Role dropdown renders those two options.
    - Test 22: Changing the selected host clears the current role selection AND triggers a fresh `listRolesForHost` call for the new host.
    - Test 23: Create button is disabled (canOpen=false) when identity-mode is ON and role selection is empty, even if all other required fields are filled.
    - Test 24: On successful submit, the birth stream body includes `role: <selectedRoleName>`.
    - Test 25: Zero-roles-on-host response ([]) renders an inline "no roles on this host — create one first" hint (text-only in this plan; the click handler is a no-op stub — will be wired to CreateRoleDialog in Plan 22-04).
    - Test 26: role state is reset on modal close (mirroring existing state reset pattern at L318-351).
    - Test 27: Role dropdown is NOT shown when identity-mode is OFF (per D-CONTEXT §UX rules "Required-role dropdown is CREATE-only" — meaningless outside identity-birth flow; no visual clutter).
  </behavior>
  <action>
    1. Import: add `listRolesForHost, type RoleSummary` (both exported from identities-api.ts in Task 1) to the existing api-client import block.

    2. Add new state at top of component (near existing state around L243-265):
       - `const [selectedRole, setSelectedRole] = useState<string>("");` — role name string; "" = nothing picked.
       - `const [rolesForHost, setRolesForHost] = useState<RoleSummary[]>([]);` — list from API.
       - `const [rolesLoading, setRolesLoading] = useState<boolean>(false);`
       - `const [rolesError, setRolesError] = useState<string | null>(null);`

    3. Add a useEffect keyed on `selectedHost?.id` and `identityMode` — fires when either changes AND identity-mode is on:
       - If !selectedHost || !identityMode: reset rolesForHost=[], selectedRole="".
       - Else: setRolesLoading(true), await listRolesForHost(parseInt(selectedHost.id, 10)), setRolesForHost(result), clear selectedRole (host changed → force re-pick), setRolesLoading(false). Catch: setRolesError(err.message), setRolesForHost([]).

    4. Extend the modal close reset (L327-343) to also clear the new state: selectedRole, rolesForHost, rolesLoading, rolesError.

    5. Render a new Role dropdown between the host picker (L590-633) and the identity-birth field cluster (~L640+). Only render when identityMode is true. UI: use the same visual pattern as existing pickers (native `<select>` with `<option value="">Pick a role…</option>` disabled+selected as placeholder, then map rolesForHost to `<option value={r.name}>{r.name}</option>` — optionally show description as a small caption below the select). Fire onChange to setSelectedRole(e.target.value). Add a small below-select hint: when rolesForHost.length === 0 && !rolesLoading && !rolesError, render `<button type="button" onClick={/* no-op stub — wired in 22-04 */}>no roles on this host — create one first</button>` — this preserves the UX affordance per D-CONTEXT §Failure modes even before CreateRoleDialog exists.

    6. Update the `canOpen` predicate (currently L511-520). When identityMode is true, add `&& selectedRole !== ""` to the AND chain. When identityMode is false, do NOT check selectedRole (role is CREATE-only per D-CONTEXT §UX rules).

    7. In `handleBirth` (L425-500), extend the `openBirthStream(...)` body payload to include `role: selectedRole` — a single new key in the request body object at L436-444.

    8. Write component tests (vitest + @testing-library/react — mirror the shape of existing NewSessionDialog.test.tsx if present at src/ui/sidebar/NewSessionDialog.test.tsx). Cover behaviors 20-27. Mock listRolesForHost + openBirthStream.

    9. TypeScript: update the type used to describe the birth payload if a named type exists (search for `BirthStreamRequest` or similar in identities-api.ts). Add `role: string` field to that type.

    Do NOT touch:
    - Existing collision precheck logic (L373-403).
    - Existing avatar batch logic (L406-422).
    - Existing birth stream event handling (L450-500) — the role: frontmatter is written by the orchestrator on the box, invisible to the SSE stream.
    - Existing identity-mode-off session-only flow (path field, session name, host picker unchanged).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npx vitest run src/ui/sidebar/NewSessionDialog.test.tsx 2>/dev/null; npx tsc --noEmit 2>&amp;1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: component tests 20-27 pass in NewSessionDialog test suite (extend existing file or create new companion file `NewSessionDialog.role-dropdown.test.tsx`).
    - Source assertion: `grep -c "listRolesForHost" src/ui/sidebar/NewSessionDialog.tsx` returns at least 2 (import + call site).
    - Source assertion: `grep -c "selectedRole" src/ui/sidebar/NewSessionDialog.tsx` returns at least 5 (state + reset + render + canOpen + payload).
    - Source assertion: `grep -c "no roles on this host" src/ui/sidebar/NewSessionDialog.tsx` returns 1 (zero-roles inline hint present).
    - Source assertion: birth stream body includes role — `grep -c "role: selectedRole" src/ui/sidebar/NewSessionDialog.tsx` returns 1.
    - Type safety: `cd /home/ubuntu/skynet &amp;&amp; npx tsc --noEmit` passes (no new type errors).
    - No un-wired affordance: the "no roles" hint's onClick is a documented no-op stub with a comment referencing Plan 22-04 (grep for the referencing comment: `grep -c "22-04\|SRIC-04" src/ui/sidebar/NewSessionDialog.tsx` returns at least 1).
    - No changes to handleBirth's event-loop consumer (L450-490): only the body payload at L436-444 grows the `role` key.
  </acceptance_criteria>
  <done>
    Role dropdown is live in NewSessionDialog. Submit blocks when empty. Birth payload carries role. Zero-roles state has a hint (click-wire deferred to 22-04). Ready for end-to-end UAT after 22-04 lands.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GET /roles?hostId=<n> | Untrusted hostId query param crosses to backend; auth via JWT cookie. |
| Backend → SSH exec on target host | roleName strings from `ls ~/.claude/roles/` are shell-interpolated into `cat` commands per-role; must be regex-validated. |
| Browser → POST /identities/birth | Untrusted `role` string in JSON body crosses to backend; must be regex-validated before reaching the orchestrator (which shell-interpolates it). |
| Skynet backend → tailnet homeserver (thenasty:8008) | Relay-register POST for the pre-written identity — no auth, open registration on the tailnet. Fleet-dependent per RESEARCH Environment Availability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02-01 | Tampering (shell injection via `role` in birth-payload interpolated into pre-write path + relay register script) | identity-birth-orchestrator.ts Step 2.5 | mitigate | Two-layer defense: identity-birth.ts POST handler validates via ROLE_NAME_PATTERN before invoking birthIdentity; orchestrator re-validates opts.role at Step 2.5 entry per RESEARCH Security Domain V10. Regex `/^[a-z0-9-]+$/` blocks quotes, backticks, semicolons, spaces. |
| T-22-02-02 | Tampering (shell injection via role directory names read from `ls ~/.claude/roles/`) | roles-list-for-host.ts | mitigate | ROLE_NAME_PATTERN gate on each ls entry BEFORE it's used in the batched cat command per Task 1 Step 6 test 10. Malformed dir names silently dropped. |
| T-22-02-03 | Access Control (cross-user role listing via spoofed hostId) | roles-list-for-host.ts | mitigate | `resolveHostById(hostId, userId)` gate returns null for cross-user hosts, 404 response per Task 1 test 3. Matches existing identity-exists-on-host.ts pattern. |
| T-22-02-04 | Denial of Service (relay-register hang blocks entire birth flow) | identity-birth-orchestrator.ts Step 2.5 relay register | mitigate | Reuse existing execWithTimeout / execCommand timeout wrapper (3s per artifact-reader, configurable). On timeout: emit step failed + ended{ok:false} per existing orchestrator failure policy at L17-18. Ashley sees clear failure, doesn't lose fleet access. |
| T-22-02-05 | Denial of Truth (frontend silently succeeds create with no role assigned when API breaks) | NewSessionDialog.tsx | mitigate | canOpen gate requires selectedRole !== "" — Create button disabled. When listRolesForHost fails (rolesError set), dropdown renders error state, selectedRole stays "" → submit blocked. Test 23. |
| T-22-02-06 | Information Disclosure (relay account credentials leaked in SSE stream `reason` on register failure) | identity-birth-orchestrator.ts sanitizeError | mitigate | Reuse existing `sanitizeError` at identity-birth-orchestrator.ts:169-176 for all `reason` fields emitted from Step 2.5. Matches RESEARCH Security Domain V7. |
| T-22-02-07 | Tampering (SFTP EEXIST trap on second birth of same identity name) | writeMarkdownFileAtomic call at Step 2.5 | mitigate | Uses ext_openssh_rename helper per RESEARCH Pitfall 3 — the existing regression test at identity-artifact-reader.remote-writes.test.ts guards this. Collision precheck at NewSessionDialog level (L389-403 existing) blocks same-name submission before we get here. |
| T-22-02-SC | Tampering (supply chain — no new packages) | package.json | accept | Zero new dependencies added. All infra (js-yaml, ssh2, express, sharp) already present. No legitimacy audit needed per RESEARCH Package Legitimacy Audit section. |
</threat_model>

<verification>
- All new tests pass: roles-list-for-host.test.ts + identity-birth-orchestrator.role-frontmatter.test.ts + NewSessionDialog role dropdown tests.
- All existing tests still pass: `cd /home/ubuntu/skynet &amp;&amp; npx vitest run src/backend/database/routes/ src/ui/sidebar/`.
- TypeScript type-check passes end-to-end: `npx tsc --noEmit`.
- Nginx parity gate: both nginx.conf and nginx-https.conf have the /roles location block, and the block appears before /identities in both.
- Route mount ordering gate: /roles registered in database.ts before generic /identities mount.
- No new npm packages.
- Manual smoke (post-deploy, Ashley UAT — deferred to phase-level UAT): open NewSessionDialog, pick a fleet host, verify Role dropdown populates from that host's ~/.claude/roles/ contents; pick a role; complete birth; verify the fresh identity's `~/.claude/identities/<name>/<name>.md` on the box contains `role: <picked>` frontmatter.
</verification>

<success_criteria>
- SRIC-02 requirement met: NewSessionDialog blocks submit unless a role is picked from a host-scoped dropdown; birth flow writes `role:` frontmatter into the new identity file; roles-list endpoint responds through both nginx paths.
- Assumption A4 (B4b(a) resolution) implemented Skynet-side, no cross-boundary changes required.
- Backend two-step in Plan 22-01 can now find role frontmatter in every fleshly-birthed identity file post this deploy.
- Zero fallback branches added — the required-role gate is enforced, not softened.
</success_criteria>

<output>
Create `.planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-02-SUMMARY.md` when done.
</output>
