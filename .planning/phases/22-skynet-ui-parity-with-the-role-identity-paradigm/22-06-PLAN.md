---
phase: 22-skynet-ui-parity-with-the-role-identity-paradigm
plan: 06
type: execute
wave: 2
depends_on:
  - 22-01-PLAN
files_modified:
  - src/backend/claude-session/identity-artifact-reader.ts
  - src/backend/claude-session/identity-artifact-reader.role-file.test.ts
  - src/backend/claude-session/claude-session-server.ts
  - src/ui/api/claude-session-api.ts
  - src/ui/features/pretty-view/IdentityModal.tsx
  - src/ui/features/pretty-view/RoleFileTab.tsx
  - src/ui/features/pretty-view/RoleFileTab.test.tsx
autonomous: true
requirements:
  - SRIC-06
tags: [skynet, roles, backend, frontend, identity-modal, role-tab, websocket]

must_haves:
  truths:
    - "IdentityModal renders a Role tab as the FIRST tab in NAV_SECTIONS (position 0)"
    - "activeTab defaults to `role` on modal open (per D-CONTEXT §UX rules — Role tab is FIRST and DEFAULT)"
    - "Role tab renders the identity's role file (~/.claude/roles/<role>/<role>.md) contents via the backend two-step (identity file → frontmatter → role artifact)"
    - "Role tab supports inline edit → save → re-read via `identity:update-role-file` WS op, mirroring the identity-file tab pattern"
    - "New WS ops `identity:get-role-file` and `identity:update-role-file` exist in claude-session-server.ts"
    - "readRoleFile and writeRoleFile helpers exist in identity-artifact-reader.ts and use the resolveRoleForIdentity two-step from Plan 22-01"
    - "SFTP EEXIST regression test still passes (writeRoleFile uses writeMarkdownFileAtomic)"
    - "Zero fallback branches — a missing role: frontmatter throws (per Pitfall 8 lock)"
  artifacts:
    - path: "src/backend/claude-session/identity-artifact-reader.ts"
      provides: "readRoleFile + writeRoleFile helpers"
      contains: "readRoleFile"
    - path: "src/backend/claude-session/claude-session-server.ts"
      provides: "New WS handlers for `identity:get-role-file` and `identity:update-role-file` (byte-shape mirrors of existing identity-file handlers)"
      contains: "identity:get-role-file"
    - path: "src/ui/api/claude-session-api.ts"
      provides: "New WS type definitions — IdentityGetRoleFilePayload, IdentityRoleFileEvent, IdentityUpdateRoleFilePayload, IdentityRoleFileUpdatedEvent"
      contains: "IdentityGetRoleFilePayload"
    - path: "src/ui/features/pretty-view/IdentityModal.tsx"
      provides: "NAV_SECTIONS with role as position-0 entry; activeTab default = `role`; sixth parallel WS one-shot fetch for role file; RoleFileTab renderer integration; updateRoleFile save handler"
      contains: "\"role\""
    - path: "src/ui/features/pretty-view/RoleFileTab.tsx"
      provides: "New tab renderer — byte-shape mirror of IdentityFileTab.tsx"
      exports: ["RoleFileTab"]
  key_links:
    - from: "src/ui/features/pretty-view/IdentityModal.tsx"
      to: "identity:get-role-file WS op"
      via: "openOneShot pattern (mirrors L331-366)"
      pattern: "identity:get-role-file"
    - from: "src/backend/claude-session/claude-session-server.ts"
      to: "readRoleFile/writeRoleFile (identity-artifact-reader.ts)"
      via: "handler imports + calls"
      pattern: "readRoleFile\\|writeRoleFile"
    - from: "src/backend/claude-session/identity-artifact-reader.ts (readRoleFile)"
      to: "resolveRoleForIdentity (introduced Plan 22-01)"
      via: "two-step composition"
      pattern: "resolveRoleForIdentity"
---

<objective>
Land the Role tab in IdentityModal as the FIRST and DEFAULT tab per SRIC-06. Backend adds `identity:get-role-file` + `identity:update-role-file` WS ops that use the two-step (identity file → `role:` frontmatter → open `~/.claude/roles/<role>/<role>.md`) via `resolveRoleForIdentity` (introduced Plan 22-01). Frontend adds `RoleFileTab.tsx` (byte-shape mirror of IdentityFileTab.tsx), reorders NAV_SECTIONS to place role at index 0, changes activeTab default from `"identity"` to `"role"`, adds a sixth parallel one-shot WS request to the modal-open effect, and wires an updateRoleFile save handler.

Purpose: SRIC-06 — makes the current identity's role file visible and editable in the same modal Ashley already uses for identity/history/handoff/wakeups. Reuses the two-step contract from 22-01 (frontend API stays `(identityKey, hostId)` — the two-step is backend-internal per LOCKED D-CONTEXT decision).

Output:
- New helpers `readRoleFile(conn, identityKey)` and `writeRoleFile(conn, identityKey, contents)` in identity-artifact-reader.ts.
- New WS handlers for `identity:get-role-file` (byte-shape mirror of L1928-1975 identity:get-identity-file) and `identity:update-role-file` (byte-shape mirror of L2213+ identity:update-identity-file) in claude-session-server.ts.
- New WS type definitions in claude-session-api.ts.
- New `RoleFileTab.tsx` (mirrors IdentityFileTab.tsx pattern).
- Modified IdentityModal.tsx: NAV_SECTIONS array, activeTab default, additional openOneShot invocation, additional state slot, additional save handler, additional render branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-CONTEXT.md
@.planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-RESEARCH.md
@.planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-01-PLAN.md
@src/backend/claude-session/identity-artifact-reader.ts
@src/backend/claude-session/claude-session-server.ts
@src/ui/api/claude-session-api.ts
@src/ui/features/pretty-view/IdentityModal.tsx
@src/ui/features/pretty-view/IdentityFileTab.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add readRoleFile + writeRoleFile helpers to identity-artifact-reader.ts (reuse resolveRoleForIdentity from Plan 22-01)</name>
  <files>src/backend/claude-session/identity-artifact-reader.ts, src/backend/claude-session/identity-artifact-reader.role-file.test.ts</files>
  <read_first>
    - src/backend/claude-session/identity-artifact-reader.ts (esp. L215-249 for readIdentityFile shape to mirror; L943-967 for writeIdentityFile shape to mirror; L826-906 for writeMarkdownFileAtomic; and the new resolveRoleForIdentity + getLocalRolesRoot from Plan 22-01)
    - .planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-01-PLAN.md (confirms resolveRoleForIdentity + getLocalRolesRoot signatures land in Wave 1)
    - .planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-RESEARCH.md (Example 1 at L710-737 for readRoleFile sketch — Port it as reference, but note Example returns `{markdown, role}` while this task returns just `{markdown}` to byte-match the existing identity-file wire shape)
  </read_first>
  <behavior>
    - Test 1: readRoleFile (LOCAL branch, conn=null) reads from `${getLocalRolesRoot()}/<role>/<role>.md`, returns `{markdown}` string.
    - Test 2: readRoleFile (REMOTE branch) execs `cat "$HOME/.claude/roles/<role>/<role>.md" 2>/dev/null || true` and returns `{markdown: <stdout>}`.
    - Test 3: readRoleFile throws (via resolveRoleForIdentity) when identity file has no role: frontmatter — matches Pitfall 8 no-fallback rule.
    - Test 4: readRoleFile returns `{markdown: ""}` when the role file itself doesn't exist on disk (LOCAL ENOENT + REMOTE empty stdout) — but the identity file did have valid role frontmatter (so resolveRoleForIdentity succeeded).
    - Test 5: writeRoleFile validates identityKey via IDENTITY_KEY_RE (same defense as writeIdentityFile at L943).
    - Test 6: writeRoleFile (REMOTE) calls writeMarkdownFileAtomic with target path `~/.claude/roles/<role>/<role>.md` (NOT identity path).
    - Test 7: writeRoleFile (LOCAL) writes via fs tmp+rename atomic pattern to `${getLocalRolesRoot()}/<role>/<role>.md`.
    - Test 8: writeRoleFile propagates the resolveRoleForIdentity throw when identity file has no role frontmatter.
    - Test 9: writeRoleFile caps contents at the same byte-size cap as writeIdentityFile (find the cap constant at L943+; MUST match for consistency).
  </behavior>
  <action>
    Add TWO new exported functions to `src/backend/claude-session/identity-artifact-reader.ts`, placed near their sibling identity counterparts:

    1. `readRoleFile(conn, identityKey)` — place immediately after readIdentityFile (L249). Signature `(conn: SSHClientType | null, identityKey: string) => Promise<{markdown: string}>` — WIRE SHAPE IDENTICAL to readIdentityFile so the WS handler in claude-session-server.ts is a mechanical mirror.
       - Body: first line `const role = await resolveRoleForIdentity(conn, identityKey);` (throws on missing role per Plan 22-01 contract).
       - LOCAL branch: `const filePath = path.join(getLocalRolesRoot(), role, role + ".md");` then `fs.readFile(filePath, "utf-8")`; ENOENT → return `{markdown: ""}`.
       - REMOTE branch: `const cmd = ` `` `cat "$HOME/.claude/roles/${role}/${role}.md" 2>/dev/null || true` `` ` (role is IDENTITY_KEY_RE-safe per resolveRoleForIdentity's defense-in-depth check from Plan 22-01), then `execWithTimeout(conn, cmd)` and return `{markdown: stdout}`.

    2. `writeRoleFile(conn, identityKey, contents)` — place immediately after writeIdentityFile (L967). Signature `(conn: SSHClientType | null, identityKey: string, contents: string) => Promise<void>` — mirrors writeIdentityFile.
       - Body: first `if (!IDENTITY_KEY_RE.test(identityKey)) throw new Error(...)` (mirror writeIdentityFile guard).
       - Then check byte cap on contents (mirror the same cap value writeIdentityFile uses — find the constant at file top).
       - Then `const role = await resolveRoleForIdentity(conn, identityKey);` (throws propagate).
       - LOCAL branch: `const targetPath = path.join(getLocalRolesRoot(), role, role + ".md");` then use the same fs tmp+rename atomic pattern writeIdentityFile uses at L950-965 (mkdir -p role dir first, since the folder may not exist yet — actually the role folder MUST exist because Plan 22-04 creates it; but defensive mkdir is cheap).
       - REMOTE branch: `const remoteHome = ...` then `writeMarkdownFileAtomic(conn, ` `` `${remoteHome}/.claude/roles/${role}/${role}.md` `` `, contents)` — LOAD-BEARING SFTP helper per RESEARCH Pitfall 3.

    3. Create `src/backend/claude-session/identity-artifact-reader.role-file.test.ts` covering tests 1-9. Mock `resolveRoleForIdentity` OR mock its internal `readIdentityFile` dep. Use the same test scaffolding as `identity-artifact-reader.remote-writes.test.ts` — install a fake ssh2 conn with `exec` and `sftp` methods; install the ext_openssh_rename trap check to guard against a regression to raw sftp.rename.

    Do NOT:
    - Add a `Users` lucide icon or any frontend surface (that's Task 3).
    - Change the readIdentityFile / writeIdentityFile signatures.
    - Return `{markdown, role}` from readRoleFile — return only `{markdown}` to match the identity-file wire shape (byte-shape mirror rule per RESEARCH Pattern 2).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npx vitest run src/backend/claude-session/identity-artifact-reader.role-file.test.ts src/backend/claude-session/identity-artifact-reader.remote-writes.test.ts src/backend/claude-session/identity-artifact-reader.two-step.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: all 9 tests in role-file.test.ts pass.
    - Source assertion: `grep -c "^export async function readRoleFile\|^export async function writeRoleFile" src/backend/claude-session/identity-artifact-reader.ts` returns 2.
    - Source assertion: `grep -c "resolveRoleForIdentity" src/backend/claude-session/identity-artifact-reader.ts` returns at least 4 (Plan 22-01 introduced 3 usages; this task adds 2 more = 5 min, but exact count depends on Plan 22-01's implementation — use `>= 4` as loose gate).
    - Source assertion: `grep -c "writeMarkdownFileAtomic" src/backend/claude-session/identity-artifact-reader.ts` returns at least 3 (existing writeIdentityFile + writeIdentityHistory + writeIdentityHandoff = 3, plus new writeRoleFile = 4 min).
    - Source assertion (path substitution): `grep -c '\$HOME/.claude/roles/\${role}/\${role}\.md\|path.join(getLocalRolesRoot(), role, role' src/backend/claude-session/identity-artifact-reader.ts` returns at least 2 (read path + write path).
    - Plan 22-01 tests still pass: `identity-artifact-reader.two-step.test.ts` all 16 tests green.
    - SFTP EEXIST regression test still passes.
    - No signature drift on readIdentityFile / writeIdentityFile — `grep -A 4 "^export async function readIdentityFile\|^export async function writeIdentityFile" src/backend/claude-session/identity-artifact-reader.ts` shows same argument shapes.
  </acceptance_criteria>
  <done>
    Backend helpers readRoleFile + writeRoleFile are byte-shape mirrors of their identity-file counterparts, doing the two-step internally. Tests green. Ready for Task 2 to wire the WS handlers.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add `identity:get-role-file` + `identity:update-role-file` WS handlers to claude-session-server.ts + type defs to claude-session-api.ts</name>
  <files>src/backend/claude-session/claude-session-server.ts, src/ui/api/claude-session-api.ts</files>
  <read_first>
    - src/backend/claude-session/claude-session-server.ts (L1928-1984 for identity:get-identity-file handler — MIRROR BYTE-SHAPE; L2209-2274 for identity:update-identity-file handler — MIRROR BYTE-SHAPE; L1815-1900 for how bounties handler is scaffolded)
    - src/ui/api/claude-session-api.ts (L219 for the response event discriminated-union member list; L352-360 for IdentityGetIdentityFilePayload + IdentityIdentityFileEvent; L467+ for IdentityUpdateIdentityFilePayload)
    - src/backend/claude-session/identity-artifact-reader.ts (find the just-added readRoleFile + writeRoleFile signatures from Task 1)
    - .planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-RESEARCH.md (Pattern 2 for one-shot WS request/response pattern — no changes needed)
  </read_first>
  <behavior>
    - Test 10 (backend WS integration): send `{type: "identity:get-role-file", identityKey: "tina", hostId: 5}` → handler resolves host, opens SSH conn, calls readRoleFile, responds `{type: "identity:role-file", markdown: "<contents>"}`. Uses existing WS test harness (`claude-session-server.aside.test.ts` or `.aside.integration.test.ts` as template).
    - Test 11: send get-role-file with missing identityKey → response `{type: "identity:role-file", markdown: "", error: "identityKey is required"}` (mirrors identity:get-identity-file error shape at L1974).
    - Test 12: send get-role-file when identity has no role frontmatter → response with `{error: "..."}` string containing the resolveRoleForIdentity throw message (surfaced from readRoleFile).
    - Test 13: send `{type: "identity:update-role-file", identityKey: "tina", hostId: 5, contents: "…"}` → handler resolves host, opens SSH conn, calls writeRoleFile, then re-reads via readRoleFile for confirmed markdown, responds `{type: "identity:role-file-updated", markdown: "…"}` (mirrors identity:update-identity-file at L2213+).
    - Test 14: update-role-file with contents exceeding byte cap → error response.
    - Test 15: update-role-file when identity has no role frontmatter → error response (writeRoleFile propagates the throw).
    - Test 16 (type integrity): TypeScript type-check passes with the four new payload/event types exported from claude-session-api.ts and consumed by the frontend Task 3.
  </behavior>
  <action>
    1. In `src/ui/api/claude-session-api.ts`, near L352-360 (byte-shape mirror the identity-file type block):
       ```ts
       export type IdentityGetRoleFilePayload = {
         type: "identity:get-role-file";
         identityKey: string;
         hostId?: number;
       };
       export type IdentityRoleFileEvent = { type: "identity:role-file"; markdown: string; error?: string };
       ```
       And near L467 (mirror IdentityUpdateIdentityFilePayload):
       ```ts
       export type IdentityUpdateRoleFilePayload = {
         type: "identity:update-role-file";
         identityKey: string;
         hostId: number;
         contents: string;
       };
       export type IdentityRoleFileUpdatedEvent = { type: "identity:role-file-updated"; markdown: string; error?: string };
       ```
       Add BOTH new event types to the outbound discriminated union at L219+ so TypeScript exhaustiveness checks pass at the WS listener call sites.

    2. In `src/backend/claude-session/claude-session-server.ts`:
       - Import `readRoleFile, writeRoleFile` from `./identity-artifact-reader.js` (extend the existing import at L22).
       - Add a new handler block right after the identity:get-identity-file handler (L1928-1984). BYTE-SHAPE MIRROR — same auth check, same hostId parse, same identityKey regex validate, same LOCAL vs REMOTE branch via isLocalHostId, same sshLogger.info shape with `operation: "identity_get_role_file"`, same one-shot WS response `{type: "identity:role-file", markdown, error?}`. The ONLY difference is the reader function: `readRoleFile(conn, identityKey)` instead of `readIdentityFile(conn, identityKey)`.
       - Add a new handler block right after the identity:update-identity-file handler (L2209+). BYTE-SHAPE MIRROR — same validation, same LOCAL/REMOTE routing, same post-write re-read for confirmed markdown, response `{type: "identity:role-file-updated", markdown, error?}`. The only difference: `writeRoleFile` + `readRoleFile` instead of `writeIdentityFile` + `readIdentityFile`.
       - Update the top-of-file JSDoc comment block (L49-101) that documents the WS wire protocol to include the two new message types and response types.

    3. Extend an existing test file OR create a companion `claude-session-server.role-file.test.ts`:
       - Cover tests 10-15 using the existing WS integration test harness (find pattern in `claude-session-server.aside.integration.test.ts`).
       - Mock resolveHostById + connectOneShot + readRoleFile + writeRoleFile at the module level to control responses.

    Do NOT:
    - Add new authenticate paths — the WS bridge is already authenticated in the same handler pattern as identity:get-identity-file.
    - Change any existing WS handler.
    - Add nginx blocks — WS ops ride the existing `/claude-session/websocket/` block per RESEARCH F10 explicit note "WS routes do NOT need new nginx blocks".
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npx vitest run src/backend/claude-session/ &amp;&amp; npx tsc --noEmit 2>&amp;1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: tests 10-15 pass in the new/updated test file.
    - Source assertion: `grep -c 'msg.type === "identity:get-role-file"\|msg.type === "identity:update-role-file"' src/backend/claude-session/claude-session-server.ts` returns 2 (both handlers present).
    - Source assertion: `grep -c "readRoleFile\|writeRoleFile" src/backend/claude-session/claude-session-server.ts` returns at least 2 (imported + called).
    - Source assertion (JSDoc updated): `grep -c "identity:get-role-file\|identity:update-role-file\|identity:role-file" src/backend/claude-session/claude-session-server.ts` returns at least 6 (2 request types + 2 response types + JSDoc mentions).
    - API type defs: `grep -c "IdentityGetRoleFilePayload\|IdentityRoleFileEvent\|IdentityUpdateRoleFilePayload\|IdentityRoleFileUpdatedEvent" src/ui/api/claude-session-api.ts` returns at least 4 (all four types defined + exported).
    - Discriminated union updated: `grep -A 1 "IdentityIdentityFileEvent" src/ui/api/claude-session-api.ts | grep -c "IdentityRoleFileEvent\|IdentityRoleFileUpdatedEvent"` — at least one of the two new events appears within a few lines of the identity-file event in the outbound union list.
    - Type safety: `npx tsc --noEmit` passes.
    - No changes to nginx configs (WS ops don't need blocks): `git diff docker/nginx.conf docker/nginx-https.conf` returns empty for this plan.
  </acceptance_criteria>
  <done>
    Backend WS handlers respond to get-role-file and update-role-file mirroring the identity-file counterparts. Type defs available on the frontend. Ready for Task 3 to consume.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Role tab to IdentityModal (first position, default activeTab) + RoleFileTab component + sixth parallel WS one-shot fetch + updateRoleFile save handler</name>
  <files>src/ui/features/pretty-view/IdentityModal.tsx, src/ui/features/pretty-view/RoleFileTab.tsx, src/ui/features/pretty-view/RoleFileTab.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/IdentityModal.tsx (esp. L2 for lucide-react imports; L167 for activeTab default; L202-208 for NAV_SECTIONS array; L211-216 for TabState declarations; L223-277 for openOneShot pattern + fetch effect; L331-366 for the parallel openOneShot invocations; L459-528 for sendIdentityMutation + updateIdentityFile save handler shape; L700+ for the tab render switch that renders the correct tab component)
    - src/ui/features/pretty-view/IdentityFileTab.tsx (full file — RoleFileTab is a byte-shape mirror; same props {state, onSave?}, same edit-mode toolbar, same monospace textarea, same TabState<string> discriminated union)
    - src/ui/api/claude-session-api.ts (find the new IdentityGetRoleFilePayload / IdentityRoleFileEvent / IdentityUpdateRoleFilePayload / IdentityRoleFileUpdatedEvent types from Task 2)
    - .planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-CONTEXT.md (§UX rules "Role tab is FIRST and DEFAULT — not slotted after Identity, not toggleable in position"; §Claude's Discretion "Icon for the Role tab: probably Users from lucide")
  </read_first>
  <behavior>
    - Test 17: RoleFileTab renders `<div>Loading…</div>` (or existing loading UI) when state.status === "loading".
    - Test 18: RoleFileTab renders the markdown preview when state.status === "ready" and onSave is undefined (read-only mode).
    - Test 19: RoleFileTab renders the edit toolbar + textarea when onSave is provided; clicking Save calls onSave with the draft contents; onSave-Promise-resolves flips back to read mode.
    - Test 20: RoleFileTab renders error state when state.status === "error".
    - Test 21 (modal integration): IdentityModal opens with activeTab === "role" (default) — asserted by which tab pane is initially visible in a testing-library render.
    - Test 22 (modal integration): NAV_SECTIONS position 0 is `{value: "role", label: "Role", Icon: Users}`. Existing tabs {identity, bounties, history, wakeups, handoff} follow at positions 1-5, order preserved from prior state.
    - Test 23 (modal integration): On modal open, a SIXTH openOneShot fires with `{type: "identity:get-role-file", identityKey, hostId}`; on response `{type: "identity:role-file", markdown}`, roleFileState is set to `{status: "ready", data: markdown}`.
    - Test 24 (modal integration): updateRoleFile save handler sends `identity:update-role-file` mutation; on response `{type: "identity:role-file-updated", markdown}` the state re-hydrates.
  </behavior>
  <action>
    1. Create `src/ui/features/pretty-view/RoleFileTab.tsx` — byte-shape mirror of `IdentityFileTab.tsx`. Rename all identifiers `identity` → `role` in surface labels (component name RoleFileTab, textarea placeholder text, "Save" button label if any is prefixed). Keep the exact same props shape `{state: TabState<string>; onSave?: (contents: string) => Promise<void>;}` and the exact same edit-mode toolbar / monospace textarea / cancel-restores-original behavior. Do NOT reimplement — literally copy the file, rename identifiers, that's it.

    2. Create `src/ui/features/pretty-view/RoleFileTab.test.tsx` covering behaviors 17-20 — mirror shape of any existing IdentityFileTab.test.tsx (if absent, use vitest + @testing-library/react from scratch).

    3. Modify `src/ui/features/pretty-view/IdentityModal.tsx`:
       - L2 imports: add `Users` from lucide-react to the existing import line (`import { User, Users, Target, Clock, AlarmClock, Handshake } from "lucide-react";` — merge into the existing lucide import).
       - Import RoleFileTab: `import { RoleFileTab } from "./RoleFileTab";`.
       - Import the new WS types: `IdentityGetRoleFilePayload, IdentityRoleFileEvent, IdentityUpdateRoleFilePayload, IdentityRoleFileUpdatedEvent` from `../../api/claude-session-api`.
       - L167: change `useState("identity")` to `useState("role")` — activeTab defaults to role per D-CONTEXT §UX rules.
       - L202-208: update NAV_SECTIONS array — INSERT `{value: "role", label: "Role", Icon: Users}` at position 0 (index 0). Existing five entries follow at 1-5. Keep `as const` assertion.
       - L211-216: add a new state slot `const [roleFileState, setRoleFileState] = useState<TabState<string>>({ status: "loading" });`.
       - L223-236: extend the reset block to also reset `setRoleFileState({ status: "loading" });`.
       - L331-366: add a SIXTH openOneShot invocation modeled on the identity-file fetch at L331 — payload `{type: "identity:get-role-file", identityKey: identity.identityKey, hostId}` (hostId may be undefined when using LOCAL bind-mount; type is `hostId?: number`), expectedType `"identity:role-file"`, onSuccess sets `setRoleFileState({status: "ready", data: ev.markdown})`, onError sets `setRoleFileState({status: "error", error: err})` (mirror shape).
       - Around L514-528 (updateIdentityFile save handler): add a byte-shape mirror `updateRoleFile` handler that calls `sendIdentityMutation<IdentityUpdateRoleFilePayload, IdentityRoleFileUpdatedEvent>({type: "identity:update-role-file", identityKey, hostId, contents}, "identity:role-file-updated")` and on response sets roleFileState to `{status: "ready", data: ev.markdown}`.
       - Tab render switch (around L700+): add a new branch that renders `<RoleFileTab state={roleFileState} onSave={updateRoleFile} />` when activeTab === "role".

    4. Do NOT:
       - Add any no-role fallback branch — if roleFileState errors (identity had no role frontmatter), RoleFileTab renders its error state normally per Test 20. Ashley sees a clear error, does not see a fake empty state (Pitfall 8).
       - Reorder or touch existing tab render branches for identity/bounties/history/wakeups/handoff.
       - Change existing openOneShot invocations for the other five tabs.
       - Add new backend calls — Task 2's WS handlers already exist.

    5. Extend `IdentityModal.test.tsx` (or create `IdentityModal.role-tab.test.tsx`) covering tests 21-24. Mock openClaudeSessionSocket to control WS response types.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; npx vitest run src/ui/features/pretty-view/ &amp;&amp; npx tsc --noEmit 2>&amp;1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: tests 17-24 pass.
    - Source assertion: `grep -c 'export function RoleFileTab\|export const RoleFileTab' src/ui/features/pretty-view/RoleFileTab.tsx` returns 1.
    - Source assertion (NAV_SECTIONS position 0): the FIRST entry of NAV_SECTIONS in IdentityModal.tsx has value `"role"` — verified by `awk '/const NAV_SECTIONS = \[/,/\] as const;/' src/ui/features/pretty-view/IdentityModal.tsx | grep -n '{ value:' | head -1 | grep -c '"role"'` returns 1.
    - Source assertion (activeTab default): `grep -c 'useState("role")' src/ui/features/pretty-view/IdentityModal.tsx` returns 1 AND `grep -c 'useState("identity")' src/ui/features/pretty-view/IdentityModal.tsx` returns 0 (identity default is REMOVED).
    - Source assertion (Users icon): `grep -c "Users," src/ui/features/pretty-view/IdentityModal.tsx` returns at least 1 (imported from lucide-react in the existing block).
    - Source assertion (new state slot): `grep -c "roleFileState" src/ui/features/pretty-view/IdentityModal.tsx` returns at least 4 (declaration + reset + set-on-success + render).
    - Source assertion (new WS fetch): `grep -c "identity:get-role-file" src/ui/features/pretty-view/IdentityModal.tsx` returns at least 1.
    - Source assertion (new save handler): `grep -c "identity:update-role-file" src/ui/features/pretty-view/IdentityModal.tsx` returns at least 1.
    - Source assertion (RoleFileTab render): `grep -c "<RoleFileTab" src/ui/features/pretty-view/IdentityModal.tsx` returns 1.
    - No no-role fallback: `grep -in "no role\|missing role\|role fallback" src/ui/features/pretty-view/IdentityModal.tsx src/ui/features/pretty-view/RoleFileTab.tsx` returns no matches EXCEPT allowed comments. If a match is present, it must be inside a comment that documents WHY there is no fallback — reviewer verifies.
    - Type safety: `npx tsc --noEmit` passes.
    - Existing IdentityModal tests (bounties, identity file, history, wakeups, handoff render) still pass.
  </acceptance_criteria>
  <done>
    Role tab is FIRST and DEFAULT in IdentityModal. RoleFileTab renders role file with edit+save via new WS ops. All frontend surface is in place. Ashley can now view and edit `~/.claude/roles/<role>/<role>.md` for any identity via the modal.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → WS identity:get-role-file / identity:update-role-file | Untrusted identityKey + hostId + contents cross to backend WS handlers. |
| Backend → SSH exec on target host | role name (extracted from identity file frontmatter, re-validated by IDENTITY_KEY_RE via resolveRoleForIdentity per Plan 22-01) is shell-interpolated into `cat` and `mkdir` and (via writeMarkdownFileAtomic) SFTP path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-06-01 | Tampering (shell injection via role name in cat + writeMarkdownFileAtomic path) | readRoleFile + writeRoleFile REMOTE branches | mitigate | `resolveRoleForIdentity` throws when extracted role fails `IDENTITY_KEY_RE.test(role)` — defense-in-depth from Plan 22-01. Role never reaches shell if it fails the gate. |
| T-22-06-02 | Access Control (WS handler serves another user's role file via spoofed hostId) | claude-session-server.ts identity:get-role-file / update-role-file handlers | mitigate | New handlers reuse the SAME resolveHostById + userId gate as the existing identity-file handlers. Handlers were copied byte-shape from L1928-1984 / L2213+ which already enforce this per RESEARCH V4. |
| T-22-06-03 | Data corruption (SFTP EEXIST trap on second edit of same role file) | writeRoleFile SFTP write | mitigate | Uses writeMarkdownFileAtomic (ext_openssh_rename) per Pitfall 3. The existing regression test at identity-artifact-reader.remote-writes.test.ts installs a raw-rename trap that would fail loudly if writeRoleFile ever regresses. |
| T-22-06-04 | Denial of Service (unbounded contents payload) | writeRoleFile byte cap + claude-session-server update handler | mitigate | Byte cap in writeRoleFile mirrors writeIdentityFile cap (Test 9). Update handler validates before calling writeRoleFile. |
| T-22-06-05 | Denial of Truth (missing role frontmatter is silently accepted as empty content, corrupting the fleet) | readRoleFile / writeRoleFile via resolveRoleForIdentity | mitigate | Throws propagate up through the WS handler and become `{error: "..."}` responses. Frontend RoleFileTab renders the error state (test 20). No fallback empty state per Pitfall 8. |
| T-22-06-06 | Information Disclosure (WS error message leaks tailnet paths or SSH stderr) | claude-session-server.ts error paths | mitigate | Reuse existing sanitizeError helper used by identity-file handlers. |
| T-22-06-SC | Tampering (supply chain — no new packages) | package.json | accept | Zero new dependencies. |
</threat_model>

<verification>
- All new tests pass: role-file.test.ts, claude-session-server WS role-file tests, RoleFileTab.test.tsx, IdentityModal role-tab integration tests.
- All existing IdentityModal/tab tests still pass.
- Plan 22-01 tests still pass (`resolveRoleForIdentity` behavior unchanged).
- TypeScript type-check passes.
- No nginx config changes (WS ops ride existing block).
- No new npm packages.
- Manual smoke (post-deploy, phase-level UAT): open IdentityModal for tina; verify Role tab is FIRST + DEFAULT; verify contents match `~/.claude/roles/tina/tina.md` on tina's host; edit → save; verify the file was updated on disk (compare byte-for-byte via SSH cat).
</verification>

<success_criteria>
- SRIC-06 requirement met: Role tab is FIRST + DEFAULT in IdentityModal; the tab reads + edits `~/.claude/roles/<role>/<role>.md` for the current identity via the backend two-step; frontend API stays `(identityKey, hostId)`.
- Reuses Plan 22-01's `resolveRoleForIdentity` — proves the two-step helper generalizes to a fourth caller (bounties, history, and now role-file read + write).
- Zero fallback branches added; missing role frontmatter surfaces as a clear error state, not a silent empty state.
</success_criteria>

<output>
Create `.planning/phases/22-skynet-ui-parity-with-the-role-identity-paradigm/22-06-SUMMARY.md` when done.
</output>
