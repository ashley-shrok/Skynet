---
phase: 91-relay-mediated-group-conversations-sub-slice-c-new-conversat
plan: 05
type: execute
wave: 4
depends_on:
  - 91-00
  - 91-01
  - 91-04
files_modified:
  - src/ui/features/pretty-conversations/NewConversationModal.tsx
  - src/ui/features/pretty-conversations/NewConversationModal.test.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
  - src/ui/AppShell.tsx
  - src/ui/AppShell.new-conversation.test.tsx
autonomous: true
requirements:
  - shape-91-modal-shell
  - shape-91-mobile-desktop-parity
  - shape-91-three-dot-menu-item
  - shape-91-pane-open-integration
  - T-91-FE-01
  - T-91-FE-02
tags:
  - frontend
  - modal
  - orchestration
  - materialization-integration
  - slice-b-handoff
  - slice-d-handoff
  - slice-c
must_haves:
  truths:
    - "NewConversationModal renders a radix DialogPrimitive Root+Overlay+Content shell with onInteractOutside preventDefault (X + Esc are the only close paths) — matches GlobalFilesModal.tsx canonical pattern."
    - "The modal fills the phone viewport (absolute inset-4) on mobile and centers at max-w-[560px] on desktop (md: breakpoint) — same design, adapted to width, per shape §Shape."
    - "The modal owns form state via useNewConversationForm hook and composes ParticipantChipStrip + ParticipantSearchInput + ParticipantList + a mandatory-name input + a Create button that reflects the hook's gate state (disabled + hint text on the four negative reasons)."
    - "Humans data source is getUsersListBasic() (widened by Plan 00 to include mxid); users with mxid=null are filtered out at MODAL construction (they cannot participate in a relay-mediated room); humans without a persisted colorHue get one derived via hueFromSessionName(mxid) via useMemo."
    - "Agents data source is useIdentities(); each Identity has identityKey but NO mxid field — agent mxid is derived at MODAL construction as `@${identityKey.toLowerCase()}:${serverName}` where serverName is extracted from the viewing user's own mxid via MXID_REGEX (viewing-user-store's useViewingUserMxid); agents with unresolvable serverName (viewingUserMxid=null) are filtered out until the fetch resolves."
    - "On Create button click: (a) submitting=true (hook), (b) call createRelayRoom(request), (c) on ok fire onCreated(result) prop which closes the modal + threads the response into AppShell's onCreateRelayRoom handler; on error set hook.error and clear submitting so the user can retry."
    - "PrettyConversationsPanel mounts <NewConversationModal> as a portal-mounted sibling of GlobalFilesModal; the header three-dot menu at L2067-2072 gains a 'New conversation' item that opens the modal. Icon-placement is v1-throwaway per shape §Philosophy — riding the existing showPencilButton gate is fine."
    - "AppShell wires onCreateRelayRoom callback using the CANONICAL openTab signature (Host | null, TabType, restore?, options?) mirroring the existing onRelayRoomRowClick at AppShell.tsx:2139-2166 byte-for-byte, THEN triggers an explicit /sessions/list refetch → updateFleetSessions so the sidebar row appears immediately (getSessionList runs ONCE per page-load — no polling — so an explicit refresh is REQUIRED, not optional, per shape §Shape step 3)."
  artifacts:
    - path: "src/ui/features/pretty-conversations/NewConversationModal.tsx"
      provides: "Modal shell composing hook + sub-components + api client; owns submit-and-close orchestration; agent-mxid derivation via serverName extracted from viewingUserMxid."
      contains: "export function NewConversationModal"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "New menu item + portal-mounted modal + onCreateRelayRoom pass-through prop."
      contains: "New conversation"
    - path: "src/ui/AppShell.tsx"
      provides: "onCreateRelayRoom handler wired at PrettyConversationsPanel invocation site; opens a relay-room tab via canonical openTab signature; triggers explicit getSessionList + updateFleetSessions refresh."
      contains: "onCreateRelayRoom"
  key_links:
    - from: "src/ui/features/pretty-conversations/NewConversationModal.tsx"
      to: "src/ui/features/pretty-conversations/useNewConversationForm.ts"
      via: "useNewConversationForm hook"
      pattern: "useNewConversationForm"
    - from: "src/ui/features/pretty-conversations/NewConversationModal.tsx"
      to: "src/ui/api/relay-room-create-api.ts"
      via: "createRelayRoom submit call"
      pattern: "createRelayRoom"
    - from: "src/ui/AppShell.tsx"
      to: "src/ui/shell/tabUtils.tsx"
      via: "openTab({sessionKind: 'relay-room', relayRoomId, relayRoomTitle}) opens the RelayRoomPane via Slice D dispatcher"
      pattern: "sessionKind: \"relay-room\""
    - from: "src/ui/AppShell.tsx (onCreateRelayRoom body)"
      to: "src/ui/state/conversation-store.ts (updateFleetSessions)"
      via: "explicit refresh — getSessionList() → updateFleetSessions() to surface the new relay-room row without waiting for the next page reload"
      pattern: "updateFleetSessions"
---

<objective>
Compose everything shipped in Waves 0-3 into a working end-to-end user flow: the header three-dot menu gains a 'New conversation' item, the modal opens, the user picks participants + names the room, clicks Create, the room + invites land on the relay, the session materializes, an explicit fleet-refresh fires so the sidebar row surfaces immediately, and the pane opens.

Purpose: This is the user-visible slice of Slice C. Every prior plan was foundation (types, primitives, route, sub-components). This plan wires them together at the AppShell level so an end-to-end click-through works. The modal is a portal-mounted sibling of GlobalFilesModal — same close discipline, same viewport strategy, same visual language.

Output: One new modal component + tests, three file modifications (panel + AppShell), one new AppShell test file. Zero pretty-view interior touches (D-01 upheld). Zero backend touches. Zero new CSS classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-relay-mediated-group-conversations-sub-slice-c-new-conversat/91-CONTEXT.md
@.planning/phases/91-relay-mediated-group-conversations-sub-slice-c-new-conversat/91-PATTERNS.md
@.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-07-SUMMARY.md
@src/ui/features/pretty-view/GlobalFilesModal.tsx
@src/ui/features/session-launcher/NewSessionDialog.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement NewConversationModal shell + tests</name>
  <files>
    src/ui/features/pretty-conversations/NewConversationModal.tsx,
    src/ui/features/pretty-conversations/NewConversationModal.test.tsx
  </files>
  <read_first>
    src/ui/features/pretty-conversations/useNewConversationForm.ts (Plan 01),
    src/ui/features/pretty-conversations/participant-types.ts (Plan 01 — wire types),
    src/ui/api/relay-room-create-api.ts (Plan 04),
    src/ui/features/pretty-conversations/ParticipantChip.tsx + ChipStrip + SearchInput + List (Plan 04),
    src/ui/features/pretty-view/GlobalFilesModal.tsx L192-276 (canonical radix Dialog shell — Root+Portal+Overlay+Content+Close pattern; onInteractOutside preventDefault; absolute inset-4 mobile-desktop parity),
    src/ui/features/session-launcher/NewSessionDialog.tsx L20-104 (mandatory-name state + setError + handleSubmit),
    src/ui/api/user-management-api.ts (Plan 00 output — BasicUser now includes mxid: string | null),
    src/ui/state/identities-store.ts (useIdentities — agents data source; Identity has identityKey + displayName + colorHue + avatarUrl but NO mxid field),
    src/ui/state/viewing-user-store.ts (useViewingUserMxid — viewer's own mxid),
    src/ui/features/pretty-view/relay-mxid-resolve.ts (MXID_REGEX = `/^@([^:]+):(.+)$/` — grouping localpart + serverName; Plan 05 uses this regex to extract serverName from viewingUserMxid),
    src/ui/features/terminal/session-hue.ts L12 (hueFromSessionName — djb2 mod 360 for humans-without-persisted-hue),
    .planning/phases/91-.../91-PATTERNS.md § NewConversationModal.tsx (full shell sketch: header + name field + chips + search + list + create button + hint text; radix shell verbatim from GlobalFilesModal)
  </read_first>
  <behavior>
    - Test 1 (render closed): render <NewConversationModal open={false} onOpenChange={vi.fn()} onCreated={vi.fn()} /> — the dialog is NOT in the DOM (radix hides via data-state).
    - Test 2 (render open): render with open={true} — the dialog IS in the DOM; getByRole('dialog', { name: /new conversation/i }) succeeds; the room-name input exists; the search input exists; the Create button exists; the Create button is disabled (initial state: no name, no picks).
    - Test 3 (enter name): fireEvent.change on room-name input with value 'Chat' — hint text updates to 'Pick at least one participant' (gate transitions from no-room-name to no-participants).
    - Test 4 (pick human): mock getUsersListBasic to return [{id:'u1', username:'alice', mxid:'@alice:s'}, {id:'u2', username:'bob', mxid:'@bob:s'}]; render; click the row for Alice — Alice appears in the chip strip; Create button state is single-agent-only? No — she's a human, so gate becomes ok:true (assuming name is set). Cover the human-only-count-1 case; Create should be enabled.
    - Test 5 (pick single agent — single_agent_disallowed at UI): mock useIdentities to return one identity with identityKey='nelly' + mock viewingUserMxid='@alice:thenasty.taild9b663.ts.net' so the derived agent mxid becomes '@nelly:thenasty.taild9b663.ts.net'; click that agent row + set name — Create button STAYS disabled with hint 'A single-agent conversation already exists in your list' (or shape-file-compatible variant per useNewConversationForm gate hint).
    - Test 6 (submit + close): with name='Chat' + one human picked, click Create — createRelayRoom (mocked to resolve { ok: true, roomId: '!r:s', sessionId: 's-1', roomTitle: 'Chat' }) is called once; onCreated is called with the response; onOpenChange(false) is called (modal closes).
    - Test 7 (double-click debounce — T-91-FE-01): render as Test 6 setup; fire click on Create TWICE in rapid succession (no await between them). Assert createRelayRoom is called EXACTLY ONCE (the second click hits the submitting-gate branch and is a no-op).
    - Test 8 (error surface): createRelayRoom mocked to reject with Error('proxy'); click Create; hook.error is set; the modal REMAINS open; submitting is cleared so the user can retry.
    - Test 9 (close-on-X): click the X close button — onOpenChange(false) fires.
    - Test 10 (close-on-Esc): fireEvent.keyDown Escape — onOpenChange(false) fires.
    - Test 11 (close-outside-does-nothing): fireEvent.pointerDown on the overlay — onOpenChange is NOT called (onInteractOutside preventDefault). Matches GlobalFilesModal discipline (patch #111f pattern).
    - Test 12 (self-exclude): mock viewing-user-store to return viewingUserMxid='@alice:s'; humans data has Alice + Bob; render + open the modal — the picker list shows ONLY Bob (Alice self-excluded by the hook).
    - Test 13 (humans-without-hue derived): mock getUsersListBasic to return a user without a persisted colorHue; the participant flowing into ParticipantList has a numeric colorHue derived via hueFromSessionName(mxid) — assert the derived hue equals the deterministic djb2 mod 360 of the mxid (call hueFromSessionName in the test to get the expected value).
    - Test 14 (mxid=null filter): mock getUsersListBasic to return one user with mxid=null (pre-Phase-88); render — that user does NOT appear in the picker. Test the filter is applied before shaping into PickedParticipant.
    - Test 15 (agent mxid derivation from viewingUserMxid serverName): mock viewingUserMxid='@alice:thenasty.taild9b663.ts.net'; mock useIdentities with one Identity {identityKey: 'Nelly'}; assert the participant flowing into ParticipantList has mxid='@nelly:thenasty.taild9b663.ts.net' (localpart lowercased, serverName copied from viewer's mxid). Reference: same convention used in bridge-config-writer.ts:214 (backend) — replicated frontend-side here since Identity carries no mxid over the wire.
    - Test 16 (agent filter when viewingUserMxid=null): mock viewingUserMxid=null; assert the participant list has ZERO agents (serverName is unavailable, so agents cannot be shaped safely). Once viewingUserMxid resolves, the list re-populates.
    - Test 17 (data source scoping — T-91-FE-02): the modal uses getUsersListBasic + useIdentities + useViewingUserMxid as the ONLY three data sources for the picker. Grep-assertable on the file: no other user-listing or identity-listing import appears.
    - Test 18 (mobile-vs-desktop CSS): the DialogContent has className containing 'absolute inset-4' (mobile fill) AND 'md:max-w-[560px]' (desktop cap). Grep-assertable.
  </behavior>
  <action>
    Create `src/ui/features/pretty-conversations/NewConversationModal.tsx` per PATTERNS.md § NewConversationModal.tsx.

    Signature: `export function NewConversationModal({ open, onOpenChange, onCreated }: { open: boolean; onOpenChange: (open: boolean) => void; onCreated: (result: CreateRelayRoomResponse) => void })`.

    ─── Data sourcing (inside the component) ───

    viewingUserMxid: `const viewingUserMxid = useViewingUserMxid()` from `@/state/viewing-user-store`.

    serverName derivation: use MXID_REGEX from `@/features/pretty-view/relay-mxid-resolve` (exported constant `/^@([^:]+):(.+)$/`) to extract group 2 from viewingUserMxid. Wrap in a useMemo:
    ```
    const serverName = useMemo(() => {
      if (!viewingUserMxid) return null;
      const m = viewingUserMxid.match(MXID_REGEX);
      return m ? m[2] : null;
    }, [viewingUserMxid]);
    ```
    When serverName is null (fetch pending, malformed viewer mxid), agents CANNOT be mxid-shaped and are omitted from the picker (Test 16).

    Humans: `const [basicUsers, setBasicUsers] = useState<BasicUser[] | null>(null); useEffect(() => { if (!open) return; getUsersListBasic().then(setBasicUsers).catch(err => { setBasicUsers([]); console.warn({ operation: 'new_conversation_modal_users_fetch_failed', err: err instanceof Error ? err.message : 'unknown' }); }); }, [open]);` — fetch when modal opens. Then via useMemo, shape into PickedParticipant[]:
    ```
    const humans = useMemo<PickedParticipant[]>(() => {
      if (!basicUsers) return [];
      return basicUsers
        .filter((u): u is BasicUser & { mxid: string } => typeof u.mxid === "string" && u.mxid.length > 0) // Plan 00 mxid nullable — filter nulls (Test 14)
        .map((u) => ({
          mxid: u.mxid,
          displayName: u.username,
          colorHue: hueFromSessionName(u.mxid), // derived hue (Test 13)
          avatarUrl: null, // humans have no persisted avatar today
          role: "human" as const,
          userId: u.id,
        }));
    }, [basicUsers]);
    ```

    Agents: `const { identities } = useIdentities()`; via useMemo shape into PickedParticipant[]:
    ```
    const agents = useMemo<PickedParticipant[]>(() => {
      if (!serverName) return []; // serverName not yet resolved (Test 16)
      return identities.map((id) => ({
        mxid: `@${id.identityKey.toLowerCase()}:${serverName}`, // canonical derivation (Test 15) — same shape as bridge-config-writer.ts:214
        displayName: id.displayName,
        colorHue: id.colorHue, // Identity carries this — no derivation needed for agents
        avatarUrl: id.avatarUrl ?? null,
        role: "agent" as const,
        identityKey: id.identityKey,
        subtitle: id.title ?? id.role ?? undefined, // agents get a subtitle line per shape §Shape (title from Identity is the role/short-label field)
      }));
    }, [identities, serverName]);
    ```

    Hook: `const form = useNewConversationForm({ humans, agents, viewingUserMxid })`.

    ─── Layout (inside DialogPrimitive.Root open={open} onOpenChange={onOpenChange} modal={false}) ───

    - Portal + Overlay: verbatim from GlobalFilesModal.tsx L192-201 (className+style block).
    - Content: verbatim from GlobalFilesModal.tsx L202-223 BUT with the width refinement — add `md:max-w-[560px] md:left-1/2 md:-translate-x-1/2 md:right-auto md:inset-y-8` on the Content className so desktop caps at 560px while mobile fills. Or an inline style with a `@media (min-width: 768px)` block (planner's discretion).
    - onInteractOutside: `(e) => e.preventDefault()` — X + Esc only close paths (Test 11).
    - Content body (top to bottom, per shape §Shape):
      1. Header — title "New conversation" (h2 with role by default) + close X button per GlobalFilesModal.tsx L253-276 verbatim.
      2. Room name field — a labeled input controlled by form.roomName / form.setRoomName. Label 'Room name'; input aria-required='true'; placeholder 'e.g. Design review'; autoFocus on modal open.
      3. Chips strip — <ParticipantChipStrip picked={form.picked} onRemove={form.remove} />.
      4. Search input — <ParticipantSearchInput value={form.searchQuery} onChange={form.setSearchQuery} />.
      5. Participant list — <ParticipantList humans={form.availableHumans} agents={form.availableAgents} pickedMxids={new Set(form.picked.map(p => p.mxid))} onToggle={form.toggle} filterActive={form.searchQuery.trim() !== ''} humansTotal={humans.length - (viewingUserMxid ? 1 : 0)} agentsTotal={agents.length} />.
      6. Create button — a shadcn Button with disabled={!form.gate.ok} + onClick={handleSubmit}. Below the button, hint text derived from form.gate (per PATTERNS.md gateHint function shape). If form.error is set, render it as red text below the hint.

    ─── handleSubmit function ───
    - if (!form.gate.ok) return.
    - form.setSubmitting(true); form.setError(null);
    - try { const result = await createRelayRoom({ roomName: form.roomName.trim(), humanMxids: form.picked.filter(p => p.role === 'human').map(p => p.mxid), agentMxids: form.picked.filter(p => p.role === 'agent').map(p => p.mxid) }); onCreated(result); onOpenChange(false); }
    - catch (err) { form.setError(err instanceof Error ? err.message : 'Failed to create conversation'); }
    - finally { form.setSubmitting(false); }

    Log at boundary points (structured, not JSON.stringify): on open, on submit start, on submit ok, on submit error. Use `console.info({ operation: 'new_conversation_modal_...' })` — never JSON.stringify events or error objects.

    NEVER dangerouslySetInnerHTML on displayName, roomName, or error strings.

    Companion test file `NewConversationModal.test.tsx` per PATTERNS.md § test-file-structure. Mocks BEFORE imports:
    ```
    vi.mock('@/state/identities-store', ...);
    vi.mock('@/api/user-management-api', ...);
    vi.mock('@/api/relay-room-create-api', ...);
    vi.mock('@/state/viewing-user-store', () => ({ useViewingUserMxid: vi.fn(() => '@alice:thenasty.taild9b663.ts.net') }));
    ```
    Fixture builder for identities + users. Cover the 18 behaviors above.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-conversations/NewConversationModal.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function NewConversationModal" src/ui/features/pretty-conversations/NewConversationModal.tsx` == 1
    - `grep -c "useNewConversationForm" src/ui/features/pretty-conversations/NewConversationModal.tsx` >= 1
    - `grep -c "createRelayRoom" src/ui/features/pretty-conversations/NewConversationModal.tsx` >= 1
    - `grep -c "getUsersListBasic" src/ui/features/pretty-conversations/NewConversationModal.tsx` >= 1 (humans data source — T-91-FE-02 scoping)
    - `grep -c "useIdentities" src/ui/features/pretty-conversations/NewConversationModal.tsx` >= 1 (agents data source — T-91-FE-02 scoping)
    - `grep -c "useViewingUserMxid" src/ui/features/pretty-conversations/NewConversationModal.tsx` >= 1 (serverName + self-exclude source)
    - `grep -c "MXID_REGEX" src/ui/features/pretty-conversations/NewConversationModal.tsx` >= 1 (serverName extraction from viewer's mxid — B2 fix)
    - `grep -c "identityKey.toLowerCase()" src/ui/features/pretty-conversations/NewConversationModal.tsx` >= 1 (agent mxid derivation — B2 fix; localpart lowercased to match relay-mxid-resolve convention)
    - `grep -c "hueFromSessionName" src/ui/features/pretty-conversations/NewConversationModal.tsx` >= 1 (human hue derivation at render time)
    - `grep -c "u.mxid" src/ui/features/pretty-conversations/NewConversationModal.tsx` >= 1 (Plan 00 BasicUser.mxid consumption)
    - `grep -c 'typeof u.mxid === "string"' src/ui/features/pretty-conversations/NewConversationModal.tsx` >= 1 (null-mxid filter — B1 fix layer)
    - `grep -c "absolute inset-4" src/ui/features/pretty-conversations/NewConversationModal.tsx` >= 1 (mobile fill)
    - `grep -c "md:max-w-\\[560px\\]" src/ui/features/pretty-conversations/NewConversationModal.tsx` >= 1 (desktop cap)
    - `grep -c "onInteractOutside" src/ui/features/pretty-conversations/NewConversationModal.tsx` >= 1 (X + Esc discipline)
    - `grep -c "dangerouslySetInnerHTML" src/ui/features/pretty-conversations/NewConversationModal.tsx` == 0
    - `grep -c "JSON.stringify(" src/ui/features/pretty-conversations/NewConversationModal.tsx` == 0
    - All 18 tests in NewConversationModal.test.tsx pass
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The modal renders end-to-end and calls the api client on submit with correctly-shaped humanMxids (from BasicUser.mxid) and agentMxids (derived from identityKey + serverName).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PrettyConversationsPanel menu item + portal-mounted modal + onCreateRelayRoom prop</name>
  <files>
    src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx,
    src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
  </files>
  <read_first>
    src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx L2067-2072 (existing three-dot menu items — order-locked per Phase 44 Pitfall 8; "New conversation" is a clean addition, not a reshuffle),
    src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx L2025-2041 (existing modal-mount block for GlobalFilesModal — pattern to mirror for NewConversationModal),
    src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx L570-590 (existing useState declarations — where to add newConversationModalOpen),
    src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx L270-320 (PrettyConversationsPanelProps — where to add onCreateRelayRoom prop),
    src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx L978 (existing showPencilButton gate on menu-button visibility — reuse),
    src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx (existing "Test 5" pattern for menu-item-opens-dialog — pattern to mirror),
    .planning/phases/91-.../91-PATTERNS.md § PrettyConversationsPanel modify
  </read_first>
  <behavior>
    - Test 1 (menu item exists): render <PrettyConversationsPanel ...props> with onCreateSession + onCreateRelayRoom={vi.fn()}; click the header three-dot menu button — menu shows a 'New conversation' item.
    - Test 2 (menu item opens modal): click the 'New conversation' item — the modal is mounted (findByRole('dialog', { name: /new conversation/i }) succeeds).
    - Test 3 (onCreateRelayRoom threads through): mock createRelayRoom + fire the modal's Create — the panel's onCreateRelayRoom prop is called with the response.
    - Test 4 (order-locked existing items unchanged): grep the modified panel file for the existing item labels ('New agent', 'New role', 'Edit global files…', 'Edit skills…') in that order — the four labels appear in that sequence. 'New conversation' is added at the TOP of the array (before 'New agent'). MUST NOT reshuffle the four existing items relative to each other.
    - Test 5 (portal-mount pattern): NewConversationModal is mounted as a sibling of GlobalFilesModal (grep-verifiable) with controlled state (newConversationModalOpen).
    - Test 6 (menu-button gate unchanged): showPencilButton gate at L978 is byte-unchanged; the menu button is visible only when onCreateSession is defined; adding onCreateRelayRoom does NOT introduce a new visibility path.
  </behavior>
  <action>
    Modify `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` in four places:

    Edit 1 — Props interface (~L270-320): Add `onCreateRelayRoom?: (result: CreateRelayRoomResponse) => void;` to `PrettyConversationsPanelProps`. Import `CreateRelayRoomResponse` from './participant-types' (Plan 01).

    Edit 2 — Local state (~L570-590): Add `const [newConversationModalOpen, setNewConversationModalOpen] = useState(false);` alongside existing modal state.

    Edit 3 — Menu item array (~L2067-2072): Add `{ label: "New conversation", onClick: () => setNewConversationModalOpen(true) }` at position 0. Add a comment `// Phase 91 — v1 throwaway placement; conversation-list area redesign will resurface this.` The existing order lock `KEEP ORDER: New agent → New role → Edit global files… → Edit skills… (Phase 44 Pitfall 8)` continues to hold for those four items relative to each other.

    Edit 4 — Modal mount (~L2025-2041 area, sibling of GlobalFilesModal): Add `<NewConversationModal open={newConversationModalOpen} onOpenChange={setNewConversationModalOpen} onCreated={(result) => { setNewConversationModalOpen(false); onCreateRelayRoom?.(result); }} />` with import at file top.

    Companion test additions in `PrettyConversationsPanel.test.tsx`: append a new describe block 'Phase 91 — New conversation menu item + modal' with the 6 behaviors above. Mock createRelayRoom via `vi.mock("@/api/relay-room-create-api", ...)` at file top.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx -t "New conversation"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "New conversation" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` >= 2 (menu-item label + comment reference)
    - `grep -c "onCreateRelayRoom" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` >= 3
    - `grep -c "NewConversationModal" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` >= 2 (import + JSX mount)
    - `grep -c "newConversationModalOpen" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` >= 3
    - Existing 4 menu items ('New agent', 'New role', 'Edit global files…', 'Edit skills…') appear in that order relative to each other in the source
    - Existing showPencilButton gate at L978 area UNCHANGED
    - Tests in the new describe block pass; ALL pre-existing tests still pass
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Clicking the header three-dot menu shows a 'New conversation' item; clicking it opens the modal. onCreateRelayRoom prop threads the create response to AppShell (Task 3).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AppShell wires onCreateRelayRoom callback via canonical openTab + explicit fleet-refresh</name>
  <files>
    src/ui/AppShell.tsx,
    src/ui/AppShell.new-conversation.test.tsx
  </files>
  <read_first>
    src/ui/AppShell.tsx L1431-1500 (canonical openTab signature: `openTab(host: Host | null, type: TabType, restore?: {instanceId, restoredSessionId}, options?: {targetTmuxSession?, label?, allowCreateTmux?, sessionKind?, relayRoomId?, relayRoomTitle?}) => string`),
    src/ui/AppShell.tsx L2139-2166 (canonical onRelayRoomRowClick handler — the byte-for-byte shape to mirror; uses openTab(null, "terminal", undefined, {sessionKind, relayRoomId, relayRoomTitle, label, allowCreateTmux:false}) + selectConversationDeferred(newTabId) + isTouchDevice/isMobile follow-ups),
    src/ui/AppShell.tsx L745 area (existing `getSessionList()` call at page-load — same function this plan re-invokes for the explicit refresh),
    src/ui/state/conversation-store.ts L1040-1053 (updateFleetSessions(sessions) setter — the sink for the refresh),
    src/ui/api/sessions-api.ts (getSessionList — returns RemoteTmuxSession[] widened by Plan 90-01 with kind + relay-room fields),
    .planning/phases/91-.../91-PATTERNS.md § AppShell.tsx modify
  </read_first>
  <behavior>
    - Test 1 (canonical openTab signature): grep confirms the onCreateRelayRoom callback body invokes openTab with the EXACT signature shape `openTab(null, "terminal", undefined, { sessionKind: "relay-room", relayRoomId, relayRoomTitle, label, allowCreateTmux: false })` — mirroring L2139-2166 byte-for-byte. No positional label arg between type and options bag.
    - Test 2 (selectConversationDeferred called): the newly-opened tabId is passed to selectConversationDeferred — same pattern as row-click handler at L2163.
    - Test 3 (touch-device navigation follow-ups): if isTouchDevice → navigateToView(); if isMobile → setSidebarOpen(false). Byte-for-byte with L2164-2165.
    - Test 4 (explicit fleet refresh — W5 fix): after openTab + selectConversationDeferred, the callback fires `void getSessionList().then(sessions => updateFleetSessions(sessions))` (or an equivalent try/catch shape) so the new relay-room row surfaces in the sidebar without waiting for a page reload. getSessionList runs ONCE per page-load — no polling — so this explicit refresh is REQUIRED, not optional. Assertion: grep confirms both `getSessionList` and `updateFleetSessions` appear inside the onCreateRelayRoom callback body.
    - Test 5 (defense-in-depth on missing roomId): if result.roomId is falsy the callback log-warns via `console.warn({ operation: 'new_conversation_modal_missing_room_id', roomTitle })` and does NOT call openTab, selectConversationDeferred, or the refresh. Matches Slice D's defensive pattern.
    - Test 6 (structured log at success): the callback emits `console.info({ operation: 'new_conversation_modal_tab_opened', roomId, roomTitle })` after openTab. Never JSON.stringify the result object.
    - Test 7 (structured log on refresh failure): if getSessionList rejects, the catch logs `console.warn({ operation: 'new_conversation_modal_refresh_failed', err: err.message })` — swallow the throw (the sidebar will surface the row on next page reload; the tab is already open so the user can send).
    - Test 8 (existing wiring unchanged): the existing onRelayRoomRowClick handler at L2139-2166 is byte-unchanged; onDetachedRowClick and onRdpRowClick handlers byte-unchanged.
  </behavior>
  <action>
    Modify `src/ui/AppShell.tsx` at the <PrettyConversationsPanel ...props> invocation site (near onRelayRoomRowClick at L2139-2166). Add ONE new prop mirroring the existing onRelayRoomRowClick shape byte-for-byte:

    ```
    onCreateRelayRoom={(result: CreateRelayRoomResponse) => {
      if (!result.roomId) {
        console.warn({
          operation: "new_conversation_modal_missing_room_id",
          roomTitle: result.roomTitle,
        });
        return;
      }
      // Mirror onRelayRoomRowClick shape (L2139-2166) byte-for-byte.
      // openTab signature: (host: Host | null, type: TabType, restore?: {...}, options?: {...}) — 4 args.
      const newTabId = openTab(null, "terminal", undefined, {
        sessionKind: "relay-room",
        relayRoomId: result.roomId,
        relayRoomTitle: result.roomTitle ?? null,
        label: result.roomTitle ?? result.roomId,
        allowCreateTmux: false,
      });
      selectConversationDeferred(newTabId);
      if (isTouchDevice) navigateToView();
      if (isMobile) setSidebarOpen(false);
      console.info({
        operation: "new_conversation_modal_tab_opened",
        roomId: result.roomId,
        roomTitle: result.roomTitle,
      });
      // W5: explicit fleet refresh — getSessionList runs ONCE per page-load
      // (no polling), so the new relay-room row needs an explicit push into
      // the store or the sidebar won't reflect it until the next reload.
      // Best-effort: log-and-swallow refresh failure; the tab is already
      // open so the user's pane works regardless.
      void getSessionList()
        .then((sessions) => {
          updateFleetSessions(sessions);
        })
        .catch((err: unknown) => {
          console.warn({
            operation: "new_conversation_modal_refresh_failed",
            roomId: result.roomId,
            err: err instanceof Error ? err.message : "unknown",
          });
        });
    }}
    ```

    Imports to add at the top of AppShell.tsx (if not already present):
    - `import type { CreateRelayRoomResponse } from "@/features/pretty-conversations/participant-types";`
    - `import { updateFleetSessions } from "@/state/conversation-store";` (grep first — may already be imported; getSessionList is already imported at L80).

    Do NOT touch:
    - The existing openTab function definition at L1431 (already Host|null-widened by Plan 90-07).
    - The existing onRelayRoomRowClick handler at L2139-2166.
    - onDetachedRowClick, onRdpRowClick, or any other callback prop.
    - The showPencilButton gate.

    Structured log discipline: `console.info` / `console.warn` with plain object of extracted fields; never `console.info(JSON.stringify(...))` and never `console.info(event)` where event is raw.

    ─── Test file ───

    Create `src/ui/AppShell.new-conversation.test.tsx` co-located per Slice D convention. Test scaffold:
    ```
    vi.mock('@/api/sessions-api', () => ({ getSessionList: vi.fn(async () => []), killTmuxSession: vi.fn() }));
    vi.mock('@/state/conversation-store', async () => {
      const actual = await vi.importActual<typeof import('@/state/conversation-store')>('@/state/conversation-store');
      return { ...actual, updateFleetSessions: vi.fn() };
    });
    // Additional mocks per the same pattern used in AppShell.persistence.test.tsx.
    ```
    Cover the 8 behaviors above by rendering a stripped-down AppShell harness with the modal open and the create callback firing. If a full AppShell mount is too heavy, extract the callback body into a testable inline function OR structural-grep-only tests (grep for the required strings + line-order assertions). Planner's discretion, but at minimum:
    - Test 1 (grep-based): the `onCreateRelayRoom` prop assignment in AppShell.tsx contains `openTab(null, "terminal", undefined, {` and NOT `openTab(null, "terminal", "...", null,` (regression guard against 5-arg drift).
    - Test 4 (grep-based): both `getSessionList()` and `updateFleetSessions(` appear inside the onCreateRelayRoom callback body — extract the callback block via a simple source-slice around the `onCreateRelayRoom={` marker.
    - Behaviors requiring runtime (2, 3, 5-7) are covered by Plan 06's flow test — this file's grep-based coverage is the structural gate.
  </action>
  <verify>
    <automated>npx vitest run src/ui/AppShell.new-conversation.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "onCreateRelayRoom" src/ui/AppShell.tsx` >= 1
    - `grep -c 'openTab(null, "terminal", undefined,' src/ui/AppShell.tsx` >= 2 (existing row-click handler at L2155 + new create-modal handler — one more site than pre-plan)
    - `grep -c "sessionKind: \"relay-room\"" src/ui/AppShell.tsx` >= 2 (canonical shape: existing + new)
    - `grep -c "relayRoomId:" src/ui/AppShell.tsx` >= 2
    - `grep -c "new_conversation_modal_tab_opened" src/ui/AppShell.tsx` == 1
    - `grep -c "new_conversation_modal_missing_room_id" src/ui/AppShell.tsx` == 1
    - `grep -c "new_conversation_modal_refresh_failed" src/ui/AppShell.tsx` == 1 (W5 refresh error branch)
    - `grep -c "updateFleetSessions" src/ui/AppShell.tsx` >= 1 (W5 refresh call site)
    - Line-order check: within the source block starting at the `onCreateRelayRoom={` marker and ending at its closing `}}`, `openTab(...)` MUST appear BEFORE `getSessionList()` (refresh runs AFTER tab-open so the pane is warm when the sidebar row lands).
    - `grep -c "JSON.stringify(" src/ui/AppShell.tsx` — must NOT increase from pre-plan count
    - Existing onRelayRoomRowClick handler in AppShell.tsx UNCHANGED — grep for its unique operation-key strings (e.g. `"relay-room row missing roomId at AppShell"`) confirms the pre-plan text is preserved
    - `npx tsc --noEmit` clean
    - AppShell.new-conversation.test.tsx tests pass
  </acceptance_criteria>
  <done>Every seam Slice C touches is now wired end-to-end: three-dot menu → modal → api call → response → AppShell → openTab (canonical 4-arg signature) → selectConversationDeferred → tabUtils dispatcher (Slice D) → RelayRoomSessionPane (Slice D) → RelayRoomPane (Slice D); AND the sidebar row surfaces immediately via the explicit fleet-refresh (no waiting for the next page reload).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Skynet backend (via createRelayRoom) | JWT cookie auth; backend enforces threats T-91-BE-01..04 (Plan 03). |
| PickedParticipant.displayName + roomName + error message rendered as React text | Text-only rendering; T-91 shared pattern #1. |
| Modal data sources | ONLY getUsersListBasic + useIdentities + useViewingUserMxid. Grep gate at Task 1 enforces no other api imports (T-91-FE-02). |
| Agent mxid derivation | Client-side computed from Identity.identityKey + serverName (extracted from viewer's own mxid via MXID_REGEX). The value is submitted to the backend, which validates it against MXID_RE + fleet registry (Plan 03 T-91-BE-04). A malformed serverName cannot spoof an out-of-fleet target because the backend fleet-registry filter drops non-fleet mxids. |
| viewingUserMxid → self-exclusion + serverName | Read from viewing-user-store; the hook filters self out; the modal uses serverName for agent-mxid shaping. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-FE-01 | Denial of Service | Rapid double-click on Create fires two room-creates | mitigate | Modal-side sync guard: `if (form.gate.reason === 'submitting') return`. The hook flips submitting=true BEFORE the createRelayRoom call, so a second click within the same tick hits the guard. Test 7 asserts createRelayRoom is called EXACTLY ONCE on double-click. Backend materialize ON CONFLICT is the second layer. |
| T-91-FE-02 | Information Disclosure | Modal exposes user-set from another Skynet instance (multi-tenant leak) | mitigate | Grep gate on the modal file: the only api imports allowed are `relay-room-create-api` + `user-management-api` + `identities-store` + `viewing-user-store`. Any other user-listing source is a bug caught by the grep gate at Task 1 acceptance criteria. |
| T-91-05-T1 | Tampering | Agent-mxid derivation from client-controlled Identity.identityKey | mitigate | Frontend-derived mxid is submitted as one entry in agentMxids[]; Plan 03 backend applies MXID_RE grammar validation + fleet-registry filter (T-91-BE-04) — any spoofed identityKey that would produce an mxid outside the fleet is dropped server-side. Defense-in-depth. |
| T-91-05-T2 | Tampering | displayName / roomName XSS via React text | mitigate | Every text render uses React text children. Grep gate `dangerouslySetInnerHTML == 0`. |
| T-91-05-I1 | Information Disclosure | Error message leaks server-side details | mitigate | form.setError uses `err.message` on Error instances or a friendly default; backend errors are code-strings — no PII. |
| T-91-05-D1 | Denial of Service | Sidebar refresh (getSessionList) fails and leaves the user without visual confirmation of the create | mitigate | Refresh is best-effort inside a try/catch (Test 7 asserts the catch fires). The tab is already open from the openTab call — the user's pane works regardless of refresh success. The next natural page-load re-fetches getSessionList and surfaces the row from the store (materializeRelayRoomSession has already persisted it server-side per Plan 03). |
| T-91-05-SC | Supply Chain | New modal introduced | accept | Zero new packages. |

`security_enforcement=true` — the T-91-FE-01 debounce is the load-bearing DoS mitigation; T-91-FE-02 is the tenant-scoping enforcement; T-91-05-T1 defers to the Plan 03 backend fleet-registry filter for defense against client-controlled mxid spoofing.
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npx vitest run src/ui/features/pretty-conversations/ src/ui/AppShell.new-conversation.test.tsx` — all new tests pass; pre-existing PrettyConversationsPanel.test.tsx (4700+ lines) regressions caught.
- Grep gates in each task's acceptance_criteria satisfied.
- `git diff --stat` limited to the six files listed in `files_modified`. No pretty-view interior touches (D-01). No backend touches.
</verification>

<success_criteria>
End-to-end flow works in vitest + jsdom: menu item click → modal open → pick + name → Create → api mock → onCreated → AppShell openTab (canonical signature) → selectConversationDeferred → explicit fleet refresh → sidebar surfaces the row → relay-room tab visible in state. Real-browser UAT is Plan 07. Zero pretty-view interior touches. Zero backend touches. Zero new CSS classes.
</success_criteria>

<output>
Create `.planning/phases/91-relay-mediated-group-conversations-sub-slice-c-new-conversat/91-05-SUMMARY.md` when done.
</output>
