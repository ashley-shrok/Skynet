---
phase: 91-relay-mediated-group-conversations-sub-slice-c-new-conversat
plan: 05
type: execute
wave: 4
depends_on:
  - 91-01
  - 91-04
files_modified:
  - src/ui/features/pretty-conversations/NewConversationModal.tsx
  - src/ui/features/pretty-conversations/NewConversationModal.test.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
  - src/ui/AppShell.tsx
autonomous: true
requirements:
  - shape-91-modal-shell
  - shape-91-mobile-desktop-parity
  - shape-91-three-dot-menu-item
  - shape-91-pane-open-integration
  - T-91-FE-01
  - T-91-FE-02
tags:
  - frontend
  - modal
  - orchestration
  - materialization-integration
  - slice-b-handoff
  - slice-d-handoff
  - slice-c
must_haves:
  truths:
    - "NewConversationModal renders a radix DialogPrimitive Root+Overlay+Content shell with onInteractOutside preventDefault (X + Esc are the only close paths) — matches GlobalFilesModal.tsx canonical pattern."
    - "The modal fills the phone viewport (absolute inset-4) on mobile and centers at max-w-[560px] on desktop (md: breakpoint) — same design, adapted to width, per shape §Shape."
    - "The modal owns form state via useNewConversationForm hook and composes ParticipantChipStrip + ParticipantSearchInput + ParticipantList + a mandatory-name input + a Create button that reflects the hook's gate state (disabled + hint text on the four negative reasons)."
    - "Humans data source is getUsersListBasic() enriched with mxid; agents data source is useIdentities() filtered to identities with mxid; humans without a persisted colorHue get one derived via hueFromSessionName(mxid) at MODAL construction (per PATTERNS.md § Palette Reference § Human hue derivation)."
    - "On Create button click: (a) submitting=true (hook), (b) call createRelayRoom(request), (c) on ok fire onCreated(result) prop which closes the modal + threads the response into AppShell's onCreateRelayRoom handler; on error set hook.error and clear submitting so the user can retry."
    - "PrettyConversationsPanel mounts <NewConversationModal> as a portal-mounted sibling of GlobalFilesModal; the header three-dot menu at L2067-2072 gains a 'New conversation' item that opens the modal. Icon-placement is v1-throwaway per shape §Philosophy — riding the existing showPencilButton gate is fine."
    - "AppShell wires onCreateRelayRoom callback: on modal onCreated, openTab(null, 'terminal', hostName, hostId, {sessionKind: 'relay-room', relayRoomId: response.roomId, relayRoomTitle: response.roomTitle}) — same shape as the row-click path in Slice D 90-07."
    - "Sub-slice B handoff: materializeRelayRoomSession has ALREADY run server-side (Plan 03). The next /sessions/list poll surfaces the row — no explicit sidebar refresh trigger needed at the frontend (observation loop is the safety net; create-time write is the fast path). If UX warrants shrinking the window, a small refresh trigger is planner's discretion in Task 2 — documented but not required."
  artifacts:
    - path: "src/ui/features/pretty-conversations/NewConversationModal.tsx"
      provides: "Modal shell composing hook + sub-components + api client; owns submit-and-close orchestration."
      contains: "export function NewConversationModal"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "New menu item + portal-mounted modal + onCreateRelayRoom pass-through prop."
      contains: "New conversation"
    - path: "src/ui/AppShell.tsx"
      provides: "onCreateRelayRoom handler wired at PrettyConversationsPanel invocation site; opens a relay-room tab with the create response."
      contains: "onCreateRelayRoom"
  key_links:
    - from: "src/ui/features/pretty-conversations/NewConversationModal.tsx"
      to: "src/ui/features/pretty-conversations/useNewConversationForm.ts"
      via: "useNewConversationForm hook"
      pattern: "useNewConversationForm"
    - from: "src/ui/features/pretty-conversations/NewConversationModal.tsx"
      to: "src/ui/api/relay-room-create-api.ts"
      via: "createRelayRoom submit call"
      pattern: "createRelayRoom"
    - from: "src/ui/AppShell.tsx"
      to: "src/ui/shell/tabUtils.tsx"
      via: "openTab({sessionKind: 'relay-room', relayRoomId, relayRoomTitle}) opens the RelayRoomPane via Slice D dispatcher"
      pattern: "sessionKind: \"relay-room\""
---

<objective>
Compose everything shipped in Waves 1-3 into a working end-to-end user flow: the header three-dot menu gains a 'New conversation' item, the modal opens, the user picks participants + names the room, clicks Create, the room + invites land on the relay, the session materializes, and the pane opens.

Purpose: This is the user-visible slice of Slice C. Every prior plan was foundation (types, primitives, route, sub-components). This plan wires them together at the AppShell level so an end-to-end click-through works. The modal is a portal-mounted sibling of GlobalFilesModal — same close discipline, same viewport strategy, same visual language.

Output: One new modal component + tests, two file modifications (panel + AppShell). Zero pretty-view interior touches (D-01 upheld). Zero backend touches. Zero new CSS classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-relay-mediated-group-conversations-sub-slice-c-new-conversat/91-CONTEXT.md
@.planning/phases/91-relay-mediated-group-conversations-sub-slice-c-new-conversat/91-PATTERNS.md
@.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-07-SUMMARY.md
@src/ui/features/pretty-view/GlobalFilesModal.tsx
@src/ui/features/session-launcher/NewSessionDialog.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement NewConversationModal shell + tests</name>
  <files>
    src/ui/features/pretty-conversations/NewConversationModal.tsx,
    src/ui/features/pretty-conversations/NewConversationModal.test.tsx
  </files>
  <read_first>
    src/ui/features/pretty-conversations/useNewConversationForm.ts (Plan 01),
    src/ui/features/pretty-conversations/participant-types.ts (Plan 01 — wire types),
    src/ui/api/relay-room-create-api.ts (Plan 04),
    src/ui/features/pretty-conversations/ParticipantChip.tsx + ChipStrip + SearchInput + List (Plan 04),
    src/ui/features/pretty-view/GlobalFilesModal.tsx L192-276 (canonical radix Dialog shell — Root+Portal+Overlay+Content+Close pattern; onInteractOutside preventDefault; absolute inset-4 mobile-desktop parity),
    src/ui/features/session-launcher/NewSessionDialog.tsx L20-104 (mandatory-name state + setError + handleSubmit),
    src/ui/api/user-management-api.ts (getUsersListBasic — humans data source),
    src/ui/state/identities-store.ts L206 (useIdentities — agents data source),
    src/ui/state/viewing-user-store.ts (grep for viewingUserMxid selector — Slice A + D wired the store; Task 1 consumes it read-only),
    src/ui/features/terminal/session-hue.ts L12 (hueFromSessionName — djb2 mod 360 for humans-without-persisted-hue),
    .planning/phases/91-.../91-PATTERNS.md § NewConversationModal.tsx (full shell sketch: header + name field + chips + search + list + create button + hint text; radix shell verbatim from GlobalFilesModal)
  </read_first>
  <behavior>
    - Test 1 (render closed): render <NewConversationModal open={false} onOpenChange={vi.fn()} onCreated={vi.fn()} /> — the dialog is NOT in the DOM (radix hides via data-state).
    - Test 2 (render open): render with open={true} — the dialog IS in the DOM; getByRole('dialog', { name: /new conversation/i }) succeeds; the room-name input exists; the search input exists; the Create button exists; the Create button is disabled (initial state: no name, no picks).
    - Test 3 (enter name): fireEvent.change on room-name input with value 'Chat' — hint text updates to 'Pick at least one participant' (gate transitions from no-room-name to no-participants).
    - Test 4 (pick human): mock getUsersListBasic to return [{id:'u1', username:'alice', mxid:'@alice:s'}, {id:'u2', username:'bob', mxid:'@bob:s'}]; render; click the row for Alice — Alice appears in the chip strip; Create button state is single-agent-only? No — she's a human, so gate becomes ok:true (assuming name is set). Cover the human-only-count-1 case; Create should be enabled.
    - Test 5 (pick single agent — single_agent_disallowed at UI): mock useIdentities to return one agent with mxid; click that agent row + set name — Create button STAYS disabled with hint 'A single-agent conversation already exists in your list' (or shape-file-compatible variant per useNewConversationForm gate hint).
    - Test 6 (submit + close): with name='Chat' + one human picked, click Create — createRelayRoom (mocked to resolve { ok: true, roomId: '!r:s', sessionId: 's-1', roomTitle: 'Chat' }) is called once; onCreated is called with the response; onOpenChange(false) is called (modal closes).
    - Test 7 (double-click debounce — T-91-FE-01): render as Test 6 setup; fire click on Create TWICE in rapid succession (no await between them). Assert createRelayRoom is called EXACTLY ONCE (the second click hits the submitting-gate branch and is a no-op). This is the front-end half of the T-91-FE-01 debounce mitigation. Combined with the backend idempotency (materialize ON CONFLICT DO NOTHING keyed on user_id+room_id), the flow is safe against rapid clicks. NOTE: the two Matrix room-creates would still produce two rooms — this is why the modal-side sync guard is load-bearing.
    - Test 8 (error surface): createRelayRoom mocked to reject with Error('proxy'); click Create; hook.error is set; the modal REMAINS open; submitting is cleared so the user can retry. UI shows an error hint (the plan is flexible on rendering — an inline red text near Create is sufficient).
    - Test 9 (close-on-X): click the X close button — onOpenChange(false) fires.
    - Test 10 (close-on-Esc): fireEvent.keyDown Escape — onOpenChange(false) fires.
    - Test 11 (close-outside-does-nothing): fireEvent.pointerDown on the overlay — onOpenChange is NOT called (onInteractOutside preventDefault). Matches GlobalFilesModal discipline (patch #111f pattern).
    - Test 12 (self-exclude): mock viewing-user-store to return viewingUserMxid='@alice:s'; humans data has Alice + Bob; render + open the modal — the picker list shows ONLY Bob (Alice self-excluded by the hook).
    - Test 13 (humans-without-hue derived): mock getUsersListBasic to return a user without a persisted colorHue; the participant flowing into ParticipantList has a numeric colorHue derived via hueFromSessionName(mxid) — assert the derived hue equals the deterministic djb2 mod 360 of the mxid (call hueFromSessionName in the test to get the expected value).
    - Test 14 (data source scoping — T-91-FE-02): the modal uses getUsersListBasic + useIdentities as the ONLY two data sources for the picker. Grep-assertable on the file: no other user-listing or identity-listing import appears. This is the code-level enforcement of tenant scoping.
    - Test 15 (mobile-vs-desktop CSS): the DialogContent has className containing 'absolute inset-4' (mobile fill) AND 'md:max-w-[560px]' (desktop cap). Grep-assertable.
  </behavior>
  <action>
    Create `src/ui/features/pretty-conversations/NewConversationModal.tsx` per PATTERNS.md § NewConversationModal.tsx.

    Signature: `export function NewConversationModal({ open, onOpenChange, onCreated }: { open: boolean; onOpenChange: (open: boolean) => void; onCreated: (result: CreateRelayRoomResponse) => void })`.

    Data sourcing (inside the component):
    - Humans: `const humans = useHumansForPicker()` — a small local helper (defined at the top of this file OR inline) that (a) calls getUsersListBasic() via useEffect+useState (or SWR-style pattern if that convention exists in this feature dir — grep), (b) shapes each user into PickedParticipant with role='human', displayName=user.username, mxid=user.mxid (filter out users without mxid), userId=user.id, avatarUrl=null (humans have no avatar today), colorHue=null. Human hue derivation happens next.
    - Agents: `const { identities } = useIdentities()`; map into PickedParticipant with role='agent', mxid=identity.mxid (filter out identities without mxid), displayName=identity.displayName, colorHue=identity.colorHue, avatarUrl=identity.avatarUrl ?? null, identityKey=identity.identityKey, subtitle=identity.role ?? undefined (or whatever short label the identity carries — grep the Identity type). Filter out agents whose mxid matches viewingUserMxid (defense-in-depth; the hook also self-excludes).
    - Human hue derivation at MODAL construction: for each human PickedParticipant with colorHue===null, replace it with hueFromSessionName(participant.mxid) so the picker + chip strip render a stable per-human color. Do this ONCE via useMemo keyed on the humans list — not per-render. Import hueFromSessionName from src/ui/features/terminal/session-hue.ts.

    viewingUserMxid: `const viewingUserMxid = useViewingUserMxid()` — grep for the store selector's exported name. Pass to the hook.

    Hook: `const form = useNewConversationForm({ humans, agents, viewingUserMxid })`.

    Layout (inside DialogPrimitive.Root open={open} onOpenChange={onOpenChange} modal={false}):
    - Portal + Overlay: verbatim from GlobalFilesModal.tsx L192-201 (className+style block).
    - Content: verbatim from GlobalFilesModal.tsx L202-223 BUT with the width refinement — add `md:max-w-[560px] md:left-1/2 md:-translate-x-1/2 md:right-auto md:inset-y-8` on the Content className (or equivalent centering shape) so desktop caps at 560px while mobile fills. If the tailwind chain gets too long, an inline style with a `@media (min-width: 768px)` block is acceptable (planner's discretion).
    - onInteractOutside: `(e) => e.preventDefault()` — X + Esc only close paths (Test 11).
    - Content body (top to bottom, per shape §Shape):
      1. Header — title "New conversation" (h2 with role by default) + close X button per GlobalFilesModal.tsx L253-276 verbatim.
      2. Room name field — a labeled input controlled by form.roomName / form.setRoomName. Label 'Room name'; input aria-required='true'; placeholder 'e.g. Design review'; autoFocus on modal open (a11y — first interactive element).
      3. Chips strip — <ParticipantChipStrip picked={form.picked} onRemove={form.remove} />.
      4. Search input — <ParticipantSearchInput value={form.searchQuery} onChange={form.setSearchQuery} />.
      5. Participant list — <ParticipantList humans={form.availableHumans} agents={form.availableAgents} pickedMxids={new Set(form.picked.map(p => p.mxid))} onToggle={form.toggle} filterActive={form.searchQuery.trim() !== ''} humansTotal={humans.length - (viewingUserMxid ? 1 : 0)} agentsTotal={agents.length} />.
      6. Create button — a shadcn Button with disabled={!form.gate.ok} + onClick={handleSubmit}. Below the button, hint text derived from form.gate (per PATTERNS.md gateHint function shape). If form.error is set, render it as red text below the hint.

    handleSubmit function inside the modal:
    - if (!form.gate.ok) return.
    - form.setSubmitting(true); form.setError(null);
    - try { const result = await createRelayRoom({ roomName: form.roomName.trim(), humanMxids: form.picked.filter(p => p.role === 'human').map(p => p.mxid), agentMxids: form.picked.filter(p => p.role === 'agent').map(p => p.mxid) }); onCreated(result); onOpenChange(false); }
    - catch (err) { form.setError(err instanceof Error ? err.message : 'Failed to create conversation'); }
    - finally { form.setSubmitting(false); }

    Log at boundary points (structured, not JSON.stringify): on open, on submit start, on submit ok, on submit error. Use `console.info({ operation: 'new_conversation_modal_...' })` or the ambient logger if one is imported by this feature dir (grep — the pretty-conversations dir currently uses plain console + structured fields).

    NEVER dangerouslySetInnerHTML on displayName, roomName, or error strings.

    Companion test file `NewConversationModal.test.tsx` per PATTERNS.md § test-file-structure. Mocks BEFORE imports: mock `@/state/identities-store`, `@/api/user-management-api`, `@/api/relay-room-create-api`, and `@/state/viewing-user-store` at top of file. Fixture builder for identities + users. Cover the 15 behaviors above.
  </action>
  <verify>
    <automated>npx vitest run --run src/ui/features/pretty-conversations/NewConversationModal.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function NewConversationModal" src/ui/features/pretty-conversations/NewConversationModal.tsx` == 1
    - `grep -c "useNewConversationForm" src/ui/features/pretty-conversations/NewConversationModal.tsx` >= 1
    - `grep -c "createRelayRoom" src/ui/features/pretty-conversations/NewConversationModal.tsx` >= 1
    - `grep -c "getUsersListBasic" src/ui/features/pretty-conversations/NewConversationModal.tsx` >= 1 (humans data source — T-91-FE-02 scoping)
    - `grep -c "useIdentities" src/ui/features/pretty-conversations/NewConversationModal.tsx` >= 1 (agents data source — T-91-FE-02 scoping)
    - `grep -c "hueFromSessionName" src/ui/features/pretty-conversations/NewConversationModal.tsx` >= 1 (human hue derivation at render time)
    - `grep -c "absolute inset-4" src/ui/features/pretty-conversations/NewConversationModal.tsx` >= 1 (mobile fill)
    - `grep -c "md:max-w-\\[560px\\]" src/ui/features/pretty-conversations/NewConversationModal.tsx` >= 1 (desktop cap)
    - `grep -c "onInteractOutside" src/ui/features/pretty-conversations/NewConversationModal.tsx` >= 1 (X + Esc discipline)
    - `grep -c "dangerouslySetInnerHTML" src/ui/features/pretty-conversations/NewConversationModal.tsx` == 0
    - `grep -c "JSON.stringify(" src/ui/features/pretty-conversations/NewConversationModal.tsx` == 0 (structured logs — no event stringification)
    - `grep -cE "from \"@/api/|from \"\\.\\./\\.\\./api/\"" src/ui/features/pretty-conversations/NewConversationModal.tsx | grep -v "relay-room-create-api\\|user-management-api" | wc -l` == 0 (the ONLY api imports are the two allowed sources — enforces T-91-FE-02)
    - All 15 tests in NewConversationModal.test.tsx pass
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The modal renders end-to-end and calls the api client on submit. Panel wiring (Task 2) and AppShell wiring (Task 3) will make it reachable from the UI and its result useful.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PrettyConversationsPanel menu item + portal-mounted modal + onCreateRelayRoom prop</name>
  <files>
    src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx,
    src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
  </files>
  <read_first>
    src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx L2067-2072 (existing three-dot menu items — order-locked per Phase 44 Pitfall 8; "New conversation" is a clean addition, not a reshuffle),
    src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx L2025-2041 (existing modal-mount block for GlobalFilesModal — pattern to mirror for NewConversationModal),
    src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx L570-590 (existing useState declarations — where to add newConversationModalOpen),
    src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx L270-320 (PrettyConversationsPanelProps — where to add onCreateRelayRoom prop),
    src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx L978 (existing showPencilButton gate on menu-button visibility — reuse),
    src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx (existing "Test 5" pattern for menu-item-opens-dialog — pattern to mirror),
    .planning/phases/91-.../91-PATTERNS.md § PrettyConversationsPanel modify
  </read_first>
  <behavior>
    - Test 1 (menu item exists): render <PrettyConversationsPanel ...props> with onCreateSession + onCreateRelayRoom={vi.fn()}; click the header three-dot menu button — menu shows a 'New conversation' item.
    - Test 2 (menu item opens modal): click the 'New conversation' item — the modal is mounted (findByRole('dialog', { name: /new conversation/i }) succeeds).
    - Test 3 (onCreateRelayRoom threads through): mock createRelayRoom + fire the modal's Create — the panel's onCreateRelayRoom prop is called with the response. (This may be covered by an integration test in Plan 06; if so, this test can be a light structural assertion.)
    - Test 4 (order-locked existing items unchanged): grep the modified panel file for the existing item labels ('New agent', 'New role', 'Edit global files…', 'Edit skills…') in that order — the four labels appear in that sequence. 'New conversation' is added at the TOP of the array (before 'New agent') OR at another position — planner's discretion but MUST NOT reshuffle the four existing items relative to each other.
    - Test 5 (portal-mount pattern): NewConversationModal is mounted as a sibling of GlobalFilesModal (grep-verifiable) with controlled state (newConversationModalOpen).
    - Test 6 (menu-button gate unchanged): showPencilButton gate at L978 is byte-unchanged; the menu button is visible only when onCreateSession is defined; adding onCreateRelayRoom does NOT introduce a new visibility path (v1-throwaway placement per shape).
  </behavior>
  <action>
    Modify `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` in four places:

    Edit 1 — Props interface (~L270-320): Add `onCreateRelayRoom?: (result: CreateRelayRoomResponse) => void;` to `PrettyConversationsPanelProps`. Import `CreateRelayRoomResponse` from '../pretty-conversations/participant-types' (Plan 01).

    Edit 2 — Local state (~L570-590): Add `const [newConversationModalOpen, setNewConversationModalOpen] = useState(false);` alongside the existing modal state declarations.

    Edit 3 — Menu item array (~L2067-2072): Add `{ label: "New conversation", onClick: () => setNewConversationModalOpen(true) }` to the menu-items array. Place at position 0 (top of the menu) — the shape file's "three-dot menu" placement is v1-throwaway and doesn't lock relative order. Add a comment `// Phase 91 — v1 throwaway placement; conversation-list area redesign will resurface this.` The existing order lock `KEEP ORDER: New agent → New role → Edit global files… → Edit skills… (Phase 44 Pitfall 8)` continues to hold for those four items relative to each other; 'New conversation' is a NEW item at position 0.

    Edit 4 — Modal mount (~L2025-2041 area, sibling of GlobalFilesModal): Add `<NewConversationModal open={newConversationModalOpen} onOpenChange={setNewConversationModalOpen} onCreated={(result) => { setNewConversationModalOpen(false); onCreateRelayRoom?.(result); }} />` with import at the top of the file. Comment: `/* Phase 91 — portal-mounted sibling of GlobalFilesModal; owns form state via useNewConversationForm; on successful create, calls onCreateRelayRoom which threads through AppShell to open the pane. */`

    Companion test additions in `PrettyConversationsPanel.test.tsx`: append a new describe block 'Phase 91 — New conversation menu item + modal' with the 6 behaviors above. Mock createRelayRoom via `vi.mock("@/api/relay-room-create-api", ...)` at file top. The existing 4700+ lines of tests remain byte-untouched.
  </action>
  <verify>
    <automated>npx vitest run --run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx -t "New conversation"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "New conversation" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` >= 2 (menu-item label + comment reference)
    - `grep -c "onCreateRelayRoom" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` >= 3 (prop declaration + prop reference in menu item onClick or modal onCreated callback + comment)
    - `grep -c "NewConversationModal" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` >= 2 (import + JSX mount)
    - `grep -c "newConversationModalOpen" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` >= 3 (useState declaration + open prop + setter in onClick)
    - Existing 4 menu items ('New agent', 'New role', 'Edit global files…', 'Edit skills…') appear in that order relative to each other in the source (grep + line-number ordering) — Phase 44 Pitfall 8 lock preserved
    - Existing showPencilButton gate at L978 area is UNCHANGED (git diff shows no touches to that line range)
    - Tests in the new describe block pass; ALL pre-existing tests still pass (regression baseline)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Clicking the header three-dot menu shows a 'New conversation' item; clicking it opens the modal. onCreateRelayRoom prop threads the create response to AppShell (Task 3 wires the last mile).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AppShell wires onCreateRelayRoom callback + opens the relay-room tab</name>
  <files>src/ui/AppShell.tsx</files>
  <read_first>
    src/ui/AppShell.tsx L2088-2115 (existing onRelayRoomRowClick handler from Slice D 90-07 — the shape to mirror; opens a tab with sessionKind: 'relay-room' + relayRoomId + relayRoomTitle),
    src/ui/AppShell.tsx L2139-2160 (Slice D onRelayRoomRowClick alternate location — check whichever L range still houses the shape after any subsequent edits),
    .planning/phases/90-.../90-07-SUMMARY.md § AppShell.openTab widened (openTab signature widened to accept Host | null; hostNameForId + hostName fallbacks documented),
    .planning/phases/91-.../91-PATTERNS.md § AppShell.tsx modify
  </read_first>
  <behavior>
    - Test 1 (structural — callback wired): grep confirms `onCreateRelayRoom` is passed to <PrettyConversationsPanel ...> at the invocation site with a function literal.
    - Test 2 (structural — openTab shape): the callback body calls openTab with (null OR undefined for host, then the tab-open options bag) and the options bag contains `sessionKind: "relay-room"`, `relayRoomId: result.roomId`, `relayRoomTitle: result.roomTitle`. Matches the shape established by Slice D's onRelayRoomRowClick handler.
    - Test 3 (label fallback locked): the tab label sourced from `result.roomTitle ?? result.roomId` (or an equivalent nullish-coalesce chain). Assert via grep.
    - Test 4 (structural log): the callback emits a structured console.info (or ambient log) with operation='new_conversation_modal_tab_opened' + userId (if available in this scope) + roomId + roomTitle. Never JSON.stringify the result object.
    - Test 5 (defense-in-depth on missing roomId): if result.roomId is falsy (empty string, undefined), the callback log-warns and does NOT call openTab. Matches Slice D's defensive log-and-fall-through pattern at 90-07.
    - Test 6 (existing wiring unchanged): the existing onRelayRoomRowClick handler at L2088-2160 is byte-unchanged; the existing onDetachedRowClick and onRdpRowClick handlers are byte-unchanged.
  </behavior>
  <action>
    Modify `src/ui/AppShell.tsx` at the <PrettyConversationsPanel ...props> invocation site. Add ONE new prop:

    ```
    onCreateRelayRoom={(result: CreateRelayRoomResponse) => {
      if (!result.roomId) {
        console.warn({ operation: 'new_conversation_modal_missing_room_id', roomTitle: result.roomTitle });
        return;
      }
      console.info({ operation: 'new_conversation_modal_tab_opened', roomId: result.roomId, roomTitle: result.roomTitle });
      openTab(null, "terminal", result.roomTitle ?? result.roomId, null, {
        sessionKind: "relay-room",
        relayRoomId: result.roomId,
        relayRoomTitle: result.roomTitle ?? null,
      });
    }}
    ```

    Adjust the openTab call signature to whatever AppShell's actual openTab function requires — the exact arg positions are AppShell-side (host arg, tabType, label, hostId, options bag). Grep for the existing onRelayRoomRowClick handler in AppShell.tsx to get the CANONICAL shape and mirror it byte-for-byte. Any deviation from the existing openTab call shape is a bug — the whole point is that this new create path lands the SAME way a row-click lands.

    Import CreateRelayRoomResponse at the top of AppShell.tsx from '@/features/pretty-conversations/participant-types' (Plan 01).

    Do NOT touch the existing onRelayRoomRowClick handler. Do NOT touch openTab's signature — Slice D 90-07 already widened it to accept Host | null. Do NOT touch any other callback prop.

    Structured log discipline: `console.info` / `console.warn` with a plain object of extracted fields; never `console.info(JSON.stringify(...))` and never `console.info(event)` where event is a raw DOM event or error.

    Optional (planner's discretion): add a small trigger for a `/sessions/list` refresh so the sidebar entry appears sooner than the next natural poll cycle. PATTERNS.md § AppShell.tsx modify calls this out — the observation loop is the safety net, so this is a UX enhancement, not a correctness requirement. If a suitable refresh function is grep-findable in the ambient state (e.g., `useFleetSessions().refresh()`), call it right after openTab. If not, leave it for a follow-up; the pane will still open via openTab with the roomId/roomTitle from the create response — sidebar entry follows on the next poll.

    Test additions: create `src/ui/AppShell.new-conversation.test.tsx` co-located per Slice D convention (Plan 90-07 established this — small feature-scoped test file rather than extending the huge parent test file). Cover the 6 behaviors above by rendering a stripped-down AppShell harness with the modal open and the create callback firing. Alternative: piggyback the E2E integration into Plan 06's flow test — if Plan 06 covers Tests 1-6 through a full user flow, this file can be a small structural-grep-only test.
  </action>
  <verify>
    <automated>npx vitest run --run src/ui/AppShell.new-conversation.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "onCreateRelayRoom" src/ui/AppShell.tsx` >= 1 (callback wired at PrettyConversationsPanel invocation site)
    - `grep -c "sessionKind: \"relay-room\"" src/ui/AppShell.tsx` >= 2 (existing row-click handler already fires this; new create-modal handler is the second occurrence — grep-verify the count increased by exactly 1)
    - `grep -c "relayRoomId:" src/ui/AppShell.tsx` >= 2 (same argument — one more site than pre-plan)
    - `grep -c "new_conversation_modal_tab_opened" src/ui/AppShell.tsx` == 1
    - `grep -c "new_conversation_modal_missing_room_id" src/ui/AppShell.tsx` == 1
    - `grep -c "JSON.stringify(" src/ui/AppShell.tsx` — must NOT increase from pre-plan count
    - Existing onRelayRoomRowClick handler in AppShell.tsx UNCHANGED (byte-verify via git diff on the L2088-2160 range OR grep for the existing operation-key strings unchanged)
    - `npx tsc --noEmit` clean
    - Test file passes (behaviors 1-6)
  </acceptance_criteria>
  <done>Every seam Slice C touches is now wired end-to-end: three-dot menu → modal → api call → response → AppShell → openTab → tabUtils dispatcher (Slice D) → RelayRoomSessionPane (Slice D) → RelayRoomPane (Slice D). Ashley can UAT the full flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Skynet backend (via createRelayRoom) | JWT cookie auth; backend enforces threats T-91-BE-01..04 (Plan 03). |
| PickedParticipant.displayName + roomName + error message rendered as React text | Text-only rendering; T-91 shared pattern #1. |
| Modal data sources | ONLY getUsersListBasic + useIdentities. Grep gate at Task 1 enforces no other api imports (T-91-FE-02). |
| viewingUserMxid → self-exclusion | Read from viewing-user-store; the hook filters. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-FE-01 | Denial of Service | Rapid double-click on Create fires two room-creates | mitigate | Modal-side sync guard: `if (form.gate.reason === 'submitting') return`. The hook flips submitting=true BEFORE the createRelayRoom call, so a second click within the same tick hits the guard. Test 7 asserts createRelayRoom is called EXACTLY ONCE on double-click. Backend materialize ON CONFLICT keyed on (user_id, room_id) is the second layer of defense (a different roomId per Matrix-create means two rooms would still land — the modal-side guard is load-bearing). |
| T-91-FE-02 | Information Disclosure | Modal exposes user-set from another Skynet instance (multi-tenant leak) | mitigate | Grep gate on the modal file: the only api imports allowed are `relay-room-create-api` (write path — user-scoped by backend JWT) and `user-management-api` (read path — user-scoped in single-tenant Skynet; the underlying getUsersListBasic route is already tenant-scoped for future multi-tenant). Plus `identities-store` (fleet-scoped agents). Any other user-listing source is a bug caught by the grep gate at Task 1 acceptance criteria. |
| T-91-05-T1 | Tampering | displayName / roomName XSS via React text | mitigate | Every text render uses React text children. Grep gate `dangerouslySetInnerHTML == 0` on all four Wave 4 files. |
| T-91-05-I1 | Information Disclosure | Error message leaks server-side details | mitigate | form.setError uses `err.message` on Error instances or a friendly default; the backend errors are code-strings (room_name_required, single_agent_disallowed, etc.) — no PII, no stack traces, no query fragments. |
| T-91-05-SC | Supply Chain | New modal introduced | accept | Zero new packages. Uses @radix-ui/react-dialog (already installed for GlobalFilesModal), shadcn Button, lucide-react. |

`security_enforcement=true` — the T-91-FE-01 debounce is the load-bearing DoS mitigation; T-91-FE-02 is the load-bearing tenant-scoping enforcement.
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npx vitest run --run src/ui/features/pretty-conversations/ src/ui/AppShell.new-conversation.test.tsx` — all new tests pass; pre-existing PrettyConversationsPanel.test.tsx (4700+ lines) regressions caught.
- Grep gates in each task's acceptance_criteria satisfied.
- `git diff --stat` limited to the five files listed in `files_modified`. No pretty-view interior touches (D-01). No backend touches.
- Interior lock check: `git diff --name-only HEAD~3 HEAD -- src/ui/features/pretty-view/ src/backend/` should return empty when Waves 4 + 5 finish.
</verification>

<success_criteria>
End-to-end flow works in vitest + jsdom: menu item click → modal open → pick + name → Create → api mock → onCreated → AppShell openTab → relay-room tab in state. Real-browser UAT is deferred to Plan 07 (mobile + desktop parity human-check). Zero pretty-view interior touches. Zero backend touches. Zero new CSS classes.
</success_criteria>

<output>
Create `.planning/phases/91-relay-mediated-group-conversations-sub-slice-c-new-conversat/91-05-SUMMARY.md` when done.
</output>
