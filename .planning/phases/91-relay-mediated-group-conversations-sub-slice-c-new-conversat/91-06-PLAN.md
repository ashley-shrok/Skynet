---
phase: 91-relay-mediated-group-conversations-sub-slice-c-new-conversat
plan: 06
type: execute
wave: 5
depends_on:
  - 91-01
  - 91-02
  - 91-03
  - 91-04
  - 91-05
files_modified:
  - src/ui/features/pretty-conversations/NewConversationModal.flow.test.tsx
autonomous: true
requirements:
  - shape-91-in-process-flow-coverage
  - shape-91-materialization-integration
tags:
  - integration-test
  - user-flow
  - regression-coverage
  - tdd
  - slice-c
must_haves:
  truths:
    - "One integration test file drives the complete user flow through mocked API + mocked stores: menu-item click → modal open → participant selection → name entry → Create click → mocked createRelayRoom → onCreated callback → assert the tab-open shape."
    - "The test's assertion set covers the shape file's positive path AND every §What would make it wrong branch that has a reproducible failure mode: double-click debounce, single-agent-only disable, zero-participant disable, blank-name disable, self-exclude in picker."
    - "The test runs in ~2s under vitest + jsdom with no browser required — a fast regression guard that reproduces the E2E flow without full-app bring-up."
    - "Slice B materialization integration is asserted at the mock boundary: the mocked createRelayRoom returns a sessionId that the test asserts flowed through to the openTab call (proof that the create-time fast path is wired end-to-end at the frontend seams)."
  artifacts:
    - path: "src/ui/features/pretty-conversations/NewConversationModal.flow.test.tsx"
      provides: "End-to-end user-flow test file separate from unit-test files."
      contains: "describe(\"NewConversationModal — full user flow"
  key_links:
    - from: "src/ui/features/pretty-conversations/NewConversationModal.flow.test.tsx"
      to: "src/ui/api/relay-room-create-api.ts"
      via: "vi.mock at module boundary"
      pattern: "vi.mock.*relay-room-create-api"
---

<objective>
Land a single flow-level test file that drives the complete Slice C user flow — menu click → modal → pick + name → Create → callback → tab-open. This is regression coverage separate from unit-test coverage: unit tests assert per-component behavior (Plans 01-05); this test asserts the pieces fit together.

Purpose: PATTERNS.md § User-Flow Test Pattern (line 1024) locks the shape; Plan 05 test files cover per-component behavior but do NOT drive the panel → modal → api → callback chain in one render. This plan closes that gap with a single dedicated flow test.

Output: One new test file. Zero source-code touches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-relay-mediated-group-conversations-sub-slice-c-new-conversat/91-CONTEXT.md
@.planning/phases/91-relay-mediated-group-conversations-sub-slice-c-new-conversat/91-PATTERNS.md
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
@src/ui/features/pretty-conversations/NewConversationModal.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create flow-level integration test that drives menu → modal → create → callback</name>
  <files>src/ui/features/pretty-conversations/NewConversationModal.flow.test.tsx</files>
  <read_first>
    src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (Plan 05 output — menu item wiring + modal mount),
    src/ui/features/pretty-conversations/NewConversationModal.tsx (Plan 05 output — modal shell),
    src/ui/features/pretty-conversations/PrettyConversationsPanel.relay-room.test.tsx (Slice D — recent flow-style test file in the same feature dir; use this as the scaffold),
    .planning/phases/91-.../91-PATTERNS.md § User-Flow Test Pattern (locks the mocking + assertion shape)
  </read_first>
  <behavior>
    - Test 1 (happy path — full flow): render <PrettyConversationsPanel> with props including onCreateRelayRoom={vi.fn()}. Mock getUsersListBasic to return [alice, bob]; mock useIdentities to return [{ mxid: '@nelly:s', displayName: 'Nelly', colorHue: 200, identityKey: 'nelly', avatarUrl: null }]; mock createRelayRoom to resolve { ok: true, roomId: '!r:s', sessionId: 's-1', roomTitle: 'Test chat' }. Steps: (a) click header menu button, (b) click 'New conversation', (c) fireEvent.change on room-name input to 'Test chat', (d) click Bob row, (e) click Nelly row, (f) click Create. Assert: createRelayRoom was called ONCE with { roomName: 'Test chat', humanMxids: ['@bob:s'], agentMxids: ['@nelly:s'] }; onCreateRelayRoom prop was called ONCE with the response; the modal dialog is no longer in the DOM (closed post-create).
    - Test 2 (double-click debounce guard — T-91-FE-01): same setup as Test 1; on step (f), fire click TWICE in rapid succession (no async gap). Assert createRelayRoom is called EXACTLY ONCE (the submitting guard fired on the second click).
    - Test 3 (single-agent-only disable): same setup but click ONLY Nelly (no humans). Assert Create button is disabled; assert hint text mentions 'single' or 'agent' (shape-flexible on exact wording — the hook's gateHint decides).
    - Test 4 (zero-participant disable): same setup but click no rows. Assert Create disabled; hint mentions 'participant'.
    - Test 5 (blank-name disable): same setup, pick Bob but leave name blank. Assert Create disabled; hint mentions 'name'.
    - Test 6 (self-exclude): mock viewing-user-store to return viewingUserMxid='@alice:s'; render the flow — the picker's Humans section shows ONLY Bob (Alice self-excluded). This asserts the hook's self-exclusion is wired at the modal level.
    - Test 7 (backend-error surface): mock createRelayRoom to reject with Error('room_name_required'). Same setup as Test 1 through step (f). Assert: the modal STAYS open (dialog still in DOM); the hint text or an error element shows 'room_name_required' (or the error.message); the Create button is re-enabled (submitting=false); a second click retries and (with the mock re-primed to resolve) closes the modal.
    - Test 8 (search filter narrows): pick 'B' in the search input. Assert only Bob is visible in the humans section (Alice hidden if she was viewing user's excluded, else hidden by filter). Section header shows 'Humans (1 of 2)' or shape-compatible variant.
  </behavior>
  <action>
    Create `src/ui/features/pretty-conversations/NewConversationModal.flow.test.tsx` per PATTERNS.md § User-Flow Test Pattern. Follow the scaffold of `PrettyConversationsPanel.relay-room.test.tsx` (Slice D 90-07) — this is the freshest flow-style test file in the same feature dir.

    Top-of-file mocks (declared BEFORE component imports; vitest hoists vi.mock):
    ```
    vi.mock('@/state/identities-store', () => ({
      useIdentities: () => ({ byKey: new Map(), identities: [], loaded: true, refresh: async () => {} }),
    }));
    vi.mock('@/api/user-management-api', () => ({
      getUsersListBasic: vi.fn(async () => []),
    }));
    vi.mock('@/api/relay-room-create-api', () => ({
      createRelayRoom: vi.fn(async () => ({ ok: true, roomId: '!room:server', sessionId: 's-1', roomTitle: 'Test chat' })),
    }));
    vi.mock('@/state/viewing-user-store', () => ({ /* whatever the actual store shape is; grep first */ }));
    ```

    Adjust each mock per-test using `vi.mocked(...)` handles. Use `render(<PrettyConversationsPanel {...props} onCreateRelayRoom={onCreateSpy} />)` where props include a minimal-but-sufficient row set + onCreateSession=vi.fn() so the pencil menu button is visible (its gate is `typeof onCreateSession === "function"` per PATTERNS.md § PrettyConversationsPanel modify).

    Sequence for Test 1 exactly per PATTERNS.md § End-to-end flow test (line 1046 onwards):
    1. `fireEvent.click(screen.getByTestId('pv-header-menu-button'))` — or the equivalent test-id used by the panel.
    2. `fireEvent.click(screen.getByRole('menuitem', { name: /New conversation/i }))`.
    3. `const dialog = await screen.findByRole('dialog', { name: /new conversation/i })`.
    4. `fireEvent.change(screen.getByLabelText(/room name/i), { target: { value: 'Test chat' } })`.
    5. `fireEvent.click(within(dialog).getByText('Bob'))`.
    6. `fireEvent.click(within(dialog).getByText('Nelly'))`.
    7. `fireEvent.click(within(dialog).getByRole('button', { name: /create/i }))`.
    8. `await waitFor(() => expect(onCreateSpy).toHaveBeenCalledWith({ ok: true, roomId: '!room:server', sessionId: 's-1', roomTitle: 'Test chat' }))`.
    9. `await waitFor(() => expect(screen.queryByRole('dialog', { name: /new conversation/i })).not.toBeInTheDocument())`.

    For each of the other 7 tests, adjust the mocked responses + interaction sequence + assertions. Reuse the fixture builder across tests to keep the file readable.

    NO source-code edits. NO changes to any Wave 1-5 file. This is a pure test-file addition.
  </action>
  <verify>
    <automated>npx vitest run --run src/ui/features/pretty-conversations/NewConversationModal.flow.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "describe(\"NewConversationModal" src/ui/features/pretty-conversations/NewConversationModal.flow.test.tsx` == 1
    - `grep -c "vi.mock.*relay-room-create-api" src/ui/features/pretty-conversations/NewConversationModal.flow.test.tsx` == 1
    - `grep -c "vi.mock.*user-management-api" src/ui/features/pretty-conversations/NewConversationModal.flow.test.tsx` == 1
    - `grep -c "vi.mock.*identities-store" src/ui/features/pretty-conversations/NewConversationModal.flow.test.tsx` == 1
    - `grep -c "onCreateRelayRoom" src/ui/features/pretty-conversations/NewConversationModal.flow.test.tsx` >= 2 (prop wire + assertion)
    - `grep -c "createRelayRoom" src/ui/features/pretty-conversations/NewConversationModal.flow.test.tsx` >= 2 (mock declaration + spy assertion)
    - All 8 tests pass under `npx vitest run --run src/ui/features/pretty-conversations/NewConversationModal.flow.test.tsx`
    - Test file runtime under 5s (measured via vitest reporter output)
    - Zero source-code changes: `git diff --name-only HEAD~1 HEAD -- src/ | grep -v NewConversationModal.flow.test.tsx | wc -l` == 0 for this task's commit
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The Slice C user flow has regression coverage that runs in ~2s under CI. Any future edit that breaks the menu → modal → api → callback chain trips this test file. Ready for the Wave 6 human UAT checkpoint (Plan 07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file → mocked api/store modules | vi.mock is per-file — no bleed into unit tests in adjacent files. |
| No new production trust boundary | This plan is test-code-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-06-R1 | Repudiation | Flow test that silently no-ops | mitigate | Every test uses `expect(...).toHaveBeenCalledWith(...)` on the mocked createRelayRoom + onCreateRelayRoom spies — a no-op flow (buttons clicked but no api call fired) would trip the assertion. Test 2's `toHaveBeenCalledTimes(1)` on double-click is the most sensitive assertion here. |
| T-91-06-D1 | Denial of Service | Test file leaks state across tests | mitigate | `beforeEach(() => { vi.clearAllMocks(); vi.mocked(getUsersListBasic).mockResolvedValue([...]); ... })` — same discipline used in sessions.test.ts per Phase 89. |
| T-91-06-SC | Supply Chain | New test file | accept | Zero new packages. Uses vitest + @testing-library/react (already present). |

`security_enforcement=true` — test-only plan. Threats are correctness-of-test, not runtime.
</threat_model>

<verification>
- `npx vitest run --run src/ui/features/pretty-conversations/NewConversationModal.flow.test.tsx` — 8/8 pass.
- Test file runtime < 5s.
- Zero source-code touches: `git diff --name-only HEAD~1 HEAD -- src/` shows only the new .flow.test.tsx file.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
Every future PR touching the Slice C flow (menu item, modal shell, api client, callback wiring) trips this flow test if it regresses the user-visible chain. Together with the Plan 05 unit tests, this closes the regression window before the Plan 07 human UAT.
</success_criteria>

<output>
Create `.planning/phases/91-relay-mediated-group-conversations-sub-slice-c-new-conversat/91-06-SUMMARY.md` when done.
</output>
