---
phase: 91-relay-mediated-group-conversations-sub-slice-c-new-conversat
plan: 04
type: execute
wave: 3
depends_on:
  - 91-01
  - 91-02
  - 91-03
files_modified:
  - src/ui/api/relay-room-create-api.ts
  - src/ui/api/relay-room-create-api.test.ts
  - src/ui/features/pretty-conversations/ParticipantChip.tsx
  - src/ui/features/pretty-conversations/ParticipantChipStrip.tsx
  - src/ui/features/pretty-conversations/ParticipantSearchInput.tsx
  - src/ui/features/pretty-conversations/ParticipantList.tsx
  - src/ui/features/pretty-conversations/ParticipantList.test.tsx
autonomous: true
requirements:
  - shape-91-frontend-api-client
  - shape-91-chip-strip
  - shape-91-search-with-clear
  - shape-91-sectioned-participant-list
  - T-91-FE-02
tags:
  - frontend
  - api-client
  - sub-components
  - tdd
  - slice-c
must_haves:
  truths:
    - "createRelayRoom(req) posts to /relay-room/create via authApi (JWT cookie attached) and returns the typed CreateRelayRoomResponse; errors surface via handleApiError with a friendly action label."
    - "ParticipantChip renders (avatar swatch colored by identity hue, displayName truncated, X-remove button with aria-label); ParticipantChipStrip renders the picked-list or an empty-state placeholder when picked is empty."
    - "ParticipantSearchInput reuses the .pv-search-container/.pv-search-input/.pv-search-clear CSS classes verbatim from PrettyConversationsPanel.tsx:1698-1729 with the Search icon + X-clear affordance."
    - "ParticipantList renders two sections (Humans, Agents) with row avatars + names + optional agent subtitles + a check-circle affordance on the right; tap/click toggles selection; keyboard Enter/Space also toggles; section header shows 'N of M' count when a filter is active."
    - "Human colorHue is derived at render time via a deterministic hash (djb2 mod 360) on mxid — no users.color_hue schema column added, matches PATTERNS.md § 'Human hue derivation' recommendation."
    - "Modal (Plan 05) data source is scoped to viewer's tenant via getUsersListBasic + useIdentities — both are user-scoped by the backend; the picker never surfaces users from another tenant (T-91-FE-02)."
  artifacts:
    - path: "src/ui/api/relay-room-create-api.ts"
      provides: "createRelayRoom(req: CreateRelayRoomRequest) axios wrapper."
      contains: "export async function createRelayRoom"
    - path: "src/ui/features/pretty-conversations/ParticipantChip.tsx"
      provides: "Single chip: color swatch + name + X-remove."
      contains: "export function ParticipantChip"
    - path: "src/ui/features/pretty-conversations/ParticipantChipStrip.tsx"
      provides: "Strip wrapper with empty-state placeholder."
      contains: "export function ParticipantChipStrip"
    - path: "src/ui/features/pretty-conversations/ParticipantSearchInput.tsx"
      provides: "Type-to-filter search input with clear-button affordance."
      contains: "export function ParticipantSearchInput"
    - path: "src/ui/features/pretty-conversations/ParticipantList.tsx"
      provides: "Sectioned Humans + Agents list with tap-to-toggle."
      contains: "export function ParticipantList"
  key_links:
    - from: "src/ui/features/pretty-conversations/NewConversationModal.tsx (Plan 05)"
      to: "src/ui/api/relay-room-create-api.ts"
      via: "createRelayRoom call on Create-button submit"
      pattern: "createRelayRoom"
    - from: "src/ui/features/pretty-conversations/NewConversationModal.tsx (Plan 05)"
      to: "src/ui/features/pretty-conversations/ParticipantList.tsx"
      via: "ParticipantList mounted inside modal body"
      pattern: "<ParticipantList"
    - from: "src/ui/features/pretty-conversations/ParticipantChip.tsx"
      to: "src/ui/features/pretty-conversations/participant-types.ts"
      via: "PickedParticipant prop type import"
      pattern: "PickedParticipant"
---

<objective>
Land the leaf-level frontend building blocks: (1) the api client that POSTs to /relay-room/create, and (2) the four presentational sub-components the modal composes (chip, chip strip, search input, sectioned participant list). Every artifact here has an exact analog in PATTERNS.md and reuses existing CSS classes / hue conventions. This wave is deliberately front-end-only and pure UI — no api call is made by these sub-components; the modal shell (Plan 05) wires them together with the hook (Plan 01) + api client.

Purpose: Splitting the modal shell from its sub-components keeps every Slice C plan under 3 tasks and each task under ~30% context. It also makes each sub-component testable in isolation (ParticipantList.test.tsx renders the list with fixture data + asserts click-to-toggle callback shape).

Output: Five new frontend files + two new test files. Zero backend touches. Zero modal-shell touches (that's Plan 05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-relay-mediated-group-conversations-sub-slice-c-new-conversat/91-CONTEXT.md
@.planning/phases/91-relay-mediated-group-conversations-sub-slice-c-new-conversat/91-PATTERNS.md
@src/ui/features/pretty-view/AttachmentChipStrip.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
@src/ui/api/user-management-api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Frontend api client + ParticipantSearchInput + ParticipantChip/ChipStrip</name>
  <files>
    src/ui/api/relay-room-create-api.ts,
    src/ui/api/relay-room-create-api.test.ts,
    src/ui/features/pretty-conversations/ParticipantChip.tsx,
    src/ui/features/pretty-conversations/ParticipantChipStrip.tsx,
    src/ui/features/pretty-conversations/ParticipantSearchInput.tsx
  </files>
  <read_first>
    src/ui/features/pretty-conversations/participant-types.ts (Plan 01 — PickedParticipant + CreateRelayRoomRequest/Response wire types),
    src/ui/api/user-management-api.ts L1-40 (canonical authApi.post + handleApiError wrapper convention),
    src/ui/features/pretty-view/AttachmentChipStrip.tsx L52-83 + L103-193 (strip wrapper + individual chip patterns; X-remove button; role='list' + role='listitem' a11y),
    src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx L1698-1729 (search-with-clear input pattern — .pv-search-container/.pv-search-input/.pv-search-clear CSS classes to reuse verbatim),
    src/ui/features/pretty-conversations/pretty-conversations.css (grep for .pv-search-* class defs — reuse without redefining),
    src/ui/features/pretty-view/RelayInboundBubble.tsx L82 (hue → hsl(colorHue, 80%, 60%) recipe; NEUTRAL_GREY 'hsl(210, 8%, 50%)' fallback),
    .planning/phases/91-.../91-PATTERNS.md § relay-room-create-api.ts, § ParticipantChip/ChipStrip, § ParticipantSearchInput
  </read_first>
  <behavior>
    - Test 1 (api client happy path): createRelayRoom({ roomName: 'Chat', humanMxids: ['@a:s'], agentMxids: ['@b:s'] }) with authApi.post mocked to resolve `{ data: { ok: true, roomId: '!r:s', sessionId: 's-1', roomTitle: 'Chat' } }` — returns { ok: true, roomId: '!r:s', sessionId: 's-1', roomTitle: 'Chat' }. The URL argument is '/relay-room/create'. The body argument equals the request as-passed.
    - Test 2 (api client error path): authApi.post rejects with axios error containing response.status=400 + response.data.error='room_name_required' — createRelayRoom throws (via handleApiError) with a message that includes 'create relay room' (action label) OR the backend-supplied error code — planner picks the shape, matching user-management-api.ts convention.
    - Test 3 (ParticipantChip render): render <ParticipantChip participant={{ mxid: '@a:s', displayName: 'Alice', colorHue: 200, avatarUrl: null, role: 'human', userId: 'u1' }} onRemove={vi.fn()} /> — the rendered DOM shows Alice as text, the color swatch element has inline style with `hsl(200`, and the X-remove button has aria-label='Remove Alice'.
    - Test 4 (ParticipantChip remove): fireEvent.click on the X-remove button fires onRemove('@a:s'). Verify.
    - Test 5 (ParticipantChip hue=null fallback): render a chip with colorHue=null — the swatch element has inline background 'hsl(210, 8%, 50%)' (NEUTRAL_GREY from RelayInboundBubble.tsx:82).
    - Test 6 (ParticipantChipStrip empty state): render <ParticipantChipStrip picked={[]} onRemove={vi.fn()} /> — the DOM contains placeholder text 'No participants selected yet' (or shape-file-compatible variant). role='list' is NOT emitted when empty (empty list is not a list).
    - Test 7 (ParticipantChipStrip populated): render with picked=[a, b] — DOM contains 2 chips; role='list' with aria-label='Selected participants' is present; role='listitem' fires twice.
    - Test 8 (ParticipantSearchInput controlled): render <ParticipantSearchInput value='' onChange={fn} /> — no clear button visible. Fire onChange with 'ab'. Render again with value='ab' — clear button visible. Fire onClick on clear button — onChange fires with ''.
    - Test 9 (ParticipantSearchInput a11y): role='searchbox' (via type='search' on the input), aria-label='Search participants', placeholder='Search participants'.
    - Test 10 (ParticipantSearchInput CSS class reuse): the input element carries className='pv-search-input'; the outer container carries className='pv-search-container'; the clear button carries className='pv-search-clear'. Grep-assertable in the source file. NO new CSS is defined by these three components — reuse only.
  </behavior>
  <action>
    Create `src/ui/api/relay-room-create-api.ts` following the shape locked in 91-PATTERNS.md § relay-room-create-api.ts:
    - Import authApi from "@/main-axios" (grep the file for the correct alias — user-management-api.ts uses this pattern).
    - Import handleApiError (grep for the import path in user-management-api.ts — likely "./error-utils").
    - Import CreateRelayRoomRequest + CreateRelayRoomResponse from "../features/pretty-conversations/participant-types" (Plan 01 output).
    - Export `async function createRelayRoom(req: CreateRelayRoomRequest): Promise<CreateRelayRoomResponse>` — one try/catch wrapping `await authApi.post("/relay-room/create", req)`. Return response.data cast to CreateRelayRoomResponse. On catch call handleApiError(error, "create relay room") — matches convention.
    - Companion test file `relay-room-create-api.test.ts` mocks authApi via `vi.mock("@/main-axios", ...)` and asserts the two behaviors above (happy path + error path).

    Create `src/ui/features/pretty-conversations/ParticipantChip.tsx`:
    - Import PickedParticipant from ./participant-types + Button from shadcn (grep for existing button import shape in the feature dir) + X from lucide-react + cn from "@/lib/utils" (or wherever cn lives in this codebase — grep to confirm).
    - Signature: `export function ParticipantChip({ participant, onRemove }: { participant: PickedParticipant; onRemove: (mxid: string) => void })`.
    - Body derives swatchColor: participant.colorHue !== null ? `hsl(${participant.colorHue}, 80%, 60%)` : 'hsl(210, 8%, 50%)' (matches RelayInboundBubble.tsx:82 verbatim — do NOT invent a new hue formula).
    - Render structure per PATTERNS.md § ParticipantChip (inline-flex div with data-testid='participant-chip', data-role={participant.role}, role='listitem'; a span with size-3 rounded-full swatch styled via background: swatchColor; a span with truncate for displayName; a Button with size='icon-xs' variant='ghost' onClick calling onRemove(participant.mxid) with aria-label=`Remove ${displayName}` and X icon inside).
    - NEVER use dangerouslySetInnerHTML — displayName is text-content only (shared pattern #1 from PATTERNS.md, T-17-03-01 provenance).

    Create `src/ui/features/pretty-conversations/ParticipantChipStrip.tsx`:
    - Signature: `export function ParticipantChipStrip({ picked, onRemove, className }: { picked: PickedParticipant[]; onRemove: (mxid: string) => void; className?: string })`.
    - If picked.length === 0 → return the empty-state placeholder div (italic muted text "No participants selected yet" per shape §Shape). Do NOT emit role='list' in this case.
    - Else → return a div with data-testid='participant-chip-strip', role='list', aria-label='Selected participants', className={cn('flex flex-wrap gap-2 px-1 py-1', className)}. Map over picked to render <ParticipantChip> per item with key={participant.mxid}.

    Create `src/ui/features/pretty-conversations/ParticipantSearchInput.tsx`:
    - Signature: `export function ParticipantSearchInput({ value, onChange, placeholder = "Search participants" }: { value: string; onChange: (next: string) => void; placeholder?: string })`.
    - Body — verbatim adaptation of PrettyConversationsPanel.tsx:1698-1729 with these changes:
      - data-testid changes to 'participant-search-*' (was 'pretty-conversations-search-*').
      - aria-label changes to 'Search participants'.
      - placeholder changes to props.placeholder (default 'Search participants').
      - onChange calls props.onChange with e.target.value (controlled).
      - Clear button onClick calls onChange('') — no local state.
    - REUSE .pv-search-container/.pv-search-input/.pv-search-icon/.pv-search-clear classes verbatim. Do NOT define new CSS classes. Do NOT add module-scoped styles.
  </action>
  <verify>
    <automated>npx vitest run --run src/ui/api/relay-room-create-api.test.ts src/ui/features/pretty-conversations/ParticipantChip.tsx src/ui/features/pretty-conversations/ParticipantChipStrip.tsx src/ui/features/pretty-conversations/ParticipantSearchInput.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function createRelayRoom" src/ui/api/relay-room-create-api.ts` == 1
    - `grep -c "/relay-room/create" src/ui/api/relay-room-create-api.ts` == 1 (endpoint locked; must match backend mount point from Plan 03)
    - `grep -c "authApi.post" src/ui/api/relay-room-create-api.ts` == 1
    - `grep -c "dangerouslySetInnerHTML" src/ui/features/pretty-conversations/ParticipantChip.tsx` == 0 (T-91 shared pattern #1)
    - `grep -c "dangerouslySetInnerHTML" src/ui/features/pretty-conversations/ParticipantChipStrip.tsx` == 0
    - `grep -c "hsl(210, 8%, 50%)" src/ui/features/pretty-conversations/ParticipantChip.tsx` == 1 (NEUTRAL_GREY fallback locked from RelayInboundBubble.tsx:82)
    - `grep -c "pv-search-input" src/ui/features/pretty-conversations/ParticipantSearchInput.tsx` == 1 (CSS class reuse)
    - `grep -c "pv-search-container" src/ui/features/pretty-conversations/ParticipantSearchInput.tsx` == 1
    - `grep -c "pv-search-clear" src/ui/features/pretty-conversations/ParticipantSearchInput.tsx` == 1
    - `grep -v '^\s*[/*]' src/ui/features/pretty-conversations/ParticipantSearchInput.tsx | grep -c "className=\"pv-search" >= 3` (CSS class references in code, not comments)
    - `grep -c "role=\"list\"" src/ui/features/pretty-conversations/ParticipantChipStrip.tsx` >= 1 (a11y — populated strip only)
    - `grep -c "aria-label" src/ui/features/pretty-conversations/ParticipantChip.tsx` >= 1 (Remove button aria)
    - `grep -c "aria-label" src/ui/features/pretty-conversations/ParticipantSearchInput.tsx` >= 1 (searchbox aria)
    - All 10 tests in the two test files (api-test + inline component tests) pass
    - `npx tsc --noEmit` clean on all five new source files
  </acceptance_criteria>
  <done>Wave 4 modal shell can import the api client + three sub-components. Plan 06 integration test can mock createRelayRoom via vi.mock at the module boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ParticipantList sectioned list with tap-to-toggle + tests</name>
  <files>
    src/ui/features/pretty-conversations/ParticipantList.tsx,
    src/ui/features/pretty-conversations/ParticipantList.test.tsx
  </files>
  <read_first>
    src/ui/features/pretty-conversations/participant-types.ts (Plan 01 — PickedParticipant shape),
    src/ui/features/pretty-conversations/PrettyConversationRow.tsx L1083-1089 (--pv-hue inline emission pattern) + L1195-1217 (avatar disc + img/initial fallback),
    src/ui/features/pretty-view/RelayInboundBubble.tsx L82 (hsl(colorHue, 80%, 60%) formula),
    src/ui/features/terminal/session-hue.ts L12 (hueFromSessionName — djb2 mod 360 pattern; REUSE for human colorHue derivation at render time when identity carries no colorHue),
    .planning/phases/91-.../91-PATTERNS.md § ParticipantList.tsx (full sectioned list render sketch),
    src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx (row-under-test scaffold — mount pattern + fireEvent + assertion style)
  </read_first>
  <behavior>
    - Test 1 (renders two sections): render <ParticipantList humans={[a, b]} agents={[x]} pickedMxids={new Set()} onToggle={vi.fn()} /> — DOM contains 'Humans' section header + 'Agents' section header; three rows total.
    - Test 2 (row click toggles): fireEvent.click on row a — onToggle('@a:s') is called.
    - Test 3 (keyboard toggle): fireEvent.keyDown on row a with key='Enter' — onToggle('@a:s'); same for key=' ' (space) — onToggle fires; preventDefault called (space would otherwise scroll).
    - Test 4 (selected state): render with pickedMxids=new Set(['@a:s']) — the row for a has aria-selected='true'; the check circle has filled treatment (test either by class assertion or by data-attribute).
    - Test 5 (unselected state): rows not in pickedMxids have aria-selected='false'; check circle in unfilled state.
    - Test 6 (empty humans section): humans=[], agents=[x] — the Humans section header still renders OR the section is omitted entirely — planner picks; the SHAPE FILE §Shape says "one sectioned list" implying both sections always visible even if empty. Assert whichever choice is made in this test.
    - Test 7 (N-of-M count when filter active): render with filterActive=true prop, humans=[a] (filtered from a total of 4) — section header renders 'Humans (1 of 4)'. Filter-inactive case renders just 'Humans'.
    - Test 8 (agent subtitle line): render an agent PickedParticipant with subtitle='the bounty maintainer' — DOM contains that subtitle text under the agent's displayName; human rows with subtitle=undefined do NOT render a subtitle.
    - Test 9 (avatar disc fallback): render a participant with avatarUrl=null and displayName='Alice' — the disc contains initial letter 'A' (uppercase).
    - Test 10 (avatar disc img): render a participant with avatarUrl='https://x/a.png' — the disc renders an <img src='https://x/a.png'>; alt is empty string (avatar is decorative — a11y rule).
    - Test 11 (hue applied to disc): render with colorHue=200 — the avatar disc's inline style contains 'hsl(200'.
  </behavior>
  <action>
    Create `src/ui/features/pretty-conversations/ParticipantList.tsx` per PATTERNS.md § ParticipantList.tsx. Signature: `export function ParticipantList({ humans, agents, pickedMxids, onToggle, filterActive = false, humansTotal, agentsTotal }: { humans: PickedParticipant[]; agents: PickedParticipant[]; pickedMxids: Set<string>; onToggle: (mxid: string) => void; filterActive?: boolean; humansTotal?: number; agentsTotal?: number })`.

    Body renders:
    - `<div className="flex flex-col overflow-y-auto" role="listbox" aria-label="Participant picker">` (or similar) as the scrollable wrapper.
    - Section 1: `<SectionHeader>Humans{filterActive && humansTotal !== undefined ? ` (${humans.length} of ${humansTotal})` : ''}</SectionHeader>` — inline sub-component OR just a div with the section-header CSS class (`.pv-participant-section-header` or reuse a matching class from pretty-conversations.css if one already fits — grep-check; do NOT add new CSS if a match exists).
    - Then humans.map((p) => <ParticipantRow key={p.mxid} participant={p} selected={pickedMxids.has(p.mxid)} onToggle={onToggle} />).
    - Section 2: `<SectionHeader>Agents{filterActive && agentsTotal !== undefined ? ` (${agents.length} of ${agentsTotal})` : ''}</SectionHeader>` + agents.map(...).
    - Empty-section handling: if humans.length === 0 in the filter-inactive case, render a muted "No humans available" placeholder inside the section body. Same for agents.

    ParticipantRow sub-component (defined in the same file — not a separate export):
    - Props: participant: PickedParticipant, selected: boolean, onToggle: (mxid: string) => void.
    - Container: `<div role="option" aria-selected={selected} tabIndex={0} onClick={() => onToggle(participant.mxid)} onKeyDown={(e) => { if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); onToggle(participant.mxid); } }} style={hue !== null ? { '--pv-hue': hue } : {}} className={cn('flex items-center gap-3 px-3 py-2 cursor-pointer border border-transparent rounded-lg hover:bg-white/5', selected && 'bg-[hsla(var(--pv-hue,210),50%,38%,0.20)] border-[hsla(var(--pv-hue,210),65%,55%,0.32)]')}>`.
    - Avatar disc: div sized 8 (size-8 rounded-full shrink-0 overflow-hidden) with background `hsl(${hue ?? 210}, 60%, 45%)` — if avatarUrl truthy render <img src={avatarUrl} alt="" className="size-full object-cover">, else render `<span className="flex items-center justify-center size-full text-sm font-medium text-white">{displayName.charAt(0).toUpperCase()}</span>`.
    - Body: flex-1 min-w-0 div containing displayName (truncated) + optional subtitle line for role==='agent' with subtitle prop.
    - Check circle: div on right, size-5 rounded-full border-2; when selected → 'border-emerald-400 bg-emerald-400' and contains <Check className="size-3 text-black" />; when unselected → 'border-white/20 bg-transparent' and no icon inside.

    Human colorHue derivation: PickedParticipant.colorHue is provided by the hook consumer. In the modal (Plan 05), humans that lack a persisted color_hue will get one derived at MODAL-CONSTRUCTION time via `hueFromSessionName(participant.mxid)` (imported from src/ui/features/terminal/session-hue.ts). This plan does NOT do the derivation — it just consumes whatever colorHue is on the passed participant object. Plan 05's <action> section will handle the derivation before feeding participants into ParticipantList.

    Companion test file `ParticipantList.test.tsx` mirrors PrettyConversationRow.test.tsx scaffold. Use fixture builder + render(...) + fireEvent/getByRole/etc. Cover the 11 behaviors.
  </action>
  <verify>
    <automated>npx vitest run --run src/ui/features/pretty-conversations/ParticipantList.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function ParticipantList" src/ui/features/pretty-conversations/ParticipantList.tsx` == 1
    - `grep -c "role=\"option\"" src/ui/features/pretty-conversations/ParticipantList.tsx` >= 1 (row a11y role — for the ParticipantRow)
    - `grep -c "aria-selected" src/ui/features/pretty-conversations/ParticipantList.tsx` >= 1
    - `grep -c "onKeyDown" src/ui/features/pretty-conversations/ParticipantList.tsx` >= 1 (keyboard toggle supported)
    - `grep -c "dangerouslySetInnerHTML" src/ui/features/pretty-conversations/ParticipantList.tsx` == 0 (T-91 shared pattern #1)
    - `grep -cE "(Humans|Agents)" src/ui/features/pretty-conversations/ParticipantList.tsx` >= 2 (both section headers present)
    - `grep -c "--pv-hue" src/ui/features/pretty-conversations/ParticipantList.tsx` >= 1 (hue custom property emitted inline per PrettyConversationRow.tsx pattern)
    - `grep -c "of \${" src/ui/features/pretty-conversations/ParticipantList.tsx` >= 1 (N of M count template literal; equivalent form '(${humans.length} of' is acceptable — grep pattern flexible)
    - All 11 tests in ParticipantList.test.tsx pass
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The modal shell (Plan 05) can mount `<ParticipantList humans={...} agents={...} pickedMxids={hook.pickedMxids} onToggle={hook.toggle} filterActive={hook.searchQuery !== ''} humansTotal={rawHumans.length} agentsTotal={rawAgents.length} />` and get the full picker UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Skynet backend (via createRelayRoom axios call) | JWT cookie attached via authApi; existing auth boundary. Body validation is server-side (Plan 03). |
| PickedParticipant.displayName rendered as React text | Text-only rendering; NEVER dangerouslySetInnerHTML (shared pattern #1). |
| picker data source | getUsersListBasic (user-scoped) + useIdentities (fleet-scoped in single-tenant Skynet); no cross-tenant leak surface exists in the current data model, but the modal (Plan 05) must use these two sources and no others (T-91-FE-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-04-T1 | Tampering | displayName / roomName XSS | mitigate | Every text render uses React text children (interpolation, not innerHTML). Grep gate `dangerouslySetInnerHTML == 0` on all four sub-component files. Task 1 + Task 2 acceptance criteria include this gate. |
| T-91-FE-02 | Information Disclosure | Cross-tenant picker leak | mitigate (Plan 05) | Sub-components in this plan are purely presentational and consume whatever list the parent passes. The tenant-scoping enforcement lives in Plan 05 where getUsersListBasic + useIdentities are invoked. This plan's contribution is the type surface — a wrong data source in Plan 05 would surface via the same presentational layer, so PATTERNS-locked source enumeration is the actual mitigation. Documented here for full-phase STRIDE coverage; enforced in Plan 05. |
| T-91-04-I1 | Information Disclosure | Api-error message contents | mitigate | handleApiError formats a friendly action label without leaking server-side details. Follows user-management-api.ts convention. |
| T-91-04-SC | Supply Chain | New frontend files added | accept | Zero new packages. Uses React + existing shadcn Button + lucide-react (all pre-installed). Test files use existing vitest scaffolding. |

`security_enforcement=true` — this is presentational + api-client work. Text-XSS is the primary mitigation; tenant-scoping is enforced upstream in Plan 05.
</threat_model>

<verification>
- `npx tsc --noEmit` clean on all seven new files.
- `npx vitest run --run src/ui/api/relay-room-create-api.test.ts src/ui/features/pretty-conversations/ParticipantList.test.tsx` — all tests pass (2 api tests + 11 list tests + Task 1 inline sub-component tests via a small test file for chips/chip-strip/search-input if one is created; if reused inside ParticipantList.test.tsx via composition, that also satisfies).
- Grep gates in each task's acceptance_criteria satisfied.
- `git diff --stat` limited to the seven files listed in `files_modified`. No modal-shell touches (that's Plan 05). No backend touches.
</verification>

<success_criteria>
Wave 4's NewConversationModal can compose the four sub-components + the api client into the finished modal. Zero new CSS classes introduced — every visual affordance reuses existing .pv-search-*, hue-tint, and pretty-conversations.css tokens. Existing PrettyConversationsPanel + all pretty-view surfaces byte-untouched.
</success_criteria>

<output>
Create `.planning/phases/91-relay-mediated-group-conversations-sub-slice-c-new-conversat/91-04-SUMMARY.md` when done.
</output>
