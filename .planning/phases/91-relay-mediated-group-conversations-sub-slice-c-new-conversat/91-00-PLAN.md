---
phase: 91-relay-mediated-group-conversations-sub-slice-c-new-conversat
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/backend/database/routes/user-admin-routes.ts
  - src/backend/database/routes/users-list-basic.test.ts
  - src/ui/api/user-management-api.ts
autonomous: true
requirements:
  - shape-91-users-list-basic-mxid
tags:
  - foundation
  - backend-and-frontend-type
  - users-list-basic
  - phase-88-mxid-source
  - tdd
  - slice-c
must_haves:
  truths:
    - "GET /users/list-basic response contains an mxid field on every user row — non-null for Phase 88+ users, null for pre-Phase-88 users who were never minted a relay identity."
    - "Frontend BasicUser type carries mxid: string | null; the pre-existing id + username fields remain unchanged."
    - "Backend explicit-column drizzle select adds mxid to the SELECT list; server-side self-exclusion via ne(users.id, userId) is byte-unchanged."
    - "Users without an mxid are surfaced by the API but the modal (Plan 05) filters them client-side — a user with no mxid cannot participate in a relay-mediated room by construction (Phase 88 didn't mint one for them)."
  artifacts:
    - path: "src/backend/database/routes/user-admin-routes.ts"
      provides: "Widened /users/list-basic route selecting mxid alongside id + username."
      contains: "mxid: users.mxid"
    - path: "src/ui/api/user-management-api.ts"
      provides: "BasicUser interface widened with mxid: string | null."
      contains: "mxid: string | null"
    - path: "src/backend/database/routes/users-list-basic.test.ts"
      provides: "Coverage: mxid present on rows for users with mxid, null on rows without."
      contains: "mxid"
  key_links:
    - from: "src/ui/features/pretty-conversations/NewConversationModal.tsx (Plan 05)"
      to: "src/ui/api/user-management-api.ts"
      via: "getUsersListBasic() → BasicUser[] with mxid"
      pattern: "user.mxid"
    - from: "src/ui/features/pretty-conversations/useNewConversationForm.ts (Plan 01)"
      to: "PickedParticipant.mxid derived from BasicUser.mxid at modal-construction time"
      via: "type-only chain"
      pattern: "PickedParticipant"
---

<objective>
Widen `/users/list-basic` (backend + frontend type) to include the `mxid` column. This is the smallest-diff prerequisite for Slice C's Humans picker: without an mxid on each user row, the modal cannot send `humanMxids` in the create-room request, and no human ever gets invited.

Purpose: Blocker B1 fix. Plans 01 + 05 assume `user.mxid` is available client-side; today `BasicUser` types only `{id, username}` and the backend SELECT is `{id, username}`. This plan lands the two-line widening — backend SELECT + frontend interface — plus a test covering the null case (pre-Phase-88 users).

Output: One-line backend SELECT addition, one-line frontend type addition, two-line test addition. Zero pretty-view touches. Zero modal touches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-relay-mediated-group-conversations-sub-slice-c-new-conversat/91-CONTEXT.md
@.planning/phases/91-relay-mediated-group-conversations-sub-slice-c-new-conversat/91-PATTERNS.md
@.planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-01-SUMMARY.md
@src/backend/database/routes/user-admin-routes.ts
@src/ui/api/user-management-api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen /users/list-basic backend SELECT + BasicUser frontend type</name>
  <files>
    src/backend/database/routes/user-admin-routes.ts,
    src/backend/database/routes/users-list-basic.test.ts,
    src/ui/api/user-management-api.ts
  </files>
  <read_first>
    src/backend/database/routes/user-admin-routes.ts L74-126 (existing /users/list-basic handler — SELECT columns at L113-114; ne(users.id, userId) self-exclusion at L116),
    src/backend/database/routes/users-list-basic.test.ts (existing test scaffold — Phase 38 shape),
    src/ui/api/user-management-api.ts L20-40 (existing BasicUser interface + getUsersListBasic — Phase 38 shape),
    src/backend/database/db/schema.ts (grep for `mxid` on users table — Phase 88 slice A added this column; confirm the drizzle field name is `mxid` and its type is `text().nullable()` or equivalent),
    .planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-01-SUMMARY.md (Phase 88 mxid minting contract — mxid is populated at POST /users/create for new users; pre-Phase-88 users have mxid=NULL until re-mint)
  </read_first>
  <behavior>
    - Test 1 (backend): request GET /users/list-basic as authed user in a fixture where the OTHER users in the DB have mxid populated — response body is `{ users: [{id, username, mxid: '@alice:s'}, ...] }`. The mxid field is present on every row.
    - Test 2 (backend): request as authed user in a fixture where ONE of the other users has mxid=NULL — that row's response is `{id, username, mxid: null}`. Assertion: null is preserved, not coerced to empty string; not omitted from the row shape.
    - Test 3 (backend): the JSDoc/OpenAPI docblock is updated to describe the new mxid field; the response schema section (if present) reflects `{id, username, mxid: string | null}`. Structural check — grep for the field name in the docblock.
    - Test 4 (backend self-exclusion): the requester's own row is NOT in the response (ne(users.id, userId) behavior is byte-unchanged). Regression baseline.
    - Test 5 (frontend type): the exported BasicUser interface is `{ id: string; username: string; mxid: string | null }`. Assertion: `npx tsc --noEmit` on a file that consumes BasicUser without narrowing on mxid works only if the field is optional-or-nullable; use `string | null` (nullable required — a mxid-less user is a real state we surface to callers).
  </behavior>
  <action>
    Modify `src/backend/database/routes/user-admin-routes.ts` in one place — the SELECT list at L113-114. Add `mxid: users.mxid`:

    ```
    const otherUsers = await db
      .select({ id: users.id, username: users.username, mxid: users.mxid })
      .from(users)
      .where(ne(users.id, userId));
    ```

    Update the JSDoc/OpenAPI docblock (L76-104) to describe the new mxid field. Add one line in the response body description: `Returns `{ users: [{id, username, mxid}] }`. mxid is the Phase 88 slice A relay identity (`@localpart_human:server`); null for pre-Phase-88 users who were never minted a relay identity.`

    Do NOT modify the WHERE clause (ne(users.id, userId) stays as-is). Do NOT modify the 401/500 paths.

    Modify `src/backend/database/routes/users-list-basic.test.ts` — add two new tests:
    - "returns mxid on every row" — fixture with two users having mxid populated; assert response.body.users[0].mxid + response.body.users[1].mxid are the exact strings inserted.
    - "returns null mxid for pre-Phase-88 users" — fixture with one user having mxid=null (pre-Phase-88 state); assert the row exists in the response with mxid: null.

    Modify `src/ui/api/user-management-api.ts` — widen the BasicUser interface at L27-30:

    ```
    export interface BasicUser {
      id: string;
      username: string;
      /** Phase 88 slice A relay identity (`@localpart_human:server`).
       *  Null for pre-Phase-88 users who were never minted a relay
       *  identity. Slice C's new-conversation modal (Phase 91) filters
       *  mxid-less users out of the Humans picker — they cannot
       *  participate in a relay-mediated room by construction. */
      mxid: string | null;
    }
    ```

    Update the JSDoc block above `getUsersListBasic()` to describe the shape change: mention that mxid is now part of the response and null for pre-Phase-88 users.

    Zero other changes. The existing consumers of BasicUser (identity-sharing picker per Phase 38) do not read `.mxid` — adding a field is additive-only and does not break them.
  </action>
  <verify>
    <automated>npx vitest run src/backend/database/routes/users-list-basic.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mxid: users.mxid" src/backend/database/routes/user-admin-routes.ts` == 1 (SELECT list widened)
    - `grep -c "mxid" src/backend/database/routes/user-admin-routes.ts` >= 2 (SELECT + docblock reference — count may be higher if docblock is verbose)
    - `grep -c "mxid: string | null" src/ui/api/user-management-api.ts` == 1 (BasicUser field added)
    - `grep -c "mxid" src/backend/database/routes/users-list-basic.test.ts` >= 3 (two new tests + any existing incidental mentions — must not decrease)
    - `npx vitest run src/backend/database/routes/users-list-basic.test.ts` — all pre-existing tests still pass + 2 new tests pass
    - `npx tsc --noEmit` clean
    - Existing consumers of BasicUser (grep for `getUsersListBasic` call sites) show no TS errors — additive field only.
  </acceptance_criteria>
  <done>Wave 1 (Plan 01) can shape PickedParticipant.mxid from BasicUser.mxid. Wave 4 (Plan 05) can filter out mxid-less users client-side and ship humanMxids: [BasicUser.mxid, ...] in the create-room request.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skynet backend → response body | mxid is a durable Phase 88 identifier; already exposed in other contexts (relay bubbles, session lists). Adding it to /users/list-basic is a small, deliberate widening. |
| Response DTO shape | Additive-only change; no field removed, no field renamed. Pre-Phase-91 consumers ignore the new field. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-00-I1 | Information Disclosure | mxid surfaced in /users/list-basic | accept | mxid is not a secret — it's a Phase 88 durable public identifier. Same value is emitted by relay-bubble sender fields, session lists, and identity resolution. Adding it to the picker route is consistent with the existing surface. The route is JWT-authenticated so only authenticated Skynet users see it (single-tenant scope). |
| T-91-00-T1 | Tampering | Client caches mxid values | accept | Mxid is a read-only identifier; frontend cannot tamper with a server-side mxid via this route (write path is /users/create — Phase 88). |
| T-91-00-SC | Supply Chain | New field added | accept | Zero new packages. Additive schema-column reference to an existing users.mxid column landed in Phase 88 slice A. |

`security_enforcement=true` — additive DTO field on an already-authenticated route. No new attack surface.
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npx vitest run src/backend/database/routes/users-list-basic.test.ts` — all pre-existing + 2 new tests pass.
- `git diff --stat` limited to three files listed in `files_modified`. No pretty-view or modal touches.
- Consumer regression: `grep -rn "getUsersListBasic" src/ui/ | grep -v "\.test\." | wc -l` — every call-site's tsc typecheck remains clean (additive field only).
</verification>

<success_criteria>
Plans 01 + 05 can consume `BasicUser.mxid` as `string | null` and shape it into `PickedParticipant.mxid` client-side (Plan 05 filters out null-mxid users before feeding the picker list). Zero regression on Phase 38 identity-sharing picker (which never read mxid). Backend response schema is additive.
</success_criteria>

<output>
Create `.planning/phases/91-relay-mediated-group-conversations-sub-slice-c-new-conversat/91-00-SUMMARY.md` when done.
</output>
