---
phase: 42-conversation-list-flat-recency-sort-with-pins-zone-at-top-rd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/state/conversation-store.ts
  - src/ui/state/conversation-store.test.ts
  - src/ui/features/pretty-conversations/pretty-conversations.css
  - src/ui/features/pretty-conversations/PrettyConversationRow.tsx
  - src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
autonomous: true
requirements: []
tags:
  - ui
  - conversation-list
  - sort
  - css-retirement

must_haves:
  truths:
    - "The middle of the conversation list is one flat column (no per-host divider chips between rows)."
    - "The pinned zone at the top renders in the pre-existing (host, role, label) order and does not shuffle when its rows receive activity."
    - "The RDP zone at the bottom renders in the pre-existing (host, role, label) order."
    - "The RDP section header is not rendered when zero RDP hosts have enableRdp === true (regression, already met at the store)."
    - "No conversation row renders with the CSS class name `ambient`; no row visually recedes; every row carries the same visual weight."
    - "The ready-dot renders on every non-working, non-RDP row regardless of active-set membership (regression per patch #447)."
    - "The middle sort has a stable, deterministic fallback until Plan 03 lands the real per-message recency signal (fallback: insertion order of the underlying openTabs + fleetSessions unions, established below)."
  artifacts:
    - path: "src/ui/state/conversation-store.ts"
      provides: "New ConversationList shape with `middle: ConversationRow[]` + `rdpGroup: HostGroup | null`; middle-zone comparator `compareByRecencyDesc` with no-history-to-top rule; retention of `compareByHostRoleLabel` for activeSet/pinned/RDP sort sites."
      contains: "compareByRecencyDesc"
    - path: "src/ui/features/pretty-conversations/pretty-conversations.css"
      provides: "Ambient-recession selectors (L572-621 block, plus L1075 hover-reveal) removed. `.pv-row.ambient` matches nothing."
    - path: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx"
      provides: "`isAmbient` derivation removed; `isAmbient && 'ambient'` removed from className composition. `inActiveSet` prop remains (drives deactivate machinery)."
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "Middle-zone renderer consumes `snapshot.middle` as one flat row list (no per-host divider chips). RDP zone renderer consumes `snapshot.rdpGroup`. Pinned + activeSet + Hidden branches unchanged."
  key_links:
    - from: "src/ui/state/conversation-store.ts (computeSnapshot)"
      to: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (list render)"
      via: "ConversationList shape: `{ activeSet, pinned, middle, rdpGroup }`"
      pattern: "snapshot\\.middle"
    - from: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx (cn(...) composition)"
      to: "src/ui/features/pretty-conversations/pretty-conversations.css"
      via: "class-name toggles"
      pattern: "isAmbient|\\.ambient"
---

<objective>
Retire the ambient-recession visual and reshape the conversation list into three zones — pinned (top, stable (host, role, label) order), middle (flat, currently insertion-order fallback until Plan 03 lands the real recency signal), and RDP (bottom, stable (host, role, label) order, header hides when zero rows).

Purpose: This plan ships the shape change independently of the recency-signal wire-protocol work in Plan 03. Every row visually reads the same weight (ambient dimming gone); the "where should I look next" story is carried by position + ready-dot only. The middle zone degrades cleanly to a deterministic insertion-order fallback until the fleet-status protocol extension lands.

Output: Reshaped `ConversationList` type + `computeSnapshot`; retired ambient CSS + row-class-toggle; flat middle renderer in the panel; regression tests for RDP-header-hides-on-zero + ready-dot-on-all-non-working-rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-conversation-list-flat-recency-sort-with-pins-zone-at-top-rd/42-CONTEXT.md
@.planning/phases/42-conversation-list-flat-recency-sort-with-pins-zone-at-top-rd/42-RESEARCH.md
@.planning/phases/42-conversation-list-flat-recency-sort-with-pins-zone-at-top-rd/42-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reshape ConversationList and add compareByRecencyDesc with insertion-order fallback</name>
  <files>src/ui/state/conversation-store.ts, src/ui/state/conversation-store.test.ts</files>
  <read_first>
    - src/ui/state/conversation-store.ts (L1-80 shape header + L360-670 sort + tier-assembly + RDP + hidden-filter path)
    - src/ui/state/conversation-store.test.ts (whole file — Test 2 host-tree-order lock at L162-165 must be replaced with a middle-flip assertion; 59 `grouped` references must be triaged for retention vs rewrite)
    - .planning/phases/42-conversation-list-flat-recency-sort-with-pins-zone-at-top-rd/42-RESEARCH.md (§Sort logic + §Additional gotchas §A test-file damage assessment)
    - .planning/phases/42-conversation-list-flat-recency-sort-with-pins-zone-at-top-rd/42-CONTEXT.md (§Locked by research checkpoint — three-plan split contract)
  </read_first>
  <behavior>
    - `ConversationList` type gains `middle: ConversationRow[]` (a flat, non-host-partitioned list) and replaces the general-purpose `grouped: HostGroup[]` with a narrower `rdpGroup: HostGroup | null` (only ever the `__rdp__` sentinel or `null`).
    - `activeSet: ConversationRow[]` and `pinned: ConversationRow[]` are UNCHANGED in shape and content — still sorted by `compareByHostRoleLabel`.
    - New comparator `compareByRecencyDesc(a, b)`:
        (1) Rows with `lastMessageAt == null` (no history) sort BEFORE rows with any timestamp.
        (2) Among no-history rows: fall back to a stable insertion-order key (see Test C below) so ordering is deterministic across snapshot recomputes.
        (3) Among rows with timestamps: `lastMessageAt` DESC (freshest first).
        (4) When two rows have the same `lastMessageAt`: fall back to insertion-order key for stability.
    - Since Plan 03 has not yet landed the `lastMessageAt` signal, EVERY row's `lastMessageAt` is currently `null`. Middle-zone rows therefore all fall into the no-history branch and are ordered by insertion-order key — this is the documented Plan 01 fallback.
    - Middle zone is built by taking every non-pinned, non-active-set, non-RDP identity-tmux + fleet-synthetic row (i.e. the same set the current tier-3 host-buckets contain) and unioning them into ONE flat array, then sorted by `compareByRecencyDesc`.
    - `rdpGroup` retains the exact synthesis at conversation-store.ts:570-635 (host.enableRdp === true, hostTree-first, orphan pass, sorted by `compareByHostRoleLabel`). The `if (rdpRows.length > 0)` gate at L632 is PRESERVED verbatim — when zero RDP rows exist, `rdpGroup = null`.
    - Hidden filter at L637-662 is applied to `activeSet` and `pinned` as before, and to `middle` (was `grouped`). `rdpGroup` is exempt per the existing "RDP rows never hidden" contract.
    - `collectHostOrder` (L314-328) is RETAINED — it is still consumed by the RDP synthesis hostTree-order pass at L601-616.
    - `byHostId` per-host bucketing (L515-529, whichever middle-tier build code exists) is RETIRED — middle becomes one flat array.
    - Insertion-order key for the middle zone: the source row's index within the concatenated iteration `[...conversationTabs, ...fleetSyntheticRows]`. Computed once during middle-build and stashed on a WeakMap keyed by row-object identity (never mutate the ConversationRow), OR — simpler — capture the index at push time into a parallel `middleOrderKey: number[]` and pass into the comparator via a closure. Pick whichever is simpler for the executor; both are deterministic across a single computeSnapshot invocation, which is all that matters for stability of sort within one snapshot.
    - Test A (middle flat + insertion order fallback): given openTabs = [tabA-hostH1, tabB-hostH2] and fleetSessions = [sess1-hostH1, sess2-hostH2] with all `lastMessageAt` null and no pins/active-set/RDP, `snapshot.middle` returns exactly [tabA, tabB, sess1, sess2] in that iteration order.
    - Test B (middle flip supersedes Phase 25 Test 2 host-tree lock): the middle no longer preserves depth-first host-tree order — replace the previous Test 2 assertion with an assertion that middle is a flat array with no per-host bucketing. Retain the depth-first hostTree assertion ONLY for the RDP synthesis at L601-616 (which still uses hostTree order internally).
    - Test C (no-history-to-top): given two rows R1 (lastMessageAt=1000) and R2 (lastMessageAt=null), `compareByRecencyDesc(R1, R2) > 0` (R2 sorts before R1) and `snapshot.middle[0].id === R2.id`.
    - Test D (recency DESC): given R1 (lastMessageAt=2000) and R2 (lastMessageAt=1000), `compareByRecencyDesc(R1, R2) < 0` (R1 sorts before R2).
    - Test E (rdpGroup null on zero RDP): given hostsFlat with zero hosts having `enableRdp === true`, `snapshot.rdpGroup === null`. Regression for Ashley lock #7.
    - Test F (rdpGroup contains sentinel on ≥1 RDP): given one host with `enableRdp === true`, `snapshot.rdpGroup !== null && snapshot.rdpGroup.hostId === "__rdp__" && snapshot.rdpGroup.rows.length === 1`.
    - Test G (pinned stays (host, role, label)): given two pinned rows with different lastMessageAt, they sort by `compareByHostRoleLabel` (label order), NOT by recency. Locks the "pins don't shuffle on activity" contract even after Plan 03 lands.
    - Test H (RDP stays (host, role, label)): analogous to G for the RDP zone.
    - Existing tests that assert the OLD `grouped: HostGroup[]` shape must be triaged:
        - Tests asserting activeSet/pinned/RDP tier structure — KEEP, adapt only if the field name they read changed.
        - Tests asserting per-host grouping in the middle (Test 2 at L162-165 + any siblings) — REWRITE to assert middle-flat.
        - Tests asserting RDP-group structure — KEEP, retarget from `grouped[grouped.length - 1]` to `snapshot.rdpGroup`.
        - Tests asserting hidden-filter behavior — KEEP, retarget from `grouped` to `middle` where the middle zone is the assertion target.
  </behavior>
  <action>
    Edit src/ui/state/conversation-store.ts to reshape the `ConversationList` return type from `{ activeSet, pinned, grouped: HostGroup[] }` to `{ activeSet, pinned, middle: ConversationRow[], rdpGroup: HostGroup | null }`. Add the new `compareByRecencyDesc` comparator following the behavior contract above (no-history-to-top; DESC on lastMessageAt; insertion-order fallback for stability). Rewrite `computeSnapshot` middle-zone build: replace the per-host bucketing loop (the current `byHostId` map and hostTree walk that lands in `grouped` for non-RDP hosts) with a single flat push into `middleRows: ConversationRow[]`, tracking each row's push-index in a parallel array for the stable-sort fallback. Sort `middleRows` with `compareByRecencyDesc`. Preserve `compareByHostRoleLabel` and its call sites at activeSet (L473), pinned (L511), and RDP synthesis (L631). Emit `rdpGroup: rdpRows.length > 0 ? { hostId: "__rdp__", hostName: "", rows: rdpRows } : null` (this replaces the old `if (rdpRows.length > 0) grouped.push(...)` at L632-633). Update the hidden-filter pass at L637-662 to filter `middleRows` in addition to activeSet/pinned; RDP rows remain exempt. Update the header comment at conversation-store.ts:1-90 to document the new three-zone shape (pinned/middle/rdpGroup) and note that Plan 03 will populate `lastMessageAt` on rows via the fleet-status protocol extension.

    Rewrite src/ui/state/conversation-store.test.ts as follows: (a) find every `snapshot.grouped` reference and retarget to either `snapshot.middle` (for middle-tier assertions) or `snapshot.rdpGroup` (for RDP assertions); (b) rewrite Test 2 (L162-165) to assert `middle` is flat (no host bucketing) and follows insertion order given null lastMessageAt; (c) add Tests A, C, D, E, F, G, H as described in <behavior>. Do NOT delete Tests that still make sense (host-tree-order for RDP synthesis, hidden-filter behavior applied to middle). Update `.planning/phases/42-conversation-list-flat-recency-sort-with-pins-zone-at-top-rd/` reference in any test file header comments that cite Phase 25 as the sort authority.
  </action>
  <verify>
    <automated>npx vitest run src/ui/state/conversation-store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "compareByRecencyDesc" src/ui/state/conversation-store.ts` returns a value >= 1
    - `grep -c "compareByHostRoleLabel" src/ui/state/conversation-store.ts` returns a value >= 4 (activeSet L473, pinned L511, RDP synthesis L631, plus its definition — the two middle-zone call sites at L539 + L566 have been retired)
    - `grep -E "snapshot\\.grouped\\b|state\\.grouped\\b" src/ui/state/conversation-store.ts | grep -v '^\s*//' | wc -l` returns 0 (the field `grouped` on ConversationList is gone; only `middle` and `rdpGroup` remain)
    - `grep -c "middle:" src/ui/state/conversation-store.ts` returns >= 1 (new field in the ConversationList type)
    - `grep -c "rdpGroup" src/ui/state/conversation-store.ts` returns >= 1 (new field in the ConversationList type)
    - `npx vitest run src/ui/state/conversation-store.test.ts` exits 0 with 0 failing tests
    - Test asserting no-history-to-top exists in the test file: `grep -c "no.history\\|noHistory\\|lastMessageAt.*null" src/ui/state/conversation-store.test.ts` returns >= 1
    - Test asserting `rdpGroup === null` when zero RDP hosts exists: `grep -c "rdpGroup.*null\\|rdpGroup === null" src/ui/state/conversation-store.test.ts` returns >= 1
    - `npx tsc --noEmit` exits 0 (no type errors introduced)
  </acceptance_criteria>
  <done>
    ConversationList carries `{ activeSet, pinned, middle, rdpGroup }` with all four fields populated correctly by `computeSnapshot`. The middle zone is flat, ordered by `compareByRecencyDesc` with insertion-order fallback (all rows currently null lastMessageAt). Pinned + activeSet + RDP zones sort by `compareByHostRoleLabel` unchanged. `rdpGroup` is `null` iff zero RDP hosts. All existing tests pass; new tests A-H pass; middle-flip regression tests replace the Phase 25 host-tree-order lock.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retire ambient-recession CSS + row className toggle; update panel middle renderer to consume snapshot.middle + snapshot.rdpGroup</name>
  <files>src/ui/features/pretty-conversations/pretty-conversations.css, src/ui/features/pretty-conversations/PrettyConversationRow.tsx, src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx, src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx, src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-conversations/pretty-conversations.css (L1-40 header comment, L560-635 ambient block, L974-994 the `.active-set` hover-reveal rules for deactivate, L1075 the `.pv-row.pv-row--desktop.ambient:not(:hover):not(:focus-within) .pv-hide-action` selector)
    - src/ui/features/pretty-conversations/PrettyConversationRow.tsx (L1-50 header, L200-270 isAmbient derivation, L840-880 cn(...) className composition)
    - src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx (whole file — 3 tests around the `ambient` class need update/delete per RESEARCH §A)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (L920-1200 render tree — replace the middle-zone render branch that iterates `displayedGrouped` and emits per-host divider chips with a flat-middle renderer; adapt RDP branch to consume `snapshot.rdpGroup`)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx (whole file — Tests 19A/19B assert per-host divider chips render, per RESEARCH §A; retire those assertions and add flat-middle assertions + rdp-header-hides-on-zero regression)
    - .planning/phases/42-conversation-list-flat-recency-sort-with-pins-zone-at-top-rd/42-RESEARCH.md (§2 Ambient-recession visual — exact selectors + line numbers; §Additional gotchas §A + §C `activeSet` retention rationale)
    - .planning/phases/42-conversation-list-flat-recency-sort-with-pins-zone-at-top-rd/42-CONTEXT.md (§Ready-dot uniformity + retirement of ambient recession — Ashley lock)
  </read_first>
  <behavior>
    - `.pv-row.ambient`, `.pv-row.ambient .pv-avatar`, `.pv-row.ambient:hover`, `.pv-row.ambient .pv-body .pv-label`, `.pv-row.ambient .pv-body .pv-host` (all in pretty-conversations.css L572-621) are DELETED from the CSS file.
    - `.pv-row.pv-row--desktop.ambient:not(:hover):not(:focus-within) .pv-hide-action` at L1075 is DELETED (if that hide-action hover-reveal behavior is desired to stay for desktop rows generally, it can be re-added as `.pv-row.pv-row--desktop:not(:hover):not(:focus-within) .pv-hide-action` — decide based on whether removing it changes desktop-hover behavior on any row that has a hide-action).
    - Header comments in pretty-conversations.css (L1-40) that describe the ambient scope simplification are updated to note the retirement.
    - The `.active-set` CSS class + its selectors at L978, L982, L994 (deactivate hover-reveal) are UNCHANGED. `.active-set` is load-bearing for deactivate-action visibility per RESEARCH §Pitfall 3. Only the AMBIENT selectors go.
    - PrettyConversationRow.tsx: the `isAmbient` derivation at L256-259 (`const isAmbient = !isRdp && !inActiveSet;`) is DELETED. The `isAmbient && "ambient"` entry at L877 (inside the `cn(...)` className composition) is DELETED. The `inActiveSet && "active-set"` entry at L873 is PRESERVED (drives the deactivate-action selectors surviving in the CSS). The `inActiveSet` prop is PRESERVED (drives the deactivate machinery, swipe wiring at L387-397).
    - Row header comments at PrettyConversationRow.tsx:4-6, L18-19, L38-39, L46-47, L212-213, L843-845 that describe ambient recession are updated to note retirement + that the row now carries no `.ambient` class.
    - PrettyConversationRow.test.tsx: any test asserting a row DOES receive the `.ambient` class (Test 18 / 18b per RESEARCH §A) is DELETED (the behavior is gone). Any test asserting a row does NOT receive `.ambient` (Test 12) is either kept (trivially true) or simplified to assert simply that no row ever has the `ambient` class regardless of active-set membership.
    - Regression test (new, in PrettyConversationRow.test.tsx): given `isWorking === false`, `!isRecycling`, `!hasQueuePending`, the row renders `<span data-pv-conv-ready-dot="true">` REGARDLESS of `inActiveSet` value. Locks Ashley lock #8 (ready-dot on all non-working rows) survives the ambient CSS retirement.
    - PrettyConversationsPanel.tsx: replace the `displayedGrouped.map((group) => { ... })` block at L1038-1139 with:
        (a) A middle-zone renderer that iterates `displayedMiddle` (the panel-local derivation from `snapshot.middle` after applying the anyFilterOn bounty-count filter — parallel to the existing `displayedPinned`/`displayedGrouped` pattern at L525-532). Renders each row via `PrettyConversationRowLive`, no divider chips, no per-host bucketing.
        (b) An RDP-zone renderer that consumes `snapshot.rdpGroup` (or the panel-local `displayedRdpGroup` if the bounty-count filter is applied there — inherit the existing filter policy: RDP is currently NOT filtered by bounty-count, so `displayedRdpGroup = snapshot.rdpGroup`). Renders the "Remote desktop" divider chip + Monitor-glyph rows IFF `displayedRdpGroup !== null`. When null, nothing renders (matches Ashley lock #7 — no header when zero RDP rows).
        (c) The Hidden section at L1140-1197 is UNCHANGED.
    - The activeSet render at L957-979 and the pinned render at L994-1034 are UNCHANGED (this plan does not retire the activeSet render tier per Ashley lock #5 — only its ambient visual dimension is retired via the row-class-toggle change above).
    - PrettyConversationsPanel.test.tsx: Tests 19A (L623) and 19B (L664) that assert per-host divider chips render for non-RDP groups are REWRITTEN to assert:
        - No `[data-testid="host-divider"]` element renders inside the middle zone (the render surface between the pinned divider and the RDP divider) regardless of how many hosts contribute rows.
        - Middle rows all appear inside a single flat container without per-host wrappers.
        - The `[data-testid="rdp-divider"]` element renders IFF the store's `rdpGroup !== null`.
    - Regression test in PrettyConversationsPanel.test.tsx (new): given a store snapshot with `rdpGroup: null`, the panel does NOT render any element with `data-testid="rdp-divider"` (Ashley lock #7 — hidden header on zero RDP).
    - Regression test in PrettyConversationsPanel.test.tsx (new): given a store snapshot where every middle-zone row has `isWorking: false` and none are RDP, EVERY rendered middle row has a `[data-pv-conv-ready-dot="true"]` element (Ashley lock #8 — ready-dot on all non-working rows survives ambient CSS retirement).
  </behavior>
  <action>
    Edit src/ui/features/pretty-conversations/pretty-conversations.css: DELETE the entire block at L572-621 (all `.pv-row.ambient*` selectors), DELETE L1075 (the ambient hide-action hover-reveal), and DELETE the L623-630 comment about ambient rows showing the ready-dot. Update the file's header comment (L17-23) to note "ambient recession visual retired in Phase 42 — every row carries the same visual weight per Ashley 2026-08-14". Leave `.active-set` selectors at L978, L982, L994 UNTOUCHED — they gate the deactivate-action hover-reveal and are non-ambient. Verify with `grep -c "\.ambient" pretty-conversations.css` returning 0.

    Edit src/ui/features/pretty-conversations/PrettyConversationRow.tsx: DELETE the `isAmbient` derivation at L256-259 and DELETE `isAmbient && "ambient",` from the `cn(...)` composition at L877. Update the header comment (L1-50, L38-39 recipe) and L212-213, L843-845 inline comments to reflect that the row no longer carries an `.ambient` class and ambient recession is retired. Preserve the `inActiveSet` prop, the `inActiveSet && "active-set"` entry at L873, and all other behavior (swipe wiring, deactivate machinery, ready-dot render at L1089).

    Edit src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx: delete tests asserting a row DOES carry `.ambient` (Test 18 / 18b area). Add a new test that asserts the rendered row NEVER carries an `.ambient` class for any combination of `inActiveSet`/`isRdp` inputs. Add a new regression test that asserts the ready-dot renders (`[data-pv-conv-ready-dot="true"]` present) when `isWorking === false && !isRecycling && !hasQueuePending`, REGARDLESS of `inActiveSet`.

    Edit src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx: replace the `displayedGrouped.map((group) => { ... })` block at L1038-1139 with (a) a flat middle-zone renderer that iterates `displayedMiddle` and emits `PrettyConversationRowLive` rows in one container (no divider chips), and (b) a conditional RDP-zone renderer that consumes `displayedRdpGroup` (derived from `snapshot.rdpGroup`) and renders the existing "Remote desktop" divider chip + rows IFF `displayedRdpGroup !== null`. Rename the local `displayedGrouped` derivation at L525-532 into two derivations: `displayedMiddle: ConversationRow[]` (apply the anyFilterOn bounty-count filter to `snapshot.middle` using the same predicate the current `displayedGrouped` derivation uses) and `displayedRdpGroup: HostGroup | null` (pass through `snapshot.rdpGroup` unchanged — RDP is not filtered by bounty-count today). Update the store subscription to read the new `middle` and `rdpGroup` fields.

    Edit src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx: retire Tests 19A + 19B per behavior. Add a middle-zone-is-flat test that asserts `container.querySelectorAll('[data-testid="host-divider"]').length === 0` after render. Add the two new regression tests (rdp-header-hides-on-zero and ready-dot-on-all-non-working). Update all `snapshot.grouped` references in the test file to the new shape (`snapshot.middle` and `snapshot.rdpGroup`) — same triage rules as Task 1's test-file rewrite.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\.pv-row\.ambient\|\.ambient" src/ui/features/pretty-conversations/pretty-conversations.css` returns 0
    - `grep -c "\.active-set" src/ui/features/pretty-conversations/pretty-conversations.css` returns >= 3 (unchanged — the L978/L982/L994 deactivate hover-reveal selectors survive)
    - `grep -c "isAmbient" src/ui/features/pretty-conversations/PrettyConversationRow.tsx` returns 0
    - `grep -c "\"ambient\"" src/ui/features/pretty-conversations/PrettyConversationRow.tsx` returns 0
    - `grep -c "\"active-set\"" src/ui/features/pretty-conversations/PrettyConversationRow.tsx` returns >= 1 (unchanged — the classname toggle survives)
    - `grep -c "inActiveSet" src/ui/features/pretty-conversations/PrettyConversationRow.tsx` returns >= 2 (prop is preserved for deactivate machinery)
    - `grep -c "displayedGrouped\|snapshot\.grouped" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns 0
    - `grep -c "displayedMiddle\|snapshot\.middle" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns >= 1
    - `grep -c "displayedRdpGroup\|snapshot\.rdpGroup" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns >= 1
    - `npx vitest run src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` exits 0 with 0 failing tests
    - Full suite `npx vitest run` exits 0 with 0 failing tests (fork rule — full-suite green precondition)
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0 (Vite bundle succeeds; ambient CSS gone from the built artifact)
    - Snap-reorder lock (Ashley lock #6 — no animation on middle-zone reorder): `grep -nE "\.pv-row[^{}]*\{[^}]*transition[^}]*(transform|top|order)" src/ui/features/pretty-conversations/pretty-conversations.css` returns no matches — no CSS transition on transform/top/order was introduced on the row selector. If any pre-existing transition rule matches, verify it was present before this plan and is unrelated to position (e.g. background-color hover), and note the exception in SUMMARY.
  </acceptance_criteria>
  <done>
    The ambient-recession visual is entirely retired: `.ambient` matches no CSS selector, the row component adds no `ambient` className, and every row visually reads the same weight. The middle zone in the panel renders one flat column of rows (no per-host divider chips). The RDP zone header hides when zero RDP rows exist. The ready-dot regression test locks patch #447 behavior post-retirement. All tests pass and the frontend builds clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → CSS class name | None — no user input drives CSS class strings in this plan. |
| user input → React rendered text | Row labels are user-controlled (tmux session names, identity displayNames) but React escapes text nodes by default at the existing render sites — no change in escaping surface here. |
| module code → global CSS | Retiring selectors is a subtractive change; no new global rules land. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01-01 | Tampering | conversation-store.ts shape rename | mitigate | TypeScript type checking catches all consumers of the removed `grouped` field; acceptance criteria include `npx tsc --noEmit` exit 0 to verify no consumer is silently broken. |
| T-42-01-02 | Denial of Service (local) | middle-zone flat sort on very large lists | accept | Ashley's fleet is ~20 sessions; sorting a 20-element array on every snapshot is trivial. If the fleet ever grows to >200, revisit with a stable-sort benchmark. |
| T-42-01-03 | Information Disclosure | row label rendering post-CSS-retirement | accept | React text-node escaping is unchanged; no `dangerouslySetInnerHTML` is introduced. Retiring visual dimming does not change what data reaches the DOM — only how it is painted. |
| T-42-01-04 | Repudiation | test rewrite loses historical assertions | mitigate | New tests A-H in Task 1 + regression tests in Task 2 (rdp-header-hides-on-zero + ready-dot-on-all-non-working) preserve the behaviors Phase 25/patch #447 established. Retired assertions are explicitly for behavior Ashley locked as gone (host-tree grouping in middle, ambient class presence). |
</threat_model>

<verification>
- `npx vitest run src/ui/state/conversation-store.test.ts src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` exits 0
- `npx vitest run` (full suite) exits 0 with 0 failing tests
- `npx tsc --noEmit` exits 0
- `npm run build` exits 0
- Grep gates from Task 1 + Task 2 acceptance criteria all pass
</verification>

<success_criteria>
- Ambient-recession CSS is entirely retired from `pretty-conversations.css`.
- Row component adds no `ambient` className under any prop combination.
- `ConversationList` shape is `{ activeSet, pinned, middle, rdpGroup }` with `middle: ConversationRow[]` flat and `rdpGroup: HostGroup | null` (null when zero RDP rows).
- Middle zone renders as one flat column in the panel with no per-host divider chips.
- RDP zone header renders only when `rdpGroup !== null`.
- Ready-dot renders on every non-working, non-RDP row regardless of active-set membership (regression locked by test).
- All tests pass; frontend builds clean.
</success_criteria>

<output>
Create `.planning/phases/42-conversation-list-flat-recency-sort-with-pins-zone-at-top-rd/42-01-SUMMARY.md` when done, documenting: (a) the new ConversationList shape, (b) the insertion-order fallback for the middle zone until Plan 03 lands `lastMessageAt`, (c) which existing tests were rewritten and why, (d) any surprises encountered (e.g. surviving consumers of the old `grouped` field that TypeScript surfaced), (e) confirmation that `.active-set` CSS + row-class-toggle survived per Ashley lock #5.
</output>
