---
phase: 17-pretty-view-relay-bubbles-skynet-integration
plan: 04
type: execute
wave: 3
depends_on:
  - 17-01
  - 17-02
  - 17-03
files_modified:
  - .planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-BUILD-VERIFY-LOG.md
  - .planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-UAT-CHECKLIST.md
  - .planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-PATCHES-MD-ENTRY.md
autonomous: false
requirements:
  - RELAYBUB-01
  - RELAYBUB-02
  - RELAYBUB-03
  - RELAYBUB-04
  - RELAYBUB-05
  - RELAYBUB-06
tags:
  - pretty-view
  - relay
  - deploy
  - uat
  - checkpoint

must_haves:
  truths:
    - "npm run build exits 0; vite output contains bundled RelayOutboundBubble + RelayInboundBubble; no source .ts/.tsx changes are made in this plan"
    - "17-UAT-CHECKLIST.md exists and walks each of RELAYBUB-01, 02, 03, 04, 05, 06 as a testable step Ashley can run against a real Skynet instance"
    - "17-PATCHES-MD-ENTRY.md exists with a draft skynet-patches.md entry for the next patch number, ready to paste when Ashley greenlights the deploy"
    - "17-BUILD-VERIFY-LOG.md captures the tsc + vitest + npm build outputs proving the phase is code-side ready, PLUS runs 17-02's deferred end-to-end curl smoke against the deployed instance (if not already run in 17-02) to prove the /relay-pointer endpoint is truly wired backend-side (returns 400 whitelist_reject or 401 auth-required, NOT 200 with index.html)"
    - "All endpoint references in this plan use `/relay-pointer` (main-backend mount per 17-02), NOT the stale `/claude-session/relay-pointer` path from the earlier revision — a drift-gate on the awk block-matcher is used (same technique as 17-02) so stale paths cannot silently pass"
    - "Deploy itself is NOT executed inside this plan — the 15-min deadman rollback timer runs behind Ashley's separate `docker compose up -d --force-recreate skynet` per fork DEPLOY DISCIPLINE"
  artifacts:
    - path: ".planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-BUILD-VERIFY-LOG.md"
      provides: "tsc + vitest + npm build output snapshot proving code-side readiness + deferred /relay-pointer smoke output (if 17-02 deferred it)"
    - path: ".planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-UAT-CHECKLIST.md"
      provides: "6-item + polish walkthrough Ashley runs post-deploy"
    - path: ".planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-PATCHES-MD-ENTRY.md"
      provides: "draft skynet-patches.md entry for the next patch number"
  key_links: []
---

<objective>
Deploy checkpoint. Verify the code side is release-ready (build clean,
tests green, both nginx configs mirror-consistent), author the Ashley UAT
checklist covering RELAYBUB-01..06 as testable steps, draft the
skynet-patches.md entry ready to paste when Ashley greenlights the deploy.

Purpose: Per fork DEPLOY DISCIPLINE (CLAUDE.md), every
`docker compose up -d --force-recreate skynet` runs behind the 15-min
deadman rollback timer, no exceptions. This plan produces the paperwork
Ashley needs to run that deploy herself; the plan does NOT push code, does
NOT touch source files.

Output: Three markdown artifacts in the phase directory + one blocking
human-verify checkpoint gating on Ashley's post-deploy UAT sign-off.

**Endpoint path lock:** all references to the file-pointer proxy in this
plan use `/relay-pointer` (mounted on the main backend at port 30001 per
plan 17-02) — NOT the stale `/claude-session/relay-pointer` path from
earlier revisions. The drift-gate uses 17-02's awk block-matcher (not a
fragile sed range on `/claude-session/...` — that would silently pass by
extracting empty output on both sides and diffing them to zero).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-UI-SPEC.md
@.planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-01-SUMMARY.md
@.planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-02-SUMMARY.md
@.planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification + UAT checklist + patches-md draft</name>
  <files>.planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-BUILD-VERIFY-LOG.md, .planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-UAT-CHECKLIST.md, .planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-PATCHES-MD-ENTRY.md</files>
  <read_first>
    - .planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-01-SUMMARY.md (backend parser + wire types state)
    - .planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-02-SUMMARY.md (nginx-endpoint state + awk-block-matcher diff-gate proof; ALSO check whether 17-02 deferred the end-to-end curl smoke — if so, the exact curl commands are documented there and MUST be run in this plan post-deploy)
    - .planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-03-SUMMARY.md (frontend bubble components state + emitted-CSS SPACED-rgba confirmation)
    - .planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-UI-SPEC.md (Ashley's visual byte-shape lock for the UAT visual-parity checks)
    - Any prior deploy-checkpoint plan in this repo for format reference (e.g. `.planning/phases/05-pretty-view-file-upload-support/05-04-PLAN.md` or `.planning/phases/06-telegram-like-interface/06-05-PLAN.md`) — grep first: `grep -l 'BUILD-VERIFY-LOG\|UAT-CHECKLIST\|PATCHES-MD-ENTRY' .planning/phases/*/`, pick the most recent and mirror its structure
    - skynet-patches.md at repo root (grep for it, read the last 3 entries to learn the header shape + entry conventions Ashley uses)
  </read_first>
  <action>
    Step 1 — run build verification (no source diffs; commands only):
      1. `cd /home/ubuntu/skynet && npx tsc --noEmit 2>&1 | tee /tmp/17-tsc.log` — expect exit 0.
      2. `npx vitest run 2>&1 | tee /tmp/17-vitest.log` — expect full test suite pass; capture the summary line.
      3. `npm run build 2>&1 | tee /tmp/17-build.log` — expect exit 0; capture the final "built in X.XXs" line.
      4. `grep -l 'RelayOutboundBubble\|RelayInboundBubble' dist/assets/*.js` — expect at least one chunk name (proof both components bundled).
      5. Emitted CSS SPACED-rgba verification (backstop for plan 17-03's canonical whitespace lock): `grep -Fq 'rgba(64, 96, 160, 0.28)' dist/assets/*.css && grep -Fq 'rgba(200, 128, 64, 0.28)' dist/assets/*.css && echo BUILD-CSS-SPACED-OK`.
      6. **Nginx block drift gate (awk block-matcher — same technique as 17-02 Task 2; MUST use `/relay-pointer` NOT the stale `/claude-session/relay-pointer` path)**:
         ```
         awk '/^[[:space:]]*location ~ \^\/relay-pointer/{flag=1} flag{print; if(/^[[:space:]]*}/){flag=0}}' docker/nginx.conf > /tmp/17-04-relay-http.txt
         awk '/^[[:space:]]*location ~ \^\/relay-pointer/{flag=1} flag{print; if(/^[[:space:]]*}/){flag=0}}' docker/nginx-https.conf > /tmp/17-04-relay-https.txt
         [ -s /tmp/17-04-relay-http.txt ] || { echo "FATAL: /relay-pointer block missing from nginx.conf"; exit 1; }
         [ -s /tmp/17-04-relay-https.txt ] || { echo "FATAL: /relay-pointer block missing from nginx-https.conf"; exit 1; }
         diff /tmp/17-04-relay-http.txt /tmp/17-04-relay-https.txt || { echo "FATAL: nginx block drift"; exit 1; }
         echo "NGINX-DRIFT-GATE-OK"
         ```
         The `-s` non-empty checks BEFORE the diff prevent the silent-pass-on-empty-extraction failure mode (checker iter-2 finding #1: if the extraction pattern doesn't match ANY line in either file, both files are empty and diff returns 0 — but the block is missing, not identical).
      7. **Deferred end-to-end curl smoke from 17-02** (if 17-02's SUMMARY documented it as deferred): read the exact curl commands from 17-02-SUMMARY.md and run them against the deployed instance (this step runs AFTER Ashley's deploy in Task 2 step 2, but the smoke COMMANDS are prepared here and included in the BUILD-VERIFY-LOG's "post-deploy smoke commands" section for Ashley to execute or for Claude to run once the deploy is live). Expected outcomes:
         - `curl -sS -o /tmp/relay-smoke-body.txt -w '%{http_code}\n' 'https://term.gigaashley.click/relay-pointer?hostId=999999&path=/etc/passwd'` returns HTTP code in `{400, 401}` (400 = whitelist_reject if authentication middleware runs after route matching; 401 = missing-token if authenticateJWT runs first — either code is acceptable, both prove the request reached the backend router and was rejected). MUST NOT return `200` (200 = SPA fallback caught the request = CLAUDE.md silent-index.html failure mode).
         - `head -c 40 /tmp/relay-smoke-body.txt` MUST NOT print `<!DOCTYPE html>` or `<!doctype html>` (either would prove SPA fallback masking).
         - Unauthenticated variant: `curl -sS -o /dev/null -w '%{http_code}\n' 'https://term.gigaashley.click/relay-pointer'` (no cookie) returns `{400, 401}`, NOT `200`.
    Step 2 — Author 17-BUILD-VERIFY-LOG.md. Include: date, current git commit hash (`git rev-parse HEAD`), tsc log tail (10 lines), vitest summary line + counts, npm build final line, dist-chunk grep output, emitted-CSS SPACED-rgba confirmation (BUILD-CSS-SPACED-OK), nginx-drift-gate output (NGINX-DRIFT-GATE-OK — with both extracted block bodies pasted inline as proof they matched non-empty), the exact end-to-end curl smoke commands prepared for post-deploy execution, and a one-line "READY FOR ASHLEY GREENLIGHT" verdict.
    Step 3 — Author 17-UAT-CHECKLIST.md. Walk Ashley through each RELAYBUB-01..06 in order:
      * RELAYBUB-01 outbound: "On a tina-identity pretty-view tab, wait for or trigger a real Matrix relay send (Ashley's curl-PUT to a room). Confirm: bubble appears right-aligned, cool-blue glass color, header `▸ relay send → !room-alias:server`, body shows the sent message text."
      * RELAYBUB-02 inbound: "On the same tab, wait for or trigger an inbound relay (Ashley sends 'banana banana banana' from her Element client to the room). Confirm: bubble appears left-aligned, warm-orange glass color, header shows `● ashley · !room-alias:server`, body shows `banana banana banana`."
      * RELAYBUB-03 sender-hue: "On the inbound bubble from step 2, confirm the leading avatar-dot is rendered in Ashley's identity colorHue (not the neutral grey fallback). Cross-check against the ashley identity in the identity registry (colorHue value matches)."
      * RELAYBUB-04 long inbound: "Ask Ashley to send a long (>500 char) message from Element. Confirm: bubble shows `📄 body written to /tmp/relay-msg-XXXX.txt` preview line at top and the full fetched body inline below within ~1s (the browser devtools Network tab should show a request to `/relay-pointer?hostId=...&path=/tmp/relay-msg-...` returning 200 — NOT `/claude-session/relay-pointer` which was the stale earlier-revision path). If backend is unreachable, confirm the `📄 fetch failed (…)` indicator appears instead of a silent drop."
      * RELAYBUB-05 extraction fallback: "In a session that has an outbound relay send using a shell-var interpolation (e.g. `curl -X PUT … -d \"$body\"`), confirm the outbound bubble still appears with a `⚠ extraction failed — no -d single/double quoted arg found` line + a `show source` toggle button. Click the toggle — the raw command should expand + collapse."
      * RELAYBUB-06 no-regression: "On a normal Claude Code pretty-view session (no relay activity), confirm: user/assistant bubbles look identical to pre-deploy; ComposeBox typing + send still works; IdentityBadge unchanged; WipBubble/PlanPendingBubble/AsideBubble all still render on their triggering conditions; session-holding banner appears normally on /id reset; keyboard chord Ctrl+Shift+O still toggles pretty ↔ tmux mode."
      * Plus 3 polish items: (a) mobile viewport check both bubbles wrap correctly; (b) copy the body text from an inbound bubble via native browser select — should work per RENDER-04; (c) rapid-fire relay round-trips (5+ in ~30s) don't visually stutter or double-emit.
    Step 4 — Author 17-PATCHES-MD-ENTRY.md. Format: match the last skynet-patches.md entry conventions (patch number NN+1 where NN is the current highest number in the file), title `#NN pretty-view relay bubbles — Skynet integration (Phase 17)`, body paragraphs covering (a) what shipped (2 new bubble variants + backend detection + SSRF-safe pointer-fetch at `/relay-pointer` on the main Express backend), (b) requirements closed (RELAYBUB-01..06), (c) prototype byte-shape lock (Ashley 6/6 acceptance 2026-07-28), (d) both nginx.conf and nginx-https.conf updated per CLAUDE.md caveat with awk-block-matcher drift-gate proving byte-identical blocks, (e) 15-min deadman deploy discipline observed. Ready to paste into skynet-patches.md at deploy time.
  </action>
  <verify>
    <automated>ls -la /home/ubuntu/skynet/.planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-BUILD-VERIFY-LOG.md /home/ubuntu/skynet/.planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-UAT-CHECKLIST.md /home/ubuntu/skynet/.planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-PATCHES-MD-ENTRY.md && grep -c 'RELAYBUB-0[1-6]' /home/ubuntu/skynet/.planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-UAT-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-BUILD-VERIFY-LOG.md && test -f .planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-UAT-CHECKLIST.md && test -f .planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-PATCHES-MD-ENTRY.md` — all three artifacts exist
    - `grep -c 'RELAYBUB-01\|RELAYBUB-02\|RELAYBUB-03\|RELAYBUB-04\|RELAYBUB-05\|RELAYBUB-06' .planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-UAT-CHECKLIST.md` returns at least 6 (each req has a testable step)
    - `grep -c 'READY FOR ASHLEY GREENLIGHT\|ready for.*greenlight' .planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-BUILD-VERIFY-LOG.md` returns at least 1 (verdict line present)
    - `grep -c 'NGINX-DRIFT-GATE-OK\|BUILD-CSS-SPACED-OK' .planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-BUILD-VERIFY-LOG.md` returns at least 2 (both structural gates recorded)
    - `grep -c '15-min deadman\|15 min deadman\|deadman rollback' .planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-PATCHES-MD-ENTRY.md` returns at least 1 (deploy discipline noted)
    - `grep -c 'nginx.conf\|nginx-https.conf' .planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-PATCHES-MD-ENTRY.md` returns at least 2 (both configs called out — CLAUDE.md caveat honored in the patch entry itself)
    - **Nginx exact-match regression gate (checker iter-3 finding #1 cross-plan drift lock)**: `grep -c 'location = /relay-pointer' .planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-04-PLAN.md .planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-BUILD-VERIFY-LOG.md` returns 0 (forbids the earlier exact-match `=` form anywhere in this plan's own text OR the produced deploy-artifact log — mirrors 17-02's L239 regression gate to prevent cross-plan pattern drift; the awk block-matcher above uses the regex `~` form matching plan 17-02's nginx blocks, so any `=` occurrence would indicate a partial migration and immediate cascade blocker)
    - **Stale-path drift-gate** (checker iter-2 finding #1 lock): `grep -c '/claude-session/relay-pointer' .planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-BUILD-VERIFY-LOG.md .planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-UAT-CHECKLIST.md .planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-PATCHES-MD-ENTRY.md` returns 0 (zero stale paths — the correct path is `/relay-pointer` per plan 17-02's main-backend mount)
    - `grep -c '/relay-pointer' .planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-UAT-CHECKLIST.md` returns at least 1 (the RELAYBUB-04 UAT step references the correct URL Ashley will see in devtools)
    - `git status --short src/ docker/ 2>&1 | wc -l` returns 0 (this plan touches ZERO source files and ZERO docker files — RELAYBUB-06 gate)
  </acceptance_criteria>
  <done>Three artifacts land in the phase directory; build+test+nginx-drift-gate (with non-empty extraction check) evidence captured; UAT checklist walks Ashley through each RELAYBUB-01..06 using the correct `/relay-pointer` endpoint path; patches-md entry drafted; ZERO source or docker file changes; zero stale `/claude-session/relay-pointer` references anywhere in the three artifacts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Ashley post-deploy UAT sign-off</name>
  <what-built>
    Phase 17 is code-side complete. Plans 17-01/17-02/17-03 landed:
    - Backend session-file-parser detects Matrix relay outbound + inbound turns (byte-shape ported from prototype 6/6 acceptance battery)
    - Backend SSRF-safe `/relay-pointer` endpoint on the main Express backend (port 30001) for long-inbound file-pointer fetch, mirror-registered in nginx.conf + nginx-https.conf, with `head -c` remote-side size cap so oversized files cannot exhaust backend RAM
    - Frontend RelayOutboundBubble + RelayInboundBubble render blue (right-aligned) + orange (left-aligned) glass bubbles with hue-chain sender resolution + file-pointer fetch (against `/relay-pointer` — NOT the stale `/claude-session/relay-pointer` path from earlier revisions) + extraction-failure ⚠ graceful fallback
    - Zero touches to ChatMessage / ComposeBox / IdentityBadge / any other pretty-view interior surface
    17-BUILD-VERIFY-LOG.md confirms tsc-clean + vitest-green + npm-build-clean + emitted-CSS SPACED-rgba + nginx-drift-gate-OK (with non-empty-extraction check).
  </what-built>
  <how-to-verify>
    1. Ashley reads 17-BUILD-VERIFY-LOG.md and confirms build/test/nginx-drift evidence is what she expects.
    2. Ashley runs `docker compose up -d --force-recreate skynet` behind the 15-min deadman rollback timer (per fork DEPLOY DISCIPLINE — Claude does NOT deploy).
    3. **Post-deploy, before UAT walk**: run the end-to-end curl smoke commands from 17-BUILD-VERIFY-LOG.md's "post-deploy smoke commands" section. Both variants MUST return HTTP `{400, 401}` and MUST NOT return `200` with HTML body. If either returns `200` with HTML, the deploy is HOT — the CLAUDE.md silent-index.html failure mode fired. Roll back before the deadman fires and route the fix to plan 17-02 (nginx block missing or misrouted).
    4. After deploy + smoke green, Ashley opens a fleet-connected tab in Skynet and walks the 17-UAT-CHECKLIST.md steps in order:
       - Item 1 (RELAYBUB-01 outbound blue bubble)
       - Item 2 (RELAYBUB-02 inbound orange bubble)
       - Item 3 (RELAYBUB-03 sender-hue on inbound header dot)
       - Item 4 (RELAYBUB-04 long inbound file-pointer fetch — devtools Network tab shows `/relay-pointer?hostId=...&path=...` returning 200)
       - Item 5 (RELAYBUB-05 extraction-fallback ⚠ + source toggle)
       - Item 6 (RELAYBUB-06 no regression on normal turns / IdentityBadge / ComposeBox / WipBubble / holding banner / keyboard chord)
       - 3 polish items (mobile wrap, native copy, rapid-fire stability)
    5. On UAT green, Ashley pastes 17-PATCHES-MD-ENTRY.md's draft into skynet-patches.md as the next patch number.
    6. On UAT red on any item, Ashley describes what she saw; the resulting fix routes back to whichever plan (17-01 backend / 17-02 nginx or backend proxy / 17-03 frontend) owns that surface.
  </how-to-verify>
  <resume-signal>
    Type "approved" once Ashley has UAT-signed off + patches-md entry is pasted, OR describe the failure(s) so the fix can be scoped to the correct upstream plan.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new in this plan — no source edits, no runtime changes. The threat models from plans 17-01, 17-02, 17-03 stand.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04-01 | Denial of Service | premature deploy | mitigate | Blocking human-verify checkpoint gates on Ashley's greenlight. The 15-min deadman rollback timer runs behind her deploy command per CLAUDE.md DEPLOY DISCIPLINE. Claude does NOT execute the deploy. |
| T-17-04-02 | Tampering | stale endpoint path in deploy artifacts (checker iter-2 finding #1) | mitigate | All three artifacts (BUILD-VERIFY-LOG, UAT-CHECKLIST, PATCHES-MD-ENTRY) are grep-gated for zero `/claude-session/relay-pointer` occurrences. The nginx drift-gate uses awk block-matcher on `/relay-pointer` AND has an explicit non-empty-extraction preflight (`[ -s /tmp/... ] || fatal`) so the silent-pass-on-empty failure mode is impossible. |
| T-17-04-SC | Tampering | npm packages | mitigate | Zero new packages installed in this plan. |
</threat_model>

<verification>
- All three artifacts exist in the phase directory.
- UAT checklist covers every RELAYBUB-01..06 explicitly using the correct `/relay-pointer` endpoint path.
- Build-verify log captures tsc/vitest/npm-build evidence + BUILD-CSS-SPACED-OK + NGINX-DRIFT-GATE-OK (with non-empty preflight) + post-deploy smoke commands.
- Zero source file changes, zero docker file changes in this plan (RELAYBUB-06 defensive posture).
- Zero stale `/claude-session/relay-pointer` references in any artifact (drift gate from checker iter-2 finding #1).
- Human checkpoint pauses execution until Ashley UATs post-deploy.
</verification>

<success_criteria>
- Ashley has a walk-through document she can use post-deploy without needing to re-derive test scenarios.
- Patches-md entry is paste-ready.
- Deploy is Ashley's separate action behind the 15-min deadman timer — Claude does not push code.
- On UAT green, the phase closes; on UAT red, revisions route to the correct upstream plan.
- All endpoint references use `/relay-pointer` — zero stale `/claude-session/relay-pointer` paths.
</success_criteria>

<output>
Create `.planning/phases/17-pretty-view-relay-bubbles-skynet-integration/17-04-SUMMARY.md` when done (after Ashley's UAT sign-off).
Include: build-verify evidence snapshot (BUILD-CSS-SPACED-OK + NGINX-DRIFT-GATE-OK gate outputs), post-deploy curl smoke output (both variants, actual HTTP codes returned), UAT walkthrough result per item (green / green-with-polish-note / red-plus-fix-plan), the exact skynet-patches.md entry that landed with its patch number, deploy timestamp + Ashley's greenlight confirmation, any polish/followup items filed as quick-tasks.
</output>
