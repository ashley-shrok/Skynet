---
phase: 116-image-gen-skill-file-drop-broker-for-openai-image-generation
plan: 04
type: execute
wave: 2
depends_on:
  - 116-01
  - 116-02
  - 116-03
files_modified:
  - substrate/scripts/tests/image-gen.test.sh
  - src/backend/image-gen-requests/end-to-end.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "substrate/scripts/tests/image-gen.test.sh runs hermetically (HOME=<mktemp -d>) and covers: (1) request-drop happy path, (2) ref-before-json write-order, (3) 5-minute timeout via IMAGE_GEN_TIMEOUT_SEC env override, (4) planted success response is picked up and PNGs moved to output dir, (5) planted failure response prints failure JSON on stderr with non-zero exit"
    - "src/backend/image-gen-requests/end-to-end.test.ts drives the full backend wire: a fake request file → mocked SSH channel returns tab-separated stdout → scan claims → queue enqueues → worker dequeues → mocked fetch returns fake OpenAI response → writeMarkdownFileAtomic + writeBinaryFileAtomic mocks receive the correct paths + payloads"
    - "end-to-end.test.ts also covers the failure paths: 429 → failure.json {reason:'rate_limited'}, TTL-expired item → failure.json {reason:'expired'} + no fetch call, missing OPENAI_API_KEY → failure.json {reason:'not_configured'}"
    - "Both test suites pass hermetically with no network access + no real SSH"
    - "The bash test driver mirrors substrate/scripts/tests/fleet-status-sweep.test.sh conventions (mktemp+trap+assert helpers)"
  artifacts:
    - path: "substrate/scripts/tests/image-gen.test.sh"
      provides: "hermetic bash test suite for the helper script"
      contains: "mktemp"
    - path: "src/backend/image-gen-requests/end-to-end.test.ts"
      provides: "backend end-to-end wire test with mocked SSH + mocked fetch"
      contains: "IMAGE_GEN_SCAN_CMD"
  key_links:
    - from: "substrate/scripts/tests/image-gen.test.sh"
      to: "substrate/scripts/image-gen"
      via: "HELPER=$REPO_ROOT/substrate/scripts/image-gen; HOME=$FIXTURE bash $HELPER ..."
      pattern: "substrate/scripts/image-gen"
    - from: "src/backend/image-gen-requests/end-to-end.test.ts"
      to: "src/backend/image-gen-requests/scan-orchestrator.ts + queue.ts + worker.ts"
      via: "imports the factory + queue.setProcessImageGen + starts a scan tick with mocked deps"
      pattern: "createImageGenScanOrchestrator"
---

<objective>
Validate the full request→response wire end-to-end for both the caller side (bash helper) and the backend side (scan → queue → worker → adapter → response drop), using hermetic fixtures and mocks so the tests run offline with no OpenAI credential and no real SSH access.

Purpose: Confirm the two halves compose correctly. Wave 1 tested the leaves in isolation; Plan 03 tested each backend module in isolation. This plan is the "does the whole thing work together" test — the shape-file-locked wire protocol either lands as designed or the integration surfaces the gap before the phase ships.

Output: 1 new bash test file exercising the helper script hermetically, 1 new vitest end-to-end test driving the backend wire against mocked SSH + mocked fetch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-image-gen-skill-file-drop-broker-for-openai-image-generation/116-CONTEXT.md
@.planning/phases/116-image-gen-skill-file-drop-broker-for-openai-image-generation/116-RESEARCH.md
@.planning/phases/116-image-gen-skill-file-drop-broker-for-openai-image-generation/116-PATTERNS.md
@.planning/phases/116-image-gen-skill-file-drop-broker-for-openai-image-generation/116-01-SUMMARY.md
@.planning/phases/116-image-gen-skill-file-drop-broker-for-openai-image-generation/116-02-SUMMARY.md
@.planning/phases/116-image-gen-skill-file-drop-broker-for-openai-image-generation/116-03-SUMMARY.md

# Analog test files:
@substrate/scripts/tests/fleet-status-sweep.test.sh
@src/backend/spawn-requests/scan-orchestrator.test.ts

# System under test:
@substrate/scripts/image-gen
@src/backend/image-gen-requests/scan-orchestrator.ts
@src/backend/image-gen-requests/queue.ts
@src/backend/image-gen-requests/worker.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: substrate/scripts/tests/image-gen.test.sh — hermetic bash test driver for the helper</name>
  <files>substrate/scripts/tests/image-gen.test.sh</files>
  <read_first>
    - substrate/scripts/tests/fleet-status-sweep.test.sh (the canonical template — header + path resolution, mktemp+trap cleanup, PASS/FAIL/CURRENT_TEST bookkeeping, fail/assert_eq helpers, and the per-test `CURRENT_TEST=` + isolated-scratch-tree convention)
    - substrate/scripts/image-gen (Plan 02 output — the helper we're testing)
    - .planning/phases/116-image-gen-skill-file-drop-broker-for-openai-image-generation/116-CONTEXT.md (D-10, D-14, D-15, D-16)
    - .planning/phases/116-image-gen-skill-file-drop-broker-for-openai-image-generation/116-PATTERNS.md (substrate/scripts/tests/image-gen.test.sh section — 5-test surface)
  </read_first>
  <behavior>
    - Script is executable (`chmod +x`), starts with `#!/usr/bin/env bash`, uses `set -u`, `shellcheck shell=bash` directive.
    - HOME is set to a `mktemp -d` fixture for every helper invocation so the real `~/fleet/image-gen-requests/` is never touched.
    - Test 1 (happy request drop): run `IMAGE_GEN_TIMEOUT_SEC=2 HOME=$FIXTURE bash "$HELPER" "test prompt"` in the background; sleep ~0.5s; assert that exactly one `*.json` file appeared in `$FIXTURE/fleet/image-gen-requests/`; parse its contents and assert `.prompt == "test prompt"`, `.requested_at` present (via `jq -r` if available; else raw grep). Then kill/wait the background helper to let the timeout fire.
    - Test 2 (ref-before-json write order): create a fake `$FIXTURE/fake.png` (any bytes). Run helper with `--ref $FIXTURE/fake.png`; observe the ref file must appear BEFORE the JSON. Two acceptable implementations: (a) use `inotifywait` if available and assert the ref-file CREATE event precedes the json CREATE event; (b) portable fallback — run the helper against a slow-write emulator: patch the helper's json write to `sleep 0.2` before the mv (not possible without modifying the helper, so skip this approach); (c) simpler: after the run completes (with timeout expired), assert that during a mid-run poll BOTH files exist together, and rely on the helper's script logic being reviewed elsewhere. Recommended: (a) with `inotifywait` gated on availability (`command -v inotifywait` — skip test with warning if not installed).
    - Test 3 (5-min timeout via env override): run `IMAGE_GEN_TIMEOUT_SEC=2 HOME=$FIXTURE bash "$HELPER" "prompt"`; capture stderr + exit code; assert exit code non-zero AND stderr contains `"reason":"expired"`.
    - Test 4 (planted success): run helper in background; sleep ~0.3s to let it write its request file; read the request filename to derive the uuid; plant `$FIXTURE/fleet/image-gen-requests/<uuid>.success.json` (small JSON: `{"images":["<uuid>.success.0.png"],"size":"1024x1024","model":"gpt-image-1","n":1,"generation_time_ms":123}`) + `$FIXTURE/fleet/image-gen-requests/<uuid>.success.0.png` (any bytes); wait for helper to exit; assert exit code 0, assert stdout contains a path ending in `.png`, assert the moved PNG exists at that path, assert the wire files (`.success.json`, request `.json`) are deleted.
    - Test 5 (planted failure): run helper in background; sleep ~0.3s; plant `<uuid>.failure.json` with `{"reason":"content_blocked","message":"provider refused"}`; wait for exit; assert non-zero exit, assert stderr contains `content_blocked`, assert `.failure.json` and request `.json` are deleted.
    - Final report: print `PASS: <count> FAIL: <count>` and any failure messages; exit 0 on all pass, non-zero on any fail.
    - Every test resets `$FIXTURE` structure so tests don't leak state into each other (either mktemp per test OR rm -rf inside cleanup + mkdir -p between tests).
  </behavior>
  <action>
    Copy substrate/scripts/tests/fleet-status-sweep.test.sh as the template. Preserve: shebang, `set -u`, SCRIPT_DIR/REPO_ROOT resolution pattern, mktemp+trap EXIT cleanup, PASS/FAIL bookkeeping, `fail()` and `assert_eq()` helpers, `CURRENT_TEST` marker.

    Locate the helper via `HELPER="$REPO_ROOT/substrate/scripts/image-gen"` and assert it exists + is executable. If not, print a fatal and exit 1.

    Between tests, either recreate `$FIXTURE` or clear `$FIXTURE/fleet/image-gen-requests/` and `$FIXTURE/fleet/image-gen-outputs/` so state doesn't leak.

    Test 1 implementation:
    - `IMAGE_GEN_TIMEOUT_SEC=2 HOME="$FIXTURE" bash "$HELPER" "test prompt" >/dev/null 2>&1 &`
    - `helper_pid=$!`
    - `sleep 0.4`
    - `files=("$FIXTURE"/fleet/image-gen-requests/*.json)`
    - `assert_eq 1 "${#files[@]}" "expected exactly one request json"`
    - `body=$(cat "${files[0]}")`
    - Use `jq -r '.prompt'` if available (`command -v jq`), else `grep -o '"prompt":"[^"]*"' "${files[0]}"`, to extract the prompt and assert it equals `test prompt`.
    - `wait "$helper_pid" || true` to let it finish + return.

    Test 2 implementation:
    - If `command -v inotifywait >/dev/null 2>&1`:
      - `printf 'fake png bytes' > "$FIXTURE/fake.png"`
      - Start `inotifywait -m -e create --format '%f' "$FIXTURE/fleet/image-gen-requests/" > "$FIXTURE/events.log" 2>/dev/null &`
      - `IMAGE_GEN_TIMEOUT_SEC=2 HOME="$FIXTURE" bash "$HELPER" "prompt" --ref "$FIXTURE/fake.png" >/dev/null 2>&1 &`
      - `sleep 0.8`; kill inotifywait; wait helper.
      - Parse `events.log`: the first non-tmp line matching `\.ref\.png$` must appear before the first line matching `^[0-9a-f-]{36}\.json$`. Assert.
    - Else: `printf 'SKIP: inotifywait unavailable\n'` and skip test.

    Test 3 implementation:
    - `IMAGE_GEN_TIMEOUT_SEC=2 HOME="$FIXTURE" bash "$HELPER" "prompt" > "$FIXTURE/stdout" 2> "$FIXTURE/stderr"; rc=$?`
    - `assert_eq 1 "$rc" "expected exit 1 on timeout"` (or non-zero — planner picks; the helper exits 1 per Plan 02 Task 2)
    - `grep -q '"reason":"expired"' "$FIXTURE/stderr" || fail "expected expired reason in stderr"`

    Test 4 implementation:
    - `IMAGE_GEN_TIMEOUT_SEC=10 HOME="$FIXTURE" bash "$HELPER" "prompt" > "$FIXTURE/stdout" 2> "$FIXTURE/stderr" &`
    - `helper_pid=$!`
    - `sleep 0.4`
    - Read the request filename: `req_file=$(ls "$FIXTURE"/fleet/image-gen-requests/*.json 2>/dev/null | head -1); uuid=$(basename "$req_file" .json)`
    - Plant success: `printf '{"images":["%s.success.0.png"],"size":"1024x1024","model":"gpt-image-1","n":1,"generation_time_ms":123}\n' "$uuid" > "$FIXTURE/fleet/image-gen-requests/$uuid.success.json.tmp.$$" && mv "$FIXTURE/fleet/image-gen-requests/$uuid.success.json.tmp.$$" "$FIXTURE/fleet/image-gen-requests/$uuid.success.json"` (atomic per D-10)
    - Plant PNG: `printf 'fake png' > "$FIXTURE/fleet/image-gen-requests/$uuid.success.0.png.tmp.$$" && mv "$FIXTURE/fleet/image-gen-requests/$uuid.success.0.png.tmp.$$" "$FIXTURE/fleet/image-gen-requests/$uuid.success.0.png"`
    - `wait "$helper_pid"; rc=$?`
    - `assert_eq 0 "$rc" "expected exit 0 on success"`
    - `moved_path=$(head -1 "$FIXTURE/stdout")`
    - `[ -f "$moved_path" ] || fail "moved PNG not at $moved_path"`
    - `[ ! -f "$FIXTURE/fleet/image-gen-requests/$uuid.success.json" ] || fail "wire success.json not cleaned"`
    - `[ ! -f "$FIXTURE/fleet/image-gen-requests/$uuid.json" ] || fail "wire request.json not cleaned"`

    Test 5 implementation: similar to Test 4 but plant `.failure.json` with `{"reason":"content_blocked",...}`; assert non-zero exit; assert stderr contains `content_blocked`; assert both wire files cleaned.

    Final report: `printf 'PASS: %d FAIL: %d\n' "$PASS" "$FAIL"`; if any FAILURES, `printf '%s\n' "${FAILURES[@]}"`; `[ "$FAIL" -eq 0 ]`.

    Do NOT touch the real `$HOME/fleet/` — every test MUST set HOME=$FIXTURE.
    Do NOT depend on network access.
    Do NOT depend on the backend being running.
  </action>
  <verify>
    <automated>test -x /home/ubuntu/skynet-nebula/substrate/scripts/tests/image-gen.test.sh && bash /home/ubuntu/skynet-nebula/substrate/scripts/tests/image-gen.test.sh</automated>
  </verify>
  <done>
    File exists, is executable, `bash -n` clean, and running it exits 0 with PASS >= 4 (Test 2 may skip if inotifywait unavailable). No file created outside `$FIXTURE`.
  </done>
</task>

<task type="auto">
  <name>Task 2: end-to-end.test.ts — backend wire test (mocked SSH + mocked fetch)</name>
  <files>src/backend/image-gen-requests/end-to-end.test.ts</files>
  <read_first>
    - src/backend/spawn-requests/scan-orchestrator.test.ts (382 lines — the whole file is the analog structure for a scan-orchestrator-driven integration test using vi.useFakeTimers + mocked channel + mocked enqueue)
    - src/backend/image-gen-requests/scan-orchestrator.ts (Plan 03 output — the factory + IMAGE_GEN_SCAN_CMD to drive)
    - src/backend/image-gen-requests/queue.ts (Plan 03 output — enqueue + setProcessImageGen + setWorkerDeps + startPool)
    - src/backend/image-gen-requests/worker.ts (Plan 03 output — processImageGen entry point)
    - src/backend/image-gen-requests/adapter.ts (Plan 01 output — the fetch-based call we're mocking)
    - .planning/phases/116-image-gen-skill-file-drop-broker-for-openai-image-generation/116-RESEARCH.md (Q11 gives the end-to-end test structure — Setup / Action / Verify)
    - .planning/phases/116-image-gen-skill-file-drop-broker-for-openai-image-generation/116-CONTEXT.md (D-22 TTL, D-25 not_configured)
  </read_first>
  <behavior>
    - Test file lives at `src/backend/image-gen-requests/end-to-end.test.ts`. Uses vitest, imports from the sibling modules only (no cross-package imports).
    - Setup for each test:
      - `vi.mock("../utils/logger.js", ...)` mocks logger.
      - `vi.stubGlobal("fetch", vi.fn())` mocks the OpenAI HTTPS call.
      - `beforeEach`: reset queue state via `__resetForTests`; construct fresh mocks for `writeMarkdownFileAtomic` + `writeBinaryFileAtomic` + `isLocalHostId` + `resolveHostById` + `connectOneShot`; set `process.env.OPENAI_API_KEY = "sk-test"`; construct a token bucket via `createTokenBucket(600)` (10/sec — high enough that the token acquire is effectively instant in tests); construct workerDeps injecting all mocks; call `setWorkerDeps + setProcessImageGen(processImageGen) + startPool()`.
      - `afterEach`: `stopPool()`; `vi.useRealTimers()`; delete env; `__resetForTests()`; `vi.restoreAllMocks()`.
    - Test HAPPY: mock fetch to return `{ok:true, status:200, json: async () => ({data:[{b64_json: Buffer.from("fake png bytes").toString("base64")}]})}`; construct a PendingImageGen with a fresh uuid, `body:{prompt:"cat", requested_at:new Date().toISOString(), n:1}`, `hostIdNum: 42`, `hostId:"42"`, `userId:"user-1"`; call `queue.enqueue(item)`; await a microtask cycle (`await new Promise(r => setImmediate(r))` or `await vi.advanceTimersByTimeAsync(10)`); assert:
      - `fetch` called once with URL matching `https://api.openai.com/v1/images/generations`, method POST, body containing `"model":"gpt-image-1"` and `"prompt":"cat"`.
      - `writeBinaryFileAtomic` called once with path matching `$HOME/fleet/image-gen-requests/<uuid>.success.0.png` and buffer bytes equal to "fake png bytes".
      - `writeMarkdownFileAtomic` called once with path matching `$HOME/fleet/image-gen-requests/<uuid>.success.json` and body containing `"model":"gpt-image-1"`, `"n":1`, `"images":["<uuid>.success.0.png"]`.
      - PNG write happened BEFORE JSON write (assert via `.mock.invocationCallOrder`).
    - Test RATE_LIMITED: mock fetch to return status 429; enqueue item; assert `writeBinaryFileAtomic` NOT called; assert `writeMarkdownFileAtomic` called with path ending `.failure.json` and body `{"reason":"rate_limited"}`.
    - Test PROVIDER_UNAVAILABLE: mock fetch to return status 503; assert failure.json with `provider_unavailable`.
    - Test CONTENT_BLOCKED: mock fetch to return status 400 with body `{error:{code:"content_policy_violation", message:"nope"}}`; assert failure.json with `content_blocked` + message `nope`.
    - Test NOT_CONFIGURED: delete `process.env.OPENAI_API_KEY`; enqueue item; assert failure.json with `not_configured`; assert fetch NOT called.
    - Test EXPIRED: enqueue item with `body.requested_at` set to `new Date(Date.now() - 6 * 60 * 1000).toISOString()` (6 min ago); assert failure.json with `expired`; assert fetch NOT called; assert token bucket NOT acquired.
    - Test SCAN_INTEGRATION (bonus): construct a mocked SSH channel where `channel.exec(cmd)` returns a tab-separated stdout `"<uuid>.json\t{\"prompt\":\"cat\",\"requested_at\":\"<ISO>\"}\n"` when cmd matches the IMAGE_GEN_SCAN_CMD; construct a mocked listSubstrateHosts returning one fake host; construct the scan orchestrator with these deps + real queue enqueue; call `orchestrator.start()`; assert queue drains the enqueued item + fetch is called + response files written.
  </behavior>
  <action>
    Build the test file using the analog structure. Each test is a `it(...)` block inside a `describe("image-gen end-to-end", ...)`. All test cases MUST be hermetic — no real network, no real SSH, no real filesystem writes (writeMarkdownFileAtomic + writeBinaryFileAtomic are injected mocks — they never actually touch disk in tests).

    Concrete scaffolding:
    - Import: `beforeEach, afterEach, describe, it, expect, vi` from vitest; `enqueue, setProcessImageGen, setWorkerDeps, startPool, stopPool, __resetForTests` from `./queue.js`; `processImageGen` (or its production wrapper) from `./worker.js`; `createTokenBucket` from `./token-bucket.js`; `createImageGenScanOrchestrator, IMAGE_GEN_SCAN_CMD` from `./scan-orchestrator.js`; types from `./types.js`.
    - `makeItem(overrides)`: factory returning a `PendingImageGen` with sensible defaults + spread overrides.
    - `makeMockWorkerDeps()`: returns `{writeMarkdownFileAtomic: vi.fn().mockResolvedValue(undefined), writeBinaryFileAtomic: vi.fn().mockResolvedValue(undefined), connectOneShot: vi.fn().mockResolvedValue({end: vi.fn()}), execCommand: vi.fn(), isLocalHostId: vi.fn().mockReturnValue(true), resolveHostById: vi.fn().mockResolvedValue({id:"42"}), callOpenAiImageGen: <the real adapter>, tokenBucket, now: () => Date.now()}`.
    - Note that when `isLocalHostId` returns true, the worker takes the LOCAL branch and calls `writeMarkdownFileAtomic(null, path, body)` + `writeBinaryFileAtomic(null, path, buf)` — the mock signatures accept `null` as conn.
    - Use `vi.stubGlobal("fetch", fetchMock)` in `beforeEach`; the adapter reads `process.env.OPENAI_API_KEY` on each call so setting it in beforeEach works.
    - After enqueue, wait for the worker loop to complete the item. Options: (a) resolve fetch synchronously (via `mockResolvedValueOnce`) then await `new Promise(r => setImmediate(r))` a few times — the worker's `await deps.callOpenAiImageGen(...)` resolves in a microtask; (b) use fake timers + `vi.advanceTimersByTimeAsync(100)`; (c) use a helper `await queueDrained()` that spins until `isEmpty()` returns true AND `fetchMock.mock.calls.length === expectedCalls`. Recommend (a) — simplest and matches spawn-requests test pattern.

    SCAN_INTEGRATION test specifically:
    - Mock `channel.exec` to inspect the cmd string; on match to IMAGE_GEN_SCAN_CMD return the tab-separated stdout; on any other cmd (e.g., the companion-fetch cat) return an empty string or null.
    - Construct `listSubstrateHosts` returning `[{id:"42", name:"test-host", _connDetails:{}}]`.
    - Construct `acquireChannel` returning the mocked channel; `releaseChannel` as `vi.fn()`.
    - Use `deps.setInterval` = `vi.fn()` to capture the interval callback for direct invocation (mirror spawn-requests/scan-orchestrator.test.ts:67-100).
    - Call `orch.start()`; the initial pass runs. Await queue drain. Assert full pipeline.

    Do NOT rely on real timing (no `setTimeout(resolve, 100)` — that's flaky in CI). Use `await new Promise(setImmediate)` or fake timers.
    Do NOT use a real OpenAI API key. Every test sets a placeholder.
    Do NOT hit real disk. Every filesystem-adjacent call is mocked.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-nebula && npx vitest run src/backend/image-gen-requests/end-to-end.test.ts</automated>
  </verify>
  <done>
    File exists and all test cases pass; test runtime is < 5 seconds; grep confirms `vi.stubGlobal("fetch"` is present; grep confirms `writeMarkdownFileAtomic` and `writeBinaryFileAtomic` are asserted with the correct paths (matching `image-gen-requests` folder + `success.<i>.png` / `success.json` / `failure.json` suffixes).
  </done>
</task>

<task type="auto">
  <name>Task 3: Phase-close automated verification bundle (human_verify_mode=end-of-phase)</name>
  <files>.planning/phases/116-image-gen-skill-file-drop-broker-for-openai-image-generation/116-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/116-image-gen-skill-file-drop-broker-for-openai-image-generation/116-01-SUMMARY.md
    - .planning/phases/116-image-gen-skill-file-drop-broker-for-openai-image-generation/116-02-SUMMARY.md
    - .planning/phases/116-image-gen-skill-file-drop-broker-for-openai-image-generation/116-03-SUMMARY.md
    - .planning/config.json (workflow.human_verify_mode value — confirms end-of-phase review path)
  </read_first>
  <action>
    Per `workflow.human_verify_mode: end-of-phase` in .planning/config.json, this plan does NOT emit an inline blocking checkpoint. Instead, run the full automated verification bundle below, capture pass/fail status per command in the SUMMARY, and hand off to the phase-orchestrator's end-of-phase review flow. Ashley's UAT happens at the phase level (not inside a plan) once every plan's SUMMARY has landed.

    Execute (in order, capturing exit code + relevant tail of stdout for each):
    1. `cd /home/ubuntu/skynet-nebula && npx vitest run src/backend/image-gen-requests/` — expect all green across all 8 test files (Plan 01: 4 + Plan 03: 3 + Plan 04: 1).
    2. `cd /home/ubuntu/skynet-nebula && npx vitest run src/backend/claude-session/` — expect no regression from writeBinaryFileAtomic export.
    3. `cd /home/ubuntu/skynet-nebula && bash substrate/scripts/tests/image-gen.test.sh` — expect PASS ≥ 4 (Test 2 may skip if inotifywait unavailable), FAIL 0.
    4. `cd /home/ubuntu/skynet-nebula && npx tsc --noEmit` — expect 0 errors introduced.
    5. `bash -n /home/ubuntu/skynet-nebula/substrate/scripts/image-gen` — clean syntax.
    6. `test -x /home/ubuntu/skynet-nebula/substrate/scripts/image-gen && echo OK` — executable bit set.
    7. `grep -c "image-gen-skill\|image-gen-helper" /home/ubuntu/skynet-nebula/src/backend/distributor/catalog.ts` — expect exactly 2.

    Additional grep invariants (fast structural spot-checks):
    - `grep -c "MUST NOT include PHI" /home/ubuntu/skynet-nebula/substrate/skills/image-gen/SKILL.md` — expect 1
    - `grep -c "^export async function writeBinaryFileAtomic" /home/ubuntu/skynet-nebula/src/backend/claude-session/identity-artifact-reader.ts` — expect 1
    - `grep -c "createImageGenScanOrchestrator" /home/ubuntu/skynet-nebula/src/backend/starter.ts` — expect ≥ 1
    - `grep -c "from ['\"].*ssh-poll-orchestrator" /home/ubuntu/skynet-nebula/src/backend/image-gen-requests/scan-orchestrator.ts` — expect 0 (RESEARCH.md Q2 invariant)

    Write results to 116-04-SUMMARY.md. If any command fails, mark the SUMMARY status FAILED and stop here — the phase-orchestrator picks up the failure and routes to gap closure. If all pass, mark SUMMARY status PASSED and note that phase-level UAT is the next step (Ashley reviews via `/gsd:build` close flow or equivalent phase-close mechanism).

    Do NOT push, docker build, or docker compose up (D-28). Executor's remit stops here — the substrate distributor picks up the new skill + helper on its own schedule after Ashley's ship greenlight.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-nebula && npx vitest run src/backend/image-gen-requests/ && bash substrate/scripts/tests/image-gen.test.sh && npx tsc --noEmit</automated>
    <human-check>
      Ashley reviews at phase-close time (not inside this plan). Manual eyeballs:
      - substrate/skills/image-gen/SKILL.md: PHI directive at top + inline echo + full 7-row failure table
      - substrate/scripts/image-gen: ref-before-json write order visible in source
      - src/backend/starter.ts: image-gen wiring block sits AFTER the spawn-scan block
      - src/backend/image-gen-requests/scan-orchestrator.ts: no import from ssh-poll-orchestrator.js (Q2 invariant)
    </human-check>
  </verify>
  <done>
    All 7 automated commands exit 0. 116-04-SUMMARY.md records each command's result. No file pushed / built / deployed by this task.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture $FIXTURE → real $HOME | HOME env override MUST isolate all helper writes; a bug that lets writes escape $FIXTURE would trash the user's real ~/fleet/ dir |
| Mocked fetch → real network | vi.stubGlobal MUST fully intercept; a bug that lets fetch fall through would make an unauthenticated call to OpenAI in CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-04-01 | Tampering | image-gen.test.sh writes escape $FIXTURE | mitigate | Every helper invocation sets `HOME=$FIXTURE`. Test framework asserts no file created outside $FIXTURE (spot-check via `find $HOME -newer $FIXTURE 2>/dev/null | head`). |
| T-116-04-02 | Info Disclosure | end-to-end.test.ts leaks a real API key via fetch to OpenAI | mitigate | `vi.stubGlobal("fetch", fetchMock)` fully intercepts. Every test sets a placeholder OPENAI_API_KEY like `sk-test` — no real credential in the test source. |
| T-116-04-03 | DoS | End-to-end test runs slow enough to block CI | mitigate | All timing is via fake timers or microtask flush; no real setTimeout waits. Full test suite budget: 5s. |
| T-116-04-04 | Spoofing | Bash test uses non-hermetic $HOME | mitigate | mktemp per-suite + explicit HOME=$FIXTURE prefix on every helper invocation; asserted in Task 1 done criteria. |
</threat_model>

<verification>
Run:
- `npx vitest run src/backend/image-gen-requests/` → 8 test files green (Plan 01: 4 + Plan 03: 3 + Plan 04: 1)
- `bash substrate/scripts/tests/image-gen.test.sh` → PASS ≥ 4, FAIL 0
- `npx tsc --noEmit` → clean

Manual review checklist:
- [ ] end-to-end.test.ts sets `process.env.OPENAI_API_KEY = "sk-test"` in beforeEach (no real credential in test source)
- [ ] image-gen.test.sh sets `HOME="$FIXTURE"` on every helper invocation (grep confirms zero invocations without HOME override)
- [ ] end-to-end.test.ts uses microtask flush or fake timers (no real setTimeout waits > 10ms)
- [ ] The final checkpoint task correctly reflects the Wave 1 + Wave 2 + Wave 2 (04) full coverage
</verification>

<success_criteria>
Phase 116 complete when:
- All three tasks pass
- Ashley's manual verify checkpoint approved
- No push / build / deploy triggered by executor (D-28)
- Substrate distributor will pick up the new skill + helper on its own schedule after Ashley ships (out of executor's remit)
</success_criteria>

<output>
Create `.planning/phases/116-image-gen-skill-file-drop-broker-for-openai-image-generation/116-04-SUMMARY.md` when done. Capture: which tests landed, actual runtime for the vitest end-to-end suite, whether Test 2 (inotifywait-based) ran or was skipped, any deviation from RESEARCH.md Q11 test structure, and any assumption from Plan 01 (particularly Assumption A3 — /edits multipart shape) that ended up validated or deferred.

Additionally create `.planning/phases/116-image-gen-skill-file-drop-broker-for-openai-image-generation/116-SUMMARY.md` (phase-level) rolling up 116-01/02/03/04 SUMMARYs: which files landed, all D-01..D-28 decisions mapped to which plan implemented them, and a one-line status per decision (Implemented / Deferred / N/A).
</output>
