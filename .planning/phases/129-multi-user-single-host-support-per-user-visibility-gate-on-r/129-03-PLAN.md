---
phase: 129-multi-user-single-host-support-per-user-visibility-gate-on-r
plan: 03
type: execute
wave: 2
depends_on: [129-01]
files_modified:
  - src/backend/database/routes/sessions.ts
  - src/backend/database/routes/sessions.test.ts
  - src/backend/database/routes/roles-list-for-host.ts
  - src/backend/database/routes/roles-list-for-host.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "GET /sessions/list filters out rows whose sessionName (== identityKey) fails the identity+role intersection gate (D-2, D-7 — no orphan session rows)"
    - "GET /roles?hostId=<n> filters out roles whose users list closes for the caller username (D-2 role-side gate; used by the NewSessionDialog role picker per shape §Prior-context)"
    - "sessions.ts reuses the identity file's markdown for BOTH role extraction AND cosmetics extraction — one SSH round-trip per session, not two (Assumption A5)"
    - "roles-list-for-host.ts preserves the raw users field through its narrowing so the gate can evaluate it (currently narrowing at L249 drops non-cosmetic fields — the extension must preserve users:)"
    - "Both endpoints fail-open on read errors (null cosmetics → visible) to preserve the D-8 'not a permission system' contract — hidden-because-unreadable would leak a permission-system behavior"
    - "The role-picker gate uses identityCos=null (only the role side matters when picking a role for a NEW identity) — passes only role gate through isIdentityVisibleToUser"
  artifacts:
    - path: "src/backend/database/routes/sessions.ts"
      provides: "GET /list handler with per-request callerUsername + per-session identity+role gate in the roleResolveBlock area"
      contains: "isIdentityVisibleToUser"
    - path: "src/backend/database/routes/roles-list-for-host.ts"
      provides: "GET / handler with per-request callerUsername + per-role gate; narrowing extended to preserve raw users field"
      contains: "isIdentityVisibleToUser"
  key_links:
    - from: "sessions.ts /list handler"
      to: "identity-visibility-gate.ts + host-user-counter.ts"
      via: "per-session gate inside roleResolveBlock; single SSH read reused for role+cosmetics"
      pattern: "isIdentityVisibleToUser\\("
    - from: "roles-list-for-host.ts / handler"
      to: "identity-visibility-gate.ts + host-user-counter.ts"
      via: "post-cosmetics filter before response emit"
      pattern: "isIdentityVisibleToUser\\("
---

<objective>
Close the two Wave-2 deep-gate leaks per D-7:

1. `GET /sessions/list` (sessions.ts) — the endpoint the sidebar joins with `/identities` (see conversation-store.ts L849-918 fleetSyntheticRows). Currently Pitfall 3: it reads only the role name via resolveRoleForIdentity, so the identity-side users gate silently doesn't work for session rows. Fix: extend the roleResolveBlock to read the identity file directly (single SSH round-trip yielding BOTH role name AND cosmetics per Assumption A5) and gate the row.

2. `GET /roles?hostId=<n>` (roles-list-for-host.ts) — the role picker in the new-agent UI (NewSessionDialog.tsx). Per shape §"What would make it wrong" bullet 5: "The role picker in the new-agent UI shows roles the user can't see. Whatever gates the sidebar has to gate the picker too." Extend the existing per-role cosmetics extraction to preserve the raw users field and gate the list.

Purpose: Closes two of the four Wave-2 deep-gate surfaces (129-02 closes /identities; 129-04 closes /conversation-search; 129-05 closes the WS frames).

Output: Two route handlers extended with per-request callerUsername + per-row gate + structured logs at the gate seams.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-CONTEXT.md
@.planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-PATTERNS.md
@.planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-RESEARCH.md
@.planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-01-SUMMARY.md
@src/backend/database/routes/sessions.ts
@src/backend/database/routes/roles-list-for-host.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend sessions.ts /list — read identity file once, gate row on intersection (D-7 + Pitfall 3)</name>
  <files>src/backend/database/routes/sessions.ts, src/backend/database/routes/sessions.test.ts</files>
  <read_first>
    - src/backend/database/routes/sessions.ts L294-425 (GET /list handler — the roleResolveBlock area is at L403-425)
    - src/backend/database/routes/sessions.ts L400-430 (resolveRoleForIdentity call site — this is where the identity file is currently read for role extraction only; Pitfall 3 lock is here)
    - src/backend/claude-session/identity-artifact-reader.ts L370-417 (readIdentityFile + resolveRoleForIdentity — verify readIdentityFile returns the FULL markdown so extractCosmeticsFromFrontmatter can be called on the same string, avoiding a second SSH read per Assumption A5)
    - src/backend/database/routes/identities.ts L361-386 (roleReadCache pattern — mirror this in sessions.ts for per-host role-file memoization)
    - src/backend/database/routes/sessions.test.ts (existing test scaffold — mirror the vi.mock stack)
    - .planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-PATTERNS.md § "sessions.ts (MODIFY — GET /list L294-425)"
    - .planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-RESEARCH.md § "Pitfall 3"
  </read_first>
  <behavior>
    - Test A (single-user host, session row untagged → visible): Ashley owns a host, has a live tmux session "muffin". GET /sessions/list returns the row for muffin. Zero regression.
    - Test B (multi-user host, session row untagged → visible to both): Ashley owns host, Zoe has hostAccess. Session "muffin" exists; identity file lacks users: key. Both Ashley's and Zoe's requests see the row.
    - Test C (multi-user host, identity file users:[ashley] → row hidden from Zoe): Ashley's response includes muffin row; Zoe's response does not include muffin. D-7 depth — the row is absent, not stripped-of-role.
    - Test D (multi-user host, role file users:[ashley] → identity in that role hidden from Zoe): even with identity file untagged, the ROLE side of the gate closes for Zoe. Row absent from Zoe's response.
    - Test E (both sides tagged, non-overlapping intersection): same as identities test — Ashley sees, Zoe does not.
    - Test F (identity file read fails mid-fanout — fail-open): row is either dropped by the pre-existing error path OR surfaced with role=null; the gate MUST NOT convert a read-failure into a false-positive HIDE (a hidden-because-unreadable row is a permission-system behavior forbidden by D-8; fail-open preserves visibility as the safer default).
    - Test G (single SSH round-trip verified): Test asserts that resolveRoleForIdentity is NOT called anymore (replaced by direct readIdentityFile+extractCosmeticsFromFrontmatter+extractRoleFromMarkdown) OR that the total number of readIdentityFile calls per row is exactly 1 (Assumption A5 lock via mock call-count).
  </behavior>
  <action>
    Rewrite the roleResolveBlock section of the /list handler (sessions.ts L403-425). Two structural changes:

    1. **Per-request callerUsername fetch** (top of handler, near the userId extraction): `const callerUsername = await getUsernameForUserId(userId);` (imports from ../../utils/host-user-counter.js). Warn-log if null with `operation: "sessions_gate_username_missing"`.

    2. **Per-host role memo** (mirror identities.ts L361-386): declare a `roleReadCache = new Map<string, Promise<RawCosmetics | null>>()` scoped to the per-host fanout branch. Helper: `readRoleCosmeticsMemoized(roleName)` calls `readRoleFileByName(conn, roleName)` then `extractCosmeticsFromFrontmatter(md)`. On error, cache `null` — matches the D-8 fail-open contract.

    3. **Replace resolveRoleForIdentity with direct readIdentityFile + extract-both** in the roleResolveBlock area. Call `readIdentityFile(conn, row.sessionName)` ONCE to get the markdown; extract `role` via `extractRoleFromMarkdown(markdown)`; extract `identityCos` via `extractCosmeticsFromFrontmatter(markdown)`. Fetch `roleCos` via `readRoleCosmeticsMemoized(role)` when role is non-null, else null.

    4. **Apply the gate**: compute `const visible = isIdentityVisibleToUser(identityCos, roleCos, callerUsername)`. Attach as a transient property `row._visible = visible` (OR track in a parallel Map<sessionName, boolean> per PATTERNS.md note — pick one; the parallel-Map approach avoids row-shape mutation).

    5. **Filter after Promise.all**: `const filtered = rows.filter((r) => visibleMap.get(r.sessionName) !== false)`. The `!== false` shape (rather than `=== true`) preserves the D-8 fail-open contract: rows not yet in the map (e.g. read failure before gate ran) stay visible.

    6. **Add structured logs at the gate seam**: `systemLogger.debug("Phase 129: session hidden by visibility gate", {operation: "sessions_gate_hidden", sessionName, hostId, callerUsername})` when a row is hidden. Warn-log for missing username per box-maintainer directive.

    Follow TDD: write the 7 behavior tests A-G first in sessions.test.ts as a new "Phase 129: visibility gate" describe block. Watch fail. Implement above. Verify green.

    IMPORTANT preserve-invariants:
    - Do NOT touch the pre-existing session-row fields (host_id, session_name, has_zellij, etc.) — the gate is a row filter, not a shape change.
    - Do NOT remove any existing error-path branches; the gate lives on top of the existing pipeline.
    - Do NOT introduce a second SSH read per row — the single readIdentityFile call yields both role and cosmetics (Assumption A5 lock).
  </action>
  <verify>
    <automated>npx vitest related --run src/backend/database/routes/sessions.test.ts src/backend/database/routes/sessions.ts</automated>
  </verify>
  <acceptance_criteria>
    - behavior: All 7 new "Phase 129: visibility gate" tests pass; all pre-existing sessions.test.ts tests pass.
    - source: `grep -n "getUsernameForUserId(userId)" src/backend/database/routes/sessions.ts` returns exactly 1 hit (per-request).
    - source: `grep -n "isIdentityVisibleToUser(" src/backend/database/routes/sessions.ts` returns exactly 1 hit (per-session).
    - source: `grep -n "resolveRoleForIdentity" src/backend/database/routes/sessions.ts` returns 0 hits in the /list handler body (replaced by readIdentityFile+extract-both). If the file uses resolveRoleForIdentity elsewhere for a different purpose, that's fine — but the /list branch must go direct.
    - source: `grep -n "sessions_gate_hidden\|sessions_gate_username_missing" src/backend/database/routes/sessions.ts` returns at least 2 hits (structured log seams present).
    - source: `grep -n "roleReadCache\|readRoleCosmeticsMemoized" src/backend/database/routes/sessions.ts` returns at least 1 hit (memo pattern present — mirrors identities.ts).
  </acceptance_criteria>
  <done>
    - /sessions/list is deep-gated; Pitfall 3 closed.
    - Single SSH round-trip per session preserved (Assumption A5).
    - Structured logs at both seams.
    - No regression to fail-open behavior on read errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend roles-list-for-host.ts /roles?hostId=<n> — filter by role.users gate</name>
  <files>src/backend/database/routes/roles-list-for-host.ts, src/backend/database/routes/roles-list-for-host.test.ts</files>
  <read_first>
    - src/backend/database/routes/roles-list-for-host.ts (full file, 299 lines — study the existing per-role cosmetics loop at L238-266)
    - src/backend/database/routes/roles-list-for-host.ts L237-266 (the loop that builds cosByName Map via extractCosmeticsFromFrontmatter; the narrowing at L251-257 currently forwards only 5 fields — MUST be extended to preserve raw users OR fetch raw users separately)
    - src/backend/database/routes/roles-list-for-host.test.ts (existing test scaffold — mirror vi.mock stack)
    - .planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-PATTERNS.md § "roles-list-for-host.ts (MODIFY — GET /)"
    - .planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-CONTEXT.md § "Locked decisions" bullet 6 ("Role picker in new-agent UI DOES filter by role.users gate")
  </read_first>
  <behavior>
    - Test A (single-user host, role untagged → visible): Ashley requests /roles?hostId=1 for a host she alone owns; response includes role "muffin-friend". Zero regression.
    - Test B (multi-user host, role untagged → visible to both): shared host; role "muffin-friend" has no users: key. Both Ashley and Zoe see it.
    - Test C (multi-user host, role users:[ashley] → visible to Ashley, hidden from Zoe): Ashley's response includes "muffin-friend"; Zoe's does not.
    - Test D (multi-user host, role users:[ashley,zoe] → visible to both): shared explicitly. Both see.
    - Test E (identity-side users: is IGNORED by role-picker gate): a role with users:[ashley,zoe] whose eventual-child-identity would narrow to [ashley] still appears in Zoe's role picker — because Zoe is CREATING a new identity, not viewing an existing one; the identity-side gate is not applicable yet. This is why the gate call passes `identityCos=null`.
    - Test F (defensive: username lookup fails → fail-open): mock getUsernameForUserId to null; role list is returned unfiltered with a warn log.
    - Test G (role file read fails mid-loop → row skipped per pre-existing error path): behavior unchanged from pre-129.
  </behavior>
  <action>
    Edit roles-list-for-host.ts /roles handler (~L108-299). Three changes:

    1. **Per-request callerUsername fetch** (top of handler, adjacent to the userId extraction — locate via `grep -n "userId" src/backend/database/routes/roles-list-for-host.ts`): `const callerUsername = await getUsernameForUserId(userId);` (import from ../../utils/host-user-counter.js). Warn-log if null with `operation: "roles_list_gate_username_missing"`.

    2. **Preserve raw users field through the cosmetics narrowing**. Current narrowing at L251-257 (per PATTERNS.md) forwards only 5 fields into `RoleCosmetics`. Two options:
       - Option A: store the RAW `extractCosmeticsFromFrontmatter(md)` output in a parallel Map<name, RawCosmetics>, and read `.users` from it at gate-apply time.
       - Option B: extend the narrowing to include users: string[].

       Choose Option A — it keeps the RoleCosmetics wire-shape unchanged (frontend doesn't need users: in the response; the gate is backend-side; and D-6 says NO new UI affordance). Declare `rawCosByName = new Map<string, RawCosmetics>()` alongside the existing cosByName, populate from the same extract call.

    3. **Filter validRoles by gate** before building the `result` array (near L261-266). Iterate `validRoles`, compute `const raw = rawCosByName.get(name) ?? null` and `const gateOpen = isIdentityVisibleToUser(null, raw, callerUsername)`, keep only when gateOpen. The `identityCos=null` argument reduces the intersection to just the role side (correct for a role-picker context per PATTERNS.md § "Design detail").

    4. **Structured logs at the gate seam**: `systemLogger.debug("Phase 129: role hidden by picker gate", {operation: "roles_list_gate_hidden", role: name, hostId, callerUsername})` when a role is filtered out.

    Follow TDD: write the 7 tests A-G first in roles-list-for-host.test.ts as a new "Phase 129: role-picker gate" describe block. Watch fail. Implement above. Verify green.

    IMPORTANT preserve-invariants:
    - Do NOT change the RoleCosmetics wire-shape (D-6: no new UI affordance).
    - Do NOT gate on identity-side (identity file gate only bites when viewing existing identities, not when picking a role for a new one).
    - Do NOT touch the batched-cat single-round-trip pattern (roles-list-for-host.ts L191-196) — SSH resource discipline is load-bearing.
  </action>
  <verify>
    <automated>npx vitest related --run src/backend/database/routes/roles-list-for-host.test.ts src/backend/database/routes/roles-list-for-host.ts</automated>
  </verify>
  <acceptance_criteria>
    - behavior: All 7 new "Phase 129: role-picker gate" tests pass; all pre-existing tests pass.
    - source: `grep -n "getUsernameForUserId(userId)" src/backend/database/routes/roles-list-for-host.ts` returns exactly 1 hit.
    - source: `grep -n "isIdentityVisibleToUser(null," src/backend/database/routes/roles-list-for-host.ts` returns exactly 1 hit (identityCos=null lock: role-side-only gate).
    - source: `grep -n "roles_list_gate_hidden\|roles_list_gate_username_missing" src/backend/database/routes/roles-list-for-host.ts` returns at least 2 hits (structured log seams).
    - source: `grep -n "rawCosByName\|new Map<string, RawCosmetics>" src/backend/database/routes/roles-list-for-host.ts` returns at least 1 hit (raw cosmetics preserved for gate — Option A).
    - source: Existing `RoleCosmetics` type / narrowing shape at L249-257 is not extended to include users: (D-6 wire-shape lock; verify by inspection).
  </acceptance_criteria>
  <done>
    - /roles?hostId=<n> is gated per D-2 role-side intersection.
    - NewSessionDialog's role dropdown naturally receives a shorter list — zero frontend code change needed per D-6.
    - Structured logs at both seams.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTPS client → /sessions/list handler | authenticateJWT + host-access; new identity+role gate on top |
| HTTPS client → /roles?hostId=<n> handler | authenticateJWT + host-access; new role-side gate on top |
| SSH read of identity/role markdown | Fail-open on parse/read error — matches D-8 "not a permission system" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-03-01 | Info Disclosure | /sessions/list leaks hidden identity as orphan row | mitigate | Read identity file (once per row, Assumption A5) → gate on intersection → parallel-Map filter after Promise.all; Pitfall 3 closed |
| T-129-03-02 | Info Disclosure | /roles picker shows role Zoe can't create identities under | mitigate | role-side gate with identityCos=null; NewSessionDialog naturally shows shorter list |
| T-129-03-03 | Performance | Second SSH read per session for gate would double SSH pressure | mitigate | Assumption A5: reuse readIdentityFile output for BOTH role extract AND cosmetics extract; single round-trip per row |
| T-129-03-04 | Repudiation | Silent gate decisions on session/role rows | mitigate | Debug-level structured log at each hidden-row decision with sessionName/role + hostId + callerUsername |
| T-129-03-05 | Denial of Service | Fail-closed on identity read error would hide legitimate rows on transient SSH errors | accept | Fail-open per D-8; read-error rows stay visible with role=null |
| T-129-03-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
- Both suites pass end-to-end.
- No double-SSH-read regression on sessions.ts (Assumption A5).
- Role picker gate uses identityCos=null (Option A: raw cosmetics preserved without extending wire-shape).
</verification>

<success_criteria>
- 14 total new tests (7 for sessions, 7 for roles-list) turn red-then-green.
- Both files add per-request callerUsername + per-row gate + structured logs.
- Zero regression to pre-129 single-user-host behavior on either endpoint.
- Sessions.ts uses single SSH round-trip per row.
</success_criteria>

<output>
Create `.planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-03-SUMMARY.md` listing: (a) test counts red-then-green for both files, (b) confirmation that resolveRoleForIdentity is no longer called in sessions.ts /list branch, (c) confirmation that RoleCosmetics wire-shape is unchanged (D-6 lock).
</output>
