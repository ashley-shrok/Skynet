---
phase: 129-multi-user-single-host-support-per-user-visibility-gate-on-r
plan: 02
type: execute
wave: 2
depends_on: [129-01]
files_modified:
  - src/backend/database/routes/identities.ts
  - src/backend/database/routes/identities.get-disk.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "GET /identities filters out identities where isIdentityVisibleToUser returns false for the caller's username (D-2, D-7 deep gate)"
    - "The caller's username is fetched exactly ONCE per request via getUsernameForUserId (not per-identity, not per-host) — matches identities.ts per-request-cost discipline"
    - "Read fail on identity or role frontmatter still surfaces the identity with safe-default cosmetics (fail-open on read failure — matches the existing null-cosmetics contract; the visibility filter is not a permission system per D-8)"
    - "Ashley's request sees identities whose users list contains ashley OR is empty/absent on both role AND identity sides; identities tagged users:[zoe] on either side do NOT appear (D-2, D-3)"
    - "The 'unknown caller username' defensive branch (getUsernameForUserId returns null) treats the gate as disabled (returns all identities the current host-access predicate would allow), and logs the mismatch as a warning"
  artifacts:
    - path: "src/backend/database/routes/identities.ts"
      provides: "GET / handler with per-request callerUsername + per-identity gate applied inside the fanout Promise.all"
      contains: "isIdentityVisibleToUser"
    - path: "src/backend/database/routes/identities.get-disk.test.ts"
      provides: "Cross-user visibility tests — Ashley's request sees identity A (users:[ashley]) and Zoe's does not"
  key_links:
    - from: "identities.ts GET / handler"
      to: "identity-visibility-gate.ts isIdentityVisibleToUser"
      via: "call inside identityKeys.map, BEFORE publicIdentity"
      pattern: "isIdentityVisibleToUser\\("
    - from: "identities.ts GET / handler"
      to: "host-user-counter.ts getUsernameForUserId"
      via: "per-request lookup before per-host fanout"
      pattern: "getUsernameForUserId\\("
---

<objective>
Apply the Phase 129 per-user visibility gate to `GET /identities` — the primary REST surface the frontend's `conversation-store.ts` polls to build the sidebar. This is the FIRST of the deep-gate seams (D-7): if an identity is filtered here, no static row appears; the wave-2 sibling plans (129-03, 129-04, 129-05) handle the other surfaces that could leak an identity name (session-list, search, WS frames).

Purpose: Ships the largest single-user-facing gate — every fresh sidebar mount hits this endpoint. Together with plans 129-03 and 129-05, this makes "hidden identity is invisible on page-load" true.

Output: `identities.ts` fanout extended with per-request `callerUsername` fetch + per-identity `isIdentityVisibleToUser` filter in the roleReadCache-memoized branch. Cross-user integration test proves Ashley's list ≠ Zoe's list on a shared host.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-CONTEXT.md
@.planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-PATTERNS.md
@.planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-RESEARCH.md
@.planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-01-SUMMARY.md
@src/backend/database/routes/identities.ts
@src/backend/database/routes/identities.get-disk.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cross-user visibility test cases to identities.get-disk.test.ts (RED)</name>
  <files>src/backend/database/routes/identities.get-disk.test.ts</files>
  <read_first>
    - src/backend/database/routes/identities.get-disk.test.ts (full file — study the existing vi.mock scaffold for auth-manager, drizzle, host-resolver, and how the test fires HTTP requests through bare-Express + node http.request)
    - src/backend/database/routes/identities.ts L294-485 (GET / handler + fanout — the exact code path the tests are exercising)
    - src/backend/fleet-status/identity-visibility-gate.test.ts (from Plan 129-01 — the pure-gate matrix that these integration tests are the wire-level analog of)
    - .planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-CONTEXT.md § "Locked decisions" bullets 2, 3, 7
  </read_first>
  <behavior>
    - Test A (single-user host — no evidence of feature per D-4 + shape §"What would make it wrong" bullet 1): Ashley requests /identities for a host she alone owns; response includes identity "muffin" (whose file has NO users: frontmatter key). Zero regression from pre-129 shape.
    - Test B (multi-user host, identity untagged → visible to both users per D-3): Ashley owns t1000, Zoe has hostAccess. Identity "muffin" has NO users: key. Ashley sees muffin. Zoe sees muffin. Fallback rule confirmed at the wire.
    - Test C (multi-user host, identity tagged users:[ashley] → visible to Ashley, hidden from Zoe per D-2): Ashley sees "muffin"; Zoe does NOT see "muffin". Response for Zoe simply lacks the row entirely (no ghost, no stripped-down cosmetics — D-7 deep gate).
    - Test D (multi-user host, role tagged users:[ashley] → identity in that role is hidden from Zoe per D-2 intersection): even if identity file has NO users: key, role.users:[ashley] closes the gate. Zoe sees zero identities in that role; Ashley sees them all.
    - Test E (both sides tagged, non-overlapping → hidden per intersection): role users:[ashley,zoe], identity users:[ashley] → Ashley sees it, Zoe does not.
    - Test F (unknown username defensive branch): mock `getUsernameForUserId` to return null. GET /identities STILL returns rows (fail-open — a defective userId lookup must not empty out the sidebar per D-8 "not a permission system"). A warning is logged.
    - Test G (identity file read fails mid-fanout — existing fail-open contract): the failing identity's row is either dropped (per existing L445 null-return) OR surfaced with safe defaults per the pre-129 contract. Gate MUST NOT convert existing null-drops into false-positive visibility; do NOT regress the null-cosmetics contract.
  </behavior>
  <action>
    Extend the existing identities.get-disk.test.ts with a new `describe("Phase 129: per-user visibility gate", () => { ... })` block. Reuse the file's existing vi.mock stack (auth-manager, drizzle, host-resolver, artifact-reader, ssh-one-shot, logger). Add three fixture helpers:

    - `mockGetUsernameForUserId(userId, username)` — mocks `getUsernameForUserId` from `../../utils/host-user-counter.js` to resolve userId→username for the test.
    - `mockIdentityWithUsers(identityKey, hostId, roleName, identityUsers?, roleUsers?)` — builds a frontmatter fixture where the identity file contains `users: [...]` when identityUsers is provided, and the role file contains `users: [...]` when roleUsers is provided. Both default to absent.
    - `fireGetIdentitiesAs(username, identityHostsQuery)` — sets the auth mock so `req.userId` resolves through `getUsernameForUserId` to the given username, then fires GET /identities?identityHosts=... and returns the parsed JSON body.

    Write tests A-G verbatim per &lt;behavior&gt;. Every test asserts the ROW SHAPE at the wire (JSON array): the hidden identity is absent from the array (D-7 — no stripped-down row, no ghost). All tests MUST fail on RED — the gate is not yet wired in Task 2.

    Structured logging assertion: Test F asserts a `systemLogger.warn` was called with an `operation: "identities_gate_username_missing"` payload (or similar) when getUsernameForUserId returns null. This makes the box-maintainer directive testable, not just recommended.

    Do NOT touch the pre-existing test cases in this file. Extend, do not rewrite.
  </action>
  <verify>
    <automated>npx vitest related --run src/backend/database/routes/identities.get-disk.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - behavior: 7 new tests exist in the "Phase 129: per-user visibility gate" describe block.
    - behavior: All 7 new tests currently FAIL (RED phase — implementation lands in Task 2). Vitest run reports 7 failures in this describe block only; pre-existing tests still pass.
    - source: `grep -n "Phase 129: per-user visibility gate" src/backend/database/routes/identities.get-disk.test.ts` returns exactly 1 hit.
    - source: `grep -c "mockGetUsernameForUserId\|mockIdentityWithUsers\|fireGetIdentitiesAs" src/backend/database/routes/identities.get-disk.test.ts` returns at least 3 hits (fixture helpers present).
    - source: `grep -n "identities_gate_username_missing\|username.*missing" src/backend/database/routes/identities.get-disk.test.ts` returns at least 1 hit (Test F warn-log assertion).
  </acceptance_criteria>
  <done>
    - 7 red tests land in the extended describe block.
    - Fixture helpers reusable across tests.
    - Test file still lints and vi.mock scaffold intact.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the gate + per-request username lookup in identities.ts GET / handler (GREEN)</name>
  <files>src/backend/database/routes/identities.ts</files>
  <read_first>
    - src/backend/database/routes/identities.ts L294-485 (GET / handler — full fanout body; the exact seam is inside identityKeys.map at ~L389-450)
    - src/backend/database/routes/identities.ts L361-386 (roleReadCache pattern — per-host memoization of readRoleFileByName; the gate call goes AFTER cosmetics + roleCosmetics extraction but BEFORE publicIdentity)
    - src/backend/database/routes/identities.ts L153-254 (publicIdentity — the returned shape; gate output is a filter, NOT a shape change)
    - src/backend/database/routes/identities.ts L450 (existing .filter((x) => x !== null) — the gate collapses onto this same null-drop path)
    - .planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-PATTERNS.md § "identities.ts (MODIFY — GET / fanout L294-485)"
    - .planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-RESEARCH.md § "Applying the gate at GET /identities" (design note on why we gate BEFORE publicIdentity rather than plumbing raw cosmetics through the row)
  </read_first>
  <behavior>
    - After this task, all 7 Task-1 tests turn GREEN.
    - Regression check: all pre-existing identities.get-disk.test.ts tests still pass.
    - The gate is called EXACTLY ONCE per (identityKey, hostId) pair inside the fanout — proven by placing it inside identityKeys.map alongside the existing null-drop return.
    - The username fetch is called EXACTLY ONCE per request — proven by declaring `const callerUsername = await getUsernameForUserId(userId)` at the top of the handler before any per-host / per-identity work.
  </behavior>
  <action>
    Edit identities.ts GET / handler. Two changes:

    1. **Per-request username fetch** (BEFORE the per-host fanout, near L316 where `userId` is extracted from `req as AuthenticatedRequest`): call `const callerUsername = await getUsernameForUserId(userId);` where `getUsernameForUserId` imports from `../../utils/host-user-counter.js`. If null, log `systemLogger.warn("Phase 129: GET /identities callerUsername lookup failed — gate disabled for this request", {operation: "identities_gate_username_missing", userId})`. Do NOT abort the request; a null callerUsername passes through to `isIdentityVisibleToUser` and disables the gate per Plan 01 Task 2 Test 1 (null-bypass semantic).

    2. **Per-identity gate** (inside `identityKeys.map` at ~L389-450, AFTER `cosmetics` and `roleCosmetics` are extracted, BEFORE the `return publicIdentity(...)` call): insert
       ```
       if (!isIdentityVisibleToUser(cosmetics, roleCosmetics, callerUsername)) return null;
       ```
       where `isIdentityVisibleToUser` imports from `../../fleet-status/identity-visibility-gate.js`. The gate result collapses onto the existing L450 `.filter((x): x is ReturnType<typeof publicIdentity> => x !== null)` — no new filter, no new shape change.

    IMPORTANT preserve-invariants:
    - Do NOT touch `publicIdentity`'s signature (the "safe-default cosmetics for a null-cosmetics row" path in publicIdentity is the existing D-8 fail-open contract for read errors — unrelated to the gate).
    - Do NOT touch the `roleReadCache` memo — the gate reads from the already-cached roleCosmetics.
    - Do NOT add a second gate anywhere else in this file (single call site inside the fanout).

    Add ONE structured log at the gate seam per box-maintainer directive: when the gate returns false (identity is hidden), log `systemLogger.debug("Phase 129: identity hidden by visibility gate", {operation: "identities_gate_hidden", identityKey, hostId, callerUsername})`. This is at the debug level — traces per-request gate decisions when troubleshooting a "why don't I see this identity?" bug. NEVER at info/warn; hidden-by-design is not a warning.
  </action>
  <verify>
    <automated>npx vitest related --run src/backend/database/routes/identities.get-disk.test.ts src/backend/database/routes/identities.ts</automated>
  </verify>
  <acceptance_criteria>
    - behavior: All 7 Phase-129 tests from Task 1 now PASS.
    - behavior: All pre-existing identities.get-disk.test.ts tests still pass (zero regression).
    - source: `grep -n "getUsernameForUserId(userId)" src/backend/database/routes/identities.ts` returns exactly 1 hit (per-request fetch, not per-host).
    - source: `grep -n "isIdentityVisibleToUser(" src/backend/database/routes/identities.ts` returns exactly 1 hit (single call site inside the fanout).
    - source: `grep -n "return publicIdentity" src/backend/database/routes/identities.ts` returns at least 1 hit and every hit is preceded (within the same map body) by the isIdentityVisibleToUser gate — verify by inspection.
    - source: `grep -n "identities_gate_username_missing\|identities_gate_hidden" src/backend/database/routes/identities.ts` returns at least 2 hits (both structured log seams present per box-maintainer directive).
    - source: `grep -c "resolveIdentityAppearance" src/backend/database/routes/identities.ts` returns the same count as before the edit (this file does not import resolveIdentityAppearance today; gate lives OUTSIDE the cascade authority per Plan 01 design lock).
  </acceptance_criteria>
  <done>
    - GET /identities response is deep-gated per D-2 + D-3 + D-7.
    - Callers on single-user hosts see zero behavior change (Test A green).
    - Cross-user visibility on shared hosts matches intersection semantics (Tests B-E green).
    - Defensive null-username branch fail-opens with structured warn log (Test F green).
    - No regression to fail-open null-cosmetics contract (Test G green + pre-existing tests still pass).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTPS client → GET /identities handler | authenticateJWT gates the userId; gate applies on top of host-access gate |
| userId → getUsernameForUserId → username string | DB may return null (defensive fail-open — pass null to gate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-02-01 | Info Disclosure | GET /identities response leaks hidden identity | mitigate | Per-identity isIdentityVisibleToUser call inside fanout; hidden identities return null and collapse onto existing L450 null-filter (D-2 + D-7 deep gate) |
| T-129-02-02 | Info Disclosure | Ghost row with stripped cosmetics reveals hidden identity presence | mitigate | Gate applied BEFORE publicIdentity call; the row is NEVER constructed, not "constructed then stripped" (D-7 shape §"depth of gate") |
| T-129-02-03 | Denial of Service | Defective getUsernameForUserId lookup empties the sidebar | accept | Fail-open per D-8: null callerUsername disables the gate; caller still sees rows their host-access allows. Warn-logged for ops visibility. |
| T-129-02-04 | Repudiation | Silent gate decisions in prod prevent ops from diagnosing "why don't I see this identity?" | mitigate | Debug-level structured log at every hidden-by-gate decision with {identityKey, hostId, callerUsername} per box-maintainer directive |
| T-129-02-SC | Tampering | npm installs | accept | No new packages installed |
</threat_model>

<verification>
- Full identities.get-disk.test.ts suite passes (both new and pre-existing tests).
- No accidental double-gate (gate called exactly once per identity per request — verified via grep in Task 2 acceptance).
- Fail-open on defective username lookup is tested + logged.
</verification>

<success_criteria>
- 7 new red-then-green tests prove Ashley/Zoe visibility on a shared host.
- Zero regression to pre-129 single-user-host behavior.
- Structured logs added at two seams (missing-username warn + hidden-decision debug).
</success_criteria>

<output>
Create `.planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-02-SUMMARY.md` when done, listing: (a) exact test count red-then-green, (b) confirmed single-call-site for the gate in identities.ts (line number), (c) any behavior discovered during green-phase implementation that differs from RESEARCH assumptions.
</output>
