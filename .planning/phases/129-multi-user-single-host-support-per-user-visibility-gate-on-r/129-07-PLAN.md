---
phase: 129-multi-user-single-host-support-per-user-visibility-gate-on-r
plan: 07
type: execute
wave: 3
depends_on: [129-01]
files_modified:
  - src/backend/database/routes/identity-birth-orchestrator.ts
  - src/backend/database/routes/identity-birth-orchestrator.test.ts
  - src/backend/database/routes/identity-birth.ts
  - src/backend/database/routes/identity-birth.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "POST /identities/birth auto-tags the creator's Skynet username in the new identity file's users: frontmatter list ONLY WHEN the target host has strictly more than one Skynet user with access (D-4 conditional)"
    - "On single-user hosts, POST /identities/birth writes the identity file with NO users: key (D-4 majority-case invisibility)"
    - "creatorUsername is threaded via BirthOptions.creatorUsername?: string — the orchestrator stays pure (injected deps pattern) and the route handler owns the DB lookups"
    - "buildIdentityFileBody emits the users: pair via the existing pairs.push([...]) pattern ONLY when opts.creatorUsername is a non-empty string — absent-⇒-omit fallback preserved (D-3)"
    - "getUsernameForUserId failure on a multi-user host → auto-tag skipped, warn log fires, identity is still created (write-side fail-open exception per PATTERNS.md 'Read-path fail-open, write-path fail-closed')"
    - "The pre-existing byte-shape of buildIdentityFileBody's yaml.dump output is preserved verbatim (canonical options unchanged, only pairs[] gains one optional entry)"
    - "Identity-clone endpoint (identity-clone.ts) is NOT modified in this phase — flagged as follow-up per RESEARCH § no-leak audit 'Optionally auto-tag clone target if multi-user; recommend gating this the same way as birth for symmetry. Flag as follow-up if needed.' The v1 lock excludes clone auto-tag to keep this phase's write-side surface as small as possible."
  artifacts:
    - path: "src/backend/database/routes/identity-birth-orchestrator.ts"
      provides: "BirthOptions.creatorUsername?: string + buildIdentityFileBody emits users pair when opts.creatorUsername present"
      contains: "creatorUsername"
    - path: "src/backend/database/routes/identity-birth.ts"
      provides: "POST /birth handler resolves isHostMultiUser + getUsernameForUserId before calling birthIdentity; threads creatorUsername via BirthOptions"
      contains: "isHostMultiUser"
    - path: "src/backend/database/routes/identity-birth-orchestrator.test.ts"
      provides: "buildIdentityFileBody tests for opts.creatorUsername present/absent + byte-shape preservation"
    - path: "src/backend/database/routes/identity-birth.test.ts"
      provides: "POST /birth tests for single-user (no auto-tag), multi-user (creatorUsername passed to orchestrator), lookup-fail (skip + warn)"
  key_links:
    - from: "identity-birth.ts POST handler"
      to: "host-user-counter.ts isHostMultiUser + getUsernameForUserId"
      via: "pre-orchestrator DB lookup for isMultiUser + creatorUsername"
      pattern: "isHostMultiUser\\(hostId\\)"
    - from: "identity-birth.ts POST handler"
      to: "identity-birth-orchestrator.ts BirthOptions.creatorUsername"
      via: "opts.creatorUsername passed through to birthIdentity"
      pattern: "creatorUsername:"
    - from: "identity-birth-orchestrator.ts buildIdentityFileBody"
      to: "yaml.dump pairs[] absent-⇒-omit"
      via: "conditional pairs.push([\"users\", [opts.creatorUsername]])"
      pattern: "pairs\\.push\\(\\[\"users\""
---

<objective>
Ship the write-side half of D-4 for identity creation, symmetric with 129-06 role auto-tag. The two-file split is required because `identity-birth-orchestrator.ts` is a pure orchestrator with injected deps (per its own file discipline) — the DB lookups happen in the route handler (identity-birth.ts) and creatorUsername is threaded through as an optional field on `BirthOptions`.

Design guardrails per D-5 + Pitfall 5:
- Auto-tag runs INSIDE the initial-write buildIdentityFileBody call — never rewrites existing files.
- Uses the existing `pairs.push([field, value])` absent-⇒-omit pattern; buildIdentityFileBody's yaml.dump byte-shape stays verbatim.
- The Identity-clone endpoint (identity-clone.ts) is EXPLICITLY excluded from this phase per RESEARCH § no-leak audit table — flagged as follow-up.

Purpose: Wave-3 write-side seam for identities. Together with 129-06, this closes the D-4 write-side. New role + identity on shared hosts get auto-tagged; the file is self-managing per shape §"self-managing in the shared case".

Output: Orchestrator extended with `creatorUsername?: string` in BirthOptions + conditional pairs.push in buildIdentityFileBody; route handler extended with pre-orchestrator DB lookups + creatorUsername threading; both with matching tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-CONTEXT.md
@.planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-PATTERNS.md
@.planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-RESEARCH.md
@.planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-01-SUMMARY.md
@src/backend/database/routes/identity-birth-orchestrator.ts
@src/backend/database/routes/identity-birth-orchestrator.test.ts
@src/backend/database/routes/identity-birth.ts
@src/backend/database/routes/identity-birth.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend BirthOptions + buildIdentityFileBody with users pair (pure orchestrator side)</name>
  <files>src/backend/database/routes/identity-birth-orchestrator.ts, src/backend/database/routes/identity-birth-orchestrator.test.ts</files>
  <read_first>
    - src/backend/database/routes/identity-birth-orchestrator.ts L196-260 (BirthOptions interface — add `creatorUsername?: string` after `task?: string` at L223)
    - src/backend/database/routes/identity-birth-orchestrator.ts L544-604 (buildIdentityFileBody — pairs[] absent-⇒-omit pattern; insert conditional pairs.push after L583-585 task block, BEFORE L587 yaml.dump call)
    - src/backend/database/routes/identity-birth-orchestrator.ts L570-575 (canonical yaml.dump options {sortKeys:false, lineWidth:-1, noRefs:true, forceQuotes:false} — byte-shape must be preserved)
    - src/backend/database/routes/identity-birth-orchestrator.test.ts (existing test scaffold — mirror the buildIdentityFileBody unit test pattern)
    - .planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-PATTERNS.md § "identity-birth-orchestrator.ts (MODIFY — BirthOptions L196 + buildIdentityFileBody L544-604)"
    - .planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-RESEARCH.md § "Auto-tag insertion at POST /identities/birth"
  </read_first>
  <behavior>
    - Test A (opts.creatorUsername absent → NO users: key in output — absent-⇒-omit fallback per D-3): call buildIdentityFileBody with a fully-specified opts object that DOES NOT include creatorUsername. yaml.load the returned string; assert `parsed.users` is undefined (NOT `null`, NOT `[]`).
    - Test B (opts.creatorUsername = "" (empty string) → NO users: key — normalization matches the pairs.push guard `opts.creatorUsername.length > 0`): assert `parsed.users` is undefined.
    - Test C (opts.creatorUsername = "user" → users: [user] emitted): yaml.load returns `{users: ["user"]}`.
    - Test D (opts.creatorUsername = "the user" case preservation lock): yaml.load returns `{users: ["the user"]}` — matches DB storage discipline per Pitfall 7.
    - Test E (byte-shape preservation — key order): with opts including title, colorHue, voice, task, and creatorUsername, assert the emitted frontmatter has fields in this order: role, displayName, title, colorHue, voice, task, users (creatorUsername appended AFTER task per PATTERNS.md pattern; sortKeys:false preserves insertion order).
    - Test F (byte-shape preservation — no unexpected diff on the pre-129 field set): with opts WITHOUT creatorUsername, the emitted string equals the emit that pre-edit code would have produced for the same opts. Assert by comparing against a golden-file / hardcoded expected string — proves NO regression on existing pre-129 identity file byte-shape.
    - Test G (BirthOptions type extension is source-visible): TS compilation of `const opts: BirthOptions = { ...requiredFields, creatorUsername: "user" }` succeeds without error.
  </behavior>
  <action>
    Edit identity-birth-orchestrator.ts:

    1. **BirthOptions extension** (L196-260, add after L223 `task?: string`): `creatorUsername?: string;` with JSDoc citing D-4 (multi-user-only conditional) + noting that the route handler is responsible for setting this field and MUST NOT pass it on single-user hosts per shape §"invisible in majority case".

    2. **buildIdentityFileBody extension** (L544-604, insert conditional pairs.push AFTER the L583-585 task block, BEFORE the L587 yaml.dump call):
       ```
       // Phase 129 D-4 auto-tag: users:[creator] on multi-user hosts. Route
       // handler (identity-birth.ts) sets opts.creatorUsername only when
       // isHostMultiUser=true; absent-⇒-omit fallback preserves zero-migration
       // shape on single-user hosts.
       if (typeof opts.creatorUsername === "string" && opts.creatorUsername.length > 0) {
         pairs.push(["users", [opts.creatorUsername]]);
       }
       ```

    3. **Do NOT touch the yaml.dump call at L587** — the byte-shape is preserved verbatim, only the pairs array gains one optional entry.

    Follow TDD: write the 7 behavior tests A-G first in identity-birth-orchestrator.test.ts as a new `describe("Phase 129: buildIdentityFileBody creatorUsername handling", () => { ... })` block. Watch fail. Implement above. Verify green.

    IMPORTANT preserve-invariants:
    - Do NOT modify any other BirthOptions field, any other pairs.push branch, or the canonical yaml.dump options.
    - The orchestrator stays pure — no DB imports, no getUsernameForUserId call. It receives creatorUsername as an opaque string from the route handler.
    - No structured log at this seam — the orchestrator is a pure builder; logging happens at the route-handler seam (Task 2). Adding a log here would violate the file's "pure orchestrator" discipline.
  </action>
  <verify>
    <automated>npx vitest related --run src/backend/database/routes/identity-birth-orchestrator.test.ts src/backend/database/routes/identity-birth-orchestrator.ts</automated>
  </verify>
  <acceptance_criteria>
    - behavior: All 7 new "Phase 129: buildIdentityFileBody creatorUsername handling" tests pass; all pre-existing identity-birth-orchestrator.test.ts tests pass.
    - source: `grep -n "creatorUsername?: string" src/backend/database/routes/identity-birth-orchestrator.ts` returns at least 1 hit (BirthOptions extension).
    - source: `grep -n "pairs.push\\(\\[\"users\"" src/backend/database/routes/identity-birth-orchestrator.ts` returns exactly 1 hit (single insertion inside buildIdentityFileBody).
    - source: `grep -c "yaml.dump" src/backend/database/routes/identity-birth-orchestrator.ts` returns the same count as pre-edit (yaml.dump call unchanged).
    - source: `grep -n "isHostMultiUser\|getUsernameForUserId" src/backend/database/routes/identity-birth-orchestrator.ts` returns 0 hits (orchestrator stays pure — no DB imports).
    - source: `grep -n "sshLogger\|systemLogger" src/backend/database/routes/identity-birth-orchestrator.ts | wc -l` returns the same count as pre-edit (no new logs at this seam — orchestrator stays pure).
  </acceptance_criteria>
  <done>
    - BirthOptions extended with creatorUsername?: string.
    - buildIdentityFileBody emits users pair when opts.creatorUsername present.
    - Byte-shape preserved (Test F golden-file lock).
    - Case preservation locked (Test D).
    - Orchestrator stays pure — no DB imports leak.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire pre-orchestrator DB lookup + creatorUsername threading in identity-birth.ts POST handler</name>
  <files>src/backend/database/routes/identity-birth.ts, src/backend/database/routes/identity-birth.test.ts</files>
  <read_first>
    - src/backend/database/routes/identity-birth.ts L92-345 (POST /birth handler — locate userId extraction, host access check, and the birthIdentity call; the auto-tag DB lookups insert BETWEEN validation and birthIdentity)
    - src/backend/database/routes/identity-birth.ts (full file — grep for `birthIdentity(` to find every call site and the exact opts-construction shape)
    - src/backend/database/routes/identity-birth.test.ts (existing test scaffold — SSE + orchestrator mock pattern)
    - .planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-PATTERNS.md § "identity-birth.ts (MODIFY — POST handler L94-380)"
    - .planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-RESEARCH.md § "Auto-tag insertion at POST /identities/birth"
  </read_first>
  <behavior>
    - Test A (single-user host → creatorUsername NOT passed to birthIdentity, no auto-tag): the user owns t1000 alone; POST /birth {name: "muffin"} etc. Assert the mocked birthIdentity was called with opts.creatorUsername === undefined. isHostMultiUser returns false; assert getUsernameForUserId is NOT called (efficiency).
    - Test B (multi-user host + valid getUsernameForUserId → creatorUsername passed): isHostMultiUser true, getUsernameForUserId returns "user". birthIdentity called with opts.creatorUsername === "user".
    - Test C (multi-user host + RBAC-role share): isHostMultiUser true via role expansion; auto-tag fires (Assumption A6 landed at write side too).
    - Test D (multi-user host + getUsernameForUserId returns null → creatorUsername NOT passed, warn log fires): mock lookup to null; birthIdentity called with opts.creatorUsername === undefined; assert `sshLogger.warn` called with `operation: "identity_birth_username_lookup_failed"` + payload {userId, hostId, name}. Identity is STILL created (write-side fail-open per PATTERNS.md exception).
    - Test E (host-access failure short-circuits before DB lookups): if the pre-existing host access gate fails (403), isHostMultiUser is NOT called; birthIdentity is NOT called.
    - Test F (birth-flow error paths preserved): existing SSE error emit shapes (e.g. mid-birth SSH failure) still emit correctly; auto-tag path adds no new error path — a null creatorUsername passes through cleanly.
    - Test G (info log at successful auto-tag path): assert `sshLogger.info` called with `operation: "identity_birth_auto_tagged"` + payload {name, hostId, creatorUsername} when the creator is passed through to birthIdentity.
  </behavior>
  <action>
    Edit identity-birth.ts POST handler. Insert TWO DB lookups AFTER the pre-existing validation + host-access checks and BEFORE the `await birthIdentity(...)` call:

    ```
    // Phase 129 D-4 auto-tag: check if the target host is multi-user; if so,
    // resolve the caller's Skynet username so the orchestrator can auto-tag
    // the new identity file. On single-user hosts, both queries are skipped
    // (creatorUsername stays undefined; buildIdentityFileBody omits the
    // users: key per absent-⇒-omit fallback). Write-side fail-open on
    // username lookup failure per PATTERNS.md 'Read-path fail-open,
    // write-path fail-closed' EXCEPTION.
    const isMultiUser = await isHostMultiUser(hostId);
    let creatorUsername: string | undefined = undefined;
    if (isMultiUser) {
      const resolved = await getUsernameForUserId(userId);
      if (resolved) {
        creatorUsername = resolved;
      } else {
        sshLogger.warn("Phase 129: identity-birth username lookup failed — auto-tag skipped", {
          operation: "identity_birth_username_lookup_failed",
          userId, hostId, name,
        });
      }
    }
    ```

    Then thread `creatorUsername` into the birthIdentity opts object:
    ```
    await birthIdentity({
      ...existingOpts,
      creatorUsername,  // undefined on single-user hosts or lookup fail
    }, emit, deps);
    ```

    Imports: `import { isHostMultiUser, getUsernameForUserId } from "../../utils/host-user-counter.js";`.

    Structured log at successful auto-tag: after the lookup succeeds, emit `sshLogger.info("Phase 129: identity-birth auto-tagged", {operation: "identity_birth_auto_tagged", name, hostId, creatorUsername})` — matches the roles-create.ts logging shape from Plan 129-06.

    Follow TDD: write the 7 behavior tests A-G first in identity-birth.test.ts as a new `describe("Phase 129: auto-tag on multi-user hosts", () => { ... })` block. Mock birthIdentity to capture its opts arg without executing the real orchestrator flow. Watch fail. Implement above. Verify green.

    IMPORTANT preserve-invariants:
    - Do NOT touch identity-clone.ts (RESEARCH no-leak audit: "Optionally auto-tag clone target if multi-user; recommend gating this the same way as birth for symmetry. Flag as follow-up if needed." — v1 excludes clone auto-tag to keep this phase's scope minimal).
    - Do NOT touch the pre-existing SSE emit flow — auto-tag adds ONLY the DB lookup + opts-threading. If the lookup itself fails (throws), do NOT abort the birth request; catch and treat as single-user (defensive fail-open per D-8).
    - Do NOT hoist the auto-tag logic into a shared helper. Pitfall 5 lock (write-side): auto-tag lives inline in the initial-write path only.
  </action>
  <verify>
    <automated>npx vitest related --run src/backend/database/routes/identity-birth.test.ts src/backend/database/routes/identity-birth.ts</automated>
  </verify>
  <acceptance_criteria>
    - behavior: All 7 new "Phase 129: auto-tag on multi-user hosts" tests pass; all pre-existing identity-birth.test.ts tests pass.
    - source: `grep -n "isHostMultiUser(hostId)" src/backend/database/routes/identity-birth.ts` returns exactly 1 hit.
    - source: `grep -n "getUsernameForUserId(userId)" src/backend/database/routes/identity-birth.ts` returns exactly 1 hit.
    - source: `grep -n "creatorUsername" src/backend/database/routes/identity-birth.ts` returns at least 2 hits (local variable + opts threading).
    - source: `grep -n "identity_birth_username_lookup_failed\|identity_birth_auto_tagged" src/backend/database/routes/identity-birth.ts` returns at least 2 hits (both structured log seams).
    - source: `grep -n "isHostMultiUser\|getUsernameForUserId" src/backend/database/routes/identity-clone.ts 2>/dev/null` returns 0 hits (v1 scope lock: clone endpoint explicitly NOT modified).
    - source: The isHostMultiUser call site is AFTER the host-access check and BEFORE the birthIdentity call (verify by inspection).
  </acceptance_criteria>
  <done>
    - POST /birth resolves creatorUsername per D-4 conditional.
    - creatorUsername threaded through BirthOptions to buildIdentityFileBody.
    - Single-user hosts stay silent (Test A).
    - Multi-user hosts (direct + RBAC-role) get creator resolved (Tests B, C).
    - Defensive fail-open on lookup failure (Test D).
    - identity-clone.ts explicitly NOT modified (v1 scope lock).
    - Structured logs at both seams.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTPS client → POST /identities/birth | authenticateJWT + host-access; auto-tag DB lookups on top |
| BirthOptions.creatorUsername → orchestrator | Opaque string; orchestrator does not re-validate |
| pairs.push([&quot;users&quot;, [opts.creatorUsername]]) → yaml.dump | Standard string-array serialization; no shell interpolation, no path traversal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-07-01 | Tampering | Auto-tag writes wrong username → hidden-from-actual-creator | mitigate | getUsernameForUserId reads from DB post-JWT-authn; case preserved; warn log on lookup failure |
| T-129-07-02 | Tampering | Auto-tag fires on single-user host — violates D-4 invisibility | mitigate | isHostMultiUser guard in route handler; Test A locks; RBAC-role-expansion in Plan 01 Task 3 ensures single-user hosts really are single-user |
| T-129-07-03 | Info Disclosure | identity-clone.ts creates new identity without auto-tag on shared hosts | accept (v1 scope) | Flagged as follow-up in RESEARCH no-leak audit; v1 excludes clone auto-tag to keep the phase's write-side surface minimal. Documented in Task 2 preserve-invariants. |
| T-129-07-04 | Tampering | Orchestrator gains DB imports and violates pure-orchestrator discipline | mitigate | creatorUsername threaded as opaque string via BirthOptions; orchestrator has 0 DB imports (Task 1 acceptance) |
| T-129-07-05 | Repudiation | Silent auto-tag decisions in prod | mitigate | Info log at successful auto-tag + warn log at skipped-lookup-failure (box-maintainer directive) |
| T-129-07-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
- Both suites (orchestrator + route handler) pass end-to-end.
- Byte-shape preservation locked via Test 1F golden-file assertion.
- Orchestrator stays free of DB imports (Task 1 grep assertion).
- identity-clone.ts confirmed unmodified (Task 2 grep assertion).
</verification>

<success_criteria>
- 14 new red-then-green tests total (7 per task).
- Auto-tag branch shipped for identity-birth; matches roles-create shape from Plan 129-06.
- Orchestrator purity preserved; only the route handler owns DB lookups.
- Case preservation + byte-shape preservation both locked.
</success_criteria>

<output>
Create `.planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-07-SUMMARY.md` listing: (a) test counts red-then-green for both files, (b) confirmation that identity-clone.ts is unmodified (v1 scope lock — flagged as follow-up in this summary for future phase reference), (c) confirmation that buildIdentityFileBody's yaml.dump call is byte-identical (golden-file Test 1F).
</output>
