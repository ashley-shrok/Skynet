---
phase: 129-multi-user-single-host-support-per-user-visibility-gate-on-r
plan: 08
type: execute
wave: 4
depends_on: [129-01, 129-02, 129-03, 129-04, 129-05, 129-06, 129-07]
files_modified: []
autonomous: true
requirements: []
must_haves:
  truths:
    - "npm run build:backend succeeds — every backend TS change from Plans 01-07 typechecks against the full backend project (frontend tsc --noEmit does NOT catch backend errors per box-maintainer directive)"
    - "npm run build succeeds — full vite build + backend tsc pass together, catching any cross-boundary type regression"
    - "The full phase-scoped vitest sweep passes: every touched test file from Plans 01-07 runs green in a single `npx vitest related --run` invocation covering all modified source files"
    - "Zero new eslint warnings introduced by Plans 01-07 (delta check via git diff)"
    - "The RESEARCH § no-leak audit table's 'YES' rows are ALL covered by a plan — verification via grep in this task ensures every enumerated surface has isIdentityVisibleToUser somewhere in its handler"
  artifacts:
    - path: ".planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-08-SUMMARY.md"
      provides: "Final phase-wide gate report: typecheck output, vitest counts per file, no-leak audit coverage matrix, eslint delta"
  key_links: []
---

<objective>
Final Wave-4 phase-wide gate task. Per box-maintainer standing directive: "Frontend `tsc --noEmit` doesn't catch backend TS errors. For any patch touching `src/backend/`, planner should include a pre-push typecheck step: `npm run build:backend && npm run build` — the strictest local check short of full docker build. This runs LOCAL in-plan, not as ship step."

Since Plans 01-07 collectively touch ~11 backend files (identity-appearance, identity-artifact-reader, identity-visibility-gate, host-user-counter, identities, sessions, roles-list-for-host, conversation-search, app-frame-filter, fleet-status-server, roles-create, identity-birth-orchestrator, identity-birth), the cross-file type contract MUST be verified before this phase is considered done.

This is NOT a ship task per box-maintainer directive — no `git push`, no `docker build`, no `docker compose up --force-recreate`, no HTTPS 200 verify, no docker logs sanity-check. Those are orchestrator-owned steps handled after phase completion.

Purpose: Catch any cross-plan integration regression before the orchestrator picks up the phase for shipping. Also verifies no-leak audit coverage — every "YES" row in RESEARCH gets a grep-hit for isIdentityVisibleToUser in the corresponding source file.

Output: A summary file documenting the gate results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-CONTEXT.md
@.planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-RESEARCH.md
@.planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-01-SUMMARY.md
@.planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-02-SUMMARY.md
@.planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-03-SUMMARY.md
@.planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-04-SUMMARY.md
@.planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-05-SUMMARY.md
@.planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-06-SUMMARY.md
@.planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run backend + full-project typecheck (npm run build:backend && npm run build)</name>
  <files></files>
  <read_first>
    - .planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-01-SUMMARY.md through 129-07-SUMMARY.md (verify every plan reported green tests and no assumption-violations)
    - package.json (verify build:backend and build scripts exist and their exact command shapes)
  </read_first>
  <acceptance_criteria>
    - behavior: `npm run build:backend` exits 0 with no TS errors.
    - behavior: `npm run build` (which chains vite build + backend tsc + copy) exits 0 with no TS errors.
    - behavior: Any TS error output is captured verbatim in the SUMMARY.md and the plan is BLOCKED — do NOT ship the phase until every reported error is fixed by revising the appropriate Plan 01-07 output. Fixes MUST target the specific plan whose source introduced the error (do NOT patch code from inside this plan — this plan is a gate, not an editor).
    - test-command: `npm run build:backend && npm run build` returns exit code 0.
  </acceptance_criteria>
  <action>
    Execute `npm run build:backend` from the repo root. Capture stdout+stderr. If exit code != 0, STOP and record the exact error output in the SUMMARY.md — DO NOT proceed to `npm run build`. DO NOT attempt to patch source code from inside this plan — this plan is a verification gate.

    If build:backend succeeds, execute `npm run build`. Capture stdout+stderr. Same policy: stop on failure, record verbatim, block the phase.

    If both succeed, record the final exit codes + summary of what was built in the SUMMARY.md as "Task 1 GREEN".

    Structured log per box-maintainer directive: not applicable — this task runs shell commands, not application code. The command outputs themselves are the diagnostic.

    NO other actions in this task. NO ship commands (no docker build, no docker compose up, no git push, no HTTPS verify). Ship is orchestrator-owned per box-maintainer standing directive.
  </action>
  <verify>
    <automated>npm run build:backend && npm run build</automated>
  </verify>
  <done>
    - Both build commands exit 0.
    - Output captured in SUMMARY.md.
    - No source edits made from inside this plan.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run phase-scoped vitest sweep across every file modified in Plans 01-07</name>
  <files></files>
  <read_first>
    - Every 129-NN-SUMMARY.md file (verify the union of files_modified across all seven plans)
    - The `files_modified` frontmatter of each plan file 129-01-PLAN.md through 129-07-PLAN.md (source of truth for the sweep target list)
  </read_first>
  <acceptance_criteria>
    - behavior: `npx vitest related --run <every touched source file from Plans 01-07>` exits 0 with all tests passing across the union.
    - behavior: The pass count matches the sum of per-plan pass counts reported in each 129-NN-SUMMARY.md — no test dropped from the sweep due to a related-flag miss.
    - test-command: The full sweep command is recorded in SUMMARY.md verbatim so it can be re-run by ops.
  </acceptance_criteria>
  <action>
    Build the sweep command by concatenating the files_modified from Plans 01-07:

    Source files:
    - src/backend/fleet-status/identity-appearance.ts
    - src/backend/fleet-status/identity-visibility-gate.ts
    - src/backend/claude-session/identity-artifact-reader.ts
    - src/backend/utils/host-user-counter.ts
    - src/backend/database/routes/identities.ts
    - src/backend/database/routes/sessions.ts
    - src/backend/database/routes/roles-list-for-host.ts
    - src/backend/database/routes/conversation-search.ts
    - src/backend/fleet-status/app-frame-filter.ts
    - src/backend/fleet-status/fleet-status-server.ts
    - src/backend/database/routes/roles-create.ts
    - src/backend/database/routes/identity-birth-orchestrator.ts
    - src/backend/database/routes/identity-birth.ts

    Test files (the `related` flag maps source to test but explicit inclusion ensures nothing is missed):
    - src/backend/fleet-status/identity-visibility-gate.test.ts (NEW)
    - src/backend/utils/host-user-counter.test.ts (NEW)
    - src/backend/claude-session/identity-artifact-reader.users.test.ts (NEW, or the existing test file the narrower's test cases landed in per Plan 01 Task 1 decision)
    - src/backend/database/routes/identities.get-disk.test.ts
    - src/backend/database/routes/sessions.test.ts
    - src/backend/database/routes/roles-list-for-host.test.ts
    - src/backend/database/routes/conversation-search.test.ts
    - src/backend/fleet-status/app-frame-filter.test.ts
    - src/backend/database/routes/roles-create.test.ts
    - src/backend/database/routes/identity-birth-orchestrator.test.ts
    - src/backend/database/routes/identity-birth.test.ts

    Run `npx vitest related --run <all source files above>` as ONE command. Capture stdout+stderr + pass/fail counts.

    Verify the totals match the sum reported across 129-01-SUMMARY.md through 129-07-SUMMARY.md. If the sweep count is lower, investigate — a missing test file means the related flag didn't map the source correctly, and the missing tests should be added explicitly.

    Record the final sweep command + total pass count in SUMMARY.md.

    Same policy as Task 1: DO NOT patch source code from this plan. Block on failure; report the failing test to the responsible Plan 01-07 for revision.

    NO ship commands.
  </action>
  <verify>
    <automated>npx vitest related --run src/backend/fleet-status/identity-appearance.ts src/backend/fleet-status/identity-visibility-gate.ts src/backend/claude-session/identity-artifact-reader.ts src/backend/utils/host-user-counter.ts src/backend/database/routes/identities.ts src/backend/database/routes/sessions.ts src/backend/database/routes/roles-list-for-host.ts src/backend/database/routes/conversation-search.ts src/backend/fleet-status/app-frame-filter.ts src/backend/fleet-status/fleet-status-server.ts src/backend/database/routes/roles-create.ts src/backend/database/routes/identity-birth-orchestrator.ts src/backend/database/routes/identity-birth.ts</automated>
  </verify>
  <done>
    - Full sweep exits 0 with pass count matching the union of per-plan reported counts.
    - Sweep command recorded verbatim in SUMMARY.md.
  </done>
</task>

<task type="auto">
  <name>Task 3: No-leak audit coverage matrix — grep verification</name>
  <files>.planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-08-SUMMARY.md</files>
  <read_first>
    - .planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-RESEARCH.md § "Complete no-leak audit — every surface that exposes identity or role names" (the source-of-truth table)
  </read_first>
  <acceptance_criteria>
    - source: For each "YES" row in the no-leak audit table, `grep -c "isIdentityVisibleToUser" <file>` returns at least 1 hit. Exact surfaces to verify (must have ≥ 1 grep hit each):
      - `src/backend/database/routes/identities.ts` (from Plan 129-02)
      - `src/backend/database/routes/sessions.ts` (from Plan 129-03)
      - `src/backend/database/routes/roles-list-for-host.ts` (from Plan 129-03)
      - `src/backend/database/routes/conversation-search.ts` (from Plan 129-04)
      - `src/backend/fleet-status/fleet-status-server.ts` (Plan 129-05 wires the closure; app-frame-filter.ts calls canUserSeeIdentity → resolveIdentityGate → composed closure)
    - source: For each "NO" row explicitly documented in the audit (`GET /identities/:identityKey/avatar`, `PUT /identities/:identityKey`, `POST /identities/clone`, `POST /identities/:key/archive`, `GET /roles/:name/avatar?hostId=<n>`, `POST /roles/:name/avatar?hostId=<n>`, `POST /agent-reset`, `WS app-*` frames, `WS pong`), the SUMMARY.md documents the "NO" rationale (matches the audit table's justification — no plan required, follow-up flagged for /agent-reset if wanted).
    - source: For each auto-tag write site, `grep -c "isHostMultiUser" <file>` returns at least 1 hit:
      - `src/backend/database/routes/roles-create.ts` (from Plan 129-06)
      - `src/backend/database/routes/identity-birth.ts` (from Plan 129-07)
    - source: `grep -c "isHostMultiUser\|getUsernameForUserId\|isIdentityVisibleToUser" src/backend/database/routes/identity-clone.ts` returns 0 hits (v1 scope lock — clone auto-tag flagged as follow-up, NOT included in this phase per Plan 129-07 Task 2).
  </acceptance_criteria>
  <action>
    Iterate the no-leak audit table from RESEARCH.md § "Complete no-leak audit". For each "YES" row, run `grep -c "isIdentityVisibleToUser" <file>` and record the count. For each "NO" row, verify the audit's rationale is still valid (nothing new since RESEARCH date) and record the rationale in the SUMMARY.

    Build a coverage matrix in SUMMARY.md as a markdown table:
    ```
    | Surface | File | Audit Disposition | Grep Result | Plan |
    |---------|------|-------------------|-------------|------|
    | GET /identities | identities.ts | YES | 1 hit ✓ | 129-02 |
    | ...             | ...           | ... | ...      | ...   |
    ```

    Explicitly list the "NO — deferred" rows too (identity-clone.ts auto-tag; /agent-reset if not covered; potentially project-list-changed WS frame if plan-time enumeration didn't add it to Plan 05's gated list). Note whether each is intentionally deferred per shape/RESEARCH, or missed and needs follow-up.

    Verify the auto-tag write sites: `grep -c "isHostMultiUser" src/backend/database/routes/roles-create.ts` and `grep -c "isHostMultiUser" src/backend/database/routes/identity-birth.ts` — both must be ≥ 1.

    Verify identity-clone.ts exclusion: `grep -c "isHostMultiUser\|getUsernameForUserId\|isIdentityVisibleToUser" src/backend/database/routes/identity-clone.ts` must be 0 (v1 scope lock).

    If any required grep returns 0 unexpectedly, STOP — report the gap in SUMMARY.md as a BLOCKING issue and route back to the responsible plan for revision. Do NOT patch source from this plan.
  </action>
  <verify>
    <automated>bash -c 'set -eu; for f in src/backend/database/routes/identities.ts src/backend/database/routes/sessions.ts src/backend/database/routes/roles-list-for-host.ts src/backend/database/routes/conversation-search.ts src/backend/fleet-status/fleet-status-server.ts; do c=$(grep -c "isIdentityVisibleToUser" "$f" 2>/dev/null || echo 0); if [ "$c" -lt 1 ]; then echo "FAIL: $f has 0 isIdentityVisibleToUser hits"; exit 1; fi; done; for f in src/backend/database/routes/roles-create.ts src/backend/database/routes/identity-birth.ts; do c=$(grep -c "isHostMultiUser" "$f" 2>/dev/null || echo 0); if [ "$c" -lt 1 ]; then echo "FAIL: $f has 0 isHostMultiUser hits"; exit 1; fi; done; c=$(grep -c "isHostMultiUser\|getUsernameForUserId\|isIdentityVisibleToUser" src/backend/database/routes/identity-clone.ts 2>/dev/null || echo 0); if [ "$c" -ne 0 ]; then echo "FAIL: identity-clone.ts has unexpected Phase 129 hits ($c) — v1 scope lock violated"; exit 1; fi; echo "OK: all no-leak audit YES rows covered; clone.ts confirmed excluded"'</automated>
  </verify>
  <done>
    - Every "YES" row in the no-leak audit has ≥ 1 grep hit in its target file.
    - "NO — deferred" rows documented with rationale in SUMMARY.
    - Auto-tag write sites verified.
    - identity-clone.ts explicit exclusion verified.
    - Coverage matrix written to SUMMARY.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local shell → npm scripts | Sandboxed dev env; no network exposure |
| Grep verification → source files | Read-only inspection; no writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-08-01 | Repudiation | Silent typecheck failure ships broken phase | mitigate | Block on non-zero exit code from npm run build:backend or npm run build; report verbatim to SUMMARY |
| T-129-08-02 | Info Disclosure | No-leak audit gap silently ships (a "YES" surface missing its gate) | mitigate | Task 3 grep matrix + bash-verify one-liner catches every missing hit at gate-time |
| T-129-08-03 | Tampering | Ship-side action leaks into this plan (docker build / deploy) | mitigate | Explicit box-maintainer directive citation at plan header; no docker/git-push/HTTPS-verify commands anywhere in this file |
| T-129-08-SC | Tampering | npm installs during build | accept | Standard project deps only; no new packages installed by Plans 01-07 per each plan's threat model |
</threat_model>

<verification>
- All three tasks GREEN.
- SUMMARY.md documents typecheck output, sweep count, coverage matrix.
- No ship commands executed anywhere in this plan.
</verification>

<success_criteria>
- npm run build:backend + npm run build both exit 0.
- Full phase-scoped vitest sweep passes (count matches per-plan totals).
- No-leak audit coverage matrix documented with every "YES" surface grep-verified.
- identity-clone.ts exclusion locked (v1 scope).
- SUMMARY.md produced.
</success_criteria>

<output>
Create `.planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-08-SUMMARY.md` containing:
- Task 1 output: exact stdout of `npm run build:backend` and `npm run build`.
- Task 2 output: exact command run + total tests / pass / fail / duration.
- Task 3 output: coverage matrix markdown table + confirmation that identity-clone.ts is unmodified.
- Explicit note: "Phase 129 is CODE-COMPLETE. Ship is orchestrator-owned per box-maintainer standing directive — no docker build / no docker compose up / no HTTPS verify performed from any executor plan."
</output>
