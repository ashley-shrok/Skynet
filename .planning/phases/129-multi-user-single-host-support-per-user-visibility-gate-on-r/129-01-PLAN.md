---
phase: 129-multi-user-single-host-support-per-user-visibility-gate-on-r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/fleet-status/identity-appearance.ts
  - src/backend/fleet-status/identity-visibility-gate.ts
  - src/backend/fleet-status/identity-visibility-gate.test.ts
  - src/backend/claude-session/identity-artifact-reader.ts
  - src/backend/utils/host-user-counter.ts
  - src/backend/utils/host-user-counter.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "RawCosmetics carries an optional users: string[] field parseable from role and identity frontmatter (D-10)"
    - "extractCosmeticsFromFrontmatter normalizes users: entries (trim + drop non-strings + drop empties) and stays absent-when-empty (D-3, D-10)"
    - "isIdentityVisibleToUser returns true iff BOTH role.users gate AND identity.users gate pass, with case-sensitive comparison against the caller username (D-2)"
    - "Empty/absent users on either side means 'no gate on that side' (D-3 fallback rule)"
    - "callerUsername === null disables the gate entirely (true) — internal-server / test bypass"
    - "isHostMultiUser returns true iff strictly more than one distinct Skynet user has access to the host (owner + direct-user hostAccess + hostAccess.roleId-scoped members via userRoles) (D-4)"
    - "getUsernameForUserId returns the DB row's username (case-preserved) for a given userId, or null when the row is absent"
  artifacts:
    - path: "src/backend/fleet-status/identity-visibility-gate.ts"
      provides: "Pure isIdentityVisibleToUser(identityCos, roleCos, callerUsername) — companion to resolveIdentityAppearance"
      exports: ["isIdentityVisibleToUser"]
    - path: "src/backend/utils/host-user-counter.ts"
      provides: "isHostMultiUser + getUsernameForUserId shared DB helpers for create endpoints AND gate call sites"
      exports: ["isHostMultiUser", "getUsernameForUserId"]
    - path: "src/backend/fleet-status/identity-visibility-gate.test.ts"
      provides: "Full pure-fn matrix test for the intersection gate"
    - path: "src/backend/utils/host-user-counter.test.ts"
      provides: "Multi-user detection tests covering owner-only, direct-user share, RBAC-role share, absent host"
  key_links:
    - from: "identity-appearance.ts"
      to: "identity-visibility-gate.ts"
      via: "shared RawCosmetics type import"
      pattern: "import type \\{ RawCosmetics \\} from \"\\./identity-appearance"
    - from: "identity-artifact-reader.ts extractCosmeticsFromFrontmatter"
      to: "RawCosmetics.users? field"
      via: "narrowing block emits out.users when array-of-non-empty-strings"
      pattern: "out\\.users = normalized"
---

<objective>
Ship the four foundational primitives every downstream Phase 129 plan depends on:

1. Extend `RawCosmetics` (identity-appearance.ts) with the optional `users?: string[]` field per D-10 (field name locked) + D-3 (absent-⇒-omit fallback).
2. Extend `extractCosmeticsFromFrontmatter` (identity-artifact-reader.ts) with a narrowing block that parses `users:` entries as strings, trims/filters, and emits absent when empty (preserves zero-migration invariant).
3. Create `identity-visibility-gate.ts` — the pure companion `isIdentityVisibleToUser(identityCos, roleCos, callerUsername)` function implementing D-2 intersection semantics with the D-3 fallback rule. Uses case-sensitive comparison (RESEARCH Pitfall 7 lock).
4. Create `host-user-counter.ts` — shared DB helpers `isHostMultiUser(hostId)` (implements D-4 multi-user detection, expanding RBAC-role grants per Assumption A6) + `getUsernameForUserId(userId)` (JWT gives userId only, per Pitfall 6).

**Design lock (per RESEARCH Open Question 1 + PATTERNS.md recommendation):** DO NOT widen `resolveIdentityAppearance`'s signature. The gate lives in a companion pure function every caller invokes SEPARATELY. Preserves Phase 111 T-111-08 "one cascade authority" invariant AND makes "did we gate this call site?" an audit-visible grep for `isIdentityVisibleToUser(`.

Purpose: Every wave-2 read-path plan and every wave-3 write-path plan needs these four primitives. Shipping them together in Wave 1 lets the wave-2 plans run in parallel against a stable, tested API.

Output: Two new pure/util modules (both with tests), one type-extended in-place, one narrower-extended in-place, and zero touches to any route/orchestrator code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-CONTEXT.md
@.planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-RESEARCH.md
@.planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-PATTERNS.md
@src/backend/fleet-status/identity-appearance.ts
@src/backend/fleet-status/identity-appearance.test.ts
@src/backend/database/db/schema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend RawCosmetics + extractCosmeticsFromFrontmatter narrowing block</name>
  <files>src/backend/fleet-status/identity-appearance.ts, src/backend/claude-session/identity-artifact-reader.ts</files>
  <read_first>
    - src/backend/fleet-status/identity-appearance.ts (full file, ~222 lines — the RawCosmetics type is at L31-47 alongside project?: string)
    - src/backend/claude-session/identity-artifact-reader.ts L3128-3236 ONLY (extractCosmeticsFromFrontmatter — return-type declaration + narrowing blocks)
    - src/backend/claude-session/identity-artifact-reader.ts L428-446 (LOAD-BEARING CONSTRAINT comment — DO NOT use extractor as round-tripper; write side must go through full yaml.load/yaml.dump)
    - .planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-PATTERNS.md § "identity-appearance.ts" and § "identity-artifact-reader.ts"
  </read_first>
  <behavior>
    - Test 1: A markdown file with frontmatter containing `users: [user]` parses to `{users: ["user"]}` via extractCosmeticsFromFrontmatter.
    - Test 2: A markdown file with `users: [user, zoe]` parses to `{users: ["user", "zoe"]}` preserving order.
    - Test 3: A markdown file WITHOUT a `users` key parses to a RawCosmetics object where `users` is absent (NOT `[]`, NOT null, NOT `undefined` as a set key — the key does not exist on the returned object per the absent-⇒-omit invariant).
    - Test 4: `users: []` (empty list) also yields absent — normalization drops empty arrays to preserve the fallback semantic.
    - Test 5: `users: [user, "", "   ", 42, null]` normalizes to `["user"]` (trim + drop-non-string + drop-empty per PATTERNS.md narrowing block).
    - Test 6: `users:` with a scalar (not a list) — e.g. `users: user` — yields absent (Array.isArray gate rejects non-arrays; matches how `project` narrower behaves for wrong-type input).
    - Test 7: RawCosmetics type extension is source-visible — a TS consumer can write `const c: RawCosmetics = { users: ["user"] }` without a compile error.
  </behavior>
  <action>
    Edit identity-appearance.ts at the RawCosmetics type block (L31-47): add `users?: string[]` as a new optional field alongside `project?: string`. Include a JSDoc block noting: "Phase 129: per-user visibility gate — YAML list of Skynet usernames. Empty/absent = 'no gate on this side' (D-3 fallback). Case-sensitive comparison, matching DB users.username storage discipline (users.ts L172 eq(users.username, username))." Do NOT change the resolveIdentityAppearance signature — the gate lives in a companion pure module (see Task 3). Do NOT alter any existing field or the merge cascade body per D-8 (visibility filter, not permission system) and per Phase 111 T-111-08 single-cascade invariant.

    Edit identity-artifact-reader.ts extractCosmeticsFromFrontmatter (L3128-3236 range): add `users?: string[]` to the return-type declaration (near the project?: string field). Add a narrowing block near L3232 (end of function, before `return out`) that mirrors the array-of-strings shape from PATTERNS.md § "identity-artifact-reader.ts": call `Array.isArray(src.users)`, filter to `typeof === "string"`, trim, drop empties, and emit `out.users = normalized` only when `normalized.length > 0` (this preserves the absent-⇒-omit fallback per D-3). Do NOT touch the fail-open L3162-3168 branch (YAML parse failure returns {}). Do NOT expose `users` in any other narrower or extractor in this file — this is the ONE read seam.

    Follow TDD: write behavior tests 1-7 first as a new file `src/backend/claude-session/identity-artifact-reader.users.test.ts` (mirroring the shape of any existing `identity-artifact-reader.*.test.ts` files — create a minimal test scaffold if none exists), watch them fail, then implement the two edits above until they pass.

    Structured logging per box-maintainer directive: NOT required for this pure-parse task (no lifecycle boundary crossed; the narrower is a synchronous helper). No hostId/identityKey/userId decision is made here.
  </action>
  <verify>
    <automated>npx vitest related --run src/backend/claude-session/identity-artifact-reader.users.test.ts src/backend/fleet-status/identity-appearance.ts</automated>
  </verify>
  <acceptance_criteria>
    - behavior: All 7 test cases from &lt;behavior&gt; pass.
    - source: `grep -n "users?: string\[\]" src/backend/fleet-status/identity-appearance.ts` returns exactly 1 hit inside the RawCosmetics type block.
    - source: `grep -n "users?: string\[\]" src/backend/claude-session/identity-artifact-reader.ts` returns exactly 1 hit inside the extractCosmeticsFromFrontmatter return-type declaration.
    - source: `grep -n "out\.users = normalized" src/backend/claude-session/identity-artifact-reader.ts` returns exactly 1 hit inside the narrowing block (proves the absent-⇒-omit branch is present).
    - source: `grep -c "resolveIdentityAppearance(args:" src/backend/fleet-status/identity-appearance.ts` returns 1 (signature unchanged — Phase 111 T-111-08 preserved).
    - source: `grep -c "callerUsername" src/backend/fleet-status/identity-appearance.ts` returns 0 (gate does NOT live inside the cascade — Design Lock).
  </acceptance_criteria>
  <done>
    - RawCosmetics has users?: string[] field with JSDoc citing D-10 + D-3.
    - extractCosmeticsFromFrontmatter parses + normalizes users: entries and honors absent-⇒-omit.
    - Test file passes and covers the 7 behavior cases.
    - resolveIdentityAppearance signature and body are unchanged (design lock).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create identity-visibility-gate.ts pure module + full-matrix tests</name>
  <files>src/backend/fleet-status/identity-visibility-gate.ts, src/backend/fleet-status/identity-visibility-gate.test.ts</files>
  <read_first>
    - src/backend/fleet-status/identity-appearance.ts L1-50 (imports discipline + RawCosmetics type — the ONLY import this new module makes)
    - src/backend/fleet-status/identity-appearance.test.ts L1-50 (test file scaffold pattern — mirror vitest imports and fixture-builder shape)
    - .planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-PATTERNS.md § "identity-visibility-gate.ts (NEW)" (verbatim body pattern to use)
    - .planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-RESEARCH.md § "Intersection gate as a pure function" + § "Common Pitfall 7"
    - .planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-CONTEXT.md § "Locked decisions" bullets 2, 3
  </read_first>
  <behavior>
    - Test 1 (null caller — gate disabled): `isIdentityVisibleToUser(anything, anything, null)` returns true regardless of the two cosmetics args. Covers internal-server / test bypass path per PATTERNS.md file-header discipline.
    - Test 2 (both sides empty/absent → open per D-3): `isIdentityVisibleToUser({}, {}, "user")` returns true. Same for `isIdentityVisibleToUser(null, null, "user")`. Same for `{users: []}` on both sides.
    - Test 3 (both sides list the user → open for the user): `isIdentityVisibleToUser({users:["user"]}, {users:["user"]}, "user")` returns true.
    - Test 4 (both sides list the user → CLOSED for Zoe — D-2 intersection): `isIdentityVisibleToUser({users:["user"]}, {users:["user"]}, "zoe")` returns false.
    - Test 5 (role open, identity narrow → CLOSED for non-matching user): `isIdentityVisibleToUser({users:["user"]}, {}, "zoe")` returns false. Identity gate closes for Zoe even though role is open.
    - Test 6 (role narrow, identity open → CLOSED for non-matching user): `isIdentityVisibleToUser({}, {users:["user"]}, "zoe")` returns false. Role gate closes for Zoe even though identity is open.
    - Test 7 (shared role, identity-side narrow to the user → CLOSED for Zoe): `isIdentityVisibleToUser({users:["user"]}, {users:["user","zoe"]}, "zoe")` returns false. Proves intersection semantic — a shared role can contain identities narrowed to a single user (matches shape file "shared role can still contain identities that only one of them sees").
    - Test 8 (shared role AND shared identity → open for both users): `isIdentityVisibleToUser({users:["user","zoe"]}, {users:["user","zoe"]}, "user")` returns true AND `isIdentityVisibleToUser({users:["user","zoe"]}, {users:["user","zoe"]}, "zoe")` returns true.
    - Test 9 (case sensitivity per Pitfall 7): `isIdentityVisibleToUser({users:["the user"]}, {users:["the user"]}, "user")` returns false (case-sensitive comparison locked). Documented as intended behavior per D-2 + RESEARCH Assumption A2.
    - Test 10 (null cosmetics on either side): `isIdentityVisibleToUser(null, {users:["user"]}, "user")` returns true (null identityCos treated as "no gate"); `isIdentityVisibleToUser({users:["user"]}, null, "zoe")` returns false (identity gate still closes on Zoe).
  </behavior>
  <action>
    Create `src/backend/fleet-status/identity-visibility-gate.ts` verbatim from PATTERNS.md § "identity-visibility-gate.ts (NEW)" file-body pattern. The module:
    - imports `type { RawCosmetics } from "./identity-appearance.js"` ONLY (no DB, no express, no zod, no logger — preserves circular-import safety AND the "route layer and fleet-status can depend on it without a cycle" discipline documented in PATTERNS.md).
    - exports one named function `isIdentityVisibleToUser(identityCosmetics: RawCosmetics | null, roleCosmetics: RawCosmetics | null, callerUsername: string | null): boolean`.
    - Body implements D-2 intersection semantics with the D-3 fallback rule using `Array.isArray(list) === false || list.length === 0 || list.includes(callerUsername)` per side.
    - `callerUsername === null` short-circuits to `return true` (internal bypass).
    - File-header JSDoc cites D-2, D-3, D-8 (visibility filter, not permission system), and Pitfall 7 (case-sensitive lock).

    Follow TDD: write `identity-visibility-gate.test.ts` covering the 10 behavior cases from &lt;behavior&gt; FIRST (mirror `identity-appearance.test.ts`'s vitest import shape + describe/it structure — NO makeArgs builder needed, this is a 3-arg pure function). Watch tests fail, then implement the module until all 10 pass.

    Structured logging: NOT required — pure function, no lifecycle boundary. If the caller wants to log a gate decision at the seam, it logs from the seam (Wave 2/3 plans).
  </action>
  <verify>
    <automated>npx vitest related --run src/backend/fleet-status/identity-visibility-gate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - behavior: All 10 test cases pass.
    - source: `grep -c "import" src/backend/fleet-status/identity-visibility-gate.ts` returns exactly 1 (the RawCosmetics type import only — no DB, no logger, no express).
    - source: `grep -n "callerUsername === null" src/backend/fleet-status/identity-visibility-gate.ts` returns exactly 1 hit (null-bypass branch present).
    - source: `grep -n "\.toLowerCase()\|\.toUpperCase()" src/backend/fleet-status/identity-visibility-gate.ts` returns 0 hits (case-sensitive comparison — Pitfall 7 lock).
    - source: `grep -c "export function isIdentityVisibleToUser" src/backend/fleet-status/identity-visibility-gate.ts` returns 1.
  </acceptance_criteria>
  <done>
    - Pure module ships with 10 passing tests covering the full [callerNull × emptyLists × userOnBoth × userOnRoleOnly × userOnIdOnly × zoeOnBothUserEmpty × case-sensitivity × nullCosmetics] matrix.
    - Zero DB / express / logger imports — reusable by both fleet-status and database/routes layers.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create host-user-counter.ts (isHostMultiUser + getUsernameForUserId) + tests</name>
  <files>src/backend/utils/host-user-counter.ts, src/backend/utils/host-user-counter.test.ts</files>
  <read_first>
    - src/backend/utils/permission-manager.ts L162-260 (canAccessHost — this is the AUTHORITATIVE host-access predicate; multi-user detection MUST match its shape per RESEARCH Assumption A6 and no-leak audit lock. The predicate expands hostAccess.roleId via userRoles, so isHostMultiUser must too.)
    - src/backend/database/db/schema.ts L586-601 (userRoles table — junction from users→roles used to expand roleId-based hostAccess grants)
    - src/backend/database/db/schema.ts L500-530 (hostAccess table — carries userId OR roleId; both count toward "distinct users with access")
    - src/backend/database/db/schema.ts L12-40 (users table — username column, stored as-typed at register per users.ts L172)
    - src/backend/database/db/schema.ts L103-140 (hosts table — hosts.userId is the owner)
    - .planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-PATTERNS.md § "host-user-counter.ts (NEW)"
    - .planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-RESEARCH.md § "Multi-user detection helper" + Assumption A6 (RBAC-role-expansion IS in scope for this phase)
  </read_first>
  <behavior>
    - Test 1 (isHostMultiUser: unknown host): `isHostMultiUser(9999)` returns false when the hostId does not exist in the hosts table. Prevents auto-tag on a phantom host.
    - Test 2 (isHostMultiUser: owner-only, no shares): host with hosts.userId=U1 and NO hostAccess rows returns false. This is the majority case — a single-user host must never trigger auto-tag (D-4 + shape §"What would make it wrong" bullet 1: single-user hosts see no evidence of the feature).
    - Test 3 (isHostMultiUser: direct-user share): host with hosts.userId=U1 AND hostAccess row {hostId=h, userId=U2, roleId=null} returns true (U1 + U2 = 2 distinct users).
    - Test 4 (isHostMultiUser: owner-shared-back-to-owner is not double-counted): host with hosts.userId=U1 AND hostAccess row {hostId=h, userId=U1} returns false — Set dedup collapses (defensive per PATTERNS.md).
    - Test 5 (isHostMultiUser: RBAC-role share — RESEARCH Assumption A6): host with hosts.userId=U1 AND hostAccess row {hostId=h, userId=null, roleId=R2} where userRoles has {userId=U2, roleId=R2} returns true (owner U1 + role-member U2 = 2). This is load-bearing — RESEARCH flagged that skipping role-expansion would make the user+Zoe's shared t1000 auto-tag stay silent when they share via an RBAC role.
    - Test 6 (isHostMultiUser: RBAC-role share with the owner as sole member): host with hosts.userId=U1 AND hostAccess row {hostId=h, roleId=R1} where userRoles has {userId=U1, roleId=R1} returns false (only U1 across owner + role expansion).
    - Test 7 (isHostMultiUser: two overlapping shares — one direct + one via role): host with hosts.userId=U1 AND hostAccess rows {userId=U2} AND {roleId=R2 → userRoles has userId=U2} returns true (U1 + U2, dedup) — count is 2, not 3.
    - Test 8 (getUsernameForUserId: known user): returns the exact case-preserved username string ("the user" stays "the user" — Pitfall 7 lock).
    - Test 9 (getUsernameForUserId: unknown userId): returns null. Defensive path — a JWT-authenticated userId that lacks a users row is a shape-violation but must not throw.
  </behavior>
  <action>
    Create `src/backend/utils/host-user-counter.ts` following the PATTERNS.md § "host-user-counter.ts (NEW)" body pattern BUT extend `isHostMultiUser` to also expand `hostAccess.roleId → userRoles → user IDs` per RESEARCH Assumption A6. Specifically:

    - Query 1: `SELECT userId FROM hostAccess WHERE hostId=?` — collect direct-user shares.
    - Query 2: `SELECT roleId FROM hostAccess WHERE hostId=? AND roleId IS NOT NULL` — collect role-scoped shares.
    - Query 3: For any collected roleIds, `SELECT userId FROM userRoles WHERE roleId IN (...)` — expand to member userIds.
    - Query 4: `SELECT userId FROM hosts WHERE id=? LIMIT 1` — get owner.
    - Build a `Set<string>` starting with owner (return false if unknown host), add every non-null direct-user share userId, add every role-expanded userId. Return `set.size > 1`.
    - IMPORTANT: `hostAccess.expiresAt` filter is NOT part of the multi-user count per shape §"Auto-tag scope" (auto-tag is a snapshot-at-creation decision; expiring shares still make the host multi-user at that moment — a fresh role/identity gets tagged with creator's username, and if a share later expires, the file stays as-is). Log this decision in the file's JSDoc.

    Export two functions: `isHostMultiUser(hostId: number): Promise<boolean>` and `getUsernameForUserId(userId: string): Promise<string | null>`. Both use `db` from `../database/db/index.js` and drizzle-orm `eq` / `inArray` where needed.

    Follow TDD: write `host-user-counter.test.ts` first using vitest's `vi.mock` on the db module (mirror `permission-manager.test.ts` scaffold pattern). Write the 9 behavior tests, watch them fail, then implement. The test file uses fake in-memory rows keyed to fixture IDs so the tests are hermetic and deterministic (no real SQLite required).

    Structured logging per box-maintainer directive: add a `systemLogger.debug` entry at the top of `isHostMultiUser` with `{operation: "host_user_counter_check", hostId}` and one at the end with `{operation: "host_user_counter_result", hostId, distinctUsers: set.size, isMultiUser: result}`. This is a gate-seam decision — write the diagnostic hostId + count at every check so failing auto-tags are traceable in prod logs. Import `systemLogger` from `./logging.js` (verify path via `grep -l "systemLogger" src/backend/utils/*.ts` if unclear).
  </action>
  <verify>
    <automated>npx vitest related --run src/backend/utils/host-user-counter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - behavior: All 9 test cases pass.
    - source: `grep -c "export async function isHostMultiUser" src/backend/utils/host-user-counter.ts` returns 1.
    - source: `grep -c "export async function getUsernameForUserId" src/backend/utils/host-user-counter.ts` returns 1.
    - source: `grep -n "userRoles" src/backend/utils/host-user-counter.ts` returns at least 1 hit (proves RBAC-role expansion is present — Assumption A6 lock).
    - source: `grep -n "systemLogger.debug\|systemLogger.info" src/backend/utils/host-user-counter.ts` returns at least 1 hit (structured log at gate seam per box-maintainer directive).
    - source: `grep -n "expiresAt" src/backend/utils/host-user-counter.ts` returns 0 hits (expiresAt filter deliberately excluded per shape §"Auto-tag scope" — snapshot semantic).
  </acceptance_criteria>
  <done>
    - Two exported helpers with 9 passing tests.
    - Multi-user detection matches permission-manager.canAccessHost's role-expansion shape (Assumption A6 landed).
    - Structured logging at gate seam per box-maintainer directive.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontmatter file on disk → parser | Frontmatter authored manually / by orchestrator; parser must narrow to string arrays |
| JWT userId → username DB lookup | Auth middleware guarantees userId is a real JWT subject; DB row may be absent (defensive path) |
| hostAccess table → multi-user count | Data integrity constraint; roleId expansion via userRoles is the authoritative pattern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-01-01 | Tampering | extractCosmeticsFromFrontmatter users? narrowing | mitigate | Array.isArray + typeof === "string" + trim + drop empties; narrowing block rejects scalars, nulls, non-strings per Test 5/6 |
| T-129-01-02 | Info Disclosure | isIdentityVisibleToUser gate logic | mitigate | Pure function; no side effects; case-sensitive comparison prevents accidental case-mismatch false positives (Pitfall 7). Tested against full 10-case matrix. |
| T-129-01-03 | Tampering | isHostMultiUser RBAC-role expansion | mitigate | Expand hostAccess.roleId via userRoles per Assumption A6; matches permission-manager.canAccessHost's authoritative shape. Test 5/6/7 lock behavior. |
| T-129-01-04 | Repudiation | Multi-user detection failure silently skips auto-tag | mitigate | Structured log at isHostMultiUser entry + exit with hostId + distinctUsers count per box-maintainer directive; ops can trace missed auto-tags in prod logs. |
| T-129-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase per RESEARCH § Package Legitimacy Audit |
</threat_model>

<verification>
- All three tasks' vitest suites pass (scoped runs).
- No accidental resolveIdentityAppearance signature change (grep verification in Task 1 acceptance).
- No accidental case-insensitive comparison in the gate (grep verification in Task 2 acceptance).
- No accidental omission of RBAC-role expansion in the counter (grep verification in Task 3 acceptance).
</verification>

<success_criteria>
- 4 new/modified source files ship with 26 total new tests (7 + 10 + 9) passing.
- RawCosmetics extended with `users?: string[]`.
- extractCosmeticsFromFrontmatter parses + normalizes the new field with absent-⇒-omit fallback.
- Pure gate module (identity-visibility-gate.ts) exports `isIdentityVisibleToUser` with zero DB/express/logger imports.
- DB helpers (host-user-counter.ts) export `isHostMultiUser` (RBAC-role-aware) and `getUsernameForUserId` with structured logging.
- Zero touches to routes, orchestrators, or WS filter (those come in waves 2/3).
</success_criteria>

<output>
Create `.planning/phases/129-multi-user-single-host-support-per-user-visibility-gate-on-r/129-01-SUMMARY.md` when done, listing: (a) exact final signatures of the 4 exports, (b) test count and pass/fail state, (c) any assumption changes vs RESEARCH (e.g., discovered edge case in permission-manager.ts), (d) files modified with line-count deltas.
</output>
