---
phase: 65-wake-up-days-gate-humanize-form-editor-round-trip-preservati
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/claude-session/identity-artifact-reader.ts
  - src/backend/claude-session/identity-artifact-reader.humanize-wakeup.test.ts
autonomous: true
requirements: []
tags: [wakeup, humanizer, days-gate, identity-artifact-reader]

must_haves:
  truths:
    - "D-02 · humanizer treats a full-7 `days` array (any order, deduplicated) as no-gate — renders identically to a spec with no `days` field"
    - "D-03 · humanizer renders subset days in canonical mon→sun order regardless of the input order"
    - "D-05 · humanizer renders any 1..6-day subset that is not weekdays or weekends as capitalized 3-letter abbreviations joined with `/` (e.g. `Mon/Wed/Fri at 23:00 (box-local)`)"
    - "humanizer renders the exact weekdays set `{mon,tue,wed,thu,fri}` as `Weekdays at <time> (box-local)` on daily and as `Weekdays every <interval>` on interval"
    - "humanizer renders the exact weekends set `{sat,sun}` as `Weekends at <time> (box-local)` on daily and as `Weekends every <interval>` on interval"
    - "D-01 · weekly + days gate: when `day ∈ days`, humanizer renders days-gate-substituted form (e.g. `Weekdays at 09:00 (box-local)`) and drops the redundant `on <Day>`; when `day ∉ days` (malformed / NEVER FIRES case), humanizer renders `Weekly on <Day> at <time> (box-local) — NEVER FIRES (weekly day excluded from days gate)`"
    - "D-05/D-07 · humanizer tolerates malformed `s.days` values (non-array, non-string entries, uppercase, whitespace, duplicates, unknown 3-letter codes) without throwing — filters defensively through a weekday-guard"
    - "backwards compat (Success Criteria #5) · a wakeup spec WITHOUT `days` (or with `days: []`) renders identically to today: `Daily at 23:00 (box-local)`, `Every 2h`, `Weekly on Mon at 09:00 (box-local)`, `custom schedule`"
    - "one_shot + `days` combination is treated as no-gate: one_shot render is unchanged even if a `days` array is present"
  artifacts:
    - path: "src/backend/claude-session/identity-artifact-reader.ts"
      provides: "extended humanizeWakeupSchedule at L54-81 with days-gate awareness on interval/daily/weekly branches; new internal helpers for defensive normalization + subset-label rendering"
      contains: "humanizeWakeupSchedule"
    - path: "src/backend/claude-session/identity-artifact-reader.humanize-wakeup.test.ts"
      provides: "vitest suite covering D-01..D-03, D-05 humanizer cases + regression tests for missing/empty `days` (Success Criteria #4 + #5)"
      contains: "humanizeWakeupSchedule"
  key_links:
    - from: "src/backend/claude-session/identity-artifact-reader.ts:584"
      to: "humanizeWakeupSchedule"
      via: "LOCAL branch call site — passes `parsed.schedule` (from JSON.parse of `<slug>.json`) to humanizer; `scheduleHuman` result written to `Wakeup.scheduleHuman`"
      pattern: "humanizeWakeupSchedule\\(parsed\\.schedule\\)"
    - from: "src/backend/claude-session/identity-artifact-reader.ts:636"
      to: "humanizeWakeupSchedule"
      via: "REMOTE (SSH) branch call site — same signature, same output field"
      pattern: "humanizeWakeupSchedule\\(parsed\\.schedule\\)"
    - from: "src/ui/features/pretty-view/WakeupsTab.tsx:376"
      to: "wakeup.scheduleHuman"
      via: "collapsed-row secondary line renders the humanizer's string verbatim"
      pattern: "wakeup\\.scheduleHuman"
---

<objective>
Extend `humanizeWakeupSchedule` in `src/backend/claude-session/identity-artifact-reader.ts` so an optional top-level `s.days: string[]` day-of-week gate renders correctly on the DISPLAY side of the identity modal. Today the humanizer reads only `type` / `at` / `day` (weekly) and silently drops `s.days`, so a spec like `{type:"daily", at:"23:00", days:["mon","tue","wed","thu","fri"]}` renders as `"Daily at 23:00 (box-local)"` — indistinguishable from a true 7-day-a-week schedule (Ashley report on Aqua@Workstation, 2026-08-31).

Purpose: fixes DISPLAY half of the Phase 65 symmetric bug pair. Renderer side is a single choke point (`humanizeWakeupSchedule`, called at L584 + L636); fixing it here fixes every render location.

Output:
- Extended `humanizeWakeupSchedule` handling `s.days` per CONTEXT decisions D-01, D-02, D-03, D-05, D-07.
- New unit-test file `identity-artifact-reader.humanize-wakeup.test.ts` covering all four Success-Criteria-#4 cases (weekdays / weekends / full-7 / arbitrary subset) plus regression tests keeping today's no-`days`-field renders byte-identical (Success Criteria #5).
- Zero changes to call sites, `Wakeup` wire type, frontend rendering, scheduler, or any other file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/65-wake-up-days-gate-humanize-form-editor-round-trip-preservati/65-CONTEXT.md
@CLAUDE.md
@src/backend/claude-session/identity-artifact-reader.ts
@src/backend/claude-session/session-file-parser.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write failing unit tests for humanizer days-gate cases</name>
  <files>src/backend/claude-session/identity-artifact-reader.humanize-wakeup.test.ts</files>
  <read_first>
    - src/backend/claude-session/identity-artifact-reader.ts L1-81 (humanizer + module header — confirm import path + export name).
    - src/backend/claude-session/session-file-parser.test.ts L1-60 (sibling test style — vitest `{describe, it, expect}` imports, no framework wrappers beyond that, single-file layout with grouped `describe` blocks).
    - .planning/phases/65-wake-up-days-gate-humanize-form-editor-round-trip-preservati/65-CONTEXT.md § "Success Criteria" (all six items) and § "Decisions (locked)" (all of D-01..D-07 but especially D-01, D-02, D-03, D-05, D-07).
  </read_first>
  <behavior>
    Test file at `src/backend/claude-session/identity-artifact-reader.humanize-wakeup.test.ts`, importing the already-exported `humanizeWakeupSchedule` from `./identity-artifact-reader.js` (matches the `.js`-extension ESM style used by session-file-parser.test.ts). Follow the sibling `{describe, it, expect}` layout. Every test is a synchronous single-call assertion: `expect(humanizeWakeupSchedule(inputObj)).toBe("<expected string>")`. Group tests under three `describe` blocks:

    Block A — "humanizeWakeupSchedule — days-gate (Phase 65 / CONTEXT D-01..D-05)":

    1. daily + weekdays exact set → `"Weekdays at 23:00 (box-local)"` for input `{type:"daily", at:"23:00", days:["mon","tue","wed","thu","fri"]}` (D-05 substitution; Success Criteria #4 case 1).
    2. daily + weekdays in scrambled order → same string `"Weekdays at 23:00 (box-local)"` for input `{type:"daily", at:"23:00", days:["fri","mon","wed","tue","thu"]}` (D-03 canonical order — order-insensitive detection).
    3. daily + weekends exact set → `"Weekends at 23:00 (box-local)"` for input `{type:"daily", at:"23:00", days:["sat","sun"]}` (D-05 substitution; Success Criteria #4 case 2).
    4. daily + full-7 (any order, deduplicated) → `"Daily at 23:00 (box-local)"` for input `{type:"daily", at:"23:00", days:["sun","sat","fri","thu","wed","tue","mon"]}` (D-02 drop-the-field render; Success Criteria #4 case 3).
    5. daily + full-7 with duplicates (also drops the field) → `"Daily at 23:00 (box-local)"` for input `{type:"daily", at:"23:00", days:["mon","mon","tue","wed","thu","fri","sat","sun"]}` (D-02 + D-07 defensive dedup).
    6. daily + arbitrary subset {mon,wed,fri} → `"Mon/Wed/Fri at 23:00 (box-local)"` for input `{type:"daily", at:"23:00", days:["fri","mon","wed"]}` (D-05 `/`-separator + D-03 canonical mon→sun order; Success Criteria #4 case 4).
    7. daily + arbitrary subset {tue,thu} → `"Tue/Thu at 23:00 (box-local)"` for input `{type:"daily", at:"23:00", days:["thu","tue"]}` (D-03 + D-05 canonical order proof, non-weekdays/non-weekends).
    8. interval + weekdays → `"Weekdays every 2h"` for input `{type:"interval", every:"2h", days:["mon","tue","wed","thu","fri"]}` (D-05 substitution on interval; "Every ..." verb becomes "every ...").
    9. interval + weekends → `"Weekends every 30m"` for input `{type:"interval", every:"30m", days:["sat","sun"]}`.
    10. interval + arbitrary subset → `"Mon/Wed/Fri every 2h"` for input `{type:"interval", every:"2h", days:["mon","wed","fri"]}`.
    11. interval + full-7 → `"Every 2h"` for input `{type:"interval", every:"2h", days:["mon","tue","wed","thu","fri","sat","sun"]}` (D-02 no-gate render).
    12. weekly with `day ∈ days` (weekdays gate, day=mon) → `"Weekdays at 09:00 (box-local)"` for input `{type:"weekly", day:"mon", at:"09:00", days:["mon","tue","wed","thu","fri"]}` (D-01 gate-substituted form — drops the redundant `on Mon`).
    13. weekly with `day ∈ days` (arbitrary subset) → `"Mon/Fri at 09:00 (box-local)"` for input `{type:"weekly", day:"mon", at:"09:00", days:["mon","fri"]}` (D-01 + D-05 subset form; drops the redundant `on Mon`).
    14. weekly + full-7 → `"Weekly on Mon at 09:00 (box-local)"` for input `{type:"weekly", day:"mon", at:"09:00", days:["mon","tue","wed","thu","fri","sat","sun"]}` (D-02 drops the field — standard weekly render).
    15. weekly with `day ∉ days` (NEVER FIRES malformed case) → `"Weekly on Mon at 09:00 (box-local) — NEVER FIRES (weekly day excluded from days gate)"` for input `{type:"weekly", day:"mon", at:"09:00", days:["tue","wed"]}` (D-01 defensive branch — surface visibly).

    Block B — "humanizeWakeupSchedule — backwards compat (Phase 65 / Success Criteria #5)":

    16. daily without `days` field → `"Daily at 23:00 (box-local)"` for input `{type:"daily", at:"23:00"}` (byte-identical to pre-Phase-65 output).
    17. daily with `days: []` (empty array) → `"Daily at 23:00 (box-local)"` for input `{type:"daily", at:"23:00", days:[]}` (empty === absent === every day per scheduler + D-04).
    18. interval without `days` → `"Every 2h"` for input `{type:"interval", every:"2h"}`.
    19. interval with numeric `every` (legacy branch at L63-65) → `"Every 30m"` for input `{type:"interval", every:30}`.
    20. weekly without `days` → `"Weekly on Mon at 09:00 (box-local)"` for input `{type:"weekly", day:"mon", at:"09:00"}`.
    21. daily without `at` → `"Daily (box-local)"` for input `{type:"daily"}` (L69-70 fallback preserved).
    22. weekly without `day` → `"Weekly on ? at 09:00 (box-local)"` for input `{type:"weekly", at:"09:00"}` (L74-77 `?` fallback preserved).
    23. unknown type → `"custom schedule"` for input `{type:"unknown-thing"}` (L80 fallback preserved).
    24. non-object schedule → `"custom schedule"` for input `null`, `undefined`, `"string"`, `42` (L55 fallback preserved — one assertion per input, or a `.each` table).

    Block C — "humanizeWakeupSchedule — defensive input handling (Phase 65 / D-07)":

    25. daily + malformed `days` (not an array) → falls back to no-gate render `"Daily at 23:00 (box-local)"` for input `{type:"daily", at:"23:00", days:"mon"}` (single string, not array).
    26. daily + malformed `days` (non-string entries) → filters non-strings, renders remaining as if that were the input; e.g. `{type:"daily", at:"23:00", days:["mon",42,null,"fri"]}` → `"Mon/Fri at 23:00 (box-local)"` (filters through weekday guard; two survivors are canonical subset).
    27. daily + `days` with unknown 3-letter codes → filters unknowns, renders survivors; e.g. `{type:"daily", at:"23:00", days:["mon","xyz","fri"]}` → `"Mon/Fri at 23:00 (box-local)"`.
    28. daily + `days` with uppercase / whitespace entries → treated as MALFORMED unless the extended humanizer explicitly normalizes; per D-07 defensive stance, expect the humanizer to lowercase-then-trim before the guard so `{type:"daily", at:"23:00", days:["MON","Tue","fri "]}` → `"Mon/Tue/Fri at 23:00 (box-local)"`. (This is the ONE normalization step; documented in the spec — see acceptance criteria.)
    29. daily + `days` where ALL entries are invalid → falls back to no-gate render `"Daily at 23:00 (box-local)"` for input `{type:"daily", at:"23:00", days:["xyz","abc"]}` (empty post-filter set === absent === every day).
    30. one_shot + `days` → one_shot render is unchanged (Phase 65 does not gate one_shot). Since the current L54-81 humanizer has no `one_shot` branch, this returns `"custom schedule"` (matches today's behavior for one_shot type — the fallback at L80). Confirm this test PASSES both before and after the humanizer change (it is a no-op assertion proving the days-gate logic does not accidentally intercept one_shot).

    Test file MUST run in isolation via `npx vitest run src/backend/claude-session/identity-artifact-reader.humanize-wakeup.test.ts` and MUST currently FAIL on cases 1-15 and 25-29 (proving RED before implementation) while cases 16-24 and 30 currently PASS (regression baseline). Do NOT modify `identity-artifact-reader.ts` in this task.
  </behavior>
  <action>
    Create `src/backend/claude-session/identity-artifact-reader.humanize-wakeup.test.ts` importing `humanizeWakeupSchedule` from `./identity-artifact-reader.js` (extension form matches sibling `session-file-parser.test.ts` L2-6). Structure: three `describe` blocks (A/B/C above), 30 `it` assertions total. Use `expect(fn(input)).toBe(expected)` synchronous form. No fixtures beyond inline literals. Do not touch `identity-artifact-reader.ts`.

    Run `npx vitest run src/backend/claude-session/identity-artifact-reader.humanize-wakeup.test.ts --reporter=verbose` and capture the failure count. Expected: 20 failures (cases 1-15 + 25-29, which is 15 + 5), 10 passes (cases 16-24 + 30, which is 9 + 1). If any regression case (16-24, 30) fails, STOP and investigate — those must be green against the current humanizer.
  </action>
  <verify>
    <automated>npx vitest run src/backend/claude-session/identity-artifact-reader.humanize-wakeup.test.ts --reporter=verbose 2>&1 | grep -E "Tests +[0-9]+ failed \\| [0-9]+ passed"</automated>
  </verify>
  <done>
    - Test file exists at the target path and imports `humanizeWakeupSchedule` from the sibling module using the `.js` ESM extension.
    - `npx vitest run` on the new file shows exactly 20 failures (cases 1-15 + 25-29) and 10 passes (cases 16-24 + 30); zero test-runner errors.
    - `identity-artifact-reader.ts` is UNCHANGED (proof: `git diff --stat src/backend/claude-session/identity-artifact-reader.ts` empty).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — extend humanizer to honor `s.days` per D-01/D-02/D-03/D-05/D-07</name>
  <files>src/backend/claude-session/identity-artifact-reader.ts</files>
  <read_first>
    - src/backend/claude-session/identity-artifact-reader.ts L1-81 (current humanizer — the only region touched by this task; do NOT modify anything below L81).
    - .planning/phases/65-wake-up-days-gate-humanize-form-editor-round-trip-preservati/65-CONTEXT.md § "In-scope this phase → Plan 65-01" and § "Decisions (locked)" D-01/D-02/D-03/D-05/D-07.
    - src/backend/claude-session/identity-artifact-reader.humanize-wakeup.test.ts (the RED-phase suite created in Task 1 — this task's implementation must make all 30 tests pass without editing the test file).
  </read_first>
  <action>
    In `src/backend/claude-session/identity-artifact-reader.ts`, extend the `humanizeWakeupSchedule` function at L54-81 with an internal `days`-gate layer. Do NOT change the exported signature (`(schedule: unknown) => string`). Do NOT change L1-46 (module header, imports, and section comment block) or L82+. Add helpers as top-level `const` / `function` declarations directly ABOVE `humanizeWakeupSchedule` (between the L47 doc-block region and the L54 signature line, i.e. in the L47-53 helper-insertion zone) so they are file-private but visible to the humanizer.

    Concrete edits, in order:

    1. Add a module-private constant `const CANONICAL_WEEKDAYS = ["mon","tue","wed","thu","fri","sat","sun"] as const;` and its element type `type WeekdayCode = (typeof CANONICAL_WEEKDAYS)[number];`. Mirrors `wakeup-scheduler.py` L118-119 order per D-03.
    2. Add a module-private predicate `function isWeekdayCode(v: unknown): v is WeekdayCode` returning true iff `v` is one of the seven strings above. Do NOT export.
    3. Add a module-private helper `function normalizeDaysGate(rawDays: unknown): WeekdayCode[] | null` with defensive semantics per D-07:
       - If `rawDays` is not an array → return `null` (treat as no-gate).
       - Otherwise map each entry: if it is a string, lowercase-and-trim it (`.toLowerCase().trim()`); if not a string, drop it.
       - Filter through `isWeekdayCode`.
       - Deduplicate (build a `Set<WeekdayCode>`).
       - Sort by canonical mon→sun order (index in `CANONICAL_WEEKDAYS`).
       - If the resulting array is empty → return `null` (empty === no-gate per D-04 + Python `if days:` semantics).
       - If the resulting array has length 7 → return `null` (full-7 === no-gate per D-02).
       - Otherwise return the normalized subset.
    4. Add a module-private helper `function daysGateLabel(days: WeekdayCode[]): string` that returns:
       - If `days` is exactly `["mon","tue","wed","thu","fri"]` (deep-equal to the canonical weekdays subset) → return `"Weekdays"`.
       - If `days` is exactly `["sat","sun"]` → return `"Weekends"`.
       - Otherwise → return capitalized 3-letter joined with `/`, e.g. `"Mon/Wed/Fri"`. Capitalization is a per-token `s.charAt(0).toUpperCase() + s.slice(1)` (values are lowercase-normalized 3-letter, so this yields `Mon`, `Tue`, ...). Per D-05.
    5. Modify the `type === "interval"` branch (L58-67) so:
       - Compute `const gate = normalizeDaysGate(s.days);` at the top of the branch.
       - Build the existing `Every <spec>` string as today (using either string or numeric `every`; preserve `"custom schedule"` fallback for missing/invalid `every`).
       - If `gate` is non-null AND the base string is a well-formed `Every <x>` (not the `"custom schedule"` fallback), substitute the verb: replace the leading `"Every "` with `"<label> every "` where `<label>` is `daysGateLabel(gate)`. So `Every 2h` + weekdays gate → `"Weekdays every 2h"`. If the base returned `"custom schedule"`, ignore the gate (return the fallback unchanged).
    6. Modify the `type === "daily"` branch (L68-71) so:
       - Compute `const gate = normalizeDaysGate(s.days);` at the top.
       - Base string is `Daily at <at> (box-local)` (or `Daily (box-local)` when `at` empty) — build as today.
       - If `gate` is non-null: substitute the verb. Replace the leading `Daily` with `daysGateLabel(gate)` — yielding `"Weekdays at 23:00 (box-local)"` / `"Weekends at 23:00 (box-local)"` / `"Mon/Wed/Fri at 23:00 (box-local)"`. Preserve the `at <at>` middle and `(box-local)` suffix verbatim.
    7. Modify the `type === "weekly"` branch (L72-79) so:
       - Compute `const gate = normalizeDaysGate(s.days);` at the top.
       - If `gate` is null → return the existing weekly base string (`Weekly on <Day> at <at> (box-local)` or `Weekly on <Day> (box-local)`).
       - If `gate` is non-null:
         - Determine `dayInGate: boolean` — the weekly-slot day (lowercased 3-letter from `s.day`) is a member of `gate`. Use `isWeekdayCode(dayLower) && gate.includes(dayLower)`.
         - If `dayInGate` → return the daily-style days-gate-substituted form: `"<label> at <at> (box-local)"` where `<label>` is `daysGateLabel(gate)` (drops the redundant `on <Day>` per D-01).
         - If NOT `dayInGate` (malformed NEVER-FIRES case per D-01) → return the base weekly string with the suffix ` — NEVER FIRES (weekly day excluded from days gate)`. Full string: `"Weekly on <Day> at <at> (box-local) — NEVER FIRES (weekly day excluded from days gate)"` (or `"Weekly on ? at <at> (box-local) — NEVER FIRES ..."` when `s.day` was missing — mirrors the existing `?` fallback at L74-77).
    8. Do NOT add a `one_shot` branch — the current L80 `custom schedule` fallback catches it and Phase 65 explicitly does not gate one_shot (§ "Out of scope").
    9. Preserve L82+ verbatim. Do not export any of the new helpers.

    Run `npx vitest run src/backend/claude-session/identity-artifact-reader.humanize-wakeup.test.ts --reporter=verbose` after each substep if desired; final result must be 30/30 passing. Also run `npx vitest run --related src/backend/claude-session/identity-artifact-reader.ts` to prove no sibling test regressed (there are ~7 sibling `identity-artifact-reader.*.test.ts` files — none touch the humanizer today, but confirm with the scoped run).
  </action>
  <verify>
    <automated>npx vitest run src/backend/claude-session/identity-artifact-reader.humanize-wakeup.test.ts --reporter=verbose 2>&1 | grep -E "Tests +30 passed"</automated>
  </verify>
  <done>
    - `npx vitest run src/backend/claude-session/identity-artifact-reader.humanize-wakeup.test.ts` reports 30/30 passing, zero failures, zero skips.
    - `npx vitest run --related src/backend/claude-session/identity-artifact-reader.ts` exits 0 with no regression in the ~7 sibling `identity-artifact-reader.*.test.ts` files (proof: exit code 0 + no `FAIL` lines in output).
    - `grep -n "humanizeWakeupSchedule\\|CANONICAL_WEEKDAYS\\|normalizeDaysGate\\|daysGateLabel\\|isWeekdayCode" src/backend/claude-session/identity-artifact-reader.ts` shows: `humanizeWakeupSchedule` exported (one match on `export function`), all four new helpers present as file-private declarations above the humanizer (four additional matches), and no accidental duplicates.
    - `git diff src/backend/claude-session/identity-artifact-reader.ts` shows changes CONFINED to L47-81 region + helper additions immediately above (no diffs in L1-46 module header, no diffs in L82+ constants/functions/reader logic).
    - `npm run build:backend` exits 0 (per CLAUDE.md backend build gate) — humanizer change does not break the tsc compile.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| identity-artifact-reader → humanizer | already-validated identity artifact JSON (parsed from `~/.claude/identities/<key>/wakeups/<slug>.json` via `JSON.parse` in both LOCAL and REMOTE branches at L577 + L629) crosses into `humanizeWakeupSchedule`. The schedule value is `Record<string, unknown>` — the humanizer already treats it as untrusted (L55 `typeof schedule !== "object"` guard). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01 | Tampering / DoS | `humanizeWakeupSchedule` handling of `s.days` on ill-formed input (non-array, sparse array, non-string entries, extremely long array, prototype-polluted object) | mitigate | `normalizeDaysGate` returns `null` (no-gate) for any non-array input; filters non-strings via type guard; deduplicates with a `Set<WeekdayCode>` (bounded to 7 elements post-filter regardless of input length); does not iterate prototype chain (`.filter`/`.map` on a plain array). Test cases 25-29 cover the malformed-input surface. |
| T-65-02 | Information disclosure | `humanizeWakeupSchedule` returned string reaches the browser via `Wakeup.scheduleHuman` | accept | The humanizer returns hardcoded prose + normalized 3-letter day codes + timestamp values (`s.at`, `s.every`) that were already flowing to the frontend pre-Phase-65. `s.days` values are filtered through a hardcoded 7-string allowlist (`CANONICAL_WEEKDAYS`) — no user-supplied text reaches the output. No new information-disclosure surface introduced. |
</threat_model>

<verification>
Phase-level plan-01 verification (executor confirms all pass before summary):

1. `npx vitest run src/backend/claude-session/identity-artifact-reader.humanize-wakeup.test.ts --reporter=verbose` → 30/30 passing.
2. `npx vitest run --related src/backend/claude-session/identity-artifact-reader.ts` → exit 0, no `FAIL` lines.
3. `npm run build:backend` → exit 0 (CLAUDE.md backend build gate).
4. `git diff --stat` → exactly two files changed: `src/backend/claude-session/identity-artifact-reader.ts` (edits confined to L47-81 region + helpers) and `src/backend/claude-session/identity-artifact-reader.humanize-wakeup.test.ts` (new file).
5. Manual smoke assertion (source-inspection, no runtime): `grep -c "^[^/]*humanizeWakeupSchedule" src/backend/claude-session/identity-artifact-reader.ts` (uncommented lines only) shows: 1 `export function` line + 2 call sites at L584 + L636 (call-sites unchanged). Total unique-line count matches pre-Phase-65 baseline for call sites (2) — proves no accidental new call sites were added.
</verification>

<success_criteria>
- Success Criteria #4 covered: humanizer test file asserts weekdays / weekends / full-7 / arbitrary-subset outputs on the daily type (test cases 1, 3, 4, 6 respectively).
- Success Criteria #5 covered: humanizer regression tests (block B, cases 16-24) prove pre-Phase-65 renders are byte-identical for all specs without `days`.
- D-01 covered: weekly + `days` gate branch tested at cases 12, 13 (`day ∈ days`) and 15 (`day ∉ days` NEVER FIRES).
- D-02 covered: full-7 drops the field on daily (case 4), interval (case 11), weekly (case 14); duplicates dedup to full-7 also drops (case 5).
- D-03 covered: canonical mon→sun order verified on scrambled weekdays input (case 2) and arbitrary-subset input (cases 6, 7, 10).
- D-05 covered: `/`-separator capitalized 3-letter format verified at cases 6, 7, 10.
- D-07 covered: defensive filtering verified at cases 25-29 (malformed types, non-strings, uppercase/whitespace, unknown codes, all-invalid).
- Wire shape unchanged (D-07): humanizer signature and return type unchanged; scheduler / `Wakeup` type / frontend renderer / call sites all untouched.
</success_criteria>

<output>
Create `.planning/phases/65-wake-up-days-gate-humanize-form-editor-round-trip-preservati/65-01-SUMMARY.md` when done, following `templates/summary.md`. Record: 30 unit tests added, humanizer extended in place at L54-81 with 4 new file-private helpers, zero call-site changes, zero wire-shape changes, zero regressions in the 7 sibling identity-artifact-reader test files.
</output>
