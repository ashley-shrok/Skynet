---
phase: 93-relay-rooms-use-the-chat-surface-one-surface-two-data-source
plan: 02
type: execute
wave: 2
depends_on:
  - 92-01
files_modified:
  - src/ui/features/pretty-view/AgentBadgeWithMeter.tsx
  - src/ui/features/pretty-view/MultiBadgeAnchor.tsx
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-view/AgentBadgeWithMeter.test.tsx
  - src/ui/features/pretty-view/MultiBadgeAnchor.test.tsx
  - src/ui/features/pretty-view/PrettyView.multi-badge.test.tsx
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-08
  - D-18
tags:
  - multi-badge
  - port-from-deletion
  - anchor-extension
  - harness-regression-floor
  - slice-2
must_haves:
  truths:
    - "Harness case renders exactly one `<IdentityBadge>` at `absolute top-4 right-5 z-[101]` byte-identical to master (Slice 1 preserved this; Slice 2 preserves it)."
    - "Relay case renders `<MultiBadgeAnchor>` at the same anchor position, with badges growing leftward from the right edge (D-01)."
    - "MultiBadgeAnchor sorts humans-first-then-agents, alphabetical within each role, viewing-user self-excluded (D-03)."
    - "MultiBadgeAnchor distinguishes 'loading' (adapter isReady=false, WS still connecting) from 'empty' (isReady=true with zero non-self participants) — loading renders a subtle placeholder, empty renders nothing (per Warning 2 UX-regression fix)."
    - "AgentBadgeWithMeter carries a shrunk meter + reset appendage below the badge (visual reference: retiring `AgentBadgeWithAppendage.tsx`)."
    - "Human badges in relay case render WITHOUT a meter (D-02)."
    - "Badge-click in relay case is a no-op — no IdentityModal, no navigation (D-18)."
    - "Every case-based branch in this slice reads `source.kind` (D-08); MultiBadgeAnchor is only mounted when `source.kind === 'relay'`."
  artifacts:
    - path: "src/ui/features/pretty-view/AgentBadgeWithMeter.tsx"
      provides: "Byte-port of retiring `AgentBadgeWithAppendage.tsx`: shrunk meter + reset appendage below badge. Import path lives in pretty-view/ so the relay pane retirement doesn't orphan it."
      contains: "export function AgentBadgeWithMeter"
    - path: "src/ui/features/pretty-view/MultiBadgeAnchor.tsx"
      provides: "New leftward-growing badge row anchored at `absolute top-4 right-5 z-[101]`. Mounts humans + agents from participants. Only rendered when source.kind === 'relay'. Takes an `isReady: boolean` prop to distinguish loading (WS connecting) from empty (zero non-self participants)."
      contains: "export function MultiBadgeAnchor"
    - path: "src/ui/features/pretty-view/PrettyView.multi-badge.test.tsx"
      provides: "Assertions: harness one-badge regression, relay multi-badge sort/self-exclude/role-attr, badge-click no-op in relay, position class parity, loading-vs-empty state discrimination."
      contains: "describe(\"PrettyView multi-badge"
  key_links:
    - from: "src/ui/features/pretty-view/PrettyView.tsx"
      to: "src/ui/features/pretty-view/MultiBadgeAnchor.tsx"
      via: "conditional mount at badge anchor when source.kind === 'relay'"
      pattern: "source.kind === \"relay\".*MultiBadgeAnchor"
    - from: "src/ui/features/pretty-view/MultiBadgeAnchor.tsx"
      to: "src/ui/features/pretty-view/AgentBadgeWithMeter.tsx"
      via: "agent cell renders AgentBadgeWithMeter"
      pattern: "AgentBadgeWithMeter"
    - from: "src/ui/features/pretty-view/AgentBadgeWithMeter.tsx"
      to: "src/ui/api/fleet-status-client.ts useSessionContextPct"
      via: "context-meter data read"
      pattern: "useSessionContextPct"
---

<objective>
Land the multi-badge extension of PrettyView's upper-right badge anchor (D-01/D-02/D-03). Ports `AgentBadgeWithAppendage.tsx` (from the still-live retiring pane tree) into a new `pretty-view/AgentBadgeWithMeter.tsx` — a byte-preserving port, then re-composes it with a new `MultiBadgeAnchor.tsx` that anchors leftward-growing badges at PrettyView's existing `absolute top-4 right-5 z-[101]` position. PrettyView's badge anchor grows a `source.kind === "relay"` branch that renders MultiBadgeAnchor; the `source.kind === "harness"` branch stays byte-identical to master (single `<IdentityBadge>` at the same anchor, same onClick opens IdentityModal).

Purpose: The multi-badge component is the single genuinely-new shape this phase introduces. Landing it now — with the standalone relay pane STILL routed to and functional — means the harness case is proven regression-clean before we rewire the dispatcher in Slice 4. Tests are colocated in `pretty-view/` from the outset so the retirement slice can delete `relay-room-pane/` without orphaning any assertion.

Output: `AgentBadgeWithMeter.tsx`, `MultiBadgeAnchor.tsx`, PrettyView badge-anchor case-branch, colocated tests. Harness regression floor: bit-for-bit unchanged at the anchor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-relay-rooms-use-the-chat-surface-one-surface-two-data-source/93-CONTEXT.md
@.planning/phases/93-relay-rooms-use-the-chat-surface-one-surface-two-data-source/93-RESEARCH.md
@.planning/phases/93-relay-rooms-use-the-chat-surface-one-surface-two-data-source/93-PATTERNS.md
@.planning/phases/93-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port AgentBadgeWithAppendage → AgentBadgeWithMeter (byte-preserve; relocate to pretty-view/)</name>
  <files>
    src/ui/features/pretty-view/AgentBadgeWithMeter.tsx,
    src/ui/features/pretty-view/AgentBadgeWithMeter.test.tsx
  </files>
  <read_first>
    - src/ui/features/relay-room-pane/AgentBadgeWithAppendage.tsx (all 292 lines — this is the SOURCE file being ported; the port preserves visuals/behavior byte-for-byte)
    - src/ui/features/relay-room-pane/AgentBadgeWithAppendage.test.tsx (all 299 lines — port the assertions to the new test file)
    - src/ui/api/fleet-status-client.ts (lines 285-305 — `useSessionContextPct` hook this port consumes; the "Plan 06 AgentBadgeWithAppendage" comment at L292 stays as-is for this slice, Slice 5 sweeps comments)
    - src/ui/state/session-working-store.ts (`useSessionIsWorking`, `useSessionIsRecycling` exports — key format `\`${hostId}:${tmuxSessionName}\``)
    - src/ui/features/terminal/IdentityBadge.tsx (lines 100-130 — the base badge primitive; positioning at L110)
    - .planning/phases/93-relay-rooms-use-the-chat-surface-one-surface-two-data-source/93-PATTERNS.md § "src/ui/features/pretty-view/AgentBadgeWithMeter.tsx (NEW file, or inline in MultiBadgeAnchor)" — imports pattern + read-side + write-side plumbing + constants verbatim
    - .planning/phases/93-relay-rooms-use-the-chat-surface-one-surface-two-data-source/93-RESEARCH.md § "Don't Hand-Roll" (`useSessionContextPct`, `/agent-reset` endpoint) + § "Common Pitfalls → Pitfall 1"
  </read_first>
  <behavior>
    - Test 1: Rendered `<AgentBadgeWithMeter identityKey="alice" mxid="@alice:example" hostId={1} tmuxSessionName="alice" />` produces the same DOM tree as the retiring `AgentBadgeWithAppendage` with the same props — verified by port-comparison test that reads a rendered container's outerHTML from each and diffs them (or, equivalently, ports the retiring `AgentBadgeWithAppendage.test.tsx` assertion set verbatim and passes them).
    - Test 2: Context-meter reads via `useSessionContextPct(hostId, tmuxSessionName)` — verified by mocking the hook and asserting the meter width class reflects the mocked pct.
    - Test 3: Reset button dispatches `POST /agent-reset/{hostId}/{encodeURIComponent(tmuxSessionName)}` with body `''` — verified by mocking `authApi.post` and asserting the exact URL + body.
    - Test 4: Meter width constant is `"6rem"` (SHRUNK from ComposeBox's `12rem`); segment count constant is `12`.
  </behavior>
  <action>
    Read the full 292-line `src/ui/features/relay-room-pane/AgentBadgeWithAppendage.tsx` end-to-end. Create `src/ui/features/pretty-view/AgentBadgeWithMeter.tsx` as a byte-preserving port with these changes ONLY:

    1. Rename the exported function from `AgentBadgeWithAppendage` to `AgentBadgeWithMeter` (three sites: `export function`, JSDoc header if present, and test file expectations).
    2. Update the module-header JSDoc: change "Phase 90 Slice D Plan 06" reference to "Phase 93 Slice 2 — ported byte-for-byte from Slice D's AgentBadgeWithAppendage per D-10 (deletion source stays untouched until Slice 4 retirement)".
    3. Update imports for any that use `@/features/relay-room-pane/*` to use the new home. In practice this port has no cross-`relay-room-pane/` imports — verify by re-reading the imports block of the source file first. All imports (`authApi` from `@/main-axios`, `useSessionIsWorking` + `useSessionIsRecycling` from `@/state/session-working-store`, `useSessionContextPct` from `@/api/fleet-status-client`, `IdentityBadge` from `@/features/terminal/IdentityBadge`, `RotateCcw` from `lucide-react`, `cn` from `@/lib/utils`) point at unchanged locations.
    4. Preserve ALL visuals: shrunk meter width `"6rem"`, segment count `12`, all Tailwind classes, all state derivations, all pointer-event guards, all disabled-state discrimination.
    5. Preserve ALL structured-log operations (`agent_badge_reset_*` slugs — verbatim strings if they exist in the source).
    6. Preserve write-side URL construction exactly: `authApi.post(\`/agent-reset/${hostId}/${encodeURIComponent(tmuxSessionName)}\`, { body: '' })`.

    Do NOT delete the source file `src/ui/features/relay-room-pane/AgentBadgeWithAppendage.tsx` in this task. Slice 4 deletes the whole retiring tree. This slice creates the port; the retiring pane still routes to its original.

    Create `src/ui/features/pretty-view/AgentBadgeWithMeter.test.tsx` — port the 299-line `src/ui/features/relay-room-pane/AgentBadgeWithAppendage.test.tsx` verbatim, updating:
    - Import path `@/features/relay-room-pane/AgentBadgeWithAppendage` → `@/features/pretty-view/AgentBadgeWithMeter`.
    - Component name references in the test descriptions/assertions.
    - Any `data-testid` values referenced — if the source uses `agent-badge-with-appendage`, rename to `agent-badge-with-meter` AND update the production file accordingly.

    Do NOT modify anything outside these two new files.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/AgentBadgeWithMeter.test.tsx src/ui/features/relay-room-pane/AgentBadgeWithAppendage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/features/pretty-view/AgentBadgeWithMeter.tsx` exists.
    - `grep -c "export function AgentBadgeWithMeter" src/ui/features/pretty-view/AgentBadgeWithMeter.tsx` returns 1.
    - `grep -c "6rem\|SEG_COUNT.*12\|/agent-reset/" src/ui/features/pretty-view/AgentBadgeWithMeter.tsx | grep -v '^#'` — presence of all three load-bearing constants/URLs.
    - `grep -c "encodeURIComponent(tmuxSessionName)" src/ui/features/pretty-view/AgentBadgeWithMeter.tsx` returns at least 1 (path-traversal defense per RESEARCH.md V5).
    - Ported test file's 15+ assertions pass — `npx vitest run src/ui/features/pretty-view/AgentBadgeWithMeter.test.tsx` green.
    - The source file `src/ui/features/relay-room-pane/AgentBadgeWithAppendage.tsx` is UNTOUCHED — verify via `git status` shows no modification to that path.
    - The source file's tests still pass — `npx vitest run src/ui/features/relay-room-pane/AgentBadgeWithAppendage.test.tsx` green (nothing changed there).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>
    Byte-preserving port of AgentBadgeWithAppendage → AgentBadgeWithMeter landed in pretty-view/ with equivalent tests. Original untouched. Both files pass their tests independently.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Land MultiBadgeAnchor with leftward-growing layout + humans-first-alphabetical sort + loading-vs-empty state discrimination</name>
  <files>
    src/ui/features/pretty-view/MultiBadgeAnchor.tsx,
    src/ui/features/pretty-view/MultiBadgeAnchor.test.tsx
  </files>
  <read_first>
    - src/ui/features/relay-room-pane/IdentityBadgeRow.tsx (all 290 lines — SOURCE for sort discipline at L242-290 and agent-cell shape at L181-228; port these into the new file)
    - src/ui/features/relay-room-pane/IdentityBadgeRow.test.tsx (all 273 lines — port assertion set into the new test file)
    - src/ui/features/relay-room-pane/relay-room-api.ts (lines 55-120 — `RelayRoomParticipantsResponse`, `HumanParticipant`, `AgentParticipant` type shapes; MultiBadgeAnchor consumes these until Slice 4 folds relay-room-api.ts into use-relay-adapter.ts)
    - src/ui/features/pretty-view/AgentBadgeWithMeter.tsx (from Task 1 — the agent-cell renders this)
    - src/ui/features/terminal/IdentityBadge.tsx (lines 100-130 — human-cell renders this bare; note the baked-in `absolute top-4 right-5 z-[101]` at L110)
    - src/ui/state/viewing-user-store.ts (`useViewingUserMxid` — MultiBadgeAnchor needs viewing-user's mxid for D-03 self-exclusion)
    - src/ui/api/fleet-status-client.ts (fleet-identity-hosts map — used by agent cell to resolve mxid → hostId)
    - src/ui/features/pretty-view/relay-mxid-resolve.ts (`resolveMxidToIdentity(mxid, byKey)` — mxid → identity lookup)
    - .planning/phases/93-relay-rooms-use-the-chat-surface-one-surface-two-data-source/93-PATTERNS.md § "src/ui/features/pretty-view/MultiBadgeAnchor.tsx (NEW file)" — analog excerpts + rehome-to-leftward-growing-absolute-anchor pattern
    - .planning/phases/93-relay-rooms-use-the-chat-surface-one-surface-two-data-source/93-RESEARCH.md § "Existing Code Insights → PrettyView anatomy — badge anchor location" + § "Common Pitfalls → Pitfall 1" + § "Open Questions → Q3 IdentityBadge positioning-context hack"
    - .planning/phases/93-relay-rooms-use-the-chat-surface-one-surface-two-data-source/93-CONTEXT.md D-01, D-02, D-03, D-18
  </read_first>
  <behavior>
    - Test 1 (sort discipline, D-03): Given participants with humans `[c, a, b]` and agents `[z, x, y]` plus viewing-user mxid `@viewer:example`, MultiBadgeAnchor's rendered `data-testid="relay-room-participant"` nodes appear in the DOM in order: humans-alphabetical `[a, b, c]`, then agents-alphabetical `[x, y, z]`. Visually (via flex-row-reverse OR right-to-left DOM order — TBD in action), rightmost badge is the "first" listed participant.
    - Test 2 (self-exclusion, D-03): Given participants including viewing-user's own mxid in the humans array, the rendered set excludes viewing-user. Assert count === participants.humans.length - 1 + participants.agents.length when viewer is in humans.
    - Test 3 (role attribution): Each rendered cell has `data-role="human"` or `data-role="agent"` matching its participant classification.
    - Test 4 (agent-cell rendering, D-02): Agent cells render `<AgentBadgeWithMeter>` (assert by `data-testid` or by the shrunk-meter DOM feature); human cells render bare `<IdentityBadge>` WITHOUT a meter (no `agent-badge-with-meter` testid in human cells).
    - Test 5 (position, D-01): The root container has classes `absolute top-4 right-5 z-[101]` — parity with harness single-badge position at PrettyView.tsx:3349-3366.
    - Test 6 (badge-click no-op in relay, D-18): Clicking a human badge invokes no callback (no IdentityModal, no navigation); Clicking an agent badge — the AgentBadgeWithMeter port defines its own click behavior (badge itself is inert, reset button separately clickable), which stays as-inherited from the port.
    - Test 7 (missing-host-mapping graceful degradation): If an agent's identityKey has no entry in `fleetIdentityHosts`, the agent cell falls back to a bare `<IdentityBadge>` without appendage, and a structured warning logs via `console.warn`.
    - Test 8 (loading state, Warning 2): Given `isReady={false}` AND `participants={{ humans: [], agents: [] }}`, MultiBadgeAnchor renders a subtle placeholder / spinner (assert via `data-testid="multi-badge-anchor-loading"` OR by asserting the root container contains a placeholder child). It does NOT render nothing (the empty case) and does NOT render actual badges (there are none to render).
    - Test 9 (empty state, Warning 2): Given `isReady={true}` AND `participants={{ humans: [<only viewer>], agents: [] }}` (i.e. every human is the viewer, self-excluded), MultiBadgeAnchor renders NOTHING inside the root container (empty case — no placeholder, no badges). Assert the root container has zero rendered participant cells AND no loading placeholder.
    - Test 10 (ready-with-participants renders normally, Warning 2): Given `isReady={true}` AND populated `participants`, MultiBadgeAnchor renders badges per sort discipline (Test 1) — no loading placeholder present.
  </behavior>
  <action>
    Create `src/ui/features/pretty-view/MultiBadgeAnchor.tsx` starting from `src/ui/features/relay-room-pane/IdentityBadgeRow.tsx` structure. Key changes vs. the source:

    1. **Rehome the root container** to `absolute top-4 right-5 z-[101] flex flex-row-reverse items-start gap-2` (the harness anchor position, growing leftward via `flex-row-reverse`). NOT a `w-full flex flex-row gap-2 justify-start pl-4 pr-4 pt-3 pb-1 relative` top-of-pane row (Slice D's shape).
    2. **Import the ported `AgentBadgeWithMeter`** from `@/features/pretty-view/AgentBadgeWithMeter` (NOT `@/features/relay-room-pane/AgentBadgeWithAppendage`).
    3. **Preserve the sort discipline verbatim** from `IdentityBadgeRow.tsx:242-290`:

       ```
       const humansOther = participants.humans
         .filter((h) => h.mxid !== viewingUserMxid)
         .slice()
         .sort((a, b) => a.displayName.localeCompare(b.displayName));
       const agentsSorted = participants.agents
         .slice()
         .sort((a, b) => a.identityKey.localeCompare(b.identityKey));
       ```

    4. **Preserve the agent-cell shape** from `IdentityBadgeRow.tsx:181-228`. Include the `hostId === undefined` graceful degradation (bare `<IdentityBadge>` fallback + `console.warn({ operation: "agent_badge_no_host_mapping", identityKey })`).
    5. **DOM ordering with `flex-row-reverse`** per PATTERNS.md § "MultiBadgeAnchor" target shape: the FIRST DOM child is RIGHTMOST visually. Emit `{agentsSorted.map(...)}` FIRST in JSX (so agents visually adjacent to the harness single-badge anchor point), then `{humansOther.map(...)}` (so humans grow further left). This choice is documented per RESEARCH.md Assumption A3 — if the render test at slice execution shows a visual mismatch, fall back to `flex-row` with explicit `.reverse()` on the array.
    6. **Cell wrapper hack** per RESEARCH.md Open Question 3: wrap each `<IdentityBadge>` (human cell) OR `<AgentBadgeWithMeter>` (agent cell) in `<div className="relative shrink-0 h-[72px] w-[220px] flex flex-col items-stretch">` (human cell) or `h-[92px] w-[220px]` (agent cell, accounting for the meter appendage) — this scopes IdentityBadge's baked-in `absolute` positioning to the cell.
    7. **Props signature** (Warning 2 — takes an `isReady: boolean` for loading-vs-empty discrimination):

       ```
       export interface MultiBadgeAnchorProps {
         participants: { humans: HumanParticipant[]; agents: AgentParticipant[] };
         viewingUserMxid: string;
         fleetIdentityHosts: Record<string, number>;
         isReady: boolean;  // false = WS still connecting (loading placeholder); true = participants list is authoritative
       }
       export function MultiBadgeAnchor(props: MultiBadgeAnchorProps) { ... }
       ```

       PrettyView (Task 3) resolves `participants`, `viewingUserMxid`, `fleetIdentityHosts`, and `isReady` from adapter state / existing stores and passes them explicitly. `isReady` maps directly to `adapter.isReady`. MultiBadgeAnchor is a pure render function (no hooks that read stores directly, keeping it easy to test).

    8. **Loading vs. empty discrimination inside the component** (Warning 2):

       ```
       const humansOther = participants.humans.filter(...).sort(...);
       const agentsSorted = participants.agents.slice().sort(...);
       const totalCells = humansOther.length + agentsSorted.length;

       if (!isReady) {
         // Loading: WS is still connecting, no participants frame received yet.
         // Render a subtle placeholder so the user knows the room is loading,
         // not empty.
         return (
           <div className="absolute top-4 right-5 z-[101] flex flex-row-reverse items-start gap-2"
                data-testid="multi-badge-anchor">
             <div data-testid="multi-badge-anchor-loading" className="opacity-40 text-xs">
               loading participants...
             </div>
           </div>
         );
       }

       if (totalCells === 0) {
         // Empty: WS is ready but no non-self participants. In practice this
         // shouldn't happen for a relay room (implies at least one other
         // participant) but the guard keeps the render safe.
         return null;
       }

       // Otherwise render the badge row per sort discipline.
       return (
         <div className="absolute top-4 right-5 z-[101] flex flex-row-reverse items-start gap-2"
              data-testid="multi-badge-anchor">
           {agentsSorted.map(...)}
           {humansOther.map(...)}
         </div>
       );
       ```

       The concrete placeholder shape is planner discretion — a subtle low-opacity text label, a spinner icon, or a fixed-width skeleton element are all acceptable. The load-bearing constraint is that a relay tab with `isReady=false` renders SOMETHING (not empty space or a bare compose bar).

    9. **Root `data-testid="multi-badge-anchor"`** — for the render test to grab (present in both loading and ready-with-participants branches). The empty case renders `null` and produces no testid.

    10. **NO reads of `source.kind`** — this component is only ever mounted when `source.kind === "relay"` (PrettyView gates the mount in Task 3). D-08 discipline: one check at the boundary; component itself is source-oblivious.

    Create `src/ui/features/pretty-view/MultiBadgeAnchor.test.tsx` — port the 273-line assertion set from `src/ui/features/relay-room-pane/IdentityBadgeRow.test.tsx`, adapting:
    - Test the new props shape (participants + viewingUserMxid + fleetIdentityHosts + isReady passed explicitly, not sourced from stores inside the component).
    - Assert the new root `absolute top-4 right-5 z-[101] flex flex-row-reverse` classes (position parity with harness anchor per D-01).
    - Assert the DOM order matches the flex-row-reverse ordering choice (agents first in DOM = agents visually rightmost, humans last in DOM = humans visually leftmost).
    - Preserve the self-exclusion and role-attribution assertions verbatim.
    - Add Test 8, Test 9, Test 10 for loading-vs-empty discrimination.

    Do NOT modify the source `IdentityBadgeRow.tsx` — Slice 4 deletes it.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/MultiBadgeAnchor.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/features/pretty-view/MultiBadgeAnchor.tsx` exists with `export function MultiBadgeAnchor`.
    - `grep -c "absolute top-4 right-5 z-\[101\]" src/ui/features/pretty-view/MultiBadgeAnchor.tsx` returns at least 1 (position class).
    - `grep -c "flex-row-reverse\|\\.reverse()" src/ui/features/pretty-view/MultiBadgeAnchor.tsx` returns at least 1 (leftward-growing direction).
    - `grep -c "localeCompare" src/ui/features/pretty-view/MultiBadgeAnchor.tsx` returns at least 2 (humans sort + agents sort).
    - `grep -c "AgentBadgeWithMeter" src/ui/features/pretty-view/MultiBadgeAnchor.tsx` returns at least 2 (import + use).
    - `grep -c "filter.*viewingUserMxid" src/ui/features/pretty-view/MultiBadgeAnchor.tsx` returns at least 1 (self-exclusion).
    - `grep -c "agent_badge_no_host_mapping" src/ui/features/pretty-view/MultiBadgeAnchor.tsx` returns 1 (graceful-degradation log preserved).
    - **isReady prop wired** — `grep -c "isReady" src/ui/features/pretty-view/MultiBadgeAnchor.tsx | grep -v '^#'` returns at least 2 (prop declaration + guard read).
    - **Loading testid present** — `grep -c "multi-badge-anchor-loading" src/ui/features/pretty-view/MultiBadgeAnchor.tsx` returns at least 1 (loading placeholder discriminator).
    - Test file's 18+ assertions pass (existing 15 + new Tests 8/9/10) — `npx vitest run src/ui/features/pretty-view/MultiBadgeAnchor.test.tsx` green.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>
    MultiBadgeAnchor lands with humans-first-alphabetical sort, self-exclusion, role-attribution, leftward-growing layout at the harness anchor position, graceful degradation on missing host mapping, and loading-vs-empty state discrimination via the `isReady` prop.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add PrettyView badge-anchor case-branch (harness = existing single badge byte-identical, relay = MultiBadgeAnchor)</name>
  <files>
    src/ui/features/pretty-view/PrettyView.tsx,
    src/ui/features/pretty-view/PrettyView.multi-badge.test.tsx
  </files>
  <read_first>
    - src/ui/features/pretty-view/PrettyView.tsx (lines 3340-3410 — the badge anchor at L3349-3366 and the sibling task-pill at L3391+; DO NOT alter the task-pill; z-[100] must stay below badge's z-[101])
    - src/ui/features/pretty-view/MultiBadgeAnchor.tsx (from Task 2)
    - src/ui/features/pretty-view/AgentBadgeWithMeter.tsx (from Task 1)
    - src/ui/features/relay-room-pane/RelayRoomPane.tsx (lines 55-170 — how the relay pane resolves participants + viewingUserMxid + fleetIdentityHosts currently; PrettyView needs the same three inputs when mounting MultiBadgeAnchor. Note: adapter state is stubbed until Slice 3 lands the real hook. This task threads the WIRING; live participants come with Slice 3.)
    - src/ui/state/viewing-user-store.ts (`useViewingUserMxid` — PrettyView resolves viewing-user internally)
    - src/ui/api/fleet-status-client.ts (`useFleetStatus` or similar — provides fleetIdentityHosts map)
    - .planning/phases/93-relay-rooms-use-the-chat-surface-one-surface-two-data-source/93-PATTERNS.md § "src/ui/features/pretty-view/PrettyView.tsx (EXTEND-IN-PLACE)" → "Extension surface #1 — badge anchor (D-01/D-02/D-03)" — target shape with byte-identical harness preservation
    - .planning/phases/93-relay-rooms-use-the-chat-surface-one-surface-two-data-source/93-RESEARCH.md § "Common Pitfalls → Pitfall 1: Harness regression" — the explicit rule that harness-case badge mount MUST render byte-identical DOM
  </read_first>
  <behavior>
    - Test 1 (harness regression floor — BYTE-IDENTICAL): Mount PrettyView with `source={{ kind: "harness", hostId: 1, tmuxSession: "s1" }}` and mock `pvIdentityKey` present. Assert the badge subtree serialized via `container.querySelector('.pv-identity-breathe')?.outerHTML` is byte-equal to the same subtree rendered from an unmodified PrettyView at the same source (via a fixture / pre-slice snapshot). Alternatively: assert exactly ONE `<IdentityBadge>` mounts at `absolute top-4 right-5 z-[101]` with same onClick/onLongPress/onContextMenu prop behavior.
    - Test 2 (harness DOES NOT mount MultiBadgeAnchor): `container.querySelector('[data-testid="multi-badge-anchor"]')` is `null` when `source.kind === "harness"`.
    - Test 3 (relay case mounts MultiBadgeAnchor + hides single-badge): Mount with `source={{ kind: "relay", roomId: "!x:example", roomTitle: "test" }}`. Assert `container.querySelector('[data-testid="multi-badge-anchor"]')` exists AND the single-badge mount at L3349-3366 is NOT rendered (no bare `<IdentityBadge>` for viewer's identity outside the anchor).
    - Test 4 (relay case renders passed participants): Mount with a relay source AND stub the adapter to return participants + viewingUserMxid + fleetIdentityHosts + isReady=true. Assert MultiBadgeAnchor receives them and renders per Task 2's sort discipline.
    - Test 5 (task-pill unchanged): The centered task-pill at L3391+ remains at `absolute top-4 left-1/2 -translate-x-1/2 z-[100]` in both cases — unchanged by this task.
    - Test 6 (badge-click behavior, D-18): Harness case badge onClick opens IdentityModal (as today). Relay case: the MultiBadgeAnchor's human badges do nothing on click; agent badges' click behavior is inherited from AgentBadgeWithMeter's port (badge itself inert, reset button separately clickable — from Task 1's port).
    - Test 7 (loading state passthrough, Warning 2): Mount with `source.kind === "relay"` AND adapter stubbed to return `isReady=false, participants={ humans: [], agents: [] }`. Assert MultiBadgeAnchor's loading placeholder renders (`[data-testid="multi-badge-anchor-loading"]` exists).
  </behavior>
  <action>
    Locate the badge anchor block in `src/ui/features/pretty-view/PrettyView.tsx` at L3349-3366 (read L3340-3410 first to confirm exact anchor). Wrap the existing `{pvIdentityKey && (<IdentityBadge ... />)}` block with an outer `source.kind === "harness"` gate per PATTERNS.md § "Extension surface #1" target shape:

    ```
    {source.kind === "harness" && pvIdentityKey && (
      <IdentityBadge
        identityKey={pvIdentityKey}
        hostId={hostId}
        onClick={() => setIsIdentityModalOpen(true)}
        onLongPress={onTogglePrettyMode}
        tabId={tabId}
        onContextMenu={
          identityBadgeContextMenuItems && identityBadgeContextMenuItems.length > 0
            ? (e) => { e.preventDefault(); setIdentityBadgeMenu({ x: e.clientX, y: e.clientY }); }
            : undefined
        }
      />
    )}
    ```

    Add the relay-branch mount immediately after the harness single-badge block, BEFORE the identity-badge context menu render at L3367+. Do NOT wrap the harness case in any intermediate div — the DOM at the badge anchor for `source.kind === "harness"` must stay byte-identical (Pitfall 1). Only the outer JSX conditional changes, not the mounted subtree.

    Relay-branch shape (per Warning 2 — MultiBadgeAnchor now handles the loading placeholder internally, so PrettyView unconditionally mounts it when `source.kind === "relay"` and passes `adapter.participants` + `adapter.isReady` through):

    ```
    {source.kind === "relay" && (
      <MultiBadgeAnchor
        participants={adapter.participants ?? { humans: [], agents: [] }}
        viewingUserMxid={viewingUserMxid ?? ""}
        fleetIdentityHosts={fleetIdentityHosts}
        isReady={adapter.isReady}
      />
    )}
    ```

    Note: `adapter.participants` may be `null` when `source.kind !== "relay"` (harness shim), but this JSX gate only fires when `source.kind === "relay"`, and Slice 3's `useRelayAdapter` initial state is `participants: { humans: [], agents: [] }` (Warning 2 mandate) — so the `?? { humans: [], agents: [] }` fallback is defensive only. Do NOT gate on `adapter.participants` truthiness — the Slice 1 stubbed relay adapter returns `participants: { humans: [], agents: [] }` (Warning 2 mandate) so the anchor mounts and shows the loading placeholder immediately, matching the D-01 anchor position from mount.

    Import `MultiBadgeAnchor` from `@/features/pretty-view/MultiBadgeAnchor` at the top of PrettyView.tsx. Resolve `viewingUserMxid` via `useViewingUserMxid()` from `@/state/viewing-user-store` (add the import if not present). Resolve `fleetIdentityHosts` from the existing fleet-status data source PrettyView already reads OR via a new call to the fleet-status-client hook — check existing PrettyView imports first; if a hook already surfaces this map, reuse it. If not, add `useFleetStatus()` from `@/api/fleet-status-client` and pluck the identity-hosts map from its result.

    Wire adapter state passthrough: this task ASSUMES `adapter.participants` (initial `{ humans: [], agents: [] }`) and `adapter.isReady` (initial `false`) exist on the return shape. Slice 1's stubbed relay adapter satisfies this (per Warning 2 alignment); Slice 3 replaces the stub with the real adapter that flips `isReady=true` when the first participants frame arrives. This task's relay-source render therefore correctly shows the loading placeholder while `adapter.isReady === false`, and swaps to the real participant badges once Slice 3 lands.

    Do NOT modify `handleComposeSend` (L1176-1180) — Slice 3 case-selects the send handler. Do NOT modify the ingestion effect (L1852+) — the Slice 1 gate is already in place.

    Create `src/ui/features/pretty-view/PrettyView.multi-badge.test.tsx` with the seven behavior tests above. Use `render()` from `@testing-library/react`. For the byte-identical assertion (Test 1), the practical approach is:
    - Render PrettyView with a harness source that produces a known `pvIdentityKey`.
    - Grab the `pv-identity-breathe` badge element and assert its class list, onClick handler behavior (via `fireEvent.click`), and absence of any wrapping `<div>` between it and PrettyView's root's badge-anchor sibling group.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/PrettyView.multi-badge.test.tsx src/ui/features/pretty-view/PrettyView.test.tsx src/ui/features/pretty-view/PrettyView.task-pill.test.tsx src/ui/features/pretty-view/PrettyView.source-prop.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "source.kind === \"harness\"" src/ui/features/pretty-view/PrettyView.tsx` — increases by at least 1 vs. Slice 1 (the new badge-anchor gate).
    - `grep -c "source.kind === \"relay\"" src/ui/features/pretty-view/PrettyView.tsx` — returns at least 1 (the MultiBadgeAnchor mount gate). Exact enumeration of expected `source.kind === "relay"` sites is deferred to Slice 3 Task 3's acceptance criteria (Warning 3); Slice 2 adds the FIRST of the ≥5 sites.
    - `grep -c "MultiBadgeAnchor" src/ui/features/pretty-view/PrettyView.tsx` returns at least 2 (import + JSX use).
    - `grep -c "adapter.isReady\|isReady={adapter" src/ui/features/pretty-view/PrettyView.tsx | grep -v '^#'` returns at least 1 (isReady threaded to MultiBadgeAnchor).
    - PrettyView test suite — `npx vitest run src/ui/features/pretty-view/PrettyView.test.tsx` — passes with ZERO diff vs. pre-slice (Pitfall 1 harness regression floor).
    - `PrettyView.task-pill.test.tsx` passes — task pill's z-[100] stays BELOW badge z-[101] (no positioning regression).
    - `PrettyView.multi-badge.test.tsx` Test 1 passes: harness case renders exactly ONE badge at `absolute top-4 right-5 z-[101]` byte-identical.
    - `PrettyView.multi-badge.test.tsx` Test 3 passes: relay case renders `[data-testid="multi-badge-anchor"]` and does NOT render the single-badge mount.
    - `PrettyView.multi-badge.test.tsx` Test 7 passes: relay case with `adapter.isReady=false` renders the loading placeholder.
    - Manual: `git diff src/ui/features/pretty-view/PrettyView.tsx` should show a wrapper-only change at L3349 (adding `source.kind === "harness" &&` in front of the existing `pvIdentityKey && (...)`) plus a new sibling block for the relay case. Absolutely no changes to the interior of the harness IdentityBadge mount.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>
    PrettyView's badge anchor now hosts a case-branch. Harness renders byte-identical single-badge; relay mounts MultiBadgeAnchor unconditionally when source.kind === "relay" (loading placeholder shows while adapter.isReady=false). Task-pill unaffected. Standalone relay pane still routed to (real relay tabs still show that pane, not the shared surface — that swap comes in Slice 4).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React component prop consumer | `MultiBadgeAnchor` receives participants, viewingUserMxid, fleetIdentityHosts, isReady from PrettyView. |
| DOM stacking context at badge anchor | z-[101] badge vs. z-[100] task-pill layering must not regress. |
| Matrix mxid string handling | mxids flow through display name resolution; URL construction for `/agent-reset` uses `encodeURIComponent`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-02-01 | Tampering | Path traversal via `tmuxSessionName` in `/agent-reset` URL | mitigate | Port preserves `encodeURIComponent(tmuxSessionName)` verbatim from `AgentBadgeWithAppendage.tsx` (RESEARCH.md § "Shared Patterns → encodeURIComponent on Matrix identifiers"). Task 1 acceptance criteria greps for this call. |
| T-92-02-02 | Information Disclosure | Structured logging in `AgentBadgeWithMeter` | mitigate | Port preserves the source file's structured log operations verbatim. No raw event bodies or Matrix response payloads logged. |
| T-92-02-03 | Tampering | Harness case regression (Pitfall 1) | mitigate | Wrapper-only change at the badge anchor (`source.kind === "harness" &&` prefix). Harness subtree byte-preserved. All existing PrettyView tests must pass unchanged (Pitfall 1 acceptance criterion). |
| T-92-02-04 | XSS | Participant `displayName` rendering in badges | mitigate | Display names render via React JSX text children (`{h.displayName}`) — NEVER `dangerouslySetInnerHTML`. Preserved from tiffany's RelayInboundBubble discipline (T-17-03-01) and from the retiring IdentityBadgeRow's source (which already followed this discipline). RESEARCH.md § "Security Domain → V5 Input Validation". |
| T-92-02-05 | Denial of Service | z-index layering regression | mitigate | Task 3 preserves badge z-[101] and task-pill z-[100]. `PrettyView.task-pill.test.tsx` runs green as an acceptance criterion. |
| T-92-02-06 | Denial of Service (UX regression) | Loading vs. empty state confusion | mitigate | Warning 2 fix: MultiBadgeAnchor discriminates loading (isReady=false → subtle placeholder) from empty (isReady=true + zero cells → null render). Prevents relay tab from appearing empty during WS-open-but-no-participants-frame-yet window. |
| T-92-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed. Package Legitimacy Audit N/A. |
</threat_model>

<verification>
- `npx vitest run src/ui/features/pretty-view/AgentBadgeWithMeter.test.tsx src/ui/features/pretty-view/MultiBadgeAnchor.test.tsx src/ui/features/pretty-view/PrettyView.multi-badge.test.tsx src/ui/features/pretty-view/PrettyView.test.tsx src/ui/features/pretty-view/PrettyView.task-pill.test.tsx src/ui/features/pretty-view/PrettyView.source-prop.test.tsx` — this slice + prior slice's tests all green.
- `npx vitest run src/ui/features/relay-room-pane/AgentBadgeWithAppendage.test.tsx src/ui/features/relay-room-pane/IdentityBadgeRow.test.tsx` — retiring pane's tests still green (nothing changed there; standalone still routes to it).
- `npx tsc --noEmit` clean.
- Harness regression floor: full PrettyView.test.tsx passes with zero snapshot diffs.
</verification>

<success_criteria>
- `AgentBadgeWithMeter.tsx` + tests in pretty-view/ (byte-preserved port of AgentBadgeWithAppendage).
- `MultiBadgeAnchor.tsx` + tests in pretty-view/ with leftward-growing layout, humans-first-alphabetical, self-exclusion, role attribution, graceful degradation on missing host mapping, AND loading-vs-empty state discrimination via `isReady` prop (Warning 2).
- PrettyView's badge anchor has a case-branch: harness renders byte-identical single-badge, relay renders MultiBadgeAnchor with adapter.participants + adapter.isReady wired through.
- Harness case DOM at the badge anchor is byte-identical to master.
- All existing PrettyView tests pass unchanged.
- Standalone relay pane STILL routed to and functional; real relay tabs still show the standalone pane (which will retire in Slice 4).
- `npx tsc --noEmit` passes.
</success_criteria>

<output>
Create `.planning/phases/93-relay-rooms-use-the-chat-surface-one-surface-two-data-source/92-02-SUMMARY.md` when done.
</output>
