---
phase: 93-relay-rooms-use-the-chat-surface-one-surface-two-data-source
plan: 04
type: execute
wave: 4
depends_on:
  - 92-01
  - 92-02
  - 92-03
files_modified:
  - src/ui/shell/tabUtils.tsx
  - src/ui/features/relay-room-pane/AgentBadgeWithAppendage.tsx
  - src/ui/features/relay-room-pane/AgentBadgeWithAppendage.test.tsx
  - src/ui/features/relay-room-pane/IdentityBadgeRow.tsx
  - src/ui/features/relay-room-pane/IdentityBadgeRow.test.tsx
  - src/ui/features/relay-room-pane/RelayMessageList.tsx
  - src/ui/features/relay-room-pane/RelayMessageList.test.tsx
  - src/ui/features/relay-room-pane/RelayRoomInboundBubble.tsx
  - src/ui/features/relay-room-pane/RelayRoomInboundBubble.test.tsx
  - src/ui/features/relay-room-pane/RelayRoomPane.tsx
  - src/ui/features/relay-room-pane/RelayRoomPane.test.tsx
  - src/ui/features/relay-room-pane/error-state.tsx
  - src/ui/features/relay-room-pane/error-state.test.tsx
  - src/ui/features/relay-room-pane/relay-room-api.ts
  - src/ui/features/relay-room-pane/use-relay-room-stream.ts
  - src/ui/features/relay-room-pane/use-relay-room-stream.test.ts
  - src/ui/shell/RelayRoomSessionPane.tsx
  - src/ui/shell/RelayRoomSessionPane.test.tsx
autonomous: true
requirements:
  - D-04
  - D-05
  - D-06
tags:
  - dispatcher-rewire
  - retirement
  - deletion-graph
  - harness-regression-floor
  - slice-4
must_haves:
  truths:
    - "tabUtils dispatcher's relay-room branch in TerminalOrIdentitySessionPane mounts `<PrettyView source={{ kind: 'relay', roomId, roomTitle }} />` (D-04)."
    - "The renderTabContent early-return branch at tabUtils.tsx:314 for relay-room tabs retires; host-optional handling lives inside TerminalOrIdentitySessionPane (D-06)."
    - "sessionKind STAYS as tab-level discriminator; Tab type unchanged (D-05). Only what the dispatcher's relay branch RENDERS changes."
    - "src/ui/features/relay-room-pane/ deletes entirely (all 8 production files + 7 test files, ~4,275 LOC)."
    - "src/ui/shell/RelayRoomSessionPane.tsx + its test delete entirely (D-04)."
    - "grep -rn 'features/relay-room-pane\\|shell/RelayRoomSessionPane\\|use-relay-room-stream\\|RelayRoomInboundBubble\\|AgentBadgeWithAppendage' src/ tests/ scripts/ returns zero hits (Pitfall 3)."
    - "npx tsc --noEmit passes — no dangling type refs."
    - "Vitest cache staleness cleared post-retirement (rm -rf node_modules/.vitest)."
    - "Harness case still byte-identical at every extension anchor."
    - "Real relay-room tabs now render through the shared surface (PrettyView with source.kind === 'relay'). Full end-to-end: participants render via MultiBadgeAnchor, messages via message-list, send via adapter, error state via ChatSurfaceErrorState."
  artifacts:
    - path: "src/ui/shell/tabUtils.tsx"
      provides: "Dispatcher rewired: relay branch mounts PrettyView with relay source; renderTabContent early-return retired; host-optional handling internal to TerminalOrIdentitySessionPane."
      contains: "kind: \"relay\""
  key_links:
    - from: "src/ui/shell/tabUtils.tsx"
      to: "src/ui/features/pretty-view/PrettyView.tsx"
      via: "TerminalOrIdentitySessionPane relay branch mounts PrettyView with source.kind === 'relay'"
      pattern: "kind:\\s*\"relay\""
---

<objective>
The retirement slice. Atomically rewire the dispatcher (tabUtils.tsx) to route relay-room tabs to the shared PrettyView with `source={{ kind: "relay", roomId, roomTitle }}` (D-04), retire the renderTabContent early-return branch at tabUtils.tsx:314 (D-06) by moving host-optional handling into TerminalOrIdentitySessionPane, then DELETE the entire standalone relay-room-pane tree (`src/ui/features/relay-room-pane/` + `src/ui/shell/RelayRoomSessionPane.tsx` + tests, ~4,275 LOC). Sweep the deleted-tree references and run the exhaustive retirement grep to confirm zero dangling imports. Clear vitest cache staleness.

Purpose: This is the point where relay-room tabs stop rendering the standalone pane and start rendering through the shared chat surface. Slices 1-3 built the shared surface's relay-case machinery; nothing routed to it. This slice does the swap and disposes of the duplicate. Harness case must stay byte-identical — the dispatcher change is scoped to the relay branch, and IdentitySessionPane (Slice 1 Task 3) already handles harness routing to PrettyView.

Output: dispatcher swap, retirement of the pane tree, retirement of RelayRoomSessionPane, retirement of tabUtils early-return, retirement grep clean, tsc clean, vitest cache clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-relay-rooms-use-the-chat-surface-one-surface-two-data-source/93-CONTEXT.md
@.planning/phases/93-relay-rooms-use-the-chat-surface-one-surface-two-data-source/93-RESEARCH.md
@.planning/phases/93-relay-rooms-use-the-chat-surface-one-surface-two-data-source/93-PATTERNS.md
@.planning/phases/93-01-SUMMARY.md
@.planning/phases/92-02-SUMMARY.md
@.planning/phases/92-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire tabUtils.tsx dispatcher — relay branch mounts PrettyView; retire early-return at L314 (D-04, D-06)</name>
  <files>
    src/ui/shell/tabUtils.tsx
  </files>
  <read_first>
    - src/ui/shell/tabUtils.tsx (all 373 lines — read full file; specifically lines 23-30 (lazy-import of RelayRoomSessionPane), lines 187-222 (TerminalOrIdentitySessionPane relay branch), lines 274-373 (renderTabContent + case "terminal" including the early-return at L306-326))
    - src/ui/features/pretty-view/PrettyView.tsx (verify the source prop shape after Slices 1-3)
    - src/ui/features/pretty-view/sources/chat-surface-source.ts (Slice 1 output — ChatSurfaceSource type)
    - .planning/phases/93-relay-rooms-use-the-chat-surface-one-surface-two-data-source/93-PATTERNS.md - section "src/ui/shell/tabUtils.tsx (DISPATCHER-REWIRE)" — all three rewire surfaces excerpted verbatim
    - .planning/phases/93-relay-rooms-use-the-chat-surface-one-surface-two-data-source/93-RESEARCH.md - section "Deletion Graph - External refs" (specific line changes for tabUtils.tsx)
    - .planning/phases/93-relay-rooms-use-the-chat-surface-one-surface-two-data-source/93-CONTEXT.md D-04, D-05, D-06
  </read_first>
  <behavior>
    - Test 1 (dispatch to PrettyView for relay-room tabs): Given a tab with `sessionKind: "relay-room"`, `relayRoomId: "!x:example"`, `relayRoomTitle: "test"`, `host: null`, the dispatcher's rendered tree contains a PrettyView with `source.kind === "relay"`, `source.roomId === "!x:example"`, `source.roomTitle === "test"`. RelayRoomSessionPane is NOT mounted.
    - Test 2 (early-return retired): renderTabContent for a relay-room tab of type "terminal" no longer takes the L306-326 early-return branch. Verified via source-file grep + a rendering test that mounts a relay-room tab and asserts PrettyView appears (via TerminalOrIdentitySessionPane's relay branch).
    - Test 3 (host-null gate handles relay-room correctly): For a `type: "terminal"` + `sessionKind: "relay-room"` tab, the `!host` gate does NOT short-circuit to "no host selected" — TerminalOrIdentitySessionPane is reached and its relay branch renders PrettyView.
    - Test 4 (harness case regression floor): For a `type: "terminal"` + non-relay-room + host present tab, the render tree is unchanged from master. For an identity pane tab, IdentitySessionPane still mounts PrettyView with source.kind === "harness" (Slice 1 Task 3 outcome).
    - Test 5 (identity fall-through in TerminalOrIdentitySessionPane): When `tab.sessionKind === "relay-room"` but `tab.relayRoomId` is missing/null, the code falls through to the identity/terminal branches (defensive-fallthrough per the current comment at L198-203).
    - Test 6 (Suspense fallback preserved): The Suspense fallback with `EmptyState icon={TerminalSquare} messageKey="terminal.noHostSelected"` still wraps the relay branch mount.
  </behavior>
  <action>
    Read src/ui/shell/tabUtils.tsx end-to-end (373 lines).

    **Rewire #1: Lazy import at L23-25** — DELETE. The `const RelayRoomSessionPane = lazy(() => import("@/shell/RelayRoomSessionPane") ...)` block retires. Do NOT add a new lazy import for PrettyView — PrettyView is already used transitively via IdentitySessionPane; for the relay branch, add a direct import at the top:

    ```
    import { PrettyView } from "@/features/pretty-view/PrettyView";
    ```

    (Verify PrettyView's export shape — if it's a default export, adjust accordingly. Check via `grep "^export" src/ui/features/pretty-view/PrettyView.tsx` before writing this import.)

    **Rewire #2: TerminalOrIdentitySessionPane relay branch at L204-222** — REPLACE the RelayRoomSessionPane mount with a PrettyView mount:

    ```
    if (tab.sessionKind === "relay-room") {
      if (!tab.relayRoomId) {
        console.warn("relay-room tab missing relayRoomId", { tabId: tab.id });
        // Fall through to the existing dispatcher below (defensive per comment at L198-203).
      } else {
        return (
          <Suspense fallback={<EmptyState icon={TerminalSquare} messageKey="terminal.noHostSelected" />}>
            <PrettyView
              source={{
                kind: "relay",
                roomId: tab.relayRoomId,
                roomTitle: tab.relayRoomTitle ?? null,
              }}
              className="h-full w-full"
              isVisible={isVisible}
            />
          </Suspense>
        );
      }
    }
    ```

    Note: Suspense wrapper preserved to keep the same loading UX (though PrettyView isn't lazy in this render, the Suspense boundary is harmless and matches existing pattern).

    **Rewire #3: renderTabContent case "terminal" early-return at L306-326** — DELETE the entire relay-room early-return branch. Replace with a host-null gate that is aware of `tab.sessionKind === "relay-room"`:

    Read L295-L340 first to see the exact current shape. After deletion, the case "terminal" block reads roughly:

    ```
    case "terminal":
      // Phase 93 Slice 4 (D-06): host-null is a valid state ONLY for
      // relay-room sessionKind. All other terminal-type tabs still require
      // a host. TerminalOrIdentitySessionPane handles the relay branch
      // internally with host-optional handling. The Phase 91 UAT fix's
      // inline RelayRoomSessionPane branch is retired here — the shared
      // chat surface subsumes the standalone pane per D-04.
      if (!host && tab.sessionKind !== "relay-room") {
        return <EmptyState icon={TerminalSquare} messageKey="terminal.noHostSelected" />;
      }
      return (
        <TerminalOrIdentitySessionPane
          tab={tab}
          host={host as Host /* narrowed above OR null-permitted for relay-room via runtime check */}
          {/* ... rest of props unchanged ... */}
        />
      );
    ```

    Because TerminalOrIdentitySessionPane's props declare `host: Host`, widen the signature to `host: Host | null` on TerminalOrIdentitySessionPane's props interface. Inside its body, only the relay branch reads through when `host` is null; the identity + fall-through branches gate on `host != null` narrowing (they already do because they read `host.id`).

    Read the exact signature of TerminalOrIdentitySessionPane at ~L165-195 (props interface + function declaration). Update the `host: Host` prop to `host: Host | null`. Inside the function body, wherever `host.id` or another `host.*` field is read, add a narrowing check `host != null` above the read — or leave the existing reads unchanged because they only fire under `isIdentityPane` which already implicitly requires an identity-resolvable session.

    Do NOT touch AppShell.tsx's onRelayRoomRowClick or openTab call — those keep working with sessionKind: "relay-room" per D-05.

    Do NOT delete anything else in this task. Task 2 does the mass deletion. This task's job is only the dispatcher rewire — after this, real relay-room tabs already route to the shared surface via TerminalOrIdentitySessionPane, but the standalone pane files still exist on disk (just unreachable).

    Update existing tabUtils.test.tsx if the mocks are load-bearing to the tests you're about to run. If tabUtils.test.tsx mocks RelayRoomSessionPane, update the mock to point at PrettyView temporarily — Slice 5 does the full test migration; this task just keeps the file compiling. The minimal fix: change the `vi.mock("@/shell/RelayRoomSessionPane", ...)` to `vi.mock("@/features/pretty-view/PrettyView", () => ({ PrettyView: (props) => <div data-testid="mock-pretty-view" data-source-kind={props.source?.kind}>...</div> }))` and update the two assertion sites at L205, L242 to check for `data-testid="mock-pretty-view"` + `data-source-kind === "relay"`. Slice 5 will refine test naming + coverage.
  </action>
  <verify>
    <automated>npx vitest run src/ui/shell/tabUtils.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RelayRoomSessionPane" src/ui/shell/tabUtils.tsx` returns 0 (import + both mount sites removed).
    - `grep -c "lazy(() => import(\"@/shell/RelayRoomSessionPane" src/ui/shell/tabUtils.tsx` returns 0.
    - `grep -c "kind: \"relay\"" src/ui/shell/tabUtils.tsx` returns at least 1 (new source-prop mount).
    - `grep -c "PrettyView" src/ui/shell/tabUtils.tsx` returns at least 2 (import + JSX mount).
    - `grep -c "tab.sessionKind === \"relay-room\"" src/ui/shell/tabUtils.tsx` returns at least 2 (TerminalOrIdentitySessionPane branch + case "terminal" host-null exception).
    - The early-return block at former L306-326 is REMOVED — verify by absence of a second `<RelayRoomSessionPane` JSX line (already 0 per first criterion).
    - tabUtils.test.tsx compiles and its tests pass (with the mock updated to PrettyView).
    - `npx tsc --noEmit` passes.
    - Manual: the Suspense fallback with `EmptyState icon={TerminalSquare} messageKey="terminal.noHostSelected"` is preserved around the relay branch mount.
  </acceptance_criteria>
  <done>
    Dispatcher routes relay-room tabs to PrettyView with source.kind === "relay". Early-return at former L314 retired. Host-null gate carries a `!== "relay-room"` exception. TerminalOrIdentitySessionPane widened to accept host: Host | null. Harness routing untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete standalone relay-room-pane tree + RelayRoomSessionPane; run retirement grep; clear vitest cache (D-04, Pitfall 3, Pitfall 6)</name>
  <files>
    src/ui/features/relay-room-pane/AgentBadgeWithAppendage.tsx,
    src/ui/features/relay-room-pane/AgentBadgeWithAppendage.test.tsx,
    src/ui/features/relay-room-pane/IdentityBadgeRow.tsx,
    src/ui/features/relay-room-pane/IdentityBadgeRow.test.tsx,
    src/ui/features/relay-room-pane/RelayMessageList.tsx,
    src/ui/features/relay-room-pane/RelayMessageList.test.tsx,
    src/ui/features/relay-room-pane/RelayRoomInboundBubble.tsx,
    src/ui/features/relay-room-pane/RelayRoomInboundBubble.test.tsx,
    src/ui/features/relay-room-pane/RelayRoomPane.tsx,
    src/ui/features/relay-room-pane/RelayRoomPane.test.tsx,
    src/ui/features/relay-room-pane/error-state.tsx,
    src/ui/features/relay-room-pane/error-state.test.tsx,
    src/ui/features/relay-room-pane/relay-room-api.ts,
    src/ui/features/relay-room-pane/use-relay-room-stream.ts,
    src/ui/features/relay-room-pane/use-relay-room-stream.test.ts,
    src/ui/shell/RelayRoomSessionPane.tsx,
    src/ui/shell/RelayRoomSessionPane.test.tsx
  </files>
  <read_first>
    - .planning/phases/93-relay-rooms-use-the-chat-surface-one-surface-two-data-source/93-RESEARCH.md - section "Deletion Graph (D-04)" — inventory of every file to delete + external refs to update
    - .planning/phases/93-relay-rooms-use-the-chat-surface-one-surface-two-data-source/93-PATTERNS.md - section "Shared Patterns - Retirement sweep grep (Pitfall 3 + Pitfall 6 mitigation)" — exact grep commands to run
    - src/ui/features/pretty-view/AgentBadgeWithMeter.tsx (Slice 2 Task 1 port — the replacement exists)
    - src/ui/features/pretty-view/MultiBadgeAnchor.tsx (Slice 2 Task 2 replacement)
    - src/ui/features/pretty-view/sources/use-relay-adapter.ts (Slice 3 Task 1 replacement)
    - src/ui/features/pretty-view/sources/relay-room-api.ts (Slice 3 Task 1 replacement)
    - src/ui/features/pretty-view/ChatSurfaceErrorState.tsx (Slice 3 Task 1 replacement)
    - src/ui/features/pretty-view/RelayInboundBubble.tsx (D-16 shared primitive — both cases use this, NOT the deleted fork; verify by grep)
  </read_first>
  <behavior>
    - Behavior 1: All files listed under files_modified are deleted from disk.
    - Behavior 2: Full-project grep for retiring symbols returns zero hits under src/, tests/, scripts/: `grep -rn "features/relay-room-pane\|shell/RelayRoomSessionPane\|use-relay-room-stream\|RelayRoomInboundBubble\|AgentBadgeWithAppendage\|IdentityBadgeRow\|RelayRoomPane\|RelayMessageList" src/ tests/ scripts/ 2>/dev/null` (comment refs handled separately in Task 3 / Slice 5).
    - Behavior 3: `npx tsc --noEmit` passes — zero dangling type refs.
    - Behavior 4: `npx vitest run` on any of the phase's test suites (pretty-view/, tabUtils, etc.) passes after `rm -rf node_modules/.vitest`. Verify no stale test cache references retired files.
    - Behavior 5: Harness regression floor holds: PrettyView.test.tsx, PrettyView.source-prop.test.tsx, PrettyView.multi-badge.test.tsx, PrettyView.relay-source.test.tsx, PrettyView.compose-send.test.tsx, PrettyView.task-pill.test.tsx, ComposeBox.test.tsx, ComposeBox.mode-hide.test.tsx all pass.
  </behavior>
  <action>
    Verify replacements exist before deleting (Slice 2 + Slice 3 must have shipped):
    - `test -f src/ui/features/pretty-view/AgentBadgeWithMeter.tsx || exit 1`
    - `test -f src/ui/features/pretty-view/MultiBadgeAnchor.tsx || exit 1`
    - `test -f src/ui/features/pretty-view/sources/use-relay-adapter.ts || exit 1`
    - `test -f src/ui/features/pretty-view/sources/relay-room-api.ts || exit 1`
    - `test -f src/ui/features/pretty-view/ChatSurfaceErrorState.tsx || exit 1`
    - `test -f src/ui/features/pretty-view/RelayInboundBubble.tsx || exit 1` (tiffany's shipped primitive; both cases use this, per D-16)

    Delete via `rm` (files are tracked in git and will be recorded as deletions on commit):

    ```
    rm src/ui/features/relay-room-pane/AgentBadgeWithAppendage.tsx
    rm src/ui/features/relay-room-pane/AgentBadgeWithAppendage.test.tsx
    rm src/ui/features/relay-room-pane/IdentityBadgeRow.tsx
    rm src/ui/features/relay-room-pane/IdentityBadgeRow.test.tsx
    rm src/ui/features/relay-room-pane/RelayMessageList.tsx
    rm src/ui/features/relay-room-pane/RelayMessageList.test.tsx
    rm src/ui/features/relay-room-pane/RelayRoomInboundBubble.tsx
    rm src/ui/features/relay-room-pane/RelayRoomInboundBubble.test.tsx
    rm src/ui/features/relay-room-pane/RelayRoomPane.tsx
    rm src/ui/features/relay-room-pane/RelayRoomPane.test.tsx
    rm src/ui/features/relay-room-pane/error-state.tsx
    rm src/ui/features/relay-room-pane/error-state.test.tsx
    rm src/ui/features/relay-room-pane/relay-room-api.ts
    rm src/ui/features/relay-room-pane/use-relay-room-stream.ts
    rm src/ui/features/relay-room-pane/use-relay-room-stream.test.ts
    rmdir src/ui/features/relay-room-pane 2>/dev/null || true

    rm src/ui/shell/RelayRoomSessionPane.tsx
    rm src/ui/shell/RelayRoomSessionPane.test.tsx
    ```

    Run the exhaustive retirement grep. Must return zero HITS from src/, tests/, scripts/ EXCEPT possibly comment-only references (those are for Slice 5 or handled in Task 3 below):

    ```
    grep -rn "features/relay-room-pane\|shell/RelayRoomSessionPane\|use-relay-room-stream\|RelayRoomInboundBubble\|AgentBadgeWithAppendage\|IdentityBadgeRow\|RelayRoomPane\|RelayMessageList" src/ tests/ scripts/ 2>/dev/null
    ```

    Investigate every hit. Expected non-code hits (comment refs to be handled in Task 3 or Slice 5):
    - src/ui/AppShell.tsx (comment refs at L1448, L2156)
    - src/ui/state/conversation-store.ts (comment at L193)
    - src/ui/state/viewing-user-store.ts (JSDoc at L6-10, L148, L167)
    - src/ui/api/fleet-status-client.ts (comment at L292)
    - src/backend/relay-room-stream/matrix-message-fetch.ts (comment at L106)

    If ANY hit is a real import (not a comment), that's a Pitfall 3 miss — investigate before proceeding. Real imports are load-bearing and blocking.

    Clear vitest cache staleness per RESEARCH.md § Runtime State Inventory:

    ```
    rm -rf node_modules/.vitest
    ```

    Run scoped vitest to catch any silent regression: `npx vitest run src/ui/features/pretty-view/ src/ui/shell/tabUtils.test.tsx`.

    Run tsc: `npx tsc --noEmit`. Zero errors required.
  </action>
  <verify>
    <automated>bash -c 'GREP_RESULT=$(grep -rn "features/relay-room-pane\|shell/RelayRoomSessionPane\|use-relay-room-stream\|RelayRoomInboundBubble\|AgentBadgeWithAppendage\|IdentityBadgeRow\|RelayRoomPane\|RelayMessageList" src/ tests/ scripts/ 2>/dev/null | grep -v "^[^:]*:[0-9]*: *//\|^[^:]*:[0-9]*: *\*" | grep -v "\.planning/"); [ -z "$GREP_RESULT" ] || { echo "Non-comment refs found:"; echo "$GREP_RESULT"; exit 1; }' && npx tsc --noEmit && npx vitest run src/ui/features/pretty-view/ src/ui/shell/tabUtils.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Directory `src/ui/features/relay-room-pane/` does not exist (or is empty).
    - Files `src/ui/shell/RelayRoomSessionPane.tsx` and `src/ui/shell/RelayRoomSessionPane.test.tsx` do not exist.
    - `git status` shows 17 deletions matching the files_modified list.
    - `grep -rn "features/relay-room-pane\|shell/RelayRoomSessionPane\|use-relay-room-stream\|RelayRoomInboundBubble\|AgentBadgeWithAppendage\|IdentityBadgeRow\|RelayRoomPane\|RelayMessageList" src/ tests/ scripts/ 2>/dev/null | grep -v "^[^:]*:[0-9]*: *//" | grep -v "^[^:]*:[0-9]*: *\\*"` returns ONLY comment-line hits (5 known sites listed in read_first) — no import lines, no JSX references, no vi.mock references.
    - `npx tsc --noEmit` passes.
    - `npx vitest run src/ui/features/pretty-view/ src/ui/shell/tabUtils.test.tsx` passes.
    - Vitest cache cleared — `test ! -d node_modules/.vitest` (or the cache regenerated fresh).
    - Harness regression floor: PrettyView.test.tsx passes with zero snapshot diffs vs. master.
  </acceptance_criteria>
  <done>
    17 files deleted (8 production + 7 tests in relay-room-pane/ + RelayRoomSessionPane.tsx + its test). Retirement grep clean of non-comment references. tsc + scoped vitest green.
  </done>
</task>

<task type="auto">
  <name>Task 3: End-to-end verification — real relay-room tab dispatches to PrettyView shared surface</name>
  <files>
    src/ui/shell/tabUtils.test.tsx
  </files>
  <read_first>
    - src/ui/shell/tabUtils.tsx (Task 1 output)
    - src/ui/shell/tabUtils.test.tsx (existing test file — read the current assertions at L205 + L242 that need to be updated)
    - src/ui/features/pretty-view/PrettyView.relay-source.test.tsx (Slice 3 Task 3 — the end-to-end relay-case rendering test)
    - .planning/phases/93-relay-rooms-use-the-chat-surface-one-surface-two-data-source/93-PATTERNS.md - section "src/ui/shell/tabUtils.test.tsx (TEST-MIGRATE)" — the migration target
    - .planning/phases/93-relay-rooms-use-the-chat-surface-one-surface-two-data-source/93-CONTEXT.md D-21
  </read_first>
  <behavior>
    - Test 1: Given a Tab with sessionKind === "relay-room" + relayRoomId, `renderTabContent(tab)` renders PrettyView with source.kind === "relay". Verified via testid + data-source-kind attribute on a mocked PrettyView.
    - Test 2: Given a Tab with sessionKind === "harness" (or absent), `renderTabContent(tab)` renders through TerminalOrIdentitySessionPane and (for identity panes) IdentitySessionPane → PrettyView with source.kind === "harness".
    - Test 3: renderTabContent for a relay-room tab with type: "terminal" + host: null returns the PrettyView mount (via TerminalOrIdentitySessionPane's relay branch), NOT the "no host selected" EmptyState.
    - Test 4: renderTabContent for a non-relay-room terminal tab with host: null returns the "no host selected" EmptyState (harness regression floor).
  </behavior>
  <action>
    Update src/ui/shell/tabUtils.test.tsx to reflect the new dispatch shape per D-21 + PATTERNS.md § "src/ui/shell/tabUtils.test.tsx (TEST-MIGRATE)":

    1. Replace the `vi.mock("@/shell/RelayRoomSessionPane", ...)` block with `vi.mock("@/features/pretty-view/PrettyView", () => ({ PrettyView: (props: any) => <div data-testid="mock-pretty-view" data-source-kind={props.source?.kind ?? "none"} data-room-id={props.source?.roomId ?? ""}>{"PV"}</div> }))`.

    2. Update the JSDoc references at L8 + L16 (per RESEARCH.md § Deletion Graph external refs) from "RelayRoomSessionPane" mentions to "PrettyView with source.kind === 'relay'".

    3. Update the two assertion sites at L205, L242:
       - BEFORE: `expect(screen.getByTestId("mock-relay-room-session-pane")).toBeInTheDocument();`
       - AFTER: `const pv = screen.getByTestId("mock-pretty-view"); expect(pv).toHaveAttribute("data-source-kind", "relay");`

    4. Add Test 1 through Test 4 above as new assertions if not already covered by existing tests.

    Preserve every existing tabUtils.test.tsx assertion that isn't about relay-room routing. The harness and identity-pane assertions must stay unchanged (regression floor per D-05).

    Do NOT touch any other file in this task.
  </action>
  <verify>
    <automated>npx vitest run src/ui/shell/tabUtils.test.tsx src/ui/features/pretty-view/PrettyView.relay-source.test.tsx src/ui/features/pretty-view/PrettyView.source-prop.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mock-relay-room-session-pane" src/ui/shell/tabUtils.test.tsx` returns 0 (retired testid).
    - `grep -c "mock-pretty-view" src/ui/shell/tabUtils.test.tsx` returns at least 2 (mock + assertion).
    - `grep -c "data-source-kind" src/ui/shell/tabUtils.test.tsx` returns at least 1 (dispatch-target assertion).
    - `grep -c "RelayRoomSessionPane" src/ui/shell/tabUtils.test.tsx` returns 0 (no dangling refs including in JSDoc).
    - Test 1 through Test 4 above pass.
    - `npx vitest run src/ui/shell/tabUtils.test.tsx` green.
    - Existing tabUtils harness + identity-pane tests pass unchanged.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>
    tabUtils.test.tsx migrated to assert dispatch to PrettyView with relay source. Harness/identity assertions unchanged. End-to-end dispatch verified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tab type discriminator -> component tree | tabUtils dispatcher branches on `tab.sessionKind` to decide which component tree renders. |
| Deleted-tree dangling references | External files (AppShell, conversation-store, viewing-user-store, fleet-status-client, backend matrix-message-fetch) may reference retired symbols in comments (Pitfall 6). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-04-01 | Denial of Service via dispatcher misdispatch | tabUtils.tsx dispatcher | mitigate | Task 1 preserves the sessionKind === "relay-room" branch (D-05) and the defensive-fallthrough when relayRoomId is missing. Task 3 asserts end-to-end dispatch behavior with tests. Harness routing untouched (Task 1 acceptance criteria explicitly verify identity-pane path still renders IdentitySessionPane -> PrettyView with harness source). |
| T-92-04-02 | Tampering via dangling imports (Pitfall 3) | Post-deletion state | mitigate | Task 2 runs the exhaustive retirement grep as an acceptance criterion (returns zero code refs; only known comment-line hits at 5 sites are allowed). `npx tsc --noEmit` catches any dangling type refs. Vitest cache cleared to catch stale test-file references. |
| T-92-04-03 | Information Disclosure via comment references (Pitfall 6) | Stale comment refs in AppShell, conversation-store, viewing-user-store, fleet-status-client, matrix-message-fetch | accept | Comments referencing retired symbols are HARMLESS to runtime but confusing. Slice 5 sweeps them. This slice's retirement grep explicitly separates code hits (blocking) from comment hits (accepted, Slice-5 owned). |
| T-92-04-04 | Denial of Service via host: null regression | TerminalOrIdentitySessionPane props widening | mitigate | Task 1 widens `host: Host` to `host: Host | null`. Inside TOISP body, existing host reads (host.id) only fire in the identity branch which implicitly requires an identity-resolvable session; adding a `host != null` narrowing is defensive. Task 3 Test 3 asserts the relay-room tab renders PrettyView (not EmptyState) with host === null. Task 3 Test 4 asserts non-relay-room terminal tab with host === null still renders EmptyState (harness regression floor). |
| T-92-04-05 | Repudiation via lost Suspense fallback | tabUtils.tsx relay branch | mitigate | Task 1 preserves the Suspense fallback with `EmptyState icon={TerminalSquare} messageKey="terminal.noHostSelected"` around the new PrettyView mount. Acceptance criteria include this manual check. |
| T-92-04-06 | Information Disclosure via structured logs | Retirement grep results | mitigate | Retirement grep script only lists retiring symbol names + paths, never file contents. No sensitive data logged. |
| T-92-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages. Package Legitimacy Audit N/A. |
</threat_model>

<verification>
- `npx vitest run src/ui/shell/tabUtils.test.tsx src/ui/features/pretty-view/` — this slice + all prior slices' regression floor.
- `npx tsc --noEmit` — zero dangling type refs after deletion.
- Retirement grep clean of code references (comment hits acceptable, deferred to Slice 5).
- `rm -rf node_modules/.vitest` and re-run vitest to confirm cache doesn't hide staleness.
- Harness case regression floor: PrettyView.test.tsx + PrettyView.task-pill.test.tsx + PrettyView.compose-send.test.tsx + PrettyView.autoplay.test.tsx unchanged.
- Real relay-room tab now routes to the shared surface end-to-end (Test 1 in Task 3).
</verification>

<success_criteria>
- tabUtils.tsx relay branch mounts `<PrettyView source={{ kind: "relay", roomId, roomTitle }} />` (D-04).
- Early-return at former L306-326 retired; host-null gate has `!== "relay-room"` exception (D-06).
- TerminalOrIdentitySessionPane widened to accept host: Host | null.
- All 17 retiring files deleted (`src/ui/features/relay-room-pane/*` + RelayRoomSessionPane.tsx + tests).
- Retirement grep clean of code references.
- `npx tsc --noEmit` passes.
- tabUtils.test.tsx asserts dispatch to PrettyView with source.kind === "relay".
- Harness case renders byte-identically.
- Real relay-room tabs now use the shared chat surface end-to-end.
- Vitest cache cleared.
</success_criteria>

<output>
Create `.planning/phases/93-relay-rooms-use-the-chat-surface-one-surface-two-data-source/92-04-SUMMARY.md` when done.
</output>
