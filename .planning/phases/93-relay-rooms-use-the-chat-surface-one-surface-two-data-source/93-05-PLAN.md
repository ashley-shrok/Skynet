---
phase: 93-relay-rooms-use-the-chat-surface-one-surface-two-data-source
plan: 05
type: execute
wave: 5
depends_on:
  - 92-01
  - 92-02
  - 92-03
  - 92-04
files_modified:
  # The comment-sweep working set is DETERMINED AT EXECUTION TIME by the
  # grep-enumerate-first Step 0 (Blocker 4). The files below are the KNOWN
  # sites researched at plan-authoring time; if the grep enumeration finds
  # additional hits, those files also count as modified by this task.
  - src/ui/AppShell.tsx
  - src/ui/state/conversation-store.ts
  - src/ui/state/viewing-user-store.ts
  - src/ui/api/fleet-status-client.ts
  - src/backend/relay-room-stream/matrix-message-fetch.ts
  - src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx
autonomous: true
requirements:
  - D-14
  - D-21
  - Pitfall 4
  - Pitfall 6
tags:
  - comment-sweep
  - test-migration
  - optimistic-bubble-parity
  - final-cleanup
  - slice-5
must_haves:
  truths:
    - "Every source-file comment reference to retired symbols (RelayRoomSessionPane, RelayRoomPane, RelayMessageList, IdentityBadgeRow, AgentBadgeWithAppendage, RelayRoomInboundBubble, use-relay-room-stream, relay-room-pane) is updated to reference the current shared-surface names OR deleted if the surrounding context no longer applies (Pitfall 6)."
    - "The working set for the comment-sweep is enumerated BEFORE editing via a grep-enumerate-first Step 0 (Blocker 4). Every enumerated hit is resolved (edited or reported as false positive with justification)."
    - "Full-project comment-sweep grep returns zero hits under src/: `grep -rn 'RelayRoomSessionPane\\|RelayRoomPane\\|RelayMessageList\\|IdentityBadgeRow\\|AgentBadgeWithAppendage\\|RelayRoomInboundBubble\\|use-relay-room-stream\\|relay-room-pane' src/`."
    - "Optimistic-bubble parity test lands in `PrettyView.optimistic-bubbles.test.tsx` (or extends the existing file if present): asserts a relay-case send emits an optimistic bubble on send-attempt (D-14 fleet rule), matched-and-replaced by live_event echo via Matrix `unsigned.transaction_id` correlation (Pitfall 4 regression floor — Test 5 of the retired use-relay-room-stream.test.ts)."
    - "Full harness case regression floor holds: every PrettyView + ComposeBox + IdentityBadge + IdentitySessionPane + tabUtils test passes unchanged from master."
    - "`npx tsc --noEmit` passes."
    - "Scoped vitest against all touched paths passes clean."
  artifacts:
    - path: "src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx"
      provides: "Relay-case optimistic-bubble emission + Matrix unsigned.transaction_id echo correlation regression test (Pitfall 4). Ports Test 5 of the retired use-relay-room-stream.test.ts to the shared-surface level per D-21."
      contains: "describe(\"PrettyView optimistic bubbles"
  key_links:
    - from: "src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx"
      to: "src/ui/features/pretty-view/sources/use-relay-adapter.ts"
      via: "assertions read adapter.sendMessage + adapter.messages pending/echo state"
      pattern: "adapter.sendMessage\\|unsigned.transaction_id"
---

<objective>
Close out the phase. Two small motions:

1. **Comment sweep (Pitfall 6):** Update every stale-comment site left over from Slice 4 retirement. The known sites from RESEARCH.md's deletion graph are AppShell.tsx L1448/L2156, conversation-store.ts L193, viewing-user-store.ts JSDoc/L148/L167, fleet-status-client.ts L292, matrix-message-fetch.ts L106 — BUT the plan does NOT trust these line numbers as an exhaustive working set (Blocker 4 discipline). Instead, Task 1 begins with a grep-enumerate-first Step 0 that produces the authoritative working set from the current source tree. Every enumerated hit is then resolved (edited to remove the retired name, or reported as a false positive with justification).

2. **Optimistic-bubble parity test (D-14 + D-21 + Pitfall 4):** Land the load-bearing regression test at the shared-surface level. The retired `use-relay-room-stream.test.ts` contained Test 5 (per its file header L50-57 + RESEARCH.md Pitfall 4) asserting Matrix `unsigned.transaction_id` echo correlation — pending removed when the live event echoes back. Slice 3 Task 1 ported this test into `use-relay-adapter.test.ts` at the hook level; this slice adds the equivalent assertion at the PrettyView-composed level so the FULL send → optimistic bubble → echo → replace pipeline is covered end-to-end.

Purpose: Slice 4 shipped retirement. Slice 5 closes the loop — no dangling comment refs (auditable via grep-enumerate-first), full test coverage of the load-bearing send/echo path at the level real users experience it (through the shared surface, not the isolated adapter).

Output: enumerated + resolved comment updates + 1 new test file (or extended existing one), scoped vitest green, full retirement grep (including comments) returns zero hits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-relay-rooms-use-the-chat-surface-one-surface-two-data-source/93-CONTEXT.md
@.planning/phases/93-relay-rooms-use-the-chat-surface-one-surface-two-data-source/93-RESEARCH.md
@.planning/phases/93-relay-rooms-use-the-chat-surface-one-surface-two-data-source/93-PATTERNS.md
@.planning/phases/93-01-SUMMARY.md
@.planning/phases/92-02-SUMMARY.md
@.planning/phases/92-03-SUMMARY.md
@.planning/phases/92-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep every stale-comment site (Pitfall 6) — grep-enumerate-first, resolve-every-hit</name>
  <files>
    src/ui/AppShell.tsx,
    src/ui/state/conversation-store.ts,
    src/ui/state/viewing-user-store.ts,
    src/ui/api/fleet-status-client.ts,
    src/backend/relay-room-stream/matrix-message-fetch.ts
  </files>
  <read_first>
    - .planning/phases/93-relay-rooms-use-the-chat-surface-one-surface-two-data-source/93-RESEARCH.md - section "Deletion Graph - External refs that are file COMMENTS ONLY" (the known-at-authoring-time sites)
    - .planning/phases/93-relay-rooms-use-the-chat-surface-one-surface-two-data-source/93-PATTERNS.md - section "Shared Patterns - Retirement sweep grep (Pitfall 3 + Pitfall 6 mitigation)"
    - (Per-file read_first entries below are DERIVED from Step 0's grep output — do not pre-open files by line number until the enumeration produces the authoritative list.)
  </read_first>
  <behavior>
    - Behavior 1 (working set enumeration, Blocker 4): Step 0 grep produces a full list of stale-reference hits under src/. Every hit in that list is resolved by end of task.
    - Behavior 2: `grep -rn "RelayRoomSessionPane\|RelayRoomPane\|RelayMessageList\|IdentityBadgeRow\|AgentBadgeWithAppendage\|RelayRoomInboundBubble\|use-relay-room-stream\|relay-room-pane" src/` returns zero hits at task completion.
    - Behavior 3: Every touched file compiles cleanly (`npx tsc --noEmit`).
    - Behavior 4: Every touched file's runtime behavior is unchanged — comment edits are strictly non-functional.
    - Behavior 5: The updated comments accurately describe the current shared-surface architecture: relay-room tabs render through PrettyView with source.kind === "relay"; participants render via MultiBadgeAnchor; messages render through the shared message-list; the WS is owned by useRelayAdapter under pretty-view/sources/.
  </behavior>
  <action>
    **Step 0 (grep-enumerate-first, MANDATORY per Blocker 4 — run BEFORE editing anything):**

    ```
    grep -rn 'RelayRoomSessionPane\|RelayRoomPane\|RelayMessageList\|IdentityBadgeRow\|AgentBadgeWithAppendage\|RelayRoomInboundBubble\|use-relay-room-stream\|relay-room-pane' src/ > /tmp/92-05-sweep-hits.txt
    ```

    The output of this grep IS the working set for this task. Every line in the output must be resolved before task completion. Do NOT rely on the plan-authored line numbers below (they were researched at authoring time and MAY have shifted due to Slice 4 retirement edits — Blocker 4 explicitly forbids trusting them).

    **Step 1 (classify each hit):** For each line in `/tmp/92-05-sweep-hits.txt`, classify it:
    - **(a) Stale comment referencing a retired symbol** — the intended target of the sweep. EDIT the comment per Step 2 substance guide.
    - **(b) False positive** — the match is inside a string literal that must remain (e.g., a data-testid string historically named `"relay-room-pane"` that a test still references, but that testid is intentional and unchanged by Phase 93). REPORT the false positive in the completion note with justification. Slice 4's retirement should have already handled testid renames; if a false positive is legitimately unchangeable, escalate to the developer before hand-waving.
    - **(c) Missed retirement** — the match is INSIDE PRODUCTION CODE (an import, a JSX use, a type ref, an identifier) that Slice 4 should have retired. This is a Slice 4 defect surfaced by Slice 5. STOP and file a Slice 4 gap-closure task; do not paper over.

    **Step 2 (substance guide for each stale-comment site — apply the Step 0 hits, not the pre-authored L-numbers):**

    The following are the KNOWN sites from RESEARCH.md's authoring-time survey. Use them as guidance for the substance of the rewrite, but the AUTHORITATIVE working set is Step 0's grep output.

    **A. src/ui/AppShell.tsx — dispatcher branch commentary (originally near L1448)** — the comment describes tabUtils's dispatcher branching. Update to: `// so tabUtils's dispatcher branches to the shared chat surface (PrettyView with source.kind === "relay") per Phase 93.`

    **B. src/ui/AppShell.tsx — openTab-with-null-host commentary (originally near L2156)** — the comment describes relay-room tabs having no fleet host. Update to reference "the shared chat surface (PrettyView with relay source)" preserving the surrounding narrative about relay-room tabs having no fleet host.

    **C. src/ui/state/conversation-store.ts — mount-target commentary (originally near L193)** — the comment references "either PrettyView or RelayRoomSessionPane". Update to: `// PrettyView (either source variant: harness or relay per Phase 93).`

    **D. src/ui/state/viewing-user-store.ts — three refs originally at L6-10 JSDoc, L148, L167:**
       - JSDoc header block: rewrite to reference "MultiBadgeAnchor's D-03 self-exclusion filter" (replacing "IdentityBadgeRow's D-07 self-exclusion filter") and "PrettyView's shared inbound-vs-outbound bubble discrimination via RelayInboundBubble" (replacing "RelayMessageList's inbound-vs-outbound bubble discrimination"). Preserve the semantic points (viewing user is not rendered as a participant badge; viewing user's own messages render as outbound bubbles, others as inbound).
       - Validation guard comment (originally near L148): update to reference "PrettyView with relay source does this validation via useRelayAdapter" or equivalent — but ONLY IF Step 0's grep confirms a matching hit at or near L148. If Step 0's grep does NOT surface a hit at this vicinity, treat the plan-authored L148 as a stale researcher pointer and skip it (Blocker 4: do not edit lines the enumeration did not surface).
       - Structured-logs comment (originally near L167): update to reference "useRelayAdapter" — but ONLY IF Step 0's grep confirms a matching hit. Same discipline as above.

    **E. src/ui/api/fleet-status-client.ts — future-tense reference (originally near L292)** — currently: `//   - Plan 06 AgentBadgeWithAppendage (future): reads the SAME hook so the`. Update to: `//   - AgentBadgeWithMeter (src/ui/features/pretty-view/AgentBadgeWithMeter.tsx per Phase 93): reads the SAME hook so the` (or trim the future-tense wording since AgentBadgeWithMeter now exists in the present).

    **F. src/backend/relay-room-stream/matrix-message-fetch.ts — extractBody narrative (originally near L106)** — currently: `// events used to fall through to \`RelayMessageList.extractBody\` and`. Update to reference the current consumer of extracted body content per Slice 3's port. If `extractBody` moved into `useRelayAdapter`'s MatrixEvent -> StreamEvent mapper (Slice 3 Task 1), reference that; if it moved into `matrix-message-fetch.ts` itself, note the internalization. If the comment is stale-narrative that no longer describes any real path, delete the comment entirely rather than write fiction.

    **Step 3 (resolve any additional hits Step 0 surfaced that aren't in A-F):** For each additional stale-comment hit surfaced by Step 0's grep, rewrite the comment following the same discipline: name the current shared-surface architecture (PrettyView with source.kind === "relay", MultiBadgeAnchor, useRelayAdapter, AgentBadgeWithMeter, RelayInboundBubble). Do not invent architectural claims. Do not change runtime behavior.

    **Rules of engagement:**
    - Comment-only edits. No runtime behavior change. `git diff` should show only comment-line changes.
    - Do NOT invent architectural claims. Only refresh the naming to reflect Slices 1-4's actual output.
    - If a comment's original context (e.g., "in the future, feature X will do Y") no longer applies because the feature shipped, replace with present-tense description of the current state — or delete the sentence.
    - Preserve any Ashley-verbatim, Phase-XX-reference, or ticket-ID markers in the surrounding comment text.
    - **Report the enumeration in the completion note**: attach the contents of `/tmp/92-05-sweep-hits.txt` (or its equivalent) plus the per-hit classification (edited / false-positive / escalated) so the checker can audit that every hit was resolved.
  </action>
  <verify>
    <automated>bash -c 'grep -rn "RelayRoomSessionPane\|RelayRoomPane\|RelayMessageList\|IdentityBadgeRow\|AgentBadgeWithAppendage\|RelayRoomInboundBubble\|use-relay-room-stream\|relay-room-pane" src/ 2>/dev/null; RC=$?; if [ $RC -eq 0 ]; then echo "STALE REFS REMAINING (above)"; exit 1; fi; exit 0' && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - **Step 0 grep-enumerate-first output is attached to the completion note** (Blocker 4 discipline). The plan's acceptance is verifiable against a known starting set: the executor's completion note MUST include the full `/tmp/92-05-sweep-hits.txt` contents (or equivalent) AND a per-hit classification (edited to remove the retired name / false-positive with justification / escalated as Slice 4 defect).
    - `grep -rn "RelayRoomSessionPane\|RelayRoomPane\|RelayMessageList\|IdentityBadgeRow\|AgentBadgeWithAppendage\|RelayRoomInboundBubble\|use-relay-room-stream\|relay-room-pane" src/` returns zero hits at task completion.
    - `git diff` for every touched file shows ONLY comment-line changes (no JSX, no function-body, no type, no import changes). Verified via `git diff --stat` and manual inspection.
    - `npx tsc --noEmit` passes.
    - All PrettyView + ComposeBox + tabUtils + IdentitySessionPane + AppShell tests pass unchanged (behavior unchanged; comments only).
  </acceptance_criteria>
  <done>
    Step 0 grep enumeration performed and attached to the completion note. Every hit resolved (edited / false-positive-with-justification / escalated). Full-project retirement grep returns zero hits. tsc clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Land optimistic-bubble parity test at the PrettyView-composed level (D-14, Pitfall 4)</name>
  <files>
    src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx
  </files>
  <read_first>
    - src/ui/features/pretty-view/PrettyView.tsx (handleComposeSend at L1176+ post-Slice-3 for the case-selected send; adapter.sendMessage wiring)
    - src/ui/features/pretty-view/sources/use-relay-adapter.test.ts (Slice 3 Task 1 — contains hook-level Test 5 correlation assertion; this task lifts it up one level)
    - src/ui/features/pretty-view/ComposeBox.tsx (the mqid generation site — verify where mqid originates so the test can spy on the produced value)
    - src/ui/features/pretty-view/RelayInboundBubble.tsx (D-16 primitive — outbound bubble render for the viewer's own messages routes through OutboundBubble at src/ui/components/OutboundBubble.tsx per Slice D Plan 02)
    - src/ui/components/OutboundBubble.tsx (the sending/failed state renderer — how optimistic bubbles show up)
    - .planning/phases/93-relay-rooms-use-the-chat-surface-one-surface-two-data-source/93-RESEARCH.md - section "Common Pitfalls - Pitfall 4" (Matrix unsigned.transaction_id echo correlation) + section "Common Pitfalls - Pitfall 5" (optimistic-bubble timing skew — D-14 fleet rule)
    - .planning/phases/93-relay-rooms-use-the-chat-surface-one-surface-two-data-source/93-CONTEXT.md D-14, D-21
  </read_first>
  <behavior>
    - Test 1 (optimistic bubble emitted on send-attempt, D-14): Mount PrettyView with source.kind === "relay" + a mocked useRelayAdapter that spies on sendMessage. Trigger a send via ComposeBox (fireEvent Enter with "hello"). Assert:
      1. adapter.sendMessage called once with ("hello", <some-mqid-string>).
      2. An outbound bubble renders with content "hello" and state "sending" (or the equivalent DOM signature — check OutboundBubble's sending-state class).
    - Test 2 (echo correlation replaces optimistic, Pitfall 4): After Test 1's send, dispatch a mocked live_event through the adapter with sender === viewingUserMxid AND `event.unsigned.transaction_id === <the-mqid-from-Test-1>`. Assert:
      1. The optimistic bubble's "sending" state clears (either the pending record is removed and the real event replaces it, or the bubble transitions to a non-pending render).
      2. The message-list shows exactly ONE bubble for "hello" (not two — the optimistic wasn't duplicated by the echo).
    - Test 3 (timeout -> failed state, D-14): Trigger a send. Do NOT dispatch a live_event echo. Advance timers by 20 seconds (vi.useFakeTimers + vi.advanceTimersByTime). Assert the optimistic bubble transitions to "failed" state.
    - Test 4 (fail-immediately when WS not open): Mount with adapter simulating WS not open (adapter.sendMessage seeds pending as failed synchronously). Trigger send. Assert the failed-state bubble appears immediately (no 20s wait).
    - Test 5 (mqid preservation end-to-end): The mqid ComposeBox generates flows byte-for-byte through PrettyView.handleComposeSend -> adapter.sendMessage. Assert via spy that the mqid argument to sendMessage matches the mqid ComposeBox included in its send event.
    - Test 6 (harness case unchanged): Mount PrettyView with source.kind === "harness". Trigger a send. Assert adapter.sendMessage is NOT called; the harness onSend prop IS called with the text + mqid.
  </behavior>
  <action>
    Check for an existing `src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx` first:

    ```
    ls src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx 2>/dev/null
    ```

    Two paths:

    **Path A — file exists:** Extend it with the 6 behaviors above under a new `describe("relay source", ...)` block. Preserve every existing assertion (harness case optimistic bubble tests presumably already there).

    **Path B — file does NOT exist:** Create it from scratch with two describe blocks: `describe("harness source", ...)` covering existing behavior at the shared-surface level (mount with source.kind === "harness", trigger send, assert onSend called and optimistic bubble renders), and `describe("relay source", ...)` covering Tests 1-5 above.

    Test scaffolding pattern:

    - Use `vi.mock("@/features/pretty-view/sources/use-relay-adapter", () => ({ useRelayAdapter: vi.fn() }))` to control adapter state per test.
    - Provide a factory function that returns a controllable adapter mock with methods `sendMessage(spy)`, `pushMessage(event)`, `setError(err)` — the test manipulates the mock like a mini-state machine.
    - Use `vi.useFakeTimers()` for Test 3's 20-second timer advance.
    - For Test 5, the byte-for-byte mqid preservation, capture ComposeBox's generated mqid via a spy on the compose-send event OR by reading the mqid from the sendMessage spy's first-call arguments and comparing to a spy on ComposeBox's internal generation (if not observable, assert only that sendMessage received A NON-EMPTY mqid string matching the `pv-optim-` prefix pattern used by ComposeBox — per PrettyView L314 comment).

    Note on OutboundBubble's rendering: the "sending" and "failed" visual states are captured in OutboundBubble's DOM output; verify by reading OutboundBubble.tsx first to know which class/testid marks each state.

    Do NOT modify any production file in this task. This is a test-migration task per D-21.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx src/ui/features/pretty-view/PrettyView.test.tsx src/ui/features/pretty-view/PrettyView.source-prop.test.tsx src/ui/features/pretty-view/PrettyView.multi-badge.test.tsx src/ui/features/pretty-view/PrettyView.relay-source.test.tsx src/ui/features/pretty-view/sources/use-relay-adapter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx` exists (either newly created or extended).
    - `grep -c "describe(\"relay source\"\|describe.*relay-source\|it.*optimistic bubble.*relay" src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx` returns at least 1.
    - `grep -c "unsigned.transaction_id\|unsigned\\?\\.transaction_id" src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx` returns at least 1 (Pitfall 4 correlation coverage).
    - `grep -c "vi.useFakeTimers\|vi\\.advanceTimersByTime" src/ui/features/pretty-view/PrettyView.optimistic-bubbles.test.tsx` returns at least 1 (timeout test).
    - Test 1 through Test 6 pass.
    - Existing PrettyView tests unchanged and green.
    - Existing `use-relay-adapter.test.ts` still green (hook-level Test 5 still asserts the same correlation at the lower level).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>
    Optimistic-bubble parity + Matrix echo correlation asserted at both the hook level (Slice 3 Task 1) AND the PrettyView-composed level (Slice 5 Task 2). Pitfall 4 has redundant regression coverage.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source-file comment references -> future readers | Stale references confuse future maintainers but have no runtime effect. |
| Test-composed send/echo pipeline -> production code | Tests exercise the same production path a real user would; regression coverage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-05-01 | Information Disclosure via stale comments | Every stale-comment site in src/ | mitigate | Task 1 grep-enumerate-first Step 0 (Blocker 4) produces the authoritative working set. Every enumerated hit is resolved (edited / false-positive / escalated). Task 1 acceptance criteria include the full-project grep for retired symbol names returning zero hits AND the completion note attaching the enumeration output. Future readers get accurate architecture from comments. |
| T-92-05-02 | Repudiation via Matrix echo correlation drift (Pitfall 4) | Slice 3's use-relay-adapter port | mitigate | Task 2 lands the shared-surface-level end-to-end regression test for the send -> optimistic -> echo -> replace pipeline. Redundant with the hook-level assertion in use-relay-adapter.test.ts (Slice 3 Task 1). If Pitfall 4 regresses in the future, both test files fail loudly. |
| T-92-05-03 | Denial of Service via optimistic-bubble timing regression (Pitfall 5) | send-path | mitigate | Task 2 Test 1 asserts optimistic emission on send-attempt (not after WS ack). D-14 rule preserved. Test 3 asserts 20s timeout -> failed state. Test 4 asserts fail-immediately when WS not open. |
| T-92-05-04 | Tampering via test-only mock permitting non-existent state | Task 2 test mocks | accept | Test mocks a factory that returns a controllable adapter. Mock permits states the real adapter would produce (pending, echoed, failed, ws-closed). If the mock permits an impossible state, the test doesn't catch a real bug — but tests still cover the real happy path. Accept as low-severity. |
| T-92-05-05 | Tampering via missed comment-sweep hit (Blocker 4) | Task 1 working-set enumeration | mitigate | Task 1 mandates a grep-enumerate-first Step 0 that produces the authoritative working set BEFORE editing. Every hit must be resolved (edited / false-positive / escalated). The completion note attaches the enumeration for auditability. Missing a hit is detected by the final full-project grep in the automated verify step. |
| T-92-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages. Package Legitimacy Audit N/A. |
</threat_model>

<verification>
- `npx vitest run src/ui/features/pretty-view/ src/ui/shell/tabUtils.test.tsx src/ui/shell/IdentitySessionPane.test.tsx` — full phase-scoped test suite green.
- `grep -rn "RelayRoomSessionPane\|RelayRoomPane\|RelayMessageList\|IdentityBadgeRow\|AgentBadgeWithAppendage\|RelayRoomInboundBubble\|use-relay-room-stream\|relay-room-pane" src/ 2>/dev/null` returns zero hits.
- `npx tsc --noEmit` clean.
- Harness case: byte-identical DOM at every extension anchor.
- Real relay-room tabs: use the shared chat surface, participants render via MultiBadgeAnchor, messages via message-list, send via useRelayAdapter, error state via ChatSurfaceErrorState, all case-selection driven by `source.kind`.
</verification>

<success_criteria>
- Task 1's Step 0 grep-enumerate-first output attached to the completion note; every enumerated hit resolved (edited / false-positive / escalated).
- Every stale-comment site rewritten to reference current shared-surface architecture.
- Full-project retirement grep (including comments) returns zero hits.
- Optimistic-bubble parity test lands at the PrettyView-composed level with the 6 behaviors above.
- Every phase-scoped test passes.
- `npx tsc --noEmit` passes.
- Harness case still byte-identical everywhere.
- Real relay-room tabs use the shared chat surface end-to-end.
- Phase objective met: one chat surface, two data sources.
</success_criteria>

<output>
Create `.planning/phases/93-relay-rooms-use-the-chat-surface-one-surface-two-data-source/92-05-SUMMARY.md` when done.
</output>
