---
phase: 133-retire-bounties-concept-from-skynet-remove-rolemodal-bountie
plan: 03
type: execute
wave: 3
depends_on: [136-02]
files_modified:
  - src/ui/features/pretty-view/RoleModal.tsx
  - src/ui/features/pretty-view/RoleModal.test.tsx
  - src/ui/features/pretty-view/PrettyView.role-modal-swap.test.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "RoleModal renders exactly 3 nav tabs — Role file / Runbooks / Wakeups"
    - "No React code path in the UI attempts to mount RoleBountiesTab or BountyCard"
    - "PrettyConversationsPanel no longer declares the dead `filterLabel` const or references the `filterPinnedBounties` i18n key"
  artifacts:
    - path: "src/ui/features/pretty-view/RoleModal.tsx"
      provides: "3-tab role modal (no Bounties nav entry, no RoleBountiesTab import, no Target icon import if only used for bounties)"
      excludes: 'value: "bounties"'
      excludes_import: "RoleBountiesTab"
    - path: "src/ui/features/pretty-view/RoleModal.test.tsx"
      provides: "Test A retitled to '3 tabs — Role file / Runbooks / Wakeups'; no mockListBountiesForRoleName scaffold"
      excludes: "mockListBountiesForRoleName"
    - path: "src/ui/features/pretty-view/PrettyView.role-modal-swap.test.tsx"
      provides: "Test file without listBountiesForRoleName mock"
      excludes: "listBountiesForRoleName"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "Panel without dead filterLabel const"
      excludes: "filterPinnedBounties"
  key_links:
    - from: "src/ui/features/pretty-view/RoleModal.tsx"
      to: "NAV_SECTIONS constant"
      via: "3-entry array (role / runbooks / wakeups)"
      pattern: 'NAV_SECTIONS'
---

<objective>
Wave 2: Surgery on the UI consumer files that reference bounties. RoleModal loses its Bounties nav entry + TabsContent block + RoleBountiesTab import; the test file loses matching mock scaffolding and its Test A assertion count. PrettyConversationsPanel drops the dead `filterLabel` declaration.

Purpose: This is the visible UI change — after this wave lands, no user-visible surface in Skynet exposes bounties. RoleModal shrinks from 4 tabs to 3.
Output: Two source files + two test files edited; scoped tests still pass; `tsc --noEmit -p tsconfig.app.json` clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/136-retire-bounties-concept-from-skynet-remove-rolemodal-bountie/136-RESEARCH.md
@src/ui/features/pretty-view/RoleModal.tsx
@src/ui/features/pretty-view/RoleModal.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit RoleModal.tsx + RoleModal.test.tsx as an atomic pair</name>
  <files>src/ui/features/pretty-view/RoleModal.tsx, src/ui/features/pretty-view/RoleModal.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/RoleModal.tsx (full file — verify line numbers)
    - src/ui/features/pretty-view/RoleModal.test.tsx (full file — verify Test A + mock scaffolding)
    - .planning/phases/136-retire-bounties-concept-from-skynet-remove-rolemodal-bountie/136-RESEARCH.md § Frontend UI table + Pitfall 2
  </read_first>
  <action>
    Edit BOTH files in the same commit (they share the RoleBountiesTab + listBountiesForRoleName type/import surface — editing them together prevents any intermediate broken state).

    ---
    In `src/ui/features/pretty-view/RoleModal.tsx`:

    1. Line 18 header docblock: change "runbooks / bounties / wakeups" to "runbooks / wakeups" — remove "bounties / ".
    2. Line 48 lucide-react import: remove `Target,` from the icon import list IF `Target` is not used elsewhere in the file. Verify with grep: `grep -n '\bTarget\b' src/ui/features/pretty-view/RoleModal.tsx` after removing the L88 nav entry. If it appears only in the import + the L88 entry, remove from both. If `Target` is used somewhere else in the file (unlikely per research), leave the import alone.
    3. Line 75: DELETE `import { RoleBountiesTab } from "./RoleBountiesTab";`.
    4. Line 88: from `NAV_SECTIONS` array, DELETE the entry `{ value: "bounties", label: "Bounties", Icon: Target },`. Preserve the other 3 entries and array-comma formatting.
    5. Line 207 header docblock: change "runbooks + role-wakeups scoping, bounty read, avatar upload." to "runbooks + role-wakeups scoping, avatar upload." — remove "bounty read, ".
    6. Lines 645-654 area: DELETE the entire `<TabsContent value="bounties">...</TabsContent>` block including its `<RoleBountiesTab ... />` child. Preserve surrounding `<TabsContent value="role">`, `<TabsContent value="runbooks">`, `<TabsContent value="wakeups">` blocks byte-for-byte.

    ---
    In `src/ui/features/pretty-view/RoleModal.test.tsx`:

    7. Line 6 file-header comment: change "Bounties tab consumes listBountiesForRoleName. Avatar upload" to something like "Avatar upload" (or delete the sentence entirely — planner's discretion given comment is docstring-style).
    8. Line 12 test docstring: change "A. renders 4 tabs — Role file / Runbooks / Bounties / Wakeups" to "A. renders 3 tabs — Role file / Runbooks / Wakeups; default active tab = \"role\"".
    9. Lines 83-88: DELETE the entire `const mockListBountiesForRoleName = vi.fn().mockResolvedValue({ bounties: [], archivedBounties: [] });` declaration block.
    10. Lines 102-103: from the `vi.mock("../../api/claude-session-api", ...)` factory return object, DELETE `listBountiesForRoleName: (...args: unknown[]) => mockListBountiesForRoleName(...args),`. Preserve every other mock entry.
    11. Lines 157-160: from the `beforeEach` block, DELETE the entire `mockListBountiesForRoleName.mockResolvedValue({ bounties: [], archivedBounties: [] });` call. Preserve every other beforeEach line.
    12. Lines 172-193 (Test A body): change the `it(` title to match the new docstring from step 8; change the assertion that counted 4 nav buttons + asserted "Bounties" at index 2 — the revised test asserts EXACTLY 3 nav buttons with textContent matching, in order: "Role file", "Runbooks", "Wakeups". Delete `expect(navButtons![2].textContent).toContain("Bounties");` (line ~183). Update any surrounding assertions on `navButtons.length` from `4` to `3` and reindex the Wakeups assertion from `[3]` to `[2]`.
    13. Anywhere else in the file (Tests B-M bodies): remove any surviving `mockListBountiesForRoleName.mockResolvedValue(...)` calls. Grep after edit: `grep -c mockListBountiesForRoleName src/ui/features/pretty-view/RoleModal.test.tsx` should return `0`.

    ---
    Do NOT delete RoleBountiesTab.tsx here — that's Plan 04 Task 1. Do NOT touch other files in this task.
  </action>
  <verify>
    <automated>grep -c 'RoleBountiesTab\|mockListBountiesForRoleName\|listBountiesForRoleName' src/ui/features/pretty-view/RoleModal.tsx src/ui/features/pretty-view/RoleModal.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'RoleBountiesTab' src/ui/features/pretty-view/RoleModal.tsx` returns `0`
    - `grep -c '"bounties"' src/ui/features/pretty-view/RoleModal.tsx` returns `0` (nav value string gone)
    - `grep -c 'mockListBountiesForRoleName' src/ui/features/pretty-view/RoleModal.test.tsx` returns `0`
    - `grep -c 'listBountiesForRoleName' src/ui/features/pretty-view/RoleModal.test.tsx` returns `0`
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0 — RoleBountiesTab.tsx still exists so import-resolution passes even though we no longer import it
    - `npx vitest related --run src/ui/features/pretty-view/RoleModal.test.tsx` exits 0 with test A (renamed) passing on 3-tab assertion
  </acceptance_criteria>
  <done>RoleModal shows 3 tabs; its test file references no bounty mocks; typecheck + scoped tests clean.</done>
</task>

<task type="auto">
  <name>Task 2: Edit PrettyView.role-modal-swap.test.tsx — strip listBountiesForRoleName mock</name>
  <files>src/ui/features/pretty-view/PrettyView.role-modal-swap.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/PrettyView.role-modal-swap.test.tsx (specifically lines 75-95 mock factory area)
    - .planning/phases/136-retire-bounties-concept-from-skynet-remove-rolemodal-bountie/136-RESEARCH.md § Frontend UI table (PrettyView.role-modal-swap.test.tsx entry)
  </read_first>
  <action>
    Edit `src/ui/features/pretty-view/PrettyView.role-modal-swap.test.tsx`:

    1. Line 78 area comment (if present): if a comment references RoleBountiesTab, either delete the comment line or rewrite to describe the surviving mock surface only.
    2. Line 86 area: DELETE the `listBountiesForRoleName: vi.fn()...` mock entry from the `vi.mock("../../api/claude-session-api", ...)` factory return object. Preserve every other mock entry and the surrounding vi.mock structure.

    Do NOT delete the file. Do NOT touch other test files in this task.

    Verify: `grep -c 'listBountiesForRoleName\|RoleBountiesTab' src/ui/features/pretty-view/PrettyView.role-modal-swap.test.tsx` returns 0.
  </action>
  <verify>
    <automated>grep -c 'listBountiesForRoleName\|RoleBountiesTab' src/ui/features/pretty-view/PrettyView.role-modal-swap.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'listBountiesForRoleName\|RoleBountiesTab' src/ui/features/pretty-view/PrettyView.role-modal-swap.test.tsx` returns `0`
    - `npx vitest related --run src/ui/features/pretty-view/PrettyView.role-modal-swap.test.tsx` exits 0 — role-modal-swap behavior unchanged
  </acceptance_criteria>
  <done>PrettyView role-modal-swap test no longer mocks listBountiesForRoleName.</done>
</task>

<task type="auto">
  <name>Task 3: Delete dead filterLabel + filterPinnedBounties from PrettyConversationsPanel.tsx</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx</files>
  <read_first>
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (specifically lines 2310-2325 to see the surrounding label declarations)
    - .planning/phases/136-retire-bounties-concept-from-skynet-remove-rolemodal-bountie/136-RESEARCH.md § Pitfall 3
  </read_first>
  <action>
    Edit `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`:

    1. Lines 2317-2319: DELETE the entire `const filterLabel = t("nav.conversations.filterPinnedBounties", { defaultValue: "Filter by pinned bounties" });` declaration block (3 lines). Preserve the surrounding `newRoleLabel`, `rdpSectionLabel`, `loadingLabel` declarations byte-for-byte.

    2. Verify no other code path in the file uses `filterLabel`: `grep -c filterLabel src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` should return `0` after edit. If any usage exists that we missed, STOP and report — the variable was declared as dead code per research, so no consumer should exist. If a consumer exists, this task needs a re-plan.

    Do NOT search the i18n JSON files here — the `filterPinnedBounties` translation key can stay in the JSON files (inert per R-5). If a follow-on wants to clean the JSON keys, that's a separate scope.

    Do NOT touch any other line in the file. The panel has ~15 other bounty-shaped comment references (all describe retired GSD-workflow wires per RESEARCH.md LEAVE_ALONE bucket) — do NOT scrub them.
  </action>
  <verify>
    <automated>grep -c 'filterLabel\|filterPinnedBounties' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'filterLabel\|filterPinnedBounties' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns `0`
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0 — no orphan reference to a deleted const
    - `npx vitest related --run src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` exits 0 — panel rendering unchanged
  </acceptance_criteria>
  <done>PrettyConversationsPanel dead filterLabel + i18n key reference removed; typecheck clean.</done>
</task>

</tasks>

<verification>
Wave 2 gate:
- `npx tsc --noEmit -p tsconfig.app.json` exits 0
- `npx vitest related --run src/ui/features/pretty-view/RoleModal.test.tsx src/ui/features/pretty-view/PrettyView.role-modal-swap.test.tsx src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` exits 0
- RoleModal.tsx no longer imports RoleBountiesTab
- RoleModal has exactly 3 nav entries
</verification>

<success_criteria>
- All 3 UI files edited per action instructions
- 3-tab RoleModal renders without regression to Role file / Runbooks / Wakeups tabs
- Frontend TypeScript compiles clean
- Scoped tests pass
</success_criteria>

<output>
Create `.planning/phases/136-retire-bounties-concept-from-skynet-remove-rolemodal-bountie/136-03-SUMMARY.md` when done.
</output>
