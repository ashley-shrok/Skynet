---
phase: 133-retire-bounties-concept-from-skynet-remove-rolemodal-bountie
plan: 05
type: execute
wave: 5
depends_on: [136-04]
files_modified:
  - src/ui/api/claude-session-api.ts
  - src/ui/api/claude-session-api.role-reads.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Frontend API layer exports no bounty helper functions"
    - "Frontend API layer exports no bounty wire types (Bounty, BountyPriority, BountyStatus, BountyFieldsPatch)"
    - "ClaudeSessionServerFrame discriminated union has no bounty-shaped entries"
  artifacts:
    - path: "src/ui/api/claude-session-api.ts"
      provides: "Frontend WS API surface without any bounty exports"
      excludes: "listBountiesForRoleName, Bounty, BountyPriority, BountyStatus, BountyFieldsPatch, IdentityListBountiesPayload, IdentityBountiesEvent, RoleListBountiesPayload, RoleBountiesLoadedEvent"
    - path: "src/ui/api/claude-session-api.role-reads.test.ts"
      provides: "Role-reads test file without the listBountiesForRoleName describe block"
      excludes: "listBountiesForRoleName"
  key_links:
    - from: "src/ui/api/claude-session-api.ts"
      to: "ClaudeSessionServerFrame union type"
      via: "discriminated union"
      pattern: "type ClaudeSessionServerFrame ="
---

<objective>
Wave 4: Delete the frontend WS API layer's bounty surface — `listBountiesForRoleName` helper, all `Bounty*` types, all `Identity*Bounty*` and `Role*Bounties*` payload/event types, and their entries in the `ClaudeSessionServerFrame` discriminated union. Then strip the matching test describe block.

Purpose: Removes the frontend-side wire type contract for bounties. After this wave, the backend can safely delete the matching handlers (Plan 06) because no frontend caller can invoke them.
Output: 2 files edited; ~200 lines of type declarations gone from claude-session-api.ts; scoped tests still pass; `tsc --noEmit -p tsconfig.app.json` clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/136-retire-bounties-concept-from-skynet-remove-rolemodal-bountie/136-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit src/ui/api/claude-session-api.ts — delete listBountiesForRoleName + all bounty wire types</name>
  <files>src/ui/api/claude-session-api.ts</files>
  <read_first>
    - src/ui/api/claude-session-api.ts (specifically the "Patch #87: identity bounties WS wire types" doc block around L602, the Bounty type declarations L615-650, the ClaudeSessionServerFrame union around L495-L516, and the listBountiesForRoleName function around L1531)
    - .planning/phases/136-retire-bounties-concept-from-skynet-remove-rolemodal-bountie/136-RESEARCH.md § Frontend API Surface (comprehensive symbol list)
  </read_first>
  <action>
    Edit `src/ui/api/claude-session-api.ts` — bulk deletion of every bounty-shaped export. Verify with the RESEARCH.md § Frontend API Surface list before deleting each symbol; if the actual line number in the file has drifted from research, grep for the identifier and delete based on identifier match, not line number.

    ---
    DELETE these function exports:
    - `listBountiesForRoleName` (around L1531 — including its full function body, JSDoc header if any, and trailing blank-line separator)

    DELETE these type exports (all typically live under a "Patch #87: identity bounties WS wire types" doc block starting around L602):
    - `Bounty` (interface — ~L615-650)
    - `BOUNTY_PRIORITY_VALUES` (readonly array) and `BountyPriority` (union type derived from it)
    - `BOUNTY_STATUS_VALUES` (readonly array) and `BountyStatus` (union type derived from it)
    - `BountyFieldsPatch` (type)
    - `IdentityListBountiesPayload`, `IdentityBountiesEvent`
    - `IdentityUpdateBountyPriorityPayload`, `IdentityBountyPriorityUpdatedEvent`
    - `IdentityUpdateBountyStatusPayload`, `IdentityBountyStatusUpdatedEvent`
    - `IdentityUpdateBountyPinnedPayload`, `IdentityBountyPinnedUpdatedEvent`
    - `IdentityUpdateBountyNeedsDeskPayload`, `IdentityBountyNeedsDeskUpdatedEvent`
    - `IdentityUpdateBountyFieldsPayload`, `IdentityBountyFieldsUpdatedEvent`
    - `IdentityArchiveBountyPayload`, `IdentityBountyArchivedEvent`
    - `IdentityDeleteBountyPayload`, `IdentityBountyDeletedEvent`
    - `RoleListBountiesPayload`, `RoleBountiesLoadedEvent`

    Also delete the "Patch #87: identity bounties WS wire types" doc block header comment itself (the multi-line JSDoc-ish block that introduces the type family).

    ---
    EDIT the `ClaudeSessionServerFrame` discriminated union (or equivalent server-frame union type — grep for `type ClaudeSessionServerFrame` or a similar `type Foo = X | Y | Z` shape aggregating server-emitted event types). Remove these `|` entries:
    - `| IdentityBountiesEvent`
    - `| IdentityBountyPriorityUpdatedEvent`
    - `| IdentityBountyStatusUpdatedEvent`
    - `| IdentityBountyPinnedUpdatedEvent`
    - `| IdentityBountyNeedsDeskUpdatedEvent`
    - `| IdentityBountyFieldsUpdatedEvent`
    - `| IdentityBountyArchivedEvent`
    - `| IdentityBountyDeletedEvent`
    - `| RoleBountiesLoadedEvent`

    Preserve every non-bounty union member byte-for-byte. Preserve the union type's name, opening line, and closing semicolon.

    ---
    Do NOT touch `postSpeak`, `postSpeakStream`, `getRoleFileByName`, `listRoleWakeupsByName`, or any non-bounty helper. Do NOT touch the file's imports section unless removing an import that only bounty types used (grep to verify).

    Verify: `grep -cE '\b(Bounty|Bounties|bounty|bounties)\b' src/ui/api/claude-session-api.ts` should return 0 in code (comments-only outside this file's scope, but this file has no historical GSD-workflow bounty comments per research — count should be 0).
  </action>
  <verify>
    <automated>grep -cE '\b(Bounty|Bounties|listBountiesForRoleName|BOUNTY_PRIORITY_VALUES|BOUNTY_STATUS_VALUES|BountyFieldsPatch)\b' src/ui/api/claude-session-api.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '\b(Bounty|Bounties|listBountiesForRoleName|BOUNTY_PRIORITY_VALUES|BOUNTY_STATUS_VALUES|BountyFieldsPatch)\b' src/ui/api/claude-session-api.ts` returns `0`
    - `grep -c 'IdentityBountiesEvent\|RoleBountiesLoadedEvent\|IdentityBounty' src/ui/api/claude-session-api.ts` returns `0`
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0 — no orphan reference from any consumer (RoleBountiesTab.tsx already deleted in Plan 04, so no consumer remains)
    - The file still exports all non-bounty helpers (`postSpeak`, `postSpeakStream`, `getRoleFileByName`, `listRoleWakeupsByName`, WS frame types for non-bounty events)
  </acceptance_criteria>
  <done>Frontend API layer bounty surface fully retired; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Edit src/ui/api/claude-session-api.role-reads.test.ts — delete listBountiesForRoleName describe block</name>
  <files>src/ui/api/claude-session-api.role-reads.test.ts</files>
  <read_first>
    - src/ui/api/claude-session-api.role-reads.test.ts (specifically the `describe("listBountiesForRoleName one-shot helper")` block around L114-L179 — 2 tests R3, R4)
    - .planning/phases/136-retire-bounties-concept-from-skynet-remove-rolemodal-bountie/136-RESEARCH.md § Test File Impact (EDIT_REMOVE list item 4)
  </read_first>
  <action>
    Edit `src/ui/api/claude-session-api.role-reads.test.ts`:

    1. DELETE the entire `describe("listBountiesForRoleName one-shot helper", () => { ... })` block (around L114-L179 — spans the 2 tests R3 and R4).
    2. Preserve the surrounding `describe("getRoleFileByName ...")` and `describe("listRoleWakeupsByName ...")` blocks byte-for-byte (4 tests total across those two describes).
    3. If the imports section imports `listBountiesForRoleName` from `../claude-session-api`, DELETE that import (Plan 05 Task 1 removed the export, so an import would fail typecheck).
    4. If any shared fixture/helper at the top of the file was only used inside the deleted describe block, delete that too. Grep after delete: `grep -c listBountiesForRoleName src/ui/api/claude-session-api.role-reads.test.ts` should return 0.

    Do NOT touch getRoleFileByName or listRoleWakeupsByName tests. Do NOT delete the whole file.
  </action>
  <verify>
    <automated>grep -c 'listBountiesForRoleName\|Bounty\|Bounties' src/ui/api/claude-session-api.role-reads.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'listBountiesForRoleName\|Bounty\|Bounties' src/ui/api/claude-session-api.role-reads.test.ts` returns `0`
    - `npx vitest related --run src/ui/api/claude-session-api.role-reads.test.ts` exits 0 with the remaining 4 tests (getRoleFileByName + listRoleWakeupsByName describes) passing
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0 — no orphan import
  </acceptance_criteria>
  <done>Role-reads test file no longer exercises listBountiesForRoleName; other role-read tests survive intact.</done>
</task>

</tasks>

<verification>
Wave 4 gate:
- `npx tsc --noEmit -p tsconfig.app.json` exits 0
- `npx vitest related --run src/ui/api/claude-session-api.ts` exits 0
- claude-session-api.ts has zero bounty exports
- Role-reads test file has 4 surviving tests, all green
</verification>

<success_criteria>
- Both files edited
- Frontend TypeScript compiles clean
- No frontend caller can invoke bounty WS handlers anymore
</success_criteria>

<output>
Create `.planning/phases/136-retire-bounties-concept-from-skynet-remove-rolemodal-bountie/136-05-SUMMARY.md` when done.
</output>
