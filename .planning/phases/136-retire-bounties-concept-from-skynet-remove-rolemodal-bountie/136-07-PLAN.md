---
phase: 133-retire-bounties-concept-from-skynet-remove-rolemodal-bountie
plan: 07
type: execute
wave: 7
depends_on: [136-06]
files_modified:
  - src/backend/claude-session/identity-artifact-reader.ts
  - src/backend/claude-session/identity-artifact-reader.two-step.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "identity-artifact-reader.ts exports zero bounty reader/writer functions"
    - "identity-artifact-reader.ts exports zero bounty types/constants"
    - "identity-artifact-reader.two-step.test.ts still covers readIdentityHistory + resolveRoleForIdentity + extractRoleFromMarkdown + extractCosmeticsFromFrontmatter + getLocalRolesRoot without exercising bounty readers"
  artifacts:
    - path: "src/backend/claude-session/identity-artifact-reader.ts"
      provides: "Identity artifact reader with bounty surface fully amputated"
      excludes: "readIdentityBounties, readRoleBountiesByName, writeIdentityBountyPriority, writeIdentityBountyStatus, writeIdentityBountyPinned, writeIdentityBountyNeedsDesk, writeIdentityBountyFields, archiveIdentityBounty, deleteIdentityBounty, normalizeBounty, BOUNTY_PRIORITY_VALUES, BountyPriority, BOUNTY_STATUS_VALUES, BountyStatus, TERMINAL_BOUNTY_STATUSES, BountyFieldsPatch, IDMEDIT_MAX_BOUNTY_JSON_BYTES"
    - path: "src/backend/claude-session/identity-artifact-reader.two-step.test.ts"
      provides: "Two-step test file covering history + resolve tests, no bounty tests"
      excludes: "readIdentityBounties"
  key_links: []
---

<objective>
Wave 6: The final deletion — retire all bounty reader/writer functions, helpers, types, and constants from `identity-artifact-reader.ts` (~1500 lines gone). Preserve every non-bounty export (readIdentityFile, readIdentityHistory, readIdentityHandoff, resolveRoleForIdentity, readRoleFileByName, wakeup helpers, avatar helpers, readIdentityTrappedWork). Then surgically edit the two-step test file to remove bounty tests (10/12/14/16) while preserving the interleaved history tests (11/13/15) — Pitfall 4 warns against wholesale-deleting the file.

Purpose: Removes the last on-disk trace of the bounty vertical. After this wave, only historical GSD-workflow comments remain (LEAVE_ALONE bucket per research).
Output: 1 source file + 1 test file edited; `npm run build:backend` exits 0; scoped tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/136-retire-bounties-concept-from-skynet-remove-rolemodal-bountie/136-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit identity-artifact-reader.ts — amputate all bounty readers/writers/types/constants</name>
  <files>src/backend/claude-session/identity-artifact-reader.ts</files>
  <read_first>
    - src/backend/claude-session/identity-artifact-reader.ts — grep-first, then read ranges. Enumerate targets with `grep -nE '\b(normalizeBounty|readIdentityBounties|readRoleBountiesByName|writeIdentityBounty|archiveIdentityBounty|deleteIdentityBounty|BOUNTY_PRIORITY_VALUES|BountyPriority|BOUNTY_STATUS_VALUES|BountyStatus|TERMINAL_BOUNTY_STATUSES|BountyFieldsPatch|IDMEDIT_MAX_BOUNTY_JSON_BYTES)\b' src/backend/claude-session/identity-artifact-reader.ts`. Read each declaration's line range to determine its full extent (function body + JSDoc).
    - .planning/phases/136-retire-bounties-concept-from-skynet-remove-rolemodal-bountie/136-RESEARCH.md § Backend API Surface (reader/writer list) + § Types and Schemas + § Anti-Patterns to Avoid (do NOT delete readIdentityHistory or readIdentityHandoff)
  </read_first>
  <action>
    Edit `src/backend/claude-session/identity-artifact-reader.ts` — surgical amputation of every bounty-shaped construct.

    ---
    DELETE these type/constant declarations (grep for the identifier; delete each declaration block including its JSDoc header and trailing blank-line separator):
    - `BOUNTY_PRIORITY_VALUES` (readonly array, ~L1418-1420) + `BountyPriority` union type (~L1420-1425)
    - `BOUNTY_STATUS_VALUES` (readonly array, ~L1438-1440) + `BountyStatus` union type (~L1440-1445) + `TERMINAL_BOUNTY_STATUSES` (readonly array, ~L1445-1450)
    - `BountyFieldsPatch` type (~L2014)
    - `IDMEDIT_MAX_BOUNTY_JSON_BYTES` constant (~L2568)

    ---
    DELETE these functions (delete each in its entirety — signature + body + closing brace + surrounding blank-line separators + JSDoc header if any):
    - `normalizeBounty` (internal helper, ~L1055-1096 — not exported)
    - `readIdentityBounties` (~L1762 + ~200 lines body)
    - `readRoleBountiesByName` (~L3916 + ~175 lines body)
    - `writeIdentityBountyPriority` (~L4349 + body)
    - `writeIdentityBountyStatus` (~L4433 + body)
    - `writeIdentityBountyPinned` (~L4512 + body)
    - `writeIdentityBountyNeedsDesk` (~L4594 + body)
    - `writeIdentityBountyFields` (~L4703 + ~200 lines body — this is the largest single deletion; its complex input validation goes with it per RESEARCH.md § Security Domain "do NOT preserve pieces of that validation")
    - `archiveIdentityBounty` (~L4901 + body)
    - `deleteIdentityBounty` (~L5040 + body)

    ---
    PRESERVE these exports byte-for-byte (verify with grep after edits):
    - `readIdentityFile`
    - `readIdentityHistory` (per Pitfall — deferred to a later phase)
    - `readIdentityHandoff` (per Pitfall — deferred to a later phase)
    - `writeIdentityFile`, `writeIdentityHistory`, `writeIdentityHandoff` if they exist (Phase 18 IDMEDIT-01/02/03 requirements)
    - `resolveRoleForIdentity`
    - `readRoleFileByName`
    - `readIdentityTrappedWork`
    - All wakeup helpers (readIdentityWakeups, writeIdentityWakeups, listRoleWakeupsByName variants, etc.)
    - All avatar helpers (writeIdentityAvatar variants)
    - `extractRoleFromMarkdown`, `extractCosmeticsFromFrontmatter`, `getLocalRolesRoot` (used by the two-step test file)
    - Any generic infrastructure helpers (SSH exec wrappers, path normalizers) that the surviving readers depend on

    ---
    Cleanup pass: after all deletions, run `grep -cE '\b(bount|Bount|BOUNTY)\b' src/backend/claude-session/identity-artifact-reader.ts`. Expected surviving hits should be very close to 0 — if any survive, they are either (a) missed deletion targets or (b) comment/JSDoc references to bounty from surviving code (should be nil per research). Audit each visually.

    Do NOT touch identity-artifact-reader.two-step.test.ts in this task — that's Task 2.
  </action>
  <verify>
    <automated>grep -vE '^\s*(//|\*)' src/backend/claude-session/identity-artifact-reader.ts | grep -cE '\b(bount|Bount|BOUNTY)\b'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -vE '^\s*(//|\*)' src/backend/claude-session/identity-artifact-reader.ts | grep -cE '\b(bount|Bount|BOUNTY)\b'` returns `0` (no non-comment bounty references)
    - `grep -c 'export.*readIdentityBounties\|export.*readRoleBountiesByName\|export.*writeIdentityBounty\|export.*archiveIdentityBounty\|export.*deleteIdentityBounty\|export.*BountyPriority\|export.*BountyStatus\|export.*BountyFieldsPatch\|export.*IDMEDIT_MAX_BOUNTY_JSON_BYTES' src/backend/claude-session/identity-artifact-reader.ts` returns `0`
    - Surviving exports still present: `grep -c 'export.*readIdentityHistory\|export.*readIdentityHandoff\|export.*resolveRoleForIdentity\|export.*readRoleFileByName' src/backend/claude-session/identity-artifact-reader.ts` returns `≥4`
    - `npm run build:backend` exits 0
  </acceptance_criteria>
  <done>identity-artifact-reader.ts bounty surface fully amputated; ~1500 lines deleted; non-bounty exports preserved intact.</done>
</task>

<task type="auto">
  <name>Task 2: Edit identity-artifact-reader.two-step.test.ts — remove bounty tests 10/12/14/16, preserve history tests 11/13/15</name>
  <files>src/backend/claude-session/identity-artifact-reader.two-step.test.ts</files>
  <read_first>
    - src/backend/claude-session/identity-artifact-reader.two-step.test.ts (full file — the tests are interleaved: 10 bounty, 11 history, 12 bounty, 13 history, 14 bounty, 15 history, 16 bounty). Verify test numbering + shared fixture location (research says fixtures at L77 + L280-306).
    - .planning/phases/136-retire-bounties-concept-from-skynet-remove-rolemodal-bountie/136-RESEARCH.md § Pitfall 4 + § Test File Impact (EDIT_REMOVE list item 1)
  </read_first>
  <action>
    Edit `src/backend/claude-session/identity-artifact-reader.two-step.test.ts` — this file is a SHARED fixture test per Pitfall 4. Do NOT delete wholesale.

    Structure to preserve:
    - Tests 1-9 (setup / extractRoleFromMarkdown / extractCosmeticsFromFrontmatter / resolveRoleForIdentity / getLocalRolesRoot) — UNCHANGED
    - Tests 11, 13, 15 (readIdentityHistory two-step) — MOVE into a fresh `describe("readIdentityHistory — two-step", () => { ... })` block if they currently live inside a `describe("readIdentityBounties + readIdentityHistory — two-step")` wrapper (per research L273). Preserve their bodies byte-for-byte.

    Structure to delete:
    - Tests 10, 12, 14, 16 (any test whose body calls `readIdentityBounties` — grep for the identifier and delete each `it(` block including its body and closing brace/comma).
    - The wrapping `describe("readIdentityBounties + readIdentityHistory — two-step", () => { ... })` block header IF it wraps ONLY the tests being modified — replace the wrapper's title with `"readIdentityHistory — two-step"` (or split into two describes, one preserved for history and one removed for bounty).
    - Shared bounty fixture setup at L280-L306 area (per research) — delete any `bountyRoot` / `bountyFixture` / bounty-shaped `beforeAll`/`beforeEach` setup calls that history tests don't need.
    - Line 77 area bounty import: delete the `readIdentityBounties` from any `import { A, B, readIdentityBounties, C } from "./identity-artifact-reader"` destructure.

    Verify: `grep -c 'readIdentityBounties\|Bounty\|Bounties' src/backend/claude-session/identity-artifact-reader.two-step.test.ts` should return 0. If history-side fixture depended on a shared identity folder that bounty fixture also set up, transplant the minimal setup into the history block's beforeEach.

    Run scoped test after edit: `npx vitest related --run src/backend/claude-session/identity-artifact-reader.two-step.test.ts` — must exit 0 with the history tests (formerly 11, 13, 15) and non-bounty tests (1-9) all passing.
  </action>
  <verify>
    <automated>grep -cE 'readIdentityBounties|Bounty|Bounties|BOUNTY' src/backend/claude-session/identity-artifact-reader.two-step.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'readIdentityBounties|Bounty|Bounties|BOUNTY' src/backend/claude-session/identity-artifact-reader.two-step.test.ts` returns `0`
    - Surviving tests: at minimum the 3 history tests (11/13/15) + tests 1-9 (non-bounty). Grep `grep -cE '\bit\(' src/backend/claude-session/identity-artifact-reader.two-step.test.ts` returns approximately the pre-edit count minus 4 (tests 10/12/14/16 removed).
    - `npx vitest related --run src/backend/claude-session/identity-artifact-reader.two-step.test.ts` exits 0
    - `npm run build:backend` exits 0
  </acceptance_criteria>
  <done>Two-step test file preserves history + resolve coverage; bounty tests removed cleanly with no shared-fixture breakage.</done>
</task>

<task type="auto">
  <name>Task 3: Phase-wide surface audit — confirm zero live bounty symbols remain in src/</name>
  <files>(no files modified — audit only)</files>
  <read_first>
    - .planning/phases/136-retire-bounties-concept-from-skynet-remove-rolemodal-bountie/136-RESEARCH.md § Verification Approach (phase-gate audit greps 4, 5, 6)
  </read_first>
  <action>
    Run three audit greps over `src/` to confirm complete retirement:

    1. **Live symbol grep:**
       `git grep -nE '\b(readIdentityBounties|readRoleBountiesByName|writeIdentityBounty[A-Z]|archiveIdentityBounty|deleteIdentityBounty|listBountiesForRoleName|RoleBountiesTab|BountyCard|BountyPriority|BountyStatus|BountyFieldsPatch|BOUNTY_PRIORITY_VALUES|BOUNTY_STATUS_VALUES|TERMINAL_BOUNTY_STATUSES|IDMEDIT_MAX_BOUNTY_JSON_BYTES|handleRoleListBounties|__handleRoleListBountiesForTests|normalizeBounty)\b' src/`
       MUST return zero hits.

    2. **Wire-type string grep:**
       `git grep -nE '"role:list-bounties"|"role:bounties-loaded"|"identity:list-bounties"|"identity:bounties"|"identity:update-bounty|"identity:bounty-|"identity:archive-bounty"|"identity:delete-bounty"' src/`
       MUST return zero hits.

    3. **Human-review sweep (informational — not gating):**
       `git grep -cnE '\b(bount|Bount)\b' src/ | head -50`
       Expected surviving hits: comment-only references in the LEAVE_ALONE bucket from RESEARCH.md § Complete Enumeration (starter.ts, ssh-poll-orchestrator.ts, voice/*, branding/*, ComposeBox.tsx, PrettyView.tsx, IdentityModal.tsx, various test files with historical GSD-workflow comments, session-file-parser fixture prose). Ballpark: ~40 files, ~150 total occurrences. Print the output so the orchestrator can review; do NOT fail the task on this grep — it's a sanity check.

    If audit #1 or #2 returns any hit, STOP and report the file+line — that's a missed deletion. If audit #3 shows an unexpected file (e.g., a NEW file we didn't classify in research), report for orchestrator judgment.

    Do NOT run `npm run build` or `docker build`. Do NOT run full-suite vitest. Do NOT push. This is a scoped audit, not the phase-wide UAT gate — the orchestrator owns that.
  </action>
  <verify>
    <automated>(git grep -cnE '\b(readIdentityBounties|readRoleBountiesByName|writeIdentityBounty[A-Z]|archiveIdentityBounty|deleteIdentityBounty|listBountiesForRoleName|RoleBountiesTab|BountyCard|BountyPriority|BountyStatus|BountyFieldsPatch|BOUNTY_PRIORITY_VALUES|BOUNTY_STATUS_VALUES|TERMINAL_BOUNTY_STATUSES|IDMEDIT_MAX_BOUNTY_JSON_BYTES|handleRoleListBounties|__handleRoleListBountiesForTests|normalizeBounty)\b' src/ 2>&1 | awk -F: '{s+=$NF} END {print s+0}') </automated>
  </verify>
  <acceptance_criteria>
    - Audit #1 (live symbol grep) returns 0 hits — the verify command's awk sum equals `0`
    - Audit #2 (wire-type string grep) returns 0 hits — verify manually
    - Audit #3 (human-review) surviving hits classify against RESEARCH.md LEAVE_ALONE bucket (starter.ts, voice/*, branding/*, ssh-poll-orchestrator.ts, ComposeBox.tsx, PrettyView.tsx, IdentityModal.tsx, session-file-parser.ts, various *.test.ts with historical comments); orchestrator confirms no unexpected new-file surfaces
    - `npm run build:backend` exits 0
    - `npx vitest related --run src/backend/claude-session/ src/ui/api/ src/ui/features/pretty-view/ src/ui/features/pretty-conversations/` exits 0
  </acceptance_criteria>
  <done>Phase retirement complete; every live bounty symbol removed from src/; only historical GSD-workflow comments remain (documented as LEAVE_ALONE).</done>
</task>

</tasks>

<verification>
Wave 6 gate:
- `npm run build:backend` exits 0
- `npx vitest related --run` for touched-scope exits 0
- Audit greps #1 and #2 return 0 hits
- Audit grep #3's surviving hits match RESEARCH.md LEAVE_ALONE classification
</verification>

<success_criteria>
- identity-artifact-reader.ts bounty exports fully amputated
- Two-step test file preserves history/resolve coverage
- Phase-wide audit confirms zero live bounty symbols in src/
- Backend TypeScript compiles clean
- Scoped tests across all touched modules pass
- Only LEAVE_ALONE historical comments remain (per RESEARCH.md classification)
</success_criteria>

<output>
Create `.planning/phases/136-retire-bounties-concept-from-skynet-remove-rolemodal-bountie/136-07-SUMMARY.md` when done.
</output>
