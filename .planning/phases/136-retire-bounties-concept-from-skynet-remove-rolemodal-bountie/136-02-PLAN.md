---
phase: 133-retire-bounties-concept-from-skynet-remove-rolemodal-bountie
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - src/backend/claude-session/identity-artifact-reader.archive-bounty.test.ts
  - src/backend/claude-session/identity-artifact-reader.delete-bounty.test.ts
  - src/backend/claude-session/identity-artifact-reader.write-bounty-pinned.test.ts
  - src/backend/claude-session/identity-artifact-reader.write-bounty-status.test.ts
  - src/backend/claude-session/identity-artifact-reader.include-archived.test.ts
  - src/backend/claude-session/identity-artifact-reader.empty-bounties-remote.test.ts
  - src/ui/features/pretty-view/RoleBountiesTab.test.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Seven bounty-only test files no longer exist in the source tree"
    - "The remaining test suite still passes (no shared-fixture breakage)"
  artifacts:
    - path: "src/backend/claude-session/"
      provides: "identity-artifact-reader test directory minus 6 bounty-only test files"
      excludes: "archive-bounty.test.ts, delete-bounty.test.ts, write-bounty-pinned.test.ts, write-bounty-status.test.ts, include-archived.test.ts, empty-bounties-remote.test.ts"
    - path: "src/ui/features/pretty-view/"
      provides: "pretty-view directory minus RoleBountiesTab.test.tsx"
      excludes: "RoleBountiesTab.test.tsx"
  key_links: []
---

<objective>
Wave 1: Delete the 7 leaf test files that contain ONLY bounty tests. These files have zero cross-tests and zero shared fixtures with non-bounty tests — deletion cannot break `tsc` or other tests.

Purpose: Removes ~800 lines of test code that would otherwise fail once the underlying bounty readers/writers are deleted in Waves 5+. Frees Wave 5 to delete `identity-artifact-reader.ts` bounty exports without red tests.
Output: 7 files deleted; remaining vitest scoped run still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/136-retire-bounties-concept-from-skynet-remove-rolemodal-bountie/136-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete 6 backend bounty-only test files</name>
  <files>src/backend/claude-session/identity-artifact-reader.archive-bounty.test.ts, src/backend/claude-session/identity-artifact-reader.delete-bounty.test.ts, src/backend/claude-session/identity-artifact-reader.write-bounty-pinned.test.ts, src/backend/claude-session/identity-artifact-reader.write-bounty-status.test.ts, src/backend/claude-session/identity-artifact-reader.include-archived.test.ts, src/backend/claude-session/identity-artifact-reader.empty-bounties-remote.test.ts</files>
  <read_first>
    - .planning/phases/136-retire-bounties-concept-from-skynet-remove-rolemodal-bountie/136-RESEARCH.md § Test File Impact — Detailed Enumeration (DELETE_FILE list items 1-6)
  </read_first>
  <action>
    Delete the six listed test files with `git rm`. All six test files exclusively exercise bounty readers/writers (archiveIdentityBounty, deleteIdentityBounty, writeIdentityBountyPinned, writeIdentityBountyStatus, readIdentityBounties includeArchived semantics, readIdentityBounties empty-dir tolerance). None of them share fixtures with non-bounty tests per RESEARCH.md Pitfall 4 analysis.

    Do NOT delete `identity-artifact-reader.two-step.test.ts` (that's an EDIT in a later plan — it shares fixtures with history tests). Do NOT delete `identity-artifact-reader.trapped-work.test.ts` (comment-only refs only, LEAVE_ALONE bucket).

    Verify after deletion: `ls src/backend/claude-session/identity-artifact-reader.*.test.ts` should show a shorter list — remaining files include (at minimum) `two-step.test.ts`, `trapped-work.test.ts`, and any other non-bounty test files that existed before.
  </action>
  <verify>
    <automated>test ! -e src/backend/claude-session/identity-artifact-reader.archive-bounty.test.ts && test ! -e src/backend/claude-session/identity-artifact-reader.delete-bounty.test.ts && test ! -e src/backend/claude-session/identity-artifact-reader.write-bounty-pinned.test.ts && test ! -e src/backend/claude-session/identity-artifact-reader.write-bounty-status.test.ts && test ! -e src/backend/claude-session/identity-artifact-reader.include-archived.test.ts && test ! -e src/backend/claude-session/identity-artifact-reader.empty-bounties-remote.test.ts && echo "all-6-deleted"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 files no longer exist on disk (`ls` returns "No such file" for each)
    - `git status` shows all 6 as deleted (staged via `git rm`)
    - `identity-artifact-reader.two-step.test.ts` still exists (untouched here)
    - `identity-artifact-reader.trapped-work.test.ts` still exists (untouched here)
    - Verify command output equals `all-6-deleted`
  </acceptance_criteria>
  <done>Six backend bounty-only test files removed from the source tree.</done>
</task>

<task type="auto">
  <name>Task 2: Delete RoleBountiesTab.test.tsx</name>
  <files>src/ui/features/pretty-view/RoleBountiesTab.test.tsx</files>
  <read_first>
    - .planning/phases/136-retire-bounties-concept-from-skynet-remove-rolemodal-bountie/136-RESEARCH.md § Test File Impact — Detailed Enumeration (DELETE_FILE list item 7)
  </read_first>
  <action>
    Delete `src/ui/features/pretty-view/RoleBountiesTab.test.tsx` with `git rm`. All 6 tests in the file exclusively exercise RoleBountiesTab component behavior. There is no non-bounty content and no other test file imports from it.

    Verify: file no longer exists; RoleBountiesTab.tsx still exists (deletion of the source component is Plan 04's job — sequencing matters because tsc happens after component deletion).
  </action>
  <verify>
    <automated>test ! -e src/ui/features/pretty-view/RoleBountiesTab.test.tsx && test -e src/ui/features/pretty-view/RoleBountiesTab.tsx && echo "test-file-deleted-src-preserved"</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/features/pretty-view/RoleBountiesTab.test.tsx` no longer exists on disk
    - `src/ui/features/pretty-view/RoleBountiesTab.tsx` still exists (source deletion happens in Plan 04)
    - `git status` shows the .test.tsx as deleted
    - Verify command output equals `test-file-deleted-src-preserved`
  </acceptance_criteria>
  <done>Bounty-only frontend test file removed.</done>
</task>

<task type="auto">
  <name>Task 3: Scoped test run to confirm no shared-fixture breakage</name>
  <files>(no files modified)</files>
  <read_first>
    - .planning/phases/136-retire-bounties-concept-from-skynet-remove-rolemodal-bountie/136-RESEARCH.md § Pitfall 4 (two-step test file shared fixtures)
  </read_first>
  <action>
    Run scoped vitest against the identity-artifact-reader test dir + pretty-view test dir to confirm the 7 deletions did not break any surviving test's fixtures or imports. Fast smoke — no full-suite gate.

    Command: `npx vitest related --run src/backend/claude-session/identity-artifact-reader.ts src/ui/features/pretty-view/RoleBountiesTab.tsx` (uses `related` to reduce scope to files affected by the surviving reader/component modules).

    If any test fails with an import-of-deleted-file error, that's a Pitfall 4 miss — the failing test's shared setup depended on one of the deleted files. Report the file + line and STOP; do not attempt automatic fix (revert not needed since deletions are staged not committed at this point — orchestrator will decide next step).

    Do NOT run `npx vitest run` (full suite). Do NOT run `npm run build` or `docker build`. Do NOT push.
  </action>
  <verify>
    <automated>npx vitest related --run src/backend/claude-session/identity-artifact-reader.ts src/ui/features/pretty-view/RoleBountiesTab.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest related --run` exits 0
    - No test file emits "Cannot find module" or "Cannot destructure" errors that trace back to the 7 deleted files
    - Scoped test count is lower than pre-deletion baseline by exactly the number of tests that lived in the 7 deleted files (approximate — orchestrator inspects the count line in vitest output)
  </acceptance_criteria>
  <done>Wave 1 leaf deletions confirmed safe — no shared fixture broke.</done>
</task>

</tasks>

<verification>
Wave 1 gate:
- All 7 files absent from disk
- `npx vitest related --run` for touched-module scope exits 0
- No `tsc` regressions (deletion of test-only files cannot break tsc; no explicit typecheck needed here, but Plan 04 will pick it up)
</verification>

<success_criteria>
- 7 files deleted (6 backend + 1 frontend)
- Scoped vitest still green
- Wave 2 (UI surgery in Plan 03) can proceed
</success_criteria>

<output>
Create `.planning/phases/136-retire-bounties-concept-from-skynet-remove-rolemodal-bountie/136-02-SUMMARY.md` when done.
</output>
