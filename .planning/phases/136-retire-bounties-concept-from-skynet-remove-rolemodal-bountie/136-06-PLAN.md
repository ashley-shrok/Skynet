---
phase: 133-retire-bounties-concept-from-skynet-remove-rolemodal-bountie
plan: 06
type: execute
wave: 6
depends_on: [136-05]
files_modified:
  - src/backend/claude-session/claude-session-server.ts
  - src/backend/claude-session/claude-session-server.role-reads.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "claude-session-server.ts's WebSocket router has zero branches for `identity:*-bounty*` or `role:list-bounties` message types"
    - "handleRoleListBounties function no longer exists"
    - "__handleRoleListBountiesForTests export no longer exists"
    - "claude-session-server.ts no longer imports bounty readers/writers from identity-artifact-reader.ts"
  artifacts:
    - path: "src/backend/claude-session/claude-session-server.ts"
      provides: "WS server without any bounty routing"
      excludes: "identity:list-bounties, identity:update-bounty, identity:archive-bounty, identity:delete-bounty, role:list-bounties, handleRoleListBounties, __handleRoleListBountiesForTests, readIdentityBounties import, writeIdentityBounty imports, archiveIdentityBounty import, deleteIdentityBounty import, readRoleBountiesByName import"
    - path: "src/backend/claude-session/claude-session-server.role-reads.test.ts"
      provides: "role-reads test file without role:list-bounties describe block"
      excludes: "role:list-bounties, readRoleBountiesByName, __handleRoleListBountiesForTests"
  key_links:
    - from: "src/backend/claude-session/claude-session-server.ts"
      to: "WS message router"
      via: "if (msg.type === X) branches"
      pattern: 'if \(msg\.type ==='
---

<objective>
Wave 5: Delete the backend WS handler layer for bounties from `claude-session-server.ts` — 10 router branches, the extracted `handleRoleListBounties` function, its test-hook export, the doc header inventory of bounty wire types, and the imports of every bounty reader/writer from identity-artifact-reader. Then strip the matching test describe block from the role-reads test.

Purpose: After this wave, the backend cannot route any bounty message even if a client somehow sent one. The reader/writer functions in identity-artifact-reader.ts become dead weight — Plan 07 removes them.
Output: ~600 lines gone from claude-session-server.ts; scoped tests pass; `npm run build:backend` exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/136-retire-bounties-concept-from-skynet-remove-rolemodal-bountie/136-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit claude-session-server.ts — delete all bounty WS routing, function, and imports</name>
  <files>src/backend/claude-session/claude-session-server.ts</files>
  <read_first>
    - src/backend/claude-session/claude-session-server.ts — search the file with grep before opening ranges. Use `grep -nE "bount|Bount" src/backend/claude-session/claude-session-server.ts | head -60` to enumerate all hit locations (60+ lines expected). Read each hit context.
    - .planning/phases/136-retire-bounties-concept-from-skynet-remove-rolemodal-bountie/136-RESEARCH.md § Backend API Surface (WS handler table with wire-type-to-reader mapping)
  </read_first>
  <action>
    Edit `src/backend/claude-session/claude-session-server.ts` — bulk deletion of every bounty-shaped construct. Line numbers below are per-research approximate; grep for the identifier and delete by identifier match if drift occurred.

    ---
    DELETE from the imports section (near top of file, roughly L78-L110):
    - Any `import` line pulling the following names from `./identity-artifact-reader`:
      - `readIdentityBounties`
      - `readRoleBountiesByName`
      - `writeIdentityBountyPriority`
      - `writeIdentityBountyStatus`
      - `writeIdentityBountyPinned`
      - `writeIdentityBountyNeedsDesk`
      - `writeIdentityBountyFields`
      - `archiveIdentityBounty`
      - `deleteIdentityBounty`
      - `normalizeBounty` (if imported — internal helper, unlikely to be exported)
      - `BountyPriority`, `BountyStatus`, `BountyFieldsPatch`, `BOUNTY_PRIORITY_VALUES`, `BOUNTY_STATUS_VALUES`, `TERMINAL_BOUNTY_STATUSES`, `IDMEDIT_MAX_BOUNTY_JSON_BYTES` (if imported)
    - Preserve every import of a non-bounty symbol from identity-artifact-reader (readIdentityFile, readIdentityHistory, readIdentityHandoff, resolveRoleForIdentity, readRoleFileByName, wakeup helpers, avatar helpers, readIdentityTrappedWork, etc.). If bounty and non-bounty symbols share a single `import { A, B, C } from` line, remove ONLY the bounty names from the destructure list; keep the import statement itself.

    ---
    DELETE the doc-header comment block that inventories all bounty wire types (approximately L123-L212 area — a JSDoc-style comment listing every `identity:*-bounty*` and `role:list-bounties` message with descriptions). Also delete related header lines in the `L1635` / `L1643` neighborhood that describe the bounty WS wires. Preserve non-bounty wire type documentation intact.

    ---
    DELETE the extracted handler function `handleRoleListBounties` (function starts around L1719, ~60 lines). Include any JSDoc above it. Also DELETE the test-hook export `export const __handleRoleListBountiesForTests = handleRoleListBounties;` (around L1986).

    ---
    DELETE the following router branches in the main WS message handler (search for `if (msg.type === "..."` patterns). Ten branches total:
    - `if (msg.type === "identity:list-bounties")` — around L5695
    - `if (msg.type === "identity:update-bounty-priority")` — grep for exact line
    - `if (msg.type === "identity:update-bounty-status")` — around L6521
    - `if (msg.type === "identity:update-bounty-pinned")` — around L6599
    - `if (msg.type === "identity:update-bounty-needs-desk")` — around L6675
    - `if (msg.type === "identity:update-bounty-fields")` — around L6751
    - `if (msg.type === "identity:archive-bounty")` — around L6378
    - `if (msg.type === "identity:delete-bounty")` — around L6450
    - `if (msg.type === "role:list-bounties")` — around L6213 (contains `await handleRoleListBounties(ws, msg, userId);`)
    Delete each entire branch block including its opening `if (`, closing `}`, and any surrounding blank-line separators. Preserve every non-bounty branch byte-for-byte.

    ---
    Cleanup pass: after all deletions, grep `grep -nE '\b(bount|Bount)' src/backend/claude-session/claude-session-server.ts` and audit each surviving hit. Expected surviving hits after this wave = ZERO (this file has no historical GSD-workflow bounty comments per research; every hit was load-bearing). If any hit surfaces post-edit, STOP and inspect — it's either a comment worth deleting or a missed router branch.

    Do NOT touch identity-artifact-reader.ts in this task — that's Plan 07. Do NOT touch handoff, history, wakeup, or role-file handlers/readers.
  </action>
  <verify>
    <automated>grep -cE '\b(bount|Bount|handleRoleListBounties|__handleRoleListBountiesForTests|readIdentityBounties|readRoleBountiesByName|writeIdentityBounty|archiveIdentityBounty|deleteIdentityBounty)\b' src/backend/claude-session/claude-session-server.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '\b(bount|Bount|handleRoleListBounties|__handleRoleListBountiesForTests)\b' src/backend/claude-session/claude-session-server.ts` returns `0`
    - `grep -cE 'msg\.type === "(identity|role):[a-z-]*bount' src/backend/claude-session/claude-session-server.ts` returns `0`
    - `grep -c 'readIdentityBounties\|readRoleBountiesByName\|writeIdentityBounty\|archiveIdentityBounty\|deleteIdentityBounty' src/backend/claude-session/claude-session-server.ts` returns `0`
    - `npm run build:backend` exits 0 (backend TypeScript compiles — since identity-artifact-reader.ts still exports the readers, unused exports don't fail typecheck; Plan 07 removes them next)
    - Non-bounty WS handlers still routed: grep `msg\.type === "identity:get-role-file"` and `msg\.type === "identity:list-role-wakeups"` (or similar surviving handlers) still return ≥1 match
  </acceptance_criteria>
  <done>Backend WS server no longer routes or handles bounty messages; backend builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Edit claude-session-server.role-reads.test.ts — delete role:list-bounties describe block + related mock scaffolding</name>
  <files>src/backend/claude-session/claude-session-server.role-reads.test.ts</files>
  <read_first>
    - src/backend/claude-session/claude-session-server.role-reads.test.ts (specifically lines 25-90 imports+mocks+SendPayload type, and the `describe("role:list-bounties WS handler")` block around L190-L299 — 6 tests)
    - .planning/phases/136-retire-bounties-concept-from-skynet-remove-rolemodal-bountie/136-RESEARCH.md § Test File Impact (EDIT_REMOVE list item 2)
  </read_first>
  <action>
    Edit `src/backend/claude-session/claude-session-server.role-reads.test.ts`:

    1. Line ~31: DELETE `readRoleBountiesByName: vi.fn(),` from the vi.mock factory for identity-artifact-reader.
    2. Line ~40: DELETE the `readRoleBountiesByName` name from the import statement (if it's part of a destructured `import { A, B, readRoleBountiesByName } from ...` — remove only that name; keep other imports).
    3. Line ~45: DELETE `import { __handleRoleListBountiesForTests } from "../..."` (the handler function no longer exists after Plan 06 Task 1).
    4. Lines ~56-57: DELETE `bounties?: unknown[]` and `archivedBounties?: unknown[]` fields from the `SendPayload` (or equivalent test-helper) type definition. Preserve all other fields.
    5. Line ~79: DELETE `vi.mocked(readRoleBountiesByName).mockReset();` from the `beforeEach` block.
    6. Lines ~190-299: DELETE the entire `describe("role:list-bounties WS handler", () => { ... })` block (6 tests).

    Preserve the `describe("role:get-file WS handler")` (5 tests) and `describe("role:list-wakeups WS handler")` (5 tests) blocks byte-for-byte.

    Verify: `grep -cE 'bount|Bount|readRoleBountiesByName|__handleRoleListBountiesForTests' src/backend/claude-session/claude-session-server.role-reads.test.ts` returns 0.
  </action>
  <verify>
    <automated>grep -cE 'bount|Bount|readRoleBountiesByName|__handleRoleListBountiesForTests' src/backend/claude-session/claude-session-server.role-reads.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'bount|Bount|readRoleBountiesByName|__handleRoleListBountiesForTests' src/backend/claude-session/claude-session-server.role-reads.test.ts` returns `0`
    - `npx vitest related --run src/backend/claude-session/claude-session-server.role-reads.test.ts` exits 0 — remaining 10 tests (5 role:get-file + 5 role:list-wakeups) all pass
    - `npm run build:backend` exits 0 — no orphan mock import
  </acceptance_criteria>
  <done>Server role-reads test no longer tests role:list-bounties; remaining role-read tests survive.</done>
</task>

</tasks>

<verification>
Wave 5 gate:
- `npm run build:backend` exits 0
- `npx vitest related --run src/backend/claude-session/claude-session-server.ts src/backend/claude-session/claude-session-server.role-reads.test.ts` exits 0
- claude-session-server.ts has zero bounty routing branches
- No dead imports of bounty readers/writers
</verification>

<success_criteria>
- Both files edited
- Backend TypeScript compiles clean
- Backend WS server cannot route bounty messages
- Role-reads test suite survives with non-bounty tests intact
</success_criteria>

<output>
Create `.planning/phases/136-retire-bounties-concept-from-skynet-remove-rolemodal-bountie/136-06-SUMMARY.md` when done.
</output>
