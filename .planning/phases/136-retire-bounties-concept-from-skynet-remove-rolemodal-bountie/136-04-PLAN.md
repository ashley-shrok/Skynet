---
phase: 133-retire-bounties-concept-from-skynet-remove-rolemodal-bountie
plan: 04
type: execute
wave: 4
depends_on: [136-03]
files_modified:
  - src/ui/features/pretty-view/RoleBountiesTab.tsx
  - src/ui/features/pretty-view/BountyCard.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "RoleBountiesTab.tsx no longer exists in the source tree"
    - "BountyCard.tsx no longer exists in the source tree"
    - "No frontend code references either deleted component"
  artifacts:
    - path: "src/ui/features/pretty-view/"
      provides: "Directory without RoleBountiesTab.tsx or BountyCard.tsx"
      excludes: "RoleBountiesTab.tsx, BountyCard.tsx"
  key_links: []
---

<objective>
Wave 3: Delete the two frontend source components that Plan 03 stopped importing. Sequenced AFTER Plan 03 because RoleBountiesTab was still imported by RoleModal.tsx until Plan 03 landed — deleting the source before that would break `tsc`.

Purpose: Physical removal of the ~1946 lines of dead component code (RoleBountiesTab 385 lines + BountyCard 1561 lines). Frees Plan 05 to delete the wire-type layer they depended on.
Output: 2 files deleted; `tsc --noEmit -p tsconfig.app.json` still clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/136-retire-bounties-concept-from-skynet-remove-rolemodal-bountie/136-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify no remaining imports of RoleBountiesTab or BountyCard, then delete both files</name>
  <files>src/ui/features/pretty-view/RoleBountiesTab.tsx, src/ui/features/pretty-view/BountyCard.tsx</files>
  <read_first>
    - .planning/phases/136-retire-bounties-concept-from-skynet-remove-rolemodal-bountie/136-RESEARCH.md § Complete Enumeration — Frontend UI table (RoleBountiesTab.tsx + BountyCard.tsx rows)
  </read_first>
  <action>
    Before deleting either file, verify that Plan 03 successfully removed all imports:

    1. `grep -rn "from.*RoleBountiesTab\|from ['\"].*/RoleBountiesTab['\"]" src/` — MUST return zero hits. If any hit surfaces, STOP and report — Plan 03 missed something; do NOT proceed with deletion until fixed.
    2. `grep -rn "from.*BountyCard\|from ['\"].*/BountyCard['\"]" src/` — MUST return zero hits (BountyCard's only consumer was RoleBountiesTab, which is about to be deleted). If any hit surfaces outside RoleBountiesTab.tsx itself, STOP and report.

    Once both greps confirm zero external consumers:

    3. `git rm src/ui/features/pretty-view/RoleBountiesTab.tsx`
    4. `git rm src/ui/features/pretty-view/BountyCard.tsx`

    Verify neither file exists on disk. Verify `npx tsc --noEmit -p tsconfig.app.json` exits 0 — no orphan references.

    Note: The `Bounty` type imported into RoleBountiesTab from `claude-session-api.ts` disappears from this file's import graph automatically; the wire-type deletion in Plan 05 will then clear the `Bounty` type from claude-session-api.ts itself.
  </action>
  <verify>
    <automated>test ! -e src/ui/features/pretty-view/RoleBountiesTab.tsx && test ! -e src/ui/features/pretty-view/BountyCard.tsx && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c 'error TS' | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/features/pretty-view/RoleBountiesTab.tsx` no longer exists on disk
    - `src/ui/features/pretty-view/BountyCard.tsx` no longer exists on disk
    - `git status` shows both as deleted (staged via `git rm`)
    - Pre-delete import grep returned 0 hits (documented in run log)
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0 (verify output line count of `error TS` matches is `0`)
    - `npx vitest related --run src/ui/features/pretty-view/` exits 0 — surviving pretty-view tests still pass
  </acceptance_criteria>
  <done>Two ~1946-line frontend components deleted; frontend still typechecks.</done>
</task>

</tasks>

<verification>
Wave 3 gate:
- Both files absent from disk
- `npx tsc --noEmit -p tsconfig.app.json` exits 0
- `npx vitest related --run src/ui/features/pretty-view/` exits 0
</verification>

<success_criteria>
- RoleBountiesTab.tsx + BountyCard.tsx removed
- Frontend TypeScript still compiles
- Scoped pretty-view tests still pass
</success_criteria>

<output>
Create `.planning/phases/136-retire-bounties-concept-from-skynet-remove-rolemodal-bountie/136-04-SUMMARY.md` when done.
</output>
