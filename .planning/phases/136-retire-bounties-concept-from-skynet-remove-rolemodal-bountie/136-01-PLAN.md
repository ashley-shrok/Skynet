---
phase: 133-retire-bounties-concept-from-skynet-remove-rolemodal-bountie
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - substrate/skills/role/SKILL.md
  - src/backend/database/routes/roles-create.ts
  - src/backend/database/routes/roles-create.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "New `/role <name>` invocations on managed hosts no longer create a `bounties/` subfolder"
    - "New role creation via Skynet's POST /roles endpoint no longer creates `~/fleet/roles/<name>/bounties/`"
    - "The substrate-distributed `role` SKILL.md no longer describes bounties as the role's knowledge home"
  artifacts:
    - path: "substrate/skills/role/SKILL.md"
      provides: "Distributed /role skill, bounty-free"
      excludes: 'mkdir -p "$ROLE_DIR/bounties"'
    - path: "src/backend/database/routes/roles-create.ts"
      provides: "POST /roles endpoint that no longer mkdirs bounties/"
      excludes: 'mkdir "$HOME/fleet/roles/${name}/bounties"'
  key_links:
    - from: "src/backend/database/routes/roles-create.ts"
      to: "SSH exec mkdir chain"
      via: "atomic mkdir command sent over SSH"
      pattern: 'mkdir -p "\$HOME/fleet/roles" && mkdir "\$HOME/fleet/roles/\$\{name\}"'
---

<objective>
Wave 0 foundation: stop the bleeding by removing the two code sites that CREATE `bounties/` folders on new-role setup. Independent of the TypeScript deletion chain — touches only the POST /roles handler's SSH exec string and the distributed substrate skill file.

Purpose: If we skip this, every future `/role <name>` call (both on Skynet via POST /roles and on managed hosts via the `/role` skill) continues to spawn empty `bounties/` folders even after Skynet stops reading them. Wave 0 makes the retirement forward-consistent.
Output: SKILL.md + roles-create.ts edits + roles-create.test.ts comment update; backend build clean; roles-create scoped tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/136-retire-bounties-concept-from-skynet-remove-rolemodal-bountie/136-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit substrate/skills/role/SKILL.md — strip bounty prose + mkdir line</name>
  <files>substrate/skills/role/SKILL.md</files>
  <read_first>
    - substrate/skills/role/SKILL.md (full file — ~100 lines)
    - .planning/phases/136-retire-bounties-concept-from-skynet-remove-rolemodal-bountie/136-RESEARCH.md § Runtime State Inventory + Pitfall 5
  </read_first>
  <action>
    Edit substrate/skills/role/SKILL.md in place. Make four surgical changes:

    1. Line 4 (top-of-file description): replace "shared knowledge/bounty/history home that" with "shared knowledge/history home that" — remove the word "bounty".
    2. Line 18 (directory-layout bullet): replace "role file, bounty pool, history, deeper" with "role file, history, deeper" — remove "bounty pool, ".
    3. Line 64: DELETE the entire line `mkdir -p "$ROLE_DIR/bounties"`.
    4. Line 89: replace "live in bounties, not here." with prose that no longer references bounties. Suggested replacement: "live in the role file body, not here." (retain surrounding sentence intact).
    5. Line 96: in the announcement text, remove the "empty `bounties/` +" fragment from the "Next step" phrasing. Suggested: change "> bounties/ + empty history.md)" to "> empty history.md)".

    Do NOT introduce any new files. Do NOT rewrite unrelated prose. Verify no remaining case-insensitive `bount` matches remain in the file after edits.

    Distributor push (per Pitfall 5): fire-and-forget — do not attempt to trigger a distributor sweep from this task; the substrate distributor picks the file up on its normal cadence after Skynet deploys.
  </action>
  <verify>
    <automated>grep -cEi 'bount' substrate/skills/role/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cEi 'bount' substrate/skills/role/SKILL.md` returns `0`
    - `grep -c 'mkdir -p "\$ROLE_DIR/bounties"' substrate/skills/role/SKILL.md` returns `0`
    - File still parses as markdown (opens cleanly; no dangling code fences)
    - No other line in the file is modified beyond the five bullets in the action
  </acceptance_criteria>
  <done>Substrate role skill file carries zero bounty references; future `/role` runs on managed hosts will not create `bounties/`.</done>
</task>

<task type="auto">
  <name>Task 2: Edit src/backend/database/routes/roles-create.ts + roles-create.test.ts — remove bounty mkdir from POST /roles</name>
  <files>src/backend/database/routes/roles-create.ts, src/backend/database/routes/roles-create.test.ts</files>
  <read_first>
    - src/backend/database/routes/roles-create.ts (specifically lines 40-60 docblock header, 285-300 provisions comment, 495-515 exec chain block)
    - src/backend/database/routes/roles-create.test.ts (specifically R-5 test around lines 400-415)
  </read_first>
  <action>
    Edit `src/backend/database/routes/roles-create.ts`:

    1. Line ~509 SSH exec chain: change the command string from
       `mkdir -p "$HOME/fleet/roles" && mkdir "$HOME/fleet/roles/${name}" && mkdir "$HOME/fleet/roles/${name}/bounties"`
       to
       `mkdir -p "$HOME/fleet/roles" && mkdir "$HOME/fleet/roles/${name}"`
       (drop the trailing ` && mkdir "$HOME/fleet/roles/${name}/bounties"` fragment).
    2. Line ~44 docblock: change `8. Provision: mkdir -p bounties/, touch history.md (Phase 22, unchanged).` to `8. Provision: touch history.md (Phase 22, bounties/ removed Phase 136).`
    3. Line ~292 header comment: change `Provisions ~/fleet/roles/<name>/ + bounties/ + history.md + <name>.md` to `Provisions ~/fleet/roles/<name>/ + history.md + <name>.md`.
    4. Line ~500 in-body comment: rewrite the "The nested `bounties/` subdir is added" sentence to reflect that only the CHILD `~/fleet/roles/${name}` dir is created atomically now. Preferred: change to `//    touching the winner's file. Only the CHILD dir is created atomically —` (or an equivalent phrasing that keeps the load-bearing atomic-mkdir explanation but drops the bounties reference).

    Preserve every other line in the file byte-for-byte. Do NOT touch validation, error handling, ROLE_NAME_PATTERN, or the EEXIST-to-409 mapping — the R-5 test's atomicity behavior must remain unchanged (the outer `mkdir "$HOME/fleet/roles/${name}"` still fails with EEXIST on collision, still surfaces as 409).

    Edit `src/backend/database/routes/roles-create.test.ts`:

    5. R-5 test area (around lines 405-410): update any comment that reads `mkdir CHILD/bounties` (or similar) to reflect the new atomic chain — the CHILD mkdir is the one that trips EEXIST, so update the prose to say "mkdir CHILD" (drop `/bounties`). Do NOT change the test body's assertions or shell-command mock; the effective test is unchanged (409 on EEXIST at the CHILD mkdir step).

    Verify: `grep -cE 'bount' src/backend/database/routes/roles-create.ts` returns 0. Test file may still carry occasional non-load-bearing comment refs — audit output visually against the RESEARCH.md classification.
  </action>
  <verify>
    <automated>grep -vE '^\s*(//|\*|#)' src/backend/database/routes/roles-create.ts | grep -cEi 'bount'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -vE '^\s*(//|\*|#)' src/backend/database/routes/roles-create.ts | grep -cEi 'bount'` returns `0` (no bounty references outside comments)
    - `grep -c 'mkdir "\$HOME/fleet/roles/\${name}/bounties"' src/backend/database/routes/roles-create.ts` returns `0`
    - `grep -c 'mkdir "\$HOME/fleet/roles/\${name}"' src/backend/database/routes/roles-create.ts` returns at least `1` (the CHILD mkdir survives)
    - `npm run build:backend` exits 0
    - `npx vitest related --run src/backend/database/routes/roles-create.test.ts` exits 0 — all R-1..R-N tests pass, R-5 collision-to-409 behavior unchanged
  </acceptance_criteria>
  <done>POST /roles endpoint provisions `~/fleet/roles/<name>/` without a nested `bounties/` subdir; roles-create test suite green.</done>
</task>

</tasks>

<verification>
Wave 0 gate:
- `npm run build:backend` exits 0
- `npx vitest related --run src/backend/database/routes/roles-create.test.ts` exits 0
- Substrate SKILL.md contains zero bounty references
- roles-create.ts SSH exec chain no longer mkdirs `bounties/`
</verification>

<success_criteria>
- Both files edited per action instructions
- Zero non-comment bounty references remain in either target TypeScript file
- Scoped test file passes
- Backend TypeScript compiles clean
</success_criteria>

<output>
Create `.planning/phases/136-retire-bounties-concept-from-skynet-remove-rolemodal-bountie/136-01-SUMMARY.md` when done.
</output>
