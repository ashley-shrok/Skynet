---
phase: 79-telegram-bridge-phase-b
plan: 03
type: execute
wave: 2
depends_on:
  - 79-01
files_modified:
  - src/backend/telegram/getme-proxy.ts
  - src/backend/telegram/getme-proxy.test.ts
  - src/backend/telegram/routes.ts
  - src/backend/telegram/routes.test.ts
  - src/backend/telegram/shared-volume.ts
  - src/backend/telegram/shared-volume.test.ts
  - src/backend/database/database.ts
  - docker/nginx.conf
  - docker/nginx-https.conf
autonomous: true
requirements:
  - TGB-05
  - TGB-08
  - TGB-09
  - TGB-10
  - TGB-11
must_haves:
  truths:
    - "POST /telegram/validate returns {ok:true, botUsername, botId, firstName} for a syntactically valid bot token (mocked getMe true) and {ok:false, error} for a rejected one."
    - "POST /telegram/activate writes a telegram_bot_tokens row (via tokens-store.setTelegramBotToken) AND triggers rewriteRegistryFromCurrentState() (Plan 04) so /state/registry.json + <identityKey>.bottoken + <human>.token all reflect the new row before the response returns; returns 200 + botUsername; requires an authenticated user; enforces humanUserId matches the authenticated user."
    - "POST /telegram/disconnect deletes the telegram_bot_tokens row for the identity, unlinks /state/<identityKey>.bottoken, AND triggers rewriteRegistryFromCurrentState() so /state/registry.json reflects the removal; returns 200; requires the authenticated user to be the row's humanUserId."
    - "GET /telegram/:identityKey returns {status: 'unconfigured'} if no row exists; {status: 'connected', botUsername, telegramChatId} if configured."
    - "Every new backend route has matching nginx location blocks in BOTH docker/nginx.conf AND docker/nginx-https.conf."
    - "The shared-volume path constant TG_BRIDGE_STATE_DIR = '/state' is exported from src/backend/telegram/shared-volume.ts and consumed by Plans 04, 08."
    - "assertSafeHumanName and botTokenFilePath are exported from shared-volume.ts so Plan 04's bot-token-file-writer and Plan 08's reconcile can both import them."
  artifacts:
    - path: "src/backend/telegram/getme-proxy.ts"
      provides: "validateBotToken(token) -> {ok:true, botUsername, botId, firstName} | {ok:false, error}"
      exports: ["validateBotToken"]
    - path: "src/backend/telegram/getme-proxy.test.ts"
      provides: "Tests for valid token, invalid token, timeout, network error, no-username-set"
      min_lines: 60
    - path: "src/backend/telegram/routes.ts"
      provides: "Express router mounted at /telegram — 4 routes: validate, activate, disconnect, GET status; activate + disconnect fire rewriteRegistryFromCurrentState (Plan 04) + writeBotTokenFile/deleteBotTokenFile (Plan 04)"
      exports: ["default"]
    - path: "src/backend/telegram/routes.test.ts"
      provides: "Route-level tests covering happy-path + auth-fail + own-identity-check + validation-400 + activate-triggers-registry-rewrite + disconnect-unlinks-bottoken"
      min_lines: 120
    - path: "src/backend/telegram/shared-volume.ts"
      provides: "Constant TG_BRIDGE_STATE_DIR = '/state' + path helpers (registryPath, humanTokenPath, humanSincePath, humanTokenDeadPath, botTokenFilePath, configEnvPath) + exported assertSafeHumanName guard"
      exports: ["TG_BRIDGE_STATE_DIR", "registryPath", "humanTokenPath", "humanSincePath", "humanTokenDeadPath", "botTokenFilePath", "configEnvPath", "assertSafeHumanName"]
    - path: "docker/nginx.conf"
      provides: "location ~ ^/telegram(/.*)?$ block, verbatim copy of /matrix-admin block"
      contains: "^/telegram(/.*)?$"
    - path: "docker/nginx-https.conf"
      provides: "location ~ ^/telegram(/.*)?$ block, verbatim copy of /matrix-admin block"
      contains: "^/telegram(/.*)?$"
    - path: "src/backend/database/database.ts"
      provides: "Import + mount of telegramRoutes at /telegram, alongside matrixAdminRoutes at /matrix-admin"
      contains: 'app.use("/telegram", telegramRoutes)'
  key_links:
    - from: "src/backend/telegram/routes.ts"
      to: "src/backend/telegram/tokens-store.ts"
      via: "setTelegramBotToken / getTelegramBotToken / deleteTelegramBotToken calls"
      pattern: "TelegramBotToken"
    - from: "src/backend/telegram/routes.ts"
      to: "src/backend/telegram/getme-proxy.ts"
      via: "validateBotToken(token) call in /telegram/validate handler"
      pattern: "validateBotToken"
    - from: "src/backend/telegram/routes.ts"
      to: "src/backend/telegram/bridge-config-writer.ts"
      via: "rewriteRegistryFromCurrentState() called after every activate + disconnect (blocker B-2 fix)"
      pattern: "rewriteRegistryFromCurrentState"
    - from: "src/backend/telegram/routes.ts"
      to: "src/backend/telegram/bot-token-file-writer.ts"
      via: "writeBotTokenFile after activate, deleteBotTokenFile after disconnect (blocker B-1 fix)"
      pattern: "writeBotTokenFile|deleteBotTokenFile"
    - from: "src/backend/telegram/routes.ts"
      to: "src/backend/utils/auth-manager.ts"
      via: "AuthManager.getInstance().createAuthMiddleware() / createAdminMiddleware()"
      pattern: "createAuthMiddleware|createAdminMiddleware"
    - from: "src/backend/database/database.ts"
      to: "src/backend/telegram/routes.ts"
      via: "import telegramRoutes from '../telegram/routes.js'; app.use('/telegram', telegramRoutes)"
      pattern: 'app\.use\("/telegram"'
---

<objective>
Land the backend HTTP surface for Telegram bridge activation/disconnection/status, plus the nginx wiring required per CLAUDE.md (BOTH conf files), plus the shared-volume path-constant module that Plans 04 + 08 consume.

**Post-revision changes (blockers B-1 + B-2):** the activate + disconnect handlers now MUST trigger the file-writing side-effects that make the bridge see the new state. Without these, activation returns 200 but the bridge never learns about the new agent — wrist-reachability requires a full container restart to pick up the change. The wiring is: activate → tokens-store.setTelegramBotToken → bot-token-file-writer.writeBotTokenFile → bridge-config-writer.rewriteRegistryFromCurrentState → bridge's inotifywait (Plan 05) picks up the registry.json change within a second.

Purpose: The frontend TelegramTab (Plan 07) needs these routes to save/load/paste-validate bot tokens. Plans 04 (bridge-config-writer) and 08 (reconcile-dead-tokens) need `TG_BRIDGE_STATE_DIR`, `assertSafeHumanName`, and its path helpers. Without this plan, no user-facing flow is reachable and the file-writing plans have no path constants to import.

Output:
- `src/backend/telegram/getme-proxy.ts` + test — backend-side proxy for `https://api.telegram.org/bot<TOKEN>/getMe` (never expose token to browser; per PATTERNS.md § telegram-getme-proxy.ts, mirrors matrix-admin-client.ts:127-171 shape).
- `src/backend/telegram/routes.ts` + test — Express router with 4 routes: `POST /telegram/validate`, `POST /telegram/activate`, `POST /telegram/disconnect`, `GET /telegram/:identityKey`. Activate + disconnect wire through Plan 04 file writers.
- `src/backend/telegram/shared-volume.ts` — path constants + `assertSafeHumanName` (exported so Plan 04 + Plan 08 can import) + `botTokenFilePath` helper.
- `src/backend/database/database.ts` — one import + one `app.use("/telegram", telegramRoutes)` mount, alongside the existing `/matrix-admin` mount.
- `docker/nginx.conf` + `docker/nginx-https.conf` — `location ~ ^/telegram(/.*)?$` block in BOTH files, verbatim copy of the `/matrix-admin` block, per CLAUDE.md nginx caveat.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/79-telegram-bridge-phase-b-substrate-promotion-identity-modal-t/79-CONTEXT.md
@.planning/phases/79-telegram-bridge-phase-b-substrate-promotion-identity-modal-t/79-RESEARCH.md
@.planning/phases/79-telegram-bridge-phase-b-substrate-promotion-identity-modal-t/79-PATTERNS.md
@.planning/phases/79-telegram-bridge-phase-b-substrate-promotion-identity-modal-t/79-01-PLAN.md
@src/backend/matrix/matrix-admin-routes.ts
@src/backend/matrix/matrix-admin-client.ts
@src/backend/database/database.ts
@docker/nginx.conf
@docker/nginx-https.conf
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared-volume path constants (with exported assertSafeHumanName + botTokenFilePath) + Telegram getMe proxy</name>
  <read_first>
    - src/backend/telegram/shared-volume.ts (target — new file)
    - src/backend/telegram/shared-volume.test.ts (target — new file)
    - src/backend/telegram/getme-proxy.ts (target — new file)
    - src/backend/telegram/getme-proxy.test.ts (target — new file)
    - src/backend/matrix/matrix-admin-client.ts (analog — read lines 127-171 for the fetch+AbortController+timeout+non-2xx pattern; per PATTERNS.md § telegram-getme-proxy.ts, mirror the shape)
    - src/backend/utils/logger.ts (dependency — databaseLogger used for structured logging)
    - .planning/phases/79-.../79-RESEARCH.md § Q11 (Telegram /getMe response shape — verbatim)
    - .planning/phases/79-.../79-PATTERNS.md § telegram-getme-proxy.ts (full pattern excerpt + adaptations)
  </read_first>
  <behavior>
    - shared-volume: `TG_BRIDGE_STATE_DIR` equals the string literal `/state` (respects `TG_BRIDGE_STATE_DIR_OVERRIDE` env var if set, for tests only — see Plan 04 Task 1).
    - shared-volume: `registryPath()` returns `/state/registry.json`; `humanTokenPath("ashley")` returns `/state/ashley.token`; `humanSincePath("ashley")` returns `/state/ashley.since`; `humanTokenDeadPath("ashley")` returns `/state/ashley.token-dead`; `botTokenFilePath("alexander")` returns `/state/alexander.bottoken`; `configEnvPath()` returns `/state/config.env`.
    - shared-volume: `assertSafeHumanName(name)` is exported (so Plan 04's bot-token-file-writer AND Plan 08's reconcile-dead-tokens can both use it); throws on any name that doesn't match `/^[a-z0-9][a-z0-9_-]{0,63}$/`.
    - shared-volume: helpers refuse path traversal — `humanTokenPath("../etc/passwd")` throws and does NOT return `/state/../etc/passwd`.
    - getme-proxy: `validateBotToken("<valid-mock-token>")` with fetch mocked to return `{ok:true, result:{id:123, username:"foo_bot", first_name:"Foo"}}` resolves to `{ok:true, botUsername:"foo_bot", botId:123, firstName:"Foo"}`.
    - getme-proxy: `validateBotToken("<invalid>")` with fetch mocked to return `{ok:false, description:"Unauthorized"}` resolves to `{ok:false, error:"Unauthorized"}`.
    - getme-proxy: `validateBotToken` when fetch aborts on timeout resolves to `{ok:false, error:"Telegram unreachable"}` (or a stable code); does NOT throw.
    - getme-proxy: `validateBotToken` when the bot has no username set (`{ok:true, result:{id:1, first_name:"X"}}` — username missing) resolves to `{ok:false, error:"Bot has no username set (create one via @BotFather)"}`.
    - getme-proxy: the bot token value NEVER appears in any log emitted by the proxy (grep on captured log output → 0).
  </behavior>
  <action>
    Per D-03 shared-volume constants + PATTERNS.md § No Analog Found for the path-helper design + RESEARCH § Q11 for the getMe shape.

    File 1 — `src/backend/telegram/shared-volume.ts`:
    - `const OVERRIDE = process.env.TG_BRIDGE_STATE_DIR_OVERRIDE;`
    - `export const TG_BRIDGE_STATE_DIR = OVERRIDE || "/state";` — comment MUST cite Plan 04 Task 1 as origin AND note the override is test-only.
    - `export function assertSafeHumanName(name: string): void` — throws `new Error("humanName failed shared-volume path guard: <name>")` if `!/^[a-z0-9][a-z0-9_-]{0,63}$/.test(name)`. **Exported** (Plan 04 + Plan 08 both import).
    - `export function registryPath(): string { return \`${TG_BRIDGE_STATE_DIR}/registry.json\`; }`
    - `export function configEnvPath(): string { return \`${TG_BRIDGE_STATE_DIR}/config.env\`; }`
    - `export function humanTokenPath(humanName: string): string` — calls `assertSafeHumanName(humanName)` first; then returns `${TG_BRIDGE_STATE_DIR}/${humanName}.token`.
    - `export function humanSincePath(humanName: string): string` — same guard; returns `.since`.
    - `export function humanTokenDeadPath(humanName: string): string` — same guard; returns `.token-dead`.
    - `export function botTokenFilePath(identityKey: string): string` — same guard (identityKey slugs use the same regex per Assumption A5); returns `${TG_BRIDGE_STATE_DIR}/${identityKey}.bottoken`. **Blocker B-1 dep: Plan 04 imports this.**

    File 2 — `src/backend/telegram/getme-proxy.ts`:
    Follow the shape from `src/backend/matrix/matrix-admin-client.ts:127-171` (loginAsUser) verbatim except:
    - Signature: `export async function validateBotToken(botToken: string): Promise<{ ok: true; botUsername: string; botId: number; firstName: string } | { ok: false; error: string }>`.
    - URL: `` `https://api.telegram.org/bot${encodeURIComponent(botToken)}/getMe` ``.
    - Method: GET; no Authorization header; no body.
    - Timeout: `AbortController` + `setTimeout(() => controller.abort(), 10_000)`.
    - Parse response as `{ ok: boolean; result?: { id: number; username?: string; first_name?: string }; description?: string }`.
    - Return `{ok:true, botUsername: data.result.username, botId: data.result.id, firstName: data.result.first_name ?? ""}` on success; `{ok:false, error: data.description ?? "Telegram rejected token"}` on `!data.ok`; `{ok:false, error:"Bot has no username set (create one via @BotFather)"}` if `data.result` present but `.username` missing.
    - On AbortError: `{ok:false, error:"Telegram unreachable"}`.
    - On other throws: log via `databaseLogger.error("telegram getMe proxy error", err, { operation: "telegram_getme_proxy" })` and return `{ok:false, error:"Telegram proxy error"}`.
    - NEVER include the token value in any log; on the `operation: "telegram_getme_proxy"` info log for success, include only `botUsername`.

    File 3 — `src/backend/telegram/getme-proxy.test.ts`:
    Mock `global.fetch` via vitest `vi.stubGlobal("fetch", vi.fn())`. Cover all six behaviors listed above.

    File 4 — `src/backend/telegram/shared-volume.test.ts`:
    Cover: constant matches `/state`; the OVERRIDE env-var path works (set + import + assert); all six path helpers return expected strings; traversal input throws; assertSafeHumanName rejects `../foo`, `foo bar`, empty string, and 65-char names; accepts `ashley`, `zoey`, `commander-zoey`, `agent_007`.
  </action>
  <acceptance_criteria>
    - `test -f src/backend/telegram/shared-volume.ts && test -f src/backend/telegram/getme-proxy.ts && test -f src/backend/telegram/getme-proxy.test.ts && test -f src/backend/telegram/shared-volume.test.ts` (all four exist).
    - `grep -c '^export const TG_BRIDGE_STATE_DIR' src/backend/telegram/shared-volume.ts` returns 1.
    - `grep -c '^export function \(registryPath\|humanTokenPath\|humanSincePath\|humanTokenDeadPath\|botTokenFilePath\|configEnvPath\|assertSafeHumanName\)' src/backend/telegram/shared-volume.ts` returns 7 (all seven exports — six paths + one guard).
    - `grep -c '^export async function validateBotToken' src/backend/telegram/getme-proxy.ts` returns 1.
    - `grep -c 'api\.telegram\.org/bot\${encodeURIComponent' src/backend/telegram/getme-proxy.ts` returns 1.
    - `grep -v '^[[:space:]]*//\|^[[:space:]]*\*' src/backend/telegram/getme-proxy.ts | grep -Ec 'log\.(info|error|warn).*botToken|log\..*\${botToken}'` returns 0 (no logging of the token value; comment lines excluded).
    - `npx vitest run src/backend/telegram/getme-proxy.test.ts src/backend/telegram/shared-volume.test.ts --reporter=basic` all tests pass.
    - `npx tsc --noEmit -p tsconfig.backend.json 2>&1 | grep -E "src/backend/telegram/(getme-proxy|shared-volume)" | head` returns nothing.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/backend/telegram/getme-proxy.test.ts src/backend/telegram/shared-volume.test.ts --reporter=basic</automated>
  </verify>
  <done>Shared-volume constants module exists with traversal-safe helpers, exported `assertSafeHumanName` (Plans 04 + 08 consumer), and `botTokenFilePath` (Plan 04 B-1 dep); Telegram getMe proxy handles success + rejection + timeout + missing-username + network-error branches; bot token value never logged; all tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create /telegram routes + wire activate/disconnect through rewriteRegistryFromCurrentState + writeBotTokenFile/deleteBotTokenFile + mount in database.ts + add nginx location blocks in BOTH conf files</name>
  <read_first>
    - src/backend/telegram/routes.ts (target — new file)
    - src/backend/telegram/routes.test.ts (target — new file)
    - src/backend/matrix/matrix-admin-routes.ts (analog — full 126 lines; per PATTERNS.md § routes.ts, verbatim template for router setup + POST handler shape + error handling)
    - src/backend/utils/auth-manager.ts (dependency — read the `createAuthMiddleware()` signature at line 806 and `createAdminMiddleware()` at line 969; the middleware sets `req.userId` on the `AuthenticatedRequest`)
    - src/backend/telegram/tokens-store.ts (dependency — created in Plan 01)
    - src/backend/telegram/getme-proxy.ts (dependency — created in Task 1 of this plan)
    - src/backend/telegram/bridge-config-writer.ts (dependency — created in Plan 04 Task 2; blocker B-2)
    - src/backend/telegram/bot-token-file-writer.ts (dependency — created in Plan 04 Task 1; blocker B-1)
    - src/backend/database/database.ts (target — read lines 20-25 and 1815-1825 to see how matrixAdminRoutes are imported and mounted; mirror that shape for telegramRoutes)
    - docker/nginx.conf (target — read lines 150-165 for the `/matrix-admin` location block verbatim)
    - docker/nginx-https.conf (target — read lines 160-180 for the `/matrix-admin` location block verbatim)
    - CLAUDE.md § "Nginx caveat" (mandatory — matching blocks in BOTH files)
    - .planning/phases/79-.../79-PATTERNS.md § routes.ts + § database.ts mount + § nginx files (full excerpts)
    - .planning/phases/79-.../79-CONTEXT.md § 3B activation errors — activate/disconnect/validate response shape
  </read_first>
  <behavior>
    - `POST /telegram/validate` with body `{botToken: "..."}` and authenticated user: returns `{ok:true, botUsername, botId, firstName}` on valid token (fetch mocked); `{ok:false, error}` on invalid; 400 on missing/malformed body; 401 without auth.
    - `POST /telegram/activate` with body `{identityKey, botToken, humanUserId, telegramChatId?}` and authenticated user matching humanUserId: (1) validates the token via getMe proxy; on getMe failure, returns 400 + the error. (2) On success, calls `setTelegramBotToken(identityKey, {...})`. (3) Calls `writeBotTokenFile(identityKey, botToken)` (Plan 04 — blocker B-1). (4) Calls `rewriteRegistryFromCurrentState()` (Plan 04 — blocker B-2) — if this returns `{ok:false}`, logs a warn but still responds 200 (the bridge will pick up on next reconcile). (5) Returns 200 + `{ok:true, botUsername}`. Returns 403 if authenticated user's ID does not match humanUserId in body. Returns 401 without auth.
    - `POST /telegram/disconnect` with body `{identityKey}` and authenticated user matching the row's humanUserId: (1) fetches existing row via `getTelegramBotToken(identityKey)`; if null, 404; if `row.humanUserId !== authUserId`, 403. (2) `deleteTelegramBotToken(identityKey)`. (3) `deleteBotTokenFile(identityKey)` (Plan 04 — B-1; silent on absent). (4) `rewriteRegistryFromCurrentState()` (Plan 04 — B-2). (5) Returns 200 + `{ok:true}`. Returns 401 without auth.
    - `GET /telegram/:identityKey` with authenticated user: returns `{status:"unconfigured"}` if no row; `{status:"connected", botUsername, telegramChatId}` if row exists (never returns the bot token to the client). 401 without auth.
    - Post-mount in database.ts: `curl -i http://localhost:30001/telegram/validate -X POST -H "Content-Type: application/json" -d '{}'` returns 401 (auth required), NOT 404 (route missing).
    - Post-nginx: `grep -c '~ \^/telegram(/\.\*)?\$' docker/nginx.conf` returns 1; `grep -c '~ \^/telegram(/\.\*)?\$' docker/nginx-https.conf` returns 1.
    - **Blocker B-2 acceptance behavior:** the routes.test.ts adds an integration-flavored test that stubs the tokens-store + Plan 04 writers, calls `/telegram/activate`, and asserts BOTH `writeBotTokenFile` AND `rewriteRegistryFromCurrentState` were called (in that order) BEFORE the 200 response was sent.
  </behavior>
  <action>
    Per RESEARCH.md § Q11 (backend proxy shape) + PATTERNS.md § routes.ts (verbatim shape from matrix-admin-routes.ts) + CONTEXT.md § 3B activation errors + CLAUDE.md § Nginx caveat + revision blockers B-1 (writeBotTokenFile wiring) + B-2 (rewriteRegistryFromCurrentState wiring).

    File 1 — `src/backend/telegram/routes.ts`:

    Follow the shape of `src/backend/matrix/matrix-admin-routes.ts` verbatim:
    - Imports: `express`, `Request`, `Response`, `AuthManager`, `authLogger`, `AuthenticatedRequest`, plus `setTelegramBotToken`, `getTelegramBotToken`, `deleteTelegramBotToken` from `./tokens-store.js`, `validateBotToken` from `./getme-proxy.js`, `writeBotTokenFile` + `deleteBotTokenFile` from `./bot-token-file-writer.js`, `rewriteRegistryFromCurrentState` from `./bridge-config-writer.js`.
    - Regex constants at top: `const TELEGRAM_BOT_TOKEN_RE = /^[0-9]{9,10}:[A-Za-z0-9_-]{35}$/;` (per PATTERNS.md § Validation) and `const IDENTITY_KEY_RE = /^[a-z0-9][a-z0-9_-]{0,63}$/;`.
    - `const router = express.Router(); const authManager = AuthManager.getInstance(); const authenticateJWT = authManager.createAuthMiddleware();`.
    - Four routes:
      1. `router.post("/validate", express.json(), authenticateJWT, async (req, res) => { ... })` — validates body has `botToken` matching `TELEGRAM_BOT_TOKEN_RE` (early-return 400 otherwise); calls `validateBotToken(botToken)`; responds with the result JSON directly.

      2. `router.post("/activate", express.json(), authenticateJWT, async (req, res) => { ... })` — validates `identityKey` (regex), `botToken` (regex), `humanUserId` (non-empty string), `telegramChatId` (optional string). Checks `(req as AuthenticatedRequest).userId === humanUserId`; if not, 403 `{error:"not your identity"}`. Calls `validateBotToken`; if `!ok`, 400 with the error. On success:
         ```
         await setTelegramBotToken(identityKey, {botToken, botUsername: validateResult.botUsername, humanUserId, telegramChatId: telegramChatId ?? null});

         // Blocker B-1: bridge reads bot token from per-agent .bottoken file. Write it now.
         try {
           await writeBotTokenFile(identityKey, botToken);
         } catch (err) {
           authLogger.warn("activate: writeBotTokenFile failed (bridge will not see new bot until reconcile)", {
             operation: "telegram_activate_bottoken_write_failed",
             identityKey,
             error: err instanceof Error ? err.message : "unknown",
           });
         }

         // Blocker B-2: activate/disconnect MUST trigger a registry rewrite. Without
         // this, /state/registry.json stays stale until Skynet restart, and the
         // bridge's inotifywait has nothing to react to.
         const rewriteResult = await rewriteRegistryFromCurrentState();
         if (!rewriteResult.ok) {
           authLogger.warn("activate: registry rewrite failed (bridge may lag until next startup or reconcile)", {
             operation: "telegram_activate_registry_rewrite_failed",
             identityKey,
             error: rewriteResult.error,
           });
         }

         res.json({ok:true, botUsername: validateResult.botUsername});
         ```
         Response is 200 regardless of writeBotTokenFile/rewrite failure — the DB row IS persisted, and Plan 08's reconcile loop is the safety net. Log-warn is the observability path.

      3. `router.post("/disconnect", express.json(), authenticateJWT, async (req, res) => { ... })` — validates `identityKey` (regex). Fetches existing row via `getTelegramBotToken(identityKey)`; if null, 404; if `row.humanUserId !== authUserId`, 403. On authz success:
         ```
         await deleteTelegramBotToken(identityKey);

         // Blocker B-1: bridge stops routing this bot once .bottoken is gone.
         await deleteBotTokenFile(identityKey);   // silent no-op if already absent

         // Blocker B-2: rewrite registry so bridge's inotifywait picks up removal.
         const rewriteResult = await rewriteRegistryFromCurrentState();
         if (!rewriteResult.ok) {
           authLogger.warn("disconnect: registry rewrite failed", {
             operation: "telegram_disconnect_registry_rewrite_failed",
             identityKey,
             error: rewriteResult.error,
           });
         }

         res.json({ok:true});
         ```

      4. `router.get("/:identityKey", authenticateJWT, async (req, res) => { ... })` — validates `identityKey` param regex; fetches row via `getTelegramBotToken(identityKey)`; if null, 200 `{status:"unconfigured"}`; else 200 `{status:"connected", botUsername: row.botUsername, telegramChatId: row.telegramChatId}`. NEVER include `botToken` in the response.

    - Error handling: wrap each async handler body in try/catch; on error, `authLogger.error("Failed to <op>", err); res.status(500).json({error:"Failed to <op>"})`. Verbatim from matrix-admin-routes.ts:97-100 pattern.
    - Never log the bot token value in any operation; log `{botUsername, identityKey}` for context. Verbatim from PATTERNS.md § Shared Patterns / Logging.
    - `export default router;`

    File 2 — `src/backend/telegram/routes.test.ts`:
    Use supertest (already in package.json — check with `grep supertest package.json`; if absent, use fetch-against-a-local-app pattern from matrix-admin-routes.test.ts). Cover all four routes with happy-path + auth-fail + own-identity-check + validation-400 tests. Mock the getme-proxy via `vi.mock("./getme-proxy.js", () => ({ validateBotToken: vi.fn() }))`. Mock the two Plan 04 writers via `vi.mock("./bot-token-file-writer.js", () => ({ writeBotTokenFile: vi.fn(), deleteBotTokenFile: vi.fn() }))` and `vi.mock("./bridge-config-writer.js", () => ({ rewriteRegistryFromCurrentState: vi.fn().mockResolvedValue({ok: true, agentCount: 1, humanCount: 1}) }))`.

    Add these blocker-specific tests:
    - **B-1a**: activate happy-path calls `writeBotTokenFile(identityKey, botToken)` exactly once. Assert via `vi.mocked(writeBotTokenFile).toHaveBeenCalledWith("alexander", "1234:abc")`.
    - **B-1b**: disconnect happy-path calls `deleteBotTokenFile(identityKey)` exactly once.
    - **B-2a**: activate happy-path calls `rewriteRegistryFromCurrentState()` exactly once, AFTER `setTelegramBotToken` (assert call order via mock.invocationCallOrder).
    - **B-2b**: disconnect happy-path calls `rewriteRegistryFromCurrentState()` exactly once, AFTER `deleteTelegramBotToken`.
    - **B-2c**: activate response is 200 even when `rewriteRegistryFromCurrentState` returns `{ok:false, error:"..."}` — assert the warn log was fired via `vi.spyOn(authLogger, "warn")`.
    - **B-1/B-2 repro test (integration-flavored):** activate → assert both file writes AND registry rewrite were called BEFORE `res.json` was called (order verification).

    File 3 — `src/backend/database/database.ts`:
    Add ONE import near the existing `import matrixAdminRoutes from "../matrix/matrix-admin-routes.js";` (line 22):
    ```
    import telegramRoutes from "../telegram/routes.js";
    ```
    Add ONE mount near `app.use("/matrix-admin", matrixAdminRoutes);` (line ~1818):
    ```
    // Phase 79 Plan 03 — Telegram bridge routes (validate, activate, disconnect, status).
    // Auth-gated via authManager.createAuthMiddleware() inside routes.ts.
    // activate + disconnect handlers fire Plan 04's file writers + registry
    // rewrite as a side-effect (blockers B-1 + B-2 fix).
    app.use("/telegram", telegramRoutes);
    ```
    Do NOT reorder existing mounts. Do NOT touch any other code in database.ts.

    File 4 — `docker/nginx.conf` (HTTP config):
    Read the existing `location ~ ^/matrix-admin(/.*)?$` block at lines ~153-162. Immediately after that block, add a byte-identical copy with `matrix-admin` → `telegram`. Comment header line: `# Phase 79 Plan 03 — Telegram bridge routes: validate/activate/disconnect/status.`

    File 5 — `docker/nginx-https.conf` (HTTPS config):
    Same edit as File 4, applied to the corresponding block at lines ~164-173.

    CRITICAL: BOTH nginx files must be edited. Skipping either breaks the frontend on `.map` file loads (SPA fallback serves index.html) — per CLAUDE.md nginx caveat and PATTERNS.md § Pitfall 1.
  </action>
  <acceptance_criteria>
    - `test -f src/backend/telegram/routes.ts && test -f src/backend/telegram/routes.test.ts` (both exist).
    - `grep -c 'router\.post("/validate"\|router\.post("/activate"\|router\.post("/disconnect"\|router\.get("/:identityKey"' src/backend/telegram/routes.ts` returns 4.
    - `grep -c 'TELEGRAM_BOT_TOKEN_RE\s*=\s*/\^\[0-9\]' src/backend/telegram/routes.ts` returns 1.
    - `grep -c 'validateBotToken' src/backend/telegram/routes.ts` returns at least 2 (imported + called).
    - **Blocker B-1 wiring** — `grep -c 'writeBotTokenFile\|deleteBotTokenFile' src/backend/telegram/routes.ts` returns at least 4 (2 imports + 1 activate call + 1 disconnect call).
    - **Blocker B-2 wiring** — `grep -c 'rewriteRegistryFromCurrentState' src/backend/telegram/routes.ts` returns at least 3 (1 import + 1 activate call + 1 disconnect call).
    - `grep -v '^[[:space:]]*//\|^[[:space:]]*\*' src/backend/telegram/routes.ts | grep -Ec 'res\.json.*botToken|log\..*\${botToken}'` returns 0 (bot token never returned to client, never logged).
    - `grep -c 'import telegramRoutes from "\.\./telegram/routes\.js"' src/backend/database/database.ts` returns 1.
    - `grep -c 'app\.use("/telegram", telegramRoutes)' src/backend/database/database.ts` returns 1.
    - `grep -v '^[[:space:]]*#' docker/nginx.conf | grep -c '~ \^/telegram(/\.\*)?\$'` returns 1 (nginx location block present; comment-lines excluded so heading prose can't self-invalidate).
    - `grep -v '^[[:space:]]*#' docker/nginx-https.conf | grep -c '~ \^/telegram(/\.\*)?\$'` returns 1 (BOTH files edited per CLAUDE.md nginx caveat).
    - `npx vitest run src/backend/telegram/routes.test.ts --reporter=basic` all tests pass — including the B-1a/B-1b/B-2a/B-2b/B-2c blocker-specific tests.
    - `npx tsc --noEmit -p tsconfig.backend.json 2>&1 | grep -E "src/backend/telegram/routes|src/backend/database/database" | head` returns nothing new.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/backend/telegram/routes.test.ts --reporter=basic && grep -v '^[[:space:]]*#' docker/nginx.conf | grep -c '~ \^/telegram(/\.\*)?\$' && grep -v '^[[:space:]]*#' docker/nginx-https.conf | grep -c '~ \^/telegram(/\.\*)?\$'</automated>
  </verify>
  <done>All four /telegram/* routes are auth-gated, own-identity-checked, and behaviourally correct. Activate + disconnect fire Plan 04's writeBotTokenFile/deleteBotTokenFile AND rewriteRegistryFromCurrentState (closing blockers B-1 + B-2 — bridge sees new state within a second via inotifywait, no Skynet restart needed). The routes are mounted at /telegram in database.ts. BOTH nginx conf files have a location block. Bot token is never returned in HTTP response bodies or written to logs. Ready for Plan 07 (frontend) to hit these routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ↔ /telegram/* routes | Unauthenticated + malicious input crosses here |
| /telegram/validate ↔ api.telegram.org | External HTTP; token embedded in URL path segment |
| routes.ts ↔ tokens-store.ts | Backend-internal; identity-ownership check is the security seam |
| routes.ts ↔ Plan 04 file writers (bind-mount) | Side-effect surface; failures logged but not surfaced to client (200 still returned) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-03-01 | Spoofing | /telegram/activate | mitigate | createAuthMiddleware sets req.userId from verified JWT; handler compares to body.humanUserId and 403s on mismatch (per CONTEXT § 4A authz spec). |
| T-79-03-02 | Tampering | body input on all POSTs | mitigate | Regex-guard identityKey, botToken; whitelist telegramChatId as string-or-null; early-return 400 on any mismatch (per RESEARCH § V5). |
| T-79-03-03 | Information Disclosure | GET /telegram/:identityKey response | mitigate | Explicitly returns `{status, botUsername, telegramChatId}` only; botToken field is NEVER included in the response body (enforced by acceptance-criteria grep). |
| T-79-03-04 | Information Disclosure | Logging bot token | mitigate | authLogger.info calls log `{botUsername, identityKey}` only; grep gate in acceptance criteria catches `${botToken}` interpolation. |
| T-79-03-05 | DoS | /telegram/validate as token-brute-force oracle | accept | Telegram's own /getMe endpoint rate-limits at their edge; Skynet is single-tenant per install; if brute-force becomes a real threat, add express-rate-limit middleware in a follow-up phase (not blocking Phase 79 per RESEARCH § Security Domain). |
| T-79-03-06 | Elevation of Privilege | Nginx SPA-fallback masks route | mitigate | Two location blocks added (BOTH conf files) per CLAUDE.md nginx caveat; grep gates in acceptance criteria enforce both are present. |
| T-79-03-07 | Tampering | Path traversal via identityKey → shared-volume paths | mitigate | Task 1's `assertSafeHumanName` guard on all human-path helpers rejects traversal; Task 2's IDENTITY_KEY_RE regex additionally guards at the HTTP boundary. |
| T-79-03-08 | Denial of Service | rewriteRegistryFromCurrentState blocking activate/disconnect for a long time | accept | The Plan 04 function is bounded by loginAsUser calls (~200ms each) + fs writes; typical latency <500ms per RESEARCH § Q9. If it becomes a real UX issue, wrap in `setImmediate(...)` to defer past the response — not needed for Phase 79. |
| T-79-03-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages. All deps in-tree (express, drizzle, AuthManager, tokens-store from Plan 01, getme-proxy from Task 1, Plan 04 writers). |
</threat_model>

<verification>
- `npx vitest run src/backend/telegram --reporter=basic` — all Plan 03 tests green (getme-proxy, shared-volume, routes) including the blocker B-1/B-2 tests.
- `grep -c '~ \^/telegram(/\.\*)?\$' docker/nginx.conf docker/nginx-https.conf` — both non-comment locations present in BOTH files.
- Manual smoke (after container rebuild + Plan 06 bringup + Plan 09 cutover): `curl -s -X POST http://localhost:30001/telegram/validate -H "Content-Type: application/json" -d '{}'` returns 401 (auth required), not 404 (route missing) and not 500. After a successful `POST /telegram/activate`, `docker exec skynet ls /state/<agent>.bottoken` returns the file and `docker exec skynet cat /state/registry.json | jq '.agents[].name'` shows the new agent within ~1s.
- Route mount visible via `grep 'app\.use\("/telegram"' src/backend/database/database.ts` — returns 1.
</verification>

<success_criteria>
- Backend HTTP surface for Telegram bridge activation exists and is auth-gated.
- Activate + disconnect trigger the Plan 04 file writers + registry rewrite as a side-effect (blockers B-1 + B-2 closed — wrist-reachability activation flow is live, no Skynet restart required).
- Bot tokens are never returned in HTTP response bodies to clients.
- Bot tokens are never written to any log.
- BOTH `docker/nginx.conf` AND `docker/nginx-https.conf` have `/telegram` location blocks (CLAUDE.md nginx caveat honored).
- Shared-volume path constants module is importable by Plans 04 + 08; exports `assertSafeHumanName` + `botTokenFilePath` for downstream consumers.
</success_criteria>

<output>
Create `.planning/phases/79-telegram-bridge-phase-b-substrate-promotion-identity-modal-t/79-03-SUMMARY.md` when done. Include: (a) which 4 routes were created + their auth requirements, (b) the exact nginx block bytes added, (c) confirmation that grep for bot-token leakage in logs/response returns 0, (d) the specific 4-route + auth+own-identity test list from routes.test.ts, (e) blocker B-1 wiring: writeBotTokenFile call sites + count, (f) blocker B-2 wiring: rewriteRegistryFromCurrentState call sites + count, (g) the shared-volume exports list (should include `assertSafeHumanName` + `botTokenFilePath`).
</output>
