---
phase: 79-telegram-bridge-phase-b
plan: 09
type: execute
wave: 5
depends_on:
  - 79-05
  - 79-06
  - 79-07
  - 79-08
files_modified: []
autonomous: false
requirements:
  - TGB-01
  - TGB-02
  - TGB-04
  - TGB-05
  - TGB-07
  - TGB-08
must_haves:
  truths:
    - "Skynet's tg-bridge Docker service is running on t1000 (docker ps shows tg-bridge with 'Up' status)."
    - "docker exec tg-bridge cat /state/config.env shows MATRIX_ROOT and STT_URL populated (no placeholders)."
    - "docker exec tg-bridge ls /state/ashley.token /state/zoey.token returns both files, both mode 0600, both non-empty."
    - "docker exec tg-bridge ls /state/ashley.since exists after at least 30s of bridge running (cursor persisted at least once)."
    - "cursor-persistence-repro.sh (Plan 05 Task 3, blocker B-3) passes locally BEFORE checkpoint prompt — asserts CURSOR GUARD invariant with `set -x` traces."
    - "Ashley sends a test message from her Apple Watch to one of her agents; the agent receives it in a Claude session (Matrix room)."
    - "Ashley kills+restarts the tg-bridge container mid-conversation; no messages are dropped (cursor persistence works in production, not just in the repro)."
    - "docker exec tg-bridge ls /state | grep -c '\\.cred$' returns 0 (no legacy .cred files anywhere on the volume)."
    - "Nina's install on thenasty (/home/thenasty/.config/tg-bridge/) is shut down: systemctl --user status tg-bridge.service on thenasty shows 'inactive' or 'not-found'; the ~15 bots are still active but routed through Skynet's bridge, not thenasty's."
    - "ssh root@100.113.23.63 ls /home/thenasty/.config/tg-bridge/*.cred 2>/dev/null returns empty (source .cred files deleted post-cutover)."
    - "Pre-shutdown filename list of thenasty's .cred files is captured to /tmp/thenasty-cred-files-before-delete.txt AND to the SUMMARY, so if cutover fails post-shutdown, the operator has a reversibility path (re-mint via POST /matrix-admin/migrate-cred-files before re-enabling Nina's unit) — blocker W-6 fix."
  artifacts: []
  key_links: []
---

<objective>
Blocking human-verify checkpoint that gates cutover from Nina's thenasty install to Skynet's new Docker-service bridge. This is the go-live moment for Phase 79.

Purpose: All prior plans (01-08) have landed the code + config + tests. This plan verifies end-to-end that (a) the bridge Docker service actually works on t1000, (b) Ashley's wrist-reachability is preserved through the cutover, (c) cursor persistence actually prevents the reboot-replay flood (blocker B-3 automated repro + human production smoke — both required), (d) legacy .cred files are all gone, and (e) Nina's thenasty install can be safely shut down WITH a documented reversibility path (blocker W-6 fix). Nothing in this plan writes code — it's checkpoint-shaped work that only humans can perform.

**Blocker B-5 mirror motion (revision resolution):** Plan 06 now parametrizes host-varying paths via `${SKYNET_HOST_DIR:-/opt/skynet}`. The mirror step is a plain `cp docker/docker-compose.yml /opt/skynet/docker-compose.yml` — byte-identical, no reconciliation logic. SKYNET_HOST_DIR defaults to `/opt/skynet` so the compose file works on the live host without any operator action.

**Automation-first check:** the executor MUST automate every command that can be automated (docker ps, docker exec grep, curl POST for migration, SSH ls on thenasty, cursor-persistence-repro.sh). Only the "Ashley confirms wrist message received" step is truly human-only. The cutover is orchestrator-owned per fleet rule — this plan blocks on Ashley's approval before shutting down Nina's install.

Output: no file artifacts. Deliverable is the checkpoint approval + the cutover completion, captured in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/79-telegram-bridge-phase-b-substrate-promotion-identity-modal-t/79-CONTEXT.md
@.planning/phases/79-telegram-bridge-phase-b-substrate-promotion-identity-modal-t/79-RESEARCH.md
@.planning/phases/79-telegram-bridge-phase-b-substrate-promotion-identity-modal-t/79-05-PLAN.md
@.planning/phases/79-telegram-bridge-phase-b-substrate-promotion-identity-modal-t/79-06-PLAN.md
@.planning/phases/79-telegram-bridge-phase-b-substrate-promotion-identity-modal-t/79-07-PLAN.md
@.planning/phases/79-telegram-bridge-phase-b-substrate-promotion-identity-modal-t/79-08-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-cutover automated verification battery (must pass before checkpoint prompts) — includes B-3 repro invocation</name>
  <read_first>
    - .planning/phases/79-.../79-CONTEXT.md § Success definition (all bullet points — this task's checks map 1:1)
    - .planning/phases/79-.../79-05-PLAN.md Task 3 (cursor-persistence-repro.sh — Plan 09 Task 1 invokes it as blocker B-3 automation)
    - .planning/phases/79-.../79-06-PLAN.md (docker-compose surface — the executor may need to `cp docker/docker-compose.yml /opt/skynet/docker-compose.yml` then `docker compose up -d tg-bridge`; blocker B-5 makes this a plain byte-copy)
    - .planning/phases/79-.../79-08-PLAN.md (migration endpoint — this task POSTs to it as part of the pre-checkpoint setup)
  </read_first>
  <action>
    Execute the following automation battery in order. Each command's output MUST be captured in the SUMMARY. If any command returns non-zero, STOP — do not proceed to Task 2 (checkpoint prompt); instead diagnose and either fix the underlying plan or escalate to Ashley/Tina.

    **Pre-work — deploy mirror (orchestrator-owned per fleet rule; blocker B-5 resolution):**
    ```
    # Plan 06 parametrizes host-varying paths via ${SKYNET_HOST_DIR:-/opt/skynet},
    # so the mirror is a plain byte-copy — no reconciliation logic needed.
    sudo cp docker/docker-compose.yml /opt/skynet/docker-compose.yml
    sudo docker compose -f /opt/skynet/docker-compose.yml config >/dev/null && echo "compose config OK"
    ```
    If `docker compose config` fails: STOP — investigate the parametrization (should not happen because SKYNET_HOST_DIR defaults to `/opt/skynet` which exists on t1000).

    Then:
    ```
    cd /opt/skynet && sudo docker compose build tg-bridge
    cd /opt/skynet && sudo docker compose up -d tg-bridge
    ```

    **Automation battery A-J:**

    Step A — Verify the tg-bridge service is up:
    ```
    sudo docker ps --filter name=tg-bridge --format '{{.Names}} {{.Status}}'
    ```
    Expected: one line, `tg-bridge Up <duration>`.

    Step B — Verify Skynet wrote config.env:
    ```
    sudo docker exec tg-bridge cat /state/config.env
    ```
    Expected: file exists, contains `MATRIX_ROOT=http://100.113.23.63:8008` (or whatever the current admin creds hold) and `STT_URL=http://100.80.122.111:8000/v1/audio/transcriptions`, plus the "Written by Skynet at boot" header comment.

    Step C — Invoke the migration endpoint (Plan 08's `POST /matrix-admin/migrate-cred-files`):
    ```
    ADMIN_JWT="$(<get admin JWT from Ashley's session cookie or from a saved test creds file>)"
    curl -sS -X POST -H "Authorization: Bearer $ADMIN_JWT" \
      https://term.example.com/matrix-admin/migrate-cred-files
    ```
    Expected: 200 response with `{ok:true, results:[{humanName:"ashley",mxid:"@ashley:...",status:"minted"},{humanName:"zoey",mxid:"@zoey:...",status:"minted"},...], deletedCredFiles:[...]}`.

    Step D — Verify per-human token files exist post-migration:
    ```
    sudo docker exec tg-bridge ls -la /state/ashley.token /state/zoey.token
    ```
    Expected: both files exist, mode `-rw-------` (0600), non-zero size, owned by root (or whatever UID the tg-bridge container runs as).

    Step D2 — Verify per-agent .bottoken files exist (blocker B-1 wire-through):
    ```
    sudo docker exec tg-bridge sh -c 'ls /state/*.bottoken 2>/dev/null | head -5'
    ```
    Expected: after a `POST /telegram/activate` for at least one identity (from Step J or from a startup sync), one or more `.bottoken` files appear. If Step J hasn't run yet, this may return empty — re-verify after Step J.

    Step E — Verify registry.json is written and Nina-shaped-minus-cred-minus-bot_token:
    ```
    sudo docker exec tg-bridge cat /state/registry.json | jq '.agents[0] | keys'
    ```
    Expected: array of keys does NOT contain `bot_token` or (in `.humans[0]`) `cred`.

    Step F — Verify zero .cred files anywhere:
    ```
    sudo docker exec tg-bridge sh -c 'ls /state/*.cred 2>/dev/null; ls /state/**/*.cred 2>/dev/null'
    ```
    Expected: empty output.

    Step G — Verify ship-gate no-hardcoded-IPs in the deployed bridge script:
    ```
    sudo docker exec tg-bridge grep -E '100\.113\.23\.63|100\.80\.122\.111' /app/bridge.sh
    ```
    Expected: exit code 1 (no matches).

    Step H — Verify bridge Matrix cursor persistence surface exists (files may not exist yet if no messages have flowed):
    ```
    sudo docker exec tg-bridge sh -c 'ls /state/*.since 2>/dev/null; echo exit=$?'
    ```
    Expected: after at least one message flows in Task 2, this returns at least ashley.since and zoey.since.

    **Step H2 — CURSOR GUARD automated repro (blocker B-3, CONTEXT § Success definition bullet 6):**
    ```
    bash substrate/services/tg-bridge/cursor-persistence-repro.sh
    ```
    Expected: exit code 0 with output ending `=== ALL REPROS PASS — CURSOR GUARD invariant holds ===`. This is the automated repro test CONTEXT explicitly requires (in addition to the Task 2 human smoke test). If it fails, STOP — Plan 05 Task 3 has a regression.

    Step I — Verify nginx routes:
    ```
    curl -si https://term.example.com/telegram/nonexistent-identity -X GET
    ```
    Expected: 401 (auth required) OR 404 (unknown identity, after auth) — NEVER 200 with `text/html` content-type (that means SPA fallback kicked in and BOTH nginx blocks are missing — Plan 03 was mis-shipped).

    Step J — Verify the frontend Telegram tab renders (headless):
    Direct Ashley (or a test user) to open https://term.example.com, log in, open an identity modal, switch to Identity scope, click the Telegram pill. Screenshot expected: paste-token form (if identity has no telegram_bot_tokens row) OR connected view (if it does, from the migration).

    Only after ALL eleven steps A-J (including H2 blocker-B-3 repro) pass, proceed to Task 2.
  </action>
  <acceptance_criteria>
    - Every command in Steps A-I + H2 has been executed and its output captured verbatim in the SUMMARY.
    - Step A returns "Up <duration>" line.
    - Step B shows `MATRIX_ROOT=` and `STT_URL=` populated with real values.
    - Step C returns 200 with per-human results including at least Ashley + Zoey status="minted".
    - Step D shows both /state/ashley.token and /state/zoey.token with mode 0600.
    - Step E shows registry.json shape without `bot_token` or `cred` field.
    - Step F returns empty (zero .cred files under /state/).
    - Step G returns exit 1 (zero hardcoded IPs in the deployed bridge script).
    - **Step H2 (blocker B-3):** `bash substrate/services/tg-bridge/cursor-persistence-repro.sh 2>&1 | grep -c 'ALL REPROS PASS'` returns 1.
    - Step I returns 401/404, NOT 200 text/html.
    - Step J: Ashley confirms the Telegram tab renders correctly in her browser session (verbal or Slack/discord ack captured in the SUMMARY).
  </acceptance_criteria>
  <verify>
    <automated>sudo docker ps --filter name=tg-bridge --format '{{.Names}} {{.Status}}' | grep -c '^tg-bridge Up' && bash substrate/services/tg-bridge/cursor-persistence-repro.sh 2>&1 | grep -c 'ALL REPROS PASS'</automated>
  </verify>
  <done>All eleven pre-cutover checks pass (including the B-3 automated cursor-persistence repro); the Skynet bridge is proven working before the checkpoint prompt asks Ashley to authorize thenasty shutdown.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Blocking checkpoint — Ashley tests wrist reachability + cursor persistence + authorizes Nina's thenasty shutdown</name>
  <what-built>
    All Phase 79 code + config + Docker service. Skynet's new tg-bridge Docker service is running on t1000 with:
    - `bridge.sh` sourcing config from /state/config.env (no hardcoded IPs).
    - Per-human Matrix tokens minted via admin, stored at /state/{humanName}.token (0600).
    - Per-agent Telegram bot tokens at /state/{identityKey}.bottoken (0600, Plan 04 bot-token-file-writer — blocker B-1).
    - Matrix cursor persistence per human at /state/{humanName}.since (deletes reboot-replay flood).
    - Dead-token sentinel + Skynet's 30s reconcile loop for automatic re-mint on 401.
    - inotifywait-based registry reload — activation/disconnect in the identity modal triggers bridge re-init within a second (via Plan 03's activate/disconnect → Plan 04's rewriteRegistryFromCurrentState — blocker B-2).
    - Identity modal Telegram tab with paste-token → validate → activate → connected flow (humanUserId sourced from getUserInfo() — blocker W-3).
    - Zero .cred files anywhere on the shared volume (kill-the-cred invariant enforced).
    - Automated CURSOR GUARD repro (`substrate/services/tg-bridge/cursor-persistence-repro.sh`) — passed in Task 1 Step H2, blocker B-3.

    Nina's install on thenasty is STILL RUNNING at this checkpoint — cutover shutdown is what Ashley is being asked to authorize.
  </what-built>
  <how-to-verify>
    STEP 1 — Wrist-reachability smoke test.
    - From Ashley's Apple Watch, send a Telegram message to one of her agents (e.g. `@alexander_skynet_bot` — bot username per registry).
    - Confirm the agent's Claude session receives it (visible in the terminal pane, or in the Matrix room via a Matrix client).
    - Expected latency: <5s from wrist → agent.
    - If the message does NOT arrive within 30s, STOP — do not proceed. Diagnose whether Nina's bridge is still handling it (which would mean Skynet's bridge isn't receiving the getUpdates poll) or Skynet's bridge is receiving but failing to mx_send (401 → check `docker logs tg-bridge` for LOUD entries).

    STEP 2 — Cursor persistence smoke test (production complement to Task 1 Step H2's automated repro).
    - Have Ashley (or a helper) send 3 Telegram messages to an agent in quick succession.
    - Wait ~5s for them to route through.
    - Run: `sudo docker restart tg-bridge` (kills + restarts the container — this simulates the reconcile-restart cycle).
    - Have Ashley send a 4th message immediately.
    - Confirm ALL FOUR messages arrived in the agent's session (no gap, no duplicates from reboot-replay).
    - Run: `sudo docker exec tg-bridge cat /state/ashley.since` — expected: non-empty string (Matrix next_batch token, opaque).
    - This proves cursor persistence works IN PRODUCTION (Task 1 Step H2's automated repro proved it works IN ISOLATION). Both are required per CONTEXT § Success definition bullet 6 (blocker B-3).

    STEP 3 — Identity modal activation smoke test.
    - Ashley opens https://term.example.com, logs in, opens any identity modal, switches to Identity scope, clicks the Telegram pill.
    - If that identity is unconfigured: pastes a bot token from @BotFather; confirms the "Waiting for /start" state appears; sends /start to the bot from her Telegram; confirms transition to Connected state.
    - **Blocker B-2 sub-check:** after activation, run `sudo docker exec tg-bridge cat /state/registry.json | jq '.agents | map(.name)'` — the newly activated identity's name should be in the list within ~2 seconds. If registry.json is stale, Plan 03's activate handler is not wiring through Plan 04's `rewriteRegistryFromCurrentState` — blocker B-2 regressed.
    - **Blocker B-1 sub-check:** `sudo docker exec tg-bridge ls /state/<newly-activated-identityKey>.bottoken` — file exists, 0600. If missing, Plan 03's activate handler is not calling `writeBotTokenFile` — blocker B-1 regressed.
    - If that identity IS configured (migrated): clicks Disconnect → confirms the AlertDialog text is correct → confirms return to paste-state → verify the `.bottoken` file for that identity is gone (`docker exec tg-bridge ls /state/<key>.bottoken` returns "No such file or directory").
    - This proves the frontend flow end-to-end.

    STEP 4 — Authorize thenasty shutdown.
    - Ashley either APPROVES cutover (types "approved: shut down thenasty") — the executor then proceeds to Task 3, which stops Nina's install.
    - Or REJECTS cutover ("hold — cursor persistence dropped a message" / "connected state doesn't show my Telegram handle" / etc.) — the executor stops here, captures the reason in the SUMMARY, and the phase returns to gap-closure for a follow-up plan.
  </how-to-verify>
  <resume-signal>
    Ashley types one of:
    - "approved: shut down thenasty" → proceed to Task 3.
    - "hold: <reason>" → stop, capture reason, escalate to Tina for gap-closure planning.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Post-approval cutover — shut down Nina's thenasty install + delete legacy .cred files (with reversibility spelled out — blocker W-6)</name>
  <read_first>
    - .planning/phases/79-.../79-CONTEXT.md § Success definition (last two bullets: no host-side systemd unit; Nina's install can be shut down after cutover)
    - .planning/phases/79-.../79-RESEARCH.md § Runtime State Inventory (Nina's install shape: systemd USER unit + /home/thenasty/.config/tg-bridge/)
    - .planning/phases/79-.../79-RESEARCH.md § Pitfall 4 (deletion timing — mint fresh, verify, THEN delete)
  </read_first>
  <action>
    ONLY execute this task if Task 2's checkpoint returned `approved: shut down thenasty`. Otherwise, SKIP this task and jump to writing the SUMMARY with "cutover deferred pending Ashley re-approval".

    **Reversibility note (blocker W-6):** if any step 4-6 below surfaces a problem, the recovery path is: (a) STOP the deletion, (b) re-enable Nina's unit via `ssh root@100.113.23.63 "sudo -u thenasty systemctl --user enable tg-bridge.service && sudo -u thenasty systemctl --user start tg-bridge.service"` — but Nina's bridge NEEDS the .cred files to relogin. If .cred files are already deleted at Step 3, re-mint via `POST /matrix-admin/migrate-cred-files` (Plan 08) which writes fresh `.token` files to Skynet's /state/ AND fresh tokens directly for the thenasty install path (adjust deploy-side scripts if needed to place the tokens on thenasty). The captured pre-shutdown filename list (see Step 3) tells the operator which .cred files existed for accurate re-mint targeting.

    Step 1 — Stop Nina's tg-bridge on thenasty:
    ```
    ssh root@100.113.23.63 "sudo -u thenasty systemctl --user stop tg-bridge.service"
    ```
    Verify: `ssh root@100.113.23.63 "sudo -u thenasty systemctl --user is-active tg-bridge.service"` returns `inactive` (or exit code 3).

    Step 2 — Disable Nina's tg-bridge on thenasty (prevents restart on next boot):
    ```
    ssh root@100.113.23.63 "sudo -u thenasty systemctl --user disable tg-bridge.service"
    ```
    Verify: `ssh root@100.113.23.63 "sudo -u thenasty systemctl --user is-enabled tg-bridge.service"` returns `disabled` (or exit code 1).

    **Step 3 (blocker W-6 reversibility) — CAPTURE the .cred file inventory BEFORE deletion:**
    ```
    # First, capture filenames AND file sizes/mtime for reversibility audit.
    # This is the ONLY per-file provenance we have — .cred contents are plaintext
    # passwords we deliberately do NOT copy anywhere (that's the whole security
    # win). But knowing WHICH files existed is enough for the operator to know
    # WHOM to re-mint if cutover later fails.
    ssh root@100.113.23.63 "ls -la /home/thenasty/.config/tg-bridge/*.cred 2>/dev/null" > /tmp/thenasty-cred-files-before-delete.txt
    cat /tmp/thenasty-cred-files-before-delete.txt   # sanity — echo the list

    # Now delete.
    ssh root@100.113.23.63 "rm -f /home/thenasty/.config/tg-bridge/*.cred"
    ssh root@100.113.23.63 "ls /home/thenasty/.config/tg-bridge/*.cred 2>/dev/null"
    ```
    Expected: last command returns empty (all .cred files gone). The `/tmp/thenasty-cred-files-before-delete.txt` file is preserved in the SUMMARY.

    **Reversibility (blocker W-6):** if any subsequent step reveals cutover problems, the operator can:
    1. Read `/tmp/thenasty-cred-files-before-delete.txt` to see which humans had .cred files (typically Ashley + Zoey).
    2. POST `/matrix-admin/migrate-cred-files` (Plan 08 endpoint) to mint fresh Matrix tokens for those humans, writing to Skynet's `/state/{humanName}.token`.
    3. If also needing to restart Nina's bridge as a hard rollback: copy the fresh `.token` files from Skynet's shared volume to `/home/thenasty/.config/tg-bridge/{humanName}.token` (Nina's bridge reads `.token` files, not `.cred` — the `.cred` files were the *fallback* relogin path Nina's bridge used on 401. The `.token` files are the primary auth artifact, and mint-fresh gives Nina's bridge everything it needs to resume WITHOUT the .cred files).
    4. Re-enable + start Nina's unit per the `ssh ... systemctl --user enable/start` commands (inverse of Steps 1-2).

    Step 4 — Wrist-reachability re-smoke:
    - Ashley sends one more Telegram message to an agent — this MUST route through Skynet's bridge (Nina's is stopped).
    - Confirm delivery.

    Step 5 — Document the cutover in the SUMMARY:
    - Timestamp of shutdown.
    - List of .cred filenames that were deleted (from /tmp/thenasty-cred-files-before-delete.txt).
    - The reversibility path (verbatim from the Step 3 note above) — so future operators know how to roll back without re-planning the phase.
    - Confirmation from Ashley that wrist-reachability continues.

    Step 6 — Recommend follow-ups (for RETROSPECTIVE.md):
    - The permanent CI regression guard on `grep -E '100\.113\.23\.63|100\.80\.122\.111' substrate/services/tg-bridge/bridge.sh` (per CONTEXT § Success definition ship-gate) should be added in a Phase 80 wave-0 task or a `.github/workflows/` CI addition — flag but do not implement here.
    - Nina's `/home/thenasty/.config/tg-bridge/` directory can be archived + removed in a later cleanup phase — not this phase's job.
  </action>
  <acceptance_criteria>
    - Step 1: `systemctl --user is-active tg-bridge.service` on thenasty returns `inactive`.
    - Step 2: `systemctl --user is-enabled tg-bridge.service` on thenasty returns `disabled`.
    - **Step 3 (W-6):** `/tmp/thenasty-cred-files-before-delete.txt` exists AND is non-empty (captures the pre-delete .cred inventory for reversibility).
    - Step 3: `ls /home/thenasty/.config/tg-bridge/*.cred` on thenasty returns empty.
    - Step 4: Ashley confirms one more wrist message arrived after Nina's shutdown.
    - Step 5: SUMMARY includes cutover timestamp + deleted .cred file list + the verbatim W-6 reversibility path.
  </acceptance_criteria>
  <verify>
    <automated>ssh -o StrictHostKeyChecking=no root@100.113.23.63 "sudo -u thenasty systemctl --user is-active tg-bridge.service" | grep -q 'inactive\|unknown' && test -s /tmp/thenasty-cred-files-before-delete.txt</automated>
  </verify>
  <done>Nina's install is stopped, disabled, and stripped of .cred files. Skynet's bridge is the sole wrist-reachability path. Pre-delete filename inventory captured to /tmp for W-6 reversibility. Phase 79 is shippable — all TGB-01 through TGB-11 requirements are satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cutover window | Both bridges are momentarily inactive between Skynet-up and thenasty-down; message drop is possible |
| SSH root access to thenasty | Executor has broad shell access; must scope commands narrowly |
| Ashley's approval decision | Human decision governs whether shutdown proceeds |
| .cred deletion → reversibility loss | Without the pre-delete filename inventory, rollback becomes guesswork; W-6 mitigates via /tmp capture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-09-01 | Denial of Service | Message drop during cutover window | mitigate | Skynet's bridge is proven working before thenasty is stopped (Task 2 smoke tests + Task 1 Step H2 automated repro); both bridges running simultaneously briefly is acceptable (Telegram bot API doesn't rate-limit by source, Matrix rooms tolerate duplicate reads — the same message may briefly route through both but neither drops it). |
| T-79-09-02 | Information Disclosure | .cred files persist on thenasty (plaintext Matrix passwords) | mitigate | Task 3 Step 3 explicitly deletes them + verifies absence. |
| T-79-09-03 | Elevation of Privilege | SSH root access misused | accept | Executor runs only the four commands documented in Task 3; commands are scoped to systemctl --user for the thenasty user + rm of *.cred files inside a specific dir. |
| T-79-09-04 | Repudiation | Cutover decision undocumented | mitigate | SUMMARY captures Ashley's approval verbatim + timestamp + deleted file list + W-6 reversibility path. |
| T-79-09-05 | Tampering | thenasty's .cred files re-created after deletion (e.g., a re-enabled bridge auto-mints) | mitigate | Task 3 Step 2 disables the systemd unit — no auto-restart path. If Ashley re-enables it later, that's out of Phase 79 scope. |
| T-79-09-06 | Denial of Service | Cutover fails post-shutdown, no reversibility path documented | mitigate | Blocker W-6 fix: pre-delete inventory captured; Task 3 Step 3 documents verbatim reversibility via POST /matrix-admin/migrate-cred-files + copy fresh .token files to thenasty. |
| T-79-09-SC | Tampering | No package installs in this plan | mitigate | Zero code changes; this is checkpoint-shaped work only. |
</threat_model>

<verification>
- Task 1: all 11 automated checks pass with captured output in SUMMARY (including B-3 repro).
- Task 2: Ashley's explicit "approved: shut down thenasty" recorded in SUMMARY.
- Task 3: thenasty tg-bridge inactive+disabled, .cred files deleted, one more wrist message confirmed post-cutover; pre-delete inventory captured to /tmp AND SUMMARY.
- All TGB-01 through TGB-11 requirements satisfied — cross-reference in SUMMARY.
</verification>

<success_criteria>
- Skynet's tg-bridge Docker service is running on t1000.
- Ashley has confirmed wrist-reachability works via Skynet's bridge (not Nina's).
- Cursor persistence is proven both in isolation (B-3 automated repro) and in production (Task 2 Step 2 human smoke).
- Nina's install on thenasty is stopped, disabled, and .cred files deleted (with reversibility path documented — W-6 fix).
- Identity modal Telegram tab renders correctly for both unconfigured and connected identities.
- Zero .cred files exist anywhere on the shared volume OR on thenasty.
- docker/docker-compose.yml mirrored to /opt/skynet/docker-compose.yml as a byte-copy (B-5 fix: parametrization eliminates drift).
- Phase 79 success definition (CONTEXT § Success definition) — every bullet TRUE.
</success_criteria>

<output>
Create `.planning/phases/79-telegram-bridge-phase-b-substrate-promotion-identity-modal-t/79-09-SUMMARY.md` when done. Include: (a) captured output of all 11 Task 1 checks (including B-3 repro exit code + output tail), (b) Ashley's verbatim approval (or rejection reason if held), (c) cutover timestamp + deleted .cred file list, (d) the verbatim W-6 reversibility path (so future operators can copy-paste-execute a rollback), (e) any post-cutover issues + follow-up items for RETROSPECTIVE.md, (f) explicit cross-reference of TGB-01..TGB-11 to which prior plan or Task-2/3 check satisfied each.
</output>
