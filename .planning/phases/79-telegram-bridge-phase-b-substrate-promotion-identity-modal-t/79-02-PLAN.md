---
phase: 79-telegram-bridge-phase-b
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/config/media-endpoints.ts
  - src/backend/config/media-endpoints.test.ts
  - src/backend/database/routes/voice.ts
autonomous: true
requirements:
  - TGB-03
must_haves:
  truths:
    - "STT_URL, TTS_URL, TTS_STREAM_URL, VOICES_URL exist as exports from src/backend/config/media-endpoints.ts."
    - "voice.ts imports these constants from the new module instead of hardcoding them inline."
    - "MATRIX_HOMESERVER_BASE resolver (async function) exists in the new module, sourced from matrix_admin_creds.homeserverBase."
    - "grep for the hardcoded Tailscale IPs (100.113.23.63 and 100.80.122.111) in voice.ts returns zero matches."
  artifacts:
    - path: "src/backend/config/media-endpoints.ts"
      provides: "Shared TS constants for STT_URL, TTS_URL, TTS_STREAM_URL, VOICES_URL + async MATRIX_HOMESERVER_BASE resolver"
      exports: ["STT_URL", "TTS_URL", "TTS_STREAM_URL", "VOICES_URL", "getMatrixHomeserverBase"]
    - path: "src/backend/config/media-endpoints.test.ts"
      provides: "Test coverage: constant values match old inline values byte-for-byte; MATRIX_HOMESERVER_BASE returns admin creds base"
      min_lines: 30
    - path: "src/backend/database/routes/voice.ts"
      provides: "Refactored to import STT_URL/TTS_URL/TTS_STREAM_URL/VOICES_URL from the new module (no inline hardcodes)"
      contains: 'from "../../config/media-endpoints.js"'
  key_links:
    - from: "src/backend/database/routes/voice.ts"
      to: "src/backend/config/media-endpoints.ts"
      via: "import { STT_URL, TTS_URL, TTS_STREAM_URL, VOICES_URL } from \"../../config/media-endpoints.js\""
      pattern: "media-endpoints"
    - from: "src/backend/config/media-endpoints.ts"
      to: "src/backend/matrix/matrix-admin-creds-store.ts"
      via: "getMatrixAdminCreds() call inside getMatrixHomeserverBase()"
      pattern: "getMatrixAdminCreds"
---

<objective>
Create a shared TypeScript module holding all media / Matrix endpoint constants that will need to be consumed by BOTH `voice.ts` (existing) AND the bridge-config-writer (Plan 04, which serializes them into `/state/config.env` for the tg-bridge Docker service). Refactor `voice.ts` to import from this module — proving the "shared source of truth" invariant works.

Purpose: This is user-locked decision #3. Without this module, the tg-bridge will hardcode STT/Matrix URLs in `bridge.sh`, violating the Phase 79 ship-gate reliability check (grep of bridge.sh for the Tailscale IPs must return 0). This plan is Wave 1 because Plan 04 (bridge-config-writer) depends on this module existing.

Output:
- `src/backend/config/media-endpoints.ts` — NEW module. Exports `STT_URL`, `TTS_URL`, `TTS_STREAM_URL`, `VOICES_URL` as `export const` string literals (verbatim from voice.ts:28-34). Also exports `async function getMatrixHomeserverBase(): Promise<string | null>` that reads `matrix_admin_creds.homeserverBase` via `getMatrixAdminCreds()`.
- `src/backend/config/media-endpoints.test.ts` — NEW test file. Asserts constants are non-empty strings with expected shape; asserts `getMatrixHomeserverBase()` returns the stored admin creds base.
- `src/backend/database/routes/voice.ts` — MODIFIED. Delete the four `const STT_URL = "..."` / `const TTS_URL = "..."` / `const TTS_STREAM_URL = "..."` / `const VOICES_URL = "..."` inline declarations at lines ~27-34; replace with a single import from `../../config/media-endpoints.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/79-telegram-bridge-phase-b-substrate-promotion-identity-modal-t/79-CONTEXT.md
@.planning/phases/79-telegram-bridge-phase-b-substrate-promotion-identity-modal-t/79-RESEARCH.md
@.planning/phases/79-telegram-bridge-phase-b-substrate-promotion-identity-modal-t/79-PATTERNS.md
@src/backend/database/routes/voice.ts
@src/backend/matrix/matrix-admin-creds-store.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/backend/config/media-endpoints.ts + test</name>
  <read_first>
    - src/backend/config/media-endpoints.ts (target — new file; `src/backend/config/` directory does not yet exist and will be created)
    - src/backend/config/media-endpoints.test.ts (target — new file)
    - src/backend/database/routes/voice.ts (source of the exact string values — read lines 25-40 to capture STT_URL, TTS_URL, TTS_STREAM_URL, VOICES_URL verbatim; do NOT paraphrase, do NOT alter case, do NOT rewrite HTTP → HTTPS)
    - src/backend/matrix/matrix-admin-creds-store.ts (dependency — `getMatrixAdminCreds()` returns `{ homeserverBase, userId, accessToken, password } | null`; used by `getMatrixHomeserverBase()`)
    - .planning/phases/79-.../79-PATTERNS.md § `src/backend/config/media-endpoints.ts (shared TS constants)` — the design-from-first-principles guidance
    - .planning/phases/79-.../79-RESEARCH.md § Pitfall 5 (STT_URL currently hardcoded) + § Pitfall 6 (Matrix homeserver base URL — reuse admin creds)
    - .planning/phases/79-.../79-CONTEXT.md § user-locked #3 (STT_URL extraction into shared TS constant module)
  </read_first>
  <behavior>
    - Test 1: `import { STT_URL, TTS_URL, TTS_STREAM_URL, VOICES_URL } from './media-endpoints'` succeeds; all four are non-empty strings that start with "http://" or "https://".
    - Test 2: Each constant EQUALS the value currently hardcoded in `voice.ts` — the test hard-codes the expected string (byte-identical) so any drift breaks the test immediately.
    - Test 3: `getMatrixHomeserverBase()` returns `null` when no admin creds row exists (fresh DB); returns the stored `homeserverBase` string when a row exists.
    - Test 4: `getMatrixHomeserverBase` return-type asserts as `Promise<string | null>` (compile-time check via a `const _typecheck: string | null = await getMatrixHomeserverBase();` in the test).
  </behavior>
  <action>
    Per user-locked D-03 (STT_URL extraction into shared TS constant module — verbatim from CONTEXT § Locked decisions #3), create `src/backend/config/media-endpoints.ts` as a new module (directory must be created — no existing `src/backend/config/` directory per PATTERNS.md § No Analog Found).

    Read the current hardcoded values from `src/backend/database/routes/voice.ts` (lines ~28-34). The values at time of writing are:
    - `STT_URL` = `"http://100.80.122.111:8000/v1/audio/transcriptions"`
    - `TTS_URL` = `"http://100.80.122.111:8001/v1/audio/speech"`
    - `TTS_STREAM_URL` = `"http://100.80.122.111:8001/tts"` (per voice.ts:32-33 patch #237 comment)
    - `VOICES_URL` = `"http://100.80.122.111:8001/get_predefined_voices"`

    Read `voice.ts` FIRST — if any value has drifted since this plan was written, use whatever voice.ts currently holds (source of truth). Comment above each `export const` MUST cite `voice.ts:<line>` and the original patch note (e.g. patch #237 for TTS_STREAM_URL).

    File shape:
    ```
    // Phase 79 Plan 02 (D-03) — shared media endpoint constants.
    // Consumers: voice.ts (existing) + bridge-config-writer (Plan 04, which
    // serializes STT_URL + MATRIX_HOMESERVER_BASE into /state/config.env for
    // the tg-bridge Docker service, per CONTEXT § Locked decisions #3).
    //
    // Values are byte-for-byte migrated from voice.ts:28-34. If they need
    // to change in the future, change them HERE — both voice.ts and the
    // bridge inherit the update.

    export const STT_URL = "...";   // voice.ts:28 (Nelly-verified live 2026-07-27)
    export const TTS_URL = "...";   // voice.ts:31
    export const TTS_STREAM_URL = "...";  // voice.ts:33 (patch #237)
    export const VOICES_URL = "...";  // voice.ts:34

    // Async because matrix_admin_creds may not yet be ingested on fresh Skynet.
    // Returns null if no admin creds row exists.
    // Consumers (bridge-config-writer) MUST handle null (do not write config.env
    // until admin creds are present — per RESEARCH § Pitfall 6 Option 1).
    export async function getMatrixHomeserverBase(): Promise<string | null> {
      const { getMatrixAdminCreds } = await import("../matrix/matrix-admin-creds-store.js");
      const creds = await getMatrixAdminCreds();
      return creds?.homeserverBase ?? null;
    }
    ```

    Use dynamic `await import(...)` for the getMatrixAdminCreds dependency to avoid a static-import cycle (matrix-admin-creds-store.ts already depends on the database layer at module-load).

    Create `src/backend/config/media-endpoints.test.ts` with vitest tests covering all four behaviors. For the `getMatrixHomeserverBase()` tests, mock `getMatrixAdminCreds` via `vi.mock("../matrix/matrix-admin-creds-store.js", () => ({ getMatrixAdminCreds: vi.fn() }))` and toggle the return value across two `it()` blocks.

    Do NOT modify voice.ts in this task — Task 2 handles that. Keep this task's edit surface strictly to the two new files under `src/backend/config/`.
  </action>
  <acceptance_criteria>
    - `test -d src/backend/config && test -f src/backend/config/media-endpoints.ts && test -f src/backend/config/media-endpoints.test.ts` (dir + both files exist).
    - `grep -c '^export const STT_URL\|^export const TTS_URL\|^export const TTS_STREAM_URL\|^export const VOICES_URL' src/backend/config/media-endpoints.ts` returns 4.
    - `grep -c '^export async function getMatrixHomeserverBase' src/backend/config/media-endpoints.ts` returns 1.
    - `npx vitest run src/backend/config/media-endpoints.test.ts --reporter=basic` all tests pass.
    - The exact string value of `STT_URL` in `media-endpoints.ts` matches the current voice.ts value: `diff <(grep 'STT_URL = "http' src/backend/config/media-endpoints.ts | sed 's/.*STT_URL = //; s/;.*//') <(grep 'STT_URL = "http' src/backend/database/routes/voice.ts | head -1 | sed 's/.*STT_URL = //; s/;.*//')` returns no diff.
    - `npx tsc --noEmit -p tsconfig.backend.json 2>&1 | grep -E "src/backend/config/media-endpoints" | head` returns nothing.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/backend/config/media-endpoints.test.ts --reporter=basic</automated>
  </verify>
  <done>Module exists at the canonical path; all four constants byte-match the voice.ts originals; getMatrixHomeserverBase() correctly returns admin creds base or null; all tests pass; no TS errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor voice.ts to import media endpoints from the new module (delete inline consts)</name>
  <read_first>
    - src/backend/database/routes/voice.ts (target — full read, ~470 lines; verify all four constants are used ONLY within this file, no other file imports them)
    - src/backend/config/media-endpoints.ts (dependency — the new module from Task 1)
    - src/backend/database/routes/voice.test.ts (if exists) or grep for any voice.ts test file — the refactor must not change behavior
  </read_first>
  <behavior>
    - Test 1: voice.ts continues to hit `STT_URL` when handling STT requests (imported symbol, not inline const).
    - Test 2: `grep -c 'const STT_URL\|const TTS_URL\|const TTS_STREAM_URL\|const VOICES_URL' src/backend/database/routes/voice.ts` returns 0 (all four inline consts removed).
    - Test 3: Existing voice.ts test coverage (whatever the current suite is) remains green.
  </behavior>
  <action>
    Per D-03 (shared source-of-truth for media endpoints), refactor `src/backend/database/routes/voice.ts` to import the four constants from the module created in Task 1.

    Steps (surgical — do NOT touch unrelated code):
    1. Grep for any other file that imports STT_URL / TTS_URL / TTS_STREAM_URL / VOICES_URL from voice.ts: `grep -rn "from.*voice.ts.*STT_URL\|import.*STT_URL.*voice" src/` — expect zero results (all usages are internal to voice.ts per RESEARCH § Q2). If any external importer exists, fix that too by pointing it at `media-endpoints.js`.
    2. Delete the four inline `const STT_URL = "..."` / `const TTS_URL = "..."` / `const TTS_STREAM_URL = "..."` / `const VOICES_URL = "..."` declarations at voice.ts:~27-34. Keep the surrounding patch-note comments (like `// --- Locked STT endpoint (Nelly-verified live, 2026-07-27) ---`) since they document intent; just add a single-line follow-up like `// Phase 79 Plan 02 (D-03): moved to src/backend/config/media-endpoints.ts — shared with tg-bridge.`.
    3. At the top of voice.ts (with the other imports), add: `import { STT_URL, TTS_URL, TTS_STREAM_URL, VOICES_URL } from "../../config/media-endpoints.js";` — the `.js` suffix is required (ESM under NodeNext resolution).
    4. Do NOT change any function signatures, control flow, error handling, or logging in voice.ts. This refactor is byte-identical semantically.
  </action>
  <acceptance_criteria>
    - `grep -c 'const STT_URL\|const TTS_URL\|const TTS_STREAM_URL\|const VOICES_URL' src/backend/database/routes/voice.ts` returns 0.
    - `grep -c 'from "../../config/media-endpoints' src/backend/database/routes/voice.ts` returns 1.
    - `grep -c 'STT_URL\|TTS_URL\|TTS_STREAM_URL\|VOICES_URL' src/backend/database/routes/voice.ts` remains >= 4 (the constants are still USED, just imported not declared).
    - Existing voice.ts tests still pass: `npx vitest run src/backend/database/routes/voice --reporter=basic` (or the closest existing suite; if no test exists for voice.ts specifically, run `npx vitest run --changed --reporter=basic` and confirm no failures).
    - `npx tsc --noEmit -p tsconfig.backend.json 2>&1 | grep -E "src/backend/database/routes/voice\.ts" | head` returns nothing.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'const STT_URL\|const TTS_URL\|const TTS_STREAM_URL\|const VOICES_URL' src/backend/database/routes/voice.ts | grep -q '^0$' && grep -c 'from "../../config/media-endpoints' src/backend/database/routes/voice.ts</automated>
  </verify>
  <done>voice.ts imports the four constants from the new module; zero inline hardcodes remain; behavior unchanged. The "shared source of truth" invariant is now enforceable by grep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| module-load ↔ DB access | `getMatrixHomeserverBase()` awaits the DB; must not fire at import time (would race with DB init) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-02-01 | Tampering | Endpoint constant drift | mitigate | Task 1 acceptance-criteria diff catches any drift from voice.ts values at plan time; long-term, the shared module IS the single source (drift is impossible after Task 2). |
| T-79-02-02 | Information Disclosure | Endpoint URLs visible in module | accept | Endpoints are Tailscale-internal IPs, already visible to any operator with source-tree access; no new exposure. |
| T-79-02-03 | Denial of Service | `getMatrixHomeserverBase()` fires at import time | mitigate | Function is async and called by consumers on-demand (not at module load); dynamic import avoids static-import cycle. |
| T-79-02-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages. |
</threat_model>

<verification>
- `npx vitest run src/backend/config/media-endpoints.test.ts --reporter=basic` — all tests green.
- `npx vitest run src/backend/database/routes/voice --reporter=basic` (or nearest existing suite) — no regressions.
- `grep -rn 'const STT_URL\|const TTS_URL\|const TTS_STREAM_URL\|const VOICES_URL' src/backend/database/routes/voice.ts` returns 0.
- `grep -rn '100\.80\.122\.111' src/backend/database/routes/voice.ts` returns 0 (all hardcoded IPs moved to the shared module).
</verification>

<success_criteria>
- Shared TS module `src/backend/config/media-endpoints.ts` exists with 4 endpoint constants + `getMatrixHomeserverBase()` async resolver.
- `voice.ts` imports from the shared module; zero inline hardcoded IP strings remain in voice.ts.
- `getMatrixHomeserverBase()` correctly returns `matrix_admin_creds.homeserverBase` (or null on fresh install).
- All tests green.
</success_criteria>

<output>
Create `.planning/phases/79-telegram-bridge-phase-b-substrate-promotion-identity-modal-t/79-02-SUMMARY.md` when done. Include: (a) the four exact string values exported, (b) grep counts for the acceptance criteria, (c) any consumers of voice.ts's old inline consts found by the discovery grep in Task 2.
</output>
