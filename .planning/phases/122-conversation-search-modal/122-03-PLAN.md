---
phase: 122-conversation-search-modal
plan: 03
type: execute
wave: 2
depends_on:
  - 122-02
files_modified:
  - src/ui/api/conversation-search-api.ts
  - src/ui/state/search-store.ts
  - src/ui/state/search-store.test.ts
  - src/ui/features/pretty-conversations/ConversationSearchModal.tsx
  - src/ui/features/pretty-conversations/ConversationSearchModal.test.tsx
  - src/ui/features/pretty-conversations/ConversationSearchRow.tsx
  - src/ui/features/pretty-conversations/pretty-conversations.css
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/AppShell.tsx
autonomous: true
requirements:
  - D-01
  - D-03
  - D-04
  - D-05
  - D-06
  - D-11
  - D-12
  - D-14
  - D-15
  - D-16

must_haves:
  truths:
    - "A magnifying-glass button appears in the sidebar header's `.pv-header-actions` cluster; clicking it opens the ConversationSearchModal (D-03: button-only, no keyboard shortcut)"
    - "Typing in the modal's input does NOT fire the network (D-01); pressing Enter fires POST /conversation-search once"
    - "Result rows render title (aiTitle || identityKey), identity + host subtext, and a snippet where the matched substring is visually highlighted via a React `<span className='pv-search-hit'>`, NOT dangerouslySetInnerHTML (D-11)"
    - "Clicking an active result invokes the same open-conversation flow AppShell already uses for `onDetachedRowClick` (openTab + selectConversationDeferred) and closes the modal (D-04, D-14)"
    - "Clicking an archived result triggers `window.alert(...)` with a blunt \"coming soon\" message and the modal stays open (D-15); no open-flow is attempted since unarchiving is out of scope (D-16)"
    - "Clicking `Load more` re-fetches with `offset += 20`, appends new rows to the existing list, hides the button when `hasMore` is false (D-12)"
    - "Closing and reopening the modal shows the last query and accumulated results (D-05); empty state only shows on first-ever open or after the user clears the input (D-06)"
  artifacts:
    - path: "src/ui/api/conversation-search-api.ts"
      provides: "searchConversations(query, offset, limit) — authApi wrapper mirroring workspace-api.ts pattern; exports ConversationSearchResult + ConversationSearchResponse types"
      exports: ["searchConversations", "ConversationSearchResult", "ConversationSearchResponse"]
    - path: "src/ui/state/search-store.ts"
      provides: "Module-scoped store (useSyncExternalStore) holding { query, results, hasMore, isFetching, hasEverOpened, error }; exports hook + mutators"
      exports: ["useSearchState", "setSearchQuery", "clearSearch", "startNewSearch", "appendResults", "setFetching", "setError"]
    - path: "src/ui/features/pretty-conversations/ConversationSearchModal.tsx"
      provides: "Radix DialogPrimitive modal shell with header (title + close X), body (search input + results list + load-more), footer (result count / hasMore indicator)"
      exports: ["ConversationSearchModal"]
      min_lines: 150
    - path: "src/ui/features/pretty-conversations/ConversationSearchRow.tsx"
      provides: "Single result-row component; renders title, identity+host, snippet with highlight span"
      exports: ["ConversationSearchRow"]
    - path: "src/ui/features/pretty-conversations/pretty-conversations.css"
      provides: "`.pv-search-hit` CSS rule for the snippet highlight"
      contains: "pv-search-hit"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "New magnifying-glass header button + modal mount + onSearchResultOpenActive prop"
      contains: "ConversationSearchModal"
    - path: "src/ui/AppShell.tsx"
      provides: "onSearchResultOpenActive handler passed to PrettyConversationsPanel (mirrors onDetachedRowClick shape at line 3068)"
      contains: "onSearchResultOpenActive"
  key_links:
    - from: "src/ui/features/pretty-conversations/ConversationSearchModal.tsx"
      to: "src/ui/api/conversation-search-api.ts"
      via: "fetch on Enter key + on load-more click"
      pattern: "searchConversations"
    - from: "src/ui/features/pretty-conversations/ConversationSearchModal.tsx"
      to: "src/ui/state/search-store.ts"
      via: "useSyncExternalStore hook + mutator calls"
      pattern: "useSearchState|startNewSearch|appendResults"
    - from: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      to: "src/ui/features/pretty-conversations/ConversationSearchModal.tsx"
      via: "modal mount + open state useState"
      pattern: "ConversationSearchModal"
    - from: "src/ui/AppShell.tsx"
      to: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      via: "onSearchResultOpenActive prop threading (mirrors onDetachedRowClick at AppShell.tsx:3068)"
      pattern: "onSearchResultOpenActive"
---

<objective>
Ship the frontend of the search modal: API client, module-scoped store (for D-05 state persistence), the modal shell (Radix DialogPrimitive lifted from NewConversationModal), the result-row component, the header button + modal mount inside PrettyConversationsPanel, and the AppShell prop that resolves a search-result click into a tab-open (mirroring the existing `onDetachedRowClick` at AppShell.tsx:3068).

This plan does NOT remove the existing sidebar filter — that lands in plan 04 to satisfy D-18's ordering constraint (modal must be usable before filter is removed).

Purpose: Deliver the user-facing search interaction end-to-end. After this plan lands and its tests pass, the phase is functionally complete except for the D-17 filter cleanup.
Output: A working modal that opens on button click, fires on Enter, renders results with snippets + highlights, paginates via load-more, closes-on-active-click, alerts-on-archived-click, and preserves state across opens.

Contract prerequisite from plan 02: Each `ConversationSearchResult` MUST include `tmuxSessionName: string | null` in addition to the 10 fields already listed. If plan 02's SUMMARY does not include this field, this plan's Task 1 MUST first add it to the backend result-row builder in `src/backend/database/routes/conversation-search.ts` (a small forward-patch — derive it from the identity key using the same rule the existing sidebar uses in `conversation-store.ts`; grep `targetTmuxSession` in the store to find the derivation), because `AppShell.tsx:3068`'s existing `onDetachedRowClick` handler reads `row.targetTmuxSession` and calls `openTab(host, "terminal", undefined, { targetTmuxSession, label, allowCreateTmux: false })`. A search-result click that skips this field cannot open a tab.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-conversation-search-modal/122-CONTEXT.md
@.planning/phases/122-conversation-search-modal/122-RESEARCH.md
@.planning/phases/122-conversation-search-modal/122-02-SUMMARY.md
@src/ui/features/pretty-conversations/NewConversationModal.tsx
@src/ui/features/pretty-conversations/CreateProjectModal.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
@src/ui/state/conversation-store.ts
@src/ui/api/workspace-api.ts
@src/ui/api/identity-archive-api.ts
@src/ui/main-axios.ts
@src/ui/AppShell.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: search-store + API client + tmuxSessionName forward-patch to backend (with tests)</name>
  <files>
    src/ui/state/search-store.ts,
    src/ui/state/search-store.test.ts,
    src/ui/api/conversation-search-api.ts
  </files>
  <read_first>
    - src/ui/state/conversation-store.ts (lines 2345-2424 — the archived-fleet-rows slice pattern: module-scoped `let state`, `listeners = new Set`, `subscribe/notify/getSnapshot`, exported hook via `useSyncExternalStore`, exported mutators. Also grep the file for `targetTmuxSession` to find how the row-shape derives the tmux session name from an identity key; the search-result equivalent must use the same rule)
    - src/ui/api/workspace-api.ts (lines 1-100 — `authApi.post(url, body)` + `handleApiError(err, context)` + the `axios.isAxiosError` error-class extraction pattern; mirror for the search API)
    - src/ui/api/identity-archive-api.ts (lines 1-32 — minimal API wrapper reference; simpler than workspace-api, closer in size to what the search API needs)
    - src/ui/main-axios.ts (verify `authApi` export and `handleApiError` signature)
    - .planning/phases/122-conversation-search-modal/122-02-SUMMARY.md (canonical `ConversationSearchResult` TypeScript type shape from the backend — mirror byte-for-byte on the client; if `tmuxSessionName: string | null` is not in the field list, forward-patch the backend as described in the action)
    - src/backend/database/routes/conversation-search.ts (from plan 02 — read the result-row builder inside `runOneHost`; the forward-patch adds `tmuxSessionName` to the row object)
    - src/ui/AppShell.tsx (line 3068-3081 — read `onDetachedRowClick` to confirm the exact field name it reads: `row.targetTmuxSession`. The API type field name may be either `tmuxSessionName` OR `targetTmuxSession` — pick whichever matches the sidebar row shape so client-side code is consistent)
    - .planning/phases/122-conversation-search-modal/122-CONTEXT.md (D-05 state persistence, D-06 empty-state gating rules)
    - .planning/phases/122-conversation-search-modal/122-RESEARCH.md (`## Architecture Patterns` Pattern 4 for the store recipe; `## Code Examples` Frontend API wrapper section for the API client shape)
  </read_first>
  <behavior>
    search-store tests:
    - Initial state: `query: ""`, `results: []`, `hasMore: false`, `isFetching: false`, `hasEverOpened: false`, `error: null`
    - `setSearchQuery("foo")` updates `query` and sets `hasEverOpened: true`, notifies subscribers
    - `clearSearch()` resets `query: ""`, `results: []`, `hasMore: false`, `error: null` but preserves `hasEverOpened: true` (so empty-state does not reappear after a search + clear per D-06)
    - `startNewSearch("foo")` atomically sets `{ query: "foo", results: [], hasMore: false, isFetching: true, error: null, hasEverOpened: true }` in a single notify
    - `appendResults([...rows], hasMoreFlag)` concatenates to existing results and updates hasMore + clears isFetching
    - `setFetching(true)` sets isFetching + clears error; `setFetching(false)` sets isFetching false without touching error
    - `setError("some_class")` sets error + isFetching false
    - Subscribers registered via `subscribe()` (private) get notified on every mutation
    - `useSearchState` returns the current snapshot (renderHook test)

    conversation-search-api tests:
    - `searchConversations(query, offset, limit)` calls `authApi.post("/conversation-search", { query, offset, limit })` and returns `response.data`
    - On axios error with `error.response.data.error === "query_too_long"` throws an `Error` whose `name === "ConversationSearchError"` and `message === "query_too_long"`
    - On non-axios error, calls `handleApiError(err, "search conversations")` and rethrows
  </behavior>
  <action>
    First, confirm the backend field for tmux-session name. Grep `src/backend/database/routes/conversation-search.ts` for `tmuxSessionName` or `targetTmuxSession`. If neither is present in the result-row object built inside `runOneHost`, forward-patch the backend to add it now: derive the value using the SAME rule the sidebar uses (grep `src/ui/state/conversation-store.ts` for `targetTmuxSession` to find the derivation function OR a naming convention like `\`gsd-\${identityKey}\``; whichever the codebase uses today, mirror byte-for-byte in the backend). Add the field to the exported TypeScript `ConversationSearchResult` type in `conversation-search.ts` AND to a corresponding test case in `conversation-search.test.ts`. Do NOT invent a new naming rule.

    Create `src/ui/api/conversation-search-api.ts`:
    - Import `axios`, `authApi`, `handleApiError` from `@/main-axios`
    - Export TypeScript types `ConversationSearchResult` and `ConversationSearchResponse` — copy the field list VERBATIM from `.planning/phases/122-conversation-search-modal/122-02-SUMMARY.md`'s type dump, plus `tmuxSessionName: string | null` (or `targetTmuxSession: string | null` — match the backend field name): `transcriptPath: string`, `transcriptMtime: number`, `identityKey: string`, `hostId: number`, `hostName: string`, `aiTitle: string | null`, `snippet: string`, `hitStart: number`, `hitLength: number`, `isArchived: boolean`, `tmuxSessionName: string | null`. Top-level `{ results: ConversationSearchResult[]; hasMore: boolean }`
    - Export `async function searchConversations(query: string, offset: number, limit: number): Promise<ConversationSearchResponse>` with body: `try { const response = await authApi.post("/conversation-search", { query, offset, limit }); return response.data as ConversationSearchResponse; } catch (error) { if (axios.isAxiosError(error)) { const backendClass = error.response?.data?.error; if (typeof backendClass === "string" && backendClass.length > 0) { const rich = new Error(backendClass); rich.name = "ConversationSearchError"; throw rich; } } handleApiError(error, "search conversations"); throw error; }`

    Create `src/ui/state/search-store.ts`:
    - Import `useSyncExternalStore` from `react`
    - Import `ConversationSearchResult` type from `@/api/conversation-search-api`
    - Define `type SearchState = { query: string; results: ConversationSearchResult[]; hasMore: boolean; isFetching: boolean; hasEverOpened: boolean; error: string | null }`
    - Module-scoped `let state: SearchState = { query: "", results: [], hasMore: false, isFetching: false, hasEverOpened: false, error: null }`
    - `const listeners = new Set<() => void>()`; `function subscribe(l) { listeners.add(l); return () => listeners.delete(l); }`; `function notify() { for (const l of listeners) l(); }`; `function getSnapshot() { return state; }`
    - `export function useSearchState() { return useSyncExternalStore(subscribe, getSnapshot, getSnapshot); }`
    - `export function setSearchQuery(q: string): void { state = { ...state, query: q, hasEverOpened: true }; notify(); }`
    - `export function clearSearch(): void { state = { ...state, query: "", results: [], hasMore: false, error: null }; notify(); }` — `hasEverOpened` deliberately preserved (D-06 gate)
    - `export function startNewSearch(q: string): void { state = { query: q, results: [], hasMore: false, isFetching: true, hasEverOpened: true, error: null }; notify(); }`
    - `export function appendResults(rows: ConversationSearchResult[], hasMore: boolean): void { state = { ...state, results: [...state.results, ...rows], hasMore, isFetching: false, error: null }; notify(); }`
    - `export function setFetching(v: boolean): void { state = { ...state, isFetching: v, ...(v ? { error: null } : {}) }; notify(); }`
    - `export function setError(msg: string): void { state = { ...state, error: msg, isFetching: false }; notify(); }`
    - `export function _resetForTests(): void { state = { query: "", results: [], hasMore: false, isFetching: false, hasEverOpened: false, error: null }; notify(); }`

    Create `src/ui/state/search-store.test.ts` covering every behavior bullet using vitest + `@testing-library/react`'s `renderHook`. Also add a co-located API-client smoke test at `src/ui/api/conversation-search-api.test.ts` (small — mock axios).
  </action>
  <verify>
    <automated>cd /home/ubuntu/fleet/identities/morpheus-box-maintainer/workspace/skynet &amp;&amp; npx vitest run src/ui/state/search-store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/api/conversation-search-api.ts` exports `searchConversations`, `ConversationSearchResult`, `ConversationSearchResponse`
    - `ConversationSearchResult` field list matches the backend type in `src/backend/database/routes/conversation-search.ts` (grep both files: the same 11 field names appear in each)
    - `src/ui/state/search-store.ts` exports `useSearchState`, `setSearchQuery`, `clearSearch`, `startNewSearch`, `appendResults`, `setFetching`, `setError`, `_resetForTests`
    - `clearSearch()` preserves `hasEverOpened: true` (test proves this — this is the D-06 gate)
    - `startNewSearch()` sets all six state fields atomically in a single notify (test asserts single subscriber callback fires)
    - `search-store.test.ts` passes (`npx vitest run src/ui/state/search-store.test.ts` returns 0)
    - Backend forward-patch (if needed) is landed: `grep -q "tmuxSessionName\|targetTmuxSession" src/backend/database/routes/conversation-search.ts` returns match, and the corresponding backend test still passes
    - `tsc --noEmit -p .` returns 0 errors from the new/edited files
  </acceptance_criteria>
  <done>Store + API client + backend field parity are all green and tested. The modal in Task 2 can import both and get compile-time-checked types. The AppShell handler in Task 3 has a real `tmuxSessionName` field to read from result rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ConversationSearchModal + ConversationSearchRow components + `.pv-search-hit` CSS (with tests)</name>
  <files>
    src/ui/features/pretty-conversations/ConversationSearchModal.tsx,
    src/ui/features/pretty-conversations/ConversationSearchModal.test.tsx,
    src/ui/features/pretty-conversations/ConversationSearchRow.tsx,
    src/ui/features/pretty-conversations/pretty-conversations.css
  </files>
  <read_first>
    - src/ui/features/pretty-conversations/NewConversationModal.tsx (lines 268-441 — the modal chrome recipe to lift verbatim: `DialogPrimitive.Root`, `.Portal`, `.Overlay` at `z-[110]` with backdrop-blur, `.Content` at `z-[120]` with the glass-morphism style block, `onInteractOutside={e => e.preventDefault()}` guard, responsive `absolute inset-4` mobile + `md:max-w-[560px] md:max-h-[720px] md:left-1/2 md:top-1/2 md:-translate-x-1/2 md:-translate-y-1/2` desktop)
    - src/ui/features/pretty-conversations/CreateProjectModal.tsx (secondary modal reference — form-body + footer layout patterns useful for the modal body layout)
    - src/ui/api/conversation-search-api.ts (imports `searchConversations` and the types)
    - src/ui/state/search-store.ts (imports the store hooks + mutators including `startNewSearch`)
    - src/ui/features/pretty-conversations/pretty-conversations.css (grep for `pv-search-hit` — expect zero matches before this task; this task appends a new rule)
    - .planning/phases/122-conversation-search-modal/122-CONTEXT.md (D-01 Enter-only fire, D-04 close-on-active-click, D-05 state persistence, D-06 empty-state gating, D-11 row shape, D-12 pagination, D-14 active-click behavior, D-15 archived-click behavior)
    - .planning/phases/122-conversation-search-modal/122-RESEARCH.md (`## Architecture Patterns` Pattern 3 for the exact modal shell to copy; `## Common Pitfalls` Pitfall 3 for the tenant-scoping note; `## Open Questions` #4 for the highlight-render approach: React text children + `<span>` split around hit indices, never `dangerouslySetInnerHTML`)
  </read_first>
  <behavior>
    ConversationSearchModal tests (using `@testing-library/react` + jsdom + vitest):
    - Rendering with `open={false}` mounts nothing visible (Radix Portal not open)
    - Rendering with `open={true}` and store state `hasEverOpened: false` shows the empty-state placeholder text "Type a query and press Enter" (or equivalent)
    - Typing in the input does NOT call `searchConversations` (mocked) — assert mock call count remains 0 after `userEvent.type(input, "hello")`
    - Pressing Enter with a non-empty trimmed query calls `searchConversations(query, 0, 20)` exactly once, calls `startNewSearch(query)` before the fetch, and on resolve calls `appendResults(response.results, response.hasMore)` exactly once
    - Result rows render `aiTitle || identityKey` as title, `identityKey · hostName` as subtext, and a snippet with a `<span class="pv-search-hit">` wrapping exactly `snippet.slice(hitStart, hitStart + hitLength)` when `hitStart >= 0`
    - Row DOM does NOT contain any `dangerouslySetInnerHTML` attribute (grep the rendered HTML)
    - Clicking an active result (row where `isArchived === false`) calls `props.onOpenActiveConversation(result)` AND calls `props.onOpenChange(false)` (D-04, D-14)
    - Clicking an archived result (row where `isArchived === true`) calls `window.alert` with a message matching `/coming soon/i` AND does NOT call `onOpenChange` (D-15); mock via `vi.spyOn(window, "alert")`
    - Load-more button visible when `hasMore === true`; clicking calls `searchConversations(query, results.length, 20)` and calls `appendResults` on resolve
    - Load-more button hidden when `hasMore === false`
    - Unmounting the modal (setting open={false}) then remounting (open={true} again) still shows the last query + accumulated results — assert D-05 by verifying `useSearchState()` snapshot is identical before/after the unmount cycle
    - After `startNewSearch("foo")` + resolve with empty results, empty-state does NOT re-render (shows "No results for \"foo\"" instead of "Type a query and press Enter") — asserts D-06 corner
    - No keyboard shortcut binding — the modal component has no global `keydown` listener registered on `document` (grep the component source for `document.addEventListener`, expect 0)
  </behavior>
  <action>
    Create `src/ui/features/pretty-conversations/ConversationSearchModal.tsx`:
    - Imports: `useEffect`, `useState` from `react`; `Dialog as DialogPrimitive` from `radix-ui`; `X`, `Search`, `Loader2` from `lucide-react`; `cn` from `@/lib/utils`; `searchConversations` + types from `@/api/conversation-search-api`; `useSearchState`, `startNewSearch`, `clearSearch`, `appendResults`, `setError` from `@/state/search-store`; `ConversationSearchRow` from `./ConversationSearchRow`
    - Props type: `{ open: boolean; onOpenChange: (open: boolean) => void; onOpenActiveConversation: (result: ConversationSearchResult) => void }`
    - Component body:
      - Read store via `const state = useSearchState()`
      - Local controlled-input value: `const [localValue, setLocalValue] = useState(state.query)`
      - `useEffect` on `open` transition true→X: when open becomes true AND the stored query differs from localValue, re-seed local from store (respects D-05: reopening shows last query even if user modified without pressing Enter)
      - `handleEnter`: `const trimmed = localValue.trim(); if (!trimmed) return; startNewSearch(trimmed); try { const response = await searchConversations(trimmed, 0, 20); appendResults(response.results, response.hasMore); } catch (err) { setError(err instanceof Error ? err.message : "unknown_error"); }`
      - `handleLoadMore`: `try { const response = await searchConversations(state.query, state.results.length, 20); appendResults(response.results, response.hasMore); } catch (err) { setError(err instanceof Error ? err.message : "unknown_error"); }` — note appendResults itself flips isFetching false; the load-more button's disabled state binds to `state.isFetching`; before the fetch, set fetching via a small direct mutation OR call `setFetching(true)` (add to imports)
      - `handleClear`: `clearSearch(); setLocalValue("")`
      - `handleRowClick(result)`: `if (result.isArchived) { window.alert("Opening archived conversations isn't wired up yet — coming soon."); return; } onOpenActiveConversation(result); onOpenChange(false);`
    - JSX (lift NewConversationModal.tsx structure verbatim):
      - `<DialogPrimitive.Root open={open} onOpenChange={onOpenChange} modal={true}>`
      - `<DialogPrimitive.Portal>`
      - `<DialogPrimitive.Overlay className={cn("absolute inset-0 z-[110] bg-black/40", "supports-backdrop-filter:backdrop-blur-xs duration-100", "data-open:animate-in data-open:fade-in-0", "data-closed:animate-out data-closed:fade-out-0")} />`
      - `<DialogPrimitive.Content onInteractOutside={(e) => e.preventDefault()} className={cn("absolute inset-4 z-[120] outline-none", "flex flex-col overflow-hidden rounded-[24px]", "md:max-w-[560px] md:max-h-[720px] md:left-1/2 md:top-1/2 md:right-auto md:bottom-auto md:-translate-x-1/2 md:-translate-y-1/2", "data-open:animate-in data-open:fade-in-0 data-open:zoom-in-95 duration-100", "data-closed:animate-out data-closed:fade-out-0 data-closed:zoom-out-95")} style={... same glass style as NewConversationModal ...}>`
      - `<DialogPrimitive.Title className="sr-only">Search conversations</DialogPrimitive.Title>`
      - Header row: search icon + `<input value={localValue} onChange={e => setLocalValue(e.target.value)} onKeyDown={e => { if (e.key === "Enter") { e.preventDefault(); void handleEnter(); } }} placeholder="Search conversations..." data-testid="conversation-search-input" autoFocus />` + clear button (when `localValue.length > 0`) + close X button (calls `onOpenChange(false)`)
      - Body:
        - if `state.isFetching && state.results.length === 0` → spinner + "Searching..."
        - else if `!state.hasEverOpened || (state.query === "" && state.results.length === 0 && localValue.length === 0)` → empty state "Type a query and press Enter"
        - else if `state.results.length === 0 && !state.isFetching` → `No results for "{state.query}"`
        - else → `state.results.map(r => <ConversationSearchRow key={r.transcriptPath} result={r} onClick={() => handleRowClick(r)} />)`
      - Footer: if `state.hasMore` → `<button onClick={() => void handleLoadMore()} disabled={state.isFetching} data-testid="conversation-search-load-more">Load more</button>`; if `state.error` → `<div role="alert">{state.error}</div>`
      - `</DialogPrimitive.Content></DialogPrimitive.Portal></DialogPrimitive.Root>`
    - Style: tailwind classes matching NewConversationModal.tsx patterns; keep under ~280 lines

    Create `src/ui/features/pretty-conversations/ConversationSearchRow.tsx`:
    - Props: `{ result: ConversationSearchResult; onClick: () => void }`
    - Body: renders `<button onClick={onClick} data-testid={\`conversation-search-row-\${result.transcriptPath}\`} data-archived={result.isArchived ? "true" : "false"}>`
    - Inside:
      - Title line: `<div>{result.aiTitle || result.identityKey}{result.isArchived && <span className="pv-search-archived-pill">archived</span>}</div>`
      - Subtext line: `<div>{result.identityKey} · {result.hostName}</div>`
      - Snippet with highlight: if `result.hitStart >= 0 && result.hitLength > 0`:
        - `const before = result.snippet.slice(0, result.hitStart)`
        - `const hit = result.snippet.slice(result.hitStart, result.hitStart + result.hitLength)`
        - `const after = result.snippet.slice(result.hitStart + result.hitLength)`
        - Render `<div>{before}<span className="pv-search-hit">{hit}</span>{after}</div>`
      - Else render the raw snippet in a plain div

    Edit `src/ui/features/pretty-conversations/pretty-conversations.css`:
    - Append (near the end of the file, in a new section commented `/* Phase 122: search modal — result-row highlight span. Kept in this stylesheet (not the modal's tsx) so future edits stay colocated with related pv-* rules. */`) new rules:
      - `.pv-search-hit { background: rgba(255, 215, 100, 0.35); color: inherit; padding: 0 2px; border-radius: 2px; font-weight: 600; }`
      - `.pv-search-archived-pill { display: inline-block; margin-left: 8px; padding: 1px 6px; font-size: 10px; text-transform: uppercase; letter-spacing: 0.5px; background: rgba(180, 140, 90, 0.35); color: #e8e4d8; border-radius: 999px; }`

    Create `src/ui/features/pretty-conversations/ConversationSearchModal.test.tsx` covering every behavior bullet using `@testing-library/react` + `vi.mock('@/api/conversation-search-api', () => ({ searchConversations: vi.fn() }))` + `_resetForTests()` from search-store in `beforeEach`. Use `@testing-library/user-event` for keyboard interactions.
  </action>
  <verify>
    <automated>cd /home/ubuntu/fleet/identities/morpheus-box-maintainer/workspace/skynet &amp;&amp; npx vitest run src/ui/features/pretty-conversations/ConversationSearchModal.test.tsx src/ui/state/search-store.test.ts &amp;&amp; grep -q 'pv-search-hit' src/ui/features/pretty-conversations/pretty-conversations.css</automated>
  </verify>
  <acceptance_criteria>
    - `ConversationSearchModal.tsx` uses `DialogPrimitive.Root`, `.Portal`, `.Overlay`, `.Content` (grep confirms all four)
    - `ConversationSearchModal.tsx` does NOT contain `dangerouslySetInnerHTML` (grep returns 0)
    - `ConversationSearchModal.tsx` does NOT register `document.addEventListener` — no global keyboard shortcut (grep returns 0)
    - `ConversationSearchRow.tsx` exports the component; the snippet-with-hit path uses a `<span className="pv-search-hit">` (grep confirms)
    - `pretty-conversations.css` contains both `.pv-search-hit` and `.pv-search-archived-pill` rules
    - All behavior-bullet tests in `ConversationSearchModal.test.tsx` pass
    - Vitest run of both test files exits 0
    - `tsc --noEmit -p .` returns 0 errors from the new/edited files
  </acceptance_criteria>
  <done>Modal + row components are complete, tested, and provably respect D-01, D-04, D-05, D-06, D-11, D-12, D-14, D-15. The next task wires them into the sidebar.</done>
</task>

<task type="auto">
  <name>Task 3: Wire header button + modal mount into PrettyConversationsPanel + AppShell handler</name>
  <files>
    src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx,
    src/ui/AppShell.tsx
  </files>
  <read_first>
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (lines 2304-2370 — the `.pv-header-actions` cluster containing all existing header buttons; the new search button lands as the FIRST child of this cluster. Read line 67 for the existing `lucide-react` import so `Search` can be confirmed already imported. Read the top of the file for the current props type declaration — need to add `onSearchResultOpenActive?: (result: ConversationSearchResult) => void`)
    - src/ui/AppShell.tsx (lines 3068-3081 — the existing `onDetachedRowClick` handler passed to `PrettyConversationsPanel`; verbatim shape: `const host = row.host; if (!host) return; const sessionName = row.targetTmuxSession; if (!sessionName) return; const newTabId = openTab(host, "terminal", undefined, { targetTmuxSession: sessionName, label: sessionName, allowCreateTmux: false }); selectConversationDeferred(newTabId); if (isTouchDevice) navigateToView(); if (isMobile) setSidebarOpen(false);`. The new `onSearchResultOpenActive` handler mirrors this shape exactly, adapted to `result: ConversationSearchResult`)
    - src/ui/features/pretty-conversations/ConversationSearchModal.tsx (props signature: `{ open, onOpenChange, onOpenActiveConversation }`)
    - src/ui/api/conversation-search-api.ts (type import for `ConversationSearchResult`)
    - .planning/phases/122-conversation-search-modal/122-CONTEXT.md (D-03 no keyboard shortcut — button is the ONLY open path)
    - .planning/phases/122-conversation-search-modal/122-RESEARCH.md (`## Code Examples` "Modal → panel → header wiring" section for the exact button JSX shape; `## Open Questions` #2 for the button-placement rationale — leftmost inside `.pv-header-actions`)
  </read_first>
  <action>
    Edit `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`:
    - Confirm line 67 imports include `Search` from `lucide-react` (it already does per the existing filter — do NOT remove that import; plan 04 handles removal + re-add)
    - Add `import { ConversationSearchModal } from "./ConversationSearchModal";` alongside other feature-local imports near the top of the imports section
    - Add `import type { ConversationSearchResult } from "@/api/conversation-search-api";` at the top of the imports (types-only)
    - Extend the component's props type/interface to include `onSearchResultOpenActive?: (result: ConversationSearchResult) => void` (adjacent to the existing `onDetachedRowClick` prop declaration)
    - Add local state near the other useState calls (~line 896 area): `const [searchModalOpen, setSearchModalOpen] = useState(false);`
    - In the JSX at `.pv-header-actions` (line 2304+), add a NEW button as the FIRST child of that div (before the existing `pv-pencil` "New conversation" button at ~L2313): `<button type="button" className="pv-pencil" aria-label="Search conversations" title="Search conversations" data-testid="pv-header-search-button" onClick={() => setSearchModalOpen(true)}><Search size={18} /></button>`. Gate it identically to the existing New-conversation button (same `showPencilButton` conditional if present in the surrounding JSX)
    - Mount the modal at the end of the top-level returned JSX tree (just before the closing tag of the panel's root wrapper element): `<ConversationSearchModal open={searchModalOpen} onOpenChange={setSearchModalOpen} onOpenActiveConversation={(result) => onSearchResultOpenActive?.(result)} />` (or `props.onSearchResultOpenActive?.(result)` depending on whether props is destructured — match the file's existing convention)

    Edit `src/ui/AppShell.tsx`:
    - Add at the top of the imports: `import type { ConversationSearchResult } from "@/api/conversation-search-api";`
    - Find the `<PrettyConversationsPanel ... onDetachedRowClick={(row) => { ... }} />` mount at line 3068
    - Add a new sibling prop on the same JSX element, immediately after `onDetachedRowClick`: `onSearchResultOpenActive={(result) => { const host = hostsById.get(result.hostId); if (!host) return; const sessionName = result.tmuxSessionName; if (!sessionName) return; const newTabId = openTab(host, "terminal", undefined, { targetTmuxSession: sessionName, label: result.aiTitle || sessionName, allowCreateTmux: false }); selectConversationDeferred(newTabId); if (isTouchDevice) navigateToView(); if (isMobile) setSidebarOpen(false); }}`
    - `hostsById` — grep AppShell.tsx for the actual lookup name (may be `hostsById`, `hostMap`, `hostLookup`, or similar); adapt the identifier to whatever the file uses in the surrounding scope of line 3068. The shape at line 3069 is `const host = row.host` — that field is present on the sidebar row because the row shape carries it; the search result doesn't, hence the `hostsById.get(result.hostId)` lookup. If the file's existing scope at that line does NOT have a hostsById-shaped lookup available, derive it from wherever the sidebar rows get populated with `.host` — likely a `hosts` array in scope: `const host = hosts.find(h => h.id === result.hostId)`. Use whichever pattern the file already uses for host-id-to-host lookup elsewhere.
    - `result.tmuxSessionName` field name must match whatever field name plan 03 Task 1 landed on (either `tmuxSessionName` or `targetTmuxSession` — grep the API type to confirm and use the exact name)
  </action>
  <verify>
    <automated>cd /home/ubuntu/fleet/identities/morpheus-box-maintainer/workspace/skynet &amp;&amp; grep -q 'data-testid="pv-header-search-button"' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx &amp;&amp; grep -q 'ConversationSearchModal' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx &amp;&amp; grep -q 'onSearchResultOpenActive' src/ui/AppShell.tsx &amp;&amp; grep -q 'onSearchResultOpenActive' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx &amp;&amp; npx tsc --noEmit -p .</automated>
  </verify>
  <acceptance_criteria>
    - `PrettyConversationsPanel.tsx` contains the new `<button data-testid="pv-header-search-button" ...>` in the `.pv-header-actions` cluster
    - `PrettyConversationsPanel.tsx` contains a `<ConversationSearchModal ... />` mount at top-level JSX
    - `PrettyConversationsPanel.tsx` props type declares `onSearchResultOpenActive?: (result: ConversationSearchResult) => void`
    - `AppShell.tsx` passes `onSearchResultOpenActive` to `<PrettyConversationsPanel ... />` and the handler references `openTab(host, "terminal", ...)` with `targetTmuxSession`, mirrors `selectConversationDeferred`, `navigateToView`, `setSidebarOpen(false)` from the sibling `onDetachedRowClick` handler at line 3068
    - `npx tsc --noEmit -p .` returns 0 errors from the modified files
    - `npx vitest run src/ui/features/pretty-conversations/ConversationSearchModal.test.tsx src/ui/state/search-store.test.ts src/backend/database/routes/conversation-search.test.ts` — all still green (regression check)
  </acceptance_criteria>
  <done>The search modal is fully wired: click the sidebar magnifying-glass button → modal opens → type + Enter → results appear → click active row → tab opens + modal closes → click archived row → alert + modal stays. The next plan removes the now-orphaned filter-as-you-type input to satisfy D-17 under the D-18 ordering constraint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser DOM → React render | Snippet text may contain arbitrary transcript content; must not be rendered as HTML |
| module-scoped store → auth lifecycle | Store outlives navigation; a hypothetical logout leaves stale query + results in memory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-FE-01 | Tampering (client XSS) | ConversationSearchRow snippet rendering | mitigate | Backend returns `snippet: string` (plain text) + `hitStart` + `hitLength`; row splits the string into three React text-child spans, wrapping the middle span with `className="pv-search-hit"`. NO `dangerouslySetInnerHTML` anywhere. React auto-escapes text children. Test asserts zero `dangerouslySetInnerHTML` in the rendered DOM (per D-11 / Open Question 4) |
| T-122-FE-02 | Information Disclosure | module-scoped search-store persists after auth changes | accept | Skynet is single-tenant per browser tab in practice. Store dies on page reload. If multi-tenancy ships later, add a subscription to the auth-changed signal calling `clearSearch()` (Pitfall 3 in RESEARCH.md). Documented here so future work knows where to add the wire-up |
| T-122-FE-03 | Denial of Service (client) | user rapid-clicks Load more, firing many concurrent fetches | mitigate | Load-more button binds `disabled` to `state.isFetching`; fetch flow starts with a `setFetching(true)` call before the network hop; button unclickable until the response arrives |
</threat_model>

<verification>
- `npx vitest run src/ui/state/search-store.test.ts src/ui/features/pretty-conversations/ConversationSearchModal.test.tsx src/backend/database/routes/conversation-search.test.ts` → all tests pass
- `npx tsc --noEmit -p .` → zero errors introduced
- Manual smoke (developer runs the frontend after this plan lands): click the magnifying-glass button → modal opens → type "claude" + Enter → results render with highlighted snippets → click an active row → tab opens + modal closes → reopen modal → last query + results still visible (D-05) → click an archived row → alert appears + modal stays open
- `grep -c "dangerouslySetInnerHTML" src/ui/features/pretty-conversations/ConversationSearchModal.tsx src/ui/features/pretty-conversations/ConversationSearchRow.tsx` → 0
</verification>

<success_criteria>
User can open the modal, type a query, press Enter, see results with highlighted snippets, click load-more to page, click an active result to jump-and-close, click an archived result to see a blunt alert. Modal state persists across opens. No keyboard shortcut. No global event listeners. Tests prove every D-XX decision this plan owns. Code is committed with green tests.
</success_criteria>

<output>
Create `.planning/phases/122-conversation-search-modal/122-03-SUMMARY.md` when done. The SUMMARY MUST call out (a) the exact field name used for tmux-session in `ConversationSearchResult` (so plan 04 doesn't guess wrong if any related edits touch this field), (b) that the existing filter-as-you-type is STILL PRESENT in `PrettyConversationsPanel.tsx` (this plan does not remove it; plan 04 does per D-18), and (c) the browser-side smoke checklist for the phase-level UAT verification.
</output>
