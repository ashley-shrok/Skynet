---
phase: 122-conversation-search-modal
plan: 04
type: execute
wave: 3
depends_on:
  - 122-03
files_modified:
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/features/pretty-conversations/pretty-conversations.css
autonomous: false
requirements:
  - D-17
  - D-18

must_haves:
  truths:
    - "The old inline filter-as-you-type input at the top of the sidebar is GONE — no `<input className='pv-search-input' />` is rendered anywhere in PrettyConversationsPanel"
    - "All entangled state that supported the old filter is removed: `searchQuery` useState, `searchContainerRef` useRef, `SEARCH_HIDDEN_SENTINEL_KEY` constant + sessionStorage read, `matchesSearch` callback, `trimmedSearchQuery` + `searchMatches` useMemo, and the `searchMatches !== null` JSX branch"
    - "CSS rules `.pv-search-container`, `.pv-search-icon`, `.pv-search-input`, `.pv-search-input::placeholder`, `.pv-search-input::-webkit-search-*`, `.pv-search-clear` are removed from pretty-conversations.css"
    - "`.pv-search-hit` and `.pv-search-archived-pill` (added in plan 03) are PRESERVED — they belong to the new modal, not the removed input"
    - "The `Search` icon import from lucide-react remains (the new header button uses it); the `X` icon import remains only if some non-removed consumer still uses it"
    - "The three-zone view (pinned / middle / rdpGroup / activeSet) renders as before because the old filter's flat-match branch is gone"
    - "`tsc --noEmit -p .` passes and existing PrettyConversationsPanel tests are updated to not reference the removed testids"
  artifacts:
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "PrettyConversationsPanel without the inline filter input + its supporting state/callbacks/memos/refs/JSX"
    - path: "src/ui/features/pretty-conversations/pretty-conversations.css"
      provides: "pretty-conversations.css without the removed `.pv-search-container` block; `.pv-search-hit` + `.pv-search-archived-pill` from plan 03 are preserved"
  key_links: []
---

<objective>
Remove the D-17 filter-as-you-type input and everything that supported it, now that the plan 03 modal is usable. Per D-18, this MUST land after plan 03 (never before) so there is no regression window where neither the modal nor the filter exists.

The filter is entangled across 8+ locations in `PrettyConversationsPanel.tsx` plus a CSS block. Pitfall 5 in RESEARCH.md enumerated every site precisely — this plan works through that list mechanically.

Purpose: Close the phase's D-17 deliverable and prevent dead-code drift (matchesSearch + searchMatches would keep running on every render with `trimmedSearchQuery === ""` if we removed only the JSX).
Output: A slimmed-down PrettyConversationsPanel that renders the three-zone view unconditionally (no `searchMatches !== null` branch), a CSS file with the old input rules removed and the new highlight rules preserved, and tests updated to reflect the new reality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-conversation-search-modal/122-CONTEXT.md
@.planning/phases/122-conversation-search-modal/122-RESEARCH.md
@.planning/phases/122-conversation-search-modal/122-03-SUMMARY.md
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
@src/ui/features/pretty-conversations/pretty-conversations.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove filter input JSX + supporting state + CSS + refresh tests</name>
  <files>
    src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx,
    src/ui/features/pretty-conversations/pretty-conversations.css
  </files>
  <read_first>
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (10 specific line regions must be inspected before edits — see the enumerated deletion list in the action; use offset+limit Read calls on each region rather than reading the whole 3.3k-line file):
      - Line 232 (SEARCH_HIDDEN_SENTINEL_KEY constant)
      - Line 67 (lucide-react imports — confirm `Search` is used by the new header button so we keep it; check `X` usage separately)
      - Line 896 (searchQuery useState)
      - Line 906-935 (searchContainerRef useRef + one-shot sessionStorage-gated cold-load scroll-hide useEffect)
      - Line 1153-1222 (matchesSearch useCallback)
      - Line 1239-1259 (trimmedSearchQuery + searchMatches useMemo)
      - Line 2393-2424 (`.pv-search-container` JSX block)
      - Line 2522-2540 (searchMatches !== null ternary branch)
      - Line 232 sessionStorage read of SEARCH_HIDDEN_SENTINEL_KEY (also inside the useEffect at 906-935)
      - Any other referrer of `searchQuery`, `matchesSearch`, `searchMatches`, `trimmedSearchQuery`, or `SEARCH_HIDDEN_SENTINEL_KEY` in the file (use `grep -n` to enumerate before starting)
    - src/ui/features/pretty-conversations/pretty-conversations.css (lines 265-306 for `.pv-search-container` / `.pv-search-icon` / `.pv-search-input` / `.pv-search-input::-webkit-search-*` / `.pv-search-input::placeholder` / `.pv-search-clear` rules — DELETE these; PRESERVE the newer `.pv-search-hit` and `.pv-search-archived-pill` rules added in plan 03)
    - PrettyConversationsPanel test files (grep the `src/ui/features/pretty-conversations/` directory for `.test.tsx` files that reference `pv-search-input`, `pv-search-clear`, or `matchesSearch` — those tests need to be updated or removed)
    - .planning/phases/122-conversation-search-modal/122-CONTEXT.md (D-17 removal requirement, D-18 ordering constraint that this plan honors)
    - .planning/phases/122-conversation-search-modal/122-RESEARCH.md (`## Common Pitfalls` Pitfall 5 for the exact 10-step deletion checklist)
  </read_first>
  <action>
    Work through Pitfall 5's 10-step deletion checklist verbatim, adapted to the exact line ranges present in the file at edit time (line numbers may have shifted slightly if plan 03 added imports; use grep to relocate each named identifier before editing). Concrete steps:

    1. Delete the `SEARCH_HIDDEN_SENTINEL_KEY` constant at ~line 232
    2. Delete the `searchQuery` useState at ~line 896: `const [searchQuery, setSearchQuery] = useState("");`
    3. Delete the `searchContainerRef` useRef at ~line 906 and the entire one-shot cold-load scroll-hide `useEffect` at ~lines 906-935 that reads and writes `SEARCH_HIDDEN_SENTINEL_KEY` in sessionStorage
    4. Delete the `matchesSearch` useCallback at ~lines 1153-1222 (roughly 60 lines including its comment header)
    5. Delete the `trimmedSearchQuery` and `searchMatches` useMemo block at ~lines 1239-1259
    6. Delete the `.pv-search-container` JSX block at ~lines 2393-2424 (the `<div className="pv-search-container">...` wrapper containing the icon, input, and clear button)
    7. Delete the `searchMatches !== null ? ... : ...` ternary branch at ~lines 2522-2540. Simplify the JSX to unconditionally render the three-zone view (whatever the else branch of the ternary was). Read the surrounding JSX carefully — the ternary opens somewhere around L2529 and the else branch is the three-zone renderer; the resulting JSX after removal is just the else-branch content (unwrap the ternary)
    8. Delete the `.pv-search-container / .pv-search-icon / .pv-search-input / .pv-search-input::-webkit-search-decoration / .pv-search-input::-webkit-search-cancel-button / .pv-search-input::-webkit-search-results-button / .pv-search-input::-webkit-search-results-decoration / .pv-search-input::placeholder / .pv-search-clear / .pv-search-clear:hover` CSS rules from `pretty-conversations.css` (starting ~line 265, spans through ~line 330). PRESERVE `.pv-search-hit` and `.pv-search-archived-pill` (added in plan 03, near the end of the file)
    9. On the lucide-react import at line 67: verify `Search` is still needed (yes — the new header button in plan 03 uses it; do NOT remove it). Verify `X` is still needed (grep the file for `<X ` after all other deletions land — if there are no other consumers, remove `X` from the import; if any other JSX uses it, keep it)
    10. Update or delete tests: run `grep -rn "pv-search-input\|pv-search-clear\|pv-search-container\|matchesSearch\|SEARCH_HIDDEN_SENTINEL_KEY" src/ui/features/pretty-conversations/`. For each hit in a test file: if the test was asserting the OLD filter behavior, replace it with an assertion that the OLD selectors are ABSENT (proves D-17 removal) and that the NEW `pv-header-search-button` opens the modal (positive assertion that the replacement is in place). If the test was purely about the old filter with no useful modal-side equivalent, delete the test with a git commit note

    After all deletions:
    - `grep -c 'pv-search-input\|matchesSearch\|SEARCH_HIDDEN_SENTINEL_KEY\|searchQuery\|searchMatches\|trimmedSearchQuery\|searchContainerRef' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` — should return 0 (or very close to 0, allowing for any comment references you deliberately keep as tombstones). If the count is not 0, hunt down the remaining references
    - `grep -c 'pv-search-container\|\.pv-search-icon\|\.pv-search-input\|\.pv-search-clear' src/ui/features/pretty-conversations/pretty-conversations.css` — should return 0
    - `grep -c 'pv-search-hit\|pv-search-archived-pill' src/ui/features/pretty-conversations/pretty-conversations.css` — should return ≥ 2 (the plan-03 rules are preserved)
    - `grep -c 'pv-header-search-button' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` — should return ≥ 1 (the plan-03 button is preserved)

    If any test file is deleted, add a brief comment (or commit message) explaining the deletion is D-17 cleanup — no other feature depended on the removed test.
  </action>
  <verify>
    <automated>cd /home/ubuntu/fleet/identities/morpheus-box-maintainer/workspace/skynet &amp;&amp; test $(grep -c 'pv-search-input\|matchesSearch\|SEARCH_HIDDEN_SENTINEL_KEY\|searchContainerRef\|trimmedSearchQuery' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx) -eq 0 &amp;&amp; test $(grep -Ec '(^|\s)\.pv-search-container|(^|\s)\.pv-search-icon|(^|\s)\.pv-search-input|(^|\s)\.pv-search-clear' src/ui/features/pretty-conversations/pretty-conversations.css) -eq 0 &amp;&amp; grep -q 'pv-search-hit' src/ui/features/pretty-conversations/pretty-conversations.css &amp;&amp; grep -q 'pv-header-search-button' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx &amp;&amp; npx tsc --noEmit -p . &amp;&amp; npx vitest run src/ui/features/pretty-conversations/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pv-search-input\|matchesSearch\|SEARCH_HIDDEN_SENTINEL_KEY\|searchContainerRef\|trimmedSearchQuery' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns 0
    - `grep -c 'searchQuery' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns 0 (or comment-only survivors — verify with `grep -v '^\s*//' | grep -c searchQuery` returns 0)
    - `grep -c 'searchMatches' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns 0
    - The `pv-search-container` CSS block (lines 265-330 area) is gone from `pretty-conversations.css`
    - `.pv-search-hit` and `.pv-search-archived-pill` (plan-03 additions) are still present in `pretty-conversations.css`
    - The `pv-header-search-button` (plan-03 addition) is still present in `PrettyConversationsPanel.tsx`
    - `npx tsc --noEmit -p .` returns 0 errors from the modified files (and does not surface unused-import warnings from the removed state)
    - `npx vitest run src/ui/features/pretty-conversations/` — all tests in the pretty-conversations directory pass, including any refreshed tests that assert the OLD selectors are absent AND the NEW modal button is present
    - The three-zone view (pinned / middle / rdpGroup / activeSet) still renders in the file — spot-check by grepping for the identifiers of those data slices in the JSX
  </acceptance_criteria>
  <done>Filter-as-you-type is gone from JSX, state, callbacks, memos, refs, sentinel constant, and CSS. Tests are green and updated to reflect the new modal-only reality. Plan-03's modal chrome and header button are untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verifies end-to-end phase behavior after filter removal</name>
  <files>
    src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx,
    src/ui/features/pretty-conversations/pretty-conversations.css,
    src/ui/features/pretty-conversations/ConversationSearchModal.tsx,
    src/backend/database/routes/conversation-search.ts,
    docker/nginx.conf
  </files>
  <read_first>
    - .planning/phases/122-conversation-search-modal/122-CONTEXT.md (D-01 through D-19 — the human is verifying every locked decision)
    - .planning/phases/122-conversation-search-modal/122-03-SUMMARY.md (frontend smoke checklist)
  </read_first>
  <what-built>The complete phase 122 delivery is now sittable in a browser: sidebar header has a magnifying-glass button, no inline filter input, and the modal + backend endpoint are wired end-to-end.</what-built>
  <how-to-verify>
    1. Rebuild + reload the frontend (`docker compose restart` or whatever the developer's local iteration cycle is). Nginx must have picked up the new `/conversation-search` location — if the location does not exist, `curl` against `http://localhost/conversation-search` should return the backend's 401 (not nginx's default index.html fallback)
    2. Open the app in a browser; find the sidebar. Confirm the inline filter-as-you-type input at the top is GONE
    3. Confirm a magnifying-glass button is present in the sidebar header alongside the existing New-conversation / Create-project / Edit-roles / Edit-global-files / More-menu buttons
    4. Click the magnifying-glass button. The modal opens
    5. Empty-state placeholder visible (D-06 first-open)
    6. Type "claude" (or any word likely to appear in transcripts) — confirm NOTHING happens until Enter is pressed (D-01)
    7. Press Enter. Spinner appears, then result rows render. Each row shows title, `identityKey · hostName`, and a snippet with a highlighted matched substring (D-11)
    8. Scroll to bottom; if `hasMore`, click Load more — new rows append (D-12)
    9. Click an ACTIVE (non-archived-pill) result — a terminal tab opens and the modal closes (D-04, D-14)
    10. Reopen the modal via the magnifying-glass button — the last query + accumulated results are STILL VISIBLE (D-05)
    11. Click an ARCHIVED (archived-pill visible) result — a browser alert appears with "coming soon" wording, and the modal STAYS OPEN (D-15)
    12. Type a different query and press Enter — the results reset to the new query's hits, the accumulated ones are cleared (this is startNewSearch semantics)
    13. Clear the input (X button or backspace to empty) — results-area shows "No results for..." or is empty; NOT the first-open placeholder (D-06)
    14. Reload the whole page — the modal state is reset (as expected; module-scoped store dies on reload)
    15. Open browser devtools; grep the DOM for `dangerouslySetInnerHTML` inside a result-row — should be zero
    16. Optional stress: type a query with shell metacharacters (`foo'; touch /tmp/OWNED-uat-check; #`) and press Enter. After the response arrives, SSH into a host or check locally: `test ! -e /tmp/OWNED-uat-check` should pass. This is the T-122-01 shell-injection defense proof
  </how-to-verify>
  <action>Pause execution and wait for the human to walk through the 16-step UAT checklist. Do NOT close the phase until the resume-signal is received.</action>
  <verify>
    <human-check>Human has completed all 16 UAT steps in a browser and reported the results back.</human-check>
  </verify>
  <acceptance_criteria>
    - Human has explicitly approved (via the resume-signal)
    - Any follow-up issues discovered during UAT are captured in the resume message so a follow-up plan can address them
  </acceptance_criteria>
  <resume-signal>Type "approved" (or "approved with follow-ups: ...") to close the phase. If any step fails, describe which one and what you observed; the executor will address before closing.</resume-signal>
  <done>Phase 122 UAT complete; human has signed off (with or without follow-ups).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new boundaries introduced by this cleanup plan. Threat model is inherited from plan 02 (backend) and plan 03 (frontend); this plan strictly removes code and cannot add new threats.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-D17-01 | Elevation of Privilege | over-broad grep-and-delete accidentally removes a still-needed identifier (e.g. deleting `Search` icon import breaks the plan-03 header button) | mitigate | The verify step includes `grep -q 'pv-header-search-button' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` AND runs the vitest suite in `src/ui/features/pretty-conversations/` — a broken import surfaces immediately in tsc/vitest before commit. Action step 9 also explicitly gates removal of the `Search` import on "yes it's still used" |
| T-122-D17-02 | Denial of Service (regression) | filter removed but modal is not usable → user has NO way to find conversations | mitigate | The wave ordering (plan 04 depends_on plan 03) is enforced by GSD's wave system; the human-verify checkpoint here proves the modal actually works before the filter's cleanup is considered done |
</threat_model>

<verification>
- `npx vitest run src/ui/features/pretty-conversations/` → all tests pass (including refreshed ones)
- `npx tsc --noEmit -p .` → zero errors
- Grep counts match the acceptance criteria
- Human confirms all 16 UAT steps pass in a browser
</verification>

<success_criteria>
Phase 122 is functionally complete: the sidebar has no inline filter, the modal is discoverable via the header button, all decisions D-01 through D-19 are implemented and verified, and code is committed with green tests. The archived-branch of the search corpus is either working (plan 01 verdict `go-same-helper` or `go-mtime-fallback`) or explicitly deferred with developer sign-off (verdict `go-sidecar-required` or `no-go` — surfaced back to the phase-level decision).
</success_criteria>

<output>
Create `.planning/phases/122-conversation-search-modal/122-04-SUMMARY.md` when done. SUMMARY should list (a) the exact file / line-count deltas from the deletion, (b) which tests were refreshed vs deleted, (c) any Search/X import decisions made in action step 9, and (d) the human's UAT verdict from Task 2.
</output>
