---
phase: 122-conversation-search-modal
plan: 02
type: execute
wave: 1
depends_on:
  - 122-01
files_modified:
  - src/backend/database/routes/conversation-search.ts
  - src/backend/database/routes/conversation-search.test.ts
  - src/backend/claude-session/session-search-snippet.ts
  - src/backend/claude-session/session-search-snippet.test.ts
  - src/backend/claude-session/list-archived-identity-keys.ts
  - src/backend/database/database.ts
  - docker/nginx.conf
  - docker/nginx-https.conf
autonomous: true
requirements:
  - D-01
  - D-02
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12
  - D-13
  - D-19

must_haves:
  truths:
    - "POST /conversation-search accepts { query, offset, limit } from a JWT-authenticated caller and returns { results, hasMore } within PER_HOST_TIMEOUT_MS worst case"
    - "For each of the caller's SSH+autoTmux-enabled hosts, both `~/fleet/identities/` and `~/fleet/identities-archive/` are enumerated and the latest transcript per identity is resolved via `discoverIdentitySessionFile` (or the fallback per plan 01 verdict)"
    - "Content search is case-insensitive plain substring (fixed-string grep -F -i) with no regex or fuzzy behavior"
    - "Each result row carries { transcriptPath, transcriptMtime, identityKey, hostId, hostName, aiTitle, snippet, hitStart, hitLength, isArchived } — enough for the frontend to render + route clicks with zero client-side joins"
    - "Results across all hosts are aggregated, sorted by transcriptMtime desc (recency-first per D-10), and sliced [offset, offset+limit)"
    - "A user query containing shell metacharacters (`;`, `$`, backticks, single-quotes, newlines) does not execute those bytes as shell syntax on any managed host"
    - "One offline / slow host cannot 500 the endpoint or stall it beyond PER_HOST_TIMEOUT_MS"
  artifacts:
    - path: "src/backend/database/routes/conversation-search.ts"
      provides: "POST /conversation-search endpoint handler + shell script template + Result type export"
      exports: ["default (Router)"]
      min_lines: 200
    - path: "src/backend/claude-session/session-search-snippet.ts"
      provides: "snippetForHit(rawJsonlLine, query) — JSON-parses hit line, extractText, windows ±80 chars around match, returns { snippet, hitStart, hitLength }"
      exports: ["snippetForHit"]
    - path: "src/backend/claude-session/list-archived-identity-keys.ts"
      provides: "listArchivedIdentityKeysOnHost(conn) — mirrors listIdentityKeysOnHost but roots at ~/fleet/identities-archive/"
      exports: ["listArchivedIdentityKeysOnHost"]
    - path: "src/backend/database/routes/conversation-search.test.ts"
      provides: "Unit coverage: per-host fan-out, empty-query short-circuit, shell-injection safety, timeout race, isArchived flag, sort order, offset/limit slicing, error-class shape on failure"
    - path: "src/backend/claude-session/session-search-snippet.test.ts"
      provides: "Unit coverage: JSON-parsed hit line → windowed snippet with correct hitStart/hitLength; degraded fallback when JSON.parse throws; ellipsis at boundaries; case-insensitive index lookup"
    - path: "src/backend/database/database.ts"
      provides: "Mount of conversationSearchRoutes at /conversation-search"
      contains: "conversation-search"
    - path: "docker/nginx.conf"
      provides: "location block routing /conversation-search to backend 30001"
      contains: "conversation-search"
    - path: "docker/nginx-https.conf"
      provides: "location block routing /conversation-search to backend 30001 (parity with nginx.conf)"
      contains: "conversation-search"
  key_links:
    - from: "src/backend/database/routes/conversation-search.ts"
      to: "src/backend/claude-session/discover-identity-session-file.ts"
      via: "per-identity latest-transcript resolution"
      pattern: "discoverIdentitySessionFile"
    - from: "src/backend/database/routes/conversation-search.ts"
      to: "src/backend/claude-session/identity-artifact-reader.ts"
      via: "live-tree identity enumeration"
      pattern: "listIdentityKeysOnHost"
    - from: "src/backend/database/routes/conversation-search.ts"
      to: "src/backend/claude-session/list-archived-identity-keys.ts"
      via: "archive-tree identity enumeration"
      pattern: "listArchivedIdentityKeysOnHost"
    - from: "src/backend/database/routes/conversation-search.ts"
      to: "src/backend/ssh/host-resolver.js + ssh-one-shot.js + tmux-helper.js"
      via: "cross-host fan-out"
      pattern: "resolveHostById.*connectOneShot.*execCommand"
    - from: "src/backend/database/routes/conversation-search.ts"
      to: "src/backend/claude-session/session-search-snippet.ts"
      via: "post-grep JSON-aware snippet extraction"
      pattern: "snippetForHit"
    - from: "src/backend/database/database.ts"
      to: "src/backend/database/routes/conversation-search.ts"
      via: "route mount"
      pattern: "conversationSearchRoutes"
---

<objective>
Implement the backend content-search endpoint plus its two supporting helpers (archive-tree identity enumerator, JSON-aware snippet extractor), wire the route into `database.ts`, and add parity nginx location blocks so the browser can reach it in both http and https modes.

Reuse the codebase's established primitives byte-for-byte: `discoverIdentitySessionFile` for per-identity transcript resolution (D-09), the `sessions.ts:319-566` `Promise.all(candidates.map(...))` shape for cross-host fan-out (D-19), `AuthManager.createAuthMiddleware()` + `resolveHostById` + `connectOneShot` + `execCommand` for the auth + SSH lifecycle. The only genuinely new logic is (a) archive-tree directory enumeration, (b) the shell-side grep script, and (c) the JSON-aware snippet windowing.

Purpose: Ship the server side of the search modal before the frontend depends on it. Wave 2 needs a working endpoint contract to build against.
Output: A tested, mounted, nginx-proxied `POST /conversation-search` endpoint with response shape `{ results: ConversationSearchResult[], hasMore: boolean }`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/122-conversation-search-modal/122-CONTEXT.md
@.planning/phases/122-conversation-search-modal/122-RESEARCH.md
@.planning/phases/122-conversation-search-modal/122-01-CHECKPOINT-RESULTS.md
@src/backend/database/routes/sessions.ts
@src/backend/database/routes/workspace-routes.ts
@src/backend/database/routes/identity-archive.ts
@src/backend/claude-session/discover-identity-session-file.ts
@src/backend/claude-session/identity-artifact-reader.ts
@src/backend/claude-session/session-file-parser.ts
@src/backend/database/database.ts
@docker/nginx.conf
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: snippetForHit helper + archive-tree identity enumerator (with tests)</name>
  <files>
    src/backend/claude-session/session-search-snippet.ts,
    src/backend/claude-session/session-search-snippet.test.ts,
    src/backend/claude-session/list-archived-identity-keys.ts
  </files>
  <read_first>
    - src/backend/claude-session/session-file-parser.ts (specifically line 150 `function extractText(content: unknown): string` — this is the load-bearing text extractor for a JSONL `message.content` field which may be a string OR an array of content blocks with `{ type: "text", text: "..." }` and other block shapes; snippetForHit calls it on the parsed hit line's `message.content`)
    - src/backend/claude-session/identity-artifact-reader.ts (line 1144 `export async function listIdentityKeysOnHost(conn)` — the shape to mirror for the archive-tree enumerator; note the LOCAL vs REMOTE branch split at the top of the function body, the `find ... 2>/dev/null || true` graceful-degrade idiom, and the identity-key normalization)
    - .planning/phases/122-conversation-search-modal/122-RESEARCH.md (`## Common Pitfalls` Pitfall 4 for the exact snippet algorithm and the fallback contract when JSON.parse fails; `## Common Pitfalls` Pitfall 6 for shell-injection discipline; `## Code Examples` section for exported types and API contract)
    - .planning/phases/122-conversation-search-modal/122-01-CHECKPOINT-RESULTS.md (verdict + wave-1-implications — governs whether archive enumeration uses same helper or must fall back)
  </read_first>
  <behavior>
    snippetForHit tests:
    - Given a JSONL line whose `message.content` is a string containing the query at char index 200, returns `snippet` windowed ±80 chars (so length ≤ 161) with `…` prefix and `hitStart` pointing to the query inside the snippet string
    - Given `message.content` as an array of blocks including `{ type: "text", text: "..." }`, `extractText` concatenation is what gets windowed
    - Case-insensitive: query `"HELLO"` finds `hello` in text and reports `hitLength = 5`
    - When query does not appear in the extracted text (rare — grep matched JSON syntax not the extracted human text), returns first 160 chars of extracted text with `hitStart = -1`, `hitLength = 0`
    - When JSON.parse throws (malformed line), returns first 160 chars of the raw line with `hitStart = -1`, `hitLength = 0` — degraded but non-crashing
    - Ellipsis: only prefixed if `start > 0`, only suffixed if `end < rawText.length`

    listArchivedIdentityKeysOnHost tests are NOT required in this task (the helper is thin and its runtime behavior is covered by the endpoint integration test in Task 2). Manual smoke test in Task 2's verify step is sufficient.
  </behavior>
  <action>
    Create `src/backend/claude-session/session-search-snippet.ts` exporting `snippetForHit(rawLine: string, query: string): { snippet: string; hitStart: number; hitLength: number }`. Implementation contract per Pitfall 4:
    1. Try `JSON.parse(rawLine)`. On throw → return `{ snippet: rawLine.slice(0, 160), hitStart: -1, hitLength: 0 }`
    2. Read `obj?.message?.content` and pass to `extractText` imported from `./session-file-parser.js` (function at line 150 of that file)
    3. Lowercase both `rawText` and `query`, run `indexOf` for the hit position; if `idx === -1` return first 160 chars with `hitStart: -1, hitLength: 0`
    4. Compute `start = Math.max(0, idx - 80)`, `end = Math.min(rawText.length, idx + query.length + 80)`
    5. `snippet = (start > 0 ? "…" : "") + rawText.slice(start, end) + (end < rawText.length ? "…" : "")`
    6. `hitStart = idx - start + (start > 0 ? 1 : 0)` (accounting for the ellipsis char shifting indices in the snippet string); `hitLength = query.length`

    Create `src/backend/claude-session/session-search-snippet.test.ts` using vitest + `import { describe, it, expect } from "vitest"` covering every behavior bullet above. Use inline JSONL string fixtures — do not read real files.

    Create `src/backend/claude-session/list-archived-identity-keys.ts` exporting `listArchivedIdentityKeysOnHost(conn: Client | null): Promise<string[]>`. Signature and structure mirror `listIdentityKeysOnHost` at `identity-artifact-reader.ts:1144` but the shell command roots at `~/fleet/identities-archive/` instead of `~/fleet/identities/`. Include the same `find ... -maxdepth 1 -mindepth 1 -type d 2>/dev/null || true` graceful-degrade pattern so a host without an archive dir returns `[]` cleanly. Match the LOCAL vs REMOTE branch split of the reference function (LOCAL: `conn === null`, walk via node fs). Do NOT reimplement the identity-key parsing — read directory names, strip trailing slashes, filter for non-empty strings, return the array.

    If the plan 01 verdict is `go-mtime-fallback`, ALSO export a helper `resolveArchivedIdentityLatestJsonl(conn, identityKey): Promise<string | null>` that does a raw mtime-desc walk of `~/.claude/projects/**/*.jsonl` and returns the newest — the endpoint route in Task 2 will call it for archived identities instead of `discoverIdentitySessionFile`. If the verdict is `go-same-helper`, skip that helper and the endpoint uses `discoverIdentitySessionFile` for both branches. If the verdict is `go-sidecar-required` or `no-go`, STOP and escalate to the developer per the phase-level checkpoint — do not proceed with Task 2 until direction is provided.
  </action>
  <verify>
    <automated>cd /home/ubuntu/fleet/identities/morpheus-box-maintainer/workspace/skynet &amp;&amp; npx vitest run src/backend/claude-session/session-search-snippet.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/backend/claude-session/session-search-snippet.ts` exports `snippetForHit` with the exact signature above
    - `snippetForHit.test.ts` covers all six behavior bullets and passes
    - `src/backend/claude-session/list-archived-identity-keys.ts` exports `listArchivedIdentityKeysOnHost(conn)` with LOCAL + REMOTE branches and returns `[]` cleanly when the archive dir is missing on the host
    - `tsc --noEmit` passes for the new files (`cd /home/ubuntu/fleet/identities/morpheus-box-maintainer/workspace/skynet &amp;&amp; npx tsc --noEmit -p .` returns 0 errors from the new files)
    - No `dangerouslySetInnerHTML` or shell interpolation anywhere in the new code (grep the new files: zero matches for `dangerously`, zero for backtick-wrapped user-input shell composition)
  </acceptance_criteria>
  <done>Two new helper files plus one test file exist, tests are green, tsc is clean. The endpoint in Task 2 can import both helpers by their exported names.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire POST /conversation-search endpoint + test coverage + nginx parity</name>
  <files>
    src/backend/database/routes/conversation-search.ts,
    src/backend/database/routes/conversation-search.test.ts,
    src/backend/database/database.ts,
    docker/nginx.conf,
    docker/nginx-https.conf
  </files>
  <read_first>
    - src/backend/database/routes/sessions.ts (specifically lines 294-566 — the cross-host fan-out handler shape; copy the host-filter block at 298-317 that filters by `h.enableSsh && cfg.autoTmux !== false`; copy the per-host `try { resolveHostById → connectOneShot → work → conn.end } catch { log + return [] }` skeleton at 305-330)
    - src/backend/database/routes/workspace-routes.ts (lines 75 auth middleware application, 475 `canAccessHost` optional gate; body validation and error response pattern; use as secondary reference)
    - src/backend/database/routes/identity-archive.ts (line 76+ — narrow single-purpose route shape; mirror this size/structure not the full sessions.ts complexity)
    - src/backend/claude-session/discover-identity-session-file.ts (exported at line 330; call as `await discoverIdentitySessionFile(conn, identityKey)`; contract returns absolute-path string or null; do NOT modify this file)
    - src/backend/claude-session/identity-artifact-reader.ts (line 1144 `listIdentityKeysOnHost(conn)` — call for the live-tree enumeration)
    - src/backend/claude-session/list-archived-identity-keys.ts (created in Task 1 — call for the archive-tree enumeration)
    - src/backend/claude-session/session-search-snippet.ts (created in Task 1 — call as `snippetForHit(rawLine, query)`)
    - src/backend/ssh/tmux-helper.js (`execCommand(conn, cmd)` — wraps SSH exec channel; use for the per-host grep script invocation)
    - src/backend/database/database.ts (lines 8-95 import cluster for imports, line ~1919+ `app.use("/...", routes)` cluster for the mount; place the new `app.use("/conversation-search", conversationSearchRoutes)` alongside other identity- and workspace-scoped mounts around line 1970-1980)
    - docker/nginx.conf (lines 515-534 `location ~ ^/workspace(/.*)?$` block as the template — same `proxy_pass http://127.0.0.1:30001`, `proxy_http_version 1.1`, X-Forwarded-* headers, `proxy_read_timeout 60s` (search may take up to ~PER_HOST_TIMEOUT_MS = 30s worst case; use 60s to give slack), `client_max_body_size 32k` (query body is tiny))
    - docker/nginx-https.conf (paste the same block; parity is load-bearing per RESEARCH.md `## Sources` note about CLAUDE.md nginx caveat)
    - .planning/phases/122-conversation-search-modal/122-CONTEXT.md (D-01 Enter-only, D-02 case-insensitive plain substring, D-07/D-08 corpus, D-10 recency sort, D-11 result-row fields, D-12 offset/limit pagination, D-19 cross-host pattern)
    - .planning/phases/122-conversation-search-modal/122-RESEARCH.md (`## Architecture Patterns` Pattern 1 for the fan-out verbatim shape; `## Code Examples` section for the endpoint skeleton and per-host grep script; `## Security Domain` for auth + input validation + shell-injection controls; `## Common Pitfalls` for 6 concrete failure modes to test against)
    - .planning/phases/122-conversation-search-modal/122-01-CHECKPOINT-RESULTS.md (governs whether archived-branch uses `discoverIdentitySessionFile` or the fallback from Task 1)
  </read_first>
  <behavior>
    Route contract tests (mocked SSH layer):
    - POST /conversation-search with empty JWT → 401 (from AuthManager middleware)
    - POST /conversation-search with valid JWT and `{ query: "" }` → 200 `{ results: [], hasMore: false }` (empty-query short-circuit — no fan-out, no grep)
    - POST /conversation-search with valid JWT, `{ query: "foo" }`, mocked host list of 2 hosts each returning 3 identity keys each with a mocked JSONL → results array contains up to 6 rows aggregated across hosts
    - Results sorted by `transcriptMtime` desc (recency-first per D-10)
    - `offset: 20, limit: 20` returns rows 21-40 by slicing the sorted aggregation
    - When one host's mocked `execCommand` throws, the other host's rows still return (per-host try/catch/return-empty pattern from sessions.ts:319-566)
    - When one host's mocked `execCommand` never resolves within `PER_HOST_TIMEOUT_MS`, `Promise.race` fires the timeout branch and that host contributes `[]`; endpoint still returns 200
    - `isArchived: true` set on rows whose identity came from the archive enumeration; `false` on rows from the live enumeration
    - Shell-injection safety: query = `"foo'; touch /tmp/OWNED-{unique-hex}; #"` runs the endpoint cleanly; test asserts `/tmp/OWNED-{unique-hex}` does NOT exist after the call (skip this specific assertion on CI where /tmp may be sandboxed; keep the assertion for local dev — gate it behind `process.env.RUN_SHELL_INJECTION_ASSERTION === "1"` if needed)
    - Query length >500 chars → 400 with `{ error: "query_too_long" }` (bounds argv size per Security Domain)
    - Response shape: every result row has all 10 fields (`transcriptPath`, `transcriptMtime`, `identityKey`, `hostId`, `hostName`, `aiTitle`, `snippet`, `hitStart`, `hitLength`, `isArchived`); `hasMore: boolean` at the top level
  </behavior>
  <action>
    Create `src/backend/database/routes/conversation-search.ts`. Structure:

    1. Imports (mirror `sessions.ts:1-40` shape): `express`, `AuthManager`, `sshLogger`, `db`, `hosts`, `SimpleDBOps`, `resolveHostById`, `connectOneShot`, `execCommand`, `discoverIdentitySessionFile`, `listIdentityKeysOnHost`, `listArchivedIdentityKeysOnHost`, `snippetForHit`, `eq` from drizzle-orm.
    2. File-local constants: `CONNECT_TIMEOUT_MS = 5_000`, `PER_HOST_TIMEOUT_MS = 30_000`, `MAX_QUERY_LEN = 500`, `MAX_HITS_PER_FILE = 3`, `SNIPPET_HALF_WINDOW = 80` (already applied inside snippetForHit; kept here as doc).
    3. `const router = express.Router()` + `const authenticateJWT = AuthManager.getInstance().createAuthMiddleware()`.
    4. `router.post("/", express.json({ limit: "16kb" }), authenticateJWT, async (req, res) => { ... })`. Body prelude:
       - Extract `userId` from `(req as AuthenticatedRequest).userId`
       - Parse body: `query = typeof body?.query === "string" ? body.query.trim() : ""`; `offset = integer ≥ 0 else 0`; `limit = integer in [1, 100] else 20`
       - If `query.length === 0` → `return res.json({ results: [], hasMore: false })`
       - If `query.length > MAX_QUERY_LEN` → `return res.status(400).json({ error: "query_too_long" })`
    5. Host projection: mirror `sessions.ts:298-317` — `SimpleDBOps.select(db.select().from(hosts).where(eq(hosts.userId, userId)), "ssh_data", userId)`; filter to `enableSsh && (terminalConfig.autoTmux !== false)`. Call the filtered set `candidates`.
    6. Cross-host fan-out: `const perHost = await Promise.all(candidates.map(async (h) => { ... }))`. Inside each map:
       - `try { const resolved = await resolveHostById(h.id, userId); if (!resolved) return []; const conn = await connectOneShot(resolved, CONNECT_TIMEOUT_MS); try { return await runOneHost(conn, h.id, resolved.hostname, query); } finally { try { conn.end(); } catch {} } } catch (e) { sshLogger.debug("conversation-search: host skipped", { hostId: h.id, error: e instanceof Error ? e.message : "unknown" }); return []; }`
       - Wrap `runOneHost` in `Promise.race([runOneHost(...), new Promise((_, rej) => setTimeout(() => rej(new Error("per_host_timeout")), PER_HOST_TIMEOUT_MS))])` at the call site
    7. `runOneHost(conn, hostId, hostName, query)` internal function:
       - `const liveKeys = await listIdentityKeysOnHost(conn)`
       - `const archivedKeys = await listArchivedIdentityKeysOnHost(conn)`
       - `const liveResolved = await Promise.all(liveKeys.map(async (k) => ({ key: k, path: await discoverIdentitySessionFile(conn, k), isArchived: false })))`
       - `const archivedResolved = await Promise.all(archivedKeys.map(async (k) => ({ key: k, path: await [discoverIdentitySessionFile OR resolveArchivedIdentityLatestJsonl per plan-01 verdict](conn, k), isArchived: true })))`
       - Merge, filter out `path === null`
       - Build a single shell script (see below) that reads the resolved paths + the query, greps each file with `grep -F -i -n --max-count=3`, emits `<mtime>\t<path>\t<lineno>\t<raw-line>\n` per hit
       - `await execCommand(conn, shellScript)`; parse the stdout line-by-line; for each hit call `snippetForHit(rawLine, query)`; construct result row `{ transcriptPath: path, transcriptMtime: Number(mtime) * 1000, identityKey: key, hostId, hostName, aiTitle: null, snippet, hitStart, hitLength, isArchived }`
       - `aiTitle: null` for now — plan comment out: "aiTitle piggyback deferred to inline follow-up (Open Question 3)"; frontend uses `identityKey` as row header when `aiTitle` is null
       - Return rows
    8. Shell script (constructed in JS, executed via `execCommand`): use `sh -c '<script>' -- "$QUERY"` positional-argument form so the query never touches the shell parser. Concrete argv construction: `["sh", "-c", scriptBody, "--", query, ...resolvedPaths]`. Inside `scriptBody`: `QUERY="$1"; shift; for path in "$@"; do mtime=$(stat -c "%Y" "$path" 2>/dev/null || echo 0); grep -F -i -n --max-count=3 -- "$QUERY" "$path" 2>/dev/null | while IFS=: read -r lineno content; do printf "%s\t%s\t%s\t%s\n" "$mtime" "$path" "$lineno" "$content"; done; done`. Verify `execCommand` supports argv arrays; if it only accepts a string, use `shellSingleQuote(query)` from `discover-identity-session-file.ts:247` and single-quote-wrap the query into the command string.
    9. Aggregate: `const flat = perHost.flat(); flat.sort((a, b) => b.transcriptMtime - a.transcriptMtime); const page = flat.slice(offset, offset + limit); return res.json({ results: page, hasMore: flat.length > offset + limit });`
    10. Export `default router`.

    Create `src/backend/database/routes/conversation-search.test.ts` covering all bullets in the `<behavior>` block. Mock `resolveHostById`, `connectOneShot`, `execCommand`, `listIdentityKeysOnHost`, `listArchivedIdentityKeysOnHost`, `discoverIdentitySessionFile` at the module level via vitest's `vi.mock()`. Use `supertest` (already in devDeps per `workspace-routes.test.ts` pattern) to hit the mounted app.

    Edit `src/backend/database/database.ts`:
    - Add `import conversationSearchRoutes from "./routes/conversation-search.js";` alongside the other route imports (~line 40-100)
    - Add `app.use("/conversation-search", conversationSearchRoutes);` alongside the other `app.use` mounts (~line 1970-1980, near identity-archive since it's the closest semantic sibling)

    Edit `docker/nginx.conf` AND `docker/nginx-https.conf` (parity required — CLAUDE.md nginx caveat):
    - Add a `location ~ ^/conversation-search(/.*)?$ { ... }` block using the exact template of the `/workspace` block at line 525
    - Set `proxy_read_timeout 60s` (per-host timeout is 30s worst case; add slack)
    - Set `client_max_body_size 32k` (query body is tiny)
    - Add a leading comment: `# Phase 122: /conversation-search — POST-only fleet-fanout content search. Backing router is app.use("/conversation-search", conversationSearchRoutes) in database.ts. Parity between nginx.conf and nginx-https.conf is load-bearing per CLAUDE.md nginx caveat.`
  </action>
  <verify>
    <automated>cd /home/ubuntu/fleet/identities/morpheus-box-maintainer/workspace/skynet &amp;&amp; npx vitest run src/backend/database/routes/conversation-search.test.ts src/backend/claude-session/session-search-snippet.test.ts &amp;&amp; grep -q 'conversation-search' src/backend/database/database.ts &amp;&amp; grep -q 'conversation-search' docker/nginx.conf &amp;&amp; grep -q 'conversation-search' docker/nginx-https.conf</automated>
  </verify>
  <acceptance_criteria>
    - `src/backend/database/routes/conversation-search.ts` exports a default `Router` with a single `POST /` handler applying `express.json({ limit: "16kb" })` then `authenticateJWT`
    - Endpoint handler references `resolveHostById`, `connectOneShot`, `execCommand`, `discoverIdentitySessionFile`, `listIdentityKeysOnHost`, `listArchivedIdentityKeysOnHost`, `snippetForHit` (grep these identifiers in the file — all present)
    - Every result row includes the 10 fields listed in `<must_haves.truths>` — enforced by TypeScript export `ConversationSearchResult` type also declared in this file (matches the frontend contract in plan 03)
    - `src/backend/database/routes/conversation-search.test.ts` covers: empty query short-circuit, auth failure, aggregation across 2 mocked hosts, recency sort, offset/limit slice, per-host error → other host still returns rows, per-host timeout → 200 with empty from that host, query length cap → 400, shell-injection-safe query does not create the tempfile
    - All new tests pass (`npx vitest run src/backend/database/routes/conversation-search.test.ts src/backend/claude-session/session-search-snippet.test.ts` exits 0)
    - `src/backend/database/database.ts` imports `conversationSearchRoutes` and mounts it at `/conversation-search`
    - `docker/nginx.conf` contains a `location ~ ^/conversation-search(/.*)?$` block with `proxy_pass http://127.0.0.1:30001`, `proxy_read_timeout 60s`, `client_max_body_size 32k`
    - `docker/nginx-https.conf` contains the exact same block (parity)
    - `tsc --noEmit -p .` produces zero errors from the new/modified files
    - `grep -v '^#' src/backend/database/routes/conversation-search.ts | grep -c 'discoverIdentitySessionFile'` returns >= 1 (proves the load-bearing helper is called — accounts for comments per header hygiene rule)
  </acceptance_criteria>
  <done>The endpoint accepts a JWT-authenticated POST, fans out across the caller's SSH+autoTmux hosts, walks both live + archive identity trees, resolves each identity's latest JSONL via `discoverIdentitySessionFile` (or the plan-01 fallback), runs a shell-injection-safe grep, aggregates + sorts + paginates, and returns 10-field result rows. Tests prove the contract. nginx routes it in both http and https.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → backend | Untrusted `{ query, offset, limit }` body enters the route handler |
| backend → managed host SSH | Query string is composed into a shell command executed on each host |
| managed host filesystem → snippet extraction | Raw JSONL bytes parsed as JSON — may be malformed or contain non-text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-01 | Tampering | shell command constructed with user query on each host | mitigate | Pass query via `sh -c '<script>' -- "$QUERY"` positional-arg form so query never enters shell parser; use `grep -F` (fixed-string, no regex) per D-02; assert with an explicit test that `query = "foo'; touch /tmp/OWNED-{hex}; #"` produces zero side effects |
| T-122-02 | Information Disclosure | cross-tenant result leakage across hosts | mitigate | Host projection scoped by `eq(hosts.userId, userId)` per `sessions.ts:298-302` — user only sees results from their own hosts; no additional `canAccessHost` needed since userId filter is authoritative |
| T-122-03 | Denial of Service | slow / unreachable host stalls the whole endpoint | mitigate | Per-host `Promise.race([work, timeout(PER_HOST_TIMEOUT_MS)])`; per-host `try/catch → return []` so one host's failure yields empty slice not global 500; `MAX_HITS_PER_FILE=3` bounds per-host result-set memory; `MAX_QUERY_LEN=500` bounds argv size |
| T-122-04 | Denial of Service | pathological query with massive fan-out grep | mitigate | Query length cap 500 chars; `--max-count=3` per file caps output; per-host 30s timeout puts an absolute ceiling |
| T-122-05 | Tampering | snippet extraction on malformed JSONL line | accept | `JSON.parse` throw falls back to raw-line slice — degraded but non-crashing; malformed lines are not a security issue since they're already on the host under the user's control |
| T-122-06 | Tampering (client) | snippet rendered as HTML enables reflected XSS | mitigate | Backend returns `{ snippet: string, hitStart, hitLength }` — plain string, no HTML markup; frontend (plan 03) renders via React text children + `<span>` split around hit indices, NEVER `dangerouslySetInnerHTML`. This threat model row is a note-to-plan-03 |
| T-122-SC | Tampering | npm installs | mitigate | Zero new packages this phase — Package Legitimacy Gate not triggered per RESEARCH.md `## Package Legitimacy Audit` |
</threat_model>

<verification>
- `npx vitest run src/backend/database/routes/conversation-search.test.ts src/backend/claude-session/session-search-snippet.test.ts` → all tests pass
- `npx tsc --noEmit -p .` → zero errors introduced by the new/edited files
- `grep -q "conversation-search" src/backend/database/database.ts` → mount present
- `grep -q "conversation-search" docker/nginx.conf` AND `grep -q "conversation-search" docker/nginx-https.conf` → parity confirmed
- Manual smoke (optional inside the sandbox, mandatory in UAT after phase ships): `curl -X POST http://localhost:30001/conversation-search -H "Content-Type: application/json" -H "Cookie: <valid-jwt-cookie>" -d '{"query":"claude","offset":0,"limit":5}'` returns a 200 with the 10-field result shape
</verification>

<success_criteria>
Wave 2's frontend plan can `authApi.post("/conversation-search", { query, offset, limit })` and receive the exact `{ results: ConversationSearchResult[], hasMore: boolean }` shape with all 10 fields per row. Shell-injection test proves the query is safe to interpolate. One offline host does not 500 the endpoint. Recency sort is provably correct. Code is committed with green tests.
</success_criteria>

<output>
Create `.planning/phases/122-conversation-search-modal/122-02-SUMMARY.md` when done. SUMMARY MUST include the exported TypeScript type shape for `ConversationSearchResult` verbatim so plan 03's executor can import + mirror it on the client side without needing to re-read the backend file.
</output>
