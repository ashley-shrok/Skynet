---
phase: 122-conversation-search-modal
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/122-conversation-search-modal/122-01-CHECKPOINT-RESULTS.md
autonomous: false
requirements:
  - D-08
  - D-09
user_setup: []

must_haves:
  truths:
    - "Empirical evidence recorded for whether `discoverIdentitySessionFile(conn, identityKey)` returns a non-null path for at least 3 known-archived identities on t1000"
    - "The returned JSONL (if any) is visually confirmed to belong to the archived identity (first user line references `/id <identityKey>`)"
    - "Investigation results dictate whether Wave 1 uses the same helper for archived identities or must introduce a sidecar / mtime fallback path"
  artifacts:
    - path: ".planning/phases/122-conversation-search-modal/122-01-CHECKPOINT-RESULTS.md"
      provides: "Empirical A1 verification results plus GO / NO-GO signal for Wave 1"
      contains: "verdict:"
  key_links:
    - from: ".planning/phases/122-conversation-search-modal/122-01-CHECKPOINT-RESULTS.md"
      to: "src/backend/claude-session/discover-identity-session-file.ts"
      via: "helper invocation from ad-hoc REPL/script"
      pattern: "discoverIdentitySessionFile"
---

<objective>
Empirically verify Assumption A1 from RESEARCH.md (`## Assumptions Log`, row A1) before any endpoint code is written: for at least 3 known-archived identities on host t1000, does `discoverIdentitySessionFile(conn, identityKey)` still return a resolvable transcript path?

The whole archived-branch of D-08 (walk `~/fleet/identities-archive/` and search each archived identity's latest transcript) hinges on this. If the helper returns null for archived identities, Wave 1 must route archived-corpus discovery differently (sidecar file recorded at retire time, or mtime-only fallback per RESEARCH.md `## Common Pitfalls` Pitfall 1 options (a) / (b)).

Purpose: Prevent building the phase on top of a false assumption; produce a written verdict that Wave 1 executor reads before starting.
Output: `.planning/phases/122-conversation-search-modal/122-01-CHECKPOINT-RESULTS.md` — a small file recording (a) identity keys tested, (b) helper return values, (c) sample JSONL first-line content for non-null returns, (d) explicit `verdict: go-same-helper | go-mtime-fallback | go-sidecar-required | no-go` line, and (e) any Wave 1 implication notes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/122-conversation-search-modal/122-CONTEXT.md
@.planning/phases/122-conversation-search-modal/122-RESEARCH.md
@src/backend/claude-session/discover-identity-session-file.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Identify 3 known-archived identities on t1000 and probe discoverIdentitySessionFile</name>
  <files>.planning/phases/122-conversation-search-modal/122-01-CHECKPOINT-RESULTS.md</files>
  <read_first>
    - src/backend/claude-session/discover-identity-session-file.ts (need to see LOCAL branch entry at line 335 `discoverIdentitySessionFileLocal(identityName)` and the exported signature at line 330 `discoverIdentitySessionFile(conn, identityName)`; the caller passes `conn = null` to run the LOCAL branch inside the container against `HOME_HOST_DIR`)
    - src/backend/claude-session/identity-artifact-reader.ts (line 1144 `listIdentityKeysOnHost(conn)` — used as reference for how the live tree is enumerated; the archive tree probe mirrors this but roots at `~/fleet/identities-archive`)
    - .planning/phases/122-conversation-search-modal/122-RESEARCH.md (`## Common Pitfalls` Pitfall 1 for exact investigation protocol; `## Open Questions` #1 for the question phrasing; `## Assumptions Log` row A1 for what "wrong" looks like)
    - .planning/phases/122-conversation-search-modal/122-CONTEXT.md (D-08 for the archive-directory requirement and D-09 for the "reuse helper" constraint)
  </read_first>
  <action>
    Enumerate `~/fleet/identities-archive/` on t1000 (this box) via `ls -1 ~/fleet/identities-archive/ | head -20` to get archived identity folder names. Pick 3 distinct names for the probe; prefer names that look like they had real conversation activity (skip obvious throwaways if the folder is nearly empty).

    Then, for each chosen `identityKey`, invoke `discoverIdentitySessionFile(null, identityKey)` (the LOCAL branch — `conn=null` runs `discoverIdentitySessionFileLocal(identityName)` per line 335 of the helper). Do this via a short one-shot `node --experimental-vm-modules` REPL/script that imports the compiled JS from `dist/` if available, or a temporary `.mjs` script placed under `/tmp/` that imports from source via `tsx`. Do NOT commit the throwaway script; the RESULTS.md file is the deliverable.

    For each identity, record in the RESULTS.md file:
    - `identityKey` value probed
    - `discoverIdentitySessionFile` return value (either the absolute path or the literal string `null`)
    - If non-null: `head -1 <returned-path>` output showing the first line of the transcript, plus a note confirming (or refuting) that the first user-role line references `/id <identityKey>` — the helper's matching rule per RESEARCH.md `## Pattern 2` "Semantics of 'latest'" paragraph

    Then compute a verdict line:
    - `verdict: go-same-helper` — if >=2 of 3 return a non-null path AND the first user line references the identity's `/id`. Wave 1 uses `discoverIdentitySessionFile` for both live + archived branches unchanged.
    - `verdict: go-mtime-fallback` — if the helper returns null for archived identities but a raw `~/.claude/projects/**/*.jsonl` walk sorted by mtime desc finds a plausible candidate (record one such candidate as proof). Wave 1 uses the helper for live, and a new "latest-jsonl-by-mtime for the identity's project slug" walker for archived — surfaced as a Wave 1 sub-task.
    - `verdict: go-sidecar-required` — if neither the helper nor a mtime walk finds anything for archived. Wave 1 must record the transcript path in a sidecar file at retire time. This deferred-work signal is a phase-scope escalation to the developer.
    - `verdict: no-go` — if the probe cannot be run at all (e.g. no archived identities exist on t1000). Escalate to the developer to run the probe on a host that has archives.

    Include a `wave-1-implications:` section beneath the verdict — free-form 2-6 bullet notes on any implementation drift the Wave 1 endpoint should account for based on what was actually observed.

    Do not include a fenced code block containing the JSONL first-line content itself — the transcript content may contain sensitive user context. Instead record a redacted description: `first-user-line references /id <key>: yes | no | partial-match` plus optional non-content structural fields like `type: user`, `parentUuid: <short-hash>`.
  </action>
  <verify>
    <automated>test -f .planning/phases/122-conversation-search-modal/122-01-CHECKPOINT-RESULTS.md &amp;&amp; grep -E '^verdict:\s*(go-same-helper|go-mtime-fallback|go-sidecar-required|no-go)\b' .planning/phases/122-conversation-search-modal/122-01-CHECKPOINT-RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/122-conversation-search-modal/122-01-CHECKPOINT-RESULTS.md` exists
    - File contains at least 3 identity key entries with their probe results
    - File contains exactly one `verdict:` line matching one of the four allowed verdicts
    - File contains a `wave-1-implications:` section
    - No throwaway probe script is committed (probe script, if any, lives under `/tmp/` and is not in the git working tree)
  </acceptance_criteria>
  <done>The Wave 1 executor can open `122-01-CHECKPOINT-RESULTS.md` and know without ambiguity whether to use `discoverIdentitySessionFile` unchanged for archived identities, use a mtime fallback, escalate for sidecar work, or block on developer input.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human confirms verdict + acknowledges Wave 1 implications</name>
  <files>.planning/phases/122-conversation-search-modal/122-01-CHECKPOINT-RESULTS.md</files>
  <read_first>
    - .planning/phases/122-conversation-search-modal/122-01-CHECKPOINT-RESULTS.md (the artifact produced by Task 1)
  </read_first>
  <what-built>The empirical A1 checkpoint results file with a verdict on whether `discoverIdentitySessionFile` works for archived identities.</what-built>
  <how-to-verify>
    1. Open `.planning/phases/122-conversation-search-modal/122-01-CHECKPOINT-RESULTS.md`
    2. Read the 3 identity probe entries — do they look plausibly representative?
    3. Read the `verdict:` line — is it justified by what the probes showed?
    4. Read the `wave-1-implications:` bullets — do they capture what Wave 1 needs to know?
    5. If `verdict: no-go` or `verdict: go-sidecar-required`, decide whether to proceed with an alternate approach (defer the archived branch, ship live-only first) or pause the phase entirely.
  </how-to-verify>
  <action>Pause execution and wait for the human to review Task 1's output at `.planning/phases/122-conversation-search-modal/122-01-CHECKPOINT-RESULTS.md`. Do NOT proceed to plan 02 until the resume-signal is received.</action>
  <verify>
    <human-check>Human has read the RESULTS.md file, understands the verdict, and typed the resume-signal string.</human-check>
  </verify>
  <acceptance_criteria>
    - Human has explicitly acknowledged the verdict (via the resume-signal)
    - If verdict is `go-mtime-fallback` / `go-sidecar-required` / `no-go`, the human's resume message includes direction for plan 02 (or an escalation to defer the archived branch)
  </acceptance_criteria>
  <resume-signal>Type "approved" (or "approved, note that ...") to unblock Wave 1. If the verdict is not `go-same-helper` and the developer wants an approach adjustment before Wave 1, provide direction in the resume message.</resume-signal>
  <done>Human has reviewed the checkpoint results and given a go-signal (with or without adjustments) for plan 02.</done>
</task>

</tasks>

<verification>
The RESULTS.md file exists, has a machine-parseable `verdict:` line, and the human has approved it as reflecting reality on t1000.
</verification>

<success_criteria>
Wave 1's executor can read `122-01-CHECKPOINT-RESULTS.md` and know exactly which archived-corpus resolution strategy to implement (same helper vs mtime fallback vs escalate) without needing to re-run any investigation.
</success_criteria>

<output>
Create `.planning/phases/122-conversation-search-modal/122-01-SUMMARY.md` when done. The SUMMARY should note the verdict and any Wave 1 implications flagged.
</output>
