---
phase: 61-wip-indicator-shell-idle-gate
plan: 03
type: execute
wave: 3
depends_on:
  - 61-01
  - 61-02
files_modified:
  - src/ui/state/session-working-store.ts
  - src/ui/state/session-working-store.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "The `main` predicate at session-working-store.ts:206 is REVISED from `busy || shell` to `busy || (shell && (lastStopAt === null || lastStatusChangeAt > lastStopAt))`. No-Stop-yet (`lastStopAt === null` OR omitted) defaults `shell` to on (rollout safety per CONTEXT.md D-05)."
    - "`WorkingRecord` gains two new cached fields: `lastStopAt: number | null` and `lastStatusChangeAt: number | null`. Both are preserved across every Axis (A, B, C, D, E) republish per the Pitfall-3 invariant."
    - "Two new Axes (F for lastStopAt, G for lastStatusChangeAt) added to `publishFleetStatusSessionState` mirroring the direct-swap-and-notify pattern of Axis D (dormant) / Axis E (recycling). Each fires ONLY when the wire field is not `undefined` AND the value differs from cache."
    - "Wire semantic: `lastStopAt: <number>` sets value; `lastStopAt: null` sets null; `lastStopAt` absent (undefined) preserves cached value — matches the optional-nullable convention established for Axis D/E."
    - "Existing Test B (`shell + bg:[] → isWorking true`, locked by inline-260823-wip-shell-is-work) is EXPLICITLY REVISED. New expectation: `shell + bg:[] + no-Stop-yet → isWorking true (default-on rollout safety)`. Header comment block updated to cite Phase 61 supersession."
    - "Consumers of `useSessionIsWorking` (WipBubble in PrettyView; row dot in PrettyConversationRow via PrettyConversationsPanel) inherit the fix WITHOUT any consumer-site changes. No file under `src/ui/features/pretty-view/` or `src/ui/features/pretty-conversations/` is modified in this plan."
    - "Frontend Poppy/aqua/wilma live-behavior on the next Phase 61 deploy: their next real turn-end lands the per-session stop file for the first time → backend publishes lastStopAt for the first time → frontend predicate flips `shell` back off (correct)."
  artifacts:
    - path: "src/ui/state/session-working-store.ts"
      provides: "Revised `main` predicate with stop-gate; WorkingRecord extended with lastStopAt + lastStatusChangeAt; Axis F + G blocks; Axis A/D/E cache-preservation of new fields"
      contains: "shellCountsAsWork"
    - path: "src/ui/state/session-working-store.test.ts"
      provides: "Revised Test B + four new Phase 61 tests covering canonical stop-gate cases + cache-preservation across Axis A republishes"
      contains: "Phase 61"
  key_links:
    - from: "src/ui/state/session-working-store.ts publishFleetStatusSessionState (main predicate)"
      to: "src/ui/features/pretty-view/PrettyView.tsx WipBubble mount gate (via useSessionIsWorking)"
      via: "useSessionIsWorking hook — no consumer-site changes"
      pattern: "useSessionIsWorking"
    - from: "src/ui/state/session-working-store.ts publishFleetStatusSessionState (main predicate)"
      to: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx row dot render (via useSessionIsWorking through wrapper)"
      via: "useSessionIsWorking hook — no consumer-site changes"
      pattern: "useSessionIsWorking"
---

<objective>
Frontend consumer for the Phase 61 wire additions. Two coordinated changes to `session-working-store.ts`:

1. **`main` predicate revision** at line 206–207. Old: `busy || shell` (per inline-260823-wip-shell-is-work). New: `busy || (shell && stop-gate-fresh)` where `stop-gate-fresh` = `lastStopAt === null OR lastStatusChangeAt > lastStopAt`. Rollout safety: `lastStopAt === null` defaults `shell` to on so freshly-launched sessions and lazy-rollout sessions do not go dark.

2. **`WorkingRecord` extension** with two new cached axes (`lastStopAt: number | null`, `lastStatusChangeAt: number | null`) preserved across every Axis republish (A/B/C/D/E), plus two new Axis blocks (F for lastStopAt, G for lastStatusChangeAt) mirroring the direct-swap-and-notify pattern of Axis D/E.

The existing Test B (`shell + bg:[] → true`) — currently locked to the pre-Phase-57 behavior via the inline-260823-wip-shell-is-work note — is EXPLICITLY REVISED with a supersession comment; four new tests cover the canonical stop-gate cases + cache preservation.

Consumers (WipBubble in PrettyView; row dot in PrettyConversationRow) inherit the fix through the existing `useSessionIsWorking` hook — zero consumer-site changes.

Purpose: complete the Phase 61 vertical slice. Ready for the orchestrator's deploy motion.

Output: source diff on session-working-store.ts + test diff on session-working-store.test.ts (revised Test B + 4 new tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-wip-indicator-shell-idle-gate-use-stop-hook-to-reject-stale-/61-CONTEXT.md
@.planning/phases/61-wip-indicator-shell-idle-gate-use-stop-hook-to-reject-stale-/61-RESEARCH.md
@.planning/phases/61-wip-indicator-shell-idle-gate-use-stop-hook-to-reject-stale-/61-01-PLAN.md
@.planning/phases/61-wip-indicator-shell-idle-gate-use-stop-hook-to-reject-stale-/61-02-PLAN.md
@src/ui/state/session-working-store.ts
@src/ui/state/session-working-store.test.ts
@src/ui/api/fleet-status-types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend WorkingRecord + add Axis F/G blocks + preserve new axes across Axis A/D/E republishes + revise the main predicate to include the stop-gate</name>
  <files>src/ui/state/session-working-store.ts</files>
  <read_first>
    - src/ui/state/session-working-store.ts (the WHOLE file — specifically WorkingRecord type at lines 91-137 for the field-addition pattern; the `main` predicate at line 204-207; the Axis A block at lines 227-267 for the cache-preservation pattern; the Axis D block at lines 287-316 and Axis E block at lines 318-348 for the direct-swap-and-notify pattern the new Axis F/G blocks must mirror)
    - src/ui/api/fleet-status-types.ts (the frontend SessionState mirror updated in Plan 61-01 — confirms lastStopAt + lastStatusChangeAt are `?: number | null` optional-nullable so the predicate must handle `null`, `undefined`, AND numeric values)
    - .planning/phases/61-wip-indicator-shell-idle-gate-use-stop-hook-to-reject-stale-/61-RESEARCH.md § Code Examples "Frontend predicate" for the exact revised predicate shape AND § Common Pitfalls Pitfall 3 for the Axis A cache-preservation rule (this is the Phase 53 recurring bug; the plan MUST honor it or lastStopAt gets wiped on unrelated isWorking flips)
  </read_first>
  <behavior>
    - `WorkingRecord` type (currently ends with `recycling: boolean;` at line 136) gains two new fields at the END:
      - `lastStopAt: number | null` — mirrors the wire field; null when the wire carried null OR omitted the field.
      - `lastStatusChangeAt: number | null` — mirrors the wire field; null when the wire carried null OR omitted the field.
    - `main` predicate at line 206-207 is REPLACED. New shape:
      ```
      const lastStopAt = state_arg.lastStopAt ?? null;
      const lastStatusChangeAt = state_arg.lastStatusChangeAt ?? null;
      const shellCountsAsWork =
        state_arg.status === "shell" &&
        (lastStopAt === null ||
          (lastStatusChangeAt !== null && lastStatusChangeAt > lastStopAt));
      const main =
        state_arg.status === "busy" || shellCountsAsWork;
      ```
      Semantics: `lastStopAt === null` (no evidence of stop yet — fresh session OR lazy rollout) defaults `shell` to on; otherwise `shell` is on ONLY if `lastStatusChangeAt` is present AND strictly greater than `lastStopAt`.
    - Axis A `nextMap.set(...)` write at lines 258-264 gains `lastStopAt: existing?.lastStopAt ?? null` and `lastStatusChangeAt: existing?.lastStatusChangeAt ?? null` — MUST preserve new axes from cache, matching the Pitfall-3 invariant (the exact bug Phase 53 solved for recycling).
    - Axis A logger `console.info({...})` at lines 238-255 gains `lastStopAt: existing?.lastStopAt ?? null`, `lastStatusChangeAt: existing?.lastStatusChangeAt ?? null`, `previousLastStopAt: existing?.lastStopAt ?? null`, `previousLastStatusChangeAt: existing?.lastStatusChangeAt ?? null` for forensic tracing (matches the existing dormant/recycling logger entries).
    - Axis D `nextMap.set(...)` write at lines 306-312 gains `lastStopAt: existingAfterAxes.lastStopAt` and `lastStatusChangeAt: existingAfterAxes.lastStatusChangeAt` — preserve on dormant flip.
    - Axis E `nextMap.set(...)` write at lines 338-344 gains `lastStopAt: existingAfterAxes.lastStopAt` and `lastStatusChangeAt: existingAfterAxes.lastStatusChangeAt` — preserve on recycling flip.
    - NEW Axis F block appended after Axis E (mirrors Axis D/E direct-swap-and-notify pattern):
      - Guard: `if (state_arg.lastStopAt !== undefined) { ... }` — undefined means the wire omitted the field (pre-Phase-57 backend); do NOT change cached value.
      - Value normalize: `const lastStopAt = state_arg.lastStopAt;` (allow null through; a wire null explicitly resets the cache to null).
      - Compare + swap: `const existingAfterAxes = state.map.get(key); if (existingAfterAxes !== undefined && existingAfterAxes.lastStopAt !== lastStopAt) { ... nextMap.set(key, { ...all-existing-axes..., lastStopAt }); notify(); }`.
      - Brand-new-key case is handled by Axis A (as with D/E); Axis F only fires on change post-Axis-A.
    - NEW Axis G block appended after Axis F, mirroring Axis F exactly with `lastStatusChangeAt` swapped for `lastStopAt`.
    - Add a block-comment header above the revised `main` predicate documenting Phase 61's supersession of `inline-260823-wip-shell-is-work` (the existing 2-line comment at 204-205 becomes a Phase 61 header that CITES the prior inline note as history but establishes the new rule authoritatively). The header explains: shell now requires stop-gate freshness; default-on when `lastStopAt === null` is rollout safety per CONTEXT.md D-05.
    - Add per-axis header comments for Axis F + G in the same style as the Axis D/E comment blocks (lines 287-297 / 318-329) — cite Phase 61 Plan 03, name the wire semantic (undefined preserves / null resets / number sets), reference RESEARCH § Frontend predicate.
    - The `__resetForTest` helper (used by tests) MUST reset the new axes correctly — inspect its current implementation; if it constructs a fresh `state = { map: new Map() }`, no change needed (new fields are cache-preserved and start absent). Verify by grepping and reading.
  </behavior>
  <action>
1. **Extend WorkingRecord type** (lines 91-137): after the existing `recycling: boolean;` field, add:
   - A `// Phase 61 Plan 03 (WIP shell-idle gate — 2026-08-26): stop-gate axes.` block-comment header.
   - `lastStopAt: number | null;` — one-line doc citing "mirrors wire lastStopAt; null when wire carried null OR omitted the field; cache-preserved across all Axis republishes per Pitfall-3 invariant."
   - `lastStatusChangeAt: number | null;` — same shape doc.

2. **Revise the `main` predicate** (currently at lines 204-207). Replace the existing 4-line block with:
   - A new block-comment header (~12-15 lines) that supersedes the inline-260823-wip-shell-is-work note. Text (paraphrase, do not copy verbatim): "Phase 61 Plan 03 (WIP-shell-idle-gate 2026-08-26): shell now counts as work ONLY when the session's status has transitioned since its last Stop-hook fire — the stop-gate. lastStopAt === null (fresh session, or lazy-rollout session that pre-dates the Phase 61 hook script upgrade) defaults shell to on. The inline-260823-wip-shell-is-work rule (Ashley 2026-08-23: shell is real tool-execution work) is preserved for mid-turn shell — real-work turns dwelling in shell have their lastStatusChangeAt bumped by the backend on every status transition, so lastStatusChangeAt > lastStopAt while the turn runs. It only trips false for stale-shell (Poppy/aqua/wilma pattern) where status transitioned to shell BEFORE the last Stop fire and has not moved since. See 61-CONTEXT.md § Shape."
   - The new predicate:
     - `const lastStopAt = state_arg.lastStopAt ?? null;` (normalizes undefined → null)
     - `const lastStatusChangeAt = state_arg.lastStatusChangeAt ?? null;` (same normalization)
     - `const shellCountsAsWork = state_arg.status === "shell" && (lastStopAt === null || (lastStatusChangeAt !== null && lastStatusChangeAt > lastStopAt));`
     - `const main = state_arg.status === "busy" || shellCountsAsWork;`

3. **Extend Axis A `console.info` log** (lines 238-255): add four new keys at the END of the log object literal (after `previousRecycling`):
   - `lastStopAt: existing?.lastStopAt ?? null,`
   - `lastStatusChangeAt: existing?.lastStatusChangeAt ?? null,`
   - `previousLastStopAt: existing?.lastStopAt ?? null,`
   - `previousLastStatusChangeAt: existing?.lastStatusChangeAt ?? null,`

4. **Extend Axis A `nextMap.set` write** (lines 258-264): add two new keys at the END of the record literal (after `recycling: existing?.recycling ?? false,`):
   - `lastStopAt: existing?.lastStopAt ?? null,`
   - `lastStatusChangeAt: existing?.lastStatusChangeAt ?? null,`
   (This is the Pitfall-3 fix — MUST preserve cache on Axis A republish.)

5. **Extend Axis D `nextMap.set` write** (lines 306-312): add:
   - `lastStopAt: existingAfterAxes.lastStopAt,`
   - `lastStatusChangeAt: existingAfterAxes.lastStatusChangeAt,`

6. **Extend Axis E `nextMap.set` write** (lines 338-344): add same two lines.

7. **Append NEW Axis F block** immediately after Axis E (after line 348, before the function's closing brace at line 349). Structure (mirror Axis E exactly):
   - Block-comment header (~12 lines) citing Phase 61 Plan 03, naming the wire semantic (undefined preserves; null resets; number sets), noting that brand-new-key is handled by Axis A.
   - `if (state_arg.lastStopAt !== undefined) { const lastStopAt = state_arg.lastStopAt; const existingAfterAxes = state.map.get(key); if (existingAfterAxes !== undefined && existingAfterAxes.lastStopAt !== lastStopAt) { const nextMap = new Map(state.map); nextMap.set(key, { isWorking: existingAfterAxes.isWorking, lastMessageAt: existingAfterAxes.lastMessageAt, aiTitle: existingAfterAxes.aiTitle, dormant: existingAfterAxes.dormant, recycling: existingAfterAxes.recycling, lastStopAt, lastStatusChangeAt: existingAfterAxes.lastStatusChangeAt }); state = { map: nextMap }; notify(); } }`

8. **Append NEW Axis G block** immediately after Axis F, identical structure with `lastStatusChangeAt` swapped for `lastStopAt` on the guard + comparison + set-value.

9. Run `npx vitest run src/ui/state/session-working-store` after each incremental save. Pre-existing Tests A/C/D/E/F/G/H/I/J/K/L MUST remain green. Test B will FAIL until Task 2's revision — that is expected and desired (validation that the new predicate is wired).

10. Run `npm run build:backend` — the frontend TS is not compiled by this command, but `session-working-store.ts` is imported by other frontend modules; run `tsc --noEmit` on the whole project (via `npm run typecheck` or equivalent from `package.json` — inspect first) to catch any consumer breakage.

**MUST NOT**:
- Remove or reorder any existing Axis (A/B/C/D/E). Axes F/G are strict additions AFTER Axis E.
- Modify the existing `__resetForTest` implementation unless investigation shows it must be extended (it should be a no-op change — the Map is rebuilt fresh).
- Add any behavior that reads WorkingRecord fields other than at their existing consumer sites — do NOT introduce a new `useSessionLastStopAt` hook (not needed; the predicate consumes it internally).
- Change any file under `src/ui/features/pretty-view/` or `src/ui/features/pretty-conversations/` — consumers are invariant under this plan.
- Trust `sessionJson.updatedAt` OR any wire field OTHER than `lastStatusChangeAt` as the fresh-transition signal (Pitfall 4 — but this is a backend-side concern; on the frontend the invariant is: read only from state_arg.lastStatusChangeAt).
  </action>
  <verify>
    <automated>npx vitest run src/ui/state/session-working-store --reporter=verbose</automated>
    <automated>grep -c 'lastStopAt' src/ui/state/session-working-store.ts</automated>
    <automated>grep -c 'lastStatusChangeAt' src/ui/state/session-working-store.ts</automated>
    <automated>grep -c 'shellCountsAsWork' src/ui/state/session-working-store.ts</automated>
    <automated>grep -c 'Axis F\|Axis G' src/ui/state/session-working-store.ts</automated>
  </verify>
  <acceptance_criteria>
    - Pre-existing tests A/C/D/E/F/G/H/I/J/K/L pass (Test B will fail until Task 2 revises it — that failure is the wire-up validation).
    - `grep -v '^ *//' src/ui/state/session-working-store.ts | grep -c 'lastStopAt'` >= 8 (WorkingRecord field + predicate normalize + Axis A log 2 refs + Axis A set + Axis D set + Axis E set + Axis F guard + Axis F set — comment-excluded count ≥ 8).
    - `grep -v '^ *//' src/ui/state/session-working-store.ts | grep -c 'lastStatusChangeAt'` >= 8 (same pattern for the second axis).
    - `grep -c 'shellCountsAsWork' src/ui/state/session-working-store.ts` >= 1 (revised predicate present).
    - `grep -c 'Axis F' src/ui/state/session-working-store.ts` >= 1 (new block header present).
    - `grep -c 'Axis G' src/ui/state/session-working-store.ts` >= 1.
    - `grep -c 'inline-260823-wip-shell-is-work' src/ui/state/session-working-store.ts` >= 1 (superseded note is CITED in the new header for historical traceability — MUST NOT delete the reference).
    - Consumer surfaces untouched: `git diff --stat src/ui/features/pretty-view/ src/ui/features/pretty-conversations/` shows zero files changed for this plan.
  </acceptance_criteria>
  <done>WorkingRecord extended with two new axes; main predicate replaced with stop-gate variant; Axis A/D/E cache-preservation extended; Axis F/G swap-and-notify blocks appended; pre-existing tests still green except Test B (revised in Task 2); zero consumer-site changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Revise Test B header comment + assertion for the Phase 61 stop-gate + add four new tests covering canonical stop-gate cases and cache preservation across Axis A republishes</name>
  <files>src/ui/state/session-working-store.test.ts</files>
  <read_first>
    - src/ui/state/session-working-store.test.ts (the WHOLE file — specifically the `makeState` helper at lines 52-63 for the fixture-builder pattern; the existing Test B header + body at lines 84-107 which MUST be revised; the Test C pattern at lines 109-133 as the closest fixture shape for the new tests; the Test G no-op-notify-guard pattern at lines 217+ to understand snapshot-reference assertions for the cache-preservation test)
    - src/ui/state/session-working-store.ts (the Task-1-updated file — the exact predicate logic under test)
    - .planning/phases/61-wip-indicator-shell-idle-gate-use-stop-hook-to-reject-stale-/61-RESEARCH.md § Common Pitfalls Pitfall 8 (skipped Test B revision — the EXACT bug this task prevents) AND § Validation Architecture for the scoped test command
  </read_first>
  <behavior>
    - Test B's header block-comment (lines 84-93) is REVISED to cite Phase 61 supersession. New header text (paraphrase): "Phase 61 Plan 03 (2026-08-26) SUPERSEDES the inline-260823-wip-shell-is-work rule at the predicate boundary but preserves it as the FALLBACK for no-Stop-yet sessions. Test B now asserts the rollout-safety default-on branch: shell + no Stop-hook signal (fresh session, or lazy-rollout session that pre-dates the Phase 61 backend upgrade) → isWorking true (default-on, matches CONTEXT.md D-05). The stale-shell case (shell + lastStatusChangeAt < lastStopAt) is covered by new Test M below." Reference to inline-260823-wip-shell-is-work is preserved in the header for historical traceability.
    - Test B's assertion body (lines 96-106) is REVISED. New expectation: publishing `makeState({ status: "shell" })` WITHOUT a `lastStopAt` field on the SessionState (i.e. wire omitted / pre-Phase-57 backend) still yields `useSessionIsWorking === true` — this is the rollout-safety default-on branch. The test name changes from "shell IS real tool-execution work" to "shell + no-Stop-yet defaults to true (rollout-safety per CONTEXT.md D-05)".
    - Four NEW tests appended AFTER Test L (the source-level grep test at end of file), forming Tests M / N / O / P:
      - **Test M: shell + stale stop → isWorking FALSE** — `makeState({ status: "shell", lastStopAt: 5000, lastStatusChangeAt: 1000 })` (transitioned to shell at t=1000, then Stop fired at t=5000 — the Poppy/aqua/wilma pattern). Expect `useSessionIsWorking === false`.
      - **Test N: shell + fresh status-change (mid-turn) → isWorking TRUE** — `makeState({ status: "shell", lastStopAt: 1000, lastStatusChangeAt: 5000 })` (Stop fired at t=1000; status transitioned to shell at t=5000 — real mid-turn shell). Expect `useSessionIsWorking === true`.
      - **Test O: busy overrides the stop-gate — status='busy' always working regardless of lastStopAt/lastStatusChangeAt ordering** — `makeState({ status: "busy", lastStopAt: 5000, lastStatusChangeAt: 1000 })` (stale even by shell rules). Expect `useSessionIsWorking === true` (busy is unconditional).
      - **Test P: Axis A republish (isWorking flip) PRESERVES cached lastStopAt** — publish `makeState({ status: "shell", lastStopAt: 1000, lastStatusChangeAt: 5000 })` (isWorking=true). Then publish `makeState({ status: "idle", lastStopAt: undefined })` (Axis A fires because isWorking flips false; wire omits lastStopAt). Assert the cached `lastStopAt` is STILL 1000 by reading `getSessionWorkingSnapshot().get("h1:s1")?.lastStopAt === 1000` (Pitfall 3 regression guard). Optionally also verify the Axis A frame's cached lastStatusChangeAt is preserved at 5000.
    - Tests A/C/D/E/F/G/H/I/J/K/L remain UNMODIFIED.
  </behavior>
  <action>
1. **Revise Test B header** (lines 84-93 currently): Replace the multi-line comment block with a new block that CITES the Phase 61 supersession. Preserve the reference to inline-260823-wip-shell-is-work as historical context. Reference RESEARCH.md § Common Pitfalls Pitfall 8 as the deliberate reason for this revision.

2. **Revise Test B body** (lines 95-107 currently):
   - Change the `describe(...)` title from `"session-working-store: Test B — status:'shell' + bg:[] → isWorking true (inline-260823-wip-shell-is-work)"` to `"session-working-store: Test B — status:'shell' + no-Stop-yet → isWorking true (Phase 61 rollout-safety default-on, supersedes inline-260823-wip-shell-is-work)"`.
   - Change the `it(...)` title from `"publishFleetStatusSessionState with status:'shell' → useSessionIsWorking returns true (shell IS real tool-execution work)"` to `"shell + wire omitted lastStopAt/lastStatusChangeAt → useSessionIsWorking returns true (rollout-safety default-on per CONTEXT.md D-05)"`.
   - The assertion body itself stays substantially the same (`makeState({ status: "shell" })` does not set lastStopAt/lastStatusChangeAt → they are undefined → predicate treats as null → default-on) → expect `true`. Add an inline comment above the assertion: `// lastStopAt undefined → predicate normalizes to null → default-on branch fires`.

3. **Append Test M** after Test L (the source-level grep test — locate via `grep -n "Test L" src/ui/state/session-working-store.test.ts`):
   - `describe("session-working-store: Test M — status:'shell' + stale stop (Poppy/aqua/wilma pattern) → isWorking FALSE", () => { it("shell with lastStatusChangeAt strictly less than lastStopAt → false (stale-shell)", () => { const { result, rerender } = renderHook(() => useSessionIsWorking("h1:s1")); act(() => { publishFleetStatusSessionState("h1", makeState({ status: "shell", lastStopAt: 5000, lastStatusChangeAt: 1000 })); }); rerender(); expect(result.current).toBe(false); }); });`

4. **Append Test N**:
   - `describe("session-working-store: Test N — status:'shell' + fresh status-change (mid-turn) → isWorking TRUE", () => { it("shell with lastStatusChangeAt strictly greater than lastStopAt → true (real mid-turn shell)", () => { const { result, rerender } = renderHook(() => useSessionIsWorking("h1:s1")); act(() => { publishFleetStatusSessionState("h1", makeState({ status: "shell", lastStopAt: 1000, lastStatusChangeAt: 5000 })); }); rerender(); expect(result.current).toBe(true); }); });`

5. **Append Test O**:
   - `describe("session-working-store: Test O — status:'busy' bypasses stop-gate → isWorking TRUE regardless of stop ordering", () => { it("busy with lastStatusChangeAt strictly less than lastStopAt → true (busy is unconditional)", () => { const { result, rerender } = renderHook(() => useSessionIsWorking("h1:s1")); act(() => { publishFleetStatusSessionState("h1", makeState({ status: "busy", lastStopAt: 5000, lastStatusChangeAt: 1000 })); }); rerender(); expect(result.current).toBe(true); }); });`

6. **Append Test P** (the Pitfall-3 regression guard):
   - `describe("session-working-store: Test P — Axis A republish PRESERVES cached lastStopAt (Phase 61 Pitfall-3 regression guard)", () => { it("publishing an isWorking-flipping frame without lastStopAt preserves the cached lastStopAt", () => { act(() => { publishFleetStatusSessionState("h1", makeState({ status: "shell", lastStopAt: 1000, lastStatusChangeAt: 5000 })); }); const snapBefore = getSessionWorkingSnapshot(); expect(snapBefore.get("h1:s1")?.lastStopAt).toBe(1000); expect(snapBefore.get("h1:s1")?.isWorking).toBe(true); act(() => { publishFleetStatusSessionState("h1", makeState({ status: "idle" })); }); const snapAfter = getSessionWorkingSnapshot(); expect(snapAfter.get("h1:s1")?.lastStopAt).toBe(1000); expect(snapAfter.get("h1:s1")?.lastStatusChangeAt).toBe(5000); expect(snapAfter.get("h1:s1")?.isWorking).toBe(false); }); });`
   - IMPORTANT: `makeState({ status: "idle" })` MUST NOT set lastStopAt or lastStatusChangeAt — the fixture builder's spread pattern (line 52-63) already achieves this because they are absent from the base object AND absent from the override. The test asserts the Axis-A-republish path (isWorking flips from true → false) preserves the cached lastStopAt of 1000 via the Task 1 Axis A `nextMap.set` extension.

7. Run `npx vitest run src/ui/state/session-working-store` after each new test. All tests (A through P) MUST pass. If Test P fails, Task 1's Axis A cache-preservation extension was NOT applied to the new axes — return to Task 1.

**MUST NOT**:
- Delete Test B — REVISE its expectation and header (the deliberate rule from RESEARCH § Pitfall 8).
- Delete or modify any of Tests A, C, D, E, F, G, H, I, J, K, L — they cover invariant behaviors that Phase 61 does NOT change.
- Introduce new imports beyond what's already at the top of the test file.
- Change the `beforeEach(() => __resetForTest())` setup pattern.
- Skip the Test P snapshot-assertion approach in favor of a simpler `useSessionIsWorking`-only test — the snapshot inspection is what catches Pitfall 3 (a `useSessionIsWorking`-only test would see the correct `false` on the second frame regardless of whether lastStopAt was preserved).
  </action>
  <verify>
    <automated>npx vitest run src/ui/state/session-working-store --reporter=verbose</automated>
    <automated>grep -c 'Test [MNOP]' src/ui/state/session-working-store.test.ts</automated>
    <automated>grep -c 'Phase 61' src/ui/state/session-working-store.test.ts</automated>
    <automated>grep -c 'inline-260823-wip-shell-is-work' src/ui/state/session-working-store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/ui/state/session-working-store` — all tests A through P green (16 tests total: A, B revised, C, D, E, F, G, H, I, J, K, L, M, N, O, P).
    - `grep -c 'Test M\|Test N\|Test O\|Test P' src/ui/state/session-working-store.test.ts` >= 4 (all four new tests present).
    - `grep -c 'Phase 61' src/ui/state/session-working-store.test.ts` >= 2 (at least the Test B revised header + one new test header cite the phase).
    - `grep -c 'inline-260823-wip-shell-is-work' src/ui/state/session-working-store.test.ts` >= 1 (superseded rule is CITED in Test B's revised header — MUST NOT delete for historical traceability).
    - Test B's `it(...)` title does NOT contain the words "shell IS real tool-execution work" (old expectation removed).
    - Test B's `it(...)` title contains the words "rollout-safety" OR "default-on" (new expectation present).
    - Test P's assertion body includes `expect(snapAfter.get("h1:s1")?.lastStopAt).toBe(1000)` (Pitfall-3 regression guard is a snapshot inspection, not a `useSessionIsWorking` shortcut).
  </acceptance_criteria>
  <done>Test B revised in header + title + expectation to the rollout-safety default-on branch; four new tests (M/N/O/P) cover stale-stop, mid-turn shell, busy-bypasses-gate, and the Pitfall-3 Axis-A cache-preservation regression guard; all 16 tests green; inline-260823-wip-shell-is-work citation preserved for historical traceability.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend WS frame → frontend session-working-store `publishFleetStatusSessionState` | Two new optional-nullable fields cross this boundary; the frontend does not zod-validate (per fleet-status-types.ts header docblock) and instead treats each field as `number | null | undefined` per the wire contract established in Plan 61-01. |
| session-working-store WorkingRecord → useSessionIsWorking predicate consumers | The predicate change alters the observable contract of `useSessionIsWorking(key)` for `status:"shell"` sessions. Consumer surfaces (WipBubble mount gate; row dot render) inherit the new behavior without modification — but this is a semantic contract change and MUST be documented in the Task 1 header comment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-03-01 | Information Disclosure | Predicate incorrectly flips shell to on during real work (CONTEXT.md § "What would make it wrong" — missing real work is worse than false-positive-when-idle) | mitigate | Rollout-safety default-on branch: `lastStopAt === null` (no evidence) defaults shell to on. Backend guarantees `lastStatusChangeAt` is bumped on every real status transition (Plan 61-02 Test P57-02-C), so mid-turn shell always reads `lastStatusChangeAt > lastStopAt` → on. Test N covers this canonical case. |
| T-61-03-02 | Repudiation | Cache-loss of lastStopAt on Axis A republish (Phase 53 Pitfall 3 recurring) | mitigate | Axis A `nextMap.set` extended with `lastStopAt: existing?.lastStopAt ?? null` and `lastStatusChangeAt: existing?.lastStatusChangeAt ?? null`. Test P is the regression guard — publishes an isWorking-flipping frame with wire-omitted lastStopAt, asserts cached value survives. |
| T-61-03-03 | Tampering | Skipped Test B revision would leave the pre-Phase-57 rule locked as a truth (RESEARCH § Pitfall 8) | mitigate | Task 2 EXPLICITLY revises Test B; acceptance criteria include a grep proving the old expectation string ("shell IS real tool-execution work") is absent. |
| T-61-03-04 | Information Disclosure | Frontend consumer surface unexpectedly affected (WipBubble or row dot breakage) | mitigate | Consumer files under `src/ui/features/pretty-view/` and `src/ui/features/pretty-conversations/` are EXPLICITLY out-of-scope; acceptance criteria include a `git diff --stat` assertion showing zero changes to those paths. |
| T-61-03-SC | Tampering | Package installs | accept | No new packages installed in this plan — plan is source-diff only within the existing frontend module. |
</threat_model>

<verification>
- `npx vitest run src/ui/state/session-working-store` — all 16 tests green (A/B-revised/C/D/E/F/G/H/I/J/K/L/M/N/O/P).
- Source assertions from Task 1 acceptance (lastStopAt/lastStatusChangeAt/shellCountsAsWork/Axis F/Axis G/inline-260823-wip-shell-is-work presence).
- Test-file assertions from Task 2 acceptance (Tests M/N/O/P present; Phase 61 header cite present; Test B title revised).
- Consumer invariance: `git diff --stat src/ui/features/pretty-view/ src/ui/features/pretty-conversations/` shows zero files changed for this plan's scope.
</verification>

<success_criteria>
- Frontend `main` predicate revised to `busy || (shell && stop-gate-fresh)` with `lastStopAt === null` defaulting shell to on.
- WorkingRecord extended with two new cached axes; Axis A/D/E cache-preservation extended; Axis F/G swap-and-notify blocks appended.
- Test B revised to cover the rollout-safety default-on branch (with the superseded rule cited for traceability); four new tests (M/N/O/P) cover stale-stop, mid-turn shell, busy-bypasses-gate, and the Axis-A Pitfall-3 regression guard.
- Zero consumer-site changes — WipBubble and row dot inherit the fix via the existing `useSessionIsWorking` hook.
- Frontend TypeScript compiles clean; scoped vitest run green.
- Phase 61 vertical slice is code-complete. Orchestrator handles the deploy motion per fleet rule (no deploy task at executor scope).
</success_criteria>

<output>
Create `.planning/phases/61-wip-indicator-shell-idle-gate-use-stop-hook-to-reject-stale-/61-03-SUMMARY.md` when done. Include:
- Confirmation the revised `main` predicate exactly matches the shape in RESEARCH § Frontend predicate.
- Confirmation Test B was revised (not deleted) with the superseded rule cited.
- Confirmation Tests M/N/O/P all green with their canonical case names listed.
- Confirmation zero files changed under `src/ui/features/pretty-view/` or `src/ui/features/pretty-conversations/`.
- Any unexpected consumer-site TypeScript error surfaced by the frontend build (should be zero — the predicate reads new WorkingRecord fields that are visible only to the store's own module).
- The Poppy/aqua/wilma post-deploy prediction: on the next real turn each session completes after the Phase 61 hook is deployed, the per-session Stop file lands for the first time, backend publishes lastStopAt for the first time, frontend predicate reads `lastStopAt > lastStatusChangeAt (from 18-20 hours ago)` → shellCountsAsWork = false → indicator OFF (correct).
</output>
