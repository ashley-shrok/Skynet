---
phase: 61-wip-indicator-shell-idle-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/fleet-status/stop-hook.sh
  - src/backend/fleet-status/remote-hook-install.ts
  - src/backend/fleet-status/wire-protocol.ts
  - src/ui/api/fleet-status-types.ts
  - src/backend/fleet-status/wire-protocol.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Editing stop-hook.sh alone does NOT desync STOP_HOOK_SCRIPT_CONTENTS — Test 11 in remote-hook-install.test.ts still asserts byte-exact equality."
    - "The new stop-hook.sh writes an ADDITIONAL per-session file at ~/.claude/fleet-status/stop-<session_id>.json on every Stop-hook fire, keyed on the session_id extracted from the piped stdin JSON, in addition to the existing ~/.claude/fleet-status/last-stop-payload.json write."
    - "SessionStateSchema accepts two new optional-nullable numeric fields: lastStopAt (unix millis) and lastStatusChangeAt (unix millis)."
    - "FRAME_SCHEMA_VERSION is UNCHANGED at 1 (additive-optional invariant per T-41-03-05, matching Phases 41/47/52/53)."
    - "A frame from a pre-Phase-57 backend that OMITS lastStopAt and lastStatusChangeAt still parses cleanly via SessionStateSchema.safeParse (back-compat)."
    - "The frontend SessionState interface at src/ui/api/fleet-status-types.ts mirrors the two new optional-nullable fields — no lockstep-mirror drift (Phase 53 Pitfall 7)."
  artifacts:
    - path: "src/backend/fleet-status/stop-hook.sh"
      provides: "Additive per-session Stop payload write"
      contains: "stop-"
    - path: "src/backend/fleet-status/remote-hook-install.ts"
      provides: "STOP_HOOK_SCRIPT_CONTENTS constant byte-in-sync with stop-hook.sh"
      contains: "stop-"
    - path: "src/backend/fleet-status/wire-protocol.ts"
      provides: "SessionStateSchema with lastStopAt + lastStatusChangeAt optional-nullable fields"
      contains: "lastStopAt"
    - path: "src/ui/api/fleet-status-types.ts"
      provides: "Frontend SessionState mirror of the two new fields"
      contains: "lastStopAt"
  key_links:
    - from: "src/backend/fleet-status/remote-hook-install.ts (STOP_HOOK_SCRIPT_CONTENTS)"
      to: "src/backend/fleet-status/stop-hook.sh"
      via: "Test 11 byte-equality assertion in remote-hook-install.test.ts"
      pattern: "STOP_HOOK_SCRIPT_CONTENTS.*toBe\\(diskContents\\)"
    - from: "src/backend/fleet-status/wire-protocol.ts SessionStateSchema"
      to: "src/ui/api/fleet-status-types.ts SessionState interface"
      via: "Lockstep mirror convention (see Phase 53 Pitfall 7)"
      pattern: "lastStopAt\\?: number \\| null"
---

<objective>
Foundation slice for the WIP-shell-idle-gate. Two atomic changes that MUST land together but ship no runtime behavior on their own:

1. **Managed-box hook script** gains an additive per-session file write. `stop-hook.sh` (source) AND `STOP_HOOK_SCRIPT_CONTENTS` (inlined copy in `remote-hook-install.ts`) are updated in the SAME commit so Test 11 (byte-equality gate) stays green.

2. **Wire schema** gains two new optional-nullable numeric fields — `lastStopAt` and `lastStatusChangeAt` — on `SessionStateSchema`, mirrored in `src/ui/api/fleet-status-types.ts`. `FRAME_SCHEMA_VERSION` stays at 1 (additive-optional invariant, fifth iteration of the pattern established by Phase 41).

Purpose: unblock 61-02 (backend polling of the new files) and 61-03 (frontend predicate consuming the new axes). No runtime effect until 61-02 populates the fields and 61-03 reads them.

Output: Two source diffs (stop-hook.sh + wire-protocol.ts + mirror files) + extended wire-protocol.test.ts covering the additive-axis test pattern established by Phase 53 (P53-01 A-F block).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-wip-indicator-shell-idle-gate-use-stop-hook-to-reject-stale-/61-CONTEXT.md
@.planning/phases/61-wip-indicator-shell-idle-gate-use-stop-hook-to-reject-stale-/61-RESEARCH.md
@src/backend/fleet-status/stop-hook.sh
@src/backend/fleet-status/remote-hook-install.ts
@src/backend/fleet-status/wire-protocol.ts
@src/ui/api/fleet-status-types.ts
@src/backend/fleet-status/wire-protocol.test.ts
@src/backend/fleet-status/remote-hook-install.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend stop-hook.sh with an additive per-session file write AND update STOP_HOOK_SCRIPT_CONTENTS in lockstep</name>
  <files>src/backend/fleet-status/stop-hook.sh, src/backend/fleet-status/remote-hook-install.ts</files>
  <read_first>
    - src/backend/fleet-status/stop-hook.sh (the 20-line current script — must be extended, not rewritten)
    - src/backend/fleet-status/remote-hook-install.ts (specifically the STOP_HOOK_SCRIPT_CONTENTS template-literal at lines 73-93; the entire literal MUST end up byte-for-byte identical to the on-disk stop-hook.sh after this task, per Test 11)
    - src/backend/fleet-status/remote-hook-install.test.ts (Test 11 at ~line 482-490 to understand the byte-equality assertion — do NOT modify this test; it is the gate)
    - src/backend/fleet-status/types.ts (StopHookPayloadSchema — confirms the piped stdin JSON has a stable `session_id: string` field)
    - .planning/phases/61-wip-indicator-shell-idle-gate-use-stop-hook-to-reject-stale-/61-RESEARCH.md § Code Examples "Stop hook — additive per-session write" (the exact bash shape recommended, including bash-vs-sh interpreter concern) AND § Open Questions #1 (managed-box bash vs sh) AND § Assumptions Log A1/A3 (bash regex + stat portability)
  </read_first>
  <behavior>
    - stop-hook.sh reads the piped stdin JSON ONCE into a bash variable, writes it atomically to BOTH the existing box-wide file AND a new per-session file, and exits 0.
    - Per-session file path: `${PAYLOAD_DIR}/stop-${session_id}.json` where `session_id` is extracted from the stdin JSON via a bash-native regex capturing the value of the `"session_id":"..."` key.
    - The regex accepts session_id values matching the character class `[a-zA-Z0-9_-]+` ONLY. Any character outside that class (including `/`, `.`, spaces, quotes, backticks) MUST cause the extraction to fail and the per-session write to be SKIPPED — the box-wide write still proceeds. This is a Tampering defense (T-61-01-SC: attacker-controlled session_id cannot escape to arbitrary paths).
    - Extraction failure (missing key, malformed JSON, character-class violation) is fail-open: box-wide file written; per-session file simply not written this fire. The hook still `exit 0`.
    - Interpreter change: the inner `timeout 2 sh -c '...'` becomes `timeout 2 bash -c '...'` because the new regex extraction requires bash 3.2+'s `[[ =~ ]]` operator (not POSIX-portable to dash/ash). Verified via Research § Environment Availability: bash is present on every managed box per the existing `#!/bin/bash` shebang and harness dependency.
    - The `timeout 2` ceiling MUST stay in place — the hook fires synchronously during Claude Code turn completion and cannot stall.
    - `.tmp` + `mv` atomicity pattern preserved for BOTH files (POSIX rename is atomic on same filesystem).
    - STOP_HOOK_SCRIPT_CONTENTS in remote-hook-install.ts MUST be updated in the SAME COMMIT to the exact same byte-content as the new stop-hook.sh (accounting for template-literal escaping: `$` → `\$`, `${...}` → `\${...}`). Test 11 (`STOP_HOOK_SCRIPT_CONTENTS).toBe(diskContents)`) is the enforcement gate.
  </behavior>
  <action>
Extend the shipped `stop-hook.sh` with an additive per-session file write. Read the current 20-line script first (`Read`) to preserve every existing line verbatim except the timeout invocation. The change set is:

1. **Constants**: after the existing `PAYLOAD_FILE` and `TMP_FILE` (box-wide) declarations, do NOT add new top-level constants — the per-session path is derived INSIDE the timeout subshell because it depends on the extracted session_id.

2. **Interpreter swap**: the inner `timeout 2 sh -c "..."` becomes `timeout 2 bash -c '...'` (single-quoted outer to make bash regex literals readable; ${HOME}-style variables passed via shell-argument or nested single-quote-double-quote dance as needed). Use the single-quote-outer + `"'"$VAR"'"` embedding pattern documented in Research § Code Examples so the parent shell's ${HOME} / ${PAYLOAD_DIR} / ${BOX_TMP_FILE} still expand at parent-shell time (matches the current script's approach).

3. **Read stdin once**: inside the `timeout 2 bash -c` block, `payload="$(cat)"` captures the entire JSON document into a bash variable ONCE (stdin is not re-readable).

4. **Box-wide write (unchanged behavior)**: `printf '%s' "$payload" > "$BOX_TMP_FILE" && mv "$BOX_TMP_FILE" "$PAYLOAD_FILE"`. Preserves the box-wide write for existing `backgroundTasks[]` consumers.

5. **Regex extract session_id**: `if [[ "$payload" =~ \"session_id\"[[:space:]]*:[[:space:]]*\"([a-zA-Z0-9_-]+)\" ]]; then sid="${BASH_REMATCH[1]}"; ...`. The character class `[a-zA-Z0-9_-]+` is the ONLY accepted charset — this is the security gate against path-traversal via a compromised harness. Do NOT loosen to `[^"]+` or similar.

6. **Per-session write (guarded by regex match)**: inside the `then` branch, derive `per_session_file="${PAYLOAD_DIR}/stop-${sid}.json"`, `per_session_tmp="${per_session_file}.$$.tmp"`, and `printf '%s' "$payload" > "$per_session_tmp" && mv "$per_session_tmp" "$per_session_file"`. Both `printf` and `mv` are POSIX-portable and behave identically under bash.

7. **Outer `|| true` preserved**: the entire timeout-wrapped operation stays best-effort — any failure (extract miss, disk full, timeout expiry) MUST NOT leak a non-zero exit code back to the harness.

8. **Immediately** after saving stop-hook.sh, open `src/backend/fleet-status/remote-hook-install.ts` and update the `STOP_HOOK_SCRIPT_CONTENTS` template literal (lines 73-93) to match byte-for-byte. Every `$` in the shell script becomes `\$` in the template literal; every `${...}` becomes `\${...}`. Every SINGLE-quoted region in the shell script stays single-quoted in the template literal. Every double-quoted region in the shell script stays double-quoted in the template literal. Preserve the existing header comment block verbatim.

9. Run Test 11 immediately after saving both files (`npx vitest run src/backend/fleet-status/remote-hook-install --testNamePattern "Test 11"`). If Test 11 fails, the byte-content diverges — DO NOT proceed to Task 2 until it passes.

**MUST NOT**:
- Change the `PAYLOAD_FILE` path (still `${PAYLOAD_DIR}/last-stop-payload.json`) — external consumers may depend on it.
- Widen the session_id character class beyond `[a-zA-Z0-9_-]+`.
- Remove the `timeout 2` ceiling.
- Move the box-wide write inside the `if [[ ... ]]` branch — it MUST fire unconditionally to preserve existing behavior for `backgroundTasks[]` consumers.
- Use `jq` or any other non-bash dependency (Research § Assumption A1 — regex is fleet-portable).
  </action>
  <verify>
    <automated>npx vitest run src/backend/fleet-status/remote-hook-install --reporter=verbose</automated>
    <automated>bash -n src/backend/fleet-status/stop-hook.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n src/backend/fleet-status/stop-hook.sh` exits 0 (syntax check on the shell script).
    - `npx vitest run src/backend/fleet-status/remote-hook-install --testNamePattern "Test 11"` passes — STOP_HOOK_SCRIPT_CONTENTS byte-matches stop-hook.sh on disk.
    - `grep -c 'stop-' src/backend/fleet-status/stop-hook.sh` >= 1 (per-session filename shape present).
    - `grep -c 'session_id' src/backend/fleet-status/stop-hook.sh` >= 1 (session_id extraction present).
    - `grep -c 'timeout 2 bash -c' src/backend/fleet-status/stop-hook.sh` == 1 (interpreter changed to bash for regex support).
    - `grep -c '\[a-zA-Z0-9_-\]' src/backend/fleet-status/stop-hook.sh` == 1 (character-class defense present).
    - `grep -c 'last-stop-payload.json' src/backend/fleet-status/stop-hook.sh` == 1 (box-wide filename unchanged).
    - Manual smoke: `printf '{"session_id":"abc-def-123","hook_event_name":"Stop","background_tasks":[]}' | bash src/backend/fleet-status/stop-hook.sh` in an env where `HOME=/tmp/test-hook` (mkdir first) exits 0 AND creates BOTH `/tmp/test-hook/.claude/fleet-status/last-stop-payload.json` AND `/tmp/test-hook/.claude/fleet-status/stop-abc-def-123.json`.
    - Manual attack-defense: `printf '{"session_id":"../../evil","hook_event_name":"Stop","background_tasks":[]}' | bash src/backend/fleet-status/stop-hook.sh` (with the same HOME) exits 0 AND creates ONLY the box-wide file — no file called `stop-../../evil.json` and no file outside `${HOME}/.claude/fleet-status/`.
  </acceptance_criteria>
  <done>Both `stop-hook.sh` and `STOP_HOOK_SCRIPT_CONTENTS` updated in a single commit; Test 11 green; syntax check green; manual smoke and attack-defense smoke both produce expected results.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend SessionStateSchema with lastStopAt + lastStatusChangeAt optional-nullable fields and mirror on the frontend</name>
  <files>src/backend/fleet-status/wire-protocol.ts, src/ui/api/fleet-status-types.ts, src/backend/fleet-status/wire-protocol.test.ts</files>
  <read_first>
    - src/backend/fleet-status/wire-protocol.ts (specifically the SessionStateSchema block at lines 195-215 and the FRAME_SCHEMA_VERSION declaration; the phase-comment header pattern at lines 156-193 for how prior Phases 52/53 documented their axis additions inline)
    - src/ui/api/fleet-status-types.ts (specifically the SessionState interface at lines 88-148 and the Phase 52/53 field docblock pattern at lines 118-147 for how prior mirror-adds documented lockstep obligation)
    - src/backend/fleet-status/wire-protocol.test.ts (the test file; specifically Test 1 for FRAME_SCHEMA_VERSION-lock pattern and the tests that validate SessionState optional fields — use `grep -n 'lastMessageAt\|aiTitle\|dormant\|recycling' src/backend/fleet-status/wire-protocol.test.ts` to locate the prior-phase test blocks to mirror)
    - .planning/phases/61-wip-indicator-shell-idle-gate-use-stop-hook-to-reject-stale-/61-RESEARCH.md § Code Examples "Wire schema extension" AND "Wire schema — back-compat test pattern" for the exact zod field shape and the six-test-per-field template
    - .planning/phases/61-wip-indicator-shell-idle-gate-use-stop-hook-to-reject-stale-/61-RESEARCH.md § Common Pitfalls Pitfall 7 (frontend mirror miss — the exact bug Phase 53 hit)
  </read_first>
  <behavior>
    - `SessionStateSchema` gains two new fields via `.extend`-shape append (i.e. new lines added inside the `z.object({...})` call, following the exact position pattern of Phase 53's `recycling` field): `lastStopAt: z.number().nullable().optional()` and `lastStatusChangeAt: z.number().nullable().optional()`.
    - Both fields receive an inline block-comment header in the same style as the Phase 52 `dormant` and Phase 53 `recycling` block comments (lines 156-193 style), naming them as Phase 61 additions and citing the additive-optional T-41-03-05 invariant.
    - `FRAME_SCHEMA_VERSION` is UNCHANGED — remains `1`. Test 1 in wire-protocol.test.ts continues to pass.
    - The frontend mirror at `src/ui/api/fleet-status-types.ts` gets both new fields on the `SessionState` interface with `?: number | null` optional-nullable type and mirror-style block comments citing "MUST stay in lockstep with the backend schema" (same convention as lines 132/147).
    - New wire-protocol.test.ts tests cover, for BOTH new fields (twelve new tests total): forward-with-number (parse succeeds, value preserved), forward-with-explicit-null (parse succeeds, value is null), back-compat-omitted (parse succeeds, field is undefined on the parsed result), type-enforcement (a string value causes parse failure with the field name in the error path), FRAME_SCHEMA_VERSION-lock re-assertion (unchanged at 1), and same-frame both-fields-populated (parse succeeds with both fields on the same state).
  </behavior>
  <action>
1. **wire-protocol.ts — schema extension**: Extend `SessionStateSchema` (currently lines 195-215) by adding two new lines INSIDE the `z.object({...})` call, immediately after the `recycling: z.boolean().nullable().optional(),` line. New lines:
   - `lastStopAt: z.number().nullable().optional(),`
   - `lastStatusChangeAt: z.number().nullable().optional(),`
   Prepend each line with an inline single-line comment `// Phase 61 Plan 01 — <field>: <one-sentence purpose>` (matches the inline-comment style of lines 207/209/211/213).

2. **wire-protocol.ts — block-comment header**: Add a new block-comment header immediately before the `export const SessionStateSchema = z.object({...})` line, in the same style as the Phase 53 header at lines 156-193. The header names:
   - The two new fields (`lastStopAt`, `lastStatusChangeAt`).
   - Their source: `lastStopAt` derived from `stat -c %Y` of `~/.claude/fleet-status/stop-<sessionId>.json` on the target host (multiplied by 1000 to be unix millis, matching every other timestamp axis on the wire); `lastStatusChangeAt` derived server-side by comparing this-tick `sessionJson.status` to the previous-tick cached status in `PidCacheEntry.lastStatus`.
   - Their semantics: `number` = value present; `null` = normalised-null in transit (backend may emit when it cannot distinguish); `undefined` = emitting backend pre-dates Phase 61 (treated as "no signal" by consumers).
   - Scope-lock: `lastStopAt` is UNCONDITIONALLY the mtime of the per-session Stop file; it does NOT fall back to the box-wide file. `lastStatusChangeAt` MUST NOT be sourced from `sessionJson.updatedAt` (harness bumps that on compose-box typing).
   - Additive-optional invariant: FRAME_SCHEMA_VERSION deliberately HELD AT 1 (T-41-03-05 mitigation, fifth iteration inheriting the pattern established by Phase 41 lastMessageAt and continued by Phase 47 aiTitle + Phase 52 dormant + Phase 53 recycling).

3. **fleet-status-types.ts — frontend mirror**: Immediately after the `recycling?: boolean | null;` line (line ~147), add:
   - A JSDoc-style block comment in the same style as lines 133-147 explaining the two new fields, citing "Phase 61 (WIP-shell-idle-gate 2026-08-26)" and the lockstep-mirror obligation ("MUST stay in lockstep with the backend schema").
   - `lastStopAt?: number | null;`
   - `lastStatusChangeAt?: number | null;`

4. **wire-protocol.test.ts — new test block**: Add a new `describe("wire-protocol Phase 61 additive axes — lastStopAt + lastStatusChangeAt", () => {...})` block AT THE END of the existing file (do not modify or reorder existing tests). Inside, add twelve tests following the P41/P47/P52/P53 pattern documented in Research § Wire schema — back-compat test pattern. For BOTH `lastStopAt` and `lastStatusChangeAt` (six per field):

   a. `Test P57-01-A: SessionStateSchema accepts state with <field> as a number` — construct `{ ...validSessionState, <field>: 1730000000000 }`; `SessionStateSchema.safeParse(...)` returns `success: true` and `data.<field> === 1730000000000`.

   b. `Test P57-01-B: SessionStateSchema accepts state with <field> as null` — `{ ...validSessionState, <field>: null }` parses successfully and `data.<field> === null`.

   c. `Test P57-01-C: SessionStateSchema accepts state OMITTING <field> (back-compat)` — `validSessionState` unmodified (no `<field>` key at all) parses successfully and `data.<field> === undefined`.

   d. `Test P57-01-D: SessionStateSchema REJECTS state with <field> as a string` — `{ ...validSessionState, <field>: "not-a-number" }` returns `success: false`; assert the error path includes the field name.

   e. `Test P57-01-E: FRAME_SCHEMA_VERSION remains 1 (additive-optional invariant)` — this ONE test asserted per PHASE (not per field), assert `FRAME_SCHEMA_VERSION === 1`.

   f. `Test P57-01-F: SessionStateSchema accepts state with BOTH new fields populated in the same frame` — `{ ...validSessionState, lastStopAt: 1730000000000, lastStatusChangeAt: 1730000005000 }` parses successfully and both values are preserved.

   That's 4 tests × 2 fields + 1 shared version-lock + 1 shared both-fields test = 10 tests total in the new describe block. Use the existing `validSessionState` fixture at line 16 of wire-protocol.test.ts as the base — do NOT duplicate its declaration.

5. Run `npx vitest run src/backend/fleet-status/wire-protocol` after each save; all pre-existing tests must remain green AND the new tests must pass. Also run `npm run build:backend` (per CLAUDE.md — frontend `tsc --noEmit` does NOT catch backend TS errors) to ensure the schema change compiles cleanly against consumers.

**MUST NOT**:
- Reorder or remove any existing fields on `SessionStateSchema`.
- Bump `FRAME_SCHEMA_VERSION` from `1` to any other value.
- Add validation constraints beyond `z.number().nullable().optional()` (e.g. `.positive()`, `.int()`, `.min(0)`) — the backend derives these values from `stat -c %Y * 1000` which is trivially non-negative; extra constraints would create back-compat risk if any legacy frame ever produces a boundary value.
- Import from `src/backend/` inside `src/ui/api/fleet-status-types.ts` (build-boundary invariant documented in the file's header docblock).
- Remove or edit the existing Test 11 in `remote-hook-install.test.ts` — that test remains the byte-equality gate.
  </action>
  <verify>
    <automated>npx vitest run src/backend/fleet-status/wire-protocol --reporter=verbose</automated>
    <automated>npm run build:backend</automated>
    <automated>grep -c 'lastStopAt' src/backend/fleet-status/wire-protocol.ts</automated>
    <automated>grep -c 'lastStopAt' src/ui/api/fleet-status-types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/backend/fleet-status/wire-protocol` passes all pre-existing tests AND the 10 new tests (grand total green).
    - `npm run build:backend` exits 0 (backend TS compiles with the new schema fields; catches consumer breakage the frontend `tsc --noEmit` would miss per CLAUDE.md).
    - `grep -v '^ *//' src/backend/fleet-status/wire-protocol.ts | grep -c 'lastStopAt'` >= 1 (source assertion; excludes header comment matches).
    - `grep -v '^ *//' src/backend/fleet-status/wire-protocol.ts | grep -c 'lastStatusChangeAt'` >= 1.
    - `grep -v '^ *//' src/ui/api/fleet-status-types.ts | grep -c 'lastStopAt'` >= 1 (frontend mirror present, excludes header comment matches).
    - `grep -v '^ *//' src/ui/api/fleet-status-types.ts | grep -c 'lastStatusChangeAt'` >= 1.
    - `grep -c 'FRAME_SCHEMA_VERSION = 1' src/backend/fleet-status/wire-protocol.ts` >= 1 (version constant unchanged).
    - `grep -c 'FRAME_SCHEMA_VERSION = 1' src/ui/api/fleet-status-types.ts` >= 1 (frontend mirror unchanged).
    - `grep -c 'Phase 61' src/backend/fleet-status/wire-protocol.ts` >= 1 (block-comment header present).
    - `grep -c 'Phase 61' src/ui/api/fleet-status-types.ts` >= 1 (frontend mirror block-comment present).
    - `grep -c 'describe.*Phase 61' src/backend/fleet-status/wire-protocol.test.ts` >= 1 (new describe block present).
  </acceptance_criteria>
  <done>Schema extended with both new optional-nullable fields, frontend mirror updated in lockstep, FRAME_SCHEMA_VERSION unchanged at 1, wire-protocol.test.ts has 10 new tests covering forward-number / forward-null / back-compat-omitted / type-enforcement / version-lock / both-fields for the additive axes, backend build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code harness → stop-hook.sh (stdin JSON) | The harness pipes JSON to the hook via stdin. In normal operation this is trusted local IPC, but a compromised harness (or a malicious `session_id` value crafted upstream) is a real Tampering vector because the hook script uses `session_id` in a filesystem path. |
| stop-hook.sh (bash regex extraction) → filesystem write path | The extracted `session_id` becomes part of a file path (`stop-${sid}.json`). Any character outside the accepted class could enable path traversal. |
| Backend wire protocol (SessionStateSchema) → frontend mirror | Extending the schema without updating the frontend mirror is a documented Phase 53 recurring bug (Pitfall 7). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01-01 | Tampering | stop-hook.sh session_id extraction | mitigate | Bash regex character class strictly `[a-zA-Z0-9_-]+` — any `/`, `.`, space, quote, backtick, or other metacharacter fails the match and skips the per-session write. Verified in Task 1 attack-defense smoke test. |
| T-61-01-02 | Denial of Service | stop-hook.sh timeout escalation | accept | Existing `timeout 2` ceiling preserved; hook still exits 0 on timeout via `|| true`. Cannot stall Claude Code turn completion. |
| T-61-01-03 | Tampering | STOP_HOOK_SCRIPT_CONTENTS byte-drift vs stop-hook.sh | mitigate | Test 11 in remote-hook-install.test.ts asserts byte-exact equality. Task 1 acceptance requires Test 11 green before commit; drift is impossible to ship without failing CI. |
| T-61-01-04 | Information Disclosure | Wire schema back-compat break | mitigate | Both new fields `.optional().nullable()`; FRAME_SCHEMA_VERSION held at 1; new wire-protocol.test.ts Test P57-01-C asserts back-compat parse of frame OMITTING both fields. |
| T-61-01-05 | Tampering | frontend mirror silently drifts from backend schema | mitigate | Explicit block-comment header on the frontend interface citing "MUST stay in lockstep with the backend schema" (same convention as lines 132/147); reviewer catches drift on grep-audit; Task 2 acceptance requires field presence in BOTH files. |
| T-61-01-SC | Tampering | Package installs | accept | No new packages installed in this plan — plan is source-diff only. |
</threat_model>

<verification>
- Test 11 (STOP_HOOK_SCRIPT_CONTENTS byte-matches stop-hook.sh) passes: `npx vitest run src/backend/fleet-status/remote-hook-install --testNamePattern "Test 11"`.
- Wire-protocol tests including new Phase 61 additive-axis block pass: `npx vitest run src/backend/fleet-status/wire-protocol`.
- Backend build clean: `npm run build:backend`.
- Bash syntax check on stop-hook.sh: `bash -n src/backend/fleet-status/stop-hook.sh`.
- Manual per-session smoke: piping a real Stop payload through stop-hook.sh produces both the box-wide and per-session files (Task 1 acceptance criteria).
- Manual attack-defense smoke: piping a Stop payload with a path-traversal `session_id` produces ONLY the box-wide file (Task 1 acceptance criteria).
</verification>

<success_criteria>
- stop-hook.sh writes both the existing box-wide payload file AND a new per-session file `stop-<session_id>.json`, with the per-session write gated behind a strict `[a-zA-Z0-9_-]+` character-class regex on `session_id`.
- STOP_HOOK_SCRIPT_CONTENTS in remote-hook-install.ts is byte-identical to the on-disk stop-hook.sh (Test 11 green).
- SessionStateSchema in wire-protocol.ts accepts `lastStopAt?: number | null` and `lastStatusChangeAt?: number | null` as additive-optional fields; back-compat with pre-Phase-57 frames preserved; FRAME_SCHEMA_VERSION unchanged at 1.
- src/ui/api/fleet-status-types.ts mirrors both fields with matching optional-nullable type signatures.
- Ten new tests in wire-protocol.test.ts document the additive-axis contract for both fields.
- Backend TypeScript compiles clean.
- Zero runtime behavior change on its own — no session-state consumer reads the new fields yet; that lands in 61-02 (backend) and 61-03 (frontend).
</success_criteria>

<output>
Create `.planning/phases/61-wip-indicator-shell-idle-gate-use-stop-hook-to-reject-stale-/61-01-SUMMARY.md` when done. Include:
- Confirmation both files (stop-hook.sh + STOP_HOOK_SCRIPT_CONTENTS) updated in the SAME commit.
- Confirmation Test 11 green.
- Confirmation `npm run build:backend` exits 0.
- The 10 new wire-protocol test names.
- Any deviation from the recommended bash regex shape (e.g. if the fleet required a `grep -P` fallback per Research § Assumption A1).
</output>
