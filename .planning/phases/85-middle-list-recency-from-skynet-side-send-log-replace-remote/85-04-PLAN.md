---
phase: 79-middle-list-recency-from-skynet-side-send-log-replace-remote
plan: 04
type: execute
wave: 3
depends_on:
  - 85-02
files_modified:
  - src/backend/fleet-status/ssh-poll-orchestrator.ts
  - src/backend/fleet-status/ssh-poll-orchestrator.test.ts
autonomous: true
requirements:
  - D-07
  - D-08
must_haves:
  truths:
    - "The per-tick lastMessageAt derivation in `ssh-poll-orchestrator.ts` around L1595-1650 stops reading `scanTailForNewestMessageAt` for the lastMessageAt axis, and instead calls `getIdentityLastSend(identityName)` where identityName is the resolved tmuxSession name for the session (D-07)."
    - "`scanTailForLatestAiTitle` on the same tail buffer STAYS CALLED — aiTitle derivation is unchanged."
    - "`scanTailForNewestMessageAt` and `isAshleyRealUserTurn` functions REMAIN EXPORTED and remain defined in the file — other consumers (D-08: sessions.ts + potential future readers) still use them. Do NOT delete either function."
    - "The tail exec still fires when a jsonlPath is resolved — but only for the aiTitle axis. On any tick where aiTitle derivation is not needed (all ticks — aiTitle is always derived), the exec continues; the change is which scanner consumes the buffer for lastMessageAt."
    - "The fingerprint composition at L769 is unchanged in shape — `state.lastMessageAt` remains an axis. When `getIdentityLastSend(name)` returns a fresher value on a subsequent tick, the fingerprint delta triggers a publish per the existing contract."
    - "The stale-tail rediscovery counter logic (L1590, L1636-1659) either retires from the lastMessageAt path OR is repurposed to only track the aiTitle path — pick one and document it. Preferred: retire from lastMessageAt (the send-log store never goes stale in the JSONL-rotation sense), keep it firing on the aiTitle-only signal so ai-title rediscovery still works."
    - "In-process test proves that a call to `stampIdentityLastSend(\"ivy\", 5000)` mutates the store, and the next `processPid` tick for a session whose identityName resolves to \"ivy\" publishes a SessionState frame with `lastMessageAt === 5000` (fingerprint delta triggers the publish)."
    - "Scoped test `npx vitest run --related src/backend/fleet-status/ssh-poll-orchestrator.ts` passes."
  artifacts:
    - path: "src/backend/fleet-status/ssh-poll-orchestrator.ts"
      provides: "swapped lastMessageAt derivation source"
      contains: "getIdentityLastSend"
    - path: "src/backend/fleet-status/ssh-poll-orchestrator.test.ts"
      provides: "test proving store update → published SessionState.lastMessageAt on next tick"
  key_links:
    - from: "src/backend/fleet-status/ssh-poll-orchestrator.ts"
      to: "src/backend/fleet-status/identity-send-log-store.ts getIdentityLastSend"
      via: "await import + call inside per-tick per-session loop"
      pattern: "getIdentityLastSend"
    - from: "SessionState.lastMessageAt (published)"
      to: "identity_send_log.last_send_at"
      via: "getIdentityLastSend(tmuxSession) lookup, keyed on identity name (D-02, D-07)"
      pattern: "state.lastMessageAt.*getIdentityLastSend|derivedLastMessageAt.*getIdentityLastSend"
---

<objective>
Swap the source of `SessionState.lastMessageAt` in the per-tick SSH-poll
orchestrator from the JSONL tail scan (`scanTailForNewestMessageAt`) to
the new send-log store lookup (`getIdentityLastSend`). Wire shape,
fingerprint contract, and every other axis are unchanged (D-07). The
transcript-scan pipeline stays alive for aiTitle + all other consumers
(D-08).

Purpose: This is THE swap that makes Phase 85 real. Once this lands and
Plan 85-06 fires the frontend stamp, sending to Ivy will make Ivy rise
in the middle zone within one fleet-status tick. Before this lands, the
store is populated but nothing reads it.

Output: The per-tick block around L1595-1650 reads the store instead of
the tail-scan for lastMessageAt. The tail-scan continues for aiTitle.
Test proves the round-trip: store write → next tick publish → SessionState
frame with the fresh value.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/85-middle-list-recency-from-skynet-side-send-log-replace-remote/79-CONTEXT.md
@src/backend/fleet-status/ssh-poll-orchestrator.ts
@src/backend/fleet-status/wire-protocol.ts
@src/backend/fleet-status/session-file-cache.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Swap the lastMessageAt derivation in the per-tick per-session block</name>
  <read_first>
    - src/backend/fleet-status/ssh-poll-orchestrator.ts (L418-476) — `isAshleyRealUserTurn` predicate. STAYS ALIVE per D-08. Do NOT delete.
    - src/backend/fleet-status/ssh-poll-orchestrator.ts (L508-521) — `scanTailForNewestMessageAt`. STAYS ALIVE per D-08 (sessions.ts still uses it via its byte-parallel copy). Do NOT delete.
    - src/backend/fleet-status/ssh-poll-orchestrator.ts (L552-573) — `scanTailForLatestAiTitle`. Unchanged. Still called from the same tail buffer.
    - src/backend/fleet-status/ssh-poll-orchestrator.ts (L1250-1260) — `tmuxSession` resolution for the current PID. This resolved string is the identity name for D-02 lookup.
    - src/backend/fleet-status/ssh-poll-orchestrator.ts (L1575-1660) — the exact block being swapped. Note the fail-open pattern: on discovery/exec failure, `derivedLastMessageAt` keeps its cached value. The swap preserves fail-open semantics: on `getIdentityLastSend` returning null OR throwing, keep the cached value (null on cold start).
    - src/backend/fleet-status/ssh-poll-orchestrator.ts (L725-769) — `computeFingerprint`. lastMessageAt is at position 5 of the fingerprint template. A change in lastMessageAt still produces a fingerprint delta → publish. No change needed to computeFingerprint.
    - src/backend/fleet-status/identity-send-log-store.ts (from Plan 85-02) — `getIdentityLastSend(identityName): Promise<number | null>`. The exact signature to consume.
    - .planning/phases/85-middle-list-recency-from-skynet-side-send-log-replace-remote/79-CONTEXT.md § D-07 (swap source), § D-08 (transcript-scan stays alive for other consumers), § Fingerprint / publish trigger.
  </read_first>
  <files>src/backend/fleet-status/ssh-poll-orchestrator.ts</files>
  <behavior>
    - Around L1575-1660, the block that resolves `derivedLastMessageAt`:
      - BEFORE: derivedLastMessageAt = scanTailForNewestMessageAt(tailRaw) when discovery+exec succeed; else cached value.
      - AFTER: derivedLastMessageAt = (await getIdentityLastSend(tmuxSession)) if tmuxSession is a non-null string; else cached value. Do NOT keyed on jsonlPath — the store is keyed on identity name, not path.
    - The tail exec continues to fire (aiTitle still needs it). Only the lastMessageAt line inside the "if (jsonlPath !== null)" block changes.
    - Actually: because the store lookup no longer requires a jsonlPath, hoist the getIdentityLastSend call OUTSIDE the "if (jsonlPath !== null)" block. lastMessageAt is derivable regardless of whether the JSONL was discovered. The if-block continues to run for the aiTitle axis (still needs jsonlPath + tail).
    - Guard: if `tmuxSession` is null (identity name unknown for this PID), skip the getIdentityLastSend call — keep the cached value. This preserves the cold-start "unknown identity" path.
    - Wrap the getIdentityLastSend call in try/catch (fail-open per Plan 85-02 contract — the module itself catches drizzle errors, but belt-and-suspenders here since this is on the hot per-tick path).
    - The stale-tail rediscovery counter (nextStaleTailTickCount, STALE_TAIL_REDISCOVERY_THRESHOLD) branches (L1636-1659): the three-branch logic (advance / no-history / stale) drove lastMessageAt's cache invalidation for JSONL rotation. Post-swap the store never rotates in that way. RETIRE the lastMessageAt-specific counter logic from this block. If nextStaleTailTickCount is still needed for aiTitle discovery rediscovery, keep the counter but drive it off the aiTitle scan result (scannedAiTitle) instead. If it's not needed for aiTitle either (aiTitle discovery rediscovery has never been separately governed), leave the counter mechanics but stop the increment on the lastMessageAt axis. Document the choice with an inline comment referencing Phase 85 + D-07.
    - Publication path: `derivedLastMessageAt` is fed into the SessionState construction later in processPid (unchanged). Fingerprint recomposes with the new value; if it differs from lastPublishedFingerprint, publishSessionState fires.
    - Structured log: emit `systemLogger.debug` on the getIdentityLastSend call with `operation: "fleet_status_last_message_at_from_store"`, hostId, identityName (tmuxSession), lookupResult (the returned number or null).
    - DO NOT DELETE `scanTailForNewestMessageAt` or `isAshleyRealUserTurn`. Add an inline comment above `scanTailForNewestMessageAt` noting it remains exported/defined for `src/backend/database/routes/sessions.ts` byte-parallel copy consumers per D-08. Add an inline comment above `isAshleyRealUserTurn` noting it stays alive as a shared helper.
  </behavior>
  <action>
    Import `getIdentityLastSend` at the top of ssh-poll-orchestrator.ts from `./identity-send-log-store.js`. Locate the block at L1575-1660. Reshape as follows:

    1. Immediately after the `let derivedLastMessageAt: number | null = cached?.lastMessageAt ?? null;` line (L1588), add a new block ABOVE the `if (jsonlPath !== null)` conditional:
       - `if (tmuxSession !== null) { try { const stored = await getIdentityLastSend(tmuxSession); if (stored !== null) { derivedLastMessageAt = stored; } } catch (err) { systemLogger.debug("Fleet-status: getIdentityLastSend failed — keeping cached lastMessageAt", { operation: "fleet_status_send_log_lookup_failed", fleetHostId: host.id, identityName: tmuxSession, error: err instanceof Error ? err.message : "unknown" }); } }`
       - This is the new source-of-truth for lastMessageAt.

    2. Inside the existing `if (jsonlPath !== null) { const tailRaw = await channel.exec(...); ... }` block:
       - REMOVE the line `scanned = scanTailForNewestMessageAt(tailRaw);` — the lastMessageAt derivation no longer reads from the tail.
       - KEEP the line `scannedAiTitle = scanTailForLatestAiTitle(tailRaw);` — aiTitle derivation is unchanged.
       - REMOVE the three-branch stale-tick logic (L1636-1650) that governed lastMessageAt's rediscovery counter. Replace with a simpler comment: `// Phase 85 (D-07): lastMessageAt no longer derived from tail — see store lookup above. Stale-tail counter retires from this axis. aiTitle discovery rediscovery, if needed in future, would drive off scannedAiTitle.`
       - The rediscovery threshold check at L1656-1659 (`if (nextStaleTailTickCount >= STALE_TAIL_REDISCOVERY_THRESHOLD) { jsonlPath = null; nextStaleTailTickCount = 0; }`) — since the counter no longer increments, this branch will never trip. Leave it or delete it. Preferred: DELETE the block and the STALE_TAIL_REDISCOVERY_THRESHOLD constant IF it has no other callers. `grep -rn "STALE_TAIL_REDISCOVERY_THRESHOLD" src/backend/fleet-status/` to check. If it's only used here, delete the constant + the branch.

    3. Above `function scanTailForNewestMessageAt` (L508), add a comment: `// Phase 85 (D-08): retired from the lastMessageAt derivation but STAYS EXPORTED — `src/backend/database/routes/sessions.ts` maintains a byte-parallel copy and hand-mirrors this scanner for the /sessions/list dormant-side derivation. If either copy changes, update BOTH.`

    4. Above `function isAshleyRealUserTurn` (L418), add a comment: `// Phase 85 (D-08): stays alive as a shared helper. Not called from this file's own lastMessageAt path (retired), but the sessions.ts byte-parallel copy of scanTailForNewestMessageAt still depends on this predicate's contract.`

    5. Consult the passenger cache shape (`PidCacheEntry` L200-260 range — see session-file-cache.ts or the local type in this file): the field `staleTailTickCount` on the cache entry may become dead weight. If it's read/written only inside the deleted three-branch block, remove it from the type + all writes. If it's referenced elsewhere (search for `staleTailTickCount`), leave it in place and merely retire the write path. Document the choice.

    Do NOT touch computeFingerprint. Do NOT touch scanTailForLatestAiTitle. Do NOT touch discoverIdentityJsonlPathViaChannel. Do NOT touch source B (dormant-poll) — the swap is source A only per the phase boundary.
  </action>
  <verify>
    <automated>npx vitest run --related src/backend/fleet-status/ssh-poll-orchestrator.ts src/backend/fleet-status/ssh-poll-orchestrator.test.ts</automated>
  </verify>
  <done>
    - `grep -c "getIdentityLastSend" src/backend/fleet-status/ssh-poll-orchestrator.ts` returns >= 2 (import + call).
    - `grep -c "^function scanTailForNewestMessageAt\|^export function scanTailForNewestMessageAt" src/backend/fleet-status/ssh-poll-orchestrator.ts` returns exactly 1 (function still defined — D-08).
    - `grep -c "^function isAshleyRealUserTurn" src/backend/fleet-status/ssh-poll-orchestrator.ts` returns exactly 1 (function still defined — D-08).
    - `grep -c "scanTailForNewestMessageAt(tailRaw)" src/backend/fleet-status/ssh-poll-orchestrator.ts` returns exactly 0 (the call inside the per-tick block is deleted).
    - `grep -c "scanTailForLatestAiTitle(tailRaw)" src/backend/fleet-status/ssh-poll-orchestrator.ts` returns exactly 1 (aiTitle scan preserved).
    - `npm run build:backend` compiles cleanly (no unused-import warnings; if scanTailForNewestMessageAt is no longer called in-file, verify no linter complains — the function itself is still exported for the sessions.ts byte-parallel discipline but that's a documentation contract, not an import contract).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test — store write → next processPid tick publishes SessionState with fresh lastMessageAt</name>
  <read_first>
    - src/backend/fleet-status/ssh-poll-orchestrator.test.ts (all lines) — the existing test harness pattern. Note how it fakes the SshChannel, drives processPid, and captures publishSessionState calls via a fake registry.
    - src/backend/fleet-status/identity-send-log-store.ts (from Plan 85-02) — the module the test will call to seed a stamp.
  </read_first>
  <files>src/backend/fleet-status/ssh-poll-orchestrator.test.ts</files>
  <behavior>
    - Test 85-04-01: With no prior stamp for identity "ivy", processPid for a session whose tmuxSession resolves to "ivy" publishes a SessionState with `lastMessageAt: null` (cold-start). The tail exec still fires; the aiTitle path is unaffected.
    - Test 85-04-02: After calling `stampIdentityLastSend("ivy", 5000)`, the next processPid tick for the same session publishes a SessionState with `lastMessageAt: 5000`. The fingerprint delta triggers publish (verified by observing publishSessionState was called with the new value).
    - Test 85-04-03: After calling `stampIdentityLastSend("ivy", 6000)`, the next tick publishes `lastMessageAt: 6000`. Fingerprint delta again.
    - Test 85-04-04: On the tick where the stamp is unchanged (no new send), processPid does NOT publish (fingerprint unchanged) — verifies the fingerprint-suppression contract still holds.
    - Test 85-04-05: When `getIdentityLastSend` is mocked to throw, processPid keeps the cached lastMessageAt (fail-open contract) and still publishes if OTHER axes changed. Doesn't blow up the poll loop.
    - Test 85-04-06: Scan is NOT called for lastMessageAt anymore — spy on `scanTailForNewestMessageAt` (if exported/spyable) OR verify by asserting a stamp advances the row's lastMessageAt while the tail is EMPTY (jsonlPath still discovered but tail returns "" — the store lookup is the only signal).
  </behavior>
  <action>
    Add a new `describe("Phase 85 lastMessageAt source swap — send-log store")` block to ssh-poll-orchestrator.test.ts. Reuse the existing test infrastructure (fake SshChannel, fake registry, fake clock). Set up per-test: an in-memory DB with the identity_send_log table (import the CREATE-TABLE sql from Plan 85-01's migration test const), reset the store between tests. For each test, wire the fake registry to capture publishSessionState calls. Assert on the captured frames' `lastMessageAt` field. Test 85-04-05 uses `vi.spyOn(storeModule, "getIdentityLastSend").mockRejectedValueOnce(new Error("db unavailable"))` to exercise the try/catch.
  </action>
  <verify>
    <automated>npx vitest run --related src/backend/fleet-status/ssh-poll-orchestrator.test.ts</automated>
  </verify>
  <done>
    - All 6 new test cases pass.
    - Existing ssh-poll-orchestrator.test.ts test cases remain green (no regressions to Phase 41/44/47/52/53/59/62 test coverage).
    - `grep -c "Phase 85" src/backend/fleet-status/ssh-poll-orchestrator.test.ts` returns >= 1.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store → orchestrator | Trusted — in-process module call. |
| SSH channel → orchestrator | Untrusted — remote shell output. Unchanged by this plan; the swap removes one consumer of the untrusted tail buffer (scanTailForNewestMessageAt) but the tail is still fed to scanTailForLatestAiTitle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-04-01 | Denial of Service | store lookup fails per tick | mitigate | Try/catch around getIdentityLastSend keeps cached value on failure; poll loop continues. Test 85-04-05 proves the fail-open path. |
| T-85-04-02 | Tampering | stale cache after store write | accept | The store lookup runs every tick; a stale cache only survives one tick beyond the store update. Optimistic client stamp (Plan 85-06) fills the instant-reorder gap. |
| T-85-04-03 | Information Disclosure | lookup logs identity name | accept | Same log discipline as every other fleet-status log (hostId + identityName appear widely). |
| T-85-04-04 | Elevation of Privilege | store read bypasses auth | accept | This is an in-process call from an already-trusted backend module. No user-facing route added by this plan. |
| T-85-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages. Audit-clean. |
</threat_model>

<verification>
- Task 1 verified by the test suite in Task 2, plus `npm run build:backend` clean.
- Task 2 verified by all 6 new test cases + regression check on existing tests.
- Cross-cutting: manual sanity check post-deploy (orchestrator scope): tail `/opt/skynet/console-forward-logs/console-forward.log` for `fleet_status_last_message_at_from_store` log lines; confirm they fire per-tick per-session with the identity name and the resolved ts.
</verification>

<success_criteria>
- The per-tick lastMessageAt derivation reads from `getIdentityLastSend` instead of `scanTailForNewestMessageAt`.
- `scanTailForNewestMessageAt` and `isAshleyRealUserTurn` remain defined (unused-in-file but exported for D-08 consumers).
- `scanTailForLatestAiTitle` continues to be called on the same tail buffer.
- Test proves the round-trip: store write → next tick publish → SessionState frame with fresh value.
- Fail-open on store lookup failure (cached value survives; poll loop continues).
- No regression to existing fleet-status axis tests.
- Scoped tests green: `npx vitest run --related src/backend/fleet-status/ssh-poll-orchestrator.ts`.
- `npm run build:backend` compiles cleanly.
</success_criteria>

<constraints>
- Executor stops at code + commit + scoped tests green.
- Do NOT deploy, push, build docker, force-recreate, or run the full suite.
- Do NOT use git worktrees. Work in `~/skynet-tiffany` on `feat/tab-title-from-tmux`.
- Do NOT delete `scanTailForNewestMessageAt` or `isAshleyRealUserTurn` — D-08 requires them for other consumers.
- Do NOT touch the wire-protocol.ts SessionState.lastMessageAt field type or name — D-07 explicitly preserves the wire shape.
- Do NOT touch computeFingerprint — lastMessageAt is already an axis; the source swap does not change the fingerprint contract.
- Do NOT touch source B (dormant-poll `pollDormantOnlyIdentities`) — the phase boundary limits the swap to source A (per-tick per-PID).
- The store lookup is on the hot per-tick per-session path. Keep it fast: no serialization overhead beyond the drizzle round-trip; the store module already caches the connection.
</constraints>

<output>
Create `.planning/phases/85-middle-list-recency-from-skynet-side-send-log-replace-remote/85-04-SUMMARY.md` when done.
</output>
