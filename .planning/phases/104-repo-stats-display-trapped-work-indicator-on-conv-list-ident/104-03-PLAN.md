---
phase: 104-repo-stats-display-trapped-work-indicator-on-conv-list-ident
plan: 03
type: execute
wave: 2
depends_on:
  - 104-01
  - 104-02
files_modified:
  - src/backend/claude-session/identity-artifact-reader.ts
  - src/backend/claude-session/claude-session-server.ts
  - src/backend/claude-session/identity-artifact-reader.count-bounties.test.ts
  - src/backend/claude-session/claude-session-server.count-bounties.test.ts
  - src/ui/api/claude-session-api.ts
  - src/ui/api/claude-session-api.count-bounties.test.ts
  - src/ui/state/bounty-counts-store.ts
  - src/ui/state/bounty-counts-store.test.ts
  - src/ui/features/pretty-conversations/PrettyBountyCountBadge.tsx
  - src/ui/features/pretty-conversations/PrettyBountyCountBadge.test.tsx
  - src/ui/features/pretty-conversations/PrettyConversationRow.tsx
  - src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx
  - src/ui/features/pretty-conversations/NewConversationModal.flow.test.tsx
  - src/ui/features/pretty-conversations/pretty-conversations.css
  - src/ui/features/pretty-view/IdentityModal.test.tsx
  - src/ui/features/pretty-view/IdentityModal.coordinator-empty.test.tsx
  - src/ui/features/pretty-view/IdentityModal.inherit-override.test.tsx
  - src/ui/features/pretty-view/IdentityModal.voice.test.tsx
  - src/ui/features/pretty-view/IdentityModal.stays-awake.test.tsx
  - src/ui/features/pretty-view/IdentityModal.wakeup-crud.test.tsx
autonomous: true
requirements:
  - D-04
  - D-11
tags:
  - deletion
  - refactor
  - bounty-counts
  - filter-toggles
  - retire

must_haves:
  truths:
    - "readIdentityBountyCounts function no longer exists in identity-artifact-reader.ts (D-04)"
    - "handleIdentityCountBounties WS handler + identity:count-bounties WS route no longer exist in claude-session-server.ts (D-04)"
    - "countIdentityBounties + all BountyCount* types no longer exist in claude-session-api.ts (D-04)"
    - "bounty-counts-store.ts and bounty-counts-store.test.ts files no longer exist on disk (D-04)"
    - "PrettyBountyCountBadge.tsx and PrettyBountyCountBadge.test.tsx files no longer exist on disk (D-04)"
    - "PrettyConversationRow.tsx renders no .pv-bounty-badge-wrap[data-testid=\"pv-bounty-badge-pinned\"] or [data-testid=\"pv-bounty-badge-needs-desk\"] element in any state (D-04)"
    - "PrettyConversationsPanel.tsx no longer imports startBountyCountPoller / useAllBountyCounts / bountyCountsCompositeKey (D-04)"
    - "PrettyConversationsPanel.tsx filter popover no longer contains data-testid=\"pv-filter-toggle-pinned\" or data-testid=\"pv-filter-toggle-needs-desk\" buttons (D-11 — the Ready toggle survives; only the two bounty-scoped toggles are retired alongside the store)"
    - "The trapped-work indicator (Plan 02) remains functional on both surfaces after the delete pass — no regression"
    - "All test-file mock stubs referring to @/state/bounty-counts-store are removed (D-04)"
    - "npm run type-check exits 0 after the delete pass (compile-green invariant)"
    - "The full frontend + backend test suite exits 0 after the delete pass (no orphan test failures)"
  artifacts:
    - path: src/backend/claude-session/identity-artifact-reader.ts
      provides: "File exists WITHOUT readIdentityBountyCounts; readIdentityTrappedWork (from Plan 01) intact"
      contains: "readIdentityTrappedWork"
    - path: src/backend/claude-session/claude-session-server.ts
      provides: "File exists WITHOUT handleIdentityCountBounties / identity:count-bounties route / readIdentityBountyCounts import; identity:probe-trapped-work route + handleIdentityProbeTrappedWork intact"
      contains: "handleIdentityProbeTrappedWork"
    - path: src/ui/api/claude-session-api.ts
      provides: "File exists WITHOUT countIdentityBounties / BountyCount* types; probeIdentityTrappedWork intact"
      contains: "probeIdentityTrappedWork"
    - path: src/ui/features/pretty-conversations/PrettyConversationRow.tsx
      provides: "File exists WITHOUT useBountyCounts import / bountyCounts hook call / two inline pv-bounty-badge JSX wraps; useTrappedWork + indicator JSX intact"
      contains: "pv-trapped-work-indicator"
    - path: src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
      provides: "File exists WITHOUT startBountyCountPoller / useAllBountyCounts / bountyCountsCompositeKey / matchesFilterForRow bounty branches / pinnedOnly + needsDeskOnly state / pv-filter-toggle-pinned + pv-filter-toggle-needs-desk buttons; startTrappedWorkPoller intact"
      contains: "startTrappedWorkPoller"
    - path: src/ui/features/pretty-conversations/pretty-conversations.css
      provides: "File exists WITHOUT .pv-avatar .pv-bounty-badge-wrap positioning rules or .pv-bounty-badge / .pv-bounty-badge-wrap / .pv-bounty-badge-icon / .pv-bounty-badge-num style rules; .pv-avatar .pv-trapped-work-indicator rules intact"
      contains: "pv-trapped-work-indicator"
  key_links:
    - from: "grep-sweep verification"
      to: "src/ tree"
      via: "grep -r on retired identifiers returns zero non-comment matches after commit"
      pattern: "readIdentityBountyCounts|handleIdentityCountBounties|countIdentityBounties|useBountyCounts|useAllBountyCounts|bountyCountsCompositeKey|startBountyCountPoller|BountyCountTarget|IdentityCountBountiesPayload|BountyCountResult|IdentityBountyCountsEvent|PrettyBountyCountBadge|invalidateIdentity|identity:count-bounties|identity:bounty-counts|pv-bounty-badge"
---

<objective>
Retire the entire bounty-count wire alongside the two role-scoped avatar-corner badges it fed. Per D-04, this is a full-domain deletion — not an unwiring, not a leave-file-in-place. Every layer goes: backend detector fn, backend WS handler + route + import, frontend api types + fn, whole store, badge component + its test, both inline JSX wraps in the row, the CSS positioning + style rules, the panel-level filter helpers, the two filter menu items, and every test file (delete or de-mock) that referenced any of it.

Per D-11 (Open Question #1 resolution): the pinned + needs-desk filter menu items in the panel popover are DELETED alongside their data source. The Ready filter (Phase 52) survives — it has an independent data source (session-working-store). The filter popover shrinks to a single Ready toggle; the popover button + dot indicator + Radix Popover machinery remain.

Order dependency (per RESEARCH.md task-ordering note + Anti-Patterns to Avoid): this plan runs in Wave 2, AFTER Plans 01 + 02 land the new trapped-work wire GREEN. If the new wire has a UAT-surfacing bug, the bounty-count wire is still available as rollback capacity until this plan commits. Once this plan commits, the retirement is atomic and irreversible.

Purpose: Reclaim the avatar-corner slot for the trapped-work indicator alone; remove role-scoped signal-smearing across identity rows (the pinned/needs-desk badges showed the same count on every identity sharing a role — anti-pattern the shape file explicitly identifies as "what would make it wrong"); eliminate one polling loop + one WS message type from the fleet.

Output: Zero net-new features. ~15 files modified with deletions + ~4-6 test/source files removed entirely. Compile-green invariant per Pitfall #4 mitigation: grep-sweep the full identifier list before committing; if any reference remains, fix it in the same commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/104-repo-stats-display-trapped-work-indicator-on-conv-list-ident/104-CONTEXT.md
@.planning/phases/104-repo-stats-display-trapped-work-indicator-on-conv-list-ident/104-RESEARCH.md
@.planning/phases/104-repo-stats-display-trapped-work-indicator-on-conv-list-ident/104-01-SUMMARY.md
@.planning/phases/104-repo-stats-display-trapped-work-indicator-on-conv-list-ident/104-02-SUMMARY.md

@src/backend/claude-session/identity-artifact-reader.ts
@src/backend/claude-session/claude-session-server.ts
@src/ui/api/claude-session-api.ts
@src/ui/state/bounty-counts-store.ts
@src/ui/features/pretty-conversations/PrettyConversationRow.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
@src/ui/features/pretty-conversations/pretty-conversations.css
@src/ui/features/pretty-conversations/PrettyBountyCountBadge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend deletion pass — remove readIdentityBountyCounts + handleIdentityCountBounties + identity:count-bounties route + tests + import</name>
  <files>src/backend/claude-session/identity-artifact-reader.ts, src/backend/claude-session/claude-session-server.ts, src/backend/claude-session/identity-artifact-reader.count-bounties.test.ts, src/backend/claude-session/claude-session-server.count-bounties.test.ts</files>
  <read_first>
    - src/backend/claude-session/identity-artifact-reader.ts (L4019-4146 — readIdentityBountyCounts function + surrounding section header comment to remove wholesale)
    - src/backend/claude-session/claude-session-server.ts (L65-80 imports region, L1159-1347 handleIdentityCountBounties + __handleIdentityCountBountiesForTests, L115 + L176 wire doc comments, L5697-5717 identity:count-bounties WS route, L2744 + L1354 + L1501 stray comment references)
    - .planning/phases/104-repo-stats-display-trapped-work-indicator-on-conv-list-ident/104-CONTEXT.md (D-04 deletion scope)
    - .planning/phases/104-repo-stats-display-trapped-work-indicator-on-conv-list-ident/104-RESEARCH.md (Removal Inventory in Runtime State Inventory section, Pitfall #4 grep-sweep list)
  </read_first>
  <action>
    Delete from `src/backend/claude-session/identity-artifact-reader.ts`:
    - The entire `// 10. readIdentityBountyCounts — ...` section including the section-header comment block starting at L4019 and the function body ending at L4146. Remove the whole span cleanly; do NOT leave a stub or "retired" placeholder.
    - Do NOT touch `readIdentityTrappedWork` (Plan 01), `resolveRoleForIdentity`, `IDENTITY_KEY_RE`, `shellEscape`, `execWithTimeout`, or any other helper — those all remain live for other consumers.

    Delete from `src/backend/claude-session/claude-session-server.ts`:
    - The `readIdentityBountyCounts` entry from the import block at L73 (leave surrounding imports untouched — only remove the one line).
    - The wire-doc comment lines at L115 and L176 that describe `identity:count-bounties` + `identity:bounty-counts`.
    - The whole handler section from ~L1159 (starts at "Quick 260727-tb1: identity:count-bounties handler" section-header comment) through L1347 (`__handleIdentityCountBountiesForTests` export line inclusive). This spans: `CountBountiesTarget` type, `CountBountiesResult` type, `readOneTarget` helper, `handleIdentityCountBounties` function, `__handleIdentityCountBountiesForTests` export. Remove the whole span.
    - The WS router route block at L5697-5717 (the whole `if (msg.type === "identity:count-bounties") { ... }` branch INCLUDING its preceding doc-comment block at L5697-5713).
    - Update the "same seam pattern as `__handleIdentityCountBountiesForTests`" comments at L2744 and L1354 and L1501 — either delete the reference or replace it with a reference to `__handleIdentityProbeTrappedWorkForTests` (the Plan 01 successor seam). Executor picks based on which reads cleaner in context.
    - Do NOT touch `handleIdentityProbeTrappedWork` / `__handleIdentityProbeTrappedWorkForTests` / `identity:probe-trapped-work` route (Plan 01).

    Delete these two files entirely (`git rm`):
    - `src/backend/claude-session/identity-artifact-reader.count-bounties.test.ts`
    - `src/backend/claude-session/claude-session-server.count-bounties.test.ts`

    Do NOT touch `identity-artifact-reader.trapped-work.test.ts` or `claude-session-server.trapped-work.test.ts` (Plan 01 test files).
  </action>
  <verify>
    <automated>npx vitest run src/backend/claude-session/identity-artifact-reader.trapped-work.test.ts src/backend/claude-session/claude-session-server.trapped-work.test.ts && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - Grep asserts (filter out comments — bare `grep -c` counts JSDoc, so filter with `grep -v '^\s*[/*]'` before counting): `grep -v '^\s*[/*]' src/backend/claude-session/identity-artifact-reader.ts | grep -c "readIdentityBountyCounts"` returns 0
    - Grep asserts: `grep -v '^\s*[/*]' src/backend/claude-session/claude-session-server.ts | grep -c "readIdentityBountyCounts\|handleIdentityCountBounties\|__handleIdentityCountBountiesForTests\|identity:count-bounties\|identity:bounty-counts"` returns 0
    - CLI: `test -f src/backend/claude-session/identity-artifact-reader.count-bounties.test.ts` exits 1 (file removed)
    - CLI: `test -f src/backend/claude-session/claude-session-server.count-bounties.test.ts` exits 1 (file removed)
    - Grep asserts: `grep -c "readIdentityTrappedWork" src/backend/claude-session/identity-artifact-reader.ts` returns at least 1 (Plan 01 addition intact)
    - Grep asserts: `grep -c "handleIdentityProbeTrappedWork" src/backend/claude-session/claude-session-server.ts` returns at least 2 (Plan 01 handler + route intact)
    - Test command: `npx vitest run src/backend/claude-session/identity-artifact-reader.trapped-work.test.ts src/backend/claude-session/claude-session-server.trapped-work.test.ts` exits 0 (25 Plan 01 tests still pass)
    - CLI: `npm run type-check` exits 0
    - CLI: `npm run build:backend 2>&1 | grep -v "polly-adapter\|transcribe-adapter" | grep -c "error TS"` returns 0 (no new type errors — the two pre-existing AWS SDK errors from Phase 98 are filtered out per STATE.md L29 note)
  </acceptance_criteria>
  <done>
    Backend bounty-count wire fully retired; readIdentityTrappedWork + handleIdentityProbeTrappedWork intact; type-check + backend build clean; the two count-bounties test files removed from git.
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend store + api + badge + row deletion pass — remove bounty-counts-store, PrettyBountyCountBadge, useBountyCounts consumption in row, all types + fn in claude-session-api, all bounty-related tests + mock stubs</name>
  <files>src/ui/api/claude-session-api.ts, src/ui/api/claude-session-api.count-bounties.test.ts, src/ui/state/bounty-counts-store.ts, src/ui/state/bounty-counts-store.test.ts, src/ui/features/pretty-conversations/PrettyBountyCountBadge.tsx, src/ui/features/pretty-conversations/PrettyBountyCountBadge.test.tsx, src/ui/features/pretty-conversations/PrettyConversationRow.tsx, src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx, src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx, src/ui/features/pretty-conversations/NewConversationModal.flow.test.tsx, src/ui/features/pretty-view/IdentityModal.test.tsx, src/ui/features/pretty-view/IdentityModal.coordinator-empty.test.tsx, src/ui/features/pretty-view/IdentityModal.inherit-override.test.tsx, src/ui/features/pretty-view/IdentityModal.voice.test.tsx, src/ui/features/pretty-view/IdentityModal.stays-awake.test.tsx, src/ui/features/pretty-view/IdentityModal.wakeup-crud.test.tsx</files>
  <read_first>
    - src/ui/api/claude-session-api.ts (L1053-1216 — countIdentityBounties + all BountyCount* types + wire doc block to remove)
    - src/ui/state/bounty-counts-store.ts (whole file — 225 lines, delete outright)
    - src/ui/features/pretty-conversations/PrettyBountyCountBadge.tsx (70 lines — delete outright)
    - src/ui/features/pretty-conversations/PrettyConversationRow.tsx (L138-145 useBountyCounts import block + comment, L327-340 useBountyCounts hook call + surrounding comment, L1170-1253 avatar+two bounty-badge inline JSX wraps to remove)
    - src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx (L126-128 vi.mock bounty-counts-store block, L733 conjunct comment, L1824-1935 bounty-badge visibility tests, L2870-3200 avatar-corner spinner rules tests referencing pv-bounty-badge selectors)
    - .planning/phases/104-repo-stats-display-trapped-work-indicator-on-conv-list-ident/104-CONTEXT.md (D-04 deletion scope, D-11 filter-toggle deletion — actually filter deletion is Task 3; this task is store+api+badge+row)
    - .planning/phases/104-repo-stats-display-trapped-work-indicator-on-conv-list-ident/104-RESEARCH.md (Removal Inventory + Pitfall #4 exhaustive grep list)
  </read_first>
  <action>
    Delete from `src/ui/api/claude-session-api.ts`:
    - The whole wire-doc block from ~L1053 (starts at "Phase 26 widening: per-target results…" comment) through L1094 (last IdentityBountyCountsEvent line). This removes: doc block, `BountyCountTarget` type, `IdentityCountBountiesPayload` type, `BountyCountResult` type, `IdentityBountyCountsEvent` type.
    - The `countIdentityBounties` function block from L1159 (JSDoc opening) through L1216 (closing brace).
    - Update the "Phase 90 Plan 90-03 (D-08.3)" comment at L1218-1229 that references "same … pattern countIdentityBounties uses above" — either delete the reference (if the comment reads cleanly without it) or replace with a reference to `probeIdentityTrappedWork` (the Plan 02 successor).
    - Do NOT touch `probeIdentityTrappedWork` / `TrappedWork*` types / `IdentityProbeTrappedWorkPayload` etc. (Plan 02).

    Delete these files entirely (`git rm`):
    - `src/ui/api/claude-session-api.count-bounties.test.ts`
    - `src/ui/state/bounty-counts-store.ts`
    - `src/ui/state/bounty-counts-store.test.ts`
    - `src/ui/features/pretty-conversations/PrettyBountyCountBadge.tsx`
    - `src/ui/features/pretty-conversations/PrettyBountyCountBadge.test.tsx`

    Delete from `src/ui/features/pretty-conversations/PrettyConversationRow.tsx`:
    - Import cleanup at L133: change `import { Pin, Monitor, GitPullRequestDraft } from "lucide-react";` → `import { GitPullRequestDraft } from "lucide-react";` (Pin + Monitor were only used by the retiring badges — grep the whole file to confirm no other Pin/Monitor consumer inside PrettyConversationRow.tsx; if grep shows a remaining consumer, keep the relevant import).
    - Delete L138-145: the `// Phase 68 Plan 04:` line + `import { useBountyCounts } from "@/state/bounty-counts-store";` + the entire "// Phase 48 Plan 05: PrettyBountyCountBadge is no longer instantiated…" comment block.
    - Delete L327-340: the "Phase 26 Plan 03 / Phase 48 Plan 05: per-row bounty counts pair…" comment block + the `useBountyCounts(...)` call. Keep `rowHostIdNum` (still used by useTrappedWork per Plan 02).
    - Delete the two inline JSX wraps at L1224-1253 (both `.pv-bounty-badge-wrap[data-testid="pv-bounty-badge-pinned"]` bottom-left + `[data-testid="pv-bounty-badge-needs-desk"]` bottom-right blocks). The `.pv-avatar` container div remains; the trapped-work indicator JSX (from Plan 02) remains as the sole avatar-corner affordance.
    - Update the section comment at L1172-1195 that describes "Phase 48 Plan 05: `.pv-avatar` is now the positioning host for the Pin + Monitor bounty-count badges…" — replace with a brief Phase 104 note explaining `.pv-avatar` now hosts only the trapped-work indicator, or delete the stale explanation entirely.
    - Do NOT touch the trapped-work indicator JSX (Plan 02) at the bottom of `.pv-avatar`.

    Delete from `src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx`:
    - The `vi.mock("@/state/bounty-counts-store", () => ({ useBountyCounts: () => undefined }))` block at L126-128 (leave the trapped-work mock from Plan 02 intact).
    - The entire `describe("PrettyConversationRow: bounty badge visibility — useBountyCounts pair (Phase 26 Plan 03)", ...)` block at L1824-1935 (about 6 tests).
    - The Plan-02-added Row Test 8 (coexistence-with-bounty-badges) — this test was flagged in Plan 02 as scheduled for deletion in Plan 03 since the bounty badges no longer render.
    - Any `pv-bounty-badge` selector references in the avatar-corner spinner-rules tests at L2870-3200 — either delete the assertions that reference those data-testids OR delete the surrounding `it(...)` cases if they only exist to test bounty-badge coexistence with spinner ring. Preserve any assertions about the spinner ring itself or the trapped-work indicator's coexistence with the spinner (add a new assertion mirroring the retired bounty-badge coexistence if the spinner tests rely on such a coexistence assertion).
    - Any `conjunct !hasQueuePending` L733 comment reference to "pinned bounty" — either update or drop the stale phrasing.

    Update mock stubs across test files (search + destructive-edit — grep first, then delete):
    - `src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx:41` — `vi.mock("@/state/bounty-counts-store", …)` block: DELETE. If a corresponding trapped-work mock exists (Plan 02 Task 2 Part E instructed to add one), keep it.
    - `src/ui/features/pretty-conversations/NewConversationModal.flow.test.tsx:133-135` — same DELETE.
    - `src/ui/features/pretty-view/IdentityModal.test.tsx:17,94` — DELETE the vi.mock stub AND the comment block that describes it.
    - `src/ui/features/pretty-view/IdentityModal.coordinator-empty.test.tsx:115` — DELETE the vi.mock stub.
    - `src/ui/features/pretty-view/IdentityModal.inherit-override.test.tsx:94` — DELETE.
    - `src/ui/features/pretty-view/IdentityModal.voice.test.tsx:87` — DELETE.
    - `src/ui/features/pretty-view/IdentityModal.stays-awake.test.tsx:85` — DELETE.
    - `src/ui/features/pretty-view/IdentityModal.wakeup-crud.test.tsx:109` — DELETE.
    - `src/ui/features/pretty-conversations/PrettyConversationsPanel.role-management-flow.test.tsx:98` — DELETE (also listed in initial grep-sweep).

    Preserve compile-green invariant per Pitfall #4: BEFORE committing, run the exhaustive grep-sweep from RESEARCH.md Pitfall #4 (`grep -rn "bounty-counts-store\|useBountyCounts\|useAllBountyCounts\|bountyCountsCompositeKey\|refreshBountyCounts\|startBountyCountPoller\|invalidateIdentity\|countIdentityBounties\|BountyCountTarget\|IdentityCountBountiesPayload\|BountyCountResult\|IdentityBountyCountsEvent\|identity:count-bounties\|identity:bounty-counts\|pv-bounty-badge\|PrettyBountyCountBadge\|readIdentityBountyCounts\|handleIdentityCountBounties\|__handleIdentityCountBountiesForTests" src/`) and verify zero non-comment matches remain. If any match remains, fix in the same commit (Task 3 handles the panel-level bounty consumers; this task's grep-sweep excludes PrettyConversationsPanel.tsx's remaining bounty imports which Task 3 owns).
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx src/ui/features/pretty-view/IdentityModal.test.tsx && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `test -f src/ui/state/bounty-counts-store.ts` exits 1 (file removed)
    - CLI: `test -f src/ui/state/bounty-counts-store.test.ts` exits 1
    - CLI: `test -f src/ui/features/pretty-conversations/PrettyBountyCountBadge.tsx` exits 1
    - CLI: `test -f src/ui/features/pretty-conversations/PrettyBountyCountBadge.test.tsx` exits 1
    - CLI: `test -f src/ui/api/claude-session-api.count-bounties.test.ts` exits 1
    - Grep asserts (excluding PrettyConversationsPanel.tsx which Task 3 owns): `grep -rl "bounty-counts-store\|useBountyCounts\|BountyCountTarget\|IdentityCountBountiesPayload\|BountyCountResult\|IdentityBountyCountsEvent\|countIdentityBounties\|PrettyBountyCountBadge\|invalidateIdentity" src/ | grep -v "PrettyConversationsPanel.tsx"` returns no files
    - Grep asserts: `grep -c "pv-bounty-badge" src/ui/features/pretty-conversations/PrettyConversationRow.tsx` returns 0
    - Grep asserts: `grep -c "useBountyCounts" src/ui/features/pretty-conversations/PrettyConversationRow.tsx` returns 0
    - Grep asserts: `grep -c "useTrappedWork" src/ui/features/pretty-conversations/PrettyConversationRow.tsx` returns at least 2 (Plan 02 intact)
    - Grep asserts: `grep -c "probeIdentityTrappedWork" src/ui/api/claude-session-api.ts` returns at least 1 (Plan 02 intact)
    - Grep asserts: `grep -v '^\s*[/*]' src/ui/api/claude-session-api.ts | grep -c "countIdentityBounties\|BountyCountTarget\|IdentityCountBountiesPayload\|BountyCountResult\|IdentityBountyCountsEvent"` returns 0
    - Test command: `npx vitest run src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` exits 0 (trapped-work tests pass; retired bounty-badge tests removed)
    - Test command: `npx vitest run src/ui/features/pretty-view/IdentityModal.test.tsx src/ui/features/pretty-view/IdentityModal.coordinator-empty.test.tsx src/ui/features/pretty-view/IdentityModal.inherit-override.test.tsx src/ui/features/pretty-view/IdentityModal.voice.test.tsx src/ui/features/pretty-view/IdentityModal.stays-awake.test.tsx src/ui/features/pretty-view/IdentityModal.wakeup-crud.test.tsx` exits 0 (all pass without bounty-counts mock stubs)
    - Test command: `npx vitest run src/ui/features/pretty-conversations/NewConversationModal.flow.test.tsx src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx` exits 0
    - CLI: `npm run type-check` exits 0
  </acceptance_criteria>
  <done>
    bounty-counts-store + PrettyBountyCountBadge + api types/fn all deleted; row no longer renders or consumes any bounty-count anything; all test-file mock stubs removed; type-check clean; trapped-work indicator + wire (Plans 01+02) unaffected.
  </done>
</task>

<task type="auto">
  <name>Task 3: PrettyConversationsPanel deletion pass — retire bounty-count poller mount + panel-level filter helpers + pinned + needs-desk filter menu items + CSS bounty rules; verify panel test suite green</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx, src/ui/features/pretty-conversations/pretty-conversations.css, src/ui/features/pretty-conversations/PrettyConversationsPanel.role-management-flow.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (L117-121 bounty-counts imports, L504-560 bounty-count poller mount, L562-575 invalidateIdentity comment block, L710-834 filter state + matchesFilterForRow + pinnedOnly/needsDeskOnly branches, L1615-1706 filter popover UI, L836+ display filtered arrays)
    - src/ui/features/pretty-conversations/pretty-conversations.css (L644-675 avatar-corner bounty-badge positioning rules, L1308-1412 .pv-bounty-badge / .pv-bounty-badge-wrap / .pv-bounty-badge-icon / .pv-bounty-badge-num style + mobile-bump rules)
    - .planning/phases/104-repo-stats-display-trapped-work-indicator-on-conv-list-ident/104-CONTEXT.md (D-04 delete scope, D-11 filter-toggle deletion — Ready toggle survives)
    - .planning/phases/104-repo-stats-display-trapped-work-indicator-on-conv-list-ident/104-RESEARCH.md (Open Question #1 resolution direction, Pitfall #4 grep-sweep, Pitfall #5 dormant-identities-must-be-polled — applies to trapped-work poller unchanged)
  </read_first>
  <action>
    Delete from `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`:
    - Import block at L117-121: delete the entire `import { bountyCountsCompositeKey, startBountyCountPoller, useAllBountyCounts } from "@/state/bounty-counts-store";` statement. Leave the Plan 02 `import { startTrappedWorkPoller } from "@/state/trapped-work-store";` INTACT.
    - The bounty-count poller `useEffect` at L504-560 (starts with "Quick 260727-tb1: bounty-count poller mount" comment; ends after `const stop = startBountyCountPoller(getTargets, 60_000); return stop; }, []);`). Delete the whole `useEffect` block including its preceding comment. Leave the Plan 02 trapped-work poller `useEffect` INTACT (it should be a sibling `useEffect` below).
    - The "Quick 260727-tb1: identity:bounty-priority-updated piggyback" comment block at L562-575 that describes the invalidateIdentity wiring context — obsolete after deletion.
    - The `pinnedOnly` + `needsDeskOnly` state declarations at L715-716 (`useState(false)` pair). Leave `readyOnly` (L717), `filterPopoverOpen` (L718), and their setters intact.
    - Update `anyFilterOn` derivation at L722 from `readyOnly || pinnedOnly || needsDeskOnly` → `readyOnly` (single-toggle).
    - The `useAllBountyCounts` hook call at L724-729 (with its Patch #167 comment block).
    - Update `matchesFilterForRow` useMemo at L810-834: remove the `pinnedOk` branch (L820), the `needsDeskOk` branch (L821), and the `hostIdNum`/`hostId`/`key`/`pair` derivation (L816-819). The `bountyCounts` dependency (L834) and `bountyCountsCompositeKey` reference also go. Simplify the return to `readyOk` only. Update the surrounding doc-comment header at L795-809 to reflect the new single-predicate shape.
    - The two filter-menu-item `<button>` elements at L1675-1704 (`data-testid="pv-filter-toggle-pinned"` + `data-testid="pv-filter-toggle-needs-desk"` blocks). Keep the `data-testid="pv-filter-toggle-ready"` button at L1660-1674.

    Delete from `src/ui/features/pretty-conversations/pretty-conversations.css`:
    - The whole "PHASE 48 PLAN 05 — AVATAR-CORNER BOUNTY BADGE POSITIONING" comment block + rules at L644-675 (the `.pv-avatar .pv-bounty-badge-wrap` base rule + both `[data-testid=…]`-scoped positioning rules). The trapped-work indicator's own CSS block from Plan 02 (`.pv-avatar .pv-trapped-work-indicator`) INTACT.
    - The whole `.pv-bounty-badge` + `.pv-bounty-badge-wrap` + `.pv-bounty-badge-icon` + `.pv-bounty-badge-num` style block at L1308-1350 (including the V12 notification-badge comment header at L1308).
    - The mobile-bump rules for `.pv-bounty-badge` + `.pv-bounty-badge-icon` + `.pv-bounty-badge-num` at L1398-1412 (inside the `@media (max-width: 767.98px)` block). Trapped-work indicator's mobile-bump rules from Plan 02 INTACT.

    Delete from `src/ui/features/pretty-conversations/PrettyConversationsPanel.role-management-flow.test.tsx`:
    - The `vi.mock("@/state/bounty-counts-store", …)` block at L98 (already listed in Task 2 — this task confirms).

    Final grep-sweep (Pitfall #4): run `grep -rn "bounty-counts-store\|useBountyCounts\|useAllBountyCounts\|bountyCountsCompositeKey\|refreshBountyCounts\|startBountyCountPoller\|invalidateIdentity\|countIdentityBounties\|BountyCountTarget\|IdentityCountBountiesPayload\|BountyCountResult\|IdentityBountyCountsEvent\|identity:count-bounties\|identity:bounty-counts\|pv-bounty-badge\|PrettyBountyCountBadge\|readIdentityBountyCounts\|handleIdentityCountBounties\|__handleIdentityCountBountiesForTests" src/` (no exclusions this time — all references must be gone). If any match remains, fix it in the same commit.

    Update `pv-filter-toggles` popover test coverage if any exists — the pinned + needs-desk toggle tests must be removed (they no longer exist). The Ready toggle tests remain.
  </action>
  <verify>
    <automated>grep -rn "bounty-counts-store\|useBountyCounts\|useAllBountyCounts\|bountyCountsCompositeKey\|refreshBountyCounts\|startBountyCountPoller\|invalidateIdentity\|countIdentityBounties\|BountyCountTarget\|IdentityCountBountiesPayload\|BountyCountResult\|IdentityBountyCountsEvent\|identity:count-bounties\|identity:bounty-counts\|pv-bounty-badge\|PrettyBountyCountBadge\|readIdentityBountyCounts\|handleIdentityCountBounties\|__handleIdentityCountBountiesForTests" src/ | grep -v '^\s*[/*]' | wc -l | grep -q '^0$' && npm run type-check && npx vitest run src/ui/features/pretty-conversations/</automated>
  </verify>
  <acceptance_criteria>
    - Grep sweep: `grep -rn "bounty-counts-store\|useBountyCounts\|useAllBountyCounts\|bountyCountsCompositeKey\|refreshBountyCounts\|startBountyCountPoller\|invalidateIdentity\|countIdentityBounties\|BountyCountTarget\|IdentityCountBountiesPayload\|BountyCountResult\|IdentityBountyCountsEvent\|identity:count-bounties\|identity:bounty-counts\|pv-bounty-badge\|PrettyBountyCountBadge\|readIdentityBountyCounts\|handleIdentityCountBounties\|__handleIdentityCountBountiesForTests" src/` returns zero lines (full retirement; do NOT filter comments — the whole domain is gone, not merely dormant)
    - Grep asserts: `grep -c 'data-testid="pv-filter-toggle-pinned"' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns 0
    - Grep asserts: `grep -c 'data-testid="pv-filter-toggle-needs-desk"' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns 0
    - Grep asserts: `grep -c 'data-testid="pv-filter-toggle-ready"' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns 1 (Ready toggle survives per D-11)
    - Grep asserts: `grep -c "startTrappedWorkPoller" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns at least 2 (Plan 02 poller intact)
    - Grep asserts: `grep -c "pinnedOnly\|needsDeskOnly" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns 0
    - Grep asserts: `grep -c "readyOnly" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns at least 3 (state + derivation + button — Ready toggle intact)
    - Grep asserts: `grep -c "pv-trapped-work-indicator" src/ui/features/pretty-conversations/pretty-conversations.css` returns at least 2 (Plan 02 rules intact)
    - CLI: `npm run type-check` exits 0
    - CLI: `npm run build:frontend 2>&1 | grep -c "error"` returns 0
    - Test command: `npx vitest run src/ui/features/pretty-conversations/` exits 0 (all pretty-conversations tests pass, including Plan 02 trapped-work tests, minus retired bounty-badge tests)
    - Test command: `npx vitest run src/ui/features/pretty-view/IdentityModal.test.tsx src/ui/features/pretty-view/IdentityModal.coordinator-empty.test.tsx src/ui/features/pretty-view/IdentityModal.inherit-override.test.tsx src/ui/features/pretty-view/IdentityModal.voice.test.tsx src/ui/features/pretty-view/IdentityModal.stays-awake.test.tsx src/ui/features/pretty-view/IdentityModal.wakeup-crud.test.tsx` exits 0
  </acceptance_criteria>
  <done>
    Panel bounty-count poller + panel-level filter helpers + two filter menu items + all bounty-scoped CSS retired; Ready toggle + Ready predicate + Radix Popover machinery intact; trapped-work poller (Plan 02) intact; full-domain grep sweep returns zero matches; type-check clean; full frontend build clean; all frontend tests pass. Bounty-count wire is fully and irreversibly retired.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deployed running container ↔ deleted-code stale browser tab | After deploy, a stale browser tab with cached JS bundle would still send `identity:count-bounties` frames the retired handler no longer routes; response never arrives; the (also-retired) bounty-counts-store's transport-error handler was designed for exactly this case (logs + poller stops trying). Post-deletion, the browser has no handler for the response either — but the response never arrives, so there is nothing to un-handle. Transient failure mode, resolves on browser refresh. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-10 | Denial of Service | Post-deploy stale browser tab sends `identity:count-bounties` frames indefinitely; backend WS router has no matching branch; router's default path logs "unknown message type" | accept | Bounded transient exposure: the retired frontend bundle's poller runs at 60s cadence, and the backend's "unknown message type" log path is a WARN not an ERROR; log volume is ~1 line per 60s per stale tab per identity in the tab's view. Browser refresh clears it. Not worth mitigation code — the stale-bundle-post-deploy pattern is fleet-general and this new instance carries no unique risk. |
| T-104-11 | Information Disclosure | Deleting the invalidateIdentity mock from IdentityModal test files might reveal that IdentityModal never actually consumed invalidateIdentity in production code — an audit red-flag | accept | Confirmed via grep: IdentityModal.tsx never imported invalidateIdentity (only the .test.tsx files mocked it, per CONTEXT.md's post-research inspection). The mock stubs were defensive over-mocking. Deletion is safe; no production call site to preserve. |
| T-104-12 | Tampering | The delete sweep could accidentally remove a subtly-shared helper (e.g., a utility that both bounty-counts and something else used) | mitigate | Task 3's final grep-sweep gate uses the exhaustive identifier list from RESEARCH.md Pitfall #4 (17+ identifiers). Task 1's grep-filter approach (`grep -v '^\s*[/*]'`) catches production references. Task 2 + Task 3 both include type-check + build gates. Any accidental over-deletion surfaces as a TypeScript compile error in the same commit. |
| T-104-13 | Denial of Service | Removing the panel filter helpers could break the display-filtering pipeline that Ready toggle also uses | mitigate | Task 3 explicitly preserves `readyOnly` state + `matchesFilterForRow` structure (just removes the pinned/needsDesk branches); the Ready predicate's data source (`rowSessionStates` from session-working-store) is independent of the bounty-count store. Test coverage: `npx vitest run src/ui/features/pretty-conversations/` includes any existing Ready-filter tests, which must remain green post-deletion. |
| T-104-SC | Tampering | No package installs. | n/a | This plan is pure deletion; no npm install actions. |
</threat_model>

<verification>
- Grep sweep across full `src/` tree for the 17+ retired identifiers returns ZERO matches (D-04 full-retirement gate)
- `npm run type-check` exits 0 after each task's commit (compile-green invariant)
- `npm run build:frontend` + `npm run build:backend` both exit 0 (modulo the two pre-existing AWS SDK errors from Phase 98 that resolve at Docker build)
- `npx vitest run src/` exits 0 (full test suite green — no orphan tests referencing retired identifiers)
- Grep confirms Plan 02 additions (probeIdentityTrappedWork, useTrappedWork, startTrappedWorkPoller, pv-trapped-work-indicator, GitPullRequestDraft imports) all intact
- Grep confirms Plan 01 additions (readIdentityTrappedWork, handleIdentityProbeTrappedWork, identity:probe-trapped-work, identity:trapped-work) all intact
- Ready filter toggle (Phase 52) survives with its `data-testid="pv-filter-toggle-ready"` button and readyOnly state
- The trapped-work indicator remains the SOLE avatar-corner affordance in `.pv-avatar` on both surfaces (no bounty badges, no other competitors)
</verification>

<success_criteria>
- The `readIdentityBountyCounts` + `handleIdentityCountBounties` + `countIdentityBounties` + `bounty-counts-store` + `PrettyBountyCountBadge` + `useAllBountyCounts` + `bountyCountsCompositeKey` + `startBountyCountPoller` + `invalidateIdentity` + `BountyCountTarget` + `IdentityCountBountiesPayload` + `BountyCountResult` + `IdentityBountyCountsEvent` + `identity:count-bounties` + `identity:bounty-counts` + `pv-bounty-badge*` identifier family is FULLY RETIRED across `src/` — zero grep matches in production or test code
- The two `pv-filter-toggle-pinned` + `pv-filter-toggle-needs-desk` menu items are gone; the `pv-filter-toggle-ready` menu item remains (D-11)
- The trapped-work indicator + wire from Plans 01 + 02 are fully intact and functional on both surfaces
- Type-check + full frontend + backend builds all clean
- Full test suite green; no orphan tests
- Deploy is orchestrator territory (D-10) — no deploy tasks in this plan; the deploy waits for Shape 3 (Phase 96) to ship, per fleet coord
</success_criteria>

<output>
Create `.planning/phases/104-repo-stats-display-trapped-work-indicator-on-conv-list-ident/104-03-SUMMARY.md` when done. Summary must document: (a) grep-sweep output confirming zero matches for the 17+ identifier list, (b) confirmation that Plans 01 + 02 additions remain intact (spot-grep each), (c) any tests that were deleted vs updated, (d) confirmation of type-check + both builds green, (e) confirmation that Ready toggle survives, (f) atomic commit SHA(s) — recommend one commit per task (backend / frontend-store-badge-row / panel-css) for review clarity, but a single all-in-one commit is acceptable if compile-green invariant holds throughout.
</output>
