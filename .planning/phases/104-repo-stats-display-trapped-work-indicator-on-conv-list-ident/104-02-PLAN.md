---
phase: 104-repo-stats-display-trapped-work-indicator-on-conv-list-ident
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/api/claude-session-api.ts
  - src/ui/api/claude-session-api.trapped-work.test.ts
  - src/ui/state/trapped-work-store.ts
  - src/ui/state/trapped-work-store.test.ts
  - src/ui/features/pretty-conversations/PrettyConversationRow.tsx
  - src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/features/pretty-conversations/pretty-conversations.css
  - src/ui/features/terminal/IdentityBadge.tsx
  - src/ui/features/terminal/IdentityBadge.test.tsx
autonomous: true
requirements:
  - D-03
  - D-05
  - D-06
  - D-07
  - D-08
tags:
  - frontend
  - pretty-conversations
  - identity-badge
  - websocket
  - store

must_haves:
  truths:
    - "When trapped-work-store returns {hasTrappedWork: true} for an identity, PrettyConversationRow renders the git-pull-request-draft indicator in the avatar's bottom-right corner with the tooltip text \"Has local work not yet pushed to any remote\" (D-05, D-07)"
    - "When trapped-work-store returns undefined (pre-fetch) OR {hasTrappedWork: false}, PrettyConversationRow renders NO indicator (D-06 start-absent, silent-for-non-participants)"
    - "When trapped-work-store returns {hasTrappedWork: true} for an identity, IdentityBadge (pretty-view header) renders the same git-pull-request-draft indicator in the avatar's bottom-right corner with the same tooltip text (D-05 consistency across surfaces)"
    - "When IdentityBadge's identity has no trapped work OR the probe hasn't returned yet, no indicator is rendered (D-06)"
    - "The trapped-work-store is polled from PrettyConversationsPanel at 60_000 ms cadence + window.focus refresh, using the same getTargets closure shape as startBountyCountPoller (D-08)"
    - "probeIdentityTrappedWork sends exactly ONE identity:probe-trapped-work frame per invocation and resolves on the first matching identity:trapped-work response (D-03)"
    - "The new indicator uses lucide-react's GitPullRequestDraft icon (D-05), warm-amber styling built against existing --color-pv-* tokens (D-05), and no click handler (D-05 hover-only affordance)"
  artifacts:
    - path: src/ui/api/claude-session-api.ts
      provides: "probeIdentityTrappedWork + TrappedWorkTarget + IdentityProbeTrappedWorkPayload + TrappedWorkResult + IdentityTrappedWorkEvent types"
      exports:
        - probeIdentityTrappedWork
        - TrappedWorkTarget
        - IdentityTrappedWorkEvent
    - path: src/ui/state/trapped-work-store.ts
      provides: "useTrappedWork hook + useAllTrappedWork hook + trappedWorkCompositeKey + startTrappedWorkPoller + refreshTrappedWork"
      exports:
        - useTrappedWork
        - startTrappedWorkPoller
    - path: src/ui/features/pretty-conversations/PrettyConversationRow.tsx
      provides: "Trapped-work indicator rendered on .pv-avatar bottom-right, gated on useTrappedWork(identityKey, hostId).hasTrappedWork === true"
      contains: "pv-trapped-work-indicator"
    - path: src/ui/features/terminal/IdentityBadge.tsx
      provides: "Trapped-work indicator rendered in inner fragment near avatar, gated on useTrappedWork(identityKey, hostId).hasTrappedWork === true"
      contains: "pv-trapped-work-indicator"
    - path: src/ui/features/pretty-conversations/pretty-conversations.css
      provides: "CSS rules for .pv-avatar .pv-trapped-work-indicator warm-amber corner pill styling"
      contains: "pv-trapped-work-indicator"
  key_links:
    - from: src/ui/features/pretty-conversations/PrettyConversationRow.tsx
      to: src/ui/state/trapped-work-store.ts
      via: "useTrappedWork(identity?.identityKey ?? null, hostId) hook subscription per row"
      pattern: "useTrappedWork\\("
    - from: src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
      to: src/ui/state/trapped-work-store.ts
      via: "startTrappedWorkPoller(getTargets, 60_000) mount in useEffect"
      pattern: "startTrappedWorkPoller"
    - from: src/ui/state/trapped-work-store.ts
      to: src/ui/api/claude-session-api.ts
      via: "probeIdentityTrappedWork(targets) call inside refreshTrappedWork"
      pattern: "probeIdentityTrappedWork"
    - from: src/ui/features/terminal/IdentityBadge.tsx
      to: src/ui/state/trapped-work-store.ts
      via: "useTrappedWork(identityKey, hostId ?? null) hook at top of component"
      pattern: "useTrappedWork\\("
---

<objective>
Add the frontend plumbing that produces the trapped-work indicator on the two identity surfaces (D-05): the conversation-list row's avatar-corner and the pretty-view header IdentityBadge's avatar-corner. This plan is ADDITIVE — the new store, api function, and indicator JSX land ALONGSIDE the existing bounty-count wire; nothing is deleted here. The delete pass (retiring bounty-counts + the two Pin/Monitor avatar badges + the pinned/needs-desk filter toggles) is Plan 03 after this indicator is green.

Wire path mirrors bounty-counts byte-shape per D-03: one-shot WS request/response (`probeIdentityTrappedWork`) → module-scoped `useSyncExternalStore` map keyed by `${identityKey}:${hostId ?? "local"}` → per-row `useTrappedWork` hook → conditional JSX. Poll cadence is 60_000 ms + window.focus refresh per D-08 (matches the bounty-counts-store cadence — the CONTEXT.md "~30s" figure was corrected in RESEARCH.md).

Visual per D-05: `GitPullRequestDraft` from lucide-react (verified installed), warm amber built against existing `--color-pv-*` tokens (do NOT invent new palette values per D-05 constraint), drop-shadow glow at ~40% intensity, native `title=""` tooltip carrying D-07 copy `"Has local work not yet pushed to any remote"`. No click handler (D-05 hover-only affordance).

Purpose: With this plan green + Plan 01's backend green, the feature is functionally complete BUT the two old bounty-count badges are still visible in the row's avatar corners. Plan 03 removes them and the whole bounty-count wire. This ordering is deliberate: Plan 03's blast radius is smaller if we can visually A/B the indicator against the (still-present) bounty badges first.

Output: 1 new store, 1 new api function block, 2 new indicators (one per surface), 1 poller mount, 2 new test files, 4 modified components (test-mock stubs + new indicator JSX), 1 modified CSS file (new selector rules).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/104-repo-stats-display-trapped-work-indicator-on-conv-list-ident/104-CONTEXT.md
@.planning/phases/104-repo-stats-display-trapped-work-indicator-on-conv-list-ident/104-RESEARCH.md

@src/ui/api/claude-session-api.ts
@src/ui/state/bounty-counts-store.ts
@src/ui/features/pretty-conversations/PrettyConversationRow.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
@src/ui/features/pretty-conversations/pretty-conversations.css
@src/ui/features/terminal/IdentityBadge.tsx
@src/ui/index.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add frontend WS api + module-scoped trapped-work-store + tests</name>
  <files>src/ui/api/claude-session-api.ts, src/ui/api/claude-session-api.trapped-work.test.ts, src/ui/state/trapped-work-store.ts, src/ui/state/trapped-work-store.test.ts</files>
  <read_first>
    - src/ui/api/claude-session-api.ts (L1060-1216 — countIdentityBounties byte-shape mirror)
    - src/ui/api/claude-session-api.count-bounties.test.ts (whole file — test-shape mirror)
    - src/ui/state/bounty-counts-store.ts (whole file — byte-shape mirror for new store)
    - src/ui/state/bounty-counts-store.test.ts (whole file — test-shape mirror)
    - .planning/phases/104-repo-stats-display-trapped-work-indicator-on-conv-list-ident/104-CONTEXT.md (D-03 wire path, D-06 start-absent, D-08 cadence)
    - .planning/phases/104-repo-stats-display-trapped-work-indicator-on-conv-list-ident/104-RESEARCH.md (Common Operation 2 for probeIdentityTrappedWork skeleton, Pattern 6 for test shape)
  </read_first>
  <behavior>
    - API Test 1: probeIdentityTrappedWork opens a WS, sends one `{type: "identity:probe-trapped-work", targets: [...]}` frame on open, resolves with the raw response when a matching `{type: "identity:trapped-work", results: [...]}` frame arrives, then closes the socket.
    - API Test 2: onerror before response rejects with `Error("Connection failed")`.
    - API Test 3: onclose before response rejects with `Error("Connection failed")`.
    - API Test 4: unrelated frames (`{type: "other"}`) are ignored; only `identity:trapped-work` frames resolve.
    - API Test 5: JSON parse errors on incoming messages are swallowed (keep waiting for a valid frame).
    - Store Test 1 (useTrappedWork pre-fetch): renderHook of useTrappedWork("tina", null) returns undefined before any refresh.
    - Store Test 2 (useTrappedWork identityKey null): renderHook of useTrappedWork(null, null) returns undefined (short-circuit — non-identity rows).
    - Store Test 3 (useTrappedWork post-fetch): after refreshTrappedWork resolves with `{hasTrappedWork: true}` for tina@null, useTrappedWork("tina", null) returns `{hasTrappedWork: true}`.
    - Store Test 4 (composite-key isolation): useTrappedWork("tina", null) and useTrappedWork("tina", 42) yield independent snapshots.
    - Store Test 5 (batching): startTrappedWorkPoller(getTargets, 60_000) → exactly ONE probeIdentityTrappedWork call carrying all N deduped targets per tick.
    - Store Test 6 (poller cadence): after starting, initial fetch fires immediately, then every 60_000 ms interval fires an additional call; stop-fn clears the interval.
    - Store Test 7 (window.focus refresh): dispatching a window `focus` event fires an additional probe call; stop-fn removes the focus listener.
    - Store Test 8 (per-target error preservation): a target with `error` field in the response preserves the last-known snapshot for that composite key (does not overwrite with false).
    - Store Test 9 (transport-error resilience): probeIdentityTrappedWork rejection is caught by refreshTrappedWork; the store does not throw and the poller continues on the next tick.
    - Store Test 10 (useAllTrappedWork): returns the full ReadonlyMap snapshot; keys use the composite-key format `${identityKey}:${hostId ?? "local"}`.
  </behavior>
  <action>
    Part A — `probeIdentityTrappedWork` in claude-session-api.ts:

    Add a new section at the end of the bounty-counts wire block (immediately after `countIdentityBounties` at ~L1216), BEFORE `updateRoleFile` at L1229 (or wherever the next existing section starts — read the file to find the exact insertion point). Add:

    - Doc comment block mirroring L1053-1072 pattern, but describing the trapped-work wire: single one-shot request/response, wire types `identity:probe-trapped-work` → `identity:trapped-work`, per-target error field preserves shape uniformity, D-03 mirror-of-bounty-counts note, and the D-06 start-absent semantics.
    - `export type TrappedWorkTarget = { identityKey: string; hostId: number | null };`
    - `export type IdentityProbeTrappedWorkPayload = { type: "identity:probe-trapped-work"; targets: TrappedWorkTarget[]; };`
    - `export type TrappedWorkResult = { identityKey: string; hostId: number | null; hasTrappedWork: boolean; error?: string; };`
    - `export type IdentityTrappedWorkEvent = { type: "identity:trapped-work"; results: TrappedWorkResult[]; };`
    - `export function probeIdentityTrappedWork(targets: TrappedWorkTarget[]): Promise<IdentityTrappedWorkEvent>` — byte-shape mirror of `countIdentityBounties` L1173-1216. Differ ONLY in: request `type` string, response `type` string, resolved-shape name. Rejection copy stays `Error("Connection failed")` for parity with the bounty-counts wire.

    Part B — `trapped-work-store.ts` new file:

    Byte-shape mirror of `bounty-counts-store.ts` (225 lines). Same module structure (module-scoped state Map, listeners Set, `notify()`, `subscribe()`, `compositeKey()`). Differ in:

    - Import `probeIdentityTrappedWork` (not `countIdentityBounties`) from `@/api/claude-session-api`.
    - State value type: `{ hasTrappedWork: boolean }` (not `{ pinnedCount, needsDeskCount }`).
    - `useTrappedWork(identityKey, hostId)` — mirrors `useBountyCounts` including the identityKey-null short-circuit at L79.
    - `useAllTrappedWork()` — mirrors `useAllBountyCounts`.
    - `trappedWorkCompositeKey(identityKey, hostId)` — mirrors `bountyCountsCompositeKey`.
    - `refreshTrappedWork(targets)` — mirrors `refreshBountyCounts`. Per-target error path MUST preserve last-known snapshot (do NOT overwrite with false — mirrors L145-150 behavior for the pair; here it's the boolean).
    - `startTrappedWorkPoller(getTargets, intervalMs = 60_000)` — mirrors `startBountyCountPoller` VERBATIM. Same 60s default + `window.focus` listener + stop-fn shape.
    - `__resetTrappedWorkForTest()` — mirrors L222 test-only helper.
    - Do NOT include an `invalidateIdentity` equivalent — the trapped-work store has no post-mutation invalidation trigger analog (the bounty-count store's invalidateIdentity was for identity:update-bounty-priority, which does not exist for git state; the 60s poll + focus refresh cover the recovery window).

    File header JSDoc block MUST cite the mirror source (`bounty-counts-store.ts`) and the phase decision anchors (D-03, D-06, D-08).

    Part C — test files:

    - `claude-session-api.trapped-work.test.ts` — byte-shape mirror of `claude-session-api.count-bounties.test.ts`. Same imports, same mocking of `openClaudeSessionSocket`, same describe/it structure. Cover API Tests 1-5 above.
    - `trapped-work-store.test.ts` — byte-shape mirror of `bounty-counts-store.test.ts`. Same imports, same `vi.mock("@/api/claude-session-api", () => ({ probeIdentityTrappedWork: vi.fn() }))` pattern, same `beforeEach(() => __resetTrappedWorkForTest())`. Cover Store Tests 1-10 above.

    Do NOT delete or modify any existing bounty-count code.
  </action>
  <verify>
    <automated>npx vitest run src/ui/api/claude-session-api.trapped-work.test.ts src/ui/state/trapped-work-store.test.ts src/ui/api/claude-session-api.count-bounties.test.ts src/ui/state/bounty-counts-store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Grep asserts: `grep -c "^export function probeIdentityTrappedWork" src/ui/api/claude-session-api.ts` returns 1
    - Grep asserts: `grep -c "^export type TrappedWorkTarget" src/ui/api/claude-session-api.ts` returns 1
    - Grep asserts: `grep -c "^export function useTrappedWork" src/ui/state/trapped-work-store.ts` returns 1
    - Grep asserts: `grep -c "^export function startTrappedWorkPoller" src/ui/state/trapped-work-store.ts` returns 1
    - Grep asserts: `grep -c "invalidateIdentity" src/ui/state/trapped-work-store.ts` returns 0 (no invalidation trigger for git state — different semantics from bounty-counts)
    - Grep asserts: `grep -c "60_000\|60000" src/ui/state/trapped-work-store.ts` returns at least 1 (D-08 cadence)
    - Test command: `npx vitest run src/ui/api/claude-session-api.trapped-work.test.ts` exits 0 (5 tests pass)
    - Test command: `npx vitest run src/ui/state/trapped-work-store.test.ts` exits 0 (10 tests pass)
    - Test command: `npx vitest run src/ui/api/claude-session-api.count-bounties.test.ts src/ui/state/bounty-counts-store.test.ts` still exits 0 (bounty-counts unchanged)
    - CLI: `npm run type-check` exits 0
    - Behavior assertion (Store Test 5): mocked `probeIdentityTrappedWork` call count is EXACTLY 1 per poller tick with N targets in a single call array
  </acceptance_criteria>
  <done>
    New api function + new store both exist as byte-shape mirrors of the bounty-counts pair; 15 new tests pass; existing bounty-counts tests untouched and still pass; type-check clean; no invalidateIdentity equivalent (deliberate omission per D-06 semantics).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render trapped-work indicator on PrettyConversationRow avatar corner + wire poller in PrettyConversationsPanel + CSS + component tests</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationRow.tsx, src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx, src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx, src/ui/features/pretty-conversations/pretty-conversations.css</files>
  <read_first>
    - src/ui/features/pretty-conversations/PrettyConversationRow.tsx (whole file focus L130-160 imports, L320-345 useBountyCounts pattern, L1170-1260 avatar+badges JSX)
    - src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx (L100-205 mock-store pattern at L126, L1824-1935 bounty-badge visibility tests for shape parity)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (L117-121 bounty-counts-store imports, L500-560 bounty-count poller mount pattern to mirror, L720-729 useAllBountyCounts pattern — do NOT touch the filter helpers in this plan; Plan 03 owns filter deletion)
    - src/ui/features/pretty-conversations/pretty-conversations.css (L644-675 avatar-corner badge positioning block, L1308-1350 V12 notification-badge style block, L1352-1412 mobile-bump block)
    - src/ui/index.css:143-159 (--color-pv-* palette tokens — build indicator against these; do NOT invent new palette values)
    - src/ui/state/trapped-work-store.ts (created in Task 1 — for import)
    - .planning/phases/104-repo-stats-display-trapped-work-indicator-on-conv-list-ident/104-CONTEXT.md (D-05 visual, D-06 start-absent, D-07 tooltip copy, D-08 cadence)
    - .planning/phases/104-repo-stats-display-trapped-work-indicator-on-conv-list-ident/104-RESEARCH.md (Pattern 3 avatar-corner slot reuse, Pattern 5 icon+palette tokens, Pitfall #2 pointer-events tooltip, Pitfall #5 always-poll dormant identities)
  </read_first>
  <behavior>
    - Row Test 1 (indicator absent — undefined): mock useTrappedWork returns undefined for the row's identity → no element with `data-testid="pv-trapped-work-indicator"` in the DOM.
    - Row Test 2 (indicator absent — false): mock useTrappedWork returns `{hasTrappedWork: false}` → no element with `data-testid="pv-trapped-work-indicator"` in the DOM.
    - Row Test 3 (indicator present — true): mock useTrappedWork returns `{hasTrappedWork: true}` → element with `data-testid="pv-trapped-work-indicator"` exists inside `.pv-avatar`.
    - Row Test 4 (tooltip attribute): the indicator element has `title="Has local work not yet pushed to any remote"` (D-07 exact copy).
    - Row Test 5 (icon has aria-hidden): the SVG icon inside the indicator has `aria-hidden="true"` (screen-reader-invisible per accessibility hygiene).
    - Row Test 6 (icon is GitPullRequestDraft): the indicator's inner SVG has the lucide-react `git-pull-request-draft` glyph — assert via a distinguishing class-name or the exported component name (`GitPullRequestDraft` renders with class starting `lucide-git-pull-request-draft` per lucide-react convention).
    - Row Test 7 (non-identity row): rendering a row with no identity (RDP row, no matchKey) → no indicator (useTrappedWork short-circuits on null identityKey).
    - Row Test 8 (existing bounty badges unchanged): with the pre-existing useBountyCounts mock returning `{pinnedCount: 3, needsDeskCount: 1}` AND useTrappedWork returning `{hasTrappedWork: true}`, ALL THREE affordances render simultaneously (Pin badge bl, Monitor badge br, trapped-work indicator — the trapped-work indicator lands in a distinct slot so it can visually coexist during Wave 1; Plan 03 removes the two bounty badges). This test is DELETED in Plan 03.
    - Panel Test (integration, non-mock): PrettyConversationsPanel mount fires the trapped-work poller — assert via `startTrappedWorkPoller` mock being called with a function + `60_000` (existing panel-mount test infra reuses the same vi.mock strategy the bounty-count poller test uses; extend, do not replace).
  </behavior>
  <action>
    Part A — PrettyConversationRow.tsx modifications:

    1. Add import to the lucide-react import block at L133: change `import { Pin, Monitor } from "lucide-react";` to `import { Pin, Monitor, GitPullRequestDraft } from "lucide-react";` (Pin + Monitor stay for Plan 03 to remove).
    2. Add import block for the new store immediately after the existing L139 useBountyCounts import: `import { useTrappedWork } from "@/state/trapped-work-store";`.
    3. Add hook call inside the row component immediately after the L337-340 useBountyCounts block:
       - `const trappedWork = useTrappedWork(identity?.identityKey ?? null, Number.isFinite(rowHostIdNum) ? rowHostIdNum : null);`
    4. Add new JSX inside `.pv-avatar` (~L1254, immediately BEFORE the closing `</div>` of `.pv-avatar`, AFTER the two existing bounty-badge wraps). The new element structure:
       - `<span className="pv-trapped-work-indicator" data-testid="pv-trapped-work-indicator" title="Has local work not yet pushed to any remote">` (D-07 tooltip copy VERBATIM — no rewording)
       - Inside: `<GitPullRequestDraft className="pv-trapped-work-icon" aria-hidden="true" />`
       - Gate: `{trappedWork?.hasTrappedWork === true && (...)}` (D-06 start-absent — undefined and false BOTH render nothing).
    5. The new element uses a distinct CSS class `.pv-trapped-work-indicator` — do NOT reuse `.pv-bounty-badge-wrap` (Plan 03 will delete the bounty rules; keeping classes separate prevents accidental cross-pollination).

    Part B — PrettyConversationsPanel.tsx modifications (POLLER MOUNT ONLY — do NOT touch filter helpers):

    1. Add import block for the poller alongside the existing bounty-counts imports at L117-121:
       - Add: `import { startTrappedWorkPoller } from "@/state/trapped-work-store";` (separate import statement so Plan 03's delete of the bounty-counts imports is clean).
    2. Add a NEW useEffect immediately after the existing bounty-count poller useEffect at L530-560. Mirror the getTargets closure verbatim — same walk over `pinnedRowsRef.current + middleRef.current + rdpGroupRef.current.rows` for identity resolution + dedup by composite key (Pitfall #5 in RESEARCH.md: MUST poll dormant identities too — do NOT gate on activeSet). Call `startTrappedWorkPoller(getTargets, 60_000)` (D-08 cadence). Return the stop-fn from the effect.
    3. Do NOT delete the existing bounty-count poller. Both pollers coexist for Wave 1. Plan 03 removes the bounty-count poller.
    4. Do NOT touch `useAllBountyCounts`, `bountyCountsCompositeKey`, `matchesFilterForRow`, `pinnedOnly`, `needsDeskOnly`, or the filter popover UI. Those all belong to Plan 03's delete scope.

    Part C — pretty-conversations.css modifications (ADDITIVE ONLY):

    Add a new CSS block AFTER the existing `.pv-avatar .pv-bounty-badge-wrap` rules block (~L675, before `.pv-avatar .pv-avatar-fallback-icon svg` at L681). Structure:

    - `.pv-avatar .pv-trapped-work-indicator { position: absolute; bottom: -4px; right: -8px; z-index: 2; pointer-events: none; display: inline-flex; align-items: center; justify-content: center; width: 18px; height: 18px; border-radius: 999px; background: <warm-amber-value-built-from-existing-tokens>; box-shadow: 0 0 12px <warm-amber-glow-40%-intensity>; }` — the exact amber color values are Claude's Discretion (per D-05) but MUST be built from `--color-pv-*` tokens read from `src/ui/index.css:143-159` or from `hsla(35, ...)` family (the pretty-view neutral warm hue per RESEARCH.md Pattern 5). Do NOT introduce new palette variables (D-05 constraint).
    - `.pv-avatar .pv-trapped-work-indicator .pv-trapped-work-icon { width: 12px; height: 12px; color: #f0ebe0; /* cream — matches existing .pv-bounty-badge-icon color */ }`
    - Add matching mobile-bump rules inside the existing `@media (max-width: 767.98px)` block at L1358 (scale indicator to ~28px width/height like the bounty-badge mobile bump).
    - Pitfall #2 mitigation: if `pointer-events: none` prevents the native title tooltip from firing during execution/UAT, either (a) change to `pointer-events: auto` and rely on click-bubble to `.pv-avatar` (row's onClick still fires via bubble; test manually during smoke), or (b) split into an outer `pointer-events: none` wrap with an inner `pointer-events: auto` child carrying the title. Executor picks at UAT; the primary path is `pointer-events: none` for parity with bounty-badge-wrap L666.

    Part D — PrettyConversationRow.test.tsx modifications:

    1. Add a NEW top-level `vi.mock` block near the existing bounty-counts mock at L126:
       - `let currentTrappedWork: { hasTrappedWork: boolean } | undefined = undefined;`
       - `vi.mock("@/state/trapped-work-store", () => ({ useTrappedWork: () => currentTrappedWork }));`
    2. Add a new `describe("PrettyConversationRow: trapped-work indicator visibility", () => { ... })` block at the end of the file. Cover Row Tests 1-7 above.
    3. Row Test 8 (coexistence with bounty badges) is added in this plan; Plan 03 will DELETE it alongside the bounty badges.

    Part E — Panel test integration (existing PrettyConversationsPanel tests):
    - Extend any existing `vi.mock("@/state/bounty-counts-store", ...)` mock in Panel-related test files to also mock `@/state/trapped-work-store` with `startTrappedWorkPoller: vi.fn(() => () => {})` — otherwise mount fails.
    - Grep for tests importing `PrettyConversationsPanel` and add the mock stub to each. Do NOT create new panel-mount tests in this task; existing panel tests are sufficient to verify the mount does not crash. Plan 03 removes the bounty-count mock; the trapped-work mock stays.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Grep asserts: `grep -c "GitPullRequestDraft" src/ui/features/pretty-conversations/PrettyConversationRow.tsx` returns at least 2 (import + JSX usage)
    - Grep asserts: `grep -c "useTrappedWork" src/ui/features/pretty-conversations/PrettyConversationRow.tsx` returns at least 2 (import + call)
    - Grep asserts: `grep -c 'data-testid="pv-trapped-work-indicator"' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` returns 1
    - Grep asserts: `grep -c 'title="Has local work not yet pushed to any remote"' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` returns 1
    - Grep asserts: `grep -c "startTrappedWorkPoller" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns at least 2 (import + call)
    - Grep asserts: `grep -c "startBountyCountPoller" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns at least 2 (still present — Plan 03 removes)
    - Grep asserts: `grep -c "pv-trapped-work-indicator" src/ui/features/pretty-conversations/pretty-conversations.css` returns at least 2 (base rule + icon rule)
    - Grep asserts: `grep -c "pv-bounty-badge-wrap" src/ui/features/pretty-conversations/pretty-conversations.css` matches its pre-existing count (bounty CSS untouched here)
    - Test command: `npx vitest run src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` exits 0 with the 8 new tests passing AND all pre-existing tests passing (including the bounty-badge tests at L1824-1935 which are unchanged)
    - Test command: `npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx src/ui/features/pretty-conversations/NewConversationModal.flow.test.tsx` exits 0 (panel-adjacent tests with mock stubs pass)
    - CLI: `npm run type-check` exits 0
    - Behavior assertion (Row Test 3): asserting `screen.queryByTestId("pv-trapped-work-indicator")` returns non-null inside `.pv-avatar` container
  </acceptance_criteria>
  <done>
    Trapped-work indicator renders in the conv-list row avatar's bottom-right corner when useTrappedWork returns hasTrappedWork:true; absent otherwise; poller mounted in the panel at 60_000 ms cadence + focus refresh; CSS built against existing --color-pv-* / hsla-35 hue tokens with no new palette values; all row + adjacent tests pass; bounty-count wire remains fully live (Plan 03 removes it).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render trapped-work indicator on IdentityBadge (pretty-view header) with inline-style corner placement + tests</name>
  <files>src/ui/features/terminal/IdentityBadge.tsx, src/ui/features/terminal/IdentityBadge.test.tsx</files>
  <read_first>
    - src/ui/features/terminal/IdentityBadge.tsx (whole file — inner fragment structure L130-198, coordinator watermark inline-style pattern L147-156, hostId prop threading L18)
    - src/ui/features/terminal/IdentityBadge.test.tsx (whole file — BADGE-COORD-1/2/3 pattern at L392+, mock strategy at L1-150, FIXTURE shape at L27-35)
    - src/ui/state/trapped-work-store.ts (created in Task 1 — for import)
    - .planning/phases/104-repo-stats-display-trapped-work-indicator-on-conv-list-ident/104-CONTEXT.md (D-05 same visual across both surfaces, D-07 tooltip copy)
    - .planning/phases/104-repo-stats-display-trapped-work-indicator-on-conv-list-ident/104-RESEARCH.md (Pattern 4 IdentityBadge integration — inline-style corner placement, hostId reactivation for store lookup, Open Question #6 coordinator-watermark stacking)
  </read_first>
  <behavior>
    - Badge Test BADGE-TRAP-1 (indicator present when hasTrappedWork:true): with mock useTrappedWork returning `{hasTrappedWork: true}`, IdentityBadge root contains an element with `data-testid="pv-trapped-work-indicator"`.
    - Badge Test BADGE-TRAP-2 (indicator absent when undefined OR false): with mock returning undefined, no indicator element in the DOM; separately with mock returning `{hasTrappedWork: false}`, no indicator element.
    - Badge Test BADGE-TRAP-3 (tooltip copy + icon aria-hidden): the indicator's `title` attribute equals `"Has local work not yet pushed to any remote"`; the inner SVG has `aria-hidden="true"`.
    - Badge Test BADGE-TRAP-4 (coexistence with coordinator watermark): coordinator=true + hasTrappedWork:true → both `data-testid="coordinator-watermark"` AND `data-testid="pv-trapped-work-indicator"` render (both `pointer-events: none`, distinct z-index, non-overlapping semantic layers per Pattern 4).
    - Badge Test BADGE-TRAP-5 (identityKey null → no indicator): rendering IdentityBadge with identityKey=null → no indicator (short-circuits via useTrappedWork null-identityKey path).
    - Badge Test BADGE-TRAP-6 (hostId threading): mock the useTrappedWork hook to record its arguments; render IdentityBadge with hostId=42 → assert useTrappedWork was called with `(identityKey, 42)` (not `(identityKey, null)` — Pattern 4 reactivation).
  </behavior>
  <action>
    Part A — IdentityBadge.tsx modifications:

    1. Add imports at the top of the file (after existing L6-9 coordinator-watermark import):
       - `import { GitPullRequestDraft } from "lucide-react";`
       - `import { useTrappedWork } from "@/state/trapped-work-store";`
    2. Add hook call inside the IdentityBadge function body, immediately after the L104 early-return `if (!identity) return null;` guard (BEFORE the hue derivation at L109). Signature per Pattern 4:
       - `const trappedWork = useTrappedWork(identityKey, hostId ?? null);`
       - hostId prop at L18 is `hostId?: number` — reactivate its use for the store lookup (Pattern 4 explicitly reactivates it after Phase 68 made it a no-op for avatar-URL construction). Do NOT change the prop declaration.
    3. Add new JSX inside the `inner` fragment (between L156 coordinator-watermark closing bracket and L164 `<img>` element). The new element:
       - `{trappedWork?.hasTrappedWork === true && (`
       - `  <span aria-hidden="true" data-testid="pv-trapped-work-indicator" title="Has local work not yet pushed to any remote" style={{ position: "absolute", left: "calc(8px + 56px - 14px)", top: "calc(8px + 56px - 14px)", width: 18, height: 18, borderRadius: 999, display: "inline-flex", alignItems: "center", justifyContent: "center", background: "hsla(35, 65%, 55%, 0.85)", boxShadow: "0 0 12px hsla(35, 65%, 55%, 0.4)", zIndex: 2, pointerEvents: "none" }}>`
       - `    <GitPullRequestDraft width={12} height={12} color="#f0ebe0" aria-hidden="true" />`
       - `  </span>`
       - `)}`
       - Note: `aria-hidden="true"` on the wrap AND on the icon — the tooltip is not read by screen readers (visual-only affordance; the underlying trapped-work state is not surfaced elsewhere accessibly in this phase, which is acceptable per D-05 hover-only affordance + shape file's "silent for non-participants" intent).
       - Note: `pointer-events: none` per Pitfall #2 caveat — if UAT reveals the tooltip does not fire, executor switches to `pointerEvents: "auto"` (badge is not click-through-critical unlike the row).
       - Positioning math: the pill is `padding: "8px 18px 8px 8px"` (L116) around a 56×56 avatar starting at inline position 8px from left. The indicator sits at the avatar's bottom-right corner; `left: calc(8px + 56px - 14px)` = 50px from pill-left; `top: calc(8px + 56px - 14px)` = 50px from pill-top. This is a taste-adjustable placement; executor confirms visual placement against the mockup at `~/.claude/roles/box-maintainer/bounties/repo-stats-display/tasting/index.html` during UAT.
    4. Update the top-of-file comment block (L1-9 region) to add a brief note that hostId is now used (reactivated per Phase 104) for the trapped-work store lookup, superseding the Phase 68 "no longer used" note.

    Part B — IdentityBadge.test.tsx modifications:

    1. Add a top-level `vi.mock("@/state/trapped-work-store", ...)` block near the existing test setup at L1-150:
       - `let currentBadgeTrappedWork: { hasTrappedWork: boolean } | undefined = undefined;`
       - `vi.mock("@/state/trapped-work-store", () => ({ useTrappedWork: vi.fn(() => currentBadgeTrappedWork) }));`
    2. Add a new `describe("IdentityBadge — Phase 104 trapped-work indicator", () => { ... })` block AFTER the existing BADGE-COORD-* block at L392+. Cover Badge Tests BADGE-TRAP-1 through BADGE-TRAP-6 above.
    3. Coordinator-watermark tests (BADGE-COORD-1/2/3) MUST still pass unchanged — do NOT modify them.
    4. Restore `currentBadgeTrappedWork = undefined` in `beforeEach` to prevent cross-test bleed (matches the BADGE-COORD FIXTURE reset pattern).
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/terminal/IdentityBadge.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Grep asserts: `grep -c "GitPullRequestDraft" src/ui/features/terminal/IdentityBadge.tsx` returns at least 2 (import + JSX usage)
    - Grep asserts: `grep -c "useTrappedWork" src/ui/features/terminal/IdentityBadge.tsx` returns at least 2 (import + call)
    - Grep asserts: `grep -c 'data-testid="pv-trapped-work-indicator"' src/ui/features/terminal/IdentityBadge.tsx` returns 1
    - Grep asserts: `grep -c 'title="Has local work not yet pushed to any remote"' src/ui/features/terminal/IdentityBadge.tsx` returns 1
    - Grep asserts: `grep -c "coordinator-watermark" src/ui/features/terminal/IdentityBadge.tsx` matches its pre-existing count (coordinator surface untouched)
    - Test command: `npx vitest run src/ui/features/terminal/IdentityBadge.test.tsx` exits 0 with all 6 new BADGE-TRAP tests AND all pre-existing BADGE-COORD tests passing
    - CLI: `npm run type-check` exits 0
    - Behavior assertion (BADGE-TRAP-4): asserting BOTH `screen.getByTestId("coordinator-watermark")` AND `screen.getByTestId("pv-trapped-work-indicator")` return non-null when coordinator=true AND hasTrappedWork=true
    - Behavior assertion (BADGE-TRAP-6): the useTrappedWork mock's `.mock.calls[0]` is `[identityKey, 42]` (not `[identityKey, null]` — verifies hostId reactivation per Pattern 4)
  </acceptance_criteria>
  <done>
    Trapped-work indicator renders in the IdentityBadge's inner fragment at the avatar's bottom-right corner when useTrappedWork returns hasTrappedWork:true; absent otherwise; hostId prop is threaded into the store lookup; coordinator watermark coexists cleanly (both pointer-events: none, non-overlapping semantic layers); all badge tests including BADGE-COORD-* still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend WS response → frontend store | `identity:trapped-work` response frame parsed and applied to store; malformed shapes are handled by shape guards in probeIdentityTrappedWork (JSON.parse in try/catch, response.type discriminator). |
| Store → React components | Store returns `{hasTrappedWork: boolean} | undefined`; components use strict-equality gate `=== true` so both `undefined` and `false` render nothing. |
| User input → indicator | None — the indicator is presence-based, no user interaction beyond hover (D-05). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-06 | Denial of Service | Malformed response frame from a compromised WS could cause frontend crash | mitigate | `probeIdentityTrappedWork` wraps `JSON.parse` in try/catch (mirrors countIdentityBounties L1202); non-matching-type frames are ignored (L1194 pattern); rejection converts to `Error("Connection failed")`. Store's `refreshTrappedWork` catches transport-level rejection (mirrors bounty-counts-store L133-141). No unhandled promise paths. |
| T-104-07 | Spoofing | A malicious peer box could return `{"hasTrappedWork":true}` for an identity that has no trapped work, causing false-positive indicator | accept | The indicator is a RESCUE signal (D-06 philosophy), not a security signal. A false-positive nudges Ashley to check a repo that turns out to be clean — cost is one glance. Peer boxes are already trusted infrastructure (they hold identity credentials). Not a mitigatable threat within the phase scope. |
| T-104-08 | Information Disclosure | The indicator's mere presence discloses "this identity has code work in progress" to anyone who can see the browser | accept | Skynet's threat model already trusts the browser session (authenticated user has full fleet-access). The indicator is coarser than existing surfaces like the conversation label + AI title, which already leak much more per-identity signal. Explicit acceptance per phase scope. |
| T-104-09 | Tampering | Frontend user could DevTools-modify the store to force-render an indicator that wasn't sent | accept | Same threat model as any client-side state — the backend is authoritative. Client-side tampering has no server-side effect (indicator is display-only). No mitigation needed. |
| T-104-SC | Tampering | Package installs | n/a | No new packages. `lucide-react@1.28.0` (already in package.json L157) provides GitPullRequestDraft; verified installed via `node_modules/lucide-react/dist/esm/icons/git-pull-request-draft.mjs` per RESEARCH.md Package Legitimacy Audit + Standard Stack table. |
</threat_model>

<verification>
- All new Plan 02 tests pass: 5 API tests + 10 store tests + 8 row tests + 6 badge tests = 29 total green
- All pre-existing frontend tests still pass (regression check — this plan is purely additive; bounty-counts wire remains fully live)
- `npm run type-check` exits 0
- Grep: `.pv-trapped-work-indicator` selector appears in both `.tsx` (row + badge) and `.css` files
- Grep: exact tooltip string `"Has local work not yet pushed to any remote"` appears exactly TWICE across the two surface files (D-07 D-05 consistency)
- Grep: `GitPullRequestDraft` import + JSX use in both PrettyConversationRow.tsx AND IdentityBadge.tsx
- Grep: `startTrappedWorkPoller` mounted in PrettyConversationsPanel.tsx with 60_000 ms cadence
</verification>

<success_criteria>
- Trapped-work indicator renders on BOTH surfaces (conv-list row + IdentityBadge header) when the store returns hasTrappedWork:true, absent otherwise
- Same tooltip copy, same icon, same warm-amber styling on both surfaces (D-05 consistency)
- Store polls on 60s cadence + window.focus (D-08), always covers dormant identities (Pitfall #5)
- probeIdentityTrappedWork is a byte-shape mirror of countIdentityBounties (D-03)
- Bounty-count wire remains fully live and functional (Plan 03 removes it after this plan is UAT-green)
- 29 new tests pass; 0 pre-existing test regressions; type-check clean
- No new palette values introduced in CSS (D-05 constraint — built against existing --color-pv-* / hsla-35 tokens)
</success_criteria>

<output>
Create `.planning/phases/104-repo-stats-display-trapped-work-indicator-on-conv-list-ident/104-02-SUMMARY.md` when done. Summary must document: (a) exact file:line for each new indicator render site, (b) exact file:line for the store poller mount, (c) all 29 test names + pass status, (d) confirmation that bounty-count wire is fully preserved (Plan 03 responsibility), (e) confirmation that D-05 tooltip copy matches VERBATIM on both surfaces, (f) atomic commit SHA(s).
</output>
