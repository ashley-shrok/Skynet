---
phase: 104-repo-stats-display-trapped-work-indicator-on-conv-list-ident
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/claude-session/identity-artifact-reader.ts
  - src/backend/claude-session/claude-session-server.ts
  - src/backend/claude-session/identity-artifact-reader.trapped-work.test.ts
  - src/backend/claude-session/claude-session-server.trapped-work.test.ts
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-06
  - D-09
tags:
  - ssh
  - identity
  - git-state
  - claude-session-server

must_haves:
  truths:
    - "Given a peer-box identity whose ~/fleet/identities/<name>/workspace/ contains a git repo with a remote + dirty tracked files, readIdentityTrappedWork returns {hasTrappedWork: true} (D-01)"
    - "Given the same repo but with no dirty state and no local-only commits and no stashes, readIdentityTrappedWork returns {hasTrappedWork: false} (D-01)"
    - "Given a repo without any remote configured, readIdentityTrappedWork ignores it (silent-fail) and returns {hasTrappedWork: false} (D-01)"
    - "Given a workspace with an outer .git and an inner nested repo at deeper depth, only the outermost is probed (D-01 outermost-only)"
    - "Given a target identity with no ~/fleet/identities/<name>/workspace/ directory present, readIdentityTrappedWork returns {hasTrappedWork: false} without throwing (D-06 silent-fail for pre-migration identities)"
    - "One identity:probe-trapped-work WS request carrying N targets across K distinct non-local hostIds fans out to exactly K connectOneShot invocations (D-03)"
    - "readIdentityTrappedWork does NOT call resolveRoleForIdentity — the workspace path has no role indirection (D-09)"
  artifacts:
    - path: src/backend/claude-session/identity-artifact-reader.ts
      provides: "readIdentityTrappedWork(conn, identityKey) function with LOCAL + REMOTE branches"
      exports:
        - readIdentityTrappedWork
    - path: src/backend/claude-session/claude-session-server.ts
      provides: "handleIdentityProbeTrappedWork WS handler + identity:probe-trapped-work route + __handleIdentityProbeTrappedWorkForTests seam"
      exports:
        - handleIdentityProbeTrappedWork
        - __handleIdentityProbeTrappedWorkForTests
    - path: src/backend/claude-session/identity-artifact-reader.trapped-work.test.ts
      provides: "Unit tests for readIdentityTrappedWork (LOCAL branch fixture repos + REMOTE branch mocked SSH client)"
      contains: "describe(\"readIdentityTrappedWork\""
    - path: src/backend/claude-session/claude-session-server.trapped-work.test.ts
      provides: "WS handler tests for handleIdentityProbeTrappedWork batching + fan-out"
      contains: "describe(\"handleIdentityProbeTrappedWork\""
  key_links:
    - from: src/backend/claude-session/claude-session-server.ts
      to: src/backend/claude-session/identity-artifact-reader.ts
      via: "import { readIdentityTrappedWork } from './identity-artifact-reader'"
      pattern: "readIdentityTrappedWork"
    - from: src/backend/claude-session/claude-session-server.ts
      to: "WS message router at ~L5714 region"
      via: "if (msg.type === \"identity:probe-trapped-work\") { await handleIdentityProbeTrappedWork(ws, msg, userId); return; }"
      pattern: "identity:probe-trapped-work"
---

<objective>
Add the backend detector + WS wire that produces a per-identity binary "hasTrappedWork" answer, sourced from an SSH-remote python3 walk over `~/fleet/identities/<name>/workspace/` on the peer box. This plan is ADDITIVE ONLY — it introduces the new `readIdentityTrappedWork` function alongside (not replacing) the existing `readIdentityBountyCounts`, and adds a sibling WS handler + route. Deletion of the retiring bounty-counts wire happens in Plan 03 AFTER the new wire is green (blast-radius insurance per D-04-ordering).

Per D-02 (post-research correction), the workspace subdir is hard-coded to `~/fleet/identities/<identityKey>/workspace/` — do NOT hard-code `skynet/`, do NOT introduce an env-var override. Per D-06, absent-or-empty workspace returns `{hasTrappedWork:false}` silently — no error, no fallback path. Per D-09, this reader does NOT call `resolveRoleForIdentity` (workspace-scoped, not role-scoped — saves an SSH round-trip vs blindly mirroring the bounty-counts reader).

Purpose: The frontend indicator (Plan 02) needs a live WS response frame `identity:trapped-work` to display anything. This plan produces that frame. Deploy waits for Shape 3 (Phase 96) to ship — coordinated at ship time, not planning time (D-10).

Output: One new function + one new WS handler + one new WS route + two new test files. Zero deletions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/104-repo-stats-display-trapped-work-indicator-on-conv-list-ident/104-CONTEXT.md
@.planning/phases/104-repo-stats-display-trapped-work-indicator-on-conv-list-ident/104-RESEARCH.md

@src/backend/claude-session/identity-artifact-reader.ts
@src/backend/claude-session/claude-session-server.ts
@src/backend/claude-session/identity-artifact-reader.count-bounties.test.ts
@src/backend/claude-session/claude-session-server.count-bounties.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add readIdentityTrappedWork to identity-artifact-reader.ts with LOCAL+REMOTE branches and full unit test coverage</name>
  <files>src/backend/claude-session/identity-artifact-reader.ts, src/backend/claude-session/identity-artifact-reader.trapped-work.test.ts</files>
  <read_first>
    - src/backend/claude-session/identity-artifact-reader.ts (whole file for pattern, especially L4020-4146 readIdentityBountyCounts, L170-180 IDENTITY_KEY_RE, L336-338 shellEscape, L352-365 execWithTimeout)
    - src/backend/claude-session/identity-artifact-reader.count-bounties.test.ts (whole file — mirror source for test shape, LOCAL fixture pattern via os.mkdtemp, REMOTE mocked-SSH pattern)
    - .planning/phases/104-repo-stats-display-trapped-work-indicator-on-conv-list-ident/104-CONTEXT.md (D-01 eligibility rules, D-02 workspace path, D-06 silent-fail, D-09 skip resolveRoleForIdentity)
    - .planning/phases/104-repo-stats-display-trapped-work-indicator-on-conv-list-ident/104-RESEARCH.md (RESOLUTION section for workspace path, Pattern 1 skeleton, Pattern 2 exact git commands, Pitfall #3 shell-quoting, Pitfall #6 skip resolveRoleForIdentity)
  </read_first>
  <behavior>
    - Test 1 (LOCAL, workspace absent): identityKey with no ~/fleet/identities/<key>/workspace/ dir returns {hasTrappedWork: false} without throwing. Uses os.mkdtemp fixture with no workspace subdir.
    - Test 2 (LOCAL, workspace present but empty): workspace exists with no .git dirs anywhere → {hasTrappedWork: false}.
    - Test 3 (LOCAL, one repo clean with remote): workspace/foo/.git initialized + `git remote add origin` + committed clean tree with commits pushed to a bare local remote → {hasTrappedWork: false}.
    - Test 4 (LOCAL, one repo dirty with remote): same repo + modify a tracked file → {hasTrappedWork: true}.
    - Test 5 (LOCAL, one repo local-only commits with remote): same repo + new commit not on any remote branch → {hasTrappedWork: true}.
    - Test 6 (LOCAL, one repo with stash): same repo + `git stash` a change → {hasTrappedWork: true}.
    - Test 7 (LOCAL, repo without remote — SILENT-FAIL): workspace/bar/.git with a dirty tracked file BUT no remote configured → {hasTrappedWork: false} (D-01 ignore rule).
    - Test 8 (LOCAL, untracked files only in an eligible repo): workspace/baz/.git with a remote + only untracked files → {hasTrappedWork: false} (D-01 ignore rule — untracked = loose scratch).
    - Test 9 (LOCAL, outermost-only rule): workspace/outer/.git eligible+clean AND workspace/outer/vendored/.git eligible+dirty. Nested repo is ignored → {hasTrappedWork: false}.
    - Test 10 (LOCAL, depth-limit boundary): workspace/a/b/c/.git at depth 3 (max) IS found (dirty → true); workspace/a/b/c/d/.git at depth 4 is NOT found.
    - Test 11 (LOCAL, malformed .git swallowed): workspace/broken/ where .git is a file with garbage → does NOT throw; the broken entry is skipped and any sibling eligible+dirty repo still surfaces true.
    - Test 12 (LOCAL, path-traversal identityKey rejected): identityKey like "../etc" throws "invalid identityKey" via IDENTITY_KEY_RE gate.
    - Test 13 (LOCAL, D-09 no resolveRoleForIdentity call): assert that the LOCAL branch does NOT read the identity's frontmatter file (spy on fs.readFile — expect it is never called on the identity .md path).
    - Test 14 (REMOTE, happy path — mocked SSH conn): conn.exec is called exactly once with a command containing "python3 -c" and the string `"$HOME/fleet/identities/tina/workspace"`; mocked stdout of `{"hasTrappedWork":true}` → function returns {hasTrappedWork: true}.
    - Test 15 (REMOTE, malformed payload): mocked stdout of `not-json` throws `remote trapped-work returned malformed payload: not-json`.
    - Test 16 (REMOTE, malformed shape): mocked stdout of `{"other":1}` throws `remote trapped-work returned malformed payload: ...`.
    - Test 17 (REMOTE, D-09 no resolveRoleForIdentity): mocked conn.exec is called exactly ONCE (not twice — no role-file read round-trip).
  </behavior>
  <action>
    Add a new async function `readIdentityTrappedWork(conn: SSHClientType | null, identityKey: string): Promise<{ hasTrappedWork: boolean }>` immediately after `readIdentityBountyCounts` at the end of identity-artifact-reader.ts (~L4146). Per D-02, hard-code `const WORKSPACE_PATH_SUFFIX = "fleet/identities"` and construct the workspace path as `~/${WORKSPACE_PATH_SUFFIX}/${identityKey}/workspace`. Do NOT read from an env var. Do NOT hard-code `skynet/`. Do NOT introduce `IDENTITY_WORKSPACE_SUBDIR`.

    Per D-09, DO NOT call `resolveRoleForIdentity` — workspace path has no role indirection (Pitfall #6 in RESEARCH.md). Save the SSH round-trip.

    Structure:
    1. Validate `identityKey` against `IDENTITY_KEY_RE`; throw `"invalid identityKey"` on mismatch (mirrors `readIdentityBountyCounts` L4058).
    2. `const MAX_DEPTH = 3;` module-scoped or function-scoped constant.
    3. LOCAL branch (`conn === null`): use Node `fs/promises` + `path` to walk `path.join(homedir(), WORKSPACE_PATH_SUFFIX, identityKey, "workspace")`. Import `os.homedir` if not already available (identity-artifact-reader uses `path.join` extensively — grep for the existing homedir source before adding new imports). Return `{hasTrappedWork: false}` on ENOENT. Walk to MAX_DEPTH, apply outermost-only rule (don't descend past a found .git dir), for each found repo run: `git remote` via `spawnSync("git", ["-C", repo, "remote"])` — if empty stdout, skip; then `git status --porcelain=v1` — if any line's second char is NOT `?`, return true; then `git rev-list --branches --not --remotes --count` — if int > 0, return true; then `git stash list` — if any output, return true. Return false if no repo triggered.
    4. REMOTE branch: build a python3 script per RESEARCH.md Pattern 2 (full sketch at L337-380). Compose command as `python3 -c ${shellEscape(script)} "$HOME/fleet/identities/${identityKey}/workspace"` (mirrors L4122-4124 double-quote-around-$HOME pattern). Call `execWithTimeout(conn, cmd)` (existing helper at L352-365 — 15s timeout per patch 260902-3ll). Parse stdout as JSON; validate shape (`typeof parsed === "object" && parsed !== null && typeof parsed.hasTrappedWork === "boolean"`); throw `remote trapped-work returned malformed payload: ${stdout}` on any parse or shape failure.
    5. The python3 script MUST: (a) walk to MAX_DEPTH=3, (b) skip descent past found .git dirs (outermost-only), (c) skip symlinks (`os.path.islink` guard), (d) 5s per-subprocess timeout on each `git` shell-out, (e) per-repo `git remote` short-circuit BEFORE running status/stash/rev-list, (f) short-circuit `print + sys.exit(0)` on the first trapped-state finding, (g) `try/except` swallow all per-file/per-subprocess errors (poisoned repo must not fail the whole answer — matches L4096 pattern), (h) return `{"hasTrappedWork":false}` if root missing (FileNotFoundError branch), (i) print single JSON line and exit.
    6. Local branch test fixtures: use `os.mkdtemp` + real `git init` + `git config user.email/user.name` + real `git commit` + a bare local repo for the "remote" so `git remote add origin file:///…` works and `git push` populates remote-tracking refs (mirrors the pattern `identity-artifact-reader.count-bounties.test.ts` uses for LOCAL tmpdir fixtures at L60-80). To override the homedir lookup for LOCAL branch tests, either (a) monkey-patch `os.homedir()` via `vi.spyOn(os, "homedir")` OR (b) accept an optional `_localRootForTest?: string` third parameter to the function purely for testability (mirror whatever pattern `readIdentityBountyCounts` uses for its LOCAL tests — read `count-bounties.test.ts` to identify the existing pattern first; do NOT invent a new one).
    7. Add JSDoc comment block at function head citing D-01 eligibility rules, D-02 workspace path, D-06 silent-fail, D-09 skip-role-lookup. Reference the mirror source `readIdentityBountyCounts` and note the intentional divergence (no role lookup, workspace-scoped path).

    Write the test file `identity-artifact-reader.trapped-work.test.ts` following the exact test-shape of `identity-artifact-reader.count-bounties.test.ts` — same imports, same describe/it structure, same fixture-tmpdir cleanup in afterEach. Cover all 17 tests listed under `<behavior>`. Use `vitest`'s `vi.mock` for the REMOTE branch's ssh2 conn mock (mirror `count-bounties.test.ts` REMOTE mock pattern).

    Do NOT delete `readIdentityBountyCounts` — that happens in Plan 03. Both functions coexist during this plan.
  </action>
  <verify>
    <automated>npx vitest run src/backend/claude-session/identity-artifact-reader.trapped-work.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Grep asserts: `grep -c "^export async function readIdentityTrappedWork" src/backend/claude-session/identity-artifact-reader.ts` returns 1
    - Grep asserts: `grep -c "resolveRoleForIdentity" src/backend/claude-session/identity-artifact-reader.ts | grep -v '^#'` matches its pre-existing count exactly (no new call added inside the new function body — filter out comments/JSDoc). Verify by isolating the new function body via awk/sed range and grepping THAT body for `resolveRoleForIdentity` — expect 0 occurrences.
    - Grep asserts: `grep -c 'fleet/identities' src/backend/claude-session/identity-artifact-reader.ts` returns at least 1 (new function uses this path)
    - Grep asserts: `grep -c 'skynet"' src/backend/claude-session/identity-artifact-reader.ts` matches pre-existing count (no new `"skynet"` string introduced — CONTEXT.md D-02 forbids this)
    - Grep asserts: `grep -c 'IDENTITY_WORKSPACE_SUBDIR' src/backend/claude-session/identity-artifact-reader.ts` returns 0 (no env-var override per D-02)
    - Grep asserts: `grep -c 'const MAX_DEPTH' src/backend/claude-session/identity-artifact-reader.ts` returns at least 1
    - Test command: `npx vitest run src/backend/claude-session/identity-artifact-reader.trapped-work.test.ts` exits 0 with all 17 tests passing
    - Test command: `npx vitest run src/backend/claude-session/identity-artifact-reader.count-bounties.test.ts` still exits 0 (bounty-counts unaffected)
    - CLI: `npm run type-check` exits 0
    - Behavior assertion: In Test 14 (REMOTE happy path), the command string passed to the mocked SSH conn contains the exact substring `"$HOME/fleet/identities/tina/workspace"` (verify via mock call inspection)
    - Behavior assertion: In Test 17, `conn.exec` mock is called exactly 1 time (no role-file round-trip)
  </acceptance_criteria>
  <done>
    New function readIdentityTrappedWork exists with LOCAL + REMOTE branches; 17 unit tests all pass; existing readIdentityBountyCounts + its tests remain untouched and passing; type-check clean; the function's SSH command uses the hard-coded `~/fleet/identities/<key>/workspace` path with no env-var override and no `skynet/` string.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire handleIdentityProbeTrappedWork WS handler + identity:probe-trapped-work route into claude-session-server.ts with fan-out tests</name>
  <files>src/backend/claude-session/claude-session-server.ts, src/backend/claude-session/claude-session-server.trapped-work.test.ts</files>
  <read_first>
    - src/backend/claude-session/claude-session-server.ts (whole handleIdentityCountBounties block L1159-1347 for byte-shape mirror; L5700-5717 WS router route pattern; L65-80 imports region)
    - src/backend/claude-session/claude-session-server.count-bounties.test.ts (whole file for test-shape mirror — batching, fan-out, per-target error isolation, dead-host handling)
    - src/backend/claude-session/identity-artifact-reader.ts (readIdentityTrappedWork just added in Task 1 — for import at L73 region)
    - .planning/phases/104-repo-stats-display-trapped-work-indicator-on-conv-list-ident/104-CONTEXT.md (D-03 wire path mirror)
    - .planning/phases/104-repo-stats-display-trapped-work-indicator-on-conv-list-ident/104-RESEARCH.md (Pattern 1 fan-out, Common Operation 1 handler skeleton)
  </read_first>
  <behavior>
    - Test 1 (empty targets): calling handler with `{targets: []}` sends one `{type: "identity:trapped-work", results: []}` frame and returns.
    - Test 2 (local-only batch, no SSH): 3 targets all with hostId=null → 0 connectOneShot invocations; response `results` has 3 entries each with `hasTrappedWork` boolean (from mocked readIdentityTrappedWork(null, key)).
    - Test 3 (batching — same hostId group): 5 targets same hostId=42 → EXACTLY 1 connectOneShot invocation; 5 readIdentityTrappedWork calls sharing the same conn; response has 5 entries.
    - Test 4 (batching — multiple hostId groups): 2 targets hostId=42 + 3 targets hostId=99 + 1 target hostId=null → EXACTLY 2 connectOneShot invocations (one per non-local hostId, local group skips connect); response has 6 entries.
    - Test 5 (dead host — all targets get error field): resolveHostById throws → all bucket targets return `{identityKey, hostId, hasTrappedWork: false, error: <string>}`. Response frame still emitted with all entries.
    - Test 6 (per-target error isolation via Promise.allSettled): 3 targets same hostId; readIdentityTrappedWork throws for the middle one → response has 3 entries where entry 0 and 2 have `hasTrappedWork` from the resolved value and entry 1 has `error: <string>` + `hasTrappedWork: false`.
    - Test 7 (WS router route wire): sending `{type: "identity:probe-trapped-work", targets: [...]}` on the WS router at ~L5714 region invokes handleIdentityProbeTrappedWork (assert via the exported handler being called through the router path).
    - Test 8 (response type tag): response frame's `type` field is EXACTLY the string `"identity:trapped-work"` (matches Plan 02's frontend api expectation).
  </behavior>
  <action>
    Add `readIdentityTrappedWork` to the existing import block from `./identity-artifact-reader` (~L65-80 region — the block that already imports `readIdentityBountyCounts` at L73). Insert alphabetically-nearby or immediately after; do NOT remove `readIdentityBountyCounts` (Plan 03 handles that).

    Add a new type block + handler + test seam immediately after `__handleIdentityCountBountiesForTests` at L1347, before the Phase 22 SRIC-06 block at L1349:

    - `type TrappedWorkTarget = { identityKey: string; hostId: number | null };`
    - `type TrappedWorkResult = { identityKey: string; hostId: number | null; hasTrappedWork: boolean; error?: string };`
    - Private helper `readOneTrappedWorkTarget(conn, identityKey)` → `readIdentityTrappedWork(conn, identityKey)` (mirrors `readOneTarget` at L1186-1193).
    - `export async function handleIdentityProbeTrappedWork(ws, msg, userId)` — byte-shape mirror of `handleIdentityCountBounties` L1195-1342, differing ONLY in: (a) response type tag `"identity:trapped-work"` (not `"identity:bounty-counts"`), (b) `results` field name (not `counts`), (c) per-result shape carries `hasTrappedWork: boolean` (not `pinnedCount/needsDeskCount`), (d) delegates to `readIdentityTrappedWork` (not `readIdentityBountyCounts`), (e) error-case shape uses `hasTrappedWork: false` (matches Plan 02 frontend expectation).
    - `export const __handleIdentityProbeTrappedWorkForTests = handleIdentityProbeTrappedWork;` — test seam mirroring L1347.

    Add the WS router route in the same location pattern as L5714-5717 (immediately after the `identity:count-bounties` route or in a nearby sibling block):
    - `if (msg.type === "identity:probe-trapped-work") { await handleIdentityProbeTrappedWork(ws, msg, userId); return; }`

    Update the top-of-file WS wire documentation comment block (the one that lists inbound + outbound message types starting at L115) to add the two new message-type entries: `identity:probe-trapped-work` (client→server) and `identity:trapped-work` (server→client). Follow the existing pattern verbatim.

    Write the test file `claude-session-server.trapped-work.test.ts` following the exact shape of `claude-session-server.count-bounties.test.ts` — same imports, same mocking strategy (`vi.mock('./identity-artifact-reader')`, mock `resolveHostById`, mock `connectOneShot`), same describe/it structure. Cover all 8 tests. Use `__handleIdentityProbeTrappedWorkForTests` as the drive point (mirrors `__handleIdentityCountBountiesForTests` usage in the count-bounties test).

    Do NOT delete anything in this task. Do NOT modify existing handleIdentityCountBounties or its route.
  </action>
  <verify>
    <automated>npx vitest run src/backend/claude-session/claude-session-server.trapped-work.test.ts src/backend/claude-session/claude-session-server.count-bounties.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Grep asserts: `grep -c "^export async function handleIdentityProbeTrappedWork" src/backend/claude-session/claude-session-server.ts` returns 1
    - Grep asserts: `grep -c "__handleIdentityProbeTrappedWorkForTests" src/backend/claude-session/claude-session-server.ts` returns at least 1
    - Grep asserts: `grep -c "identity:probe-trapped-work" src/backend/claude-session/claude-session-server.ts` returns at least 2 (route match + doc comment)
    - Grep asserts: `grep -c "identity:trapped-work" src/backend/claude-session/claude-session-server.ts` returns at least 2 (response send + doc comment)
    - Grep asserts: `grep -c "readIdentityTrappedWork" src/backend/claude-session/claude-session-server.ts` returns at least 2 (import + call)
    - Grep asserts: `grep -c "handleIdentityCountBounties" src/backend/claude-session/claude-session-server.ts` returns its pre-existing count (delete pass in Plan 03; unchanged here)
    - Test command: `npx vitest run src/backend/claude-session/claude-session-server.trapped-work.test.ts` exits 0 with all 8 tests passing
    - Test command: `npx vitest run src/backend/claude-session/claude-session-server.count-bounties.test.ts` still exits 0
    - CLI: `npm run type-check` exits 0
    - Behavior assertion (Test 3): mocked `connectOneShot` invocation count is EXACTLY 1 for 5 same-hostId targets
    - Behavior assertion (Test 8): response frame `type` field equals literal `"identity:trapped-work"` (not `"identity:bounty-counts"` or any variant)
  </acceptance_criteria>
  <done>
    WS handler + route + test seam + doc-comment updates all landed; 8 handler tests pass; count-bounties tests still pass; type-check clean; the new WS message type strings `identity:probe-trapped-work` and `identity:trapped-work` are wired end-to-end from router to handler to response frame.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS client → Skynet backend | Frontend sends `identity:probe-trapped-work` frames; untrusted `targets` array must be validated (existing pattern — object-shape filter + IDENTITY_KEY_RE validation inside reader). |
| Skynet backend → peer box via SSH | `python3 -c <script> "$HOME/fleet/identities/<identityKey>/workspace"` — identityKey validated by IDENTITY_KEY_RE upstream, direct interpolation inside double-quotes is the established pattern (see L4102-4124 for the mirror). |
| Peer box `python3` script → local `git` subprocesses | Untrusted repo contents on peer box; `subprocess.run(..., timeout=5)` per-command timeout + `try/except` swallow guards against hostile repo state (submodule loops, huge stashes, malformed refs). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-01 | Tampering | identityKey shell injection into `"$HOME/fleet/identities/${identityKey}/workspace"` | mitigate | Validate identityKey via `IDENTITY_KEY_RE = /^[a-z0-9_-]{1,64}$/` at function entry (mirrors readIdentityBountyCounts L4058); regex forbids `$`, `"`, `'`, `;`, `` ` ``, spaces, `..` — safe for direct interpolation inside double-quotes per Pitfall #3 in RESEARCH.md and the L4102-4124 pattern comment. |
| T-104-02 | Denial of Service | Poisoned repo (symlink loop, huge stash file, hung `git` call) blocks the whole probe | mitigate | (a) 5s per-subprocess `timeout=5` on every `subprocess.run` inside the python3 script; (b) outer `execWithTimeout` 15s cap on the whole SSH exec; (c) `os.path.islink` guard skips symlinks during walk; (d) MAX_DEPTH=3 bound on tree traversal. |
| T-104-03 | Denial of Service | Malformed `.git` (garbage file where dir expected) throws unhandled and fails the whole probe | mitigate | Wrap each `git` subprocess call in `try/except Exception: pass` — per-repo error must NOT fail the whole answer (mirrors readIdentityBountyCounts L4096 pattern). LOCAL-branch tests cover Test 11 for this case. |
| T-104-04 | Information Disclosure | Remote stdout might leak repo names/paths through error messages | accept | Error messages only expose the identityKey (already known to the caller) and a generic "malformed payload: <stdout>" — the stdout is bounded to a single JSON line by the script's `print+sys.exit(0)` control flow, and the caller cannot inject arbitrary text into stdout. Bounded blast radius. |
| T-104-05 | Denial of Service | SSH connection exhaustion — batched fan-out opens one connectOneShot per hostId group; malicious frontend sending 100 unique hostIds could saturate | mitigate | Existing per-host semaphore (Phase 101 shipped, `getHostSemaphore(hostId)` — cap 8 slots per hostId) applies transitively through connectOneShot + execWithTimeout. New handler adds no new SSH producer surface beyond what handleIdentityCountBounties already establishes; no additional mitigation required. |
| T-104-SC | Tampering | Package installs (npm/pip/cargo) | n/a | No package installs in this plan. Every dependency (`ssh2`, `ws`, `vitest`) is already in package.json (per RESEARCH.md Package Legitimacy Audit — no new packages introduced). |
</threat_model>

<verification>
- All Plan 01 tests (readIdentityTrappedWork 17 tests + handleIdentityProbeTrappedWork 8 tests = 25 total) pass green
- Pre-existing bounty-counts tests all still pass (regression check — this plan is purely additive)
- `npm run type-check` exits 0
- Grep: no new usage of `resolveRoleForIdentity` inside the readIdentityTrappedWork function body (D-09 compliance)
- Grep: no new hard-coded `"skynet"` string, no `IDENTITY_WORKSPACE_SUBDIR` env-var reference (D-02 compliance)
- Grep: exactly one hard-coded reference to the workspace path prefix `fleet/identities` in the new function (D-02 compliance)
</verification>

<success_criteria>
- `readIdentityTrappedWork(conn, identityKey)` exists in identity-artifact-reader.ts and returns `{hasTrappedWork: boolean}` per D-01 eligibility rules and D-06 silent-fail semantics
- `handleIdentityProbeTrappedWork` WS handler exists in claude-session-server.ts with byte-shape-mirror-of-handleIdentityCountBounties structure (batched fan-out per hostId, Promise.allSettled per-target error isolation, one connectOneShot per non-local hostId group)
- `identity:probe-trapped-work` WS route wired at the router
- `identity:trapped-work` response frame documented in top-of-file wire comment
- 25 new tests all pass; all pre-existing bounty-counts tests still pass; type-check clean
- Zero deletions in this plan (Plan 03 owns the delete pass)
</success_criteria>

<output>
Create `.planning/phases/104-repo-stats-display-trapped-work-indicator-on-conv-list-ident/104-01-SUMMARY.md` when done. Summary must document: (a) exact `readIdentityTrappedWork` signature and file:line location, (b) exact WS handler + route file:line locations, (c) all 25 test names + pass status, (d) confirmation that no bounty-counts code was deleted (Plan 03 responsibility), (e) confirmation that D-09 (no resolveRoleForIdentity call) and D-02 (hard-coded fleet/identities path) held, (f) atomic commit SHA.
</output>
