---
phase: 107-hide-identity-rows-via-disk-sentinel
plan: 03
type: execute
wave: 3
depends_on:
  - 107-02
files_modified:
  - src/backend/database/db/index.ts
  - src/backend/database/db/schema.ts
  - src/backend/database/db/index.migration.test.ts
autonomous: true
requirements:
  - D-02
  - SC-4
must_haves:
  truths:
    - "After boot post-deploy, `PRAGMA table_info(user_preferences)` returns no row named `hidden_conversation_ids`. The physical column is gone (SC-4, D-02)."
    - "Boot on a fresh install (no legacy column, e.g. after the addColumnIfNotExists line for hidden_conversation_ids is deleted) is a silent no-op — migration is idempotent."
    - "The column drop is persisted via `DatabaseSaveTrigger.forceSave('phase-107-hidden-sentinel-migration')` — reason tag uses Phase 107's actual number so the migration survives container restart before any unrelated write fires the debounced save."
    - "The pinned_conversation_ids column stays dropped as it was after Phase 92 Plan 03 — no re-introduction, no restore. The migration adds a second run*Drop function alongside runPinColumnDrop; both drops run at boot."
    - "Nothing in src/ still reads or writes hidden_conversation_ids (verified by grep — Plan 02 already retired those consumers)."
    - "Sibling columns on user_preferences (theme, font_size, accent_color, language, reopen_tabs_on_login, user_id, updated_at) are UNTOUCHED — regression trap P107-04 asserts each column's VALUE survives the drop verbatim."
    - "The `runHiddenColumnDrop` function is a byte-mirror of `runPinColumnDrop` at db/index.ts L910-913 — same assertSqliteSupportsDropColumn preflight, same dropColumnIfExists call, only the column name differs. This mirror discipline makes future migrations trivial to grep and audit."
    - "runHiddenColumnDrop runs BEFORE the addColumnIfNotExists sweep in migrateSchema — same ordering rationale as runPinColumnDrop at db/index.ts L978-987 (drops before adds so a stale install cannot briefly re-add the column in the same boot cycle)."
    - "Preflight throw is fatal — mirrors Phase 92 Plan 03 pattern (T-66-04-04 'boot aborts before schema corruption'). forceSave failure is non-fatal warn — dropColumnIfExists is idempotent, next boot retries."
  artifacts:
    - path: "src/backend/database/db/index.ts"
      provides: "runHiddenColumnDrop() export + migrateSchema() invocation + labeled forceSave `phase-107-hidden-sentinel-migration`; addColumnIfNotExists('user_preferences', 'hidden_conversation_ids', 'TEXT') line at L993 DELETED"
      contains: "runHiddenColumnDrop\\|phase-107-hidden-sentinel-migration"
    - path: "src/backend/database/db/schema.ts"
      provides: "hiddenConversationIds field DELETED from userPreferences drizzle mirror at L850 (pinnedConversationIds already deleted by Phase 92 Plan 03)"
    - path: "src/backend/database/db/index.migration.test.ts"
      provides: "Two new tests: P107-01 (OLD schema with hidden_conversation_ids column → runHiddenColumnDrop → column absent + sibling VALUES preserved + post-drop SELECT throws) + P107-02 (NEW schema without the column → idempotent no-op)"
  key_links:
    - from: "migrateSchema() in db/index.ts"
      to: "runHiddenColumnDrop(sqlite)"
      via: "runs AFTER runPinColumnDrop preflight try/catch AND BEFORE the addColumnIfNotExists sweep — same ordering rationale as the pin drop"
      pattern: "runHiddenColumnDrop\\(sqlite\\)"
    - from: "runHiddenColumnDrop"
      to: "dropColumnIfExists(sqliteDb, 'user_preferences', 'hidden_conversation_ids')"
      via: "shared idempotent probe-then-drop helper (Phase 66 pattern, mirror of Phase 92 Plan 03 runPinColumnDrop)"
      pattern: "dropColumnIfExists.*user_preferences.*hidden_conversation_ids"
---

<objective>
Drop the `hidden_conversation_ids` column from `user_preferences` per D-02 — no dormant column left behind, mirroring the Phase 92 Plan 03 shape byte-for-byte for the hidden slice. Plan 02 already severed every read/write in src/, so this drop is safe: nothing consumes the column post-Plan-02, and this migration removes the dead storage cleanly.

Purpose: Close the two-sources-of-truth window at the schema level for the hidden slice, EXACTLY as Phase 92 Plan 03 closed it for the pin slice. The CONTEXT.md § Schema migration section directly cites the reason tag literal `phase-107-hidden-sentinel-migration` — this plan is that literal's implementation site.

Output: `runHiddenColumnDrop()` exported from db/index.ts (byte-mirror of `runPinColumnDrop` at L910-913), migrateSchema() invocation with labeled forceSave `phase-107-hidden-sentinel-migration`, schema.ts drizzle mirror updated (hiddenConversationIds field removed), addColumnIfNotExists sweep line for `hidden_conversation_ids` at L993 deleted, migration test with the mandatory Test 1 (OLD schema drop) + Test 2 (NEW schema idempotent) pair.

Ordering note: the pin+hidden fanout in Plan 02 restructured the PUT handler to run pin fanout first, then hidden fanout, in a shared try/finally. That is IN THE HANDLER, not in the boot migration. The migration ORDER at boot is: (1) runIdentitiesCosmeticDrops (Phase 66); (2) runIdentitiesTableDrop + forceSave phase-68 (Phase 68); (3) runPinColumnDrop preflight (Phase 92); (4) runHiddenColumnDrop preflight (Phase 107 — THIS PLAN); (5) addColumnIfNotExists sweep (theme, font_size, accent_color, language — WITHOUT hidden_conversation_ids anymore); (6) labeled forceSave `phase-107-hidden-sentinel-migration` (batches both the pin drop AND the hidden drop AND the sweep in one atomic file write). This preserves the "drops-before-adds" discipline from Phase 66/92 and keeps the shape uniform.

Deploy race consideration: The migration ordering vs the frontend rewire (Plan 04) has a subtle window. If deploy sequence is (a) new code goes live, (b) OLD tabs still open with pre-Plan-04 frontend still sending `hiddenConversationIds` via PUT /user-preferences without identityHosts, (c) backend hidden fanout rejects with 400 "identityHosts required" (Plan 02 HID-107-PUT-07d). This is a boot-window problem the CONTEXT.md acknowledges implicitly by putting schema drop (Plan 03) BEFORE frontend rewire (Plan 04) in the wave order — meaning: after this plan ships and before Plan 04 ships, ANY frontend that still calls putHiddenIds without identityHosts will 400. But per the CONTEXT.md § Constraints "wave dependencies" line, that's acceptable — Alice is a single user; the deploy motion is quick; a mid-deploy tab-open will surface as a 400, not data loss. The DB column is gone; the fanout is live; the frontend fixes on the next remount.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/shapes/shape-hidden-identity-rows-via-sentinel.md
@.planning/phases/107-hide-identity-rows-via-disk-sentinel-mirror-phase-92-for-the/107-CONTEXT.md
@.planning/phases/105-pin-sentinel-migration-move-identity-pin-state-from-skynet-d/105-03-SUMMARY.md

@src/backend/database/db/index.ts
@src/backend/database/db/schema.ts
@src/backend/database/db/index.migration.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add runHiddenColumnDrop + wire into migrateSchema with labeled forceSave `phase-107-hidden-sentinel-migration`; delete drizzle mirror field + addColumnIfNotExists sweep line; write migration test P107-01 + P107-02</name>
  <files>src/backend/database/db/index.ts, src/backend/database/db/schema.ts, src/backend/database/db/index.migration.test.ts</files>
  <read_first>
    - src/backend/database/db/index.ts (runPinColumnDrop at L892-913 — the exact template to mirror; migrateSchema at L915+ including the runPinColumnDrop invocation at L978-987 with the preflight try/catch; the addColumnIfNotExists sweep at L989-993 — line L993 `addColumnIfNotExists("user_preferences", "hidden_conversation_ids", "TEXT")` is DELETED here; the phase-92 forceSave at L1006-1017 — the Phase-107 forceSave replaces this label OR ships alongside; assertSqliteSupportsDropColumn helper referenced from Phase 66 — no re-invention needed)
    - src/backend/database/db/schema.ts L850 (`hiddenConversationIds: text("hidden_conversation_ids"),` — deleted here; pinnedConversationIds already deleted by Phase 92 Plan 03; surrounding fields untouched)
    - src/backend/database/db/index.migration.test.ts (the Phase 92 P92-01 + P92-02 tests appended at the end — the exact structural template for P107-01 + P107-02; the OLD_USER_PREFERENCES_CREATE_SQL const from Phase 92 Plan 03 — this task adjusts / adds a new OLD_USER_PREFERENCES_WITH_HIDDEN_CREATE_SQL to include hidden_conversation_ids AND excludes pinned_conversation_ids since the pin column is already dropped by Phase 92)
    - .planning/phases/105-pin-sentinel-migration-move-identity-pin-state-from-skynet-d/105-03-SUMMARY.md (§ Accomplishments — runPinColumnDrop shape + forceSave label + preflight throw pattern; Test P92-01 + P92-02 structure — mirror byte-for-byte)
  </read_first>
  <behavior>
    - Test P107-01 (OLD schema with hidden_conversation_ids → runHiddenColumnDrop → column absent + sibling values preserved + post-drop SELECT throws): seed an in-memory sqlite with `OLD_USER_PREFERENCES_WITH_HIDDEN_CREATE_SQL` (schema has hidden_conversation_ids but NOT pinned_conversation_ids — matches post-Phase-92 pre-Phase-107 shape). Insert one row with real values: `theme="dark", hidden_conversation_ids='[\"fleet::1::alice\",\"fleet::2::bob\"]', language="en", updated_at="2026-01-01T00:00:00.000Z"`. Sanity: preCols contains hidden_conversation_ids. Run `runHiddenColumnDrop(db)`. Assert: postCols does NOT contain hidden_conversation_ids; DOES contain theme, language, updated_at, reopen_tabs_on_login, user_id, font_size, accent_color; `db.prepare("SELECT theme, language FROM user_preferences WHERE user_id = ?").get("u1")` returns theme === "dark" AND language === "en" byte-for-byte; `expect(() => db.prepare("SELECT hidden_conversation_ids FROM user_preferences").get()).toThrow()`.
    - Test P107-02 (NEW schema without hidden_conversation_ids → idempotent no-op): seed with `NEW_USER_PREFERENCES_CREATE_SQL` (from Phase 92 Plan 03 — the shape WITHOUT hidden_conversation_ids either, since post-Phase-107 both columns are gone). Actually — the Phase 92 Plan 03 NEW schema INCLUDES hidden_conversation_ids; that's the "already dropped pin, still has hidden" shape. For Phase 107 the "new" schema is the "both dropped" shape. Declare a new `NEW_USER_PREFERENCES_POST_HIDDEN_DROP_CREATE_SQL` const above the Phase 107 describe block:

        const NEW_USER_PREFERENCES_POST_HIDDEN_DROP_CREATE_SQL = `
          CREATE TABLE user_preferences (
            user_id TEXT PRIMARY KEY,
            reopen_tabs_on_login INTEGER NOT NULL DEFAULT 0,
            theme TEXT,
            font_size TEXT,
            accent_color TEXT,
            language TEXT,
            updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
          );
        `;

      Sanity: columnNames does NOT contain hidden_conversation_ids OR pinned_conversation_ids. Run `runHiddenColumnDrop(db)`. Assert: not-throws; PRAGMA shape unchanged; every sibling column still present.
    - Test P107-03 (VALUE preservation regression trap — inline in P107-01): the seeded row's non-hidden VALUES survive the drop byte-for-byte. This is the D-02 sibling-preservation trap: any future refactor that accidentally batches both column drops together with a broader ALTER (or drops the wrong column) fails this test loudly.
    - Test P107-04 (post-drop SELECT throws — inline in P107-01): a raw `SELECT hidden_conversation_ids FROM user_preferences` throws — proves the column is physically absent, not just hidden from PRAGMA output.
  </behavior>
  <action>
    In src/backend/database/db/index.ts:

    Add a new exported function `runHiddenColumnDrop(sqliteDb)` — byte-mirror of `runPinColumnDrop` at L910-913. Place it IMMEDIATELY BELOW runPinColumnDrop in source order (chronological migration ordering — reader can grep for `run.*Drop` and see the migrations in phase-number sequence). Concrete shape:

      /**
       * Phase 107 D-02 — drop the `hidden_conversation_ids` column from
       * `user_preferences`. Hide state lives on disk as `~/fleet/identities/
       * <name>/.hidden` post-Phase-107 (matches `.no-dormancy` / `.recycle-
       * requested` / `.pinned` presence-is-meaning convention). Plan 02
       * already retired every reader/writer of this column in src/ — this
       * drop retires the dead storage.
       *
       * Idempotent via dropColumnIfExists (probes SELECT; ALTER TABLE DROP
       * COLUMN on hit; silent no-op on miss). Byte-mirror of runPinColumnDrop
       * at L910-913 — the only difference is the column name.
       *
       * Exported so index.migration.test.ts can exercise Test P107-01 (OLD
       * schema drop) + Test P107-02 (NEW schema idempotent no-op) against
       * test-owned in-memory databases — parallel to runPinColumnDrop /
       * runIdentitiesCosmeticDrops export pattern.
       */
      export function runHiddenColumnDrop(sqliteDb: Database.Database): void {
        assertSqliteSupportsDropColumn(sqliteDb);
        dropColumnIfExists(sqliteDb, "user_preferences", "hidden_conversation_ids");
      }

    In migrateSchema at L915+, add a `runHiddenColumnDrop(sqlite)` invocation AFTER the existing `runPinColumnDrop` preflight try/catch at L978-987 AND BEFORE the `addColumnIfNotExists` sweep at L989-993. Wrap in the same preflight try/catch pattern (mirrors L978-987 verbatim):

      try {
        runHiddenColumnDrop(sqlite);
      } catch (preflightErr) {
        databaseLogger.error(
          "Phase 107 hidden-column drop preflight failed",
          preflightErr,
          { operation: "schema_migration_preflight" },
        );
        throw preflightErr;
      }

    At L993, DELETE the line `addColumnIfNotExists("user_preferences", "hidden_conversation_ids", "TEXT");` — fresh installs no longer create the column (D-02 "no dormant column"). Keep the L989-L992 addColumnIfNotExists lines for theme / font_size / accent_color / language UNTOUCHED.

    At L1006-1017, UPDATE the forceSave label from `"phase-92-pin-sentinel-migration"` → `"phase-107-hidden-sentinel-migration"`. The single labeled forceSave AFTER the addColumnIfNotExists sweep now covers BOTH the pin drop (still ships every boot — idempotent, silent no-op post-first-boot) AND the hidden drop AND the remaining addColumnIfNotExists sweep in one atomic file write. This is the Phase 92 Plan 03 pattern applied to the next phase: the forceSave label is always the LATEST migration touching this file, batching all prior mutations. Verify the label reads exactly `"phase-107-hidden-sentinel-migration"` per CONTEXT.md § Schema migration.

    Concrete new forceSave block replaces the L1006-1017 block:

      try {
        await DatabaseSaveTrigger.forceSave("phase-107-hidden-sentinel-migration");
      } catch (saveError) {
        databaseLogger.warn(
          "[phase-107] forceSave failed post-drop (non-fatal — dropColumnIfExists is idempotent, next boot retries)",
          {
            operation: "schema_migration_force_save_post_drop",
            reason: "phase-107-hidden-sentinel-migration",
            error: saveError,
          },
        );
      }

    In src/backend/database/db/schema.ts:

    At L850, DELETE the line `hiddenConversationIds: text("hidden_conversation_ids"),`. The pinnedConversationIds line at L838 is already gone (Phase 92 Plan 03). Surrounding fields (user_id, reopen_tabs_on_login, theme, font_size, accent_color, language, updated_at) are UNTOUCHED. Post-edit, the userPreferences sqliteTable has no conversation-ids fields at all — both slices migrated to disk sentinels.

    Verify with grep BEFORE running the migration test: `grep -c 'hiddenConversationIds' src/backend/database/db/schema.ts` returns 0. `grep -c 'pinnedConversationIds' src/backend/database/db/schema.ts` returns 0. If either returns non-zero, Plans 02 or 92 wasn't fully landed and this task should NOT proceed.

    In src/backend/database/db/index.migration.test.ts:

    Update the destructured import at the top of the file to also include `runHiddenColumnDrop`:

      import {
        assertSqliteSupportsDropColumn,
        dropColumnIfExists,
        runIdentitiesCosmeticDrops,
        runIdentitiesTableDrop,
        runPinColumnDrop,
        runHiddenColumnDrop,
      } from "./index.js";

    Above the existing `Phase 92 migration — drop pinned_conversation_ids` describe block, declare two new SQL const literals (naming disambiguates from the Phase 92 pair):

      const OLD_USER_PREFERENCES_WITH_HIDDEN_CREATE_SQL = `
        CREATE TABLE user_preferences (
          user_id TEXT PRIMARY KEY,
          reopen_tabs_on_login INTEGER NOT NULL DEFAULT 0,
          theme TEXT,
          font_size TEXT,
          accent_color TEXT,
          language TEXT,
          hidden_conversation_ids TEXT,
          updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
        );
      `;

      const NEW_USER_PREFERENCES_POST_HIDDEN_DROP_CREATE_SQL = `
        CREATE TABLE user_preferences (
          user_id TEXT PRIMARY KEY,
          reopen_tabs_on_login INTEGER NOT NULL DEFAULT 0,
          theme TEXT,
          font_size TEXT,
          accent_color TEXT,
          language TEXT,
          updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
        );
      `;

    Append a new describe block AFTER the Phase 92 block: `describe("Phase 107 migration — drop hidden_conversation_ids from user_preferences")`. Inside it, two `it(...)` bodies mirroring the P92-01 + P92-02 pattern verbatim, with the column swapped:

    Test P107-01 body:
      - `const db = new Database(":memory:")`
      - `db.exec(OLD_USER_PREFERENCES_WITH_HIDDEN_CREATE_SQL)`
      - Insert one row: `db.prepare("INSERT INTO user_preferences (user_id, reopen_tabs_on_login, theme, font_size, accent_color, language, hidden_conversation_ids, updated_at) VALUES (?, ?, ?, ?, ?, ?, ?, ?)").run("u1", 0, "dark", null, null, "en", '[\"fleet::1::alice\",\"fleet::2::bob\"]', "2026-01-01T00:00:00.000Z")`
      - Sanity: preCols = columnNames(db, "user_preferences"); expect preCols to contain hidden_conversation_ids.
      - `runHiddenColumnDrop(db)`
      - postCols = columnNames(db, "user_preferences");
      - `expect(postCols).not.toContain("hidden_conversation_ids")`
      - `expect(postCols).toContain("theme")` — sibling preserved
      - `expect(postCols).toContain("language")` — sibling preserved
      - Read the seeded row: `const row = db.prepare("SELECT theme, language FROM user_preferences WHERE user_id = ?").get("u1"); expect(row.theme).toBe("dark"); expect(row.language).toBe("en")` — inline P107-03 sibling-VALUE-preserved assertion.
      - `expect(() => db.prepare("SELECT hidden_conversation_ids FROM user_preferences").get()).toThrow()` — inline P107-04 post-drop SELECT throws.

    Test P107-02 body:
      - `const db = new Database(":memory:")`
      - `db.exec(NEW_USER_PREFERENCES_POST_HIDDEN_DROP_CREATE_SQL)`
      - Sanity: `columnNames(db, "user_preferences")` does NOT contain hidden_conversation_ids AND does NOT contain pinned_conversation_ids.
      - `expect(() => runHiddenColumnDrop(db)).not.toThrow()`
      - Post-migration: columnNames shape unchanged.

    Keep tests atomic — one describe block, two `it(...)` bodies, no shared state between them (each opens its own `new Database(":memory:")`).

    Post-edit checkpoint: existing Phase 92 P92-01 + P92-02 tests continue to pass unchanged (they use their own OLD_USER_PREFERENCES_CREATE_SQL + NEW_USER_PREFERENCES_CREATE_SQL const literals which still include pinned_conversation_ids in OLD and hidden_conversation_ids in NEW — those SQL literals are static test fixtures for the Phase 92 code path and are NOT affected by the Phase 107 additions).

    TDD ordering: append the two P107 tests (RED — `runHiddenColumnDrop is not a function`), then apply the db/index.ts + schema.ts edits (GREEN). Two commits acceptable (test RED, feat GREEN), or one combined per Alice's discretion. Same discipline as Phase 92 Plan 03.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-vega &amp;&amp; npx vitest run src/backend/database/db/index.migration.test.ts --reporter=basic 2>&amp;1 | tail -40 &amp;&amp; npm run build:backend 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    Two new Phase 107 tests pass (P107-01 with inline P107-03/P107-04 assertions, P107-02). All existing Phase 66/68/72/75/79/85/92 tests in the same file still pass (no regression). Backend build clean (`npm run build:backend` exits 0). Grep-hygiene:
    - `grep -c 'runHiddenColumnDrop\\|phase-107-hidden-sentinel-migration' src/backend/database/db/index.ts` >= 3 (runHiddenColumnDrop function definition, migrateSchema invocation try/catch, forceSave label).
    - `grep -c 'hiddenConversationIds' src/backend/database/db/schema.ts` returns 0 (drizzle mirror field removed).
    - `grep -c 'addColumnIfNotExists.*hidden_conversation_ids' src/backend/database/db/index.ts` returns 0 (sweep line deleted).
    - `grep -c 'runHiddenColumnDrop' src/backend/database/db/index.migration.test.ts` >= 3 (import + P107-01 body + P107-02 body).
    - `grep -c 'pinnedConversationIds' src/backend/database/db/schema.ts` returns 0 (still absent — Phase 92 Plan 03 removed it; regression trap).
    - `grep -c 'phase-92-pin-sentinel-migration' src/backend/database/db/index.ts` returns 0 (forceSave label replaced by phase-107).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB migration boot path | Runs before request-handling; no untrusted input at this seam |
| DB file on disk | Encrypted at rest via existing DatabaseFileEncryption; forceSave persists the schema mutation atomically |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-03-01 | Denial of Service | migrateSchema() drop-column preflight on old SQLite | mitigate | assertSqliteSupportsDropColumn (Phase 66 preflight — reused verbatim) throws loudly on SQLite < 3.35 rather than silently continuing with a partially-migrated schema (T-66-04-04 precedent) |
| T-107-03-02 | Data Loss | dropping hidden_conversation_ids loses existing hide history | accept per CONTEXT.md § Schema migration "NO data migration" | Same acceptance shape as Phase 92 Plan 03 T-92-03-02: no code path in Skynet participates in migration. Users' existing DB-backed hide lists are lost. Alice aware (implicit — mirrors phase-92 precedent). |
| T-107-03-03 | Tampering | forceSave label mislabeled → future ops-grep broken | mitigate | Literal label `"phase-107-hidden-sentinel-migration"` locked by CONTEXT.md § Schema migration verbatim quote; presence-grepped by Task 1's done step + `runHiddenColumnDrop\\|phase-107-hidden-sentinel-migration` >= 3 assertion |
| T-107-03-04 | Data Loss | drop bundles pinned_conversation_ids as well by mistake (already-dropped column — would silent-no-op via dropColumnIfExists, but a broader ALTER pattern could regress) | mitigate | Test P107-01's assertion topology explicitly probes for theme, language, updated_at, reopen_tabs_on_login preservation — a future refactor that broadens the drop would fail these assertions loudly. Additionally, `grep -c 'pinnedConversationIds' src/backend/database/db/schema.ts` == 0 assertion ensures the pin column is not re-introduced by regression. |
| T-107-03-05 | Repudiation | migration failure invisible in logs | accept | Existing databaseLogger.error/warn shape at L1008-1015 (phase-92 precedent) preserved with the phase-107 label swap; log tag `schema_migration_force_save_post_drop` grepable by ops with the new `reason: "phase-107-hidden-sentinel-migration"` field |
| T-107-03-06 | Deploy race | Post-drop, mid-deploy, pre-Plan-04 frontend still calls putHiddenIds without identityHosts → 400 | accept | Alice single-user; deploy motion is quick; a mid-deploy tab-open surfaces as a 400 in the browser console, not data loss (the DB column is gone and the disk state is authoritative from Plan 02 onwards). Next remount post-Plan-04 fixes the wire signature. |
</threat_model>

<verification>
- Task 1: `runHiddenColumnDrop` exported (byte-mirror of runPinColumnDrop); migrateSchema invocation added AFTER runPinColumnDrop preflight AND BEFORE addColumnIfNotExists sweep; addColumnIfNotExists("user_preferences", "hidden_conversation_ids", "TEXT") deleted from the sweep; schema.ts hiddenConversationIds field deleted; forceSave label swapped to `phase-107-hidden-sentinel-migration` exactly matching CONTEXT.md's quality-gate literal.
- Task 1: Test P107-01 (OLD → drop → column absent, sibling VALUES preserved, post-drop SELECT throws) + Test P107-02 (NEW → idempotent) pass. Full test file green (no Phase 66/68/72/75/79/85/92 regression).
- Backend build sanity: `npm run build:backend` exits 0 with no new type errors (drizzle mirror field deletion is safe — Plan 02 already retired every consumer in src/).
- Manual grep-hygiene (all run from cd /home/ubuntu/skynet-vega):
  - `grep -c 'hiddenConversationIds\\|hidden_conversation_ids' src/backend/database/db/schema.ts` returns 0.
  - `grep -c 'hidden_conversation_ids' src/backend/database/db/index.ts` returns >= 1 (only inside the runHiddenColumnDrop dropColumnIfExists call + its docblock — no addColumnIfNotExists or other reference).
  - `grep -c 'runHiddenColumnDrop\\|phase-107-hidden-sentinel-migration' src/backend/database/db/index.ts` >= 3.
  - `grep -rn 'row\\.hiddenConversationIds\\|updates\\.hiddenConversationIds' src/backend/` returns 0 (Plan 02 already severed these — the schema drop is safe only if this is true).
</verification>

<success_criteria>
- The `hidden_conversation_ids` column is physically dropped from `user_preferences` on any deploy that boots this migration (SC-4, D-02).
- The drop is idempotent — re-boots after the initial drop are silent no-ops.
- `runHiddenColumnDrop` is a byte-mirror of `runPinColumnDrop` — same preflight, same helper call, only column name differs.
- `phase-107-hidden-sentinel-migration` forceSave label persists the schema mutation across container restart, exactly as CONTEXT.md specifies.
- Drop runs BEFORE addColumnIfNotExists sweep in migrateSchema — same ordering discipline as Phase 66 / Phase 92 Plan 03.
- Preflight throw is fatal (boot aborts before schema corruption); forceSave failure is non-fatal warn (idempotent drop retries next boot).
- Both `pinned_conversation_ids` (Phase 92) and `hidden_conversation_ids` (Phase 107) columns are now absent from user_preferences — the DB column pair for the two-slice migration is fully retired.
- Plan 04 unblocked: frontend can rewire hidden hydrate to identity-metadata projection knowing the DB source is physically gone; no stale-DB-read code path can silently re-surface.
</success_criteria>

<output>
Create `.planning/phases/107-hide-identity-rows-via-disk-sentinel-mirror-phase-92-for-the/107-03-SUMMARY.md` when done, documenting:
- The runHiddenColumnDrop export + its byte-mirror relationship to runPinColumnDrop (Phase 92 Plan 03 precedent).
- The forceSave label swap `phase-92-pin-sentinel-migration` → `phase-107-hidden-sentinel-migration` (single labeled forceSave batches both drops + the sweep in one atomic file write — Phase 92 Plan 03 pattern applied to the next phase).
- The Test P107-03/04 regression traps for sibling VALUE preservation and post-drop SELECT throw.
- Confirmation that the drop is the LAST plumbing step for the hidden slice at the schema level — Plan 04 wires the frontend to the new source of truth AFTER the column is gone.
- The deploy-race note from the CONTEXT: this Plan ships BEFORE Plan 04, so mid-deploy tabs may 400 on hidden PUTs — acceptable per Alice single-user context, self-heals on remount post-Plan-04.
</output>
</content>
</invoke>