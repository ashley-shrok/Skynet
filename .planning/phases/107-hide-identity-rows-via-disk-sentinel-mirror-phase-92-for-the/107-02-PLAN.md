---
phase: 107-hide-identity-rows-via-disk-sentinel
plan: 02
type: execute
wave: 2
depends_on:
  - 107-01
files_modified:
  - src/backend/database/routes/user-preferences.ts
  - src/backend/database/routes/user-preferences.test.ts
  - src/backend/database/routes/identities.ts
  - src/backend/database/routes/identities.disk-read.test.ts
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - SC-2
  - SC-3
must_haves:
  truths:
    - "GET /identities response objects grow a `hidden: boolean` field emitted by publicIdentity — populated per-identity by `identityFileExists(identityKey, '.hidden', ...)` launched in the SAME Promise.all wave as the existing `.pinned` probe (SC-2, D-03)."
    - "The `.hidden` probe is fail-closed on any stat error → hidden:false (never over-report hidden; a transient stat failure must NOT hide an identity from the sidebar) — matches the `.pinned` fail-closed contract at identities.ts L387-391."
    - "PUT /user-preferences with body.hiddenConversationIds=[k1,k2,...] fans out per-identity `.hidden` writes/removes over Plan 01's primitive: for each identity key in the new set, writeIdentityFile(key, '.hidden', '', ...); for each key in the previous set but not the new set, removeIdentityFile(key, '.hidden', ...); response echoes back the disk-derived hidden key list (SC-3, D-05)."
    - "The hidden fanout REUSES the same connByHost map the pin fanout opens — when BOTH pinnedConversationIds AND hiddenConversationIds arrive in the same PUT body, one connection per unique host serves BOTH fanouts. Sequence: pin fanout, then hidden fanout, in the same try/finally block."
    - "PUT /user-preferences REQUIRES the identityHosts body field whenever hiddenConversationIds is present — 400 when missing OR when any hidden key lacks a host mapping (mirrors the pin path's PUT-92-06/PUT-92-07d guards)."
    - "The `hidden_conversation_ids` DB column is no longer read or written by ANY code in src/ — parseHiddenConversationIds deleted, pickPreferences no longer surfaces the field, updates.hiddenConversationIds no longer assigned. Plan 03 physically drops the column safely because nothing consumes it post-this-plan (D-02)."
    - "H3 lowercase-on-disk invariant continues to hold: identityHosts keys arriving at PUT are lowercased by parseIdentityHosts at user-preferences.ts:86-96 verbatim, and the disk-fanout at identities.ts iterates the raw folder-name output of listIdentityKeysOnHost — the identityKey passed to identityFileExists / writeIdentityFile / removeIdentityFile for `.hidden` matches the on-disk folder byte-for-byte, same as the `.pinned` path."
    - "hiddenConversationIds body validation (non-array / non-string / length-cap) preserved verbatim from the pre-plan handler at user-preferences.ts:400-419 — the DoS mitigation constant HIDDEN_CONVERSATION_IDS_MAX_LENGTH stays in force even though the write path shifted from DB to disk."
    - "The pin path (pinnedConversationIds fanout at user-preferences.ts:233-393) is byte-for-byte UNCHANGED — this plan adds the hidden fanout as a sibling block, does NOT restructure the pin block, does NOT rename identityHosts, does NOT reshape the connByHost map. Regression traps on the pin path stay green."
    - "GET /user-preferences response body no longer contains hiddenConversationIds — the field disappears from pickPreferences alongside the already-retired pinnedConversationIds (both slices now derive from the identity-metadata payload frontend-side)."
  artifacts:
    - path: "src/backend/database/routes/identities.ts"
      provides: "publicIdentity response gains `hidden: boolean` sibling to `pinned`; disk-fanout probes `.hidden` in the SAME Promise.all wave as `.pinned`; H3 comment block extended to name `.hidden`"
      contains: "identityFileExists\\(.+\\.hidden|hidden:"
    - path: "src/backend/database/routes/identities.disk-read.test.ts"
      provides: "New HID-107-01..06 tests mirroring the PUB-92-01..06 shape for the `.hidden` field + parallel-probe assertion + fail-closed + H3 verbatim-passthrough"
      contains: "HID-107"
    - path: "src/backend/database/routes/user-preferences.ts"
      provides: "PUT handler grows a hidden fanout block sibling to the pin block (identityHosts required, delta-diff → writeIdentityFile/removeIdentityFile with `.hidden`, re-derive from disk for echo); GET handler retires hiddenConversationIds row read; parseHiddenConversationIds deleted"
      contains: "writeIdentityFile.+\\.hidden|removeIdentityFile.+\\.hidden|identityFileExists.+\\.hidden"
    - path: "src/backend/database/routes/user-preferences.test.ts"
      provides: "New HID-107-* tests: fanout writes, removes, no-op same-set, identityHosts required, echo re-derivation from disk, combined pin+hidden PUT shares conns, H3 verbatim-passthrough; retires legacy hiddenConversationIds DB-write assertions"
      contains: "HID-107"
  key_links:
    - from: "src/backend/database/routes/identities.ts disk-fanout inner Promise.all"
      to: "identityFileExists(identityKey, '.hidden', { hostId, conn })"
      via: "sibling probe to the existing `.pinned` probe in the SAME Promise.all wave — one round-trip per identity, two probes"
      pattern: "identityFileExists\\(.+\\.hidden"
    - from: "src/backend/database/routes/user-preferences.ts hidden fanout block"
      to: "writeIdentityFile(k, '.hidden', '', ...) + removeIdentityFile(k, '.hidden', ...)"
      via: "delta-diff over parseIdentityHosts's already-lowercased key set; reuses the pin fanout's connByHost map"
      pattern: "writeIdentityFile\\(.+\\.hidden|removeIdentityFile\\(.+\\.hidden"
    - from: "src/backend/database/routes/user-preferences.ts pickPreferences"
      to: "row.hiddenConversationIds"
      via: "REMOVED — parseHiddenConversationIds deleted, pickPreferences no longer projects the field, GET response omits it"
      pattern: "^(?!.*parseHiddenConversationIds).*$"
---

<objective>
Rewire the backend hidden slice: sever every read/write of `hiddenConversationIds` from the user_preferences DB row and reroute both to Plan 01's per-identity file primitive with `.hidden` as the relPath, EXACTLY mirroring what Phase 92 Plan 02 did for the pinned slice.

After this plan:
- **Read path** — `publicIdentity` grows a `hidden: boolean` field populated by `identityFileExists(name, ".hidden", ...)` launched in the SAME Promise.all wave as the existing `.pinned` probe (parallel, not serial). Same fail-closed contract (any exists throw → hidden:false).
- **Write path** — PUT /user-preferences with `hiddenConversationIds: string[]` runs the pin fanout FIRST (unchanged), then runs the hidden fanout as a sibling block: probes disk-derived previousSet, computes delta, fans writes+removes in parallel, re-derives disk state for the response echo. Both fanouts share the connByHost map opened once per unique host.
- **GET path** — pickPreferences no longer surfaces `hiddenConversationIds` from the row; parseHiddenConversationIds is deleted. The row column is no longer touched by src/ code, unblocking Plan 03 to drop it physically.
- **Retirement of DB column** — Plan 03 (wave 3) drops `hidden_conversation_ids` from user_preferences once nothing consumes it. This plan lands the code-side severance; the schema drop is a separate wave.

Purpose: Cross the two-sources-of-truth gap for the hidden slice, exactly as Phase 92 Plan 02 crossed it for the pinned slice. Until this plan lands, both the DB column AND the disk sentinel could exist independently; after this plan, disk is authoritative and DB is unreferenced.

H3 invariant lock (inherited verbatim from Phase 92 Plan 02): every identity folder on the target host is named with a lowercase identityKey (identity-artifact-reader IDENTITY_KEY_RE `/^[a-z0-9_-]{1,64}$/` forbids uppercase). identityHosts keys arriving at PUT are lowercased by parseIdentityHosts at user-preferences.ts:86-96 (existing helper). The disk-fanout at identities.ts iterates listIdentityKeysOnHost's raw folder-name output (already lowercase). This plan's `.hidden` writes/probes MUST pass those keys VERBATIM to the primitive — no `.toLowerCase()`, no reconstruction. The regression traps below (HID-107-06 + HID-107-08) assert byte-for-byte match.

D-02 note (asymmetric with Phase 92): Phase 92 Plan 02 was labeled "hiddenConversationIds slice UNTOUCHED (D-02 out-of-scope) — HIDE 1-10 pass verbatim; HIDE-X test updated to explicitly assert the pin path is DB-independent while the hide path still hits the row." Phase 107 REVERSES that D-02: hidden is now IN scope. Retire the HIDE-X regression trap; retire the HIDE 1-10 DB-write tests; add HID-107-* tests that assert the disk-fanout shape and the DB decoupling of BOTH slices post-migration.

Output: user-preferences.ts (hidden fanout block added sibling to pin block; parseHiddenConversationIds deleted; pickPreferences no longer projects hiddenConversationIds), identities.ts (publicIdentity + disk-fanout extended to probe `.hidden` in parallel with `.pinned`), colocated test suites updated for the new contract INCLUDING the H3 verbatim-passthrough regression traps AND the combined pin+hidden PUT conn-sharing test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/shapes/shape-hidden-identity-rows-via-sentinel.md
@.planning/phases/107-hide-identity-rows-via-disk-sentinel-mirror-phase-92-for-the/107-CONTEXT.md
@.planning/phases/105-pin-sentinel-migration-move-identity-pin-state-from-skynet-d/105-02-SUMMARY.md

@src/backend/database/routes/user-preferences.ts
@src/backend/database/routes/identities.ts
@src/backend/claude-session/per-identity-file.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend publicIdentity + disk-fanout to populate `hidden: boolean` via identityFileExists (parallel with `.pinned` probe)</name>
  <files>src/backend/database/routes/identities.ts, src/backend/database/routes/identities.disk-read.test.ts</files>
  <read_first>
    - src/backend/database/routes/identities.ts (publicIdentity signature L159-260 — sixth arg is currently `pinned: boolean` from Phase 92 Plan 02; the seventh arg will be `hidden: boolean`; the disk-fanout inner Promise.all at L370-419 launches the `.pinned` probe alongside readIdentityFile — the `.hidden` probe joins the same wave; the H3 comment block at L378-386 already documents the verbatim-passthrough rule and can be extended to name `.hidden`)
    - src/backend/claude-session/per-identity-file.ts (identityFileExists — signature unchanged from Plan 01; `.hidden` now permitted by ALLOWED_REL_PATHS)
    - src/backend/database/routes/identities.disk-read.test.ts (existing PUB-92-01..06 test topology — the exact template to mirror for HID-107-01..06)
    - .planning/phases/105-pin-sentinel-migration-move-identity-pin-state-from-skynet-d/105-02-SUMMARY.md (§ Accomplishments — Read path shipped: publicIdentity's sixth-arg extension pattern + fail-closed `.catch(() => false)` shape — the template for the seventh arg)
  </read_first>
  <behavior>
    - Test PUB-107-signature-1 (default hidden:false): publicIdentity called with SIX args (omitting the new seventh `hidden` arg) still compiles and returns `hidden: false` — matches the `pinned: boolean = false` default shape. Locks the fail-closed default contract per D-01 "presence is meaning".
    - Test PUB-107-signature-2 (seventh arg surfaces): publicIdentity called with SEVEN args (all cosmetic args + role + roleCosmetics + pinned:false + hidden:true) returns an object with hidden === true.
    - Test PUB-107-signature-3 (pinned unchanged): passing (identityKey, hostId, cosmetics, role, roleCosmetics, pinned:true, hidden:false) returns pinned:true AND hidden:false — the two fields are independent, neither shadowing the other.
    - Test HID-107-01 (hidden:false when no `.hidden` sentinel): stub identityFileExists to return true for `.pinned` and false for `.hidden` → response.hidden === false, response.pinned === true.
    - Test HID-107-02 (hidden:true when `.hidden` sentinel present): stub identityFileExists to return false for `.pinned` and true for `.hidden` → response.hidden === true, response.pinned === false.
    - Test HID-107-03 (hidden:false on `.hidden` exists failure — fail-closed): stub identityFileExists to throw for `.hidden` and succeed for `.pinned` → response.hidden === false (the exists throw is caught → false), response.pinned unaffected. Mirrors the D-01 "presence is meaning" invariant: a stat failure MUST NOT paint an identity as hidden by mistake, and MUST NOT leak the failure into the `.pinned` probe's result.
    - Test HID-107-04 (parallel with `.pinned` probe — one Promise.all wave, TWO probes per identity, not serial): the disk-fanout's inner Promise.all launches BOTH `identityFileExists(k, '.pinned', ...)` AND `identityFileExists(k, '.hidden', ...)` in the same await boundary as readIdentityFile. Assertable by counting the number of concurrent identityFileExists invocations at the moment readIdentityFile is called (should be 2 per identity — one for each sentinel — not sequential). NO added round-trip per identity vs the pre-plan `.pinned`-only shape (the shape is 2 stat probes running in parallel; still one await boundary).
    - Test HID-107-05 (per-identity — no memo): unlike role-cosmetics which memoizes per (host, role), `.hidden` is per-identity — each identity gets its own identityFileExists call for `.hidden`, no deduplication. Assert the stub is called once per identity per sentinel type (2 calls per identity: one for `.pinned`, one for `.hidden`).
    - Test HID-107-06 (H3 lock — identityKey passed VERBATIM to identityFileExists for `.hidden`): the disk-fanout iterates listIdentityKeysOnHost's returned keys (e.g. ["tina", "alice-01"]); assert the identityFileExists spy for `.hidden` receives EXACTLY those strings as first arg (byte-for-byte, no case coercion between the fanout and the primitive). Positive-shape lock: for every call, assert `arg === arg.toLowerCase()` — a defensive lock that no future edit accidentally uppercases the key before threading to the primitive.
  </behavior>
  <action>
    In src/backend/database/routes/identities.ts:

    Extend the publicIdentity function signature at L159-194 to accept a SEVENTH argument `hidden: boolean = false`. Place it AFTER the existing sixth `pinned` arg. Mirror the same JSDoc structure the pinned arg uses (Phase 92 Plan 92-02 Task 1 pattern). Concretely, add to the signature:

      /** Phase 107 Plan 107-02 Task 1: hidden:boolean surface for the per-identity
       *  `.hidden` sentinel (D-03/D-04, SC-2). Populated by the GET /identities
       *  disk fanout via identityFileExists in the SAME Promise.all wave as
       *  the `.pinned` probe and the role-cosmetic read (see disk-fanout at
       *  L370+). Default false is the fail-closed safe-default per D-01
       *  "presence is meaning" — a caller that omits this argument reports the
       *  identity as unhidden rather than fabricating a truthy state. Sibling
       *  to `pinned` above; independent axis. */
      hidden: boolean = false,

    In the returned object (currently emits `pinned` at L258), add `hidden` as the sibling field IMMEDIATELY after `pinned`:

      pinned,
      // Phase 107 Plan 107-02: hidden:boolean derived from on-demand disk read
      // of `.hidden` sentinel via identityFileExists at request time (D-03).
      // No DB mirror, no in-memory cache. Frontend Plan 04 rewires
      // conversation-store's hiddenIds derivation from GET /user-preferences
      // to this per-identity field.
      hidden,

    In the disk-fanout at L370-419 (per-identity inner Promise.all), add a `hiddenPromise` sibling to the existing `pinnedPromise` at L387-391 and extend the `Promise.all` at L393-396 to await BOTH:

      const pinnedPromise = identityFileExists(
        identityKey,
        ".pinned",
        { hostId, conn },
      ).catch(() => false); // fail-closed per D-01 (PUB-92-03)
      // Phase 107 Plan 107-02: parallel `.hidden` probe in the SAME
      // Promise.all wave as `.pinned` + readIdentityFile. Same fail-closed
      // contract (exists throw → hidden:false per D-01 — a stat failure
      // must NEVER paint an identity as hidden by mistake). HID-107-03
      // locks the exception path; HID-107-04 locks the parallel-wave shape.
      const hiddenPromise = identityFileExists(
        identityKey,
        ".hidden",
        { hostId, conn },
      ).catch(() => false);

      const [{ markdown }, pinned, hidden] = await Promise.all([
        readIdentityFile(conn, identityKey),
        pinnedPromise,
        hiddenPromise,
      ]);

    Thread `hidden` into the `publicIdentity(...)` call at L407-414 as the seventh argument:

      return publicIdentity(
        identityKey,
        hostId,
        cosmetics,
        role,
        roleCosmetics,
        pinned,
        hidden,
      );

    Extend the H3 lowercase-on-disk invariant comment block at L378-386 to name `.hidden` alongside `.pinned` — trivial prose extension, no behavior. Concretely, change "identityFileExists" (singular) → "identityFileExists for both `.pinned` and `.hidden`" and the trailing note "so it matches the on-disk folder segment byte-for-byte" now applies to both probes.

    Do NOT change: avatarUrl, coordinator, task, role, roleDefaults, any other publicIdentity field, the roleCosmetics memo shape, the per-host connectOneShot pattern, or any other disk-fanout logic. Purely additive.

    In src/backend/database/routes/identities.disk-read.test.ts:

    Add a new describe block titled `Phase 107 Plan 107-02: publicIdentity.hidden + disk-fanout .hidden probe`. Inside it, three signature unit tests (PUB-107-signature-1/2/3) + six fanout tests (HID-107-01..06). Mirror the existing PUB-92-* topology verbatim — same mock scaffolding for identityFileExists spy, same disk-fanout mocking for the GET /identities handler entry, same fail-closed assertion shape.

    Concrete mock reuse rules for the new tests:
    - The existing identityFileExists spy already resolves calls with different relPath arguments; extend it to differentiate `.pinned` vs `.hidden` via a mockImplementation that reads the second argument. Existing pre-plan tests use `mockResolvedValue(false)` or `mockResolvedValue(true)` — the new tests use `mockImplementation((_key, relPath, _opts) => relPath === '.hidden' ? Promise.resolve(true) : Promise.resolve(false))` (or symmetric).
    - Test HID-107-04's parallel-wave assertion: use a fixture where readIdentityFile takes a controlled amount of time (fake timers or a manual promise) and assert both identityFileExists calls have fired BEFORE readIdentityFile resolves. Alternative simpler shape: assert `identityFileExists` mock.calls.length === 2 * identityKeys.length after a single fanout tick — same information.
    - Test HID-107-06's byte-for-byte assertion: use the same fixture-key list the existing PUB-92-06 test uses (["tina", "alice-01", "role_underscore"]); assert `identityFileExistsSpy.mock.calls.filter(c => c[1] === '.hidden').map(c => c[0])` equals the fixture list byte-for-byte. Positive-shape lock: for every call, `expect(c[0]).toBe(c[0].toLowerCase())`.

    Do NOT retire or modify existing PUB-92-* tests — they still lock the pinned side of the fanout. The new HID-107-* tests are strictly additive.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-vega &amp;&amp; npx vitest run src/backend/database/routes/identities.disk-read.test.ts src/backend/database/routes/identities.get-disk.test.ts --reporter=basic 2>&amp;1 | tail -40</automated>
  </verify>
  <done>
    All three PUB-107-signature-* tests + six HID-107-* tests pass. Existing PUB-92-01..06 + PUB-M-* + other disk-read tests unregressed. `grep -c 'hidden:' src/backend/database/routes/identities.ts` >= 2 (signature default + return-object emission). `grep -c 'identityFileExists.*\\.hidden' src/backend/database/routes/identities.ts` >= 1 (disk-fanout probe wiring). `grep -c 'H3 lowercase-on-disk invariant' src/backend/database/routes/identities.ts` >= 1 (comment block preserved and extended to name `.hidden`).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire PUT /user-preferences hidden fanout sibling to pin fanout (shares connByHost); retire hiddenConversationIds row I/O in GET + updates</name>
  <files>src/backend/database/routes/user-preferences.ts, src/backend/database/routes/user-preferences.test.ts</files>
  <read_first>
    - src/backend/database/routes/user-preferences.ts (parseHiddenConversationIds L52-66 — DELETED here; pickPreferences L98-111 — hiddenConversationIds line REMOVED; parseIdentityHosts L86-96 — REUSED verbatim; the entire pin fanout block L215-393 including openConnForHost usage, uniqueHostIds dedupe at L284, connByHost Map at L285, the try/finally at L286-392 with SSH conn cleanup, the delta-diff at L318-327, the fan Promise.all at L333-346, the disk-authoritative echo at L349-372, the didFanoutSentinels flag at L231/347/425; the existing hidden validation at L395-419 which DELETES the updates.hiddenConversationIds assignment; the "No preferences provided" guard at L425)
    - src/backend/database/routes/user-preferences.test.ts (existing HIDE 1-10 tests + HIDE-X test — this task retires them and replaces with HID-107-* tests; existing PUT-92-* pin tests unchanged; SAVE 92-02 test unchanged; existing GET-92-01 tests — extend to include hidden-omission assertion)
    - .planning/phases/105-pin-sentinel-migration-move-identity-pin-state-from-skynet-d/105-02-SUMMARY.md (§ Accomplishments Task 2 — the exact fanout template being mirrored for hidden)
    - src/backend/claude-session/per-identity-file.ts (writeIdentityFile / removeIdentityFile / identityFileExists — same signatures as pin path; `.hidden` now permitted by Plan 01)
  </read_first>
  <behavior>
    - Test HID-107-PUT-01 (hide one identity — writes .hidden once): PUT with body `{hiddenConversationIds: ["tina"], identityHosts: {"tina": 1}}` when the prior disk state had `[]` → writeIdentityFile("tina", ".hidden", "", { hostId: 1, conn: ... }) called exactly once; removeIdentityFile for `.hidden` NOT called; response echoes back `{hiddenConversationIds: ["tina"], ...}` (disk-derived echo).
    - Test HID-107-PUT-02 (unhide one identity — removes .hidden once): PUT with body `{hiddenConversationIds: [], identityHosts: {"tina": 1}}` when the prior disk state had `["tina"]` (mock identityFileExists to return true for `.hidden` on the probe) → removeIdentityFile("tina", ".hidden", ...) called once; writeIdentityFile for `.hidden` NOT called; response echoes `hiddenConversationIds: []`.
    - Test HID-107-PUT-03 (swap hides — adds one, removes one): prior `["alice"]`, new `["bob"]` → writeIdentityFile("bob", ".hidden", ...) AND removeIdentityFile("alice", ".hidden", ...) fire in the same Promise.all wave.
    - Test HID-107-PUT-04 (no-op — same set): prior `["tina"]`, new `["tina"]` → NO writeIdentityFile, NO removeIdentityFile calls for `.hidden`; response echoes `["tina"]`.
    - Test HID-107-PUT-05 (write failure surfaces synchronously per D-06): stub writeIdentityFile to throw on `.hidden` → handler returns 500; response body does NOT echo hiddenConversationIds; conns closed via finally.
    - Test HID-107-PUT-06 (hidden key not in identityHosts map → 400): PUT body says hide "unknown-key" but identityHosts doesn't map it to a host → 400 with error mentioning "identity host required"; NO `.hidden` writes attempted (mirrors the pin path's PUT-92-06).
    - Test HID-107-PUT-07a/b/c (validation retained: non-array, non-string, oversize): pre-plan validation at L400-417 preserved verbatim — non-array 400, non-string element 400, length > 1000 400. Fires BEFORE any fanout.
    - Test HID-107-PUT-07d (identityHosts missing → 400 when hiddenConversationIds present): PUT body carries hiddenConversationIds but omits identityHosts → 400 "identityHosts required in body when setting hiddenConversationIds" (mirrors the pin path's guard at L256-260 verbatim).
    - Test HID-107-PUT-08 (H3 lock — identityKey passed VERBATIM to primitive for `.hidden`): spy on writeIdentityFile; assert its first arg on the `.hidden` call is EXACTLY the string "tina" byte-for-byte from the input identityHosts (post-parseIdentityHosts lowercasing). Additionally: PUT body with `{hiddenConversationIds: ["Tina"], identityHosts: {"Tina": 1}}` — assert EITHER (a) request rejected at 400 via parseIdentityHosts's toLowerCase collapse producing an identityHosts miss for "Tina", OR (b) writeIdentityFile receives "tina" lowercased AND the primitive's IDENTITY_KEY_RE rejects raw "Tina". Both branches acceptable — the invariant is NO uppercase identityKey ever reaches disk for `.hidden`.
    - Test HID-107-PUT-09 (combined pin+hidden PUT shares connByHost): PUT with body `{pinnedConversationIds: ["tina"], hiddenConversationIds: ["alice"], identityHosts: {"tina": 1, "alice": 2}}` — assert openConnForHost is called EXACTLY once for hostId 1 AND EXACTLY once for hostId 2 (NOT twice per host). Both fanouts run inside the same try/finally block and share the same connByHost Map. Pin fanout runs FIRST (existing order), hidden fanout runs SECOND, then both echoes attach to the response. Response body contains BOTH `pinnedConversationIds: ["tina"]` AND `hiddenConversationIds: ["alice"]`, each disk-derived.
    - Test HID-107-PUT-10 (response echo re-derived from disk for hidden — trust disk, not client): stub identityFileExists to return true for `.hidden` on "bob" AND "tina" AFTER the fanout writes; client submits `hiddenConversationIds: ["tina"]` and identityHosts `{"tina": 1, "bob": 1}`; echo returns whatever the post-fanout disk probe reports — locks the "disk is truth" invariant (mirrors PUT-92 Task 2 Part B step 6).
    - Test HID-107-GET-01 (GET /user-preferences no longer contains hiddenConversationIds): handleGetPreferences returns a body where `"hiddenConversationIds" in body === false` (field disappears from pickPreferences). Retires the pre-plan HIDE 1-10 tests that asserted parseHiddenConversationIds shape.
    - Test HID-107-DB-01 (pin/hide fanout are DB-independent — regression trap on D-02): after HID-107-PUT-01 (hide "tina" via fanout), assert the user_preferences row's `hidden_conversation_ids` column value is UNCHANGED from its pre-PUT state (null or whatever the fixture seeded). Symmetrically after HID-107-PUT-09 (combined), assert both `pinned_conversation_ids` AND `hidden_conversation_ids` columns are UNCHANGED — both slices are fully DB-decoupled post-plan.
  </behavior>
  <action>
    Two-part rewire.

    Part A — GET path retirement + shared helper:

    Delete `parseHiddenConversationIds` at L40-66 entirely.

    In `pickPreferences` at L98-111, DROP the `hiddenConversationIds: parseHiddenConversationIds(row?.hiddenConversationIds)` line at L108-110. The pickPreferences function no longer projects hiddenConversationIds. GET response body will lack the field.

    Do NOT invent a new GET-time derivation. Frontend Plan 04 rewires the hidden hydrate to derive from the identity-metadata payload (each identity's `hidden: boolean` field from Task 1), symmetric to the pin shift. The backend GET handler thus does NOT need to reconstruct a per-user aggregate — the aggregate derives frontend-side from the per-identity `hidden: boolean` Task 1 added.

    Part B — PUT path hidden fanout (sibling block to the existing pin fanout):

    In `handlePutPreferences`, DELETE the pre-plan hidden block at L395-419 in its entirety (this is the block that assigns `updates.hiddenConversationIds = JSON.stringify(hiddenConversationIds)`). It's being replaced.

    In its place, add a new `hiddenConversationIds` fanout block MODELED VERBATIM on the pin fanout at L233-393. Concrete rules:

    1. Placement: the new block goes AFTER the existing pin fanout block (which ends at L393 with the `}` closing the `if (pinnedConversationIds !== undefined)` scope) and BEFORE the retired hidden validation block (which is being deleted). The two fanouts (pin, then hidden) live as SIBLING top-level `if (...) { ... }` blocks inside `handlePutPreferences`.

    2. Body validation: preserve the existing hidden validation semantics verbatim — non-array 400, non-string element 400, length > 1000 400. Reuse the HIDDEN_CONVERSATION_IDS_MAX_LENGTH constant at L37 unchanged. Fires BEFORE any fanout.

    3. identityHosts REQUIRED: mirror the pin path's guard at L256-260 verbatim — if `identityHostsRaw === undefined || identityHostsRaw === null`, return 400 with `"identityHosts required in body when setting hiddenConversationIds"`. If both pin AND hidden are present in the same body, this check has ALREADY fired inside the pin block; the hidden block's check is a defense-in-depth re-check for the hidden-only case.

    4. parseIdentityHosts REUSE: call `parseIdentityHosts(identityHostsRaw)` — the SAME helper the pin path uses at L261. Do NOT re-derive.

    5. Per-key host mapping check: mirror L273-279 verbatim, s/pinnedConversationIds/hiddenConversationIds/. Every key in the hiddenConversationIds array MUST have a host mapping in identityHosts, otherwise 400.

    6. connByHost sharing: this is the load-bearing invariant. If pin fanout ALREADY ran (pinnedConversationIds was present in the same body), the pin block opened a connByHost Map inside its try/finally. Hidden fanout runs INSIDE THE SAME try/finally block, reusing the same map — do NOT open a second set of SSH conns per unique host. Concretely, restructure the pin block's try/finally scope to enclose BOTH fanouts:

         let connByHost: Map<number, import("ssh2").Client | null> | null = null;
         let didFanoutSentinels = false;
         try {
           if (pinnedConversationIds !== undefined) {
             // ... existing pin body ...
             // (dedupe + openConnForHost + previousSet probe + delta +
             //  fan writes/removes + re-derive echo + set didFanoutSentinels)
             // At the top of this block, if connByHost is still null, allocate
             // it and open conns for pin's uniqueHostIds.
           }
           if (hiddenConversationIds !== undefined) {
             // Validate + parseIdentityHosts + per-key host check as above.
             // If connByHost is still null (pin didn't run), allocate + open
             // conns for hidden's uniqueHostIds.
             // If connByHost already exists (pin ran first), extend it with
             // any hostIds not yet mapped (union of pin.uniqueHostIds and
             // hidden.uniqueHostIds — dedupe by hostId).
             // Then run the hidden fanout: previousSet probe, delta, fan
             // writes/removes, re-derive echo — using identityFileExists /
             // writeIdentityFile / removeIdentityFile with '.hidden' as relPath.
             // Set didFanoutSentinels = true if any add/remove ran (or on
             // any successful fanout dispatch).
           }
         } catch (e) {
           databaseLogger.error("Sentinel fan-out failed", e, {
             operation: "user_preferences_sentinel_fanout",
             userId,
           });
           return res.status(500).json({ error: "Failed to update user preferences" });
         } finally {
           if (connByHost) {
             for (const conn of connByHost.values()) {
               if (conn) {
                 try { conn.end(); } catch { /* ignore */ }
               }
             }
           }
         }

       IMPORTANT: the restructure is ADDITIVE around the existing pin block — the pin body's behavior does NOT change. Every existing PUT-92-* test MUST continue to pass. The conn-lifecycle guard now covers both fanouts in one finally instead of the pin path's current single-fanout finally. Verify by running the pin test suite after the restructure BEFORE adding the hidden logic.

    7. Echo attachment: mirror the pin path's `_echoPinned` pattern verbatim with `_echoHidden`. Stash the disk-authoritative post-fanout hidden set on `(res as unknown as { _echoHidden: string[] })._echoHidden`. Below the try/catch, when building the response body, if `_echoHidden !== undefined`, attach `responseBody.hiddenConversationIds = echoHidden`. Pin and hidden echoes are independent; when both are present, both attach.

    8. didFanoutSentinels flag semantics: extend to true for EITHER a pin fanout OR a hidden fanout. The "No preferences provided" guard at L425 already reads this flag — no additional change needed to that guard's shape, only the flag's setter is extended.

    9. Remove pre-plan hidden updates handling: delete the `updates.hiddenConversationIds = JSON.stringify(hiddenConversationIds)` assignment (was at L418). The row is no longer written for this field. The DB write block at L433-475 continues to write the OTHER user_preferences fields (theme, fontSize, accent_color, language, reopen_tabs_on_login) UNCHANGED.

    10. H3 anti-coercion audit: after edits, `grep -c '\.toLowerCase\\|\.toUpperCase' src/backend/database/routes/user-preferences.ts` should return exactly 1 hit — the existing `out[k.toLowerCase()] = v;` at parseIdentityHosts L92. NO additional case-coercion between the parseIdentityHosts entry boundary and any writeIdentityFile / removeIdentityFile / identityFileExists call for either `.pinned` or `.hidden`.

    11. Comment: at the top of the new hidden fanout block, mirror the pin block's H3 comment at L215-230 verbatim, s/pinnedConversationIds/hiddenConversationIds/ and s/`.pinned`/`.hidden`/. Explicitly cite Phase 107 Plan 107-02 in the block header.

    12. DO NOT change: HIDDEN_CONVERSATION_IDS_MAX_LENGTH constant, PINNED_CONVERSATION_IDS_MAX_LENGTH constant, openConnForHost helper, resolveHostById import, isLocalHostId import, parseIdentityHosts helper, connectOneShot import, DatabaseSaveTrigger.forceSave call at L463 (label stays as `user_preferences_updated` — this file does not own the phase-107 migration; Plan 03 owns the schema drop's forceSave label), the openapi block at L534-566 (planner discretion whether to update the openapi doc to reflect the fanout — recommended trivial prose update in the openapi block naming Phase 107, mirror the pin openapi extension pattern).

    Grep-hygiene checkpoint AFTER edits (all cd /home/ubuntu/skynet-vega):
    - `grep -c 'parseHiddenConversationIds' src/backend/database/routes/user-preferences.ts` returns 0 (helper deleted).
    - `grep -c 'row\\.hiddenConversationIds\\|updates\\.hiddenConversationIds\\|userPreferences\\.hiddenConversationIds' src/backend/database/routes/user-preferences.ts` returns 0 (no row read/write for the field).
    - `grep -c 'writeIdentityFile.*\\.hidden\\|removeIdentityFile.*\\.hidden' src/backend/database/routes/user-preferences.ts` >= 2 (both write and remove wired for `.hidden`).
    - `grep -c 'identityFileExists.*\\.hidden' src/backend/database/routes/user-preferences.ts` >= 1 (previousSet probe + response-echo re-derive).

    In src/backend/database/routes/user-preferences.test.ts:

    Retire the pre-plan HIDE 1-10 DB-write tests AND the HIDE-X regression trap (the trap asserted the pin path was DB-independent while the hide path still hit the row — both halves of that assertion are now false and re-asserting them false is a weak test). Replace with HID-107-PUT-01..10 + HID-107-GET-01 + HID-107-DB-01 as described above. Reuse the pin test mock scaffolding for writeIdentityFile / removeIdentityFile / identityFileExists — the same vi.mock block at the top of the file works for the hidden side.

    Existing PUT-92-* pin tests MUST continue to pass verbatim — no signature changes to the pin path, no behavior changes to the pin block. If the restructure of the try/finally scope to enclose both fanouts affects a pin test's conn-count assertion (unlikely — pin's connByHost dedupe still holds), fix the test to assert the SHARED map's behavior post-restructure, but do NOT weaken the pin invariant.

    Test topology reuse: HID-107-PUT-01..05/07/10 mirror PUT-92-01..05/07/07d verbatim, s/pinnedConversationIds/hiddenConversationIds/ and s/`.pinned`/`.hidden`/. HID-107-PUT-08 mirrors PUT-92-08a/b. HID-107-PUT-09 is NEW — the combined-request conn-sharing test that has no pin-side equivalent (Phase 92 never had a second slice to combine with). HID-107-DB-01 is the D-02 regression trap: assert both DB columns stay untouched after fanouts.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-vega &amp;&amp; npx vitest run src/backend/database/routes/user-preferences.test.ts --reporter=basic 2>&amp;1 | tail -50</automated>
  </verify>
  <done>
    All ten new HID-107-PUT-* tests + HID-107-GET-01 + HID-107-DB-01 pass. All existing PUT-92-* pin tests continue to pass unchanged. Existing GET-92-01 tests updated for the hidden-omission (asserted "hiddenConversationIds" in body === false alongside the pinned-omission). SAVE 92-02 tests (pin-only PUT no forceSave) plus new SAVE 107-01 (hidden-only PUT no forceSave — the DB write block skips when only hiddenConversationIds and no theme/fontSize/etc.) both pass. Grep-hygiene:
    - `grep -c 'parseHiddenConversationIds' src/backend/database/routes/user-preferences.ts` returns 0.
    - `grep -c 'row\\.hiddenConversationIds\\|updates\\.hiddenConversationIds\\|userPreferences\\.hiddenConversationIds' src/backend/database/routes/user-preferences.ts` returns 0.
    - `grep -c 'writeIdentityFile.*\\.hidden\\|removeIdentityFile.*\\.hidden' src/backend/database/routes/user-preferences.ts` >= 2.
    - `grep -c 'identityFileExists.*\\.hidden' src/backend/database/routes/user-preferences.ts` >= 1.
    - `grep -c 'H3 lowercase-on-disk invariant' src/backend/database/routes/user-preferences.ts` >= 2 (existing pin comment block preserved + new hidden comment block added).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend PUT body → user-preferences handler | Untrusted input: hiddenConversationIds array + identityHosts map (identityHosts reused verbatim from pin path — same trust surface, no new attack vectors) |
| user-preferences handler → per-identity-file primitive | Backend-internal; primitive re-gates identityKey against IDENTITY_KEY_RE (H1 lock from Phase 92 Plan 01) and relPath against the three-item whitelist (Phase 107 Plan 01 extended the set to include `.hidden`) |
| Backend → target host SFTP | Same auth path identity-birth uses (Phase 77 → Phase 92 Plan 01 primitive) — no new attack surface for `.hidden`; delegates to writeMarkdownFileAtomic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-02-01 | Tampering | Sentinel forgery on compromised host — attacker touches `.hidden` for identities that should be visible | accept | Hidden visibility is user-cosmetic; no authorization signal derives from hidden state; no privilege escalation possible. Same acceptance shape as T-92-02-01 for pinned. |
| T-107-02-02 | Elevation of Privilege | Wire write from unauthorized caller invoking writeIdentityFile for `.hidden` | mitigate | writeIdentityFile only called from authenticated JWT-gated PUT /user-preferences handler; primitive's gates (stricter IDENTITY_KEY_RE + ALLOWED_REL_PATHS whitelist including `.hidden` per Plan 01) are defense-in-depth |
| T-107-02-03 | Race Condition | Two concurrent hide toggles for the same identity | accept | Sentinel write is idempotent (touch or unlink is atomic on POSIX filesystems); UI toggle is user-serial per D-06 semantics; on the rare race, last-write-wins on the fs is fine per D-01 "presence is meaning" |
| T-107-02-04 | Denial of Service | Client submits massive hiddenConversationIds array | mitigate | Existing HIDDEN_CONVERSATION_IDS_MAX_LENGTH=1000 cap preserved verbatim in HID-107-PUT-07c |
| T-107-02-05 | Denial of Service | identityHosts map with N different hostIds forces N SSH conn opens per PUT — worse when pin + hidden BOTH run | mitigate | connByHost map SHARED between pin fanout and hidden fanout (Task 2 action step 6) — one conn per unique host across BOTH slices; overall PUT bounded by MAX_LENGTH * conn-timeout budget |
| T-107-02-06 | Data Loss / Information Disclosure | `.hidden` sentinel body written with sensitive contents by accident | mitigate | Plan 01 primitive threads empty string `""` through unchanged; Plan 01 Test H-03 asserts zero-byte contract; Plan 02 fanout also passes `""` verbatim — sentinel is always zero bytes |
| T-107-02-07 | Tampering | Response echo trusts client input rather than re-reading disk | mitigate | Post-fanout re-derive of disk state for BOTH pin and hidden echoes — the echoed arrays are AUTHORITATIVE disk state, not the client-submitted sets. Same D-06 truth-first invariant as pin path. |
| T-107-02-08 | Tampering | H3 stealth case-coercion between PUT body and primitive fanout writes a `.hidden` sentinel to a folder no reader can see | mitigate | HID-107-PUT-08 (H3 lock) asserts writeIdentityFile receives the identityKey byte-for-byte from the parseIdentityHosts-lowercased map. HID-107-06 asserts the read-side fanout also passes verbatim. Primitive's IDENTITY_KEY_RE gate rejects any uppercase key that somehow slipped through, so the failure is loud (throw) rather than silent (sentinel-written-to-invisible-folder). |
| T-107-02-09 | Denial of Service | Combined pin+hidden PUT floods the target box with 2N sentinel writes | accept | Fanouts run in parallel via Promise.all inside the shared connByHost; a hostile client can already dispatch pin-only or hidden-only 1000-key PUTs — the combined case doubles the ceiling but is bounded by (2 * 1000) * conn-timeout, still well below any resource-exhaustion threshold. Rate-limiting is JWT-scoped upstream. |
</threat_model>

<verification>
- Task 1: publicIdentity signature grows a seventh `hidden: boolean = false` arg; disk-fanout probes identityFileExists for `.hidden` in the same Promise.all wave as `.pinned`; nine new tests pass (PUB-107-signature-1/2/3 + HID-107-01..06 including HID-107-06 H3 verbatim-passthrough); existing PUB-92-* tests unregressed.
- Task 2: PUT handler adds hidden fanout as a sibling block to the pin fanout, sharing connByHost inside one try/finally; GET no longer reads hiddenConversationIds from the row; existing pin slice tests unchanged; twelve new tests pass (HID-107-PUT-01..10 + HID-107-GET-01 + HID-107-DB-01); existing user-preferences.test.ts pin suite green.
- Manual grep-hygiene (all run from cd /home/ubuntu/skynet-vega):
  - `grep -c 'parseHiddenConversationIds' src/backend/database/routes/user-preferences.ts` returns 0.
  - `grep -c 'row\\.hiddenConversationIds\\|updates\\.hiddenConversationIds\\|userPreferences\\.hiddenConversationIds' src/backend/database/routes/user-preferences.ts` returns 0.
  - `grep -c 'identityFileExists.*\\.hidden' src/backend/database/routes/identities.ts` >= 1.
  - `grep -c 'H3 lowercase-on-disk invariant' src/backend/database/routes/` returns >= 3 (one per handler: identities.ts, user-preferences.ts pin block, user-preferences.ts hidden block).
  - `grep -c '\\.toLowerCase\\|\\.toUpperCase' src/backend/database/routes/user-preferences.ts` returns exactly 1 (parseIdentityHosts entry-boundary lowercase only).
- Backend build sanity: `cd /home/ubuntu/skynet-vega && npm run build:backend 2>&amp;1 | tail -20` exits 0 with no new type errors (per fleet role directive — tsc --noEmit alone doesn't catch backend errors).
</verification>

<success_criteria>
- Backend read path exposes `hidden: boolean` per identity, on-demand from disk (SC-2, D-03).
- Backend write path fans out to per-identity `.hidden` sentinel writes/removes synchronously (SC-3, D-05).
- The `user_preferences.hidden_conversation_ids` DB column is no longer read or written anywhere in src/ — proven by grep-hygiene.
- Plan 03 can safely drop the physical column now that nothing consumes it (D-02).
- Combined pin+hidden PUT bodies share the connByHost map — one SSH conn per unique host across BOTH fanouts (HID-107-PUT-09).
- H3 lock: every identityKey argument that reaches writeIdentityFile / removeIdentityFile / identityFileExists for `.hidden` matches the on-disk folder name byte-for-byte. HID-107-06 + HID-107-PUT-08 are the regression traps.
- Pin path byte-for-byte unchanged — existing PUT-92-* + PUB-92-* tests all pass verbatim.
</success_criteria>

<output>
Create `.planning/phases/107-hide-identity-rows-via-disk-sentinel-mirror-phase-92-for-the/107-02-SUMMARY.md` when done, documenting:
- The publicIdentity signature extension (seventh arg `hidden: boolean` fail-closed default, sibling to pinned).
- The parallel `.hidden` probe in the disk-fanout Promise.all wave (no added round-trip per identity; two stat probes per fanout entry).
- The PUT hidden fanout delta-diff algorithm mirroring the pin path (previousSet from disk, nextSet from body, symmetric diff → writes+removes).
- The connByHost sharing invariant for combined pin+hidden PUT bodies (one conn per unique host across BOTH fanouts inside the shared try/finally).
- The response-echo re-derivation for hidden (disk is truth, not client input) — D-06 truth-first invariant preserved.
- Confirmation that pin slice (PUT-92-* + PUB-92-*) is byte-for-byte unchanged.
- Confirmation that the DB decoupling is complete for hidden AND pinned (HID-107-DB-01 asserts both columns untouched post-fanout — the D-02 regression trap for Phase 107's scope reversal from Phase 92's out-of-scope call).
- H3 lowercase-on-disk invariant: where it's enforced for `.hidden` (parseIdentityHosts entry boundary — reused verbatim from pin path — plus primitive IDENTITY_KEY_RE gate + listIdentityKeysOnHost read boundary), where the regression traps live (HID-107-PUT-08 + HID-107-06).
</output>
</content>
</invoke>