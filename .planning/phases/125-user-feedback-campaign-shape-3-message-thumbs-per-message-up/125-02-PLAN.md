---
phase: 124-user-feedback-campaign-shape-3-message-thumbs-per-message-up
plan: 02
type: execute
wave: 2
depends_on:
  - 125-01
files_modified:
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-view/PrettyView.feedback-thumbs.test.tsx
autonomous: true
requirements:
  - D-12
  - D-13
  - D-14
  - D-18
  - D-19
  - D-23
  - D-24
  - D-25
  - D-27
  - D-32
  - D-33
  - D-34
  - D-35
  - D-36
  - D-37
  - D-38
  - D-41
  - D-42
  - D-43
  - D-44
  - D-45
  - D-46
  - D-53
  - D-54

must_haves:
  truths:
    - "PrettyView threads two new callback props to ChatMessage (only the ChatMessage-branch of the message-type conditional at L3951-3970): `onThumbsUp={handleThumbsUp}` and `onThumbsDown={handleThumbsDown}` (D-38)."
    - "`handleThumbsUp(eventId)` builds the shape-1 payload `{ kind: 'thumbs_up', userNote: '', messageRef: eventId, exchangeText }` where `exchangeText` is computed from the assistant message content + the immediately-prior user turn's content (D-18/D-32/D-33/D-34); calls `postFeedback(payload)` (void, fire-and-forget per Pattern S-02); fires `toast.success('Thanks — feedback sent.', { duration: 2000 })` (D-19/Pattern S-03)."
    - "`handleThumbsDown(eventId)` computes the same exchangeText and stores `{ eventId, exchangeText }` in a PrettyView-local state `feedbackModalContext`; setting this state opens a PrettyView-mounted `<FeedbackModal variant='thumbs_down' />` (D-23)."
    - "FeedbackModal's `onSubmit(userNote)` handler fires `postFeedback({ kind: 'thumbs_down', userNote, messageRef: feedbackModalContext.eventId, exchangeText: feedbackModalContext.exchangeText })` + `toast.success` (D-25 submit path) then clears `feedbackModalContext` back to null."
    - "FeedbackModal's `onDismissWithoutSubmit` handler fires `postFeedback({ kind: 'thumbs_down', userNote: '', messageRef, exchangeText })` + `toast.success` (D-25 dismiss path); the modal is closed via `onOpenChange(false)` which sets context back to null."
    - "For an assistant message with a prior user turn: `exchangeText` includes both (format: labeled two-block or blockquote — planner picks; document choice in summary) (D-33)."
    - "For an assistant message with NO prior user turn (edge case — assistant greeting opens the conversation): `exchangeText` contains ONLY the assistant content (D-35)."
    - "RelayInboundBubble, WaitingBubble, RelayOutboundBubble, ImageBubble, MalformedBubble, user bubbles, and the pending-send optimistic bubble path are ALL untouched by this plan — no `onThumbsUp` / `onThumbsDown` props threaded to them (D-12/D-13/D-14)."
    - "No changes to any file under `src/backend/feedback/`, `src/ui/feedback/`, or `src/ui/features/pretty-conversations/` (D-36)."
  artifacts:
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "thumbs-up + thumbs-down plumbing: exchangeText computation + postFeedback fires + FeedbackModal thumbs_down mount"
      contains: "handleThumbsUp"
    - path: "src/ui/features/pretty-view/PrettyView.feedback-thumbs.test.tsx"
      provides: "D-53 test cases 8-11 (exchangeText plumbing + RelayInboundBubble/WaitingBubble/UserBubble unaffected)"
      contains: "describe"
  key_links:
    - from: "src/ui/features/pretty-view/PrettyView.tsx"
      to: "src/ui/features/pretty-view/ChatMessage.tsx (onThumbsUp / onThumbsDown props from Plan 01)"
      via: "prop threading at the ChatMessage invocation site (L3951-3970)"
      pattern: "onThumbsUp=|onThumbsDown="
    - from: "src/ui/features/pretty-view/PrettyView.tsx"
      to: "src/ui/feedback/feedback-api.ts (postFeedback)"
      via: "fire-and-forget POST /feedback via `void postFeedback(payload)` per Pattern S-02"
      pattern: "void postFeedback\\("
    - from: "src/ui/features/pretty-view/PrettyView.tsx"
      to: "src/ui/feedback/FeedbackModal.tsx"
      via: "Radix Dialog mount with variant='thumbs_down' + local `feedbackModalContext` atom per Pattern S-04"
      pattern: "<FeedbackModal"
    - from: "src/ui/features/pretty-view/PrettyView.tsx"
      to: "sonner toast singleton (mounted at src/main.tsx:243)"
      via: "toast.success invocation on every postFeedback fire path per Pattern S-03"
      pattern: "toast\\.success\\("
---

<objective>
Shape 3 second slice: the PrettyView-owned plumbing that turns ChatMessage's `onThumbsUp` / `onThumbsDown` callbacks (introduced in Plan 01) into real `postFeedback` fires and, for thumbs-down, into a `FeedbackModal` mount in `variant="thumbs_down"`. This plan (a) builds `exchangeText` for each assistant message by looking up the previous user-turn message in the `effectiveMessages` array (D-33/D-34) and handling the no-prior-turn edge case (D-35), (b) fires the thumbs-up payload immediately + toasts (D-18/D-19), (c) opens a PrettyView-mounted FeedbackModal for thumbs-down (D-23) — using a local `feedbackModalContext` atom of type `null | { eventId: string; exchangeText: string }` (Pattern S-04 recommendation option B), (d) fires the thumbs-down submit + dismiss payloads with the correct userNote (empty for dismiss, typed for submit) (D-25), (e) toasts on every fire path (D-19/D-27), (f) threads the callbacks ONLY to the ChatMessage invocation site and NOT to any other bubble type (D-12/D-13/D-14/D-36), and (g) adds a colocated test file covering D-53 cases 8-11 (plumbing + scope constraints).

Purpose: This slice completes the shape-3 wiring so the affordance introduced in Plan 01 actually reaches shape 1's backend + toast. It's the "new caller of shape 1" per shape 2's template.

Output: Modified `PrettyView.tsx` + new colocated test file `PrettyView.feedback-thumbs.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/125-user-feedback-campaign-shape-3-message-thumbs-per-message-up/125-CONTEXT.md
@.planning/phases/125-user-feedback-campaign-shape-3-message-thumbs-per-message-up/125-PATTERNS.md
@.planning/phases/125-user-feedback-campaign-shape-3-message-thumbs-per-message-up/125-01-SUMMARY.md
@.planning/shapes/shape-feedback-pipeline.closed.md
@.planning/shapes/shape-feedback-general-button.closed.md
@src/ui/features/pretty-view/PrettyView.tsx
@src/ui/features/pretty-view/ChatMessage.tsx
@src/ui/feedback/feedback-api.ts
@src/ui/feedback/FeedbackModal.tsx
@src/ui/AppShell.tsx
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PrettyView-local `feedbackModalContext` state → Radix Dialog render | Client-side modal open state, no server involvement |
| PrettyView handler → `postFeedback` → shape 1 backend | Existing authenticated POST /feedback route (T-121-* covers this); shape 3 adds no new fields |
| Modal typed `userNote` → wire payload | Existing shape-1 payload contract; content-inclusion gate + payload caps enforced server-side (T-121-* covers this) |

## STRIDE Threat Register

Shape 1's backend + payload threats (T-121-*) cover: transport auth, payload-cap validation, content-inclusion gating, exchangeText/messageRef normalization, CRLF stripping, size limits, SMTP failure handling, dev-chord production gating. Shape 3 adds NO new backend routes, NO new payload fields, NO new modal variants (D-36/D-37/D-41/D-43). Shape-3-scope threats below are all LOW severity.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-04 | Information Disclosure | `exchangeText` computed from prior user turn — could leak content the user did not intend to send | mitigate | Shape 1's server-side content-inclusion gate (`FEEDBACK_INCLUDE_CONTENT` env flag, default OFF) already strips `exchangeText` at the route boundary BEFORE `composeBody` when the flag is unset (per shape 1 close-out "content-inclusion opts in explicitly"). Shape 3 sends optimistically per D-18's client-forwards-content lock; the backend decides landing. No mitigation needed at this layer beyond honoring the contract. |
| T-125-05 | Information Disclosure | `exchangeText` prior-turn lookup accidentally reaches BACK into another user's relay content (RelayInboundBubble is the closest prior sibling of an assistant bubble in some views) | mitigate | Prior-turn lookup MUST filter for `type === "message"` AND `role === "user"`. RelayInbound / RelayOutbound / Image / Malformed frames MUST NOT be treated as the "prior user turn" — they carry `type === "relay_inbound"|"relay_outbound"|"image"|"malformed_line"` and are neither ChatMessage `role: "user"` entries nor part of the assistant/user exchange semantic. Test case D-53 case 11 covers the normal-exchange path; add a specific test that a RelayInbound frame sitting immediately before an assistant ChatMessage is skipped when computing prior-turn. |
| T-124-06 | Tampering | Client sends a fabricated `messageRef` (e.g., dev tools injecting a fake eventId) | accept | LOW: eventId is not a security assertion — it's a diagnostic marker in the operator's email body per shape 1's `messageRef` field. Backend already caps `messageRef` at 256 chars + strips CRLF at the route boundary per shape 1 feedback-routes.ts. No trust decision is made based on messageRef contents. |
| T-124-07 | Denial of Service | Rapid thumbs-down opens → dismisses cycle to spam empty-userNote payloads | accept | LOW: shape 1 D-28 accepted the risk; shape 3 D-28 keeps the accepted risk. `postFeedback` is fire-and-forget with server-side pre-send logging as the fallback record. |
| T-124-SC | Tampering | npm/pip/cargo installs | accept | LOW: no new dependencies added in this plan. `sonner`, `@/feedback/feedback-api`, `@/feedback/FeedbackModal` are all already imported elsewhere in the codebase (verified via AppShell.tsx L357-359 + L4165-4212 analog). |

Shared shape-1 threats (T-121-*) not re-litigated here per security_note guidance.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Modify PrettyView.tsx — wire onThumbsUp/onThumbsDown handlers + exchangeText computation + FeedbackModal mount</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/PrettyView.tsx (targeted read — L338-361 for StreamEvent + insertion-sort context, L3340-3350 for the existing `m.type === "message" && m.role === "user"` idiom the executor must mirror, L3880-3980 for the messages.map + ChatMessage invocation site, L1-30 for existing imports)
    - src/ui/api/claude-session-api.ts (L43-46 — verify `MessageEvent.type` is literally `"message"`, NOT undefined; the exchangeText lookup discriminator MUST use `m.type === "message"`)
    - src/ui/features/pretty-view/ChatMessage.tsx (after Plan 01 changes — verify the exact prop signatures of `onThumbsUp` and `onThumbsDown` match `(eventId: string) => void`)
    - src/ui/AppShell.tsx (L357-359 for the `feedbackOpen` atom analog + L4165-4212 for the FeedbackModal mount + submit/dismiss handler template — Pattern S-04)
    - src/ui/feedback/feedback-api.ts (verify `postFeedback` signature + `FeedbackPayload` type shape)
    - src/ui/feedback/FeedbackModal.tsx (verify `FeedbackModalProps` — `open`, `variant`, `onOpenChange`, `onSubmit`, `onDismissWithoutSubmit` signatures)
    - .planning/phases/125-user-feedback-campaign-shape-3-message-thumbs-per-message-up/125-CONTEXT.md (D-18, D-23, D-25, D-32, D-33, D-34, D-35, D-38 — the plumbing + exchangeText contract)
    - .planning/phases/125-user-feedback-campaign-shape-3-message-thumbs-per-message-up/125-PATTERNS.md ("Pattern S-04 FeedbackModal thumbs_down variant mount" section — the atom-open-submit-dismiss quartet template)
  </read_first>
  <behavior>
    - PrettyView holds a new local state atom: `const [feedbackModalContext, setFeedbackModalContext] = useState<null | { eventId: string; exchangeText: string }>(null);`. Initially `null` (modal closed).
    - Two new handler callbacks defined near the existing `handleLongPressSpeak` handler:
      - `handleThumbsUp(eventId: string): void` — locates the assistant message in `effectiveMessages` by `m.type === "message" && m.eventId === eventId && m.role === "assistant"`, computes `exchangeText` INLINE (see action prose — no separate helper function), calls `void postFeedback({ kind: "thumbs_up", userNote: "", messageRef: eventId, exchangeText })`, fires `toast.success("Thanks — feedback sent.", { duration: 2000 })`.
      - `handleThumbsDown(eventId: string): void` — same INLINE exchangeText computation, then `setFeedbackModalContext({ eventId, exchangeText })` (which opens the modal). Does NOT fire postFeedback here — the modal's onSubmit / onDismissWithoutSubmit handlers fire it (D-25).
    - The ChatMessage invocation site at L3951-3970 gets two new props threaded in: `onThumbsUp={handleThumbsUp}` and `onThumbsDown={handleThumbsDown}`.
    - RelayInboundBubble (L3927-3947), RelayOutboundBubble (L3914-3926), ImageBubble (L3906-3913), MalformedBubble (L3948-3949), and the pendingSends.map optimistic bubbles are ALL untouched — no thumbs prop threading (D-12/D-13/D-14).
    - A new `<FeedbackModal>` mount is added at PrettyView's return, sibling to the pretty-view UI (not inside the messages map). It reads open from `feedbackModalContext !== null` and always renders `variant="thumbs_down"` (D-23 lock — the modal-open-atom is thumbs_down-only in this plan). Its `onOpenChange(next)` handler: if `next === false`, sets `feedbackModalContext(null)` (Radix pattern: this fires on close-X click, backdrop click, and Escape key). Its `onSubmit(userNote)` handler: reads `feedbackModalContext`; if not null, fires `void postFeedback({ kind: "thumbs_down", userNote, messageRef: ctx.eventId, exchangeText: ctx.exchangeText })` + `toast.success(...)` + `setFeedbackModalContext(null)`. Its `onDismissWithoutSubmit()` handler: reads `feedbackModalContext`; if not null, fires `void postFeedback({ kind: "thumbs_down", userNote: "", messageRef: ctx.eventId, exchangeText: ctx.exchangeText })` + `toast.success(...)`. (Note: `onDismissWithoutSubmit` is called by FeedbackModal internally when the modal closes without a submit — matches AppShell L4204-4210's pattern; do not manually re-invoke it from `onOpenChange`.)
    - exchangeText format: for a normal user-then-assistant exchange, produce a two-block labeled text: `"**User:**\n\n${userContent}\n\n---\n\n**Assistant:**\n\n${assistantContent}"`. For the no-prior-user-turn edge case (D-35), produce ONLY `"**Assistant:**\n\n${assistantContent}"`. Do NOT strip or escape markdown — pass through verbatim per shape 1 D-21.
    - Prior-user-turn lookup: walk `effectiveMessages` backward from the assistant message's index, and select the first entry where `m.type === "message" && m.role === "user"` (per T-125-05 mitigation and matching the existing idiom at PrettyView.tsx L3346). Frames whose `type` is `"image"`, `"relay_outbound"`, `"relay_inbound"`, or `"malformed_line"` are skipped by this filter — they are NOT chat exchange turns. If no matching `role === "user"` ChatMessage entry is found before hitting index 0, emit assistant-only.
  </behavior>
  <action>
    Add imports at the top of `PrettyView.tsx` (near the existing feature-store imports): `import { postFeedback } from "@/feedback/feedback-api";`, `import { FeedbackModal } from "@/feedback/FeedbackModal";`, `import { toast } from "sonner";`. Verify these three imports do not conflict with any existing named imports in PrettyView.tsx via grep before adding.

    Inside the PrettyView component body, near the existing `handleLongPressSpeak` handler (grep for its definition to locate; use its location as the anchor for the new handlers), add:
    - `const [feedbackModalContext, setFeedbackModalContext] = useState<null | { eventId: string; exchangeText: string }>(null);`

    Define the two handlers as `useCallback`s. **Option A (chosen — inline computeExchangeText into each handler body).** Do NOT extract a separate `computeExchangeText` helper function; the exchange-text lookup logic lives INSIDE each `useCallback` body so `effectiveMessages` is the direct closure dependency and the `[effectiveMessages]` deps list is exhaustive-deps-correct. The logic inside both handlers is identical and looks like this (executor writes real TypeScript, not fenced code — this is the reference behavior):

    Inside `handleThumbsUp`: find the assistant message's index in `effectiveMessages` via `.findIndex((m) => m.type === "message" && m.eventId === eventId && m.role === "assistant")`. If not found, return early with no fire (defensive — should not happen for a normally-mounted assistant bubble). If found, walk backward from `assistantIdx - 1` down to `0` and select the first entry matching `m.type === "message" && m.role === "user"`. Build `exchangeText`: if a prior user entry was found, format as `"**User:**\n\n${priorUser.content}\n\n---\n\n**Assistant:**\n\n${assistant.content}"`; otherwise (no prior user turn — D-35 edge case) format as `"**Assistant:**\n\n${assistant.content}"`. Then call `void postFeedback({ kind: "thumbs_up", userNote: "", messageRef: eventId, exchangeText })` and `toast.success("Thanks — feedback sent.", { duration: 2000 })`. The `useCallback` deps list is `[effectiveMessages]`.

    Inside `handleThumbsDown`: same inline exchange-text computation (identical assistant-lookup + backward-walk + format), then `setFeedbackModalContext({ eventId, exchangeText })` — do NOT call `postFeedback` here. The `useCallback` deps list is `[effectiveMessages]`.

    The lookup discriminator MUST use `m.type === "message"` (matching `MessageEvent.type` at `src/ui/api/claude-session-api.ts:43-46` and the existing PrettyView idiom at L3346), NOT `m.type === undefined`. Frames whose `type` is `"image"`, `"relay_outbound"`, `"relay_inbound"`, or `"malformed_line"` do NOT match this filter and are correctly skipped from the exchange-turn walk.

    Modify the ChatMessage invocation at L3951-3970: add `onThumbsUp={handleThumbsUp}` and `onThumbsDown={handleThumbsDown}` as new prop lines alongside the existing prop list. Do NOT add these props to RelayInboundBubble, RelayOutboundBubble, ImageBubble, MalformedBubble, or WaitingBubble invocations — verify via grep after the edit that only the ChatMessage-branch invocation has the new props.

    Add the FeedbackModal mount at PrettyView's return, near the outer end of the returned JSX tree (sibling to the main pretty-view UI wrapper — NOT nested inside the messages map). The mount takes: `open={feedbackModalContext !== null}`, `variant="thumbs_down"` (D-23/D-37 lock — the ONLY variant this plan uses), `onOpenChange={(next) => { if (!next) setFeedbackModalContext(null); }}`, `onSubmit={(userNote) => { if (!feedbackModalContext) return; void postFeedback({ kind: "thumbs_down", userNote, messageRef: feedbackModalContext.eventId, exchangeText: feedbackModalContext.exchangeText }); toast.success("Thanks — feedback sent.", { duration: 2000 }); setFeedbackModalContext(null); }}`, and `onDismissWithoutSubmit={() => { if (!feedbackModalContext) return; void postFeedback({ kind: "thumbs_down", userNote: "", messageRef: feedbackModalContext.eventId, exchangeText: feedbackModalContext.exchangeText }); toast.success("Thanks — feedback sent.", { duration: 2000 }); }}`. (Note: `onDismissWithoutSubmit` is invoked by FeedbackModal itself when the modal closes without a submit; do not manually invoke it from `onOpenChange`.)

    This yields exactly three distinct `postFeedback` call sites in PrettyView.tsx: (1) handleThumbsUp's fire, (2) modal onSubmit fire, (3) modal onDismissWithoutSubmit fire. Plus one `postFeedback` import at the top of the file = 4 total occurrences of the token `postFeedback` in the file. Each of the three call sites carries a distinct D-XX behavior (D-18 thumbs_up path, D-25 thumbs_down submit path, D-25 thumbs_down dismiss path) — do NOT consolidate them into a shared helper. Distinct `kind` values (`"thumbs_up"` vs `"thumbs_down"`) and distinct `userNote` values (`""` for thumbs_up, typed for submit, `""` for dismiss) mean these three fires cannot share a body.

    Do NOT touch: any file under `src/backend/feedback/`, `src/ui/feedback/`, or `src/ui/features/pretty-conversations/` (D-36 hard lock). Do NOT touch `src/ui/AppShell.tsx` — its existing FeedbackModal mount for the dev chord stays as-is; shape 3 mounts its own second modal at PrettyView level per Pattern S-04 option (B) rationale (self-contained, no cross-boundary coupling).

    Do NOT introduce new modal variants — always `variant="thumbs_down"` when this mount is open (D-37).
  </action>
  <verify>
    <automated>cd /home/ubuntu/fleet/identities/lark-box-maintainer/workspace/skynet && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'postFeedback' src/ui/features/pretty-view/PrettyView.tsx` returns exactly 4 — 1 import + 3 distinct call sites (handleThumbsUp path, modal onSubmit path, modal onDismissWithoutSubmit path). Each call site fires with a distinct combination of `kind` and `userNote` per D-18/D-25 and MUST remain a separate expression (do NOT collapse into a shared helper — that would elide the D-25 lock that submit-OR-dismiss both fire).
    - Source assertion: `grep -c 'toast.success' src/ui/features/pretty-view/PrettyView.tsx` returns at least 3 (thumbs-up path + modal-submit path + modal-dismiss path).
    - Source assertion: `grep -c '<FeedbackModal' src/ui/features/pretty-view/PrettyView.tsx` returns exactly 1 (one modal mount).
    - Source assertion: `grep -n 'variant="thumbs_down"' src/ui/features/pretty-view/PrettyView.tsx` matches on the FeedbackModal mount (the ONLY variant this plan uses; D-37 lock — no other variants introduced).
    - Source assertion: `grep -n 'onThumbsUp={handleThumbsUp}' src/ui/features/pretty-view/PrettyView.tsx` matches exactly once (on the ChatMessage invocation, not on RelayInboundBubble/RelayOutboundBubble/ImageBubble).
    - Source assertion: `grep -n 'onThumbsDown={handleThumbsDown}' src/ui/features/pretty-view/PrettyView.tsx` matches exactly once.
    - Source assertion: `grep -B 2 'onThumbsUp' src/ui/features/pretty-view/PrettyView.tsx | grep -c 'RelayInboundBubble\|RelayOutboundBubble\|ImageBubble\|MalformedBubble\|WaitingBubble\|pending'` returns 0 (thumbs callbacks appear ONLY on the ChatMessage invocation, never near a non-ChatMessage bubble).
    - Discriminator assertion: `grep -n 'm.type === "message"' src/ui/features/pretty-view/PrettyView.tsx` shows at least 3 matches after the edit — one pre-existing (L3346) plus one inside handleThumbsUp's inline lookup plus one inside handleThumbsDown's inline lookup. NO occurrence of `m.type === undefined` inside either new handler (that discriminator is factually wrong per `MessageEvent.type: "message"` at src/ui/api/claude-session-api.ts:43-46).
    - Hooks-lint assertion: `computeExchangeText` MUST NOT appear as a separately-defined helper function or `const` in PrettyView.tsx — the exchange-text lookup is inlined into each of the two `useCallback` bodies (Option A). `grep -c 'computeExchangeText' src/ui/features/pretty-view/PrettyView.tsx` returns 0.
    - Source assertion: `git diff --stat src/backend/feedback/ src/ui/feedback/ src/ui/features/pretty-conversations/` shows no changes in these paths (D-36 hard lock).
    - Source assertion: `git diff --stat src/ui/AppShell.tsx` shows no changes (this plan does not touch AppShell per PATTERNS.md option (B) rationale).
    - Type-check assertion: `npx tsc --noEmit` exits 0.
    - Behavior assertion (deferred to Task 2 test file): the plumbing behaviors above are exercised by the D-53 case 8-11 tests in Task 2.
  </acceptance_criteria>
  <done>
    PrettyView.tsx compiles with `npx tsc --noEmit`; the file exposes `handleThumbsUp` and `handleThumbsDown` handlers wired to the ChatMessage invocation only; the FeedbackModal mount exists exactly once with `variant="thumbs_down"` and correct submit/dismiss handlers; exchangeText is computed inline in each handler using the `m.type === "message"` discriminator (matching MessageEvent.type at claude-session-api.ts:43-46 and PrettyView.tsx:3346); NO separate `computeExchangeText` helper exists (Option A); exchangeText is correct for both the normal exchange and the no-prior-user-turn edge case; no feedback-pipeline files or shape-2 files were touched. Test-driving of behavior happens in Task 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add colocated test file PrettyView.feedback-thumbs.test.tsx covering D-53 cases 8-11</name>
  <files>src/ui/features/pretty-view/PrettyView.feedback-thumbs.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/PrettyView.tsx (after Task 1 changes — verify the new handler names, the FeedbackModal mount, and the ChatMessage prop threading)
    - src/ui/features/pretty-view/PrettyView.autoplay.test.tsx (structure template for PrettyView-scope tests — how it constructs a fixture messages array, mocks, and asserts on rendered bubble types)
    - src/ui/features/pretty-view/ChatMessage.feedback-thumbs.test.tsx (from Plan 01 — mock recipe template for feedback + voice mocks)
    - src/ui/feedback/FeedbackModal.tsx (verify `data-testid="feedback-close"` at L168 and `data-testid="feedback-send"` at L206 — these are the stable selectors Test 4 and Test 5 must use)
    - .planning/phases/125-user-feedback-campaign-shape-3-message-thumbs-per-message-up/125-CONTEXT.md (D-53 cases 8-11 — the plumbing test coverage this file owns)
    - .planning/phases/125-user-feedback-campaign-shape-3-message-thumbs-per-message-up/125-PATTERNS.md ("D-53 case 8-10 sanity check" paragraph — the fixture messages array recipe)
  </read_first>
  <behavior>
    - Test suite name: `describe("PrettyView feedback-thumbs plumbing (shape 3)", () => { ... })`.
    - **Test 1 — Thumbs-up fires postFeedback with correct payload including exchangeText (D-53 case 3 plumbing + case 11 exchangeText):** Mock `useFeedbackEnabled` to return `true`. Mock `postFeedback` and `toast.success` as vi.fn spies. Render PrettyView with a fixture messages array containing `[{ type: "message", role: "user", content: "What is 2+2?", eventId: "u1", ts: 1 }, { type: "message", role: "assistant", content: "It is 4.", eventId: "a1", ts: 2 }]` (both as regular ChatMessage-type entries — `type: "message"` per MessageEvent contract). `fireEvent.click(screen.getByTestId("pv-chat-message-thumbs-up"))` (there will be exactly one thumbs-up button, the one on the a1 assistant bubble). Assert: `postFeedback` called exactly once with `{ kind: "thumbs_up", userNote: "", messageRef: "a1", exchangeText: <string containing both "What is 2+2?" AND "It is 4."> }`; `toast.success` called with the exact text `"Thanks — feedback sent."` and `{ duration: 2000 }`.
    - **Test 2 — exchangeText for assistant-only (no prior user turn) (D-53 case 11 edge):** Mock as Test 1. Render PrettyView with a fixture messages array of just `[{ type: "message", role: "assistant", content: "Hello — how can I help?", eventId: "a-greet", ts: 1 }]` (no prior user turn). Click thumbs-up. Assert: `postFeedback` called with `messageRef: "a-greet"` and `exchangeText` containing `"Hello — how can I help?"` but NOT containing any `**User:**` block prefix.
    - **Test 3 — Thumbs-down opens FeedbackModal in thumbs_down variant (D-53 case 4):** Mock as Test 1. Render PrettyView with the same fixture as Test 1. `fireEvent.click(screen.getByTestId("pv-chat-message-thumbs-down"))`. Assert: an element with `data-testid="feedback-dialog"` (FeedbackModal.tsx L141) is now present; `postFeedback` has NOT yet been called (fire happens on submit/dismiss, not on open).
    - **Test 4 — Modal submit fires thumbs_down payload with userNote (D-53 case 4):** Mock as Test 1. Click thumbs-down to open modal. Type text into the modal's textarea (find via `screen.getByPlaceholderText("Anything you want to add?")` per shape 1 D-11 thumbs_down placeholder). Click the Send button via `screen.getByTestId("feedback-send")` (FeedbackModal.tsx L206). Assert: `postFeedback` called with `{ kind: "thumbs_down", userNote: <typed text>, messageRef: "a1", exchangeText: <string containing both user + assistant content> }`; `toast.success` called; modal is closed (dialog element removed).
    - **Test 5 — Modal dismiss (close-X) fires thumbs_down payload with EMPTY userNote (D-53 case 5):** Mock as Test 1. Click thumbs-down to open modal. Click `screen.getByTestId("feedback-close")` (the close-X button at FeedbackModal.tsx L168 with `aria-label="Close without note"`). This triggers Radix's DialogPrimitive.Close → `onOpenChange(false)` → FeedbackModal's `onDismissWithoutSubmit` wrapper (per FeedbackModal.tsx L124-134). Assert: `postFeedback` called with `{ kind: "thumbs_down", userNote: "", messageRef: "a1", exchangeText: <same as Test 4> }`; `toast.success` called; modal is closed. Do NOT use `fireEvent.keyDown(document.body, { key: "Escape" })` — Radix Dialog handles Escape at the DialogPrimitive.Content level and the document.body path is flaky under jsdom; the stable-testid click is the reliable path.
    - **Test 6 — RelayInboundBubble unaffected when feedback ON (D-53 case 8):** Mock `useFeedbackEnabled` to return `true`. Render PrettyView with a fixture messages array containing a `relay_inbound` frame `{ type: "relay_inbound", room: "!r:s", sender: "@u:s", body: "external msg", eventId: "r1", ts: 1 }`. Assert: NO element with `data-testid="pv-chat-message-action-strip"` exists (RelayInboundBubble is a different component and Plan 01's strip only lives on ChatMessage); NO element with `data-testid="pv-chat-message-thumbs-up"` or `-thumbs-down` exists. (Note: RelayInboundBubble keeps its own in-bubble speak button — this test does NOT need to verify that; that's RelayInboundBubble's own responsibility.)
    - **Test 7 — WaitingBubble unaffected when feedback ON (D-53 case 9):** Render PrettyView with a fixture that triggers WaitingBubble render (planner-picks the exact PrettyView state that renders WaitingBubble — grep PrettyView.tsx for `<WaitingBubble` to find the trigger condition). Assert: NO `data-testid="pv-chat-message-action-strip"` or thumbs testids present.
    - **Test 8 — User bubble unaffected when feedback ON (D-53 case 10):** Mock `useFeedbackEnabled` to return `true`. Render PrettyView with a fixture messages array containing `{ type: "message", role: "user", content: "hi", eventId: "u-only", ts: 1 }` and no assistant reply. Assert: NO `data-testid="pv-chat-message-action-strip"`; NO thumbs testids. **(Intentional overlap note: this belt-and-suspenders coverage is deliberate — Plan 01 test 8 verifies the SAME D-53 case 10 from ChatMessage's perspective by rendering `<ChatMessage role="user">` directly; this Plan 02 test 8 verifies from PrettyView's perspective by rendering the full `<PrettyView>` tree with a user-message fixture. Both angles are cheap and worth keeping.)**
    - **Test 9 — Prior-turn lookup skips RelayInboundBubble frames (T-125-05 mitigation):** Mock `useFeedbackEnabled` to return `true`. Mock `postFeedback` spy. Render PrettyView with fixture `[{ type: "message", role: "user", content: "real user turn", eventId: "u1", ts: 1 }, { type: "relay_inbound", room: "!r:s", sender: "@u:s", body: "external relay content", eventId: "r1", ts: 2 }, { type: "message", role: "assistant", content: "reply to user1", eventId: "a1", ts: 3 }]`. Click thumbs-up on the a1 assistant bubble. Assert: `postFeedback` called with `exchangeText` containing `"real user turn"` and `"reply to user1"`, but NOT containing `"external relay content"` (the relay frame between them is skipped by the prior-turn lookup because its `type` is `"relay_inbound"`, not `"message"`).
  </behavior>
  <action>
    Create `src/ui/features/pretty-view/PrettyView.feedback-thumbs.test.tsx` mirroring the structure of `PrettyView.autoplay.test.tsx` (whatever mock stack it uses for PrettyView-scope tests) PLUS the feedback + voice mock recipe from Plan 01's `ChatMessage.feedback-thumbs.test.tsx`.

    Required module mocks (top-level before describe):
    - `vi.mock("@/feedback/feedback-store", () => ({ useFeedbackEnabled: vi.fn(() => true), }))` (default ON; individual tests override for OFF cases).
    - `vi.mock("@/feedback/feedback-api", () => ({ postFeedback: vi.fn(async () => {}), }))` (async no-op — Pattern S-02 fire-and-forget).
    - `vi.mock("sonner", () => ({ toast: { success: vi.fn() }, Toaster: () => null }))` (Toaster stub so any accidental import doesn't break test render).
    - Voice + player mocks (postSpeakStream, postSpeak, SAMPLE_PHRASE, createWebAudioStreamPlayer) copied from Plan 01's test file — required because assistant bubbles render the strip's speak button which touches these paths.
    - Any additional PrettyView-scope mocks that `PrettyView.autoplay.test.tsx` uses to enable a bare PrettyView render (e.g., mocking `useSyncExternalStore` sources, identity-hue context, pane-context providers). Read that file for the exact stack and copy it verbatim.

    Structure: nine `it(...)` cases per the `<behavior>` section above. Use `screen.getByTestId` for the strip + thumbs buttons; use `screen.queryByTestId` for negative assertions.

    Fixture rows for message-type entries MUST include the explicit `type: "message"` discriminator to match `MessageEvent` at `src/ui/api/claude-session-api.ts:43-46` — the prior-turn lookup in Task 1 filters on `m.type === "message"`, so a fixture missing this field will not be selected as a user turn (the tests exist to catch that regression, so the fixtures must be correct).

    For `postFeedback` payload assertions, use `expect(postFeedback).toHaveBeenCalledWith(expect.objectContaining({ kind: "thumbs_up", messageRef: "a1", exchangeText: expect.stringContaining("What is 2+2?") }))` — this style tolerates the exact exchangeText format the executor picks in Task 1 without brittle exact-string matching, while still verifying the key contract (both sides of the exchange present).

    For the modal-open test (Test 3), use `screen.getByTestId("feedback-dialog")` (FeedbackModal.tsx L141) as the stable selector; fall back to `screen.getByPlaceholderText("Anything you want to add?")` only if the testid selector proves unstable.

    For the modal-close test (Test 5), use `fireEvent.click(screen.getByTestId("feedback-close"))` (FeedbackModal.tsx L168). Do NOT use `fireEvent.keyDown(document.body, { key: "Escape" })` — that path is flaky in jsdom because Radix listens on DialogPrimitive.Content, not on document.body. The close-X click exercises the same `onOpenChange(false)` → `onDismissWithoutSubmit` code path and is reliable.

    Do NOT re-test any ChatMessage-internal behavior in this file — that lives in `ChatMessage.feedback-thumbs.test.tsx` (Plan 01). This file's scope is strictly PrettyView-level plumbing: exchangeText computation correctness, postFeedback fires with correct payloads, modal-open on thumbs-down, modal-submit + modal-dismiss firing paths, and the untouched-bubble-types assertions. Test 8 is an intentional PrettyView-scope re-verification of D-53 case 10 (user bubble unaffected) that complements Plan 01's ChatMessage-scope verification of the same case — this belt-and-suspenders overlap is deliberate, not a bug.
  </action>
  <verify>
    <automated>cd /home/ubuntu/fleet/identities/lark-box-maintainer/workspace/skynet && npx vitest related --run src/ui/features/pretty-view/PrettyView.tsx src/ui/features/pretty-view/PrettyView.feedback-thumbs.test.tsx 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `test -f src/ui/features/pretty-view/PrettyView.feedback-thumbs.test.tsx` exits 0.
    - Source assertion: `grep -cE '^\s*it\(' src/ui/features/pretty-view/PrettyView.feedback-thumbs.test.tsx` returns 9 (tolerant of any leading-whitespace scheme the executor uses; nine `it(...)` cases per the behavior list).
    - Source assertion: `grep -c 'vi.mock(\"@/feedback/feedback-store\"' src/ui/features/pretty-view/PrettyView.feedback-thumbs.test.tsx` returns 1.
    - Source assertion: `grep -c 'vi.mock(\"@/feedback/feedback-api\"' src/ui/features/pretty-view/PrettyView.feedback-thumbs.test.tsx` returns 1.
    - Source assertion: `grep -c 'vi.mock(\"sonner\"' src/ui/features/pretty-view/PrettyView.feedback-thumbs.test.tsx` returns 1.
    - Source assertion: `grep -c 'toHaveBeenCalledWith' src/ui/features/pretty-view/PrettyView.feedback-thumbs.test.tsx` returns at least 5 (four postFeedback payload assertions + at least one toast.success assertion).
    - Source assertion: `grep -c 'feedback-close' src/ui/features/pretty-view/PrettyView.feedback-thumbs.test.tsx` returns at least 1 (Test 5 uses the stable close-button testid, NOT a document.body Escape keydown).
    - Source assertion: `grep -c 'keyDown.*Escape' src/ui/features/pretty-view/PrettyView.feedback-thumbs.test.tsx` returns 0 (no Escape-on-document fallback per WARNING 1).
    - Behavior assertion: Test 1 verifies both user and assistant content in exchangeText via `expect.stringContaining(...)` on both strings.
    - Behavior assertion: Test 9 verifies the T-125-05 mitigation — an interposed relay_inbound frame does NOT contribute to exchangeText.
    - Test-command assertion: `npx vitest related --run src/ui/features/pretty-view/PrettyView.tsx src/ui/features/pretty-view/PrettyView.feedback-thumbs.test.tsx` exits 0 with all nine tests passing and no other regressions in the touched-file suite.
    - Type-check assertion: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    New test file exists, `vitest related --run` on the touched paths reports all nine tests green, no regressions in colocated PrettyView sibling suites, `tsc --noEmit` clean.
  </done>
</task>

</tasks>

<verification>
- Executor runs `npx vitest related --run src/ui/features/pretty-view/PrettyView.tsx src/ui/features/pretty-view/PrettyView.feedback-thumbs.test.tsx src/ui/features/pretty-view/ChatMessage.tsx src/ui/features/pretty-view/ChatMessage.feedback-thumbs.test.tsx` and gets a clean pass across both plans' tests.
- Executor runs `npx tsc --noEmit` and gets exit 0.
- Executor confirms via `git diff --stat` that no files under `src/backend/feedback/`, `src/ui/feedback/`, `src/ui/features/pretty-conversations/`, or `src/ui/AppShell.tsx` changed in this plan's diff.
- Executor confirms via grep that RelayInboundBubble/RelayOutboundBubble/ImageBubble/MalformedBubble/WaitingBubble invocations in PrettyView.tsx did NOT gain thumbs props.
</verification>

<success_criteria>
- PrettyView.tsx renders the FeedbackModal on thumbs-down click; fires postFeedback correctly on thumbs-up, thumbs-down submit, and thumbs-down dismiss with correct payloads.
- exchangeText correctly includes prior user turn for a normal exchange and correctly emits assistant-only for the no-prior-turn edge case.
- Relay/Waiting/User bubbles remain unaffected regardless of feedbackEnabled.
- All nine tests in PrettyView.feedback-thumbs.test.tsx pass.
- `tsc --noEmit` clean.
- No feedback-pipeline files or shape-2 files touched.
- Commit made on branch `feat/tab-title-from-tmux`.
</success_criteria>

<output>
Create `.planning/phases/125-user-feedback-campaign-shape-3-message-thumbs-per-message-up/125-02-SUMMARY.md` when done, following `@$HOME/.claude/get-shit-done/templates/summary.md`. Note in the summary: the exact `exchangeText` format string picked (two-block labeled vs blockquote — whichever the executor implemented); confirmation that the exchange-text lookup was inlined into each `useCallback` body (Option A — no separate `computeExchangeText` helper); confirmation that the modal-close test used `feedback-close` testid click (not Escape keydown); confirmation that AppShell.tsx was NOT touched (option B per PATTERNS.md); the count of shape-1 / shape-2 files untouched (should be zero touched); the nine test cases and their pass status.
</output>
</content>
