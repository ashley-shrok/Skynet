---
phase: 53-backend-authoritative-recycling-signal-one-wire-axis-two-con
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/api/fleet-status-types.ts
  - src/ui/state/session-working-store.ts
  - src/ui/state/session-working-store.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "fleet-status-types.ts's SessionState interface mirrors the Phase 53 wire schema by adding recycling?: boolean | null (browser-side mirror of the new backend field)"
    - "fleet-status-types.ts ALSO closes the Phase 52 documentation gap by adding dormant?: boolean | null to the same interface (per RESEARCH § Pitfall 7 — the field was added to the backend wire schema in Phase 52 but never mirrored on the browser side because tsconfig strict:false hid the compile-time gap)"
    - "session-working-store's WorkingRecord type gains a recycling: boolean field (default false), mirroring the Phase 52 dormant field exactly"
    - "publishFleetStatusSessionState grows an Axis E block (recycling) below Axis D (dormant), using the same direct swap-and-notify semantics: fire only when state_arg.recycling is not undefined AND differs from cache"
    - "Axis A (isWorking) republish preserves the cached recycling value (existing?.recycling ?? false) so a working-state flip cannot wipe a recycling:true set by a prior frame"
    - "advanceSessionLastMessageAt and advanceSessionAiTitle new-record creation paths ALSO seed recycling:false (dormant seed defaults extended for the new axis)"
    - "useSessionIsRecycling(key: string | null): boolean hook exists and returns strict boolean (null key → false; unknown key → false; record.recycling → the cached value)"
    - "The Phase 52 dormant tests remain green after the WorkingRecord type extension"
  artifacts:
    - path: "src/ui/api/fleet-status-types.ts"
      provides: "Browser-side mirror of SessionStateSchema with both dormant?: boolean | null (Phase 52 gap closure) and recycling?: boolean | null (Phase 53 new field)"
      contains: "recycling?: boolean | null"
    - path: "src/ui/state/session-working-store.ts"
      provides: "Fifth axis on WorkingRecord (recycling: boolean) + useSessionIsRecycling hook + Axis E swap-and-notify block + Axis A/B/C preservation of recycling"
      exports: ["useSessionIsRecycling"]
    - path: "src/ui/state/session-working-store.test.ts"
      provides: "Test coverage for Axis E — 7 cases mirroring Phase 52's dormant P52-01-i…vii"
      contains: "Phase 53 Plan 02"
  key_links:
    - from: "src/ui/state/session-working-store.ts (publishFleetStatusSessionState)"
      to: "WorkingRecord.recycling field"
      via: "swap-and-notify block reads state_arg.recycling"
      pattern: "state_arg\\.recycling"
    - from: "src/ui/state/session-working-store.ts (useSessionIsRecycling)"
      to: "WorkingRecord.recycling field"
      via: "getSnapshot returns record.recycling"
      pattern: "record\\.recycling"
    - from: "src/ui/api/fleet-status-types.ts (SessionState interface)"
      to: "browser-side type of the wire frame carrying the recycling field"
      via: "additive-optional interface field"
      pattern: "recycling\\?: boolean \\| null"
---

<objective>
Add the browser half of the backend-authoritative recycling signal:
- Mirror the new backend `recycling?: boolean | null` wire field on the
  frontend `SessionState` interface (fleet-status-types.ts). While the file
  is open, ALSO close the Phase 52 documentation gap by adding the missing
  `dormant?: boolean | null` mirror (RESEARCH § Pitfall 7 — the field was
  added to the backend wire in Phase 52 but never mirrored on the browser
  side because tsconfig `strict:false` hid the compile-time gap).
- Add a fifth axis to WorkingRecord (`recycling: boolean`, default false).
- Add an Axis E swap-and-notify block to publishFleetStatusSessionState
  BELOW the existing Axis D (dormant) block, following the dormant pattern
  verbatim.
- Preserve the cached recycling value across Axis A (isWorking) republishes
  by adding `recycling: existing?.recycling ?? false` to the Axis A nextMap
  write (defensive against the Pitfall-3 pattern documented in RESEARCH).
- Extend the dormant-seed defaults in advanceSessionLastMessageAt and
  advanceSessionAiTitle new-record creation paths so recycling starts false.
- Export a new hook `useSessionIsRecycling(key: string | null): boolean`.
- Add 7 test cases mirroring Phase 52's dormant P52-01-i…vii shape.

Browser-only change. Runs in parallel with Plan 53-01 (backend wire +
poller) because the two plans touch zero shared files. Plan 53-03
(consumer swap + retirement) depends on this plan's exported hook.

Fork-specific plan discipline: this plan stops at code + commit + tests
green. NO docker build, NO docker compose recreate, NO deploy. Full-suite
vitest is the ORCHESTRATOR's ship-gate; executors run scoped tests only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-backend-authoritative-recycling-signal-one-wire-axis-two-con/53-CONTEXT.md
@.planning/phases/53-backend-authoritative-recycling-signal-one-wire-axis-two-con/53-RESEARCH.md
@.planning/phases/52-convo-list-filter-restyle-popover-add-ready-toggle/52-01-PLAN.md
@src/ui/api/fleet-status-types.ts
@src/ui/state/session-working-store.ts
@src/ui/state/session-working-store.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mirror recycling + dormant fields on the frontend SessionState interface (fleet-status-types.ts)</name>
  <files>src/ui/api/fleet-status-types.ts</files>
  <read_first>
    - src/ui/api/fleet-status-types.ts (full file — lines 88-119, the SessionState interface; note the existing `lastMessageAt?: number | null` at line 106 and `aiTitle?: string | null` at line 118; note the header docblock at lines 1-17 stating "Field names and discriminants must match wire-protocol.ts EXACTLY. Any change to the backend wire-protocol MUST be mirrored here" — this task is that mirror)
    - src/backend/fleet-status/wire-protocol.ts (lines 127-174 — the Phase 52 dormant docblock at 127-154 and the current schema at 156-174 with `dormant: z.boolean().nullable().optional()` at line 173 that MUST be mirrored on the browser side; Phase 53 Plan 01 Task 1 adds `recycling: z.boolean().nullable().optional()` — this task mirrors both)
    - .planning/phases/53-backend-authoritative-recycling-signal-one-wire-axis-two-con/53-RESEARCH.md § Pitfall 7 (the exact statement that dormant was never mirrored to fleet-status-types.ts in Phase 52 because strict:false hid the gap; Phase 53 closes the gap while the file is already open)
  </read_first>
  <behavior>
    - The frontend `SessionState` interface at src/ui/api/fleet-status-types.ts (currently ending at line 119 with the aiTitle field) grows TWO new optional fields:
      1. `dormant?: boolean | null` — mirror of the Phase 52 backend field that was never added to the browser mirror.
      2. `recycling?: boolean | null` — mirror of the new Phase 53 backend field.
    - Both fields use the additive-optional pattern (nullable + optional) — identical shape to lastMessageAt / aiTitle mirrors above.
    - No behavior change at runtime — `strict: false` means existing code paths that read these fields off SessionState already compile; this change is documentation + type safety for future callers.
  </behavior>
  <action>
    In src/ui/api/fleet-status-types.ts:

    1. In the `SessionState` interface (lines 88-119), immediately AFTER the existing `aiTitle?: string | null;` field at line 118 and BEFORE the closing `}` at line 119, insert two new fields with docblocks:
       - First new field: `dormant?: boolean | null;` prefaced by a docblock header "Phase 52 (mirror gap-closure per Phase 53 Plan 02 Task 1)" followed by prose covering: source is the `~/.claude/identities/<tmuxSession>/.dormant` sentinel file on the target host; semantics `true` = sentinel present (identity parked by supervisor), `false` = sentinel absent (normal operation), `null` = normalised-null in transit (SSH error), `undefined` = emitting backend pre-dates Phase 52; frontend treats undefined and null identically (both → false at the session-working-store boundary); mirrors backend `SessionStateSchema.dormant`; added only now (Phase 53 Plan 02) because the field was previously undocumented on the browser mirror — tsconfig.app.json's `strict: false` allowed the omission to compile silently; adding it now gives future readers a canonical browser-side type; MUST stay in lockstep with the backend schema.
       - Second new field: `recycling?: boolean | null;` prefaced by a docblock header "Phase 53 Plan 02 (2026-08-21)" followed by prose covering: source is the caretaker's `~/.claude/identities/<tmuxSession>/.recycled-at` sentinel file on the target host — renamed from `.recycle-requested` at recycle-intent detection (before the outgoing claude PID exits), removed with an 8s delay after the fresh claude is up and driven through `/id`; scope is EXCLUSIVELY /id-reset-initiated identity replacement — NOT memory-cap restarts, NOT dormancy-wake, NOT connection drops; semantics `true` = sentinel present (recycle in flight), `false` = sentinel absent (any other state), `null` = normalised-null in transit (SSH error), `undefined` = emitting backend pre-dates Phase 53; frontend treats undefined and null identically (both → false at the session-working-store boundary — see session-working-store.ts Axis E block added in Task 2 below); mirrors backend `SessionStateSchema.recycling`; MUST stay in lockstep with the backend schema.
       - Follow the existing docblock style (JSDoc-comment single-line `//` prefixed) already used for the sibling `lastMessageAt?: number | null;` and `aiTitle?: string | null;` fields at lines 97-106 and 107-118.

    2. Do NOT touch the header docblock at lines 1-17. Do NOT touch the FRAME_SCHEMA_VERSION constant at line 23. Do NOT touch any other interface (BackgroundTask, ShellTask, etc., FrontendSubscribeFrame, FrontendSnapshotFrame, FrontendGoneFrame, etc.). Do NOT relax the `pid: number;` at line 92 to `pid: number | null;` — that Phase 52 Plan 01 Task 3 gap is out of scope for this plan (research recommendation is to close it opportunistically but the CONTEXT.md is silent on it; leave for a separate opportunistic cleanup).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "recycling?: boolean | null" src/ui/api/fleet-status-types.ts` returns exactly 1.
    - `grep -c "dormant?: boolean | null" src/ui/api/fleet-status-types.ts` returns exactly 1.
    - `grep -c "Phase 53 Plan 02" src/ui/api/fleet-status-types.ts` returns a value ≥ 1.
    - `grep -c "Phase 52 (mirror gap-closure per Phase 53 Plan 02" src/ui/api/fleet-status-types.ts` returns exactly 1.
    - `grep -c "FRAME_SCHEMA_VERSION = 1 as const" src/ui/api/fleet-status-types.ts` returns exactly 1 (schema version unchanged).
    - `npx tsc --noEmit 2>&amp;1 | grep -c "error TS"` returns 0.
  </acceptance_criteria>
  <done>
    Browser SessionState interface mirrors both new backend wire fields (dormant + recycling) with additive-optional shape; docblocks cite Phase 52 gap-closure rationale + Phase 53 scope-lock; no other interface changes; tsc clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Axis E (recycling) to WorkingRecord + Axis E block in publishFleetStatusSessionState + useSessionIsRecycling hook + Axis A/B/C preservation (session-working-store.ts + test)</name>
  <files>src/ui/state/session-working-store.ts, src/ui/state/session-working-store.test.ts</files>
  <read_first>
    - src/ui/state/session-working-store.ts (full file — the WorkingRecord type at lines 65-94 with the Phase 52 `dormant: boolean` field at line 93; the publishFleetStatusSessionState function at lines 155-267 with the Axis A block at lines 191-218, Axis B at 220-224, Axis C at 226-236, and Axis D (dormant) at lines 238-266 — Axis E MUST clone Axis D verbatim substituting recycling; the advanceSessionLastMessageAt helper at lines 318-349 with the nextRecord dormant default at line 335; the advanceSessionAiTitle helper at lines 406-433 with the nextRecord dormant default at line 419; the useSessionIsDormant hook at lines 527-535 that useSessionIsRecycling MUST clone verbatim; the getSessionWorkingSnapshot return type at lines 543-548 that must extend to include recycling; the __resetForTest helper at lines 634-637 that stays unchanged)
    - src/ui/state/session-working-store.test.ts (the Phase 52 Plan 01 dormant test suite at lines 998-1150 — the 7 test cases P52-01-i…vii that Phase 53 Task 2 tests MUST mirror; the makeState helper wherever declared for its default field shape — search for `function makeState` — new tests use `makeState({ status: "idle", recycling: true })` etc.)
    - .planning/phases/52-convo-list-filter-restyle-popover-add-ready-toggle/52-01-PLAN.md § Task 1 § Action § "Working-store changes" (the reference implementation Phase 53 clones for the Axis D → Axis E pattern)
    - .planning/phases/53-backend-authoritative-recycling-signal-one-wire-axis-two-con/53-RESEARCH.md § Pitfall 3 (Axis A must preserve cached recycling — critical or a working-state flip wipes recycling:true)
  </read_first>
  <behavior>
    - `WorkingRecord` gains a `recycling: boolean` field. Default `false` on cold-start.
    - `publishFleetStatusSessionState` grows an Axis E block BELOW the existing Axis D block, using direct swap-and-notify semantics identical to Axis D: fires ONLY when `state_arg.recycling !== undefined` AND the cached recycling value differs. Wire semantic: `recycling: true` sets true; `recycling: false` sets false; `recycling` absent (undefined) preserves cache; `recycling: null` at wire → collapsed to false at store boundary (`state_arg.recycling === true` check). Distinct from Axis A/B/C (max-wins for lastMessageAt, last-wins for aiTitle, direct-swap for isWorking).
    - Axis A's `nextMap.set(key, { ... })` writes MUST preserve the cached recycling via `recycling: existing?.recycling ?? false` — otherwise a working-state flip after a recycling:true frame wipes the recycling signal (Pitfall 3).
    - The Axis A log record MUST include `recycling: existing?.recycling ?? false` and `previousRecycling: existing?.recycling ?? false` alongside the existing dormant/previousDormant fields for observability parity.
    - `advanceSessionLastMessageAt` and `advanceSessionAiTitle` new-record creation paths MUST include `recycling: existing?.recycling ?? false` in the constructed nextRecord (dormant-seed defaults extended). The `WorkingRecord` type would otherwise fail to construct.
    - New exported hook: `useSessionIsRecycling(key: string | null): boolean` — clone of useSessionIsDormant (lines 527-535) with `record.dormant` substituted by `record.recycling`. Returns strict boolean (never null/undefined): null key → false; unknown key → false; record present → `record.recycling`.
    - `getSessionWorkingSnapshot` return type at line 545 grows a `recycling: boolean` field in the inline object shape.
    - `publishFleetStatusSessionGone` unchanged (deletes the whole record; recycling clears with it).
    - `__resetForTest` unchanged.
  </behavior>
  <action>
    A. src/ui/state/session-working-store.ts changes:

    A1. Extend the WorkingRecord type at lines 65-94 by adding a new field `recycling: boolean;` AFTER the existing Phase 52 `dormant: boolean` at line 93 and BEFORE the closing `};` at line 94. Prefix the field with a `//` docblock (mirror the style of the dormant docblock at lines 84-92) whose header reads "Phase 53 Plan 02 — the backend-authoritative recycling signal mirrored from the fleet-status wire frame (SessionState.recycling)" and whose body covers: `true` when the wire frame carried `recycling: true` (the caretaker's `.recycled-at` sentinel present, identity being replaced via /id-reset); `false` when the wire frame carried `false`, `null`, or omitted the field entirely (safe default — no false positives); wire's three-valued input (true / false / null / undefined) collapses to strict boolean at the store boundary in the Axis E block below via `state_arg.recycling === true`; hook consumers read a simple boolean; reconciliation is direct swap-and-notify (no max-wins, no last-wins) — recycling is a strict boolean gate on the sentinel file's presence; preserved across Axis A (isWorking) republishes via `recycling: existing?.recycling ?? false` in the Axis A nextMap write (defensive against the Pitfall-3 pattern documented in Phase 53 RESEARCH.md); consumed by useSessionIsRecycling(key), which Plan 53-03 wires into both the PrettyView holding overlay mount gate AND the PrettyConversationRow row-spinner input.

    A2. Update the Axis A log record at lines 192-207. After the existing `dormant: existing?.dormant ?? false,` field at line 202, insert a new `recycling: existing?.recycling ?? false,` field. After the existing `previousDormant: existing?.dormant ?? false,` field at line 206, insert a new `previousRecycling: existing?.recycling ?? false,` field. Do NOT split or renumber the log record; keep it a single `console.info({ ... })` call — just extend the object literal in place.

    A3. Update the Axis A `nextMap.set(key, { ... })` at lines 209-215. Immediately after the `dormant: existing?.dormant ?? false,` field at line 214 (before the closing `});` at line 215), insert `recycling: existing?.recycling ?? false,`. This preserves recycling across every isWorking flip and is the Pitfall-3 defense.

    A4. Update the header block comment at lines 1-58 to add a Phase 53 bullet. Insert a new `//` block AFTER the existing Phase 47 (Plan 03) bullet ending at line 54 and BEFORE the "Store pattern mirrors src/ui/state/conversation-store.ts" bullet at line 56. The new bullet's header reads "Phase 53 (Plan 02): the store now carries a FIFTH axis, recycling, with direct swap-and-notify semantics (like dormant Axis D)"; body covers: publish path grows an Axis E block below Axis D; sourced from the backend-authoritative `.recycled-at` sentinel plumbed by Plan 53-01; consumed by BOTH the PrettyView holding overlay AND the PrettyConversationRow row spinner via useSessionIsRecycling — retires the client-side session-recycling-store (Plan 53-03) which required a mounted pane to publish; see 53-CONTEXT.md § Shape (one source for both surfaces).

    A5. Add the new Axis E block in publishFleetStatusSessionState IMMEDIATELY AFTER the existing Axis D block (which ends at line 266 with the closing `}` of the outer `if (state_arg.dormant !== undefined) { ... }`) and IMMEDIATELY BEFORE the outer function's closing `}` at line 267. The block MUST be structurally identical to Axis D at lines 238-266, cloning verbatim with these substitutions:
       - Header comment: "── Axis E — recycling swap-and-notify block (Phase 53 Plan 02) ──" followed by prose describing the wire semantic (optional field: `recycling: true` sets true; `recycling: false` sets false (explicit reset); `recycling` absent (undefined) preserves the cached value — an Axis-A-only republish that carries no recycling signal must NOT wipe a recycling:true set by a prior frame; matches the optional-field convention on the wire), the direct swap-and-notify rationale (no max-wins, no last-wins — recycling is a strict boolean gate on the `.recycled-at` sentinel file's presence; fire only when we have an explicit signal AND it differs from cache; brand-new-key case is handled above in Axis A which now also preserves recycling in its nextMap write; here we only fire on change).
       - Outer guard predicate: `if (state_arg.recycling !== undefined) { ... }` (substitute `recycling` for Axis D's `dormant`).
       - Local const: `const recycling = state_arg.recycling === true;`
       - Existing-record lookup: `const existingAfterAxes = state.map.get(key);`
       - Inner change-detection predicate: `if (existingAfterAxes !== undefined && existingAfterAxes.recycling !== recycling) { ... }`.
       - Body of the inner if: build `const nextMap = new Map(state.map);`, call `nextMap.set(key, { ... })` writing the FIVE fields in the order isWorking / lastMessageAt / aiTitle / dormant / recycling — all four preserved fields sourced from `existingAfterAxes.<field>` and the new `recycling` field sourced from the local `recycling` const; then `state = { map: nextMap };` and `notify();`.

    A6. Update advanceSessionLastMessageAt (lines 318-349). In the nextRecord construction at lines 331-336, add `recycling: existing?.recycling ?? false,` on a new line AFTER the existing `dormant: existing?.dormant ?? false,` at line 335. This is a required WorkingRecord field so the object literal otherwise fails to type-check (once WorkingRecord grows the recycling field in A1).

    A7. Update advanceSessionAiTitle (lines 406-433). In the nextRecord construction at lines 415-420, add `recycling: existing?.recycling ?? false,` on a new line AFTER the existing `dormant: existing?.dormant ?? false,` at line 419. Same rationale as A6.

    A8. Update getSessionWorkingSnapshot (lines 543-548). Extend the inline object shape at line 545 by appending `; recycling: boolean` inside the inline `{ ... }` type literal. Before: `{ isWorking: boolean; lastMessageAt: number | null; aiTitle: string | null; dormant: boolean }`. After: `{ isWorking: boolean; lastMessageAt: number | null; aiTitle: string | null; dormant: boolean; recycling: boolean }`. Do NOT change the function body or signature otherwise.

    A9. Add the new exported hook `useSessionIsRecycling` AFTER the existing `useSessionIsDormant` at line 535 and BEFORE `getSessionWorkingSnapshot` at line 543. The hook body MUST be a structural clone of useSessionIsDormant at lines 527-535 with `record.dormant` substituted by `record.recycling`. Signature: `export function useSessionIsRecycling(key: string | null): boolean`. Body: define `const getSnapshot = (): boolean => { if (key === null) return false; const record = state.map.get(key); if (record === undefined) return false; return record.recycling; };` then `return useSyncExternalStore(subscribe, getSnapshot, getSnapshot);`. Prefix the export with a JSDoc `/** ... */` block whose header reads "Phase 53 Plan 02 — Hook: derive 'is this session recycling?' for a single key. Returns a strict boolean (never null/undefined)"; body enumerates the four snapshot cases (null key → false short-circuit; unknown key → false default-safe; recycling === false → false sentinel absent; recycling === true → true sentinel present identity being replaced via /id-reset); cites the source (caretaker's `~/.claude/identities/<tmuxSession>/.recycled-at` sentinel, plumbed via ssh-poll-orchestrator source A per Phase 53 RESEARCH § Assumption A1, see Phase 53 Plan 01); documents the two consumer surfaces (PrettyView holding overlay mount gate + PrettyConversationRow row-spinner input in Plan 53-03) and the retirement of the client-side session-recycling-store that previously bridged them.

    A10. Do NOT touch publishFleetStatusSessionGone. Do NOT touch subscribeSessionWorkingStore. Do NOT touch useSessionIsWorking / useSessionIsWorkingRaw / useSessionIsDormant. Do NOT touch getSessionLastMessageAt / useSessionLastMessageAt / getSessionAiTitle / useSessionAiTitle / seedSessionLastMessageAt / seedSessionAiTitle. Do NOT touch __resetForTest.

    B. src/ui/state/session-working-store.test.ts changes:

    B1. At the end of the file, AFTER the last existing describe block (the Phase 52 Plan 01 dormant tests ending after line 1150), add a new `//` header comment block mirroring the Phase 52 shape at lines 998-1010. Header line: "Phase 53 Plan 02 — Recycling axis (Axis E) + useSessionIsRecycling hook." Body prose: source is `~/.claude/identities/<tmuxSession>/.recycled-at` sentinel file on the target host (identity being replaced via /id-reset); boolean strict (`recycling: boolean` default false in WorkingRecord). Then enumerate the seven test IDs (Test P53-02-i through Test P53-02-vii) with one-line summaries matching the P52-01-i…vii shape at lines 1002-1008.

    B2. Add the import of useSessionIsRecycling from the store. Locate the existing import block that pulls in useSessionIsDormant (search for `useSessionIsDormant` at the top of the test file — the imports mirror the store's exports) and add `useSessionIsRecycling` alongside it as a named import.

    B3. Add a new `describe("session-working-store (Phase 53 Plan 02): recycling axis (Axis E) + useSessionIsRecycling hook", () => { ... })` block. Inside it, add the seven `it(...)` cases below, each mirroring the corresponding P52-01 case verbatim with `recycling` substituted for `dormant` and `useSessionIsRecycling` substituted for `useSessionIsDormant`:

       - **P53-02-i** — publish recycling:true → hook returns true. Mirror P52-01-i at lines 1013-1027. Use `renderHook(() => useSessionIsRecycling("h1:s1"))`; initial `result.current === false`; `act(() => { publishFleetStatusSessionState("h1", makeState({ status: "idle", recycling: true })); })`; rerender; `expect(result.current).toBe(true)`.

       - **P53-02-ii** — publish recycling:false → hook returns false. Mirror P52-01-ii at lines 1030-1043. Use `makeState({ status: "idle", recycling: false })`; assert `result.current === false`.

       - **P53-02-iii** — publish without recycling field → hook returns false. Mirror P52-01-iii at lines 1046-1057. Use `makeState({ status: "idle" })` (recycling omitted → undefined at wire → collapses to false at store boundary); assert `result.current === false`.

       - **P53-02-iv** — re-publish SAME recycling value → no additional notify beyond Axis A/B/C/D. Mirror P52-01-iv at lines 1060-1085. Subscribe callback via `subscribeSessionWorkingStore`; publish with `recycling:true` (captures Axis A + Axis E notifies as `n0`); publish AGAIN with the SAME `recycling:true` value and a bumped `updatedAt` (to prove the guard isn't just relying on shallow equality of the whole state); assert `cb.mock.calls.length === n0` (Axis D no-ops, Axis E no-ops, Axis A no-ops because isWorking unchanged, Axis B/C no-ops without lastMessageAt/aiTitle).

       - **P53-02-v** — toggle recycling while isWorking unchanged → Axis E notify fires. Mirror P52-01-v at lines 1088-1114. First publish with `recycling:false` (Axis A fires for new key, captures `n0`); toggle recycling to true (isWorking still idle → false, so Axis A no-ops); assert `cb.mock.calls.length === n0 + 1` (Axis E fires only). Assert `getSessionWorkingSnapshot().get("h1:s1")?.recycling === true`.

       - **P53-02-vi** — null key → false; unknown key → false. Mirror P52-01-vi at lines 1117-1125. `renderHook(() => useSessionIsRecycling(null))` → `result.current === false`; `renderHook(() => useSessionIsRecycling("never-published:key"))` → `result.current === false`.

       - **P53-02-vii** — recycling persists across Axis A (isWorking) republish. Mirror P52-01-vii at lines 1128-1150. First publish `recycling:true` + idle; then publish `status:"busy"` with recycling omitted (recycling undefined → Axis E does NOT fire; Axis A fires with isWorking flipping to true); assert `getSessionWorkingSnapshot().get("h1:s1")?.recycling === true` (Axis A's preservation of `existing?.recycling` from step A3 holds the value — this is the Pitfall-3 defense test).

    B4. If the existing `makeState` helper (search for `function makeState` in the file) does not accept a `recycling` field in its options, extend the helper: the signature likely accepts `Partial<SessionState>`; the mirror on fleet-status-types.ts (Task 1) already added the field so the helper's spread already accepts it via z.infer-mirroring type widening. Verify by locating the helper and running tsc — any error message will pinpoint the widening needed.
  </action>
  <verify>
    <automated>npx vitest run src/ui/state/session-working-store 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "recycling: boolean" src/ui/state/session-working-store.ts` returns a value ≥ 2 (WorkingRecord field + getSessionWorkingSnapshot inline type).
    - `grep -c "state_arg\.recycling" src/ui/state/session-working-store.ts` returns a value ≥ 2 (Axis E predicate + collapse expression).
    - `grep -c "existing\?\\.recycling" src/ui/state/session-working-store.ts` returns a value ≥ 4 (Axis A log record `recycling` + `previousRecycling`, Axis A nextMap write, advanceSessionLastMessageAt nextRecord, advanceSessionAiTitle nextRecord).
    - `grep -c "^export function useSessionIsRecycling" src/ui/state/session-working-store.ts` returns exactly 1.
    - `grep -c "useSessionIsRecycling" src/ui/state/session-working-store.ts` returns a value ≥ 2 (export + docblock reference).
    - `grep -c "P53-02-" src/ui/state/session-working-store.test.ts` returns a value ≥ 7 (seven test IDs across it names).
    - `grep -c "Phase 53 Plan 02" src/ui/state/session-working-store.test.ts` returns a value ≥ 1 (header block-comment).
    - `npx vitest run src/ui/state/session-working-store 2>&amp;1 | grep -E "Tests.*passed|Tests.*failed"` shows 0 failures. Existing Phase 52 Plan 01 dormant tests (P52-01-i…vii) MUST remain green — the WorkingRecord extension is backward-compatible.
    - `npx tsc --noEmit 2>&amp;1 | grep -c "error TS"` returns 0.
  </acceptance_criteria>
  <done>
    WorkingRecord grows Axis E (recycling: boolean default false); publishFleetStatusSessionState grows Axis E swap-and-notify block cloning Axis D shape; Axis A preserves cached recycling across isWorking flips (Pitfall 3 defense); advanceSessionLastMessageAt + advanceSessionAiTitle new-record paths seed recycling:false; useSessionIsRecycling hook exported with dormant-hook shape; getSessionWorkingSnapshot return type extended; 7 new test cases lock the axis contract; existing Phase 52 dormant tests remain green; tsc clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → browser (fleet-status WS) | Additive optional field; browser reads via JSON.parse wrapped in try/catch (T-34-18 mitigation from Phase 34); no zod on the browser side per the fleet-status-types.ts header docblock. `state_arg.recycling` is treated as `boolean | null | undefined` and collapsed to strict boolean at the Axis E store boundary via `=== true` check — malformed types (e.g., string, number) all evaluate to false, which is the safe default. |
| browser store → React consumers | useSessionIsRecycling returns strict boolean; consumers cannot receive null/undefined for the recycling axis; downstream row/overlay predicates read a plain boolean. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-02-01 | Tampering | JSON.parse of wire frames carrying recycling | mitigate | Existing WS onmessage try/catch (T-34-18) already logs + drops malformed frames without crashing. The Axis E `state_arg.recycling === true` check treats every non-`true` value (including malformed strings, numbers, undefined, null) as false — a compromised backend cannot force recycling:true via a malformed payload that survives try/catch. |
| T-53-02-02 | Information Disclosure | recycling axis visible in browser dev tools | accept | Same information already published to the retired session-recycling-store from PrettyView.tsx's useEffect; backend-authoritative source EXTENDS visibility to unmounted rows but reveals no NEW categorical information. Same profile as T-53-01-03 (backend side). |
| T-53-02-03 | Repudiation | State transitions on the recycling axis | accept | Axis A log record already carries `recycling` and `previousRecycling` for state-change observability. Axis E block does NOT emit its own log line (matches Axis D convention — no separate log to avoid noise). If future forensics needs per-axis-E log entries, add via a follow-up patch. |
| T-53-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs — hook additions only against existing React + useSyncExternalStore infrastructure. Package legitimacy gate N/A. |
</threat_model>

<verification>
- `npx vitest run src/ui/state/session-working-store 2>&amp;1 | grep -E "Test Files.*passed|Tests.*passed"` shows all suites passing (including Phase 52 P52-01-i…vii).
- `npx tsc --noEmit 2>&amp;1 | grep -c "error TS"` returns 0.
- `grep -c "^export function useSessionIsRecycling" src/ui/state/session-working-store.ts` returns 1 (hook available for Plan 53-03 consumers).
- `grep -c "recycling?: boolean | null" src/ui/api/fleet-status-types.ts` returns 1.
- `grep -c "dormant?: boolean | null" src/ui/api/fleet-status-types.ts` returns 1 (Phase 52 gap closed).
- `grep -c "P53-02-" src/ui/state/session-working-store.test.ts` returns ≥ 7 (seven test cases present).
</verification>

<success_criteria>
- fleet-status-types.ts mirrors both the Phase 52 dormant field (gap closure) and the Phase 53 recycling field (new addition) with additive-optional shape.
- session-working-store carries a fifth axis (recycling) with the same direct-swap-and-notify semantics as dormant.
- Axis A preserves cached recycling across working-state flips (Pitfall 3 defense).
- advanceSessionLastMessageAt + advanceSessionAiTitle new-record paths seed recycling:false so the WorkingRecord constructor stays satisfiable.
- useSessionIsRecycling hook exported for Plan 53-03 consumption.
- Test coverage locks all 7 P53-02-i…vii cases (mirror of Phase 52's dormant coverage).
- All existing Phase 52 dormant tests remain green.
- No new nginx work; no docker deploy tasks; no new package installs.
</success_criteria>

<output>
Create `.planning/phases/53-backend-authoritative-recycling-signal-one-wire-axis-two-con/53-02-SUMMARY.md` when done. Summary MUST include: exact line ranges touched in each file, snapshot of the extended WorkingRecord type, snapshot of the fleet-status-types.ts SessionState interface showing both dormant + recycling fields in position, the exact useSessionIsRecycling hook signature for Plan 53-03 to consume, and a note confirming the Axis A preservation (Pitfall 3 defense) is in place with the exact line reference.
</output>
