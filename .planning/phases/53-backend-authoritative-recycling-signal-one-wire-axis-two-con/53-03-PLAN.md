---
phase: 53-backend-authoritative-recycling-signal-one-wire-axis-two-con
plan: 03
type: execute
wave: 2
depends_on:
  - 53-01
  - 53-02
files_modified:
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-view/PrettyView.phase29.test.tsx
  - src/ui/features/pretty-view/PrettyView.test.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
  - src/ui/state/session-recycling-store.ts
  - src/ui/state/session-recycling-store.test.ts
  - src/ui/state/session-queue-pending-store.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "PrettyView.tsx no longer imports publishSessionRecycling from @/state/session-recycling-store"
    - "PrettyView.tsx no longer runs the useEffect that publishes to session-recycling-store on renderedState changes (the old effect body at lines 2415-2425 is deleted)"
    - "PrettyView.tsx computes const isRecycling = useSessionIsRecycling(sessionWorkingKey); at the same hook scope as isWorking (immediately after line 1074) reusing the existing sessionWorkingKey constant at line 1073"
    - "PrettyView.tsx SessionHoldingOverlay mount gate at line 2749 changes from `renderedState === \"holding\"` to `isRecycling` (backend-authoritative axis; blind rows now see their own recycling state)"
    - "PrettyView.tsx ComposeBox props `isHolding` (line 3105) and `recycleActive` (line 3109) both change from `renderedState === \"holding\"` to `isRecycling` — one source for all three consumer sites per CONTEXT.md scope-lock"
    - "PrettyConversationsPanel.tsx no longer imports useSessionRecycling from @/state/session-recycling-store"
    - "PrettyConversationsPanel.tsx PrettyConversationRowLive (line 219) swaps useSessionRecycling → useSessionIsRecycling; the isRecycling prop propagation to PrettyConversationRow at line 238 stays semantically identical (both hooks return boolean/strict-boolean for known keys)"
    - "session-recycling-store.ts is DELETED from disk"
    - "session-recycling-store.test.ts is DELETED from disk"
    - "session-queue-pending-store.ts docblock (lines 20-47, comment-only) is updated so retired references to session-recycling-store are removed or rephrased — no code change needed in that file"
    - "PrettyView.phase29.test.tsx grep-gate test 'publishes session-recycling on renderedState === \"holding\"' (line 219-223) is either DELETED or REPLACED with an assertion that PrettyView does NOT import from @/state/session-recycling-store AND does consume useSessionIsRecycling from the working-store"
    - "PrettyView.test.tsx Test F ('pane_state:holding trumps loading — SessionHoldingOverlay mounts') is updated so the fixture publishes a recycling:true wire frame to the working-store instead of relying on the pane_state:holding frame alone to drive the overlay mount"
    - "No production import of @/state/session-recycling-store exists anywhere in src/ after the plan completes"
    - "PrettyConversationsPanel.test.tsx does NOT need a schema change — the existing mockWorkingSnapshot typing already includes dormant; a per-test recycling seed can be added inline via the same mockIsWorkingByKey/mockIsDormantByKey pattern (a new mockIsRecyclingByKey is optional and only needed if a test explicitly seeds recycling)"
  artifacts:
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "SessionHoldingOverlay + ComposeBox recycle-disable both fed from working-store useSessionIsRecycling; publishSessionRecycling import + effect removed"
      contains: "useSessionIsRecycling"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "Row-spinner recycling input fed from working-store useSessionIsRecycling; session-recycling-store import removed"
      contains: "useSessionIsRecycling"
    - path: "src/ui/state/session-recycling-store.ts"
      provides: "DELETED — client-side recycling bridge retired"
      contains: null
    - path: "src/ui/state/session-recycling-store.test.ts"
      provides: "DELETED — retired along with the store"
      contains: null
  key_links:
    - from: "src/ui/features/pretty-view/PrettyView.tsx (SessionHoldingOverlay mount + ComposeBox isHolding/recycleActive props)"
      to: "src/ui/state/session-working-store.ts (useSessionIsRecycling hook exported by Plan 53-02)"
      via: "const isRecycling = useSessionIsRecycling(sessionWorkingKey)"
      pattern: "useSessionIsRecycling\\(sessionWorkingKey\\)"
    - from: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (PrettyConversationRowLive isRecycling prop)"
      to: "src/ui/state/session-working-store.ts (useSessionIsRecycling)"
      via: "const isRecycling = useSessionIsRecycling(sessionKey)"
      pattern: "useSessionIsRecycling\\(sessionKey\\)"
---

<objective>
Swap both recycling-signal consumers from the client-side
`session-recycling-store` bridge to the new backend-authoritative
`useSessionIsRecycling` hook exported by Plan 53-02, then delete the
retired store and its test file. After this plan, the row-spinner in the
conversation list correctly reflects each session's recycling state
regardless of whether that session's PrettyView is currently mounted —
which is the entire reason this phase exists (see CONTEXT.md § What would
make it wrong: "if a row in the conversation list still needs its
pretty-view to be currently mounted for its recycling indicator to be
accurate").

Three consumer swaps in PrettyView.tsx (all fed from the same working-store
axis per CONTEXT.md "one source for both surfaces" scope-lock):
1. SessionHoldingOverlay mount gate at line 2749 (`renderedState === "holding"` → `isRecycling`).
2. ComposeBox `isHolding` prop at line 3105 (`renderedState === "holding"` → `isRecycling`).
3. ComposeBox `recycleActive` prop at line 3109 (`renderedState === "holding"` → `isRecycling`).

RESEARCH § Open Question 1 flagged a theoretical 2s-poll-vs-instantaneous-
pane_state latency caveat. The CONTEXT.md is unambiguous about the design
goal ("one source for both surfaces") and the recycle window is 8s minimum
(supervisor's built-in delayed removal after fresh claude is up), so the
2s poll cadence guarantees at least 4 ticks of coverage during any real
recycle. All three consumer sites switch together.

One consumer swap in PrettyConversationsPanel.tsx:
- PrettyConversationRowLive's `useSessionRecycling(sessionKey)` at line 219
  swaps to `useSessionIsRecycling(sessionKey)`.

Then delete:
- src/ui/state/session-recycling-store.ts
- src/ui/state/session-recycling-store.test.ts

And touch up test files and stale doc comments:
- src/ui/features/pretty-view/PrettyView.phase29.test.tsx — retire the
  grep-gate test that pinned `publishSessionRecycling(...)` to the
  `renderedState === "holding"` string (that publish call is gone).
- src/ui/features/pretty-view/PrettyView.test.tsx — update Test F
  ("pane_state:holding trumps loading — SessionHoldingOverlay mounts")
  to seed a recycling:true wire frame into the working-store as the
  fixture, since the overlay is no longer gated on `renderedState`.
- src/ui/state/session-queue-pending-store.ts — the header docblock at
  lines 20-47 references the retired session-recycling-store as a sibling
  pattern; comment-only edit to remove those references. NO code change.

Depends on BOTH Plan 53-01 (backend wire + poller publish the recycling
field) AND Plan 53-02 (working-store Axis E + useSessionIsRecycling
exported). Deletion of the client-side bridge is the LAST step in this
plan — every consumer MUST be rewired first (RESEARCH § Pitfall 4).

Fork-specific plan discipline: this plan stops at code + commit + tests
green. NO docker build, NO docker compose recreate. Deploys are
orchestrator-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-backend-authoritative-recycling-signal-one-wire-axis-two-con/53-CONTEXT.md
@.planning/phases/53-backend-authoritative-recycling-signal-one-wire-axis-two-con/53-RESEARCH.md
@src/ui/features/pretty-view/PrettyView.tsx
@src/ui/features/pretty-view/PrettyView.phase29.test.tsx
@src/ui/features/pretty-view/PrettyView.test.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
@src/ui/state/session-recycling-store.ts
@src/ui/state/session-recycling-store.test.ts
@src/ui/state/session-queue-pending-store.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Swap PrettyView's three recycling consumer sites to useSessionIsRecycling, then update the PrettyView.phase29.test.tsx grep-gate and PrettyView.test.tsx Test F fixture</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx, src/ui/features/pretty-view/PrettyView.phase29.test.tsx, src/ui/features/pretty-view/PrettyView.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/PrettyView.tsx (line 58 — the `publishSessionRecycling` import that must be REMOVED; lines 59-63 — the working-store import block that must be EXTENDED with useSessionIsRecycling; line 1073 — the existing `const sessionWorkingKey = \`${hostId}:${tmuxSession ?? ""}\`;` that the new hook reuses; line 1074 — `const isWorking = useSessionIsWorking(sessionWorkingKey);` — insert the new `const isRecycling = useSessionIsRecycling(sessionWorkingKey);` IMMEDIATELY AFTER this line; lines 2415-2425 — the entire block-comment + useEffect that publishes to the retired store, both must be DELETED; line 2749 — the SessionHoldingOverlay mount gate `{renderedState === "holding" && <SessionHoldingOverlay />}` that swaps to `{isRecycling && <SessionHoldingOverlay />}`; line 3105 — ComposeBox `isHolding={renderedState === "holding"}` swaps to `isHolding={isRecycling}`; line 3109 — ComposeBox `recycleActive={renderedState === "holding"}` swaps to `recycleActive={isRecycling}`)
    - src/ui/features/pretty-view/PrettyView.phase29.test.tsx (lines 219-223 — the grep-gate test asserting the `publishSessionRecycling(...)` call still exists; that call is gone and the test must be retired or replaced)
    - src/ui/features/pretty-view/PrettyView.test.tsx (lines 1687-1711 — Test F "pane_state:holding trumps loading — SessionHoldingOverlay mounts (Phase 30 clean-swap)"; the fixture currently fires pane_state:holding to drive the overlay mount but under Phase 53 the mount is gated on the recycling axis instead; the fixture must seed publishFleetStatusSessionState with recycling:true)
    - src/ui/state/session-working-store.ts (the exported useSessionIsRecycling hook signature — added by Plan 53-02 — and the publishFleetStatusSessionState function for the test fixture seed)
    - .planning/phases/53-backend-authoritative-recycling-signal-one-wire-axis-two-con/53-RESEARCH.md § Pitfall 2 (do NOT tangle wsTransportState / paneState / usePaneResolvingMachine with the recycling swap — those stay untouched) + § Pitfall 5 (remove both the import AND the useEffect in the same edit — no orphaned import)
    - .planning/phases/53-backend-authoritative-recycling-signal-one-wire-axis-two-con/53-CONTEXT.md § Scope edges — "In:" (one source-swap in the chat surface's holding overlay logic + one source-swap in the row spinner + retirement of the client-side recycling bridge store)
  </read_first>
  <behavior>
    - After the edit, PrettyView.tsx has no import of, no export of, and no reference to publishSessionRecycling / session-recycling-store.
    - After the edit, PrettyView.tsx imports useSessionIsRecycling from @/state/session-working-store alongside useSessionIsWorking + useSessionIsWorkingRaw.
    - After the edit, PrettyView.tsx computes `const isRecycling = useSessionIsRecycling(sessionWorkingKey);` immediately after `const isWorking = useSessionIsWorking(sessionWorkingKey);` at line 1074.
    - After the edit, all THREE recycling consumer sites in PrettyView.tsx (SessionHoldingOverlay mount gate, ComposeBox isHolding, ComposeBox recycleActive) read `isRecycling` from the new hook. None reads `renderedState === "holding"` for recycling purposes.
    - The DormancyOverlay, PrettyViewLoadingOverlay, PrettyViewErrorOverlay, inactive fallback, and connection-drop paths continue to read `renderedState === "dormant"` / `renderedState === "resolving"` (via showResolvingSpinner) / `renderedState === "error"` / `renderedState === "inactive"` / `wsTransportState`. Only the "holding" case switches.
    - PrettyView.phase29.test.tsx grep-gate test at lines 219-223 is DELETED (the publishSessionRecycling call no longer exists) AND REPLACED with a new grep gate: PrettyView imports useSessionIsRecycling from the working-store; PrettyView does NOT import from @/state/session-recycling-store; PrettyView renders SessionHoldingOverlay gated on isRecycling (not `renderedState === "holding"`).
    - PrettyView.phase29.test.tsx overlay-mount grep gate at line 192 (`expect(pvSrc).toMatch(/renderedState === "holding"/)`) is DELETED — that string is legitimately gone from PrettyView.tsx after the swap. The other four `renderedState === "..."` matches (dormant, error, inactive, showResolvingSpinner &&) remain valid and stay untouched.
    - PrettyView.test.tsx Test F is updated so the fixture seeds a wire frame carrying recycling:true (via `publishFleetStatusSessionState`) instead of relying on the pane_state:holding frame to mount the overlay. Test F's original semantic goal (the holding overlay mounts) is preserved; the trigger changes from a WS pane_state frame to a store-level publish call.
  </behavior>
  <action>
    A. src/ui/features/pretty-view/PrettyView.tsx changes:

    A1. Line 58 — DELETE the entire import statement `import { publishSessionRecycling } from "@/state/session-recycling-store";`. No other line touches this import.

    A2. Lines 59-62 — EXTEND the existing working-store import block. The current block imports `useSessionIsWorking` and `useSessionIsWorkingRaw` as two named imports from `@/state/session-working-store`. Add a third named import `useSessionIsRecycling` on a new line between `useSessionIsWorkingRaw,` and the closing `} from "@/state/session-working-store";`. Preserve the block's existing multi-line-imports formatting.

    A3. Immediately AFTER line 1074 (`const isWorking = useSessionIsWorking(sessionWorkingKey);`), insert two new lines: a `//` docblock comment followed by the hook call. The docblock header reads "Phase 53 Plan 03 — backend-authoritative recycling signal, sourced from the working-store's Axis E (Plan 53-02). Replaces the retired client-side session-recycling-store bridge which required this PrettyView to be mounted for a row's recycling state to be visible. Consumed by THREE sites below: the SessionHoldingOverlay mount gate (patch #74 site) AND ComposeBox's isHolding + recycleActive props. Reuses sessionWorkingKey (line above) — same key shape." Then the hook call: `const isRecycling = useSessionIsRecycling(sessionWorkingKey);`.

    A4. Lines 2415-2425 — DELETE the entire block-comment + useEffect that publishes to the retired store. The exact deletion range covers the seven-line block-comment starting at line 2415 ("Publish session-recycling state to store keyed on...") AND the four-line useEffect at lines 2422-2425 that calls `publishSessionRecycling(key, renderedState === "holding");`. Replace those 11 lines with a single Phase-53 tombstone `//` comment (2-3 lines) noting: "Phase 53 Plan 03 — the publishSessionRecycling useEffect was retired here. The overlay + ComposeBox recycling props now read the backend-authoritative recycling axis via useSessionIsRecycling(sessionWorkingKey) above (see line ~1075). Retired store: src/ui/state/session-recycling-store.ts (deleted in this plan's Task 2)." This tombstone comment MUST NOT contain any live call to `publishSessionRecycling(` — grep will assert its absence.

    A5. Line 2749 — swap the SessionHoldingOverlay mount gate expression. Before: `{renderedState === "holding" && <SessionHoldingOverlay />}`. After: `{isRecycling && <SessionHoldingOverlay />}`. Also REWRITE the block comment immediately above at lines 2744-2748 (currently "Phase 30 (PS30-06): SessionHoldingOverlay gated on `renderedState === \"holding\"` (backend-authoritative via the paneState React state slot fed by the `case \"pane_state\"` WS handler)"). Replace with a Phase 53 header noting the swap and rationale — cover: the mount is now gated on the backend-authoritative recycling axis (working-store Axis E) via `useSessionIsRecycling(sessionWorkingKey)`; previously gated on `renderedState === "holding"` (Phase 30 PS30-06) which was fed by the pane_state WS frame from THIS pane's own claude-session server — an unmounted pane's row therefore could not observe its own recycling state, which is the bug this phase closes; `isRecycling` is derived from the fleet-status poller reading the caretaker's `.recycled-at` sentinel per Plan 53-01; the connection-drop overlay (a separate concern per CONTEXT.md) stays on `wsTransportState`.

    A6. Line 3105 — swap the ComposeBox `isHolding` prop expression. Before: `isHolding={renderedState === "holding"}`. After: `isHolding={isRecycling}`. Update the preceding block comment at lines 3100-3104 (currently "Phase 30 (PS30-04): isHolding derives from `renderedState === \"holding\"` (backend-authoritative). The local isHolding state slot is DELETED — see the 'isHolding local state slot DELETED' comment at the top of the render body.") with a Phase 53 replacement noting that isHolding now derives from `isRecycling` (working-store Axis E) — one source for all three ComposeBox + overlay recycle sites per CONTEXT.md scope-lock.

    A7. Line 3109 — swap the ComposeBox `recycleActive` prop expression. Before: `recycleActive={renderedState === "holding"}`. After: `recycleActive={isRecycling}`. Update the preceding line-3106-3107 comment (currently "Phase 30: recycleActive derives from `renderedState === \"holding\"`. Semantic preserved (WS-side-effecting controls disabled while the holding overlay is up).") with a Phase 53 replacement noting the recycleActive now derives from `isRecycling` (same source as isHolding above and the overlay mount).

    A8. Do NOT touch DormancyOverlay mount (line 2968 — `renderedState === "dormant"`). Do NOT touch reconnectingActive (line 3127 — status/renderedState error branches). Do NOT touch inactive fallback (line 2764 — `renderedState === "inactive"`). Do NOT touch showResolvingSpinner (line 2760). Do NOT touch dormantActive on ComposeBox (line 3133 — `renderedState === "dormant" || waking`). Do NOT touch case "session_holding" (line 1857) or case "session_holding_cleared" (line 1870) WS handlers — those still fire on the WS side and drive paneState updates for OTHER consumers of paneState; only the recycling signal was decoupled from paneState. Do NOT touch usePaneResolvingMachine, paneState, or wsTransportState.

    B. src/ui/features/pretty-view/PrettyView.phase29.test.tsx changes:

    B1. Lines 191-203 — the "PrettyView.tsx: overlay mount gates use renderedState === '...' (backend-authoritative)" test. UPDATE by DELETING the single assertion line `expect(pvSrc).toMatch(/renderedState === "holding"/);` at line 192. Keep the other four matches (dormant, error, inactive, showResolvingSpinner). Update the test name to remove the "holding" reference (it now covers only dormant/error/inactive/resolving). Add a one-line body comment noting that "holding" was intentionally moved off renderedState by Phase 53 Plan 03 and is now driven by useSessionIsRecycling — locked by the new grep gate in step B2 below.

    B2. Lines 219-223 — REPLACE the entire "PrettyView.tsx: publishes session-recycling on `renderedState === \"holding\"`..." `it(...)` block with a new grep-gate test whose name is "Phase 53 Plan 03: PrettyView consumes useSessionIsRecycling from the working-store (NOT the retired session-recycling-store bridge)" and whose body executes FOUR assertions against `pvSrc` (the file-source string loaded at the top of the describe block): (i) `expect(pvSrc).not.toMatch(/publishSessionRecycling/)` — the retired call site is gone (the tombstone comment may name the function but MUST NOT include an open-paren; enforce the stricter check `not.toMatch(/publishSessionRecycling\(/)` to allow the tombstone-comment mention); (ii) `expect(pvSrc).not.toMatch(/from ["']@\/state\/session-recycling-store["']/)` — no live import path (the tombstone comment mentions the path but not as a `from "..."` clause); (iii) `expect(pvSrc).toMatch(/useSessionIsRecycling/)` — new hook consumed; (iv) `expect(pvSrc).toMatch(/\{isRecycling && <SessionHoldingOverlay/)` — the overlay mount gate reads isRecycling (not renderedState === "holding").

    B3. Do NOT touch any other test in this file. Do NOT touch the integration tests in GROUP 2-7 (Tests A-F pane_state:active/holding/etc.) except where they specifically assert SessionHoldingOverlay mounting — those live in PrettyView.test.tsx (Task 1C below), not this file. If any integration test in phase29 mounts PrettyView and asserts SessionHoldingOverlay presence, verify it also seeds the working-store recycling axis; if it does not, add a `publishFleetStatusSessionState` seed to the beforeEach or the test body. Grep for `SessionHoldingOverlay` in phase29 to find any such assertion — the file's own §Group 3 Test B ("pane_state:holding → SessionHoldingOverlay mounted" at line 313) is the specific case to inspect.

    B4. GROUP 3 Test B and GROUP 4 Test C in PrettyView.phase29.test.tsx (lines 292-370 approximately) — if either asserts SessionHoldingOverlay mount/unmount purely on the strength of pane_state:holding / pane_state:active WS frames, they MUST be updated to ALSO publish a matching recycling wire frame via `publishFleetStatusSessionState` imported from `@/state/session-working-store`. Concrete pattern for the arrange step: import `publishFleetStatusSessionState` from the working-store at the top of the file; inside an `await act(async () => { ... })` block, invoke `publishFleetStatusSessionState("1", { hostId: "1", tmuxSession: "s1", sessionId: "test-session", pid: 12345, status: "idle", backgroundTasks: [], updatedAt: Date.now(), lastMessageAt: null, aiTitle: null, dormant: false, recycling: true })` to drive the overlay mount. Test C's unmount assertion adds a follow-up publish with `recycling: false` in the same shape.

    B5. **Phase 53 plan-checker fix (2026-08-21)** — GROUP 7 Test F in PrettyView.phase29.test.tsx (D-11 "don't-flicker", line 484 approximately) ALSO mounts SessionHoldingOverlay via a pane_state:holding fixture and asserts on its behavior across state churn. It is NOT covered explicitly by B4's line-range naming but WILL regress without a recycling seed once PrettyView.tsx's overlay trigger is swapped. Apply the same `publishFleetStatusSessionState(..., recycling: true)` seed pattern from B4 at the point where Test F would otherwise expect the overlay to be mounted, and add a follow-up `recycling: false` publish at the point where Test F would otherwise expect it unmounted. Same import + `await act(async () => { ... })` wrapping. The B3 grep instruction is intended to catch this — B5 is the belt-and-suspenders name-call so the executor cannot miss it.

    C. src/ui/features/pretty-view/PrettyView.test.tsx changes:

    C1. Test F at lines 1687-1711 ("pane_state:holding trumps loading — SessionHoldingOverlay mounts (Phase 30 clean-swap)"). The fixture at lines 1703-1704 currently fires TWO WS frames — `fireWsFrame(ws, { type: 'session_holding' });` and `fireWsFrame(ws, { type: 'pane_state', state: 'holding', reason: 'id_reset' });` — to drive the mount. AFTER those two lines, add a third fixture step: import `publishFleetStatusSessionState` from `@/state/session-working-store` at the top of the file (add to an existing working-store import block if one exists, else create a new import), then inside an `await act(async () => { ... })` block invoke `publishFleetStatusSessionState("1", { hostId: "1", tmuxSession: "s1", sessionId: "test-session", pid: 12345, status: "idle", backgroundTasks: [], updatedAt: Date.now(), lastMessageAt: null, aiTitle: null, dormant: false, recycling: true })`. Prefix the new fixture step with a `//` comment noting: "Phase 53 Plan 03 — SessionHoldingOverlay mount is now gated on the backend-authoritative recycling axis (useSessionIsRecycling), not pane_state:holding. Seed the working-store so the overlay mounts." Update Test F's docblock to note that Phase 53 Plan 03 rewired the mount trigger from pane_state:holding to the working-store recycling axis.

    C2. Grep the rest of PrettyView.test.tsx for any other assertion that mounts SessionHoldingOverlay or asserts on its presence/absence. Any such assertion requires the same working-store seed pattern from C1. Grep pattern: `grep -n "SessionHoldingOverlay\|holdingOverlay" src/ui/features/pretty-view/PrettyView.test.tsx` — inspect every hit and update where required with the same seed shape.

    C3. If SessionHoldingOverlay.test.tsx contains any assertion that couples the overlay to renderedState/paneState (rather than testing the SessionHoldingOverlay component in isolation with the overlay as a rendered prop), inspect and update. The SessionHoldingOverlay component itself is unchanged in this plan — its consumer contract (mount when isRecycling=true) is what changed. If SessionHoldingOverlay.test.tsx only tests the rendered visual (aria-label, message copy) with no mount-gate coupling, no change needed.

    C4. Do NOT touch ComposeBox.recycle-disable.test.tsx — that file tests ComposeBox in isolation with `recycleActive` passed as a prop; it does not care about the PrettyView-side source of the prop. Verify by grepping the file for `session-recycling-store` or `publishSessionRecycling` — should be zero hits.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/PrettyView src/ui/features/pretty-view/PrettyView.phase29 src/ui/features/pretty-view/PrettyView.test 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "publishSessionRecycling(" src/ui/features/pretty-view/PrettyView.tsx` returns 0 (no live function-call; tombstone comment naming the identifier without a call-open-paren is tolerated).
    - `grep -c 'from "@/state/session-recycling-store"' src/ui/features/pretty-view/PrettyView.tsx` returns 0 (no live import statement).
    - `grep -c "useSessionIsRecycling" src/ui/features/pretty-view/PrettyView.tsx` returns a value ≥ 2 (import + call site).
    - `grep -c "const isRecycling = useSessionIsRecycling(sessionWorkingKey)" src/ui/features/pretty-view/PrettyView.tsx` returns exactly 1.
    - `grep -c "{isRecycling && <SessionHoldingOverlay" src/ui/features/pretty-view/PrettyView.tsx` returns exactly 1.
    - `grep -c "isHolding={isRecycling}" src/ui/features/pretty-view/PrettyView.tsx` returns exactly 1.
    - `grep -c "recycleActive={isRecycling}" src/ui/features/pretty-view/PrettyView.tsx` returns exactly 1.
    - No live-code reference to `renderedState === "holding"` in PrettyView.tsx: `grep -v '^\s*//' src/ui/features/pretty-view/PrettyView.tsx | grep -v '^\s*\*' | grep -c 'renderedState === "holding"'` returns 0.
    - `grep -c "renderedState === \"dormant\"" src/ui/features/pretty-view/PrettyView.tsx` returns a value ≥ 2 (DormancyOverlay mount + dormantActive on ComposeBox — untouched by this plan).
    - `grep -c "Phase 53 Plan 03" src/ui/features/pretty-view/PrettyView.tsx` returns a value ≥ 4 (import block note + hook docblock + tombstone + overlay + at least one ComposeBox comment).
    - `grep -c "publishSessionRecycling" src/ui/features/pretty-view/PrettyView.phase29.test.tsx` returns 0 (grep-gate test rewritten).
    - `grep -c "useSessionIsRecycling" src/ui/features/pretty-view/PrettyView.phase29.test.tsx` returns a value ≥ 1 (new grep-gate test).
    - `npx vitest run src/ui/features/pretty-view/PrettyView 2>&amp;1 | grep -E "Tests.*passed|Tests.*failed"` shows 0 failures. If any integration test regresses (SessionHoldingOverlay not found where expected), locate the fixture and add the working-store recycling seed per C1's pattern.
    - `npx tsc --noEmit 2>&amp;1 | grep -c "error TS"` returns 0.
  </acceptance_criteria>
  <done>
    PrettyView's three recycling consumer sites (overlay mount + ComposeBox isHolding + ComposeBox recycleActive) all read `isRecycling` from useSessionIsRecycling(sessionWorkingKey); the publishSessionRecycling import and effect are deleted; the retired-store name appears only in a tombstone comment; the PrettyView.phase29.test.tsx grep-gate is rewritten to lock the new hook consumption; PrettyView.test.tsx Test F (and any sibling test that mounts SessionHoldingOverlay) is updated to seed the working-store recycling frame. All PrettyView tests green; tsc clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Swap PrettyConversationsPanel's row-spinner recycling source to useSessionIsRecycling; delete session-recycling-store.ts + test; update queue-pending-store docblock</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx, src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx, src/ui/state/session-recycling-store.ts, src/ui/state/session-recycling-store.test.ts, src/ui/state/session-queue-pending-store.ts</files>
  <read_first>
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (line 102 — `import { useSessionRecycling } from "@/state/session-recycling-store";` — REMOVE; lines 85-101 — the working-store import block that must be EXTENDED with useSessionIsRecycling; line 219 — `const isRecycling = useSessionRecycling(sessionKey);` — swap the RHS to useSessionIsRecycling; line 238 — `isRecycling={isRecycling === true}` — the coercion may stay (idempotent — useSessionIsRecycling already returns strict boolean, so `=== true` is a no-op; keep for symmetry with sibling props OR simplify to `isRecycling={isRecycling}`; RESEARCH § Code Examples § "PrettyConversationsPanel" says both are fine — prefer the strict `{isRecycling}` for clarity now that the source is guaranteed boolean)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx (line 318 — `mockWorkingSnapshot` typed as `Map<string, { isWorking; lastMessageAt; aiTitle; dormant }>` — this typing DOES need to be extended with `recycling: boolean` because Plan 53-02 extends the WorkingRecord type; lines 320-343 — the `useSessionIsWorkingSpy` / `useSessionIsDormantSpy` / `getSessionWorkingSnapshotSpy` pattern that a new `useSessionIsRecyclingSpy` follows; lines 330-344 — the vi.mock("@/state/session-working-store", ...) block that needs `useSessionIsRecycling` added; line 464 + 3762 + 3875+ — the seeding sites for mockWorkingSnapshot that construct WorkingRecord objects and must add `recycling: false` to satisfy the extended type)
    - src/ui/state/session-recycling-store.ts (the file to DELETE — read it once to know what's being removed; single default export nothing, three named exports: publishSessionRecycling, useSessionRecycling, getSessionRecyclingSnapshot, __resetForTest)
    - src/ui/state/session-recycling-store.test.ts (the file to DELETE)
    - src/ui/state/session-queue-pending-store.ts (lines 20-47 header docblock — comment-only cleanup; remove or rephrase the four `session-recycling-store` mentions at lines 24, 34, 44)
    - .planning/phases/53-backend-authoritative-recycling-signal-one-wire-axis-two-con/53-RESEARCH.md § Pitfall 4 (delete the store LAST, after all consumers are rewired, to avoid mid-plan build failures)
    - .planning/phases/53-backend-authoritative-recycling-signal-one-wire-axis-two-con/53-RESEARCH.md § Pitfall 6 (verify the imported hook name is useSessionIsRecycling — NOT useSessionIsDormant — before touching the panel)
  </read_first>
  <behavior>
    - PrettyConversationsPanel.tsx line 102 `import { useSessionRecycling }` is REMOVED.
    - PrettyConversationsPanel.tsx working-store import block (lines 85-101) is EXTENDED to include useSessionIsRecycling.
    - PrettyConversationRowLive (lines 179-244) — the `const isRecycling = useSessionRecycling(sessionKey);` at line 219 becomes `const isRecycling = useSessionIsRecycling(sessionKey);` (same sessionKey shape — both hooks share the `${hostId}:${tmuxSession ?? ""}` convention per RESEARCH § Architecture).
    - Line 238 — the coercion `isRecycling={isRecycling === true}` is simplified to `isRecycling={isRecycling}` (useSessionIsRecycling returns strict boolean; `=== true` is provably redundant). PrettyConversationRow's `isRecycling?: boolean` prop accepts either — no type change needed.
    - PrettyConversationsPanel.test.tsx grows a `mockIsRecyclingByKey` Map + `useSessionIsRecyclingSpy` mirroring the mockIsDormantByKey / useSessionIsDormantSpy pattern; the vi.mock block adds `useSessionIsRecycling: (sessionKey) => useSessionIsRecyclingSpy(sessionKey)`. Every construction of a mockWorkingSnapshot record adds `recycling: false` (or the required value) to satisfy the extended WorkingRecord type from Plan 53-02.
    - Reset in `afterEach` (or equivalent) at line 464 + 3762 clears mockIsRecyclingByKey alongside the sibling maps.
    - session-recycling-store.ts is DELETED from disk (git rm).
    - session-recycling-store.test.ts is DELETED from disk (git rm).
    - session-queue-pending-store.ts header docblock (lines 20-47) has its four references to `session-recycling-store` removed or replaced with `session-working-store`. NO code change in that file.
    - After the plan, `grep -r "session-recycling-store" src/` returns ZERO hits.
    - After the plan, `grep -rw "useSessionRecycling" src/` returns ZERO hits (the retired hook name; the new hook has "Is" in the middle — useSessionIsRecycling — so the word-boundary grep won't collide).
  </behavior>
  <action>
    A. src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx changes:

    A1. Line 102 — DELETE the entire import statement `import { useSessionRecycling } from "@/state/session-recycling-store";`.

    A2. Lines 85-101 — EXTEND the existing working-store import block. The current block imports six named identifiers (useSessionIsWorking, useSessionAiTitle, useSessionIsDormant, getSessionWorkingSnapshot, subscribeSessionWorkingStore — plus the associated docblocks). Add `useSessionIsRecycling` as a NEW named import between `useSessionIsDormant` and `getSessionWorkingSnapshot`. Prefix the new import with a single-line `//` comment: "Phase 53 Plan 03 — recycling axis (Plan 53-02) hook. Replaces the retired session-recycling-store bridge." Preserve the existing docblocks on sibling imports.

    A3. Lines 174-178 — REWRITE the block comment above PrettyConversationRowLive that currently mentions the retired store and "All three stores share the exact same `${hostId}:${tmuxSession ?? ""}` key shape." Replace with a Phase 53 block comment covering: recycling-axis subscription now reads from the working-store (useSessionIsRecycling — backend-authoritative via the fleet-status poller); previously bridged via the client-side session-recycling-store which required a mounted PrettyView pane to publish; row-spinner correctness for unmounted rows is the entire reason for the swap; all working-store hooks share the exact same `${hostId}:${tmuxSession ?? ""}` key shape via `sessionWorkingKey()` at line 162; the quick-260802-w9e session-queue-pending-store subscription (the FOURTH gate `!hasQueuePending`) stays.

    A4. Lines 215-219 — swap the hook call. REPLACE the existing `//` comment block at lines 215-218 (quick-260730-qbl recycling-store consumption docblock) AND the line 219 hook call. The new comment reads "Phase 53 Plan 03 — recycling axis from the backend-authoritative working-store. Keyed identically to the useSessionIsWorking hook above — all working-store hooks share the same `${hostId}:${tmuxSession ?? ""}` shape via sessionWorkingKey() at line 162. Returns strict boolean (never null) — the `=== true` coercion at the prop site below is now redundant but kept as `isRecycling={isRecycling}` for readability." The new call: `const isRecycling = useSessionIsRecycling(sessionKey);`.

    A5. Line 238 — simplify the prop pass-through. Before: `isRecycling={isRecycling === true}`. After: `isRecycling={isRecycling}`. Rationale: the hook now guarantees a strict boolean (never null), so the `=== true` coercion is provably redundant. Keeping the coercion would still be semantically correct but is misleading to future readers who would infer the source can be null.

    B. src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx changes:

    B1. Line 318 — EXTEND the `mockWorkingSnapshot` type declaration by appending `; recycling: boolean` inside the inline object type. Before: `let mockWorkingSnapshot: Map<string, { isWorking: boolean; lastMessageAt: number | null; aiTitle: string | null; dormant: boolean }> = new Map();`. After: `let mockWorkingSnapshot: Map<string, { isWorking: boolean; lastMessageAt: number | null; aiTitle: string | null; dormant: boolean; recycling: boolean }> = new Map();`.

    B2. Line 328 — EXTEND the `getSessionWorkingSnapshotSpy` type cast accordingly. Same append-`; recycling: boolean` treatment as B1 above, applied to the `as ReadonlyMap<string, { ... }>` cast inside the vi.fn body.

    B3. AFTER the useSessionIsDormantSpy declaration at lines 316-327 (specifically after line 327), add a parallel recycling spy + seed Map. Structure: a `//` comment "Phase 53 Plan 03 — per-test seeding for the recycling axis. Mirrors the mockIsDormantByKey pattern. Tests seed this Map in arrange steps; afterEach clears it."; then `let mockIsRecyclingByKey: Map<string | null, boolean> = new Map();`; then `const useSessionIsRecyclingSpy = vi.fn((sessionKey: string | null) => { if (sessionKey === null) return false; return mockIsRecyclingByKey.get(sessionKey) ?? false; });`.

    B4. Lines 330-344 — EXTEND the `vi.mock("@/state/session-working-store", () => ({ ... }))` block. Add a new key `useSessionIsRecycling: (sessionKey: string | null) => useSessionIsRecyclingSpy(sessionKey),` after the existing `useSessionIsDormant` entry at line 343. Update the block comment referencing Phase 52 (currently at lines 337-341) to also cite Phase 53 Plan 03: "Phase 52 Plan 03 (plan-checker B-2 fix) + Phase 53 Plan 03: Panel.tsx now imports useSessionIsRecycling in addition to useSessionIsDormant + getSessionWorkingSnapshot; without this stub every render throws TypeError."

    B5. NO vi.mock for "@/state/session-recycling-store" should exist in this file after the swap. Grep the file for `session-recycling-store` — if any mock/import block references it, DELETE that block. (Likely none, since the panel previously used the store directly with no explicit mock; verify.)

    B6. Line 464 — the beforeEach reset. Add `mockIsRecyclingByKey = new Map();` alongside the existing `mockWorkingSnapshot = new Map();` reset (probably next to the sibling resets on adjacent lines).

    B7. Line 3762 — the afterEach cleanup. Add `mockIsRecyclingByKey.clear();` alongside the existing `mockWorkingSnapshot.clear();`.

    B8. Lines 3875, 3894, 3914, 4018 — every `mockWorkingSnapshot.set("tina-session", { isWorking: ..., lastMessageAt: ..., aiTitle: ..., dormant: ... });` site MUST add `recycling: false,` (or the required value if the test explicitly seeds recycling for a specific case) as an additional trailing property in the object literal. The extended WorkingRecord type from Plan 53-02 requires this field. Grep the file for `mockWorkingSnapshot.set(` — every hit needs `recycling: false` added.

    C. DELETE the retired store + test files:

    C1. Run `git rm src/ui/state/session-recycling-store.ts` (or use the equivalent delete-via-Bash + git track; make sure the file is removed from disk AND untracked from git in the same step — a stray file causes `grep -r "session-recycling-store" src/` to still hit).

    C2. Run `git rm src/ui/state/session-recycling-store.test.ts`.

    D. src/ui/state/session-queue-pending-store.ts docblock cleanup:

    D1. Lines 20-47 header docblock — four references to `session-recycling-store` remain from the pre-Phase-53 wording:
       - Line 24 currently reads `// session-working-store / session-recycling-store). There is no "unknown"` — change to `// session-working-store). There is no "unknown"`.
       - Line 34 currently reads `// session-working-store and session-recycling-store — the three stores` — change to `// session-working-store — the two stores` (queue-pending + working = two).
       - Line 44 currently reads `// rationale as session-working-store / session-recycling-store: a re-mount` — change to `// rationale as session-working-store: a re-mount`.
       - Any other stray "recycling-store" mention in the header — same treatment. Grep the file: `grep -n "recycling-store" src/ui/state/session-queue-pending-store.ts` — inspect every hit and delete the retired-store reference while preserving the surrounding sentence structure.

    D2. Add a one-line note at the top of the header docblock (near the file header comment) referencing the Phase 53 retirement. Suggested wording: "Phase 53 Plan 03 (2026-08-21): the session-recycling-store bridge was retired; this store remains as-is (queue-pending has no backend-authoritative source per Phase 53 CONTEXT.md — 'queue-pending state genuinely doesn't survive the pretty-view's own X-minute cleanup — the client is the only source that knows whether the queue is still armed')."

    D3. NO code change in this file. Comment-only edits.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel 2>&amp;1 | tail -20 &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "session-recycling-store" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns 0.
    - `grep -cw "useSessionRecycling" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns 0 (retired hook name; word-boundary avoids collision with useSessionIsRecycling).
    - `grep -c "useSessionIsRecycling" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns a value ≥ 2 (import + call site).
    - `test ! -f src/ui/state/session-recycling-store.ts` (file is gone).
    - `test ! -f src/ui/state/session-recycling-store.test.ts` (file is gone).
    - `grep -rc "session-recycling-store" src/ 2>/dev/null | grep -v ":0$" | wc -l` returns 0 — full retirement.
    - `grep -rcw "useSessionRecycling" src/ 2>/dev/null | grep -v ":0$" | wc -l` returns 0 — retired hook name absent everywhere.
    - `grep -c "useSessionIsRecycling" src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` returns a value ≥ 2 (spy declaration + vi.mock key).
    - `grep -c "mockIsRecyclingByKey" src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` returns a value ≥ 3 (declaration + spy body + reset in beforeEach/afterEach).
    - `grep -c "recycling: boolean" src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` returns a value ≥ 2 (mockWorkingSnapshot type + getSessionWorkingSnapshotSpy cast).
    - `grep -c "recycling-store" src/ui/state/session-queue-pending-store.ts` returns 0 (docblock cleanup).
    - `npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel 2>&amp;1 | grep -E "Tests.*passed|Tests.*failed"` shows 0 failures. Every existing Panel test must still pass — the type extension + new recycling default (false) is backward-compatible for tests that don't care about the axis.
    - `npx tsc --noEmit 2>&amp;1 | grep -c "error TS"` returns 0.
    - `npm run build 2>&amp;1 | tail -5` completes cleanly (proves no dangling imports of the retired store anywhere in the app bundle).
  </acceptance_criteria>
  <done>
    PrettyConversationsPanel's row-spinner recycling input is fed from useSessionIsRecycling; the retired session-recycling-store and its test are DELETED from disk; the panel test file mocks the new hook and extends every mockWorkingSnapshot record with recycling:false; session-queue-pending-store's docblock no longer references the retired sibling; full app build clean; no dangling imports; all Panel tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser store → PrettyView consumer | useSessionIsRecycling returns strict boolean; the SessionHoldingOverlay mount gate + ComposeBox recycle-disable predicates receive a plain boolean — no null/undefined branches to reason about. |
| browser store → PrettyConversationRowLive | Same as above; the isRecycling prop passed to PrettyConversationRow is strict boolean; showSpinnerOn predicate at PrettyConversationRow.tsx:973 (`inActiveSet && (isWorking === true || isRecycling || hasQueuePending)`) continues to evaluate correctly. |
| filesystem → git | Deleting session-recycling-store.ts + .test.ts leaves no dangling `.js`/`.d.ts` in the build output because Vite/tsc rebuild from scratch each run; verify via full `npm run build`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-03-01 | Availability | PrettyView SessionHoldingOverlay mount latency | accept | The theoretical 2s-poll-vs-instantaneous-pane_state latency (RESEARCH § Open Question 1): under Phase 53 the overlay mount is gated on useSessionIsRecycling, which observes the backend poller's 2s cadence. The supervisor's .recycled-at sentinel is held for a minimum of 8s past the fresh claude being up (agent-supervisor lines 471-475, 530). Worst-case observable delay from recycle-start to overlay-mount is ≤ 2s; the mount window is 8s minimum. No user-visible regression under normal recycle timing. Ashley's fleet directive is "one source for both surfaces" — this is the accepted tradeoff per CONTEXT.md scope-lock. |
| T-53-03-02 | Availability | ComposeBox recycle-disable window shrinkage | accept | Same underlying latency as T-53-03-01. If Ashley taps Send in the first ≤2s of a recycle before the poll tick lands, the previous architecture (client-inferred from `renderedState === "holding"` via pane_state WS) would have disabled Send instantly. The new architecture may allow the tap to reach the WS layer briefly. Two mitigating factors: (a) the WS layer's send path (patch #43 session-file work) tolerates a mid-recycle send via its own queue; (b) the ComposeBox recycle-disable is a UX affordance, not a correctness gate. Accepted per CONTEXT.md scope-lock; if problematic in UAT, follow-up plan can restore instantaneous pane_state gating for ComposeBox specifically while keeping the overlay + row-spinner on the working-store axis. |
| T-53-03-03 | Repudiation | Deletion of session-recycling-store leaves the git history | accept | git log preserves the file's full history; no forensic loss. The retirement is documented in this plan's SUMMARY, the tombstone comment in PrettyView.tsx, and the updated docblock in session-queue-pending-store.ts. |
| T-53-03-04 | Denial of Service | Dangling import causing app-wide build failure post-delete | mitigate | Deletion is the LAST step in Task 2 (Pitfall 4). Every consumer must be swapped in Task 1 + Task 2 A/B before the file is removed. Verification via `npm run build` in the acceptance criteria proves no dangling reference survives. |
| T-53-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs. Package legitimacy gate N/A. |
</threat_model>

<verification>
- `npx vitest run src/ui/features/pretty-view src/ui/features/pretty-conversations src/ui/state/session-working-store 2>&amp;1 | grep -E "Test Files.*passed|Tests.*passed"` shows all suites passing.
- `test ! -f src/ui/state/session-recycling-store.ts &amp;&amp; test ! -f src/ui/state/session-recycling-store.test.ts &amp;&amp; echo "retired"` prints "retired".
- `grep -rc "session-recycling-store" src/ 2>/dev/null | grep -v ":0$" | wc -l` returns 0 (no hits anywhere in src/).
- `grep -rcw "useSessionRecycling" src/ 2>/dev/null | grep -v ":0$" | wc -l` returns 0 (retired hook name absent everywhere; word-boundary avoids collision with useSessionIsRecycling).
- `npx tsc --noEmit 2>&amp;1 | grep -c "error TS"` returns 0.
- `npm run build 2>&amp;1 | tail -3` exit 0.
- `grep -c "useSessionIsRecycling" src/ui/features/pretty-view/PrettyView.tsx` returns ≥ 2.
- `grep -c "useSessionIsRecycling" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns ≥ 2.
</verification>

<success_criteria>
- Both consumer surfaces (PrettyView holding overlay + PrettyConversationRow row spinner) read recycling state from the SAME working-store axis via useSessionIsRecycling.
- PrettyView's ComposeBox isHolding + recycleActive props both fed from the same isRecycling boolean (one source for all three sites per CONTEXT.md scope-lock).
- session-recycling-store.ts + its test file DELETED from disk and untracked from git.
- session-queue-pending-store.ts docblock no longer references the retired sibling.
- No dangling imports; full app build clean; all consumer tests green.
- The bug this phase exists to fix is closed: an unmounted row's spinner now correctly reflects that session's recycling state (verifiable in UAT by starting a recycle on an identity whose PrettyView is not currently mounted and observing the row spinner in the conversation list).
- No deploy in this plan (per fork-specific plan discipline).
</success_criteria>

<output>
Create `.planning/phases/53-backend-authoritative-recycling-signal-one-wire-axis-two-con/53-03-SUMMARY.md` when done. Summary MUST include: the exact PrettyView.tsx line ranges deleted (import at line 58, useEffect at 2415-2425), the exact three consumer-site swap lines (2749, 3105, 3109), the PrettyConversationsPanel.tsx swap line (219), the deleted files (session-recycling-store.ts + .test.ts) with their prior LOC, a snapshot of the PrettyView.phase29.test.tsx test rewrite (old vs new), a snapshot of the PrettyView.test.tsx Test F fixture change (before/after), the exact PrettyConversationsPanel.test.tsx mock additions (mockIsRecyclingByKey + spy + vi.mock key), the count of mockWorkingSnapshot record sites updated with `recycling: false`, and the docblock cleanup in session-queue-pending-store.ts (line numbers before/after). Include a final "bug-fix confirmation" note explaining how a UAT scenario (start /id-reset on identity X while PrettyView for identity X is unmounted) would demonstrate the row spinner now correctly showing recycling state — this is the phase's success criterion per CONTEXT.md § What would make it wrong.
</output>
