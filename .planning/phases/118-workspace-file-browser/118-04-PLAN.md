---
phase: 118-workspace-file-browser
plan: 04
type: execute
wave: 3
depends_on: ["118-03"]
files_modified:
  - src/ui/features/pretty-view/IdentityModal.tsx
autonomous: true
requirements: [D-01, D-02, D-03, D-18]

must_haves:
  truths:
    - "NAV_SECTIONS in IdentityModal.tsx contains a new entry { value: 'workspace', label: 'Workspace', Icon: Folder } visible to ALL users (not gated on isAdmin) (D-01, D-02)"
    - "A <TabsContent value='workspace'> block mounts <WorkspaceTab identity={identity} hostId={hostId} hue={hue} /> inline in the tab body — no new modal, no new dialog (D-01, D-03, D-10 inherited)"
    - "The tab body uses the flex-1 min-h-0 overflow-hidden flex flex-col className so WorkspaceTab handles its own scroll (matches PATTERNS § IdentityModal.tsx integration)"
    - "The Folder icon is imported from lucide-react in the existing lucide import line (currently: AlarmClock, Pencil, Send, User, X)"
    - "No other code in IdentityModal.tsx is touched — only the NAV_SECTIONS array, the imports line, and the new TabsContent block. The activeTab state, the icon-bar renderer, the modal chrome, the identity/wakeups/telegram tabs, and the useEffect fetch block all remain byte-identical outside the surgical additions."
    - "The workspace tab appears in the bottom icon-bar between 'Wakeups' and (when isAdmin) 'Telegram' — because the new entry lands before the isAdmin spread in NAV_SECTIONS"
  artifacts:
    - path: "src/ui/features/pretty-view/IdentityModal.tsx"
      provides: "IdentityModal with Workspace tab wired via NAV_SECTIONS + TabsContent"
      contains: "value: \"workspace\""
      exports: ["IdentityModal"]
  key_links:
    - from: "src/ui/features/pretty-view/IdentityModal.tsx"
      to: "src/ui/features/pretty-view/WorkspaceTab.tsx"
      via: "import WorkspaceTab from \"./WorkspaceTab\""
      pattern: "import.*WorkspaceTab.*from.*[\"'']\\./WorkspaceTab"
    - from: "src/ui/features/pretty-view/IdentityModal.tsx NAV_SECTIONS"
      to: "the bottom icon-bar renderer at L1621-1651"
      via: "iterating the array (existing code — automatic tab button render)"
      pattern: "value: \"workspace\""
    - from: "src/ui/features/pretty-view/IdentityModal.tsx TabsContent"
      to: "WorkspaceTab component"
      via: "<TabsContent value=\"workspace\">...<WorkspaceTab ... /></TabsContent>"
      pattern: "TabsContent[^>]*value=[\"']workspace[\"']"
---

<objective>
Integrate WorkspaceTab (built in Plan 118-03) into IdentityModal via three surgical edits: one lucide import addition, one NAV_SECTIONS entry, and one TabsContent block. This makes the tab discoverable through the existing badge-click → modal path (D-03 — no new entry point), available to every user (D-02 — not admin-gated), and rendered inline inside the modal (D-01 + D-10 inherited).

Purpose: implements D-01 (tab in IdentityModal via NAV_SECTIONS), D-02 (all users, not admin-gated — landed BEFORE the isAdmin spread), D-03 (no new badge menu / separate modal — reuses the existing left-click-badge → IdentityModal path). D-18's host chip is DEFERRED to WorkspaceTab's own header (already delivered in Plan 118-03) — this plan does NOT touch the IdentityModal-level header chrome.

Output: `src/ui/features/pretty-view/IdentityModal.tsx` with three localized additions — an import, a NAV_SECTIONS entry, and a TabsContent block. All existing behavior preserved byte-identically outside these three sites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/118-workspace-file-browser/118-CONTEXT.md
@.planning/phases/118-workspace-file-browser/118-PATTERNS.md
@.planning/phases/118-workspace-file-browser/118-03-PLAN.md
@src/ui/features/pretty-view/IdentityModal.tsx
@src/ui/features/pretty-view/WorkspaceTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Folder import + Workspace NAV_SECTIONS entry + Workspace TabsContent block to IdentityModal.tsx</name>
  <files>src/ui/features/pretty-view/IdentityModal.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/IdentityModal.tsx L30 (existing lucide-react import — Folder must join the existing named-import list)
    - src/ui/features/pretty-view/IdentityModal.tsx L188-215 (IdentityModal props signature — identity, hostId, hue all in scope for TabsContent children)
    - src/ui/features/pretty-view/IdentityModal.tsx L308-318 (NAV_SECTIONS constant array — new entry inserted BEFORE the `...(isAdmin ? ...)` spread so it renders for every user)
    - src/ui/features/pretty-view/IdentityModal.tsx L1560-1610 (existing TabsContent blocks — new block follows the same className pattern as the identity-wakeups block at L1574 but replaces `overflow-y-auto px-6 py-4` with `overflow-hidden flex flex-col` because WorkspaceTab manages its own scroll / padding internally per Plan 118-03)
    - src/ui/features/pretty-view/IdentityModal.tsx L1621-1651 (bottom icon-bar renderer — no changes needed; it maps NAV_SECTIONS automatically, so the new entry surfaces as a tab button without new render code)
    - src/ui/features/pretty-view/WorkspaceTab.tsx (from Plan 118-03 — confirms the default export shape: `export default function WorkspaceTab({ identity, hostId, hue })`)
    - .planning/phases/118-workspace-file-browser/118-PATTERNS.md § IdentityModal.tsx integration (the exact NAV_SECTIONS entry and TabsContent block shapes to paste)
  </read_first>
  <action>
    Three surgical edits, in this order:

    **Edit 1 — Import Folder from lucide-react (add to existing named-import at L30):**
    Change the existing line `import { AlarmClock, Pencil, Send, User, X } from "lucide-react";` to `import { AlarmClock, Folder, Pencil, Send, User, X } from "lucide-react";` (alphabetical insertion of Folder between AlarmClock and Pencil).

    **Edit 2 — Add Workspace to NAV_SECTIONS (at L308-318):**
    Insert this new object as the third entry in the NAV_SECTIONS array, immediately after the identity-wakeups entry at L310 and BEFORE the `...(isAdmin ? ... : [])` spread at L315:
    `{ value: "workspace", label: "Workspace", Icon: Folder },`
    Placement rationale: BEFORE the isAdmin spread means every user sees the tab (D-02). Placement AFTER identity-wakeups keeps the natural read order Identity → Wakeups → Workspace → [Telegram-if-admin]. Add a lead-in comment: `// Phase 118 (D-01, D-02): Workspace tab — available to ALL users (not admin-gated per D-22, D-02). Renders WorkspaceTab (Plan 118-03) inline in the tab body per D-10 (no modal stacking).`

    **Edit 3 — Add the WorkspaceTab import (top of file, alongside other feature imports at L78-83):**
    Insert `import WorkspaceTab from "./WorkspaceTab";` on its own line near the existing IdentityFileTab / WakeupsTab / TelegramTab imports. Alphabetical / grouped placement acceptable.

    **Edit 4 — Add the Workspace TabsContent block (in the Tabs body at L1560-1610, after the identity-wakeups TabsContent at L1587 and before the isAdmin Telegram block at L1596):**
    ```
    {/* Phase 118 (D-01, D-09, D-10): Workspace tab body — inline-swap mode
        (list ↔ viewer) handled INSIDE WorkspaceTab; no Dialog wrapper, no
        nested modal (D-10). WorkspaceTab owns its own padding + scroll —
        this className drops the parent's overflow-y-auto/px/py in favor of
        overflow-hidden flex flex-col so the child's internal scrollers own
        the height. */}
    <TabsContent
      value="workspace"
      className="flex-1 min-h-0 overflow-hidden flex flex-col"
    >
      <WorkspaceTab identity={identity} hostId={hostId} hue={hue} />
    </TabsContent>
    ```

    NO other changes anywhere in IdentityModal.tsx. The existing NAV_SECTIONS shape (`... as const`), the activeTab state + setActiveTab handler, the icon-bar renderer, the useEffect fetch block for identity/wakeups/telegram, the DialogPrimitive.Root + Portal + Content chrome, and every other tab body remain untouched. This is a purely additive integration.

    D-03 self-check: no new UI element added anywhere outside the three edit sites. No badge-click menu, no separate modal. The workspace becomes discoverable through the existing badge → IdentityModal → tab bar path.
  </action>
  <verify>
    <automated>cd /home/ubuntu/fleet/identities/echo-box-maintainer/workspace/skynet &amp;&amp; grep -c "value: \"workspace\"" src/ui/features/pretty-view/IdentityModal.tsx &amp;&amp; grep -c "import WorkspaceTab from" src/ui/features/pretty-view/IdentityModal.tsx &amp;&amp; grep -c "Folder," src/ui/features/pretty-view/IdentityModal.tsx &amp;&amp; grep -c "TabsContent" src/ui/features/pretty-view/IdentityModal.tsx &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -c "IdentityModal" | grep -q "^0$" &amp;&amp; echo TSC_CLEAN_INTEGRATION</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "value: \"workspace\"" src/ui/features/pretty-view/IdentityModal.tsx` returns 1 (NAV_SECTIONS entry)
    - `grep -c "label: \"Workspace\"" src/ui/features/pretty-view/IdentityModal.tsx` returns 1
    - `grep -c "Icon: Folder" src/ui/features/pretty-view/IdentityModal.tsx` returns 1
    - `grep -c "import WorkspaceTab from" src/ui/features/pretty-view/IdentityModal.tsx` returns 1
    - `grep -c "Folder," src/ui/features/pretty-view/IdentityModal.tsx` returns ≥ 1 (Folder added to lucide-react import — appears in the alphabetized named-import list)
    - `grep -c "TabsContent.*value=\"workspace\"\\|value=\"workspace\"" src/ui/features/pretty-view/IdentityModal.tsx` returns ≥ 2 (NAV_SECTIONS entry + TabsContent value)
    - `grep -c "<WorkspaceTab " src/ui/features/pretty-view/IdentityModal.tsx` returns 1 (mount site)
    - The Workspace NAV_SECTIONS entry appears BEFORE the isAdmin spread — verify: `awk '/NAV_SECTIONS = \[/,/\] as const/' src/ui/features/pretty-view/IdentityModal.tsx | grep -n "value: \"workspace\"\\|isAdmin" | head -3` — the workspace line number must be LESS than the isAdmin line number
    - `npx tsc --noEmit` produces zero errors mentioning IdentityModal.tsx (types propagate cleanly from WorkspaceTabProps)
    - `git diff --stat src/ui/features/pretty-view/IdentityModal.tsx` shows a small delta (~15-25 lines added, 1 line changed — the lucide import) — NOT a large refactor. If the diff is > 40 lines added, the task is over-editing.
  </acceptance_criteria>
  <done>The Workspace tab appears in IdentityModal for all users; clicking it mounts WorkspaceTab inline; TSC clean; no other IdentityModal code touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IdentityModal activeTab state → WorkspaceTab mount | activeTab is a string user-controllable via clicking; the value "workspace" is validated by NAV_SECTIONS as-const — no injection surface |
| identity + hostId + hue props → WorkspaceTab | Same values IdentityModal already threads to WakeupsTab and TelegramTab — trust surface is inherited from the existing modal, not expanded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-P4-01 | Elevation of Privilege | Workspace tab reachable by users without host access | mitigate | UI visibility is only a soft gate — the real access check is `permissionManager.canAccessHost` in Plan 118-01. Even if a user clicks the tab without host access, every backend call returns 403 permission_denied and the tab renders the consumer-user copy from Plan 118-02. D-22: not admin-gated. |
| T-118-P4-02 | Repudiation / Stance | New import path opening a leak toward agent | mitigate | This task only imports WorkspaceTab from a sibling file in pretty-view/. WorkspaceTab itself is grep-gated in Plan 118-03 for zero agent-facing side effects. D-04 blind stance inherited. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean across IdentityModal + WorkspaceTab
- git diff shows small, surgical additions only (~15-25 lines) in IdentityModal.tsx
- No other files touched
</verification>

<success_criteria>
- Workspace tab visible in IdentityModal bottom icon-bar for every user
- Clicking the tab mounts WorkspaceTab inline in the tab body — no new modal opens
- All existing IdentityModal behavior (identity tab, wakeups tab, telegram tab for admins, useEffect fetches, modal chrome) preserved byte-identically outside the surgical additions
- Ready for end-to-end UAT in Plan 118-05
</success_criteria>

<output>
Create `.planning/phases/118-workspace-file-browser/118-04-SUMMARY.md` with:
- Files touched (1 file — IdentityModal.tsx)
- Line diff summary (Folder import added, NAV_SECTIONS entry added, WorkspaceTab import added, TabsContent block added — expected ~15-25 lines total)
- Confirmation that no other IdentityModal code was modified
- git log of the commit
</output>
