---
phase: 58-identity-badge-drag-as-third-gesture-press-and-drag-rearrang
plan: 02
type: execute
wave: 2
depends_on: [58-01]
files_modified:
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
  - src/ui/AppShell.tsx
  - src/ui/features/terminal/IdentityBadge.tsx
  - src/ui/shell/IdentitySessionPane.tsx
  - src/ui/features/pretty-view/PrettyView.tsx
autonomous: true
requirements:
  - PV58-CONVLIST-DROP-TARGET-CLOSE
  - PV58-BADGE-DROP-ON-PANE-REARRANGE-INTEGRATION
  - PV58-DOCLOSETAB-TREE-RECONCILE
  - PV58-STRUCTURED-LOGGING

must_haves:
  truths:
    - "Dropping a badge (application/x-skynet-badge payload with a valid tabId) on the conv-list panel calls the panel's onCloseSession(tabId) prop"
    - "AppShell wires onCloseSession to the existing closeTab function so badge-drop-on-conv-list closes the tab (with the confirm-tab-close toast branch preserved via closeTab's existing behavior)"
    - "The conv-list drop handler VALIDATES the payload tabId against the panel's known open tabs list before firing onCloseSession; a payload with an unknown tabId is silently dropped (defense against untrusted dataTransfer per security_config)"
    - "A dragover with only text/plain (row-drag payload from PrettyConversationRow) on the conv-list panel does NOT preventDefault and does NOT capture the drag — no accidental close for row-drags landing back on the list"
    - "A drop with only application/x-skynet-row (row-drag payload) on the conv-list panel does NOT call onCloseSession — the discriminator MIME 'application/x-skynet-badge' is required"
    - "A conv-list drop-close emits a single [convlist-drop] structured log line with tabId"
    - "The badges rendered at IdentitySessionPane.tsx:376 (terminal-mode surface) AND PrettyView.tsx:2927 (pretty-view surface) both receive the current tab's id via their new tabId prop"
    - "Existing doCloseTab reconcile setSplitTree(prev => removeLeaf(prev, id)) at AppShell.tsx:1498 fires when badge-drop-on-conv-list routes through closeTab (regression assertion, no new code — the reconcile ALREADY exists)"
    - "Integration test: dragstart on a rendered IdentityBadge (with tabId + not mobile) writing dual-MIME payload, followed by drop through the same wire contract, exercises the whole path end-to-end via the existing Pane drop text/plain branch (assertion-only, no new production code — Phase 56 openSessionInTree already handles the rearrange case)"
  artifacts:
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "onDragOver + onDrop handlers on the panel outermost DOM element; onCloseSession prop"
      contains: "onCloseSession"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx"
      provides: "New Phase 58 test block covering conv-list drop target behavior (badge drop closes, row drop does not, dragover type-gate, validation of tabId against open tabs, structured log)"
      contains: "application/x-skynet-badge"
    - path: "src/ui/AppShell.tsx"
      provides: "onCloseSession={closeTab} prop plumb at the PrettyConversationsPanel render site (~L1868); tabIds list plumbed through as openTabIds for the panel's validation"
      contains: "onCloseSession="
    - path: "src/ui/shell/IdentitySessionPane.tsx"
      provides: "tabId={tab.id} threaded to the IdentityBadge mount at :376"
      contains: "tabId={tab.id}"
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "tabId prop threaded through to the IdentityBadge mount at :2927"
      contains: "tabId"
    - path: "src/ui/features/terminal/IdentityBadge.tsx"
      provides: "already carries tabId prop from Plan 58-01 — this plan MAY re-touch only to satisfy any consuming-site propagation requirement (e.g. if a new consumer needs a slightly wider prop type). Most likely unchanged."
  key_links:
    - from: "PrettyConversationsPanel.tsx onDrop"
      to: "onCloseSession prop → AppShell.tsx closeTab (:1520)"
      via: "prop-drilling from AppShell render site"
      pattern: "onCloseSession=\\{closeTab\\}"
    - from: "PrettyConversationsPanel.tsx onDrop"
      to: "openTabIds prop → validation guard (per security_config)"
      via: "openTabIds.includes(parsedTabId) gate before firing onCloseSession"
      pattern: "openTabIds"
    - from: "AppShell.tsx closeTab → doCloseTab"
      to: "setSplitTree((prev) => removeLeaf(prev, id)) at :1498"
      via: "existing reconcile — verified present, no code change needed"
      pattern: "setSplitTree.*removeLeaf"
    - from: "IdentityBadge.tsx onDragStart text/plain payload"
      to: "SplitView.tsx Pane onDrop text/plain branch (:340)"
      via: "openSessionInTree(tabId, path, edge) — the Phase 56 rearrange path"
      pattern: "openSessionInTree"
---

<objective>
Deliver the Phase 58 close-and-rearrange integration by (1) wiring the conv-list panel as a drop target that closes the session when a badge is dropped on it, (2) plumbing tabId through the two IdentityBadge mount sites (IdentitySessionPane terminal-mode surface + PrettyView pretty-mode surface) so real badges become drag sources, (3) plumbing closeTab through AppShell as the panel's onCloseSession handler, and (4) proving via an integration test that badge-drop-on-Pane-edge routes through the existing Phase 56 rearrange machinery (openSessionInTree) correctly. Also verifies (no new code) that the existing `setSplitTree(prev => removeLeaf(prev, id))` reconcile inside doCloseTab (AppShell.tsx:1498) fires when a badge-drop-close routes through closeTab, so a stale leaf isn't left in the URL-encoded split tree.

Purpose: Delivers PV58-CONVLIST-DROP-TARGET-CLOSE, PV58-BADGE-DROP-ON-PANE-REARRANGE-INTEGRATION, PV58-DOCLOSETAB-TREE-RECONCILE (assertion-only — reconcile already exists), and continues PV58-STRUCTURED-LOGGING with the [convlist-drop] prefix. On ship, the parent bounty `bring-back-split-view` closes.

Output: PrettyConversationsPanel with panel-level onDragOver + onDrop handlers gated on the application/x-skynet-badge MIME + tabId validation against openTabIds. New Phase 58 tests in PrettyConversationsPanel.test.tsx (A-G, seven tests). AppShell wires onCloseSession={closeTab} + openTabIds={tabs.map(t=>t.id)}. IdentitySessionPane + PrettyView pass tabId={tab.id} / tabId={hostTabId} to their IdentityBadge mounts. Integration test asserts the Phase 56 rearrange path from a badge dragstart to a Pane drop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/58-identity-badge-drag-as-third-gesture-press-and-drag-rearrang/58-CONTEXT.md
@.planning/phases/58-identity-badge-drag-as-third-gesture-press-and-drag-rearrang/58-01-PLAN.md
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
@src/ui/features/pretty-conversations/PrettyConversationRow.tsx
@src/ui/AppShell.tsx
@src/ui/shell/IdentitySessionPane.tsx
@src/ui/features/pretty-view/PrettyView.tsx
@src/ui/features/terminal/IdentityBadge.tsx
@src/ui/lib/split-tree.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add panel-level drop handlers + onCloseSession + openTabIds props to PrettyConversationsPanel, TDD-first</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx, src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx lines 268-338 (component prop signature and destructure) — where the new onCloseSession + openTabIds props slot in
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx lines 1264-1270 (outermost `<div className="relative flex flex-col flex-1 min-h-0 overflow-hidden pb-[env(safe-area-inset-bottom)]" data-testid="pretty-conversations-panel">`) — the DOM element where new onDragOver + onDrop handlers attach
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx (full file) — find via `ls src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx`; extend with the new Phase 58 describe block
    - src/ui/features/pretty-conversations/PrettyConversationRow.tsx lines 927-951 — reference for the row-drag payload contract (application/x-skynet-row) so Test B can construct a realistic non-badge dataTransfer
    - .planning/phases/58-identity-badge-drag-as-third-gesture-press-and-drag-rearrang/58-CONTEXT.md § "In-scope this phase" items 4-5 and § "Test coverage additions" Plan 58-02 A-G
  </read_first>
  <behavior>
    - Test A (badge-drop-closes): render panel with `onCloseSession={fn}` + `openTabIds={["tab-tina-42"]}`; fire drop on panel root with dataTransfer.getData("application/x-skynet-badge") returning `JSON.stringify({tabId:"tab-tina-42"})`; assert `fn` called once with `"tab-tina-42"`.
    - Test B (row-drag-does-not-close): fire drop with dataTransfer.getData("application/x-skynet-row") returning the row payload shape but dataTransfer.getData("application/x-skynet-badge") returning ""; assert `fn` NOT called.
    - Test C (only-text-plain-does-not-close): fire drop with ONLY text/plain="tab-tina-42" set (no application/x-skynet-badge, no application/x-skynet-row); assert `fn` NOT called — discriminator MIME is required (belt-and-suspenders against future rich-payload sources).
    - Test D (dragover-type-gate): fire dragover with dataTransfer.types = ["application/x-skynet-row"] (row-drag); assert `event.defaultPrevented === false` after handler runs (row-drag NOT captured). Then fire dragover with dataTransfer.types = ["application/x-skynet-badge"]; assert `event.defaultPrevented === true` (badge-drag captured to permit drop).
    - Test E (tabId-validation-unknown-tab-silent-drop): render panel with `openTabIds={["tab-alice-1"]}` (only alice open); fire drop with badge payload `JSON.stringify({tabId:"tab-mallory-999"})`; assert `fn` NOT called and NO exception thrown (silent drop per security_config: "validate the received tabId matches an entry in the current tabs[] array before calling closeTab").
    - Test F (structured-log-on-close): spy on `console.info`; fire successful badge drop as in Test A; assert ONE call whose first arg is a string starting with `[convlist-drop] ` and containing `tabId=tab-tina-42`. Extract fields explicitly; do NOT `JSON.stringify(event)`.
    - Test G (malformed-payload-silent-drop): fire drop with application/x-skynet-badge = `"{not valid json"`; assert `fn` NOT called and NO exception thrown. Defense against arbitrary dataTransfer content.
  </behavior>
  <action>
    RED (write failing tests first):
    - If `src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` exists, extend it with a new `describe("Phase 58: conv-list drop target for badge close", () => { … })` block containing Tests A-G. If it does NOT exist, create the file with minimal render harness modeled on existing pretty-conversations tests (`ls src/ui/features/pretty-conversations/*.test.tsx` to find analogs; PrettyConversationRow.test.tsx is the nearest neighbor).
    - Use `fireEvent.drop(panel, { dataTransfer: <stub> })` and `fireEvent.dragOver(panel, { dataTransfer: <stub> })` where the stub is a plain object exposing `getData(type)` reading from a Map + `types` array field. jsdom does NOT construct a real DataTransfer — this is the standard testing pattern (mirror the Test A pattern from IdentityBadge.test.tsx Plan 58-01).
    - Mock all required panel dependencies (useConversations, useIdentities, etc.) — mirror the mocks the existing panel tests use. Provide minimal props: variant="desktop", onDeactivateRow=(() => {}), onCloseSession, openTabIds, and any other REQUIRED props the current signature enforces.
    - Run: `npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` — MUST show Tests A-G failing (component does not yet have drop handlers or new props).
    GREEN (implement in PrettyConversationsPanel.tsx):
    - Add TWO new props to the interface at lines 279-337:
      - `onCloseSession?: (tabId: string) => void;` — optional so callers/tests that don't wire it can render safely (badge-drop-close is a fresh add; the panel must not regress when unwired). Rationale documented inline: "Phase 58 PV58-CONVLIST-DROP-TARGET-CLOSE — receives a validated tabId from a dropped badge; AppShell wires this to closeTab."
      - `openTabIds?: readonly string[];` — optional; defaults to empty array. Used ONLY for the tabId validation guard in the drop handler (per security_config: validate the payload tabId matches an entry in currently-open tabs before calling closeTab).
    - Destructure both new props at ~L268-278 alongside the existing prop destructure. Add `openTabIds = []` default in the destructure.
    - Add a `handlePanelDragOver` handler bound before the return JSX (near the existing `handleRowSelect` / `handleRowDeactivate` helpers). Body: read `e.dataTransfer.types`; if `types` includes `"application/x-skynet-badge"`, call `e.preventDefault()` (this signals the browser that this element is a valid drop target, permitting the drop event to fire). Otherwise, do NOTHING (do not preventDefault) so row-drags fall through without capture. NO log line here — dragover fires constantly during a drag hover; only dragstart + drop should log.
    - Add a `handlePanelDrop` handler. Body:
      1. Read `raw = e.dataTransfer.getData("application/x-skynet-badge")`. If raw is empty string, return early (not a badge drop — silently no-op; row-drops fall through to nothing).
      2. Parse raw as JSON inside a try/catch. On parse error, return early (Test G).
      3. Extract `tabId` field. If not a non-empty string, return early.
      4. Guard: if `openTabIds` array does NOT include the parsed tabId, return early (Test E — per security_config validation).
      5. Call `e.preventDefault()` to signal the drop is handled (prevents default browser behavior like navigating to a text URL).
      6. Emit `console.info(`[convlist-drop] close tabId=${tabId}`)` (Test F — explicit-field extraction per fleet logging directive).
      7. Call `onCloseSession?.(tabId)` — optional call so tests without the prop don't throw.
    - Attach both handlers to the outermost panel `<div>` at ~L1264-1270:
      ```
      onDragOver={handlePanelDragOver}
      onDrop={handlePanelDrop}
      ```
      (No fenced code in action per gsd rules — this is a schematic in prose; the executor writes real JSX at the anchor.)
    - Run: `npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` — MUST show all seven Phase 58 tests passing PLUS every pre-existing test in the file passing (regression gate).
    Commit:
    - `feat(58-02): PrettyConversationsPanel panel-level drop target for badge close + tabId validation (PV58-CONVLIST-DROP-TARGET-CLOSE, PV58-STRUCTURED-LOGGING)`
  </action>
  <acceptance_criteria>
    - `npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` exits 0 with all pre-existing tests plus the seven new Phase 58 tests (A/B/C/D/E/F/G) passing.
    - `grep -c 'onCloseSession' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns >= 2 (prop declaration + call site).
    - `grep -c 'openTabIds' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns >= 2 (prop declaration + validation guard).
    - `grep -c 'application/x-skynet-badge' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns >= 2 (dragover type-gate + drop getData read).
    - `grep -c 'application/x-skynet-row' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns 0 — this plan does NOT reference the row MIME (row-drops on the panel fall through by absence of the badge MIME, not by explicit exclusion of the row MIME).
    - `grep -c '\[convlist-drop\]' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns >= 1.
    - `grep -c 'JSON.stringify(e' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx | head -1` returns 0 additions from this plan — verify the diff by comparing to git baseline. (Existing JSON.stringify calls in the file for legitimate payloads are unrelated.)
    - Handler placement: `onDragOver={handlePanelDragOver}` and `onDrop={handlePanelDrop}` appear on the outermost panel `<div>` at the return statement (grep + read to verify placement).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx</automated>
  </verify>
  <done>Seven Phase 58 tests pass; existing panel tests continue to pass; panel accepts a validated badge payload and fires onCloseSession; unknown-tabId / malformed / row-drag payloads are silently ignored; structured log emits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Plumb tabId to badge mounts + wire AppShell onCloseSession + openTabIds + integration test asserting Phase 56 rearrange from badge drag</name>
  <files>src/ui/AppShell.tsx, src/ui/shell/IdentitySessionPane.tsx, src/ui/features/pretty-view/PrettyView.tsx, src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx</files>
  <read_first>
    - src/ui/AppShell.tsx lines 1448-1538 (doCloseTab + closeTab definitions — note line 1498 `setSplitTree((prev) => removeLeaf(prev, id))` already exists; PV58-DOCLOSETAB-TREE-RECONCILE is assertion-only, no code change)
    - src/ui/AppShell.tsx lines 1868-1968 (PrettyConversationsPanel render site — where onCloseSession + openTabIds props get wired)
    - src/ui/shell/IdentitySessionPane.tsx lines 103, 371-381 (the terminal-mode IdentityBadge mount — receives `tab` in scope; add `tabId={tab.id}`)
    - src/ui/features/pretty-view/PrettyView.tsx lines 148-211 (PrettyViewProps interface — add optional `tabId?: string`) and lines 2917-2932 (the pretty-view IdentityBadge mount — add `tabId={tabId}`)
    - src/ui/shell/IdentitySessionPane.tsx full mount site of PrettyView (grep for `<PrettyView` in that file) — thread `tabId={tab.id}` on the PrettyView mount so PrettyView receives its own current tab id and can forward to its inner IdentityBadge
    - src/ui/lib/split-tree.ts lines 249-... (removeLeaf implementation — for integration-test familiarity; NO CODE CHANGES)
    - Existing PrettyConversationsPanel.test.tsx from Task 1 for the mock setup pattern to extend with the integration test
  </read_first>
  <behavior>
    - Integration Test H (badge-drop-on-conv-list-routes-through-closeTab-and-reconciles-splitTree): mount a minimal AppShell-like harness OR extend PrettyConversationsPanel.test.tsx with a scenario that renders the panel with `onCloseSession={mockCloseTab}` + `openTabIds={["tab-tina-42"]}`; fire a badge drop; assert `mockCloseTab("tab-tina-42")` was called. The reconcile assertion is COVERED BY EXISTING CODE — AppShell.tsx:1498 already calls setSplitTree((prev) => removeLeaf(prev, id)) inside doCloseTab; this test asserts the wire contract (panel → prop → callback) is intact, and the existing reconcile executes automatically when the caller wires onCloseSession={closeTab}. Document this decision inline in the test.
    - Integration Test I (badge-dragstart-payload-shape-matches-Pane-onDrop-contract): render an IdentityBadge with `tabId="tab-alice-1"` (via the same setup pattern as Plan 58-01 Test A); fire dragstart with a captured dataTransfer stub; assert the stub's Map contains `text/plain -> "tab-alice-1"` — the exact key the Pane onDrop text/plain branch at SplitView.tsx:340 reads via `openSessionInTree(tabId, path, edge)`. This is the load-bearing "the badge is a valid drag source for the existing rearrange machinery" assertion, without needing to mount SplitView itself. Full end-to-end Pane rearrange is exercised implicitly by Phase 56's existing SplitView.test.tsx which passes 26/26 unchanged.
    - Test J (regression: Phase 57 SplitView.test.tsx continues to pass 26/26 unchanged after this plan lands). Verify by running the scoped path.
  </behavior>
  <action>
    Sub-step 2a: Plumb tabId through IdentityBadge mount sites.
    - In `src/ui/shell/IdentitySessionPane.tsx` at the IdentityBadge mount at line 376, add `tabId={tab.id}` — `tab` is already in scope (see line 103 `const tabId = tab.id`). Note: the local const already named `tabId` at line 103 is used elsewhere; either reuse it (`tabId={tabId}`) OR reference `tab.id` directly. Reuse the const for consistency.
    - In `src/ui/features/pretty-view/PrettyView.tsx` at the PrettyViewProps interface (~L148-211), add a new optional prop `tabId?: string;` with inline comment: "Phase 58: forwarded to the inner IdentityBadge so drag-source semantics activate for pretty-view-mounted badges. Optional so pre-Phase-58 callers (tests, standalone previews) keep working — badge falls back to draggable=false when tabId is absent."
    - Destructure `tabId` from props at the component signature (~L148 destructure block, wherever the other props are pulled).
    - At the IdentityBadge mount at line 2927, add `tabId={tabId}`.
    - At the IdentitySessionPane mount site of `<PrettyView …/>` (find via `grep -n '<PrettyView' src/ui/shell/IdentitySessionPane.tsx`), thread `tabId={tab.id}` so PrettyView receives its own tab id and forwards to its inner IdentityBadge.
    Sub-step 2b: Wire AppShell onCloseSession + openTabIds at the PrettyConversationsPanel render site.
    - In `src/ui/AppShell.tsx` at ~L1868 (the `<PrettyConversationsPanel …/>` render), add two new props inline with the existing ones:
      - `onCloseSession={closeTab}` — reuses the internal closeTab function verbatim (which handles the confirm-tab-close toast branch and routes to doCloseTab, which in turn already fires the setSplitTree removeLeaf reconcile at line 1498).
      - `openTabIds={tabs.map((t) => t.id)}` — the validation source for the panel's drop guard. Uses the panel's already-in-scope `tabs` state.
    - Add an inline comment above the two new props: "Phase 58 PV58-CONVLIST-DROP-TARGET-CLOSE + PV58-DOCLOSETAB-TREE-RECONCILE: badge drop on the conv-list panel closes the tab. closeTab already reconciles splitTree via removeLeaf inside doCloseTab (AppShell.tsx:1498) — no additional wiring needed here."
    - Do NOT modify doCloseTab or closeTab or the removeLeaf call at line 1498. Existing reconcile satisfies PV58-DOCLOSETAB-TREE-RECONCILE.
    Sub-step 2c: Add integration tests H + I to PrettyConversationsPanel.test.tsx (extend Task 1's file).
    - Test H: extend the existing Phase 58 describe block with the scenario described above — assert the wire contract from panel drop → onCloseSession callback fires with the validated tabId. Document inline that the tree reconcile side is covered by AppShell.tsx:1498 (already exists pre-Phase-58) and does not need a separate assertion in this scoped panel test.
    - Test I: render an IdentityBadge with `tabId="tab-alice-1"` + `onClick={() => {}}`; fire dragstart with a fresh dataTransfer stub; assert `stub.map.get("text/plain") === "tab-alice-1"` — the payload shape the Pane onDrop at SplitView.tsx:340 reads. Comment inline: "This asserts the payload the badge sets IS the payload the Phase 56 Pane onDrop text/plain branch reads via openSessionInTree(tabId, path, edge) — no need to mount SplitView; that layer is UAT-verified by SplitView.test.tsx 26/26."
    Sub-step 2d: Regression gate — run the Phase 57 SplitView test suite and confirm 26/26 still pass.
    - Run `npx vitest run src/ui/shell/SplitView.test.tsx` and confirm exit 0 with 26 passing.
    Commit (single commit for the whole task):
    - `feat(58-02): wire badge tabId at mount sites + AppShell onCloseSession + integration tests (PV58-BADGE-DROP-ON-PANE-REARRANGE-INTEGRATION, PV58-DOCLOSETAB-TREE-RECONCILE)`
  </action>
  <acceptance_criteria>
    - `npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` exits 0 with Tests A-G (from Task 1) PLUS Tests H + I passing.
    - `npx vitest run src/ui/shell/SplitView.test.tsx` exits 0 with 26/26 passing (Phase 57 regression gate).
    - `npx vitest run src/ui/features/terminal/IdentityBadge.test.tsx` exits 0 with 13/13 passing (Plan 58-01 regression gate).
    - `grep -c 'onCloseSession=\{closeTab\}' src/ui/AppShell.tsx` returns 1.
    - `grep -c 'openTabIds=\{tabs\.map' src/ui/AppShell.tsx` returns 1.
    - `grep -c 'tabId=\{tabId\}\|tabId=\{tab\.id\}' src/ui/shell/IdentitySessionPane.tsx` returns >= 2 (one on IdentityBadge, one on PrettyView).
    - `grep -c 'tabId=\{tabId\}' src/ui/features/pretty-view/PrettyView.tsx` returns >= 1 (on the IdentityBadge mount).
    - `grep -c 'tabId\?: string' src/ui/features/pretty-view/PrettyView.tsx` returns >= 1 (prop declaration in PrettyViewProps interface).
    - `grep -n 'setSplitTree((prev) => removeLeaf(prev, id))' src/ui/AppShell.tsx` returns a match on or near line 1498 — verify this line was NOT deleted (the reconcile PV58-DOCLOSETAB-TREE-RECONCILE relies on).
    - No changes to `src/ui/lib/split-tree.ts` (`git diff --stat src/ui/lib/split-tree.ts` returns empty).
    - No changes to `src/ui/shell/SplitView.tsx` (`git diff --stat src/ui/shell/SplitView.tsx` returns empty).
    - No changes to `src/ui/features/pretty-conversations/PrettyConversationRow.tsx` (`git diff --stat …` returns empty).
    - No changes to `src/ui/features/pretty-view/PrettyView.tsx` besides the tabId prop threading (verify by reading the diff — should be ~3 line additions: interface, destructure, mount).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx src/ui/features/terminal/IdentityBadge.test.tsx src/ui/shell/SplitView.test.tsx</automated>
  </verify>
  <done>Panel test file passes A-G + H + I; Phase 57 SplitView 26/26 regression passes; Plan 58-01 IdentityBadge 13/13 regression passes; badges at both mount sites (IdentitySessionPane + PrettyView) receive their tab's id; AppShell wires onCloseSession={closeTab} and openTabIds={tabs.map(...)} at the panel render site; existing doCloseTab reconcile is preserved unchanged; parent bounty `bring-back-split-view` is ready to close on Ashley UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dataTransfer → conv-list panel drop handler | **untrusted-input surface** (this plan's new attack surface per security_config). A dropped payload's tabId string is fully attacker-controllable in principle — any page-scoped script or clever drag source could inject `{tabId:"tab-mallory-999"}`. |
| conv-list panel drop handler → onCloseSession → AppShell.closeTab | trusted-after-validation. Once the tabId passes the openTabIds guard, closeTab treats it as a legitimate close request (same contract as the kbd-shortcut and Deactivate button paths). |
| dataTransfer → Pane onDrop (Phase 56, unchanged) | trusted contract, reused as-is. Badge text/plain payload = row text/plain payload = tabId. The Pane's openSessionInTree already handles the "session not in tabs[]" case by opening it (patch #511 resolveRowPayloadTabId ladder at AppShell.tsx:1683-1707); a bogus tabId on a Pane drop degrades to a no-op after that ladder fails. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-02-01 | Spoofing/Elevation | Attacker-controlled dataTransfer payload injects an arbitrary tabId string, hoping to close a tab the user did not intend to close | **mitigate** | Task 1 drop handler validates the parsed tabId against `openTabIds` prop (currently-open tab ids from AppShell.tabs) BEFORE calling onCloseSession. Test E asserts an unknown tabId is silently dropped. Per security_config: "in the conv-list drop handler, validate the received tabId matches an entry in the current tabs[] array before calling closeTab." |
| T-58-02-02 | Tampering | Malformed JSON in application/x-skynet-badge payload throws an unhandled exception, crashing the panel | mitigate | Task 1 drop handler wraps JSON.parse in try/catch; Test G asserts malformed payload is silently dropped without throwing. |
| T-58-02-03 | Denial of Service | An attacker triggers a flood of badge drops to spam closeTab and force churn | accept | Local-only interaction (in-page badge dragged by the same user session). closeTab already handles the confirm-tab-close toast preference internally at AppShell.tsx:1520-1538 — repeated closes just queue toasts. No amplification vector. |
| T-58-02-04 | Repudiation | Missing telemetry on conv-list drop-close makes a "who closed my session?" debug session impossible | mitigate | Structured `[convlist-drop] close tabId=<x>` log per Test F. Explicit-field extraction; do NOT JSON.stringify(event). |
| T-58-02-05 | Information Disclosure | `[convlist-drop]` log emits the tabId which is also visible in AppShell state | accept | tabId is not PII; already visible in the DOM (data-testid attributes on rows) and console (existing [pv-split-tree] diags). No new disclosure surface. |
| T-58-02-06 | Tampering | An unrelated drop on the panel (e.g. dragging a file from the OS) fires the drop handler and could preventDefault, blocking the browser's default file-open behavior | accept | The dragover handler's type-gate on application/x-skynet-badge means non-badge drops never get preventDefault called during dragover — the drop event on the panel fires with the browser's default not-a-drop-target semantic, so a file drop bubbles up as if the panel weren't there. Test D asserts this. |
| T-58-02-07 | Tampering | Stale leaf in splitTree after closeTab fires would leave the URL-encoded layout referencing a torn-down session | mitigate | AppShell.tsx:1498 `setSplitTree((prev) => removeLeaf(prev, id))` inside doCloseTab already fires on every close path (kbd shortcut, Deactivate button, badge drop). PV58-DOCLOSETAB-TREE-RECONCILE is satisfied by existing code; Task 2 acceptance criteria include a grep assertion that this line remains present after the plan. |
</threat_model>

<verification>
- `npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx src/ui/features/terminal/IdentityBadge.test.tsx src/ui/shell/SplitView.test.tsx` — all three files exit 0.
- `git diff --stat src/ui/lib/split-tree.ts src/ui/shell/SplitView.tsx src/ui/features/pretty-conversations/PrettyConversationRow.tsx src/ui/lib/split-tree-url.ts` — all four MUST return empty diffs (files untouched per phase scope-lock).
- Manual read: AppShell.tsx:1498 `setSplitTree((prev) => removeLeaf(prev, id))` line is present and unchanged.
- Manual read: the pretty-conversations panel's outermost `<div data-testid="pretty-conversations-panel">` at ~L1264 has BOTH `onDragOver={handlePanelDragOver}` AND `onDrop={handlePanelDrop}` attributes.
- End-to-end UAT (Ashley, post-ship, out of executor scope): drag a badge from Pane A's edge onto Pane B's edge in a multi-session split tree, confirm the source cell collapses and the target cell splits. Drag a badge onto the conv-list, confirm the session closes and the split tree reconciles without leaving a stale leaf.
</verification>

<success_criteria>
1. PrettyConversationsPanel.test.tsx Phase 58 block (Tests A-G + H + I) all pass.
2. Pre-existing PrettyConversationsPanel.test.tsx tests all pass (regression).
3. IdentityBadge.test.tsx 13/13 pass (Plan 58-01 regression).
4. SplitView.test.tsx 26/26 pass (Phase 57 regression).
5. AppShell wires `onCloseSession={closeTab}` + `openTabIds={tabs.map((t) => t.id)}` at the PrettyConversationsPanel render site.
6. IdentitySessionPane passes `tabId={tabId}` to its IdentityBadge mount at line 376 AND `tabId={tab.id}` to its PrettyView mount.
7. PrettyView accepts a `tabId?: string` prop and forwards it to its inner IdentityBadge at line 2927.
8. Existing `setSplitTree((prev) => removeLeaf(prev, id))` reconcile at AppShell.tsx:1498 is unchanged (verified via grep + read).
9. Files `src/ui/lib/split-tree.ts`, `src/ui/shell/SplitView.tsx`, `src/ui/features/pretty-conversations/PrettyConversationRow.tsx`, `src/ui/lib/split-tree-url.ts` are UNTOUCHED (verified via git diff).
10. Parent bounty `bring-back-split-view` moves to closable state on Ashley UAT (post-ship, out of executor scope).
</success_criteria>

<output>
Create `.planning/phases/58-identity-badge-drag-as-third-gesture-press-and-drag-rearrang/58-02-SUMMARY.md` when done.
</output>
