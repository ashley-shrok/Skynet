---
phase: 70-rewrite-prettyview-auto-scroll-from-first-principles-two-sta
plan: 02
type: execute
wave: 2
depends_on:
  - 70-01
files_modified:
  - src/ui/features/pretty-view/use-auto-scroll.ts
  - src/ui/features/pretty-view/use-auto-scroll.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "useAutoScroll(paneKey) returns { scrollRef, jumpToBottom, onSendFired, mode, revealed } — NEW API surface, `sentinelRef` no longer exists, `scrollToBottomAndFollow` and `isPinnedToBottom` are renamed"
    - "Scroll listener converts raw scroll events to `{kind:'user-input'}` reducer events ONLY when the scroll was user-caused (event.isTrusted === true), never for programmatic writes"
    - "MutationObserver on the scroll container's childList + subtree emits ONE `{kind:'content-changed'}` reducer event per DOM mutation batch"
    - "ResizeObserver on the scroll container emits `{kind:'container-resized'}` reducer events"
    - "All chase writes are coalesced into a single scrollTop = scrollHeight write per requestAnimationFrame, regardless of how many events fired within the frame"
    - "Chase writes use synchronous scrollTop assignment (never scrollTo({behavior:'smooth'}) or any smooth-scroll API) — instant-only per shape file § Chase behavior LOCKED"
    - "Mount-landing hides the scroll container until first non-zero contentHeight measurement, then jumps to bottom, then flips `revealed` true — no visible flash at top on first paint"
    - "Total observer count in the hook is ≤ 2 (one MutationObserver + one ResizeObserver) — no IntersectionObserver, no sentinel-div consumer, no per-child ResizeObserver"
    - "The `mode` state drives jump-to-bottom pill visibility via `mode === 'not-at-bottom'`"
    - "Session re-entry (unmount preserved by React tree, hook not remounted) does NOT touch scroll position"
  artifacts:
    - path: "src/ui/features/pretty-view/use-auto-scroll.ts"
      provides: "Thin React hook wrapping the pure auto-scroll-machine reducer + DOM refs + RAF-coalesced chase writer + hide-pin-reveal landing + [pv-scroll-diag] logging per reducer decision"
      exports: ["useAutoScroll", "UseAutoScrollResult"]
      contains: "auto-scroll-machine"
      min_lines: 150
    - path: "src/ui/features/pretty-view/use-auto-scroll.test.ts"
      provides: "renderHook + JSDOM scroll-container harness + TestConsumer-driven behavior tests locking T1-T15 from PATTERNS.md"
      contains: "makeScrollEl"
      min_lines: 400
  key_links:
    - from: "src/ui/features/pretty-view/use-auto-scroll.ts"
      to: "src/ui/features/pretty-view/auto-scroll-machine.ts"
      via: "import { reduce, createInitialState, type AutoScrollState, BOTTOM_TOLERANCE_PX, BOTTOM_TOLERANCE_TOUCH_EXTRA_PX } from './auto-scroll-machine'"
      pattern: "from \"./auto-scroll-machine\""
    - from: "src/ui/features/pretty-view/use-auto-scroll.ts"
      to: "src/ui/hooks/use-is-touch-device.ts"
      via: "import { useIsTouchDevice } from '../../hooks/use-is-touch-device'"
      pattern: "from.*use-is-touch-device"
---

<objective>
Rewrite `use-auto-scroll.ts` as a thin ~150-200 LOC React hook wrapping the pure `reduce()` from `./auto-scroll-machine.ts`. The hook owns the DOM refs, the RAF coalescer, the observer wiring, the `[pv-scroll-diag]` logging, and the hide-pin-reveal mount-landing — nothing else. All state-transition logic delegates to the pure reducer.

Rewrite `use-auto-scroll.test.ts` around the T1-T15 coverage list from 71-PATTERNS.md § "Test coverage list", preserving the two load-bearing patterns from the current test file (`makeScrollEl` JSDOM harness at L53-118, `fireScroll(el)` synthetic-event helper at L120-128) and adding a TestConsumer component so event-driven behaviors (send-fired, jump-clicked, revealed) can be exercised through real DOM.

Purpose: This is the "single named seam" (per Phase 30's usePaneResolvingMachine.ts docstring pattern) that PrettyView.tsx binds to. The hook's contract IS the reducer's contract plus a small set of named event handlers. Any deviation from the reducer's truth table is a bug in this file; the hook must not add its own mode logic.

Output: Two file rewrites (both existing files replaced). NEW API surface `{ scrollRef, jumpToBottom, onSendFired, mode, revealed }` — the old surface `{ scrollRef, sentinelRef, scrollToBottomAndFollow, isPinnedToBottom }` is deleted. PrettyView.tsx integration edits land in Plan 70-03.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/ubuntu/skynet-tina/.planning/phases/71-rewrite-prettyview-auto-scroll-from-first-principles-two-sta/71-CONTEXT.md
@/home/ubuntu/skynet-tina/.planning/phases/71-rewrite-prettyview-auto-scroll-from-first-principles-two-sta/71-PATTERNS.md
@/home/ubuntu/skynet-tina/.planning/phases/71-rewrite-prettyview-auto-scroll-from-first-principles-two-sta/71-01-SUMMARY.md
@/home/ubuntu/skynet-tina/src/ui/features/pretty-view/auto-scroll-machine.ts
@/home/ubuntu/skynet-tina/src/ui/features/pretty-view/usePaneResolvingMachine.ts
@/home/ubuntu/skynet-tina/src/ui/features/pretty-view/useHoldToRecord.ts
@/home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.ts
@/home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.test.ts
@/home/ubuntu/skynet-tina/src/ui/hooks/use-is-touch-device.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite use-auto-scroll.ts as thin wrapper around the pure reducer</name>
  <files>src/ui/features/pretty-view/use-auto-scroll.ts</files>
  <read_first>
    - /home/ubuntu/skynet-tina/src/ui/features/pretty-view/usePaneResolvingMachine.ts (STRUCTURAL ANALOG for thin-wrapper hook shape — copy the header docstring philosophy at L1-14 "trivial derivation ... named seam ... contract testable in isolation"; copy the argument-object + result-object export pattern at L28-41; note the hook is 8 LOC of body — the auto-scroll wrapper is bigger only because it owns DOM refs + RAF coalescer + observers, NOT because it owns transition logic)
    - /home/ubuntu/skynet-tina/src/ui/features/pretty-view/useHoldToRecord.ts (STRUCTURAL ANALOG for ref-vs-state discipline — copy the refs block header + per-ref rationale comments at L201-232; adopt the "This is a ref (not state) because reads happen during render and setting it must not trigger a re-render" convention verbatim for RAF handles, DOM element pointers, hasLandedOnce, lastMeasuredDistance; copy the constants-block-with-shape-file-line-reference style from L82-104)
    - /home/ubuntu/skynet-tina/src/ui/features/pretty-view/auto-scroll-machine.ts (the pure reducer — read its exports, especially the `AutoScrollEvent` discriminated union variants and the `reduce()` return shape `{next, effect}`)
    - /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.ts (the CURRENT implementation being replaced — read to identify what to DELETE: sentinelRef callback ref at L110, IntersectionObserver wiring, `didFirstContentScrollRef` first-content bypass at L47, `pinnedRef` scroll-listener authority mixing, MutationObserver + per-child ResizeObserver duo — the observer stack that phase 70 explicitly repudiates)
    - /home/ubuntu/skynet-tina/src/ui/hooks/use-is-touch-device.ts (source of the `useIsTouchDevice()` boolean the hook consumes to build `{kind:"user-input", isTouch}` events)
    - /home/ubuntu/skynet-tina/.planning/phases/71-rewrite-prettyview-auto-scroll-from-first-principles-two-sta/71-PATTERNS.md (§ "use-auto-scroll.ts (REWRITE)" — full pattern spec for this file, including the target return-object shape at L200-209, the ref-vs-state table at L184-208, the diagnostic-logging reshape at L237-240)
    - /home/ubuntu/skynet-tina/.planning/phases/71-rewrite-prettyview-auto-scroll-from-first-principles-two-sta/71-CONTEXT.md (§ "Code context" — the accessory-bubble mount siblings the MutationObserver watches; § "What would make it wrong" bullets 3-6 — the six anti-patterns this file must not slide back into)
  </read_first>
  <behavior>
    - Hook signature: `useAutoScroll(paneKey: string): UseAutoScrollResult` — messageCount parameter REMOVED (it was a proxy for content-changed detection; the MutationObserver on scroll-container children is now the direct source).
    - Return shape (exact keys, no more, no fewer): `{ scrollRef, jumpToBottom, onSendFired, mode, revealed }`.
      * `scrollRef: (el: HTMLElement | null) => void` — callback ref for the scroll container.
      * `jumpToBottom: () => void` — dispatches `{kind:"jump-clicked"}` to reducer, wired by PrettyView.tsx to the jump-to-bottom pill onClick AND the LoadMore `onGoodToGo` prop (see Plan 70-03).
      * `onSendFired: () => void` — dispatches `{kind:"send-fired"}` to reducer, wired by PrettyView.tsx `handleComposeSend` (Plan 70-03).
      * `mode: Mode` — the current state.mode from the reducer, exposed as React state so re-renders fire on transition. Drives jump-to-bottom pill visibility in PrettyView.tsx.
      * `revealed: boolean` — hide-pin-reveal state. `false` from mount until first `effect: "reveal"` returned by reducer, then `true` forever. Drives the `visibility: hidden` wrapper in PrettyView.tsx (see Plan 70-03).
    - Refs (NOT state — do not trigger re-renders):
      * `scrollElRef` — the bound scroll container HTMLElement (use `useState<HTMLElement | null>` for the ref-CALLBACK to fire mount-driven useEffects, but the ELEMENT VALUE is read via ref semantics in event handlers — same pattern as current file L98-100).
      * `stateRef` — the current `AutoScrollState` (mirror of the reducer state; read synchronously in event handlers).
      * `rafHandleRef` — pending requestAnimationFrame handle for chase-write coalescing (null when no chase pending).
      * `pendingChaseRef` — boolean flag: does the current RAF frame need to write scrollTop = scrollHeight?
      * `mutationObserverRef` — the MutationObserver watching scroll-container children.
      * `resizeObserverRef` — the ResizeObserver watching the scroll container.
      * `mountLandingActiveRef` — boolean flag: true from mount until the reducer returns `effect: "reveal"` (which flips `hasLandedOnce` true). Read inside the ResizeObserver callback to decide whether to ALSO dispatch a `{kind:"measured"}` event alongside the always-dispatched `{kind:"container-resized"}`. Cleared in the dispatch wrapper on the same call that observes `effect === "reveal"`.
    - State (triggers re-renders):
      * `mode` — mirror of stateRef.current.mode for the return value.
      * `revealed` — for the hide-pin-reveal visibility gate.
    - Event → reducer wiring:
      * Scroll listener on scrollEl: on `scroll` event, IF `event.isTrusted === true` (user-caused, not programmatic), compute `distanceFromBottom = scrollHeight - scrollTop - clientHeight`, dispatch `{kind:"user-input", distanceFromBottom, isTouch: isTouchDevice}`. If `event.isTrusted === false` OR the event fired within the same RAF as a chase-write (guarded by `pendingChaseRef`), SKIP dispatch — this is the input-origin bit from shape-file § What would make it wrong bullet 4 ("iOS momentum-scroll silent-flip during chase — input-origin bit skipped").
      * MutationObserver on scrollEl with `{childList: true, subtree: true}`: on any mutation, dispatch `{kind:"content-changed"}`. This uniformly handles new messages arriving, accessory bubbles (WipBubble/WaitingBubble/PlanPendingBubble/AsideBubble) mounting/unmounting, and in-bubble content growth — the symmetric-event-handling invariant per § Shape para 2.
      * ResizeObserver on scrollEl: on `resize`, dispatch `{kind:"container-resized"}`. Handles window resize, pane-count/split-layout change, browser zoom.
      * `jumpToBottom()` action: dispatches `{kind:"jump-clicked"}`.
      * `onSendFired()` action: dispatches `{kind:"send-fired"}`.
      * Mount-landing measurement (SEPARATE mechanism — the ResizeObserver ALWAYS dispatches `{kind:"container-resized"}` unconditionally per bullet above; mount-landing rides alongside it, does NOT special-case the RO output). Wiring: a dedicated `useEffect(() => {...}, [scrollEl])` that (a) fires an initial synchronous `dispatch({kind:"measured", distanceFromBottom: computeDistance(scrollEl), contentHeight: scrollEl.scrollHeight})` where `computeDistance(el) = el.scrollHeight - el.scrollTop - el.clientHeight` (NOT hardcoded 0 — the initial distance reflects actual geometry, so if the first content mount already leaves scrollTop at bottom the distance is 0, and if content overflows before mount-jump the distance is non-zero and the reducer handles it correctly), and (b) subscribes an ADDITIONAL callback into the ResizeObserver tick chain (via a `mountLandingActiveRef` boolean checked inside the RO callback) so that on every RO tick UNTIL `stateRef.current.hasLandedOnce === true` the hook dispatches BOTH `{kind:"container-resized"}` AND `{kind:"measured", distanceFromBottom: computeDistance(scrollEl), contentHeight: scrollEl.scrollHeight}` — two events per tick during the mount window, one event per tick (only container-resized) after `hasLandedOnce` flips true. The `mountLandingActiveRef` is set to `true` on mount and set to `false` in the dispatch wrapper the moment the reducer returns `effect: "reveal"` (which is the same moment `hasLandedOnce` flips true per Plan 70-01's mount-landing branch). This structure keeps the ResizeObserver's PER-EVENT-KIND output uniform (always `container-resized`) — no contamination from the observer branching on state — while still driving mount-landing measured events through the same tick clock.
    - Reducer dispatch wrapper (single seam): every event handler calls `dispatch(event)` which computes `const { next, effect } = reduce(stateRef.current, event); stateRef.current = next;` and then, in order: (i) if `next.mode !== previousMode` call `setMode(next.mode)` and log `mode-in` / `mode-out` as appropriate; (ii) if `effect === "reveal"` call `setRevealed(true)`, clear `mountLandingActiveRef.current = false`, and log `mount-land`; (iii) if `effect === "chase"` mark `pendingChaseRef.current = true`, call `scheduleRafChase()`, and log `chase-write`; (iv) ELSE IF `effect === "none"` AND `stateRef.current.mode === "not-at-bottom"` AND the event kind is one of `content-changed | container-resized | measured` (i.e. a bottom-moving event fired while the user is scrolled away), log `chase-skip` with `{event: event.kind, mode: stateRef.current.mode}` — this is the observability signal for the "content grew but we deliberately didn't chase because user is scrolled up" case (§ Shape para on no-yank invariant); (v) if the event kind is `user-input` with `event.isTrusted === true` log `user-gesture`; if the incoming raw `scroll` DOM event was skipped because `event.isTrusted === false` OR `pendingChaseRef.current === true`, the scroll-listener itself (NOT the dispatch wrapper, since dispatch is never called in that path) logs `programmatic-skip` at the skip site.
    - Chase writer: `scheduleRafChase()` is idempotent within a frame (guarded by `rafHandleRef.current !== null`). Inside the RAF callback: `if (pendingChaseRef.current && scrollEl) { scrollEl.scrollTop = scrollEl.scrollHeight; pendingChaseRef.current = false; }`. Then `rafHandleRef.current = null`. This is the "single write per animation frame regardless of how many events triggered them" behavior from shape-file § Chase behavior.
    - Diagnostic logging: log `[pv-scroll]` (drop the `-diag` suffix per phase-71 event class per phase-guidance) at each dispatch point. Format: `[pv-scroll] {event-name} mode={mode} dist={distanceFromBottom} paneKey={paneKey}`. Event names to use (from phase-guidance): `mode-in`, `mode-out`, `chase-write`, `chase-skip`, `mount-land`, `user-gesture`, `programmatic-skip`. Each name has a defined emission site per the dispatch-wrapper spec above (`chase-skip` is emitted in the ELSE branch when a bottom-moving event lands on `mode === "not-at-bottom"` with `effect === "none"`). Log OUTSIDE the reducer at the dispatch site, not inside `reduce()` (keeps reducer pure).
    - Cleanup: on hook unmount, disconnect both observers, cancel any pending RAF, remove the scroll listener. Session re-entry (paneKey stays same, hook not remounted) MUST NOT touch scroll position — the hook body has no `useEffect` that resets state on paneKey change. Per shape-file § Session re-entry "The view stays where the user left it, since the pretty view stays mounted across session switches" — the state carried in stateRef survives across paneKey changes if the hook is not remounted. paneKey is retained as a parameter FOR LOGGING ONLY (included in every log line), not for logic.
  </behavior>
  <action>
Rewrite `src/ui/features/pretty-view/use-auto-scroll.ts` from scratch — do not incrementally edit the current file. The current file is the observer-stack shape phase 70 explicitly repudiates (per 71-PATTERNS.md § "No Analog Found" note L493 "the current use-auto-scroll.ts itself is DELIBERATELY NOT used as a pattern reference").

Structure the new file in this section order (mirroring useHoldToRecord.ts's organization):
  1) Block-comment header: (a) purpose — "phase 70 rewrite: thin React wrapper around the pure `auto-scroll-machine` reducer"; (b) architectural framing — copy the usePaneResolvingMachine.ts L1-14 docstring style: "named seam ... contract testable in isolation via renderHook AND via the pure reducer's own tests ... ZERO transition logic in this file — every mode change is a `reduce()` return"; (c) invariants list — cite shape-file § What would make it wrong bullets 1-6 by number and state how this file enforces each; (d) grep-gate summary listing the observer-count gate ("≤ 2 observers total: 1 MutationObserver + 1 ResizeObserver, 0 IntersectionObserver, 0 sentinel-div consumer") and the "no smooth-scroll" gate.
  2) Imports: React (`useCallback, useEffect, useRef, useState`), `./auto-scroll-machine` (all types + `reduce` + `createInitialState` + `BOTTOM_TOLERANCE_PX` + `BOTTOM_TOLERANCE_TOUCH_EXTRA_PX`), `../../hooks/use-is-touch-device` (`useIsTouchDevice`).
  3) `UseAutoScrollResult` interface — the exact five-key return type described in `<behavior>`.
  4) `useAutoScroll(paneKey: string): UseAutoScrollResult` implementation:
     4a) State block (mode + revealed) with the useHoldToRecord.ts L229-232 rationale comment shape ("mode is state because it drives pill visibility in the consumer's render — a re-render must fire on transition").
     4b) Ref block with per-ref rationale comments matching useHoldToRecord.ts L201-227 style. Rationale for stateRef: "mirror of the reducer's authoritative state; read synchronously inside event handlers so we always dispatch against the freshest state without waiting for React to flush the setMode".
     4c) `useIsTouchDevice()` invocation, stored in a ref so the value is available inside event handlers without triggering re-runs.
     4d) `dispatch(event)` internal function — the single seam. Order per the `<behavior>` dispatch-wrapper spec: (i) call `reduce(stateRef.current, event)`, (ii) update stateRef.current, (iii) log mode-in/mode-out on transition + call `setMode`, (iv) on `effect === "reveal"` call `setRevealed(true)` + clear `mountLandingActiveRef.current` + log `mount-land`, (v) on `effect === "chase"` set `pendingChaseRef.current = true`, call `scheduleRafChase()`, + log `chase-write`, (vi) on `effect === "none"` when the event was a bottom-moving kind (`content-changed | container-resized | measured`) AND `stateRef.current.mode === "not-at-bottom"`, log `chase-skip` with the event kind — this makes the reducer's "user scrolled up so we deliberately did not chase" decision visible in the runtime log stream.
     4e) `scheduleRafChase()` internal function — idempotent RAF scheduler. Guarded by `rafHandleRef.current !== null`. Inside RAF callback: write `scrollEl.scrollTop = scrollEl.scrollHeight` if `pendingChaseRef.current`, then reset the pending flag and rafHandleRef.
     4f) `useEffect` on `[scrollEl]` for observer setup: wire the scroll listener with `event.isTrusted` gate (log `programmatic-skip` and return early when the raw scroll event has `event.isTrusted === false` OR `pendingChaseRef.current === true`), the MutationObserver with `{childList: true, subtree: true}`, the ResizeObserver. Return cleanup disconnecting all three + cancelling any pending RAF.
     4g) `useEffect` on `[scrollEl]` for mount-landing kick-off: fire an initial `dispatch({kind:"measured", distanceFromBottom: scrollEl ? scrollEl.scrollHeight - scrollEl.scrollTop - scrollEl.clientHeight : 0, contentHeight: scrollEl?.scrollHeight ?? 0})` — the distanceFromBottom is computed from live geometry per the `computeDistance(el) = el.scrollHeight - el.scrollTop - el.clientHeight` formula from `<behavior>` line "Mount-landing measurement" (NOT hardcoded 0; the reducer needs the true initial distance so an already-at-bottom first paint transitions correctly and a pre-scrolled first paint is respected). Subsequent measured events flow via the ResizeObserver while `mountLandingActiveRef.current === true`.
     4h) `jumpToBottom = useCallback(() => dispatch({kind:"jump-clicked"}), [])` and `onSendFired = useCallback(() => dispatch({kind:"send-fired"}), [])`.
     4i) Return `{ scrollRef, jumpToBottom, onSendFired, mode, revealed }`.

DO NOT do any of the following (each is a specific anti-pattern from § What would make it wrong):
  - Do NOT add an `if (event.kind === "content-changed") {...} else if (event.kind === "accessory-mount") {...}` special-case block in the hook — the reducer treats these uniformly and any per-kind branching in the hook contaminates the invariant (bullet 1).
  - Do NOT skip the `event.isTrusted` gate on scroll — this is what prevents chase-writes from re-transitioning mode (bullet 2 "programmatic scroll write transitions mode → recursive-bug pattern returns"). Also skip when `pendingChaseRef.current === true` as belt-and-suspenders.
  - Do NOT use `scrollEl.scrollIntoView({behavior:"smooth"})`, `scrollEl.scrollTo({behavior:"smooth"})`, or CSS `scroll-behavior: smooth`. Only synchronous `scrollEl.scrollTop = scrollEl.scrollHeight` writes (§ Chase behavior LOCKED "instant scroll writes ... not smooth-scroll").
  - Do NOT add a third observer (no IntersectionObserver, no per-child ResizeObserver). Exactly 1 MutationObserver + 1 ResizeObserver (bullet 6 "three or more distinct kinds of watchers racing each other ... slid back into current code's shape").
  - Do NOT reset scroll position on paneKey change. paneKey is parameter-carried FOR LOGGING ONLY. The current file's L98-100 + L112-115 `paneKey`-tracking effect that resets `pinnedRef` and jumps to scrollHeight is DELETED — session re-entry preserves position per shape § Session re-entry.
  - Do NOT export `sentinelRef`. The new return API has exactly five keys.

Naming: use `dispatch` as the internal wrapper name (matches Redux/React idiom). Use `scheduleRafChase` (not `scheduleChase`) so the RAF-scheduling nature is visible at call sites.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -E "use-auto-scroll\.ts" | grep -v "\.test\.ts" | wc -l | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/ui/features/pretty-view/use-auto-scroll.ts` (rewritten, not incrementally edited).
    - `grep -c "IntersectionObserver" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.ts` returns `0` (shape-file reverses the sentinel-IO decision).
    - `grep -c "sentinelRef" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.ts` returns `0` (sentinel API surface deleted).
    - `grep -c "MutationObserver\|ResizeObserver" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.ts` is between `2` and `4` inclusive (accounts for type + constructor references; the OBSERVER-INSTANCE count is exactly 1 of each — this grep is a proxy).
    - `grep -Ec "behavior:\s*[\"']smooth[\"']|scroll-behavior:\s*smooth|scrollIntoView" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.ts` returns `0` (no smooth-scroll — instant writes only per § Chase behavior LOCKED).
    - `grep -c "scrollTop\s*=" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.ts` returns at least `1` (the chase-write assignment).
    - `grep -c "requestAnimationFrame\|cancelAnimationFrame" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.ts` returns at least `2` (RAF scheduling + cleanup cancellation).
    - `grep -c "isTrusted" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.ts` returns at least `1` (user-input origin gate on scroll listener — the "input-origin bit" from § What would make it wrong bullet 4).
    - `grep -c "from \"\./auto-scroll-machine\"" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.ts` returns `1` (single import of the pure reducer).
    - `grep -c "reduce(" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.ts` returns at least `1` (reducer called in dispatch wrapper).
    - `grep -c "\[pv-scroll\]" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.ts` returns at least `3` (multiple log sites per event class — verifies the reshape from `[pv-scroll-diag]` to `[pv-scroll]` per phase-guidance).
    - `grep -c "chase-skip" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.ts` returns at least `1` (dispatch-wrapper emission site for the bottom-moving-event-while-not-at-bottom case per `<behavior>` dispatch-wrapper spec item iv).
    - `grep -c "\[pv-scroll-diag\]" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.ts` returns `0` (old log prefix retired).
    - `grep -c "didFirstContentScrollRef\|pinnedRef\|isPinnedToBottom\|scrollToBottomAndFollow" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.ts` returns `0` (old API surface + first-content bypass ref deleted).
    - `cd /home/ubuntu/skynet-tina && npx tsc --noEmit` reports zero errors specifically referencing `use-auto-scroll.ts` (test file may have errors until Task 2 lands).
  </acceptance_criteria>
  <done>use-auto-scroll.ts rewritten as a thin wrapper around `reduce()`; every anti-pattern grep gate returns zero; new API surface `{scrollRef, jumpToBottom, onSendFired, mode, revealed}` in place; compiles under tsc.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite use-auto-scroll.test.ts to lock T1-T15 hook-level SM semantics</name>
  <files>src/ui/features/pretty-view/use-auto-scroll.test.ts</files>
  <read_first>
    - /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.test.ts (CURRENT test file — PRESERVE the `makeScrollEl` JSDOM harness at L53-118 and the `fireScroll(el)` synthetic-event helper at L120-128 verbatim per 71-PATTERNS.md § "Preserve two load-bearing patterns"; DELETE Tests 1-8 which lock the old three-observer hook — those are gone. The Phase 43 preamble at L1-33 also gets deleted, replaced with a Phase 71 preamble.)
    - /home/ubuntu/skynet-tina/src/ui/features/pretty-view/useHoldToRecord.test.tsx (STRUCTURAL ANALOG for test-consumer-component pattern — copy the TestConsumer at L100-116 for a hook with event handlers wired to DOM; copy the beforeEach/afterEach fake-timers pattern at L122-132 for the mount-landing tests that need to walk RAF deterministically)
    - /home/ubuntu/skynet-tina/src/ui/features/pretty-view/usePaneResolvingMachine.test.tsx (STRUCTURAL ANALOG for the API-surface-lock Test 8-style at L188-206 — adopt verbatim for T14 asserting `Object.keys(result.current).sort()` equals `["jumpToBottom", "mode", "onSendFired", "revealed", "scrollRef"]` AND `sentinelRef`, `scrollToBottomAndFollow`, `isPinnedToBottom` are undefined)
    - /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.ts (the rewritten hook from Task 1 — this test imports from it)
    - /home/ubuntu/skynet-tina/.planning/phases/71-rewrite-prettyview-auto-scroll-from-first-principles-two-sta/71-PATTERNS.md (§ "use-auto-scroll.test.ts (REWRITE)" — full pattern spec including the T1-T15 coverage list at L387-404)
  </read_first>
  <behavior>
    - Preserve `makeScrollEl(opts)` verbatim from L53-118 of the current file (returns `{el, setScrollHeight, setScrollTop, getScrollTop, addEventListenerSpy, removeEventListenerSpy}`). This harness is the canonical JSDOM scroll-container mock in this codebase and remains load-bearing.
    - Preserve `fireScroll(el)` verbatim from L120-128 with one modification: dispatch via `el.dispatchEvent(new Event("scroll", {bubbles: true}))` and ADD a helper `firePrograSscroll(el)` that dispatches an event with `isTrusted` NOT set (JSDOM synthetic events default `isTrusted: false`, which is what we want for the "programmatic-write does not trigger mode transition" test).
    - Add a `TestConsumer` component that renders the hook's `scrollRef` on a `<div>`, exposes `mode` and `revealed` as `data-*` attributes, and provides buttons wired to `jumpToBottom` and `onSendFired` — following useHoldToRecord.test.tsx L100-116 shape.
    - Use `vi.useFakeTimers({ shouldAdvanceTime: false })` in `beforeEach` so RAF ticks can be walked deterministically via `vi.advanceTimersByTime()`. Restore real timers in `afterEach`.
    - Cover the T1-T15 test list from 71-PATTERNS.md § "Test coverage list":
      * T1: Cold mount → hide-pin-reveal → visible + at-bottom. Assert `data-revealed="true"` and `data-mode="at-bottom"` after RAF flush + ResizeObserver tick.
      * T2: User scrolls up via wheel → mode flips to `not-at-bottom`. Use `makeScrollEl` with initialScrollTop far from bottom, dispatch synthetic scroll with `isTrusted: true` (via `Object.defineProperty(event, 'isTrusted', {value: true})` before dispatch — JSDOM defaults to false).
      * T3: User scrolls back near bottom (distance < BOTTOM_TOLERANCE_PX) → mode flips to `at-bottom`.
      * T4: New message arrives while at-bottom → single scroll-write per RAF. Trigger via mutation (append child to el), advance RAF, assert `getScrollTop() === scrollHeight - clientHeight` (approximately) and the write happened exactly once.
      * T5: **LOAD-BEARING** — user scrolled-up + new message arrives → NO scroll-write, NO mode change. This is the no-yank invariant. Explicit `it.skip` is FORBIDDEN — this test must pass. Assert `getScrollTop()` unchanged after mutation + RAF flush.
      * T6: Accessory mount while at-bottom → chase. Append a fake `<div>` (represents WipBubble) to the scroll container, RAF flush, assert scrollTop moved to bottom.
      * T7: Accessory unmount while at-bottom → chase. Remove a child, RAF flush, assert scrollTop moved to bottom.
      * T8: Container resize while at-bottom → chase. Update `clientHeight` via `Object.defineProperty` and dispatch a ResizeObserver-equivalent event OR call the ResizeObserver callback directly via a spy. Since JSDOM lacks native ResizeObserver, stub it with a minimal shim that captures the callback and lets the test invoke it manually.
      * T9: Container resize while not-at-bottom → NO write, NO mode change.
      * T10: `jumpToBottom()` action → flips to at-bottom, writes scrollTop = scrollHeight. Fire via TestConsumer's button.
      * T11: `onSendFired()` from at-bottom → stays at-bottom + chase; from not-at-bottom → flips to at-bottom + chase.
      * T12: Programmatic write never triggers mode transition. Set `pendingChaseRef` state (via triggering a chase), dispatch a scroll event with `isTrusted: false`, assert mode stayed unchanged.
      * T13: Cleanup — unmount removes scroll listener, disconnects MutationObserver + ResizeObserver, cancels any pending RAF. Assert `removeEventListenerSpy` called for `scroll` type; assert observer `.disconnect()` was called (via observer stub).
      * T14: API-surface lock — `Object.keys(result.current).sort()` returns EXACTLY `["jumpToBottom", "mode", "onSendFired", "revealed", "scrollRef"]`. Assert `result.current.sentinelRef === undefined`, `result.current.scrollToBottomAndFollow === undefined`, `result.current.isPinnedToBottom === undefined` (verbatim shape from usePaneResolvingMachine.test.tsx L188-206).
      * T15: paneKey change (session re-entry via `rerender`) MUST NOT touch scroll position AND MUST NOT reset mode. Render with `paneKey="pane-A"`, scroll to a specific position, `rerender` with `paneKey="pane-B"`, assert `getScrollTop()` unchanged and `data-mode` unchanged.
  </behavior>
  <action>
Rewrite `src/ui/features/pretty-view/use-auto-scroll.test.ts` from scratch — do not incrementally edit. The current file locks the old three-observer model (per 71-PATTERNS.md § "Existing test-file preamble to REPLACE" at L317).

Structure the file:
  1) Header block-comment: (a) purpose — "Phase 71 rewrite: locks the new two-state auto-scroll state machine at the HOOK level. Reducer-level coverage lives in ./auto-scroll-machine.test.ts (pure function truth-table). This file covers the DOM/React glue: hide-pin-reveal, RAF-coalesced writes, observer wiring, and the `isTrusted` input-origin gate."; (b) Coverage strategy — mention `makeScrollEl` + `TestConsumer` + fake timers; (c) T5 as the load-bearing no-yank regression (preserve the comment from the current file at L29-31).
  2) Imports: `describe, it, expect, vi, beforeEach, afterEach` from vitest; `render, screen, act, fireEvent` from `@testing-library/react`; `renderHook` from `@testing-library/react`; `useLayoutEffect` from `react` (for TestConsumer's synchronous scrollRef binding — required to inject the pre-made `makeScrollEl` element before the hook's observer-setup useEffect runs); `useAutoScroll` from `./use-auto-scroll`.
  3) `makeScrollEl(opts)` helper — verbatim from current file L53-118.
  4) `fireScroll(el)` and `fireProgrammaticScroll(el)` helpers — modified from current L120-128 to distinguish trusted vs untrusted events via `Object.defineProperty(event, 'isTrusted', {value: true/false, configurable: true})` before dispatch.
  5) ResizeObserver stub: `class ResizeObserverStub { callback: ResizeObserverCallback; constructor(cb) { this.callback = cb; ResizeObserverStub.lastInstance = this; } observe() {} disconnect() {} unobserve() {} static lastInstance: ResizeObserverStub | null = null; }` — installed via `global.ResizeObserver = ResizeObserverStub` in beforeEach, restored in afterEach. Tests trigger a resize by calling `ResizeObserverStub.lastInstance?.callback([...], observer)`.
  6) MutationObserver stub: similar shape as ResizeObserver stub. JSDOM has real MutationObserver but it's async — the stub gives tests synchronous control.
  7) `TestConsumer` component (PINNED signature — accepts pre-made `el` from `makeScrollEl` so geometry is defined BEFORE the hook's observer-setup `useEffect(..., [scrollEl])` fires):

```
function TestConsumer({paneKey, el}: {paneKey: string; el: HTMLElement}) {
  const {scrollRef, mode, revealed, jumpToBottom, onSendFired} = useAutoScroll(paneKey);
  // useLayoutEffect binds the callback ref SYNCHRONOUSLY on mount, BEFORE
  // the hook's observer-setup useEffect runs (useEffect is scheduled after
  // useLayoutEffect). This guarantees scrollEl inside the hook is the
  // makeScrollEl-produced element (with its stubbed scrollHeight /
  // clientHeight / scrollTop getters + setters) at the moment the
  // observer-setup effect installs the ResizeObserver + MutationObserver +
  // scroll listener. Without this ordering, the observer-setup effect
  // would see scrollEl === null and skip installation, then the RO / MO
  // stubs would never receive the hook's callback.
  useLayoutEffect(() => { scrollRef(el); return () => scrollRef(null); }, [el]);
  return (<>
    <button data-testid="jump" onClick={jumpToBottom} />
    <button data-testid="send" onClick={onSendFired} />
    <div data-testid="probe" data-mode={mode} data-revealed={revealed ? "true" : "false"} />
  </>);
}
```

Call sites: `const {el} = makeScrollEl({initialScrollTop, scrollHeight, clientHeight}); render(<TestConsumer paneKey="pane-A" el={el} />);`. The rendered `<div data-testid="probe">` is what tests read for mode/revealed assertions; `el` is what the hook actually observes for scroll geometry. Do NOT render the hook's `scrollRef` on a DOM `<div>` inside TestConsumer's JSX — that would create TWO scroll targets (the real DOM div and the pre-made el) racing for the ref, defeating the geometry-injection contract. The pre-made `el` is the sole scroll target; the rendered `<div data-testid="probe">` is a passive read surface.
  8) `beforeEach` + `afterEach`: install stubs, install fake timers, cleanup.
  9) Test bodies T1-T15 as described above. Group into `describe` blocks by feature (hide-pin-reveal, scroll-listener, observer-driven-chase, actions, API-surface, cleanup).

For T4-T9 (observer-driven tests), the ResizeObserverStub / MutationObserverStub pattern lets the test synchronously invoke the observer callback that the hook registered — this matches useHoldToRecord.test.tsx's pattern of driving events through fireEvent for pointer-callback-driven behavior.

For T5, T9, T12 (the LOAD-BEARING no-yank / no-mode-transition tests), each `expect` must include a comment naming the § What would make it wrong bullet number it defends (e.g. "// § What would make it wrong bullet 2: programmatic scroll write must NOT transition mode").

Every `it` name must include which T-number it maps to for cross-reference: `it("T1: cold mount → hide-pin-reveal completes → mode=at-bottom + revealed=true", ...)`. This makes it trivial to verify all T1-T15 are present via `grep -c 'it("T'`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; npx vitest run --reporter=basic src/ui/features/pretty-view/use-auto-scroll.test.ts 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/ui/features/pretty-view/use-auto-scroll.test.ts` (rewritten).
    - `cd /home/ubuntu/skynet-tina && npx vitest run --reporter=basic src/ui/features/pretty-view/use-auto-scroll.test.ts` exits code 0 with all tests passing.
    - `grep -Ec 'it\("T[0-9]+' /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.test.ts` returns at least `15` (T1-T15 all present).
    - `grep -c "makeScrollEl" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.test.ts` returns at least `2` (helper declared + used).
    - `grep -c "isTrusted" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.test.ts` returns at least `2` (trusted + untrusted helpers).
    - `grep -c "ResizeObserverStub\|MutationObserverStub" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.test.ts` returns at least `4` (stub classes declared + referenced in tests).
    - `grep -c 'sentinelRef' /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.test.ts` returns at least `1` (asserted-undefined in T14 API-surface lock; no OTHER references to the deleted sentinel API).
    - `grep -c 'scrollToBottomAndFollow\|isPinnedToBottom' /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.test.ts` returns at least `2` (asserted-undefined in T14; must NOT appear elsewhere as an API expectation).
    - `grep -c 'it\.skip\|xit\|describe\.skip' /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.test.ts` returns `0` (T5 and every load-bearing test must actually run — no skipping allowed).
    - Test count reported by vitest is ≥ 15 (T1-T15).
    - `cd /home/ubuntu/skynet-tina && npx tsc --noEmit` reports zero errors specifically referencing `use-auto-scroll.test.ts`.
  </acceptance_criteria>
  <done>Test file rewritten covering T1-T15 verbatim per PATTERNS.md; all tests pass under vitest; TestConsumer pattern in place; ResizeObserver + MutationObserver stubs give tests synchronous control; no skipped tests.</done>
</task>

</tasks>

<verification>
Run the pretty-view scoped suite to confirm neither this plan's rewrites nor the pure reducer from Plan 70-01 broke anything else. PrettyView.tsx integration edits haven't landed yet (Plan 70-03), so PrettyView-consuming tests may report the old API-surface deletion as a failure — that is EXPECTED and lands in Plan 70-03. This plan's scope is bounded to the hook file + its own test file plus the pure reducer file.

```bash
cd /home/ubuntu/skynet-tina && npx vitest run --reporter=basic src/ui/features/pretty-view/auto-scroll-machine.test.ts src/ui/features/pretty-view/use-auto-scroll.test.ts 2>&1 | tail -20
```

Both must be green.
</verification>

<success_criteria>
- `src/ui/features/pretty-view/use-auto-scroll.ts` rewritten as a thin wrapper around `reduce()`; API surface is exactly `{scrollRef, jumpToBottom, onSendFired, mode, revealed}`.
- `src/ui/features/pretty-view/use-auto-scroll.test.ts` rewritten with T1-T15 all present and passing.
- Both files compile under `tsc --noEmit`.
- All anti-pattern grep gates in both tasks return zero (no IntersectionObserver, no sentinelRef, no smooth-scroll, no old log prefix, no old ref names).
- The pure reducer test file from Plan 70-01 remains green (regression check).
</success_criteria>

<output>
Create `.planning/phases/71-rewrite-prettyview-auto-scroll-from-first-principles-two-sta/71-02-SUMMARY.md` when done — record the exact TestConsumer signature and the ResizeObserverStub / MutationObserverStub API so Plan 70-03's PrettyView.tsx edits know the hook is behaviorally locked and integration edits can proceed with confidence.
</output>
