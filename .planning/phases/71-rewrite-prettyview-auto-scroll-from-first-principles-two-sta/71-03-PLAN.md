---
phase: 70-rewrite-prettyview-auto-scroll-from-first-principles-two-sta
plan: 03
type: execute
wave: 3
depends_on:
  - 70-02
files_modified:
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/shell/CollapsedPanelCloseLane.tsx
  - src/ui/features/pretty-view/ComposeBox.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "PrettyView.tsx destructures the NEW hook API: `{ scrollRef, jumpToBottom, onSendFired, mode, revealed }` from `useAutoScroll(paneKey)` — no `sentinelRef`, no `scrollToBottomAndFollow`, no `isPinnedToBottom`"
    - "The scroll container div carries the CSS class `overflow-anchor:none` — browser scroll-anchoring is EXPLICITLY DISABLED (reversing the Phase 43 decision at L3108-3111)"
    - "The `<div data-pv-scroll-sentinel />` bottom sentinel (L3329-3334) is DELETED — no descendant with `data-pv-scroll-sentinel` remains in PrettyView.tsx"
    - "Hide-pin-reveal wrapper: the scroll container (or its immediate children content wrapper) carries `visibility: hidden` while `revealed === false`, flips to `visibility: visible` when `revealed === true`"
    - "Jump-to-bottom pill visibility gate is `mode === 'not-at-bottom' && messages.length > 0` — replaces `!isPinnedToBottom && messages.length > 0`"
    - "handleComposeSend calls `onSendFired()` at its previous scrollToBottomAndFollow call site (L1081)"
    - "The `onGoodToGo` prop passed to LoadMoreOlderButton / compose surface at L3482 receives `jumpToBottom` (renamed from `scrollToBottomAndFollow`)"
    - "The L155-167 RENDER-03 auto-scroll comment block is rewritten to describe the two-state machine (no more 'three engines' framing)"
    - "PrettyView.tsx contains no residual [pv-scroll-diag] log-prefix references (the L406 runtime log + L396/L402 supporting comments renamed to [pv-scroll])"
    - "Two orphan API-reference comments outside pretty-view are updated: CollapsedPanelCloseLane.tsx L112 sentinelRef reference deleted, ComposeBox.tsx L2493 scrollToBottomAndFollow reference updated to jumpToBottom"
  artifacts:
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "Consumer wiring for the new useAutoScroll API, hide-pin-reveal DOM structure, disabled browser scroll-anchoring, sentinel-div deletion, [pv-scroll-diag] log-prefix rename at L406"
      contains: "useAutoScroll(paneKey)"
    - path: "src/ui/shell/CollapsedPanelCloseLane.tsx"
      provides: "Orphan sentinelRef reference in L112 comment rewritten (the referenced analog was deleted by Phase 71)"
      contains: "callback-ref"
    - path: "src/ui/features/pretty-view/ComposeBox.tsx"
      provides: "Orphan scrollToBottomAndFollow reference in L2493 comment updated to jumpToBottom (Phase 71 rename)"
      contains: "jumpToBottom"
  key_links:
    - from: "src/ui/features/pretty-view/PrettyView.tsx"
      to: "src/ui/features/pretty-view/use-auto-scroll.ts"
      via: "useAutoScroll(paneKey) with new destructure {scrollRef, jumpToBottom, onSendFired, mode, revealed}"
      pattern: "useAutoScroll\\(paneKey\\)"
    - from: "PrettyView.tsx scroll container className"
      to: "browser scroll-anchoring OFF"
      via: "Tailwind arbitrary property [overflow-anchor:none]"
      pattern: "\\[overflow-anchor:none\\]"
    - from: "PrettyView.tsx jump-to-bottom pill"
      to: "hook mode state"
      via: "mode === 'not-at-bottom' visibility gate + onClick={jumpToBottom}"
      pattern: "mode === \"not-at-bottom\""
    - from: "PrettyView.tsx handleComposeSend"
      to: "hook send-fired action"
      via: "onSendFired() call at the former scrollToBottomAndFollow site"
      pattern: "onSendFired\\(\\)"
---

<objective>
Wire the new `useAutoScroll(paneKey)` API from Plan 70-02 into `PrettyView.tsx`. This is the integration plan — every edit site in `PrettyView.tsx` that references the OLD hook API (`sentinelRef`, `scrollToBottomAndFollow`, `isPinnedToBottom`) is either renamed to the new API or deleted. Additionally, apply the LOCKED shape-file requirements to the DOM: enable `overflow-anchor:none` on the scroll container (reversing the Phase 43 decision), add the hide-pin-reveal `visibility` wrapper, delete the `<div data-pv-scroll-sentinel />` element.

Purpose: The consumer surface is where the new state machine meets the actual browser DOM. Every shape-file invariant that involves the DOM (browser scroll-anchoring OFF, hide-pin-reveal on first paint, jump-to-bottom pill bound to `mode`) lives in this file. Getting the destructure right + the visibility wrapper right + the CSS class right is what closes the gap between the pure reducer + thin wrapper (Plans 01+02) and shipped behavior.

Output: One file modified (`PrettyView.tsx`). Zero new files. Zero test rewrites in this plan — the existing PrettyView-consuming tests inherit the behavior contract from Plan 70-02's use-auto-scroll.test.ts, and any PrettyView-integration test regressions get triaged in Plan 70-04's verification pass.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/ubuntu/skynet-tina/.planning/phases/71-rewrite-prettyview-auto-scroll-from-first-principles-two-sta/71-CONTEXT.md
@/home/ubuntu/skynet-tina/.planning/phases/71-rewrite-prettyview-auto-scroll-from-first-principles-two-sta/71-PATTERNS.md
@/home/ubuntu/skynet-tina/.planning/phases/71-rewrite-prettyview-auto-scroll-from-first-principles-two-sta/71-02-SUMMARY.md
@/home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.ts
@/home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update hook destructure + handleComposeSend + LoadMore prop wiring at L1050-1083 + L3482</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx</files>
  <read_first>
    - /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx (READ ONCE, targeted line ranges — L1050-1083 for the hook call + handleComposeSend, L3470-3500 for the onGoodToGo prop site; extract the exact identifier names and callback shapes in a single pass so no re-read is needed for either edit site in Task 1 or Task 2)
    - /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.ts (the rewritten hook — confirm the EXACT return-shape key names and callback signatures before editing the destructure)
    - /home/ubuntu/skynet-tina/.planning/phases/71-rewrite-prettyview-auto-scroll-from-first-principles-two-sta/71-PATTERNS.md (§ "PrettyView.tsx (EDITS)" edit-sites 1-2 and 7 at L410-425 — the L155-167 comment block, the L1056-1059 hook call site, the L3482 onGoodToGo prop)
  </read_first>
  <behavior>
    - L1059 destructure updated from `const { scrollRef, sentinelRef, scrollToBottomAndFollow, isPinnedToBottom } = useAutoScroll(paneKey, messages.length);` to `const { scrollRef, jumpToBottom, onSendFired, mode, revealed } = useAutoScroll(paneKey);`.
    - `messages.length` argument REMOVED (the hook no longer needs it — MutationObserver on the scroll container's children is the direct source of content-changed events per Plan 70-02 § behavior).
    - L1081 body of `handleComposeSend` updated from `scrollToBottomAndFollow();` to `onSendFired();`. The comment at L1079-1080 ("A send is the strongest possible 'I want to see the reply' signal — force stick + jump regardless of prior scroll position.") stays — it accurately describes `onSendFired()`'s semantic (§ Transitions LOCKED "send flips to at-bottom regardless of prior state").
    - L1083 dependency array of the `useCallback` updated from `[onSend, scrollToBottomAndFollow]` to `[onSend, onSendFired]`.
    - L3482 `onGoodToGo={scrollToBottomAndFollow}` updated to `onGoodToGo={jumpToBottom}` (the LoadMore surface's "continue when settled" callback maps semantically to a jump-to-bottom action).
    - Additional comment updates:
      * L1051-1055 comment describing paneKey's original site: update the parenthetical "(paneKey is now USED)" reference to point to the new hook's paneKey-for-logging-only role — this comment currently claims paneKey drives an effect that resets pinned=true, which is no longer true.
      * L1457 comment "composeScrollRefs collapsed: the outer scroll container now binds directly to useAutoScroll's scrollRef (43-06's frozen API surface)" — update "43-06's frozen API surface" to "phase-71 rewrite's stable API surface".
      * L1583 comment "paneKey is computed in the render body (line 465) and passed into useAutoScroll" — L465 is stale (paneKey moved to L1056 in Phase 32 per the L1051-1055 comment); update the line reference to L1056. Also add a note: "paneKey is now parameter-carried FOR LOGGING ONLY — the hook does not react to paneKey changes per shape-file § Session re-entry."
      * L2143 comment "useAutoScroll pins the viewport to it" — accurate but update phrasing to reflect the new machine: "useAutoScroll's MutationObserver dispatches {kind:'content-changed'} on this mount; the reducer's at-bottom → chase branch anchors the viewport."
  </behavior>
  <action>
Apply the destructure rename + argument-removal + comment updates in a single Edit pass. The hook is invoked at exactly one site (L1059 confirmed by `grep -n "useAutoScroll(" PrettyView.tsx` in the pre-flight); handleComposeSend is at exactly one site (L1065-1083 confirmed by `grep -n "handleComposeSend" PrettyView.tsx`).

For the L1059 destructure change, use Edit with the exact old string `const { scrollRef, sentinelRef, scrollToBottomAndFollow, isPinnedToBottom } = useAutoScroll(paneKey, messages.length);` — this is unique in the file (confirmed by grep).

For the L1081 body change, use Edit with the exact old string `    scrollToBottomAndFollow();` inside handleComposeSend's function body — verify uniqueness before editing (there is only one scrollToBottomAndFollow call outside comments in the file per the pre-flight grep at line 1081).

For the L1083 dependency array change, use Edit with the exact old string `  }, [onSend, scrollToBottomAndFollow]);` — this specific dependency-array shape occurs once in the file.

For the L3482 prop change, use Edit with the exact old string `          onGoodToGo={scrollToBottomAndFollow}` — this exact prop assignment occurs once in the file.

For each comment update: read the specific line range once via Read with offset+limit, then apply a targeted Edit with the exact old comment string as the target. Do NOT re-Read the file after edits — Edit reports success/failure directly.

For the L1051-1055 comment about paneKey's original site, do a Read at offset=1050 limit=10 first (single pass), extract the exact text, then Edit. Same pattern for L1457, L1583, L2143.

Do NOT touch anything else in this task — the scroll container className update (L3112), the jump-to-bottom pill update (L3280-3286), the sentinel div deletion (L3319-3334), the RENDER-03 comment block (L155-167) all land in Task 2.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -E "PrettyView\.tsx" | grep -v "\.test\." | wc -l | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "scrollToBottomAndFollow" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx` returns `0` after Task 1 (Task 2 will delete the last remaining reference inside a comment near the jump-to-bottom pill; Task 1 completes all non-comment code-level replacements).
      - Note: Task 1 may leave the L3274/L3277 comment text still mentioning `scrollToBottomAndFollow` — that lands in Task 2. This gate runs AFTER Task 2. Task 1's own intermediate acceptance is: `grep -Ec "scrollToBottomAndFollow\(\)|onClick=\{scrollToBottomAndFollow\}|onGoodToGo=\{scrollToBottomAndFollow\}|\[onSend, scrollToBottomAndFollow\]" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx` returns `0` (all executable references to the old identifier removed).
    - `grep -c "useAutoScroll(paneKey)" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx` returns `1` (hook called with single argument — messages.length removed).
    - `grep -c "useAutoScroll(paneKey, messages\.length)" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx` returns `0` (old two-arg call site removed).
    - `grep -c "{ scrollRef, jumpToBottom, onSendFired, mode, revealed }" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx` returns `1` (new destructure in place, exact ordering per hook return type — matches Plan 70-02 § behavior).
    - `grep -c "onSendFired()" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx` returns at least `1` (handleComposeSend body updated).
    - `grep -c "onGoodToGo={jumpToBottom}" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx` returns `1` (LoadMore prop renamed).
    - `grep -c "\[onSend, onSendFired\]" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx` returns `1` (dependency array updated).
    - `cd /home/ubuntu/skynet-tina && npx tsc --noEmit` reports zero errors specifically referencing PrettyView.tsx related to the auto-scroll hook types (there may be pre-existing TS errors in this large file unrelated to this task; those are out of scope — only auto-scroll-hook-typed errors block this task).
  </acceptance_criteria>
  <done>Hook destructure updated to new 5-key API; messages.length argument removed; handleComposeSend calls onSendFired; LoadMore's onGoodToGo prop renamed; four supporting comment sites updated; tsc reports no auto-scroll-typed errors in PrettyView.tsx.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete sentinel div + update scroll container className + hide-pin-reveal wrapper + jump-to-bottom pill wiring + rewrite RENDER-03 comment block</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx</files>
  <read_first>
    - /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx (READ ONCE, targeted line ranges — L155-167 for the RENDER-03 comment block, L3095-3140 for the scroll container div + its opening comments, L3225-3320 for the accessory-mount comment block + jump-to-bottom pill, L3319-3335 for the sentinel div; extract everything in a single pass — no re-reads)
    - BEFORE editing, run `grep -n "isPinnedToBottom" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx` to enumerate every occurrence and record them here so no comment reference is missed. As of planning (post-Task 1 of Plan 70-03), the CURRENT occurrences are at: L3278 (the `isPinnedToBottom` word inside the jump-to-bottom pill's block comment describing the OLD API — "`isPinnedToBottom` is hook-derived from the single scroll listener") and L3280 (the `!isPinnedToBottom` expression in the JSX visibility gate). Task 1 does NOT touch this identifier — Task 2 handles both sites. If a fresh grep reports any occurrences NOT in this list, treat them as additional sites requiring the same disposition (rewrite comment to describe `mode`, delete gate expression) before finalizing Task 2.
    - /home/ubuntu/skynet-tina/.planning/phases/71-rewrite-prettyview-auto-scroll-from-first-principles-two-sta/71-PATTERNS.md (§ "PrettyView.tsx (EDITS)" edit-sites 1, 3, 4, 5, 6, 8 at L410-427)
    - /home/ubuntu/skynet-tina/.planning/shapes/shape-pv-autoscroll-rewrite.md (§ Shape "Mount landing" + "Explicit ownership of scroll position" + "Jump-to-bottom button" — LOCKED requirements that drive these edits)
  </read_first>
  <behavior>
    - L155-167 RENDER-03 comment block rewritten to describe the two-state model. Structure the new comment as:
      "2. RENDER-03 auto-scroll: two-state position-derived state machine via `useAutoScroll(paneKey)`. Modes: `at-bottom` (chase-on-content-change) and `not-at-bottom` (frozen). Transitions: OUT-of-at-bottom ONLY on user-input events (wheel/touch/scrollbar/keyboard) outside a ~28px tolerance; INTO-at-bottom on jump-clicked, send-fired, or user-input inside tolerance. Programmatic writes NEVER transition mode. Symmetric event handling: new messages, WIP indicator appear/disappear, accessory bubbles (WipBubble / WaitingBubble / PlanPendingBubble / AsideBubble) appear/disappear, window resize, pane-count / split-layout change — all one event class ('content-changed' or 'container-resized') consumed uniformly by the reducer. Chase writes are instant (no smooth-scroll), coalesced one-per-RAF. Browser scroll-anchoring (`overflow-anchor:none`) disabled on the container — the hook owns scroll position exclusively. Mount-landing via hide-pin-reveal: the content wrapper starts `visibility:hidden`, the hook waits for the first non-zero content-height measurement, jumps to bottom, then flips `revealed`. See use-auto-scroll.ts + auto-scroll-machine.ts headers and .planning/shapes/shape-pv-autoscroll-rewrite.md for LOCKED shape."
    - L3095-3113 scroll container div className updated:
      * Current: `className="flex-1 min-h-0 overflow-y-auto overflow-x-hidden overscroll-contain px-4 py-3"`
      * New: `className="flex-1 min-h-0 overflow-y-auto overflow-x-hidden overscroll-contain [overflow-anchor:none] px-4 py-3"`
      * Add `[overflow-anchor:none]` (Tailwind arbitrary property) — reverses the Phase 43 decision at L3108-3111. Per shape-file § Explicit ownership "The state machine takes exclusive ownership of scroll position. The browser's built-in feature that tries to preserve visual position across content mutations is disabled on the scroll container, so the browser and the state machine don't fight each other."
    - L3108-3111 comment updated:
      * Current: "Phase 43 (plan 43-07a): [overflow-anchor:none] REMOVED — browser default overflow-anchor:auto is load-bearing for prepend/growth preservation. With TanStack Virtual gone, the browser's native scroll-anchoring is the sole scroll-position authority through measurement changes."
      * New: "Phase 71 (rewrite): [overflow-anchor:none] RESTORED — the state machine in useAutoScroll owns scroll position exclusively (shape-file § Explicit ownership LOCKED). Browser scroll-anchoring fighting explicit `scrollTop = scrollHeight` writes was one of the six pitfalls named in shape-file § What would make it wrong. Load-more anchor preservation (the reason Phase 43 removed this) is deferred to the follow-on `load-more-scroll-and-order-corruption` bounty per Phase 71 § Scope edges Out-of-scope."
    - Hide-pin-reveal wrapper: wrap the messages content (everything inside the scroll container div — the `LoadMoreOlderButton`, the `messages.map(...)` output, all accessory bubbles, the jump-to-bottom pill, but NOT the scroll container itself since `visibility: hidden` on the scroll container hides its scrollbar too) in a single new `<div>` that carries `style={{ visibility: revealed ? "visible" : "hidden" }}`. Placement: immediately after the scroll container's opening `<div ref={scrollRef} ...>` and immediately before `<LoadMoreOlderButton .../>`, with the closing `</div>` immediately before the sentinel-div-deletion site (see next bullet) — i.e. wrap the entire scroll-container content list.
    - L3280-3286 jump-to-bottom pill visibility gate updated:
      * Current: `{!isPinnedToBottom && messages.length > 0 && (` and `onClick={scrollToBottomAndFollow}`.
      * New: `{mode === "not-at-bottom" && messages.length > 0 && (` and `onClick={jumpToBottom}`.
    - L3270-3279 jump-to-bottom pill comment block updated:
      * Update `scrollToBottomAndFollow` references to `jumpToBottom`; update `MutationObserver + ResizeObserver in useAutoScroll` still accurate (both observers still in place per Plan 70-02); update `isPinnedToBottom` reference to `mode`.
    - L3259-3268 accessory-mount region comment updated to reflect the new model:
      * Update the "useAutoScroll's MutationObserver + per-child ResizeObserver catches this accessory's mount" to "useAutoScroll's MutationObserver on the scroll container's children dispatches a single `{kind:'content-changed'}` event to the state machine — symmetric with every other content-mount signal per shape § Symmetric event handling. No per-accessory-type wiring."
    - L3319-3334 sentinel div DELETED entirely (both the block comment `inline-260830-pv-scroll-sentinel-inside-scroller` explaining its purpose AND the `<div ref={sentinelRef} data-pv-scroll-sentinel aria-hidden="true" style={...} />` element). This is the "sentinel div DELETE ... delete the `sentinelRef` destructure" edit-site 6 from PATTERNS.md § "PrettyView.tsx (EDITS)".
    - The hide-pin-reveal wrapper's closing `</div>` sits immediately before the scroll container div's closing `</div>` (which was previously the position of the deleted sentinel div).
  </behavior>
  <action>
Apply all six edits in this task. Order: (1) delete the sentinel div FIRST so subsequent edits don't need to accommodate its presence, (2) update the scroll container className + preceding comment, (3) add the hide-pin-reveal wrapper `<div>` opening after the scroll container's opening tag, (4) close the hide-pin-reveal wrapper with `</div>` before the scroll container's closing tag, (5) update the jump-to-bottom pill visibility gate + onClick + surrounding comments, (6) update the accessory-mount comment block, (7) rewrite the RENDER-03 comment block at L155-167.

Each edit uses the Edit tool with an exact-string match against the CURRENT file contents. Every target string is verified unique in the pre-flight Read (Task 1 confirmed the file has one hook call site, one handleComposeSend, one onGoodToGo prop, one sentinel div — the edit sites in this task are similarly unique).

Sentinel div deletion: use Edit with old-string spanning L3319 through L3334 including the block comment. The exact old_string is:
  Read L3319-3335 in the pre-flight, use the exact captured text as the Edit old_string, and replace with the CLOSING `</div>` of the hide-pin-reveal wrapper (added in step 4). If done as a combined delete-and-close, this is one atomic Edit; alternatively delete the sentinel with an Edit that replaces with empty string, then add the hide-pin-reveal closing tag via a separate Edit against a nearby anchor.

Recommended sequence to minimize edit-string ambiguity:
  1) Delete sentinel div (Edit: sentinel comment + div → empty string).
  2) Wrap accessory + messages region with hide-pin-reveal `<div style={{visibility: revealed ? "visible" : "hidden"}}>` opening tag (Edit: inject immediately before `<LoadMoreOlderButton`).
  3) Add closing `</div>` for hide-pin-reveal (Edit: inject immediately before the scroll container div's closing `</div>` — which is now where the sentinel used to be).
  4) Update scroll container className (Edit against the current className string).
  5) Update the L3108-3111 pre-className comment (Edit against the current Phase 43 comment).
  6) Update jump-to-bottom pill: (a) visibility gate `!isPinnedToBottom` → `mode === "not-at-bottom"`, (b) `onClick={scrollToBottomAndFollow}` → `onClick={jumpToBottom}`, (c) the L3270-3278 comment block references.
  7) Update L3259-3268 accessory-mount comment.
  8) Rewrite the L155-167 RENDER-03 comment block.

For every comment rewrite, preserve the surrounding numbered-invariant style (numbered list `1. ... 2. ... 3. ...`) already used in the RENDER-01 / RENDER-03 / FALLBACK-01 block. This matches the "Follow the same numbered-invariant style already used in the surrounding RENDER-01 / RENDER-03 / FALLBACK-01 comment block" guidance from PATTERNS.md § "PrettyView.tsx (EDITS)" edit-site 1.

Do NOT touch L3259 accessory bubble mount SITES themselves — WipBubble, WaitingBubble, PlanPendingBubble, AsideBubble mount as siblings of the messages.map output remains structurally unchanged. The reducer's MutationObserver on scroll-container childList picks up their mount/unmount uniformly with everything else per Plan 70-02.

Do NOT smooth-scroll anywhere. Do NOT add any additional gate on the jump-to-bottom pill beyond `mode === "not-at-bottom" && messages.length > 0` (no unread-count badge per § Scope edges Out-of-scope + § Vehicle notes "Plain pill, no unread count or badge").
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -E "PrettyView\.tsx" | grep -v "\.test\." | wc -l | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "data-pv-scroll-sentinel" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx` returns `0` (sentinel div DELETED — the PATTERNS.md § "Structural-grep acceptance gates" L477 gate).
    - `grep -c "sentinelRef" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx` returns `0` (no residual references to the deleted API).
    - `grep -c "\[overflow-anchor:none\]" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx` returns at least `1` (the Tailwind arbitrary-property class on the scroll container — proves the browser scroll-anchoring OFF invariant from shape § Explicit ownership).
    - `grep -c 'mode === "not-at-bottom"' /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx` returns at least `1` (jump-to-bottom pill visibility gate uses the new API).
    - `grep -c "!isPinnedToBottom" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx` returns `0` (old visibility gate removed).
    - `grep -c "isPinnedToBottom" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx` returns `0` — comments referencing the deleted identifier must be rewritten to describe the new identifier (`mode`) or deleted entirely; no migration-note comments retaining the old name (e.g. "// isPinnedToBottom → mode" is PROHIBITED; comments describe current state only, no historical breadcrumbs to the deleted API).
    - `grep -c "onClick={jumpToBottom}" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx` returns at least `1` (pill wired to jumpToBottom).
    - `grep -c "scrollToBottomAndFollow" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx` returns `0` (all references — code AND comments — removed; combines Task 1 and Task 2 gates).
    - `grep -c "visibility: revealed" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx` returns at least `1` (hide-pin-reveal wrapper in place).
    - `grep -c "Three engines\|three engines" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx` returns `0` (stale L155-167 comment block rewritten — no reference to the OLD three-engine framing).
    - `grep -c "two-state\|state machine\|reducer" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx` returns at least `1` (new comment references the new architecture).
    - `cd /home/ubuntu/skynet-tina && npx tsc --noEmit` reports zero errors specifically referencing PrettyView.tsx related to auto-scroll types.
    - `cd /home/ubuntu/skynet-tina && npx vitest run --reporter=basic src/ui/features/pretty-view/PrettyView --related /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx 2>&1 | tail -20` — scoped PrettyView tests report either all-passing OR only pre-existing unrelated failures (any auto-scroll-related test regressions must be triaged before this task is done).
  </acceptance_criteria>
  <done>Sentinel div deleted; overflow-anchor:none in place; hide-pin-reveal wrapper in place; jump-to-bottom pill uses new API; RENDER-03 comment block rewritten; all structural-grep gates pass; tsc clean for PrettyView.tsx auto-scroll types; scoped PrettyView tests green modulo pre-existing unrelated failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rename PrettyView.tsx L406 [pv-scroll-diag] log prefix + update two orphan API-reference comments outside the pretty-view directory</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx, src/ui/shell/CollapsedPanelCloseLane.tsx, src/ui/features/pretty-view/ComposeBox.tsx</files>
  <read_first>
    - /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx L390-410 (READ ONCE — the cap-drop diagnostic log block; the runtime `console.info` at L406 uses the old `[pv-scroll-diag]` prefix and the two preceding comment lines L396 + L402 also reference `pv-scroll-diag`)
    - /home/ubuntu/skynet-tina/src/ui/shell/CollapsedPanelCloseLane.tsx L105-125 (READ ONCE — the orphan comment at L112 references `sentinelRef pattern in use-auto-scroll.ts`; sentinelRef is DELETED by Plan 70-02)
    - /home/ubuntu/skynet-tina/src/ui/features/pretty-view/ComposeBox.tsx L2485-2500 (READ ONCE — the orphan comment at L2493 references `scrollToBottomAndFollow`; the new API name in Plan 70-02 is `jumpToBottom`; the parent-bound `onGoodToGo` prop is now wired to `jumpToBottom` per Plan 70-03 Task 1)
    - /home/ubuntu/skynet-tina/.planning/phases/71-rewrite-prettyview-auto-scroll-from-first-principles-two-sta/71-CONTEXT.md § "Files NOT touched by Phase 71" — the `PrettyConversationsPanel.tsx` panel is out-of-scope and MUST NOT be touched by this task (a fresh grep of `pv-scroll-diag` across the repo confirmed as of planning that PrettyConversationsPanel.tsx does not contain that token; if a future edit adds one there, that is a separate scope decision NOT covered by this task)
  </read_first>
  <behavior>
    - PrettyView.tsx L406 `console.info` call's format string updated: `[pv-scroll-diag] cap-drop ...` → `[pv-scroll] cap-drop ...`. This is the ONLY runtime log statement that still uses the old prefix in PrettyView.tsx (confirmed by grep pre-flight); Plan 04's sweep grep depends on this rename to succeed.
    - PrettyView.tsx L396 comment `// pv-scroll-diag (2026-08-23): cap-drop is the primary suspect for` → `// pv-scroll (2026-08-23): cap-drop is the primary suspect for` (keep the dated-diagnostic history reference — it's operator lore worth preserving, just under the new prefix).
    - PrettyView.tsx L402 comment `// event happened and lets us correlate with the pv-scroll-diag` → `// event happened and lets us correlate with the pv-scroll` (same rationale).
    - CollapsedPanelCloseLane.tsx L112 comment `// mounts, matching the sentinelRef pattern in use-auto-scroll.ts.` → `// mounts. (The pattern reference this comment used to cite — sentinelRef in use-auto-scroll.ts — was deleted by the Phase 71 auto-scroll rewrite; the callback-ref → useState pattern remains correct for the reason described above regardless of the deleted analog.)` — rewrites the analog reference into a self-contained rationale since the analog no longer exists.
    - ComposeBox.tsx L2493 comment `// scrollToBottomAndFollow) alongside handleQuickSend, matching the 'thumbs up'` → `// jumpToBottom via the parent-bound onGoodToGo prop per Phase 71) alongside handleQuickSend, matching the 'thumbs up'` — updates the identifier reference to match the Phase 71 API rename (`scrollToBottomAndFollow` → `jumpToBottom`). Preserve the surrounding L2491-2495 block-comment intent (the /explain send-path parity with the thumbs-up quick-button).
  </behavior>
  <action>
Apply five Edit-tool substitutions in this task. Each targets a unique string (pre-flight grep confirms each is unique in its file).

For PrettyView.tsx:
  1) Edit against exact old-string `      \`[pv-scroll-diag] cap-drop prevLen=${prev.length} cap=${cap} droppedEid=${dropped.eventId.slice(0, 8)} newAddedEid=${next.eventId.slice(0, 8)}\`,` → new-string with `[pv-scroll]` prefix. (Note: the `${...}` template segments are LITERAL in the file — copy the source exactly, do NOT interpolate anything at plan-read time; the Edit target is the raw source text.)
  2) Edit against exact old-string `    // pv-scroll-diag (2026-08-23): cap-drop is the primary suspect for` → `    // pv-scroll (2026-08-23): cap-drop is the primary suspect for`.
  3) Edit against exact old-string `    // event happened and lets us correlate with the pv-scroll-diag` → `    // event happened and lets us correlate with the pv-scroll`.

For CollapsedPanelCloseLane.tsx:
  4) Edit against exact old-string `  // mounts, matching the sentinelRef pattern in use-auto-scroll.ts.` → the new rationale-based comment described in `<behavior>` above. Preserve the exact leading indentation (two spaces) so the surrounding block-comment alignment stays clean.

For ComposeBox.tsx:
  5) Edit against exact old-string `            // scrollToBottomAndFollow) alongside handleQuickSend, matching the 'thumbs up'` → `            // jumpToBottom via the parent-bound onGoodToGo prop per Phase 71) alongside handleQuickSend, matching the 'thumbs up'`. Preserve the 12-space leading indentation matching the surrounding JSX-comment column.

Do NOT touch any other line in these three files. Do NOT rename any log prefix in `PrettyConversationsPanel.tsx` (out-of-scope per CONTEXT.md § "Files NOT touched by Phase 71"). Do NOT search for and rewrite additional `scrollToBottomAndFollow` or `sentinelRef` references outside these two specific comments — Task 1 and Task 2 of this plan handle every in-scope reference in PrettyView.tsx.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; grep -c "\[pv-scroll-diag\]" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx | grep -q "^0$" &amp;&amp; grep -c "sentinelRef pattern" /home/ubuntu/skynet-tina/src/ui/shell/CollapsedPanelCloseLane.tsx | grep -q "^0$" &amp;&amp; grep -c "scrollToBottomAndFollow" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/ComposeBox.tsx | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\[pv-scroll-diag\]" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx` returns `0` — the runtime log line at L406 renamed AND the two supporting comments at L396 + L402 renamed. Plan 04's log-prefix sweep depends on this.
    - `grep -c "\[pv-scroll\]" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx` returns at least `1` (the renamed cap-drop log site) — the new prefix present after rename.
    - `grep -c "sentinelRef" /home/ubuntu/skynet-tina/src/ui/shell/CollapsedPanelCloseLane.tsx` returns `0` — the orphan comment reference removed.
    - `grep -c "scrollToBottomAndFollow" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/ComposeBox.tsx` returns `0` — the orphan comment reference updated to `jumpToBottom`.
    - `grep -c "jumpToBottom" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/ComposeBox.tsx` returns at least `1` — the replacement identifier present.
    - `cd /home/ubuntu/skynet-tina && npx tsc --noEmit` reports no NEW errors traced to any of the three files touched (comment-only edits should be tsc-neutral).
    - `grep -c "\[pv-scroll-diag\]" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyConversationsPanel.tsx 2>/dev/null || echo 0` returns `0` (PrettyConversationsPanel.tsx was not touched by this task — this gate confirms scope containment; a value of `0` proves either the file did not contain the token OR the file was not modified).
  </acceptance_criteria>
  <done>PrettyView.tsx L406 log prefix + L396/L402 supporting comments renamed to `[pv-scroll]`; two orphan API-reference comments in CollapsedPanelCloseLane.tsx and ComposeBox.tsx updated to reflect the deleted / renamed identifiers; PrettyConversationsPanel.tsx untouched per CONTEXT.md scope boundary; all grep gates return expected counts.</done>
</task>

</tasks>

<verification>
Run the pretty-view scoped test suite (this plan's scope + upstream regression check):

```bash
cd /home/ubuntu/skynet-tina && npx vitest run --reporter=basic src/ui/features/pretty-view/ 2>&1 | tail -30
```

Full pretty-view suite must be green. Any failure whose stack trace lands in `PrettyView.tsx` or in a test file exercising the auto-scroll surface (`use-auto-scroll.test.ts`, `PrettyView.*.test.tsx` that mount the scroll container) blocks this plan. Failures unrelated to auto-scroll (e.g. voice, upload, aside — none of which this plan touches) are pre-existing and out of scope.

Additionally run `npx tsc --noEmit` and confirm no new errors traced to PrettyView.tsx related to the auto-scroll destructure or the hide-pin-reveal wrapper.
</verification>

<success_criteria>
- All structural-grep gates across all three tasks return the expected counts (12 in Task 1 + Task 2, plus 6 in the new Task 3).
- `PrettyView.tsx` compiles under `tsc --noEmit` with no new auto-scroll-typed errors.
- Pretty-view scoped vitest suite is green modulo pre-existing failures unrelated to the auto-scroll surface.
- All references to the old API (`sentinelRef`, `scrollToBottomAndFollow`, `isPinnedToBottom`, `data-pv-scroll-sentinel`) are gone from `PrettyView.tsx` — code AND comments.
- The three shape-file DOM invariants (`overflow-anchor:none` on container, hide-pin-reveal wrapper, jump-to-bottom pill bound to `mode`) are all in place.
</success_criteria>

<output>
Create `.planning/phases/71-rewrite-prettyview-auto-scroll-from-first-principles-two-sta/71-03-SUMMARY.md` when done — record: (a) any test-file failures observed during scoped-suite runs that were triaged as pre-existing / unrelated to Phase 71 (with 1-line justification per file), (b) the exact hide-pin-reveal DOM shape landed (opening + closing tag anchor points), (c) whether the LoadMore `onGoodToGo` prop rename introduced any type mismatches that had to be handled elsewhere.
</output>
