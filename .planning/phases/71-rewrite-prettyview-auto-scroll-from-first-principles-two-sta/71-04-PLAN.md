---
phase: 70-rewrite-prettyview-auto-scroll-from-first-principles-two-sta
plan: 04
type: execute
wave: 4
depends_on:
  - 70-03
files_modified: []
autonomous: false

requirements: []

must_haves:
  truths:
    - "No file under src/ui/features/pretty-view/ contains any residual reference to the old auto-scroll API (sentinelRef, scrollToBottomAndFollow, isPinnedToBottom, data-pv-scroll-sentinel, pinnedRef, didFirstContentScrollRef)"
    - "The observer-count invariant holds across the codebase: exactly 1 MutationObserver + 1 ResizeObserver in use-auto-scroll.ts, 0 IntersectionObserver in use-auto-scroll.ts"
    - "Vitest pretty-view suite is fully green"
    - "The four bounty scenarios each work in a real browser: (a) session lands at bottom on entry, (b) window resize preserves bottom anchor when at-bottom, (c) drag-drop pane change preserves bottom anchor when at-bottom, (d) accessory bubble mount does not cover latest message when at-bottom"
    - "The jump-to-bottom pill appears when and only when the user is scrolled up; disappears when scrolled back to within tolerance"
    - "No visible flash at top on first paint (hide-pin-reveal working)"
    - "Session re-entry preserves scroll position (switch away + back → same scrollTop)"
  artifacts: []
  key_links: []
---

<objective>
Close Phase 71 with a two-part verification pass: (1) a codebase-wide contamination sweep that greps for any residual references to the old auto-scroll API across the ENTIRE repo (not just the three files edited by Plans 01-03), catching stale usages in tests, comments, or unrelated files that the file-scoped grep gates in prior plans could have missed; (2) a human-verify checkpoint against the four bounty scenarios in a real browser, per shape-file § Vehicle notes "Four bounties expected to close on ship: pretty-view-auto-scroll-three-bug-rewrite, conversations-scroll-to-bottom-on-load, pretty-view-scroll-to-bottom-after-send, conversation-list-scroll-delay-on-load."

Purpose: The prior plans landed the code. This plan proves the code shipped what the shape file promised. Every "What would make it wrong" bullet from the shape file becomes a specific verification step here. Failures in either task get triaged to a follow-up plan (`70-05-PLAN.md`) rather than papered over — this plan's role is to make failures visible and actionable, not to hide them.

Output: One codebase sweep report (findings + disposition per residual reference), one bounty-scenario verification report (four scenarios × pass/fail × Ashley's verification signature). No code changes in this plan under normal (green-sweep + green-scenarios) conditions.

**Scope note:** The bounty `conversation-list-scroll-delay-on-load` lives in `PrettyConversationsPanel.tsx` (the pretty-CONVERSATIONS panel), NOT in `PrettyView.tsx` per CONTEXT.md § Code context "Files NOT touched by Phase 71". This plan verifies whether Phase 71's changes incidentally close that bounty (they might, if the fix was upstream) or whether it needs a separate small follow-on. Do NOT expand scope to fix the conversation-list panel here.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/ubuntu/skynet-tina/.planning/phases/71-rewrite-prettyview-auto-scroll-from-first-principles-two-sta/71-CONTEXT.md
@/home/ubuntu/skynet-tina/.planning/phases/71-rewrite-prettyview-auto-scroll-from-first-principles-two-sta/71-PATTERNS.md
@/home/ubuntu/skynet-tina/.planning/shapes/shape-pv-autoscroll-rewrite.md
@/home/ubuntu/skynet-tina/.planning/phases/71-rewrite-prettyview-auto-scroll-from-first-principles-two-sta/71-01-SUMMARY.md
@/home/ubuntu/skynet-tina/.planning/phases/71-rewrite-prettyview-auto-scroll-from-first-principles-two-sta/71-02-SUMMARY.md
@/home/ubuntu/skynet-tina/.planning/phases/71-rewrite-prettyview-auto-scroll-from-first-principles-two-sta/71-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Codebase-wide contamination sweep + observer-invariant verification</name>
  <files>(read-only: entire repo under src/, tests, and .planning/phases/70-*)</files>
  <read_first>
    - /home/ubuntu/skynet-tina/.planning/phases/71-rewrite-prettyview-auto-scroll-from-first-principles-two-sta/71-03-SUMMARY.md (Plan 03's disposition notes on any test-file failures triaged as pre-existing — use this to distinguish "known unrelated" from "sweep-detected regression")
    - /home/ubuntu/skynet-tina/.planning/phases/71-rewrite-prettyview-auto-scroll-from-first-principles-two-sta/71-PATTERNS.md (§ "Structural-grep acceptance gates" at L469-479 — the canonical list of grep gates the sweep expands to codebase scope)
    - /home/ubuntu/skynet-tina/.planning/shapes/shape-pv-autoscroll-rewrite.md (§ "What would make it wrong" all six bullets — the sweep asserts each bullet's failure mode is absent)
  </read_first>
  <acceptance_criteria>
    Run the sweep as a sequence of grep gates. Each gate has an EXPECTED count and any deviation is a failure that must be either (a) triaged as intentional (with 1-line justification recorded in this task's output) or (b) fixed inline before the task completes.

    Contamination sweep grep gates (whole-repo, excluding `.planning/` since that holds historical documentation):

    - `grep -rln "sentinelRef" /home/ubuntu/skynet-tina/src /home/ubuntu/skynet-tina/tests 2>/dev/null | wc -l` returns `0` — the old sentinel API name appears NOWHERE in code.
    - `grep -rln "scrollToBottomAndFollow" /home/ubuntu/skynet-tina/src /home/ubuntu/skynet-tina/tests 2>/dev/null | wc -l` returns `0` — the old scroll action name gone from all code.
    - `grep -rln "isPinnedToBottom" /home/ubuntu/skynet-tina/src /home/ubuntu/skynet-tina/tests 2>/dev/null | wc -l` returns `0` — the old pinned-state name gone.
    - `grep -rln "data-pv-scroll-sentinel" /home/ubuntu/skynet-tina/src /home/ubuntu/skynet-tina/tests 2>/dev/null | wc -l` returns `0` — the old DOM attribute gone.
    - `grep -rln "pinnedRef\|didFirstContentScrollRef" /home/ubuntu/skynet-tina/src /home/ubuntu/skynet-tina/tests 2>/dev/null | wc -l` returns `0` — the old hook-internal ref names gone.

    Observer-invariant grep gates (scoped to use-auto-scroll.ts):

    - `grep -c "IntersectionObserver" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.ts` returns `0` — no IO in the new hook per shape-file § Explicit ownership.
    - `grep -c "new MutationObserver" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.ts` returns exactly `1` — exactly one MO instance.
    - `grep -c "new ResizeObserver" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.ts` returns exactly `1` — exactly one RO instance.

    Log-prefix invariant (SCOPED — PrettyConversationsPanel.tsx is out-of-scope per 71-CONTEXT.md § "Files NOT touched by Phase 71" and is explicitly whitelisted below):

    - `grep -rln "\[pv-scroll-diag\]" /home/ubuntu/skynet-tina/src/ui/features/pretty-view /home/ubuntu/skynet-tina/src/ui/shell 2>/dev/null | grep -v PrettyConversationsPanel | wc -l` returns `0` — the old log prefix retired per phase-guidance ("reshape to match new state-machine event classes: `[pv-scroll]` prefix (drop `-diag` suffix per new arch)"). Scope narrowed to the two directories where Phase 71's rewrite lands (`pretty-view/` for the auto-scroll module + PrettyView.tsx, `shell/` for the CollapsedPanelCloseLane.tsx orphan-comment fix in Plan 70-03 Task 3). The `grep -v PrettyConversationsPanel` whitelist explicitly excludes the pretty-conversations panel, which is a separate feature-directory whose scroll logic is untouched by Phase 71; any `[pv-scroll-diag]` references there predate Phase 71 and are out-of-scope for this sweep.
    - `grep -c "\[pv-scroll\]" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.ts` returns at least `3` (the new log prefix is present in the new hook).
    - `grep -c "\[pv-scroll\]" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx` returns at least `1` (the renamed cap-drop log site from Plan 70-03 Task 3 — proves the L406 rename landed).

    Smooth-scroll absolute-prohibition sweep (scoped to the four pretty-view files touched):

    - `grep -Ec "behavior:\s*[\"']smooth[\"']" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.ts /home/ubuntu/skynet-tina/src/ui/features/pretty-view/auto-scroll-machine.ts /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx 2>/dev/null` returns `0` — instant scroll writes only per shape § Chase behavior LOCKED.

    Test-suite regression check:

    - `cd /home/ubuntu/skynet-tina && npx vitest run --reporter=basic src/ui/features/pretty-view/ 2>&1 | tail -5` — reports zero failing tests. If any failing tests appear, they MUST be triaged: cross-reference against 71-03-SUMMARY.md's pre-existing-failures list. Any new failures not on that list block this task and require a fix or a formal deferral note.

    Type-check regression:

    - `cd /home/ubuntu/skynet-tina && npx tsc --noEmit 2>&1 | grep -Ec "pretty-view/(use-auto-scroll|auto-scroll-machine|PrettyView)\.tsx?"` returns `0` — no tsc errors traced to any of the four Phase-70-touched files.

    Deferred-scope sanity (verify no scope creep):

    - `grep -rn "load-more.*anchor\|load-more.*preserve" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.ts /home/ubuntu/skynet-tina/src/ui/features/pretty-view/auto-scroll-machine.ts 2>/dev/null | wc -l` returns `0` — the deferred load-more anchor logic did NOT bleed into Phase 71 code.
    - `grep -rn "unread\|badge" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/use-auto-scroll.ts /home/ubuntu/skynet-tina/src/ui/features/pretty-view/PrettyView.tsx | grep -i "jump\|scroll\|bottom" | wc -l` returns `0` — no unread-count badge added to jump-to-bottom pill per § Scope edges Tempting-but-no.
  </acceptance_criteria>
  <action>
Execute every grep gate above in sequence. For each gate:
  - If the observed count matches the expected count, mark PASS and continue.
  - If the observed count DOES NOT match: (a) identify each file/line the grep matched, (b) inspect via Read to determine whether the reference is stale code (must fix) or intentional (e.g. a `.planning/` retrospective document referencing history — those are allowed and the grep should already exclude `.planning/`; if the reference is in a code file, it is a residual and must be fixed).

For any residual reference detected in code, apply an inline fix via Edit — do NOT open a new plan. This task's whole purpose is to make the codebase clean; deferring residuals to another plan would leave the phase in an "almost done but…" state.

For the test-suite regression check specifically: if a failing test appears that is NOT on the 71-03-SUMMARY.md pre-existing-failures list, first check whether it's a flake by re-running JUST that test file three times. If it fails consistently, triage: is it a real Phase 71 regression (fix inline) or a pre-existing failure that Plan 03 missed adding to its list (record here, do not fix if unrelated)?

Report structure — write findings to `.planning/phases/71-rewrite-prettyview-auto-scroll-from-first-principles-two-sta/71-04-SWEEP-REPORT.md` with sections:
  1. Contamination sweep — for each of the 5 contamination gates, one line: `GATE: {gate name} — {PASS|FAIL} ({count} matches)`. If FAIL, list files/lines and disposition.
  2. Observer-invariant sweep — same shape for 3 gates.
  3. Log-prefix sweep — same shape for 3 gates (1 scoped-directory absence gate + 2 presence gates).
  4. Smooth-scroll sweep — 1 gate.
  5. Test-suite check — pass/fail summary + list of any failing tests + disposition.
  6. Type-check — pass/fail with error output if any.
  7. Deferred-scope sanity — 2 gates.
  8. Overall verdict — GREEN (all gates pass, ready for Task 2) or RED (list of items to fix before Task 2 can proceed).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; \
      { test -f .planning/phases/71-rewrite-prettyview-auto-scroll-from-first-principles-two-sta/71-04-SWEEP-REPORT.md || { echo "FAIL: SWEEP-REPORT.md missing"; exit 1; } } &amp;&amp; \
      { CT=$(grep -rln "sentinelRef" src tests 2>/dev/null | wc -l); [ "$CT" = "0" ] || { echo "FAIL: contamination-01 sentinelRef found in $CT file(s)"; exit 1; } } &amp;&amp; \
      { CT=$(grep -rln "scrollToBottomAndFollow" src tests 2>/dev/null | wc -l); [ "$CT" = "0" ] || { echo "FAIL: contamination-02 scrollToBottomAndFollow found in $CT file(s)"; exit 1; } } &amp;&amp; \
      { CT=$(grep -rln "isPinnedToBottom" src tests 2>/dev/null | wc -l); [ "$CT" = "0" ] || { echo "FAIL: contamination-03 isPinnedToBottom found in $CT file(s)"; exit 1; } } &amp;&amp; \
      { CT=$(grep -rln "data-pv-scroll-sentinel" src tests 2>/dev/null | wc -l); [ "$CT" = "0" ] || { echo "FAIL: contamination-04 data-pv-scroll-sentinel found in $CT file(s)"; exit 1; } } &amp;&amp; \
      { CT=$(grep -rlnE "pinnedRef|didFirstContentScrollRef" src tests 2>/dev/null | wc -l); [ "$CT" = "0" ] || { echo "FAIL: contamination-05 pinnedRef/didFirstContentScrollRef found in $CT file(s)"; exit 1; } } &amp;&amp; \
      { CT=$(grep -c "IntersectionObserver" src/ui/features/pretty-view/use-auto-scroll.ts); [ "$CT" = "0" ] || { echo "FAIL: observer-invariant-01 IntersectionObserver present ($CT matches)"; exit 1; } } &amp;&amp; \
      { CT=$(grep -c "new MutationObserver" src/ui/features/pretty-view/use-auto-scroll.ts); [ "$CT" = "1" ] || { echo "FAIL: observer-invariant-02 expected exactly 1 new MutationObserver, got $CT"; exit 1; } } &amp;&amp; \
      { CT=$(grep -c "new ResizeObserver" src/ui/features/pretty-view/use-auto-scroll.ts); [ "$CT" = "1" ] || { echo "FAIL: observer-invariant-03 expected exactly 1 new ResizeObserver, got $CT"; exit 1; } } &amp;&amp; \
      { CT=$(grep -rln "\[pv-scroll-diag\]" src/ui/features/pretty-view src/ui/shell 2>/dev/null | grep -v PrettyConversationsPanel | wc -l); [ "$CT" = "0" ] || { echo "FAIL: log-prefix-01 [pv-scroll-diag] still present in $CT file(s) within scoped directories"; exit 1; } } &amp;&amp; \
      { CT=$(grep -c "\[pv-scroll\]" src/ui/features/pretty-view/use-auto-scroll.ts); [ "$CT" -ge "3" ] || { echo "FAIL: log-prefix-02 expected at least 3 [pv-scroll] in use-auto-scroll.ts, got $CT"; exit 1; } } &amp;&amp; \
      echo "PASS: all 8 gates (5 contamination + 3 observer-invariant) + 2 log-prefix independently re-verified"</automated>
  </verify>
  <done>Sweep report written; every grep gate PASSES or has an inline fix applied; overall verdict is GREEN; ready for human-verify checkpoint in Task 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the four bounty scenarios in a real browser</name>
  <what-built>
    Phase 71 landed a rewrite of the PrettyView auto-scroll layer as a two-state (at-bottom / not-at-bottom) position-derived state machine:
      - `src/ui/features/pretty-view/auto-scroll-machine.ts` (NEW pure reducer)
      - `src/ui/features/pretty-view/use-auto-scroll.ts` (rewritten thin wrapper)
      - `src/ui/features/pretty-view/PrettyView.tsx` (consumer integration edits — sentinel div deleted, `[overflow-anchor:none]` added, hide-pin-reveal wrapper added, jump-to-bottom pill re-wired, RENDER-03 comment block rewritten)

    Behavioral changes visible to Ashley:
      - Session ALWAYS lands at bottom on entry (hide-pin-reveal — no visible flash at top).
      - Window resize while at bottom preserves the bottom anchor (chase fires on ResizeObserver tick).
      - Drag-dropping a session into a new split-pane while at bottom preserves the bottom anchor (same event class as window resize).
      - Accessory bubbles (WIP indicator, waiting bubble, plan-pending bubble, aside bubble) appearing above the compose box while at bottom do NOT cover the latest message (chase fires on MutationObserver tick).
      - Jump-to-bottom pill appears when and only when the user is scrolled up more than ~28px from bottom (~60px on touch devices for iOS momentum slack).
      - Send-message always flips to at-bottom regardless of prior scroll position.
      - Switching away from a session and coming back preserves the scroll position (no auto-scroll on session re-entry).
      - No smooth-scroll animation on any chase — always instant.

    The observer-count contamination invariant is enforced by grep gates (Task 1 SWEEP-REPORT): exactly 1 MutationObserver + 1 ResizeObserver in the hook, 0 IntersectionObserver, 0 sentinel div. The state-machine contamination invariant is enforced by the pure reducer's compile-time exhaustiveness sentinel and the truth-table tests.
  </what-built>
  <how-to-verify>
    1. Task 1 (SWEEP-REPORT) is GREEN. If it is RED, do NOT proceed here — resolve sweep failures first.

    2. Ashley opens the deployed Skynet at https://term.gigaashley.click (or the current staging URL if a staging build is in play) in whichever browser + form-factor she wants to test — the invariants must hold across desktop-Chrome, mobile-Safari, and mobile-Chrome per the shape-file's iOS touch-slack requirement.

    3. **Bounty verification — pretty-view-auto-scroll-three-bug-rewrite** (the primary bounty this phase closes):
       (3a) Pick a session with existing messages (say the current active Claude-code session on a peer). Click into it from the conversation list. EXPECTED: view lands at the bottom immediately, showing the latest message. FAIL if: view lands at the top and jumps down visibly, or view lands somewhere mid-list.
       (3b) Wait for Claude to emit a new message (or type + send `/pause` and let Claude respond). EXPECTED: new message appears at the bottom, view chases to keep it visible (scrollTop moves with scrollHeight). FAIL if: message appears out of view, or the view yanks unexpectedly if she scrolled up.
       (3c) Scroll up manually via wheel or trackpad. EXPECTED: mode flips to not-at-bottom within ~28px of scrolling up, jump-to-bottom pill appears at the bottom-right. FAIL if: pill doesn't appear, or appears while she's still within 28px of bottom.
       (3d) Click the jump-to-bottom pill. EXPECTED: view instantly (no smooth-scroll) jumps to the bottom, pill disappears, mode flips back to at-bottom. FAIL if: smooth-scroll animation observed, or pill stays visible.
       (3e) While scrolled up, wait for a new message. EXPECTED: view does NOT yank — stays where she left it. Pill remains visible. FAIL if: view yanks to bottom despite being scrolled up (this is the "no yank when scrolled up" invariant regression from Test 5).

    4. **Bounty verification — conversations-scroll-to-bottom-on-load** (bounty at pretty-view level):
       (4a) Switch tabs / windows or close the Skynet tab, then reopen the same URL cold. EXPECTED: the currently-selected session's view lands at bottom on cold load, no visible flash at top before jumping. FAIL if: any visible flash at top (hide-pin-reveal failed), or view lands anywhere other than bottom.

    5. **Bounty verification — pretty-view-scroll-to-bottom-after-send**:
       (5a) Scroll up manually into the middle of a long session. EXPECTED: pill visible, mode = not-at-bottom.
       (5b) Type a short message in the compose box and hit Enter. EXPECTED: view flips to at-bottom + chases to the bottom + shows Ashley's own just-sent message and any reply. FAIL if: view stays scrolled-up after send.

    6. **Window resize + drag-drop preservation** (implicit under bounty (3), explicit here):
       (6a) With view at bottom, resize the browser window (drag a corner smaller then bigger). EXPECTED: view stays anchored at bottom throughout the resize. FAIL if: bottom anchor is lost during resize.
       (6b) With view at bottom in a single-pane layout, drag another session's tab to open it in a split-pane layout (if the current multi-view surface supports it — verify via the "add session" affordance Ashley knows). EXPECTED: original session's view still at bottom in the resized pane. FAIL if: bottom anchor lost.

    7. **Accessory bubble non-occlusion** (the core of the bounty):
       (7a) Trigger a Claude action that produces a WipBubble or PlanPendingBubble (e.g. send a plan-mode message and wait for approval prompt, OR wait for a long-running tool to fire the WIP indicator). EXPECTED: while at bottom, the accessory bubble appears above the compose box AND the latest message remains fully visible (the chase adjusts scrollTop to compensate for the accessory's height push). FAIL if: latest message gets covered by the accessory.
       (7b) When the accessory unmounts (WIP finishes, plan is approved), same behavior: latest message stays visible.

    8. **Session re-entry preservation**:
       (8a) Scroll to a specific message mid-list in session A. Switch to a different session B. Switch back to session A. EXPECTED: view is at the same scrollTop as when Ashley left. FAIL if: view is at the bottom OR at any different scrollTop than when left.

    9. **iOS touch slack** (mobile-Safari only, skip if desktop-only verification):
       (9a) On iPhone Safari, at bottom, do a small overscroll rubber-band (drag down past the bottom and let go). EXPECTED: mode stays at-bottom despite the momentum-scroll delta briefly registering distance-from-bottom > 28px, because the touch-extra-slack (60px total tolerance) absorbs momentum overshoot. FAIL if: the rubber-band silently flips mode to not-at-bottom and the pill appears.

    10. **Conversation-list scroll bounty check** (`conversation-list-scroll-delay-on-load` — separate panel):
        (10a) Open Skynet cold on a wide browser window. Observe the LEFT-side conversation list (PrettyConversationsPanel). EXPECTED behavior per that bounty: the current-session row should be visible without needing to scroll. FAIL if: current session's row is scrolled out of view on cold load.
        Note: this bounty may or may not close on Phase 71 — Phase 71 didn't touch the conversation-list panel. If (10a) FAILS, that's a separate follow-on ticket, NOT a Phase 71 blocker. Ashley notes the disposition.

    For each scenario, Ashley records PASS or FAIL. Any FAIL blocks phase-shipping and either (a) requires a follow-up plan (70-05) to close the gap, or (b) requires the phase to be considered "shipped with known-open gap" if the gap is minor and Ashley accepts it.
  </how-to-verify>
  <resume-signal>
    Ashley: type `approved` if ALL scenarios (3a-8a; 9a if mobile-tested; 10a is informational) PASS. Otherwise describe which scenario(s) failed and observed vs. expected behavior for each.

    If approved, this task closes Phase 71 with the four expected bounty closures pending orchestrator ship-gate. If revisions are needed, a follow-up 70-05-PLAN.md will be created to close the identified gaps.
  </resume-signal>
</task>

</tasks>

<verification>
Task 1's SWEEP-REPORT.md file exists and its final line reports "Overall verdict: GREEN". Task 2's human-verify checkpoint returns `approved`.

If both hold, Phase 71's plan-scope work is done. The orchestrator ship-gate (full-suite test run + docker build + deploy + post-deploy verification) is the next step, but that is orchestrator scope per the box-maintainer standing directive — this plan does not attempt to ship.
</verification>

<success_criteria>
- SWEEP-REPORT.md file exists with overall verdict GREEN and every grep gate PASS.
- Ashley's real-browser verification of the four bounty scenarios (three primary bounties + one informational conversation-list check) returns `approved`.
- No residual references to the old auto-scroll API anywhere in `src/` or `tests/`.
- Test suite green for `src/ui/features/pretty-view/`.
- Every "What would make it wrong" bullet from the shape file has been verified either structurally (Task 1 grep gates) or behaviorally (Task 2 scenarios).
</success_criteria>

<output>
Create `.planning/phases/71-rewrite-prettyview-auto-scroll-from-first-principles-two-sta/71-04-SUMMARY.md` when both tasks complete. Record:
  - Sweep report verdict + link to `71-04-SWEEP-REPORT.md`.
  - Which of the four bounty scenarios PASSED / FAILED / were deferred.
  - Disposition on the `conversation-list-scroll-delay-on-load` bounty (does it close on Phase 71 shipping, or does it need a separate small follow-on?).
  - Any known-open items Ashley accepted as acceptable-to-ship-with (with 1-line rationale each).
  - Which of the four expected bounty closures (per shape-file § Vehicle notes) are cleared for orchestrator to close when shipping.
</output>
