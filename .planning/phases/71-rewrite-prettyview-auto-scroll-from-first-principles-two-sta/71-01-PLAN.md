---
phase: 70-rewrite-prettyview-auto-scroll-from-first-principles-two-sta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/auto-scroll-machine.ts
  - src/ui/features/pretty-view/auto-scroll-machine.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "reduce(state, event) is a pure function — repeated calls with identical inputs return structurally-equal outputs"
    - "OUT-of-at-bottom transitions occur ONLY on {kind:'user-input'} events landing outside tolerance"
    - "INTO-at-bottom transitions occur on {kind:'jump-clicked'}, {kind:'send-fired'}, and {kind:'user-input'} landing inside tolerance"
    - "All other event kinds (content-changed, container-resized, measured) never transition mode — symmetric event handling; accessory-mount and accessory-unmount are subsumed into content-changed by the wrapper hook's MutationObserver per Plan 70-02"
    - "Mount-landing produces effect:'reveal' on the first measured event with non-zero content height, then hasLandedOnce is true forever after"
    - "Every reducer output for at-bottom mode + a bottom-moving event returns effect:'chase'; every reducer output for not-at-bottom mode + a bottom-moving event returns effect:'none'"
    - "Compile-time exhaustiveness sentinel (_exhaust: never) exists on every switch over AutoScrollEvent.kind and Mode"
  artifacts:
    - path: "src/ui/features/pretty-view/auto-scroll-machine.ts"
      provides: "Pure two-state auto-scroll reducer + string-literal type unions (Mode, AutoScrollEvent, AutoScrollEffect, AutoScrollState) + BOTTOM_TOLERANCE_PX / BOTTOM_TOLERANCE_TOUCH_EXTRA_PX constants"
      exports: ["reduce", "Mode", "AutoScrollEvent", "AutoScrollEffect", "AutoScrollState", "BOTTOM_TOLERANCE_PX", "BOTTOM_TOLERANCE_TOUCH_EXTRA_PX", "createInitialState"]
      contains: "export function reduce"
      min_lines: 200
    - path: "src/ui/features/pretty-view/auto-scroll-machine.test.ts"
      provides: "Pure-function truth-table tests covering every (Mode × EventKind) cell, mount-landing, and API-shape lock — zero mocks / zero DOM / zero timers"
      contains: "describe.each"
      min_lines: 200
  key_links:
    - from: "src/ui/features/pretty-view/auto-scroll-machine.ts"
      to: "(nothing — pure module, zero imports)"
      via: "structural-grep gate: grep -v '^#' auto-scroll-machine.ts | grep -c '^import ' = 0"
      pattern: "^import "
    - from: "src/ui/features/pretty-view/auto-scroll-machine.test.ts"
      to: "src/ui/features/pretty-view/auto-scroll-machine.ts"
      via: "import { reduce, type AutoScrollState, type AutoScrollEvent, type Mode, type AutoScrollEffect } from './auto-scroll-machine'"
      pattern: "from \"./auto-scroll-machine\""
---

<objective>
Land the pure `auto-scroll-machine.ts` reducer that implements Phase 71's LOCKED two-state (at-bottom / not-at-bottom) position-derived auto-scroll state machine, plus the truth-table test file that locks every transition cell.

Purpose: The reducer is the CORE ARTIFACT of the phase. Every other file (the hook wrapper, PrettyView integration, verification) is glue around this pure function. The "no I/O imports" invariant makes reducer tests import+call cheap and forces the shape file's "single invariant, applied uniformly" rule into the type system — a future contributor cannot add a special-case for accessory-mount vs content-changed vs container-resized without doing it inside the switch statement, where the compile-time exhaustiveness sentinel makes the divergence visible in `npx tsc --noEmit`.

Output: Two new files under `src/ui/features/pretty-view/`. No changes to any existing file. Zero DOM. Zero React. Zero timers.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/ubuntu/skynet-tina/.planning/phases/71-rewrite-prettyview-auto-scroll-from-first-principles-two-sta/71-CONTEXT.md
@/home/ubuntu/skynet-tina/.planning/phases/71-rewrite-prettyview-auto-scroll-from-first-principles-two-sta/71-PATTERNS.md
@/home/ubuntu/skynet-tina/.planning/shapes/shape-pv-autoscroll-rewrite.md
@/home/ubuntu/skynet-tina/src/ui/features/pretty-view/resolve-phase.ts
@/home/ubuntu/skynet-tina/src/ui/features/pretty-view/resolve-phase.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create pure auto-scroll-machine.ts reducer with locked truth table</name>
  <files>src/ui/features/pretty-view/auto-scroll-machine.ts</files>
  <read_first>
    - /home/ubuntu/skynet-tina/src/ui/features/pretty-view/resolve-phase.ts (VERBATIM STRUCTURAL ANALOG — copy the "no I/O imports" header comment shape, the string-literal union declarations, the pure-reducer branch-order comment style, and the `_exhaust: never` compile-time exhaustiveness sentinel pattern from lines 41-48 (header), 80-122 (unions), 151-200 (reducer + switch + sentinel))
    - /home/ubuntu/skynet-tina/.planning/phases/71-rewrite-prettyview-auto-scroll-from-first-principles-two-sta/71-PATTERNS.md (§ "auto-scroll-machine.ts (NEW — pure reducer)" — pattern spec for this exact file; § "Shared Patterns" — pure-reducer-extraction + exhaustiveness sentinel + structural-grep gates)
    - /home/ubuntu/skynet-tina/.planning/phases/71-rewrite-prettyview-auto-scroll-from-first-principles-two-sta/71-CONTEXT.md (§ "Shape" paras 1-4 — LOCKED transition rules; § "What would make it wrong" bullets 1, 2, 6 — the anti-patterns this reducer must make structurally impossible)
    - /home/ubuntu/skynet-tina/.planning/shapes/shape-pv-autoscroll-rewrite.md (§ Shape / § Philosophy — source-of-truth agreement, do not deviate)
  </read_first>
  <behavior>
    - Exported type `Mode = "at-bottom" | "not-at-bottom"` — exactly these two string literals, no third state.
    - Exported discriminated-union type `AutoScrollEvent` with exactly these variants (kind field, no more, no fewer): `{ kind: "measured"; distanceFromBottom: number; contentHeight: number }`, `{ kind: "content-changed" }`, `{ kind: "container-resized" }`, `{ kind: "user-input"; distanceFromBottom: number; isTouch: boolean }`, `{ kind: "jump-clicked" }`, `{ kind: "send-fired" }`.
    - Exported type `AutoScrollEffect = "chase" | "reveal" | "none"`.
    - Exported type `AutoScrollState = { mode: Mode; hasLandedOnce: boolean; lastMeasuredDistance: number }`.
    - Exported `createInitialState(): AutoScrollState` returns `{ mode: "at-bottom", hasLandedOnce: false, lastMeasuredDistance: 0 }` (initial mode is at-bottom because the first mount lands at bottom via hide-pin-reveal).
    - Exported constants: `BOTTOM_TOLERANCE_PX = 28` (with comment naming shape-file § Shape para 1 "roughly one line of body text, ~24-32px, pick center"), `BOTTOM_TOLERANCE_TOUCH_EXTRA_PX = 32` (with comment naming shape-file § Prior context "iOS touch-momentum overshoot" pitfall and § Shape para 1 "additional slack on touch devices").
    - Exported `reduce(state: AutoScrollState, event: AutoScrollEvent): { next: AutoScrollState; effect: AutoScrollEffect }` implementing this LOCKED truth table:
      * `mode: "at-bottom"` + `{kind:"user-input"}` with `distanceFromBottom > (isTouch ? BOTTOM_TOLERANCE_PX + BOTTOM_TOLERANCE_TOUCH_EXTRA_PX : BOTTOM_TOLERANCE_PX)` → `{ next.mode: "not-at-bottom", effect: "none" }` (the OUT transition, ONLY user-input can trigger it).
      * `mode: "at-bottom"` + `{kind:"user-input"}` inside tolerance → `{ next.mode: "at-bottom", effect: "none" }` (staying at bottom via user gesture, no chase needed).
      * `mode: "at-bottom"` + any of `content-changed | container-resized | measured` → `{ next.mode: "at-bottom", effect: "chase" }` (symmetric-event chase).
      * `mode: "at-bottom"` + `jump-clicked | send-fired` → `{ next.mode: "at-bottom", effect: "chase" }` (already at bottom, still chase to guarantee position).
      * `mode: "not-at-bottom"` + `{kind:"user-input"}` with distance inside tolerance → `{ next.mode: "at-bottom", effect: "chase" }` (IN transition via scrolling back to bottom).
      * `mode: "not-at-bottom"` + `{kind:"user-input"}` outside tolerance → `{ next.mode: "not-at-bottom", effect: "none" }` (still scrolled up).
      * `mode: "not-at-bottom"` + `content-changed | container-resized | measured` → `{ next.mode: "not-at-bottom", effect: "none" }` (LOAD-BEARING: no yank when scrolled up).
      * `mode: "not-at-bottom"` + `jump-clicked` → `{ next.mode: "at-bottom", effect: "chase" }`.
      * `mode: "not-at-bottom"` + `send-fired` → `{ next.mode: "at-bottom", effect: "chase" }` (send flips regardless of prior state).
      * Mount-landing: when `state.hasLandedOnce === false` AND event is `{kind:"measured"; contentHeight > 0}` AND `mode === "at-bottom"` → `{ next: {...state, hasLandedOnce: true}, effect: "reveal" }`. (Contentheight > 0 gate is the hide-pin-reveal wait; the reveal effect flips the surface from hidden to visible; the chase to bottom happens as part of the same event class via a subsequent reduce with content-changed if needed — reveal is the terminal step of mount-landing, not a chase.)
    - Header comment MUST include a paragraph stating "NO I/O IMPORTS — pure function only. No React imports, no DOM imports, no logger imports, no timer scheduling, no wall-clock reads. Enforced by the acceptance structural-grep gate: `grep -v '^#' src/ui/features/pretty-view/auto-scroll-machine.ts | grep -c '^import '` → 0" (copied verbatim in shape from resolve-phase.ts lines 41-48).
    - Every `switch` on `event.kind` MUST have a `default: { const _exhaust: never = event; return _exhaust; }` sentinel (verbatim from resolve-phase.ts lines 182-186). Same for any switch on `state.mode` if present.
  </behavior>
  <action>
Create `src/ui/features/pretty-view/auto-scroll-machine.ts` following the resolve-phase.ts structural analog verbatim. The file MUST have zero non-type imports and zero references to `document`, `window`, `React`, `requestAnimationFrame`, `console`, `Date`, `performance`, `setTimeout`, `setInterval` — these are the concrete "no I/O" guardrails and every one is a grep gate below.

Structure the file in this exact section order (mirroring resolve-phase.ts):
  1) Header block-comment: (a) purpose ("pure two-state auto-scroll reducer — the deterministic core of Phase 71 rewrite"), (b) SHAPE-FILE cross-reference ("`.planning/shapes/shape-pv-autoscroll-rewrite.md` § Shape, § Philosophy, § What would make it wrong — LOCKED"), (c) truth-table diagram in ASCII table form matching the behavior spec above (mode × event.kind → next.mode + effect), (d) "NO I/O IMPORTS" invariant paragraph with the exact grep-gate command spelled out.
  2) String-literal union `Mode` (comment: "The two-state model is LOCKED — shape file § Shape para 1: any third state is contamination per § What would make it wrong bullet 6 and § Scope edges Tempting-but-no bullet 2 ('Adding a distinct sending/loading state ... is contamination').").
  3) Discriminated-union `AutoScrollEvent` with the six variants named in `<behavior>`. Each variant on its own line with an inline comment naming the trigger source (e.g. `// scroll listener + isTouch from useIsTouchDevice()`, `// MutationObserver on scroll-container children`, `// ResizeObserver on scroll container`, `// jump-to-bottom pill onClick`, `// handleComposeSend`, `// initial mount + content-height observer poll`).
  4) `AutoScrollEffect` union.
  5) `AutoScrollState` type.
  6) Constants block `BOTTOM_TOLERANCE_PX` + `BOTTOM_TOLERANCE_TOUCH_EXTRA_PX` with comments locking each to shape-file line references (per useHoldToRecord.ts constants-block pattern from PATTERNS.md § "Constants block pattern").
  7) `createInitialState()` factory (comment: "initial mode is at-bottom because the first mount lands at bottom via the hook's hide-pin-reveal; the reveal effect fires when hasLandedOnce flips true").
  8) `reduce()` function implementing the truth table. Order branches to match the ASCII table in the header. Include the `_exhaust: never` exhaustiveness sentinel on the event.kind switch verbatim from resolve-phase.ts lines 182-186.

The reducer MUST return a NEW object for `next` on every call (never return `state` unchanged) so referential-equality-based memoization downstream is not accidentally load-bearing. Use `{ ...state, mode: ... }` shape.

Do NOT log inside the reducer (keep pure). Do NOT reference `[pv-scroll-diag]` or any other logger. Diagnostic logging is the WRAPPER hook's job in Plan 70-02.

Do NOT add a "programmatic-write" event variant. The reducer's contract is that the CALLER never emits an event for a programmatic write — chase writes happen as side-effects of `effect: "chase"` being returned, and by construction cannot loop back into `reduce()`. This is the "if any programmatic scroll write can transition mode, the recursive-bug pattern the current code trips on is back" invariant from shape-file § What would make it wrong bullet 2 — made structurally impossible by never having a `programmatic-write` variant in `AutoScrollEvent`.
  </action>
  <verify>
    <automated>test -f /home/ubuntu/skynet-tina/src/ui/features/pretty-view/auto-scroll-machine.ts && cd /home/ubuntu/skynet-tina &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "auto-scroll-machine\.ts" | wc -l | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/ui/features/pretty-view/auto-scroll-machine.ts`.
    - `grep -v '^\s*[/*]' /home/ubuntu/skynet-tina/src/ui/features/pretty-view/auto-scroll-machine.ts | grep -c '^import '` returns `0` (no non-type imports — no React, no DOM, no logger; the structural-grep gate).
    - `grep -Ec "document|window\.|requestAnimationFrame|console\.|Date\.|performance\.|setTimeout|setInterval" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/auto-scroll-machine.ts` returns `0` (no browser/timer/wall-clock reads).
    - `grep -c '^export function reduce' /home/ubuntu/skynet-tina/src/ui/features/pretty-view/auto-scroll-machine.ts` returns `1`.
    - `grep -c '_exhaust: never' /home/ubuntu/skynet-tina/src/ui/features/pretty-view/auto-scroll-machine.ts` returns at least `1` (compile-time exhaustiveness sentinel on the event.kind switch).
    - `grep -c 'export type Mode\b' /home/ubuntu/skynet-tina/src/ui/features/pretty-view/auto-scroll-machine.ts` returns `1`.
    - `grep -c 'BOTTOM_TOLERANCE_PX' /home/ubuntu/skynet-tina/src/ui/features/pretty-view/auto-scroll-machine.ts` returns at least `2` (declaration + reference in reduce).
    - `grep -Ec '"at-bottom"|"not-at-bottom"' /home/ubuntu/skynet-tina/src/ui/features/pretty-view/auto-scroll-machine.ts` returns at least `4` (both literals used in Mode union + in reduce branches).
    - No `programmatic-write` string literal appears in the file: `grep -c programmatic-write /home/ubuntu/skynet-tina/src/ui/features/pretty-view/auto-scroll-machine.ts` returns `0`.
    - `cd /home/ubuntu/skynet-tina && npx tsc --noEmit` reports zero errors specifically referencing `auto-scroll-machine.ts` (other pre-existing errors in the tree are outside this task's scope).
  </acceptance_criteria>
  <done>Pure reducer file exists, compiles cleanly under `tsc --noEmit`, and every structural-grep gate above passes zero-tolerance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create auto-scroll-machine.test.ts with full truth-table coverage</name>
  <files>src/ui/features/pretty-view/auto-scroll-machine.test.ts</files>
  <read_first>
    - /home/ubuntu/skynet-tina/src/ui/features/pretty-view/resolve-phase.test.ts (VERBATIM STRUCTURAL ANALOG — copy the import block shape from lines 30-36, the type-membership self-check pattern from lines 47-69, the per-transition-class describe blocks from lines 71-209, and the `describe.each` full-matrix pattern from lines 220-270)
    - /home/ubuntu/skynet-tina/src/ui/features/pretty-view/auto-scroll-machine.ts (the file just created in Task 1 — this test imports from it)
    - /home/ubuntu/skynet-tina/.planning/phases/71-rewrite-prettyview-auto-scroll-from-first-principles-two-sta/71-PATTERNS.md (§ "auto-scroll-machine.test.ts (NEW)" — pattern spec for this exact file; § "Per-transition test structure" bullets — the six required transition-class describes)
  </read_first>
  <behavior>
    - `import { describe, it, expect } from "vitest"` — no other test-lib imports. No `renderHook`, no `@testing-library/react`, no `vi.fn`, no `document.createElement`.
    - Import block matches resolve-phase.test.ts lines 30-36 shape: `import { reduce, createInitialState, type Mode, type AutoScrollEvent, type AutoScrollEffect, type AutoScrollState, BOTTOM_TOLERANCE_PX, BOTTOM_TOLERANCE_TOUCH_EXTRA_PX } from "./auto-scroll-machine";`.
    - Type-membership self-check arrays for `Mode` values and `AutoScrollEvent.kind` values, each declared `as const satisfies readonly Mode[]` / `readonly AutoScrollEvent["kind"][]` (verbatim shape from resolve-phase.test.ts lines 47-60). A runtime `if (ALL_MODES.length !== 2 || ALL_EVENT_KINDS.length !== 6) throw new Error(...)` self-check follows (verbatim from resolve-phase.test.ts lines 65-69).
    - Test coverage groups (each its own `describe`), one `it` per row:
      * "at-bottom → at-bottom" — symmetric event chase: content-changed, container-resized, measured (all with `next.mode === "at-bottom"`, `effect === "chase"`).
      * "at-bottom → not-at-bottom" — ONLY user-input outside tolerance transitions; every other event MUST stay at-bottom. Include one `it` per non-user-input event kind asserting `next.mode` stayed `"at-bottom"` (contamination-guard tests).
      * "not-at-bottom → not-at-bottom" — LOAD-BEARING no-yank: content-changed, container-resized, measured all return `effect: "none"` and `next.mode` unchanged.
      * "not-at-bottom → at-bottom" — three specific triggers: jump-clicked, send-fired, user-input inside tolerance. Every other event MUST NOT trigger IN.
      * "Programmatic write cannot transition mode" — assert AutoScrollEvent has NO `programmatic-write` variant by checking the ALL_EVENT_KINDS array doesn't contain it; document that the reducer's contract makes this structurally impossible (no matching variant to dispatch).
      * "Mount-landing" — `hasLandedOnce: false` + `{kind:"measured", contentHeight: 100, distanceFromBottom: 0}` → `next.hasLandedOnce: true`, `effect: "reveal"`. A subsequent `measured` with `hasLandedOnce: true` → `effect: "chase"` (or `"none"` per truth table).
      * "iOS touch slack" — user-input at exactly `BOTTOM_TOLERANCE_PX + 1` with `isTouch: true` MUST stay at-bottom (within touch-extra slack); user-input at `BOTTOM_TOLERANCE_PX + BOTTOM_TOLERANCE_TOUCH_EXTRA_PX + 1` with `isTouch: true` MUST transition to not-at-bottom.
    - Full-matrix `describe.each` covering every (Mode × event.kind) cell asserting `next.mode` + `effect` per the LOCKED truth table (2 modes × 6 event kinds = 12 cells minimum; for user-input add both inside-tolerance and outside-tolerance rows plus touch variants).
    - Cardinality sanity test asserting the FULL_MATRIX length matches expected cell count.
    - Test 15-style compile-time exhaustiveness comment in a final `describe` (runtime no-op; documents that the `_exhaust: never` in the reducer catches future event additions at build time).
  </behavior>
  <action>
Create `src/ui/features/pretty-view/auto-scroll-machine.test.ts` following resolve-phase.test.ts verbatim in structure. Test file MUST be pure import+expect — no mocks, no timers, no DOM, no renderHook.

Suggested `it` count targeting ≥ 20 named tests plus the full-matrix `describe.each`:
  - Group 1 "at-bottom → at-bottom via symmetric events" — 3 tests (content-changed, container-resized, measured).
  - Group 2 "at-bottom → at-bottom via user-input inside tolerance" — 2 tests (desktop tolerance, touch tolerance).
  - Group 3 "at-bottom → not-at-bottom (OUT — only via user-input outside tolerance)" — 4 tests (desktop just-outside, desktop far-outside, touch just-outside-of-extra-slack, touch far-outside).
  - Group 4 "at-bottom → at-bottom (contamination guard: non-user-input events NEVER transition OUT)" — 3 tests (content-changed with huge distance, container-resized with huge distance, measured with huge distance — all MUST return `next.mode: "at-bottom"`; this is the primary anti-contamination gate).
  - Group 5 "not-at-bottom → not-at-bottom (LOAD-BEARING no-yank)" — 3 tests (content-changed, container-resized, measured — all `effect: "none"`).
  - Group 6 "not-at-bottom → at-bottom (IN — three specific triggers)" — 3 tests (jump-clicked, send-fired, user-input inside tolerance).
  - Group 7 "Send-fired flips at-bottom regardless of prior mode" — 2 tests (from at-bottom → still at-bottom + chase; from not-at-bottom → at-bottom + chase).
  - Group 8 "Mount-landing (hide-pin-reveal)" — 3 tests: (a) `createInitialState()` returns `hasLandedOnce: false`, (b) first `measured` with `contentHeight > 0` returns `effect: "reveal"` and flips `hasLandedOnce: true`, (c) second `measured` no longer returns "reveal" (idempotent).
  - Group 9 "Programmatic-write structural impossibility" — 1 test asserting the ALL_EVENT_KINDS array does not include `"programmatic-write"` (documents the § What would make it wrong bullet 2 invariant).
  - Group 10 "Full 2×6 matrix" — `describe.each` over a FULL_MATRIX const of `{mode, event, expectedNextMode, expectedEffect}` cells asserting each cell.
  - Group 11 "Compile-time exhaustiveness (documented)" — 1 runtime no-op test with a comment naming the `_exhaust: never` sentinel and `npx tsc --noEmit` as the actual gate.

Every test name MUST include the transition class it exercises so a regression's failure output names the exact cell that broke (matches resolve-phase.test.ts style "Test N: transport=X + paneState=Y → expected"). Use `.toBe<Mode>` and `.toBe<AutoScrollEffect>` generic form where possible for stricter typing (see resolve-phase.test.ts lines 82-84).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; npx vitest run --reporter=basic src/ui/features/pretty-view/auto-scroll-machine.test.ts 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/ui/features/pretty-view/auto-scroll-machine.test.ts`.
    - `cd /home/ubuntu/skynet-tina && npx vitest run --reporter=basic src/ui/features/pretty-view/auto-scroll-machine.test.ts` exits with code 0 and reports all tests passing.
    - Test count reported by vitest is ≥ 20 (excluding the `describe.each` expansion, which adds ≥ 12 more).
    - `grep -c '@testing-library' /home/ubuntu/skynet-tina/src/ui/features/pretty-view/auto-scroll-machine.test.ts` returns `0` (no renderHook, no React testing infrastructure — pure function tests only).
    - `grep -Ec "vi\.fn|vi\.mock|vi\.useFakeTimers|vi\.spyOn" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/auto-scroll-machine.test.ts` returns `0` (no mocks, no timer manipulation).
    - `grep -c "document\.createElement\|new Event(" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/auto-scroll-machine.test.ts` returns `0` (no DOM in reducer tests).
    - `grep -c "ALL_MODES\|ALL_EVENT_KINDS" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/auto-scroll-machine.test.ts` returns at least `4` (type-membership self-checks declared + referenced in the length runtime check + describe titles).
    - `grep -c "describe\.each\|it\.each" /home/ubuntu/skynet-tina/src/ui/features/pretty-view/auto-scroll-machine.test.ts` returns at least `1` (full-matrix table-driven test).
    - `cd /home/ubuntu/skynet-tina && npx tsc --noEmit` reports zero errors specifically referencing `auto-scroll-machine.test.ts`.
  </acceptance_criteria>
  <done>All truth-table tests pass under vitest; zero mocks / zero DOM / zero timers detected via grep gates; compile clean under tsc.</done>
</task>

</tasks>

<verification>
Both grep gates and the vitest run in the acceptance criteria above are the phase-level checks for this plan. Additionally, run the full pretty-view scoped test suite to confirm no cross-file regression:

```bash
cd /home/ubuntu/skynet-tina && npx vitest run --reporter=basic src/ui/features/pretty-view/ 2>&1 | tail -10
```

(Full-file scope for confidence — this plan only adds two files; existing tests must remain green.)
</verification>

<success_criteria>
- `src/ui/features/pretty-view/auto-scroll-machine.ts` exists and compiles under `tsc --noEmit` with zero non-type imports.
- `src/ui/features/pretty-view/auto-scroll-machine.test.ts` exists and passes under `vitest run` with ≥ 20 tests reported.
- All structural-grep gates in both tasks' `<acceptance_criteria>` return the expected counts.
- No modifications to any existing file (this plan is additive-only).
</success_criteria>

<output>
Create `.planning/phases/71-rewrite-prettyview-auto-scroll-from-first-principles-two-sta/71-01-SUMMARY.md` when done — record the exact `AutoScrollEvent` variant shapes chosen (including field names on discriminated-union variants) so Plan 70-02's wrapper knows the exact call signatures.
</output>
