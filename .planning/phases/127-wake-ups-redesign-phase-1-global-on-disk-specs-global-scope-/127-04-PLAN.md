---
phase: 127-wake-ups-redesign-phase-1-global-on-disk-specs-global-scope-
plan: 04
type: execute
wave: 3
depends_on:
  - 127-03
files_modified:
  - substrate/skills/id/SKILL.md
autonomous: false
requirements: []
user_setup: []
must_haves:
  truths:
    - "substrate/skills/id/SKILL.md documents the new global wake-up scope alongside the existing per-identity scope in the Scheduled wake-ups section"
    - "substrate/skills/id/SKILL.md spec-format section explicitly names the new fields (prompt, roles, skills), the kebab-case slug convention (D-03), and their D-NN provenance"
    - "The fleet-side /home/ubuntu/fleet/.claude/skills/id/coordinator-instructions.md file has the entire Type C section removed AND every satellite per-role wake-up reference stripped (D-16(b))"
    - "The single per-role wakeup spec directory on THIS host — ~/fleet/roles/box-maintainer/wakeups/ — has zero .json spec files (verified by RESEARCH §6) and requires no migration; a migration audit note is written into the phase summary confirming this"
    - "Fleet-wide per-role spec sweep protocol is documented in the phase summary so orchestrator can execute the remote-host portion post-greenlight (D-14, D-15 escalation threshold)"
  artifacts:
    - path: "substrate/skills/id/SKILL.md"
      provides: "Updated Scheduled wake-ups section covering per-identity + global scope"
      contains: "Global wake-ups"
    - path: "/home/ubuntu/fleet/.claude/skills/id/coordinator-instructions.md"
      provides: "Fleet-side coordinator instructions with per-role wake-up Type C fully removed"
      contains: "(NOT contains) Type C: Scheduled wake-up"
  key_links:
    - from: "substrate/skills/id/SKILL.md"
      to: "~/fleet/wakeups/<slug>/wakeup.json"
      via: "Spec-format documentation naming the global-scope path"
      pattern: "wakeups/<slug>/wakeup\\.json"
---

<objective>
Close out phase 1 by documenting the new mechanism (substrate/skills/id/SKILL.md — catalog-distributed to every managed host on the next distributor sweep) and retiring the old-mechanism artifacts (fleet-side coordinator-instructions.md — NOT in the catalog, direct edit only per RESEARCH §7) and confirming the local-host per-role migration state (D-14 says this is a hand-migration; RESEARCH §6 confirmed zero specs on this host, so migration is a verify-and-document step here, not a transform).

This plan is `autonomous: false` because it ends in a checkpoint:human-verify that hands the phase back to Ashley for the deploy greenlight — the fleet-wide per-role spec sweep and the fleet-side coordinator-instructions.md edit are non-code changes that intersect with distributor-managed and hand-edited fleet files respectively, and the "code + commit + scoped tests green" ownership boundary calls for user confirmation before the phase-close motion.

Purpose: D-16(b), D-16(c) — retire per-role wake-up dispatch code + docs; D-14, D-15 — hand-migration with sweep-and-write protocol; also document the global mechanism in the id-skill so future readers understand both scopes.

Output: (a) SKILL.md updated with a new Global wake-ups subsection alongside the existing per-identity section; (b) fleet-side coordinator-instructions.md with Type C removed (edit path is local filesystem because the file is NOT distributor-managed); (c) migration audit note in the phase summary confirming local host has zero per-role specs and describing the remote-host sweep protocol for the orchestrator to execute post-greenlight; (d) a blocking human-verify checkpoint listing exactly what Ashley needs to confirm before the phase ships.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/127-wake-ups-redesign-phase-1-global-on-disk-specs-global-scope-/127-CONTEXT.md
@.planning/phases/127-wake-ups-redesign-phase-1-global-on-disk-specs-global-scope-/127-RESEARCH.md
@.planning/phases/127-wake-ups-redesign-phase-1-global-on-disk-specs-global-scope-/127-PATTERNS.md
@.planning/shapes/shape-wake-ups-redesign.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update substrate/skills/id/SKILL.md — add Global wake-ups subsection</name>
  <files>substrate/skills/id/SKILL.md</files>
  <read_first>
    substrate/skills/id/SKILL.md lines 902-982 (the entire Scheduled wake-ups section; MUST read to preserve every existing subsection and match the JSONC-block style, the emoji conventions in headers like `### ⚠️ Who may create one`, and the exact spec-format wording)
    .planning/phases/127-wake-ups-redesign-phase-1-global-on-disk-specs-global-scope-/127-PATTERNS.md § substrate/skills/id/SKILL.md (has the recommended new subsection wording — mirror but adapt to actual line context read from the file)
    .planning/shapes/shape-wake-ups-redesign.md § Philosophy — especially the "Continuity is the prompt author's job" clause; new documentation must preserve that framing
    .planning/phases/127-wake-ups-redesign-phase-1-global-on-disk-specs-global-scope-/127-CONTEXT.md § Specifics — the user's philosophy on scheduling ownership ("only user authorizes creating one") applies verbatim to global wake-ups too; the new section must repeat the governance rule
  </read_first>
  <behavior>
    - Behavior: `grep -c '^## Scheduled wake-ups' substrate/skills/id/SKILL.md` returns 1 (the existing section header is unchanged)
    - Behavior: a new subsection with header `### Global wake-ups — a fresh identity per fire` (or similarly self-explanatory) is added immediately after the existing spec-format subsection at ~line 954, before the Firing semantics subsection at line 959
    - Behavior: the new subsection covers: (i) the on-disk path `~/fleet/wakeups/<slug>/wakeup.json` (D-01); (ii) the folder-per-slug rationale — companion files (D-02); (iii) the new fields (name, enabled, schedule, prompt, roles, skills — with the semantic that prompt is the newborn's first turn per D-05, roles+skills define what identity gets born per D-04); (iv) the fire behavior — drops a create-identity request file, a fresh identity is born, no running session woken (shape § "How wake-ups fire"); (v) a governance line repeating the user-reserved rule (agents may suggest, only the user authorizes creation — per Specifics)
    - Behavior: the existing per-identity subsection is byte-identical to pre-change (regression guard) — same headers, same JSONC block for the instruction/schedule shape, same wording; only the new subsection is inserted
    - Behavior: the existing Firing semantics subsection at line 959 has one line appended noting that global-scope specs share the same first-sight-anchor + one-catch-up + .fired-sentinel semantics (shape § "Fire semantics that must survive the port")
    - Behavior: no per-role wake-up documentation is added anywhere — this section explicitly does not mention `~/fleet/roles/<role>/wakeups/` because that mechanism is being sunset by D-16
  </behavior>
  <action>
    In substrate/skills/id/SKILL.md read the full Scheduled wake-ups section (lines 902-982 pre-change). Insert a new subsection between the existing `### Spec format` block and the existing `### Firing semantics (so there are no surprises)` block. The new subsection begins with header `### Global wake-ups — a fresh identity per fire` (matching the sentence-case + em-dash pattern of the surrounding subsections). The body: (1) one-paragraph overview naming the path `~/fleet/wakeups/<slug>/wakeup.json` (D-01), the folder-per-slug convention so companion files sit alongside (D-02), and the fire behavior — the scheduler drops a create-identity request file which Skynet's backend consumes to birth a fresh identity carrying the spec's roles and skills, first turn = the spec's prompt. (2) A JSONC block mirroring the existing per-identity spec-format block but with the global-scope fields — name / enabled / schedule / prompt / roles / skills — annotated with brief trailing comments and the (D-04, D-05) provenance. (3) A one-line note that continuity across recurring fires is the prompt author's job (shape's Philosophy). (4) A ⚠️ governance line repeating the user-reserved rule — "an agent may SUGGEST a global wake-up but only the user AUTHORIZES creating one" — matching the emoji + capitalization convention of the existing ⚠️ Who may create one subsection at line 909. Do NOT touch the pre-existing `### ⚠️ Who may create one`, `### Spec format`, or `### Firing semantics` subsections beyond the one-line append below. Then, in the Firing semantics subsection, append a single closing sentence: "Global-scope specs (see § Global wake-ups above) share these exact firing semantics — same first-sight anchor, same one-catch-up-on-missed-slot, same one_shot `.fired` sentinel + spec self-delete." Preserve every other byte of the file. Do NOT modify any other section (the § Ambient plumbing note at line 48, the § Fleet directives block, etc.) — the new documentation is scoped strictly to the Scheduled wake-ups section.
  </action>
  <verify>
    <automated>grep -c '^## Scheduled wake-ups' substrate/skills/id/SKILL.md | grep -q '^1$' && grep -c '^### Global wake-ups' substrate/skills/id/SKILL.md | grep -q '^1$' && grep -q 'fleet/wakeups/<slug>/wakeup.json\|fleet/wakeups/&lt;slug&gt;/wakeup.json' substrate/skills/id/SKILL.md && grep -q 'prompt' substrate/skills/id/SKILL.md && grep -q 'roles' substrate/skills/id/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c '^## Scheduled wake-ups' substrate/skills/id/SKILL.md` == 1 (top-level section preserved)
    - Source assertion: `grep -c '^### Global wake-ups' substrate/skills/id/SKILL.md` == 1 (new subsection present, exactly once)
    - Source assertion: `grep -c 'fleet/wakeups/<slug>/wakeup.json' substrate/skills/id/SKILL.md` >= 1 (new path documented)
    - Source assertion: `grep -c 'fleet/roles/.*/wakeups' substrate/skills/id/SKILL.md` == 0 (per-role path NOT documented — that mechanism is being sunset)
    - Source assertion: `grep -c '"instruction"' substrate/skills/id/SKILL.md` >= 1 (per-identity spec-format JSONC block preserved verbatim — regression guard)
    - Source assertion: `grep -c '"prompt"' substrate/skills/id/SKILL.md` >= 1 (new global-scope JSONC block naming the prompt field)
    - Source assertion: `grep -c 'user AUTHORIZES\|user-reserved\|user reserves\|user-approved' substrate/skills/id/SKILL.md` >= 2 (governance rule appears in both the pre-existing ⚠️ subsection AND the new Global wake-ups subsection)
    - Source assertion: `grep -c 'first-sight anchor\|first-sight' substrate/skills/id/SKILL.md` >= 1 (Firing semantics subsection retains its wording)
    - Behavior: rendering the file's Scheduled wake-ups section is coherent — the sequence is ⚠️ Who may create one → Spec format (per-identity) → Global wake-ups (new) → Firing semantics (with global-scope append). No orphaned prose or dangling headers.
  </acceptance_criteria>
  <done>SKILL.md carries the new Global wake-ups subsection with path/shape/governance documented; pre-existing per-identity content is byte-identical outside the two intentional insertions; grep gates confirm no per-role residue.</done>
</task>

<task type="auto">
  <name>Task 2: Remove per-role wake-up references from fleet-side coordinator-instructions.md</name>
  <files>/home/ubuntu/fleet/.claude/skills/id/coordinator-instructions.md</files>
  <read_first>
    /home/ubuntu/fleet/.claude/skills/id/coordinator-instructions.md — full file is 501 lines; MUST read at minimum lines 15-30 (frontmatter and the header paragraph at line 22 that mentions "role-scoped wake-up scheduler"), lines 50-70 (the Type detection paragraph around line 55 that enumerates Type C), lines 110-145 (the entire Type C block at 117-138), lines 395-410 (the wakeups discussion around lines 400-402 that references the role folder), lines 480-505 (the paragraph at 486-499 that describes constructing the per-role scheduler invocation)
    .planning/phases/127-wake-ups-redesign-phase-1-global-on-disk-specs-global-scope-/127-RESEARCH.md §5(b) — the pre-verified inventory of per-role references and their approximate line numbers
    .planning/phases/127-wake-ups-redesign-phase-1-global-on-disk-specs-global-scope-/127-PATTERNS.md § coordinator-instructions.md — the exact target references
    IMPORTANT: this file lives OUTSIDE the skynet git tree (path is /home/ubuntu/fleet/.claude/... not $CWD/...). Per RESEARCH §7 the file was retired from the distributor catalog on 2026-09-20; direct edit on the local host is the only channel. The edit is executor-scope for this box only — fleet-wide propagation to other hosts is orchestrator-scope (see checkpoint at Task 4).
  </read_first>
  <behavior>
    - Behavior: `grep -c 'Type C' /home/ubuntu/fleet/.claude/skills/id/coordinator-instructions.md` returns 0 (Type C header + every "Type C" reference in enumerations gone)
    - Behavior: `grep -c 'wakeup-scheduler-role\|role-scoped wake' /home/ubuntu/fleet/.claude/skills/id/coordinator-instructions.md` returns 0
    - Behavior: `grep -c 'fleet/roles/.*/wakeups' /home/ubuntu/fleet/.claude/skills/id/coordinator-instructions.md` returns 0 (no lingering per-role wake-up path references)
    - Behavior: the Type detection paragraph (line 55 pre-change: "a wake-up is Type C based on how it was delivered") is either removed or amended so it no longer enumerates a non-existent Type C — must be internally consistent
    - Behavior: the identity-scoped wake-up references (paths under `~/fleet/identities/<name>/wakeups/`) MAY remain if the surrounding prose is still correct after Type C is removed — those describe the per-session self-scheduling pattern the shape protects. Read each remaining wakeup mention in context; keep it if still accurate, remove or rewrite the sentence if it referenced role-general wake-ups in comparison
    - Behavior: `wc -l` on the file post-edit is strictly less than pre-edit (bytes were removed, not just reflowed) — a floor of ~30 lines removed based on the Type C block alone
    - Behavior: markdown structure remains valid — no orphan headers, no unclosed code fences, no dangling list bullets
  </behavior>
  <action>
    Edit /home/ubuntu/fleet/.claude/skills/id/coordinator-instructions.md (absolute path, NOT relative to skynet tree). Perform the following removals in one pass: (1) Remove the entire "### Type C: Scheduled wake-up (from your wake-up scheduler) → ROUTE-AND-DROP variant" section (approximately lines 117-138 based on pre-edit inventory). Include the blank line immediately after it if that leaves two consecutive blank lines. (2) At line ~22, in the sentence that reads "AND an extra role-scoped wake-up scheduler (so role-general wakes fire on you)": excise the "AND an extra role-scoped wake-up scheduler (so role-general wakes fire on you)" clause so the sentence remains grammatical (e.g. drop the trailing clause and any adjoining conjunction). (3) At line ~55 in the Type detection paragraph, remove the Type C enumeration ("a wake-up is Type C based on how it was delivered (wake-up scheduler...)") and any sentence that treats Type C as a live category; adjust adjacent enumeration numbering if necessary. (4) At lines ~486-499, remove the paragraph describing "an identity-scoped wake-up scheduler, AND an extra role-scoped wake-up scheduler against ~/fleet/roles/<role>/wakeups/" and the follow-on paragraph about constructing `python3 ~/.local/bin/wakeup-scheduler ~/fleet/roles/<role>` — trim wholly, leaving surrounding structure intact. (5) At lines ~400-402, revise any bullet or sentence that steers wake-up authoring toward `~/fleet/roles/<role>/wakeups/` — either remove the guidance entirely or rewrite it to point at the new global convention documented in SKILL.md § Global wake-ups (Task 1). After each edit, re-read the surrounding paragraph and confirm it is grammatical and internally consistent — do NOT leave dangling clauses ("...AND ..."), incorrect enumerations ("Types A/B/D"), or orphan list bullets. Preserve every reference to identity-scoped self-scheduling (`~/fleet/identities/<name>/wakeups/`) UNLESS it appears in a comparison with the removed per-role path — in that case rewrite the sentence to stand alone. Do NOT add any new content pointing to the global scope in this file — that documentation is centralized in SKILL.md (Task 1) and coordinator-instructions.md is fleet-side prose about the coordinator's dispatch flow, not the wake-up mechanism itself.
  </action>
  <verify>
    <automated>grep -c 'Type C' /home/ubuntu/fleet/.claude/skills/id/coordinator-instructions.md | grep -q '^0$' && grep -c 'wakeup-scheduler-role\|role-scoped wake' /home/ubuntu/fleet/.claude/skills/id/coordinator-instructions.md | grep -q '^0$' && grep -c 'fleet/roles/.*/wakeups' /home/ubuntu/fleet/.claude/skills/id/coordinator-instructions.md | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'Type C' /home/ubuntu/fleet/.claude/skills/id/coordinator-instructions.md` == 0
    - Source assertion: `grep -c 'wakeup-scheduler-role' /home/ubuntu/fleet/.claude/skills/id/coordinator-instructions.md` == 0
    - Source assertion: `grep -c 'role-scoped wake' /home/ubuntu/fleet/.claude/skills/id/coordinator-instructions.md` == 0
    - Source assertion: `grep -c 'fleet/roles/[^/]*/wakeups' /home/ubuntu/fleet/.claude/skills/id/coordinator-instructions.md` == 0
    - Source assertion: `grep -c '### Type ' /home/ubuntu/fleet/.claude/skills/id/coordinator-instructions.md` matches the remaining Type headers only (Types A, B, D — verify against pre-edit inventory in RESEARCH; expected count depends on which Types existed pre-edit — record the pre and post counts in the summary)
    - Source assertion: `wc -l /home/ubuntu/fleet/.claude/skills/id/coordinator-instructions.md | awk '{print $1}'` returns strictly less than 501 (pre-edit line count from Task 2 read)
    - Behavior: markdown structural sanity — every `###` header is followed by prose (no orphan headers); every code fence opened is closed; visual read of the modified regions confirms no dangling clauses or bad enumerations
    - Behavior: identity-scoped self-scheduling references remain accurate — grep for `identities/[^/]*/wakeups` should still return >= 1 (that mechanism is preserved per shape's protection)
  </acceptance_criteria>
  <done>The fleet-side coordinator-instructions.md file no longer contains Type C, per-role wake-up path references, or role-scoped wake-up scheduler prose; the file remains structurally valid; identity-scoped wake-up references are preserved where still accurate.</done>
</task>

<task type="auto">
  <name>Task 3: Migration audit — verify zero per-role specs on this host, document remote-host sweep protocol</name>
  <files>.planning/phases/127-wake-ups-redesign-phase-1-global-on-disk-specs-global-scope-/127-04-MIGRATION-AUDIT.md</files>
  <read_first>
    .planning/phases/127-wake-ups-redesign-phase-1-global-on-disk-specs-global-scope-/127-CONTEXT.md § Per-role sunset (D-14, D-15)
    .planning/phases/127-wake-ups-redesign-phase-1-global-on-disk-specs-global-scope-/127-RESEARCH.md §6 Migration Protocol (has the pre-verified count of zero specs on this host and the step-by-step protocol for any spec found)
  </read_first>
  <behavior>
    - Behavior: on this host, `find ~/fleet/roles -maxdepth 3 -type f -name '*.json' -path '*/wakeups/*'` returns zero results (already pre-verified in RESEARCH; re-confirm at task execution time)
    - Behavior: the audit file at .planning/phases/127-wake-ups-redesign-phase-1-global-on-disk-specs-global-scope-/127-04-MIGRATION-AUDIT.md records: (a) date of audit; (b) find command output for this host (empty); (c) `~/fleet/roles/*/wakeups/.state/` directories that exist (per-role scheduler pidfiles — they can stay, they will not be re-populated after ambient-monitor.py rolls); (d) the step-by-step protocol for the orchestrator to execute on OTHER managed hosts post-greenlight; (e) the D-15 escalation threshold (>~10 specs total across the fleet → swap hand-migration for a script)
    - Behavior: audit file is committed alongside the phase's other artifacts (no separate commit needed; belongs to the phase 127 planning tree)
  </behavior>
  <action>
    Run the audit commands on this host and capture their output: (a) `find ~/fleet/roles -maxdepth 3 -type f -name '*.json' -path '*/wakeups/*' 2>/dev/null` and record the result (expected: empty per RESEARCH §6). (b) `find ~/fleet/roles -maxdepth 3 -type d -name wakeups 2>/dev/null` to record which roles have the folder shape even if empty. (c) `find ~/fleet/roles -maxdepth 4 -type f -path '*/wakeups/.state/*' 2>/dev/null` to record any lingering per-role scheduler pidfiles/state (safe to leave; will not repopulate after ambient-monitor.py rolls per Plan 03). Then write .planning/phases/127-wake-ups-redesign-phase-1-global-on-disk-specs-global-scope-/127-04-MIGRATION-AUDIT.md as a markdown document with sections: (1) "This host — audit" showing the three command outputs with a summary line "N specs found (expected 0 per RESEARCH §6)". (2) "Remote hosts — sweep protocol" documenting the exact commands orchestrator must run on each managed host post-greenlight — mirror RESEARCH §6 step-by-step: for each spec found under `~/fleet/roles/*/wakeups/*.json`, read the schedule/instruction/name fields, derive a kebab-case slug (from spec.name or filename stem), write `~/fleet/wakeups/<slug>/wakeup.json` with `enabled` (from source or default true), `schedule` (verbatim), `prompt` (= source's `instruction`), `name` (verbatim if present), `roles: [<role-from-source-path>]`, then write `~/fleet/wakeups/<slug>/migrated-from.md` per PATTERNS.md § migrated-from.md shape (original path + timestamp + preserved fields), then delete the source spec. (3) "Escalation threshold (D-15)" documenting that if the fleet-wide count exceeds ~10 specs, the migration should be scripted rather than hand-executed — flag this decision to Ashley in the phase-close checkpoint (Task 4). (4) "Ownership boundary" noting that this executor scope only covers the local-host audit + this document; the actual remote-host sweep + edits are orchestrator scope per the phase-1-ownership-boundary rule (Ashley greenlights, orchestrator executes). Do NOT run the sweep against remote hosts; do NOT create any global wake-up specs; do NOT delete or edit any per-role spec even on this host (there are none, and even if there were, the migration protocol requires user greenlight per D-14).
  </action>
  <verify>
    <automated>test -f .planning/phases/127-wake-ups-redesign-phase-1-global-on-disk-specs-global-scope-/127-04-MIGRATION-AUDIT.md && grep -c 'This host — audit\|Remote hosts — sweep protocol\|Escalation threshold\|Ownership boundary' .planning/phases/127-wake-ups-redesign-phase-1-global-on-disk-specs-global-scope-/127-04-MIGRATION-AUDIT.md | awk '$1 >= 4' && test "$(find ~/fleet/roles -maxdepth 3 -type f -name '*.json' -path '*/wakeups/*' 2>/dev/null | wc -l)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `test -f .planning/phases/127-wake-ups-redesign-phase-1-global-on-disk-specs-global-scope-/127-04-MIGRATION-AUDIT.md` exits 0
    - CLI: `find ~/fleet/roles -maxdepth 3 -type f -name '*.json' -path '*/wakeups/*' 2>/dev/null | wc -l` returns 0 (this host has zero per-role specs to migrate)
    - Source assertion: `grep -c 'This host — audit' .planning/phases/127-wake-ups-redesign-phase-1-global-on-disk-specs-global-scope-/127-04-MIGRATION-AUDIT.md` == 1
    - Source assertion: `grep -c 'Remote hosts — sweep protocol' .planning/phases/127-wake-ups-redesign-phase-1-global-on-disk-specs-global-scope-/127-04-MIGRATION-AUDIT.md` == 1
    - Source assertion: `grep -c 'Escalation threshold' .planning/phases/127-wake-ups-redesign-phase-1-global-on-disk-specs-global-scope-/127-04-MIGRATION-AUDIT.md` == 1
    - Source assertion: `grep -c 'Ownership boundary' .planning/phases/127-wake-ups-redesign-phase-1-global-on-disk-specs-global-scope-/127-04-MIGRATION-AUDIT.md` == 1
    - Source assertion: the audit document names D-14 and D-15 at least once each (traceability)
    - Behavior: no files were created under `~/fleet/wakeups/` and no files under `~/fleet/roles/*/wakeups/` were deleted by this task (audit-only, no mutation)
  </acceptance_criteria>
  <done>Migration audit document exists at .planning/phases/127-wake-ups-redesign-phase-1-global-on-disk-specs-global-scope-/127-04-MIGRATION-AUDIT.md, records zero specs on this host, documents the remote-host sweep protocol for orchestrator execution, and names the D-15 escalation threshold.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Phase-close checkpoint — Ashley greenlights the deploy motion</name>
  <what-built>
    Phase 127 code + docs are complete and scoped-tests green. The executor has:
    (Plan 01) Extended the spawn-requests schema to accept `roles: []`, `skills: []`, `prompt: string` — all backend tests pass under `npx vitest run src/backend/spawn-requests/ src/backend/fleet-status/`.
    (Plan 02) Added `--mode global` to wakeup-scheduler.py — reads specs from `~/fleet/wakeups/<slug>/wakeup.json`, drops `~/fleet/spawn-requests/<uuid>.json` on fire, orphan-check bypassed. Substrate test at `substrate/scripts/tests/wakeup-scheduler.test.sh` passes 5/5.
    (Plan 03) Inserted global-scheduler spawn in agent-supervisor.sh main; removed per-role scheduler-spawn from ambient-monitor.py. Per-identity wakeup-scheduler spawn (lines 313-318) preserved verbatim.
    (Plan 04 Task 1) SKILL.md updated with Global wake-ups subsection alongside existing per-identity content.
    (Plan 04 Task 2) Fleet-side `/home/ubuntu/fleet/.claude/skills/id/coordinator-instructions.md` has Type C and all per-role wake-up references removed.
    (Plan 04 Task 3) Migration audit at `.planning/phases/.../127-04-MIGRATION-AUDIT.md` confirms zero per-role specs on this host; documents the remote-host sweep protocol.
  </what-built>
  <how-to-verify>
    1. Read the executor's phase summary artifacts:
       - `.planning/phases/127-wake-ups-redesign-phase-1-global-on-disk-specs-global-scope-/127-01-SUMMARY.md`
       - `.planning/phases/127-wake-ups-redesign-phase-1-global-on-disk-specs-global-scope-/127-02-SUMMARY.md`
       - `.planning/phases/127-wake-ups-redesign-phase-1-global-on-disk-specs-global-scope-/127-03-SUMMARY.md`
       - `.planning/phases/127-wake-ups-redesign-phase-1-global-on-disk-specs-global-scope-/127-04-SUMMARY.md`
       - `.planning/phases/127-wake-ups-redesign-phase-1-global-on-disk-specs-global-scope-/127-04-MIGRATION-AUDIT.md`
    2. Confirm the D-15 escalation call: how many per-role specs did the executor find on this host, and — if you have knowledge of other managed hosts — do you want a remote-host sweep performed BEFORE the deploy motion, or as part of it?
    3. Read (or spot-read) the coordinator-instructions.md diff on this host to confirm the Type C removal is grammatical and does not break unrelated flows.
    4. Approve the orchestrator to proceed with the deploy motion. Orchestrator will:
       (a) push through the standard fleet-substrate distributor sweep (D-18 — every catalog-managed file rides this channel), causing every managed host to receive new bytes for `substrate/scripts/wakeup-scheduler.py`, `substrate/scripts/agent-supervisor.sh`, `substrate/scripts/ambient-monitor.py`, and `substrate/skills/id/SKILL.md`;
       (b) `systemctl --user restart agent-supervisor.service` will fire automatically on each host per the catalog restartHook, bringing the global scheduler online;
       (c) if remote hosts have per-role specs (per Task 3's sweep protocol), migrate them by hand OR run the automation script per D-15 threshold;
       (d) directly edit `/home/ubuntu/fleet/.claude/skills/id/coordinator-instructions.md` on any OTHER managed hosts that hold coordinator identities (the file is not distributor-managed per RESEARCH §7).
    5. Sanity-check on this host after the deploy motion: `ps -ef | grep 'wakeup-scheduler.*--mode global'` should list the new global-scope process; `~/fleet/wakeups/global-scheduler.log` should exist and contain a startup line.
  </how-to-verify>
  <resume-signal>
    Type "approved — ship" to greenlight the deploy motion.
    Type "approved with notes: <notes>" to greenlight with specific handling requests (e.g. "handle remote-host sweep first, then push").
    Type "hold" and describe blockers to pause the phase-close.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executor scope → orchestrator scope | The executor makes changes to substrate-tree files (distributor pushes them) and to the local-host fleet-side coordinator-instructions.md; the actual multi-host deploy motion (systemctl restart on other hosts, remote-host coordinator-instructions.md edits, remote per-role spec sweep) is orchestrator-scope per the phase-1-ownership-boundary. The checkpoint at Task 4 is the hand-off. |
| Fleet-side coordinator-instructions.md → coordinator agents on other hosts | The file is NOT distributor-managed (retired 2026-09-20 per catalog.ts). Edits to the local copy do not propagate. Other managed hosts still have the pre-edit version until the orchestrator directly edits them. During the interim window between phase-1 deploy and the manual sweep, remote coordinators will still be documented (in their local copy) to route Type C wake-ups — but per-role wake-ups no longer fire (Plan 03 removed the spawn), so the code path is dead even if the doc still describes it. Non-fatal per RESEARCH § Pitfall 4 (opaque escalation, not a functional break). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-04-S | Spoofing | SKILL.md content authenticity on managed hosts | mitigate | SKILL.md is catalog-managed; the distributor sweep is the authenticated write path. Same threat posture as every other catalog-distributed file. |
| T-127-04-T | Tampering | An attacker editing the coordinator-instructions.md file to re-introduce a per-role wake-up path AFTER Plan 04's removal | accept | The file is fleet-side prose, not code. Even if an attacker restored the Type C prose, the underlying code path (ambient-monitor.py wakeup-scheduler-role spawn) is gone per Plan 03 — the doc would be lying, not exploitable. |
| T-127-04-R | Repudiation | Migration audit trail | mitigate | The audit document at 127-04-MIGRATION-AUDIT.md is a durable record of the executor's local-host find output and the protocol for orchestrator to follow. Attributability is preserved via git commit history. |
| T-127-04-I | Information Disclosure | Migration audit exposing role-folder contents | accept | Role folders under ~/fleet/roles are already visible to any user reading the fleet tree; the audit just captures a snapshot. No PII disclosure beyond what fleet-tree browsers already see. |
| T-127-04-D | Denial of Service | Coordinator receives a stale doc pointing at a dead code path (RESEARCH § Pitfall 4) | accept | Documented in RESEARCH as expected transient — the coordinator's opaque escalation surface is small (a per-role wake-up event that can never fire → coordinator escalates confusion to user). Orchestrator sweeps remote coordinator-instructions.md files post-greenlight per Task 4 checkpoint step (d). Not a functional break. |
| T-127-04-E | Elevation of Privilege | Fleet-side coordinator-instructions.md edit granting new authority | mitigate | The edit only removes content; no new capabilities are added. Authority profile of every coordinator identity is strictly reduced (one dead dispatch path removed). |
| T-127-04-SC | Tampering | npm/pip/cargo installs | mitigate | Zero external packages installed. Doc-only + audit-only work. |
</threat_model>

<verification>
- Task 1 verify command passes (SKILL.md updated correctly)
- Task 2 verify command passes (coordinator-instructions.md purged of per-role references)
- Task 3 verify command passes (migration audit exists, this host has zero specs)
- Task 4 checkpoint gate: Ashley resume signal received
</verification>

<success_criteria>
- SKILL.md § Scheduled wake-ups now covers both per-identity and global scope; per-identity content byte-identical to pre-change
- Fleet-side coordinator-instructions.md on this host has Type C and per-role wake-up references removed; file remains structurally valid
- Migration audit document at 127-04-MIGRATION-AUDIT.md records local-host state (zero specs) and documents the remote-host sweep protocol for orchestrator execution
- Phase-close checkpoint reached: Ashley reviewed the phase artifacts and greenlit the deploy motion (or paused with feedback)
- Threat model: no new authority added; every removal shrinks the attack surface; supply-chain surface unchanged
</success_criteria>

<output>
Create .planning/phases/127-wake-ups-redesign-phase-1-global-on-disk-specs-global-scope-/127-04-SUMMARY.md when done, listing:
- SKILL.md before/after line counts for the Scheduled wake-ups section
- coordinator-instructions.md before/after line counts + the exact grep-verified counts of the four purged patterns (Type C, wakeup-scheduler-role, role-scoped wake, fleet/roles/*/wakeups)
- Migration audit outcome for this host (zero specs) and any pidfile state observed under ~/fleet/roles/*/wakeups/.state/
- Ashley's resume-signal disposition (approved / approved with notes / hold) — the executor pauses execution at the checkpoint and records the response verbatim once received
- Deploy motion handoff notes for orchestrator: (a) distributor push; (b) remote-host coordinator-instructions.md edits; (c) remote per-role spec sweep per audit protocol; (d) sanity check on this host and one other for the global scheduler process + log
</output>
