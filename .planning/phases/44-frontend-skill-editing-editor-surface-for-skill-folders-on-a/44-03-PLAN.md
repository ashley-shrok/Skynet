---
phase: 44-frontend-skill-editing-editor-surface-for-skill-folders-on-a
plan: 03
type: execute
wave: 3
depends_on:
  - 44-01
  - 44-02
files_modified:
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - .planning/phases/44-frontend-skill-editing-editor-surface-for-skill-folders-on-a/44-UAT-CHECKLIST.md
  - .planning/phases/44-frontend-skill-editing-editor-surface-for-skill-folders-on-a/44-PATCH-DRAFT.md
autonomous: true
requirements: []
tags:
  - skynet-fork
  - skills-editor
  - integration
  - uat
  - pretty-conversations

must_haves:
  truths:
    - "The header MoreVertical menu in PrettyConversationsPanel has a fourth item labeled `Edit skills…` positioned immediately AFTER `Edit global files…` — this is the ONLY entry-point for the feature (D-01: sibling menu entry, not top-level surface)"
    - "Clicking `Edit skills…` in the menu opens the SkillsEditorModal (via `setSkillsEditorModalOpen(true)`); ESC or close X dismisses it — mirrors GlobalFilesModal open/close semantics"
    - "SkillsEditorModal is mounted as a portal sibling of GlobalFilesModal in the same panel tree — both consume `hostTree` from the AppShell useHostTree() thread; `defaultHostId` is `null` on the menu-trigger path (mirrors GlobalFilesModal mount)"
    - "The full frontend test suite exits 0: `npx vitest run` returns 0 exit code with no regression in adjacent tests"
    - "Both `44-UAT-CHECKLIST.md` and `44-PATCH-DRAFT.md` exist in the phase directory, ready for the orchestrator to hand to Ashley post-deploy"
  artifacts:
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "New useState for skillsEditorModalOpen, SkillsEditorModal mount alongside GlobalFilesModal, and menu item entry positioned after Edit global files…"
      contains: "skillsEditorModalOpen"
    - path: ".planning/phases/44-frontend-skill-editing-editor-surface-for-skill-folders-on-a/44-UAT-CHECKLIST.md"
      provides: "10+ user-verification steps walking D-01..D-16 for Ashley to walk post-deploy"
      contains: "Edit skills…"
      min_lines: 60
    - path: ".planning/phases/44-frontend-skill-editing-editor-surface-for-skill-folders-on-a/44-PATCH-DRAFT.md"
      provides: "skynet-patches.md entry draft (motivation, files touched, rebase risk) ready for orchestrator PIN"
      contains: "Phase 44"
      min_lines: 40
  key_links:
    - from: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      to: "src/ui/features/pretty-view/SkillsEditorModal.tsx"
      via: "imports SkillsEditorModal and mounts <SkillsEditorModal open={skillsEditorModalOpen} onOpenChange={setSkillsEditorModalOpen} hostTree={hostTree ?? null} defaultHostId={null} />"
      pattern: "SkillsEditorModal"
    - from: "PrettyConversationsPanel menu"
      to: "skillsEditorModalOpen state"
      via: "onClick handler in the menu items array: { label: \"Edit skills…\", onClick: () => setSkillsEditorModalOpen(true) }"
      pattern: "Edit skills"
---

<objective>
Wire the Wave 2 SkillsEditorModal into the PrettyConversationsPanel header menu as a sibling of the existing `Edit global files…` entry (per D-01), then generate the UAT checklist and skynet-patches.md draft the orchestrator will use to close the phase.

Purpose: complete the D-01 requirement — Ashley reaches skill editing from the same menu as global-files editing, one click below it — with the minimum viable diff to `PrettyConversationsPanel.tsx` (parallel state + parallel mount + parallel menu item). No new UI surface, no new prop thread, no keyboard shortcut. Feature is invisible until the menu is opened.

Output:
- One-line changes to `PrettyConversationsPanel.tsx` at three landmark positions (L60 import, L485 state, L1583+L1616 mount+menu)
- `44-UAT-CHECKLIST.md` — Nyquist-style step-by-step for Ashley to walk on production post-deploy, covering every D-XX decision
- `44-PATCH-DRAFT.md` — skynet-patches.md entry draft the orchestrator will paste at PIN time
- Full frontend test suite exits 0 (guards against accidental cross-file regression in the panel)

**Executor scope stops at code + commit + tests-green.** Deploy is orchestrator-managed (skynet-patches.md write, docker build, container recreate, HTTPS 200 verify, coord-room announces) per the box-maintainer role file. Do NOT run `git push`, `docker`, or any container command in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-frontend-skill-editing-editor-surface-for-skill-folders-on-a/44-CONTEXT.md
@.planning/phases/44-frontend-skill-editing-editor-surface-for-skill-folders-on-a/44-RESEARCH.md
@.planning/phases/44-frontend-skill-editing-editor-surface-for-skill-folders-on-a/44-PATTERNS.md
@.planning/phases/44-frontend-skill-editing-editor-surface-for-skill-folders-on-a/44-UI-SPEC.md
@.planning/phases/44-frontend-skill-editing-editor-surface-for-skill-folders-on-a/44-01-SUMMARY.md
@.planning/phases/44-frontend-skill-editing-editor-surface-for-skill-folders-on-a/44-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire SkillsEditorModal into PrettyConversationsPanel — three-point edit</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx</files>
  <read_first>
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx L55-75 (existing imports region, especially L60 `import GlobalFilesModal from "@/features/pretty-view/GlobalFilesModal"`)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx L480-495 (existing state declarations, especially L484-485 `// Phase 23 (GEFM-05): GlobalFilesModal open/closed toggle` + `const [globalFilesModalOpen, setGlobalFilesModalOpen] = useState(false)`)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx L1575-1595 (existing GlobalFilesModal mount at L1583-1588 with its 5-line comment header at L1578-1582)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx L1610-1625 (existing menu items array at L1613-1617, especially L1616 `{ label: "Edit global files…", onClick: () => setGlobalFilesModalOpen(true) }`)
    - src/ui/features/pretty-view/SkillsEditorModal.tsx (the Wave 2 component you're mounting — verify its default export name and props signature)
    - .planning/phases/44-frontend-skill-editing-editor-surface-for-skill-folders-on-a/44-UI-SPEC.md § Copywriting Contract § Menu entry (L108-116 — menu label locked to `Edit skills…` with trailing ellipsis matching Phase 23 pattern)
    - .planning/phases/44-frontend-skill-editing-editor-surface-for-skill-folders-on-a/44-PATTERNS.md § PrettyConversationsPanel.tsx (menu mount edit — the exact positions for import + state + mount + menu item)
    - .planning/phases/44-frontend-skill-editing-editor-surface-for-skill-folders-on-a/44-RESEARCH.md § The Menu Mount Site (L586-599 with verbatim wiring instructions) + § Pitfall 8 (menu-order regression guard)
  </read_first>
  <behavior>
    - PrettyConversationsPanel.tsx imports `SkillsEditorModal` from `@/features/pretty-view/SkillsEditorModal` immediately after the existing GlobalFilesModal import at L60.
    - Component body declares `const [skillsEditorModalOpen, setSkillsEditorModalOpen] = useState(false)` immediately after the existing `globalFilesModalOpen` state at L485, with a matching one-line phase-44 comment header.
    - JSX renders `<SkillsEditorModal open={skillsEditorModalOpen} onOpenChange={setSkillsEditorModalOpen} hostTree={hostTree ?? null} defaultHostId={null} />` immediately after the existing GlobalFilesModal mount at L1583-1588 (parallel sibling, same portal container).
    - Menu items array gains a fourth entry `{ label: "Edit skills…", onClick: () => setSkillsEditorModalOpen(true) }` positioned AFTER `Edit global files…` (UI-SPEC L112) — this is the ONLY invocation path.
    - A one-line comment `// KEEP ORDER: New agent → New role → Edit global files… → Edit skills…` is placed immediately above the menu items array to guard against alphabetization/refactor drift (RESEARCH.md § Pitfall 8).
    - All existing PrettyConversationsPanel behavior is preserved end-to-end — pretty-conversations rows, global-files menu entry, host tree threading, new-session dialog. Zero behavior changes beyond the four line additions.
  </behavior>
  <action>
    Four minimal edits to `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`. Do them all in this task; each is a 1-2 line surgical addition.

    1. **Import (add after L60)** — insert a new import line immediately after `import GlobalFilesModal from "@/features/pretty-view/GlobalFilesModal";`:
       - New line content: `import SkillsEditorModal from "@/features/pretty-view/SkillsEditorModal";`
       - Preserve the exact import path style (default import, `@/features/...` alias).

    2. **State declaration (add after L485)** — insert immediately after the existing `const [globalFilesModalOpen, setGlobalFilesModalOpen] = useState(false);`:
       - New two-line block:
         - Comment line: `// Phase 44 SKILLED-01: SkillsEditorModal open/closed toggle (opened from menu item, sibling of GlobalFilesModal).`
         - Code line: `const [skillsEditorModalOpen, setSkillsEditorModalOpen] = useState(false);`
       - Match the exact idiom of the surrounding useState declarations (no extra blank lines).

    3. **Modal mount (add after L1588)** — insert immediately after the existing `<GlobalFilesModal ... />` closing tag at L1588 (after the JSX self-close). Match GlobalFilesModal's prop shape exactly — hostTree and defaultHostId are the two known-good values:
       - Wrap in a matching `{/* Phase 44 SKILLED-01: SkillsEditorModal — portal-mounted sibling of GlobalFilesModal. Opened via the header menu's "Edit skills…" item. defaultHostId={null} deliberate — the panel-header trigger has no active-conversation context, so the modal falls through to its own host picker. */}` comment (mirror the 5-line comment header at L1578-1582 in shape).
       - New JSX element:
         - Component: `<SkillsEditorModal>`
         - Props: `open={skillsEditorModalOpen}`, `onOpenChange={setSkillsEditorModalOpen}`, `hostTree={hostTree ?? null}`, `defaultHostId={null}`
         - Self-close.

    4. **Menu item (add after L1616)** — locate the menu items array starting at L1613 with `{ label: "New agent", ... }`. The array currently has 3 items ending at L1616 with `{ label: "Edit global files…", onClick: () => setGlobalFilesModalOpen(true) },`. Two changes:
       - **Above the array opening bracket** (before the `[` on the line preceding L1613), add a single-line comment: `// KEEP ORDER: New agent → New role → Edit global files… → Edit skills…` (Pitfall 8 regression guard).
       - **After L1616** (after the "Edit global files…" line and its trailing comma), add a fourth item: `{ label: "Edit skills…", onClick: () => setSkillsEditorModalOpen(true) },`
       - Preserve indentation matching the surrounding array items.

    **CRITICAL — anti-patterns to reject:**
    - Do NOT swap the menu-item ordering under any circumstance. `Edit skills…` MUST come AFTER `Edit global files…` (UI-SPEC L112 locked, Pitfall 8 guard).
    - Do NOT add a keyboard shortcut for the menu entry (UI-SPEC L116 explicitly excludes it).
    - Do NOT thread `defaultHostId` from any active-conversation context — the panel header has no active session, so `defaultHostId={null}` is the correct value (matches GlobalFilesModal wiring L1588).
    - Do NOT add any icon to the menu item (UI-SPEC L116: "no icon, no keyboard shortcut hint, no beta badge").
    - Do NOT modify the mobile menu-item padding (`max-md:py-[18px] max-md:px-[14px]` at L1623) — the new item inherits automatically (RESEARCH.md § Menu Mount Site).

    Do NOT include fenced code blocks in this action — the existing wiring at L60, L485, L1583-1588, L1613-1617 is your byte-shape source and lives in your `<read_first>` list.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tiffany && grep -c "skillsEditorModalOpen\|SkillsEditorModal" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx && grep -c "Edit skills" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "error TS" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` contains `import SkillsEditorModal from "@/features/pretty-view/SkillsEditorModal";` (grep — exactly 1 hit)
    - File contains `const [skillsEditorModalOpen, setSkillsEditorModalOpen] = useState(false);` (grep — exactly 1 hit)
    - File contains `<SkillsEditorModal` opening JSX tag (grep — exactly 1 hit)
    - File contains `hostTree={hostTree ?? null}` inside the SkillsEditorModal mount (grep — the pattern must appear on the same JSX element as SkillsEditorModal)
    - File contains menu item `{ label: "Edit skills…", onClick: () => setSkillsEditorModalOpen(true) }` (grep for `"Edit skills…"` — exactly 1 hit)
    - Menu item ORDER: `Edit skills…` appears AFTER `Edit global files…` in the source. Verify with `grep -n "Edit skills\\|Edit global files" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` and confirm the line number of Edit global files < line number of Edit skills.
    - File contains the `KEEP ORDER: New agent → New role → Edit global files… → Edit skills…` guard comment (grep for `KEEP ORDER`)
    - `grep -c "SkillsEditorModal\\|skillsEditorModalOpen" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns >=5 (import + comment + state + JSX mount + open prop + onOpenChange prop + menu onClick)
    - `npx tsc --noEmit -p tsconfig.json` produces zero errors for the file
    - Diff size to PrettyConversationsPanel.tsx is minimal: `git diff --stat src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` shows fewer than 20 lines changed
  </acceptance_criteria>
  <done>Menu wiring lands with minimum diff, the entry point is available at the same menu as global-files, and TypeScript is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Full test suite green + UAT checklist + patch draft</name>
  <files>.planning/phases/44-frontend-skill-editing-editor-surface-for-skill-folders-on-a/44-UAT-CHECKLIST.md, .planning/phases/44-frontend-skill-editing-editor-surface-for-skill-folders-on-a/44-PATCH-DRAFT.md</files>
  <read_first>
    - .planning/phases/44-frontend-skill-editing-editor-surface-for-skill-folders-on-a/44-CONTEXT.md (D-01..D-16 — every decision needs a UAT step)
    - .planning/phases/44-frontend-skill-editing-editor-surface-for-skill-folders-on-a/44-UI-SPEC.md § Copywriting Contract, § Destructive actions (every label/copy string a human will see is here — the UAT walks these)
    - .planning/phases/44-frontend-skill-editing-editor-surface-for-skill-folders-on-a/44-01-SUMMARY.md (backend endpoint contracts + path-safety guarantees — patch-draft cites these)
    - .planning/phases/44-frontend-skill-editing-editor-surface-for-skill-folders-on-a/44-02-SUMMARY.md (frontend component tree + race-fix preservation — patch-draft cites these)
    - .planning/phases/44-frontend-skill-editing-editor-surface-for-skill-folders-on-a/44-RESEARCH.md § Pitfall 2 (patch #446 layer-enumeration reflex — patch-draft rebase-risk section cites this)
    - Recent patch entries in skynet-patches.md if accessible via `head -100 skynet-patches.md` — mirror the entry style, tone, and section headers exactly (motivation → files touched → deploy note → rebase risk)
  </read_first>
  <behavior>
    - Full frontend + backend Vitest suite exits 0 with no regression from Waves 1-2.
    - `.planning/phases/44-frontend-skill-editing-editor-surface-for-skill-folders-on-a/44-UAT-CHECKLIST.md` contains 10+ numbered steps walking every D-XX decision from CONTEXT.md; each step has explicit "Action" + "Expected" + "Pass/Fail" sub-fields; steps are ordered by the fast-path Ashley cares about (menu → host → skill → tab → edit → save → add file → delete file → delete skill → refresh + verify).
    - `.planning/phases/44-frontend-skill-editing-editor-surface-for-skill-folders-on-a/44-PATCH-DRAFT.md` is a skynet-patches.md entry draft with sections: `Motivation`, `What shipped` (bullet list of the 6 new files + 5 modified files), `Backend surface` (7 endpoints), `Frontend surface` (5 components), `Path-safety design` (regex + prefix assertion + shellEscape), `Nginx blocks` (both configs), `Rebase risk` (LOW — additive backend + additive frontend), `Deploy note` (mandatory 15-min deadman timer, mirrors Phase 23 shipping posture).
  </behavior>
  <action>
    Two artifacts to write, plus one verification run. Do the verification first — if it fails, fix before writing the docs.

    **Step 1: Run the full Vitest suite** — `cd /home/ubuntu/skynet-tiffany && npx vitest run 2>&1 | tail -40`. If any test fails, either it's an unrelated regression (investigate + fix in a follow-up commit if <5 minutes; otherwise report as blocker) or it's a Phase 44 test that regressed against Wave 1/2 (fix in place). Do NOT proceed to Step 2 until exit code is 0.

    **Step 2: Write `.planning/phases/44-frontend-skill-editing-editor-surface-for-skill-folders-on-a/44-UAT-CHECKLIST.md`** as a Markdown file. Structure:

    - Top matter: title `# Phase 44 UAT Checklist — Frontend Skill Editing`, subtitle referencing D-01..D-16 coverage
    - Preamble paragraph: "Walk these steps on production Skynet (https://gigaashley.click or https://skynet-ec2.ashleycook.com) after Ashley's build+deploy. Sign off each step with a checkbox + timestamp. Any Pass/Fail=Fail routes to the phase's next planning cycle."
    - 10+ numbered steps as sections `## Step 1: ...` through `## Step N: ...`, each containing:
      - `**D-XX:**` reference line naming which CONTEXT decision(s) this step verifies
      - `**Action:**` explicit user action ("Open Skynet in a browser. Click the ⋮ menu at the top-right of the conversation list. Confirm the four-item menu shows `New agent` / `New role` / `Edit global files…` / `Edit skills…` in that order.")
      - `**Expected:**` explicit expected result ("The menu contains exactly four items. `Edit skills…` sits below `Edit global files…`. Menu closes on outside click.")
      - `**Pass/Fail:** ☐ Pass ☐ Fail — Notes:`

    Cover these 10+ verifications at minimum:
    1. Menu entry present + positioned after `Edit global files…` (D-01)
    2. Modal opens with host `<select>` visible + placeholder `Pick a host…` (D-02)
    3. Picking a host reveals the skill `<select>` with placeholder `Pick a skill…` (D-03)
    4. Picking a skill populates the bottom tab bar with files (including subfolder files as flat tabs with path-relative labels like `tests/basic.py`) (D-04, D-05)
    5. Skill with many files (test on a skill with 15+ files if fleet has one — the `build` skill on thenasty may qualify) shows horizontal-scroll tab bar (D-06)
    6. Opening a text tab shows the same editor pane as global-files; Save button disabled until edit; Save persists (D-07)
    7. Opening a non-text tab (create a test one via `dd if=/dev/urandom of=~/.claude/skills/<skill>/binary.bin bs=1 count=64` on the host beforehand) shows the AlertTriangle placeholder `Not a text file` + body `This file isn't text and can't be edited here.` (D-08)
    8. `+ Add file` prompts for a filename, creates it, and auto-selects the new tab; duplicate name is rejected with an error (D-09)
    9. Delete-file trigger (Trash2 next to Save) opens a modal-in-modal confirm dialog with `Delete file?` heading + `<skill>/<path>` in monospace + `This can't be undone.`; Cancel closes; Delete removes the tab (D-10)
    10. Delete-skill trigger (Trash2 in header) opens a modal-in-modal confirm dialog with `Delete skill?` heading + `<skill>` in monospace + `This removes the skill folder and every file inside it. This can't be undone.`; Delete removes the skill from the dropdown (D-11)
    11. Menu item does NOT show a keyboard shortcut hint, an icon, or a beta badge (D-01 + UI-SPEC L116)
    12. Existing `Edit global files…` menu entry continues to work unchanged (regression guard for Wave 3 wiring)
    13. Path safety: attempt to send a crafted request via DevTools with `skill: "../etc"` — backend returns 400, no file is read/written/deleted (D-16 + T-44-02 defense verification)
    14. ESC closes the modal; clicking outside does NOT close it (mirrors GlobalFilesModal `onInteractOutside preventDefault`)

    **Step 3: Write `.planning/phases/44-frontend-skill-editing-editor-surface-for-skill-folders-on-a/44-PATCH-DRAFT.md`** as a Markdown file ready to paste into `skynet-patches.md` at PIN time. Mirror the tone and section headers of recent patch entries. Structure:

    - Top matter: title `# Patch #TBD — Phase 44: Frontend skill editing (editor surface for skill folders on a host)`, dated 2026-08-18 (planning date; orchestrator adjusts to ship date)
    - `## Motivation` — one paragraph: Ashley asked for a fast-path to make quick edits to a skill's files on any managed host without leaving the frontend, mirroring the Phase 23 global-files editor surface Ashley already uses. The trigger is fast-adjustment work: three selections max (menu → host → skill) then a tab click.
    - `## What shipped` — bullet list:
      - Backend: 7 new endpoints under `/skills-editor/*` (list-skills, list-files, read, write with mtime-conflict 409, create with duplicate-409, delete-file, delete-skill)
      - Frontend: 6 new files (skills-api.ts, SkillsEditorModal.tsx, SkillsEditorModal.test.tsx, SkillFileTab.tsx, SkillFileTab.test.tsx, DeleteConfirmDialog.tsx) + 1 modified panel (PrettyConversationsPanel.tsx menu wiring)
      - Nginx: parallel `location ~ ^/skills-editor(/.*)?$` blocks in both `docker/nginx.conf` and `docker/nginx-https.conf` (patch #446 reflex)
    - `## Backend surface` — cite the 7 endpoints with method + path + response shape, one line each
    - `## Frontend surface` — cite the 5 components with role, one line each (skills-api.ts as helpers; SkillsEditorModal + SkillFileTab + DeleteConfirmDialog as UI; PrettyConversationsPanel as mount site)
    - `## Path-safety design` — one paragraph describing the four-layer defense: (1) `SKILL_NAME_RE` regex gate on skill name, (2) `isSafeRelativePath` regex gate on file path, (3) post-compose `absPath.startsWith(skillRoot + "/")` prefix assertion, (4) `shellEscape` single-quote wrapping before any shell interpolation. Note that DELETE /skill's `rm -rf` is life-critical and has dedicated test coverage.
    - `## Files touched` — list every path with lines added/removed if git diff gives them, or just paths otherwise
    - `## Deploy note` — mandatory 15-minute deadman timer per DEPLOY DISCIPLINE; verify HTTPS 200 on `/skills-editor/skills?hostId=<n>` before timer cancels
    - `## Rebase risk` — LOW: additive backend router + additive frontend component tree + parallel nginx blocks + reuse of shipped Phase 23 patterns; zero upstream Skynet surfaces disturbed
    - `## Bounty tracker` — reference the `~/.claude/roles/box-maintainer/bounties/frontend-skill-editing/` bounty (if orchestrator has one — leave placeholder `~/.claude/roles/box-maintainer/bounties/frontend-skill-editing/` and let orchestrator fill in)
    - `## Related` — reference: Phase 23 (`/global-files` editor — the direct precedent), patch #446 (layer-enumeration reflex), quick-260805-7rq (useEffect race — preserved verbatim in Phase 44's lazy-load effect)
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tiffany && npx vitest run 2>&1 | tail -20 && ls -la .planning/phases/44-frontend-skill-editing-editor-surface-for-skill-folders-on-a/44-UAT-CHECKLIST.md .planning/phases/44-frontend-skill-editing-editor-surface-for-skill-folders-on-a/44-PATCH-DRAFT.md 2>&1 | head</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 (full test suite)
    - `.planning/phases/44-frontend-skill-editing-editor-surface-for-skill-folders-on-a/44-UAT-CHECKLIST.md` exists and is >=60 lines
    - UAT checklist contains >=14 `## Step` sections (14 verifications above)
    - Every UAT step has `**D-` reference (grep for `**D-` returns >=14)
    - Every UAT step has `**Action:**`, `**Expected:**`, `**Pass/Fail:**` sub-fields (grep for `Pass/Fail:` returns >=14)
    - `.planning/phases/44-frontend-skill-editing-editor-surface-for-skill-folders-on-a/44-PATCH-DRAFT.md` exists and is >=40 lines
    - Patch draft contains sections: `## Motivation`, `## What shipped`, `## Backend surface`, `## Frontend surface`, `## Path-safety design`, `## Deploy note`, `## Rebase risk` (grep for each header — all must be present)
    - Patch draft mentions `patch #446` in the rebase risk or shipped section (documenting the layer-enumeration reflex)
    - Patch draft has NO placeholder text like "TODO" or "FIXME" (grep — must return 0)
  </acceptance_criteria>
  <done>Full test suite green, UAT checklist ready for Ashley, and skynet-patches.md draft ready for orchestrator PIN. Executor's remit is complete; deploy is orchestrator's next motion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Plan 03 does not introduce a new trust boundary. It wires an existing component into the panel — every runtime boundary was analyzed and mitigated in Plans 01 and 02. See:
- Plan 01 threat model: browser → nginx → Express → SSH exec/SFTP boundaries (T-44-01 through T-44-10)
- Plan 02 threat model: browser DOM → SkillsEditorModal component state → axios boundaries (T-44-11 through T-44-15)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-16 | Tampering | PrettyConversationsPanel menu items array — refactor accidentally reorders or drops the `Edit skills…` entry, breaking D-01 fast-path | mitigate | Task 1 adds a `// KEEP ORDER: New agent → New role → Edit global files… → Edit skills…` guard comment (RESEARCH.md § Pitfall 8). UAT Step 1 verifies the menu ordering post-deploy. Acceptance criterion asserts the source-line ordering (grep -n confirms `Edit global files…` line number < `Edit skills…` line number). |
| T-44-17 | Denial of service | Panel state accidentally re-renders modal on every parent update because `hostTree` reference changes | accept | GlobalFilesModal has the same threading; it works in production (Phase 23 shipped 2026-08-05, no re-render bug reports). Same posture inherited. `hostTree` in AppShell is `useState<HostFolder | null>` — reference stable across parent renders unless hosts refetch. |
| T-44-18 | Information disclosure | UAT checklist or patch draft accidentally embeds fleet credentials, host names, or user secrets | mitigate | Task 2 explicitly uses placeholder names (`<skill>`, `<n>`, `~/.claude/skills/`) and cites generic host examples (thenasty as a public host name, gigaashley.click as a public URL). No credential fragments, no tailnet IPs beyond the public domain names. |
</threat_model>

<verification>
- Task 1 verify: PrettyConversationsPanel.tsx contains all four edits (import, state, mount, menu item with KEEP ORDER comment); `npx tsc --noEmit` clean
- Task 2 verify: `npx vitest run` exits 0; both markdown files exist with all required sections
- Combined `<automated>` gate for the plan: `cd /home/ubuntu/skynet-tiffany && npx vitest run 2>&1 | tail -5 && grep -c "SkillsEditorModal\|Edit skills" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`
</verification>

<success_criteria>
- PrettyConversationsPanel wires the modal with minimum diff (import + state + mount + menu item)
- Menu item ordering guarded by KEEP ORDER comment (Pitfall 8 defense)
- Full frontend test suite exits 0
- UAT checklist covers every D-XX decision with Action/Expected/Pass-Fail structure
- Patch draft has motivation/shipped/backend/frontend/path-safety/deploy/rebase-risk sections ready for orchestrator PIN
- No deploy commands run in this plan (executor scope ends at code + tests-green)
</success_criteria>

<output>
Create `.planning/phases/44-frontend-skill-editing-editor-surface-for-skill-folders-on-a/44-03-SUMMARY.md` when done, following the template. Include:
- The four edit points in PrettyConversationsPanel.tsx with line numbers as landed
- Confirmation that all Waves 1-2 tests still pass alongside the Wave 3 wiring
- Pointer to `44-UAT-CHECKLIST.md` and `44-PATCH-DRAFT.md` for orchestrator handoff
- Files touched with sizes
- Deploy discipline reminder: orchestrator owns skynet-patches.md write + docker build + container recreate + HTTPS 200 verify + coord-room announces + 15-min deadman
</output>
</content>
