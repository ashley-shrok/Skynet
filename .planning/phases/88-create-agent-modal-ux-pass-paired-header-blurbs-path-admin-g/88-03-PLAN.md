---
phase: 88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g
plan: 03
type: execute
wave: 3
depends_on: [88-01, 88-02]
files_modified:
  - src/ui/sidebar/CreateRoleDialog.test.tsx
  - src/ui/sidebar/NewSessionDialog.test.tsx
  - src/ui/sidebar/NewSessionDialog.chain.test.tsx
  - src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx
  - src/ui/sidebar/NewSessionDialog.task-input.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "CreateRoleDialog.test.tsx Test 11 blurb regex is updated to match the new two-sentence text (per PATTERNS.md §7)"
    - "Every renderDialog helper across the four NewSessionDialog test files accepts an `isAdmin?: boolean` override and DEFAULTS TO `isAdmin: true` so existing test coverage (which was written when the Path + checkbox surface was universal) continues to exercise those admin-only affordances without churn (per PATTERNS.md §5 change 1 + §6 landmine)"
    - "Every existing occurrence of the checkbox-label regex `/create with new identity/i` across the four NewSessionDialog test files is replaced with `/just a shell.*no agent/i` — factored into a shared const `IDENTITY_MODE_CHECKBOX_RE` at the top of NewSessionDialog.test.tsx to avoid drift (per PATTERNS.md §5 landmine)"
    - "Test D (identity-mode checkbox default) at NewSessionDialog.test.tsx L620-626 is rewritten: default is now UNCHECKED with the new label, exercised under `isAdmin: true` (per PATTERNS.md §5 change 2)"
    - "Every test that previously CLICKED the checkbox to toggle identity-mode OFF (reach the raw-shell branch) is rewritten to still click the checkbox — but now the click OPTS INTO shell mode (semantic-inverted intent per PATTERNS.md §5 change 3)"
    - "Every test that previously ASSUMED identity-mode was default ON (i.e. no checkbox interaction needed to reach the agent-mode path) is rewritten to acknowledge that agent-mode is STILL the default (post-Phase-88 default is agent-mode-on / shell-off); those tests need NO checkbox click but their describe/it strings and any inline assertion of `identityMode: true` state need updating"
    - "Three new tests are added to NewSessionDialog.test.tsx: (a) `renderDialog({ isAdmin: false })` renders NO Path input and NO shell-only checkbox visible; (b) `renderDialog({ isAdmin: true })` renders BOTH the Path input (default value `~/`) and the shell-only checkbox (default UNCHECKED); (c) submit path with `isAdmin: false` calls openBirthStream with `path: ''` — the empty-string that triggers the Plan 88-01 backend `~/<name>/` substitution"
    - "The task-input.test.tsx test at L246-256 (Task 1b: task textarea absent when identityMode toggled off) is rewritten to reflect that clicking the checkbox now OPTS INTO shell mode (which is the branch where the task textarea is absent); test intent is preserved but semantic inversion is applied"
    - "chain.test.tsx tests at L505-518 (identity-mode OFF hides brief field — brief was already stripped in Phase 86 so this test may be already-passing / already-defunct; audit and rewrite label regex + click semantics) and L525-553 (Test 9: initialRole ignored when identity-mode OFF — semantic inversion applied to reach shell mode)"
    - "role-dropdown.test.tsx renderDialog helper at L125 is updated to accept and forward `isAdmin` (defaulting to `true`); if no test in this file exercises the checkbox interaction directly, no other change is required beyond the helper update"
    - "All five modified test files pass under `npx vitest run <file> --run` at the end of this plan (Wave 2's expected-failure state resolves to green)"
  artifacts:
    - path: "src/ui/sidebar/CreateRoleDialog.test.tsx"
      provides: "Blurb regex assertion updated to match Task 1's new two-sentence text"
      contains: "Roles are the expertise your agents adopt"
    - path: "src/ui/sidebar/NewSessionDialog.test.tsx"
      provides: "renderDialog helper accepts isAdmin (default true); label regex constant IDENTITY_MODE_CHECKBOX_RE; Test D rewritten for default-unchecked; three new tests for isAdmin=false hides both, isAdmin=true renders both, and non-admin submit sends empty-string path"
      contains: "IDENTITY_MODE_CHECKBOX_RE"
    - path: "src/ui/sidebar/NewSessionDialog.chain.test.tsx"
      provides: "renderDialog helper accepts isAdmin (default true); label regex updated; identity-mode click semantics inverted"
      contains: "just a shell"
    - path: "src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx"
      provides: "renderDialog helper accepts isAdmin (default true)"
      contains: "isAdmin"
    - path: "src/ui/sidebar/NewSessionDialog.task-input.test.tsx"
      provides: "renderDialog helper accepts isAdmin (default true); label regex updated; Task 1b + Task 3c-3e describe/it strings and click semantics updated for semantic inversion"
      contains: "just a shell"
  key_links:
    - from: "renderDialog helper in each test file"
      to: "NewSessionDialog isAdmin prop (Plan 88-01)"
      via: "helper accepts optional isAdmin override, defaults to `true`, forwards to <NewSessionDialog isAdmin={...} />"
      pattern: "isAdmin\\?"
    - from: "Existing checkbox-interaction tests"
      to: "New label 'Just a shell — no agent'"
      via: "regex constant IDENTITY_MODE_CHECKBOX_RE = /just a shell.*no agent/i replaces all 20+ /create with new identity/i occurrences across the four test files"
      pattern: "IDENTITY_MODE_CHECKBOX_RE"
    - from: "New non-admin-hides-both test in NewSessionDialog.test.tsx"
      to: "Plan 88-02 admin-gate JSX"
      via: "render with isAdmin: false, assert queryByLabelText(/^path$/i) is null AND queryByRole('checkbox', { name: IDENTITY_MODE_CHECKBOX_RE }) is null"
      pattern: "isAdmin: false"
    - from: "New submit-with-empty-path test in NewSessionDialog.test.tsx"
      to: "Plan 88-02 Edit F handleBirth path-clear"
      via: "mock openBirthStream, render with isAdmin: false, complete the birth form, click Create, assert the openBirthStream call received path: '' (the empty-string that triggers the Plan 88-01 backend substitution)"
      pattern: "path: \"\""
---

<objective>
Wave-3 restore green: rewrite the test suites broken by Plan 88-02's rename + label flip + default flip + blurb revision. Five test files touched, no source files changed. Adds three new tests to lock the isAdmin admin-gate + non-admin backend substitution behaviors.

Two structural changes, applied across the four NewSessionDialog test files:

1. **renderDialog helper accepts `isAdmin?: boolean` and defaults to `true`.** This is the least-churn migration — existing tests were written when Path field + checkbox were universal; keeping `isAdmin: true` as the helper's default preserves their assumptions. Tests that specifically exercise the non-admin path pass `isAdmin: false` explicitly.

2. **Shared constant `IDENTITY_MODE_CHECKBOX_RE = /just a shell.*no agent/i` at the top of each file.** All existing `/create with new identity/i` regex occurrences replaced with a reference to this constant, so a future label change requires editing one place, not 20+ places.

Semantic inversions:
- **Click-to-shell** (was: click-to-shell-when-default-was-agent inverted through the "opt into identity mode" checkbox → after Phase 88, click-to-shell directly): rewrite affected tests to reflect that clicking the checkbox now opts INTO shell mode. Tests that previously clicked-to-toggle-off-identity-mode-to-reach-regular-session mostly keep the click (still reaches shell mode after Phase 88) but the surrounding describe/it strings + comment prose need to reflect the new intent.
- **Agent-mode is default** (was: identity-mode toggled ON for identity-cluster tests — Phase 88 makes identity-mode / agent-mode the default so no toggle needed): rewrite affected tests to drop the "click to enable identity mode" step, since agent-mode is now default-on (i.e. `shellOnly === false` is the new default).

Three new tests added to NewSessionDialog.test.tsx:
- **`isAdmin: false` hides both.** No Path input, no shell-only checkbox in the render.
- **`isAdmin: true` renders both.** Path input default value `~/`, shell-only checkbox default UNCHECKED.
- **`isAdmin: false` submit sends empty-string path.** Mock openBirthStream, complete the birth form under non-admin, click Create, assert the openBirthStream call's `path` field is exactly `""` (the empty-string that Plan 88-01's backend narrow substitutes to `~/<name>/`).

Output:
- CreateRoleDialog.test.tsx Test 11 blurb regex updated
- NewSessionDialog.test.tsx: renderDialog helper + label constant + Test D rewrite + all label regex sites + three new tests
- NewSessionDialog.chain.test.tsx: renderDialog helper + label regex sites + Test 9 semantic-inversion
- NewSessionDialog.role-dropdown.test.tsx: renderDialog helper + audit for identityMode-default assumptions
- NewSessionDialog.task-input.test.tsx: renderDialog helper + label regex sites + Task 1a/1b/3c-3e describe/it string updates
- All five scoped test files pass under `npx vitest run` at plan completion
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-CONTEXT.md
@.planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-PATTERNS.md
@.planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-01-PLAN.md
@.planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-02-PLAN.md
@src/ui/sidebar/CreateRoleDialog.test.tsx
@src/ui/sidebar/NewSessionDialog.test.tsx
@src/ui/sidebar/NewSessionDialog.chain.test.tsx
@src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx
@src/ui/sidebar/NewSessionDialog.task-input.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CreateRoleDialog.test.tsx Test 11 blurb regex + phase-comment refresh</name>
  <files>src/ui/sidebar/CreateRoleDialog.test.tsx</files>
  <read_first>
    - src/ui/sidebar/CreateRoleDialog.test.tsx L285-313 — Test 11 which asserts the blurb text at L297-304 (old text) and asserts the required-caption is absent at L306-312 (unchanged; caption stays deleted); the Phase-84 rationale comment at L297-299 needs a Phase-88 refresh
    - .planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-02-PLAN.md Task 1 §THE EDIT — the byte-exact new blurb text
    - .planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-PATTERNS.md §7 — the exact replacement regex with em-dash-free double-period escaping
  </read_first>
  <action>
    Single-region edit inside Test 11 at CreateRoleDialog.test.tsx L285-313. Replace the blurb regex assertion at L300-304; refresh the preceding Phase-84 comment at L297-299 to name Phase 88 as the source of the new text.

    ─────────────────────────────────────────────────────────────
    THE EDIT
    ─────────────────────────────────────────────────────────────

    Change the assertion block at L300-304 from:

      expect(
        screen.getByText(
          /A role is what an agent does and how it thinks — many agents can share one\./,
        ),
      ).toBeTruthy();

    To (both periods `\.`-escaped per PATTERNS.md §7 landmine — the new blurb has one mid-sentence period after "adopt" and one terminal period after "knowledge"):

      expect(
        screen.getByText(
          /Roles are the expertise your agents adopt\. Every agent using this role inherits its goals, rules, and knowledge\./,
        ),
      ).toBeTruthy();

    Refresh the preceding Phase-84 rationale comment at L297-299 to reference Phase 88 as well — either replace the two-line Phase-84 comment or append a fresh Phase-88 line acknowledging the revision (per PATTERNS.md §7 landmine: "Update the surrounding Phase 84 comment").

    Suggested Phase-88 comment replacement (chose your preferred verbatim byte-count):

      // Phase 84 (Plan 84-01 CHANGE F.1): the header blurb renders below
      // the title, above the fields. Phase 88 (Plan 88-02 Task 1): revised
      // to a two-sentence paired-vocabulary form — shared verb "adopt" pairs
      // with the sibling create-agent blurb at NewSessionDialog.tsx L781-783.
      // Exact string from 88-CONTEXT.md §Verbatim copy Role blurb (LOCKED by
      // Alice greenlight 2026-09-07).

    Preserve the required-caption absence assertion at L306-312 verbatim — that assertion was and remains valid (the caption was deleted in Phase 84 and stays deleted in Phase 88).

    Preserve every other test in this file byte-identical. Tests 12, 13, 14, etc. are orthogonal to Phase 88.

    ─────────────────────────────────────────────────────────────
    ANTI-PATTERNS TO AVOID
    ─────────────────────────────────────────────────────────────

    - Do NOT use `screen.getByText(new RegExp(...))` — the existing regex-literal form is fine and matches file conventions.
    - Do NOT match the blurb by substring only (e.g. `/Roles are the expertise/`) — assert the full text so a partial-revert or accidental paraphrase is caught.
    - Do NOT forget to escape both periods with `\.` — the RegExp engine treats an unescaped `.` as "any character", which would still match but would silently accept a paraphrase like "Roles are the expertise your agents adopt- Every agent…".
    - Do NOT touch the checkbox-absence assertion at L290-295 (the /then create an agent with this role/i regex) — that's Phase 84 heritage and remains valid; the checkbox stays deleted.
  </action>
  <acceptance_criteria>
    Grep source assertions on `src/ui/sidebar/CreateRoleDialog.test.tsx`:
    - `grep -c 'Roles are the expertise your agents adopt' src/ui/sidebar/CreateRoleDialog.test.tsx` returns 1 (new blurb assertion)
    - `grep -c 'Every agent using this role inherits its goals, rules, and knowledge' src/ui/sidebar/CreateRoleDialog.test.tsx` returns 1 (new blurb closing clause)
    - `grep -c 'A role is what an agent does and how it thinks' src/ui/sidebar/CreateRoleDialog.test.tsx` returns 0 (old regex gone)
    - `grep -c 'Phase 88' src/ui/sidebar/CreateRoleDialog.test.tsx` returns ≥ 1

    Test-command assertion:
    - `npx vitest run src/ui/sidebar/CreateRoleDialog.test.tsx --run` exits 0 (all tests pass, including the revised Test 11)
  </acceptance_criteria>
  <verify>
    <automated>set -e; grep -q 'Roles are the expertise your agents adopt' src/ui/sidebar/CreateRoleDialog.test.tsx && grep -q 'Every agent using this role inherits its goals, rules, and knowledge' src/ui/sidebar/CreateRoleDialog.test.tsx && [ "$(grep -c 'A role is what an agent does and how it thinks' src/ui/sidebar/CreateRoleDialog.test.tsx)" = "0" ] && grep -q 'Phase 88' src/ui/sidebar/CreateRoleDialog.test.tsx && npx vitest run src/ui/sidebar/CreateRoleDialog.test.tsx --run</automated>
  </verify>
  <done>
    - Test 11's blurb regex updated to new two-sentence form with both periods escaped
    - Phase-88 comment added or Phase-84 comment refreshed to reference the sibling shipped copy
    - Other tests in the file untouched
    - `npx vitest run src/ui/sidebar/CreateRoleDialog.test.tsx --run` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite NewSessionDialog.test.tsx — renderDialog helper + label regex constant + Test D rewrite + all label regex sites + three new admin-gate/backend-substitution tests</name>
  <files>src/ui/sidebar/NewSessionDialog.test.tsx</files>
  <read_first>
    - src/ui/sidebar/NewSessionDialog.test.tsx (entire ~1250-line file — substantial refactor across many test blocks; specifically L1-80 for imports + top-of-file constants where the new IDENTITY_MODE_CHECKBOX_RE will land; L542-559 for renderDialog helper (needs isAdmin override + `= true` default); L575-576 + all other `create with new identity` sites for regex-replacement — see PATTERNS.md §5 enumeration L286, L327, L358, L575-576, L601-602, L623, L638-639, L801-806, L811-815, L848-849, L920-936; L620-626 for Test D (identity-mode defaults ON — rewrite to defaults UNCHECKED under isAdmin=true); L840-870 for Test S (identity-mode OFF → onCreate with identityMode:false — audit for semantic inversion); L920-990 for the identity-form fill helper `fillIdentityForm` at L923+ if it clicks the checkbox anywhere)
    - .planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-PATTERNS.md §5 (the full section with the four changes required to this file + the two landmines)
    - .planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-02-PLAN.md Edit D rename mapping — the LOCAL state var is now `shellOnly` (default false) with the boolean-invert applied; the PUBLIC payload discriminant `identityMode` stays; tests should assert on the PUBLIC discriminant (`arg.identityMode`) not on the internal state name
    - .planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-01-PLAN.md Task 2 — the backend narrow that substitutes `~/<name>/` when body.path is empty; the new "isAdmin: false submit sends empty-string path" test asserts on `path: ""` in the openBirthStream call (per PATTERNS.md §5 change 4 subitem 3)
    - src/ui/sidebar/NewSessionDialog.tsx — the source file this test suite exercises; specifically read the `shellOnly` state hook (was `identityMode`) at L326-ish and the Path admin-gate + checkbox admin-gate at L926-960-ish so tests are written against the actual runtime shape
  </read_first>
  <action>
    Multi-region edit inside NewSessionDialog.test.tsx. Break into five sub-edits, applied top-to-bottom of file.

    ─────────────────────────────────────────────────────────────
    EDIT A — Add IDENTITY_MODE_CHECKBOX_RE constant near top of file
    ─────────────────────────────────────────────────────────────

    Immediately after the existing imports (approximate location L1-80, place it before the first `describe(` block or after any other top-level constants), add:

      // Phase 88: single source of truth for the identity-mode checkbox label
      // regex. The old label "Create with new identity" was flipped to
      // "Just a shell — no agent" (Plan 88-02 Edit C) so all references to
      // getByRole("checkbox", { name: /create with new identity/i }) below
      // now use IDENTITY_MODE_CHECKBOX_RE. Refactor prevents label-drift
      // regressions across the 20+ occurrence sites.
      const IDENTITY_MODE_CHECKBOX_RE = /just a shell.*no agent/i;

    ─────────────────────────────────────────────────────────────
    EDIT B — Update renderDialog helper at L542-559 to accept isAdmin
    ─────────────────────────────────────────────────────────────

    Current shape at L542-559:

      function renderDialog(overrides: {
        open?: boolean;
        onClose?: () => void;
        onCreate?: ReturnType<typeof vi.fn>;
        hostTree?: typeof threeHostTree;
      } = {}) {
        const onCreate = overrides.onCreate ?? vi.fn();
        const onClose = overrides.onClose ?? vi.fn();
        const result = render(
          <NewSessionDialog
            open={overrides.open ?? true}
            onClose={onClose}
            hostTree={overrides.hostTree ?? threeHostTree}
            onCreate={onCreate}
          />,
        );
        return { ...result, onCreate, onClose };
      }

    Rewrite to add `isAdmin?: boolean` with default `true` (least-churn migration per PATTERNS.md §5 change 1):

      function renderDialog(overrides: {
        open?: boolean;
        onClose?: () => void;
        onCreate?: ReturnType<typeof vi.fn>;
        hostTree?: typeof threeHostTree;
        // Phase 88: admin-gate override. Default is `true` because most
        // existing tests were written when the Path input + identity-mode
        // checkbox were universal — keeping the helper's default at
        // isAdmin=true preserves their assumptions without churn. Tests
        // that specifically exercise the non-admin path pass isAdmin:
        // false explicitly (see the three new tests near the bottom of
        // this file per Plan 88-03 Task 2 Edit E).
        isAdmin?: boolean;
      } = {}) {
        const onCreate = overrides.onCreate ?? vi.fn();
        const onClose = overrides.onClose ?? vi.fn();
        const result = render(
          <NewSessionDialog
            open={overrides.open ?? true}
            onClose={onClose}
            hostTree={overrides.hostTree ?? threeHostTree}
            onCreate={onCreate}
            isAdmin={overrides.isAdmin ?? true}
          />,
        );
        return { ...result, onCreate, onClose };
      }

    ─────────────────────────────────────────────────────────────
    EDIT C — Replace all /create with new identity/i occurrences with IDENTITY_MODE_CHECKBOX_RE
    ─────────────────────────────────────────────────────────────

    Per PATTERNS.md §5's enumeration, the file has ~16-20 occurrences of the label regex — including but not limited to L286, L327, L358, L575-576, L601-602, L623, L638-639, L801-806, L811-815, L848-849, L920-936 (line numbers approximate pre-edit; will shift as earlier edits land).

    For each occurrence:

      const checkbox = getByRole("checkbox", { name: /create with new identity/i });

    Replace with:

      const checkbox = getByRole("checkbox", { name: IDENTITY_MODE_CHECKBOX_RE });

    Also update any `queryByRole` calls with the same regex. Preserve every other line — helper signature, describe/it strings, arg destructures, act/fireEvent calls — byte-identical unless another edit in this task explicitly modifies them.

    ─────────────────────────────────────────────────────────────
    EDIT D — Rewrite Test D at L618-626
    ─────────────────────────────────────────────────────────────

    Current shape:

      describe("NewSessionDialog: Test D — identity-mode defaults ON", () => {
        it("Test D: initial render → identity-mode checkbox is checked", () => {
          const { getByRole } = renderDialog();
          const checkbox = getByRole("checkbox", { name: /create with new identity/i }) as HTMLInputElement;
          expect(checkbox.checked).toBe(true);
        });
      });

    Rewrite (per PATTERNS.md §5 change 2):

      describe("NewSessionDialog: Test D — identity-mode checkbox defaults UNCHECKED (admin-only, agent mode is default)", () => {
        it("Test D: initial render with isAdmin=true → 'Just a shell — no agent' checkbox is unchecked", () => {
          // Phase 88: default flipped from checked-true (identity mode ON was
          // the default) to unchecked-false (agent mode IS the default). The
          // local state variable was renamed from `identityMode` to `shellOnly`
          // and its useState default flipped from `true` to `false`. Checkbox
          // is admin-gated so this test renders under isAdmin=true (the
          // renderDialog helper default) to see the checkbox at all.
          const { getByRole } = renderDialog({ isAdmin: true });
          const checkbox = getByRole("checkbox", { name: IDENTITY_MODE_CHECKBOX_RE }) as HTMLInputElement;
          expect(checkbox.checked).toBe(false);
        });
      });

    ─────────────────────────────────────────────────────────────
    EDIT E — Audit existing tests that click-the-checkbox for semantic inversion
    ─────────────────────────────────────────────────────────────

    Per PATTERNS.md §5 landmine — Tests 5, 6, 7 (line numbers approximate: L570-582 for Test A, L590-608 for Test B, etc.) explicitly click the checkbox to toggle OFF the (then-default-ON) identity mode to reach the regular-session path. After Phase 88 the default IS agent-mode (checkbox unchecked); clicking now OPTS INTO shell mode.

    For each test that used to be shaped as "click checkbox to reach shell mode":
    - The `fireEvent.click(checkbox)` call REMAINS (still needed to reach shell mode after Phase 88; the click just has inverted intent).
    - The describe/it string that used to read "identity mode OFF" or "regular session mode" should be updated to read "shell mode (click checkbox to opt in)" for clarity.
    - Any assertion referencing `identityMode` on the callback payload (`expect(arg.identityMode).toBe(false)` for shell-mode expectations) stays UNCHANGED — the PUBLIC payload discriminant name is preserved per Plan 88-02.
    - Any assertion referencing `identityMode` on the callback payload (`expect(arg.identityMode).toBe(true)` for agent-mode expectations) also stays unchanged.

    For each test that used to reach agent mode WITHOUT clicking (because default was ON):
    - No click needed; agent-mode is still the default (just via `shellOnly=false` instead of `identityMode=true`).
    - The describe/it string still reads "identity mode ON" / "agent mode" fine (surface-level accurate).
    - No functional change required unless the test specifically asserts on the local state var name (it shouldn't — tests hit the DOM, not React internals).

    Suggested Test A (L569-579) revised describe/it string acknowledging Phase 88 (path is admin-gated now; the "visible in BOTH modes" assumption no longer holds without isAdmin=true):

      describe("NewSessionDialog: Test A — path field visible in both modes UNDER isAdmin", () => {
        it("Test A: path input present with agent-mode default, and still present after clicking checkbox to opt into shell mode", () => {
          const { getByLabelText, getByRole } = renderDialog(); // isAdmin defaults to true
          expect(getByLabelText(/^path$/i)).toBeTruthy();
          const checkbox = getByRole("checkbox", { name: IDENTITY_MODE_CHECKBOX_RE });
          fireEvent.click(checkbox); // opt into shell mode
          expect(getByLabelText(/^path$/i)).toBeTruthy();
        });
      });

    Apply the same shape update pattern to Tests B through S as appropriate. Tests that already implicitly work under the new semantics (agent-mode default, agent-mode = identity-cluster visible) may only need the label-regex replacement from Edit C plus a describe/it string touch-up.

    Test S at L844-857 (identity-mode OFF → onCreate with identityMode:false) — the click-to-shell-mode step remains valid; describe/it string can stay similar. The `expect(arg.identityMode).toBe(false)` assertion stays (public payload discriminant preserved).

    Test AA at L1098+ (all 5 steps complete + ended:ok:true → onClose called + onCreate called with identityMode:true payload) — this tests the AGENT-mode path; agent-mode is now default so no click needed. The `expect(arg.identityMode).toBe(true)` assertion stays. Describe/it string can stay similar.

    ─────────────────────────────────────────────────────────────
    EDIT F — Add three new tests to NewSessionDialog.test.tsx
    ─────────────────────────────────────────────────────────────

    Append near the end of the file (before the closing brace of the file's outer scope, or as a new final `describe` block). Per PATTERNS.md §5 change 4 + PrettyConversationsPanel.test.tsx L4722-4743 sibling shape:

      // Phase 88 new tests (Plan 88-03 Task 2 Edit F): admin-gate behavior +
      // non-admin backend substitution round-trip. These lock the three
      // shape-file surface changes (Path admin-gate, checkbox admin-gate,
      // backend `~/<name>/` substitution) with regression coverage that
      // did not exist pre-Phase-88.
      describe("NewSessionDialog: Phase 88 admin-gate + backend substitution", () => {
        it("Phase 88 T1: isAdmin=false → no Path input AND no shell-only checkbox rendered", () => {
          const { queryByLabelText, queryByRole } = renderDialog({ isAdmin: false });
          expect(queryByLabelText(/^path$/i)).toBeNull();
          expect(queryByRole("checkbox", { name: IDENTITY_MODE_CHECKBOX_RE })).toBeNull();
        });

        it("Phase 88 T2: isAdmin=true → Path input present with default value '~/' AND shell-only checkbox present unchecked", () => {
          const { getByLabelText, getByRole } = renderDialog({ isAdmin: true });
          const pathInput = getByLabelText(/^path$/i) as HTMLInputElement;
          expect(pathInput).toBeTruthy();
          expect(pathInput.value).toBe("~/");
          const checkbox = getByRole("checkbox", { name: IDENTITY_MODE_CHECKBOX_RE }) as HTMLInputElement;
          expect(checkbox).toBeTruthy();
          expect(checkbox.checked).toBe(false);
        });

        it("Phase 88 T3: isAdmin=false submit path sends path:'' — backend substitutes `~/<name>/` per Plan 88-01 narrow", async () => {
          // Mock openBirthStream to capture the call args without exercising SSE.
          // Follow the mock pattern from Test AA at L1098+ or the module-mock
          // setup near the top of the file for openBirthStream / birth-related
          // imports. The exact mock shape depends on how the file already
          // stubs openBirthStream — reuse that pattern verbatim.
          //
          // Test setup (paraphrased — align with existing mock scaffolding):
          //   1. renderDialog({ isAdmin: false, onCreate: vi.fn() })
          //   2. Fill required fields: pick a host (or use oneHostTree helper
          //      if the file has one), type a valid identity name (matches
          //      IDENTITY_NAME_PATTERN — e.g. "test-agent"), pick a role from
          //      the role dropdown (the file's existing fillIdentityForm
          //      helper at L923+ or an inline shape).
          //   3. Click the Create button.
          //   4. Wait for openBirthStream to have been called (await
          //      waitFor(() => expect(openBirthStreamMock).toHaveBeenCalled()))
          //   5. Assert the call arg: expect(openBirthStreamMock.mock.calls[0][0].path).toBe("");
          //
          // Rationale: Plan 88-02 Edit F rewrites handleBirth's openBirthStream
          // path arg to `isAdmin ? normalizedPath : ""`. Under isAdmin=false
          // this test proves the client sends empty-string on the wire,
          // handing off working-directory computation to Plan 88-01's backend
          // narrow at identity-birth.ts:206 which substitutes `~/<name>/`.
          //
          // NOTE: this test does NOT need the backend to run — it only
          // verifies the WIRE contract from the frontend. The backend
          // substitution behavior is tested separately (see Plan 88-01 Task 2
          // acceptance_criteria) against identity-birth.test.ts. This test
          // closes the round-trip visibility.
          expect(true).toBe(true); // placeholder — replace with real assertion following the recipe above
        });
      });

    IMPORTANT: the third test T3 has a placeholder assertion (`expect(true).toBe(true)`) — the executor MUST replace this with a real assertion that calls the openBirthStream mock. The recipe is spelled out in the comment; the executor consults the existing mock scaffolding in the file (grep for `openBirthStream` at file top — the file already stubs it for Test 5, Test AA, and probably others). The placeholder is left in-plan because the exact mock name and module path depends on which import style the file uses; do NOT hardcode a specific mock identifier in this plan.

    ─────────────────────────────────────────────────────────────
    ANTI-PATTERNS TO AVOID
    ─────────────────────────────────────────────────────────────

    - Do NOT change the helper's default from `isAdmin: true` to `isAdmin: false`. The default-true migration is the whole least-churn point of PATTERNS.md §5 change 1. Non-admin tests pass `isAdmin: false` explicitly.
    - Do NOT assert on the local state var name `shellOnly` inside any test — that's a React-internals detail invisible to the DOM. Tests hit the checkbox `.checked` property (which reflects `shellOnly` truthy → checked, `shellOnly` falsy → unchecked), the callback payload `arg.identityMode` (public discriminant, unchanged), and the DOM presence/absence of the Path + checkbox elements.
    - Do NOT replace the `arg.identityMode` payload assertions with `arg.shellOnly` — the public payload discriminant name stays `identityMode` per Plan 88-02 Edit D. The rename is LOCAL only.
    - Do NOT delete existing tests. Every test in the file that referenced the old checkbox label gets updated (Edit C) or rewritten (Edit D, Edit E). If a test is truly obsolete under Phase 88, note it in the SUMMARY.md but do not delete it in this plan — a deletion pass belongs to a follow-up cleanup bounty.
    - Do NOT ship Edit F's T3 with the placeholder assertion still in place. The executor must replace `expect(true).toBe(true)` with the real openBirthStream mock assertion before verify runs.
    - Do NOT touch tests in unrelated files (e.g. AppShell.test.tsx, PrettyConversationsPanel.test.tsx are NOT modified by this task — Task 4 in this plan handles role-dropdown; Task 3 handles chain; Task 5 handles task-input).
  </action>
  <acceptance_criteria>
    Grep source assertions on `src/ui/sidebar/NewSessionDialog.test.tsx`:
    - `grep -c 'IDENTITY_MODE_CHECKBOX_RE' src/ui/sidebar/NewSessionDialog.test.tsx` returns ≥ 15 (declaration + ~15 usages replacing the old regex sites — count depends on final label-regex-usage total after Edit E's test rewrites; must be ≥ 15)
    - `grep -c '/just a shell.*no agent/i' src/ui/sidebar/NewSessionDialog.test.tsx` returns 1 (declared once in the constant; all usages reference the constant identifier not the regex literal)
    - `grep -c '/create with new identity/i' src/ui/sidebar/NewSessionDialog.test.tsx` returns 0 (all old label regexes replaced)
    - `grep -c 'isAdmin' src/ui/sidebar/NewSessionDialog.test.tsx` returns ≥ 6 (helper param + helper default + T1 + T2 + T3 + at least one other explicit isAdmin: true reference from Edit E rewrites — exact count is not the assertion; a minimum threshold that catches under-coverage is)
    - `grep -c 'isAdmin: false' src/ui/sidebar/NewSessionDialog.test.tsx` returns ≥ 2 (T1 + T3 pass isAdmin: false explicitly)
    - `grep -c 'isAdmin: true' src/ui/sidebar/NewSessionDialog.test.tsx` returns ≥ 1 (T2 explicitly asserts admin path — helper default handles the rest)
    - `grep -c 'Phase 88' src/ui/sidebar/NewSessionDialog.test.tsx` returns ≥ 3 (constant comment + helper comment + new tests describe block)
    - `grep -c 'expect(true).toBe(true)' src/ui/sidebar/NewSessionDialog.test.tsx` returns 0 (T3 placeholder replaced with real assertion; if this grep returns 1, the executor did not complete Edit F)

    Test-command assertion:
    - `npx vitest run src/ui/sidebar/NewSessionDialog.test.tsx --run` exits 0
  </acceptance_criteria>
  <verify>
    <automated>set -e; grep -c 'IDENTITY_MODE_CHECKBOX_RE' src/ui/sidebar/NewSessionDialog.test.tsx | awk '$1 >= 15 { exit 0 } { exit 1 }' && [ "$(grep -c '/just a shell.*no agent/i' src/ui/sidebar/NewSessionDialog.test.tsx)" = "1" ] && [ "$(grep -c '/create with new identity/i' src/ui/sidebar/NewSessionDialog.test.tsx)" = "0" ] && grep -c 'isAdmin' src/ui/sidebar/NewSessionDialog.test.tsx | awk '$1 >= 6 { exit 0 } { exit 1 }' && grep -c 'isAdmin: false' src/ui/sidebar/NewSessionDialog.test.tsx | awk '$1 >= 2 { exit 0 } { exit 1 }' && grep -c 'isAdmin: true' src/ui/sidebar/NewSessionDialog.test.tsx | awk '$1 >= 1 { exit 0 } { exit 1 }' && grep -c 'Phase 88' src/ui/sidebar/NewSessionDialog.test.tsx | awk '$1 >= 3 { exit 0 } { exit 1 }' && [ "$(grep -c 'expect(true).toBe(true)' src/ui/sidebar/NewSessionDialog.test.tsx)" = "0" ] && npx vitest run src/ui/sidebar/NewSessionDialog.test.tsx --run</automated>
  </verify>
  <done>
    - IDENTITY_MODE_CHECKBOX_RE constant declared at top of file
    - renderDialog helper accepts isAdmin (default `true`) and forwards to <NewSessionDialog>
    - All ~16-20 /create with new identity/i occurrences replaced with IDENTITY_MODE_CHECKBOX_RE
    - Test D rewritten for default-unchecked under isAdmin=true
    - Existing checkbox-click tests updated with Phase-88 describe/it strings and semantic-inversion comments; assertions on `arg.identityMode` (public payload discriminant) unchanged
    - Three new tests added: T1 (isAdmin=false hides both), T2 (isAdmin=true renders both defaults), T3 (isAdmin=false submit sends path:'')
    - T3's placeholder `expect(true).toBe(true)` replaced with a real openBirthStream mock assertion
    - `npx vitest run src/ui/sidebar/NewSessionDialog.test.tsx --run` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 3: Update NewSessionDialog.chain.test.tsx — renderDialog helper + label regex sites + Test 9 semantic-inversion</name>
  <files>src/ui/sidebar/NewSessionDialog.chain.test.tsx</files>
  <read_first>
    - src/ui/sidebar/NewSessionDialog.chain.test.tsx (specifically the renderDialog helper near the top of the file, and the two /create with new identity/i sites at L514 and L546. The test at L500-520 asserts "identity-mode OFF hides brief field" — brief was already stripped in Phase 86, so this test may already be defunct or already passing on empty branch; audit intent + apply label rewrite. The test at L525-553 is Test 9 "initialRole ignored when identity-mode OFF" — same click-to-shell-mode pattern as NewSessionDialog.test.tsx Test S)
    - .planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-PATTERNS.md §6 (chain.test.tsx section — the two audit items: Test 5/9 need mental-model updates, or explicit rewrites if the click-semantics inversion breaks intent)
    - .planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-02-PLAN.md Edit C + Edit D — the label change + rename mapping
  </read_first>
  <action>
    Three sub-edits inside NewSessionDialog.chain.test.tsx.

    ─────────────────────────────────────────────────────────────
    EDIT A — Update renderDialog helper (if the file has one) to accept isAdmin
    ─────────────────────────────────────────────────────────────

    Locate the renderDialog helper near the top of the file (grep for `function renderDialog` — chain.test.tsx uses a shared shape with NewSessionDialog.test.tsx per PATTERNS.md §6). Add `isAdmin?: boolean` param with default `true`, forward to <NewSessionDialog isAdmin={overrides.isAdmin ?? true} />. Same shape as Task 2 Edit B for the sibling test file. If the file uses inline `<NewSessionDialog … />` renders instead of a helper (as some of Test 5 and Test 9 do), the raw renders should each add `isAdmin={true}` to preserve the pre-Phase-88 admin-surface-visible assumption in existing coverage.

    ─────────────────────────────────────────────────────────────
    EDIT B — Replace /create with new identity/i at L514 and L546
    ─────────────────────────────────────────────────────────────

    Replace the checkbox-label regex at both sites with either a file-local `IDENTITY_MODE_CHECKBOX_RE` constant (recommended — declare it near the file's top imports; same shape as Task 2 Edit A) OR with the inline literal `/just a shell.*no agent/i` if the file has no top-of-file constants section.

    ─────────────────────────────────────────────────────────────
    EDIT C — Refresh describe/it strings for Test 5 and Test 9
    ─────────────────────────────────────────────────────────────

    Update the describe/it strings that reference "identity-mode OFF" to accurately describe the post-Phase-88 semantics. The test intent is preserved; the STORY the strings tell needs to reflect that "identity-mode OFF" is now reached by CHECKING the "Just a shell — no agent" checkbox (opt in to shell), not by unchecking a "Create with new identity" checkbox (opt out of identity mode).

    Test 5 (L500-520) suggested describe/it rewrite:

      describe("NewSessionDialog chain: Test 5 — brief field absent (brief was stripped in Phase 86; identity-cluster/shell-mode toggle no longer shows it either)", () => {
        it("Test 5: open with seed, then opt into shell mode via checkbox → brief field is not rendered", async () => { ... });
      });

    Test 9 (L525-553) suggested describe/it rewrite:

      describe("NewSessionDialog chain: Test 9 — initialRole ignored when user opts into shell mode", () => {
        it("Test 9: open with seed → role dropdown populated; click 'Just a shell — no agent' to opt into shell → role dropdown gone; seed does not leak into shell mode", async () => { ... });
      });

    Add a Phase-88 comment above each rewritten test explaining the semantic-inversion story.

    ─────────────────────────────────────────────────────────────
    ANTI-PATTERNS TO AVOID
    ─────────────────────────────────────────────────────────────

    - Do NOT change the fireEvent.click call itself — the click is still valid; it now opts INTO shell mode. Only the surrounding prose describes the intent differently.
    - Do NOT delete tests. Test 5's brief-field-absent assertion is defunct at RUNTIME (brief always absent post-Phase-86) but the test still exercises the shell-mode transition path so keep it.
    - Do NOT touch the initialHost / initialRole prop-forward tests unrelated to the checkbox click — they should still pass under the renderDialog helper isAdmin=true default without any edit beyond Edit A's helper update.
  </action>
  <acceptance_criteria>
    Grep source assertions on `src/ui/sidebar/NewSessionDialog.chain.test.tsx`:
    - `grep -c '/create with new identity/i' src/ui/sidebar/NewSessionDialog.chain.test.tsx` returns 0
    - `grep -c 'just a shell.*no agent' src/ui/sidebar/NewSessionDialog.chain.test.tsx` returns ≥ 1 (either as `IDENTITY_MODE_CHECKBOX_RE` constant or inline regex — one or the other must appear)
    - `grep -c 'isAdmin' src/ui/sidebar/NewSessionDialog.chain.test.tsx` returns ≥ 1 (helper update or inline forward)
    - `grep -c 'Phase 88' src/ui/sidebar/NewSessionDialog.chain.test.tsx` returns ≥ 1 (rewrite comment anchor)

    Test-command assertion:
    - `npx vitest run src/ui/sidebar/NewSessionDialog.chain.test.tsx --run` exits 0
  </acceptance_criteria>
  <verify>
    <automated>set -e; [ "$(grep -c '/create with new identity/i' src/ui/sidebar/NewSessionDialog.chain.test.tsx)" = "0" ] && grep -q 'just a shell.*no agent' src/ui/sidebar/NewSessionDialog.chain.test.tsx && grep -q 'isAdmin' src/ui/sidebar/NewSessionDialog.chain.test.tsx && grep -q 'Phase 88' src/ui/sidebar/NewSessionDialog.chain.test.tsx && npx vitest run src/ui/sidebar/NewSessionDialog.chain.test.tsx --run</automated>
  </verify>
  <done>
    - renderDialog helper accepts isAdmin (default true) OR inline renders forward isAdmin={true}
    - Both L514 and L546 checkbox-label regex sites updated
    - Test 5 and Test 9 describe/it strings refreshed for semantic-inversion
    - `npx vitest run src/ui/sidebar/NewSessionDialog.chain.test.tsx --run` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 4: Update NewSessionDialog.role-dropdown.test.tsx renderDialog helper for isAdmin</name>
  <files>src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx</files>
  <read_first>
    - src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx (specifically L125-140-ish for the renderDialog helper; the file has 0 references to the checkbox-label regex per the read_first grep at PATTERNS.md §6 — so this task is primarily the helper isAdmin update plus verification that no other Phase-88-affected surface breaks anything)
    - .planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-PATTERNS.md §6 (role-dropdown.test.tsx section — 0 checkbox refs; but tests exercise the role dropdown which renders inside the identity-cluster gate `{!shellOnly && (…)}`. Post-Phase-88 the default IS agent-mode-on so the role dropdown IS visible without any checkbox interaction; existing tests should continue to work but the helper needs isAdmin so the dialog compiles under the new prop signature — technically optional because the prop has a fail-closed default of false, but PATTERNS.md §6 recommends the helper add it and default to true to make explicit that the file exercises admin-mode)
  </read_first>
  <action>
    Single edit: renderDialog helper at L125-140-ish gains `isAdmin?: boolean` param with default `true`, forwarded to `<NewSessionDialog isAdmin={overrides.isAdmin ?? true} />`. Same shape as Task 2 Edit B and Task 3 Edit A.

    Add a one-line Phase-88 anchor comment above the helper explaining that the default is `true` to preserve existing coverage's admin-surface assumption.

    No other change is required in this file — the tests exercise the role dropdown which is inside `{!shellOnly && (…)}` (Plan 88-02 Edit D-3 renamed gate at L963); agent-mode is the new default so `shellOnly=false` on mount means the role dropdown renders on first mount, matching the pre-Phase-88 "identity-mode ON by default" behavior. No test-body edits needed.

    ─────────────────────────────────────────────────────────────
    ANTI-PATTERNS TO AVOID
    ─────────────────────────────────────────────────────────────

    - Do NOT add checkbox-label regex constants to this file — no test exercises the checkbox. Adding a constant that isn't used is dead code.
    - Do NOT add new tests to this file. The three new admin-gate tests land in NewSessionDialog.test.tsx per Task 2 Edit F.
  </action>
  <acceptance_criteria>
    Grep source assertions on `src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx`:
    - `grep -c 'isAdmin' src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx` returns ≥ 2 (param declaration + forward on <NewSessionDialog>)
    - `grep -c 'Phase 88' src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx` returns ≥ 1 (comment anchor)

    Test-command assertion:
    - `npx vitest run src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx --run` exits 0
  </acceptance_criteria>
  <verify>
    <automated>set -e; grep -c 'isAdmin' src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx | awk '$1 >= 2 { exit 0 } { exit 1 }' && grep -q 'Phase 88' src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx && npx vitest run src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx --run</automated>
  </verify>
  <done>
    - renderDialog helper accepts `isAdmin?: boolean` param defaulting to `true`; forwards `isAdmin={overrides.isAdmin ?? true}` to `<NewSessionDialog>`
    - Phase-88 anchor comment above helper
    - No test-body edits required in this file — agent-mode default (post-Phase-88) matches the pre-Phase-88 identity-mode-ON default at the role-dropdown-visibility surface
    - `npx vitest run src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx --run` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 5: Update NewSessionDialog.task-input.test.tsx — renderDialog helper + label regex sites + Task 1a/1b/3c-3e describe/it strings for semantic-inversion</name>
  <files>src/ui/sidebar/NewSessionDialog.task-input.test.tsx</files>
  <read_first>
    - src/ui/sidebar/NewSessionDialog.task-input.test.tsx (specifically L120-144 for the renderDialog helper (uses `oneHostTree` default); L236-256 for Task 1a "task textarea rendered when identityMode=true" and Task 1b "task textarea absent when identityMode toggled off" — both need describe/it string updates + label-regex replacement at L249-252; L428-465 for Task 3c/3d/3e "identityMode on + no host picked → hint visible" etc. — all reference the identityMode default assumption + Task 3e clicks the checkbox at L458-461)
    - .planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-PATTERNS.md §6 (task-input.test.tsx section — 5 refs to identityMode in describe/it strings at L236, L246, L428, L445, L455; two click sites at L249-252 and L458-461)
    - .planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-02-PLAN.md Edit D — rename mapping (LOCAL var is `shellOnly`, PUBLIC discriminant `identityMode` preserved on payload)
  </read_first>
  <action>
    Four sub-edits inside NewSessionDialog.task-input.test.tsx.

    ─────────────────────────────────────────────────────────────
    EDIT A — Update renderDialog helper at L127-144 to accept isAdmin
    ─────────────────────────────────────────────────────────────

    Add `isAdmin?: boolean` param with default `true`, forwarded to `<NewSessionDialog isAdmin={overrides.isAdmin ?? true} />`. Same shape as Task 2 Edit B and Task 3 Edit A.

    ─────────────────────────────────────────────────────────────
    EDIT B — Add IDENTITY_MODE_CHECKBOX_RE constant OR use inline regex at each click site
    ─────────────────────────────────────────────────────────────

    Two label-regex sites at L249 and L458 (approximate; may drift as earlier edits land). Replace `/create with new identity/i` with either a top-of-file `IDENTITY_MODE_CHECKBOX_RE = /just a shell.*no agent/i` constant (recommended — matches Task 2's pattern for consistency across the four NewSessionDialog test files) OR inline literals.

    ─────────────────────────────────────────────────────────────
    EDIT C — Update Task 1a describe/it string at L236 for post-Phase-88 semantic
    ─────────────────────────────────────────────────────────────

    Current: `it("Task 1a: task textarea is rendered when identityMode=true", async () => { ... })`.

    Under Phase 88 the local state var is `shellOnly` and agent-mode is the new default (`shellOnly === false`) — no checkbox click needed to reach agent-mode. The task textarea's render gate is `{!shellOnly && (…)}` (Plan 88-02 Edit D-3 renamed). So on fresh mount under isAdmin=true, the task textarea IS rendered. Rewrite the describe/it string to reflect this without invoking the OLD state-var name:

      it("Task 1a: task textarea is rendered when agent mode is on (the new default — no checkbox click needed post-Phase-88)", async () => { ... });

    No test body change required — the test already renders under renderDialog default (post-Edit-A: isAdmin=true) and asserts the textarea presence, which agent-mode-default satisfies.

    ─────────────────────────────────────────────────────────────
    EDIT D — Update Task 1b describe/it string + comment at L246 for post-Phase-88 semantic
    ─────────────────────────────────────────────────────────────

    Current: `it("Task 1b: task textarea is absent when identityMode is toggled off", async () => { const { getByRole } = renderDialog(); const checkbox = getByRole("checkbox", { name: /create with new identity/i }); fireEvent.click(checkbox); ... expect the textarea to be absent })`.

    Under Phase 88 the click OPTS INTO shell mode (which is the branch where the task textarea is NOT rendered — because the task textarea is inside `{!shellOnly && (…)}`, i.e. only rendered when NOT in shell mode). The click stays, but the intent inverts.

    Rewrite:

      it("Task 1b: task textarea is absent when user opts into shell mode (checks the 'Just a shell — no agent' checkbox)", async () => {
        const { getByRole } = renderDialog();
        // Phase 88: click OPTS INTO shell mode (was: opted out of identity mode).
        // The checkbox label flipped from "Create with new identity" to
        // "Just a shell — no agent"; the local state var was renamed from
        // `identityMode` to `shellOnly` and the useState default flipped
        // from `true` to `false`. Semantics: unchecked = agent mode = task
        // textarea rendered; checked = shell mode = task textarea absent.
        const checkbox = getByRole("checkbox", { name: IDENTITY_MODE_CHECKBOX_RE }); // or inline /just a shell.*no agent/i
        fireEvent.click(checkbox);
        // Assertions unchanged — textarea still absent in shell mode.
      });

    ─────────────────────────────────────────────────────────────
    EDIT E — Update Task 3c/3d/3e describe/it strings + Task 3e click at L458
    ─────────────────────────────────────────────────────────────

    Task 3c at L428: `it("Task 3c: identityMode on + no host picked → 'Pick a host to see available roles' hint is visible", ...)`. Update the string to reference agent-mode:

      it("Task 3c: agent mode (default) + no host picked → 'Pick a host to see available roles' hint is visible", async () => { ... });

    Task 3d at L445: `it("Task 3d: identityMode on + one host auto-picked → hint is NOT visible + role dropdown appears", ...)`. Update:

      it("Task 3d: agent mode (default) + one host auto-picked → hint is NOT visible + role dropdown appears", async () => { ... });

    Task 3e at L455-461: `it("Task 3e: identityMode OFF + no host picked → hint is NOT visible (hint is identity-mode-only)", ...)` with a click at L458-461. Update string + click regex:

      it("Task 3e: user opts into shell mode + no host picked → hint is NOT visible (hint is agent-mode-only)", async () => {
        const { getByRole } = renderDialog({ hostTree: twoHostTree });
        // Phase 88: click opts INTO shell mode (was: opted out of identity mode).
        const checkbox = getByRole("checkbox", { name: IDENTITY_MODE_CHECKBOX_RE }); // or inline /just a shell.*no agent/i
        fireEvent.click(checkbox);
        // Assertions unchanged — hint absent in shell mode.
      });

    ─────────────────────────────────────────────────────────────
    ANTI-PATTERNS TO AVOID
    ─────────────────────────────────────────────────────────────

    - Do NOT modify test-body assertions unless the assertion literally references `identityMode` as a runtime value (React internals) — test-side references to the payload discriminant `arg.identityMode` (public) stay unchanged.
    - Do NOT delete tests. Every Task 1a/1b/3c/3d/3e test remains — describe/it strings + comments update, click behavior preserves semantic intent under inversion.
    - Do NOT change the `oneHostTree` / `twoHostTree` fixtures — they are orthogonal to Phase 88.
  </action>
  <acceptance_criteria>
    Grep source assertions on `src/ui/sidebar/NewSessionDialog.task-input.test.tsx`:
    - `grep -c '/create with new identity/i' src/ui/sidebar/NewSessionDialog.task-input.test.tsx` returns 0 (old label regex gone)
    - `grep -c 'just a shell.*no agent' src/ui/sidebar/NewSessionDialog.task-input.test.tsx` returns ≥ 2 (both click sites — either as constant declaration + usages or inline regex at each site)
    - `grep -c 'isAdmin' src/ui/sidebar/NewSessionDialog.task-input.test.tsx` returns ≥ 2 (helper param + forward)
    - `grep -c 'Phase 88' src/ui/sidebar/NewSessionDialog.task-input.test.tsx` returns ≥ 2 (Task 1b comment + at least one other rewrite comment)
    - `grep -cE 'identityMode(=true|=false)' src/ui/sidebar/NewSessionDialog.task-input.test.tsx` returns 0 (describe/it string references to identityMode-with-boolean-suffix all rewritten)

    Test-command assertion:
    - `npx vitest run src/ui/sidebar/NewSessionDialog.task-input.test.tsx --run` exits 0
  </acceptance_criteria>
  <verify>
    <automated>set -e; [ "$(grep -c '/create with new identity/i' src/ui/sidebar/NewSessionDialog.task-input.test.tsx)" = "0" ] && grep -c 'just a shell.*no agent' src/ui/sidebar/NewSessionDialog.task-input.test.tsx | awk '$1 >= 2 { exit 0 } { exit 1 }' && grep -c 'isAdmin' src/ui/sidebar/NewSessionDialog.task-input.test.tsx | awk '$1 >= 2 { exit 0 } { exit 1 }' && grep -c 'Phase 88' src/ui/sidebar/NewSessionDialog.task-input.test.tsx | awk '$1 >= 2 { exit 0 } { exit 1 }' && [ "$(grep -cE 'identityMode(=true|=false)' src/ui/sidebar/NewSessionDialog.task-input.test.tsx)" = "0" ] && npx vitest run src/ui/sidebar/NewSessionDialog.task-input.test.tsx --run</automated>
  </verify>
  <done>
    - renderDialog helper accepts isAdmin (default true) and forwards to <NewSessionDialog>
    - Both label-regex sites (L249, L458) updated to the new label form
    - Task 1a describe/it string updated to remove "identityMode=true" language
    - Task 1b describe/it string + comment updated for semantic-inversion (click opts into shell mode)
    - Task 3c/3d describe/it strings updated to reference "agent mode (default)"
    - Task 3e describe/it string updated + comment for semantic-inversion
    - `npx vitest run src/ui/sidebar/NewSessionDialog.task-input.test.tsx --run` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runtime → source under test | Tests execute in vitest / jsdom; no network, no filesystem writes, no privileged operations. Only exercises the DOM shape and callback payloads of NewSessionDialog + CreateRoleDialog. No new attack surface introduced by test edits. |
| Mocked openBirthStream (Task 2 Edit F test T3) → test assertion | vi.fn() capture of the call args. No SSE opened, no backend hit, no cross-origin request. Assertion is byte-comparison on the mock's call arg. Zero risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-03-01 | Repudiation | Test refactor introduces a regression that hides a real Phase 88 bug behind a passing test | mitigate | Each new/rewritten test's assertion mirrors the source shape from Plan 88-02: T1 asserts the two admin-gated DOM elements are absent when isAdmin=false; T2 asserts they are present and defaults match Plan 88-02 Edit B + Edit C + Edit D; T3 asserts the wire contract from Plan 88-02 Edit F. Test rewrites for existing tests preserve the check semantics — only describe/it strings + label regex change; assertion bodies stay against the pre-Phase-88 shape of `arg.identityMode` (public payload discriminant, unchanged). Coverage does not decrease. |
| T-88-03-02 | Denial of Service | Test file consumes >50% context by growing too large during the refactor | mitigate | Task 2 Edit C is a mechanical regex-replacement, not a full rewrite. Edit F adds only three tests (~60 lines total). File grows by ~100 lines; total remains under executable context budget for a vitest run. |
| T-88-03-03 | Information Disclosure | Test file inadvertently logs credentials or paths | accept | Tests use fake paths (`~/`, mock host IPs `10.0.0.1`), synthetic identity names (`test-agent`), and vi.fn() mocks. No real filesystem paths, credentials, or environment secrets appear in test setup. |
| T-88-03-SC | Tampering | Zero new npm installs in this plan | accept | Pure TSX / test-file edits. No package.json / package-lock.json changes. |
</threat_model>

<verification>
Campaign constraint (Alice 2026-09-07): NO full-suite `npx vitest run`, NO docker build, NO docker cp, NO `docker compose up`, NO deploy. Scoped test runs only. Verification commands for this plan:

- `grep -c 'Roles are the expertise your agents adopt' src/ui/sidebar/CreateRoleDialog.test.tsx` returns 1
- `grep -c 'A role is what an agent does and how it thinks' src/ui/sidebar/CreateRoleDialog.test.tsx` returns 0
- `grep -c 'IDENTITY_MODE_CHECKBOX_RE' src/ui/sidebar/NewSessionDialog.test.tsx` returns ≥ 15
- `grep -c '/create with new identity/i' src/ui/sidebar/NewSessionDialog.test.tsx` returns 0
- `grep -c '/create with new identity/i' src/ui/sidebar/NewSessionDialog.chain.test.tsx` returns 0
- `grep -c '/create with new identity/i' src/ui/sidebar/NewSessionDialog.task-input.test.tsx` returns 0
- `grep -c 'isAdmin: false' src/ui/sidebar/NewSessionDialog.test.tsx` returns ≥ 2 (T1 + T3 new tests)
- `grep -c 'isAdmin' src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx` returns ≥ 2
- `grep -c 'Phase 88' src/ui/sidebar/CreateRoleDialog.test.tsx` returns ≥ 1
- `grep -c 'Phase 88' src/ui/sidebar/NewSessionDialog.test.tsx` returns ≥ 3
- `grep -c 'Phase 88' src/ui/sidebar/NewSessionDialog.chain.test.tsx` returns ≥ 1
- `grep -c 'Phase 88' src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx` returns ≥ 1
- `grep -c 'Phase 88' src/ui/sidebar/NewSessionDialog.task-input.test.tsx` returns ≥ 2
- All five scoped test runs exit 0:
  - `npx vitest run src/ui/sidebar/CreateRoleDialog.test.tsx --run`
  - `npx vitest run src/ui/sidebar/NewSessionDialog.test.tsx --run`
  - `npx vitest run src/ui/sidebar/NewSessionDialog.chain.test.tsx --run`
  - `npx vitest run src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx --run`
  - `npx vitest run src/ui/sidebar/NewSessionDialog.task-input.test.tsx --run`

Combined scoped run at the end of the plan (recommended final gate):
  `npx vitest run src/ui/sidebar/CreateRoleDialog.test.tsx src/ui/sidebar/NewSessionDialog.test.tsx src/ui/sidebar/NewSessionDialog.chain.test.tsx src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx src/ui/sidebar/NewSessionDialog.task-input.test.tsx --run` exits 0
</verification>

<success_criteria>
- CreateRoleDialog.test.tsx Test 11 asserts the new two-sentence blurb regex; old blurb regex removed
- NewSessionDialog.test.tsx has IDENTITY_MODE_CHECKBOX_RE constant, renderDialog helper accepts isAdmin (default true), Test D rewritten for default-unchecked, all label regex sites replaced, three new tests (T1 isAdmin=false hides both / T2 isAdmin=true renders both defaults / T3 non-admin submit sends path:'')
- NewSessionDialog.chain.test.tsx has renderDialog helper accepting isAdmin, both label regex sites replaced, Test 5 + Test 9 describe/it strings refreshed for semantic-inversion
- NewSessionDialog.role-dropdown.test.tsx has renderDialog helper accepting isAdmin
- NewSessionDialog.task-input.test.tsx has renderDialog helper accepting isAdmin, both label regex sites replaced, Task 1a/1b/3c/3d/3e describe/it strings refreshed for semantic-inversion
- All five scoped test runs exit 0 — the Wave 2 expected-failure state is fully resolved
- Test coverage GAINS three new assertions (admin-gate hide, admin-gate render+defaults, non-admin backend substitution wire contract) that did not exist pre-Phase 88; total coverage is a strict superset of pre-Phase-88 coverage
- No source files touched in this plan; only the five test files listed
- Plan set complete: Wave 1 (Plan 88-01) shipped prop-plumb + backend narrow; Wave 2 (Plan 88-02) shipped modal UI + state-var rename + submit invariant; Wave 3 (Plan 88-03) restored green + locked new coverage
</success_criteria>

<output>
Create `.planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-03-SUMMARY.md` when done
</output>
