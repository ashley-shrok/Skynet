---
phase: 88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g
plan: 02
type: execute
wave: 2
depends_on: [88-01]
files_modified:
  - src/ui/sidebar/CreateRoleDialog.tsx
  - src/ui/sidebar/NewSessionDialog.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "CreateRoleDialog blurb text at DialogDescription (CreateRoleDialog.tsx:527-529) is replaced BYTE-EXACT with: 'Roles are the expertise your agents adopt. Every agent using this role inherits its goals, rules, and knowledge.' (per 88-CONTEXT.md §Verbatim copy Role blurb + PATTERNS.md §1)"
    - "NewSessionDialog `startDescription` defaultValue at L781-783 is replaced BYTE-EXACT with: 'Agents are the workers you chat with. Each one adopts a role that shapes what they know and how they help.' (per 88-CONTEXT.md §Verbatim copy Agent blurb + PATTERNS.md §2a)"
    - "NewSessionDialog Path input block at L926-942 is wrapped in `{isAdmin && (…)}` — non-admin never renders the Path field (per 88-CONTEXT.md §Path field admin-gate + PATTERNS.md §2b)"
    - "NewSessionDialog identity-mode checkbox + its wrapping `<div className='flex items-center gap-2'>` block at L944-960 is wrapped in `{isAdmin && (…)}` — non-admin never renders the checkbox (per 88-CONTEXT.md §Identity-mode checkbox admin-gate + PATTERNS.md §2c)"
    - "The checkbox label text at NewSessionDialog.tsx L958 is replaced BYTE-EXACT with: 'Just a shell — no agent' (per 88-CONTEXT.md §Verbatim label + PATTERNS.md §2c change 2). The em-dash is the Unicode U+2014, matching the em-dash already used in the sibling CreateRoleDialog blurb — no ASCII hyphen substitution."
    - "The local state variable `identityMode` is RENAMED to `shellOnly` throughout NewSessionDialog.tsx (state hook, close-reset, all local read-sites); the PUBLIC callback-payload discriminant of the SAME name `identityMode` in NewSessionOnCreateOpts (L138-158) STAYS UNCHANGED as an unrelated wire contract per PATTERNS.md §2e rename recommendation"
    - "The renamed `shellOnly` state variable's `useState` default flips from `true` (old `identityMode`) to `false` (new `shellOnly`) — checkbox default UNCHECKED, agent mode default ON. The close-reset at L436 flips from `setIdentityMode(true)` to `setShellOnly(false)`. Semantic invariant: `shellOnly === true` means checkbox CHECKED = user opting into raw shell; `shellOnly === false` means checkbox UNCHECKED = user is spawning an agent (default)."
    - "The submit-onclick handler at L1131-1153 enforces the non-admin invariant: effective shell-only branch requires BOTH `isAdmin && shellOnly` truthy — a non-admin user cannot spawn a raw shell even if some future caller passes a stale `shellOnly=true` local state (defense-in-depth per 88-CONTEXT.md §Non-admin invariant + PATTERNS.md §2g)"
    - "The handleBirth submit path at L631 continues to send `path: normalizedPath` — non-admin's Path field is admin-gated (never rendered) so `path` state remains at its initial `useState('~/')` from L322, but Plan 88-01's backend substitution takes precedence for non-admin because Plan 88-02 additionally clears the path to empty-string when non-admin AT THE handleBirth submit call site (Task 2 Edit F). Admins keep sending their explicit path."
    - "The regular-session (raw shell) submit-onclick branch at L1143-1148 sends `identityMode: false` unchanged — the callback-payload discriminant is UNCHANGED; the branch is only reachable when `isAdmin && shellOnly` are BOTH truthy"
  artifacts:
    - path: "src/ui/sidebar/CreateRoleDialog.tsx"
      provides: "Two-sentence header blurb replacing the Phase-84 one-sentence blurb inside <DialogDescription> at L527-529"
      contains: "Roles are the expertise your agents adopt"
    - path: "src/ui/sidebar/NewSessionDialog.tsx"
      provides: "Two-sentence agent blurb defaultValue at L781-783; Path input admin-gated at L926-942; identity-mode checkbox admin-gated + label flipped + default flipped at L944-960; local state var renamed to `shellOnly`; submit-path invariant `isAdmin && shellOnly` at L1131-1153; non-admin path-clear at handleBirth (L631)"
      contains: "shellOnly"
  key_links:
    - from: "NewSessionDialog Path field render at L926-942"
      to: "isAdmin prop from Plan 88-01"
      via: "JSX wrap `{isAdmin && (<div>…</div>)}` — non-admin never renders the field"
      pattern: "\\{isAdmin && \\("
    - from: "NewSessionDialog identity-mode checkbox render at L944-960"
      to: "isAdmin prop from Plan 88-01"
      via: "JSX wrap `{isAdmin && (<div>…</div>)}` — non-admin never sees the checkbox"
      pattern: "\\{isAdmin && \\("
    - from: "NewSessionDialog submit-onclick at L1131-1153"
      to: "identity-birth stream OR onCreate regular-session payload"
      via: "invariant `isAdmin && shellOnly` — non-admin always routes to handleBirth (agent mode); admin explicitly opts into raw shell to reach the regular-session branch"
      pattern: "isAdmin && shellOnly"
    - from: "NewSessionDialog handleBirth submit at L631"
      to: "identity-birth.ts POST /identities/birth (Plan 88-01 backend narrow)"
      via: "when !isAdmin, handleBirth sends `path: ''` (skipping normalizePath); backend substitutes `~/<name>/`. When isAdmin, handleBirth sends `path: normalizePath(path)` (typically `~/` from the field default)"
      pattern: "path: !isAdmin \\? \"\""
---

<objective>
Wave-2 land the modal UI + submit-invariant edits. Consumes Plan 88-01's `isAdmin` prop for the admin gates and Plan 88-01's backend substitution for the non-admin path default.

Two files touched, one atomic paired-blurb constraint (per shape §What would make it wrong: "Ship agent blurb without revising role blurb" = visible inconsistency). Both blurbs land in the same commit surface.

`NewSessionDialog.tsx` gains six substantive edits inside one file:
1. Agent blurb defaultValue replacement (paired with role-side revision).
2. Path field admin-gate wrap.
3. Identity-mode checkbox admin-gate wrap + label flip.
4. Local state variable rename `identityMode` → `shellOnly` (state hook + close-reset + all local read-sites) with default flip `true` → `false`. The PUBLIC callback-payload discriminant of the SAME name `identityMode` in NewSessionOnCreateOpts (L138-158) STAYS UNCHANGED — it is a public wire contract consumed by AppShell.tsx L2040-2044 (see PATTERNS.md §2e).
5. Submit-onclick handler invariant enforcement (`isAdmin && shellOnly` for the regular-session branch — defense-in-depth for the non-admin never-a-shell rule).
6. handleBirth `path: !isAdmin ? "" : normalizedPath` — non-admin sends empty-string to trigger Plan 88-01's backend `~/<name>/` substitution; admin sends the normalized path.

Purpose: land the paired-blurb pattern to keep the two dialogs visually consistent (Phase 84's role blurb + this bounty's agent blurb pair off the shared verb "adopt"); tighten the create-agent modal for non-admin users (no Path field, no shell checkbox); flip the checkbox semantics to match the "Skynet is an agent app where raw shell is the exception" stance from shape §Philosophy; resolve the `identityMode` semantic-drift trap (shape §What would make it wrong item #5) by renaming the local state var.

Output:
- CreateRoleDialog blurb byte-exact revised
- NewSessionDialog agent blurb defaultValue byte-exact set
- NewSessionDialog Path field non-rendered when !isAdmin (JSX gate)
- NewSessionDialog identity-mode checkbox non-rendered when !isAdmin (JSX gate)
- NewSessionDialog checkbox label byte-exact "Just a shell — no agent" (U+2014 em-dash)
- Local state variable `identityMode` renamed to `shellOnly`; default flipped from `true` to `false`; close-reset flipped from `setIdentityMode(true)` to `setShellOnly(false)`; all local read-sites updated to consume the new semantic (see §Rename mapping in Task 2)
- Public callback-payload discriminant `identityMode` in NewSessionOnCreateOpts type + at the two onCreate call sites (L720 and L1147) UNCHANGED — this is a public wire contract
- Submit-onclick handler at L1131-1153 gates the regular-session branch on `isAdmin && shellOnly` (defense-in-depth)
- handleBirth at L631 sends `path: !isAdmin ? "" : normalizedPath` (empty-string non-admin submit triggers backend `~/<name>/` substitution)
- `npx tsc --noEmit` clean
- Scoped tests deferred to Plan 88-03 (which rewrites the 16+ `identityMode` test refs in NewSessionDialog.test.tsx and the sibling test files, plus the blurb assertion in CreateRoleDialog.test.tsx). Wave 2's tsc-clean and grep assertions verify the source edits without asserting test pass — the existing tests will break on Wave 2 landing (label regex, default-checked assumption, blurb text). Plan 88-03 restores green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-CONTEXT.md
@.planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-PATTERNS.md
@.planning/shapes/shape-create-agent-modal-ux-pass.md
@.planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-01-PLAN.md
@src/ui/sidebar/CreateRoleDialog.tsx
@src/ui/sidebar/NewSessionDialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revise the CreateRoleDialog blurb to the new two-sentence form (paired with Task 2's agent blurb)</name>
  <files>src/ui/sidebar/CreateRoleDialog.tsx</files>
  <read_first>
    - src/ui/sidebar/CreateRoleDialog.tsx (specifically L512-530 — the DialogHeader block with DialogTitle at L513, the Phase-84 rationale comment at L514-525, and the DialogDescription blurb at L527-529 with the current text "A role is what an agent does and how it thinks — many agents can share one.")
    - .planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-CONTEXT.md §Verbatim copy Role blurb — LOCKED byte-exact: "Roles are the expertise your agents adopt. Every agent using this role inherits its goals, rules, and knowledge."
    - .planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-PATTERNS.md §1 (the exact slot + wrapper-element invariant: keep <DialogDescription> wrapper for aria-describedby a11y wiring, do NOT swap for <p> or <span>)
    - .planning/shapes/shape-create-agent-modal-ux-pass.md §Shape (paired blurb section — the shared verb "adopt" and the paired-vocabulary rationale)
  </read_first>
  <action>
    Single edit inside CreateRoleDialog.tsx: replace the blurb text at L528 (inside the DialogDescription block at L527-529). Preserve the <DialogDescription> wrapper element (per PATTERNS.md §1 landmine — the shadcn Dialog primitive needs the description slot for aria-describedby a11y wiring).

    ─────────────────────────────────────────────────────────────
    THE EDIT
    ─────────────────────────────────────────────────────────────

    Locate the current text on L528:

      A role is what an agent does and how it thinks — many agents can share one.

    Replace with (BYTE-EXACT — em-dashes in Alice's greenlit text are absent; two periods, one mid-sentence and one terminal):

      Roles are the expertise your agents adopt. Every agent using this role inherits its goals, rules, and knowledge.

    Preserve the two-line indentation shape of L527-529 verbatim — only the text on L528 changes.

    ─────────────────────────────────────────────────────────────
    UPDATE the Phase-84 rationale comment block at L514-525
    ─────────────────────────────────────────────────────────────

    The old comment at L516-519 names "the future create-agent blurb" and previews "An agent is one specific worker doing a role, with its own name and history." — that preview text is NOT what shipped in Task 2 of this plan; the actual sibling copy is "Agents are the workers you chat with. Each one adopts a role that shapes what they know and how they help." Update the L514-525 comment block by either:

    (a) Deleting the old two Phase-84 rationale blocks entirely and adding a fresh Phase-88 rationale comment (recommended per PATTERNS.md §1 landmine "Update or replace the Phase 84 rationale block") that:
      - Anchors on the phrase "Phase 88"
      - Names the sibling copy (byte-exact from Task 2's edit) and where it lands (NewSessionDialog.tsx L781-783 in `startDescription`)
      - Names the shared verb "adopt" as the paired-vocabulary anchor
      - Preserves the aria-describedby a11y wiring rationale for the <DialogDescription> wrapper

    OR

    (b) Preserving the old two blocks and appending a marker comment (e.g. `// Phase 88: blurb revised to two-sentence paired-vocabulary form — see NewSessionDialog.tsx L781-783 for the sibling agent copy.`) between them and the DialogDescription element. This is lower-churn but leaves obsolete Phase-84-era preview text visible; prefer (a) unless the byte-count of comment lines matters for a diff-review reason.

    In either case, the FINAL rendered <DialogDescription> element and the visible blurb text are byte-identical to the target above.

    ─────────────────────────────────────────────────────────────
    ANTI-PATTERNS TO AVOID
    ─────────────────────────────────────────────────────────────

    - Do NOT replace the <DialogDescription> wrapper with <p> or <span> — the shadcn Dialog primitive expects the description slot for aria-describedby.
    - Do NOT introduce a new i18n key like `nav.createRoleBlurb`. Per 88-CONTEXT.md §prior_context "Copy-guard" invariant (Phase 84 established the in-place-defaultValue-edit-only rule; Phase 88 pairs), this blurb is a bare JSX string literal inside <DialogDescription>. Downstream translators pick it up on the normal cadence.
    - Do NOT change the ASCII text "your agents" to "user agents" or any other paraphrase. The verb is "adopt" (deliberate paired vocabulary with the agent blurb); Alice greenlit the exact string per 88-CONTEXT.md §Verbatim copy.
    - Do NOT touch any other line in CreateRoleDialog.tsx. The single-host guard at L303-357 (Phase 84 shipped), the ROLE_NAME_PATTERN validation, the handleSubmit flow, the onChainToCreateIdentity callback wiring, the DialogTitle, the DialogFooter — all stay byte-identical.
    - Do NOT delete the "Phase 84" anchor phrase from the file entirely — the (a) option's fresh Phase-88 comment can name-drop Phase 84 as the origin phase for the paired-blurb pattern; the (b) option preserves the anchor natively. Either way `grep -c 'Phase 84' src/ui/sidebar/CreateRoleDialog.tsx` should still return ≥ 1 after the edit (existing L28-31 top-of-file Phase 84 comment header is untouched by this task).
  </action>
  <acceptance_criteria>
    Blurb revision is complete when every check below holds:

    Grep source assertions on `src/ui/sidebar/CreateRoleDialog.tsx`:
    - `grep -c 'Roles are the expertise your agents adopt' src/ui/sidebar/CreateRoleDialog.tsx` returns 1 (new blurb present, byte-exact opening clause)
    - `grep -c 'Every agent using this role inherits its goals, rules, and knowledge' src/ui/sidebar/CreateRoleDialog.tsx` returns 1 (new blurb present, byte-exact closing clause)
    - `grep -c 'A role is what an agent does and how it thinks' src/ui/sidebar/CreateRoleDialog.tsx` returns 0 (old blurb gone)
    - `grep -c '<DialogDescription>' src/ui/sidebar/CreateRoleDialog.tsx` returns ≥ 1 (wrapper element preserved)
    - `grep -c 'Phase 88' src/ui/sidebar/CreateRoleDialog.tsx` returns ≥ 1 (rationale-comment anchor)
    - `grep -c 'Phase 84' src/ui/sidebar/CreateRoleDialog.tsx` returns ≥ 1 (origin-phase anchor preserved — either in a Phase-88 comment referencing Phase 84 as the paired-blurb origin OR in the untouched top-of-file Phase-84 comment header)

    Structural / build-level:
    - `npx tsc --noEmit` completes with no new errors mentioning CreateRoleDialog.tsx

    Test-command assertion:
    - `npx vitest run src/ui/sidebar/CreateRoleDialog.test.tsx --run` exits NON-ZERO on Test 11's blurb regex (expected — Plan 88-03 updates the regex). This is an EXPECTED failure at Wave 2 landing; verify the failure is exclusively the blurb-regex assertion in Test 11 at CreateRoleDialog.test.tsx L297-304 by running the command and confirming no other tests failed. Any additional failure is a regression and must be fixed before proceeding.
  </acceptance_criteria>
  <verify>
    <automated>set -e; grep -c 'Roles are the expertise your agents adopt' src/ui/sidebar/CreateRoleDialog.tsx | grep -q '^1$' && grep -c 'Every agent using this role inherits its goals, rules, and knowledge' src/ui/sidebar/CreateRoleDialog.tsx | grep -q '^1$' && [ "$(grep -c 'A role is what an agent does and how it thinks' src/ui/sidebar/CreateRoleDialog.tsx)" = "0" ] && grep -q '<DialogDescription>' src/ui/sidebar/CreateRoleDialog.tsx && grep -q 'Phase 88' src/ui/sidebar/CreateRoleDialog.tsx && grep -q 'Phase 84' src/ui/sidebar/CreateRoleDialog.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>
    - CreateRoleDialog.tsx L527-529 DialogDescription content byte-exactly matches the new two-sentence blurb
    - <DialogDescription> wrapper element preserved (aria-describedby a11y wiring intact)
    - Phase-84 preview-text comment at L516-519 is either updated to reference the actual shipped sibling copy or replaced with a fresh Phase-88 rationale comment
    - `npx tsc --noEmit` clean
    - Old blurb text fully removed from source
    - Test 11 in CreateRoleDialog.test.tsx expected to fail on regex mismatch until Plan 88-03 updates it — this is expected at Wave 2 landing
  </done>
</task>

<task type="auto">
  <name>Task 2: Land all six substantive edits to NewSessionDialog.tsx — agent blurb, Path admin-gate, checkbox admin-gate + label flip, `identityMode` local state rename to `shellOnly` + default flip, submit-onclick invariant, handleBirth path-clear for non-admin</name>
  <files>src/ui/sidebar/NewSessionDialog.tsx</files>
  <read_first>
    - src/ui/sidebar/NewSessionDialog.tsx (entire ~1158-line file — this task touches six regions and depends on the WHOLE surface; read at least: L1-30 top-of-file comment header for the Phase-88 addendum insertion; L27 comment on `identityMode`; L120-158 for the NewSessionOnCreateOpts type — the identityMode discriminant on lines 138-158 is PUBLIC and stays UNCHANGED; L277-312 for the destructured props signature (Plan 88-01 already added isAdmin here); L322 for the path useState default; L324-326 for the identityMode state hook to rename + flip; L405-419 for the chain-prefill open-effect that reads identityMode; L436 for the close-reset that flips default; L485-529 for the roles-fetch effect that reads identityMode; L546-581 for the pickPoolName effect; L631 for the handleBirth normalizePath call site (non-admin path-clear ships here); L644-720 for the handleBirth openBirthStream call chain and the onCreate call at L716-722 with `identityMode: true` PUBLIC discriminant; L747-751 for the canOpen predicate; L781-783 for the startDescription defaultValue (agent blurb replacement); L816-819 for the DialogHeader block (only the {startDescription} slot is affected, no wrapper changes); L898 for the regular-session name-input render gate; L926-942 for the Path field (admin-gate wrap target); L944-960 for the checkbox block (admin-gate wrap + label flip target); L963 for the identity-cluster render gate; L967 + L1036 for descriptive comments; L1119 for identityPath prop passed down; L1131-1153 for the submit-onclick handler (invariant enforcement target) + the PUBLIC identityMode discriminant at L1147)
    - .planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-CONTEXT.md — the FULL §decisions block, especially: Verbatim label (LOCKED), Path field admin-gate (LOCKED), Identity-mode checkbox admin-gate + inversion (LOCKED including semantic-drift trap resolution), isAdmin prop plumbing (Plan 88-01 already landed the prop), Backend birth endpoint change (this task's Edit F sends empty-string for non-admin to consume Plan 88-01's server-side substitution). Also read §risks: paired-blurb constraint, non-admin invariant, identityMode drift trap.
    - .planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-PATTERNS.md §2a (blurb slot), §2b (Path gate), §2c (checkbox gate + label flip), §2d (default flip), §2e (rename recommendation with the FULL 16-reference enumeration — L27, L123, L139, L144, L151, L326, L405, L416-419, L436, L485, L529, L546, L556, L581, L712, L720, L747, L751, L898, L949, L963, L967, L1036, L1138, L1147 — the PATTERNS list is exhaustive; consult before editing), §2f (isAdmin prop — already landed in Plan 88-01, do NOT re-add), §2g (submit-path invariant), §2h (path-clear at handleBirth submit — this task picks the frontend-side complement to Plan 88-01's backend narrow: send `""` for non-admin so backend substitutes `~/<name>/`)
    - .planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-01-PLAN.md (Task 1 Edit A summary — the isAdmin prop signature this task now consumes; Task 2 Edit summary — the backend narrow this task's Edit F sends empty-string to)
    - .planning/shapes/shape-create-agent-modal-ux-pass.md §What would make it wrong item #5 (semantic-drift trap resolution — this task's Edit D-1 through D-3 IS the resolution)
  </read_first>
  <action>
    Six coordinated edits inside NewSessionDialog.tsx, ordered top-to-bottom of file so line numbers stay predictable during the edit session. This is a substantial single-file rename + gate + label + default-flip pass; commit as one atomic change per the paired-blurb constraint from shape §What would make it wrong ("Ship agent blurb without revising role blurb" = visible inconsistency — Task 1 role blurb + Task 2's Edit A agent blurb ship together).

    ─────────────────────────────────────────────────────────────
    RENAME MAPPING (used across Edits D-1, D-2, D-3, E, F and referenced throughout Edits B, C)
    ─────────────────────────────────────────────────────────────

    The LOCAL state variable `identityMode` (state hook + close-reset + all local read-sites at L27 comment, L405, L416-419, L436, L485, L529, L546, L556, L581, L712, L747, L751, L898, L949, L963, L967, L1036, L1138) is RENAMED to `shellOnly`. The SEMANTIC also FLIPS as a consequence of the checkbox-default flip:

      old `identityMode === true`  → checkbox CHECKED → identity/agent mode ON (was default)
      old `identityMode === false` → checkbox UNCHECKED → raw shell mode
      new `shellOnly === true`     → checkbox CHECKED → raw shell mode (opt-in, admin-only)
      new `shellOnly === false`    → checkbox UNCHECKED → agent mode ON (new default)

    Consequently, EVERY read-site of the old `identityMode` needs BOTH a name change AND a boolean invert:

      old code                                   →   new code
      -----------------------------------------      ---------------------------------
      identityMode                               →   !shellOnly
      !identityMode                              →   shellOnly
      identityMode && <foo>                      →   !shellOnly && <foo>
      !identityMode && <foo>                      →   shellOnly && <foo>
      identityMode: true (public payload key)    →   identityMode: true (UNCHANGED — public wire contract)
      identityMode: false (public payload key)   →   identityMode: false (UNCHANGED — public wire contract)

    KEEP the public callback-payload discriminant unchanged in these THREE places (per PATTERNS.md §2e "Rename recommendation" + 88-CONTEXT.md §Identity-mode checkbox: "the callback-payload discriminant of the same name is a public contract consumed by AppShell.tsx:2040-2044"):

      - L123-158: the type definition of NewSessionOnCreateOpts — three variants with `identityMode: false | true | "existing"` as the discriminant literal. UNCHANGED.
      - L716-722: the handleBirth success `onCreate({..., identityMode: true, ...})` — UNCHANGED.
      - L1143-1148: the regular-session submit-onclick `onCreate({..., identityMode: false, ...})` — UNCHANGED.

    All other identityMode identifiers in NewSessionDialog.tsx are LOCAL — they rename to `shellOnly` (with inverted boolean semantics per the table above).

    ─────────────────────────────────────────────────────────────
    EDIT A — Agent blurb: replace startDescription defaultValue at L781-783
    ─────────────────────────────────────────────────────────────

    Locate the current binding at L781-783:

      const startDescription = t("nav.newSessionDescription", {
        defaultValue: "Pick a host and (optionally) name the agent.",
      });

    Replace the `defaultValue` string with the LOCKED byte-exact agent blurb from 88-CONTEXT.md §Verbatim copy:

      Agents are the workers you chat with. Each one adopts a role that shapes what they know and how they help.

    Precede the binding with a one-line Phase-88 anchor comment naming the sibling copy target (CreateRoleDialog.tsx L527-529 revised in this plan's Task 1) and the shared verb "adopt". Do NOT create a new i18n key; per 88-CONTEXT.md §Verbatim copy + Phase 84 Copy-guard (see 88-PATTERNS.md §2a), edits happen in-place on the `defaultValue`.

    Do NOT modify the DialogHeader render at L816-819 — `<DialogDescription>{startDescription}</DialogDescription>` reads the updated defaultValue automatically. The wrapper element stays; no JSX-level change to the header itself.

    ─────────────────────────────────────────────────────────────
    EDIT B — Path field admin-gate wrap at L926-942
    ─────────────────────────────────────────────────────────────

    Wrap the entire <div className="flex flex-col gap-1.5">…</div> block spanning L927-942 in `{isAdmin && (…)}`. The `isAdmin` identifier is destructured from props by Plan 88-01 and is already in scope.

    Add a preceding comment block (immediately BEFORE the new `{isAdmin && (` gate) that:
    - Anchors on "Phase 88"
    - Names the source-of-truth admin-gate idiom this mirrors: PrettyConversationsPanel.tsx:1651 (`{isAdmin && <WeeklyUsageMeter />}`)
    - Names the fail-closed default: when the caller forgets to pass `isAdmin`, Plan 88-01's `isAdmin = false` destructure default means non-admin behavior applies (field not rendered)
    - Names the wire flow to Plan 88-01's backend narrow: Edit F in this plan clears `path` to empty-string on non-admin submit, which triggers the backend `~/<name>/` substitution

    Preserve the inner JSX byte-for-byte — do NOT change the label text "Path", the `htmlFor="new-session-path"`, the `id="new-session-path"`, the `aria-label="Path"`, the `placeholder="~/"`, the `disabled={formDisabled}`, or the two className strings on the label and input.

    The existing "Path field — visible in BOTH modes, below host list + above identity-mode checkbox" comment at L926 becomes misleading after this edit (the field is no longer visible in both modes — it's admin-only). Either delete that comment or rewrite it to "Path field — visible ONLY when isAdmin is truthy; non-admin agents get a per-agent `~/<name>/` working directory computed at Plan 88-01's backend narrow when body.path is empty."

    ─────────────────────────────────────────────────────────────
    EDIT C — Identity-mode checkbox admin-gate wrap + label flip at L944-960
    ─────────────────────────────────────────────────────────────

    (C.1) Wrap the entire <div className="flex items-center gap-2">…</div> block at L945-960 in `{isAdmin && (…)}` — same idiom as Edit B. Fail-closed via Plan 88-01's `isAdmin = false` destructure default.

    (C.2) Change the label text at L958 from:

      Create with new identity

    to (LOCKED byte-exact from 88-CONTEXT.md §Verbatim label; use Unicode U+2014 em-dash, NOT an ASCII hyphen or en-dash):

      Just a shell — no agent

    (C.3) The checkbox `checked` binding at L949 currently reads `checked={identityMode}`. Under the rename mapping this becomes `checked={shellOnly}` (naked read of the renamed state variable — no boolean invert because the semantic is now aligned: `shellOnly === true` = checkbox CHECKED). See Edit D for the state hook rename that makes this valid.

    (C.4) The `onChange` handler at L950 currently reads `onChange={(e) => !formDisabled && setIdentityMode(e.target.checked)}`. Under the rename mapping this becomes `onChange={(e) => !formDisabled && setShellOnly(e.target.checked)}` (naked setter rename — no boolean invert).

    (C.5) Add a preceding comment block (before the new `{isAdmin && (` gate) that:
    - Anchors on "Phase 88"
    - Names the LOCKED verbatim label per 88-CONTEXT.md §Verbatim label
    - References Edit D for the state var rename + default flip rationale
    - Names the source-of-truth admin-gate idiom: PrettyConversationsPanel.tsx:1651

    The `htmlFor="new-session-identity-mode"` and `id="new-session-identity-mode"` attributes stay byte-identical — do NOT rename the DOM `id` to match the state variable rename. The `id` is a stable HTML anchor for the label association; renaming it would break any test that queries by `getByRole("checkbox", { name: /just a shell.*no agent/i })` OR future selectors keyed on the DOM id. The disconnect between "DOM id says identity-mode" and "state var says shellOnly" is acceptable — the id is not user-visible and does not participate in the semantic invariant.

    ─────────────────────────────────────────────────────────────
    EDIT D — Rename local state variable `identityMode` → `shellOnly`; flip default `true` → `false`; flip close-reset `setIdentityMode(true)` → `setShellOnly(false)`; update all LOCAL read-sites
    ─────────────────────────────────────────────────────────────

    (D.1) STATE HOOK at L324-326. Replace:

      // Identity-mode checkbox (defaults ON per IDUI-01).
      // When on, reveals the identity-birth field cluster.
      const [identityMode, setIdentityMode] = useState(true);

    With (default flipped `true` → `false`; variable renamed; semantic-invariant comment inline per PATTERNS.md §2e recommendation):

      // Phase 88 (88-CONTEXT.md §Identity-mode checkbox): local state variable
      // renamed from `identityMode` to `shellOnly` to resolve the semantic-
      // drift trap (shape §What would make it wrong item #5). The checkbox is
      // now labeled "Just a shell — no agent" (admin-only, gated in Edit C)
      // and defaults UNCHECKED. Semantic invariant:
      //   shellOnly === true  → checkbox CHECKED → user opting into raw shell
      //   shellOnly === false → checkbox UNCHECKED → agent mode (new default)
      // The PUBLIC callback-payload discriminant `identityMode` at
      // NewSessionOnCreateOpts (L123-158) and at the two onCreate call sites
      // (L716-722, L1143-1148) is UNCHANGED — it is a wire contract consumed
      // by AppShell.tsx:2040-2044 (PATTERNS.md §2e).
      const [shellOnly, setShellOnly] = useState(false);

    (D.2) CLOSE-RESET at L436. Change:

      setIdentityMode(true);

    To:

      setShellOnly(false);

    Note both the setter rename AND the boolean flip — the pre-Phase-88 reset re-armed the default (true meant identity mode ON, which was the default); the post-Phase-88 reset also re-arms the default (false means shell-only OFF = agent mode = the new default).

    (D.3) LOCAL READ-SITES — apply the RENAME MAPPING (name change + boolean invert) at each of these locations. Per PATTERNS.md §2e's enumerated list, these are all the LOCAL identityMode references — the PUBLIC discriminant sites (L123, L139, L144, L151 in NewSessionOnCreateOpts type; L720 in handleBirth onCreate; L1147 in regular-session submit onCreate) are explicitly EXCLUDED from the rename.

    Line-by-line (line numbers pre-edit; may shift as earlier edits land):
      - L27 (comment): update from "When Create fires with identityMode=true, opens an SSE stream…" to "When Create fires without shellOnly (or with !shellOnly), opens an SSE stream…" — reflect the new default semantic.
      - L405-419 (chain-prefill open-effect): the branch at L416-419 currently reads `if (initialRole && identityMode)`. Rename to `if (initialRole && !shellOnly)` (boolean invert applied — agent mode was previously `identityMode` truthy, now agent mode is `shellOnly` falsy).
      - L485: `if (!selectedHost || !identityMode) { return; }` becomes `if (!selectedHost || shellOnly) { return; }` (roles-fetch effect skips fetch when agent mode is OFF — old: `!identityMode` = shell mode; new: `shellOnly` = shell mode).
      - L529: `}, [selectedHost, identityMode]);` becomes `}, [selectedHost, shellOnly]);` (dependency array rename — no boolean invert on identifiers in dep arrays; naked rename).
      - L546-556 (pickPoolName effect): comment at L546 rename; the guard at L556 `if (!identityMode || !selectedRole || !selectedHost) return;` becomes `if (shellOnly || !selectedRole || !selectedHost) return;`.
      - L581: `}, [selectedRole, selectedHost, identityMode]);` becomes `}, [selectedRole, selectedHost, shellOnly]);`.
      - L712 (comment in handleBirth): update from "shape (see NewSessionOnCreateOpts identityMode:true variant)." to "shape (see NewSessionOnCreateOpts identityMode:true variant — PUBLIC payload key unchanged from Phase 88; local `shellOnly` state variable is the renamed store)."
      - L747: `const nameValid = identityMode` becomes `const nameValid = !shellOnly` (agent-mode validation branch).
      - L751: `const canOpen = !birthing && (identityMode` becomes `const canOpen = !birthing && (!shellOnly` (agent-mode canOpen branch — the parenthesized ternary continues into the shell-mode branch on L759).
      - L898: `{!identityMode && (` becomes `{shellOnly && (` (regular-session name-input render gate — old: `!identityMode` = shell mode; new: `shellOnly` = shell mode).
      - L963: `{identityMode && (` becomes `{!shellOnly && (` (identity-cluster render gate — the whole identity-birth field cluster renders in agent mode).
      - L967 (comment inside the identity-cluster block): update from "identityMode is ON" to "agent mode is ON (i.e. `!shellOnly`, the default when the admin-only checkbox is unchecked or non-admin never sees it)".
      - L1036 (comment): update any reference to "gated on identityMode along with the whole cluster" to "gated on !shellOnly along with the whole cluster (agent-mode gate)".
      - L1138: `if (identityMode) {` inside the submit-onclick becomes part of Edit E's invariant enforcement (see Edit E below — this line is REWRITTEN as `if (!(isAdmin && shellOnly))`, not just renamed). Do not touch it in Edit D; Edit E owns it.

    (D.4) Verify no naked `identityMode` identifier remains in NewSessionDialog.tsx OUTSIDE of the four public-payload-discriminant sites (L123, L139, L144, L151 in the type; L720 in the handleBirth onCreate; L1147 in the regular-session onCreate). Verification grep in Task 2's acceptance_criteria isolates the public sites from local read-sites.

    ─────────────────────────────────────────────────────────────
    EDIT E — Submit-onclick invariant enforcement at L1131-1153
    ─────────────────────────────────────────────────────────────

    Locate the onClick handler currently at L1135-1150:

      onClick={() => {
        if (!canOpen || !selectedHost) return;
        const normalizedPath = normalizePath(path);
        if (identityMode) {
          // Identity-mode ON: start birth stream
          void handleBirth();
        } else {
          // Identity-mode OFF: existing regular-session contract + path
          onCreate({
            host: selectedHost,
            sessionName: sessionName.length > 0 ? sessionName : undefined,
            path: normalizedPath,
            identityMode: false,
          });
        }
      }}

    Rewrite the branch condition to enforce the non-admin invariant per PATTERNS.md §2g + 88-CONTEXT.md §Non-admin invariant:

      onClick={() => {
        if (!canOpen || !selectedHost) return;
        const normalizedPath = normalizePath(path);

        // Phase 88 invariant (88-CONTEXT.md §Non-admin invariant, defense-in-
        // depth): non-admin users cannot spawn a raw shell. The `shellOnly`
        // checkbox is admin-gated (never renders when !isAdmin — see Edit C)
        // and the local state defaults to `false`. Enforce again at the
        // submit path: even if some future caller passes a stale `shellOnly=
        // true` local state (or the JSX gate is bypassed by a rendering bug),
        // the branch is only taken when BOTH isAdmin AND shellOnly are true.
        // Only admins who explicitly checked the "Just a shell — no agent"
        // checkbox reach the regular-session branch below.
        const effectiveShellOnly = isAdmin && shellOnly;

        if (!effectiveShellOnly) {
          // Agent mode (default for non-admin, and default for admin unless
          // the shell-only checkbox is explicitly checked): start birth
          // stream. The birth stream's SSE handler at handleBirth (L610+)
          // consumes handleBirth's payload where the wire discriminant
          // `identityMode: true` is set at L720 — that PUBLIC payload key
          // is unchanged from Phase 88.
          void handleBirth();
        } else {
          // Admin explicitly opted into raw shell. Preserve the existing
          // regular-session contract — the PUBLIC callback-payload
          // discriminant `identityMode: false` on L1147 is UNCHANGED and
          // remains the AppShell.tsx:2040-2044 narrowing signal for
          // regular-session vs identity-mode. Path passes through as
          // normalizedPath (admin's `useState("~/")` default + any override).
          onCreate({
            host: selectedHost,
            sessionName: sessionName.length > 0 ? sessionName : undefined,
            path: normalizedPath,
            identityMode: false,
          });
        }
      }}

    The two branches' bodies (handleBirth call, regular-session onCreate object) are byte-identical to pre-Phase-88 CONTENT — only the branch predicate changed from `identityMode` (local var) to `!effectiveShellOnly` (invariant-enforced local var).

    ─────────────────────────────────────────────────────────────
    EDIT F — handleBirth path-clear at L631 for non-admin
    ─────────────────────────────────────────────────────────────

    Locate the `normalizedPath` computation at handleBirth L631:

      const normalizedPath = normalizePath(path);

    This is fine for admin submits — admin has the Path field visible (Edit B admin-gate), `path` state defaults to `"~/"` (L322), and normalizePath maps that through. But for non-admin, the Path field is not rendered (Edit B), the `path` state remains at its initial `"~/"` value (default from L322) unless the user typed into a field that doesn't exist. If handleBirth sends `path: "~/"`, the backend narrow from Plan 88-01 Task 2 sees a NON-empty string and returns `"~/"` unchanged — the `~/<name>/` substitution never fires.

    To make the backend substitution effective for non-admin, this task's Edit F sends `path: ""` when `!isAdmin`:

    Locate the `openBirthStream` invocation currently at L641-660 (approximate — the exact line depends on where handleBirth's args start). Find the line:

      path: normalizedPath,

    (currently at L645 per PATTERNS.md §2h and the read_first verification). Replace with:

      // Phase 88: non-admin submissions send empty-string `path` so the
      // backend narrow at identity-birth.ts:206 (Plan 88-01 Task 2) substitutes
      // `~/<name>/` server-side — non-admin agents each get their own working
      // directory named after themselves per shape §Philosophy. Admin submits
      // send the normalized field value (`"~/"` default from L322, or any
      // override the admin typed into the Path input at L926-942).
      path: isAdmin ? normalizedPath : "",

    Do NOT change any other field on the openBirthStream call — `hostId`, `name`, `colorHue`, `voice`, `role`, `title`, `task`, `avatarCandidateId`, etc. all pass through unchanged. Do NOT skip `normalizePath` in the admin branch — admin's typed path may need whitespace-trim and tilde-normalization (see the normalizePath function at L97 for what it does).

    Do NOT touch the sibling regular-session submit path at L1143-1148 (`path: normalizedPath` there stays byte-identical — that branch is only reachable for admins per Edit E's invariant, so admin's explicit path is what should go on the wire).

    ─────────────────────────────────────────────────────────────
    TOP-OF-FILE COMMENT HEADER ADDENDUM (optional but recommended)
    ─────────────────────────────────────────────────────────────

    Before the L27 comment (or in a location matching the file's existing Phase-N comment-header convention at L1-30), insert a single Phase 88 anchor comment naming the six edits + the paired-blurb constraint + the identityMode rename + the isAdmin dependency (Plan 88-01). Similar in shape to what Plan 84-01 did for the CreateRoleDialog header per 84-01-PLAN.md CHANGE G. Anchor phrase "Phase 88" and reference "shellOnly" and "isAdmin" for grep-based verification.

    ─────────────────────────────────────────────────────────────
    ANTI-PATTERNS TO AVOID
    ─────────────────────────────────────────────────────────────

    - Do NOT rename the four PUBLIC identityMode discriminant sites (L123, L139, L144, L151 in NewSessionOnCreateOpts; L720 in handleBirth onCreate; L1147 in regular-session onCreate). AppShell.tsx:2040-2044 narrows on this discriminant name; renaming would break the AppShell narrow and require a coordinated cross-file rename that shape §Scope explicitly deferred to a future bounty.
    - Do NOT skip the boolean invert step during the rename. Every LOCAL `identityMode` read-site's boolean semantic FLIPS as a consequence of the checkbox-default flip. The rename mapping table at the top of this action block is the source of truth. Missing an invert produces a subtle behavioral bug (e.g. agent-cluster renders in shell mode and vice-versa) that will not be caught by `tsc --noEmit`.
    - Do NOT rename the DOM `id="new-session-identity-mode"` to `id="new-session-shell-only"`. Rename the STATE variable, not the HTML anchor. See Edit C.
    - Do NOT touch the checkbox label's Unicode em-dash — U+2014 (—), NOT U+2013 (–) or U+002D (-). The label text is byte-exact per 88-CONTEXT.md §Verbatim label.
    - Do NOT combine Edit E's invariant `if (!(isAdmin && shellOnly))` into a single-expression conditional with the pre-guard `if (!canOpen || !selectedHost)`. Keep the two `if` statements distinct — the pre-guard rejects unsubmittable state; the invariant routes the submit branch. Combining them muddies the intent.
    - Do NOT introduce a `useMemo` or `useCallback` around the `effectiveShellOnly` computation in Edit E. It's a two-boolean AND that runs once per click; the memoization overhead exceeds the runtime cost.
    - Do NOT touch the identityMode reference at the L124-158 type definition. That's the NewSessionOnCreateOpts three-way discriminated union — public contract. Renaming would break AppShell.tsx narrowing.
    - Do NOT skip the trailing-comma / semicolon nuances of the rewrite. The onClick handler at L1135-1150 is inside an object attribute value; TypeScript / React syntax is strict about the arrow body's shape. Follow the exact shape of Edit E's replacement block.
    - Do NOT delete or reword the L738 catch-block error path — it's orthogonal to Phase 88 and covers birth-stream network errors.
    - Do NOT touch normalizePath at L97 or its behavior. Edit F changes what the CALLER at L645 sends; the utility is unchanged. Per PATTERNS.md §2h landmine, do NOT change normalizePath's empty-string mapping either — the caller-side `isAdmin ? normalizedPath : ""` bypasses normalizePath for the non-admin branch cleanly.
    - Do NOT ship the agent blurb (Edit A) without shipping Task 1's role blurb revision. Per shape §What would make it wrong: paired update, or visible inconsistency. Both blurbs land in the same commit or contiguous commits on the same deploy — Wave 2 lands both files together.
  </action>
  <acceptance_criteria>
    All six edits complete when every check below holds. Grep source assertions on `src/ui/sidebar/NewSessionDialog.tsx`:

    EDIT A (agent blurb):
    - `grep -c 'Agents are the workers you chat with' src/ui/sidebar/NewSessionDialog.tsx` returns 1
    - `grep -c 'Each one adopts a role that shapes what they know and how they help' src/ui/sidebar/NewSessionDialog.tsx` returns 1
    - `grep -c 'Pick a host and (optionally) name the agent' src/ui/sidebar/NewSessionDialog.tsx` returns 0 (old default gone)
    - `grep -c 'nav.newSessionDescription' src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 1 (i18n key preserved — in-place defaultValue edit only)

    EDIT B + EDIT C (admin gates):
    - `grep -cE '\{isAdmin && \(' src/ui/sidebar/NewSessionDialog.tsx` returns 2 (Path field + checkbox — the two admin-gates)
    - `grep -c 'aria-label="Path"' src/ui/sidebar/NewSessionDialog.tsx` returns 1 (Path field inner JSX preserved byte-for-byte inside the new gate)
    - `grep -c 'new-session-identity-mode' src/ui/sidebar/NewSessionDialog.tsx` returns 2 (DOM `id` and `htmlFor` on the label — DOM anchors NOT renamed per Edit C anti-pattern; check both attributes present)
    - `grep -c 'Just a shell — no agent' src/ui/sidebar/NewSessionDialog.tsx` returns 1 (new label text with U+2014 em-dash)
    - `grep -c 'Create with new identity' src/ui/sidebar/NewSessionDialog.tsx` returns 0 (old label gone)

    EDIT D (state var rename + default flip):
    - `grep -cE 'useState\(false\).*// $|const \[shellOnly, setShellOnly\] = useState\(false\)' src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 1 — the exact substring `const [shellOnly, setShellOnly] = useState(false)` (check the simpler literal: `grep -c 'const \[shellOnly, setShellOnly\] = useState(false)' src/ui/sidebar/NewSessionDialog.tsx` returns 1)
    - `grep -c 'const \[identityMode, setIdentityMode\]' src/ui/sidebar/NewSessionDialog.tsx` returns 0 (old state hook gone)
    - `grep -c 'setShellOnly(false)' src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 1 (close-reset present)
    - `grep -c 'setIdentityMode(true)' src/ui/sidebar/NewSessionDialog.tsx` returns 0 (old close-reset gone)
    - `grep -cE 'checked=\{shellOnly\}' src/ui/sidebar/NewSessionDialog.tsx` returns 1 (checkbox `checked` binding renamed)
    - `grep -cE 'setShellOnly\(e\.target\.checked\)' src/ui/sidebar/NewSessionDialog.tsx` returns 1 (checkbox `onChange` setter renamed)
    - Public payload discriminant preserved: `grep -c 'identityMode: true' src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 1 (handleBirth onCreate at L720); `grep -c 'identityMode: false' src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 1 (regular-session onCreate at L1147 unchanged)
    - Public type discriminant preserved: `grep -cE 'identityMode: (false|true|"existing")' src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 4 (three type-literal sites in NewSessionOnCreateOpts + at least one payload site)
    - No stray local `identityMode` naked reads: the file's remaining `identityMode` occurrences ONLY appear as OBJECT-KEY / TYPE-DISCRIMINANT contexts (i.e. `identityMode:` or `identityMode ===` in a switch on the type or code comments referencing the discriminant name). Manual audit or precise grep: `grep -nE '\bidentityMode\b' src/ui/sidebar/NewSessionDialog.tsx` output MUST NOT contain a line that reads `identityMode` as a NAKED expression (e.g. `if (identityMode)`, `!identityMode &&`, `identityMode ?`, `[…, identityMode]`) — every line must be an object key, a type literal, or a comment. The executor MUST inspect the grep output manually before considering this criterion satisfied.

    EDIT E (submit-onclick invariant):
    - `grep -c 'effectiveShellOnly' src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 1 (invariant local computed)
    - `grep -c 'isAdmin && shellOnly' src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 1 (invariant expression)
    - `grep -cE '(if \(identityMode\)|if \(\!identityMode\))' src/ui/sidebar/NewSessionDialog.tsx` returns 0 (old submit-onclick predicate gone)

    EDIT F (handleBirth path-clear):
    - `grep -cE 'isAdmin \? normalizedPath : ""' src/ui/sidebar/NewSessionDialog.tsx` returns 1 (the wire-side non-admin path-clear expression at L645)

    General:
    - `grep -c 'Phase 88' src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 5 (Edit A comment + Edit B gate comment + Edit C gate comment + Edit D state-hook comment + Edit E invariant comment + Edit F path-clear comment; at least five distinct Phase-88 anchor points)
    - `grep -c 'shellOnly' src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 15 (state hook declaration + close-reset + all local read-sites: L27-comment + L405/L416-419 chain-prefill + L436 close-reset + L485/L529 roles-fetch + L546-556/L581 pickPoolName + L747/L751 canOpen + L898 name-input gate + L949 checked binding + L950 onChange setter + L963 identity-cluster gate + L967/L1036 comments + L1131-1153 submit-onclick — approximately 15-20 occurrences)

    Structural / build-level:
    - `npx tsc --noEmit` completes with no new errors mentioning NewSessionDialog.tsx (rename produces no type errors because both the state var and its setter are renamed together and no external reader referenced the local var)

    Test-command assertion:
    - `npx vitest run src/ui/sidebar/NewSessionDialog.test.tsx src/ui/sidebar/NewSessionDialog.chain.test.tsx src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx src/ui/sidebar/NewSessionDialog.task-input.test.tsx src/ui/sidebar/CreateRoleDialog.test.tsx --run` is EXPECTED to fail on multiple test files after Wave 2 lands — these tests still reference the old label regex `/create with new identity/i`, still assume `identityMode` default = true, still assert the old blurb. Plan 88-03 rewrites them. At Wave 2 landing, the ONLY acceptable failures are ones that trace directly to those three pre-Phase-88 assumptions. If any test fails for a reason unrelated to those three (e.g. crash-on-mount, unrelated regression), that is a regression and must be fixed before proceeding to Plan 88-03.
  </acceptance_criteria>
  <verify>
    <automated>set -e; grep -q 'Agents are the workers you chat with' src/ui/sidebar/NewSessionDialog.tsx && grep -q 'Each one adopts a role that shapes what they know and how they help' src/ui/sidebar/NewSessionDialog.tsx && [ "$(grep -c 'Pick a host and (optionally) name the agent' src/ui/sidebar/NewSessionDialog.tsx)" = "0" ] && grep -c 'nav.newSessionDescription' src/ui/sidebar/NewSessionDialog.tsx | awk '$1 >= 1 { exit 0 } { exit 1 }' && [ "$(grep -cE '\{isAdmin && \(' src/ui/sidebar/NewSessionDialog.tsx)" = "2" ] && grep -q 'aria-label="Path"' src/ui/sidebar/NewSessionDialog.tsx && grep -q 'Just a shell — no agent' src/ui/sidebar/NewSessionDialog.tsx && [ "$(grep -c 'Create with new identity' src/ui/sidebar/NewSessionDialog.tsx)" = "0" ] && grep -q 'const \[shellOnly, setShellOnly\] = useState(false)' src/ui/sidebar/NewSessionDialog.tsx && [ "$(grep -c 'const \[identityMode, setIdentityMode\]' src/ui/sidebar/NewSessionDialog.tsx)" = "0" ] && grep -q 'setShellOnly(false)' src/ui/sidebar/NewSessionDialog.tsx && [ "$(grep -c 'setIdentityMode(true)' src/ui/sidebar/NewSessionDialog.tsx)" = "0" ] && grep -qE 'checked=\{shellOnly\}' src/ui/sidebar/NewSessionDialog.tsx && grep -c 'identityMode: true' src/ui/sidebar/NewSessionDialog.tsx | awk '$1 >= 1 { exit 0 } { exit 1 }' && grep -c 'identityMode: false' src/ui/sidebar/NewSessionDialog.tsx | awk '$1 >= 1 { exit 0 } { exit 1 }' && grep -q 'effectiveShellOnly' src/ui/sidebar/NewSessionDialog.tsx && grep -q 'isAdmin && shellOnly' src/ui/sidebar/NewSessionDialog.tsx && [ "$(grep -cE '(if \(identityMode\)|if \(\!identityMode\))' src/ui/sidebar/NewSessionDialog.tsx)" = "0" ] && grep -qE 'isAdmin \? normalizedPath : ""' src/ui/sidebar/NewSessionDialog.tsx && grep -c 'Phase 88' src/ui/sidebar/NewSessionDialog.tsx | awk '$1 >= 5 { exit 0 } { exit 1 }' && grep -c 'shellOnly' src/ui/sidebar/NewSessionDialog.tsx | awk '$1 >= 15 { exit 0 } { exit 1 }' && npx tsc --noEmit</automated>
  </verify>
  <done>
    - Edit A: Agent blurb byte-exact in startDescription defaultValue; old text gone
    - Edit B: Path field wrapped in `{isAdmin && (…)}`; inner JSX byte-identical; obsolete "visible in BOTH modes" comment updated
    - Edit C: Checkbox block wrapped in `{isAdmin && (…)}`; label text "Just a shell — no agent" with U+2014 em-dash; checkbox `checked` binding and `onChange` setter renamed to `shellOnly` / `setShellOnly`; DOM `id`/`htmlFor` preserved
    - Edit D: State hook renamed to `[shellOnly, setShellOnly]` with `useState(false)` default; close-reset flipped to `setShellOnly(false)`; ALL local read-sites at L27, L405, L416-419, L485, L529, L546-556, L581, L747, L751, L898, L963, L967, L1036 renamed with boolean-invert applied; public payload discriminant `identityMode` at type L123-158 and at onCreate call sites L720 + L1147 UNCHANGED
    - Edit E: Submit-onclick handler at L1131-1153 gates the regular-session branch on `isAdmin && shellOnly` (via `effectiveShellOnly` local); non-admin always routes to handleBirth
    - Edit F: handleBirth openBirthStream call at ~L645 sends `path: isAdmin ? normalizedPath : ""` — non-admin submits empty-string triggering Plan 88-01 backend narrow's `~/<name>/` substitution
    - `npx tsc --noEmit` clean (rename is tsc-consistent — state var + setter are both renamed and no external reader of the local var exists)
    - Scoped vitest expected to FAIL on the four NewSessionDialog test files + CreateRoleDialog Test 11 — these failures are Plan 88-03's remit and are expected at Wave 2 landing
    - Task 1 and Task 2 land in the same commit surface per shape §What would make it wrong paired-blurb constraint
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Non-admin user's browser DOM | With Edit B and Edit C's admin gates, non-admin users' DOM never contains a Path input, a shell-only checkbox, or the associated event handlers. The admin-only affordances are absent from the render tree, not merely CSS-hidden — reducing the client-side attack surface. |
| Non-admin submit handler → handleBirth | Edit F clears `path` to empty-string on non-admin submits, delegating the working-directory computation to Plan 88-01's server-authoritative substitution. Client-supplied path is not trusted for non-admin. |
| Local state `shellOnly` → submit-onclick invariant | Edit E's defense-in-depth invariant `isAdmin && shellOnly` ensures that even if a rendering bug caused the checkbox to render for non-admin, or if a stale state value slipped through, the submit path routes to handleBirth (agent branch) — never to the regular-session onCreate (shell branch). |
| Public callback payload `identityMode` discriminant | UNCHANGED. AppShell.tsx:2040-2044's narrowing on `identityMode: true | false | "existing"` still receives the same wire shape. No cross-file rename risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-02-01 | Elevation of Privilege | Non-admin user reaches the regular-session (raw shell) branch of the submit handler | mitigate | Edit E enforces `effectiveShellOnly = isAdmin && shellOnly` as the branch predicate. When `!isAdmin`, effectiveShellOnly is always `false` regardless of local `shellOnly` state, and the branch routes to handleBirth (agent mode). Defense-in-depth complementary to the JSX gate at Edit C which prevents the checkbox from rendering for non-admin in the first place. |
| T-88-02-02 | Elevation of Privilege | Non-admin user configures their agent's working directory (path traversal, `/etc/`, root-owned dirs) | mitigate | Edit B admin-gates the Path field render — non-admin's DOM never contains a Path input, no `onChange` handler is wired, `path` state stays at its `useState("~/")` default (L322). Edit F further sends `path: ""` on non-admin submits, triggering Plan 88-01's server-authoritative `~/<name>/` substitution regardless of whatever the client tried to put on the wire. Residual risk: a non-admin CLIENT could bypass the UI and POST directly to /identities/birth with a non-empty path — that residual is deferred to a future backend authorization gate per Plan 88-01's T-88-01-02 mitigation. |
| T-88-02-03 | Repudiation | `identityMode` semantic-drift trap: future reader interprets `identityMode === true` as "checkbox checked" (which now means shell mode) when the wire discriminant convention says `identityMode: true = agent mode` | mitigate | Edit D renames the LOCAL state variable to `shellOnly` and applies the boolean invert at every LOCAL read-site. The public payload discriminant name `identityMode` is preserved on the wire (unchanged type + unchanged onCreate call sites) to maintain the AppShell narrowing contract. The rename makes it impossible to read a `shellOnly` reference and misinterpret it as an `identityMode` reference — the drift trap is eliminated at compile time. |
| T-88-02-04 | Denial of Service | Rename creates a subtle regression — an unrenamed local read-site combined with a boolean-invert-forgotten site produces broken canOpen / gate behavior that ships silently | mitigate | Edit D's exhaustive line-by-line enumeration (per PATTERNS.md §2e list) + the acceptance_criteria manual-inspection step ("naked identityMode read-sites") + the Plan 88-03 test rewrites which exercise the new default + admin-conditional-render + admin-explicit-shell-branch behavior together verify the rename produced the intended behavior. Wave 3 IS the safety net. |
| T-88-02-05 | Tampering | Blurb text substitution attack — someone commits an unauthorized text change under the guise of "byte-exact Phase 88 blurb" | accept | Textual reviews happen post-execute per shape §Vehicle notes (unbiased general-purpose subagent code review before /close). Byte-exact grep assertions in acceptance_criteria catch typos and paraphrases at Wave 2 landing. Neither is a runtime security control; both are integrity checks against the LOCKED text at 88-CONTEXT.md §Verbatim copy. |
| T-88-02-SC | Tampering | Zero new npm installs in this plan | accept | Pure TSX edit — no package.json, no package-lock.json, no npm install invocation. Package legitimacy audit N/A per 88-CONTEXT.md §constraints. |
</threat_model>

<verification>
Campaign constraint (Alice 2026-09-07): NO full-suite `npx vitest run`, NO docker build, NO docker cp, NO `docker compose up`, NO deploy. Executor runs SCOPED tests only. Verification commands for this plan:

- `grep -c 'Roles are the expertise your agents adopt' src/ui/sidebar/CreateRoleDialog.tsx` returns 1
- `grep -c 'A role is what an agent does and how it thinks' src/ui/sidebar/CreateRoleDialog.tsx` returns 0
- `grep -c 'Agents are the workers you chat with' src/ui/sidebar/NewSessionDialog.tsx` returns 1
- `grep -c 'Each one adopts a role that shapes what they know and how they help' src/ui/sidebar/NewSessionDialog.tsx` returns 1
- `grep -c 'Pick a host and (optionally) name the agent' src/ui/sidebar/NewSessionDialog.tsx` returns 0
- `grep -cE '\{isAdmin && \(' src/ui/sidebar/NewSessionDialog.tsx` returns 2
- `grep -c 'Just a shell — no agent' src/ui/sidebar/NewSessionDialog.tsx` returns 1 (Unicode em-dash U+2014)
- `grep -c 'Create with new identity' src/ui/sidebar/NewSessionDialog.tsx` returns 0
- `grep -c 'const \[shellOnly, setShellOnly\] = useState(false)' src/ui/sidebar/NewSessionDialog.tsx` returns 1
- `grep -c 'const \[identityMode, setIdentityMode\]' src/ui/sidebar/NewSessionDialog.tsx` returns 0
- `grep -c 'setShellOnly(false)' src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 1
- `grep -c 'setIdentityMode(true)' src/ui/sidebar/NewSessionDialog.tsx` returns 0
- `grep -cE 'checked=\{shellOnly\}' src/ui/sidebar/NewSessionDialog.tsx` returns 1
- `grep -c 'identityMode: true' src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 1 (public payload discriminant preserved)
- `grep -c 'identityMode: false' src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 1 (public payload discriminant preserved)
- `grep -c 'effectiveShellOnly' src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 1
- `grep -c 'isAdmin && shellOnly' src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 1
- `grep -cE 'isAdmin \? normalizedPath : ""' src/ui/sidebar/NewSessionDialog.tsx` returns 1
- `grep -c 'shellOnly' src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 15
- `grep -c 'Phase 88' src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 5
- `grep -c 'Phase 88' src/ui/sidebar/CreateRoleDialog.tsx` returns ≥ 1
- `npx tsc --noEmit` completes with no new errors

Expected test failures at Wave 2 landing (Plan 88-03 restores green):
- CreateRoleDialog.test.tsx Test 11 blurb regex mismatch
- NewSessionDialog.test.tsx Test D default-checked assertion, `renderDialog` helper lacking isAdmin, all 16+ /create with new identity/i regex matches
- NewSessionDialog.chain.test.tsx tests that click the checkbox to toggle identity-mode
- NewSessionDialog.role-dropdown.test.tsx tests that assume identity-mode is default-on
- NewSessionDialog.task-input.test.tsx Task 1a/1b + Task 3c-3e that reference identityMode default
</verification>

<success_criteria>
- CreateRoleDialog blurb text byte-exactly matches "Roles are the expertise your agents adopt. Every agent using this role inherits its goals, rules, and knowledge."
- NewSessionDialog startDescription defaultValue byte-exactly matches "Agents are the workers you chat with. Each one adopts a role that shapes what they know and how they help."
- Both blurbs use the shared verb "adopt" and land in the same commit surface (paired-blurb constraint per shape §What would make it wrong)
- NewSessionDialog Path field is rendered ONLY when isAdmin is truthy
- NewSessionDialog identity-mode checkbox is rendered ONLY when isAdmin is truthy, with label "Just a shell — no agent" (U+2014 em-dash)
- Local state variable `identityMode` is renamed to `shellOnly` throughout the file (state hook, close-reset, all local read-sites) with default flipped from `true` to `false`; boolean invert applied at every LOCAL read-site
- Public callback-payload discriminant `identityMode` in NewSessionOnCreateOpts + at the two onCreate call sites UNCHANGED (AppShell.tsx narrowing preserved)
- Submit-onclick handler at L1131-1153 gates the regular-session (raw shell) branch on `isAdmin && shellOnly` — defense-in-depth so non-admin can never spawn a shell even if some future caller passes stale state
- handleBirth openBirthStream call sends `path: ""` when `!isAdmin` — the empty-string triggers Plan 88-01 backend narrow's `~/<name>/` substitution; admin sends normalized non-empty path
- `npx tsc --noEmit` clean
- Test breakage at Wave 2 landing is limited to (a) blurb regex, (b) label regex, (c) default-checked assumption — all three restored in Plan 88-03
- Wave 3 (Plan 88-03) unblocked: test file has a stable rename target (`shellOnly`), a stable label ("Just a shell — no agent"), a stable admin-gate contract (isAdmin prop), and a stable backend substitution contract (empty path → `~/<name>/`) to write against
</success_criteria>

<output>
Create `.planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-02-SUMMARY.md` when done
</output>
</content>
</invoke>