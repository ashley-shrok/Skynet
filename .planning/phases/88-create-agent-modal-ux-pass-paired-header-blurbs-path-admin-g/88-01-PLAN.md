---
phase: 88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/sidebar/NewSessionDialog.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/backend/database/routes/identity-birth.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "NewSessionDialog accepts an `isAdmin?: boolean` prop with fail-closed default `false` — non-admin behavior applies whenever a caller forgets to pass the prop (matches the sibling shape at PrettyConversationsPanel.tsx:285 for the WeeklyUsageMeter gate, per PATTERNS.md §2f)"
    - "PrettyConversationsPanel forwards its existing `isAdmin` prop (already destructured at L285, typed at L368) to the `<NewSessionDialog>` render at L1935-1957 — one new attribute `isAdmin={isAdmin}` on the JSX element (per PATTERNS.md §3)"
    - "identity-birth.ts POST /identities/birth handler substitutes `~/<name>/` for the effective path when the request body's `path` is absent or empty-string — extends the existing `parsedPath` narrow at L206 by mirroring the L213-214 / L218-221 Phase 86 empty-⇒-default idiom (per PATTERNS.md §2h backend approach + PATTERNS.md §4)"
    - "Admin submits (path present, e.g. `~/` or an override) still resolve to that exact path — the backend fallback only fires when the body path is empty or absent (per PATTERNS.md §4 landmine: admin-typed empty-string edge case)"
    - "The BirthRequest wire contract in src/ui/api/identities-api.ts stays functionally the same — `path` may be sent as empty-string for non-admin submits; no shape or type change is required on the frontend BirthRequest interface for this plan (per PATTERNS.md §4 landmine)"
    - "The name substituted into `~/<name>/` is lower-cased and trimmed (matching what the client already sends at NewSessionDialog.tsx:644 `name: name.toLowerCase()` and what the backend already validates at identity-birth.ts:111 `!name.trim()`) — no kebab-cased transform beyond what the client already sends"
  artifacts:
    - path: "src/ui/sidebar/NewSessionDialog.tsx"
      provides: "NewSessionDialog component whose destructured props signature accepts `isAdmin?: boolean` with default `false`; type surface adds `isAdmin?: boolean` next to the existing `initialBrief?: string | null`"
      contains: "isAdmin = false"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "PrettyConversationsPanel forwards its existing `isAdmin` value to the <NewSessionDialog> render call site"
      contains: "isAdmin={isAdmin}"
    - path: "src/backend/database/routes/identity-birth.ts"
      provides: "Extended parsedPath narrow that substitutes `~/<name>/` when the request body path is empty or absent"
      contains: "~/${"
  key_links:
    - from: "PrettyConversationsPanel.tsx L285 destructured `isAdmin` prop"
      to: "NewSessionDialog.tsx new `isAdmin` prop"
      via: "JSX prop-forward at PrettyConversationsPanel.tsx L1935-1957 (<NewSessionDialog isAdmin={isAdmin} … />)"
      pattern: "isAdmin=\\{isAdmin\\}"
    - from: "identity-birth.ts POST /identities/birth handler at L206 parsedPath narrow"
      to: "orchestrator opts.path at L336 (unchanged)"
      via: "extended narrow: empty-string path OR non-string path → substitute `~/<name>/` (name is the already-trimmed identity name from the same payload)"
      pattern: "parsedPath ="
    - from: "NewSessionDialog.tsx `isAdmin` prop destructuring"
      to: "downstream consumers landed in Plan 88-02 (Path field admin-gate, checkbox admin-gate, submit-path invariant)"
      via: "prop is destructured with fail-closed default `false`; Plan 88-02 wraps two JSX blocks in `{isAdmin && (…)}` and adds an `isAdmin && shellOnly` check at the submit-onclick handler"
      pattern: "isAdmin"
---

<objective>
Wave-1 foundation for Phase 88. Two independent surface changes that Plan 88-02's UI edits and Plan 88-03's test edits depend on:

1. **Frontend prop plumbing.** Add `isAdmin?: boolean` (default `false`) to `NewSessionDialog`. Forward `isAdmin={isAdmin}` from `PrettyConversationsPanel` at the existing `<NewSessionDialog>` call site (the panel already has `isAdmin` in scope at L285 with fail-closed default `= false`, used at L1651 for `<WeeklyUsageMeter />` — this plan wires up a second consumer of the same prop).

2. **Backend path default.** Extend the `parsedPath` narrow at `identity-birth.ts:206` so that an empty-string or absent `path` in the birth payload substitutes `~/<name>/` where `<name>` is the identity's name from the same payload. Admin submits (path present) still resolve to the exact path they sent.

Purpose: unblock Plan 88-02's admin-gate JSX wrapping (`{isAdmin && …}`) and its Path-field admin-gate. Once this plan lands, non-admin users whose modal omits the Path field can submit a birth without an explicit path and land in `~/<name>/` — the "each agent gets its own working directory" scoping per shape §Philosophy.

Purpose (cont.): No user-visible behavior change ships in Wave 1 alone. The `isAdmin` prop lands with a fail-closed default; the backend fallback lands as a safe extension of an existing empty-⇒-default pattern (Phase 86 Plan 86-04 at L213-214 for title, L218-221 for avatarCandidateId). Both changes are dormant until Plan 88-02 wires the frontend admin-gates.

Output:
- `NewSessionDialog.tsx` prop signature adds `isAdmin?: boolean` with default `false`, both in the destructure at L277-284 and in the inline type block at L285-312.
- `PrettyConversationsPanel.tsx` `<NewSessionDialog>` render at L1935-1957 gains one `isAdmin={isAdmin}` attribute.
- `identity-birth.ts` L206 narrow extended: empty-or-absent path → `~/${(typeof name === "string" ? name.trim().toLowerCase() : "")}/`.
- `npx tsc --noEmit` passes with no new errors in these three files.
- Scoped tests: `npx vitest run src/ui/sidebar/NewSessionDialog.test.tsx src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx --run` continues to pass (no test-file edits in this plan — existing tests do not exercise `isAdmin` on NewSessionDialog nor the empty-path branch on the backend, so wave-1 lands without churn on assertions; wave-3 adds the coverage). Backend test file `src/backend/database/routes/identity-birth.test.ts` (if present) runs scoped: `npx vitest run src/backend/database/routes/identity-birth.test.ts --run`; existing tests submit non-empty paths and continue to pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-CONTEXT.md
@.planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-PATTERNS.md
@.planning/shapes/shape-create-agent-modal-ux-pass.md
@src/ui/sidebar/NewSessionDialog.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
@src/backend/database/routes/identity-birth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plumb `isAdmin?: boolean` prop (fail-closed default `false`) from PrettyConversationsPanel into NewSessionDialog</name>
  <files>src/ui/sidebar/NewSessionDialog.tsx, src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx</files>
  <read_first>
    - src/ui/sidebar/NewSessionDialog.tsx (specifically L277-312 — the current destructure at L277-284 and the inline type-annotation block at L285-312; do NOT rename any of `open`/`onClose`/`hostTree`/`onCreate`/`initialHost`/`initialRole`/`initialBrief`. Also skim L27 comment header for the existing Phase-84 additions style so the Phase 88 addendum matches idiom.)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx L285 (`isAdmin = false` destructure — fail-closed default, source of truth), L368 (`isAdmin?: boolean` type declaration + surrounding JSDoc comment at L363-368 explaining fail-closed rationale — this is the comment block Task 1's JSDoc on NewSessionDialog should paraphrase), L1651 (`{isAdmin && <WeeklyUsageMeter />}` — the in-repo canonical `{isAdmin && …}` admin-gate idiom this bounty extends), L1935-1957 (the existing `<NewSessionDialog>` render — the call site that receives the new prop-forward)
    - .planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-CONTEXT.md §Implementation Decisions `isAdmin prop plumbing (fail-closed)` — LOCKED default `false` at prop destructuring
    - .planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-PATTERNS.md §2f + §3 — the exact idiom + JSDoc template lifted from PrettyConversationsPanel.tsx L363-368
  </read_first>
  <action>
    Two coordinated edits, one commit surface. Frontend prop-plumb only — no admin-gated JSX in this task (Plan 88-02 owns the gates).

    ─────────────────────────────────────────────────────────────
    EDIT A — src/ui/sidebar/NewSessionDialog.tsx: add `isAdmin?: boolean = false` to prop signature
    ─────────────────────────────────────────────────────────────

    (A.1) At the destructuring block L277-284, insert `isAdmin = false,` as the FINAL entry in the destructure list, immediately after `initialBrief: _initialBrief,` at L284. Preserve ALL existing prop names, their order, and the `_initialBrief` renaming — this task ONLY adds one entry, does not reorder or rename anything else.

    (A.2) At the inline type-annotation block L285-312, add `isAdmin?: boolean;` as the FINAL entry, immediately after `initialBrief?: string | null;` at L311, before the closing `}) {` on L312. Precede it with a JSDoc block paraphrasing the fail-closed rationale from PrettyConversationsPanel.tsx L363-368 — see 88-PATTERNS.md §2f for the recommended shape. Include these three anchor phrases in the JSDoc: "Phase 88", "fail-closed", and "PrettyConversationsPanel.tsx L285" so grep-based verification lands. The JSDoc MUST also name the two consumers Plan 88-02 will add (Path field admin-gate at the current L926-942 block, identity-mode checkbox admin-gate at the current L944-960 block) so future readers see the wire the prop is threading.

    (A.3) Do NOT add any admin-gated JSX in this task. The prop is destructured and typed only. Any `{isAdmin && (…)}` wrappings ship in Plan 88-02. No linter/tsc will complain about an unused destructured param that has a default — the pattern already exists at L284 with `initialBrief: _initialBrief` (unused-with-underscore-prefix) and at L285 the whole `isAdmin` destructure is used implicitly by the type declaration and by future consumers.

    ─────────────────────────────────────────────────────────────
    EDIT B — src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx: forward `isAdmin={isAdmin}` at L1935-1957
    ─────────────────────────────────────────────────────────────

    In the `{showPencilButton && (<NewSessionDialog ... />)}` JSX block at L1935-1958, add one new prop attribute `isAdmin={isAdmin}` on the `<NewSessionDialog>` element. Suggested position: on its own line immediately after `initialBrief={chainPrefill?.description ?? null}` at L1956 and before the self-closing `/>` at L1957. Add a one-line trailing comment on the same JSX line or immediately before it:

      // Phase 88: forward admin-gate to NewSessionDialog for Path field + shell checkbox visibility (see 88-CONTEXT.md §isAdmin prop plumbing).

    The value expression is the same `isAdmin` variable already destructured at L285 and consumed at L1651 for the `<WeeklyUsageMeter />` — do NOT introduce a new state hook, ref, or context lookup; the identifier `isAdmin` is already in scope in this component body.

    Do NOT touch the sibling `<CreateRoleDialog>` render at L1968-1981 — per PATTERNS.md §3 landmine + 88-CONTEXT.md §prior_context, CreateRoleDialog is admin-only-visible upstream anyway; Phase 88 is the first bounty to introduce admin-gating INSIDE a dialog and only NewSessionDialog needs the prop.

    ─────────────────────────────────────────────────────────────
    ANTI-PATTERNS TO AVOID
    ─────────────────────────────────────────────────────────────

    - Do NOT change the default from `false` to `true` "for developer convenience during testing" — the whole point of Phase 88 is fail-closed on the non-admin path per shape §What would make it wrong: "Non-admin sees the 'Just a shell' checkbox" = leaked shell access.
    - Do NOT rename the destructured prop to `admin` or `isAdminUser` — the name `isAdmin` matches the panel-side identifier at L285, the AppShell forward at L2025, and the WeeklyUsageMeter gate at L1651. Consistency across the isAdmin chain is intentional.
    - Do NOT thread `isAdmin` through any additional intermediate component (e.g. a wrapper HOC). The panel already has it in scope; the dialog needs it directly. One prop-forward attribute, done.
    - Do NOT touch identity-birth.ts in this task — Task 2 owns the backend edit.
    - Do NOT delete or reorder the existing PrettyConversationsPanel JSDoc comment block at L1951-1953 explaining chain-prefill semantics — that comment is Phase 22 heritage and its content is orthogonal to Phase 88.
  </action>
  <acceptance_criteria>
    Frontend + call-site plumbing is complete when every check below holds:

    Grep source assertions on `src/ui/sidebar/NewSessionDialog.tsx`:
    - `grep -c 'isAdmin = false' src/ui/sidebar/NewSessionDialog.tsx` returns 1 (destructure with fail-closed default)
    - `grep -c 'isAdmin?: boolean' src/ui/sidebar/NewSessionDialog.tsx` returns 1 (type annotation on props interface)
    - `grep -c 'Phase 88' src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 1 (JSDoc comment anchor)
    - `grep -c 'fail-closed' src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 1 (JSDoc rationale anchor)
    - `grep -c 'PrettyConversationsPanel.tsx L285' src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 1 (JSDoc source-of-truth anchor)
    - `grep -c '{isAdmin && (' src/ui/sidebar/NewSessionDialog.tsx` returns 0 (no admin-gated JSX yet — that ships in Plan 88-02)

    Grep source assertion on `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`:
    - `grep -c 'isAdmin={isAdmin}' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns 1 (the new prop-forward on <NewSessionDialog>; note the existing L1651 usage does NOT include a `={isAdmin}` since it's a naked identifier in a `{isAdmin && …}` boolean context — this grep pattern only matches the JSX attribute form)
    - `grep -c 'NewSessionDialog' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns ≥ 6 (imports at L117, comment refs at L127/L1236/L1259/L1265/L1931 + the render at L1936 — count stays approximately what it was; the new attribute doesn't add a new NewSessionDialog reference)

    Structural / build-level:
    - `npx tsc --noEmit` completes with no new errors mentioning either file (running from repo root against tsconfig.json)

    Test-command assertion (scoped to changed files; no test edits in this plan):
    - `npx vitest run src/ui/sidebar/NewSessionDialog.test.tsx src/ui/sidebar/NewSessionDialog.chain.test.tsx src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx src/ui/sidebar/NewSessionDialog.task-input.test.tsx --run` exits 0. Existing tests do not pass `isAdmin` so they exercise the fail-closed default; the destructure with `= false` produces the same runtime behavior as the pre-plan code (no `isAdmin`-dependent branch exists yet in NewSessionDialog).
    - `npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx --run` exits 0. Existing tests either pass `isAdmin={true}` or omit the prop; either way the new attribute forwards the value without regressing any assertion.
  </acceptance_criteria>
  <verify>
    <automated>set -e; grep -c 'isAdmin = false' src/ui/sidebar/NewSessionDialog.tsx | grep -q '^1$' && grep -c 'isAdmin?: boolean' src/ui/sidebar/NewSessionDialog.tsx | grep -q '^1$' && grep -q 'Phase 88' src/ui/sidebar/NewSessionDialog.tsx && grep -q 'fail-closed' src/ui/sidebar/NewSessionDialog.tsx && grep -q 'PrettyConversationsPanel.tsx L285' src/ui/sidebar/NewSessionDialog.tsx && [ "$(grep -c '{isAdmin && (' src/ui/sidebar/NewSessionDialog.tsx)" = "0" ] && grep -c 'isAdmin={isAdmin}' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx | grep -q '^1$' && npx tsc --noEmit && npx vitest run src/ui/sidebar/NewSessionDialog.test.tsx src/ui/sidebar/NewSessionDialog.chain.test.tsx src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx src/ui/sidebar/NewSessionDialog.task-input.test.tsx src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx --run</automated>
  </verify>
  <done>
    - NewSessionDialog destructure at L277-284 gains one line `isAdmin = false,` after `initialBrief: _initialBrief,`
    - NewSessionDialog type block at L285-312 gains one line `isAdmin?: boolean;` after `initialBrief?: string | null;` with preceding JSDoc containing the three anchor phrases "Phase 88", "fail-closed", "PrettyConversationsPanel.tsx L285"
    - PrettyConversationsPanel `<NewSessionDialog>` render at L1935-1957 gains one attribute `isAdmin={isAdmin}` with a one-line Phase 88 comment
    - `npx tsc --noEmit` clean
    - Scoped vitest run (five test files listed above) exits 0
    - No admin-gated JSX added to NewSessionDialog.tsx in this task (that ships in Plan 88-02); grep for `{isAdmin && (` in NewSessionDialog.tsx returns 0
    - CreateRoleDialog render at PrettyConversationsPanel.tsx:1968-1981 UNCHANGED (no isAdmin attribute added — CreateRoleDialog is admin-only-visible upstream per PATTERNS.md §3 landmine)
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend identity-birth.ts parsedPath narrow — substitute `~/<name>/` when the request body path is empty or absent</name>
  <files>src/backend/database/routes/identity-birth.ts</files>
  <read_first>
    - src/backend/database/routes/identity-birth.ts (specifically L95-142 for the request-body destructure + validation gates including name validation at L111-114 which guarantees a trimmed non-empty name string reaches L206; L204-231 for the current parsedColorHue / parsedVoice / parsedPath / parsedTitle / parsedAvatarCandidateId / parsedTask / parsedPoolPicked block — the immediate context around the edit; L320-345 for how parsedPath threads into the orchestrator opts at L336 so downstream consumers are unaffected by the shape change)
    - .planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-CONTEXT.md §Implementation Decisions `Backend birth endpoint change` (LOCKED that backend-vs-frontend is planner's call; this plan picks backend — rationale: extends the already-established L213-214 / L218-221 Phase 86 empty-⇒-default idiom at the same seam)
    - .planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-PATTERNS.md §2h (both frontend and backend seam options — this plan picks backend per Recommended-per-shape note) and §4 (identity-birth.ts extension detail + downstream consumer note on tmux -c + SFTP; both accept `~` and `~/<name>/`)
    - .planning/phases/86-cosmetics-migrate-to-role-move-title-hue-voice-avatar-from-i/86-04-PLAN.md (backend section) — the sibling precedent that established the pattern this plan extends: parsedTitle at L213-214 uses `title.trim() ? title.trim() : ""`, parsedAvatarCandidateId at L218-221 uses `avatarCandidateId.trim() ? avatarCandidateId.trim() : ""`. Phase 88's parsedPath extension is the same idiom shape.
  </read_first>
  <action>
    Single edit to the parsedPath narrow at L206. Everything else in the file — the pre-narrow destructure at L100, the name validation at L111-114, the parsedColorHue / parsedVoice / parsedTitle / parsedAvatarCandidateId / parsedTask / parsedPoolPicked lines at L204-230, the orchestrator invocation at L320-345, the SSE progress emission, the 400/503 gates — stays byte-identical.

    ─────────────────────────────────────────────────────────────
    THE EDIT at L206
    ─────────────────────────────────────────────────────────────

    Replace the single line at L206:

      const parsedPath = (typeof path === "string" ? path : "~") as string;

    With a multi-line narrow that adds an empty-string branch and substitutes `~/<name>/` when appropriate. Precede the narrow with a comment block that:
    - Anchors on the phrase "Phase 88" so `grep 'Phase 88' src/backend/database/routes/identity-birth.ts` matches
    - Names the frontend admin-gate that lands in Plan 88-02 (Path field at NewSessionDialog.tsx L926-942 wrapped in `{isAdmin && (…)}`) so the reader sees the wire connection
    - Notes the invariant: when `!isAdmin`, the frontend omits the Path input UI so `body.path` arrives as empty-string or absent; when `isAdmin`, the frontend's `useState("~/")` at L322 + `normalizePath` at L631 guarantee a non-empty `body.path`
    - Notes admin-typed-empty-string edge case: admin whose UI defaults to `"~/"` would have to actively clear the field to hit the fallback; if they do, treating that as "give me the per-agent default" is a reasonable interpretation (matches shape §Philosophy: "each agent gets its own working directory named after itself keeps agents from stepping on each other in a shared home")
    - Names the source-of-truth pattern this extends: "Extends the L213-214 / L218-221 Phase 86 Plan 86-04 empty-⇒-default idiom to the parsedPath narrow"

    The narrow shape (adapt exact JS/TS syntax to match file conventions):

      // Phase 88: substitute `~/<name>/` when the request body path is
      // absent or empty. Non-admin submits from NewSessionDialog (Plan 88-02's
      // admin-gate wraps the Path input at L926-942 in `{isAdmin && (…)}`)
      // arrive here with body.path === "" or body.path === undefined.
      // Admin submits arrive with body.path === "~/" (frontend default at
      // NewSessionDialog.tsx L322 + normalizePath at L631) or an override.
      // Extends the L213-214 / L218-221 Phase 86 Plan 86-04 empty-⇒-default
      // idiom used for title and avatarCandidateId. Downstream: parsedPath is
      // threaded into orchestrator opts.path at L336 which is consumed by the
      // tmux -c working-directory argument (step 2 of birth) and SFTP write
      // target (step 2.5 role frontmatter); both accept `~/<name>/` and expand
      // it via shell/SFTP tilde-resolution. The name substituted is the
      // already-trimmed identity name from the same payload (see the
      // `!name.trim()` guard at L111-114 above), lower-cased to match what
      // the client sends at NewSessionDialog.tsx:644 and the identity-birth
      // orchestrator invocation at L330 `name: name.trim()`.
      const parsedPath = (
        typeof path === "string" && path.trim()
          ? path
          : `~/${name.trim().toLowerCase()}/`
      ) as string;

    Preserve the exact variable name (`parsedPath`) so the L336 orchestrator invocation `path: parsedPath,` requires no downstream change. Preserve the trailing `as string` cast so the type shape at the assignment site is unchanged for TypeScript's inference of the orchestrator argument object.

    ─────────────────────────────────────────────────────────────
    NAME SEMANTICS + INVARIANTS
    ─────────────────────────────────────────────────────────────

    - `name` is guaranteed to be a non-empty trimmable string by the L111-114 guard (rejects with 400 otherwise). No additional null-check needed inside the narrow — the L111-114 gate has already fired by the time execution reaches L206.
    - `name.trim().toLowerCase()` matches the client-side transform at NewSessionDialog.tsx L644 (`name: name.toLowerCase()`) and the orchestrator invocation transform at identity-birth.ts L330 (`name: name.trim()`). The substituted path directory name is therefore byte-identical to the identity name that gets used in every other tmux / SFTP / frontmatter path. If the identity name is "amelia", the substituted path is `~/amelia/`.
    - Do NOT apply any additional kebab-casing / slug transform beyond `.trim().toLowerCase()`. Names are already validated by the caller-side `IDENTITY_NAME_PATTERN` regex; the backend does not need to re-slug.

    ─────────────────────────────────────────────────────────────
    ANTI-PATTERNS TO AVOID
    ─────────────────────────────────────────────────────────────

    - Do NOT change the wire contract of the frontend `BirthRequest` interface at src/ui/api/identities-api.ts L495-527 — `path: string` stays. Empty-string is a valid value the backend now handles gracefully.
    - Do NOT add a separate `pathAbsent` boolean field to the payload. The empty-string / absent branch is inferred from the string content directly, matching how parsedTitle at L213-214 and parsedAvatarCandidateId at L218-221 already infer absence.
    - Do NOT change the `parsedPath` variable name — L336 already consumes it. Renaming would force a second, unrelated edit at L336.
    - Do NOT change the L336 orchestrator invocation. The orchestrator's opts.path contract is a string; it doesn't care whether the string is `"~/"` (admin) or `"~/amelia/"` (non-admin) — both expand via tmux and SFTP the same way.
    - Do NOT introduce a URL-encode / shell-escape on the substituted name inside the narrow. The identity name is already validated kebab-case-lowercase by the frontend regex and the orchestrator re-validates via ROLE_NAME_PATTERN-style checks at L146; no additional escaping is required for the tmux `-c` argument or SFTP path.
    - Do NOT touch the `parsedColorHue`, `parsedVoice`, `parsedTitle`, `parsedAvatarCandidateId`, `parsedTask`, or `parsedPoolPicked` lines. This task is the parsedPath narrow only.
    - Do NOT delete or reword the L207-212 Phase 86 comment block above parsedTitle — it explains the sibling idiom for title and is the direct precedent this task extends. Preserving it keeps the pattern trail visible for future readers.
  </action>
  <acceptance_criteria>
    Backend narrow is complete when every check below holds:

    Grep source assertions on `src/backend/database/routes/identity-birth.ts`:
    - `grep -c 'Phase 88' src/backend/database/routes/identity-birth.ts` returns ≥ 1 (the new narrow comment block)
    - `grep -cE 'parsedPath =' src/backend/database/routes/identity-birth.ts` returns 1 (still exactly one assignment to parsedPath — the narrow replacement, not a duplicate)
    - `grep -cE 'name\.trim\(\)\.toLowerCase\(\)' src/backend/database/routes/identity-birth.ts` returns ≥ 1 (the substitution expression inside the new narrow)
    - `grep -c 'as string' src/backend/database/routes/identity-birth.ts | awk '$1 >= 4 { exit 0 } { exit 1 }'` — the pre-plan file has multiple `as string` casts (parsedPath, parsedVoice, parsedTask); the new narrow preserves the parsedPath cast so the total count does not decrease
    - `grep -c 'path: parsedPath' src/backend/database/routes/identity-birth.ts` returns 1 (L336 orchestrator invocation unchanged)
    - `grep -c 'typeof path === "string" ? path : "~"' src/backend/database/routes/identity-birth.ts` returns 0 (old single-line narrow gone)

    Behavior assertions (from the shape of the new narrow):
    - Body `{ path: "~/" }` → parsedPath === `"~/"` (admin path, admin submit — unchanged from pre-plan behavior for admin's default)
    - Body `{ path: "/tmp/agent-x" }` → parsedPath === `"/tmp/agent-x"` (admin override — unchanged from pre-plan behavior)
    - Body `{ path: "" }` → parsedPath === `` `~/${name.trim().toLowerCase()}/` `` (non-admin submit)
    - Body with no `path` key at all → parsedPath === `` `~/${name.trim().toLowerCase()}/` `` (non-admin submit via undefined branch)
    - Body `{ path: "   " }` (whitespace-only) → parsedPath === `` `~/${name.trim().toLowerCase()}/` `` (whitespace-only string fails `.trim()` predicate — matches the L213-214 title idiom's whitespace handling)

    Structural / build-level:
    - `npx tsc --noEmit` completes with no new errors mentioning identity-birth.ts

    Test-command assertion (scoped to the changed file only — no test edits in this plan):
    - `npx vitest run src/backend/database/routes/identity-birth.test.ts --run` exits 0 IF that test file exists; otherwise scope defaults to a nearby test file that exercises identity-birth (grep repository for a filename matching `identity-birth*.test.ts` before running). Existing tests submit non-empty paths (mostly `"~/"` or an explicit override) and continue to resolve via the truthy-string branch of the new narrow; behavior for those tests is byte-identical.
  </acceptance_criteria>
  <verify>
    <automated>set -e; grep -q 'Phase 88' src/backend/database/routes/identity-birth.ts && [ "$(grep -cE 'parsedPath =' src/backend/database/routes/identity-birth.ts)" = "1" ] && grep -qE 'name\.trim\(\)\.toLowerCase\(\)' src/backend/database/routes/identity-birth.ts && [ "$(grep -c 'path: parsedPath' src/backend/database/routes/identity-birth.ts)" = "1" ] && [ "$(grep -c 'typeof path === "string" ? path : "~"' src/backend/database/routes/identity-birth.ts)" = "0" ] && npx tsc --noEmit && (test -f src/backend/database/routes/identity-birth.test.ts && npx vitest run src/backend/database/routes/identity-birth.test.ts --run || npx vitest run --related src/backend/database/routes/identity-birth.ts --run)</automated>
  </verify>
  <done>
    - L206 single-line narrow replaced by the multi-line empty-⇒-default narrow with the anchor comment block
    - Empty-string / absent / whitespace-only path → parsedPath resolves to `` `~/${name.trim().toLowerCase()}/` ``; non-empty admin submits pass through unchanged
    - `path: parsedPath` invocation at L336 untouched (grep count unchanged)
    - Sibling parsedTitle / parsedAvatarCandidateId narrows at L213-214 and L218-221 untouched
    - `npx tsc --noEmit` clean
    - Scoped vitest run for the backend test file exits 0
    - Frontend files unchanged in this task (that's Task 1's surface)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Non-admin caller (browser) → NewSessionDialog | The user's role in the AppShell state (`users.is_admin` from `/users/me`) is the source truth for the `isAdmin` value that PrettyConversationsPanel forwards. Non-admin users' Skynet client never sees `isAdmin={true}` on the DOM. Same-origin auth session — no new AuthN/AuthZ surface introduced by Phase 88; the wire boundary just gains a fail-closed default. |
| NewSessionDialog → PrettyConversationsPanel props | Static prop-forward inside a same-tab React tree. The `isAdmin` value flows AppShell → PrettyConversationsPanel L2025 → NewSessionDialog. No IPC, no fetch, no serialization. The prop is a boolean literal at every hop. |
| Frontend `body.path` → backend `identity-birth.ts` POST /identities/birth | Client-supplied path already crosses this boundary today. The new empty-string branch substitutes a server-computed `~/<name>/` where `<name>` is the identity name from the same payload; the substitution is authoritative (server does not trust the client to supply the non-admin default). |
| Backend `parsedPath` → tmux `-c` argument + SFTP write target | Downstream consumers of `opts.path` in `identity-birth-orchestrator.ts`. `~/<name>/` expands via shell tilde-resolution at tmux invocation and SFTP path resolution; identity name is already regex-validated kebab-case-lowercase upstream. No new shell-metacharacter surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-01-01 | Elevation of Privilege | `isAdmin` prop on NewSessionDialog defaults to `true` if the caller forgets to pass it | mitigate | Fail-closed default `isAdmin = false` at the destructure in NewSessionDialog.tsx (Task 1 Edit A.1). Matches the sibling default at PrettyConversationsPanel.tsx:285 for the WeeklyUsageMeter. Verification: `grep -c 'isAdmin = false' src/ui/sidebar/NewSessionDialog.tsx` returns 1. |
| T-88-01-02 | Elevation of Privilege | Non-admin user's forged body.path (`../../..`, `/etc/`, `/root/.ssh`) leaks agent working directory outside the intended `~/<name>/` scoping | mitigate | Backend authoritative substitution at parsedPath narrow — when body.path is empty (which is what the admin-gated frontend UI sends for non-admin), the server-computed `~/<name>/` wins regardless of any leaked path field on the wire. Note: Plan 88-01 alone does NOT prevent a NON-ADMIN CLIENT from posting a non-empty `body.path` directly to the endpoint (bypassing the UI); that residual risk is deferred to a future backend authorization gate (out of scope for this bounty per shape §Scope — the shape only asks for the substitution behavior, not a role-based path-validation gate). The UI-side gate at Plan 88-02 is defense-in-depth for the browser flow, not a bypassable-client mitigation. |
| T-88-01-03 | Tampering | Substituted `~/<name>/` contains shell metacharacters | accept | Name is already regex-validated by IDENTITY_NAME_PATTERN on the frontend (kebab-case-lowercase alphanumeric only) and re-validated at the backend orchestrator via ROLE_NAME_PATTERN-style checks upstream. `.trim().toLowerCase()` is idempotent for regex-validated names. No new shell-injection surface. |
| T-88-01-04 | Denial of Service | Whitespace-only or crafted-empty body.path fails to substitute and reaches downstream tmux invocation as invalid path | mitigate | The narrow's `.trim()` predicate rejects whitespace-only strings and takes the substitution branch, matching the L213-214 parsedTitle idiom's whitespace handling. Downstream tmux invocation always receives either a non-empty admin-typed string or a computed `~/<name>/` string; never receives the empty-string. |
| T-88-01-SC | Tampering | Zero new npm installs in this plan | accept | Pure TSX + TS edit — no `package.json`, no `package-lock.json`, no `npm install` invocation. Package legitimacy audit N/A per 88-CONTEXT.md §constraints (no new deps in Phase 88). |
</threat_model>

<verification>
Campaign constraint (Alice 2026-09-07): NO full-suite `npx vitest run`, NO docker build, NO docker cp, NO `docker compose up`, NO deploy. Executor runs SCOPED tests only. Verification commands for this plan:

- `grep -c 'isAdmin = false' src/ui/sidebar/NewSessionDialog.tsx` returns 1
- `grep -c 'isAdmin?: boolean' src/ui/sidebar/NewSessionDialog.tsx` returns 1
- `grep -c 'Phase 88' src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 1
- `grep -c 'fail-closed' src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 1
- `grep -c 'PrettyConversationsPanel.tsx L285' src/ui/sidebar/NewSessionDialog.tsx` returns ≥ 1
- `grep -c '{isAdmin && (' src/ui/sidebar/NewSessionDialog.tsx` returns 0
- `grep -c 'isAdmin={isAdmin}' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns 1
- `grep -c 'Phase 88' src/backend/database/routes/identity-birth.ts` returns ≥ 1
- `grep -cE 'parsedPath =' src/backend/database/routes/identity-birth.ts` returns 1
- `grep -cE 'name\.trim\(\)\.toLowerCase\(\)' src/backend/database/routes/identity-birth.ts` returns ≥ 1
- `grep -c 'typeof path === "string" ? path : "~"' src/backend/database/routes/identity-birth.ts` returns 0
- `grep -c 'path: parsedPath' src/backend/database/routes/identity-birth.ts` returns 1
- `npx tsc --noEmit` completes with no new errors
- `npx vitest run src/ui/sidebar/NewSessionDialog.test.tsx src/ui/sidebar/NewSessionDialog.chain.test.tsx src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx src/ui/sidebar/NewSessionDialog.task-input.test.tsx src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx --run` exits 0
- If `src/backend/database/routes/identity-birth.test.ts` exists: `npx vitest run src/backend/database/routes/identity-birth.test.ts --run` exits 0

Note: this plan's outputs are dormant until Plan 88-02 consumes the isAdmin prop for admin-gating. Existing test coverage on NewSessionDialog and PrettyConversationsPanel does not exercise the new prop (fail-closed default = false; no isAdmin-dependent branches exist yet). Plan 88-03 (Wave 3) adds explicit isAdmin-conditional tests.
</verification>

<success_criteria>
- `NewSessionDialog` accepts `isAdmin?: boolean` prop with fail-closed default `false` (destructure + type declaration + JSDoc)
- `PrettyConversationsPanel` forwards `isAdmin={isAdmin}` to the `<NewSessionDialog>` render at L1935-1957
- `identity-birth.ts` L206 parsedPath narrow substitutes `~/<name>/` when the request body path is empty or absent
- Admin submits (body.path non-empty) resolve to the exact submitted path — no behavior change for admin
- No admin-gated JSX added to NewSessionDialog in this plan (that's Plan 88-02)
- No test files edited in this plan (that's Plan 88-03)
- `npx tsc --noEmit` clean; scoped vitest runs exit 0
- CreateRoleDialog render UNCHANGED at PrettyConversationsPanel.tsx:1968-1981
- Plan 88-02 unblocks: it can now consume `isAdmin` from props for the Path admin-gate + checkbox admin-gate + submit invariant
- Plan 88-03 unblocks: it can now write tests that pass `isAdmin={true|false}` explicitly and assert the backend substitution behavior end-to-end
</success_criteria>

<output>
Create `.planning/phases/88-create-agent-modal-ux-pass-paired-header-blurbs-path-admin-g/88-01-SUMMARY.md` when done
</output>
</content>
</invoke>