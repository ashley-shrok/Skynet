---
phase: 90-role-management-modal-split
plan: 05
type: execute
wave: 3
depends_on:
  - 90-01
  - 90-02
  - 90-04
files_modified:
  - src/ui/features/pretty-view/RolesListModal.tsx
  - src/ui/features/pretty-view/RolesListModal.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "`RolesListModal` is a new global modal that portals to `document.body` (mirrors GlobalFilesModal chrome + host-picker pattern verbatim)."
    - "Modal header carries: title ('Roles') + host-picker <select> + '+ New role' button (right-aligned near close) + close X."
    - "Host-picker hides / auto-selects when the user has exactly one host (matches Phase 84's shared 'hide-host-picker-when-only-one-host' primitive shape); shows when multi-host."
    - "Body renders an alphabetical list of roles for the selected host, each row rendered in `.pv-row` treatment: `border-radius: var(--radius-pv-bubble)` (14px), hue-tinted linear-gradient background keyed on the role's `colorHue` (fallback hue 190 per D-05), hue-tinted border, hue-tinted layered box-shadow, backdrop-filter blur, 8px vertical gap."
    - "Row content: 40px round `.pv-avatar`-style disc containing `<img src={roleAvatarUrl(hostId, roleName)}>` + role's display name (falls back to title-cased slug per D-05) + right-side chevron."
    - "Row click closes the roles-list modal AND opens `<RoleModal>` for that role (swap-not-stack per D-03) — RolesListModal returns via `onSelectRole` callback prop to the parent (PrettyView / PrettyConversationsPanel — wired in Plan 90-06)."
    - "'+ New role' button opens `CreateRoleDialog` on top of RolesListModal (stack, not swap — planner-pick per D-10 discretion). On CreateRoleDialog success, RolesListModal re-fetches the roles list so the new role appears."
    - "Empty state (host has zero roles): renders 'This host has no roles yet.' + surfaces the '+ New role' button prominently (D-05 empty-state clause)."
    - "Sort order: alphabetical by `displayName` (D-05)."
  artifacts:
    - path: "src/ui/features/pretty-view/RolesListModal.tsx"
      provides: "New global modal — host-picker + alphabetical .pv-row rendered list + '+ New role' header button"
      exports: ["RolesListModal"]
      min_lines: 200
    - path: "src/ui/features/pretty-view/RolesListModal.test.tsx"
      provides: "In-process test coverage: host-picker, .pv-row rendering, click routes onSelectRole, '+ New role' opens CreateRoleDialog, empty state"
  key_links:
    - from: "src/ui/features/pretty-view/RolesListModal.tsx"
      to: "src/ui/api/identities-api.ts"
      via: "listRolesForHost(hostId) + roleAvatarUrl(hostId, name)"
      pattern: "listRolesForHost"
    - from: "src/ui/features/pretty-view/RolesListModal.tsx"
      to: "src/ui/sidebar/CreateRoleDialog.tsx"
      via: "'+ New role' header button opens on click (stack)"
      pattern: "CreateRoleDialog"
---

<objective>
Create the new `RolesListModal` component: a global-viewport modal (portals to `document.body`) reached from the panel-header three-dots menu (wired in Plan 90-06), showing an alphabetical list of roles on the selected host with `.pv-row`-styled rows in each role's own hue. Header carries a '+ New role' button that opens `CreateRoleDialog` (stack), folding the "New role" three-dots entry into this modal.

Purpose: implements D-02 (host-picker mirrors Edit-global-files), D-05 (`.pv-row` conversation-row treatment for rows), D-10 (+ New role button in modal header, not floating/inline/persistent). This is the front door to the role modal.

Output: two new component files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/90-role-management-modal-split-role-level-tabs-out-of-the-ident/90-CONTEXT.md
@.planning/shapes/shape-role-management-modal-split.md
@src/ui/features/pretty-view/GlobalFilesModal.tsx
@src/ui/features/pretty-view/SkillsEditorModal.tsx
@src/ui/features/pretty-conversations/pretty-conversations.css
@src/ui/api/identities-api.ts
@src/ui/sidebar/CreateRoleDialog.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create `RolesListModal.tsx` — modal shell + host-picker + fetch effect</name>
  <files>src/ui/features/pretty-view/RolesListModal.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/GlobalFilesModal.tsx lines 1-260 (whole shell — MIRROR verbatim: props signature, portal-to-body pattern, host-picker `<select>` at header, single-host auto-select useEffect, cancellable fetch effect on hostId change)
    - src/ui/features/pretty-view/SkillsEditorModal.tsx (second reference for global-modal chrome + host-picker convention — cross-check patterns)
    - src/ui/api/identities-api.ts lines 270-290 (`listRolesForHost` + extended `RoleSummary` type from Plan 90-01 — the fetch call this modal drives on hostId change)
    - src/ui/api/identities-api.ts (roleAvatarUrl helper from Plan 90-02)
    - src/ui/features/pretty-conversations/pretty-conversations.css lines 482-517 (`.pv-row` CSS — the exact hue-tinted gradient + border + shadow + backdrop-filter tokens the rows must apply inline)
    - src/ui/features/pretty-conversations/pretty-conversations.css lines 544-586 (`.pv-avatar` CSS — the 40px round disc treatment for the role avatar)
    - src/ui/sidebar/CreateRoleDialog.tsx lines 128-170 (`CreateRoleDialogProps` — modal opens with `hostTree` prop; RolesListModal has hostTree in scope already from its own host-picker upstream prop, threads it through)
    - .planning/phases/90-role-management-modal-split-role-level-tabs-out-of-the-ident/90-CONTEXT.md § D-02, D-05, D-10
  </read_first>
  <behavior>
    - Test A (portal to body): mounted with `open={true}` → DialogPrimitive.Portal has no `container` prop; DialogContent appears in `document.body`.
    - Test B (host-picker multi-host): `hostTree` has 3 hosts → header `<select>` renders 3 `<option>` elements + a "Pick a host…" placeholder.
    - Test C (host-picker single-host auto-select): `hostTree` has 1 host → the picker is either hidden OR shows the single host auto-selected (match Phase 84's shared primitive shape).
    - Test D (fetch on host change): user picks host id 3 → `listRolesForHost(3)` fires. Mocked response of 5 roles → 5 rows render.
    - Test E (loading state): before fetch resolves → skeleton or "Loading…" branch renders.
    - Test F (error state): fetch rejects → error branch renders with the error message.
    - Test G (defaultHostId honored): `defaultHostId=2` + host 2 present in tree → host 2 auto-selected on open (mirrors GlobalFilesModal L88-90).
    - Test H (reset on close): closing the modal + reopening resets `selectedHostId`, `roles`, `activeState` (mirrors GlobalFilesModal L79-86).
  </behavior>
  <action>
    Create `src/ui/features/pretty-view/RolesListModal.tsx`. Component signature:
    ```
    export interface RolesListModalProps {
      open: boolean;
      onOpenChange: (open: boolean) => void;
      hostTree: HostFolder | null;
      defaultHostId: number | null;
      /** Row click callback — parent handles swap-not-stack coordination (close list, open role modal). */
      onSelectRole: (params: {roleName: string; roleCosmetics: RoleSummary; hostId: number}) => void;
    }
    ```
    Byte-shape mirror of `GlobalFilesModal.tsx` L61-260:
    1. State slots: `selectedHostId: number | null`, `rolesState: TabState<RoleSummary[]>`, `createRoleOpen: boolean`.
    2. `flatHosts` memo (same `collectAllHosts` helper — either duplicate or import from a shared util if one exists; grep first).
    3. `useEffect` on `open` change: reset state on close; auto-select defaultHostId or single host on open (mirrors GlobalFilesModal L79-94).
    4. `useEffect` on `selectedHostId` change: `listRolesForHost(selectedHostId)` with cancellable pattern (mirrors GlobalFilesModal L97-119). Sort roles by `displayName ?? name` alphabetically before setting state.
    5. Render `DialogPrimitive.Root` → `DialogPrimitive.Portal` (NO container) → Overlay + Content with the same global-modal chrome IdentityModal + GlobalFilesModal share (background hue-agnostic — planner-pick: use a neutral hue 220 gradient matching GlobalFilesModal L216-220 since this modal displays MULTIPLE roles at once, not one).
    6. Header: title "Roles" (left) + host-picker `<select>` (center, mirrors GlobalFilesModal L234-248) + '+ New role' button (right-of-center, opens `CreateRoleDialog`) + close X (right).
    7. Body: rows list — Task 2 renders each row; Task 1 renders the layered branches (host-not-picked / loading / error / empty / list).
    Reference D-02 in the top-of-file JSDoc.
  </action>
  <verify>
    <automated>npx vitest run --related src/ui/features/pretty-view/RolesListModal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/features/pretty-view/RolesListModal.tsx` exists and exports `RolesListModal`.
    - `grep -c "container" src/ui/features/pretty-view/RolesListModal.tsx` returns 0.
    - `grep -c "listRolesForHost" src/ui/features/pretty-view/RolesListModal.tsx` returns >=1.
    - `npx vitest run --related src/ui/features/pretty-view/RolesListModal.tsx` reports 8 tests passing (A–H).
    - `npm run build` compiles clean.
  </acceptance_criteria>
  <done>Modal shell + host-picker + fetch effect wired; layered branches render; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render `.pv-row`-styled role rows + '+ New role' header button + empty state</name>
  <files>src/ui/features/pretty-view/RolesListModal.tsx, src/ui/features/pretty-view/RolesListModal.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-conversations/pretty-conversations.css lines 482-517 (`.pv-row` — exact linear-gradient stops, hsla vars, border spec, box-shadow layered stack, backdrop-filter, border-radius token `var(--radius-pv-bubble)` = 14px)
    - src/ui/features/pretty-conversations/pretty-conversations.css lines 544-586 (`.pv-avatar` — 40px, border-radius 999px, hue gradient background, inner image object-fit cover, inset+outer shadow)
    - src/ui/features/pretty-view/GlobalFilesModal.tsx lines 260-380 (row rendering shape — inline styles used for hue-tinted glass since class-based hue is not viable; mirror this approach)
    - src/ui/sidebar/CreateRoleDialog.tsx (CreateRoleDialogProps — stack the dialog on top of RolesListModal via a Portal mount inside RolesListModal's body OR a sibling mount coordinated via `createRoleOpen` state; planner-pick: sibling mount at the end of the Portal — simpler and matches Phase 22 SRIC-04 pattern from PrettyConversationsPanel)
    - .planning/phases/90-role-management-modal-split-role-level-tabs-out-of-the-ident/90-CONTEXT.md § D-05, D-10
  </read_first>
  <behavior>
    - Test A (row visual — full cosmetics): role `{name: 'box-maintainer', displayName: 'Box Maintainer', colorHue: 320, avatar: 'box-maintainer.webp'}` → row inline style contains `hsla(320, 50%, 38%, 0.55)` (matches `.pv-row` gradient stop), 40px avatar `<img>` with `src` matching `/roles/box-maintainer/avatar?hostId=<n>`.
    - Test B (row visual — no cosmetics fallback): role `{name: 'foo', description: ''}` (Plan 90-01 backwards-compat, no cosmetic fields present) → row uses hue 190 fallback + neutral placeholder avatar. Row still clickable.
    - Test C (displayName fallback to title-cased slug): role `{name: 'box-maintainer'}` (no displayName) → row renders "Box Maintainer" (title-cased kebab → space-separated).
    - Test D (sort alphabetical by displayName): roles `[{name:'zebra', displayName:'Ally'}, {name:'aardvark', displayName:'Zeb'}]` → row order Ally, Zeb.
    - Test E (row click): clicking a row → `onSelectRole({roleName: 'box-maintainer', roleCosmetics: {name, description, title, displayName, colorHue, voice, avatar}, hostId: 3})` fires with the FULL RoleSummary payload (so the parent can pass it to RoleModal without a re-fetch).
    - Test F (+ New role opens CreateRoleDialog): clicking header '+ New role' button → CreateRoleDialog mounts (its `open` prop toggles true). RolesListModal STAYS open (stack per D-10 planner-pick).
    - Test G (CreateRoleDialog success re-fetches list): CreateRoleDialog's `onCreated` fires → `listRolesForHost` re-fires (state reset to loading, then new response).
    - Test H (empty state): host has zero roles → renders 'This host has no roles yet.' + '+ New role' button remains available in the header.
  </behavior>
  <action>
    In `RolesListModal.tsx` body, render the layered branches:
    - `selectedHostId === null` → "Pick a host to see its roles."
    - `rolesState.status === 'loading'` → skeleton (mirrors GlobalFilesModal loading).
    - `rolesState.status === 'error'` → error branch.
    - `rolesState.status === 'ready' && rolesState.data.length === 0` → empty state text.
    - `rolesState.status === 'ready'` → rows list.

    Each row is a `<button type="button">` (row is a full-width clickable element with `.pv-row`-style INLINE styles keyed on the role's hue):
    ```
    style={{
      borderRadius: 14,
      background: `linear-gradient(160deg, hsla(${hue}, 50%, 38%, 0.55), hsla(${hue}, 45%, 24%, 0.60))`,
      border: `1px solid hsla(${hue}, 65%, 55%, 0.32)`,
      boxShadow: `<layered shadow with hue glow, mirrors .pv-row spec at pretty-conversations.css:482-517>`,
      backdropFilter: 'blur(...) saturate(...)',
      padding: '10px 12px',
      marginBottom: 8,
      display: 'flex',
      alignItems: 'center',
      gap: 10,
      cursor: 'pointer',
    }}
    ```
    Row content: 40px round `<img>` with `.pv-avatar`-style inline styles (background gradient with hue, borderRadius 999, box-shadow inset+outer) + `<span>` with displayName text + right-side chevron (lucide `ChevronRight` icon at low opacity).

    On row click: `onSelectRole({roleName: role.name, roleCosmetics: role, hostId: selectedHostId})`. The parent (Plan 90-06) closes RolesListModal + opens RoleModal.

    Header '+ New role' button: `<button onClick={() => setCreateRoleOpen(true)}>+ New role</button>` styled to match SkillsEditorModal's header action button (grep for the button styling there). Mount CreateRoleDialog as a sibling in a Portal at end of the modal body:
    ```
    <CreateRoleDialog
      open={createRoleOpen}
      onClose={() => setCreateRoleOpen(false)}
      hostTree={hostTree}
      onCreated={() => {
        setCreateRoleOpen(false);
        // Trigger re-fetch by bumping a version counter or re-invoking the fetch effect
        setRolesState({ status: 'loading' });
        listRolesForHost(selectedHostId).then(...).catch(...);
      }}
    />
    ```

    Reference D-05 + D-10 in code comments at the touch sites.

    Sibling test file `RolesListModal.test.tsx` with the 8 cases above (extend the Task-1 test scaffold if it already exists as a stub; else create fresh).
  </action>
  <verify>
    <automated>npx vitest run --related src/ui/features/pretty-view/RolesListModal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Row rendering produces the exact `.pv-row` inline style tokens for hue substitution (verified by test A + B).
    - `grep -c "onSelectRole" src/ui/features/pretty-view/RolesListModal.tsx` returns >=1.
    - `grep -c "CreateRoleDialog" src/ui/features/pretty-view/RolesListModal.tsx` returns >=1.
    - `npx vitest run --related src/ui/features/pretty-view/RolesListModal.tsx` reports all 16 tests passing (8 Task-1 + 8 Task-2).
    - Empty state text present when roles list is `[]`.
    - `npm run build` compiles clean.
  </acceptance_criteria>
  <done>Rows render with correct .pv-row treatment; row click emits onSelectRole with full RoleSummary; + New role button opens CreateRoleDialog and re-fetches on success; empty state present.</done>
</task>

</tasks>

<verification>
- `npx vitest run --related src/ui/features/pretty-view/RolesListModal.tsx` → all 16 cases green.
- `npm run build` → clean.
- Visual spot-check on the /pv-row-treatment inline styles matches `pretty-conversations.css:482-517` values.
</verification>

<success_criteria>
- Plan 90-06's `PrettyConversationsPanel` three-dots menu can wire "Edit roles…" to open `<RolesListModal>` and coordinate swap-to-`<RoleModal>` on row click.
- No new capabilities landed inside `CreateRoleDialog` itself (Phase 84's UX-pass shape stays intact — CONTEXT § out-of-scope).
</success_criteria>

<output>
Create `.planning/phases/90-role-management-modal-split-role-level-tabs-out-of-the-ident/90-05-SUMMARY.md` when done.
</output>
