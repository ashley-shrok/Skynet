---
phase: 90-role-management-modal-split
plan: 10
type: execute
wave: 7
depends_on:
  - 90-09
files_modified:
  - src/ui/features/pretty-view/RoleModal.tsx
  - src/ui/features/pretty-view/RoleCosmeticEditBlock.tsx
  - src/ui/features/pretty-view/RoleBountiesTab.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-view/RoleModal.test.tsx
  - src/ui/features/pretty-view/RoleCosmeticEditBlock.test.tsx
  - src/ui/features/pretty-view/RoleBountiesTab.test.tsx
  - src/backend/database/routes/roles.ts
  - src/backend/database/routes/roles-list-for-host.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "identityShimKey removed from RoleModal, RoleBountiesTab, PrettyConversationsPanel wiring, and PrettyView (if present there). No component receives an identityShimKey prop anywhere after this plan."
    - "RoleModal role-file read uses getRoleFileByName({roleName, hostId}) from Plan 90-09 — no identity indirection."
    - "RoleBountiesTab uses listBountiesForRoleName({roleName, hostId}) from Plan 90-09 — no identity indirection."
    - "Role wakeups tab in RoleModal uses listRoleWakeupsByName + createRoleWakeupByName + updateRoleWakeupByName + deleteRoleWakeupByName from Plan 90-09 — no identity indirection."
    - "Role-file save handler continues to use updateRoleFileByName from Plan 90-03 (already role-name-keyed — no change)."
    - "RoleCosmeticEditBlock save handler now uploads avatar bytes via updateRoleAvatarByName(hostId, roleName, file) from Plan 90-09 BEFORE writing markdown frontmatter. Two-step: upload avatar (returns filename), then splice frontmatter's avatar: field to that filename, then updateRoleFileByName. If avatar upload fails, abort the markdown write and surface the error to the user."
    - "mergeCosmeticsIntoMarkdown bug fixed: user CAN clear a title, voice, or avatar once set. Empty draft value on a field that WAS set = frontmatter key deletion (not preservation of the old value). Distinguish 'unset from the start' from 'cleared by the user this session' via a separate cleared-keys set or sentinel value."
    - "LOW: ROLE_NAME_PATTERN consolidated to one exported location (planner picks — probably src/backend/database/routes/role-name-pattern.ts or add to identity-artifact-reader.ts). Other files import from that single source rather than cloning the regex literal."
    - "LOW: roles.ts:181 catch-all 502 differentiates 'host unreachable' from 'role file parse/read error' — distinct error messages so future debugging isn't misled."
  artifacts:
    - path: "src/ui/features/pretty-view/RoleModal.tsx"
      provides: "RoleModal without identityShimKey prop — reads role file + wakeups via role-name-keyed helpers"
      contains: "getRoleFileByName"
    - path: "src/ui/features/pretty-view/RoleBountiesTab.tsx"
      provides: "RoleBountiesTab without identityShimKey prop — reads bounties via listBountiesForRoleName"
      contains: "listBountiesForRoleName"
    - path: "src/ui/features/pretty-view/RoleCosmeticEditBlock.tsx"
      provides: "Save handler that uploads avatar bytes AND writes frontmatter, in that order. Clearable title/voice/avatar cosmetics."
      contains: "updateRoleAvatarByName"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "RolesListModal + RoleModal wiring without identityShimKey derivation"
    - path: "src/backend/database/routes/roles.ts"
      provides: "GET /roles/:name/avatar with distinct error codes for host-unreachable vs read-error"
  key_links:
    - from: "src/ui/features/pretty-view/RoleModal.tsx"
      to: "src/ui/api/claude-session-api.ts"
      via: "import { getRoleFileByName, listRoleWakeupsByName, createRoleWakeupByName, updateRoleWakeupByName, deleteRoleWakeupByName } from '../../api/claude-session-api'"
      pattern: "getRoleFileByName"
    - from: "src/ui/features/pretty-view/RoleCosmeticEditBlock.tsx"
      to: "src/ui/api/identities-api.ts"
      via: "import { updateRoleAvatarByName } from '../../api/identities-api'"
      pattern: "updateRoleAvatarByName"
---

<objective>
Close the D-08.3 divergence and fix the HIGH-severity findings from the unbiased code review. Drop `identityShimKey` completely; wire everything through role-name-keyed helpers added in Plan 90-09. Wire avatar file bytes upload so cosmetic edits persist end-to-end. Fix the title-clear bug. Sweep two LOW-severity quality items (ROLE_NAME_PATTERN dedup, catch-all 502 differentiation) while touching adjacent code.

Purpose: this is the closer for the review-driven fix pass. After this plan, the role modal reads role-scope content addressable purely by role name — no identity indirection anywhere, matching what CONTEXT.md D-08.3 locked as the correct design.

Output: refactored RoleModal + RoleCosmeticEditBlock + RoleBountiesTab + PrettyConversationsPanel wiring + companion test updates. Backend cleanups on roles.ts. No new components; no shape changes.
</objective>

<references>
- Unbiased code-review findings from `/build` step 4 — the source of truth for what this plan fixes. Two HIGH findings on the shim + one HIGH on avatar bytes + one MEDIUM on title-clear + two LOW on quality.
- CONTEXT.md D-08.3 (verbatim rejection of the shim pattern this plan removes).
- Plan 90-04 (`e7c25654`) — the origin of the shim pattern being removed. Files RoleModal.tsx, RoleCosmeticEditBlock.tsx, RoleBountiesTab.tsx.
- Plan 90-05 (`bd8a61b7`) — RolesListModal. Verify it doesn't need shim changes (should already pass roleName + hostId directly to onSelectRole).
- Plan 90-06 (`7b80ce29`) — PrettyConversationsPanel + PrettyView wiring where the shim is derived at L2044-2046. This is where the shim derivation removal happens.
- Plan 90-09 output: 7 new helpers to consume.
</references>

<tasks>
## Task 1: Refactor RoleModal.tsx to drop shim + wire avatar upload

<read_first>
- src/ui/features/pretty-view/RoleModal.tsx (all 711 lines from Plan 90-04)
- src/ui/api/claude-session-api.ts (new helpers from Plan 90-09)
- src/ui/api/identities-api.ts (updateRoleAvatarByName helper from Plan 90-09)
</read_first>

<action>
- Remove `identityShimKey` prop from RoleModal's props interface.
- Replace all uses of the shim inside RoleModal:
  - Role-file read: use `getRoleFileByName({roleName, hostId})` (Plan 90-09) instead of the identity-keyed `getRoleFile` variant.
  - Role wakeups fetch/CRUD: use listRoleWakeupsByName / createRoleWakeupByName / updateRoleWakeupByName / deleteRoleWakeupByName (Plan 90-09).
  - Role-file save (already role-name-keyed via updateRoleFileByName — no change needed; verify).
- In handleRoleFileSave (or wherever the cosmetic-edit save routes to), after `mergeCosmeticsIntoMarkdown` produces the new markdown, add avatar-file handling BEFORE the markdown write:
  1. If cosmeticDraft has a picked avatar file (from RoleCosmeticEditBlock — pass it up through onSaveCosmetics prop or similar), call `updateRoleAvatarByName(hostId, roleName, file)` first.
  2. On success, use the returned `filename` to splice into the frontmatter's `avatar:` field (overwriting the cosmeticDraft's `avatar` value if it differs).
  3. Then call `updateRoleFileByName` with the finalized markdown.
  4. If avatar upload fails, abort BEFORE the markdown write — surface the error to the user, keep the modal open, preserve draft.
</action>

<acceptance_criteria>
- `grep -c "identityShimKey" src/ui/features/pretty-view/RoleModal.tsx` returns 0.
- `grep -c "getRoleFileByName\|listRoleWakeupsByName\|createRoleWakeupByName\|updateRoleWakeupByName\|deleteRoleWakeupByName" src/ui/features/pretty-view/RoleModal.tsx` returns >= 3.
- `grep -c "updateRoleAvatarByName" src/ui/features/pretty-view/RoleModal.tsx` returns >= 1 (called from the save handler).
- `npx tsc --noEmit` clean.
</acceptance_criteria>

## Task 2: Refactor RoleBountiesTab.tsx to drop shim

<read_first>
- src/ui/features/pretty-view/RoleBountiesTab.tsx (482 lines from Plan 90-04)
</read_first>

<action>
- Remove `identityShimKey` prop.
- Replace all identity-keyed bounty fetches with `listBountiesForRoleName({roleName, hostId, includeArchived?})` (Plan 90-09).
- Preserve existing UI behavior (search, accordion, archive lazy-load).
- If any bounty MUTATION operations were routed through identity-keyed handlers, note them — this plan does NOT add role-name-keyed bounty mutation variants (bounties are typically edited via terminal, not through the modal UI in v1). If the current code has mutation handlers, either leave them as-is (still identity-keyed) with a comment `TODO: v2 — role-name-keyed bounty mutations` OR remove them entirely if they're UI-unused. Executor decides based on what the code shows.
</action>

<acceptance_criteria>
- `grep -c "identityShimKey" src/ui/features/pretty-view/RoleBountiesTab.tsx` returns 0.
- `grep -c "listBountiesForRoleName" src/ui/features/pretty-view/RoleBountiesTab.tsx` returns >= 1.
- `npx tsc --noEmit` clean.
</acceptance_criteria>

## Task 3: Refactor RoleCosmeticEditBlock.tsx — fix avatar handling + title-clear

<read_first>
- src/ui/features/pretty-view/RoleCosmeticEditBlock.tsx (432 lines from Plan 90-04)
</read_first>

<action>
Fix HIGH: avatar file discard bug.
- Currently `const [, setAvatarFile]` at ~L319 discards the file. Restore state (`const [avatarFile, setAvatarFile]`), and thread the file up to the parent (RoleModal) via the existing `onSaveCosmetics` prop or a new dedicated prop.
- RoleModal's save handler (Task 1) uses the file.

Fix MEDIUM: title-clear bug in `mergeCosmeticsIntoMarkdown`.
- Currently `draftVal !== undefined && draftVal !== ""` skips empty drafts. Instead, distinguish:
  - Field-was-not-touched-in-session: skip (existing frontmatter survives).
  - Field-was-cleared-in-session: delete the frontmatter key even if draft is empty.
- Implementation: maintain a `clearedKeys: Set<string>` in RoleCosmeticEditBlock. Any field that transitions from set → cleared adds to `clearedKeys`. Pass `clearedKeys` alongside the draft to `mergeCosmeticsIntoMarkdown`. Merge logic: for each key in clearedKeys, DELETE that key from frontmatter (regardless of draft value). For other keys, apply draft value if non-empty; skip if empty.
- Verify: setting a title, saving, opening again, clearing the title, saving → the frontmatter `title:` key IS removed from the file.

Do NOT change the visual controls (Title / ColorPicker / VoicePicker / avatar generator flow) — only the state plumbing.
</action>

<acceptance_criteria>
- `grep -c "avatarFile" src/ui/features/pretty-view/RoleCosmeticEditBlock.tsx` returns >= 2 (state + at least one usage).
- `grep -c "clearedKeys\|cleared_keys" src/ui/features/pretty-view/RoleCosmeticEditBlock.tsx` returns >= 1.
- `mergeCosmeticsIntoMarkdown` (wherever it lives — RoleModal or a shared helper) accepts a clearedKeys set and deletes those frontmatter keys.
- Test coverage in RoleCosmeticEditBlock.test.tsx demonstrates the clear-title flow: (1) set title, save, (2) re-open, clear title, save, (3) resulting markdown does not contain `title:` line.
- Test coverage demonstrates the avatar file is actually passed up to the save handler (not discarded).
- `npx tsc --noEmit` clean.
</acceptance_criteria>

## Task 4: Refactor PrettyConversationsPanel.tsx + PrettyView.tsx — drop shim derivation

<read_first>
- src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (L2044-2046 where identityShimKey is derived)
- src/ui/features/pretty-view/PrettyView.tsx (shim consumption sites from Plan 90-06)
</read_first>

<action>
- Remove the identityShimKey derivation logic entirely from PrettyConversationsPanel.tsx (the `identitiesByKey` lookup at ~L2044-2046).
- Remove the identityShimKey prop from any place it was threaded to RoleModal / RoleBountiesTab / PrettyView.
- Verify no dead references remain — grep the whole `src/ui/features/` tree.
</action>

<acceptance_criteria>
- `grep -rc "identityShimKey" src/ui/features/` returns 0.
- `grep -rc "identityShimKey" src/ui/` returns 0 (including api layer — should be 0 there already since Plan 90-04's shim was purely a component-level prop).
- `npx tsc --noEmit` clean.
</acceptance_criteria>

## Task 5: LOW-severity backend cleanups

<read_first>
- src/backend/database/routes/roles.ts (Plan 90-02 file, L181 catch-all)
- src/backend/database/routes/roles-list-for-host.ts (L47 ROLE_NAME_PATTERN definition)
- src/backend/claude-session/identity-artifact-reader.ts (if ROLE_NAME_PATTERN is redefined here from Plan 90-03, promote to shared)
</read_first>

<action>
Cleanup 1: consolidate ROLE_NAME_PATTERN.
- Choose ONE canonical export location — recommend `src/backend/utils/role-name-pattern.ts` (new small file) OR reuse an existing utils module. Executor picks based on repo conventions.
- Move the regex constant + export a `ROLE_NAME_PATTERN` binding and an `isValidRoleName(name: string): boolean` helper.
- Update roles-list-for-host.ts, roles.ts, identity-artifact-reader.ts, claude-session-server.ts to import from the shared location instead of re-declaring.

Cleanup 2: `roles.ts` catch-all differentiation at the GET /roles/:name/avatar handler (L181-ish per the review report).
- Currently a single try/catch wraps SSH resolve + file read + response streaming and returns 502 "host unreachable" for everything.
- Refactor: distinguish `resolveHostById` failures (→ 502 "host not resolvable") from `readRoleFileByName`/frontmatter-parse failures (→ 502 "role file read error") from actual streaming failures (→ 502 "avatar stream error"). Same HTTP code (502) — different response bodies + logging.

Frontend has no companion changes for this task.
</action>

<acceptance_criteria>
- New shared ROLE_NAME_PATTERN location exists as a single source. Grep for the regex literal `/^[a-z0-9-]+$/` OR `ROLE_NAME_PATTERN =` across the backend — should appear in the shared file, and imports (not redefinitions) in the 3-4 consumer files.
- `grep -c "role file read error\|host not resolvable\|avatar stream error" src/backend/database/routes/roles.ts` >= 2 (differentiated error messages present).
- `npm run build:backend && npm run build` exit 0.
</acceptance_criteria>
</tasks>

<verification>
Commit atomically per plan (`fix(90-10): drop identityShimKey, wire role avatar upload, fix title-clear, LOW quality sweep`). Or split into 2 commits (frontend refactor + backend cleanups) — executor's discretion.

Run scoped tests:
- Frontend: RoleModal.test.tsx + RoleCosmeticEditBlock.test.tsx + RoleBountiesTab.test.tsx + PrettyConversationsPanel.role-management-flow.test.tsx + PrettyView.role-modal-swap.test.tsx.
- Backend: roles.test.ts + roles.avatar-write.test.ts (Plan 90-08's) + roles-list-for-host.test.ts.
- Verify Plan 90-07's role-name-keyed tests still green.
- Verify Plan 90-09's api-layer tests still green.

Build gates: `npx tsc --noEmit && npm run build && npm run build:backend`.

Write SUMMARY.md.

Stop at commit. No push, no docker, no full-suite.
</verification>
