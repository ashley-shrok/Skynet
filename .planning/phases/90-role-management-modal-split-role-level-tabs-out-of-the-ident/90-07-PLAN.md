---
phase: 90-role-management-modal-split
plan: 07
type: execute
wave: 5
depends_on: []
files_modified:
  - src/backend/claude-session/identity-artifact-reader.ts
  - src/backend/claude-session/claude-session-server.ts
  - src/backend/claude-session/claude-session-server.role-reads.test.ts
  - src/backend/claude-session/claude-session-server.role-wakeup-crud.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "New WS wire type `role:get-file` accepts `{roleName, hostId}` and returns `{markdown, error?}` — mirrors identity:get-role-file but keyed directly on roleName (no identity indirection)."
    - "New WS wire type `role:list-bounties` accepts `{roleName, hostId, includeArchived?}` and returns `{bounties: Bounty[], archivedBounties?: Bounty[], error?}` — mirrors identity:list-bounties but keyed on roleName."
    - "New WS wire type `role:list-wakeups` accepts `{roleName, hostId}` and returns `{wakeups: Wakeup[], error?}` — mirrors identity:list-role-wakeups but keyed on roleName."
    - "New WS wire types `role:create-wakeup`, `role:update-wakeup`, `role:delete-wakeup` accept `{roleName, hostId, ...wakeupSpec}` and return `{wakeups: Wakeup[], error?}` — mirror identity:{create|update|delete}-role-wakeup shape."
    - "All 6 new handlers validate roleName via ROLE_NAME_PATTERN (`/^[a-z0-9-]+$/`) before any SSH/disk work. Invalid roleName surfaces `{error: 'invalid roleName'}` without touching disk."
    - "Missing/unresolvable hostId surfaces `{error: 'host not found'}`."
    - "Read/write helpers are role-name-keyed variants of the existing identity-keyed helpers — reuse the underlying file-reader/writer primitives; only the resolution step differs (roleName direct vs identity→role indirection)."
  artifacts:
    - path: "src/backend/claude-session/identity-artifact-reader.ts"
      provides: "readRoleFileByName, readRoleBountiesByName, readRoleWakeupsByName, writeRoleWakeupByName, deleteRoleWakeupByName — role-name-keyed variants of existing identity-keyed helpers"
      contains: "readRoleFileByName"
    - path: "src/backend/claude-session/claude-session-server.ts"
      provides: "6 new WS handlers (role:get-file, role:list-bounties, role:list-wakeups, role:create-wakeup, role:update-wakeup, role:delete-wakeup) + dispatcher branches"
      contains: "handleRoleGetFile"
    - path: "src/backend/claude-session/claude-session-server.role-reads.test.ts"
      provides: "Vitest coverage of role:get-file + role:list-bounties + role:list-wakeups happy paths, invalid-roleName, host-not-found, SSH-failure fallbacks"
    - path: "src/backend/claude-session/claude-session-server.role-wakeup-crud.test.ts"
      provides: "Vitest coverage of role:create-wakeup + role:update-wakeup + role:delete-wakeup happy paths + validation + persistence"
  key_links:
    - from: "src/backend/claude-session/claude-session-server.ts"
      to: "src/backend/claude-session/identity-artifact-reader.ts"
      via: "import { readRoleFileByName, readRoleBountiesByName, readRoleWakeupsByName, writeRoleWakeupByName, deleteRoleWakeupByName }"
      pattern: "readRoleFileByName"
---

<objective>
Add 6 role-name-keyed WS wire types + companion backend helpers to eliminate the `identityShimKey` D-08.3 divergence surfaced in the unbiased code review. These are the READ + wakeup-CRUD analogs to the WRITE variant that Plan 90-03 already added (`role:update-file` + `writeRoleFileByName`).

Purpose: closes the D-08.3 gap — the shape's role modal must be a complete home for role-scope content addressable by role name, not by identity indirection. The shim was the executor's "planner's discretion" compromise; this plan replaces it.

Output: 6 new WS handlers + 5 new backend helper functions (readRoleFileByName may already exist from Plan 90-03 — mirror its shape for the others) + companion Vitest coverage.
</objective>

<references>
- CONTEXT.md § D-08 (which endpoints/wire types exist, planner picks shape). This plan picks: role-name-keyed WS variants for reads + wakeup CRUD, mirroring `role:update-file` (Plan 90-03) shape and byte-for-byte identity-keyed WS handler patterns.
- Plan 90-03 (`728625fc`, `e59ed45c`, `dab91099`) — the shape to mirror. `role:update-file` handler at `claude-session-server.ts` (search for `handleRoleUpdateFile`), `writeRoleFileByName` helper at `identity-artifact-reader.ts` (search for `export async function writeRoleFileByName`).
- Existing identity-keyed handlers to mirror: `identity:get-role-file`, `identity:list-bounties`, `identity:list-role-wakeups`, `identity:{create|update|delete}-role-wakeup`. Find via `grep -nE "identity:get-role-file|identity:list-bounties|identity:list-role-wakeups|identity:create-role-wakeup|identity:update-role-wakeup|identity:delete-role-wakeup" src/backend/claude-session/claude-session-server.ts`.
- ROLE_NAME_PATTERN definition: `src/backend/database/routes/roles-list-for-host.ts:47` (or wherever the constant is exported from).
- SSH resolution + one-shot pattern: `resolveHostById(hostId, userId)` + `connectOneShot(...)` — same as existing handlers.
</references>

<tasks>
## Task 1: Backend read helpers (`identity-artifact-reader.ts`)

<read_first>
- src/backend/claude-session/identity-artifact-reader.ts (find existing `readRoleFileByName` if it landed in 90-03; existing identity-keyed readers: `readIdentityFile`, `readRoleFile`, `listBountiesForIdentity` (or similar — search), `listRoleWakeupsForIdentity`, `writeRoleWakeup`, `deleteRoleWakeup`)
- src/backend/claude-session/claude-session-server.ts (for the wire-shape context — how the identity-keyed handlers call into the readers)
</read_first>

<action>
Add these exported functions to `identity-artifact-reader.ts` (or verify they exist from Plan 90-03; add whichever are missing):

1. `readRoleFileByName(conn, roleName): Promise<string>` — reads `~/.claude/roles/<roleName>/<roleName>.md` directly (no identity indirection). May already exist from 90-03; if so, verify signature.

2. `readRoleBountiesByName(conn, roleName, includeArchived): Promise<{ bounties: Bounty[], archivedBounties: Bounty[] }>` — reads `~/.claude/roles/<roleName>/bounties/` via same shell one-liner + bounty.json parse that the identity-keyed variant uses. Skip `bounties/archive/` when includeArchived is false; include it when true (mirrors existing behavior).

3. `readRoleWakeupsByName(conn, roleName): Promise<Wakeup[]>` — reads `~/.claude/roles/<roleName>/wakeups/*.json` (same as identity-keyed variant).

4. `writeRoleWakeupByName(conn, roleName, wakeup): Promise<void>` — writes `~/.claude/roles/<roleName>/wakeups/<name>.json` (create or update — full-overwrite of the file). Wakeup spec conforms to the existing Wakeup type.

5. `deleteRoleWakeupByName(conn, roleName, wakeupName): Promise<void>` — removes `~/.claude/roles/<roleName>/wakeups/<name>.json`. Wakeup-name gate via safe-filename regex (mirror what the identity-keyed variant does).

Reuse SFTP/exec primitives from the existing identity-keyed helpers verbatim. The only delta is which path they operate on — `~/.claude/roles/<roleName>/` instead of `<resolveRoleFolder(identityKey)>`. Add `ROLE_NAME_PATTERN` guard at each function's entry.

Do NOT modify the existing identity-keyed helpers — leave them in place. This plan adds new peer functions, doesn't refactor existing ones.
</action>

<acceptance_criteria>
- `grep -c "export async function readRoleFileByName" src/backend/claude-session/identity-artifact-reader.ts` returns 1.
- `grep -c "export async function readRoleBountiesByName" src/backend/claude-session/identity-artifact-reader.ts` returns 1.
- `grep -c "export async function readRoleWakeupsByName" src/backend/claude-session/identity-artifact-reader.ts` returns 1.
- `grep -c "export async function writeRoleWakeupByName" src/backend/claude-session/identity-artifact-reader.ts` returns 1.
- `grep -c "export async function deleteRoleWakeupByName" src/backend/claude-session/identity-artifact-reader.ts` returns 1.
- Each function calls `ROLE_NAME_PATTERN.test(roleName)` at entry, throwing an error on failure.
- `npm run build:backend && npm run build` exit 0.
</acceptance_criteria>

## Task 2: Backend WS handlers (`claude-session-server.ts`)

<read_first>
- src/backend/claude-session/claude-session-server.ts (find the existing role:update-file handler from Plan 90-03 — mirror its shape for the 6 new handlers)
- The 6 identity-keyed handlers listed in <references>
</read_first>

<action>
Add 6 new WS wire types + handlers, each mirroring the existing identity-keyed variant but keyed on `{roleName, hostId}` instead of `{identityKey}`:

1. `role:get-file` → response type `role:file-loaded` returning `{markdown, error?}`.
2. `role:list-bounties` → response type `role:bounties-loaded` returning `{bounties, archivedBounties?, error?}`. Accepts optional `includeArchived: boolean` in request.
3. `role:list-wakeups` → response type `role:wakeups-loaded` returning `{wakeups, error?}`.
4. `role:create-wakeup` → response type `role:wakeup-created` returning `{wakeups, error?}` (re-emit full list after create).
5. `role:update-wakeup` → response type `role:wakeup-updated` returning `{wakeups, error?}`.
6. `role:delete-wakeup` → response type `role:wakeup-deleted` returning `{wakeups, error?}`.

Wire each handler through the same dispatcher pattern the existing wire types use. Include `__handle*ForTests` seams matching Plan 90-03's convention.

Guard: roleName via ROLE_NAME_PATTERN, hostId via resolveHostById, contents/wakeup shape via lightweight validation before disk touch.
</action>

<acceptance_criteria>
- `grep -c "role:get-file" src/backend/claude-session/claude-session-server.ts` >= 2 (at least the dispatcher branch and doc comment).
- Same for `role:list-bounties`, `role:list-wakeups`, `role:create-wakeup`, `role:update-wakeup`, `role:delete-wakeup`.
- `grep -c "__handleRole" src/backend/claude-session/claude-session-server.ts` >= 6 (one seam per handler, for tests).
- `npm run build:backend && npm run build` exit 0.
</acceptance_criteria>

## Task 3: Vitest coverage (two new test files)

<read_first>
- src/backend/claude-session/claude-session-server.role-update-file.test.ts (Plan 90-03's test — mirror its shape)
</read_first>

<action>
Create `claude-session-server.role-reads.test.ts` covering:
- Happy path for role:get-file (returns markdown).
- Happy path for role:list-bounties (returns bounties + archivedBounties when includeArchived: true).
- Happy path for role:list-wakeups.
- Invalid roleName surfaces `{error: 'invalid roleName'}` for each read type.
- Missing/unresolvable hostId surfaces `{error: 'host not found'}` for each.

Create `claude-session-server.role-wakeup-crud.test.ts` covering:
- Happy paths for role:create-wakeup, role:update-wakeup, role:delete-wakeup.
- Each returns a fresh list post-mutation.
- Invalid roleName gate.
- Invalid wakeup shape rejection.
</action>

<acceptance_criteria>
- Both test files exist and pass: `npx vitest run --project backend src/backend/claude-session/claude-session-server.role-reads.test.ts src/backend/claude-session/claude-session-server.role-wakeup-crud.test.ts` exit 0.
- Total test count across both files >= 10.
</acceptance_criteria>
</tasks>

<verification>
Commit atomically per plan (`feat(90-07): role-name-keyed read + wakeup-CRUD WS variants`). Run scoped tests: `npx vitest run src/backend/claude-session/claude-session-server.role-reads.test.ts src/backend/claude-session/claude-session-server.role-wakeup-crud.test.ts src/backend/claude-session/claude-session-server.role-update-file.test.ts` (verify Plan 90-03's tests still green). Backend build gate: `npm run build:backend && npm run build`. Write SUMMARY.md at `.planning/phases/90-role-management-modal-split-role-level-tabs-out-of-the-ident/90-07-SUMMARY.md`.

Stop at commit. No push, no docker, no full-suite.
</verification>
