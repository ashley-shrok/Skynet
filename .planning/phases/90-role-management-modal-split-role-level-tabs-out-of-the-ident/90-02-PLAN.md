---
phase: 90-role-management-modal-split
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/database/routes/roles.ts
  - src/backend/database/routes/roles.test.ts
  - src/backend/database/database.ts
  - src/ui/api/identities-api.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "`GET /roles/:name/avatar?hostId=<n>` streams the role's sibling avatar bytes with the correct Content-Type when the role file's frontmatter names an avatar filename AND the sibling file exists on disk."
    - "Absent avatar (role has no `avatar:` frontmatter key OR the named sibling file is missing) responds with 404 + `{error: 'no avatar'}` (mirrors existing identity-avatar 404 contract at identities.ts:820-828)."
    - "Invalid `name` param (fails ROLE_NAME_PATTERN kebab-case) responds with 400 BEFORE any SSH work."
    - "Missing/invalid `hostId` query responds with 400 BEFORE any SSH work."
    - "Unresolvable host responds with 502 (matches identity avatar endpoint's cross-user isolation)."
    - "Frontend helper `roleAvatarUrl(hostId, roleName)` returns the URL string `/roles/<name>/avatar?hostId=<n>` for the roles-list modal + role modal to consume as an `<img src>`."
  artifacts:
    - path: "src/backend/database/routes/roles.ts"
      provides: "New Express router — `GET /roles/:name/avatar?hostId=<n>` endpoint using readRoleFileByName + readAvatarSiblingFileByRole"
      contains: "readAvatarSiblingFileByRole"
    - path: "src/backend/database/routes/roles.test.ts"
      provides: "Vitest coverage: happy path, 404 (no frontmatter avatar), 404 (frontmatter avatar but file missing), 400 (bad name), 400 (bad hostId), 502 (host not found), path-traversal defense"
  key_links:
    - from: "src/backend/database/routes/roles.ts"
      to: "src/backend/claude-session/identity-artifact-reader.ts"
      via: "import { readRoleFileByName, readAvatarSiblingFileByRole, extractCosmeticsFromFrontmatter, AVATAR_MIME_FROM_EXT }"
      pattern: "readAvatarSiblingFileByRole"
    - from: "src/backend/database/database.ts"
      to: "src/backend/database/routes/roles.ts"
      via: "app.use('/roles', rolesRouter) — mounted alongside the existing roles-list-for-host router"
      pattern: "/roles"
---

<objective>
Add a new backend route `GET /roles/:name/avatar?hostId=<n>` that resolves a role's on-disk avatar (via role-file frontmatter → sibling file) and streams the bytes with the correct Content-Type. Byte-shape mirror of the existing `GET /identities/:key/avatar?hostId=<n>` endpoint (`identities.ts:767-870`), swapped for role-scope addressing.

Purpose: implements D-08.2 (shape open — planner picks `GET /roles/:name/avatar?hostId=<n>` matching the identity avatar path shape). Directly enables D-05 (`.pv-row` rows carry 40px round `.pv-avatar` with the role's own avatar image) and D-06 (role modal header shows the role's avatar).

Output: new router file + database mount + companion tests + frontend URL helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/90-role-management-modal-split-role-level-tabs-out-of-the-ident/90-CONTEXT.md
@src/backend/database/routes/identities.ts
@src/backend/database/routes/roles-list-for-host.ts
@src/backend/claude-session/identity-artifact-reader.ts
@src/backend/database/database.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: New `src/backend/database/routes/roles.ts` router with `GET /:name/avatar` endpoint + tests</name>
  <files>src/backend/database/routes/roles.ts, src/backend/database/routes/roles.test.ts</files>
  <read_first>
    - src/backend/database/routes/identities.ts lines 767-870 (`GET /:identityKey/avatar` — byte-shape reference for the new route: hostId validation, IDENTITY_KEY_RE gate, host resolver call, LOCAL/REMOTE conn split, readAvatarSiblingFile call, MIME lookup, res.type/res.send)
    - src/backend/database/routes/roles-list-for-host.ts lines 40-60 (ROLE_NAME_PATTERN definition — reuse the same regex + timeout constants)
    - src/backend/claude-session/identity-artifact-reader.ts lines 583-650 (`readRoleFileByName` — takes conn + roleName, returns `{markdown}`)
    - src/backend/claude-session/identity-artifact-reader.ts lines 2395-2462 (`readAvatarSiblingFileByRole` — takes conn + roleName + avatarFilename, returns `{bytes, mime, ext}` or null)
    - src/backend/claude-session/identity-artifact-reader.ts lines 2140-2240 (`extractCosmeticsFromFrontmatter` — narrows `avatar` field)
    - src/backend/database/routes/identity-avatar-batch.test.ts (test scaffold pattern for avatar endpoints — request/response, host resolver mock, SSH conn mock)
  </read_first>
  <behavior>
    - Test A (happy path, remote host): role `box-maintainer` has `avatar: box-maintainer.webp` in frontmatter + the sibling file exists → 200 with `Content-Type: image/webp` and body === bytes returned by `readAvatarSiblingFileByRole`.
    - Test B (happy path, local host): same as A but `hostId` maps to local via `isLocalHostId` → `readRoleFileByName(null, ...)` + `readAvatarSiblingFileByRole(null, ...)` are called with conn=null.
    - Test C (no frontmatter avatar): role file has no `avatar:` frontmatter key → 404 with `{error: 'no avatar'}`.
    - Test D (frontmatter avatar but file missing): frontmatter names `foo.webp` but `readAvatarSiblingFileByRole` returns null → 404 with `{error: 'no avatar'}`.
    - Test E (invalid role name): request to `/roles/../secret/avatar?hostId=1` → 400 with `{error: 'name must match [a-z0-9-]+'}`. Fires BEFORE any host resolver or SSH work.
    - Test F (missing hostId): `/roles/foo/avatar` (no query) → 400.
    - Test G (invalid hostId): `/roles/foo/avatar?hostId=abc` → 400.
    - Test H (host resolver returns null / unreachable): 502 with `{error: 'host unreachable'}`.
    - Test I (path-traversal defense): request with `%2E%2E%2F` encoded traversal in the name param — Express decodes into `../`, which fails ROLE_NAME_PATTERN → 400. (STRIDE T-22-02-02 parallel.)
    - Test J (role file read fails — SSH exec throws): readRoleFileByName rejects → caught → 404 with `{error: 'no avatar'}` (silent fallback — mirrors identities.ts:846-848 behavior).
  </behavior>
  <action>
    Create `src/backend/database/routes/roles.ts` as a NEW Express Router file. Mirror the byte-shape of `identities.ts:767-870` `GET /:identityKey/avatar` handler, adapted for role scope:
    (1) Import: `express`, `AuthenticatedRequest`, `AuthManager`, `resolveHostById`, `connectOneShot`, `readRoleFileByName`, `readAvatarSiblingFileByRole`, `extractCosmeticsFromFrontmatter`, `AVATAR_MIME_FROM_EXT`, `isLocalHostId`, `sshLogger`.
    (2) Define `ROLE_NAME_PATTERN = /^[a-z0-9-]+$/` locally (do NOT export from roles-list-for-host.ts to keep the two routers independent — matches the "clone-then-extract" convention from CONTEXT § deferred anti-patterns).
    (3) Route `router.get("/:name/avatar", authenticateJWT, async (req, res) => { ... })`. Flow:
      - Validate `req.params.name` against ROLE_NAME_PATTERN → 400 on fail.
      - Validate `req.query.hostId` is positive integer → 400 on fail.
      - `isLocalHostId(hostId)` → local branch (conn = null); else `resolveHostById(hostId, userId)` → 502 on null; else `connectOneShot(host, 5000)` → 502 on throw.
      - `const { markdown } = await readRoleFileByName(conn, roleName)` — try/catch: catch → 404 `{error: 'no avatar'}`.
      - `const cos = extractCosmeticsFromFrontmatter(markdown)` — if `!cos.avatar` → 404 `{error: 'no avatar'}`.
      - `const readResult = await readAvatarSiblingFileByRole(conn, roleName, cos.avatar)` — null → 404 `{error: 'no avatar'}`; on throw → 404 (same silent-fallback semantics as identities.ts:846).
      - Success: `res.type(readResult.mime).send(readResult.bytes)`.
      - `finally { conn?.end() }` guard.
    (4) Export `default router`.

    Create `src/backend/database/routes/roles.test.ts` with the 10 test cases above. Use supertest + Express test app pattern from `identity-avatar-batch.test.ts`. Mock `readRoleFileByName`, `readAvatarSiblingFileByRole`, `resolveHostById`, `connectOneShot` via `vi.mock`. Reference D-08.2 in the file's top-of-file comment.
  </action>
  <verify>
    <automated>npx vitest run --related src/backend/database/routes/roles.ts src/backend/database/routes/roles.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/backend/database/routes/roles.ts` exists and default-exports an Express router.
    - `npx vitest run --related src/backend/database/routes/roles.ts` reports 10 tests passing (A–J).
    - `grep -c "ROLE_NAME_PATTERN" src/backend/database/routes/roles.ts` returns >=2 (definition + use site).
    - `npm run build:backend` compiles clean.
    - Path-traversal test (Test I) confirms `../` in the name param never reaches SSH work.
  </acceptance_criteria>
  <done>New router file + tests green + path-traversal defense proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mount `/roles/:name/avatar` router in database.ts + add `roleAvatarUrl` frontend helper</name>
  <files>src/backend/database/database.ts, src/ui/api/identities-api.ts</files>
  <read_first>
    - src/backend/database/database.ts (find the existing mount site for `rolesListForHostRouter` — mount the new router adjacent so `/roles/:name/avatar` and `/roles?hostId=<n>` sit under the same base path but bind at different route depths)
    - src/backend/database/routes/roles-list-for-host.ts (existing `/roles` mount pattern — the router is mounted at `/roles` and defines `router.get("/", ...)` for the enumeration; the new router mounts at the SAME `/roles` base and defines `router.get("/:name/avatar", ...)`)
    - src/ui/api/identities-api.ts lines 200-230 (find the existing `identityAvatarUrl(hostId, key)` helper — mirror its shape for the new `roleAvatarUrl` helper)
  </read_first>
  <behavior>
    - Test A: `roleAvatarUrl(3, 'box-maintainer')` returns `/roles/box-maintainer/avatar?hostId=3`.
    - Test B: after Task 1 lands, a live HTTP request to the mounted route responds per Task 1's behavior contract.
    - Test C: Express route ordering — `/roles?hostId=<n>` (the enumeration) still resolves via the roles-list-for-host router; `/roles/foo/avatar` resolves via the new router. (Router mount order matters — the two routers can coexist because `/` and `/:name/avatar` do not collide.)
  </behavior>
  <action>
    In `src/backend/database/database.ts`: locate the existing `app.use('/roles', rolesListForHostRouter)` mount and add `app.use('/roles', rolesRouter)` immediately AFTER it (Express matches routes in mount order; the two mount ranges do not overlap because they bind at different path depths — enumeration is `/` on the first router, avatar is `/:name/avatar` on the second). Add the import at the top of the file next to the existing roles-list import: `import rolesRouter from './routes/roles.js';`.

    In `src/ui/api/identities-api.ts`: add a new export `roleAvatarUrl(hostId: number, roleName: string): string` that returns the string `/roles/${roleName}/avatar?hostId=${hostId}`. Place it adjacent to `listRolesForHost` (L278). Reference Phase 90 Plan 90-02 + D-08.2 in a code comment. Do NOT introduce a fetching function — this returns a URL string for `<img src>` consumption, matching the identity avatar pattern where the URL is baked into `identity.avatarUrl` and rendered directly.
  </action>
  <verify>
    <automated>npx vitest run --related src/backend/database/database.ts src/backend/database/routes/roles.ts src/ui/api/identities-api.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "app.use('/roles', rolesRouter)" src/backend/database/database.ts` returns >=1.
    - `grep -c "roleAvatarUrl" src/ui/api/identities-api.ts` returns >=1.
    - `npm run build:backend && npm run build` compiles clean.
    - Roles enumeration route from Plan 90-01 still resolves (route mount ordering did not shadow it).
    - `npx vitest run --related src/backend/database/routes/roles.ts src/backend/database/routes/roles-list-for-host.ts` — BOTH suites green.
  </acceptance_criteria>
  <done>Router mounted; frontend helper available; both routes coexist; typecheck clean.</done>
</task>

</tasks>

<verification>
- `npx vitest run --related src/backend/database/routes/roles.ts src/backend/database/routes/roles-list-for-host.ts` → all cases green.
- `npm run build:backend && npm run build` → clean.
- Path-traversal defense on `:name` param provably enforced before SSH work.
</verification>

<success_criteria>
- Roles-list modal (Plan 90-05) + Role modal (Plan 90-04) can render `<img src={roleAvatarUrl(hostId, name)}>` and get real bytes for roles with cosmetic avatars.
- Roles with no avatar frontmatter surface as a broken image / neutral placeholder (D-05 fallback), NOT a 500.
</success_criteria>

<output>
Create `.planning/phases/90-role-management-modal-split-role-level-tabs-out-of-the-ident/90-02-SUMMARY.md` when done.
</output>
