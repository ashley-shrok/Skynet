---
phase: 90-role-management-modal-split
plan: 06
type: execute
wave: 4
depends_on:
  - 90-04
  - 90-05
files_modified:
  - src/ui/features/pretty-view/IdentityModal.tsx
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx
  - src/ui/features/pretty-view/IdentityModal.title-line-jump.test.tsx
  - src/ui/features/pretty-view/IdentityModal.scope-switch.test.tsx
  - src/ui/features/pretty-view/IdentityModal.role-tab.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "IdentityModal no longer renders the top segmented scope switch (`role='group' aria-label='Scope'` element at L2089-2141 is DELETED)."
    - "IdentityModal no longer renders role-scope tabs — Role file / Runbooks / Bounties / Role-Wakeups tabs and their TabsContent panes are DELETED. Post-phase tab set: Identity file, Wakeups (identity-scope), Telegram (3 tabs)."
    - "IdentityModal drops the `useModalScope`/`setModalScope`/`ModalScope` import + related state slot (`scope`) + related effect + related `onScopeChange` callback. Post-phase, ModalScope is dead code (planner-pick: retire in this phase — cheaper than a follow-up sweep)."
    - "IdentityModal role-scope state slots (`roleFileState`, `roleWakeupsState`, role-scope wakeup fetch effect) are DELETED — that state lives in RoleModal now."
    - "IdentityModal's title-line span at L1738-1742 gains a clickable treatment: cursor:pointer + color `#c4b89a` + dotted underline `rgba(196, 184, 154, 0.35)` at 2px offset + trailing chevron `›` span with `marginLeft: 3px, opacity: 0.7` + 120ms transition. On hover: color `#f0ebe0` + solid underline `rgba(240, 235, 224, 0.6)` + chevron opacity 1. `title` attr: `Open role modal: <role-display-name>`. Click closes identity modal + opens role modal for the identity's role."
    - "When `identity.title` is null, the same clickable treatment attaches to the displayName span at L1735-1737 (fallback per D-04)."
    - "PrettyView owns swap-not-stack coordination: new state slot `roleModalOpenState: {roleName, roleCosmetics, identityShimKey} | null` + `handleOpenRoleModal(identity)` handler + `<RoleModal>` mount at document.body (no `container` prop). The handler closes IdentityModal and opens RoleModal at the same tick."
    - "PrettyConversationsPanel three-dots menu order after this phase: `[New agent, Edit roles…, Edit global files…, Edit skills…]`. 'New role' entry is DELETED; 'Edit roles…' entry is INSERTED between 'New agent' and 'Edit global files…' with onClick `() => setRolesListModalOpen(true)`."
    - "PrettyConversationsPanel mounts `<RolesListModal>` as a sibling to `<GlobalFilesModal>` + `<SkillsEditorModal>`. Its `onSelectRole` handler closes the list AND opens `<RoleModal>` (also mounted here at document.body). Both are opened via the same swap-not-stack coordination as PrettyView's identity-modal jump."
    - "Stale test `PrettyConversationsPanel.new-role-button.test.tsx` is rewritten to assert 'Edit roles…' entry presence + click opens RolesListModal (planner-pick: rewrite, not retire — the coverage is still valuable, just for the new entry point per D-07)."
    - "Stale test `IdentityModal.scope-switch.test.tsx` is either RETIRED (scope switch no longer exists to test) or rewritten to assert its ABSENCE. Planner-pick: rewrite as a single 'scope-switch-absent' assertion (keeps test scaffold as a regression guard against accidental re-introduction)."
    - "Stale test `IdentityModal.role-tab.test.tsx` (asserts role-scope tab behavior in identity modal) is RETIRED — the role-tab surface is gone from identity modal. Coverage moves to `RoleModal.test.tsx` in Plan 90-04."
    - "New test `IdentityModal.title-line-jump.test.tsx` covers: title-line click closes identity modal + fires open-role-modal handler; displayName-line fallback fires when identity has no title; keyboard Enter/Space activation."
  artifacts:
    - path: "src/ui/features/pretty-view/IdentityModal.tsx"
      provides: "Post-refactor identity modal — 3 tabs only, no scope switch, title-line clickable treatment"
      contains: "onOpenRoleModal"
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "Owns swap-not-stack coordination for identity-modal title-line → role modal transition; mounts RoleModal"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "Three-dots menu swaps 'New role' → 'Edit roles…'; mounts RolesListModal + RoleModal siblings"
    - path: "src/ui/features/pretty-view/IdentityModal.title-line-jump.test.tsx"
      provides: "In-process coverage of the title-line clickable treatment"
  key_links:
    - from: "src/ui/features/pretty-view/IdentityModal.tsx"
      to: "PrettyView.tsx via onOpenRoleModal prop"
      via: "click handler on title-line span"
      pattern: "onOpenRoleModal"
    - from: "src/ui/features/pretty-view/PrettyView.tsx"
      to: "src/ui/features/pretty-view/RoleModal.tsx"
      via: "mount + state slot"
      pattern: "RoleModal"
    - from: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      to: "src/ui/features/pretty-view/RolesListModal.tsx"
      via: "three-dots 'Edit roles…' entry opens the modal"
      pattern: "RolesListModal"
---

<objective>
Land the IdentityModal refactor + wire the new modals into their parent surfaces + swap the three-dots menu + update the tests that go stale as a result. This is the plan that makes the previous 5 plans user-visible.

Refactor scope (in order of intent):
1. IdentityModal drops the scope switch + 4 role-scope tabs + all role-scope state slots + `useModalScope` dependencies (D-09 clean-cut).
2. IdentityModal title-line clickable treatment (D-04 exact spec — dotted underline, chevron, hover shifts, click → close-identity + open-role-modal).
3. PrettyView mounts RoleModal + threads onOpenRoleModal handler through IdentityModal.
4. PrettyConversationsPanel three-dots menu: "New role" → "Edit roles…" (D-07); mount RolesListModal + RoleModal siblings.
5. Stale tests: rewrite `PrettyConversationsPanel.new-role-button.test.tsx` for the new "Edit roles…" surface; rewrite `IdentityModal.scope-switch.test.tsx` as a scope-switch-absent regression guard; retire `IdentityModal.role-tab.test.tsx`; add `IdentityModal.title-line-jump.test.tsx` for the new clickable treatment.

Purpose: implements D-04 (title-line clickable treatment), D-07 (three-dots menu swap), D-09 (identity modal after this phase: role scope removed entirely). Wires the surfaces landed in Plans 90-04 + 90-05 into their triggers. Resolves the campaign step from CONTEXT § domain: "Identity modal refactor — drop the top segmented scope switch and drop the four role-scope tabs."

Output: refactored IdentityModal + PrettyView + PrettyConversationsPanel + test collateral swap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/90-role-management-modal-split-role-level-tabs-out-of-the-ident/90-CONTEXT.md
@.planning/shapes/shape-role-management-modal-split.md
@src/ui/features/pretty-view/IdentityModal.tsx
@src/ui/features/pretty-view/PrettyView.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
@src/ui/features/pretty-view/RoleModal.tsx
@src/ui/features/pretty-view/RolesListModal.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IdentityModal refactor — drop scope switch, drop 4 role-scope tabs, drop role-scope state slots, add title-line clickable treatment</name>
  <files>src/ui/features/pretty-view/IdentityModal.tsx, src/ui/features/pretty-view/IdentityModal.title-line-jump.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/IdentityModal.tsx lines 100-115 (`useModalScope`, `setModalScope`, `ModalScope` imports — DELETE)
    - src/ui/features/pretty-view/IdentityModal.tsx lines 255-278 (scope state slot + onScopeChange callback + scope-derived-from-coordinator default — DELETE)
    - src/ui/features/pretty-view/IdentityModal.tsx lines 340-370 (`NAV_SECTIONS_ROLE` array + `NAV_SECTIONS = scope === "role" ? ... : ...` conditional — DELETE `NAV_SECTIONS_ROLE`; UNWRAP `NAV_SECTIONS_IDENTITY` as the only NAV_SECTIONS)
    - src/ui/features/pretty-view/IdentityModal.tsx lines 375-395 (`roleFileState`, `roleWakeupsState` useState slots — DELETE; also delete the parallel fetch effect at L415+ that populates them via `identity:list-role-wakeups`)
    - src/ui/features/pretty-view/IdentityModal.tsx lines 2089-2141 (`<div role='group' aria-label='Scope'>` and its wrapper `<div className='shrink-0 px-4 pt-3 pb-2 flex justify-center'>` — DELETE the whole block)
    - src/ui/features/pretty-view/IdentityModal.tsx lines 2160-2200 (`<TabsContent value='role'>` — DELETE; contains `<RoleFileTab>`)
    - src/ui/features/pretty-view/IdentityModal.tsx lines 2180-2190 (`<TabsContent value='runbooks'>` — DELETE; contains `<RunbooksTab>`)
    - src/ui/features/pretty-view/IdentityModal.tsx lines 2200-2450 (`<TabsContent value='bounties'>` — DELETE; the whole sticky-search + BountyCard.map block since it's role-scope)
    - src/ui/features/pretty-view/IdentityModal.tsx lines 2515-2540 (`<TabsContent value='role-wakeups'>` — DELETE; contains `<WakeupsTab state={roleWakeupsState} ... scope='role'>`)
    - src/ui/features/pretty-view/IdentityModal.tsx lines 1731-1745 (the title-line span at L1738-1742 — where the clickable treatment attaches; also the displayName span at L1735-1737 for the fallback)
    - .planning/phases/90-role-management-modal-split-role-level-tabs-out-of-the-ident/90-CONTEXT.md § D-04 (exact color codes + underline offsets + hover states + chevron marginLeft/opacity — MUST be verbatim)
    - .planning/phases/90-role-management-modal-split-role-level-tabs-out-of-the-ident/90-CONTEXT.md § D-09 (drop role-scope entirely + ModalScope retire pick)
  </read_first>
  <behavior>
    - Test A (post-refactor tab count): mounted with any identity → `getAllByRole('tab')` returns 3 tabs matching Identity file / Wakeups / Telegram (identity-scope only).
    - Test B (no scope switch): `queryByRole('group', {name: /scope/i})` returns null.
    - Test C (no role-scope-file tab): `queryByRole('tab', {name: /^role file$/i})` returns null.
    - Test D (no runbooks tab in identity modal): `queryByRole('tab', {name: /runbooks/i})` returns null (runbooks moved to RoleModal per Plan 90-04).
    - Test E (no bounties tab in identity modal): `queryByRole('tab', {name: /bounties/i})` returns null.
    - Test F (no role-wakeups tab): only one Wakeups tab present, with `value='identity-wakeups'`; `queryAllByRole('tab', {name: /wakeups/i})` returns array length 1.
    - Test G (title-line clickable — identity has title): `identity.title === 'Skynet'` → the span rendering "Skynet" is a clickable element (button role OR clickable span with role='button' + tabIndex=0) with:
      - `title` attr matching `Open role modal:`
      - inline style `color: '#c4b89a'`
      - inline style containing `text-decoration` with dotted + underline color `rgba(196, 184, 154, 0.35)`
      - a nested chevron span with `›` text + inline style `marginLeft: 3px, opacity: 0.7`
    - Test H (title-line click fires onOpenRoleModal): clicking the title-line element → mocked `onOpenRoleModal(identity)` prop fires. IdentityModal's own onOpenChange(false) fires at the same tick (swap coordination is in the click handler).
    - Test I (title-line fallback when title null): `identity.title === null` → the displayName span (L1735-1737) gets the same clickable treatment instead. Test asserts click on displayName span fires `onOpenRoleModal`.
    - Test J (keyboard activation): Enter or Space key on focused title-line span → `onOpenRoleModal` fires (a11y).
    - Test K (identity has no role — defensive): `identity.role === null` → title-line still renders BUT is NOT clickable (no chevron, no cursor:pointer, no click handler). This is a defensive branch — an identity without a role has no target for the jump.
    - Test L (ModalScope removed): `grep -c "useModalScope\|setModalScope\|ModalScope" src/ui/features/pretty-view/IdentityModal.tsx` returns 0.
  </behavior>
  <action>
    In `IdentityModal.tsx`:
    1. Add a new required prop `onOpenRoleModal: (identity: Identity) => void` to the component's props interface (next to the existing `onOpenRunbook` prop). Update the props destructure at L192-215.
    2. Delete the `useModalScope`/`setModalScope`/`ModalScope` imports (L107-111).
    3. Delete the `scope`, `onScopeChange`, `storedScope`, and derived-scope-from-coordinator state + callback + effect (L255-278).
    4. Delete `NAV_SECTIONS_ROLE` (L351-361). Rename `NAV_SECTIONS_IDENTITY` → `NAV_SECTIONS` (unwrap the scope-conditional at L369).
    5. Delete `roleFileState`, `roleWakeupsState` state slots (L376, L384). Delete the parallel role-scope fetch effects (grep for `identity:list-role-wakeups` + `identity:get-role-file` fetch effects in IdentityModal and delete them).
    6. Delete the scope switch block at L2089-2141 (the `<div role='group' aria-label='Scope'>` wrapper — both the outer flex-center div AND the inner group div).
    7. Delete `<TabsContent value='role'>` block (L2161-2166 — the RoleFileTab render).
    8. Delete `<TabsContent value='runbooks'>` block (L2180-2189).
    9. Delete `<TabsContent value='bounties'>` block (the whole sticky-search + BountyCard render — spans roughly L2212-2515; grep for `TabsContent value="bounties"` to find the start).
    10. Delete `<TabsContent value='role-wakeups'>` block (L2515-2540 range; grep for `value="role-wakeups"`).
    11. Delete the `activeTab` default that pointed to `"role"` — new default is `"identity"` (identity-scope's first tab). Update `useState<string>(scope === "role" ? "role" : "identity")` → `useState<string>("identity")`.
    12. Title-line clickable treatment. At L1731-1743 replace the current `<div className='flex flex-col min-w-0 flex-1'>` block's inner spans with the following logic (concrete component identifiers, no fenced code):
        - Compute `canJumpToRole = identity.role !== null` boolean.
        - Compute `hasTitle = typeof identity.title === 'string' && identity.title.length > 0` boolean.
        - Render the displayName span at L1735-1737 as-is when `hasTitle === true`; when `hasTitle === false && canJumpToRole === true`, wrap its text in a clickable element (span with role='button' + tabIndex=0 + onClick handler + onKeyDown for Enter/Space) that carries the clickable-treatment inline styles from D-04.
        - Render the title span at L1738-1742 wrapped in the same clickable element WHEN `canJumpToRole === true`. Inline styles verbatim from D-04: `color: '#c4b89a'`, `textDecoration: 'underline'`, `textDecorationStyle: 'dotted'`, `textDecorationColor: 'rgba(196, 184, 154, 0.35)'`, `textUnderlineOffset: '2px'`, `cursor: 'pointer'`, `transition: 'color 120ms, text-decoration 120ms'`.
        - Nested chevron span AFTER the text: `<span style={{marginLeft: 3, opacity: 0.7, transition: 'opacity 120ms'}}>›</span>`.
        - Hover styles (D-04: color #f0ebe0, decoration-color rgba(240,235,224,0.6), decoration-style solid, chevron opacity 1): implemented via `onMouseEnter`/`onMouseLeave` inline style flips (matches the panel-menu hover pattern at PrettyConversationsPanel.tsx L2061-2062). Do NOT use pseudo-classes (:hover) via className — the inline-hover-flip is already the fleet convention.
        - `title` attr: `Open role modal: ${identity.role}` (or the role's displayName if known; else the slug — D-04 spec says role-display-name; identity.role is the slug fallback).
        - onClick handler: `(e) => { e.stopPropagation(); onOpenRoleModal(identity); onOpenChange(false); }` (close identity modal + fire the open-role-modal handler at the same tick — swap coordination lives here).
        - onKeyDown handler: `if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); onOpenRoleModal(identity); onOpenChange(false); }` (a11y).
    13. Verify Plan 90-04's RoleModal + Plan 90-05's RolesListModal are imported ONLY in the parent files (PrettyView + PrettyConversationsPanel — Tasks 2 + 3 below) — IdentityModal itself does not import RoleModal.

    Create `src/ui/features/pretty-view/IdentityModal.title-line-jump.test.tsx` with the 12 cases above (A–L). Use the existing IdentityModal test-scaffold pattern (grep `IdentityModal.runbooks-swap.test.tsx` for the closest analog — same swap-not-stack shape).

    Reference D-04 + D-09 in code comments at the touch sites.
  </action>
  <verify>
    <automated>npx vitest run --related src/ui/features/pretty-view/IdentityModal.tsx src/ui/features/pretty-view/IdentityModal.title-line-jump.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "role=\"group\"" src/ui/features/pretty-view/IdentityModal.tsx` returns 0 (scope switch removed).
    - `grep -c "useModalScope\|setModalScope\|ModalScope" src/ui/features/pretty-view/IdentityModal.tsx` returns 0 (import + all uses removed).
    - `grep -c "value=\"role\"\|value=\"runbooks\"\|value=\"bounties\"\|value=\"role-wakeups\"" src/ui/features/pretty-view/IdentityModal.tsx` returns 0 (all 4 role-scope TabsContent gone).
    - `grep -c "NAV_SECTIONS_ROLE\|NAV_SECTIONS_IDENTITY" src/ui/features/pretty-view/IdentityModal.tsx` returns 0 (both renamed → single `NAV_SECTIONS`).
    - `grep -c "roleFileState\|roleWakeupsState" src/ui/features/pretty-view/IdentityModal.tsx` returns 0.
    - `grep -c "onOpenRoleModal" src/ui/features/pretty-view/IdentityModal.tsx` returns >=2 (prop declaration + click handler).
    - `grep -c "#c4b89a" src/ui/features/pretty-view/IdentityModal.tsx` returns >=1 (D-04 color literal, dotted-underline branch).
    - `npx vitest run --related src/ui/features/pretty-view/IdentityModal.tsx src/ui/features/pretty-view/IdentityModal.title-line-jump.test.tsx` reports title-line-jump 12 tests + surviving IdentityModal tests passing.
    - `npm run build:backend && npm run build` compiles clean (backend included for belt-suspenders per box-maintainer rule — even though this task is frontend-only, ModalScope removal may touch shared types).
  </acceptance_criteria>
  <done>IdentityModal is 3 tabs; scope switch gone; ModalScope retired; title-line clickable treatment present per D-04; sibling test file covers the new surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PrettyView owns swap coordination — mount RoleModal + thread onOpenRoleModal through IdentityModal</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/PrettyView.tsx lines 3250-3295 (existing IdentityModal + RunbookEditorModal mount site — MIRROR the RunbookEditorModal swap-not-stack pattern from Plan 89-06 for the new RoleModal mount)
    - src/ui/features/pretty-view/PrettyView.tsx lines 680-700 (existing identity-modal state slot + handleOpenRunbook handler — MIRROR the shape for the new roleModalOpenState + handleOpenRoleModal)
    - src/ui/features/pretty-view/PrettyView.tsx lines 1640-1650 (existing `setIsIdentityModalOpen(false)` at the close-swap site — MIRROR)
    - src/ui/features/pretty-view/IdentityModal.tsx (post-Task-1: props signature now requires `onOpenRoleModal`)
    - src/ui/features/pretty-view/RoleModal.tsx (Plan 90-04 output — props signature: `{open, onOpenChange, roleName, roleCosmetics, hostId, identityShimKey, onOpenRunbook}`)
    - src/ui/api/identities-api.ts (fetch `RoleSummary` shape via `listRolesForHost` OR extract cosmetics from the identity's `roleDefaults` field if PrettyView already has them cached — grep for `roleDefaults`)
  </read_first>
  <behavior>
    - Test A (state slot exists): PrettyView has `roleModalOpenState: {roleName, roleCosmetics, identityShimKey, hue} | null` state slot.
    - Test B (handler shape): `handleOpenRoleModal(identity: Identity)` handler exists. Body derives `roleName = identity.role` (defensive guard: null → return without state change). Derives `roleCosmetics` from `identity.roleDefaults` if present (roleDefaults carries the role's cosmetics per identity API contract) OR fetches via `listRolesForHost(hostId).then(rows => rows.find(r => r.name === identity.role))`. Sets `roleModalOpenState = {roleName, roleCosmetics, identityShimKey: identity.identityKey, hue: roleCosmetics.colorHue ?? 190}`.
    - Test C (identity-modal close on title-line click): `handleOpenRoleModal` also fires `setIsIdentityModalOpen(false)` at the same tick — swap-not-stack.
    - Test D (RoleModal mounted): `<RoleModal>` rendered as a sibling of `<IdentityModal>` when `roleModalOpenState !== null`.
    - Test E (RoleModal close-does-not-reopen-IdentityModal): closing the role modal → `setRoleModalOpenState(null)`. IdentityModal STAYS closed per D-03 (no per-pane context, no back-navigation).
    - Test F (RoleModal runbook click: nested swap): runbook row click inside RoleModal → RoleModal's `onOpenRunbook` prop fires → PrettyView's existing `handleOpenRunbook` fires → RoleModal closes + RunbookEditorModal opens. (Mirrors Plan 89-06's IdentityModal → RunbookEditorModal swap but with RoleModal as the parent.)
    - Test G (IdentityModal prop threaded): `<IdentityModal onOpenRoleModal={handleOpenRoleModal} ... />` present at the IdentityModal mount site.
  </behavior>
  <action>
    In `PrettyView.tsx`:
    1. Add a new state slot near the existing `runbookEditorOpenState` at L680-700: `const [roleModalOpenState, setRoleModalOpenState] = useState<{roleName: string; roleCosmetics: RoleSummary; identityShimKey: string; hue: number} | null>(null);`
    2. Add a handler `handleOpenRoleModal(identity: Identity)` alongside `handleOpenRunbook`:
       - Defensive guard: `if (identity.role === null) return;`
       - Build `roleCosmetics: RoleSummary` — prefer `identity.roleDefaults` if present (grep for its shape in identities-api.ts; roleDefaults carries title/colorHue/voice/avatar for the identity's role); if not present, fall back to fetching via `listRolesForHost(hostId).then(rows => rows.find(r => r.name === identity.role))`. Reference D-04 in a code comment for the fallback rationale.
       - Set state: `setRoleModalOpenState({roleName: identity.role, roleCosmetics, identityShimKey: identity.identityKey, hue: roleCosmetics.colorHue ?? 190});`
       - NOTE: IdentityModal's title-line onClick already fires `onOpenChange(false)` (Task 1). PrettyView doesn't need to double-close.
    3. Thread the handler into `<IdentityModal>` at L3252: add `onOpenRoleModal={handleOpenRoleModal}` prop.
    4. Mount `<RoleModal>` as a sibling of `<IdentityModal>` (between `<IdentityModal>` and `<RunbookEditorModal>` at L3262-3294). Wire:
       ```
       {roleModalOpenState && (
         <RoleModal
           open={true}
           onOpenChange={(open) => { if (!open) setRoleModalOpenState(null); }}
           roleName={roleModalOpenState.roleName}
           roleCosmetics={roleModalOpenState.roleCosmetics}
           hostId={hostId}
           identityShimKey={roleModalOpenState.identityShimKey}
           onOpenRunbook={(runbookName) => {
             // Nested swap-not-stack: RoleModal → RunbookEditorModal.
             setRoleModalOpenState(null);
             handleOpenRunbook(runbookName);  // existing PrettyView handler from Plan 89-06
           }}
         />
       )}
       ```
       Reference D-03 (no per-pane context, no restore-on-close) in a code comment.
    5. `handleOpenRunbook` currently takes `(runbookName: string)` and reads `identity.role` from PrettyView's scope. When called from RoleModal, the roleName is known — planner-pick: extend `handleOpenRunbook` to take `(runbookName: string, roleName?: string)` where `roleName` overrides the identity-derived one. Update the two callers accordingly. Alternatively, plumb `roleName` into `runbookEditorOpenState`. Grep the existing handler and pick whichever is cleaner.

    Add sibling test cases to the existing `IdentityModal.runbooks-swap.test.tsx` OR create a new `PrettyView.role-modal-swap.test.tsx` file. Planner-pick: create a new file `src/ui/features/pretty-view/PrettyView.role-modal-swap.test.tsx` with the 7 cases above (A–G) to keep the sibling test scaffold isolated per the Phase-89 convention.
  </action>
  <verify>
    <automated>npx vitest run --related src/ui/features/pretty-view/PrettyView.tsx src/ui/features/pretty-view/PrettyView.role-modal-swap.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "roleModalOpenState\|handleOpenRoleModal" src/ui/features/pretty-view/PrettyView.tsx` returns >=4 (state slot + handler + at least 2 use sites).
    - `grep -c "RoleModal" src/ui/features/pretty-view/PrettyView.tsx` returns >=2 (import + mount).
    - `grep -c "onOpenRoleModal=" src/ui/features/pretty-view/PrettyView.tsx` returns >=1 (thread into IdentityModal).
    - `npx vitest run --related src/ui/features/pretty-view/PrettyView.tsx` reports all new + existing tests passing.
    - `npm run build` compiles clean.
  </acceptance_criteria>
  <done>PrettyView owns swap coordination; RoleModal mounted; nested runbook-swap-from-role-modal path proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PrettyConversationsPanel — swap 'New role' → 'Edit roles…' + mount RolesListModal + RoleModal + rewrite stale test</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx, src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx lines 1961-1990 (existing CreateRoleDialog mount — planner-pick: LEAVE IT MOUNTED here as a fallback / no-op after this phase since 'New role' menu entry is deleted. Alternative: remove the mount entirely since RolesListModal now owns creation. Grep for other callers — if `createRoleDialogOpen` is only set from the deleted menu entry, safe to delete both the state slot and the mount. Check via grep before deleting.)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx lines 1995-2011 (existing GlobalFilesModal + SkillsEditorModal mount site — INSERT `<RolesListModal>` + `<RoleModal>` mounts as siblings)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx lines 2036-2064 (three-dots menu `items` array — SWAP `{label: "New role", ...}` → `{label: "Edit roles…", onClick: () => setRolesListModalOpen(true)}`)
    - src/ui/features/pretty-view/RolesListModal.tsx (Plan 90-05 output — props: `{open, onOpenChange, hostTree, defaultHostId, onSelectRole}`)
    - src/ui/features/pretty-view/RoleModal.tsx (Plan 90-04 output — props: `{open, onOpenChange, roleName, roleCosmetics, hostId, identityShimKey, onOpenRunbook}`)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx (whole file — the "New role" menu-item assertions become false; rewrite for "Edit roles…" entry per D-07)
    - .planning/phases/90-role-management-modal-split-role-level-tabs-out-of-the-ident/90-CONTEXT.md § D-07 (menu order + entry swap semantics)
  </read_first>
  <behavior>
    - Test A (menu order): opening the three-dots menu → entries render in order `New agent`, `Edit roles…`, `Edit global files…`, `Edit skills…` (4 total; 'New role' deleted).
    - Test B ('Edit roles…' opens RolesListModal): clicking `Edit roles…` → RolesListModal mounts with `open={true}`.
    - Test C ('Edit roles…' onclick preserves menu-close): after clicking `Edit roles…`, the three-dots menu closes (mirrors the existing pattern at L2047 `closeMenu()`).
    - Test D (row click swaps to RoleModal): clicking a row in RolesListModal fires its `onSelectRole` → RolesListModal closes AND RoleModal opens (swap-not-stack per D-03).
    - Test E (role modal close): closing RoleModal → RoleModal unmounts. RolesListModal does NOT reopen (D-03 no restore-on-close).
    - Test F (RolesListModal + RoleModal mounted at document.body): both DialogPrimitive.Portal calls have no `container` prop; both appear as children of `document.body` (not inside chatRegionEl or any pane-scoped container).
    - Test G (defaultHostId=null): panel-header has no active-conversation context → `<RolesListModal defaultHostId={null} />` — matches GlobalFilesModal + SkillsEditorModal L1999/L2010 pattern.
    - Test H (stale test rewrite): the old `PrettyConversationsPanel.new-role-button.test.tsx` no longer asserts on "New role" text; instead asserts on "Edit roles…" text. Test 21a → renamed / adapted: "Test 21a (Phase 90 D-07): 'Edit roles…' is a menu item in the MoreVertical dropdown"; Test 21b: "MoreVertical menu button absent when onCreateSession is undefined" (unchanged — the gate condition still holds); Test 21c: renamed to "clicking 'Edit roles…' from the menu opens RolesListModal (detection signal: the modal's 'Roles' title present)".
  </behavior>
  <action>
    In `PrettyConversationsPanel.tsx`:
    1. Add state slot: `const [rolesListModalOpen, setRolesListModalOpen] = useState(false);` (adjacent to `globalFilesModalOpen`).
    2. Add state slot for the swap target: `const [roleModalOpenState, setRoleModalOpenState] = useState<{roleName: string; roleCosmetics: RoleSummary; identityShimKey: string} | null>(null);`
       - Note: PrettyConversationsPanel is opening RoleModal from a HOST-level context (roles-list is per-host, not per-identity). The `identityShimKey` is derived at row-click time by picking ANY identity holding the target role on the selected host — planner-pick: derive from the identities-store (grep `useIdentities` — the panel already imports it). If no such identity exists, plumb the wire-shim as a special sentinel (e.g., empty string) that RoleModal's tabs handle by rendering an error branch on identity-keyed reads.
    3. Three-dots menu items array (L2036-2042): swap the second entry.
       - BEFORE: `{ label: "New role", onClick: () => setCreateRoleDialogOpen(true) }`
       - AFTER: `{ label: "Edit roles…", onClick: () => setRolesListModalOpen(true) }`
       - Add a code comment referencing D-07 at the touch site.
    4. CreateRoleDialog mount (L1971-1985): planner-pick — LEAVE MOUNTED but only openable via the (soon-to-be-deleted) `createRoleDialogOpen` state. Grep for all `setCreateRoleDialogOpen(true)` call sites; if the ONLY caller was the deleted menu item, DELETE both the state slot and the mount. If any OTHER caller exists (e.g., an onChainToCreateIdentity return path that toggles it back open), keep the mount and delete only the menu-entry caller.
    5. Mount `<RolesListModal>` as a sibling of `<GlobalFilesModal>` + `<SkillsEditorModal>` at L1995-2011:
       ```
       <RolesListModal
         open={rolesListModalOpen}
         onOpenChange={setRolesListModalOpen}
         hostTree={hostTree ?? null}
         defaultHostId={null}
         onSelectRole={({roleName, roleCosmetics, hostId}) => {
           // D-03 swap-not-stack: close list, open modal.
           setRolesListModalOpen(false);
           // Resolve identityShimKey — pick any identity holding this role on the selected host.
           const shim = identities.find(i => i.role === roleName && i.hostId === hostId)?.identityKey ?? '';
           setRoleModalOpenState({roleName, roleCosmetics, identityShimKey: shim});
         }}
       />
       ```
    6. Mount `<RoleModal>` as a sibling immediately below `<RolesListModal>`:
       ```
       {roleModalOpenState && (
         <RoleModal
           open={true}
           onOpenChange={(open) => { if (!open) setRoleModalOpenState(null); }}
           roleName={roleModalOpenState.roleName}
           roleCosmetics={roleModalOpenState.roleCosmetics}
           hostId={/* derived from the selected host in RolesListModal — plumbed through onSelectRole payload */}
           identityShimKey={roleModalOpenState.identityShimKey}
           onOpenRunbook={(runbookName) => {
             // Nested swap: RoleModal → RunbookEditorModal — but the panel doesn't own RunbookEditorModal.
             // Planner-pick: mount RunbookEditorModal here too, mirroring PrettyView's mount.
             // Set a separate runbookEditorOpenState slot with {roleName, runbookName} and render RunbookEditorModal below.
             setRoleModalOpenState(null);
             setRunbookEditorOpenState({roleName: roleModalOpenState!.roleName, runbookName});
           }}
         />
       )}
       ```
       Also add a `runbookEditorOpenState` state slot + `<RunbookEditorModal>` mount here so RoleModal opened from the panel can still open runbooks (mirrors PrettyView's mount at L3281-3294).
    7. Add imports at the top: `import { RolesListModal } from '@/features/pretty-view/RolesListModal';`, `import { RoleModal } from '@/features/pretty-view/RoleModal';`, `import RunbookEditorModal from '@/features/pretty-view/RunbookEditorModal';` (default import matches Phase 89 pattern).

    Rewrite `src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx` per Test H's behavior spec. Keep the file name for git-blame continuity BUT update the describe block name and test-case names to reference "Edit roles…" and Phase 90 D-07.

    Reference D-07 in code comments at the menu-swap touch site.
  </action>
  <verify>
    <automated>npx vitest run --related src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "New role" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns 0 in the menu items array (verify via reading L2036-2064 explicitly, not just any "New role" string globally).
    - `grep -c "Edit roles…" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns >=1.
    - `grep -c "RolesListModal\|RoleModal " src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns >=4 (imports + 2 mounts).
    - `PrettyConversationsPanel.new-role-button.test.tsx` no longer asserts on the string "New role" as a menu entry; asserts on "Edit roles…" instead.
    - `npx vitest run --related src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` reports all 3 rewritten test cases + surviving tests passing.
    - `npm run build` compiles clean.
  </acceptance_criteria>
  <done>Menu swap done; RolesListModal + RoleModal (+ RunbookEditorModal for nested swap) mounted; stale new-role-button.test rewritten for the new entry point.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Retire / rewrite stale IdentityModal tests (`scope-switch.test.tsx` + `role-tab.test.tsx`)</name>
  <files>src/ui/features/pretty-view/IdentityModal.scope-switch.test.tsx, src/ui/features/pretty-view/IdentityModal.role-tab.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/IdentityModal.scope-switch.test.tsx (whole file — currently asserts scope-switch presence + tap-to-flip behavior; those assertions are false post-Task-1)
    - src/ui/features/pretty-view/IdentityModal.role-tab.test.tsx (whole file — currently asserts role-tab render + RoleFileTab wiring in identity modal; those assertions are false post-Task-1)
    - .planning/phases/90-role-management-modal-split-role-level-tabs-out-of-the-ident/90-CONTEXT.md § D-09 (scope-switch removal)
    - .planning/phases/90-role-management-modal-split-role-level-tabs-out-of-the-ident/90-CONTEXT.md § D-04 discretion note ("Either update it or retire it. Planner picks.")
  </read_first>
  <behavior>
    - Test A (scope-switch.test.tsx REWRITE — regression guard): the file now contains a SINGLE `describe("IdentityModal (Phase 90 D-09): scope switch removed", ...)` block with one it-block: "does NOT render a Scope segmented group" — asserts `queryByRole('group', {name: /scope/i})` returns null in a mounted IdentityModal.
    - Test B (scope-switch.test.tsx REWRITE — second guard): a second it-block "does NOT render role-scope tabs" — asserts `queryByRole('tab', {name: /role file|runbooks|bounties/i})` returns null.
    - Test C (role-tab.test.tsx RETIREMENT): the file is DELETED via `git rm` — the coverage moved to Plan 90-04's `RoleModal.test.tsx`. Confirm by absence.
    - Test D (surviving IdentityModal tests still green): `IdentityModal.test.tsx`, `IdentityModal.bounties-filter.test.tsx`, `IdentityModal.coordinator-empty.test.tsx`, `IdentityModal.inherit-override.test.tsx`, `IdentityModal.lazy-archive.test.tsx`, `IdentityModal.runbooks-swap.test.tsx`, `IdentityModal.stays-awake.test.tsx`, `IdentityModal.voice.test.tsx`, `IdentityModal.wakeup-crud.test.tsx` — some of these may assert on role-scope surfaces (e.g., `bounties-filter` is role-scope; `runbooks-swap` is role-scope). For each: (a) if the test's whole subject moved to RoleModal → DELETE the file (like role-tab.test.tsx); (b) if the test's subject stays in identity modal → keep; (c) if the test's subject partially overlaps → rewrite the affected it-blocks.
      - `IdentityModal.bounties-filter.test.tsx` — bounties tab is role-scope, DELETED from IdentityModal in Task 1. RETIRE (delete file); coverage moves to `RoleBountiesTab.test.tsx` (Plan 90-04 Task 2).
      - `IdentityModal.runbooks-swap.test.tsx` — asserts identity-modal-runbook-swap-not-stack behavior. That coordination is Plan 89-06's; after Plan 90, runbook click in identity modal doesn't exist because the Runbooks tab is gone. RETIRE (delete file); coverage for RoleModal → RunbookEditorModal swap lives in Plan 90-04's `RoleModal.test.tsx` (Test I) + Plan 90-06 Task 2's `PrettyView.role-modal-swap.test.tsx` (Test F).
      - `IdentityModal.inherit-override.test.tsx` — asserts the identity-modal cosmetic-edit block's inherit/override affordances. Those STAY on the identity modal per D-01/D-09 (Phase 86 identity-scope affordances preserved). KEEP.
      - `IdentityModal.wakeup-crud.test.tsx` — asserts wakeup CRUD. Identity-scope wakeups still tested here; role-scope wakeup test cases (if any) must be moved or deleted. GREP for `role-wakeups` / `list-role-wakeups` inside the file first; if found, delete those specific it-blocks. Otherwise KEEP.
      - `IdentityModal.voice.test.tsx`, `IdentityModal.stays-awake.test.tsx`, `IdentityModal.coordinator-empty.test.tsx`, `IdentityModal.lazy-archive.test.tsx` — identity-scope, KEEP. If any assert scope-switch presence, rewrite that it-block similar to Test A above.
      - `IdentityModal.test.tsx` — the main test file; grep for `role-scope`, `NAV_SECTIONS_ROLE`, `RoleFileTab`, `role-wakeups` and delete or rewrite affected it-blocks.
  </behavior>
  <action>
    1. Rewrite `IdentityModal.scope-switch.test.tsx`: replace the whole file body with a single describe block per Test A + B above. Keep the file for regression-guard purposes (D-09 planner-pick: cheaper than dead-code-sweep to delete).
    2. Delete `IdentityModal.role-tab.test.tsx` via `git rm` (Test C).
    3. Delete `IdentityModal.bounties-filter.test.tsx` via `git rm` (bounties moved to RoleModal).
    4. Delete `IdentityModal.runbooks-swap.test.tsx` via `git rm` (runbooks moved to RoleModal — coverage lives in RoleModal.test.tsx + PrettyView.role-modal-swap.test.tsx).
    5. For each surviving sibling test file listed under Test D behavior, grep for role-scope references. If any it-block asserts on role-scope surfaces that no longer exist in IdentityModal, delete THAT it-block (not the whole file). Preserve identity-scope coverage.
    6. Run `npx vitest run --related src/ui/features/pretty-view/IdentityModal.tsx` and iterate until all remaining tests pass. Any test that fails because it references a role-scope surface: either delete the it-block (surface moved to RoleModal) or rewrite it against RoleModal (already done in Plan 90-04 test files — deletion is the right call here).
  </action>
  <verify>
    <automated>npx vitest run --related src/ui/features/pretty-view/IdentityModal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/ui/features/pretty-view/IdentityModal.role-tab.test.tsx` returns "No such file" (file deleted).
    - `ls src/ui/features/pretty-view/IdentityModal.bounties-filter.test.tsx` returns "No such file".
    - `ls src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx` returns "No such file".
    - `IdentityModal.scope-switch.test.tsx` contains at most 2 it-blocks and both are absent-assertions.
    - `npx vitest run --related src/ui/features/pretty-view/IdentityModal.tsx` reports all surviving tests passing.
    - `IdentityModal.inherit-override.test.tsx` still passes (identity-scope cosmetic-edit affordances preserved per D-01/D-09).
  </acceptance_criteria>
  <done>Stale role-scope tests retired; scope-switch-absent regression guard installed; identity-scope tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: End-to-end in-process user-flow test — three-dots → Edit roles… → row → role modal → cosmetic edit → save</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationsPanel.role-management-flow.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx (pre-Task-4 deletion — was the closest analog for a multi-modal swap flow test; the shape it followed is worth studying before deletion)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx (Task 3's rewrite — test scaffold + mocks are the closest analog for a three-dots-menu-driven flow)
    - src/ui/features/pretty-view/RolesListModal.test.tsx (Plan 90-05 — mocks for listRolesForHost)
    - src/ui/features/pretty-view/RoleModal.test.tsx (Plan 90-04 — mocks for role-file WS + updateRoleFileByName)
    - .planning/phases/90-role-management-modal-split-role-level-tabs-out-of-the-ident/90-CONTEXT.md § domain — Tests bullet ("in-process user-flow tests walking: panel-header → three-dots → 'Edit roles…' → roles-list modal renders → click a role row → role modal opens with correct hue + content → make a cosmetic edit → save → close. Also: identity-modal title-line click → identity modal closes → role modal opens. Also: '+ New role' button in roles-list opens CreateRoleDialog.")
  </read_first>
  <behavior>
    - Test A (end-to-end panel-header path): mount PrettyConversationsPanel → click three-dots trigger → click "Edit roles…" → RolesListModal renders → mock host-picker to select host 3 → mock `listRolesForHost` returns 2 roles → assert 2 rows rendered → click first row → RolesListModal closes → RoleModal renders with the clicked role's hue + displayName + avatar src.
    - Test B (cosmetic edit inside role modal): from Test A's role modal → title input changes to "New Title" → save button clicks → mocked `updateRoleFileByName(roleName, 3, <expected combined markdown>)` fires with the frontmatter block containing `title: New Title`.
    - Test C (role modal close → dismisses to panel): close button in role modal → role modal unmounts → RolesListModal does NOT reopen (D-03).
    - Test D ('+ New role' in roles-list): from Test A's RolesListModal state → click header '+ New role' button → CreateRoleDialog mounts with `open={true}`. RolesListModal stays mounted underneath (stack per D-10).
    - Test E (identity-modal title-line → role modal path — separate mount): mount PrettyView with an identity that has `role: 'box-maintainer'` + `title: 'Skynet'` + open the identity modal → click title-line span → identity modal closes → RoleModal renders with box-maintainer hue + content.
  </behavior>
  <action>
    Create `src/ui/features/pretty-conversations/PrettyConversationsPanel.role-management-flow.test.tsx` with the 5 cases above. Use the mock patterns from `PrettyConversationsPanel.new-role-button.test.tsx` for the panel scaffolding + `IdentityModal.runbooks-swap.test.tsx` pre-deletion for the swap-flow scaffolding. Mock: `listRolesForHost`, `roleAvatarUrl`, `updateRoleFileByName`, the WS wire types the modals fetch through, and `useIdentities` (returns a Map with a shim identity for the target role so RoleModal's identityShimKey path resolves).

    Reference D-03 + D-04 + D-05 + D-07 + D-10 in the file's top-of-file comment block — this is the covering test for the whole shape.
  </action>
  <verify>
    <automated>npx vitest run --related src/ui/features/pretty-conversations/PrettyConversationsPanel.role-management-flow.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/features/pretty-conversations/PrettyConversationsPanel.role-management-flow.test.tsx` exists.
    - `npx vitest run --related src/ui/features/pretty-conversations/PrettyConversationsPanel.role-management-flow.test.tsx` reports all 5 tests passing.
    - Test coverage explicitly walks the shape file's "surfaces" list (roles-list modal + role modal + identity modal title-line jump).
  </acceptance_criteria>
  <done>End-to-end covering test in place; all 5 flow cases green.</done>
</task>

</tasks>

<verification>
- `npx vitest run --related src/ui/features/pretty-view/IdentityModal.tsx src/ui/features/pretty-view/PrettyView.tsx src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx src/ui/features/pretty-view/RoleModal.tsx src/ui/features/pretty-view/RolesListModal.tsx` → all cases green.
- `npm run build:backend && npm run build` → clean.
- `git ls-files | grep -c "IdentityModal.role-tab.test.tsx"` returns 0 (retired).
- `git ls-files | grep -c "IdentityModal.bounties-filter.test.tsx"` returns 0 (retired).
- `git ls-files | grep -c "IdentityModal.runbooks-swap.test.tsx"` returns 0 (retired).
- `git ls-files | grep -c "RoleModal.tsx"` returns 1 (Plan 90-04 output).
- `git ls-files | grep -c "RolesListModal.tsx"` returns 1 (Plan 90-05 output).
</verification>

<success_criteria>
- Identity modal is 3 tabs (identity file / wakeups / telegram); scope switch and 4 role-scope tabs are gone.
- Title-line clickable treatment matches D-04 verbatim; title fallback to displayName works when identity has no title.
- Three-dots menu order matches D-07 exactly: `[New agent, Edit roles…, Edit global files…, Edit skills…]`.
- Roles-list modal + role modal are reachable from both entry points (three-dots menu AND identity-modal title-line jump).
- Stale tests either retired or rewritten as regression guards; new test files cover the swap flows.
- Campaign constraint honored per D-11: no docker build, no full-suite test run (`--related` scoped only), no deploy. Push-only after `git pull --rebase origin feat/tab-title-from-tmux`.
</success_criteria>

<output>
Create `.planning/phases/90-role-management-modal-split-role-level-tabs-out-of-the-ident/90-06-SUMMARY.md` when done.
</output>
