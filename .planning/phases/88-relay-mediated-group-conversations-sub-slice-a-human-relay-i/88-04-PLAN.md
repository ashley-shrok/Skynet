---
phase: 88-relay-mediated-group-conversations-sub-slice-a-human-relay-i
plan: 04
type: execute
wave: 3
depends_on: ["88-01", "88-02", "88-03"]
files_modified:
  - src/backend/database/routes/users.ts
  - src/backend/database/routes/users.test.ts
  - src/backend/database/routes/delete-user-data.ts
  - src/backend/database/routes/delete-user-data.test.ts
  - src/backend/database/db/schema.ts
autonomous: true
requirements: [D-09, D-10, D-14]
tags: [phase-88, delete-flow, matrix-deactivate, schema-comment]

must_haves:
  truths:
    - "D-09: DELETE /users/delete-account calls deactivateUser(userRecord.mxid) BEFORE db.delete(users) when mxid is non-null"
    - "D-09: deleteUserAndRelatedData calls deactivateUser(mxid) BEFORE db.delete(users) when mxid is non-null; mxid is fetched from users row FIRST (Pitfall 5 — cannot read after row delete)"
    - "D-10: On Synapse-unreachable at delete time, deactivateUser failure is LOGGED (with mxid + status + error, so a future sweep can find the orphan) and the delete PROCEEDS anyway (best-effort — do not surface the failure to the caller / user)"
    - "D-10: deactivate BEFORE the row DELETE — if deactivate succeeds and DELETE fails, Matrix side is already clean and the DELETE can safely retry"
    - "mxid-null (legacy users, OIDC users): deactivateUser is NEVER called; delete proceeds as before"
    - "D-14: users.mxid column comment (schema.ts lines 35-40) updated to reflect Skynet-owned provisioning (minted at create, password discarded, POST /users/:id/mxid still exists for legacy/admin use, legacy suffix-less mxids continue to work)"
    - "D-14: matrix_admin_creds table comment (schema.ts lines 694-697) updated to reflect that human relay creds are NOT owned by the human — Skynet mints and discards, access tokens come from admin loginAsUser, humans never log in to Matrix directly"
    - "Existing tests in users.test.ts (DELETE describe block) and delete-user-data.test.ts still pass; 3 new tests in DELETE describe + 3 new tests in delete-user-data test file pass"
    - "OIDC path (registerOIDCUser) untouched per D-12 — an OIDC-created user lands with mxid=null; delete works unchanged"
    - "Telegram bridge behavior (rewriteRegistryFromCurrentState) untouched — bridge still gates on telegramBotTokens row; deactivated Matrix accounts are simply not bridged"
  artifacts:
    - path: "src/backend/database/routes/users.ts"
      provides: "deactivate-before-row-delete wired into DELETE /users/delete-account with best-effort D-10 semantics"
      contains: "deactivateUser"
    - path: "src/backend/database/routes/delete-user-data.ts"
      provides: "deactivate-before-row-delete wired into deleteUserAndRelatedData; mxid fetched from row FIRST (Pitfall 5)"
      contains: "deactivateUser"
    - path: "src/backend/database/routes/users.test.ts"
      provides: "3 new tests in DELETE describe block: mxid-populated triggers deactivate, mxid-null skips deactivate, Synapse-fail proceeds anyway"
      contains: "mockDeactivateUser"
    - path: "src/backend/database/routes/delete-user-data.test.ts"
      provides: "vi.mock for matrix-admin-client; 3 new tests: mxid-populated triggers deactivate, mxid-null skips, Synapse-fail proceeds anyway; bootstrapUsersTable + insertUser extended for mxid column"
      contains: "mockDeactivateUser"
    - path: "src/backend/database/db/schema.ts"
      provides: "users.mxid comment updated (D-14 housekeeping) + matrix_admin_creds table-level comment updated"
      contains: "Skynet mints"
  key_links:
    - from: "src/backend/database/routes/users.ts DELETE /users/delete-account"
      to: "src/backend/matrix/matrix-admin-client.ts deactivateUser"
      via: "await deactivateUser(userRecord.mxid) before db.delete(users), wrapped in best-effort log-and-proceed"
      pattern: "await deactivateUser\\(userRecord\\.mxid\\)"
    - from: "src/backend/database/routes/delete-user-data.ts"
      to: "src/backend/matrix/matrix-admin-client.ts deactivateUser"
      via: "await deactivateUser(mxid) before db.delete(users); mxid extended into the existing avatarRow select projection"
      pattern: "await deactivateUser\\("
---

<objective>
Complete Phase 88 slice A's delete-flow half by (a) wiring deactivateUser into BOTH delete paths (DELETE /users/delete-account in users.ts + deleteUserAndRelatedData in delete-user-data.ts) with best-effort D-10 semantics, (b) adding scoped tests proving deactivate-before-delete, mxid-null-skip, and Synapse-fail-still-deletes for each path, and (c) updating the two schema comments (users.mxid + matrix_admin_creds) per D-14 housekeeping to reflect Skynet-owned provisioning.

Purpose: Symmetric namespace hygiene with Plan 03. Every mxid this codebase mints (via Plan 03) or hand-imports must be deactivated on user delete so the identifier is preserved (not re-registerable) and the account is closed. Best-effort deactivation on delete (D-10, asymmetric with create's D-04 refuse) means user-delete UX is never blocked by Synapse infra weirdness — the operator gets a log line to sweep later, the user gets their deletion.

Output: modified users.ts (DELETE /delete-account only), modified users.test.ts (3 new tests in DELETE describe), modified delete-user-data.ts (deactivate wired), modified delete-user-data.test.ts (mock added + 3 new tests + bootstrapUsersTable/insertUser extended for mxid column), modified schema.ts (2 comment blocks updated — text only, no DDL).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-CONTEXT.md
@.planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-RESEARCH.md
@.planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-PATTERNS.md
@.planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-02-SUMMARY.md
@.planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-03-SUMMARY.md
@src/backend/database/routes/users.ts
@src/backend/database/routes/users.test.ts
@src/backend/database/routes/delete-user-data.ts
@src/backend/database/routes/delete-user-data.test.ts
@src/backend/database/db/schema.ts
@src/backend/matrix/matrix-admin-client.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire deactivate-before-delete into DELETE /users/delete-account in users.ts (D-09, D-10 best-effort)</name>
  <files>src/backend/database/routes/users.ts</files>
  <read_first>
    - src/backend/database/routes/users.ts lines 33-40 (import block — Plan 03 already added the matrix-admin-client import; verify deactivateUser is already imported before adding elsewhere)
    - src/backend/database/routes/users.ts lines 2279-2366 (the ENTIRE DELETE /users/delete-account handler — the target of the new deactivate-before-DELETE insertion)
    - src/backend/database/routes/users.ts lines 2289-2295 (the `userRecord` fetch — `userRecord.mxid` is available here without a second query)
    - src/backend/database/routes/users.ts lines 2345-2358 (the existing db.delete(users) + forceSave block — new deactivate call goes BEFORE db.delete)
    - .planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-RESEARCH.md § "Pattern 5: best-effort deactivation on delete paths"
    - .planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-RESEARCH.md § "Pitfall 5: `deleteUserAndRelatedData` — mxid must be fetched BEFORE row delete" (applies to users.ts too — mxid must be captured from userRecord before db.delete)
    - .planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-PATTERNS.md § "src/backend/database/routes/users.ts — DELETE /users/delete-account extension"
  </read_first>
  <behavior>
    - Behavior 1 (mxid populated → deactivate before delete): user has mxid, DELETE call succeeds, deactivateUser was called EXACTLY ONCE with userRecord.mxid, the call happened BEFORE the db.delete(users), response is 200, row is gone
    - Behavior 2 (mxid null → deactivate skipped): user has mxid=null (legacy OIDC or pre-Plan-03 user), DELETE succeeds, deactivateUser was NEVER called, response is 200, row is gone
    - Behavior 3 (Synapse fails → delete proceeds): user has mxid, mockDeactivateUser returns { ok: false, status: 504, error: "admin_api_timeout" }, DELETE call still returns 200, warning is logged with mxid + status + error, row is gone
  </behavior>
  <action>
    Modify the DELETE /users/delete-account handler (starts at line 2279, ends at line 2366) in `src/backend/database/routes/users.ts`. Do NOT touch any other route (POST /create is Plan 03's target, already done).

    STEP A — Verify imports. Plan 03 already added `import { createOrUpdateUser, deactivateUser } from "../../matrix/matrix-admin-client.js";` at the top of the file. If not present (Plan 03 hasn't landed yet), add it now — but the plan dependency chain guarantees Plan 03 has landed first (`depends_on: ["88-01", "88-02", "88-03"]` in this plan's frontmatter). If the import already exists (expected), no change needed.

    STEP B — Insert the deactivate-before-DELETE block between the existing avatar-unlink try/catch (ends at line 2343 with the closing `}` of the unlinkErr catch block) and the existing `await db.delete(users).where(eq(users.id, userId));` at line 2345.

    Insertion body (implement as directive prose):
    - The mxid is already available as `userRecord.mxid` from the fetch at lines 2290-2295. Do NOT run a second SELECT — reuse the already-loaded record.
    - Wrap the deactivation in a conditional gated on non-null mxid: `if (userRecord.mxid) { ... } else { /* legacy or OIDC user, skip deactivate */ }`.
    - Inside the conditional: `const deactivateResult = await deactivateUser(userRecord.mxid);`
    - If `!deactivateResult.ok`: log via `authLogger.warn("Matrix account deactivation failed on delete-account (orphaned mxid logged for future sweep — D-10 best-effort)", { operation: "delete_account_matrix_deactivate_failed", userId, mxid: userRecord.mxid, status: deactivateResult.status, error: deactivateResult.error });`
    - Do NOT throw. Do NOT return. Do NOT alter the response — control MUST fall through to the `await db.delete(users)` at line 2345 whether deactivation succeeded, failed, or was skipped.
    - Add a brief inline comment above the block: `// Phase 88 D-09 — deactivate Matrix account BEFORE row DELETE (Pitfall 5 — cannot read mxid after DELETE). Best-effort per D-10: Synapse failure logs a warning with the orphan mxid and proceeds with the row DELETE anyway. Refusing to delete the Skynet account because of Synapse infra weirdness is worse UX than leaving a temporarily-orphaned deactivated Matrix account behind.`

    STEP C — Do NOT modify the forceSave block at lines 2351-2358 (per RESEARCH Q3 it's already correctly wired and covers the DELETE persistence).

    STEP D — Do NOT modify the outer try/catch at lines 2289-2365 or the outer 500 error handler at line 2362. The deactivateUser call is best-effort (never throws in the discriminated-union path), and the outer catch is for genuine unhandled throws (which the deactivate flow does not produce).

    Do NOT modify POST /users/create (Plan 03's territory, already done). Do NOT modify any other route.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-taylor && npx tsc --noEmit 2>&amp;1 | grep -E "users\.ts|error TS" | head -20; grep -c 'await deactivateUser(userRecord.mxid)' src/backend/database/routes/users.ts; grep -c 'delete_account_matrix_deactivate_failed' src/backend/database/routes/users.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'await deactivateUser(userRecord.mxid)' src/backend/database/routes/users.ts` returns exactly 1 (the DELETE-flow call — Plan 03 did not use `userRecord.mxid` since its rollback uses the local `mintedMxid` variable)
    - `grep -c 'delete_account_matrix_deactivate_failed' src/backend/database/routes/users.ts` returns exactly 1 (the DELETE-path failure-log operation code)
    - `grep -c 'if (userRecord.mxid)' src/backend/database/routes/users.ts` returns exactly 1 (the mxid-null-skip conditional)
    - Line-order check: the LINE NUMBER of `await deactivateUser(userRecord.mxid)` MUST be LESS THAN the LINE NUMBER of the SECOND `await db.delete(users).where(eq(users.id, userId))` in the file (there are two: one in Plan 03's encryption-rollback, one in this DELETE flow — this asserts deactivate-before-delete). Verify by comparing `grep -n 'await deactivateUser(userRecord.mxid)'` line vs `grep -n 'await db.delete(users).where(eq(users.id, userId))'` in the DELETE handler range (lines 2279-2366)
    - `npx tsc --noEmit` reports zero new errors
    - `grep -c 'router.post("/create"' src/backend/database/routes/users.ts` returns 1 (POST /create route not accidentally modified)
    - The Plan 03 grep invariants still hold: `grep -c 'relayPassword' src/backend/database/routes/users.ts` == 2 (Plan 04 does not touch this)
  </acceptance_criteria>
  <done>
    - Deactivate-before-delete inserted, gated on non-null mxid
    - Best-effort log-and-proceed pattern on Synapse failure
    - No POST /create modifications
    - tsc clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 3 new tests to users.test.ts DELETE describe block covering deactivate-on-mxid, skip-on-null, log-and-proceed-on-Synapse-fail</name>
  <files>src/backend/database/routes/users.test.ts</files>
  <read_first>
    - src/backend/database/routes/users.ts (Task 1's updated file — the target under test)
    - src/backend/database/routes/users.test.ts lines 1794-1868 (the ENTIRE `describe("DELETE /users/delete-account ...")` block — 1 existing test + where new tests go)
    - src/backend/database/routes/users.test.ts lines 1814-1838 (the beforeEach for the DELETE describe — mockDeactivateUser reset goes here; Plan 03 already added mockDeactivateUser at the file-level mocks)
    - src/backend/database/routes/users.test.ts lines 1841-1867 (a full happy-path DELETE test as a shape template — auth setup, sqliteDb insert, deleteAccountWithAuth call, expectations)
    - src/backend/database/routes/users.test.ts lines 300-360 (Plan 03 already added mockDeactivateUser + vi.mock for matrix-admin-client at this location; verify it's present)
    - .planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-PATTERNS.md § "src/backend/database/routes/users.test.ts — extend with mint/deactivate coverage" (new test cases for DELETE describe block)
  </read_first>
  <behavior>
    - Behavior A (existing test still passes): the 1 existing `it("DELETE /users/delete-account — M5: forceSave called ...")` test still passes with the new beforeEach reset added
    - Behavior B (new test — mxid populated triggers deactivate BEFORE db.delete): insert a user with mxid="@alice_human:thenasty.taild9b663.ts.net", call DELETE /users/delete-account with valid password; assert (a) response 200, (b) mockDeactivateUser called exactly once with the mxid, (c) row is gone from users table
    - Behavior C (new test — mxid null skips deactivate): insert a user with mxid=null, DELETE succeeds, assert (a) response 200, (b) mockDeactivateUser was NOT called (`expect(mockDeactivateUser).not.toHaveBeenCalled()`), (c) row is gone
    - Behavior D (new test — Synapse fail proceeds with delete): insert a user with mxid non-null, mock mockDeactivateUser to return `{ ok: false, status: 504, error: "admin_api_timeout" }`, DELETE succeeds; assert (a) response 200 (delete not blocked), (b) mockDeactivateUser was called once, (c) row is gone (delete proceeded), (d) authLogger.warn was called (verify via the existing logger mock if it captures calls; if not, at minimum verify the response is 200 which is the observable behavior)
  </behavior>
  <action>
    Modify `src/backend/database/routes/users.test.ts`:

    STEP A — In the `describe("DELETE /users/delete-account ...")` block (starts line 1794), extend the existing `beforeEach` (lines 1814-1838) to add `mockDeactivateUser.mockClear();` and `mockDeactivateUser.mockResolvedValue({ ok: true });` alongside the existing mockClear + mockResolvedValue calls. Do NOT touch any other beforeEach reset — just add these two lines.

    STEP B — Verify Plan 03 already added the file-level `mockDeactivateUser` var + `vi.mock("../../matrix/matrix-admin-client.js", ...)` block (should be at lines 300-360 area). If NOT present, this is a Plan 03 regression — flag it in your summary. This plan does NOT re-declare that mock.

    STEP C — Add THREE new `it(...)` cases at the END of the DELETE describe block (just before the closing `});` at line 1868). Each new test mirrors the shape of the existing test at lines 1841-1867 (bcrypt-hashed password, sqliteDb insert, deleteAccountWithAuth call, assertions).

    New Test B — "DELETE /users/delete-account — deactivateUser called with userRecord.mxid before row DELETE (D-09)":
    - Setup: `const password = "s3cret123"; const passwordHash = await bcrypt.hash(password, 4);`
    - Insert user with mxid: `sqliteDb.prepare("INSERT OR REPLACE INTO users (id, username, password_hash, is_admin, is_oidc, avatar_path, mxid) VALUES (?, ?, ?, ?, ?, ?, ?)").run("alice-id", "alice", passwordHash, 0, 0, null, "@alice_human:thenasty.taild9b663.ts.net");` — NOTE: if bootstrapDb() at line 1817 does NOT include the mxid column in the users CREATE TABLE, extend that helper too (a bootstrapDb function should exist in the file — find and edit it to add `mxid TEXT` if missing; grep-verify).
    - `authControl.userId = "alice-id";`
    - `const res = await deleteAccountWithAuth(deleteServer, { password, jwt: "valid-jwt" });`
    - `expect(res.status).toBe(200);`
    - `expect(mockDeactivateUser).toHaveBeenCalledTimes(1);`
    - `expect(mockDeactivateUser).toHaveBeenCalledWith("@alice_human:thenasty.taild9b663.ts.net");`
    - `const count = sqliteDb.prepare("SELECT COUNT(*) as c FROM users WHERE id = ?").get("alice-id") as { c: number };`
    - `expect(count.c).toBe(0);`

    New Test C — "DELETE /users/delete-account — mxid=null skips deactivateUser (legacy/OIDC user)":
    - Same setup EXCEPT insert with `mxid=null`:
    - `sqliteDb.prepare("...").run("bob-id", "bob", passwordHash, 0, 0, null, null);`
    - `authControl.userId = "bob-id";`
    - `const res = await deleteAccountWithAuth(deleteServer, { password, jwt: "valid-jwt" });`
    - `expect(res.status).toBe(200);`
    - `expect(mockDeactivateUser).not.toHaveBeenCalled();`
    - Row-gone assertion identical to Test B.

    New Test D — "DELETE /users/delete-account — Synapse fail on deactivate logs warning and proceeds with DELETE (D-10)":
    - Same setup as Test B (user has mxid) EXCEPT configure mock to fail:
    - `mockDeactivateUser.mockResolvedValueOnce({ ok: false, status: 504, error: "admin_api_timeout" });`
    - `const res = await deleteAccountWithAuth(deleteServer, { password, jwt: "valid-jwt" });`
    - `expect(res.status).toBe(200);` — key assertion: delete NOT blocked by Synapse failure
    - `expect(mockDeactivateUser).toHaveBeenCalledTimes(1);`
    - `expect(mockDeactivateUser).toHaveBeenCalledWith("@alice_human:thenasty.taild9b663.ts.net");`
    - Row-gone assertion identical to Test B — proves delete PROCEEDED despite Synapse failure.

    STEP D — Do NOT modify the existing forceSave test (lines 1841-1867). Do NOT modify any other describe block.

    NOTE on bootstrapDb schema: if the existing bootstrapDb function's users CREATE TABLE does not include the `mxid TEXT` column, EXTEND it to include the column. Grep for `bootstrapDb` and inspect. Adding a nullable column to a CREATE TABLE IF NOT EXISTS in an in-memory test DB is safe and does not affect existing tests.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-taylor && npx vitest run --related src/backend/database/routes/users.ts src/backend/database/routes/users.test.ts 2>&amp;1 | tail -80</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --related src/backend/database/routes/users.ts src/backend/database/routes/users.test.ts` reports: (a) all pre-existing tests still pass across the file, (b) the 3 new DELETE tests pass, (c) zero failures, zero skipped
    - `grep -c 'DELETE /users/delete-account — deactivateUser' src/backend/database/routes/users.test.ts` returns exactly 1 (Test B title)
    - `grep -c 'mxid=null skips deactivateUser' src/backend/database/routes/users.test.ts` returns exactly 1 (Test C title)
    - `grep -c 'Synapse fail on deactivate logs warning' src/backend/database/routes/users.test.ts` returns exactly 1 (Test D title)
    - `grep -c 'expect(mockDeactivateUser).toHaveBeenCalledWith' src/backend/database/routes/users.test.ts` returns at least 2 (Tests B and D both assert the mxid was passed)
    - `grep -c 'expect(mockDeactivateUser).not.toHaveBeenCalled' src/backend/database/routes/users.test.ts` returns at least 1 (Test C mxid-null-skip)
    - If bootstrapDb was extended: `grep -c 'mxid TEXT' src/backend/database/routes/users.test.ts` returns at least 1
  </acceptance_criteria>
  <done>
    - 3 new DELETE tests pass
    - No regressions to existing tests
    - Test C proves mxid=null path skips deactivate (D-09 conditional)
    - Test D proves D-10 best-effort: response 200 despite Synapse failure
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire deactivate-before-delete into deleteUserAndRelatedData in delete-user-data.ts (D-09, D-10, Pitfall 5)</name>
  <files>src/backend/database/routes/delete-user-data.ts</files>
  <read_first>
    - src/backend/database/routes/delete-user-data.ts lines 1-31 (imports — new deactivateUser import goes here)
    - src/backend/database/routes/delete-user-data.ts lines 33-122 (the ENTIRE deleteUserAndRelatedData helper — target of the deactivate insertion at line 108-109 area)
    - src/backend/database/routes/delete-user-data.ts lines 92-109 (the existing avatarRow select + unlinkUserAvatar + db.delete(users) block — the deactivate insertion mirrors the mxid-fetch shape of avatarRow)
    - .planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-RESEARCH.md § "Pitfall 5: `deleteUserAndRelatedData` — mxid must be fetched BEFORE row delete" (critical — the mxid select must happen before db.delete)
    - .planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-PATTERNS.md § "src/backend/database/routes/delete-user-data.ts (service helper, CRUD)" (full analog map)
  </read_first>
  <behavior>
    - Behavior 1 (mxid populated): deleteUserAndRelatedData("alice_id") where alice's mxid is "@alice_human:...": deactivateUser called once with the mxid BEFORE db.delete(users) runs, row is gone at end
    - Behavior 2 (mxid null): deleteUserAndRelatedData("bob_id") where bob's mxid is null: deactivateUser NEVER called, row is gone at end
    - Behavior 3 (Synapse fail is best-effort): mockDeactivateUser returns { ok: false, status: 504, ... }; deleteUserAndRelatedData does NOT throw, row is still gone at end, warning is logged
  </behavior>
  <action>
    Modify `src/backend/database/routes/delete-user-data.ts`:

    STEP A — Add import at the top of the file. Insert after the existing `import { unlinkUserAvatar } from "./user-avatar-storage.js";` at line 4:
    - `import { deactivateUser } from "../../matrix/matrix-admin-client.js";`

    STEP B — Extend the existing `avatarRow` fetch at lines 101-104 to ALSO project the `mxid` column. Change the select projection from `{ avatarPath: users.avatarPath }` to `{ avatarPath: users.avatarPath, mxid: users.mxid }`. This is the Pitfall 5 fix — the mxid MUST be captured before the row DELETE, otherwise it's gone forever.

    STEP C — Insert the deactivate-before-DELETE block. Location: between the closing `}` of the existing `if (avatarRow.length > 0) { ... }` block at line 107 and the `await db.delete(users)...` at line 109.

    Insertion body (directive prose):
    - Extract the mxid from the already-loaded avatarRow: use a conditional to only proceed if avatarRow.length > 0 AND avatarRow[0].mxid is truthy: `if (avatarRow.length > 0 && avatarRow[0].mxid) { ... }`
    - Inside the conditional: `const deactivateResult = await deactivateUser(avatarRow[0].mxid);`
    - If `!deactivateResult.ok`: log via `authLogger.warn("Matrix account deactivation failed in deleteUserAndRelatedData (orphaned mxid logged for future sweep — D-10 best-effort)", { operation: "delete_user_and_related_data_matrix_deactivate_failed", userId, mxid: avatarRow[0].mxid, status: deactivateResult.status, error: deactivateResult.error });`
    - Do NOT throw, do NOT return early — fall through to the `await db.delete(users)` at line 109.
    - Add a brief inline comment above the block: `// Phase 88 D-09 — deactivate Matrix account BEFORE row DELETE (Pitfall 5 — mxid must be fetched from the row before it's gone; extended into the avatarRow select projection above). Best-effort per D-10: Synapse failure logs a warning with the orphan mxid and proceeds with the row DELETE anyway. This helper is called by admin-side + OIDC-merge paths; the helper's caller is responsible for the HTTP response.`

    STEP D — Do NOT add forceSave. Per RESEARCH Q3, this helper's callers own persistence — do NOT add DatabaseSaveTrigger here (would be a new pattern the plan does not authorize).

    STEP E — Do NOT modify the outer try/catch at lines 34-121 or the `throw error` at line 120. The deactivate flow is discriminated-union-safe and does not throw.

    Do NOT modify any other function in this file (there are no others — the file exports only deleteUserAndRelatedData).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-taylor && npx tsc --noEmit 2>&amp;1 | grep -E "delete-user-data\.ts|error TS" | head -20; grep -c 'from "../../matrix/matrix-admin-client.js"' src/backend/database/routes/delete-user-data.ts; grep -c 'await deactivateUser(avatarRow\[0\].mxid)' src/backend/database/routes/delete-user-data.ts; grep -c 'mxid: users.mxid' src/backend/database/routes/delete-user-data.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'from "../../matrix/matrix-admin-client.js"' src/backend/database/routes/delete-user-data.ts` returns exactly 1 (the new import)
    - `grep -c 'await deactivateUser(avatarRow\[0\].mxid)' src/backend/database/routes/delete-user-data.ts` returns exactly 1
    - `grep -c 'mxid: users.mxid' src/backend/database/routes/delete-user-data.ts` returns exactly 1 (the extended select projection)
    - `grep -c 'delete_user_and_related_data_matrix_deactivate_failed' src/backend/database/routes/delete-user-data.ts` returns exactly 1 (the failure-log operation code)
    - Line-order check: `grep -n 'await deactivateUser' src/backend/database/routes/delete-user-data.ts` line number MUST be LESS THAN `grep -n 'await db.delete(users).where(eq(users.id, userId))'` line number (deactivate-before-DELETE)
    - `grep -c 'DatabaseSaveTrigger\|forceSave' src/backend/database/routes/delete-user-data.ts` returns 0 (per RESEARCH Q3 — this helper does NOT do its own forceSave; callers own persistence)
    - `npx tsc --noEmit` reports zero new errors
  </acceptance_criteria>
  <done>
    - deactivateUser import added
    - avatarRow projection extended to include mxid (Pitfall 5 fix)
    - deactivate call inserted before db.delete, gated on non-null mxid
    - Best-effort log-and-proceed on Synapse failure
    - No forceSave added (per RESEARCH Q3)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add mock + 3 new tests to delete-user-data.test.ts (mxid-triggers-deactivate, mxid-null-skips, Synapse-fail-still-deletes); extend bootstrapUsersTable + insertUser for mxid column</name>
  <files>src/backend/database/routes/delete-user-data.test.ts</files>
  <read_first>
    - src/backend/database/routes/delete-user-data.ts (Task 3's updated file — the target under test)
    - src/backend/database/routes/delete-user-data.test.ts lines 1-50 (file-header + shared state + mockUnlinkUserAvatar declaration pattern to mirror for mockDeactivateUser)
    - src/backend/database/routes/delete-user-data.test.ts lines 47-100 (existing vi.mock block for ../db/index.js — the Drizzle chain proxy; new proxy MUST support the extended select projection with `mxid`)
    - src/backend/database/routes/delete-user-data.test.ts lines 253-291 (bootstrapUsersTable and insertUser helpers — both need mxid column extension)
    - src/backend/database/routes/delete-user-data.test.ts lines 297-441 (the existing describe block with 3 existing tests — where new tests go)
    - .planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-PATTERNS.md § "src/backend/database/routes/delete-user-data.test.ts — add deactivate-path cases" (full spec — mock addition, bootstrapUsersTable update, insertUser update, new test cases)
  </read_first>
  <behavior>
    - Behavior A: existing 3 tests still pass with the new mock and helper extensions
    - Behavior B (new — mxid-populated triggers deactivate): insertUser with mxid="@dave_human:server", call deleteUserAndRelatedData("dave_id"), assert mockDeactivateUser called once with the mxid, assert row gone
    - Behavior C (new — mxid-null skips): insertUser with mxid=null, deleteUserAndRelatedData, assert mockDeactivateUser NOT called, assert row gone
    - Behavior D (new — Synapse fail proceeds): insertUser with mxid non-null, mock returns fail, deleteUserAndRelatedData does NOT throw, mockDeactivateUser called once, row still gone
  </behavior>
  <action>
    Modify `src/backend/database/routes/delete-user-data.test.ts`:

    STEP A — Add mockDeactivateUser at file scope, following the exact pattern of mockUnlinkUserAvatar at line 41:
    - Add above line 43: `const mockDeactivateUser = vi.fn<[string], Promise<{ ok: true } | { ok: false; status: number; error: string }>>();`

    STEP B — Add the vi.mock for matrix-admin-client.js alongside the existing user-avatar-storage vi.mock (which is roughly around line 224 based on PATTERNS.md guidance; find via grep). Insert:
    - `vi.mock("../../matrix/matrix-admin-client.js", () => ({ deactivateUser: (...args: [string]) => mockDeactivateUser(...args), }));`

    STEP C — Update the vi.mock for `../db/index.js` (the Drizzle proxy at lines 47-100 approximately). The existing `buildSelectChain(projection)` helper needs to support projecting `mxid` alongside `avatarPath`. Inspect the current implementation of buildSelectChain — if it hardcodes only `avatarPath`, extend it to project any column named in the projection object AS-IS from the sqliteDb query result. Concrete guidance: after the select query runs and gets a row, iterate over the keys of the passed-in `projection` object and produce a result object with matching keys read from the sqliteDb row. If the current implementation already generalizes, no change needed — verify by reading the code carefully.

    STEP D — Extend `bootstrapUsersTable` (lines 253-263) to add `mxid TEXT` column:
    - Change the CREATE TABLE users statement to include `mxid TEXT` after `avatar_path TEXT` — matching Plan 03's INSERT column addition. Do NOT add NOT NULL — the column is nullable per Phase 75 schema.

    STEP E — Extend `insertUser` helper (lines 273-291) to accept an optional `mxid?: string | null` field:
    - Signature: `opts: { id: string; username?: string; avatarPath?: string | null; mxid?: string | null }`
    - Change INSERT column list to include `mxid` and add the `?` placeholder + `opts.mxid ?? null` positional arg.
    - Concrete SQL: `INSERT INTO users (id, username, password_hash, is_admin, is_oidc, avatar_path, mxid) VALUES (?, ?, ?, ?, ?, ?, ?)`
    - Concrete .run: `.run(opts.id, opts.username ?? opts.id, "hash", 0, 0, opts.avatarPath ?? null, opts.mxid ?? null);`

    STEP F — Extend the `beforeEach` in the describe block (lines 300-317) to reset mockDeactivateUser:
    - Add `mockDeactivateUser.mockClear();`
    - Add `mockDeactivateUser.mockResolvedValue({ ok: true });`

    STEP G — Add 3 new `it(...)` cases at the END of the describe block (before the closing `});` at line 441 approximately):

    New Test B — "deleteUserAndRelatedData calls deactivateUser with mxid before row DELETE (D-09)":
    - `const { deleteUserAndRelatedData } = await import("./delete-user-data.js");`
    - `insertUser(sqliteDb!, { id: "dave_id", avatarPath: null, mxid: "@dave_human:server" });`
    - `await deleteUserAndRelatedData("dave_id");`
    - `expect(mockDeactivateUser).toHaveBeenCalledTimes(1);`
    - `expect(mockDeactivateUser).toHaveBeenCalledWith("@dave_human:server");`
    - `const remaining = sqliteDb!.prepare("SELECT * FROM users WHERE id = ?").all("dave_id");`
    - `expect(remaining).toHaveLength(0);`

    New Test C — "deleteUserAndRelatedData skips deactivateUser when mxid is null":
    - `insertUser(sqliteDb!, { id: "eve_id", avatarPath: null, mxid: null });`
    - `await deleteUserAndRelatedData("eve_id");`
    - `expect(mockDeactivateUser).not.toHaveBeenCalled();`
    - Assert row gone (same pattern as Test B).

    New Test D — "deleteUserAndRelatedData proceeds with row delete on Synapse deactivate failure (D-10 best-effort)":
    - `insertUser(sqliteDb!, { id: "frank_id", avatarPath: null, mxid: "@frank_human:server" });`
    - `mockDeactivateUser.mockResolvedValueOnce({ ok: false, status: 504, error: "admin_api_timeout" });`
    - `await expect(deleteUserAndRelatedData("frank_id")).resolves.toBeUndefined();` — critical: MUST NOT throw
    - `expect(mockDeactivateUser).toHaveBeenCalledTimes(1);`
    - `expect(mockDeactivateUser).toHaveBeenCalledWith("@frank_human:server");`
    - Assert row gone (proves delete PROCEEDED despite Synapse fail).

    Do NOT modify the existing 3 tests. Do NOT add tests outside the existing describe block.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-taylor && npx vitest run --related src/backend/database/routes/delete-user-data.ts src/backend/database/routes/delete-user-data.test.ts 2>&amp;1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --related src/backend/database/routes/delete-user-data.ts src/backend/database/routes/delete-user-data.test.ts` reports: 3 pre-existing tests still pass + 3 new tests pass, zero failures, zero skipped
    - `grep -c 'const mockDeactivateUser' src/backend/database/routes/delete-user-data.test.ts` returns exactly 1
    - `grep -c 'vi.mock("../../matrix/matrix-admin-client.js"' src/backend/database/routes/delete-user-data.test.ts` returns exactly 1
    - `grep -c 'mxid TEXT' src/backend/database/routes/delete-user-data.test.ts` returns at least 1 (bootstrapUsersTable extended)
    - `grep -cE 'INSERT INTO users \(.*mxid\) VALUES' src/backend/database/routes/delete-user-data.test.ts` returns 1 (insertUser INSERT extended)
    - `grep -c '@dave_human:server\|@eve\|@frank_human' src/backend/database/routes/delete-user-data.test.ts` returns at least 2 (dave and frank fixtures — eve is null so no mxid string)
    - `grep -c 'expect(mockDeactivateUser).toHaveBeenCalledWith' src/backend/database/routes/delete-user-data.test.ts` returns at least 2 (Tests B and D)
    - `grep -c 'expect(mockDeactivateUser).not.toHaveBeenCalled' src/backend/database/routes/delete-user-data.test.ts` returns at least 1 (Test C)
    - `grep -c 'resolves.toBeUndefined' src/backend/database/routes/delete-user-data.test.ts` returns at least 1 (Test D — best-effort must-not-throw)
  </acceptance_criteria>
  <done>
    - mockDeactivateUser + vi.mock added
    - bootstrapUsersTable + insertUser extended for mxid column
    - Drizzle proxy select-chain supports mxid projection
    - 3 new tests pass proving deactivate/skip/best-effort
  </done>
</task>

<task type="auto">
  <name>Task 5: Update schema.ts comments for users.mxid + matrix_admin_creds to reflect Skynet-owned provisioning (D-14 housekeeping)</name>
  <files>src/backend/database/db/schema.ts</files>
  <read_first>
    - src/backend/database/db/schema.ts lines 12-51 (users table definition; specifically lines 35-40 with the mxid column comment to update, and the mxid column declaration at line 40)
    - src/backend/database/db/schema.ts lines 684-710 (matrix_admin_creds table definition; specifically lines 694-697 with the "human relay creds are owned by the human" comment to update)
    - .planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-CONTEXT.md § D-14 (the housekeeping decision — comments only, no DDL)
    - .planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-RESEARCH.md § Q6 (Drizzle comment-only changes do NOT generate migrations)
  </read_first>
  <behavior>
    - Behavior 1 (users.mxid comment updated): the block comment above `mxid: text("mxid")` at line 40 reflects Skynet-owned provisioning (mint at create, password discarded, legacy suffix-less mxids continue to work, POST /users/:id/mxid still exists for legacy/admin association use)
    - Behavior 2 (matrix_admin_creds comment updated): the comment block at lines 694-697 (currently claiming "human relay creds are owned by the human and never stored anywhere in Skynet") is updated to reflect that Skynet mints and discards, humans do NOT own their Matrix credentials, and access tokens come from admin loginAsUser
    - Behavior 3 (no DDL changes): the `mxid: text("mxid")` column declaration itself and the matrixAdminCreds table structure are UNCHANGED — comments only
    - Behavior 4 (no migration generated): after this edit, `npx drizzle-kit generate` (if run) produces no new migration (comment changes don't affect DDL per RESEARCH Q6). Test task does not need to run drizzle-kit — this is a documentation claim.
  </behavior>
  <action>
    Modify `src/backend/database/db/schema.ts`:

    STEP A — Update the comment block at lines 35-40 (currently reads: "Phase 75 Plan 01 (Q3 locked decision) — mxid mapping for the Matrix relay. Nullable: only humans with a registered relay account have one, and it's populated via POST /users/:id/mxid (Plan 03) or the one-shot import for Ashley/Zoe/Laura. Not a credential; agents' relay identifiers live on-disk in ~/.claude/identities/<name>/relay.json per fleet convention.").

    New comment text (directive — write in the same block-comment style, 5-7 lines, above the `mxid: text("mxid")` line):
    - Reference Phase 88 slice A (2026-09-08) as the update source: "Phase 88 slice A (D-14 housekeeping)".
    - State the new provisioning model: Skynet mints the whole Matrix account (via matrix-admin-client.createOrUpdateUser) as part of POST /users/create; the mxid is populated in the same INSERT that writes the user row.
    - Note the naming convention: new-user mxids follow `@<sanitized-username>_human:<server_name>` (bijective sanitizer, `_human` suffix). Legacy users (Ashley/Zoey/Laura on t1000, imported before slice A) have suffix-less mxids that continue to work indefinitely.
    - Note that nullable is preserved: OIDC users (D-12 out of scope) and legacy pre-Phase-88 users may still have mxid=null.
    - Note the existing admin endpoint: POST /users/:id/mxid (user-admin-routes.ts) is untouched — kept for admin-manual mxid association use.
    - Note that DELETE /users/delete-account and deleteUserAndRelatedData now deactivate the Matrix account (best-effort) when mxid is populated.
    - Note that Skynet holds no password for this account: mint password is discarded; runtime access tokens come from admin loginAsUser (deferred to a later sub-slice).

    STEP B — Update the comment block at lines 694-697 (currently reads: "This is the ONLY relay credential that lives in Skynet's own storage. Agent relay creds live on-disk at ~/.claude/identities/<name>/relay.json (fleet convention, Phase 69); human relay creds are owned by the human and never stored anywhere in Skynet.").

    New comment text (directive — 4-6 lines, above `export const matrixAdminCreds = sqliteTable("matrix_admin_creds", {`):
    - State: This is the sole set of Matrix credentials stored in Skynet DB — the @skynet-admin singleton used by matrix-admin-client for every admin API call.
    - State: Agent relay creds live on-disk at ~/.claude/identities/<name>/relay.json per fleet convention (Phase 69) — unchanged.
    - STATE THE UPDATE (D-14): Human relay accounts are Skynet-owned as of Phase 88 slice A — Skynet mints via admin API (createOrUpdateUser) with a Skynet-generated password that is discarded on the spot. Humans hold no credential of their own for their Matrix account. Runtime access tokens come from admin.loginAsUser (deferred to later sub-slice per D-13). Direct human login to Matrix clients (Element etc.) is explicitly not supported (D-08 rationale).

    STEP C — Do NOT modify the mxid column declaration (`mxid: text("mxid")` at line 40) — comment only.

    STEP D — Do NOT modify the matrixAdminCreds table body (lines 698-710) — comment only.

    STEP E — Do NOT touch any other schema table.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-taylor && npx tsc --noEmit 2>&amp;1 | grep -E "schema\.ts|error TS" | head -20; grep -c 'Phase 88' src/backend/database/db/schema.ts; grep -c 'Skynet mints\|Skynet-owned' src/backend/database/db/schema.ts; grep -c 'human relay creds are owned by the human' src/backend/database/db/schema.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Phase 88' src/backend/database/db/schema.ts` returns at least 2 (one in each updated comment — users.mxid + matrix_admin_creds)
    - `grep -c 'Skynet-owned\|Skynet mints' src/backend/database/db/schema.ts` returns at least 2 (present in both updated comments)
    - `grep -c 'human relay creds are owned by the human' src/backend/database/db/schema.ts` returns 0 (the legacy claim is removed — D-14 explicitly rejects this framing)
    - `grep -c 'mxid: text("mxid")' src/backend/database/db/schema.ts` returns 1 (column declaration unchanged)
    - `grep -c 'matrixAdminCreds = sqliteTable' src/backend/database/db/schema.ts` returns 1 (table declaration unchanged)
    - `grep -c '_human' src/backend/database/db/schema.ts` returns at least 1 (naming convention mentioned in the mxid comment)
    - `grep -c 'D-14\|D-08\|D-13' src/backend/database/db/schema.ts` returns at least 1 (traceability to CONTEXT decisions)
    - `npx tsc --noEmit` reports zero new errors (comments don't affect types, but verify the file still parses)
  </acceptance_criteria>
  <done>
    - users.mxid block comment updated with Skynet-owned provisioning story
    - matrix_admin_creds block comment updated; legacy "owned by human" claim removed
    - No DDL changes (grep verifies both column and table declarations unchanged)
    - tsc still clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user → DELETE /users/delete-account | JWT-authenticated caller with valid password crosses here (existing auth + password + last-admin guards upstream) |
| admin caller / OIDC-merge → deleteUserAndRelatedData | server-internal caller (admin routes / OIDC callback merge) — mxid comes from server-loaded users row |
| both delete paths → deactivateUser | mxid string (from users.mxid, server-controlled) flows into best-effort deactivation |
| deactivateUser → Synapse | outbound HTTPS with admin bearer (inherited from Plan 02's threat surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-16 | Denial of Service | Synapse-unreachable blocks user's self-delete | mitigate | D-10 best-effort explicitly proceeds with row DELETE on Synapse failure. Test 3 in Task 2 + Test D in Task 4 both prove response 200 despite mocked Synapse failure. |
| T-88-17 | Tampering | Pitfall 5 — mxid captured after row DELETE (would leak orphan Matrix account forever) | mitigate | Task 3 Step B extends the avatarRow select projection to include mxid, capturing it in memory BEFORE the row DELETE. Task 1's DELETE handler reuses the already-loaded userRecord.mxid from the auth-time SELECT. Line-order check in acceptance criteria proves deactivate-before-delete for both paths. |
| T-88-18 | Information Disclosure | log spam / user-visible Synapse error on user-facing delete flow | mitigate | Deactivate failure is logged at `warn` level with structured fields (userId, mxid, status, error) — never surfaced in the HTTP response body. The user sees the same success response whether Synapse deactivation succeeded or failed. |
| T-88-19 | Elevation of Privilege | attacker forces deactivation of another user's mxid via race | accept | Both delete paths use server-loaded userRecord.mxid (from the authenticated userId via the JWT auth middleware or admin auth); the mxid cannot be caller-supplied. No new attack surface introduced beyond what existed for the DELETE route itself. |
| T-88-20 | Tampering | schema comment update introduces DDL drift or missing migration | mitigate | Comment-only changes per D-14. Drizzle diffs only DDL structure (types, constraints, table def) — not JSDoc/block comments. Task 5 acceptance grep proves the column declaration + table declaration lines are unchanged. RESEARCH Q6 documents Drizzle behavior for comment-only edits (no migration generated). |
| T-88-SC | Tampering | npm package installs | mitigate | No new packages. Only existing in-tree deactivateUser (from Plan 02) + existing schema.ts + existing test infrastructure. RESEARCH.md § Package Legitimacy Audit confirms N/A. |
</threat_model>

<verification>
- Run: `npx vitest run --related src/backend/database/routes/users.ts src/backend/database/routes/users.test.ts src/backend/database/routes/delete-user-data.ts src/backend/database/routes/delete-user-data.test.ts src/backend/database/db/schema.ts` — all pre-existing tests + 6 new tests (3 in each of the two delete test files) pass
- Run: `npx tsc --noEmit` — zero new errors in the five modified files
- Grep: `grep -c 'await deactivateUser(userRecord.mxid)' src/backend/database/routes/users.ts` == 1 (DELETE-flow deactivate call)
- Grep: `grep -c 'await deactivateUser(avatarRow\[0\].mxid)' src/backend/database/routes/delete-user-data.ts` == 1 (helper deactivate call)
- Grep: `grep -c 'human relay creds are owned by the human' src/backend/database/db/schema.ts` == 0 (D-14 legacy claim removed)
- Grep: `grep -c 'Phase 88' src/backend/database/db/schema.ts` >= 2 (both comment blocks updated)
- Line-order check (users.ts DELETE handler): `grep -n 'await deactivateUser(userRecord.mxid)' src/backend/database/routes/users.ts` line number < `grep -n 'await db.delete(users).where(eq(users.id, userId))' src/backend/database/routes/users.ts` line number for the DELETE-route occurrence
- Line-order check (delete-user-data.ts): `grep -n 'await deactivateUser' src/backend/database/routes/delete-user-data.ts` line number < `grep -n 'await db.delete(users)' src/backend/database/routes/delete-user-data.ts` line number
</verification>

<success_criteria>
- Both delete paths deactivate the Matrix account BEFORE the row DELETE when mxid is populated (D-09)
- mxid=null path (legacy / OIDC users per D-12) skips deactivation without error
- Synapse-unreachable on delete never blocks the Skynet-side delete (D-10 best-effort) — user's self-delete still works, admin-side delete still works, OIDC-merge still works
- Log lines on deactivation failure carry the mxid so a future orphan-sweep tool (deferred per CONTEXT.md) can grep them out
- Schema comments accurately describe Skynet-owned provisioning (D-14 housekeeping) — the legacy "human owns their relay creds" claim is fully removed
- No DDL changes to the users or matrix_admin_creds tables — no migration generated
- Telegram bridge behavior unchanged (bridge's `telegramBotTokens` gate at bridge-config-writer.ts step 6 remains the effective filter for which users get bridged)
- OIDC-callback registerOIDCUser path (per D-12) untouched — new users through that path continue to land with mxid=null and are unaffected by these delete-flow changes
</success_criteria>

<output>
Create `.planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-04-SUMMARY.md` when done, containing:
- Line-number ranges of the deactivate insertion for both delete paths
- Confirmation that mxid is captured BEFORE db.delete in both paths (Pitfall 5 avoided)
- Confirmation that no forceSave was added to deleteUserAndRelatedData (RESEARCH Q3 rule)
- Test run output showing all pre-existing tests + 6 new tests pass
- Grep confirmation that the D-14 legacy comment ("human relay creds are owned by the human") is fully removed
- Grep confirmation that no DDL changes were made to schema.ts (column and table declarations grep-count unchanged)
- Statement of end-of-Phase-88 verification: `find .planning/phases/88-* -name "88-*-SUMMARY.md" | wc -l` should return 4 (four plan summaries), and the phase is ready for end-of-phase human-check + code review + closeout
</output>
