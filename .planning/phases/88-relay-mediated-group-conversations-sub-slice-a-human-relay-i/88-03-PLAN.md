---
phase: 88-relay-mediated-group-conversations-sub-slice-a-human-relay-i
plan: 03
type: execute
wave: 2
depends_on: ["88-01", "88-02"]
files_modified:
  - src/backend/database/routes/users.ts
  - src/backend/database/routes/users.test.ts
  - src/backend/database/routes/user-avatars.integration.test.ts
autonomous: true
requirements: [D-03, D-04, D-05, D-08, D-11]
tags: [phase-88, users-create, matrix-mint, integration]

must_haves:
  truths:
    - "D-03: POST /users/create both PICKS the mxid (via buildHumanMxid) AND mints the Matrix account (via createOrUpdateUser); the existing POST /users/:id/mxid admin endpoint is NOT touched"
    - "D-04: Synapse-unreachable at mint time (or getMatrixAdminCreds returns null) → 500 response, ZERO local side effects (no avatar file written, no user row inserted)"
    - "D-05: Mint happens BEFORE avatar write (mint-first ordering). Rollback path for post-mint failures deactivates the just-minted Matrix account (best-effort log)"
    - "D-08: Password generated by generateHumanRelayPassword(), passed to createOrUpdateUser, then discarded on the spot — never logged, never stored in Skynet DB, never sent in an HTTP response"
    - "D-11: Displayname passed to createOrUpdateUser at mint time (title-cased local part of username, or full email — implementer chooses per D-11 discretion)"
    - "Raw INSERT column list at users.ts:180-201 is extended to include the `mxid` column (Pitfall 1) — the minted mxid is persisted in the same INSERT that writes the user row; no separate UPDATE"
    - "users.test.ts: all 7 existing POST /users/create tests still pass with the new mocks added (no regressions); 3 new create-flow tests pass (mint-success, mint-failure-refuses, post-mint-failure-rolls-back-deactivate)"
    - "user-avatars.integration.test.ts: existing end-to-end integration test still passes with the new mocks added (no regressions)"
  artifacts:
    - path: "src/backend/database/routes/users.ts"
      provides: "POST /users/create with Step 3.5 (mint) + extended rollback (deactivate on post-mint failure) + INSERT column list extension for mxid"
      contains: "createOrUpdateUser"
    - path: "src/backend/database/routes/users.test.ts"
      provides: "vi.mock for matrix-admin-client + username-to-mxid + matrix-admin-creds-store; 3 new tests in describe(\"POST /users/create\")"
      contains: "mockCreateOrUpdateUser"
    - path: "src/backend/database/routes/user-avatars.integration.test.ts"
      provides: "vi.mock for matrix-admin-client + username-to-mxid + matrix-admin-creds-store (default happy-path stubs so existing integration test still passes)"
      contains: "vi.mock(\"../../matrix/matrix-admin-client.js\""
  key_links:
    - from: "src/backend/database/routes/users.ts POST /users/create"
      to: "src/backend/matrix/matrix-admin-client.ts createOrUpdateUser"
      via: "await createOrUpdateUser(mxid, relayPassword, displayname) at Step 3.5"
      pattern: "await createOrUpdateUser\\("
    - from: "src/backend/database/routes/users.ts POST /users/create rollback branches"
      to: "src/backend/matrix/matrix-admin-client.ts deactivateUser"
      via: "await deactivateUser(mxid) in the avatar-write / SQL-INSERT / encryption-setup catch blocks"
      pattern: "await deactivateUser\\("
    - from: "src/backend/database/routes/users.ts POST /users/create"
      to: "src/backend/matrix/username-to-mxid.ts buildHumanMxid + generateHumanRelayPassword + extractServerName"
      via: "import from ../../matrix/username-to-mxid.js"
      pattern: "from \"\\.\\./\\.\\./matrix/username-to-mxid\\.js\""
    - from: "INSERT INTO users column list"
      to: "mxid column"
      via: "add `mxid` at end of both column list and placeholder list; pass mintedMxid as last positional arg"
      pattern: "INSERT INTO users \\(.*, mxid\\) VALUES"
---

<objective>
Extend `POST /users/create` in `src/backend/database/routes/users.ts` to eagerly provision the Matrix relay identity as part of user creation (D-03), with mint-first ordering + post-mint rollback deactivation (D-05), Synapse-unreachable → 500 with zero side effects (D-04), password minted-and-discarded (D-08), Matrix displayname set at mint time (D-11), and the `mxid` column added to the raw INSERT (Pitfall 1). Update `users.test.ts` to mock the new dependencies and add three create-flow test cases. Update `user-avatars.integration.test.ts` to mock the same dependencies so the existing end-to-end test keeps passing.

Purpose: This is the CORE integration for slice A's create half. After this plan, every newly-created Skynet user has a durable Matrix relay identity by the time the POST /users/create response returns 200. The rollback discipline ensures that any post-mint failure leaves the Matrix side clean (deactivated) — an orphan mxid rather than a phantom account. The delete half (Plan 04) uses the same primitives to preserve symmetric namespace hygiene.

Output: Modified `users.ts` (POST /create only — DELETE and other routes untouched), modified `users.test.ts` (3 new tests + mocks), modified `user-avatars.integration.test.ts` (mocks added — no assertion changes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-CONTEXT.md
@.planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-RESEARCH.md
@.planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-PATTERNS.md
@.planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-01-SUMMARY.md
@.planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-02-SUMMARY.md
@src/backend/database/routes/users.ts
@src/backend/database/routes/users.test.ts
@src/backend/database/routes/user-avatars.integration.test.ts
@src/backend/matrix/matrix-admin-client.ts
@src/backend/matrix/username-to-mxid.ts
@src/backend/matrix/matrix-admin-creds-store.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire mint-first + INSERT-column-extension + post-mint rollback into POST /users/create (D-03, D-04, D-05, D-08, D-11)</name>
  <files>src/backend/database/routes/users.ts</files>
  <read_first>
    - src/backend/database/routes/users.ts lines 1-40 (import block — new imports MUST be added here)
    - src/backend/database/routes/users.ts lines 90-289 (the ENTIRE POST /users/create handler — the target of Step 3.5 insertion + INSERT column-list extension + rollback extensions)
    - src/backend/matrix/username-to-mxid.ts (Plan 01 output — the four helpers being imported)
    - src/backend/matrix/matrix-admin-client.ts (Plan 02 output — createOrUpdateUser signature at lines 66-70 and deactivateUser signature added at end of file)
    - src/backend/matrix/matrix-admin-creds-store.ts (the getMatrixAdminCreds signature; returns null when unbootstrapped)
    - .planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-RESEARCH.md § "Pattern 4: mint-first ordering with post-mint rollback" — exact code shape
    - .planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-RESEARCH.md § "Pitfall 1: Row INSERT does not include `mxid` column today"
    - .planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-RESEARCH.md § Q10 "Rollback path — exact deactivation call sequence"
    - .planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-PATTERNS.md § "src/backend/database/routes/users.ts — POST /users/create extension" (full analog map)
  </read_first>
  <behavior>
    - Behavior 1 (mint-success happy path): a POST /users/create with a valid username/password/avatar succeeds, the response is 200, and the users row has `mxid` populated with the just-minted value
    - Behavior 2 (mint-failure refuses create): if createOrUpdateUser returns { ok: false, ... }, the response is 500, ZERO local side effects occur (no avatar file, no users row), and the error is logged with the mxid + status + error code
    - Behavior 3 (getMatrixAdminCreds null refuses create): if getMatrixAdminCreds returns null (admin creds not bootstrapped), the response is 500, ZERO local side effects, and the error is logged with a distinct code from mint failure
    - Behavior 4 (post-mint avatar-write failure deactivates + 500): if createOrUpdateUser succeeds but writeUserAvatar throws, deactivateUser is called with the minted mxid (best-effort log, ignore result), the response is 500, no users row is inserted
    - Behavior 5 (post-mint SQL INSERT failure deactivates + unlinks avatar + 500): if the raw INSERT throws, unlinkUserAvatar is called (existing) AND deactivateUser is called with the minted mxid (best-effort log), the response is 500
    - Behavior 6 (post-mint encryption-setup failure deactivates + unlinks avatar + deletes row + 500): if authManager.registerUser throws, unlinkUserAvatar + db.delete(users) run (existing) AND deactivateUser is called with the minted mxid (best-effort log), the response is 500
    - Behavior 7 (relayPassword never logged): grep of the entire updated handler for the variable name `relayPassword` MUST show it appearing ONLY in (a) the const declaration and (b) the createOrUpdateUser call — never inside an `authLogger` call, never in a JSON response, never in an error message
    - Behavior 8 (deactivate on rollback is best-effort): the deactivateUser calls in the three rollback branches MUST NOT throw or block the 500 response — a deactivation failure logs the mxid + error code and proceeds to the return
  </behavior>
  <action>
    Modify `src/backend/database/routes/users.ts` in the following surgical order. Do NOT modify any other route in this file (DELETE /delete-account is Plan 04's target).

    STEP A — Add imports (at the end of the existing import block around lines 33-39, just before `const authManager` at line 41):

    - `import { createOrUpdateUser, deactivateUser } from "../../matrix/matrix-admin-client.js";`
    - `import { buildHumanMxid, generateHumanRelayPassword, extractServerName } from "../../matrix/username-to-mxid.js";`
    - `import { getMatrixAdminCreds } from "../../matrix/matrix-admin-creds-store.js";`

    STEP B — Insert Step 3.5 (the mint step) between the uniqueness-check block (ends at line 145 with the 409 return) and the "Step 4" avatar-write block (begins at line 151 `let avatarFilename`). Insert AFTER `const id = nanoid();` at line 149 so `id` is available if needed. Place it INSIDE the outer `try` block (which starts at line 133), so any unexpected throw during mint is caught by the outer error handler at line 285.

    Step 3.5 body (this is the CRITICAL new block — implement as directive prose, not code):

    - Call `const adminCreds = await getMatrixAdminCreds();` — resolves the encrypted homeserver base + admin user + admin token.
    - If `adminCreds` is null: log via `authLogger.error("Matrix admin creds missing during user create", { operation: "user_create_admin_creds_missing", username })`, then `return res.status(500).json({ error: "relay identity provisioning failed: admin creds missing" });`. Because this branch fires BEFORE any file write or INSERT, D-04's "zero side effects" invariant is satisfied automatically.
    - Compute `const serverName = extractServerName(adminCreds.homeserverBase);` — pure helper, cannot throw.
    - Compute `const mintedMxid = buildHumanMxid(username, serverName);` — pure helper, cannot throw.
    - Compute `const relayPassword = generateHumanRelayPassword();` — 48-char hex, D-08.
    - Derive displayname per D-11. Implementation choice (per D-11 "implementer to pick"): use the pre-`@` local part of the username, title-cased. Concretely: `const displayname = username.includes("@") ? username.split("@")[0] : username;` then title-case the first letter: `const displayname = ${first_local_part}[0].toUpperCase() + ${first_local_part}.slice(1);`. Extract this into a small local `deriveDisplayname(username: string): string` helper defined at module scope just above the router if useful for readability, OR inline it — either is acceptable, but the logic MUST be single-source (no duplication).
    - Call `const mintResult = await createOrUpdateUser(mintedMxid, relayPassword, displayname);`.
    - If `mintResult.ok === false`: log via `authLogger.error("Matrix account mint failed during user create", { operation: "user_create_matrix_mint_failed", username, mxid: mintedMxid, status: mintResult.status, error: mintResult.error })` — note the log record includes `mxid`, `status`, `error` but does NOT include `relayPassword`. Then `return res.status(500).json({ error: "relay identity provisioning failed" });`. Again, zero side effects because we haven't reached the avatar write yet — D-04 satisfied.
    - After successful mint, the local variable `relayPassword` MUST be discarded on the spot per D-08. Do NOT stash it in a request-scoped var, do NOT log it. The best way to enforce this is to declare `relayPassword` inside a tightly-scoped block (`{ ... }`) that ends immediately after the createOrUpdateUser call, OR to `relayPassword = "";` reassign after the call (defensive scrubbing — the JS engine cannot enforce it, but it makes reviewer intent explicit).

    STEP C — Extend the raw INSERT column list at lines 180-201 to include `mxid` (Pitfall 1):

    - In the SQL string (currently ends with `..., avatar_path) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`), APPEND `, mxid` before the closing paren of the column list, and APPEND `, ?` before the closing paren of the VALUES placeholder list.
    - The final SQL must read: `INSERT INTO users (id, username, password_hash, is_admin, is_oidc, client_id, client_secret, issuer_url, authorization_url, token_url, identifier_path, name_path, scopes, totp_secret, totp_enabled, totp_backup_codes, avatar_path, mxid) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)` — one additional column, one additional placeholder.
    - In the `.run(...)` call at lines 183-201, APPEND `mintedMxid` as the LAST positional argument (18th arg — matching the new 18th placeholder).
    - Do NOT change any other column, do NOT reorder existing columns, do NOT switch to Drizzle ORM (the raw INSERT is intentional per RESEARCH.md Pitfall 1).

    STEP D — Extend the three post-mint rollback branches. Each addition is best-effort: call deactivateUser, log the result if non-ok, and PROCEED to the existing 500 response. The deactivate call MUST NOT be awaited-and-rethrown — use a small helper OR inline a try/catch. Recommendation: define a local const at the top of the handler (after Step 3.5 completes) called `bestEffortDeactivate` that captures `mintedMxid` and returns a function. Example directive: `const bestEffortDeactivate = async (reason: string) => { const r = await deactivateUser(mintedMxid); if (!r.ok) { authLogger.warn("Matrix account deactivation failed during create rollback (orphaned mxid logged for future sweep)", { operation: reason, mxid: mintedMxid, status: r.status, error: r.error }); } };`

    Then insert `await bestEffortDeactivate("user_create_deactivate_after_avatar_fail")` (or the corresponding reason label) into each of the three catch branches BEFORE the existing `return res.status(500).json(...)`:

    - Branch 1 — avatar-write catch at lines 157-170: insert `await bestEffortDeactivate("user_create_deactivate_after_avatar_fail")` after the existing `authLogger.error(...)` and before the `return res.status(500).json({ error: "avatar write failed" })`. The mime-mismatch early-return path (lines 158-165) also needs deactivation before returning 400, because we minted before the write attempt — add the same call in that branch too.
    - Branch 2 — SQL INSERT catch at lines 204-212: insert `await bestEffortDeactivate("user_create_deactivate_after_insert_fail")` after `await unlinkUserAvatar(avatarFilename)` and before the `return res.status(500).json({ error: "user create failed" })`.
    - Branch 3 — encryption-setup catch at lines 244-260: insert `await bestEffortDeactivate("user_create_deactivate_after_encryption_fail")` after `await db.delete(users).where(eq(users.id, id))` and before the `return res.status(500).json({ error: "Failed to setup user security..." })`.

    STEP E — Verify the outer error handler at lines 285-288 (`} catch (err) { authLogger.error("Failed to create user", err); res.status(500).json({ error: "Failed to create user" }); }`) is NOT modified. Any Step 3.5 throw (unexpected — the pure helpers cannot throw, and createOrUpdateUser + getMatrixAdminCreds return discriminated unions, not throws) falls through here. Do NOT add deactivation logic in the outer catch — if we reach it after mint success, we don't know whether we did or didn't mint, so best-effort deactivation with a maybe-unknown mxid is unsafe. Accept the outer-catch case as "already at zero side effects" (we're only in the outer catch on throws we didn't anticipate, and those happen BEFORE the row is committed to persistence via forceSave). Document this decision with a one-line comment at the outer catch.

    DO NOT MODIFY:
    - The DELETE /users/delete-account route (lines 2279-2366) — that's Plan 04.
    - Any other route (registration lookups, PUT /:id/avatar, GET /:id/avatar, etc.).
    - The existing forceSave block at lines 265-272 (already covers the mxid write since it's now part of the INSERT).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-taylor && npx tsc --noEmit 2>&amp;1 | grep -E "users\.ts|error TS" | head -20; grep -c 'createOrUpdateUser\|deactivateUser\|buildHumanMxid\|generateHumanRelayPassword\|extractServerName\|getMatrixAdminCreds' src/backend/database/routes/users.ts; grep -c 'INSERT INTO users.*, mxid) VALUES' src/backend/database/routes/users.ts; grep -c 'user_create_matrix_mint_failed\|user_create_admin_creds_missing\|user_create_deactivate_after' src/backend/database/routes/users.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'createOrUpdateUser' src/backend/database/routes/users.ts` returns at least 2 (import + call site)
    - `grep -c 'deactivateUser' src/backend/database/routes/users.ts` returns at least 2 (import + at least 1 call site — should be 4+ with 3 rollback branches referencing bestEffortDeactivate that awaits deactivateUser once)
    - `grep -c 'buildHumanMxid\|generateHumanRelayPassword\|extractServerName' src/backend/database/routes/users.ts` returns at least 6 (each of 3 helpers used at least twice: import + call site)
    - `grep -c 'getMatrixAdminCreds' src/backend/database/routes/users.ts` returns at least 2 (import + call site in Step 3.5)
    - `grep -cE 'INSERT INTO users \(.*, mxid\) VALUES \(.*\?, \?\)' src/backend/database/routes/users.ts` returns 1 (proves INSERT column list AND placeholder list both got extended)
    - `grep -c 'user_create_matrix_mint_failed' src/backend/database/routes/users.ts` returns 1 (the mint-failure log operation)
    - `grep -c 'user_create_admin_creds_missing' src/backend/database/routes/users.ts` returns 1 (the null-creds log operation)
    - `grep -cE 'user_create_deactivate_after_(avatar|insert|encryption)_fail' src/backend/database/routes/users.ts` returns 3 (one log operation per rollback branch — mime-mismatch branch may reuse one of these labels or add a 4th; count is >=3)
    - `grep -c 'relayPassword' src/backend/database/routes/users.ts` returns exactly 2 (declaration + createOrUpdateUser call — proves NO logging or JSON-response inclusion of the password per D-08 / T-88-03)
    - `grep -cE 'authLogger\.(warn|error|info)\(.*relayPassword' src/backend/database/routes/users.ts` returns 0 (double-check: relayPassword never inside a log call)
    - `grep -cE 'res\.(status|json).*relayPassword' src/backend/database/routes/users.ts` returns 0 (relayPassword never in an HTTP response body)
    - `npx tsc --noEmit` reports zero new errors in `users.ts`
    - `grep -c 'router.delete("/delete-account"' src/backend/database/routes/users.ts` returns 1 (DELETE route header still present, unmodified — sanity check that we didn't accidentally touch it)
  </acceptance_criteria>
  <done>
    - Step 3.5 (mint) inserted after uniqueness check, before avatar write
    - Admin-creds-null branch returns 500 with zero side effects
    - Mint-failure branch returns 500 with zero side effects
    - INSERT column list extended with mxid + placeholder
    - Three rollback branches call bestEffortDeactivate before returning 500 (and the mime-mismatch 400 branch)
    - relayPassword grep proves never-logged discipline
    - tsc clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add mocks + 3 new tests to users.test.ts covering create-flow mint success / mint failure / post-mint rollback</name>
  <files>src/backend/database/routes/users.test.ts</files>
  <read_first>
    - src/backend/database/routes/users.ts (Task 1's updated file — the target under test)
    - src/backend/database/routes/users.test.ts lines 1-100 (file header + existing imports + the user-avatar-storage vi.mock at line 61 as a precedent for how new module mocks look)
    - src/backend/database/routes/users.test.ts lines 300-360 (mock section — where new vi.mock blocks go)
    - src/backend/database/routes/users.test.ts lines 633-714 (describe("POST /users/create ...") block — where new tests go and how existing tests are structured)
    - src/backend/database/routes/users.test.ts lines 660-700 (a full happy-path test as a shape template)
    - .planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-RESEARCH.md § Q4 "Test patterns for POST /users/create + DELETE routes" and § Q9 "Existing tests that could break"
    - .planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-PATTERNS.md § "src/backend/database/routes/users.test.ts — extend with mint/deactivate coverage" (full mock + test spec)
  </read_first>
  <behavior>
    - Behavior A (existing tests still pass): after adding the new mocks, all 7 existing POST /users/create tests (in the describe at line 633) still pass without any assertion changes — they inherit the happy-path default mocks
    - Behavior B (new test — mint-success wires mxid into users row): a valid create call, mock createOrUpdateUser to resolve ok with mxid `"@alice_human:thenasty.taild9b663.ts.net"`, assert (a) response is 200, (b) mockCreateOrUpdateUser was called exactly once, (c) the users row's mxid column matches the mocked mxid (query the in-memory sqlite the test uses)
    - Behavior C (new test — mint-failure refuses create): mock createOrUpdateUser to resolve `{ ok: false, status: 502, error: "admin_api_proxy_error" }`; call POST /users/create; assert (a) response status is 500, (b) response body is `{ error: "relay identity provisioning failed" }`, (c) NO users row is inserted (SELECT COUNT returns 0), (d) writeUserAvatar mock was NEVER called, (e) deactivateUser mock was NEVER called
    - Behavior D (new test — post-mint avatar-write failure calls deactivateUser): mock createOrUpdateUser to resolve ok, mock writeUserAvatar to throw; call POST /users/create; assert (a) response is 500, (b) mockDeactivateUser was called exactly once with the minted mxid, (c) NO users row is inserted
  </behavior>
  <action>
    Modify `src/backend/database/routes/users.test.ts`:

    STEP A — Add three new vi.mock blocks in the mock section (around lines 330-360, alongside the existing vi.mock blocks). Place them AFTER the existing `vi.mock("./delete-user-data.js", ...)` at line 330:

    Mock 1 — matrix-admin-client.js:
    - Declare TWO module-level mock vars (following the hoisting-safe pattern seen at lines 147-149 for existing hoisted mocks) BEFORE the vi.mock block:
      - `const mockCreateOrUpdateUser = vi.fn<[string, string, string?], Promise<{ ok: true; mxid: string; password: string; status: number } | { ok: false; status: number; error: string }>>();`
      - `const mockDeactivateUser = vi.fn<[string], Promise<{ ok: true } | { ok: false; status: number; error: string }>>();`
    - vi.mock block:
      - `vi.mock("../../matrix/matrix-admin-client.js", () => ({ createOrUpdateUser: (...args: [string, string, string?]) => mockCreateOrUpdateUser(...args), deactivateUser: (...args: [string]) => mockDeactivateUser(...args), }));`
    - NOTE ON HOISTING: vi.mock is hoisted; the getter-wrapper pattern is required so the vi.fn refs are lazy-evaluated at call time (see existing lines 147-149 for precedent).

    Mock 2 — username-to-mxid.js (fixed happy-path stubs so tests don't have to reason about sanitizer output):
    - `vi.mock("../../matrix/username-to-mxid.js", () => ({ buildHumanMxid: vi.fn((_username: string, _serverName: string) => "@alice_human:thenasty.taild9b663.ts.net"), generateHumanRelayPassword: vi.fn(() => "deadbeef00112233445566778899aabbccddeeff00112233"), extractServerName: vi.fn(() => "thenasty.taild9b663.ts.net"), sanitizeUsernameToLocalpart: vi.fn((s: string) => s.toLowerCase()), }));`

    Mock 3 — matrix-admin-creds-store.js (happy-path default: admin creds resolve non-null):
    - `vi.mock("../../matrix/matrix-admin-creds-store.js", () => ({ getMatrixAdminCreds: vi.fn(async () => ({ homeserverBase: "http://100.113.23.63:8008", userId: "@skynet-admin:thenasty.taild9b663.ts.net", accessToken: "syt_admin_token", password: "admin-pw", })), }));`

    STEP B — Extend the existing `beforeEach` in `describe("POST /users/create ...")` (around lines 674-695). Immediately after any existing per-test resets, add:
    - `mockCreateOrUpdateUser.mockClear();`
    - `mockDeactivateUser.mockClear();`
    - `mockCreateOrUpdateUser.mockResolvedValue({ ok: true, mxid: "@alice_human:thenasty.taild9b663.ts.net", password: "test-pw", status: 201 });`
    - `mockDeactivateUser.mockResolvedValue({ ok: true });`

    This ensures the 7 existing tests get happy-path defaults and don't break.

    STEP C — Add 3 new `it(...)` cases at the END of the `describe("POST /users/create ...")` block (just before the closing `});` at approximately line 1189):

    New Test B — "mint-success populates users.mxid":
    - Perform a POST /users/create with a valid multipart body (username, password, avatar bytes) — mirror the shape of the existing happy-path tests in the same describe block.
    - Assert response status is 200.
    - Assert `mockCreateOrUpdateUser` was called exactly once (`expect(mockCreateOrUpdateUser).toHaveBeenCalledTimes(1)`).
    - Assert the first argument was the mocked mxid (`expect(mockCreateOrUpdateUser.mock.calls[0][0]).toBe("@alice_human:thenasty.taild9b663.ts.net")`).
    - Query the in-memory sqlite for the newly-created user's mxid — SELECT mxid FROM users WHERE username = <the username used> — and assert it equals `"@alice_human:thenasty.taild9b663.ts.net"`. Use the same DB access pattern as any existing test in the file that reads the users row after a create.
    - Assert `mockDeactivateUser` was NOT called (`expect(mockDeactivateUser).not.toHaveBeenCalled()`).

    New Test C — "mint-failure refuses create with zero side effects (D-04)":
    - Configure `mockCreateOrUpdateUser.mockResolvedValueOnce({ ok: false, status: 502, error: "admin_api_proxy_error" });` at the top of the test (overrides the beforeEach happy-path default).
    - Perform the same POST /users/create call.
    - Assert response status is 500.
    - Assert response body contains `{ error: "relay identity provisioning failed" }` (exact string).
    - Assert `mockCreateOrUpdateUser` was called exactly once.
    - Assert `mockDeactivateUser` was NEVER called (mint didn't succeed, no deactivation needed).
    - Assert NO users row exists — `SELECT COUNT(*) FROM users WHERE username = <the username>` returns 0.
    - Assert the writeUserAvatar mock was NEVER called (existing mock reference in the file — verify by asserting its call count is 0 for this test). If writeUserAvatar mock is not currently exposed for call-count assertion, add a `vi.spyOn` or extend the existing user-avatar-storage vi.mock at line 61 to also track the write helper — do NOT rewrite the whole mock, just add the tracking capability.

    New Test D — "post-mint avatar-write failure calls deactivateUser (D-05 rollback)":
    - Leave `mockCreateOrUpdateUser` at the beforeEach happy-path default (ok, mxid populated).
    - Configure the writeUserAvatar mock to REJECT (throw) on this test. The existing `vi.mock("./user-avatar-storage.js", ...)` at line 61 uses `importOriginal` — extend it OR use a `vi.mocked(writeUserAvatar).mockRejectedValueOnce(new Error("disk full"))` pattern if the mock structure supports it. If neither works cleanly, add a targeted `vi.spyOn(...)` inside this test.
    - Perform POST /users/create.
    - Assert response status is 500.
    - Assert `mockDeactivateUser` was called exactly once (`expect(mockDeactivateUser).toHaveBeenCalledTimes(1)`).
    - Assert `mockDeactivateUser` was called with the minted mxid: `expect(mockDeactivateUser.mock.calls[0][0]).toBe("@alice_human:thenasty.taild9b663.ts.net");`
    - Assert NO users row exists.

    Do NOT modify the 7 existing tests. Do NOT modify the `describe("PUT /users/:id/avatar ...")`, `describe("GET /users/:id/avatar ...")`, or `describe("DELETE /users/delete-account ...")` blocks — Plan 04 owns the DELETE block.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-taylor && npx vitest run --related src/backend/database/routes/users.ts src/backend/database/routes/users.test.ts 2>&amp;1 | tail -80</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --related src/backend/database/routes/users.ts src/backend/database/routes/users.test.ts` reports: (a) all pre-existing tests still pass (no regressions across all 4 describe blocks in the file), (b) 3 new tests in `describe("POST /users/create ...")` pass, (c) zero failures, zero skipped
    - `grep -c 'mockCreateOrUpdateUser\|mockDeactivateUser' src/backend/database/routes/users.test.ts` returns at least 6 (2 declarations + at least 4 uses across new tests)
    - `grep -c 'vi.mock("../../matrix/matrix-admin-client.js"' src/backend/database/routes/users.test.ts` returns exactly 1
    - `grep -c 'vi.mock("../../matrix/username-to-mxid.js"' src/backend/database/routes/users.test.ts` returns exactly 1
    - `grep -c 'vi.mock("../../matrix/matrix-admin-creds-store.js"' src/backend/database/routes/users.test.ts` returns exactly 1
    - `grep -c 'relay identity provisioning failed' src/backend/database/routes/users.test.ts` returns exactly 1 (from Test C's exact-error-string assertion)
    - `grep -c '@alice_human:thenasty.taild9b663.ts.net' src/backend/database/routes/users.test.ts` returns at least 3 (mock default + Tests B and D assertions)
    - `grep -c 'describe("POST /users/create' src/backend/database/routes/users.test.ts` returns exactly 1 (did not accidentally create a second describe block)
  </acceptance_criteria>
  <done>
    - Existing 7 POST /users/create tests still pass with the new mocks defaulted to happy-path
    - 3 new tests exercise mint-success, mint-failure, and post-mint rollback deactivation
    - No changes to unrelated describe blocks
    - Scoped vitest run reports all pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add matrix-admin-client / username-to-mxid / matrix-admin-creds-store mocks to user-avatars.integration.test.ts</name>
  <files>src/backend/database/routes/user-avatars.integration.test.ts</files>
  <read_first>
    - src/backend/database/routes/user-avatars.integration.test.ts lines 1-100 (file header + imports)
    - src/backend/database/routes/user-avatars.integration.test.ts lines 79-332 (existing vi.mock block section — where new mocks go)
    - src/backend/database/routes/user-avatars.integration.test.ts lines 585-650 (the bootstrapUsersTable — verify `mxid` column already present at approximately line 590 per PATTERNS.md)
    - .planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-PATTERNS.md § "src/backend/database/routes/user-avatars.integration.test.ts — mock update"
  </read_first>
  <behavior>
    - Behavior A: after adding the three new vi.mock blocks, the existing `it("create → serve → change → serve → delete → verify-clean")` integration test still passes with no assertion changes
    - Behavior B: the added mocks match the same shape used by users.test.ts (Task 2) — createOrUpdateUser resolves ok, deactivateUser resolves ok, buildHumanMxid returns a fixed mxid, generateHumanRelayPassword returns a fixed 48-char hex, extractServerName returns a fixed server, getMatrixAdminCreds resolves a fixed non-null value
  </behavior>
  <action>
    Modify `src/backend/database/routes/user-avatars.integration.test.ts`:

    STEP A — Locate the existing vi.mock block cluster (around lines 79-332 per PATTERNS.md). Add THREE new vi.mock blocks alongside them (place them anywhere in that cluster — grouping them together at the end is fine).

    Mock 1 — matrix-admin-client:
    - `vi.mock("../../matrix/matrix-admin-client.js", () => ({ createOrUpdateUser: vi.fn(async () => ({ ok: true, mxid: "@alice_human:thenasty.taild9b663.ts.net", password: "test-pw", status: 201 })), deactivateUser: vi.fn(async () => ({ ok: true })), }));`

    Mock 2 — username-to-mxid:
    - `vi.mock("../../matrix/username-to-mxid.js", () => ({ buildHumanMxid: vi.fn(() => "@alice_human:thenasty.taild9b663.ts.net"), generateHumanRelayPassword: vi.fn(() => "deadbeef00112233445566778899aabbccddeeff00112233"), extractServerName: vi.fn(() => "thenasty.taild9b663.ts.net"), sanitizeUsernameToLocalpart: vi.fn((s: string) => s.toLowerCase()), }));`

    Mock 3 — matrix-admin-creds-store:
    - `vi.mock("../../matrix/matrix-admin-creds-store.js", () => ({ getMatrixAdminCreds: vi.fn(async () => ({ homeserverBase: "http://100.113.23.63:8008", userId: "@skynet-admin:thenasty.taild9b663.ts.net", accessToken: "syt_admin_token", password: "admin-pw", })), }));`

    STEP B — Do NOT modify the bootstrapUsersTable CREATE TABLE statement. PATTERNS.md verified `mxid TEXT` is already at line 590, so the schema supports the mxid write from the extended INSERT.

    STEP C — Do NOT modify any `it(...)` case. The existing integration test's assertions are unchanged; the new mocks just make the imports resolve.

    STEP D — Do NOT add any new test. This plan's new test coverage lives in users.test.ts (Task 2); the integration test is only touched to keep it green.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-taylor && npx vitest run --related src/backend/database/routes/user-avatars.integration.test.ts 2>&amp;1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --related src/backend/database/routes/user-avatars.integration.test.ts` reports the existing test passing (no regressions)
    - `grep -c 'vi.mock("../../matrix/matrix-admin-client.js"' src/backend/database/routes/user-avatars.integration.test.ts` returns exactly 1
    - `grep -c 'vi.mock("../../matrix/username-to-mxid.js"' src/backend/database/routes/user-avatars.integration.test.ts` returns exactly 1
    - `grep -c 'vi.mock("../../matrix/matrix-admin-creds-store.js"' src/backend/database/routes/user-avatars.integration.test.ts` returns exactly 1
    - `grep -c '@alice_human:thenasty.taild9b663.ts.net' src/backend/database/routes/user-avatars.integration.test.ts` returns at least 2 (createOrUpdateUser mock + buildHumanMxid mock)
    - Number of `it(...)` cases in this file is unchanged from pre-modification count
  </acceptance_criteria>
  <done>
    - 3 new mocks added
    - No new tests, no test assertions changed
    - Existing integration test still green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → POST /users/create | untrusted `username`, `password`, avatar bytes cross here (existing validation + registration gate + mime whitelist all upstream) |
| POST /users/create → sanitizer + mint | `username` (validated non-empty, unique-in-DB) flows into buildHumanMxid + createOrUpdateUser |
| POST /users/create → Skynet DB INSERT | server-composed `mintedMxid` (never user-controlled after sanitizer) flows into the raw INSERT |
| POST /users/create rollback branches → deactivateUser | `mintedMxid` (in scope from Step 3.5) flows into best-effort deactivation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-03 | Information Disclosure | relayPassword logged / persisted / returned in HTTP response | mitigate | Password lives in a single local `const relayPassword` bound to `generateHumanRelayPassword()`, passed exactly once to `createOrUpdateUser`, then goes out of scope. Test suite acceptance criteria grep proves the string `relayPassword` appears exactly twice (declaration + createOrUpdateUser call — never in `authLogger`, `res.status`, or `res.json`). No new DB column added; the Skynet users table never sees the value. |
| T-88-10 | Denial of Service | mint call hang blocks user create | mitigate | createOrUpdateUser inherits the 30s AbortController timeout from matrix-admin-client.ts. On timeout, mintResult.ok===false triggers the D-04 refuse-create path (500 response, zero side effects). Documented behavior. |
| T-88-11 | Tampering | race between mint success and INSERT (username uniqueness enforced at DB, mint idempotent) | accept | The mint is IDEMPOTENT (Synapse admin PUT v2 upserts). If two concurrent create attempts for the same username race: (a) the DB-level username uniqueness check gates one out at line 138; (b) if both pass the uniqueness check somehow (racy read), both attempt mint — mint upserts safely, no leaked state; (c) whichever hits INSERT second gets a UNIQUE constraint violation and rolls back — the rollback deactivates via bestEffortDeactivate, which will race with the winner's own mint but again the admin API is idempotent under the hood. Documented per RESEARCH.md Q10. Acceptable per phase context brief item 6c. |
| T-88-12 | Tampering | race between mint + concurrent deactivation from a delete (very unlikely — same admin token, same server) | accept | This would require a user being both created and deleted at the same instant; the delete path (Plan 04) reads users.mxid before deactivating, so it can't race with an INSERT that hasn't happened yet. Documented per phase context brief item 6d. |
| T-88-13 | Information Disclosure | Synapse response body leak (Matrix errcodes, admin error strings) into Skynet HTTP response or logs | mitigate | The mint-failure log at Step 3.5 includes only `mxid`, `status`, `error` (where `error` is one of the module-scoped constants from matrix-admin-client.ts, NOT the upstream response body — this was verified in Plan 02 Test 2). The mint-failure HTTP response returns the fixed string `"relay identity provisioning failed"`, not the upstream body. |
| T-88-14 | Tampering | Pitfall 1 — mxid silently not persisted because INSERT column list was hand-rolled without mxid | mitigate | Step C of Task 1 explicitly extends the INSERT column list AND placeholder list AND positional args to include mxid. Task 2 Test B queries `SELECT mxid FROM users` and asserts non-null — a regression would fail this test immediately. |
| T-88-15 | Tampering | rollback branches skip deactivation on encryption-fail (Pitfall 4 — phantom Matrix account) | mitigate | Task 1 Step D wires bestEffortDeactivate into all THREE post-mint rollback branches (avatar, INSERT, encryption). Task 2 Test D explicitly asserts deactivateUser called on avatar-write failure. RESEARCH.md Q10 documents the exact sequence. |
| T-88-SC | Tampering | npm package installs | mitigate | No new packages. Only existing in-tree modules (matrix-admin-client, username-to-mxid, matrix-admin-creds-store, all authored earlier in the phase or pre-existing). RESEARCH.md § Package Legitimacy Audit confirms N/A. |
</threat_model>

<verification>
- Run: `npx vitest run --related src/backend/database/routes/users.ts src/backend/database/routes/users.test.ts src/backend/database/routes/user-avatars.integration.test.ts` — all pre-existing tests + 3 new create-flow tests pass
- Run: `npx tsc --noEmit` — zero new errors
- Grep: `grep -c 'relayPassword' src/backend/database/routes/users.ts` == 2 (declaration + createOrUpdateUser call only)
- Grep: `grep -cE 'INSERT INTO users \(.*, mxid\) VALUES' src/backend/database/routes/users.ts` == 1 (proves INSERT column-list extension)
- Grep: `grep -c 'user_create_matrix_mint_failed\|user_create_admin_creds_missing' src/backend/database/routes/users.ts` == 2 (both distinct failure-log operation codes present)
- Grep: `grep -cE 'user_create_deactivate_after_(avatar|insert|encryption)_fail' src/backend/database/routes/users.ts` >= 3 (one log op per rollback branch)
- Grep: `grep -c 'router.delete("/delete-account"' src/backend/database/routes/users.ts` == 1 (DELETE route not accidentally modified)
</verification>

<success_criteria>
- POST /users/create is the FIRST place in the codebase where new users automatically get a Matrix relay identity — no manual admin action required
- Synapse unreachable at create → 500 with zero side effects (D-04)
- Post-mint failures roll back cleanly, deactivating the orphaned Matrix account best-effort with a grep-findable log line (T-88-15 mitigation, deferred-cleanup enabler per CONTEXT.md deferred ideas)
- The relayPassword variable exists only in the tight scope of Step 3.5 and never leaks to logs, DB, or HTTP responses (D-08)
- Every new users row committed after this plan has `mxid` populated (INSERT column-list extension per Pitfall 1)
- Existing 7 POST /users/create tests + existing user-avatars integration test still pass — no assertion regressions
- 3 new tests prove happy-path mint, refuse-on-Synapse-down, and post-mint rollback deactivation
- Plan 04 can now assume every deletable user with a non-null mxid was minted by this create flow (or hand-imported legacy) — deactivation on delete is symmetric
</success_criteria>

<output>
Create `.planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-03-SUMMARY.md` when done, containing:
- The exact insertion point of Step 3.5 (line number range in users.ts)
- The exact INSERT statement after extension (grep-quoted)
- Displayname derivation rule chosen (title-cased pre-`@` local part, per D-11 discretion)
- Test run output showing pre-existing tests + 3 new tests all pass
- Grep confirmation that `relayPassword` appears exactly twice (D-08 discipline)
- Grep confirmation that `deactivated: true` and `PUT /_synapse/admin/v2/users` do NOT appear in the new code (Pitfall 2 avoided — the deactivation path uses the primitive from Plan 02, not a re-implementation)
- The three rollback-branch log operation codes chosen
</output>
