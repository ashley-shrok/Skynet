---
phase: 88-relay-mediated-group-conversations-sub-slice-a-human-relay-i
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/matrix/matrix-admin-client.ts
  - src/backend/matrix/matrix-admin-client.test.ts
autonomous: true
requirements: [D-09, D-10]
tags: [phase-88, matrix, admin-client, deactivate, synapse]

must_haves:
  truths:
    - "deactivateUser hits POST /_synapse/admin/v1/deactivate/{mxid} (Synapse-official deactivation endpoint per RESEARCH.md Q1 — NOT the PUT v2 endpoint)"
    - "Request body is `{ erase: false }` explicit (defensive per Assumption A3 — prevents accidental history erasure)"
    - "mxid is passed through encodeURIComponent in the URL (defense-in-depth against any sanitizer bypass; mirrors createOrUpdateUser line 76 pattern per T-75-05)"
    - "Return type is exactly `DeactivateUserOk = { ok: true }` (NOT `AdminOk<Record<string, never>>` — see line-233 comment in matrix-admin-client.ts explaining strict-tsc rejection)"
    - "Follows the six-step invariant list from matrix-admin-client.ts lines 1-18: creds resolve → encodeURIComponent → Authorization Bearer + Content-Type json → 30s AbortController → discriminated-union result → clearTimeout in both success and error paths"
    - "Reuses the module's existing error constants (ERR_CREDS_MISSING, ERR_NON_2XX, ERR_TIMEOUT, ERR_PROXY) — no new constants added"
    - "operation label in databaseLogger is `\"matrix_admin_deactivate_user\"`"
    - "Never leaks upstream response body or admin token in logs (T-88 Info Disclosure mitigation)"
  artifacts:
    - path: "src/backend/matrix/matrix-admin-client.ts"
      provides: "new export deactivateUser(mxid: string): Promise<DeactivateUserOk | AdminErr> + type DeactivateUserOk"
      contains: "export async function deactivateUser"
    - path: "src/backend/matrix/matrix-admin-client.test.ts"
      provides: "new describe(\"deactivateUser\") block with 7 test cases covering happy path, non-2xx, timeout, network error, creds missing, encodeURIComponent, erase-false body"
      min_lines: 60
  key_links:
    - from: "src/backend/matrix/matrix-admin-client.ts (new deactivateUser)"
      to: "src/backend/matrix/matrix-admin-creds-store.ts getMatrixAdminCreds"
      via: "existing module-scoped import (line 20) — no new import needed"
      pattern: "await getMatrixAdminCreds\\(\\)"
    - from: "src/backend/matrix/matrix-admin-client.ts (new deactivateUser)"
      to: "POST /_synapse/admin/v1/deactivate/{mxid}"
      via: "fetch call with method: \"POST\", body: JSON.stringify({ erase: false })"
      pattern: "/_synapse/admin/v1/deactivate/"
---

<objective>
Add the sixth admin primitive `deactivateUser` to `matrix-admin-client.ts`, following the exact structural pattern of `makeRoomAdmin` (lines 237-283 — the closest analog: no response payload, POST method, `{ ok: true }` return type). Also extend the sibling test file with a `describe("deactivateUser")` block covering happy path, non-2xx, timeout, network error, missing creds, URL encoding, and the `erase: false` body defensive default.

Purpose: Plans 03 and 04 both consume this primitive. Adding it to `matrix-admin-client.ts` keeps all Synapse admin API calls in one file (per the module's stated invariant list at lines 1-18), reuses the existing error constants + AbortController pattern, and inherits the module's no-token-leak / no-upstream-body-leak discipline. The Synapse deactivation endpoint is `POST /_synapse/admin/v1/deactivate/{mxid}` (RESEARCH.md Q1 verified against Synapse docs) — distinct from the PUT v2 endpoint `createOrUpdateUser` uses.

Output: One new export + type in `matrix-admin-client.ts`, one new `describe` block in the test file. Landable + shippable in isolation — nothing in the codebase calls this primitive until Plans 03 and 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-CONTEXT.md
@.planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-RESEARCH.md
@.planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-PATTERNS.md
@src/backend/matrix/matrix-admin-client.ts
@src/backend/matrix/matrix-admin-client.test.ts
@src/backend/matrix/matrix-admin-creds-store.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deactivateUser primitive to matrix-admin-client.ts (POST /_synapse/admin/v1/deactivate/{mxid}, erase:false body, DeactivateUserOk return)</name>
  <files>src/backend/matrix/matrix-admin-client.ts</files>
  <read_first>
    - src/backend/matrix/matrix-admin-client.ts lines 1-18 (module file-header invariant list — the 6 rules every primitive must honor)
    - src/backend/matrix/matrix-admin-client.ts lines 30-42 (existing error constants + AdminOk/AdminErr types — reuse verbatim, do NOT declare new ones)
    - src/backend/matrix/matrix-admin-client.ts lines 229-284 (the makeRoomAdmin block — the CLOSEST STRUCTURAL ANALOG per 88-PATTERNS.md: no-payload return, POST method, `{ ok: true }` return type, same 5-step pattern)
    - src/backend/matrix/matrix-admin-client.ts lines 66-118 (createOrUpdateUser — reference for the encodeURIComponent(mxid) URL-construction pattern at line 76 that mitigates T-75-05 mxid path-traversal)
    - .planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-RESEARCH.md § "Pattern 1: deactivateUser primitive (NEW)" — exact code shape to write
    - .planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-RESEARCH.md § "Assumption A3" — the `{ erase: false }` explicit body decision
    - .planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-RESEARCH.md § "Pitfall 2: Using PUT v2 with `deactivated: true` instead of POST v1/deactivate" — why the POST v1/deactivate endpoint is correct
    - .planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-PATTERNS.md § "src/backend/matrix/matrix-admin-client.ts — add `deactivateUser`" (full analog map with what differs from makeRoomAdmin)
  </read_first>
  <behavior>
    - Test 1 (happy path 200): mock fetch to return 200 with empty body; deactivateUser("@bob:host") returns { ok: true }
    - Test 2 (non-2xx propagates status): mock fetch to return 403 with { errcode: "M_FORBIDDEN" }; deactivateUser returns { ok: false, status: 403, error: "admin_api_non_2xx" }; upstream body is NOT included in the returned error
    - Test 3 (AbortError → timeout): stubFetchAbort(); deactivateUser returns { ok: false, status: 504, error: "admin_api_timeout" }
    - Test 4 (network error): stubFetchNetworkError(); deactivateUser returns { ok: false, status: 502, error: "admin_api_proxy_error" }
    - Test 5 (creds missing): getMatrixAdminCreds mock returns null; deactivateUser returns { ok: false, status: 500, error: "matrix_admin_creds_missing" }; fetch is NEVER called (assert fetch mock call count == 0)
    - Test 6 (encodeURIComponent on mxid path arg): a fetch spy captures the URL; deactivateUser("@bob:host with space") produces a URL containing `%40bob%3Ahost%20with%20space` (mxid is encoded, not literal)
    - Test 7 (body contains erase:false): a fetch spy captures the request body; JSON.parse(body).erase === false (defensive default per Assumption A3)
  </behavior>
  <action>
    Extend `src/backend/matrix/matrix-admin-client.ts` by ADDING one new section at the end of the file (after the `getSharedDMRoom` block, at end-of-file). Do NOT re-order existing exports, do NOT modify existing primitives.

    New section (append to end of file, before any trailing whitespace/EOF):

    Section header comment (mirror line 229-231 style):
    - `// ---------------------------------------------------------------------------`
    - `// deactivateUser — POST /_synapse/admin/v1/deactivate/{mxid}`
    - `// ---------------------------------------------------------------------------`

    Explanatory JSDoc comment above the type declaration (mirror lines 233-236 wording style):
    - Note: `DeactivateUserOk = { ok: true }` — no payload beyond the ok flag. The `AdminOk<Record<string, never>>` form is rejected by strict tsc per the existing MakeRoomAdminOk comment at line 233-236 — declare directly as `{ ok: true }`.

    Type export:
    - `export type DeactivateUserOk = { ok: true };`

    JSDoc above the function (mirror lines 239-243 style):
    - One-paragraph description: "Deactivate a Synapse account by mxid. Called from POST /users/create rollback (Plan 03) and both delete paths (Plan 04). Best-effort at call sites — callers log-and-proceed on non-ok per D-10."
    - Endpoint line: `POST /_synapse/admin/v1/deactivate/{mxid}`
    - Body-shape note: sends `{ erase: false }` explicit (defensive per RESEARCH.md Assumption A3 — prevents accidental room-history erasure).

    Function body — VERBATIM structural mirror of `makeRoomAdmin` (lines 245-283) with only the following per-line differences:

    1. Signature: `export async function deactivateUser(mxid: string): Promise<DeactivateUserOk | AdminErr>` (single arg, not the two-arg roomIdOrAlias+userId shape)
    2. Creds check: identical (`const creds = await getMatrixAdminCreds(); if (!creds) return { ok: false, status: 500, error: ERR_CREDS_MISSING };`)
    3. URL: `` `${creds.homeserverBase}/_synapse/admin/v1/deactivate/${encodeURIComponent(mxid)}` `` — note the `deactivate/` path segment differs from `rooms/.../make_room_admin`, and the encodeURIComponent wraps the `mxid` per T-75-05 (mirror of createOrUpdateUser line 76 discipline)
    4. Body: `const body = { erase: false };` — NO `user_id` field, explicit `erase: false` per Assumption A3
    5. AbortController + timeoutId setup: identical to makeRoomAdmin lines 257-258
    6. fetch call: identical structure to lines 260-268 (method POST, Bearer auth, Content-Type json, JSON.stringify(body), signal)
    7. clearTimeout(timeoutId) called BOTH in the success branch AND the catch branch (invariant #6 from lines 1-18)
    8. Success handling: `if (!response.ok) return { ok: false, status: response.status, error: ERR_NON_2XX }; return { ok: true };` — identical to makeRoomAdmin lines 270-273
    9. Catch block: identical to lines 274-283 EXCEPT `operation` label — use `"matrix_admin_deactivate_user"` (not `"matrix_admin_make_room_admin"`)
    10. AbortError → `{ ok: false, status: 504, error: ERR_TIMEOUT }` (identical)
    11. Other errors → databaseLogger.error(...) then `{ ok: false, status: 502, error: ERR_PROXY }` (identical, only the operation label changes)

    Do NOT add any new error constant. Do NOT modify any existing export. Do NOT reorder imports. Do NOT touch loginAsUser, createOrUpdateUser, joinRoom, makeRoomAdmin, listRooms, countUsersMatching, buildRelayJsonBody, or getSharedDMRoom.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-taylor && npx tsc --noEmit 2>&amp;1 | grep -E "matrix-admin-client\.ts|error TS" | head -20; grep -c '^export async function deactivateUser' src/backend/matrix/matrix-admin-client.ts; grep -c '/_synapse/admin/v1/deactivate/' src/backend/matrix/matrix-admin-client.ts; grep -c 'matrix_admin_deactivate_user' src/backend/matrix/matrix-admin-client.ts; grep -c 'export type DeactivateUserOk' src/backend/matrix/matrix-admin-client.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^export async function deactivateUser' src/backend/matrix/matrix-admin-client.ts` returns exactly 1
    - `grep -c 'export type DeactivateUserOk = { ok: true }' src/backend/matrix/matrix-admin-client.ts` returns exactly 1
    - `grep -c '/_synapse/admin/v1/deactivate/' src/backend/matrix/matrix-admin-client.ts` returns at least 1 (the URL template)
    - `grep -c 'matrix_admin_deactivate_user' src/backend/matrix/matrix-admin-client.ts` returns exactly 1 (the operation label — no duplicates, no typos)
    - `grep -c 'erase: false' src/backend/matrix/matrix-admin-client.ts` returns exactly 1 (the body constant — defensive default per Assumption A3)
    - `grep -c 'encodeURIComponent(mxid)' src/backend/matrix/matrix-admin-client.ts` returns at least 1 (the deactivateUser URL — T-75-05 defense)
    - `grep -c 'deactivated: true' src/backend/matrix/matrix-admin-client.ts` returns 0 (proves Pitfall 2 avoided — did NOT reuse the PUT v2 endpoint)
    - `grep -c 'ERR_CREDS_MISSING\|ERR_NON_2XX\|ERR_TIMEOUT\|ERR_PROXY' src/backend/matrix/matrix-admin-client.ts` count is >= 4 pre-existing + 4 new uses in the deactivateUser body (net-new count of at least 4 occurrences)
    - `npx tsc --noEmit` reports zero new errors in this file
    - `grep -c '^const ERR_' src/backend/matrix/matrix-admin-client.ts` remains 5 (no new error constants introduced — reuses the existing five)
  </acceptance_criteria>
  <done>
    - New export deactivateUser follows the makeRoomAdmin structural pattern exactly
    - Uses POST /_synapse/admin/v1/deactivate/{mxid} (not PUT v2 with deactivated:true)
    - Body sends { erase: false } explicit
    - mxid wrapped in encodeURIComponent
    - Reuses existing error constants, no new ones
    - tsc clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add describe("deactivateUser") block to matrix-admin-client.test.ts with 7 test cases</name>
  <files>src/backend/matrix/matrix-admin-client.test.ts</files>
  <read_first>
    - src/backend/matrix/matrix-admin-client.ts (Task 1's updated file with the new deactivateUser export)
    - src/backend/matrix/matrix-admin-client.test.ts lines 1-93 (file header, HAPPY_CREDS fixture, mockFetchResponse helper, stubFetchOk, stubFetchAbort, stubFetchNetworkError, beforeEach — all reusable, do NOT redeclare)
    - src/backend/matrix/matrix-admin-client.test.ts lines 34-43 (existing import block from `./matrix-admin-client.js` — deactivateUser must be added here in alphabetical or grouped position)
    - src/backend/matrix/matrix-admin-client.test.ts lines 315-403 (the `describe("makeRoomAdmin")` block — the CLOSEST STRUCTURAL ANALOG; new describe follows the same test shape)
    - src/backend/matrix/matrix-admin-client.test.ts lines 199-221 (createOrUpdateUser test for encodeURIComponent URL discipline — pattern for Test 6 URL-capture assertion)
    - .planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-PATTERNS.md § "src/backend/matrix/matrix-admin-client.test.ts — add `deactivateUser` describe block" (full analog map + specific test case list)
  </read_first>
  <behavior>
    - The 7 test behaviors enumerated in Task 1's `<behavior>` block, each as a distinct `it(...)` case inside a single `describe("deactivateUser")` block.
  </behavior>
  <action>
    Extend `src/backend/matrix/matrix-admin-client.test.ts`:

    1. Update the import block (currently at lines 34-43) to ADD `deactivateUser` to the imported names from `./matrix-admin-client.js`. Place it either at the end of the alphabetical group or after `getSharedDMRoom` (grouped with the other primitives). ALSO add `DeactivateUserOk` to the same import if the test file wants to reference the return type in test annotations — otherwise skip. Keep the existing 8 imports intact.

    2. APPEND a new `describe("deactivateUser", () => { ... })` block at the END of the file (after all existing describe blocks). Do NOT insert in the middle — this makes it easy to review the diff.

    Inside the new describe, add 7 `it(...)` cases:

    Test 1 — happy path 200 → { ok: true }:
    - `stubFetchOk(200, {});`
    - `const result = await deactivateUser("@bob:thenasty.taild9b663.ts.net");`
    - `expect(result.ok).toBe(true);`

    Test 2 — non-2xx 403 propagates status, no upstream body leak:
    - `stubFetchOk(403, { errcode: "M_FORBIDDEN", error: "Cannot deactivate" });`
    - `const result = await deactivateUser("@bob:thenasty.taild9b663.ts.net");`
    - `expect(result.ok).toBe(false);`
    - `if (!result.ok) { expect(result.status).toBe(403); expect(result.error).toBe("admin_api_non_2xx"); }`
    - Additionally assert the upstream body values (`"M_FORBIDDEN"`, `"Cannot deactivate"`) do NOT appear anywhere in the stringified result: `expect(JSON.stringify(result)).not.toContain("M_FORBIDDEN"); expect(JSON.stringify(result)).not.toContain("Cannot deactivate");`

    Test 3 — AbortError → { ok: false, status: 504, error: "admin_api_timeout" }:
    - `stubFetchAbort();`
    - `const result = await deactivateUser("@bob:host");`
    - `expect(result.ok).toBe(false); if (!result.ok) { expect(result.status).toBe(504); expect(result.error).toBe("admin_api_timeout"); }`

    Test 4 — network error → { ok: false, status: 502, error: "admin_api_proxy_error" }:
    - `stubFetchNetworkError();`
    - `const result = await deactivateUser("@bob:host");`
    - `expect(result.ok).toBe(false); if (!result.ok) { expect(result.status).toBe(502); expect(result.error).toBe("admin_api_proxy_error"); }`

    Test 5 — creds missing → { ok: false, status: 500, error: "matrix_admin_creds_missing" }, fetch never called:
    - `vi.mocked(getMatrixAdminCreds).mockResolvedValueOnce(null);`
    - `const fetchMock = vi.fn();`
    - `vi.stubGlobal("fetch", fetchMock);`
    - `const result = await deactivateUser("@bob:host");`
    - `expect(result.ok).toBe(false); if (!result.ok) { expect(result.status).toBe(500); expect(result.error).toBe("matrix_admin_creds_missing"); }`
    - `expect(fetchMock).not.toHaveBeenCalled();`

    Test 6 — mxid is encodeURIComponent'd in URL (mirror createOrUpdateUser test at lines 199-221):
    - `const fetchMock = vi.fn(async () => mockFetchResponse(200, {}));`
    - `vi.stubGlobal("fetch", fetchMock);`
    - `await deactivateUser("@bob:host with space");`
    - `const calledUrl = fetchMock.mock.calls[0][0] as string;`
    - `expect(calledUrl).toContain("%40bob%3Ahost%20with%20space");` — the mxid is percent-encoded
    - `expect(calledUrl).toContain("/_synapse/admin/v1/deactivate/");` — the endpoint path is present

    Test 7 — body contains { erase: false } (defensive default per Assumption A3):
    - `const fetchMock = vi.fn(async () => mockFetchResponse(200, {}));`
    - `vi.stubGlobal("fetch", fetchMock);`
    - `await deactivateUser("@bob:host");`
    - `const requestInit = fetchMock.mock.calls[0][1] as RequestInit;`
    - `const parsedBody = JSON.parse(requestInit.body as string);`
    - `expect(parsedBody.erase).toBe(false);`
    - `expect(requestInit.method).toBe("POST");`

    Do NOT redeclare HAPPY_CREDS, stubFetchOk, stubFetchAbort, stubFetchNetworkError, or mockFetchResponse — they are already in scope from the file header (verified in read_first).

    Do NOT modify any existing describe block. Do NOT touch the file's beforeEach at line 89.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-taylor && npx vitest run --related src/backend/matrix/matrix-admin-client.ts src/backend/matrix/matrix-admin-client.test.ts 2>&amp;1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --related src/backend/matrix/matrix-admin-client.ts src/backend/matrix/matrix-admin-client.test.ts` reports ALL existing tests still passing PLUS the 7 new tests in `describe("deactivateUser")` passing (zero failures, zero skipped)
    - `grep -c 'describe("deactivateUser"' src/backend/matrix/matrix-admin-client.test.ts` returns exactly 1
    - `grep -c '  it(' src/backend/matrix/matrix-admin-client.test.ts` returns a count that is at least 7 higher than the pre-existing count (7 new it cases added)
    - `grep -c 'deactivateUser' src/backend/matrix/matrix-admin-client.test.ts` returns at least 8 (1 in import + 7 in tests)
    - `grep -c '%40bob%3Ahost%20with%20space' src/backend/matrix/matrix-admin-client.test.ts` returns exactly 1 (Test 6 URL-encoding assertion is present)
    - `grep -c 'parsedBody.erase' src/backend/matrix/matrix-admin-client.test.ts` returns exactly 1 (Test 7 erase:false assertion is present)
    - No new helper functions (stubFetchOk, stubFetchAbort, stubFetchNetworkError) are re-declared — grep for `^function stub` count is unchanged from pre-existing count
  </acceptance_criteria>
  <done>
    - All 7 deactivateUser tests pass
    - All pre-existing tests still pass (no regressions)
    - Test 2 explicitly proves no upstream body leak
    - Test 6 explicitly proves encodeURIComponent applied
    - Test 7 explicitly proves { erase: false } sent
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → deactivateUser | mxid string enters from Plan 03 (create-flow rollback) and Plan 04 (delete-flow); mxid was either just-minted by createOrUpdateUser or read from users.mxid column (both server-controlled) |
| deactivateUser → Synapse admin API | outbound HTTPS to `{homeserverBase}/_synapse/admin/v1/deactivate/{mxid}` with Bearer admin token in the Authorization header |
| Synapse response → caller | discriminated-union return; upstream body is NEVER included (invariant #5 from file-header) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-05 | Tampering | mxid path-injection into Synapse URL | mitigate | `encodeURIComponent(mxid)` at URL construction (mirrors createOrUpdateUser line 76 T-75-05 discipline). Test 6 explicitly verifies percent-encoding on a malicious-lookalike mxid (`"@bob:host with space"` — the space, `@`, and `:` all encode). Defense-in-depth with Plan 01's sanitizer output. |
| T-88-06 | Information Disclosure | upstream response body leak (Synapse errcodes / error strings) into Skynet logs or API responses | mitigate | Return type is `{ ok: false, status, error }` with `error` bound to one of five module-scoped constants — the upstream response body is NEVER read into the return value. Test 2 explicitly asserts upstream body strings (`"M_FORBIDDEN"`, `"Cannot deactivate"`) do NOT appear in `JSON.stringify(result)`. The catch-block's `databaseLogger.error` passes `err` (a thrown error) as the second arg — never the response body. |
| T-88-07 | Information Disclosure | admin access_token leak into logs | mitigate | Token is only used in the `Authorization: Bearer ${creds.accessToken}` header; never logged, never printed. Verified by grep: the string `accessToken` appears only in the header construction, never in a `databaseLogger` call. |
| T-88-08 | Denial of Service | hung Synapse call blocks Skynet forever | mitigate | 30s AbortController timeout inherited via `REQUEST_TIMEOUT_MS = 30_000` (line 23). Test 3 explicitly verifies AbortError → 504. |
| T-88-09 | Tampering | history-erasure via accidental `erase: true` | mitigate | Body constant is `{ erase: false }` explicit — a future edit that changes the value trips Test 7 immediately. Assumption A3 in RESEARCH.md documents this defensive choice. |
| T-88-SC | Tampering | npm package installs | mitigate | No new packages installed. Only Node built-ins (`fetch`, `AbortController`, `setTimeout`, `DOMException`) + existing module-scoped `getMatrixAdminCreds`, `databaseLogger`. RESEARCH.md § Package Legitimacy Audit confirms "not applicable" for this phase. |
</threat_model>

<verification>
- Run: `npx vitest run --related src/backend/matrix/matrix-admin-client.ts src/backend/matrix/matrix-admin-client.test.ts` — all existing tests + 7 new tests pass
- Run: `npx tsc --noEmit` — zero new errors
- Grep: `grep -c '^export async function deactivateUser' src/backend/matrix/matrix-admin-client.ts` == 1
- Grep: `grep -c '/_synapse/admin/v1/deactivate/' src/backend/matrix/matrix-admin-client.ts` == 1 (URL constructed exactly once — the primitive)
- Grep: `grep -c 'deactivated: true' src/backend/matrix/matrix-admin-client.ts` == 0 (proves Pitfall 2 avoided — did NOT reuse PUT v2)
- Grep: `grep -c 'erase: false' src/backend/matrix/matrix-admin-client.ts` == 1 (defensive default in body)
- Grep: `grep -c 'encodeURIComponent(mxid)' src/backend/matrix/matrix-admin-client.ts` >= 1 (the deactivateUser URL — T-75-05/T-88-05 defense)
- Grep: `grep -c 'matrix_admin_deactivate_user' src/backend/matrix/matrix-admin-client.ts` == 1 (operation label used once, no typos)
</verification>

<success_criteria>
- Plans 03 and 04 can `import { deactivateUser } from "../../matrix/matrix-admin-client.js"` with no other setup
- The primitive follows the module's six invariant rules (creds check → encodeURIComponent → Bearer+json → AbortController 30s → discriminated union → clearTimeout in both paths) — verifiable by structural comparison with makeRoomAdmin
- The correct Synapse endpoint (POST /_synapse/admin/v1/deactivate/{mxid}) is used, NOT the PUT v2 approach
- `{ erase: false }` defensive body prevents accidental room-history erasure
- Existing 5 admin primitives + buildRelayJsonBody + getSharedDMRoom are unmodified — the new primitive is purely additive
</success_criteria>

<output>
Create `.planning/phases/88-relay-mediated-group-conversations-sub-slice-a-human-relay-i/88-02-SUMMARY.md` when done, containing:
- The new export signature: `deactivateUser(mxid: string): Promise<DeactivateUserOk | AdminErr>`
- Confirmation of endpoint choice: POST /_synapse/admin/v1/deactivate/{mxid} (not PUT v2)
- Confirmation of `{ erase: false }` body per Assumption A3
- Test run output showing all pre-existing tests still pass PLUS 7 new deactivateUser tests pass
- Grep verification that `deactivated: true` does NOT appear in the file (Pitfall 2 avoided)
- Line number where the new export was appended
</output>
