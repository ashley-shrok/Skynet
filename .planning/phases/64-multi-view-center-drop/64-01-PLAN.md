---
phase: 64-multi-view-center-drop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/lib/split-tree.ts
  - src/ui/lib/split-tree.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Calling replaceLeaf(root, targetTabId, replacementTabId) on a tree where targetTabId is a leaf returns a new tree whose target cell's tabId equals replacementTabId, with the target cell's ancestry (splits above it, directions) byte-identical to the input"
    - "Calling replaceLeaf where replacementTabId already exists elsewhere in the tree first removes that pre-existing leaf (collapsing its parent split into the surviving sibling per removeLeaf's rule) BEFORE flipping the target cell — net effect on a 3-cell tree is a move, not a duplicate"
    - "Calling replaceLeaf with targetTabId === replacementTabId returns the input root by reference identity (Object.is holds), no reallocation, no warning log"
    - "Calling replaceLeaf where targetTabId is not present in the tree returns the input root by reference identity (Object.is holds) and emits ONE console.warn line starting with '[split-tree] replaceLeaf: target not found'"
    - "Calling swapLeaves(root, tabIdA, tabIdB) on a tree containing both leaves returns a new tree whose two leaves have exchanged tabIds, with EVERY internal split node's direction and structural shape preserved (a leaf-content-only mutation)"
    - "Calling swapLeaves with tabIdA === tabIdB returns the input root by reference identity (Object.is holds), no reallocation, no warning log"
    - "Calling swapLeaves where either tabIdA or tabIdB is missing from the tree returns the input root by reference identity (Object.is holds) and emits ONE console.warn line starting with '[split-tree] swapLeaves: leaf not found'"
    - "Neither replaceLeaf nor swapLeaves emits console.info from the helper body — structured [pv-split-drop] logging belongs to the AppShell handler layer that will be added in Plan 64-02"
    - "Both helpers are pure with respect to the input tree — no in-place mutation of any input SplitNode; every ancestor on the rewrite path is freshly allocated per the existing replaceAt convention at split-tree.ts:213-240"
  artifacts:
    - path: "src/ui/lib/split-tree.ts"
      provides: "replaceLeaf(root, targetTabId, replacementTabId) + swapLeaves(root, tabIdA, tabIdB) pure helpers added alongside existing findLeaf / getNodeAt / insertAtEdge / removeLeaf / collectTabIds / computeEdgeZone exports"
      contains: "export function replaceLeaf"
    - path: "src/ui/lib/split-tree.test.ts"
      provides: "New Phase 64 describe block covering nine unit tests (5 replaceLeaf + 4 swapLeaves) matching CONTEXT.md § Test coverage additions"
      contains: "Phase 64"
  key_links:
    - from: "src/ui/lib/split-tree.ts new replaceLeaf helper"
      to: "src/ui/lib/split-tree.ts existing removeLeaf helper at :249-257 + existing replaceAt private helper at :213-240"
      via: "compose removeLeaf(root, replacementTabId) THEN structural leaf replacement (own path walk or reuse replaceAt indirectly by materializing a session leaf and replacing at target's fresh path)"
      pattern: "removeLeaf.*then.*replace"
    - from: "src/ui/lib/split-tree.ts new swapLeaves helper"
      to: "src/ui/lib/split-tree.ts existing findLeaf helper at :92-106"
      via: "findLeaf(root, tabIdA) + findLeaf(root, tabIdB) to locate both leaves, then leaf-content-only mutation via the existing replaceAt private helper at :213-240 applied twice"
      pattern: "findLeaf.*findLeaf"
    - from: "src/ui/lib/split-tree.test.ts new Phase 64 describe block"
      to: "src/ui/lib/split-tree.test.ts existing leaf() + split() + assertInvariant() helpers at :34-62"
      via: "reuse the module-local test helpers verbatim; do NOT duplicate them"
      pattern: "assertInvariant"
---

<objective>
Add two pure immutable helpers to `src/ui/lib/split-tree.ts` — `replaceLeaf(root, targetTabId, replacementTabId)` and `swapLeaves(root, tabIdA, tabIdB)` — with full vitest unit coverage extending `src/ui/lib/split-tree.test.ts`. Both helpers operate purely on the `SplitNode` tree, are side-effect-free at the helper layer (no `console.info` from the helper body — only defensive `console.warn` on missing leaves), and are drop-in composable with the existing `findLeaf` / `getNodeAt` / `insertAtEdge` / `removeLeaf` / `collectTabIds` / `computeEdgeZone` surface.

`replaceLeaf` is the tree-op backing "drop a conversation-list-row payload onto the center of an already-open session's body — the target session is removed from its grid cell and the dragged session takes that cell in place; the displaced session drops out of the grid entirely (still present in the conv list — just no longer occupying a slot)." Per CONTEXT.md § In-scope item 1: if `replacementTabId` is already elsewhere in the tree, it is first removed (via the existing `removeLeaf` invariant-preserving collapse); if `targetTabId === replacementTabId`, the call is a benign no-op returning root by reference identity. Defensive warn-log on missing target so the Plan 64-02 handler layer can rely on helper-level defensiveness.

`swapLeaves` is the tree-op backing "drop an already-open session's identity badge onto the center of another already-open session — the two sessions trade slots; both remain live in the grid." Per CONTEXT.md § In-scope item 1: pure leaf-content-only mutation (both cells stay put in the tree structurally; only their `tabId` fields trade). If `tabIdA === tabIdB`, benign no-op; if either leaf is not found, defensive warn-log + reference-identity return.

Purpose: Delivers the foundation-layer half of the Phase 64 vehicle notes: "Plan 01 = pure tree helpers + unit tests (standalone). Plan 02 = SplitView center-drop wiring + AppShell handlers + component/integration tests (depends on Plan 01)." Zero UI wiring in this plan; zero React / DOM imports (matches the existing `src/ui/lib/split-tree.ts` module header: "NO REACT / DOM / BACKEND IMPORTS. This module compiles and tests in any TS environment.").

Output: Updated `src/ui/lib/split-tree.ts` with two new exported functions + JSDoc that cites CONTEXT.md edge cases; extended `src/ui/lib/split-tree.test.ts` with a new `describe("split-tree — Phase 64: center-drop tree ops (replaceLeaf + swapLeaves)", () => { … })` block containing the nine tests enumerated in CONTEXT.md § Test coverage additions (Plan 64-01 unit tests). All pre-existing split-tree tests continue to pass unchanged (regression gate — 27+ tests currently in the file per grep).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64-multi-view-center-drop/64-CONTEXT.md
@.planning/shapes/shape-multi-view-center-drop.md
@src/ui/lib/split-tree.ts
@src/ui/lib/split-tree.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add replaceLeaf + swapLeaves to split-tree.ts, TDD-first</name>
  <files>src/ui/lib/split-tree.ts, src/ui/lib/split-tree.test.ts</files>
  <read_first>
    - src/ui/lib/split-tree.ts (full file, 397 lines) — target being modified; new helpers slot in AFTER the existing `computeEdgeZone` block, before EOF. Reuse the module's private `replaceAt` helper at :213-240 (it is not exported but is available inside the module for the new helpers to call). Reuse `findLeaf` at :92-106 for path lookups. Reuse `removeLeaf` at :249-257 for the replacement-already-elsewhere case per CONTEXT.md § In-scope item 1.
    - src/ui/lib/split-tree.test.ts (full file, 515 lines) — extension target. New Phase 64 describe block goes AFTER the existing "split-tree — Phase 57: edge-zone hit-testing" describe block at :406, at end-of-file. Reuse the module-local `leaf(id)` factory at :34, `split(direction, a, b)` factory at :36-40, and `assertInvariant(node)` helper at :46-62 — do NOT duplicate them.
    - .planning/phases/64-multi-view-center-drop/64-CONTEXT.md § "Discovery findings that make the estimate small" item 3 (defines the exact helper signatures and semantics) + § "In-scope this phase" item 1 (same-id no-op contract, defensive warn behavior, purity constraint) + § "Edge cases the plan MUST cover" items 1, 2, 3, 7, 8 (the specific behaviors Test 1-9 exercise) + § "Test coverage additions" Plan 64-01 (the exact nine tests: Test 1-5 replaceLeaf, Test 6-9 swapLeaves).
    - src/ui/lib/split-tree.ts header lines 41-44 — the "NO REACT / DOM / BACKEND IMPORTS" module rule; new helpers MUST NOT add any imports beyond what the file already has (nothing new required — everything the helpers need is already in-module).
  </read_first>
  <behavior>
    Nine new tests, matching CONTEXT.md § Test coverage additions § Plan 64-01. Suggested labels prefix each with "Phase 64:" to distinguish from existing tests. All nine live inside a single new `describe("split-tree — Phase 64: center-drop tree ops (replaceLeaf + swapLeaves)", () => { … })` block appended after the existing Phase 57 describe at line 406.

    replaceLeaf tests:
    - Test 1 (replace-at-root-single-session): `const tree = leaf("a");` → `const next = replaceLeaf(tree, "a", "b");` → assert `next?.kind === "session"` AND `(next as any).tabId === "b"` AND `assertInvariant(next)` passes. Tree shape preserved: still a single-leaf root. Reference-identity: `Object.is(next, tree)` MAY be false (replacement is a distinct tabId, so a new node is allocated), but SHOULD hold for the same-id no-op case tested in Test 4.
    - Test 2 (replace-in-2-cell-with-not-yet-in-tree-replacement): `const tree = split("vertical", leaf("a"), leaf("b"));` → `const next = replaceLeaf(tree, "b", "c");` → assert `assertInvariant(next)` passes, `next!.kind === "split"`, `(next as any).direction === "vertical"`, `findLeaf(next, "a")` is `[0]` (unchanged sibling), `findLeaf(next, "c")` is `[1]` (replacement in target's slot), `findLeaf(next, "b")` is `null` (target evicted). Sibling subtree at index 0 SHOULD share reference with the input's index-0 child (`Object.is((next as any).children[0], (tree as any).children[0])` holds — the sibling was not on the rewrite path).
    - Test 3 (replace-where-replacement-is-elsewhere-3-cell): `const tree = split("vertical", leaf("a"), split("horizontal", leaf("b"), leaf("c")));` → `const next = replaceLeaf(tree, "c", "a");` → assert `assertInvariant(next)` passes. The replacement `a` was at root path `[0]`; after replaceLeaf: `a` at path `[1]` (took c's slot inside the horizontal split), the original `[0]` cell + `[1][0]` cell (which was `b`) collapsed via the removeLeaf-of-a step. Expected structure: `split("horizontal", leaf("b"), leaf("a"))` at root (parent vertical split collapsed because removeLeaf-of-a made left child null → surviving sibling promoted). Concretely: `next!.kind === "split"` AND `(next as any).direction === "horizontal"` AND `findLeaf(next, "b")` is `[0]` AND `findLeaf(next, "a")` is `[1]` AND `findLeaf(next, "c")` is `null` (evicted from target slot).
    - Test 4 (same-id-no-op): `const tree = split("vertical", leaf("a"), leaf("b"));` → `const next = replaceLeaf(tree, "a", "a");` → assert `Object.is(next, tree) === true` (reference-identity return, per must_have truth 3). NO console.warn spy call. `assertInvariant(next)` passes.
    - Test 5 (target-not-found-defensive-warn): spy on `console.warn`; `const tree = leaf("a");` → `const next = replaceLeaf(tree, "ghost", "b");` → assert `Object.is(next, tree) === true` AND `warnSpy` called exactly once with a first-arg string starting with `"[split-tree] replaceLeaf: target not found"` and containing `targetTabId=ghost`. Restore spy in afterEach or at test end.

    swapLeaves tests:
    - Test 6 (swap-2-cell): `const tree = split("vertical", leaf("a"), leaf("b"));` → `const next = swapLeaves(tree, "a", "b");` → assert `assertInvariant(next)` passes, `next!.kind === "split"`, `(next as any).direction === "vertical"` (structure preserved), `findLeaf(next, "a")` is `[1]`, `findLeaf(next, "b")` is `[0]` (both traded slots). Structural preservation: `Object.is((next as any).children[0].kind, "session")` etc — both children still leaves, split direction identical.
    - Test 7 (swap-deep-tree-A-and-C): `const tree = split("vertical", leaf("a"), split("horizontal", leaf("b"), leaf("c")));` → `const next = swapLeaves(tree, "a", "c");` → assert `assertInvariant(next)` passes. Expected structure `split("vertical", leaf("c"), split("horizontal", leaf("b"), leaf("a")))` — same shape (vertical root with horizontal on right), only leaves at path `[0]` and `[1][1]` traded. Concretely: `findLeaf(next, "a")` is `[1, 1]`, `findLeaf(next, "b")` is `[1, 0]` (unchanged sibling of the swapped leaf), `findLeaf(next, "c")` is `[0]`. The inner horizontal split's direction MUST remain `"horizontal"`.
    - Test 8 (same-id-no-op): `const tree = split("vertical", leaf("a"), leaf("b"));` → `const next = swapLeaves(tree, "a", "a");` → assert `Object.is(next, tree) === true`. NO warn spy call.
    - Test 9 (missing-leaf-defensive-warn): spy on `console.warn`; `const tree = split("vertical", leaf("a"), leaf("b"));` → `const next = swapLeaves(tree, "a", "ghost");` → assert `Object.is(next, tree) === true` AND `warnSpy` called exactly once with a first-arg string starting with `"[split-tree] swapLeaves: leaf not found"` and containing `tabId=ghost`. Second scenario in the same it-block: reset spy, call `swapLeaves(tree, "ghost", "a")` (swap with missing FIRST arg), assert same reference-identity return AND same warn shape.

    All nine tests MUST NOT introduce console.info assertions — per must_have truth 8, helpers emit NO console.info; only defensive console.warn. If the executor is tempted to add a `[pv-split-drop]` or `[pv-split-preview]` log to the helper body, they have violated the plan — those logs belong to the Plan 64-02 handler layer per CONTEXT.md § In-scope item 5.
  </behavior>
  <action>
    RED (write failing tests first):
    - At end of `src/ui/lib/split-tree.test.ts` (after the closing brace of the Phase 57 describe block at ~L515), append a new describe block: `describe("split-tree — Phase 64: center-drop tree ops (replaceLeaf + swapLeaves)", () => { … })`. Inside, add all nine tests above, in order. Reuse the module-local `leaf(id)` and `split(direction, a, b)` factories and the `assertInvariant(node)` helper — they are already in-scope at file top.
    - Import the two new symbols at the top of the file: extend the existing import block at :12-24 to include `replaceLeaf` and `swapLeaves` alongside `findLeaf`, `getNodeAt`, `insertAtEdge`, `removeLeaf`, `collectTabIds`, `computeEdgeZone`.
    - For Test 5 and Test 9, use `vi.spyOn(console, "warn").mockImplementation(() => {})` at the top of the it-block and restore with `.mockRestore()` at end. Match the pattern used elsewhere in the fork's tests (grep `vi.spyOn(console` for precedents; SplitView.test.tsx:515 uses the same pattern with `beforeEach`).
    - Run: `npx vitest run src/ui/lib/split-tree.test.ts` — MUST show the nine new Phase 64 tests failing (imports resolve to `undefined` since the helpers don't exist yet). All pre-existing tests (Phase 56 Tests 1-11 in the first describe + Phase 57 Tests 1-13 in the second describe) MUST continue to pass — the new test block is additive.

    GREEN (implement in split-tree.ts):
    - At end of `src/ui/lib/split-tree.ts` (after the closing brace of `computeEdgeZone` at ~L397), append two new exported functions with JSDoc citing Phase 64 CONTEXT.md.
    - `export function replaceLeaf(root: SplitNode | null, targetTabId: string, replacementTabId: string): SplitNode | null`:
      Behavior sketch (executor writes the real TypeScript):
      1. `if (root === null) return root;` — null tree, nothing to replace, return by reference (matches removeLeaf's null-in null-out contract at :253).
      2. `if (targetTabId === replacementTabId) return root;` — same-id no-op per Edge case #1 (must_have truth 3). Reference-identity return.
      3. `const targetPath = findLeaf(root, targetTabId);` — locate target.
      4. `if (targetPath === null) { console.warn(`[split-tree] replaceLeaf: target not found targetTabId=${targetTabId}`); return root; }` — defensive: warn + reference-identity return. Uses `` ` `` template literal, single explicit field, no JSON.stringify per fleet logging directive.
      5. `const withoutReplacement = removeLeaf(root, replacementTabId);` — dedup step per Edge case #3. `removeLeaf` returns the input by reference if `replacementTabId` was not in the tree (its documented no-op behavior at :245), so this is cheap in the mainline "replacement not in tree" case (Edge case #4). If the replacement WAS in the tree, `removeLeaf` collapses its parent split per invariant.
      6. `if (withoutReplacement === null) return { kind: "session", tabId: replacementTabId };` — degenerate case: the tree was a single-leaf root and the replacement WAS that leaf (target and replacement swapped roles); guard prevents crashing on rediscovery.
      7. `const freshTargetPath = findLeaf(withoutReplacement, targetTabId);` — rediscover: `removeLeaf` may have collapsed an ancestor of the target, shifting its path. Follows the AppShell openSessionInTree precedent at :1642-1675 which rediscovers after removeLeaf for the same reason.
      8. `if (freshTargetPath === null) return withoutReplacement;` — target was collateral in the collapse (shouldn't happen for distinct tabIds, but be defensive; preserves surviving cells over throwing).
      9. `return replaceAt(withoutReplacement, freshTargetPath, { kind: "session", tabId: replacementTabId });` — reuse the existing private `replaceAt` at :213-240 to swap the leaf at the fresh path with the replacement session leaf. `replaceAt` allocates every ancestor on the rewrite path per its documented purity contract.
      JSDoc: include a link comment to `.planning/phases/64-multi-view-center-drop/64-CONTEXT.md § In-scope item 1` and enumerate the four branches (null tree, same-id no-op, target not found, mainline). Note: NO console.info from this helper; structured [pv-split-drop] logging is in Plan 64-02's AppShell handler layer.
    - `export function swapLeaves(root: SplitNode | null, tabIdA: string, tabIdB: string): SplitNode | null`:
      Behavior sketch:
      1. `if (root === null) return root;` — null tree, nothing to swap.
      2. `if (tabIdA === tabIdB) return root;` — same-id no-op per Edge case #1 (must_have truth 6). Reference-identity return, NO warn.
      3. `const pathA = findLeaf(root, tabIdA);` and `const pathB = findLeaf(root, tabIdB);` — locate both.
      4. `if (pathA === null || pathB === null) { console.warn(`[split-tree] swapLeaves: leaf not found tabId=${pathA === null ? tabIdA : tabIdB}`); return root; }` — defensive: log the missing one, reference-identity return (must_have truth 7). If BOTH are missing, log the first (tabIdA); the AppShell handler layer's guard makes this unreachable in practice, but the log helps debuggability if the invariant ever breaks.
      5. Apply `replaceAt` TWICE to swap leaves in place. Sequence: `const step1 = replaceAt(root, pathA, { kind: "session", tabId: tabIdB });` then `const step2 = replaceAt(step1, pathB, { kind: "session", tabId: tabIdA });`. Rationale: because swap is a leaf-content-only mutation (structure identical), pathA and pathB remain valid across both calls — replaceAt only mutates leaf contents at the specified path, never restructures. Note the ORDER matters ONLY if pathA is an ancestor of pathB or vice-versa, but leaves cannot be ancestors of other leaves (leaves are terminal), so order is irrelevant here.
      6. `return step2;` — the final swapped tree.
      JSDoc: cite CONTEXT.md § In-scope item 1 + § Edge case #7 (deep-tree swap). Note that the structural shape (every internal split's direction) is preserved by construction because we never touch splits — only leaf contents at two paths.
    - Do NOT add any new imports. Everything the two helpers need is already in-module (`findLeaf`, `removeLeaf`, `replaceAt`, `SplitNode`).
    - Run: `npx vitest run src/ui/lib/split-tree.test.ts` — MUST show ALL tests passing (previously passing Phase 56 + Phase 57 tests still green PLUS the nine new Phase 64 tests green).

    Commit (single atomic commit for the task):
    - `feat(64-01): split-tree replaceLeaf + swapLeaves pure helpers for center-drop`
  </action>
  <acceptance_criteria>
    - `npx vitest run src/ui/lib/split-tree.test.ts` exits 0 with ALL tests passing. Pre-existing Phase 56 Tests 1-11 + Phase 57 Tests 1-13 (25 tests before this plan; grep confirms) continue to pass; nine new Phase 64 tests pass.
    - `grep -c "^export function replaceLeaf" src/ui/lib/split-tree.ts` returns 1 (exactly one exported replaceLeaf definition).
    - `grep -c "^export function swapLeaves" src/ui/lib/split-tree.ts` returns 1 (exactly one exported swapLeaves definition).
    - `grep -c "^import\|^} from " src/ui/lib/split-tree.ts` returns 0 — the module still has NO imports (matches header rule at :41-44).
    - `grep -v '^[[:space:]]*//' src/ui/lib/split-tree.ts | grep -v '^[[:space:]]*\*' | grep -c 'console\.info' ` returns 0 — replaceLeaf + swapLeaves emit NO console.info from the helper body (excludes comment references). If Plan 64-02 later adds `[pv-split-drop]` logs, they live in AppShell handlers, not here. (Comments about "no console.info" are still permitted; the grep filter above strips comments before counting.)
    - `grep -c 'console\.warn' src/ui/lib/split-tree.ts` returns >= 2 (one warn in replaceLeaf for target-not-found, one warn in swapLeaves for leaf-not-found; both cited by must_have truths 4 and 7).
    - `grep -c '\[split-tree\] replaceLeaf: target not found' src/ui/lib/split-tree.ts` returns >= 1.
    - `grep -c '\[split-tree\] swapLeaves: leaf not found' src/ui/lib/split-tree.ts` returns >= 1.
    - `grep -c 'JSON\.stringify' src/ui/lib/split-tree.ts` returns 0 — no event-serialization (the module has no Event objects; this is a belt-and-suspenders defense against a future refactor sneaking a `JSON.stringify(e)` in).
    - `grep -c '"Phase 64"\|Phase 64' src/ui/lib/split-tree.test.ts` returns >= 9 (each of the nine new tests references "Phase 64" in its test name).
    - Manual read: the two new exported functions are appended at end-of-file AFTER `computeEdgeZone` (:397); the existing private `replaceAt` helper at :213-240 is UNCHANGED (grep-verify by looking for its signature `function replaceAt(root: SplitNode, path: SplitPath, replacement: SplitNode): SplitNode` — should still appear exactly once).
    - Manual read: the pre-existing describe blocks in split-tree.test.ts (Phase 56 tests + Phase 57 edge-zone tests) are UNMODIFIED — the new Phase 64 describe is appended as a sibling.
    - No other files modified. `git diff --stat` after the commit shows exactly two files: `src/ui/lib/split-tree.ts` and `src/ui/lib/split-tree.test.ts`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/ui/lib/split-tree.test.ts</automated>
  </verify>
  <done>All split-tree.test.ts tests pass (existing Phase 56 + Phase 57 tests continue green; nine new Phase 64 tests green); replaceLeaf + swapLeaves exist as pure exported helpers; neither helper emits console.info from its body; both emit defensive console.warn for missing-target / missing-leaf cases; same-id no-op returns root by reference identity for both helpers; module retains its NO-IMPORTS invariant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller (Plan 64-02 AppShell handler) → replaceLeaf / swapLeaves | trusted-in-tree — the caller (setSplitTree functional updater in AppShell) passes tabId strings sourced from the SplitView drop handler after MIME-payload parsing. Not a security boundary at THIS layer; the security boundary is the SplitView drop handler in Plan 64-02 which parses `application/x-skynet-badge` / `application/x-skynet-row` payloads. |
| helper body → SplitNode input tree | pure — helpers do not mutate input; every node on the rewrite path is freshly allocated per the module's existing purity contract (documented at :127-131 for insertAtEdge and enforced by tests via `Object.is` sharing assertions on Phase 56 Test 8). Threat model relies on this being preserved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-01-01 | Tampering | Malicious caller passes crafted tabIds hoping the helper mutates the input tree or throws in a way that corrupts the caller's state | accept | Pure fork-local UI helper called ONLY from AppShell's functional setSplitTree updater (Plan 64-02). No auth boundary at the helper layer. Purity is enforced by construction (helpers never assign to input node fields; new nodes allocated via replaceAt which is already documented pure at :213). Same trust model as all other split-tree helpers. |
| T-64-01-02 | Denial of Service | Repeated calls with same-id no-op case (Edge #1) burn CPU cycles or leak identity checks | accept | Same-id branch is `if (targetTabId === replacementTabId) return root;` — O(1), no allocation, no logging. Cheaper than the reference-identity check `Object.is` on unchanged data would be. Not a DoS surface. |
| T-64-01-03 | Repudiation | Missing-leaf case (Edge #2, Edge #5) fails silently making a "why didn't my drop work?" debug session impossible | mitigate | Both helpers emit ONE explicit-field `console.warn` on missing leaf: `[split-tree] replaceLeaf: target not found targetTabId=<x>` and `[split-tree] swapLeaves: leaf not found tabId=<x>`. Matches the existing fleet logging directive Ashley 2026-08-11 (explicit-field extraction; do NOT JSON.stringify input). Structured `[pv-split-drop]` info-level logs are Plan 64-02's job at the AppShell handler layer per CONTEXT.md § In-scope item 5. |
| T-64-01-04 | Information Disclosure | Warn logs emit tabId which could leak session identifiers | accept | tabId is a fork-local identifier already visible in DOM (data-testid attributes on Panes at SplitView.tsx:461 `data-tab-id={tabId}`), in URL (via split-tree-url.ts encoding), and in existing structured logs ([pv-split-tree] openSessionInTree at AppShell.tsx:1608). No new disclosure surface — the same tabId flows through the same in-process logging pipe. Not PII. |
</threat_model>

<verification>
- `npx vitest run src/ui/lib/split-tree.test.ts` exits 0 with all pre-existing tests (Phase 56 Tests 1-11 in the top-level describe + Phase 57 Tests 1-13 in the edge-zone describe) plus the nine new Phase 64 tests passing.
- Manual read: the pre-existing `replaceAt` private helper at src/ui/lib/split-tree.ts:213-240 is UNCHANGED; the new `replaceLeaf` and `swapLeaves` exported helpers appear at end-of-file after `computeEdgeZone` (:397).
- `git diff --stat` after commit shows exactly two files touched: `src/ui/lib/split-tree.ts` (helper additions) and `src/ui/lib/split-tree.test.ts` (nine new tests).
- No other files in the working tree modified — this is a Wave 1 pure-helper plan with zero UI wiring.
</verification>

<success_criteria>
1. `replaceLeaf(root, targetTabId, replacementTabId)` exists as an exported function in `src/ui/lib/split-tree.ts` with the semantics enumerated in must_haves.truths (1-4) and CONTEXT.md § In-scope item 1.
2. `swapLeaves(root, tabIdA, tabIdB)` exists as an exported function with the semantics in must_haves.truths (5-7).
3. Both helpers are pure with respect to inputs — no in-place mutation of any input SplitNode (must_have truth 9).
4. Neither helper emits `console.info` from its body (must_have truth 8); both emit defensive `console.warn` for missing-leaf cases (truths 4 + 7).
5. All 25 pre-existing split-tree.test.ts tests continue to pass unchanged (regression gate).
6. All 9 new Phase 64 tests pass, matching CONTEXT.md § Test coverage additions § Plan 64-01 exactly.
7. `src/ui/lib/split-tree.ts` module retains its NO-IMPORTS invariant (grep verification).
8. Single atomic commit `feat(64-01): split-tree replaceLeaf + swapLeaves pure helpers for center-drop` created on the current branch (no push, per box-maintainer executor scope).
</success_criteria>

<output>
Create `.planning/phases/64-multi-view-center-drop/64-01-SUMMARY.md` when done.
</output>
