---
phase: 64-multi-view-center-drop
plan: 02
type: execute
wave: 2
depends_on: [64-01]
files_modified:
  - src/ui/shell/SplitView.tsx
  - src/ui/shell/SplitView.test.tsx
  - src/ui/AppShell.tsx
  - src/ui/AppShell.split-tree.test.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "A dragover on a Pane's outer div at a cursor position whose computeEdgeZone returns 'center' AND whose dataTransfer.types includes 'application/x-skynet-badge' renders the coral drop-preview overlay with data-zone='center' and full-cell geometry (left=0, top=0, width=rect.width, height=rect.height)"
    - "A dragover on a Pane's outer div at a center-zone cursor position AND whose dataTransfer.types includes 'application/x-skynet-row' (but NOT badge) also renders the coral overlay with data-zone='center' and full-cell geometry"
    - "A dragover at a center-zone cursor position with dataTransfer.types containing NEITHER 'application/x-skynet-badge' NOR 'application/x-skynet-row' renders NO overlay (the existing hasSkynetDragPayload gate at SplitView.tsx:275 already blocks this — regression assertion)"
    - "A drop on a Pane at a center-zone cursor position with an application/x-skynet-badge payload whose parsed tabId differs from the target cell's tabId invokes the new onSwapInTree(sourceTabId, targetTabId) prop exactly once; onOpenSessionInTree and onReplaceInTree and onDropRowInTree are NOT called"
    - "A drop on a Pane at a center-zone cursor position with an application/x-skynet-row payload (JSON with .id === sourceTabId) OR text/plain=sourceTabId fallback invokes the new onReplaceInTree(sourceTabId, targetTabId) prop exactly once; onSwapInTree is NOT called; onOpenSessionInTree is NOT called (center-drop routes to replace, not to the edge-drop rearrange path)"
    - "A drop on a Pane at a center-zone cursor position with a badge payload whose parsed tabId equals the target cell's tabId (self-drop) invokes NO handler (silent no-op per CONTEXT.md § Edge case #1) and emits a structured log line the executor may pattern-name '[pv-split-drop] center-self-drop-ignored'"
    - "A drop on a Pane at a center-zone cursor position with a badge/row MIME present but a malformed / unparseable payload emits [pv-split-drop] center-drop-unknown-mime log and invokes NO handler (silent no-op per CONTEXT.md § Edge case #5)"
    - "The pre-existing edge-zone drop behavior (Pane onDrop at left/right/top/bottom zones) is BYTE-UNCHANGED — text/plain fallback still routes to onOpenSessionInTree; rich-payload branch still routes to onDropRowInTree; all Phase 56 + 57 + quick-260829-mbp text-selection tests continue to pass"
    - "AppShell defines replaceInTree(replacementTabId, targetTabId) as a useCallback that wraps setSplitTree((prev) => replaceLeaf(prev, targetTabId, replacementTabId)) and emits ONE structured log [pv-split-drop] replace target=<tabId> with=<tabId>"
    - "AppShell defines swapInTree(tabIdA, tabIdB) as a useCallback that wraps setSplitTree((prev) => swapLeaves(prev, tabIdA, tabIdB)) and emits ONE structured log [pv-split-drop] swap a=<tabId> b=<tabId>"
    - "The SplitView render site at AppShell.tsx:~2544 wires onReplaceInTree={replaceInTree} + onSwapInTree={swapInTree} alongside existing onOpenSessionInTree + onDropRowInTree props"
    - "Integration test: with two open sessions in a two-leaf split tree, a badge-drag from Pane A's tabId onto Pane B's center dispatches through the new wiring and produces a tree whose two leaves have exchanged tabIds; both tabs remain in tabs[] (neither closeTab'd)"
    - "Integration test: with two open sessions in a split tree, a conv-list-row drag (source tabId = Pane A's open session id) onto Pane B's center dispatches through the new wiring; the resulting tree has A occupying B's cell; B is REMOVED from the tree (findLeaf(nextTree, 'b') === null) but B REMAINS in the tabs list (not closeTab'd — 'still present in the conv list, just no longer occupying a slot' per CONTEXT.md § What this is)"
    - "Portal-preservation integration test: after a center-drop swap, both sessions' portal-target DOM nodes retain reference identity (Object.is holds on tab-node-for=<tabId> parent element pre/post swap) — mirrors Phase 56 Test 6 assertion pattern at AppShell.split-tree.test.tsx:468"
    - "The [pv-split-drop] center-dead-zone ignored log line at SplitView.tsx:349-354 is REPLACED — its consumers (grep across ~/.claude/roles/box-maintainer/bounties/) are checked before removal per CONTEXT.md § What would make Phase 64 wrong"
  artifacts:
    - path: "src/ui/shell/SplitView.tsx"
      provides: "Pane component gains onReplaceInTree + onSwapInTree props; center-zone drop handler dispatches replace-vs-swap by MIME; overlay renders for center zone with full-cell geometry when a skynet MIME is present; overlayGeometryForZone gains a center branch (or new sibling helper overlayGeometryForCenter)"
      contains: "onSwapInTree"
    - path: "src/ui/shell/SplitView.test.tsx"
      provides: "New Phase 64 describe block covering center-zone overlay rendering (4 tests) + center-drop dispatch (5 tests) + edge-zone regression (1 test) matching CONTEXT.md § Test coverage additions Plan 64-02 SplitView tests"
      contains: "Phase 64"
    - path: "src/ui/AppShell.tsx"
      provides: "replaceInTree + swapInTree useCallback handlers alongside openSessionInTree at ~L1600-1713; onReplaceInTree={replaceInTree} + onSwapInTree={swapInTree} prop wiring on the <SplitView /> render at ~L2544"
      contains: "replaceInTree"
    - path: "src/ui/AppShell.split-tree.test.tsx"
      provides: "Three new integration tests at end of the existing describe block: end-to-end swap preserves both sessions in tabs[]; end-to-end replace kicks displaced session out of tree but keeps in tabs[]; portal-preservation across swap"
      contains: "Phase 64"
  key_links:
    - from: "src/ui/shell/SplitView.tsx Pane onDrop native listener center branch"
      to: "onReplaceInTree + onSwapInTree props → AppShell.tsx replaceInTree + swapInTree useCallbacks"
      via: "prop-drilling from AppShell <SplitView /> render at ~L2544"
      pattern: "onSwapInTree=\\{swapInTree\\}"
    - from: "src/ui/AppShell.tsx replaceInTree + swapInTree handlers"
      to: "src/ui/lib/split-tree.ts replaceLeaf + swapLeaves (Plan 64-01 exports)"
      via: "setSplitTree((prev) => replaceLeaf|swapLeaves(prev, ...))"
      pattern: "setSplitTree.*(replaceLeaf|swapLeaves)"
    - from: "src/ui/shell/SplitView.tsx overlay JSX at :474-489"
      to: "overlayGeometryForZone helper at :46-63 (extended to accept 'center' OR new sibling helper overlayGeometryForCenter(rect))"
      via: "the JSX gate `dropPreview.zone !== 'center'` is REPLACED with logic that renders overlay for center when hasSkynetDragPayload has already permitted the dragover"
      pattern: "data-zone=\"center\""
    - from: "src/ui/shell/SplitView.tsx Pane onDrop center-branch MIME discriminator"
      to: "src/ui/features/terminal/IdentityBadge.tsx dragstart payload contract (application/x-skynet-badge + text/plain=<tabId>) at :163-167 AND src/ui/features/pretty-conversations/PrettyConversationRow.tsx dragstart payload contract (application/x-skynet-row + text/plain=<row.id>) at :927-951"
      via: "e.dataTransfer.types check + e.dataTransfer.getData('application/x-skynet-badge')/'application/x-skynet-row') + JSON.parse of the payload"
      pattern: "application/x-skynet-(badge|row)"
    - from: "src/ui/AppShell.tsx doCloseTab reconcile at :1543 (existing setSplitTree removeLeaf(prev, id))"
      to: "unchanged — Phase 64 does NOT close tabs, so this reconcile is untouched (regression assertion via grep)"
      via: "not touched"
      pattern: "setSplitTree.*removeLeaf"
---

<objective>
Wire Phase 64 end-to-end on top of Plan 64-01's `replaceLeaf` + `swapLeaves` pure helpers. Three sub-deliverables in one plan (all in scope per CONTEXT.md § Vehicle notes: "Plan 02 = SplitView center-drop wiring + AppShell handlers + component/integration tests"):

1. **AppShell handlers.** Add `replaceInTree(replacementTabId, targetTabId)` and `swapInTree(tabIdA, tabIdB)` as `useCallback`s in `src/ui/AppShell.tsx` alongside the existing `openSessionInTree` at :1600-1713. Both mirror `openSessionInTree`'s functional-updater shape: `setSplitTree((prev) => replaceLeaf|swapLeaves(prev, ...))`. Each emits ONE structured `[pv-split-drop]` log per CONTEXT.md § In-scope item 5. Wire both as `onReplaceInTree={replaceInTree}` + `onSwapInTree={swapInTree}` at the `<SplitView />` render at :2544. No changes to `openSessionInTree`, `onDropRowInTree`, `doCloseTab`, or the URL-sync effect at :868 (auto-syncs because both handlers go through `setSplitTree`).

2. **SplitView center-zone drop + overlay wiring.** Rewrite the center-dead-zone short-circuit at `src/ui/shell/SplitView.tsx:349-355` from silent-return to source-conditioned dispatch: read `e.dataTransfer.types`; if `application/x-skynet-badge` present, parse the badge payload for `sourceTabId`, guard against self-drop (source === target), then call `onSwapInTree(sourceTabId, targetTabId)`. If `application/x-skynet-row` present (or `text/plain` as fallback per the existing Pane onDrop contract at :359, :380), extract the row's tabId (from row payload's `.id` OR text/plain) then call `onReplaceInTree(sourceTabId, targetTabId)`. Unknown MIME (malformed / not-a-skynet-payload) → silent no-op with `[pv-split-drop] center-drop-unknown-mime` log. Extend the overlay JSX gate at :474 to also render for `dropPreview.zone === "center"` when the drag carries a skynet MIME (the existing `hasSkynetDragPayload` gate at :275 already prevents non-skynet drags from setting `dropPreview` in the first place, so the overlay's condition can safely drop the `!== "center"` clause once the geometry helper handles the center case). Add a center branch to `overlayGeometryForZone` (or a new sibling helper `overlayGeometryForCenter(rect)` returning `{ left: 0, top: 0, width: rect.width, height: rect.height }`) so the overlay covers the whole cell. Coral RGBA values (`rgba(255, 184, 150, 0.22)` fill + `rgba(255, 184, 150, 0.60)` border), `zIndex: 20`, `pointer-events: none`, geometry transition — all reused verbatim per CONTEXT.md § In-scope item 4 ("Same coral vocabulary the shape file locks in"). Add new props `onReplaceInTree?: (replacementTabId, targetTabId) => void` and `onSwapInTree?: (tabIdA, tabIdB) => void` to the Pane component's interface at :198-219 and to the SplitView top-level component props (whichever level currently forwards `onOpenSessionInTree` down to Pane).

3. **Component + integration tests.** Extend `src/ui/shell/SplitView.test.tsx` with 10 new Phase 64 tests matching CONTEXT.md § Test coverage additions § Plan 64-02 SplitView tests (Tests 10-18 numbered per CONTEXT.md; renumber to Phase 64 Tests 1-10 in test names for local clarity). Extend `src/ui/AppShell.split-tree.test.tsx` with 3 new integration tests (Tests 19-21 per CONTEXT.md; renumber to Phase 64 Tests 1-3 in test names) covering end-to-end swap, end-to-end replace with displaced-session-stays-in-tabs, and portal-preservation across swap.

Purpose: Delivers Phase 64's user-visible payoff — coral highlight on center hover, replace-vs-swap semantics dispatched by drag source, both wired through the same `setSplitTree`-URL-sync path Phase 56 established. On ship, the shape file `.planning/shapes/shape-multi-view-center-drop.md` closes.

Output: `src/ui/shell/SplitView.tsx` with center-drop dispatch + full-cell overlay + two new Pane props; `src/ui/shell/SplitView.test.tsx` with 10 new Phase 64 tests; `src/ui/AppShell.tsx` with `replaceInTree` + `swapInTree` useCallbacks + SplitView prop wiring; `src/ui/AppShell.split-tree.test.tsx` with 3 new integration tests. All pre-existing SplitView tests (Phase 56 Plan 02 + Plan 03 + Phase 57 + patch #524 stacking-context + quick-260829-mbp text-selection) and AppShell.split-tree tests (Tests 3-10) continue to pass unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64-multi-view-center-drop/64-CONTEXT.md
@.planning/phases/64-multi-view-center-drop/64-01-PLAN.md
@src/ui/lib/split-tree.ts
@src/ui/shell/SplitView.tsx
@src/ui/shell/SplitView.test.tsx
@src/ui/AppShell.tsx
@src/ui/AppShell.split-tree.test.tsx
@src/ui/features/terminal/IdentityBadge.tsx
@src/ui/features/pretty-conversations/PrettyConversationRow.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SplitView center-drop dispatch + full-cell overlay + two new Pane props, TDD-first</name>
  <files>src/ui/shell/SplitView.tsx, src/ui/shell/SplitView.test.tsx</files>
  <read_first>
    - src/ui/shell/SplitView.tsx (full file, 640 lines) — target being modified. Focus areas: (a) Pane props interface at :198-219 (where new onReplaceInTree + onSwapInTree props slot in); (b) native onDrop listener at :332-388 (where the center-dead-zone short-circuit at :349-355 becomes the new dispatch branch); (c) overlayGeometryForZone helper at :46-63 (where the center branch is added); (d) overlay JSX at :474-489 (where the `zone !== "center"` gate is rewritten); (e) SplitView top-level component props + Pane render site (grep `onOpenSessionInTree=` inside SplitView.tsx to find where props forward down — Pane render is inside PaneTree at ~L500+, or wherever the recursive renderer instantiates Pane).
    - src/ui/shell/SplitView.tsx :172-183 — `hasSkynetDragPayload` helper. Center-drop dispatch reuses this + `e.dataTransfer.types` inspection for the badge-vs-row MIME discriminator.
    - src/ui/shell/SplitView.test.tsx (full file, 793 lines) — extension target. Reuse the shared helpers `dispatchDropAt`, `dispatchDragOverAt`, `dispatchDragLeaveAt`, `findPaneOuter`, `mockRect` at :429-512. New Phase 64 describe block goes AFTER the existing "SplitView — Phase 57: drop-preview overlay + edge-zone hit-testing" describe (ends around :793).
    - src/ui/features/terminal/IdentityBadge.tsx :158-172 — the badge dragstart payload contract. Payload MIME `application/x-skynet-badge` carries `JSON.stringify({ tabId: id })`; text/plain carries the raw tabId. Both are set. Center-drop dispatch code MUST parse the badge JSON to extract sourceTabId (mirroring what Plan 58 Plan 02 PrettyConversationsPanel does at its onDrop handler).
    - src/ui/features/pretty-conversations/PrettyConversationRow.tsx :927-951 — the conv-list-row dragstart payload contract. Payload MIME `application/x-skynet-row` carries `JSON.stringify({ id: row.id, host, targetTmuxSession, fleetOnly, rdpHostRow })`; text/plain carries `row.id`. Center-drop replace-branch reads either `.id` from the parsed rich payload OR text/plain fallback.
    - .planning/phases/64-multi-view-center-drop/64-CONTEXT.md § "In-scope this phase" items 3 + 4 (the exact SplitView center-drop handling contract + the whole-body coral highlight geometry) + § "Edge cases" items 1, 2, 4, 5, 6 + § "Test coverage additions" Plan 64-02 SplitView tests (the 10 tests to add).
    - src/ui/shell/SplitView.text-selection-drag.test.tsx (full file, ~294 lines) — quick-260829-mbp regression suite. Center-drop dispatch MUST NOT break these tests (they assert text-selection drags never render overlay or invoke handlers — hasSkynetDragPayload gate remains the gate).
  </read_first>
  <behavior>
    Ten new tests in a new `describe("SplitView — Phase 64: center-drop replace-vs-swap", () => { … })` block appended after the Phase 57 describe. Test names prefixed with "Phase 64" for local clarity. Numbering below matches CONTEXT.md § Test coverage additions § Plan 64-02 SplitView tests (Tests 10-18 in CONTEXT.md renumber to Phase 64 Tests 1-9 here; add a Test 10 covering deep-tree center-drop from CONTEXT.md § Edge case #7 for robustness).

    Overlay rendering (Tests 1-4):
    - Phase 64 Test 1 (badge-mime-center-overlay-full-cell): render `<SplitView splitTree={leaf("a")} tabs={[tabA]} />`; mock rect to (0,0,100,100); dispatchDragOverAt at (50, 50) — DEAD CENTER — with dataTransfer `{ types: ["application/x-skynet-badge", "text/plain"], getData: () => "" }`. Assert overlay `[data-testid="pane-drop-preview-overlay"]` renders with `data-zone="center"` AND style `left: "0px"`, `top: "0px"`, `width: "100px"`, `height: "100px"` (full cell).
    - Phase 64 Test 2 (row-mime-center-overlay-full-cell): same setup but dataTransfer types `["application/x-skynet-row", "text/plain"]`. Assert same overlay + data-zone="center" + full-cell geometry.
    - Phase 64 Test 3 (unknown-mime-center-no-overlay): dispatchDragOverAt at (50, 50) with dataTransfer types `["text/plain"]` ONLY (no skynet MIME). Assert overlay is NULL. Regression assertion — the existing hasSkynetDragPayload gate at :275 already blocks this; Phase 64 must not weaken it.
    - Phase 64 Test 4 (center-dragleave-clears-overlay): dispatchDragOverAt at (50, 50) with badge MIME → overlay renders → dispatchDragLeaveAt at (500, 500) (OUTSIDE the mocked rect) → overlay is NULL. Reuses existing dragleave bounding-rect guard at :319-330 unchanged (regression).

    Drop dispatch (Tests 5-9):
    - Phase 64 Test 5 (badge-mime-center-drop-calls-swap): render `<SplitView splitTree={leaf("target")} tabs={[tabTarget]} onSwapInTree={swapSpy} onReplaceInTree={replaceSpy} onOpenSessionInTree={openSpy} onDropRowInTree={rowDropSpy} />` where all four are `vi.fn()`; mock rect (0,0,100,100); dispatchDropAt at (50, 50) with dataTransfer `{ types: ["application/x-skynet-badge", "text/plain"], getData: (k) => k === "application/x-skynet-badge" ? JSON.stringify({tabId: "source"}) : k === "text/plain" ? "source" : "" }`. Assert `swapSpy` called EXACTLY once with `("source", "target")`. Assert `replaceSpy`, `openSpy`, `rowDropSpy` all NOT called.
    - Phase 64 Test 6 (row-mime-center-drop-calls-replace): same shell but dataTransfer `{ types: ["application/x-skynet-row", "text/plain"], getData: (k) => k === "application/x-skynet-row" ? JSON.stringify({id: "source", host: null, targetTmuxSession: null, fleetOnly: false, rdpHostRow: false}) : k === "text/plain" ? "source" : "" }`. Assert `replaceSpy` called EXACTLY once with `("source", "target")`. Assert `swapSpy`, `openSpy`, `rowDropSpy` all NOT called (edge-zone rich-payload path is unreachable from center).
    - Phase 64 Test 7 (badge-mime-self-drop-silent): dispatchDropAt at (50, 50) with dataTransfer `{ types: ["application/x-skynet-badge", "text/plain"], getData: (k) => k === "application/x-skynet-badge" ? JSON.stringify({tabId: "target"}) : k === "text/plain" ? "target" : "" }` — source tabId EQUALS the target's own tabId. Assert NO handler called (swapSpy, replaceSpy, openSpy, rowDropSpy all zero calls). Structured log MAY emit `[pv-split-drop] center-self-drop-ignored` per must_have truth 6 (assert via infoSpy that at least one call's first-arg string contains "center-self-drop-ignored" — non-load-bearing exact string, executor picks the token).
    - Phase 64 Test 8 (unknown-mime-center-drop-silent-with-log): dispatchDropAt at (50, 50) with dataTransfer `{ types: ["text/plain"], getData: (k) => k === "text/plain" ? "selected-text" : "" }` (text-selection drag pattern). The dragover onto this Pane never fires (hasSkynetDragPayload rejects), so this test simulates the pathological "drop bypasses dragover" case: it verifies the drop handler is a total function — hasSkynetDragPayload returns false at :333, and the handler early-returns. Assert NO handler called. NO overlay was rendered. NO throw.
    - Phase 64 Test 9 (edge-zone-drop-regression): render with all four spies; dispatchDropAt at (10, 50) — LEFT edge of a 100x100 rect — with the standard row-payload dataTransfer. Assert `rowDropSpy` called with `(parsedRowPayload, [], "left")` (Phase 56 rich-payload path preserved verbatim) AND `swapSpy`, `replaceSpy` both NOT called. Then a second scenario in the same it-block: dispatchDropAt at (10, 50) with text/plain-only-plus-badge-MIME dataTransfer (`{ types: ["application/x-skynet-badge", "text/plain"], getData: (k) => k === "text/plain" ? "source" : "" }`) — assert `openSpy("source", [], "left")` called (Phase 58 badge-onto-edge rearrange path preserved verbatim) AND `swapSpy`, `replaceSpy` NOT called. This ensures the edge-zone dispatch is BYTE-UNCHANGED and Phase 64 only touched the center branch.

    Deep-tree regression (Test 10):
    - Phase 64 Test 10 (deep-tree-center-drop-in-nested-cell): render with `splitTree = split("vertical", leaf("a"), split("horizontal", leaf("b"), leaf("c")))` and `tabs=[tabA,tabB,tabC]`; find the Pane content for cell "c" via querySelector `[data-tab-id="c"]`; walk to its outer via findPaneOuter; mock rect (0,0,100,100); dispatchDropAt at (50, 50) center with badge payload `{tabId: "a"}`. Assert `swapSpy` called with `("a", "c")` — the deep-tree case in CONTEXT.md § Edge case #7 exercised end-to-end through the SplitView layer.

    All ten tests MUST leave the pre-existing Phase 56 / Phase 57 / text-selection-drag test suites unchanged and passing.
  </behavior>
  <action>
    RED (write failing tests first):
    - At end of `src/ui/shell/SplitView.test.tsx` (after the closing brace of the Phase 57 describe block near :793), append the new Phase 64 describe block with the ten tests above. Reuse `dispatchDropAt`, `dispatchDragOverAt`, `dispatchDragLeaveAt`, `findPaneOuter`, `mockRect`, and the `infoSpy` beforeEach pattern from the Phase 57 describe — the helpers are file-scoped at :429-512 and available to the new describe. Add local `tabA`, `tabB`, `tabC`, `tabTarget` factory calls at the top of the new describe (mirror the Tab factory used at ~L60 of the file).
    - Run: `npx vitest run src/ui/shell/SplitView.test.tsx` — MUST show the ten new Phase 64 tests failing (either compile-failing on the missing onReplaceInTree / onSwapInTree props OR runtime-failing on the missing center-drop dispatch + overlay-branch behavior). All pre-existing tests MUST continue to pass.

    GREEN (implement in SplitView.tsx):

    Sub-step 1a — Extend Pane props interface at :198-219:
    - Add two new optional props alongside existing `onOpenSessionInTree` and `onDropRowInTree`:
      - `onReplaceInTree?: (replacementTabId: string, targetTabId: string) => void` — Phase 64 center-drop-from-conv-list dispatch. AppShell wires this to the `replaceInTree` useCallback in Task 2.
      - `onSwapInTree?: (tabIdA: string, tabIdB: string) => void` — Phase 64 center-drop-from-open-badge dispatch. AppShell wires this to `swapInTree` in Task 2.
    - JSDoc: cite `.planning/phases/64-multi-view-center-drop/64-CONTEXT.md § In-scope item 3` next to each prop. Note that both are optional so pre-Phase-64 test harnesses (Phase 56 / 57 tests) continue to render without them (silent-no-op if unwired — safe default matching the existing onOpenSessionInTree optionality).

    Sub-step 1b — Extend overlay geometry helper at :46-63:
    - Two options; executor chooses:
      OPTION A: Extend `overlayGeometryForZone`'s type signature to accept `"center"` and add a `case "center": return { left: 0, top: 0, width: w, height: h };` branch. Update the switch's exhaustiveness type from `Exclude<DropZone, "center">` to `DropZone`.
      OPTION B: Add a sibling helper `function overlayGeometryForCenter(rect: DOMRect | {...}): { left: number; top: number; width: number; height: number } { return { left: 0, top: 0, width: rect.width ?? (rect.right - rect.left), height: rect.height ?? (rect.bottom - rect.top) }; }` and have the JSX call `overlayGeometryForZone(dropPreview.zone, dropPreview.rect)` for edges and `overlayGeometryForCenter(dropPreview.rect)` for center.
    - Prefer OPTION A for uniformity (one function returns geometry for any zone). Delete or update the JSDoc note at :42-45 that says "Center is deliberately EXCLUDED from the input type" — that note is now stale.

    Sub-step 1c — Extend overlay JSX at :474-489:
    - Current: `{dropPreview !== null && dropPreview.zone !== "center" && ( <div … /> )}`.
    - Change to: `{dropPreview !== null && ( <div … data-zone={dropPreview.zone} style={{ ...overlayGeometryForZone(dropPreview.zone, dropPreview.rect), … }} /> )}`.
    - The safety of removing the `!== "center"` clause: the hasSkynetDragPayload gate at :275 already prevents non-skynet drags from setting `dropPreview` in the first place, so any dropPreview with zone=center is guaranteed to have a valid source MIME (must_have truth 3).
    - The `data-zone={dropPreview.zone}` binding already emits `data-zone="center"` when zone is center — Phase 64 Test 1 relies on this.
    - Style, coral RGBA, zIndex, pointer-events, transition — all UNCHANGED (must_have truth: "same coral vocabulary").

    Sub-step 1d — Rewrite the center-drop short-circuit at :349-355:
    - REPLACE the current three-line branch (`console.info([pv-split-drop] center-dead-zone ignored ...); return;`) with the Phase 64 dispatch. Behavior sketch:
      - `if (zone === "center") { … dispatch branch … return; }` — the branch is entered only when the zone is center; the else-path (edge zones) runs the existing byte-unchanged edge-drop dispatch at :356-387.
      - Inside the center branch:
        a. Read `const badgeJson = e.dataTransfer?.getData("application/x-skynet-badge") ?? "";`
        b. If `badgeJson.length > 0`: try `JSON.parse(badgeJson)`, extract `sourceTabId = parsed?.tabId` (string). If it is a non-empty string:
           - Self-drop guard: if `sourceTabId === tabId` (target cell's tabId, in scope from Pane props at :191), emit `console.info(`[pv-split-drop] center-self-drop-ignored path=${JSON.stringify(path)} tabId=${sourceTabId}`)` and return. (Test 7 asserts this — the exact string can be adjusted by the executor as long as it contains "center-self-drop-ignored".)
           - Otherwise emit `console.info(`[pv-split-drop] center-drop dispatch=swap path=${JSON.stringify(path)} sourceTabId=${sourceTabId} targetTabId=${tabId}`)` and call `onSwapInTree?.(sourceTabId, tabId)` then return.
           - On JSON.parse failure OR missing/empty sourceTabId: fall through to the row-branch below (or to the unknown-MIME branch), do NOT throw. Emit `console.warn(`[pv-split-drop] center-drop badge-parse failed path=${JSON.stringify(path)}: ${(err as Error).message}`)` before falling through.
        c. If badge branch did not fire, read `const rowJson = e.dataTransfer?.getData("application/x-skynet-row") ?? "";` If it is non-empty, try `JSON.parse(rowJson)`, extract `sourceTabId = parsed?.id` (string). If non-empty:
           - Self-drop guard: if `sourceTabId === tabId`, emit `[pv-split-drop] center-self-drop-ignored` log and return (defensive — a conv-list row of the already-in-target-cell session is technically a self-drop under center-replace semantics; treat as no-op).
           - Otherwise emit `console.info(`[pv-split-drop] center-drop dispatch=replace-rich path=${JSON.stringify(path)} rowId=${sourceTabId} targetTabId=${tabId}`)` and call `onReplaceInTree?.(sourceTabId, tabId)` then return.
        d. If neither badge nor rich-row branches fired, try text/plain fallback: `const payloadTabId = e.dataTransfer?.getData("text/plain") ?? "";` If non-empty AND != tabId AND `onReplaceInTree` is defined:
           - Emit `console.info(`[pv-split-drop] center-drop dispatch=replace-fallback path=${JSON.stringify(path)} payloadTabId=${payloadTabId} targetTabId=${tabId}`)` and call `onReplaceInTree(payloadTabId, tabId)` then return.
           - Rationale for the text/plain fallback: badges write BOTH badge-MIME AND text/plain=tabId (see IdentityBadge.tsx:163-167) — but a rare misuse could set text/plain without setting badge; and third-party sources that only set text/plain=tabId still route to replace as a graceful fallback. This preserves the Phase 56 text/plain-as-lowest-common-denominator contract at the edge-zone dispatch (:380-387) applied consistently to center too.
        e. If we reach this point, no valid payload was found. Emit `console.info(`[pv-split-drop] center-drop-unknown-mime path=${JSON.stringify(path)} types=${JSON.stringify(Array.from(e.dataTransfer?.types ?? []))}`)` per CONTEXT.md § In-scope item 5 line 4, then return.
    - CRITICAL: the branch structure MUST leave the edge-zone dispatch at :356-387 UNCHANGED. Test 9 (edge-zone-drop-regression) verifies this. Do NOT refactor the edge dispatch; only replace the center-dead-zone `console.info(... ignored); return;` block.

    Sub-step 1e — Extend the Pane render site (top-level SplitView component OR PaneTree recursive renderer, whichever passes down onOpenSessionInTree):
    - Grep `onOpenSessionInTree=` inside `src/ui/shell/SplitView.tsx` to find the render site — Pane receives these props from PaneTree, which receives them from SplitView (top-level exported component). Wherever onOpenSessionInTree is forwarded, also forward onReplaceInTree + onSwapInTree.
    - Add matching prop types to the SplitView top-level component's Props type AND the PaneTree component's Props type (whichever exists in the file).

    Run: `npx vitest run src/ui/shell/SplitView.test.tsx src/ui/shell/SplitView.text-selection-drag.test.tsx` — MUST show all pre-existing tests still passing PLUS the ten new Phase 64 tests passing. Especially: the text-selection-drag suite MUST NOT regress (asserts hasSkynetDragPayload gate still blocks non-skynet drags from either overlay OR handler dispatch).

    Commit (single atomic commit for the task):
    - `feat(64-02): SplitView center-drop dispatches replace-vs-swap by MIME + full-cell coral overlay + onReplaceInTree/onSwapInTree props`
  </action>
  <acceptance_criteria>
    - `npx vitest run src/ui/shell/SplitView.test.tsx src/ui/shell/SplitView.text-selection-drag.test.tsx` exits 0. Pre-existing tests (Phase 56 Plan 02 + Plan 03, Phase 57 tests 1-13, quick-260829-mbp 5 tests) all pass; the ten new Phase 64 tests pass.
    - `grep -c 'onSwapInTree' src/ui/shell/SplitView.tsx` returns >= 3 (Pane props declaration + SplitView top-level props declaration + one call site in the center-drop dispatch; may be higher if the recursive PaneTree also declares/threads it).
    - `grep -c 'onReplaceInTree' src/ui/shell/SplitView.tsx` returns >= 3 (same pattern).
    - `grep -c 'data-zone="center"\|data-zone={dropPreview.zone}' src/ui/shell/SplitView.tsx` returns >= 1 — the overlay JSX renders `data-zone` bound to the current zone including center (or explicit "center" literal if the executor prefers a conditional).
    - `grep -c 'center-dead-zone ignored' src/ui/shell/SplitView.tsx` returns 0 — the pre-existing silent-no-op log line at :351-354 is REPLACED per CONTEXT.md § What would make Phase 64 wrong ("[pv-split-drop] log stream broken. Check with a grep across ~/.claude/roles/box-maintainer/bounties/ for any references — none expected").
    - `grep -rc 'center-dead-zone ignored' ~/.claude/roles/box-maintainer/bounties/ 2>/dev/null | grep -v ':0$' | wc -l` returns 0 (no bounty forensic queries depend on the retired log string). If ANY match found, the executor MUST update those references in the same commit or defer the log-string change with a follow-up bounty note.
    - `grep -c '\[pv-split-drop\] center-drop' src/ui/shell/SplitView.tsx` returns >= 2 (at least the dispatch=swap log and either dispatch=replace-rich or dispatch=replace-fallback — the executor may consolidate these but at least two distinct center-drop log branches must exist for Tests 5, 6, 7 observability).
    - `grep -c '\[pv-split-drop\] center-drop-unknown-mime' src/ui/shell/SplitView.tsx` returns >= 1 (CONTEXT.md § In-scope item 5 line 4).
    - `grep -c 'application/x-skynet-badge\|application/x-skynet-row' src/ui/shell/SplitView.tsx` returns >= 4 (both MIMEs referenced in `hasSkynetDragPayload` at :178-183 already + at least one badge reference in the new center-drop dispatch + at least one row reference in the new center-drop dispatch).
    - `grep -c 'JSON\.stringify(e)\|JSON\.stringify(event)' src/ui/shell/SplitView.tsx` returns 0 — never stringify DOM Event objects (fleet logging directive Ashley 2026-08-11).
    - The overlay geometry helper handles the center zone: either `grep -c 'case "center":' src/ui/shell/SplitView.tsx` returns >= 1 (OPTION A) OR `grep -c 'overlayGeometryForCenter' src/ui/shell/SplitView.tsx` returns >= 2 (OPTION B, definition + call site).
    - Manual read: the existing `hasSkynetDragPayload` helper at :178-183 is UNCHANGED (regression — quick-260829-mbp text-selection defense depends on it).
    - Manual read: the edge-zone drop dispatch at :356-387 (previously `if (richJson && onDropRowInTree)` branch + text/plain fallback) is UNCHANGED — Phase 64 only replaces the center-dead-zone `return;` block above it.
    - Manual read: the `<img … draggable={false}/>` pattern is NOT introduced in SplitView.tsx (belongs to IdentityBadge.tsx, out of scope).
    - No changes to `src/ui/lib/split-tree.ts` in this task (`git diff --stat src/ui/lib/split-tree.ts` returns empty — Plan 64-01 owned that file).
    - `npx vitest run src/ui/shell/SplitView.stacking-context.test.tsx` exits 0 if the file exists (patch #524 regression — `isolate` on Pane wrapper untouched by Phase 64).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/ui/shell/SplitView.test.tsx src/ui/shell/SplitView.text-selection-drag.test.tsx</automated>
  </verify>
  <done>Ten new Phase 64 SplitView tests pass; all pre-existing SplitView + text-selection-drag tests pass; Pane component has onReplaceInTree + onSwapInTree props; center-drop dispatch branches by MIME to swap (badge) or replace (row/fallback) with self-drop and unknown-MIME silent guards; full-cell coral overlay renders on center hover when a skynet MIME is present; edge-zone drop dispatch is byte-unchanged (regression pass).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AppShell replaceInTree + swapInTree useCallbacks + SplitView prop wiring + three integration tests</name>
  <files>src/ui/AppShell.tsx, src/ui/AppShell.split-tree.test.tsx</files>
  <read_first>
    - src/ui/AppShell.tsx :1600-1713 (openSessionInTree useCallback — the mutation-path precedent replaceInTree + swapInTree mirror in shape: functional setSplitTree updater + structured log emission).
    - src/ui/AppShell.tsx :274 (`const [splitTree, setSplitTree] = useState<SplitNode | null>(null);` — the state hook both new handlers call).
    - src/ui/AppShell.tsx :868-909 (URL-sync effect — encodes splitTree into the URL on every change; both new handlers auto-sync because they go through setSplitTree with a value-changing updater. No wiring change needed here — regression assertion only.).
    - src/ui/AppShell.tsx :1543 (existing `setSplitTree((prev) => removeLeaf(prev, id));` inside doCloseTab — UNCHANGED by Phase 64; grep verify presence).
    - src/ui/AppShell.tsx :2544-2565 (<SplitView /> render site — new props wire here alongside existing onOpenSessionInTree + onDropRowInTree).
    - src/ui/AppShell.tsx imports at file top — grep `from "@/lib/split-tree"` to find the existing import block that currently pulls in `findLeaf`, `getNodeAt`, `insertAtEdge`, `removeLeaf`, `collectTabIds`, `SplitNode`, `SplitPath`, `DropEdge`. Extend to also import `replaceLeaf` and `swapLeaves` (Plan 64-01 exports).
    - src/ui/AppShell.split-tree.test.tsx :89-308 (MechanismScaffold definition — the test harness that mirrors AppShell code paths). Phase 64 integration tests either (a) extend MechanismScaffold to expose replaceInTree + swapInTree on the registerHandle payload alongside the existing openSessionInTree, OR (b) exercise the mechanism through a directly-invoked helper function that duplicates the AppShell replaceInTree/swapInTree body against the scaffold's splitTree state. Prefer (a): extend the scaffold — cleaner, mirrors Phase 56 test 6's pattern for portal-preservation assertion.
    - src/ui/AppShell.split-tree.test.tsx :468-515 (Test 6 — portal-preservation contract; the Phase 64 portal-preservation test mirrors this exact pattern for a center-drop swap).
    - .planning/phases/64-multi-view-center-drop/64-CONTEXT.md § "In-scope this phase" items 2 + 6 + § "Test coverage additions" Plan 64-02 AppShell integration tests (Tests 19-21).
  </read_first>
  <behavior>
    Three new integration tests appended to the existing `describe("AppShell split-tree mechanism (Phase 56 Plan 02)", () => { … })` block at src/ui/AppShell.split-tree.test.tsx:359. All three use `MechanismScaffold` extended to expose `replaceInTree` and `swapInTree` on the `registerHandle` payload (see Task 2 action step 2c below). Test names prefixed with "Phase 64" for local clarity.

    - Phase 64 Test 1 (end-to-end swap via center-drop preserves both sessions in tabs[]): create two tabs `tabA = makeTab("t-aaa", "host1", "aqua")` and `tabB = makeTab("t-bbb", "host1", "nelly")`; render `<MechanismScaffold tabs={[tabA, tabB]} registerHandle={(h) => { handle = h; }} />` where handle now exposes `swapInTree(tabIdA, tabIdB): void`. Wait for tabsReady, then seed the tree with two openSessionInTree calls: `openSessionInTree(tabA.id, [], "left")` and `openSessionInTree(tabB.id, [], "right")`. Assert the tree pre-swap is `split("vertical", leaf("t-aaa"), leaf("t-bbb"))` via `handle.getSplitTree()`. Now call `handle.swapInTree("t-aaa", "t-bbb")`. Assert (after await Promise.resolve()) the new tree is `split("vertical", leaf("t-bbb"), leaf("t-aaa"))` — same structural shape, only leaf tabIds traded. Assert `tabs` (still `[tabA, tabB]` — the scaffold doesn't have a closeTab call in swapInTree by design). Assert the URL-sync effect fired: `window.location.hash` contains the encoded tree with the two sessions in the new order (or use the scaffold's onSplitTreeChange callback to grab the fired value).
    - Phase 64 Test 2 (end-to-end replace via center-drop kicks displaced session out of tree, keeps in tabs[]): seed two tabs and a two-leaf tree the same way as Test 1. Assert pre-replace tree is `split("vertical", leaf("t-aaa"), leaf("t-bbb"))`. Now call `handle.replaceInTree("t-aaa", "t-bbb")` — replace tabB's cell with tabA (which was already in the tree at path [0], so the vertical split collapses per replaceLeaf's dedup-first behavior). Assert the new tree is `leaf("t-aaa")` (single-cell root — tabA moved into tabB's old slot; tabB evicted from tree; source cell of tabA collapsed with the parent split). Assert `findLeaf(newTree, "t-aaa")` is `[]` (root) AND `findLeaf(newTree, "t-bbb")` is `null` (evicted). CRITICAL: the tabs[] array remains `[tabA, tabB]` unchanged — the displaced session is still in tabs[], only kicked out of the tree per CONTEXT.md § What this is line 3 ("The displaced session drops out of the grid entirely — it's still present in the conv list, no longer occupying a slot"). Assert the MechanismScaffold did NOT invoke any closeTab-analog (verify via absence — the scaffold doesn't have a closeTab, so this is implicit; add a comment inline citing that Phase 64 replace is a tree-op, not a tab-lifecycle op).
    - Phase 64 Test 3 (portal-preservation across center-drop swap — mirrors Phase 56 Test 6 pattern): seed two tabs + two-leaf tree as before. Grab `contentABefore = document.querySelector('[data-testid="content-t-aaa"]');` and `nodeABefore = contentABefore.parentElement;` — the portal target for tabA. Also grab `nodeBBefore = document.querySelector('[data-testid="content-t-bbb"]').parentElement;` for tabB. Call `handle.swapInTree("t-aaa", "t-bbb")`, wait for effects, then grab `nodeAAfter` and `nodeBAfter` the same way. Assert `Object.is(nodeAAfter, nodeABefore) === true` AND `Object.is(nodeBAfter, nodeBBefore) === true` — both DOM nodes reparented into the other Pane, not remounted. Add an inline comment: "Phase 64 swap is a leaf-content-only tree mutation; MechanismScaffold's DOM-placement effect at :249-277 reparents the SAME tab node into whichever Pane is now assigned to each tab, so the underlying React subtree does not unmount. This satisfies CONTEXT.md § What would make Phase 64 wrong: 'Portal preservation broken on swap.'"

    Task 1 tests (SplitView layer) already assert center-drop → swap/replace dispatch calls the correct handler with correct args (Tests 5-9). Task 2 tests (AppShell integration layer) assert the handler-to-tree wire works end-to-end and preserves both the tabs-list invariant (Test 2) and the portal-preservation invariant (Test 3).
  </behavior>
  <action>
    RED (write failing tests first):

    Sub-step 2a — Extend MechanismScaffold to expose replaceInTree + swapInTree on registerHandle:
    - In `src/ui/AppShell.split-tree.test.tsx`, extend the `registerHandle` type at :108-111 to include:
      - `replaceInTree: (replacementTabId: string, targetTabId: string) => void;`
      - `swapInTree: (tabIdA: string, tabIdB: string) => void;`
    - In the MechanismScaffold body (~L172-215 openSessionInTree definition), add two new useCallbacks that mirror the shape AppShell will get in the GREEN step. Both are functional-updater wrappers around split-tree helpers:
      - `replaceInTree = useCallback((replacementTabId, targetTabId) => { setSplitTree((prev) => replaceLeaf(prev, targetTabId, replacementTabId)); }, [])`
      - `swapInTree = useCallback((tabIdA, tabIdB) => { setSplitTree((prev) => swapLeaves(prev, tabIdA, tabIdB)); }, [])`
    - Extend the imports at file top: add `replaceLeaf` and `swapLeaves` to the existing `from "@/lib/split-tree"` import block (they will exist post-Plan-64-01).
    - Extend the useEffect at :221-227 that populates registerHandle to include the two new callbacks in the emitted handle object.
    - Do NOT add [pv-split-drop] logs to the scaffold's callbacks — the scaffold's job is to mirror shape, not full production behavior. The real AppShell handlers (added in Sub-step 2b) emit the logs; scaffold callbacks are pure setSplitTree wrappers matching the AppShell shape.

    Sub-step 2b — Append three Phase 64 integration tests inside the existing describe block:
    - Locate the closing brace of the describe block at ~L664 (after Test 10 the previous integration test); append the three new tests before the closing brace.
    - Use the same `act(async () => { … })` pattern the existing Tests 6-10 use. Import `replaceLeaf`, `swapLeaves` if the test needs them directly for a computed-expected-tree comparison (they will exist post-Plan-64-01); alternatively use structural equality via `findLeaf` calls.
    - For Test 3 (portal-preservation), the DOM-placement effect in MechanismScaffold at :249-277 already handles reparenting per the tree state — no change needed to the effect for a swap because after `swapLeaves`, `findLeaf(splitTree, tabId)` returns a DIFFERENT path than before for each tabId, and the effect body reparents each tab's node into `paneElsRef.current.get(tab.id)` for its new path. Verify by manual read that the effect is invariant-preserving under swap (it should be — it iterates `tabs`, not `splitTree` paths, so swap is transparent to the reparenting logic).
    - Run: `npx vitest run src/ui/AppShell.split-tree.test.tsx` — MUST show the three new Phase 64 tests failing at the registerHandle payload (missing replaceInTree/swapInTree) OR at import time (if replaceLeaf/swapLeaves imports fail). This is expected RED for the test file only.

    GREEN (implement in AppShell.tsx):

    Sub-step 2c — Import replaceLeaf + swapLeaves:
    - Grep `from "@/lib/split-tree"` in src/ui/AppShell.tsx to locate the existing import; extend it to add `replaceLeaf` and `swapLeaves` alongside the existing symbols (`findLeaf`, `getNodeAt`, `insertAtEdge`, `removeLeaf`, `collectTabIds`, plus types).

    Sub-step 2d — Add replaceInTree + swapInTree useCallbacks (mirror openSessionInTree shape at :1600-1713):
    - Immediately after the closing `}, [])` of openSessionInTree at :1713 (BEFORE the Patch #511 comment block at :1715-1720), insert two new useCallbacks:
      - `replaceInTree = useCallback((replacementTabId: string, targetTabId: string) => { console.info(`[pv-split-drop] replace target=${targetTabId} with=${replacementTabId}`); setSplitTree((prev) => replaceLeaf(prev, targetTabId, replacementTabId)); setFocusedTabId(replacementTabId); }, [])`
      - `swapInTree = useCallback((tabIdA: string, tabIdB: string) => { console.info(`[pv-split-drop] swap a=${tabIdA} b=${tabIdB}`); setSplitTree((prev) => swapLeaves(prev, tabIdA, tabIdB)); setFocusedTabId(tabIdA); }, [])` — symmetric focus semantics per CONTEXT.md § In-scope item 2 (revised): the session the user was "carrying" during the drag (`tabIdA` = the badge that was dragged) lands focused in its new cell. Do NOT leave focus to executor discretion — the symmetric-focus rule is locked.
    - Structured log placement: BEFORE the setSplitTree call (matches openSessionInTree's `[pv-split-tree] openSessionInTree tabId=... prev=...` diag placement at :1607-1609 — log the intent, then apply the mutation; if the mutation returns prev-by-reference (helper's no-op branch), the tree simply doesn't change, no additional log needed).
    - Explicit-field extraction per fleet logging directive: log `target=<tabId>` and `with=<tabId>` as separate scalar fields, do NOT JSON.stringify anything (mirrors [badge-drag] pattern at IdentityBadge.tsx:170).
    - Empty dependency array `[]` — matches openSessionInTree's `[]` at :1712 (setSplitTree is a state setter, stable identity; setFocusedTabId same).
    - JSDoc on both: cite `.planning/phases/64-multi-view-center-drop/64-CONTEXT.md § In-scope item 2` + reference Plan 64-01's `replaceLeaf` / `swapLeaves` helpers.

    Sub-step 2e — Wire props on <SplitView /> render at :2544-2565:
    - Add two new prop lines alongside existing `onOpenSessionInTree={openSessionInTree}` and `onDropRowInTree={...}`:
      - `onReplaceInTree={replaceInTree}`
      - `onSwapInTree={swapInTree}`
    - Add an inline comment above the two new props: "Phase 64 center-drop wiring: SplitView center-zone drop dispatches to replaceInTree (from conv-list source MIME) or swapInTree (from open badge source MIME). Both go through setSplitTree so the URL-sync effect at :868 auto-encodes the new tree — no additional wiring needed."

    Sub-step 2f — Regression assertions (no code change, verify via grep in acceptance_criteria):
    - `openSessionInTree` at :1600-1713 UNCHANGED.
    - `doCloseTab` reconcile at :1543 (`setSplitTree((prev) => removeLeaf(prev, id));`) UNCHANGED.
    - URL-sync effect at :868 UNCHANGED — it reads `splitTree` and encodes on every change; both new handlers trigger it via setSplitTree.

    Run: `npx vitest run src/ui/AppShell.split-tree.test.tsx` — MUST show all pre-existing tests (Tests 3-10) passing PLUS the three new Phase 64 tests passing. Also run `npx vitest run src/ui/shell/SplitView.test.tsx src/ui/shell/SplitView.text-selection-drag.test.tsx` to confirm Task 1's tests still pass (should — Task 2 only touches AppShell.tsx + AppShell.split-tree.test.tsx).

    Commit (single atomic commit for the task):
    - `feat(64-02): AppShell replaceInTree + swapInTree useCallbacks + SplitView prop wiring + integration tests`
  </action>
  <acceptance_criteria>
    - `npx vitest run src/ui/AppShell.split-tree.test.tsx` exits 0 with pre-existing Tests 3-10 passing AND the three new Phase 64 integration tests passing.
    - `npx vitest run src/ui/shell/SplitView.test.tsx src/ui/shell/SplitView.text-selection-drag.test.tsx` exits 0 (Task 1 tests still green — Task 2 did not touch SplitView.tsx).
    - `grep -c 'const replaceInTree = useCallback\|replaceInTree = useCallback' src/ui/AppShell.tsx` returns 1.
    - `grep -c 'const swapInTree = useCallback\|swapInTree = useCallback' src/ui/AppShell.tsx` returns 1.
    - `grep -c 'onReplaceInTree={replaceInTree}' src/ui/AppShell.tsx` returns 1.
    - `grep -c 'onSwapInTree={swapInTree}' src/ui/AppShell.tsx` returns 1.
    - `grep -c '\[pv-split-drop\] replace target=' src/ui/AppShell.tsx` returns 1.
    - `grep -c '\[pv-split-drop\] swap a=' src/ui/AppShell.tsx` returns 1.
    - `grep -c 'replaceLeaf\|swapLeaves' src/ui/AppShell.tsx` returns >= 3 (import statement adds 2 of the count + at least 1 reference each in the two useCallback bodies; total >= 4 if you count both symbols in the import block and both in the bodies).
    - `grep -n 'setSplitTree((prev) => removeLeaf(prev, id))' src/ui/AppShell.tsx` returns a match on or near line 1543 (existing doCloseTab reconcile UNCHANGED per Phase 64 scope).
    - `grep -c 'const openSessionInTree = useCallback' src/ui/AppShell.tsx` returns 1 AND the function body at :1600-1713 is UNCHANGED (verify by reading a few key lines: the [pv-split-tree] log at :1607-1609, the same-cell no-op at :1624-1635, the withoutDup dance at :1648-1707 should all still be present verbatim).
    - `grep -c 'JSON\.stringify(e)\|JSON\.stringify(event)' src/ui/AppShell.tsx` returns 0 (no new event-stringify — regression guard against future refactors sneaking one in via a Phase 64 handler).
    - `git diff --stat` after the commit shows exactly TWO files: `src/ui/AppShell.tsx` and `src/ui/AppShell.split-tree.test.tsx`. No touches to `src/ui/lib/split-tree.ts` (Plan 64-01's file), no touches to `src/ui/shell/SplitView.tsx` (Task 1's file), no touches to `src/ui/shell/SplitView.test.tsx` (Task 1's file).
    - `grep -c '"Phase 64"' src/ui/AppShell.split-tree.test.tsx` returns >= 3 (each new test's name references Phase 64).
    - Integration Test 3 (portal-preservation) uses the `Object.is(nodeAAfter, nodeABefore)` assertion pattern — grep `Object\.is.*nodeA` inside the new tests should show at least two calls (one per swapped node A and B).
    - Manual read: MechanismScaffold's DOM-placement effect at :249-277 UNCHANGED — the reparenting logic is transparent to swap (iterates tabs, not paths), so no scaffold-side effect body change was needed.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/ui/AppShell.split-tree.test.tsx src/ui/shell/SplitView.test.tsx src/ui/shell/SplitView.text-selection-drag.test.tsx</automated>
  </verify>
  <done>Three new Phase 64 integration tests pass; pre-existing AppShell.split-tree.test.tsx tests (3-10) pass; SplitView test suites (Task 1 + text-selection-drag) still pass; AppShell defines replaceInTree + swapInTree useCallbacks that emit structured [pv-split-drop] logs before delegating to Plan 64-01's helpers via setSplitTree; SplitView render site wires both new handlers; openSessionInTree, doCloseTab reconcile, and URL-sync effect all untouched (regression pass).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dataTransfer → SplitView Pane center-drop dispatch | **untrusted-input surface** (this plan's new attack surface). A dropped payload's badge-JSON or row-JSON is fully attacker-controllable in principle — any page-scoped script or clever drag source could inject `{tabId:"tab-mallory-999"}`. The validation posture here differs from Phase 58 Plan 02 (conv-list drop = destructive close): center-drop is a tree-op that MAY reference a tabId not in the current tree, and per Plan 64-01 the helpers silently no-op with a warn log if the tabId is missing. So a bogus sourceTabId → replaceLeaf warns and returns root unchanged; swapLeaves warns and returns root unchanged. NO tree corruption, NO tab close, NO WS teardown — same "silent-safe" posture as the edge-zone rearrange path per Phase 58 CONTEXT.md § Trust Boundary line 3. |
| SplitView center-drop handler → onReplaceInTree / onSwapInTree props → AppShell handlers | trusted-after-source-validation. AppShell handlers wrap Plan 64-01 helpers which are themselves defensive (warn + reference-identity no-op on missing tabId). Not a security boundary — a helper-layer failure is loud (console.warn) and non-destructive. |
| AppShell handlers → setSplitTree → URL-sync effect | trusted contract. The URL fragment is signed against the same tabs[] state; a bogus tabId that somehow made it into splitTree would fail to encode (encoder returns null for unknown tabIds per Phase 56 Plan 02) and the tree would degrade to a partial rather than crash. Same graceful-degradation posture as Phase 56 Test 7. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-02-01 | Spoofing/Tampering | Attacker-controlled dataTransfer payload injects an arbitrary badge JSON `{tabId:"tab-mallory-999"}` on a center drop, hoping to trigger swapLeaves with a tabId not in the tree and cause a state anomaly | **mitigate** | Plan 64-01 swapLeaves helper defensively no-ops (returns root by reference + console.warn) when either tabId is missing. Task 1 Test 8 (unknown-mime silent) + Plan 64-01 Test 9 (missing-leaf defensive warn) together establish the two-layer defense. Additionally the AppShell replaceInTree/swapInTree handlers make no assumption about payload validity — they hand off to the helpers which enforce the invariant. |
| T-64-02-02 | Tampering | Malformed JSON in application/x-skynet-badge or application/x-skynet-row payload throws an unhandled exception, crashing the Pane's native drop listener | mitigate | Task 1 center-drop dispatch wraps JSON.parse in try/catch (same pattern the existing edge-zone rich-payload branch uses at :364-378). Test 8 (unknown-mime-center-drop-silent-with-log) asserts a malformed / missing-payload drop is silent + logged. Fall-through to text/plain fallback branch AND finally to `[pv-split-drop] center-drop-unknown-mime` log means no exception escapes. |
| T-64-02-03 | Denial of Service | Repeated rapid center-drops from a rogue script spam swapInTree/replaceInTree, triggering setSplitTree churn + URL-sync effect churn + [pv-split-drop] log spam | accept | Local-only interaction (in-page drag from same user session). setSplitTree is a state setter; React batches updates. URL-sync effect debounces via useEffect dep chain naturally. Log spam is bounded by human-drag-rate (~1 Hz per drag session). No amplification vector. |
| T-64-02-04 | Repudiation | Missing telemetry on center-drop makes a "why did my session get replaced?" debug session impossible | mitigate | Task 1 SplitView emits `[pv-split-drop] center-drop dispatch=(swap\|replace-rich\|replace-fallback) path=... sourceTabId=... targetTabId=...` on every dispatched drop; `[pv-split-drop] center-self-drop-ignored` on self-drops; `[pv-split-drop] center-drop-unknown-mime` on unparseable payloads. Task 2 AppShell emits `[pv-split-drop] replace target=... with=...` and `[pv-split-drop] swap a=... b=...` per invocation. Explicit-field extraction throughout. |
| T-64-02-05 | Information Disclosure | `[pv-split-drop]` logs emit tabIds which could leak session identifiers | accept | Same rationale as T-58-01-01 in Plan 58-01: tabId is not PII; already visible in DOM (data-tab-id attributes on Pane content divs at SplitView.tsx:461), in URL fragment (via split-tree-url.ts encoding), and in existing structured logs ([pv-split-tree] at AppShell.tsx:1608, [pv-split-drop] at SplitView.tsx:362, [badge-drag] at IdentityBadge.tsx:170). No new disclosure surface. |
| T-64-02-06 | Tampering | Displaced session on replace ends up half-mounted / half-torn-down — the promise "still there, just not in a slot" is broken | mitigate | Phase 64 replace is a pure tree-op (calls replaceLeaf via setSplitTree); it does NOT call closeTab. Task 2 Test 2 explicitly asserts `tabs` array unchanged after replace (`[tabA, tabB]` in, `[tabA, tabB]` out). The MechanismScaffold's DOM-placement effect reparents the displaced tab's node BACK into `normalViewRef` when `findLeaf(splitTree, tab.id) === null` (per :271-274) — the same code path that runs on any tab that's not in the tree. WS lifetime is per-tab and independent of tree position; no WS teardown fires. |
| T-64-02-07 | Tampering | Portal preservation broken on swap — a swap causes both sessions' React subtrees to re-mount, losing scroll position / compose-box draft / WebSocket connection state | mitigate | Task 2 Test 3 explicitly asserts `Object.is(nodeAAfter, nodeABefore) === true` for both swapped nodes — the DOM-placement effect reparents the SAME per-tab node into whichever Pane element it's now assigned to. React portals into that node preserve React-tree parentage, so PrettyView / Terminal / dashboard components portaled into the tab's node are not touched by the reparenting. This is the CONTEXT.md § What would make Phase 64 wrong: "Portal preservation broken on swap" checkpoint. |
| T-64-02-08 | Tampering | splitTree ends up with a stale leaf tabId referencing a tab not in tabs[] | mitigate | replaceLeaf and swapLeaves cannot introduce a phantom tabId — they only operate on tabIds already present in the input tree (or the replacementTabId parameter which is passed in by the SplitView dispatch, which itself reads from a dataTransfer payload validated to be a non-empty string). If a payload contains a tabId not in tabs[], setSplitTree stores it, but the DOM-placement effect and PaneTree renderer both tolerate stale leaves gracefully (per SplitView.tsx:448-457 `tab === null` "Session no longer exists" placeholder). The AppShell doCloseTab reconcile at :1543 keeps splitTree pruned as tabs close. |
| T-64-02-09 | Tampering | URL fragment does not update after replace or swap because a Phase 64 handler bypassed setSplitTree | mitigate | Both new AppShell handlers wrap setSplitTree (Sub-step 2d explicitly enforces the pattern). URL-sync effect at :868 fires on every splitTree change automatically. Test 1 (swap end-to-end) verifies URL sync fires by inspecting the encoded fragment post-swap. |
| T-64-02-10 | Repudiation | Retirement of the `[pv-split-drop] center-dead-zone ignored` log breaks a downstream forensic query or bounty dashboard depending on the exact string | mitigate | Task 1 acceptance criteria include `grep -rc 'center-dead-zone ignored' ~/.claude/roles/box-maintainer/bounties/ 2>/dev/null | grep -v ':0$' | wc -l` returning 0 (no matches). If ANY match found, the executor MUST update those references in the same commit or defer the log-string change per CONTEXT.md § What would make Phase 64 wrong last checkpoint. |
</threat_model>

<verification>
- `npx vitest run src/ui/AppShell.split-tree.test.tsx src/ui/shell/SplitView.test.tsx src/ui/shell/SplitView.text-selection-drag.test.tsx` exits 0 — all three files pass (Task 1 + Task 2 combined).
- `npx vitest run src/ui/lib/split-tree.test.ts` still exits 0 (Plan 64-01 regression — Task 2 did not touch split-tree.ts).
- `npx vitest run src/ui/features/terminal/IdentityBadge.test.tsx src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` exits 0 — Phase 58 badge + conv-list panel + row drag suites are UNTOUCHED by Phase 64 and must not regress.
- `npx vitest run src/ui/shell/SplitView.stacking-context.test.tsx` exits 0 if the file exists (patch #524 regression).
- `git diff --stat` after Plan 64-02 completion shows exactly FOUR files touched across the two tasks: `src/ui/shell/SplitView.tsx` (Task 1) + `src/ui/shell/SplitView.test.tsx` (Task 1) + `src/ui/AppShell.tsx` (Task 2) + `src/ui/AppShell.split-tree.test.tsx` (Task 2). NO touches to `src/ui/lib/split-tree.ts` (Plan 64-01's file).
- Manual read: `src/ui/AppShell.tsx:1543` `setSplitTree((prev) => removeLeaf(prev, id));` inside doCloseTab is present and unchanged (regression guard — the reconcile Phase 58 Plan 02 also depended on).
- Manual read: `src/ui/shell/SplitView.tsx` `hasSkynetDragPayload` helper at :178-183 is UNCHANGED (quick-260829-mbp regression).
- Manual read: `src/ui/shell/SplitView.tsx` edge-zone drop dispatch (rich-payload branch + text/plain fallback at :356-387) is UNCHANGED — Phase 64 only replaces the center-dead-zone branch above it.
- End-to-end UAT (Ashley, post-ship, out of executor scope per box-maintainer role): with two open sessions in a split, drag one session's identity badge onto the other's center — assert both sessions still live and trade positions (swap). Drag a conv-list row of a NOT-yet-open session onto an open session's center — assert the row's session opens directly into that cell and the previously-occupying session drops back to conv-list-only visibility. Drag a conv-list row of an ALREADY-open session onto a different open session's center — assert the source cell collapses and the drop-cell shows the source session; displaced session drops out of grid but stays live in conv list.
</verification>

<success_criteria>
1. `replaceLeaf` and `swapLeaves` from Plan 64-01 are consumed by two new AppShell useCallbacks `replaceInTree` and `swapInTree` (both structural mirrors of `openSessionInTree` at :1600-1713).
2. AppShell wires `onReplaceInTree={replaceInTree}` + `onSwapInTree={swapInTree}` on the `<SplitView />` render site at :2544, alongside existing `onOpenSessionInTree` + `onDropRowInTree` props.
3. SplitView Pane center-zone drop dispatches by MIME:
   - `application/x-skynet-badge` present → parse → guard self-drop → `onSwapInTree(sourceTabId, targetTabId)`.
   - `application/x-skynet-row` present → parse → guard self-drop → `onReplaceInTree(sourceTabId, targetTabId)`.
   - `text/plain` fallback (badge-less, row-less, but with a tabId) → `onReplaceInTree(payloadTabId, targetTabId)`.
   - Unknown / malformed / empty → silent no-op with `[pv-split-drop] center-drop-unknown-mime` log.
4. SplitView Pane center-zone dragover with a skynet MIME renders coral drop-preview overlay with `data-zone="center"` and FULL-CELL geometry (`left: 0, top: 0, width: rect.width, height: rect.height`).
5. Coral RGBA + border + zIndex + pointer-events + transition all UNCHANGED from the edge-zone overlay — "same coral vocabulary" per shape file lock.
6. Structured logging emitted per CONTEXT.md § In-scope item 5:
   - AppShell: `[pv-split-drop] replace target=<tabId> with=<tabId>` and `[pv-split-drop] swap a=<tabId> b=<tabId>`.
   - SplitView: `[pv-split-drop] center-drop dispatch=<swap|replace-rich|replace-fallback> path=... sourceTabId=... targetTabId=...` and `[pv-split-drop] center-self-drop-ignored ...` and `[pv-split-drop] center-drop-unknown-mime path=... types=...`.
   - The pre-existing `[pv-split-drop] center-dead-zone ignored` log is RETIRED (no bounty consumer depends on it — verified via grep).
7. Ten new SplitView Phase 64 tests pass; three new AppShell.split-tree.test.tsx Phase 64 integration tests pass.
8. All pre-existing tests still pass: SplitView (Phase 56 Plan 02 + Plan 03 + Phase 57 + quick-260829-mbp text-selection + patch #524 stacking-context if present), AppShell.split-tree (Tests 3-10), IdentityBadge (13 tests from Plan 58-01), PrettyConversationsPanel (Phase 58 Plan 02 + prior), split-tree.test.ts (all Phase 56/57/64 tests).
9. `doCloseTab` reconcile at AppShell.tsx:1543 UNCHANGED (regression guard).
10. `hasSkynetDragPayload` helper at SplitView.tsx:178-183 UNCHANGED (quick-260829-mbp text-selection regression guard).
11. Edge-zone drop dispatch at SplitView.tsx:356-387 UNCHANGED (Phase 56 rearrange path preserved).
12. `openSessionInTree` at AppShell.tsx:1600-1713 UNCHANGED.
13. URL-sync effect at AppShell.tsx:868 UNCHANGED — auto-fires on setSplitTree via both new handlers, so URL fragment reflects the new tree after every replace or swap without any additional wiring.
14. Portal preservation invariant preserved across swap: `Object.is(nodeAAfter, nodeABefore)` holds for both swapped sessions' DOM nodes (Task 2 Test 3).
15. Displaced-session-stays-in-tabs invariant preserved across replace: `tabs` array unchanged after replaceInTree fires; displaced session evicted from splitTree but not closeTab'd (Task 2 Test 2).
16. Two atomic commits per box-maintainer executor scope: `feat(64-02): SplitView center-drop dispatches replace-vs-swap by MIME + full-cell coral overlay + onReplaceInTree/onSwapInTree props` (Task 1) and `feat(64-02): AppShell replaceInTree + swapInTree useCallbacks + SplitView prop wiring + integration tests` (Task 2). No push, no docker build, no deploy — orchestrator-only per role directive.
</success_criteria>

<output>
Create `.planning/phases/64-multi-view-center-drop/64-02-SUMMARY.md` when done.
</output>
