---
phase: 62-wip-indicator-hook-based-rewrite
plan: 02
type: execute
wave: 2
depends_on: ["63-01-PLAN.md"]
files_modified:
  - src/backend/fleet-status/remote-hook-install.ts
  - src/backend/fleet-status/remote-hook-install.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "installStopHook (retained function name for callsite compatibility) drops THREE scripts onto every newly-acquired managed-box SSH channel: the existing skynet-fleet-status-stop.sh (unchanged — still writes background-tasks payload for the orthogonal consumer), a new skynet-fleet-status-activity.sh, and a new skynet-fleet-status-stopped.sh"
    - "installStopHook merges FIVE hook entries into ~/.claude/settings.json — the existing hooks.Stop[0].hooks[] entry pointing at skynet-fleet-status-stop.sh (unchanged), plus four new merge targets: hooks.UserPromptSubmit[0].hooks[] + hooks.PreToolUse[0].hooks[] both pointing at skynet-fleet-status-activity.sh, and hooks.StopFailure[0].hooks[] + hooks.PermissionRequest[0].hooks[] both pointing at skynet-fleet-status-stopped.sh, plus hooks.Stop[0].hooks[] also appended with the stopped-hook path (so Stop fires BOTH the existing background-tasks-carrying stop-hook AND the new marker-touching stopped-hook)"
    - "Second invocation of installStopHook is idempotent: no duplicate hook entries appear in any of the five hook arrays, and the script drops are no-op re-writes (identical bytes over identical paths)"
    - "STOP_HOOK_SCRIPT_CONTENTS constant (existing, unchanged) plus TWO new constants ACTIVITY_HOOK_SCRIPT_CONTENTS and STOPPED_HOOK_SCRIPT_CONTENTS are exported for byte-drift detection tests (mirrors the existing Test 11 pattern that asserts constant === on-disk file bytes)"
    - "All existing 10 remote-hook-install.test.ts tests still pass unchanged (the new install behavior is additive — existing Stop-hook install path is preserved)"
  artifacts:
    - path: "src/backend/fleet-status/remote-hook-install.ts"
      provides: "Extended installStopHook that drops 3 scripts + merges 5 hook entries; two new exported script-contents constants"
      contains: "ACTIVITY_HOOK_SCRIPT_CONTENTS, STOPPED_HOOK_SCRIPT_CONTENTS"
    - path: "src/backend/fleet-status/remote-hook-install.test.ts"
      provides: "Extended test coverage for the new install shape (script drops, settings merges, idempotency across all five hook keys, byte-drift detection for the two new constants)"
  key_links:
    - from: "installStopHook"
      to: "src/backend/fleet-status/activity-hook.sh + stopped-hook.sh (created in Plan 62-01)"
      via: "inlined constants read at module load; NOT runtime fs read (mirrors STOP_HOOK_SCRIPT_CONTENTS pattern per remote-hook-install.ts docblock lines 25-32)"
      pattern: "ACTIVITY_HOOK_SCRIPT_CONTENTS|STOPPED_HOOK_SCRIPT_CONTENTS"
    - from: "installStopHook merged settings.json"
      to: "~/.claude/settings.json.hooks.{UserPromptSubmit,PreToolUse,StopFailure,PermissionRequest,Stop}[0].hooks[]"
      via: "readAndMergeStopHookSettings generalized to five hook keys (or a new helper per hook key that reuses the existing three-level nesting pattern)"
      pattern: "UserPromptSubmit|PreToolUse|StopFailure|PermissionRequest"
---

<objective>
Extend the existing `installStopHook` in `remote-hook-install.ts` to drop the two new hook scripts (created in Plan 62-01) alongside the existing stop-hook, and to merge four new hook-event entries into `~/.claude/settings.json` alongside the existing hooks.Stop entry. The retained function name preserves the single callsite in `starter.ts` (line 249) — no starter.ts changes are needed in this phase.

Purpose: This plan is the propagation vehicle. Once merged and deployed by the orchestrator, every SSH-channel acquire on a managed box will (a) drop the two new marker-touching scripts and (b) register them under the four new hook events. The old stop-hook.sh stays installed (still writes the background-tasks-carrying box-wide payload for the orthogonal consumer path — CONTEXT.md §Out of scope). The old hooks.Stop entry stays; a new stopped-hook entry is ALSO appended to hooks.Stop so Stop fires both.

Output: Extended installer + comprehensive test coverage. The installer is idempotent (per shape §What would make it wrong — repeated ops must not corrupt settings.json). All existing tests continue to pass. New tests cover: script drops for the two new scripts, five-key hook merge, idempotency across all five keys, byte-drift detection for the two new script constants.

Rollout note (repeats CONTEXT.md §Rollout — locked choice for this phase set): Plan 62-03 chooses Option 1 from CONTEXT.md §Rollout (backend falls back to old predicate on absent markers). That choice means this installer does NOT need a "force re-install" story — the old machinery keeps running on unupgraded boxes until per-identity rollout hits them, at which point THIS installer runs (via starter.ts's existing `hookInstallAttempted`-gated first-acquire callsite) and the new markers become present.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63-wip-indicator-hook-based-rewrite/63-CONTEXT.md
@.planning/phases/63-wip-indicator-hook-based-rewrite/63-01-PLAN.md
@src/backend/fleet-status/remote-hook-install.ts
@src/backend/fleet-status/stop-hook.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend remote-hook-install.ts — inline the two new script constants + generalize the settings merge to five hook keys + drop three scripts per install</name>
  <read_first>
    - src/backend/fleet-status/remote-hook-install.ts (the file being modified — read the WHOLE file; the docblock lines 25-32 establish the "inline .sh at module load" pattern that MUST be extended, not replaced; the DEFAULT_REMOTE_HOOK_PATH constant at line 63 needs siblings for the two new paths; the readAndMergeStopHookSettings function at line 143 must generalize; the installStopHook function at line 217 must extend its steps 3 and 6b)
    - src/backend/fleet-status/activity-hook.sh (produced by Plan 62-01 Task 1 — its contents become the ACTIVITY_HOOK_SCRIPT_CONTENTS constant; the drift-detection Test asserts byte equality)
    - src/backend/fleet-status/stopped-hook.sh (produced by Plan 62-01 Task 2 — its contents become the STOPPED_HOOK_SCRIPT_CONTENTS constant)
    - src/backend/fleet-status/stop-hook.sh (unchanged — still installed as the box-wide background-tasks-payload writer; the existing STOP_HOOK_SCRIPT_CONTENTS constant + its Test-11 drift check stay)
    - src/backend/fleet-status/remote-hook-install.test.ts (the test file being modified in Task 2 below — read the current shape to understand what test-support constructs must survive; especially the MockSshChannel class + makeSettings helper + the `~` → absolute-path expansion tests that revolve around `home = /home/agent`)
    - src/backend/starter.ts lines 246-260 (the ONLY runtime caller of installStopHook — confirms the signature `(channel, opts?) => Promise<InstallResult>` MUST NOT change)
    - .planning/phases/63-wip-indicator-hook-based-rewrite/63-CONTEXT.md § Existing surfaces (the extension contract) + § Scope (In: extending remote-hook-install.ts to install the four new hook events alongside the existing Stop)
  </read_first>
  <behavior>
    - installStopHook(channel) against an empty-settings.json managed box results in THREE files present at ~/.claude/hooks/ (skynet-fleet-status-stop.sh, skynet-fleet-status-activity.sh, skynet-fleet-status-stopped.sh), each executable, each byte-identical to its module-inlined script-contents constant.
    - installStopHook(channel) against an empty-settings.json managed box produces a settings.json where hooks.Stop has TWO entries (stop-hook + stopped-hook), hooks.UserPromptSubmit has ONE entry (activity-hook), hooks.PreToolUse has ONE entry (activity-hook), hooks.StopFailure has ONE entry (stopped-hook), hooks.PermissionRequest has ONE entry (stopped-hook).
    - Second installStopHook(channel) against the SAME channel + settings.json returns { hookInstalled: true, settingsUpdated: false } — the settings-JSON tmp+mv step is skipped entirely (idempotency across all five hook keys).
    - Third-party existing hooks in any of the five keys are preserved unchanged (only the fleet-status entries are appended; readAndMergeHookSettings shallow-copy discipline is preserved from the original readAndMergeStopHookSettings).
    - The existing single callsite in starter.ts (line 249) compiles without modification — the installStopHook signature `(channel, opts?) => Promise<InstallResult>` is unchanged.
    - The ACTIVITY_HOOK_SCRIPT_CONTENTS and STOPPED_HOOK_SCRIPT_CONTENTS module-level constants are byte-identical to their sibling .sh files (drift-detection tests in Task 2 assert this).
    - `test -x` verify failure on any one of the three scripts throws an error message identifying which script failed (post-partial-install diagnosability).
  </behavior>
  <action>
    Modify `src/backend/fleet-status/remote-hook-install.ts`:

    1. Add TWO new default-path constants beside `DEFAULT_REMOTE_HOOK_PATH`:
       - `DEFAULT_REMOTE_ACTIVITY_HOOK_PATH = "~/.claude/hooks/skynet-fleet-status-activity.sh"`
       - `DEFAULT_REMOTE_STOPPED_HOOK_PATH = "~/.claude/hooks/skynet-fleet-status-stopped.sh"`
       Keep `DEFAULT_REMOTE_HOOK_PATH = "~/.claude/hooks/skynet-fleet-status-stop.sh"` unchanged (existing background-tasks-payload script).

    2. Extend `InstallOpts` interface with two optional escape hatches: `remoteActivityHookPath?: string` and `remoteStoppedHookPath?: string`. Preserve `remoteHookPath?` unchanged for callsite back-compat.

    3. Add TWO new exported script-contents constants: `ACTIVITY_HOOK_SCRIPT_CONTENTS` and `STOPPED_HOOK_SCRIPT_CONTENTS`, each holding the byte-exact contents of the corresponding sibling .sh file from Plan 62-01. Follow the STOP_HOOK_SCRIPT_CONTENTS template literal pattern EXACTLY (`\$\$` for shell parameter expansion, `\${...}` for interpolation escapes). Add block comments matching the STOP_HOOK_SCRIPT_CONTENTS docblock (lines 68-72) — SOURCE OF TRUTH warning citing the drift-detection test.

    4. Generalize `readAndMergeStopHookSettings` into a shape that handles ANY hook-event key. Two acceptable strategies (executor choice):
       Strategy A: Rename to `readAndMergeHookSettings(currentSettings, hookEventName, remoteHookPath)` and update the ONE existing callsite in `installStopHook` (line 372). Then call it five times: once per hook event key (`Stop`, `UserPromptSubmit`, `PreToolUse`, `StopFailure`, `PermissionRequest`), threading the correct remoteHookPath argument for each (stop-hook path for Stop; activity-hook path for UserPromptSubmit + PreToolUse; stopped-hook path for StopFailure + PermissionRequest; stopped-hook path ALSO appended to Stop). Export the renamed helper.
       Strategy B: Keep readAndMergeStopHookSettings as a Stop-key-hardcoded façade over a new private generic `readAndMergeHookEntry(currentSettings, hookEventName, remoteHookPath)`. Keeps the OLD export for any external caller (there are none; scripts/README.md documents no external usage of the helper, only of installStopHook/uninstallStopHook). This strategy minimizes test churn on Tests 3, 5, 6, 7 which currently import readAndMergeStopHookSettings.
       PREFER Strategy A — the "Stop" hardcoding in the helper name will confuse the next reader (Chekhov's Gun principle from CLAUDE.md-adjacent codebase conventions).

    5. Extend `installStopHook` step 3 (script drop) to drop THREE scripts sequentially — stop-hook, activity-hook, stopped-hook — each via the existing `cat > path.tmp <<'EOF' ... EOF; mv .tmp path; chmod +x` pattern. Use distinct heredoc sentinels per script (STOPHOOK_EOF, ACTIVITY_HOOK_EOF, STOPPED_HOOK_EOF) to avoid the theoretical case where one script's contents contain another's sentinel.

    6. Extend `installStopHook` step 4 (verify) to `test -x` all THREE remote script paths — throw with a script-specific error message on any failure.

    7. Extend `installStopHook` step 6b (merge) — call the generalized merge helper FIVE times, threading the results through the running `merged` object:
       - `merged1 = mergeFor("Stop", stopHookPath)` (existing behavior)
       - `merged2 = mergeFor("Stop", stoppedHookPath)` on merged1 (appends stopped-hook to hooks.Stop alongside stop-hook — Stop fires BOTH)
       - `merged3 = mergeFor("UserPromptSubmit", activityHookPath)` on merged2
       - `merged4 = mergeFor("PreToolUse", activityHookPath)` on merged3
       - `merged5 = mergeFor("StopFailure", stoppedHookPath)` on merged4
       - `merged6 = mergeFor("PermissionRequest", stoppedHookPath)` on merged5
       Track `alreadyInstalled` as the AND of all six merge results — if all six report the entry already present, skip the settings write; otherwise write the final `merged6`.

    8. Extend the log entry `fleet_status_hook_install_complete` to include forensic fields for the two new script paths (`remoteActivityHookPath`, `remoteStoppedHookPath`) so post-install SSH inspection can prove all three were installed on a given host.

    9. Extend `uninstallStopHook` to remove ALL FIVE hook entries + delete ALL THREE remote script files (activity + stopped + existing stop). Preserve the payload-directory-not-deleted invariant (line 419 docblock) — post-mortem inspection of the last-captured stop payload stays valuable.

    10. Do NOT modify the settings.json write path's tmp-file + mv atomicity (already correct). Do NOT modify the `~` → `$HOME` tilde-expansion logic (lines 232-259, patch #453 aftermath — critical migration behavior that must not regress).

    Preserve function signature of `installStopHook(channel, opts?)`. Preserve `readAndMergeStopHookSettings` export (Strategy B) OR export the new `readAndMergeHookSettings` name (Strategy A) — either way, the module's public surface grows only additively.
  </action>
  <verify>
    <automated>npx vitest run src/backend/fleet-status/remote-hook-install.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/backend/fleet-status/remote-hook-install.test.ts` — all existing 10 tests still pass AND the new tests added in Task 2 below pass. Overall exit 0.
    - `grep -c 'ACTIVITY_HOOK_SCRIPT_CONTENTS\|STOPPED_HOOK_SCRIPT_CONTENTS' src/backend/fleet-status/remote-hook-install.ts` returns ≥ 4 (each constant defined once + each exported once + each referenced at least once inside installStopHook).
    - `grep -c 'UserPromptSubmit\|PreToolUse\|StopFailure\|PermissionRequest' src/backend/fleet-status/remote-hook-install.ts` returns ≥ 4 (each hook event key referenced at least once for the merge calls; may be higher if forensic logging includes them).
    - `grep -c 'skynet-fleet-status-activity\.sh\|skynet-fleet-status-stopped\.sh' src/backend/fleet-status/remote-hook-install.ts` returns ≥ 2 (the two new default paths are referenced).
    - `npx tsc --noEmit -p tsconfig.json` (or the equivalent project-defined type-check command — check package.json scripts for `typecheck` / `check-types`) exits 0 (no TS errors introduced).
    - The existing single callsite in src/backend/starter.ts at line 249 still compiles: `grep -c 'installStopHook' src/backend/starter.ts` unchanged (no signature-break required a starter.ts edit).
  </acceptance_criteria>
  <done>remote-hook-install.ts modified, two new exported script-contents constants present, five-key merge in place, existing tests still green. Ready for Task 2 to add the new tests that prove the five-key install shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend remote-hook-install.test.ts — cover the two new script drops, five-key merge, idempotency across all keys, byte-drift detection for the two new constants</name>
  <read_first>
    - src/backend/fleet-status/remote-hook-install.test.ts (the file being modified — read the WHOLE file; especially Tests 2, 4, 8, 9, 10 which structure the async installStopHook / uninstallStopHook tests, and the MockSshChannel class + makeSettings helper which the new tests reuse)
    - src/backend/fleet-status/remote-hook-install.ts (as modified in Task 1 above — the extended installer's contract; the new constants + generalized helper name)
    - src/backend/fleet-status/activity-hook.sh + stopped-hook.sh (Plan 62-01 outputs — byte-source for the drift-detection assertions)
  </read_first>
  <behavior>
    - Existing Tests 1-10 continue to pass without modification (or with minimal modification limited to updating expected settings.json shapes if Task 1 chose Strategy A and renamed the exported helper).
    - New Test 11 (activity-hook drift detection): asserts `ACTIVITY_HOOK_SCRIPT_CONTENTS === fs.readFileSync('src/backend/fleet-status/activity-hook.sh', 'utf-8')` byte-for-byte, mirroring the existing Test-11 pattern for STOP_HOOK_SCRIPT_CONTENTS. (If existing Test 11 already occupies that number, use Test 11a / Test 12 / etc. — number is not the invariant; the drift-check pattern is.)
    - New Test (stopped-hook drift detection): same pattern for STOPPED_HOOK_SCRIPT_CONTENTS === stopped-hook.sh bytes.
    - New Test (install drops three scripts): installStopHook against a fresh MockSshChannel executes THREE `cat > .../<script>.tmp <<'...'; mv; chmod +x` sequences — asserts via channel.callLog that all three script paths appear in the log in order (stop, activity, stopped — or whatever deterministic order Task 1 chose; the invariant is "all three, sequential, each with atomic .tmp+mv+chmod").
    - New Test (install merges five hook keys): installStopHook against a fresh channel (empty settings.json) causes the final settings.json write to contain:
        hooks.Stop with TWO entries (stop-hook path + stopped-hook path),
        hooks.UserPromptSubmit with ONE entry (activity-hook path),
        hooks.PreToolUse with ONE entry (activity-hook path),
        hooks.StopFailure with ONE entry (stopped-hook path),
        hooks.PermissionRequest with ONE entry (stopped-hook path).
      Assert by parsing the JSON body of the last `cat > .../settings.json.tmp.$$ <<'SETTINGS_EOF'` call in channel.callLog.
    - New Test (idempotency across all five keys): call installStopHook TWICE against a channel whose SECOND settings.json read returns the settings.json produced by the FIRST install. Assert (a) the second install performs zero settings.json writes (channel.callLog contains only one `cat > .../settings.json.tmp.$$` after both installs), (b) all six merge calls report alreadyInstalled=true, and (c) the return value from the second call has `settingsUpdated: false`.
    - New Test (partial idempotency — pre-existing Stop hook but no activity/stopped): channel is seeded with a settings.json that ALREADY contains the existing stop-hook entry (simulating an upgrade from pre-Phase-62 install). Assert the second install ADDS the five new merges (Stop appended with stopped-hook, plus UserPromptSubmit/PreToolUse/StopFailure/PermissionRequest all populated) without duplicating the existing Stop entry.
    - New Test (third-party hook preservation across ALL FIVE new event keys — Concern #6 regression proof): seed settings.json with pre-existing third-party (non-fleet-status) entries in EACH of the four new event keys AND alongside the existing Stop entry. Concretely: `hooks.Stop = [{ hooks: [{ type: 'command', command: '/opt/user-stop-hook.sh' }] }]`, `hooks.UserPromptSubmit = [{ hooks: [{ type: 'command', command: '/opt/user-prompt-hook.sh' }] }]`, `hooks.PreToolUse = [{ hooks: [{ type: 'command', command: '/opt/user-tool-hook.sh' }] }]`, `hooks.StopFailure = [{ hooks: [{ type: 'command', command: '/opt/user-fail-hook.sh' }] }]`, `hooks.PermissionRequest = [{ hooks: [{ type: 'command', command: '/opt/user-perm-hook.sh' }] }]`. Call installStopHook once. Assert:
      (a) ALL FIVE third-party entries are preserved intact in the final settings.json — none deleted, none mutated, none re-ordered relative to their pre-existing position.
      (b) Our fleet-status entries are ADDED (either appended to the same hooks[] group after the third-party entry, or as a separate group — either shape satisfies Claude Code's hook merge semantics per RESEARCH §2).
      (c) Every third-party command string appears exactly ONCE in the final settings.json (no accidental duplication).
      (d) Second installStopHook call against the resulting settings.json is idempotent: no settings.json write occurs, no third-party entry is re-mutated, no fleet-status entry is duplicated.
      This test is the primary regression guard for MEDIUM-severity Concern #6 raised in plan review — proves the shallow-copy discipline of the generalized merge helper preserves ALL third-party entries across ALL five hook keys, not just the pre-existing Stop entry.
    - New Test (uninstall removes all five entries + three script files): uninstallStopHook against a channel seeded with a Phase-62-shaped settings.json + all three script paths asserts all five hook entries are removed AND all three script files are deleted via `rm -f`. Payload directory NOT deleted (preserved invariant).
    - New Test (script drop failure aborts install): mock the `test -x` verify step for the activity-hook to return non-OK; assert installStopHook throws with a message that identifies WHICH script failed the verify (so operators can diagnose partial-install states).
    - Existing Test 11 (STOP_HOOK_SCRIPT_CONTENTS drift) — CONFIRM this test still passes unchanged (stop-hook.sh is not modified in this phase).
  </behavior>
  <action>
    Add the new tests to `src/backend/fleet-status/remote-hook-install.test.ts` at logical positions (drift-detection tests grouped with existing Test 11; install-shape tests grouped with existing Tests 2-4; idempotency tests grouped with existing Test 3; uninstall tests grouped with existing Test 8).

    Use the existing MockSshChannel + makeSettings helpers. Extend makeSettings (or add a new helper `makePhase62Settings(stopHookPath, activityHookPath, stoppedHookPath)`) to produce the five-key settings.json shape used by the idempotency tests.

    For byte-drift tests: use `fs.readFileSync(path.join(__dirname, 'activity-hook.sh'), 'utf-8')` — mirroring the existing Test-11 pattern (grep for `readFileSync` in the test file to find the exact idiom).

    For the "install merges five hook keys" test: extract the last SETTINGS_EOF heredoc body from channel.callLog with a helper like `extractLastSettingsWrite(channel.callLog)` (grep the test file — this may already exist for Test 2 or Test 6).

    For the "partial idempotency" test: this covers the Phase-62 UPGRADE case — a box that had the old Phase-59 stop-hook installed and is now being re-installed with the Phase-62 five-key shape. This is the primary path through which per-identity rollout will hit each managed box.

    Do NOT modify or delete the existing 10 tests unless Task 1 chose Strategy A (renamed the exported helper) — in which case update the impacted `import { ... } from './remote-hook-install.js'` line and any test that calls the helper by its old name.
  </action>
  <verify>
    <automated>npx vitest run src/backend/fleet-status/remote-hook-install.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/backend/fleet-status/remote-hook-install.test.ts` — ALL tests pass (existing 10 + the ≥ 7 new tests added here). Exit 0.
    - `grep -Ec 'it\(' src/backend/fleet-status/remote-hook-install.test.ts` returns ≥ 18 (10 old + 8 new — the eight new tests are: activity-hook drift, stopped-hook drift, install-drops-three-scripts, install-merges-five-hook-keys, idempotency-across-all-five-keys, partial-upgrade, third-party-hook-preservation-across-all-five-keys, uninstall-removes-all-three, per-script-verify-failure — count may be higher if Task 2 added extra edge-case tests).
    - Grep for the drift-detection pattern: `grep -c 'readFileSync.*activity-hook\|readFileSync.*stopped-hook' src/backend/fleet-status/remote-hook-install.test.ts` returns ≥ 2.
    - Grep for the five-key coverage: `grep -c 'UserPromptSubmit\|PreToolUse\|StopFailure\|PermissionRequest' src/backend/fleet-status/remote-hook-install.test.ts` returns ≥ 4.
    - No changes to any file outside of src/backend/fleet-status/remote-hook-install.ts and its .test.ts. Confirm via `git diff --stat` — exactly two files touched.
  </acceptance_criteria>
  <done>All install-side coverage in place. The installer is proven idempotent + five-key + three-script. Ready for Plan 62-03 to swap the backend WIP predicate to consume the two new markers over SSH.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skynet backend → managed-box SSH channel | The installer writes shell scripts + edits settings.json on the managed box via an authenticated SSH connection. |
| Installer → settings.json parse | The installer reads existing settings.json which could be an attacker-modified state (malicious hook entries pre-existing). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-02-01 | Tampering | settings.json merge — existing hook entries preserved | mitigate | readAndMergeHookSettings (generalized) preserves ALL existing hook keys and only APPENDS the fleet-status entry to the specified event's hooks[] array — the existing helper's shallow-copy discipline (lines 168-193) is preserved when generalizing. Test "partial idempotency" (Task 2) proves existing Stop entry is preserved when the four new events are added. |
| T-62-02-02 | Tampering | settings.json contains invalid JSON | mitigate | Existing throw-not-overwrite behavior (lines 331-343) preserved — an attacker-corrupted settings.json cannot be silently clobbered by an install. Test 10 (existing) proves this. |
| T-62-02-03 | Denial-of-Service | Install failure blocks orchestrator poll cycle | accept | Existing acquire-path behavior (starter.ts line 411-425) fires installStopHook as fire-and-forget on FIRST successful acquire per lifecycle; failures are logged but never block the returned channel. This phase does not change that pattern. |
| T-62-02-SC | Tampering | Package installs (npm/pip/cargo) | n/a | No new package dependencies introduced. RESEARCH.md Package Legitimacy Gate not applicable. |
| T-62-02-04 | Elevation-of-Privilege | Heredoc injection via script contents containing sentinel string | mitigate | Distinct heredoc sentinels per script (STOPHOOK_EOF, ACTIVITY_HOOK_EOF, STOPPED_HOOK_EOF) prevent one script's contents from prematurely closing another's heredoc. The two new scripts are Plan 62-01 authored (trusted content) and their .sh sources do not contain their own sentinel strings — verified by the byte-drift Test 11 pattern which fully reads and asserts the contents. |
| T-62-02-05 | Repudiation | Which of the three script drops was the one that failed | mitigate | Task 1 step 6 makes the throw message on `test -x` verify failure identify the specific script (stop / activity / stopped) so operators can diagnose partial installs. The `fleet_status_hook_install_complete` log includes all three remote paths for post-install forensic inspection. |
</threat_model>

<verification>
- Existing 10 tests still pass unchanged.
- ≥ 8 new tests pass covering the five-key install shape, byte-drift for the two new script constants, idempotency across all five keys, partial-upgrade case, third-party-hook-preservation across all five keys (Concern #6 regression proof), uninstall-all-three, and per-script verify-failure identification.
- No signature change to installStopHook — starter.ts callsite at line 249 unchanged.
- No new package dependencies.
</verification>

<success_criteria>
Extended installer + comprehensive test coverage complete. When the orchestrator deploys this + Plan 62-03 + Plan 62-04, the FIRST successful SSH-channel acquire on each managed box (per `hookInstallAttempted` gate in starter.ts) will (a) drop all three scripts and (b) merge all five hook entries into settings.json. Per-identity rollout order per Ashley (Nelly-on-thenasty first, then propagate) is orchestrator-managed via the deploy timing — this code produces no divergent behavior between identities; only the install-time-per-box determines when the new hooks become active on each box.
</success_criteria>

<output>
Create `.planning/phases/63-wip-indicator-hook-based-rewrite/63-02-SUMMARY.md` when done. Cite the executor's remit-stop: code + commit + scoped tests green — NO deploy, NO ship, NO push, NO per-identity re-install trigger (all orchestrator-managed post-code).
</output>
