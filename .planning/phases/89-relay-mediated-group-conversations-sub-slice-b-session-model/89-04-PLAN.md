---
phase: 89-relay-mediated-group-conversations-sub-slice-b-session-model
plan: 04
type: execute
wave: 4
depends_on: [89-01, 89-03]
files_modified:
  - src/backend/database/routes/sessions.ts
  - src/backend/database/routes/sessions.test.ts
autonomous: true
requirements: [D-15]
must_haves:
  truths:
    - "GET /sessions/list continues to derive the existing harness sessions from SSH + tmux + JSONL byte-identically to the current behavior (no regression on the harness path per D-01 scope anchor)."
    - "After the derived-harness-sessions block resolves, the handler queries listActiveRelayRoomSessions(userId) and appends each active relay-room row to the response as a session-list item."
    - "Every item in the merged response carries a `kind` marker (planner-locked concrete field name — see Task 1 action): `kind: 'harness'` for the derived-tmux items, `kind: 'relay-room'` for the appended stored-table items — so the frontend (slice D) can branch on the marker."
    - "The merge step respects the existing PER_HOST_TIMEOUT_MS + CONNECT_TIMEOUT_MS timeout budgets (D-15 note in CONTEXT.md — the DB query is local + cheap so this is trivially satisfied; the plan explicitly verifies no accidental slow path)."
    - "If the relay-room DB query throws, the harness sessions are still returned (best-effort merge — better to show 5 harness rows than 500 the entire endpoint)."
  artifacts:
    - path: "src/backend/database/routes/sessions.ts"
      provides: "GET /sessions/list merge step: derived harness sessions + active relay-room rows with kind marker"
      contains: "listActiveRelayRoomSessions"
    - path: "src/backend/database/routes/sessions.test.ts"
      provides: "Scoped tests for the merge: harness-only case, relay-only case, merged case, DB-throw fallback"
  key_links:
    - from: "src/backend/database/routes/sessions.ts (GET /sessions/list handler)"
      to: "src/backend/relay-sessions/relay-room-sessions-store.ts::listActiveRelayRoomSessions"
      via: "single DB query post-derived-harness-block"
      pattern: "listActiveRelayRoomSessions\\(userId\\)"
---

<objective>
Merge the new stored relay-room sessions into the /sessions/list response so the frontend receives a single flat list of both kinds (D-15). Attach a kind marker on every item so slice D's frontend can branch on how to render each. Preserve the existing harness-derivation path completely intact (D-01 scope anchor: this slice adds a peer path, does NOT modify the derived-at-request-time path).

Purpose: This is the single point of user-visible integration in Phase 89 — the merge is what makes the observation loop's work visible in /sessions/list (though the actual pane rendering waits for slice D). The `kind` marker is the wire-protocol handshake with slice D.

Output: Extended /sessions/list handler that reads listActiveRelayRoomSessions and appends rows to the flat response; every item carries `kind`. Scoped tests cover the merge cases including the DB-throw fallback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/89-relay-mediated-group-conversations-sub-slice-b-session-model/89-CONTEXT.md
@.planning/phases/89-relay-mediated-group-conversations-sub-slice-b-session-model/89-01-PLAN.md
@.planning/phases/89-relay-mediated-group-conversations-sub-slice-b-session-model/89-03-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend /sessions/list handler with relay-room merge + kind marker</name>
  <files>src/backend/database/routes/sessions.ts, src/backend/database/routes/sessions.test.ts</files>
  <read_first>
    - src/backend/database/routes/sessions.ts (READ ONCE — extract in a single pass: L1-46 imports + timeout constant docblocks; L248-261 TmuxSessionRow interface; L274-552 the entire /sessions/list handler — study the exact response shape at L543-544 flat.sort + res.json(flat) and DO NOT alter the harness-derived rows in place; you're APPENDING to the flat array after Promise.all resolves and BEFORE the sort/res.json call)
    - src/backend/relay-sessions/relay-room-sessions-store.ts (already-created Plan 01 — refer to listActiveRelayRoomSessions signature)
    - .planning/phases/89-relay-mediated-group-conversations-sub-slice-b-session-model/89-CONTEXT.md (D-15 — merge shape + kind marker; D-01 scope anchor — do NOT modify the derived-harness path byte-identity)
  </read_first>
  <behavior>
    - Test 1 (harness-only): user has 2 harness sessions from tmux and zero active relay-room rows. Response: array of 2 items, each with `kind: "harness"` field added, all other TmuxSessionRow fields unchanged.
    - Test 2 (relay-only): user has 0 harness sessions (no autoTmux hosts) and 3 active relay-room rows. Response: array of 3 items, each with `kind: "relay-room"`, `roomId`, `roomTitle`, `lastActivityAt`, `createdAt`, `updatedAt` fields.
    - Test 3 (merged): user has 2 harness + 3 relay = 5-item response. First 2 have kind='harness', last 3 have kind='relay-room'. Both kinds share NO field names beyond `kind` (except createdAt/created — see Task 1 for the concrete decision).
    - Test 4 (DB throw fallback): listActiveRelayRoomSessions throws. Response: the 2 harness rows only, each with kind='harness'. Response status is 200 (best-effort merge — do NOT fail the endpoint on the relay side). A warning is logged via databaseLogger.
    - Test 5 (timeout budget): the total endpoint response time remains within the existing budget — verified by asserting that listActiveRelayRoomSessions is called AFTER the Promise.all across candidates resolves, NOT wrapped in setTimeout (it's an in-process DB query and does not need the 30s cap).
    - Test 6 (no regression on harness path): existing harness-only test cases pass unchanged (the harness fields are byte-identical; kind='harness' is the only added field).
  </behavior>
  <action>
    In sessions.ts:

    1. Add import at the top: `import { listActiveRelayRoomSessions } from "../../relay-sessions/relay-room-sessions-store.js";` (matching ESM `.js` extension convention).

    2. Extend the TmuxSessionRow interface at L248-261: add `kind: "harness"` as a required literal field. Every construction site of TmuxSessionRow inside the Promise.all block (L330-341) MUST set `kind: "harness" as const`. This makes it a compile-time invariant that every harness row emitted carries the marker.

    3. Define a new interface for the appended items:
       ```
       interface RelayRoomSessionRow {
         kind: "relay-room";
         id: string;
         roomId: string;
         roomTitle: string | null;
         lastActivityAt: string | null;
         createdAt: string;
         updatedAt: string;
       }
       ```
       Concrete field naming rationale (D-15 planner's-call locked here): field is `kind` (matches the existing convention in Skynet's wire protocol elsewhere — grep for `type:` / `kind:` in wire-protocol.ts and pick the more common form. Fleet convention: `kind` is used in fleet-status wire protocol; wire-protocol.ts as reference). Union member values `"harness"` and `"relay-room"` are kebab-case for wire-shape consistency with the existing frame-type strings.

    4. Change the response type of the handler's final block. After the `results` Promise.all resolves at L541-542 and before `res.json(flat)` at L544:
       ```
       (No fenced code in action — see: fetch active relay-room rows in a try/catch that logs and swallows on error; map each row to a RelayRoomSessionRow with kind: "relay-room" set as const; concatenate to flat; then the existing sort by b.created - a.created is preserved but its comparator now needs a fallback for relay rows which have no `created` field — see below.)
       ```

    5. Concrete merge code shape (write it directly, do not embed fenced blocks):
       - After `const results = await Promise.all(...)` at L541, before the flat/sort/res.json.
       - `let relayRows: RelayRoomSessionRow[] = [];`
       - `try { const active = await listActiveRelayRoomSessions(userId); relayRows = active.map(r => ({ kind: "relay-room" as const, id: r.id, roomId: r.roomId, roomTitle: r.roomTitle, lastActivityAt: r.lastActivityAt, createdAt: r.createdAt, updatedAt: r.updatedAt })); } catch (e) { databaseLogger.warn("[phase-89] listActiveRelayRoomSessions failed — returning harness rows only (best-effort merge)", { operation: "sessions_list_relay_merge_failed", userId, error: e instanceof Error ? e.message : "unknown" }); relayRows = []; }`
       - Build the harness flat: `const harnessFlat = results.flat();`
       - Combine: `const flat = [...harnessFlat, ...relayRows];`
       - Sort: the existing `b.created - a.created` comparator only works for harness rows. Preserve existing behavior for harness rows and place relay rows AT THE END without inter-mixing (slice D can re-sort by lastActivityAt if desired per D-15 — sort ordering across the merged list is explicitly deferred to slice D). Concrete implementation: `const harnessSorted = harnessFlat.sort((a, b) => b.created - a.created); const flat = [...harnessSorted, ...relayRows];`

    6. `return res.json(flat);` — response type is now `Array<TmuxSessionRow | RelayRoomSessionRow>`. Slice D's frontend switches on `item.kind` to render.

    7. Add a docblock immediately above the new merge block:
       "Phase 89 D-15 merge — appends active relay-room rows to the harness-derived list.
        Every item in the merged response carries a `kind` marker so slice D can branch.
        Harness rows are sorted by `created` (unchanged from Phase 47); relay rows are appended
        after — slice D re-sorts by `lastActivityAt` if desired per D-15 explicit deferral.
        Best-effort: listActiveRelayRoomSessions failure logs a warning and returns
        harness-only (better UX than 500-ing the entire endpoint). D-01 scope anchor: the
        harness-derivation path above is byte-identical; this block is APPEND-ONLY."

    In sessions.test.ts:
    - Add 6 scoped tests covering behaviors 1-6 above. Mock listActiveRelayRoomSessions via vi.mock('../../relay-sessions/relay-room-sessions-store.js', ...) — return controlled arrays per test.
    - For the harness-side of tests 1/3/6, use whatever mock pattern the existing sessions.test.ts uses for connectOneShot + execCommand. If sessions.test.ts does not exist, this task additionally creates it — extract the mock pattern from an adjacent test file (e.g. host-autostart-routes.test.ts or users.test.ts). Verify: the /sessions/list route is Express-mounted at a router — testing it needs either a supertest-style integration test or a direct handler-function extraction.
    - If sessions.test.ts requires substantial new scaffolding beyond a light supertest harness, note this in SUMMARY.md and cover the merge logic via a unit-test-friendly extraction: refactor the merge block into a small pure helper `mergeRelayRoomsIntoFlat(harnessFlat, relayRows): Array<TmuxSessionRow | RelayRoomSessionRow>` (default export from sessions.ts OR a co-located helper file `sessions-merge-helper.ts`), test the helper directly with fixtures, and cover the DB-throw fallback path via a tiny mocked handler test.

    Preferred: extract `mergeRelayRoomsIntoFlat` to a co-located file `src/backend/database/routes/sessions-merge-helper.ts` — pure function, trivial to test. The handler calls it inline: `const flat = mergeRelayRoomsIntoFlat(harnessFlat, relayRows);`. This keeps sessions.ts diff surgical and moves the testable logic to a pure module. Create `sessions-merge-helper.test.ts` with the 3 pure-merge scenarios (harness-only, relay-only, merged); the DB-throw fallback is verified via a mocked-handler test or by asserting listActiveRelayRoomSessions is wrapped in try/catch (grep-level acceptance criterion).
  </action>
  <verify>
    <automated>npx vitest run --related src/backend/database/routes/sessions.ts src/backend/relay-sessions/relay-room-sessions-store.ts</automated>
    <human-check>grep -c 'listActiveRelayRoomSessions' src/backend/database/routes/sessions.ts must return >= 1; grep -c 'kind: "harness"' src/backend/database/routes/sessions.ts must return >= 1; grep -c 'kind: "relay-room"' src/backend/database/routes/sessions.ts must return >= 1; grep -c 'sessions_list_relay_merge_failed' src/backend/database/routes/sessions.ts must return >= 1; grep -c 'PER_HOST_TIMEOUT_MS' src/backend/database/routes/sessions.ts must remain unchanged from pre-plan-89 count (verify: this file's PER_HOST_TIMEOUT_MS usage must NOT decrease — the harness-derivation path is byte-identical); grep -v '^ *[/\*]' src/backend/database/routes/sessions.ts | grep -c 'setTimeout.*PER_HOST_TIMEOUT_MS' must remain unchanged (3 sites — Promise.race blocks for tmux list-sessions + role-resolve + recency-signals per L59-60 docblock)</human-check>
  </verify>
  <done>
    /sessions/list returns a flat merged list where every item carries a `kind` marker. Harness rows are sorted by `created` (unchanged); relay rows appended after (slice D re-sorts). DB-throw is best-effort log-and-swallow. Timeout budgets unchanged. Tests cover harness-only, relay-only, merged, and DB-throw-fallback cases.
  </done>
</task>

</tasks>

<verification>
- /sessions/list returns a flat list with kind='harness' | 'relay-room' on every item.
- Harness-derivation path is byte-identical to pre-plan (D-01 scope anchor verified via grep count of PER_HOST_TIMEOUT_MS + setTimeout usage).
- DB-throw fallback returns harness-only with 200 (best-effort merge).
- Sort ordering across kinds explicitly deferred to slice D per D-15 (relay rows appended after harness rows).
</verification>

<success_criteria>
- Handler extended with the merge block + kind marker on both sides.
- listActiveRelayRoomSessions consumed in try/catch that logs and swallows.
- Merge logic extracted to a pure helper (sessions-merge-helper.ts) or inline — planner-flex, but tested regardless.
- Scoped tests pass.
- No regression in existing harness-only test cases.
</success_criteria>

<output>
Create `.planning/phases/89-relay-mediated-group-conversations-sub-slice-b-session-model/89-04-SUMMARY.md` when done.
</output>
