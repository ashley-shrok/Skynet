---
phase: 89-relay-mediated-group-conversations-sub-slice-b-session-model
plan: 02
type: execute
wave: 2
depends_on: [89-01]
files_modified:
  - src/backend/matrix/matrix-admin-client.ts
  - src/backend/matrix/matrix-admin-client.test.ts
  - src/backend/relay-sessions/registry-rooms.ts
  - src/backend/relay-sessions/registry-rooms.test.ts
  - src/backend/relay-sessions/registry-rooms-backfill.ts
  - src/backend/relay-sessions/registry-rooms-backfill.test.ts
  - src/backend/database/routes/identity-birth-orchestrator.ts
  - src/backend/database/routes/identity-birth-orchestrator.test.ts
  - src/backend/database/routes/users.ts
  - src/backend/database/routes/users.test.ts
autonomous: true
requirements: [D-10, D-11, D-12, D-13]
must_haves:
  truths:
    - "Two registry rooms (agents + humans) exist on the Matrix homeserver, created idempotently by Skynet on boot per D-10."
    - "Both registry room IDs are added to the admin_rooms ignore-list at creation time per D-13."
    - "A new agent's Matrix account is joined to the agents registry room at POST /identities/birth (inside runRelayMintAndWrite, post-mint) per D-11 — the id-skill self-register block is NOT the pathway."
    - "A new human's Matrix account is joined to the humans registry room at POST /users/create (post-mint, before response) per D-11."
    - "A one-time backfill enumerates all pre-existing agents (from fleet-status roster) and all pre-existing humans (from users table where mxid IS NOT NULL) and joins each to the appropriate registry room; backfill is idempotent per D-12."
    - "Backfill runs at boot before the first observation loop tick, gated on a `has_backfilled` settings row so it fires exactly once per Skynet instance install."
  artifacts:
    - path: "src/backend/matrix/matrix-admin-client.ts"
      provides: "createRoom primitive + getRoomIdForAlias helper for idempotent registry-room creation"
      exports: ["createRoom"]
    - path: "src/backend/relay-sessions/registry-rooms.ts"
      provides: "ensureRegistryRoomsExist (boot-time), joinAgentToAgentsRegistry, joinHumanToHumansRegistry, getAgentsRegistryRoomId, getHumansRegistryRoomId"
      exports: ["ensureRegistryRoomsExist", "joinAgentToAgentsRegistry", "joinHumanToHumansRegistry", "getAgentsRegistryRoomId", "getHumansRegistryRoomId"]
    - path: "src/backend/relay-sessions/registry-rooms-backfill.ts"
      provides: "runRegistryRoomsBackfill (one-shot, gated on settings flag)"
      exports: ["runRegistryRoomsBackfill"]
  key_links:
    - from: "src/backend/relay-sessions/registry-rooms.ts"
      to: "src/backend/matrix/matrix-admin-client.ts::joinRoom + createRoom"
      via: "admin-credentialed HTTP"
      pattern: "createRoom|joinRoom"
    - from: "src/backend/relay-sessions/registry-rooms.ts"
      to: "src/backend/relay-sessions/admin-rooms-ignore-list.ts::addAdminRoom"
      via: "populate ignore-list at room creation time per D-13"
      pattern: "addAdminRoom"
    - from: "src/backend/database/routes/identity-birth-orchestrator.ts::runRelayMintAndWrite"
      to: "src/backend/relay-sessions/registry-rooms.ts::joinAgentToAgentsRegistry"
      via: "post-mint hook (after Step 6 mint succeeds, before Step 7)"
      pattern: "joinAgentToAgentsRegistry"
    - from: "src/backend/database/routes/users.ts (POST /users/create)"
      to: "src/backend/relay-sessions/registry-rooms.ts::joinHumanToHumansRegistry"
      via: "post-mint hook (after createOrUpdateUser succeeds, before or during INSERT)"
      pattern: "joinHumanToHumansRegistry"
---

<objective>
Populate the classification substrate: two registry rooms (agents + humans) that give the observation loop a positive homeserver-persistent signal for "who is what" per D-09. Skynet is the account creator for both types (Phase 75 + Phase 88 established this — D-11), so joining at mint time is the natural hook. Existing accounts predate the rooms, so a one-time backfill enumerates + joins them per D-12. Registry rooms themselves are added to the admin_rooms ignore-list (D-13) so the observation loop skips materializing them.

Purpose: Plan 03 (observation loop) needs to ask "is the other member of a two-party room in the agents registry?" — this plan makes the "yes/no" answer authoritative and homeserver-persistent (survives host downtime).

Output: New `createRoom` primitive on matrix-admin-client (mirror the existing primitive-conventions there). New `registry-rooms.ts` module with ensure-on-boot + join-hooks. New `registry-rooms-backfill.ts` module with one-shot idempotent enumeration. Wire the join hooks into both mint sites (identity-birth-orchestrator + users.ts).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/89-relay-mediated-group-conversations-sub-slice-b-session-model/89-CONTEXT.md
@.planning/phases/89-relay-mediated-group-conversations-sub-slice-b-session-model/89-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add createRoom primitive to matrix-admin-client</name>
  <files>src/backend/matrix/matrix-admin-client.ts, src/backend/matrix/matrix-admin-client.test.ts</files>
  <read_first>
    - src/backend/matrix/matrix-admin-client.ts (READ ONCE — extract in a single pass: L1-45 module docblock + REQUEST_TIMEOUT_MS + error-code constants + AdminOk/AdminErr discriminated-union shape; L177-230 joinRoom (closest existing pattern for a POST with body against a Synapse endpoint); L544-602 deactivateUser (existing POST primitive with body — matches the "POST + JSON body + discriminated-union return" shape you'll reproduce))
    - src/backend/matrix/matrix-admin-client.test.ts (READ ONCE — pattern reference: existing joinRoom/deactivateUser test conventions with the shared fetch mock; look for the fetch-mock helper's setup + the ok/err assertion style)
  </read_first>
  <behavior>
    - Test 1: createRoom with { name: "agents-registry", preset: "private_chat", visibility: "private" } sends POST to `${homeserverBase}/_matrix/client/v3/createRoom` (NOT the admin API — createRoom uses the client-server API), Authorization: Bearer <admin token>, body includes name + preset + visibility, and returns `{ ok: true, roomId }` on 200.
    - Test 2: On 400 (invalid room state), returns `{ ok: false, status: 400, error: ERR_NON_2XX }`.
    - Test 3: On admin creds missing, returns `{ ok: false, status: 500, error: ERR_CREDS_MISSING }`.
    - Test 4: On AbortError (timeout), returns `{ ok: false, status: 504, error: ERR_TIMEOUT }` — timeoutId is cleared in both success + error paths.
    - Test 5: On network throw, returns `{ ok: false, status: 502, error: ERR_PROXY }`; the admin token is NEVER logged (searchable via `expect(loggerSpy).not.toHaveBeenCalledWith(expect.stringContaining(admin token substring))`).
  </behavior>
  <action>
    Add `createRoom` primitive to matrix-admin-client.ts following the existing primitive conventions exactly (docblock header, error-code constants, AbortController + REQUEST_TIMEOUT_MS wrapper, clearTimeout in both branches, discriminated-union return via AdminOk<T> | AdminErr).

    Endpoint: POST `${creds.homeserverBase}/_matrix/client/v3/createRoom` (client-server API — the admin API has no createRoom; the admin credential is a normal Matrix access_token that works on both APIs). Headers: `Authorization: Bearer ${creds.accessToken}`, `Content-Type: application/json`.

    Body: JSON with optional fields — planner-locked concrete shape:
      - `name: string` (display name — required arg)
      - `preset: "private_chat" | "trusted_private_chat"` (default "private_chat" — closed, invite-only)
      - `visibility: "public" | "private"` (default "private" — not published in the public room directory)
      - `room_alias_name?: string` (optional — if provided, sets the canonical alias localpart; the returned room will have alias `#{alias}:{server_name}`)

    Signature:
      `export type CreateRoomOk = AdminOk<{ roomId: string; roomAlias?: string }>;`
      `export async function createRoom(opts: { name: string; preset?: "private_chat" | "trusted_private_chat"; visibility?: "public" | "private"; roomAliasName?: string }): Promise<CreateRoomOk | AdminErr>`

    Response parse: expect `{ room_id: string, room_alias?: string }`; if `room_id` is missing or wrong type, return `{ ok: false, status: 500, error: ERR_NO_TOKEN }` (reuse the existing "expected-field-missing" code — matches loginAsUser L155-159 pattern).

    Logging on error: `databaseLogger.error("matrix admin proxy error", err, { operation: "matrix_admin_create_room" })` — NEVER include creds.accessToken.

    Add 5 tests to matrix-admin-client.test.ts describing Tests 1-5 above, using the existing fetch-mock helper.
  </action>
  <verify>
    <automated>npx vitest run src/backend/matrix/matrix-admin-client.test.ts</automated>
    <human-check>grep -c 'export async function createRoom' src/backend/matrix/matrix-admin-client.ts must return 1; grep -c '_matrix/client/v3/createRoom' src/backend/matrix/matrix-admin-client.ts must return 1; grep -c 'operation: "matrix_admin_create_room"' src/backend/matrix/matrix-admin-client.ts must return >= 1</human-check>
  </verify>
  <done>
    createRoom primitive added following the exact conventions of the existing 5 primitives. All 5 new tests pass. Admin token never logged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Registry-rooms module (ensure-on-boot + join hooks + accessors)</name>
  <files>src/backend/relay-sessions/registry-rooms.ts, src/backend/relay-sessions/registry-rooms.test.ts</files>
  <read_first>
    - src/backend/matrix/matrix-admin-client.ts (already-read — refer to createRoom + joinRoom signatures from Task 1)
    - src/backend/relay-sessions/admin-rooms-ignore-list.ts (already-created Plan 01 — refer to addAdminRoom signature)
    - src/backend/database/db/index.ts (READ ONCE around L600-670 — settings-table read/write patterns used for allow_registration / guac_enabled — you will use the same pattern for `agents_registry_room_id` and `humans_registry_room_id` settings keys)
    - .planning/phases/89-relay-mediated-group-conversations-sub-slice-b-session-model/89-CONTEXT.md (D-10, D-11, D-13)
  </read_first>
  <behavior>
    - Test 1: `ensureRegistryRoomsExist()` on a fresh Skynet install with no admin creds returns `{ ok: false, reason: "creds_missing" }` and does NOT throw.
    - Test 2: `ensureRegistryRoomsExist()` when neither settings row exists calls createRoom TWICE (once with name "agents-registry", once with name "humans-registry"), persists the returned room_ids to settings (keys `agents_registry_room_id`, `humans_registry_room_id`), and calls addAdminRoom for each returned room_id.
    - Test 3: `ensureRegistryRoomsExist()` when both settings rows already exist is a no-op that returns `{ ok: true, agentsRoomId, humansRoomId }` — does NOT call createRoom again (idempotent per D-10 boot-safe).
    - Test 4: `getAgentsRegistryRoomId()` returns the stored room_id (or null if the settings row is absent).
    - Test 5: `joinAgentToAgentsRegistry(agentMxid)` reads the stored agents-registry room_id, calls joinRoom(roomId, agentMxid); returns the joinRoom result verbatim.
    - Test 6: `joinAgentToAgentsRegistry(agentMxid)` when the settings row is absent (rooms not yet ensured) returns `{ ok: false, status: 500, error: "registry_room_not_configured" }` — does NOT throw.
    - Test 7: joinHumanToHumansRegistry mirrors Test 5/6 for the humans room.
    - Test 8: ensureRegistryRoomsExist logs structured entries at each interaction boundary (creds-missing, create-room fire, addAdminRoom fire) via databaseLogger.
  </behavior>
  <action>
    Create `src/backend/relay-sessions/registry-rooms.ts` exporting:

      - `SETTINGS_KEY_AGENTS_REGISTRY = "agents_registry_room_id"` and `SETTINGS_KEY_HUMANS_REGISTRY = "humans_registry_room_id"` (exported const strings — the observation loop in Plan 03 needs to import these).

      - `getAgentsRegistryRoomId(): Promise<string | null>` — SELECT value FROM settings WHERE key = SETTINGS_KEY_AGENTS_REGISTRY; returns the stored value or null.

      - `getHumansRegistryRoomId(): Promise<string | null>` — mirror.

      - `ensureRegistryRoomsExist(): Promise<{ ok: true; agentsRoomId: string; humansRoomId: string } | { ok: false; reason: string }>` — idempotent boot-time init:
        1. If getMatrixAdminCreds() returns null → return `{ ok: false, reason: "creds_missing" }`.
        2. Read both settings keys. If both present → return `{ ok: true, agentsRoomId, humansRoomId }` (no-op happy path — boot-safe).
        3. For each missing setting: call createRoom({ name, preset: "private_chat", visibility: "private", roomAliasName }) where name/alias match the D-10-planner-choice concrete strings (locked below), take the returned roomId, persist to settings via `INSERT OR REPLACE INTO settings (key, value) VALUES (?, ?)`, call `await addAdminRoom(roomId)`, call `await DatabaseSaveTrigger.forceSave("phase-89-registry-rooms-ensure")` in try/catch that logs and swallows.
        4. Return the pair of room_ids.

        Concrete D-10 planner-choice values (locked in this plan — do NOT re-question):
          - Agents room: `name = "Skynet agents directory (internal)"`, `roomAliasName = "_skynet_agents_directory"` (leading underscore is a fleet-convention marker for internal-only rooms; the parenthetical "(internal)" in display name reduces the "human accidentally stumbled in and is confused" surface per D-10 planner-choice guidance).
          - Humans room: `name = "Skynet humans directory (internal)"`, `roomAliasName = "_skynet_humans_directory"`.

      - `joinAgentToAgentsRegistry(agentMxid: string): Promise<{ ok: true; roomId: string } | { ok: false; status: number; error: string }>`:
        1. Read getAgentsRegistryRoomId(). If null → return `{ ok: false, status: 500, error: "registry_room_not_configured" }`.
        2. Call `joinRoom(roomId, agentMxid)` from matrix-admin-client. Return its result verbatim (already a discriminated union).

      - `joinHumanToHumansRegistry(humanMxid: string)`: mirror.

    Structured logging via databaseLogger at each interaction boundary: `{operation: "registry_rooms_ensure_start"}`, `{operation: "registry_rooms_create_fire", role: "agents"|"humans"}`, `{operation: "registry_rooms_join", role, mxid}` (mxid is not a secret — safe to log). NEVER log the admin access_token.

    Create `registry-rooms.test.ts` covering Tests 1-8 using mocked matrix-admin-client + in-memory settings table + mocked DatabaseSaveTrigger + mocked getMatrixAdminCreds.
  </action>
  <verify>
    <automated>npx vitest run src/backend/relay-sessions/registry-rooms.test.ts</automated>
    <human-check>grep -c 'export async function ensureRegistryRoomsExist' src/backend/relay-sessions/registry-rooms.ts must return 1; grep -c 'export async function joinAgentToAgentsRegistry' src/backend/relay-sessions/registry-rooms.ts must return 1; grep -c 'export async function joinHumanToHumansRegistry' src/backend/relay-sessions/registry-rooms.ts must return 1; grep -c 'addAdminRoom' src/backend/relay-sessions/registry-rooms.ts must return >= 2 (called once per registry room per D-13); grep -c 'phase-89-registry-rooms-ensure' src/backend/relay-sessions/registry-rooms.ts must return >= 1</human-check>
  </verify>
  <done>
    Module exports the 5 primitives with locked signatures. ensureRegistryRoomsExist is idempotent (no-op happy path when both settings present; creates + persists + ignore-lists on first boot). Both join hooks return discriminated unions matching the matrix-admin-client convention. Tests 1-8 pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire join hooks into POST /identities/birth (runRelayMintAndWrite) AND POST /users/create</name>
  <files>src/backend/database/routes/identity-birth-orchestrator.ts, src/backend/database/routes/identity-birth-orchestrator.test.ts, src/backend/database/routes/users.ts, src/backend/database/routes/users.test.ts</files>
  <read_first>
    - src/backend/database/routes/identity-birth-orchestrator.ts (READ ONCE around L645-841 — runRelayMintAndWrite Step 6 body where the mint + login-as-user land; the join hook fires after mintResult.ok === true is confirmed, BEFORE Step 7 relay.json body build, per D-11 post-mint semantics)
    - src/backend/database/routes/users.ts (READ ONCE around L155-345 — POST /users/create mint + INSERT block; the join hook fires after `mintResult.ok` is confirmed, before or during the DB INSERT. Best-effort semantics — a failed join does NOT fail the create, matches D-12's tolerance for backfill covering gaps)
    - src/backend/relay-sessions/registry-rooms.ts (already-created — refer to joinAgentToAgentsRegistry / joinHumanToHumansRegistry signatures)
    - .planning/phases/89-relay-mediated-group-conversations-sub-slice-b-session-model/89-CONTEXT.md (D-11 — Skynet-side hook, NOT via the id-skill self-register block; D-12 backfill covers gaps so per-account join failure is best-effort)
  </read_first>
  <behavior>
    - Test 1 (orchestrator): runRelayMintAndWrite Step 6 fires joinAgentToAgentsRegistry(mxid) AFTER mintResult.ok === true and AFTER loginResult.ok === true, BEFORE Step 7 (relay.json body build).
    - Test 2 (orchestrator): If joinAgentToAgentsRegistry returns { ok: false }, Step 6 does NOT throw — logs a warning and proceeds to Step 7 (best-effort per D-12 rationale — backfill is the safety net).
    - Test 3 (orchestrator): If joinAgentToAgentsRegistry throws (unexpected error), Step 6 catches, logs a warning, proceeds to Step 7 (defense-in-depth).
    - Test 4 (users.ts create): POST /users/create fires joinHumanToHumansRegistry(mintedMxid) AFTER createOrUpdateUser succeeds; failed join logs a warning and does NOT return 500 (best-effort).
    - Test 5 (users.ts create): joinHumanToHumansRegistry is NOT called on the mint-failure path (mintResult.ok === false already returns 500 before the hook site).
    - Test 6 (both): existing scoped tests remain green — no regressions in mint / login / relay.json write / avatar / INSERT.
  </behavior>
  <action>
    **In identity-birth-orchestrator.ts runRelayMintAndWrite Step 6** (around L720-783 — inside the `await runStep(6, async () => { ... })` block, AFTER the `mintedAccessToken = loginResult.accessToken` line, BEFORE the closing `});`):

    Insert best-effort registry-room join:
    ```
    (No fenced code in action — see: fire await joinAgentToAgentsRegistry(mxid); on non-ok, log a warning via databaseLogger with keys { operation: "identity_birth_registry_join_failed", mxid, status, error }; on thrown, catch and log { operation: "identity_birth_registry_join_threw", mxid, error }; either way proceed — do NOT throw, do NOT modify mintResult/loginResult state.)
    ```
    Import: `joinAgentToAgentsRegistry` from `../../relay-sessions/registry-rooms.js`. Add to the top-level imports block matching the ESM `.js` extension convention used throughout the file.

    Rationale docblock (place immediately above the new call): "Phase 89 D-11 hook. Best-effort per D-12: backfill covers gaps. A failed join does NOT fail the birth — the observation loop's classification will fall back to 'unknown foreign account' until the next backfill run or manual admin trigger corrects it."

    **In identity-birth-orchestrator.test.ts**: Add 3 tests (Tests 1-3) using the existing dep-injection mock pattern (BirthDeps) — you'll need to add a new dep `joinAgentToAgentsRegistry` to BirthDeps OR call the registry-rooms module directly and mock the module via vi.mock. Follow whichever pattern the existing tests use for calls to non-BirthDeps modules (e.g. how DatabaseSaveTrigger or databaseLogger is handled in the tests).

    Note: if you add joinAgentToAgentsRegistry to BirthDeps, wire the real function in identity-birth.ts's deps assembly block (around L475+); the test pattern will match how matrixCreateOrUpdateUser is wired.

    **In users.ts POST /users/create** (around L280-330 — AFTER the `db.$client.transaction(...)` block that INSERTs the user row succeeds, BEFORE the `DatabaseSaveTrigger.forceSave("phase-85-user-avatar-create")` call at L334):

    Insert best-effort registry-room join:
    ```
    (No fenced code in action — see: fire await joinHumanToHumansRegistry(mintedMxid); on non-ok, log a warning via authLogger with keys { operation: "user_create_registry_join_failed", userId: id, mxid: mintedMxid, status, error }; on thrown, catch and log { operation: "user_create_registry_join_threw", userId: id, mxid: mintedMxid, error }; either way proceed to the forceSave line and the success response.)
    ```
    Import: `joinHumanToHumansRegistry` from `../../relay-sessions/registry-rooms.js`.

    Rationale docblock: "Phase 89 D-11 hook. Best-effort per D-12."

    **In users.test.ts**: Add 2 tests (Tests 4-5) using the existing mock pattern for matrix-admin-client (Phase 88 already established this — check the Phase 88 Plan 03 mocks used in the create-flow tests). Mock joinHumanToHumansRegistry via vi.mock of the registry-rooms module.
  </action>
  <verify>
    <automated>npx vitest run src/backend/database/routes/identity-birth-orchestrator.test.ts src/backend/database/routes/users.test.ts</automated>
    <human-check>grep -c 'joinAgentToAgentsRegistry' src/backend/database/routes/identity-birth-orchestrator.ts must return >= 1; grep -c 'joinHumanToHumansRegistry' src/backend/database/routes/users.ts must return >= 1; grep -c 'identity_birth_registry_join_failed\|identity_birth_registry_join_threw' src/backend/database/routes/identity-birth-orchestrator.ts must return >= 2; grep -c 'user_create_registry_join_failed\|user_create_registry_join_threw' src/backend/database/routes/users.ts must return >= 2</human-check>
  </verify>
  <done>
    Both mint sites fire the registry-room join post-mint. Failures are best-effort (log-and-proceed). Tests 1-6 pass. No regressions in existing create-flow or birth-flow tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: One-time backfill module + boot-time gated invocation</name>
  <files>src/backend/relay-sessions/registry-rooms-backfill.ts, src/backend/relay-sessions/registry-rooms-backfill.test.ts</files>
  <read_first>
    - src/backend/relay-sessions/registry-rooms.ts (already-created — refer to join primitives + settings-key accessors)
    - src/backend/database/db/index.ts (already-read settings-table pattern; the `has_backfilled_registry_rooms` gate is the same shape as allow_registration / guac_enabled)
    - src/backend/fleet-status/ssh-poll-orchestrator.ts (READ ONCE — the OrchestratorDeps interface declares `listIdentityHostingHosts()` at ~L77 as a top-level method; grep the file for its call sites to see the accessor pattern. This is your source for host enumeration. Then look at how identities-per-host are surfaced elsewhere. If no in-memory identity roster surface is directly importable from a top-level module, the backfill can enumerate identities from the source-of-truth on-disk: SSH into each host and list `~/.claude/identities/*/` folders — but that violates D-09's "no disk-based check" rationale so DO NOT use that path for the OBSERVATION LOOP. Preferred: use whatever module exposes the fleet-status roster; if none exists as an importable top-level function, add one — narrow single-purpose `listAllIdentitiesAcrossHosts(): Promise<Array<{ hostId, identityName, mxid?: string }>>` — but this is a big surface change. INSTEAD: for backfill of agents, enumerate from the identity-artifact reader's on-host list AT BACKFILL TIME ONLY (backfill runs once, at boot, so per-host SSH cost is bounded). This is a one-shot operational script, not a live-classification path — the D-09 "no disk-based check" rationale applies to the observation loop (which runs every 10s and needs host-outage-tolerance), not to a boot-time backfill.)
    - .planning/phases/89-relay-mediated-group-conversations-sub-slice-b-session-model/89-CONTEXT.md (D-12 — idempotent, safe to re-run, enumerates from fleet-status roster for agents + users table for humans)
  </read_first>
  <behavior>
    - Test 1: `runRegistryRoomsBackfill()` reads the settings row `has_backfilled_registry_rooms`. If value === "true", returns `{ ok: true, skipped: true }` immediately without enumerating.
    - Test 2: When has_backfilled is absent/false, enumerates humans from `SELECT mxid FROM users WHERE mxid IS NOT NULL AND mxid != ''` and calls joinHumanToHumansRegistry for each. Idempotent joins per D-12 — a mxid already in the room returns ok via the Synapse admin API (joinRoom is idempotent).
    - Test 3: When has_backfilled is absent/false, enumerates agents via a fleet-status roster accessor (extract or use existing). For each agent mxid found, calls joinAgentToAgentsRegistry. Per-agent join failure logs a warning and continues (best-effort per D-12).
    - Test 4: After successful backfill, writes `INSERT OR REPLACE INTO settings (key, value) VALUES ('has_backfilled_registry_rooms', 'true')` and fires DatabaseSaveTrigger.forceSave("phase-89-backfill-complete").
    - Test 5: If backfill is invoked while another instance is mid-flight (concurrent boot — rare but possible), the second call is a no-op via the settings gate (subject to a tiny race window on the first-run; acceptable per D-12 idempotency contract).
    - Test 6: Structured logging at boundaries: entry ({ operation: "registry_backfill_start" }), per-role enumeration count ({ operation: "registry_backfill_enumerated", role, count }), per-account failure ({ operation: "registry_backfill_join_failed", role, mxid, status, error }), completion ({ operation: "registry_backfill_complete", agentsAttempted, humansAttempted, agentsFailed, humansFailed }).
  </behavior>
  <action>
    Create `src/backend/relay-sessions/registry-rooms-backfill.ts` exporting `runRegistryRoomsBackfill(): Promise<{ ok: true; skipped?: boolean; agentsAttempted?: number; humansAttempted?: number; agentsFailed?: number; humansFailed?: number } | { ok: false; reason: string }>`.

    Concrete flow:
    1. Read settings row `has_backfilled_registry_rooms`. If value === "true" → return `{ ok: true, skipped: true }` immediately (D-12 idempotent gate — safe to re-run).
    2. Check ensureRegistryRoomsExist() first (defense-in-depth — backfill assumes rooms exist; if creds missing → return `{ ok: false, reason: "creds_missing" }`).
    3. Enumerate humans: `db.$client.prepare("SELECT mxid FROM users WHERE mxid IS NOT NULL AND mxid != ''").all()` — filter to non-null non-empty. For each: `await joinHumanToHumansRegistry(mxid)` — increment humansAttempted; on non-ok increment humansFailed and log a warning.
    4. Enumerate agents: use fleet-status roster if a top-level accessor is available. The concrete accessor path is planner-flex — options in preference order:
       (a) if `ssh-poll-orchestrator.ts` exposes a live per-host identity list via a top-level export (grep for `export.*identit` or `getRoster` in fleet-status/*.ts to find), consume that.
       (b) if no such export exists, add a narrow single-purpose helper to identity-artifact-reader.ts or a new small module `src/backend/relay-sessions/enumerate-agent-mxids.ts` that, at backfill-time only, reads the on-disk `~/.claude/identities/<name>/relay.json` mxid via SSH exec against each fleet host from `listIdentityHostingHosts()`. This is acceptable per D-09's scope rationale — the "no disk-based check" rule targets the observation loop (which runs every 10s and must tolerate host outages), not a one-shot boot-time backfill (which can accept "host was offline during backfill → gets rediscovered on next backfill re-run").
       For each mxid found: `await joinAgentToAgentsRegistry(mxid)` — increment agentsAttempted; on non-ok increment agentsFailed and log a warning.
    5. Write `INSERT OR REPLACE INTO settings (key, value) VALUES ('has_backfilled_registry_rooms', 'true')` — flip the gate ON. Await `DatabaseSaveTrigger.forceSave("phase-89-backfill-complete")` in try/catch that logs and swallows.
    6. Return the tally.

    Rationale docblock at top of module: "Phase 89 Plan 02 Task 4 — D-12 one-time backfill. Enumerates existing agents (fleet-status roster or on-disk mxid read at backfill-time only — acceptable one-shot cost) + existing humans (users table). Idempotent: gated on settings row `has_backfilled_registry_rooms`. Per-account join failures are best-effort (log-and-continue). Safe to re-run by clearing the settings row (operational escape hatch — no admin UI in this slice per D-16)."

    Boot-time invocation: NOT in this task (Plan 03 owns the observation-loop bootstrap that fires backfill before the first tick; keep this module a pure importable function so the observation-loop bootstrap can call it in sequence).

    Create `registry-rooms-backfill.test.ts` covering Tests 1-6 with mocked matrix-admin-client + mocked registry-rooms module + in-memory settings + users table with 2-3 mxid rows. For Test 3's agent enumeration mocking, wrap the agent-enumeration source (whichever option you chose) so it's mockable per-test.
  </action>
  <verify>
    <automated>npx vitest run src/backend/relay-sessions/registry-rooms-backfill.test.ts</automated>
    <human-check>grep -c 'has_backfilled_registry_rooms' src/backend/relay-sessions/registry-rooms-backfill.ts must return >= 2 (read gate + write gate); grep -c 'joinHumanToHumansRegistry\|joinAgentToAgentsRegistry' src/backend/relay-sessions/registry-rooms-backfill.ts must return >= 2; grep -c 'phase-89-backfill-complete' src/backend/relay-sessions/registry-rooms-backfill.ts must return >= 1; grep -c 'operation: "registry_backfill_' src/backend/relay-sessions/registry-rooms-backfill.ts must return >= 3</human-check>
  </verify>
  <done>
    Backfill module exports runRegistryRoomsBackfill with the D-12 idempotent-gate contract. Human enumeration via users table; agent enumeration via chosen accessor (documented in module docblock). Per-account failure logs + continues. Gate written + forceSave paired on success. Tests 1-6 pass. Boot-time invocation deferred to Plan 03's observation-loop bootstrap.
  </done>
</task>

</tasks>

<verification>
Registry rooms exist idempotently on boot. Both are in the admin_rooms ignore-list. Join hooks fire post-mint at both create sites (best-effort). Backfill enumerates existing accounts on first boot and gates itself on a settings row.
</verification>

<success_criteria>
- createRoom primitive follows the existing matrix-admin-client conventions.
- registry-rooms.ts exports ensure/join/getter primitives; ensure is idempotent; both join hooks return discriminated unions.
- Both mint sites (runRelayMintAndWrite + POST /users/create) call the appropriate join hook post-mint with log-and-proceed semantics.
- Backfill module gated on settings row; idempotent; per-account log-and-continue.
- All scoped tests pass.
</success_criteria>

<output>
Create `.planning/phases/89-relay-mediated-group-conversations-sub-slice-b-session-model/89-02-SUMMARY.md` when done.
</output>
