---
phase: 89-relay-mediated-group-conversations-sub-slice-b-session-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/database/db/index.ts
  - src/backend/database/db/schema.ts
  - src/backend/relay-sessions/relay-room-sessions-store.ts
  - src/backend/relay-sessions/relay-room-sessions-store.test.ts
  - src/backend/relay-sessions/admin-rooms-ignore-list.ts
  - src/backend/relay-sessions/admin-rooms-ignore-list.test.ts
autonomous: true
requirements: [D-01, D-02, D-03, D-14, D-16]
must_haves:
  truths:
    - "A new table relay_room_sessions exists in both raw SQLite (db/index.ts) and Drizzle mirror (db/schema.ts) per D-01/D-02 (two peer paths — NOT a discriminator on existing sessions table)."
    - "The uniqueness constraint on (user_id, room_id) exists (NOT scoped to active-only) per D-02 so re-invite reactivation reuses the same row."
    - "materializeRelayRoomSession(userId, roomId, roomTitle) is idempotent: repeat calls do not create duplicate rows and do not throw per D-14 (schema is the coordinator)."
    - "markRelayRoomSessionInactive(userId, roomId) transitions state from 'active' to 'inactive' preserving the row (NOT DELETE) per D-03."
    - "listActiveRelayRoomSessions(userId) returns only rows where state = 'active'."
    - "refreshRelayRoomLastActivity(userId, roomId, isoTimestamp) updates last_activity_at per D-05."
    - "Every INSERT/UPDATE in the store is followed by DatabaseSaveTrigger.forceSave('phase-89-...') wrapped in try/catch that logs a warning on failure (crown-jewel invariant)."
    - "A separate admin_rooms table (or settings row containing JSON array — planner's call, per D-16 fits either) is created and exposes isAdminRoom(roomId) + addAdminRoom(roomId) primitives with matching forceSave discipline."
  artifacts:
    - path: "src/backend/database/db/index.ts"
      provides: "CREATE TABLE IF NOT EXISTS relay_room_sessions + admin_rooms in the raw-SQL init block"
      contains: "CREATE TABLE IF NOT EXISTS relay_room_sessions"
    - path: "src/backend/database/db/schema.ts"
      provides: "Drizzle mirror table relayRoomSessions + adminRooms"
      contains: "export const relayRoomSessions"
    - path: "src/backend/relay-sessions/relay-room-sessions-store.ts"
      provides: "materializeRelayRoomSession, markRelayRoomSessionInactive, listActiveRelayRoomSessions, refreshRelayRoomLastActivity"
      exports: ["materializeRelayRoomSession", "markRelayRoomSessionInactive", "listActiveRelayRoomSessions", "refreshRelayRoomLastActivity", "reactivateRelayRoomSession"]
    - path: "src/backend/relay-sessions/admin-rooms-ignore-list.ts"
      provides: "isAdminRoom, addAdminRoom"
      exports: ["isAdminRoom", "addAdminRoom", "listAdminRooms"]
    - path: "src/backend/relay-sessions/relay-room-sessions-store.test.ts"
      provides: "Scoped tests covering INSERT-OR-IGNORE idempotency + state transitions + reactivate-existing-row"
    - path: "src/backend/relay-sessions/admin-rooms-ignore-list.test.ts"
      provides: "Scoped tests covering add/query idempotency"
  key_links:
    - from: "src/backend/relay-sessions/relay-room-sessions-store.ts"
      to: "src/backend/database/db/index.ts (db.$client)"
      via: "raw better-sqlite3 prepare + run"
      pattern: "db\\.\\$client\\.prepare"
    - from: "src/backend/relay-sessions/relay-room-sessions-store.ts"
      to: "src/backend/utils/database-save-trigger.ts::DatabaseSaveTrigger.forceSave"
      via: "post-write persistence"
      pattern: "forceSave\\(\"phase-89-"
---

<objective>
Land the storage substrate for Phase 89: a new `relay_room_sessions` table (peer path to derived harness sessions per D-01), a small `admin_rooms` ignore-list surface (D-16), and the primitive functions the observation loop (Plan 03) + slice C's create-room flow will both call. Row shape is locked exactly per D-02. Uniqueness on `(user_id, room_id)` is the coordinator (D-14). Reactivation on re-invite reuses the same row (D-03). Every write is paired with `DatabaseSaveTrigger.forceSave` (crown-jewel invariant).

Purpose: All downstream plans (89-02 registry-rooms/backfill, 89-03 observation loop, 89-04 merge) call the primitives created here. No behavior surfaces to users in this plan — pure infrastructure.

Output: Two new tables (raw SQL + Drizzle mirror kept in sync), a new module directory `src/backend/relay-sessions/`, two primitive modules with scoped unit tests, all writes forceSave-paired.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/89-relay-mediated-group-conversations-sub-slice-b-session-model/89-CONTEXT.md
@.planning/shapes/shape-relay-session-model-generalization.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add relay_room_sessions + admin_rooms tables (raw SQL + Drizzle mirror)</name>
  <files>src/backend/database/db/index.ts, src/backend/database/db/schema.ts</files>
  <read_first>
    - src/backend/database/db/index.ts (READ ONCE — extract in a single pass: L170-197 existing sessions/trusted_devices raw-SQL init block; L1280-1360 incremental CREATE TABLE IF NOT EXISTS migration block pattern used by sessions/trusted_devices/network_topology; L907-909 phase-75 matrix_admin_creds persistence-block comment for the "labeled forceSave immediately after new-table init" pattern)
    - src/backend/database/db/schema.ts (READ ONCE — extract in a single pass: L1-101 users/settings/sessions/trustedDevices Drizzle mirror pattern; L700-717 matrixAdminCreds mirror as reference for a phase-added table that mirrors a raw-SQL block)
    - .planning/phases/89-relay-mediated-group-conversations-sub-slice-b-session-model/89-CONTEXT.md (D-02 row-shape lock, D-16 ignore-list storage flex)
  </read_first>
  <behavior>
    - Test 1: `SELECT sql FROM sqlite_master WHERE name = 'relay_room_sessions'` returned SQL contains exactly the columns from D-02: `id TEXT PRIMARY KEY`, `user_id TEXT NOT NULL`, `room_id TEXT NOT NULL`, `room_title TEXT`, `state TEXT NOT NULL`, `last_activity_at TEXT`, `created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`, `updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`.
    - Test 2: A separate `CREATE UNIQUE INDEX IF NOT EXISTS relay_room_sessions_user_room_uidx ON relay_room_sessions(user_id, room_id)` exists — NOT scoped by a WHERE clause on state (per D-02 rationale about re-invite reactivation).
    - Test 3: `SELECT sql FROM sqlite_master WHERE name = 'admin_rooms'` returned SQL contains `room_id TEXT PRIMARY KEY` and `created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`.
    - Test 4: Both tables survive process restart in the existing db init sequence — `SELECT id FROM relay_room_sessions LIMIT 1` and `SELECT room_id FROM admin_rooms LIMIT 1` do NOT throw after re-invoking initializeDb.
    - Test 5: Drizzle mirror exports `relayRoomSessions` and `adminRooms` with column names snake_case matching the raw SQL (per existing mirror discipline).
  </behavior>
  <action>
    In db/index.ts: add two new CREATE TABLE IF NOT EXISTS blocks in the same style as the L170-197 initial-block AND paired existence-check migration blocks around L1280+ (both sites — belt-and-suspenders same as existing tables). Table name `relay_room_sessions` (planner's call per D-16 Claude's Discretion; follows the `snake_case_plural_of_entity` convention already used by `sessions`, `trusted_devices`, `matrix_admin_creds`). Column list from D-02 verbatim (no extras, no reorders, no type changes). Add `CREATE UNIQUE INDEX IF NOT EXISTS relay_room_sessions_user_room_uidx ON relay_room_sessions(user_id, room_id)` immediately after the CREATE TABLE (unscoped — do not add `WHERE state = 'active'` per D-02 reactivation rationale). Foreign key `FOREIGN KEY (user_id) REFERENCES users (id) ON DELETE CASCADE` matches the sessions/trusted_devices pattern.

    Also add table `admin_rooms` (columns: `room_id TEXT PRIMARY KEY`, `created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`) in both the initial block and the incremental migration block. Chosen shape: dedicated small table (D-16 planner-choice — a table is easier to inspect than a JSON-in-settings row for future ops, and the storage cost difference is negligible for the ~2 rows expected).

    Immediately after the incremental migration block runs successfully, call `await DatabaseSaveTrigger.forceSave("phase-89-relay-sessions-schema-init")` wrapped in try/catch that logs a warning on failure (matches the L907-909 phase-75 matrix_admin_creds pattern). Do NOT propagate save errors.

    In schema.ts: add Drizzle mirrors after `matrixAdminCreds`:
      - `relayRoomSessions` exports the eight D-02 columns using the same `text(...).notNull()`, `text(...).default(sql\`CURRENT_TIMESTAMP\`)`, and `.references(() => users.id, { onDelete: "cascade" })` shapes as the existing `sessions` mirror. State column: `text("state").notNull()` — no enum constraint at Drizzle level (SQLite has no enums; the store module owns the values).
      - `adminRooms` mirror with matching columns.
    Copy the docblock convention from `matrixAdminCreds` (comment referencing the phase that added it).
  </action>
  <verify>
    <automated>npx vitest run --related src/backend/database/db/index.ts src/backend/database/db/schema.ts</automated>
    <human-check>grep -c 'CREATE TABLE IF NOT EXISTS relay_room_sessions' src/backend/database/db/index.ts must return 2 (initial + migration); grep -c 'CREATE UNIQUE INDEX IF NOT EXISTS relay_room_sessions_user_room_uidx' src/backend/database/db/index.ts must return 2; grep -c 'export const relayRoomSessions' src/backend/database/db/schema.ts must return 1; grep -c 'export const adminRooms' src/backend/database/db/schema.ts must return 1; grep -c 'phase-89-relay-sessions-schema-init' src/backend/database/db/index.ts must return >= 1</human-check>
  </verify>
  <done>
    Two new tables (relay_room_sessions + admin_rooms) exist in raw SQLite init AND in Drizzle schema mirror. Uniqueness index on (user_id, room_id) is unscoped. Table init is paired with a labeled forceSave call wrapped per the phase-75 pattern. Existing scoped tests remain green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Relay-room-sessions store module with idempotent primitives</name>
  <files>src/backend/relay-sessions/relay-room-sessions-store.ts, src/backend/relay-sessions/relay-room-sessions-store.test.ts</files>
  <read_first>
    - src/backend/database/db/index.ts (already-read table shape — refer to your Task 1 additions; do NOT re-read the whole file)
    - src/backend/fleet-status/identity-send-log-store.ts (READ ONCE — reference for a Phase-85 store module with max-wins semantics, DB-local writes, and the DatabaseSaveTrigger.forceSave pattern; use as the structural template for shape, log messages, and test conventions)
    - src/backend/utils/database-save-trigger.ts (READ ONCE — confirm the forceSave signature and error semantics)
    - src/backend/database/routes/host-autostart-routes.ts (READ ONCE around L173-181 — the canonical labeled-forceSave-with-try/catch pattern)
    - .planning/phases/89-relay-mediated-group-conversations-sub-slice-b-session-model/89-CONTEXT.md (D-02, D-03, D-14)
  </read_first>
  <behavior>
    - Test 1: `materializeRelayRoomSession("user-A", "!room1:server", "Room 1")` inserts a row with state='active', a fresh UUID id, and last_activity_at defaulting to null (until Task's refresh primitive is called).
    - Test 2: Calling `materializeRelayRoomSession("user-A", "!room1:server", "Room 1")` twice does NOT create a duplicate row and does NOT throw — the second call is a no-op (INSERT ... ON CONFLICT(user_id, room_id) DO NOTHING).
    - Test 3: `markRelayRoomSessionInactive("user-A", "!room1:server")` updates the state column to 'inactive' and updates updated_at; the row is NOT deleted (SELECT COUNT still returns 1).
    - Test 4: After Test 3, `listActiveRelayRoomSessions("user-A")` returns an empty array (state filter kicks in).
    - Test 5: `reactivateRelayRoomSession("user-A", "!room1:server")` updates the inactive row back to 'active' and refreshes updated_at (per D-03 — same row flips, not a new insert).
    - Test 6: `refreshRelayRoomLastActivity("user-A", "!room1:server", "2026-09-08T12:00:00Z")` updates last_activity_at to that value.
    - Test 7: Every mutating primitive calls DatabaseSaveTrigger.forceSave with a label prefixed "phase-89-" — verified by spying on the mocked forceSave and asserting it was called (per crown-jewel invariant).
    - Test 8: If forceSave rejects, the primitive still returns successfully — the caller does NOT see a throw (log-and-swallow per host-autostart-routes.ts:173-181 pattern).
  </behavior>
  <action>
    Create module `src/backend/relay-sessions/relay-room-sessions-store.ts` exporting these primitives (concrete signatures — do NOT invent alternates):
      - `materializeRelayRoomSession(userId: string, roomId: string, roomTitle: string | null): Promise<void>` — generates a UUID id (use crypto.randomUUID matching the fleet convention used elsewhere; check identity-send-log-store.ts for the local idiom), runs `INSERT INTO relay_room_sessions (id, user_id, room_id, room_title, state) VALUES (?, ?, ?, ?, 'active') ON CONFLICT(user_id, room_id) DO NOTHING` via db.$client.prepare. Post-write: `await DatabaseSaveTrigger.forceSave("phase-89-relay-session-materialize")` in try/catch that logs a warning via databaseLogger and swallows.
      - `markRelayRoomSessionInactive(userId: string, roomId: string): Promise<void>` — runs `UPDATE relay_room_sessions SET state = 'inactive', updated_at = CURRENT_TIMESTAMP WHERE user_id = ? AND room_id = ? AND state = 'active'`. Post-write forceSave with label "phase-89-relay-session-inactive".
      - `reactivateRelayRoomSession(userId: string, roomId: string): Promise<void>` — runs `UPDATE relay_room_sessions SET state = 'active', updated_at = CURRENT_TIMESTAMP WHERE user_id = ? AND room_id = ? AND state = 'inactive'`. Post-write forceSave with label "phase-89-relay-session-reactivate". Rationale: D-03 keeps the row and flips it on re-invite.
      - `listActiveRelayRoomSessions(userId: string): Promise<Array<{ id: string; roomId: string; roomTitle: string | null; lastActivityAt: string | null; createdAt: string; updatedAt: string }>>` — runs `SELECT id, room_id AS roomId, room_title AS roomTitle, last_activity_at AS lastActivityAt, created_at AS createdAt, updated_at AS updatedAt FROM relay_room_sessions WHERE user_id = ? AND state = 'active' ORDER BY updated_at DESC`. Read-only (no forceSave).
      - `refreshRelayRoomLastActivity(userId: string, roomId: string, isoTs: string): Promise<void>` — runs `UPDATE relay_room_sessions SET last_activity_at = ?, updated_at = CURRENT_TIMESTAMP WHERE user_id = ? AND room_id = ?`. Post-write forceSave with label "phase-89-relay-session-refresh-activity".

    Structured logging at boundaries per fleet rule: log via databaseLogger at each primitive entry with keys `{operation, userId, roomId}`. Do NOT log the entire row shape (no useful signal, just noise).

    Create `src/backend/relay-sessions/relay-room-sessions-store.test.ts` covering Tests 1-8. Use an in-memory better-sqlite3 with the exact CREATE TABLE + CREATE UNIQUE INDEX DDL from Task 1 (import shared test-DB scaffolding if identity-send-log-store.test.ts has one; else inline the DDL string). Mock DatabaseSaveTrigger.forceSave with vi.mock to assert calls and simulate rejection for Test 8.
  </action>
  <verify>
    <automated>npx vitest run src/backend/relay-sessions/relay-room-sessions-store.test.ts</automated>
    <human-check>grep -c 'ON CONFLICT(user_id, room_id) DO NOTHING' src/backend/relay-sessions/relay-room-sessions-store.ts must return 1; grep -v '^ *[/\*]' src/backend/relay-sessions/relay-room-sessions-store.ts | grep -c 'forceSave.*"phase-89-' must return >= 4 (four mutating primitives); grep -c "state = 'active'" src/backend/relay-sessions/relay-room-sessions-store.ts must return >= 2 (listActive filter + reactivate WHERE)</human-check>
  </verify>
  <done>
    Store module exports all 5 primitives with locked signatures. Every mutating primitive is INSERT-OR-IGNORE / conditional-UPDATE idempotent. Every mutating primitive is paired with a labeled forceSave in try/catch that logs and swallows. Tests 1-8 pass. State transitions preserve the row per D-03.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Admin-rooms ignore-list module</name>
  <files>src/backend/relay-sessions/admin-rooms-ignore-list.ts, src/backend/relay-sessions/admin-rooms-ignore-list.test.ts</files>
  <read_first>
    - src/backend/relay-sessions/relay-room-sessions-store.ts (already-created in Task 2 — reference for module conventions; do NOT re-read the whole store)
    - src/backend/database/db/index.ts (already-read admin_rooms table shape from Task 1; do NOT re-read)
    - .planning/phases/89-relay-mediated-group-conversations-sub-slice-b-session-model/89-CONTEXT.md (D-13, D-16 — Skynet-instance-owned, internal, general-purpose bucket)
  </read_first>
  <behavior>
    - Test 1: `isAdminRoom("!agents-registry:server")` returns false when the table is empty.
    - Test 2: After `addAdminRoom("!agents-registry:server")`, `isAdminRoom("!agents-registry:server")` returns true.
    - Test 3: Calling `addAdminRoom("!agents-registry:server")` twice does NOT throw and does NOT create a duplicate row (INSERT OR IGNORE on the PRIMARY KEY).
    - Test 4: `listAdminRooms()` returns an array of room IDs (all admin rooms in the table, unsorted OK — used only for internal auditing).
    - Test 5: Every mutating primitive calls DatabaseSaveTrigger.forceSave with a label prefixed "phase-89-" — verified via mock.
  </behavior>
  <action>
    Create `src/backend/relay-sessions/admin-rooms-ignore-list.ts` exporting:
      - `isAdminRoom(roomId: string): Promise<boolean>` — runs `SELECT 1 FROM admin_rooms WHERE room_id = ? LIMIT 1` via db.$client.prepare(...).get(...); returns boolean based on presence. NO forceSave (read-only).
      - `addAdminRoom(roomId: string): Promise<void>` — runs `INSERT OR IGNORE INTO admin_rooms (room_id) VALUES (?)`. Post-write `await DatabaseSaveTrigger.forceSave("phase-89-admin-room-add")` in try/catch that logs and swallows.
      - `listAdminRooms(): Promise<string[]>` — runs `SELECT room_id FROM admin_rooms` and returns the array of room_id values.

    Structured logging: log at addAdminRoom entry with `{operation: "admin_rooms_add", roomId}`.

    Create `admin-rooms-ignore-list.test.ts` covering Tests 1-5 with the same in-memory-DB scaffolding pattern from Task 2's test file.
  </action>
  <verify>
    <automated>npx vitest run src/backend/relay-sessions/admin-rooms-ignore-list.test.ts</automated>
    <human-check>grep -c 'INSERT OR IGNORE INTO admin_rooms' src/backend/relay-sessions/admin-rooms-ignore-list.ts must return 1; grep -c 'phase-89-admin-room-add' src/backend/relay-sessions/admin-rooms-ignore-list.ts must return >= 1; grep -c 'export function isAdminRoom\|export async function isAdminRoom' src/backend/relay-sessions/admin-rooms-ignore-list.ts must return 1</human-check>
  </verify>
  <done>
    Ignore-list module exports isAdminRoom + addAdminRoom + listAdminRooms. Idempotent add (PRIMARY KEY INSERT OR IGNORE). forceSave paired on the mutating primitive. Tests 1-5 pass.
  </done>
</task>

</tasks>

<verification>
Schema present in both raw SQL and Drizzle mirror. All primitives are idempotent (uniqueness constraint is the coordinator per D-14). All writes forceSave-paired per crown-jewel invariant. Scoped tests green.
</verification>

<success_criteria>
- Two new tables landed in db/index.ts (init + migration blocks) + schema.ts mirror.
- Unique index on (user_id, room_id) exists (unscoped).
- Store module exports 5 primitives with locked signatures.
- Ignore-list module exports 3 primitives.
- Every mutating primitive paired with labeled `phase-89-*` forceSave in try/catch.
- Scoped vitest passes for both new test files.
</success_criteria>

<output>
Create `.planning/phases/89-relay-mediated-group-conversations-sub-slice-b-session-model/89-01-SUMMARY.md` when done.
</output>
