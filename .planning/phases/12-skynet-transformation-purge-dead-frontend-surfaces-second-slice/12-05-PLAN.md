---
phase: 12-skynet-transformation-purge-dead-frontend-surfaces-second-slice
plan: 05
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - src/ui/shell/Tab.tsx
autonomous: true
requirements: [PURGE-08]

must_haves:
  truths:
    - "src/ui/shell/Tab.tsx (the Termix visible tab bar chrome file) is deleted from disk"
    - "Invisible tab plumbing (mount/unmount, WebSocket lifecycle, focus routing) survives untouched — Tab.tsx has zero imports across src/, confirming it is standalone visible chrome"
    - "shell/TabContext.tsx STAYS (retained tab state provider; different file from Tab.tsx)"
    - "shell/CommandPalette.tsx, shell/SplitView.tsx, shell/tabUtils.tsx all STAY (retained AppShell dependencies)"
  artifacts: []
  key_links:
    - from: "src/ui/shell/TabContext.tsx"
      to: "retained tab state provider"
      via: "STAYS"
      pattern: "TabContext"
---

<objective>
Delete `src/ui/shell/Tab.tsx` — the Termix visible tab bar chrome file that Ashley does not see in Skynet. Phase 11 already unmounted every AppShell reference to the Termix top-strip tab UI; this file has zero remaining imports across `src/` (grep-verified at 12-01-STRIP-LIST Section F). The invisible tab plumbing lives in shell/TabContext.tsx, shell/tabUtils.tsx, and AppShell.tsx's tab machinery — all RETAINED.

Purpose: Delivers PURGE-08. Simplest deletion in Phase 12 — one file, zero import consumers, no test file, no cross-tree refactor needed.

Output: 1 atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-CONTEXT.md
@.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete src/ui/shell/Tab.tsx (Termix tab bar chrome)</name>
  <files>src/ui/shell/Tab.tsx</files>
  <read_first>
    - .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md § Section F (Tab.tsx enumeration + zero-import grep evidence)
    - src/ui/shell/Tab.tsx (verify 442 lines, contains `interface TabProps` + `export function Tab({...})` — the visible tab-strip renderer with click-handlers, X-close button, split icon, per-tab-type icon rendering)
    - src/ui/shell/TabContext.tsx (STAYS — this is DIFFERENT from Tab.tsx; provides the tab state machinery)
    - src/ui/shell/tabUtils.tsx (STAYS — the tab render dispatcher; no import of Tab.tsx)
  </read_first>
  <action>
    Pre-deletion grep gate:
    - `grep -rn 'from "@/shell/Tab.tsx"\|from "@/shell/Tab"\|from "./Tab.tsx"\|from "./Tab"' src/ --include="*.ts" --include="*.tsx"` → expected 0 non-comment hits
    - `grep -rn "import.*Tab\b" src/ui/shell/ --include="*.ts" --include="*.tsx" | grep -v "TabContext\|TabProvider\|useTabs\|TabType\|TabSpec\|Tab \}"` → verify no cross-shell reference to the Tab component (Tab.tsx export is `Tab`, but consumers of the WORD "Tab" through TabContext/TabProvider/etc. are retained module names — the grep filter excludes those)

    If any survivor exists, STOP and route back to planner — Section F of STRIP-LIST claims zero imports.

    Delete:
    ```
    git rm src/ui/shell/Tab.tsx
    ```

    Post-deletion verification:
    - `test ! -f src/ui/shell/Tab.tsx` — passes
    - `test -f src/ui/shell/TabContext.tsx` — passes (retained)
    - `test -f src/ui/shell/CommandPalette.tsx` — passes (retained)
    - `test -f src/ui/shell/SplitView.tsx` — passes (retained)
    - `test -f src/ui/shell/tabUtils.tsx` — passes (retained)
    - `npx tsc --noEmit` — exit 0
    - `npx vitest run` — at Phase 11 baseline (524/526)

    Commit message: `chore(shell): delete Tab.tsx — Termix visible tab bar chrome unused in Skynet (Phase 12 Plan 05 PURGE-08)`.
  </action>
  <verify>
    <automated>
    test ! -f src/ui/shell/Tab.tsx &&
    test -f src/ui/shell/TabContext.tsx &&
    test -f src/ui/shell/CommandPalette.tsx &&
    test -f src/ui/shell/SplitView.tsx &&
    test -f src/ui/shell/tabUtils.tsx &&
    test -f src/ui/sidebar/NewSessionDialog.tsx &&
    grep -rn 'from "@/shell/Tab.tsx"\|from "@/shell/Tab"\|from "./Tab.tsx"' src/ --include="*.ts" --include="*.tsx" | grep -v "^[^:]*:[[:space:]]*//" | wc -l | grep -qE "^0$" &&
    npx tsc --noEmit
    </automated>
  </verify>
  <done>
    src/ui/shell/Tab.tsx gone; TabContext + CommandPalette + SplitView + tabUtils PRESERVED; sidebar/NewSessionDialog PRESERVED; zero import survivors; tsc-clean; single atomic commit landed with prescribed message.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tab.tsx vs TabContext.tsx confusion | Two files with similar names in shell/. Tab.tsx (visible tab-strip renderer) dies. TabContext.tsx (tab state provider) STAYS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05-01 | Tampering | Accidentally delete TabContext.tsx | mitigate | Task's `<files>` block names ONLY `src/ui/shell/Tab.tsx`. Verify includes `test -f src/ui/shell/TabContext.tsx` PASS gate. |
| T-12-05-02 | Tampering | Break invisible tab plumbing | mitigate | Tab.tsx has zero cross-tree imports (STRIP-LIST Section F). Invisible plumbing (TabContext, TabProvider, useTabs, tabUtils dispatcher, AppShell tabs state) is in different files. Verify tsc-clean + vitest baseline. |
| T-12-05-03 | Tampering | features/keyboard/ accidentally deleted | mitigate | Task does NOT touch features/keyboard/. |
| T-12-05-04 | Tampering | sidebar/NewSessionDialog.tsx accidentally deleted | mitigate | Task does NOT touch sidebar/. Verify includes `test -f src/ui/sidebar/NewSessionDialog.tsx` PASS gate. |
| T-12-05-SC | Tampering | npm/pip/cargo installs | accept | Zero installs. |
</threat_model>

<verification>
- Tab.tsx deleted; other shell/ files preserved; sidebar/NewSessionDialog preserved; features/keyboard/ preserved
- Zero import survivors
- tsc-clean, vitest baseline
- 1 atomic commit landed
</verification>

<success_criteria>
- src/ui/shell/Tab.tsx gone
- shell/TabContext.tsx, CommandPalette.tsx, SplitView.tsx, tabUtils.tsx PRESERVED
- PURGE-08 delivered
</success_criteria>

<output>
Create `.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-05-SUMMARY.md` on completion.
</output>
