---
phase: 12-skynet-transformation-purge-dead-frontend-surfaces-second-slice
plan: 07
type: execute
wave: 5
depends_on: [12-01, 12-02, 12-03, 12-04, 12-05, 12-06]
files_modified:
  - .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-BUILD-VERIFY-LOG.md
  - .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-UAT-CHECKLIST.md
  - .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-PATCHES-MD-ENTRY.md
autonomous: true
requirements: [PURGE-06, PURGE-07, PURGE-08, PURGE-09, PURGE-10]

must_haves:
  truths:
    - "Full production build (npx tsc --noEmit + npx vitest run + npm run build) captured with exact byte counts, exit codes, and test counts at the Phase 12 tip"
    - "Every grep hygiene gate from 12-01-STRIP-LIST Section K is run at the phase boundary with observed vs expected values recorded"
    - "AppShell bundle-size delta vs Phase 11 tip (75.43 kB) is captured as the concrete purge-landed evidence"
    - "UAT checklist has explicit hash-fragment probes with both-outcomes-acceptable framing (per Phase 11 checker W-4 fix precedent)"
    - "UAT deploy runbook cites ~/.claude/identities/tina/deploy-runbook.md as authoritative (per Phase 11 checker B-2 fix precedent) and explicitly names the fork CLAUDE.md 15-min deadman line as stale"
    - "Patch #139 draft is paste-ready with multi-commit-under-one-pin format (patches #104, #105, #128, #138 precedents)"
    - "Zero source-tree modifications in this plan (docs-only)"
  artifacts:
    - path: ".planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-BUILD-VERIFY-LOG.md"
      provides: "Phase-boundary build verification log with all 12+ grep hygiene gates + bundle-size delta"
      contains: "tsc, vitest, npm run build, PURGE-06, PURGE-07, PURGE-08, PURGE-09, PURGE-10"
      min_lines: 150
    - path: ".planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-UAT-CHECKLIST.md"
      provides: "Ashley's post-deploy UAT walkthrough — desktop + mobile + cross-viewport regression + failure route-back table + deploy runbook"
      contains: "Desktop UAT, Mobile UAT, deploy-runbook.md, batch patches"
      min_lines: 200
    - path: ".planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-PATCHES-MD-ENTRY.md"
      provides: "Paste-ready patch #139 draft for Tina's termix-patches.md"
      contains: "patch #139, Skynet, purge, batches with subsequent"
      min_lines: 100
  key_links:
    - from: "12-BUILD-VERIFY-LOG.md"
      to: "Ashley's deploy handoff"
      via: "PASS verdict"
      pattern: "PURGE-06.*PASS"
    - from: "12-UAT-CHECKLIST.md § Post-UAT deploy runbook"
      to: "~/.claude/identities/tina/deploy-runbook.md"
      via: "authoritative citation"
      pattern: "deploy-runbook.md"
---

<objective>
Close Phase 12 with pure documentation: build-verify log capturing tsc + vitest + npm run build at the Phase 12 tip, UAT checklist for Ashley's post-deploy walkthrough, and patch #139 draft for Tina's termix-patches.md. Mirror Phase 11 Plan 04's format verbatim (which shipped patch #138). Zero source-tree modifications.

Purpose: Delivers the deploy-readiness state — Ashley walks the UAT checklist, Tina pastes patch #139 into termix-patches.md, both artifacts are batched per fleet-standing "batch patches into meaningful deploys" rule (Ashley 2026-07-23) with subsequent Phase 13 backend-route deletion.

Output: 3 documentation files + 1 SUMMARY, all committed atomically.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-CONTEXT.md
@.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md
@.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-04-SUMMARY.md
@.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-BUILD-VERIFY-LOG.md
@.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-UAT-CHECKLIST.md
@.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-PATCHES-MD-ENTRY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author 12-BUILD-VERIFY-LOG.md — full production build verification with grep hygiene gates + bundle-size delta</name>
  <files>.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-BUILD-VERIFY-LOG.md</files>
  <read_first>
    - .planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-BUILD-VERIFY-LOG.md (mirror format)
    - .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md § Section K (verification gates enumeration)
  </read_first>
  <action>
    Run all three verification commands from repo root and capture verbatim output:
    1. `npx tsc --noEmit` — record exit code + any diagnostics
    2. `npx vitest run` — record pass/fail counts + failure list (verify Phase 11 baseline preserved: 524/526 pass, 2 pre-existing ComposeBox failures)
    3. `npm run build` — record build time + AppShell chunk size (raw + gzip). Compare against Phase 11 tip AppShell = 75.43 kB (raw) / 20.49 kB (gzip). Compute Phase 12 delta: raw byte change + percent + gzip byte change + percent.

    Author `12-BUILD-VERIFY-LOG.md` with 5 sections mirroring Phase 11 Plan 04's structure:
    - **§1 tsc** — exit code + baseline comparison table across Phase 10 tip, Phase 11 tip, Phase 12 Plan 02/03/04/05/06/07 waypoints
    - **§2 vitest** — pass count + failure list + delta walkthrough from Phase 11 baseline
    - **§3 npm run build** — build time + AppShell chunk delta headline with detailed causes breakdown (sidebar panels deleted, dashboard subtree deleted, shell/Tab.tsx deleted, session-launcher relocation, dead locale keys removed)
    - **§4 grep hygiene gates** — run every gate from 12-01-STRIP-LIST Section K with expected + observed counts + PASS/FAIL status. Comment-filter methodology note at the end explains any non-zero residuals (all inside `//` or `{/* */}` — Phase 10 Wave 4 policy).
    - **§5 requirement traceability** — PURGE-06, PURGE-07, PURGE-08, PURGE-09, PURGE-10 each mapped to specific commits + specific verification gates.

    Include the 5-way requirement trace with exact commit SHAs from the Phase 12 waypoints. PURGE-09 must document its side-effect-of-PURGE-06 delivery (UserProfilePanel commandPaletteShortcutEnabled toggle dies with the panel; no standalone shortcut editor UI file existed).

    Commit message: `docs(12-07): phase 12 build verification log`.
  </action>
  <verify>
    <automated>
    test -f .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-BUILD-VERIFY-LOG.md &&
    grep -qE "tsc" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-BUILD-VERIFY-LOG.md &&
    grep -qE "vitest" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-BUILD-VERIFY-LOG.md &&
    grep -qE "npm run build|build" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-BUILD-VERIFY-LOG.md &&
    grep -q "PURGE-06" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-BUILD-VERIFY-LOG.md &&
    grep -q "PURGE-07" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-BUILD-VERIFY-LOG.md &&
    grep -q "PURGE-08" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-BUILD-VERIFY-LOG.md &&
    grep -q "PURGE-09" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-BUILD-VERIFY-LOG.md &&
    grep -q "PURGE-10" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-BUILD-VERIFY-LOG.md
    </automated>
  </verify>
  <done>
    Build-verify log exists with 5 sections; tsc + vitest + npm run build all recorded with exact numbers; every PURGE-06..10 requirement traced; grep hygiene gates all PASS or documented with comment-only residual explanation.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author 12-UAT-CHECKLIST.md — Ashley's post-deploy walkthrough + failure route-back + deploy runbook</name>
  <files>.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-UAT-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-UAT-CHECKLIST.md (mirror format including dual-outcome-acceptable hash-fragment probes and authoritative-deploy-runbook citation)
  </read_first>
  <action>
    Author `12-UAT-CHECKLIST.md` mirroring Phase 11's format:
    - **Desktop UAT (N items):** fresh page-load still lands on PrettyLandingCard (Phase 11 preservation), no navigation path to any deleted panel (host manager, snippets, admin, credentials, history, sessions, split-screen, connections, user profile), pretty-conversations sidebar renders unchanged, RDP row click opens Guacamole (PURGE-05 preservation from Phase 11), NewSessionDialog pencil opens the picker (PROTECTED file), CommandPalette double-shift opens with fleet session list (retained after Plan 02 session-launcher relocate), tab-strip absent (shell/Tab.tsx deleted), keyboard shortcuts still work (Ctrl+Shift+O pretty-view toggle preserved via features/keyboard/), direct hash-fragment probes for `#hosts`/`#admin`/`#snippets`/`#dashboard`/`#network_graph` with both-outcomes-acceptable framing (404-equivalent OR PrettyLandingCard both acceptable — the load-bearing gate is: no dead-surface panel renders).
    - **Mobile UAT (N items):** fresh landing on pretty-conversations list, tap-row → view screen, back button, no bottom nav bar, RDP row tap opens Guacamole, iOS PWA reinstall safe-area check (from Phase 11 pattern).
    - **Cross-viewport regression (N items):** message-queue drawer, pretty-view compose behavior, RDP session usable, session persistence (A→B→A no reconnect — T-06-02-01 preservation), fleet-native rows on fresh load (Phase 7 preservation), NewSessionDialog session-name auto-fallback (TG-09 preservation).
    - **Failure route-back table:** map ~15 symptoms to specific Plan/Task locations for immediate route-back signal. Include: hash-fragment probes (per fragment), CommandPalette regression signals (session-launcher relocate bug), NewSessionDialog broken (isFolder inline regression from Plan 02 Task 1), RDP click-through broken (Plan 03 accidental damage), locale-key i18n fallback broken (Plan 06 over-strip).
    - **Post-UAT deploy runbook** citing `~/.claude/identities/tina/deploy-runbook.md` as authoritative + explicitly naming the fork CLAUDE.md 15-min deadman line as stale (retired 2026-07-21) with `claude-md-15min-deadman-stale` bounty tracker. Include: fleet-standing batching rule (Ashley 2026-07-23), check-before-recreate one-liner, Ashley pre-warn about first-hard-refresh HTTP2_PROTOCOL_ERROR white-screen. Same content as Phase 11 UAT — copy verbatim with Phase 12 patch #139 substitutions.

    Section headings MUST include the exact strings `Desktop UAT`, `Mobile UAT`, `Cross-viewport` (or `regression`), `Failure route-back` (or `Failure → route-back`) for the acceptance grep gates.

    Commit message: `docs(12-07): phase 12 UAT checklist + patch #139 draft` (this bundles Task 2 + Task 3 into one commit; adjust if the SUMMARY commit is separate).
  </action>
  <verify>
    <automated>
    test -f .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-UAT-CHECKLIST.md &&
    grep -qE "Desktop UAT" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-UAT-CHECKLIST.md &&
    grep -qE "Mobile UAT" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-UAT-CHECKLIST.md &&
    grep -qE "Cross-viewport|regression" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-UAT-CHECKLIST.md &&
    grep -qE "Failure route-back|Failure → route-back" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-UAT-CHECKLIST.md &&
    grep -q "deploy-runbook.md" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-UAT-CHECKLIST.md &&
    grep -qE "batch patches|batching rule|meaningful deploys" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-UAT-CHECKLIST.md &&
    grep -qE "#hosts|#admin|#snippets|#dashboard" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-UAT-CHECKLIST.md
    </automated>
  </verify>
  <done>
    UAT checklist exists with Desktop + Mobile + Cross-viewport + failure route-back sections; deploy-runbook.md cited as authoritative; batching rule + stale CLAUDE.md callout present; hash-fragment probes enumerated.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author 12-PATCHES-MD-ENTRY.md — paste-ready patch #139 draft with multi-commit-under-one-pin format</name>
  <files>.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-PATCHES-MD-ENTRY.md</files>
  <read_first>
    - .planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-PATCHES-MD-ENTRY.md (mirror format — patch #138 precedent)
  </read_first>
  <action>
    Author `12-PATCHES-MD-ENTRY.md` as a paste-ready patch #139 entry for Tina's termix-patches.md, mirroring Phase 11's format (which is itself patterned on patches #104, #105, #128):
    - Motivating gap paragraph with Ashley's 2026-07-23 UAT quote and the reference to Phase 11 patch #138 as the predecessor.
    - Fix-summary paragraphs (one per PURGE-06..10): sidebar panel + subtree deletion (PURGE-06), dashboard/ subtree deletion (PURGE-07), Tab.tsx tab bar chrome deletion (PURGE-08), shortcut editor UI resolution via UserProfilePanel deletion side-effect (PURGE-09 — no standalone file existed), dead locale key strip across 34 files (PURGE-10).
    - Preservation paragraph: sidebar/NewSessionDialog.tsx STAYS (retained pretty-conversations pencil), features/keyboard/ STAYS (retained on-screen modifier bar for Terminal + Guacamole), features/session-launcher/ NEW (relocated from dashboard/ for CommandPalette consumption), backend untouched (Phase 13 territory), RDP baseline unchanged.
    - Bundle-size headline paragraph with the raw + gzip byte deltas vs Phase 11 tip.
    - Scope-fence held paragraph: what Phase 12 did NOT touch (backend, docker/caddy/nginx, encrypted-SQLite schema).
    - New test coverage delta: any tests added (unlikely — this is pure deletion) or removed (test files for deleted panels).
    - Files-touched enumeration by category: sidebar/ (29 deleted + 1 modified), dashboard/ (17 deleted + FullScreenAppWrapper disposition), shell/ (1 deleted + 2 modified — CommandPalette + tabUtils), features/session-launcher/ (4 created), locales/ (34 modified).
    - Verification quote-block referring readers to 12-BUILD-VERIFY-LOG.md.
    - Grep-hygiene gates listed with observed values.
    - Design source-of-truth: 12-CONTEXT.md, 12-01-STRIP-LIST.md, tina.md § Skynet direction, bounty at ~/.claude/identities/tina/bounties/skynet-transformation-purge-dead-surfaces/, deploy-runbook.md.
    - Rebase-risk paragraph: HIGH per CONTEXT.md scope-fence discipline.
    - Commit list: 1 Plan 01 + 3 Plan 02 + 4 Plan 03 + 2 Plan 04 + 1 Plan 05 + 2 Plan 06 + this plan's 3 commits = ~16 commits total. SHAs filled at paste-time (placeholders acceptable per Phase 11 pattern).
    - Deploy status: code-complete, NOT deployed, batched per fleet-standing rule.
    - Explicit contract line: `**patch #139 batches with subsequent Phase 13 backend-route purge patches.**` (exact lowercase phrase for the grep gate).
    - No Co-Authored-By trailer (fork convention).
    - Fill-in placeholders section for SHA resolution at paste-time.
    - Post-paste bookkeeping guidance: bump patch count line, commit the pin, `/close skynet-transformation-purge-dead-surfaces` if Phase 12 completes the bounty scope (verify against bounty.json state), tina.md update guidance.

    Commit either in a separate `docs(12-07): phase 12 patch #139 draft` commit OR bundled with Task 2 in `docs(12-07): phase 12 UAT checklist + patch #139 draft`. Match Phase 11 Plan 04's commit-bundling pattern for consistency.
  </action>
  <verify>
    <automated>
    test -f .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-PATCHES-MD-ENTRY.md &&
    grep -qE "patch #139|patch 139" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-PATCHES-MD-ENTRY.md &&
    grep -qE "Skynet|purge|Ship of Theseus" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-PATCHES-MD-ENTRY.md &&
    grep -q "bounties/skynet-transformation-purge-dead-surfaces" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-PATCHES-MD-ENTRY.md &&
    grep -q "deploy-runbook.md" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-PATCHES-MD-ENTRY.md &&
    grep -q "batches with subsequent" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-PATCHES-MD-ENTRY.md
    </automated>
  </verify>
  <done>
    Patch #139 draft exists, paste-ready, mirrors Phase 11 #138 format; requirement mapping present; batching contract line present; deploy-runbook citation present; SHA placeholders resolvable at paste-time.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| False PASS in build-verify | If executor hand-types PASS status without running commands, deploy failure risk. Task 1 requires verbatim command output pasted from live bash. |
| Deploy-runbook staleness | Fork CLAUDE.md has stale 15-min deadman line. UAT deploy-runbook MUST cite identity-authored deploy-runbook.md and name the fork CLAUDE.md line as stale (per Phase 11 checker B-2 fix precedent). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07-01 | Repudiation | Hand-typed build-verify PASS status | mitigate | Task 1 requires verbatim bash output paste. Any hand-typed status is a Rule 1 fail. |
| T-12-07-02 | Tampering | Missed UAT observation → broken deploy | mitigate | UAT checklist includes at least one observation per PURGE-06..10 + explicit hash-fragment probes + failure route-back table + RDP verify (PURGE-05 preservation). |
| T-12-07-03 | Tampering | Following stale deploy procedure | mitigate | UAT deploy-runbook cites identity deploy-runbook.md as authoritative + explicitly names fork CLAUDE.md line as stale with tracker. Same pattern as Phase 11. |
| T-12-07-04 | Information disclosure | Docs contain internal file paths | accept | Same trust class as existing planning tree. |
| T-12-07-SC | Tampering | npm/pip/cargo installs | accept | Zero installs. Docs-only. |
</threat_model>

<verification>
- Task 1: 12-BUILD-VERIFY-LOG.md exists with 5 sections; tsc + vitest + npm run build all recorded; every PURGE-06..10 traced; grep gates all PASS or comment-only residuals documented
- Task 2: 12-UAT-CHECKLIST.md exists with Desktop UAT + Mobile UAT + Cross-viewport + failure route-back + deploy runbook; hash-fragment probes enumerated; deploy-runbook.md cited; stale CLAUDE.md callout present
- Task 3: 12-PATCHES-MD-ENTRY.md exists with patch #139 label + Skynet/purge/Ship of Theseus + bounty citation + deploy-runbook + `batches with subsequent` lowercase phrase
- Zero source files staged in any Plan 07 commit
</verification>

<success_criteria>
- Phase 12 is code-complete-pending-Ashley-UAT-and-deploy-greenlight state
- Ashley has concrete UAT checklist for post-deploy walkthrough
- Tina has paste-ready patch #139 entry for termix-patches.md
- All 3 docs committed atomically; zero src/ files touched
- 5-way requirement traceability (PURGE-06..10) established in build-verify log
- Batching contract with Phase 13 explicit in patch #139 draft
</success_criteria>

<output>
Create `.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-07-SUMMARY.md` on completion.
</output>
