---
phase: 12-skynet-transformation-purge-dead-frontend-surfaces-second-slice
plan: 06
type: execute
wave: 4
depends_on: [12-01, 12-03, 12-04, 12-05]
files_modified:
  - src/ui/locales/en.json
  - src/ui/locales/translated/af_ZA.json
  - src/ui/locales/translated/ar_SA.json
  - src/ui/locales/translated/bg_BG.json
  - src/ui/locales/translated/bn_BD.json
  - src/ui/locales/translated/ca_ES.json
  - src/ui/locales/translated/cs_CZ.json
  - src/ui/locales/translated/da_DK.json
  - src/ui/locales/translated/de_DE.json
  - src/ui/locales/translated/el_GR.json
  - src/ui/locales/translated/es_ES.json
  - src/ui/locales/translated/fi_FI.json
  - src/ui/locales/translated/fr_FR.json
  - src/ui/locales/translated/he_IL.json
  - src/ui/locales/translated/hi_IN.json
  - src/ui/locales/translated/hu_HU.json
  - src/ui/locales/translated/id_ID.json
  - src/ui/locales/translated/it_IT.json
  - src/ui/locales/translated/ja_JP.json
  - src/ui/locales/translated/ko_KR.json
  - src/ui/locales/translated/nl_NL.json
  - src/ui/locales/translated/no_NO.json
  - src/ui/locales/translated/pl_PL.json
  - src/ui/locales/translated/pt_BR.json
  - src/ui/locales/translated/pt_PT.json
  - src/ui/locales/translated/ro_RO.json
  - src/ui/locales/translated/ru_RU.json
  - src/ui/locales/translated/sr_SP.json
  - src/ui/locales/translated/sv_SE.json
  - src/ui/locales/translated/th_TH.json
  - src/ui/locales/translated/tr_TR.json
  - src/ui/locales/translated/uk_UA.json
  - src/ui/locales/translated/vi_VN.json
  - src/ui/locales/translated/zh_CN.json
  - src/ui/locales/translated/zh_TW.json
autonomous: true
requirements: [PURGE-10]

must_haves:
  truths:
    - "Every dead locale key enumerated in 12-01-STRIP-LIST Section H is removed from all 34 locale JSON files (en.json + 33 translated files)"
    - "Surviving nav.* keys with retained-UI consumers (nav.home for TabContext, nav.terminal + nav.serverStats + nav.fileManager + nav.docker + nav.tunnels for TabContext/tabUtils, nav.newSession* for NewSessionDialog, nav.conversations, nav.close, nav.cancel, nav.confirmClose, nav.cannotSplitTab, nav.hostTabTitle, nav.copyPassword, nav.copySudoPassword, nav.passwordCopied, nav.noPasswordAvailable, nav.failedToCopyPassword, nav.refreshTab, nav.openFileManager, nav.roleAdministrator, nav.roleUser) are UNTOUCHED"
    - "tsc --noEmit stays exit 0 across the removal (typed-i18n consumers of any accidentally-still-referenced removed key would break tsc — this is the load-bearing gate)"
    - "vitest matches Phase 11 baseline (524/526)"
  artifacts: []
  key_links:
    - from: "Locale key removal"
      to: "typed-i18n compile-time safety net"
      via: "tsc --noEmit"
      pattern: "tsc"
---

<objective>
Remove every dead locale key from all 34 `src/ui/locales/*.json` files. Dead keys are enumerated in 12-01-STRIP-LIST Section H with grep-verified 0-code-consumer status after Plans 03/04/05 landed. Batch the removal into 2 atomic commits per Phase 11 STRIP-LIST Section B item 7 grouping: (batch-1) AppRail-adjacent keys (`pinAppRail`, `pinAppRailDesc`), (batch-2) dead nav.* keys with post-deletion 0-consumer status. Both commits are tsc-clean per Phase 12 CONTEXT.md `<decisions>` § "Locale string deletion — Tsc-clean is the safety net".

Purpose: Delivers PURGE-10. The typed-i18n compile-time key check is the load-bearing gate — if any dead key is accidentally left in the code but removed from JSON (or vice versa), tsc fails.

Output: 2 atomic commits, tsc-clean per commit, vitest at Phase 11 baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-CONTEXT.md
@.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove pinAppRail + pinAppRailDesc from all 34 locale files (batch-1)</name>
  <files>
    src/ui/locales/en.json,
    src/ui/locales/translated/af_ZA.json,
    src/ui/locales/translated/ar_SA.json,
    src/ui/locales/translated/bg_BG.json,
    src/ui/locales/translated/bn_BD.json,
    src/ui/locales/translated/ca_ES.json,
    src/ui/locales/translated/cs_CZ.json,
    src/ui/locales/translated/da_DK.json,
    src/ui/locales/translated/de_DE.json,
    src/ui/locales/translated/el_GR.json,
    src/ui/locales/translated/es_ES.json,
    src/ui/locales/translated/fi_FI.json,
    src/ui/locales/translated/fr_FR.json,
    src/ui/locales/translated/he_IL.json,
    src/ui/locales/translated/hi_IN.json,
    src/ui/locales/translated/hu_HU.json,
    src/ui/locales/translated/id_ID.json,
    src/ui/locales/translated/it_IT.json,
    src/ui/locales/translated/ja_JP.json,
    src/ui/locales/translated/ko_KR.json,
    src/ui/locales/translated/nl_NL.json,
    src/ui/locales/translated/no_NO.json,
    src/ui/locales/translated/pl_PL.json,
    src/ui/locales/translated/pt_BR.json,
    src/ui/locales/translated/pt_PT.json,
    src/ui/locales/translated/ro_RO.json,
    src/ui/locales/translated/ru_RU.json,
    src/ui/locales/translated/sr_SP.json,
    src/ui/locales/translated/sv_SE.json,
    src/ui/locales/translated/th_TH.json,
    src/ui/locales/translated/tr_TR.json,
    src/ui/locales/translated/uk_UA.json,
    src/ui/locales/translated/vi_VN.json,
    src/ui/locales/translated/zh_CN.json,
    src/ui/locales/translated/zh_TW.json
  </files>
  <read_first>
    - .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md § Section H batch-1
    - src/ui/locales/en.json (verify existence of pinAppRail + pinAppRailDesc keys — they may be nested inside a settings/hosts/interface sub-object; identify the exact JSON path)
    - Pre-run: `grep -l "pinAppRail" src/ui/locales/*.json src/ui/locales/translated/*.json | wc -l` → expected 34 (all files have the key)
    - Pre-run: `grep -rn "pinAppRail" src/ --include="*.ts" --include="*.tsx"` → expected 0 code hits (retired since Phase 11 STRIP-LIST Section B item 7 confirmed dead upstream carryover)
  </read_first>
  <action>
    Identify the exact JSON path of `pinAppRail` + `pinAppRailDesc` in `src/ui/locales/en.json` by reading the file and locating the containing object (likely under a settings/appearance/interface sub-object).

    Write a Node script or use a JSON-safe editor to remove both keys from all 34 files. Do NOT use `sed` on JSON — it breaks nested structure and quoting. Recommended approach:

    ```bash
    node -e '
    const fs = require("fs");
    const path = require("path");
    const files = [
      "src/ui/locales/en.json",
      ...fs.readdirSync("src/ui/locales/translated").map(f => path.join("src/ui/locales/translated", f))
    ];
    function stripKeys(obj, keysToStrip) {
      if (obj === null || typeof obj !== "object") return;
      for (const k of Object.keys(obj)) {
        if (keysToStrip.includes(k)) delete obj[k];
        else stripKeys(obj[k], keysToStrip);
      }
    }
    for (const f of files) {
      const data = JSON.parse(fs.readFileSync(f, "utf8"));
      stripKeys(data, ["pinAppRail", "pinAppRailDesc"]);
      fs.writeFileSync(f, JSON.stringify(data, null, 2) + "\n");
    }
    '
    ```

    Preserve every other key + value verbatim. Preserve original JSON formatting: 2-space indent + trailing newline. If any file's existing indent differs (verify by reading a sample), match that file's convention — do NOT introduce a whole-file diff via formatting churn. If files use inconsistent formatting across the tree, defer to the en.json convention.

    Post-write verification:
    - `grep -c pinAppRail src/ui/locales/en.json` → 0
    - `grep -l pinAppRail src/ui/locales/*.json src/ui/locales/translated/*.json | wc -l` → 0
    - `grep -rn pinAppRail src/ --include="*.ts" --include="*.tsx"` → 0 (unchanged; no code consumer existed pre-Phase-11)
    - `npx tsc --noEmit` → exit 0
    - `npx vitest run` → at Phase 11 baseline

    Commit message: `chore(locales): remove dead pinAppRail + pinAppRailDesc keys from all 34 locale files (Phase 12 Plan 06 PURGE-10 batch-1)`.
  </action>
  <verify>
    <automated>
    grep -l pinAppRail src/ui/locales/*.json src/ui/locales/translated/*.json | wc -l | grep -qE "^0$" &&
    grep -rn "pinAppRail" src/ --include="*.ts" --include="*.tsx" | wc -l | grep -qE "^0$" &&
    npx tsc --noEmit
    </automated>
  </verify>
  <done>
    Zero pinAppRail / pinAppRailDesc keys remain in any of the 34 locale files; zero code consumers (unchanged from baseline); tsc-clean; single atomic commit landed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove dead nav.* keys from all 34 locale files (batch-2)</name>
  <files>
    src/ui/locales/en.json,
    src/ui/locales/translated/af_ZA.json,
    src/ui/locales/translated/ar_SA.json,
    src/ui/locales/translated/bg_BG.json,
    src/ui/locales/translated/bn_BD.json,
    src/ui/locales/translated/ca_ES.json,
    src/ui/locales/translated/cs_CZ.json,
    src/ui/locales/translated/da_DK.json,
    src/ui/locales/translated/de_DE.json,
    src/ui/locales/translated/el_GR.json,
    src/ui/locales/translated/es_ES.json,
    src/ui/locales/translated/fi_FI.json,
    src/ui/locales/translated/fr_FR.json,
    src/ui/locales/translated/he_IL.json,
    src/ui/locales/translated/hi_IN.json,
    src/ui/locales/translated/hu_HU.json,
    src/ui/locales/translated/id_ID.json,
    src/ui/locales/translated/it_IT.json,
    src/ui/locales/translated/ja_JP.json,
    src/ui/locales/translated/ko_KR.json,
    src/ui/locales/translated/nl_NL.json,
    src/ui/locales/translated/no_NO.json,
    src/ui/locales/translated/pl_PL.json,
    src/ui/locales/translated/pt_BR.json,
    src/ui/locales/translated/pt_PT.json,
    src/ui/locales/translated/ro_RO.json,
    src/ui/locales/translated/ru_RU.json,
    src/ui/locales/translated/sr_SP.json,
    src/ui/locales/translated/sv_SE.json,
    src/ui/locales/translated/th_TH.json,
    src/ui/locales/translated/tr_TR.json,
    src/ui/locales/translated/uk_UA.json,
    src/ui/locales/translated/vi_VN.json,
    src/ui/locales/translated/zh_CN.json,
    src/ui/locales/translated/zh_TW.json
  </files>
  <read_first>
    - .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md § Section H batch-2 (final list of dead nav.* keys after Plans 03/04/05 confirmed 0 code consumers)
    - src/ui/locales/en.json § "nav" object (verify current key inventory)
    - Verify Plans 03, 04, 05 all committed successfully (deleted files no longer consume nav.* keys)
  </read_first>
  <action>
    Pre-Task-2 grep audit — for each candidate dead key, run and confirm 0 code hits:
    ```bash
    for k in dashboard hosts snippets admin credentials history hostManager sessions userProfile connections quickConnect sshTools networkGraph splitScreen; do
      code_hits=$(grep -rn "t([\"']nav\.${k}[\"']" src/ --include="*.ts" --include="*.tsx" 2>/dev/null | wc -l)
      echo "nav.${k}: ${code_hits} code consumer(s)"
    done
    ```
    Expected all outputs = 0 (Plan 03 deleted UserProfilePanel + splitScreen consumers via shell/Tab.tsx deletion in Plan 05).

    If ANY key shows >0 consumers, that key STAYS (do NOT remove); flag in the Task 2 SUMMARY with the surviving consumer file:line.

    Additionally, cross-check the KEEP list — these MUST NOT be removed:
    - `nav.home`, `nav.terminal`, `nav.serverStats`, `nav.fileManager`, `nav.docker`, `nav.tunnels` (consumed by shell/TabContext.tsx computeUniqueTitle + shell/tabUtils.tsx EmptyState renderers — all retained)
    - `nav.newSession`, `nav.newSessionTitle`, `nav.newSessionDescription`, `nav.newSessionSearchHosts`, `nav.newSessionNameLabel`, `nav.newSessionNamePlaceholder`, `nav.newSessionNameError`, `nav.newSessionNoHosts`, `nav.newSessionHostList` (consumed by sidebar/NewSessionDialog.tsx — retained)
    - `nav.conversations`, `nav.close`, `nav.cancel`, `nav.confirmClose`, `nav.cannotSplitTab`, `nav.hostTabTitle`, `nav.copyPassword`, `nav.copySudoPassword`, `nav.passwordCopied`, `nav.noPasswordAvailable`, `nav.failedToCopyPassword`, `nav.refreshTab`, `nav.openFileManager`, `nav.roleAdministrator`, `nav.roleUser` (consumed by various retained modules — spot-check with grep before writing the removal script)

    Write a Node script mirroring Task 1's pattern but stripping the following nested keys under the `nav` object across all 34 files. Use the confirmed dead-key list from the STRIP-LIST Section H batch-2 (planner-estimate below; executor cross-checks against grep before writing):

    Candidate dead nav keys (confirm each has 0 code hits before stripping):
    `dashboard`, `hosts`, `snippets`, `admin`, `credentials`, `history`, `hostManager`, `sessions`, `userProfile`, `connections`, `quickConnect`, `sshTools`, `networkGraph`, `splitScreen`, `sshManager` (verify sshManager consumer status — was consumed by TabContext.addTab type check at TabContext line 156 which STAYS; if grep confirms 0 code hits, remove; otherwise keep).

    ```bash
    node -e '
    const fs = require("fs");
    const path = require("path");
    const DEAD_NAV_KEYS = /* PASTE VERIFIED LIST HERE — from grep audit */ [];
    const files = [
      "src/ui/locales/en.json",
      ...fs.readdirSync("src/ui/locales/translated").map(f => path.join("src/ui/locales/translated", f))
    ];
    for (const f of files) {
      const data = JSON.parse(fs.readFileSync(f, "utf8"));
      if (data.nav) {
        for (const k of DEAD_NAV_KEYS) {
          delete data.nav[k];
        }
      }
      fs.writeFileSync(f, JSON.stringify(data, null, 2) + "\n");
    }
    '
    ```

    Preserve every non-nav key and every KEEP-list nav key verbatim.

    Post-write verification:
    - For each removed key, `grep -c '"<key>"' src/ui/locales/en.json` → 0
    - For each removed key, `grep -c '"<key>"' src/ui/locales/translated/*.json | grep -v ":0"` → 0 (no file has any lingering hits)
    - For each removed key, `grep -rn "t([\"']nav\.<key>[\"']" src/ --include="*.ts" --include="*.tsx"` → 0
    - `npx tsc --noEmit` → exit 0 (typed-i18n gate — any surviving code consumer of a removed key breaks tsc)
    - `npx vitest run` → at Phase 11 baseline (524/526)

    If tsc breaks, a code consumer was missed. Executor identifies the surviving reference from the tsc error message, and EITHER (a) that key stays in JSON (revert its removal + document in SUMMARY), OR (b) that code consumer is a Phase-11-adjacent stray that also needs stripping (rare; route back to planner).

    Commit message: `chore(locales): remove dead nav.* keys from all 34 locale files — dashboard, hosts, snippets, admin, credentials, history, hostManager, sessions, userProfile, connections, quickConnect, sshTools, networkGraph, splitScreen (Phase 12 Plan 06 PURGE-10 batch-2)`. Adjust the enumerated key list in the commit message to match what was actually removed.
  </action>
  <verify>
    <automated>
    grep -c '"dashboard"' src/ui/locales/en.json | grep -qE "^0$" &&
    grep -c '"hostManager"' src/ui/locales/en.json | grep -qE "^0$" &&
    grep -c '"snippets"' src/ui/locales/en.json | grep -qE "^0$" &&
    grep -c '"admin"' src/ui/locales/en.json | grep -qE "^0$" &&
    grep -c '"hosts"' src/ui/locales/en.json | grep -qE "^0$" &&
    grep -c '"credentials"' src/ui/locales/en.json | grep -qE "^0$" &&
    grep -c '"history"' src/ui/locales/en.json | grep -qE "^0$" &&
    grep -c '"connections"' src/ui/locales/en.json | grep -qE "^0$" &&
    grep -c '"sessions"' src/ui/locales/en.json | grep -qE "^0$" &&
    grep -c '"userProfile"' src/ui/locales/en.json | grep -qE "^0$" &&
    grep -c '"conversations"' src/ui/locales/en.json | grep -qE "^[1-9]" &&
    grep -c '"newSession"' src/ui/locales/en.json | grep -qE "^[1-9]" &&
    grep -c '"home"' src/ui/locales/en.json | grep -qE "^[1-9]" &&
    grep -c '"terminal"' src/ui/locales/en.json | grep -qE "^[1-9]" &&
    npx tsc --noEmit
    </automated>
  </verify>
  <done>
    Dead nav.* keys removed from all 34 files; KEEP-list nav keys (home, terminal, serverStats, fileManager, docker, tunnels, newSession*, conversations, close, cancel, confirmClose, cannotSplitTab, hostTabTitle, copyPassword, copySudoPassword, passwordCopied, noPasswordAvailable, failedToCopyPassword, refreshTab, openFileManager, roleAdministrator, roleUser) PRESERVED; tsc-clean (typed-i18n gate); vitest baseline; single atomic commit landed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Typed-i18n compile-time gate | react-i18next TFunction generics resolve keys at compile time. Removing a key that a code consumer still uses would break tsc. This is the load-bearing safety net — tsc-clean per commit is the only certain proof no surviving consumer was missed. |
| JSON structural integrity | 34 files edited via Node JSON parse+stringify. Preserves nested structure but MAY change formatting (indent, trailing whitespace, key order). Executor uses `JSON.stringify(data, null, 2) + "\n"` to match en.json convention. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06-01 | Tampering | Remove a still-consumed key → tsc break | mitigate | Task 2 pre-strip grep audit runs `grep -rn "t([\"']nav\.<key>[\"']" src/` for every candidate; only keys with 0 code hits get stripped. tsc-clean post-strip is the load-bearing gate. If tsc breaks, executor reverts the offending key removal or routes back. |
| T-12-06-02 | Tampering | Remove a KEEP-list key by accident | mitigate | Task 2 explicitly enumerates the KEEP list (nav.home, nav.terminal, nav.newSession*, etc.) with retained-UI consumer file:line evidence. Removal script only strips keys explicitly listed in DEAD_NAV_KEYS array. Verify includes `grep -c "conversations" src/ui/locales/en.json > 0` PASS gate. |
| T-12-06-03 | Tampering | JSON structural corruption | mitigate | Node JSON.parse + JSON.stringify preserves nested structure. Formatting drift (indent/whitespace changes) is acceptable — the JSON validator on tsc build gates any broken syntax. `git diff` should show only key removals, not structural whitespace churn if we match en.json's convention. |
| T-12-06-04 | Tampering | pinAppRail still has a Section-B-adjacent code consumer we missed | mitigate | Phase 11 STRIP-LIST Section B item 7 confirmed 0 code consumers pre-Phase-11. Task 1 pre-strip grep re-verifies this. |
| T-12-06-05 | Information disclosure | Locale files edit leaks path info in git diff | accept | JSON diff is public-safe locale content only. |
| T-12-06-06 | Tampering | Package installs | accept | Zero installs. Only Node scripts. |
| T-12-06-SC | Tampering | npm/pip/cargo installs | accept | Zero installs. |
</threat_model>

<verification>
- Task 1: pinAppRail + pinAppRailDesc gone from all 34 files; tsc-clean; committed
- Task 2: dead nav.* keys gone from all 34 files; KEEP-list nav keys preserved; tsc-clean (load-bearing typed-i18n gate); vitest baseline; committed
- 2 atomic commits landed
</verification>

<success_criteria>
- All 34 locale files stripped of pinAppRail + pinAppRailDesc + dead nav.* keys
- KEEP-list nav keys (home, terminal, serverStats, fileManager, docker, tunnels, newSession*, conversations, close, cancel, confirmClose, cannotSplitTab, hostTabTitle, copyPassword*, refreshTab, openFileManager, roleAdministrator, roleUser) preserved
- tsc --noEmit exit 0 (proves no code consumer of removed keys)
- vitest at Phase 11 baseline
- PURGE-10 delivered
</success_criteria>

<output>
Create `.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-06-SUMMARY.md` on completion.
</output>
