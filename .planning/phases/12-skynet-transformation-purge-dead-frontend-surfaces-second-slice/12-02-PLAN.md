---
phase: 12-skynet-transformation-purge-dead-frontend-surfaces-second-slice
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - src/ui/sidebar/NewSessionDialog.tsx
  - src/ui/shell/CommandPalette.tsx
  - src/ui/shell/tabUtils.tsx
  - src/ui/features/session-launcher/NewSessionDialog.tsx
  - src/ui/features/session-launcher/sshHostToHost.ts
  - src/ui/features/session-launcher/RemoteHostChips.tsx
  - src/ui/features/session-launcher/NewSessionHostChips.tsx
autonomous: true
requirements: [PURGE-06, PURGE-07]

must_haves:
  truths:
    - "After this plan lands, `sidebar/NewSessionDialog.tsx` imports zero symbols from `@/sidebar/SidebarTree` (so SidebarTree.tsx becomes deletion-safe in Plan 03)"
    - "After this plan lands, `shell/CommandPalette.tsx` imports zero files from `@/dashboard/` (so src/ui/dashboard/ becomes deletion-safe in Plan 04)"
    - "After this plan lands, `shell/tabUtils.tsx` imports zero files from `@/dashboard/` (so src/ui/dashboard/cards/ becomes deletion-safe in Plan 04)"
    - "The retained UI behavior of NewSessionDialog (pretty-conversations pencil), CommandPalette (double-shift fleet search), and tabUtils network_graph render is preserved end-to-end — this is refactor without behavior change"
    - "TypeScript compiles clean after every commit (tsc-clean-per-commit invariant)"
    - "Vitest matches Phase 11 baseline (524/526) after every commit"
  artifacts:
    - path: "src/ui/features/session-launcher/NewSessionDialog.tsx"
      provides: "The relocated dashboard NewSessionDialog for CommandPalette consumption"
      contains: "export function NewSessionDialog"
    - path: "src/ui/features/session-launcher/sshHostToHost.ts"
      provides: "The relocated sshHostToHost adapter"
      contains: "export function sshHostToHost"
    - path: "src/ui/features/session-launcher/RemoteHostChips.tsx"
      provides: "The relocated RemoteHostChips component"
      contains: "export"
    - path: "src/ui/features/session-launcher/NewSessionHostChips.tsx"
      provides: "The relocated NewSessionHostChips component"
      contains: "export"
    - path: "src/ui/sidebar/NewSessionDialog.tsx"
      provides: "Retained pretty-conversations pencil consumer with inlined isFolder"
      contains: "function isFolder"
  key_links:
    - from: "src/ui/shell/CommandPalette.tsx"
      to: "src/ui/features/session-launcher/*"
      via: "import"
      pattern: "from \"@/features/session-launcher/"
    - from: "src/ui/sidebar/NewSessionDialog.tsx"
      to: "isFolder inlined type-guard"
      via: "local function definition"
      pattern: "function isFolder\\("
    - from: "src/ui/shell/tabUtils.tsx"
      to: "PrettyLandingCard fallback for network_graph case"
      via: "case return"
      pattern: "case \"network_graph\":"
---

<objective>
Perform three pre-flight refactors that enable the Wave 3 deletion plans (03/04/05) to run tsc-clean-per-commit without breaking retained UI:
1. Inline the 3-line `isFolder` type-guard from `sidebar/SidebarTree.tsx` into `sidebar/NewSessionDialog.tsx` so SidebarTree can be deleted in Plan 03 without breaking the pretty-conversations pencil consumer.
2. Relocate the 4 dashboard-shared files (NewSessionDialog, sshHostToHost, RemoteHostChips, NewSessionHostChips) that CommandPalette (retained) consumes to a NEW retained-UI location `src/ui/features/session-launcher/`, and update CommandPalette's imports. This isolates the retained CommandPalette wire from `src/ui/dashboard/` so Plan 04 can delete the entire dashboard subtree.
3. Replace `shell/tabUtils.tsx`'s `case "network_graph": return <NetworkGraphCard/>` with a PrettyLandingCard fallback (mirror the Phase 11 dashboard-swap pattern), stripping the NetworkGraphCard import, so `src/ui/dashboard/cards/` can be deleted in Plan 04.

Purpose: Wave 3 deletion plans (03/04/05) MUST run tsc-clean per commit. This plan performs the refactors that would otherwise force a tsc-broken intermediate state. This is refactor-without-behavior-change: the retained UI behaves identically before and after.

Output: 3 atomic commits (one per refactor), tsc-clean per commit, vitest at Phase 11 baseline (524/526).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-CONTEXT.md
@.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md
@.planning/phases/11-skynet-transformation-purge-dead-skynet-surfaces-first-slice/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Inline isFolder into sidebar/NewSessionDialog.tsx and drop the @/sidebar/SidebarTree import</name>
  <files>src/ui/sidebar/NewSessionDialog.tsx</files>
  <read_first>
    - src/ui/sidebar/NewSessionDialog.tsx (verify current line 38 `import { isFolder } from "@/sidebar/SidebarTree";` and line 52 consumer `if (isFolder(child))`)
    - src/ui/sidebar/SidebarTree.tsx lines 54-56 (the exact isFolder body to inline)
    - src/ui/sidebar/NewSessionDialog.test.tsx (verify test suite passes after inline; the isFolder behavior is unchanged)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (verify it consumes the sidebar NewSessionDialog import at line 56 — this is the load-bearing retained consumer)
  </read_first>
  <action>
    Strip the current line 38 `import { isFolder } from "@/sidebar/SidebarTree";` from `src/ui/sidebar/NewSessionDialog.tsx`. Add a top-level local function `isFolder` inside the same file (before the first usage at current line 52), with the exact same type signature and body as SidebarTree.tsx:54-56. The isFolder body is a 3-line type-guard checking `"children" in item` (or the equivalent property discriminator; verify by reading SidebarTree.tsx:54-56 verbatim before writing). Include a comment above the inlined function: `// Local type-guard inlined from src/ui/sidebar/SidebarTree.tsx (Phase 12 Plan 02 — enables SidebarTree deletion in Plan 03).` The function is NOT exported — it stays a module-private helper. All other imports, exports, JSX, effect logic, host filtering, session-name validation logic in NewSessionDialog stay verbatim. Zero behavior change — this is a mechanical refactor.

    Do NOT touch the two other imports NewSessionDialog uses from within sidebar/ — verify current line 38 is the ONLY `@/sidebar/SidebarTree` import in the file. Do NOT rename the file. Do NOT modify NewSessionDialog.test.tsx (Task 3 hygiene note only if tests need it, which they should not for a mechanical inline).

    Commit message: `refactor(sidebar): inline isFolder into NewSessionDialog (Phase 12 Plan 02 — pre-flight for SidebarTree deletion in Plan 03)`.
  </action>
  <verify>
    <automated>
    grep -c 'from "@/sidebar/SidebarTree"' src/ui/sidebar/NewSessionDialog.tsx | grep -qE "^0$" &&
    grep -cE '^\s*function isFolder\s*\(' src/ui/sidebar/NewSessionDialog.tsx | grep -qE "^1$" &&
    npx tsc --noEmit &&
    npx vitest run src/ui/sidebar/NewSessionDialog.test.tsx &&
    npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
    </automated>
  </verify>
  <done>
    NewSessionDialog.tsx has zero imports from `@/sidebar/SidebarTree`, a local `function isFolder(...)` type-guard is defined in module scope, tsc-clean, NewSessionDialog.test.tsx and PrettyConversationsPanel.test.tsx both pass. Single atomic commit landed with the prescribed message.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Relocate the 4 dashboard-shared files to src/ui/features/session-launcher/ and update CommandPalette imports</name>
  <files>
    src/ui/features/session-launcher/NewSessionDialog.tsx,
    src/ui/features/session-launcher/sshHostToHost.ts,
    src/ui/features/session-launcher/RemoteHostChips.tsx,
    src/ui/features/session-launcher/NewSessionHostChips.tsx,
    src/ui/shell/CommandPalette.tsx
  </files>
  <read_first>
    - src/ui/dashboard/NewSessionDialog.tsx (source file to relocate; different from sidebar/NewSessionDialog.tsx which STAYS)
    - src/ui/dashboard/sshHostToHost.ts (source file to relocate)
    - src/ui/dashboard/RemoteHostChips.tsx (source file to relocate)
    - src/ui/dashboard/NewSessionHostChips.tsx (source file to relocate)
    - src/ui/shell/CommandPalette.tsx (current import lines 9, 10, 11-14, 15-19 all point at @/dashboard/; verify these are the ONLY dashboard imports in the file)
    - src/ui/sidebar/SessionsPanel.tsx (also imports @/dashboard/sshHostToHost at line 7; note that SessionsPanel dies in Plan 03 so no relocate needed for that consumer — but confirm)
    - src/ui/dashboard/cards/NetworkGraphCard.tsx (may transitively depend on one of the 4 relocated files; do a Read to enumerate its imports before proceeding)
  </read_first>
  <action>
    Create the new directory `src/ui/features/session-launcher/` and copy the 4 files verbatim from `src/ui/dashboard/`:
    - `src/ui/dashboard/NewSessionDialog.tsx` → `src/ui/features/session-launcher/NewSessionDialog.tsx`
    - `src/ui/dashboard/sshHostToHost.ts` → `src/ui/features/session-launcher/sshHostToHost.ts`
    - `src/ui/dashboard/RemoteHostChips.tsx` → `src/ui/features/session-launcher/RemoteHostChips.tsx`
    - `src/ui/dashboard/NewSessionHostChips.tsx` → `src/ui/features/session-launcher/NewSessionHostChips.tsx`

    Use `git mv` for a clean history if none of the four files transitively import each other via relative paths — verify with a grep pass on each. If any file imports another of the four via relative path (e.g., NewSessionHostChips imports sshHostToHost via `./sshHostToHost`), those relative imports remain valid because both files move together. If any file imports one of the four via `@/dashboard/<name>` alias, rewrite the alias to `@/features/session-launcher/<name>` after the move.

    Verify each relocated file's non-sibling imports still resolve from the new location. Test the resolutions:
    - `@/components/*`, `@/lib/*`, `@/types/*`, `@/main-axios`, `@/api/*`, `lucide-react`, `react`, `react-i18next` — all module-alias or npm imports resolve identically from the new location (the `@/` alias maps to `src/ui/` in tsconfig).
    - If ANY relocated file imports `@/dashboard/<anything>`, that import MUST be updated to point at the new session-launcher path (or the referenced dashboard file is dying and the import needs a different resolution — flag for planner route-back).

    Update `src/ui/shell/CommandPalette.tsx` imports:
    - Line 9: `import { NewSessionDialog } from "@/dashboard/NewSessionDialog";` → `import { NewSessionDialog } from "@/features/session-launcher/NewSessionDialog";`
    - Line 10: `import { sshHostToHost } from "@/dashboard/sshHostToHost";` → `import { sshHostToHost } from "@/features/session-launcher/sshHostToHost";`
    - Line 11-14: `import { RemoteHostChips, isProtocolHost } from "@/dashboard/RemoteHostChips";` → `... from "@/features/session-launcher/RemoteHostChips";`
    - Line 15-19: `import { NewSessionHostChips, isAutoTmuxHost, isSshLaunchableHost } from "@/dashboard/NewSessionHostChips";` → `... from "@/features/session-launcher/NewSessionHostChips";`

    Do NOT modify the OLD dashboard-tree copies of the 4 files in this task — Plan 04 deletes the entire `src/ui/dashboard/` subtree. Between Task 2 completion and Plan 04 landing, the OLD copies remain on disk as dead code (0 consumers) — this is acceptable per Phase 10 Wave 4 precedent.

    Do NOT relocate `src/ui/sidebar/SessionsPanel.tsx`'s import of `@/dashboard/sshHostToHost` — SessionsPanel dies in Plan 03; the import dies with the file, no relocate needed.

    Do NOT modify `src/ui/dashboard/cards/NetworkGraphCard.tsx` in this task (Task 3 handles the network_graph render case separately; NetworkGraphCard itself dies with dashboard/ in Plan 04).

    Do NOT modify `src/ui/dashboard/DashboardTab.tsx`, `Dashboard.tsx`, `SessionDashboard.tsx` — all die in Plan 04.

    Commit message: `refactor(session-launcher): relocate 4 dashboard-shared files consumed by CommandPalette to features/session-launcher/ (Phase 12 Plan 02 — pre-flight for dashboard/ deletion in Plan 04)`.
  </action>
  <verify>
    <automated>
    test -f src/ui/features/session-launcher/NewSessionDialog.tsx &&
    test -f src/ui/features/session-launcher/sshHostToHost.ts &&
    test -f src/ui/features/session-launcher/RemoteHostChips.tsx &&
    test -f src/ui/features/session-launcher/NewSessionHostChips.tsx &&
    grep -c 'from "@/dashboard/' src/ui/shell/CommandPalette.tsx | grep -qE "^0$" &&
    grep -c 'from "@/features/session-launcher/' src/ui/shell/CommandPalette.tsx | grep -qE "^4$" &&
    npx tsc --noEmit &&
    npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
    </automated>
  </verify>
  <done>
    4 files exist at new session-launcher/ paths, CommandPalette.tsx imports zero @/dashboard/ paths and 4 @/features/session-launcher/ paths, tsc-clean, PrettyConversationsPanel test suite passes at baseline. Single atomic commit landed with the prescribed message.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Replace shell/tabUtils.tsx network_graph render with PrettyLandingCard fallback + strip NetworkGraphCard import</name>
  <files>src/ui/shell/tabUtils.tsx</files>
  <read_first>
    - src/ui/shell/tabUtils.tsx (verify line 28 `import { NetworkGraphCard } from "@/dashboard/cards/NetworkGraphCard";`, verify line 278-279 the render case `case "network_graph": return <NetworkGraphCard embedded={false} />`, verify PrettyLandingCard is already imported around line ~9-15 area post-Phase-11)
    - src/ui/features/pretty-view/PrettyLandingCard.tsx (already exists from Phase 11 Plan 02; the fallback component)
    - src/types/ui-types.ts (verify `network_graph` is a valid TabType at line 162 + 234 — MUST be preserved because tabUtils switch needs it)
  </read_first>
  <action>
    Strip `import { NetworkGraphCard } from "@/dashboard/cards/NetworkGraphCard";` from `src/ui/shell/tabUtils.tsx` (current line 28). If PrettyLandingCard is already imported (from Phase 11 Plan 02), reuse the existing import; do NOT add a duplicate import.

    Rewrite the render case at current lines 278-279:
    ```
    case "network_graph":
      return <NetworkGraphCard embedded={false} />;
    ```
    to render `<PrettyLandingCard />` instead (same as the Phase 11 `case "dashboard"` handling). Include a comment matching the Phase 11 pattern:
    ```
    case "network_graph":
      // Phase 12 Plan 02 (pre-flight for dashboard/ deletion in Plan 04): renders
      // the pretty-view empty-landing card. The network_graph TabType is preserved
      // in the union but no UI path reaches it in Skynet; the retired
      // NetworkGraphCard from src/ui/dashboard/cards/ becomes dead code and is
      // deleted with the dashboard subtree in Plan 04.
      return <PrettyLandingCard />;
    ```

    Do NOT modify the `case "network_graph":` in `tabIcon` (current line 113-114 returns `<Network className="size-3.5" />`). The Network icon comes from lucide-react (already imported) — it does NOT reach dashboard/cards/; leave it intact for TabType-union completeness.

    Do NOT strip the `network_graph` TabType from `src/types/ui-types.ts` — the union entry stays for exhaustive-switch safety (mirror Phase 11's "dashboard" TabType preservation decision).

    Do NOT touch any other case in tabUtils.tsx — `case "host-manager": case "user-profile": case "admin-settings": return null;` at lines 281-284 already return null; leave that trio verbatim.

    Rule 1 self-check before commit: your comment text above must NOT contain the literal string `NetworkGraphCard` as a raw code identifier if the plan's verify grep pattern excludes it. Read the verify block carefully. To be safe, mention "the retired NetworkGraphCard from src/ui/dashboard/cards/" inside a `//` line-comment where the grep `grep -v '^\s*//'` filter would exclude it, OR rephrase to avoid the literal. Rule 1 fix is acceptable per Phase 11 precedent.

    Commit message: `refactor(tabUtils): swap network_graph render to PrettyLandingCard (Phase 12 Plan 02 — pre-flight for dashboard/cards/ deletion in Plan 04)`.
  </action>
  <verify>
    <automated>
    grep -c 'from "@/dashboard/cards/NetworkGraphCard"' src/ui/shell/tabUtils.tsx | grep -qE "^0$" &&
    grep -cE 'case "network_graph"' src/ui/shell/tabUtils.tsx | grep -qE "^[1-9]" &&
    grep -q 'PrettyLandingCard' src/ui/shell/tabUtils.tsx &&
    grep -v '^\s*//' src/ui/shell/tabUtils.tsx | grep -v '^\s*\*' | grep -c 'NetworkGraphCard' | grep -qE "^0$" &&
    npx tsc --noEmit &&
    npx vitest run src/ui/features/pretty-view/PrettyLandingCard.test.tsx
    </automated>
  </verify>
  <done>
    tabUtils.tsx imports zero paths from `@/dashboard/`, the network_graph case returns PrettyLandingCard, no non-comment code reference to NetworkGraphCard remains in tabUtils.tsx, tsc-clean, PrettyLandingCard test suite passes. Single atomic commit landed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Refactor-vs-deletion sequence | Wave 2 (this plan) must land BEFORE Wave 3 deletion plans so tsc-clean-per-commit invariant holds. A commit sequence violation would leave an intermediate state where sidebar/NewSessionDialog imports a deleted symbol OR CommandPalette imports a deleted file. |
| Retained-UI behavior preservation | Refactors must not change observable behavior of NewSessionDialog (pretty-conversations pencil), CommandPalette (double-shift fleet search), or tabUtils network_graph render. Regression here would surface as broken TG-09 or a broken command palette. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | Tampering | sidebar/NewSessionDialog.tsx isFolder inline | mitigate | Executor reads SidebarTree.tsx:54-56 verbatim before writing the inlined function; type signature preserved. Verify: NewSessionDialog.test.tsx passes (isFolder consumer behavior unchanged) + PrettyConversationsPanel.test.tsx passes (pencil-mounts-NewSessionDialog integration unchanged). |
| T-12-02-02 | Tampering | CommandPalette.tsx import redirect | mitigate | 4 imports rewritten in one commit. Post-commit grep verifies 0 `@/dashboard/` imports remain in CommandPalette.tsx. Retained-UI verification: `npm run dev` render test out of scope, but tsc-clean plus PrettyConversationsPanel.test.tsx pass confirms the wiring holds. |
| T-12-02-03 | Tampering | tabUtils.tsx network_graph case swap | mitigate | Pattern mirrors Phase 11 Plan 02 (dashboard case swap) which shipped tsc-clean. PrettyLandingCard.test.tsx passes (fallback component behavior unchanged). Verify: no non-comment NetworkGraphCard reference remains. |
| T-12-02-04 | Tampering | Wrong NewSessionDialog file relocated | mitigate | Two files share the name `NewSessionDialog.tsx` — one at `sidebar/` (retained, PROTECTED) and one at `dashboard/` (relocated in this plan). Executor MUST use exact-path `git mv src/ui/dashboard/NewSessionDialog.tsx src/ui/features/session-launcher/NewSessionDialog.tsx` and MUST NOT touch `src/ui/sidebar/NewSessionDialog.tsx` in Task 2. Guardrail: Task 1 modifies sidebar/NewSessionDialog.tsx (protected inline), Task 2 relocates dashboard/NewSessionDialog.tsx (different file). Any conflation trips tsc. Additionally: PrettyConversationsPanel.tsx line 56 imports `@/sidebar/NewSessionDialog` — grep gate ensures this line unchanged. |
| T-12-02-05 | Tampering | Transitive dashboard import from a relocated file | mitigate | Task 2 read_first requires enumerating every import in the 4 relocated files. If any imports `@/dashboard/<other>`, executor rewrites the alias to `@/features/session-launcher/<other>` (if the target moved) or flags for planner route-back (if the target is dying with dashboard/ deletion). |
| T-12-02-06 | Tampering | Package installs | accept | No new npm deps. |
| T-12-02-SC | Tampering | npm/pip/cargo installs | accept | Zero installs in this plan. |
</threat_model>

<verification>
- Task 1: `grep -c 'from "@/sidebar/SidebarTree"' src/ui/sidebar/NewSessionDialog.tsx` = 0; isFolder defined locally; NewSessionDialog + PrettyConversationsPanel tests pass
- Task 2: `grep -c 'from "@/dashboard/' src/ui/shell/CommandPalette.tsx` = 0; 4 files exist at new paths; tsc-clean
- Task 3: `grep -c 'NetworkGraphCard' src/ui/shell/tabUtils.tsx` (non-comment) = 0; PrettyLandingCard renders network_graph case; tsc-clean
- After all 3 tasks: `npx tsc --noEmit` exit 0, `npx vitest run` at Phase 11 baseline (524/526 with 2 pre-existing ComposeBox failures)
- `sidebar/NewSessionDialog.tsx` still exists (PROTECTED); `features/keyboard/**` untouched (PROTECTED)
</verification>

<success_criteria>
- Plan 03 can delete SidebarTree.tsx without tsc-breaking sidebar/NewSessionDialog.tsx
- Plan 04 can delete src/ui/dashboard/ entirely without tsc-breaking CommandPalette.tsx or tabUtils.tsx
- Zero behavior change in NewSessionDialog, CommandPalette, or tabUtils network_graph render
- 3 atomic commits landed, tsc-clean-per-commit, test suite green at Phase 11 baseline
- The retained sidebar/NewSessionDialog.tsx file at PrettyConversationsPanel.tsx:56 import path stays functional
</success_criteria>

<output>
Create `.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-02-SUMMARY.md` on completion.
</output>
