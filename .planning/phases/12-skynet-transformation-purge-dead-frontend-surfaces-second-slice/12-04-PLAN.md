---
phase: 12-skynet-transformation-purge-dead-frontend-surfaces-second-slice
plan: 04
type: execute
wave: 3
depends_on: [12-01, 12-02]
files_modified:
  - src/ui/dashboard/Dashboard.tsx
  - src/ui/dashboard/DashboardTab.tsx
  - src/ui/dashboard/SessionDashboard.tsx
  - src/ui/dashboard/NewSessionDialog.tsx
  - src/ui/dashboard/NewSessionHostChips.tsx
  - src/ui/dashboard/RemoteHostChips.tsx
  - src/ui/dashboard/sshHostToHost.ts
  - src/ui/dashboard/cards/NetworkGraphCard.tsx
  - src/ui/dashboard/cards/ServerOverviewCard.tsx
  - src/ui/dashboard/cards/ServerStatsCard.tsx
  - src/ui/dashboard/cards/RecentActivityCard.tsx
  - src/ui/dashboard/cards/QuickActionsCard.tsx
  - src/ui/dashboard/components/DashboardSettingsDialog.tsx
  - src/ui/dashboard/hooks/useDashboardPreferences.ts
  - src/ui/dashboard/panels/UpdateLog.tsx
  - src/ui/dashboard/panels/alerts/AlertCard.tsx
  - src/ui/dashboard/panels/alerts/AlertManager.tsx
  - src/ui/features/FullScreenAppWrapper.tsx
autonomous: false
requirements: [PURGE-07]

must_haves:
  truths:
    - "src/ui/dashboard/ subtree is deleted from disk"
    - "The 'dashboard' TabType in src/types/ui-types.ts STAYS as load-bearing fallback per Phase 11 preservation"
    - "shell/tabUtils.tsx case 'dashboard' still returns PrettyLandingCard (unchanged from Phase 11 Plan 02)"
    - "shell/tabUtils.tsx case 'network_graph' now returns PrettyLandingCard (from Plan 02 Task 3 refactor)"
    - "shell/CommandPalette.tsx imports 4 files from features/session-launcher (from Plan 02 Task 2 refactor)"
    - "FullScreenAppWrapper.tsx cross-cutting dashboard import resolution: either FullScreenAppWrapper's Dashboard mount is refactored to a warm-glass placeholder, OR FullScreenAppWrapper itself is deleted after audit, OR the whole dashboard/ deletion is scope-fenced with a checkpoint route-back to planner"
  artifacts:
    - path: "src/ui/features/session-launcher/NewSessionDialog.tsx"
      provides: "STAYS — the relocated CommandPalette consumer (created by Plan 02)"
      contains: "export function NewSessionDialog"
  key_links:
    - from: "src/ui/shell/CommandPalette.tsx"
      to: "src/ui/features/session-launcher/*"
      via: "import (relocated by Plan 02)"
      pattern: "@/features/session-launcher/"
    - from: "src/ui/shell/tabUtils.tsx"
      to: "PrettyLandingCard for dashboard + network_graph fallbacks"
      via: "case return"
      pattern: "PrettyLandingCard"
---

<objective>
Delete the entire `src/ui/dashboard/` subtree, including Dashboard.tsx, DashboardTab.tsx, SessionDashboard.tsx, the 4 files that Plan 02 already relocated to features/session-launcher/ (dashboard-side copies now stale duplicates), the whole cards/ + components/ + hooks/ + panels/ subdirs. Resolve the FullScreenAppWrapper.tsx cross-cutting import (line 7: `import { Dashboard } from "@/dashboard/Dashboard.tsx"`) via one of three routes: (a) refactor FullScreenAppWrapper to render a warm-glass placeholder / disable the alternate `/host/<id>` full-screen route, (b) delete FullScreenAppWrapper.tsx if audit confirms it is unreachable, or (c) route back to planner with a checkpoint if Ashley signals FullScreenAppWrapper is load-bearing.

Purpose: Delivers PURGE-07 (`src/ui/dashboard/` deletion). This plan is the largest single-directory sweep in Phase 12 and is the one with genuine cross-cutting concern (FullScreenAppWrapper). Task 1 audits FullScreenAppWrapper before deletion; Task 2 handles it (refactor or delete); Task 3 deletes the entire dashboard/ tree.

Output: 2 or 3 atomic commits — one for FullScreenAppWrapper resolution, one for the dashboard/ subtree deletion, one final SUMMARY commit. tsc-clean per commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-CONTEXT.md
@.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md
@.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Audit FullScreenAppWrapper.tsx consumers and disposition its Dashboard import</name>
  <files>src/ui/features/FullScreenAppWrapper.tsx</files>
  <read_first>
    - src/ui/features/FullScreenAppWrapper.tsx (full read — 149 lines)
    - .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md § Section J (FullScreenAppWrapper cross-cutting concern documented)
    - Grep-verify FullScreenAppWrapper consumers: `grep -rn "FullScreenAppWrapper" src/ --include="*.ts" --include="*.tsx"`
    - Grep-verify Dashboard import consumers OUTSIDE the dashboard/ subtree
  </read_first>
  <action>
    Run these greps and enumerate the observed results:
    1. `grep -rn "FullScreenAppWrapper" src/ --include="*.ts" --include="*.tsx"` — list every consumer (route entry points, other feature-*App.tsx files, etc.)
    2. `grep -rn "Dashboard" src/ --include="*.ts" --include="*.tsx" | grep -v "^src/ui/dashboard/" | grep -v "PrettyLandingCard\|dashboardPreferences" | grep -v "^[^:]*:[[:space:]]*//"` — list every remaining Dashboard consumer OUTSIDE the dying subtree
    3. `grep -rn 'from "@/dashboard/Dashboard"\|from "@/dashboard/Dashboard.tsx"' src/` — pinpoint import statements

    Based on the audit, choose ONE of three dispositions and execute:

    **Disposition A (LIKELY DEFAULT — refactor to placeholder):** FullScreenAppWrapper.tsx currently renders `<Dashboard isAuthenticated={false} ...>` inside the unauthenticated branch (lines 113-119). If audit shows FullScreenAppWrapper is only consumed by feature *App.tsx files (TerminalApp, GuacamoleApp, ServerStatsApp, DockerApp, TunnelApp, FileManagerApp) for the alternate `/host/<id>` full-screen route, and that route serves the unauthenticated Dashboard for login flow, then:
      - Replace the `<Dashboard ...>` render with either an inline `<PrettyLandingCard/>` (from features/pretty-view/) OR a minimal warm-glass placeholder telling the user to log in via the main app entry point.
      - Strip the `import { Dashboard } from "@/dashboard/Dashboard.tsx";` line.
      - Preserve TabProvider, SidebarProvider, CommandHistoryProvider, Toaster wrapping — those come from retained modules.
      - Preserve the authenticated branch (line 134-146) which renders `{children(hostConfig, loading)}` — that's the load-bearing case for the alternate full-screen mount.
      - Commit message: `refactor(FullScreenAppWrapper): replace Dashboard unauthenticated-branch render with pretty-view placeholder (Phase 12 Plan 04 — pre-flight for dashboard/ deletion)`.

    **Disposition B (delete FullScreenAppWrapper entirely):** If audit shows FullScreenAppWrapper is unreachable from any active route (no `<Route path="/host/*">` mount, no import in main.tsx entry), delete FullScreenAppWrapper.tsx AND propagate that deletion to every `*App.tsx` consumer. This is HIGH-BLAST-RADIUS — only choose if the audit is unambiguous (Ashley confirms the /host/<id> route is dead in Skynet). Commit message: `chore(FullScreenAppWrapper): delete unreachable /host/<id> route wrapper (Phase 12 Plan 04)`.

    **Disposition C (route back to planner via checkpoint):** If audit shows FullScreenAppWrapper IS load-bearing for a route Ashley uses, but the exact Dashboard render behavior is unclear, STOP and route back. Add a checkpoint task; the executor waits for Ashley/planner clarification.

    Executor discretion applies. Default expectation: Disposition A is the minimum-risk path. Disposition B is faster if the audit clearly shows no reachable consumer of the /host/<id> route in Skynet. Whichever disposition is chosen, document the reasoning in the commit body.

    Post-task verification:
    - `grep -c "from.*@/dashboard/Dashboard" src/ui/features/FullScreenAppWrapper.tsx` → 0 (import stripped) OR file no longer exists
    - `grep -rn 'from "@/dashboard/Dashboard"' src/` → 0 hits (no other consumer)
    - tsc-clean
  </action>
  <verify>
    <automated>
    ( grep -c 'from "@/dashboard/Dashboard' src/ui/features/FullScreenAppWrapper.tsx 2>/dev/null | grep -qE "^0$" || test ! -f src/ui/features/FullScreenAppWrapper.tsx ) &&
    grep -rn 'from "@/dashboard/Dashboard' src/ --include="*.ts" --include="*.tsx" | wc -l | grep -qE "^0$" &&
    npx tsc --noEmit
    </automated>
  </verify>
  <done>
    FullScreenAppWrapper's dashboard/Dashboard.tsx dependency broken via one of the three dispositions. tsc-clean. Single atomic commit landed. Reasoning documented in commit body.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete src/ui/dashboard/ subtree entirely in one atomic commit</name>
  <files>
    src/ui/dashboard/Dashboard.tsx,
    src/ui/dashboard/DashboardTab.tsx,
    src/ui/dashboard/SessionDashboard.tsx,
    src/ui/dashboard/NewSessionDialog.tsx,
    src/ui/dashboard/NewSessionHostChips.tsx,
    src/ui/dashboard/RemoteHostChips.tsx,
    src/ui/dashboard/sshHostToHost.ts,
    src/ui/dashboard/cards/NetworkGraphCard.tsx,
    src/ui/dashboard/cards/ServerOverviewCard.tsx,
    src/ui/dashboard/cards/ServerStatsCard.tsx,
    src/ui/dashboard/cards/RecentActivityCard.tsx,
    src/ui/dashboard/cards/QuickActionsCard.tsx,
    src/ui/dashboard/components/DashboardSettingsDialog.tsx,
    src/ui/dashboard/hooks/useDashboardPreferences.ts,
    src/ui/dashboard/panels/UpdateLog.tsx,
    src/ui/dashboard/panels/alerts/AlertCard.tsx,
    src/ui/dashboard/panels/alerts/AlertManager.tsx
  </files>
  <read_first>
    - Task 1 landed — FullScreenAppWrapper.tsx no longer imports Dashboard
    - Plan 02 Task 2 landed — 4 dashboard-shared files relocated to features/session-launcher/
    - Plan 02 Task 3 landed — shell/tabUtils.tsx swapped network_graph case
    - Plan 03 Task 1 landed — SessionsPanel.tsx (which imported `@/dashboard/sshHostToHost`) is deleted
    - .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md § Section E (dashboard subtree enumeration)
  </read_first>
  <action>
    Pre-deletion grep gate — enumerate remaining consumers of dashboard/ files:
    - `grep -rn 'from "@/dashboard/\|from "@dashboard/' src/ --include="*.ts" --include="*.tsx" | grep -v "^src/ui/dashboard/" | grep -v "^[^:]*:[[:space:]]*//"` → expected 0 (all cross-tree consumers refactored/relocated in Tasks 1 + Plan 02 + Plan 03)
    - `grep -rn 'NetworkGraphCard\|DashboardTab\|SessionDashboard\|NewSessionHostChips\|RemoteHostChips\|sshHostToHost' src/ --include="*.ts" --include="*.tsx" | grep -v "^src/ui/dashboard/" | grep -v "^src/ui/features/session-launcher/" | grep -v "^[^:]*:[[:space:]]*//" | grep -v "^[^:]*:[[:space:]]*\*"` → expected 0 non-comment hits

    If ANY non-comment survivor exists, STOP and route back to planner. A common miss would be an internal dashboard/ inter-file import through a relative path — but since the whole tree dies together, that's fine.

    Delete the entire subtree:
    ```
    git rm -rf src/ui/dashboard/
    ```
    (Verify no `.test.ts`/`.test.tsx` files are missed — `find src/ui/dashboard -type f` before the rm command captures the exact file list.)

    After deletion, run:
    - `test ! -d src/ui/dashboard` — should pass (directory gone)
    - `test -d src/ui/features/session-launcher` — should PASS (the relocated files STAY)
    - `test -d src/ui/features/keyboard` — should PASS (retained on-screen modifier bar)
    - `test -f src/ui/sidebar/NewSessionDialog.tsx` — should PASS (retained pretty-conversations pencil consumer)
    - `npx tsc --noEmit` — exit 0
    - `npx vitest run` — at Phase 11 baseline (524/526)

    Commit message: `chore(dashboard): delete entire src/ui/dashboard/ subtree (Phase 12 Plan 04 PURGE-07 — 17 files across Dashboard/cards/components/hooks/panels)`.
  </action>
  <verify>
    <automated>
    test ! -d src/ui/dashboard &&
    test -d src/ui/features/session-launcher &&
    test -d src/ui/features/keyboard &&
    test -f src/ui/sidebar/NewSessionDialog.tsx &&
    grep -rn 'from "@/dashboard/' src/ --include="*.ts" --include="*.tsx" | grep -v "^[^:]*:[[:space:]]*//" | grep -v "^[^:]*:[[:space:]]*\*" | grep -v "^[^:]*:[[:space:]]*{/\*" | wc -l | grep -qE "^0$" &&
    grep -rn "DashboardTab\|SessionDashboard\|NewSessionHostChips\|RemoteHostChips" src/ --include="*.ts" --include="*.tsx" | grep -v "^src/ui/features/session-launcher/" | grep -v "^[^:]*:[[:space:]]*//" | grep -v "^[^:]*:[[:space:]]*\*" | grep -v "^[^:]*:[[:space:]]*{/\*" | wc -l | grep -qE "^0$" &&
    npx tsc --noEmit
    </automated>
  </verify>
  <done>
    src/ui/dashboard/ directory gone; features/session-launcher + features/keyboard + sidebar/NewSessionDialog PRESERVED; zero cross-tree dashboard/ import survivors; tsc-clean; single atomic commit landed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FullScreenAppWrapper cross-cut | The wrapper is imported by 6-7 feature *App.tsx files. Refactor vs delete choice has BIG blast radius. Task 1 audits and picks disposition deliberately. |
| dashboard/ tree ↔ TabType union | `src/types/ui-types.ts` keeps `dashboard`, `network_graph`, `host-manager`, `user-profile`, `admin-settings` TabTypes. This plan does NOT strip those from the union — they stay for tabUtils exhaustive-switch safety. Future Phase 13 can revisit. |
| session-launcher/ preservation | Plan 02 Task 2 created features/session-launcher/. This plan MUST NOT delete session-launcher/. Verify gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-01 | Tampering | Wrong FullScreenAppWrapper disposition breaks /host/<id> route | mitigate | Task 1 audit greps enumerate every consumer. Executor documents reasoning in commit body. Default disposition (A - refactor to placeholder) is minimum-risk. Post-deploy Ashley UAT catches regression. |
| T-12-04-02 | Tampering | features/session-launcher/ accidentally deleted (path confusion with dashboard/) | mitigate | Task 2 verify includes `test -d src/ui/features/session-launcher` PASS gate. `git rm -rf src/ui/dashboard/` targets specifically dashboard/, not session-launcher/. |
| T-12-04-03 | Tampering | features/keyboard/ accidentally deleted (path confusion) | mitigate | Task 2 verify includes `test -d src/ui/features/keyboard` PASS gate. |
| T-12-04-04 | Tampering | sidebar/NewSessionDialog.tsx accidentally deleted | mitigate | Task 2 verify includes `test -f src/ui/sidebar/NewSessionDialog.tsx` PASS gate. The two NewSessionDialog files (sidebar vs dashboard) have different paths — `git rm -rf src/ui/dashboard/` deletes the dashboard version only. |
| T-12-04-05 | Tampering | Missed transitive dashboard consumer | mitigate | Task 2 pre-deletion grep gate enumerates cross-tree imports. If a survivor exists, task ROUTES BACK to planner. |
| T-12-04-06 | Denial of service | Dashboard chunk in bundle after deletion | accept | Rolldown code-splitting will drop the async chunk; bundle size shrinks as recorded in Plan 07 build verify log. Expected further shrink from Phase 11 baseline 75.43 kB AppShell. |
| T-12-04-07 | Tampering | Package installs | accept | Zero installs. |
| T-12-04-SC | Tampering | npm/pip/cargo installs | accept | Zero installs. |
</threat_model>

<verification>
- Task 1: FullScreenAppWrapper Dashboard import broken via disposition A/B/C; tsc-clean; committed
- Task 2: `test ! -d src/ui/dashboard` passes; session-launcher + keyboard + sidebar/NewSessionDialog PRESERVED; zero cross-tree survivors; tsc-clean; committed
- Full `npx vitest run` at Phase 11 baseline
- 2 atomic commits landed
</verification>

<success_criteria>
- src/ui/dashboard/ subtree entirely gone (17+ files)
- FullScreenAppWrapper cross-cut resolved (refactor or delete)
- src/ui/features/session-launcher/ PRESERVED (Plan 02 output)
- src/ui/features/keyboard/ PRESERVED (retained on-screen modifier bar)
- src/ui/sidebar/NewSessionDialog.tsx PRESERVED (retained pretty-conversations pencil)
- tsc-clean, vitest baseline
- PURGE-07 delivered
</success_criteria>

<output>
Create `.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-04-SUMMARY.md` on completion.
</output>
