---
phase: 12-skynet-transformation-purge-dead-frontend-surfaces-second-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md
autonomous: true
requirements: [PURGE-06, PURGE-07, PURGE-08, PURGE-09, PURGE-10]

must_haves:
  truths:
    - "Every deletion target in Phase 12 has a grep-verified consumer analysis before any source file is deleted"
    - "The retained-UI import graph (pretty-conversations, pretty-view, terminal, guacamole, CommandPalette, TabContext, SplitView, tabUtils) is explicitly enumerated so no deletion plan crosses it"
    - "sidebar/NewSessionDialog.tsx protection: enumerated as KEEP with its full import chain audited"
    - "src/ui/features/keyboard/* protection: enumerated as KEEP — it is on-screen modifier bar for Terminal + GuacamoleApp, NOT the shortcut editor UI"
    - "Every dead nav.* locale key + pinAppRail is enumerated with grep-verified 0 code consumers"
    - "Pre-flight refactor targets are enumerated (Plan 02 input contract): inline isFolder into NewSessionDialog, relocate 4 dashboard-shared files consumed by CommandPalette, replace tabUtils network_graph render"
    - "PURGE-09 resolution in Section G names BOTH the writer (UserProfilePanel commandPaletteShortcutEnabled toggle) AND the reader (AppShell.tsx state variable + gate expression + effect dep + storage-event listener) — Plan 03 Task 1 retires both in the same atomic commit; no orphan reader remains"
    - "Deletion-order dependencies are explicit: which files must be pre-refactored (Plan 02) vs which can be deleted in parallel (Plans 03/04/05)"
  artifacts:
    - path: ".planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md"
      provides: "Authoritative deletion-target contract for Plans 02-06"
      contains: "Section A Sidebar simple leaves, Section B Admin subtree, Section C HostManager subtree, Section D SidebarTree, Section E Dashboard subtree, Section F Tab bar chrome (shell/Tab.tsx), Section G Keyboard shortcut editor UI resolution (writer+reader pair including AppShell.tsx teardown scope), Section H Locale strip list, Section I Pre-flight refactor targets, Section J Retained-UI protection list, Section K Verification gates"
      min_lines: 300
  key_links:
    - from: "12-01-STRIP-LIST.md § Section I (pre-flight refactor)"
      to: "Plan 02 (12-02-PLAN.md)"
      via: "consumed as input contract"
      pattern: "Section I"
    - from: "12-01-STRIP-LIST.md § Sections A-D (sidebar) + § Section J (retained protection)"
      to: "Plan 03 (12-03-PLAN.md)"
      via: "consumed as deletion-target contract"
      pattern: "Section A|Section B|Section C|Section D|Section J"
    - from: "12-01-STRIP-LIST.md § Section E (dashboard)"
      to: "Plan 04 (12-04-PLAN.md)"
      via: "consumed as deletion-target contract"
      pattern: "Section E"
    - from: "12-01-STRIP-LIST.md § Section F (tab bar chrome)"
      to: "Plan 05 (12-05-PLAN.md)"
      via: "consumed as deletion-target contract"
      pattern: "Section F"
    - from: "12-01-STRIP-LIST.md § Section H (locale strip)"
      to: "Plan 06 (12-06-PLAN.md)"
      via: "consumed as deletion-target contract"
      pattern: "Section H"
    - from: "12-01-STRIP-LIST.md § Section K (verification)"
      to: "Plan 07 (12-07-PLAN.md)"
      via: "consumed as phase-boundary verification gates"
      pattern: "Section K"
---

<objective>
Produce the authoritative Phase 12 strip-list document `12-01-STRIP-LIST.md` that enumerates every deletion target (sidebar panels, Admin subtree, HostManager subtree, SidebarTree, dashboard/ subtree, shell/Tab.tsx, dead locale keys), the pre-flight refactor targets that Plan 02 must execute BEFORE any deletion plan runs (inline isFolder, relocate 4 dashboard-shared files consumed by CommandPalette, replace tabUtils network_graph render), and the retained-UI protection list that every downstream plan must respect. Every deletion claim is grep-verified against the working tree; every non-zero grep count is explained.

Purpose: Phase 11 established the enumeration-first pattern via `11-01-STRIP-LIST.md`; Phase 12 mirrors that shape verbatim so Plans 02-06 consume a single authoritative contract and do NOT re-derive targets from source. This is a read-only audit — zero source-tree modifications.

Output: `12-01-STRIP-LIST.md` at ~350-500 lines organized into 11 sections (A through K) each with grep-verified evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-CONTEXT.md
@.planning/phases/11-skynet-transformation-purge-dead-skynet-surfaces-first-slice/11-01-STRIP-LIST.md
@.planning/phases/11-skynet-transformation-purge-dead-skynet-surfaces-first-slice/11-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 12-01-STRIP-LIST.md with grep-verified enumeration of every Phase 12 deletion target + pre-flight refactor + retained-UI protection list</name>
  <files>.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md</files>
  <read_first>
    - .planning/phases/11-skynet-transformation-purge-dead-skynet-surfaces-first-slice/11-01-STRIP-LIST.md (mirror this shape verbatim)
    - .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-CONTEXT.md (LOCKED scope + Specific Ideas)
    - .planning/REQUIREMENTS.md § Dead-Surfaces Purge — Second Slice (PURGE-06..PURGE-10 wording)
    - src/ui/AppShell.tsx (verify no residual imports of the deletion targets remain post-Phase-11)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (retained-UI import graph — MUST NOT break)
    - src/ui/sidebar/NewSessionDialog.tsx (PROTECTED — imports `isFolder` from SidebarTree; enumerate its full transitive import chain)
    - src/ui/shell/CommandPalette.tsx (RETAINED — imports 4 files from src/ui/dashboard/: NewSessionDialog, sshHostToHost, RemoteHostChips, NewSessionHostChips; enumerate the relocate-required set)
    - src/ui/shell/tabUtils.tsx (RETAINED — imports NetworkGraphCard from src/ui/dashboard/cards/; enumerate the replace-required render case)
    - src/ui/features/keyboard/ (RETAINED — enumerate all 5 files: Toolbar.tsx, sshAdapter.ts, guacamoleAdapter.ts, inputAdapter.ts, sshAdapter.test.ts; grep-verify each is consumed by Terminal.tsx or GuacamoleApp.tsx and NOT the shortcut-editor UI referenced in PURGE-09)
    - src/ui/locales/en.json (source of truth for nav.* keys)
  </read_first>
  <action>
    Author `.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md` with the following exact 11-section structure. Every claim MUST cite the exact grep command + observed line count/hit list at authoring time. Mirror Phase 11's shape verbatim (11-01-STRIP-LIST.md is the shape reference).

    **Section A: Sidebar simple-leaf panel files.** Enumerate each of the 12 sidebar panel files that Phase 11 stripped imports of (HostsPanel, SessionsPanel, CredentialsPanel, QuickConnectPanel, SshToolsPanel, SnippetsPanel, HistoryPanel, SplitScreenPanel, ConnectionsPanel, UserProfilePanel, plus their file paths). For each: (a) exact file path, (b) `wc -l` line count, (c) grep command `grep -rn "from.*sidebar/<Name>\|from.*'./<Name>'" src/ --include="*.ts" --include="*.tsx"` with observed hit count and file list (must be 0 non-comment code hits for safe deletion after Phase 11's strip), (d) confirmation the identifier does NOT appear in retained UI (pretty-conversations, pretty-view, terminal, guacamole, CommandPalette, TabContext, SplitView, tabUtils). Include a table with columns `# | File | Lines | Non-comment consumers | Test file present?`. Also enumerate: `.test.tsx` sibling files (delete alongside), any files imported ONLY within the panel subtree (transitive orphans).

    **Section B: Admin subtree files.** Enumerate AdminSettingsPanel.tsx, AdminApiKeysSection.tsx, AdminIdentitiesSection.tsx, AdminManagementSections.tsx, AdminSettingsSections.tsx, AdminSettingsShared.tsx, AdminUserDialogs.tsx. Same evidence format as Section A. Grep-verify inter-section import chain: which sections import from which — deletion within the same commit is required to preserve tsc-clean intermediate state.

    **Section C: HostManager subtree files.** Enumerate HostManager.tsx, HostManagerData.ts, HostManagerTabs.tsx, HostShareModal.tsx, HostEditor.tsx, HostEditorData.ts, HostEditorFeatureTabs.tsx, HostEditorGeneralTab.tsx, HostEditorGuacamoleTabs.tsx, HostEditorStatsTab.tsx, HostCredentialList.tsx, CredentialEditorView.tsx. Same evidence format. Enumerate inter-file imports.

    **Section D: SidebarTree.tsx handling.** SidebarTree.tsx exports `isFolder`, `HostItem`, `FolderItem`, `SidebarTree` and a helper `collectAllHosts`. Grep-verify consumers: HostsPanel (dying with Section A), NewSessionDialog (PROTECTED — imports `isFolder`). Enumerate the exact line where NewSessionDialog imports `isFolder` (currently line 38: `import { isFolder } from "@/sidebar/SidebarTree";`). Enumerate every export in SidebarTree.tsx and every consumer of each export. Cite Plan 02 §I item 1 as the pre-flight refactor that enables SidebarTree deletion (inline `isFolder` — 3-line helper — into NewSessionDialog.tsx).

    **Section E: src/ui/dashboard/ subtree.** Enumerate every file: Dashboard.tsx, DashboardTab.tsx, SessionDashboard.tsx, NewSessionDialog.tsx (dashboard version — DIFFERENT from sidebar/NewSessionDialog.tsx; do NOT confuse), NewSessionHostChips.tsx, RemoteHostChips.tsx, sshHostToHost.ts, plus every file under cards/ (NetworkGraphCard, ServerOverviewCard, ServerStatsCard, RecentActivityCard, QuickActionsCard), components/ (DashboardSettingsDialog), hooks/ (useDashboardPreferences), panels/ (UpdateLog + alerts/AlertCard + alerts/AlertManager). For each: line count + non-comment consumer grep + inter-file dependency map. Explicitly enumerate the 4 files consumed by RETAINED UI (dashboard/NewSessionDialog.tsx, sshHostToHost.ts, RemoteHostChips.tsx, NewSessionHostChips.tsx via CommandPalette + dashboard/cards/NetworkGraphCard.tsx via tabUtils) with their exact retained-consumer file:line references. Route these 4 files to Plan 02 §I as pre-flight refactor / relocate targets. Only after Plan 02 relocates them can dashboard/ be safely deleted.

    **Section F: shell/Tab.tsx (Skynet tab bar chrome).** Grep-verify: `grep -rn "from.*\"@/shell/Tab\.tsx\"\|from.*\"@/shell/Tab\"\|from.*\"./Tab\"" src/` returns 0 hits — the file is already orphaned by Phase 11's landing swap (Skynet tab strip was never mounted in Skynet's AppShell post-Phase-11). Delete safely. Enumerate the file's line count and confirm no sibling test file. Note: shell/TabContext.tsx (different file), shell/CommandPalette.tsx, shell/SplitView.tsx, shell/tabUtils.tsx STAY (retained by AppShell + other consumers — see Section J).

    **Section G: PURGE-09 "keyboard shortcut editor UI" resolution.** Grep-verify: `src/ui/features/keyboard/` contains 5 files (Toolbar.tsx, sshAdapter.ts, sshAdapter.test.ts, guacamoleAdapter.ts, inputAdapter.ts). Every file's consumer grep shows only Terminal.tsx and GuacamoleApp.tsx (retained UI on-screen modifier bar for RDP + tmux). This subtree is RETAINED-UI. The "shortcut editor UI" referenced in PURGE-09 CONTEXT.md item 4 resolves to the `commandPaletteShortcutEnabled` toggle inside UserProfilePanel.tsx (dies as part of Section A) AND its READER half inside `src/ui/AppShell.tsx` (state variable at lines 282-286, gate expression at line 343, effect dependency at line 350, storage-event listener useEffect at lines 352-363). PURGE-09 is delivered FULLY as a side-effect of PURGE-06 only when BOTH the writer (UserProfilePanel toggle at lines 489-492 read + 1025-1036 setter) AND the reader (AppShell state + gate + listener) retire in the SAME atomic commit. Document this resolution explicitly so the executor: (a) does not attempt to delete `features/keyboard/` and break Terminal + Guacamole, and (b) understands that Plan 03 Task 1 tears out the AppShell reader half in the same commit as the UserProfilePanel deletion — no intermediate commit may have an orphaned reader without a writer. Enumerate the exact AppShell line numbers (with a drift-safe note to executor: re-grep `grep -n "commandPaletteShortcutEnabled" src/ui/AppShell.tsx` before editing since surrounding shell code may have shifted). Also note: the CommandPalette double-shift open behavior is RETAINED unconditionally after the reader teardown — Ashley never disabled it, toggle was default-true, only reachable through UserProfilePanel which no longer exists; the gate becomes hardcoded-true which is equivalent to removing it. General pattern: this Section G resolution establishes the "orphan-reader-after-writer-death" discipline for future deletion phases — when deleting a file that WRITES to shared state (localStorage / IPC event / module singleton / atom / store), the planner MUST grep for READERS of the same state and either delete them in the same atomic commit or explicitly retain them with a hardcoded default that survives the writer's absence.

    **Section H: Dead locale strings.** Enumerate every candidate key with its consumer analysis. Run for each candidate: `grep -rn "t(\"nav\.<key>\"" src/ --include="*.ts" --include="*.tsx"` (also match `t('nav.<key>'`). Candidates: `nav.dashboard`, `nav.hosts`, `nav.snippets`, `nav.admin`, `nav.credentials`, `nav.history`, `nav.hostManager`, `nav.sessions`, `nav.splitScreen`, `nav.userProfile`, `nav.connections`, `nav.quickConnect`, `nav.sshTools`, `nav.networkGraph`, `nav.home`, plus `pinAppRail`, `pinAppRailDesc`. For each, record: (a) code consumer count at authoring, (b) code consumer count PROJECTED after Plan 03/04/05 deletion (should be 0), (c) safe-to-delete status. Group keys into removal batches (one batch = one commit): (batch-1) AppRail-adjacent (`pinAppRail`, `pinAppRailDesc`); (batch-2) dead nav.* keys with post-deletion 0-consumer status. Enumerate the 34 locale JSON files: `src/ui/locales/en.json` (source) + 33 files under `src/ui/locales/translated/*.json` (translated). Provide the exact `ls src/ui/locales/translated/*.json | wc -l` output as evidence for the 33 count. Note: keys with surviving retained-UI consumers (nav.home consumed by shell/TabContext.tsx that STAYS, nav.terminal, nav.serverStats, nav.fileManager, nav.docker, nav.tunnels, nav.newSession, nav.newSessionTitle, nav.newSessionDescription, nav.newSessionSearchHosts, nav.newSessionNameLabel, nav.newSessionNamePlaceholder, nav.newSessionNameError, nav.newSessionNoHosts, nav.newSessionHostList, nav.conversations, nav.close, nav.cancel, nav.confirmClose, nav.cannotSplitTab, nav.hostTabTitle, nav.copyPassword, nav.copySudoPassword, nav.passwordCopied, nav.noPasswordAvailable, nav.failedToCopyPassword, nav.refreshTab, nav.openFileManager, nav.roleAdministrator, nav.roleUser) STAY. Verify each STAYS-consumer with a grep hit list. Note: `nav.admin`, `nav.userProfile`, `nav.splitScreen` are currently ONLY consumed by shell/Tab.tsx (dying in Section F); after Section F deletion, they become safe to strip.

    **Section I: Pre-flight refactor targets (Plan 02 input contract).** Three refactors are required BEFORE any deletion plan runs, so that deletion plans stay tsc-clean per commit.
      (I.1) Inline `isFolder` (3-line type-guard) from SidebarTree.tsx into sidebar/NewSessionDialog.tsx. After refactor, NewSessionDialog imports zero symbols from `@/sidebar/SidebarTree`. Enumerate the exact 3-line isFolder body at SidebarTree.tsx:54-56.
      (I.2) Relocate the 4 dashboard-shared files consumed by CommandPalette (retained) to a NEW retained-UI location `src/ui/features/session-launcher/`. Files: `dashboard/NewSessionDialog.tsx → features/session-launcher/NewSessionDialog.tsx`, `dashboard/sshHostToHost.ts → features/session-launcher/sshHostToHost.ts`, `dashboard/RemoteHostChips.tsx → features/session-launcher/RemoteHostChips.tsx`, `dashboard/NewSessionHostChips.tsx → features/session-launcher/NewSessionHostChips.tsx`. Enumerate CommandPalette.tsx's exact import lines (currently 9, 10, 11-14, 15-19). After refactor, CommandPalette imports 4 files from `@/features/session-launcher/`. Also enumerate the sidebar/SessionsPanel.tsx dashboard/sshHostToHost import (line 7) — dies with SessionsPanel deletion, so no relocate needed for that consumer. Explicit note: the SIDEBAR NewSessionDialog.tsx (PROTECTED, consumed by pretty-conversations) is DIFFERENT from the dashboard NewSessionDialog.tsx being relocated. Preserve both file identities.
      (I.3) Replace the `network_graph` render case in shell/tabUtils.tsx (currently line 278-279: `case "network_graph": return <NetworkGraphCard embedded={false} />`) with a PrettyLandingCard fallback (mirror the Phase 11 dashboard-swap pattern). Strip the `NetworkGraphCard` import at line 28. Preserve the `case "network_graph":` line in tabIcon at line 113-114 (the Network icon consumer doesn't reach dashboard/cards/) or evaluate whether to null-return. Enumerate the exact edits.

    **Section J: Retained-UI protection list.** Enumerate every file that MUST NOT be touched by any Phase 12 plan, with a one-line rationale. Include:
      - `src/ui/features/pretty-conversations/**` (retained visible list surface)
      - `src/ui/features/pretty-view/**` (retained visible chat surface)
      - `src/ui/features/terminal/**` (retained tmux terminal renderer)
      - `src/ui/features/guacamole/**` (retained RDP/VNC surface)
      - `src/ui/features/keyboard/**` (retained on-screen modifier bar for Terminal + Guacamole — NOT the shortcut editor UI; see Section G)
      - `src/ui/features/file-manager/**`, `src/ui/features/server-stats/**`, `src/ui/features/docker/**`, `src/ui/features/tunnel/**`, `src/ui/features/sessions/**`, `src/ui/features/FullScreenAppWrapper.tsx` (retained tab renderers behind TabType) — note: FullScreenAppWrapper imports dashboard/Dashboard.tsx; document this as a Phase 13 concern (FullScreenAppWrapper only serves the alternate `/host/<id>` full-screen route, which Ashley may not use in Skynet — but backend routing is Phase 13)
      - `src/ui/shell/CommandPalette.tsx` (retained double-shift fleet search)
      - `src/ui/shell/TabContext.tsx` (retained tab state provider used by CommandHistoryProvider + retained tab renderers)
      - `src/ui/shell/SplitView.tsx` (retained split-view mechanism used by AppShell)
      - `src/ui/shell/tabUtils.tsx` (retained tab render dispatcher used by AppShell)
      - `src/ui/sidebar/NewSessionDialog.tsx` (retained — pretty-conversations pencil consumer; PROTECTED CRITICALLY — deletion in error would break TG-09 new-session flow)
      - `src/ui/sidebar/NewSessionDialog.test.tsx` (test file for the retained NewSessionDialog)
      - `src/backend/**` (Phase 13 territory)
      - `src/types/ui-types.ts` (the `dashboard` TabType at line 162 + 234 stays as load-bearing fallback per Phase 11 preservation; the `network_graph`, `host-manager`, `user-profile`, `admin-settings` TabTypes stay because tabUtils switch cases still list them — evaluate for future strip in Phase 13)
      - `src/ui/AppShell.tsx` (retained shell — only touched by Plan 02 for the tabUtils network_graph fallback wiring if applicable; NOT touched by any deletion plan in Phase 12)
      - Note: the FullScreenAppWrapper → Dashboard chain is a genuine cross-cutting concern. Document explicitly: FullScreenAppWrapper.tsx imports Dashboard from `@/dashboard/Dashboard.tsx`. If dashboard/ is deleted, FullScreenAppWrapper breaks. Resolution options: (a) delete FullScreenAppWrapper too (need Phase 13 audit — check if backend route /host/<id> full-screen is used), (b) refactor FullScreenAppWrapper to render a warm-glass placeholder, (c) defer entire dashboard/ deletion to Phase 13. Plan 04 executor decides based on Section J enumeration.

    **Section K: Verification gates (Plan 07 input contract).** Enumerate the exact grep + toolchain commands Plan 07 runs at the phase boundary:
      - `test ! -f src/ui/sidebar/<each Section A/B/C file>` — expected pass for every deleted file
      - `test ! -f src/ui/sidebar/SidebarTree.tsx` — expected pass
      - `find src/ui/dashboard -type f | wc -l` — expected 0 (or documented count of retained-under-different-decision files)
      - `test ! -f src/ui/shell/Tab.tsx` — expected pass
      - `test -f src/ui/sidebar/NewSessionDialog.tsx` — expected PASS (PROTECTED)
      - `test -d src/ui/features/keyboard` — expected PASS (retained)
      - `grep -rn "HostsPanel\|SessionsPanel\|CredentialsPanel\|QuickConnectPanel\|SshToolsPanel\|SnippetsPanel\|HistoryPanel\|SplitScreenPanel\|ConnectionsPanel\|UserProfilePanel\|AdminSettingsPanel\|AdminApiKeysSection\|AdminIdentitiesSection\|AdminManagementSections\|AdminSettingsSections\|AdminSettingsShared\|AdminUserDialogs" src/ --include="*.ts" --include="*.tsx" | grep -v "^[^:]*:[[:space:]]*//" | grep -v "^[^:]*:[[:space:]]*\*"` — expected 0 non-comment code hits
      - Same repeated for HostManager, HostEditor, HostShareModal, HostCredentialList, CredentialEditorView, SidebarTree, DashboardTab, Dashboard, SessionDashboard, NewSessionHostChips, RemoteHostChips, sshHostToHost, NetworkGraphCard
      - `grep -rn "sidebar/NewSessionDialog" src/ | grep -v "\.md$"` — expected >0 (PROTECTED — used by pretty-conversations)
      - `grep -rn "features/keyboard/Toolbar\|features/keyboard/sshAdapter\|features/keyboard/guacamoleAdapter\|features/keyboard/inputAdapter" src/` — expected >0 (retained by Terminal + Guacamole)
      - `grep -rn "commandPaletteShortcutEnabled" src/ --include="*.ts" --include="*.tsx" | grep -v "^[^:]*:[[:space:]]*//" | grep -v "^[^:]*:[[:space:]]*\*"` — expected 0 non-comment code hits (writer AND reader both retired by Plan 03 Task 1's atomic commit; PURGE-09 full delivery gate)
      - `grep -rn "commandPaletteShortcutEnabledChanged" src/ --include="*.ts" --include="*.tsx" | grep -v "^[^:]*:[[:space:]]*//" | grep -v "^[^:]*:[[:space:]]*\*"` — expected 0 non-comment code hits (dead event name for the retired writer/reader pair)
      - `grep -cE "lastShiftTime" src/ui/AppShell.tsx` — expected >=2 (retained double-shift detector; guards against Task 1's AppShell surgery accidentally removing the CommandPalette open path)
      - For each removed locale key, `grep -rn "\"<key>\"" src/ui/locales/*.json src/ui/locales/translated/*.json | wc -l` — expected 0
      - For each removed locale key, `grep -rn "t(\"<key>\"" src/ --include="*.ts" --include="*.tsx"` — expected 0
      - `npx tsc --noEmit` — expected exit 0
      - `npx vitest run` — expected pass at Phase 11 baseline (524/526 or better; the 2 pre-existing ComposeBox failures ARE inherited baseline, not new)
      - `npm run build` — expected exit 0
      - Bundle-size headline: capture AppShell chunk size delta vs Phase 11 tip (Phase 11 = 75.43 kB); Phase 12 expected further shrink from Rolldown code-splitting reduction as the now-fully-deleted files no longer contribute to the async-chunk graph.

    Every non-zero grep count in Sections A-H is either: (i) inside a `//` line comment or `{/* */}` JSX block-comment per Phase 10 Wave 4 policy (acceptable historical annotations, no code-hit gate violation), (ii) a live retained-UI consumer requiring a Plan 02 pre-flight refactor (per Section I), or (iii) a scope-fence violation requiring route-back to planner. No hit is left unclassified.

    Do NOT modify any source file — this is a discovery + documentation pass ONLY. `git status src/` MUST return clean after this task.
  </action>
  <verify>
    <automated>
    test -f .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md &&
    grep -cE "^## Section" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md &&
    grep -qE "^## Section A\b" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md &&
    grep -qE "^## Section B\b" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md &&
    grep -qE "^## Section C\b" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md &&
    grep -qE "^## Section D\b" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md &&
    grep -qE "^## Section E\b" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md &&
    grep -qE "^## Section F\b" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md &&
    grep -qE "^## Section G\b" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md &&
    grep -qE "^## Section H\b" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md &&
    grep -qE "^## Section I\b" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md &&
    grep -qE "^## Section J\b" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md &&
    grep -qE "^## Section K\b" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md &&
    grep -q "sidebar/NewSessionDialog" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md &&
    grep -q "features/keyboard" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md &&
    grep -q "isFolder" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md &&
    grep -q "CommandPalette" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md &&
    grep -q "NetworkGraphCard" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md &&
    grep -qE "shell/Tab\.tsx" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md &&
    grep -q "PURGE-06" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md &&
    grep -q "PURGE-07" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md &&
    grep -q "PURGE-08" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md &&
    grep -q "PURGE-09" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md &&
    grep -q "PURGE-10" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md &&
    grep -q "commandPaletteShortcutEnabled" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md &&
    grep -q "AppShell" .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md &&
    git status --porcelain src/ | wc -l | grep -qE "^0$"
    </automated>
  </verify>
  <done>
    12-01-STRIP-LIST.md exists at expected path with 11 sections (A-K), every deletion target has grep-verified evidence, sidebar/NewSessionDialog + features/keyboard/ both explicitly enumerated as PROTECTED, pre-flight refactor targets in Section I are named with exact file paths, retained-UI list in Section J is complete, Section K verification gates are Plan 07 ready. Zero source-tree modifications (`git status src/` clean).
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit strip-list + plan metadata as one doc-only atomic commit</name>
  <files>.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md</files>
  <read_first>
    - .planning/phases/11-skynet-transformation-purge-dead-skynet-surfaces-first-slice/11-01-SUMMARY.md (mirror the doc-only commit pattern from Phase 11 Plan 01)
  </read_first>
  <action>
    Stage `.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md` (and the plan's SUMMARY.md if the orchestrator already emitted it) via `git add`. Commit with message `docs(12-01): enumerate Phase 12 deletion targets + pre-flight refactor set + retained-UI protection list`. Zero source-tree files in the diff — verify via `git diff --cached --name-only` before commit.
  </action>
  <verify>
    <automated>
    git log -1 --format="%s" | grep -qE "^docs\(12-01\)" &&
    git show --name-only HEAD | grep -qE "12-01-STRIP-LIST\.md" &&
    git show --name-only HEAD | grep -vE "^src/" | wc -l | grep -qE "^[1-9]"
    </automated>
  </verify>
  <done>
    Single atomic doc-only commit landed with the strip-list. No src/ files in the diff. Commit message matches the `docs(12-01):` convention. HEAD carries the STRIP-LIST + optional SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planner-to-executor doc contract | The strip-list becomes the input contract for Plans 02-06; a mis-enumeration here cascades into 5 downstream plans producing wrong deletions or missing pre-flight refactors. |
| Planner-to-executor retained-UI graph | Section J lists what MUST NOT be deleted; a missing entry here allows a downstream plan to break retained UI (pretty-conversations, terminal, guacamole). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | Tampering | 12-01-STRIP-LIST.md — pre-flight refactor omission | mitigate | Every retained-UI consumer of a to-be-deleted file is explicitly enumerated in Section I with the exact refactor step (inline isFolder, relocate 4 files, replace network_graph render). Executor cross-checks Section I against grep evidence in Sections D + E before authoring. |
| T-12-01-02 | Tampering | 12-01-STRIP-LIST.md — NewSessionDialog protection missing | mitigate | Section J lists `sidebar/NewSessionDialog.tsx` as CRITICALLY PROTECTED with rationale (pretty-conversations pencil consumer, breaks TG-09). Section K includes a `grep -rn "sidebar/NewSessionDialog" src/` gate expected >0. Both plan and executor call out the file-name collision with `dashboard/NewSessionDialog.tsx` (different file, dies in Phase 12) to prevent confusion. |
| T-12-01-03 | Tampering | 12-01-STRIP-LIST.md — features/keyboard/ mis-identified as PURGE-09 target | mitigate | Section G explicitly resolves PURGE-09 to "no standalone file exists" via grep-verified analysis that features/keyboard/ 5 files are all consumed by Terminal + GuacamoleApp (retained UI). Executor MUST include the Toolbar.tsx + sshAdapter.ts + guacamoleAdapter.ts + inputAdapter.ts consumer greps as evidence. |
| T-12-01-04 | Tampering | 12-01-STRIP-LIST.md — locale key over-strip | mitigate | Section H enumerates SURVIVING nav.* keys with retained-UI consumer greps (nav.home for TabContext, nav.terminal + nav.serverStats etc. for TabContext computeUniqueTitle + tabUtils EmptyState renderers, nav.newSession* for NewSessionDialog); dead-key list is scoped to keys with 0 code consumers post-deletion. Section K gate `grep -rn "t(\"<key>\"" src/` = 0 is the load-bearing verification. |
| T-12-01-05 | Information disclosure | 12-01-STRIP-LIST.md — leaks internal file paths | accept | Strip-list contents same trust class as Phase 11 STRIP-LIST (already in the planning tree). No production info disclosed. |
| T-12-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (doc-only). |
</threat_model>

<verification>
- Strip-list authored with 11 sections (A-K) per template
- Every claim grep-verified with exact command + observed hit list
- Pre-flight refactor set (Section I) is complete and cites exact file paths + line numbers
- Retained-UI protection list (Section J) explicitly enumerates sidebar/NewSessionDialog.tsx and features/keyboard/
- Verification gates (Section K) are Plan 07 phase-boundary ready
- Zero source-tree modifications (`git status src/` returns clean)
- Doc-only atomic commit landed on `feat/tab-title-from-tmux` branch
</verification>

<success_criteria>
- `12-01-STRIP-LIST.md` file exists and validates against the 11-section template
- Every downstream Plan (02-06) can consume Sections A-I as its input contract without re-deriving deletion targets from source
- Plan 07 (build verify + UAT) has an authoritative Section K verification-gate list
- `sidebar/NewSessionDialog.tsx` and `features/keyboard/**` are unambiguously flagged as PROTECTED
- `dashboard/NewSessionDialog.tsx` (different file) is unambiguously flagged as DELETION target after Plan 02 relocates it
- Locale key strip list (Section H) has grep-verified 0-code-consumer status per key with clear batch groupings for Plan 06
</success_criteria>

<output>
Create `.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-SUMMARY.md` on completion.
</output>
