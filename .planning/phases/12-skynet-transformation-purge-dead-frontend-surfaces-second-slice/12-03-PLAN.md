---
phase: 12-skynet-transformation-purge-dead-frontend-surfaces-second-slice
plan: 03
type: execute
wave: 3
depends_on: [12-01, 12-02]
files_modified:
  - src/ui/sidebar/HostsPanel.tsx
  - src/ui/sidebar/SessionsPanel.tsx
  - src/ui/sidebar/CredentialsPanel.tsx
  - src/ui/sidebar/QuickConnectPanel.tsx
  - src/ui/sidebar/SshToolsPanel.tsx
  - src/ui/sidebar/SnippetsPanel.tsx
  - src/ui/sidebar/HistoryPanel.tsx
  - src/ui/sidebar/SplitScreenPanel.tsx
  - src/ui/sidebar/ConnectionsPanel.tsx
  - src/ui/sidebar/UserProfilePanel.tsx
  - src/ui/sidebar/AdminSettingsPanel.tsx
  - src/ui/sidebar/AdminApiKeysSection.tsx
  - src/ui/sidebar/AdminIdentitiesSection.tsx
  - src/ui/sidebar/AdminManagementSections.tsx
  - src/ui/sidebar/AdminSettingsSections.tsx
  - src/ui/sidebar/AdminSettingsShared.tsx
  - src/ui/sidebar/AdminUserDialogs.tsx
  - src/ui/sidebar/HostManager.tsx
  - src/ui/sidebar/HostManagerData.ts
  - src/ui/sidebar/HostManagerTabs.tsx
  - src/ui/sidebar/HostShareModal.tsx
  - src/ui/sidebar/HostEditor.tsx
  - src/ui/sidebar/HostEditorData.ts
  - src/ui/sidebar/HostEditorFeatureTabs.tsx
  - src/ui/sidebar/HostEditorGeneralTab.tsx
  - src/ui/sidebar/HostEditorGuacamoleTabs.tsx
  - src/ui/sidebar/HostEditorStatsTab.tsx
  - src/ui/sidebar/HostCredentialList.tsx
  - src/ui/sidebar/CredentialEditorView.tsx
  - src/ui/sidebar/SidebarTree.tsx
autonomous: true
requirements: [PURGE-06, PURGE-09]

must_haves:
  truths:
    - "Every sidebar panel file enumerated in 12-01-STRIP-LIST Sections A/B/C is deleted from disk"
    - "SidebarTree.tsx is deleted after Plan 02's inline-isFolder refactor lands"
    - "sidebar/NewSessionDialog.tsx STAYS on disk with all its tests still passing"
    - "sidebar/NewSessionDialog.test.tsx STAYS on disk with tests still passing"
    - "Every deletion commit is tsc-clean and vitest-baseline-passing"
    - "PURGE-09 is delivered as a side-effect of UserProfilePanel deletion (commandPaletteShortcutEnabled toggle dies with the panel)"
  artifacts:
    - path: "src/ui/sidebar/NewSessionDialog.tsx"
      provides: "STAYS — retained pretty-conversations pencil consumer"
      contains: "export function NewSessionDialog"
    - path: "src/ui/sidebar/NewSessionDialog.test.tsx"
      provides: "STAYS — test file for retained NewSessionDialog"
      contains: "describe"
  key_links:
    - from: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      to: "src/ui/sidebar/NewSessionDialog.tsx"
      via: "import (PROTECTED)"
      pattern: "@/sidebar/NewSessionDialog"
---

<objective>
Delete every Phase-11-orphaned sidebar panel file enumerated in 12-01-STRIP-LIST Sections A (simple leaves: HostsPanel, SessionsPanel, CredentialsPanel, QuickConnectPanel, SshToolsPanel, SnippetsPanel, HistoryPanel, SplitScreenPanel, ConnectionsPanel, UserProfilePanel), Section B (Admin subtree: AdminSettingsPanel + 6 section files), Section C (HostManager subtree: HostManager + HostManagerData/Tabs + HostShareModal + HostEditor + 5 Editor* files + HostCredentialList + CredentialEditorView), and Section D (SidebarTree.tsx — safely deletable after Plan 02 Task 1 inlined isFolder into sidebar/NewSessionDialog.tsx). Every deletion commit is tsc-clean per Phase 10 Wave 4 precedent. sidebar/NewSessionDialog.tsx and its test file STAY.

Purpose: Delivers PURGE-06 (sidebar panel + subtree deletion). PURGE-09 is delivered as a side-effect because UserProfilePanel (dying in this plan) is the sole owner of the commandPaletteShortcutEnabled toggle — the only "shortcut editor UI" in the codebase per 12-01-STRIP-LIST Section G.

Output: 4 atomic commits (one per coherent subtree — sidebar simple leaves, Admin subtree, HostManager subtree, SidebarTree), tsc-clean per commit, vitest at Phase 11 baseline, `grep -rn "HostsPanel\|SessionsPanel\|..." src/ | grep -v "^[^:]*:\s*//"` returns 0 non-comment hits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-CONTEXT.md
@.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md
@.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete 10 sidebar simple-leaf panel files in one atomic commit (Section A)</name>
  <files>
    src/ui/sidebar/HostsPanel.tsx,
    src/ui/sidebar/SessionsPanel.tsx,
    src/ui/sidebar/CredentialsPanel.tsx,
    src/ui/sidebar/QuickConnectPanel.tsx,
    src/ui/sidebar/SshToolsPanel.tsx,
    src/ui/sidebar/SnippetsPanel.tsx,
    src/ui/sidebar/HistoryPanel.tsx,
    src/ui/sidebar/SplitScreenPanel.tsx,
    src/ui/sidebar/ConnectionsPanel.tsx,
    src/ui/sidebar/UserProfilePanel.tsx
  </files>
  <read_first>
    - .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md § Section A (enumeration + grep evidence)
    - src/ui/AppShell.tsx (verify 10 panel imports at lines 22-31 + 59 were stripped in Phase 11 Plan 03, so no import needs re-stripping)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (verify retained UI does NOT import any of the 10 dying panels)
    - src/ui/shell/CommandPalette.tsx (verify Plan 02 already redirected the 4 dashboard imports; CommandPalette does NOT import any of the 10 dying panels)
    - src/ui/sidebar/UserProfilePanel.tsx (verify commandPaletteShortcutEnabled toggle at line 490 + 1030-1034 dies with the file — the ONLY "shortcut editor UI" in the codebase per Section G — delivers PURGE-09)
  </read_first>
  <action>
    Pre-deletion grep gate for each of the 10 target files. Run each command and verify 0 non-comment code hits:
    - `grep -rn "from.*['\"]@/sidebar/HostsPanel['\"]\|from.*['\"].*HostsPanel['\"]" src/ --include="*.ts" --include="*.tsx" | grep -v "^[^:]*:[[:space:]]*//" | grep -v "^[^:]*:[[:space:]]*\*"` → expected 0
    - Repeat for: SessionsPanel, CredentialsPanel, QuickConnectPanel, SshToolsPanel, SnippetsPanel, HistoryPanel, SplitScreenPanel, ConnectionsPanel, UserProfilePanel
    - If ANY grep returns a non-comment hit for a live consumer OTHER than the file itself, STOP and route back to planner — a surviving consumer means the file is not orphaned yet.

    Delete all 10 files in one atomic commit:
    ```
    git rm src/ui/sidebar/HostsPanel.tsx src/ui/sidebar/SessionsPanel.tsx src/ui/sidebar/CredentialsPanel.tsx src/ui/sidebar/QuickConnectPanel.tsx src/ui/sidebar/SshToolsPanel.tsx src/ui/sidebar/SnippetsPanel.tsx src/ui/sidebar/HistoryPanel.tsx src/ui/sidebar/SplitScreenPanel.tsx src/ui/sidebar/ConnectionsPanel.tsx src/ui/sidebar/UserProfilePanel.tsx
    ```

    Check for sibling `.test.tsx` files for any of the 10 (Section A of STRIP-LIST enumerates these). If any test file exists for a deleted panel, delete it in the same commit. Do NOT delete `src/ui/sidebar/NewSessionDialog.test.tsx` (that's for the retained NewSessionDialog).

    Post-deletion verification:
    - `npx tsc --noEmit` exit 0
    - `npx vitest run` at Phase 11 baseline (524/526 or better)
    - Repo-wide grep for each deleted identifier returns 0 non-comment code hits (comments per Phase 10 Wave 4 policy are acceptable)
    - `test -f src/ui/sidebar/NewSessionDialog.tsx` — PROTECTED file STAYS
    - `test -f src/ui/features/keyboard/Toolbar.tsx` — PROTECTED (retained on-screen modifier bar)

    Commit message: `chore(sidebar): delete 10 dead panel files — HostsPanel, SessionsPanel, CredentialsPanel, QuickConnectPanel, SshToolsPanel, SnippetsPanel, HistoryPanel, SplitScreenPanel, ConnectionsPanel, UserProfilePanel (Phase 12 Plan 03 PURGE-06 + side-effect PURGE-09 via UserProfilePanel commandPaletteShortcutEnabled toggle)`.
  </action>
  <verify>
    <automated>
    test ! -f src/ui/sidebar/HostsPanel.tsx &&
    test ! -f src/ui/sidebar/SessionsPanel.tsx &&
    test ! -f src/ui/sidebar/CredentialsPanel.tsx &&
    test ! -f src/ui/sidebar/QuickConnectPanel.tsx &&
    test ! -f src/ui/sidebar/SshToolsPanel.tsx &&
    test ! -f src/ui/sidebar/SnippetsPanel.tsx &&
    test ! -f src/ui/sidebar/HistoryPanel.tsx &&
    test ! -f src/ui/sidebar/SplitScreenPanel.tsx &&
    test ! -f src/ui/sidebar/ConnectionsPanel.tsx &&
    test ! -f src/ui/sidebar/UserProfilePanel.tsx &&
    test -f src/ui/sidebar/NewSessionDialog.tsx &&
    test -f src/ui/sidebar/NewSessionDialog.test.tsx &&
    test -f src/ui/features/keyboard/Toolbar.tsx &&
    grep -rn "HostsPanel\|SessionsPanel\|CredentialsPanel\|QuickConnectPanel\|SshToolsPanel\|SnippetsPanel\|HistoryPanel\|SplitScreenPanel\|ConnectionsPanel\|UserProfilePanel" src/ --include="*.ts" --include="*.tsx" | grep -v "^[^:]*:[[:space:]]*//" | grep -v "^[^:]*:[[:space:]]*\*" | grep -v "^[^:]*:[[:space:]]*{/\*" | wc -l | grep -qE "^0$" &&
    npx tsc --noEmit
    </automated>
  </verify>
  <done>
    All 10 sidebar simple-leaf files gone from disk; NewSessionDialog + its test STAY; features/keyboard/ STAYS; zero non-comment identifier survivors across src/; tsc-clean; single atomic commit with prescribed message.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete Admin subtree (AdminSettingsPanel + 6 section files) in one atomic commit (Section B)</name>
  <files>
    src/ui/sidebar/AdminSettingsPanel.tsx,
    src/ui/sidebar/AdminApiKeysSection.tsx,
    src/ui/sidebar/AdminIdentitiesSection.tsx,
    src/ui/sidebar/AdminManagementSections.tsx,
    src/ui/sidebar/AdminSettingsSections.tsx,
    src/ui/sidebar/AdminSettingsShared.tsx,
    src/ui/sidebar/AdminUserDialogs.tsx
  </files>
  <read_first>
    - .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md § Section B (Admin subtree enumeration + inter-section import chain)
    - Task 1 committed successfully — HostsPanel etc. gone (verify via `test ! -f`)
    - Each of the 7 admin files' inter-section imports (Section B of STRIP-LIST enumerates these) — verify all inter-file imports stay within the deleted set
  </read_first>
  <action>
    Note: Task 1 deleted `AdminSettingsPanel.tsx` was mounted in AppShell? Verify with grep — Phase 11 Plan 03 stripped `import { AdminSettingsPanel } from "@/sidebar/AdminSettingsPanel"` at AppShell.tsx line 30 already. So AdminSettingsPanel is import-orphaned. AdminApiKeysSection, AdminIdentitiesSection, AdminManagementSections, AdminSettingsSections, AdminSettingsShared, AdminUserDialogs — each of these was imported ONLY by AdminSettingsPanel (verify via grep at Section B of STRIP-LIST).

    Wait — verify carefully. AdminSettingsPanel is listed in Section A above (Task 1 deleted it). So this task should skip AdminSettingsPanel and just delete the 6 Admin*Section files. Let me correct: the file list at the top of this task starts with AdminSettingsPanel by mistake — Task 1 already deleted it. Executor: verify the current state via `test -f src/ui/sidebar/AdminSettingsPanel.tsx` — if it returns non-zero (file gone from Task 1), remove AdminSettingsPanel.tsx from this task's file list; if file still present (Task 1 skipped it), delete it here.

    Pre-deletion grep gate for each of the 6 Admin*Section files. Run each command and verify 0 non-comment code hits from OUTSIDE the Admin subtree:
    - `grep -rn "AdminApiKeysSection\|AdminIdentitiesSection\|AdminManagementSections\|AdminSettingsSections\|AdminSettingsShared\|AdminUserDialogs" src/ --include="*.ts" --include="*.tsx" | grep -v "^src/ui/sidebar/Admin" | grep -v "^[^:]*:[[:space:]]*//" | grep -v "^[^:]*:[[:space:]]*\*"` → expected 0

    If Task 1 skipped AdminSettingsPanel.tsx, run its consumer grep too and delete alongside.

    Delete atomically:
    ```
    git rm src/ui/sidebar/AdminApiKeysSection.tsx src/ui/sidebar/AdminIdentitiesSection.tsx src/ui/sidebar/AdminManagementSections.tsx src/ui/sidebar/AdminSettingsSections.tsx src/ui/sidebar/AdminSettingsShared.tsx src/ui/sidebar/AdminUserDialogs.tsx
    ```
    (Add `src/ui/sidebar/AdminSettingsPanel.tsx` to the rm list if still on disk.)

    Delete any sibling `.test.tsx` files in the same commit.

    Post-deletion verification: same gates as Task 1 for the Admin identifiers.

    Commit message: `chore(sidebar): delete Admin subtree — AdminApiKeysSection, AdminIdentitiesSection, AdminManagementSections, AdminSettingsSections, AdminSettingsShared, AdminUserDialogs (Phase 12 Plan 03 PURGE-06)`.
  </action>
  <verify>
    <automated>
    test ! -f src/ui/sidebar/AdminSettingsPanel.tsx &&
    test ! -f src/ui/sidebar/AdminApiKeysSection.tsx &&
    test ! -f src/ui/sidebar/AdminIdentitiesSection.tsx &&
    test ! -f src/ui/sidebar/AdminManagementSections.tsx &&
    test ! -f src/ui/sidebar/AdminSettingsSections.tsx &&
    test ! -f src/ui/sidebar/AdminSettingsShared.tsx &&
    test ! -f src/ui/sidebar/AdminUserDialogs.tsx &&
    test -f src/ui/sidebar/NewSessionDialog.tsx &&
    grep -rn "AdminApiKeysSection\|AdminIdentitiesSection\|AdminManagementSections\|AdminSettingsSections\|AdminSettingsShared\|AdminUserDialogs\|AdminSettingsPanel" src/ --include="*.ts" --include="*.tsx" | grep -v "^[^:]*:[[:space:]]*//" | grep -v "^[^:]*:[[:space:]]*\*" | grep -v "^[^:]*:[[:space:]]*{/\*" | wc -l | grep -qE "^0$" &&
    npx tsc --noEmit
    </automated>
  </verify>
  <done>
    Admin subtree files gone; NewSessionDialog STAYS; zero non-comment identifier survivors; tsc-clean; single atomic commit landed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Delete HostManager subtree + HostEditor + HostShareModal + HostCredentialList + CredentialEditorView in one atomic commit (Section C)</name>
  <files>
    src/ui/sidebar/HostManager.tsx,
    src/ui/sidebar/HostManagerData.ts,
    src/ui/sidebar/HostManagerTabs.tsx,
    src/ui/sidebar/HostShareModal.tsx,
    src/ui/sidebar/HostEditor.tsx,
    src/ui/sidebar/HostEditorData.ts,
    src/ui/sidebar/HostEditorFeatureTabs.tsx,
    src/ui/sidebar/HostEditorGeneralTab.tsx,
    src/ui/sidebar/HostEditorGuacamoleTabs.tsx,
    src/ui/sidebar/HostEditorStatsTab.tsx,
    src/ui/sidebar/HostCredentialList.tsx,
    src/ui/sidebar/CredentialEditorView.tsx
  </files>
  <read_first>
    - .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md § Section C (HostManager subtree enumeration + inter-file import chain)
    - Task 1 + Task 2 committed successfully
  </read_first>
  <action>
    Pre-deletion grep gate for the whole subtree:
    - `grep -rn "HostManager\|HostEditor\|HostShareModal\|HostCredentialList\|CredentialEditorView" src/ --include="*.ts" --include="*.tsx" | grep -v "^src/ui/sidebar/Host" | grep -v "^src/ui/sidebar/CredentialEditorView" | grep -v "^[^:]*:[[:space:]]*//" | grep -v "^[^:]*:[[:space:]]*\*"` → expected 0

    Note: HostEditor imports `useTabsSafe from "@/shell/TabContext"` (line 37) — TabContext is retained; the import dies with HostEditor.tsx and no cross-file cleanup needed.

    Delete atomically:
    ```
    git rm src/ui/sidebar/HostManager.tsx src/ui/sidebar/HostManagerData.ts src/ui/sidebar/HostManagerTabs.tsx src/ui/sidebar/HostShareModal.tsx src/ui/sidebar/HostEditor.tsx src/ui/sidebar/HostEditorData.ts src/ui/sidebar/HostEditorFeatureTabs.tsx src/ui/sidebar/HostEditorGeneralTab.tsx src/ui/sidebar/HostEditorGuacamoleTabs.tsx src/ui/sidebar/HostEditorStatsTab.tsx src/ui/sidebar/HostCredentialList.tsx src/ui/sidebar/CredentialEditorView.tsx
    ```

    Delete any sibling `.test.tsx` files in the same commit.

    Commit message: `chore(sidebar): delete HostManager + HostEditor subtree — HostManager, HostManagerData, HostManagerTabs, HostShareModal, HostEditor, HostEditorData, HostEditorFeatureTabs, HostEditorGeneralTab, HostEditorGuacamoleTabs, HostEditorStatsTab, HostCredentialList, CredentialEditorView (Phase 12 Plan 03 PURGE-06)`.
  </action>
  <verify>
    <automated>
    test ! -f src/ui/sidebar/HostManager.tsx &&
    test ! -f src/ui/sidebar/HostManagerData.ts &&
    test ! -f src/ui/sidebar/HostManagerTabs.tsx &&
    test ! -f src/ui/sidebar/HostShareModal.tsx &&
    test ! -f src/ui/sidebar/HostEditor.tsx &&
    test ! -f src/ui/sidebar/HostEditorData.ts &&
    test ! -f src/ui/sidebar/HostEditorFeatureTabs.tsx &&
    test ! -f src/ui/sidebar/HostEditorGeneralTab.tsx &&
    test ! -f src/ui/sidebar/HostEditorGuacamoleTabs.tsx &&
    test ! -f src/ui/sidebar/HostEditorStatsTab.tsx &&
    test ! -f src/ui/sidebar/HostCredentialList.tsx &&
    test ! -f src/ui/sidebar/CredentialEditorView.tsx &&
    test -f src/ui/sidebar/NewSessionDialog.tsx &&
    grep -rn "HostManager\|HostEditor\|HostShareModal\|HostCredentialList\|CredentialEditorView" src/ --include="*.ts" --include="*.tsx" | grep -v "^[^:]*:[[:space:]]*//" | grep -v "^[^:]*:[[:space:]]*\*" | grep -v "^[^:]*:[[:space:]]*{/\*" | wc -l | grep -qE "^0$" &&
    npx tsc --noEmit
    </automated>
  </verify>
  <done>
    HostManager + HostEditor subtree gone; NewSessionDialog STAYS; zero non-comment identifier survivors; tsc-clean; single atomic commit landed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Delete src/ui/sidebar/SidebarTree.tsx (Section D)</name>
  <files>src/ui/sidebar/SidebarTree.tsx</files>
  <read_first>
    - .planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-01-STRIP-LIST.md § Section D
    - Plan 02 Task 1 SUMMARY (verify sidebar/NewSessionDialog.tsx has zero `@/sidebar/SidebarTree` imports and a local isFolder inline)
    - Tasks 1, 2, 3 above committed successfully (HostsPanel, which imports SidebarTree, is gone)
  </read_first>
  <action>
    Pre-deletion grep gate:
    - `grep -rn "SidebarTree" src/ --include="*.ts" --include="*.tsx" | grep -v "^src/ui/sidebar/SidebarTree" | grep -v "^[^:]*:[[:space:]]*//" | grep -v "^[^:]*:[[:space:]]*\*"` → expected 0 (the only consumers were HostsPanel — dead in Task 1 — and NewSessionDialog — refactored in Plan 02 Task 1)
    - `grep -rn "isFolder" src/ --include="*.ts" --include="*.tsx" | grep -v "^src/ui/sidebar/NewSessionDialog.tsx" | grep -v "^src/ui/sidebar/SidebarTree" | grep -v "^src/ui/state/conversation-store.ts" | grep -v "^[^:]*:[[:space:]]*//" | grep -v "^[^:]*:[[:space:]]*\*"` → expected 0 (NewSessionDialog's local inline is the only remaining consumer; conversation-store.ts has a comment-only reference; SidebarTree.tsx itself dies)

    Delete:
    ```
    git rm src/ui/sidebar/SidebarTree.tsx
    ```

    Delete any sibling `.test.tsx` in the same commit.

    Commit message: `chore(sidebar): delete SidebarTree.tsx — isFolder inlined into NewSessionDialog in Plan 02 (Phase 12 Plan 03 PURGE-06)`.
  </action>
  <verify>
    <automated>
    test ! -f src/ui/sidebar/SidebarTree.tsx &&
    test -f src/ui/sidebar/NewSessionDialog.tsx &&
    test -f src/ui/sidebar/NewSessionDialog.test.tsx &&
    grep -rn "SidebarTree" src/ --include="*.ts" --include="*.tsx" | grep -v "^[^:]*:[[:space:]]*//" | grep -v "^[^:]*:[[:space:]]*\*" | grep -v "^[^:]*:[[:space:]]*{/\*" | wc -l | grep -qE "^0$" &&
    grep -cE '^\s*function isFolder\s*\(' src/ui/sidebar/NewSessionDialog.tsx | grep -qE "^1$" &&
    npx tsc --noEmit &&
    npx vitest run src/ui/sidebar/NewSessionDialog.test.tsx &&
    npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
    </automated>
  </verify>
  <done>
    SidebarTree.tsx gone; NewSessionDialog STAYS with local isFolder; NewSessionDialog + PrettyConversationsPanel tests pass; tsc-clean; single atomic commit landed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pre-Wave-3 refactor dependency | Plan 02 MUST land before this plan starts (SidebarTree deletion depends on the inline isFolder refactor in Plan 02 Task 1). |
| sidebar/NewSessionDialog.tsx protection | Every task's file_list explicitly EXCLUDES NewSessionDialog.tsx + NewSessionDialog.test.tsx. Verify gates include `test -f src/ui/sidebar/NewSessionDialog.tsx` = pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | Tampering | Accidentally delete sidebar/NewSessionDialog.tsx | mitigate | NewSessionDialog is explicitly NOT in any task's `<files>` block. Every task's `<verify>` includes `test -f src/ui/sidebar/NewSessionDialog.tsx` PASS gate. Every task's `<action>` uses explicit `git rm` with named files — no wildcards, no `git rm src/ui/sidebar/*Panel.tsx` sweeps. |
| T-12-03-02 | Tampering | Accidentally break retained UI import graph | mitigate | Pre-deletion grep gate on each subtree confirms zero non-comment consumers OUTSIDE the deleted set. If a live consumer surfaces, task ROUTES BACK to planner (no deletion). Post-deletion tsc-clean is the load-bearing gate that no import survives. |
| T-12-03-03 | Tampering | features/keyboard/ accidentally deleted (mis-identified as PURGE-09 target) | mitigate | Task 1 `<verify>` includes `test -f src/ui/features/keyboard/Toolbar.tsx` PASS. STRIP-LIST Section G explicitly resolves PURGE-09 as side-effect of UserProfilePanel deletion (commandPaletteShortcutEnabled toggle) — no standalone shortcut editor file exists. Plan 03 does NOT touch src/ui/features/keyboard/. |
| T-12-03-04 | Tampering | SidebarTree deleted before Plan 02 Task 1 lands | mitigate | Task 4 read_first requires Plan 02 Task 1 SUMMARY verification. Task 4 verify includes `grep -cE '^\s*function isFolder\s*\(' src/ui/sidebar/NewSessionDialog.tsx = 1` gate — proves the inline landed BEFORE deleting SidebarTree. If missing, tsc-clean gate fails. |
| T-12-03-05 | Tampering | RDP/VNC/Guacamole render regression | mitigate | This plan touches ZERO retained-UI files. `case "rdp"`, `case "vnc"`, `case "telnet"` in tabUtils.tsx untouched. onRdpRowClick handler in AppShell untouched. Guacamole subtree untouched. |
| T-12-03-06 | Tampering | Package installs | accept | Zero installs. Pure git rm. |
| T-12-03-SC | Tampering | npm/pip/cargo installs | accept | Zero installs. |
</threat_model>

<verification>
- Task 1 verify: 10 sidebar panels gone; NewSessionDialog + test STAY; features/keyboard/ STAYS; 0 non-comment identifier survivors; tsc-clean
- Task 2 verify: Admin subtree gone; identifiers 0-survivor; tsc-clean
- Task 3 verify: HostManager + HostEditor subtree gone; identifiers 0-survivor; tsc-clean
- Task 4 verify: SidebarTree gone; NewSessionDialog local isFolder present; tsc-clean; NewSessionDialog + PrettyConversationsPanel tests pass
- All 4 atomic commits landed with `chore(sidebar):` prefix
- Full `npx vitest run` at Phase 11 baseline (524/526 with 2 pre-existing ComposeBox failures)
</verification>

<success_criteria>
- All 29 files enumerated in 12-01-STRIP-LIST Sections A/B/C/D deleted from disk
- src/ui/sidebar/NewSessionDialog.tsx + NewSessionDialog.test.tsx PRESERVED
- src/ui/features/keyboard/ subtree UNTOUCHED
- 4 atomic tsc-clean commits landed
- vitest matches Phase 11 baseline (524/526)
- PURGE-06 delivered; PURGE-09 delivered as side-effect (UserProfilePanel commandPaletteShortcutEnabled toggle gone)
</success_criteria>

<output>
Create `.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-03-SUMMARY.md` on completion.
</output>
