---
phase: 72-feature-02-slice-2-reconcile-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/distributor/catalog.ts
  - src/backend/distributor/catalog.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "The 15 substrate items are enumerated as a hand-maintained TypeScript array reviewable in one file"
    - "Each catalog entry declares its bundled path (under /app/fleet-substrate/), its install path on the managed host, and its restart hook (systemd unit name or 'none')"
    - "Only the agent-supervisor script carries a restart hook (agent-supervisor.service); the other 14 items declare 'none'"
    - "Both skills/ tree (9 skill folders totalling 13 files) and scripts/ tree (6 helper scripts) are covered"
  artifacts:
    - path: "src/backend/distributor/catalog.ts"
      provides: "CatalogEntry type + FLEET_SUBSTRATE_CATALOG constant (15 entries)"
      exports: ["CatalogEntry", "FLEET_SUBSTRATE_CATALOG"]
    - path: "src/backend/distributor/catalog.test.ts"
      provides: "Structural assertions on the catalog"
  key_links:
    - from: "src/backend/distributor/catalog.ts"
      to: "/app/fleet-substrate/**"
      via: "bundledPath strings"
      pattern: "/app/fleet-substrate/(skills|scripts)/"
---

<objective>
Land the hand-maintained TypeScript catalog of the 15 fleet-substrate items that later plans in this phase will iterate over. Pure data + type declaration + structural tests. No SSH, no filesystem I/O, no orchestrator wiring — a first-class reviewable module that names every item, its bundled-side location (mirroring the /app/fleet-substrate/ layout established by slice 1), its install-side target on a managed host under the single ubuntu user, and the systemd --user unit (if any) whose restart the sweep must fire after the item's bytes change.

Purpose: The whole phase pivots around this catalog. Slice 1 vendored the 15 items and made the image copy them into /app/fleet-substrate/; slice 2's sweep needs to know, for each item, what to compare against what, and what to restart after writing. Hand-maintained (not auto-generated) per the feature-02 doc's design decision and the shape file's "small and reviewable" philosophy.

Output: `src/backend/distributor/` module born with catalog + type + test scaffolding that Plans 02–04 depend on.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-feature-02-slice-2-reconcile-loop/72-CONTEXT.md
@.planning/shapes/shape-feature-02-slice-1-vendor.closed.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create catalog type + hand-maintained 15-item array with structural tests</name>
  <files>src/backend/distributor/catalog.ts, src/backend/distributor/catalog.test.ts</files>
  <read_first>
    - /home/ubuntu/skynet-tiffany/.planning/phases/73-feature-02-slice-2-reconcile-loop/72-CONTEXT.md (full — governs every field on the CatalogEntry type)
    - /home/ubuntu/skynet-tiffany/.planning/shapes/shape-feature-02-slice-1-vendor.closed.md (governs bundled path /app/fleet-substrate/ and the skills/ vs scripts/ split that mirrors the install layout)
    - /home/ubuntu/skynet-tiffany/substrate/skills/ (enumerate — 9 skill directories: agent-relay [SKILL.md + recv.sh], backlog [SKILL.md], bounty [SKILL.md], claude-code-harness-auth [SKILL.md], id [SKILL.md + actor-status-prompt.md + clone-picker-prompt.md + coordinator-instructions.md], next-bounty [SKILL.md], promote-to-coordinator [SKILL.md], queue [SKILL.md], role [SKILL.md] — 13 skill files total)
    - /home/ubuntu/skynet-tiffany/substrate/scripts/ (enumerate — 6 helper scripts: agent-supervisor.sh, claude-usage-collector.py, context-watch.py, install-usage-reporter.sh, usage-reporter.sh, wakeup-scheduler.py)
    - /home/ubuntu/.claude/roles/box-maintainer/bounties/ai-plus-mvp-project/feature-02-skynet-distributor.md (the Per-item catalog section — shows the target-shape TypeScript with bundledPath / installedPath / restartAfter fields; agent-supervisor installs to ~/.local/bin/agent-supervisor and has restartAfter: ["agent-supervisor.service"])
    - /home/ubuntu/skynet-tiffany/src/backend/fleet-status/liveness-check.ts (pure-lib pattern — no I/O in this module, mirror that discipline)
  </read_first>
  <behavior>
    - Test 1: FLEET_SUBSTRATE_CATALOG.length === 15
    - Test 2: Every entry has bundledPath starting with "/app/fleet-substrate/skills/" or "/app/fleet-substrate/scripts/"
    - Test 3: Every entry has installPath starting with "~/.claude/skills/" (for skill items) or "~/.local/bin/" (for helper scripts) — no other install roots
    - Test 4: Exactly ONE entry has restartHook !== null; that entry's restartHook === "agent-supervisor.service" AND its installPath === "~/.local/bin/agent-supervisor"
    - Test 5: Every slug is unique (no duplicate catalog entries)
    - Test 6: The 9 skills directory contents are represented: agent-relay contributes 2 entries (SKILL.md + recv.sh); id contributes 4 entries (SKILL.md + 3 companion .md files); the 7 other skills contribute 1 entry each (SKILL.md only) — total 13 skill entries + 2 scripts-that-are-skill-adjacent? NO — actual breakdown is 13 skill entries under skills/ + 2 scripts-only entries under scripts/. Correction: 15 = 13 skill files (agent-relay 2 + id 4 + 7 × 1) + 2 headline scripts (agent-supervisor + wakeup-scheduler)? Consult feature-02 doc + substrate enumeration — the shape doc names "seven single-file skills + six standalone helper scripts + identity system (1 skill + 3 companions) + messaging surface (1 skill + 1 receiver bundled with it)". That gives skills-side entries = 4 (id + 3 companions) + 2 (agent-relay + recv.sh) + 7 (single-file skills) = 13; scripts-side = 6 (supervisor + wakeup + context-watch + usage-reporter + install-usage-reporter + claude-usage-collector); TOTAL = 19. HOWEVER the CONTEXT.md + shape doc + feature-02 doc all say "15 items". Reconcile: the "15" refers to shipping items ("skill" is one item; agent-relay + recv.sh ships as one skill with a bundled companion, id + 3 companions ships as one skill with 3 companions). If catalog rows are per-FILE (which the byte-compare + push mechanism requires), the count is 19. Task: choose per-FILE rows (required for the byte-compare pattern in Plan 03), and assert Test 1 as `length === 19` with a comment explaining "15 conceptual items across 19 files (agent-relay = 2 files, id = 4 files, the other 7 skills = 1 file each, plus 6 scripts)". Cross-check enumeration against substrate/ before finalizing count.
    - Test 7: Every entry with installPath starting "~/.local/bin/" corresponds to a script file whose bundled sibling on disk carries the executable bit (verified by reading fs.statSync bit patterns on the bundled path in the test; ALL 6 script bundled files are chmod 755 per the ls -la output confirmed in slice-1 close)
  </behavior>
  <action>
    Create `src/backend/distributor/` directory with two files:

    **catalog.ts** — export a TypeScript interface `CatalogEntry` with these fields:
    - `slug: string` — unique kebab-case identifier used only in log lines (examples: "id-skill", "id-actor-status-prompt", "agent-relay-skill", "agent-relay-recv", "agent-supervisor", "wakeup-scheduler")
    - `bundledPath: string` — absolute path inside the container image (starts with "/app/fleet-substrate/")
    - `installPath: string` — target path on the managed host expressed with leading "~/" (the sweep will resolve against the ubuntu user's HOME via `$HOME` expansion in the remote shell)
    - `restartHook: string | null` — systemd --user unit name (e.g. "agent-supervisor.service") that must be restarted via `systemctl --user restart <unit>` after the file's bytes change; `null` for items with no restart hook

    Also export `FLEET_SUBSTRATE_CATALOG: readonly CatalogEntry[]` — the hand-maintained array. The 19 rows partition as follows (skill entries first, then scripts):

    - id: 4 rows — /app/fleet-substrate/skills/id/{SKILL.md, actor-status-prompt.md, clone-picker-prompt.md, coordinator-instructions.md} → ~/.claude/skills/id/{same}
    - agent-relay: 2 rows — /app/fleet-substrate/skills/agent-relay/{SKILL.md, recv.sh} → ~/.claude/skills/agent-relay/{same}
    - Seven single-file skills (backlog, bounty, claude-code-harness-auth, next-bounty, promote-to-coordinator, queue, role): 1 row each — /app/fleet-substrate/skills/<slug>/SKILL.md → ~/.claude/skills/<slug>/SKILL.md — restartHook null
    - agent-supervisor: /app/fleet-substrate/scripts/agent-supervisor.sh → ~/.local/bin/agent-supervisor — restartHook "agent-supervisor.service"
    - wakeup-scheduler: /app/fleet-substrate/scripts/wakeup-scheduler.py → ~/.local/bin/wakeup-scheduler — restartHook null
    - context-watch: /app/fleet-substrate/scripts/context-watch.py → ~/.local/bin/context-watch — restartHook null
    - usage-reporter: /app/fleet-substrate/scripts/usage-reporter.sh → ~/.local/bin/usage-reporter — restartHook null
    - install-usage-reporter: /app/fleet-substrate/scripts/install-usage-reporter.sh → ~/.local/bin/install-usage-reporter — restartHook null
    - claude-usage-collector: /app/fleet-substrate/scripts/claude-usage-collector.py → ~/.local/bin/claude-usage-collector — restartHook null

    All skill entries carry restartHook: null. The full list totals 19 rows (13 skill-side + 6 scripts-side); include a docblock comment referencing the "15 conceptual items across 19 files" reconciliation.

    Add a top-of-file docblock that:
    - Cross-references .planning/phases/73-feature-02-slice-2-reconcile-loop/72-CONTEXT.md as the shape source of truth
    - States the pure-lib discipline: NO fs, NO ssh, NO process, NO logger — this module is data only
    - States that mode is NOT declared per entry (Plan 03's byte-push helpers read the bundled file's mode from disk at push time — git IS the mode source of truth per the shape doc)
    - States that the restartHook value is passed verbatim to `systemctl --user restart <hook>` on the target host (no interpretation happens in catalog.ts)

    **catalog.test.ts** — vitest tests as enumerated in the <behavior> block. The Test 7 executable-bit check uses `fs.statSync` against the local repo path derived by substituting "/app/fleet-substrate/" for the repo-root-relative "substrate/" prefix (i.e. read from `substrate/scripts/agent-supervisor.sh` on the dev machine, not from the container path). Skip Test 7 (with a clear it.skip comment referencing this note) if run in an environment where `substrate/` is not present — the test is a sanity check, not a hard gate.

    Do NOT import anything from src/backend/fleet-status/, src/backend/database/, src/backend/ssh/, or src/backend/utils/. The only allowed imports are vitest primitives + node:fs for Test 7. This module has zero runtime dependencies on the rest of the backend.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tiffany &amp;&amp; npx vitest run src/backend/distributor/catalog.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "^export const FLEET_SUBSTRATE_CATALOG" src/backend/distributor/catalog.ts` equals 1
    - Source assertion: `grep -c "^export interface CatalogEntry" src/backend/distributor/catalog.ts` equals 1
    - Source assertion: `grep -v '^ *\*\|^ *//' src/backend/distributor/catalog.ts | grep -c '"agent-supervisor.service"'` equals 1 (the restartHook string appears in exactly one non-comment location)
    - Source assertion: `grep -v '^ *\*\|^ *//' src/backend/distributor/catalog.ts | grep -c "/app/fleet-substrate/"` equals 19 (one occurrence per catalog row's bundledPath, comments excluded)
    - Source assertion: `grep -c "^import" src/backend/distributor/catalog.ts` equals 0 (catalog.ts imports nothing — pure data + type)
    - Behavior assertion: `npx vitest run src/backend/distributor/catalog.test.ts` reports all tests passing (Test 7 may be skipped in the eventual container environment; must run + pass on the dev machine where substrate/ exists)
    - Behavior assertion: catalog.test.ts contains a test whose name or body asserts `FLEET_SUBSTRATE_CATALOG.filter(e =&gt; e.restartHook !== null).length === 1`
  </acceptance_criteria>
  <done>
    src/backend/distributor/catalog.ts exports the CatalogEntry interface + the 19-row FLEET_SUBSTRATE_CATALOG constant matching the enumeration in <action>. catalog.test.ts exists with 7 structural tests all green. No runtime imports (data-only module). agent-supervisor is the sole entry with a non-null restartHook. Both files are staged for commit.
  </done>
</task>

</tasks>

<verification>
Plan-level verification:
1. `cd /home/ubuntu/skynet-tiffany &amp;&amp; ls src/backend/distributor/` shows exactly `catalog.ts` and `catalog.test.ts` (this plan does not create any other files in that directory)
2. `cd /home/ubuntu/skynet-tiffany &amp;&amp; npx vitest run src/backend/distributor/` runs green
3. `cd /home/ubuntu/skynet-tiffany &amp;&amp; npx tsc --noEmit` shows no new type errors introduced by catalog.ts (data-only module; if tsc errors exist elsewhere in the tree, they must be pre-existing and unchanged by this plan)
</verification>

<success_criteria>
- CatalogEntry interface + FLEET_SUBSTRATE_CATALOG constant exported
- 19 rows: 13 skill-side + 6 scripts-side (partitioning matches the enumeration in <action>)
- Exactly one row has restartHook = "agent-supervisor.service"; all others null
- Zero runtime imports; module is pure data
- All 7 structural tests pass
- Commit lands on feat/tab-title-from-tmux with the two files staged
</success_criteria>

<output>
Create `.planning/phases/73-feature-02-slice-2-reconcile-loop/73-01-SUMMARY.md` when done, capturing:
- Final row count and per-item breakdown
- Any deviations from the enumeration in <action> (should be none — CONTEXT.md is authoritative)
- Confirmation that catalog.ts has zero runtime imports
- Test results (`npx vitest run src/backend/distributor/catalog.test.ts` output tail)
</output>
</content>
</invoke>