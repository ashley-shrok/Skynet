---
phase: 29-unified-session-entry-state-machine-single-resolving-spinner
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/resolve-phase.ts
  - src/ui/features/pretty-view/resolve-phase.test.ts
autonomous: true
requirements:
  - PHASE29-REQ-04
tags:
  - phase-29
  - pure-reducer
  - test-seam

must_haves:
  truths:
    - "resolvePhase(wsState, backendFirstFrame) returns a deterministic Phase for every combination in the SPEC req 4 truth table"
    - "resolvePhase is pure — importing it triggers zero React/WS/logger side effects"
    - "The Phase type union has exactly 6 members: resolving | active | holding | dormant | inactive | error"
  artifacts:
    - path: "src/ui/features/pretty-view/resolve-phase.ts"
      provides: "Pure resolvePhase reducer + WsState/BackendFirstFrame/Phase type unions"
      exports: ["resolvePhase", "WsState", "BackendFirstFrame", "Phase"]
    - path: "src/ui/features/pretty-view/resolve-phase.test.ts"
      provides: "Truth-table unit tests covering every (wsState × backendFirstFrame) combination"
      contains: "describe(\"resolvePhase"
  key_links:
    - from: "src/ui/features/pretty-view/resolve-phase.ts"
      to: "no dependencies (pure module)"
      via: "test-seam split per layer1-detect.ts pattern"
      pattern: "^import"
---

<objective>
Create the pure test-seam reducer `resolvePhase(wsState, backendFirstFrame) → Phase` (SPEC Requirement 4) that encodes the entire truth table as a single pure function with type-safe exhaustiveness. This is the deterministic core of the new pane-entry state machine. Ships alongside a truth-table unit test that exercises every input combination.

Per D-04 (test-seam split), this module is imported by `usePaneResolvingMachine` (plan 29-02) but exists independently so its correctness can be verified without any React/WS setup. Pattern is copied from `src/backend/claude-session/layer1-detect.ts` verbatim per PATTERNS.md section 1.

Purpose: Eliminate the racing-state-machines problem at its root — one function, one truth table, one deterministic phase for every input combination. Enables SPEC req 4 (truth-table unit-testability), unblocks plan 29-02 (hook uses this function).

Output: `resolve-phase.ts` (pure reducer + 3 exported type unions) and `resolve-phase.test.ts` (~20 truth-table `it` cases across 4 describe blocks).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-unified-session-entry-state-machine-single-resolving-spinner/29-SPEC.md
@.planning/phases/29-unified-session-entry-state-machine-single-resolving-spinner/29-CONTEXT.md
@.planning/phases/29-unified-session-entry-state-machine-single-resolving-spinner/29-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create pure resolvePhase reducer with type unions</name>
  <files>src/ui/features/pretty-view/resolve-phase.ts</files>
  <read_first>
    - src/backend/claude-session/layer1-detect.ts (canonical fork test-seam pattern — copy header/type-union/pure-reducer shape exactly)
    - .planning/phases/29-unified-session-entry-state-machine-single-resolving-spinner/29-SPEC.md § "Requirements" 3 and 4 (input axes + truth table)
    - .planning/phases/29-unified-session-entry-state-machine-single-resolving-spinner/29-CONTEXT.md § "Implementation Decisions" D-04, D-10, D-11 (post-resolve semantics — resolvePhase is called by the hook but does NOT itself gate on hasEverResolved; that is the hook's concern)
    - .planning/phases/29-unified-session-entry-state-machine-single-resolving-spinner/29-PATTERNS.md § 1 (analog + type-union + reducer signature reference)
  </read_first>
  <behavior>
    - resolvePhase("not-connected", any BackendFirstFrame) === "resolving"
    - resolvePhase("opening", any BackendFirstFrame) === "resolving"
    - resolvePhase("open", "not-yet") === "resolving"
    - resolvePhase("open", "active") === "active"
    - resolvePhase("open", "session_holding") === "holding"
    - resolvePhase("open", "dormant") === "dormant"
    - resolvePhase("open", "inactive") === "inactive"
    - resolvePhase("failed-permanently", any BackendFirstFrame) === "error"
    - TypeScript exhaustiveness: adding a new BackendFirstFrame value without updating the resolver fails `tsc --noEmit` via the `_exhaust: never` sentinel
    - Zero runtime dependencies — pure function, no React/WS/logger imports
  </behavior>
  <action>
    Create `src/ui/features/pretty-view/resolve-phase.ts` implementing the SPEC req 4 truth table exactly as a pure function.

    Module-header comment block MUST follow the `layer1-detect.ts:1-48` pattern verbatim in shape (multi-paragraph rationale block). Header MUST include the anchor comment `// phase-29: pure resolvePhase reducer — test-seam split per layer1-detect.ts pattern` for structural-grep discoverability by plan 29-04 tests, AND state "NO I/O IMPORTS — pure function only" as an invariant.

    Export three type union aliases exactly (name and members literal, in this order):
    - `WsState = "not-connected" | "opening" | "open" | "failed-permanently"`
    - `BackendFirstFrame = "not-yet" | "active" | "inactive" | "session_holding" | "dormant"`
    - `Phase = "resolving" | "active" | "holding" | "dormant" | "inactive" | "error"`

    Export a single pure function `resolvePhase(wsState: WsState, backendFirstFrame: BackendFirstFrame): Phase` implementing the SPEC req 4 table with this exact branch order (matches PATTERNS.md section 1 pseudo-code):
    1. `if (wsState === "failed-permanently") return "error";` (D-04 error semantics — WS terminal state is the only path to error phase)
    2. `if (wsState === "not-connected" || wsState === "opening") return "resolving";`
    3. `if (backendFirstFrame === "not-yet") return "resolving";` (wsState is "open" past here)
    4. `if (backendFirstFrame === "active") return "active";`
    5. `if (backendFirstFrame === "session_holding") return "holding";`
    6. `if (backendFirstFrame === "dormant") return "dormant";`
    7. `if (backendFirstFrame === "inactive") return "inactive";`
    8. Exhaustiveness sentinel: `const _exhaust: never = backendFirstFrame; return _exhaust;` (compile-time guard — adding a BackendFirstFrame variant without updating the branch list fails tsc)

    NO React imports. NO logger imports. NO WS imports. This file's `^import` line count MUST be 0 (there are no imports at all — every reference is inline). Enforced by plan 29-04's grep gate.

    Do NOT include any `setTimeout`, `Date.now()`, or any wall-clock logic — pure function only (SPEC req 5).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | grep -E "resolve-phase\.ts" || echo "tsc clean on resolve-phase.ts"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/ui/features/pretty-view/resolve-phase.ts` exists
    - Source contains the exact anchor comment string `phase-29: pure resolvePhase reducer` (single grep match)
    - Source contains exactly these three exported type aliases (grep for `export type WsState`, `export type BackendFirstFrame`, `export type Phase` — each returns exactly 1 hit)
    - `WsState` union has exactly these 4 string literal members: `"not-connected"`, `"opening"`, `"open"`, `"failed-permanently"`
    - `BackendFirstFrame` union has exactly these 5 string literal members: `"not-yet"`, `"active"`, `"inactive"`, `"session_holding"`, `"dormant"`
    - `Phase` union has exactly these 6 string literal members: `"resolving"`, `"active"`, `"holding"`, `"dormant"`, `"inactive"`, `"error"`
    - Source contains exactly 1 `export function resolvePhase(` occurrence
    - Source contains `_exhaust: never` sentinel (compile-time exhaustiveness gate)
    - Source contains zero `import ` statements (grep `^import ` returns 0 hits) — pure module invariant per PATTERNS.md
    - Source contains zero `setTimeout`, zero `Date.now`, zero `require(` calls
    - `npx tsc --noEmit` exits 0 with no errors mentioning `resolve-phase.ts`
  </acceptance_criteria>
  <done>Pure resolvePhase reducer file exists, exports the 3 named type unions + resolvePhase function, is import-free, is compile-clean, and encodes the SPEC req 4 truth table exactly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Truth-table unit tests for resolvePhase</name>
  <files>src/ui/features/pretty-view/resolve-phase.test.ts</files>
  <read_first>
    - src/ui/features/pretty-view/resolve-phase.ts (source-under-test — imports from here)
    - src/backend/claude-session/layer1-detect.test.ts (analog test file — copy header + grouped-describe structure)
    - .planning/phases/29-unified-session-entry-state-machine-single-resolving-spinner/29-SPEC.md § "Requirements" 4 acceptance criteria (every truth-table row gets its own `it`)
    - .planning/phases/29-unified-session-entry-state-machine-single-resolving-spinner/29-PATTERNS.md § 3 (analog test-file pattern + suggested describe groupings)
  </read_first>
  <behavior>
    - Every one of the 20 (wsState × backendFirstFrame) combinations gets a dedicated `it` assertion
    - Test file imports resolvePhase + the three type unions from ./resolve-phase (relative path, no `.js` extension in TS source — mirror layer1-detect.test.ts import shape)
    - Every it-block ends in `expect(resolvePhase(w, b)).toBe(expectedPhase)` with the expected phase literal spelled out
    - Grouped in 4 describe blocks (one per wsState value)
  </behavior>
  <action>
    Create `src/ui/features/pretty-view/resolve-phase.test.ts` covering the full 4×5 = 20 truth-table combinations from SPEC req 4.

    Test-file header comment MUST include the anchor `// phase-29: truth-table tests for resolvePhase — SPEC req 4` (plan 29-04 grep may reference this).

    Import block: `import { describe, it, expect } from "vitest";` and `import { resolvePhase, type WsState, type BackendFirstFrame, type Phase } from "./resolve-phase";` (mirror `layer1-detect.test.ts:19-27` shape but with no `.js` extension because this file is under `src/ui/` which does not use ESM extension imports in the fork — verify by checking `session-recycling-store.test.ts` import shape).

    Organize into exactly 4 describe blocks, one per wsState value:

    1. `describe("resolvePhase — wsState=not-connected → always resolving", () => { ... })` with 5 `it` blocks, one per BackendFirstFrame value ("not-yet", "active", "inactive", "session_holding", "dormant"). Each asserts `expect(resolvePhase("not-connected", <bff>)).toBe("resolving")`.

    2. `describe("resolvePhase — wsState=opening → always resolving", () => { ... })` — 5 `it` blocks, same shape as above but wsState="opening".

    3. `describe("resolvePhase — wsState=open — terminal-phase branch", () => { ... })` with 5 `it` blocks:
       - `it("open + not-yet → resolving", ...)` asserts `.toBe("resolving")`
       - `it("open + active → active", ...)` asserts `.toBe("active")`
       - `it("open + session_holding → holding", ...)` asserts `.toBe("holding")`
       - `it("open + dormant → dormant", ...)` asserts `.toBe("dormant")`
       - `it("open + inactive → inactive", ...)` asserts `.toBe("inactive")`

    4. `describe("resolvePhase — wsState=failed-permanently → always error", () => { ... })` — 5 `it` blocks, all asserting `.toBe("error")` regardless of backendFirstFrame value (D-04: WS terminal failure is the only path to `error` phase).

    Total: 20 `it` blocks across 4 describes.

    Do NOT stub timers, do NOT mock anything — this is a pure function test. NO `beforeEach` / `afterEach` needed.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/resolve-phase.test.ts 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `src/ui/features/pretty-view/resolve-phase.test.ts` exists
    - Source contains the anchor comment `phase-29: truth-table tests for resolvePhase`
    - Test file has exactly 4 `describe(` blocks (one per wsState value)
    - Test file has exactly 20 `it(` blocks (4 × 5 truth-table combinations)
    - `npx vitest run src/ui/features/pretty-view/resolve-phase.test.ts` exits 0
    - Vitest output reports 20 tests passed, 0 failed
    - Test file does NOT use `vi.useFakeTimers` or `vi.stubGlobal` (pure function; no timer/global mocking needed)
  </acceptance_criteria>
  <done>All 20 truth-table combinations are individually asserted, tests pass, and the test file follows the layer1-detect.test.ts structural pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan touches only pure functions and unit tests. No trust boundaries are crossed. No user input is parsed. No WS/network/filesystem I/O.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01-01 | Tampering | resolve-phase.ts | accept | Pure client-side reducer with no external inputs beyond the two type-constrained parameters; TypeScript exhaustiveness sentinel (`_exhaust: never`) prevents silent tampering by adding new enum values without updating the branch list — such an attempt fails `tsc --noEmit` at build time. |
| T-29-01-02 | Information disclosure | resolve-phase.ts | accept | No logging, no analytics, no telemetry emission. Function has zero side effects. |

**Attack-surface delta:** none. This plan introduces no new attack surface — a pure function whose input types are constrained string unions defined in the same file.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 with no errors in resolve-phase.ts or resolve-phase.test.ts
- `npx vitest run src/ui/features/pretty-view/resolve-phase.test.ts` reports 20/20 tests passed
- Source-level grep on resolve-phase.ts:
  - `grep -c "^import " src/ui/features/pretty-view/resolve-phase.ts` returns 0
  - `grep -c "setTimeout\|Date.now\|require(" src/ui/features/pretty-view/resolve-phase.ts` returns 0
  - `grep -c "^export type " src/ui/features/pretty-view/resolve-phase.ts` returns exactly 3
  - `grep -c "^export function resolvePhase" src/ui/features/pretty-view/resolve-phase.ts` returns exactly 1
</verification>

<success_criteria>
- Pure resolvePhase function exists with the SPEC req 4 truth table encoded exactly
- Three type unions (WsState, BackendFirstFrame, Phase) exported with the exact members from SPEC
- 20 truth-table unit tests pass
- File is import-free (pure module); TypeScript exhaustiveness sentinel present
- Plan 29-02 can import `resolvePhase` and the type unions from this file with zero additional setup
</success_criteria>

<output>
Create `.planning/phases/29-unified-session-entry-state-machine-single-resolving-spinner/29-01-SUMMARY.md` when done, documenting: (a) the exact type union members exported, (b) the exhaustiveness sentinel used, (c) truth-table test count (should read "20/20 passed"), (d) any deviations from the PATTERNS.md analog and why.
</output>
