---
phase: 29-unified-session-entry-state-machine-single-resolving-spinner
plan: 05
type: execute
wave: 4
depends_on:
  - 29-01
  - 29-02
  - 29-03
  - 29-04
files_modified:
  - src/ui/features/pretty-view/PrettyView.test.tsx
  - src/ui/features/pretty-view/PrettyView.phase29.test.tsx
  - src/ui/features/pretty-view/PrettyViewLoadingOverlay.test.tsx
  - src/ui/features/pretty-view/SessionHoldingOverlay.test.tsx
  - src/ui/features/pretty-view/DormancyOverlay.test.tsx
  - src/ui/state/session-recycling-store.test.ts
autonomous: true
requirements:
  - PHASE29-REQ-01
  - PHASE29-REQ-02
  - PHASE29-REQ-05
  - PHASE29-REQ-06
  - PHASE29-REQ-07
tags:
  - phase-29
  - integration-tests
  - structural-grep-gates
  - flicker-regression

must_haves:
  truths:
    - "Every SPEC acceptance-criterion assertion has a corresponding test that fails if the invariant is violated"
    - "Structural-grep tests on PrettyView.tsx enforce: only resolving spinner during phase='resolving' (req 2); each terminal overlay gated on its phase value (req 6); zero setTimeout in usePaneResolvingMachine.ts beyond the delay-arm (req 5); no 'Connecting…' or 'Connection lost' strings (SPEC boundary)"
    - "Three named flicker regressions are locked: (a) black-screen 'Connecting…' on entry to an already-active pane, (b) 'Connection lost' half-screen box, (c) stale 'Waking up…' on an already-awake session — each drives the historical bad input sequence and asserts resolving spinner is the only overlay"
    - "session-recycling-store test locks resolving → holding transition per SPEC req 7 (dot NOT suppressed during resolving; dot suppressed when phase enters holding; dot un-suppressed when phase leaves holding)"
    - "All plan 29-04 test breakages (documented in that plan's SUMMARY) are resolved — existing PrettyView.test.tsx / PrettyViewLoadingOverlay.test.tsx / SessionHoldingOverlay.test.tsx / DormancyOverlay.test.tsx assertions updated to reference the new phase-driven mount gates"
    - "npx vitest run exits 0 with zero failures across the entire project"
  artifacts:
    - path: "src/ui/features/pretty-view/PrettyView.phase29.test.tsx"
      provides: "New phase 29 integration + structural-grep + flicker-regression test file"
      contains: "describe(\"phase 29"
    - path: "src/ui/features/pretty-view/PrettyView.test.tsx"
      provides: "Existing test file audited + updated to align with phase-29 mount gates"
      contains: "phase-29:"
  key_links:
    - from: "src/ui/features/pretty-view/PrettyView.phase29.test.tsx"
      to: "src/ui/features/pretty-view/PrettyView.tsx"
      via: "reads source + drives mocked WS to assert overlay behavior"
      pattern: "readFileSync.*PrettyView\\.tsx"
    - from: "src/ui/state/session-recycling-store.test.ts"
      to: "src/ui/features/pretty-view/PrettyView.tsx"
      via: "new test drives PrettyView through phase transitions and asserts publisher output"
      pattern: "publishSessionRecycling|getSessionRecyclingSnapshot"
---

<objective>
Land the phase 29 test suite: (a) fix every existing test broken by plan 29-04's PrettyView rewire, (b) add the new structural-grep gates + entry-trigger tests + three named flicker regression tests + session-recycling-store transition test that SPEC's acceptance criteria demand. Ship the phase code-complete: `npx vitest run` exits 0 with zero failures.

This is the "close the phase" plan. It runs in Wave 4 after every code-producing plan (29-01, 29-02, 29-03, 29-04) has landed. Its inputs are: plan 29-04's SUMMARY (listing the specific test breakages introduced by the PrettyView rewire, so this plan can audit each one), plan 29-04's shipped `wsState`+`backendFirstFrame` derivation logic (so the integration tests can drive the right mock inputs to produce the right phases), and the SPEC.md acceptance-criteria list (so no assertion is left unwritten).

Two files created new; five files modified. All existing tests updated in-place — no new "shadow test file" pattern. The new phase 29 integration + grep tests live in `PrettyView.phase29.test.tsx` (sibling to the existing `PrettyView.test.tsx`, following the existing sibling pattern `PrettyView.aside.test.tsx` and `PrettyView.virtualization.test.tsx`).

Purpose: Prove the phase 29 win holds — the three named flicker cases from Ashley's original report are structurally impossible in the post-refactor code, the SPEC acceptance-criteria list is 100% honored, and the full suite is green so tiffany can hand off to the deploy motion.

Output: Two new test files, four existing test files audited + updated, full suite green.

Deploy is out of scope (fleet rule: sub-agents do not do deploys). Once this plan lands green, tiffany takes over the rebase/build/deploy motion after Ashley greenlight.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-unified-session-entry-state-machine-single-resolving-spinner/29-SPEC.md
@.planning/phases/29-unified-session-entry-state-machine-single-resolving-spinner/29-CONTEXT.md
@.planning/phases/29-unified-session-entry-state-machine-single-resolving-spinner/29-PATTERNS.md
@.planning/phases/29-unified-session-entry-state-machine-single-resolving-spinner/29-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit + fix existing overlay tests broken by plan 29-04's PrettyView rewire</name>
  <files>
    src/ui/features/pretty-view/PrettyView.test.tsx,
    src/ui/features/pretty-view/PrettyViewLoadingOverlay.test.tsx,
    src/ui/features/pretty-view/SessionHoldingOverlay.test.tsx,
    src/ui/features/pretty-view/DormancyOverlay.test.tsx
  </files>
  <read_first>
    - .planning/phases/29-unified-session-entry-state-machine-single-resolving-spinner/29-04-SUMMARY.md (specific failing tests + their assertion messages — this is the input)
    - src/ui/features/pretty-view/PrettyView.test.tsx (existing test file — audit every describe/it that references retired state names)
    - src/ui/features/pretty-view/PrettyViewLoadingOverlay.test.tsx (5 existing tests; mount-gate change from `isBooting && !dormant && !showOverlay` to `phase === "resolving" && showResolvingSpinner` — tests that mount the component in isolation still work because the component itself is unchanged per D-01)
    - src/ui/features/pretty-view/SessionHoldingOverlay.test.tsx (mount-gate change from `showOverlay` to `phase === "holding"` — tests that mount the component in isolation still work; only tests that reach into PrettyView's mount context need audit)
    - src/ui/features/pretty-view/DormancyOverlay.test.tsx (mount-gate change from `dormant` to `phase === "dormant"` — similarly component-isolated tests are unaffected)
    - src/ui/features/pretty-view/PrettyView.tsx (as shipped by plan 29-04 — verify expected wsState/backendFirstFrame derivation to drive the mock WS correctly in test updates)
  </read_first>
  <action>
    Audit each failing test listed in plan 29-04's SUMMARY. For each test, categorize the failure into one of these three patterns and apply the corresponding fix. Every material change adds a `// phase-29:` comment tag explaining the update.

    Pattern A — "test asserts a retired local state or text": Update the assertion to reference the new phase-driven equivalent. Examples:
    - Test asserts `screen.getByText(/Connecting…/i)` → the "Connecting…" text is retired (SPEC boundary). REWRITE the test to assert `screen.getByRole("status", { name: /Loading/i })` (the resolving spinner's aria-label) OR delete the test if it was purely testing the retired text and no equivalent behavior remains. Prefer rewrite over delete when the underlying user-visible behavior (something covers the surface during connect) still exists.
    - Test asserts "Connection lost" text visible → REWRITE to assert `screen.getByRole("alert", { name: /Connection failed/i })` (the new PrettyViewErrorOverlay).
    - Test asserts `isBooting=true` behavior → REWRITE to assert `phase === "resolving"` observable behavior (spinner mounted with proper delay-arm).
    - Test asserts `showOverlay=true` behavior → REWRITE to assert `phase === "holding"` (SessionHoldingOverlay mounted).

    Pattern B — "test drives inputs that used to produce a specific overlay but now produce a different phase": Update the test's inputs to produce the intended phase under the new state machine. Example: a test that fires `ws.onopen` + `setStatus("streaming")` used to hide loading; under the new model the same sequence produces phase="active" via the state machine, and the spinner already stopped rendering. The test's assertion (`no loading overlay visible`) still holds — just verify the input sequence still exercises the intended edge.

    Pattern C — "test asserts the exact wall-clock timeout behavior of a retired watchdog": DELETE the test entirely and add a replacement `it.todo("[phase 29] retired: 600000ms holdingTimeoutError watchdog — no wall-clock deadline in new state machine (SPEC req 5)");` in the same describe block so the intent trail is preserved. Same for the 10s loading auto-dismiss test if one exists.

    For each of the 4 existing test files, at minimum:

    1. `PrettyView.test.tsx`:
       - Every describe block that references `isBooting` / `showOverlay` / `holdingTimeoutError` / `setStatus("connecting")` transient text / `errorMessage` transient text: audit + update per Pattern A/B/C above.
       - The Test A (patch #148 retry-on-close) at PrettyView.test.tsx:~342 likely still works because it doesn't gate on any retired UI state — verify by running.
       - Any test that mounts `<PrettyView ...>` and reaches for a specific overlay by text: update the query to use the new role-based queries (`getByRole("status" | "alert")`).
       - Add a `// phase-29: audit note` comment at the top of the file explaining the retired-state pattern for future readers.

    2. `PrettyViewLoadingOverlay.test.tsx`:
       - Component-isolated tests (rendering `<PrettyViewLoadingOverlay />` directly) are UNCHANGED by D-01 (component file body preserved). All 5 tests should still pass without modification.
       - Verify by running: `npx vitest run src/ui/features/pretty-view/PrettyViewLoadingOverlay.test.tsx`.
       - If any test fails, it's a real regression — investigate and fix.

    3. `SessionHoldingOverlay.test.tsx`:
       - Component-isolated tests unchanged. However: plan 29-04 dropped the `error={holdingTimeoutError}` prop from the mount site. If any test exercises the error variant AND requires it to be reachable from PrettyView context, either (a) rewrite to mount SessionHoldingOverlay directly with `error={true}` (component-isolated), or (b) mark as `it.todo("[phase 29] error variant no longer reachable from PrettyView — separate follow-up if reintroduced")`.

    4. `DormancyOverlay.test.tsx`:
       - Component-isolated tests unchanged. Nothing to update unless a specific test drives the parent PrettyView and asserts dormant-overlay mount behavior — in that case update the drive-input to produce backendFirstFrame="dormant" so the state machine phase settles on "dormant" and the overlay mounts.

    For each test file: after edits, run `npx vitest run <file>` and confirm exit 0.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/PrettyView.test.tsx src/ui/features/pretty-view/PrettyViewLoadingOverlay.test.tsx src/ui/features/pretty-view/SessionHoldingOverlay.test.tsx src/ui/features/pretty-view/DormancyOverlay.test.tsx 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Every failing test listed in `.planning/phases/29-unified-session-entry-state-machine-single-resolving-spinner/29-04-SUMMARY.md` is either updated to pass OR converted to a documented `it.todo(...)` with a phase-29 rationale
    - `npx vitest run` on the 4 files above exits 0 with zero failures
    - Every material update in the 4 files carries a `// phase-29:` comment tag explaining the change
    - No test assertion references retired state names: grep `showOverlay\|holdingTimeoutError\|isBooting` across the 4 test files should show only comment-annotated references (in `phase-29:` tags or `it.todo(...)` descriptions), zero live assertions
  </acceptance_criteria>
  <done>All 4 existing overlay/PrettyView test files pass after plan 29-04's rewire; retired-state references removed or explicitly documented as `it.todo`; audit comments in place.</done>
</task>

<task type="auto">
  <name>Task 2: Create new integration + structural-grep + flicker-regression tests</name>
  <files>src/ui/features/pretty-view/PrettyView.phase29.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/PrettyView.tsx (as shipped by plan 29-04 — needed to know how to drive mocked WS to produce specific phases)
    - src/ui/features/pretty-view/usePaneResolvingMachine.ts (hook shape)
    - src/ui/features/pretty-view/PrettyView.test.tsx (existing WS-mock harness pattern — lines 15-56 mock openClaudeSessionSocket; lines 290-322 fake-timers + ResizeObserver stub)
    - src/ui/features/terminal/Terminal.wiring.test.ts lines 544-627 (structural-grep test pattern with planted comment-tag anchors)
    - .planning/phases/29-unified-session-entry-state-machine-single-resolving-spinner/29-SPEC.md § "Acceptance Criteria" (every checkbox in this list needs a test that would fail if the invariant broke)
    - .planning/phases/29-unified-session-entry-state-machine-single-resolving-spinner/29-PATTERNS.md § 9 (structural-grep + flicker-regression patterns)
  </read_first>
  <action>
    Create `src/ui/features/pretty-view/PrettyView.phase29.test.tsx` — the new phase 29 integration + grep + flicker-regression test file.

    Header comment includes anchor: `// phase-29: integration + structural-grep + flicker-regression test suite (SPEC acceptance criteria)`

    Imports (mirror PrettyView.test.tsx harness pattern for WS mocking + timers; add readFileSync for structural-grep):
    ```
    import { describe, it, expect, beforeEach, afterEach, vi } from "vitest";
    import { render, screen, fireEvent, act } from "@testing-library/react";
    import { readFileSync } from "node:fs";
    import { join, dirname } from "node:path";
    import { fileURLToPath } from "node:url";
    // Import the WS-mock harness helpers from the existing PrettyView.test.tsx pattern — either
    // duplicate the vi.mock block or import a shared helper if one exists. Prefer duplication
    // for test-file isolation (the existing PrettyView.test.tsx uses a top-level vi.mock; do the
    // same here so the tests are independently runnable).
    // ... vi.mock('@/api/claude-session-api', () => ({ ... }))
    import { PrettyView } from "./PrettyView";

    const HERE = dirname(fileURLToPath(import.meta.url));
    const PV_SRC_PATH = join(HERE, "PrettyView.tsx");
    const HOOK_SRC_PATH = join(HERE, "usePaneResolvingMachine.ts");
    const RESOLVE_SRC_PATH = join(HERE, "resolve-phase.ts");
    ```

    Organize into 5 describe blocks:

    1. `describe("phase 29 — structural-grep gates (SPEC req 2, 5, 6, boundary)", () => { ... })`:
       Read PrettyView.tsx source via `readFileSync(PV_SRC_PATH, "utf-8")` in a `beforeEach` or once at the top of the describe. Then:
       - `it("SPEC req 2: only PrettyViewLoadingOverlay mounts under phase === 'resolving' (no other overlay in the same JSX block)", () => { ... })`:
         - Find the resolving-spinner mount block by anchor: `const anchorIdx = pvSrc.indexOf("phase-29: PrettyViewLoadingOverlay is the resolving-phase spinner");`
         - `expect(anchorIdx).toBeGreaterThan(0);`
         - `const block = pvSrc.slice(anchorIdx, anchorIdx + 500);` (window sized for the JSX condition + component name)
         - `expect(block).toContain('phase === "resolving"');`
         - `expect(block).toContain("PrettyViewLoadingOverlay");`
         - `expect(block).not.toContain("SessionHoldingOverlay");`
         - `expect(block).not.toContain("DormancyOverlay");`
         - `expect(block).not.toContain("PrettyViewErrorOverlay");`
       - `it("SPEC req 6: SessionHoldingOverlay gated on phase === 'holding'", () => { ... })`:
         - `const anchorIdx = pvSrc.indexOf("phase-29: SessionHoldingOverlay gated on phase");`
         - `expect(anchorIdx).toBeGreaterThan(0);`
         - `const block = pvSrc.slice(anchorIdx, anchorIdx + 300);`
         - `expect(block).toContain('phase === "holding"');`
         - `expect(block).toMatch(/<SessionHoldingOverlay\b/);`
       - `it("SPEC req 6: DormancyOverlay gated on phase === 'dormant'", () => { ... })`:
         - Same shape as above with the DormancyOverlay anchor + `phase === "dormant"`.
       - `it("SPEC req 6: inactive fallback gated on phase === 'inactive'", () => { ... })`:
         - Same shape with the inactive fallback anchor + `phase === "inactive"` + text "no active Claude session".
       - `it("SPEC req 6: PrettyViewErrorOverlay gated on phase === 'error'", () => { ... })`:
         - Same shape with the PrettyViewErrorOverlay anchor + `phase === "error"` + `<PrettyViewErrorOverlay`.
       - `it("SPEC req 5: usePaneResolvingMachine.ts contains exactly one setTimeout (the 150ms delay-arm)", () => { ... })`:
         - `const hookSrc = readFileSync(HOOK_SRC_PATH, "utf-8");`
         - `const matches = (hookSrc.match(/setTimeout\(/g) ?? []); expect(matches.length).toBe(1);`
         - `expect(hookSrc).not.toMatch(/setInterval|requestIdleCallback/);`
       - `it("SPEC req 5: PrettyView.tsx contains zero setTimeout in the retired-watchdog windows (grep on '600000' returns 0)", () => { ... })`:
         - Strip comment lines before grepping to avoid false positives from the DELETED-marker comments: `const nonComment = pvSrc.split("\n").filter((l) => !l.trim().startsWith("//")).join("\n");`
         - `expect(nonComment).not.toContain("600000");`
         - `expect(nonComment).not.toMatch(/setTimeout\([^)]+,\s*10000\s*\)/);` (10s auto-dismiss deleted)
       - `it("SPEC boundary: PrettyView.tsx does NOT render 'Connecting…' or 'Connection lost' text (non-comment lines)", () => { ... })`:
         - Same non-comment stripping as above.
         - `expect(nonComment).not.toContain("Connecting…");`
         - `expect(nonComment).not.toContain("Connection lost");`
       - `it("SPEC req 3: usePaneResolvingMachine.ts lists exactly wsState and backendFirstFrame as resolution inputs", () => { ... })`:
         - `const hookSrc = readFileSync(HOOK_SRC_PATH, "utf-8");`
         - `expect(hookSrc.indexOf("phase-29: resolution inputs — wsState + backendFirstFrame ONLY")).toBeGreaterThan(0);`
       - `it("SPEC req 4: resolve-phase.ts is import-free (pure module)", () => { ... })`:
         - `const resolveSrc = readFileSync(RESOLVE_SRC_PATH, "utf-8");`
         - `const importLines = resolveSrc.split("\n").filter((l) => l.match(/^import /)); expect(importLines.length).toBe(0);`

    2. `describe("phase 29 — SPEC req 1: entry-edge triggers enter resolving via one shared code path", () => { ... })`:
       Use the WS-mock + fake-timers harness. Render PrettyView with mocked openClaudeSessionSocket.
       - `it("cold mount: fresh pane render immediately shows phase='resolving' (delay-armed spinner mounts at 150ms)", () => { ... })`:
         - Render `<PrettyView hostId={1} tmuxSession="s1" isVisible={true} ...(other required props)... />`.
         - Immediately: assert no spinner overlay yet (still within 150ms delay-arm window).
         - `act(() => { vi.advanceTimersByTime(150); });`
         - Assert exactly one overlay is present: the resolving spinner. Use `screen.getByRole("status", { name: /Loading/i })`.
         - Assert NO other overlay is present: `expect(screen.queryByRole("alert")).toBeNull();` (no error overlay) and `expect(screen.queryByText(/session is asleep/i)).toBeNull();` (no dormancy overlay) and `expect(screen.queryByText(/session recycling/i)).toBeNull();` (no holding overlay).
       - `it("warm re-focus: hidden→visible flip on an already-mounted PrettyView re-enters resolving", () => { ... })`:
         - Render with isVisible=false initially. Drive to phase=active via WS mock (open + streaming). Assert no spinner.
         - Rerender with isVisible=true.
         - Assert phase transitions back to resolving; after 150ms delay-arm the spinner is the only overlay.
       - `it("PWA foreground: document.visibilitychange to visible re-enters resolving on a visible pane", () => { ... })`:
         - Render with isVisible=true. Drive to phase=active. Assert no spinner.
         - Fire `act(() => { document.dispatchEvent(new Event("visibilitychange")); });` with document.visibilityState="visible".
         - Assert phase transitions to resolving; spinner mounts after 150ms delay-arm.

    3. `describe("phase 29 — flicker regression 1: black-screen 'Connecting…' on entry to an already-active pane", () => { ... })`:
       - `it("historical bad path: fresh mount + isVisible toggle + streaming frame sequence — resolving spinner is the only overlay throughout, never a raw 'Connecting…' text", () => { ... })`:
         - Render PrettyView with the input sequence that produced this flicker under the old model (fresh mount, ws.onopen fires, then streaming frame arrives ~200ms later).
         - Throughout the entire window: assert `screen.queryByText(/Connecting…/i)` is null. Assert `screen.queryByText(/Connection lost/i)` is null.
         - After WS opens + streaming frame arrives: assert phase="active", no overlay mounted.
         - Assert during the 200ms pre-first-frame window: the resolving spinner is present (via `screen.getByRole("status", { name: /Loading/i })`) and it is the only overlay.

    4. `describe("phase 29 — flicker regression 2: 'Connection lost' half-screen box briefly appears", () => { ... })`:
       - `it("historical bad path: WS onclose during initial resolve produces error phase only after ladder exhausts, not a transient text flash", () => { ... })`:
         - Render, then simulate WS onclose repeatedly for MAX_RECONNECT_ATTEMPTS+1 iterations.
         - Assert throughout the retry-in-flight window: `screen.queryByText(/Connection lost/i)` is null; phase is "resolving" and spinner is up.
         - After the ladder exhausts (wsState becomes "failed-permanently"): assert `screen.getByRole("alert", { name: /Connection failed/i })` (the new PrettyViewErrorOverlay) is present and is the ONLY overlay.
         - Assert `screen.queryByRole("status", { name: /Loading/i })` is null (spinner gone).

    5. `describe("phase 29 — flicker regression 3: stale 'Waking up…' on a session that has been awake for a while", () => { ... })`:
       - `it("historical bad path: warm re-focus on a session that was previously dormant but has since woken — no stale 'Waking up…' flash", () => { ... })`:
         - Render with backendFirstFrame history that produced this flicker (previous session was dormant with waking=true, but now the fresh backend emit is active). Drive the WS mock to emit an `active`-equivalent first frame on the warm re-focus retry.
         - Assert throughout the warm re-focus + first-frame window: no "Waking up…" text is visible.
         - After first-frame arrives: phase="active", no overlay.

    Do NOT add tests that duplicate resolve-phase.test.ts (truth table) or usePaneResolvingMachine.test.tsx (hook behavior). This file is INTEGRATION-level only — it exercises PrettyView with mocked WS to prove the pieces compose correctly.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/PrettyView.phase29.test.tsx 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - File `src/ui/features/pretty-view/PrettyView.phase29.test.tsx` exists
    - Source contains anchor comment `phase-29: integration + structural-grep + flicker-regression test suite`
    - Test file has exactly 5 top-level describe blocks (structural gates + entry triggers + 3 flicker regressions)
    - Test file has at least 15 `it(` blocks total (8 structural-grep gates + 3 entry-trigger tests + 3 flicker regressions + a few edge assertions per block)
    - Each of the three named flicker regressions (a/b/c from SPEC background) has a dedicated describe block AND asserts the historical bad text is NOT visible
    - `npx vitest run src/ui/features/pretty-view/PrettyView.phase29.test.tsx` exits 0 with all tests passing
    - Structural-grep gate for SPEC req 5 (setTimeout count in hook file) reports exactly 1
    - Structural-grep gate for SPEC boundary (no Connecting…/Connection lost in PrettyView.tsx non-comment lines) passes
  </acceptance_criteria>
  <done>Phase 29 integration test file exists with 5 describe blocks covering structural-grep gates, entry triggers, and 3 flicker regressions; all tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Add session-recycling-store resolving→holding transition test (SPEC req 7)</name>
  <files>src/ui/state/session-recycling-store.test.ts</files>
  <read_first>
    - src/ui/state/session-recycling-store.test.ts (existing 5 tests — verify they still pass unchanged per SPEC req 7)
    - src/ui/state/session-recycling-store.ts (publish contract: publishSessionRecycling(key, isRecycling))
    - src/ui/features/pretty-view/PrettyView.tsx (post plan 29-04 — the publisher call site uses `phase === "holding"`)
    - src/ui/features/pretty-view/usePaneResolvingMachine.ts (hook — needed to know how to drive the phase transitions from a test)
    - .planning/phases/29-unified-session-entry-state-machine-single-resolving-spinner/29-SPEC.md § "Requirements" 7 acceptance criteria (existing tests still pass + new test locks resolving→holding transition)
    - .planning/phases/29-unified-session-entry-state-machine-single-resolving-spinner/29-PATTERNS.md § 10 (recycling-store test pattern)
  </read_first>
  <action>
    Add a new describe block to `src/ui/state/session-recycling-store.test.ts` locking the SPEC req 7 phase → recycling-store contract preservation.

    First: run `npx vitest run src/ui/state/session-recycling-store.test.ts` and confirm all 5 existing tests still pass unmodified (SPEC req 7 explicitly preserves this — do NOT touch existing tests).

    Then append a new describe block at the bottom of the file:

    ```
    // phase-29: SPEC req 7 — publish contract preserved with new source-of-truth (phase === "holding")
    describe("session-recycling-store — phase 29 resolving→holding transition (SPEC req 7)", () => {
      it("publish(key, false) during resolving → snapshot returns false (dot NOT suppressed during resolving)", () => { ... });
      it("publish(key, true) on transition into holding → snapshot returns true (dot suppressed)", () => { ... });
      it("publish(key, false) on transition out of holding → snapshot returns false (dot un-suppressed)", () => { ... });
      it("resolving → active → holding → active sequence: snapshot values track exactly {false, false, true, false}", () => { ... });
    });
    ```

    Implementation of each `it`:

    - Use the `__resetForTest()` helper (session-recycling-store.ts:136) in `beforeEach` (mirror existing test structure).
    - For each test: manually call `publishSessionRecycling(key, isRecycling)` with the expected boolean flag for each phase transition, then assert `getSessionRecyclingSnapshot().get(key)` returns the expected value.
    - This does NOT require mounting PrettyView — it exercises the store's contract directly. The store's caller (PrettyView post-refactor) is verified by task 2's structural-grep gate `expect(pvSrc).toMatch(/publishSessionRecycling.*phase === "holding"/)`.

    Do NOT modify any existing test. All 5 existing tests must continue to pass unchanged (SPEC req 7 explicit).

    Optionally: add a comment at the top of the existing test file noting `// phase-29: contract preserved — see appended describe block for the new phase-source-of-truth test.` for future readers.
  </action>
  <verify>
    <automated>npx vitest run src/ui/state/session-recycling-store.test.ts 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Existing 5 tests in `session-recycling-store.test.ts` continue to pass unchanged (verify by inspecting git diff — only the new describe block should appear as additions; existing tests untouched)
    - New describe block `session-recycling-store — phase 29 resolving→holding transition (SPEC req 7)` has at least 4 `it` blocks
    - `npx vitest run src/ui/state/session-recycling-store.test.ts` exits 0 with all tests (existing + new) passing
    - New tests exercise the store's public surface (publishSessionRecycling + getSessionRecyclingSnapshot) — no need to mount PrettyView here (integration path is covered by task 2's structural-grep gate)
  </acceptance_criteria>
  <done>session-recycling-store.test.ts has a new describe block covering the phase→publisher contract; all existing tests unchanged; full file passes.</done>
</task>

<task type="auto">
  <name>Task 4: Full-suite green precondition — npx vitest run exits 0</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx, src/ui/features/pretty-view/PrettyView.test.tsx, src/ui/features/pretty-view/PrettyView.phase29.test.tsx</files>
  <read_first>
    - Output of `npx vitest run 2>&amp;1` from prior task completions (assess remaining failures)
    - .planning/phases/29-unified-session-entry-state-machine-single-resolving-spinner/29-SPEC.md § "Constraints" (full-suite green precondition explicit)
  </read_first>
  <action>
    Run the full suite. Fix any remaining failures. The bar is `npx vitest run` exits 0 with zero failures.

    Investigation checklist for each remaining failure:
    1. Is this a test that was NOT in plan 29-04's SUMMARY breakage list but is now failing? — real regression, investigate.
    2. Is this a test in a file this plan already touched (task 1, 2, or 3)? — either the audit missed it or the fix was incomplete; loop back and correct.
    3. Is this a test in an unrelated file (e.g. Terminal.wiring.test.ts, PrettyConversationsPanel.test.tsx)? — check if plan 29-04's rewire broke something ambient (e.g. an assertion on the PrettyView export shape). Fix without regressing the phase 29 work.

    Do NOT paper over failures with `it.skip` or `it.todo` unless the test's assertion was truly semantically invalidated by the plan 29-04 rewire and cannot be rewritten to a phase-29-compatible equivalent. In that case: convert to `it.todo("[phase 29] <reason>")` with a clear rationale.

    Also run `npx tsc --noEmit` and confirm 0 errors. Fix any type errors introduced across the phase.

    Per fleet constraint: `npm run build:backend` is NOT required unless `git diff --stat src/backend/` shows changes. Confirm no backend files changed:
    ```
    git diff --stat src/backend/ 2>/dev/null
    ```
    If this reports any files, that's a scope-violation — the phase is frontend-only. Revert accidental backend changes.

    Do NOT run docker build, docker compose up, git push, docker cp, or nginx reload. Deploy is orchestrator-only (fleet rule).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | tail -5; echo "---"; npx vitest run 2>&amp;1 | tail -20; echo "---"; git diff --stat src/backend/ 2>/dev/null || echo "no backend changes"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 with zero type errors
    - `npx vitest run` exits 0 with zero failures (all suites green)
    - `git diff --stat src/backend/` reports zero backend files changed (frontend-only phase per fleet constraint)
    - No tests were converted to `it.skip` (allowed: `it.todo` with a phase-29 rationale for truly invalidated assertions)
    - Test count net change: at least +25 (20 from plan 29-01 truth-table + 5-6 from plan 29-02 error overlay + 8-10 from plan 29-03 hook behavior + 15+ from task 2 integration + 4 from task 3 recycling-store), minus any deleted tests from task 1's audit
  </acceptance_criteria>
  <done>Full suite green; tsc clean; no backend changes; no skipped tests. Phase 29 is code-complete pending Ashley UAT + deploy motion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan touches only test files (no production code changes). Test files run in the vitest jsdom sandbox. No new attack surface introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-05-01 | Tampering | Structural-grep gates | mitigate | The grep gates enforce the phase 29 invariants at the source-code level; a future rebase that quietly reintroduces `showOverlay` or a `setTimeout` watchdog fails the grep count. Refactor-safe (grep matches surface tokens, not exact line numbers). |
| T-29-05-02 | Tampering | Flicker regression tests | mitigate | Each of Ashley's three named flicker cases has a dedicated test that drives the historical bad input sequence; a regression that reintroduces any of the three flicker patterns fails its specific test. |
| T-29-05-03 | Repudiation | `it.todo` conversions | mitigate | Every `it.todo` conversion carries a `[phase 29]` prefix + rationale in its description, making the intent trail visible in vitest output for future readers. |

**Attack-surface delta:** none. Test-only file additions/modifications; production code untouched by this plan (previous plans owned production edits).

**Test-only deviation note (per patch quick-260808-ho2, quick-260809-cnx precedent):** if any test needs to silence a console.info from the retired 10s auto-dismiss diagnostic (which is deleted by plan 29-04 — no silencing needed) OR if the jsdom document.visibilityState needs to be overridden to test the PWA foreground trigger (via `Object.defineProperty(document, "visibilityState", { configurable: true, value: "visible" })`), document the specific deviation inline with a `// phase-29 test-only:` comment tag mirroring the existing precedent.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run` exits 0 with zero failures (this is the SPEC constraint's hard bar)
- `git diff --stat src/backend/` reports zero backend files changed
- All new test files carry `phase-29:` anchor comments; all updated test files carry `phase-29:` audit comments
- Ashley's three named flicker cases each have a passing regression test
- Every SPEC.md acceptance-criteria checkbox has a test that would fail if the invariant broke (either an existing test, a new phase 29 test, or an existing test audited to remain aligned)
</verification>

<success_criteria>
- Phase 29 SPEC acceptance-criteria list is 100% covered by tests
- Full vitest suite is green
- tsc is clean
- No backend files touched
- Two new test files (PrettyView.phase29.test.tsx + updated session-recycling-store.test.ts describe block)
- Four existing overlay/PrettyView test files audited + updated
- Phase 29 is CODE-COMPLETE — deploy motion (rebase, build, verify, push, patches.md entry) is orchestrator-only and NOT part of any plan in this phase
</success_criteria>

<output>
Create `.planning/phases/29-unified-session-entry-state-machine-single-resolving-spinner/29-05-SUMMARY.md` when done. MUST document:
- Full test count delta (before phase 29 vs after)
- Test file inventory: which test files created new, which modified, which unchanged
- Any `it.todo` conversions with rationale
- Any real regressions found + fixed (as distinct from planned test-update work)
- Confirmation of the deploy-scope handoff: "Phase 29 is CODE-COMPLETE. Tests green. tsc clean. Deploy motion (rebase to main, docker build, verify on staging, git push, patches.md entry) is orchestrator-only per fleet rule — awaiting Ashley greenlight before tiffany initiates deploy."
</output>
