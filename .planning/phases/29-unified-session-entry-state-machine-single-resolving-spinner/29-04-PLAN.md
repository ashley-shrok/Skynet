---
phase: 29-unified-session-entry-state-machine-single-resolving-spinner
plan: 04
type: execute
wave: 3
depends_on:
  - 29-01
  - 29-02
  - 29-03
files_modified:
  - src/ui/features/pretty-view/PrettyView.tsx
autonomous: true
requirements:
  - PHASE29-REQ-02
  - PHASE29-REQ-05
  - PHASE29-REQ-06
  - PHASE29-REQ-07
tags:
  - phase-29
  - prettyview-rewire
  - deletion-heavy

must_haves:
  truths:
    - "PrettyView.tsx consumes usePaneResolvingMachine and derives all overlay mount gates from the returned `phase`"
    - "Only PrettyViewLoadingOverlay renders when phase === 'resolving' — all other overlays are gated on phase !== 'resolving' (SPEC req 2)"
    - "Every terminal overlay is mounted only when phase equals its terminal value: SessionHoldingOverlay iff 'holding', DormancyOverlay iff 'dormant', inactive fallback iff 'inactive', PrettyViewErrorOverlay iff 'error' (SPEC req 6)"
    - "The 600000ms holdingTimeoutError watchdog is deleted; the 10s PrettyViewLoadingOverlay auto-dismiss is deleted (SPEC req 5)"
    - "The strings 'Connection lost' and 'Connecting…' are not rendered anywhere in PrettyView.tsx (SPEC boundary)"
    - "session-recycling-store publishes on [phase === 'holding', hostId, tmuxSession] instead of [showOverlay, hostId, tmuxSession] (SPEC req 7)"
    - "ComposeBox recycleActive, dormantActive, reconnectingActive props derive from phase, not from the retired local state hooks"
  artifacts:
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "Rewired host component consuming the new state machine hook"
      contains: "usePaneResolvingMachine"
  key_links:
    - from: "src/ui/features/pretty-view/PrettyView.tsx"
      to: "src/ui/features/pretty-view/usePaneResolvingMachine.ts"
      via: "hook call at top of component body"
      pattern: "usePaneResolvingMachine\\("
    - from: "src/ui/features/pretty-view/PrettyView.tsx"
      to: "src/ui/features/pretty-view/PrettyViewErrorOverlay.tsx"
      via: "mount site gated on phase === 'error'"
      pattern: "PrettyViewErrorOverlay"
    - from: "src/ui/features/pretty-view/PrettyView.tsx"
      to: "src/ui/state/session-recycling-store.ts"
      via: "publishSessionRecycling call keyed on phase === 'holding'"
      pattern: "publishSessionRecycling"
---

<objective>
Rewire `PrettyView.tsx` to consume the new `usePaneResolvingMachine` hook as the single authoritative pane-entry state machine. Replace ~5 racing local-state-driven mount gates with `phase === "<value>"` guards, delete the two wall-clock watchdogs (600000ms holding-timeout + 10s loading auto-dismiss) that SPEC req 5 bans, retire the transient "Connecting…" and "Connection lost" text nodes that SPEC boundary retires, mount the new `PrettyViewErrorOverlay` at `phase === "error"`, and update the session-recycling-store publisher to derive from `phase === "holding"` (SPEC req 7 contract-preserving change).

This is the deletion-heavy plan: it retires the `showOverlay` state (patch #74's delay-arm), the `holdingTimeoutError` state, the `isBooting` state + ref, and their associated effects; it retires the two setTimeout watchdogs; it retires the transient text-status branches. What remains: `dormant`/`waking`/`wakingStartTs`/`elapsedSeconds`/`wakeError` stay LOCAL (they carry DormancyOverlay's internal prop state, not mount-gate state — the mount gate flips from `dormant` to `phase === "dormant"`), and the ComposeBox prop wiring is updated to derive `recycleActive`/`dormantActive`/`reconnectingActive` from `phase` instead of the retired locals.

Wave 3 — depends on plan 29-01 (resolve-phase.ts), plan 29-02 (PrettyViewErrorOverlay), and plan 29-03 (usePaneResolvingMachine).

Purpose: Land the actual UI-behavior change Ashley asked for. Once this plan ships, the three named flicker cases are structurally impossible: only one overlay can render during `phase === "resolving"`, and the terminal phase resolves deterministically from `wsState`+`backendFirstFrame`.

Output: rewired `PrettyView.tsx` with `usePaneResolvingMachine` mounted, all mount gates flipped to `phase === "..."`, two watchdogs deleted, transient text deleted, error overlay wired, ComposeBox props rewired, session-recycling-store publisher rewired.

TESTS: Existing `PrettyView.test.tsx` may fail on assertions that referenced the retired local states (isBooting, showOverlay, status="connecting"/"error" text). Plan 29-05 owns updating the existing test file + adding new integration tests. This plan touches ONLY `PrettyView.tsx` — the test file updates land in the next plan to keep this change reviewable.

This plan MUST leave the codebase in a state where `npx tsc --noEmit` exits 0 and `npx vitest run` may have test failures that are the tracked responsibility of plan 29-05 (documented in this plan's SUMMARY). The failing tests should ONLY be tests whose assertions reference the retired states; no runtime regressions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-unified-session-entry-state-machine-single-resolving-spinner/29-SPEC.md
@.planning/phases/29-unified-session-entry-state-machine-single-resolving-spinner/29-CONTEXT.md
@.planning/phases/29-unified-session-entry-state-machine-single-resolving-spinner/29-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire usePaneResolvingMachine into PrettyView + rewire mount gates + delete watchdogs</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/PrettyView.tsx (full file — this task rewires ~10-15 distinct sites; read the whole file to see current state and avoid breaking unrelated wiring)
    - src/ui/features/pretty-view/usePaneResolvingMachine.ts (just-created hook — imports + shape)
    - src/ui/features/pretty-view/resolve-phase.ts (WsState + BackendFirstFrame type unions — needed to derive wsState and backendFirstFrame from existing PrettyView state)
    - src/ui/features/pretty-view/PrettyViewErrorOverlay.tsx (just-created component — import + mount)
    - src/ui/features/pretty-view/PrettyViewLoadingOverlay.tsx (unchanged; kept as the resolving-phase spinner render per D-01)
    - src/ui/features/pretty-view/SessionHoldingOverlay.tsx (unchanged; mount gate flips only)
    - src/ui/features/pretty-view/DormancyOverlay.tsx (unchanged; mount gate flips only)
    - .planning/phases/29-unified-session-entry-state-machine-single-resolving-spinner/29-SPEC.md § "Requirements" 2, 5, 6, 7 acceptance criteria
    - .planning/phases/29-unified-session-entry-state-machine-single-resolving-spinner/29-CONTEXT.md § "Implementation Decisions" D-01 (spinner visual identity), D-10-D-12 (post-resolve semantics), and § "Claude's Discretion" (migration ordering, publisher relocation, failed-permanently derivation)
    - .planning/phases/29-unified-session-entry-state-machine-single-resolving-spinner/29-PATTERNS.md § 7 (current mount-gate JSX to REPLACE + target shape + effects to DELETE + state hooks to SUBSUME/DELETE)
  </read_first>
  <action>
    Rewire `src/ui/features/pretty-view/PrettyView.tsx`. Work in three logical sub-steps: (A) derive inputs and mount the hook, (B) rewire mount gates + retire text nodes, (C) delete retired effects + rewire the recycling publisher + rewire ComposeBox props.

    Every material change site adds a comment tagged `// phase-29: <purpose>` for plan 29-05's structural-grep tests to anchor on. Every DELETION replaces the deleted block with a one-line comment: `// phase-29: DELETED — <what was here> (SPEC req 5 no-timeout-heuristics; see phase 29 SUMMARY)` so the intent is discoverable in a future rebase / bisect.

    Per D-01 (Option A of PATTERNS.md section 8): KEEP `PrettyViewLoadingOverlay.tsx` unchanged. Its file body does not change; only its mount gate flips. This preserves the existing `PrettyViewLoadingOverlay.test.tsx` test suite verbatim.

    Per D-08 error-copy default: pass no props to `PrettyViewErrorOverlay` beyond `onRetry` (component copy is fixed at "Connection failed — retry" per D-08 default; UAT can adjust the component's internal copy later without touching PrettyView.tsx).

    Sub-step A — derive resolution inputs + mount the hook:

    1. Add import at the top of the file (alphabetize with existing imports):
       - `import { usePaneResolvingMachine } from "./usePaneResolvingMachine";`
       - `import { PrettyViewErrorOverlay } from "./PrettyViewErrorOverlay";`
       - Import the type unions if needed for local derivation: `import type { WsState, BackendFirstFrame } from "./resolve-phase";`

    2. Derive `wsState: WsState` from existing state. Per Claude's Discretion in CONTEXT.md, pick the observation-based derivation (no changes to WS retry ladder in this phase — SPEC out-of-scope, D-13). Compute in the component body directly above the hook mount:
       ```
       // phase-29: derive wsState for the state machine from existing PrettyView state.
       // - status === "connecting" AND reconnectAttemptsRef.current === 0 → "not-connected"
       // - status === "connecting" AND reconnectAttemptsRef.current > 0    → "opening" (retry in flight)
       // - status === "streaming" OR "inactive"                            → "open"
       // - status === "error" AND reconnectAttemptsRef.current >= MAX_RECONNECT_ATTEMPTS → "failed-permanently"
       // - status === "error" AND reconnectAttemptsRef.current <  MAX_RECONNECT_ATTEMPTS → "opening" (retry pending)
       ```
       Implementation choice: because `reconnectAttemptsRef` is a ref (mutations do NOT trigger re-render), pair the derivation with a small state slot to observe attempt-count changes across re-renders. Add:
       ```
       const [reconnectAttempts, setReconnectAttempts] = useState<number>(0);
       ```
       Then wherever `reconnectAttemptsRef.current` is mutated (grep for `reconnectAttemptsRef.current = 0` and `reconnectAttemptsRef.current += 1` — there are ~3 sites), immediately follow with `setReconnectAttempts(reconnectAttemptsRef.current);`. Add a comment `// phase-29: mirror into state so wsState derivation re-runs on retry-attempt changes` at each mutation site.

       Then derive wsState synchronously in the render body (single expression):
       ```
       // phase-29: wsState derivation for the state machine (D-13 — WS ladder unchanged, observed only).
       const wsState: WsState =
         status === "streaming" || status === "inactive"
           ? "open"
           : status === "error" && reconnectAttempts >= MAX_RECONNECT_ATTEMPTS
             ? "failed-permanently"
             : status === "connecting" && reconnectAttempts === 0
               ? "not-connected"
               : "opening";
       ```

    3. Derive `backendFirstFrame: BackendFirstFrame` from existing state. This one is trickier because the current code reacts to each frame type reactively; there is no single "first frame observed" hook. Add a per-pane ref that captures the first-frame verdict:
       ```
       // phase-29: backendFirstFrame — capture the first backend verdict per pane. Reset on paneKey change.
       const backendFirstFrameRef = useRef<BackendFirstFrame>("not-yet");
       const [backendFirstFrame, setBackendFirstFrame] = useState<BackendFirstFrame>("not-yet");
       ```
       Add a small helper `const captureFirstFrame = useCallback((v: BackendFirstFrame) => { if (backendFirstFrameRef.current !== "not-yet") return; backendFirstFrameRef.current = v; setBackendFirstFrame(v); }, []);`.
       Then at the WS message handler sites (grep for `setStatus("streaming")` at ~L909, `setStatus("inactive")` at ~L954, `setIsHolding(true)` at ~L1100, `case "dormant":` at ~L967), insert a `captureFirstFrame(...)` call BEFORE the existing setState calls:
       - Before the `setStatus("streaming")` at ~L909 in the initial `session` frame case: `captureFirstFrame("active");`
       - Before the `setStatus("inactive")` at ~L954: `captureFirstFrame("inactive");`
       - Before the `setIsHolding(true)` at ~L1100 in the session_holding frame case: `captureFirstFrame("session_holding");`
       - Inside the `case "dormant":` at ~L967, when `parsed.dormant === true`: `captureFirstFrame("dormant");` (only true dormant counts as a first-frame verdict; `dormant === false` frames do not — they're clearing a previously-set dormant state).
       Additionally: reset `backendFirstFrameRef.current` to `"not-yet"` (and `setBackendFirstFrame("not-yet")`) inside the fresh-pane cold-mount reset block at PrettyView.tsx:725-767. Add comment `// phase-29: reset first-frame verdict on cold mount (new pane needs to re-resolve)`.

    4. Mount the hook AFTER the derivations above, BEFORE any JSX return. Also compute the derived `phase` and `showSpinner` locals from the returned object:
       ```
       // phase-29: single authoritative pane-entry state machine (SPEC req 1, 3).
       const { phase, showSpinner: showResolvingSpinner, requestRetry } = usePaneResolvingMachine({
         hostId,
         tmuxSession,
         isVisible,
         wsState,
         backendFirstFrame,
       });
       ```

    5. Define the retry handler as a stable callback for the error overlay:
       ```
       // phase-29: retry handler for PrettyViewErrorOverlay (D-09).
       // Fresh WS reconnect via a synthetic entry-trigger edge.
       const handleRetry = useCallback(() => {
         reconnectAttemptsRef.current = 0;
         setReconnectAttempts(0);
         setStatus("connecting");
         requestRetry();
         setRetryKey((k) => k + 1);
       }, [requestRetry]);
       ```

    Sub-step B — rewire mount gates + retire text nodes (at PrettyView.tsx:~1770-1875 region per PATTERNS.md section 7):

    6. REPLACE the block at PrettyView.tsx:~1834 (`{showOverlay && <SessionHoldingOverlay error={holdingTimeoutError} />}`) with:
       ```
       {/* phase-29: SessionHoldingOverlay gated on phase === "holding" (SPEC req 6). */}
       {phase === "holding" && <SessionHoldingOverlay />}
       ```
       NOTE: Drop the `error={holdingTimeoutError}` prop — holdingTimeoutError is being retired (SPEC req 5). SessionHoldingOverlay's default `error=false` covers the base case. If Ashley subsequently wants a distinct visual for a long-held session, that is a separate follow-up.

    7. REPLACE the `{dormant && <DormancyOverlay ... />}` block at PrettyView.tsx:~1841-1849 with:
       ```
       {/* phase-29: DormancyOverlay gated on phase === "dormant" (SPEC req 6). Internal props (waking/elapsedSeconds/wakeError) stay local. */}
       {phase === "dormant" && (
         <DormancyOverlay
           waking={waking}
           elapsedSeconds={elapsedSeconds}
           onWake={handleWake}
           error={wakeError}
         />
       )}
       ```

    8. REPLACE the `{isBooting && !dormant && !showOverlay && <PrettyViewLoadingOverlay />}` block at PrettyView.tsx:~1856 with:
       ```
       {/* phase-29: PrettyViewLoadingOverlay is the resolving-phase spinner (D-01). Mount gate flips from isBooting/showOverlay/dormant mutual-exclusion to phase-derived + delay-armed showResolvingSpinner (D-04). */}
       {phase === "resolving" && showResolvingSpinner && <PrettyViewLoadingOverlay />}
       ```

    9. DELETE the `{status === "connecting" && ...Connecting…}` block at PrettyView.tsx:~1857-1861. Replace with:
       ```
       {/* phase-29: DELETED — status === "connecting" transient "Connecting…" text node (SPEC boundary + acceptance grep). Resolving spinner covers this window now. */}
       ```

    10. REPLACE the `{status === "inactive" && !dormant && ...}` block at PrettyView.tsx:~1863-1867 with:
        ```
        {/* phase-29: inactive fallback gated on phase === "inactive" (SPEC req 6). */}
        {phase === "inactive" && (
          <div className="flex-1 flex items-center justify-center p-4 text-sm text-[var(--color-pv-fg-muted)]">
            no active Claude session
          </div>
        )}
        ```

    11. DELETE the `{status === "error" && errorMessage && ...}` block at PrettyView.tsx:~1869-1871. Replace with the new PrettyViewErrorOverlay mount:
        ```
        {/* phase-29: PrettyViewErrorOverlay gated on phase === "error" — replaces transient "Connection lost" text (SPEC boundary + req 6). */}
        {phase === "error" && <PrettyViewErrorOverlay onRetry={handleRetry} />}
        ```

    Sub-step C — delete retired effects + rewire publisher + rewire ComposeBox props:

    12. DELETE the 600000ms holdingTimeoutError watchdog effect at PrettyView.tsx:~1508-1522 (`useEffect(() => { if (!isHolding) return; const t = setTimeout(() => { setHoldingTimeoutError(true); }, 600000); ... }, [isHolding]);`). Replace with:
        ```
        // phase-29: DELETED — 600000ms holdingTimeoutError watchdog (SPEC req 5 no-timeout-heuristics).
        // Backend HOLDING_TIMEOUT_TICKS=200 (10min) remains the authoritative "give up" signal;
        // when it fires, backend emits an `inactive` frame which the state machine reads via
        // backendFirstFrame — no client-side wall-clock deadline needed.
        ```

    13. DELETE the paired `holdingTimeoutError` reset effect at PrettyView.tsx:~1530-1533 (`useEffect(() => { if (!isHolding) setHoldingTimeoutError(false); }, [isHolding]);`). Replace with:
        ```
        // phase-29: DELETED — holdingTimeoutError reset effect (retired with the watchdog above).
        ```

    14. DELETE the 10s PrettyViewLoadingOverlay auto-dismiss effect at PrettyView.tsx:~1547-1554 (`useEffect(() => { if (!isBooting) return; const t = setTimeout(() => { console.info(...); setIsBooting(false); }, 10000); ... }, [isBooting]);`). Replace with:
        ```
        // phase-29: DELETED — 10s PrettyViewLoadingOverlay auto-dismiss (SPEC req 5 no-timeout-heuristics).
        // Resolving spinner now stays up until wsState transitions off "opening"/"not-connected"
        // AND backendFirstFrame transitions off "not-yet", OR wsState hits "failed-permanently".
        // No wall-clock dismissal.
        ```

    15. DELETE the delay-arm `showOverlay` effect at PrettyView.tsx:~1487-1497 (patch #74's `useEffect(() => { if (!isHolding) { setShowOverlay(false); return; } const t = setTimeout(() => { setShowOverlay(true); }, 350); ... }, [isHolding]);`). Replace with:
        ```
        // phase-29: DELETED — patch #74 showOverlay delay-arm useEffect. Delay-arm semantics moved
        // into usePaneResolvingMachine's showSpinner (D-04 — 150ms for the resolving spinner).
        // SessionHoldingOverlay now mounts directly on phase === "holding" without a separate
        // delay-arm state (post-resolve steady state means holding phase enters cleanly without
        // spinner flash — see D-11).
        ```

    16. RETIRE state hooks (SPEC req 5 + PATTERNS.md section 7):
        - DELETE `const [showOverlay, setShowOverlay] = useState(false);` at PrettyView.tsx:~319
        - DELETE `const [holdingTimeoutError, setHoldingTimeoutError] = useState(false);` at PrettyView.tsx:~326
        - DELETE `const [isBooting, setIsBooting] = useState(false);` at PrettyView.tsx:~343
        - DELETE `const isBootingRef = useRef<boolean>(false);` at PrettyView.tsx:~348 (and its mirror effect at PrettyView.tsx:~1347-1353)
        - DELETE every remaining `setHoldingTimeoutError(...)` call site (grep for all occurrences and remove; the state no longer exists)
        - DELETE every remaining `setIsBooting(...)` call site (grep for all; state gone)
        - DELETE every remaining `setShowOverlay(...)` call site (grep for all; state gone)
        - DELETE every `isBootingRef.current = ...` mirror line
        - KEEP `[isHolding, setIsHolding]` at PrettyView.tsx:~307 — it's still an internal signal used by the WS handlers to note recycle detection; but its role is now purely to trigger `captureFirstFrame("session_holding")` (from step 3). Add a rename-comment: `// phase-29: isHolding retained as an internal recycle-detection flag; its overlay-visibility role is retired (phase === "holding" now owns that).`
        - KEEP dormant/waking/wakingStartTs/elapsedSeconds/wakeError/handleWake — they carry DormancyOverlay's internal state, not the mount gate. Add a comment above the `const [dormant, ...] = useState(false);` block: `// phase-29: dormant/waking/etc. remain local because DormancyOverlay reads them as props. Mount gate flips from `dormant` to `phase === "dormant"`. State machine reads `captureFirstFrame("dormant")` on the dormant=true frame path — that is how the phase transition is armed.`

    17. REWIRE the session-recycling-store publisher at PrettyView.tsx:~1576-1579 (SPEC req 7). REPLACE:
        ```
        useEffect(() => {
          const key = `${hostId}:${tmuxSession ?? ""}`;
          publishSessionRecycling(key, showOverlay);
        }, [showOverlay, hostId, tmuxSession]);
        ```
        WITH:
        ```
        // phase-29: publish session-recycling state to store keyed on phase === "holding" (SPEC req 7).
        // Semantic preserved (dot suppressed exactly when the holding overlay is visible), source
        // of truth changed from `showOverlay` (retired) to `phase === "holding"` (deterministic).
        useEffect(() => {
          const key = `${hostId}:${tmuxSession ?? ""}`;
          publishSessionRecycling(key, phase === "holding");
        }, [phase, hostId, tmuxSession]);
        ```

    18. REWIRE ComposeBox props at PrettyView.tsx:~2085-2122. Find the `<ComposeBox ...` mount and update the four affected props (leave every other prop untouched):
        - `isHolding={isHolding}` → keep as-is (isHolding local state is retained per step 16)
        - `recycleActive={showOverlay}` → `recycleActive={phase === "holding"}`
        - `reconnectingActive={status === "error"}` → `reconnectingActive={phase === "error"}`
        - `dormantActive={dormant || waking}` → `dormantActive={phase === "dormant" || waking}` (waking stays as internal in-flight-wake signal even during a transient phase transition)

        Also update the outer mount-gate on ComposeBox at PrettyView.tsx:~2086 (`{onSend && (status === "streaming" || status === "error" || dormant) && (`). ComposeBox should render whenever ANY of the terminal phases can accept input — mirror the existing logic but read from phase:
        - REPLACE with: `{onSend && (status === "streaming" || phase === "error" || phase === "dormant") && (`
        - Add comment: `// phase-29: ComposeBox mount gate — streaming (post-resolve active), error, dormant. Resolving/inactive/holding don't need ComposeBox rendered (holding disables via recycleActive but overlay covers the surface).`
        - Actually reconsider — hold this replacement, and check if a preserving-current-behavior formulation is simpler: `status === "streaming"` still works post-refactor because status is unchanged. Simply flip `status === "error"` → `phase === "error"` and `dormant` → `phase === "dormant"`. Keep the outer OR-chain structure intact.

    Sub-step D — cleanup verification (in-task):

    19. After all edits: grep for orphaned references to retired state names. Zero of the following should appear ANYWHERE in the file post-edit:
        - `showOverlay` (state name — should be entirely removed)
        - `holdingTimeoutError` (state name — should be entirely removed)
        - `setIsBooting` (setter — state gone)
        - `isBootingRef` (ref — retired with isBooting)
        - `setHoldingTimeoutError` (setter)
        - `setShowOverlay` (setter)
        - `"Connecting…"` (retired text node — U+2026 ellipsis)
        - `"Connection lost"` (retired text node)
        - `"Connection closed"` (this appears in the WS onclose errorMessage — retain per D-13 "WS ladder unchanged"; it's not a user-facing UI string post-refactor since the error overlay hides errorMessage)

    20. Verify `npx tsc --noEmit` exits 0 for the whole project. Fix any type errors introduced by the rewire (typically wrong-type or unused-import complaints).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | grep -E "PrettyView\.tsx" | head -20; echo "---"; grep -c "phase-29:" src/ui/features/pretty-view/PrettyView.tsx; echo "--- retired-name check (all should be 0):"; grep -c "showOverlay\|holdingTimeoutError\|setIsBooting\|isBootingRef\|Connecting…\|Connection lost" src/ui/features/pretty-view/PrettyView.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 with zero errors mentioning PrettyView.tsx
    - `grep -c "usePaneResolvingMachine(" src/ui/features/pretty-view/PrettyView.tsx` returns exactly 1
    - `grep -c "PrettyViewErrorOverlay" src/ui/features/pretty-view/PrettyView.tsx` returns at least 2 (import + mount)
    - `grep -c "phase-29:" src/ui/features/pretty-view/PrettyView.tsx` returns at least 15 (comment tags on every material change site)
    - Retired-name grep gates (all MUST return 0):
      - `grep -c "\bshowOverlay\b" src/ui/features/pretty-view/PrettyView.tsx` returns 0
      - `grep -c "\bholdingTimeoutError\b" src/ui/features/pretty-view/PrettyView.tsx` returns 0
      - `grep -c "\bsetIsBooting\b" src/ui/features/pretty-view/PrettyView.tsx` returns 0
      - `grep -c "\bisBootingRef\b" src/ui/features/pretty-view/PrettyView.tsx` returns 0
      - `grep -c "\bsetHoldingTimeoutError\b" src/ui/features/pretty-view/PrettyView.tsx` returns 0
      - `grep -c "\bsetShowOverlay\b" src/ui/features/pretty-view/PrettyView.tsx` returns 0
    - Retired UI-string grep gates (excluding comments — use `-v '^\s*//'` filter or grep only string literals):
      - `grep -v '^\s*//' src/ui/features/pretty-view/PrettyView.tsx | grep -c 'Connecting…'` returns 0
      - `grep -v '^\s*//' src/ui/features/pretty-view/PrettyView.tsx | grep -c 'Connection lost'` returns 0
    - Watchdog deletion gates:
      - `grep -c "600000" src/ui/features/pretty-view/PrettyView.tsx` returns 0 (the holding-timeout constant no longer appears)
      - `grep -c "10s timeout dismiss" src/ui/features/pretty-view/PrettyView.tsx` returns 0 (the 10s loading auto-dismiss log line is gone)
    - Mount-gate presence gates:
      - `grep -c 'phase === "resolving"' src/ui/features/pretty-view/PrettyView.tsx` returns at least 1
      - `grep -c 'phase === "holding"' src/ui/features/pretty-view/PrettyView.tsx` returns at least 2 (overlay mount + recycling publisher)
      - `grep -c 'phase === "dormant"' src/ui/features/pretty-view/PrettyView.tsx` returns at least 2 (overlay mount + ComposeBox prop)
      - `grep -c 'phase === "inactive"' src/ui/features/pretty-view/PrettyView.tsx` returns at least 1
      - `grep -c 'phase === "error"' src/ui/features/pretty-view/PrettyView.tsx` returns at least 2 (overlay mount + ComposeBox prop)
    - The session-recycling publisher's dependency array contains `phase, hostId, tmuxSession` (grep for `[phase, hostId, tmuxSession]`)
    - `git diff --stat src/backend/ 2>/dev/null` reports zero backend files changed (frontend-only phase per fleet constraint)
  </acceptance_criteria>
  <done>PrettyView.tsx compiles clean; all mount gates flip to phase-derived; two watchdogs deleted; three retired state hooks (showOverlay/holdingTimeoutError/isBooting) removed; transient "Connecting…"/"Connection lost" text nodes removed; PrettyViewErrorOverlay mounted at phase === "error"; ComposeBox props rewired; session-recycling-store publisher rewired to phase === "holding".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a client-side state-machine refactor of `PrettyView.tsx`. No new endpoints, no new WS message types, no auth changes, no user-input surfaces added. The retry-button on the error overlay (mounted here, defined in plan 29-02) is a user-gesture-triggered fresh WS reconnect — same UX shape as DormancyOverlay's Wake button which passed prior fleet security review.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04-01 | Denial of service | Retry button firing rapid retries | mitigate | handleRetry calls setRetryKey(k=>k+1) which schedules a fresh WS via the existing patch #148 retry ladder — the ladder's linear-with-cap backoff (delay = min(2000 * (attempts+1), 8000)) throttles any user hammering the button; no new rate-limit needed beyond what patch #148 established. |
| T-29-04-02 | Tampering | Retired-state ghost references | mitigate | Grep-gate acceptance criteria enforce zero references to showOverlay/holdingTimeoutError/isBooting/setIsBooting/etc. — a future rebase that reintroduces one of these names fails the acceptance grep. |
| T-29-04-03 | Repudiation | Deleted watchdogs | accept | Backend HOLDING_TIMEOUT_TICKS=200 (10min) is the authoritative "give up" signal; deleting the frontend lockstep watchdog does not remove the terminal state — it just moves the source of truth back to the backend where it belongs (D-04, SPEC req 5). |
| T-29-04-04 | Information disclosure | Error overlay | accept | Copy is "Connection failed — retry" — static string, no sensitive data. errorMessage local state is no longer displayed (the transient "Connection lost" text is retired), but is retained internally for debugging/console-diag continuity. |
| T-29-04-05 | Elevation of privilege | phase-derived compose props | accept | ComposeBox already gates its WS-side-effecting controls via the existing `recycleActive`/`reconnectingActive`/`dormantActive` props (SPEC boundary); this plan only changes the derivation source (from local state to `phase`), not the props' semantics. No new privilege surface. |

**Attack-surface delta:** none. Existing WS/frame protocol untouched (SPEC out-of-scope + D-13). Input validation surfaces unchanged. iOS Safari backdrop-filter hardening preserved (PrettyViewLoadingOverlay kept intact per D-01; new PrettyViewErrorOverlay carries the same tokens per plan 29-02). Motion channel guardrail preserved: resolving spinner keeps animate-spin (work), all terminal-state overlays keep static glyphs (state).
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run` — full suite. NOTE: existing `PrettyView.test.tsx` assertions that reference retired local state names (isBooting, showOverlay, status === "connecting" text, status === "error" errorMessage text) MAY fail here — this is expected and is the tracked responsibility of plan 29-05. Every failing test MUST be a test whose assertion is invalidated by the intentional state-hook retirement, not a real regression. Document the specific failing test names + assertion messages in this plan's SUMMARY so plan 29-05 can address each one.
- `git diff --stat src/backend/` reports 0 files (frontend-only phase — backend build unaffected per fleet constraint)
- All source-level grep gates in the acceptance criteria pass
</verification>

<success_criteria>
- PrettyView.tsx consumes usePaneResolvingMachine and derives every overlay mount gate from `phase`
- The 600000ms holdingTimeoutError watchdog is deleted (SPEC req 5)
- The 10s PrettyViewLoadingOverlay auto-dismiss is deleted (SPEC req 5)
- Retired local states (showOverlay, holdingTimeoutError, isBooting + ref + mirror) fully removed from the file
- Transient "Connecting…" and "Connection lost" text nodes removed from JSX (SPEC boundary)
- PrettyViewErrorOverlay mounted at `{phase === "error" && ...}` with a working retry callback
- session-recycling-store publishes on `phase === "holding"` with dependency array `[phase, hostId, tmuxSession]` (SPEC req 7 contract preserved)
- ComposeBox recycleActive/reconnectingActive/dormantActive props derive from `phase`
- tsc clean; full-suite failures documented for plan 29-05 to address
</success_criteria>

<output>
Create `.planning/phases/29-unified-session-entry-state-machine-single-resolving-spinner/29-04-SUMMARY.md` when done. MUST document:
- List of every deleted useState / useEffect / setter / ref (for plan 29-05 test audit)
- List of every renamed / rewired prop on ComposeBox (for plan 29-05 test audit)
- List of every failing test in the existing suite (from `npx vitest run`), grouped by test file, with the specific assertion messages — this is the input for plan 29-05
- Exact wsState + backendFirstFrame derivation logic shipped (for plan 29-05 to reference in integration tests)
- Any Claude's Discretion decisions made during implementation (e.g. did handleRetry additionally clear errorMessage? did the outer ComposeBox mount-gate rewrite land as originally specified in step 18 or a slightly different shape?)
</output>
