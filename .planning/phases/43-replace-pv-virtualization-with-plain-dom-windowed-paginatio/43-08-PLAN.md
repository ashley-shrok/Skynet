---
phase: 43-replace-pv-virtualization-with-plain-dom-windowed-paginatio
plan: 08
type: execute
wave: 4
depends_on: ['43-07b']
files_modified:
  - src/ui/features/pretty-view/PrettyView.virtualization.test.tsx
  - src/ui/features/pretty-view/PrettyView.estimateSize.test.tsx
  - package.json
  - package-lock.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "The two virt-specific test files no longer exist on disk"
    - "@tanstack/react-virtual is removed from package.json dependencies AND devDependencies (three separate checks: jq-parse of package.json, grep-count of package-lock.json, grep-r of src/)"
    - "grep -r '@tanstack/react-virtual' src/ returns zero hits (no residual imports)"
    - "grep -c '@tanstack/react-virtual' package-lock.json returns 0"
    - "nginx configs (docker/nginx.conf and docker/nginx-https.conf) are UNCHANGED across the phase — fetch_older is a WS message extension on the existing /claude-session/websocket/ route, NOT a new HTTP route; CLAUDE.md nginx caveat does not apply"
    - "npx vitest run — full suite green"
    - "npm run build:backend — exit 0"
    - "npm run build — exit 0"
  artifacts:
    - path: "package.json"
      provides: "Slim dependency list without @tanstack/react-virtual"
    - path: "package-lock.json"
      provides: "Regenerated lock reflecting the removed dep"
  key_links:
    - from: "package.json"
      to: "installed node_modules"
      via: "npm uninstall @tanstack/react-virtual"
      pattern: "@tanstack/react-virtual"
---

<objective>
Complete the deletion pass: remove the two virt-specific test files, uninstall `@tanstack/react-virtual`, verify no residual references via THREE separate executable criteria (jq of package.json + grep of package-lock.json + grep -r of src/), verify nginx configs are byte-unchanged (executable `git diff` check — not prose), and confirm the full test + build pipelines are green end-to-end.

Purpose: Plan 43-07a/b landed the replacement but left the two virt-only tests failing (expected — they exercise virtualizer internals that no longer exist). This cleanup plan finalizes the phase: the codebase becomes byte-for-byte free of TanStack Virtual, nginx is provably unchanged, and Phase 43's success criteria (bounded DOM cost, no virt-jitter, ~5KB bundle shrink) become observable in production.

Output: Two deleted files, two modified config files (package.json + lock), plus a verification pass confirming (a) zero residual TanStack Virtual references via three independent checks, (b) nginx configs unchanged via executable git diff, (c) full monorepo test suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-CONTEXT.md
@.planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-PATTERNS.md
@CLAUDE.md

# Deletion targets
@src/ui/features/pretty-view/PrettyView.virtualization.test.tsx
@src/ui/features/pretty-view/PrettyView.estimateSize.test.tsx

# Package files
@package.json

# Nginx configs — MUST be unchanged (executable byte-verify)
@docker/nginx.conf
@docker/nginx-https.conf
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete virt-specific test files</name>
  <read_first>
    - src/ui/features/pretty-view/PrettyView.virtualization.test.tsx (confirm this file exists and is expected-to-be-failing per plan 43-07a/b's completion)
    - src/ui/features/pretty-view/PrettyView.estimateSize.test.tsx (same)
    - .planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-CONTEXT.md `<decisions>` § "Deletion scope" (both files enumerated by name)
  </read_first>
  <files>src/ui/features/pretty-view/PrettyView.virtualization.test.tsx, src/ui/features/pretty-view/PrettyView.estimateSize.test.tsx</files>
  <action>
    Use `git rm` to delete both files:
      git rm src/ui/features/pretty-view/PrettyView.virtualization.test.tsx
      git rm src/ui/features/pretty-view/PrettyView.estimateSize.test.tsx
    Verify both are gone: `ls src/ui/features/pretty-view/PrettyView.virtualization.test.tsx src/ui/features/pretty-view/PrettyView.estimateSize.test.tsx` returns "No such file or directory" for both. Verify the pretty-view test suite is now fully green: `npx vitest run src/ui/features/pretty-view/` — exit 0. The infrastructure (ResizeObserver polyfill, offsetHeight override, WS-stub factory) was already lifted into `PrettyView.plain-dom.test.tsx` and `PrettyView.windowed-pagination.test.tsx` and `PrettyView.aside.test.tsx` in Wave 3 — no other tests depend on the deleted files.
  </action>
  <verify>
    <automated>test ! -f src/ui/features/pretty-view/PrettyView.virtualization.test.tsx</automated>
    <automated>test ! -f src/ui/features/pretty-view/PrettyView.estimateSize.test.tsx</automated>
    <automated>npx vitest run src/ui/features/pretty-view/</automated>
  </verify>
  <acceptance_criteria>
    - Both files are absent from disk (test -f returns non-zero for each).
    - `git status` shows both as staged deletions.
    - `npx vitest run src/ui/features/pretty-view/` exits 0 (full pretty-view suite green — plan 43-07a/b's residual failures now resolved by removal).
  </acceptance_criteria>
  <done>
    Two virt-specific test files deleted; pretty-view suite fully green. Commit as `chore(43-08): delete virt-specific test files`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Uninstall @tanstack/react-virtual + verify zero residual references (three independent checks) + verify nginx unchanged</name>
  <read_first>
    - package.json (locate the @tanstack/react-virtual entry — likely in dependencies at version 3.14.9 per 43-CONTEXT.md)
    - docker/nginx.conf, docker/nginx-https.conf (should be byte-unchanged across the phase — no new HTTP routes were added)
    - .planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-CONTEXT.md `<decisions>` § "Deletion scope" (explicit dep-removal directive)
    - CLAUDE.md § "Nginx caveat" — nginx location blocks are required for NEW backend HTTP routes; fetch_older is a WS message-type extension on the existing /claude-session/websocket/ route, so the caveat does NOT apply, and the executable check below proves it
  </read_first>
  <files>package.json, package-lock.json</files>
  <action>
    Step 1 — uninstall the dep: `npm uninstall @tanstack/react-virtual` from the repo root. This should update both package.json and package-lock.json atomically.

    Step 2 — run THREE independent residual-reference checks (each in the acceptance criteria as a separate executable gate; do NOT combine):

    Check A (package.json dependency entry absent — jq-based, unambiguous):
      jq -r '.dependencies["@tanstack/react-virtual"] // .devDependencies["@tanstack/react-virtual"] // "null"' package.json
    Expected output: `null` (both dependencies and devDependencies do NOT contain the key).

    Check B (package-lock.json has no residual reference):
      grep -c '@tanstack/react-virtual' package-lock.json
    Expected: 0.

    Check C (no source imports remain):
      grep -r --include='*.ts' --include='*.tsx' '@tanstack/react-virtual' src/
    Expected: no matches (grep exits nonzero when zero matches; the acceptance criterion asserts exit code 1 OR grep returns empty stdout).

    Step 3 — nginx-unchanged executable check (replacing prose-only claim):
      test -z "$(git diff HEAD docker/nginx.conf docker/nginx-https.conf)"
    Expected: exit 0 (both nginx files byte-identical to HEAD as of pre-phase; fetch_older is a WS message extension on the existing /claude-session/websocket/ route, so no location block additions were needed).

    Step 4 — if any check fails: STOP. Do NOT proceed to build/test. Report the specific check that failed:
    - Check A fail → package.json still lists the dep → npm uninstall didn't take → re-run or investigate npm state.
    - Check B fail → package-lock.json still references → re-run `npm install` after uninstall to regenerate lock.
    - Check C fail → surface the specific file:line grep hit → this means plan 43-07a Region A missed an import → revert the uninstall and file as a plan 43-07a gap-fix (do NOT attempt to fix in this plan).
    - Step 3 fail → surface which nginx file was modified and by which prior commit → investigate; nginx changes were explicitly out-of-scope for the phase and would indicate accidental contamination from other work.

    Step 5 — if all four checks pass, run the full test suite: `npx vitest run` — every test file green. Run the full build: `npm run build && npm run build:backend` — both exit 0. Verify the bundle-size reduction is visible: `du -sh node_modules/@tanstack/react-virtual 2>/dev/null || echo REMOVED` should print REMOVED.
  </action>
  <verify>
    <automated>test "$(jq -r '.dependencies["@tanstack/react-virtual"] // .devDependencies["@tanstack/react-virtual"] // "null"' package.json)" = "null"</automated>
    <automated>test "$(grep -c '@tanstack/react-virtual' package-lock.json)" = "0"</automated>
    <automated>! grep -r --include='*.ts' --include='*.tsx' '@tanstack/react-virtual' src/</automated>
    <automated>test -z "$(git diff HEAD docker/nginx.conf docker/nginx-https.conf)"</automated>
    <automated>test ! -d node_modules/@tanstack/react-virtual</automated>
    <automated>npx vitest run 2>&1 | tail -5</automated>
    <automated>npm run build:backend</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Check A: `jq -r '.dependencies["@tanstack/react-virtual"] // .devDependencies["@tanstack/react-virtual"] // "null"' package.json` returns EXACTLY the string `null` (the dep is absent from BOTH dependencies and devDependencies).
    - Check B: `grep -c '@tanstack/react-virtual' package-lock.json` returns EXACTLY `0`.
    - Check C: `grep -r --include='*.ts' --include='*.tsx' '@tanstack/react-virtual' src/` returns zero matches (grep exits with code 1 = "no matches" → the `!` prefix in the verify block turns this into exit 0 for the gate).
    - nginx unchanged: `test -z "$(git diff HEAD docker/nginx.conf docker/nginx-https.conf)"` exits 0 (both nginx files byte-identical to HEAD; the fetch_older feature is a WS message extension on the existing /claude-session/websocket/ route, not a new HTTP route — CLAUDE.md nginx caveat does not apply and is provable via git diff).
    - `node_modules/@tanstack/react-virtual` directory is absent.
    - `npx vitest run` (full monorepo suite) exits 0.
    - `npm run build:backend` exits 0.
    - `npm run build` exits 0.
    - Optional (documentation only): if the build output prints a bundle size, note the reduction in the SUMMARY.md; the 43-CONTEXT.md predicted ~5KB.
  </acceptance_criteria>
  <done>
    TanStack Virtual dependency fully evicted (proven by three independent executable checks); nginx configs byte-unchanged (proven by git diff); full test + build pipeline green. Commit as `chore(43-08): remove @tanstack/react-virtual dependency`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human UAT checkpoint — Phase 43 acceptance walk</name>
  <what-built>
    Phase 43 is code-complete after Task 2. This checkpoint gates on Ashley's own walk of the acceptance criteria in the running application (either local dev or a deploy under the mandatory 15-min deadman timer per CLAUDE.md). The Nyquist rule: no phase closes until a human verifies the user-observable outcome.
  </what-built>
  <how-to-verify>
    In a browser tab connected to a Skynet instance running the Phase 43 build:

    1. Open a PrettyView on a Claude Code session that has an existing conversation with at least 60 messages (if none exists, seed one by running a chatty prompt loop in a tmux pane, then click into that pane's pretty view).

    2. On initial connect, count the visible messages in DevTools: `document.querySelectorAll('[data-pv-bubble]').length` should return exactly 50 (INITIAL_WINDOW) — no more, no less. Confirm the view is scrolled to the bottom.

    3. Scroll to the top of the loaded messages. Within ~250ms of scrolling to within 500px of top, expect the "loading older messages…" hint to appear IF the round-trip is slow enough. Then expect a batch of ~50 older messages to prepend and the loading hint to disappear. Confirm the visible content DID NOT jump — your read position stays put across the prepend (this is `overflow-anchor:auto` working).

    4. Keep scrolling up until you reach the file's first message. Confirm no further fetches fire (reachedBeginning). Confirm no console errors.

    5. Scroll back to the bottom. In DevTools, fire a synthetic long-running conversation to push the working set past 150 (paste a long prompt, then interact enough to accumulate 100+ new turns — OR test via a tmux pane where an agent is actively working and generating lots of tool-call turns; message count grows over minutes). At some point after messages.length exceeds 150 (WORKING_SET_CAP), verify: (a) the DOM count `document.querySelectorAll('[data-pv-bubble]').length` never exceeds 150, (b) new messages continue to appear at the bottom, (c) if you were pinned to bottom, you stayed pinned.

    6. Scroll UP past the (now-dropped) oldest end of the current window. Expect fetch_older to fire; expect the previously-dropped messages to prepend back in.

    7. Scroll up 5 screens rapidly. Confirm no snap-back-to-bottom, no jump-to-different-area, no virt-jitter of any kind.

    8. Open a fresh browser tab, connect to a plain-shell pane (no Claude), confirm the "no active Claude session" fallback still works (no regression).

    9. On iOS PWA (if available), repeat steps 2, 3, 7. Confirm the same behaviors hold on Safari — overflow-anchor:auto was added in Safari 17 (per 43-CONTEXT.md `<canonical_refs>` § "CSS + browser-behavior reference"); Ashley's iPhone is on modern iOS.

    10. Confirm the observation channel is intact: context-% still updates, plan-pending bubble still appears when Claude uses TodoWrite, backgroundedAgents/Shells panels still populate. None of these are windowed — they read every line from the whole file.

    Success = every step behaves as described with zero jitter, zero yank-back, zero missing observations.
  </how-to-verify>
  <resume-signal>Type "approved" if all 10 steps pass. Type "revise:<issue>" if any step fails — the orchestrator will route the failure back to plan 43-07a/b (surgery gap) or plan 43-04 (backend gap) as appropriate.</resume-signal>
</task>

</tasks>

<verification>
- `test ! -f src/ui/features/pretty-view/PrettyView.virtualization.test.tsx && test ! -f src/ui/features/pretty-view/PrettyView.estimateSize.test.tsx` — exit 0.
- Check A (jq): `test "$(jq -r '.dependencies["@tanstack/react-virtual"] // .devDependencies["@tanstack/react-virtual"] // "null"' package.json)" = "null"` — exit 0.
- Check B (lockfile grep): `test "$(grep -c '@tanstack/react-virtual' package-lock.json)" = "0"` — exit 0.
- Check C (src grep): `! grep -r --include='*.ts' --include='*.tsx' '@tanstack/react-virtual' src/` — exit 0.
- Nginx unchanged: `test -z "$(git diff HEAD docker/nginx.conf docker/nginx-https.conf)"` — exit 0.
- `test ! -d node_modules/@tanstack/react-virtual` — exit 0.
- `npx vitest run` — exit 0 (full monorepo suite).
- `npm run build && npm run build:backend` — both exit 0.
- Human UAT approval on the 10-step walk.
</verification>

<success_criteria>
- Two virt-specific test files gone.
- @tanstack/react-virtual gone from package.json + lockfile + node_modules — proven by THREE independent executable checks (jq, grep -c, grep -r).
- nginx configs byte-unchanged — proven by executable git diff check, not prose claim.
- Full pipeline (vitest + backend build + frontend build) green.
- Human confirms the Phase 43 user-observable outcome (bounded DOM, jitter-free scroll, working windowing) on both desktop and iOS PWA.
</success_criteria>

<output>
Create `.planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-08-SUMMARY.md` when done. Note in the summary: `nginx.conf` and `nginx-https.conf` were verified byte-unchanged via `test -z "$(git diff HEAD docker/nginx.conf docker/nginx-https.conf)"` (exit 0) — the fetch_older feature extends the existing `/claude-session/websocket/` WS message-type dispatcher rather than adding a new URL, so CLAUDE.md's nginx caveat did not apply and the executable check confirms zero drift.
</output>
</content>
</invoke>