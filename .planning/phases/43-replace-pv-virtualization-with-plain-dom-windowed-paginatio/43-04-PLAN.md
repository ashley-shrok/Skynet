---
phase: 43-replace-pv-virtualization-with-plain-dom-windowed-paginatio
plan: 04
type: execute
wave: 2
depends_on: ['43-01', '43-02', '43-03']
files_modified:
  - src/backend/claude-session/claude-session-server.ts
  - src/backend/claude-session/claude-session-server.fetch-older.test.ts
  - src/backend/claude-session/claude-session-server.history-window.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "handleFetchOlder is an exported module-scope function (not inline in the switch) with a companion __handleFetchOlderForTests seam mirroring __handleIdentityCountBountiesForTests"
    - "The WS onmessage switch has a `case 'fetch_older':` branch that (a) validates sshConn+currentSessionFile presence, (b) delegates to handleFetchOlder, (c) on missing precondition sends a `{ type: 'fetch_older_batch', frames: [], error: '...' }` response frame instead of hanging the client"
    - "handleFetchOlder validates msg.anchorEventId (non-empty string) + msg.count (positive int, capped), calls resolveEventIdToLine to look up the anchor line, then calls readSessionFileRange for the [max(1, anchorLine-count), anchorLine-1] slice, then emits `{ type: 'fetch_older_batch', frames: [...], reachedBeginning?: bool, error?: '...' }` — same shape as declared in claude-session-api.ts by plan 43-03"
    - "The WS handshake reads `historyWindow` from the connect URL query string (mirroring the JWT query-param fallback at L1618-1622) and threads it into both tailSessionFile() call sites (~L2826 and ~L5210)"
    - "Missing/invalid historyWindow -> tailSessionFile is called with NO initialLines override -> byte-equivalent to the pre-Phase-43 unbounded behavior"
    - "The observation channel (onLine fan-out at ~L2014, parseSessionLine emission switch at L2394-2472, layer1-detect, context-pct-from-jsonl, plan-pending-parser, backgroundedAgents/Shells, dormant-poll, id-reset detector) is BYTE-FOR-BYTE UNCHANGED — proven via delimiter-anchored diff, not a line-number-based git-diff-range check"
    - "When historyWindow is set (bounded initial replay), the observation fan-out still receives EVERY line from the whole-file tail — only the emission channel to the client is windowed"
  artifacts:
    - path: "src/backend/claude-session/claude-session-server.ts"
      provides: "historyWindow handshake plumb + fetch_older WS handler + observation channel preserved verbatim (delimiter-anchor-proven)"
      contains: "handleFetchOlder"
    - path: "src/backend/claude-session/claude-session-server.fetch-older.test.ts"
      provides: "Direct __handleFetchOlderForTests coverage: happy path, invalid args, no-session precondition, anchor-not-found, reachedBeginning flag, exec-error"
    - path: "src/backend/claude-session/claude-session-server.history-window.test.ts"
      provides: "Coverage for URL-query parse + threading into tailSessionFile + observation fan-out unaffected by historyWindow bound"
  key_links:
    - from: "src/backend/claude-session/claude-session-server.ts fetch_older switch branch"
      to: "src/backend/claude-session/claude-session-server.ts handleFetchOlder"
      via: "direct call"
      pattern: "handleFetchOlder\\("
    - from: "src/backend/claude-session/claude-session-server.ts URL query parse (post-JWT)"
      to: "tailSessionFile() calls at ~L2826 and ~L5210"
      via: "historyWindow variable threaded as 5th arg"
      pattern: "tailSessionFile\\("
    - from: "handleFetchOlder"
      to: "src/backend/claude-session/session-file-range.ts resolveEventIdToLine + readSessionFileRange"
      via: "import + sequential call (resolve first, range read second)"
      pattern: "resolveEventIdToLine|readSessionFileRange"
---

<objective>
Land the backend Wave 2 work: (a) add the `handleFetchOlder` extracted handler + its `__handleFetchOlderForTests` seam, wired into the WS message switch; the handler resolves anchorEventId → anchorLine via `resolveEventIdToLine`, computes the line range, calls `readSessionFileRange`, and emits the response frame; (b) parse `historyWindow` from the WS handshake URL query string and thread it into both `tailSessionFile()` call sites; (c) leave the observation channel byte-for-byte unchanged and PROVE it via delimiter-anchored diff (not a fragile line-number-based git diff range). Two dedicated test files cover the two new surfaces plus one targeted test locking observation fan-out under bounded emission.

Purpose: This plan brings the backend to "the wire contract described in 43-03 is fully served." After this ships, a bench client could connect with `?historyWindow=50`, receive a bounded initial replay, send a `fetch_older` payload containing only `anchorEventId + count`, and receive a `fetch_older_batch` response. No frontend consumer wired yet (plan 43-07a/b).

Output: Modifications inside `claude-session-server.ts` are surgical (three localized regions), plus two new test files. No changes to `session-file-tail.ts` (43-01), `session-file-range.ts` (43-02), or `claude-session-api.ts` (43-03) — those were the dependencies of this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-CONTEXT.md
@.planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-PATTERNS.md
@CLAUDE.md

# The parameterized helper from 43-01 (threads historyWindow through)
@src/backend/claude-session/session-file-tail.ts

# The two range helpers from 43-02 (called by handleFetchOlder)
@src/backend/claude-session/session-file-range.ts

# The wire types from 43-03 (payload + event shape; NO anchorLine field)
@src/ui/api/claude-session-api.ts

# The file being surgically modified
@src/backend/claude-session/claude-session-server.ts

# Extracted-handler + test-seam analog (SAME shape as handleFetchOlder needs)
@src/backend/claude-session/claude-session-server.role-file.test.ts

# Apply-shape alternative test seam (compose-send)
@src/backend/claude-session/claude-session-server.compose-send.test.ts

# Wire-count analog (count-bounties)
@src/backend/claude-session/claude-session-server.count-bounties.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write failing tests for __handleFetchOlderForTests + historyWindow handshake parsing + observation fan-out under historyWindow</name>
  <read_first>
    - src/backend/claude-session/claude-session-server.role-file.test.ts (analog for extracted-handler test — how ws is stubbed, how msg is passed, how the emitted response frame is captured and asserted)
    - src/backend/claude-session/claude-session-server.count-bounties.test.ts L28-69 (analog for ssh-mock + ws-stub setup; L47 shows the `__handleIdentityCountBountiesForTests` import shape to mirror)
    - src/backend/claude-session/claude-session-server.compose-send.test.ts L42-100 (apply-shape seam alternative — if handleFetchOlder is a pure function of small inputs, this shape may be cleaner)
    - src/backend/claude-session/claude-session-server.ts L717-793 (handleIdentityGetRoleFile — the shape handleFetchOlder must mirror)
    - src/backend/claude-session/claude-session-server.ts L1618-1622 (JWT URL-query fallback — the analog for parsing historyWindow off the URL)
    - src/backend/claude-session/claude-session-server.ts L2014-L2400 (onLine handler + observation fan-out — this region must be UNCHANGED; the observation fan-out test asserts every line reaches parseSessionLine regardless of historyWindow)
    - src/backend/claude-session/session-file-range.ts (exports both resolveEventIdToLine + readSessionFileRange — the handler calls both)
    - src/ui/api/claude-session-api.ts (Wave 1's FetchOlderPayload EXACTLY = { type, anchorEventId, count } — no anchorLine — and FetchOlderBatchEvent — the payload/event shapes to assert on)
    - .planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-PATTERNS.md § "2. claude-session-server.ts" (both the historyWindow handshake pattern AND the extracted-handler pattern)
  </read_first>
  <files>src/backend/claude-session/claude-session-server.fetch-older.test.ts, src/backend/claude-session/claude-session-server.history-window.test.ts</files>
  <behavior>
    fetch-older test file:
    - Test 1 (happy path — full sequence): mock `resolveEventIdToLine` to return 200 (anchor found at line 200). Mock `readSessionFileRange` to return an array of 3 fake parsed emission frames. Call `__handleFetchOlderForTests({ ws, msg: { type: "fetch_older", anchorEventId: "evt-100", count: 50 }, sshConn: fakeConn, currentSessionFile: "/tmp/sess.jsonl" })`. Assert: (a) resolveEventIdToLine was called with (fakeConn, "/tmp/sess.jsonl", "evt-100"); (b) readSessionFileRange was called with (fakeConn, "/tmp/sess.jsonl", 150, 199) — startLine = max(1, 200-50) = 150; endLine = 200-1 = 199; (c) ws.send was called with a JSON string that parses to `{ type: "fetch_older_batch", frames: [<3 frames>], reachedBeginning: false }`; no error field.
    - Test 2 (anchor-not-found → error frame): mock resolveEventIdToLine returns null; assert readSessionFileRange was NOT called; assert ws.send received `{ type: "fetch_older_batch", frames: [], error: "anchor-not-found" }`.
    - Test 3 (invalid anchorEventId rejects without exec): call with `msg.anchorEventId = ""` (or missing, or non-string); assert ws.send received `{ type: "fetch_older_batch", frames: [], error: <non-empty> }` and NEITHER resolveEventIdToLine NOR readSessionFileRange was called.
    - Test 4 (invalid count rejects): call with `msg.count = 0` (and separately with count = -1, count = 100000); each case: no resolver or range call, ws.send received error-shape.
    - Test 5 (startLine clamps at 1 → reachedBeginning=true): mock resolveEventIdToLine returns 30 (anchor near start of file); with count = 50, computed startLine = max(1, 30-50) = 1, endLine = 29. Assert: readSessionFileRange called with (fakeConn, path, 1, 29); ws.send emitted frame has `reachedBeginning: true`.
    - Test 6 (readSessionFileRange returns null → error frame): mock resolveEventIdToLine returns 200; mock readSessionFileRange returns null (range read failed). Assert ws.send received `{ type: "fetch_older_batch", frames: [], error: "read-failed" }`.
    - Test 7 (no sshConn or no currentSessionFile → error, no exec): call with sshConn=null; assert error-shape sent, no resolver called. Same with currentSessionFile=null.
    - Test 8 (handler throws → error-shape, never bubbles): mock resolveEventIdToLine to THROW synchronously; assert ws.send emits error-shape and the handler does not throw upward.

    history-window test file:
    - Test 9 (parse from URL): stub a fake `req` object with `url = "/claude-session/websocket/?token=abc&historyWindow=50"`. Feed it into whichever helper the executor extracts for the parse (or drive the connection handler directly if simpler). Assert the parsed value === 50.
    - Test 10 (missing historyWindow → undefined): url without the param yields undefined; the tailSessionFile call would receive undefined (no override).
    - Test 11 (invalid values → undefined): "abc", "0", "-5", "999999999" all parse to undefined (out of range or NaN).
    - Test 12 (integration: thread into tailSessionFile): mock `tailSessionFile` module; drive the code path that calls it at ~L2826 (or extract that region into a helper if needed); assert the initialLines arg is the parsed historyWindow value (or undefined when missing).
    - Test 13 (observation fan-out unaffected by historyWindow bound — MED-4): mock ws + mock ssh conn. Simulate a handshake with historyWindow=50. Spy on `parseSessionLine` (or on layer1-detect / context-pct-from-jsonl / plan-pending-parser — whichever downstream derivation is easiest to spy in the existing test infrastructure). Feed 100 mock JSONL lines through the tail callback (each a valid parseable frame with unique eventId). Assert: (a) all 100 lines reached parseSessionLine — the observation-side derivation spy was called 100 times, proving historyWindow ONLY bounds the initial tail read, not the per-line fan-out; (b) the observation spy call count is INDEPENDENT of historyWindow. This test lives in this file (or in `claude-session-server.history-window.test.ts` since it's about the historyWindow semantics) — executor's call.
  </behavior>
  <action>
    Create `src/backend/claude-session/claude-session-server.fetch-older.test.ts` mirroring the mock + ws-stub shape from `claude-session-server.count-bounties.test.ts`. Import `__handleFetchOlderForTests` from `./claude-session-server.js` (the seam does not exist yet — this is what makes the tests RED). Mock `./session-file-range.js` with `vi.fn()` on BOTH `resolveEventIdToLine` AND `readSessionFileRange`. Set up a ws stub with a `send: vi.fn()` that collects payloads. Write Tests 1-8 above. Each `it` block should be self-contained: fresh mocks, fresh ws stub, clear expectations on what was called with what args and what ws.send emitted. Critically: Tests 1 and 5 assert on the EXACT (startLine, endLine) tuple passed to readSessionFileRange — this locks the arithmetic.

    Create `src/backend/claude-session/claude-session-server.history-window.test.ts`. This file may need to use a smaller helper seam if the executor decides to extract the URL-parse to a named function (recommended — makes the test trivial). If the executor prefers to test the parse inline via the full connection handler, mock ws2 + ssh2 + req and drive the handshake. Either way, Tests 9-12 must be present with the exact assertions on parsed values and the tailSessionFile arg forwarding. For Test 13 (observation fan-out), use whatever test-drive shape best exercises the onLine → parseSessionLine → observation-derivation path — most likely spinning up the connection handler with a mocked `tailSessionFile` that immediately invokes the onLine callback 100 times with fixture lines, then asserting on the observation spy. Placement of Test 13 is executor's choice: either extend history-window.test.ts (natural home since it's about the historyWindow semantics) OR add to fetch-older.test.ts if the mock setup is closer there.

    Run `npx vitest run src/backend/claude-session/claude-session-server.fetch-older.test.ts src/backend/claude-session/claude-session-server.history-window.test.ts` and confirm both files FAIL (import errors on the missing exports count as red).
  </action>
  <verify>
    <automated>npx vitest run src/backend/claude-session/claude-session-server.fetch-older.test.ts src/backend/claude-session/claude-session-server.history-window.test.ts 2>&1 | grep -E "failed|FAIL|Cannot|not found" ; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - Both new test files exist.
    - `grep -c "__handleFetchOlderForTests" src/backend/claude-session/claude-session-server.fetch-older.test.ts` returns >= 1.
    - `grep -c "fetch_older_batch" src/backend/claude-session/claude-session-server.fetch-older.test.ts` returns >= 6 (one per test's assertion, minimum — 8 tests covering 6+ ws.send assertions).
    - `grep -c "resolveEventIdToLine" src/backend/claude-session/claude-session-server.fetch-older.test.ts` returns >= 4 (mock setup + call-arg + call-count + not-called assertions).
    - `grep -c "readSessionFileRange" src/backend/claude-session/claude-session-server.fetch-older.test.ts` returns >= 3.
    - `grep -c "reachedBeginning" src/backend/claude-session/claude-session-server.fetch-older.test.ts` returns >= 2 (Test 1 false-case + Test 5 true-case).
    - `grep -c "anchor-not-found" src/backend/claude-session/claude-session-server.fetch-older.test.ts` returns >= 1 (Test 2 error string).
    - `grep -c "historyWindow" src/backend/claude-session/claude-session-server.history-window.test.ts` returns >= 4.
    - Test 13 (observation fan-out) present in one of the two files — grep for a marker string like "observation fan-out" OR "parseSessionLine" call-count assertion.
    - Both files fail vitest with red output.
  </acceptance_criteria>
  <done>
    Failing tests committed. No source edits yet. Commit as `test(43-04): add failing specs for handleFetchOlder + historyWindow handshake + observation fan-out`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — add observation-channel anchor comments (pre-edit), snapshot the region, add handleFetchOlder handler + wire fetch_older switch case + parse historyWindow + thread into tailSessionFile, then byte-verify observation region unchanged</name>
  <read_first>
    - src/backend/claude-session/claude-session-server.ts (targeted region reads only; do NOT dump 5638 lines — read L715-800 for handleIdentityGetRoleFile analog, L1600-1700 for JWT/URL parse region, L1680-1720 for post-auth init cursor, L2000-2050 for onLine start region (anchor comment insertion site), L2394-2410 for parseSessionLine switch start, L2380-2410 for onLine region end (anchor comment insertion site), L2820-2840 for first tailSessionFile call, L4190-4220 for aside_arm dispatch analog, L5200-5220 for second tailSessionFile call)
    - src/backend/claude-session/session-file-range.ts (both resolveEventIdToLine + readSessionFileRange — imports for the handler)
    - src/backend/claude-session/session-file-tail.ts (confirm signature accepts the new 5th arg from plan 43-01)
    - src/ui/api/claude-session-api.ts (FetchOlderPayload = { type, anchorEventId, count } — NO anchorLine; FetchOlderBatchEvent — the type shapes the response must satisfy)
    - src/backend/claude-session/claude-session-server.fetch-older.test.ts (the RED spec from Task 1 — the exact assertions to satisfy)
    - src/backend/claude-session/claude-session-server.history-window.test.ts (RED spec from Task 1)
    - .planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-PATTERNS.md § "2. claude-session-server.ts" (extracted-handler pattern; URL query-param parse pattern; dispatch-site analog)
    - .planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-CONTEXT.md `<decisions>` § "Observation channel UNTOUCHED" (the hard rule — parseSessionLine emission switch L2394-2472 must not change; onLine fan-out at L2014 must not change; layer1-detect L2044 must not change)
  </read_first>
  <files>src/backend/claude-session/claude-session-server.ts</files>
  <action>
    Modify `src/backend/claude-session/claude-session-server.ts` in five surgical regions plus one pre-edit anchor-comment insertion. Do NOT reformat unrelated code, do NOT touch the observation channel body.

    PRE-EDIT STEP — insert observation-channel anchor comments FIRST, snapshot the region, then edit everything else:
    1. Locate the `onLine` arrow function definition (search: `grep -n "const onLine = (line: string) => {" src/backend/claude-session/claude-session-server.ts` — expected around L2014). Immediately BEFORE that line, insert the anchor comment on its own line: `// ── PHASE-43 OBSERVATION CHANNEL START — DO NOT EDIT BODY BELOW; extend switch cases in Region B only ──`
    2. Locate the closing `};` that ends the onLine arrow (expected around L2400 — verify by scanning forward from the opening; there is exactly one balanced closing that matches). Immediately AFTER that closing `};` (on the next line), insert: `// ── PHASE-43 OBSERVATION CHANNEL END ──`
    3. Snapshot the region into `/tmp/43-04-obs-before.txt` for later byte-verify:
       `awk '/PHASE-43 OBSERVATION CHANNEL START/,/PHASE-43 OBSERVATION CHANNEL END/' src/backend/claude-session/claude-session-server.ts > /tmp/43-04-obs-before.txt`
    4. Commit ONLY the anchor comments and the snapshot capture as a separate step (or continue in the same commit but snapshot before any Region A-E edits — the invariant is that the snapshot captures the pre-edit state of the anchored region).

    Region A — add `handleFetchOlder` + `__handleFetchOlderForTests`. Place the function definition at module scope alongside `handleIdentityGetRoleFile` (near L717-793). Import `resolveEventIdToLine` AND `readSessionFileRange` from `./session-file-range.js`. Signature: `export async function handleFetchOlder({ ws, msg, sshConn, currentSessionFile }: { ws: WebSocket; msg: unknown; sshConn: Client | null; currentSessionFile: string | null }): Promise<void>`. Body (implement in this exact order to match the RED spec's assertion ordering):
      (1) Coerce msg to `{ anchorEventId?: unknown; count?: unknown }`.
      (2) Validate anchorEventId is a non-empty string; validate count is a positive integer <= 500 (the cap). On either failure: `ws.send(JSON.stringify({ type: "fetch_older_batch", frames: [], error: "invalid-args" }))` and return.
      (3) If sshConn === null OR currentSessionFile === null: `ws.send(JSON.stringify({ type: "fetch_older_batch", frames: [], error: "no-session" }))` and return.
      (4) Wrap steps 5-8 in try/catch. On throw: `ws.send(JSON.stringify({ type: "fetch_older_batch", frames: [], error: "handler-threw" }))` and return.
      (5) Call `const anchorLine = await resolveEventIdToLine(sshConn, currentSessionFile, anchorEventId);`. If anchorLine === null: `ws.send(JSON.stringify({ type: "fetch_older_batch", frames: [], error: "anchor-not-found" }))` and return.
      (6) Compute `const startLine = Math.max(1, anchorLine - count);` and `const endLine = anchorLine - 1;`. If endLine < startLine (edge case: anchor is at line 1 itself, count=anything): treat as reachedBeginning with empty frames — `ws.send(JSON.stringify({ type: "fetch_older_batch", frames: [], reachedBeginning: true }))` and return.
      (7) Call `const frames = await readSessionFileRange(sshConn, currentSessionFile, startLine, endLine);`. If frames === null: `ws.send(JSON.stringify({ type: "fetch_older_batch", frames: [], error: "read-failed" }))` and return.
      (8) Build the response: `const reachedBeginning = startLine === 1;` `ws.send(JSON.stringify({ type: "fetch_older_batch", frames, reachedBeginning }))`.

    Immediately after the function, export the seam: `export const __handleFetchOlderForTests = handleFetchOlder;` (or an equivalent named seam per the count-bounties convention at L715).

    Region B — dispatch site. In the WS onmessage switch (find via `grep -n "msg.type === \"aside_arm\"" src/backend/claude-session/claude-session-server.ts` to locate the analog at ~L4197-4215), add a new branch `if (msg.type === "fetch_older") { await handleFetchOlder({ ws, msg, sshConn, currentSessionFile }); return; }` — place it near the aside_arm branch or wherever the surrounding branches cluster the per-connection dispatch. Verify the surrounding branches use `await` correctly and that the return prevents fall-through.

    Region C — URL query-param parse for historyWindow. Immediately after the JWT auth block at L1618-1622 (before the next major init section — the executor should read L1620-1690 to identify the precise insertion point), add:
        const urlObj2 = new URL(req.url || "", "http://localhost");
        const historyWindowRaw = urlObj2.searchParams.get("historyWindow");
        const historyWindowParsed = (() => {
          if (!historyWindowRaw) return undefined;
          const n = Number.parseInt(historyWindowRaw, 10);
          if (!Number.isFinite(n) || n <= 0 || n > 5000) return undefined;
          return n;
        })();
    Store this on the per-connection state (whatever pattern claude-session-server.ts uses — a local variable in the connection closure is sufficient because it's set once at handshake).

    Region D — thread historyWindow into tailSessionFile call at ~L2826. Find via `grep -n "tailSessionFile(" src/backend/claude-session/claude-session-server.ts`. Add `historyWindowParsed` as the 5th positional arg (per the 43-01 signature) OR as the new option field if the 43-01 executor chose an options-object shape. Do the same at the second call site (~L5210).

    Region E — extract a small `parseHistoryWindow(req)` helper if it makes Task 1's Tests 9-11 assertions cleaner. Executor's discretion: if the test file drives the full connection handler, no extraction needed; if it drives a pure function, extract and export as `__parseHistoryWindowForTests`.

    POST-EDIT BYTE-VERIFY — after all Region A-E edits complete:
    1. Extract the anchored observation region:
       `awk '/PHASE-43 OBSERVATION CHANNEL START/,/PHASE-43 OBSERVATION CHANNEL END/' src/backend/claude-session/claude-session-server.ts > /tmp/43-04-obs-after.txt`
    2. Diff against the pre-edit snapshot:
       `diff /tmp/43-04-obs-before.txt /tmp/43-04-obs-after.txt`
    3. Exit code MUST be 0 (empty diff — the entire anchored region including onLine body + parseSessionLine emission switch + all observation derivations is byte-identical to pre-edit). If the diff is non-empty, the executor has accidentally touched the observation channel — revert those changes before proceeding.

    Note on inert anchor comments: the two anchor comment lines survive as inert comments in production; they are cheap, they enable this byte-verify pattern for any future phase touching claude-session-server.ts, and they document the boundary. Do not remove them.

    CRITICAL — the following must be UNCHANGED (each is a hard-preserve per 43-CONTEXT.md and enforced by the anchor-comment byte-verify above):
    - onLine handler body at ~L2014-L2400 (except for the two new anchor comment lines wrapping it)
    - parseSessionLine emission switch at L2394-2472
    - layer1-detect branch
    - dormant-poll, context-pct-from-jsonl call sites, plan-pending-parser call sites, backgroundedAgents/Shells set-updates
    - id-reset detector
    - Any wire-frame shape for message/image/relay_outbound/relay_inbound/malformed

    Run all three test files: `npx vitest run src/backend/claude-session/claude-session-server.fetch-older.test.ts src/backend/claude-session/claude-session-server.history-window.test.ts` — every test from Task 1 must PASS. Then run the full backend test suite: `npx vitest run src/backend/` — must be green (regression protection for the observation-channel preserve).
  </action>
  <verify>
    <automated>npx vitest run src/backend/claude-session/claude-session-server.fetch-older.test.ts src/backend/claude-session/claude-session-server.history-window.test.ts</automated>
    <automated>npx vitest run src/backend/claude-session/</automated>
    <automated>npm run build:backend</automated>
    <automated>grep -c "handleFetchOlder" src/backend/claude-session/claude-session-server.ts</automated>
    <automated>grep -c "readSessionFileRange" src/backend/claude-session/claude-session-server.ts</automated>
    <automated>grep -c "resolveEventIdToLine" src/backend/claude-session/claude-session-server.ts</automated>
    <automated>grep -c "PHASE-43 OBSERVATION CHANNEL START" src/backend/claude-session/claude-session-server.ts</automated>
    <automated>grep -c "PHASE-43 OBSERVATION CHANNEL END" src/backend/claude-session/claude-session-server.ts</automated>
    <automated>awk '/PHASE-43 OBSERVATION CHANNEL START/,/PHASE-43 OBSERVATION CHANNEL END/' src/backend/claude-session/claude-session-server.ts > /tmp/43-04-obs-after.txt && diff /tmp/43-04-obs-before.txt /tmp/43-04-obs-after.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "handleFetchOlder" src/backend/claude-session/claude-session-server.ts` returns >= 3 (definition + seam export + dispatch call).
    - `grep -c "__handleFetchOlderForTests" src/backend/claude-session/claude-session-server.ts` returns >= 1.
    - `grep -c "readSessionFileRange" src/backend/claude-session/claude-session-server.ts` returns >= 2 (import + call).
    - `grep -c "resolveEventIdToLine" src/backend/claude-session/claude-session-server.ts` returns >= 2 (import + call).
    - `grep -c "\"fetch_older\"" src/backend/claude-session/claude-session-server.ts` returns >= 2 (case guard + response type literal, at minimum).
    - `grep -c "historyWindow" src/backend/claude-session/claude-session-server.ts` returns >= 3 (query parse + thread into 2 tailSessionFile calls, or via a helper).
    - `grep -c "PHASE-43 OBSERVATION CHANNEL START" src/backend/claude-session/claude-session-server.ts` returns EXACTLY 1 (anchor comment placed).
    - `grep -c "PHASE-43 OBSERVATION CHANNEL END" src/backend/claude-session/claude-session-server.ts` returns EXACTLY 1 (anchor comment placed).
    - `awk '/PHASE-43 OBSERVATION CHANNEL START/,/PHASE-43 OBSERVATION CHANNEL END/' src/backend/claude-session/claude-session-server.ts > /tmp/43-04-obs-after.txt && diff /tmp/43-04-obs-before.txt /tmp/43-04-obs-after.txt` returns exit code 0 (observation region byte-unchanged — this is the executable byte-verify replacing the prior line-number-based diff).
    - Every existing backend test in `src/backend/` PASSES (regression protection).
    - Both new test files PASS.
    - `npm run build:backend` exits 0.
  </acceptance_criteria>
  <done>
    Backend WS gains historyWindow handshake + fetch_older handler (using resolveEventIdToLine + readSessionFileRange in sequence); observation channel preserved byte-for-byte (proven via delimiter-anchored diff); two new test files green; full backend suite green; backend build clean. Commit as `feat(43-04): historyWindow handshake + fetch_older WS handler with eventId→line resolution`.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/backend/claude-session/claude-session-server.fetch-older.test.ts src/backend/claude-session/claude-session-server.history-window.test.ts` — exit 0.
- `npx vitest run src/backend/` — exit 0 (no regression in observation-channel tests: layer1, dormant-poll, plan-pending-parser, count-bounties, compose-send, etc.).
- `npm run build:backend` — exit 0.
- Observation-channel invariant: `awk '/PHASE-43 OBSERVATION CHANNEL START/,/PHASE-43 OBSERVATION CHANNEL END/' src/backend/claude-session/claude-session-server.ts | diff - /tmp/43-04-obs-before.txt` returns exit 0 (executable byte-verify, delimiter-anchored, survives arbitrary line-number shifts).
- Observation fan-out under historyWindow bound: Test 13 in the history-window suite passes, proving parseSessionLine (or its observation derivations) is called for every line regardless of the emission-side historyWindow bound.
</verification>

<success_criteria>
- Backend implements the full wire contract described by plan 43-03's types (eventId-only, server does line lookup).
- Frontend (plan 43-07a/b) can immediately consume it.
- Observation-channel preservation is provable by an executable delimiter-anchored diff (not a fragile line-range check).
- Observation fan-out is empirically shown to be unaffected by historyWindow bound (Test 13).
</success_criteria>

<output>
Create `.planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-04-SUMMARY.md` when done.
</output>
</content>
</invoke>