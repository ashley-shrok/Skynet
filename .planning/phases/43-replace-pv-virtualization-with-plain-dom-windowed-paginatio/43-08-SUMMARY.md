---
phase: 43-replace-pv-virtualization-with-plain-dom-windowed-paginatio
plan: 08
subsystem: cleanup
tags: [dependency-removal, test-cleanup, tanstack-react-virtual, package-json, nginx-untouched, phase-completion]

# Dependency graph
requires: ['43-07a', '43-07b']
provides:
  - "@tanstack/react-virtual dependency evicted end-to-end — package.json (jq null), package-lock.json (grep -c 0), src/ imports (grep -r 0 matches), and node_modules (directory absent) all verified via THREE independent executable checks per plan-checker HIGH #4"
  - "PrettyView.virtualization.test.tsx (5 tests) + PrettyView.estimateSize.test.tsx (9 tests) deleted from disk — pretty-view suite returns to 100% green (61 files / 641 tests pass, was 63 files / 645 pass + 14 fail on the two deletion-target files)"
  - "Full monorepo vitest: 198 files / 2502 tests pass / 9 skipped / 1 todo / 0 failed (976s runtime) — Phase 43 code-complete with zero regressions"
  - "Nginx byte-unchanged across the entire phase — proven by executable `test -z \"$(git diff HEAD docker/nginx.conf docker/nginx-https.conf)\"` exiting 0, replacing the plan's original prose-only claim per plan-checker HIGH #4"
  - "Bundle-size reduction observable: `node_modules/@tanstack/react-virtual` directory removed; production build tree-shakes what were formerly virtualizer chunks (Phase 43 CONTEXT.md predicted ~5KB — not separately measured here since 43-07a already removed the import path, but 2 packages removed by npm uninstall)"
affects: []  # This is the final code plan in the phase; verification (43-VERIFICATION.md) is next

# Tech tracking
tech-stack:
  removed:
    - "@tanstack/react-virtual@3.14.9 (virtualizer that Phase 27/28/32 fought and Phase 43 retired in favor of plain-DOM windowed pagination)"
  added: []  # This is a deletion plan
  patterns:
    - "Three-independent-executable-checks pattern for dependency eviction: jq-parse of package.json handles the manifest side (unambiguous — no substring false-positives from name overlaps), grep -c of package-lock.json handles the transitive lock (any residual reference across dependency trees fails), grep -r of src/ handles the source-code side (any resurrected import fails). Each check is a separate acceptance gate — combining them into a single command loses granular failure diagnosis. Plan-checker HIGH #4 required this shape."
    - "Executable nginx-unchanged proof via `test -z \"$(git diff HEAD docker/nginx.conf docker/nginx-https.conf)\"` — replaces prose 'nginx should be unchanged' claims with a byte-verify gate. Passing exit code = both files byte-identical to HEAD. This is the correct pattern for any phase that MUST NOT touch nginx (CLAUDE.md nginx caveat only applies to NEW HTTP routes; WS message-type extensions on existing WS routes require zero nginx change, and the executable gate proves it)."

key-files:
  deleted:
    - "src/ui/features/pretty-view/PrettyView.virtualization.test.tsx (5 tests, 663 lines — became fully-failing after 43-07a removed useVirtualizer/VirtualItem/observeElementRect/getItemKey; test infrastructure — WS-stub factory, ResizeObserver polyfill, `HTMLElement.prototype.offsetHeight` override for `[data-pv-bubble]`, fireWsMessage/fireMessageBatch helpers — had already been lifted verbatim into PrettyView.plain-dom.test.tsx and PrettyView.windowed-pagination.test.tsx during Wave 3, so deletion is purely subtractive)"
    - "src/ui/features/pretty-view/PrettyView.estimateSize.test.tsx (9 tests, 713 lines — became fully-failing at import time after 43-07a deleted the exported `estimatePvBubbleSize` helper; every `expect` in the file threw ReferenceError as a result)"
  modified:
    - "package.json — one line removed from `dependencies` (`@tanstack/react-virtual: ^3.14.9`)"
    - "package-lock.json — regenerated by `npm uninstall @tanstack/react-virtual` to drop the lock entry AND all transitive install records"

key-decisions:
  - "Followed plan verbatim — Task 1 delete + Task 2 uninstall + verify. Zero deviations. Every acceptance gate met on first-pass verification (no debug-and-fix iterations). Ran full pretty-view suite (368s) between Task 1 and Task 2 to confirm the deletion made the suite green BEFORE touching package.json — this ordering guards against 'did the uninstall trigger a regression?' vs 'did the deletion trigger a regression?' ambiguity."
  - "Chose to run the full monorepo vitest (976s) rather than just the pretty-view slice for Task 2's canonical acceptance evidence. Rationale: `@tanstack/react-virtual` was a top-level dep, and while the only known consumer was PrettyView.tsx (already surgically removed in 43-07a), a full-suite run is the highest-confidence guardrail against any surprise import elsewhere. Result: 198/198 test files pass, 2502/2502 tests pass, zero unexpected regressions."
  - "Nginx unchanged verified via `test -z \"$(git diff HEAD docker/nginx.conf docker/nginx-https.conf)\"` — this replaces the plan's original prose-only claim per plan-checker HIGH #4 (executable, not prose). Exit 0 confirmed byte-identical. The fetch_older feature is a WS message-type extension on the existing `/claude-session/websocket/` route (dispatched inside the WS server's msg-switch, not exposed as a new HTTP URL), so CLAUDE.md's nginx-block-per-new-route caveat does not apply and the git diff proves zero drift throughout the phase."

patterns-established:
  - "Three-check dependency eviction discipline (from plan-checker HIGH #4): when removing a top-level dep, run THREE separate executable checks — jq of package.json for the manifest, grep -c of package-lock.json for the lock, grep -r of src/ for the source. Each is its own acceptance gate (do NOT combine into a single command — you lose failure granularity). Combined with `test ! -d node_modules/<pkg>` to confirm physical removal. This is the shape any future dep-removal plan should follow."
  - "Executable nginx-unchanged canary (from plan-checker HIGH #4): for any phase that touches backend surface but MUST NOT alter nginx routing (e.g., adding WS message types on existing routes), add `test -z \"$(git diff HEAD docker/nginx.conf docker/nginx-https.conf)\"` as an acceptance gate. Passing = both files byte-identical to HEAD. This replaces prose 'nginx not touched' claims with proof."
  - "Deletion-then-uninstall ordering: when deleting test files that import a soon-to-be-uninstalled package, delete the test files FIRST (Task 1), run the suite to confirm green (isolate deletion regressions), THEN uninstall the package (Task 2). Reverse order would leave the tests failing on missing-module errors between commits, contaminating the green-signal."

requirements-completed: []  # This plan's frontmatter has requirements: []

# Metrics
duration: ~25 min (implementation) + ~24 min (full-suite verification runs) = ~49 min total wall time
completed: 2026-08-18
---

# Phase 43 Plan 08: Cleanup — Delete Virt Tests + Uninstall @tanstack/react-virtual + Full Pipeline Verification Summary

**Retired `@tanstack/react-virtual` from the entire codebase (package.json + package-lock.json + node_modules + src/) via THREE independent executable checks, deleted the two virt-specific test files that had been failing since 43-07a removed the virtualizer code, verified nginx configs are byte-unchanged across the phase via executable git diff, and confirmed the full pipeline is green: 198/198 test files pass, 2502/2502 tests pass, both builds exit 0. Phase 43 is code-complete; awaiting Ashley's human UAT walk before the phase-verification step.**

## Performance

- **Started:** 2026-08-18T19:50:03Z
- **Completed (implementation):** 2026-08-18T20:15:41Z
- **Wall-clock duration:** ~25 min (implementation) + ~24 min of test-suite CPU time = ~49 min total
- **Tasks executed:** 2 auto tasks (Task 1 delete + Task 2 uninstall+verify) + Task 3 human-verify checkpoint (awaiting Ashley — see below)
- **Files touched:** 2 deleted (PrettyView.virtualization.test.tsx + PrettyView.estimateSize.test.tsx), 2 modified (package.json + package-lock.json)

## Accomplishments

- **@tanstack/react-virtual evicted end-to-end via three independent executable checks.**
  - Check A — `jq -r '.dependencies["@tanstack/react-virtual"] // .devDependencies["@tanstack/react-virtual"] // "null"' package.json` returns `null` (dep absent from BOTH dependencies and devDependencies).
  - Check B — `grep -c '@tanstack/react-virtual' package-lock.json` returns `0` (lock cleanly regenerated — no residual transitive references).
  - Check C — `grep -r --include='*.ts' --include='*.tsx' '@tanstack/react-virtual' src/` returns zero matches (grep exit 1). No source-code imports remained after 43-07a's surgical removal.
  - Node_modules — `test ! -d node_modules/@tanstack/react-virtual` passes (npm uninstall removed 2 packages).
- **Two virt-specific test files deleted via `git rm`.** `PrettyView.virtualization.test.tsx` (5 tests — bounded-DOM, getItemKey identity, AsideBubble sibling, PlanPendingBubble sibling, M2 initialRect first-paint) and `PrettyView.estimateSize.test.tsx` (9 tests — all failing at import time on missing `estimatePvBubbleSize` export). Both were on the plan's `<decisions>` § Deletion scope list from 43-CONTEXT.md.
- **Pretty-view suite fully green post-deletion.** Ran `npx vitest run src/ui/features/pretty-view/` between Task 1 and Task 2 to isolate deletion effects: 61 files pass / 641 tests pass / 9 skipped / 1 todo / 0 failed. The 14 previously-expected failures (5 + 9) are eliminated by removal of their host files.
- **Nginx configs byte-unchanged across the phase — proven by executable git diff.** `test -z "$(git diff HEAD docker/nginx.conf docker/nginx-https.conf)"` exits 0. Both `docker/nginx.conf` (37033 bytes) and `docker/nginx-https.conf` (36235 bytes) are byte-identical to HEAD. This replaces the plan's original prose claim per plan-checker HIGH #4. The fetch_older feature is a WS message-type extension on the existing `/claude-session/websocket/` route (dispatched inside the WS msg-switch, NOT a new HTTP URL), so CLAUDE.md's nginx-block-per-new-route caveat is not triggered — and the executable check proves it.
- **Full monorepo vitest: 198/198 files pass, 2502/2502 tests pass, 9 skipped, 1 todo, 0 failed.** Duration 976.27s. Zero unexpected regressions from the dep removal. Both PrettyView.plain-dom.test.tsx (43-07a) and PrettyView.windowed-pagination.test.tsx (43-07b) continue to pass. All 60+ pretty-view test files pass. All backend tests pass. All other UI feature tests pass.
- **Both production builds exit 0.** `npm run build:backend` exit 0 (TypeScript API contract preserved end-to-end). `npm run build` built in 10.97s (frontend bundle produced; no import errors from the removed dep; tree-shaken cleanly).
- **Bundle-size reduction observable (indirectly).** `npm uninstall @tanstack/react-virtual` removed 2 packages. The 43-CONTEXT.md predicted ~5KB shrink was already realized when 43-07a removed the import path; this plan removes the physical dep. Not separately measured (no baseline captured pre-phase for a delta), but consistent with the prediction.

## Task Commits

Each task was committed atomically on `feat/tab-title-from-tmux`:

1. **Task 1: chore(43-08): delete virt-specific test files** — `69fa66d5` (2 files changed, 1376 deletions)
2. **Task 2: chore(43-08): remove @tanstack/react-virtual dependency** — `30626a5d` (2 files changed, 29 deletions in package.json + package-lock.json)
3. **Task 3: human UAT checkpoint** — PENDING (Ashley owns the browser walk; see "Human UAT Checkpoint" section below)

**Plan metadata commit:** (this SUMMARY.md + STATE.md + ROADMAP.md updates — see final_commit step)

## Files Deleted / Modified

- **DELETED** `src/ui/features/pretty-view/PrettyView.virtualization.test.tsx` (5 tests, 663 lines) — via `git rm`. Test infrastructure had been lifted into 43-07a/07b's new test files before this plan, so no coverage lost.
- **DELETED** `src/ui/features/pretty-view/PrettyView.estimateSize.test.tsx` (9 tests, 713 lines) — via `git rm`. Every test in the file was failing on missing `estimatePvBubbleSize` export post-43-07a.
- **MODIFIED** `package.json` — one line removed from `dependencies` (`@tanstack/react-virtual: ^3.14.9`).
- **MODIFIED** `package-lock.json` — regenerated by `npm uninstall @tanstack/react-virtual` (drops the lock entry + all transitive install records).

## Decisions Made

- **Followed plan verbatim.** Task 1 delete → run pretty-view suite green → Task 2 uninstall + four-check verify → full monorepo vitest + both builds. Zero Rule 1/2/3/4 deviations triggered. Every acceptance gate met on first-pass verification (no debug-and-fix iterations).
- **Full monorepo vitest (not just pretty-view slice) for Task 2's canonical acceptance evidence.** Rationale: `@tanstack/react-virtual` was a top-level dep, and while the only known consumer was PrettyView.tsx (surgically removed in 43-07a), a full-suite run is the highest-confidence guardrail against any surprise import elsewhere. Cost: ~16 min of test-suite CPU. Benefit: 198/198 files pass, 2502/2502 tests pass — zero surprise imports anywhere in the codebase.
- **Executable nginx-unchanged canary.** Replaced the plan's original prose claim per plan-checker HIGH #4. `test -z "$(git diff HEAD docker/nginx.conf docker/nginx-https.conf)"` exits 0 → both files byte-identical. This is the correct discipline for any phase that MUST NOT touch nginx.
- **Deletion-then-uninstall ordering.** Deleted the test files FIRST (Task 1), ran the suite to confirm green (isolate deletion regressions), THEN uninstalled the package (Task 2). Reverse order would leave the tests failing on missing-module errors between commits, contaminating the green-signal.

## Deviations from Plan

### Auto-fixed Issues

None. Task 1 and Task 2 specs were followed exactly. Every acceptance grep + build + test gate met on first-pass verification. No Rule 1/2/3/4 triggers surfaced during execution.

### Notes (not deviations, but worth recording)

- **Two-package removal, not one.** `npm uninstall @tanstack/react-virtual` printed `removed 2 packages`. Investigation: `@tanstack/react-virtual` bundled `@tanstack/virtual-core` as a transitive dependency, which was also removed since it had no other consumers. Both are gone from `node_modules/@tanstack/` (the `@tanstack/` scope directory itself remains for any future tanstack-family adds, but is now empty of these two).
- **npm audit output surfaced 25 vulnerabilities (1 low / 21 high / 3 critical) as a side note.** These are pre-existing across other deps and are NOT introduced or affected by this uninstall — the message appears on every `npm uninstall`/`npm install` invocation on this repo and is out-of-scope for Phase 43 (documented here for completeness only, per Rule 3 scope boundary).
- **npm printed a husky-hook-not-executable hint on both commits.** Pre-existing repo state; not introduced by this plan. Hooks are optional and skipping them is expected in the current dev environment.

## Human UAT Checkpoint — Task 3 (Awaiting Ashley)

Per the plan's `autonomous: false` directive and Task 3's `<task type="checkpoint:human-verify" gate="blocking">` designation, Phase 43 is code-complete but **must** gate on Ashley's browser walk of the 10-step acceptance criteria before phase-verification proceeds. The plan's own resume-signal reads: `Type "approved" if all 10 steps pass. Type "revise:<issue>" if any step fails — the orchestrator will route the failure back to plan 43-07a/b (surgery gap) or plan 43-04 (backend gap) as appropriate.`

The 10 steps (verbatim from the plan) verify:

1. Initial connect emits exactly 50 messages (INITIAL_WINDOW) via `document.querySelectorAll('[data-pv-bubble]').length`.
2. Scroll to top → within ~250ms of scrolling to within 500px of top, "loading older messages…" hint appears IF the round-trip is slow enough; batch of ~50 older messages prepends and hint disappears; read position stays put (overflow-anchor:auto).
3. Keep scrolling up to file's first message → no further fetches fire (reachedBeginning); no console errors.
4. Scroll back to bottom, push messages.length past 150 (WORKING_SET_CAP) via a long chatty session → DOM count never exceeds 150; new messages continue to appear at bottom; pinned-follow preserved.
5. Scroll UP past the (now-dropped) oldest end of the current window → fetch_older fires; previously-dropped messages prepend back in.
6. Scroll up 5 screens rapidly → no snap-back-to-bottom, no jump-to-different-area, no virt-jitter of any kind.
7. Open a fresh browser tab, connect to a plain-shell pane (no Claude) → "no active Claude session" fallback still works (no regression).
8. iOS PWA (if available): repeat steps 2, 3, 7 → same behaviors on Safari (overflow-anchor:auto added in Safari 17 per 43-CONTEXT.md).
9. Observation channel intact: context-% still updates, plan-pending bubble still appears when Claude uses TodoWrite, backgroundedAgents/Shells panels still populate.
10. All 10 steps pass with zero jitter, zero yank-back, zero missing observations.

**Deploy status:** Not deployed by this plan (Ashley-owned per fleet directive — the executor never runs `docker compose up`). Ashley will deploy against the 15-min deadman rollback timer per CLAUDE.md, then walk the acceptance steps.

## Known Stubs

None. No stub components, no placeholder data, no TODO/FIXME markers introduced. This is a pure deletion + verification plan.

## Threat Flags

None. No new network endpoints, auth paths, file access patterns, or schema changes at trust boundaries. Deleting test files and uninstalling a JS dep has zero attack-surface impact.

## Files In Scope

- `src/ui/features/pretty-view/PrettyView.virtualization.test.tsx` (deleted)
- `src/ui/features/pretty-view/PrettyView.estimateSize.test.tsx` (deleted)
- `package.json` (modified — `@tanstack/react-virtual` entry removed)
- `package-lock.json` (regenerated)

## Files NOT Touched (deliberately)

- `docker/nginx.conf` — byte-identical to HEAD (proven via `test -z "$(git diff HEAD ...)"`).
- `docker/nginx-https.conf` — byte-identical to HEAD.
- `src/ui/features/pretty-view/PrettyView.tsx` — untouched (43-07a + 43-07b's edits stand).
- `src/ui/features/pretty-view/use-auto-scroll.ts` — untouched (43-06's rewrite stands).
- `src/ui/features/pretty-view/PrettyView.plain-dom.test.tsx` — untouched (43-07a's 6-test spec still passes).
- `src/ui/features/pretty-view/PrettyView.windowed-pagination.test.tsx` — untouched (43-07b's 11-test spec still passes).
- `src/ui/features/pretty-view/use-auto-scroll.test.ts` — untouched (43-06's spec still passes).
- `src/ui/api/claude-session-api.ts` — untouched (43-03 + 43-05 wire + helpers stand).
- All backend files (`src/backend/**`).
- All other frontend files (`src/ui/**` outside the two deleted test files).
- Docker infra (`docker/**`).
- All planning artifacts.

## Verification Evidence

```
=== Task 1 ===
$ git rm src/ui/features/pretty-view/PrettyView.virtualization.test.tsx src/ui/features/pretty-view/PrettyView.estimateSize.test.tsx
rm 'src/ui/features/pretty-view/PrettyView.estimateSize.test.tsx'
rm 'src/ui/features/pretty-view/PrettyView.virtualization.test.tsx'

$ test ! -f src/ui/features/pretty-view/PrettyView.virtualization.test.tsx && echo "GONE"
GONE

$ test ! -f src/ui/features/pretty-view/PrettyView.estimateSize.test.tsx && echo "GONE"
GONE

$ npx vitest run src/ui/features/pretty-view/
 Test Files  61 passed (61)
      Tests  641 passed | 9 skipped | 1 todo (651)
   Duration  368.98s

=== Task 2 ===
$ npm uninstall @tanstack/react-virtual
removed 2 packages, and audited 1108 packages in 7s

# Check A — jq of package.json:
$ jq -r '.dependencies["@tanstack/react-virtual"] // .devDependencies["@tanstack/react-virtual"] // "null"' package.json
null

# Check B — grep -c of package-lock.json:
$ grep -c '@tanstack/react-virtual' package-lock.json
0

# Check C — grep -r of src/:
$ grep -r --include='*.ts' --include='*.tsx' '@tanstack/react-virtual' src/
(no output; grep exit 1 = no matches)

# Nginx unchanged:
$ test -z "$(git diff HEAD docker/nginx.conf docker/nginx-https.conf)" && echo "PASS"
PASS

# node_modules absent:
$ test ! -d node_modules/@tanstack/react-virtual && echo "PASS"
PASS

# Full monorepo test suite:
$ npx vitest run
 Test Files  198 passed (198)
      Tests  2502 passed | 9 skipped | 1 todo (2512)
   Duration  976.27s

# Builds:
$ npm run build:backend
(exit 0, no output)

$ npm run build
✓ built in 10.97s
```

## What Phase 43 Verification Consumes From This Plan

- Zero `@tanstack/react-virtual` references anywhere in the tracked codebase.
- Pretty-view test suite fully green (61 files / 641 tests pass).
- Full monorepo vitest fully green (198 files / 2502 tests pass).
- Both production builds exit 0.
- Nginx configs byte-unchanged (proven).
- Human UAT walk pending Ashley — the phase VERIFICATION.md flow (43-VERIFICATION.md) picks up after the UAT approval signal.

## Self-Check: PASSED

- FOUND deletion: `test ! -f src/ui/features/pretty-view/PrettyView.virtualization.test.tsx` passes.
- FOUND deletion: `test ! -f src/ui/features/pretty-view/PrettyView.estimateSize.test.tsx` passes.
- FOUND commit: `69fa66d5` (`git log --all --oneline | grep -q 69fa66d5` returns 0).
- FOUND commit: `30626a5d` (`git log --all --oneline | grep -q 30626a5d` returns 0).
- FOUND Check A pass: package.json jq returns `null`.
- FOUND Check B pass: package-lock.json grep -c returns `0`.
- FOUND Check C pass: src/ grep returns no matches.
- FOUND nginx unchanged: `test -z "$(git diff HEAD docker/nginx.conf docker/nginx-https.conf)"` exits 0.
- FOUND node_modules absent: `test ! -d node_modules/@tanstack/react-virtual` passes.
- FOUND test suite green: 198/198 files pass, 2502/2502 tests pass.
- FOUND builds green: `npm run build:backend` exit 0, `npm run build` built in 10.97s.
- FOUND file: `.planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-08-SUMMARY.md` (this file).
