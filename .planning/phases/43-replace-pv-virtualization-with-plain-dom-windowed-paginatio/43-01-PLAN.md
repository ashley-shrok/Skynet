---
phase: 43-replace-pv-virtualization-with-plain-dom-windowed-paginatio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/claude-session/session-file-tail.ts
  - src/backend/claude-session/session-file-tail.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "tailSessionFile still starts at file line 1 when no initialLines override is supplied (backcompat preserved byte-for-byte)"
    - "When called with initialLines=N (positive int), the remote tail process is invoked with -n N (no `+`) so the initial slice is bounded to the last N lines before following"
    - "Invalid initialLines (0, negative, NaN, huge) falls back to the -n +1 default rather than passing a nonsense shell arg"
  artifacts:
    - path: "src/backend/claude-session/session-file-tail.ts"
      provides: "tailSessionFile with optional bounded-initial-lines parameter"
      contains: "initialLines"
    - path: "src/backend/claude-session/session-file-tail.test.ts"
      provides: "Unit coverage locking the command-shape backcompat + override paths"
      contains: "tail -F -n"
  key_links:
    - from: "src/backend/claude-session/session-file-tail.ts"
      to: "shellEscape + tail -F command construction"
      via: "String concatenation at the exec call site"
      pattern: "tail -F -n"
---

<objective>
Parameterize `tailSessionFile` in `src/backend/claude-session/session-file-tail.ts` so callers can opt into a bounded-initial-lines slice (`tail -F -n N`) while every existing caller that does NOT opt in continues to get the current unbounded `tail -F -n +1` behavior byte-for-byte.

Purpose: This is the atomic backend primitive Wave 2 will thread the `historyWindow` handshake value through. Landing the parameterization + its own unit test in isolation keeps the change grep-obvious for reviewers and lets the downstream WS-handler plan focus purely on wiring.

Output: The `tailSessionFile` signature grows an optional `initialLines` parameter (or a small options object — executor's call as long as it keeps the four current positional args' meanings unchanged). The shell command string branches: default -> `tail -F -n +1 <path>` (unchanged); override -> `tail -F -n <N> <path>`. A new dedicated test file locks both paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-CONTEXT.md
@.planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-PATTERNS.md
@CLAUDE.md

# Analog (self — parameterize existing helper)
@src/backend/claude-session/session-file-tail.ts

# Existing test infrastructure shape for backend claude-session tests
@src/backend/claude-session/context-pct-from-jsonl.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write session-file-tail unit test that pins backcompat + parameterized command shapes</name>
  <read_first>
    - src/backend/claude-session/session-file-tail.ts (whole file; header comment L7-22 anticipates the `-n +1` swap; exec call at L80 is the branch point)
    - src/backend/claude-session/context-pct-from-jsonl.test.ts (existing test shape in this same dir — imports, ssh2 mocking convention, describe/it layout)
    - .planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-PATTERNS.md § "1. session-file-tail.ts — parameterize initial line count" (analog + backcompat rule)
    - .planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-CONTEXT.md `<decisions>` § "Backend contract additions" (backcompat verbatim)
  </read_first>
  <files>src/backend/claude-session/session-file-tail.test.ts</files>
  <behavior>
    - Test 1 (backcompat): calling `tailSessionFile(mockConn, "/tmp/session.jsonl", onLine, onError)` with NO initialLines override invokes `mockConn.exec` with the exact string `tail -F -n +1 '/tmp/session.jsonl'`.
    - Test 2 (override happy path): calling with `initialLines: 50` invokes `mockConn.exec` with the exact string `tail -F -n 50 '/tmp/session.jsonl'` (no `+`, no other chars between `-n` and the number).
    - Test 3 (invalid override coerces to default): passing initialLines=0, initialLines=-5, initialLines=NaN, and initialLines=1e12 each fall back to `-n +1` — assert the emitted command matches the backcompat string.
    - Test 4 (path escaping preserved): pass a path with a single quote (`/tmp/sess'ion.jsonl`) and confirm the command uses the same `shellEscape` result as the unparameterized path — do not regress escaping.
  </behavior>
  <action>
    Create `src/backend/claude-session/session-file-tail.test.ts`. Mirror the vitest import + mock shape of `src/backend/claude-session/context-pct-from-jsonl.test.ts`. Mock ssh2 `Client` via a hand-rolled stub exposing an `exec(command, cb)` spy whose callback receives `(null, streamStub)` where streamStub minimally supports `on`, `stderr.on`, `end`, `close`. Do NOT drive stdout — the tests assert on the command string handed to `exec`, not on line delivery. Import `tailSessionFile` from `./session-file-tail.js`. For each of the 4 behaviors above, construct a fresh stub, call `tailSessionFile`, assert `execSpy.mock.calls[0][0]` equals the expected string. Run `npx vitest run src/backend/claude-session/session-file-tail.test.ts` and confirm each `it` block FAILS with a message about the current source not accepting `initialLines` (either TypeScript compile error against the new signature, or a runtime mismatch on the emitted command). The RED must be observable.
  </action>
  <verify>
    <automated>npx vitest run src/backend/claude-session/session-file-tail.test.ts 2>&1 | grep -E "failed|FAIL" ; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `src/backend/claude-session/session-file-tail.test.ts` exists.
    - `grep -c "tail -F -n +1" src/backend/claude-session/session-file-tail.test.ts` returns >= 2 (backcompat + invalid-override paths reference the exact literal).
    - `grep -c "tail -F -n 50" src/backend/claude-session/session-file-tail.test.ts` returns >= 1 (override happy path locks the exact spacing).
    - Vitest run shows every new `it` block FAILING before the implementation change lands.
  </acceptance_criteria>
  <done>
    Failing test file committed. No source-file changes in this task. `git log -1 --oneline` shows a `test(43-01):` commit adding only the new test file.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — extend tailSessionFile signature + command branch, keep every non-opt-in caller unchanged</name>
  <read_first>
    - src/backend/claude-session/session-file-tail.ts (whole file — the change is bounded to the exec-call construction at L80 and the exported function signature at L35-40)
    - src/backend/claude-session/session-file-tail.test.ts (the failing tests from Task 1 — the exact command strings asserted)
    - .planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-PATTERNS.md § "1. session-file-tail.ts" § "Backcompat rule"
  </read_first>
  <files>src/backend/claude-session/session-file-tail.ts</files>
  <behavior>
    - Every current call site (verify with grep) that passes 4 positional args continues to compile and behave identically.
    - The new optional 5th parameter is named `initialLines` and typed `number | undefined` (or wrapped in an options object per executor's judgment — the observable behavior is what matters).
    - Command construction branch: `Number.isFinite(initialLines) && initialLines > 0 && initialLines <= 1_000_000` picks the parameterized form; anything else falls through to `-n +1`.
  </behavior>
  <action>
    Modify `src/backend/claude-session/session-file-tail.ts`. Add the optional `initialLines?: number` parameter to `tailSessionFile`'s signature. Inside the function, immediately before the current `const command = "tail -F -n +1 " + shellEscape(absolutePath);` line at L80, insert a small validated-int helper: `const boundedN = (typeof initialLines === "number" && Number.isFinite(initialLines) && initialLines > 0 && initialLines <= 1_000_000) ? Math.floor(initialLines) : null;` Then replace the const initializer with a ternary: `const command = boundedN !== null ? ("tail -F -n " + boundedN + " " + shellEscape(absolutePath)) : ("tail -F -n +1 " + shellEscape(absolutePath));` Do NOT touch the header comment, error handling, STDERR_ACCUMULATION_LIMIT_BYTES, or the stop() helper. Run `npx vitest run src/backend/claude-session/session-file-tail.test.ts` — every test from Task 1 must now PASS. Also run `git grep -n "tailSessionFile(" src/backend/` to enumerate current callers and confirm each still uses the 4-arg positional shape (Wave 2 will thread the 5th arg through those call sites; this plan does NOT modify callers).
  </action>
  <verify>
    <automated>npx vitest run src/backend/claude-session/session-file-tail.test.ts</automated>
    <automated>npm run build:backend</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "initialLines" src/backend/claude-session/session-file-tail.ts` returns >= 2 (signature + validated-int helper).
    - `grep -c "tail -F -n +1" src/backend/claude-session/session-file-tail.ts` returns == 1 (backcompat fallback still emitted verbatim; header comment reference does not count — the source string literal used by the fallback must survive).
    - `grep -c "tail -F -n " src/backend/claude-session/session-file-tail.ts` returns >= 2 (both branches present).
    - Vitest passes on the file.
    - `npm run build:backend` exits 0 (no other callers broken by the signature change — adding an optional param is source-compatible).
  </acceptance_criteria>
  <done>
    All 4 tests from Task 1 pass. `git grep -n "tailSessionFile(" src/backend/` returns the same call sites as before the change, each still calling with 4 positional args. Commit as `feat(43-01): parameterize tailSessionFile initial-lines slice`.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/backend/claude-session/session-file-tail.test.ts` — exit 0, 4+ tests passing.
- `npm run build:backend` — exit 0.
- `git grep -n "tailSessionFile(" src/backend/` — every caller compiles with the new optional param (Wave 2 threads values through; this wave does not).
- `grep -c "tail -F -n +1" src/backend/claude-session/session-file-tail.ts` returns 1 (backcompat literal preserved).
</verification>

<success_criteria>
- `tailSessionFile` accepts an optional `initialLines` param without breaking any existing caller.
- Command shape branches deterministically based on validity of `initialLines`.
- Dedicated unit test file locks both paths + invalid-override coercion + path escaping.
- No changes anywhere outside `session-file-tail.ts` + `session-file-tail.test.ts`.
</success_criteria>

<output>
Create `.planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-01-SUMMARY.md` when done.
</output>
