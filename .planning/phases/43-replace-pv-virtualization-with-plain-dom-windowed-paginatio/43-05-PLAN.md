---
phase: 43-replace-pv-virtualization-with-plain-dom-windowed-paginatio
plan: 05
type: execute
wave: 2
depends_on: ['43-03']
files_modified:
  - src/ui/api/claude-session-api.ts
  - src/ui/api/claude-session-api.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "claude-session-api.ts exports a small helper (name: sendFetchOlder or similar) that takes an open WebSocket + a FetchOlderPayload and JSON-stringifies + sends it — no new socket is opened"
    - "claude-session-api.ts exposes a runtime type-guard `isFetchOlderBatchEvent(x)` returning true iff x has `type === 'fetch_older_batch'` and a frames array"
    - "Both the existing streaming path (message/image/relay_*) and the new fetch_older_batch response route through the same onmessage stream — PrettyView.tsx plan 43-06 will add the `case` branch"
  artifacts:
    - path: "src/ui/api/claude-session-api.ts"
      provides: "Runtime helpers to send fetch_older + recognize fetch_older_batch responses"
      exports: ["sendFetchOlder", "isFetchOlderBatchEvent"]
    - path: "src/ui/api/claude-session-api.test.ts"
      provides: "Unit coverage: sendFetchOlder stringifies + calls ws.send; isFetchOlderBatchEvent guard true/false cases"
  key_links:
    - from: "sendFetchOlder"
      to: "WebSocket.send"
      via: "JSON.stringify"
      pattern: "ws\\.send"
    - from: "isFetchOlderBatchEvent"
      to: "type guard on the FetchOlderBatchEvent shape from 43-03"
      via: "runtime discriminator check"
      pattern: "fetch_older_batch"
---

<objective>
Add two small runtime helpers to `src/ui/api/claude-session-api.ts` that the Wave 3 PrettyView plan will consume: `sendFetchOlder(ws, payload)` for the outbound request path and `isFetchOlderBatchEvent(x)` for the inbound frame recognition. This isolates the wire runtime from PrettyView's onmessage switch so the type/runtime contract can be tested independently.

Purpose: PrettyView plan 43-06 is the largest and riskiest plan in the phase. Landing these two small helpers first (with their own test) means the PrettyView plan can focus purely on the render surgery + client-side windowing state — the wire integration is a two-line import.

Output: Two exported helpers + a small test file. No consumer wiring yet. This plan is intentionally small and can run in parallel with plan 43-04 (backend Wave 2) since they touch different files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-CONTEXT.md
@.planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-PATTERNS.md

# Self — the file being extended (wire types from 43-03 must already be exported)
@src/ui/api/claude-session-api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write failing tests for sendFetchOlder + isFetchOlderBatchEvent</name>
  <read_first>
    - src/ui/api/claude-session-api.ts (verify FetchOlderPayload + FetchOlderBatchEvent are exported by plan 43-03 — if this plan is running, plan 43-03 has landed)
    - .planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-PATTERNS.md § "4. claude-session-api.ts" — the wire type pair convention
  </read_first>
  <files>src/ui/api/claude-session-api.test.ts</files>
  <behavior>
    - Test 1 (sendFetchOlder happy path): given a fake `ws = { send: vi.fn(), readyState: WebSocket.OPEN }` and `payload = { type: "fetch_older", anchorEventId: "evt-100", anchorLine: 150, count: 50 }`, `sendFetchOlder(ws, payload)` calls `ws.send` exactly once with `JSON.stringify(payload)`.
    - Test 2 (sendFetchOlder gates on readyState): with `readyState = WebSocket.CONNECTING` (or CLOSED), `sendFetchOlder` does NOT call `ws.send` and returns false (or otherwise indicates non-delivery — executor picks the signal but must be observable).
    - Test 3 (isFetchOlderBatchEvent true cases): the guard returns true for `{ type: "fetch_older_batch", frames: [] }`, `{ type: "fetch_older_batch", frames: [{}, {}], reachedBeginning: true }`, and `{ type: "fetch_older_batch", frames: [], error: "no session" }`.
    - Test 4 (isFetchOlderBatchEvent false cases): returns false for `null`, `undefined`, `"string"`, `{ type: "message" }`, `{ type: "fetch_older_batch" }` (no frames array), `{ type: "fetch_older_batch", frames: "not-array" }`.
  </behavior>
  <action>
    Create `src/ui/api/claude-session-api.test.ts` (if a claude-session-api.test.ts already exists on this branch — check with `ls src/ui/api/` — extend it instead of creating a new one; the same tests belong in whichever file). Import `sendFetchOlder` and `isFetchOlderBatchEvent` from `./claude-session-api.js` (or the appropriate module resolution). Both symbols do not exist yet — expect module-resolution or named-import errors. Write the four tests above. Run `npx vitest run src/ui/api/claude-session-api.test.ts` and confirm all four fail.
  </action>
  <verify>
    <automated>npx vitest run src/ui/api/claude-session-api.test.ts 2>&1 | grep -E "failed|FAIL|Cannot|not found" ; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists.
    - `grep -c "sendFetchOlder" src/ui/api/claude-session-api.test.ts` returns >= 2.
    - `grep -c "isFetchOlderBatchEvent" src/ui/api/claude-session-api.test.ts` returns >= 4.
    - All four tests fail with red output.
  </acceptance_criteria>
  <done>
    Failing tests committed. Commit as `test(43-05): add failing specs for sendFetchOlder + guard`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — add sendFetchOlder + isFetchOlderBatchEvent to claude-session-api.ts</name>
  <read_first>
    - src/ui/api/claude-session-api.ts (the file being extended; the FetchOlderPayload + FetchOlderBatchEvent types must already be exported by plan 43-03)
    - src/ui/api/claude-session-api.test.ts (Task 1's failing spec — exact signatures + guard branches to satisfy)
  </read_first>
  <files>src/ui/api/claude-session-api.ts</files>
  <action>
    Modify `src/ui/api/claude-session-api.ts`. Add two exports near the FetchOlder type declarations (or in whatever region the existing runtime helpers cluster — executor picks placement).

    (1) `export function sendFetchOlder(ws: WebSocket, payload: FetchOlderPayload): boolean` — body: if `ws.readyState !== WebSocket.OPEN` return false; try { ws.send(JSON.stringify(payload)); return true; } catch { return false; }`. Do not throw.

    (2) `export function isFetchOlderBatchEvent(x: unknown): x is FetchOlderBatchEvent` — body: `if (!x || typeof x !== "object") return false; const o = x as Record<string, unknown>; return o.type === "fetch_older_batch" && Array.isArray(o.frames);`. This is the minimal shape check; error/reachedBeginning are optional and don't gate the guard.

    Do NOT modify any other function, do NOT touch openClaudeSessionSocket or countIdentityBounties, do NOT change the FetchOlderPayload/FetchOlderBatchEvent shapes. Run `npx vitest run src/ui/api/claude-session-api.test.ts` — all four tests must PASS. Run `npm run build` — exit 0.
  </action>
  <verify>
    <automated>npx vitest run src/ui/api/claude-session-api.test.ts</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function sendFetchOlder" src/ui/api/claude-session-api.ts` returns 1.
    - `grep -c "export function isFetchOlderBatchEvent" src/ui/api/claude-session-api.ts` returns 1.
    - `grep -c "fetch_older_batch" src/ui/api/claude-session-api.ts` returns >= 2 (existing type from 43-03 + new guard literal).
    - All Task 1 tests PASS.
    - `npm run build` exits 0.
    - `grep -rn "sendFetchOlder\|isFetchOlderBatchEvent" src/ --include="*.ts" --include="*.tsx" | grep -v claude-session-api` returns ZERO hits (no premature wiring).
  </acceptance_criteria>
  <done>
    Helpers exported + tested; no consumers wired yet. Commit as `feat(43-05): add sendFetchOlder + isFetchOlderBatchEvent runtime helpers`.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/ui/api/claude-session-api.test.ts` — exit 0.
- `npm run build` — exit 0.
- Grep confirms no other file yet imports the new helpers (Wave 3 will).
</verification>

<success_criteria>
- Two small runtime helpers landed with a dedicated test file.
- Wave 3 PrettyView plan can import them as a two-line contract without needing to hand-roll JSON send or hand-roll type-narrowing.
</success_criteria>

<output>
Create `.planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-05-SUMMARY.md` when done.
</output>
