---
phase: 43-replace-pv-virtualization-with-plain-dom-windowed-paginatio
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/api/claude-session-api.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "openClaudeSessionSocket accepts an optional { historyWindow?: number } arg; when supplied it appears as ?historyWindow=N on the connect URL"
    - "openClaudeSessionSocket() with no args still returns a WebSocket to /claude-session/websocket/ with no query string (backcompat for any caller not opting in — including countIdentityBounties L851)"
    - "FetchOlderPayload is EXACTLY { type: 'fetch_older'; anchorEventId: string; count: number } — NO anchorLine field, no optional secondary hint"
    - "FetchOlderBatchEvent is exported and aligns with the existing Payload/Event type-pair convention (mirror IdentityCountBountiesPayload / IdentityBountyCountsEvent L814-830)"
    - "TypeScript build is clean; no consumer wiring is done in this plan"
  artifacts:
    - path: "src/ui/api/claude-session-api.ts"
      provides: "Wire scaffolding: historyWindow connect option + FetchOlderPayload + FetchOlderBatchEvent types"
      exports: ["openClaudeSessionSocket", "FetchOlderPayload", "FetchOlderBatchEvent"]
  key_links:
    - from: "src/ui/api/claude-session-api.ts"
      to: "The WS URL string constructed inside openClaudeSessionSocket"
      via: "Optional query-param append"
      pattern: "historyWindow="
---

<objective>
Extend `src/ui/api/claude-session-api.ts` with wire-scaffolding-only changes for Phase 43: (a) accept an optional `{ historyWindow?: number }` argument on `openClaudeSessionSocket` and append it as a query-string param, (b) export new `FetchOlderPayload` and `FetchOlderBatchEvent` types mirroring the existing Payload/Event convention. NO consumer wiring in this plan — Wave 2's plans wire types they need.

Purpose: Landing the wire types + the socket-opener extension as a standalone scaffolding change means Wave 2 backend (43-04) and Wave 2 frontend runtime helpers (43-05) can both compile against the SAME frozen type shape without racing over the api file. It also makes the query-param backcompat auditable in isolation via `grep countIdentityBounties` — nothing else in the tree should change behavior.

Output: `claude-session-api.ts` gains a tiny signature-compatible extension to `openClaudeSessionSocket` (any caller still using `openClaudeSessionSocket()` compiles) plus two new exported types. No new runtime functions. No changes to any file except `claude-session-api.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-CONTEXT.md
@.planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-PATTERNS.md

# Self — analog is this file's own existing patterns
@src/ui/api/claude-session-api.ts

# For reference — the ParsedLine union whose emission variants will be carried on FetchOlderBatchEvent.frames
@src/backend/claude-session/session-file-parser.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend openClaudeSessionSocket with optional historyWindow + add FetchOlder wire types</name>
  <read_first>
    - src/ui/api/claude-session-api.ts L14-23 (openClaudeSessionSocket current shape — target of the signature extension)
    - src/ui/api/claude-session-api.ts L814-830 (IdentityCountBountiesPayload + IdentityBountyCountsEvent — the exact type-pair naming convention to mirror)
    - src/ui/api/claude-session-api.ts L845-889 (countIdentityBounties — the ONE caller that still uses openClaudeSessionSocket() with no args; must continue to compile and behave identically)
    - src/ui/api/claude-session-api.ts L25-58 (existing StreamEvent-like wire types — check that FetchOlderBatchEvent's `frames` array element type aligns with the union already declared)
    - src/backend/claude-session/session-file-parser.ts (ParsedLine union — the shape of the frames the backend will emit inside a fetch_older_batch)
    - .planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-PATTERNS.md § "4. claude-session-api.ts" (wire type template + connect-with-historyWindow shape)
    - .planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-CONTEXT.md `<decisions>` § "Backend contract additions" — the wire contract is LOCKED: `anchorEventId: string` + `count: number`. The server resolves the eventId to a line offset on demand via `grep -n '"uuid":"<anchorEventId>"' <path>`. NO `anchorLine` field on the wire. The client physically cannot populate a line offset because ParsedLine variants carry no line offset.
  </read_first>
  <files>src/ui/api/claude-session-api.ts</files>
  <action>
    Modify `src/ui/api/claude-session-api.ts` in three surgical passes; do not touch any other function. Pass 1 — signature extension: replace the current `export function openClaudeSessionSocket(): WebSocket { ... }` at L14-23 with the same body plus an optional parameter `opts?: { historyWindow?: number }`. Inside, build a `qp` string: if `opts?.historyWindow` is a positive finite integer, set `qp = "?historyWindow=" + Math.floor(opts.historyWindow)`; else set `qp = ""`. Append `qp` to the existing URL string. Every call site that still invokes `openClaudeSessionSocket()` with zero args (grep-check: countIdentityBounties at L851 and any other) must continue to work — the parameter is optional and backcompat is byte-equivalent when omitted.

    Pass 2 — export type `FetchOlderPayload`. Place it near the other Payload/Event type pairs (e.g. immediately after `IdentityBountyCountsEvent` at L830 or in the same clustered region — executor picks placement). Shape EXACTLY: `type: "fetch_older"; anchorEventId: string; count: number;`. NO optional `anchorLine` field. NO placeholder comments hinting at future line-offset additions. This is LOCKED per 43-CONTEXT.md `<decisions>` § "Backend contract additions": the client's `messages[]` holds parsed frames from `parseSessionLine`; there is no line-offset in ParsedLine variants, so the client physically cannot populate a line offset. The server does the eventId→line lookup on demand. Add a JSDoc comment naming the field semantics: `anchorEventId` = eventId (uuid) of the oldest currently-loaded message; server resolves this to a line offset and responds with up to `count` messages STRICTLY OLDER than that anchor.

    Pass 3 — export type `FetchOlderBatchEvent`. Place adjacent to `FetchOlderPayload`. Shape: `type: "fetch_older_batch"; frames: unknown[]; reachedBeginning?: boolean; error?: string;` — the `frames` element type is intentionally `unknown[]` at this scaffolding stage because the concrete union is declared elsewhere in the file (`ClaudeSessionStreamEvent` around L25-58; the executor may narrow to that union if the type is exported, otherwise `unknown[]` + a JSDoc comment naming the expected shape is sufficient — Wave 3 PrettyView plan 43-07a/b will refine the type when it wires the case in the onmessage switch). Add a JSDoc comment: `frames` are historical ParsedLine emission variants in oldest-first order suitable for prepend; `reachedBeginning` is set by the server when startLine <= 1 (client stops firing fetch_older after this); `error` populated when the server could not resolve the anchor or the range read failed (client clears loading state, does NOT retry, per 43-CONTEXT.md § "Fetch failure handling").

    Do NOT add any runtime code (no new functions, no new consts, no new mocks). Do NOT modify any other file. Do NOT touch the existing `openClaudeSessionSubscribe` shape, `countIdentityBounties` implementation, or any other export. Run `npm run build` and confirm exit 0 (frontend build; no consumer needs to change).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -30 ; test ${PIPESTATUS[0]} -eq 0</automated>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | tail -20 ; test ${PIPESTATUS[0]} -eq 0</automated>
    <automated>grep -c "anchorLine" src/ui/api/claude-session-api.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "openClaudeSessionSocket(opts" src/ui/api/claude-session-api.ts` returns >= 1 (or matches whatever concrete signature form the executor uses — the semantic assertion is: the current default-export `openClaudeSessionSocket()` continues to compile at every existing call site).
    - `grep -c "historyWindow" src/ui/api/claude-session-api.ts` returns >= 2 (parameter destructuring + query-param assembly).
    - `grep -c "FetchOlderPayload" src/ui/api/claude-session-api.ts` returns >= 1 (export site).
    - `grep -c "FetchOlderBatchEvent" src/ui/api/claude-session-api.ts` returns >= 1 (export site).
    - `grep -c "fetch_older" src/ui/api/claude-session-api.ts` returns >= 2 (payload type literal + batch event type literal).
    - `grep -c "anchorLine" src/ui/api/claude-session-api.ts` returns EXACTLY 0 (locked: NO line-offset field on the wire).
    - `grep -n "openClaudeSessionSocket()" src/ui/api/claude-session-api.ts` shows the pre-existing call at line ~851 unchanged (countIdentityBounties still calls with no args).
    - `npm run build` exits 0.
    - `grep -rn "FetchOlderPayload\|FetchOlderBatchEvent\|historyWindow" src/ --include="*.ts" --include="*.tsx" | grep -v claude-session-api.ts` returns ZERO hits (nothing else touched by this plan).
  </acceptance_criteria>
  <done>
    Wire scaffolding in place, build clean, no consumers wired, anchorLine field forbidden. Commit as `feat(43-03): add historyWindow connect option + fetch_older wire types`.
  </done>
</task>

</tasks>

<verification>
- `npm run build` — exit 0.
- `npx tsc --noEmit -p tsconfig.json` — exit 0.
- `grep -rn "FetchOlderPayload\|FetchOlderBatchEvent" src/` shows hits ONLY in `src/ui/api/claude-session-api.ts` (no premature wiring).
- `grep -c "anchorLine" src/ui/api/claude-session-api.ts` returns 0 (wire contract locked to eventId-only).
- `grep -c "openClaudeSessionSocket()" src/ui/api/claude-session-api.ts` shows the no-arg call at L851 (or nearby) still present, unmodified.
</verification>

<success_criteria>
- Optional historyWindow connect param + two new wire types exported.
- FetchOlderPayload is exactly `{ type, anchorEventId, count }` — no line-offset field.
- Zero behavior change; every existing caller compiles and behaves identically.
- Wave 2 (backend + frontend plans) can now depend on a frozen, eventId-only fetch_older wire contract.
</success_criteria>

<output>
Create `.planning/phases/43-replace-pv-virtualization-with-plain-dom-windowed-paginatio/43-03-SUMMARY.md` when done.
</output>
</content>
</invoke>