---
phase: 128-wake-ups-redesign-campaign-shape-2-crud-api-fleet-wide-rest-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/database/routes/wakeups-list.ts
  - src/backend/database/routes/wakeups-write.ts
  - src/backend/database/routes/wakeups-list.test.ts
  - src/backend/database/routes/wakeups-write.test.ts
  - src/backend/claude-session/identity-artifact-reader.ts
  - src/backend/database/database.ts
  - docker/nginx.conf
  - docker/nginx-https.conf
autonomous: true
requirements: []
covers_decisions:
  - D-01  # HTTP REST endpoint style
  - D-02  # Fleet-wide LIST; host param on writes
  - D-03  # Thin API; no DB shadow
  - D-04  # Enumeration reuses existing roles-list-for-host (no new endpoint here)
  - D-05  # Atomic writes via .tmp + rename
  - D-06  # Hard delete + .fired sentinel cleanup
  - D-07  # kebab-case slug; 409 on collision
  - D-08  # Validation MIRRORS wakeup-scheduler.py's parser exactly
  - D-15  # SSH fan-out delimiter one-liner pattern
  - D-16  # skynet host is a managed host (LOCAL branch via isLocalHostId)
  - D-17  # Nginx paired blocks in BOTH nginx.conf and nginx-https.conf
must_haves:
  truths:
    - "GET /wakeups returns a fleet-wide aggregated array of global wake-up specs, each row carrying host/hostId/slug/name/enabled/schedule/scheduleHuman/prompt/roles[]/skills[]"
    - "POST /wakeups creates a new ~/fleet/wakeups/<slug>/wakeup.json atomically on the requested host and returns 201 with {slug, host, spec}"
    - "PATCH /wakeups/:slug overwrites an existing spec atomically and returns 200"
    - "PATCH /wakeups/:slug/toggle-enabled flips the enabled field only (atomic write) and returns 200"
    - "DELETE /wakeups/:slug hard-removes the folder AND its ~/fleet/wakeups/.state/<slug>.fired sentinel and returns 204"
    - "409 is returned when POST slug collides with an existing wake-up"
    - "400 is returned when spec violates the wakeup-scheduler.py _load_specs_global() parity gate (missing prompt/schedule, unknown schedule.type)"
    - "404 is returned when the target host is not owned by the caller (resolveHostById returns null)"
    - "LIST responds even when one managed host is down/slow — per-host timeout produces graceful [] rather than a whole-request failure"
    - "skynet's own host (isLocalHostId true) is included in fan-out and read via the container bind mount, not loopback SSH"
    - "New /wakeups routes reach the backend in BOTH HTTP and HTTPS nginx configs"
  artifacts:
    - path: "src/backend/database/routes/wakeups-list.ts"
      provides: "GET /wakeups fleet-wide fan-out router"
      min_lines: 150
      exports: ["default"]
    - path: "src/backend/database/routes/wakeups-write.ts"
      provides: "POST/PATCH/DELETE /wakeups router (per-host, JWT + resolveHostById + getHostSemaphore + atomic write)"
      min_lines: 250
      exports: ["default"]
    - path: "src/backend/database/routes/wakeups-list.test.ts"
      provides: "vitest coverage of LIST endpoint — 10+ cases"
      contains: "describe"
    - path: "src/backend/database/routes/wakeups-write.test.ts"
      provides: "vitest coverage of write endpoints — 10+ cases including 409 clobber + hard-delete-sentinel-cleanup"
      contains: "describe"
    - path: "src/backend/claude-session/identity-artifact-reader.ts"
      provides: "getLocalWakeupsRoot() helper + exported normalizeWakeupSlug for cross-file reuse"
      contains: "getLocalWakeupsRoot"
    - path: "src/backend/database/database.ts"
      provides: "app.use(\"/wakeups\", wakeupsListRoutes) + app.use(\"/wakeups\", wakeupsWriteRoutes) chained mounts"
      contains: "/wakeups"
    - path: "docker/nginx.conf"
      provides: "location ~ ^/wakeups(/.*)?$ block"
      contains: "location ~ ^/wakeups"
    - path: "docker/nginx-https.conf"
      provides: "paired /wakeups location block"
      contains: "location ~ ^/wakeups"
  key_links:
    - from: "src/backend/database/database.ts"
      to: "src/backend/database/routes/wakeups-list.ts"
      via: "app.use(\"/wakeups\", wakeupsListRoutes)"
      pattern: "app\\.use\\([\"']/wakeups[\"'], wakeupsList"
    - from: "src/backend/database/database.ts"
      to: "src/backend/database/routes/wakeups-write.ts"
      via: "app.use(\"/wakeups\", wakeupsWriteRoutes)"
      pattern: "app\\.use\\([\"']/wakeups[\"'], wakeupsWrite"
    - from: "src/backend/database/routes/wakeups-list.ts"
      to: "identity-artifact-reader.ts::isLocalHostId + humanizeWakeupSchedule + getLocalWakeupsRoot"
      via: "import"
      pattern: "from ['\"]\\.\\./\\.\\./claude-session/identity-artifact-reader"
    - from: "src/backend/database/routes/wakeups-write.ts"
      to: "identity-artifact-reader.ts::writeMarkdownFileAtomic + normalizeWakeupSlug + IDENTITY_SLUG_RE + isLocalHostId + getLocalWakeupsRoot"
      via: "import"
      pattern: "from ['\"]\\.\\./\\.\\./claude-session/identity-artifact-reader"
    - from: "docker/nginx.conf"
      to: "docker/nginx-https.conf"
      via: "paired location blocks (D-17)"
      pattern: "location ~ \\^/wakeups"
tags:
  - wakeups
  - crud-api
  - rest
  - ssh-fan-out
  - atomic-write
  - nginx
---

<objective>
Ship the new fleet-wide REST CRUD API surface for global wake-up specs, plus paired nginx blocks and Express router mounts, per D-01/D-02/D-03/D-05/D-06/D-07/D-08/D-15/D-16/D-17. This plan is purely ADDITIVE — no code is removed. Every downstream deletion in Plan 134-02 hangs off the fact that this plan lands green first (D-14 sequencing).

Purpose: Give the shape-3 UI modal an HTTP surface to consume — a fleet-wide LIST + per-host writes over the `~/fleet/wakeups/<slug>/wakeup.json` on-disk convention Phase 127 established. Files stay the source of truth (D-03); the API is a thin marshaling layer that mirrors `wakeup-scheduler.py`'s parser exactly (D-08).

Output: Two new route files, two new test files, a small helper addition to `identity-artifact-reader.ts` (`getLocalWakeupsRoot` + export `normalizeWakeupSlug`), two mount lines in `database.ts`, and paired location blocks in both nginx configs. Scoped tests green on every touched path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-wake-ups-redesign-campaign-shape-2-crud-api-fleet-wide-rest-/134-CONTEXT.md
@.planning/phases/134-wake-ups-redesign-campaign-shape-2-crud-api-fleet-wide-rest-/134-RESEARCH.md
@.planning/phases/134-wake-ups-redesign-campaign-shape-2-crud-api-fleet-wide-rest-/134-PATTERNS.md
@.planning/campaigns/wake-ups-redesign/shape-wake-ups-crud-api.md

# Analog source files — READ before editing (referenced per task via <read_first>)
@src/backend/database/routes/roles-list-for-host.ts
@src/backend/database/routes/roles-create.ts
@src/backend/database/routes/conversation-search.ts
@src/backend/claude-session/identity-artifact-reader.ts
@substrate/scripts/wakeup-scheduler.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend identity-artifact-reader.ts with getLocalWakeupsRoot() helper + export normalizeWakeupSlug</name>
  <files>src/backend/claude-session/identity-artifact-reader.ts</files>
  <read_first>
    - src/backend/claude-session/identity-artifact-reader.ts  (focus lines 233 isLocalHostId, 276-304 getLocalIdentitiesRoot + getLocalRolesRoot, 2183-2185 normalizeWakeupSlug, 1431 IDENTITY_SLUG_RE)
    - .planning/phases/134-wake-ups-redesign-campaign-shape-2-crud-api-fleet-wide-rest-/134-PATTERNS.md § "Pattern for the potential new export"
    - .planning/phases/134-wake-ups-redesign-campaign-shape-2-crud-api-fleet-wide-rest-/134-RESEARCH.md § "Don't Hand-Roll" table + Assumption A4
  </read_first>
  <behavior>
    - After the edit, `import { getLocalWakeupsRoot, normalizeWakeupSlug, IDENTITY_SLUG_RE, isLocalHostId, humanizeWakeupSchedule, writeMarkdownFileAtomic } from "./identity-artifact-reader.js"` succeeds from a sibling module.
    - `getLocalWakeupsRoot()` returns `path.join(path.dirname(getLocalIdentitiesRoot()), "wakeups")` — same HOME_HOST_DIR-derived resolution as getLocalIdentitiesRoot / getLocalRolesRoot.
    - `normalizeWakeupSlug(name: string): string` is now exported (was module-private). Body unchanged: `name.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "")`.
    - No other exports change; no per-identity or per-role wake-up function is modified in this task (removals live in Plan 134-02).
  </behavior>
  <action>
    Add a new exported function `getLocalWakeupsRoot(): string` alongside `getLocalRolesRoot` (near L304 of `identity-artifact-reader.ts`). Body derives from the same HOME_HOST_DIR resolution as `getLocalIdentitiesRoot` — cheapest impl is `return path.join(path.dirname(getLocalIdentitiesRoot()), "wakeups");`. Add `export` in front of the existing `normalizeWakeupSlug` at L2183 (byte-identical body). Do NOT modify any other function. Do NOT change any signature. Preserve every existing docstring exactly.
  </action>
  <verify>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && npx vitest related --run src/backend/claude-session/identity-artifact-reader.ts 2>&1 | tail -40</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c "^export function getLocalWakeupsRoot" src/backend/claude-session/identity-artifact-reader.ts</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c "^export function normalizeWakeupSlug" src/backend/claude-session/identity-artifact-reader.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^export function getLocalWakeupsRoot" src/backend/claude-session/identity-artifact-reader.ts` outputs `1`.
    - `grep -c "^export function normalizeWakeupSlug" src/backend/claude-session/identity-artifact-reader.ts` outputs `1` (i.e. the existing function is now exported).
    - Scoped vitest run against `identity-artifact-reader.ts` and its related tests exits 0 (no regressions in per-identity or per-role wake-up tests — those live in Plan 134-02, must still be green here).
    - Zero-diff on any other exported symbol: `git diff --stat src/backend/claude-session/identity-artifact-reader.ts` shows only additions + the single `export` keyword insertion (no deletions).
  </acceptance_criteria>
  <done>New helper + surfaced export are in place; existing test suite for identity-artifact-reader still green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create wakeups-list.ts (GET /wakeups fleet-wide fan-out) + wakeups-list.test.ts</name>
  <files>src/backend/database/routes/wakeups-list.ts, src/backend/database/routes/wakeups-list.test.ts</files>
  <read_first>
    - src/backend/database/routes/conversation-search.ts  (fleet fan-out shape at L471-635, host projection at L542-563, per-host timeout race L576-622)
    - src/backend/database/routes/roles-list-for-host.ts  (JWT + hostId gate at L108-128, error middleware L281-297)
    - src/backend/database/routes/roles-list-for-host.test.ts  (mock+Express-app boilerplate L25-90; 10-case coverage template)
    - src/backend/claude-session/identity-artifact-reader.ts  (readIdentityWakeups LOCAL branch L1438-1483 + REMOTE branch L1486-1535 — the delimiter one-liner pattern to adapt)
    - substrate/scripts/wakeup-scheduler.py  (L1-248 — _load_specs_global parser; source of truth for the returned shape's fields)
    - .planning/phases/134-wake-ups-redesign-campaign-shape-2-crud-api-fleet-wide-rest-/134-PATTERNS.md § "wakeups-list.ts" full section
  </read_first>
  <behavior>
    - `GET /wakeups` (JWT-gated) responds 200 with `{ items: WakeupListItem[] }` where each item has `slug, host, hostId, name, enabled, schedule, scheduleHuman, prompt, roles[], skills[]`.
    - Fan-out iterates `db.select().from(hosts).where(eq(hosts.userId, userId))` filtered by `enableSsh` AND `terminalConfig.autoTmux !== false`.
    - Each host wrapped in `Promise.race([work, timeout(15_000ms)])` and `.catch(() => [])` — one slow/down host contributes `[]`, does NOT poison the aggregate.
    - `isLocalHostId(hostId)` branch reads via `fs/promises` against `getLocalWakeupsRoot()`; REMOTE branch runs the exact delimiter one-liner `cd "$HOME/fleet/wakeups" 2>/dev/null && for d in */; do slug="${d%/}"; echo "===SLUG:${slug}==="; cat "$d/wakeup.json" 2>/dev/null; done` and parses each `===SLUG:<slug>===` block.
    - Poisoned JSON on one entry is skipped (sshLogger.warn) — never poisons the whole host's rows.
    - `~/fleet/wakeups/` absent on a host: silent empty (no error surfaces).
    - Missing JWT → 401 (middleware).
    - Trailing error middleware returns `{error:"internal"}` 500 on unhandled exceptions.
    - Test suite covers 10+ cases:  auth (401), 2 healthy hosts aggregate, one host connect-fails, one host times out, LOCAL branch happy, REMOTE branch happy, poisoned JSON skipped, empty directory returns [], cross-user host filtered by Drizzle projection, humanizeWakeupSchedule field populated per row.
  </behavior>
  <action>
    Create `src/backend/database/routes/wakeups-list.ts` mirroring the fan-out shape from `conversation-search.ts` L471-635 and the delimiter parse loop adapted from `identity-artifact-reader.ts::readIdentityWakeups` L1486-1535 (adapt path from `identities/<key>/wakeups/*.json` to `wakeups/<slug>/wakeup.json`). Import surface: express + Request/Response + AuthenticatedRequest + eq (drizzle-orm) + db + hosts + AuthManager + SimpleDBOps + sshLogger + resolveHostById + connectOneShot + execCommand + isLocalHostId + humanizeWakeupSchedule + getLocalWakeupsRoot + fs/promises + path. Define local type `WakeupListItem`. Constants CONNECT_TIMEOUT_MS=5_000, PER_HOST_TIMEOUT_MS=15_000. Handler: JWT auth, project hosts, filter candidates (enableSsh + autoTmux), Promise.all fan-out with per-host race + catch, flat + res.json({items: flat}). Trailing error middleware. Default-export router.

    Also create `src/backend/database/routes/wakeups-list.test.ts` mirroring `roles-list-for-host.test.ts` L25-90 boilerplate (vi.mock on auth-manager + ssh-one-shot + tmux-helper + host-resolver + drizzle db). At minimum 10 test cases per <behavior>. Use `express` + `http.createServer` on a random port pattern from the analog. Assert JSON shape includes every field named in <behavior> for at least one happy-path row.
  </action>
  <verify>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && npx vitest related --run src/backend/database/routes/wakeups-list.ts src/backend/database/routes/wakeups-list.test.ts 2>&1 | tail -60</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c "===SLUG:" src/backend/database/routes/wakeups-list.ts</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c "isLocalHostId" src/backend/database/routes/wakeups-list.ts</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c "Promise.race" src/backend/database/routes/wakeups-list.ts</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -Ec "^\s*(it|test)\(" src/backend/database/routes/wakeups-list.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "===SLUG:" src/backend/database/routes/wakeups-list.ts` >= 1 (delimiter one-liner present).
    - `grep -c "isLocalHostId" src/backend/database/routes/wakeups-list.ts` >= 1 (LOCAL/REMOTE branch present).
    - `grep -c "Promise.race" src/backend/database/routes/wakeups-list.ts` >= 1 (per-host timeout present).
    - `grep -Ec "^\s*(it|test)\(" src/backend/database/routes/wakeups-list.test.ts` >= 10 (>= 10 test cases).
    - `npx vitest related --run src/backend/database/routes/wakeups-list.ts src/backend/database/routes/wakeups-list.test.ts` exits 0.
    - `wakeups-list.ts` ends with `export default router;`.
    - No test names of the form "reject spec with too-long prompt" or "require minimum roles count" appear (Pitfall #4 — LIST is read-only anyway, but the negative check enforces the discipline).
  </acceptance_criteria>
  <done>GET /wakeups router + test file land; scoped vitest green; at least 10 test cases pass covering behavior surface.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create wakeups-write.ts (POST/PATCH/DELETE /wakeups per-host handlers) + wakeups-write.test.ts</name>
  <files>src/backend/database/routes/wakeups-write.ts, src/backend/database/routes/wakeups-write.test.ts</files>
  <read_first>
    - src/backend/database/routes/roles-create.ts  (POST-with-JWT + resolveHostById + getHostSemaphore + connectOneShot + writeMarkdownFileAtomic — full shape at L286-629)
    - src/backend/database/routes/roles-create.test.ts  (400/404/409/happy/atomic-write/auth coverage template)
    - src/backend/claude-session/identity-artifact-reader.ts  (writeIdentityWakeupCreate at L2359, writeIdentityWakeupUpdate at L2003, writeIdentityWakeupDelete at L2437 — atomic-write shape; writeMarkdownFileAtomic at L2583-2700; ext_openssh_rename prologue at L2547-2582)
    - substrate/scripts/wakeup-scheduler.py  (_load_specs_global at L224-248 — VALIDATION SOURCE OF TRUTH; _due at L180-205 for schedule.type gate; _parse_at_ts L160-177)
    - .planning/phases/134-wake-ups-redesign-campaign-shape-2-crud-api-fleet-wide-rest-/134-PATTERNS.md § "wakeups-write.ts" + "Anti-pattern warning"
    - .planning/phases/134-wake-ups-redesign-campaign-shape-2-crud-api-fleet-wide-rest-/134-RESEARCH.md § "Common Pitfalls" #1, #2, #4
  </read_first>
  <behavior>
    - `POST /wakeups` body `{host: number, spec: GlobalWakeupSpec}` → validate spec per `_load_specs_global` parity (D-08 — accept iff dict-shaped + `enabled !== false` + truthy `prompt` string + truthy `schedule` object + `schedule.type ∈ {interval, daily, weekly, one_shot}`; type-specific field checks per _due/L180-205 and _parse_at_ts) → derive slug = normalizeWakeupSlug(spec.name) → gate `IDENTITY_SLUG_RE.test(slug)` (else 400) → resolveHostById → getHostSemaphore(hostId).run → clobber probe (`[ -e ... ] && echo EXISTS || echo OK` REMOTE; `fs.access` LOCAL) → 409 on collision → atomic write (SFTP writeFile + ext_openssh_rename REMOTE; fs.writeFile(.tmp) + fs.rename LOCAL) → 201 `{slug, host, spec}`.
    - `PATCH /wakeups/:slug` body `{host, spec}` → same validation minus the clobber probe → atomic write (full-spec overwrite; no python3 merge) → 200 `{slug, host, spec}`.
    - `PATCH /wakeups/:slug/toggle-enabled` body `{host, enabled: boolean}` → read spec → mutate `enabled` field → atomic write → 200 `{slug, host, enabled}`. Missing spec → 404.
    - `DELETE /wakeups/:slug` body `{host}` → resolveHostById → getHostSemaphore → hard delete: REMOTE `rm -rf "$HOME/fleet/wakeups/${slug}" && rm -f "$HOME/fleet/wakeups/.state/${slug}.fired"` in ONE `execCommand` per Pitfall #2; LOCAL `fs.rm(dir, {recursive:true, force:true})` + `fs.unlink(sentinel).catch(()=>{})` → 204.
    - Every writer wraps its work in `getHostSemaphore(hostId).run(...)` to serialize per-host writes (Pitfall — concurrent CREATE race).
    - Every SSH conn opened is closed in a `finally` (`conn.end()`).
    - Any SSH connect failure → 502 `{error:"SSH connect failed"}`; SSH exec/write failure → 502 `{error:"SSH exec failed"}` or `"SFTP write failed"`; unhandled → trailing 500 middleware.
    - Body cap `express.json({limit:"64kb"})` (V5 ASVS — DoS-via-unbounded-body mitigation).
    - Validation does NOT add gates the scheduler wouldn't have (D-08, Pitfall #4). Do NOT enforce prompt-min-length, roles-min-count, mimetype gates, or any other opinionation.
    - Test suite covers 13+ cases per PATTERNS.md test list (missing host → 400, empty-normalized slug → 400, unknown host → 404, 409 clobber, happy CREATE REMOTE, happy CREATE LOCAL, semaphore serializes two concurrent CREATEs, missing prompt → 400, missing schedule → 400, unknown schedule.type → 400, UPDATE happy, TOGGLE happy, DELETE + sentinel cleanup verified via single execCommand mock, 401 without JWT). Regression pin: `sftp.rename` is NEVER called — install a throwing trap; only `ext_openssh_rename` is used.
  </behavior>
  <action>
    Create `src/backend/database/routes/wakeups-write.ts`. Imports mirror roles-create.ts L91-115 minus multer (this route is `application/json`), plus wakeups-specific helpers from identity-artifact-reader.js (`writeMarkdownFileAtomic`, `normalizeWakeupSlug`, `IDENTITY_SLUG_RE`, `isLocalHostId`, `getLocalWakeupsRoot`, `humanizeWakeupSchedule`). Define local type `GlobalWakeupSpec` with fields `name:string, enabled?:boolean, prompt:string, schedule:{type:"interval"|"daily"|"weekly"|"one_shot", ...}, roles?:string[], skills?:string[]`. Define local `validateGlobalWakeupSpec(spec: unknown): string | null` mirroring `_load_specs_global` L224-248 exactly — no extra gates. Register 4 handlers: `router.post("/")`, `router.patch("/:slug")`, `router.patch("/:slug/toggle-enabled")`, `router.delete("/:slug")`. Each handler follows: JWT → parse body → validate → gate slug → resolveHostById → getHostSemaphore.run → LOCAL/REMOTE branch → status response. All SSH conns opened inside a `try { conn = await connectOneShot(...) } finally { conn?.end() }` block. Use `writeMarkdownFileAtomic(conn, targetPath, JSON.stringify(validSpec, null, 2) + "\n")` with an INLINE comment: `// writeMarkdownFileAtomic is byte-agnostic despite the name — JSON body passes through unchanged (RESEARCH § Anti-patterns).` DELETE endpoint issues the combined `rm -rf ... && rm -f ...` in a single `execCommand` (Pitfall #2 verbatim). Trailing catch-all middleware + default-export router.

    Also create `src/backend/database/routes/wakeups-write.test.ts` mirroring `roles-create.test.ts` boilerplate. At minimum 13 test cases per <behavior>. Install a `vi.fn().mockImplementation(() => { throw new Error("sftp.rename must NOT be called — use ext_openssh_rename per Pitfall #1"); })` trap on `sftp.rename` and assert it never fires across the happy-path tests. Include a specific test asserting that on DELETE, the mocked `execCommand` is called with a command string containing BOTH `rm -rf` (for the folder) AND `rm -f` (for the `.fired` sentinel) — this is the D-06/Pitfall #2 regression pin.
  </action>
  <verify>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && npx vitest related --run src/backend/database/routes/wakeups-write.ts src/backend/database/routes/wakeups-write.test.ts 2>&1 | tail -80</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c "getHostSemaphore" src/backend/database/routes/wakeups-write.ts</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c "ext_openssh_rename\|writeMarkdownFileAtomic" src/backend/database/routes/wakeups-write.ts</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c '\.fired' src/backend/database/routes/wakeups-write.ts</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -Ec "^\s*(it|test)\(" src/backend/database/routes/wakeups-write.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getHostSemaphore" src/backend/database/routes/wakeups-write.ts` >= 4 (one per write handler).
    - `grep -c "ext_openssh_rename\|writeMarkdownFileAtomic" src/backend/database/routes/wakeups-write.ts` >= 1 (atomic write primitive routed through the existing helper).
    - `grep -c '\.fired' src/backend/database/routes/wakeups-write.ts` >= 1 (D-06 sentinel cleanup wired in DELETE).
    - `grep -Ec "^\s*(it|test)\(" src/backend/database/routes/wakeups-write.test.ts` >= 13.
    - Test file contains a case whose name (or expect assertion) verifies `execCommand` was called with a string matching `rm -rf.*\.fired` OR two adjacent calls covering both — grep-verifiable by `grep -c "\\.fired" src/backend/database/routes/wakeups-write.test.ts` >= 1.
    - Test file contains no assertion whose name suggests validation-drift (no "reject spec with too-long prompt", "require minimum roles count", or similar) — negative grep: `grep -Ei "too.?long|minimum roles|prompt cap" src/backend/database/routes/wakeups-write.test.ts` returns nothing.
    - `npx vitest related --run src/backend/database/routes/wakeups-write.ts src/backend/database/routes/wakeups-write.test.ts` exits 0.
    - `wakeups-write.ts` ends with `export default router;`.
  </acceptance_criteria>
  <done>POST/PATCH/DELETE handlers land with hard-delete + sentinel cleanup + scheduler-parity validation; scoped vitest green with 13+ cases including the ext_openssh_rename regression pin.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Wire routers into database.ts + add paired nginx location blocks</name>
  <files>src/backend/database/database.ts, docker/nginx.conf, docker/nginx-https.conf</files>
  <read_first>
    - src/backend/database/database.ts  (L2010-2020 — the /roles chained mounts, `app.use("/roles", rolesListForHostRoutes); app.use("/roles", rolesCreateRoutes); app.use("/roles", rolesRoutes);`; also read the import block near top-of-file for the analog import style)
    - docker/nginx.conf  (L440-449 — the /roles location block)
    - docker/nginx-https.conf  (L455-464 — the paired /roles block)
    - .planning/phases/134-wake-ups-redesign-campaign-shape-2-crud-api-fleet-wide-rest-/134-PATTERNS.md § "database.ts" + "nginx.conf + nginx-https.conf"
    - .planning/PROJECT.md § Constraints (nginx paired-blocks rule)
  </read_first>
  <behavior>
    - `database.ts` imports both new routers and chain-mounts them at `/wakeups` immediately after the existing `/roles` chain (positioned BEFORE the `/identities` late catch-alls).
    - Both nginx configs (HTTP + HTTPS) carry a matching `location ~ ^/wakeups(/.*)?$` block with the same shape as `/roles` (proxy_pass to 127.0.0.1:30001, proxy_http_version 1.1, standard proxy_set_header stanza, proxy_read_timeout 15s, client_max_body_size 64k).
    - After this task, hitting `GET /wakeups` in the dev container returns 401 (JWT gate — endpoint is reachable) rather than 200-with-index.html (which would mean nginx is falling through to the SPA).
    - `npm run build:backend && npm run build` completes without TS errors (Pitfall #7 — backend tsc discipline).
  </behavior>
  <action>
    In `src/backend/database/database.ts`: at the imports block at the top, add `import wakeupsListRoutes from "./routes/wakeups-list.js";` and `import wakeupsWriteRoutes from "./routes/wakeups-write.js";` alongside the analogous roles imports. At L2015 (immediately after `app.use("/roles", rolesCreateRoutes);`, before `app.use("/roles", rolesRoutes);` on L2020 OR immediately after L2020 — position is planner's discretion but MUST be BEFORE the `/identities` catch-alls), insert TWO lines: `app.use("/wakeups", wakeupsListRoutes);` and `app.use("/wakeups", wakeupsWriteRoutes);`, each preceded by a 1-2 line comment referencing Phase 134 D-01/D-02/D-17.

    In `docker/nginx.conf`: after the existing `/roles` block ending near L449 and before the `/global-files` block near L462, insert a new `location ~ ^/wakeups(/.*)?$` block that mirrors the `/roles` block byte-for-byte, substituting `/wakeups` for `/roles`. Include an inline comment referencing Phase 134 + the D-17 paired-blocks constraint + the client_max_body_size 64k parity with the express.json 64kb cap. Use `proxy_read_timeout 15s`.

    In `docker/nginx-https.conf`: perform the equivalent insertion after the paired `/roles` block near L464, before the paired `/global-files` block. Same block contents. Same comment.
  </action>
  <verify>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c 'app\.use\("/wakeups"' src/backend/database/database.ts</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c 'from "./routes/wakeups-list' src/backend/database/database.ts</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c 'from "./routes/wakeups-write' src/backend/database/database.ts</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c 'location ~ \^/wakeups' docker/nginx.conf</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c 'location ~ \^/wakeups' docker/nginx-https.conf</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && npm run build:backend 2>&1 | tail -30</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && npm run build 2>&1 | tail -30</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && npx vitest related --run src/backend/database/routes/wakeups-list.ts src/backend/database/routes/wakeups-write.ts src/backend/database/database.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'app\.use\("/wakeups"' src/backend/database/database.ts` == 2 (two chained mounts).
    - `grep -c 'from "./routes/wakeups-list' src/backend/database/database.ts` == 1.
    - `grep -c 'from "./routes/wakeups-write' src/backend/database/database.ts` == 1.
    - `grep -c 'location ~ \^/wakeups' docker/nginx.conf` == 1.
    - `grep -c 'location ~ \^/wakeups' docker/nginx-https.conf` == 1.
    - Both nginx blocks contain `proxy_pass http://127.0.0.1:30001;`, `proxy_read_timeout 15s;`, and `client_max_body_size 64k;` — verify via grep count each == 1 per file (2 total per token across the two configs).
    - `npm run build:backend` exits 0 (Pitfall #7 — backend TS clean).
    - `npm run build` exits 0.
    - Scoped vitest for the two route files still exits 0.
  </acceptance_criteria>
  <done>Routers wired + nginx paired-blocks present + backend tsc clean + frontend build clean + scoped tests green. Plan 134-02 (removal) is now unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Skynet REST API | Untrusted HTTP client (browser tab, agent script) sends JWT + JSON body across the nginx boundary. |
| Skynet backend → managed host over SSH | Backend interpolates slug into shell commands + SFTP paths on remote host filesystems. |
| Backend LOCAL branch → container bind mount | Backend writes/reads files on the container's mounted `/host-home/fleet/wakeups/...` tree with process-uid permissions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-01 | Tampering | wakeups-write.ts slug interpolation into SSH `rm -rf`, `mkdir -p`, existence probe | mitigate | Slug passes `IDENTITY_SLUG_RE = /^[a-z0-9_-]{1,80}$/i` at handler entry; failure → 400 before any SSH exec runs. `.` explicitly disallowed → traversal structurally impossible. Interpolation into double-quoted shell path only, never into single-quoted strings. |
| T-134-02 | Elevation of privilege / cross-user access | wakeups-write.ts, wakeups-list.ts host resolution | mitigate | Every handler calls `resolveHostById(hostId, userId)` — returns null on cross-user / unknown → 404. LIST also filters `db.select().from(hosts).where(eq(hosts.userId, userId))` before fan-out. Same primitive that mitigates T-22-04-03 across the entire Skynet REST surface. |
| T-128-03 | Denial of service | LIST fan-out over slow/dead hosts | mitigate | `Promise.race([work, timeout(15_000)])` per host + `.catch(() => [])`. One host down contributes empty rows, does not stall the whole response. |
| T-128-04 | Denial of service | POST/PATCH request body size | mitigate | `express.json({limit:"64kb"})` on every write handler. Nginx `client_max_body_size 64k` provides defense in depth at the edge. |
| T-128-05 | Tampering | Concurrent CREATE race on same slug | mitigate | `getHostSemaphore(hostId).run(...)` wraps every write handler — serializes per-host, so the second concurrent CREATE sees the freshly-written spec and 409s. |
| T-128-06 | Tampering | Non-atomic file write leaves corrupt spec the scheduler tries to load | mitigate | `writeMarkdownFileAtomic` (byte-agnostic; passes JSON through unchanged) uses SFTP `writeFile` + `ext_openssh_rename` (POSIX rename semantics; overwrites atomically). LOCAL branch uses `fs.writeFile(.tmp) + fs.rename`. `sftp.rename` is banned (Pitfall #1) — explicit test trap ensures no regression. |
| T-128-07 | Availability (silent-fail) | DELETE orphans `.state/<slug>.fired` sentinel — new spec with same slug won't fire | mitigate | DELETE endpoint issues a combined `rm -rf "$HOME/fleet/wakeups/<slug>" && rm -f "$HOME/fleet/wakeups/.state/<slug>.fired"` per Pitfall #2. Test asserts both cleanups happen in a single exec. |
| T-128-08 | Information disclosure | SSH stderr leaked into HTTP response body | mitigate | Handlers return generic error strings ("SSH connect failed" / "SSH exec failed" / "SFTP write failed"). Upstream detail goes to `sshLogger.warn/error` only. |
| T-128-09 | Repudiation | No audit trail for who created/deleted a wake-up | accept | Wake-up specs are per-user by resolveHostById gate. Fleet is single-user (the user). `sshLogger` operation-tagged entries are sufficient for post-hoc grep. Explicit audit log deferred (out of scope per shape's "thin API" philosophy). |
| T-128-10 | Correctness (drift) | API adds validation constraints the scheduler doesn't have — UI-created specs fail differently than agent-created ones | mitigate | D-08: `validateGlobalWakeupSpec` mirrors `_load_specs_global` at wakeup-scheduler.py L224-248 exactly. Negative grep in test acceptance forbids drift-flavored test names (Pitfall #4). |
| T-128-11 | Availability | Nginx paired-blocks miss (HTTPS conf missing the new location) | mitigate | Task 4 acceptance criteria explicitly grep-check both `docker/nginx.conf` AND `docker/nginx-https.conf`. D-17 constraint. |
| T-128-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan (verified by RESEARCH § "Package Legitimacy Audit — Not applicable"). No slopcheck required. |
</threat_model>

<verification>
- Every acceptance criterion in each task's `<acceptance_criteria>` block must pass.
- Scoped tests green on all four touched vitest paths: `identity-artifact-reader.ts`, `wakeups-list.ts`, `wakeups-write.ts`, `database.ts`.
- `npm run build:backend && npm run build` exits 0 (mandatory backend-TS check per Pitfall #7).
- No banned-strings violation (`npx vitest run` orchestrator-side would catch — but scoped-level grep-check acceptable at plan level: `grep -rin '\buser\b' src/backend/database/routes/wakeups-*.ts | grep -v -E '(the user|test-context)'` returns nothing).
- New route reachable end-to-end: pending Plan 134-02 completion + full-suite + docker build + docker compose up (all orchestrator-managed).
</verification>

<success_criteria>
- Four new source files exist (`wakeups-list.ts`, `wakeups-list.test.ts`, `wakeups-write.ts`, `wakeups-write.test.ts`).
- `identity-artifact-reader.ts` gains `getLocalWakeupsRoot` + exports `normalizeWakeupSlug`; no other export changes.
- `database.ts` mounts both routers at `/wakeups` chained.
- Both nginx configs carry paired `location ~ ^/wakeups(/.*)?$` blocks.
- Every task's scoped vitest exits 0.
- `npm run build:backend && npm run build` clean.
- Zero net-negative diff to any existing per-identity wake-up code path (Plan 134-02 handles per-role deletions separately).
</success_criteria>

<output>
Create `.planning/phases/134-wake-ups-redesign-campaign-shape-2-crud-api-fleet-wide-rest-/134-01-SUMMARY.md` when done. Include: files touched, tests added (case count per file), scoped test results, backend + frontend build results, and a signal to the orchestrator that Plan 134-02 is now safe to execute (D-14 sequencing satisfied).
</output>
