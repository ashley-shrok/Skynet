---
phase: 128-wake-ups-redesign-campaign-shape-2-crud-api-fleet-wide-rest-
plan: 02
type: execute
wave: 2
depends_on:
  - 134-01
files_modified:
  - src/backend/claude-session/identity-artifact-reader.ts
  - src/backend/claude-session/claude-session-server.ts
  - src/ui/api/claude-session-api.ts
  - src/ui/features/pretty-view/RoleModal.tsx
  - src/backend/claude-session/identity-artifact-reader.role-wakeups.test.ts
  - src/backend/claude-session/claude-session-server.role-wakeups.test.ts
  - src/backend/claude-session/claude-session-server.role-wakeup-crud.test.ts
  - src/ui/api/claude-session-api.role-wakeup-crud.test.ts
  - src/ui/api/claude-session-api.role-reads.test.ts
  - src/ui/features/pretty-view/PrettyView.role-modal-swap.test.tsx
autonomous: true
requirements: []
covers_decisions:
  - D-09  # WakeupsTab.tsx stays intact (used by IdentityModal); RoleModal's mount comes out
  - D-10  # Backend service function removal in identity-artifact-reader.ts
  - D-11  # WS wire-op handler + JSDoc removal in claude-session-server.ts
  - D-12  # Frontend API helper + payload/event type removal in claude-session-api.ts
  - D-13  # Test removal — 4 wholesale + 2 surgical
  - D-14  # Order-of-operations — this plan runs AFTER 134-01 lands green
must_haves:
  truths:
    - "No caller (frontend or backend) can invoke a role-scope wake-up CRUD operation after this plan lands — the API is gone, the WS handlers are gone, the UI tab is gone."
    - "RoleModal.tsx no longer imports WakeupsTab; no longer imports AlarmClock; no longer imports list/create/update/deleteRoleWakeupByName; no longer has a role-wakeups NAV_SECTION entry."
    - "IdentityModal's per-identity wake-up mount is UNCHANGED — the per-identity WakeupsTab consumer stays live."
    - "identity-artifact-reader.wakeup-crud.test.ts (per-identity, still live) stays in the repo and still passes."
    - "The 4 wholesale-delete test files no longer exist in the tree."
    - "The 2 surgically-edited test files (role-reads.test.ts + PrettyView.role-modal-swap.test.tsx) keep their non-wakeup coverage intact."
    - "`npm run build:backend && npm run build` exits 0 — no dangling imports, no dead type references."
    - "Scoped vitest across every touched file exits 0."
  artifacts:
    - path: "src/backend/claude-session/identity-artifact-reader.ts"
      provides: "Post-D-10 state — 6 role-wakeup functions removed; per-identity + shared helpers untouched"
      contains: "readIdentityWakeups"
    - path: "src/backend/claude-session/claude-session-server.ts"
      provides: "Post-D-11 state — 8 role-wakeup WS handlers removed + wire-op JSDoc pruned + import block trimmed"
      contains: "identity:list-wakeups"
    - path: "src/ui/api/claude-session-api.ts"
      provides: "Post-D-12 state — 5 role-wakeup helpers + payload/event types removed; WakeupSpecWire STAYS"
      contains: "WakeupSpecWire"
    - path: "src/ui/features/pretty-view/RoleModal.tsx"
      provides: "Post-D-09 state — role-wakeups tab removed; other tabs (role, runbooks, bounties) untouched"
      contains: "runbooks"
  key_links:
    - from: "src/ui/features/pretty-view/IdentityModal.tsx"
      to: "src/ui/features/pretty-view/WakeupsTab.tsx"
      via: "import (still present, untouched)"
      pattern: "import.*WakeupsTab"
    - from: "src/ui/features/pretty-view/RoleModal.tsx"
      to: "(no WakeupsTab import after this plan)"
      via: "removal verification"
      pattern: "!import.*WakeupsTab"
tags:
  - wakeups
  - removal
  - cleanup
  - role-scope-retirement
---

<objective>
Remove the retired per-role wake-up CRUD surface top-to-bottom: 6 backend service functions from `identity-artifact-reader.ts` (D-10), 8 WS wire-op handlers + response types + JSDoc entries from `claude-session-server.ts` (D-11), 5 frontend API helpers + associated payload/event types from `claude-session-api.ts` (D-12), the `role-wakeups` tab + its state/effects/callbacks/imports from `RoleModal.tsx` (D-09), 4 test files wholesale + 2 test files with surgical excision (D-13). Preserve every per-identity wake-up path and every `WakeupsTab.tsx` consumer that IdentityModal still uses.

Purpose: Phase 127 retired per-role wake-ups fleet-wide (28 specs migrated + scheduler stopped reading role-scope dirs). This CRUD surface is dead weight — worse, it's a silent-fail failure mode where a user adds a wake-up in `RoleModal`'s tab and the scheduler never fires it. Deleting it closes the loop and ships the shape's "two paths in, one truth out" philosophy.

Output: Substantial deletions across 4 source files + 4 test-file removals + 2 surgical test edits. Zero new code. Everything referenced in D-09..D-13 comes out; nothing else moves. Post-plan, `npm run build:backend && npm run build` clean and every scoped vitest touched exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-wake-ups-redesign-campaign-shape-2-crud-api-fleet-wide-rest-/134-CONTEXT.md
@.planning/phases/134-wake-ups-redesign-campaign-shape-2-crud-api-fleet-wide-rest-/134-RESEARCH.md
@.planning/phases/134-wake-ups-redesign-campaign-shape-2-crud-api-fleet-wide-rest-/134-PATTERNS.md
@.planning/phases/134-wake-ups-redesign-campaign-shape-2-crud-api-fleet-wide-rest-/134-01-SUMMARY.md

# Source files whose bytes come out (referenced per task via <read_first>)
@src/backend/claude-session/identity-artifact-reader.ts
@src/backend/claude-session/claude-session-server.ts
@src/ui/api/claude-session-api.ts
@src/ui/features/pretty-view/RoleModal.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Backend — delete 6 role-wakeup service functions from identity-artifact-reader.ts + audit companion helper orphans (D-10)</name>
  <files>src/backend/claude-session/identity-artifact-reader.ts</files>
  <read_first>
    - src/backend/claude-session/identity-artifact-reader.ts  (targeted line ranges per PATTERNS.md § "Deletion targets" table: L1558-~1676 readRoleWakeups, ~L4042-4173 readRoleWakeupsByName, L2098-~2168 writeRoleWakeupUpdate, L2215-2296 writeRoleWakeupCreate, L2309-~2345 writeRoleWakeupDelete, ~L4174-end writeRoleWakeupByName)
    - .planning/phases/134-wake-ups-redesign-campaign-shape-2-crud-api-fleet-wide-rest-/134-PATTERNS.md § "identity-artifact-reader.ts (MODIFIED — delete 6 functions per D-10)"
    - .planning/phases/134-wake-ups-redesign-campaign-shape-2-crud-api-fleet-wide-rest-/134-CONTEXT.md § D-10
  </read_first>
  <behavior>
    - After this task, the 6 functions named in D-10 no longer exist as exported (or private) symbols in the file: `readRoleWakeups`, `readRoleWakeupsByName`, `writeRoleWakeupUpdate`, `writeRoleWakeupCreate`, `writeRoleWakeupDelete`, `writeRoleWakeupByName`.
    - Every per-identity wake-up function still exists and works: `readIdentityWakeups`, `writeIdentityWakeupCreate`, `writeIdentityWakeupUpdate`, `writeIdentityWakeupDelete`, plus helpers `humanizeWakeupSchedule`, `writeMarkdownFileAtomic`, `IDENTITY_SLUG_RE`, `isLocalHostId`, `normalizeWakeupSlug`, `validateWakeupSpec`, `getLocalWakeupsRoot` (added in Plan 134-01).
    - Companion helpers `resolveRoleForIdentity` (L400) and `validateWakeupSpec` (L2190) stay — their per-identity consumers still need them. `normalizeWakeupSlug` stays (Plan 134-01 exported it).
    - No orphan imports: if any inner-file import (e.g. `import type { ... }` block) referenced only role-wakeup types, prune it.
    - The pass may temporarily leave unused symbols. A second pass uses tsc / vitest to flag them; delete or leave with a rationale comment per case (planner defers to executor's judgment).
  </behavior>
  <action>
    Delete the six named functions per D-10 by name (not by strict line number — the ranges in PATTERNS.md are the current-tree hint, not authoritative). Use Grep to locate each `function readRoleWakeups(`, `function readRoleWakeupsByName(`, `function writeRoleWakeupUpdate(`, `function writeRoleWakeupCreate(`, `function writeRoleWakeupDelete(`, `function writeRoleWakeupByName(` (and their `export` variants). Delete each definition body-through-closing-brace inclusive, plus any adjacent `//` header comments dedicated to that function. Do NOT touch any identity-wakeup function. Do NOT touch `humanizeWakeupSchedule`, `writeMarkdownFileAtomic`, `IDENTITY_SLUG_RE`, `isLocalHostId`, `normalizeWakeupSlug`, `validateWakeupSpec`, `getLocalWakeupsRoot`, `getLocalIdentitiesRoot`, `getLocalRolesRoot`, `resolveRoleForIdentity`. After deletions, run `npx tsc --noEmit -p tsconfig.node.json` (or `npm run build:backend`) to surface any dangling internal references — if a helper is now orphaned AND was only used by the deleted functions, delete it too (grep to prove zero remaining callers first). If any deleted function had a `type` alias or interface used only by it (e.g. `RoleWakeupResponse`), delete that too. Preserve the file's overall structure — no reformatting of untouched sections.
  </action>
  <verify>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c "function readRoleWakeups\|function readRoleWakeupsByName\|function writeRoleWakeupUpdate\|function writeRoleWakeupCreate\|function writeRoleWakeupDelete\|function writeRoleWakeupByName" src/backend/claude-session/identity-artifact-reader.ts</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c "function readIdentityWakeups\|function writeIdentityWakeupCreate\|function writeIdentityWakeupUpdate\|function writeIdentityWakeupDelete" src/backend/claude-session/identity-artifact-reader.ts</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c "^export function getLocalWakeupsRoot\|humanizeWakeupSchedule\|writeMarkdownFileAtomic\|IDENTITY_SLUG_RE\|isLocalHostId\|normalizeWakeupSlug" src/backend/claude-session/identity-artifact-reader.ts</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && npm run build:backend 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function readRoleWakeups\|function readRoleWakeupsByName\|function writeRoleWakeupUpdate\|function writeRoleWakeupCreate\|function writeRoleWakeupDelete\|function writeRoleWakeupByName" src/backend/claude-session/identity-artifact-reader.ts` outputs `0`.
    - `grep -c "function readIdentityWakeups" src/backend/claude-session/identity-artifact-reader.ts` >= 1 (per-identity survives).
    - `grep -c "function writeIdentityWakeupCreate\|function writeIdentityWakeupUpdate\|function writeIdentityWakeupDelete" src/backend/claude-session/identity-artifact-reader.ts` >= 3.
    - `grep -c "humanizeWakeupSchedule\|writeMarkdownFileAtomic\|IDENTITY_SLUG_RE\|isLocalHostId\|normalizeWakeupSlug" src/backend/claude-session/identity-artifact-reader.ts` >= 5 (all shared primitives survive).
    - `grep -c "^export function getLocalWakeupsRoot" src/backend/claude-session/identity-artifact-reader.ts` == 1 (Plan 134-01's addition still there).
    - `npm run build:backend` exits 0 (no dangling references).
    - Per-identity wake-up test still passes: `npx vitest related --run src/backend/claude-session/identity-artifact-reader.wakeup-crud.test.ts` exits 0.
  </acceptance_criteria>
  <done>Six role-wakeup functions deleted; per-identity paths + shared helpers intact; backend tsc clean; per-identity wake-up-crud test still green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Backend — delete 8 role-wakeup WS handlers + import trimming + wire-op JSDoc pruning in claude-session-server.ts + delete 3 wholesale role-wakeup test files (D-11 + D-13 backend half)</name>
  <files>src/backend/claude-session/claude-session-server.ts, src/backend/claude-session/identity-artifact-reader.role-wakeups.test.ts, src/backend/claude-session/claude-session-server.role-wakeups.test.ts, src/backend/claude-session/claude-session-server.role-wakeup-crud.test.ts</files>
  <read_first>
    - src/backend/claude-session/claude-session-server.ts  (imports L81-89 — 6 role-wakeup imports to remove; wire-op JSDoc L124-131 + L147-150 + L186-192 + L208-211; handler ranges per PATTERNS.md § "claude-session-server.ts" — L1625-1650 comment, L1795-1832 role:list-wakeups, L1833-1906 handleRoleWriteWakeup, L1918-1976 role:delete-wakeup, L1996-2003 Phase 72 comment, L2050-2108 identity:list-role-wakeups, L2117-2195 identity:update-role-wakeup, L2225-2260 identity:create-role-wakeup, L2299-2325 identity:delete-role-wakeup)
    - .planning/phases/134-wake-ups-redesign-campaign-shape-2-crud-api-fleet-wide-rest-/134-PATTERNS.md § "claude-session-server.ts (MODIFIED — delete 8 handlers per D-11)"
    - .planning/phases/134-wake-ups-redesign-campaign-shape-2-crud-api-fleet-wide-rest-/134-CONTEXT.md § D-11 + D-13
  </read_first>
  <behavior>
    - After this task, `claude-session-server.ts` no longer registers or handles any of these 8 WS wire ops: `identity:list-role-wakeups`, `identity:update-role-wakeup`, `identity:create-role-wakeup`, `identity:delete-role-wakeup`, `role:list-wakeups`, `role:create-wakeup`, `role:update-wakeup`, `role:delete-wakeup`.
    - The import block at L81-89 no longer names any of the 6 role-wakeup functions from `identity-artifact-reader.js` — the per-identity wake-up imports STAY.
    - Wire-op JSDoc blocks at L124-131, L147-150, L186-192, L208-211 lose the role-wakeup entries; per-identity entries stay.
    - The three per-role wake-up test files listed under `<files>` no longer exist on disk (`git status` shows deletions).
    - Per-identity wake-up handler code is untouched (identity:list-wakeups, identity:update-wakeup, identity:create-wakeup, identity:delete-wakeup — no `-role` in the wire-op name).
    - `identity-artifact-reader.wakeup-crud.test.ts` (per-identity, still live) is NOT deleted — verify via grep of `.role-wakeup` (with dot) vs `.wakeup-crud`.
  </behavior>
  <action>
    Grep for each wire-op string literal ("`identity:list-role-wakeups`", "`identity:update-role-wakeup`", "`identity:create-role-wakeup`", "`identity:delete-role-wakeup`", "`role:list-wakeups`", "`role:create-wakeup`", "`role:update-wakeup`", "`role:delete-wakeup`") to locate each handler block; delete each handler body from its opening `case` (or `if (msg.type === ...)`) through its trailing `break;` / block-close inclusive. Delete the surrounding comment blocks that document the retired quads (identified in PATTERNS.md at L124-131, L147-150, L186-192, L208-211, L1625-1650 comment, L1996-2003 comment). Trim the import at L81-89 to remove the 6 named role-wakeup functions (`readRoleWakeupsByName`, `readRoleWakeups`, `writeRoleWakeupUpdate`, `writeRoleWakeupCreate`, `writeRoleWakeupDelete`, `writeRoleWakeupByName`) — the per-identity imports STAY. Do NOT touch any `identity:list-wakeups`, `identity:update-wakeup`, `identity:create-wakeup`, `identity:delete-wakeup` handler (per-identity — not `-role`).

    Then delete these three test files wholesale (D-13 wholesale bucket, backend half): `src/backend/claude-session/identity-artifact-reader.role-wakeups.test.ts` (~445 lines), `src/backend/claude-session/claude-session-server.role-wakeups.test.ts` (~425 lines), `src/backend/claude-session/claude-session-server.role-wakeup-crud.test.ts` (~365 lines). Use `git rm` (or Bash `rm`) so the deletions are tracked.

    Do NOT delete `identity-artifact-reader.wakeup-crud.test.ts` — that's per-identity, still live per D-13.
  </action>
  <verify>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c "identity:list-role-wakeups\|identity:update-role-wakeup\|identity:create-role-wakeup\|identity:delete-role-wakeup" src/backend/claude-session/claude-session-server.ts</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c "\"role:list-wakeups\"\|\"role:create-wakeup\"\|\"role:update-wakeup\"\|\"role:delete-wakeup\"" src/backend/claude-session/claude-session-server.ts</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c "identity:list-wakeups\|identity:update-wakeup\|identity:create-wakeup\|identity:delete-wakeup" src/backend/claude-session/claude-session-server.ts</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c "readRoleWakeups\|readRoleWakeupsByName\|writeRoleWakeupUpdate\|writeRoleWakeupCreate\|writeRoleWakeupDelete\|writeRoleWakeupByName" src/backend/claude-session/claude-session-server.ts</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && ls src/backend/claude-session/identity-artifact-reader.role-wakeups.test.ts src/backend/claude-session/claude-session-server.role-wakeups.test.ts src/backend/claude-session/claude-session-server.role-wakeup-crud.test.ts 2>&1 | grep -c "No such file"</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && ls src/backend/claude-session/identity-artifact-reader.wakeup-crud.test.ts 2>&1 | tail -3</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && npm run build:backend 2>&1 | tail -30</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && npx vitest related --run src/backend/claude-session/claude-session-server.ts src/backend/claude-session/identity-artifact-reader.wakeup-crud.test.ts 2>&1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "identity:list-role-wakeups\|identity:update-role-wakeup\|identity:create-role-wakeup\|identity:delete-role-wakeup" src/backend/claude-session/claude-session-server.ts` outputs `0`.
    - `grep -c '"role:list-wakeups"\|"role:create-wakeup"\|"role:update-wakeup"\|"role:delete-wakeup"' src/backend/claude-session/claude-session-server.ts` outputs `0`.
    - `grep -c "identity:list-wakeups\|identity:update-wakeup\|identity:create-wakeup\|identity:delete-wakeup" src/backend/claude-session/claude-session-server.ts` >= 4 (the per-identity, non-`-role` handlers survive).
    - `grep -c "readRoleWakeups\|readRoleWakeupsByName\|writeRoleWakeupUpdate\|writeRoleWakeupCreate\|writeRoleWakeupDelete\|writeRoleWakeupByName" src/backend/claude-session/claude-session-server.ts` outputs `0` (import block trimmed).
    - `ls` of the 3 wholesale test files reports "No such file" for all 3 (count == 3).
    - `ls src/backend/claude-session/identity-artifact-reader.wakeup-crud.test.ts` succeeds (per-identity test file survives, D-13 explicit).
    - `npm run build:backend` exits 0.
    - Scoped vitest on `claude-session-server.ts` + `identity-artifact-reader.wakeup-crud.test.ts` exits 0.
  </acceptance_criteria>
  <done>WS handlers + imports + wire-op JSDoc entries removed; three wholesale test files gone; per-identity paths + test still green; backend build clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Frontend — delete 5 role-wakeup helpers + payload/event types from claude-session-api.ts + delete 1 wholesale role-wakeup-crud test file + surgical excision in role-reads.test.ts (D-12 + D-13 frontend half)</name>
  <files>src/ui/api/claude-session-api.ts, src/ui/api/claude-session-api.role-wakeup-crud.test.ts, src/ui/api/claude-session-api.role-reads.test.ts</files>
  <read_first>
    - src/ui/api/claude-session-api.ts  (deletion targets per PATTERNS.md: discriminated-union L413-416, JSDoc L710-713, payload/event types L738-791, L1296-1299 JSDoc, L1329-1373 Phase 90 Plan 90-07 payloads/events, helper functions L1499-1725; the STAYING symbol is WakeupSpecWire at L729)
    - src/ui/api/claude-session-api.role-reads.test.ts  (audit result: R1 getRoleFileByName describe at L57 STAYS; R3 listBountiesForRoleName describe at L115 STAYS; L179-241 `describe("listRoleWakeupsByName one-shot helper", ...)` block DELETES)
    - .planning/phases/134-wake-ups-redesign-campaign-shape-2-crud-api-fleet-wide-rest-/134-PATTERNS.md § "src/ui/api/claude-session-api.ts (MODIFIED — delete 5 helpers ...)" + "src/ui/api/claude-session-api.role-reads.test.ts (241 lines)"
    - .planning/phases/134-wake-ups-redesign-campaign-shape-2-crud-api-fleet-wide-rest-/134-CONTEXT.md § D-12 + D-13
  </read_first>
  <behavior>
    - `claude-session-api.ts` no longer exports (nor internally references) the 5 helpers: `listRoleWakeups` (older identity-key variant, if present), `listRoleWakeupsByName`, `createRoleWakeupByName`, `updateRoleWakeupByName`, `deleteRoleWakeupByName`.
    - The following payload/event types no longer exist: `IdentityListRoleWakeupsPayload`, `IdentityRoleWakeupsEvent`, `IdentityUpdateRoleWakeupPayload`, `IdentityRoleWakeupUpdatedEvent`, `IdentityCreateRoleWakeupPayload`, `IdentityRoleWakeupCreatedEvent`, `IdentityDeleteRoleWakeupPayload`, `IdentityRoleWakeupDeletedEvent`, `RoleListWakeupsPayload`, `RoleWakeupsLoadedEvent`, `RoleCreateWakeupPayload`, `RoleWakeupCreatedEvent`, `RoleUpdateWakeupPayload`, `RoleWakeupUpdatedEvent`, `RoleDeleteWakeupPayload`, `RoleWakeupDeletedEvent`.
    - The discriminated union at L413-416 no longer names those 4 role-wakeup event members.
    - The JSDoc comment blocks at L710-713, L1296-1299 lose their role-wakeup entries.
    - `WakeupSpecWire` (~L729) STAYS — still used by per-identity + upcoming global wakeup client code.
    - Per-identity wake-up helpers, payload types, event types are UNTOUCHED.
    - The wholesale file `src/ui/api/claude-session-api.role-wakeup-crud.test.ts` (~268 lines) is deleted.
    - `src/ui/api/claude-session-api.role-reads.test.ts` keeps the R1 (`getRoleFileByName`) and R3 (`listBountiesForRoleName`) `describe` blocks; only the L179-241 `describe("listRoleWakeupsByName one-shot helper", ...)` block is removed.
    - After edits: file length delta for role-reads.test.ts is approximately -60 to -65 lines (241 → ~180).
  </behavior>
  <action>
    In `src/ui/api/claude-session-api.ts`: delete the 5 helpers by name (grep-locate each `export function listRoleWakeups`, `export function listRoleWakeupsByName`, `export function createRoleWakeupByName`, `export function updateRoleWakeupByName`, `export function deleteRoleWakeupByName`; delete each definition body-through-closing-brace inclusive, plus any adjacent header docstring dedicated to that helper). Delete every payload/event type named in <behavior>. Remove the 4 role-wakeup members from the discriminated union at ~L413-416. Prune the JSDoc comment blocks at ~L710-713 and ~L1296-1299 of their role-wakeup entries only (keep any per-identity entries in the same block). Verify `WakeupSpecWire` (~L729) still exports. Also audit the file-level docstring near L194-195 (per PATTERNS.md note "JSDoc header comment L194-195 — has stale references"); prune stale mentions of the removed helpers.

    Delete `src/ui/api/claude-session-api.role-wakeup-crud.test.ts` wholesale (git rm or Bash rm).

    Edit `src/ui/api/claude-session-api.role-reads.test.ts`: locate the `describe("listRoleWakeupsByName one-shot helper"` block (~L179 or wherever grep finds it in the current tree). Delete from that line through the closing `});` of that describe block inclusive. Preserve every other describe block (R1 `getRoleFileByName`, R3 `listBountiesForRoleName`). If the file's header docstring mentions R5/R6 (`listRoleWakeupsByName`), prune those bullets — the file's remaining coverage is R1 + R3 only.
  </action>
  <verify>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c "export function listRoleWakeups\|export function listRoleWakeupsByName\|export function createRoleWakeupByName\|export function updateRoleWakeupByName\|export function deleteRoleWakeupByName" src/ui/api/claude-session-api.ts</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c "RoleWakeupsLoadedEvent\|RoleWakeupCreatedEvent\|RoleWakeupUpdatedEvent\|RoleWakeupDeletedEvent\|IdentityRoleWakeupsEvent\|IdentityRoleWakeupCreatedEvent\|IdentityRoleWakeupUpdatedEvent\|IdentityRoleWakeupDeletedEvent" src/ui/api/claude-session-api.ts</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c "WakeupSpecWire" src/ui/api/claude-session-api.ts</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && ls src/ui/api/claude-session-api.role-wakeup-crud.test.ts 2>&1 | grep -c "No such file"</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c "listRoleWakeupsByName one-shot helper" src/ui/api/claude-session-api.role-reads.test.ts</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c "getRoleFileByName one-shot helper\|listBountiesForRoleName one-shot helper" src/ui/api/claude-session-api.role-reads.test.ts</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && npm run build 2>&1 | tail -30</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && npx vitest related --run src/ui/api/claude-session-api.ts src/ui/api/claude-session-api.role-reads.test.ts 2>&1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function listRoleWakeups\|export function listRoleWakeupsByName\|export function createRoleWakeupByName\|export function updateRoleWakeupByName\|export function deleteRoleWakeupByName" src/ui/api/claude-session-api.ts` outputs `0`.
    - `grep -c "RoleWakeupsLoadedEvent\|RoleWakeupCreatedEvent\|RoleWakeupUpdatedEvent\|RoleWakeupDeletedEvent\|IdentityRoleWakeupsEvent\|IdentityRoleWakeupCreatedEvent\|IdentityRoleWakeupUpdatedEvent\|IdentityRoleWakeupDeletedEvent" src/ui/api/claude-session-api.ts` outputs `0`.
    - `grep -c "WakeupSpecWire" src/ui/api/claude-session-api.ts` >= 1 (STAYS).
    - `ls src/ui/api/claude-session-api.role-wakeup-crud.test.ts 2>&1 | grep -c "No such file"` outputs `1`.
    - `grep -c "listRoleWakeupsByName one-shot helper" src/ui/api/claude-session-api.role-reads.test.ts` outputs `0` (block removed).
    - `grep -c "getRoleFileByName one-shot helper\|listBountiesForRoleName one-shot helper" src/ui/api/claude-session-api.role-reads.test.ts` >= 2 (R1 + R3 preserved).
    - `npm run build` exits 0.
    - Scoped vitest on `claude-session-api.ts` + `claude-session-api.role-reads.test.ts` exits 0.
  </acceptance_criteria>
  <done>Frontend API helpers + types + JSDoc entries removed; wholesale role-wakeup-crud test file gone; role-reads.test.ts surgically trimmed to R1+R3; WakeupSpecWire + per-identity paths intact; frontend build clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Frontend — remove role-wakeups tab from RoleModal.tsx + surgical excision in PrettyView.role-modal-swap.test.tsx (D-09 + D-13 UI half)</name>
  <files>src/ui/features/pretty-view/RoleModal.tsx, src/ui/features/pretty-view/PrettyView.role-modal-swap.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/RoleModal.tsx  (deletion targets per PATTERNS.md: L46 `AlarmClock` import, L64-67 `listRoleWakeupsByName`/create/update/delete imports, L74 `WakeupsTab` import, L89 NAV_SECTIONS entry `role-wakeups`, L244-246 `roleWakeupsState`, L282 reset line, L307-321 fetch block, L328-390 callbacks block + L328-339 comment, L656-669 `<TabsContent value="role-wakeups">` block; type imports `Wakeup` L69 / `WakeupSpecWire` L68 to audit after other deletions)
    - src/ui/features/pretty-view/WakeupsTab.tsx  (READ but DO NOT MODIFY — this component STAYS per D-09; verify IdentityModal is the surviving consumer)
    - src/ui/features/pretty-view/IdentityModal.tsx  (verify its `import WakeupsTab from "./WakeupsTab"` stays — grep, don't edit)
    - src/ui/features/pretty-view/PrettyView.role-modal-swap.test.tsx  (L82-85 — 4 `vi.mock` stub lines for `listRoleWakeupsByName`/create/update/delete — DELETE these 4 lines; L261+ swap-coordination test bodies STAY)
    - .planning/phases/134-wake-ups-redesign-campaign-shape-2-crud-api-fleet-wide-rest-/134-PATTERNS.md § "RoleModal.tsx" + "PrettyView.role-modal-swap.test.tsx"
    - .planning/phases/134-wake-ups-redesign-campaign-shape-2-crud-api-fleet-wide-rest-/134-CONTEXT.md § D-09 + D-13
  </read_first>
  <behavior>
    - `RoleModal.tsx` no longer imports `AlarmClock`, `WakeupsTab`, or any of `listRoleWakeupsByName`/`createRoleWakeupByName`/`updateRoleWakeupByName`/`deleteRoleWakeupByName`.
    - `NAV_SECTIONS` array in `RoleModal.tsx` has exactly 3 entries (role, runbooks, bounties); the `role-wakeups` entry is gone.
    - No `roleWakeupsState` variable, no `listRoleWakeupsByName` fetch effect, no `updateRoleWakeup`/`createRoleWakeup`/`deleteRoleWakeup` callback definitions, and no `<TabsContent value="role-wakeups">` block remain.
    - `type Wakeup` / `type WakeupSpecWire` imports: audit after deletions — if any remaining `RoleModal.tsx` code references them, keep; else remove. Executor's discretion; document choice in commit.
    - IdentityModal.tsx's `import WakeupsTab from "./WakeupsTab"` is UNTOUCHED — verified via post-task grep.
    - `WakeupsTab.tsx` itself is byte-identical before/after (git diff empty for that file).
    - `PrettyView.role-modal-swap.test.tsx` keeps its swap-coordination test bodies (L261+); only the 4 `vi.mock` stub lines at L82-85 (`listRoleWakeupsByName: vi.fn()...`, `createRoleWakeupByName: vi.fn()...`, `updateRoleWakeupByName: vi.fn()...`, `deleteRoleWakeupByName: vi.fn()...`) are deleted. File length delta ~ -4 lines.
    - Scoped vitest on `PrettyView.role-modal-swap.test.tsx` still passes.
  </behavior>
  <action>
    In `src/ui/features/pretty-view/RoleModal.tsx`: locate each deletion target by grepping for the exact string. Delete: (a) the `AlarmClock` symbol from the `lucide-react` import line (remove the identifier only; keep the other lucide-react imports); (b) the 4 role-wakeup helpers from the `@/api/claude-session-api` import; (c) the `WakeupsTab` import line; (d) the `{ value: "role-wakeups", label: "Wakeups", Icon: AlarmClock }` NAV_SECTIONS entry; (e) the `roleWakeupsState` state declaration (`const [roleWakeupsState, setRoleWakeupsState] = useState<TabState<Wakeup[]>>({ status: "loading" });`); (f) the `setRoleWakeupsState({ status: "loading" })` reset line in the mount effect; (g) the entire `void (async () => { ... listRoleWakeupsByName ... })()` fetch block; (h) the entire `updateRoleWakeup` / `createRoleWakeup` / `deleteRoleWakeup` callback block plus its preceding comment; (i) the entire `<TabsContent value="role-wakeups">...</TabsContent>` JSX block. After deletions, audit `type Wakeup` and `type WakeupSpecWire` imports — if no remaining references in RoleModal.tsx, remove them; else keep and note in commit.

    Do NOT modify `src/ui/features/pretty-view/WakeupsTab.tsx`. Do NOT modify `src/ui/features/pretty-view/IdentityModal.tsx`. Do NOT modify any other Wakeup consumer.

    In `src/ui/features/pretty-view/PrettyView.role-modal-swap.test.tsx`: locate the `vi.mock("@/api/claude-session-api", ...)` block around L72-90. Delete the 4 lines mocking `listRoleWakeupsByName`, `createRoleWakeupByName`, `updateRoleWakeupByName`, `deleteRoleWakeupByName`. Do NOT touch any test body starting from L261. Do NOT touch any other `vi.mock` stub.
  </action>
  <verify>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c "AlarmClock\|WakeupsTab\|listRoleWakeupsByName\|createRoleWakeupByName\|updateRoleWakeupByName\|deleteRoleWakeupByName" src/ui/features/pretty-view/RoleModal.tsx</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c "value: \"role-wakeups\"" src/ui/features/pretty-view/RoleModal.tsx</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c "roleWakeupsState" src/ui/features/pretty-view/RoleModal.tsx</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c "TabsContent value=\"role-wakeups\"" src/ui/features/pretty-view/RoleModal.tsx</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c "value: \"runbooks\"\|value: \"bounties\"" src/ui/features/pretty-view/RoleModal.tsx</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c "import WakeupsTab" src/ui/features/pretty-view/IdentityModal.tsx</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && git diff --stat src/ui/features/pretty-view/WakeupsTab.tsx</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && grep -c "listRoleWakeupsByName:\|createRoleWakeupByName:\|updateRoleWakeupByName:\|deleteRoleWakeupByName:" src/ui/features/pretty-view/PrettyView.role-modal-swap.test.tsx</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && npm run build 2>&1 | tail -30</automated>
    <automated>cd /home/ubuntu/fleet/identities/vector/workspace/skynet && npx vitest related --run src/ui/features/pretty-view/RoleModal.tsx src/ui/features/pretty-view/PrettyView.role-modal-swap.test.tsx src/ui/features/pretty-view/WakeupsTab.tsx 2>&1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AlarmClock\|WakeupsTab\|listRoleWakeupsByName\|createRoleWakeupByName\|updateRoleWakeupByName\|deleteRoleWakeupByName" src/ui/features/pretty-view/RoleModal.tsx` outputs `0`.
    - `grep -c 'value: "role-wakeups"' src/ui/features/pretty-view/RoleModal.tsx` outputs `0`.
    - `grep -c "roleWakeupsState" src/ui/features/pretty-view/RoleModal.tsx` outputs `0`.
    - `grep -c 'TabsContent value="role-wakeups"' src/ui/features/pretty-view/RoleModal.tsx` outputs `0`.
    - `grep -c 'value: "runbooks"\|value: "bounties"' src/ui/features/pretty-view/RoleModal.tsx` >= 2 (other NAV_SECTIONS entries survive).
    - `grep -c "import WakeupsTab" src/ui/features/pretty-view/IdentityModal.tsx` >= 1 (per-identity mount stays — D-09).
    - `git diff --stat src/ui/features/pretty-view/WakeupsTab.tsx` reports 0 changes (byte-identical).
    - `grep -c "listRoleWakeupsByName:\|createRoleWakeupByName:\|updateRoleWakeupByName:\|deleteRoleWakeupByName:" src/ui/features/pretty-view/PrettyView.role-modal-swap.test.tsx` outputs `0` (mock stub lines gone).
    - `npm run build` exits 0.
    - Scoped vitest on RoleModal.tsx + PrettyView.role-modal-swap.test.tsx + WakeupsTab.tsx exits 0.
  </acceptance_criteria>
  <done>RoleModal's role-wakeups tab + state + effects + callbacks + imports removed; WakeupsTab.tsx byte-identical; IdentityModal's per-identity mount preserved; test-file swap-coordination bodies preserved; frontend build clean; scoped tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A — this plan is deletion-only | Every trust boundary in the removed surface goes away with the code. No new attack surface is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-02-01 | Availability (silent-fail) | Stale browser tab still on OLD build sends `role:list-wakeups` after deploy; gets no response | accept | Per Assumption A1 in RESEARCH.md: worst case is RoleModal's tab spins forever until refresh. No data loss. The user's usage pattern is hard-refresh after deploy. Documented and accepted. |
| T-134-02-02 | Correctness | Deleting a helper that turns out to still be used from an untracked call site | mitigate | Every deletion is grep-verified before removal (see acceptance criteria: post-deletion grep of the symbol outputs 0). `npm run build:backend && npm run build` gates catch any missed reference (backend tsc strict per Pitfall #7 + frontend tsc). |
| T-134-02-03 | Correctness | Accidental deletion of per-identity wake-up handler alongside per-role deletion (they share the file) | mitigate | Every task's acceptance criteria include a POSITIVE grep for the per-identity survivors: `identity:list-wakeups`, `identity:update-wakeup`, `identity:create-wakeup`, `identity:delete-wakeup` (no `-role`). `identity-artifact-reader.wakeup-crud.test.ts` MUST still pass scoped vitest. |
| T-134-02-04 | Correctness | Test-file wholesale deletion inadvertently removes non-wakeup coverage (Pitfall #6) | mitigate | Only the 3 backend + 1 frontend files whose EVERY test targets a to-be-deleted symbol are deleted wholesale. The 2 audit files (`role-reads.test.ts` + `PrettyView.role-modal-swap.test.tsx`) are surgically edited per PATTERNS.md line-level audit. Task acceptance criteria assert the surviving test blocks (R1 + R3, swap-coordination bodies) still grep-present. |
| T-134-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. No slopcheck required. |
</threat_model>

<verification>
- Every acceptance criterion in each task's `<acceptance_criteria>` block must pass.
- After all four tasks land, run: `npx vitest related --run src/backend/claude-session/identity-artifact-reader.ts src/backend/claude-session/claude-session-server.ts src/ui/api/claude-session-api.ts src/ui/features/pretty-view/RoleModal.tsx src/ui/features/pretty-view/WakeupsTab.tsx src/ui/features/pretty-view/IdentityModal.tsx` — MUST exit 0.
- `npm run build:backend && npm run build` MUST exit 0 (Pitfall #7 discipline).
- Banned-strings gate: `grep -rin '\bashley\b' src/ | grep -v -E '(the user|test-context|// user|/\* user)'` returns nothing.
- No route reachable end-to-end: pending orchestrator's full test suite + docker build + docker compose up (out of this plan's remit per box-maintainer directive).
</verification>

<success_criteria>
- Six role-wakeup functions removed from `identity-artifact-reader.ts`; per-identity paths + shared helpers intact.
- Eight role-wakeup WS handlers + wire-op JSDoc entries + 6 imports removed from `claude-session-server.ts`.
- Five role-wakeup helpers + all associated payload/event types removed from `claude-session-api.ts`; `WakeupSpecWire` STAYS.
- `RoleModal.tsx`: role-wakeups tab + state + effects + callbacks + imports removed; other tabs untouched.
- `WakeupsTab.tsx` byte-identical (still used by IdentityModal).
- 4 test files deleted wholesale: `identity-artifact-reader.role-wakeups.test.ts`, `claude-session-server.role-wakeups.test.ts`, `claude-session-server.role-wakeup-crud.test.ts`, `claude-session-api.role-wakeup-crud.test.ts`.
- 2 test files surgically trimmed: `claude-session-api.role-reads.test.ts` (R5+R6 block removed), `PrettyView.role-modal-swap.test.tsx` (4 vi.mock stub lines removed).
- `identity-artifact-reader.wakeup-crud.test.ts` (per-identity) UNTOUCHED and still passing.
- Scoped vitest green across every touched file.
- `npm run build:backend && npm run build` clean.
</success_criteria>

<output>
Create `.planning/phases/134-wake-ups-redesign-campaign-shape-2-crud-api-fleet-wide-rest-/134-02-SUMMARY.md` when done. Include: files touched (deletions + surgical edits + wholesale removals), diff line counts per file, scoped test results, backend + frontend build results, and confirmation that the phase is orchestrator-ready for the full-suite + docker-build + docker-compose-up-force-recreate + playwright-smoke deploy motion (per D-18/D-19, orchestrator-scope).
</output>
