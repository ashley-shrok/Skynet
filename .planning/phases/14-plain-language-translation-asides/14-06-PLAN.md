---
phase: 14-plain-language-translation-asides
plan: 06
type: execute
wave: 6
depends_on: [14-05]
files_modified: []
autonomous: false
requirements: [ASIDE-01, ASIDE-02, ASIDE-03, ASIDE-04, ASIDE-05, ASIDE-06, ASIDE-07, ASIDE-08, ASIDE-09, ASIDE-10, ASIDE-11]
tags: [deploy, checkpoint, uat, patches-md, bundled-deploy]

must_haves:
  truths:
    - "npm run build succeeds — production bundle is clean and includes the aside subsystem changes"
    - "npx tsc --noEmit exits 0 across the entire codebase"
    - "npx vitest run passes (or shows only pre-existing / documented failures — no regression from the pre-Phase-14 baseline)"
    - "The 14-UAT-CHECKLIST.md file exists and walks Ashley through ASIDE-01..11 post-deploy verification steps for both the trigger + render (mobile + desktop) and the cross-tab dismiss coherence"
    - "The 14-PATCHES-MD-ENTRY.md draft is ready to paste at PIN time — includes the aside subsystem AND the bundled deploy sequence with queued #150 A + C per CONTEXT.md § Phase Boundary"
    - "The deploy sequence bundling this phase's patches with the pending #150 A + C is documented — Ashley 2026-07-26 verbatim: 'there's no point in deploying until we get it in'"
    - "The deploy itself is user-gated per fleet DEPLOY DISCIPLINE (15-min deadman rollback timer mandatory per CLAUDE.md § Deploy safety) — this checkpoint stages the deploy, Ashley greenlights it"
  artifacts:
    - path: ".planning/phases/14-plain-language-translation-asides/14-UAT-CHECKLIST.md"
      provides: "Ashley post-deploy walkthrough covering ASIDE-01..11"
      contains: "ASIDE-01"
      contains_2: "ASIDE-11"
    - path: ".planning/phases/14-plain-language-translation-asides/14-PATCHES-MD-ENTRY.md"
      provides: "Draft entry for the skynet-patches.md repo-level changelog; ready to paste at PIN time"
      contains: "Phase 14"
      contains_2: "aside"
    - path: ".planning/phases/14-plain-language-translation-asides/14-BUILD-VERIFY-LOG.md"
      provides: "Captured output of npm run build + npx tsc --noEmit + npx vitest run — proves the pre-deploy build is clean"
      contains: "tsc --noEmit"
      contains_2: "vitest"
      contains_3: "build"
  key_links:
    - from: "14-06 build verification"
      to: "the queued Ashley-greenlight bundled deploy"
      via: "documented deploy sequence including #150 A + C on top of the aside subsystem"
      pattern: "bundled\\s+deploy"
    - from: "14-UAT-CHECKLIST.md"
      to: "each of the 11 ASIDE requirements"
      via: "one checklist item per requirement, with explicit test steps + expected observation"
      pattern: "ASIDE-\\d\\d"
---

<objective>
Deploy checkpoint plan for Phase 14. Runs the pre-deploy build verification (npm run build, npx tsc --noEmit, npx vitest run) and captures output. Authors the 14-UAT-CHECKLIST.md (Ashley's post-deploy walkthrough) and the 14-PATCHES-MD-ENTRY.md (draft for the repo-level skynet-patches.md changelog, ready to paste at PIN time). Presents Ashley with the bundled-deploy proposition (Phase 14 + queued #150 A + C per CONTEXT.md § Phase Boundary) as a blocking human-verify checkpoint — the actual `docker compose up -d --force-recreate skynet` runs behind Ashley's greenlight AND the mandatory 15-min deadman rollback timer per fleet DEPLOY DISCIPLINE.

Purpose: The bundled deploy is unavoidable — CONTEXT.md § Phase Boundary says #150 A + C is queued and the full aside feature must land BEFORE #150 A + C ships (Ashley: "there's no point in deploying until we get it in"). This checkpoint gathers the pre-deploy artifacts (UAT checklist, patches-md draft, build verification log), stages them for Ashley's review, and blocks on human greenlight for the actual deploy. Fleet-safety (blast radius = every managed box) means this cannot be auto-advanced.

Output: Three new markdown files in the phase directory + one blocking human-verify checkpoint. Zero source-code changes. Zero test changes. This plan is 100% verification, documentation, and gating — the work of Phase 14 was Waves 1-5; Wave 6 ships it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-plain-language-translation-asides/14-CONTEXT.md
@.planning/phases/14-plain-language-translation-asides/14-PATTERNS.md
@.planning/phases/14-plain-language-translation-asides/14-01-SUMMARY.md
@.planning/phases/14-plain-language-translation-asides/14-02-SUMMARY.md
@.planning/phases/14-plain-language-translation-asides/14-03-SUMMARY.md
@.planning/phases/14-plain-language-translation-asides/14-04-SUMMARY.md
@.planning/phases/14-plain-language-translation-asides/14-05-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<!--
  Executor line-number discipline (per plan-checker W6):
  This is Wave 6. Waves 1-5 have all touched multiple source files, shifting
  line numbers throughout. Any cited line number in the tasks below (or in
  PATTERNS.md excerpts they reference) is a POST-Wave-0 approximation only.
  Executor MUST grep-locate the target region rather than jumping to a cited
  line number. Grep for the surrounding identifier (function name, tag, string
  literal) that the referenced line is inside — the identifier is stable across
  waves; the line number is not.
-->

<task type="auto">
  <name>Task 1: Run build verification and capture output</name>
  <files>.planning/phases/14-plain-language-translation-asides/14-BUILD-VERIFY-LOG.md</files>
  <read_first>
    - .planning/phases/14-plain-language-translation-asides/14-05-SUMMARY.md (confirm the integration tests all passed before running full-build verification)
    - CLAUDE.md § Deploy safety (15-min deadman rollback requirement)
    - CLAUDE.md § Nginx caveat (both nginx.conf files need matching location blocks — Phase 14 adds NO new backend routes so this does NOT apply; note it in the log)
  </read_first>
  <action>
    (1) Run `npx tsc --noEmit` from /home/ubuntu/skynet and capture full output.

    (2) Run `npx vitest run` from /home/ubuntu/skynet and capture the summary (Test Files N passed, Tests N passed lines + any failing test names).

    (3) Run `npm run build` from /home/ubuntu/skynet and capture the Vite build summary (bundle sizes, warnings, errors).

    (4) Verify no new backend routes were introduced (grep-search claude-session-server.ts diff for new `app.get`/`app.post`/`app.use` routes — Phase 14 only added WS handlers on the existing bridge; note this explicitly since CLAUDE.md § Nginx caveat only applies to HTTP routes).

    (5) Create `.planning/phases/14-plain-language-translation-asides/14-BUILD-VERIFY-LOG.md` with sections:
        - `## npx tsc --noEmit` — captured output (exit code + any messages)
        - `## npx vitest run` — captured summary + any failing tests noted
        - `## npm run build` — captured build summary + bundle size delta from pre-Phase-14 (approximate — do not need byte-precise)
        - `## Nginx caveat check` — explicit "N/A — Phase 14 adds no HTTP routes, only new WS event types on the existing port 30011 bridge (per CONTEXT.md non-negotiable no-new-port constraint)"
        - `## Deploy safety` — reminder that the deploy must run behind the 15-min deadman rollback timer per CLAUDE.md § Deploy safety

    If ANY of tsc / vitest / build fails, the log records the failure verbatim AND Task 3's human-verify checkpoint stops the deploy — the executor MUST NOT proceed to Task 4's deploy step.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; test -f .planning/phases/14-plain-language-translation-asides/14-BUILD-VERIFY-LOG.md &amp;&amp; grep -q "tsc --noEmit" .planning/phases/14-plain-language-translation-asides/14-BUILD-VERIFY-LOG.md &amp;&amp; grep -q "vitest" .planning/phases/14-plain-language-translation-asides/14-BUILD-VERIFY-LOG.md &amp;&amp; grep -q "npm run build\|Vite" .planning/phases/14-plain-language-translation-asides/14-BUILD-VERIFY-LOG.md &amp;&amp; grep -q "Nginx caveat" .planning/phases/14-plain-language-translation-asides/14-BUILD-VERIFY-LOG.md &amp;&amp; grep -q "Deploy safety\|15-min deadman" .planning/phases/14-plain-language-translation-asides/14-BUILD-VERIFY-LOG.md</automated>
  </verify>
  <done>
    - 14-BUILD-VERIFY-LOG.md exists with all four sections (tsc, vitest, build, nginx caveat, deploy safety).
    - Build verification output captured verbatim — no summarization that loses failure signals.
    - If any of tsc / vitest / build failed, the failure is documented and Task 3 will surface it as a deploy-blocker.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author 14-UAT-CHECKLIST.md and 14-PATCHES-MD-ENTRY.md</name>
  <files>.planning/phases/14-plain-language-translation-asides/14-UAT-CHECKLIST.md, .planning/phases/14-plain-language-translation-asides/14-PATCHES-MD-ENTRY.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md § "Plain-Language Translation Asides (Phase 14)" (the ASIDE-01..11 requirements — one UAT checklist item per requirement)
    - .planning/phases/14-plain-language-translation-asides/14-CONTEXT.md (the LOCKED user decisions Ashley made 2026-07-26 — the UAT checklist must verify these are all honored)
    - Prior phase UAT checklists for structure reference — grep for `10-UAT-CHECKLIST.md`, `09-UAT-CHECKLIST.md`, or similar in .planning/phases/ (whichever most-recent UAT file exists — mirror its structure)
    - Prior phase patches-md-entry drafts — grep for `PATCHES-MD-ENTRY` in .planning/phases/ for the format Ashley expects
    - The bounty tracker at ~/.claude/identities/tina/bounties/plain-language-translation-asides/bounty.json (if accessible — mention in the patches-md entry so the bounty tracker knows to close)
  </read_first>
  <action>
    (1) Create `.planning/phases/14-plain-language-translation-asides/14-UAT-CHECKLIST.md` with 11 checklist items (one per ASIDE requirement), each with:
        - The requirement ID + verbatim text from REQUIREMENTS.md.
        - Explicit test steps Ashley walks through in a live Skynet instance on her fleet.
        - The expected observation that would satisfy the requirement.
        - Additional cross-cutting checks: (a) mobile viewport UAT for the render + morph + dismiss round-trip, (b) desktop viewport UAT, (c) cross-tab coherence test (open two browser tabs on the same session, verify dismiss in one clears the other), (d) tab-close / re-attach test (aside displayed, close tab, reopen same session, verify aside re-renders per ASIDE-09), (e) v1 overlap policy test (fire two turns in quick succession with an aside displayed after the first — verify the second turn does NOT get its own aside per ASIDE-08).

        Structure the file with clear H2 headers per section, checkboxes on each verifiable step (`- [ ]`), and a top-of-file note reminding Ashley of the deploy safety rules (15-min deadman timer is running).

    (2) Create `.planning/phases/14-plain-language-translation-asides/14-PATCHES-MD-ENTRY.md` — a draft entry for skynet-patches.md following the format of prior patches. Structure:
        - Patch number placeholder (`#XXX — Plain-Language Translation Asides + queued #150 A + C bundle` — the actual number gets assigned at PIN time)
        - One-paragraph summary of the feature (mental model, not implementation detail — pattern-match prior patches-md entries).
        - Requirement coverage: ASIDE-01..11 (list all 11 IDs).
        - File change summary: AsideBubble.tsx (new), PrettyView.tsx (modified), ComposeBox.tsx (modified), claude-session-server.ts (modified), claude-session-api.ts (modified), plus test files.
        - Deploy notes: bundled with #150 A + C per CONTEXT.md § Phase Boundary; deployed behind the standard 15-min deadman rollback per fleet DEPLOY DISCIPLINE.
        - Bounty reference: `~/.claude/identities/tina/bounties/plain-language-translation-asides/bounty.json` closes on Ashley UAT sign-off.
        - Rebase risk: MEDIUM per ROADMAP.md § Phase 14 description ("purely additive on fork-local pretty-view + backend session-tail infrastructure; no upstream Skynet surfaces touched").

    Do NOT create the actual skynet-patches.md entry yet — this is a DRAFT. The PIN-time paste happens as a separate action (per fork discipline — patches-md is updated at deploy PIN, not at plan-authoring).

    Ensure both files are written to the correct paths inside `.planning/phases/14-plain-language-translation-asides/`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; test -f .planning/phases/14-plain-language-translation-asides/14-UAT-CHECKLIST.md &amp;&amp; test -f .planning/phases/14-plain-language-translation-asides/14-PATCHES-MD-ENTRY.md &amp;&amp; for i in 01 02 03 04 05 06 07 08 09 10 11; do grep -q "ASIDE-$i" .planning/phases/14-plain-language-translation-asides/14-UAT-CHECKLIST.md || (echo "missing ASIDE-$i"; exit 1); done &amp;&amp; grep -q "cross-tab" .planning/phases/14-plain-language-translation-asides/14-UAT-CHECKLIST.md &amp;&amp; grep -q "tab-close\|re-attach\|re-mount" .planning/phases/14-plain-language-translation-asides/14-UAT-CHECKLIST.md &amp;&amp; grep -q "mobile" .planning/phases/14-plain-language-translation-asides/14-UAT-CHECKLIST.md &amp;&amp; grep -q "desktop" .planning/phases/14-plain-language-translation-asides/14-UAT-CHECKLIST.md &amp;&amp; grep -q "15-min deadman\|deadman rollback" .planning/phases/14-plain-language-translation-asides/14-UAT-CHECKLIST.md &amp;&amp; grep -q "#150" .planning/phases/14-plain-language-translation-asides/14-PATCHES-MD-ENTRY.md &amp;&amp; grep -q "aside\|ASIDE" .planning/phases/14-plain-language-translation-asides/14-PATCHES-MD-ENTRY.md &amp;&amp; grep -q "bounty" .planning/phases/14-plain-language-translation-asides/14-PATCHES-MD-ENTRY.md</automated>
  </verify>
  <done>
    - 14-UAT-CHECKLIST.md exists with 11 requirement-mapped items + cross-tab test + tab-close test + mobile/desktop coverage + deadman-timer reminder.
    - 14-PATCHES-MD-ENTRY.md exists with patch summary + requirement list + file change summary + deploy notes (bundled with #150 A + C) + bounty reference + rebase-risk note.
    - Neither file duplicates content — the UAT is for Ashley post-deploy, the patches entry is for the repo changelog at PIN time.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Ashley human-verify checkpoint — greenlight bundled deploy (Phase 14 + #150 A + C)</name>
  <what-built>
    Phase 14 (Plain-Language Translation Asides) code-complete on `feat/tab-title-from-tmux`:
    - Backend: new WS event types (aside_ready, aside_dismissed) + client-to-backend payloads (aside_arm, aside_dismissed) added additively to the existing pretty-view WS bridge on port 30011 (per CONTEXT.md non-negotiable no-new-port). Module-scope asideState Map (per CONTEXT.md § Backend per-connection state LOCK 2026-07-26) holds per-WS {armed, displayed} — cross-tab dismiss broadcast atomically flips peer state alongside sending the dismiss frame. Extraction poller reads `tmux capture-pane -p -S -200` at 300ms cadence with two-consecutive-stable-poll debounce. Arm signal is frontend-driven: PrettyView's isIdle:false→true transition (gated on pvIdentity !== null) sends aside_arm to backend, which then calls injectBtw. Client dispatch for aside_dismissed forwards Escape into tmux + broadcasts cross-tab (with peer-state-flip). Connect-time pane-probe re-emits aside_ready to late-mounting clients independent of activeViewers.size.
    - Frontend: new AsideBubble component (10px solid border in hue + three-layer neon glow at 12/32/64px per your locked aesthetic from aside-visual-snippet.js). PrettyView mounts AsideBubble at message-stream bottom, handles aside_ready/aside_dismissed WS frames, emits aside_arm on isIdle:false→true transition (identity-gated on pvIdentity !== null — this IS the trigger source, per CONTEXT.md § Trigger LOCK 2026-07-26), and wires handleAsideDismiss to WS-send dismiss + optimistic clear. ComposeBox morphs on asideActive: send button becomes X (Resume, tooltip); paperclip / thumbs-up / queue / reset all disable; textarea remains editable with draft preserved.
    - Tests: 5 extractBtwAnswer unit tests (Wave 1), 5 AsideBubble unit tests (Wave 3), 5 PrettyView integration tests (Wave 5 — including new aside_arm emission test), 5 backend integration tests (Wave 5 — including cross-tab peer-state-flip verification per B3 lock) — total 20 automated tests specific to Phase 14.
    - Deploy bundle: Phase 14 patches will ship together with the queued #150 A + C (pruner fleet-aware + URL-restore multi-tab glow, per STATE.md 2026-07-26 last_activity) per CONTEXT.md § Phase Boundary Ashley-verbatim decision.

    All build verification captured in 14-BUILD-VERIFY-LOG.md. UAT checklist at 14-UAT-CHECKLIST.md. Patches-md draft at 14-PATCHES-MD-ENTRY.md.
  </what-built>
  <how-to-verify>
    (1) Review `.planning/phases/14-plain-language-translation-asides/14-BUILD-VERIFY-LOG.md` — confirm tsc, vitest, and npm run build are all clean. Any failure blocks deploy.

    (2) Review `.planning/phases/14-plain-language-translation-asides/14-UAT-CHECKLIST.md` — the post-deploy verification walkthrough Ashley will use once the deploy lands. If any step is unclear, flag it now (revision routes back to Task 2, not to the deploy).

    (3) Review `.planning/phases/14-plain-language-translation-asides/14-PATCHES-MD-ENTRY.md` — the draft that gets pasted at PIN time. Approve the wording or send back edits.

    (4) Confirm the deploy bundle scope:
        - Phase 14 patches (new AsideBubble + PrettyView / ComposeBox / claude-session-server / claude-session-api changes)
        - Queued #150 A (pruner fleet-aware) + #150 C (URL-restore multi-tab glow)
        - All three land in one `docker compose up -d --force-recreate skynet` event behind the mandatory 15-min deadman rollback per CLAUDE.md § Deploy safety.

    (5) If greenlight: type "deploy" or "ship it" or equivalent. If not ready (something else takes priority, or the UAT / patches draft needs revision): describe the blocker and route back accordingly.

    Deploy safety reminders:
    - 15-min deadman rollback timer is MANDATORY per Ashley 2026-07-03. Even at the keyboard, no exceptions.
    - Blast radius: a bad deploy loses access to the entire fleet (Skynet is the gateway). Do not skip the timer.
    - The deploy runs the executor's own `docker compose` command — Ashley greenlights, the executor runs, the 15-min timer is watched (executor OR Ashley).
  </how-to-verify>
  <resume-signal>Approve deploy ("deploy" / "ship it") OR describe revision needed (routes back to Task 1 or Task 2) OR defer ("hold off" — Phase 14 stays code-complete-pending-deploy in STATE.md).</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Update STATE.md and ROADMAP.md — mark Phase 14 complete-pending-deploy or complete</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/STATE.md (full file, focus on last_activity + Progress section)
    - .planning/ROADMAP.md (Phase 14 section — the checkbox and Plans list at ~L44)
  </read_first>
  <action>
    (1) Update `.planning/STATE.md`:
        - Bump `last_updated` to today.
        - Extend `last_activity` with a note like "2026-07-26 — Completed Phase 14 plan-and-execute cycle (plain-language translation asides): AsideBubble + PrettyView / ComposeBox / claude-session-server / claude-session-api changes complete on `feat/tab-title-from-tmux`. Bundled deploy with #150 A + C queued per CONTEXT.md § Phase Boundary; Ashley greenlight [status per Task 3's resolution]."
        - If Ashley approved the deploy in Task 3 AND the deploy shipped: mark Phase 14 as "Deployed" in the Progress table AND flip the ROADMAP checkbox per (2) below.
        - If Ashley "held off" on deploy (Task 3 resume signal = "hold off"): the phase is NOT yet complete — do NOT flip the ROADMAP checkbox, do NOT update STATE.md's status field. Just add a note to STATE.md last_activity like "Phase 14 code-complete on branch, deploy deferred per Ashley hold-off — pending greenlight". Phase stays in-flight until deploy actually happens.
        - If Ashley approved "code-complete-pending-deploy" explicitly (a distinct signal from hold-off — Ashley approves the code and documentation but the deploy is queued behind unrelated work): flip the ROADMAP checkbox with the "✓ code-complete-pending-deploy YYYY-MM-DD" suffix; DO update STATE.md status. The distinction: hold-off = "let's revisit later"; code-complete-pending-deploy = "this is done, just not shipped yet."
        - If Ashley requested revisions: revert this task (do not update STATE.md yet — return to Task 1/2/3 as directed).

    (2) Update `.planning/ROADMAP.md`:
        - Grep-locate the Phase 14 entry (do NOT trust "~L44" — the file may have been edited).
        - Flip the checkbox from `- [ ]` to `- [x]` ONLY IF (per (1)): Ashley approved AND the resume signal was "deploy" (shipped) OR "code-complete-pending-deploy" (explicit). If the resume signal was "hold off", do NOT flip the checkbox — the phase remains in-flight.
        - When flipping, add a completion-date suffix to the phase title matching prior completed phases (e.g. "✓ deployed 2026-07-26" or "✓ code-complete-pending-deploy 2026-07-26").

    (3) DO NOT update REQUIREMENTS.md's traceability table (that is a separate maintenance step; the requirements table for ASIDE-01..11 needs adding — flag this as a follow-up in the SUMMARY, do not do it in this task since it's outside the phase-directory scope).

    If Ashley requested revisions in Task 3, this task is SKIPPED and the executor returns to the appropriate prior task instead.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; grep -q "Phase 14" .planning/STATE.md &amp;&amp; grep -q "aside\|Phase 14\|plain-language" .planning/STATE.md &amp;&amp; grep -E "^\- \[x\].*Phase 14|^\- \[x\] \*\*Phase 14" .planning/ROADMAP.md</automated>
  </verify>
  <done>
    - STATE.md last_updated bumped, last_activity notes Phase 14 completion + deploy status per Task 3 resolution.
    - ROADMAP.md Phase 14 checkbox flipped to [x] with a completion-date suffix matching prior phases.
    - If Ashley requested revisions in Task 3: this task NOT performed; executor returned to appropriate task.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-deploy build to production deploy | verified artifacts move from build-clean local state to running fleet gateway |
| Ashley greenlight to actual deploy execution | human approval gates a fleet-blast-radius action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06-01 | Denial of Service | bad deploy takes down Skynet gateway | mitigate | Mandatory 15-min deadman rollback timer per CLAUDE.md § Deploy safety. Ashley's greenlight is required (Task 3 blocking human-verify). Bundled deploy is documented in patches-md draft so rollback semantics are clear. |
| T-14-06-02 | Tampering | patches-md entry mis-summarizes the changes | mitigate | The 14-PATCHES-MD-ENTRY.md draft is a DRAFT — Ashley reviews it in Task 3 before PIN. |
| T-14-06-03 | Repudiation | who approved the deploy | accept | Task 3 checkpoint response is captured in the executor's log. STATE.md last_activity records the deploy event. No formal audit trail beyond that — matches prior phase practice. |
| T-14-06-04 | Information Disclosure | UAT checklist reveals internal aside subsystem details to a public patch changelog reader | accept | The UAT checklist lives in .planning/phases/ which is not published to any public surface; skynet-patches.md itself is the public-facing artifact and the draft in 14-PATCHES-MD-ENTRY.md is Ashley-reviewed. |
| T-14-06-05 | Elevation of Privilege | executor deploys without Ashley greenlight | mitigate | Task 3 is `gate="blocking"` — the executor cannot proceed to Task 4 without an explicit resume signal per CLAUDE.md § GSD Workflow Enforcement. Any bypass would violate the fork's DEPLOY DISCIPLINE. |
| T-14-06-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new package installs. No package legitimacy audit required. |
</threat_model>

<verification>
- 14-BUILD-VERIFY-LOG.md exists with tsc + vitest + npm run build output.
- 14-UAT-CHECKLIST.md exists, walks all 11 ASIDE requirements, includes cross-tab + tab-close + mobile + desktop + deadman-timer sections.
- 14-PATCHES-MD-ENTRY.md exists, includes patch summary + requirement list + file changes + #150 bundle note + bounty reference.
- Task 3 human-verify checkpoint reaches Ashley (or the developer-owner if in a self-review context).
- STATE.md and ROADMAP.md updated only if Ashley approves in Task 3.
- No source-code changes in this plan — 100% documentation + verification + gating.
</verification>

<success_criteria>
- Phase 14 is code-complete and either deployed (Ashley greenlit + deploy landed) OR staged-with-Ashley-review-pending. Either state advances the phase to complete-in-planning-terms.
- ALL 11 ASIDE requirements (ASIDE-01..11) have automated coverage (Wave 5) AND manual UAT coverage (this task's checklist) AND source-code implementation (Waves 1-4).
- The bundled deploy with #150 A + C is explicitly documented and Ashley-visible per CONTEXT.md § Phase Boundary.
- Deploy safety rules preserved: mandatory 15-min deadman, blast-radius awareness, human-greenlight gate.
- The bounty at ~/.claude/identities/tina/bounties/plain-language-translation-asides/ can close on Ashley's UAT sign-off (referenced in the patches-md draft so the closure signal is documented).
</success_criteria>

<output>
Create `.planning/phases/14-plain-language-translation-asides/14-06-SUMMARY.md` when done — record: build verification pass/fail summary, UAT checklist item count, patches-md draft byte-count, Task 3 resume signal received from Ashley (verbatim or paraphrased with attribution), STATE.md + ROADMAP.md updates applied (or skipped due to revision request), and a follow-up note if REQUIREMENTS.md traceability table needs the ASIDE-01..11 rows added as a separate patch.
</output>
