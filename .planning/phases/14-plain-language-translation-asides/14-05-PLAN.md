---
phase: 14-plain-language-translation-asides
plan: 05
type: execute
wave: 5
depends_on: [14-04]
files_modified:
  - src/ui/features/pretty-view/PrettyView.test.tsx
  - src/backend/claude-session/claude-session-server.aside.integration.test.ts
  - src/backend/claude-session/claude-session-server.ts
autonomous: true
requirements: [ASIDE-01, ASIDE-02, ASIDE-05, ASIDE-06, ASIDE-07, ASIDE-08, ASIDE-09, ASIDE-11]
tags: [test, integration, aside, cross-tab, frontend-arm, state-coherence]

must_haves:
  truths:
    - "Frontend integration test proves that on PrettyView's isIdle:false→true transition with pvIdentity !== null, PrettyView WS-sends {type:'aside_arm'} on the pretty-view WS (per CONTEXT.md § Trigger LOCK)"
    - "Frontend integration test proves that a mocked WS emitting aside_ready mounts an AsideBubble at the message-stream bottom AND ComposeBox morphs to X (Resume) + aux buttons disabled + textarea remains editable"
    - "Frontend integration test proves that clicking X (Resume) fires a WS-outbound aside_dismissed frame AND optimistically clears the AsideBubble"
    - "Frontend integration test proves that inbound aside_dismissed WS frame is idempotent on already-cleared state"
    - "Frontend integration test proves that fresh-pane mount resets asideText to null"
    - "Backend integration test proves that the extraction poller arms via aside_arm client message (not via any internal export or execCommand-only mock) — this is the correct test seam per frontend-arm architecture"
    - "Backend integration test proves that extraction poller emits aside_ready ONLY after two consecutive stable captures containing ASIDE_END_MARKER"
    - "Backend integration test proves cross-tab broadcast: aside_dismissed from client A causes both a dismiss frame to reach client B AND asideState.get(clientB).displayed to flip to false (per CONTEXT.md § Backend per-connection state — the atomic BOTH-STEPS rule)"
    - "Backend integration test proves v1 overlap policy (ASIDE-08): a second aside_arm on the same WS while armed/displayed is a no-op (does NOT re-invoke injectBtw)"
    - "Backend integration test proves ASIDE-09 tab-close / re-attach: on connectToPane if capture-pane output contains ASIDE_END_MARKER, aside_ready is emitted to the mounting client ONCE (regardless of activeViewers.size — per W7)"
    - "asideState Map is exposed as a named export from claude-session-server.ts so tests can inspect peer-state-flip coherence (legitimate observation of the source-of-truth Map, not a test-only export)"
  artifacts:
    - path: "src/ui/features/pretty-view/PrettyView.test.tsx"
      provides: "aside_arm emission on isIdle transition, aside_ready mount + morph, X-click dismiss + WS-outbound frame, aside_dismissed cleanup, fresh-pane reset — 5 new test cases added to the existing suite"
      contains: "aside_arm"
      contains_2: "aside_ready"
      contains_3: "aside_dismissed"
      contains_4: "Resume"
    - path: "src/backend/claude-session/claude-session-server.aside.integration.test.ts"
      provides: "extraction poller stability (arm via aside_arm dispatch), cross-tab broadcast with peer-state-flip verification, v1 overlap policy, connect-time probe — 5 new test cases with mocked execCommand + mocked WebSocket clients driven via WS message dispatch"
      contains: "aside_arm"
      contains_2: "extractionPoller"
      contains_3: "peer state"
      contains_4: "overlap"
      contains_5: "reAttachProbe"
    - path: "src/backend/claude-session/claude-session-server.ts"
      provides: "MINIMAL modification: add `export` to the existing asideState Map declaration so tests can inspect it (or add a named-export `getAsideState()` accessor if planner prefers)"
      contains: "export const asideState"
  key_links:
    - from: "PrettyView.test.tsx WS mock"
      to: "existing WS-mock pattern already in the file — extend with aside_ready / aside_dismissed frames"
      via: "reuse existing dispatcher; add outbound-spy for aside_arm and aside_dismissed frames"
      pattern: "aside_ready|aside_arm"
    - from: "backend integration test"
      to: "mock WS message dispatch (mockWs.emit('message', JSON.stringify({type: 'aside_arm'})))"
      via: "drives the arm signal via the same code path production uses — no vi.mock(execCommand)-alone approach"
      pattern: "aside_arm"
    - from: "cross-tab state-coherence test"
      to: "asideState.get(peerWs).displayed inspection"
      via: "asideState is exposed as a named export from claude-session-server.ts — tests read directly from the source-of-truth Map"
      pattern: "asideState\\.get"
---

<objective>
Add integration tests proving the full aside round-trip end-to-end under the frontend-arm architecture (CONTEXT.md § Trigger LOCK 2026-07-26) and the module-scope-state model (CONTEXT.md § Backend per-connection state LOCK 2026-07-26). Frontend tests exercise PrettyView's arm emitter + AsideBubble mount + ComposeBox morph + X-click dismiss + fresh-pane reset. Backend tests drive arm via WS `aside_arm` dispatch (not internal-function exports), exercise poller stability, verify cross-tab broadcast performs the atomic BOTH-STEPS peer-state flip, verify v1 overlap policy, and verify the connect-time re-attach probe.

Purpose: Waves 1-4 landed primitives, WS wiring (with frontend-arm + module-scope state per plan-checker B1/B2/B3 fixes), render layer, morph. Wave 5 proves the pieces integrate under the locked architecture. Integration coverage is required before Wave 6's bundled deploy.

**Key change from pre-revision Wave 5:** the arm test seam is the WS message dispatch itself — `mockWs.emit('message', JSON.stringify({type: 'aside_arm'}))` drives the same code path production uses. This is a legitimate observation seam (the WS handler is the public API of the backend); the pre-revision "vi.mock(execCommand) alone" approach couldn't drive the arm because arming now REQUIRES a client message.

Output: Three files touched — PrettyView.test.tsx extended with 5 new cases, new backend integration test file with 5 cases, and a MINIMAL source-file change to claude-session-server.ts adding `export` to the asideState Map declaration so cross-tab state-coherence tests can inspect the source-of-truth Map.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-plain-language-translation-asides/14-CONTEXT.md
@.planning/phases/14-plain-language-translation-asides/14-PATTERNS.md
@.planning/phases/14-plain-language-translation-asides/14-01-SUMMARY.md
@.planning/phases/14-plain-language-translation-asides/14-02-SUMMARY.md
@.planning/phases/14-plain-language-translation-asides/14-03-SUMMARY.md
@.planning/phases/14-plain-language-translation-asides/14-04-SUMMARY.md

# Existing files this plan reads / extends
@src/ui/features/pretty-view/PrettyView.tsx
@src/ui/features/pretty-view/PrettyView.test.tsx
@src/ui/features/pretty-view/ComposeBox.tsx
@src/ui/features/pretty-view/AsideBubble.tsx
@src/backend/claude-session/claude-session-server.ts
@src/backend/ssh/tmux-helper.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose asideState as a named export from claude-session-server.ts (minimal source change for test observation)</name>
  <files>src/backend/claude-session/claude-session-server.ts</files>
  <read_first>
    - src/backend/claude-session/claude-session-server.ts (grep-locate `const asideState = new Map` from Wave 2 — do NOT trust cited line numbers, Wave 2 will have added significant content since Wave 1)
    - .planning/phases/14-plain-language-translation-asides/14-02-SUMMARY.md (approximate location Wave 2 placed asideState)
  </read_first>
  <behavior>
    - Add `export` keyword to the existing `const asideState = new Map<...>()` declaration Wave 2 created.
    - No other source change. asideState is now importable in test files: `import { asideState } from "../claude-session-server"` (or the equivalent path from the test file location).
    - This is NOT a test-only export — asideState IS the source-of-truth Map per CONTEXT.md § Backend per-connection state. Exposing it for tests is legitimate observation, same posture as exposing any other module-scope constant. Production code within the same file continues to reference `asideState` directly (unchanged); external observation is through the export.
    - Alternative planner discretion: instead of `export const asideState`, add a named-export getter `export function getAsideState(): ReadonlyMap<WebSocket, {armed: boolean; displayed: boolean}> { return asideState; }` — this exposes read-only access but hides the mutation surface. Both approaches are acceptable; the getter form is slightly more encapsulated but adds a helper. If planner picks the getter, tests import `getAsideState()` and call `getAsideState().get(peerWs)`.
    - Zero other source changes. Zero risk of breaking existing behavior.
  </behavior>
  <action>
    Edit src/backend/claude-session/claude-session-server.ts:

    Option A (simple export):
    - Locate the Wave 2-created line `const asideState = new Map<WebSocket, { armed: boolean; displayed: boolean }>();`
    - Prepend `export `: `export const asideState = new Map<...>();`

    Option B (encapsulated getter — planner discretion):
    - Keep `const asideState` as-is (no export).
    - Add `export function getAsideState(): ReadonlyMap<WebSocket, { armed: boolean; displayed: boolean }> { return asideState; }` immediately after the asideState declaration.

    Either option is verified by the grep in the verify command. Wave 5 Task 3 (backend integration tests) will use whichever export shape landed here — the test can import either `asideState` directly or `getAsideState()`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; (grep -q "export const asideState" src/backend/claude-session/claude-session-server.ts || grep -q "export function getAsideState\|export const getAsideState" src/backend/claude-session/claude-session-server.ts) &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>
    - Either `export const asideState = new Map<...>()` OR `export function getAsideState()` present in the source file.
    - `npx tsc --noEmit` exits 0.
    - No other source change (verify by inspecting the diff — should be a 1-line `export` addition, or a 3-line getter addition, and nothing else).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Frontend integration tests — 5 new cases in PrettyView.test.tsx</name>
  <files>src/ui/features/pretty-view/PrettyView.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/PrettyView.test.tsx (full file — locate existing WS-mock scaffolding; grep for `type:"message"` / `type: "message"` / `send(JSON.stringify` to find how frames are dispatched into the mocked WS)
    - src/ui/features/pretty-view/PrettyView.tsx (Wave 3-shipped: asideText state, WS-switch cases for aside_ready/aside_dismissed, AsideBubble mount, handleAsideDismiss callback, isIdle-transition arm emitter via useEffect + prevIsIdleRef)
    - src/ui/features/pretty-view/ComposeBox.tsx (Wave 4-shipped: X icon morph + aux disable extension)
    - src/ui/features/pretty-view/AsideBubble.tsx (Wave 3-shipped: role="note")
    - src/ui/features/pretty-view/AsideBubble.test.tsx (already exists from Wave 3 — do NOT duplicate; unit tests live there, integration tests live here)
    - .planning/phases/14-plain-language-translation-asides/14-CONTEXT.md § Trigger (aside_arm is the sole trigger source — frontend integration test drives isIdle transition and asserts WS-outbound aside_arm)
  </read_first>
  <behavior>
    - **Test A — isIdle:false→true transition + pvIdentity !== null emits `{type:"aside_arm"}` on the WS** (NEW test per frontend-arm architecture):
      1. Mount PrettyView with a fleet identity (pvIdentity resolves to non-null via mocked useSessionIdentity OR by providing a tmuxSession that the test fixture maps to a real identity).
      2. Spy on `wsRef.current.send` (or the mock WS instance's send method).
      3. Simulate the initial state where isIdle is false (agent is working).
      4. Re-render or props-change to trigger isIdle=true.
      5. Assert: the spy captured a `send` call with JSON payload matching `{"type":"aside_arm"}` exactly once.
      6. Trigger isIdle=false again → isIdle=true again → assert a SECOND `{type:"aside_arm"}` was sent (the transition fires per completed turn, not one-shot).
      7. Additional sub-case: set pvIdentity to null → trigger isIdle transition → assert NO aside_arm was sent (identity gate holds per CONTEXT.md § Trigger).

    - **Test B — aside_ready mounts AsideBubble + morphs ComposeBox:**
      1. Mount PrettyView, wait for status "streaming".
      2. Dispatch mock inbound WS frame: `{ type: "aside_ready", text: "the agent is refactoring the auth flow" }`.
      3. Assert:
         - `screen.getByRole("note")` succeeds (AsideBubble mounted).
         - `screen.getByText(/the agent is refactoring the auth flow/)` succeeds.
         - `screen.getByRole("button", { name: "Resume" })` succeeds (send button morphed).
         - `screen.queryByRole("button", { name: "Send" })` returns null.
         - At least one aux button has disabled attribute.
         - Textarea is NOT disabled.

    - **Test C — X-click dismiss fires WS-outbound aside_dismissed frame AND optimistically clears AsideBubble:**
      1. Continuing from Test B state (or fresh mount with aside displayed), spy on WS-outbound frames.
      2. Click "Resume" button via userEvent or fireEvent.
      3. Assert:
         - WS send spy captured a call with JSON matching `{"type":"aside_dismissed","hostId":<n>,"tmuxSession":"<s>"}`.
         - Immediately after click, `screen.queryByRole("note")` returns null (optimistic clear).
         - `screen.queryByRole("button", { name: "Send" })` succeeds (reverted to Send).
         - Textarea value unchanged (if the test types "hello" before displaying aside, "hello" still there after dismiss).

    - **Test D — inbound aside_dismissed WS frame is idempotent on already-cleared state:**
      1. Mount + display aside as in Test B.
      2. Dispatch inbound `{ type: "aside_dismissed" }` — assert AsideBubble removed, ComposeBox reverted.
      3. Dispatch SECOND inbound `{ type: "aside_dismissed" }` — assert no crash, no state change.

    - **Test E — fresh-pane mount resets asideText to null:**
      1. Mount PrettyView with paneKey = "pane-A" + identity/host/session.
      2. Dispatch aside_ready → AsideBubble mounts.
      3. Re-render with paneKey = "pane-B" (or trigger the paneKey-change effect the fresh-pane reset watches).
      4. Assert `screen.queryByRole("note")` returns null AFTER the pane change AND BEFORE any WS frame from the new pane arrives.
  </behavior>
  <action>
    Edit src/ui/features/pretty-view/PrettyView.test.tsx to add 5 new integration test cases per behavior spec.

    (1) Add cases inside the existing describe block OR a new nested `describe("Phase 14 aside integration", () => { ... })`. Use existing async-testing utilities (waitFor, act, userEvent) — do NOT introduce new tooling.

    (2) WS mock reuse: grep for the existing pattern that fires a `{type:"message"}` inbound frame — extend that same call path with aside frames. If the file uses a mock WebSocket class, add aside_ready / aside_dismissed / aside_arm to any allowed-message-types list.

    (3) WS-outbound spy: if the file has a pattern for tracking client-to-server frames, reuse it. Else use `vi.spyOn` on the mock WebSocket's send method, or extend the mock class to record sent frames in a captured array.

    (4) For Test A, if the test harness cannot directly set isIdle to trigger the useEffect, use rerender() with a new isIdle prop value. For pvIdentity gating, either mock useSessionIdentity via vi.mock (if the test file already does this — grep to confirm) or provide a tmuxSession that the test setup maps to a real identity fixture.

    (5) Assertions prefer semantic queries (`getByRole("button", { name: "Resume" })`) over class-string introspection — robust to Wave 3/4's exact className choices.

    (6) Each test ends with clean state. If existing file uses `afterEach(cleanup)`, don't duplicate.

    Do NOT modify pre-existing tests. Do NOT modify source files (PrettyView.tsx / ComposeBox.tsx / AsideBubble.tsx). Do NOT introduce new npm dependencies. If the test harness needs a capability it lacks, extend it minimally within the test file itself.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; grep -c '"aside_arm"' src/ui/features/pretty-view/PrettyView.test.tsx | awk '{if ($1 &gt;= 2) exit 0; else exit 1}' &amp;&amp; grep -c '"aside_ready"' src/ui/features/pretty-view/PrettyView.test.tsx | awk '{if ($1 &gt;= 3) exit 0; else exit 1}' &amp;&amp; grep -c '"aside_dismissed"' src/ui/features/pretty-view/PrettyView.test.tsx | awk '{if ($1 &gt;= 3) exit 0; else exit 1}' &amp;&amp; grep -q 'name: "Resume"' src/ui/features/pretty-view/PrettyView.test.tsx &amp;&amp; grep -q 'role.*"note"' src/ui/features/pretty-view/PrettyView.test.tsx &amp;&amp; npx vitest run src/ui/features/pretty-view/PrettyView.test.tsx 2&gt;&amp;1 | tee /tmp/14-05-t2.log | tail -8 &amp;&amp; grep -qE '(Test Files\s+1 passed|passed)' /tmp/14-05-t2.log</automated>
  </verify>
  <done>
    - PrettyView.test.tsx has 5 new integration test cases covering: aside_arm emission on isIdle transition (+ identity gate negative case), aside_ready mount+morph, X-click dismiss+outbound frame+optimistic clear, inbound aside_dismissed idempotency, fresh-pane reset.
    - All new tests pass.
    - No pre-existing test modified.
    - No source-file changes.
    - `npx tsc --noEmit` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Backend integration tests — new claude-session-server.aside.integration.test.ts driven via WS dispatch</name>
  <files>src/backend/claude-session/claude-session-server.aside.integration.test.ts</files>
  <read_first>
    - src/backend/claude-session/claude-session-server.ts (Wave 1 + Wave 2 + Wave 5 Task 1 changes: extractBtwAnswer, injectBtw, sendEscapeToBtw, activeViewers Map, asideState Map (now exported OR accessible via getAsideState), extraction poller, aside_arm + aside_dismissed client dispatch, connect-time probe)
    - src/backend/claude-session/claude-session-server.aside.test.ts (if it exists from Wave 1 — planner discretion whether extraction unit tests coexist there or in this new file)
    - src/backend/ssh/tmux-helper.ts (execCommand signature — the mock target)
    - .planning/phases/14-plain-language-translation-asides/14-02-SUMMARY.md (approximate line ranges of Wave 2 additions for reference)
    - Existing backend test files in src/backend/ if any, for vitest + vi.mock + mock-WebSocket patterns this codebase uses
  </read_first>
  <behavior>
    - **Test seam architecture (per plan-checker feedback):**
      - Arm signal is driven via mock WS message dispatch: `mockWs.emit('message', JSON.stringify({type: 'aside_arm'}))`. This exercises the SAME code path production uses — no vi.mock(execCommand)-alone approach that couldn't drive the arm.
      - execCommand is mocked via `vi.mock('../ssh/tmux-helper', ...)` to control capture-pane outputs across polls.
      - asideState Map is inspected directly via the export (Task 1 above).

    - **Test A — arm via aside_arm dispatch + extraction poller stability:**
      1. Mock two things: `execCommand` (routed by command string: capture-pane returns controlled outputs; send-keys returns success) AND a fake WebSocket for the client.
      2. Simulate the WS lifecycle: connect + connectToPane discovery success.
      3. Dispatch `aside_arm` message: `mockWs.emit('message', JSON.stringify({type: 'aside_arm'}))`.
      4. Assert: execCommand was called with a tmux send-keys command containing BTW_PROMPT (or a substring proving inject fired).
      5. Assert: `asideState.get(mockWs)?.armed === true`.
      6. Advance fake timers to trigger extraction polls. Sequence: [Poll 1: no marker] → [Poll 2: marker present, output "streaming X"] → [Poll 3: marker present, output "streaming XY"] → [Poll 4: same as poll 3, stable].
      7. Assert: `aside_ready` is emitted ONCE (mockWs.send was called with `{"type":"aside_ready",...}`), on poll 4 (the stable one). NOT on poll 2 or 3.
      8. Assert: `asideState.get(mockWs)?.armed === false` after emit (disarmed) AND `.displayed === true`.

    - **Test B — cross-tab broadcast with peer-state-flip verification (validates CONTEXT.md § Backend per-connection state atomic BOTH-STEPS rule):**
      1. Mock two fake WSes: `mockWsA` and `mockWsB`, both authenticated to the same `{hostId=42, tmuxSession="tina@main"}` via connectToPane.
      2. Verify both are in `activeViewers.get("42::tina@main")` AND both have entries in asideState.
      3. Set both peers' `displayed: true` (simulate a prior aside_ready).
      4. Dispatch `aside_dismissed` from mockWsA: `mockWsA.emit('message', JSON.stringify({type: 'aside_dismissed', hostId: 42, tmuxSession: 'tina@main'}))`.
      5. Assert: mockWsB.send was called with `{"type":"aside_dismissed"}` (cross-tab frame reached peer).
      6. **Critical assertion (per B3 lock):** `asideState.get(mockWsB)?.displayed === false` — the peer's state flag flipped. This is the atomic BOTH-STEPS rule under test — without the state flip, ASIDE-08 silently breaks across tabs.
      7. Also assert: `asideState.get(mockWsA)?.displayed === false` (the initiator IS in its own activeViewers set, so its state also flipped via the same broadcast primitive).
      8. Assert: execCommand was called with the Escape send-keys pattern.

    - **Test C — v1 overlap policy (ASIDE-08): second aside_arm while armed is a no-op:**
      1. Mock one WS, mock execCommand.
      2. Dispatch aside_arm. Assert: execCommand called once with send-keys /btw.
      3. Immediately (before any aside_ready emit) dispatch aside_arm AGAIN.
      4. Assert: execCommand was NOT called a second time with send-keys /btw (still only one /btw invocation total). asideState.armed stayed true; the overlap-ignore gate held.
      5. Additional sub-case: set asideState.get(mockWs).displayed = true, armed = false. Dispatch aside_arm. Assert: NO new inject (displayed=true also gates).

    - **Test D — connect-time probe emits aside_ready to a late-mounting client independent of activeViewers.size (per W7):**
      1. Mock execCommand to return capture-pane output containing ASIDE_END_MARKER and a coherent /btw answer.
      2. Trigger connectToPane on a mock WS with no other viewers in activeViewers yet (this is the LATE-MOUNTING scenario — activeViewers.size is 0 → 1 for this session).
      3. Assert: after discovery success, mockWs.send was called with `{"type":"aside_ready","text":"..."}`.
      4. Assert: `asideState.get(mockWs)?.displayed === true`.
      5. Assert: NO injectBtw call happened during the probe (read-only mount probe — does NOT re-inject).

    - **Test E — marker-disappearance broadcasts aside_dismissed with peer-state-flip:**
      1. Two mock WSes authenticated to same session, both with displayed=true.
      2. Mock execCommand's capture-pane to return marker-present output for first poll, then marker-absent output for next poll.
      3. Advance fake timers to trigger 2+ polls.
      4. Assert: BOTH mockWsA.send AND mockWsB.send were called with `{"type":"aside_dismissed"}`.
      5. Assert: `asideState.get(mockWsA).displayed === false` AND `asideState.get(mockWsB).displayed === false`.
      6. This proves marker-disappearance uses the same broadcastAsideDismissed primitive as client-initiated dismiss — cross-tab state stays coherent regardless of dismiss origin.
  </behavior>
  <action>
    Create `src/backend/claude-session/claude-session-server.aside.integration.test.ts` with the 5 test cases per behavior spec.

    (1) Imports: `import { describe, it, expect, vi, beforeEach, afterEach } from "vitest"`. Import asideState (or getAsideState) from Task 1's export. Import extractBtwAnswer / injectBtw / sendEscapeToBtw / BTW_PROMPT / ASIDE_END_MARKER / ASIDE_POLL_INTERVAL_MS from claude-session-server if they're already exported (Wave 1 may not have exported them — planner discretion whether to add exports here, redeclare test-side constants matching Wave 1 values, or leave as internal).

    (2) Mock strategy:
        - `vi.mock('../ssh/tmux-helper', () => ({ execCommand: vi.fn() }))`. In each test use `vi.mocked(execCommand).mockImplementation((_conn, cmd) => ...)` to route based on the command string (capture-pane returns controlled outputs; send-keys returns success).
        - Mock WebSocket: create a minimal class or use EventEmitter to simulate `.emit('message', jsonString)` + `.send = vi.fn()` + `.readyState = 1` + `.on = vi.fn()`. The tests need send + readyState + emit + on.

    (3) Test structure: `describe("claude-session-server aside subsystem (frontend-arm architecture)", () => { it("Test A — arm via aside_arm + poller stability", ...); it("Test B — cross-tab broadcast with peer-state-flip", ...); it("Test C — v1 overlap policy", ...); it("Test D — connect-time probe independent of activeViewers.size", ...); it("Test E — marker-disappearance broadcasts + peer-state-flip", ...); })`.

    (4) For fake-timer polling: `vi.useFakeTimers()` in beforeEach and `vi.advanceTimersByTime(ASIDE_POLL_INTERVAL_MS)` (or the numeric literal 300 if the constant isn't exported). Restore real timers in afterEach.

    (5) For each test: reset vi.mocked(execCommand), create fresh mock WSes, clear asideState + activeViewers between tests (via afterEach that iterates and deletes entries, OR just create new WS instances so old entries garbage-collect out).

    (6) If the backend's WS-connection lifecycle is hard to drive from tests (e.g. connectToPane is not directly callable), the test may need to reach into the wss instance's connection-handler via wss.emit('connection', mockWs) or similar. Planner discretion on shape — the goal is to drive the same code path production uses. Consult 14-02-SUMMARY.md's notes on where each new block landed.

    Do NOT modify claude-session-server.ts beyond Task 1's export addition. Do NOT modify tmux-helper.ts. Do NOT add test-only source exports beyond asideState (which is a legitimate source-of-truth observation per CONTEXT.md § Backend per-connection state). If a test cannot pass because Wave 2 did not expose a needed seam, note the finding in this task's SUMMARY and use a fallback (driving via WS-emit is usually sufficient).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; test -f src/backend/claude-session/claude-session-server.aside.integration.test.ts &amp;&amp; grep -q "aside_arm" src/backend/claude-session/claude-session-server.aside.integration.test.ts &amp;&amp; grep -q "cross-tab\|peer.state\|peer-state" src/backend/claude-session/claude-session-server.aside.integration.test.ts &amp;&amp; grep -q "overlap\|ASIDE-08\|no-op" src/backend/claude-session/claude-session-server.aside.integration.test.ts &amp;&amp; grep -q "probe\|reAttach\|ASIDE-09" src/backend/claude-session/claude-session-server.aside.integration.test.ts &amp;&amp; grep -q "marker.disappear\|marker-absent\|disappearance" src/backend/claude-session/claude-session-server.aside.integration.test.ts &amp;&amp; grep -q "asideState" src/backend/claude-session/claude-session-server.aside.integration.test.ts &amp;&amp; npx vitest run src/backend/claude-session/claude-session-server.aside.integration.test.ts 2&gt;&amp;1 | tee /tmp/14-05-t3.log | tail -8 &amp;&amp; grep -qE '(5 passed|Tests\s+5\s+passed|passed\s+\(5\))' /tmp/14-05-t3.log</automated>
  </verify>
  <done>
    - `src/backend/claude-session/claude-session-server.aside.integration.test.ts` exists with 5 test cases (arm-via-aside_arm+poller stability, cross-tab broadcast+peer-state-flip verification, overlap policy, connect-time probe, marker-disappearance).
    - All 5 tests pass.
    - Arm is driven via WS message dispatch (aside_arm) — NOT via internal-function exports.
    - Cross-tab test explicitly asserts BOTH-STEPS from CONTEXT.md § Backend per-connection state: dismiss frame reaches peer AND asideState.get(peer).displayed flips to false.
    - Tests use mocked execCommand + mocked WebSocket instances; no real SSH, no real network, no real setInterval (fake timers).
    - No source-file changes beyond Task 1's export addition.
    - `npx tsc --noEmit` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new — this plan adds tests (Tasks 2-3) + a one-line source export (Task 1). Tests run in vitest sandbox.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05-01 | Tampering | mocked WebSocket assertions | mitigate | Tests assert on JSON-parsed frame shapes, not raw string equality — no injection surface. |
| T-14-05-02 | Denial of Service | test timeouts on fake-timer polling | mitigate | vi.advanceTimersByTime is synchronous; no wall-clock waits. Bounded to N poll advances per test. |
| T-14-05-03 | Information Disclosure | asideState export widens observation surface | accept | asideState is a Map of WebSocket references — WebSocket instances are only usable by code holding them. External code that could import asideState from claude-session-server would already have full-file access via the same import path. No new leak surface. |
| T-14-05-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new package installs. vitest + @testing-library/react already in devDeps. |
</threat_model>

<verification>
- 5 new PrettyView test cases exist and pass (including aside_arm emission test — the NEW test proving the frontend-arm architecture).
- 5 new backend integration test cases exist and pass in a new file — arm driven via WS dispatch, cross-tab test asserts BOTH-STEPS peer-state flip.
- asideState is exported (or accessible via getAsideState) from claude-session-server.ts — minimal source change.
- `npx tsc --noEmit` exits 0.
- Full existing test suite passes (or shows no regression from pre-Wave-5 baseline).
</verification>

<success_criteria>
- 10 new automated integration tests provide Nyquist-rule coverage for ASIDE-01 (arm emission from PrettyView + backend receipt via aside_arm dispatch), ASIDE-02 (identity gate in Test A sub-case), ASIDE-05 (aside_ready mount), ASIDE-06 (ComposeBox morph assertion), ASIDE-07 (X-click WS-outbound + optimistic clear), ASIDE-08 (overlap policy — both armed and displayed gate variants), ASIDE-09 (connect-time probe independent of activeViewers.size), ASIDE-11 (cross-tab broadcast WITH peer-state-flip verification — the atomic BOTH-STEPS rule from CONTEXT.md § Backend per-connection state).
- The suite exercises the code shipped by Waves 1-4 with only one MINIMAL source addition (Task 1's asideState export).
- Frontend-arm architecture is END-TO-END tested: Test 2A drives isIdle transition → asserts WS-outbound aside_arm; Test 3A receives aside_arm via mock WS emit → asserts execCommand /btw fired → asserts asideState.armed flipped.
- Cross-tab state coherence is EXPLICITLY tested (Test 3B + 3E) — validates the plan-checker B3 lock is not just declared but observed under test.
- Wave 6 deploy checkpoint safe to proceed.
</success_criteria>

<output>
Create `.planning/phases/14-plain-language-translation-asides/14-05-SUMMARY.md` when done — record: the 5 PrettyView test titles + pass count, the 5 backend integration test titles + pass count, the vitest output for both suites, whether Task 1 chose export-const or getter form, and a note on any test seam that turned out to need extra exposure that this task grep-verified (or added as a follow-up).
</output>
