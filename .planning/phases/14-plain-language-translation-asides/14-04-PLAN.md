---
phase: 14-plain-language-translation-asides
plan: 04
type: execute
wave: 4
depends_on: [14-03]
files_modified:
  - src/ui/features/pretty-view/ComposeBox.tsx
autonomous: true
requirements: [ASIDE-06, ASIDE-07]
tags: [frontend, pretty-view, composebox, aside, morph, body-only]

must_haves:
  truths:
    - "ComposeBoxProps interface ALREADY has asideActive?: boolean + onAsideDismiss?: () => void (added in Wave 3 Task 2 per plan-checker W3 fix) — this wave consumes them in the body, does NOT re-declare"
    - "When asideActive === true: the inside-textarea send button icon morphs from paper-plane to X icon, aria-label becomes 'Resume', title becomes 'Resume', onClick calls onAsideDismiss instead of handleSend"
    - "When asideActive === true: paperclip / interrupt (if wired) / thumbs-up / queue / reset buttons all become disabled (compound predicate `canSend === false || asideActive === true`)"
    - "When asideActive === true: textarea remains editable — the textarea's disabled predicate is NOT extended with asideActive (per CONTEXT.md § ComposeBox morph: 'Textarea remains editable. Any partial draft text is preserved verbatim')"
    - "When asideActive === false / undefined: ComposeBox behaves EXACTLY as it did before Phase 14 — zero behavior change, zero visual change"
    - "The X icon color uses the identity-hue for visual distinction from send (Ashley: 'Style change to visually distinguish from send' per CONTEXT.md § ComposeBox morph)"
    - "Send button hover tooltip changes from 'Send' to 'Resume' when asideActive"
    - "Partial draft text survives an aside-display window and dismiss round-trip via patch #57's existing autosave path (L36-49 of ComposeBox); this wave introduces no interference"
  artifacts:
    - path: "src/ui/features/pretty-view/ComposeBox.tsx"
      provides: "X icon morph on Send button + aux button disable extension via `|| asideActive === true`"
      contains: "asideActive"
      contains_2: "Resume"
      contains_3: "X className"
      contains_4: 'asideActive === true'
  key_links:
    - from: "inside-textarea Send button (around L1422-1454 — grep-verify)"
      to: "conditional-branch morph on asideActive"
      via: "single button element whose icon / aria-label / title / onClick / disabled / className branches on the prop"
      pattern: "asideActive\\s*\\?"
    - from: "aux button disabled predicates (paperclip, interrupt, thumbs-up, queue, reset)"
      to: "compound predicate extending existing disable logic"
      via: "add `|| asideActive === true` to each existing disabled predicate"
      pattern: "asideActive === true"
---

<objective>
Consume the `asideActive` + `onAsideDismiss` props that Wave 3 Task 2 added to `ComposeBoxProps`. Morph the ComposeBox body: (a) inside-textarea Send button becomes X (Resume) with identity-hue color when asideActive; (b) aux buttons (paperclip / interrupt / thumbs-up / queue / reset) extend their `disabled` predicate with `|| asideActive === true`; (c) textarea stays editable (NOT extended).

Purpose: This is the pure body-consumption wave for the ComposeBox morph. Per plan-checker W3 fix + Wave 3 Task 2, the ComposeBoxProps interface already contains the props — Wave 4's scope is body-level only. Advances ASIDE-06 (morph while aside displayed) and ASIDE-07 (X-click routes to PrettyView's handleAsideDismiss which fires WS dismiss).

Output: Body-only edits to `src/ui/features/pretty-view/ComposeBox.tsx`. No interface changes, no new files, no new tests (integration coverage in Wave 5).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-plain-language-translation-asides/14-CONTEXT.md
@.planning/phases/14-plain-language-translation-asides/14-PATTERNS.md
@.planning/phases/14-plain-language-translation-asides/14-03-PLAN.md
@.planning/phases/14-plain-language-translation-asides/14-03-SUMMARY.md

# Existing files this plan modifies
@src/ui/features/pretty-view/ComposeBox.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend aux button disabled predicates (paperclip / interrupt / thumbs-up / queue / reset)</name>
  <files>src/ui/features/pretty-view/ComposeBox.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/ComposeBox.tsx (Wave 3 Task 2 just modified this file adding `asideActive?: boolean` + `onAsideDismiss?: () => void` to ComposeBoxProps — verify by grep. Target line ranges for aux buttons per PATTERNS.md L176-183: L998 reset, L1150 paperclip, L1180 interrupt (if wired), L1208 thumbs-up, L1239 queue. Grep-verify current positions — file has been modified in Wave 3 so line numbers likely shifted. Search for `disabled={canSend === false` to enumerate the buttons that need extending, or search for known button labels like `aria-label="Send"`, `Paperclip`, `ThumbsUp`, `RotateCcw`.)
    - .planning/phases/14-plain-language-translation-asides/14-PATTERNS.md L176-184 (aux-button disable extension pattern)
    - .planning/phases/14-plain-language-translation-asides/14-CONTEXT.md § ComposeBox morph (semantics: which affordances disable, textarea stays editable)
  </read_first>
  <behavior>
    - Destructure `asideActive` (and `onAsideDismiss`, needed for Task 2) from props at the function-body top OR access via `props.asideActive` — match the file's existing convention for prop access.
    - For each of the 5 (or 4 if interrupt isn't wired) aux buttons — paperclip, interrupt, thumbs-up, queue, reset — locate the existing `disabled={...}` prop and extend the predicate:
      - Current form: `disabled={canSend === false}` (or compound like `disabled={canSend === false || queueDisabled}`)
      - New form: `disabled={canSend === false || asideActive === true}` (or `disabled={canSend === false || queueDisabled || asideActive === true}`)
    - Use `asideActive === true` (explicit equality) not bare `asideActive` — undefined props reduce correctly AND grep-ability is preserved.
    - **Textarea disabled predicate is NOT extended.** Per CONTEXT.md § ComposeBox morph verbatim: "Textarea remains editable. Any partial draft text is preserved verbatim." The existing `disabled={queueArmed}` (or similar) at the Textarea stays as-is. Adding `|| asideActive` here would violate the lock.
    - **This task does NOT touch the Send button** — Task 2 handles that morph.
    - Zero-regression when asideActive is undefined: `canSend === false || undefined === true` reduces to `canSend === false`, preserving pre-existing semantics.
  </behavior>
  <action>
    Edit src/ui/features/pretty-view/ComposeBox.tsx per PATTERNS.md L176-184:

    (1) Destructure `asideActive` and `onAsideDismiss` from props at the function-body top (or `const asideActive = props.asideActive;` etc. — match file convention). Wave 3 Task 2 already added them to the interface; this task starts consuming them.

    (2) Grep-enumerate every button whose `disabled` prop is `disabled={canSend === false}` or compound with canSend. For each, extend with `|| asideActive === true`. Expected buttons per PATTERNS.md L176-183:
        - paperclip button (around former L1150)
        - interrupt button (around former L1180 if wired — some renderings hide interrupt entirely; verify via grep)
        - thumbs-up button (around former L1208)
        - queue button (around former L1239)
        - reset button (around former L998, inside the meter's leftmost cell)

    (3) Do NOT extend the Textarea's `disabled` predicate. Do NOT touch the Send button. Do NOT add the lucide-react `X` import yet (Task 2 does that).

    Rationale for explicit `asideActive === true`: (a) undefined-safe, (b) grep-friendly for the negative-grep in Wave 3 Task 2 that confirmed the body was untouched pre-Wave-4, (c) matches the file's existing explicit-equality convention on `canSend === false`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; grep -c "asideActive === true" src/ui/features/pretty-view/ComposeBox.tsx | awk '{if ($1 &gt;= 4) exit 0; else exit 1}' &amp;&amp; ! grep -E "disabled=\{[^}]*queueArmed[^}]*asideActive" src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>
    - At least 4 aux buttons have `asideActive === true` added to their existing disabled predicate (grep count >= 4).
    - Textarea disabled predicate NOT extended with asideActive (negative-grep confirms no line has both `queueArmed` AND `asideActive` inside a disabled expression).
    - `npx tsc --noEmit` exits 0. Existing ComposeBox.test.tsx cases all pass unchanged (compound predicate reduces to pre-existing behavior when asideActive is undefined).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Morph the inside-textarea Send button to X (Resume) when asideActive</name>
  <files>src/ui/features/pretty-view/ComposeBox.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/ComposeBox.tsx (grep-locate the inside-textarea Send button — PATTERNS.md cited "L1422-1454" but Wave 3 Task 2's interface addition + Wave 4 Task 1's aux button edits will have shifted line numbers. Search for `aria-label="Send"` or the paper-plane SVG path `d="M14.536 21.686` to locate the button.)
    - .planning/phases/14-plain-language-translation-asides/14-PATTERNS.md L186-234 (verbatim morph shape — same button element, conditionally branched icon/onClick/aria-label/title/className)
    - .planning/phases/14-plain-language-translation-asides/14-CONTEXT.md § ComposeBox morph (Ashley chose X over play-arrow: "play arrow is too close to the send icon already that's there")
  </read_first>
  <behavior>
    - **Same button element, different attributes.** Morph inside the existing `<button>` — do not add a second button, do not conditionally render two different button elements. Preserves DOM identity across morph transitions (focus, keyboard tab order, parent-CSS selectors don't blink).
    - **When asideActive === true:**
      - Icon: `<X className="size-6" strokeWidth={2.25} aria-hidden="true" />` (lucide-react X, sized to match paper-plane 24×24).
      - aria-label: "Resume"
      - title: "Resume" (hover tooltip per ASIDE-06 spec)
      - onClick: `if (asideActive) { onAsideDismiss?.(); return; } if (!sendDisabled) handleSend();` — asideActive branch fires FIRST with a guard-return; `?.()` handles undefined onAsideDismiss defensively.
      - disabled: `false` — the X is always clickable. Form: `disabled={asideActive ? false : sendDisabled}`.
      - className extension for identity-hue color when morphed: per PATTERNS.md L220-223, replace the existing default vs hover color classes with `asideActive ? "text-[hsla(var(--pv-id-hue),90%,72%,0.95)] hover:text-[hsla(var(--pv-id-hue),95%,82%,1)]" : "text-[rgba(240,235,224,0.3)] hover:text-[rgba(240,235,224,0.9)]"`. All other positional / layout classes stay unchanged.
    - **When asideActive === false / undefined:**
      - Icon: existing paper-plane inline SVG.
      - aria-label / title: "Send".
      - onClick: existing `() => { if (!sendDisabled) handleSend(); }`.
      - disabled: existing sendDisabled.
      - className: existing color classes.
    - **Import extension:** add `X` to the lucide-react import at file top. Currently imports (per PATTERNS.md L241-243): `Hourglass, Paperclip, RefreshCw, RotateCcw, Square, ThumbsUp`. Insert `X` alphabetically or at the position matching file convention.
    - **Zero regression:** every existing ComposeBox.test.tsx test still passes unchanged. `getByRole("button", { name: "Send" })` still succeeds when asideActive is undefined.
  </behavior>
  <action>
    Edit the inside-textarea Send button per PATTERNS.md L186-234:

    (1) Add `X` to the lucide-react named-import list at file top.

    (2) Change the button's `onClick` to: `() => { if (asideActive) { onAsideDismiss?.(); return; } if (!sendDisabled) handleSend(); }`.

    (3) Change the button's `disabled` from `disabled={sendDisabled}` to `disabled={asideActive ? false : sendDisabled}`.

    (4) Change the button's `aria-label` from `"Send"` to `{asideActive ? "Resume" : "Send"}`. Same for `title`.

    (5) In the button's className cn() call, replace the two lines that set default vs hover color (`"text-[rgba(240,235,224,0.3)]"` and `"hover:text-[rgba(240,235,224,0.9)]"`) with a single conditional expression per PATTERNS.md L220-223 branching on asideActive. All other className args untouched.

    (6) Replace the button's children (paper-plane inline `<svg>...</svg>`) with a conditional per PATTERNS.md L226-234:
        - `{asideActive ? (<X className="size-6" strokeWidth={2.25} aria-hidden="true" />) : (<svg ...>...</svg>)}`
        - Paper-plane SVG stays identical — just wrap in the conditional.

    Do NOT touch any other part of ComposeBox. Do NOT introduce a wrapper component. Do NOT add a separate X button as a sibling. One button, one conditional-branch pattern per attribute.

    Rationale (per PATTERNS.md L188-193): DOM identity preserved across morph → focus/tab/CSS selectors don't blink.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; grep -q 'asideActive ? "Resume" : "Send"' src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; grep -q "onAsideDismiss?.()" src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; grep -q "asideActive ? false : sendDisabled" src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; grep -q '&lt;X className="size-6"' src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; grep -q "hsla(var(--pv-id-hue),90%,72%,0.95)" src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; grep -E "import\s*\{[^}]*\bX\b[^}]*\}\s*from\s*\"lucide-react\"" src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run src/ui/features/pretty-view/ComposeBox.test.tsx 2&gt;&amp;1 | tail -3</automated>
  </verify>
  <done>
    - Inside-textarea Send button conditionally morphs on asideActive: aria-label + title toggle "Resume" ↔ "Send", onClick branches to onAsideDismiss?.() when asideActive, disabled always-false when asideActive, className uses identity-hue color when morphed, X lucide icon replaces paper-plane SVG when morphed.
    - Zero new button elements, zero new wrappers — single-button-conditional-attribute pattern.
    - X imported from lucide-react.
    - `npx tsc --noEmit` exits 0.
    - Existing ComposeBox.test.tsx passes unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ComposeBox click handler → PrettyView's handleAsideDismiss → WS send | user X-click routes through Wave 3's handleAsideDismiss callback which WS-sends aside_dismissed |
| ComposeBox render pipeline | user-controlled prop (asideActive) drives conditional attributes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04-01 | Injection | asideActive prop value (boolean) | mitigate | asideActive is a boolean from PrettyView state (Wave 3) — set only from server WS frames OR optimistic clear on X-click. No user-typed value flows in. TypeScript enforces the boolean type. |
| T-14-04-02 | Elevation of Privilege | CSRF-style attack tricking user into dismissing an aside | accept | X button is only clickable in user's own authenticated pretty-view session; resulting WS frame goes over the same authenticated WS the pane established at connectToPane. Same posture as every other ComposeBox button. |
| T-14-04-03 | Denial of Service | rapid X-clicks fire many aside_dismissed frames | mitigate | React event-batching + backend's cross-tab broadcast being idempotent (Escape into an already-closed BTW overlay is a no-op in tmux) means rapid dismisses are harmless. After first dismiss, asideActive becomes false so subsequent clicks fire handleSend not dismiss. |
| T-14-04-04 | Information Disclosure | ComposeBox visual morph reveals that an aside is displayed | accept | This is the intended UX — the user KNOWS an aside is displayed. Not a leak, an affordance. |
| T-14-04-05 | Tampering | textarea remaining editable during aside allows the user to compose while agent's BTW overlay is open | accept | This is the intended UX per CONTEXT.md § ComposeBox morph. Typed draft won't be sent (send button is morphed to X); patch #57's autosave preserves it across the aside window. No injection surface because there's no send path while asideActive. |
| T-14-04-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new package installs. X icon from lucide-react (already in devDeps + used across pretty-view). No package legitimacy audit required. |
</threat_model>

<verification>
- 4+ aux buttons extended with `asideActive === true` in their disabled predicate.
- Textarea disabled predicate NOT extended.
- Inside-textarea Send button morphs on asideActive: aria-label, title, onClick, disabled, className, icon.
- X imported from lucide-react.
- `npx tsc --noEmit` clean.
- Existing ComposeBox tests pass unchanged (zero-regression contract).
</verification>

<success_criteria>
- ASIDE-06 (morph while displayed) fully advanced: send-to-X, queue/thumbs/reset disable, textarea preserved.
- ASIDE-07 (X-click dismisses) partially advanced: X click fires onAsideDismiss which reaches Wave 3's handleAsideDismiss which fires WS dismiss + optimistic clear; Wave 2's backend completes the loop.
- Zero regression when asideActive is undefined/false — entire pre-existing test suite passes unchanged.
- Body-only wave — no interface changes, no new files, no new tests. Integration coverage lands in Wave 5.
- Interface-first ordering (fixed per plan-checker W3): Wave 3 owns the ComposeBoxProps additions, Wave 4 owns the body consumption. Clean separation.
</success_criteria>

<output>
Create `.planning/phases/14-plain-language-translation-asides/14-04-SUMMARY.md` when done — record: which line numbers received the disable-predicate extensions (list each button + its post-Wave-3 line number), the Send button morph diff, tsc + vitest output, confirmation zero pre-existing ComposeBox test was modified, and confirmation the interface (asideActive?/onAsideDismiss?) was NOT re-declared here (Wave 3 owns it).
</output>
