---
phase: 14-plain-language-translation-asides
plan: 03
type: execute
wave: 3
depends_on: [14-02]
files_modified:
  - src/ui/features/pretty-view/AsideBubble.tsx
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-view/ComposeBox.tsx
autonomous: true
requirements: [ASIDE-01, ASIDE-02, ASIDE-05, ASIDE-09]
tags: [frontend, pretty-view, aside, bubble, arm-emitter, composebox-interface]

must_haves:
  truths:
    - "AsideBubble is a new React component at src/ui/features/pretty-view/AsideBubble.tsx, sibling to ChatMessage / ImageBubble / PlanPendingBubble / WipBubble"
    - "AsideBubble renders as an in-flow bubble at the bottom of the scrollable message stream — NOT an overlay, popup, or fixed-position element (per ASIDE-05)"
    - "AsideBubble background uses the identity-hue gradient identical to normal assistant bubbles from ChatMessage.tsx L124 — semantically the aside IS a bubble from the same identity"
    - "AsideBubble border is 10px solid hsla(var(--pv-id-hue), 90%, 65%, 1) with three-layer additive neon glow at 12/32/64px (per CONTEXT.md § Rendering LOCKED aesthetic)"
    - "AsideBubble accepts glow multiplier prop (default 1.0) and borderWidthPx prop (default 10) so future dial-back is one prop change"
    - "PrettyView.tsx pretty-view chat surface interior UNTOUCHED for its existing bubbles (ChatMessage / ImageBubble / PlanPendingBubble / WipBubble bytes unchanged) — AsideBubble is a NEW sibling inserted at the current L818-853 message-render slot"
    - "PrettyView handles the new aside_ready and aside_dismissed WS event cases in its existing switch at L363-379, updating a new asideText useState slot"
    - "PrettyView resets asideText to null on fresh-pane mount (L316-329 reset block) so a mount for a different pane starts clean"
    - "PrettyView emits `{type: 'aside_arm'}` on the pretty-view WS on the isIdle:false→true transition, gated on pvIdentity !== null (identity-gate frontend-side per CONTEXT.md § Trigger lock 2026-07-26)"
    - "ComposeBoxProps interface receives asideActive?: boolean + onAsideDismiss?: () => void as optional additive fields in THIS wave (Wave 3), so PrettyView's prop-passing typechecks immediately. Wave 4 consumes these props inside ComposeBox's body — the interface contract is locked here."
  artifacts:
    - path: "src/ui/features/pretty-view/AsideBubble.tsx"
      provides: "AsideBubble React component with text/glow/borderWidthPx props"
      contains: "export function AsideBubble"
      contains_2: "borderWidthPx"
      contains_3: "glow"
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "asideText state + aside_ready/aside_dismissed WS handling + AsideBubble mount at message-stream bottom + fresh-pane reset extension + isIdle-transition arm emitter (identity-gated) + handleAsideDismiss callback + ComposeBox prop plumbing"
      contains: "asideText"
      contains_2: "AsideBubble"
      contains_3: '"aside_ready"'
      contains_4: '"aside_dismissed"'
      contains_5: '"aside_arm"'
      contains_6: "handleAsideDismiss"
    - path: "src/ui/features/pretty-view/ComposeBox.tsx"
      provides: "ComposeBoxProps.asideActive?: boolean + ComposeBoxProps.onAsideDismiss?: () => void — INTERFACE EXTENSION ONLY (body consumption is Wave 4)"
      contains: "asideActive?:"
      contains_2: "onAsideDismiss?:"
  key_links:
    - from: "PrettyView WS event switch at L363-379"
      to: "setAsideText state update on aside_ready / aside_dismissed"
      via: "case in the existing switch(parsed.type) — same setState-in-switch shape as message/image/inactive"
      pattern: "case\\s+[\"']aside_ready[\"']"
    - from: "PrettyView isIdle prop (existing, from Terminal.tsx via L94/L137)"
      to: "wsRef.send({type: 'aside_arm'}) on false→true transition"
      via: "useEffect watching isIdle with a useRef holding previous value; guard on pvIdentity !== null"
      pattern: '"aside_arm"'
    - from: "PrettyView contentRef div at L818"
      to: "AsideBubble rendered as LAST child in the flex-col message stream (immediately after PlanPendingBubble at L852)"
      via: "conditional render {asideText && <AsideBubble text={asideText} />}"
      pattern: "AsideBubble"
    - from: "AsideBubble background + prose treatment"
      to: "ChatMessage.tsx L124-127 identity-hue gradient class strings"
      via: "verbatim class-string copy (do NOT import from ChatMessage — copy the strings so AsideBubble stands alone)"
      pattern: "hsla\\(var\\(--pv-id-hue\\)"
    - from: "ComposeBoxProps interface extension (asideActive + onAsideDismiss)"
      to: "PrettyView's ComposeBox mount prop passing"
      via: "interface-first ordering (moved from Wave 4 to Wave 3 per plan-checker W3) — interface owns contract, Wave 4 owns body implementation"
      pattern: "asideActive\\?:\\s*boolean"
---

<objective>
Ship the frontend layer of the aside subsystem: (1) the new AsideBubble component with locked aesthetic, (2) PrettyView's mount + WS handling + fresh-pane reset, (3) PrettyView's arm-emitter for the frontend-arm architecture (CONTEXT.md § Trigger lock 2026-07-26), (4) the ComposeBoxProps interface extension (moved into Wave 3 per plan-checker W3 to fix typecheck ordering — Wave 4 then implements only the body consumption).

Purpose: ASIDE-05 mandates in-flow render at message-stream bottom with locked 10px + neon-glow aesthetic. ASIDE-09 mandates a late-mounting client still displays the aside if the tmux BTW overlay is still open — Wave 2's connect-time probe emits aside_ready and this task's WS handler consumes it. ASIDE-01 mandates the arm signal reuses the WIP-indicator idle window — CONTEXT.md § Trigger locks this to a frontend-emitted `aside_arm` WS message driven by PrettyView's existing isIdle prop. ASIDE-02 mandates identity gating — CONTEXT.md § Trigger locks this frontend-side via `pvIdentity !== null` before emitting arm.

Output: One NEW file (AsideBubble.tsx) + two MODIFIED files (PrettyView.tsx, ComposeBox.tsx — the latter interface-only). The morphed ComposeBox BODY (button gates + Send→X morph) is Wave 4's concern — this plan wires the interface contract PLUS the render + WS state + arm-emitter layers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-plain-language-translation-asides/14-CONTEXT.md
@.planning/phases/14-plain-language-translation-asides/14-PATTERNS.md
@.planning/phases/14-plain-language-translation-asides/14-02-PLAN.md
@.planning/phases/14-plain-language-translation-asides/14-02-SUMMARY.md

# Existing files this plan reads / mounts alongside (LOCKED — do not modify contents beyond specified edits)
@src/ui/features/pretty-view/ChatMessage.tsx
@src/ui/features/pretty-view/ImageBubble.tsx
@src/ui/features/pretty-view/PlanPendingBubble.tsx
@src/ui/features/pretty-view/PrettyView.tsx
@src/ui/features/pretty-view/ComposeBox.tsx
@src/ui/features/terminal/session-hue.ts
@src/ui/api/claude-session-api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create AsideBubble component at src/ui/features/pretty-view/AsideBubble.tsx</name>
  <files>src/ui/features/pretty-view/AsideBubble.tsx, src/ui/features/pretty-view/AsideBubble.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/PlanPendingBubble.tsx (FULL file — primary analog per PATTERNS.md L26; fork its wrapper skeleton verbatim)
    - src/ui/features/pretty-view/ChatMessage.tsx L118-129 (identity-hue gradient / prose-invert / Inter-font class strings to lift verbatim per PATTERNS.md L28-32 and § Shared Patterns § Identity-hue Glass depth. Grep-verify line numbers — file may have been touched.)
    - src/ui/features/pretty-view/ImageBubble.tsx L55-93 (tertiary analog for a NEW bubble type wrapping non-prose text)
    - .planning/phases/14-plain-language-translation-asides/14-CONTEXT.md § Rendering (LOCKED aesthetic — 10px border, three-layer neon glow, EXACT hsla values)
    - .planning/phases/14-plain-language-translation-asides/14-PATTERNS.md L42-87 (wrapper + bubble skeleton to fork VERBATIM)
    - src/ui/features/pretty-view/ChatMessage.test.tsx L14-50 (test scaffold shape for pretty-view components)
    - ~/.claude/identities/tina/bounties/plain-language-translation-asides/aside-visual-snippet.js (Ashley's DevTools sign-off — canonical source for the exact CSS values)
  </read_first>
  <behavior>
    - **Props interface `AsideBubbleProps`:**
      - `text: string` — required. Aside text extracted by backend (may contain \n).
      - `glow?: number` — multiplier on each of the three neon-glow layer alpha values. Default 1.0.
      - `borderWidthPx?: number` — border width in pixels. Default 10. Not user-tunable at v1 but the prop exists per CONTEXT.md § Rendering final sentence.
    - **Render output:**
      - Outer wrapper: `<div className="flex justify-start">` — matches PlanPendingBubble.
      - Inner bubble div with role="note" + aria-label="Plain-language aside from the identity".
      - Inner div class strings (VERBATIM from PATTERNS.md L52-61 lifted from ChatMessage L124-127):
        - "leading-relaxed"
        - "rounded-[var(--radius-pv-bubble)] px-[18px] py-[14px]"
        - "backdrop-blur-xl saturate-150"
        - "[-webkit-backdrop-filter:blur(20px)_saturate(1.6)]"
        - "bg-[linear-gradient(160deg,hsla(var(--pv-id-hue),50%,38%,0.55),hsla(var(--pv-id-hue),45%,24%,0.6))]"
        - "text-[#fbf5e8]"
        - "prose prose-sm max-w-none prose-invert"
        - "font-[Inter_Variable,ui-sans-serif,system-ui,sans-serif]"
        - "[&>*:first-child]:mt-0 [&>*:last-child]:mb-0"
      - Inner div inline `style` prop for prop-driven aesthetic:
        - borderStyle: "solid"
        - borderColor: `hsla(var(--pv-id-hue), 90%, 65%, 1)`
        - borderWidth: `${borderWidthPx}px`
        - boxShadow: comma-joined string of five layers:
          1. `0 0 12px hsla(var(--pv-id-hue), 100%, 60%, ${0.7 * glow})`
          2. `0 0 32px hsla(var(--pv-id-hue), 100%, 55%, ${0.5 * glow})`
          3. `0 0 64px hsla(var(--pv-id-hue), 100%, 50%, ${0.3 * glow})`
          4. `0 8px 24px rgba(0,0,0,0.5)` — additive depth shadow
          5. `0 1px 0 rgba(255,220,170,0.18) inset` — additive inner rim
      - Bubble content: `<div className="whitespace-pre-wrap">{text}</div>` — plain-text preserving newlines. No markdown pipeline.
    - **Purity:** pure function of props — no useState, no useEffect, no refs, no event handlers on the bubble itself.
    - **Tests (AsideBubble.test.tsx):**
      1. Renders text content with role="note".
      2. Default props: borderWidth is "10px" AND boxShadow includes all three glow-layer alpha values 0.7 / 0.5 / 0.3.
      3. Custom glow=0.5: boxShadow includes halved alpha values 0.35 / 0.25 / 0.15.
      4. Custom borderWidthPx=6: borderWidth is "6px".
      5. Preserves newlines in text prop via whitespace-pre-wrap class.
  </behavior>
  <action>
    Create `src/ui/features/pretty-view/AsideBubble.tsx` per the behavior spec. Fork the wrapper skeleton verbatim from PlanPendingBubble.tsx (grep-verify current line numbers). Import cn from "@/lib/utils". Do NOT import from ChatMessage — copy class strings so AsideBubble stands alone.

    Create `src/ui/features/pretty-view/AsideBubble.test.tsx` with the 5 test cases. Use vitest + @testing-library/react shape from ChatMessage.test.tsx. Import { render, screen }, { describe, it, expect }, { AsideBubble }. For style-based assertions use `screen.getByRole("note").getAttribute("style")` or `.style.boxShadow`.

    Do NOT touch ChatMessage.tsx / ImageBubble.tsx / PlanPendingBubble.tsx / WipBubble.tsx. Aesthetic values are LOCKED per CONTEXT.md § Rendering — do NOT tune any hsla percentage, alpha value, blur radius, or border width. If any value differs from PATTERNS.md L62-77 by even one digit, CONTEXT.md is the tiebreaker.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; grep -q "export function AsideBubble" src/ui/features/pretty-view/AsideBubble.tsx &amp;&amp; grep -q "borderWidthPx" src/ui/features/pretty-view/AsideBubble.tsx &amp;&amp; grep -q "glow" src/ui/features/pretty-view/AsideBubble.tsx &amp;&amp; grep -q "hsla(var(--pv-id-hue), 90%, 65%, 1)" src/ui/features/pretty-view/AsideBubble.tsx &amp;&amp; grep -q "0 0 12px hsla(var(--pv-id-hue)" src/ui/features/pretty-view/AsideBubble.tsx &amp;&amp; grep -q "0 0 32px hsla(var(--pv-id-hue)" src/ui/features/pretty-view/AsideBubble.tsx &amp;&amp; grep -q "0 0 64px hsla(var(--pv-id-hue)" src/ui/features/pretty-view/AsideBubble.tsx &amp;&amp; grep -q 'role="note"' src/ui/features/pretty-view/AsideBubble.tsx &amp;&amp; grep -q "whitespace-pre-wrap" src/ui/features/pretty-view/AsideBubble.tsx &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run src/ui/features/pretty-view/AsideBubble.test.tsx 2&gt;&amp;1 | grep -qE '(5 passed|Tests\s+5\s+passed)'</automated>
  </verify>
  <done>
    - AsideBubble.tsx exists with exact prop signature.
    - All nine identity-hue class strings from ChatMessage L124-127 verbatim.
    - Three neon-glow boxShadow layers use LOCKED hsla values.
    - Inline style borderColor is `hsla(var(--pv-id-hue), 90%, 65%, 1)`.
    - Wrapper has role="note".
    - AsideBubble.test.tsx exists with 5 passing cases.
    - `npx tsc --noEmit` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend ComposeBoxProps interface with asideActive + onAsideDismiss (interface-only; body consumption is Wave 4)</name>
  <files>src/ui/features/pretty-view/ComposeBox.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/ComposeBox.tsx (target the ComposeBoxProps interface. Grep-locate — file was described as "~L89-195" for the props interface but any recent edit may have shifted lines. Search for `interface ComposeBoxProps` or `type ComposeBoxProps`.)
    - .planning/phases/14-plain-language-translation-asides/14-PATTERNS.md L163-174 (props signature — same shape, moved earlier in wave order per W3)
    - .planning/phases/14-plain-language-translation-asides/14-CONTEXT.md § ComposeBox morph (semantics that Wave 4 will consume from these props)
  </read_first>
  <behavior>
    - Two new optional fields added to the ComposeBoxProps interface (or type):
      - `asideActive?: boolean` — when true, ComposeBox is in ASIDE-DISPLAYED mode per ASIDE-06. Body consumption is Wave 4.
      - `onAsideDismiss?: () => void` — fired when the user clicks the morphed X (Resume). Body consumption is Wave 4.
    - Both are optional so callers that don't pass these props (existing test cases, other mount sites if any) see zero behavior change.
    - **CRITICAL: this task DOES NOT modify the ComposeBox function body.** The props may be destructured with defaults or left in `...restProps` — no other body change. Wave 4 owns:
      - The aux-button `disabled` predicate extensions (`|| asideActive === true`).
      - The Send button morph to X (Resume).
      - The lucide-react `X` import.
    - This split-wave ordering fixes plan-checker W3: PrettyView's mount at Task 3 below passes these props; if the interface didn't accept them until Wave 4, Task 3 would fail typecheck. Now Wave 3 owns the interface contract atomically; Wave 4 fills in the body.
  </behavior>
  <action>
    Edit src/ui/features/pretty-view/ComposeBox.tsx. Grep-locate the ComposeBoxProps interface (do NOT trust the cited "L89-195" — verify with grep).

    (1) Add two new optional fields to the interface, with JSDoc comments referencing ASIDE-06 / ASIDE-07 + CONTEXT.md § ComposeBox morph + "Body consumption in Wave 4 (14-04)":
        - `asideActive?: boolean;`
        - `onAsideDismiss?: () => void;`

    (2) OPTIONAL: destructure the two new props in the function signature. If destructured, do NOT reference them anywhere in the body — Wave 4 owns body consumption. If undestructured, the TypeScript unused-property warning won't fire because they're passed in via ...props / the object spread. Planner discretion — but keep the body untouched either way.

    Do NOT extend any aux-button `disabled` predicate. Do NOT modify the Send button. Do NOT add the lucide-react `X` import. All of that is Wave 4's job.

    Why this split: interface-first task ordering. Wave 3's PrettyView passes the props (Task 3 below); Wave 4 consumes them inside ComposeBox. Splitting the interface change into Wave 3 makes Wave 3's PrettyView edit typecheck immediately, and Wave 4's edit becomes purely body-level with zero interface risk.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; grep -q "asideActive?:" src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; grep -q "onAsideDismiss?:" src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; ! grep -q "asideActive === true" src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; ! grep -q 'aria-label={asideActive' src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; ! grep -E "import\s*\{[^}]*\bX\b[^}]*\}\s*from\s*\"lucide-react\"" src/ui/features/pretty-view/ComposeBox.tsx &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run src/ui/features/pretty-view/ComposeBox.test.tsx 2&gt;&amp;1 | tail -3</automated>
  </verify>
  <done>
    - ComposeBoxProps has `asideActive?: boolean` and `onAsideDismiss?: () => void` as optional fields with JSDoc references.
    - Negative-grep confirms no `asideActive === true` (Wave 4 will add these to aux-button disabled predicates).
    - Negative-grep confirms no aria-label morph (Wave 4 will add this to Send button).
    - Negative-grep confirms no X import from lucide-react (Wave 4 will add this).
    - `npx tsc --noEmit` exits 0.
    - Existing ComposeBox.test.tsx passes unchanged (interface addition is backward-compatible; body is untouched).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire PrettyView — asideText state + WS handlers + AsideBubble mount + fresh-pane reset + isIdle-transition arm emitter + handleAsideDismiss callback</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/PrettyView.tsx (verified 2026-07-26: `isIdle?: boolean | null` prop at L94, destructured at L137, `wipActive = isIdle === false` at L229; `useSessionIdentity(tmuxSession)` returns `{identity: pvIdentity, identityHue: pvIdentityHue}` at L304; target line ranges L141-210 useState block, L316-329 fresh-pane reset, L363-379 WS event switch, L702 --pv-id-hue set point, L818-853 message-render slot, L936-975 ComposeBox mount point. Grep-verify current line numbers — Wave 3 Task 1 created AsideBubble.tsx which is imported here; other edits may have shifted lines.)
    - src/ui/features/pretty-view/AsideBubble.tsx (Task 1 just-created — for the import statement)
    - src/ui/features/pretty-view/ComposeBox.tsx (Task 2 just-modified — ComposeBoxProps now accepts asideActive + onAsideDismiss)
    - src/ui/features/terminal/session-hue.ts L24 (useSessionIdentity hook — returns { identity, identityHue }; PrettyView already uses this at L304)
    - src/ui/api/claude-session-api.ts (Wave 2's new AsideReadyEvent / AsideDismissedEvent / AsideArmPayload / AsideDismissedPayload — verify names before consuming)
    - .planning/phases/14-plain-language-translation-asides/14-PATTERNS.md L90-156 (verbatim insertion points + fresh-pane reset + WS switch extension pattern — but note the ComposeBox prop-plumbing section applies at Task 3, not deferred)
    - .planning/phases/14-plain-language-translation-asides/14-CONTEXT.md § Trigger (frontend-arm architecture: emit `aside_arm` on isIdle:false→true when pvIdentity !== null) + § Rendering + § Dismiss + § Tab close / re-attach
  </read_first>
  <behavior>
    - **New useState slot** in the existing state block (alongside setMessages, setPlanPending, setStatus, etc.):
      - `const [asideText, setAsideText] = useState<string | null>(null);` — null = no aside; string = aside displayed with that text.

    - **WS event switch extension** at the existing switch (around L363-379 — grep-verify): add two new cases:
      - `case "aside_ready": setAsideText(parsed.text); break;`
      - `case "aside_dismissed": setAsideText(null); break;`

    - **AsideBubble mount** — insert IMMEDIATELY AFTER the existing `{planPending && <PlanPendingBubble />}` render at ~L852. Insertion: `{asideText !== null && <AsideBubble text={asideText} />}`. Per PATTERNS.md L115: the existing useAutoScroll ResizeObserver on contentRef (L818) will re-pin the viewport when AsideBubble mounts — no new scroll logic needed.

    - **Fresh-pane reset extension** at the existing reset block (around L316-329 — grep-verify): add `setAsideText(null);` alongside the existing per-state resets.

    - **isIdle-transition arm emitter (per CONTEXT.md § Trigger — the sole trigger source, ASIDE-01 + ASIDE-02):**
      - Add a useRef holding the PREVIOUS isIdle value: `const prevIsIdleRef = useRef<boolean | null | undefined>(isIdle);`
      - Add a useEffect watching `isIdle` (and referencing `pvIdentity` + `wsRef`):
        ```
        useEffect(() => {
          const prev = prevIsIdleRef.current;
          prevIsIdleRef.current = isIdle;
          // Fire on false → true transition ONLY. Guards:
          //   1. isIdle just became true (agent settled after completed turn)
          //   2. pvIdentity !== null (identity-gate frontend-side per CONTEXT.md § Trigger LOCK 2026-07-26 —
          //      backend does NOT gate identity; frontend owns this check)
          //   3. wsRef.current is OPEN (otherwise the send is a no-op / throws)
          if (prev === false && isIdle === true && pvIdentity != null) {
            const ws = wsRef.current;
            if (ws && ws.readyState === WebSocket.OPEN) {
              try {
                ws.send(JSON.stringify({ type: "aside_arm" }));
              } catch {
                // swallow — arm is best-effort; backend re-arm happens on next transition
              }
            }
          }
        }, [isIdle, pvIdentity]);
        ```
      - Placement: near the existing effect block in PrettyView (grep for `useEffect` and pick a semantically-adjacent placement — e.g. near the isIdle-driven wipActive computation area).
      - NOTE: the `prev === false` guard specifically excludes `prev === undefined` (initial mount) and `prev === null` (unknown/loading state) — arm only fires on a REAL transition from active to idle, not on component-mount-with-idle-already-true.

    - **handleAsideDismiss callback:** declare via useCallback near other on-* callbacks:
      ```
      const handleAsideDismiss = useCallback(() => {
        // 1. Optimistic clear (CONTEXT.md § Dismiss step 1)
        setAsideText(null);
        // 2. WS-send dismiss (CONTEXT.md § Dismiss step 3) — backend Wave 2 will
        //    sendEscapeToBtw + broadcastAsideDismissed on receipt
        const ws = wsRef.current;
        if (ws && ws.readyState === WebSocket.OPEN && hostId != null && tmuxSession != null) {
          try {
            ws.send(JSON.stringify({ type: "aside_dismissed", hostId, tmuxSession }));
          } catch { /* swallow */ }
        }
      }, [hostId, tmuxSession]);
      ```
      Reference the existing hostId + tmuxSession scope in the file (they're already captured for the initial connect — grep to confirm they're in the outer function scope).

    - **ComposeBox prop plumbing** — at the existing ComposeBox mount (around L936-975 — grep-verify), add the two new props:
      - `asideActive={asideText !== null}`
      - `onAsideDismiss={handleAsideDismiss}`
      Wave 4 will consume these inside ComposeBox's body. Task 2 above already added the props to ComposeBoxProps so the passing is typesafe.

    - **AsideBubble import** at the top of PrettyView.tsx: `import { AsideBubble } from "./AsideBubble";`
  </behavior>
  <action>
    Edit src/ui/features/pretty-view/PrettyView.tsx per behavior spec:

    (1) Add `import { AsideBubble } from "./AsideBubble";` in imports.

    (2) Add `const [asideText, setAsideText] = useState<string | null>(null);` to the useState block. Placement near setPlanPending (semantic grouping). Include a comment referencing ASIDE-05 + Phase 14.

    (3) Extend the fresh-pane reset block by adding `setAsideText(null);`.

    (4) Extend the WS event switch by adding the two new cases (`case "aside_ready"`, `case "aside_dismissed"`).

    (5) Add `const prevIsIdleRef = useRef<boolean | null | undefined>(isIdle);` — near the existing useRef declarations.

    (6) Add the isIdle-transition arm-emitter useEffect per the behavior spec (isIdle:false→true + pvIdentity !== null + wsRef OPEN → send `{type: "aside_arm"}`).

    (7) Declare `handleAsideDismiss` useCallback per the behavior spec (optimistic clear + WS-send dismiss with hostId + tmuxSession).

    (8) At the ComposeBox mount, add the two new props (asideActive + onAsideDismiss).

    (9) Insert `{asideText !== null && <AsideBubble text={asideText} />}` immediately after the existing `{planPending && <PlanPendingBubble />}` render at ~L852.

    Do NOT touch ChatMessage / ImageBubble / PlanPendingBubble / WipBubble. Do NOT modify any existing state, callback, WS handler, or reset logic — only add. Do NOT introduce a new WS subscription or new effect for message reception — the existing wsRef IS the pretty-view WS on port 30011 that Wave 2's backend now emits `aside_ready`/`aside_dismissed` on and receives `aside_arm`/`aside_dismissed` from (frontend-arm architecture per CONTEXT.md § Trigger 2026-07-26 lock).

    Rationale for owning the arm-emitter here (not Wave 4): the arm signal derives from PrettyView's own isIdle + pvIdentity state; ComposeBox has neither. Wave 4 would have to bubble both up as props just to fire the send — worse ergonomics than the useEffect living where the state does.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet &amp;&amp; grep -q 'import { AsideBubble }' src/ui/features/pretty-view/PrettyView.tsx &amp;&amp; grep -q 'asideText' src/ui/features/pretty-view/PrettyView.tsx &amp;&amp; grep -q 'setAsideText' src/ui/features/pretty-view/PrettyView.tsx &amp;&amp; grep -q '"aside_ready"' src/ui/features/pretty-view/PrettyView.tsx &amp;&amp; grep -q '"aside_dismissed"' src/ui/features/pretty-view/PrettyView.tsx &amp;&amp; grep -q '"aside_arm"' src/ui/features/pretty-view/PrettyView.tsx &amp;&amp; grep -q '&lt;AsideBubble' src/ui/features/pretty-view/PrettyView.tsx &amp;&amp; grep -q 'asideActive' src/ui/features/pretty-view/PrettyView.tsx &amp;&amp; grep -q 'onAsideDismiss' src/ui/features/pretty-view/PrettyView.tsx &amp;&amp; grep -q 'handleAsideDismiss' src/ui/features/pretty-view/PrettyView.tsx &amp;&amp; grep -q 'prevIsIdleRef' src/ui/features/pretty-view/PrettyView.tsx &amp;&amp; grep -q "isIdle === true &amp;&amp; pvIdentity" src/ui/features/pretty-view/PrettyView.tsx &amp;&amp; grep -c 'setAsideText(null)' src/ui/features/pretty-view/PrettyView.tsx | awk '{if ($1 &gt;= 2) exit 0; else exit 1}' &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>
    - AsideBubble imported.
    - asideText useState<string | null>(null) declared.
    - Fresh-pane reset extended with setAsideText(null).
    - WS event switch has cases for "aside_ready" (setAsideText(parsed.text)) and "aside_dismissed" (setAsideText(null)).
    - prevIsIdleRef useRef declared + isIdle-transition useEffect fires `{type: "aside_arm"}` on false→true when pvIdentity !== null and wsRef is OPEN (per CONTEXT.md § Trigger LOCK).
    - handleAsideDismiss useCallback: optimistic clear + WS-send `{type: "aside_dismissed", hostId, tmuxSession}`.
    - AsideBubble mounts as last child of contentRef div (after PlanPendingBubble render at L852).
    - ComposeBox mount receives `asideActive={asideText !== null}` and `onAsideDismiss={handleAsideDismiss}`.
    - setAsideText(null) appears at least twice (fresh-pane reset + aside_dismissed WS case + handleAsideDismiss body = 3 occurrences; grep count >= 2 tolerates callback body variance).
    - `npx tsc --noEmit` exits 0. Existing PrettyView tests either pass or show no regression.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS server → WS client | server sends aside_ready with `text` field containing agent-generated content — client must render safely |
| React render pipeline | user-facing surface where any XSS surface would manifest |
| Client isIdle-transition observation → WS emit | client-initiated aside_arm crosses to backend for /btw injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Injection | agent-generated `text` rendered by AsideBubble | mitigate | Rendered via `<div className="whitespace-pre-wrap">{text}</div>` — React auto-escapes. No dangerouslySetInnerHTML, no markdown pipeline. Even if the /btw answer contained `<script>` or `<img onerror>`, React renders as literal text. Matches ChatMessage precedent since Phase 1. |
| T-14-03-02 | Tampering | asideText state consumed for ComposeBox morph gate | mitigate | asideText is set only from server-authoritative WS frames (aside_ready / aside_dismissed) OR the optimistic clear on X-click. No user-input path from the compose textarea. |
| T-14-03-03 | Denial of Service | rapid isIdle transitions triggering rapid aside_arm sends | mitigate | Backend Wave 2's overlap-ignore policy (asideState.armed || .displayed → no-op) absorbs repeated arms. Frontend's `prev === false && isIdle === true` guard also filters — only real false→true transitions emit; mount-with-idle-true does not. |
| T-14-03-04 | Information Disclosure | AsideBubble aria-label leaking session context | accept | aria-label is a compile-time constant "Plain-language aside from the identity" — no PII. |
| T-14-03-05 | Elevation of Privilege | frontend spoofing aside_arm for a session it shouldn't | mitigate | See T-14-02-02: the pretty-view WS is per-connection authenticated via connectToPane; a WS can only send messages that will execute against ITS OWN pane's authenticated tmux target. No cross-session escalation surface. |
| T-14-03-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new package installs (cn from @/lib/utils already used; @testing-library/react + vitest in devDeps). No package legitimacy audit required. |
</threat_model>

<verification>
- AsideBubble.tsx exists + 5-case test passes.
- ComposeBox.tsx has asideActive + onAsideDismiss on the interface (BUT the body is UNTOUCHED — negative-greps confirm no aria-label morph, no X import, no aux-button predicate extension).
- PrettyView.tsx modified in nine specific ways per the action spec — all pattern greps pass.
- isIdle-transition arm emitter fires on `prev === false && isIdle === true && pvIdentity != null && ws OPEN` (per CONTEXT.md § Trigger LOCK).
- `npx tsc --noEmit` exits 0.
- Cross-tab-coherence latent test deferred to Wave 5.
</verification>

<success_criteria>
- AsideBubble is a standalone prop-driven pretty-view sibling. Locked aesthetic (10px border, three-layer neon glow, identity-hue gradient) is faithful to CONTEXT.md § Rendering + aside-visual-snippet.js.
- PrettyView renders AsideBubble at bottom of message stream in-flow (per ASIDE-05).
- WS aside_ready / aside_dismissed drive asideText state; fresh-pane mount resets state.
- **PrettyView emits `aside_arm` on isIdle:false→true when pvIdentity !== null — this IS the sole aside trigger source per CONTEXT.md § Trigger LOCK 2026-07-26, closing the frontend-arm architecture loop with Wave 2's backend dispatch.**
- ComposeBoxProps interface receives asideActive + onAsideDismiss so Wave 4 can consume without further interface work (fixes plan-checker W3 typecheck ordering).
- ChatMessage / ImageBubble / PlanPendingBubble / WipBubble bytes unchanged; ComposeBox BODY unchanged (Wave 4 owns).
- Advances ASIDE-01 (arm emitter is the trigger source), ASIDE-02 (frontend identity gate), ASIDE-05 (render + placement), ASIDE-09 (re-attach probe from Wave 2 delivers to a late-mounting client — this task's WS handler is the receiver).
</success_criteria>

<output>
Create `.planning/phases/14-plain-language-translation-asides/14-03-SUMMARY.md` when done — record: AsideBubble.tsx line count, test count + pass count, PrettyView.tsx diff summary (which line ranges got modified), ComposeBox.tsx diff (interface additions only — negative-grep confirmations for body-untouched), tsc output, and a note confirming ChatMessage/ImageBubble/PlanPendingBubble/WipBubble bytes unchanged.
</output>
