---
phase: 62-invisible-dormancy-client-side-follow-up
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/claude-session/claude-session-server.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every call to __applyQueueDedupForTests (at claude-session-server.ts:3916-3931) emits a `[dedup] ...` sshLogger.info line recording the tail-watcher instance ID, sessionId, contentHash, rawType, operation, action (populate|suppress|passthrough), and current map size — no gaps in the dedup call trace (per D-62-04)."
    - "Every ws.send emission that passes the dedup gate (the `if (!suppress)` branch immediately after __applyQueueDedupForTests) emits a `[frame-emit] ...` sshLogger.info line recording the tail-watcher instance ID, eventId, role, contentLen, and contentPreview (first 50 chars, newlines collapsed to spaces) — the dedup pass/fail decision and the actual wire emission are jointly greppable per tail instance."
    - "Every tail-watcher start (at the existing `Starting Claude session tail` log ~L6284) and every tail-watcher stop for wake handoff (at the existing `Dormant tail stopped for wake handoff` log ~L7193-7203) emits an augmented `[tail-lifecycle] ...` sshLogger.info line recording tail instance ID, action (start|stop), sessionFile, and reason (initial|transition|dormant-handoff) — cross-tail races can be identified from the log trace (per D-62-04)."
    - "A per-tail-watcher instance ID (crypto.randomBytes(4).toString('hex') — 8-char lowercase hex) is generated at the start of each tail lifecycle and threaded via closure variable through the three log-emission sites — no persistence beyond the closure, no reuse across tails."
    - "Zero behavioral changes: __applyQueueDedupForTests logic, key derivation, TTL, and Map.clear() behavior are UNCHANGED (per D-62-01 — Phase 62 Wave 2 is instrumentation only; actual dedup fix moves to Phase 63 after repro-with-logs). Existing tests (claude-session-server.compose-send.test.ts, optimistic-bubbles.integration.test.ts, claude-session-server.dormant-tail.test.ts, pv-send-watchdog.test.ts) remain green — the enforcement that logging is truly diagnostic-only."
  artifacts:
    - path: "src/backend/claude-session/claude-session-server.ts"
      provides: "Three new sshLogger.info log-point sites (dedup call outcome, frame-emit passthrough, tail-lifecycle start/stop) + one crypto.randomBytes(4) instance-id generation per tail-watcher lifecycle threaded via closure"
      contains: "[dedup]"
  key_links:
    - from: "src/backend/claude-session/claude-session-server.ts:3916-3931 (__applyQueueDedupForTests call site)"
      to: "sshLogger.info('[dedup] tail=<id> ...')"
      via: "immediately after the result.suppress = ... line inside the try block, before the closing brace of the if (frame.type === 'message' && frame.role === 'user') block"
      pattern: "\\[dedup\\] tail="
    - from: "src/backend/claude-session/claude-session-server.ts:3933-3938 (ws.send inside if (!suppress))"
      to: "sshLogger.info('[frame-emit] tail=<id> ...')"
      via: "immediately after the ws.send + try/catch — only for kind:'message' frames that passed dedup"
      pattern: "\\[frame-emit\\] tail="
    - from: "src/backend/claude-session/claude-session-server.ts:6284 (Starting Claude session tail) + :7193 (Dormant tail stopped for wake handoff)"
      to: "sshLogger.info('[tail-lifecycle] tail=<id> action=start|stop reason=<r>')"
      via: "augmenting adjacent to the existing lifecycle logs — the instance-id is the NEW piece"
      pattern: "\\[tail-lifecycle\\] tail="
---

<objective>
Add diagnostic-only instrumentation around the per-connection `queueEnqueueDedup` Map (declared at claude-session-server.ts:3269) and its consumer `__applyQueueDedupForTests` call site (at claude-session-server.ts:3916-3931) so the next dormant-send repro from Ashley produces definitive logs identifying the mechanism that emits two user-role bubbles for a single send. Ashley 2026-08-30 forensic analysis of `e59b2000-*.jsonl` could not identify a mechanism from static analysis (only 1 user-role emission in the JSONL, no session-file change across the wake, `transitionToActiveNew` did not fire, `queueEnqueueDedup.clear()` did not fire). D-62-01 locks this wave as INSTRUMENTATION ONLY — no change to dedup logic, key derivation, TTL, or `Map.clear()` semantics. If the logs from the next repro confirm a leak mechanism, Phase 63 fixes it with the mechanism nailed down.

Three log-point sites, one instance-id generation site:
  1. `[dedup] tail=<id> sessionId=<sid> contentHash=<hash> rawType=<t> operation=<op> action=<populate|suppress|passthrough> mapSize=<n>` — one line per __applyQueueDedupForTests call outcome.
  2. `[frame-emit] tail=<id> eventId=<eid> role=<r> contentLen=<n> contentPreview=<first-50-chars-with-newlines-collapsed-to-spaces>` — one line per ws.send passthrough (dedup did not suppress).
  3. `[tail-lifecycle] tail=<id> action=<start|stop> sessionFile=<path> reason=<initial|transition|dormant-handoff>` — augments the existing lifecycle logs with the tail instance ID.

The per-tail-watcher instance ID is `crypto.randomBytes(4).toString("hex")` (8-char lowercase hex), generated inside the closure that owns the streaming tail lifecycle and threaded via closure variable to the three log points. Never persisted beyond the closure. Any race across a tail-swap (start-old / stop-old / start-new) is identifiable from the ID transition in the logs.

Output: `claude-session-server.ts` with three new sshLogger.info calls + one crypto.randomBytes(4) call scoped to the tail closure. No test file changes, no wire-protocol changes, no dedup behavior changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/62-invisible-dormancy-client-side-follow-up-widen-client-pendin/62-CONTEXT.md
@src/backend/claude-session/claude-session-server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate per-tail-watcher instance ID + thread it through streaming-tail lifecycle log points</name>
  <files>src/backend/claude-session/claude-session-server.ts</files>
  <read_first>
    src/backend/claude-session/claude-session-server.ts (specifically: line 3133 `let tailHandle: TailHandle | null = null;` — the closure-scoped tail state; lines 3510-3525 the tail-stop block in the connection teardown path with `tailHandle.stop()` + `tailHandle = null;`; lines 6284-6292 the existing `Starting Claude session tail` sshLogger.info at the start of startActiveSessionFlow — this is where a fresh tail comes online; lines 7188-7204 the `Dormant tail stopped for wake handoff` sshLogger.info — this is where a tail is torn down for the invisible-wake handoff; line 4324 `tailHandle = tailSessionFile(sshConn, newSessionFile, onLine, onError);` — the tail reassign in transitionToActiveNew; line 6999 `tailHandle = tailSessionFile(sshConn!, sessionFile, onLine, onError);` — the tail assign at the tail of startActiveSessionFlow)
    src/backend/claude-session/claude-session-server.ts (imports section at the top of the file — check whether `crypto` is already imported: grep for `from "crypto"` or `from "node:crypto"`. If it is not imported, add `import { randomBytes } from "node:crypto";` at the top of the imports block. If it IS imported, reuse the existing import binding.)
    .planning/phases/62-invisible-dormancy-client-side-follow-up-widen-client-pendin/62-CONTEXT.md (Wave 2 section + D-62-01 + D-62-04)
  </read_first>
  <action>
    STEP A — Add or reuse the `randomBytes` import from `node:crypto`. First check with `grep -n 'from "node:crypto"\|from "crypto"' src/backend/claude-session/claude-session-server.ts`. If no existing import of `randomBytes`, add at the top of the imports block:
      ```
      import { randomBytes } from "node:crypto";
      ```

    STEP B — Introduce a closure-scoped `tailInstanceId` variable adjacent to the existing `let tailHandle: TailHandle | null = null;` at line 3133. Add immediately below it:
      ```
      // Phase 62 Wave 2 (D-62-01, D-62-04) — per-tail-watcher instance ID for
      // diagnostic instrumentation. Regenerated on every tail start (fresh
      // startActiveSessionFlow, tail restart in transitionToActiveNew, and
      // dormant-handoff → new tail). Threaded via closure into [dedup],
      // [frame-emit], and [tail-lifecycle] sshLogger.info emissions so a
      // repro log can identify which tail-watcher instance produced each
      // frame — cross-tail races are visible from the ID transitions.
      // Never persisted beyond the closure. 8-char lowercase hex (4 bytes).
      let tailInstanceId: string = "";
      ```

    STEP C — Regenerate `tailInstanceId` at EACH tail-start site:
      1. `src/backend/claude-session/claude-session-server.ts:6284` — the `sshLogger.info("Starting Claude session tail", { ... })` call inside `startActiveSessionFlow`. IMMEDIATELY BEFORE this line, insert:
         ```
         tailInstanceId = randomBytes(4).toString("hex");
         ```
         And EXTEND the existing sshLogger.info's metadata object to add `tailInstanceId,` as a new key. Then IMMEDIATELY AFTER that existing call, add a NEW sshLogger.info emission:
         ```
         sshLogger.info(`[tail-lifecycle] tail=${tailInstanceId} action=start sessionFile=${sessionFile} reason=initial`, {
           operation: "claude_session_tail_lifecycle",
           tailInstanceId,
           action: "start",
           sessionFile,
           reason: "initial",
           sessionId,
           hostId: activeHostId,
           tmuxSession: activeTmuxSession,
         });
         ```
         The `reason` field is `initial` because this is the fresh-attach path. NOTE: `startActiveSessionFlow` is ALSO invoked from the dormant→wake handoff at line 7208 (`startActiveSessionFlow({ pid, sessionFile, tmuxSession: currentTmuxSession!, hostId: currentHostId! });`) — that call re-enters the same function and hits the same lifecycle log. Distinguishing initial vs. dormant-handoff at THIS site requires knowing which caller invoked it. Solution: add an optional `startReason?: "initial" | "dormant-handoff" | "transition"` parameter to `startActiveSessionFlow`'s params object; default to `"initial"` when omitted. At the dormant-handoff call site (line 7208), pass `startReason: "dormant-handoff"`. Wire the `reason:` field of the new lifecycle log to use `startReason ?? "initial"`.

      2. `src/backend/claude-session/claude-session-server.ts:4324` — `tailHandle = tailSessionFile(sshConn, newSessionFile, onLine, onError);` inside `transitionToActiveNew`. IMMEDIATELY BEFORE this line, insert:
         ```
         tailInstanceId = randomBytes(4).toString("hex");
         ```
         And IMMEDIATELY AFTER the tailHandle assignment, add:
         ```
         sshLogger.info(`[tail-lifecycle] tail=${tailInstanceId} action=start sessionFile=${newSessionFile} reason=transition`, {
           operation: "claude_session_tail_lifecycle",
           tailInstanceId,
           action: "start",
           sessionFile: newSessionFile,
           reason: "transition",
         });
         ```

    STEP D — Emit `action=stop` at EACH tail-stop site:
      1. `src/backend/claude-session/claude-session-server.ts:7190-7204` — the `tailHandle.stop()` block inside the dormant-handoff. IMMEDIATELY AFTER the existing `sshLogger.info("Dormant tail stopped for wake handoff", { ... })` call (which sits at :7193-7203), add:
         ```
         sshLogger.info(`[tail-lifecycle] tail=${tailInstanceId} action=stop sessionFile=${currentSessionFile ?? "<unknown>"} reason=dormant-handoff`, {
           operation: "claude_session_tail_lifecycle",
           tailInstanceId,
           action: "stop",
           sessionFile: currentSessionFile ?? null,
           reason: "dormant-handoff",
         });
         ```
         Do NOT reassign `tailInstanceId = ""` here — the next startActiveSessionFlow call (at line 7208, now with `startReason: "dormant-handoff"`) will regenerate it. Leaving the old ID visible in the STOP log then a new ID visible in the START log is exactly the tail-swap trace the diagnostic needs.

      2. `src/backend/claude-session/claude-session-server.ts:3518-3525` — the connection-teardown `tailHandle.stop()` block. Note this block runs on ws.on("close") (final teardown, not a swap). IMMEDIATELY AFTER the existing `tailHandle = null;` line, add:
         ```
         if (tailInstanceId) {
           sshLogger.info(`[tail-lifecycle] tail=${tailInstanceId} action=stop sessionFile=<teardown> reason=connection-teardown`, {
             operation: "claude_session_tail_lifecycle",
             tailInstanceId,
             action: "stop",
             sessionFile: null,
             reason: "connection-teardown",
           });
           tailInstanceId = "";
         }
         ```
         The `if (tailInstanceId)` guard handles the case where the connection closes before any tail was ever started (empty string default from STEP B) — do not emit a spurious stop log for a tail that never existed. The reason value `connection-teardown` is a NEW value not in the D-62-04 spec (which named `initial | transition | dormant-handoff`) — this is a fourth reason capturing the ws-close path. Include it in the SUMMARY as an intentional additive value; do not add it to the D-62-04 spec (which is fixed by CONTEXT.md).

    Do NOT change any existing log-line text, existing operation names, or existing metadata keys. All additions are NEW emissions adjacent to existing ones.
  </action>
  <verify>
    <automated>
      # crypto/randomBytes is imported (new or existing):
      grep -qE '(from "node:crypto"|from "crypto")' src/backend/claude-session/claude-session-server.ts || (echo "FAIL: crypto import missing" && exit 1)
      grep -q 'randomBytes' src/backend/claude-session/claude-session-server.ts || (echo "FAIL: randomBytes not referenced" && exit 1)
      # tailInstanceId declared exactly once as a let:
      test "$(grep -c '^\s*let tailInstanceId' src/backend/claude-session/claude-session-server.ts)" -eq 1 || (echo "FAIL: tailInstanceId not declared exactly once as let" && exit 1)
      # tailInstanceId reassigned via randomBytes(4).toString('hex') at BOTH start sites:
      test "$(grep -c 'tailInstanceId = randomBytes(4).toString("hex")' src/backend/claude-session/claude-session-server.ts)" -ge 2 || (echo "FAIL: expected >=2 tailInstanceId reassignments at tail-start sites" && exit 1)
      # [tail-lifecycle] emitted for start AND stop actions:
      grep -q '\[tail-lifecycle\] tail=\${tailInstanceId} action=start' src/backend/claude-session/claude-session-server.ts || (echo "FAIL: [tail-lifecycle] start emission missing" && exit 1)
      grep -q '\[tail-lifecycle\] tail=\${tailInstanceId} action=stop' src/backend/claude-session/claude-session-server.ts || (echo "FAIL: [tail-lifecycle] stop emission missing" && exit 1)
      # startReason wired for dormant-handoff branch:
      grep -q 'startReason: "dormant-handoff"' src/backend/claude-session/claude-session-server.ts || (echo "FAIL: startReason wire-up missing at dormant-handoff caller" && exit 1)
      # Backend + full app build clean:
      npm run build:backend && npm run build
    </automated>
  </verify>
  <acceptance_criteria>
    - `randomBytes` from `node:crypto` (or `crypto`) is imported.
    - `let tailInstanceId: string = "";` is declared exactly once, adjacent to `let tailHandle`.
    - `tailInstanceId = randomBytes(4).toString("hex")` is executed at every tail-start site — at minimum both the `startActiveSessionFlow` opening (line 6284 vicinity) AND `transitionToActiveNew` (line 4324 vicinity) — greppable count ≥ 2.
    - `[tail-lifecycle] tail=${tailInstanceId} action=start` template-literal string appears at least once; `[tail-lifecycle] tail=${tailInstanceId} action=stop` appears at least once.
    - `startActiveSessionFlow` params include an optional `startReason?: "initial" | "dormant-handoff" | "transition"` field, and the dormant-handoff caller at line 7208 passes `startReason: "dormant-handoff"`.
    - `npm run build:backend && npm run build` exits 0 — no TS errors introduced.
    - No existing sshLogger.info line was modified except to add `tailInstanceId` as an additive metadata key on the existing `Starting Claude session tail` metadata object.
  </acceptance_criteria>
  <done>
    Per-tail-watcher instance ID generated at every tail-start; propagated via closure; emitted at every tail-lifecycle boundary (start + stop) with a reason field. Ready for Task 2 to reference `tailInstanceId` in the [dedup] and [frame-emit] emissions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add [dedup] + [frame-emit] instrumentation at the __applyQueueDedupForTests call site</name>
  <files>src/backend/claude-session/claude-session-server.ts</files>
  <read_first>
    src/backend/claude-session/claude-session-server.ts (specifically: lines 3907-3938 the __applyQueueDedupForTests call block inside the streaming-tail onLine handler — this is where the [dedup] log emits; line 3269 `const queueEnqueueDedup = new Map<string, number>();` — the Map whose size is logged; lines 3933-3938 the `if (!suppress) { try { ws.send(...); } catch { ... } }` block — this is where the [frame-emit] log emits; also lines 1500-1600 vicinity of `__applyQueueDedupForTests` implementation to understand the `result` shape it returns — it currently returns `{ suppress: boolean }`; if a `contentHash` and `rawType` and `operation` are computed inside the helper but not returned, Task 2 must derive them at the call site instead)
    src/backend/claude-session/claude-session-server.ts (result of Task 1 above — `tailInstanceId` is in closure scope; this task references it)
    .planning/phases/62-invisible-dormancy-client-side-follow-up-widen-client-pendin/62-CONTEXT.md (Wave 2 section — the exact three log-line shapes)
  </read_first>
  <action>
    STEP A — Understand what the `__applyQueueDedupForTests` `result` object returns. Locate the function definition (grep `export const __applyQueueDedupForTests\|function __applyQueueDedupForTests\|__applyQueueDedupForTests =`). Read the return type. The call site at :3921-3927 currently destructures only `result.suppress`. For the [dedup] log we need:
      - `contentHash` — the dedup key
      - `rawType` — from `rawObj.type` at the call site (already in scope: `rawObj` was JSON.parsed at :3920)
      - `operation` — from `rawObj.operation` at the call site (in scope)
      - `action` — derived: `suppress === true` → `"suppress"`; `suppress === false && rawObj.type === "queue-operation" && rawObj.operation === "enqueue"` → `"populate"` (the enqueue path is the one that ADDS to the Map); otherwise → `"passthrough"` (user-turn frames that were not previously enqueued).
      - `mapSize` — `queueEnqueueDedup.size` (in closure scope at :3269).

    If the `result` object does NOT expose `contentHash`, EXTEND its return shape to include `contentHash: string` (readable from the internal derivation the helper already computes). This is an ADDITIVE change to the return type — no existing consumers break because the current only-consumer at :3921-3927 destructures only `.suppress`. Update the helper signature comment to note the additive field for Phase 62 Wave 2. If the helper does compute a hash locally but only for internal Map lookup, expose it in the return.

    STEP B — Insert the [dedup] log emission. IMMEDIATELY AFTER the line `suppress = result.suppress;` at :3927, add:
      ```
      const dedupAction: "populate" | "suppress" | "passthrough" =
        result.suppress
          ? "suppress"
          : (typeof rawObj.type === "string" && rawObj.type === "queue-operation" &&
             typeof rawObj.operation === "string" && rawObj.operation === "enqueue")
            ? "populate"
            : "passthrough";
      const dedupContentHash = typeof result.contentHash === "string" ? result.contentHash : "<no-hash>";
      const dedupRawType = typeof rawObj.type === "string" ? rawObj.type : "<no-type>";
      const dedupOperation = typeof rawObj.operation === "string" ? rawObj.operation : "<no-op>";
      sshLogger.info(
        `[dedup] tail=${tailInstanceId} sessionId=${sessionIdFromFile ?? "<no-session>"} contentHash=${dedupContentHash} rawType=${dedupRawType} operation=${dedupOperation} action=${dedupAction} mapSize=${queueEnqueueDedup.size}`,
        {
          operation: "claude_session_dedup_gate",
          tailInstanceId,
          sessionId: sessionIdFromFile,
          contentHash: dedupContentHash,
          rawType: dedupRawType,
          dedupOperation,
          action: dedupAction,
          mapSize: queueEnqueueDedup.size,
        },
      );
      ```
    NOTE: the outer `operation:` metadata key is a fixed string identifying the log emission ("claude_session_dedup_gate"); the runtime `rawObj.operation` value is stored under the metadata key `dedupOperation` to avoid clobbering the outer `operation` key.

    STEP C — Insert the [frame-emit] log emission. Locate the `if (!suppress) { try { ws.send(JSON.stringify(frame)); } catch { ... } }` block at :3933-3938. IMMEDIATELY AFTER the `ws.send(JSON.stringify(frame))` call (still inside the try, so a ws.send throw does not skip the log; and only for kind:"message" role:"user" frames since that is the only kind that entered the dedup gate at :3918), add:
      ```
      if (frame.type === "message" && frame.role === "user") {
        const previewSource = typeof frame.content === "string" ? frame.content : String(frame.content ?? "");
        const contentPreview = previewSource.slice(0, 50).replace(/\r?\n/g, " ");
        sshLogger.info(
          `[frame-emit] tail=${tailInstanceId} eventId=${frame.eventId ?? "<no-id>"} role=${frame.role} contentLen=${previewSource.length} contentPreview=${contentPreview}`,
          {
            operation: "claude_session_frame_emit",
            tailInstanceId,
            eventId: frame.eventId ?? null,
            role: frame.role,
            contentLen: previewSource.length,
            contentPreview,
          },
        );
      }
      ```
    The inner `if (frame.type === "message" && frame.role === "user")` guard mirrors the dedup gate at :3918 — non-user-role frames pass ws.send without dedup logic AND without the [frame-emit] log, keeping log volume bounded to the frames the diagnostic actually cares about. Placement inside the try (after ws.send, before the catch) means the log fires only when ws.send did not throw; if ws.send throws, the catch drops silently as today (this preserves the pre-existing behavior of only logging what actually reached the wire).

    STEP D — Do NOT modify anything else in the surrounding block. Do NOT change __applyQueueDedupForTests's internal logic. Do NOT change queueEnqueueDedup's declaration, TTL, or `.clear()` call sites (D-62-01). Do NOT modify the `notifyPvSendWatchdog(...)` calls at :3939+ (the compose-send watchdog notify path is unrelated to this instrumentation).
  </action>
  <verify>
    <automated>
      # Both new log lines present with the tail-instance-id closure variable:
      grep -q '\[dedup\] tail=\${tailInstanceId}' src/backend/claude-session/claude-session-server.ts || (echo "FAIL: [dedup] log emission missing or does not thread tailInstanceId" && exit 1)
      grep -q '\[frame-emit\] tail=\${tailInstanceId}' src/backend/claude-session/claude-session-server.ts || (echo "FAIL: [frame-emit] log emission missing or does not thread tailInstanceId" && exit 1)
      # Operation names distinct + greppable (used by log-aggregation tooling if any):
      grep -q '"claude_session_dedup_gate"' src/backend/claude-session/claude-session-server.ts || (echo "FAIL: claude_session_dedup_gate operation name missing" && exit 1)
      grep -q '"claude_session_frame_emit"' src/backend/claude-session/claude-session-server.ts || (echo "FAIL: claude_session_frame_emit operation name missing" && exit 1)
      # __applyQueueDedupForTests is NOT modified functionally (only additive return field allowed):
      # Assert queueEnqueueDedup.clear() call count is unchanged (still exactly 1 site in applyTransitionToActiveNewCleanup):
      test "$(grep -c 'queueEnqueueDedup.clear()' src/backend/claude-session/claude-session-server.ts)" -eq 1 || (echo "FAIL: queueEnqueueDedup.clear() call count changed — Wave 2 must not touch dedup behavior (D-62-01)" && exit 1)
      # queueEnqueueDedup declaration unchanged:
      grep -q 'const queueEnqueueDedup = new Map<string, number>()' src/backend/claude-session/claude-session-server.ts || (echo "FAIL: queueEnqueueDedup declaration mutated — must be untouched (D-62-01)" && exit 1)
      # Backend + full app build clean:
      npm run build:backend && npm run build
      # Existing dedup + optimistic-bubble + dormant-tail tests all still green — this is the enforcement that Wave 2 is behavior-neutral:
      npx vitest run src/backend/claude-session/claude-session-server.compose-send.test.ts --reporter=verbose
      npx vitest run src/backend/claude-session/optimistic-bubbles.integration.test.ts --reporter=verbose 2>/dev/null || npx vitest run --testPathPattern optimistic-bubbles.integration --reporter=verbose
      npx vitest run src/backend/claude-session/claude-session-server.dormant-tail.test.ts --reporter=verbose
      npx vitest run src/backend/claude-session/pv-send-watchdog.test.ts --reporter=verbose
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '\[dedup\] tail=\${tailInstanceId}' src/backend/claude-session/claude-session-server.ts` succeeds.
    - `grep -q '\[frame-emit\] tail=\${tailInstanceId}' src/backend/claude-session/claude-session-server.ts` succeeds.
    - `grep -c 'queueEnqueueDedup.clear()' src/backend/claude-session/claude-session-server.ts` returns exactly `1` (no new clear sites — dedup behavior unchanged per D-62-01).
    - `queueEnqueueDedup` Map declaration at :3269 is unchanged (still `new Map<string, number>()`).
    - `__applyQueueDedupForTests` internal logic is UNCHANGED. If its return type was extended with `contentHash: string`, that is the ONLY additive change; no key derivation, TTL, or map-mutation semantics were altered.
    - `npm run build:backend && npm run build` exits 0.
    - All four listed test files (compose-send, optimistic-bubbles.integration, dormant-tail, pv-send-watchdog) exit 0 — proves the instrumentation is behavior-neutral.
  </acceptance_criteria>
  <done>
    [dedup] emits one line per __applyQueueDedupForTests call outcome with tail instance ID + all identifying keys; [frame-emit] emits one line per user-role ws.send passthrough with tail instance ID + preview. The next dormant-send repro from Ashley will produce grep-able logs that pin down which mechanism (if any) emits duplicate user-role frames. Actual fix moves to Phase 63.
  </done>
</task>

<task type="auto">
  <name>Task 3: Ship gate — full vitest suite green (fleet rule 2026-08-20 per D-62-05)</name>
  <files>N/A (verification-only task; no code change)</files>
  <read_first>
    .planning/phases/62-invisible-dormancy-client-side-follow-up-widen-client-pendin/62-CONTEXT.md (D-62-05 — both waves ship in one docker build/deploy; full-suite green is the ship gate; D-62-01 — behavior-neutrality enforcement)
  </read_first>
  <action>
    Run the full vitest suite exactly as the fleet rule (2026-08-20) specifies. Wave 2 touches claude-session-server.ts, which is a high-blast-radius file — many test files stub or drive its exports directly. The full-suite run catches any test that (a) asserts on log output byte-for-byte and would now trip on the new [dedup]/[frame-emit]/[tail-lifecycle] lines, (b) asserts on the tail-start closure state, or (c) counts sshLogger.info emissions per session.

    Run: `npx vitest run`

    Expected: exit 0. If any pre-existing test fails, triage: is it a log-output byte-match assertion (fix: update the test's expected log-output baseline)? Or is it a real regression (fix: revisit the instrumentation — instrumentation MUST NOT change behavior per D-62-01)? Document the outcome in the SUMMARY either way.

    Also run backend + frontend typecheck: `npm run build:backend && npm run build`.

    Wave 2 is ready to ship-bundle with Wave 1 per D-62-05.
  </action>
  <verify>
    <automated>
      npm run build:backend && npm run build
      npx vitest run
    </automated>
  </verify>
  <acceptance_criteria>
    - `npm run build:backend && npm run build` exits 0.
    - `npx vitest run` exits 0 — full suite green with the Wave 2 instrumentation in place.
    - No test file was skipped, filtered out, or renamed to bypass the run.
  </acceptance_criteria>
  <done>
    Full test suite (frontend + backend) green with Wave 2 instrumentation in place. Wave 2 is ready to ship-bundle with Wave 1 per D-62-05.
  </done>
</task>

</tasks>

<verification>
- `npm run build:backend && npm run build` exits 0.
- `npx vitest run` (full suite) exits 0 — no regressions from instrumentation.
- `npx vitest run src/backend/claude-session/claude-session-server.compose-send.test.ts` exits 0 — dedup path unchanged.
- `npx vitest run src/backend/claude-session/claude-session-server.dormant-tail.test.ts` exits 0 — tail-lifecycle path unchanged behaviorally.
- `npx vitest run src/backend/claude-session/pv-send-watchdog.test.ts` exits 0 — watchdog unaffected by log-only additions.
- Manual read: `queueEnqueueDedup` declaration + `.clear()` call sites + `__applyQueueDedupForTests` internal logic all UNCHANGED (D-62-01).
- Manual read: `tailInstanceId` is regenerated at every tail-start site (startActiveSessionFlow open + transitionToActiveNew tailSessionFile call), never persisted beyond the closure, never reused across tails.
- Manual read: three log-line shapes match the D-62-04 specification (`[dedup] ...`, `[frame-emit] ...`, `[tail-lifecycle] ...`) with the exact key names described in CONTEXT.md L50-52.
</verification>

<success_criteria>
- Every __applyQueueDedupForTests call produces a `[dedup]` log line with tail instance ID + all identifying keys.
- Every user-role ws.send passthrough produces a `[frame-emit]` log line with tail instance ID + preview.
- Every tail-watcher start and stop produces a `[tail-lifecycle]` log line with tail instance ID + reason.
- Next dormant-send repro from Ashley produces enough log evidence for Phase 63 to identify (or rule out) a dedup-Map leak mechanism.
- Zero behavioral change — proven by all existing test files remaining green.
- Ship gate: full `npx vitest run` exits 0; backend + frontend builds clean.
</success_criteria>

<output>
Create `.planning/phases/62-invisible-dormancy-client-side-follow-up-widen-client-pendin/62-02-SUMMARY.md` when done, covering: one crypto import (or reuse), one `tailInstanceId` closure var, three log-emission sites ([dedup], [frame-emit], [tail-lifecycle] × start/stop), one additive `startReason` param on startActiveSessionFlow, one extra `connection-teardown` reason value (additive to the D-62-04 spec — call it out as intentional in the summary), zero behavior changes proven by four target test-files remaining green + full-suite green.
</output>
