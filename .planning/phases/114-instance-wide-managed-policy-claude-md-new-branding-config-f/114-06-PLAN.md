---
phase: 112-instance-wide-managed-policy-claude-md-new-branding-config-f
plan: 06
type: execute
wave: 4
depends_on:
  - 112-01
  - 112-02
  - 112-03
  - 112-04
  - 112-05
files_modified:
  - src/backend/distributor/server-substrate-integration.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "T-11 end-to-end integration test exists in server-substrate-integration.test.ts"
    - "T-11 covers the happy path: branding config has instancePolicyFilename set, twinkie file present on the (mocked) Skynet server filesystem, managed host has username='root', catalog includes the twinkie row → after a simulated sweep the managed host receives /etc/claude-code/CLAUDE.md via a captured base64-d write command"
    - "T-11 covers the non-root skip case: same setup but managed host has username='ubuntu' → NO write command emitted for the twinkie row on that host"
    - "T-11 covers the removal case: branding field set but readInstancePolicyBytes returns null (e.g., missing file) → rm -f command emitted for /etc/claude-code/CLAUDE.md on root-SSH hosts, NO removal on non-root hosts"
    - "All pre-existing integration tests still pass (regression envelope preserved)"
    - "Full scoped-test command per D-23 passes: npx vitest run src/backend/branding/ src/backend/distributor/"
    - "D-24: executor scope stops at code + commit + scoped-tests-green — this plan's success_criteria explicitly hands off to the orchestrator for push/build/recreate/verify (executor task list contains NO push/build/deploy/verify-live operations)"
    - "D-25: post-deploy sanity check on t1000 (docker logs clean + /etc/claude-code/CLAUDE.md present with matching bytes) is documented as the orchestrator's step-after-ship — informational anchor only, no executor task"
  artifacts:
    - path: "src/backend/distributor/server-substrate-integration.test.ts"
      provides: "T-11 three-scenario end-to-end integration test"
      contains: "T-11"
  key_links:
    - from: "src/backend/distributor/server-substrate-integration.test.ts"
      to: "The full Plan 01-05 wiring"
      via: "verified end-to-end via the composer's captured exec commands"
      pattern: "T-11"
---

<objective>
Verify Plans 01-05 wire together correctly with an end-to-end integration test in `server-substrate-integration.test.ts`. This is the final gate: with the branding config set to a twinkie filename, a twinkie file mocked on the server filesystem, and a managed host with `username: "root"`, a simulated sweep must land the twinkie's bytes at `/etc/claude-code/CLAUDE.md` on the managed host. Cover three scenarios (happy path, non-root skip, removal) to lock the D-13/D-15/D-16/D-17/D-27 contracts end-to-end.

Purpose: T-11 (per D-22) is the "does the whole thing actually work" gate. Every prior plan tested its own layer; T-11 tests the composition. Also verifies the D-23 full-scoped-test envelope (`npx vitest run src/backend/branding/ src/backend/distributor/`) passes clean before deploy.

Output: T-11 with three sub-scenarios added, full scoped-test suite green, atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/ubuntu/skynet-tina/CLAUDE.md
@/home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-CONTEXT.md
@/home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-RESEARCH.md
@/home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-PATTERNS.md
@/home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add T-11 end-to-end integration test with three scenarios (happy path, non-root skip, removal)</name>
  <files>src/backend/distributor/server-substrate-integration.test.ts</files>
  <read_first>
    - src/backend/distributor/server-substrate-integration.test.ts (full 1079 lines — inspect the existing scenario-setup pattern, the mocks it uses for SSH channels + listSubstrateHosts + acquireChannel, the way it invokes createServerSubstrateOrchestrator, and its assertion style; T-11 must mirror that scenario-setup shape so it slots in cleanly)
    - src/backend/distributor/run-sweep.ts (as modified by Plan 05)
    - src/backend/distributor/server-substrate-orchestrator.ts (as modified by Plan 05)
    - /home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-PATTERNS.md § server-substrate-integration.test.ts pattern (L780-784 — mirror the existing scenario-setup shape)
    - /home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-CONTEXT.md D-22 (T-11 = end-to-end integration test)
  </read_first>
  <behavior>
    - Test T-11a (happy path): mock `readInstancePolicyBytes` to return `Buffer.from("# twinkie\n\ntest content")`; mock `listSubstrateHosts` to return one host with `_connDetails.username = "root"`; mock the acquired channel's exec to respond with `__READ_ENOENT__` on the read-installed-bytes call for `/etc/claude-code/CLAUDE.md` (first-install case) and `__WRITE_OK__` on the write call. After the orchestrator's start() completes, assert:
      - The channel's exec was called with a command containing `base64 -d > '/etc/claude-code/CLAUDE.md'` AND `chown root:root '/etc/claude-code/CLAUDE.md'` AND `chmod 644 '/etc/claude-code/CLAUDE.md'` (Plan 03 Task 1's system-root write shape).
      - The base64 body passed via stdin matches `Buffer.from("# twinkie\n\ntest content").toString("base64")`.
      - `readInstancePolicyBytes` was called exactly ONCE (Pitfall 6 / D-15 verification).
    - Test T-11b (non-root skip): same setup but `_connDetails.username = "ubuntu"`. After start() completes, assert:
      - NO exec call was made containing `/etc/claude-code/CLAUDE.md` (the row was gated out entirely).
      - The `sshLogger.info` spy was called with `operation: "fleet_substrate_system_root_skip"` for that (host, row) pair.
    - Test T-11c (removal on root host when field cleared): mock `readInstancePolicyBytes` to return `null`; mock `listSubstrateHosts` to return one host with `_connDetails.username = "root"`; mock the channel's exec to respond with `__REMOVE_DID__` on the rm command. After start() completes, assert:
      - The channel's exec was called with a command containing `rm -f '/etc/claude-code/CLAUDE.md'` (Plan 03 Task 2's removeInstalledFile shape) AND `__REMOVE_DID__` (sentinel in the emitted command source, not stdout).
      - NO base64 write command was emitted for the twinkie row.
    - All three scenarios preserve the fire-and-forget contract — start() resolves cleanly regardless of any per-item failure.
  </behavior>
  <action>
    Follow patterns in 114-PATTERNS.md § server-substrate-integration.test.ts.

    1) Read the existing `server-substrate-integration.test.ts` file top-to-bottom to understand:
       - How it constructs the orchestrator deps object (listSubstrateHosts / acquireChannel / releaseChannel / setInterval / clearInterval / now).
       - How it mocks the SSH channel's exec (probably a scripted response array or a dispatcher based on the command string).
       - How it mocks `bundledReaderFromDisk` for the existing 24 bundled rows (so they don't spuriously interact with the twinkie assertions).
       - Its convention for mocking `readInstancePolicyBytes` from `../branding/branding-config-loader.js` — grep for existing branding-loader mocks; if none, add one at the top of the file.

    2) Add a `describe("T-11: Phase 114 twinkie end-to-end", () => { … })` block near the end of the file (before the closing brace of the outermost describe, if any).

    3) Inside the T-11 describe, add three `it()` tests per the behavior spec above (T-11a happy path, T-11b non-root skip, T-11c removal).

    4) Shared test scaffolding (may go in a beforeEach or a helper function inside the describe):
       - Mock `readInstancePolicyBytes` per-scenario using `vi.mock` at module scope OR by overriding a shared state field. The existing test file's style dictates the choice.
       - Mock `bundledReaderFromDisk` to return null (or a valid buffer) for every existing catalog entry so those rows don't trigger writes that pollute the T-11 exec assertions. Simplest: return `{ bytes: Buffer.from(""), mode: 0o755 }` and mock installed reads to return matching empty bytes → decision layer skips all bundled rows.
       - Mock the channel's exec dispatcher to route based on command shape:
         - `base64 -w0 '/etc/claude-code/CLAUDE.md'` → `__READ_ENOENT__` (first-install case for T-11a) OR the twinkie bytes' base64 (byte-compare match, no-op case, if a fourth scenario is desired).
         - `base64 -d > '/etc/claude-code/CLAUDE.md'` (write command) → `__WRITE_OK__`.
         - `if [ -f '/etc/claude-code/CLAUDE.md' ]; then rm -f` (removal command) → `__REMOVE_DID__`.
         - Any other command → default response for bundled-row byte-compare (e.g., `__READ_OK__` with matching bytes).

    5) Assertion helpers:
       - `capturedCommands = exec.mock.calls.map(c => c[0] as string)` collects all commands.
       - `expect(capturedCommands.some(c => c.includes("chown root:root") && c.includes("/etc/claude-code/CLAUDE.md"))).toBe(true)` for T-11a.
       - `expect(capturedCommands.some(c => c.includes("/etc/claude-code/"))).toBe(false)` for T-11b.
       - `expect(capturedCommands.some(c => c.includes("rm -f") && c.includes("/etc/claude-code/CLAUDE.md"))).toBe(true)` for T-11c.

    6) If the existing integration test file already has scenario helpers (e.g., `runSweepWithHost({...})`), reuse them; add T-11 as three new invocations of the same helpers with different mock configurations. If the shape is bespoke per scenario, mirror the closest existing scenario's setup shape.
  </action>
  <verify>
    <automated>npx vitest run src/backend/distributor/server-substrate-integration.test.ts --reporter=basic && npx vitest run src/backend/branding/ src/backend/distributor/ --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -q "T-11" src/backend/distributor/server-substrate-integration.test.ts` succeeds.
    - Source assertion: `grep -c "T-11" src/backend/distributor/server-substrate-integration.test.ts` returns >= 3 (at least three sub-scenario references).
    - Source assertion: `grep -q "/etc/claude-code/CLAUDE.md" src/backend/distributor/server-substrate-integration.test.ts` succeeds (twinkie install path referenced in tests).
    - Source assertion: `grep -q "fleet_substrate_system_root_skip" src/backend/distributor/server-substrate-integration.test.ts` succeeds (non-root skip scenario asserts on the log operation).
    - Source assertion: `grep -q "rm -f\|removed" src/backend/distributor/server-substrate-integration.test.ts | grep -q "T-11"` — verify removal scenario present near T-11 (executor uses judgment; the goal is `rm -f` or `action: "removed"` appears in T-11c's assertions).
    - Behavior: `npx vitest run src/backend/distributor/server-substrate-integration.test.ts --reporter=basic` exits 0 with three new T-11 test cases passing.
    - Full scoped suite (D-23 gate): `npx vitest run src/backend/branding/ src/backend/distributor/ --reporter=basic` exits 0. This is the ship-gate boundary — orchestrator's remit begins after this passes green.
    - Never-reject: no test scenario causes start() to reject; all tests use `await orchestrator.start()` cleanly.
  </acceptance_criteria>
  <done>
    T-11 three-scenario integration test lands in server-substrate-integration.test.ts covering happy path + non-root skip + removal; full scoped suite (`npx vitest run src/backend/branding/ src/backend/distributor/`) exits 0; all pre-existing integration tests still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Integration test operates on mocked SSH channels + mocked filesystem — no real trust boundary crossings. The mitigations verified end-to-end here are:

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-16 | Integration regression | Plans 01-05 wire together incorrectly | mitigate | T-11 covers the three critical end-to-end paths: (a) happy-path push (Plans 01+02+03+05 composition), (b) non-root skip (D-13 gate + D-26 log verified end-to-end), (c) removal (D-16 + D-27 composition verified end-to-end). Any regression that breaks composition between plans fails T-11. |
| T-114-17 | Silent-deploy risk | Ship-gate green with a broken feature | mitigate | D-23 scoped-test envelope (`npx vitest run src/backend/branding/ src/backend/distributor/`) covers every file the phase touches. This plan's acceptance criteria include running that envelope; if it fails, orchestrator's ship motion halts. |

Package Legitimacy Gate: N/A — no external packages.
</threat_model>

<verification>
```
npx vitest run src/backend/distributor/server-substrate-integration.test.ts --reporter=basic
npx vitest run src/backend/branding/ src/backend/distributor/ --reporter=basic
```

Both must exit 0. The second command is D-23's executor green-gate — the scoped-test envelope that ships this phase.
</verification>

<success_criteria>
Plan 06 succeeds when: (1) T-11 three-scenario integration test lands; (2) `npx vitest run src/backend/distributor/server-substrate-integration.test.ts --reporter=basic` exits 0; (3) `npx vitest run src/backend/branding/ src/backend/distributor/ --reporter=basic` exits 0 (D-23 executor green-gate cleared); (4) all source-assertion greps in acceptance_criteria pass; (5) atomic commit produced. Executor's remit stops here (D-24) — orchestrator owns push/build/recreate/verify.
</success_criteria>

<output>
Create `.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-06-SUMMARY.md` documenting: T-11 scenarios, mock setup shape, ship-gate confirmation (D-23 envelope green), remaining orchestrator-owned steps (deploy motion per D-24 + D-25 sanity check).
</output>
