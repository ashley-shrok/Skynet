---
phase: 112-instance-wide-managed-policy-claude-md-new-branding-config-f
plan: 03
type: execute
wave: 2
depends_on:
  - 112-02
files_modified:
  - src/backend/distributor/ssh-push.ts
  - src/backend/distributor/ssh-push.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "writeInstalledBytesWithMode accepts an installMode option ('user-home' | 'system-root') defaulting to 'user-home'"
    - "For installMode='system-root': the emitted command uses shellSingleQuote() (not quotePathPreservingTilde), creates /etc/claude-code with root:root 0755, writes the file, chowns root:root, chmods 0644, and includes a symlink guard (test -f AND test ! -L) that FAILS the write if the target is a symlink"
    - "For installMode='user-home' (default): the emitted command is byte-identical to today's shape (quotePathPreservingTilde, no chown, no symlink guard) — Plan 02's 24 bundled rows must ship unchanged runtime behavior"
    - "New export removeInstalledFile(channel, installPath) exists in ssh-push.ts, returns { ok: true, action: 'removed' | 'already-absent' } | { ok: false, stage: 'remove' | 'verify', errorMessage: string }, and never throws"
    - "removeInstalledFile uses shellSingleQuote (absolute path, no tilde-expansion) and dispatches on sentinels __REMOVE_DID__ / __REMOVE_ALREADY__ / __REMOVE_FAIL__"
    - "T-09 test asserts: system-root write emits absolute-path single-quoted, no ~/ literal, chown root:root present, chmod 644 present"
    - "T-10 test asserts: byte-compare skip regression — readInstalledBytes bytes matching source bytes → writeInstalledBytesWithMode NOT called (though byte-compare lives in run-sweep, the regression guard belongs at push-helper level as a call-count assertion)"
    - "New removeInstalledFile test suite covers: __REMOVE_DID__ → removed, __REMOVE_ALREADY__ → already-absent, transport failure → ok:false stage:remove, absolute-path shell-quoting invariant"
    - "Symlink-guard test: if the post-write test finds the path is a symlink, the write is reported as ok:false stage:'verify' with an errorMessage containing 'symlink'"
  artifacts:
    - path: "src/backend/distributor/ssh-push.ts"
      provides: "writeInstalledBytesWithMode with installMode option + new removeInstalledFile export + symlink-guard post-condition on system-root writes"
      contains: "removeInstalledFile"
    - path: "src/backend/distributor/ssh-push.test.ts"
      provides: "T-09 system-root write test + T-10 byte-compare skip guard + removeInstalledFile test suite + symlink-guard test"
      contains: "installMode"
  key_links:
    - from: "src/backend/distributor/ssh-push.ts"
      to: "shellSingleQuote at discover-identity-session-file.ts:246"
      via: "existing import at L35"
      pattern: "shellSingleQuote"
    - from: "src/backend/distributor/ssh-push.ts"
      to: "sentinels __REMOVE_DID__ / __REMOVE_ALREADY__ / __REMOVE_FAIL__"
      via: "consumed by Plan 05 sweep composer removal branch"
      pattern: "__REMOVE_DID__"
---

<objective>
Extend the push transport layer to support `installMode: "system-root"` writes (absolute path, chown root:root, chmod 0644, mkdir parent 0755) and add a new peer helper `removeInstalledFile()` for the D-27 removal-push motion (fired when the runtime resolver returns null). Add a defense-in-depth symlink-guard post-condition on system-root writes (per RESEARCH.md § Security Domain last row — mitigates the "push over symlinked /etc/claude-code/CLAUDE.md on a rooted host" threat vector; guards the user's fleet against a rooted managed host stomping /etc/passwd via a symlink at the twinkie path).

Purpose: The push helpers are the SSH-transport adapter layer. Plan 05's sweep composer needs both the system-root write shape and the removal shape wired up before it can call them. This plan is dependency-blocked only by Plan 02 (needs the CatalogEntry discriminated-union types to reference `installMode` in the write helper signature); Plan 01 is not needed for this plan.

Output: `writeInstalledBytesWithMode` with `installMode` option + symlink guard on system-root writes, `removeInstalledFile` new export, T-09 + T-10 + removeInstalledFile test suite + symlink-guard test all passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/ubuntu/skynet-tina/CLAUDE.md
@/home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-CONTEXT.md
@/home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-RESEARCH.md
@/home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend writeInstalledBytesWithMode with installMode branch + symlink-guard post-condition</name>
  <files>src/backend/distributor/ssh-push.ts, src/backend/distributor/ssh-push.test.ts</files>
  <read_first>
    - src/backend/distributor/ssh-push.ts (full 268 lines — see quotePathPreservingTilde at L47-52, InstalledReadResult at L61-64, writeInstalledBytesWithMode at L146-213 (current signature + atomic exec pattern L168-169), NEVER-THROW CONTRACT at L26-33)
    - src/backend/distributor/ssh-push.test.ts (full ~380 lines — see makeChannel at L31-38, existing write tests, tilde-preservation regression at L212-238)
    - /home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-PATTERNS.md § ssh-push.ts pattern (L274-376), especially L302-316 for the installMode branch on path quoting, L319-331 for the system-root command shape (mkdir/chown/chmod chain), and § ssh-push.test.ts pattern (L668-725) for T-09 and T-10 shapes
    - /home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-RESEARCH.md § Pitfall 2 (L558-573 — path expansion mixup + T-09 assertion strengthening), § Security Domain (L840-867 — especially the last row for the symlink post-write invariant recommendation), § Assumption A6 (L819 — chown is defense-in-depth on already-root SSH)
    - /home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-CONTEXT.md D-13 (system-root push mechanics + root-user gate), D-18 (managed-host on-disk invariant: root:root 0644, dir 0755), D-19 (byte-compare mechanism)
  </read_first>
  <behavior>
    - Signature extension: `writeInstalledBytesWithMode(channel, installPath, bytes, modeOctal, opts?)` where `opts` is `{ installMode?: "user-home" | "system-root" }` defaulting to `"user-home"`. Backward-compat: every existing call site passing 4 args continues to work (opts defaults trigger user-home behavior).
    - Test T-user-home (regression): calling with no opts (or `{ installMode: "user-home" }`) emits a command that includes `quotePathPreservingTilde`-shaped output (retains `~/` literal for shell home-expansion), and does NOT include `chown root:root` or `test -L`. Byte-identical shape to today.
    - Test T-09 (D-22 + Pitfall 2): calling with `{ installMode: "system-root" }` and `installPath: "/etc/claude-code/CLAUDE.md"` emits a command that:
      - Contains `'/etc/claude-code/CLAUDE.md'` (single-quoted absolute path).
      - Does NOT contain `~/` anywhere.
      - Contains `mkdir -p '/etc/claude-code'` (or equivalent).
      - Contains `chmod 0755 '/etc/claude-code'` OR `chmod 755 '/etc/claude-code'` (parent dir mode; use whichever the toString(8) of 0o755 produces — 755, no leading zero).
      - Contains `chown root:root '/etc/claude-code'` and `chown root:root '/etc/claude-code/CLAUDE.md'` (defense-in-depth per A6).
      - Contains `chmod 644 '/etc/claude-code/CLAUDE.md'` (file mode from modeOctal argument; the caller passes 0o644 per D-18).
      - Contains a symlink-guard post-condition: after write+chown+chmod, verify `test -f '/etc/claude-code/CLAUDE.md' && test ! -L '/etc/claude-code/CLAUDE.md'`. If the guard fails, the command emits a distinct failure sentinel (e.g., `__WRITE_SYMLINK_FAIL__`).
    - Test T-symlink-guard: with a mocked channel returning stdout matching `__WRITE_SYMLINK_FAIL__`, the helper returns `{ ok: false, stage: "verify", errorMessage: <contains "symlink"> }`.
    - Test T-10 (D-22 byte-compare skip regression): although byte-compare lives in run-sweep, add a coverage note here. Deferred to Plan 05 tests if not naturally covered here — but the ssh-push.test.ts existing tilde-preservation regression (L212-238) MUST continue to pass unchanged.
    - Never-throws contract: unchanged — a throw is still caught and returned as `{ ok: false, ..., errorMessage: "__THROW__ <msg>" }`.
  </behavior>
  <action>
    Follow patterns in 114-PATTERNS.md § ssh-push.ts + 114-RESEARCH.md § Pitfall 2 + § Security Domain (symlink threat).

    1) In `src/backend/distributor/ssh-push.ts`:
       - Update the `writeInstalledBytesWithMode` signature at L146-151 to accept an optional `opts` parameter:
         ```
         export async function writeInstalledBytesWithMode(
           channel: SshChannel,
           installPath: string,
           bytes: Buffer,
           modeOctal: number,
           opts?: { installMode?: "user-home" | "system-root" },
         ): Promise<{ ok: true } | { ok: false; stage: "write" | "chmod" | "verify"; errorMessage: string }>
         ```
         Note: extends the discriminated-union failure result to include `stage: "verify"` for the symlink-guard failure. Update the JSDoc at L114-145 to document the new option and the extended failure stages.
       - Inside the function body (currently at L152-213):
         - Compute `const installMode = opts?.installMode ?? "user-home";`.
         - Compute `const escapedPath` conditionally: if `installMode === "system-root"` → use `shellSingleQuote(installPath)`; else → use `quotePathPreservingTilde(installPath)`. Same conditional for `escapedParent` (parent dir).
         - For `installMode === "user-home"`: keep the existing command shape at L168-169 exactly (byte-identical for the 24 existing catalog rows). Preserve the M2 `{ … ; } 2>&1` wrapper and the __WRITE_OK__/__WRITE_FAIL__ sentinels.
         - For `installMode === "system-root"`: emit a distinct command that chains: `mkdir -p <parent>` → `chown root:root <parent>` → `chmod 0755 <parent>` → `base64 -d > <path>` → `chown root:root <path>` → `chmod <modeStr> <path>` → SYMLINK GUARD: `test -f <path> && test ! -L <path> && echo __WRITE_OK__ || echo __WRITE_SYMLINK_FAIL__`. Wrap the full chain in `{ …; } 2>&1` matching the M2 pattern. On any step failure before the guard, emit `__WRITE_FAIL__` per the existing shape.
         - Concrete command template (system-root branch, single line for exec):
           ```
           { mkdir -p <escapedParent> && chown root:root <escapedParent> && chmod 0755 <escapedParent> && base64 -d > <escapedPath> && chown root:root <escapedPath> && chmod <modeStr> <escapedPath> && { test -f <escapedPath> && test ! -L <escapedPath> && echo __WRITE_OK__ || echo __WRITE_SYMLINK_FAIL__ ; } || echo __WRITE_FAIL__ ; } 2>&1
           ```
           (The nested `{ test -f … && test ! -L … }` runs only after mkdir+chown+chmod+base64+chown+chmod all succeed; if the target is a symlink at that point, the outer chain's success clause outputs __WRITE_SYMLINK_FAIL__ instead of __WRITE_OK__.)
         - Extend the sentinel-dispatch block (currently L184-200): after checking `endsWith("__WRITE_OK__")` (return ok:true), add a check for `endsWith("__WRITE_SYMLINK_FAIL__")` → return `{ ok: false, stage: "verify", errorMessage: "post-write invariant failed: target is a symlink at " + installPath + " (T-114-SYMLINK mitigation) — refusing to trust the file at that path" }`. Then fall through to the existing __WRITE_FAIL__ / stage-inference branch.
         - Preserve the outer try/catch never-throws contract (L201-212) unchanged.
       - Add a new export `removeInstalledFile` at the bottom of the file (after restartUserUnit) — DEFER this to Task 2 of this plan to keep the diff focused. Task 1 stops here.

    2) In `src/backend/distributor/ssh-push.test.ts`:
       - Add a new describe block or extend the existing `writeInstalledBytesWithMode` block with:
         - T-09 test (per 114-PATTERNS.md L695-706): mock channel returns `__WRITE_OK__`; call `writeInstalledBytesWithMode(channel, "/etc/claude-code/CLAUDE.md", Buffer.from("twinkie-content"), 0o644, { installMode: "system-root" })`; capture `exec.mock.calls[0][0]` as the command string; assert it CONTAINS `'/etc/claude-code/CLAUDE.md'` (single-quoted absolute), does NOT CONTAIN `~/`, CONTAINS `chown root:root`, CONTAINS `chmod 644`, CONTAINS `test ! -L`.
         - T-user-home-regression test: mock channel returns `__WRITE_OK__`; call `writeInstalledBytesWithMode(channel, "~/.claude/skills/x", Buffer.from("data"), 0o755)` (no opts); assert command CONTAINS `~/` (tilde-preservation intact) AND does NOT CONTAIN `chown root:root` AND does NOT CONTAIN `test ! -L` (user-home path untouched).
         - T-symlink-guard test: mock channel returns `__WRITE_SYMLINK_FAIL__`; call `writeInstalledBytesWithMode(channel, "/etc/claude-code/CLAUDE.md", Buffer.from("data"), 0o644, { installMode: "system-root" })`; assert result `{ ok: false, stage: "verify", errorMessage: <string containing "symlink"> }`.
       - Keep the existing tilde-preservation regression test at L212-238 unchanged — it must still pass.
  </action>
  <verify>
    <automated>npx vitest run src/backend/distributor/ssh-push.test.ts --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -q "opts?: { installMode?: \"user-home\" | \"system-root\" }" src/backend/distributor/ssh-push.ts` succeeds (signature extended).
    - Source assertion: `grep -q "__WRITE_SYMLINK_FAIL__" src/backend/distributor/ssh-push.ts` succeeds (symlink-guard sentinel present).
    - Source assertion: `grep -q "test -f.*test ! -L" src/backend/distributor/ssh-push.ts` succeeds (or equivalent shell fragment — the two tests exist in that order in the emitted command).
    - Source assertion: `grep -q "chown root:root" src/backend/distributor/ssh-push.ts` succeeds (system-root branch present).
    - Source assertion: `grep -q "installMode === \"system-root\"" src/backend/distributor/ssh-push.ts` succeeds (branch conditional visible).
    - Behavior: `npx vitest run src/backend/distributor/ssh-push.test.ts --reporter=basic` exits 0.
    - Regression gate: `grep -q "not.toContain(\"'~/\")" src/backend/distributor/ssh-push.test.ts` succeeds (existing tilde-preservation regression test at L212-238 preserved).
    - Behavior: T-09 test title contains "system-root" or "T-09" and passes; T-symlink-guard test asserts stage: "verify" and passes.
    - Never-throws contract: `grep -c "^throw " src/backend/distributor/ssh-push.ts` returns 0 (no bare throws outside catch blocks — the function still cannot escape a throw upward).
  </acceptance_criteria>
  <done>
    writeInstalledBytesWithMode branches on installMode, system-root path emits absolute-path single-quoted commands with chown root:root + chmod + symlink guard, user-home path is byte-identical to today, T-09 + T-user-home-regression + T-symlink-guard tests pass, existing tests still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add removeInstalledFile helper + full test suite</name>
  <files>src/backend/distributor/ssh-push.ts, src/backend/distributor/ssh-push.test.ts</files>
  <read_first>
    - src/backend/distributor/ssh-push.ts (as modified by Task 1 — see readInstalledBytes L76-112 for the sibling sentinel-dispatch shape; existing restartUserUnit at L233-267 as a similarly-shaped peer helper)
    - src/backend/distributor/ssh-push.test.ts (as modified by Task 1)
    - /home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-PATTERNS.md § removeInstalledFile pattern (L333-364), § removeInstalledFile test suite (L708-724)
    - /home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-RESEARCH.md § Pattern 4 (L374-423 — full canonical shape)
    - /home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-CONTEXT.md D-16 (removal semantics), D-27 (removal-push mechanics: absolute-path, no sudo, sentinel-based dispatch, restartHook null)
  </read_first>
  <behavior>
    - Signature: `export async function removeInstalledFile(channel: SshChannel, installPath: string): Promise<{ ok: true; action: "removed" | "already-absent" } | { ok: false; stage: "remove" | "verify"; errorMessage: string }>`. Never throws.
    - Emitted command uses `shellSingleQuote(installPath)` (absolute path, no tilde-preservation — installPath is expected to be `/etc/claude-code/CLAUDE.md` in practice, but the helper is path-agnostic). No `sudo`.
    - Emitted command dispatches on sentinels: `__REMOVE_DID__` (file existed, rm succeeded) → return `{ ok: true, action: "removed" }`; `__REMOVE_ALREADY__` (file was already absent) → return `{ ok: true, action: "already-absent" }`; `__REMOVE_FAIL__` (else branch — path exists but is not a regular file, or rm returned non-zero) → return `{ ok: false, stage: "verify", errorMessage: <trimmed stdout> }`.
    - Transport failure (channel.exec returns ok:false) → return `{ ok: false, stage: "remove", errorMessage: <res.message.slice(0, 200)> }`.
    - Uncaught throw → return `{ ok: false, stage: "remove", errorMessage: "__THROW__ <msg>" }` (mirrors Phase 111 M1 pattern).
    - Test T-27a: channel returns `__REMOVE_DID__` → helper returns `{ ok: true, action: "removed" }`.
    - Test T-27b: channel returns `__REMOVE_ALREADY__` → helper returns `{ ok: true, action: "already-absent" }`.
    - Test T-27c: channel returns transport failure (ok:false) → helper returns `{ ok: false, stage: "remove", errorMessage: <string> }`.
    - Test T-27d: emitted command CONTAINS `'/etc/claude-code/CLAUDE.md'` (absolute single-quoted) AND does NOT CONTAIN `~/`.
  </behavior>
  <action>
    Follow patterns in 114-PATTERNS.md § removeInstalledFile + 114-RESEARCH.md § Pattern 4.

    1) In `src/backend/distributor/ssh-push.ts`:
       - Append a new export `removeInstalledFile` after the existing `restartUserUnit` function (bottom of file). Use the exact shape from 114-RESEARCH.md § Pattern 4 (L386-417):
         ```
         export async function removeInstalledFile(
           channel: SshChannel,
           installPath: string,
         ): Promise<
           | { ok: true; action: "removed" | "already-absent" }
           | { ok: false; stage: "remove" | "verify"; errorMessage: string }
         > {
           try {
             const escaped = shellSingleQuote(installPath); // absolute path — no tilde
             const cmd =
               `{ if [ -f ${escaped} ]; then rm -f ${escaped} && echo __REMOVE_DID__ ; ` +
               `elif [ ! -e ${escaped} ]; then echo __REMOVE_ALREADY__ ; ` +
               `else echo __REMOVE_FAIL__ ; fi ; } 2>&1`;
             const res = await channel.exec(cmd);
             if (!res.ok) {
               return { ok: false, stage: "remove", errorMessage: res.message.slice(0, 200) };
             }
             const trimmed = res.stdout.trimEnd();
             if (trimmed.endsWith("__REMOVE_DID__")) return { ok: true, action: "removed" };
             if (trimmed.endsWith("__REMOVE_ALREADY__")) return { ok: true, action: "already-absent" };
             return { ok: false, stage: "verify", errorMessage: trimmed.slice(0, 500) };
           } catch (err) {
             return {
               ok: false,
               stage: "remove",
               errorMessage: `__THROW__ ${err instanceof Error ? err.message : "unknown"}`,
             };
           }
         }
         ```
       - Add a JSDoc block above the export documenting: (a) never-throws contract, (b) sentinel-based dispatch (`__REMOVE_DID__` / `__REMOVE_ALREADY__` / `__REMOVE_FAIL__`), (c) NO sudo (the D-13 root-user gate at Plan 05's composer level ensures this is only called on hosts where SSH is already root), (d) idempotent semantics (already-absent is a success outcome, not a failure), (e) uses shellSingleQuote (absolute path, tilde-preservation would be semantically wrong for /etc/ paths per Pitfall 2).

    2) In `src/backend/distributor/ssh-push.test.ts`:
       - Add a new `describe("removeInstalledFile", () => { … })` block per 114-PATTERNS.md L710-723:
         - T-27a: mock channel exec returns `__REMOVE_DID__` → assert `{ ok: true, action: "removed" }`.
         - T-27b: mock channel exec returns `__REMOVE_ALREADY__` → assert `{ ok: true, action: "already-absent" }`.
         - T-27c: mock channel exec returns `{ ok: false, reason: "transport", message: "channel died" }` → assert `{ ok: false, stage: "remove", errorMessage: <starts with "channel died" or subset> }`.
         - T-27d: mock channel captures the command string → assert `cmd.includes("'/etc/claude-code/CLAUDE.md'")` AND `!cmd.includes("~/")` AND `cmd.includes("__REMOVE_DID__")` (sentinel visible in command source, not in stdout).
       - Import the new export at the top of the test file: extend the `import { … } from "./ssh-push.js";` line.
  </action>
  <verify>
    <automated>npx vitest run src/backend/distributor/ssh-push.test.ts --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -q "^export async function removeInstalledFile" src/backend/distributor/ssh-push.ts` succeeds.
    - Source assertion: `grep -q "__REMOVE_DID__" src/backend/distributor/ssh-push.ts` succeeds.
    - Source assertion: `grep -q "__REMOVE_ALREADY__" src/backend/distributor/ssh-push.ts` succeeds.
    - Source assertion: `grep -q "__REMOVE_FAIL__" src/backend/distributor/ssh-push.ts` succeeds.
    - Source assertion: `grep -c "sudo" src/backend/distributor/ssh-push.ts` returns 0 (no sudo used in the emitted command).
    - Source assertion: `grep -q "describe(\"removeInstalledFile" src/backend/distributor/ssh-push.test.ts` succeeds (test suite present).
    - Behavior: `npx vitest run src/backend/distributor/ssh-push.test.ts --reporter=basic` exits 0 with 4 new removeInstalledFile test cases passing (T-27a..T-27d).
    - Never-throws: `grep -A 30 "^export async function removeInstalledFile" src/backend/distributor/ssh-push.ts | grep -v '^#' | grep -c "^\s*throw"` returns 0 (function catches its own throws and returns discriminated-union failures).
  </acceptance_criteria>
  <done>
    removeInstalledFile is exported from ssh-push.ts with the D-27 sentinel-based dispatch, uses shellSingleQuote (no sudo, no tilde), never throws, and T-27a..T-27d tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skynet server → managed host filesystem via SSH | Commands emitted by ssh-push.ts run on the managed host with the SSH user's privileges. For `installMode: "system-root"` rows, that user is `root` (enforced by Plan 05's composer-level gate D-13). |
| managed host `/etc/claude-code/CLAUDE.md` write path | The target path is authored in-repo (catalog.ts D-14), NOT admin input. But a compromised managed host could place a symlink at that path pre-write. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-06 | Tampering | Symlink attack at `/etc/claude-code/CLAUDE.md` — rooted managed host places a symlink to `/etc/passwd` (or similar) before the sweep runs, causing `base64 -d > <path>` to stomp the target of the symlink | mitigate | Post-write invariant guard: after write+chown+chmod, the emitted command runs `test -f <path> && test ! -L <path>` — if either check fails, the helper returns `{ ok: false, stage: "verify", errorMessage: <contains "symlink"> }` and NO further pushes happen for that host on that sweep (composer marks the row failed). T-symlink-guard test verifies. Defense-in-depth: mgmt host root already compromised is game-over on other fronts, but this guard prevents the twinkie push from becoming the attack vector. |
| T-114-07 | Tampering | Path-quoting confusion between user-home and system-root rows | mitigate | Explicit branch in writeInstalledBytesWithMode on installMode: `system-root` uses `shellSingleQuote` (absolute paths, no tilde-expansion); `user-home` continues using `quotePathPreservingTilde` (preserves `~/` for shell expansion). T-09 asserts the emitted command contains `'/etc/claude-code/…'` single-quoted and NO `~/` for system-root; T-user-home-regression asserts the inverse for user-home. |
| T-114-08 | Denial of Service | rm -f on an unexpected path shape | mitigate | removeInstalledFile guards by `test -f` before rm; the fi-branch on else emits `__REMOVE_FAIL__` (no rm attempted) for non-regular-file entries. sentinel-based dispatch surfaces this to the composer as `{ ok: false, stage: "verify" }`. |
| T-114-SC | Tampering | npm/pip/cargo installs | accept | N/A — Phase 114 installs zero external packages (per 114-RESEARCH.md § Package Legitimacy Audit). |
</threat_model>

<verification>
```
npx vitest run src/backend/distributor/ssh-push.test.ts --reporter=basic
```
</verification>

<success_criteria>
Plan 03 succeeds when: (1) `npx vitest run src/backend/distributor/ssh-push.test.ts --reporter=basic` exits 0 with T-09, T-user-home-regression, T-symlink-guard, and T-27a..T-27d all passing; (2) existing tilde-preservation regression at ssh-push.test.ts:L212-238 still passes; (3) all source-assertion greps in acceptance_criteria pass; (4) atomic commit(s) produced.
</success_criteria>

<output>
Create `.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-03-SUMMARY.md` documenting: writeInstalledBytesWithMode signature extension, symlink-guard mechanics, removeInstalledFile shape, sentinel names, test additions.
</output>
