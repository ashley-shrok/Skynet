---
phase: 112-instance-wide-managed-policy-claude-md-new-branding-config-f
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/distributor/catalog.ts
  - src/backend/distributor/catalog.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "CatalogEntry is a discriminated union on sourceKind — BundledCatalogEntry (existing 24 rows) and RuntimeCatalogEntry (twinkie)"
    - "sourceKind: 'bundled' is added as a literal string to all 24 existing rows; every bundledPath/installPath/restartHook value stays byte-identical to today"
    - "installMode is an optional field defaulting to 'user-home'; existing 24 rows either omit it or set it to 'user-home'"
    - "New twinkie row exists in FLEET_SUBSTRATE_CATALOG with slug='instance-policy-claude-md', sourceKind='runtime', resolverKey='instance-policy', installPath='/etc/claude-code/CLAUDE.md', installMode='system-root', restartHook=null"
    - "catalog.ts contains NO runtime imports (no filesystem, no SSH, no logger) — pure-lib discipline preserved"
    - "Docstring row-count reconciliation at L33-58 is updated 24 → 25 and describes the new axes (sourceKind + installMode) and the twinkie row"
    - "Existing catalog.test.ts install-path prefix invariant test is updated so system-root rows use `/etc/claude-code/` instead of `~/…` prefixes"
    - "New T-07 test asserts: 24 bundled + 1 runtime row, all bundled rows retain their bundledPath under /app/fleet-substrate/, the one runtime row matches D-14's exact shape"
  artifacts:
    - path: "src/backend/distributor/catalog.ts"
      provides: "CatalogEntry discriminated union (BundledCatalogEntry | RuntimeCatalogEntry) + 25-row FLEET_SUBSTRATE_CATALOG including the twinkie"
      contains: "instance-policy-claude-md"
    - path: "src/backend/distributor/catalog.test.ts"
      provides: "T-07 schema regression guard + updated install-path invariant filtering by installMode"
      contains: "sourceKind"
  key_links:
    - from: "src/backend/distributor/catalog.ts"
      to: "RuntimeCatalogEntry.resolverKey='instance-policy'"
      via: "consumed by Plan 05 sweep composer via deps.resolvedRuntimeBytes.get('instance-policy')"
      pattern: "resolverKey.*instance-policy"
---

<objective>
Extend the fleet-substrate catalog schema to accommodate two new orthogonal axes — `sourceKind` (bundled vs runtime) and `installMode` (user-home vs system-root) — using RESEARCH-recommended shape: a discriminated union on `sourceKind` (fundamentally changes which source-side fields are present) plus an optional `installMode` field defaulting to `"user-home"` (only affects post-write behavior, kept optional to keep the 24-row diff tiny). Add the twinkie row referencing the runtime resolver.

Purpose: Establishes the pure-data catalog contract that Plan 03 (push helpers) and Plan 05 (sweep composer) consume. This module has no runtime imports (`PURE-LIB DISCIPLINE` at L11-16), so it can land in Wave 1 alongside Plan 01 with zero coupling.

Output: `CatalogEntry` type is a discriminated union, all 24 existing rows retain their bytes but each gains `sourceKind: "bundled"`, one new twinkie row is appended, docstring row-count reconciliation is 24 → 25, T-07 schema regression test passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/ubuntu/skynet-tina/CLAUDE.md
@/home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-CONTEXT.md
@/home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-RESEARCH.md
@/home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend CatalogEntry to discriminated union + stamp existing 24 rows + append twinkie row</name>
  <files>src/backend/distributor/catalog.ts</files>
  <read_first>
    - src/backend/distributor/catalog.ts (full 299 lines — see PURE-LIB DISCIPLINE at L11-16 (no runtime imports allowed), CatalogEntry interface at L68-97, docstring row-count reconciliation at L33-58, all 24 rows L106-298)
    - /home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-PATTERNS.md § catalog.ts pattern (L202-270), especially L224-245 for the discriminated-union shape, L249-256 for the "stamp sourceKind:'bundled' on existing rows" motion, L259-268 for the twinkie row shape
    - /home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-RESEARCH.md § Pattern 1 (L240-288 — full type-theory rationale for discriminated union on sourceKind + optional installMode)
    - /home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-CONTEXT.md D-12 (two new axes), D-14 (exact twinkie row values)
  </read_first>
  <behavior>
    - Type-level: `CatalogEntry` is `BundledCatalogEntry | RuntimeCatalogEntry`. TypeScript narrows correctly on `entry.sourceKind === "bundled"` (bundledPath accessible) vs `=== "runtime"` (resolverKey accessible, bundledPath NOT accessible without narrowing error).
    - Data-level: `FLEET_SUBSTRATE_CATALOG.length === 25`.
    - Data-level: `FLEET_SUBSTRATE_CATALOG.filter(e => e.sourceKind === "bundled").length === 24` and each of those 24 has `bundledPath.startsWith("/app/fleet-substrate/")`.
    - Data-level: `FLEET_SUBSTRATE_CATALOG.filter(e => e.sourceKind === "runtime").length === 1` and that row is exactly the twinkie: slug="instance-policy-claude-md", resolverKey="instance-policy", installPath="/etc/claude-code/CLAUDE.md", installMode="system-root", restartHook=null.
    - Purity: no runtime import statements introduced (grep for `import.*from.*fs\|import.*from.*ssh\|import.*from.*logger` returns 0 matches — pure-lib discipline preserved).
  </behavior>
  <action>
    Follow patterns in 114-PATTERNS.md § catalog.ts + 114-RESEARCH.md § Pattern 1.

    1) In `src/backend/distributor/catalog.ts`:
       - Replace the `CatalogEntry` interface (L68-97) with the discriminated union per 114-PATTERNS.md L225-245:
         - Define `interface BundledCatalogEntry` with fields: `slug: string`, `sourceKind: "bundled"`, `bundledPath: string`, `installPath: string`, `installMode?: "user-home" | "system-root"`, `restartHook: string | null`. Keep the JSDoc from the current interface but split so each field's docstring lives on the corresponding branch (bundledPath docstring stays on BundledCatalogEntry).
         - Define `interface RuntimeCatalogEntry` with fields: `slug: string`, `sourceKind: "runtime"`, `resolverKey: "instance-policy"` (string-literal union — extend when new runtime rows land), `installPath: string`, `installMode: "user-home" | "system-root"` (NON-optional here since runtime rows must declare intent), `restartHook: string | null`. Add JSDoc on `resolverKey` explaining it is a lookup key the composer maps to a resolver dep; the catalog module stays runtime-free per PURE-LIB DISCIPLINE.
         - Export `type CatalogEntry = BundledCatalogEntry | RuntimeCatalogEntry;`.
         - Add a top-of-interface-block JSDoc citing Phase 114 D-12 + RESEARCH.md § Pattern 1: (a) sourceKind is the discriminant because it changes which source-side fields are present, (b) installMode is optional-with-default because it only affects post-write behavior and defaults keep the existing 24-row diff minimal.
       - Add `sourceKind: "bundled",` to each of the 24 existing rows in `FLEET_SUBSTRATE_CATALOG` (L106-298). Do NOT add `installMode` to existing rows — they rely on the optional-default. Do NOT change `slug`, `bundledPath`, `installPath`, or `restartHook` values on any existing row (byte-identity of shipped behavior).
       - Append the twinkie row to `FLEET_SUBSTRATE_CATALOG` (after the ambient-monitor row at L292-297), with the exact D-14 values:
         ```
         { slug: "instance-policy-claude-md", sourceKind: "runtime", resolverKey: "instance-policy", installPath: "/etc/claude-code/CLAUDE.md", installMode: "system-root", restartHook: null, }
         ```
         (Use the TypeScript object-literal shape; the array's `as const` at L298 forces literal typing.) Add a section-header comment above the new row explaining: (a) first runtime-sourced row (bytes come from readInstancePolicyBytes() at sweep time, not from /app/fleet-substrate/), (b) first system-root-installed row (writes to /etc/claude-code/CLAUDE.md root:root 0644, gated on hosts.username === "root" per Plan 05), (c) restartHook: null because Claude Code discovers managed-policy at every session start (per D-14 + D-20).
       - Update the file-level docstring row-count reconciliation at L33-58 to reflect 25 rows and the two new axes:
         - Bump "Total = 24" → "Total = 25" (add: "1 row for instance-policy-claude-md (Phase 114 twinkie — runtime-sourced, system-root-installed)").
         - Add a paragraph explaining the two new axes (sourceKind + installMode) and why the discriminated-union shape (per 114-PATTERNS.md L224-245).
       - PRESERVE PURE-LIB DISCIPLINE — do NOT add any `import` statement other than existing ones. The catalog stays a pure data + type module.
    2) DO NOT modify catalog.test.ts here — Task 2 owns that. DO NOT touch `resolverKey` consumption (that lives in Plan 05's sweep composer + orchestrator wiring).
  </action>
  <verify>
    <automated>npx vitest run src/backend/distributor/catalog.ts --reporter=basic || npx tsc --noEmit --project /home/ubuntu/skynet-tina/tsconfig.json 2>&1 | grep -E "catalog\.ts|error TS" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -q "interface BundledCatalogEntry" src/backend/distributor/catalog.ts` succeeds.
    - Source assertion: `grep -q "interface RuntimeCatalogEntry" src/backend/distributor/catalog.ts` succeeds.
    - Source assertion: `grep -q "type CatalogEntry = BundledCatalogEntry | RuntimeCatalogEntry" src/backend/distributor/catalog.ts` succeeds.
    - Source assertion: `grep -c "sourceKind: \"bundled\"" src/backend/distributor/catalog.ts` returns exactly 24.
    - Source assertion: `grep -c "sourceKind: \"runtime\"" src/backend/distributor/catalog.ts` returns exactly 1.
    - Source assertion: `grep -q "slug: \"instance-policy-claude-md\"" src/backend/distributor/catalog.ts` succeeds.
    - Source assertion: `grep -q "installPath: \"/etc/claude-code/CLAUDE.md\"" src/backend/distributor/catalog.ts` succeeds.
    - Source assertion: `grep -q "installMode: \"system-root\"" src/backend/distributor/catalog.ts` succeeds.
    - Source assertion: `grep -q "resolverKey: \"instance-policy\"" src/backend/distributor/catalog.ts` succeeds.
    - Purity gate: `grep -v '^ \* \|^//\|^\s*\*\|^\s*/\*' src/backend/distributor/catalog.ts | grep -E "^import" | grep -vE "^import type" | wc -l` returns 0 (only `import type` allowed; no runtime imports).
    - Docstring gate: `grep -q "Total = 25" src/backend/distributor/catalog.ts` succeeds (docstring updated).
    - TypeScript compilation: `npx tsc --noEmit -p /home/ubuntu/skynet-tina/tsconfig.json 2>&1 | grep -E "src/backend/distributor/catalog\.ts.*error TS" | wc -l` returns 0 (catalog.ts type-checks; downstream files may fail because they haven't been updated yet — that's expected until Plan 03/05).
  </acceptance_criteria>
  <done>
    Catalog is a discriminated union, 24 existing rows preserved byte-for-byte plus `sourceKind: "bundled"`, twinkie row appended with exact D-14 values, docstring reconciled 24 → 25, pure-lib discipline intact.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: T-07 schema regression guard + install-path invariant refactor by installMode</name>
  <files>src/backend/distributor/catalog.test.ts</files>
  <read_first>
    - src/backend/distributor/catalog.test.ts (full 205 lines — see Test 1 row-count at L34-50, install-path invariant at L52-70 (must be updated to filter by installMode), bundled-path/installPath-prefix invariants across L52-205)
    - src/backend/distributor/catalog.ts (as modified by Task 1)
    - /home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-PATTERNS.md § catalog.test.ts pattern (L622-666), especially L636-645 for the install-path invariant refactor and L649-664 for T-07's exact shape
    - /home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-CONTEXT.md D-22 (T-07 scope: catalog schema + regression guard)
  </read_first>
  <behavior>
    - Test 1 (existing, updated): FLEET_SUBSTRATE_CATALOG.length === 25 (was 24). This is a load-bearing bump — the test file's row-count assertion at L35 must be updated.
    - Test 3 (existing install-path invariant at L52-70, updated): the `~/.claude/skills/ | ~/.local/bin/ | ~/.config/systemd/user/` prefix invariant applies ONLY to rows where `installMode !== "system-root"` (or equivalently, only to BundledCatalogEntry-shaped rows with the default install mode). System-root rows are exempted (they use absolute paths under `/etc/`); add a sibling assertion that any `installMode === "system-root"` row has an installPath starting with `/etc/`.
    - New Test T-07 (D-22): (a) count bundled === 24, runtime === 1; (b) every bundled row has bundledPath.startsWith("/app/fleet-substrate/"); (c) the one runtime row has slug="instance-policy-claude-md", resolverKey="instance-policy", installPath="/etc/claude-code/CLAUDE.md", installMode="system-root", restartHook=null.
    - Regression guard: this test suite ensures a future refactor cannot silently break the discriminated-union shape or the twinkie's D-14 values.
  </behavior>
  <action>
    Follow patterns in 114-PATTERNS.md § catalog.test.ts.

    1) In `src/backend/distributor/catalog.test.ts`:
       - Update Test 1 (L34-50): change the count assertion from 24 to 25. Update the test's title and any inline comments describing the row-count breakdown (add "1 row for instance-policy-claude-md — Phase 114 twinkie").
       - Update the install-path prefix invariant (L52-70): split into two assertions.
         - For rows where `entry.installMode !== "system-root"` (or, using discriminated-union narrowing, for `entry.sourceKind === "bundled"` rows without `installMode` set OR set to "user-home"): the existing prefix set (`~/.claude/skills/ | ~/.local/bin/ | ~/.config/systemd/user/`) applies.
         - For rows where `entry.installMode === "system-root"`: assert `entry.installPath.startsWith("/etc/")` AND `entry.installPath === "/etc/claude-code/CLAUDE.md"` (the exact D-14 value; there is exactly one such row today, and the test acts as a regression guard if a future contributor adds a second system-root row without updating this test).
         - Include a comment in the test describing this split and citing Phase 114 D-14 for the exact system-root installPath.
       - Add a new test T-07 exactly per 114-PATTERNS.md L649-664:
         ```
         it("Test T-07: sourceKind discriminant — 24 bundled + 1 runtime row", () => {
           const bundled = FLEET_SUBSTRATE_CATALOG.filter((e) => e.sourceKind === "bundled");
           const runtime = FLEET_SUBSTRATE_CATALOG.filter((e) => e.sourceKind === "runtime");
           expect(bundled.length).toBe(24);
           expect(runtime.length).toBe(1);
           for (const e of bundled) {
             expect(typeof e.bundledPath).toBe("string");
             expect(e.bundledPath.startsWith("/app/fleet-substrate/")).toBe(true);
           }
           expect(runtime[0].slug).toBe("instance-policy-claude-md");
           expect(runtime[0].installPath).toBe("/etc/claude-code/CLAUDE.md");
           expect(runtime[0].installMode).toBe("system-root");
           expect((runtime[0] as { resolverKey: string }).resolverKey).toBe("instance-policy");
           expect(runtime[0].restartHook).toBeNull();
         });
         ```
         (Type-narrowing note: after the filter, TypeScript narrows `bundled` to BundledCatalogEntry[] and `runtime` to RuntimeCatalogEntry[] — the resolverKey access on runtime[0] is well-typed. If the narrowing does not resolve at the test-file level, add a comment explaining and use a cast as shown.)
       - Review any other tests (L97-205) that assert on `bundledPath` for all entries — if they iterate `FLEET_SUBSTRATE_CATALOG` without narrowing, add a narrowing filter (`.filter(e => e.sourceKind === "bundled")`) before the loop OR guard each iteration with `if ("bundledPath" in entry)`. This is a straightforward TypeScript compile-error fix if any exist; the executor should let `npx tsc` guide the changes.
    2) DO NOT touch run-sweep.ts, ssh-push.ts, or their tests — Plans 03 and 05 own those.
  </action>
  <verify>
    <automated>npx vitest run src/backend/distributor/catalog.test.ts --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -q "FLEET_SUBSTRATE_CATALOG.length).toBe(25)" src/backend/distributor/catalog.test.ts` succeeds (row-count bumped).
    - Source assertion: `grep -q "Test T-07: sourceKind discriminant" src/backend/distributor/catalog.test.ts` succeeds (T-07 present).
    - Source assertion: `grep -q "instance-policy-claude-md" src/backend/distributor/catalog.test.ts` succeeds.
    - Source assertion: `grep -q "/etc/claude-code/CLAUDE.md" src/backend/distributor/catalog.test.ts` succeeds (system-root install path assertion present).
    - Behavior: `npx vitest run src/backend/distributor/catalog.test.ts --reporter=basic` exits 0 with T-07 passing and all pre-existing catalog tests passing (row-count + prefix invariants updated correctly).
  </acceptance_criteria>
  <done>
    catalog.test.ts is updated for 25 rows, install-path prefix invariant split by installMode, T-07 schema regression guard added, all catalog tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Catalog.ts is pure data — no trust boundary crosses this module. The installPath and resolverKey values are hand-authored TypeScript literals, not user input.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-05 | Tampering | Silent regression that leaves runtime-row `installMode` mis-set or drops the discriminant | mitigate | T-07 regression guard test asserts exact D-14 values (slug, installPath, installMode, resolverKey, restartHook). Row-count invariant test asserts total === 25. Any future refactor that breaks these fails at CI. |

Package Legitimacy Gate: N/A — no external packages added.
</threat_model>

<verification>
```
npx vitest run src/backend/distributor/catalog.test.ts --reporter=basic
```
</verification>

<success_criteria>
Plan 02 succeeds when: (1) `npx vitest run src/backend/distributor/catalog.test.ts --reporter=basic` exits 0 with T-07 passing; (2) all catalog.ts source-assertion greps pass; (3) `npx tsc --noEmit` reports zero errors within catalog.ts itself (downstream files may still error until Plans 03/05 land); (4) pure-lib discipline preserved (no runtime imports); (5) atomic commit produced.
</success_criteria>

<output>
Create `.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-02-SUMMARY.md` documenting: shape chosen (discriminated union on sourceKind + optional installMode), 24-row byte-identity preservation confirmation, exact D-14 values for the twinkie row, docstring reconciliation.
</output>
