---
phase: 112-instance-wide-managed-policy-claude-md-new-branding-config-f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/branding/branding-config-loader.ts
  - src/backend/branding/branding-config-loader.test.ts
  - docker/branding-defaults/branding.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "BrandingConfig has a new field named instancePolicyFilename typed as string"
    - "isValidBrandingShape() accepts branding.json documents that do NOT contain the new field (optional-in-guard) so deployed operators do not lose their iconPath/wordmarkPath overrides on first boot"
    - "HARDCODED_FALLBACK.instancePolicyFilename === \"\" (empty string, matching the intentional-unset convention avatarDirectorSpec uses)"
    - "docker/branding-defaults/branding.json contains \"instancePolicyFilename\": \"\" byte-mirroring HARDCODED_FALLBACK per D-04 + Phase 70 D-14"
    - "readInstancePolicyBytes() is exported from branding-config-loader.ts, is async, returns Promise<Buffer | null>, and never throws"
    - "readInstancePolicyBytes() returns null when: (a) field empty, (b) file missing (ENOENT), (c) file exceeds 256 KB byte cap, (d) filename contains .. or escapes /etc/skynet/branding/, (e) any other read error"
    - "Over-cap file emits sshLogger.error with operation: 'branding_instance_policy_size'"
    - "Containment violation emits sshLogger.error with operation: 'branding_instance_policy_containment'"
    - "Non-ENOENT read error emits sshLogger.error with operation: 'branding_instance_policy_read'"
    - "T-01..T-05 loader tests pass (parse, absent-defaults-to-empty, over-cap, containment, ENOENT)"
  artifacts:
    - path: "src/backend/branding/branding-config-loader.ts"
      provides: "BrandingConfig with instancePolicyFilename field + isValidBrandingShape accepts absent + HARDCODED_FALLBACK includes empty-string default + new readInstancePolicyBytes export"
      contains: "instancePolicyFilename"
    - path: "docker/branding-defaults/branding.json"
      provides: "Bundled defaults JSON containing instancePolicyFilename with empty-string default"
      contains: "instancePolicyFilename"
    - path: "src/backend/branding/branding-config-loader.test.ts"
      provides: "T-01..T-05 test cases for the new field and readInstancePolicyBytes"
  key_links:
    - from: "src/backend/branding/branding-config-loader.ts"
      to: "sshLogger from ../utils/logger.js"
      via: "existing import at line 35"
      pattern: "sshLogger\\.error"
    - from: "docker/branding-defaults/branding.json"
      to: "HARDCODED_FALLBACK in branding-config-loader.ts"
      via: "byte-mirror per D-04 + Phase 70 D-14"
      pattern: "instancePolicyFilename.*\"\""
---

<objective>
Extend the branding-config subsystem with the source-of-truth field and byte-reader for the instance-wide managed-policy CLAUDE.md ("twinkie"). Adds ONE optional string field (`instancePolicyFilename`) to `BrandingConfig`, updates `isValidBrandingShape()` to accept documents WITHOUT the field (per RESEARCH Pitfall 1 option (a) — avoids stomping deployed operators' iconPath/wordmarkPath overrides on first boot after upgrade), mirrors the empty-string default into `HARDCODED_FALLBACK` and `docker/branding-defaults/branding.json`, and adds a new peer export `readInstancePolicyBytes()` that returns the twinkie file's bytes from `/etc/skynet/branding/<filename>` respecting the existing 256 KB byte cap and path-containment guard. Never-throws contract carries: every failure mode returns `null`.

Purpose: This plan establishes the source-of-truth surface. Every downstream layer (assert-boot alarm, distributor catalog+push, sweep composer) consumes `readInstancePolicyBytes()`. Without this plan, no other plan can compile.

Output: Extended `BrandingConfig` type + shape guard + fallback + defaults JSON, new `readInstancePolicyBytes()` export, and T-01..T-05 tests all passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/ubuntu/skynet-tina/CLAUDE.md
@/home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-CONTEXT.md
@/home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-RESEARCH.md
@/home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend BrandingConfig type + shape guard + HARDCODED_FALLBACK + branding-defaults JSON</name>
  <files>src/backend/branding/branding-config-loader.ts, docker/branding-defaults/branding.json</files>
  <read_first>
    - src/backend/branding/branding-config-loader.ts (full file — see current BrandingConfig type at L41-62, HARDCODED_FALLBACK at L78-98, isValidBrandingShape at L166-195, MAX_CONFIG_BYTES at L72; must preserve never-throws contract at L21)
    - docker/branding-defaults/branding.json (full 14 lines — current shape must byte-mirror HARDCODED_FALLBACK per D-04 + Phase 70 D-14)
    - /home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-PATTERNS.md § BrandingConfig type extension pattern (line 38-49), § HARDCODED_FALLBACK pattern (L51-62), § Shape-guard pattern (L64-83 — RESEARCH RECOMMENDS optional-in-guard)
    - /home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-RESEARCH.md § Common Pitfalls → Pitfall 1 (L544-556 — explains why optional-in-guard, not required-field)
    - /home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-CONTEXT.md D-01 through D-07 (branding-config schema decisions)
  </read_first>
  <behavior>
    - Test 1 (T-01, from D-21): a branding.json containing `"instancePolicyFilename": "team.md"` parses via loadBrandingConfig() and the returned config's instancePolicyFilename === "team.md".
    - Test 2 (T-02, from D-21): a branding.json that OMITS instancePolicyFilename entirely still passes isValidBrandingShape() AND loadBrandingConfig() returns a config whose instancePolicyFilename is exactly "" (empty string, not undefined, not null). This is the load-bearing regression guard against Pitfall 1.
    - Test 3: HARDCODED_FALLBACK.instancePolicyFilename === "" (byte-check, exact empty string).
    - Test 4: docker/branding-defaults/branding.json (read raw via readFileSync in a test) parses to an object whose instancePolicyFilename is exactly "".
    - Test 5: A malformed field (`instancePolicyFilename: 123`) causes isValidBrandingShape() to return false, loadBrandingConfig falls back to bundled defaults, and sshLogger.error fires with operation: "branding_config_shape".
  </behavior>
  <action>
    Follow patterns in 114-PATTERNS.md § BrandingConfig type extension + § HARDCODED_FALLBACK + § Shape-guard.

    1) In `src/backend/branding/branding-config-loader.ts`:
       - Add `instancePolicyFilename: string;` to the `BrandingConfig` type declaration (analog: `avatarDirectorSpec: string;` at L60; place adjacent to it for co-location of intentional-empty-string fields). Add a comment citing Phase 114 D-01 + D-02 + D-10 explaining: (a) empty-string convention mirrors avatarDirectorSpec (Phase 74), (b) filename NOT a URL path (contrast with iconPath/wordmarkPath — the twinkie is never HTTP-served), (c) NO bundled-default leg (D-10 departure from branding-asset pattern).
       - Add `instancePolicyFilename: "",` to the `HARDCODED_FALLBACK` object literal (place adjacent to `avatarDirectorSpec: ""` at L96 for consistency). Add a comment citing Phase 114 D-03 + D-10 explaining that empty string is the intentional-unset state and there is NO bundled-default leg.
       - Modify `isValidBrandingShape()` at L166-195 to accept documents WITHOUT the new field. Add these lines (per RESEARCH.md Pitfall 1 option (a) + 114-PATTERNS.md L82-83): `if (o.instancePolicyFilename !== undefined && typeof o.instancePolicyFilename !== "string") return false;` (checks: if the key exists AND is not a string, reject; if the key is absent, accept). Place this check adjacent to the `wipIndicatorPath` and `avatarDirectorSpec` checks (order maintainability). Add an inline comment citing Phase 114 D-01 + Pitfall 1: "optional-in-guard so deployed branding.json files without this field don't fall back to HARDCODED_FALLBACK and stomp operator overrides."
       - In `loadBrandingConfig()` at L205-256, after the shape guard passes (i.e., before `return parsed as BrandingConfig;` at L255), normalize the field: coerce `parsed.instancePolicyFilename ?? ""` so the loader always returns a `BrandingConfig` whose field is a string (never undefined). Concrete: cast parsed to a mutable record, set `(parsed as Record<string, unknown>).instancePolicyFilename ??= "";` before the return, OR return a new object spread merging the default. Pick whichever adds the fewest lines while preserving the existing `return parsed as BrandingConfig` shape — the constraint is: post-loader, `config.instancePolicyFilename` MUST be a string.

    2) In `docker/branding-defaults/branding.json`:
       - Add `"instancePolicyFilename": "",` to the JSON object. Place adjacent to `"avatarDirectorSpec": ""` at L12 (matches the co-location choice in HARDCODED_FALLBACK). Keep the file valid JSON (trailing comma discipline: JSON does not allow trailing commas — verify with `python3 -c 'import json; json.load(open("docker/branding-defaults/branding.json"))'`).
       - Verify byte-parity: HARDCODED_FALLBACK.instancePolicyFilename === "" AND branding.json instancePolicyFilename === "" — both empty strings.

    3) DO NOT add readInstancePolicyBytes() here — that is Task 2. DO NOT add a boot-alarm branch to assert-boot.ts — that is Plan 04. DO NOT add MAX_INSTANCE_POLICY_BYTES constant here — that is Task 2.

    Never-throws contract: no change. The added shape-guard line follows the existing pattern of returning false on malformed → loader falls back to bundled defaults, no throw.
  </action>
  <verify>
    <automated>npx vitest run src/backend/branding/branding-config-loader.test.ts --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "instancePolicyFilename" src/backend/branding/branding-config-loader.ts` returns >= 4 (type declaration + HARDCODED_FALLBACK + shape guard check + loader normalization).
    - Source assertion: `grep -q "o.instancePolicyFilename !== undefined && typeof o.instancePolicyFilename !== \"string\"" src/backend/branding/branding-config-loader.ts` succeeds (optional-in-guard shape present, guards Pitfall 1).
    - Source assertion: `grep -q "instancePolicyFilename: \"\"" src/backend/branding/branding-config-loader.ts` succeeds (HARDCODED_FALLBACK has empty string).
    - JSON validity: `python3 -c "import json; d = json.load(open('docker/branding-defaults/branding.json')); assert d['instancePolicyFilename'] == '', repr(d.get('instancePolicyFilename'))"` exits 0.
    - Behavior: `npx vitest run src/backend/branding/branding-config-loader.test.ts --reporter=basic` exits 0 with T-01 and T-02 both passing (add tests here, before Task 2's readInstancePolicyBytes tests).
    - Byte-parity: HARDCODED_FALLBACK's `instancePolicyFilename` value AND branding-defaults JSON's `instancePolicyFilename` value are BOTH the exact string `""` (verified by shape-check test + JSON assertion above).
    - No new throws: `grep -n "throw new Error\|throw new" src/backend/branding/branding-config-loader.ts | wc -l` returns the SAME count as before this task (never-throws contract preserved outside the existing containment-guard throws in resolveAssetPath).
  </acceptance_criteria>
  <done>
    BrandingConfig has the new field, isValidBrandingShape accepts absent-field documents (guarding Pitfall 1), HARDCODED_FALLBACK + branding-defaults JSON byte-mirror at empty string, loader normalizes to string post-guard, T-01/T-02 test cases pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add readInstancePolicyBytes() export + T-03/T-04/T-05 tests</name>
  <files>src/backend/branding/branding-config-loader.ts, src/backend/branding/branding-config-loader.test.ts</files>
  <read_first>
    - src/backend/branding/branding-config-loader.ts (as modified by Task 1 — see resolveAssetPath at L280-335 for the containment-guard pattern; MAX_CONFIG_BYTES at L72 for the byte cap constant to reuse; getBrandingAssetsDir at L111-113 for the twinkie's directory)
    - src/backend/branding/branding-config-loader.test.ts (full ~285 lines — see fs mock pattern at L69-83, freshLoader helper at L126-129; extend both to cover twinkie file reads)
    - /home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-PATTERNS.md § Path-containment guard pattern (L85-103), § Byte-cap pattern (L105-119), § ENOENT-graceful read pattern (L121-137), § readInstancePolicyBytes tests T-01..T-05 (L571-577)
    - /home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-RESEARCH.md § Pattern 5 (L425-494 — full canonical shape of readInstancePolicyBytes co-located in branding-config-loader.ts)
    - /home/ubuntu/skynet-tina/.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-CONTEXT.md D-06 (byte-cap parity), D-08 (path resolution + directory), D-09 (containment guard), D-10 (no bundled-default leg — return null on missing, NEVER fall back), D-11 (export signature + null return conditions + never-throws)
  </read_first>
  <behavior>
    - Test T-01 (D-21): with state.configJson including `instancePolicyFilename: "team.md"` AND state.twinkieFile stat/readFile returning `{ size: 100 }` + Buffer.from("hello"), `await readInstancePolicyBytes()` returns a Buffer whose byte content equals "hello".
    - Test T-02 (D-21): with state.configJson OMITTING instancePolicyFilename (the loader normalizes to ""), `await readInstancePolicyBytes()` returns null WITHOUT ever calling fs.stat on the twinkie file path (verify via mock call count === 0 for the twinkie path).
    - Test T-03 (D-21): with state.configJson including `instancePolicyFilename: "big.md"` AND state.twinkieFile stat returning size `262145` (256 KB + 1 byte), `await readInstancePolicyBytes()` returns null AND sshLogger.error is called with `operation: "branding_instance_policy_size"`. Do NOT read the file (short-circuit before readFile).
    - Test T-04 (D-21): with state.configJson including `instancePolicyFilename: "../etc/passwd"`, `await readInstancePolicyBytes()` returns null AND sshLogger.error is called with `operation: "branding_instance_policy_containment"` AND fs.stat is NEVER called (fail-fast at resolver).
    - Test T-05 (D-21): with state.configJson including `instancePolicyFilename: "team.md"` AND state.twinkieFile.error set to `{ code: "ENOENT" }`, `await readInstancePolicyBytes()` returns null AND sshLogger.error is NOT called (ENOENT is expected misconfig state — Plan 04's assert-boot fires the alarm at boot; per-sweep skip logs are handled in Plan 05).
    - Test T-05b (defense-in-depth): a non-ENOENT read error (e.g., EACCES) causes readInstancePolicyBytes to return null AND sshLogger.error is called with `operation: "branding_instance_policy_read"`.
  </behavior>
  <action>
    Follow patterns in 114-PATTERNS.md § Path-containment guard + § Byte-cap + § ENOENT-graceful read + 114-RESEARCH.md § Pattern 5.

    1) In `src/backend/branding/branding-config-loader.ts`:
       - Add a new export `readInstancePolicyBytes(): Promise<Buffer | null>` following the exact shape in 114-RESEARCH.md § Pattern 5 (L432-487):
         - Call `loadBrandingConfig()` to get the current config (relies on Task 1's normalization to string).
         - If `config.instancePolicyFilename` after `?? ""` is empty string → return `null` immediately (no log, no fs call — clean unset state per D-10).
         - Compute `assetsBase = getBrandingAssetsDir()` and `requestedPath = path.resolve(assetsBase, filename)`.
         - Containment guard mirroring resolveAssetPath L289-295: if `!requestedPath.startsWith(assetsBase + path.sep) && requestedPath !== assetsBase` → sshLogger.error with `operation: "branding_instance_policy_containment"` including filename + resolvedPath in the metadata, then `return null`. DO NOT throw (per D-11 never-throws — twinkie is called at sweep time, not HTTP-route time, so no 400 surface).
         - Wrap fs calls in try/catch. `const stat = await fs.stat(requestedPath);` — if `stat.size > MAX_CONFIG_BYTES` (reuse the existing 256 KB constant per D-06; do NOT introduce a new `MAX_INSTANCE_POLICY_BYTES` alias — direct reuse is clearer) → sshLogger.error with `operation: "branding_instance_policy_size"`, error message includes actual size + max cap + resolved path, then `return null`.
         - `const bytes = await fs.readFile(requestedPath);` (no encoding argument — returns Buffer). Return `bytes`.
         - Catch block: `const code = (err as NodeJS.ErrnoException).code;` — if `code === "ENOENT"` → return `null` silently (NO sshLogger call; the D-05 boot alarm in Plan 04 handles the "field set but file missing" misconfig loudness path). Otherwise → sshLogger.error with `operation: "branding_instance_policy_read"`, error message from err, then `return null`.
       - Place this export AFTER the existing `resolveAssetPath` function (append at end of file) for readability grouping.
       - Add a JSDoc block on the new export documenting: (a) never-throws contract (matches loader), (b) return-null conditions (empty, ENOENT, over-cap, containment, read error), (c) that ENOENT is silent by design so assert-boot (Phase 114 Plan 04) can call it and observe null to fire the misconfig alarm.

    2) In `src/backend/branding/branding-config-loader.test.ts`:
       - Extend the shared state holder (near L60-70) to include `twinkieFile: { size?: number; bytes?: Buffer; error?: NodeJS.ErrnoException }`.
       - Extend the `vi.mock("node:fs")` block (currently at ~L69-83): the `fs.promises.stat` mock must dispatch on the requested path — if path matches `/etc/skynet/branding/branding.json` return the existing config stat; if path matches `/etc/skynet/branding/<any-other-file>` return the twinkie stat (or throw twinkieFile.error). Same dispatch for `readFile`. Use a simple `.startsWith` or exact-suffix match; document the dispatch shape in a comment.
       - Add a `sshLoggerErrorSpy = vi.fn()` and update the logger mock to route `sshLogger.error` through it (mirror pattern from other test files if the mock doesn't already spy — grep for `sshLogger` usage in existing tests to see the shape).
       - Add T-01..T-05 test cases per the behavior spec above. Use `freshLoader()` for isolation. Each test resets `state.twinkieFile` in a `beforeEach` (or at the top of each `it` block, matching existing test conventions).

    3) DO NOT introduce a new module `instance-policy-reader.ts` — CONTEXT.md D-11 discretion recommends co-location; 114-RESEARCH.md Pattern 5 recommends the same.
  </action>
  <verify>
    <automated>npx vitest run src/backend/branding/branding-config-loader.test.ts --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -q "^export async function readInstancePolicyBytes" src/backend/branding/branding-config-loader.ts` succeeds (function exported).
    - Source assertion: `grep -q "operation: \"branding_instance_policy_size\"" src/backend/branding/branding-config-loader.ts` succeeds.
    - Source assertion: `grep -q "operation: \"branding_instance_policy_containment\"" src/backend/branding/branding-config-loader.ts` succeeds.
    - Source assertion: `grep -q "operation: \"branding_instance_policy_read\"" src/backend/branding/branding-config-loader.ts` succeeds.
    - No-throw invariant: `grep -A 40 "^export async function readInstancePolicyBytes" src/backend/branding/branding-config-loader.ts | grep -v '^#' | grep -c "throw"` returns 0 (readInstancePolicyBytes never throws directly).
    - Byte-cap reuse: `grep -q "stat.size > MAX_CONFIG_BYTES" src/backend/branding/branding-config-loader.ts` succeeds (reuses existing constant, does not introduce a new one).
    - Behavior: `npx vitest run src/backend/branding/branding-config-loader.test.ts --reporter=basic` exits 0 with T-01..T-05 (5 new tests minimum) all passing.
    - Behavior: `grep -c "readInstancePolicyBytes" src/backend/branding/branding-config-loader.test.ts` returns >= 5 (called by at least T-01..T-05).
  </acceptance_criteria>
  <done>
    readInstancePolicyBytes() is exported from branding-config-loader.ts, honors byte cap + containment guard + never-throws + no-bundled-fallback contract, and T-01..T-05 tests all pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin → host filesystem | Admin SSH-edits `/opt/skynet/branding/<filename>.md` on the Skynet host. This is trusted input by policy (only admins have host SSH), but the filename crosses into a `path.resolve` call that must be containment-guarded. |
| host filesystem → loader | The loader reads two files: `branding.json` (already guarded by existing shape+byte-cap patterns) and the new twinkie markdown file (this plan adds the guards). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-01 | Tampering | `readInstancePolicyBytes()` filename input | mitigate | Path-containment guard mirroring `resolveAssetPath` L289-295 — filename with `..` or that path-resolves outside `/etc/skynet/branding/` → sshLogger.error(operation: `branding_instance_policy_containment`) + return null. Verified by T-04 test. |
| T-114-02 | Denial of Service | oversized twinkie file consumes memory/CPU | mitigate | 256 KB byte cap via existing `MAX_CONFIG_BYTES` — over-cap files skip readFile and return null with sshLogger.error(operation: `branding_instance_policy_size`). Verified by T-03 test. |
| T-114-03 | Information Disclosure | non-ENOENT read errors leaking stack traces | mitigate | Never-throws contract — errors caught, error message extracted via `err instanceof Error ? err.message : String(err)`, no stack propagated to callers. sshLogger structured payload includes only path + operation + message (no stack). |
| T-114-04 | Tampering (deployment) | first-boot after Phase 114 stomps operator overrides via shape-guard rejection | mitigate | Optional-in-guard per Pitfall 1 option (a): `isValidBrandingShape()` accepts documents WITHOUT the new field. Loader normalizes to `""` post-guard. Verified by T-02 test. |

Package Legitimacy Gate: N/A — Phase 114 installs zero external packages (per 114-RESEARCH.md § Package Legitimacy Audit).
</threat_model>

<verification>
Scoped tests must pass:
```
npx vitest run src/backend/branding/branding-config-loader.test.ts --reporter=basic
```
(Full scoped-test run per D-23 executes at plan 06 closure; this plan's local gate is the loader test file only.)

Post-conditions:
- `BrandingConfig` type has `instancePolicyFilename: string`.
- `isValidBrandingShape()` accepts documents without the field.
- `HARDCODED_FALLBACK.instancePolicyFilename === ""`.
- `docker/branding-defaults/branding.json` byte-mirrors `HARDCODED_FALLBACK` on the new field.
- `readInstancePolicyBytes()` exported, never throws, honors 256 KB cap + containment guard.
- T-01..T-05 tests pass.
</verification>

<success_criteria>
Plan 01 succeeds when: (1) `npx vitest run src/backend/branding/branding-config-loader.test.ts --reporter=basic` exits 0 with T-01..T-05 passing; (2) `python3 -c "import json; d = json.load(open('docker/branding-defaults/branding.json')); assert d['instancePolicyFilename'] == ''"` exits 0; (3) all source-assertion greps in acceptance_criteria pass; (4) atomic commit(s) produced.
</success_criteria>

<output>
Create `.planning/phases/114-instance-wide-managed-policy-claude-md-new-branding-config-f/114-01-SUMMARY.md` documenting: field name chosen (`instancePolicyFilename`), rationale for optional-in-guard, files touched, tests added, any deviations from PATTERNS.md.
</output>
