---
phase: 101
plan: 06
type: execute
wave: 4
depends_on:
  - 101-02
  - 101-03
  - 101-04
  - 101-05
files_modified:
  - scripts/ci/check-ssh-semaphore-coverage.sh
  - package.json
autonomous: true
requirements:
  - SSH-CAP-CI-GUARD
must_haves:
  truths:
    - "A shell script `scripts/ci/check-ssh-semaphore-coverage.sh` exists, is chmod +x, and grep-scans the backend for `connectOneShot(` / `session.client.exec(` / `tailSessionFile(` call sites."
    - "For every match, the script asserts that the enclosing file OR a small radius around the match imports `host-semaphore-registry` OR calls `getHostSemaphore(` / `acquireTailSlot(` (D-07)."
    - "The script has an allow-list section citing legitimate exceptions: `starter.ts::makeSemaphore` if D-02 left it in place, `session-file-tail.ts` (no hostId available — covered by `acquireTailSlot` at 3 caller sites, not internally), `host-transfer.ts` (covered transitively via `openDedicatedTransferSession`), test files (`.test.ts`)."
    - "Exit code is 0 when all producers are covered, 1 (with a per-file failure list) when any uncovered producer is detected."
    - "`package.json` has a new `verify:ssh-cap` script that invokes the shell script (D-07 chosen over ESLint plugin)."
    - "The final phase sweep runs `npm run verify:ssh-cap && npm run type-check && npm run test && npm run build:backend` and all four commands return exit 0."
  artifacts:
    - path: "scripts/ci/check-ssh-semaphore-coverage.sh"
      provides: "grep-based coverage guard"
      min_lines: 40
    - path: "package.json"
      provides: "`verify:ssh-cap` script entry"
      contains: "verify:ssh-cap"
  key_links:
    - from: "package.json::scripts::verify:ssh-cap"
      to: "scripts/ci/check-ssh-semaphore-coverage.sh"
      via: "bash script invocation"
      pattern: "verify:ssh-cap"
    - from: "scripts/ci/check-ssh-semaphore-coverage.sh"
      to: "src/backend/**/*.ts (excluding tests)"
      via: "grep for connectOneShot / session.client.exec / tailSessionFile with coverage assertion"
      pattern: "connectOneShot\\|session\\.client\\.exec\\|tailSessionFile"
---

<objective>
Land a CI-grep guard so future SSH-producing code cannot slip into the tree
uncapped. This is the "test that the tests are correct" — Plans 101-01
through 101-05 covered every known producer; Plan 101-06 makes sure a future
patch that adds `connectOneShot(...)` somewhere new either uses
`getHostSemaphore(...)` or is explicitly allow-listed with a comment.

Purpose: The coverage we built in Waves 1-3 is only useful if a future
producer doesn't quietly bypass it. Grep is cheap, deterministic, and
requires zero new dependencies (D-07 chose it over an ESLint plugin).

Output: `scripts/ci/check-ssh-semaphore-coverage.sh` + `package.json`
`verify:ssh-cap` script + a full final phase sweep proving nothing regressed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/100-route-all-outbound-ssh-work-through-a-per-host-semaphore-re/101-01-SUMMARY.md
@.planning/phases/100-route-all-outbound-ssh-work-through-a-per-host-semaphore-re/101-02-SUMMARY.md
@.planning/phases/100-route-all-outbound-ssh-work-through-a-per-host-semaphore-re/101-03-SUMMARY.md
@.planning/phases/100-route-all-outbound-ssh-work-through-a-per-host-semaphore-re/101-04-SUMMARY.md
@.planning/phases/100-route-all-outbound-ssh-work-through-a-per-host-semaphore-re/101-05-SUMMARY.md

@src/backend/ssh/host-semaphore-registry.ts
@scripts/verify-input-case-unchanged.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author check-ssh-semaphore-coverage.sh</name>
  <files>scripts/ci/check-ssh-semaphore-coverage.sh</files>
  <behavior>
    - Bash script with `#!/usr/bin/env bash` shebang, `set -euo pipefail`.
    - Scans `src/backend/**/*.ts` (excluding `**/*.test.ts` and `**/*.d.ts`).
    - For each file, checks whether it contains ANY of the three SSH-producer patterns:
      - `connectOneShot(` (one-shot Client factory).
      - `session\.client\.exec(` (long-lived-session exec channel opens — the file-manager-session shape).
      - `tailSessionFile(` (long-lived tail).
    - For every file that matches at least one pattern, asserts that the file either:
      - imports `host-semaphore-registry` (matches `from "..host-semaphore-registry`), OR
      - references `getHostSemaphore(` or `acquireTailSlot(` in the file, OR
      - appears in the ALLOW_LIST (a bash array declared near the top of the script).
    - Ignores comment-only matches — filter grep with `grep -v '^\s*[/*]'` and `grep -v '^\s*#'` before the check. Use the "count non-comment lines" pattern (Nyquist rule in the planner playbook: `grep -v '^#' | grep -c token`, not raw `grep -c` on unfiltered content).
    - On success: print "SSH semaphore coverage OK: N producers scanned, M allow-listed" and exit 0.
    - On failure: print each uncovered file (with the offending line number(s)) and exit 1.
    - Runs from repo root (`cd "$(git rev-parse --show-toplevel)"` at top).

    ALLOW_LIST bash array — exact entries:

    - `src/backend/ssh/session-file-tail.ts` — covered externally by `acquireTailSlot(hostId)` at 3 caller sites in claude-session-server.ts (D-06-adjacent — no hostId in its signature).
    - `src/backend/ssh/host-transfer.ts` — covered transitively via `openDedicatedTransferSession` resolver in file-manager.ts (D-06).
    - `src/backend/ssh/ssh-one-shot.ts` — the `connectOneShot` factory ITSELF (defining the pattern, not consuming it).
    - Any file matching `**/*.test.ts` — test files pattern (already excluded by the find command but add belt-and-braces).
    - `src/backend/starter.ts` — REVIEW BASED ON 101-02 SUMMARY: if D-02 primary path was chosen (`makeSemaphore` moved into registry), starter.ts is a legitimate consumer via `getHostSemaphore` and DOES NOT need allow-listing; if D-02 alternate was chosen (`makeSemaphore` left in starter), starter.ts still imports `getHostSemaphore` at both call sites so it's still a legitimate consumer — same outcome. Do NOT allow-list starter unless a specific find surfaces it.
  </behavior>
  <read_first>
    - `scripts/verify-input-case-unchanged.sh` (existing bash script style in this repo — mirror shebang, `set -euo pipefail` conventions, exit-code shape).
    - All five SUMMARYs from Plans 101-01..101-05 (which files were wrapped, what allow-list entries need adjustment).
    - `src/backend/ssh/host-semaphore-registry.ts` (the import string to grep for).
  </read_first>
  <action>
    Create `scripts/ci/check-ssh-semaphore-coverage.sh` (directory does not exist yet — script creates it). File contents (order and shape):

    1. Shebang + `set -euo pipefail`.
    2. `cd "$(git rev-parse --show-toplevel)"` — ensure repo-relative paths.
    3. Comment header naming Phase 101 + D-07 + wilma-incident + bounty b31a5c8e.
    4. `ALLOW_LIST` bash array with the entries listed in `<behavior>`.
    5. `PRODUCER_PATTERNS` regex: `connectOneShot\(|session\.client\.exec\(|tailSessionFile\(`.
    6. `COVERAGE_PATTERNS` regex: `host-semaphore-registry|getHostSemaphore\(|acquireTailSlot\(`.
    7. `find src/backend -name '*.ts' -not -name '*.test.ts' -not -name '*.d.ts'` → for each file:
       a. If file path matches any ALLOW_LIST entry → skip.
       b. Grep the file for PRODUCER_PATTERNS, filtering out comment lines: `grep -nE "$PRODUCER_PATTERNS" "$file" | grep -v -E '^\s*[0-9]+:\s*[/*#]'`.
       c. If any non-comment producer match: check for COVERAGE_PATTERNS in the same file (`grep -qE "$COVERAGE_PATTERNS" "$file"`).
       d. If no coverage: append file path + offending line numbers to a `FAILURES` array.
    8. After scan: if `FAILURES` non-empty, print each entry and exit 1. Else print the success summary and exit 0.
    9. Make executable: end the task by running `chmod +x scripts/ci/check-ssh-semaphore-coverage.sh`.

    RATIONALE for file-level coverage assertion (not line-level): a file that imports `getHostSemaphore` and uses it once is treated as covered even if it has multiple `connectOneShot` calls, because Plan 101-03/04/05 authors would wrap all producers in a file when they touched it. This intentionally trades false-negatives (a file with one wrapped and one unwrapped producer passes) for zero false-positives on well-authored PRs. If false-negatives become a problem, tighten to line-radius matching in a follow-up patch — DO NOT do that now (D-07 chose the light option).

    Test the script by running it locally: it must exit 0 after Plans 101-01..101-05 have landed.
  </action>
  <verify>
    <automated>bash scripts/ci/check-ssh-semaphore-coverage.sh</automated>
  </verify>
  <done>
    - `scripts/ci/check-ssh-semaphore-coverage.sh` exists.
    - `test -x scripts/ci/check-ssh-semaphore-coverage.sh` succeeds.
    - Running the script returns exit 0 (assuming Plans 101-01..101-05 landed).
    - If run against a hypothetical uncovered file (temporarily added, then removed), the script exits 1 with a per-file failure line — verify manually once, do NOT commit the temp file.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire verify:ssh-cap into package.json and run final phase sweep</name>
  <files>package.json</files>
  <behavior>
    - `package.json` gains a new script entry: `"verify:ssh-cap": "bash scripts/ci/check-ssh-semaphore-coverage.sh"`.
    - Insertion is inside the existing `"scripts": { ... }` block (currently around L12-42), placed alphabetically or grouped with other `verify:*` / `check:*` scripts (there are none today — just place it after `"test:coverage"` at L23 or before `"dev"` at L24, whichever produces the smallest diff).
    - No other package.json changes — no new deps, no version bump, no formatter reshuffle.
    - Final phase sweep: after wiring the script, execute the four verification commands and confirm all four exit 0:
      1. `npm run verify:ssh-cap`
      2. `npm run type-check`
      3. `npm run test`
      4. `npm run build:backend`
    - If any of the four fail, the executor stops here and reports — do NOT paper over failures.
  </behavior>
  <read_first>
    - `package.json` L12-42 (the `scripts` block — locate the insertion point).
    - `scripts/ci/check-ssh-semaphore-coverage.sh` (verify it exists and is chmod +x from Task 1).
  </read_first>
  <action>
    Edit `package.json` — add exactly one new line inside the `"scripts"` object:

    `"verify:ssh-cap": "bash scripts/ci/check-ssh-semaphore-coverage.sh",`

    Preserve the surrounding JSON formatting (2-space indent per existing convention, trailing commas where they already exist). If a formatter is configured (prettier — see the `format` script at L13), do NOT run it as part of this task — that would rewrite unrelated whitespace. Only the one new line diff.

    Run the four-command final sweep:
    - `npm run verify:ssh-cap` — must exit 0.
    - `npm run type-check` — must exit 0.
    - `npm run test` — must exit 0 (this is the full backend + frontend test suite; expect it to take several minutes).
    - `npm run build:backend` — must exit 0.

    If ANY of the four fail, DO NOT commit. Debug + fix (routing back to Plan 101-01..101-05 SUMMARYs as needed) before proceeding. The executor's remit stops here — no push, no docker, no deploy.
  </action>
  <verify>
    <automated>npm run verify:ssh-cap && npm run type-check && npm run build:backend</automated>
  </verify>
  <done>
    - `grep -c "verify:ssh-cap" package.json` = 1.
    - `npm run verify:ssh-cap` returns exit 0.
    - `npm run type-check` returns exit 0.
    - `npm run test` returns exit 0 (full suite green).
    - `npm run build:backend` returns exit 0.
    - No push, no docker build, no deploy — executor stops at atomic commits + scoped tests green.
  </done>
</task>

</tasks>

<verification>
  - `npm run verify:ssh-cap` exit 0.
  - `npm run type-check` exit 0.
  - `npm run test` exit 0 (full suite).
  - `npm run build:backend` exit 0.
  - The CI grep guard, if temporarily fed an uncovered file, exits 1 (verified once manually per Task 1's done-criteria).
</verification>

<success_criteria>
- Phase 101 delivers a durable safety net: any future PR that adds `connectOneShot(` or `session.client.exec(` or `tailSessionFile(` outside a semaphore-covered file will fail `npm run verify:ssh-cap`.
- The final phase sweep proves waves 1-3 all landed correctly under the aggregate cap policy.
</success_criteria>

<output>
Create `.planning/phases/100-route-all-outbound-ssh-work-through-a-per-host-semaphore-re/101-06-SUMMARY.md` when done. Record: (a) allow-list final contents, (b) which of the four sweep commands ran the longest, (c) any false-negatives observed during manual grep-guard testing so a future patch can tighten to line-radius matching if warranted.
</output>
