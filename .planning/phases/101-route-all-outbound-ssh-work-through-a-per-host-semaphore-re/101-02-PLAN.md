---
phase: 101
plan: 02
type: execute
wave: 2
depends_on:
  - 101-01
files_modified:
  - src/backend/starter.ts
autonomous: true
requirements:
  - SSH-CAP-STARTER
must_haves:
  truths:
    - "starter.ts's fleet-status acquireSshChannel (both branches, L618 and L666) uses `getHostSemaphore(host.id)` instead of an in-scope `makeSemaphore(8)`."
    - "starter.ts's substrate `substrateAcquireChannel` (L888-942) uses `getHostSemaphore(host.id)` instead of the `substrateHostSemaphores` Map + inline `makeSemaphore(8)` at L911."
    - "Fleet-status and substrate producers running on the SAME hostId now share ONE 8-slot pool (D-04) — previously each had an independent 8-cap per host."
    - "The `substrateHostSemaphores.clear()` cleanup at L1019 is removed (map no longer exists) — SIGTERM handler still tears down `substrateHostClients`."
    - "starter.test.ts still passes; the visible external behavior of both channel acquire paths (health-check probe, exec-then-return-null-on-error, channel-adapter shape) is unchanged."
  artifacts:
    - path: "src/backend/starter.ts"
      provides: "Fleet-status + substrate producers routed through shared registry"
      contains: "getHostSemaphore(host.id)"
  key_links:
    - from: "src/backend/starter.ts::acquireSshChannel"
      to: "src/backend/ssh/host-semaphore-registry.ts::getHostSemaphore"
      via: "import + call at both L618 and L666 branches"
      pattern: "getHostSemaphore\\(host\\.id\\)"
    - from: "src/backend/starter.ts::substrateAcquireChannel"
      to: "src/backend/ssh/host-semaphore-registry.ts::getHostSemaphore"
      via: "import + call at L908-913 replacement site"
      pattern: "getHostSemaphore\\(host\\.id\\)"
---

<objective>
Migrate the two in-scope semaphore call sites in `starter.ts` (fleet-status +
substrate) to the shared registry from Plan 101-01. This is where D-04 lands:
the fleet-status IIFE and the substrate orchestrator IIFE currently maintain
INDEPENDENT 8-slot pools per host, meaning a single host can burn 16 exec
channels between them before we cap. After this plan they share ONE 8-slot
pool per host — correct aggregate policy.

Purpose: Kill the "two independent 8-caps per host" bug in the two heaviest
SSH producers in the codebase (fleet-status polls all hosts every N seconds;
substrate polls all substrate hosts every 30s). This is the single biggest
consolidation win in Phase 101.

Output: `starter.ts` with two migrated call sites, tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/100-route-all-outbound-ssh-work-through-a-per-host-semaphore-re/101-01-SUMMARY.md

@src/backend/starter.ts
@src/backend/ssh/host-semaphore-registry.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate fleet-status acquireSshChannel branches to registry</name>
  <files>src/backend/starter.ts</files>
  <behavior>
    - The health-check-reuse branch (currently L616-632) obtains its semaphore from `getHostSemaphore(host.id)` instead of `makeSemaphore(8)`.
    - The fresh-connect branch (currently L664-675) obtains its semaphore from `getHostSemaphore(host.id)` instead of `makeSemaphore(8)`.
    - Both branches, when acquiring the SAME host.id on the same runtime, now hold references to the SAME Semaphore instance (previously they created a NEW one each call).
    - The channel adapter's outer try/catch → null (L620-628, L668-675) is unchanged — it remains the sole null-conversion point in the exec pipeline (preserves the L98-100 contract).
    - Health probe at L632 still routes through the same sem instance.
    - No new imports beyond `getHostSemaphore`. No behavior change to `hookInstallAttempted`, hostClients Map, or event handlers.
  </behavior>
  <read_first>
    - `src/backend/starter.ts` L605-720 (both acquireSshChannel branches — the exact call sites to replace).
    - `src/backend/ssh/host-semaphore-registry.ts` (the API to call).
    - `src/backend/starter.ts` L95-123 (understand the `makeSemaphore` contract we're preserving).
  </read_first>
  <action>
    Add near existing imports at the top of `starter.ts`: `import { getHostSemaphore } from "./ssh/host-semaphore-registry.js";` (adjust to match relative path convention already in use in the file — same directory as ssh/, so `./ssh/host-semaphore-registry.js`).

    At the health-check-reuse branch (currently L618): replace `const sem = makeSemaphore(8);` with `const sem = getHostSemaphore(host.id);`. Preserve every other line: the channel adapter, the try/catch → null, the L632 probe call.

    At the fresh-connect branch (currently L666): replace `const sem = makeSemaphore(8);` with `const sem = getHostSemaphore(host.id);`. Preserve the channelAdapter definition, hookInstallAttempted install path, and all event handlers.

    Update or preserve the surrounding comment blocks at L616-617 and L664-665 to name the shift: "Bounty b31a5c8e (Phase 101): per-host semaphore now shared via registry — the fleet-status + substrate + route producers running on the same host now share a single 8-slot pool." Do NOT strip the wilma-incident citation.

    Do NOT modify `makeSemaphore`'s definition at L102-122 in this task — Plan 101-01's D-02 path decides whether it stays or moves. If Plan 101-01's SUMMARY says the factory was moved into the registry, DELETE L102-123 of starter.ts (the whole `makeSemaphore` block + its comment banner) in this task; if the SUMMARY says it stayed, leave the definition intact (registry imports from starter in that case).

    Run `npm run build:backend` after the edit.
  </action>
  <verify>
    <automated>npm run type-check && npm run test -- starter</automated>
  </verify>
  <done>
    - Both branches at L618 and L666 (line numbers pre-edit) now call `getHostSemaphore(host.id)`.
    - `grep -n "makeSemaphore(8)" src/backend/starter.ts` in the fleet-status IIFE (L500-720) returns zero hits.
    - `npm run type-check` clean.
    - `npm run test -- starter` green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate substrateAcquireChannel + drop substrateHostSemaphores map</name>
  <files>src/backend/starter.ts</files>
  <behavior>
    - `substrateAcquireChannel` (L888-942) no longer references `substrateHostSemaphores`. It calls `getHostSemaphore(host.id)` at the point where it currently does the lazy-init lookup (L908-913).
    - The module-level `const substrateHostSemaphores = new Map<...>()` declaration at L883-886 is removed.
    - The `substrateHostSemaphores.clear()` line at L1019 (inside the SIGTERM handler) is removed. The rest of that SIGTERM block (systemLogger.info, substrateOrch.stop(), for-loop that ends all `substrateHostClients`) is UNCHANGED.
    - The captured-sem shape (`capturedClient`, `capturedSem`) at L915-916 is preserved — just source `sem` from the registry.
    - Result: substrate + fleet-status running on the same host.id now share ONE 8-slot pool.
  </behavior>
  <read_first>
    - `src/backend/starter.ts` L878-950 (substrate acquire + release + Map declaration).
    - `src/backend/starter.ts` L1005-1021 (SIGTERM cleanup — the `.clear()` line to drop).
    - `src/backend/ssh/host-semaphore-registry.ts` (API).
  </read_first>
  <action>
    In `substrateAcquireChannel` (starter.ts L888-942):
    - Delete the block currently at L908-913 (`let sem = substrateHostSemaphores.get(host.id); if (!sem) { sem = makeSemaphore(8); ... }`).
    - Replace with `const sem = getHostSemaphore(host.id);` — single line.
    - Rest of the function (client lazy-init, captured references, exec-then-null adapter, warn log on connect failure) unchanged.

    Delete the map declaration at L883-886 (the `substrateHostSemaphores` Map and its 3-line comment above).

    In the SIGTERM handler (L1006-1020), delete the `substrateHostSemaphores.clear();` line at L1019. Preserve every other line (systemLogger call, substrateOrch.stop(), the for-loop ending substrateHostClients, `substrateHostClients.clear();`).

    Update the comment banner at L880-882 that references the wilma-incident: adjust to say the cap is now maintained by the shared registry (import at top of file). Keep the wilma-incident + MaxSessions=10 citation.

    If Plan 101-01's SUMMARY chose to LEAVE `makeSemaphore` in starter.ts, this task additionally removes any now-unused `makeSemaphore` reference here (there should be none after the two edits). If Plan 101-01 MOVED it, Task 1 already removed the definition — this task just verifies the file has no dangling references (`grep -c "makeSemaphore" src/backend/starter.ts | grep -v '^#'` should be 0).
  </action>
  <verify>
    <automated>npm run type-check && npm run test -- starter && npm run build:backend</automated>
  </verify>
  <done>
    - `substrateAcquireChannel` calls `getHostSemaphore(host.id)`; no local map lookup remains.
    - `grep -v '^#' src/backend/starter.ts | grep -c "substrateHostSemaphores"` = 0.
    - `grep -v '^#' src/backend/starter.ts | grep -c "makeSemaphore(8)"` = 0.
    - SIGTERM block still cleans up `substrateHostClients` (unchanged).
    - `npm run build:backend` clean.
    - `npm run test -- starter` green.
  </done>
</task>

</tasks>

<verification>
  - `npm run type-check` clean.
  - `npm run test -- starter` green.
  - `npm run build:backend` clean.
  - Both fleet-status branches + substrate branch import from `host-semaphore-registry.js`.
  - No `substrateHostSemaphores` symbol anywhere in `starter.ts`.
</verification>

<success_criteria>
- Two heaviest SSH producers (fleet-status + substrate) share ONE 8-slot pool per host.
- starter.test.ts + any starter integration tests still green.
- No behavioral regressions in either IIFE — health probes, event handlers, SIGTERM cleanup all preserved.
</success_criteria>

<output>
Create `.planning/phases/100-route-all-outbound-ssh-work-through-a-per-host-semaphore-re/101-02-SUMMARY.md` when done. Record whether `makeSemaphore` was moved (D-02 primary) or left in starter (D-02 alternate) so Plan 101-06's CI grep guard is calibrated correctly.
</output>
