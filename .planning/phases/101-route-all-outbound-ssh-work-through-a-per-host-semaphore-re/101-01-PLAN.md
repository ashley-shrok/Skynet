---
phase: 101
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/ssh/host-semaphore-registry.ts
  - src/backend/ssh/host-semaphore-registry.test.ts
autonomous: true
requirements:
  - SSH-CAP-REG
must_haves:
  truths:
    - "A single module-scope Map<string, Semaphore> exists so all SSH producers share ONE slot pool per hostId."
    - "getHostSemaphore('7') and getHostSemaphore(7) return the same Semaphore instance (D-01 key normalization)."
    - "getHostSemaphore(hostA) and getHostSemaphore(hostB) return distinct Semaphore instances."
    - "With cap=8, a 9th concurrent run(fn) call for the same hostId queues FIFO and only resumes when a prior slot releases."
    - "starter.ts continues to compile: makeSemaphore is either MOVED to the registry and re-exported (D-02 primary) or LEFT IN PLACE and re-imported by the registry (D-02 alternate); either way behavior is byte-identical."
  artifacts:
    - path: "src/backend/ssh/host-semaphore-registry.ts"
      provides: "getHostSemaphore(hostId, limit?) resolver + shared Semaphore factory"
      exports: ["getHostSemaphore", "__resetHostSemaphoreRegistryForTests"]
    - path: "src/backend/ssh/host-semaphore-registry.test.ts"
      provides: "Same-instance / distinct-instance / normalization / FIFO-under-contention coverage (D-08)"
  key_links:
    - from: "src/backend/ssh/host-semaphore-registry.ts"
      to: "src/backend/starter.ts::makeSemaphore"
      via: "either re-import (alternate) or moved-into-registry-and-re-exported (primary)"
      pattern: "makeSemaphore"
---

<objective>
Introduce a shared per-host SSH semaphore registry (D-01) that every subsequent
plan in Phase 101 will route its SSH work through. Land the module + its unit
test suite in Wave 1 so downstream waves can import from a stable, tested API.

Purpose: Today `starter.ts` has TWO independent `makeSemaphore(8)` instances
per host (fleet-status at L618/L666, substrate at L911) plus at least 9
other producer sites that acquire uncapped SSH channels. That means a single
host with fleet-status + substrate + a route-driven exec + widgets can burn
30+ concurrent ssh2 exec channels and blow past OpenSSH's default
MaxSessions=10 (the wilma-incident failure mode). Consolidating on one
`Map<hostId, Semaphore>` module-scope registry with cap=8 gives us ONE shared
slot pool per host across ALL producers — correct aggregate policy per D-04.

Output: A tested `host-semaphore-registry.ts` module + `.test.ts` companion,
both compiled and green under `npm run test -- host-semaphore-registry`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

@src/backend/starter.ts
@src/backend/ssh/host-resolver.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the host-semaphore-registry module (D-01, D-02)</name>
  <files>src/backend/ssh/host-semaphore-registry.ts</files>
  <behavior>
    - `getHostSemaphore(hostId, limit=8)` returns an object with `run<T>(fn: () => Promise<T>): Promise<T>`.
    - Module-scope `Map<string, Semaphore>` — lazy-init per host on first call.
    - Key normalization: `String(hostId)` so `1` and `"1"` hit the same slot (D-01).
    - Second and subsequent calls for the same normalized hostId return the SAME Semaphore instance (===).
    - Semaphore internals identical to `starter.ts::makeSemaphore` (L102-122): counting semaphore, FIFO queue, try/finally slot release, errors propagate unchanged (no null-conversion in this module).
    - No timing / no timeouts — pure counting semaphore.
    - Export `__resetHostSemaphoreRegistryForTests()` that clears the map — required by the test file's `beforeEach`. Underscore prefix marks it internal-only.
  </behavior>
  <read_first>
    - `src/backend/starter.ts` L95-123 (the existing `makeSemaphore` factory and its comment block — this is the reference implementation to lift or re-import per D-02).
    - `src/backend/starter.ts` L880-920 (the substrate map-of-semaphores pattern — validates the shape we're consolidating).
  </read_first>
  <action>
    Per D-02 primary path: MOVE the `makeSemaphore` factory from `starter.ts:102-122` INTO the new `host-semaphore-registry.ts` module as an internal helper. `starter.ts` then imports either (a) `makeSemaphore` from the registry for any legacy in-scope needs OR (b) drops its inline call sites in Plan 101-02. Registry file layout:

    - Header JSDoc block naming the wilma-incident MaxSessions=10 rationale (mirror the comment at `starter.ts:881-882`) and citing bounty b31a5c8e.
    - Type export `HostSemaphore = { run<T>(fn: () => Promise<T>): Promise<T> }`.
    - Internal `makeSemaphore(limit: number): HostSemaphore` — verbatim lift from `starter.ts:103-123`.
    - Module-scope `const registry = new Map<string, HostSemaphore>()`.
    - Exported `getHostSemaphore(hostId: string | number, limit: number = 8): HostSemaphore` that normalizes via `String(hostId)`, checks the map, creates+stores on miss, returns the instance.
    - Exported `__resetHostSemaphoreRegistryForTests(): void` that calls `registry.clear()`. Name-prefix `__` and JSDoc note it is test-only.

    Alternate path (D-02 executor discretion): leave `makeSemaphore` in `starter.ts` and have the registry `import { makeSemaphore } from "../starter.js"`. Either is acceptable — pick the one that keeps `starter.ts` compiling cleanly under `npm run build:backend` at end of task. If the alternate is chosen, note it in the task-2 SUMMARY notes so Plan 101-02 knows the entry point.

    NO functional changes to the semaphore semantics (D-01 preserves the wilma-incident cap, FIFO queue, error-propagation contract). NO logging, NO metrics, NO telemetry — future work.

    Run `npm run build:backend` at the end to confirm both the new module and `starter.ts` compile cleanly.
  </action>
  <verify>
    <automated>npm run type-check && npm run build:backend</automated>
  </verify>
  <done>
    - `src/backend/ssh/host-semaphore-registry.ts` exists with `getHostSemaphore` + `__resetHostSemaphoreRegistryForTests` exports.
    - `npm run type-check` passes.
    - `npm run build:backend` passes.
    - `starter.ts` still resolves its `makeSemaphore` symbol (either via move-and-re-export or in-place).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit test suite covering D-01 + D-08 invariants</name>
  <files>src/backend/ssh/host-semaphore-registry.test.ts</files>
  <behavior>
    Vitest suite with a `beforeEach` that calls `__resetHostSemaphoreRegistryForTests()` so cases do not leak.

    Test A — same hostId returns the same Semaphore instance:
      `expect(getHostSemaphore('42')).toBe(getHostSemaphore('42'))`.
    Test B — different hostIds return different Semaphore instances:
      `expect(getHostSemaphore('42')).not.toBe(getHostSemaphore('43'))`.
    Test C — key normalization (D-01): `getHostSemaphore(1)` and `getHostSemaphore('1')` return the SAME instance:
      `expect(getHostSemaphore(1)).toBe(getHostSemaphore('1'))`.
    Test D — cap=8 slot semantics with FIFO queueing (D-08):
      Acquire a semaphore for hostId 'x' with limit=8. Launch 9 `run(fn)` calls where each `fn` awaits a resolvable Deferred. Assert only 8 Deferreds have started (mark started with a counter incremented inside each fn BEFORE the await). Resolve Deferred[0]. Assert Deferred[8] (the 9th) then starts. Confirm FIFO order — Deferred[8] must start before any later addition. Finally resolve all Deferreds and await all `run` promises to keep the test hermetic.
    Test E — error path: a `run(fn)` where fn throws must still release the slot (queued next waiter resumes).

    All tests deterministic — no `setTimeout(0)` / no `await Promise.resolve()` chains that depend on microtask ordering beyond what Vitest gives synchronously. Use explicit Deferreds (Promise + captured resolve) for contention.
  </behavior>
  <read_first>
    - `src/backend/ssh/host-resolver.test.ts` L1-30 (style/imports/vitest layout used elsewhere in this directory).
    - `src/backend/starter.ts` L95-123 (semaphore semantics to test against — FIFO queue, try/finally release).
  </read_first>
  <action>
    Create `host-semaphore-registry.test.ts` alongside the module. Standard vitest `describe`/`it` layout mirroring `host-resolver.test.ts` conventions. Import from `./host-semaphore-registry.js`. `beforeEach` resets the registry. Five test cases as specified in `<behavior>` (A same-instance, B distinct-instance, C normalization, D FIFO under contention, E error-path slot release).

    Test D uses this Deferred helper (inline in the test file, NOT exported):

    - Create an array of 9 `{promise, resolve}` deferreds.
    - Track a `started` counter that each `run(fn)` increments before awaiting its deferred.
    - Kick off all 9 `run(fn)` calls without awaiting the returned promises yet.
    - `await Promise.resolve()` a couple of times so the synchronous portion of each `run` executes (the semaphore's slot check + started++ line lands before the await).
    - Assert `started === 8`.
    - Resolve deferred[0], flush microtasks, assert `started === 9`.
    - Resolve remaining 1..8, await all 9 run-promises to completion — no dangling promises.

    No mock of ssh2 needed — this test operates ONLY on the semaphore, not on real SSH.
  </action>
  <verify>
    <automated>npm run test -- host-semaphore-registry</automated>
  </verify>
  <done>
    - `src/backend/ssh/host-semaphore-registry.test.ts` exists with 5 cases (A/B/C/D/E).
    - `npm run test -- host-semaphore-registry` reports all 5 green.
    - `npm run type-check` still passes.
  </done>
</task>

</tasks>

<verification>
  - `npm run type-check` clean.
  - `npm run build:backend` clean.
  - `npm run test -- host-semaphore-registry` all 5 tests green.
  - `grep -n "getHostSemaphore" src/backend/ssh/host-semaphore-registry.ts` prints exactly one exported function.
</verification>

<success_criteria>
- Registry module + tests landed and green.
- `starter.ts` compiles unchanged in behavior (Plan 101-02 will migrate its call sites).
- Downstream plans (101-02..101-05) can `import { getHostSemaphore } from "../ssh/host-semaphore-registry.js"` (adjust relative path per site).
</success_criteria>

<output>
Create `.planning/phases/100-route-all-outbound-ssh-work-through-a-per-host-semaphore-re/101-01-SUMMARY.md` when done — record which D-02 path was chosen (move-into-registry vs re-import) so Plan 101-02 knows the entry point.
</output>
