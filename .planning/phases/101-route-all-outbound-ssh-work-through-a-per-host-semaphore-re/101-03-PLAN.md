---
phase: 101
plan: 03
type: execute
wave: 3
depends_on:
  - 101-01
files_modified:
  - src/backend/database/routes/identity-clone.ts
  - src/backend/database/routes/roles-create.ts
  - src/backend/database/routes/global-files-read-write.ts
autonomous: true
requirements:
  - SSH-CAP-ROUTES-A
must_haves:
  truths:
    - "identity-clone.ts's connectOneShot → exec* → close block runs inside `getHostSemaphore(hostId).run(...)` (D-03: wrap the WHOLE motion, not just exec)."
    - "roles-create.ts's connectOneShot → execWithTimeout + SFTP write → close block runs inside `getHostSemaphore(hostId).run(...)`."
    - "global-files-read-write.ts's connectOneShot → execWithTimeout(s) → close block (BOTH the GET-read handler at L177+ and the POST-write handler) runs inside `getHostSemaphore(hostId).run(...)`."
    - "Each route's existing 502/404/timeout semantics are preserved — the semaphore is a wrapper around the SSH motion, not a replacement of it."
    - "Slot is held from `connectOneShot(...)` through the final `conn.end()` (or its equivalent cleanup); if the caller throws inside the run, the slot still releases via try/finally in the registry (already tested in Plan 101-01)."
  artifacts:
    - path: "src/backend/database/routes/identity-clone.ts"
      provides: "identity-clone route wrapped"
    - path: "src/backend/database/routes/roles-create.ts"
      provides: "roles-create route wrapped"
    - path: "src/backend/database/routes/global-files-read-write.ts"
      provides: "global-files GET + POST wrapped"
  key_links:
    - from: "src/backend/database/routes/identity-clone.ts"
      to: "src/backend/ssh/host-semaphore-registry.ts::getHostSemaphore"
      via: "wrap `connectOneShot(...)` through `.close()` inside `getHostSemaphore(hostId).run(...)`"
      pattern: "getHostSemaphore\\("
    - from: "src/backend/database/routes/roles-create.ts"
      to: "src/backend/ssh/host-semaphore-registry.ts::getHostSemaphore"
      via: "wrap `connectOneShot(...)` through SFTP write + close inside registry.run"
      pattern: "getHostSemaphore\\("
    - from: "src/backend/database/routes/global-files-read-write.ts"
      to: "src/backend/ssh/host-semaphore-registry.ts::getHostSemaphore"
      via: "wrap both handlers' connectOneShot → exec* → close inside registry.run"
      pattern: "getHostSemaphore\\("
---

<objective>
Wrap three route-driven SSH producers in per-host semaphore slots (D-03). All
three follow the same shape: `resolveHostById → connectOneShot → execWithTimeout
(and/or SFTP) → finally { conn.end() }`. Today none of them coordinate with
fleet-status or substrate — Ashley clicking "clone identity" while the
fleet-status IIFE is polling that host can push us past MaxSessions=10.

Purpose: Close the biggest route-driven leak points to the shared cap. These
three routes are the most user-visible ("clone identity", "create role",
"read/write global file") and the most likely to fire during a fleet-status
tick.

Output: Three route files wrapped, tests green.

Parallel-safe with 101-04 and 101-05 (file-disjoint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/100-route-all-outbound-ssh-work-through-a-per-host-semaphore-re/101-01-SUMMARY.md

@src/backend/database/routes/identity-clone.ts
@src/backend/database/routes/roles-create.ts
@src/backend/database/routes/global-files-read-write.ts
@src/backend/ssh/host-semaphore-registry.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wrap identity-clone.ts SSH work motion</name>
  <files>src/backend/database/routes/identity-clone.ts</files>
  <behavior>
    - The block spanning `connectOneShot(host, ...)` through the terminal `conn.end()` (or equivalent cleanup) is executed inside `await getHostSemaphore(hostId).run(async () => { ... })` (D-03).
    - The route's error envelope shapes (400/404/502/timeout) are byte-identical to today's — the wrap does NOT swallow errors; anything thrown inside the run propagates to the existing outer try/catch.
    - The slot releases when the `run(fn)` promise settles (success OR error) — guaranteed by the registry's try/finally.
    - Pre-SSH validation (JSON body parsing, hostId validation, resolveHostById) stays OUTSIDE the semaphore. We only cap the SSH motion, not the DB lookups.
  </behavior>
  <read_first>
    - `src/backend/database/routes/identity-clone.ts` L470-580 (the `resolveHostById` call, `connectOneShot`, exec calls, and the `conn.end()` cleanup — this is the exact motion to wrap).
    - `src/backend/database/routes/identity-clone.ts` L34-45 (route flow docstring — verify semantic match after the edit).
    - `src/backend/ssh/host-semaphore-registry.ts` (API).
  </read_first>
  <action>
    Add near the existing imports: `import { getHostSemaphore } from "../../ssh/host-semaphore-registry.js";`.

    Locate the section starting at the `connectOneShot(...)` call (L479-483 in current tree) and ending at the terminal cleanup (`conn.end()` or the equivalent finally block that closes it). Wrap the ENTIRE block — including the `execCommand` / SFTP writes / any post-exec verifications — inside `await getHostSemaphore(hostId).run(async () => { ... });`.

    If the existing code assigns intermediate variables at outer scope (e.g., `let conn: ... | null = null;` at L479), keep those declarations outside the wrap so post-run cleanup / logging that references them still compiles. The `run` block's return value (if any) should be assigned back to the outer scope so response construction still works.

    Do NOT wrap `resolveHostById` — that's a DB call, not SSH work.
    Do NOT wrap the response send — `res.status(...).json(...)` should fire AFTER the run resolves.

    Preserve every log call, every error branch, every timeout constant.
  </action>
  <verify>
    <automated>npm run type-check && npm run test -- identity-clone</automated>
  </verify>
  <done>
    - `grep -c "getHostSemaphore" src/backend/database/routes/identity-clone.ts` >= 1.
    - The SSH motion is inside the wrap (visible via inspection of the diff — connectOneShot → exec → close all appear inside one `run(async () => { ... })` block).
    - `npm run test -- identity-clone` green.
    - `npm run type-check` clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wrap roles-create.ts SSH work motion</name>
  <files>src/backend/database/routes/roles-create.ts</files>
  <behavior>
    - Same shape as Task 1: the `connectOneShot(...)` through terminal `conn.end()` block runs inside `await getHostSemaphore(hostId).run(async () => { ... })`.
    - The 502 error contract on connect/exec/SFTP failure is preserved (route docstring L28).
    - `execWithTimeout` calls (L173-196) continue to enforce their own timeout inside the wrap — the semaphore does not stack a second timeout (D-01: no timing).
    - Collision probe (route step 7) stays inside the wrap since it's part of the SSH motion.
    - Pre-SSH steps (body parse, resolveHostById, cosmetics validation — route steps 1-6) stay OUTSIDE the wrap.
  </behavior>
  <read_first>
    - `src/backend/database/routes/roles-create.ts` L430-540 (the connectOneShot + execWithTimeout + SFTP write + close block).
    - `src/backend/database/routes/roles-create.ts` L169-196 (`execWithTimeout` — verify it's inside the wrap since it uses `conn`).
    - `src/backend/ssh/host-semaphore-registry.ts` (API).
  </read_first>
  <action>
    Import `getHostSemaphore` at top: `import { getHostSemaphore } from "../../ssh/host-semaphore-registry.js";`.

    Locate the section that starts at the `connectOneShot(...)` call at L442 and ends at the terminal cleanup that closes `conn`. Wrap that entire block inside `await getHostSemaphore(hostId).run(async () => { ... })`.

    The wrap MUST include: the `connectOneShot` call, every `execWithTimeout` call, the SFTP write, the collision probe, and the `conn.end()` / cleanup. Nothing SSH-side may live outside the wrap.

    The pre-wrap validation stack (`rawHostId`, `hostId = rawHostId`, `resolveHostById(hostId, userId)`) stays OUTSIDE the wrap. `hostId` is captured by the closure — that's the value the registry keys on.

    Preserve every error branch (400 malformed, 404 unknown host, 502 SSH failure), every log call, every timeout constant.
  </action>
  <verify>
    <automated>npm run type-check && npm run test -- roles-create</automated>
  </verify>
  <done>
    - `grep -c "getHostSemaphore" src/backend/database/routes/roles-create.ts` >= 1.
    - Connect + exec + SFTP + close all appear inside one `run(async () => { ... })` block (visible in the diff).
    - `npm run test -- roles-create` green.
    - `npm run type-check` clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wrap global-files-read-write.ts BOTH handlers</name>
  <files>src/backend/database/routes/global-files-read-write.ts</files>
  <behavior>
    - The GET handler (read path, currently around L120-260) has its `connectOneShot → execWithTimeout(s) → close` block wrapped inside `await getHostSemaphore(hostId).run(async () => { ... })`.
    - The POST handler (write path) has the same wrap.
    - Both handlers preserve their 502 error contract (route docstring L22).
    - Whitelist enforcement (`getFilesForHost` at L167) — if it's an SSH-side check, stays inside the wrap; if it's a config lookup only, stays outside (inspect on read to decide).
    - `execWithTimeout` inside the wrap unchanged.
  </behavior>
  <read_first>
    - `src/backend/database/routes/global-files-read-write.ts` L116-260 (GET handler body).
    - `src/backend/database/routes/global-files-read-write.ts` L260+ (POST handler body — read the full file to identify its `connectOneShot` line).
    - `src/backend/ssh/host-semaphore-registry.ts` (API).
  </read_first>
  <action>
    Import `getHostSemaphore` once at top: `import { getHostSemaphore } from "../../ssh/host-semaphore-registry.js";`.

    GET handler: locate the `connectOneShot(...)` at L180 and the terminal cleanup. Wrap the entire SSH motion (connect + `execWithTimeout("echo $HOME")` at L203 + read commands at L232-238 + close) inside `await getHostSemaphore(hostId).run(async () => { ... })`.

    POST handler: find the analogous `connectOneShot(...)` call and its cleanup. Wrap identically.

    `getFilesForHost(config, ...)` at L167 is a synchronous config lookup — stays OUTSIDE the wrap (no SSH involved).

    Pre-SSH validation for both handlers (hostId parse, resolveHostById, whitelist check) stays outside. Only the SSH motion is inside the wrap.

    Preserve every log call, every error branch, every response shape.
  </action>
  <verify>
    <automated>npm run type-check && npm run test -- global-files-read-write</automated>
  </verify>
  <done>
    - `grep -c "getHostSemaphore" src/backend/database/routes/global-files-read-write.ts` >= 2 (once per handler, or once at top-of-file if hoisted — either way, 2+ visible call sites in the code).
    - Both handlers' connect → exec → close blocks live inside `run(async () => { ... })`.
    - `npm run test -- global-files-read-write` green.
    - `npm run type-check` clean.
  </done>
</task>

</tasks>

<verification>
  - `npm run type-check` clean.
  - `npm run test -- identity-clone roles-create global-files-read-write` all green.
  - `npm run build:backend` clean.
  - All three files import from `host-semaphore-registry.js`.
</verification>

<success_criteria>
- Three route-driven producers now share the per-host cap with fleet-status + substrate.
- No route response contracts change (all existing tests green).
- Every SSH motion in these three files is inside a slot.
</success_criteria>

<output>
Create `.planning/phases/100-route-all-outbound-ssh-work-through-a-per-host-semaphore-re/101-03-SUMMARY.md` when done — record whether GET and POST in global-files share a helper or each have their own wrap (informs the CI grep guard).
</output>
