---
phase: 66-skynet-reads-and-writes-identity-cosmetics-from-disk
plan: 05
type: execute
wave: 3
depends_on:
  - 66-02
  - 66-03
files_modified:
  - src/ui/state/identities-store.ts
  - src/ui/state/conversation-store.ts
  - src/ui/features/pretty-view/IdentityModal.tsx
  - src/ui/features/pretty-view/IdentityModal.test.tsx
  - src/ui/features/pretty-view/IdentityModal.voice.test.tsx
  - src/ui/features/pretty-view/IdentityModal.bounties-filter.test.tsx
  - src/ui/features/pretty-view/IdentityModal.lazy-archive.test.tsx
  - src/ui/features/pretty-view/IdentityModal.role-tab.test.tsx
  - src/ui/features/pretty-view/IdentityModal.share.test.tsx
  - src/ui/features/pretty-view/IdentityModal.stays-awake.test.tsx
  - src/ui/features/terminal/IdentityBadge.tsx
  - src/ui/features/pretty-conversations/PrettyConversationRow.tsx
  - src/ui/features/sessions/SessionRow.tsx
  - src/ui/sidebar/CloneAgentDialog.tsx
  - src/ui/state/identities-store.enrichment.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "listIdentities() is called with a populated identityHosts map derived from conversation-store fleetSessions — first-render cosmetics come back populated (not safe-defaults) for every identity that has a fleet session on record"
    - "publicIdentity() safe-defaults for displayName/avatarMime/avatarEtag are defined in Plan 03 (moved from this plan per checker B2); Plan 05's job is to populate identityHosts so the safe-defaults path is exercised RARELY — only when a genuinely-unreachable box is hit or when no fleet session exists for an identity"
    - "IdentityModal.tsx L1264 + L1396 thread hostId into the avatarUrl via the avatarUrlWithHost helper (defined in Plan 03) — the header + edit-drawer avatar images no longer emit literal 'undefined' in the ?v=<etag> cache-bust query"
    - "Every other identity.avatarUrl consumer (grep-verified enumeration: IdentityBadge.tsx L107, PrettyConversationRow.tsx L1173, SessionRow.tsx L55, CloneAgentDialog.tsx L548) either threads hostId when the endpoint requires it (per Plan 03 Test 7 asserting 400 without hostId) OR falls back gracefully when hostId is not in scope"
    - "All 7 IdentityModal.*.test.tsx files are audited + updated to pass hostId (and any newly-required plumbing) — none break at either compile-time or runtime"
    - "No new backend API surface introduced — reuses the GET /identities endpoint with the identityHosts query param + GET /:id/avatar with the hostId query param (both defined in Plan 03); no backend edits in this plan (publicIdentity change lives in Plan 03 per B2)"
  key_reads:
    - "Density note (post-B2): this plan tightened after moving publicIdentity safe-defaults to Plan 03 — no more backend edits here, only frontend enrichment + test-file shims. Total ~14 files but only 2 files carry logic weight (identities-store.ts + IdentityModal.tsx); the rest are grep-audit-and-thread work with a mechanical pattern per site. If execution shows context creep despite the trim, split-recovery is 05a (identities-store enrichment + conversation-store accessor + enrichment test) + 05b (IdentityModal + 5 avatarUrl consumers + 7 test-file shims)."
  artifacts:
    - path: "src/ui/state/conversation-store.ts"
      provides: "New public export getFleetSessionsSnapshot() — thin accessor over state.fleetSessions (L339). sessionMatchKey is NOT re-exported here — it lives in @/features/terminal/session-hue and is imported directly by identities-store per W4 (avoids adding a re-export that would create a circular dependency risk)."
    - path: "src/ui/state/identities-store.ts"
      provides: "fetchOnce() rewired to construct identityHosts from conversation-store.getFleetSessionsSnapshot() before calling listIdentities. Imports sessionMatchKey DIRECTLY from @/features/terminal/session-hue (per W4). Uses sessionMatchKey to map session.sessionName → identityKey, picking the first session's hostId per key. Re-fetches when fleet-sessions load (single re-fetch, guarded)."
    - path: "src/ui/features/pretty-view/IdentityModal.tsx"
      provides: "L1264 (header avatar) + L1396 (edit-drawer avatar) — both wrapped with avatarUrlWithHost(identity, hostId). Cache-bust ?v=<avatarEtag> preserved but guarded against empty-string etag (skip the ?v= when etag is empty; Plan 03's safe-defaults set avatarEtag to \"\" when disk is absent)."
    - path: "src/ui/features/terminal/IdentityBadge.tsx + PrettyConversationRow.tsx + SessionRow.tsx + CloneAgentDialog.tsx"
      provides: "Each avatarUrl consumer audited. Consumers that have hostId in scope thread it via avatarUrlWithHost. Consumers that lack hostId fall back to the initial-letter placeholder rendering (already the pattern for cosmetics-absent identities per shape file)."
    - path: "src/ui/state/identities-store.enrichment.test.ts"
      provides: "Tests for the identityHosts construction — happy path (sessions populated, map built), no-sessions (empty map, listIdentities called with {}), multi-session-same-identity (first-wins hostId), refresh-after-sessions-load (single re-fetch)."
  key_links:
    - from: "identities-store.fetchOnce"
      to: "listIdentities(identityHosts) — Plan 03 endpoint"
      via: "conversation-store fleetSessions snapshot → identityKey→hostId map"
      pattern: "listIdentities\\(.*identityHosts"
    - from: "identities-store.ts"
      to: "sessionMatchKey"
      via: "direct import from @/features/terminal/session-hue (per W4)"
      pattern: "from \"@/features/terminal/session-hue\""
    - from: "IdentityModal.tsx L1264 + L1396"
      to: "avatarUrlWithHost(identity, hostId) — Plan 03 helper"
      via: "hostId prop on IdentityModal (already declared at L170)"
      pattern: "avatarUrlWithHost\\(identity"
---

<objective>
Frontend enrichment for the READ path — populate the identityHosts wire parameter at page-load so Plan 03's disk-derived cosmetics come back POPULATED on first render (not safe-defaults), and thread hostId through every identity.avatarUrl consumer so GET /:id/avatar receives the required query param.

Purpose: Plan 03 defines the disk-derived READ endpoints AND publicIdentity safe-defaults for the frontend Identity type's non-nullable-string fields (per checker B2 co-location). But Plan 03 alone leaves the frontend passing `{}` for identityHosts, so every identity comes back with safe-defaults on first render. Plan 05 is the twin plan that lights up the READ path end-to-end — populating identityHosts from conversation-store fleetSessions so the safe-defaults path is exercised RARELY (only for genuinely-unreachable boxes or identities with no fleet session on record). Without Plan 05, Plan 03's endpoints work but the UI renders placeholder-only cosmetics until a follow-up quick lands. Per Ashley's greenlight (path A), Plan 05 lands in Wave 3 after Plan 03's Wave 2 — Plan 04 (Wave 4) depends on both being green.

Output:
- src/ui/state/conversation-store.ts: new public export `getFleetSessionsSnapshot()` — thin accessor over state.fleetSessions (L339). `sessionMatchKey` is NOT re-exported here — per W4, identities-store imports it directly from `@/features/terminal/session-hue` where it already lives (L54 of conversation-store already imports it that way).
- src/ui/state/identities-store.ts: fetchOnce rewired to build identityHosts from conversation-store.getFleetSessionsSnapshot() before calling listIdentities. Imports sessionMatchKey DIRECTLY from @/features/terminal/session-hue (per W4). Uses the same sessionMatchKey / identityKey.toLowerCase() correlation pattern already in use throughout the frontend (see conversation-store L438-439). One additional re-fetch fires after fleet-sessions load if they weren't loaded at the first fetchOnce call (guarded to not loop).
- src/ui/features/pretty-view/IdentityModal.tsx L1264 + L1396: wrap the avatarUrl with avatarUrlWithHost(identity, hostId). Also guard the ?v=<etag> cache-bust: when avatarEtag is empty (the safe-default from Plan 03's publicIdentity), skip the ?v= entirely rather than emitting `?v=`.
- Grep-driven audit of all 5 other identity.avatarUrl consumers (IdentityBadge.tsx L107, PrettyConversationRow.tsx L1173, SessionRow.tsx L55, CloneAgentDialog.tsx L548) — thread hostId when in scope, fall back to placeholder when not.
- All 7 IdentityModal.*.test.tsx files audited + shimmed. If any test omits hostId from its render setup, add it. If any test asserts on the avatarUrl string shape, update the assertion to accept the new `?hostId=<n>` query param.

NOTE (per B2 — moved to Plan 03): publicIdentity() safe-defaults for displayName/avatarMime/avatarEtag now live in Plan 03 Task 2. This plan does NOT edit publicIdentity or identities.ts (backend). This plan is pure-frontend + one thin conversation-store accessor export.

NOTE (per B1): The frontend Identity type at src/ui/api/identities-api.ts L6/L11/L13 declares displayName, avatarMime, avatarEtag as NON-nullable strings. Widening those to nullable is OUT OF SCOPE for this phase (bigger blast radius per checker guidance). Plan 03's safe-defaults from publicIdentity keep the wire contract satisfied — displayName defaults to identityKey capitalized, avatarMime + avatarEtag default to empty string.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/shapes/identity-prettiness-on-disk.md
@.planning/phases/66-skynet-reads-and-writes-identity-cosmetics-from-disk/CONTEXT.md
@CLAUDE.md

@src/ui/api/identities-api.ts
@src/ui/state/identities-store.ts
@src/ui/state/conversation-store.ts
@src/ui/features/terminal/session-hue.ts
@src/ui/features/pretty-view/IdentityModal.tsx
@src/ui/features/terminal/IdentityBadge.tsx
@src/ui/features/pretty-conversations/PrettyConversationRow.tsx
@src/ui/features/sessions/SessionRow.tsx
@src/ui/sidebar/CloneAgentDialog.tsx
@.planning/phases/66-skynet-reads-and-writes-identity-cosmetics-from-disk/66-03-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build identityHosts map from conversation-store + rewire fetchOnce + enrichment test</name>
  <files>src/ui/state/identities-store.ts, src/ui/state/conversation-store.ts, src/ui/state/identities-store.enrichment.test.ts</files>
  <read_first>
    - .planning/shapes/identity-prettiness-on-disk.md ("Skynet becomes a well-informed viewer of a filesystem-based truth")
    - .planning/phases/66-skynet-reads-and-writes-identity-cosmetics-from-disk/CONTEXT.md (Track 3 spec + open question 4 on cosmetics-absent tolerance)
    - src/ui/state/identities-store.ts (fetchOnce L41-55 — the call-site for listIdentities being rewired; withDisplayCap L23-29 for the capitalization pattern precedent)
    - src/ui/state/conversation-store.ts (fleetSessions L145-166 for FleetSession shape; state.fleetSessions at L339; the sessionMatchKey correlation pattern at L437-439; sessionMatchKey is imported from @/features/terminal/session-hue at L54 — confirmed W4)
    - src/ui/features/terminal/session-hue.ts (sessionMatchKey L4 — the AUTHORITATIVE source, per W4 identities-store imports it directly from here rather than through conversation-store re-export)
    - src/ui/api/identities-api.ts (listIdentities signature widening from Plan 03 — accepts identityHosts: Record<string,number> = {}; the widening + avatarUrlWithHost helper both land in Plan 03 Task 2)
  </read_first>
  <behavior>
    ─── conversation-store accessor export ───
    - Add ONLY `getFleetSessionsSnapshot(): FleetSession[]` to the public exports of conversation-store.ts. Thin accessor — returns `state.fleetSessions` (or a shallow copy for immutability discipline; match the pattern of other snapshot accessors in the file).
    - Do NOT re-export `sessionMatchKey` from conversation-store — per W4, identities-store imports it DIRECTLY from @/features/terminal/session-hue. Rationale: session-hue is the authoritative source; adding a re-export in conversation-store creates a second import path that other modules might latch onto, and risks a circular-dependency loop if session-hue ever imports from conversation-store.

    ─── identities-store rewiring ───
    - Import `sessionMatchKey` from `@/features/terminal/session-hue` (per W4).
    - Import `getFleetSessionsSnapshot` from conversation-store.
    - New helper `buildIdentityHostsFromFleet(fleetSessions: FleetSession[]): Record<string, number>` in identities-store.ts:
      1. Iterate fleetSessions in order.
      2. For each session, compute `identityKey = sessionMatchKey(session.sessionName)` (uses the direct import from session-hue).
      3. If identityKey is non-null AND NOT already in the map: `map[identityKey] = session.hostId` (first-wins — first session encountered per identity claims its hostId; matches the "one identity, one home box" invariant per shape file).
      4. Return the map.
    - Rewire fetchOnce() at L41-55: BEFORE calling listIdentities, snapshot conversation-store's fleetSessions via `getFleetSessionsSnapshot()`. Call `listIdentities(buildIdentityHostsFromFleet(fleetSessionsSnapshot))`.
    - Handle the ordering race: if `fleetSessionsLoaded === false` at first fetchOnce, cosmetics come back as safe-defaults on first render (Plan 03's publicIdentity safe-defaults handle this cleanly — the render doesn't crash, it just shows placeholder cosmetics). Add a one-time re-fetch: subscribe to conversation-store change events (there's a subscribe/notify pattern at conversation-store L14 for identitiesByKey — mirror that pattern OR find the existing subscribe API); when fleetSessionsLoaded flips from false → true, call `refreshIdentities()` ONCE (guarded by a module-level `hasRefreshedAfterFleetLoad = false` flag to prevent loops).

    ─── Tests ───
    - New test file src/ui/state/identities-store.enrichment.test.ts:
      Test 1 (buildIdentityHostsFromFleet happy): sessions = [{hostId:1,sessionName:"tina"}, {hostId:5,sessionName:"nelly"}] → map = { tina:1, nelly:5 }
      Test 2 (buildIdentityHostsFromFleet empty): sessions = [] → map = {}
      Test 3 (buildIdentityHostsFromFleet first-wins): sessions = [{hostId:1,sessionName:"tina"}, {hostId:9,sessionName:"tina"}] → map = { tina:1 } (second occurrence ignored)
      Test 4 (buildIdentityHostsFromFleet name-normalization): sessions = [{hostId:1,sessionName:"Tina"}] → map = { tina:1 } (sessionMatchKey lowercases)
      Test 5 (fetchOnce passes populated map): mock listIdentities as a vi.fn spy; seed conversation-store state.fleetSessions = [{hostId:1,sessionName:"tina"}]; call fetchOnce; assert listIdentities was called with `{ tina: 1 }`.
      Test 6 (fetchOnce fallback to empty when fleet not loaded): mock listIdentities; ensure fleetSessions = [], fleetSessionsLoaded = false; call fetchOnce; assert listIdentities called with `{}`.
      Test 7 (refresh-after-fleet-load fires exactly once): mock listIdentities as spy; call fetchOnce with fleet not loaded (spy called 1×); simulate fleet-sessions load event; assert spy called 2× total (one refresh); simulate another fleet-sessions change; assert spy STILL 2× (guard prevents loop).
  </behavior>
  <action>
    (a) Add `getFleetSessionsSnapshot()` to conversation-store.ts's public exports. Thin accessor over `state.fleetSessions`. Per W4, do NOT re-export sessionMatchKey — identities-store imports it directly from @/features/terminal/session-hue.

    (b) Edit src/ui/state/identities-store.ts:
        - Import `sessionMatchKey` from `@/features/terminal/session-hue` (per W4 — direct import, mirrors conversation-store's L54 pattern).
        - Import `getFleetSessionsSnapshot` from conversation-store.
        - Mind the layering discipline note at conversation-store L140-143 — identities-store already imports from api, this is a state-→-state import which is allowed; if the layering rule flags it, invert: export a `notifyFleetSessionsLoaded()` from conversation-store that identities-store subscribes to instead.
        - Add `buildIdentityHostsFromFleet` helper per behavior.
        - Rewire fetchOnce to call `listIdentities(buildIdentityHostsFromFleet(getFleetSessionsSnapshot()))`.
        - Add module-level `let hasRefreshedAfterFleetLoad = false;` and subscribe to fleet-sessions load events; when it flips, call refreshIdentities() and set the guard.

    (c) Create src/ui/state/identities-store.enrichment.test.ts with all 7 tests. Model on src/ui/state/conversation-store.test.ts structure (existing test file for state modules; same vitest + vi.fn pattern).

    Commit after this task lands green: `feat(66-05): frontend identityHosts enrichment + conversation-store accessor`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina && npx vitest run --related src/ui/state/identities-store.enrichment.test.ts src/ui/state/identities-store.ts src/ui/state/conversation-store.ts</automated>
  </verify>
  <done>
    - All 7 tests in identities-store.enrichment.test.ts GREEN
    - `grep -n "buildIdentityHostsFromFleet\|getFleetSessionsSnapshot" src/ui/state/identities-store.ts` shows both symbols referenced
    - `grep -n "from \"@/features/terminal/session-hue\"" src/ui/state/identities-store.ts` shows sessionMatchKey imported directly from session-hue (per W4)
    - `grep -c "sessionMatchKey" src/ui/state/conversation-store.ts` unchanged from pre-plan baseline (no re-export added, per W4)
    - Zero regression to existing identities-store consumers (conversation-store.test.ts + identities-api.test.ts + any component test that renders identities via useIdentities)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread hostId through IdentityModal L1264+L1396 + audit all 5 avatarUrl consumers + shim 7 IdentityModal test files</name>
  <files>src/ui/features/pretty-view/IdentityModal.tsx, src/ui/features/terminal/IdentityBadge.tsx, src/ui/features/pretty-conversations/PrettyConversationRow.tsx, src/ui/features/sessions/SessionRow.tsx, src/ui/sidebar/CloneAgentDialog.tsx, src/ui/features/pretty-view/IdentityModal.test.tsx, src/ui/features/pretty-view/IdentityModal.voice.test.tsx, src/ui/features/pretty-view/IdentityModal.bounties-filter.test.tsx, src/ui/features/pretty-view/IdentityModal.lazy-archive.test.tsx, src/ui/features/pretty-view/IdentityModal.role-tab.test.tsx, src/ui/features/pretty-view/IdentityModal.share.test.tsx, src/ui/features/pretty-view/IdentityModal.stays-awake.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/IdentityModal.tsx (L162 + L170 for hostId prop declaration; L1247 + L1264 + L1277 + L1396 for the 4 avatarUrl / displayName consumer sites — per B1 enumeration)
    - src/ui/api/identities-api.ts (avatarUrlWithHost helper from Plan 03 — expected signature `avatarUrlWithHost(identity, hostId): string`)
    - src/ui/features/terminal/IdentityBadge.tsx (L107 — identity.avatarUrl consumer; needs hostId in props or fallback)
    - src/ui/features/pretty-conversations/PrettyConversationRow.tsx (L1173 — identity.avatarUrl consumer; row already has session context so hostId is derivable)
    - src/ui/features/sessions/SessionRow.tsx (L55 — identity.avatarUrl consumer; SessionRow renders per-session so session.hostId is in scope)
    - src/ui/sidebar/CloneAgentDialog.tsx (L548 — sourceIdentity.avatarUrl consumer; dialog already has hostId in scope from the clone form state)
    - All 7 IdentityModal.*.test.tsx files (per B8 enumeration) — check each render/mount setup for hostId prop
  </read_first>
  <behavior>
    ─── IdentityModal.tsx L1264 + L1396 hostId threading ───
    - L1264 (header avatar img src): change from `src={`${identity.avatarUrl}?v=${identity.avatarEtag}`}` to:
      ```
      src={identity.avatarEtag
        ? `${avatarUrlWithHost(identity, hostId)}&v=${identity.avatarEtag}`
        : avatarUrlWithHost(identity, hostId)}
      ```
      Rationale: avatarUrlWithHost already appends `?hostId=<n>`, so the etag cache-bust becomes `&v=<etag>` (not `?v=`); when etag is empty (Plan 03 safe-default when disk-cosmetics absent), skip the &v= entirely.
    - L1396 (edit-drawer avatar img src): same pattern — change from `src={avatarPreviewUrl ?? `${identity.avatarUrl}?v=${identity.avatarEtag}`}` to `src={avatarPreviewUrl ?? (identity.avatarEtag ? `${avatarUrlWithHost(identity, hostId)}&v=${identity.avatarEtag}` : avatarUrlWithHost(identity, hostId))}`.
    - Import: add `avatarUrlWithHost` to the imports from `@/api/identities-api` at the top of IdentityModal.tsx.
    - L1247 + L1277 (identity.displayName consumers): NO change needed — displayName is now guaranteed non-null-string by Plan 03's publicIdentity safe-default (per B2 move). If TSC still flags anything, that's a pre-existing bug the safe-default should have fixed.

    ─── 5 other avatarUrl consumer audits ───
    For each of the 5 sites (IdentityBadge L107, PrettyConversationRow L1173, SessionRow L55, CloneAgentDialog L548), do the following:
    1. Read the surrounding component context to determine if hostId is available in scope.
    2. If hostId IS in scope: wrap the avatarUrl usage with `avatarUrlWithHost(identity, hostId)`. Also apply the etag guard if the site uses `?v=<etag>` (grep to check).
    3. If hostId is NOT in scope: leave the src as-is (avatarUrl without ?hostId), but ADD an `onError={(e) => { e.currentTarget.style.display = "none" }}` fallback (or reuse the initial-letter placeholder pattern already in that component — grep the component for "placeholder" or "initial" or "fallback"). This lets the 400-response from GET /:id/avatar-without-hostId degrade gracefully to the placeholder rendering instead of showing a broken-image icon.
    4. Add a comment at each site indicating the Plan 05 origin: `// Phase 66 Plan 05: hostId threading — Plan 03's GET /:id/avatar requires hostId query param`.

    Concrete per-site expectations based on the pre-audit reads:
    - **IdentityBadge.tsx L107**: IdentityBadge does not currently take hostId as a prop (grep first). If it doesn't, add `hostId?: number` as an optional prop; when supplied, use avatarUrlWithHost; when omitted, fall back to placeholder. Callers of IdentityBadge that DO have hostId in scope pass it; callers that don't (rare — see terminal contexts) accept the degraded render.
    - **PrettyConversationRow.tsx L1173**: PrettyConversationRow renders per-row; row.hostId or row.host.id is typically in scope (grep for hostId in the file). Thread it via avatarUrlWithHost.
    - **SessionRow.tsx L55**: SessionRow renders per-session; session.hostId is in scope (grep to confirm). Thread it.
    - **CloneAgentDialog.tsx L548**: Dialog's clone form already has a hostId state (grep for hostId in the file). Thread it.

    ─── 7 IdentityModal test-file audit ───
    For each of the 7 test files (IdentityModal.test.tsx, IdentityModal.voice.test.tsx, IdentityModal.bounties-filter.test.tsx, IdentityModal.lazy-archive.test.tsx, IdentityModal.role-tab.test.tsx, IdentityModal.share.test.tsx, IdentityModal.stays-awake.test.tsx):
    1. Grep for `<IdentityModal` render sites in the file.
    2. Confirm each render passes hostId prop (IdentityModal.tsx L170 already declares it required, so most tests likely already pass it — but verify).
    3. If any test asserts on the emitted `<img src=...>` in the modal (header or edit drawer), update the assertion to expect the new URL shape: `avatar?hostId=<n>` or `avatar?hostId=<n>&v=<etag>` (not `avatar?v=<etag>`).
    4. If any test mounts IdentityModal without hostId (compile-time-required prop; unlikely but possible via type-any casts), add `hostId={1}` (or a scenario-appropriate value) to the render setup.
    5. Also audit any pretty-view or badge test file that constructs an Identity without hostId if the widened plumbing requires it (grep for construction sites of Identity in the test dirs; a bare-Identity construction with no hostId context is fine — only mounts of IdentityBadge/IdentityModal with the widened plumbing require hostId).
  </behavior>
  <action>
    (a) Edit IdentityModal.tsx L1264 + L1396 per behavior. Add the `avatarUrlWithHost` import.

    (b) Grep + audit + edit each of the 5 avatarUrl consumers (IdentityBadge, PrettyConversationRow, SessionRow, CloneAgentDialog). Thread hostId when in scope; add fallback when not.

    (c) Audit + shim each of the 7 IdentityModal.*.test.tsx files. Update assertions on avatarUrl string shape if any test asserts on it.

    (d) Run the full frontend test suite scoped to identity-facing components + the modal test files. Verify GREEN.

    Commit at the end: `feat(66-05): thread hostId through avatarUrl consumers + shim 7 IdentityModal test files`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina && npx vitest run --related src/ui/features/pretty-view/IdentityModal.tsx src/ui/features/pretty-view/IdentityModal.test.tsx src/ui/features/pretty-view/IdentityModal.voice.test.tsx src/ui/features/pretty-view/IdentityModal.bounties-filter.test.tsx src/ui/features/pretty-view/IdentityModal.lazy-archive.test.tsx src/ui/features/pretty-view/IdentityModal.role-tab.test.tsx src/ui/features/pretty-view/IdentityModal.share.test.tsx src/ui/features/pretty-view/IdentityModal.stays-awake.test.tsx src/ui/features/terminal/IdentityBadge.tsx src/ui/features/pretty-conversations/PrettyConversationRow.tsx src/ui/features/sessions/SessionRow.tsx src/ui/sidebar/CloneAgentDialog.tsx</automated>
  </verify>
  <done>
    - `grep -n "avatarUrlWithHost" src/ui/features/pretty-view/IdentityModal.tsx | wc -l` returns ≥ 2 (L1264 + L1396 both updated)
    - `grep -rn "identity.avatarUrl\|sourceIdentity.avatarUrl" src/ui/ --include='*.tsx' --include='*.ts' | grep -v test | grep -v identities-api.ts | grep -v avatarUrlWithHost | wc -l` returns 0 — every non-test avatarUrl consumer now routes through avatarUrlWithHost OR has an explicit fallback comment
    - All 7 IdentityModal.*.test.tsx files GREEN post-shim
    - TSC clean: `npx tsc --noEmit 2>&amp;1 | grep -E "IdentityModal|avatarUrl" | wc -l` returns 0
    - Manual sanity (dev-time only, not enforced): the header + edit-drawer avatars in IdentityModal render without emitting literal 'undefined' in the query string
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conversation-store fleetSessions snapshot → identities-store fetchOnce | in-process module-boundary; both live in the same browser tab under the same origin |
| listIdentities(identityHosts) query param → GET /identities | JWT-authed; identityHosts is caller-supplied and resolveHostById(userId) scopes the SSH fanout server-side (Plan 03 threat model T-66-03-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-05-01 | Denial of Service | first-render refresh loop | mitigate | Module-level `hasRefreshedAfterFleetLoad` guard prevents the fleet-load subscription from firing more than once. If the user manually triggers `refreshIdentities()`, the guard is not reset — subsequent fleet-load events don't chain more refreshes. |
| T-66-05-02 | Information Disclosure | identityHosts wire payload | accept | The identityHosts map contains {identityKey:hostId} pairs — identityKey is public within the user's scope (already emitted by GET /identities today), hostId is a foreign key resolved server-side. No new leakage. |
| T-66-05-03 | Availability | avatarUrl 400s when hostId absent | mitigate | Each of the 5 consumer sites either threads hostId (green path) or falls back to placeholder rendering (degraded path). No consumer breaks on 400. |
| T-66-SC | Tampering | npm installs | mitigate | NO NEW PACKAGES. |
</threat_model>

<verification>
Scoped test run:
```bash
cd /home/ubuntu/skynet-tina && npx vitest run --related \
  src/ui/state/identities-store.enrichment.test.ts \
  src/ui/state/identities-store.ts \
  src/ui/state/conversation-store.ts \
  src/ui/features/pretty-view/IdentityModal.tsx \
  src/ui/features/pretty-view/IdentityModal.test.tsx \
  src/ui/features/pretty-view/IdentityModal.voice.test.tsx \
  src/ui/features/pretty-view/IdentityModal.bounties-filter.test.tsx \
  src/ui/features/pretty-view/IdentityModal.lazy-archive.test.tsx \
  src/ui/features/pretty-view/IdentityModal.role-tab.test.tsx \
  src/ui/features/pretty-view/IdentityModal.share.test.tsx \
  src/ui/features/pretty-view/IdentityModal.stays-awake.test.tsx \
  src/ui/features/terminal/IdentityBadge.tsx \
  src/ui/features/pretty-conversations/PrettyConversationRow.tsx \
  src/ui/features/sessions/SessionRow.tsx \
  src/ui/sidebar/CloneAgentDialog.tsx
```

TSC repo-wide sanity check:
```bash
cd /home/ubuntu/skynet-tina && npx tsc --noEmit 2>&amp;1 | head -40
```

Post-plan integration (with Plans 01+02+03+05 all shipped, BEFORE Plan 04 migration): a page-load fires listIdentities with a populated identityHosts map; cosmetics come back populated for every identity that has a fleet session on record; the identities-store's populated Identity[] flows into every avatarUrl consumer with hostId in scope; the render pipeline lights up end-to-end.
</verification>

<success_criteria>
- fetchOnce constructs identityHosts from conversation-store fleetSessions before calling listIdentities
- sessionMatchKey is imported directly from @/features/terminal/session-hue in identities-store (per W4) — no re-export added to conversation-store
- IdentityModal.tsx L1264 + L1396 thread hostId via avatarUrlWithHost; etag guard prevents `?v=` when etag is empty
- All 5 avatarUrl consumers audited + updated (hostId threaded when in scope; fallback when not)
- All 7 IdentityModal.*.test.tsx files audited + shimmed; all remain green
- Zero new API surface — Plan 05 is pure frontend + a thin conversation-store accessor export
- No backend edits in Plan 05 (publicIdentity safe-defaults live in Plan 03 per B2)
- TSC repo-wide clean
- Plan 04's Wave 4 dependency on 66-05 (in addition to 66-01/02/03) is honored — Plan 04 only runs after Plan 05's frontend enrichment is green
</success_criteria>

<output>
Create `.planning/phases/66-skynet-reads-and-writes-identity-cosmetics-from-disk/66-05-SUMMARY.md` capturing:
- The identityHosts construction algorithm (first-wins per identityKey; sessionMatchKey correlation direct-imported from @/features/terminal/session-hue per W4)
- The one-shot refresh-after-fleet-load design (guarded against loops)
- Which of the 5 non-modal avatarUrl consumers required hostId threading vs. fallback rendering (the audit result)
- Confirmation that publicIdentity safe-defaults live in Plan 03 (per B2 co-location); this plan populates identityHosts so the safe-defaults path is exercised rarely
- Note to orchestrator: Plan 05 unblocks Plan 04 (Wave 4). Plan 03 (Wave 2) and Plan 05 (Wave 3) must both be green before Plan 04's migration lands.
</output>
