---
phase: 66-skynet-reads-and-writes-identity-cosmetics-from-disk
plan: 03
type: execute
wave: 2
depends_on:
  - 66-01
  - 66-02
files_modified:
  - src/backend/database/routes/identities.ts
  - src/backend/claude-session/identity-artifact-reader.ts
  - src/backend/database/routes/identities.get-disk.test.ts
  - src/backend/claude-session/identity-artifact-reader.avatar-read.test.ts
  - src/ui/api/identities-api.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "GET /identities returns each identity row with cosmetics derived from the identity's on-disk frontmatter, NOT from the store columns"
    - "Per-identity disk reads are LAZY and PER-REQUEST — hostId comes from the caller's query-string identityHosts map; no fleet-walk"
    - "GET /identities/:id/avatar reads the sibling avatar file from disk via the artifact-reader; response Content-Type is derived from the frontmatter's avatar filename extension"
    - "When an identity's box is unreachable OR its frontmatter is missing/malformed, that identity's row is returned with cosmetics-as-safe-defaults (displayName=capitalizeFirst(identityKey); title=null; colorHue=null; voice=null; avatarMime=\"\"; avatarEtag=\"\") rather than errored — matches the frontend Identity type's non-nullable-string contract per Plan 05 note; the null-vs-safe-default split honors what the wire type declares"
    - "GET /identities/:id/avatar returns 404 when the sibling avatar file cannot be read, NOT the stale store blob"
    - "publicIdentity() emits safe non-null defaults for displayName/avatarMime/avatarEtag when disk-overlay is absent — co-located with the READ flip so the null-cosmetics scenario introduced by this plan is contract-safe from the moment the flip lands (moved into Plan 03 from Plan 05 per checker B2 to eliminate cross-plan test-contract drift)"
  key_reads:
    - "This plan is intentionally dense (2 tasks totaling ~50% context; ~10 unit tests + 8 route tests + backend route flip + frontend API widening + publicIdentity safe-defaults). Density is justified because the READ path unit + route tests are tightly coupled — the unit tests pin the reader helpers' contract that the route tests mock; splitting would fragment that contract across plans and require redundant CONTEXT re-loads. Per W11, if execution shows context creep, split into 03a (helpers + unit tests) and 03b (route flip + route tests + safe-defaults) as a mid-execution recovery."
  artifacts:
    - path: "src/backend/database/routes/identities.ts"
      provides: "Flipped GET / handler — merges store rows with disk-derived cosmetics; Flipped GET /:id/avatar handler — reads sibling avatar from disk; both accept optional hostId per identity (query param) or lazy-cascade to a single caller-supplied hostId scope. publicIdentity() also updated with safe non-null defaults for displayName/avatarMime/avatarEtag (moved from Plan 05 per B2)."
      contains: "readIdentityFile"
    - path: "src/backend/claude-session/identity-artifact-reader.ts"
      provides: "New exported readAvatarSiblingFile(conn, identityKey, ext) → Promise<{bytes: Buffer; mime: string} | null>; discovers ext by first reading the .md frontmatter, or by trying candidate extensions in [webp,png,jpg,gif,svg] order"
      contains: "export async function readAvatarSiblingFile"
    - path: "src/backend/database/routes/identities.get-disk.test.ts"
      provides: "Route-level tests for GET / and GET /:id/avatar disk-derivation contract — happy path + unreachable-box safe-defaults degradation + missing-frontmatter safe-defaults degradation + synthetic unreachable-test-fixture edge case (was commander-zoey — renamed per W9)"
      contains: "describe(\"GET /identities — disk-derived cosmetics\""
    - path: "src/backend/claude-session/identity-artifact-reader.avatar-read.test.ts"
      provides: "Unit tests for readAvatarSiblingFile — REMOTE + LOCAL branches, extension-discovery via frontmatter, cap enforcement, ENOENT vs SSH-fail return semantics"
    - path: "src/ui/api/identities-api.ts"
      provides: "listIdentities widened to accept optional identityHosts: Record<identityKey, hostId>. Plan 05 populates this map at page-load from conversation-store fleetSessions so first-render cosmetics come back populated. Empty-map fallback is preserved (safe-default cosmetics per publicIdentity change, tolerated by client)."
  key_links:
    - from: "identities.ts GET /"
      to: "readIdentityFile per identity → extractCosmeticsFromFrontmatter → overlay onto public row"
      via: "per-request per-identity lazy fetch"
      pattern: "readIdentityFile\\(conn, row.identityKey"
    - from: "identities.ts GET /:id/avatar"
      to: "readAvatarSiblingFile"
      via: "conn + identityKey + ext-discovery"
      pattern: "readAvatarSiblingFile\\("
    - from: "identities.ts publicIdentity"
      to: "capitalizeFirst(identityKey) safe-default"
      via: "disk-overlay-or-safe-default pattern (moved from Plan 05 per B2)"
      pattern: "capitalizeFirst\\("
---

<objective>
Track READ — every render surface that pulls cosmetics (displayName, title, colorHue, voice, avatar) from the encrypted store flips to reading the identity's on-disk frontmatter + sibling avatar via the artifact-reader. `GET /identities` becomes a per-identity lazy disk-derivation; `GET /identities/:id/avatar` reads the sibling file. Missing / unreachable / malformed inputs yield cosmetics-as-safe-defaults rows (Ashley: "accept the ugly render") instead of erroring the whole endpoint — with the safe-defaults themselves emitted by publicIdentity() so the frontend Identity type's non-nullable-string contract stays satisfied.

Purpose: Closes the shape file's core commitment — Skynet becomes "a well-informed viewer of a filesystem-based truth". Post-flip, an agent that creates an identity purely on disk renders identically to one made through Skynet's UI, because both flow through the same disk-native truth.

Output: New readAvatarSiblingFile helper in the artifact-reader. Rewritten GET / and GET /:id/avatar handlers in identities.ts. Widened listIdentities frontend API surface to pass a per-request identityHosts hint (populated by Plan 05 from conversation-store fleetSessions). Route + unit test coverage for the happy path, the unreachable-box path (safe-defaults degradation), the missing-frontmatter path, and a synthetic unreachable-test-fixture edge case. Plus publicIdentity() safe-defaults (displayName = capitalizeFirst(identityKey); avatarMime = ""; avatarEtag = "") — MOVED HERE FROM PLAN 05 (per checker B2) so this plan's null-cosmetics scenarios are contract-safe from the moment the flip lands, eliminating cross-plan test-contract drift.

NOTE (per B3): The frontend enrichment that populates identityHosts on the wire (so the map is non-empty on the very first render) is Plan 05's scope. Plan 05 depends on this plan (wave 3, this plan wave 2) — Plan 04's migration in Wave 4 depends on both. Between "Plan 03 lands" and "Plan 05 lands" the client's listIdentities calls with `{}` and every row comes back with cosmetics-as-safe-defaults (displayName=capitalizedIdentityKey; title=null; colorHue=null; voice=null; avatar=placeholder) — Ashley greenlit that transient degradation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/shapes/identity-prettiness-on-disk.md
@.planning/phases/66-skynet-reads-and-writes-identity-cosmetics-from-disk/CONTEXT.md
@CLAUDE.md

@src/backend/database/routes/identities.ts
@src/backend/claude-session/identity-artifact-reader.ts
@src/backend/claude-session/identity-artifact-reader.remote-writes.test.ts
@src/ui/api/identities-api.ts
@src/ui/state/identities-store.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add readAvatarSiblingFile helper (REMOTE + LOCAL) with ext-discovery</name>
  <files>src/backend/claude-session/identity-artifact-reader.ts, src/backend/claude-session/identity-artifact-reader.avatar-read.test.ts</files>
  <read_first>
    - .planning/shapes/identity-prettiness-on-disk.md ("No cache. No offline fallback. Slower is better than stale.")
    - .planning/phases/66-skynet-reads-and-writes-identity-cosmetics-from-disk/CONTEXT.md (Track 3 spec — GET /identities/:id/avatar reads sibling avatar file from disk)
    - src/backend/claude-session/identity-artifact-reader.ts (readIdentityFile L409-443 for read-path pattern; sftpReadFile L1244-1261 — private binary SFTP reader; extractRoleFromMarkdown L273-284 for the frontmatter parse regex; execWithTimeout L340-354; getLocalIdentitiesRoot L211-216)
    - src/backend/claude-session/identity-artifact-reader.remote-writes.test.ts (mock-conn builder — extend for read-file mocking)
  </read_first>
  <behavior>
    - New export `AVATAR_MIME_FROM_EXT: Record<AvatarExt, string>` — inverse of MIME_TO_AVATAR_EXT from Plan 01. { webp:"image/webp", png:"image/png", jpg:"image/jpeg", gif:"image/gif", svg:"image/svg+xml" }.
    - New export `export async function readAvatarSiblingFile(conn: SSHClientType | null, identityKey: string): Promise<{ bytes: Buffer; mime: string; ext: AvatarExt } | null>`:
      1. IDENTITY_KEY_RE.test(identityKey) — else throw "invalid identityKey"
      2. First read the identity file via `readIdentityFile(conn, identityKey)` — parse frontmatter, look for `avatar: <filename>` key. If present AND filename matches `${identityKey}\.(webp|png|jpg|gif|svg)$`, extract the ext from the filename. If NOT present (or malformed), fall through to (3).
      3. Ext-discovery fallback: try each ext in AVATAR_EXT_VALUES = ["webp","png","jpg","gif","svg"] order. LOCAL branch: `fs.readFile(path.join(root, identityKey, `${identityKey}.${ext}`))` — on ENOENT try next ext, on other errors throw. REMOTE branch: use a single `ls "$HOME/.claude/identities/${identityKey}/"${identityKey}".{webp,png,jpg,gif,svg} 2>/dev/null | head -1` exec to identify which sibling exists (single SSH round-trip; bash brace expansion is available on target hosts per fleet standard), then SFTP-read that file.
      4. If no sibling exists AND (3)'s search returned nothing, return null.
      5. If SSH-layer error at any point (timeout, connect drop), throw — caller decides whether to swallow (GET /identities does, GET /:id/avatar returns 404).
      6. Bytes size cap: enforce IDMEDIT_MAX_AVATAR_BYTES = 5_000_000 (from Plan 01) as an upper bound — if the read returns more, throw "avatar exceeds cap on disk" (defensive; disk avatars shouldn't exceed this because writers cap at write-time).
    - Also new export `export function extractCosmeticsFromFrontmatter(markdown: string): { displayName?: string; title?: string; colorHue?: number; voice?: string; avatar?: string }`:
      • Use the same `^---\r?\n([\s\S]*?)\r?\n---` regex as extractRoleFromMarkdown
      • yaml.load the frontmatter block
      • Pick only { displayName, title, colorHue, voice, avatar } — ignore role and any other keys
      • Type-gate each: displayName / title / voice / avatar must be `typeof x === "string"` and length > 0 to appear in output; colorHue must be `typeof x === "number"` and 0 ≤ x ≤ 359 to appear
      • Missing / malformed / parse-error → return `{}` (empty object; caller sees "no cosmetics on disk" and renders placeholder)
    - New test file `src/backend/claude-session/identity-artifact-reader.avatar-read.test.ts`:
      Test A: LOCAL branch, sibling file exists as tina.png → returns { bytes, mime:"image/png", ext:"png" }
      Test B: LOCAL branch, no sibling file — returns null
      Test C: LOCAL branch, sibling file exists as tina.webp AND frontmatter names it → returns webp result (frontmatter is authoritative when present)
      Test D: REMOTE branch, ls returns tina.webp → SFTP-reads that, returns webp result
      Test E: REMOTE branch, ls returns empty → returns null
      Test F: extractCosmeticsFromFrontmatter with all 5 keys present → returns all 5
      Test G: extractCosmeticsFromFrontmatter with malformed YAML → returns {}
      Test H: extractCosmeticsFromFrontmatter with colorHue: 400 (out of range) → colorHue omitted from result
      Test I: extractCosmeticsFromFrontmatter with title: null (explicit YAML null) → title omitted
      Test J: invalid identityKey → throws
  </behavior>
  <action>
    Edit `src/backend/claude-session/identity-artifact-reader.ts` — add the new exports in a new section header block "6c. Cosmetics reads — Phase 66 Plan 03". Colocate readAvatarSiblingFile immediately after the writeAvatarSiblingFile from Plan 01 (both operate on the sibling file). Reuse the existing private `sftpReadFile` helper for the REMOTE branch's binary read. The REMOTE branch's ls-and-pick sequence: `await execWithTimeout(conn, `ls "$HOME/.claude/identities/${identityKey}/"${identityKey}".{webp,png,jpg,gif,svg}" 2>/dev/null | head -n1 | xargs -r basename`)` — the identityKey is IDENTITY_KEY_RE-validated so direct interpolation is shell-safe per the same pattern used at L440. Parse the basename to extract ext.

    For extractCosmeticsFromFrontmatter: mirror extractRoleFromMarkdown byte-shape (same regex; same yaml.load try/catch; same type-narrowing style).

    Create `src/backend/claude-session/identity-artifact-reader.avatar-read.test.ts`. Model the test-file structure on identity-artifact-reader.remote-writes.test.ts (same vi.mock of tmux-helper, same mock-conn builder pattern extended with an sftp.readFile spy that returns a fixed Buffer; LOCAL-branch tests vi.mock node:fs/promises).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina && npx vitest run --related src/backend/claude-session/identity-artifact-reader.avatar-read.test.ts</automated>
  </verify>
  <done>
    - All 10 tests (A-J) green
    - `grep -c "readAvatarSiblingFile\|extractCosmeticsFromFrontmatter\|AVATAR_MIME_FROM_EXT" src/backend/claude-session/identity-artifact-reader.ts | grep -v '^#'` shows 4+ occurrences (declaration + JSDoc + usage-in-doc)
    - Zero regression to existing artifact-reader test files (remote-writes, plus whatever else exists that touches the same module — run `npx vitest run --related src/backend/claude-session/identity-artifact-reader.ts` to sweep)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flip GET /identities + GET /:id/avatar to disk-derived + publicIdentity safe-defaults (RED then GREEN)</name>
  <files>src/backend/database/routes/identities.ts, src/backend/database/routes/identities.get-disk.test.ts, src/ui/api/identities-api.ts</files>
  <read_first>
    - .planning/phases/66-skynet-reads-and-writes-identity-cosmetics-from-disk/CONTEXT.md (Track 3 spec + open question 1 planner-note about per-identity lazy fetch + open question 4 about "accept the ugly render" for unreachable-fixture cases)
    - .planning/shapes/identity-prettiness-on-disk.md ("Boxes offline → cosmetics unfetchable → error state, no cache")
    - src/backend/database/routes/identities.ts (existing GET / L70-91; existing GET /:id/avatar L318-351; publicIdentity L53-68 — the safe-defaults change lands here per B2)
    - src/backend/claude-session/identity-artifact-reader.ts (Task 1's new exports: readAvatarSiblingFile, extractCosmeticsFromFrontmatter, AVATAR_MIME_FROM_EXT; readIdentityFile L409-443; isLocalHostId L198-201)
    - src/backend/database/routes/identity-birth.ts (deps assembly pattern — reuse connectOneShot + resolveHostById for the REMOTE branch)
    - src/ui/api/identities-api.ts (listIdentities L33-40 — signature widening; Identity type L6/L11/L13 — displayName/avatarMime/avatarEtag declared non-nullable strings; safe-defaults from publicIdentity satisfy this contract without widening the type)
    - src/ui/state/identities-store.ts (fetchOnce L41-55 — call-site for listIdentities update; Plan 05 rewires this to pass a populated map, but Plan 03's baseline passes `{}`; identities-store L23-29 has withDisplayCap — same capitalization pattern reused server-side for safe-default)
  </read_first>
  <behavior>
    ─── publicIdentity safe-defaults (moved from Plan 05 per B2) ───
    - Update publicIdentity() at L53-68 to use disk-overlay-or-safe-default pattern:
      • Add helper `function capitalizeFirst(s: string): string { return s.charAt(0).toUpperCase() + s.slice(1); }` — mirrors the frontend withDisplayCap pattern at identities-store L23-29 exactly.
      • `displayName`: when disk-overlay returns a string, use it; else `capitalizeFirst(row.identityKey)` (safe default — guarantees non-null string, satisfies Identity.displayName non-nullable contract).
      • `title`: nullable per the wire type; disk-overlay → string; else null.
      • `colorHue`: nullable per the wire type; disk-overlay → number; else null.
      • `voice`: nullable per the wire type; disk-overlay → string; else null.
      • `avatarMime`: non-nullable string per the wire type; disk-overlay → mime string (from readAvatarSiblingFile.mime); else `""` (empty string satisfies contract).
      • `avatarEtag`: non-nullable string per the wire type; disk-overlay → per-response etag from GET /:id/avatar; for the GET / list response, default `""` (empty string; IdentityModal.tsx L1264+L1396's ?v=<etag> cache-bust guard in Plan 05 handles the empty case).
      • Add a comment: `// Phase 66: safe-defaults for the frontend Identity type's non-nullable-string fields (displayName/avatarMime/avatarEtag). Disk-overlay populates when identityHosts is supplied AND cosmetics exist on disk; safe defaults otherwise satisfy TSC without widening the Identity type (Plan 05 defers the type widening as bigger blast radius). Moved from Plan 05 to Plan 03 per checker B2 — co-located with the READ flip that creates the null-cosmetics scenario, so this plan's own tests exercise the safe-defaults path.`

    ─── GET /identities flip ───
    - GET /identities widens its request contract to accept an OPTIONAL query parameter `identityHosts` — a URL-encoded JSON object mapping identityKey → hostId. Example: `GET /identities?identityHosts={"tina":1,"nelly":5}`. Missing / empty / malformed → treat as {} (empty map).
    - New handler flow:
      1. Load all store rows for userId (unchanged from L73-77)
      2. For each row, look up hostId in the identityHosts map (identityKey lowercased). If absent from map, the row's cosmetics are UNKNOWN — publicIdentity returns safe-defaults (displayName=capitalizeFirst(identityKey), title=null, colorHue=null, voice=null, avatarMime="", avatarEtag=""). avatarUrl still set to /identities/:id/avatar but the fetch will likely 400 without hostId query param — Plan 05 threads hostId into the avatarUrl for consumers.
      3. For each row WITH a hostId in the map, attempt (in parallel across rows via Promise.allSettled to bound wall-clock):
         a. Determine LOCAL vs REMOTE via isLocalHostId(hostId)
         b. REMOTE: connectOneShot(host, timeoutMs=5000). Timeout / connect-fail → publicIdentity returns safe-defaults for this row (SWALLOWED per shape "cosmetics unfetchable → error state" scoped to that row, not endpoint).
         c. `const { markdown } = await readIdentityFile(conn, row.identityKey)` — if markdown is empty (ENOENT / missing folder) → publicIdentity returns safe-defaults for this row
         d. `const cosmetics = extractCosmeticsFromFrontmatter(markdown)` — {} means malformed, publicIdentity returns safe-defaults
         e. Overlay: publicIdentity(row, cosmetics) — cosmetics.displayName populates over the safe-default; cosmetics.title/colorHue/voice populate over null; avatarMime + avatarEtag stay at "" for the list response (populated per-request by GET /:id/avatar).
         f. conn.end() in try/finally
      4. Return the enriched array. The endpoint NEVER errors 5xx due to a single-identity fetch failure — it may error only if the store lookup itself fails.

    ─── GET /identities/:id/avatar flip ───
    - GET /identities/:id/avatar widens to accept query `hostId=<n>` (required). Missing / non-integer → 400.
      1. Look up row by (id, userId). 404 if absent.
      2. Determine LOCAL vs REMOTE via isLocalHostId(hostId).
      3. In try/finally that closes the conn:
         a. `const readResult = await readAvatarSiblingFile(conn, row.identityKey)`
         b. If readResult is null → 404 { error: "no avatar on disk for this identity" }
         c. If SSH throws → 502 { error: "identity home box unreachable" }
         d. Otherwise: Content-Type = readResult.mime; Content-Length = readResult.bytes.byteLength; ETag = `"disk-${md5(readResult.bytes)}"` (compute md5 inline per-request; the etag is per-response for If-None-Match short-circuit only, NOT a stored value — see W6 semantic note below). Send bytes.
      4. Ignore If-None-Match unless it matches the freshly-computed etag (still allows client-side round-trip savings without server-side caching).

    NOTE on ETag semantics (per W6): Browser-side ETag revalidation via If-None-Match is standard HTTP caching; the shape file's "no cache" clause targets server-side cosmetics caching (Skynet holding a copy of the disk values), not HTTP cache-validation. ETag is per-response and not stored server-side, so it does not violate the shape rule. The comment placed inline at the ETag computation site MUST explicitly say this.

    ─── Frontend widening ───
    - Update src/ui/api/identities-api.ts:
      `listIdentities(identityHosts: Record<string, number> = {}): Promise<Identity[]>` — passes `identityHosts` as query param (URL-encoded JSON). Plan 05 wires the fetchOnce caller to pass a populated map derived from conversation-store fleetSessions; Plan 03's baseline callers pass `{}` (cosmetics come back as safe-defaults on first render — Ashley greenlit the transient degradation, and the safe-defaults from publicIdentity keep TSC happy against the non-nullable Identity fields). Add a comment in listIdentities: "identityHosts is caller-scoped. Empty map = all cosmetics as safe-defaults (matches Phase 66 transition-window semantics — Plan 05 wires the populated call site)."
      Also update `getIdentityAvatarUrl` if such a helper exists (grep to confirm; if it doesn't, update the frontend consumers directly via a per-consumer hostId append to `avatarUrl`). Simplest: add a helper `avatarUrlWithHost(identity, hostId): string { return `${identity.avatarUrl}?hostId=${hostId}` }` exported alongside; Plan 05 wires the consumers.

    ─── Tests ───
    - New test file `src/backend/database/routes/identities.get-disk.test.ts` — model on identity-birth.test.ts / identities.put-disk.test.ts. Same vi.mock of artifact-reader + ssh-one-shot + tmux-helper. Seed db with 3 identity rows: "tina" (identityKey), "nelly", "unreachable-test" (was "commander-zoey" in earlier iterations — renamed per W9 because Nadia is rescuing commander-zoey out-of-band; hardcoding real identity names as test fixtures is a general anti-pattern regardless).

      Test 1 (happy path GET / with identityHosts={tina:1,nelly:5}): mock isLocalHostId(1) true, isLocalHostId(5) false; readIdentityFile for tina returns markdown with all 5 cosmetics; readIdentityFile for nelly returns markdown with 3 of 5. Assert response array has:
        • tina row with all cosmetics populated from disk-overlay (displayName from disk, not safe-default)
        • nelly row with 3 cosmetics from disk + the other 2 as safe-defaults (title/colorHue/voice → null OR safe-default per field's nullability — if disk provided title but not colorHue+voice, colorHue=null, voice=null)
        • unreachable-test row with SAFE-DEFAULT cosmetics (no hostId supplied): displayName="Unreachable-test" (capitalizeFirst("unreachable-test")), title=null, colorHue=null, voice=null, avatarMime="", avatarEtag=""
      Note (per B3): once Plan 05 lands and populates identityHosts from fleetSessions, this "no hostId supplied" branch will be exercised only for identities that have NO session on record — the frontend will hit this path much less often, but the branch stays legitimate.

      Test 2 (unreachable box scoped to row): mock connectOneShot(nelly's host) rejects with "Host unreachable"; tina's LOCAL branch works. Assert response is 200 and tina row has cosmetics from disk-overlay, nelly row has SAFE-DEFAULT cosmetics: displayName="Nelly", title=null, colorHue=null, voice=null, avatarMime="", avatarEtag="" (NOT 502 for the whole endpoint).

      Test 3 (unreachable-test-fixture missing folder → safe-defaults — was "commander-zoey" per W9 rename): identityHosts={"unreachable-test":7}; mock readIdentityFile to return {markdown:""}. Assert response includes unreachable-test row with SAFE-DEFAULT cosmetics: displayName="Unreachable-test", title=null, colorHue=null, voice=null, avatarMime="", avatarEtag="". No error, 200. This scenario simulates a folder-name mismatch or missing identity file — the exact class of edge case the "accept the ugly render" greenlight covers.

      Test 4 (GET /:id/avatar happy path): identityId points at tina, query ?hostId=1. Mock readAvatarSiblingFile returns { bytes:Buffer.from("PNGDATA"), mime:"image/png", ext:"png" }. Assert response 200 with Content-Type image/png and body bytes match.

      Test 5 (GET /:id/avatar disk-empty → 404): mock readAvatarSiblingFile returns null. Assert response 404 with error body indicating no avatar on disk.

      Test 6 (GET /:id/avatar SSH-fail → 502): mock readAvatarSiblingFile throws "remote exec timeout". Assert 502.

      Test 7 (GET /:id/avatar missing hostId query → 400): request without ?hostId=. Assert 400 with error message containing "hostId".

      Test 8 (LOCAL branch GET /:id/avatar): identityHosts=1 which isLocalHostId returns true; readAvatarSiblingFile called with conn=null. Assert response 200 + connectOneShot NEVER called.
  </behavior>
  <action>
    Write Task 2 in strict TDD order:
    (a) Create `identities.get-disk.test.ts` with all 8 tests (Test 1 and Test 3 assertions expect safe-defaults per B2 — displayName="Unreachable-test" / "Nelly" (capitalizeFirst); avatarMime=""; avatarEtag=""; title/colorHue/voice=null). Run — they fail (RED).
    (b) Update publicIdentity() in identities.ts to emit safe non-null defaults for displayName/avatarMime/avatarEtag per behavior spec. Add capitalizeFirst helper. This is the move from Plan 05 per B2.
    (c) Rewrite the GET / handler in identities.ts per behavior spec (identityHosts query param; Promise.allSettled; publicIdentity(row, cosmetics) overlay).
    (d) Rewrite the GET /:id/avatar handler in identities.ts per behavior spec. Include the ETag semantic comment inline (per W6): `// ETag is per-response, not stored server-side. Browser-side revalidation via If-None-Match is standard HTTP cache-validation — the shape file's "no cache" clause targets server-side cosmetics caching (Skynet holding a copy of the disk values), NOT HTTP cache-validation. Per-response etag is compatible with the shape rule.`
    (e) Update src/ui/api/identities-api.ts: widen listIdentities signature; add avatarUrlWithHost helper.
    (f) Update src/ui/state/identities-store.ts fetchOnce to pass `{}` to listIdentities (matches the transition-window semantic; Plan 05 rewires this call site to pass a populated map).
    (g) Run tests — GREEN.

    Note: this is a route-flip, so DO NOT break the existing frontend consumers that currently rely on GET /identities to populate cosmetics for rendering. The pattern-degradation is deliberate per CONTEXT.md open question 4 and Ashley's greenlight to "accept the ugly render." IdentityBadge / SessionRow / PrettyConversationRow / ChatMessage will render with placeholder colors + fallback initials (or the capitalizeFirst(identityKey) as displayName) between "Plan 03 lands" and "Plan 05 lands" — Plan 05 threads hostId into the consumers + populates identityHosts in the store.

    Also: preserve the existing publicIdentity() shape so the client's TypeScript Identity type doesn't need to change. Fields that come back with safe-defaults instead of null still satisfy the type (displayName/avatarMime/avatarEtag are non-nullable strings; safe-defaults are strings). title/colorHue/voice remain nullable in the type and come back null when absent from disk.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina && npx vitest run --related src/backend/database/routes/identities.get-disk.test.ts src/backend/database/routes/identities.ts src/ui/api/identities-api.ts src/ui/state/identities-store.ts src/ui/api/identities-api.test.ts</automated>
  </verify>
  <done>
    - All 8 tests from Task 2's test file GREEN
    - Zero regression to identities-api.test.ts (signature widening is default-parameter; existing callers with zero args still work)
    - `grep -n "row.displayName\|row.title\|row.colorHue\|row.voice\|row.avatarData\|row.avatarMime" src/backend/database/routes/identities.ts` shows the GET /:id/avatar handler no longer reads `row.avatarData` / `row.avatarMime` / `row.avatarEtag` for response construction (those come from disk now); the GET / handler no longer feeds row.displayName/title/colorHue/voice directly into the response (they come from the frontmatter overlay). POST handler retains those column WRITES per Plan 04 scope.
    - `grep -n "identityHosts" src/ui/api/identities-api.ts` shows the widened signature
    - `grep -n "capitalizeFirst" src/backend/database/routes/identities.ts` shows the safe-default helper landed in publicIdentity (B2 move from Plan 05)
    - Test 1 and Test 3 assertions expect safe-defaults (displayName as capitalizeFirst; avatarMime=""; avatarEtag="") — NOT null — for cosmetics-absent rows (per B2)
    - Test 3 uses the synthetic fixture "unreachable-test" (per W9 rename); grep confirms zero references to "commander-zoey" in the new test file
    - The GET /:id/avatar ETag comment explicitly acknowledges HTTP cache-validation vs server-side caching semantics (per W6)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GET / with query identityHosts → SSH fanout | JWT-authed; hostId scoped by userId via resolveHostById before use |
| GET /:id/avatar with query hostId → SSH read | same authz; readAvatarSiblingFile shell-safe via IDENTITY_KEY_RE |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-03-01 | Denial of Service | fanout on GET / | mitigate | Per-request lazy fetch; Promise.allSettled bounds wall-clock; per-connect timeout = 5s so ~5 identities × 5s = 25s worst-case (single-user Skynet, acceptable); identityHosts map is caller-supplied so a small-fleet user's request costs O(their-identity-count) not O(fleet). Empty map = zero SSH cost. |
| T-66-03-02 | Information Disclosure | avatar bytes leak on 502 | mitigate | 502 error body uses canned string ("identity home box unreachable"); no raw SSH exception surfaced |
| T-66-03-03 | Tampering | frontmatter YAML with malicious keys | accept | extractCosmeticsFromFrontmatter type-narrows every field via typeof + range checks — a crafted frontmatter with title:{__proto__:...} is a plain object, `typeof x === "string"` filters it out, returned cosmetics missing that field |
| T-66-03-04 | Elevation of Privilege | identityHosts crafted to point at other users' hosts | mitigate | resolveHostById(hostId, userId) — same helper the birth flow uses — scopes ssh_data lookup by userId; a hostId not in the caller's scope returns error; the row's cosmetics come back as safe-defaults (silent-swallow, treated as unreachable) |
| T-66-SC | Tampering | npm installs | mitigate | NO NEW PACKAGES. js-yaml + ssh2 already deps. |
</threat_model>

<verification>
Scoped test run:
```bash
cd /home/ubuntu/skynet-tina && npx vitest run --related \
  src/backend/claude-session/identity-artifact-reader.avatar-read.test.ts \
  src/backend/database/routes/identities.get-disk.test.ts \
  src/backend/database/routes/identities.ts \
  src/ui/api/identities-api.ts \
  src/ui/api/identities-api.test.ts \
  src/ui/state/identities-store.ts
```

Post-plan integration: with Plans 01+02+03+05 shipped, a birth → PUT edit → GET / round-trip shows the edited cosmetics coming from disk. The full-suite green ship-gate is the orchestrator's responsibility.

The client's safe-defaults rendering during the transition (Plans 03 → 05 window) is INTENTIONAL and Ashley-greenlit per shape file (open question 4). Plan 05 populates identityHosts + threads hostId through the avatarUrl consumers.
</verification>

<success_criteria>
- GET / returns per-row disk-derived cosmetics (or safe-defaults) with graceful degradation on unreachable / missing-frontmatter identities
- GET /:id/avatar reads sibling file from disk; 404 on absent, 502 on SSH-fail, 200 with correct Content-Type on happy path
- The unreachable-test-fixture edge case (Test 3) yields safe-default cosmetics for that row, NOT an error on the whole endpoint (Ashley greenlit accept-the-ugly-render)
- publicIdentity() emits safe non-null defaults for displayName/avatarMime/avatarEtag (moved from Plan 05 per B2) — satisfies frontend Identity type's non-nullable-string contract without widening the type
- No cache anywhere — every render fetches fresh from disk (matches shape "Skynet quietly holds a cache is WRONG"); GET /:id/avatar's per-response ETag is HTTP cache-validation, not server-side caching (per W6)
- Backend no longer reads cosmetic columns (displayName/title/colorHue/voice/avatarData/avatarMime/avatarEtag) from the identities table in the GET handlers; those columns are DEAD in READ paths (Plan 04 physically drops them)
- Test fixtures use synthetic names ("unreachable-test") — zero references to real identity names as fixtures (per W9)
- Test 1 and Test 3 assertions expect safe-defaults (capitalizeFirst identityKey; empty-string avatarMime/avatarEtag) NOT null — per B2 co-location of safe-defaults with the READ flip
</success_criteria>

<output>
Create `.planning/phases/66-skynet-reads-and-writes-identity-cosmetics-from-disk/66-03-SUMMARY.md` capturing:
- The identityHosts caller-scoped design (rationale: no fleet-walk, no per-user hostId registry)
- The publicIdentity safe-defaults contract (moved here from Plan 05 per B2 — displayName=capitalizeFirst(identityKey); avatarMime=""; avatarEtag=""; nullable fields stay null)
- Note that Plan 05 is the twin plan that populates identityHosts from conversation-store fleetSessions + threads hostId into avatarUrl consumers — Plan 05 depends on Plan 03 (wave 3 vs wave 2)
- Confirmation that GET /:id/avatar's ETag is per-response-computed, NOT stored (matches shape "no cache" — HTTP cache-validation is distinct from server-side caching per W6)
</output>
