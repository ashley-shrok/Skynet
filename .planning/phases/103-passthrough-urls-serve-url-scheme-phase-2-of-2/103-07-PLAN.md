---
phase: 103-passthrough-urls-serve-url-scheme-phase-2-of-2
plan: 07
type: execute
wave: 2
depends_on: []
files_modified:
  - src/ui/features/pretty-view/editable-file-whitelist.ts
  - src/backend/utils/editable-file-whitelist.ts
  - src/ui/features/pretty-view/editable-file-whitelist.test.ts
autonomous: true
requirements: []
tags:
  - frontend
  - url-detection
  - regex
  - editable-file-whitelist
must_haves:
  truths:
    - "src/ui/features/pretty-view/editable-file-whitelist.ts exports a THIRD sibling regex `SKYNET_SERVE_URL_RE_CLIENT` alongside existing TAILNET_URL_RE_CLIENT (L96-97) and SKYNET_FILE_URL_RE_CLIENT (L143-144), matching `https://<hostname>-<port>.serve.term.<domain>/<path>?` (per D-29 + PATTERNS.md exact regex)"
    - "The regex is `/g` flagged like siblings; carries a comment `// ⚠️ Same /g gotcha — do not use .test(); use .match() for scanning` referencing PATTERNS.md L444"
    - "src/backend/utils/editable-file-whitelist.ts docblock (L1-27) gains a new 'PHASE 103 D-29 MIRROR-RULE UPDATE' paragraph documenting that `SKYNET_SERVE_URL_RE_CLIENT` is ALSO client-only (backend does its own URL parse in subdomain-dispatch middleware per D-11); the whitelist DATA arrays (EDITABLE_EXTENSIONS, EDITABLE_BASENAMES, classifyByExtension) remain unchanged (docstring-only edit per PATTERNS.md §editable-file-whitelist.ts 'Actual code change: docstring-only')"
    - "src/ui/features/pretty-view/editable-file-whitelist.test.ts (create if not exists) contains tests asserting SKYNET_SERVE_URL_RE_CLIENT matches valid serve URLs (multiple hosts, multiple ports, path/no-path, trailing punctuation edge cases) and does NOT match file URLs, tailnet URLs, or arbitrary web URLs — the three regexes are non-overlapping"
    - "Serve URLs are not added to the editable-file eligibility flow (use-editable-file-eligibility.ts) — per D-29 they render as PLAIN clickable links, not through the pencil-edit affordance (per PATTERNS.md 'For serve URLs, do NOT run through the eligibility byte-sniff loop')"
    - "The frontend ChatMessage.tsx (or wherever the URL-rendering happens) already renders any http/https URL as a clickable <a> via ReactMarkdown default behavior — Task 3 SPOT-CHECK confirms serve URLs render as clickable without additional wiring (planner: this is a manual spot check, executor may skip if grep confirms no special case for the file-URL/tailnet-URL classes intercepts /https:\\/\\/.*serve\\.term\\./ patterns)"
    - "Scoped tests pass: `npx vitest run src/ui/features/pretty-view/editable-file-whitelist.test.ts` exits 0"
    - "TypeScript compiles cleanly: `npx tsc --noEmit` exits 0"
  artifacts:
    - path: "src/ui/features/pretty-view/editable-file-whitelist.ts"
      provides: "SKYNET_SERVE_URL_RE_CLIENT export (third sibling regex)"
      exports: ["SKYNET_SERVE_URL_RE_CLIENT"]
      contains: "serve\\.term"
    - path: "src/backend/utils/editable-file-whitelist.ts"
      provides: "Docstring updated with PHASE 103 D-29 mirror-rule paragraph; data arrays unchanged"
      contains: "PHASE 103"
    - path: "src/ui/features/pretty-view/editable-file-whitelist.test.ts"
      provides: "Test coverage for the three sibling regexes' non-overlap invariants"
      contains: "SKYNET_SERVE_URL_RE_CLIENT"
  key_links:
    - from: "src/ui/features/pretty-view/editable-file-whitelist.ts"
      to: "serve URL regex export"
      via: "sibling regex added below SKYNET_FILE_URL_RE_CLIENT"
      pattern: "SKYNET_SERVE_URL_RE_CLIENT\\s*="
    - from: "src/backend/utils/editable-file-whitelist.ts"
      to: "docblock mirror rule"
      via: "PHASE 103 D-29 MIRROR-RULE UPDATE paragraph"
      pattern: "PHASE 103.*MIRROR-RULE"
---

<objective>
Add the frontend serve URL detection regex (SKYNET_SERVE_URL_RE_CLIENT) as a third sibling to TAILNET_URL_RE_CLIENT + SKYNET_FILE_URL_RE_CLIENT, matching the D-01 URL grammar (`<hostname>-<port>.serve.term.<domain>`) with the D-29 plain-clickable-link approach (no custom visual affordance). Update the backend twin's docblock to document the mirror-rule (data unchanged). Add regression tests for non-overlap invariants across the three regexes.

Purpose: realizes D-29 (frontend plain link) + D-30 (custom render deferred to future shape). Enables agents' constructed serve URLs to render as clickable in the chat surface without additional UX plumbing. The regex is a strictly-additive change; no existing behavior changes.

Output: 1 file modified (frontend regex), 1 file modified (backend docblock), 1 test file created/extended.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-CONTEXT.md
@.planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-PATTERNS.md
@src/ui/features/pretty-view/editable-file-whitelist.ts
@src/backend/utils/editable-file-whitelist.ts
@src/ui/features/pretty-view/use-editable-file-eligibility.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SKYNET_SERVE_URL_RE_CLIENT to editable-file-whitelist.ts + tests</name>
  <files>src/ui/features/pretty-view/editable-file-whitelist.ts, src/ui/features/pretty-view/editable-file-whitelist.test.ts</files>
  <read_first>
    - src/ui/features/pretty-view/editable-file-whitelist.ts (full file — especially L89-97 TAILNET_URL_RE_CLIENT + L125-144 SKYNET_FILE_URL_RE_CLIENT declaration blocks)
    - .planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-PATTERNS.md §src/ui/features/pretty-view/editable-file-whitelist.ts (exact regex per D-29 + /g gotcha)
    - .planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-CONTEXT.md D-29, D-30
    - src/ui/features/pretty-view/use-editable-file-eligibility.ts (L86-97 — confirm serve URLs are NOT threaded through eligibility byte-sniff)
  </read_first>
  <behavior>
    - SKYNET_SERVE_URL_RE_CLIENT.test('https://t1000-3020.serve.term.gigaashley.click') === true (NOTE: .test() on /g regex has state — the test file should use .match() OR create fresh non-global regex; the acceptance criteria will be phrased in terms of .match() result length)
    - 'https://t1000-3020.serve.term.gigaashley.click/foo/bar'.match(SKYNET_SERVE_URL_RE_CLIENT)?.length >= 1
    - 'https://thenasty-8080.serve.term.gigaashley.click'.match(SKYNET_SERVE_URL_RE_CLIENT)?.length >= 1
    - 'https://GIGAASHLEYPC-3000.serve.term.gigaashley.click'.match(SKYNET_SERVE_URL_RE_CLIENT)?.length >= 1 (uppercase hostname legal)
    - 'https://foo-bar.serve.term.gigaashley.click'.match(SKYNET_SERVE_URL_RE_CLIENT) === null (no digit port — must NOT match)
    - 'https://term.gigaashley.click/file/host/path'.match(SKYNET_SERVE_URL_RE_CLIENT) === null (file URL — should be picked up by SKYNET_FILE_URL_RE_CLIENT instead)
    - 'http://100.64.1.2:3000/'.match(SKYNET_SERVE_URL_RE_CLIENT) === null (tailnet URL — TAILNET_URL_RE_CLIENT handles)
    - Reciprocal non-overlap: a serve URL does NOT match SKYNET_FILE_URL_RE_CLIENT or TAILNET_URL_RE_CLIENT
  </behavior>
  <action>
    Modify src/ui/features/pretty-view/editable-file-whitelist.ts. Immediately after the existing SKYNET_FILE_URL_RE_CLIENT export block (around L143-144), add:

    ```
    // Phase 103 D-29: Serve URL — <hostname>-<port>.serve.term.<domain>[/path]
    // Grammar: hostname (any DNS-legal chars incl. digits and dashes, but see D-12
    // registration constraint: no trailing -\d+), literal "-", digits (port),
    // literal ".serve.term.", any DNS-legal parent domain, optional port on the
    // domain (rare — usually 443), optional path. See PATTERNS.md §editable-file-whitelist.ts.
    // ⚠️ Same /g gotcha as siblings (L89-91, L125-131): NEVER call .test() on this
    // regex; use .match() (stateless) for message-scanning, or a fresh non-global
    // regex for dispatch decisions.
    export const SKYNET_SERVE_URL_RE_CLIENT =
      /https:\/\/[a-zA-Z0-9._-]+-\d{1,5}\.serve\.term\.[a-zA-Z0-9.-]+(?::\d{1,5})?(?:\/[^\s)?#]*)?/g;
    ```

    Do NOT modify TAILNET_URL_RE_CLIENT or SKYNET_FILE_URL_RE_CLIENT. Do NOT modify the whitelist DATA arrays (EDITABLE_EXTENSIONS, EDITABLE_BASENAMES, etc.). Do NOT modify use-editable-file-eligibility.ts — serve URLs render as plain clickable links per D-29, not through the eligibility byte-sniff.

    Then create (or extend) src/ui/features/pretty-view/editable-file-whitelist.test.ts. If the file already exists, append; if not, create with vitest boilerplate. Test cases:

    describe('SKYNET_SERVE_URL_RE_CLIENT'):
    - it('matches basic serve URL') — matches 'https://t1000-3020.serve.term.gigaashley.click' → length ≥ 1
    - it('matches serve URL with path') — matches 'https://t1000-3020.serve.term.gigaashley.click/foo/bar' → length ≥ 1
    - it('matches serve URL with mixed-case hostname') — matches 'https://GIGAASHLEYPC-3000.serve.term.gigaashley.click' → length ≥ 1
    - it('rejects URL without digit port') — no match for 'https://foo-bar.serve.term.gigaashley.click'
    - it('rejects file URL') — no match for 'https://term.gigaashley.click/file/host/path'
    - it('rejects tailnet URL') — no match for 'http://100.64.1.2:3000/'
    - it('rejects arbitrary web URL') — no match for 'https://example.com/path'
    - it('rejects http scheme (must be https)') — no match for 'http://t1000-3020.serve.term.gigaashley.click' (regex intentionally requires https — this is the browser-facing URL scheme, http would be a config bug)

    describe('non-overlap invariants'):
    - it('serve URL does not match SKYNET_FILE_URL_RE_CLIENT') — for a serve URL, SKYNET_FILE_URL_RE_CLIENT.match() returns null (use fresh regex to avoid /g state)
    - it('serve URL does not match TAILNET_URL_RE_CLIENT') — same
    - it('file URL does not match SKYNET_SERVE_URL_RE_CLIENT') — reciprocal
  </action>
  <verify>
    <automated>grep -c 'SKYNET_SERVE_URL_RE_CLIENT' src/ui/features/pretty-view/editable-file-whitelist.ts &amp;&amp; grep -q 'serve\\\\.term' src/ui/features/pretty-view/editable-file-whitelist.ts &amp;&amp; test -f src/ui/features/pretty-view/editable-file-whitelist.test.ts &amp;&amp; npx vitest run src/ui/features/pretty-view/editable-file-whitelist.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: src/ui/features/pretty-view/editable-file-whitelist.ts exports `SKYNET_SERVE_URL_RE_CLIENT`
    - Source: the exported regex literal contains the substring `serve\.term\.` (escaped in regex)
    - Source: the exported regex has the `/g` flag
    - Source: TAILNET_URL_RE_CLIENT and SKYNET_FILE_URL_RE_CLIENT declarations are UNCHANGED (git diff scope check)
    - Source: EDITABLE_EXTENSIONS / EDITABLE_BASENAMES / classifyByExtension are UNCHANGED
    - Source: comment adjacent to the new regex mentions D-29 AND the /g gotcha AND references PATTERNS.md
    - Source: test file contains at least 8 `it(` blocks for SKYNET_SERVE_URL_RE_CLIENT + 3 for non-overlap
    - Behavior: `npx vitest run src/ui/features/pretty-view/editable-file-whitelist.test.ts` exits 0 with all tests passing
    - Behavior: `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Frontend regex added as third sibling; test coverage proves non-overlap with existing two; existing behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update backend twin's docblock with PHASE 103 mirror-rule paragraph</name>
  <files>src/backend/utils/editable-file-whitelist.ts</files>
  <read_first>
    - src/backend/utils/editable-file-whitelist.ts lines 1-27 (existing mirror-rule docblock — PATTERNS.md §src/backend/utils/editable-file-whitelist.ts confirms this file is DOCSTRING-ONLY for Phase 103)
    - .planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-PATTERNS.md §src/backend/utils/editable-file-whitelist.ts ('Actual code change: docstring-only')
    - .planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-CONTEXT.md D-29
  </read_first>
  <action>
    Modify src/backend/utils/editable-file-whitelist.ts. Append a new paragraph to the existing docblock (before the last `*/` of the top-of-file comment block, per PATTERNS.md §editable-file-whitelist.ts):

    ```
     *   PHASE 103 D-29 MIRROR-RULE UPDATE (2026-09-10): the frontend twin has
     *   gained a THIRD URL regex `SKYNET_SERVE_URL_RE_CLIENT` (sibling to
     *   `TAILNET_URL_RE_CLIENT` and `SKYNET_FILE_URL_RE_CLIENT`) matching the
     *   serve URL shape `https://<hostname>-<port>.serve.term.<domain>[/path]`.
     *   This backend twin does NOT re-export that regex — the backend validates
     *   serve URLs via the subdomain-dispatch middleware's own parse logic per
     *   D-11 (split on last dash of leftmost DNS label; right side must be all
     *   digits). Same rationale as TAILNET_URL_RE_CLIENT and
     *   SKYNET_FILE_URL_RE_CLIENT being client-only. The whitelist DATA
     *   (`EDITABLE_EXTENSIONS`, `EDITABLE_BASENAMES`, `classifyByExtension`)
     *   remains mirrored in lockstep as before — Phase 103 does NOT change the
     *   whitelist data itself.
    ```

    Do NOT modify any code in this file. Do NOT modify any EDITABLE_* constants. Do NOT re-export SKYNET_SERVE_URL_RE_CLIENT (per PATTERNS.md "Actual code change: docstring-only").
  </action>
  <verify>
    <automated>grep -q 'PHASE 103.*MIRROR-RULE' src/backend/utils/editable-file-whitelist.ts &amp;&amp; grep -q 'SKYNET_SERVE_URL_RE_CLIENT' src/backend/utils/editable-file-whitelist.ts &amp;&amp; grep -q 'D-11' src/backend/utils/editable-file-whitelist.ts &amp;&amp; ! grep -q '^export const SKYNET_SERVE_URL' src/backend/utils/editable-file-whitelist.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: src/backend/utils/editable-file-whitelist.ts docblock contains 'PHASE 103' and 'MIRROR-RULE'
    - Source: docblock references SKYNET_SERVE_URL_RE_CLIENT and D-11 and 'client-only'
    - Source: NO `export const SKYNET_SERVE_URL` line exists in this file (docstring-only edit, no code change)
    - Source: EDITABLE_EXTENSIONS + EDITABLE_BASENAMES + classifyByExtension are UNCHANGED (git diff scope: only docblock modified)
    - Behavior: `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Backend twin's docblock documents the Phase 103 mirror-rule; zero code changes to data or exports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent-written message → frontend regex | Agent-supplied strings scanned for URL detection; regex is defensive against malformed input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-33 | Denial of service | Malicious regex input triggering backtracking (ReDoS) | mitigate | Regex is linear-cost — no nested quantifiers with overlapping character classes. `[a-zA-Z0-9._-]+-\d{1,5}` etc. are unambiguous. Verified by inspection. |
| T-103-34 | Information disclosure | Frontend rendering serve URL as link exposes internal hostnames | accept | Same threat model as file URLs (Phase 78) — user-scoped visibility (only the user sees their own chat). No cross-user leak. |
| T-103-35 | Tampering | Regex accidentally matching file URLs or tailnet URLs | mitigate | Non-overlap invariant tested explicitly in Task 1. File URLs contain `/file/`; tailnet URLs use `100.x.y.z`; serve URLs require `.serve.term.` — three disjoint patterns. |
</threat_model>

<verification>
- `npx vitest run src/ui/features/pretty-view/editable-file-whitelist.test.ts` exits 0
- `npx tsc --noEmit` exits 0
- Grep confirms all 3 regexes coexist without overlap
- Backend twin is docstring-only edit
</verification>

<success_criteria>
- SKYNET_SERVE_URL_RE_CLIENT exported as third sibling, /g flag, with gotcha comment
- Backend twin docblock documents the mirror rule; zero code diff to data or exports
- Test coverage proves regex matches valid serve URLs and NOT file/tailnet/arbitrary URLs
</success_criteria>

<output>
Create `.planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-07-SUMMARY.md` when done
</output>
