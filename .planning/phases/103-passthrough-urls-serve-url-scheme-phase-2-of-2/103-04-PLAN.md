---
phase: 103-passthrough-urls-serve-url-scheme-phase-2-of-2
plan: 04
type: execute
wave: 4
depends_on:
  - 103-03b
files_modified:
  - src/backend/serve-url/tests/no-cookie-egress.integration.test.ts
autonomous: true
requirements: []
tags:
  - integration-test
  - security-test
  - cookie-egress
  - csrf-defense
must_haves:
  truths:
    - "src/backend/serve-url/tests/no-cookie-egress.integration.test.ts exists and follows the vi.mock hoisted-factory pattern used by user-avatars.integration.test.ts"
    - "The test stands up an echo-server upstream (Node http.createServer) that records EVERY inbound request's headers to a shared array captured across the test lifetime (per PATTERNS.md §no-cookie-egress.integration.test.ts step 1)"
    - "The test stands up the proxy-factory (from Plan 03b) pointing at the echo server (via a mocked or real localhost tunnel — mock the SSH forwardOut so the proxy target is `http://127.0.0.1:${echoPort}` directly)"
    - "For EACH of these transport scenarios, the test asserts that the echo-recorded headers object has NO `cookie`, NO `authorization`, and NO header starting with `x-skynet-`: GET, POST (JSON body), WebSocket upgrade, SSE stream (Accept: text/event-stream), multipart/form-data upload, and 302 redirect follow (per D-05 + PATTERNS.md exact enumeration)"
    - "Every test case sends a request WITH `Cookie: skynet_session=<some-value>` AND `Authorization: Bearer test` AND `X-Skynet-User-Id: 123` in the CLIENT-SIDE request headers — so a failure would demonstrate egress, not absence-of-input"
    - "The full test suite in this file counts at least 8 distinct `it(...)` blocks covering the 6 transport scenarios × the 3 header classes (cookie/auth/x-skynet) — see D-05 enumeration; may collapse to 8 test blocks with parameterized assertions each covering all 3 header classes on one transport"
    - "Test file MUST pass: `npx vitest run src/backend/serve-url/tests/no-cookie-egress.integration.test.ts` exits 0"
    - "Test file uses REAL http.createServer / real client HTTP requests (not fully-mocked) — this is an INTEGRATION test per D-05 'MUST pass to merge'"
    - "TypeScript compiles cleanly: `npx tsc --noEmit` exits 0"
  artifacts:
    - path: "src/backend/serve-url/tests/no-cookie-egress.integration.test.ts"
      provides: "CI enforcement of D-04 allowlist-strip across all HTTP + WebSocket + SSE + multipart + redirect transport paths"
      contains: "no-cookie-egress"
  key_links:
    - from: "src/backend/serve-url/tests/no-cookie-egress.integration.test.ts"
      to: "src/backend/serve-url/proxy-factory.ts"
      via: "import getOrCreateProxyForTarget"
      pattern: "getOrCreateProxyForTarget"
    - from: "src/backend/serve-url/tests/no-cookie-egress.integration.test.ts"
      to: "echo server recorded headers"
      via: "shared array + expect().toBeUndefined() assertions"
      pattern: "toBeUndefined"
---

<objective>
Build the D-05 CI integration test that enforces the D-04 allowlist-strip contract at merge time. Echo-server upstream records every header seen; test enumerates 6 transport scenarios × 3 header classes; each `it(...)` block asserts the disallowed header does NOT reach upstream. This test is the belt of the belt-and-suspenders (D-06 sampler is suspenders); MUST pass to merge per D-05.

Purpose: prevents regression. Any future refactor of proxy-factory.ts that accidentally drops the allowlist-strip (or misplaces the sec-websocket-extensions fix, which is in the same code path) fails this test. The test is co-located with Plan 03b's proxy-factory code per Phase 103 convention.

Output: 1 new integration test file (src/backend/serve-url/tests/no-cookie-egress.integration.test.ts).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-CONTEXT.md
@.planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-PATTERNS.md
@src/backend/database/routes/user-avatars.integration.test.ts
@src/backend/serve-url/types.ts
@src/backend/serve-url/proxy-factory.ts
@src/backend/serve-url/tunnel-cache.ts
@src/backend/serve-url/header-audit-sampler.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create no-cookie-egress.integration.test.ts covering 6 transports × 3 header classes</name>
  <files>src/backend/serve-url/tests/no-cookie-egress.integration.test.ts</files>
  <read_first>
    - src/backend/database/routes/user-avatars.integration.test.ts (canonical integration test structure per PATTERNS.md analog — vi.mock hoisted-factory pattern, real http.createServer setup, describe/beforeAll/afterAll shape)
    - src/backend/serve-url/proxy-factory.ts (from Plan 03b — the module under test)
    - src/backend/serve-url/tunnel-cache.ts (from Plan 03a — for mocking or bypass strategy)
    - src/backend/serve-url/types.ts (Plan 03a — ServeTarget contract)
    - .planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-PATTERNS.md §src/backend/serve-url/tests/no-cookie-egress.integration.test.ts (5-step structure + 8-case enumeration)
    - .planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-CONTEXT.md D-05
  </read_first>
  <behavior>
    - beforeAll: stand up an echo http.createServer that records inbound headers to `recordedHeaders` shared array; upgrade handler for WS also records the upgrade headers
    - beforeAll: stand up an Express app on random port that mounts getOrCreateProxyForTarget for a fake ServeTarget whose "tunnel" is really a direct pipe to the echo server's port (bypass tunnel-cache by mocking `getOrCreateProxyForTarget`'s internal Map, or by calling proxy-factory with a hand-crafted tunnelPort argument pointing at the echo)
    - afterAll: close both servers
    - beforeEach: `recordedHeaders.length = 0` (reset)
    - it("no Cookie / Authorization / X-Skynet-* headers on GET") — client sends GET with all 3 disallowed headers; assert recordedHeaders[0].cookie undefined, .authorization undefined, and no key matching /^x-skynet-/i present
    - it("no Cookie / Authorization / X-Skynet-* headers on POST JSON") — same for POST with `{ ping: 1 }` body + Content-Type: application/json
    - it("no Cookie / Authorization / X-Skynet-* headers on WebSocket upgrade") — client opens WS with `ws://` scheme via node:http.request upgrade + sends the 3 disallowed headers on the upgrade request; assert recordedUpgradeHeaders has none
    - it("no Cookie / Authorization / X-Skynet-* headers on SSE") — GET with Accept: text/event-stream; assert stream response is 200 AND recordedHeaders[0] has none of the 3
    - it("no Cookie / Authorization / X-Skynet-* headers on multipart/form-data upload") — POST with `multipart/form-data` body via FormData + Buffer; assert none of the 3 reach upstream
    - it("no Cookie / Authorization / X-Skynet-* headers on redirect follow") — echo responds with 302 + Location header (same-origin path); client follows redirect; assert BOTH the initial + followed requests recorded no disallowed headers
    - it("passes through Content-Type and Content-Length correctly") — SANITY test: POST with Content-Type: application/json, assert recordedHeaders[0]['content-type'] is 'application/json' (proves allowlist-strip is selective, not wholesale)
    - it("passes through Host header correctly") — GET, assert recordedHeaders[0].host is defined (proves host in allowlist works)
  </behavior>
  <action>
    Create src/backend/serve-url/tests/no-cookie-egress.integration.test.ts per D-05 + PATTERNS.md §no-cookie-egress.integration.test.ts. Follow the vi.mock hoisted-factory + real http.createServer structure from user-avatars.integration.test.ts.

    Structure:
    1. Import statements: describe, it, expect, beforeAll, beforeEach, afterAll, vi from vitest; http, https from node; express from express; type { AddressInfo } from node:net; getOrCreateProxyForTarget from ../proxy-factory.js; type { ServeTarget } from ../types.js.

    2. Module-level: `let echoServer: http.Server; let echoPort: number; let proxyApp: express.Application; let proxyServer: http.Server; let proxyPort: number; const recordedHeaders: Array<Record<string, string | string[] | undefined>> = []; const recordedUpgradeHeaders: Array<Record<string, string | string[] | undefined>> = [];`

    3. beforeAll: (a) start echoServer that records `req.headers` into recordedHeaders on every request AND responds 200 with `{ok: true}` (or `text/event-stream` if Accept matches SSE); handles WS upgrade by pushing req.headers into recordedUpgradeHeaders and completing a minimal upgrade handshake (or simply accepting-then-closing); (b) build a fake ServeTarget `{ hostname: 'echo', port: echoPort, host: <minimal-host-row-stub> }`; (c) call getOrCreateProxyForTarget(target, echoPort) — since proxy-factory expects a tunnelPort and the "tunnel" is a real localhost socket to the echo, passing echoPort directly bypasses tunnel-cache; (d) mount the returned middleware on proxyApp for `/*` and start proxyServer.

    4. beforeEach: reset recordedHeaders + recordedUpgradeHeaders arrays.

    5. afterAll: close both servers with await new Promise((r) => server.close(r)).

    6. Test blocks per the behavior enumeration above. Each test sends a real HTTP request to `http://127.0.0.1:${proxyPort}` with header set to `{ Cookie: 'skynet_session=abc123', Authorization: 'Bearer test', 'X-Skynet-User-Id': '999' }`. Assertions per test: `expect(recordedHeaders[0]?.cookie).toBeUndefined(); expect(recordedHeaders[0]?.authorization).toBeUndefined(); expect(Object.keys(recordedHeaders[0] ?? {}).filter(k => k.toLowerCase().startsWith('x-skynet-'))).toHaveLength(0);`

    7. Two SANITY tests: assert content-type + host DO pass through (allowlist-strip is selective, not wholesale).

    For WS: use `ws` library (already a Skynet dep) client. Send upgrade with `Cookie` header via `new WebSocket(url, { headers: {...} })`. Wait for upgrade / close event before asserting recordedUpgradeHeaders.

    For SSE: use undici (already a Skynet dep) fetch with `Accept: text/event-stream` and iterate response body reader to trigger a stream request through the proxy.

    For multipart: use FormData API (node:buffer + native FormData) and send POST with resulting multipart body.

    Do NOT wire this into any existing Express router. Do NOT depend on Plan 05's subdomain-dispatch (this test isolates proxy-factory only).
  </action>
  <verify>
    <automated>npx vitest run src/backend/serve-url/tests/no-cookie-egress.integration.test.ts 2&gt;&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Source: src/backend/serve-url/tests/no-cookie-egress.integration.test.ts exists
    - Source: file contains AT LEAST 8 distinct `it(` blocks (6 negative transport scenarios + 2 sanity/allowlist-passes tests)
    - Source: each negative `it(` block's request sets Cookie / Authorization / X-Skynet-* headers
    - Source: each negative `it(` block asserts `toBeUndefined()` on cookie AND authorization on the recorded upstream headers
    - Source: WS upgrade test uses `ws` library and asserts recordedUpgradeHeaders (not recordedHeaders) for upgrade-path headers
    - Source: SSE test sets `Accept: text/event-stream` on the client request
    - Source: multipart test sends `Content-Type: multipart/form-data` (or uses FormData that auto-sets it)
    - Source: redirect test asserts NO disallowed headers on EITHER the initial request OR the followed-redirect request
    - Source: sanity tests assert Content-Type + Host DO pass through (recordedHeaders[0]['content-type'] and .host defined)
    - Source: file uses real `http.createServer()` in beforeAll (not fully mocked) — matches user-avatars.integration.test.ts pattern per PATTERNS.md
    - Behavior: `npx vitest run src/backend/serve-url/tests/no-cookie-egress.integration.test.ts` exits 0 with ALL tests passing
    - Behavior: `npx tsc --noEmit` exits 0
    - Test integrity: injecting a bug in proxy-factory.ts that removes `stripToAllowlist` call would cause AT LEAST the GET+POST+multipart negative tests to fail (spot-check by temporarily commenting out stripToAllowlist and confirming test failures — revert after verification)
  </acceptance_criteria>
  <done>D-05 integration test enforces the D-04 allowlist-strip contract across all transport scenarios; passes on the current proxy-factory implementation; would fail if allowlist-strip is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test upstream (echo) → assertion layer | The echo records all inbound headers; test asserts absence of disallowed classes |
| Proxy under test → tunnel port | For this test, tunnel port === echo port (bypasses SSH; test isolates the proxy-factory allowlist behavior) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-19 | Tampering | Test coverage gap allowing regression in allowlist-strip | mitigate | 6 transport scenarios × 3 header classes = 18 boundary assertions minimum. Spot-check via mutation test (remove stripToAllowlist → confirm failures) baked into acceptance criteria. |
| T-103-20 | Repudiation | Test that always passes regardless of implementation | mitigate | 2 sanity tests (Content-Type + Host DO pass through) prove the test detects positive behavior, not just absence. Combined with mutation-test spot-check the test is provably meaningful. |
| T-103-21 | Denial of service | Test flakiness from real network I/O | accept | Localhost-only echo + localhost proxy = deterministic. If flakiness surfaces post-ship, add explicit timeouts on server.close() in afterAll. |
</threat_model>

<verification>
- `npx vitest run src/backend/serve-url/tests/no-cookie-egress.integration.test.ts` exits 0 with all tests passing
- `npx tsc --noEmit` exits 0
- File contains ≥ 8 `it(` blocks
- Mutation-test spot-check documented in acceptance_criteria (executor may skip the actual mutation edit — just verify existing tests exercise the removeHeader path via grep)
</verification>

<success_criteria>
- CI-enforced D-05 integration test file exists and passes on current proxy-factory implementation
- 6 transport scenarios × 3 disallowed header classes covered (~ 18 assertions across 6 negative tests)
- 2 sanity tests prove test integrity (positive-allow of Content-Type + Host)
- Real http.createServer echo upstream; not fully mocked (integration-test intent)
</success_criteria>

<output>
Create `.planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-04-SUMMARY.md` when done
</output>
