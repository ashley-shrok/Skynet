---
phase: 103-passthrough-urls-serve-url-scheme-phase-2-of-2
plan: 09
type: execute
wave: 2
depends_on: []
files_modified:
  - substrate/skills/id/SKILL.md
autonomous: false
requirements: []
tags:
  - id-skill
  - documentation
  - active-vs-passive-framing
  - fleet-substrate
must_haves:
  truths:
    - "substrate/skills/id/SKILL.md section 'Sending files to the user' (currently at L843-932) is REWRITTEN to include both file URL guidance (existing, retained) AND serve URL guidance (new) with the 'active vs passive' framing per D-26"
    - "The rewrite retains the file URL bash recipe (L865-884) VERBATIM per PATTERNS.md 'Retain the existing file-URL bash recipe (L865-884) verbatim'"
    - "The rewrite ADDS a new sub-section for serve URLs paralleling the file-URL sub-section, with: (a) URL grammar `https://<hostname>-<port>.serve.<term-parent-derived-from-skynet-parent>`, (b) concrete example `https://t1000-3020.serve.term.example.com`, (c) bash construction recipe reading ~/.claude/skynet-parent + ~/.claude/skynet-hostname (same config files Phase 78 established), (d) analog error-handling patterns (missing parent/hostname → user-facing error)"
    - "The 'active vs passive' decision guidance is added as a NEW top-level sub-section BEFORE both flavors, stating the D-26 rule verbatim: 'Active = something running on the other end (dev server, jupyter, WS stream, static server for multi-file content) → serve URL. Passive = bytes on disk (a doc, screenshot, log, config, downloadable binary) → file URL. Rule of thumb: Do you need something running on the other end for the user to have the right experience?'"
    - "The 'Why we replaced the old tailnet HTTP-server recipe' paragraph at L922-932 is EXPANDED to cover both URL schemes (file + serve) per PATTERNS.md 'expand to cover both URL schemes' — but the underlying reasoning (Chrome insecure-download friction, tailnet-only reach, agent-side server lifecycle burden) stays"
    - "The old tailnet-HTTP-server recipe (if any residue remains from pre-Phase-78) is DELETED entirely — no dual-path per D-27"
    - "NO auto-serve heuristics are documented (agents choose deliberately per D-28)"
    - "The rewrite includes rules-that-matter parallel to the file-URL section's rules (use ~/.claude/skynet-hostname not $(hostname); use port that agent explicitly chose; missing config → user-facing error not silent-fallback)"
    - "Total section length after rewrite: 100-200 lines (roughly doubled from current 90; adds serve-URL parallel plus framing; retains file-URL guidance)"
    - "Distributor sweep triggers on next Skynet rebuild+deploy — the distributor catalog at src/backend/distributor/catalog.ts:101-107 already points at /app/fleet-substrate/skills/id/SKILL.md, and Dockerfile:80 copies substrate → /app/fleet-substrate. Modifying substrate/skills/id/SKILL.md IS the push mechanism per PATTERNS.md — no distributor code change needed."
  artifacts:
    - path: "substrate/skills/id/SKILL.md"
      provides: "Rewritten 'Sending files to the user' section with active/passive framing + serve URL guidance"
      contains: "serve URL"
  key_links:
    - from: "substrate/skills/id/SKILL.md"
      to: "~/.claude/skynet-parent (per-box config from Phase 78)"
      via: "bash recipe reading the file for URL construction"
      pattern: "skynet-parent"
    - from: "substrate/skills/id/SKILL.md"
      to: "~/.claude/skynet-hostname (per-box config from Phase 78)"
      via: "bash recipe reading the file for hostname segment"
      pattern: "skynet-hostname"
---

<objective>
Rewrite the id-skill "Sending files to the user" section (substrate/skills/id/SKILL.md:843-932) to teach agents BOTH URL schemes (file + serve) with the D-26 active-vs-passive framing. Retain the file URL recipe (Phase 78 shipped it), add a parallel serve URL sub-section, expand the "why we replaced tailnet HTTP-server" paragraph to cover both schemes. Realizes D-25, D-26, D-27, D-28.

Purpose: closes the loop — the whole Phase 103 infrastructure is useless if agents don't KNOW to construct serve URLs when they've got something running on the other end. The active/passive framing is deliberately simple (one-sentence rule of thumb) so agents make the right choice without a decision tree. The distributor's next sweep pushes the new SKILL.md to every managed box; agents pick up on next `/id <name>` load.

Output: 1 file rewritten (substrate/skills/id/SKILL.md, section L843-932 replaced with expanded ~150-line section).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-CONTEXT.md
@.planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-PATTERNS.md
@substrate/skills/id/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite 'Sending files to the user' section with active/passive framing + serve URL sub-section</name>
  <files>substrate/skills/id/SKILL.md</files>
  <read_first>
    - substrate/skills/id/SKILL.md lines 843-935 (current section to rewrite — Phase 78 file-URL guidance)
    - .planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-PATTERNS.md §substrate/skills/id/SKILL.md (exact rewrite guidance — active/passive framing, retention rules, deletion rules)
    - .planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-CONTEXT.md D-25, D-26, D-27, D-28
    - .planning/shapes/shape-skynet-passthrough-urls.md §Philosophy (agents-aren't-lied-to; symmetric mental model)
  </read_first>
  <action>
    Rewrite substrate/skills/id/SKILL.md section starting at "## Sending files to the user" (L843) through the trailing separator (~L935). New structure per PATTERNS.md §SKILL.md + CONTEXT.md D-25/26/27/28:

    ```
    ## Sending files to the user

    When the user asks for something they need to see, read, download, or interact
    with — a diff, an artifact, a log, a running dev server, a jupyter notebook, a
    built webpage — Skynet gives you TWO URL schemes to make it reachable. Pick by
    the "active vs passive" rule.

    ### Active vs passive — which URL to construct

    - **Active** = something RUNNING on the other end that the user needs to
      interact with live. A dev server, a jupyter kernel, a WS stream, a static
      server hosting a multi-file page. → **serve URL**
    - **Passive** = bytes on disk that the user wants to read, download, or edit.
      A doc, a screenshot, a log, a config file, a downloadable binary, a single
      HTML snapshot. → **file URL**

    Rule of thumb: **"Do you need something running on the other end for the user
    to have the right experience?"** Yes → serve URL. No → file URL.

    Never rewrite one flavor into the other. If you handed the user a file URL, it
    stays a file URL; if a serve URL, it stays a serve URL. Same reason as
    everything else in this skill: the URL you wrote is the URL she clicks, no
    Skynet-side rewriting behind your back.

    ### File URL — passive bytes on disk

    <retain existing L843-921 file URL guidance VERBATIM, minus the intro
    paragraph which is now covered by the active/passive rule above. Specifically
    retain: the "Small text (< ~5 KB)" callout, the "Anything larger, or binary"
    section, the bash recipe reading ~/.claude/skynet-parent + ~/.claude/skynet-hostname,
    the round-trip semantics paragraph, and the "Rules that matter" bullet list.>

    ### Serve URL — active content, live proxy

    When you have something running on a port on this box — a dev server, a
    jupyter, a WS stream, an ad-hoc static server hosting a multi-file page — hand
    the user a **Skynet serve URL** that reverse-proxies through to it. Skynet
    doesn't care what's on the other side; it just proxies HTTP + WebSocket
    traffic through an SSH tunnel to whatever port you tell it.

    Grammar:

        https://<hostname>-<port>.serve.<term-parent>

    Where `<term-parent>` is derived from `~/.claude/skynet-parent`: strip the
    protocol, then the serve URL constructs as `<hostname>-<port>.serve.<the-rest>`.
    Concrete example (parent-Skynet `https://term.example.com`, this box
    named `t1000`, dev server on port 3020):

        https://t1000-3020.serve.term.example.com

    Construct like so:

        SKYNET=$(cat ~/.claude/skynet-parent 2>/dev/null)
        if [ -z "$SKYNET" ]; then
          echo "I can't share a live serve URL right now — my parent-Skynet config is missing." \
               "Ask the box-maintainer role to check the distributor sweep." >&2
          exit 1
        fi
        HOST=$(cat ~/.claude/skynet-hostname 2>/dev/null)
        if [ -z "$HOST" ]; then
          echo "I can't share a live serve URL right now — my Skynet hostname config is missing." \
               "Ask the box-maintainer role to check the distributor sweep." >&2
          exit 1
        fi
        PORT=3020                          # the port your live thing is listening on
        # Strip https:// and split into first label + rest to insert the serve subdomain
        PARENT=${SKYNET#https://}
        FIRST_LABEL=${PARENT%%.*}          # e.g. "term"
        REST=${PARENT#*.}                  # e.g. "example.com"
        printf '[%s live](https://%s-%d.serve.%s.%s)\n' "$HOST" "$HOST" "$PORT" "$FIRST_LABEL" "$REST"

    **Round-trip semantics — Skynet is a passthrough only.** Any HTTP method +
    body + WebSocket upgrade flows through unchanged. Skynet strips your session
    cookie + auth headers before forwarding — the running thing on the other end
    sees a plain request from Skynet's edge, not from a specific authenticated
    user (auth is enforced at Skynet's edge, not passed to your app).

    **Rules that matter — bake them in every time:**

    - **Use the hostname the distributor wrote to `~/.claude/skynet-hostname`, not
      an IP and not `$(hostname)`.** Same rule as the file URL — this box's DB
      record uses that exact string.
    - **The port must be listening BEFORE you cite the URL.** If you cite
      `t1000-3020.serve.term.example.com` and nothing is on 3020, the user
      sees a "port 3020 of t1000 isn't responding" interstitial page. Skynet-styled,
      polite; still, don't cite dead URLs.
    - **The port must be a plain integer in the range 1-65535.** Not a name, not
      a range. If your thing binds to a random port on start, capture the port
      first then construct the URL.
    - **Hostname can't end in `-<digits>`.** The URL parse rule splits on the
      last dash of the leftmost label to separate hostname from port. Registration
      already blocks names like `foo-42` — current fleet all passes. If the
      distributor writes a name ending in `-\d+` to your box, that's a bug —
      surface it.
    - **Missing `~/.claude/skynet-parent` OR missing `~/.claude/skynet-hostname` =
      user-facing error**, never guess or fall back. Same rule as file URLs.
    - **NO auto-fallback to the tailnet HTTP-server recipe.** That recipe is gone
      (see below). If serve URL is broken, tell the user and stop; don't stand
      up a local HTTP server as a workaround — you'd be handing her a URL Chrome
      flags as insecure, which is a worse UX than the interstitial.

    **What the user actually sees when she clicks a serve URL.** Her browser
    opens `https://<hostname>-<port>.serve.<term-parent>` under Skynet's HTTPS
    cert. Skynet checks her session + your per-user-per-host access, opens (or
    reuses) an SSH tunnel to the port on your box, and reverse-proxies HTTP +
    WebSocket bytes. Everything her browser needs — absolute-path assets,
    cookies, service workers — resolves against that same subdomain, so modern
    frontends (Vite / Next.js / anything) work naturally.

    ### Why we replaced the old tailnet HTTP-server recipe (both URL schemes)

    Both URL schemes replace the old "stand up an HTTP server on a tailnet IP
    and hand the user a link" recipe. That recipe had three chronic pain points:
    (1) Chrome flagged every download as "insecure file" because the tailnet has
    no cert path on Alice's plan; (2) the tailnet-only reach meant customer VMs
    and any box off the tailnet (T800 today, future customer boxes tomorrow)
    simply couldn't be served this way; and (3) every share saddled you with
    agent-side server lifecycle burden — a backgrounded process to kill,
    `mktemp -d` staging dirs to clean up, port juggling, PID tracking,
    self-`pkill` traps. Both Skynet passthrough URL schemes (file + serve) are
    HTTPS end-to-end, work from anywhere Skynet reaches, and no agent-side
    process to babysit (file URL is a fetch from Skynet; serve URL is a
    reverse-proxy to your already-running port — you own the process lifecycle
    exactly as long as you'd own it anyway, no extra HTTP server on top).

    ---
    ```

    Retain everything below the "---" separator as-is (next section starts).

    NO auto-serve heuristics per D-28. NO dual-path fallback to tailnet-HTTP per D-27.

    Word count target: 100-200 lines total for the rewritten section (roughly doubles the current 90-line section).
  </action>
  <verify>
    <automated>grep -q '## Sending files to the user' substrate/skills/id/SKILL.md &amp;&amp; grep -q 'Active vs passive' substrate/skills/id/SKILL.md &amp;&amp; grep -q 'serve URL' substrate/skills/id/SKILL.md &amp;&amp; grep -q '\.serve\.' substrate/skills/id/SKILL.md &amp;&amp; grep -q 'skynet-parent' substrate/skills/id/SKILL.md &amp;&amp; grep -q 'skynet-hostname' substrate/skills/id/SKILL.md &amp;&amp; grep -q 'Why we replaced the old tailnet HTTP-server recipe' substrate/skills/id/SKILL.md &amp;&amp; [ "$(grep -c 'tailnet HTTP.server\|tailnet.HTTP\|local HTTP server' substrate/skills/id/SKILL.md)" -le 3 ]</automated>
  </verify>
  <acceptance_criteria>
    - Source: substrate/skills/id/SKILL.md contains section header `## Sending files to the user`
    - Source: file contains sub-section header or bolded phrase `Active vs passive` (or "active vs passive")
    - Source: file contains the exact D-26 rule-of-thumb sentence "Do you need something running on the other end for the user to have the right experience?" (or very close paraphrase)
    - Source: file contains sub-section header for serve URL (e.g. `### Serve URL`)
    - Source: file retains sub-section for file URL (e.g. `### File URL`)
    - Source: file contains bash recipe reading `~/.claude/skynet-parent`
    - Source: file contains bash recipe reading `~/.claude/skynet-hostname`
    - Source: file contains at least one concrete serve URL example matching `https://.*-\d+\.serve\.term\.`
    - Source: file contains expanded "Why we replaced the old tailnet HTTP-server recipe" paragraph that mentions BOTH file URL and serve URL
    - Source: the OLD standalone tailnet-HTTP-server recipe (if any active-recipe residue existed pre-Phase-78) is not present — `grep -c 'python -m http.server\|python3 -m http.server' substrate/skills/id/SKILL.md` returns 0 in the Sending-files section (allow up to 3 total in file for unrelated uses like debugging examples elsewhere)
    - Source: no auto-serve heuristics documented — `grep -i 'auto-serve\|automatically detect' substrate/skills/id/SKILL.md` returns 0 in the Sending-files section
    - Source: section length between 100 and 250 lines (approximately doubled from original 90)
  </acceptance_criteria>
  <done>Section rewritten with active/passive framing, both URL schemes documented, tailnet-HTTP recipe deleted, no dual-path.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Alice reviews the rewritten section for tone + framing accuracy</name>
  <files>substrate/skills/id/SKILL.md</files>
  <read_first>
    - substrate/skills/id/SKILL.md (produced by Task 1 rewrite of lines ~843-1000)
  </read_first>
  <what-built>
    Task 1 rewrote substrate/skills/id/SKILL.md 'Sending files to the user' section (~L843-1000 post-rewrite). Includes:
    - New active/passive framing intro per D-26
    - Retained file URL sub-section (Phase 78 content)
    - New serve URL sub-section with bash recipe
    - Expanded "why we replaced tailnet HTTP-server" covering both schemes
    - No dual-path, no auto-serve
  </what-built>
  <how-to-verify>
    1. Read the rewritten section (substrate/skills/id/SKILL.md lines ~843 through the trailing "---") end-to-end as if you were an agent picking up the skill for the first time
    2. Verify the active/passive rule is the FIRST thing an agent reads — it's the decision surface, everything else is machinery
    3. Verify the file URL and serve URL sub-sections feel PARALLEL (same rhythm, same recipe shape, same rules-that-matter list) — asymmetry reads as one being "primary" and the other "secondary" which fights the shape file's "symmetric mental model" philosophy
    4. Verify the bash recipe for serve URL construction works when copy-pasted into a shell on t1000 with a mocked port listening (e.g. `python3 -m http.server 3020 &` then run the recipe) — the recipe should produce a well-formed URL string
    5. Verify tone matches the rest of the id-skill: warm, specific, not-lecturing. If it reads like Alice wrote it, it's right; if it reads like Claude wrote it, ask for rewrites
    6. Approve or request specific tone/framing/example corrections
  </how-to-verify>
  <action>
    Pause execution and present the rewritten SKILL.md section to Alice. When she approves ("approved"), the plan completes. If she requests revisions ("revise: tone / framing / recipe / example"), executor addresses each and re-presents. This is the tone gate — the section only merges once Alice says it reads right.
  </action>
  <verify>
    <automated>echo "checkpoint task — verified by human review of substrate/skills/id/SKILL.md"</automated>
  </verify>
  <resume-signal>Type "approved" if section reads correctly, or "revise: <specific-guidance>" with corrections</resume-signal>
  <done>Alice types "approved" — plan complete. If "revise" requested, executor addresses and re-checkpoints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → user via SKILL.md guidance | Guidance shapes what agents construct; wrong guidance = wrong URLs = broken UX |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-41 | Information disclosure | Guidance leaking internal architecture details users don't need | accept | SKILL.md is agent-facing (not user-facing); agents already know Skynet's shape. No exposure risk. |
| T-103-42 | Repudiation | Agents claim they "didn't know" about serve URLs post-ship | mitigate | Distributor sweep propagates the new SKILL.md; agents pick up on next `/id <name>` load. Coverage is deterministic (not opt-in). |
| T-103-43 | Elevation of privilege | Malicious agent constructs "serve URL" for a host they don't own | mitigate | Backend (Plan 05 subdomain-dispatch + Plan 03 permission check) enforces per-user-per-host access — SKILL.md guidance doesn't grant anything, just teaches URL grammar. |
| T-103-44 | Tampering | Guidance drifts from actual behavior over time | mitigate | SKILL.md is version-controlled alongside the implementation (this commit lands both). Any Phase 200+ change to serve URL behavior touches both this file AND the implementation, coupling drift-prone components. |
</threat_model>

<verification>
- SKILL.md contains all required section headers + concrete examples + bash recipes
- Human checkpoint approves tone + framing
- No auto-serve or dual-path guidance
- Section length ~100-250 lines
</verification>

<success_criteria>
- Agents on next distributor sweep + `/id <name>` load see the new active/passive rule + serve URL recipe
- Retained file URL guidance is untouched (bash recipe verbatim per PATTERNS.md)
- Tailnet-HTTP-server recipe deleted; no dual-path
- Alice signs off on tone before merge
</success_criteria>

<output>
Create `.planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-09-SUMMARY.md` when done
</output>
