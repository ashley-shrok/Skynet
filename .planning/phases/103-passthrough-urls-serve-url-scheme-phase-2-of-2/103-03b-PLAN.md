---
phase: 103-passthrough-urls-serve-url-scheme-phase-2-of-2
plan: 03b
type: execute
wave: 3
depends_on:
  - 103-03a
files_modified:
  - src/backend/serve-url/proxy-factory.ts
  - src/backend/serve-url/header-audit-sampler.ts
  - package.json
autonomous: false
requirements: []
tags:
  - serve-url
  - reverse-proxy
  - http-proxy-middleware
  - allowlist-strip
  - permessage-deflate
  - header-audit
must_haves:
  truths:
    - "src/backend/serve-url/proxy-factory.ts exports getOrCreateProxyForTarget(target, tunnelPort) that returns an http-proxy-middleware instance with target=`http://127.0.0.1:${tunnelPort}`, changeOrigin: true, ws: true, and on.proxyReqWs setting sec-websocket-extensions to empty string (per R&D GOTCHA 1 permessage-deflate strip — bake in day one)"
    - "proxy-factory.ts on.proxyReq AND on.proxyReqWs BOTH apply the allowlist-strip: iterate proxyReq.getHeaderNames(), removeHeader for any header not in HEADER_ALLOWLIST from Plan 03a's types.ts (per D-04 default-deny)"
    - "proxy-factory.ts is cached PER TARGET (Map<`${hostname}:${port}`, middleware>) — creating fresh middleware per request causes RSV1 failures per R&D GOTCHA 2"
    - "src/backend/serve-url/header-audit-sampler.ts exports emitHeaderAudit(target, phase, proxyReq) invoked from proxy-factory's on.proxyReq/on.proxyReqWs AFTER allowlist-strip; emits info at 100% during first hour (Date.now() < DEPLOY_EPOCH + 3600000) then 1% (Math.random() < 0.01); emits warn on any header outside allowlist appearing in getHeaderNames() (per D-06)"
    - "package.json adds http-proxy-middleware as a runtime dep (pinned to ^4, matches R&D POC version 4.2.0)"
    - "The package legitimacy audit gate passes: http-proxy-middleware is verified via a blocking-human checkpoint BEFORE `npm install` runs"
    - "Scoped tests pass: `npx vitest run src/backend/serve-url/` exits 0 (0 failures — expect no test files yet; adds are OK)"
    - "TypeScript compiles cleanly: `npx tsc --noEmit` exits 0"
  artifacts:
    - path: "src/backend/serve-url/proxy-factory.ts"
      provides: "Per-target-cached http-proxy-middleware factory with allowlist-strip + permessage-deflate fix"
      exports: ["getOrCreateProxyForTarget"]
    - path: "src/backend/serve-url/header-audit-sampler.ts"
      provides: "emitHeaderAudit(target, phase, proxyReq) with time-decaying sample rate + warn on out-of-allowlist headers"
      exports: ["emitHeaderAudit"]
    - path: "package.json"
      provides: "http-proxy-middleware dependency added"
      contains: "http-proxy-middleware"
  key_links:
    - from: "src/backend/serve-url/proxy-factory.ts"
      to: "src/backend/serve-url/types.ts"
      via: "HEADER_ALLOWLIST import + iterating proxyReq.getHeaderNames()"
      pattern: "HEADER_ALLOWLIST"
    - from: "src/backend/serve-url/proxy-factory.ts"
      to: "src/backend/serve-url/header-audit-sampler.ts"
      via: "emitHeaderAudit call inside on.proxyReq/on.proxyReqWs hooks"
      pattern: "emitHeaderAudit\\("
    - from: "src/backend/serve-url/proxy-factory.ts"
      to: "sec-websocket-extensions strip"
      via: "proxyReqWs sets header to empty string per R&D GOTCHA 1"
      pattern: "sec-websocket-extensions"
---

<objective>
Build the behavior half of the serve URL backend primitives: install http-proxy-middleware (after a blocking legitimacy checkpoint), then create the per-target-cached proxy factory with allowlist-strip + permessage-deflate fix, and the runtime header-audit sampler that alerts on any header slipping past the strip. Consumes Plan 03a's HEADER_ALLOWLIST + ServeTarget contracts. Realizes D-04 (allowlist-strip), D-06 (header-audit sampler), plus R&D GOTCHA 1 (permessage-deflate strip baked in day one) + GOTCHA 2 (per-target cache).

Purpose: this is the load-bearing security boundary of the whole serve URL infrastructure — every outbound request from Skynet to the agent's local port passes through this factory's on.proxyReq/on.proxyReqWs hooks. If the allowlist-strip is buggy or missing, D-04's default-deny promise breaks. The Plan 04 integration test enforces this contract at merge time.

Output: 2 new files (proxy-factory.ts, header-audit-sampler.ts) + http-proxy-middleware added to package.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-CONTEXT.md
@.planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-PATTERNS.md
@src/backend/serve-url/types.ts
@src/backend/serve-url/tunnel-cache.ts
@src/backend/utils/logger.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy checkpoint for http-proxy-middleware</name>
  <files>package.json</files>
  <read_first>
    - ~/.claude/roles/box-maintainer/bounties/archive/skynet-passthrough-urls-rd/findings-summary.md (L18 confirms http-proxy-middleware@4.2.0 matches Skynet's own dep versions per R&D POC setup)
  </read_first>
  <what-built>
    NO code yet — this is a legitimacy gate BEFORE `npm install http-proxy-middleware` runs. Package legitimacy audit per gsd package-install gate: http-proxy-middleware is used in R&D POCs 1-6, and is the canonical Node.js reverse-proxy library. Existing evidence:
    - Downloads: ~5M/week per npmjs.com/package/http-proxy-middleware
    - Maintainer: chimurai (well-known; 10+ years OSS)
    - License: MIT
    - Referenced by ssh2/express ecosystem widely
    - No known CVE against 4.x line (as of 2026-09-10)
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/http-proxy-middleware in browser
    2. Verify: package version 4.x is latest stable, weekly downloads > 1M, published in the last 90 days (freshness)
    3. Verify: maintainer field lists "chimurai" as primary; homepage points at github.com/chimurai/http-proxy-middleware
    4. Verify: GitHub repo has active issues + PRs (not abandoned) and > 10k stars
    5. Verify no fresh critical CVE by scanning GitHub Security Advisories (github.com/advisories?query=http-proxy-middleware) — accept if the last severe advisory is > 6 months old and mitigated in the version installed
    6. If any check fails, halt this plan — Ashley decides whether to accept the risk or switch to an alternative (hand-rolled http proxy using node:http.request + ws)
  </how-to-verify>
  <action>
    Pause execution and present this checkpoint. When the human approves ("approved"), Task 2 proceeds with `npm install http-proxy-middleware@^4`. If concerns raised, HALT — do not proceed to install. Executor does NOT run npm install as part of this task.
  </action>
  <verify>
    <automated>echo "checkpoint task — verified by human review of npmjs.com pages"</automated>
  </verify>
  <resume-signal>Type "approved" if package is legitimate, or "reject: switch to hand-rolled proxy" if audit surfaces concerns</resume-signal>
  <done>Human types "approved" (or equivalent) and Task 2 proceeds. If "reject" or concerns raised, plan halts and Ashley decides direction.</done>
</task>

<task type="auto">
  <name>Task 2: Install http-proxy-middleware + create header-audit-sampler.ts</name>
  <files>package.json, src/backend/serve-url/header-audit-sampler.ts</files>
  <read_first>
    - package.json (current dependency block — verify http-proxy-middleware not already listed)
    - src/backend/serve-url/types.ts (Plan 03a — HEADER_ALLOWLIST + ServeTarget from Task 1)
    - src/backend/utils/logger.ts (sshLogger + systemLogger)
    - .planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-PATTERNS.md §src/backend/serve-url/header-audit-sampler.ts
    - .planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-CONTEXT.md D-06
  </read_first>
  <action>
    Run `npm install http-proxy-middleware@^4` (matches R&D POC version) — commits to package.json + package-lock.json.

    Then create src/backend/serve-url/header-audit-sampler.ts per PATTERNS.md §header-audit-sampler.ts: export `emitHeaderAudit(target: ServeTarget, phase: 'req' | 'ws', proxyReq: http.ClientRequest): void`. Behavior per D-06: (a) let `DEPLOY_EPOCH = Date.now()` at module load (approximation of first-hour window); (b) sample rate: `shouldSample = (Date.now() < DEPLOY_EPOCH + 3600000) || (Math.random() < 0.01)`; (c) if shouldSample, `systemLogger.info('serve-url header audit', { operation: 'serve_url_header_audit', target: `${target.hostname}:${target.port}`, phase, headers: proxyReq.getHeaderNames() })`; (d) ALWAYS check for anomaly: `const outOfAllowlist = proxyReq.getHeaderNames().filter(h => !HEADER_ALLOWLIST.includes(h.toLowerCase())); if (outOfAllowlist.length > 0) systemLogger.warn('serve-url header anomaly', { operation: 'serve_url_header_anomaly', target, phase, outOfAllowlist })`. This is the D-06 "second layer of defense" — the anomaly signal fires AFTER Task 3's allowlist-strip completes so any residue is a bug.
  </action>
  <verify>
    <automated>grep -c 'http-proxy-middleware' package.json | awk '{ if ($1 &gt;= 1) exit 0; else exit 1 }' &amp;&amp; test -f src/backend/serve-url/header-audit-sampler.ts &amp;&amp; grep -c 'serve_url_header_anomaly' src/backend/serve-url/header-audit-sampler.ts | awk '{ if ($1 &gt;= 1) exit 0; else exit 1 }' &amp;&amp; grep -c 'serve_url_header_audit' src/backend/serve-url/header-audit-sampler.ts | awk '{ if ($1 &gt;= 1) exit 0; else exit 1 }' &amp;&amp; npx tsc --noEmit 2&gt;&amp;1 | grep -v '^$' | wc -l | awk '{ if ($1 == 0) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep 'http-proxy-middleware' package.json` shows the dep in dependencies (not devDependencies)
    - Source: package-lock.json includes http-proxy-middleware in its lockfile
    - Source: src/backend/serve-url/header-audit-sampler.ts exports `emitHeaderAudit`
    - Source: header-audit-sampler.ts contains both operation strings `serve_url_header_audit` (info level) and `serve_url_header_anomaly` (warn level)
    - Source: header-audit-sampler.ts has time-decay sampling logic (Date.now comparison + Math.random threshold)
    - Behavior: `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>http-proxy-middleware installed post-legitimacy-gate; header-audit-sampler.ts emits info (100% first hour, 1% after) + warn on any header outside allowlist reaching upstream.</done>
</task>

<task type="auto">
  <name>Task 3: Create proxy-factory.ts (allowlist-strip + permessage-deflate + per-target cache)</name>
  <files>src/backend/serve-url/proxy-factory.ts</files>
  <read_first>
    - src/backend/serve-url/types.ts (HEADER_ALLOWLIST + ServeTarget from Plan 03a)
    - src/backend/serve-url/tunnel-cache.ts (from Plan 03a — output tunnelPort feeds proxy target)
    - src/backend/serve-url/header-audit-sampler.ts (Task 2 — emitHeaderAudit)
    - src/backend/utils/logger.ts (sshLogger + systemLogger)
    - ~/.claude/roles/box-maintainer/bounties/archive/skynet-passthrough-urls-rd/findings-summary.md §GOTCHA 1 (permessage-deflate RSV1 fix — L137-154) and §GOTCHA 2 (per-target cache — L158-160)
    - .planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-PATTERNS.md §proxy-factory.ts
    - .planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-CONTEXT.md D-04, D-06
  </read_first>
  <action>
    Create src/backend/serve-url/proxy-factory.ts per PATTERNS.md §proxy-factory.ts. Import `createProxyMiddleware` from http-proxy-middleware, HEADER_ALLOWLIST + ServeTarget from types.ts, emitHeaderAudit from header-audit-sampler.ts. Export `getOrCreateProxyForTarget(target: ServeTarget, tunnelPort: number): RequestHandler`. Maintain a module-level `Map<`${hostname}:${port}`, RequestHandler>` (per R&D GOTCHA 2 — proxy instance MUST be cached per target). On cache miss, build middleware via createProxyMiddleware with:
    - target: `http://127.0.0.1:${tunnelPort}`
    - changeOrigin: true
    - ws: true
    - on.proxyReq(proxyReq): call stripToAllowlist(proxyReq) FIRST, then call emitHeaderAudit(target, 'req', proxyReq)
    - on.proxyReqWs(proxyReq): call stripToAllowlist(proxyReq) FIRST, then `proxyReq.setHeader('sec-websocket-extensions', '')` (force-set to empty string — upstream reads as "no extensions negotiated"; per R&D GOTCHA 1 L145-152 permessage-deflate strip), then emitHeaderAudit(target, 'ws', proxyReq)

    Helper `stripToAllowlist(proxyReq: http.ClientRequest)`: iterate `proxyReq.getHeaderNames()`, for each header name `h`, if `!HEADER_ALLOWLIST.includes(h.toLowerCase())` then `proxyReq.removeHeader(h)`. Per D-04 default-deny; NO cookies, NO Authorization, NO X-Skynet-* survive.

    Order-of-operations note for on.proxyReqWs: strip FIRST (removes sec-websocket-extensions since it's NOT in the allowlist), THEN explicitly setHeader('sec-websocket-extensions', '') to force upstream to see "no extensions negotiated" rather than "no header at all" (some upstreams treat missing vs empty differently). Reference R&D findings-summary L145-152 for the working POC pattern.

    Do NOT wire this factory to Express here — Plan 05 mounts it.
  </action>
  <verify>
    <automated>test -f src/backend/serve-url/proxy-factory.ts &amp;&amp; grep -c 'createProxyMiddleware' src/backend/serve-url/proxy-factory.ts | awk '{ if ($1 &gt;= 1) exit 0; else exit 1 }' &amp;&amp; grep -c 'sec-websocket-extensions' src/backend/serve-url/proxy-factory.ts | awk '{ if ($1 &gt;= 1) exit 0; else exit 1 }' &amp;&amp; grep -c 'HEADER_ALLOWLIST' src/backend/serve-url/proxy-factory.ts | awk '{ if ($1 &gt;= 1) exit 0; else exit 1 }' &amp;&amp; grep -c 'emitHeaderAudit' src/backend/serve-url/proxy-factory.ts | awk '{ if ($1 &gt;= 1) exit 0; else exit 1 }' &amp;&amp; npx tsc --noEmit 2&gt;&amp;1 | grep -v '^$' | wc -l | awk '{ if ($1 == 0) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - Source: src/backend/serve-url/proxy-factory.ts imports createProxyMiddleware from http-proxy-middleware
    - Source: proxy-factory.ts references HEADER_ALLOWLIST from ./types.ts and calls emitHeaderAudit from ./header-audit-sampler.ts
    - Source: proxy-factory.ts contains BOTH `proxyReq` and `proxyReqWs` handler hooks (grep for both)
    - Source: proxy-factory.ts contains `sec-websocket-extensions` setHeader with empty string second arg (per R&D GOTCHA 1)
    - Source: proxy-factory.ts contains a Map cache at module scope for per-target middleware reuse (grep for `new Map` or equivalent)
    - Source: proxy-factory.ts contains `ws: true` in the createProxyMiddleware config
    - Source: proxy-factory.ts contains `changeOrigin: true` in the createProxyMiddleware config
    - Behavior: `npx tsc --noEmit` exits 0
    - Behavior: `npx vitest run src/backend/serve-url/` exits 0 (no tests yet OK; failures NOT OK)
  </acceptance_criteria>
  <done>proxy-factory produces per-target-cached http-proxy-middleware with allowlist-strip and permessage-deflate strip baked in from day one, plus emitHeaderAudit calls for D-06 runtime alerting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skynet backend → http-proxy-middleware → tunnel port | Node-level socket layer; proxy library is trusted (see legitimacy gate) |
| Header allowlist enforcement | The load-bearing security boundary between "any header Skynet has access to" and "the small set upstream sees" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-12 | Information disclosure | Cookie / Authorization / X-Skynet-* headers leaking to upstream | mitigate | HEADER_ALLOWLIST default-deny per D-04. proxy-factory.ts stripToAllowlist iterates getHeaderNames() and removeHeader for anything not in the explicit 8-header list. Second-layer defense: header-audit-sampler emits warn on ANY header not in allowlist appearing at proxyReq time (D-06). Plan 04 integration test asserts absence. |
| T-103-13 | Tampering | permessage-deflate RSV1 corruption of WS frames | mitigate | sec-websocket-extensions header force-set to empty string in on.proxyReqWs per R&D GOTCHA 1. Baked into proxy-factory.ts day one, not left as follow-up. |
| T-103-16 | Spoofing | Fake ServeTarget passed to getOrCreateProxyForTarget | accept | proxy-factory is not exposed to network directly; only Plan 05 subdomain-dispatch (which validates JWT + resolveHostByName + canAccessHost per D-03) calls into it. Trust boundary is upstream of this module. |
| T-103-18 | Repudiation | Silent allowlist-strip removing legitimate headers | mitigate | header-audit-sampler emits info at 100% during first hour post-deploy (Date.now < DEPLOY_EPOCH + 3600000) then 1% ongoing; anomaly detection at warn level catches any header slippage. Investigatable via structured logs. |
| T-103-SC | Tampering | http-proxy-middleware supply chain | mitigate | Task 1 legitimacy checkpoint verified maintainer, downloads, CVE status BEFORE install. Pinned to ^4 in package.json (SemVer-compatible with R&D POC 4.2.0). Blocking-human gate — never auto-approvable. |
</threat_model>

<verification>
- `npx vitest run src/backend/serve-url/` exits 0 (no tests in this plan; 0 failures required)
- `npx tsc --noEmit` exits 0
- 2 new files exist under src/backend/serve-url/ (proxy-factory.ts + header-audit-sampler.ts)
- package.json includes http-proxy-middleware in dependencies (post-legitimacy checkpoint)
</verification>

<success_criteria>
- Package legitimacy gate passed by human checkpoint
- proxy-factory.ts produces per-target-cached http-proxy-middleware with (a) allowlist-strip on proxyReq/proxyReqWs, (b) sec-websocket-extensions strip on proxyReqWs, (c) emitHeaderAudit call on both
- header-audit-sampler.ts emits info (100% first hour, 1% after) + warn on any header outside allowlist reaching upstream
- Scoped tests green; tsc clean
</success_criteria>

<output>
Create `.planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-03b-SUMMARY.md` when done
</output>
