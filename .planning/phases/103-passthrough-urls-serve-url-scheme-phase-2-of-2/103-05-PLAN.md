---
phase: 103-passthrough-urls-serve-url-scheme-phase-2-of-2
plan: 05
type: execute
wave: 4
depends_on:
  - 103-02
  - 103-03b
files_modified:
  - src/backend/serve-url/subdomain-dispatch.ts
  - src/backend/serve-url/serve-route.ts
  - src/backend/database/database.ts
autonomous: true
requirements: []
tags:
  - subdomain-dispatch
  - reverse-proxy
  - jwt-auth
  - host-resolution
  - interstitial-wiring
must_haves:
  truths:
    - "src/backend/serve-url/subdomain-dispatch.ts exports createSubdomainDispatchMiddleware() that reads req.headers['x-skynet-serve-subdomain']; if absent → next() (falls through to Skynet frontend serving per PATTERNS.md 'Fallback (unknown / no subdomain)')"
    - "The middleware parses `<hostname>-<port>` per D-11: split on LAST dash of leftmost DNS label, right side must be all-digits (port), everything left is hostname; parse failure → renders an interstitial (planner may map to port_not_listening class OR add a dedicated invalid_url variant — either acceptable)"
    - "The middleware lowercases hostname BEFORE calling resolveHostByName per D-13 ('lookup uses LOWER(hostname); display case preserved in DB'). Pre-lowercase at dispatch, do NOT modify src/backend/ssh/host-resolver.ts."
    - "The middleware runs the JWT auth check via AuthManager.getInstance().createAuthMiddleware() BEFORE resolveHostByName — 401 without cookie → renders interstitial with errorClass='auth_missing' (which per interstitial.ts returns 302 redirect to primary /login?return=<original>)"
    - "The middleware runs permissionManager.canAccessHost(userId, host.id, 'read') per D-03 + D-17; on !hasAccess → interstitial errorClass='permission_denied' (403)"
    - "src/backend/serve-url/serve-route.ts exports serveUrlHandler(req, res, next) that calls tunnelCache.getOrCreate(target) and then getOrCreateProxyForTarget(target, tunnelPort) and invokes the resulting middleware on (req, res, next); on tunnel errors → renders interstitial with errorClass mapped from the error taxonomy (ssh_failure for SSH-level errors, port_not_listening for ECONNREFUSED, host_unreachable for ETIMEDOUT/EHOSTUNREACH/ENETUNREACH)"
    - "serve-route.ts calls existing resolveHostByName(hostname.toLowerCase(), userId) per D-17 owned-only pattern from Phase 78 (via subdomain-dispatch — serve-route consumes the already-resolved target on req.serveTarget)"
    - "src/backend/database/database.ts mounts subdomainDispatchMiddleware TOP-OF-STACK BEFORE bodyParser (per http-proxy-middleware v4 streaming semantics — dispatch MUST run before body-consuming middleware) AND AFTER cookieParser (so JWT cookie parses correctly)"
    - "serveUrlHandler is mounted right AFTER subdomainDispatchMiddleware in the middleware chain — both are app.use() top-of-stack; subdomain-dispatch attaches req.serveTarget then next(), serve-route consumes it"
    - "src/backend/serve-url/subdomain-dispatch.ts AND src/backend/serve-url/serve-route.ts THROW at module-load (or on factory invocation) if process.env.SKYNET_COOKIE_DOMAIN is unset — no fallback to hardcoded 'term.gigaashley.click' per D-23 (T800 sets its own value); fail loud, not silent-wrong"
    - "grep -r 'term\\.gigaashley\\.click' src/backend/serve-url/ returns 0 matches — no literal domain string appears in serve-url source (env-driven per D-23)"
    - "Scoped tests pass: `npx vitest run src/backend/serve-url/ --exclude='**/*.integration.test.ts'` exits 0 (0 failures — integration tests handled separately by Plan 04)"
    - "TypeScript compiles cleanly: `npx tsc --noEmit` exits 0"
  artifacts:
    - path: "src/backend/serve-url/subdomain-dispatch.ts"
      provides: "Express middleware factory that reads X-Skynet-Serve-Subdomain, parses per D-11, runs JWT + perm checks, dispatches to serveUrlHandler or renders interstitial"
      exports: ["createSubdomainDispatchMiddleware"]
    - path: "src/backend/serve-url/serve-route.ts"
      provides: "serveUrlHandler(req, res, next) that composes tunnel-cache + proxy-factory + error → interstitial mapping"
      exports: ["serveUrlHandler"]
    - path: "src/backend/database/database.ts"
      provides: "Mounts createSubdomainDispatchMiddleware() then serveUrlHandler top-of-stack, after cookieParser and BEFORE bodyParser (per http-proxy-middleware v4 raw-body streaming semantics)"
      contains: "createSubdomainDispatchMiddleware"
  key_links:
    - from: "src/backend/serve-url/subdomain-dispatch.ts"
      to: "src/backend/utils/auth-manager.ts"
      via: "AuthManager.getInstance().createAuthMiddleware()"
      pattern: "AuthManager\\.getInstance"
    - from: "src/backend/serve-url/subdomain-dispatch.ts"
      to: "src/backend/utils/permission-manager.ts"
      via: "PermissionManager.getInstance().canAccessHost"
      pattern: "canAccessHost"
    - from: "src/backend/serve-url/subdomain-dispatch.ts"
      to: "src/backend/ssh/host-resolver.ts"
      via: "resolveHostByName(hostname.toLowerCase(), userId)"
      pattern: "resolveHostByName\\("
    - from: "src/backend/serve-url/serve-route.ts"
      to: "src/backend/serve-url/tunnel-cache.ts"
      via: "tunnelCache.getOrCreate(target)"
      pattern: "tunnelCache\\.getOrCreate"
    - from: "src/backend/serve-url/serve-route.ts"
      to: "src/backend/serve-url/proxy-factory.ts"
      via: "getOrCreateProxyForTarget(target, tunnelPort)"
      pattern: "getOrCreateProxyForTarget"
    - from: "src/backend/database/database.ts"
      to: "src/backend/serve-url/subdomain-dispatch.ts"
      via: "app.use(createSubdomainDispatchMiddleware()) — mounted AFTER cookieParser + BEFORE bodyParser"
      pattern: "createSubdomainDispatchMiddleware"
---

<objective>
Compose the serve URL request path: (1) subdomain-dispatch middleware that reads Caddy's X-Skynet-Serve-Subdomain header, parses hostname/port per D-11, runs JWT + permission checks, and dispatches OR renders interstitial; (2) serveUrlHandler that composes tunnel-cache + proxy-factory + error→interstitial mapping; (3) mount both at the top of database.ts's middleware stack — AFTER cookieParser but BEFORE bodyParser per http-proxy-middleware v4 streaming semantics. Realizes D-03, D-11, D-13, D-14, D-17, D-23.

Purpose: turns Plan 03a/03b's isolated primitives into a functioning end-to-end request path. Anything sent to *.serve.term.<domain> (via Caddy's header_up) either proxies through SSH tunnel to the agent's port OR renders a classified interstitial. Requests without the header pass through untouched to Skynet's existing routes.

Output: 2 new files (subdomain-dispatch.ts, serve-route.ts) + 1 modification (database.ts mount).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-CONTEXT.md
@.planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-PATTERNS.md
@src/backend/serve-url/types.ts
@src/backend/serve-url/tunnel-cache.ts
@src/backend/serve-url/proxy-factory.ts
@src/backend/serve-url/interstitial.ts
@src/backend/utils/auth-manager.ts
@src/backend/utils/permission-manager.ts
@src/backend/ssh/host-resolver.ts
@src/backend/database/routes/pretty-view-fetch-host-file.ts
@src/backend/database/database.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create subdomain-dispatch.ts middleware (env-driven primaryDomain, fail-loud)</name>
  <files>src/backend/serve-url/subdomain-dispatch.ts</files>
  <read_first>
    - src/backend/utils/cors-config.ts (middleware factory pattern per PATTERNS.md §subdomain-dispatch.ts analog)
    - src/backend/utils/auth-manager.ts lines 700-750 (createAuthMiddleware + getSecureCookieOptions Plan 02 modification)
    - src/backend/utils/permission-manager.ts (canAccessHost signature)
    - src/backend/ssh/host-resolver.ts lines 370-395 (resolveHostByName)
    - src/backend/database/routes/pretty-view-fetch-host-file.ts (Phase 78 analog for auth+resolve+permission sequence L108-113, L200-240)
    - src/backend/serve-url/interstitial.ts (from Plan 03a — renderInterstitial + writeInterstitial)
    - src/backend/serve-url/types.ts (from Plan 03a)
    - .planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-PATTERNS.md §src/backend/serve-url/subdomain-dispatch.ts
    - .planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-CONTEXT.md D-03, D-11, D-13, D-14, D-17, D-23
  </read_first>
  <behavior>
    - When req.headers['x-skynet-serve-subdomain'] absent → next() with no args (fall-through to Skynet frontend serving)
    - When header present but parse fails (missing dash, right side non-digit, port out of range) → renderInterstitial variant + write response
    - When parse succeeds but JWT missing/expired → renderInterstitial('auth_missing', ...) → 302 redirect
    - When JWT valid but resolveHostByName returns null → renderInterstitial('host_unreachable', ...) — do NOT reveal "you have no such host" per info-leak invariant; presenting as offline is acceptable per user-friendliness
    - When host resolved but canAccessHost returns !hasAccess → renderInterstitial('permission_denied', ...)
    - When all checks pass → attaches ServeTarget to req (e.g. `(req as any).serveTarget = target`) and next() so Task 2's serveUrlHandler consumes it
    - Module load / factory invocation with process.env.SKYNET_COOKIE_DOMAIN unset → THROWS (fail loud, not silent-wrong) — no hardcoded 'term.gigaashley.click' fallback per D-23
  </behavior>
  <action>
    Create src/backend/serve-url/subdomain-dispatch.ts per PATTERNS.md §subdomain-dispatch.ts.

    Import AuthManager from '../utils/auth-manager.js', PermissionManager from '../utils/permission-manager.js', resolveHostByName from '../ssh/host-resolver.js', renderInterstitial + writeInterstitial from './interstitial.js', types (ServeTarget, ErrorClass) from './types.js', sshLogger from '../utils/logger.js'.

    Export `createSubdomainDispatchMiddleware()` factory. At the TOP of the factory body, read `const primaryDomain = process.env.SKYNET_COOKIE_DOMAIN; if (!primaryDomain) throw new Error('serve-url subdomain-dispatch: SKYNET_COOKIE_DOMAIN env var is required (per D-23; t1000 sets term.gigaashley.click, T800 sets its own value)');` — FAIL LOUD per W4. Do NOT default to 'term.gigaashley.click' or any other literal. Do NOT wrap the throw in try/catch. If the env is missing, Skynet MUST refuse to boot cleanly (better than silently accepting all traffic under a wrong domain).

    The factory returns an Express middleware `(req, res, next) => { ... }` that:

    1. Read `const subdomainHeader = req.headers['x-skynet-serve-subdomain'];`. If undefined or Array → next() (fall through).

    2. Parse per D-11: `const label = String(subdomainHeader).split('.')[0]; const lastDash = label.lastIndexOf('-');` — if lastDash <= 0 or lastDash === label.length - 1 → render + write parse-failure interstitial + return. Then `const portStr = label.slice(lastDash + 1);` — if !/^\d+$/.test(portStr) → interstitial + return. Then `const port = Number(portStr);` — if !Number.isFinite(port) or port <= 0 or port > 65535 → interstitial + return. Then `const hostname = label.slice(0, lastDash);`.

    3. Run JWT: adapt AuthManager.getInstance().createAuthMiddleware() invocation — call it with (req, res, callback) where callback is a Promise-resolving shim; if the auth middleware wrote to res (e.g. 401) → intercept OR wrap res so we render our own 'auth_missing' interstitial (302 to /login?return=). If it called next() cleanly → req.userId is populated → proceed. See src/backend/database/routes/pretty-view-fetch-host-file.ts L108-113 for the AuthManager singleton pattern.

    4. `const host = await resolveHostByName(hostname.toLowerCase(), req.userId!);` per D-13 pre-lowercase at dispatch. If host is nullish → renderInterstitial('host_unreachable', {hostname, port, host: <stub>}, originalUrl, primaryDomain).

    5. `const accessInfo = await PermissionManager.getInstance().canAccessHost(req.userId!, host.id, 'read');` per D-03 (planner picks 'read' per PATTERNS.md guidance). If !accessInfo.hasAccess → renderInterstitial('permission_denied', target, originalUrl, primaryDomain).

    6. All checks pass: attach `(req as any).serveTarget = { hostname: host.name, port, host };` (use canonical host.name from DB — preserves display case per D-13) and next().

    Use writeInterstitial helper from ./interstitial.js (added in Plan 03a Task 3) to shape the response — DRY across dispatch + serve-route.

    Structured logs (info-leak safe): on parse failure `sshLogger.warn('serve-url dispatch: parse-failed', { operation: 'serve_url_dispatch_parse_failed', subdomainLen: String(subdomainHeader).length })`; on unknown host `sshLogger.info('serve-url dispatch: unknown-host', { operation: 'serve_url_dispatch_unknown_host', hostname, port })`; on permission deny `sshLogger.info('serve-url dispatch: permission-denied', { operation: 'serve_url_dispatch_permission_denied', hostname, port, userId: req.userId })`.

    Compute originalUrl for redirects: `const originalUrl = \`https://\${subdomainHeader}\${req.originalUrl}\`;`.

    Info-leak invariant (T-40-05): NEVER include the raw subdomain string, err.message, or stack in response body. Only classified sentence + hostname + port.
  </action>
  <verify>
    <automated>test -f src/backend/serve-url/subdomain-dispatch.ts &amp;&amp; grep -q 'x-skynet-serve-subdomain' src/backend/serve-url/subdomain-dispatch.ts &amp;&amp; grep -q 'resolveHostByName' src/backend/serve-url/subdomain-dispatch.ts &amp;&amp; grep -q 'canAccessHost' src/backend/serve-url/subdomain-dispatch.ts &amp;&amp; [ "$(grep -c renderInterstitial src/backend/serve-url/subdomain-dispatch.ts)" -ge 4 ] &amp;&amp; grep -q '\.toLowerCase()' src/backend/serve-url/subdomain-dispatch.ts &amp;&amp; grep -q 'SKYNET_COOKIE_DOMAIN' src/backend/serve-url/subdomain-dispatch.ts &amp;&amp; ! grep -q 'term\.gigaashley\.click' src/backend/serve-url/subdomain-dispatch.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: subdomain-dispatch.ts imports AuthManager, PermissionManager, resolveHostByName, renderInterstitial, writeInterstitial, types
    - Source: file contains parse logic checking `lastIndexOf('-')` and `/^\d+$/.test(portStr)` per D-11
    - Source: file contains `hostname.toLowerCase()` before calling resolveHostByName (per D-13)
    - Source: at least 4 distinct renderInterstitial calls exist (auth_missing, host_unreachable, permission_denied, parse-failure)
    - Source: fall-through path (missing header) calls next() with NO arguments
    - Source: successful-dispatch path attaches serveTarget to req and calls next() (no arguments)
    - Source: `grep -c 'sshLogger\.\(info\|warn\)' src/backend/serve-url/subdomain-dispatch.ts` returns ≥ 3
    - Source: `grep -cE 'err\.message|error\.message|\.stack' src/backend/serve-url/subdomain-dispatch.ts` returns 0 (info-leak invariant)
    - Source: file contains `process.env.SKYNET_COOKIE_DOMAIN` reference AND a throw statement adjacent to it (fail-loud pattern per W4/D-23)
    - Source: `grep -c 'term\.gigaashley\.click' src/backend/serve-url/subdomain-dispatch.ts` returns 0 (no hardcoded domain fallback)
    - Behavior: `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>subdomain-dispatch.ts middleware handles the four gate stages (parse → JWT → resolve → permission) with interstitial-per-failure and attaches ServeTarget on success. Fails loud at factory invocation if SKYNET_COOKIE_DOMAIN unset — no hardcoded domain fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create serve-route.ts (env-driven primaryDomain, fail-loud, compose tunnel + proxy)</name>
  <files>src/backend/serve-url/serve-route.ts</files>
  <read_first>
    - src/backend/serve-url/subdomain-dispatch.ts (Task 1 — attaches serveTarget to req)
    - src/backend/serve-url/tunnel-cache.ts (from Plan 03a — getOrCreate)
    - src/backend/serve-url/proxy-factory.ts (from Plan 03b — getOrCreateProxyForTarget)
    - src/backend/serve-url/interstitial.ts (from Plan 03a — renderInterstitial + writeInterstitial)
    - src/backend/serve-url/types.ts (from Plan 03a — ErrorClass, ServeTarget)
    - src/backend/database/routes/pretty-view-fetch-host-file.ts (classifyErrorToStatus L474-514 pattern per PATTERNS.md)
    - .planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-PATTERNS.md §src/backend/serve-url/serve-route.ts + §proxy-factory.ts
    - .planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-CONTEXT.md D-14, D-15, D-23
  </read_first>
  <behavior>
    - serveUrlHandler(req, res, next) called after subdomain-dispatch attaches (req as any).serveTarget
    - Attempts tunnelCache.getOrCreate(target) — on ECONNREFUSED → renderInterstitial('port_not_listening', ...) with 502; on ETIMEDOUT/EHOSTUNREACH/ENETUNREACH → 'host_unreachable'; on SSH auth/handshake error → 'ssh_failure'
    - On tunnel success, calls getOrCreateProxyForTarget(target, tunnelPort) and invokes the middleware on (req, res, next) — the http-proxy-middleware handles HTTP + WS from there
    - Structured log emission: sshLogger.info('serve-url proxy: ok', { operation, target, status, duration }) on res.on('finish'); sshLogger.warn('serve-url proxy: error', { operation, target, errorClass, duration }) on failure paths
    - MUST NOT re-run auth or permission checks (subdomain-dispatch already did them per D-03)
    - Module load — read primaryDomain from process.env.SKYNET_COOKIE_DOMAIN; if unset, THROW (fail loud per W4/D-23); no hardcoded fallback
  </behavior>
  <action>
    Create src/backend/serve-url/serve-route.ts per PATTERNS.md §serve-route.ts (adapted — this handler runs AFTER subdomain-dispatch, so no auth wiring inside; it consumes req.serveTarget).

    At module top-level (or first-invocation lazy-load — either works), read `const primaryDomain = process.env.SKYNET_COOKIE_DOMAIN; if (!primaryDomain) throw new Error('serve-url serve-route: SKYNET_COOKIE_DOMAIN env var is required (per D-23)');` — FAIL LOUD per W4. Do NOT default to 'term.gigaashley.click' or any other literal. Same source-of-truth as subdomain-dispatch — if a shared getPrimaryDomain() helper feels cleaner, hoist to a small config module (e.g. src/backend/serve-url/config.ts) — planner allows either shape, single-throw-point preferred.

    Export `serveUrlHandler(req, res, next): Promise<void>`:

    1. `const target = (req as any).serveTarget as ServeTarget | undefined; if (!target) return next();` (defensive fall-through if dispatch didn't attach).

    2. `const startEpoch = Date.now();`

    3. `let tunnelPort: number;` in a try/catch around `tunnelCache.getOrCreate(target)`. On catch: classify via helper `classifyTunnelError(err): ErrorClass` — ECONNREFUSED → 'port_not_listening'; ETIMEDOUT/EHOSTUNREACH/ENETUNREACH → 'host_unreachable'; ssh2 error (err.level === 'client-authentication' or err.code startsWith 'SSH_' or err.name === 'ClientError') → 'ssh_failure'; fallback → 'ssh_failure'. Log warn (info-leak safe: no err.message in log message). Render interstitial via renderInterstitial + writeInterstitial (from Plan 03a) and return.

    4. On tunnel success: `const proxyMiddleware = getOrCreateProxyForTarget(target, tunnelPort);` then `proxyMiddleware(req, res, next);` — the middleware handles HTTP + WS upgrade + streaming.

    5. Register success log via `res.on('finish', () => sshLogger.info('serve-url proxy: ok', { operation: 'serve_url_proxy', target: \`\${target.hostname}:\${target.port}\`, status: res.statusCode, duration: Date.now() - startEpoch }))`. Register the listener BEFORE invoking proxyMiddleware so it survives the async proxy dispatch.

    Do NOT re-invoke AuthManager or PermissionManager here. Do NOT wire as an Express router with router.get/post — it's a plain middleware function.

    Info-leak: classifyTunnelError NEVER includes err.message in log OUTPUT — only { errorClass, target, err.code (structured only) } in structured context.
  </action>
  <verify>
    <automated>test -f src/backend/serve-url/serve-route.ts &amp;&amp; grep -q 'tunnelCache.getOrCreate' src/backend/serve-url/serve-route.ts &amp;&amp; grep -q 'getOrCreateProxyForTarget' src/backend/serve-url/serve-route.ts &amp;&amp; grep -q 'ECONNREFUSED' src/backend/serve-url/serve-route.ts &amp;&amp; [ "$(grep -cE 'port_not_listening|host_unreachable|ssh_failure' src/backend/serve-url/serve-route.ts)" -ge 3 ] &amp;&amp; [ "$(grep -cE 'err\.message|error\.message' src/backend/serve-url/serve-route.ts)" -eq 0 ] &amp;&amp; grep -q 'SKYNET_COOKIE_DOMAIN' src/backend/serve-url/serve-route.ts &amp;&amp; ! grep -q 'term\.gigaashley\.click' src/backend/serve-url/serve-route.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: serve-route.ts calls tunnelCache.getOrCreate(target)
    - Source: serve-route.ts calls getOrCreateProxyForTarget(target, tunnelPort)
    - Source: file contains all three error class strings: port_not_listening, host_unreachable, ssh_failure
    - Source: file contains ECONNREFUSED literal (used in classifyTunnelError)
    - Source: file contains res.on('finish', ...) success log wiring
    - Source: `grep -cE 'err\.message|error\.message' src/backend/serve-url/serve-route.ts` returns 0 (info-leak invariant)
    - Source: file does NOT invoke AuthManager or PermissionManager (grep both, returns 0 — subdomain-dispatch owns those)
    - Source: file references process.env.SKYNET_COOKIE_DOMAIN AND throws if unset (fail-loud per W4/D-23) — either directly or via a shared config helper that does the same
    - Source: `grep -c 'term\.gigaashley\.click' src/backend/serve-url/serve-route.ts` returns 0 (no hardcoded domain fallback)
    - Behavior: `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>serve-route.ts composes tunnel + proxy + error-class mapping; res.on('finish') success log; no auth re-runs. Fails loud at module load if SKYNET_COOKIE_DOMAIN unset; no hardcoded fallback.</done>
</task>

<task type="auto">
  <name>Task 3: Mount subdomain-dispatch + serve-route in database.ts — AFTER cookieParser, BEFORE bodyParser</name>
  <files>src/backend/database/database.ts</files>
  <read_first>
    - src/backend/database/database.ts lines 50-300 (import block, cookieParser mount, createCorsMiddleware mount, body-parser mount)
    - src/backend/database/database.ts lines 1850-1910 (existing app.use('/users', ...) etc. router mounts — the SERVE URL dispatch must come BEFORE these)
    - src/backend/serve-url/subdomain-dispatch.ts (Task 1)
    - src/backend/serve-url/serve-route.ts (Task 2)
    - .planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-PATTERNS.md §subdomain-dispatch.ts "Mount point"
    - .planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-CONTEXT.md D-24
    - https://github.com/chimurai/http-proxy-middleware — mount order guidance (v4 docs: "http-proxy-middleware streams the raw request body to the upstream — if bodyParser has already consumed the body, POST bodies get truncated")
  </read_first>
  <action>
    Modify src/backend/database/database.ts. Add two imports near existing route imports (around lines 50-60): `import { createSubdomainDispatchMiddleware } from '../serve-url/subdomain-dispatch.js';` and `import { serveUrlHandler } from '../serve-url/serve-route.js';`.

    **Mount order (DETERMINISTIC — do NOT try both orders and see what works):**

    Per http-proxy-middleware v4 documentation, http-proxy-middleware streams the raw request body to the upstream target. If bodyParser (express.json / bodyParser.urlencoded / bodyParser.raw) has already CONSUMED the request stream to produce req.body, http-proxy-middleware has nothing left to forward — POST bodies get truncated to zero bytes. This is not a "try it and see" question; it is a documented library invariant.

    **Therefore:** subdomain-dispatch + serve-route MUST mount BEFORE any bodyParser middleware in the app.use() chain.

    Final mount sequence (planner-decided per http-proxy-middleware v4 semantics):

    1. Existing createCorsMiddleware() (line ~144) — STAYS at its current position (CORS runs first for every request; Plan 02 D-07 modification handles serve-subdomain reject)
    2. Existing cookieParser (line ~288) — MOVE UP so it runs BEFORE bodyParser AND before subdomain-dispatch (cookieParser is header-only, does NOT consume the request body — safe to run before dispatch, required because dispatch needs req.cookies for JWT)
    3. NEW: `app.use(createSubdomainDispatchMiddleware());` — mount IMMEDIATELY AFTER cookieParser
    4. NEW: `app.use(serveUrlHandler);` — mount IMMEDIATELY AFTER subdomain-dispatch
    5. Existing bodyParser.json / bodyParser.urlencoded / bodyParser.raw (currently lines 285-287) — MOVE DOWN to run AFTER serveUrlHandler. Non-serve-URL requests fall through subdomain-dispatch's `next()` and hit bodyParser normally. Serve-URL requests never reach bodyParser (either terminated by dispatch or absorbed by serveUrlHandler → proxy middleware).
    6. Existing app.use((_req, res, next) => { ... }) response header middleware (line ~289) STAYS in current relative position (after bodyParser)

    **Rationale for the mount order (record in a code comment above the dispatch mount):**
    ```
    // Phase 103 D-24 mount order: subdomain-dispatch + serveUrlHandler MUST run
    // BEFORE express.json/bodyParser/bodyParser.raw — http-proxy-middleware v4
    // streams the raw request body to the upstream target, so any middleware that
    // consumes the request stream (bodyParser.*) upstream of the proxy truncates
    // POST bodies to zero bytes. cookieParser runs before dispatch because it
    // reads req.headers.cookie (header-only, no body consumption) and dispatch
    // needs req.cookies to run the JWT check.
    ```

    Per D-24 single deploy: this is part of the same ship motion as Plan 01 Caddyfile snippet + Plan 02 cookie widen. Executor does NOT deploy — orchestrator handles ship.

    Do NOT touch any existing app.use('/users', ...) etc. router mounts (lines 1850+). Do NOT touch imports for existing routes.
  </action>
  <verify>
    <automated>grep -c 'createSubdomainDispatchMiddleware' src/backend/database/database.ts | awk '{ if ($1 &gt;= 2) exit 0; else exit 1 }' &amp;&amp; grep -c 'serveUrlHandler' src/backend/database/database.ts | awk '{ if ($1 &gt;= 2) exit 0; else exit 1 }' &amp;&amp; DISPATCH_LINE=$(grep -n 'app\.use(createSubdomainDispatchMiddleware' src/backend/database/database.ts | head -1 | cut -d: -f1) &amp;&amp; BODYPARSER_LINE=$(grep -n 'app\.use(bodyParser\|app\.use(express\.json\|app\.use(express\.urlencoded' src/backend/database/database.ts | head -1 | cut -d: -f1) &amp;&amp; COOKIEPARSER_LINE=$(grep -n 'app\.use(cookieParser' src/backend/database/database.ts | head -1 | cut -d: -f1) &amp;&amp; [ -n "$DISPATCH_LINE" ] &amp;&amp; [ -n "$BODYPARSER_LINE" ] &amp;&amp; [ -n "$COOKIEPARSER_LINE" ] &amp;&amp; [ "$COOKIEPARSER_LINE" -lt "$DISPATCH_LINE" ] &amp;&amp; [ "$DISPATCH_LINE" -lt "$BODYPARSER_LINE" ] &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: src/backend/database/database.ts contains `import { createSubdomainDispatchMiddleware }` from serve-url module
    - Source: src/backend/database/database.ts contains `import { serveUrlHandler }` from serve-url module
    - Source: src/backend/database/database.ts contains exactly one `app.use(createSubdomainDispatchMiddleware())` line
    - Source: src/backend/database/database.ts contains exactly one `app.use(serveUrlHandler)` line
    - Source: line-order check via automated grep: cookieParser line-number < createSubdomainDispatchMiddleware line-number < any bodyParser/express.json/express.urlencoded line-number
    - Source: a code comment adjacent to the dispatch mount explains the http-proxy-middleware v4 raw-body streaming rationale for the mount order (grep for either "http-proxy-middleware" or "raw request body" or "truncates POST" in comments near the mount)
    - Source: existing app.use('/users', ...), app.use('/host', ...), etc. router mounts (lines ~1850+) are UNCHANGED
    - Behavior: `npx tsc --noEmit` exits 0
    - Behavior: `npx vitest run src/backend/serve-url/ --exclude='**/*.integration.test.ts'` exits 0
  </acceptance_criteria>
  <done>database.ts mounts subdomain-dispatch + serve-route AFTER cookieParser and BEFORE bodyParser (deterministic per http-proxy-middleware v4 streaming semantics); rationale documented in comment; existing routes untouched; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caddy → Skynet backend via X-Skynet-Serve-Subdomain header | Semi-trusted internal; Caddy strips client-controlled value and sets its own via `header_up` per Plan 01 |
| Browser → serve subdomain → backend | Untrusted; JWT cookie carries session state (per Plan 02 widened Domain) |
| Serve subdomain → primary term.<domain> (state-changing endpoints) | CSRF vector via widened cookie; mitigated by Plan 02 CORS reject + Plan 08 audit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-22 | Spoofing | Client-supplied X-Skynet-Serve-Subdomain | mitigate | Caddy `header_up X-Skynet-Serve-Subdomain {host}` overwrites (per Plan 01) — Caddy is trusted for this header. Backend does no additional validation because if Caddy is bypassed the attacker is already inside the Docker network. |
| T-103-23 | Information disclosure | Response body leaking hostname existence (unknown host vs offline) | mitigate | subdomain-dispatch maps null resolveHostByName result to `host_unreachable` interstitial — same body as an actually-offline host. Attacker cannot enumerate registered hosts by response fingerprint. Info-leak invariant T-40-05. |
| T-103-24 | Elevation of privilege | Missing JWT check | mitigate | AuthManager.getInstance().createAuthMiddleware() run BEFORE resolveHostByName. Test verify: unauthenticated request returns 302 to /login (auth_missing interstitial) not the proxy response. |
| T-103-25 | Tampering | Order-of-operations bug — permission check bypassed by early exception | mitigate | try/catch around each stage; failure at any stage renders interstitial. Structured logs at each stage boundary give auditability. |
| T-103-26 | Denial of service | Malformed subdomain header triggering expensive parse | accept | Parse is O(len) string ops on a single DNS label (typically < 100 chars); no regex backtracking risk. Node's split/lastIndexOf are trivial cost. |
| T-103-27 | Information disclosure | Raw subdomain string logged/echoed | mitigate | Parse-failure log emits { subdomainLen } not the full string. Interstitial bodies contain only parsed hostname+port, not raw subdomain. |
| T-103-28 | Repudiation | Missing audit trail on dispatch failures | mitigate | sshLogger.warn/info at parse-failed, unknown-host, permission-denied with { userId, hostname, port, operation } structured context. |
| T-103-45 | Tampering | Silent-wrong domain via hardcoded fallback | mitigate | subdomain-dispatch + serve-route THROW at module load / factory invocation if SKYNET_COOKIE_DOMAIN unset (per W4/D-23). Fail loud, not silent-wrong; T800 sets its own value, t1000 sets term.gigaashley.click. No literal domain string in serve-url source. |
| T-103-46 | Tampering | POST body truncation via wrong middleware mount order | mitigate | Dispatch + serveUrlHandler mount BEFORE bodyParser per http-proxy-middleware v4 raw-body streaming semantics (documented library invariant). Mount order enforced by automated line-number check in acceptance_criteria. |
</threat_model>

<verification>
- `npx vitest run src/backend/serve-url/ --exclude='**/*.integration.test.ts'` exits 0
- `npx tsc --noEmit` exits 0
- Line ordering enforced by acceptance_criteria grep (cookieParser < dispatch < bodyParser)
- New files created: subdomain-dispatch.ts, serve-route.ts
- database.ts modified only for the two new mounts + moved bodyParser lines (grep-check preserves existing routers)
- grep -r 'term.gigaashley.click' src/backend/serve-url/ returns 0 matches
</verification>

<success_criteria>
- subdomain-dispatch parses D-11 grammar, runs JWT + permission checks, renders per-failure-class interstitials, attaches ServeTarget on success
- serve-route composes tunnel + proxy + error mapping without re-running auth
- database.ts mounts both AFTER cookieParser and BEFORE bodyParser (per http-proxy-middleware v4 semantics); mount order rationale in code comment
- Info-leak invariant enforced (no err.message in bodies or logs)
- No hardcoded 'term.gigaashley.click' in serve-url source — env-driven per D-23; module throws if env unset (fail-loud, not silent-wrong)
- Scoped tests green; tsc clean
</success_criteria>

<output>
Create `.planning/phases/103-passthrough-urls-serve-url-scheme-phase-2-of-2/103-05-SUMMARY.md` when done
</output>
