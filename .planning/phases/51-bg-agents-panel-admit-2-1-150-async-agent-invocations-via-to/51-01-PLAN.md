---
phase: 51-bg-agents-panel-admit-2-1-150-async-agent-invocations-via-to
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/claude-session/claude-session-server.ts
  - src/backend/claude-session/claude-session-server.bg-agents-async-ack.test.ts
autonomous: true
requirements:
  - BG-AGENTS-51-ADMIT-ASYNC-ACK
  - BG-AGENTS-51-PRESERVE-LEGACY-COMPAT
  - BG-AGENTS-51-NO-BASH-REGRESSION
tags:
  - claude-session-server
  - backgrounded-agents
  - pretty-view
  - claude-code-v2.1.150

must_haves:
  truths:
    - "Modern (v2.1.150+) async Agent tool_use — input keys [description, prompt, subagent_type], no run_in_background — is admitted into backgroundedAgents on the tool_result async-launch-ack (toolUseResult.isAsync === true)"
    - "Modern SYNCHRONOUS Agent tool_use (no run_in_background, first tool_result is a normal completion with no isAsync) is NEVER admitted into backgroundedAgents (scratch entry silently dropped)"
    - "Legacy Agent tool_use (input.run_in_background === true) is still admitted immediately at tool_use time (backward compat), and remains in backgroundedAgents across the isAsyncAck skip-guard until real completion"
    - "Full async lifecycle (modern shape): tool_use → async-ack promotes → real completion tool_result removes; intermediate + final snapshots verified"
    - "Bash{run_in_background:true} branch behavior is unchanged (empirically still legacy shape on v2.1.150 per RESEARCH.md § 'Bash shape on v2.1.150')"
    - "Full test suite (npx vitest run) exits 0 at plan end, at baseline + 4 new tests"
  artifacts:
    - path: "src/backend/claude-session/claude-session-server.ts"
      provides: "Updated backgrounded_agents correlator with pendingAgentAdmission scratch map + dual-path admission (legacy input.run_in_background + modern isAsync ack)"
      contains: "pendingAgentAdmission"
    - path: "src/backend/claude-session/claude-session-server.bg-agents-async-ack.test.ts"
      provides: "Four fixture-based tests (Fixture A/B/C/D) exercising the four admission paths"
      contains: "toolu_A1"
  key_links:
    - from: "src/backend/claude-session/claude-session-server.ts"
      to: "pendingAgentAdmission scratch map"
      via: "closure-local Map alongside backgroundedAgents (declared near L2143)"
      pattern: "const pendingAgentAdmission"
    - from: "src/backend/claude-session/claude-session-server.ts async-ack branch (~L2603)"
      to: "backgroundedAgents.set() promotion"
      via: "for-loop over tool_result blocks in the isAsyncAck === true branch"
      pattern: "pendingAgentAdmission.get\\(.*tool_use_id"
    - from: "src/backend/claude-session/claude-session-server.ts non-async tool_result branch (~L2607)"
      to: "pendingAgentAdmission.delete()"
      via: "same loop that already runs backgroundedAgents.delete()"
      pattern: "pendingAgentAdmission\\.delete"
---

<objective>
Fix the `backgrounded_agents` correlator in `src/backend/claude-session/claude-session-server.ts` so it admits Claude Code v2.1.150+ async `Agent` invocations (which no longer carry `input.run_in_background === true` — the async signal moved to `toolUseResult.isAsync === true` on the tool_result launch-ack). Preserve backward compat with the legacy admission path (older Claude Code, or any future harness that reintroduces the flag). Add four fixture-based tests (Fixture A/B/C/D per 51-RESEARCH.md § "Test Plan").

Purpose: BG-agents panel currently silently rejects modern async Agent invocations (Bug 1 of bounty `claude-code-2-1-214-pretty-view-compat`). Ashley observed the failure live watching taylor's `gsd-executor` sub-agent — the Task tool_use is in the parent JSONL but doesn't render in the panel. This plan restores parity across the v2.1.149 → v2.1.150 shape change.

Output:
  - Updated correlator with scratch-map + dual-path admission
  - Four new tests covering async-ack admission, sync-scratch-drop, legacy backward-compat, full lifecycle
  - `npx vitest run` exits 0 at baseline + 4 new tests
  - `npm run build:backend` + `npm run build` exit 0
  - NO Bash-branch touches (empirically still legacy shape on v2.1.150+ per RESEARCH.md)
  - NO frontend / UI / nginx / docker / push touches (fleet directive — executor scope stops at code + commit + tests green)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-bg-agents-panel-admit-2-1-150-async-agent-invocations-via-to/51-CONTEXT.md
@.planning/phases/51-bg-agents-panel-admit-2-1-150-async-agent-invocations-via-to/51-RESEARCH.md
</context>

<fleet_directives>
1. NO WORKTREES. All work on main tree at `/home/ubuntu/skynet-tabitha` (branch `feat/tab-title-from-tmux`).
2. Executor scope STOPS at code + commit + tests green. NO `git push`, NO `docker build`, NO `docker compose up`, NO coord-room posts, NO `skynet-patches.md` bookkeeping. Orchestrator (tabitha) picks up deploy.
3. Never mask exit codes with `| tail` / `| head` on build/test commands. Redirect to a log file, capture `$?`, then Read the log.
4. Never leave tests failing. `npx vitest run` MUST exit 0 at plan end.
5. Use absolute paths in bash — cwd does NOT persist between Bash calls.
6. `npm run build:backend` is the strict backend typecheck for any `src/backend/**` touch (not `npx tsc --noEmit`).
7. Multi-identity branch — NO rebase, NO force-push, NO `git pull --rebase` during execution.
8. TDD RED+GREEN pattern for the parser fix (fixtures first, then implementation).
</fleet_directives>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Create four failing fixture-based tests in claude-session-server.bg-agents-async-ack.test.ts</name>

  <read_first>
    - /home/ubuntu/skynet-tabitha/src/backend/claude-session/claude-session-server.ts (whole file too large — grep for `backgroundedAgents` first at lines 2143, 2506-2620; understand the closure structure and the existing skip-guard at L2603)
    - /home/ubuntu/skynet-tabitha/src/backend/claude-session/claude-session-server.malformed-eventid.test.ts (existing test conventions — `__forTests` export pattern, describe/it/expect from vitest)
    - /home/ubuntu/skynet-tabitha/src/backend/claude-session/session-file-parser.test.ts (sidechain-line and fixture-line handling patterns)
    - /home/ubuntu/skynet-tabitha/.planning/phases/51-bg-agents-panel-admit-2-1-150-async-agent-invocations-via-to/51-CONTEXT.md (locked decisions, especially D-decisions on the dual-admission-path + scratch map)
    - /home/ubuntu/skynet-tabitha/.planning/phases/51-bg-agents-panel-admit-2-1-150-async-agent-invocations-via-to/51-RESEARCH.md (§ "Fix Shape (recommended)" for design contract, § "Test Plan (specifics for the planner)" for the four fixture shapes)
    - `grep -l '"isAsync":true' /home/ubuntu/.claude/projects/*/*.jsonl | head -3` — pick any of these JSONL files as a real-bytes source for the async-ack tool_result fixture line if taylor's `c054cef9-...jsonl` doesn't contain one. Confirmed candidates: `/home/ubuntu/.claude/projects/-home-ubuntu/6ff7e6b7-d12f-4165-9674-314a1538d770.jsonl` (7 hits) or `/home/ubuntu/.claude/projects/-home-ubuntu/c7274f12-0dbf-4fa7-9a89-9c35b6b5b39a.jsonl` (7 hits).
  </read_first>

  <files>
    - src/backend/claude-session/claude-session-server.bg-agents-async-ack.test.ts (NEW)
    - src/backend/claude-session/claude-session-server.ts (add a `__backgroundedAgentsForTests` export shim if no test-hook exists; see acceptance criteria)
  </files>

  <behavior>
    - Test 1 (Fixture A — Async Agent, modern shape): Feed a two-line fixture through the parser (via whatever test-hook the existing tests use; if none exists for the backgroundedAgents correlator, add a minimal `__admitBackgroundedAgentsLineForTests(line: string, state)` shim that runs JUST the parallel raw-line scan at L2527-2620 against a caller-provided `{ backgroundedAgents: Map, pendingAgentAdmission: Map }` state). Line 1: `type:"assistant"` with a `tool_use` block, `name:"Agent"`, `id:"toolu_A1"`, input `{description:"Test async agent", prompt:"do the thing", subagent_type:"gsd-executor"}` (NO `run_in_background` key). Line 2: `type:"user"` with a `tool_result` block whose `tool_use_id === "toolu_A1"`, and the enclosing turn object has `toolUseResult:{isAsync:true, status:"async_launched"}`. ASSERT: after line 2, `backgroundedAgents.has("toolu_A1") === true` with `subagentType === "gsd-executor"` and `description === "Test async agent"`.
    - Test 2 (Fixture B — Sync Agent, modern shape): Same line-1 Agent tool_use (id `toolu_A2`, no `run_in_background`). Line 2: `type:"user"` with a `tool_result` for `toolu_A2` carrying a real content payload and NO `isAsync` field on the enclosing turn. ASSERT: after line 2, `backgroundedAgents.has("toolu_A2") === false` AND `pendingAgentAdmission.has("toolu_A2") === false` (scratch was dropped in the else branch).
    - Test 3 (Fixture C — Legacy shape, backward compat): Line 1: `Agent` tool_use with `input.run_in_background === true`, id `toolu_A3`. ASSERT immediately after line 1: `backgroundedAgents.has("toolu_A3") === true` (legacy fast-path). Line 2: `tool_result` for `toolu_A3` with `toolUseResult.isAsync === true` (ack). ASSERT: `backgroundedAgents.has("toolu_A3") === true` still (skip-guard preserved). Line 3: `tool_result` for `toolu_A3` with completion content, no isAsync. ASSERT: `backgroundedAgents.has("toolu_A3") === false`.
    - Test 4 (Fixture D — Full lifecycle, modern shape): Line 1: modern Agent tool_use, id `toolu_A4`. Line 2: async-launch-ack tool_result (`isAsync:true`). Line 3: real completion tool_result for `toolu_A4` (no isAsync). ASSERT: snapshot after line 2 has `toolu_A4` in `backgroundedAgents`; snapshot after line 3 has `toolu_A4` NOT in `backgroundedAgents`. Also ASSERT pendingAgentAdmission is empty after line 3.
  </behavior>

  <action>
    Create a NEW test file at `/home/ubuntu/skynet-tabitha/src/backend/claude-session/claude-session-server.bg-agents-async-ack.test.ts` using the same conventions as `claude-session-server.malformed-eventid.test.ts` (vitest imports, `describe`/`it`/`expect`, `__forTests` export pattern from the module under test).

    Because the `backgrounded_agents` correlator at L2527-2620 currently runs INSIDE the `perFileTailLoop` closure (no test hook exists), you MUST add a minimal test-hook export from `claude-session-server.ts` to make the correlator unit-testable. Concretely, extract the raw-line scan body (L2527-2620) into a top-of-module helper `__admitBackgroundedAgentsLineForTests(line: string, state: { backgroundedAgents: Map<string, BackgroundedAgentEntry>; pendingAgentAdmission: Map<string, PendingAgentAdmissionEntry>; backgroundedShells: Map<string, BackgroundedShellEntry> })` that takes a raw JSONL line + a state bag and mutates the state in place. Export it as `__admitBackgroundedAgentsLineForTests` alongside other `__*ForTests` exports. Have the in-closure call site invoke the same helper with the closure's own maps. This is a strict refactor — same logic, same shape, ONLY behavior change is that the helper is now callable from tests.

    Do NOT write the pendingAgentAdmission scratch-map code yet in Task 1. In Task 1, ONLY:
    (a) Extract the existing L2527-2620 body into `__admitBackgroundedAgentsLineForTests` — no logic changes yet. `pendingAgentAdmission` parameter can be declared in the signature but the body will not USE it until Task 2. State type: `Map<string, { toolUseId: string; subagentType: string; description: string; startedAt: number }>`.
    (b) Write the four failing tests in `claude-session-server.bg-agents-async-ack.test.ts`. They will call the extracted helper with an empty state bag and assert on the resulting map contents. Real-bytes fixture guidance: for Fixture A/B/D's Agent tool_use, copy the shape of `toolu_01C3yz4A5NV4AamxHQRZH7DH` from `/home/ubuntu/.claude/projects/-home-ubuntu-skynet-taylor/c054cef9-c251-4d00-a460-8e90dbead931.jsonl` (grep for the ID). For the async-ack tool_result shape, grep any of `/home/ubuntu/.claude/projects/-home-ubuntu/6ff7e6b7-d12f-4165-9674-314a1538d770.jsonl` or `/home/ubuntu/.claude/projects/-home-ubuntu/c7274f12-0dbf-4fa7-9a89-9c35b6b5b39a.jsonl` for `"isAsync":true` and lift the enclosing turn's shape. Rename the tool_use_id to `toolu_A1` / `toolu_A2` / `toolu_A3` / `toolu_A4` in the fixtures so tests are self-contained.

    Fixtures 1/2/4 tests MUST fail at the end of Task 1 (parser has not been fixed yet — the `run_in_background === true` gate is still in place). Fixture 3 (legacy) MUST pass at the end of Task 1 (backward-compat path already works). This asymmetry is the RED signal: legacy passes, modern fails.

    Before starting: run `npx vitest run --reporter=default 2>/dev/1 | tail -20 > /tmp/51-vitest-baseline.log 2>&1; echo "EXIT=$?" >> /tmp/51-vitest-baseline.log` — actually use redirect to capture full output: `npx vitest run > /tmp/51-vitest-baseline.log 2>&1; echo "EXIT=$?" >> /tmp/51-vitest-baseline.log`. Read the tail of that log with `Read /tmp/51-vitest-baseline.log` (offset from bottom). Record baseline pass count in your working notes — Task 3's final `vitest run` MUST equal baseline + 4 new tests all passing.

    Do NOT run `git commit` in Task 1 — RED commits are allowed by fleet TDD directive but leave the commit to a single atomic commit at the end of Task 3 (per fleet directive 4: "never leave tests failing" — a RED commit would leave the tree with failing tests until Task 2 lands). Confirm the RED state by running `npx vitest run src/backend/claude-session/claude-session-server.bg-agents-async-ack.test.ts > /tmp/51-red.log 2>&1; echo "EXIT=$?" >> /tmp/51-red.log` and Read the log.
  </action>

  <verify>
    <automated>
    # Step 1: extraction sanity — helper is exported
    grep -c "__admitBackgroundedAgentsLineForTests" /home/ubuntu/skynet-tabitha/src/backend/claude-session/claude-session-server.ts
    # Expected: >= 2 (declaration site + at least one internal caller)

    # Step 2: test file exists and contains the four fixture ids
    grep -c "toolu_A1\|toolu_A2\|toolu_A3\|toolu_A4" /home/ubuntu/skynet-tabitha/src/backend/claude-session/claude-session-server.bg-agents-async-ack.test.ts
    # Expected: >= 4

    # Step 3: RED state — modern-shape tests fail, legacy passes
    npx vitest run src/backend/claude-session/claude-session-server.bg-agents-async-ack.test.ts > /tmp/51-red.log 2>&1; echo "EXIT=$?" >> /tmp/51-red.log
    # Read /tmp/51-red.log — expected: 1 passing (Fixture C legacy), 3 failing (Fixtures A/B/D). EXIT != 0.
    # If ALL FOUR pass at this stage, extraction went wrong (test-hook is exercising the wrong closure) — investigate.

    # Step 4: unchanged tests still pass (no regression from the refactor-extract)
    npx vitest run > /tmp/51-red-baseline.log 2>&1; echo "EXIT=$?" >> /tmp/51-red-baseline.log
    # Read /tmp/51-red-baseline.log — every previously-passing test still passes; only the three new modern-shape tests fail.
    </automated>
  </verify>

  <done>
    - `claude-session-server.ts` exports `__admitBackgroundedAgentsLineForTests` (refactor-extract, no behavior change).
    - `claude-session-server.bg-agents-async-ack.test.ts` exists with four tests (Fixtures A/B/C/D).
    - Fixture C (legacy) passes; Fixtures A/B/D (modern) fail (this is the RED state).
    - All pre-existing tests still pass — the refactor-extract introduced no regression.
    - No git commit yet (single atomic commit lands at end of Task 3).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement pendingAgentAdmission scratch map + dual-path admission per RESEARCH.md § "Fix Shape" steps 1-3</name>

  <read_first>
    - /home/ubuntu/skynet-tabitha/src/backend/claude-session/claude-session-server.ts L2140-2172 (backgroundedAgents + backgroundedShells declaration site — scratch map goes alongside these), L2360-2370 (existing map.clear() reset points — pendingAgentAdmission needs the same reset), L2527-2620 (the correlator scan — now inside `__admitBackgroundedAgentsLineForTests` after Task 1), L3095-3110 (second reset site — same as L2360)
    - /home/ubuntu/skynet-tabitha/src/backend/claude-session/claude-session-server.bg-agents-async-ack.test.ts (the RED tests from Task 1 — Task 2 flips them to GREEN)
    - /home/ubuntu/skynet-tabitha/.planning/phases/51-bg-agents-panel-admit-2-1-150-async-agent-invocations-via-to/51-RESEARCH.md § "Fix Shape (recommended)" (steps 1-3 spell out the exact code changes)
    - /home/ubuntu/skynet-tabitha/.planning/phases/51-bg-agents-panel-admit-2-1-150-async-agent-invocations-via-to/51-CONTEXT.md § "Parser admission gate" (D-decision: dual admission paths coexist, belt-and-suspenders)
  </read_first>

  <files>
    - src/backend/claude-session/claude-session-server.ts (add pendingAgentAdmission Map declaration alongside backgroundedAgents at ~L2143; add reset calls at ~L2363 and ~L3101; update the START gate at ~L2547-2569 to admit into scratch-or-backgrounded dual paths; extend the async-ack branch at ~L2603-2619 to PROMOTE from scratch + drop scratch on non-async completion)
  </files>

  <behavior>
    - After Task 2, all four fixtures in `claude-session-server.bg-agents-async-ack.test.ts` MUST pass:
      * Fixture A: modern async Agent → `backgroundedAgents.has("toolu_A1") === true` after ack
      * Fixture B: modern sync Agent → `backgroundedAgents.has("toolu_A2") === false` AND `pendingAgentAdmission.has("toolu_A2") === false` after sync completion
      * Fixture C: legacy Agent → `backgroundedAgents.has("toolu_A3") === true` at tool_use, still true after ack (skip-guard), false after real completion
      * Fixture D: full lifecycle → intermediate `backgroundedAgents` has `toolu_A4`; final does not
    - Bash correlator behavior is UNCHANGED — no regression on any Bash-adjacent test.
    - `-n +1` replay idempotence: repeated calls to `__admitBackgroundedAgentsLineForTests` with the SAME lines converge to the SAME map state (Map.set is last-write-wins on stable input; delete on absent is no-op).
  </behavior>

  <action>
    Apply the three edits from `51-RESEARCH.md` § "Fix Shape (recommended)" steps 1-3 to `/home/ubuntu/skynet-tabitha/src/backend/claude-session/claude-session-server.ts`.

    Step 1 — Declare `pendingAgentAdmission` alongside `backgroundedAgents` (near L2143):
    Add a `const pendingAgentAdmission = new Map<string, { toolUseId: string; subagentType: string; description: string; startedAt: number }>();` in the same closure scope as `backgroundedAgents`. Add a matching `pendingAgentAdmission.clear();` at BOTH reset sites (L2363 and L3101) so `/id reset` and per-file reset clear it too.

    Step 2 — Update the Agent START gate (L2547-2569 inside the extracted helper) per RESEARCH.md § "Fix Shape" step 2. In the tool_use block: keep the shared metadata extraction (`startedAt`, `subagentType`, `description`) but split the admission into two branches: (a) if `b?.input?.run_in_background === true`, admit directly to `backgroundedAgents.set(b.id, info)` (legacy fast-path — backward compat); (b) else, admit to `pendingAgentAdmission.set(b.id, info)` (modern late-admission path). Both branches use the SAME `info` object shape. DO NOT touch the Bash branch at L2570-2591 — it stays exactly as-is per RESEARCH.md § "Bash shape on v2.1.150" empirical finding.

    Step 3 — Extend the async-ack branch (L2603-2619 inside the extracted helper) per RESEARCH.md § "Fix Shape" step 3. In the `if (isAsyncAck)` branch: iterate the same tool_result blocks the removal branch iterates; for each `tool_use_id`, if `pendingAgentAdmission.get(id)` returns an entry, `backgroundedAgents.set(id, entry)` and `pendingAgentAdmission.delete(id)`. In the `else` (non-async completion) branch: KEEP the existing `backgroundedAgents.delete(b.tool_use_id)` AND add `pendingAgentAdmission.delete(b.tool_use_id)` in the same loop iteration — this is the "sync Agent scratch drop" behavior (Fixture B).

    After the edits, run `npx vitest run src/backend/claude-session/claude-session-server.bg-agents-async-ack.test.ts > /tmp/51-green.log 2>&1; echo "EXIT=$?" >> /tmp/51-green.log` and Read the log. All four fixtures MUST pass. If any fail, iterate on the correlator logic — but the design contract in RESEARCH.md § "Fix Shape" is deterministic; a fixture failure means the implementation diverged from the contract.

    DO NOT run `git commit` in Task 2. Single atomic commit lands at end of Task 3.

    Anti-pattern guardrails: DO NOT reduce the scratch entry to `{}` (must carry `toolUseId`/`subagentType`/`description`/`startedAt` — those are what `backgroundedAgents` consumers read). DO NOT gate the else branch of Step 2 on any additional condition beyond the outer `tool_use` + `name === "Agent"` + `typeof id === "string"` gates — that would silently skip real modern Agents. DO NOT drop the legacy fast-path — Fixture C would regress. DO NOT touch the `backgroundedShells` correlator or the task-notification detector at L2621+ (out-of-scope per RESEARCH.md § "Not-in-scope reminders").
  </action>

  <verify>
    <automated>
    # Step 1: scratch map declared, cleared, and referenced
    grep -c "pendingAgentAdmission" /home/ubuntu/skynet-tabitha/src/backend/claude-session/claude-session-server.ts
    # Expected: >= 6 (declaration + 2 clears + set + get + delete + delete-on-completion)

    # Step 2: legacy fast-path preserved
    grep -c "b?.input?.run_in_background === true" /home/ubuntu/skynet-tabitha/src/backend/claude-session/claude-session-server.ts
    # Expected: >= 2 (Agent legacy branch + Bash branch — Bash unchanged)

    # Step 3: Bash branch untouched (structural check — grep for the Bash-specific admission)
    grep -c 'name === "Bash"' /home/ubuntu/skynet-tabitha/src/backend/claude-session/claude-session-server.ts
    # Expected: >= 1 (Bash branch still present)
    grep -c "backgroundedShells.set" /home/ubuntu/skynet-tabitha/src/backend/claude-session/claude-session-server.ts
    # Expected: >= 1 (Bash admission untouched)

    # Step 4: GREEN state — all four fixtures pass
    npx vitest run src/backend/claude-session/claude-session-server.bg-agents-async-ack.test.ts > /tmp/51-green.log 2>&1; echo "EXIT=$?" >> /tmp/51-green.log
    # Read /tmp/51-green.log — expected: EXIT=0, "4 passed" in the tail.

    # Step 5: no cross-suite regression
    npx vitest run > /tmp/51-green-full.log 2>&1; echo "EXIT=$?" >> /tmp/51-green-full.log
    # Read /tmp/51-green-full.log — expected: EXIT=0, pass count = baseline + 4 (compare against /tmp/51-vitest-baseline.log from Task 1).
    </automated>
  </verify>

  <done>
    - `pendingAgentAdmission` declared in the same closure as `backgroundedAgents`; cleared at both reset sites.
    - Agent START gate splits into legacy-direct-admit branch + modern-scratch branch based on `input.run_in_background === true`.
    - Async-ack branch promotes scratch → backgroundedAgents; non-async completion branch drops scratch entry alongside the existing backgroundedAgents.delete.
    - Bash branch untouched (structural grep confirms).
    - All four Fixture tests (A/B/C/D) pass; full-suite `npx vitest run` exits 0 at baseline + 4.
    - No commit yet.
  </done>
</task>

<task type="auto">
  <name>Task 3 (VERIFY + COMMIT): Full-suite vitest + backend + frontend typecheck, single atomic commit</name>

  <read_first>
    - /home/ubuntu/skynet-tabitha/src/backend/claude-session/claude-session-server.ts (verify the Task 1 + Task 2 changes are all present in your working copy — `git diff src/backend/claude-session/claude-session-server.ts` should show scratch-map + dual-admit + async-ack promotion + non-async scratch drop)
    - /home/ubuntu/skynet-tabitha/src/backend/claude-session/claude-session-server.bg-agents-async-ack.test.ts (new file)
    - /tmp/51-vitest-baseline.log, /tmp/51-green.log, /tmp/51-green-full.log (Task 1/2 verification records)
  </read_first>

  <files>
    - (no NEW file writes; commit-only + verification)
  </files>

  <action>
    Step 1 — Final full test suite. Run in this exact form:
    `cd /home/ubuntu/skynet-tabitha && npx vitest run > /tmp/51-final-vitest.log 2>&1; echo "EXIT=$?" >> /tmp/51-final-vitest.log`
    Read `/tmp/51-final-vitest.log`. MUST exit 0. Pass count MUST equal baseline (from /tmp/51-vitest-baseline.log) + 4 new tests. If any pre-existing test regressed, halt and diagnose — never leave tests failing (fleet directive 4).

    Step 2 — Backend strict typecheck. Run:
    `cd /home/ubuntu/skynet-tabitha && npm run build:backend > /tmp/51-final-backend.log 2>&1; echo "EXIT=$?" >> /tmp/51-final-backend.log`
    Read `/tmp/51-final-backend.log`. MUST exit 0. `npm run build:backend` is the strict typecheck for `src/backend/**` per fleet directive 6 — do NOT substitute `npx tsc --noEmit`.

    Step 3 — Frontend build (defense-in-depth: shared types could regress). Run:
    `cd /home/ubuntu/skynet-tabitha && npm run build > /tmp/51-final-frontend.log 2>&1; echo "EXIT=$?" >> /tmp/51-final-frontend.log`
    Read `/tmp/51-final-frontend.log`. MUST exit 0.

    Step 4 — Confirm staged changes are ONLY the two intended files. Run:
    `cd /home/ubuntu/skynet-tabitha && git status --porcelain > /tmp/51-git-status.log 2>&1`
    Read `/tmp/51-git-status.log`. Expected exactly TWO entries (order-independent):
      - `M src/backend/claude-session/claude-session-server.ts`
      - `?? src/backend/claude-session/claude-session-server.bg-agents-async-ack.test.ts`
    If any OTHER file changed (e.g., stray formatting from vitest, a .planning/ log write, package-lock churn), STOP and diagnose — do not include unrelated changes in the commit. NB: `/tmp/51-*.log` files live OUTSIDE the repo and will not appear here.

    Step 5 — Single atomic commit. Do NOT `git push` (fleet directive 2 — orchestrator handles ship). Do NOT stage anything else. Use a HEREDOC for the message:

    ```
    cd /home/ubuntu/skynet-tabitha && \
      git add src/backend/claude-session/claude-session-server.ts \
              src/backend/claude-session/claude-session-server.bg-agents-async-ack.test.ts && \
      git commit -m "$(cat <<'EOF'
    feat(51-01): admit v2.1.150+ async Agent invocations via tool_result launch-ack

    Fix Bug 1 of bounty claude-code-2-1-214-pretty-view-compat. The
    backgrounded_agents correlator at claude-session-server.ts ~L2506 gated
    admission on input.run_in_background === true, which v2.1.150+ Agent
    tool_use payloads no longer carry. The async signal moved to the
    tool_result launch-ack (toolUseResult.isAsync === true, already parsed
    at L2603 as a skip-guard).

    - Add pendingAgentAdmission scratch map alongside backgroundedAgents.
    - Dual admission path: legacy input.run_in_background === true still
      admits at tool_use time (backward compat); modern shape stashes into
      scratch and promotes to backgroundedAgents on the async-launch-ack.
    - Sync modern Agent completions drop the scratch entry silently
      (never enter backgroundedAgents).
    - Refactor-extract the L2527-2620 raw-line scan into
      __admitBackgroundedAgentsLineForTests so the correlator is
      unit-testable.
    - Add four fixture-based tests: modern async admit, modern sync drop,
      legacy backward compat, full lifecycle.
    - Bash branch untouched — empirically still uses legacy shape on
      v2.1.150+ (RESEARCH.md § "Bash shape on v2.1.150").

    Refs bounty: claude-code-2-1-214-pretty-view-compat (Bug 1)
    EOF
    )"
    ```

    Step 6 — Verify commit landed and tree is clean. Run:
    `cd /home/ubuntu/skynet-tabitha && git log -1 --oneline > /tmp/51-commit.log 2>&1 && git status --porcelain >> /tmp/51-commit.log 2>&1`
    Read `/tmp/51-commit.log`. Expected: HEAD is the new commit; working tree clean.

    DO NOT run `git push`. DO NOT run `docker build` or `docker compose up`. DO NOT post to coord-room. DO NOT edit `~/.claude/roles/box-maintainer/skynet-patches.md`. Orchestrator (tabitha) picks up all of that after you return. Executor SCOPE STOPS HERE.
  </action>

  <verify>
    <automated>
    # Full-suite vitest exit code
    tail -1 /tmp/51-final-vitest.log
    # Expected: EXIT=0

    # Backend typecheck exit code
    tail -1 /tmp/51-final-backend.log
    # Expected: EXIT=0

    # Frontend build exit code
    tail -1 /tmp/51-final-frontend.log
    # Expected: EXIT=0

    # Commit landed and tree clean
    cd /home/ubuntu/skynet-tabitha && git log -1 --pretty=%s
    # Expected: begins with "feat(51-01): admit v2.1.150+ async Agent"
    cd /home/ubuntu/skynet-tabitha && git status --porcelain | wc -l
    # Expected: 0
    </automated>
  </verify>

  <done>
    - `npx vitest run` exits 0, pass count = baseline + 4.
    - `npm run build:backend` exits 0.
    - `npm run build` exits 0.
    - One atomic commit on branch `feat/tab-title-from-tmux` containing exactly the two intended files.
    - Working tree clean; no push, no docker, no coord-room, no skynet-patches.md edits (orchestrator scope).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSONL file → parser | Claude Code writes tool_use / tool_result lines; parser reads them raw. Format is version-dependent (v2.1.149 → v2.1.150 shape change is the motivating incident for this phase). Input is trusted (written by Claude Code harness), but SHAPE evolves. |
| parser correlator → WS pretty-view frame | backgroundedAgents map is serialized (JSON.stringify of a sorted list) and pushed to any connected frontend. Frontend renders it in the BG-agents panel. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Tampering | JSONL shape evolves again (Claude Code v2.1.151+ introduces a THIRD shape) | accept | Fixture-based tests catch drift on the next audit. Add a third admission path when observed. RESEARCH.md § "Risks" documents this. |
| T-51-02 | Denial-of-service | pendingAgentAdmission grows unbounded if sync Agents fire and no completion tool_result is ever written | accept | Real Claude Code writes tool_results promptly. If a parent crashes mid-turn, scratch entries linger until the tail restarts (see RESEARCH.md § "Lingering scratch entries"). Volume is O(sub-agent invocations per session) — bounded in practice. |
| T-51-03 | Information disclosure | subagent_type / description leak via BG-agents panel | accept | These fields are ALREADY emitted for legacy backgrounded agents; no new exposure surface. Panel rendering is unchanged. |
| T-51-04 | Elevation of privilege | Removing the `run_in_background === true` START gate admits a non-async Agent that shouldn't be tracked | mitigate | The else branch admits to `pendingAgentAdmission` (scratch), NOT `backgroundedAgents`. Scratch never emits; it only promotes on the async-launch-ack (a strong signal). Sync completions drop scratch. Verified by Fixture B test. |
| T-51-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no new deps. `grep -c "\"dependencies\"" package.json` should return 1 unchanged. |
</threat_model>

<verification>
Phase-level verification (beyond the per-task automated checks):

1. **Grep-check the fix shape landed correctly.** Run against the modified file:
   ```
   grep -c "pendingAgentAdmission" /home/ubuntu/skynet-tabitha/src/backend/claude-session/claude-session-server.ts
   ```
   Expected: >= 6 (declaration + 2 clears + set + get + delete + delete-on-completion).

2. **Grep-check backward compat preserved.** Legacy fast-path branch must still exist:
   ```
   grep -B1 -A3 "input.run_in_background === true" /home/ubuntu/skynet-tabitha/src/backend/claude-session/claude-session-server.ts | grep -c "backgroundedAgents.set"
   ```
   Expected: >= 1 (legacy Agent path still admits directly).

3. **Grep-check Bash branch untouched.** Compare pre/post — the Bash admission at L2570-2591 should be byte-identical to pre-plan HEAD. Use `git diff --stat src/backend/claude-session/claude-session-server.ts` to spot-check that the diff footprint is bounded (Agent branch + top-of-closure decl + reset sites only).

4. **Fixture test names present:** `grep -E "toolu_A[1-4]" src/backend/claude-session/claude-session-server.bg-agents-async-ack.test.ts | wc -l` should be >= 4.

5. **No unrelated file drift:** `git diff --name-only HEAD~1` should list exactly two files: the correlator source and the new test file.
</verification>

<success_criteria>
- `npx vitest run` exits 0 at plan end, pass count = baseline + 4.
- `npm run build:backend` exits 0.
- `npm run build` exits 0.
- Modern async Agent invocations (v2.1.150+ shape: no `run_in_background`, ack has `toolUseResult.isAsync === true`) are admitted to `backgroundedAgents` on the ack.
- Modern sync Agent invocations (v2.1.150+ shape, first tool_result is a real completion) are NEVER admitted to `backgroundedAgents`.
- Legacy async Agent invocations (`input.run_in_background === true`) still admit immediately at tool_use time, survive the async-ack skip-guard, and remove on real completion.
- Bash correlator behavior is unchanged (structural grep + full test suite green).
- Single atomic commit on `feat/tab-title-from-tmux`, working tree clean, no push, no docker, no coord-room.
- BG-agents panel will show taylor's `gsd-executor` sub-agent once orchestrator deploys — post-deploy verification is orchestrator scope, not executor.
</success_criteria>

<output>
Create `.planning/phases/51-bg-agents-panel-admit-2-1-150-async-agent-invocations-via-to/51-01-SUMMARY.md` when done, following `$HOME/.claude/get-shit-done/templates/summary.md`. Include:
- Files modified (exact paths)
- Commit SHA (single atomic commit)
- Baseline test count → final test count (baseline + 4)
- Any deviations from RESEARCH.md § "Fix Shape" (should be none — the design contract is deterministic)
- Explicit note: NO push, NO docker, NO coord-room, NO skynet-patches.md — orchestrator (tabitha) picks up deploy.
</output>
