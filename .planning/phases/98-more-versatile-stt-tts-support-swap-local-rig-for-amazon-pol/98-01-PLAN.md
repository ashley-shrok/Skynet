---
phase: 98-more-versatile-stt-tts-support-swap-local-rig-for-amazon-pol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/Dockerfile
  - package.json
  - package-lock.json
autonomous: false
requirements:
  - P98-STT-01
  - P98-STT-02
  - P98-TTS-01
  - Provider-choice
  - Provider-access
must_haves:
  truths:
    - "ffmpeg is present inside the runtime docker image (Stage 5) — verifiable via `docker run --rm skynet:dev ffmpeg -version`"
    - "@aws-sdk/client-polly and @aws-sdk/client-transcribe-streaming are installed at version >=3.1129.0 and resolvable from src/backend/*"
    - "Package legitimacy has been human-verified via npmjs.com before install lands"
  artifacts:
    - path: "docker/Dockerfile"
      provides: "ffmpeg apt package on runtime stage"
      contains: "ffmpeg"
    - path: "package.json"
      provides: "@aws-sdk/client-polly and @aws-sdk/client-transcribe-streaming dependencies"
      contains: "@aws-sdk/client-polly"
  key_links:
    - from: "docker/Dockerfile Stage 5"
      to: "apt-get install ffmpeg"
      via: "existing nginx/gettext-base/openssl/ca-certificates/gosu/wget line"
      pattern: "apt-get install .* ffmpeg"
    - from: "package.json dependencies"
      to: "@aws-sdk/client-polly ^3.1129.0"
      via: "npm install"
      pattern: "@aws-sdk/client-polly"
---

<objective>
Land the infrastructure prerequisites that all subsequent Phase 98 plans depend on: ffmpeg in the runtime docker image (per RESEARCH § Pitfall 7, "SHIP BLOCKER" — voice-in fails with `ENOENT: ffmpeg not found` if omitted) and the two AWS SDK v3 client packages (`@aws-sdk/client-polly`, `@aws-sdk/client-transcribe-streaming`). Package legitimacy checkpoint per RESEARCH § Package Legitimacy Audit — both packages marked `[ASSUMED VERIFIED]` (slopcheck not run at research time), so a blocking human-verify precedes install.

Purpose: Kill the ship-blocker (missing ffmpeg) and un-block adapter code that lands in Plan 04. This plan is the hard prereq for waves 2+.
Output: Modified Dockerfile with ffmpeg, updated package.json + package-lock.json with two AWS SDK packages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-more-versatile-stt-tts-support-swap-local-rig-for-amazon-pol/98-CONTEXT.md
@.planning/phases/98-more-versatile-stt-tts-support-swap-local-rig-for-amazon-pol/98-RESEARCH.md
@.planning/phases/98-more-versatile-stt-tts-support-swap-local-rig-for-amazon-pol/98-PATTERNS.md
@docker/Dockerfile
@package.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy human-verify for @aws-sdk/client-polly + @aws-sdk/client-transcribe-streaming</name>
  <files>package.json</files>
  <read_first>
    - .planning/phases/98-more-versatile-stt-tts-support-swap-local-rig-for-amazon-pol/98-RESEARCH.md (§ Package Legitimacy Audit — both packages marked [ASSUMED VERIFIED])
  </read_first>
  <what-built>
    Nothing yet — this is a pre-install verification checkpoint per RESEARCH § Package Legitimacy Audit. Both packages are AWS-authored under the `@aws-sdk` scope and canonical, but slopcheck was not available at research time. Per gate policy, `[ASSUMED VERIFIED]` requires blocking human-verify before install.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@aws-sdk/client-polly and confirm: publisher shows `@aws-sdk` scope / `aws-sdk-bot` maintainer; repository points to `github.com/aws/aws-sdk-js-v3`; version >=3.1129.0 available; weekly downloads in millions.
    2. Open https://www.npmjs.com/package/@aws-sdk/client-transcribe-streaming and confirm same: `@aws-sdk` scope; `github.com/aws/aws-sdk-js-v3` repo; version >=3.1129.0; downloads in hundreds of thousands weekly.
    3. Confirm no typosquat concern (e.g. NOT `aws-sdk-client-polly` without the leading `@`, NOT `polly-client`, NOT `aws-polly`).
    4. Approve if both packages are AWS-authored canonical clients.
  </how-to-verify>
  <action>
    Pause execution and present the checkpoint. When the human approves ("approved"), proceed to Task 2. If any concerns are raised, HALT — do not proceed to install. Both packages are `@aws-sdk` scope (AWS-authored) but slopcheck was unavailable at research time; the human's job here is to verify no typosquat/substitution has occurred at the npm registry level before running `npm install`.
  </action>
  <verify>
    <automated>echo "checkpoint task — verified by human review of npmjs.com pages"</automated>
  </verify>
  <done>
    Human types "approved" (or equivalent) and the plan proceeds to Task 2. If concerns raised, plan halts.
  </done>
  <resume-signal>Type "approved" to proceed with npm install, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add ffmpeg to docker/Dockerfile Stage 5 apt-get line (SHIP BLOCKER per RESEARCH Pitfall 7)</name>
  <files>docker/Dockerfile</files>
  <read_first>
    - docker/Dockerfile (Stage 5 begins around line 60; existing apt-get install line at line 67 has nginx gettext-base openssl ca-certificates gosu wget)
    - .planning/phases/98-more-versatile-stt-tts-support-swap-local-rig-for-amazon-pol/98-RESEARCH.md § Pitfall 7 (SHIP BLOCKER — missing ffmpeg makes `handleTranscribe` fail with `ENOENT` on first upload)
    - .planning/phases/98-more-versatile-stt-tts-support-swap-local-rig-for-amazon-pol/98-PATTERNS.md § `docker/Dockerfile:67` (MODIFIED)
  </read_first>
  <action>
    Locate the Stage 5 `RUN apt-get update && apt-get install -y nginx gettext-base openssl ca-certificates gosu wget && \` line (currently line 67). Append `ffmpeg` to the package list so it reads `... nginx gettext-base openssl ca-certificates gosu wget ffmpeg && \`. Do NOT touch Stage 1 (line 5) or Stage 4 (line 43) — ffmpeg is only needed on the runtime stage. Do NOT add any additional apt options. Do NOT reformat the surrounding line or its cleanup tail (the `rm -rf /var/lib/apt/lists/*` continuation must stay intact so the image doesn't bloat with the apt cache).
  </action>
  <verify>
    <automated>grep -E 'apt-get install .*ffmpeg' docker/Dockerfile | grep -v '^#' | wc -l | grep -q '^[1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E 'apt-get install .* nginx .* ffmpeg' docker/Dockerfile` returns exactly one match (Stage 5 runtime apt line).
    - The apt-get install line still ends with `&& \` continuation to the `rm -rf /var/lib/apt/lists/*` cleanup.
    - `git diff docker/Dockerfile` shows exactly one line changed (the apt-get install line) — no unrelated formatting drift.
    - Post-install docker build sanity is checked by a downstream integration test (`docker run --rm skynet:dev ffmpeg -version` — exit 0). Not required as this task's automated gate (docker build takes minutes), but recorded in SUMMARY for the ship-motion checklist.
  </acceptance_criteria>
  <done>
    Dockerfile modified with ffmpeg added to Stage 5 apt-get install line; grep gate passes; SUMMARY notes the deferred docker-build sanity check for the ship-motion phase.
  </done>
</task>

<task type="auto">
  <name>Task 3: Install @aws-sdk/client-polly and @aws-sdk/client-transcribe-streaming</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (dependencies section — verify neither package is already listed)
    - .planning/phases/98-more-versatile-stt-tts-support-swap-local-rig-for-amazon-pol/98-RESEARCH.md § Standard Stack (version pin `^3.1129.0`, both packages, verified via `npm view` 2026-09-10)
  </read_first>
  <action>
    Run `npm install @aws-sdk/client-polly@^3.1129.0 @aws-sdk/client-transcribe-streaming@^3.1129.0` from the repo root (Task 1 human-verify must have been approved first). Both packages resolve to caret-pinned ^3.1129.0 or the current latest in the 3.x line. Do NOT add `--save-dev` — both are production runtime dependencies (loaded by backend voice adapters at request time). Do NOT install `@aws-sdk/credential-provider-node` explicitly — it is a transitive dep of both packages (verified via `npm view @aws-sdk/client-polly dependencies`), and passing it as a direct dep risks version drift from the SDK's tested combination. Do NOT touch other dependencies in this task.
  </action>
  <verify>
    <automated>node -e "const p = require('./package.json'); if (!p.dependencies['@aws-sdk/client-polly']) throw 1; if (!p.dependencies['@aws-sdk/client-transcribe-streaming']) throw 1;" && node -e "require.resolve('@aws-sdk/client-polly'); require.resolve('@aws-sdk/client-transcribe-streaming');"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies section has `@aws-sdk/client-polly` at `^3.1129.0` or higher in the 3.x line.
    - `package.json` dependencies section has `@aws-sdk/client-transcribe-streaming` at `^3.1129.0` or higher in the 3.x line.
    - Neither package appears in `devDependencies`.
    - `package-lock.json` is regenerated and includes both packages plus their transitive `@aws-sdk/credential-provider-node` (verify: `grep -c '"@aws-sdk/credential-provider-node"' package-lock.json` >0).
    - `node -e "require.resolve('@aws-sdk/client-polly')"` exits 0.
    - `node -e "require.resolve('@aws-sdk/client-transcribe-streaming')"` exits 0.
    - No other npm packages added/removed (`git diff package.json` shows only the two additions).
  </acceptance_criteria>
  <done>
    Both AWS SDK packages installed as production deps; require.resolve succeeds; package-lock updated; no incidental package changes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local install | Package tarballs downloaded and executed at install; supply-chain risk if wrong package installed |
| Docker build → runtime image | apt package installed at build; ships to all Skynet deploys (t1000 + T800) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-01-SC | Tampering | npm packages (@aws-sdk/*) | mitigate | Task 1 blocking-human-verify against npmjs.com; both packages are canonical AWS-authored under `@aws-sdk` scope; caret pin allows patch-updates within tested 3.x line |
| T-98-01-02 | Tampering | docker apt install (ffmpeg) | accept | Debian bookworm-main package, decades old, ships with digest-verified apt repo; risk equivalent to every other apt package already in the image |
| T-98-01-03 | Denial of Service | image size bloat | accept | ffmpeg is ~30MB; image already several hundred MB (RESEARCH § A8); negligible |
| T-98-01-04 | Information Disclosure | package.json in git | accept | Version pins are non-secret; standard public metadata |
</threat_model>

<verification>
- `grep -E 'apt-get install .*ffmpeg' docker/Dockerfile | grep -v '^#'` returns a match.
- Both AWS SDK packages resolvable via `require.resolve()`.
- Package legitimacy checkpoint (Task 1) recorded as approved in SUMMARY before Tasks 2-3 executed.
</verification>

<success_criteria>
- Dockerfile Stage 5 apt-get install includes ffmpeg.
- @aws-sdk/client-polly and @aws-sdk/client-transcribe-streaming installed at ^3.1129.0+ as production deps.
- Human-verify checkpoint completed and recorded.
- No other files changed (no adapter code lands in this plan).
</success_criteria>

<output>
Create `.planning/phases/98-more-versatile-stt-tts-support-swap-local-rig-for-amazon-pol/98-01-SUMMARY.md` when done, recording: (a) approved package-legitimacy verdict, (b) exact package versions installed, (c) confirmation ffmpeg apt line updated, (d) reminder for ship motion to run `docker build && docker run --rm skynet:dev ffmpeg -version` as sanity check.
</output>
