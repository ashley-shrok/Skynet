---
phase: 98-more-versatile-stt-tts-support-swap-local-rig-for-amazon-pol
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/pickers/VoicePicker.tsx
  - src/ui/api/voice-api.ts
  - src/ui/features/pretty-view/ChatMessage.speak.test.tsx
  - src/ui/sidebar/NewSessionDialog.test.tsx
  - src/ui/sidebar/NewSessionDialog.chain.test.tsx
  - src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx
  - src/ui/sidebar/NewSessionDialog.task-input.test.tsx
  - src/ui/features/pretty-view/RoleCosmeticEditBlock.test.tsx
autonomous: true
requirements:
  - P98-CAT-01
  - P98-KILL-01
  - Voice-catalog
  - Kill-list
must_haves:
  truths:
    - "VoicePicker mounts without any HTTP fetch — no getVoices() call, no useEffect subscribing to a network response"
    - "VoicePicker renders exactly 7 options (plus the default '(default)' option): Danielle, Joanna, Ruth, Salli, Tiffany, Matthew, Stephen"
    - "getVoices export is deleted from src/ui/api/voice-api.ts; no orphan imports remain in src/ui/"
    - "postSpeak and postSpeakStream client contracts are preserved unchanged"
  artifacts:
    - path: "src/ui/features/pretty-view/pickers/VoicePicker.tsx"
      provides: "voice dropdown picker with inlined POLLY_VOICES const"
      contains: "POLLY_VOICES"
    - path: "src/ui/api/voice-api.ts"
      provides: "postSpeak + postSpeakStream (getVoices deleted)"
      exports: ["postSpeak", "postSpeakStream", "SAMPLE_PHRASE"]
  key_links:
    - from: "VoicePicker.tsx"
      to: "postSpeak (sample-play button)"
      via: "import from @/api/voice-api"
      pattern: "postSpeak"
---

<objective>
Delete the runtime voice-catalog fetch from the frontend and inline the fixed 7-voice const in `VoicePicker.tsx` (per D-Voice-catalog "DROP endpoint; inline the 7-voice const"). Delete `getVoices` from `src/ui/api/voice-api.ts` and sweep the 6 test-file mock sites still referencing it. This plan is fully frontend-scoped and can parallel Plans 01 and 02 (no backend dependencies — frontend and backend maintain two sources of truth for the 7-voice list, acceptable per RESEARCH § polly-voice-catalog.ts pitfall "low drift risk").

Purpose: Ship the frontend half of the catalog reshape early so no runtime `/voice/voices` call is made even against the current Chatterbox-shaped backend. Kill-list execution starts here per D-Kill-list.
Output: VoicePicker consumes inline const; voice-api.ts loses getVoices; test mocks updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-more-versatile-stt-tts-support-swap-local-rig-for-amazon-pol/98-CONTEXT.md
@.planning/phases/98-more-versatile-stt-tts-support-swap-local-rig-for-amazon-pol/98-RESEARCH.md
@.planning/phases/98-more-versatile-stt-tts-support-swap-local-rig-for-amazon-pol/98-PATTERNS.md
@src/ui/features/pretty-view/pickers/VoicePicker.tsx
@src/ui/api/voice-api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inline POLLY_VOICES const into VoicePicker.tsx and drop the getVoices useEffect</name>
  <files>src/ui/features/pretty-view/pickers/VoicePicker.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/pickers/VoicePicker.tsx (full file — the getVoices useEffect at lines 24-31, the useState at line 20, options render at lines 93-97, imports at line 3)
    - .planning/phases/98-more-versatile-stt-tts-support-swap-local-rig-for-amazon-pol/98-PATTERNS.md § VoicePicker.tsx
    - .planning/phases/98-more-versatile-stt-tts-support-swap-local-rig-for-amazon-pol/98-CONTEXT.md § Claude's-discretion (locked: drop endpoint, inline const)
  </read_first>
  <action>
    In `src/ui/features/pretty-view/pickers/VoicePicker.tsx`:
    (a) Drop `getVoices` from the import at line 3 — keep `postSpeak`, `SAMPLE_PHRASE`. The new import reads: import postSpeak and SAMPLE_PHRASE from `@/api/voice-api`.
    (b) Delete the `useState` on line 20 holding the `{display_name, filename}[]` voices AND the `useEffect` block at lines 24-31 that calls `getVoices()` and sets that state.
    (c) Add a module-level const OUTSIDE the component named `POLLY_VOICES` typed as `readonly { voiceId: string; displayName: string }[]` with exactly 7 entries in this order: Danielle, Joanna, Ruth, Salli, Tiffany, Matthew, Stephen. Mirror the shape of `src/backend/voice/polly-voice-catalog.ts` (Plan 02) but include only `voiceId` and `displayName` (picker does not render gender). Include a comment noting this is intentionally a second source of truth (per PATTERNS pitfall — 7 static strings, low drift risk).
    (d) Update the options `.map(...)` render (currently lines 93-97) to iterate `POLLY_VOICES` and render an `<option>` per entry keyed by `voiceId`, with `value={v.voiceId}` and `{v.displayName}` as the label. Preserve the existing default `<option value="">(default)</option>` fallback at line 92 unchanged.
    (e) Preserve the sample-play button behavior (postSpeak call, playback, error toast) verbatim — do NOT touch that code path.
    (f) Prune only the `useEffect` / `useState` imports if they are no longer used ANYWHERE in the file (`useRef` stays; verify via grep before pruning).
    (g) After edit, no `voices` state variable remains — the component reads POLLY_VOICES directly.
  </action>
  <verify>
    <automated>grep -c 'getVoices' src/ui/features/pretty-view/pickers/VoicePicker.tsx | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'getVoices' src/ui/features/pretty-view/pickers/VoicePicker.tsx` returns 0.
    - `grep -c 'POLLY_VOICES' src/ui/features/pretty-view/pickers/VoicePicker.tsx` returns >=2 (const declaration + map iteration).
    - `grep -cE 'Danielle|Joanna|Ruth|Salli|Tiffany|Matthew|Stephen' src/ui/features/pretty-view/pickers/VoicePicker.tsx` returns 7.
    - VoicePicker no longer references the old `{display_name, filename}` field shape.
    - The default `<option value="">(default)</option>` fallback still renders.
    - `tsc --noEmit` passes.
  </acceptance_criteria>
  <done>
    VoicePicker consumes inline const, no runtime fetch, 7 voices render, sample-play preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete getVoices from voice-api.ts and update the 6 test-file mock sites</name>
  <files>src/ui/api/voice-api.ts, src/ui/features/pretty-view/ChatMessage.speak.test.tsx, src/ui/sidebar/NewSessionDialog.test.tsx, src/ui/sidebar/NewSessionDialog.chain.test.tsx, src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx, src/ui/sidebar/NewSessionDialog.task-input.test.tsx, src/ui/features/pretty-view/RoleCosmeticEditBlock.test.tsx</files>
  <read_first>
    - src/ui/api/voice-api.ts (full file — getVoices at lines 53-60; postSpeak, postSpeakStream, SAMPLE_PHRASE stay)
    - src/ui/features/pretty-view/ChatMessage.speak.test.tsx (line 33 mock)
    - src/ui/sidebar/NewSessionDialog.test.tsx (line 120 mock)
    - src/ui/sidebar/NewSessionDialog.chain.test.tsx (line 72 mock)
    - src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx (line 60 mock)
    - src/ui/sidebar/NewSessionDialog.task-input.test.tsx (line 52 mock)
    - src/ui/features/pretty-view/RoleCosmeticEditBlock.test.tsx (lines 32, 139, 151 — mock + explicit await test)
  </read_first>
  <action>
    Step A — Delete from voice-api.ts:
    - Remove the entire `export async function getVoices(): Promise<{display_name, filename}[]> { ... }` block (lines 53-60).
    - Preserve everything else BYTE-FOR-BYTE (postSpeak axios helper, postSpeakStream fetch helper, SAMPLE_PHRASE const, imports).
    - If `handleApiError` becomes unused after removal, remove its import too; if still used by postSpeak, leave it.

    Step B — Update the 6 test files. For each test file listed in read_first, find the `vi.mock("@/api/voice-api", ...)` block and remove the `getVoices` entry from the returned mock object. Keep every other mocked export (postSpeak, postSpeakStream, SAMPLE_PHRASE) intact. Do NOT delete the mock blocks entirely — the other exports are still needed by the tests.

    Step C — RoleCosmeticEditBlock.test.tsx at lines 139-151 has an explicit test that awaits `getVoices` to populate VoicePicker options. Rewrite that test's assertions: since VoicePicker now uses an inline const, the 7 voice options render synchronously on mount. Drop the `getVoices` await/mock-return setup and replace it with a synchronous assertion that the dropdown contains the 7 expected voiceId option values (or the number 7 option nodes). Preserve the surrounding test's intent (VoicePicker integrates into RoleCosmeticEditBlock).

    Step D — After deletions, `grep -rn "getVoices" src/ui/` MUST return 0 hits. If any hit remains (e.g. a stale test-comment reference), delete or update it.
  </action>
  <verify>
    <automated>test $(grep -rn 'getVoices' src/ui/ | wc -l) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'getVoices' src/ui/` returns 0 hits (source AND tests).
    - `src/ui/api/voice-api.ts` still exports `postSpeak`, `postSpeakStream`, `SAMPLE_PHRASE`; `getVoices` is deleted.
    - The 6 test files' `vi.mock("@/api/voice-api", ...)` blocks remain but no longer include `getVoices` in the mock return.
    - RoleCosmeticEditBlock.test.tsx's voice-picker test no longer awaits a mocked getVoices and instead synchronously asserts on rendered options.
    - `npx vitest run src/ui/features/pretty-view/ChatMessage.speak.test.tsx src/ui/sidebar/NewSessionDialog.test.tsx src/ui/sidebar/NewSessionDialog.chain.test.tsx src/ui/sidebar/NewSessionDialog.role-dropdown.test.tsx src/ui/sidebar/NewSessionDialog.task-input.test.tsx src/ui/features/pretty-view/RoleCosmeticEditBlock.test.tsx` — all green.
    - `tsc --noEmit` passes.
  </acceptance_criteria>
  <done>
    getVoices deleted from voice-api.ts; 6 test files updated; grep sweep clean; all touched tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VoicePicker → backend `/voice/voices` | Removed — no runtime fetch after this plan; static frontend const |
| VoicePicker → backend `/voice/speak` (sample play) | Preserved via postSpeak, JWT auth unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-03-01 | Information Disclosure | inline voice-id list in bundle | accept | 7 static voice IDs are non-secret AWS Polly public names; already documented in AWS public docs |
| T-98-03-02 | Tampering | user picks arbitrary voiceId | mitigate | Backend validator (Plan 07 identities.ts + Plan 06 voice.ts) rejects any value not in whitelist; frontend is just UI convenience |
| T-98-03-03 | Denial of Service | dead code (getVoices) still in bundle | accept | Deleted in Task 2; bundle-size decrease |
</threat_model>

<verification>
- `grep -rn 'getVoices' src/ui/` returns 0.
- VoicePicker renders 7 options + default fallback.
- All 6 touched test files pass.
</verification>

<success_criteria>
- getVoices completely removed from frontend (source + tests).
- VoicePicker uses inline POLLY_VOICES const.
- Client contract for postSpeak / postSpeakStream preserved unchanged.
- All touched vitest tests pass.
</success_criteria>

<output>
Create `.planning/phases/98-more-versatile-stt-tts-support-swap-local-rig-for-amazon-pol/98-03-SUMMARY.md` recording: (a) confirmation of inline const with 7 voices, (b) grep sweep result, (c) test files updated, (d) any incidental unused imports pruned.
</output>
