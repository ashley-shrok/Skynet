---
phase: 98-more-versatile-stt-tts-support-swap-local-rig-for-amazon-pol
plan: 07
type: execute
wave: 3
depends_on:
  - 98-02
  - 98-05
files_modified:
  - src/backend/database/routes/identities.ts
  - src/backend/database/routes/identity-birth.ts
  - src/backend/database/routes/identity-clone.ts
  - src/backend/database/routes/identities.get-disk.test.ts
  - src/backend/database/routes/identities.put-disk.test.ts
  - src/backend/database/routes/identity-birth-orchestrator.test.ts
  - src/backend/database/routes/identity-birth-orchestrator.role-frontmatter.test.ts
  - src/backend/database/routes/identity-birth-orchestrator.mxid-derivation.test.ts
autonomous: true
requirements:
  - P98-MIG-02
  - Per-identity-voice-binding
must_haves:
  truths:
    - "The IDENTITY_VOICE_RE regex is deleted; validation uses isValidPollyVoice from polly-voice-catalog.ts"
    - "identities.ts PUT endpoint returns 400 with a message referencing 'supported Polly voice IDs' when meta.voice is provided and fails isValidPollyVoice"
    - "identity-birth.ts default voice is either a valid Polly voice (Joanna) OR null/undefined — not 'Elena.wav'"
    - "identity-clone.ts value passthrough uses isValidPollyVoice for validation"
    - "Existing 'Elena.wav' test fixtures updated to a valid Polly voice ID or null (whichever preserves the test's intent)"
    - "This plan depends on Plan 05 migration completing (start-order) so that by the time validation tightens, existing frontmatter has been wiped"
  artifacts:
    - path: "src/backend/database/routes/identities.ts"
      provides: "PUT identity endpoint with Polly-whitelist voice validation"
      contains: "isValidPollyVoice"
    - path: "src/backend/database/routes/identity-birth.ts"
      provides: "identity birth flow with Polly-compatible default voice"
    - path: "src/backend/database/routes/identity-clone.ts"
      provides: "identity clone flow with Polly-whitelist voice validation"
  key_links:
    - from: "identities.ts:51 (validator site)"
      to: "polly-voice-catalog.ts::isValidPollyVoice"
      via: "import + call"
      pattern: "isValidPollyVoice"
    - from: "identity-birth.ts"
      to: "polly-voice-catalog.ts::isValidPollyVoice OR POLLY_VOICE_IDS"
      via: "default voice choice + validation on incoming payload"
      pattern: "isValidPollyVoice|POLLY_VOICE_IDS"
---

<objective>
Flip the identity voice-value validator from the old Chatterbox filename regex `/^[A-Z][A-Za-z]+\.wav$/` to the new Polly whitelist via `isValidPollyVoice` (per D-Per-identity-voice-binding + P98-MIG-02). Update the two identity-birth / identity-clone routes that carry a default voice value to either a Polly-compatible default (Joanna) or null. Sweep the ~9 test files that use `Elena.wav` as a fixture to a Polly voice.

CRITICAL SEQUENCING: This plan is Wave 3 and depends on Plan 05 (voice-migration wired to starter.ts) so that operators who ship this code get their existing identity/role frontmatter WIPED on first restart BEFORE Skynet accepts HTTP traffic — otherwise the validator would 400 on any existing identity's voice value at first read/edit.

Purpose: Tighten validation post-migration so no invalid voice value can persist. Kill the legacy filename shape.
Output: Validator flipped, defaults updated, test fixtures swept.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-more-versatile-stt-tts-support-swap-local-rig-for-amazon-pol/98-CONTEXT.md
@.planning/phases/98-more-versatile-stt-tts-support-swap-local-rig-for-amazon-pol/98-RESEARCH.md
@.planning/phases/98-more-versatile-stt-tts-support-swap-local-rig-for-amazon-pol/98-PATTERNS.md
@src/backend/voice/polly-voice-catalog.ts
@src/backend/database/routes/identities.ts
@src/backend/database/routes/identity-birth.ts
@src/backend/database/routes/identity-clone.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip identities.ts validator from IDENTITY_VOICE_RE regex to isValidPollyVoice whitelist</name>
  <files>src/backend/database/routes/identities.ts</files>
  <read_first>
    - src/backend/database/routes/identities.ts (line 51 for IDENTITY_VOICE_RE constant; lines 479-487 for the validation call-site — grep for `IDENTITY_VOICE_RE` to find both AND any other references)
    - src/backend/voice/polly-voice-catalog.ts (Plan 02 — isValidPollyVoice export)
    - .planning/phases/98-more-versatile-stt-tts-support-swap-local-rig-for-amazon-pol/98-PATTERNS.md § identities.ts:51 (MODIFIED — regex replacement + call-site update)
  </read_first>
  <action>
    In `src/backend/database/routes/identities.ts`:
    (a) Delete the `const IDENTITY_VOICE_RE = /^[A-Z][A-Za-z]+\.wav$/;` line (line 51).
    (b) Add import at the top: `import { isValidPollyVoice } from "../../voice/polly-voice-catalog.js";` (verify path — `identities.ts` is at `src/backend/database/routes/`, so relative path is `../../voice/polly-voice-catalog.js`).
    (c) Update the validation call-site (currently lines 479-487): replace `!IDENTITY_VOICE_RE.test(meta.voice)` with `!isValidPollyVoice(meta.voice)`. Update the error message from `"voice must match [A-Z][A-Za-z]+\\.wav"` to `"voice must be one of the supported Polly voice IDs"`.
    (d) Grep for any other `IDENTITY_VOICE_RE` references in the file — if any found (there shouldn't be per PATTERNS analysis, but confirm), replace or delete them.
    (e) Do NOT touch any other validators in this file (colorHue, displayName, avatarMime, etc.).
  </action>
  <verify>
    <automated>grep -c 'IDENTITY_VOICE_RE' src/backend/database/routes/identities.ts | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'IDENTITY_VOICE_RE' src/backend/database/routes/identities.ts` returns 0.
    - `grep -c 'isValidPollyVoice' src/backend/database/routes/identities.ts` returns >=1.
    - The error message string references "Polly voice IDs".
    - `tsc --noEmit` passes.
  </acceptance_criteria>
  <done>
    identities.ts validator flipped; regex gone; whitelist call in place.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update identity-birth.ts + identity-clone.ts default voices + validation shape</name>
  <files>src/backend/database/routes/identity-birth.ts, src/backend/database/routes/identity-clone.ts</files>
  <read_first>
    - src/backend/database/routes/identity-birth.ts (line 95 voice threading; line 381 parsedVoice write; grep for any DEFAULT_VOICE or "Elena.wav" reference)
    - src/backend/database/routes/identity-clone.ts (lines 294, 341, 345, 397, 682-683 per PATTERNS — value passthrough; line 15 comment about ≤100 chars)
    - .planning/phases/98-more-versatile-stt-tts-support-swap-local-rig-for-amazon-pol/98-PATTERNS.md § identity-birth.ts + identity-clone.ts (MODIFIED)
    - src/backend/voice/polly-voice-catalog.ts (Plan 02 export)
  </read_first>
  <action>
    identity-birth.ts:
    (a) Grep for `Elena.wav` and any hardcoded default voice constant. If a default like `"Elena.wav"` exists, replace with `"Joanna"` (a Polly generative voice) OR drop the default and let `voice` be omitted (null-safe) — pick per what the write-side of frontmatter tolerates (verify with the yaml serializer: `voice: null` should be OMITTED from the frontmatter, not written as `null`).
    (b) If identity-birth accepts an incoming payload with a `voice` field, validate via `isValidPollyVoice` before writing to frontmatter. Return 400 with the same "Polly voice IDs" error message shape as identities.ts.
    (c) The comment/docstring on line 95 (or wherever the voice threading is documented) should be updated to reference the Polly whitelist, not the old filename regex.

    identity-clone.ts:
    (a) The value passthrough at lines 294, 341, 345, 397, 682-683 should be gated by `isValidPollyVoice` — if the source identity carries an invalid voice value, clone MUST reject with 400 (matches the tightened validator behavior).
    (b) Line 15 comment about ≤100 chars can stay (Polly voice IDs are all <15 chars, so the guardrail is fine); update the comment to reference the whitelist as the primary check.
    (c) Line 682-683's conditional write-only-if-non-empty behavior stays verbatim (preserves the null-safe frontmatter write).

    Do NOT touch identity-artifact-reader.ts — RESEARCH marks it as consuming whatever value the migration + validator leave in place, unchanged in shape.
  </action>
  <verify>
    <automated>grep -c 'Elena.wav' src/backend/database/routes/identity-birth.ts src/backend/database/routes/identity-clone.ts | grep -E ':0$' | wc -l | grep -q '^2$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Elena.wav' src/backend/database/routes/identity-birth.ts` returns 0.
    - `grep -c 'Elena.wav' src/backend/database/routes/identity-clone.ts` returns 0.
    - Both files import `isValidPollyVoice` from `../../voice/polly-voice-catalog.js`.
    - Both files' voice validation uses the whitelist (grep `isValidPollyVoice`).
    - `tsc --noEmit` passes.
  </acceptance_criteria>
  <done>
    identity-birth + identity-clone updated to Polly whitelist defaults + validation.
  </done>
</task>

<task type="auto">
  <name>Task 3: Sweep the ~9 test fixture sites using 'Elena.wav' to Polly voice IDs and update assertions</name>
  <files>src/backend/database/routes/identities.get-disk.test.ts, src/backend/database/routes/identities.put-disk.test.ts, src/backend/database/routes/identity-birth-orchestrator.test.ts, src/backend/database/routes/identity-birth-orchestrator.role-frontmatter.test.ts, src/backend/database/routes/identity-birth-orchestrator.mxid-derivation.test.ts</files>
  <read_first>
    - src/backend/database/routes/identities.get-disk.test.ts (lines 330, 335 use Elena.wav)
    - src/backend/database/routes/identities.put-disk.test.ts (lines 460, 480 use Elena.wav)
    - src/backend/database/routes/identity-birth-orchestrator.test.ts (lines 182, 370 use Elena.wav)
    - src/backend/database/routes/identity-birth-orchestrator.role-frontmatter.test.ts (lines 166, 561, 582 use Elena.wav)
    - src/backend/database/routes/identity-birth-orchestrator.mxid-derivation.test.ts (line 146 uses Elena.wav)
  </read_first>
  <action>
    For each test file, replace occurrences of the string literal `"Elena.wav"` with `"Joanna"` (a valid Polly voice ID). Verify context of each usage — some may be:
    (a) Fixture setup — value passed as `voice:` in a test frontmatter or identity payload. Straight substitution.
    (b) Assertion — `expect(...).toBe("Elena.wav")` — substitute to `"Joanna"` to match new post-migration reality.
    (c) Comments — update if the comment references the old filename shape.

    After substitution, run each touched test file individually to confirm still green. If ANY test breaks structurally (not just because of the value change), STOP and surface — this is a signal that the test expected specific old-shape behavior that the new whitelist doesn't provide, and the test needs deeper rework than a straight substitution.

    Do NOT touch test files that reference `Elena.wav` in scenarios explicitly testing REJECTION (i.e. tests that assert "old shape should now fail validation"). If any such test exists (grep may reveal them), preserve its intent — those tests are the new coverage that the whitelist is enforcing.
  </action>
  <verify>
    <automated>test $(grep -rn 'Elena.wav' src/backend/database/routes/ 2>/dev/null | grep -v '/\*\|voice-migration\|VOICE_MIGRATION\|// old\|OLD_VOICE' | wc -l) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'Elena.wav' src/backend/database/routes/` returns 0 hits in test fixtures (excluding any comment-only preservation of the old-shape reference for regression documentation).
    - Each of the 5 test files runs green individually: `npx vitest run src/backend/database/routes/identities.get-disk.test.ts src/backend/database/routes/identities.put-disk.test.ts src/backend/database/routes/identity-birth-orchestrator.test.ts src/backend/database/routes/identity-birth-orchestrator.role-frontmatter.test.ts src/backend/database/routes/identity-birth-orchestrator.mxid-derivation.test.ts`.
    - If any test file is broken structurally, the task surfaces the failure (not silently masked).
    - `tsc --noEmit` passes.
  </acceptance_criteria>
  <done>
    Test fixtures swept from Elena.wav to Joanna (or valid substitute); all 5 touched test files green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend PUT /identities/:key → validator | User payload; whitelist enforced server-side |
| Frontend POST /identities/birth → validator | Identity creation payload; whitelist enforced |
| Frontend POST /identities/clone → validator | Clone payload; whitelist enforced |
| Migration script (Plan 05) → validator | Migration MUST run first to clear old-shape values before validator tightens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-07-01 | Tampering | validator bypass via URL-encoded voice value | mitigate | isValidPollyVoice uses Set-based exact-match on a decoded string; JSON body parser handles URL-decoding; no regex ReDoS surface (Set membership is O(1)) |
| T-98-07-02 | Tampering | validator bypass via Unicode homograph | mitigate | Set membership is byte-exact; homograph characters would produce a different string → rejected |
| T-98-07-03 | Denial of Service | pre-migration validator strand (operator ships this plan without Plan 05) | mitigate | depends_on: [98-05] in frontmatter — plan cannot execute out of order; wave 3 gates on wave 2 |
| T-98-07-04 | Information Disclosure | error message reveals whitelist contents | accept | Whitelist is 7 non-secret Polly voice names already documented in AWS public docs; error message says "Polly voice IDs" without enumerating them |
</threat_model>

<verification>
- `grep -c 'IDENTITY_VOICE_RE' src/backend/database/routes/identities.ts` returns 0.
- `grep -rn 'Elena.wav' src/backend/database/routes/` returns 0 hits.
- All 5 touched test files pass.
- `tsc --noEmit` passes.
</verification>

<success_criteria>
- Validator flipped to whitelist.
- Default voice values updated to Polly-compatible.
- Test fixtures swept to Polly voice IDs.
- No orphan references to Elena.wav or IDENTITY_VOICE_RE remain.
</success_criteria>

<output>
Create `.planning/phases/98-more-versatile-stt-tts-support-swap-local-rig-for-amazon-pol/98-07-SUMMARY.md` recording: (a) validator change site + before/after, (b) default voice change locations, (c) list of test files swept.
</output>
