---
phase: 115-identity-archiving-from-the-frontend
plan: 07
type: execute
wave: 4
depends_on:
  - 115-03
  - 115-04
  - 115-05
files_modified:
  - substrate/scripts/tests/agent-supervisor-archive-scan.sh
autonomous: true
requirements: []
must_haves:
  truths:
    - "Test coverage exists for the new user-initiated sentinel-scan branch: dropping `.archive-requested` triggers `retire_identity()` on the next reconcile tick invocation."
    - "Test coverage exists for the reordered retire flow: matrix → graceful exit → tmux kill → sentinel-delete → folder-move, in that order, observable via per-step marker files or log lines."
    - "Test coverage exists for guard bypass on the user-initiated path (pinned/no-dormancy/coordinator all present + `.archive-requested` present → retire STILL fires)."
    - "Test coverage exists for guard preservation on the 180-day path (pinned/no-dormancy/coordinator preserved → daily-path retire does NOT fire)."
    - "Test coverage exists for per-step failure retry: matrix 5xx → sentinel stays → next tick retries → 3rd failure → retire-stuck sentinel drops."
    - "Test coverage exists for sentinel-delete-before-move: a simulated crash between sentinel-delete and folder-move leaves the sentinel already gone; the folder-move retry becomes a state-2 resume-from-partial (existing Phase 94 behavior)."
    - "Test coverage exists for the ambient-monitor's SIGTERM fanout: a stub ambient-monitor with 4 stub children observes the children exit before retire step 3 (tmux kill)."
    - "All new tests are added to the existing `agent-supervisor-archive-scan.sh` harness — no new test files (per RESEARCH §8 guidance to extend the existing harness)."
  artifacts:
    - path: substrate/scripts/tests/agent-supervisor-archive-scan.sh
      provides: "Extended test harness — new test sections for sentinel-scan branch, reordered retire, guard bypass, per-step failure, ambient-monitor cleanup."
  key_links:
    - from: substrate/scripts/tests/agent-supervisor-archive-scan.sh
      to: substrate/scripts/agent-supervisor.sh (post-115-04)
      via: "sources supervisor with AGENT_SUPERVISOR_LIB_ONLY=1, invokes retire_identity() and scan_archive_requested_sentinels() directly"
      pattern: "AGENT_SUPERVISOR_LIB_ONLY|scan_archive_requested_sentinels"
---

<objective>
Extend the existing Phase 94 test harness (`substrate/scripts/tests/agent-supervisor-archive-scan.sh`, 841 lines) with the coverage matrix D-22 requires. This plan does NOT add new production code — it only adds test coverage that validates the code delivered by 115-03/04/05 works as specified.

Purpose: satisfy D-22's mandate ("the full retire flow has never fired in production — treat as untrusted, revalidate end-to-end") and pin the phase's behavioral contract so future regressions are caught. This is the last plan to run because it needs 115-04's reordered retire flow, 115-03's endpoint (for the sentinel drop half of the click chain — though this test file operates on the disk-sentinel directly since it's a bash test), and 115-05's archive-tree walk (to test sentinel-delete-before-move).

Output: extended bash test harness with all new test cases passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/115-identity-archiving-from-the-frontend/115-CONTEXT.md
@.planning/phases/115-identity-archiving-from-the-frontend/115-RESEARCH.md
@.planning/phases/115-identity-archiving-from-the-frontend/115-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend `agent-supervisor-archive-scan.sh` with sentinel-scan branch + reordered-retire tests</name>
  <files>substrate/scripts/tests/agent-supervisor-archive-scan.sh</files>
  <read_first>
    - substrate/scripts/tests/agent-supervisor-archive-scan.sh (all 841 lines — read verbatim. Understand the existing harness architecture: hermetic scratch dir + `AGENT_SUPERVISOR_LIB_ONLY=1` sourcing (L33, L77, L94), Python stub homeserver (L240-287), test cases at L502-680, static-analysis gates at L321-334).
    - substrate/scripts/agent-supervisor.sh POST-115-04 state — verify `scan_archive_requested_sentinels()` exists (defined near `run_archive_scan()` at L498-577 vicinity) and can be invoked as a top-level function when the supervisor is sourced with `AGENT_SUPERVISOR_LIB_ONLY=1`.
    - substrate/scripts/agent-supervisor.sh POST-115-04 state — verify `retire_identity()` has the new step order (matrix → graceful exit → tmux kill → sentinel-delete → folder-move).
    - substrate/scripts/ambient-monitor.py § `_do_shutdown()` at L596-618 (understand the SIGTERM fanout the test needs to simulate).
    - .planning/phases/115-identity-archiving-from-the-frontend/115-RESEARCH.md § 8 "Phase 94 test infrastructure" and § 11 "Untested-in-practice risk (D-22 depth)" — RESEARCH §11 spells out the required observations 1-9 and recommends the marker-file architecture.
    - .planning/phases/115-identity-archiving-from-the-frontend/115-CONTEXT.md § D-22 (test coverage requirements list) and § D-23 (test motion — bash test lands here at executor scope; full-suite is orchestrator territory).
  </read_first>
  <action>
    Extend the existing test file. Do NOT delete existing Phase 94 test cases — the daily-path coverage still needs to pass. Add new test cases as SIBLINGS to the existing suite, in a well-marked Phase 115 section near the end of the file (before the final `main` invocation and after the last existing test case).

    **Test 1: Sentinel-scan branch invocation triggers retire.**
    - Setup: hermetic scratch dir with `~/scratch/fleet/identities/alpha/` (identity md file + minimal relay.json for a stub homeserver that returns 200). Drop `.archive-requested` inside.
    - Boot the Python stub homeserver on a random port; write its URL into relay.json.
    - Source supervisor with `AGENT_SUPERVISOR_LIB_ONLY=1`, `IDENTITIES_DIR`, `IDENTITIES_ARCHIVE_DIR`, `DORMANCY_STATE_DIR` all pointed at the scratch dir subpaths.
    - Directly invoke `scan_archive_requested_sentinels`.
    - Assert:
      - `alpha` folder no longer exists in `IDENTITIES_DIR`.
      - `alpha` folder exists in `IDENTITIES_ARCHIVE_DIR`.
      - `.archive-requested` sentinel is gone (was deleted in step 4a per 115-04 Task 1).
      - Stub homeserver's request log shows the deactivate POST arrived with the right mxid/password (extend the stub's Content-Length body capture per RESEARCH §11 recommendation).
      - `retire-fail-count-user-alpha` counter file is absent (cleared on success).
      - Log output contains all four step entry lines: `retire step 1`, `retire step 2`, `retire step 3`, `retire step 4a`.

    **Test 2: Guard bypass on user-initiated path.**
    - Setup: `alpha` identity with `.pinned` + `.no-dormancy` + `coordinator: true` frontmatter in the md file (three guards ALL present).
    - Drop `.archive-requested`.
    - Invoke `scan_archive_requested_sentinels`.
    - Assert:
      - `alpha` folder IS moved to archive (retire fired — bypass worked).
      - Stub homeserver was called (matrix deactivate happened).
    - Fixture note: `is_coordinator` at L324-328 reads the identity md file's frontmatter. Create the md file with `coordinator: true` in the YAML frontmatter.

    **Test 3: Guards preserved on 180-day daily path.**
    - Setup: `beta` identity with `.pinned` present. NO `.archive-requested`. Freshness threshold artificially set to force the daily path to consider it (or invoke `run_archive_scan()` directly with fixture-controlled freshness).
    - Invoke `run_archive_scan` (or the appropriate function that gates on guards).
    - Assert:
      - `beta` folder is UNCHANGED — still in live tree, NOT moved to archive.
      - Stub homeserver was NOT called.
    - Repeat for `.no-dormancy` and for coordinator-frontmatter.

    **Test 4: Retire flow step-order observability.**
    - Setup: `gamma` identity with `.archive-requested`. Inject step-marker instrumentation: temporarily set env vars or use a wrapper script that touches `~/scratch/.state/retire-marker-<step>-<timestamp>` before each step's real code runs. RESEARCH §11 explicitly recommends this pattern.
    - Alternatively (simpler): grep the supervisor's log output (`log` function's stderr) for the four "retire step N" entry lines added in 115-04, and verify by scanning the log stream that the FIRST occurrence of each step's entry line appears in monotonic timestamp order matching D-13 (step 1 first, then step 2, etc).
    - Assert: matrix-deactivate log line appears BEFORE graceful-exit log line, graceful-exit BEFORE tmux-kill, tmux-kill BEFORE sentinel-delete, sentinel-delete BEFORE folder-move.
    - **Recommendation:** use the log-line approach — less invasive than marker files, and the log lines already exist post-115-04.

    **Test 5: Matrix deactivate 5xx failure → retire-stuck after 3 per-tick failures.**
    - Setup: `delta` identity with `.archive-requested`. Stub homeserver configured to return 500.
    - Invoke `scan_archive_requested_sentinels` three times in a row.
    - Assert after 1st call: `retire-fail-count-user-delta` counter = 1, sentinel STILL PRESENT (step 4a not reached), folder still in live tree.
    - Assert after 2nd call: counter = 2.
    - Assert after 3rd call: counter = 3, `retire-stuck` sentinel file exists at `IDENTITIES_ARCHIVE_DIR/delta/retire-stuck` (with `mkdir -p` semantics per 115-04's fix for the new-order case).
    - Verify LOUD log lines emit for each failed attempt (grep the log stream for "ERROR: 'delta' user-initiated archive: retire_identity FAILED").

    **Test 6: Sentinel-delete-before-move — mid-move crash safety.**
    - Setup: `epsilon` identity with `.archive-requested`. Stub homeserver returns 200. Populate `IDENTITIES_ARCHIVE_DIR/epsilon/` with a placeholder file BEFORE invoking, so the `mv` in step 4b hits collision-abort (State 3 in `retire_identity()`).
    - Invoke `scan_archive_requested_sentinels`.
    - Assert:
      - Matrix deactivate was called (step 1 fired).
      - `.archive-requested` sentinel is DELETED (step 4a fired before the move failure).
      - `epsilon` folder is STILL in live tree (step 4b aborted per collision-abort — existing Phase 94 State 3 behavior).
      - `retire_identity` returned 1.
      - Fail counter = 1.
    - Note: this test documents a subtle correctness edge — after step 4a but before step 4b, if the move aborts, the sentinel is gone. The next tick sees no sentinel and does NOT retry. This is intentional per D-12 rationale ("a mid-retire crash leaves the sentinel in place for the next tick to retry") — BUT the collision-abort case is a hard-abort, not a transient crash, so no retry is appropriate. Document this in the SUMMARY as an observed-and-accepted behavior.

    **Test 7: Ambient-monitor graceful shutdown observable.**
    - Setup: `zeta` identity with `.archive-requested`. Spawn a stub ambient-monitor script (a shell script that (a) writes its own pid to `~/scratch/.state/ambient-monitor-zeta.pid`, (b) traps SIGTERM to touch `~/scratch/.state/ambient-monitor-zeta.clean-exit` then exit, (c) spawns 4 stub child processes each of which also traps SIGTERM to touch a per-child marker before exiting).
    - The stub ambient-monitor "watches" a fake harness (another stub process). Kill the fake harness manually to simulate what step 2's `/exit` accomplishes in real life. Alternatively: skip the harness-watch simulation and just verify that step 2's fixed 11-second wait elapses (log timestamps prove it).
    - Assert after retire completes:
      - 4 per-child clean-exit markers exist.
      - Ambient-monitor's clean-exit marker exists.
      - All markers' mtimes are BEFORE the tmux-kill log line's timestamp (step 3 ran after ambient-monitor cleanup completed).
    - **Simplification note:** this test is the most infrastructure-heavy. If assembling the stub ambient-monitor + 4 children is too much work, MINIMAL version: just spawn ONE stub process that responds to SIGTERM by touching a marker, and verify (a) step 2's log lines emit in the right order, (b) at least the 11-second grace wait elapsed (compare timestamps of step-2-start and step-3-start log lines), (c) the marker exists after retire. Document the simplification in SUMMARY and note the deeper coverage as follow-up work (a Deferred item — not blocking).

    **Test 8: Sentinel-scan branch invocation on a non-existent identity — no crash.**
    - Setup: `.archive-requested` in a folder `~/scratch/fleet/identities/deleted/` where the identity md file is ALSO deleted (malformed state — the folder exists but the identity was hand-deleted).
    - Invoke `scan_archive_requested_sentinels`.
    - Assert: does NOT crash the supervisor. Log line emits an error but the function returns cleanly. Retire step 1 (matrix deactivate) will fail on relay.json missing → sentinel stays → fail counter increments → after 3 → retire-stuck.

    **Static-analysis gates:** the existing gates at L321-334 (bash syntax, shellcheck line-count ≤60, pitfall greps) run against `agent-supervisor.sh` — do NOT weaken them. If a new pitfall pattern surfaces from 115-04's changes, add a targeted gate rather than raising the line-count ceiling.

    **Where the tests live in the file:**
    - Add a new section header comment: `# ============ Phase 115 tests: user-initiated sentinel-scan branch ============`
    - Place immediately after the last existing Phase 94 test (near L680), before the harness's cleanup/summary code.
    - Reuse the existing helpers: `start_stub_homeserver`, hermetic scratch dir setup, `AGENT_SUPERVISOR_LIB_ONLY` sourcing. Do NOT duplicate helpers.
    - Extend `start_stub_homeserver` per RESEARCH §11 recommendation to log request bodies to a file (currently discards at L256 per RESEARCH §11) — needed for Test 1's assertion on the deactivate payload.

    **Do NOT run the test file inside this task's `<verify>`** — that would require the extended supervisor from 115-04 to already be on disk (which it is, given `depends_on`). Verify by running the harness end-to-end.
  </action>
  <verify>
    <automated>bash substrate/scripts/tests/agent-supervisor-archive-scan.sh</automated>
  </verify>
  <done>
    - `bash -n substrate/scripts/tests/agent-supervisor-archive-scan.sh` exits 0 (syntax valid).
    - `bash substrate/scripts/tests/agent-supervisor-archive-scan.sh` exits 0 (all tests pass — both existing Phase 94 cases AND all new Phase 115 cases).
    - `grep -c "Phase 115" substrate/scripts/tests/agent-supervisor-archive-scan.sh` returns at least 1 (the section header comment).
    - `grep -c "scan_archive_requested_sentinels" substrate/scripts/tests/agent-supervisor-archive-scan.sh` returns at least 5 (one per new test that invokes it, minus Test 3 which uses `run_archive_scan`).
    - Static-analysis gates at L321-334 still pass — no new shellcheck warnings introduced to `agent-supervisor.sh` by 115-04 that trip these gates (if they do, either fix them in a follow-up or justify in SUMMARY).
    - The extended stub homeserver logs request bodies (assertable in Test 1) — `grep -c "request_body_log\|Content-Length" substrate/scripts/tests/agent-supervisor-archive-scan.sh` returns at least 2.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → supervisor script (sourced with LIB_ONLY=1) | Test harness invokes production functions directly. No untrusted input crosses this boundary — the harness fully controls the environment. |
| Test harness → Python stub homeserver | Stub runs on localhost only, bound to 127.0.0.1, random port. No external exposure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-07-01 | Denial of Service | Test spawns processes that don't clean up (real tmux sessions, stub ambient-monitor) | mitigate | Use hermetic scratch dir; test harness must `trap EXIT cleanup` to kill any spawned subprocesses. Existing harness already has this discipline — extend it consistently. |
| T-115-07-02 | Information Disclosure | Stub homeserver logs including password fields from relay.json | mitigate | The stub is localhost-only; logging is to a scratch-dir file that's deleted on cleanup. Acceptable within the test-scratch trust model. |
| T-115-07-03 | Elevation of Privilege | Test invokes supervisor functions on real filesystem paths accidentally (e.g. missing scratch-dir override) | mitigate | The existing harness enforces `AGENT_SUPERVISOR_LIB_ONLY=1` + `IDENTITIES_DIR=<scratch>/...` overrides. NEW tests must inherit the same discipline — reuse the existing setup functions, do NOT construct fresh test infra outside them. |
| T-115-07-SC | Tampering | Test harness introduces new pip/npm/cargo deps | accept | This plan uses only stdlib bash + Python. No new dependencies. |
</threat_model>

<verification>
- Full run: `bash substrate/scripts/tests/agent-supervisor-archive-scan.sh` exits 0.
- All existing Phase 94 test cases still green (no regression from 115-04's retire-order change).
- All new Phase 115 test cases green.
- `shellcheck substrate/scripts/tests/agent-supervisor-archive-scan.sh` — new warning count is 0 relative to pre-plan baseline.
</verification>

<success_criteria>
- The 8 new test cases (or the simplification-noted subset) are green.
- Existing Phase 94 tests remain green — the retire order change didn't break the daily-path behavior.
- Sentinel-scan branch behavior is pinned by test coverage.
- Reordered retire step visibility is pinned by test coverage.
- Guard-bypass matrix is pinned by test coverage.
- Failure-retry semantics are pinned by test coverage.
</success_criteria>

<output>
Create `.planning/phases/115-identity-archiving-from-the-frontend/115-07-SUMMARY.md` when done, including:
- List of new test cases with pass/fail
- Any Test 7 simplifications and their justification
- Any existing Phase 94 test cases that needed adjustment for the retire reorder (should be 0 or a small handful — flag any)
- Line count of the file pre/post
- Confirmation that all four required D-22 coverage areas are tested:
  1. Sentinel-scan branch (Test 1)
  2. Retire flow new order (Test 4)
  3. Retire failure paths (Test 5)
  4. Idempotency (Tests 6 + 8)
- Deferred / follow-up gaps (things D-22 mentions that this plan did NOT close — call them out so Ashley can decide whether to add them later or accept)
</output>
