---
phase: 115-identity-archiving-from-the-frontend
plan: 05
type: execute
wave: 2
depends_on:
  - 115-02
files_modified:
  - substrate/scripts/fleet-status-sweep.py
  - src/backend/fleet-status/sweep-schema.ts
  - src/backend/fleet-status/sweep-schema.test.ts
  - substrate/scripts/tests/fleet-status-sweep.test.sh
autonomous: true
requirements: []
must_haves:
  truths:
    - "`fleet-status-sweep.py` enumerates both `~/fleet/identities/` and `~/fleet/identities-archive/`, emitting `archived: true` on rows sourced from the archive tree."
    - "`SweepIdentityLine` schema carries an `archived?: boolean` field (sibling to the existing pinned/dormant fields; the retired `hidden?` slot was already removed by 115-02)."
    - "Rows from the archive tree do NOT crash the JSONL-tail scan / role-file scan when files are missing — fail-open behavior."
    - "Sweep tests confirm archive-tree items emit with `archived: true` and live-tree items emit without `archived` (or with `archived: false`, matching the encoding choice in code)."
  artifacts:
    - path: substrate/scripts/fleet-status-sweep.py
      provides: "Unified _enumerate_identities walk over both roots with archived flag; _build_identity_line propagates archived into emit."
    - path: src/backend/fleet-status/sweep-schema.ts
      provides: "SweepIdentityLine.archived?: boolean field added; parity map/union updated."
    - path: src/backend/fleet-status/sweep-schema.test.ts
      provides: "Test coverage for the archived field parse."
    - path: substrate/scripts/tests/fleet-status-sweep.test.sh
      provides: "Bash test coverage for the unified walk emitting archived: true from identities-archive/."
  key_links:
    - from: substrate/scripts/fleet-status-sweep.py
      to: src/backend/fleet-status/sweep-schema.ts
      via: "emit dict keys match SweepIdentityLine field names exactly"
      pattern: "\"archived\":"
    - from: src/backend/fleet-status/sweep-schema.ts
      to: src/backend/fleet-status/ssh-poll-orchestrator.ts
      via: "SweepIdentityLine.archived flows into orchestrator's identity-line processing (which 115-06 will consume)"
      pattern: "archived"
---

<objective>
Grow the fleet-status sweep to enumerate `~/fleet/identities-archive/` in addition to `~/fleet/identities/`, emitting `archived: true` on rows from the archive tree. Add the `archived?: boolean` field to `SweepIdentityLine` on the TS side. Delivers D-18. Depends on 115-02 having removed the retired `hidden` field first (files_modified overlap: 115-02 owns the removal of the `hidden` field from `sweep-schema.ts`; this plan owns the addition of the `archived` field).

Purpose: give the frontend (115-06) a data source for the archived section rows. Per RESEARCH §5, the recommended approach is a UNIFIED walk (both roots in the same loop with a flag per-root), not a second parallel walk.

Output: sweep emits archived-tree rows; schema knows about `archived`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/115-identity-archiving-from-the-frontend/115-CONTEXT.md
@.planning/phases/115-identity-archiving-from-the-frontend/115-RESEARCH.md
@.planning/phases/115-identity-archiving-from-the-frontend/115-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unified walk over identities/ + identities-archive/ in fleet-status-sweep.py</name>
  <files>substrate/scripts/fleet-status-sweep.py, substrate/scripts/tests/fleet-status-sweep.test.sh</files>
  <read_first>
    - substrate/scripts/fleet-status-sweep.py § `_enumerate_identities` at L977-1018 verbatim (the current single-root walk).
    - substrate/scripts/fleet-status-sweep.py § `_build_identity_line` at L818-878 (the emit function that shapes each identity row).
    - substrate/scripts/fleet-status-sweep.py § `main()` at L1046-1124 (where _enumerate_identities is called and _build_identity_line is invoked per identity — verify no assumption breaks with archived rows).
    - substrate/scripts/fleet-status-sweep.py — any SAFE_NAME_RE definition (used to reject dangerous folder names during the walk).
    - substrate/scripts/tests/ — check for an existing `fleet-status-sweep.test.sh` or equivalent. If none exists, create one following the pattern of `substrate/scripts/tests/agent-supervisor-archive-scan.sh` (hermetic scratch dir + subprocess invocation of the script under test).
    - .planning/phases/115-identity-archiving-from-the-frontend/115-RESEARCH.md § 5 "Adding the archive tree" (the concrete Python diff sketch — copy this shape).
    - .planning/phases/115-identity-archiving-from-the-frontend/115-CONTEXT.md § D-18 (unified walk with `archived: true` sibling to pinned/dormant).
  </read_first>
  <behavior>
    - Test 1 (live-tree only): with `~/scratch/fleet/identities/alpha/` present (empty dir with the identity md file), sweep emits an identity line for `alpha` WITHOUT `archived: true` (either `archived: false` or field absent — pick one and enforce).
    - Test 2 (archive-tree only): with `~/scratch/fleet/identities-archive/beta/` present, sweep emits an identity line for `beta` WITH `archived: true`.
    - Test 3 (both roots): with `alpha` in live tree and `beta` in archive tree, sweep emits two identity lines — alpha without archived, beta with archived.
    - Test 4 (missing archive tree): with `~/scratch/fleet/identities-archive/` absent entirely (some hosts may not have any archived identities yet), sweep DOES NOT crash and emits only live-tree rows (FileNotFoundError caught → continue per the RESEARCH §5 diff sketch).
    - Test 5 (unsafe folder name in archive tree): a folder like `../evil` should be rejected by SAFE_NAME_RE and skipped with the `identity_name_skipped` log tag — sweep does not emit a line for it.
    - Test 6 (missing role.md / jsonl in archive tree): archived identity dir is a bare folder without role.md or jsonl — sweep still emits its line without crashing, with role_cosmetics/jsonl_path fields left null/absent (fail-open per RESEARCH §5 assumption A4).
  </behavior>
  <action>
    Rewrite `_enumerate_identities(home)` in `substrate/scripts/fleet-status-sweep.py` to walk both roots per RESEARCH §5's diff sketch:

    ```python
    def _enumerate_identities(home):
        out = []
        for root, archived_flag in [
            (os.path.join(home, "fleet", "identities"), False),
            (os.path.join(home, "fleet", "identities-archive"), True),
        ]:
            try:
                with os.scandir(root) as it:
                    for entry in it:
                        if not entry.is_dir(follow_symlinks=False):
                            continue
                        name = entry.name
                        if not SAFE_NAME_RE.match(name):
                            _log("identity_name_skipped", name=name[:40])
                            continue
                        # Same sentinel probes as current L977-1018 —
                        # BUT: the `.hidden` probe is deleted (per D-21).
                        # Existing probes retained: .dormant, .recycled-at,
                        # .recycle-requested, .pinned.
                        # NEW probe: .archive-requested (readable so the frontend
                        # could show a "retire in flight" indicator if it wanted —
                        # optional; if you don't want to plumb it now, omit and
                        # let 115-06 decide whether to surface it).
                        out.append({
                            "name": name,
                            "dormant": <probe>,
                            "recycled_at": <probe>,
                            "recycle_requested": <probe>,
                            "pinned": <probe>,
                            "archived": archived_flag,
                        })
            except FileNotFoundError:
                # archive tree may not exist yet on some boxes
                continue
            except OSError as e:
                _log("identities_scandir_failed", errno=e.errno, root=root)
                continue
        return out
    ```

    (a) Delete the `hidden` probe from the sentinel scan and the emitted dict (115-02 already deleted the schema field; this plan syncs the sweep script emission).
    (b) Add `.archive-requested` as a probe if you want that field visible on `SweepIdentityLine` — RECOMMENDATION: **omit for now**. RESEARCH §5 doesn't require it, and adding a `archive_requested?: boolean` to the schema is scope creep. The `archived: true` flag alone (indicating disk-location = archive/) is sufficient for D-18. If a future phase wants a "retire in flight" surface, add then.
    (c) Update `_build_identity_line` at L818-878 to propagate `archived` from the enum dict into the emitted line. If the current function reads its sentinel flags from a dict keyed by name, add `"archived": entry.get("archived", False)` to the emit dict.
    (d) **Archive-tree identities MAY have missing files** (relay.json, role.md, jsonl). Preserve fail-open reads — if the existing code raises on missing files, wrap in try/except so archive-tree rows still emit with sparse fields. RESEARCH §5 assumption A4 flags this: the orchestrator already has fail-open reads for sparse identities so this should be a straight-through change, but VERIFY during implementation.
    (e) Do NOT change `main()` orchestration — the loop that calls `_enumerate_identities` and iterates emits per-identity continues to work.
    (f) Cross-root name collision (same name in both live and archive): unlikely per retire-flow's collision-abort logic. If it happens, both records emit (frontend gets one live, one archived — RESEARCH §5 accepts this). No dedup here.

    Create or extend `substrate/scripts/tests/fleet-status-sweep.test.sh`:
    - Model after `substrate/scripts/tests/agent-supervisor-archive-scan.sh` (RESEARCH §8): hermetic scratch dir, invoke the sweep script as a subprocess with `HOME=<scratch>` overridden, capture stdout (which is JSONL), parse each line as JSON, assert on the schema.
    - Cover the 6 behavior cases above.
    - Use only stdlib bash + Python (no third-party parsers). `python3 -c 'import json,sys; ...'` for JSON parsing is fine.
  </action>
  <verify>
    <automated>bash substrate/scripts/tests/fleet-status-sweep.test.sh</automated>
  </verify>
  <done>
    - `python3 -c "import ast; ast.parse(open('substrate/scripts/fleet-status-sweep.py').read())"` completes without error (Python syntax valid).
    - `grep -c "identities-archive" substrate/scripts/fleet-status-sweep.py` returns at least 1.
    - `grep -c '"archived"' substrate/scripts/fleet-status-sweep.py` returns at least 1 (the emit key).
    - `grep -c '"hidden"' substrate/scripts/fleet-status-sweep.py` returns 0 (115-02 removed the schema field; this plan removes the sweep emission).
    - `bash substrate/scripts/tests/fleet-status-sweep.test.sh` exits 0 with all 6 test cases passing.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add `archived?: boolean` to SweepIdentityLine schema + test</name>
  <files>src/backend/fleet-status/sweep-schema.ts, src/backend/fleet-status/sweep-schema.test.ts</files>
  <read_first>
    - src/backend/fleet-status/sweep-schema.ts POST-115-02 state (the `hidden?` field is already removed; verify by reading L109-131 area — the SweepIdentityLine shape should now have {line_kind, schema_version, identity, dormant, recycled_at, recycle_requested, jsonl_path, layer1_recycling, role?, identity_cosmetics?, role_cosmetics?, pinned?}).
    - src/backend/fleet-status/sweep-schema.ts § SWEEP_FIELD_PARITY map at L353 area (the B8/B9-style parity keys; 115-02 dropped B9 for `hidden` — this plan adds a new key for `archived`, or repurposes the freed slot).
    - src/backend/fleet-status/sweep-schema.test.ts (existing test file — read the schema test pattern to mirror).
    - .planning/phases/115-identity-archiving-from-the-frontend/115-CONTEXT.md § D-18 (sibling to pinned/dormant on SweepIdentityLine).
    - .planning/phases/115-identity-archiving-from-the-frontend/115-RESEARCH.md § 5 (Approach recommendation — schema field name is `archived?: boolean`).
  </read_first>
  <behavior>
    - Test 1: parse a sweep-line JSON `{"line_kind":"identity","identity":"beta","dormant":false,"recycled_at":null,"recycle_requested":false,"pinned":false,"archived":true, ...}` succeeds and the parsed record's `.archived === true`.
    - Test 2: parse a sweep-line JSON without an `archived` field succeeds (optional field) — parsed record's `.archived` is `undefined` OR `false`, matching the schema's default choice.
    - Test 3: parse with `archived: "notabool"` (bad type) fails per the schema's validation discipline (zod, io-ts, or hand-rolled — mirror sibling fields' validation).
  </behavior>
  <action>
    (a) In `src/backend/fleet-status/sweep-schema.ts`, add `archived?: boolean` to `SweepIdentityLine`. Place it in the same position group as the other optional appearance-like fields (pinned, etc.) — RESEARCH §5 says "sibling to existing pinned, dormant".

    (b) Update the `SWEEP_FIELD_PARITY` map (L353 area). 115-02 dropped the `B9`/`hidden` entry. If the parity map is keyed by a schema-version-compatible discriminator (B8/B9/etc.), assign the new `archived` field to a fresh key that survives cross-version parsing. Choose either:
      - Reuse the freed `"B9"` slot for `archived` (simplest — the freed slot was dedicated to per-identity boolean sentinels, which is exactly what `archived` is).
      - Or bump to a new letter/number scheme if the parity map uses monotonic slot IDs.
    Pick whichever is consistent with how prior phases (94 = dormancy, 107 = hidden) added their slots; RESEARCH §5 doesn't specify but comment (L120-126) says the `?` on optional fields is for mid-distribution old boxes without those keys — so `archived?` is the right optionality.

    (c) Update the union type at L410-411 (post-115-02 state: `"B8"` only, since `"B9"` was removed). Re-add `"B9"` if you're reusing that slot for `archived`, or add whatever new slot ID you chose.

    (d) In `src/backend/fleet-status/sweep-schema.test.ts`, add the three test cases above. Follow the file's existing parse-test pattern.
  </action>
  <verify>
    <automated>npx vitest run src/backend/fleet-status/sweep-schema.test.ts</automated>
  </verify>
  <done>
    - `grep -c "archived" src/backend/fleet-status/sweep-schema.ts` returns at least 2 (field declaration + parity map entry).
    - `grep -F "hidden" src/backend/fleet-status/sweep-schema.ts` returns 0 matches.
    - `npx vitest run src/backend/fleet-status/sweep-schema.test.ts` reports 100% green including the 3 new tests.
    - `npx tsc --noEmit` clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → sweep script | Sweep script reads `~/fleet/identities-archive/` — a new tree. Folder names are validated by `SAFE_NAME_RE` (existing gate). |
| Sweep JSONL stdout → backend ssh-poll-orchestrator | Backend parses each line via `SweepIdentityLine` schema. Malformed input from a compromised host is rejected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-05-01 | Tampering | Malicious folder in `identities-archive/` with a name like `../evil` | mitigate | `SAFE_NAME_RE.match(name)` gate (existing) rejects unsafe names before enumeration proceeds. Test 5 in Task 1 covers this. |
| T-115-05-02 | Denial of Service | Archive tree with thousands of stale folders slows sweep | accept | `os.scandir` is O(N) and sweep runs on a fixed cadence. If the archive tree grows to unreasonable sizes, retention/pruning is a Deferred idea (per CONTEXT.md deferred list). Not a Phase 115 concern. |
| T-115-05-03 | Information Disclosure | Sweep leaks sensitive filenames from archive tree in stdout | accept | The sweep already emits identity names (which are public across the fleet). Adding archived-tree identity names doesn't expand the disclosure surface. |
| T-115-05-04 | Tampering | Backend schema parse of a wire message with `archived: <malicious>` | mitigate | Schema validation via the existing SweepIdentityLine parser (zod or equivalent). Test 3 confirms bad types are rejected. |
| T-115-05-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies added by this plan. |
</threat_model>

<verification>
- `bash substrate/scripts/tests/fleet-status-sweep.test.sh` passes.
- `npx vitest run src/backend/fleet-status/sweep-schema.test.ts` passes.
- `python3 -c "import ast; ast.parse(open('substrate/scripts/fleet-status-sweep.py').read())"` passes.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- Sweep script walks both live and archive trees in a single unified loop.
- `archived: true` appears on archive-tree rows only; live-tree rows either omit or emit `archived: false`.
- Schema has `archived?: boolean` field consistent with wire-parity discipline.
- Bash test coverage for the sweep + vitest coverage for the schema.
</success_criteria>

<output>
Create `.planning/phases/115-identity-archiving-from-the-frontend/115-05-SUMMARY.md` when done, including:
- The exact final parity-map slot ID used for `archived` (e.g. "B9 (reusing freed slot)" or "new slot Bk")
- The Python function signature/line for `_enumerate_identities` post-edit
- Whether `.archive-requested` is probed in the sweep (recommendation: NO — but document your choice)
- Any RESEARCH.md-cited line numbers that drifted
- Test-case pass/fail matrix for both the bash suite and the vitest suite
</output>
