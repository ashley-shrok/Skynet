---
phase: 115-identity-archiving-from-the-frontend
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/database/routes/identities.ts
  - src/backend/database/routes/user-preferences.ts
  - src/backend/fleet-status/sweep-schema.ts
  - src/backend/fleet-status/ssh-poll-orchestrator.ts
  - src/backend/fleet-status/identity-appearance.ts
  - src/backend/fleet-status/wire-protocol.ts
  - src/ui/api/user-preferences-api.ts
  - src/ui/api/identities-api.ts
  - src/ui/state/identities-store.ts
  - src/ui/state/conversation-store.ts
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/shell/IdentitySessionPane.tsx
  - src/backend/database/routes/identities.disk-read.test.ts
  - src/backend/database/routes/user-preferences.test.ts
  - src/ui/api/user-preferences-api.test.ts
  - src/backend/fleet-status/sweep-schema.test.ts
  - src/backend/fleet-status/wire-protocol.test.ts
  - src/backend/fleet-status/identity-appearance.test.ts
  - src/backend/fleet-status/ssh-poll-orchestrator.test.ts
  - src/ui/state/conversation-store.test.ts
  - src/ui/state/identities-store.enrichment.test.ts
  - src/ui/features/pretty-conversations/NewConversationModal.flow.test.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.role-management-flow.test.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.relay-room.test.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "No production code path anywhere reads or writes `.hidden` sentinels."
    - "No production code path references `hiddenIds`, `hideConversation`, `unhideConversation`, `deriveDiskHiddenIds`, `hydrateHiddenIdsFromServer`, `putHiddenIds`, or `getHiddenIds`."
    - "`SweepIdentityLine` no longer carries a `hidden` field."
    - "`publicIdentity()` no longer emits a `hidden` field."
    - "`user-preferences` PUT no longer accepts or fans out a `hiddenConversationIds` array."
    - "The vitest suite for all touched modules is green with zero references to `.hidden`."
  artifacts:
    - path: src/backend/claude-session/per-identity-file.ts
      provides: "Primitive layer — `.hidden` already removed by 115-01; this plan removes its consumers."
    - path: src/backend/database/routes/identities.ts
      provides: "GET /identities without `hidden` field on public identity or disk-fanout probe."
    - path: src/backend/database/routes/user-preferences.ts
      provides: "PUT /user-preferences without hiddenConversationIds validation or fanout."
    - path: src/backend/fleet-status/sweep-schema.ts
      provides: "SweepIdentityLine without `hidden` field."
    - path: src/ui/state/conversation-store.ts
      provides: "Store without `hiddenIds` slice, `hideConversation`, `unhideConversation`, `useHiddenIds`."
  key_links:
    - from: "any downstream module"
      to: "src/ui/state/conversation-store.ts"
      via: "no more useHiddenIds / hiddenIds imports"
      pattern: "hiddenIds|hideConversation|unhideConversation|useHiddenIds"
---

<objective>
Retire the entire Phase 107 `.hidden` code path per D-21. This plan runs in parallel with 115-01 (primitive-layer flip) — 115-01 owns the ALLOWED_REL_PATHS + primitive test change, this plan owns every OTHER call site. After this plan lands, the repo has zero production references to `.hidden` sentinels, `hiddenIds`, or the fanout PUT machinery.

Purpose: unblock 115-05 (which reuses the `SweepIdentityLine` `hidden` slot for `archived` — but that add lands in 115-05, not here; this plan JUST removes) and 115-06 (which will introduce the new archive frontend flow into the vacuum this deletion leaves).

Output: a clean, green, `.hidden`-free tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/115-identity-archiving-from-the-frontend/115-CONTEXT.md
@.planning/phases/115-identity-archiving-from-the-frontend/115-RESEARCH.md
@.planning/phases/107-hide-identity-rows-via-disk-sentinel-mirror-phase-92-for-the/107-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete backend `.hidden` — routes, fleet-status schema, wire protocol, and their tests</name>
  <files>src/backend/database/routes/identities.ts, src/backend/database/routes/user-preferences.ts, src/backend/fleet-status/sweep-schema.ts, src/backend/fleet-status/ssh-poll-orchestrator.ts, src/backend/fleet-status/identity-appearance.ts, src/backend/fleet-status/wire-protocol.ts, src/backend/database/routes/identities.disk-read.test.ts, src/backend/database/routes/user-preferences.test.ts, src/backend/fleet-status/sweep-schema.test.ts, src/backend/fleet-status/wire-protocol.test.ts, src/backend/fleet-status/identity-appearance.test.ts, src/backend/fleet-status/ssh-poll-orchestrator.test.ts</files>
  <read_first>
    - src/backend/database/routes/identities.ts (1014 lines — read L189-197 for the `hidden` param on `publicIdentity`, L211 for the pass-through, L252-257 for the return-object field, L402 for the comment, L414-424 for the parallel `.hidden` `identityFileExists` probe, L426 for the destructure, L441-449 for the `publicIdentity(...)` call. RESEARCH §2 enumerates every site.)
    - src/backend/database/routes/user-preferences.ts (685 lines — read L14-17 imports, L33-37 `HIDDEN_CONVERSATION_IDS_MAX_LENGTH` const, L79-82 GET response comment, L143-154 destructure, L249-278 validation block, L280 branch condition, L402-511 fanout block, plus any `echoHiddenFinal`/`didFanoutSentinels` references. RESEARCH §2 enumerates every site.)
    - src/backend/fleet-status/sweep-schema.ts (413 lines — read L109-131 for `SweepIdentityLine.hidden`, L353 for `SWEEP_FIELD_PARITY` `B9` entry, L410-411 for the `"B8"|"B9"` union.)
    - src/backend/fleet-status/ssh-poll-orchestrator.ts (read L1131-1162 — `appearanceFromIdentityLine` `hidden` usage).
    - src/backend/fleet-status/identity-appearance.ts (read L73-74, L134-136, L138, L200-201 — `hidden` in `ResolvedIdentityAppearance`).
    - src/backend/fleet-status/wire-protocol.ts (read L332, L366-383 — `hidden` in `IdentityAppearanceSchema`).
    - src/backend/database/routes/identities.disk-read.test.ts (read L423, L448, L491-513, L516-557 — all `HID-107-*` and `PUB-107-*` tests are candidates for deletion).
    - src/backend/database/routes/user-preferences.test.ts (locate all tests tagged `HID-107-PUT-*` — grep for `hidden` in the test file and delete matching describe/it blocks).
    - src/backend/fleet-status/sweep-schema.test.ts, wire-protocol.test.ts, identity-appearance.test.ts, ssh-poll-orchestrator.test.ts (grep for `hidden` — remove matching test cases and mock inputs).
    - .planning/phases/115-identity-archiving-from-the-frontend/115-RESEARCH.md § 2 (complete deletion inventory)
    - .planning/phases/115-identity-archiving-from-the-frontend/115-CONTEXT.md § D-21 (retire `.hidden` code path fully)
    - .planning/phases/107-hide-identity-rows-via-disk-sentinel-mirror-phase-92-for-the/107-CONTEXT.md (understand what Phase 107 added — this plan removes the same set)
  </read_first>
  <action>
    Delete every backend `.hidden` reference site enumerated in RESEARCH §2 "Backend read path" + "Backend write path" + "Backend fleet-status feeder + sweep":

    (1) `src/backend/database/routes/identities.ts`:
      - `publicIdentity()` signature (~L189-197): remove the `hidden: boolean = false` parameter. Update the JSDoc if the fn is documented.
      - `resolveIdentityAppearance` call site (~L211): drop the `hidden` argument.
      - Return object (~L252-257): drop the `hidden: resolved.hidden` field.
      - Disk-fanout wave (~L414-424): delete the parallel `.hidden` `identityFileExists` probe entirely.
      - Destructure (~L426): reduce `const [{ markdown }, pinned, hidden] = ...` to `const [{ markdown }, pinned] = ...`.
      - `publicIdentity(...)` call (~L441-449): drop the `hidden` positional argument.
      - Comment at ~L402: change "3 channels per identity (identity file + .pinned + .hidden)" to "2 channels per identity (identity file + .pinned)". Update L339 math comment if it references `24 → 16` per 8-slot cap.

    (2) `src/backend/database/routes/user-preferences.ts`:
      - Delete `HIDDEN_CONVERSATION_IDS_MAX_LENGTH` constant (~L33-37).
      - Delete the GET response comment about hidden slice (~L79-82).
      - Delete `hiddenConversationIds` from destructure/type (~L143-154).
      - Delete the validation block (~L249-278).
      - Simplify the branch condition at ~L280: `if (pinnedConversationIds !== undefined)` — drop the `|| hiddenConversationIds !== undefined` disjunct.
      - Delete the entire HIDDEN FANOUT block (~L402-511), including all `previousStates`, `postStates`, `echoHiddenFinal`, references to `identityHosts` that exist SOLELY for the hidden fanout.
      - `didFanoutSentinels` is likely set from BOTH the pin and hidden branches — after deletion only the pin branch remains. Simplify accordingly.
      - Grep the file for `echoHiddenFinal` and delete any response-echo code referencing it.
      - Keep the primitive imports (`writeIdentityFile`, `removeIdentityFile`, `identityFileExists`) — the pinned fanout still uses them (RESEARCH §2 explicit warning at "⚠ Actually").

    (3) `src/backend/fleet-status/sweep-schema.ts`:
      - Delete `hidden?: boolean` field from `SweepIdentityLine` (L109-131 vicinity).
      - Delete the `B9` entry in `SWEEP_FIELD_PARITY` (L353 area) and adjust the `"B8"|"B9"` union type at L410-411 to drop `"B9"`.
      - DO NOT add `archived` here — that's 115-05's job.

    (4) `src/backend/fleet-status/ssh-poll-orchestrator.ts`:
      - Delete `hidden` field usage in `appearanceFromIdentityLine` (~L1131-1162).

    (5) `src/backend/fleet-status/identity-appearance.ts`:
      - Delete `hidden: boolean` from `ResolvedIdentityAppearance` (L73-74).
      - Delete `hidden` from the resolver body / defaults / pass-through (L134-136, L138, L200-201).

    (6) `src/backend/fleet-status/wire-protocol.ts`:
      - Delete `hidden` from `IdentityAppearanceSchema` (L332, L366-383).

    Tests:
    - `identities.disk-read.test.ts`: delete every `HID-107-*` and `PUB-107-*` tagged test (grep the file to enumerate). Do NOT delete pinned-related tests (`PIN-*`).
    - `user-preferences.test.ts`: delete every test whose describe/it name contains "HID-107" or asserts hidden fanout behavior. Keep pinned fanout tests.
    - `sweep-schema.test.ts`, `wire-protocol.test.ts`, `identity-appearance.test.ts`, `ssh-poll-orchestrator.test.ts`: remove test cases/mock inputs that reference `hidden`.

    Do NOT introduce archived-side additions here — that's 115-05. This plan is a pure deletion.
  </action>
  <verify>
    <automated>npx vitest run src/backend/database/routes/identities.disk-read.test.ts src/backend/database/routes/user-preferences.test.ts src/backend/fleet-status/sweep-schema.test.ts src/backend/fleet-status/wire-protocol.test.ts src/backend/fleet-status/identity-appearance.test.ts src/backend/fleet-status/ssh-poll-orchestrator.test.ts</automated>
  </verify>
  <done>
    - `grep -rn '\.hidden' src/backend/database/routes/identities.ts src/backend/database/routes/user-preferences.ts` returns 0 matches (comments included — either updated to `.archive-requested`/`.pinned` or removed).
    - `grep -rn 'hiddenConversationIds\|HIDDEN_CONVERSATION_IDS\|echoHiddenFinal' src/backend/database/routes/user-preferences.ts` returns 0 matches.
    - `grep -rn 'hidden' src/backend/fleet-status/sweep-schema.ts src/backend/fleet-status/wire-protocol.ts src/backend/fleet-status/identity-appearance.ts src/backend/fleet-status/ssh-poll-orchestrator.ts | grep -v '^#' | grep -v '// '` returns 0 matches from executable lines.
    - Scoped vitest run above passes with 100% green.
    - `npx tsc --noEmit` (if the project runs TS at test time) reports no missing-field errors from the deletion.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete frontend `.hidden` — API layer, stores, panel, badge, and their tests</name>
  <files>src/ui/api/user-preferences-api.ts, src/ui/api/identities-api.ts, src/ui/state/identities-store.ts, src/ui/state/conversation-store.ts, src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx, src/ui/shell/IdentitySessionPane.tsx, src/ui/api/user-preferences-api.test.ts, src/ui/state/conversation-store.test.ts, src/ui/state/identities-store.enrichment.test.ts, src/ui/features/pretty-conversations/NewConversationModal.flow.test.tsx, src/ui/features/pretty-conversations/PrettyConversationsPanel.role-management-flow.test.tsx, src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx, src/ui/features/pretty-conversations/PrettyConversationsPanel.relay-room.test.tsx, src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx</files>
  <read_first>
    - src/ui/api/user-preferences-api.ts (134 lines — read L18-27 for putHiddenIds signature, L45-49 for `toBareIdentityKey` shared helper, L87-134 for full Phase 107 block).
    - src/ui/api/identities-api.ts (grep for `hidden` field on `Identity` type — see RESEARCH §2 "Frontend API layer").
    - src/ui/state/identities-store.ts (read L9-15 import, L37 `APPEARANCE_CACHE_KEY`, L217-258 `deriveDiskHiddenIds`, L529 `hydrateHiddenIdsFromServer` call inside `reprojectDiskPinHideIntoRows`).
    - src/ui/state/conversation-store.ts (read L227, L350, L399, L995-997, L1804-1880, L1941-1945, L2091, L2125-2128 per RESEARCH §2 "Frontend conversation-store").
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (2251 lines — sites at L231-256 canonicalHideIdForRow/isRowHidden, L281 hidden prop, L291 onToggleHide, L748 hiddenExpanded, L974-988 hiddenRows useMemo, L1154 comment, L1406-1430 handleToggleHide, L1848-1849/1881-1882/1911-1912/2021-2022 four render sites for `onToggleHide`, L1973-2031 Hidden section render).
    - src/ui/shell/IdentitySessionPane.tsx (555 lines — sites at L140-141 useHiddenIds, L150-152 isHidden calc, L170-182 Hide/Unhide menu block, L212-213 isHidden dep).
    - `.planning/phases/115-identity-archiving-from-the-frontend/115-RESEARCH.md` § 2 (frontend deletion inventory — every citation in this task's action ties back to specific line ranges there).
    - `.planning/phases/115-identity-archiving-from-the-frontend/115-CONTEXT.md` § D-21 (retire `.hidden` code path fully — no backwards-compat).
    - Prior-context: `.planning/phases/107-hide-identity-rows-via-disk-sentinel-mirror-phase-92-for-the/107-CONTEXT.md` (Phase 107 is the phase we're undoing; understand what it added).
  </read_first>
  <action>
    Delete every frontend `.hidden` site per RESEARCH §2. **CRITICAL**: this task DELETES the current Hide menu items on both the row and the badge — the Archive replacements are added in 115-06. Between this plan landing and 115-06 landing, users will lose the Hide affordance temporarily. That's acceptable because both plans ship together at deploy time (deploy motion sees the whole phase, not intermediate states).

    (1) `src/ui/api/user-preferences-api.ts`:
      - Delete `putHiddenIds()` export at ~L18-27 and ~L87-134.
      - Delete `getHiddenIds()` export if present.
      - Delete all Phase 107 comment blocks specifically about hidden.
      - KEEP `toBareIdentityKey` helper (~L45-49) — pinned still uses it.
      - KEEP `putPinnedIds` — pinned fanout stays.

    (2) `src/ui/api/identities-api.ts`:
      - Grep for `hidden` on the `Identity` type / on any response types; delete the field.

    (3) `src/ui/state/identities-store.ts`:
      - Delete `hydrateHiddenIdsFromServer` import (~L9-15).
      - Delete `deriveDiskHiddenIds` function entirely (~L217-258).
      - Delete `hydrateHiddenIdsFromServer(deriveDiskHiddenIds(identityHosts))` call inside `reprojectDiskPinHideIntoRows` (~L529). Rename the function to `reprojectDiskPinIntoRows` if the `Hide` part no longer applies.
      - **Bump appearance-cache schema key**: at L37 area, if `APPEARANCE_CACHE_KEY` is `"skynet:identities-appearance-cache:v1"`, bump to `"v2"` (per RESEARCH §10 runtime-state inventory — the cached identity records may carry `hidden` field from before this deploy). Confirm this key name/location during Read.

    (4) `src/ui/state/conversation-store.ts`:
      - Delete `hiddenIds: ReadonlySet<string>` from `ReadonlyConversationState` (~L227).
      - Delete `hiddenIds: Set<string>` from internal `State` (~L350).
      - Delete `hiddenIds: new Set<string>()` initial value (~L399).
      - Delete `hideConversation`, `unhideConversation`, `toggleHideConversation`, `hydrateHiddenIdsFromServer` exports (~L1804-1880).
      - Delete `useHiddenIds` hook (~L1941-1945).
      - Delete `hiddenIds: state.hiddenIds` in whatever getter block references it (~L2091).
      - Delete `resetHiddenIds` test helper (~L2125-2128).
      - Update comment about hiddenIds subscription + canonicalHideIdForRow (~L995-997) — remove hidden-specific lines.

    (5) `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`:
      - Delete `canonicalHideIdForRow` (~L231-239) and `isRowHidden` (~L240-256). These are Archive's discriminator too — 115-06 will re-introduce with a rename. If deleting causes a merge conflict with 115-06, 115-06 recreates them with new names.
      - Delete `hidden?: boolean` prop on `PrettyConversationRowLive` (~L281).
      - Delete `onToggleHide?: () => void` prop (~L291).
      - Delete `hiddenExpanded` state (~L748).
      - Delete `hiddenRows` useMemo (~L974-988).
      - Delete `handleToggleHide` handler (~L1406-1430). 115-06 re-introduces `handleArchive`.
      - Delete `onToggleHide={...}` on all 4 render sites (~L1848-1849, L1881-1882, L1911-1912, L2021-2022).
      - Delete the entire Hidden section render block (~L1973-2031). 115-06 re-introduces an Archived section.
      - Update comment at ~L1154 that references `handleToggleHide`.

    (6) `src/ui/shell/IdentitySessionPane.tsx`:
      - Delete `useHiddenIds()` call (~L140-141) — keep `usePinnedIds()`.
      - Delete `isHidden` variable (~L150-152).
      - Delete the Hide/Unhide menu item block (~L170-182). 115-06 replaces it with an Archive item.
      - Delete `isHidden` from useMemo deps (~L212-213).

    Frontend tests (delete all `.hidden` coverage; test files with mock stubs will each need a small edit):
    - `src/ui/api/user-preferences-api.test.ts` (~L207+ `API-107-01` test) — delete.
    - `src/ui/state/conversation-store.test.ts` — delete hidden-slice tests (grep for `hideConversation`, `hiddenIds`).
    - `src/ui/state/identities-store.enrichment.test.ts` — delete hidden-slice tests.
    - `src/ui/features/pretty-conversations/NewConversationModal.flow.test.tsx`, `PrettyConversationsPanel.role-management-flow.test.tsx`, `PrettyConversationsPanel.new-role-button.test.tsx`, `PrettyConversationsPanel.relay-room.test.tsx`, `PrettyConversationsPanel.test.tsx` — remove `.hidden`-related `vi.mock` stubs (e.g. `deriveDiskHiddenIds: () => []`, `hydrateHiddenIdsFromServer: vi.fn()`, `hideConversation: vi.fn()`) verbatim. Tests that referenced Hide as an affordance are dropped; 115-06 will add Archive affordance tests.
  </action>
  <verify>
    <automated>npx vitest run src/ui/api/user-preferences-api.test.ts src/ui/state/conversation-store.test.ts src/ui/state/identities-store.enrichment.test.ts src/ui/features/pretty-conversations/NewConversationModal.flow.test.tsx src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx src/ui/features/pretty-conversations/PrettyConversationsPanel.role-management-flow.test.tsx src/ui/features/pretty-conversations/PrettyConversationsPanel.new-role-button.test.tsx src/ui/features/pretty-conversations/PrettyConversationsPanel.relay-room.test.tsx</automated>
  </verify>
  <done>
    - `grep -rn 'hideConversation\|unhideConversation\|useHiddenIds\|hiddenIds\|putHiddenIds\|getHiddenIds\|deriveDiskHiddenIds\|hydrateHiddenIdsFromServer' src/ui/` returns 0 matches from production code (excluding this plan's own SUMMARY file if written there).
    - `grep -rn '\.hidden\b' src/ui/` returns 0 matches from production `.ts`/`.tsx` code.
    - Scoped vitest run above passes with 100% green.
    - `npx tsc --noEmit` reports no dangling references from the deletion.
    - `IdentitySessionPane.tsx` builds without `useHiddenIds` — verify by grep and by TS compile.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → backend HTTP | The removed PUT `/user-preferences` `hiddenConversationIds` array is no longer accepted; malformed requests that still include it must fail-closed (either ignored or rejected — the removed validation block explicitly rejected malformed arrays). |
| SweepIdentityLine wire → frontend | Old sweep frames that still carry `hidden: true` from pre-deploy managed hosts must not crash the frontend after the schema field is removed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-02-01 | Denial of Service | PUT /user-preferences with stale `hiddenConversationIds` payload from an old frontend build | mitigate | Zod/Yup schema (or hand-rolled destructure) on the PUT handler should reject unknown fields silently rather than crash. Verify the branch condition at L280 handles the `hiddenConversationIds !== undefined` case as a no-op (or reject with 400) rather than a crash. Test with an integration payload that includes the deleted field. |
| T-115-02-02 | Denial of Service | SweepIdentityLine parse on frontend seeing `hidden: true` from unpatched hosts | accept | `SweepIdentityLine.hidden?` was already optional; removing the schema field means parsers using strict Zod would reject. Recommendation: use `.passthrough()` on the Zod schema (or equivalent lax parse) — RESEARCH §2 didn't call this out as a concern because Ashley controls all managed hosts and D-20 says she'll deploy origin first then sweep hosts. Accept risk with the note in the SUMMARY. |
| T-115-02-03 | Information Disclosure | Stale `.hidden` sentinels on managed hosts after this plan lands | accept | Per D-20, Ashley manually converts these outside code scope. No security impact — the sentinel file is empty. |
| T-115-02-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies added by this plan — pure deletion. |
</threat_model>

<verification>
- Scoped vitest runs green (both tasks above).
- Grep sweep: `grep -rn 'hidden' src/ui/ src/backend/ --include=*.ts --include=*.tsx | grep -v -E '(hidden.md|test|comment|SUMMARY)' | grep -v '^#'` yields only matches that are unambiguously about non-Phase-107 concepts (e.g. `dbHidden` if such a term exists elsewhere for a different feature). Investigate any remaining hits; explain each in the SUMMARY.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- Zero production references to `.hidden`, `hiddenIds`, `hideConversation`, `unhideConversation`, `putHiddenIds`, `getHiddenIds`, `deriveDiskHiddenIds`, `hydrateHiddenIdsFromServer`, `hiddenConversationIds` in `src/`.
- Zero production references to the `hidden` field on `SweepIdentityLine`, `IdentityAppearance`, or `publicIdentity()`.
- All named test files pass with the scoped vitest command.
- Frontend still builds (`npx tsc --noEmit` clean; `npm run build` — if scoped-buildable — also clean).
</success_criteria>

<output>
Create `.planning/phases/115-identity-archiving-from-the-frontend/115-02-SUMMARY.md` when done, including:
- List of files touched with line-delta summary
- Any RESEARCH.md-cited line numbers that had drifted (report actual vs cited)
- Any remaining `hidden` string matches (with justification for each — e.g. "unrelated feature" or "in a comment about historical context")
- The APPEARANCE_CACHE_KEY bump from v1 → v2 (before/after)
- Any grep-based test-stub deletions that surfaced unexpectedly (files not in this plan's file list but that grepped positive for `.hidden` stubs)
</output>
