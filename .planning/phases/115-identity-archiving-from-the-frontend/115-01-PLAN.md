---
phase: 115-identity-archiving-from-the-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/claude-session/per-identity-file.ts
  - src/backend/claude-session/per-identity-file.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "The primitive per-identity-file layer accepts `.archive-requested` writes and refuses `.hidden` writes."
    - "The primitive-layer test suite covers the new allowlist admission and the removal of `.hidden`."
  artifacts:
    - path: src/backend/claude-session/per-identity-file.ts
      provides: "ALLOWED_REL_PATHS { 'relay.json', '.pinned', '.archive-requested' } — `.hidden` removed."
    - path: src/backend/claude-session/per-identity-file.test.ts
      provides: "Test coverage confirming `.archive-requested` admits and `.hidden` refuses."
  key_links:
    - from: src/backend/claude-session/per-identity-file.ts
      to: src/backend/database/routes/identity-archive.ts (introduced by 115-03)
      via: writeIdentityFile(key, '.archive-requested', ...)
      pattern: "ALLOWED_REL_PATHS.has\\(\\\"\\.archive-requested\\\"\\)"
---

<objective>
Flip the primitive allowlist at `src/backend/claude-session/per-identity-file.ts` so the new archive endpoint (introduced in 115-03) can drop `.archive-requested`, and so the retiring `.hidden` sentinel can no longer be written. This is the smallest possible primitive-layer change; every downstream plan (115-03 archive route, 115-04 supervisor sentinel scan, 115-06 frontend) depends on `.archive-requested` being in the allowlist.

Purpose: unblock Waves 2+ by landing the primitive change first. Delivers D-08 (sentinel filename `.archive-requested`) and the primitive half of D-21 (`.hidden` code path retired).

Output: `per-identity-file.ts` + its test file updated + green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/115-identity-archiving-from-the-frontend/115-CONTEXT.md
@.planning/phases/115-identity-archiving-from-the-frontend/115-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flip ALLOWED_REL_PATHS — add `.archive-requested`, remove `.hidden`</name>
  <files>src/backend/claude-session/per-identity-file.ts, src/backend/claude-session/per-identity-file.test.ts</files>
  <read_first>
    - src/backend/claude-session/per-identity-file.ts (full file — 395 lines; the allowlist itself is at L82-86 with the doc comment at L27-31 and L77-80. Read the whole file so you know exactly what admits the allowlist gate — `writeIdentityFile`, `removeIdentityFile`, `identityFileExists` all read it.)
    - src/backend/claude-session/per-identity-file.test.ts (existing test file — the whitelist assertion lives at L273-278 and the Phase 107 describe block for `.hidden` starts around L689. Read verbatim.)
    - .planning/phases/115-identity-archiving-from-the-frontend/115-RESEARCH.md § 10 "Sentinel `.archive-requested` addition to ALLOWED_REL_PATHS" (the concrete before/after diff)
    - .planning/phases/115-identity-archiving-from-the-frontend/115-CONTEXT.md § D-08 (sentinel filename locked as `.archive-requested`) and § D-21 (`.hidden` code path fully retired)
  </read_first>
  <behavior>
    - Test 1: `ALLOWED_REL_PATHS.has(".archive-requested")` returns `true`.
    - Test 2: `ALLOWED_REL_PATHS.has(".hidden")` returns `false`.
    - Test 3: `ALLOWED_REL_PATHS.has(".pinned")` still returns `true` (regression guard).
    - Test 4: `ALLOWED_REL_PATHS.has("relay.json")` still returns `true` (regression guard).
    - Test 5: `writeIdentityFile(name, ".archive-requested", "", { hostId: LOCAL, conn: null })` on a scratch identity dir succeeds and creates an empty file at `<dir>/.archive-requested`.
    - Test 6: `writeIdentityFile(name, ".hidden", "", { hostId: LOCAL, conn: null })` rejects — throws (or returns error consistent with the existing rejection pattern for out-of-allowlist basenames).
    - Test 7: `removeIdentityFile(name, ".archive-requested", { hostId: LOCAL, conn: null })` removes an existing `.archive-requested` file idempotently.
    - Test 8: `identityFileExists(name, ".archive-requested", { hostId: LOCAL, conn: null })` returns `true` when present, `false` when absent.
    - Test 9 (SSH REMOTE branch): if the existing test suite has a REMOTE branch test at L689+ for `.hidden`, mirror the same test structure for `.archive-requested` using the existing mocked SSH connection helper.
  </behavior>
  <action>
    In `src/backend/claude-session/per-identity-file.ts`:
    (a) At the `ALLOWED_REL_PATHS` `Set` literal (currently `{ "relay.json", ".pinned", ".hidden" }` at ~L82-86 per RESEARCH §10): remove the `".hidden"` entry and add `".archive-requested"`. Final set membership MUST be exactly `{ "relay.json", ".pinned", ".archive-requested" }` — no additions, no aliases.
    (b) Update the JSDoc-style comment block at L27-31 and the "bounded set of basenames" block at L77-80 to reference `.archive-requested` where `.hidden` is currently mentioned. Cite Phase 115 in the reference tag alongside any existing Phase-107 tag. Do NOT rewrite the comment body beyond swapping the basename references.
    (c) No signature or exported-symbol changes to `writeIdentityFile`, `removeIdentityFile`, `identityFileExists`.

    In `src/backend/claude-session/per-identity-file.test.ts`:
    (a) At the whitelist admission block (~L273-278): change the assertion so it asserts `.archive-requested` is admitted and `.hidden` is NOT admitted. Keep the existing `.pinned` and `relay.json` admission assertions untouched.
    (b) Locate the "Phase 107 Plan 107-01: .hidden sentinel primitive coverage" describe block (~L689 per RESEARCH). Rename to a Phase-115 equivalent describe title (e.g. "Phase 115 Plan 115-01: .archive-requested sentinel primitive coverage"). Inside the describe: every occurrence of the string `".hidden"` becomes `".archive-requested"`. Preserve every test's LOCAL and REMOTE branch coverage — same test cases, new basename. Existing test names update accordingly (e.g. `"writes .hidden LOCAL"` → `"writes .archive-requested LOCAL"`).
    (c) Add ONE new negative test asserting `writeIdentityFile(name, ".hidden", "", ...)` rejects — this pins the deletion behavior so a future regression can't sneak `.hidden` back into the allowlist without failing.
  </action>
  <verify>
    <automated>npx vitest run src/backend/claude-session/per-identity-file.test.ts</automated>
  </verify>
  <done>
    - `src/backend/claude-session/per-identity-file.ts` L82-86 `ALLOWED_REL_PATHS` literal contains exactly `"relay.json"`, `".pinned"`, `".archive-requested"` (no `.hidden`, no other entries).
    - `npx vitest run src/backend/claude-session/per-identity-file.test.ts` reports all tests passing with zero skipped.
    - `grep -F '".hidden"' src/backend/claude-session/per-identity-file.ts` returns empty.
    - `grep -F '".archive-requested"' src/backend/claude-session/per-identity-file.ts` returns at least one match at the allowlist literal.
    - The new negative test asserting `.hidden` rejects is present in `per-identity-file.test.ts` and passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend HTTP handler → per-identity disk write | Untrusted route input (identity key + rel path) crosses into filesystem write. `ALLOWED_REL_PATHS` is the last-mile check that rejects out-of-band basenames. |
| Local process → SSH REMOTE host | `writeIdentityFile` shells to `mkdir -p` + `printf > $file` on remote hosts via an ssh2 exec channel. The allowlist gate runs BEFORE any string reaches the SSH transport. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-01-01 | Tampering | `ALLOWED_REL_PATHS` set literal | mitigate | The literal is a frozen `ReadonlySet<string>` — cannot be mutated at runtime. Verify via type: `export const ALLOWED_REL_PATHS: ReadonlySet<string>` (existing). |
| T-115-01-02 | Elevation of Privilege | Route handler → `writeIdentityFile` with attacker-controlled `relPath` | mitigate | The allowlist gate is the sole authority for admissible basenames. New test asserts `.hidden` (a former ally now removed) rejects — this ensures a stale caller that still tries `.hidden` fails closed. Existing test coverage (per RESEARCH §2, §10) already covers path-traversal defenses like `../../etc/passwd`. |
| T-115-01-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies added by this plan — only edits to existing files. |
</threat_model>

<verification>
- `npx vitest run src/backend/claude-session/per-identity-file.test.ts` passes with 100% of tests in the file green.
- `grep -rn '"\.hidden"' src/backend/claude-session/per-identity-file.ts src/backend/claude-session/per-identity-file.test.ts` returns only matches inside the negative test (asserting `.hidden` is REJECTED) — no matches from production code or positive-admission tests.
</verification>

<success_criteria>
- ALLOWED_REL_PATHS composition is exactly `{ relay.json, .pinned, .archive-requested }`.
- The primitive-layer test file covers the new admission AND the negative rejection for `.hidden`.
- No downstream code in the repo has been touched by this plan — this is a scoped primitive flip only.
</success_criteria>

<output>
Create `.planning/phases/115-identity-archiving-from-the-frontend/115-01-SUMMARY.md` when done, summarizing:
- Exact final `ALLOWED_REL_PATHS` composition
- Number of `.hidden` test cases converted to `.archive-requested`
- Test file line count delta (rough)
- Any RESEARCH.md-cited line numbers that had drifted (report actual vs cited)
</output>
