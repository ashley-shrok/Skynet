---
phase: 78-passthrough-urls-file-url-scheme-phase-1-of-2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/editable-file-whitelist.ts
  - src/backend/utils/editable-file-whitelist.ts
  - src/ui/features/pretty-view/use-editable-file-eligibility.ts
  - src/ui/features/pretty-view/EditableFileModal.tsx
  - src/ui/api/editable-file-api.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Client-side detects the new URL shape https://<domain>/file/<hostname>/<abs-path> via SKYNET_FILE_URL_RE_CLIENT and includes it in the eligibility Set (per D-01)"
    - "EditableFileModal fetches from POST /pretty-view/fetch-host-file for file URLs and from POST /pretty-view/fetch-tailnet-url for tailnet URLs (dispatch by URL shape)"
    - "Both fetch helpers return the same TailnetFetchResult envelope shape - modal + eligibility hook consume without discrimination"
    - "Failure surfaces INSIDE the modal with human-readable copy (Host unreachable, File not found, Permission denied) - no stack traces, no HTTP status codes (per D-02, D-04)"
    - "EditableFileAffordance and ChatMessage's ReactMarkdown <a> override remain byte-for-byte unchanged - the eligibility Set drives everything (per RESEARCH § Anti-Patterns)"
  artifacts:
    - path: "src/ui/features/pretty-view/editable-file-whitelist.ts"
      provides: "New export SKYNET_FILE_URL_RE_CLIENT alongside TAILNET_URL_RE_CLIENT"
      contains: "SKYNET_FILE_URL_RE_CLIENT"
    - path: "src/backend/utils/editable-file-whitelist.ts"
      provides: "Mirror-rule docblock note referencing the new client-side regex (per Phase 40 D-02 bookkeeping)"
      contains: "SKYNET_FILE_URL_RE_CLIENT"
    - path: "src/ui/features/pretty-view/use-editable-file-eligibility.ts"
      provides: "Match loop scans BOTH regexes and dispatches byte-sniff to the correct fetch helper"
      contains: "SKYNET_FILE_URL_RE_CLIENT"
    - path: "src/ui/features/pretty-view/EditableFileModal.tsx"
      provides: "Open-effect dispatch by URL shape + error-copy mapping per URL class"
      contains: "fetchHostFileUrl"
    - path: "src/ui/api/editable-file-api.ts"
      provides: "New sibling helper fetchHostFileUrl(url) alongside fetchTailnetUrl(url)"
      exports: ["fetchHostFileUrl"]
  key_links:
    - from: "src/ui/features/pretty-view/use-editable-file-eligibility.ts"
      to: "src/ui/features/pretty-view/editable-file-whitelist.ts"
      via: "import { SKYNET_FILE_URL_RE_CLIENT }"
      pattern: "SKYNET_FILE_URL_RE_CLIENT"
    - from: "src/ui/features/pretty-view/use-editable-file-eligibility.ts"
      to: "src/ui/api/editable-file-api.ts"
      via: "import { fetchHostFileUrl }"
      pattern: "fetchHostFileUrl"
    - from: "src/ui/features/pretty-view/EditableFileModal.tsx"
      to: "src/ui/api/editable-file-api.ts"
      via: "import { fetchHostFileUrl }"
      pattern: "fetchHostFileUrl"
    - from: "src/ui/api/editable-file-api.ts"
      to: "backend POST /pretty-view/fetch-host-file"
      via: "authApi.post"
      pattern: "authApi.post"
---

<objective>
Ship the frontend half of Phase 78's file-URL feature (per D-01, D-02, D-04):

- Add a sibling regex SKYNET_FILE_URL_RE_CLIENT to src/ui/features/pretty-view/editable-file-whitelist.ts alongside TAILNET_URL_RE_CLIENT, matching https://<domain>[:port]/file/<hostname>/<abs-path> with the exact grammar locked in D-01.
- Add a mirror-rule docblock note to src/backend/utils/editable-file-whitelist.ts referencing the new client-side regex (Phase 40 D-02 bookkeeping; the URL regex proper does NOT ship server-side because the backend route does its own validation).
- Extend use-editable-file-eligibility.ts so its match loop scans BOTH regexes, dedupes with stripTrailingPunct, and its byte-sniff dispatch calls the correct fetch helper by URL shape.
- Extend EditableFileModal.tsx so its open-effect fetch dispatches by URL shape (tailnet URL to fetchTailnetUrl; file URL to new fetchHostFileUrl), and its in-body error copy maps the backend's error-class strings to human-readable sentences (Host unreachable, File not found, Permission denied on the host).
- Add a new sibling API helper fetchHostFileUrl(url) in src/ui/api/editable-file-api.ts that parses the URL client-side, POSTs {hostname, absolutePath} to /pretty-view/fetch-host-file, and returns the same TailnetFetchResult envelope.

Purpose: The frontend detection + modal dispatch stack from Phase 40 recognizes and handles the new URL scheme without touching EditableFileAffordance.tsx or ChatMessage.tsx's ReactMarkdown <a> override.

Output: Two whitelist edits (client + backend mirror note), one eligibility-hook extension, one modal open-effect extension, one new API helper function.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/78-passthrough-urls-file-url-scheme-phase-1-of-2/75-CONTEXT.md
@.planning/phases/78-passthrough-urls-file-url-scheme-phase-1-of-2/75-RESEARCH.md
@.planning/phases/78-passthrough-urls-file-url-scheme-phase-1-of-2/75-PATTERNS.md
@src/ui/features/pretty-view/editable-file-whitelist.ts
@src/backend/utils/editable-file-whitelist.ts
@src/ui/features/pretty-view/use-editable-file-eligibility.ts
@src/ui/features/pretty-view/EditableFileModal.tsx
@src/ui/features/pretty-view/EditableFileAffordance.tsx
@src/ui/features/pretty-view/ChatMessage.tsx
@src/ui/api/editable-file-api.ts
@src/ui/api/main-axios.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SKYNET_FILE_URL_RE_CLIENT to client whitelist + backend mirror-rule docblock note</name>
  <files>src/ui/features/pretty-view/editable-file-whitelist.ts, src/backend/utils/editable-file-whitelist.ts, src/ui/features/pretty-view/editable-file-whitelist.test.ts (create or extend if present)</files>
  <read_first>
    - src/ui/features/pretty-view/editable-file-whitelist.ts (REQUIRED — read the full file; the new regex is added alongside TAILNET_URL_RE_CLIENT at L96-97 with the same docblock discipline at L86-97 including the /g + .test() = broken warning at L89-91)
    - src/backend/utils/editable-file-whitelist.ts (REQUIRED — read the header docblock L1-25; the mirror-rule bookkeeping note is added here without adding the regex export itself — the URL regex proper only ships client-side because the new backend route does its own validation)
    - src/ui/features/pretty-view/EditableFileAffordance.tsx (verify no changes needed — it consumes the eligibility Set from the hook and is URL-agnostic)
    - src/ui/features/pretty-view/ChatMessage.tsx L60-100 + L320-345 (verify the ReactMarkdown a-tag override gate is eligibleUrls.has(href) and does not care about URL shape)
    - .planning/phases/78-passthrough-urls-file-url-scheme-phase-1-of-2/75-PATTERNS.md § "src/ui/features/pretty-view/editable-file-whitelist.ts (EXTEND — client-side)" and § "src/backend/utils/editable-file-whitelist.ts (EXTEND — backend mirror)"
  </read_first>
  <behavior>
    - Test 1: SKYNET_FILE_URL_RE_CLIENT matches "https://term.example.com/file/thenasty/home/ubuntu/note.md" via .match() (returns non-null array)
    - Test 2: "body text https://term.example.com/file/thenasty/home/ubuntu/note.md more text".match(SKYNET_FILE_URL_RE_CLIENT) returns an array containing exactly the URL string (no trailing prose)
    - Test 3: With port: matches "https://term.example.com:8080/file/thenasty/etc/hostname"
    - Test 4: With markdown-link trailing punctuation: stripTrailingPunct("(https://term.example.com/file/thenasty/foo.md)".match(SKYNET_FILE_URL_RE_CLIENT)![0]) returns the URL without a trailing close-paren
    - Test 5: Does NOT match http:// scheme (only https:// matches — agents on Skynet always run on HTTPS deployment)
    - Test 6: Does NOT match a tailnet URL (http://100.80.122.111:8001/foo) — that shape is exclusively TAILNET_URL_RE_CLIENT's domain
    - Test 7: Regex uses /g flag (assert SKYNET_FILE_URL_RE_CLIENT.flags.includes("g") is true) — required for .match() extraction pattern
    - Test 8: TAILNET_URL_RE_CLIENT behavior is unchanged — all existing tests for it still pass byte-for-byte
  </behavior>
  <action>
    In src/ui/features/pretty-view/editable-file-whitelist.ts, add a new export SKYNET_FILE_URL_RE_CLIENT immediately after TAILNET_URL_RE_CLIENT (currently at L96-97). The regex value is the exact source given in PATTERNS.md § "src/ui/features/pretty-view/editable-file-whitelist.ts (EXTEND — client-side)": https-scheme, DNS-legal domain plus optional port, literal /file/, hostname [a-zA-Z0-9._-]+, literal slash, absolute-path terminator [^\s)?#]+ — with the /g flag. Include a docblock above the export that: (a) documents the D-01 URL shape with the concrete example https://term.example.com/file/thenasty/home/ubuntu/note.md, (b) breaks down the grammar (scheme https:// only, DNS-legal domain + optional port, literal /file/, hostname [a-zA-Z0-9._-]+, literal slash, absolute-path [^\s)?#]+ stopping at whitespace / closing paren / query / fragment), (c) mirrors the /g + .test() = broken warning from TAILNET_URL_RE_CLIENT's docblock (L89-91) — subsequent .test() calls on this /g regex mutate .lastIndex; use .match() for extraction and a fresh non-global regex (or url.startsWith(...) shape guard) for dispatch decisions, (d) documents that trailing prose punctuation is trimmed downstream by stripTrailingPunct (H2 fix).

    In src/backend/utils/editable-file-whitelist.ts, update the header docblock at L1-25 to note that the client-side file has gained SKYNET_FILE_URL_RE_CLIENT (Phase 78, D-01) as a sibling to TAILNET_URL_RE_CLIENT, and re-affirm the Phase 40 D-02 mirror rule for EDITABLE_EXTENSIONS / EDITABLE_BASENAMES / classifyByExtension. Do NOT export the URL regex here — the backend route (pretty-view-fetch-host-file.ts from Plan 01) does its own hostname + path validation with /^[a-zA-Z0-9._-]+$/ etc. This mirrors the fact that TAILNET_URL_RE_CLIENT is client-only today and the backend uses its own anchored TAILNET_URL_RE inside the tailnet route.

    Create or extend src/ui/features/pretty-view/editable-file-whitelist.test.ts covering the 8 behaviors above. If an existing test file exists, add to it; if not, create it using the repo's Vitest idiom. When testing extraction (Tests 1, 2, 3), reset the regex's lastIndex to 0 before each call OR use a fresh copy of the regex per test to avoid /g-flag state bleeding between tests (matches the invariant documented in the new regex's docblock).

    DO NOT modify EDITABLE_EXTENSIONS, EDITABLE_BASENAMES, classifyByExtension, stripTrailingPunct, or TAILNET_URL_RE_CLIENT bodies — only additive changes to this file.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run src/ui/features/pretty-view/editable-file-whitelist.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "^export const SKYNET_FILE_URL_RE_CLIENT" src/ui/features/pretty-view/editable-file-whitelist.ts returns 1
    - grep -v '^\s*//' src/backend/utils/editable-file-whitelist.ts | grep -c "SKYNET_FILE_URL_RE_CLIENT" returns 0 (regex is NOT declared/exported server-side; docblock mention with `//` prefix is allowed)
    - grep -c "SKYNET_FILE_URL_RE_CLIENT" src/backend/utils/editable-file-whitelist.ts returns at least 1 (docblock mention in comments)
    - grep -c "^export const TAILNET_URL_RE_CLIENT" src/ui/features/pretty-view/editable-file-whitelist.ts still returns 1 (unchanged)
    - npx tsc --noEmit exits 0
    - npx vitest run src/ui/features/pretty-view/editable-file-whitelist.test.ts exits 0 with 8 tests passing
    - Manual inspection confirms the client regex value in the exported const equals the source string documented in PATTERNS.md § "src/ui/features/pretty-view/editable-file-whitelist.ts (EXTEND — client-side)" — scheme, domain+port, /file/, hostname class, path terminator class, /g flag
  </acceptance_criteria>
  <done>
    New client-side regex exported with matching docblock discipline; backend twin's docblock updated to note the mirror-rule bookkeeping without adding the regex; existing whitelist data + regex bodies unchanged; scoped vitest suite green; tsc clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add fetchHostFileUrl helper + extend eligibility hook + extend modal open-effect</name>
  <files>src/ui/api/editable-file-api.ts, src/ui/features/pretty-view/use-editable-file-eligibility.ts, src/ui/features/pretty-view/EditableFileModal.tsx, src/ui/api/editable-file-api.test.ts (create or extend if present), src/ui/features/pretty-view/use-editable-file-eligibility.test.ts (create or extend if present), src/ui/features/pretty-view/EditableFileModal.test.tsx (create or extend if present)</files>
  <read_first>
    - src/ui/api/editable-file-api.ts (REQUIRED — full read; add fetchHostFileUrl alongside fetchTailnetUrl at L61-73, using the same authApi.post + handleApiError pattern)
    - src/ui/api/main-axios.ts (understand authApi — JWT auto-attached via axios request interceptor; no manual header handling needed here)
    - src/ui/features/pretty-view/use-editable-file-eligibility.ts (REQUIRED — full read all 144 lines; extension points are the imports L30-35, match loop L69-72, and async byte-sniff dispatch L99-107; also read the cancellation pattern L44-54 comment + the closure-scoped `let cancelled = false` — this is a rev-3 H3 fix that MUST NOT regress to useRef(false))
    - src/ui/features/pretty-view/EditableFileModal.tsx (REQUIRED — full read of the useEffect at L106-159 including the fetch call at L122 and error-copy at L305-341; the new dispatch is at L120-125 and the error-copy update is at L305-341)
    - .planning/phases/78-passthrough-urls-file-url-scheme-phase-1-of-2/75-PATTERNS.md § "src/ui/api/editable-file-api.ts (EXTEND — sibling fetch helper)" and § "src/ui/features/pretty-view/use-editable-file-eligibility.ts (EXTEND — scan + dispatch)" and § "src/ui/features/pretty-view/EditableFileModal.tsx (EXTEND — fetch dispatch)"
    - .planning/phases/78-passthrough-urls-file-url-scheme-phase-1-of-2/75-RESEARCH.md § "Common Pitfalls Pitfall 6" (regex /g + .test() = intermittent stale results)
  </read_first>
  <behavior>
    fetchHostFileUrl helper:
    - Test 1: fetchHostFileUrl("https://term.example.com/file/thenasty/home/ubuntu/note.md") parses to {hostname: "thenasty", absolutePath: "/home/ubuntu/note.md"} and POSTs that body to /pretty-view/fetch-host-file via authApi.post
    - Test 2: With port in URL: fetchHostFileUrl("https://term.example.com:8080/file/thenasty/etc/hostname") still parses hostname=thenasty, absolutePath=/etc/hostname (port is on the Skynet domain, not the target host)
    - Test 3: On URL that doesn't match the shape (e.g. "garbage"), throws Error("invalid file URL") synchronously (no network call attempted)
    - Test 4: On axios error, calls handleApiError(error, "fetch host file URL") and re-throws (matches fetchTailnetUrl L69 pattern)
    - Test 5: On 200 response with envelope {contentBase64, sizeBytes, contentType, extension, filename, isTextByExt, isTextByBytes}, returns that envelope as TailnetFetchResult

    Eligibility hook extension:
    - Test 6: useEditableFileEligibility(eventId, "check https://term.example.com/file/thenasty/home/ubuntu/note.md") includes the file URL in the eligibility Set (assuming .md is whitelist-eligible via classifyByExtension)
    - Test 7: Message containing BOTH a tailnet URL and a file URL: both appear in the Set (deduped via Set + stripTrailingPunct)
    - Test 8: Byte-sniff dispatch chooses fetchHostFileUrl for file URLs and fetchTailnetUrl for tailnet URLs (mocked; verify each mock is called with the right URL)
    - Test 9: DISCARD-BYTES rule preserved — bytes returned from fetchHostFileUrl in the eligibility path are NEVER stashed for the modal (the modal fires its own fresh fetch on open, per D-04 invariant at hook L108-113)
    - Test 10: Cancellation pattern preserved — closure-scoped `let cancelled = false` still used; no regression to useRef(false) (rev-3 H3 fix)

    Modal open-effect extension:
    - Test 11: When modal opens with a URL matching /^https:\/\/[^/]+\/file\//, it calls fetchHostFileUrl(url) (not fetchTailnetUrl) — verify via mock
    - Test 12: When modal opens with a tailnet URL, it still calls fetchTailnetUrl(url) — no regression
    - Test 13: On backend error class "host_unreachable", modal renders human copy containing "Host unreachable" (or equivalent human sentence — no HTTP status code, no stack trace, per D-02)
    - Test 14: On backend error class "not_found", modal renders human copy containing "File not found"
    - Test 15: On backend error class "permission_denied", modal renders human copy containing "Permission denied" and (for host-file URLs) references the hostname
    - Test 16: On backend error class "too_large", modal renders human copy explaining the 2 MB cap
    - Test 17: D-04 fresh-fetch invariant preserved — modal opens fire a fresh fetch on every open; initialMtimeRef set ONCE at fetch-success (rev-3 D-06 invariant at analog L46-49); the DISCARD-BYTES rule holds
  </behavior>
  <action>
    In src/ui/api/editable-file-api.ts: Add a new exported async function fetchHostFileUrl(url: string): Promise<TailnetFetchResult> positioned immediately after fetchTailnetUrl. Parse the URL with the regex match /^https:\/\/[^/]+\/file\/([a-zA-Z0-9._-]+)\/(.*)$/ — on no match, throw Error("invalid file URL"). Extract hostname = match[1], absolutePath = "/" + match[2] (D-01 re-add-leading-slash rule). POST {hostname, absolutePath} to /pretty-view/fetch-host-file via authApi.post(...). On axios error, call handleApiError(error, "fetch host file URL") then re-throw — mirrors fetchTailnetUrl at L61-73 exactly. Return response.data as TailnetFetchResult. Include a docblock above the function that: (a) references D-01 and D-04, (b) documents the URL parsing + re-add leading slash step, (c) notes that consumers are useEditableFileEligibility (byte-sniff, DISCARDS bytes) and EditableFileModal (fresh fetch on open, surfaces error), (d) notes the response envelope is TailnetFetchResult — do NOT add a new discriminated-union type.

    In src/ui/features/pretty-view/use-editable-file-eligibility.ts: Extend the imports at L30-35 to add fetchHostFileUrl from @/api/editable-file-api and SKYNET_FILE_URL_RE_CLIENT from ./editable-file-whitelist. Extend the match loop (currently a single-regex messageBody.match(TAILNET_URL_RE_CLIENT) ?? [] at L69-72) to scan BOTH regexes and dedupe: rawTailnet = match on tailnet regex, rawFileUrl = match on file-URL regex, matches = Array.from(new Set([...rawTailnet, ...rawFileUrl].map(u => stripTrailingPunct(u)))). In the async byte-sniff dispatch (L99-107), dispatch by URL shape using a fresh non-global test: const isFileUrl = /^https:\/\/[^/]+\/file\//.test(url); (NOT SKYNET_FILE_URL_RE_CLIENT.test(url) — that would mutate .lastIndex on the /g regex per RESEARCH Pitfall 6). Call: const result = isFileUrl ? await fetchHostFileUrl(url) : await fetchTailnetUrl(url); All other invariants preserved: `if (cancelled) return;` guard, DISCARD-BYTES rule (bytes fetched here NEVER flow to the modal — L108-113 comment retained), closure-scoped `let cancelled = false` (rev-3 H3, do not regress to useRef).

    In src/ui/features/pretty-view/EditableFileModal.tsx: Extend the open-effect at L106-159. At L120-125 area (before the fetch call at L122), add const isFileUrl = /^https:\/\/[^/]+\/file\//.test(url); (fresh non-global test). Replace the direct fetchTailnetUrl(url) call with: const fetchPromise = isFileUrl ? fetchHostFileUrl(url) : fetchTailnetUrl(url); then the .then((result) => { ... }) chain unchanged (L129-147 rest is byte-identical). Add the fetchHostFileUrl import from @/api/editable-file-api.

    In the in-body error-copy block at L305-341, extend to detect the backend error-class string from the caught error (the backend returns {error: "<class>"} with distinct classes per Plan 01's taxonomy: unknown_host, not_found, permission_denied, too_large, host_unreachable, ssh_timeout, not_a_file, path_forbidden, path_traversal, invalid_hostname). For each class, render human-readable copy (planner's discretion on exact sentences, but MUST NOT include HTTP status codes or stack traces — per D-02, D-04). Suggested copy:
    - host_unreachable → "Host unreachable — the box may be offline or the SSH channel is down."
    - not_found → "File not found: <absolutePath>" (safe to include the path — user typed/saw it in the URL, no info leak)
    - permission_denied → "Permission denied on <hostname> — the Skynet SSH user can't read this file. Ask the box owner to widen access."
    - too_large → "File too large — the 2 MB cap keeps the editor responsive. Try a smaller slice."
    - not_a_file → "Not a regular file — directories, sockets, and device files aren't viewable via file URLs."
    - path_forbidden → "Path forbidden — /proc, /sys, and /dev are not accessible via file URLs."
    - path_traversal → "Invalid path — . and .. segments aren't allowed."
    - unknown_host → "Unknown host: <hostname> — it's not registered in this Skynet, or you don't have access to it."
    - ssh_timeout → "SSH timeout — the host is slow or unreachable. Try again in a moment."
    - default / invalid_hostname / invalid_body → "Something went wrong fetching the file — try again, or check the URL."

    Preserve the existing D-04 "fresh fetch + visible failure over silent stale" comment and D-06 "initialMtimeRef set ONCE at fetch-success" comment — do not delete them.

    Create or extend src/ui/api/editable-file-api.test.ts covering Tests 1-5. Add tests for the eligibility hook (Tests 6-10) either extending an existing use-editable-file-eligibility.test.ts or creating one. Add tests for the modal open-effect (Tests 11-17) either extending an existing EditableFileModal.test.tsx or creating one. Use the repo's existing test idiom (grep the pretty-view dir for *.test.ts?x? files to match the pattern). If Vitest + Testing Library are the idiom, mock authApi.post, fetchTailnetUrl, and fetchHostFileUrl as appropriate.

    DO NOT touch EditableFileAffordance.tsx or ChatMessage.tsx's ReactMarkdown a-tag override — the eligibility Set drives everything and both files are URL-agnostic (RESEARCH § Anti-Patterns "Rewriting EditableFileAffordance.tsx or ChatMessage.tsx's a-tag override").
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run src/ui/api/editable-file-api.test.ts src/ui/features/pretty-view/use-editable-file-eligibility.test.ts src/ui/features/pretty-view/EditableFileModal.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "^export async function fetchHostFileUrl" src/ui/api/editable-file-api.ts returns 1
    - grep -c "^export .*fetchTailnetUrl" src/ui/api/editable-file-api.ts still returns 1 (unchanged)
    - grep -c "SKYNET_FILE_URL_RE_CLIENT" src/ui/features/pretty-view/use-editable-file-eligibility.ts returns at least 1
    - grep -c "fetchHostFileUrl" src/ui/features/pretty-view/use-editable-file-eligibility.ts returns at least 1
    - grep -c "fetchHostFileUrl" src/ui/features/pretty-view/EditableFileModal.tsx returns at least 1
    - grep -c "let cancelled = false" src/ui/features/pretty-view/use-editable-file-eligibility.ts returns at least 1 (rev-3 H3 preserved)
    - grep -c "useRef(false)" src/ui/features/pretty-view/use-editable-file-eligibility.ts returns 0 (rev-3 H3 not regressed)
    - grep -Ec "SKYNET_FILE_URL_RE_CLIENT\.test\(" src/ui/features/pretty-view/use-editable-file-eligibility.ts returns 0 (dispatch uses fresh non-global regex per Pitfall 6)
    - grep -Ec "SKYNET_FILE_URL_RE_CLIENT\.test\(" src/ui/features/pretty-view/EditableFileModal.tsx returns 0 (dispatch uses fresh non-global regex per Pitfall 6)
    - git diff src/ui/features/pretty-view/EditableFileAffordance.tsx returns empty (no changes)
    - git diff src/ui/features/pretty-view/ChatMessage.tsx returns empty (no changes)
    - npx tsc --noEmit exits 0
    - Scoped vitest suites for editable-file-api, use-editable-file-eligibility, and EditableFileModal all pass (17 new tests total across the three files)
    - Manual inspection of the EditableFileModal error-copy block confirms 9 human-readable sentences (one per error class) with no HTTP status codes and no stack traces
  </acceptance_criteria>
  <done>
    fetchHostFileUrl helper exists and mirrors fetchTailnetUrl; eligibility hook scans both regexes and dispatches by URL shape without mutating /g regex state; modal open-effect dispatches by URL shape and surfaces classified errors as human copy; EditableFileAffordance and ChatMessage untouched; scoped test suites green; tsc clean; rev-3 H3 cancellation pattern preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Chat message body -> React rendering | Message text from agent-authored session file; content is untrusted for XSS purposes (existing pretty-view rendering handles this) |
| React modal -> Skynet backend | Authenticated JWT-bearing POST with user-influenced body ({hostname, absolutePath}) |
| Skynet backend response -> React modal render | Backend returns classified error string OR bytes+envelope; modal renders both without eval/unescape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-F1 | Tampering | Malicious URL crafted to match regex but exploit modal fetch dispatch | mitigate | Client-side regex is anchored on https:// scheme, DNS-legal domain class, and file/ literal; URL parsing in fetchHostFileUrl uses a strict match with explicit character classes for hostname [a-zA-Z0-9._-] before POST; backend re-validates the same shape (Plan 01) — defense in depth |
| T-78-F2 | Information Disclosure | Modal renders raw backend error message (e.g. err.message) as UI text, leaking paths / usernames | mitigate | Modal error-copy block detects the {error: "<class>"} string ONLY and renders the pre-authored human sentence; err.message is logged client-side to console (for debugging) but NEVER surfaced as user-facing text; matches Plan 01's T-78-05 mitigation |
| T-78-F3 | Information Disclosure | React DevTools / network tab expose sensitive response data | accept | Standard web-app risk; JWT-authenticated route + per-user-per-host RBAC in Plan 01 ensures the response contains only files the current user has read access to; nothing new for this feature |
| T-78-F4 | Spoofing | Chat message includes attacker-crafted URL to a different Skynet domain | mitigate | Regex requires https:// + DNS-legal domain; the ONLY backend hit is /pretty-view/fetch-host-file on the current Skynet (fetchHostFileUrl POSTs to a relative path via authApi — cross-origin URLs cannot redirect the fetch target); if the URL's domain in a message body is not the current Skynet's domain, the click opens a new browser tab to that other Skynet, not this one — outside our threat surface |
| T-78-F5 | Denial of Service | Modal spam-opens many URLs, exhausting backend SSH pool | accept | Modal is user-initiated per click; user cannot open >1 modal at a time (existing modal singleton behavior); pool cap in Plan 01 (T-78-09) handles concurrent load |
| T-78-F6 | Repudiation | Client-side dispatch mistakes tailnet URL for file URL or vice versa | mitigate | Both consumers use a fresh non-global regex /^https:\/\/[^/]+\/file\// for dispatch — no /g-flag state leak per Pitfall 6; test coverage in Task 2 asserts each mock called with correct URL |
| T-78-F7 | Tampering | Whitelist regex drift between frontend and backend | mitigate | Backend twin's docblock updated with mirror-rule bookkeeping (Task 1); backend route (Plan 01) does its own hostname + path validation with equivalent character classes; drift would be caught in code review + would cause behavior divergence detectable by end-to-end smoke |
| T-78-FSC | Tampering | Package installs | accept | No new npm packages installed by this plan (all resident deps: react, axios via existing authApi). RESEARCH § "Package Legitimacy Audit" confirms N/A. |
</threat_model>

<verification>
- Frontend TS graph compiles: npx tsc --noEmit exits 0
- Whitelist tests pass: npx vitest run src/ui/features/pretty-view/editable-file-whitelist.test.ts exits 0 with 8 tests
- API helper tests pass: npx vitest run src/ui/api/editable-file-api.test.ts exits 0 with 5 tests
- Eligibility hook tests pass: npx vitest run src/ui/features/pretty-view/use-editable-file-eligibility.test.ts exits 0 with 5 tests (Tests 6-10)
- Modal open-effect tests pass: npx vitest run src/ui/features/pretty-view/EditableFileModal.test.tsx exits 0 with 7 tests (Tests 11-17)
- Zero-regression check: EditableFileAffordance.tsx and ChatMessage.tsx are byte-identical (git diff empty)
- Pitfall 6 gate: no .test() call on either /g regex in the eligibility hook or the modal (grep-verified)
</verification>

<success_criteria>
- All must_haves in frontmatter are observable via the verification block above
- New URL shape detected client-side and rendered with the existing Phase 40 pencil affordance (via eligibility Set) — no changes needed to EditableFileAffordance or ChatMessage
- Modal fetches from the correct backend endpoint based on URL shape
- Errors surface as human-readable copy per D-02 — no HTTP status codes, no stack traces
- Zero regression to Phase 40's tailnet-URL flow (byte-for-byte behavior preserved)
- Zero new npm dependencies added
</success_criteria>

<output>
Create .planning/phases/78-passthrough-urls-file-url-scheme-phase-1-of-2/78-02-SUMMARY.md when done, documenting: files created, files modified, exact fetchHostFileUrl signature + URL-parsing regex, the 9-class error-copy mapping table, and any deviations from the plan (e.g. if error-copy sentences were reworded during implementation, or if the modal's error-mode threading required extra prop plumbing).
</output>
