---
phase: 41-defer-terminal-view-mount-until-user-summons-it
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/state/session-working-store.ts
  - src/ui/state/session-tmux-store.ts
  - src/ui/state/session-tmux-store.test.ts
  - src/ui/state/session-working-store.test.ts
  - src/ui/AppShell.tsx
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-view/PrettyView.aside.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "PrettyView derives isIdle from the fleet-status broadcast (session-working-store) — no longer reads the Terminal-supplied isIdle prop"
    - "PrettyView's isIdle preserves three-state semantics: null when the broadcast has never delivered for this session, false when broadcast says working, true when broadcast says idle — matching Terminal's pre-Phase-41 null-on-first-mount behavior"
    - "AppShell's tmuxSessionNames record is populated from the fleet-status broadcast in addition to Terminal's onTmuxSessionChange callback — populate-once from broadcast is idempotent with populate-from-callback (no thrash, no double-title-flip)"
    - "Session-tmux-store is a per-(hostId, tmuxSession) → tmuxSession-name lookup fed by AppShell's fleet-status onSnapshot + onUpdate callbacks and cleared by onGone; mirrors session-working-store.ts pattern (module-scoped Map + useSyncExternalStore hook)"
    - "AppShell's document.title effect still resolves via identity displayName → tmux → tab label → SKYNET fallback chain unchanged (no regression to existing title logic)"
    - "User-visible behavior is UNCHANGED after this plan lands standalone: WipBubble, ready-dot, aside-arm gate, ComposeBox idle-send gate, and tab title all behave identically to pre-plan (isIdle was already null in production per Phase 34 vestigial state; the store-derived isIdle now produces the same null-on-first-mount followed by real signal-driven false/true)"
    - "Package-legitimacy audit not applicable — zero new npm/pip/cargo installs"
  artifacts:
    - path: src/ui/state/session-working-store.ts
      provides: "useSessionIsWorkingRaw hook — three-state variant returning boolean | null (null for absent keys, false for idle-published, true for working-published)"
      contains: "useSessionIsWorkingRaw"
    - path: src/ui/state/session-tmux-store.ts
      provides: "New store: publishFleetStatusTmuxSession + useSessionTmuxName + __resetForTest, mirroring session-working-store.ts pattern verbatim"
      contains: "publishFleetStatusTmuxSession"
    - path: src/ui/state/session-tmux-store.test.ts
      provides: "Vitest coverage for session-tmux-store (publish/hook/null-key/gone/unknown-key/multi-key isolation)"
      contains: "session-tmux-store"
    - path: src/ui/state/session-working-store.test.ts
      provides: "Extended coverage for useSessionIsWorkingRaw (null-on-absent, false-on-idle-publish, true-on-busy-publish)"
      contains: "useSessionIsWorkingRaw"
    - path: src/ui/AppShell.tsx
      provides: "Extended fleet-status client callbacks — onSnapshot/onUpdate additionally call publishFleetStatusTmuxSession(state.hostId, state.tmuxSession); onGone additionally calls publishFleetStatusTmuxSession(hostId, null)"
      contains: "publishFleetStatusTmuxSession"
    - path: src/ui/features/pretty-view/PrettyView.tsx
      provides: "Internal isIdle derivation: const isIdleDerived = useSessionIsWorkingRaw(sessionWorkingKey) mapped to boolean|null; existing isIdle prop is still accepted (backward-compat during transition) but ignored in favor of the derived value"
      contains: "useSessionIsWorkingRaw"
  key_links:
    - from: "src/ui/AppShell.tsx"
      to: "src/ui/state/session-tmux-store.ts"
      via: "publishFleetStatusTmuxSession call inside onSnapshot/onUpdate/onGone callbacks of createFleetStatusClient"
      pattern: "publishFleetStatusTmuxSession"
    - from: "src/ui/features/pretty-view/PrettyView.tsx"
      to: "src/ui/state/session-working-store.ts"
      via: "useSessionIsWorkingRaw(sessionWorkingKey) hook call replacing the isIdle prop read"
      pattern: "useSessionIsWorkingRaw"
---

<objective>
Re-source PrettyView's `isIdle` signal and expose the tmux session name via a
new session-tmux-store so PrettyView + AppShell can operate correctly when
Terminal is unmounted in Plan 41-02. Backward-compatible — Terminal is not
touched in this plan; the pane structure still nests PrettyView inside
Terminal. User-visible behavior does not change (this is the "invisible P2"
that the orchestrator research delta requires shipping FIRST, before P1's
pane restructure removes the Terminal-side signal sources).

Purpose: eliminate PrettyView's dependence on the Terminal-owned isIdle
prop AND establish a broadcast-fed tmuxSessionNames feeder in AppShell so
tab titles keep working when Terminal is unmounted. The fleet-status
`SessionState.tmuxSession` field is ALREADY on the wire (verified in
`wire-protocol.ts` L85 and `fleet-status-types.ts` L90) — NO backend
changes required.

Output: extended session-working-store (raw hook), new session-tmux-store
module, AppShell fleet-status callback extensions, PrettyView internal
isIdle derivation, and corresponding test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/CLAUDE.md
@.planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-CONTEXT.md
@.planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-RESEARCH.md
@src/ui/state/session-working-store.ts
@src/ui/state/session-waiting-store.ts
@src/ui/state/session-working-store.test.ts
@src/ui/api/fleet-status-types.ts
@src/ui/api/fleet-status-client.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add useSessionIsWorkingRaw hook + create session-tmux-store module</name>
  <files>
    src/ui/state/session-working-store.ts,
    src/ui/state/session-working-store.test.ts,
    src/ui/state/session-tmux-store.ts,
    src/ui/state/session-tmux-store.test.ts
  </files>
  <read_first>
    - src/ui/state/session-working-store.ts (existing patterns — Map + listener registry + useSyncExternalStore + __resetForTest; must mirror verbatim for session-tmux-store)
    - src/ui/state/session-waiting-store.ts (sibling companion store — proves the "same pattern, different value type" mirror is idiomatic in this codebase)
    - src/ui/state/session-working-store.test.ts (test patterns — renderHook + act + __resetForTest in beforeEach; extend this file for useSessionIsWorkingRaw coverage)
    - src/ui/api/fleet-status-types.ts (SessionState shape — tmuxSession: string | null at L90; must match this typing)
  </read_first>
  <behavior>
    - useSessionIsWorkingRaw(null) → returns null
    - useSessionIsWorkingRaw("h1:sess-1") when no publish has occurred → returns null (NOT false — this is the three-state distinction the aside-arm depends on)
    - After publishFleetStatusSessionState("h1", {status:"idle", backgroundTasks:[], ...}) → useSessionIsWorkingRaw("h1:sess-1") returns false (published, not working)
    - After publishFleetStatusSessionState("h1", {status:"busy", ...}) → useSessionIsWorkingRaw("h1:sess-1") returns true
    - After publishFleetStatusSessionGone("h1", "sess-1", "sid") → useSessionIsWorkingRaw("h1:sess-1") returns null again
    - publishFleetStatusTmuxSession("h1", "tanya") → useSessionTmuxName("h1:tanya") returns "tanya"
    - publishFleetStatusTmuxSession("h1", null) is a no-op for the null-key convention — but publishFleetStatusTmuxSessionGone("h1", "tanya") DELETES the "h1:tanya" entry (mirrors publishFleetStatusSessionGone)
    - useSessionTmuxName(null) → returns null
    - useSessionTmuxName on unknown key → returns null
    - No-op notify guard: republishing the same tmuxSession name for the same key does not notify (mirrors session-working-store's guard)
  </behavior>
  <action>
    In session-working-store.ts, ADD (do not modify existing exports) a new
    hook `useSessionIsWorkingRaw(key: string | null): boolean | null` that
    mirrors `useSessionIsWorking` but returns `null` when the key is null OR
    when the internal Map has no record for the key. The existing
    `useSessionIsWorking` stays byte-identical (returns false in those cases)
    for backward compatibility with WipBubble and any other current callers.
    The distinction is documented in the doc comment: raw preserves the
    three-state "never heard" signal; the non-raw variant collapses to false
    for legacy WipBubble semantics. Both hooks read from the SAME internal
    Map — no new state, just a different getSnapshot shape.

    Create NEW file src/ui/state/session-tmux-store.ts mirroring
    session-working-store.ts VERBATIM in structure (module-scoped State type,
    Map<string, {tmuxSession: string}> value shape, snapshotVersion counter,
    listeners Set, notify(), subscribe(), __resetForTest). Public API:
    - publishFleetStatusTmuxSession(hostId: string, tmuxSession: string | null): void
      Key = `${hostId}:${tmuxSession ?? ""}` (SAME key convention as
      session-working-store). When tmuxSession is null, this is a no-op —
      the "key with empty tmux" bucket carries no useful name; only real
      sessions get an entry. When non-null, updates the map + notifies
      unless the value is unchanged (per-key no-op notify guard). Logs
      operation: "fleet_status_tmux_publish" with hostId, tmuxSession, key.
    - publishFleetStatusTmuxSessionGone(hostId: string, tmuxSession: string | null): void
      Deletes the key if present (mirrors publishFleetStatusSessionGone
      no-op-on-absent behavior). Logs operation: "fleet_status_tmux_gone".
    - useSessionTmuxName(key: string | null): string | null
      Returns null for null key, null for absent key, else the stored
      tmuxSession string. Uses useSyncExternalStore(subscribe, getSnapshot,
      getSnapshot) with server snapshot === client snapshot (matches
      session-working-store pattern for SSR-safety).
    - getSessionTmuxSnapshot(): ReadonlyMap<string, {tmuxSession: string}>
      Test-only accessor (mirrors getSessionWorkingSnapshot; NOT gated on
      import.meta.env — Vite tree-shaker drops it in prod bundle).
    - __resetForTest(): void
      Mirrors session-working-store.__resetForTest.

    Create NEW file src/ui/state/session-tmux-store.test.ts mirroring
    session-working-store.test.ts structure (renderHook + act, __resetForTest
    in beforeEach). Tests A-H covering all bullets in <behavior> above plus:
    (I) multiple keys are independent — publish "h1:tanya" and "h2:tina"
    then delete one, verify the other survives; (J) no-op notify guard fires
    (subscribe callback count stays flat on identical republish).

    Extend session-working-store.test.ts with tests M-P for
    useSessionIsWorkingRaw: (M) null key → null; (N) absent key → null (NOT
    false — this is the delta from the existing test I); (O) idle publish →
    false; (P) busy publish → true; (Q) session-gone → null again.

    Structured logging: every publish + gone call emits a console.info with
    operation-tagged fields (never JSON.stringify(event)) per fleet
    directive 2026-08-11.
  </action>
  <verify>
    <automated>npx vitest run src/ui/state/session-tmux-store.test.ts src/ui/state/session-working-store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Command `npx vitest run src/ui/state/session-tmux-store.test.ts src/ui/state/session-working-store.test.ts` exits 0 with zero failures.
    - Grep `grep -n "export function useSessionIsWorkingRaw" src/ui/state/session-working-store.ts` returns exactly 1 hit.
    - Grep `grep -n "export function publishFleetStatusTmuxSession" src/ui/state/session-tmux-store.ts` returns exactly 1 hit.
    - Grep `grep -n "export function useSessionTmuxName" src/ui/state/session-tmux-store.ts` returns exactly 1 hit.
    - Grep `grep -c "export function useSessionIsWorking\\b" src/ui/state/session-working-store.ts` returns 1 (the existing non-raw variant is preserved byte-for-byte).
    - Grep for empty-tmux early-return in the new store: `grep -n "tmuxSession === null" src/ui/state/session-tmux-store.ts` returns at least 1 hit inside publishFleetStatusTmuxSession.
    - Test file session-tmux-store.test.ts contains at least tests A-H per behavior list; command `grep -Ec "^\\s*(it|test)\\(" src/ui/state/session-tmux-store.test.ts` returns >= 8.
    - session-working-store.test.ts contains new tests for useSessionIsWorkingRaw; command `grep -c "useSessionIsWorkingRaw" src/ui/state/session-working-store.test.ts` returns >= 4.
    - `npx tsc --noEmit` exits 0 (no type errors on the new hook or store).
  </acceptance_criteria>
  <done>
    useSessionIsWorkingRaw ships as a public hook on session-working-store returning boolean | null with three-state semantics; session-tmux-store is a fully-populated companion store with publish/gone/hook/reset exports; both stores' vitest suites pass green including new coverage for the raw hook and the tmux store's null-on-absent, no-op-notify-guard, and multi-key isolation behaviors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire AppShell fleet-status callbacks to feed session-tmux-store + rewire PrettyView isIdle to read from the store</name>
  <files>
    src/ui/AppShell.tsx,
    src/ui/features/pretty-view/PrettyView.tsx,
    src/ui/features/pretty-view/PrettyView.aside.test.tsx
  </files>
  <read_first>
    - src/ui/AppShell.tsx L380-479 (existing createFleetStatusClient useEffect + handleTmuxSessionChange callback + document.title effect — the extension points and the fallback chain to preserve)
    - src/ui/features/pretty-view/PrettyView.tsx L120-181 (props type — isIdle prop stays typed but becomes a no-op input; document this in the JSDoc)
    - src/ui/features/pretty-view/PrettyView.tsx L770-800 (existing sessionWorkingKey construction + useSessionIsWorking(sessionWorkingKey) call — the exact key format to reuse for useSessionIsWorkingRaw)
    - src/ui/features/pretty-view/PrettyView.tsx L1980-2020 (aside-arm useEffect that reads isIdle — must switch to reading the derived value)
    - src/ui/features/pretty-view/PrettyView.tsx L2600-2625 (ComposeBox isIdle prop pass-through — must also switch to the derived value)
    - src/ui/features/pretty-view/PrettyView.aside.test.tsx (existing aside-arm coverage — must extend to prove the derived-from-store path fires correctly; the isIdle prop is now vestigial)
    - src/ui/state/session-tmux-store.ts (created by Task 1 — verify the imports and Public API surface before wiring)
    - src/ui/state/session-working-store.ts (verify useSessionIsWorkingRaw from Task 1 is importable)
  </read_first>
  <behavior>
    - AppShell.onSnapshot fires publishFleetStatusTmuxSession(state.hostId, state.tmuxSession) for each SessionState in the snapshot alongside the existing publishFleetStatusSessionState + publishFleetStatusWaitingFor calls.
    - AppShell.onUpdate fires publishFleetStatusTmuxSession(state.hostId, state.tmuxSession) alongside the existing publishFleetStatusSessionState + publishFleetStatusWaitingFor calls.
    - AppShell.onGone fires publishFleetStatusTmuxSessionGone(hostId, tmuxSession) alongside publishFleetStatusSessionGone + publishFleetStatusWaitingFor(hostId, tmuxSession, null).
    - PrettyView still ACCEPTS the isIdle prop (backward-compat during transition — Terminal.tsx still passes it) but internally derives its OWN isIdle via useSessionIsWorkingRaw(sessionWorkingKey) mapped to boolean|null. The derived value is what feeds the aside-arm useEffect AND the ComposeBox isIdle prop.
    - PrettyView.aside.test.tsx: existing tests continue to pass; new tests C1-C3 assert that driving the transition VIA publishFleetStatusSessionState (not via the isIdle prop) fires aside_arm correctly on false→true transitions.
    - AppShell document.title effect behavior UNCHANGED: on a fresh terminal-mounted identity session, Terminal's onTmuxSessionChange populates tmuxSessionNames FIRST (fast local signal); the broadcast populates tmuxSessionNames as a redundant secondary feed. On a Terminal-unmounted identity session (post-Plan-41-02), the broadcast is the ONLY feed. The document.title effect reads the same tmuxSessionNames record — no code change in the effect itself.
  </behavior>
  <action>
    In AppShell.tsx around L397-421 (the createFleetStatusClient useEffect):
    - Add import: `import { publishFleetStatusTmuxSession, publishFleetStatusTmuxSessionGone } from "@/state/session-tmux-store";`
    - Inside onSnapshot: after publishFleetStatusWaitingFor(...), add `publishFleetStatusTmuxSession(state.hostId, state.tmuxSession);` inside the for loop.
    - Inside onUpdate: after publishFleetStatusWaitingFor(...), add `publishFleetStatusTmuxSession(state.hostId, state.tmuxSession);`.
    - Inside onGone: after publishFleetStatusWaitingFor(hostId, tmuxSession, null), add `publishFleetStatusTmuxSessionGone(hostId, tmuxSession);`.
    - Extend the mount-once effect's comment block to enumerate the new store as an additional dispatch target (mirroring the existing docstring pattern for waiting-store).

    Do NOT touch the document.title effect (L466-479) — the fallback chain
    identityDisplayName → tmux → activeTab.label → "SKYNET" is unchanged.
    Do NOT touch handleTmuxSessionChange (L426-438) — Terminal's callback
    path continues to feed tmuxSessionNames when Terminal is mounted; this
    plan does not remove it. Plan 41-02 optionally decommissions the
    callback for identity panes.

    Do NOT populate tmuxSessionNames from AppShell's broadcast callbacks in
    THIS plan. That mixing (tabId ↔ hostId+tmux resolution inside a stale
    closure) is deferred to Plan 41-02 where the wrapper naturally holds
    both keys in scope. The session-tmux-store keyed by hostId+tmux is
    sufficient standalone for future callers that don't need tabId
    resolution.

    In PrettyView.tsx around L770-800:
    - Add import: `import { useSessionIsWorkingRaw } from "@/state/session-working-store";`
    - Immediately after `const isWorking = useSessionIsWorking(sessionWorkingKey);`, add:
      `const isWorkingRaw = useSessionIsWorkingRaw(sessionWorkingKey);`
      `const isIdleDerived: boolean | null = isWorkingRaw === null ? null : !isWorkingRaw;`
    - Add a code comment explaining the three-state semantics + why isIdleDerived shadows the isIdle prop.

    In PrettyView.tsx replace ALL internal reads of the `isIdle` prop with
    `isIdleDerived`. Specifically:
    - Aside-arm useEffect at ~L1985-2010: change dependency array from
      `[isIdle, pvIdentity]` to `[isIdleDerived, pvIdentity]`, and change
      the reads of `isIdle` to `isIdleDerived` inside the effect body.
      Update prevIsIdleRef.current assignments accordingly.
    - ComposeBox render at ~L2616: change `isIdle={isIdle}` to
      `isIdle={isIdleDerived}`.
    - Any other in-file read of the `isIdle` prop identifier: replace with
      `isIdleDerived`. Use `grep -n "isIdle" src/ui/features/pretty-view/PrettyView.tsx`
      to enumerate BEFORE editing so no site is missed.
    - Do NOT REMOVE the `isIdle?: boolean | null` field from the Props
      interface at L135 — it stays as a no-op accepted prop for Terminal.tsx
      backward-compat during the transition between plans. Add a JSDoc note
      to that prop: "IGNORED as of Phase 41-01 — PrettyView now derives
      isIdle internally from the fleet-status session-working-store. Prop
      is preserved for source compatibility with Terminal.tsx (Plan 41-02
      removes the caller). Do not add new call sites."

    In PrettyView.aside.test.tsx: KEEP existing tests as-is (they should
    continue to pass because the aside-arm now fires on the STORE
    transition, and existing tests may be driving the isIdle prop directly
    — those become no-op prop passes but the effect still needs a signal
    source). ADD 3 new tests C1-C3:
    - C1: mount PrettyView with no store publish → isIdleDerived null →
      aside_arm never fires on mount (regression guard for the null→true
      false-positive from Pitfall 1 of the research doc).
    - C2: publish {status:"busy"} then {status:"idle"} for the pane's
      (hostId, tmuxSession) — assert aside_arm WS frame fires exactly once
      on the false→true (working→idle) transition.
    - C3: publish {status:"busy"} then {status:"idle"} then {status:"idle"}
      again → aside_arm fires exactly once (no double-fire on same-value
      republish, thanks to session-working-store's no-op notify guard).
    Use the existing test's WS-stub push array pattern; drive store
    transitions via publishFleetStatusSessionState (already imported in
    similar tests).
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/PrettyView.aside.test.tsx src/ui/state/session-tmux-store.test.ts src/ui/state/session-working-store.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Command in verify exits 0 with zero test failures and zero type errors.
    - AppShell.tsx contains the new import: `grep -n "publishFleetStatusTmuxSession" src/ui/AppShell.tsx` returns >= 3 hits (import + onSnapshot + onUpdate + onGone; 4 expected — count == 4 acceptable).
    - AppShell.tsx onSnapshot loop calls publishFleetStatusTmuxSession per state: `grep -B1 "publishFleetStatusTmuxSession(state.hostId" src/ui/AppShell.tsx` shows the call inside the for-loop and inside onUpdate.
    - AppShell.tsx onGone calls publishFleetStatusTmuxSessionGone: `grep -n "publishFleetStatusTmuxSessionGone" src/ui/AppShell.tsx` returns >= 2 hits (import + call).
    - PrettyView.tsx imports useSessionIsWorkingRaw: `grep -n "useSessionIsWorkingRaw" src/ui/features/pretty-view/PrettyView.tsx` returns >= 2 hits (import + one call site).
    - PrettyView.tsx defines the isIdleDerived local: `grep -n "isIdleDerived" src/ui/features/pretty-view/PrettyView.tsx` returns >= 3 hits (definition + aside-arm read + ComposeBox pass).
    - PrettyView.tsx aside-arm useEffect deps include isIdleDerived not isIdle: the useEffect wrapping the `prev === false && isIdleDerived === true` transition passes `[isIdleDerived, pvIdentity]` (or equivalent) — verified by grep of the deps array line just after the effect body.
    - PrettyView.tsx ComposeBox render passes derived value: excluding the props-interface declaration line and any comment lines, `grep -Pn "isIdle=\\{isIdle\\}" src/ui/features/pretty-view/PrettyView.tsx | grep -v '^\\s*//\\|^\\s*\\*'` returns zero hits (all render-site usages now say `isIdle={isIdleDerived}`).
    - PrettyView.tsx props interface STILL declares `isIdle?: boolean | null`: `grep -n "isIdle\\?:" src/ui/features/pretty-view/PrettyView.tsx` returns exactly 1 hit (the props declaration, backward-compat).
    - PrettyView.aside.test.tsx contains the three new C-tests: `grep -c "C1\\|C2\\|C3" src/ui/features/pretty-view/PrettyView.aside.test.tsx` returns >= 3.
    - Full suite green precondition: `npx vitest run` exits 0 with zero failures.
    - Backend not touched: `git diff --stat src/backend/ | grep -c fleet-status` returns 0 in the working tree scoped to this plan's edits.
  </acceptance_criteria>
  <done>
    AppShell fires publishFleetStatusTmuxSession/Gone on every fleet-status frame; PrettyView derives isIdle internally via useSessionIsWorkingRaw and uses the derived value for both the aside-arm gate and the ComposeBox pass; the isIdle prop remains typed and accepted (Terminal.tsx still passes it, ignored by PrettyView); aside test suite covers no-fire-on-null-mount, single-fire-on-transition, and no-double-fire-on-republish; full test suite green; no backend changes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fleet-status WS → browser store | Backend-authenticated JWT WS delivers SessionState frames; browser trusts field contents but wraps JSON.parse in try/catch (existing T-34-18 mitigation on fleet-status-client.ts) |
| PrettyView store read → aside_arm WS emit | PrettyView emits `{type:"aside_arm"}` on its OWN claude-session WS when isIdleDerived transitions false→true; this is an internal browser signal (not user-controlled), never containing untrusted content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Tampering | session-tmux-store publish path | mitigate | tmuxSession field is already zod-validated at fleet-status backend egress (existing T-34-18); browser wraps JSON.parse; store accepts string \| null only per TypeScript signature |
| T-41-02 | Information Disclosure | session-tmux-store map contents (in-memory only) | accept | In-memory Map on client only; no persistence layer; page refresh clears; contents (tmux session names) are already exposed via existing tabUtils/AppShell surfaces |
| T-41-03 | Denial of Service | high-frequency publishFleetStatusTmuxSession churn | mitigate | Per-key no-op notify guard on republish of identical value (mirrors session-working-store); prevents notify storm from re-emitted identical SessionState frames |
| T-41-04 | Repudiation | store state transitions | mitigate | Every publish + gone logs console.info with `operation:` tagged fields (fleet-status_tmux_publish / _gone) per fleet structured-logging directive 2026-08-11; grep-discoverable |
| T-41-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO package installs in this plan (RESEARCH.md § Package Legitimacy Audit — not applicable). No blocking checkpoint required. |
</threat_model>

<verification>
- `npx vitest run` exits 0, zero failures (fleet precondition)
- `npx tsc --noEmit` exits 0 (frontend type check; backend not touched)
- `grep -n "useSessionIsWorkingRaw" src/ui/features/pretty-view/PrettyView.tsx` returns >= 2 hits (import + call)
- `grep -n "publishFleetStatusTmuxSession" src/ui/AppShell.tsx` returns >= 4 hits (import + 3 callback sites)
- `grep -c "publishFleetStatusTmuxSessionGone" src/ui/AppShell.tsx` returns >= 2 (import + call)
- `grep -c "isIdleDerived" src/ui/features/pretty-view/PrettyView.tsx` returns >= 3 (definition + aside-arm + ComposeBox)
- New file `src/ui/state/session-tmux-store.ts` exists and exports publishFleetStatusTmuxSession, publishFleetStatusTmuxSessionGone, useSessionTmuxName, __resetForTest
- New file `src/ui/state/session-tmux-store.test.ts` exists with >= 8 tests
- No source diffs under `src/backend/` (this plan is frontend-only)
- Structured log ops appear at least once each on a manual smoke: search dev-console for `fleet_status_tmux_publish` after opening any identity session
</verification>

<success_criteria>
1. useSessionIsWorkingRaw exists as a public hook returning boolean | null with three-state semantics (null / false / true), and useSessionIsWorking preserved byte-for-byte.
2. session-tmux-store module exists with publish/gone/hook/reset exports, tested with >= 8 vitest cases including the no-op notify guard and multi-key isolation.
3. AppShell.tsx's mount-once fleet-status client callbacks additionally dispatch to session-tmux-store on every snapshot/update/gone frame.
4. PrettyView.tsx internally derives isIdle from useSessionIsWorkingRaw + inverts to boolean|null (isIdleDerived) and uses isIdleDerived for the aside-arm gate + ComposeBox pass; the incoming isIdle prop is preserved on the type but IGNORED at runtime.
5. PrettyView.aside.test.tsx proves the store-driven aside_arm transitions fire correctly (single-fire, no-fire-on-null-mount, no-double-fire-on-republish).
6. `npx vitest run` and `npx tsc --noEmit` both exit 0.
7. No backend files touched; no npm installs; no wire-format changes.
</success_criteria>

<output>
Create `.planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-01-SUMMARY.md` when done, listing:
- New symbols exported (useSessionIsWorkingRaw, session-tmux-store public API)
- Files touched with line-count deltas
- Test-count delta (baseline vs after this plan)
- Confirmation of "user-visible behavior unchanged" via grep evidence and full-suite green
- Any deviations from the plan's letter-of-the-plan (e.g., extra grep gates added, extra tests added) — document why
</output>
</content>
</invoke>