---
phase: 41-defer-terminal-view-mount-until-user-summons-it
plan: 3
type: execute
wave: 3
depends_on:
  - 41-01
  - 41-02
files_modified:
  - .planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-BUILD-VERIFY-LOG.md
  - .planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-UAT-CHECKLIST.md
  - .planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-PATCHES-MD-ENTRY.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Objective build/test posture at HEAD is recorded — npx vitest run exit 0, npx tsc --noEmit exit 0, npm run build:backend exit 0, npm run build exit 0 — captured verbatim into 41-BUILD-VERIFY-LOG.md with timestamps and commit SHA"
    - "UAT checklist walks every LOCKED decision from CONTEXT.md through Ashley's observable-behavior lens (one checklist item per behavior class) so Ashley can UAT the deployed build without re-reading the plan set"
    - "Patches.md entry is paste-ready (single markdown block) capturing what changed (in Ashley's language, not the planner's), what to watch, and the deploy motion is orchestrator-owned"
    - "This plan performs ZERO source diffs (no changes to src/) — it's a docs-only prep step for the orchestrator-owned deploy motion"
    - "The end-of-phase human UAT is a <human-check> per project workflow.human_verify_mode=end-of-phase — Ashley confirms deployed behavior against 41-UAT-CHECKLIST.md before the phase is marked COMPLETE"
  artifacts:
    - path: .planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-BUILD-VERIFY-LOG.md
      provides: "Objective build/test posture at HEAD after 41-01 + 41-02 land — vitest exit code, tsc exit code, backend build exit code, prod build exit code, commit SHA, timestamp, environment notes"
    - path: .planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-UAT-CHECKLIST.md
      provides: "Ashley's post-deploy walkthrough — one behavior class per checklist item, mapping to the CONTEXT.md LOCKED decisions"
    - path: .planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-PATCHES-MD-ENTRY.md
      provides: "Paste-ready patch entry for ~/.claude/roles/box-maintainer/skynet-patches.md — orchestrator drops this in at ship time"
  key_links:
    - from: ".planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-UAT-CHECKLIST.md"
      to: ".planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-CONTEXT.md"
      via: "each UAT checklist item cross-references the LOCKED decision it walks (e.g. `Load behavior — cold-every-time` → UAT step 3)"
      pattern: "LOCKED"
---

<objective>
Prepare deploy artifacts for the orchestrator-owned deploy motion: an
objective build-verify log capturing green test-suite + type-check + build
status at HEAD, a Nyquist-style UAT checklist Ashley walks against the
deployed build, and a paste-ready patches.md entry the orchestrator drops
into ~/.claude/roles/box-maintainer/skynet-patches.md at ship time.

Purpose: fleet rule 2026-08-08 puts the deploy motion (docker build,
docker compose up --force-recreate, HTTPS 200 verify, coord-room BEFORE +
AFTER announcements, git push, patches.md entry) squarely in the
orchestrator's remit — NOT the executor's. This plan produces the
executor-side artifacts the orchestrator consumes at ship time so the
handoff is friction-free.

Zero source diffs. This is a docs-only plan.

Output: three markdown artifacts under the phase directory + one
end-of-phase human UAT check that Ashley resolves against the deployed
build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/CLAUDE.md
@.planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-CONTEXT.md
@.planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-01-PLAN.md
@.planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture objective build-verify posture at HEAD and generate the UAT checklist + patches-md entry</name>
  <files>
    .planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-BUILD-VERIFY-LOG.md,
    .planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-UAT-CHECKLIST.md,
    .planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-PATCHES-MD-ENTRY.md
  </files>
  <read_first>
    - .planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-CONTEXT.md (the LOCKED decisions — each becomes one UAT item)
    - .planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-01-SUMMARY.md (produced by Plan 41-01 executor — read the actual test-count delta + symbol inventory it recorded)
    - .planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-02-SUMMARY.md (produced by Plan 41-02 executor — read the actual grep-gate table + surgery deltas it recorded)
    - .planning/phases/40-*/40-UAT-CHECKLIST.md if it exists, or .planning/phases/09-*/*-CHECKLIST.md for prior Nyquist-style UAT checklist patterns to imitate (fleet convention: one behavior class per item, one line for the observable, one line for the pass criterion, cross-reference to CONTEXT.md LOCKED decision)
    - ~/.claude/roles/box-maintainer/skynet-patches.md (LAST entry format — patch number, one-line summary, motivation, changes, testing evidence, deploy details; the entry template varies patch-to-patch — imitate the most recent ~3 entries)
  </read_first>
  <action>
    ── 41-BUILD-VERIFY-LOG.md ──

    Run each of the four commands in the working directory (~/skynet-tanya)
    and capture verbatim output (or the salient tail — pass/fail line +
    summary):
    - `npx vitest run` — capture the final summary line (test files pass / tests pass / skip / todo / fail counts) + exit code.
    - `npx tsc --noEmit` — capture exit code + first-line of stderr if any.
    - `npm run build:backend` — capture exit code + Ready message if applicable.
    - `npm run build` — capture exit code + Vite build summary (bundle size, chunk count, duration).

    Layout the log with:
    - `## Environment` block: HEAD commit SHA (`git rev-parse HEAD`), branch (`git rev-parse --abbrev-ref HEAD`), node version (`node --version`), npm version (`npm --version`), UTC timestamp.
    - `## Test posture` block: the four command outputs with pass/fail summary + exit code for each.
    - `## Diff scope` block: `git diff --stat HEAD~3 HEAD -- src/ .planning/` (post 41-01 + 41-02 landing; adjust ~3 to match actual commit count of the phase).
    - `## Green precondition` explicit statement: "This phase leaves the test suite at green — fleet rule `never leave tests failing` honored."
    - `## Backend green precondition`: explicit statement even if backend wasn't touched — `npm run build:backend` was run per RESEARCH.md Pitfall 5.

    ── 41-UAT-CHECKLIST.md ──

    Structure: numbered checklist items. Each item follows the pattern:
    ```
    ### N. <Behavior class name>
    - **Cross-reference:** CONTEXT.md LOCKED decision `<section title>` (e.g., `Load behavior — cold-every-time`)
    - **Setup:** <what Ashley does to reach the observable — e.g. "Open the Skynet PWA and pick Tina from the conversations list">
    - **Expected observable:** <one-sentence description of what she should see>
    - **Fail signal:** <what would look wrong; specific enough that Ashley can call it out without ambiguity>
    - **Console-log check (optional):** <if a structured log line proves the behavior, name the `operation:` field to grep for in DevTools>
    ```

    Cover at MINIMUM these behavior classes (one item each, in order):

    1. **Identity-session cold-open.** Opening an identity session shows PrettyView within the normal boot window; NO xterm "Connecting…" flash on the surface (Terminal is not mounted). Cross-ref: `Load behavior — cold-every-time` LOCKED.
    2. **Ctrl+Shift+O cold-boot into Terminal.** First press flips to the terminal surface with the existing "Connecting…" loader briefly visible, then live tmux content. Cross-ref: `Load behavior — cold-every-time` LOCKED + `Pane restructure` LOCKED.
    3. **Long-press-identity-badge cold-boot.** On the pretty-view surface, tap-and-hold the identity badge produces the same flip as Ctrl+Shift+O. Cross-ref: `Pane restructure` LOCKED.
    4. **Toggle-back tears Terminal down completely.** Second press of Ctrl+Shift+O (or long-press badge again) returns to PrettyView; the tmux content that WAS on the terminal surface is gone — a subsequent flip back to terminal cold-boots FRESH (does not resume the previous xterm view). Cross-ref: `Load behavior — cold-every-time` LOCKED (every visit is a fresh cold-boot).
    5. **PrettyView WipBubble + ready-dot behave normally through toggles.** During any toggle sequence, WipBubble/ready-dot behavior on PrettyView + conversation-list matches pre-Phase-41 semantics — fires when the backing session is working, absent when idle, absent when the fleet-status broadcast hasn't landed yet. Cross-ref: `isIdle re-sourcing` LOCKED.
    6. **Tab title populates from fleet-status broadcast on identity panes.** Open a fresh identity session; the browser tab title resolves to the identity's displayName (or tmux name during the broadcast window) — no regression from pre-Phase-41. If backend broadcast hasn't landed yet, the tab title falls back to the tab label (per LOCKED "sensible placeholder"). Cross-ref: `Tab title re-sourcing` LOCKED.
    7. **Non-identity SSH terminal sessions are unaffected.** Open a pure-SSH workstation-style host tab (one where the target tmux session is NOT a registered identity). Behavior is identical to pre-Phase-41: Terminal mounts eagerly, SSH WS dials on mount, xterm appears with "Connecting…" flash. Cross-ref: `Scope boundary` LOCKED.
    8. **RDP / VNC / dashboard tabs unaffected.** Open an RDP tab or the dashboard — behavior is byte-identical to pre-Phase-41. Cross-ref: `Scope boundary` LOCKED.
    9. **MessageQueueDrawer sends still fire from the wrapper.** With an identity pane open in PrettyView mode, open the message queue drawer (existing keyboard chord). Send a queued message. The message reaches the agent (arrives as a user turn in the conversation stream). Cross-ref: `Send path` established prior (Phase 35 patch #435; refs are hoisted correctly).
    10. **No console errors during toggle sequences.** Open an identity pane, cycle Ctrl+Shift+O five times, then open a non-identity pane, then close both. DevTools console shows zero uncaught exceptions or React warnings (structured logs with `operation:` tags are expected and fine).

    Include a final "Regression suspicions" section listing the RESEARCH.md
    §9 hot areas so Ashley knows where to look if something breaks:
    Terminal.tsx heavy surgery, PrettyView isIdleDerived path, upload path
    disabled when Terminal is unmounted (chip strip renders but startBatch
    parks — behavioral degradation ACCEPTED per LOCKED rationale in
    RESEARCH.md L302).

    ── 41-PATCHES-MD-ENTRY.md ──

    Draft a paste-ready markdown block for
    `~/.claude/roles/box-maintainer/skynet-patches.md`. Mirror the entry
    style of recent patches (read the last ~3 entries in that file). Include:
    - Patch number: `TBD (orchestrator fills at ship time)` — do NOT guess the number.
    - One-line summary (Ashley-language, not planner-language): e.g. "Identity sessions defer the terminal view until Ashley reaches for it — PrettyView loads on its own; Ctrl+Shift+O cold-boots the terminal each visit."
    - Motivation: brief — pretty-view is what Ashley uses; terminal is a rare-fallback; eager-loading the terminal on every identity session is wasted per-tab cost.
    - Files touched (accurate list): src/ui/state/session-working-store.ts, src/ui/state/session-tmux-store.ts, src/ui/state/session-tmux-store.test.ts, src/ui/state/session-working-store.test.ts, src/ui/AppShell.tsx, src/ui/shell/tabUtils.tsx, src/ui/shell/IdentitySessionPane.tsx, src/ui/shell/IdentitySessionPane.test.tsx, src/ui/features/terminal/Terminal.tsx, src/ui/features/terminal/Terminal.wiring.test.ts, src/ui/features/pretty-view/PrettyView.tsx, src/ui/features/pretty-view/PrettyView.aside.test.tsx. Backend: none.
    - Testing evidence: reference 41-BUILD-VERIFY-LOG.md numbers.
    - Deploy details: leave as `TBD (orchestrator fills at ship time)` — container image tag, container-mutation coord-room BEFORE/AFTER announcements, `git push` timestamp, HTTPS 200 verification. NONE of this is executor-owned per fleet rule 2026-08-08.
    - Rebase risk: LOW (fork-local pretty-view + terminal + tabUtils composition change; no upstream Skynet surface disturbed).
    - Bounty tracker: TBD if a bounty was opened for this phase; leave blank for orchestrator to fill.
  </action>
  <verify>
    <automated>test -s .planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-BUILD-VERIFY-LOG.md && test -s .planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-UAT-CHECKLIST.md && test -s .planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-PATCHES-MD-ENTRY.md && grep -Ec "^### [0-9]+\\." .planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-UAT-CHECKLIST.md | awk '{ if($1 >= 10) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist and are non-empty (verify command exit 0).
    - 41-BUILD-VERIFY-LOG.md captures the four commands' exit codes explicitly: grep `grep -c "exit 0\\|EXIT 0" .planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-BUILD-VERIFY-LOG.md` returns >= 4.
    - 41-BUILD-VERIFY-LOG.md includes commit SHA: grep `grep -Pc "[0-9a-f]{7,40}" .planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-BUILD-VERIFY-LOG.md` returns >= 1.
    - 41-UAT-CHECKLIST.md contains >= 10 numbered items: `grep -Ec "^### [0-9]+\\." .planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-UAT-CHECKLIST.md` returns >= 10.
    - 41-UAT-CHECKLIST.md cross-references CONTEXT.md LOCKED decisions in at least 6 items: `grep -c "Cross-reference.*LOCKED\\|CONTEXT.md" .planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-UAT-CHECKLIST.md` returns >= 6.
    - 41-PATCHES-MD-ENTRY.md leaves patch number and deploy details as TBD: `grep -c "TBD" .planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-PATCHES-MD-ENTRY.md` returns >= 2 (deploy motion is orchestrator-owned).
    - NO changes to any file under src/: `git diff --name-only src/` returns no output when scoped to this plan's edits.
    - NO deploy tasks anywhere in this plan (fleet rule 2026-08-08).
    - Full suite green precondition maintained: `npx vitest run` still exits 0 (should be untouched since no source diffs, but verify defensively).
  </acceptance_criteria>
  <done>
    Three prep artifacts exist under the phase directory: an objective
    build-verify log capturing green vitest/tsc/backend-build/frontend-build
    at HEAD post-41-01+41-02, a Nyquist UAT checklist with >= 10 items
    cross-referencing CONTEXT.md LOCKED decisions, and a paste-ready
    skynet-patches.md entry with TBD-marked orchestrator fields. Zero
    source diffs. Full test suite still green. Deploy motion remains
    entirely orchestrator-owned per fleet rule 2026-08-08.
  </done>
</task>

<task type="auto">
  <name>Task 2: End-of-phase human UAT gate (workflow.human_verify_mode=end-of-phase)</name>
  <files>
    (no source diffs — this task is the human UAT resolution)
  </files>
  <read_first>
    - .planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-UAT-CHECKLIST.md (produced by Task 1)
    - .planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-BUILD-VERIFY-LOG.md (Task 1 objective evidence)
  </read_first>
  <action>
    HUMAN VERIFY GATE: after the orchestrator has completed the deploy
    motion (docker build + docker compose up --force-recreate + HTTPS 200
    verify + coord-room announcements + git push + patches.md entry), Ashley
    walks 41-UAT-CHECKLIST.md against the deployed build.

    This task is the executor's HOLD-POINT — the executor does NOT close
    this task on its own authority. The orchestrator (or a subsequent
    /gsd-verify-phase invocation) reads Ashley's UAT outcome and marks the
    task done ONLY when Ashley explicitly signals approval.

    If Ashley reports failures, this task remains open and gap-closure
    routes back to Plan 41-01 or 41-02 (or a new gap plan) depending on
    where the observable defect maps.

    No code changes in this task under any circumstances. This is purely
    the human-verify surface per workflow.human_verify_mode=end-of-phase.
  </action>
  <verify>
    <human-check>Ashley signs off on 41-UAT-CHECKLIST.md items 1-10 against the deployed Skynet build. Approval signal: an explicit "UAT green" or per-item resolution log; anything short of that keeps this task open. If items fail, gap-closure routes to a new plan (41-04+) via /gsd-plan-phase 41 --gaps.</human-check>
  </verify>
  <acceptance_criteria>
    - Ashley's explicit approval recorded (verbatim quote or checkbox log against 41-UAT-CHECKLIST.md).
    - OR if approval withheld: gap plan(s) exist under the phase directory (41-04-PLAN.md etc.) addressing each failed observable.
    - The phase is NOT marked COMPLETE without this task's resolution one way or the other.
  </acceptance_criteria>
  <done>
    Ashley signs off on all UAT items OR gap plans are queued for the failures. Phase can then be marked complete by /gsd-verify-phase.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This plan produces docs only — no
source code, no deploy motion, no runtime surface changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-10 | Repudiation | UAT sign-off | mitigate | 41-UAT-CHECKLIST.md is the artifact-of-record for Ashley's approval; per-item observable + fail-signal wording is explicit enough that a passing sign-off is unambiguous evidence |
| T-41-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO package installs in this plan. |
</threat_model>

<verification>
- All three docs files exist and pass content greps per Task 1 acceptance criteria
- No source diffs (git diff --name-only src/ returns empty for this plan's commits)
- No deploy tasks in the plan
- Full test suite still green (untouched by docs-only work)
- End-of-phase UAT surface prepared for Ashley
</verification>

<success_criteria>
1. 41-BUILD-VERIFY-LOG.md exists with the four command outputs and their exit codes + commit SHA + branch + timestamp.
2. 41-UAT-CHECKLIST.md exists with >= 10 numbered items each cross-referencing a CONTEXT.md LOCKED decision, with Setup / Expected observable / Fail signal / optional Console-log check fields.
3. 41-PATCHES-MD-ENTRY.md exists as a paste-ready markdown block with orchestrator-filled fields marked TBD.
4. Zero source diffs.
5. Deploy motion remains entirely orchestrator-owned per fleet rule 2026-08-08.
6. End-of-phase UAT gate is prepared but held open pending Ashley's sign-off.
</success_criteria>

<output>
Create `.planning/phases/41-defer-terminal-view-mount-until-user-summons-it/41-03-SUMMARY.md` when done, listing:
- The three docs artifacts created with byte counts
- Objective build-verify numbers captured (test-count, exit codes)
- Whether Task 2 UAT sign-off is pending or resolved (verbatim quote if resolved)
- Any gap plans queued if UAT failed
</output>
</content>
</invoke>