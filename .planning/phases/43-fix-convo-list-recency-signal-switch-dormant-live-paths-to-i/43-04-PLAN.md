---
phase: 43-fix-convo-list-recency-signal-switch-dormant-live-paths-to-i
plan: 04
type: execute
wave: 3
depends_on:
  - 43-01
  - 43-02
  - 43-03
files_modified:
  - src/ui/state/conversation-store.ts
  - src/ui/state/conversation-store.test.ts
  - src/ui/AppShell.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "FleetSession type in conversation-store.ts includes optional lastMessageAt: number | null field carried from /sessions/list response through updateFleetSessions."
    - "AppShell's /sessions/list handler, after updateFleetSessions(fresh), iterates sessions and calls seedSessionLastMessageAt(s.hostId, s.sessionName, s.lastMessageAt ?? null) for each row."
    - "compareByRecencyDesc Rule 1 flips from null-to-top to null-to-bottom — rows with lastMessageAt == null now sort AFTER rows with any real timestamp."
    - "The pre-existing insertion-order-key fallback among null-lastMessageAt rows is preserved (Rule 2) so ordering is deterministic among no-history rows."
    - "Rules 3 (real timestamps DESC) and 4 (identical-timestamp fallback to insertion order) are unchanged."
  artifacts:
    - path: "src/ui/state/conversation-store.ts"
      provides: "FleetSession.lastMessageAt field carried through updateFleetSessions; compareByRecencyDesc Rule 1 flipped to null-to-bottom"
      contains: "lastMessageAt"
    - path: "src/ui/AppShell.tsx"
      provides: "seedSessionLastMessageAt call site after updateFleetSessions(fresh)"
      contains: "seedSessionLastMessageAt"
    - path: "src/ui/state/conversation-store.test.ts"
      provides: "Updated Rule 1 tests reflecting null-to-bottom semantics + regression locks for null-cluster insertion-order stability + Plan 43-03 max-wins reconciliation cross-store tests"
      contains: "null-to-bottom"
  key_links:
    - from: "src/ui/AppShell.tsx /sessions/list handler"
      to: "src/ui/state/session-working-store.ts:seedSessionLastMessageAt"
      via: "per-session iteration after updateFleetSessions(fresh)"
      pattern: "seedSessionLastMessageAt\\(s\\.hostId"
    - from: "src/ui/state/conversation-store.ts:compareByRecencyDesc"
      to: "row.lastMessageAt (populated via resolveLastMessageAt → getSessionLastMessageAt via working-store)"
      via: "null-to-bottom comparator branches"
      pattern: "aTs === null && bTs !== null.*return\\s*1"
---

<objective>
Complete the Phase 43 wire-through. Three coordinated changes on files that MUST land together (single plan due to conversation-store.ts + AppShell.tsx overlap risk with any of Plans 43-01/02/03):

1. **FleetSession type extension** — add optional `lastMessageAt?: number | null` to the `FleetSession` type at conversation-store.ts:140-146. This field is carried from the /sessions/list response (Plan 43-01) through `getSessionList()` → `updateFleetSessions()` → into `state.fleetSessions[]`. It does NOT flow into ConversationRow directly (that path stays via `resolveLastMessageAt` → working-store `getSessionLastMessageAt`); it exists on FleetSession so AppShell can pass it to `seedSessionLastMessageAt` without re-fetching.

2. **AppShell seed wiring** — after `updateFleetSessions(fresh)` at AppShell.tsx:605, iterate `fresh` and call `seedSessionLastMessageAt(s.hostId, s.sessionName, s.lastMessageAt ?? null)` for each. This is the wire consumer for the Plan 43-03 chokepoint API.

3. **compareByRecencyDesc Rule 1 flip** — retire Ashley's 2026-08-14 no-history-to-top lock at conversation-store.ts:539-542. New semantics: `lastMessageAt == null` sorts AFTER rows with a real timestamp (null-to-bottom). The insertion-order-key fallback among null rows (Rule 2) is preserved. Rules 3 + 4 unchanged.

Purpose: This closes the loop. Plans 43-01/02 give correct data at both feeds; Plan 43-03 exposes the reconciliation chokepoint; this plan hands data to the chokepoint and flips the comparator so the frontend actually renders the recency-sorted middle correctly.

Output: Wire consumer + FleetSession type + comparator flip + updated tests. Per CONTEXT.md scope_fence: NO changes to `PrettyConversationsPanel.tsx`, `pretty-conversations.css`, or any of the render-side surfaces — the fix ships purely by giving the existing comparator + snapshot pipeline correct inputs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-fix-convo-list-recency-signal-switch-dormant-live-paths-to-i/43-CONTEXT.md
@src/ui/state/conversation-store.ts
@src/ui/state/conversation-store.test.ts
@src/ui/AppShell.tsx
@src/ui/api/sessions-api.ts
@src/ui/state/session-working-store.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend FleetSession type + isFleetSession predicate for optional lastMessageAt (cache and updateFleetSessions accept new field)</name>
  <files>src/ui/state/conversation-store.ts</files>
  <read_first>
    - src/ui/state/conversation-store.ts lines 134-146 (FleetSession type declaration)
    - src/ui/state/conversation-store.ts lines 1015-1094 (isFleetSession predicate + readFleetSessionsCache + writeFleetSessionsCache — the cache-round-trip that strips the field today would drop the new lastMessageAt on any cache round-trip; must decide whether to preserve or intentionally strip it)
    - src/ui/state/conversation-store.ts lines 925-974 (updateFleetSessions — the field flows through here unchanged; shallow-equal comparison stays reference-based, no behavior change needed for this new field)
    - .planning/phases/43-fix-convo-list-recency-signal-switch-dormant-live-paths-to-i/43-CONTEXT.md decisions — the "fetch cadence" block: `/sessions/list` is fetched on AppShell mount + host-tree changes (existing behavior); no periodic refetch; localStorage cache exists for cold-start paint speed. Design choice for this task: DO persist lastMessageAt in the cache — a stale-but-close-enough value on cold-start paint is better than a null value that would hoist-to-bottom (post-flip) or top (pre-flip); the fresh fetch overwrites within ~200ms anyway.
  </read_first>
  <behavior>
    - `FleetSession` type gains `lastMessageAt?: number | null` optional field.
    - `isFleetSession` predicate accepts objects with OR without the field; if present, it must be `null` or a `number` (rejects other types defensively).
    - `readFleetSessionsCache` preserves the field in its defensive per-item filter (currently drops unknown fields at line 1057-1063).
    - `writeFleetSessionsCache` persists the field in its canonical serialization (currently drops unknown fields at line 1083-1089).
    - `updateFleetSessions` needs no change — it uses per-element ref-equality for shallow no-op detection; the new field is transported inside the same object.
  </behavior>
  <action>
    In `src/ui/state/conversation-store.ts`:
    1. Extend the `FleetSession` type at lines 140-146 by appending: `lastMessageAt?: number | null;` — optional (`?:`) so pre-Phase-43 backend responses (via the wire type in `sessions-api.ts` from Plan 43-03) deserialize into a FleetSession object that omits the field. Add a brief comment: `// Phase 43 Plan 04 — inline recency signal carried from /sessions/list route (Plan 43-01). Consumed by AppShell to feed seedSessionLastMessageAt; NOT stamped on rows (that path stays via working-store max-wins).`
    2. Extend `isFleetSession` at lines 1023-1033 to accept the new field. Add the following after the role check: `if (r.lastMessageAt !== undefined && r.lastMessageAt !== null && typeof r.lastMessageAt !== "number") return false;` — accepts undefined, null, or number; rejects other types.
    3. Extend `readFleetSessionsCache` at lines 1057-1063: add `lastMessageAt: item.lastMessageAt` (or `item.lastMessageAt ?? null`) to the pushed canonical object. Keep the defensive filter — only known fields survive.
    4. Extend `writeFleetSessionsCache` at lines 1083-1089: add `lastMessageAt: s.lastMessageAt ?? null` to the canonical serialization. Preserves cache-round-trip fidelity for the new field.
    5. Bump the localStorage cache key version at line 1021 from `"skynet:convo-fleet-cache:v1"` to `"skynet:convo-fleet-cache:v2"` — reason: v1 cached items lack lastMessageAt, and while the isFleetSession predicate accepts their absence (optional field), rehydrating a v1 cache on Phase 43 first-load would seed the frontend with objects whose lastMessageAt is undefined → resolveLastMessageAt would fall through to the working-store getter (which returns null since the working-store also starts empty on cold-boot) → null-to-bottom sorts them to the bottom instead of respecting whatever fresh /sessions/list is about to return. Bumping to v2 forces one clean cold-start after deploy; small acceptable UX cost. Add a comment referencing this rationale.
    6. Do NOT touch: FleetSession consumers other than what's listed above; `updateFleetSessions` / `removeFleetSession` / `updateHostsFlat` bodies stay unchanged.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tanya && npx vitest run src/ui/state/conversation-store.test.ts -t "fleet"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "lastMessageAt" src/ui/state/conversation-store.ts` shows the field in FleetSession type + isFleetSession predicate + read cache + write cache — at least 4 additional hits beyond pre-plan (which already had `lastMessageAt` uses via resolveLastMessageAt).
    - `grep -n 'convo-fleet-cache:v2' src/ui/state/conversation-store.ts` returns exactly 1 line (bumped key).
    - `grep -c 'convo-fleet-cache:v1' src/ui/state/conversation-store.ts` returns 0 (old key retired).
    - `npm run build` exit code 0.
    - Existing fleet-cache tests (if any in conversation-store.test.ts) continue to pass with the new field permitted; roundtrip tests preserve the value.
  </acceptance_criteria>
  <done>FleetSession carries optional lastMessageAt; cache round-trip preserves it; cache key bumped to v2 to force clean rehydrate on deploy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flip compareByRecencyDesc Rule 1 from null-to-top to null-to-bottom + update existing Rule 1 tests</name>
  <files>src/ui/state/conversation-store.ts, src/ui/state/conversation-store.test.ts</files>
  <read_first>
    - src/ui/state/conversation-store.ts lines 511-553 (compareByRecencyDesc — the exact block being modified spans the docblock at 511-533 and the branches at 537-552; only Rule 1's return values change; the insertion-order fallback and DESC branches stay verbatim)
    - src/ui/state/conversation-store.test.ts lines 2681-2782 (existing Phase 41 Plan 01 tests for compareByRecencyDesc — Test C at 2683-2703 is the direct target; Tests D/G/H are downstream verifiers that must keep passing with the flip)
    - src/ui/state/conversation-store.test.ts lines 2807-2963 (Phase 41 Plan 03 real-wire tests — Test J at 2866-2888 currently asserts "no-history row floats to top" via `expect(snap.middle.map((r) => r.id)).toEqual(["t2", "t1"]);` — this MUST flip to `["t1", "t2"]` in the new contract)
    - .planning/phases/43-fix-convo-list-recency-signal-switch-dormant-live-paths-to-i/43-CONTEXT.md § Comparator change — retire no-history-to-top. Exact code snippet locked at lines 165-172 of CONTEXT.md.
  </read_first>
  <behavior>
    - Behavior test (existing Test C flipped): two rows R1 (lastMessageAt=1000) and R2 (lastMessageAt=null) — after the flip, `snap.middle.map(r => r.id)` returns `["r1", "r2"]` (real timestamp first, null second). Pre-flip returned `["r2", "r1"]`.
    - Behavior test (existing Test J flipped): sess-1 published lastMessageAt=1000, sess-2 never published (null) → `snap.middle.map(r => r.id)` returns `["t1", "t2"]` (t1 with real timestamp first, t2 null second). Pre-flip returned `["t2", "t1"]`.
    - Test D (recency DESC) unchanged: two real timestamps still sort DESC.
    - Tests G + H (pinned/RDP zone stability) unchanged: recency ignored in those zones.
    - New Test L (null-cluster insertion-order stability): three rows, all with `lastMessageAt=null`, keep their insertion order [r-first, r-second, r-third] in `snap.middle` under the flip — Rule 2 (insertion-order fallback among null rows) still applies.
    - New Test M (mixed real + null, ordering): four rows — R1=1000, R2=null, R3=2000, R4=null (inserted in that order) → `snap.middle.map(r => r.id)` returns `["r3", "r1", "r2", "r4"]` (real timestamps DESC first — r3 then r1; then null rows in insertion-order — r2 then r4).
  </behavior>
  <action>
    Part A — In `src/ui/state/conversation-store.ts` compareByRecencyDesc (lines 511-553):

    1. Rewrite the docblock at lines 511-533 to reflect the new contract:
       - Rule 1 (new): rows with `lastMessageAt == null` (no history) sort AFTER rows with any real timestamp — freshest known activity floats to the top; null-history rows sink to the bottom.
       - Rules 2/3/4 semantics preserved verbatim (rewrite the paragraph text but keep the substance).
       - Add a header line: `// Phase 43 Plan 04 — flipped Rule 1 from null-to-top to null-to-bottom. Retires Ashley's 2026-08-14 no-history-to-top lock per 43-CONTEXT.md § Comparator change — retire no-history-to-top.`
       - Delete or update the "Since Plan 03 has NOT yet landed" comment at lines 525-528 (Plan 03 has since landed; the current Phase 43 fix follows the recency signal correctness work).

    2. Flip the branches at lines 539-542:
       Before:
       ```
       // Rule (1): no-history-to-top. Rows with null sort BEFORE rows with a
       // timestamp (return < 0 when a is null and b is not).
       if (aTs === null && bTs !== null) return -1;
       if (aTs !== null && bTs === null) return 1;
       ```
       After:
       ```
       // Rule (1): null-to-bottom (Phase 43 Plan 04 flip). Rows with null sort
       // AFTER rows with a timestamp (return > 0 when a is null and b is not).
       if (aTs === null && bTs !== null) return 1;
       if (aTs !== null && bTs === null) return -1;
       ```

    3. Preserve Rules 3 + 4 (lines 543-552) verbatim. The DESC branch and insertion-order fallback are unchanged.

    Part B — In `src/ui/state/conversation-store.test.ts`:

    4. Update the pre-existing Test C at lines 2683-2703:
       - Rename the `it(...)` title to reflect null-to-bottom: `"Test C: no-history row (lastMessageAt=null) sorts AFTER a row with any timestamp (Phase 43 Plan 04 flip)"`.
       - Update the assertion at line 2702 from `expect(snap.middle.map((r) => r.id)).toEqual(["r2", "r1"]);` to `expect(snap.middle.map((r) => r.id)).toEqual(["r1", "r2"]);` (r1 real-ts first, r2 null second).
       - Update the inline comment at lines 2699-2701 to reference the flip and Phase 43 Plan 04.

    5. Update the pre-existing Test J at lines 2866-2888:
       - Rename the `it(...)` title to: `"Test J: middle zone — row with real lastMessageAt vs. row with no wire-side signal → real-timestamp row sorts to top, no-history row sinks (Phase 43 Plan 04 flip)"`.
       - Update the assertion at line 2887 from `expect(snap.middle.map((r) => r.id)).toEqual(["t2", "t1"]);` to `expect(snap.middle.map((r) => r.id)).toEqual(["t1", "t2"]);`.
       - Update the inline comment at lines 2874-2884 to reference the flip.

    6. Add two new `it(...)` tests appended to the Phase 41 Plan 01 describe (near line 2782) OR inside a new `describe("conversation-store (Phase 43 Plan 04): compareByRecencyDesc — null-to-bottom flip", () => { ... })` block:
       - Test L (null-cluster insertion-order stability): three rows all null → `snap.middle.map(r => r.id)` returns them in insertion order.
       - Test M (mixed real + null): four rows as described in <behavior>; assert `snap.middle.map(r => r.id)` equals `["r3", "r1", "r2", "r4"]`.

    7. Sweep the file for any OTHER test that asserts `snap.middle.map(r => r.id).toEqual([...])` where the expected order was derived under the pre-flip null-to-top rule. Candidates: any test that uses `__setLastMessageAtForTest` with a mix of real timestamps + no-ts rows and asserts a specific middle order. Update each affected assertion; add an inline `// Phase 43 Plan 04: flipped from null-to-top` comment. Use `grep -n "no-history-to-top\\|null-to-top" src/ui/state/conversation-store.test.ts` to find comment-anchored candidates, then hand-inspect each. Do NOT update tests that assert pinned-zone or RDP-zone ordering (Tests G + H — those zones are recency-independent, no flip impact).

    8. Do NOT touch: the pre-existing Tests I/K (Plan 41 Plan 03 real-wire tests OTHER than J) if they don't assert on the null-vs-real branch; verify by re-reading each.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tanya && npx vitest run src/ui/state/conversation-store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "null-to-bottom" src/ui/state/conversation-store.ts` returns at least 2 lines (docblock header + branch comment).
    - `grep -c "null-to-top" src/ui/state/conversation-store.ts` returns 0 (retired language).
    - The four flipped-branch return values at lines 539-542 read literally: `if (aTs === null && bTs !== null) return 1;` and `if (aTs !== null && bTs === null) return -1;` (verify via grep: `grep -n "aTs === null && bTs !== null.*return 1" src/ui/state/conversation-store.ts` returns 1 line).
    - `grep -n 'toEqual\\(\\["r1", "r2"\\]\\)\\|toEqual\\(\\["t1", "t2"\\]\\)' src/ui/state/conversation-store.test.ts` shows the flipped assertions for Tests C and J.
    - New Test L + Test M both pass; `grep -c 'Test L\\|Test M' src/ui/state/conversation-store.test.ts` >= 2.
    - `npx vitest run src/ui/state/conversation-store.test.ts` exit code 0; all pre-existing tests pass with the flipped-order updates; no test asserts null-to-top ordering anywhere.
  </acceptance_criteria>
  <done>Rule 1 flipped; docblock + branches + affected tests updated in the same commit; full conversation-store test suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire AppShell /sessions/list handler to seed working-store per row via seedSessionLastMessageAt</name>
  <files>src/ui/AppShell.tsx</files>
  <read_first>
    - src/ui/AppShell.tsx lines 587-620 (the /sessions/list one-shot fetch effect — the exact call sites for updateFleetSessions to add the seed loop after)
    - src/ui/AppShell.tsx lines 55-70 (imports block — need to add `seedSessionLastMessageAt` import from `@/state/session-working-store`)
    - src/ui/state/session-working-store.ts (verify the `seedSessionLastMessageAt` export from Plan 43-03 lands; signature: `(hostId: number, tmuxSession: string, ts: number | null) => void`)
    - src/ui/api/sessions-api.ts (verify RemoteTmuxSession.lastMessageAt field from Plan 43-03; getSessionList response items now carry it optionally)
    - .planning/phases/43-fix-convo-list-recency-signal-switch-dormant-live-paths-to-i/43-CONTEXT.md § AppShell seed call site — the exact call `seedSessionLastMessageAt(s.hostId, s.sessionName, s.lastMessageAt ?? null)` per session.
  </read_first>
  <behavior>
    - Behavior test A (fresh fetch seeds every session): mock `getSessionList` to return `[{ hostId: 1, hostName: "h1", sessionName: "tina", created: 100, role: "sre", lastMessageAt: 1234 }, { hostId: 2, hostName: "h2", sessionName: "tiffany", created: 200, role: null, lastMessageAt: null }]`. After the effect resolves, `getSessionLastMessageAt("1:tina") === 1234` and `getSessionLastMessageAt("2:tiffany") === null` (null pass-through preserves cache-empty state).
    - Behavior test B (cached seed also fires): the `readFleetSessionsCache()` path (line 594) also seeds — after cache read + updateFleetSessions(cached), iterate `cached` and call seedSessionLastMessageAt for each. Rationale: the localStorage cache preserves lastMessageAt per Task 1 step 4; the seed loop for cached rows paints the correct middle-zone order immediately on cold-start, before the fresh fetch resolves ~200ms later.
    - Behavior test C (max-wins across cache-then-fresh): cache had `sess-A: 500`, fresh returns `sess-A: 2000` → after both seeds, `getSessionLastMessageAt("<key>") === 2000` (Plan 43-03 chokepoint contract). Reverse case: cache had `sess-A: 2000`, fresh returns `sess-A: 500` → `getSessionLastMessageAt("<key>") === 2000` (max-wins prevents regression).
    - Behavior test D (fetch failure gracefully): getSessionList throws → cached seed still fires (if any); no crash; no rejected promise leaked.
  </behavior>
  <action>
    In `src/ui/AppShell.tsx`:

    1. Extend the imports block (add near line 55 alongside the existing session-working-store imports if any, OR add a new import line near line 66-68): `import { seedSessionLastMessageAt } from "@/state/session-working-store";`. Verify the import path convention by checking how AppShell.tsx imports other session-working-store symbols — mirror that pattern (barrel export vs. direct).

    2. Modify the `/sessions/list` fetch effect at lines 587-620:

       a. AFTER the `updateFleetSessions(cached)` call at line 596, add:
          ```
          for (const s of cached) {
            seedSessionLastMessageAt(s.hostId, s.sessionName, s.lastMessageAt ?? null);
          }
          ```
          Wrap in a brief comment: `// Phase 43 Plan 04 — feed cached rows into the working-store max-wins reconciliation. If the fresh fetch below returns fresher values, they overwrite; if it returns stale or null, cache values persist.`

       b. AFTER the `updateFleetSessions(fresh)` call at line 605, add:
          ```
          for (const s of fresh) {
            seedSessionLastMessageAt(s.hostId, s.sessionName, s.lastMessageAt ?? null);
          }
          ```
          Comment: `// Phase 43 Plan 04 — seed working-store from the fresh /sessions/list snapshot. Max-wins reconciliation in the working-store handles ordering vs. WS-live updates (which may arrive before or after this).`

    3. Do NOT change: the empty-dep-array TG-17 shape lock at line 620, the try/catch failure semantics, the `cancelled` guard pattern, writeFleetSessionsCache call at line 608, or any other AppShell behavior. The change is purely additive within the existing effect body.

    4. Verify TypeScript inference on `s.lastMessageAt` — the RemoteTmuxSession field is optional per Plan 43-03; the `?? null` fallback coerces undefined → null. `s.hostId` is `number` per the type; `s.sessionName` is `string`. All match seedSessionLastMessageAt's signature `(hostId: number, tmuxSession: string, ts: number | null)`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tanya && npm run build && npx vitest run src/ui/AppShell.persistence.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "seedSessionLastMessageAt" src/ui/AppShell.tsx` returns exactly 3 hits (1 import + 2 call sites — one for cached loop, one for fresh loop).
    - `grep -c "seedSessionLastMessageAt(s.hostId, s.sessionName, s.lastMessageAt \\?\\? null)" src/ui/AppShell.tsx` returns exactly 2 (both call sites use the exact spec'd call shape).
    - `grep -c "Phase 43 Plan 04" src/ui/AppShell.tsx` >= 2 (comment markers on both seed sites).
    - No changes to `updateFleetSessions(cached)` OR `updateFleetSessions(fresh)` call arguments; both remain their original one-argument shape.
    - `npm run build` exit code 0.
    - Pre-existing AppShell.persistence.test.tsx tests still pass — the additive seed loop should not affect assertions about updateFleetSessions call counts or fetch-related behavior. If a test explicitly asserts "no additional calls after updateFleetSessions", update the assertion to permit the seed loop; the new calls are to the working-store, not conversation-store.
  </acceptance_criteria>
  <done>Both cached + fresh /sessions/list paths call seedSessionLastMessageAt per row; import + comments present; build + AppShell.persistence tests green.</done>
</task>

<task type="auto">
  <name>Task 4: Full build + test sweep for coordinated changes</name>
  <files>src/ui/state/conversation-store.ts</files>
  <read_first>
    - src/ui/state/conversation-store.ts (final state after Tasks 1-2)
    - src/ui/state/conversation-store.test.ts (final state after Task 2)
    - src/ui/AppShell.tsx (final state after Task 3)
  </read_first>
  <action>
    Run the full verification sweep per CLAUDE.md project rule (backend + frontend builds; targeted tests across all files this phase touched across all 4 plans):

    1. `cd /home/ubuntu/skynet-tanya && npm run build:backend && npm run build` — both must exit 0.
    2. `cd /home/ubuntu/skynet-tanya && npx vitest run src/backend/database/routes/sessions.test.ts src/backend/fleet-status/ssh-poll-orchestrator.test.ts src/ui/state/session-working-store.test.ts src/ui/state/conversation-store.test.ts src/ui/AppShell.persistence.test.tsx` — all files exit 0.
    3. Full sweep for the fleet-status + related tests: `npx vitest run src/backend/fleet-status/ src/backend/database/routes/ src/backend/claude-session/ src/ui/state/ src/ui/api/` — no regressions.
    4. If any prior test fails due to a Phase 43 semantics change that was NOT covered by Task 2's null-to-top → null-to-bottom flip update, hand-inspect the failure and update the assertion with an inline Phase 43 marker comment. Do NOT skip tests; do NOT introduce `as any` casts.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tanya && npm run build:backend && npm run build && npx vitest run src/backend/fleet-status/ src/backend/database/routes/ src/backend/claude-session/ src/ui/state/ src/ui/api/</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build:backend` exit 0.
    - `npm run build` exit 0.
    - `npx vitest run` across the listed dirs exit 0.
    - `git diff HEAD -- src/ui/state/conversation-store.ts src/ui/AppShell.tsx src/ui/state/conversation-store.test.ts | grep -c 'as any\\|@ts-expect-error'` returns 0.
    - `git diff HEAD -- src/ui/features/pretty-conversations/ src/backend/fleet-status/wire-protocol.ts src/backend/claude-session/discover-identity-session-file.ts | grep -c 'function\\|const\\|class'` — inspect: only export-keyword additions permitted on discover-identity-session-file.ts (per Plan 43-02 scope_fence enforcement); zero diffs permitted on pretty-conversations/ subdir (CONTEXT.md scope_fence hard block); zero diffs permitted on wire-protocol.ts (CONTEXT.md scope_fence hard block).
  </acceptance_criteria>
  <done>Whole-phase build + test sweep green; scope_fence enforced on hard-blocked files; no type-safety escape hatches.</done>
</task>

</tasks>

<verification>
- FleetSession type carries optional lastMessageAt; cache round-trip preserves it; cache key bumped to v2.
- compareByRecencyDesc Rule 1 flipped null-to-bottom; docblock updated; branches assert flipped return values.
- All comparator tests (Test C, Test J, new Test L, new Test M) reflect null-to-bottom ordering.
- AppShell seeds working-store from both cached and fresh /sessions/list rows via seedSessionLastMessageAt.
- Full test sweep across state, backend routes, fleet-status, AppShell all green.
- CONTEXT.md scope_fence hard-blocked files (PrettyConversationsPanel.tsx, pretty-conversations.css, wire-protocol.ts schema) show zero diffs.
</verification>

<success_criteria>
- The middle zone in PrettyConversationsPanel renders freshest-message rows at the top; no-history rows sink to the bottom.
- Dormant identities that were previously stuck at the top of the middle now sit at the bottom (their /sessions/list-seeded lastMessageAt is a real number pointing at their last message-bearing frame from the JSONL, not null).
- Live identities render at the correct DESC-by-recency position regardless of whether the WS-live signal or the /sessions/list-seed signal landed first (max-wins reconciliation in Plan 43-03 handles ordering).
- No visible/CSS change in the panel; the fix ships purely via correct data landing at the pre-existing comparator.
</success_criteria>

<output>
Create `.planning/phases/43-fix-convo-list-recency-signal-switch-dormant-live-paths-to-i/43-04-SUMMARY.md` when done.
</output>
