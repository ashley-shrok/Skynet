---
phase: 75-server-side-substrate-bootstrap-startup-driven-install-pass-
plan: 07
type: execute
wave: 2
depends_on:
  - 75-01
  - 75-05
files_modified:
  - src/backend/fleet-status/ssh-poll-orchestrator.ts
  - src/backend/fleet-status/ssh-poll-orchestrator.test.ts
autonomous: true
requirements: []
decisions_addressed:
  - "D-04 (existing browser-driven install-pass hook at ssh-poll-orchestrator.ts:2087-2152 is removed; server-context path fully supersedes it — keeping the belt-and-suspenders is redundant no-op code because per-item idempotence already handles the no-change case)"

must_haves:
  truths:
    - "The fleet-substrate sweep hook block at ssh-poll-orchestrator.ts:2087-2152 is deleted"
    - "The `sweepedThisInstance` and `sweepInFlight` Set declarations (currently at lines 822-833) are deleted"
    - "The `sweepedThisInstance.clear()` and `sweepInFlight.clear()` calls in the orchestrator's stop() method (currently at lines 2309-2310) are deleted"
    - "The `import { runSweepForHost } from '../distributor/run-sweep.js'` at ssh-poll-orchestrator.ts:60 is deleted IF AND ONLY IF grep confirms no other caller uses it in the file after the removals above; if it has other callers, leave it"
    - "The `import { logSweepHookError } from '../distributor/log-tags.js'` (if present and only used by the removed block) is deleted subject to the same grep-first rule"
    - "The bundledReaderFromDisk import added in 75-01 STAYS if any other code in the file uses it; otherwise it too is removed. As of pre-removal, only the deleted sweep hook consumed it (verify)."
    - "Existing ssh-poll-orchestrator tests either still pass (if they don't cover the removed block) or are updated in this same task to drop tests that exercised the removed sweep hook"
    - "Fleet-status behavior — polling, stop-hook install, per-host livenessMap, identity-recycle — is BYTE-UNCHANGED. Only the substrate-sweep block goes away."
  artifacts:
    - path: "src/backend/fleet-status/ssh-poll-orchestrator.ts"
      provides: "Fleet-status orchestrator with substrate-sweep hook removed; fleet-status behavior otherwise unchanged"
    - path: "src/backend/fleet-status/ssh-poll-orchestrator.test.ts"
      provides: "Test suite with removed-block tests deleted; remaining tests still green"
  key_links:
    - from: "src/backend/fleet-status/ssh-poll-orchestrator.ts"
      to: "src/backend/distributor/run-sweep.ts"
      via: "REMOVED — no longer imports runSweepForHost (unless other callers remain)"
      pattern: "runSweepForHost"
    - from: "src/backend/fleet-status/ssh-poll-orchestrator.ts"
      to: "sweepedThisInstance / sweepInFlight state"
      via: "REMOVED — no longer holds this state"
      pattern: "sweepedThisInstance|sweepInFlight"
---

<objective>
Remove the browser-session-driven fleet-substrate install-pass hook now that the server-context orchestrator (wired by 75-05) is proven to fire on every container start and on every host-add/flag-flip/credential-change (75-06). Per D-04: keeping the old hook as belt-and-suspenders would be redundant no-op code — per-item idempotence in the catalog already handles the case where both hooks fire against the same host (second-arriving sweep sees every catalog item already up-to-date and no-ops).

Deliberately scheduled in Wave 2 (same wave as 75-05 and 75-06), NOT before. RESEARCH Pitfall 6 warned: "Removing sweep hook before server-context orchestrator is wired" leaves substrate hosts with NO sweeps during the deployment window. Wave-2 co-scheduling ensures the new path is live before the old path is deleted.

Purpose: Deliver D-04 (redundant hook removed).

Output: Deletions in ssh-poll-orchestrator.ts and its test file, no additions except (possibly) removing now-unused imports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-server-side-substrate-bootstrap-startup-driven-install-pass-/75-CONTEXT.md
@.planning/phases/75-server-side-substrate-bootstrap-startup-driven-install-pass-/75-RESEARCH.md
@.planning/phases/75-server-side-substrate-bootstrap-startup-driven-install-pass-/75-PATTERNS.md
@.planning/phases/75-server-side-substrate-bootstrap-startup-driven-install-pass-/75-05-SUMMARY.md
@.planning/phases/75-server-side-substrate-bootstrap-startup-driven-install-pass-/75-06-SUMMARY.md
@src/backend/fleet-status/ssh-poll-orchestrator.ts
@src/backend/fleet-status/ssh-poll-orchestrator.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete substrate-sweep block, state, cleanup, and dead imports; update tests</name>
  <read_first>
    - src/backend/fleet-status/ssh-poll-orchestrator.ts (lines 55-65 — imports for runSweepForHost and logSweepHookError; identify which are ONLY used by the sweep hook)
    - src/backend/fleet-status/ssh-poll-orchestrator.ts (lines 820-840 — sweepedThisInstance and sweepInFlight declarations; identify their EXACT line range including surrounding comments)
    - src/backend/fleet-status/ssh-poll-orchestrator.ts (lines 2085-2155 — the sweep hook block; identify exact opening comment and closing brace)
    - src/backend/fleet-status/ssh-poll-orchestrator.ts (lines 2300-2315 — sweepedThisInstance.clear() and sweepInFlight.clear() in stop(); identify exact lines and surrounding comment)
    - src/backend/fleet-status/ssh-poll-orchestrator.test.ts (grep for tests that reference "sweepedThisInstance", "sweepInFlight", "runSweepForHost", "fleet-substrate", "runsFleetSubstrate" — these are the tests that must be adjusted or removed)
    - src/backend/distributor/bundled-reader.ts (created in 75-01; if the sweep hook was the only consumer of the extracted reader inside ssh-poll-orchestrator.ts, the reader is now unused HERE — but the reader stays alive because the new orchestrator (75-02) uses it; only the IMPORT inside ssh-poll-orchestrator.ts becomes removable)
  </read_first>
  <action>
    Step A — Grep for reference counts BEFORE deletion. Run these first and record the counts:
    - `grep -c "runSweepForHost" src/backend/fleet-status/ssh-poll-orchestrator.ts`
    - `grep -c "logSweepHookError" src/backend/fleet-status/ssh-poll-orchestrator.ts`
    - `grep -c "bundledReaderFromDisk" src/backend/fleet-status/ssh-poll-orchestrator.ts`
    - `grep -c "FLEET_SUBSTRATE_CATALOG" src/backend/fleet-status/ssh-poll-orchestrator.ts`
    - `grep -c "sweepedThisInstance" src/backend/fleet-status/ssh-poll-orchestrator.ts`
    - `grep -c "sweepInFlight" src/backend/fleet-status/ssh-poll-orchestrator.ts`
    - `grep -c "IdentityHostingHostRecord" src/backend/fleet-status/ssh-poll-orchestrator.ts` (this is the extended-host type used by the sweep hook; check whether it's referenced elsewhere)

    Step B — Delete the sweep hook body. Precise ranges (verify by reading before deleting — line numbers may drift slightly):

    1. Lines 2087-2152 (the entire `// Phase 72 Plan 04 — fleet-substrate sweep hook…` comment through the closing `}` of the `if (extHost.runsFleetSubstrate === true && !sweepedThisInstance.has(...))` block). Delete all of it. This is the largest deletion.

    2. Lines 822-833 (the `// Phase 72 Plan 04 — fleet-substrate sweep-hook once-per-host gating.` comment block and the two Set declarations `sweepedThisInstance` + `sweepInFlight`). Delete all of it.

    3. Lines 2305-2310 (the `// Phase 72 Plan 04 — clear sweep-hook gating alongside perHostState.` comment and the two `.clear()` calls). Delete both `.clear()` calls and the comment.

    Step C — Remove dead imports based on the Step A counts:

    - If `runSweepForHost` count went from 4 → 0 after deletions, remove the import at line 60: `import { runSweepForHost } from "../distributor/run-sweep.js";`
    - If `logSweepHookError` was only referenced inside the removed sweep hook, remove its import (check line ~55-65 for the import statement)
    - If `bundledReaderFromDisk` was only referenced by the removed sweep hook's `readBundledBytes:` argument, remove the import added in 75-01: `import { bundledReaderFromDisk } from "../distributor/bundled-reader.js";`
    - If `FLEET_SUBSTRATE_CATALOG` (referenced at line 2122 as `FLEET_SUBSTRATE_CATALOG`) is only used in the removed block, remove its import
    - If `IdentityHostingHostRecord` type is only used at line 2103 (the removed block), remove its import too (but be careful: this type may be re-exported for other consumers — grep the whole codebase before removing the type ITSELF)

    Step D — Also verify `readFile` / `stat` from `node:fs/promises` are still needed elsewhere in the file. RESEARCH.md § Removal Target step 4 flagged this — if the ONLY consumer was the local bundledReaderFromDisk (removed in 75-01), and no other consumer emerged, remove those imports too. Grep confirms.

    Step E — Update tests. Read `src/backend/fleet-status/ssh-poll-orchestrator.test.ts` and find every test that exercises the removed sweep hook:

    - Any test that asserts on `sweepedThisInstance` state → delete the test
    - Any test that mocks `runSweepForHost` from run-sweep.js → delete or convert (if the mock was defensive-only and the test's real point was fleet-status behavior, adjust; if the test's point was the sweep, delete)
    - Any test that sets `runsFleetSubstrate: true` on a host and then asserts a sweep fired → delete

    Grep for markers: `grep -n "sweepedThisInstance\|sweepInFlight\|runSweepForHost\|fleet_substrate\|runsFleetSubstrate" src/backend/fleet-status/ssh-poll-orchestrator.test.ts` — every hit needs decision (delete test or adjust).

    Do NOT delete tests that just happen to project runsFleetSubstrate in a mocked host record for reasons unrelated to the sweep hook (e.g., a test checking that fleet-status pollAllHosts loops all hosts regardless of the substrate flag).

    Step F — Run the full ssh-poll-orchestrator test suite. Iterate on test deletions/adjustments until green.

    Step G — Run TypeScript check to catch any references to removed symbols elsewhere in the codebase.

    Commit as `refactor(75-07): remove browser-driven substrate-sweep hook from ssh-poll-orchestrator`.
  </action>
  <verify>
    <automated>npx vitest run src/backend/fleet-status/ssh-poll-orchestrator.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sweepedThisInstance" src/backend/fleet-status/ssh-poll-orchestrator.ts` returns 0 (state gone)
    - `grep -c "sweepInFlight" src/backend/fleet-status/ssh-poll-orchestrator.ts` returns 0 (state gone)
    - `grep -c "Phase 72 Plan 04" src/backend/fleet-status/ssh-poll-orchestrator.ts` returns 0 (all sweep-hook comment blocks gone)
    - `grep -c "fleet-substrate sweep hook" src/backend/fleet-status/ssh-poll-orchestrator.ts` returns 0 (removed block's opening comment gone)
    - If `runSweepForHost` import was removed: `grep -c "runSweepForHost" src/backend/fleet-status/ssh-poll-orchestrator.ts` returns 0
    - `grep -c "IdentityHostingHostRecord\|SubstrateHostRecord" src/backend/fleet-status/ssh-poll-orchestrator.ts` — should be 0 or unchanged from before (if the type has other consumers, it stays; if not, remove)
    - `npx vitest run src/backend/fleet-status/ssh-poll-orchestrator.test.ts` exits 0
    - `npx tsc --noEmit` exits 0 (no dangling references anywhere in the tree)
    - Bundled-reader module (75-01) still has consumers: `grep -rn "bundledReaderFromDisk" src/backend/` shows at minimum one live consumer (the new orchestrator from 75-02) — verify by inspection, not by count-gate
  </acceptance_criteria>
  <done>
    Legacy sweep hook removed. Fleet-status orchestrator is smaller and single-purpose (poll host status, install stop hooks, drive identity recycle). Substrate sweep now runs ONLY through the server-context path from 75-05.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None crossed by this plan. Pure deletion of code that has been superseded by an equivalent server-context path.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-07-01 | Denial-of-service | Removing the browser-driven sweep hook BEFORE the server-context orchestrator is proven live would leave substrate hosts with no sweeps during the deployment window | mitigate | This plan runs in the SAME wave as 75-05 (which wires the new orchestrator into starter.ts) and 75-06 (which wires the on-add trigger). All three must be present on the container image at deploy time — the orchestrator's Wave-2 co-scheduling is the mitigation. Additionally: file-ownership rules force 75-07 into a wave >= 1 (because 75-01 also modifies ssh-poll-orchestrator.ts in Wave 0), so the sequencing is enforced by wave assignment rather than trust. |
| T-75-07-02 | Repudiation | An operator relying on browser-driven sweeps for troubleshooting would notice sweeps no longer fire on browser login | accept | The intended user-facing behavior IS that browser login is no longer required for sweeps. This is the point of Phase 75. Communicated via post-ship briefing to Stacy (see 75-CONTEXT.md § coordination notes). |
| T-75-07-03 | Tampering | Removed test cases could have caught a regression in the removed code | accept | The removed tests exercise removed code — by definition, there is no regression to catch. Any remaining test that indirectly covers fleet-status behavior stays. |
| T-75-07-04 | Denial-of-service | The removal step could accidentally delete more than intended, e.g., taking out a shared type that other modules depend on | mitigate | Step A grep-counts identify shared dependencies BEFORE any deletion. `npx tsc --noEmit` gate at end catches any missed references anywhere in the tree. |

**Load-bearing security invariant:** Nothing new; removal is defensive-code-cleanup. All previously-audited security invariants remain in effect via the new server-context path (75-02, 75-03, 75-04, 75-05, 75-06).
</threat_model>

<verification>
- All ssh-poll-orchestrator tests pass after adjustments
- `npx tsc --noEmit` clean across whole tree
- All state/comment/block deletions verified by post-delete grep counts
- bundled-reader.ts still has at least one consumer (via 75-02's new orchestrator)
</verification>

<success_criteria>
- Substrate sweep code removed from `src/backend/fleet-status/ssh-poll-orchestrator.ts`
- Related state (`sweepedThisInstance`, `sweepInFlight`) removed
- Related cleanup code in stop() removed
- Dead imports removed (per Step A/C grep-first rule)
- Test suite updated: no test asserts on removed behavior; all remaining tests green
- tsc clean; no dangling references anywhere in the tree
- Fleet-status behavior otherwise byte-unchanged
</success_criteria>

<output>
Create `.planning/phases/75-server-side-substrate-bootstrap-startup-driven-install-pass-/75-07-SUMMARY.md` when done.

Critical items to record:
1. Exact list of imports removed vs. imports left in place (with grep-count justification for each decision).
2. Exact list of test cases deleted (by name) and test cases adjusted.
3. Confirmation that ssh-poll-orchestrator.ts still has a live import graph (i.e., no broken imports anywhere in the tree).
4. Line-count delta of ssh-poll-orchestrator.ts (should shrink by ~85-100 lines total: ~66 for the sweep hook + ~12 for state + ~6 for clear + ~3 for imports).
</output>
