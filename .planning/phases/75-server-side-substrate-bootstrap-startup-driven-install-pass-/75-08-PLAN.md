---
phase: 75-server-side-substrate-bootstrap-startup-driven-install-pass-
plan: 08
type: execute
wave: 3
depends_on: []
files_modified:
  - src/backend/utils/user-crypto.ts
  - src/backend/utils/user-crypto.test.ts
  - src/backend/utils/substrate-credential-migration.ts
  - src/backend/utils/substrate-credential-migration.test.ts
  - scripts/migrate-substrate-credentials.ts
autonomous: true
requirements: []
decisions_addressed:
  - "D-12 (one-shot operator-run migration script ships in-phase)"
  - "D-13 (migration takes every existing substrate-host owner's key material as input — feasible because both live instances have small known populations)"
  - "D-14 (per-host migration: unwrap using owner's key material → re-wrap with CSKEK → update record; atomic per host)"
  - "D-15 (migration script is retired after ship — no ongoing use expected)"
  - "D-19 (migration script has its own test using a fixture database with per-user-DEK-wrapped credentials → runs migration → verifies CSKEK-wrapped output + working decrypt, INCLUDING passphrase-protected keys — see test H0)"

must_haves:
  truths:
    - "A new public method `UserCrypto.deriveDekForMigration(userId, password): Promise<Buffer>` exists that reads the user's KEK salt + encrypted DEK from the database, derives the KEK via PBKDF2 (matching the login-time parameters), decrypts the DEK, and RETURNS the DEK Buffer WITHOUT registering a userSessions entry — sessionless. Reachability of the private method chain (`getKEKSalt`, `deriveKEK`, `getEncryptedDEK`, `decryptDEK`) is VERIFIED against user-crypto.ts:493-501 (deriveKEK), 566-582 (getKEKSalt), 606-622 (getEncryptedDEK), 533-545 (decryptDEK) — all four are private instance methods on the same UserCrypto class, so a new public method on the same class can call them directly."
    - "The new module `src/backend/utils/substrate-credential-migration.ts` exports `migrateSubstrateCredentials(input: MigrationInput): Promise<MigrationResult>` where input is an array of `{userId, password}` and result is `{ perUser: Array<{userId, migrated, failed, skipped, hosts: Array<{hostId, credentialId, status}>}>, preCheck: { inlineCredentialSubstrateHosts: number[] } }`"
    - "Pre-check step: before any migration writes, the module queries the DB for hosts where `runsFleetSubstrate=true AND credentialId IS NULL` — if any exist, migration ABORTS with a loud error listing those host IDs. The 75-04 API-layer guard prevents NEW such rows; the pre-check catches PRE-EXISTING rows that predate the guard. Aborting rather than proceeding lets the operator investigate before any state change."
    - "For each `{userId, password}` in input: derive DEK sessionlessly → query `sshCredentials JOIN hosts ON hosts.credentialId = sshCredentials.id WHERE hosts.runs_fleet_substrate=true AND sshCredentials.userId=userId` → for each credential row, decrypt password/key/keyPassword with user DEK using the EXACT fieldName strings that the production write path used (`\"password\"`, `\"key\"`, `\"keyPassword\"` — camelCase for the passphrase per credential-system-encryption-migration.ts:67), re-encrypt with CSKEK using the CSKEK-side fieldName convention (`\"password\"`, `\"key\"`, `\"key_password\"` — snake_case for the passphrase per credential-system-encryption-migration.ts:94), update the sshCredentials.systemPassword/systemKey/systemKeyPassword columns → after each user's batch, call `DatabaseSaveTrigger.forceSave` to flush the in-memory SQLite to disk"
    - "The DECRYPT-side fieldName MUST match what the production encryption path used when it first wrote the ciphertext. VERIFIED against the existing analog: the user-DEK-side of credential-system-encryption-migration.ts uses `\"keyPassword\"` (camelCase) at line 67; the CSKEK-side uses `\"key_password\"` (snake_case) at line 94. This is an EXISTING production inconsistency and MUST be preserved verbatim — 'fixing' the DECRYPT side to snake_case would silently fail to decrypt every passphrase-protected substrate credential in production. Any executor tempted to unify the two sides MUST stop and raise a separate plan (out of scope for 75-08)."
    - "Atomicity per host (D-14): each per-credential update is a single drizzle `db.update(sshCredentials).set({...}).where(eq(...))` — if it throws, that host is left unchanged and the failure is logged; other hosts continue"
    - "Migration NEVER logs plaintext passwords, plaintext keys, CSKEK, or user DEKs"
    - "After all users processed, DEKs derived during migration are zeroed (`dek.fill(0)`) — matches UserCrypto's own KEK/DEK zeroing discipline at lines 66-67, 111, 154, etc."
    - "A CLI entrypoint at `scripts/migrate-substrate-credentials.ts` accepts JSON input via stdin (`[{userId, password}, ...]`) and prints the MigrationResult summary as JSON to stdout — invokable via `npx tsx scripts/migrate-substrate-credentials.ts < input.json` inside the container"
    - "The script never echoes the input to stdout or to any log — stdin containing plaintext passwords is consumed once, deriving DEKs, and the passwords are then dropped (the input array reference is not retained)"
    - "Test H0 (passphrase-protected key round-trip) exists in substrate-credential-migration.test.ts — proves the `keyPassword` fieldName chain end-to-end (user-DEK encrypt with `\"keyPassword\"` → migration decrypts with `\"keyPassword\"` → migration re-encrypts with CSKEK as `\"key_password\"` → CSKEK decrypt with `\"key_password\"` recovers the original passphrase). This test is the canary that catches any future fieldName drift on either side."
  artifacts:
    - path: "src/backend/utils/user-crypto.ts"
      provides: "New public sessionless-derive method `deriveDekForMigration(userId, password)` added; existing UserCrypto behavior unchanged"
      exports: ["deriveDekForMigration"]
    - path: "src/backend/utils/user-crypto.test.ts"
      provides: "Tests for the new derive method: happy path returns DEK; wrong password → throws; nonexistent user → throws; multiple calls do not populate userSessions"
    - path: "src/backend/utils/substrate-credential-migration.ts"
      provides: "The migration module — pre-check, per-user derive, per-host decrypt+re-encrypt, forceSave, structured result"
      exports: ["migrateSubstrateCredentials", "type MigrationInput", "type MigrationResult"]
      min_lines: 150
    - path: "src/backend/utils/substrate-credential-migration.test.ts"
      provides: "Fixture-DB tests: unmigrated → migrated, wrong password fails one user without breaking others, pre-check catches inline-credential substrate hosts and aborts, D-19 round-trip decrypt via CSKEK proves the output is usable, H0 passphrase-protected key round-trip catches fieldName drift"
      min_lines: 200
    - path: "scripts/migrate-substrate-credentials.ts"
      provides: "CLI entrypoint for operator invocation"
      min_lines: 40
  key_links:
    - from: "src/backend/utils/substrate-credential-migration.ts"
      to: "src/backend/utils/user-crypto.ts"
      via: "UserCrypto.getInstance().deriveDekForMigration(userId, password)"
      pattern: "deriveDekForMigration"
    - from: "src/backend/utils/substrate-credential-migration.ts"
      to: "src/backend/utils/system-crypto.ts"
      via: "SystemCrypto.getInstance().getCredentialSharingKey()"
      pattern: "getCredentialSharingKey"
    - from: "src/backend/utils/substrate-credential-migration.ts"
      to: "src/backend/utils/field-crypto.ts"
      via: "FieldCrypto.decryptField(userDEK) + FieldCrypto.encryptField(CSKEK)"
      pattern: "FieldCrypto\\.(en|de)cryptField"
    - from: "src/backend/utils/substrate-credential-migration.ts"
      to: "DatabaseSaveTrigger"
      via: "await DatabaseSaveTrigger.forceSave('substrate-cred-migration')"
      pattern: "DatabaseSaveTrigger.forceSave"
    - from: "scripts/migrate-substrate-credentials.ts"
      to: "src/backend/utils/substrate-credential-migration.ts"
      via: "await migrateSubstrateCredentials(input)"
      pattern: "migrateSubstrateCredentials"
---

<objective>
Ship the one-shot operator-run migration that transitions existing substrate-host credentials from per-user-DEK-only to CSKEK-wrapped. This is the deliverable that lets Phase 75 ship without breaking existing production data — hosts that were provisioned before this phase have their `sshCredentials.system_password/system_key/system_key_password` columns either NULL or populated with values that predate the substrate-CSKEK convention. The migration fills those columns correctly using each owner's key material as input.

The migration script has three parts:
1. **UserCrypto extension** — add `deriveDekForMigration(userId, password)` for sessionless DEK derivation. Resolves RESEARCH open question 2 (the exact sessionless-derive shape). VERIFIED that the four private methods needed (`getKEKSalt` line 566, `deriveKEK` line 493, `getEncryptedDEK` line 606, `decryptDEK` line 533) are all instance methods on the same UserCrypto class, so a new public instance method can call them directly without refactoring.
2. **Migration module** — pre-check for inline-credential substrate hosts (abort loudly if any exist), then per-user derive → per-host decrypt+re-encrypt → forceSave. FieldName strings on the DECRYPT side match the production write path (`"keyPassword"` camelCase for the passphrase per credential-system-encryption-migration.ts:67); FieldName strings on the ENCRYPT side match the CSKEK write path (`"key_password"` snake_case for the passphrase per credential-system-encryption-migration.ts:94). This inconsistency is EXISTING production behavior and MUST be preserved.
3. **CLI entrypoint** — stdin-JSON-in, stdout-JSON-out. Never echoes the input, never logs plaintext.

Independent of the runtime code (75-02, 75-04, 75-05, 75-06, 75-07 — none of them touch the migration path). Wave 3 assignment reflects that it can be developed and tested in parallel with the integration test in 75-09.

Purpose: Deliver D-12, D-13, D-14, D-15, D-19. Unblock Ashley's post-ship operational step: `npx tsx scripts/migrate-substrate-credentials.ts < ~/known-users.json` runs on both live instances (this one + T800).

Output: One method added to UserCrypto + two new modules + CLI script + comprehensive tests including the D-19 round-trip and the H0 passphrase-canary that catches fieldName drift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-server-side-substrate-bootstrap-startup-driven-install-pass-/75-CONTEXT.md
@.planning/phases/75-server-side-substrate-bootstrap-startup-driven-install-pass-/75-RESEARCH.md
@.planning/phases/75-server-side-substrate-bootstrap-startup-driven-install-pass-/75-PATTERNS.md
@src/backend/utils/user-crypto.ts
@src/backend/utils/credential-system-encryption-migration.ts
@src/backend/utils/system-crypto.ts
@src/backend/utils/field-crypto.ts
@src/backend/utils/database-save-trigger.ts
@src/backend/database/db/schema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add UserCrypto.deriveDekForMigration + tests</name>
  <read_first>
    - src/backend/utils/user-crypto.ts (full file). Key line ranges VERIFIED for this plan:
      - `authenticateUser` at lines 114-165 — the closest analog. The new method mirrors lines 119-140 (`getKEKSalt` → `deriveKEK` → `getEncryptedDEK` → `decryptDEK`) BUT skips lines 149-152 (setting userSessions) and lines 157-164 (the log-on-failure block, which should be REPLACED with a throw for the migration path so callers see failures loudly).
      - `deriveKEK` at lines 493-501 (private instance method, PBKDF2 with 100k iterations, returns Buffer)
      - `getKEKSalt` at lines 566-582 (private instance method, returns `KEKSalt | null` from the settings table)
      - `getEncryptedDEK` at lines 606-622 (private instance method, returns `EncryptedDEK | null` from the settings table)
      - `decryptDEK` at lines 533-545 (private instance method, aes-256-gcm decrypt with authTag)
      All four are private instance methods on the `UserCrypto` class. Adding a new PUBLIC instance method to the same class body gives direct access to all four via `this.*`. NO refactoring of the existing private surface is needed.
    - src/backend/utils/user-crypto.test.ts (if it exists — inspect existing patterns; if not, this task creates it with the new tests only)
  </read_first>
  <behavior>
    Tests for `deriveDekForMigration`:
    - Test D1 (happy path): Call `UserCrypto.getInstance().setupUserEncryption("u1", "correct-pass")` (fixture setup). Then `const dek = await UserCrypto.getInstance().deriveDekForMigration("u1", "correct-pass")`. Assert `dek instanceof Buffer && dek.length === 32`.
    - Test D2 (wrong password throws): setup with "correct-pass"; call derive with "wrong-pass" → throws (the exact error message shape depends on Node's aes-256-gcm authTag failure — do not assert exact wording, just that the call rejects; wrong KEK → wrong decrypt → authTag mismatch → throw from decipher.final())
    - Test D3 (nonexistent user throws): call derive with a userId that never had setupUserEncryption → throws with a message mentioning "KEK salt" or "not onboarded" (deriveDekForMigration converts the null return from getKEKSalt into a throw)
    - Test D4 (sessionless — does NOT touch userSessions): call setupUserEncryption → assert `getInstance().isUserUnlocked("u1") === false`. Call derive. Assert `isUserUnlocked("u1") === false` AFTER the derive call (session was never populated).
    - Test D5 (repeatable): Two consecutive derive calls with the same {userId, password} return DEKs whose bytes are equal (`dek1.equals(dek2)` — DEK is deterministic from the stored encryptedDEK + password).
  </behavior>
  <action>
    RED phase: Create or extend `src/backend/utils/user-crypto.test.ts` with tests D1-D5. Use a `beforeEach` that resets the in-memory DB and clears any user records from prior tests. Run tests, expect failure. Commit as `test(75-08): add failing deriveDekForMigration tests`.

    GREEN phase: Add a new public method to the `UserCrypto` class in `src/backend/utils/user-crypto.ts`. Place it near `authenticateUser` — the natural spot is immediately after `authenticateOIDCUser` closes (around line 246), before `getUserDataKey` (line 248). The method must:

    - Have signature `async deriveDekForMigration(userId: string, password: string): Promise<Buffer>`.
    - Include a JSDoc block explaining: (a) sessionless — does NOT populate userSessions; (b) returned Buffer is caller-owned and caller MUST zero it after use; (c) throws when the user has no KEK salt (not onboarded) or when the password does not decrypt the stored encrypted DEK; (d) DO NOT USE for authenticated user flows — use `authenticateUser` instead; (e) exists exclusively for operator-driven data migrations (Phase 75-08).
    - Body sequence:
      1. `const kekSalt = await this.getKEKSalt(userId);` — throws with a clear message if null (e.g., "No KEK salt for user ${userId} — user may not be onboarded").
      2. `const encryptedDEK = await this.getEncryptedDEK(userId);` — throws with a clear message if null.
      3. `const KEK = this.deriveKEK(password, kekSalt);` (returns Buffer).
      4. Inside a `try { ... } finally { KEK.fill(0); }`: `const DEK = this.decryptDEK(encryptedDEK, KEK);`. If DEK is empty (`!DEK || DEK.length === 0`), throw a clear "wrong password" error. Return `Buffer.from(DEK)` — the `Buffer.from(...)` copy is intentional so the caller's `dek.fill(0)` cleanup zeroes their own reference without touching any (paranoid, though there is no such reference today) internal aliases.

    Do NOT change `deriveKEK`, `getKEKSalt`, `getEncryptedDEK`, or `decryptDEK` to public — the new method is a public wrapper that composes them. Do NOT call `this.userSessions.set(...)` anywhere in the new method — that would defeat the sessionless contract (Test D4 catches this).

    Verify the method is included in the exported class surface (should be automatic — it is a public instance method on `UserCrypto`, which is already exported).

    Run tests, iterate. Commit as `feat(75-08): add UserCrypto.deriveDekForMigration for sessionless one-shot migrations`.
  </action>
  <verify>
    <automated>npx vitest run src/backend/utils/user-crypto.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "deriveDekForMigration" src/backend/utils/user-crypto.ts` returns >= 1 (method declaration)
    - `grep -c "async deriveDekForMigration" src/backend/utils/user-crypto.ts` returns 1 (async method signature)
    - The method sits inside the `class UserCrypto` block (not exported standalone) — verify by inspection
    - `grep -c "userSessions.set" src/backend/utils/user-crypto.ts` — count should be UNCHANGED vs. the pre-plan baseline (the new method does NOT call `userSessions.set`); confirm via `git diff` before merge
    - `grep -cE "^  it\(|^    it\(" src/backend/utils/user-crypto.test.ts` returns >= 5 (D1-D5)
    - `npx vitest run src/backend/utils/user-crypto.test.ts` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    UserCrypto has a sessionless-derive method. Migration module (Task 2) can consume it. No production auth path is affected. Private-method reachability confirmed at implementation time (no surprise refactor needed).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create substrate-credential-migration.ts + fixture-DB tests (including H0 passphrase-canary)</name>
  <read_first>
    - src/backend/utils/credential-system-encryption-migration.ts (full file — the exact structural analog; new module mirrors lines 24-136 with the divergences noted in behavior below). CRITICAL FIELDNAME OBSERVATION VERIFIED IN SOURCE: line 67 uses `"keyPassword"` (camelCase) on the user-DEK-DECRYPT side; line 94 uses `"key_password"` (snake_case) on the CSKEK-ENCRYPT side. This inconsistency is EXISTING production behavior — the migration MUST preserve BOTH strings verbatim on their respective sides. See warning in behavior.
    - src/backend/utils/user-crypto.ts (as modified in Task 1 above — the deriveDekForMigration entrypoint)
    - src/backend/utils/database-save-trigger.ts (lines 62-100 — the forceSave API; new module calls this after each user batch)
    - src/backend/database/db/schema.ts (lines 130-190 — sshCredentials columns: id, userId, password, key, keyPassword, keyType, username, systemPassword, systemKey, systemKeyPassword; and hosts columns needed for the join)
    - PATTERNS.md § substrate-credential-migration section (lines 270-360 area — the full field-by-field pattern)
  </read_first>
  <behavior>
    ⚠ **FieldName inconsistency warning (read this before writing tests)**: The production analog at `credential-system-encryption-migration.ts` uses DIFFERENT fieldName strings on the two sides of the passphrase field:
    - **DECRYPT side (user-DEK → plaintext)**: `FieldCrypto.decryptField(row.keyPassword, userDEK, credIdStr, "keyPassword")` — camelCase (line 67)
    - **ENCRYPT side (plaintext → CSKEK)**: `FieldCrypto.encryptField(plainKeyPassword, CSKEK, credIdStr, "key_password")` — snake_case (line 94)

    This means: existing production ciphertext in `sshCredentials.keyPassword` was written by some earlier code path with the fieldName `"keyPassword"` (camelCase). The migration MUST decrypt with `"keyPassword"` to match. When re-encrypting to `sshCredentials.systemKeyPassword`, the migration uses `"key_password"` (snake_case) to match the CSKEK side of the existing analog.

    Test H0 is the load-bearing canary that catches ANY future drift on either side.

    Test suite for `migrateSubstrateCredentials`:

    Pre-check (D-08 pre-condition — inline-credential substrate hosts abort the migration):
    - Test PC1: Fixture DB has 1 host with `runsFleetSubstrate=true AND credentialId=null` → call migrate with any input → result.preCheck.inlineCredentialSubstrateHosts contains that host's ID; migration does NOT proceed (no rows updated); function returns with `aborted: true`
    - Test PC2: Fixture DB has 0 inline-credential substrate hosts → pre-check passes; migration proceeds

    Happy path (D-13, D-14):
    - **Test H0 (PASSPHRASE-CANARY — LOAD-BEARING for fieldName correctness)**: Fixture DB has user u1 (onboarded via `setupUserEncryption("u1", "pw1")`) + 1 sshCredentials row where BOTH `key` and `keyPassword` columns are populated with user-DEK-encrypted values. To construct the fixture: call `UserCrypto.getInstance().authenticateUser("u1", "pw1", 60000)` to prime the session, then use `FieldCrypto.encryptField("<plain-key-material>", userDek, "<credId>", "key")` for the key column and `FieldCrypto.encryptField("<plain-passphrase>", userDek, "<credId>", "keyPassword")` for the keyPassword column (NOTE: `"keyPassword"` camelCase — matches production line 67). Insert the row. Create a host row with `runsFleetSubstrate=true, credentialId=<that cred's id>`. Call `migrateSubstrateCredentials([{userId:"u1", password:"pw1"}])`. Assert: (a) `result.perUser[0].migrated === 1, failed === 0`; (b) the sshCredentials row has `systemKey` and `systemKeyPassword` NOT NULL; (c) `FieldCrypto.decryptField(row.systemKey, CSKEK, credId, "key")` returns the original key plaintext; (d) `FieldCrypto.decryptField(row.systemKeyPassword, CSKEK, credId, "key_password")` returns the original passphrase plaintext (NOTE: `"key_password"` snake_case on CSKEK side — matches production line 94). This test proves the ENTIRE fieldName chain end-to-end. If either side drifts, H0 fails.
    - Test H1: Fixture DB has user u1 (onboarded via setupUserEncryption("u1", "pw1")) + 1 sshCredentials row (userId=u1, password=<encrypted with u1's DEK via FieldCrypto using fieldName `"password"`>) + 1 host (runsFleetSubstrate=true, credentialId=<that cred's id>). system_password column starts NULL. Call migrate([{userId:"u1", password:"pw1"}]) → result.perUser[0].migrated === 1, .failed === 0; after migration, the sshCredentials row has system_password NOT NULL AND FieldCrypto.decryptField(system_password, CSKEK, credId, "password") returns the original plaintext (this is the D-19 round-trip for the password field)
    - Test H2 (multiple hosts per user): u1 has 3 substrate hosts each with its own sshCredentials row → all 3 migrated in one pass; all 3 have working CSKEK decrypt after
    - Test H3 (multiple users): u1 has 2 hosts, u2 has 1 host → both users' hosts migrated in respective per-user passes; result.perUser has 2 entries

    Failure isolation (D-14 atomicity):
    - Test F1 (wrong password for one user does not affect others): u1 with correct password + u2 with wrong password → u1's hosts migrate; u2's entry has failed>=1 (or errored=true) but u1's entry has migrated=<count>; process does not throw
    - Test F2 (one cred row corrupt): if FieldCrypto.decryptField throws on one cred row within a user's batch, the OTHER rows in that user's batch still migrate; the failed row is logged and counted

    Scope (D-08 filter):
    - Test S1 (non-substrate hosts untouched): fixture has both substrate and non-substrate hosts owned by u1 → after migration, ONLY the substrate hosts have their system_* columns populated with the CSKEK re-wrap; the non-substrate hosts' rows are byte-unchanged
    - Test S2 (hosts owned by users NOT in input untouched): u1's hosts + u3's hosts (u3 not in migration input) → u3's hosts untouched

    Persistence (forceSave):
    - Test P1: DatabaseSaveTrigger.forceSave is called with a descriptive reason string after each user's batch completes; verify via vi.spyOn or mock

    Security discipline:
    - Test SEC1: Across the full migration run, no log call (systemLogger.warn/info/error, databaseLogger.warn/info/error) contains any plaintext password from the input OR the CSKEK Buffer hex representation OR the derived userDEK
    - Test SEC2: After migration completes, the DEK Buffers derived from the input passwords are zeroed (this is tricky to test in isolation — an integration approach: spy on UserCrypto.deriveDekForMigration to record returned Buffer references, then assert those Buffers are all-zeros at the end of migration)
  </behavior>
  <action>
    RED phase: Create `src/backend/utils/substrate-credential-migration.test.ts` with the ~13 tests above (H0 + H1-H3 + PC1-PC2 + F1-F2 + S1-S2 + P1 + SEC1-SEC2). Use a fixture DB — the codebase likely has a helper for spinning up an in-memory test DB; if not, use `beforeEach` to reset the shared in-memory DB and insert fixture rows via direct drizzle inserts. Mock time-consuming or noisy dependencies (`vi.mock("./logger.js")` at minimum). Run tests, expect failure. Commit as `test(75-08): add failing substrate-credential-migration test suite (H0 passphrase canary + full coverage)`.

    GREEN phase: Create `src/backend/utils/substrate-credential-migration.ts`. Structure follows the existing analog at `credential-system-encryption-migration.ts` with these load-bearing conventions:

    - Imports: `db` from `../database/db/index.js`, `sshCredentials, hosts` from `../database/db/schema.js`, `eq, and, isNull` from `drizzle-orm`, `SystemCrypto` from `./system-crypto.js`, `FieldCrypto` from `./field-crypto.js`, `UserCrypto` from `./user-crypto.js`, `DatabaseSaveTrigger` (adjust to actual export path), `databaseLogger` from `./logger.js`.

    - Public types: `MigrationInput { userId: string; password: string }`, `PerHostResult { hostId: number; credentialId: number; status: "migrated" | "failed" | "skipped" }`, `PerUserResult { userId: string; migrated: number; failed: number; skipped: number; hosts: PerHostResult[]; error?: string }`, `MigrationResult { preCheck: { inlineCredentialSubstrateHosts: number[] }; perUser: PerUserResult[]; aborted: boolean }`.

    - Public entrypoint: `async function migrateSubstrateCredentials(input: MigrationInput[]): Promise<MigrationResult>`.

    Body sequence:

    1. **Pre-check**: SELECT `hosts.id` WHERE `runsFleetSubstrate=true AND credentialId IS NULL`. If any rows, log at operation `substrate_migration_precheck_aborted` with the offending IDs; return `{ preCheck: { inlineCredentialSubstrateHosts: badIds }, perUser: [], aborted: true }` immediately (no further work).

    2. **Load CSKEK once**: `const CSKEK = await SystemCrypto.getInstance().getCredentialSharingKey();` — reused across all users.

    3. **Per-user loop** — for each `{userId, password}`:
       a. `let userDEK: Buffer | null = null;`
       b. `try { userDEK = await userCrypto.deriveDekForMigration(userId, password); ...` — the derive is inside the try so a wrong password lands in the outer catch.
       c. Query: `db.select({...}).from(sshCredentials).innerJoin(hosts, eq(hosts.credentialId, sshCredentials.id)).where(and(eq(sshCredentials.userId, userId), eq(hosts.runsFleetSubstrate, true)))`. Project all three cipher columns (`sshCredentials.password`, `sshCredentials.key`, `sshCredentials.keyPassword`) plus `hosts.id` and `sshCredentials.id`.
       d. **Per-row loop** (each row is wrapped in its own try/catch — F1/F2 atomicity):
          - Compute `credIdStr = String(row.credId)`.
          - **DECRYPT with user DEK** using EXACT production fieldName strings — verified against credential-system-encryption-migration.ts lines 44-96:
            - `plainPassword = row.credPassword ? FieldCrypto.decryptField(row.credPassword, userDEK, credIdStr, "password") : null;`
            - `plainKey = row.credKey ? FieldCrypto.decryptField(row.credKey, userDEK, credIdStr, "key") : null;`
            - `plainKeyPassword = row.credKeyPassword ? FieldCrypto.decryptField(row.credKeyPassword, userDEK, credIdStr, "keyPassword") : null;`
            - ⚠ **CRITICAL**: The passphrase field uses `"keyPassword"` (camelCase) on the DECRYPT side because that is what the production write path used (verified at credential-system-encryption-migration.ts:67). DO NOT change to snake_case — production ciphertext would fail to decrypt, and Test H0 would fail loudly.
          - **RE-ENCRYPT with CSKEK** using the CSKEK-side fieldName strings:
            - `systemPassword = plainPassword ? FieldCrypto.encryptField(plainPassword, CSKEK, credIdStr, "password") : null;`
            - `systemKey = plainKey ? FieldCrypto.encryptField(plainKey, CSKEK, credIdStr, "key") : null;`
            - `systemKeyPassword = plainKeyPassword ? FieldCrypto.encryptField(plainKeyPassword, CSKEK, credIdStr, "key_password") : null;`
            - ⚠ **CRITICAL**: The passphrase field uses `"key_password"` (snake_case) on the CSKEK-ENCRYPT side because that is what the production CSKEK write path used (verified at credential-system-encryption-migration.ts:94). This deliberate asymmetry with the DECRYPT side is EXISTING production behavior — Test H0 verifies both sides round-trip correctly with their respective fieldNames.
          - Atomic per-row update: `await db.update(sshCredentials).set({ systemPassword, systemKey, systemKeyPassword, updatedAt: new Date().toISOString() }).where(eq(sshCredentials.id, row.credId));`
          - On success: `perUserEntry.migrated += 1; perUserEntry.hosts.push({...status: "migrated"});`
          - On per-row throw: `perUserEntry.failed += 1; perUserEntry.hosts.push({...status: "failed"}); databaseLogger.warn("Substrate migration failed for credential ${row.credId}", { operation: "substrate_migration_host_failed", userId, credentialId, hostId, error: err.message });` — NEVER include `err.stack`, NEVER include plaintext.
       e. **After user's batch**: `try { await DatabaseSaveTrigger.forceSave(\`substrate-cred-migration-${userId}\`); } catch (saveError) { databaseLogger.warn("...", { operation: "substrate_migration_db_save_failed", userId, error: saveError.message }); }`.
       f. **Outer catch** (DEK-derive failure OR query failure): `perUserEntry.error = err.message; databaseLogger.warn(..., { operation: "substrate_migration_user_failed", userId, error });`.
       g. **Finally**: `if (userDEK) userDEK.fill(0);` — zero the DEK regardless of success/failure.
       h. `perUser.push(perUserEntry);`

    4. `return { preCheck: { inlineCredentialSubstrateHosts: [] }, perUser, aborted: false };`

    Security discipline (applies throughout):
    - Zero the userDEK in the `finally` block for each user.
    - Never log the password, the CSKEK, or the plaintext credential values.
    - Log only structured metadata (userId, credentialId, hostId, err.message).

    Run tests, iterate until all ~13 pass. Commit as `feat(75-08): add substrate-credential-migration module with H0 passphrase canary`.
  </action>
  <verify>
    <automated>npx vitest run src/backend/utils/substrate-credential-migration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^export async function migrateSubstrateCredentials" src/backend/utils/substrate-credential-migration.ts` returns 1
    - `grep -c "deriveDekForMigration" src/backend/utils/substrate-credential-migration.ts` returns 1
    - `grep -c "DatabaseSaveTrigger.forceSave" src/backend/utils/substrate-credential-migration.ts` returns 1
    - `grep -c "userDEK.fill(0)" src/backend/utils/substrate-credential-migration.ts` returns >= 1 (zeroing discipline)
    - `grep -c "isNull(hosts.credentialId)" src/backend/utils/substrate-credential-migration.ts` returns 1 (pre-check query)
    - `grep -c "substrate_migration_precheck_aborted\|substrate_migration_host_failed\|substrate_migration_user_failed\|substrate_migration_db_save_failed" src/backend/utils/substrate-credential-migration.ts` returns 4 (all four operation tags present)
    - **FIELDNAME CANARIES (LOAD-BEARING):**
      - `grep -c '"keyPassword"' src/backend/utils/substrate-credential-migration.ts` returns 1 (camelCase — DECRYPT side, matches production line 67)
      - `grep -c '"key_password"' src/backend/utils/substrate-credential-migration.ts` returns 1 (snake_case — CSKEK ENCRYPT side, matches production line 94)
      - If either count is 0 or 2+, the executor has either (a) forgotten the passphrase field entirely, or (b) "fixed" the inconsistency — both are BREAKING. Do not merge until both counts are exactly 1.
    - Zero occurrences of the string `console.log` or plaintext password/key patterns in log call arguments: manual read of the test suite must confirm SEC1/SEC2 pass
    - `grep -cE "^  it\(|^    it\(" src/backend/utils/substrate-credential-migration.test.ts` returns >= 12 (H0 + H1-H3 + PC1-PC2 + F1-F2 + S1-S2 + P1 + SEC1-SEC2 = 13; allow >= 12 to accommodate reasonable consolidation, but H0 MUST be present)
    - `grep -c "H0\|passphrase.*canary\|passphrase-protected" src/backend/utils/substrate-credential-migration.test.ts` returns >= 1 (H0 test is discoverable by name or by descriptive string)
    - `npx vitest run src/backend/utils/substrate-credential-migration.test.ts` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Migration module is a self-contained, fully-tested unit. D-14 atomicity (per-host update as a single drizzle statement) is verified. D-19 round-trip (encrypt with CSKEK → decrypt with CSKEK → matches original plaintext) is verified in tests H1-H3. **H0 passphrase-canary proves the entire fieldName chain end-to-end** — any future drift on either the user-DEK-DECRYPT side (`"keyPassword"`) or the CSKEK-ENCRYPT side (`"key_password"`) will be caught by CI before it silently corrupts production data.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create scripts/migrate-substrate-credentials.ts CLI entrypoint</name>
  <read_first>
    - src/backend/utils/substrate-credential-migration.ts (created in Task 2 — the migrateSubstrateCredentials function)
    - Look for existing scripts in the tree: `ls /home/ubuntu/skynet-tabitha/scripts/ 2>/dev/null` — if the directory exists and has other tsx scripts, mirror their invocation pattern (shebang, DB initialization, exit codes)
    - If no scripts/ directory exists, create it. The operator will run this from inside the container (`docker exec ... npx tsx scripts/migrate-substrate-credentials.ts < input.json`).
  </read_first>
  <action>
    Create `scripts/migrate-substrate-credentials.ts` as an executable tsx script.

    Header (JSDoc, ~30 lines): document usage (`npx tsx scripts/migrate-substrate-credentials.ts < input.json`), input format (JSON array of `{userId, password}`), output format (MigrationResult JSON to stdout), SECURITY notes (stdin contains plaintext passwords — never commit captures, use chmod-600 files, do not paste interactively), exit codes (0=completed with possible per-user failures in the JSON; 1=pre-check aborted; 2=fatal error), D-15 reminder to retire the script after both live instances have been migrated.

    Body:

    1. `async function main(): Promise<number>` — returns the exit code.
    2. Read all of stdin into a buffer using a `for await (const chunk of process.stdin)` loop; concat to a UTF-8 string; trim.
    3. If empty: `process.stderr.write("Error: no input received on stdin\n"); return 2;`
    4. `JSON.parse` the input with a try/catch that writes a clear error to stderr and returns 2 on failure.
    5. Validate the parsed value is an array; validate each entry has string `userId` and string `password`. On any validation failure, stderr + return 2.
    6. Import the migration module lazily (dynamic `import("../src/backend/utils/substrate-credential-migration.js")`) AFTER input validation, so bad stdin does not fire up the DB. Verify the relative path from `scripts/` to `src/backend/utils/` at implementation time — should be `../src/backend/utils/substrate-credential-migration.js`.
    7. Call `migrateSubstrateCredentials(input)`.
    8. Zero out `entry.password` on each input entry as a belt-and-suspenders defense (the migration module already zeroed the derived DEKs).
    9. Write `JSON.stringify(result, null, 2) + "\n"` to `process.stdout` (never stderr; never `console.log`).
    10. Return `1` if `result.aborted`, else `0`.

    Bottom: `main().then((code) => process.exit(code)).catch((err) => { process.stderr.write(\`Fatal error: ${err.message}\n\`); process.exit(2); });`

    DB initialization: inspect `src/backend/database/db/index.ts` — if `db` is a top-level singleton that self-initializes on import, no bootstrap needed. If explicit `initializeDatabaseAsync()` is required, add that before calling `migrateSubstrateCredentials`. Document the choice in the summary.

    NO console.log ANYWHERE — everything goes through process.stdout.write / process.stderr.write. NO logging of the input variable or its passwords ANYWHERE.

    No test for the CLI script itself — it is a thin shim. The migration module tests (Task 2) cover the semantic behavior; the CLI is exercised operationally by Ashley.

    Commit as `feat(75-08): add operator CLI entrypoint scripts/migrate-substrate-credentials.ts`.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/migrate-substrate-credentials.ts` exists
    - `grep -c "migrateSubstrateCredentials" scripts/migrate-substrate-credentials.ts` returns 1
    - `grep -c "process.stdin" scripts/migrate-substrate-credentials.ts` returns >= 1 (reads from stdin)
    - `grep -c "process.stdout.write" scripts/migrate-substrate-credentials.ts` returns >= 1 (writes JSON result to stdout)
    - `grep -c "console.log\|console.info" scripts/migrate-substrate-credentials.ts` returns 0 (all output goes through process.stdout or process.stderr; no accidental console.log leaks)
    - Manual inspection: input.password never appears in a process.stdout.write / stderr.write / log call (the script echoes ONLY result JSON to stdout)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Operator has a runnable CLI: `docker exec -i <container> npx tsx scripts/migrate-substrate-credentials.ts < ~/known-users.json`. Output JSON summarizes what happened per user. Non-zero exit codes signal abort or fatal errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator stdin → CLI script | plaintext passwords enter the process here. MUST be consumed once, used to derive DEKs, and then dropped. |
| CLI → migration module | script passes the input array to the module. Module returns a summary — no credentials in the return. |
| migration module → user-crypto.ts | module calls the new sessionless-derive method, gets back a DEK Buffer, uses it, zeroes it in `finally`. |
| migration module → DB (sshCredentials.system_* columns) | module writes CSKEK-wrapped ciphertext. NEVER writes plaintext. |
| module logs → log pipeline | ONLY structured metadata (userId, credentialId, hostId, err.message). Never plaintext, never CSKEK, never DEK. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-08-01 | Information disclosure | Migration script echoes stdin to stdout (would land in operator's terminal history or logs) | mitigate | Script explicitly reads stdin, parses, then writes ONLY the result JSON. Test-suite equivalent (SEC1) asserts no plaintext appears in log calls. Manual acceptance-criteria check: grep for accidental console.log or stdout.write of `input` variable. |
| T-75-08-02 | Information disclosure | DEK derived during migration persists in memory after use | mitigate | Module zeroes `userDEK.fill(0)` in the `finally` block per user. CLI additionally zeroes the input array's `password` fields as belt-and-suspenders. |
| T-75-08-03 | Information disclosure | An error thrown deep in FieldCrypto could include plaintext or DEK bytes in its stack | mitigate | All catch blocks log `err.message` only, never `err.stack`. Matches the discipline established across the phase (75-02, 75-03, 75-04). |
| T-75-08-04 | Tampering | Migration writes to sshCredentials.system_* columns; a bug in field-name mapping would silently corrupt credentials | mitigate | Field-name convention verified against the existing analog (credential-system-encryption-migration.ts): DECRYPT side uses `"password"`, `"key"`, `"keyPassword"` (camelCase per line 67); CSKEK ENCRYPT side uses `"password"`, `"key"`, `"key_password"` (snake_case per line 94). New module preserves this convention verbatim. **Test H0 is the load-bearing round-trip that catches ANY future drift on either side** — a passphrase-protected key fixture is encrypted with user DEK using `"keyPassword"`, migrated, decrypted with CSKEK using `"key_password"`, and asserted equal to the original plaintext. Two acceptance-criteria greps additionally lock the two fieldName strings to exactly one occurrence each in the migration source. |
| T-75-08-05 | Elevation of privilege | Migration script running inside the container has DB write access; a compromised script could exfiltrate all credentials | accept | Operator-run one-shot script per D-12. Same trust boundary as `docker exec` shell access — anyone who can run the script can already read the DB directly. Not a new attack surface. |
| T-75-08-06 | Denial-of-service | Migration script crashes mid-run leaving DB in a partial state | mitigate | D-14 atomicity: each per-credential update is a single drizzle UPDATE — either the row is updated cleanly or unchanged. Partial state at the RUN level means "some users migrated, others not" — safe to re-run for the remainder (module is idempotent: re-migrating an already-migrated cred re-encrypts to a new ciphertext but the plaintext round-trips correctly). |
| T-75-08-07 | Repudiation | Pre-check abort could be missed if operator ignores exit code | mitigate | Exit code 1 for aborted; result JSON has `aborted: true`; log line at operation `substrate_migration_precheck_aborted` includes the offending host IDs. Three signals for the operator to notice. |
| T-75-08-08 | Tampering | An input entry `{userId: "u1", password: "..."}` where u1 doesn't own any substrate credentials silently no-ops — could mask a typo where the operator meant a different user | accept | The perUser result entry will show `migrated: 0, failed: 0, skipped: 0, hosts: []` for that user — visible in the JSON output. Operator can cross-check against known substrate-host counts. |
| T-75-08-09 | Denial-of-service | forceSave could fail (disk full, permission error) and leave migration state in-memory only | mitigate | forceSave failure is logged at operation `substrate_migration_db_save_failed` and does NOT throw the migration — the operator sees the log line and can re-run. In-memory state is lost at next container restart, at which point the migration must be re-run. Acceptable — this is a one-shot script and operator can re-run. |

**Load-bearing security invariants (from RESEARCH.md § threat model surface):**
- "Migration script credential-exposure surface" — input via stdin only, never echoed. Enforced by CLI script structure + acceptance-criteria grep.
- "Migration script stdin (containing plaintext passwords) must not be echoed to stdout/logs" — enforced by module and CLI never touching the input array's password field for output/log purposes.
- "key material zeroed after use where possible" — `userDEK.fill(0)` in the `finally` block for each user; CLI zeroes input passwords as belt-and-suspenders.
- "fieldName parity between production write path and migration read path" — enforced by Test H0 (round-trip) + two grep gates that lock `"keyPassword"` and `"key_password"` to exactly one occurrence each.
</threat_model>

<verification>
- Task 1: 5 UserCrypto tests green; new method exists; userSessions never populated by the new method
- Task 2: ~13 migration tests green including H0 passphrase-canary; D-19 round-trip verified (encrypt with CSKEK → decrypt with CSKEK → matches original) for BOTH password AND passphrase fields
- Task 3: CLI script type-checks; grep confirms no console.log / no input echo
- `npx tsc --noEmit` clean across the whole tree
- Manual security review: no plaintext / no CSKEK / no DEK ever flows to a log call across the migration path
- FieldName grep-gates return exactly one occurrence each for `"keyPassword"` and `"key_password"` in the migration source
</verification>

<success_criteria>
- UserCrypto has `deriveDekForMigration` public method with 5 passing tests; private-method reachability verified at implementation time
- Migration module ships as a self-contained unit with 12+ passing tests including H0 passphrase-canary and D-19 round-trip
- CLI script exists at `scripts/migrate-substrate-credentials.ts` with correct stdin/stdout discipline
- Pre-check for inline-credential substrate hosts is in place and aborts loudly
- DEK zeroing discipline in place
- No plaintext / no CSKEK / no DEK ever appears in log call arguments (SEC1/SEC2 tests)
- FieldName asymmetry preserved verbatim (`"keyPassword"` camelCase on DECRYPT, `"key_password"` snake_case on ENCRYPT — matches production analog exactly)
- All acceptance criteria grep counts satisfied
</success_criteria>

<output>
Create `.planning/phases/75-server-side-substrate-bootstrap-startup-driven-install-pass-/75-08-SUMMARY.md` when done.

Critical items:
1. Document the exact form of the `deriveDekForMigration` method added and any deviations from the plan's sketched shape.
2. **Confirm H0 test is present and asserts the full fieldName chain end-to-end (user-DEK encrypt with `"keyPassword"` → migration decrypt with `"keyPassword"` → migration re-encrypt with CSKEK as `"key_password"` → CSKEK decrypt with `"key_password"` recovers original passphrase).** Report the exact plaintext-string used in the fixture (must be non-trivial — e.g., "test-passphrase-with-symbols-!@#" — to catch encoding bugs).
3. Confirm the two fieldName grep-gates: `"keyPassword"` count === 1 and `"key_password"` count === 1 in the migration source.
4. Note whether the pre-check found any inline-credential substrate hosts on the developer's local fixture DB (should be 0 for a fresh dev DB; production check happens when Ashley runs the migration).
5. Document the DB initialization pattern used by the CLI (top-level import vs. explicit initialize).
6. Note the scripts/ directory creation if it did not previously exist.
7. Include a runbook snippet for Ashley: exact `docker exec` invocation and expected stdout shape.
</output>
</content>
</invoke>