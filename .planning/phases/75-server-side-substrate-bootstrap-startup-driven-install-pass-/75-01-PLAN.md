---
phase: 75-server-side-substrate-bootstrap-startup-driven-install-pass-
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/backend/distributor/bundled-reader.ts
  - src/backend/distributor/bundled-reader.test.ts
  - src/backend/fleet-status/ssh-poll-orchestrator.ts
autonomous: true
requirements: []
decisions_addressed:
  - "D-04 (prerequisite refactor — extracting the reader before the old orchestrator hook is removed in 75-07 means both the legacy hook and the new server-context orchestrator import from the same module during the transition wave)"

must_haves:
  truths:
    - "The bundled-reader function is importable by any module in src/backend/distributor/ without going through src/backend/fleet-status/ssh-poll-orchestrator.ts"
    - "The existing legacy sweep hook at ssh-poll-orchestrator.ts:2117-2124 continues to call the same never-throw file-read behavior (fs read + stat, returns null on any failure) with no visible change in test output"
    - "The new module has its own unit test proving: returns { bytes, mode } on success; returns null on ENOENT; returns null on EACCES"
  artifacts:
    - path: "src/backend/distributor/bundled-reader.ts"
      provides: "Exported bundledReaderFromDisk function — pure fs adapter for SweepDeps.readBundledBytes"
      exports: ["bundledReaderFromDisk"]
    - path: "src/backend/distributor/bundled-reader.test.ts"
      provides: "Unit tests for the pure fs adapter (success, missing-file, permission-error)"
  key_links:
    - from: "src/backend/fleet-status/ssh-poll-orchestrator.ts"
      to: "src/backend/distributor/bundled-reader.ts"
      via: "import { bundledReaderFromDisk }"
      pattern: 'import.*bundledReaderFromDisk.*from.*"\\.\\./distributor/bundled-reader\\.js"'
---

<objective>
Extract the module-scoped `bundledReaderFromDisk` const currently defined inside `src/backend/fleet-status/ssh-poll-orchestrator.ts` (lines 2045-2057) into a standalone reusable module at `src/backend/distributor/bundled-reader.ts`. Both the legacy sweep hook (removed in 75-07) and the new `server-substrate-orchestrator.ts` (created in 75-02) need to consume the same file-read shape; putting it in `distributor/` — where the other sweep-plumbing modules live (`catalog.ts`, `run-sweep.ts`, `sweep-logic.ts`, `run-bootstrap.ts`) — is the natural home.

Purpose: Wave 0 refactor. Unblocks 75-02 (server-substrate orchestrator's file-read path) and simplifies 75-07 (removing the sweep hook doesn't drag the reader with it). Resolves RESEARCH open question A3 (extraction is the recommended shape).

Output: New pure-fs adapter module + its unit test + one-line import swap inside ssh-poll-orchestrator.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-server-side-substrate-bootstrap-startup-driven-install-pass-/75-CONTEXT.md
@.planning/phases/75-server-side-substrate-bootstrap-startup-driven-install-pass-/75-RESEARCH.md
@.planning/phases/75-server-side-substrate-bootstrap-startup-driven-install-pass-/75-PATTERNS.md
@src/backend/fleet-status/ssh-poll-orchestrator.ts
@src/backend/distributor/run-sweep.ts
@src/backend/distributor/run-bootstrap.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create bundled-reader.ts + failing unit test, then move the implementation</name>
  <read_first>
    - src/backend/fleet-status/ssh-poll-orchestrator.ts (lines 2037-2057 — the source-of-truth implementation, including the leading comment block that explains the never-throw contract)
    - src/backend/distributor/run-sweep.ts (lines 1-40 area — see `SweepDeps.readBundledBytes` type expected by the composer, so the new export's signature matches)
    - src/backend/distributor/run-bootstrap.test.ts (lines 1-70 — the vi.mock("../utils/logger.js") + makeChannel/exec pattern used across distributor tests; the new bundled-reader.test.ts follows the same import + mock discipline)
  </read_first>
  <behavior>
    - Test 1 (success): reader called with an existing readable file resolves to `{ bytes: Buffer, mode: number }` where bytes equals the file contents and mode equals the stat.mode
    - Test 2 (missing file / ENOENT): reader called with a non-existent path resolves to `null` (never throws)
    - Test 3 (permission error / EACCES): reader called with an unreadable path resolves to `null` (never throws)
    - Test 4 (never-throw contract): reader wrapped in `await expect(reader(path)).resolves.not.toThrow()` for all three above paths
  </behavior>
  <behavior>
    Test file (`src/backend/distributor/bundled-reader.test.ts`) — 4 tests:
    - Uses `beforeEach(async () => { ... mkdtemp })` + `afterEach(async () => { rm(tmpDir, { recursive: true, force: true }) })` to get a scratch dir
    - Test 1: writeFile with `{ mode: 0o644 }`, chmod to 0o600 → call `bundledReaderFromDisk(path)` → assert `result?.bytes.equals(Buffer.from("hello"))` and `(result?.mode & 0o777) === 0o600`
    - Test 2: `bundledReaderFromDisk("/nonexistent/definitely-not-here")` → `expect(result).toBeNull()`
    - Test 3: writeFile then chmod 0o000 → assert null (skip on CI where process runs as root — use `if (process.getuid && process.getuid() === 0) return;` at test top)
    - Test 4: `await expect(bundledReaderFromDisk("/nope")).resolves.not.toThrow()` — belt-and-suspenders for the never-throw contract
  </behavior>
  <action>
    RED phase: Create `src/backend/distributor/bundled-reader.test.ts` first with the 4 tests above. Import `bundledReaderFromDisk` from `./bundled-reader.js` (module does not yet exist — test file must fail to import). Run `npx vitest run src/backend/distributor/bundled-reader.test.ts` and confirm it fails with "Cannot find module" or equivalent. Commit as `test(75-01): add failing bundled-reader tests`.

    GREEN phase: Create `src/backend/distributor/bundled-reader.ts` with the following exact shape (copied verbatim from ssh-poll-orchestrator.ts:2045-2057, plus the doc comment from lines 2037-2043):

    ```
    imports: readFile, stat from "node:fs/promises"
    export const bundledReaderFromDisk: (bundledPath: string) => Promise<{ bytes: Buffer; mode: number } | null>
    ```

    Implementation body: `Promise.all([readFile(bundledPath), stat(bundledPath)])` inside try/catch, catch returns null. This is a byte-for-byte lift-and-shift of lines 2045-2057. Include a top-of-file comment block: "Pure fs adapter for SweepDeps.readBundledBytes. Never throws — returns null on any fs failure. Extracted from ssh-poll-orchestrator.ts in Phase 75-01 so both the legacy sweep hook (removed in 75-07) and the new server-substrate-orchestrator (75-02) can share one reader implementation."

    Run `npx vitest run src/backend/distributor/bundled-reader.test.ts` and confirm all 4 tests pass. Commit as `feat(75-01): extract bundled-reader.ts pure fs adapter`.
  </action>
  <verify>
    <automated>npx vitest run src/backend/distributor/bundled-reader.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/backend/distributor/bundled-reader.ts` exists
    - File exports `bundledReaderFromDisk` as a named export: `grep -c "^export const bundledReaderFromDisk" src/backend/distributor/bundled-reader.ts` returns 1
    - File `src/backend/distributor/bundled-reader.test.ts` exists with at minimum 3 `it(` blocks: `grep -c "^  it(" src/backend/distributor/bundled-reader.test.ts` returns >= 3
    - `npx vitest run src/backend/distributor/bundled-reader.test.ts` exits 0
    - The extracted function's body is byte-equivalent to ssh-poll-orchestrator.ts:2048-2056 (verify by reading both)
  </acceptance_criteria>
  <done>
    Bundled-reader is a self-contained module with green tests. Not yet consumed elsewhere — that swap happens in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewire ssh-poll-orchestrator.ts to import the extracted reader</name>
  <read_first>
    - src/backend/fleet-status/ssh-poll-orchestrator.ts (lines 1-15 — existing import block, especially the `import { runSweepForHost } from "../distributor/run-sweep.js"` at line 60; the new import follows the exact same relative-path shape)
    - src/backend/fleet-status/ssh-poll-orchestrator.ts (lines 2037-2057 — the block to delete)
    - src/backend/fleet-status/ssh-poll-orchestrator.ts (line 2123 — the call site `readBundledBytes: bundledReaderFromDisk` that must still resolve to the imported symbol after the deletion)
    - src/backend/distributor/bundled-reader.ts (the module created in Task 1)
  </read_first>
  <action>
    Modify `src/backend/fleet-status/ssh-poll-orchestrator.ts`:

    1. Add import near the top (grouped with the existing `import { runSweepForHost } from "../distributor/run-sweep.js"` at line 60): `import { bundledReaderFromDisk } from "../distributor/bundled-reader.js";`

    2. Delete lines 2037-2057 (the `bundledReaderFromDisk` local const declaration and its 8-line doc comment). Keep everything above (2036 and earlier) and everything below (2058 onward) intact.

    3. Also remove the `import { readFile, stat } from "node:fs/promises"` at the top of the file IF AND ONLY IF grep confirms `readFile`/`stat` are not referenced anywhere else in the file. Run `grep -n "readFile\|stat(" src/backend/fleet-status/ssh-poll-orchestrator.ts` after deletion — if the only hits were inside the removed block, remove the import; otherwise leave it.

    4. Verify line 2123 (`readBundledBytes: bundledReaderFromDisk`) still resolves — the symbol now comes from the import instead of the local const.

    Do NOT modify the sweep hook block at lines 2087-2152 — that removal belongs to 75-07 (which runs in a later wave after the server-substrate-orchestrator is proven wired).
  </action>
  <verify>
    <automated>npx vitest run --related src/backend/fleet-status/ssh-poll-orchestrator.ts src/backend/distributor/bundled-reader.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'from "../distributor/bundled-reader.js"' src/backend/fleet-status/ssh-poll-orchestrator.ts` returns 1
    - `grep -c "const bundledReaderFromDisk" src/backend/fleet-status/ssh-poll-orchestrator.ts` returns 0 (local const is gone)
    - The string `readBundledBytes: bundledReaderFromDisk` still appears at exactly one call site in the file: `grep -c "readBundledBytes: bundledReaderFromDisk" src/backend/fleet-status/ssh-poll-orchestrator.ts` returns 1
    - `npx tsc --noEmit` exits 0 (no type errors introduced by the import swap)
    - Existing ssh-poll-orchestrator tests still pass: `npx vitest run src/backend/fleet-status/ssh-poll-orchestrator.test.ts` exits 0
  </acceptance_criteria>
  <done>
    Legacy sweep hook now consumes the shared bundled-reader module. Ready for 75-02 to import the same module from the new server-context orchestrator.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None crossed. This plan is a pure code-motion refactor with no I/O surface changes, no auth changes, no crypto changes. The extracted function reads bundled substrate bytes from the container filesystem — those bytes are already baked into the container image at build time and are trusted by construction (container image is our own artifact).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-01-01 | Tampering | `bundled-reader.ts` reads from filesystem paths passed by the caller | accept | The path passed to `bundledReaderFromDisk` comes from `FLEET_SUBSTRATE_CATALOG` entries (baked into the container image, not user input). No path-traversal surface introduced. The `readFile` call is direct — no shell, no glob, no interpolation. |
| T-75-01-02 | Denial-of-service | Large-file read blocks event loop briefly | accept | Bundled substrate files are small (kilobyte-scale scripts + configs), and this is a pre-existing behavior — refactor changes zero behavior here. |

**No load-bearing security threats for this plan.** The heavy security threats named in the phase-level threat model (CSKEK exposure, migration credential handling, fire-and-forget error surfacing) all live in later plans (75-04, 75-08, 75-06). This plan's threat surface is a code-motion refactor.
</threat_model>

<verification>
- Task 1 test file green
- Task 2 import swap type-checks and existing ssh-poll-orchestrator tests still pass
- Byte-equivalence of the extracted function body confirmed by reading both files
- Zero behavior change verified by running the full existing ssh-poll-orchestrator test suite
</verification>

<success_criteria>
- New file `src/backend/distributor/bundled-reader.ts` exists with a single exported function matching the source-of-truth implementation
- New file `src/backend/distributor/bundled-reader.test.ts` exists with >=3 test cases covering success/missing/permission paths
- `src/backend/fleet-status/ssh-poll-orchestrator.ts` imports the extracted function instead of declaring it inline
- `npx vitest run --related src/backend/fleet-status/ssh-poll-orchestrator.ts src/backend/distributor/bundled-reader.ts` exits 0
- `npx tsc --noEmit` exits 0
</success_criteria>

<output>
Create `.planning/phases/75-server-side-substrate-bootstrap-startup-driven-install-pass-/75-01-SUMMARY.md` when done, following the summary template. Include: files created, files modified, test count added, any deviations from the plan.
</output>
