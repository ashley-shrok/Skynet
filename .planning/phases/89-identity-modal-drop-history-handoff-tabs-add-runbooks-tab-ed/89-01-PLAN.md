---
phase: 89-identity-modal-drop-history-handoff-tabs-add-runbooks-tab-ed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - substrate/skills/id/SKILL.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "substrate/skills/id/SKILL.md § Runbooks Storage subsection reads that the main markdown file MUST be named `runbook.md` (per D-19), NOT `<slug>.md`"
    - "The § 3 Loading an existing identity on-wake enumeration step (currently L423-430) no longer references `<slug>.md` in its missing-file clause; the runbook-subfolder enumeration key stays folder-name-based (unchanged per D-19)"
    - "No 'grandfather rule' migration note is added for existing `<slug>.md` runbooks (per D-20: convention is going-forward only; the on-disk rename is Alice's manual post-close pass, out of this phase's scope per 89-CONTEXT.md <domain> § Out of scope)"
    - "The distributor sweep will pick this up on next fleet-substrate propagation with no restart needed — this plan changes text only, no code, no schema, no config"
  artifacts:
    - path: "substrate/skills/id/SKILL.md"
      provides: "Distributed id-skill body reflecting the `runbook.md` sentinel-file naming convention"
      contains: "runbook.md"
  key_links:
    - from: "substrate/skills/id/SKILL.md § Runbooks / Storage"
      to: "Wave-2 backend (Plan 89-02) `find <runbookRoot> -type f -printf '%P\\n'` enumerator"
      via: "shared contract that the runbook folder's main file is `runbook.md` going forward; enumerator is naming-agnostic so it works for both grandfathered `<slug>.md` and new `runbook.md`"
      pattern: "runbook\\.md"
    - from: "substrate/skills/id/SKILL.md § 3 Loading an existing identity — on-wake runbook enumeration"
      to: "existing on-wake load flow — 'enumerate the runbook subfolders directly under `~/.claude/roles/<role>/runbooks/`'"
      via: "wording adjustment only — the flow still enumerates folder names, never reads the file"
      pattern: "runbook\\.md"
---

<objective>
Wave-1 contract-consistency edit. Update `substrate/skills/id/SKILL.md` § Runbooks storage + on-wake missing-file wording so that the id-skill body says the sentinel main file is `runbook.md` (mirroring `SKILL.md` for skills) instead of `<slug>.md`.

Purpose: Wave-2 through Wave-6 build a runbook-editor modal that reads `~/.claude/roles/<role>/runbooks/<slug>/<file>` via a name-agnostic `find -type f` enumerator. Whether the main file is called `runbook.md` (new) or `<slug>.md` (grandfathered) does not affect the modal — it enumerates every file. But the id-skill body IS the on-disk-shape contract, and per D-23 this wave ships FIRST so that no intermediate commit lands with contract drift (substrate saying `runbook.md` while backend code hasn't shipped, or vice versa; the wording change is the cheap end of the pair). Per D-20 the convention is going-forward only — the on-disk rename of the 4 existing box-maintainer runbooks (`avatar-flow/avatar-flow.md` → `avatar-flow/runbook.md`, etc.) is Alice's manual post-close pass, explicitly out of scope for this phase (see 89-CONTEXT.md § Out of scope).

Output:
- `substrate/skills/id/SKILL.md` § Runbooks / Storage subsection (currently L1288-1296): the "MUST be named `<slug>.md`" phrasing at L1295-1296 becomes "MUST be named `runbook.md`". The example storage path at L1290 (`~/.claude/roles/<role>/runbooks/<runbook-slug>/<runbook-slug>.md`) becomes `~/.claude/roles/<role>/runbooks/<runbook-slug>/runbook.md`.
- `substrate/skills/id/SKILL.md` § 3 Loading an existing identity (currently L423-430) — the L429 "A subfolder missing its `<slug>.md`" phrasing becomes "A subfolder missing its `runbook.md`". Enumeration wording (L423-428) stays folder-name-based (unchanged — the flow reads folder names, not the sentinel).
- No other subsections touched. No "grandfather rule" added.
- No source-code edits, no schema edits, no config edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/89-identity-modal-drop-history-handoff-tabs-add-runbooks-tab-ed/89-CONTEXT.md
@.planning/shapes/shape-identity-modal-tab-restructure.md
@substrate/skills/id/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update substrate/skills/id/SKILL.md — § Runbooks Storage + § 3 on-wake wording to `runbook.md` sentinel</name>
  <files>substrate/skills/id/SKILL.md</files>
  <read_first>
    - substrate/skills/id/SKILL.md L1280-1341 (the full § Runbooks section — Storage / Awareness on wake / When to invoke / Editing rules / Distinction from reference files). The Storage subsection you edit lives inside this block. Do NOT touch § When to invoke / Editing rules / Distinction from reference files at all.
    - substrate/skills/id/SKILL.md L418-431 (§ 3 Loading an existing identity — the on-wake enumeration step for runbooks). L429 has the second slug-token reference to update. Enumeration key stays folder-name-based per D-19 — DO NOT rewrite the enumeration step, only the missing-file clause.
    - .planning/phases/89-identity-modal-drop-history-handoff-tabs-add-runbooks-tab-ed/89-CONTEXT.md § Implementation Decisions D-19 (the storage-subsection change) + D-20 (going-forward only — NO grandfather rule) + D-23 (this ships as its own wave BEFORE backend/frontend so no intermediate commit has contract drift).
    - .planning/shapes/shape-identity-modal-tab-restructure.md § Shape #5 (id-skill body update — describes the two spots that shift wording) + § Philosophy "Runbook file naming standardizes on `runbook.md`, mirroring skills' `SKILL.md`" (the framing that motivates the change).
  </read_first>
  <action>
    Two coordinated wording edits in one file. Text-only — no code, no schema, no reformatting of adjacent lines.

    ─────────────────────────────────────────────────────────────
    EDIT A — § Runbooks / Storage subsection (currently L1288-1296)
    ─────────────────────────────────────────────────────────────

    (A.1) At L1290, the storage-path example currently reads the path `~/.claude/roles/<role>/runbooks/<runbook-slug>/<runbook-slug>.md`. Change the trailing `<runbook-slug>.md` segment to `runbook.md` so the line reads `~/.claude/roles/<role>/runbooks/<runbook-slug>/runbook.md`.

    (A.2) At L1295-1296, the Naming clause currently ends with "The main markdown MUST be named `<slug>.md` inside its subfolder — companion files can be named anything." Change the sentinel requirement so the clause reads:

      **Naming**: slug is kebab-case (same rule as bounties). The main markdown MUST be
      named `runbook.md` inside its subfolder — companion files can be named anything.
      This mirrors the `SKILL.md` sentinel-file convention for skills: the folder names
      the thing, and a fixed sentinel inside makes every path expression predictable.

    Preserve the two-line hanging-indent shape and the trailing period. The second sentence ("This mirrors the `SKILL.md` sentinel-file convention…") is a small addendum that captures the philosophy per shape file § Philosophy ("Runbook file naming standardizes on `runbook.md`, mirroring skills' `SKILL.md`"); it tells a future reader WHY the convention exists.

    ─────────────────────────────────────────────────────────────
    EDIT B — § 3 Loading an existing identity, on-wake enumeration step (currently L423-430)
    ─────────────────────────────────────────────────────────────

    At L429 the missing-file clause currently reads "A subfolder missing its `<slug>.md` still enumerates by folder name; the missing-file error surfaces at read-time when it's actually invoked." Change the sentinel-file name to `runbook.md` so the clause reads:

      A subfolder missing its `runbook.md` still enumerates by folder name; the missing-file
      error surfaces at read-time when it's actually invoked.

    Do NOT touch L423-428 (the "Then enumerate the runbook subfolders…" paragraph) — enumeration keys off folder names and that is unchanged per D-19. This one line is the entire edit in this subsection.

    ─────────────────────────────────────────────────────────────
    NON-EDITS — DO NOT CHANGE THESE
    ─────────────────────────────────────────────────────────────

    - Do NOT add any "grandfather rule for existing runbooks" note anywhere in the file. Per D-20 the convention is going-forward only; Alice's manual post-close pass renames the 4 existing box-maintainer runbooks (`avatar-flow/avatar-flow.md` → `avatar-flow/runbook.md`, etc.), so no grandfather branch needs to be codified.
    - Do NOT touch § When to invoke one (L1311-1320), § Editing rules (L1322-1333), § Distinction from reference files (L1335-1340). None of these reference the sentinel-file name.
    - Do NOT touch § Awareness on wake at L1298-1309 (the mtime staleness paragraph inside § Runbooks) — no sentinel-file reference to fix there.
    - Do NOT touch the L1027 short reference line about `runbooks/` (no sentinel-file name there).
    - Do NOT touch any other file. This plan is single-file text-only.
    - Do NOT reformat adjacent lines, adjust line-wrap widths, or "normalize" heading levels. The file is 1524 lines and prose is intentionally hand-wrapped.
    - Do NOT add a "Phase 89" marker anchor comment inside the substrate file (unlike Phase 88's src-file convention). The substrate is user-facing prose; the phase reference lives in the SUMMARY.md that the executor writes at plan end.

    ─────────────────────────────────────────────────────────────
    STRUCTURED LOG NOTE
    ─────────────────────────────────────────────────────────────

    This plan modifies prose only — no runtime code, so log-first diagnostics do not apply here (nothing to instrument). Backend logs come in Plan 89-02.
  </action>
  <acceptance_criteria>
    The wording is complete when every check below holds against the post-edit file:

    Grep source assertions on `substrate/skills/id/SKILL.md` (comment lines are irrelevant — it is Markdown, not a source file with `#` comments; grep counts document-body occurrences directly). All patterns below use regex character classes to avoid embedding literal angle brackets in shell (the character class `[^[:space:]/]` matches any non-space non-slash character — which safely represents the `<...>` placeholder tokens):

    - `grep -cE 'runbooks/[^[:space:]/]+/runbook\.md' substrate/skills/id/SKILL.md` returns 1 (the L1290 example path with the new sentinel — matches `runbooks/<runbook-slug>/runbook.md`).
    - `grep -cE '^\*\*Naming\*\*.*runbook\.md' substrate/skills/id/SKILL.md` returns 1 (the L1295 Naming clause line explicitly names `runbook.md`).
    - `grep -cE 'missing its .runbook\.md.' substrate/skills/id/SKILL.md` returns 1 (the L429 missing-file clause).
    - `grep -cE 'mirrors the .SKILL\.md. sentinel' substrate/skills/id/SKILL.md` returns 1 (the philosophy-anchor sentence added at the end of the Naming clause).
    - `grep -cE 'runbooks/[^[:space:]/]+/[^[:space:]/]*slug[^[:space:]/]*\.md' substrate/skills/id/SKILL.md` returns 0 (both pre-edit slug-placeholder-as-filename variants are gone — no `runbooks/<x>/<slug>.md`-style path expression left).
    - `grep -cE 'named .[^[:space:]/]*slug[^[:space:]/]*\.md.' substrate/skills/id/SKILL.md` returns 0 (the pre-edit Naming clause "named `<slug>.md`" phrasing is gone).
    - `grep -cE 'missing its .[^[:space:]/]*slug[^[:space:]/]*\.md.' substrate/skills/id/SKILL.md` returns 0 (the pre-edit missing-file clause with `<slug>.md` is gone).
    - `grep -ci grandfather substrate/skills/id/SKILL.md` returns 0 (no grandfather rule was added — per D-20 this convention is going-forward only).

    Structural preservation:
    - `wc -l < substrate/skills/id/SKILL.md` returns a line count between 1520 and 1530 inclusive (pre-edit is 1524; Edit A.2 adds one full-length sentence which may wrap onto a second line; Edit A.1 and Edit B are single-line replacements). If the count changes by more than 3, the executor over-edited or reformatted adjacent paragraphs — revert and try again.
    - `grep -c '^## Runbooks' substrate/skills/id/SKILL.md` returns 1 (top-level § Runbooks heading unchanged).
    - `grep -c '^### Storage' substrate/skills/id/SKILL.md` returns 1 (Storage subheading unchanged).

    Contract-consistency check (relative to Wave 2's backend enumerator):
    - The `runbook.md` sentinel-file mention is now the ONLY canonical name in § Runbooks Storage; Wave 2's backend uses a name-agnostic `find -type f` enumerator so grandfathered `<slug>.md` files still show up as tabs. This plan enforces the going-forward wording; grandfathered folders are not this plan's concern.

    Build-level (there is no TypeScript / build gate for substrate markdown files):
    - `head -1 substrate/skills/id/SKILL.md` returns the pre-existing top-of-file line (structure preserved).
  </acceptance_criteria>
  <verify>
    <automated>set -e; [ "$(grep -cE 'runbooks/[^[:space:]/]+/runbook\.md' substrate/skills/id/SKILL.md)" = "1" ] && [ "$(grep -cE '^\*\*Naming\*\*.*runbook\.md' substrate/skills/id/SKILL.md)" = "1" ] && [ "$(grep -cE 'missing its .runbook\.md.' substrate/skills/id/SKILL.md)" = "1" ] && [ "$(grep -cE 'mirrors the .SKILL\.md. sentinel' substrate/skills/id/SKILL.md)" = "1" ] && [ "$(grep -cE 'runbooks/[^[:space:]/]+/[^[:space:]/]*slug[^[:space:]/]*\.md' substrate/skills/id/SKILL.md)" = "0" ] && [ "$(grep -cE 'named .[^[:space:]/]*slug[^[:space:]/]*\.md.' substrate/skills/id/SKILL.md)" = "0" ] && [ "$(grep -cE 'missing its .[^[:space:]/]*slug[^[:space:]/]*\.md.' substrate/skills/id/SKILL.md)" = "0" ] && [ "$(grep -ci grandfather substrate/skills/id/SKILL.md)" = "0" ] && LC=$(wc -l < substrate/skills/id/SKILL.md) && [ "$LC" -ge "1520" ] && [ "$LC" -le "1530" ]</automated>
  </verify>
  <done>
    - § Runbooks / Storage: the example path at L1290 shifts from `<runbook-slug>.md` to `runbook.md`; the Naming clause at L1295-1296 shifts from `<slug>.md` to `runbook.md` and adds the "mirrors the `SKILL.md` sentinel-file convention…" sentence.
    - § 3 Loading an existing identity, missing-file clause at L429: shifts from `<slug>.md` to `runbook.md`. Enumeration paragraph L423-428 UNCHANGED (enumeration keys off folder names per D-19).
    - No other subsections touched. No grandfather rule added anywhere.
    - `wc -l` within 1520-1530 inclusive.
    - Grep gates listed in `<acceptance_criteria>` all pass.
    - No code / schema / config files modified in this task. Substrate-text-only plan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| substrate distributor → managed hosts | Substrate text is pushed to every managed box via the fleet distributor. No new capability here — the change is prose only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01-01 | Tampering | substrate/skills/id/SKILL.md wording | accept | Prose-only change to a distributed spec doc; no execution surface. Contract drift with backend (Plan 89-02) is mitigated by shipping this wave BEFORE backend (D-23) so no intermediate commit has substrate saying one thing while code does another. |
| T-89-01-02 | Repudiation | fleet-substrate propagation | accept | The substrate distributor is an existing trusted internal sweep; this plan does not alter propagation. Managed boxes pick the new wording up on next sweep with no restart. Grandfathered `<slug>.md` runbooks continue to enumerate correctly (backend is naming-agnostic per Wave 2 plan). |
</threat_model>

<verification>
- Grep gates in `<acceptance_criteria>` all pass.
- `wc -l substrate/skills/id/SKILL.md` within 1520-1530 inclusive.
- Manual read of § Runbooks / Storage confirms the wording flows naturally and the added "mirrors the `SKILL.md` sentinel-file convention…" sentence reads as prose, not as a bolted-on annotation.
- Manual read of § 3 Loading an existing identity / on-wake enumeration confirms only the missing-file clause shifted; the "enumerate the runbook subfolders directly under" paragraph is byte-identical.
</verification>

<success_criteria>
- Both edits landed; grep for the pre-edit slug-placeholder-as-filename patterns returns 0.
- No other edits sneaked in (grep for `grandfather` returns 0; heading counts unchanged; line count within 1520-1530).
- Substrate change is dormant — no runtime consumer depends on this wording; Wave 2 (backend) is the first plan that ships a code consumer that cares about this convention (indirectly, via the name-agnostic enumerator).
- Ready for Wave 2 to proceed.
</success_criteria>

<output>
Create `.planning/phases/89-identity-modal-drop-history-handoff-tabs-add-runbooks-tab-ed/89-01-SUMMARY.md` when done.
</output>
