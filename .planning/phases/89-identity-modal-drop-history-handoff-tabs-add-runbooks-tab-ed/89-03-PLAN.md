---
phase: 89-identity-modal-drop-history-handoff-tabs-add-runbooks-tab-ed
plan: 03
type: execute
wave: 3
depends_on:
  - 89-02
files_modified:
  - src/ui/api/runbooks-api.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Frontend API client `src/ui/api/runbooks-api.ts` exposes 7 typed helpers (listRunbooks, enumerateRunbookFiles, readRunbookFile, writeRunbookFile, createRunbookFile, deleteRunbookFile, deleteRunbook) mirroring the 7 Wave-2 backend endpoints"
    - "Byte-shape mirror of `src/ui/api/skills-api.ts` (238 lines): uses authApi + handleApiError pattern; adds a `roleName` parameter to every helper that takes `hostId` (the role dimension is threaded through every call — no coupling to any specific identity's name); exports typed error classes and result types"
    - "Two typed 409 error classes: `RunbookFileMtimeConflictError` (thrown by writeRunbookFile on 409 mtime-mismatch) + `RunbookFileAlreadyExistsError` (thrown by createRunbookFile on 409 file-exists) — INTENTIONAL duplication from skills-api.ts's error classes (matches the runtime-decoupled posture at skills-api.ts:8-11); do NOT import from skills-api.ts"
    - "Typed exports: `RunbookEntry = {name: string}`, `RunbookFileEntry = {path: string}`, `RunbookFileReadResult = {content: string, mtime: number, size: number, isText: boolean}`, `RunbookFileWriteInput`, `RunbookFileWriteResult = {mtime: number}` — mirror the skills-api.ts type surface"
    - "Every function that hits the backend catches errors via `handleApiError(error, '...')` and rethrows, EXCEPT the two 409-typed-error paths in writeRunbookFile and createRunbookFile which narrow on `response.status === 409` + shape-check before throwing the typed error"
    - "Scoped tsc --noEmit passes; no vitest file for this pure-API module (existing convention — skills-api.ts has no companion test file either; coverage is at the modal + backend level)"
  artifacts:
    - path: "src/ui/api/runbooks-api.ts"
      provides: "Frontend API surface for the 7 Wave-2 backend routes at /runbooks-editor/* — typed helpers + error classes + result types"
      contains: "listRunbooks"
      min_lines: 220
  key_links:
    - from: "src/ui/api/runbooks-api.ts function bodies"
      to: "Wave-2 backend routes at /runbooks-editor/* (Plan 89-02)"
      via: "authApi.get/post/put/delete against the exact paths landed in Wave 2; body/query shapes match Wave 2 wire spec"
      pattern: "/runbooks-editor"
    - from: "src/ui/api/runbooks-api.ts error classes"
      to: "Wave-4 RunbookEditorModal.tsx save + create handlers"
      via: "typed 409 branches drive reload-and-retry UX (mtime conflict) and prompt-error UX (file exists) — mirrors skills modal L251-274 shape"
      pattern: "RunbookFile.*Error"
---

<objective>
Wave-3 frontend API client. Create `src/ui/api/runbooks-api.ts` — a byte-shape mirror of `src/ui/api/skills-api.ts` adapted for the Wave-2 backend routes at `/runbooks-editor/*`. Threads `roleName` through every helper alongside `hostId`; exposes typed 409 error classes for the mtime-conflict + file-exists branches; exports the same result-type shape as skills-api so the Wave-4 modal (a byte-shape clone of SkillsEditorModal) can consume them without adaptation.

Purpose: exposes the wire the Wave-4 modal (`RunbookEditorModal.tsx`) calls. Ships with typed 409 error classes so the modal's save + create handlers can branch on stale-mtime and file-exists conditions with the same UX skills modal uses (window.confirm reload-and-lose-local-edits + prompt-style alert). Runtime-decoupled from skills-api.ts (D-01 clone-first-extract-later posture) — no cross-import, no shared 409 error class.

Output:
- `src/ui/api/runbooks-api.ts` — new file, ~240 lines (mirrors skills-api.ts's 238-line shape). Seven exported functions: listRunbooks, enumerateRunbookFiles, readRunbookFile, writeRunbookFile, createRunbookFile, deleteRunbookFile, deleteRunbook. Two exported error classes: RunbookFileMtimeConflictError, RunbookFileAlreadyExistsError. Five exported types: RunbookEntry, RunbookFileEntry, RunbookFileReadResult, RunbookFileWriteInput, RunbookFileWriteResult.
- `npx tsc --noEmit` passes.
- No vitest file for this module (matches skills-api.ts convention — coverage at modal + backend level; backend test file in Plan 89-02 already covers wire correctness).

Ship discipline: scoped tsc only; no full-suite test, no `git push`, no docker command.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/89-identity-modal-drop-history-handoff-tabs-add-runbooks-tab-ed/89-CONTEXT.md
@src/ui/api/skills-api.ts
@src/backend/database/routes/runbooks-editor.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/ui/api/runbooks-api.ts — 7 typed helpers + 2 error classes mirroring skills-api.ts, threaded through roleName dimension</name>
  <files>src/ui/api/runbooks-api.ts</files>
  <read_first>
    - src/ui/api/skills-api.ts — the ENTIRE 238-line file. Read all sections: L1-11 (import + file-header prose explaining runtime-decoupled 409-error-class duplication posture), L13-32 (types: SkillEntry / SkillFileEntry / SkillFileReadResult / SkillFileWriteInput / SkillFileWriteResult), L34-62 (typed 409 error classes: SkillFileMtimeConflictError + SkillFileAlreadyExistsError), L64-79 (listSkills GET), L81-100 (enumerateSkillFiles GET), L102-123 (readSkillFile POST), L125-161 (writeSkillFile PUT with 409-narrow branch), L163-197 (createSkillFile POST with 409-narrow branch), L199-218 (deleteSkillFile DELETE with body), L220-238 (deleteSkill DELETE).
    - src/backend/database/routes/runbooks-editor.ts (from Plan 89-02) — verify exact route paths and wire body/query shapes match the assumptions this file encodes. Especially: `GET /runbooks-editor/runbooks?hostId&role`, `GET /runbooks-editor/files?hostId&role&runbook`, `POST /runbooks-editor/read {hostId,role,runbook,path}`, `PUT /runbooks-editor/write {hostId,role,runbook,path,content,expectedMtime?}`, `POST /runbooks-editor/create {hostId,role,runbook,path}`, `DELETE /runbooks-editor/file {hostId,role,runbook,path}`, `DELETE /runbooks-editor/runbook {hostId,role,runbook}`.
    - src/ui/api/identities-api.ts L1-15 (Identity type — `role: string | null` at L9) — this is the source of `roleName` at the modal-open call site; the frontend threads `identity.role` into every runbooks-api call. Not a modification target here; just confirms the roleName provenance.
    - .planning/phases/89-identity-modal-drop-history-handoff-tabs-add-runbooks-tab-ed/89-CONTEXT.md § D-01 (clone-first-extract-later posture) + § code_context § Reusable Assets (skills-api.ts as byte-shape mirror target).
  </read_first>
  <action>
    Create ONE new file `src/ui/api/runbooks-api.ts`. Mirror `src/ui/api/skills-api.ts` byte-shape almost verbatim; the differences are the `roleName` addition to every function signature (and its threading into every request body/query) and the runbook-vs-skill terminology swap.

    ─────────────────────────────────────────────────────────────
    FILE HEADER
    ─────────────────────────────────────────────────────────────

    Open with the standard `import { authApi, handleApiError } from "@/main-axios";` line matching skills-api.ts:1.

    Follow with a Phase 89 / Plan 89-03 header docblock modeled on skills-api.ts L3-11:
    - "Phase 89 Plan 03: Frontend API helpers for the seven runbooks-editor endpoints landed in Wave 2 (`src/backend/database/routes/runbooks-editor.ts`). Mirrors `skills-api.ts` byte-shape (authApi + handleApiError pattern; typed 409 error classes for mtime-conflict + already-exists branches)."
    - "`RunbookFileMtimeConflictError` is DUPLICATED from `SkillFileMtimeConflictError` rather than imported — the two features share zero runtime concern (matches the L8-11 posture in skills-api.ts). Same posture as Phase 23 duplicating execWithTimeout / shellEscape (fifth intentional instance in the codebase; skills-api was the fourth)."

    ─────────────────────────────────────────────────────────────
    TYPES
    ─────────────────────────────────────────────────────────────

    Export the same five types as skills-api.ts L13-32, renamed:

      export type RunbookEntry = { name: string };

      export type RunbookFileEntry = { path: string };

      export type RunbookFileReadResult = {
        content: string;
        mtime: number;
        size: number;
        isText: boolean;
      };

      export type RunbookFileWriteInput = {
        hostId: number;
        role: string;
        runbook: string;
        path: string;
        content: string;
        expectedMtime?: number;
      };

      export type RunbookFileWriteResult = { mtime: number };

    ─────────────────────────────────────────────────────────────
    ERROR CLASSES
    ─────────────────────────────────────────────────────────────

    Export the two typed 409 error classes, duplicated from skills-api.ts L34-62 with the type name swap:

      export class RunbookFileMtimeConflictError extends Error {
        constructor(
          public readonly currentMtime: number,
          public readonly currentContent: string,
        ) {
          super("mtime mismatch");
          this.name = "RunbookFileMtimeConflictError";
        }
      }

      export class RunbookFileAlreadyExistsError extends Error {
        constructor() {
          super("file exists");
          this.name = "RunbookFileAlreadyExistsError";
        }
      }

    Precede each class with a JSDoc paraphrasing the skills-api.ts JSDoc at L34-41 and L52-56, adapted to say "Byte-shape mirror of RunbookFileMtimeConflictError… intentionally duplicated rather than imported so the two features stay runtime-decoupled." The class `.name` field MUST match the class identifier string literal so `err instanceof RunbookFileMtimeConflictError` narrowing works predictably in the modal.

    ─────────────────────────────────────────────────────────────
    FUNCTION 1 — listRunbooks(hostId, roleName)
    ─────────────────────────────────────────────────────────────

    Modeled on skills-api.ts listSkills L64-79:

      /**
       * GET /runbooks-editor/runbooks?hostId=<n>&role=<r>
       * Returns the list of runbooks for the given role on the given host
       * (`~/.claude/roles/<r>/runbooks/*`).
       * Returns [] when the runbooks folder doesn't exist (200, not 404 — per Plan 89-02
       * D-15 "missing runbooks folder within a valid role is empty-list-not-404";
       * D-15's 404 fires only when the role folder itself is absent).
       */
      export async function listRunbooks(
        hostId: number,
        roleName: string,
      ): Promise<RunbookEntry[]> {
        try {
          const response = await authApi.get("/runbooks-editor/runbooks", {
            params: { hostId, role: roleName },
          });
          return (response.data as { runbooks: RunbookEntry[] }).runbooks;
        } catch (error) {
          handleApiError(error, "list runbooks for role");
          throw error; // unreachable — handleApiError throws; satisfies TS return type
        }
      }

    ─────────────────────────────────────────────────────────────
    FUNCTION 2 — enumerateRunbookFiles(hostId, roleName, runbookName)
    ─────────────────────────────────────────────────────────────

    Modeled on skills-api.ts enumerateSkillFiles L81-100:

      export async function enumerateRunbookFiles(
        hostId: number,
        roleName: string,
        runbookName: string,
      ): Promise<RunbookFileEntry[]> {
        try {
          const response = await authApi.get("/runbooks-editor/files", {
            params: { hostId, role: roleName, runbook: runbookName },
          });
          return (response.data as { files: RunbookFileEntry[] }).files;
        } catch (error) {
          handleApiError(error, "list files in runbook");
          throw error;
        }
      }

    Precede with a JSDoc paraphrasing L81-86 — mention that paths are relative to the runbook root (e.g. `runbook.md`, `avatar-prompts/amelia.md`); returns [] when the runbook has zero files.

    ─────────────────────────────────────────────────────────────
    FUNCTION 3 — readRunbookFile(hostId, roleName, runbookName, path)
    ─────────────────────────────────────────────────────────────

    Modeled on skills-api.ts readSkillFile L102-123:

      export async function readRunbookFile(
        hostId: number,
        roleName: string,
        runbookName: string,
        path: string,
      ): Promise<RunbookFileReadResult> {
        try {
          const response = await authApi.post("/runbooks-editor/read", {
            hostId,
            role: roleName,
            runbook: runbookName,
            path,
          });
          return response.data as RunbookFileReadResult;
        } catch (error) {
          handleApiError(error, "read runbook file");
          throw error;
        }
      }

    ─────────────────────────────────────────────────────────────
    FUNCTION 4 — writeRunbookFile(input) with typed 409 mtime-conflict narrow
    ─────────────────────────────────────────────────────────────

    Modeled on skills-api.ts writeSkillFile L125-161:

      export async function writeRunbookFile(
        input: RunbookFileWriteInput,
      ): Promise<RunbookFileWriteResult> {
        try {
          const response = await authApi.put("/runbooks-editor/write", input);
          return response.data as RunbookFileWriteResult;
        } catch (error) {
          const err = error as {
            response?: {
              status?: number;
              data?: { error?: string; currentMtime?: number; currentContent?: string };
            };
          };
          // Only recognize the specific mtime-mismatch 409 shape. If the backend
          // ever adds a different 409 semantic (e.g. "concurrent write in progress")
          // we do NOT want to misinterpret it as a stale-mtime conflict — that
          // would trigger the wrong reload prompt at the user.
          if (
            err?.response?.status === 409 &&
            err.response.data?.error === "mtime mismatch"
          ) {
            throw new RunbookFileMtimeConflictError(
              err.response.data?.currentMtime ?? 0,
              err.response.data?.currentContent ?? "",
            );
          }
          handleApiError(error, "write runbook file");
          throw error;
        }
      }

    Note: the `input` shape carries `role` and `runbook` fields; PUT body is the input object verbatim so the backend receives `{hostId, role, runbook, path, content, expectedMtime}`.

    ─────────────────────────────────────────────────────────────
    FUNCTION 5 — createRunbookFile(hostId, roleName, runbookName, path) with typed 409 file-exists narrow
    ─────────────────────────────────────────────────────────────

    Modeled on skills-api.ts createSkillFile L163-197:

      export async function createRunbookFile(
        hostId: number,
        roleName: string,
        runbookName: string,
        path: string,
      ): Promise<{ path: string; mtime: number }> {
        try {
          const response = await authApi.post("/runbooks-editor/create", {
            hostId,
            role: roleName,
            runbook: runbookName,
            path,
          });
          return response.data as { path: string; mtime: number };
        } catch (error) {
          const err = error as {
            response?: { status?: number; data?: { error?: string } };
          };
          if (
            err?.response?.status === 409 &&
            err.response.data?.error === "file exists"
          ) {
            throw new RunbookFileAlreadyExistsError();
          }
          handleApiError(error, "create runbook file");
          throw error;
        }
      }

    ─────────────────────────────────────────────────────────────
    FUNCTION 6 — deleteRunbookFile(hostId, roleName, runbookName, path)
    ─────────────────────────────────────────────────────────────

    Modeled on skills-api.ts deleteSkillFile L199-218. Uses the DELETE-with-body pattern (`data` field on axios):

      export async function deleteRunbookFile(
        hostId: number,
        roleName: string,
        runbookName: string,
        path: string,
      ): Promise<void> {
        try {
          await authApi.delete("/runbooks-editor/file", {
            data: { hostId, role: roleName, runbook: runbookName, path },
          });
        } catch (error) {
          handleApiError(error, "delete runbook file");
          throw error;
        }
      }

    ─────────────────────────────────────────────────────────────
    FUNCTION 7 — deleteRunbook(hostId, roleName, runbookName)
    ─────────────────────────────────────────────────────────────

    Modeled on skills-api.ts deleteSkill L220-238. LIFE-CRITICAL server-side (Wave-2 handler uses `rm -rf`), so the frontend surface is minimal — just the axios call:

      export async function deleteRunbook(
        hostId: number,
        roleName: string,
        runbookName: string,
      ): Promise<void> {
        try {
          await authApi.delete("/runbooks-editor/runbook", {
            data: { hostId, role: roleName, runbook: runbookName },
          });
        } catch (error) {
          handleApiError(error, "delete runbook");
          throw error;
        }
      }

    ─────────────────────────────────────────────────────────────
    ANTI-PATTERNS TO AVOID
    ─────────────────────────────────────────────────────────────

    - Do NOT import from `@/api/skills-api`. This module is runtime-decoupled per the L8-11 posture. Even the error classes are duplicated verbatim rather than shared.
    - Do NOT expose the `role` field as `role` in function argument names. The parameter names use `roleName` for readability (`listRunbooks(hostId, roleName)`) — but the wire body/query field is `role` (matches Wave-2 backend expectation).
    - Do NOT default any argument. Every function requires all its arguments; the caller must always know host + role + runbook.
    - Do NOT hand-roll a fetch. Use `authApi` (from `@/main-axios`) — it carries the JWT cookie + baseURL config the rest of the app depends on.
    - Do NOT use the DELETE-no-body shape (`authApi.delete("/path")`). Both delete endpoints use the DELETE-with-body pattern (`data: {...}`) — matches Wave-2 backend body-parsing.
    - Do NOT skip the `throw error;` after `handleApiError`. handleApiError throws, but TS's return-type inference can't see through the runtime-throwing helper — the trailing throw is a satisfies-TS-only line (comment matches skills-api.ts L77 `// unreachable`).
    - Do NOT change the wire route path. Wave-2 mounts at `/runbooks-editor` and Wave-4 modal will match; drift here would 404 the entire modal.
    - Do NOT change the wire body/query field names. Backend expects `role` + `runbook` (not `roleName` + `runbookName`).
    - Do NOT add a `getRunbook` (single-runbook metadata fetch) function. Wave 4's modal opens on a slug and calls enumerateRunbookFiles + readRunbookFile directly (matches skills modal shape — no separate "get skill" call exists there either).
    - Do NOT create a companion .test.ts file. skills-api.ts has none by precedent (backend-side coverage in Plan 89-02 handles wire correctness; modal-side coverage in Wave 4 handles helper consumption).
    - Do NOT `git push`. Do NOT `docker build`. Do NOT run full-suite tests. tsc --noEmit is the only build gate for this task.
  </action>
  <acceptance_criteria>
    The API client is complete when every check below holds:

    Grep source assertions on `src/ui/api/runbooks-api.ts`:
    - `grep -c 'Phase 89' src/ui/api/runbooks-api.ts` returns >= 1 (file header anchor).
    - `grep -c '^export async function' src/ui/api/runbooks-api.ts` returns 7 (seven typed helpers exported).
    - `grep -c '^export type' src/ui/api/runbooks-api.ts` returns 5 (RunbookEntry, RunbookFileEntry, RunbookFileReadResult, RunbookFileWriteInput, RunbookFileWriteResult).
    - `grep -c '^export class Runbook' src/ui/api/runbooks-api.ts` returns 2 (RunbookFileMtimeConflictError + RunbookFileAlreadyExistsError).
    - `grep -c '/runbooks-editor/runbooks' src/ui/api/runbooks-api.ts` returns 1 (listRunbooks GET).
    - `grep -c '/runbooks-editor/files' src/ui/api/runbooks-api.ts` returns 1 (enumerateRunbookFiles GET).
    - `grep -c '/runbooks-editor/read' src/ui/api/runbooks-api.ts` returns 1 (readRunbookFile POST).
    - `grep -c '/runbooks-editor/write' src/ui/api/runbooks-api.ts` returns 1 (writeRunbookFile PUT).
    - `grep -c '/runbooks-editor/create' src/ui/api/runbooks-api.ts` returns 1 (createRunbookFile POST).
    - `grep -c '/runbooks-editor/file' src/ui/api/runbooks-api.ts` returns 1 (deleteRunbookFile DELETE — endpoint is `/file` singular, matching Wave-2 route).
    - `grep -c '/runbooks-editor/runbook' src/ui/api/runbooks-api.ts` returns >= 1 (deleteRunbook DELETE — endpoint is `/runbook` singular).
    - `grep -c 'authApi\.' src/ui/api/runbooks-api.ts` returns 7 (one authApi call per helper).
    - `grep -c 'handleApiError' src/ui/api/runbooks-api.ts` returns >= 8 (import + 7 usages, one per helper).
    - `grep -c 'mtime mismatch' src/ui/api/runbooks-api.ts` returns >= 2 (error class super() + narrow check in writeRunbookFile).
    - `grep -c 'file exists' src/ui/api/runbooks-api.ts` returns >= 2 (error class super() + narrow check in createRunbookFile).
    - `grep -c 'role: roleName' src/ui/api/runbooks-api.ts` returns >= 5 (roleName parameter threaded through GET-query-params and POST/PUT/DELETE-body across at least 5 endpoints — listRunbooks + enumerateRunbookFiles + readRunbookFile + createRunbookFile + deleteRunbookFile + deleteRunbook wire the field; writeRunbookFile passes the input object verbatim so the wire field arrives via input.role — that path uses a different grep signature and does NOT need to match this pattern).
    - `grep -c 'runbook: runbookName' src/ui/api/runbooks-api.ts` returns >= 5 (runbookName threaded through at least 5 endpoints — every helper except listRunbooks and writeRunbookFile which handles it via input object).
    - `grep -c "from \"@/api/skills-api\"" src/ui/api/runbooks-api.ts` returns 0 (runtime-decoupled — no cross-import from skills-api).

    Structural / build-level:
    - `wc -l src/ui/api/runbooks-api.ts` returns >= 220 (mirroring the 238-line skills-api.ts shape).
    - `npx tsc --noEmit` completes with no new errors mentioning runbooks-api.ts.
  </acceptance_criteria>
  <verify>
    <automated>set -e; [ "$(grep -c 'Phase 89' src/ui/api/runbooks-api.ts)" -ge "1" ] && [ "$(grep -c '^export async function' src/ui/api/runbooks-api.ts)" = "7" ] && [ "$(grep -c '^export type' src/ui/api/runbooks-api.ts)" = "5" ] && [ "$(grep -c '^export class Runbook' src/ui/api/runbooks-api.ts)" = "2" ] && [ "$(grep -c '/runbooks-editor/runbooks' src/ui/api/runbooks-api.ts)" = "1" ] && [ "$(grep -c '/runbooks-editor/files' src/ui/api/runbooks-api.ts)" = "1" ] && [ "$(grep -c '/runbooks-editor/read' src/ui/api/runbooks-api.ts)" = "1" ] && [ "$(grep -c '/runbooks-editor/write' src/ui/api/runbooks-api.ts)" = "1" ] && [ "$(grep -c '/runbooks-editor/create' src/ui/api/runbooks-api.ts)" = "1" ] && [ "$(grep -c 'authApi\.' src/ui/api/runbooks-api.ts)" = "7" ] && [ "$(grep -c 'handleApiError' src/ui/api/runbooks-api.ts)" -ge "8" ] && [ "$(grep -c 'mtime mismatch' src/ui/api/runbooks-api.ts)" -ge "2" ] && [ "$(grep -c 'file exists' src/ui/api/runbooks-api.ts)" -ge "2" ] && [ "$(grep -c 'role: roleName' src/ui/api/runbooks-api.ts)" -ge "5" ] && [ "$(grep -c 'runbook: runbookName' src/ui/api/runbooks-api.ts)" -ge "5" ] && [ "$(grep -c 'from "@/api/skills-api"' src/ui/api/runbooks-api.ts)" = "0" ] && [ "$(wc -l < src/ui/api/runbooks-api.ts)" -ge "220" ] && npx tsc --noEmit</automated>
  </verify>
  <done>
    - src/ui/api/runbooks-api.ts exists at ~230-260 lines.
    - Seven `export async function` declarations, one per Wave-2 endpoint.
    - Five `export type` declarations for entry, file-entry, read-result, write-input, write-result shapes.
    - Two `export class` typed error declarations (mtime-conflict + already-exists).
    - All wire paths match Wave-2 backend routes exactly.
    - All handlers wire `roleName` and (where relevant) `runbookName` into either the query params or the request body.
    - No cross-import from skills-api (runtime-decoupled per D-01).
    - `npx tsc --noEmit` clean.
    - No companion test file (matches skills-api precedent).
    - NO git push, NO docker command, NO full-suite test run.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser React state → authApi request | Values passed to helpers (hostId, roleName, runbookName, path, content) originate in the modal's state; nothing sensitive is added at this layer. |
| authApi response → typed 409 error class | 409 body must match the exact shape `{error, currentMtime, currentContent}` for mtime-mismatch and `{error}` for file-exists — narrowing guards against future 409 semantics being misinterpreted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-03-01 | Tampering | writeRunbookFile 409 error narrow | mitigate | Explicit narrow on `err.response.data.error === "mtime mismatch"` — a future backend 409 semantic (e.g. "concurrent write in progress") is intentionally NOT caught here; falls through to `handleApiError` per skills-api L146-160 precedent. Prevents the wrong reload-and-lose-local-edits prompt from firing on unrelated 409s. |
| T-89-03-02 | Tampering | createRunbookFile 409 error narrow | mitigate | Same posture as T-89-03-01 — narrow on `error === "file exists"` only. |
| T-89-03-03 | Information Disclosure | Error class serialization | accept | Runtime error classes carry no sensitive data (currentMtime + currentContent are the SAME data the user just fetched). handleApiError normalizes other error surfaces per its own posture. |
| T-89-03-SC | Tampering | npm installs | accept | No new npm dependencies. Uses only existing `@/main-axios` and standard TS lib. |
</threat_model>

<verification>
- `src/ui/api/runbooks-api.ts` exists at ~230-260 lines with 7 exported functions + 5 exported types + 2 exported error classes.
- Every function calls `authApi` against the exact Wave-2 route path.
- Every function catches errors via `handleApiError`, and the two 409-narrow functions add typed error rethrows before falling through.
- No cross-import from skills-api (runtime-decoupled).
- `npx tsc --noEmit` clean.
- No git push, no docker command, no full-suite test run.
</verification>

<success_criteria>
- Frontend can now import from `@/api/runbooks-api` and get a typed surface identical in shape to skills-api.
- Wave 4 (RunbookEditorModal.tsx) has a stable import target — the modal is a byte-shape clone of SkillsEditorModal and the imports at its top mirror the skills-api imports at SkillsEditorModal.tsx L8-20.
- Ready for Wave 4 to proceed.
</success_criteria>

<output>
Create `.planning/phases/89-identity-modal-drop-history-handoff-tabs-add-runbooks-tab-ed/89-03-SUMMARY.md` when done.
</output>
