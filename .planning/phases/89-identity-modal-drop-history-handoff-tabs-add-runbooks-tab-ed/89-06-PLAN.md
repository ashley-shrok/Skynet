---
phase: 89-identity-modal-drop-history-handoff-tabs-add-runbooks-tab-ed
plan: 06
type: execute
wave: 6
depends_on:
  - 89-05
files_modified:
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "PrettyView owns a new state slot `runbookEditorOpenState: {roleName: string; runbookName: string} | null` — non-null when the runbook editor is open, null when closed. The swap-not-stack coordination (D-06) fires when identity modal Runbooks tab row clicks arrive: PrettyView sets `runbookEditorOpenState` to `{roleName, runbookName}` AND simultaneously calls `setIsIdentityModalOpen(false)` — the identity modal closes at the same tick the runbook editor opens"
    - "PrettyView passes `onOpenRunbook={handleOpenRunbook}` to `<IdentityModal>` at ~L3191-3198 alongside the existing `hostId={hostId}`, `hue={pvHue}` props. `handleOpenRunbook(runbookName)` reads `pvIdentity.role` (may be null — see below) and sets state accordingly"
    - "When `pvIdentity.role` is null (identity with no resolvable role), the Runbooks tab won't have any runbooks to click (list would be empty per D-10 fast-path — RunbooksTab short-circuits at roleName === null). So handleOpenRunbook can safely assume role is non-null when invoked; a defensive guard rejects the call quietly if role is null (log-first diagnostics via console.debug), never opens the editor"
    - "PrettyView mounts `<RunbookEditorModal>` alongside `<IdentityModal>` and `<EditableFileModal>` (~L3200-3220 region). Mount is gated by `runbookEditorOpenState !== null`. Props: open=true, onOpenChange=(open)=>{ if (!open) setRunbookEditorOpenState(null); } — close signal returns to null; NO re-open of identity modal on close per D-06 swap-not-stack. hostId={hostId}, roleName={runbookEditorOpenState.roleName}, runbookName={runbookEditorOpenState.runbookName}, container={undefined} (document.body default — the modal is a top-level surface, NOT portaled into chatRegionEl unlike IdentityModal)"
    - "New test file `src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx` covers the full flow in-process: open identity modal → click Runbooks tab → click runbook row → assert identity modal closed + runbook editor open (D-06) → close runbook editor → assert identity modal did NOT reopen (also D-06). ~3-5 test cases covering the happy path + swap-close-does-not-reopen invariant + null-role-fast-path"
    - "Scoped tests pass; tsc --noEmit clean"
  artifacts:
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "Updated PrettyView with runbookEditorOpenState + handleOpenRunbook + RunbookEditorModal mount"
      contains: "RunbookEditorModal"
    - path: "src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx"
      provides: "In-process test suite walking the swap flow"
      contains: "swap"
      min_lines: 120
  key_links:
    - from: "src/ui/features/pretty-view/PrettyView.tsx handleOpenRunbook"
      to: "src/ui/features/pretty-view/RunbookEditorModal.tsx (mount)"
      via: "state slot + JSX gate — RunbookEditorModal only renders when runbookEditorOpenState is non-null; open=true when it renders; onOpenChange(false) returns to null"
      pattern: "RunbookEditorModal"
    - from: "src/ui/features/pretty-view/PrettyView.tsx handleOpenRunbook (identityModal callback)"
      to: "IdentityModal.setIsIdentityModalOpen(false)"
      via: "same handler call — identity modal closes at the same tick the runbook editor opens (D-06 swap-not-stack)"
      pattern: "setIsIdentityModalOpen\\(false\\)"
---

<objective>
Wave-6 integration + test coverage. Two coordinated edits that complete the swap-not-stack coordination (D-06):

1. **PrettyView wires the swap coordination.** New state slot `runbookEditorOpenState`; new callback `handleOpenRunbook` that closes the identity modal + opens the runbook editor at the same tick; wire `onOpenRunbook={handleOpenRunbook}` on the existing `<IdentityModal>` render; mount `<RunbookEditorModal>` alongside the existing `<IdentityModal>` / `<EditableFileModal>` sibling modals.

2. **New in-process test suite** `IdentityModal.runbooks-swap.test.tsx` walks the full flow: mount PrettyView → open identity modal → switch to Runbooks tab → click a runbook row → assert identity modal closed + runbook editor open + shows the picked runbook name → close runbook editor → assert identity modal did NOT reopen (D-06 swap-not-stack invariant).

Purpose: closes the loop. As of Plan 89-05, RunbooksTab's row click calls a `onOpenRunbook` prop that's required but not yet consumed anywhere at the top level. This plan supplies the top-level consumer + proves it works with a real user-flow test. Together the six plans deliver the full bounty per shape file § Scope § In.

Output:
- `src/ui/features/pretty-view/PrettyView.tsx` — MODIFIED: new state slot, new handler function, IdentityModal render gains one prop, new RunbookEditorModal mount block.
- `src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx` — NEW, ~150-250 lines.
- Scoped tests pass. `npx tsc --noEmit` clean.

Ship discipline: scoped tests only; no `git push`, no `docker build`, no `docker cp`, no `docker compose up`. Per D-21 + role standing directive. After this plan closes, the phase ships to origin (orchestrator-owned motion, not planner/executor). Deploy is post-campaign per D-21.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/89-identity-modal-drop-history-handoff-tabs-add-runbooks-tab-ed/89-CONTEXT.md
@.planning/shapes/shape-identity-modal-tab-restructure.md
@src/ui/features/pretty-view/PrettyView.tsx
@src/ui/features/pretty-view/RunbookEditorModal.tsx
@src/ui/features/pretty-view/IdentityModal.tsx
@src/ui/features/pretty-view/IdentityModal.coordinator-empty.test.tsx
@src/ui/features/pretty-view/RunbooksTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PrettyView.tsx — new runbookEditorOpenState + handleOpenRunbook + IdentityModal onOpenRunbook wiring + RunbookEditorModal mount</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/PrettyView.tsx L1-50 (imports at the top — the new RunbookEditorModal import goes near L42 alongside the existing IdentityModal import), L670-700 (existing state slot region including `isIdentityModalOpen` at L679 and `editorOpenState` at L684-692 — the new `runbookEditorOpenState` slot slots in here), L3180-3225 (existing sibling modal mounts — the IdentityModal render at L3190-3199 needs the new `onOpenRunbook` prop; the EditableFileModal at L3209-3220 is the sibling shape the new RunbookEditorModal mount mirrors).
    - src/ui/features/pretty-view/RunbookEditorModal.tsx (Plan 89-04) — verify prop shape: open, onOpenChange, hostId, roleName, runbookName, container?.
    - src/ui/features/pretty-view/IdentityModal.tsx (post-Plan-89-05) — verify the new `onOpenRunbook: (runbookName: string) => void` prop shape.
    - .planning/phases/89-identity-modal-drop-history-handoff-tabs-add-runbooks-tab-ed/89-CONTEXT.md § D-06 (swap-not-stack semantic — close on the runbook editor dismisses without restoring the identity modal).
    - .planning/shapes/shape-identity-modal-tab-restructure.md § Shape #3 header last paragraph ("The close button also functions semantically as 'back' — the user came from the identity modal, but v1 does not restore it") and § Philosophy "Swap-not-stack for v1, because the app doesn't currently do modal-on-modal."
  </read_first>
  <action>
    Four coordinated sub-edits inside one file (plus one new import).

    ─────────────────────────────────────────────────────────────
    EDIT A — Import RunbookEditorModal
    ─────────────────────────────────────────────────────────────

    Near L42 alongside the existing `import { IdentityModal } from "./IdentityModal";`, add:

      import RunbookEditorModal from "./RunbookEditorModal";

    Match whichever export shape Plan 89-04 used (default export → default import; named export → named import). Cross-check by grepping `src/ui/features/pretty-view/RunbookEditorModal.tsx` for `export default` — if present, use `import RunbookEditorModal from "./RunbookEditorModal";`; if named, use `import { RunbookEditorModal } from "./RunbookEditorModal";`.

    ─────────────────────────────────────────────────────────────
    EDIT B — New state slot `runbookEditorOpenState`
    ─────────────────────────────────────────────────────────────

    Near L679 (existing `isIdentityModalOpen` state), add a new state slot:

      // Phase 89 Plan 06: swap-not-stack coordination (D-06) — non-null when the
      // runbook editor is open, null when closed. Set by handleOpenRunbook (fired
      // from IdentityModal's Runbooks tab row click); cleared to null by the
      // RunbookEditorModal's onOpenChange(false) close handler. NO reopen-of-
      // identity-modal-on-close per D-06 v1 posture.
      const [runbookEditorOpenState, setRunbookEditorOpenState] = useState<
        | null
        | { roleName: string; runbookName: string }
      >(null);

    Place immediately AFTER `const [isIdentityModalOpen, setIsIdentityModalOpen] = useState(false);` at L679, so the two related modal-open state slots sit visually adjacent.

    ─────────────────────────────────────────────────────────────
    EDIT C — New handler `handleOpenRunbook`
    ─────────────────────────────────────────────────────────────

    Somewhere in the PrettyView component body BEFORE the JSX return (grep to find a natural spot — after other handleXXX callbacks like `handleStageEditedFile` at L~660-700 region, or near the top of the return-block-preceding handlers block). Add:

      // Phase 89 Plan 06: Runbooks tab row click handler. D-06 swap-not-stack —
      // closes the identity modal at the same tick the runbook editor opens.
      // Defensive guard: if the identity has no resolved role, silently no-op
      // (RunbooksTab's fetch would have returned an empty list via the D-10
      // fast-path, so no row-click should reach here in that case — but the
      // guard prevents crashes if some future call path bypasses the fast-path).
      const handleOpenRunbook = useCallback((runbookName: string): void => {
        const roleName = pvIdentity?.role ?? null;
        if (roleName === null) {
          console.debug("[PrettyView] handleOpenRunbook: no role — skipping", { runbookName });
          return;
        }
        console.debug("[PrettyView] handleOpenRunbook: swap", { roleName, runbookName });
        setIsIdentityModalOpen(false);
        setRunbookEditorOpenState({ roleName, runbookName });
      }, [pvIdentity]);

    NOTE: `pvIdentity` is the existing state slot elsewhere in this component that holds the currently-focused Identity object (grep to confirm the variable name; adjust the closure dependency and read expression to whatever the actual variable is). The useCallback dep list includes `pvIdentity` so the closure captures the latest role when the identity changes.

    ─────────────────────────────────────────────────────────────
    EDIT D — Add `onOpenRunbook` prop to existing `<IdentityModal>` render
    ─────────────────────────────────────────────────────────────

    At L3190-3199, the existing `<IdentityModal>` render currently reads:

      <IdentityModal
        open={isIdentityModalOpen}
        onOpenChange={setIsIdentityModalOpen}
        identity={pvIdentity}
        hue={pvHue}
        hostId={hostId}
        container={chatRegionEl}
      />

    Add one new attribute `onOpenRunbook={handleOpenRunbook}` immediately after the `hostId={hostId}` line and before the `container={chatRegionEl}` line. Preface with a one-line comment:

      // Phase 89 Plan 06: Runbooks tab row-click handler — D-06 swap-not-stack.
      onOpenRunbook={handleOpenRunbook}

    ─────────────────────────────────────────────────────────────
    EDIT E — Add new `<RunbookEditorModal>` sibling mount
    ─────────────────────────────────────────────────────────────

    Immediately AFTER the existing `<IdentityModal>` render block (which ends at L3199 with `/>`, and the closing brace of the pvIdentity gate at ~L3200), and AFTER the existing `{editorOpenState && <EditableFileModal ... />}` block at ~L3209-3221 (or BEFORE it — sibling order doesn't matter functionally; either place is fine — pick the spot that keeps related modals adjacent), add:

      {/* Phase 89 Plan 06: RunbookEditorModal — mounted as a top-level sibling
          alongside IdentityModal + EditableFileModal. Portal target defaults
          to document.body (no `container` prop passed) since this is a top-
          level surface per D-06 swap-not-stack — the modal is NOT portaled
          into chatRegionEl the way IdentityModal is; it fills the viewport
          like SkillsEditorModal / GlobalFilesModal do.
          runbookEditorOpenState.roleName + runbookName are already validated
          non-null by handleOpenRunbook (the defensive guard rejects null-role
          calls before setting state). */}
      {runbookEditorOpenState && (
        <RunbookEditorModal
          open={true}
          onOpenChange={(open) => {
            if (!open) {
              console.debug("[PrettyView] runbook-editor-close: no reopen (D-06 swap-not-stack)");
              setRunbookEditorOpenState(null);
            }
          }}
          hostId={hostId}
          roleName={runbookEditorOpenState.roleName}
          runbookName={runbookEditorOpenState.runbookName}
        />
      )}

    Key points:
    - `open={true}` when rendered — the mount gate (`runbookEditorOpenState && (...)`) is what controls presence; the modal itself doesn't need to see open=false because unmounting handles that (matches the `{editorOpenState && <EditableFileModal open={true} ...>}` pattern at L3209-3221).
    - `onOpenChange(false)` returns state to null. NO call to `setIsIdentityModalOpen(true)` — swap-not-stack per D-06 explicitly forbids reopening the identity modal on close.
    - `container` prop is NOT passed. Default portal target is document.body per Plan 89-04 spec. The modal is a top-level surface (unlike IdentityModal which portals into chatRegionEl).

    ─────────────────────────────────────────────────────────────
    ANTI-PATTERNS TO AVOID
    ─────────────────────────────────────────────────────────────

    - Do NOT `setIsIdentityModalOpen(true)` inside the RunbookEditorModal's `onOpenChange(false)` handler. Swap-not-stack per D-06 — close means close, no reopen. Even if it feels "nicer UX" to return the user to where they came from, D-06 is explicit that v1 does not restore.
    - Do NOT stack the two modals (both open at once). The whole point of swap-not-stack is that at any moment ≤ 1 modal is visible. handleOpenRunbook fires BOTH setIsIdentityModalOpen(false) AND setRunbookEditorOpenState(...) at the same tick; React batches them so the transition is atomic.
    - Do NOT thread `container={chatRegionEl}` on the RunbookEditorModal. Only IdentityModal portals into chatRegionEl (that's a patch #108 constraint). RunbookEditorModal is a top-level surface — document.body default.
    - Do NOT gate the RunbookEditorModal mount on `pvIdentity !== null`. Gate on `runbookEditorOpenState !== null` — the state slot IS the "should we render this modal" signal. Once open, the modal doesn't need pvIdentity in scope (the roleName/runbookName it needs are inside runbookEditorOpenState).
    - Do NOT add a "runbook editor open → suppress bounty modal open" mutex or similar. If Alice clicks a bounty in a different pane while the runbook editor is open, that's fine — the bounty modal opens in that pane; both can coexist because they're pane-local. This plan doesn't add cross-pane coordination.
    - Do NOT rename the existing `isIdentityModalOpen` state. It's consumed all through the file; touching it would balloon the diff.
    - Do NOT `git push`. Do NOT `docker build`. Do NOT `docker cp`. Do NOT `docker compose up`. Scoped test only per D-21.
  </action>
  <acceptance_criteria>
    PrettyView wiring is complete when every check below holds:

    Grep source assertions on `src/ui/features/pretty-view/PrettyView.tsx`:
    - `grep -c 'Phase 89 Plan 06' src/ui/features/pretty-view/PrettyView.tsx` returns >= 4 (comment anchors on all four sub-edits).
    - `grep -c 'RunbookEditorModal' src/ui/features/pretty-view/PrettyView.tsx` returns >= 3 (import + JSX mount + defense-in-depth comment reference).
    - `grep -c 'runbookEditorOpenState' src/ui/features/pretty-view/PrettyView.tsx` returns >= 4 (declaration + setter setter use + mount-gate + prop read).
    - `grep -c 'handleOpenRunbook' src/ui/features/pretty-view/PrettyView.tsx` returns >= 2 (declaration + JSX prop on IdentityModal).
    - `grep -c 'onOpenRunbook={handleOpenRunbook}' src/ui/features/pretty-view/PrettyView.tsx` returns 1 (prop wired on the existing IdentityModal render).
    - `grep -c 'setIsIdentityModalOpen(false)' src/ui/features/pretty-view/PrettyView.tsx` returns >= 1 (called inside handleOpenRunbook — proves the identity modal closes when the runbook editor opens).
    - `grep -c 'setIsIdentityModalOpen(true)' src/ui/features/pretty-view/PrettyView.tsx` — MUST NOT increase from its pre-edit count (grep before the edit for the exact number, then confirm the count is unchanged — the swap-not-stack invariant says the runbook editor's onOpenChange(false) must NOT call setIsIdentityModalOpen(true)). Pre-edit expected count: 0 or 1 (grep to confirm what's there before editing). Post-edit count MUST NOT be greater than pre-edit count.
    - `grep -c '\[PrettyView\] handleOpenRunbook' src/ui/features/pretty-view/PrettyView.tsx` returns >= 1 (structured debug log per role standing directive log-first diagnostics).
    - `grep -c 'D-06\|swap-not-stack' src/ui/features/pretty-view/PrettyView.tsx` returns >= 2 (comment anchors on the swap-not-stack rationale — at least in the state-slot comment + the onOpenChange handler comment).

    Structural / build-level:
    - `npx tsc --noEmit` completes with no new errors mentioning PrettyView.tsx or RunbookEditorModal.tsx or RunbooksTab.tsx.

    Scoped tests exit 0 (existing PrettyView test files must still pass):
    - `npx vitest run --related src/ui/features/pretty-view/PrettyView.tsx --run` exits 0. Existing tests do not exercise the new runbookEditorOpenState slot so they should be unaffected by the additions.
  </acceptance_criteria>
  <verify>
    <automated>set -e; [ "$(grep -c 'Phase 89 Plan 06' src/ui/features/pretty-view/PrettyView.tsx)" -ge "4" ] && [ "$(grep -c 'RunbookEditorModal' src/ui/features/pretty-view/PrettyView.tsx)" -ge "3" ] && [ "$(grep -c 'runbookEditorOpenState' src/ui/features/pretty-view/PrettyView.tsx)" -ge "4" ] && [ "$(grep -c 'handleOpenRunbook' src/ui/features/pretty-view/PrettyView.tsx)" -ge "2" ] && [ "$(grep -c 'onOpenRunbook={handleOpenRunbook}' src/ui/features/pretty-view/PrettyView.tsx)" = "1" ] && [ "$(grep -c 'setIsIdentityModalOpen(false)' src/ui/features/pretty-view/PrettyView.tsx)" -ge "1" ] && [ "$(grep -cE '\[PrettyView\] handleOpenRunbook' src/ui/features/pretty-view/PrettyView.tsx)" -ge "1" ] && [ "$(grep -cE 'D-06|swap-not-stack' src/ui/features/pretty-view/PrettyView.tsx)" -ge "2" ] && npx tsc --noEmit && npx vitest run --related src/ui/features/pretty-view/PrettyView.tsx --run</automated>
  </verify>
  <done>
    - PrettyView.tsx has new import for RunbookEditorModal.
    - PrettyView.tsx has new `runbookEditorOpenState` state slot alongside `isIdentityModalOpen`.
    - PrettyView.tsx has new `handleOpenRunbook` useCallback that reads `pvIdentity.role` + closes identity modal + opens runbook editor.
    - PrettyView.tsx `<IdentityModal>` render gains one `onOpenRunbook={handleOpenRunbook}` prop.
    - PrettyView.tsx mounts `<RunbookEditorModal>` gated on `runbookEditorOpenState !== null`, with onOpenChange(false) that only returns state to null (does NOT reopen identity modal).
    - No new `setIsIdentityModalOpen(true)` call site added (swap-not-stack invariant).
    - Structured debug logs at handleOpenRunbook + runbook-editor-close boundaries.
    - `npx tsc --noEmit` clean.
    - Scoped PrettyView test run exits 0.
    - NO git push, NO docker command.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx — in-process test suite walking the full swap flow</name>
  <files>src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/IdentityModal.coordinator-empty.test.tsx (post-Plan-89-05) — the shape to mirror. Read L1-100 (mock stubs, WsStub, openedSockets tracker, findSocketForRequestType helper) + L100-260 (delivery helpers + switchScope + clickNav + beforeEach/afterEach). The new test file uses the SAME mock harness — do NOT re-implement; extract if the codebase supports it, or duplicate the harness inline (matches existing codebase convention where each test file carries its own mock stubs).
    - src/ui/features/pretty-view/IdentityModal.role-tab.test.tsx — a simpler adjacent test to reference for `<IdentityModal>` render setup with `onOpenRunbook={vi.fn()}` prop.
    - src/ui/features/pretty-view/RunbooksTab.tsx (Plan 89-05) — verify the DOM shape row clicks produce. Rows are `<button type="button" onClick={...}>{entry.name}</button>` — so the test finds a row by text content and fires click.
    - src/ui/features/pretty-view/RunbookEditorModal.tsx (Plan 89-04) — verify the DOM signals: title reads `Edit runbook: {runbookName}` so the test asserts on that literal string.
    - src/ui/api/runbooks-api.ts — verify the mocked shape. Test file uses vitest `vi.mock('@/api/runbooks-api', ...)` to stub `listRunbooks` + `enumerateRunbookFiles` + `readRunbookFile` returning canned data.
    - .planning/phases/89-identity-modal-drop-history-handoff-tabs-add-runbooks-tab-ed/89-CONTEXT.md § D-06 (swap-not-stack semantic — the test's central invariant).
    - .planning/shapes/shape-identity-modal-tab-restructure.md § Shape #2 (Runbooks tab body — row-click behavior) + § Shape #3 (Runbook editor modal — swap target).
  </read_first>
  <action>
    Create ONE new file `src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx` — vitest test suite covering the end-to-end swap flow.

    ─────────────────────────────────────────────────────────────
    FILE HEADER
    ─────────────────────────────────────────────────────────────

    Open with a Phase 89 header docblock listing the test-case manifest:

      /**
       * Phase 89 Plan 06 — Runbooks tab swap-not-stack (D-06) in-process test suite.
       *
       * Tests walk the full flow through the identity modal → Runbooks tab → runbook
       * editor swap coordination. Locks the D-06 invariant that closing the runbook
       * editor does NOT restore the identity modal.
       *
       *   S1: happy path — open identity modal → switch to Runbooks tab → click a
       *       runbook row → identity modal closed + runbook editor rendered with
       *       the picked runbook name in the title.
       *
       *   S2: swap-close-does-not-reopen — after S1, click close on the runbook
       *       editor → runbook editor unmounts + identity modal STILL closed (no
       *       reopen — D-06 v1).
       *
       *   S3: null-role fast path — identity with role=null → Runbooks tab renders
       *       the empty state "This role has no runbooks yet." → no fetch was
       *       issued (listRunbooks mock NOT called).
       *
       *   S4: empty-list state — identity with role='some-role' → listRunbooks
       *       mock returns [] → tab body shows "This role has no runbooks yet."
       *
       *   S5: multiple runbooks alphabetical — listRunbooks returns 3 unsorted
       *       runbooks → tab body renders 3 rows in alphabetical order.
       */

    ─────────────────────────────────────────────────────────────
    MOCKS
    ─────────────────────────────────────────────────────────────

    Mock the API layer for runbooks:

      vi.mock('@/api/runbooks-api', () => ({
        listRunbooks: vi.fn(),
        enumerateRunbookFiles: vi.fn(),
        readRunbookFile: vi.fn(),
        writeRunbookFile: vi.fn(),
        createRunbookFile: vi.fn(),
        deleteRunbookFile: vi.fn(),
        deleteRunbook: vi.fn(),
        RunbookFileMtimeConflictError: class extends Error {},
        RunbookFileAlreadyExistsError: class extends Error {},
      }));

    Mock the WebSocket / claude-session-api sockets same shape as coordinator-empty.test.tsx (openClaudeSessionSocket returns a WsStub; sockets tracked in openedSockets array; `findSocketForRequestType` helper matches by parsed request type). Reuse the same helper implementations verbatim — the shape is stable across identity-modal tests.

    ─────────────────────────────────────────────────────────────
    RENDER HELPER
    ─────────────────────────────────────────────────────────────

    A helper factory that renders the identity modal + the swap coordinator (a lightweight harness that mimics PrettyView's state-management shape without pulling in the whole PrettyView component). Two options:

    Option A — full PrettyView render: too heavy; PrettyView has many dependencies (harness panels, hostTree, WS-store, etc.). AVOID.

    Option B — RECOMMENDED — a small in-file harness component:

      function TestHarness({ identity, initialRunbooks }: { identity: Identity; initialRunbooks?: RunbookEntry[] }) {
        const [isIdModalOpen, setIsIdModalOpen] = React.useState(true);
        const [runbookOpen, setRunbookOpen] = React.useState<{roleName: string; runbookName: string} | null>(null);
        const handleOpenRunbook = React.useCallback((runbookName: string) => {
          const roleName = identity.role;
          if (roleName === null) return;
          setIsIdModalOpen(false);
          setRunbookOpen({ roleName, runbookName });
        }, [identity]);
        // Configure listRunbooks mock
        if (initialRunbooks !== undefined) {
          (vi.mocked(listRunbooks) as ReturnType<typeof vi.fn>).mockResolvedValue(initialRunbooks);
        }
        return (
          <>
            <IdentityModal
              open={isIdModalOpen}
              onOpenChange={setIsIdModalOpen}
              identity={identity}
              hue={220}
              hostId={7}
              onOpenRunbook={handleOpenRunbook}
            />
            {runbookOpen && (
              <RunbookEditorModal
                open={true}
                onOpenChange={(o) => { if (!o) setRunbookOpen(null); }}
                hostId={7}
                roleName={runbookOpen.roleName}
                runbookName={runbookOpen.runbookName}
              />
            )}
          </>
        );
      }

    This harness mirrors PrettyView's coordination shape exactly (state slot + handler + gated mount) but is self-contained for testability. TestHarness proves the SHAPE of the coordination — not PrettyView's exact composition, but the same D-06 invariants.

    ─────────────────────────────────────────────────────────────
    TESTS
    ─────────────────────────────────────────────────────────────

    Use React Testing Library's `render`, `screen`, `fireEvent`, `waitFor`, `act`, `cleanup` (matching coordinator-empty.test.tsx imports). Each test starts fresh via beforeEach mock reset.

    (S1 — happy path):
      - render(<TestHarness identity={makeIdentity({role: 'box-maintainer'})} initialRunbooks={[{name: 'avatar-flow'}, {name: 'skynet-admin'}]} />)
      - Wait for identity modal to appear (assert some identity-modal-specific DOM node).
      - Fire click on the Runbooks nav-tab button (via clickNav helper — mirrors coordinator-empty.test.tsx).
      - Wait for the row "avatar-flow" to appear.
      - Fire click on the "avatar-flow" row.
      - Configure enumerateRunbookFiles mock to return `[{path:'runbook.md'}]` so the RunbookEditorModal renders its title branch.
      - Await: `screen.getByText(/Edit runbook: avatar-flow/i)` — the RunbookEditorModal is now visible.
      - Assert: the identity modal DOM is GONE (`screen.queryByTestId('id-modal-content')` returns null — adjust to whatever the identity modal's testable anchor is; if there's no data-testid, query by role="dialog" and check title text).

    (S2 — swap-close-does-not-reopen):
      - Setup: same as S1 up to "avatar-flow modal visible".
      - Fire click on the RunbookEditorModal's close button (`screen.getByRole('button', {name: /close/i})` or similar — the SkillsEditorModal's close button uses aria-label="Close" per L493-494, so RunbookEditorModal inherits that).
      - Wait for the runbook editor to unmount.
      - Assert: RunbookEditorModal is gone (query by "Edit runbook:" text returns null).
      - Assert: identity modal STILL not visible (query by identity-modal anchor returns null) — the D-06 invariant.

    (S3 — null-role fast path):
      - render(<TestHarness identity={makeIdentity({role: null})} />)
      - Wait for identity modal to appear.
      - Click Runbooks tab.
      - Wait for tab body to render — should show "This role has no runbooks yet." empty state.
      - Assert: `listRunbooks` mock was NOT called (`vi.mocked(listRunbooks).mock.calls.length === 0`).

    (S4 — empty-list state):
      - render(<TestHarness identity={makeIdentity({role: 'box-maintainer'})} initialRunbooks={[]} />)
      - Wait for identity modal + Runbooks tab click + tab body render.
      - Assert: `screen.getByText(/This role has no runbooks yet\./)` present.
      - Assert: `listRunbooks` mock WAS called once (`vi.mocked(listRunbooks).mock.calls.length === 1`) — this differs from S3 which fast-paths on null role.

    (S5 — alphabetical sort):
      - render(<TestHarness identity={makeIdentity({role: 'box-maintainer'})} initialRunbooks={[{name: 'zebra'}, {name: 'apple'}, {name: 'mango'}]} />)
      - Wait for tab body render.
      - Assert: rows appear in alphabetical order — use `screen.getAllByRole('button')` to grab all rows, filter to the runbook-row shape (they have `.text-left.px-3.py-2` class per RunbooksTab shape), map to textContent, then assert the array equals `['apple', 'mango', 'zebra']`.

    ─────────────────────────────────────────────────────────────
    beforeEach / afterEach
    ─────────────────────────────────────────────────────────────

    `beforeEach`: `vi.clearAllMocks(); openedSockets.length = 0; __resetModalScopeForTest();` (matches coordinator-empty.test.tsx L206-210).
    `afterEach`: `cleanup();` (RTL cleanup).

    ─────────────────────────────────────────────────────────────
    ANTI-PATTERNS TO AVOID
    ─────────────────────────────────────────────────────────────

    - Do NOT skip S2. The swap-not-stack invariant is the load-bearing behavioral change in this phase; S2 locks it.
    - Do NOT test PrettyView directly. It has too many upstream dependencies. TestHarness mirrors the SHAPE of the coordination — this is testing "does the coordination logic work?", not "does PrettyView have this exact wiring?" (Task 1's grep gates test the wiring).
    - Do NOT mock out the RunbookEditorModal or RunbooksTab components. Real components inside the render → the tests actually walk the DOM they produce.
    - Do NOT mock the identity modal itself. Real IdentityModal render + real Tab switching + real button click.
    - Do NOT `git push`. Do NOT `docker build`. Do NOT run full-suite tests. Scoped test run only.
  </action>
  <acceptance_criteria>
    Test file is complete when every check below holds:

    Grep source assertions on `src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx`:
    - `grep -c 'Phase 89 Plan 06' src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx` returns >= 1 (file header).
    - `grep -c "describe(" src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx` returns >= 1.
    - `grep -c "it(" src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx` returns >= 5 (S1-S5).
    - `grep -c 'S1:\|S2:\|S3:\|S4:\|S5:' src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx` returns >= 5 (test-case anchors in the header manifest).
    - `grep -c 'D-06\|swap-not-stack' src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx` returns >= 2 (invariant anchor + at least one in-test comment).
    - `grep -c 'RunbookEditorModal' src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx` returns >= 1 (rendered inside TestHarness).
    - `grep -c 'IdentityModal' src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx` returns >= 1.
    - `grep -c 'listRunbooks' src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx` returns >= 3 (S3/S4 mock-call assertions + S5 mock setup).
    - `grep -c 'vi\.mock' src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx` returns >= 1 (runbooks-api mock at least).
    - `grep -c 'onOpenRunbook' src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx` returns >= 1 (TestHarness threads it).
    - `grep -c 'This role has no runbooks yet' src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx` returns >= 2 (S3 + S4 assertions).

    Structural / build-level:
    - `wc -l src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx` returns >= 120.
    - `npx tsc --noEmit` completes with no new errors mentioning this test file.

    Test-command assertion (primary success gate):
    - `npx vitest run src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx --run` exits 0 (all 5 tests pass).
  </acceptance_criteria>
  <verify>
    <automated>set -e; [ "$(grep -c 'Phase 89 Plan 06' src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx)" -ge "1" ] && [ "$(grep -c "describe(" src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx)" -ge "1" ] && [ "$(grep -c "it(" src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx)" -ge "5" ] && [ "$(grep -c 'S1:\|S2:\|S3:\|S4:\|S5:' src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx)" -ge "5" ] && [ "$(grep -cE 'D-06|swap-not-stack' src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx)" -ge "2" ] && [ "$(grep -c 'RunbookEditorModal' src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx)" -ge "1" ] && [ "$(grep -c 'IdentityModal' src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx)" -ge "1" ] && [ "$(grep -c 'listRunbooks' src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx)" -ge "3" ] && [ "$(grep -c 'vi\.mock' src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx)" -ge "1" ] && [ "$(grep -c 'onOpenRunbook' src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx)" -ge "1" ] && [ "$(grep -c 'This role has no runbooks yet' src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx)" -ge "2" ] && [ "$(wc -l < src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx)" -ge "120" ] && npx tsc --noEmit && npx vitest run src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx --run</automated>
  </verify>
  <done>
    - `src/ui/features/pretty-view/IdentityModal.runbooks-swap.test.tsx` exists at ~150-300 lines.
    - Five test cases S1-S5 covering happy path, swap-close-does-not-reopen, null-role fast path, empty list, alphabetical sort.
    - Tests render a small TestHarness that mirrors PrettyView's coordination shape (state slot + handler + gated mount).
    - runbooks-api is mocked; listRunbooks return value is controlled per-test.
    - `npx vitest run <path>` exits 0.
    - `npx tsc --noEmit` clean.
    - NO git push, NO docker command, NO full-suite test run.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IdentityModal.onOpenRunbook callback → PrettyView.handleOpenRunbook | runbookName arrives from a user click on a row that was rendered from listRunbooks output — the value was validated server-side per Plan 89-02 gates. |
| runbookEditorOpenState React state → RunbookEditorModal props | roleName + runbookName are treated as opaque strings; downstream RunbookEditorModal calls into runbooks-api which re-validates server-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-06-01 | Repudiation | Swap-not-stack invariant | mitigate | S2 test locks that closing the runbook editor does NOT reopen the identity modal. A future refactor that accidentally re-adds `setIsIdentityModalOpen(true)` to the close handler breaks this test loud. |
| T-89-06-02 | Denial of Service | Handler fired against null role | mitigate | handleOpenRunbook has a defensive guard that returns silently when pvIdentity.role is null. RunbooksTab's D-10 fast-path prevents ever reaching this state in practice (empty list → no rows → no click), but the guard is defense-in-depth. |
| T-89-06-SC | Tampering | npm installs | accept | No new npm dependencies. |
</threat_model>

<verification>
- PrettyView.tsx has new state slot + handler + IdentityModal prop wiring + RunbookEditorModal mount.
- IdentityModal.runbooks-swap.test.tsx exists with S1-S5 test cases covering the D-06 invariant.
- `npx tsc --noEmit` clean.
- Scoped tests exit 0.
- No git push, no docker command, no full-suite test run.
</verification>

<success_criteria>
- Full flow works end-to-end in a real vitest DOM: identity modal opens → Runbooks tab → click row → identity modal closes + runbook editor opens with picked runbook name → close runbook editor → identity modal stays closed.
- D-06 swap-not-stack invariant locked by S2 test — future refactors that accidentally reintroduce stack behavior break the build.
- Phase 89 ready for close: `/close identity-modal-tab-restructure` → unbiased code review → agent UAT walks the flow → campaign ship greenlight (post-campaign, orchestrator-owned).
- No git push, no docker command in this plan. Push is orchestrator-owned motion after all plans land.
</success_criteria>

<output>
Create `.planning/phases/89-identity-modal-drop-history-handoff-tabs-add-runbooks-tab-ed/89-06-SUMMARY.md` when done.
</output>
