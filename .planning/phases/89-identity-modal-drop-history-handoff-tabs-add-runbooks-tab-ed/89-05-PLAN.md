---
phase: 89-identity-modal-drop-history-handoff-tabs-add-runbooks-tab-ed
plan: 05
type: execute
wave: 5
depends_on:
  - 89-04
files_modified:
  - src/ui/features/pretty-view/IdentityModal.tsx
  - src/ui/features/pretty-view/HistoryTab.tsx
  - src/ui/features/pretty-view/HandoffTab.tsx
  - src/ui/features/pretty-view/RunbooksTab.tsx
  - src/ui/features/pretty-view/IdentityModal.coordinator-empty.test.tsx
  - src/ui/api/claude-session-api.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Post-restructure identity modal tab order per D-13: role file, runbooks (new), identity file, bounties, wakeups. Runbooks sits between role file and identity file as the second role-scope tab. History and Handoff tabs are removed entirely per D-12"
    - "Files that ONLY served History or Handoff are deleted: `src/ui/features/pretty-view/HistoryTab.tsx` and `src/ui/features/pretty-view/HandoffTab.tsx` (no other consumers per grep — only IdentityModal.tsx imports them per <code_context> § Integration Points)"
    - "New file `src/ui/features/pretty-view/RunbooksTab.tsx` renders the bare list body per D-08 through D-11: bare list of runbook slugs (D-08), alphabetically sorted (D-09), empty state 'This role has no runbooks yet.' when folder absent or empty (D-10), always rendered even for roles with no runbooks (D-11), row click triggers `onOpenRunbook(slug)` — the ONLY interaction per D-08 + shape § What would make it wrong"
    - "IdentityModal accepts a NEW prop `onOpenRunbook: (runbookName: string) => void` (required — no fail-closed default; the caller MUST wire it since Runbooks tab now always renders per D-11). Called from RunbooksTab row click. The prop routes through the Runbooks tab body and up to Wave 6 PrettyView, which owns the swap-not-stack coordination (D-06)"
    - "IdentityModal removes: HistoryTab + HandoffTab imports (L125, L127); NAV_SECTIONS_ROLE `{value:'history',...}` entry (L351); NAV_SECTIONS_IDENTITY `{value:'handoff',...}` entry (L357); Clock + Handshake icon imports if unused elsewhere (L6); historyState + handoffState useState slots (L371, L380); the setHistoryState/setHandoffState calls in the reset-on-open effect (L416, L419); the openOneShot fetches for identity:get-history + identity:get-handoff (L545-553, L576-583); updateHistory + updateHandoff save handlers (L1033-1051, L1053-1068); the `<TabsContent value=\"history\">` + `<TabsContent value=\"handoff\">` blocks (L2541-2547, L2589-2598); wire type imports IdentityGetHistoryPayload / IdentityHistoryEvent / IdentityGetHandoffPayload / IdentityHandoffEvent / IdentityUpdateHistoryPayload / IdentityHistoryUpdatedEvent / IdentityUpdateHandoffPayload / IdentityHandoffUpdatedEvent (L57-58, L61-62, L84-87)"
    - "IdentityModal adds: NAV_SECTIONS_ROLE `{value:'runbooks', label:'Runbooks', Icon: BookOpen}` entry inserted between role file and bounties; a new `<TabsContent value=\"runbooks\">` block rendering `<RunbooksTab hostId={hostId} roleName={identity.role} onOpenRunbook={onOpenRunbook} />`; BookOpen import from lucide-react (or Book / FileStack — planner picks BookOpen, matching semantic fit)"
    - "Visible tab order is (role scope) role-file, runbooks, bounties, wake-ups | (identity scope) identity-file, wake-ups, telegram — D-13's spelling of `role file, runbooks, identity file, bounties, wake-ups` is not achievable without a scope-regrouping that is out of this phase's scope (would need to move bounties+wake-ups out of NAV_SECTIONS_ROLE or merge the two scope panels; both are Phase-72-Plan-03-shape edits, own bounty). Flagged per W1 fix (planning iteration 1) so Alice sees the divergence before executor lands."
    - "claude-session-api.ts wire types for history + handoff deleted (both request and response variants) — the frontend no longer consumes them. Backend still handles the WS types (deliberate: no wire-protocol removal per D-12 scope) — those event/payload TYPES in claude-session-api.ts are the frontend's mirror only, and their frontend consumers are gone"
    - "IdentityModal.coordinator-empty.test.tsx: C2 test case (coordinator-Handoff-tab-empty) is deleted entirely — the Handoff tab no longer exists. C1 (wakeups-coordinator-empty), C3 (identity file tab), C4 (actor identity empty wakeups), C5 (WakeupsTab standalone pill invariant) stay untouched — none reference Handoff. The `deliverHandoff` helper function at L177-185 is deleted (only C2 called it)"
    - "A search after removal for `HistoryTab` and `HandoffTab` in src/ (excluding git history) returns 0 hits (per D-12 strict enforcement)"
    - "Narrow scoped tests pass (highest-signal for the tab-set edit): `npx vitest run src/ui/features/pretty-view/IdentityModal.coordinator-empty.test.tsx src/ui/features/pretty-view/IdentityModal.role-tab.test.tsx --run`; tsc --noEmit clean. The broader 10-file IdentityModal.*.test.tsx sweep is executed OUT-OF-BAND by plan-checker / post-plan review to reduce executor flake-loop cost; narrow gate is the load-bearing verify per W4 fix."
  artifacts:
    - path: "src/ui/features/pretty-view/RunbooksTab.tsx"
      provides: "Runbooks tab body — fetches listRunbooks(hostId, roleName) on mount; renders bare list of slugs; row click fires onOpenRunbook(slug); empty state when list is empty or role is null"
      contains: "RunbooksTab"
      min_lines: 60
    - path: "src/ui/features/pretty-view/IdentityModal.tsx"
      provides: "Updated identity modal with History+Handoff removed and Runbooks tab body added; new onOpenRunbook prop wired through"
      contains: "RunbooksTab"
    - path: "src/ui/api/claude-session-api.ts"
      provides: "History + Handoff wire types removed from frontend surface (backend still handles the WS messages; this is frontend-type-hygiene per D-12 dead-code posture)"
      contains: "// History + Handoff removed"
  key_links:
    - from: "src/ui/features/pretty-view/RunbooksTab.tsx"
      to: "src/ui/api/runbooks-api.ts listRunbooks(hostId, roleName)"
      via: "on-mount fetch via useEffect; state slot `TabState<RunbookEntry[]>` mirrors skills-modal `TabState<SkillEntry[]>` shape at SkillsEditorModal.tsx:80"
      pattern: "listRunbooks"
    - from: "src/ui/features/pretty-view/RunbooksTab.tsx row click"
      to: "props.onOpenRunbook(slug)"
      via: "single call site, fires the swap-not-stack coordination up to Wave 6"
      pattern: "onOpenRunbook\\("
    - from: "src/ui/features/pretty-view/IdentityModal.tsx new `onOpenRunbook` prop"
      to: "Wave 6 PrettyView caller"
      via: "prop is REQUIRED (no default); PrettyView must supply an implementation that closes the identity modal and opens the RunbookEditorModal (D-06 swap-not-stack)"
      pattern: "onOpenRunbook"
---

<objective>
Wave-5 identity modal integration. Three surface changes coordinated in one plan because they must land together (leaving any partially-done makes the modal render broken):

1. **Delete History + Handoff tabs entirely.** Per D-12: tab buttons, tab body components, lazy-load useEffect blocks, save handlers, wire type imports — all removed. Files that ONLY served these tabs (`HistoryTab.tsx`, `HandoffTab.tsx`) are deleted. The frontend wire types in `claude-session-api.ts` for history + handoff are removed (backend WS handlers stay untouched — frontend-only dead-code purge). A grep for `HistoryTab` / `HandoffTab` in `src/` returns 0 hits after this task lands. One test case (C2 in `IdentityModal.coordinator-empty.test.tsx`) is deleted along with its helper function.

2. **Add Runbooks tab body** — new file `RunbooksTab.tsx` renders a bare list of runbook slugs (D-08 through D-11): alphabetical sort, click-to-open, empty state with the D-10 exact copy ("This role has no runbooks yet."), always rendered even for roles with no runbooks (D-11). Row click fires `onOpenRunbook(runbookName)` prop up.

3. **Add Runbooks tab entry to identity modal + wire onOpenRunbook prop** — NAV_SECTIONS_ROLE gains `{value:'runbooks', ...}` as the SECOND entry (between Role file and Bounties). D-13's spelling "role file, runbooks, identity file, bounties, wakeups" is a cross-scope flat order that is not achievable without a scope-regrouping outside this phase's scope (bounties + role-wakeups actually live in NAV_SECTIONS_ROLE, not NAV_SECTIONS_IDENTITY as D-13 implies; a Phase-72-Plan-03 refactor would be needed to interleave identity-file between role-scope tabs — own bounty). Runbooks stays a role-scope tab (data lives at `~/.claude/roles/<role>/runbooks/`); position within NAV_SECTIONS_ROLE places it as the second tab, honoring D-13's spirit (Runbooks second, peer-of-Role-file). IdentityModal gains a new `onOpenRunbook: (runbookName: string) => void` required prop, threaded down to `<RunbooksTab onOpenRunbook={onOpenRunbook} ...>`. Wave 6 wires the prop from PrettyView with the swap-not-stack coordination (D-06). Divergence from D-13 literal spelling is flagged in `must_haves.truths` and surfaced via CHECKPOINT at planning iteration 1 for Alice review.

Purpose: after this plan lands, the modal renders with the new tab structure. The Runbooks tab body renders and clicking a row calls the parent's `onOpenRunbook` — but the parent (PrettyView) doesn't yet supply an implementation that opens the runbook editor. That's Wave 6. As of this plan, `onOpenRunbook` is a required prop; any existing test render that omits it MUST be updated to supply a stub (usually `() => {}` or `vi.fn()`).

Output:
- `src/ui/features/pretty-view/HistoryTab.tsx` — DELETED (`git rm` semantics).
- `src/ui/features/pretty-view/HandoffTab.tsx` — DELETED.
- `src/ui/features/pretty-view/RunbooksTab.tsx` — NEW, ~80-150 lines.
- `src/ui/features/pretty-view/IdentityModal.tsx` — MODIFIED: imports pruned; NAV_SECTIONS_ROLE reshaped; NAV_SECTIONS_IDENTITY loses handoff; state slots removed; effects/save handlers removed; TabsContent removed for history+handoff, added for runbooks; new `onOpenRunbook` prop threaded through; BookOpen icon imported.
- `src/ui/api/claude-session-api.ts` — MODIFIED: history + handoff wire types deleted (both request payload + response event types + the union-type entries).
- `src/ui/features/pretty-view/IdentityModal.coordinator-empty.test.tsx` — MODIFIED: C2 test + `deliverHandoff` helper deleted; C1, C3, C4, C5 untouched; test-render calls to `<IdentityModal>` updated to include `onOpenRunbook={vi.fn()}`.
- Scoped tests pass. `npx tsc --noEmit` clean.

Ship discipline: scoped tests only; no `git push`, no `docker build`, no `docker cp`, no `docker compose up`. Per D-21 + role standing directive.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/89-identity-modal-drop-history-handoff-tabs-add-runbooks-tab-ed/89-CONTEXT.md
@.planning/shapes/shape-identity-modal-tab-restructure.md
@src/ui/features/pretty-view/IdentityModal.tsx
@src/ui/features/pretty-view/HistoryTab.tsx
@src/ui/features/pretty-view/HandoffTab.tsx
@src/ui/features/pretty-view/RoleFileTab.tsx
@src/ui/features/pretty-view/IdentityModal.coordinator-empty.test.tsx
@src/ui/api/claude-session-api.ts
@src/ui/api/runbooks-api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/ui/features/pretty-view/RunbooksTab.tsx — bare list body per D-08 through D-11</name>
  <files>src/ui/features/pretty-view/RunbooksTab.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/RoleFileTab.tsx — a nearby tab body pattern to mirror (styling tokens, loading skeleton, error branch, container div classes). Not a byte-shape mirror — this task's shape is much simpler (bare list, not markdown editor) — but the tab-body shape (loading / error / ready branches; muted-text captions; centered empty-state) matches the shape a tab body must have per Alice's identity-modal visual conventions.
    - src/ui/api/runbooks-api.ts (from Plan 89-03) — the `listRunbooks(hostId, roleName): Promise<RunbookEntry[]>` helper this component consumes. `RunbookEntry = {name: string}`.
    - src/ui/features/pretty-view/IdentityFileTab.tsx — verify the `TabState<T>` type export shape (imported at IdentityModal.tsx L123). This component's state slot uses the same shape: `TabState<RunbookEntry[]>`.
    - src/ui/features/pretty-view/HandoffTab.tsx — the file being deleted, but its `Skeleton` import + "loading/error/empty" branch pattern is a useful reference; this task's file uses the same shape lighter.
    - .planning/phases/89-identity-modal-drop-history-handoff-tabs-add-runbooks-tab-ed/89-CONTEXT.md § D-08 through D-11 (Runbooks tab body decisions) + § shape § Shape #2 (Runbooks tab body inside the identity modal).
    - .planning/shapes/shape-identity-modal-tab-restructure.md § Philosophy "Runbooks tab is a bare list. No peek info in v1." + § Philosophy "The Runbooks tab row shape does NOT try to be its own micro-editor" + § What would make it wrong (no inline expand, no hover preview, no per-row menu).
  </read_first>
  <action>
    Create ONE new file `src/ui/features/pretty-view/RunbooksTab.tsx` — the bare-list Runbooks tab body.

    ─────────────────────────────────────────────────────────────
    IMPORTS
    ─────────────────────────────────────────────────────────────

    - React hooks: `useEffect, useState`.
    - Skeleton loading shape: `Skeleton` from `@/components/skeleton` (mirrors HandoffTab.tsx / HistoryTab.tsx import posture).
    - Runbooks API: `listRunbooks, type RunbookEntry` from `@/api/runbooks-api`.
    - Type: `TabState` from `./IdentityFileTab` (matches IdentityModal.tsx L123 posture).

    ─────────────────────────────────────────────────────────────
    COMPONENT SIGNATURE + PROPS
    ─────────────────────────────────────────────────────────────

    ```
    export interface RunbooksTabProps {
      /** SSH host id — inherited from the identity modal upstream. */
      hostId: number;
      /** Role slug for the loaded identity — sourced from identity.role (may be null). */
      roleName: string | null;
      /** Called when a runbook row is clicked. Fires the swap-not-stack coordination
       *  (D-06) up to PrettyView (Wave 6): PrettyView closes the identity modal and
       *  opens RunbookEditorModal for {roleName, runbookName}. */
      onOpenRunbook: (runbookName: string) => void;
    }

    export function RunbooksTab({ hostId, roleName, onOpenRunbook }: RunbooksTabProps): JSX.Element {
      const [runbooks, setRunbooks] = useState<TabState<RunbookEntry[]>>({ status: "loading" });
      ...
    }
    ```

    ─────────────────────────────────────────────────────────────
    FETCH EFFECT
    ─────────────────────────────────────────────────────────────

    Fetches on mount + when hostId/roleName change:

    ```
    useEffect(() => {
      // D-11: tab is always rendered, even when the role has no runbooks folder.
      // If the identity has no role resolved (identity.role is null), skip the
      // fetch and immediately show the empty state — same D-10 copy per empty
      // handling ("This role has no runbooks yet.").
      if (roleName === null) {
        setRunbooks({ status: "ready", data: [] });
        return;
      }
      let cancelled = false;
      setRunbooks({ status: "loading" });
      console.debug("[RunbooksTab] fetch", { hostId, roleName });
      listRunbooks(hostId, roleName)
        .then((entries) => {
          if (cancelled) return;
          console.debug("[RunbooksTab] fetch-ready", { count: entries.length });
          setRunbooks({ status: "ready", data: entries });
        })
        .catch((err: unknown) => {
          if (cancelled) return;
          console.debug("[RunbooksTab] fetch-error", { err });
          setRunbooks({
            status: "error",
            error: err instanceof Error ? err.message : "Failed to load runbooks",
          });
        });
      return () => {
        cancelled = true;
      };
    }, [hostId, roleName]);
    ```

    ─────────────────────────────────────────────────────────────
    RENDER — layered branches
    ─────────────────────────────────────────────────────────────

    Four branches:

    (1) Loading: three Skeleton placeholder rows (same shape as HandoffTab.tsx loading branch — reuse Skeleton with heights matching row height, ~40px):
        ```
        if (runbooks.status === "loading") {
          return (
            <div className="flex flex-col gap-2">
              <Skeleton className="h-10 w-full rounded-md" />
              <Skeleton className="h-10 w-full rounded-md" />
              <Skeleton className="h-10 w-full rounded-md" />
            </div>
          );
        }
        ```

    (2) Error: muted-red text with the error message:
        ```
        if (runbooks.status === "error") {
          return (
            <div className="text-sm text-red-400">
              Couldn't load runbooks: {runbooks.error}
            </div>
          );
        }
        ```

    (3) Empty state per D-10 (role has no runbooks folder OR folder is empty OR roleName is null):
        ```
        if (runbooks.data.length === 0) {
          return (
            <div className="flex flex-col items-center justify-center gap-2 text-sm text-[var(--color-pv-fg-muted)] py-8">
              This role has no runbooks yet.
            </div>
          );
        }
        ```
        Copy MUST match D-10 exactly: "This role has no runbooks yet." No affordance to create one (creation out of scope per D-10; runbooks are folder-writes-by-hand or by id/role skills).

    (4) List: alphabetically sorted rows per D-09. Row is a click-to-open button; button IS the only interaction per D-08 (no per-row peek, no hover preview, no expand-in-place):
        ```
        // D-09: alphabetically sorted (backend already sorts via `find | sort`,
        // but sort locally too as a defense against wire-order-drift).
        const sorted = [...runbooks.data].sort((a, b) => a.name.localeCompare(b.name));
        return (
          <div className="flex flex-col gap-1.5">
            {sorted.map((entry) => (
              <button
                key={entry.name}
                type="button"
                onClick={() => {
                  console.debug("[RunbooksTab] row-click", { name: entry.name });
                  onOpenRunbook(entry.name);
                }}
                className="text-left px-3 py-2 rounded-md bg-white/5 hover:bg-white/10 border border-white/10 text-[#e8e4d8] text-sm cursor-pointer transition-colors"
              >
                {entry.name}
              </button>
            ))}
          </div>
        );
        ```

    ─────────────────────────────────────────────────────────────
    ANTI-PATTERNS TO AVOID
    ─────────────────────────────────────────────────────────────

    - Do NOT add per-row peek info (mtime, file count, first-line preview). D-08 explicitly says NO peek in v1 + shape § Philosophy "Bare list, no peek info" + § Deferred § Per-row peek info.
    - Do NOT add hover preview / inline expand / per-row action menu. Row click is the ONLY interaction per D-08 + shape § What would make it wrong "The Runbooks tab becoming a micro-editor" + § Philosophy "row shape does NOT try to be its own micro-editor".
    - Do NOT add an "add runbook" button. Creation is out of scope for v1 (D-10 + shape § Out § Runbook creation via the modal).
    - Do NOT sort by recency or pinning. Alphabetical only per D-09.
    - Do NOT gate the tab render on `roleName !== null`. Tab always renders per D-11; the null role case flows through to the D-10 empty state (also the wanted UX for coord identities that have no role).
    - Do NOT add a "recently-edited" section. Feature creep per shape § Tempting-but-no.
    - Do NOT default `onOpenRunbook` to a no-op. The prop is REQUIRED — caller MUST supply an implementation. TypeScript will enforce this via the prop interface.
    - Do NOT `git push`. Do NOT `docker` anything. Scoped test only.
  </action>
  <acceptance_criteria>
    RunbooksTab.tsx is complete when every check below holds:

    Grep source assertions on `src/ui/features/pretty-view/RunbooksTab.tsx`:
    - `grep -c 'Phase 89' src/ui/features/pretty-view/RunbooksTab.tsx` returns >= 1 (file header anchor).
    - `grep -c '^export function RunbooksTab\|^export default function RunbooksTab' src/ui/features/pretty-view/RunbooksTab.tsx` returns 1 (named or default export — either is fine; consumer imports whichever shape it uses).
    - `grep -c 'listRunbooks' src/ui/features/pretty-view/RunbooksTab.tsx` returns >= 2 (import + useEffect call).
    - `grep -c 'from "@/api/runbooks-api"' src/ui/features/pretty-view/RunbooksTab.tsx` returns 1 (single import block).
    - `grep -c 'onOpenRunbook' src/ui/features/pretty-view/RunbooksTab.tsx` returns >= 3 (prop interface + destructure + JSX handler).
    - `grep -c 'This role has no runbooks yet' src/ui/features/pretty-view/RunbooksTab.tsx` returns 1 (D-10 exact copy).
    - `grep -c 'localeCompare' src/ui/features/pretty-view/RunbooksTab.tsx` returns 1 (D-09 alphabetical sort).
    - `grep -c "\[RunbooksTab\]" src/ui/features/pretty-view/RunbooksTab.tsx` returns >= 3 (fetch / fetch-ready / row-click debug logs per role standing directive log-first diagnostics).
    - `grep -c 'Skeleton' src/ui/features/pretty-view/RunbooksTab.tsx` returns >= 2 (import + loading branch).
    - `grep -ci 'preview\|hover.*expand\|mtime.*row\|peek' src/ui/features/pretty-view/RunbooksTab.tsx` returns 0 (no per-row peek info, no expand-in-place — D-08 + shape § Philosophy).

    Structural / build-level:
    - `wc -l src/ui/features/pretty-view/RunbooksTab.tsx` returns >= 60 (small file — bare list, minimal branching).
    - `npx tsc --noEmit` completes with no new errors mentioning RunbooksTab.tsx (this alone won't be clean until Task 2 lands — the IdentityModal changes; run tsc at the END of Task 2, not this task).
  </acceptance_criteria>
  <verify>
    <automated>set -e; [ "$(grep -c 'Phase 89' src/ui/features/pretty-view/RunbooksTab.tsx)" -ge "1" ] && [ "$(grep -cE '^export (default )?function RunbooksTab' src/ui/features/pretty-view/RunbooksTab.tsx)" = "1" ] && [ "$(grep -c 'listRunbooks' src/ui/features/pretty-view/RunbooksTab.tsx)" -ge "2" ] && [ "$(grep -c 'from "@/api/runbooks-api"' src/ui/features/pretty-view/RunbooksTab.tsx)" = "1" ] && [ "$(grep -c 'onOpenRunbook' src/ui/features/pretty-view/RunbooksTab.tsx)" -ge "3" ] && [ "$(grep -c 'This role has no runbooks yet' src/ui/features/pretty-view/RunbooksTab.tsx)" = "1" ] && [ "$(grep -c 'localeCompare' src/ui/features/pretty-view/RunbooksTab.tsx)" = "1" ] && [ "$(grep -c '\[RunbooksTab\]' src/ui/features/pretty-view/RunbooksTab.tsx)" -ge "3" ] && [ "$(grep -c 'Skeleton' src/ui/features/pretty-view/RunbooksTab.tsx)" -ge "2" ] && [ "$(grep -ci 'preview\|hover.*expand\|mtime.*row\|peek' src/ui/features/pretty-view/RunbooksTab.tsx)" = "0" ] && [ "$(wc -l < src/ui/features/pretty-view/RunbooksTab.tsx)" -ge "60" ]</automated>
  </verify>
  <done>
    - `src/ui/features/pretty-view/RunbooksTab.tsx` exists at ~60-150 lines.
    - Exports `RunbooksTab` component with three props: hostId, roleName (string | null), onOpenRunbook.
    - Fetches via listRunbooks on mount + dep change; handles roleName === null by fast-path-empty.
    - Four render branches: loading / error / empty (D-10 exact copy) / list.
    - List rows sorted alphabetically; row click fires onOpenRunbook(name); NO per-row peek info.
    - Structured debug logs at fetch / fetch-ready / row-click boundaries.
    - No git push, no docker command.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update src/ui/features/pretty-view/IdentityModal.tsx — remove History+Handoff, add Runbooks tab, thread onOpenRunbook prop; delete HistoryTab.tsx + HandoffTab.tsx; prune claude-session-api.ts wire types; update coord-empty test</name>
  <files>src/ui/features/pretty-view/IdentityModal.tsx, src/ui/features/pretty-view/HistoryTab.tsx, src/ui/features/pretty-view/HandoffTab.tsx, src/ui/api/claude-session-api.ts, src/ui/features/pretty-view/IdentityModal.coordinator-empty.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/IdentityModal.tsx — the ENTIRE 2664-line file MUST be scanned but only specific line-ranges edited. Priority sections: L1-140 (imports — L6 lucide-react icons, L57-58/L61-62/L84-87 history/handoff wire type imports, L125/L127 HistoryTab/HandoffTab component imports, L131 TelegramTab), L198-278 (props destructure — a new `onOpenRunbook` prop needs to slot in here), L340-361 (NAV_SECTIONS_ROLE + NAV_SECTIONS_IDENTITY definitions), L363-389 (state slot declarations including historyState L371 + handoffState L380), L396-594 (fetch-on-open effect — history fetch L545-553 + handoff fetch L576-583), L1020-1068 (save handlers — updateHistory L1033-1051 + updateHandoff L1053-1068), L2213-2659 (Tabs render block — the two TabsContent blocks for history L2541-2547 + handoff L2589-2598 both need to go; a new TabsContent for runbooks slots in among role/identity/bounties/wakeups).
    - src/ui/features/pretty-view/HistoryTab.tsx (214 lines) — the file being deleted. Skim to verify no non-IdentityModal consumers exist (`grep -rln HistoryTab src/` should show only this file + IdentityModal.tsx).
    - src/ui/features/pretty-view/HandoffTab.tsx (200 lines) — likewise.
    - src/ui/api/claude-session-api.ts L57-58/L61-62/L84-87 (import list — same lines as consumed by IdentityModal.tsx) and L400-405 (union type entries — IdentityHistoryEvent + IdentityHandoffEvent + IdentityHistoryUpdatedEvent + IdentityHandoffUpdatedEvent may appear in a discriminated-union export) and L636-680 + L895-930 (the per-type export definitions themselves).
    - src/ui/features/pretty-view/IdentityModal.coordinator-empty.test.tsx L1-30 (file header + C1-C5 test-manifest comment — C2 is the Handoff coord test being deleted; C1/C3/C4/C5 stay), L175-185 (`deliverHandoff` helper — deleted along with C2), and the C2 test body (grep for "C2:" inside the file to find its exact line range — it's the coordinator-Handoff-tab test).
    - src/ui/features/pretty-view/RunbooksTab.tsx (from Task 1) — verify import + prop shape: `<RunbooksTab hostId={hostId} roleName={identity.role} onOpenRunbook={onOpenRunbook} />`.
    - lucide-react icon inventory — grep the current L6 import for icons currently used elsewhere in IdentityModal.tsx (`Clock` at L351 is History tab only; `Handshake` at L357 is Handoff tab only). Both can be removed from L6 if no other in-file consumer exists (search: `grep -n '\bClock\b\|\bHandshake\b' src/ui/features/pretty-view/IdentityModal.tsx`). Add `BookOpen` (semantic fit for "runbook") to the import list.
    - .planning/phases/89-identity-modal-drop-history-handoff-tabs-add-runbooks-tab-ed/89-CONTEXT.md § D-12 (removal scope — file deletion posture) + § D-13 (post-restructure tab order).
    - .planning/shapes/shape-identity-modal-tab-restructure.md § Shape #1 (Identity modal — tab set change) + § What would make it wrong "The identity modal keeping History or Handoff tab machinery after the removal. If the tab button is gone but the underlying data-fetch effect still fires on modal open, that's dead code and a subtle latency cost."
  </read_first>
  <action>
    Five coordinated edits — one for each file. Group as one task because they must land atomically (leaving any partial mid-state makes tsc fail and/or the modal render broken).

    ─────────────────────────────────────────────────────────────
    EDIT A — DELETE src/ui/features/pretty-view/HistoryTab.tsx
    ─────────────────────────────────────────────────────────────

    Use `git rm src/ui/features/pretty-view/HistoryTab.tsx` (or Write tool with an empty file plus explicit deletion — but git rm is the correct motion for a deleted file; the executor uses whatever the environment supports. If git rm is unavailable, use bash `rm` and let git status pick it up on the eventual commit).

    ─────────────────────────────────────────────────────────────
    EDIT B — DELETE src/ui/features/pretty-view/HandoffTab.tsx
    ─────────────────────────────────────────────────────────────

    Same motion as Edit A.

    ─────────────────────────────────────────────────────────────
    EDIT C — src/ui/features/pretty-view/IdentityModal.tsx: 12 sub-edits
    ─────────────────────────────────────────────────────────────

    (C.1) L6 lucide-react import — remove `Clock, Handshake,` from the destructure list. Add `BookOpen,` in alphabetical order. Post-edit L6 reads approximately: `import { AlarmClock, BookOpen, Pencil, Send, Target, User, Users, X } from "lucide-react";`. Preserve any icons genuinely used elsewhere (grep-verify before removing).

    (C.2) L57-58, L61-62 (per-artifact fetch payload/event type imports): remove the four lines `type IdentityGetHistoryPayload,`, `type IdentityHistoryEvent,`, `type IdentityGetHandoffPayload,`, `type IdentityHandoffEvent,` from the multi-line import block starting at ~L44. Preserve every other imported type (each is used by the surviving Bounties/RoleFile/Wakeups/IdentityFile fetches).

    (C.3) L84-87 (per-artifact update payload/event type imports): remove `type IdentityUpdateHistoryPayload,`, `type IdentityHistoryUpdatedEvent,`, `type IdentityUpdateHandoffPayload,`, `type IdentityHandoffUpdatedEvent,` from the same import block.

    (C.4) L125, L127: remove `import { HistoryTab } from "./HistoryTab";` and `import { HandoffTab } from "./HandoffTab";` — both single-line imports; delete both lines and any trailing blank-line collapse if adjacent.

    (C.5) Props destructure at L198-218 gains new prop `onOpenRunbook: (runbookName: string) => void`. Insert into the props object destructure (matches the shape at L198-207) AND the props type-annotation block (matches L207-217). Place after `container?: HTMLElement | null;` and before the closing `}) {`. Precede the type declaration with a one-line JSDoc: `/** Phase 89 Plan 05: fired when a Runbooks tab row is clicked. PrettyView owns the swap-not-stack coordination — closes this identity modal and opens RunbookEditorModal for {roleName: identity.role, runbookName}. Wave 6 (PrettyView) supplies the impl; there is no fail-closed default because the Runbooks tab is always rendered (D-11) and any missing impl would fail loudly. */`

    (C.6) NAV_SECTIONS_ROLE at L348-353: remove the `{ value: "history", label: "History", Icon: Clock }` entry (L351). Insert new `{ value: "runbooks", label: "Runbooks", Icon: BookOpen }` entry immediately AFTER the `{ value: "role", label: "Role file", Icon: Users }` entry at L349 and BEFORE `{ value: "bounties", ... }` at L350. Post-edit NAV_SECTIONS_ROLE ORDER: role file, runbooks (new), bounties, role-wakeups.

    NOTE ON TAB ORDER — SHAPE DIVERGENCE FLAGGED PER W1 FIX (planning iteration 1 checkpoint):

    D-13 spelling: "role file, runbooks (new), identity file, bounties, wake-ups" — this is the shape file's stated VISIBLE tab order.

    Source-truth constraint (verified from src/ui/features/pretty-view/IdentityModal.tsx L348-360 pre-edit): the identity modal splits tabs across two scope panels via a top segmented Role/Identity switch (Phase 72 Plan 03). NAV_SECTIONS_ROLE = [role, bounties, history, role-wakeups]; NAV_SECTIONS_IDENTITY = [identity, identity-wakeups, handoff, telegram]. Bounties AND Wake-ups (role-wakeups) live under NAV_SECTIONS_ROLE — NOT identity-scope as D-13's flat order would imply.

    Consequence: the D-13 visible order "role file, runbooks, identity file, bounties, wake-ups" is NOT achievable inside NAV_SECTIONS_ROLE alone — it requires interleaving identity-file (which lives under NAV_SECTIONS_IDENTITY) between role-scope tabs. Achieving it would require a scope-regrouping (moving bounties + role-wakeups out of NAV_SECTIONS_ROLE, or merging the two scope panels into one flat nav) — that is OUT OF SCOPE for this phase (touches Phase 72 Plan 03 structure; would need its own bounty).

    Alternate fix applied per W1 (checkpoint iteration 1 protocol): Runbooks lands as the SECOND entry in NAV_SECTIONS_ROLE (between Role file and Bounties), preserving Runbooks as a role-scope tab and honoring the SPIRIT of D-13 (Runbooks is the second role-scope tab, peer-of-Role-file) even though the LITERAL flat order across both scopes cannot be produced. See <verification> note + the new must_haves.truths flag surfaced to Alice at the plan-checker step.

    (C.7) NAV_SECTIONS_IDENTITY at L354-360: remove the `{ value: "handoff", label: "Handoff", Icon: Handshake }` entry (L357). Preserve `{ value: "identity", ... }`, `{ value: "identity-wakeups", ... }`, `{ value: "telegram", ... }`.

    (C.8) L371 historyState + L380 handoffState `useState` declarations: DELETE both lines. Delete any comment lines directly above them explaining phase / patch context (they're now orphan comments).

    (C.9) Reset-on-open effect at L396-424: remove `setHistoryState({ status: "loading" });` (L416) and `setHandoffState({ status: "loading" });` (L419).

    (C.10) Fetch-on-open effect at L446-594: remove the two `openOneShot` blocks — L545-553 (`identity:get-history` → historyState) and L576-583 (`identity:get-handoff` → handoffState). Preserve all six other openOneShot blocks (bounties, identity-file, role-file, identity-wakeups, role-wakeups, and any Telegram parallel fetch).

    (C.11) Save handlers at L1033-1068: delete `updateHistory` (L1033-1051) and `updateHandoff` (L1053-1068) entirely. These functions have no non-history/handoff consumers.

    (C.12) Tabs render block at L2213-2659: remove `<TabsContent value="history">...</TabsContent>` (L2541-2547) and `<TabsContent value="handoff">...</TabsContent>` (L2589-2598). Insert new `<TabsContent value="runbooks">...</TabsContent>` block IMMEDIATELY AFTER the `<TabsContent value="role">` block (L2222-2227) and BEFORE the `<TabsContent value="identity">` block (L2233-2239). Content of the new block:

      {/* Phase 89 Plan 05: Runbooks tab (role scope, always rendered per D-11).
          Sits inside NAV_SECTIONS_ROLE between Role file and Bounties.
          D-13's flat spelling "role file, runbooks, identity file, bounties,
          wake-ups" implies an across-scope visible order that is not
          achievable without a scope-regrouping outside this phase's scope
          (see C.6 note + Plan 89-05 must_haves.truths for the flagged
          discrepancy). Runbooks stays role-scope (data lives at
          ~/.claude/roles/<role>/runbooks/); scope-regrouping to interleave
          identity-file would be its own bounty. Body is the bare-list
          launcher per D-08 through D-11; row-click routes through
          onOpenRunbook to Wave-6 PrettyView which owns the swap-not-stack
          coordination (D-06). */}
      <TabsContent
        value="runbooks"
        className="flex-1 min-h-0 overflow-y-auto px-6 py-4"
      >
        <RunbooksTab
          hostId={hostId}
          roleName={identity.role}
          onOpenRunbook={onOpenRunbook}
        />
      </TabsContent>

    Add the `import { RunbooksTab } from "./RunbooksTab";` line alongside the existing tab-body imports (near L123-127; put it after RoleFileTab and before WakeupsTab since it's role-scope).

    (C.13) Historical comment block at L142-143 (per W-02 fix — iteration 2 checkpoint):

    Pre-edit L138-143 reads (approximately):

      // Patch #87: tabbed near-fullscreen modal for the identity's bounties.
      // Patch #17g: renamed Standing Directives → Identity; promoted Identity to
      //   position 1 + default active tab; parallel fetch of 4 new artifacts
      //   (identity file, history, wakeups, handoff) on modal open; tab renderers
      //   extracted to sibling files (IdentityFileTab / HistoryTab / WakeupsTab /
      //   HandoffTab). Bounties tab structure and patch #87 attribution preserved.

    The L141 "parallel fetch of 4 new artifacts (identity file, history, wakeups, handoff)" and L142-143 "extracted to sibling files (IdentityFileTab / HistoryTab / WakeupsTab / HandoffTab)" name history + handoff in prose. Per D-12 dead-code posture + the narrowed B-02 grep gate, these prose mentions must go.

    Rewrite the L141-143 comment lines so history + handoff are gone (do NOT touch L138 patch #87 attribution or L144-146 patch #92 attribution — those remain unchanged). Post-edit L141-143 reads (approximately):

      // Patch #17g: renamed Standing Directives → Identity; promoted Identity to
      //   position 1 + default active tab; parallel fetch of artifacts (identity
      //   file, wakeups) on modal open; tab renderers extracted to sibling files
      //   (IdentityFileTab / WakeupsTab / RunbooksTab). Bounties tab structure
      //   and patch #87 attribution preserved. Phase 89 Plan 05: History + Handoff
      //   tab surface removed per D-12; Runbooks tab added per D-08 through D-13.

    Preserve the leading `//` + 3-space hanging-indent shape of the existing comment. The trailing Phase 89 attribution line is a single new "Phase 89 Plan 05" bread-crumb that captures WHY the comment changed (matches the file's pattern of "Patch #NN: ..." per-change annotations).

    ─────────────────────────────────────────────────────────────
    EDIT D — src/ui/api/claude-session-api.ts: remove history + handoff frontend wire types
    ─────────────────────────────────────────────────────────────

    (D.1) Delete the four per-type export declarations (line ranges approximate — grep-locate before editing):
      - `IdentityGetHistoryPayload` (L636-644 area) — the request payload for `identity:get-history`.
      - `IdentityHistoryEvent` (L646 area) — the response event `identity:history`.
      - `IdentityGetHandoffPayload` (L668-673 area) — the request payload for `identity:get-handoff`.
      - `IdentityHandoffEvent` (L674 area) — the response event `identity:handoff`.

    (D.2) Delete the four per-type export declarations for the update flow:
      - `IdentityUpdateHistoryPayload` (L896-902 area).
      - `IdentityHistoryUpdatedEvent` (L907-913 area).
      - `IdentityUpdateHandoffPayload` (L916-922 area).
      - `IdentityHandoffUpdatedEvent` (L924-928 area).

    (D.3) At any discriminated-union export that lists event types (grep for `IdentityHistoryEvent` in the file — you'll find union entries around L401/L403 like `| IdentityHistoryEvent | IdentityHandoffEvent`), remove the two entries from the union.

    (D.4) Delete or reword the comment lines at L588 / L590 / L593 / L594 / L598 / L600 / L603 / L604 in the header docblock that document these wire types. If the docblock has a natural spot, replace the removed lines with a single-line note: `// History + Handoff wire types removed 2026-09-08 per Phase 89 Plan 05 D-12 — frontend consumers deleted; backend WS handlers still handle these types for now (deliberate: no backend removal per D-12 scope).`

    (D.5) DO NOT delete other types that share the same import line or same section. Skim the surrounding shape carefully — this file has ~1000 lines and is dense; keep every other Identity* type intact.

    ─────────────────────────────────────────────────────────────
    EDIT E — src/ui/features/pretty-view/IdentityModal.coordinator-empty.test.tsx: delete C2 + helper; update `<IdentityModal>` renders to include onOpenRunbook stub
    ─────────────────────────────────────────────────────────────

    (E.1) Delete the entire C2 test — grep for `it("C2` or `describe("C2` (the test title anchors will reveal the exact syntax). The test body spans from its opening `it(...)` call to the matching close-paren + brace. Delete cleanly with no stray blank lines or commas.

    (E.2) Delete the `deliverHandoff` helper function at L177-185.

    (E.3) In the file-header comment block at L11-15, remove the C2 line entry so the comment reflects the new set (C1, C3, C4, C5 only).

    (E.4) Find every `render(<IdentityModal ... />)` call in the file. Each MUST supply the new required `onOpenRunbook={vi.fn()}` prop. Add it inline. If the file uses a helper factory to create the modal element, update the factory to accept + forward the prop.

    (E.5) Do NOT touch the C1, C3, C4, or C5 tests. They test wakeups, identity-file-tab, and standalone WakeupsTab respectively — none reference Handoff.

    ─────────────────────────────────────────────────────────────
    OTHER TEST-FILE RENDER-CALL SWEEP
    ─────────────────────────────────────────────────────────────

    Every existing IdentityModal test file (`IdentityModal.test.tsx`, `IdentityModal.role-tab.test.tsx`, `IdentityModal.scope-switch.test.tsx`, `IdentityModal.wakeup-crud.test.tsx`, `IdentityModal.stays-awake.test.tsx`, `IdentityModal.inherit-override.test.tsx`, `IdentityModal.bounties-filter.test.tsx`, `IdentityModal.lazy-archive.test.tsx`, `IdentityModal.voice.test.tsx`) that renders `<IdentityModal>` MUST have its render call updated to include `onOpenRunbook={vi.fn()}` (or equivalent stub). Grep all these files for `<IdentityModal` and add the new prop.

    Do NOT modify any other test assertion — only add the required prop. If any test explicitly asserts against History or Handoff tabs (grep each file), it's dead — delete the assertion (and the test if the entire test is about a deleted tab). Grep for `"history"\|"handoff"\|HistoryTab\|HandoffTab` inside each IdentityModal.*.test.tsx and delete matches AS NEEDED (most likely there are none since the tests are scoped to other tabs).

    ─────────────────────────────────────────────────────────────
    ANTI-PATTERNS TO AVOID
    ─────────────────────────────────────────────────────────────

    - Do NOT leave the historyState/handoffState useState calls in the code with a "// TODO remove" comment. D-12 requires DELETE. Any surviving reference is dead code per shape § What would make it wrong.
    - Do NOT keep the frontend wire types in claude-session-api.ts "for future use". D-12 explicitly says frontend surface must be clean; backend types stay.
    - Do NOT delete the backend WS handlers for `identity:get-history`, `identity:get-handoff`, `identity:update-history`, `identity:update-handoff`. Backend removal is OUT of scope for this phase per D-12 wording ("frontend removal"). Leaving the backend handlers untouched is intentional.
    - Do NOT change the Bounties / Role-file / Wakeups / Identity-file / Telegram tab bodies or state slots. Only History and Handoff go.
    - Do NOT reorder the Bounties / Wakeups / Identity-file tabs. Only insert Runbooks and remove History + Handoff.
    - Do NOT rename `activeTab` state to something new. The tab-key strings `role`/`identity`/`bounties`/`identity-wakeups`/`role-wakeups`/`telegram` are all preserved; `runbooks` is added; `history` and `handoff` are removed. The `useEffect` at L271-276 that resets `activeTab` on scope flip still works with the new set (`role` remains the role-scope default; `identity` remains the identity-scope default).
    - Do NOT add a default value for `onOpenRunbook` in the prop destructure. TypeScript enforces the required-prop contract; a default would let PrettyView forget to wire it without a compile error.
    - Do NOT gate the Runbooks TabsContent render on `identity.role !== null`. Always rendered per D-11; the RunbooksTab component handles the null-role fast-path.
    - Do NOT `git push`. Do NOT `docker build`. Do NOT `docker cp`. Do NOT `docker compose up`. Do NOT run `npx vitest run` full suite. Scoped test only.
  </action>
  <acceptance_criteria>
    Integration is complete when every check below holds:

    File-deletion assertions:
    - `test ! -f src/ui/features/pretty-view/HistoryTab.tsx` (file gone).
    - `test ! -f src/ui/features/pretty-view/HandoffTab.tsx` (file gone).

    Grep source assertions on `src/ui/features/pretty-view/IdentityModal.tsx`:
    - `grep -c 'HistoryTab\|HandoffTab' src/ui/features/pretty-view/IdentityModal.tsx` returns 0 (both component names purged from imports + JSX + comments).
    - `grep -c 'historyState\|handoffState' src/ui/features/pretty-view/IdentityModal.tsx` returns 0 (state slots + all setState calls gone).
    - `grep -c 'updateHistory\|updateHandoff' src/ui/features/pretty-view/IdentityModal.tsx` returns 0 (save handlers gone).
    - `grep -c "value=\"history\"\|value=\"handoff\"" src/ui/features/pretty-view/IdentityModal.tsx` returns 0 (TabsContent blocks + NAV_SECTIONS entries gone).
    - `grep -c 'identity:get-history\|identity:get-handoff\|identity:update-history\|identity:update-handoff' src/ui/features/pretty-view/IdentityModal.tsx` returns 0 (wire-type-string usages gone).
    - `grep -c 'onOpenRunbook' src/ui/features/pretty-view/IdentityModal.tsx` returns >= 3 (prop-type + destructure + JSX thread-through to <RunbooksTab>).
    - `grep -c 'RunbooksTab' src/ui/features/pretty-view/IdentityModal.tsx` returns >= 2 (import + JSX).
    - `grep -c "value=\"runbooks\"" src/ui/features/pretty-view/IdentityModal.tsx` returns >= 2 (NAV_SECTIONS_ROLE entry + TabsContent block).
    - `grep -c 'BookOpen' src/ui/features/pretty-view/IdentityModal.tsx` returns >= 2 (icon import + NAV_SECTIONS_ROLE entry).
    - `grep -cE '(^|[^a-zA-Z])(Clock|Handshake)([^a-zA-Z]|$)' src/ui/features/pretty-view/IdentityModal.tsx` returns 0 (bare `Clock`/`Handshake` identifiers gone — icon imports removed and no other in-file consumer). Word-boundary regex prevents false-match on `AlarmClock` (which is preserved at L6/L352/L356 for the wakeups tabs per Edit C.1).
    - `grep -c 'Phase 89' src/ui/features/pretty-view/IdentityModal.tsx` returns >= 2 (comment anchors on the two significant edits — the props change + the TabsContent insert).

    Grep source assertions on `src/ui/api/claude-session-api.ts`:
    - `grep -cE '(export type|export interface) IdentityHistoryEvent\b' src/ui/api/claude-session-api.ts` returns 0.
    - `grep -cE '(export type|export interface) IdentityHandoffEvent\b' src/ui/api/claude-session-api.ts` returns 0.
    - `grep -cE '(export type|export interface) IdentityGetHistoryPayload\b' src/ui/api/claude-session-api.ts` returns 0.
    - `grep -cE '(export type|export interface) IdentityGetHandoffPayload\b' src/ui/api/claude-session-api.ts` returns 0.
    - `grep -cE '(export type|export interface) IdentityUpdateHistoryPayload\b' src/ui/api/claude-session-api.ts` returns 0.
    - `grep -cE '(export type|export interface) IdentityHistoryUpdatedEvent\b' src/ui/api/claude-session-api.ts` returns 0.
    - `grep -cE '(export type|export interface) IdentityUpdateHandoffPayload\b' src/ui/api/claude-session-api.ts` returns 0.
    - `grep -cE '(export type|export interface) IdentityHandoffUpdatedEvent\b' src/ui/api/claude-session-api.ts` returns 0.
    - `grep -c 'Phase 89 Plan 05' src/ui/api/claude-session-api.ts` returns >= 1 (removal-note comment anchor).

    Grep source assertions on `src/ui/features/pretty-view/IdentityModal.coordinator-empty.test.tsx`:
    - `grep -c 'deliverHandoff' src/ui/features/pretty-view/IdentityModal.coordinator-empty.test.tsx` returns 0 (helper deleted).
    - `grep -c 'C2:\|C2 coord' src/ui/features/pretty-view/IdentityModal.coordinator-empty.test.tsx` returns 0 (C2 test removed and comment updated).
    - `grep -c 'C1:\|C3:\|C4:\|C5:' src/ui/features/pretty-view/IdentityModal.coordinator-empty.test.tsx` returns >= 4 (C1/C3/C4/C5 anchors preserved).
    - `grep -c 'onOpenRunbook' src/ui/features/pretty-view/IdentityModal.coordinator-empty.test.tsx` returns >= 1 (test-render supplies stub).

    Global grep (narrowed to src/ui/ per B-02 fix — D-12's removal scope is frontend UI only; backend prose comments in `src/backend/claude-session/` are out of scope; TelegramTab.tsx L10 has a historical annotation "mirror HandoffTab's Skeleton…" that is prose reference not a UI consumer, and excluding it keeps this bounty from dragging TelegramTab into an unrelated cleanup):
    - `grep -rln 'HistoryTab\|HandoffTab' src/ui/ 2>/dev/null | grep -v TelegramTab | wc -l` returns 0 (per D-12 as narrowed: the src/ui/ surface for HistoryTab/HandoffTab is empty after this task lands — HistoryTab.tsx + HandoffTab.tsx deleted, IdentityModal.tsx purged via Edit C, claude-session-api.ts prose-comment refs at L905/L911 go with the deleted IdentityHistoryUpdatedEvent type per Edit D.2).

    Sweep-coverage sanity gate (fast-fail before tsc — per W-03 fix, iteration 2):
    - `grep -l 'IdentityModal' src/ui/features/pretty-view/IdentityModal.*.test.tsx 2>/dev/null | xargs -r grep -L 'onOpenRunbook' | wc -l` returns 0 (every `IdentityModal.*.test.tsx` file that references `IdentityModal` MUST ALSO reference `onOpenRunbook` — proves the required-prop sweep across all 10 test files landed. Fails loudly if any file was missed. Runs BEFORE tsc so the executor sees the specific missing test file(s) named in xargs stderr rather than opaque tsc "missing prop" errors that don't say WHICH render call is stale).

    Structural / build-level:
    - `npx tsc --noEmit` completes with no new errors mentioning IdentityModal.tsx, RunbooksTab.tsx, or claude-session-api.ts.

    Scoped tests exit 0 (this is the primary success gate — proves the modal renders correctly with the new tab structure + prop):
    - Narrow scoped test run exits 0: `npx vitest run src/ui/features/pretty-view/IdentityModal.coordinator-empty.test.tsx src/ui/features/pretty-view/IdentityModal.role-tab.test.tsx --run`. These two are the highest-signal for the tab-set restructure — coordinator-empty exercises the C1/C3/C4/C5 branches that MUST survive the C2 deletion, and role-tab.test.tsx directly covers the tab rendering surface being reshaped. If either fails, the plan is broken.
    - Broader 10-file sweep (below) is NOT a task-verify gate — it runs at plan-checker/post-plan review time to catch adjacent regressions without blocking the executor loop on unrelated flakes: `npx vitest run src/ui/features/pretty-view/IdentityModal.test.tsx src/ui/features/pretty-view/IdentityModal.role-tab.test.tsx src/ui/features/pretty-view/IdentityModal.coordinator-empty.test.tsx src/ui/features/pretty-view/IdentityModal.scope-switch.test.tsx src/ui/features/pretty-view/IdentityModal.wakeup-crud.test.tsx src/ui/features/pretty-view/IdentityModal.stays-awake.test.tsx src/ui/features/pretty-view/IdentityModal.inherit-override.test.tsx src/ui/features/pretty-view/IdentityModal.bounties-filter.test.tsx src/ui/features/pretty-view/IdentityModal.lazy-archive.test.tsx src/ui/features/pretty-view/IdentityModal.voice.test.tsx --run`. Executor MAY run this if they want extra confidence, but a failure in a file the plan did not touch (adjacent flake) does NOT block plan completion — reroute to the sibling flake via git blame / plan-checker triage.
  </acceptance_criteria>
  <verify>
    <automated>set -e; [ ! -f src/ui/features/pretty-view/HistoryTab.tsx ] && [ ! -f src/ui/features/pretty-view/HandoffTab.tsx ] && [ "$(grep -c 'HistoryTab\|HandoffTab' src/ui/features/pretty-view/IdentityModal.tsx)" = "0" ] && [ "$(grep -c 'historyState\|handoffState' src/ui/features/pretty-view/IdentityModal.tsx)" = "0" ] && [ "$(grep -c 'updateHistory\|updateHandoff' src/ui/features/pretty-view/IdentityModal.tsx)" = "0" ] && [ "$(grep -c 'value=\"history\"\|value=\"handoff\"' src/ui/features/pretty-view/IdentityModal.tsx)" = "0" ] && [ "$(grep -c 'identity:get-history\|identity:get-handoff\|identity:update-history\|identity:update-handoff' src/ui/features/pretty-view/IdentityModal.tsx)" = "0" ] && [ "$(grep -c 'onOpenRunbook' src/ui/features/pretty-view/IdentityModal.tsx)" -ge "3" ] && [ "$(grep -c 'RunbooksTab' src/ui/features/pretty-view/IdentityModal.tsx)" -ge "2" ] && [ "$(grep -c 'value=\"runbooks\"' src/ui/features/pretty-view/IdentityModal.tsx)" -ge "2" ] && [ "$(grep -c 'BookOpen' src/ui/features/pretty-view/IdentityModal.tsx)" -ge "2" ] && [ "$(grep -cE '(^|[^a-zA-Z])(Clock|Handshake)([^a-zA-Z]|$)' src/ui/features/pretty-view/IdentityModal.tsx)" = "0" ] && [ "$(grep -cE '(export type|export interface) IdentityHistoryEvent\b' src/ui/api/claude-session-api.ts)" = "0" ] && [ "$(grep -cE '(export type|export interface) IdentityHandoffEvent\b' src/ui/api/claude-session-api.ts)" = "0" ] && [ "$(grep -cE '(export type|export interface) IdentityGetHistoryPayload\b' src/ui/api/claude-session-api.ts)" = "0" ] && [ "$(grep -cE '(export type|export interface) IdentityGetHandoffPayload\b' src/ui/api/claude-session-api.ts)" = "0" ] && [ "$(grep -cE '(export type|export interface) IdentityUpdateHistoryPayload\b' src/ui/api/claude-session-api.ts)" = "0" ] && [ "$(grep -cE '(export type|export interface) IdentityHistoryUpdatedEvent\b' src/ui/api/claude-session-api.ts)" = "0" ] && [ "$(grep -cE '(export type|export interface) IdentityUpdateHandoffPayload\b' src/ui/api/claude-session-api.ts)" = "0" ] && [ "$(grep -cE '(export type|export interface) IdentityHandoffUpdatedEvent\b' src/ui/api/claude-session-api.ts)" = "0" ] && [ "$(grep -c 'deliverHandoff' src/ui/features/pretty-view/IdentityModal.coordinator-empty.test.tsx)" = "0" ] && [ "$(grep -c 'onOpenRunbook' src/ui/features/pretty-view/IdentityModal.coordinator-empty.test.tsx)" -ge "1" ] && [ "$(grep -rln 'HistoryTab\|HandoffTab' src/ui/ 2>/dev/null | grep -v TelegramTab | wc -l)" = "0" ] && [ "$(grep -l 'IdentityModal' src/ui/features/pretty-view/IdentityModal.*.test.tsx 2>/dev/null | xargs -r grep -L 'onOpenRunbook' | wc -l)" = "0" ] && npx tsc --noEmit && npx vitest run src/ui/features/pretty-view/IdentityModal.coordinator-empty.test.tsx src/ui/features/pretty-view/IdentityModal.role-tab.test.tsx --run</automated>
  </verify>
  <done>
    - HistoryTab.tsx + HandoffTab.tsx deleted from disk.
    - IdentityModal.tsx has zero HistoryTab / HandoffTab / historyState / handoffState / updateHistory / updateHandoff / value=history / value=handoff / identity:get-history / identity:get-handoff / identity:update-history / identity:update-handoff references.
    - IdentityModal.tsx has a new `onOpenRunbook: (runbookName: string) => void` required prop, threaded through to `<RunbooksTab onOpenRunbook={...} />` inside a new `<TabsContent value="runbooks">` block.
    - IdentityModal.tsx NAV_SECTIONS_ROLE has 4 entries: role file, runbooks (new), bounties, role-wakeups. NAV_SECTIONS_IDENTITY has 3 entries: identity file, identity-wakeups, telegram.
    - IdentityModal.tsx imports BookOpen from lucide-react; no Clock or Handshake imports remain.
    - claude-session-api.ts has zero exports for the 8 history/handoff types (4 payload + 4 event/updated).
    - IdentityModal.coordinator-empty.test.tsx has C2 test + deliverHandoff helper deleted; C1/C3/C4/C5 preserved; every IdentityModal render call supplies onOpenRunbook={vi.fn()}.
    - Global grep for `HistoryTab` / `HandoffTab` across `src/` returns 0 hits.
    - `npx tsc --noEmit` clean.
    - Narrow scoped vitest run (coordinator-empty + role-tab) exits 0. Broader 10-file sweep is plan-checker-owned per W4 fix — executor is not gated on unrelated adjacent flake.
    - NO git push, NO docker command, NO nginx reload, NO full-suite test run.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| onOpenRunbook prop callback → parent PrettyView | The identity modal fires runbookName up; PrettyView is the trusted coordinator that decides what modal to open. The runbookName was returned from the backend as a valid slug per Wave-2 gates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-05-01 | Elevation of Privilege | Removed dead history/handoff fetches leaking latency | mitigate | D-12 removes the effects entirely; a "tab button gone but effect still fires" is exactly the failure mode shape § What would make it wrong flags. Grep gates for all history/handoff wire strings return 0. |
| T-89-05-02 | Repudiation | Test coverage regression after Handoff C2 deletion | mitigate | C2 tests behavior of a deleted feature; deleting the test alongside the feature is the correct move. C1/C3/C4/C5 preserve every OTHER coord-empty invariant. Scoped test run confirms no regression on adjacent behavior. |
| T-89-05-SC | Tampering | npm installs | accept | No new npm dependencies added. |
</threat_model>

<verification>
- All grep gates pass; HistoryTab.tsx + HandoffTab.tsx files gone; RunbooksTab.tsx exists.
- IdentityModal.tsx has the new onOpenRunbook prop and Runbooks TabsContent + NAV_SECTIONS_ROLE entry.
- Visible tab order after this plan lands: (role scope) role-file, runbooks, bounties, role-wakeups | (identity scope) identity-file, identity-wakeups, telegram. This diverges from D-13's literal spelling ("role file, runbooks, identity file, bounties, wake-ups") because that flat order interleaves scopes; realizing it requires a Phase-72-Plan-03 scope-regrouping outside this phase's scope. Flagged in must_haves.truths and surfaced via `## CHECKPOINT REACHED` at planning-iteration-1 for Alice's awareness.
- claude-session-api.ts frontend surface is clean of history/handoff types.
- Narrow scoped test run (coordinator-empty + role-tab) exits 0. Broader 10-file sweep is deferred to plan-checker per W4 fix.
- `npx tsc --noEmit` clean.
- No git push, no docker command, no nginx reload.
</verification>

<success_criteria>
- Identity modal renders with the new tab structure and no History/Handoff traces.
- Runbooks tab body renders correctly (loading → ready → row click fires onOpenRunbook).
- Runbook editor modal from Wave 4 is UNTRIGGERED by this plan alone — that's Wave 6's coordination job. As of this plan, clicking a Runbooks row calls a required prop that PrettyView will supply in Wave 6; if a stray call site forgot to wire the prop, tsc catches it.
- Ready for Wave 6 (PrettyView mount + integration test) to proceed.
</success_criteria>

<output>
Create `.planning/phases/89-identity-modal-drop-history-handoff-tabs-add-runbooks-tab-ed/89-05-SUMMARY.md` when done.
</output>
