---
phase: 85-cosmetics-migrate-to-role
plan: 03
type: execute
wave: 2
depends_on: [85-01, 85-02]  # 85-01 Task 3 widens createRole client on src/ui/api/identities-api.ts; 85-02 widens the POST /roles endpoint. This plan calls the widened client against the widened endpoint — needs both.
files_modified:
  - src/ui/sidebar/CreateRoleDialog.tsx
autonomous: true
requirements: [D-CTX-85-surface-3, D-CTX-85-empty-not-scenario]
must_haves:
  truths:
    - "CreateRoleDialog renders Title input, ColorPicker, VoicePicker, and avatar generator+carousel controls in addition to the existing Name/Description/Host fields per D-CTX-85-surface-3"
    - "Avatar generator uses seed mapping: name=role name (kebab-case), title=role Title field, brief=role Description, colorHue=role ColorPicker value per D-CTX-85-surface-3"
    - "Submission blocks until Title, Voice, ColorHue AND a picked/uploaded avatar are all set (roles cannot have empty cosmetics per D-CTX-85-empty-not-scenario)"
    - "Successful submit calls widened createRole() with {name, description, hostId, cosmetics} + avatar File, then invokes onChainToCreateIdentity + onCreated as today"
    - "ColorPicker, VoicePicker components are IMPORTED from `@/features/pretty-view/pickers/*` — REUSED, not reimplemented per D-CTX-85-surface-3 LOCKED pattern"
  artifacts:
    - path: src/ui/sidebar/CreateRoleDialog.tsx
      provides: "cosmetic-authoring UI on top of existing role-name/description/host layout; wire to widened createRole() from Plan 85-02"
  key_links:
    - from: src/ui/sidebar/CreateRoleDialog.tsx
      to: '@/features/pretty-view/pickers/ColorPicker'
      via: "import + JSX usage identical to NewSessionDialog L1249-1254"
      pattern: "import.*ColorPicker.*pickers/ColorPicker"
    - from: src/ui/sidebar/CreateRoleDialog.tsx
      to: '@/features/pretty-view/pickers/VoicePicker'
      via: "import + JSX usage identical to NewSessionDialog L1232-1238"
      pattern: "import.*VoicePicker.*pickers/VoicePicker"
    - from: src/ui/sidebar/CreateRoleDialog.tsx handleSubmit
      to: createRole from @/api/identities-api (widened in Plan 85-02)
      via: "call passes {cosmetics: {title, colorHue, voice}} + avatar File"
      pattern: "createRole\\(\\{.*cosmetics.*\\}"
---

<objective>
Grow `CreateRoleDialog.tsx` with the four cosmetic authoring controls locked in D-CTX-85-surface-3: Title input, ColorPicker (reused), VoicePicker (reused), and avatar generator with 3-candidate carousel + manual upload path (inlined per D-CTX-85-surface-3 "Planner's discretion (b) — only one caller remains post-phase; extraction can happen later if a third caller emerges"). Wire the submit handler to the multipart `createRole()` client shipped in Plan 85-02, passing the four cosmetic values as `cosmetics` + the picked avatar as a File. Enforce the D-CTX-85-empty-not-scenario constraint: submission blocks until all four cosmetics are set.

Purpose: Wave-2 frontend delivery of D-CTX-85-surface-3. Depends on Plan 85-02 (createRole client accepts cosmetics + avatar File). NOT dependent on Plan 85-01 (this dialog does not read the Identity type — it writes a role).

Output: `CreateRoleDialog.tsx` with cosmetic controls inserted between Description and Host picker; state for title/voice/hue/avatar; handleSubmit passes cosmetics + File to widened createRole().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/85-cosmetics-migrate-to-role-move-title-hue-voice-avatar-from-i/85-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cosmetic authoring controls + avatar generator to CreateRoleDialog</name>
  <files>src/ui/sidebar/CreateRoleDialog.tsx</files>
  <read_first>
    - src/ui/sidebar/CreateRoleDialog.tsx (entire 419-line file — the shape this task extends)
    - src/ui/sidebar/NewSessionDialog.tsx L313-345 (state pattern for cosmetic fields), L654-704 (handleGenerate + handleManualUpload — inline these into CreateRoleDialog with role-scoped seeds per D-CTX-85-surface-3), L1186-1256 (Title / Voice / Color picker JSX blocks to mirror), L1257-1366 (Avatar section: Generate/Regenerate button + Upload button + candidate carousel + manual preview — the layout to mirror verbatim; the only substantive difference is the seed source: role's own state, not identity-mode's name/title/brief)
    - src/ui/features/pretty-view/pickers/ColorPicker.tsx (48 lines — confirm {value, onChange, disabled?, id?} contract)
    - src/ui/features/pretty-view/pickers/VoicePicker.tsx (123 lines — confirm {value, onChange, disabled?, id?, ariaLabel?} contract)
    - src/ui/api/identities-api.ts L144-171 (postGenerateAvatarBatch signature — accepts {name, title, brief, colorHue}), L173-196 (postManualAvatarCandidate signature)
  </read_first>
  <behavior>
    - Test 1: CreateRoleDialog renders a Title input (labelled `Title`) below the Description textarea, above the Host picker section.
    - Test 2: CreateRoleDialog renders ColorPicker (imported from `@/features/pretty-view/pickers/ColorPicker`) with initial hue value chosen randomly per open (matches NewSessionDialog L320 `Math.floor(Math.random() * 360)`).
    - Test 3: CreateRoleDialog renders VoicePicker (imported from `@/features/pretty-view/pickers/VoicePicker`) with empty initial value and `id="create-role-voice"`.
    - Test 4: CreateRoleDialog renders a `Generate` button (disabled until name+title+description are all non-empty AND colorHue is set) and an `Upload…` button (label-with-sr-only-input pattern matching NewSessionDialog L1279-1305).
    - Test 5: Clicking Generate calls `postGenerateAvatarBatch({name, title, brief: description, colorHue})` — brief field is the role Description (per D-CTX-85-surface-3 "existing role description doubles as the brief for avatar generation").
    - Test 6: On successful generate, three candidate images render in a horizontal row; clicking one sets it as the pickedCandidateId (aria-selected=true on the picked one, false on others).
    - Test 7: Manual upload sets a preview image AND pickedCandidateId to the id returned by `postManualAvatarCandidate`; clears the generated candidates carousel (mutual exclusion per NewSessionDialog L688-698 pattern).
    - Test 8: Create button is DISABLED until name (kebab-case valid) + description (non-empty) + host (picked) + title (non-empty) + voice (non-empty) + colorHue (set — always set since seeded randomly) + pickedCandidateId (non-null) are all satisfied.
    - Test 9: Successful submit calls `createRole({name, description, hostId, cosmetics: {title, colorHue, voice}}, avatarFile)` where `avatarFile` is a File object derived from either the picked generated candidate (fetched from candidate URL and converted to File) OR the manually-uploaded File.
    - Test 10: After successful create, `onChainToCreateIdentity` and `onCreated` are invoked with the pre-existing arg shapes; dialog closes; state resets on next open (matches L148-161).
    - Test 11: Failed generate (postGenerateAvatarBatch throws) surfaces the error inline near the Generate button — the dialog stays open, other fields remain editable.
    - Test 12 (regression): the Phase 84 changes are preserved — header blurb still renders ("A role is what an agent does and how it thinks…"), required-caption still absent, host picker still hidden when flatHosts.length === 1 (single host auto-selected), title conforms to "New role".
  </behavior>
  <action>
    In `src/ui/sidebar/CreateRoleDialog.tsx`:

    1. Add imports at the top of the imports block: `ColorPicker` from `@/features/pretty-view/pickers/ColorPicker`, `VoicePicker` from `@/features/pretty-view/pickers/VoicePicker`, `postGenerateAvatarBatch`, `postManualAvatarCandidate`, `type AvatarCandidate` from `@/api/identities-api`. Add lucide-react `Loader2` for spinner states (already used by NewSessionDialog L1272 pattern).

    2. Add state at L120 (right after existing state at L116-121):
       - `title` (string, default `""`)
       - `voice` (string, default `""`)
       - `colorHue` (number, default `Math.floor(Math.random() * 360)` — random on each open so never-touched roles aren't all cyan)
       - `candidates` (AvatarCandidate[])
       - `pickedCandidateId` (string | null)
       - `manualPreviewUrl` (string | null)
       - `manualUrlRef` (ref, for cleanup)
       - `genLoading`, `genError`, `uploadLoading`, `uploadError` (mirror NewSessionDialog L329-337)

    3. Extend the reset-on-close effect at L148-161 to clear all new state on close AND to re-seed colorHue randomly on open. Preserve the existing auto-select-single-host behavior.

    4. Add `handleGenerate()` (mirror NewSessionDialog L654-676) calling `postGenerateAvatarBatch({name, title, brief: description, colorHue})` — note `brief` is the role's Description field per D-CTX-85-surface-3.

    5. Add `handleManualUpload()` (mirror NewSessionDialog L678-704). Preserve mutual exclusion: uploading clears generated candidates and vice versa.

    6. Add a `handleSubmit()` extension: after `createRole` returns 201, do NOT invoke onChainToCreateIdentity yet — first fetch the picked candidate's bytes into a File. For a generated candidate: `fetch(candidate.url)` → `.blob()` → `new File([blob], filename, {type: mime})`. For a manual upload: the File is already stored in a state slot (add `manualFileRef` to track it separately from the preview URL — mirror how NewSessionDialog holds the file for the birth stream but note NewSessionDialog uses candidateId indirection because the backend orchestrates the actual upload later; CreateRoleDialog does not have a birth stream, so this dialog must ship the File directly in the createRole multipart call). Pass the File to `createRole(input, avatarFile)`.

    Avatar transport path (LOCKED): raw File in multipart. Plan 85-02's endpoint contract is already committed to this shape (`data` JSON field + `avatar` file part, mirroring the identity PUT precedent). Do NOT consider a candidateId-indirection variant — that would require retroactively adjusting Plan 85-02's already-landed endpoint contract, which is structurally impossible in this wave graph. For a generated candidate: `fetch(candidate.url)` → `.blob()` → `new File([blob], `${name}.webp`, {type: 'image/webp'})`. For a manual upload: use the raw `File` object captured at input.change time — track it in a `manualFileRef` state slot separate from the preview URL.

    7. Rewrite the JSX between the Description textarea (L316) and the host picker section (L318) to insert (in order):
       - Title input (mirror NewSessionDialog L1186-1202, adapt id/aria-label to `create-role-title`)
       - Voice picker (mirror L1224-1239, id `create-role-voice`)
       - Color picker (mirror L1241-1255, id `create-role-color`)
       - Avatar section (mirror L1257-1366): Generate/Regenerate button + Upload button + candidate carousel + manual preview + inline error strips

    8. Extend the `canOpen` validation at L169 to also require `title.trim().length > 0`, `voice.length > 0` (colorHue is always non-null since seeded), and `pickedCandidateId !== null`.

    9. In `handleSubmit()` at L172-210, replace the current `createRole({name, description, hostId: hostIdNum})` call at L178-182 with the widened call: `createRole({name, description, hostId: hostIdNum, cosmetics: {title, colorHue, voice}}, avatarFile)`. Preserve the existing 409 → `RoleAlreadyExistsError` branch and the `onChainToCreateIdentity` + `onCreated` + `onClose` sequence unchanged.

    10. Do NOT touch any Phase 84 additions: header blurb (L273), required-caption absence, host picker single-host hide (L328), title conform to "New role" (L219-221).

    Do NOT modify NewSessionDialog in this plan — its strip is Plan 85-04. Do NOT modify the createRole client — it was widened in Plan 85-01 Task 3 (moved there to resolve the wave-1 file-ownership conflict on identities-api.ts; both 85-01 and 85-02 are now depends_on for this plan).
  </action>
  <acceptance_criteria>
    - `grep -n "import.*ColorPicker.*pickers/ColorPicker" src/ui/sidebar/CreateRoleDialog.tsx` returns exactly 1 line.
    - `grep -n "import.*VoicePicker.*pickers/VoicePicker" src/ui/sidebar/CreateRoleDialog.tsx` returns exactly 1 line.
    - `grep -n "postGenerateAvatarBatch\\|postManualAvatarCandidate" src/ui/sidebar/CreateRoleDialog.tsx` returns ≥ 2 matches (imports + call sites).
    - `grep -n "brief:.*description\\|brief: description" src/ui/sidebar/CreateRoleDialog.tsx` returns ≥ 1 match (the seed-mapping enforcement — role Description doubles as brief per D-CTX-85-surface-3).
    - `grep -n "cosmetics:" src/ui/sidebar/CreateRoleDialog.tsx` returns ≥ 1 match (createRole call site).
    - `grep -n "id=\"create-role-title\"\\|id=\"create-role-voice\"\\|id=\"create-role-color\"" src/ui/sidebar/CreateRoleDialog.tsx` returns 3 matches.
    - `grep -c "Phase 84" src/ui/sidebar/CreateRoleDialog.tsx | grep -v '^#'` returns ≥ 5 (existing Phase 84 comment blocks preserved as regression guards).
    - `grep -n "A role is what an agent does" src/ui/sidebar/CreateRoleDialog.tsx` returns 1 match (Phase 84 blurb preserved).
    - `npx tsc -p tsconfig.json --noEmit 2>&1 | grep -E "CreateRoleDialog\\.tsx" | wc -l` reports 0 new errors.
    - `npx vitest run --related src/ui/sidebar/CreateRoleDialog.tsx` exits 0 (existing Phase 84 tests may fail at this stage until Plan 85-06 updates them — that is expected; the point of this check is TypeScript+build health, not test pass. Record the failing test names in the SUMMARY for Plan 85-06 to consume).
  </acceptance_criteria>
  <verification>
    <automated>npx tsc -p tsconfig.json --noEmit 2>&1 | grep -c "CreateRoleDialog\\.tsx" | tr -d '\n' | { read c; test "$c" = "0" && echo "typecheck clean" || echo "typecheck errors: $c"; }</automated>
  </verification>
  <done>CreateRoleDialog renders four cosmetic authoring controls, avatar generator+carousel+upload; canOpen gates on all four cosmetics; handleSubmit calls widened createRole with cosmetics + File; Phase 84 additions preserved. Test failures from stale Phase 84 test assertions are expected and will be addressed in Plan 85-06.</done>
</task>

</tasks>

<threat_model>
Frontend UI plan — no new endpoints, no new user-input surface beyond the four cosmetic fields whose server-side validation was locked in Plan 85-02. No STRIDE entries beyond T-85-SC (package legitimacy — N/A: no new npm deps added; both pickers already ship in this repo, `postGenerateAvatarBatch` already exists).

No new attack surface: extends existing admin-only role creation endpoint via the multipart shape locked in Plan 85-02.
</threat_model>

<verification>
- `grep -n "ColorPicker\\|VoicePicker" src/ui/sidebar/CreateRoleDialog.tsx | wc -l` reports ≥ 4 (2 imports + 2 JSX usages).
- `npx tsc -p tsconfig.json --noEmit` completes with no new CreateRoleDialog errors.
- Manual smoke (not automated in this plan — Plan 85-06 owns tests): opening the dialog in the app renders Title / Voice / Color pickers and the avatar generator carousel.
</verification>

<success_criteria>
- The four cosmetic controls (Title + ColorPicker + VoicePicker + avatar generator) render in the dialog.
- Submit blocks until all cosmetics + avatar are set.
- createRole called with widened multipart shape.
- Phase 84 additions preserved (blurb, no required-caption, single-host hide, "New role" title).
</success_criteria>

<output>
Create `.planning/phases/85-cosmetics-migrate-to-role-move-title-hue-voice-avatar-from-i/85-03-SUMMARY.md` when done. Summary captures: (1) which avatar-file-transport path was chosen (raw File in multipart vs candidateId indirection) with rationale, (2) exact placement of new controls in the dialog layout (line ranges), (3) list of Phase 84 test assertions that will break under this plan (for Plan 85-06 to update), (4) whether canOpen gate implementation deviated from D-CTX-85-empty-not-scenario in any way.
</output>
