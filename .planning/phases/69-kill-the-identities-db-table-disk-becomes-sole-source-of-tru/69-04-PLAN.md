---
phase: 69-kill-the-identities-db-table-disk-becomes-sole-source-of-tru
plan: 04
type: execute
wave: 3
depends_on:
  - 69-02
  - 69-03
files_modified:
  - src/ui/api/identities-api.ts
  - src/ui/state/identities-store.ts
  - src/ui/features/pretty-view/IdentityModal.tsx
  - src/ui/features/terminal/IdentityBadge.tsx
  - src/ui/features/sessions/SessionRow.tsx
  - src/ui/features/pretty-conversations/PrettyConversationRow.tsx
  - src/ui/sidebar/CloneAgentDialog.tsx
  - src/ui/sidebar/NewSessionDialog.tsx
  - src/ui/AppShell.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "The frontend Identity interface at src/ui/api/identities-api.ts no longer declares id, createdAt, or updatedAt — the three fields are physically absent from the type"
    - "updateIdentity(identityKey, meta, avatarFile, hostId) — signature changed: first arg is now identityKey (string, lowercase) not id (nanoid); URL path built as `/identities/${encodeURIComponent(identityKey)}`"
    - "avatarUrlWithHost helper is DELETED — publicIdentity.avatarUrl now bakes hostId in server-side; frontend consumers use identity.avatarUrl directly (with etag suffix still appended where present)"
    - "applyIdentityChange(next: Identity | null, removedKey?: string) — second arg renamed from removedId to removedKey; filter changes from `i.id !== removedId` to `i.identityKey.toLowerCase() !== removedKey.toLowerCase()`; update logic keys on identityKey (was id)"
    - "IdentityModal.tsx onSave passes identity.identityKey (was identity.id) to updateIdentity; the useEffect at L604 depends on identity.identityKey (was identity.id); no other identity.id references remain in the file"
    - "IdentityBadge.tsx renders `<img src={etag ? `${identity.avatarUrl}&v=${etag}` : identity.avatarUrl}>` — avatarUrlWithHost import removed; hostId is already baked into identity.avatarUrl by the backend"
    - "SessionRow.tsx + PrettyConversationRow.tsx — same avatarUrlWithHost removal; render identity.avatarUrl directly (with etag suffix)"
    - "CloneAgentDialog.tsx L320 passes `identityId: result.identityKey` (was `result.id`); this is the field name in the NewSessionOnCreateOpts type union which stays named `identityId` for backward-compatibility with downstream (or executor rename to identityKey — see behavior for choice discussion)"
    - "NewSessionDialog.tsx BIRTH_STEP_LABELS + BIRTH_STEP_BLURBS shrink from 5 entries to 4 (IF Wave 2 Plan 03 picked SHAPE A for Step 1 collision-probe merge). If Wave 2 Plan 03 picked SHAPE B (keep 5 steps), NewSessionDialog.tsx stays unchanged. Executor checks 69-03-SUMMARY.md for the decision."
    - "npm-side tests (component tests + hooks tests + store tests) pass after the type + signature updates"
    - "npx tsc --noEmit exits 0 across the entire repo including src/ui/"
    - "deleteIdentity is not restored — it was deleted in 69-01 and stays deleted"
    - "shareIdentity + ShareIdentityPicker are not restored — deleted in 69-01, stay deleted"
    - "No runtime code path in src/ui/ treats birthEvent.identityId (or the ended-event identityId payload) as a nanoid PK lookup key — Task 4 traces every consumer and either (a) confirms the value is only used as an opaque handle / display proxy / passed-through payload, or (b) migrates any surviving `identities.find(i => i.id === value)` lookup to identityKey-based lookup via the store's byKey map (per checker iteration 1 BLOCKER 3 fix)"
    - "Task 4 documents the consumer-scan result explicitly in 69-04-SUMMARY.md — the point of the trace is that it was DONE, not that a specific grep count is zero (the ended-event payload legitimately carries an identityId field name that survives Phase 69 as an opaque string handle; grep matches on the field name are expected and fine)"
  artifacts:
    - path: "src/ui/api/identities-api.ts"
      provides: "Identity interface narrowed to 10 fields (identityKey, displayName, title, colorHue, voice, role, avatarMime, avatarUrl, avatarEtag, coordinator) — id/createdAt/updatedAt removed. updateIdentity signature changed to (identityKey, meta, avatar, hostId) with URL `/identities/${encodeURIComponent(identityKey)}`. avatarUrlWithHost helper deleted (backend now bakes hostId into avatarUrl)."
      contains: "interface Identity"
    - path: "src/ui/state/identities-store.ts"
      provides: "State.byKey still Map<string, Identity> keyed on identityKey.toLowerCase() (unchanged); applyIdentityChange second arg renamed from removedId to removedKey with identityKey-based filter; the internal `list.findIndex((i) => i.id === next.id)` becomes `list.findIndex((i) => i.identityKey.toLowerCase() === next.identityKey.toLowerCase())`"
    - path: "src/ui/features/pretty-view/IdentityModal.tsx"
      provides: "L99 ShareIdentityPicker import already gone (69-01); L604 useEffect dep identity.id → identity.identityKey; L1137 updateIdentity(identity.id, ...) → updateIdentity(identity.identityKey, ...); L1371 <ShareIdentityPicker identityId={identity.id}...> already gone (69-01); avatarUrlWithHost imports + usages removed — direct identity.avatarUrl (with etag suffix where present)"
    - path: "src/ui/features/terminal/IdentityBadge.tsx"
      provides: "avatarUrlWithHost import removed; render path uses identity.avatarUrl directly. `const finalUrl = hostId != null ? avatarUrlWithHost(...) : identity.avatarUrl` collapses to just `identity.avatarUrl`. The `hostId` prop stays because the badge still forwards it in other paths (or gets deleted if executor confirms no other consumer; keep it if in doubt)."
    - path: "src/ui/features/sessions/SessionRow.tsx"
      provides: "Same avatarUrlWithHost removal — src={avatarUrlWithHost(identity, session.hostId)} → src={identity.avatarUrl}"
    - path: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx"
      provides: "Same avatarUrlWithHost removal — src={avatarUrlWithHost(identity, rowHostIdNum)} → src={identity.avatarUrl}"
    - path: "src/ui/sidebar/CloneAgentDialog.tsx"
      provides: "avatarUrlWithHost import removed; usage at L567 → identity.avatarUrl direct. identityId payload at L320 sources from result.identityKey (not result.id); if the NewSessionOnCreateOpts type field name is renamed identityId → identityKey, propagate to NewSessionDialog.tsx type union"
    - path: "src/ui/sidebar/NewSessionDialog.tsx"
      provides: "identityMode:existing type union field renamed identityId → identityKey (or kept as identityId opaque — executor picks). BIRTH_STEP_LABELS + BIRTH_STEP_BLURBS shrink 5→4 IF 69-03 SHAPE A."
  key_links:
    - from: "IdentityBadge / SessionRow / PrettyConversationRow / IdentityModal / CloneAgentDialog"
      to: "identity.avatarUrl string"
      via: "direct render — backend now bakes hostId into the URL"
      pattern: "src=\\{identity\\.avatarUrl"
    - from: "IdentityModal onSave"
      to: "updateIdentity(identityKey, ...)"
      via: "the identityKey-based PUT — matches backend's /:identityKey rewire from 69-02"
      pattern: "updateIdentity\\(identity\\.identityKey"
    - from: "identities-store applyIdentityChange"
      to: "identityKey-based lookup + Map<identityKey.toLowerCase(), Identity>"
      via: "the store's index is already identityKey.toLowerCase(); update semantics align"
      pattern: "identityKey\\.toLowerCase\\(\\)"
---

<objective>
Frontend cascade — the mechanical follow-on to Wave 2's backend rewires. The backend now emits publicIdentity in a narrower shape (10 fields, hostId baked into avatarUrl) and PUT/GET-avatar routes accept identityKey (not id) as their URL param. This plan aligns the frontend to match: prune id/createdAt/updatedAt from the Identity type, rewire updateIdentity to identityKey, delete the avatarUrlWithHost helper (no longer needed — backend bakes hostId), migrate all identity.id consumer references to identity.identityKey, update applyIdentityChange to key on identityKey, and (if 69-03 shrank birth to 4 steps) shrink NewSessionDialog.tsx's step arrays.

Purpose: Closes the type + call-site drift left by Wave 2. Post-plan, the frontend Identity type IS the disk-authoritative shape (name + cosmetics + role + coordinator + avatarUrl-with-hostId) — nothing on the frontend claims to know a DB-only field. Frontend TSC comes back green; component tests pass with the new type surface.

Output: (a) identities-api.ts: Identity interface narrowed; updateIdentity signature migrated; avatarUrlWithHost deleted. (b) identities-store.ts: applyIdentityChange renamed second arg + identityKey-keyed. (c) Five component files (IdentityModal, IdentityBadge, SessionRow, PrettyConversationRow, CloneAgentDialog) migrated off identity.id + avatarUrlWithHost. (d) NewSessionDialog.tsx type union renames identityId → identityKey (or kept opaque per executor choice); BIRTH_STEP_LABELS/BLURBS shrink to 4 entries iff Wave 2 Plan 03 picked SHAPE A. (e) Task 4 audits every birthEvent.identityId / ended-event identityId consumer in src/ui/ and confirms no runtime `.find(i => i.id === value)` lookup survives — belt-and-suspenders backup to the TSC gate from Task 1 narrowing the Identity type (per checker iteration 1 BLOCKER 3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-kill-the-identities-db-table-disk-becomes-sole-source-of-tru/CONTEXT.md
@.planning/shapes/shape-kill-identities-table.md
@.planning/phases/69-kill-the-identities-db-table-disk-becomes-sole-source-of-tru/69-02-PLAN.md
@.planning/phases/69-kill-the-identities-db-table-disk-becomes-sole-source-of-tru/69-03-PLAN.md
@CLAUDE.md

@src/ui/api/identities-api.ts
@src/ui/state/identities-store.ts
@src/ui/features/pretty-view/IdentityModal.tsx
@src/ui/features/terminal/IdentityBadge.tsx
@src/ui/features/sessions/SessionRow.tsx
@src/ui/features/pretty-conversations/PrettyConversationRow.tsx
@src/ui/sidebar/CloneAgentDialog.tsx
@src/ui/sidebar/NewSessionDialog.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Narrow Identity type + rewire updateIdentity + delete avatarUrlWithHost in identities-api.ts (TDD)</name>
  <files>src/ui/api/identities-api.ts, src/ui/state/identities-store.ts</files>
  <read_first>
    - src/ui/api/identities-api.ts L3-22 (Identity interface — id/createdAt/updatedAt fields to drop)
    - src/ui/api/identities-api.ts L84-92 (avatarUrlWithHost helper — to delete; note the JSDoc explaining Phase 66 Plan 03 wiring)
    - src/ui/api/identities-api.ts L111-131 (updateIdentity function — signature change: first arg from id to identityKey; URL path change)
    - src/ui/api/identities-api.ts L38-55 (buildFormData + buildUpdateFormData helpers — buildUpdateFormData stays; JSDoc updates)
    - src/ui/state/identities-store.ts L173-186 (applyIdentityChange — second arg removedId → removedKey; internal update logic keys on identityKey)
    - src/ui/state/identities-store.ts L21-30 (State + State.byKey — already keyed on identityKey.toLowerCase(); no shape change, just narrower Identity)
    - src/ui/state/identities-store.ts L41-47 (withDisplayCap — unchanged; still uses identity.displayName)
    - Any identities-store test file if present (grep for `identities-store.test.` in src/ui/state/) — mocks may need Identity-shape update
    - 69-02-SUMMARY.md — confirms backend publicIdentity's 10-field shape and hostId-baked avatarUrl
  </read_first>
  <behavior>
    ---- identities-api.ts ----

    Identity interface narrows from 13 fields to 10:
    ```
    export interface Identity {
      identityKey: string;
      displayName: string;
      title: string | null;
      colorHue: number | null;
      voice: string | null;
      role: string | null;
      avatarMime: string;
      avatarUrl: string;  // NOW includes ?hostId=... baked in by backend
      avatarEtag: string;
      coordinator: boolean;
    }
    ```
    DELETED: id, createdAt, updatedAt.

    avatarUrlWithHost function (L84-92) DELETED entirely along with its JSDoc.

    updateIdentity signature: `updateIdentity(identityKey: string, meta: IdentityInput, avatar: File | null, hostId: number): Promise<Identity>`. First arg renamed. URL construction: `authApi.put(`/identities/${encodeURIComponent(identityKey)}`, ...)` — note the encodeURIComponent (matches getIdentityNoDormancy's discipline at L219).

    buildUpdateFormData signature unchanged; JSDoc updated to say "Phase 69: no longer includes hostId in the URL path; hostId is a body field per Phase 66 Plan 02 (still needed to route the backend disk-write)".

    deleteIdentity: already gone from 69-01. Verify absent.

    shareIdentity + ShareIdentityResponse + Phase 38 comment block: already gone from 69-01. Verify absent.

    ---- identities-store.ts ----

    applyIdentityChange signature: `applyIdentityChange(next: Identity | null, removedKey?: string): void`. Body changes:
    ```
    let list = state.identities.slice();
    if (removedKey) {
      const removedKeyLc = removedKey.toLowerCase();
      list = list.filter((i) => i.identityKey.toLowerCase() !== removedKeyLc);
    } else if (next) {
      const nextKeyLc = next.identityKey.toLowerCase();
      const idx = list.findIndex((i) => i.identityKey.toLowerCase() === nextKeyLc);
      if (idx >= 0) list[idx] = next;
      else list.push(next);
    }
    setIdentities(list);
    ```

    setIdentities body unchanged (already builds byKey from identityKey.toLowerCase()).

    withDisplayCap unchanged.

    Test file update (if identities-store.test.ts exists — grep to confirm; skip this bullet if not): update the Identity shape in seed objects to drop id/createdAt/updatedAt.
  </behavior>
  <action>
    TDD order — 1 cycle.

    RED cycle:
    1. If any component tests exercise updateIdentity with an id arg or applyIdentityChange with a removedId arg, update those tests. If identities-store.test.ts exists, update Identity shape in test seed objects. Commit `test(69-04): RED — Identity type narrow + updateIdentity signature`.

    GREEN cycle:
    2. Update identities-api.ts: narrow Identity interface, delete avatarUrlWithHost, rewire updateIdentity signature + URL. Update identities-store.ts: rename applyIdentityChange second arg + rewrite body to identityKey-keyed. Commit `feat(69-04): GREEN — Identity type narrow + updateIdentity signature`.

    Post-cycle: `npx tsc --noEmit` reports the remaining Task 2/3 consumer errors (identity.id in IdentityModal + CloneAgentDialog + avatarUrlWithHost calls in the 5 components). Those are Task 2/3's scope. Backend TSC unaffected.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; npx vitest run --related src/ui/api/identities-api.ts src/ui/state/identities-store.ts 2>&amp;1 | tail -30 &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -E "identity\\.id|avatarUrlWithHost|removedId" | head -20</automated>
  </verify>
  <done>
    - `grep -c "^  id:\\|^  createdAt:\\|^  updatedAt:" src/ui/api/identities-api.ts` returns 0 (Identity interface no longer declares these three)
    - `grep -c "avatarUrlWithHost" src/ui/api/identities-api.ts` returns 0 (helper deleted)
    - `grep -c "updateIdentity.*identityKey.*string" src/ui/api/identities-api.ts` returns at least 1 (signature landed)
    - `grep -c "encodeURIComponent(identityKey)" src/ui/api/identities-api.ts` returns at least 1 (URL path uses encoded identityKey)
    - `grep -c "removedKey" src/ui/state/identities-store.ts` returns at least 1 (arg renamed)
    - `grep -c "removedId" src/ui/state/identities-store.ts` returns 0 (old name gone)
    - `grep -c "i\\.id !== removedId\\|i\\.id === next\\.id" src/ui/state/identities-store.ts` returns 0 (id-based filter gone)
    - identities-store tests (if any) pass; identities-api tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate consumers off avatarUrlWithHost + off identity.id — IdentityModal, IdentityBadge, SessionRow, PrettyConversationRow, CloneAgentDialog (TDD)</name>
  <files>src/ui/features/pretty-view/IdentityModal.tsx, src/ui/features/terminal/IdentityBadge.tsx, src/ui/features/sessions/SessionRow.tsx, src/ui/features/pretty-conversations/PrettyConversationRow.tsx, src/ui/sidebar/CloneAgentDialog.tsx</files>
  <read_first>
    - Task 1 output (Identity interface narrowed; updateIdentity signature; avatarUrlWithHost deleted) — TSC will fail at every consumer site until this task lands
    - src/ui/features/pretty-view/IdentityModal.tsx L34 (updateIdentity + avatarUrlWithHost imports), L270-278 (comment reference to identity.id — the comment itself needs updating), L604 (useEffect dep array — identity.id → identity.identityKey), L1137 (updateIdentity(identity.id, ...) → updateIdentity(identity.identityKey, ...)), L1300-1325 + L1470-1478 (avatarUrlWithHost render call sites)
    - src/ui/features/terminal/IdentityBadge.tsx L5 (avatarUrlWithHost import), L15-17 (JSDoc reference), L120-126 (render call: `hostId != null ? avatarUrlWithHost(...) : identity.avatarUrl`)
    - src/ui/features/sessions/SessionRow.tsx L7 (import) + L61 (call site `src={avatarUrlWithHost(identity, session.hostId)}`)
    - src/ui/features/pretty-conversations/PrettyConversationRow.tsx L139-140 (JSDoc + import) + L1205 (call site `src={avatarUrlWithHost(identity, rowHostIdNum)}`)
    - src/ui/sidebar/CloneAgentDialog.tsx L51 (import), L320 (identityId payload — sources result.id currently, migrate to result.identityKey), L559-567 (avatarUrlWithHost render call site)
    - src/ui/sidebar/NewSessionDialog.tsx L131-134 (identityMode:"existing" type union — field name choice: keep as identityId or rename to identityKey)
  </read_first>
  <behavior>
    Migration rules (apply to each of the 5 component files):

    RULE 1 — avatarUrlWithHost call sites:
    Every occurrence of `avatarUrlWithHost(identity, hostId)` or `avatarUrlWithHost(identity, someHostIdVar)` becomes just `identity.avatarUrl`. The backend now bakes hostId into avatarUrl at emit time; the frontend has nothing to append.

    Where the current code has an etag-busting suffix: `${avatarUrlWithHost(identity, hostId)}&v=${identity.avatarEtag}` becomes `${identity.avatarUrl}&v=${identity.avatarEtag}`. Note the `&` (not `?`) — identity.avatarUrl already ends with `?hostId=N` so an etag becomes an additional query param. Verify by inspection at each call site.

    Delete the `avatarUrlWithHost` import line at the top of each file.

    Delete the `const finalUrl = hostId != null ? avatarUrlWithHost(...) : identity.avatarUrl` intermediate in IdentityBadge — collapse to just `identity.avatarUrl` used directly in the src attribute.

    RULE 2 — identity.id references:
    In IdentityModal.tsx:
    - L604 useEffect deps: `[open, identity.id, identity.title, identity.voice, identity.colorHue]` becomes `[open, identity.identityKey, identity.title, identity.voice, identity.colorHue]`. Semantic: the effect resets drafts when the modal opens or when the identity/title/voice/hue changes. identityKey is the canonical "which identity are we editing" signal; matches the comment already at L270-278.
    - L1137 updateIdentity call: `updateIdentity(identity.id, meta, avatarFile, hostId)` becomes `updateIdentity(identity.identityKey, meta, avatarFile, hostId)`.
    - L271 comment "identity.id is the row PK, which also changes on..." — update to "identity.id no longer exists post-Phase-68; identityKey is the canonical handle" OR delete the parenthetical (the comment's point about identityKey being the semantic key still stands).

    In CloneAgentDialog.tsx:
    - L320 `identityId: result.id` — decision point. Backend now returns publicIdentity with NO id field. Choice: (a) rename the destination type field `identityId` → `identityKey` in NewSessionDialog.tsx's type union AND change the RHS to `result.identityKey`, OR (b) keep the type field name `identityId` (opaque handle) and change the RHS to `result.identityKey` (payload is a string; caller treats as opaque). Executor picks — recommend (a) for cleanliness. Document choice in SUMMARY. If (a): also update AppShell.tsx L1983 comment if it references identityId.

    RULE 3 — other identity.id survivor sites:
    Grep across all 5 files for `identity.id` and `identity\.id` after applying RULE 2. Any survivors → migrate to identity.identityKey. There should be none once L604 + L1137 + L320 + ShareIdentityPicker (already deleted) are addressed.

    Verify per file:
    - IdentityModal.tsx: post-edit, `grep -c "identity\.id\b" src/ui/features/pretty-view/IdentityModal.tsx` returns 0 (only "identity.identityKey" appears).
    - CloneAgentDialog.tsx: post-edit, `grep -c "identity\.id\b\|result\.id\b" src/ui/sidebar/CloneAgentDialog.tsx` returns 0.
    - IdentityBadge.tsx: no identity.id references pre-phase; verify grep returns 0.
    - SessionRow.tsx + PrettyConversationRow.tsx: no identity.id references pre-phase; verify grep returns 0.
  </behavior>
  <action>
    TDD order — 1 cycle across all 5 files (they're mechanical migrations).

    RED cycle:
    1. Update any component tests that mocked avatarUrlWithHost or asserted on identity.id in these five components. Grep `avatarUrlWithHost\|identity\.id` in src/ui/features/pretty-view/IdentityModal.test.tsx, IdentityBadge.test.tsx (if exists), SessionRow.test.tsx (if exists), PrettyConversationRow.test.tsx (if exists), CloneAgentDialog.test.tsx (if exists). Update assertions. Commit `test(69-04): RED — component consumer migration`.

    GREEN cycle:
    2. Apply RULE 1 + RULE 2 + RULE 3 across all 5 component files. Commit `feat(69-04): GREEN — component consumer migration`.

    Post-cycle: `npx tsc --noEmit` clean across src/ui/. If Task 3 (NewSessionDialog step arrays) is needed, it lands next.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; npx vitest run --related src/ui/features/pretty-view/IdentityModal.tsx src/ui/features/terminal/IdentityBadge.tsx src/ui/features/sessions/SessionRow.tsx src/ui/features/pretty-conversations/PrettyConversationRow.tsx src/ui/sidebar/CloneAgentDialog.tsx 2>&amp;1 | tail -40 &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - `grep -rn "avatarUrlWithHost" src/ui/ 2>&amp;1 | grep -v ".test."` returns 0 (helper deleted; all call sites migrated)
    - `grep -c "identity\\.id\\b" src/ui/features/pretty-view/IdentityModal.tsx src/ui/features/terminal/IdentityBadge.tsx src/ui/features/sessions/SessionRow.tsx src/ui/features/pretty-conversations/PrettyConversationRow.tsx src/ui/sidebar/CloneAgentDialog.tsx` returns 0
    - `grep -c "updateIdentity(identity\\.identityKey" src/ui/features/pretty-view/IdentityModal.tsx` returns exactly 1
    - `grep -c "src=\\{identity\\.avatarUrl" src/ui/features/terminal/IdentityBadge.tsx src/ui/features/sessions/SessionRow.tsx src/ui/features/pretty-conversations/PrettyConversationRow.tsx` returns at least 3 (one per file's render path)
    - Frontend TSC clean (npx tsc --noEmit exits 0)
    - Scoped component tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: NewSessionDialog.tsx — rename identityId type field (or keep) + shrink BIRTH_STEP arrays if 69-03 SHAPE A (TDD)</name>
  <files>src/ui/sidebar/NewSessionDialog.tsx</files>
  <read_first>
    - .planning/phases/69-kill-the-identities-db-table-disk-becomes-sole-source-of-tru/69-03-SUMMARY.md (executor decision on SHAPE A vs SHAPE B for Step 1 collision probe)
    - src/ui/sidebar/NewSessionDialog.tsx L131-134 (identityMode:"existing" type union with identityId: string field)
    - src/ui/sidebar/NewSessionDialog.tsx L138-154 (BIRTH_STEP_LABELS with 5 entries + BIRTH_STEP_BLURBS with 5 entries)
    - src/ui/sidebar/NewSessionDialog.tsx (whole file — grep for `identityId` and `BIRTH_STEP` to find all touch points)
    - src/ui/AppShell.tsx (grep for `identityId` and `identityMode` — one comment reference at L1983 may need updating; also verify no runtime consumer reads opts.identityId — if none, the field rename is purely cosmetic)
  </read_first>
  <behavior>
    ---- identityId type field decision ----

    If Task 2 executor picked OPTION (a) [rename field]:
    - Change the `identityMode:"existing"` union member at L131-134 from `identityId: string` to `identityKey: string`.
    - Grep the file for any runtime read of `.identityId` in that union arm. If found, migrate to `.identityKey`. If none, the field rename is type-surface-only.
    - Grep AppShell.tsx for `identityId` in the comment at L1983 or elsewhere; update comment references.

    If Task 2 executor picked OPTION (b) [keep field name identityId]:
    - No change to the type union — the field name stays identityId but its VALUE now carries the identityKey string. Add a JSDoc comment on the field: "Phase 69: this string is the identityKey (was nanoid PK pre-phase). Field name retained for backward-compatibility with downstream callers; treat as opaque identity handle."

    ---- BIRTH_STEP arrays ----

    Read 69-03-SUMMARY.md to determine SHAPE:

    If SHAPE A (Step 1 merged into Step 2):
    - Shrink BIRTH_STEP_LABELS from 5 entries to 4. Delete entry 0 ("Create Skynet identity record"); reindex remaining 4.
    - Shrink BIRTH_STEP_BLURBS from 5 entries to 4. Delete entry 0 (the "couldn't create the Skynet identity record" text); reindex remaining 4.
    - Update any code that maps BirthEvent step numbers to labels/blurbs (`birthEvent.n` is the 1-based step index; if the backend renumbered to 1-4, the array index is `n - 1` still). Verify the mapping still works — grep for `BIRTH_STEP_LABELS[` or `BIRTH_STEP_BLURBS[` in the file.
    - Update the BirthEvent type import if the backend narrowed the `n` type union from `1|2|3|4|5` to `1|2|3|4` — grep for `import.*BirthEvent` in NewSessionDialog.tsx and update accordingly.

    If SHAPE B (Step 1 kept as dedicated on-disk collision probe):
    - Update BIRTH_STEP_LABELS[0] text from "Create Skynet identity record" to "Check identity name is available on host".
    - Update BIRTH_STEP_BLURBS[0] text from "Couldn't create the Skynet identity record. Nothing was created — safe to retry." to something like "The identity name is already in use on this host. Pick a different name and retry."
    - Array LENGTH stays 5. BirthEvent `n` type union stays 1-5.

    Comment near the arrays explaining the phase: "Phase 69: [describe the shape choice from 69-03-SUMMARY.md briefly]."
  </behavior>
  <action>
    TDD order — 1 cycle. Small footprint; test coverage may be nil.

    RED cycle:
    1. If NewSessionDialog has tests exercising BIRTH_STEP arrays or the identityId payload, update them. Commit `test(69-04): RED — NewSessionDialog step arrays + identity handle`.

    GREEN cycle:
    2. Apply the identityId decision + BIRTH_STEP shrink-or-relabel per SHAPE from 69-03-SUMMARY.md. Commit `feat(69-04): GREEN — NewSessionDialog step arrays + identity handle`.

    Post-cycle: `npx tsc --noEmit` clean.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; npx vitest run --related src/ui/sidebar/NewSessionDialog.tsx 2>&amp;1 | tail -20 &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - BIRTH_STEP_LABELS length matches the SHAPE chosen in 69-03 (4 for SHAPE A; 5 for SHAPE B)
    - BIRTH_STEP_BLURBS length matches BIRTH_STEP_LABELS
    - identityMode:"existing" type union field is EITHER `identityKey: string` (OPTION a) OR `identityId: string` with a Phase-68 opacity comment (OPTION b)
    - Frontend TSC clean
    - Full identity-family + component tests scoped run green
  </done>
</task>

<task type="auto">
  <name>Task 4: Trace every birthEvent.identityId / ended-event identityId consumer in src/ui/ and confirm no runtime code path treats the value as a nanoid PK for a `.find(i => i.id === ...)` lookup (BLOCKER 3 fix)</name>
  <files>src/ui/sidebar/NewSessionDialog.tsx, src/ui/AppShell.tsx</files>
  <read_first>
    - Task 1 output (Identity interface no longer declares `id`; TSC will fail on any `.find(i => i.id === value)` lookup — this task is the belt-and-suspenders trace confirming there are no such lookups hiding behind a fallback / type-assertion)
    - 69-03-PLAN.md § Task 1 behavior — the birth orchestrator's final `emit({ type: "ended", ok: true, identityId, sessionName: opts.name })` where identityId is now `opts.name` (the identityKey string). The FIELD name `identityId` on the SSE event payload survives Phase 69 (renaming the wire event is out of scope — it would cascade to backend + frontend + tests + operator log strings; not worth it for a semantic-only shift). The VALUE the field carries is now the identityKey.
    - src/ui/sidebar/NewSessionDialog.tsx — the primary birth-flow consumer of the ended event. Grep confirms `endedEvent.identityId` is destructured but the current code (L676-696 in phase-69-pre-Wave-3 state) only reads `endedEvent.ok` and `endedEvent.type`; the identityId field is never dereferenced further. Verify this stays true after all Wave 3 edits — if executor picks OPTION (a) in Task 2 (rename NewSessionOnCreateOpts field identityId → identityKey), the birth-success onCreate branch at L712-723 also carries the identityKey (which was already opts.name pre-phase).
    - src/ui/AppShell.tsx L1976-2009 (onCreateSession callback) — three-way discriminated union narrowing on opts.identityMode. The "existing" branch at L1991-1993 reads `opts.identityName` (NOT opts.identityId) as the tmux session name. The identityId field on the "existing" arm is NEVER read at runtime; it's a passed-through payload only.
    - src/ui/sidebar/CloneAgentDialog.tsx L312-325 — clone auto-route's onCreateSession call site. Post-Task-2, the identityId field of the emitted opts sources from result.identityKey (per checker: BLOCKER 3 recognizes this migration is already in Task 2's scope; this task is the AUDIT that no OTHER consumer is doing a `.find(i => i.id === opts.identityId)` lookup somewhere unexpected).
    - src/ui/api/identities-api.ts L437 (Type declaration `identityId?: string` on the BirthEvent "ended" arm — stays; the field name is stable on the wire).
  </read_first>
  <action>
    Belt-and-suspenders audit — no source-file edits expected unless the grep sweep uncovers a surviving consumer.

    Step 1 — repository-wide grep sweep. Run in this order, capturing output for the SUMMARY:
    ```
    grep -rn "birthEvent\.identityId\|ended.*identityId\|onBirthEnded" src/ui/ | grep -v "\.test\."
    grep -rn "\.identityId" src/ui/ | grep -v "\.test\." | grep -v "src/ui/api/identities-api\.ts"
    grep -rn "identities\.find" src/ui/ | grep -v "\.test\."
    grep -rn "\.find(.*=== .*identityId\|\.find(.*i\.id ===" src/ui/ | grep -v "\.test\."
    ```

    Step 2 — for each hit, classify:
    - PASSED-THROUGH (opaque handle): the value is placed on another object and forwarded to a callback / stored in state / sent to a WS server — no dereference. Example: `onCreate({..., identityId: result.identityKey })`. SAFE. No edit.
    - DISPLAYED (as string): rendered directly (e.g. `<span>{identityId}</span>`), used as a key in a React map, or logged. SAFE. No edit. The value happens to be an identityKey now; renders correctly.
    - LOOKUP AS PK (nanoid): pattern `identities.find(i => i.id === value)` or equivalent (`Map<id, Identity>.get(value)`, `identityById[value]`). BROKEN after Task 1 — migrate to identityKey-based lookup:
      - Replace `identities.find(i => i.id === value)` with `identities.find(i => i.identityKey.toLowerCase() === value.toLowerCase())` OR (preferred) `byKey.get(value.toLowerCase())` from the store.
      - Rename local variables from `identityId` → `identityKey` in the surrounding block for readability.
      - Update comments to note the Phase 69 semantic shift.

    Step 3 — document result in SUMMARY explicitly:
    - "Trace complete: N call sites found, all classified as PASSED-THROUGH or DISPLAYED. Zero LOOKUP-AS-PK sites found. No migration edits required."
    - OR: "Trace complete: N call sites found, K required migration. Migrated: [list files + line ranges]."

    Step 4 — informational grep gate for `<done>`. The point is to prove the trace WAS DONE, not that a specific count is zero. The identityId field name survives Phase 69 as an opaque string; grep matches on the field name are expected.

    Expected outcome based on pre-plan trace (recorded in checker iteration 1 findings): all consumers are PASSED-THROUGH or DISPLAYED. NewSessionDialog.tsx never dereferences endedEvent.identityId (only reads .ok / .type). AppShell.tsx uses opts.identityName (not opts.identityId) for the "existing" branch. CloneAgentDialog.tsx L320 already migrates result.id → result.identityKey in Task 2. No LOOKUP-AS-PK sites survive to Wave 4.

    If the audit finds a surprise, expand `<files>` and apply the migration inline in this task.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; { echo "=== birthEvent/onBirthEnded scan ==="; grep -rn "birthEvent\.identityId\|ended.*identityId\|onBirthEnded" src/ui/ | grep -v "\.test\." || true; echo "=== .identityId consumer scan ==="; grep -rn "\.identityId" src/ui/ | grep -v "\.test\." | grep -v "src/ui/api/identities-api\.ts" || true; echo "=== identities.find nanoid-PK-lookup scan ==="; grep -rn "identities\.find" src/ui/ | grep -v "\.test\." || true; echo "=== .find(...i.id === ...) generic pattern scan ==="; grep -rn "\.find(.*i\.id ===" src/ui/ | grep -v "\.test\." || true; echo "=== TSC ==="; npx tsc --noEmit 2>&amp;1 | tail -20; }</automated>
  </verify>
  <done>
    - `grep -rn "\.find(.*i\.id ===\|\.find(.*=== .*\.identityId" src/ui/ 2>&amp;1 | grep -v "\.test\." | wc -l` returns 0 — NO surviving nanoid-PK-lookup pattern in src/ui/ non-test code (this is the actual safety gate)
    - `grep -rn "birthEvent\.identityId\|ended.*identityId" src/ui/ 2>&amp;1 | wc -l` returns a NUMBER (informational — executor documents the count in SUMMARY; the field name legitimately survives as an opaque payload key on the SSE event)
    - Full frontend TSC clean: `npx tsc --noEmit` exits 0 (Task 1's Identity-narrowing removes `id` from the type; any surviving `.id` read on an Identity would TSC-error — this + the grep are belt-and-suspenders)
    - SUMMARY records the trace-classification result explicitly (per BLOCKER 3 acceptance criterion: the point is the trace was DONE, not that a specific grep-count is zero)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| avatarUrl string composition | Backend now owns the FULL URL string (path + query); frontend consumers render it directly. Any regression that reintroduces avatarUrlWithHost on the frontend would double-append hostId (malformed URL) — grep gate in <done> catches this. |
| Identity type narrowing | Consumers that read identity.id or identity.createdAt/updatedAt post-phase get `undefined` at runtime (types don't declare them). Frontend TSC catches most; a stray `identity.id` in a JSX expression compiles as-if-optional and renders empty string — grep gate catches. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-04-01 | Data Integrity | stale identity.id read post-type-narrow | mitigate | Frontend TSC surfaces every consumer at compile time (identity.id is not on the narrower Identity type). Grep gate in <done> for identity\.id in the migrated files provides a second net. Runtime evaluation of a stale `identity.id` yields `undefined`, which renders as empty string in JSX or breaks a URL construction visibly. |
| T-69-04-02 | Compatibility | AppShell / other consumer with hidden identity.id read | mitigate | Post-Task 2 scoped grep sweep across src/ui/ for identity\.id (not identityKey) finds any surviving reference. TSC further gates on the narrowed Identity type. |
| T-69-04-03 | Availability | avatarUrl now-baked hostId encoding edge cases | accept | encodeURIComponent(identityKey) in updateIdentity handles URL-unsafe chars (IDENTITY_KEY_RE is [a-z0-9._=/+-] — `/` and `+` are URL-special; encodeURIComponent handles them). Backend receives the decoded param via Express default url-decoding. |
| T-69-04-04 | Availability | applyIdentityChange identityKey collision if two different-cased keys | accept | State.byKey is Map<identityKey.toLowerCase(), Identity> unchanged; the applyIdentityChange filter is now also lowercase-normalized. Consistent case handling; matches pre-phase invariant (identityKey lowercase throughout). |
| T-68-SC | Tampering | npm installs | mitigate | NO NEW PACKAGES. |
</threat_model>

<verification>
Scoped test run:
```bash
cd /home/ubuntu/skynet-tina && npx vitest run --related \
  src/ui/api/identities-api.ts \
  src/ui/state/identities-store.ts \
  src/ui/features/pretty-view/IdentityModal.tsx \
  src/ui/features/terminal/IdentityBadge.tsx \
  src/ui/features/sessions/SessionRow.tsx \
  src/ui/features/pretty-conversations/PrettyConversationRow.tsx \
  src/ui/sidebar/CloneAgentDialog.tsx \
  src/ui/sidebar/NewSessionDialog.tsx
```

TSC repo-wide sanity check:
```bash
cd /home/ubuntu/skynet-tina && npx tsc --noEmit 2>&1 | tail -30
```

Frontend consumer grep sweep (all must return 0):
```bash
grep -rn "avatarUrlWithHost" src/ui/ | grep -v ".test."
grep -c "identity\.id\b" src/ui/features/pretty-view/IdentityModal.tsx src/ui/features/terminal/IdentityBadge.tsx src/ui/features/sessions/SessionRow.tsx src/ui/features/pretty-conversations/PrettyConversationRow.tsx src/ui/sidebar/CloneAgentDialog.tsx
grep -c "removedId" src/ui/state/identities-store.ts
grep -c "^  id:\|^  createdAt:\|^  updatedAt:" src/ui/api/identities-api.ts  # in Identity interface only — indentation-sensitive but 2-space is stable
```

Ship-gate (orchestrator, NOT executor): full `npx vitest run` green before docker build.
</verification>

<success_criteria>
- Identity interface at src/ui/api/identities-api.ts declares exactly 10 fields (no id, no createdAt, no updatedAt)
- avatarUrlWithHost helper is physically deleted from identities-api.ts
- All frontend consumers render identity.avatarUrl directly (no avatarUrlWithHost calls survive)
- updateIdentity takes identityKey as first arg + builds URL with encodeURIComponent(identityKey)
- IdentityModal onSave passes identity.identityKey (was identity.id)
- CloneAgentDialog onCreateSession payload sources from result.identityKey (was result.id)
- applyIdentityChange second arg renamed removedId → removedKey with identityKey-based filter
- NewSessionDialog BIRTH_STEP arrays match 69-03 SHAPE decision (4 entries for A, 5 for B)
- NewSessionDialog identityMode:"existing" field renamed or kept opaque per executor choice
- No frontend consumer accesses .id, .createdAt, or .updatedAt on an Identity instance
- Frontend TSC clean
- Scoped component tests pass
</success_criteria>

<output>
Create `.planning/phases/69-kill-the-identities-db-table-disk-becomes-sole-source-of-tru/69-04-SUMMARY.md` capturing:
- OPTION (a) vs (b) decision for NewSessionOnCreateOpts.identityId → identityKey field rename
- BIRTH_STEP array size change (4 or 5) based on 69-03 SHAPE
- Test file update count (per file: how many tests updated, deleted, added)
- Any surprise TSC errors post-migration (e.g., a lurking identity.id consumer somewhere unexpected)
- Confirmation that no frontend consumer reads .id / .createdAt / .updatedAt on Identity
- Task 4 trace results: enumerate every `.identityId` / `birthEvent.identityId` / `onBirthEnded` consumer found in src/ui/ non-test code; classify each as PASSED-THROUGH / DISPLAYED / LOOKUP-AS-PK; note zero LOOKUP-AS-PK sites (the acceptance criterion for BLOCKER 3)
- Handoff to Wave 4: frontend is fully migrated; backend can DROP TABLE identities without a live consumer worrying about the row shape
</output>
