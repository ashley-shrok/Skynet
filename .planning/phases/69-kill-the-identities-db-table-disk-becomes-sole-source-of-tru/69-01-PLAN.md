---
phase: 69-kill-the-identities-db-table-disk-becomes-sole-source-of-tru
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/database/database.ts
  - src/backend/database/routes/identities.ts
  - src/backend/database/routes/identity-share.ts
  - src/backend/database/routes/identity-share.test.ts
  - src/ui/features/pretty-view/IdentityModal.tsx
  - src/ui/features/pretty-view/IdentityModal.share.test.tsx
  - src/ui/features/pretty-view/ShareIdentityPicker.tsx
  - src/ui/features/pretty-view/ShareIdentityPicker.test.tsx
  - src/ui/api/identities-api.ts
  - src/ui/api/user-management-api.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Grep for the string 'identity-share' across src/ returns zero hits (file gone, no imports, no cross-refs)"
    - "Grep for the string 'ShareIdentityPicker' across src/ui/ returns zero hits (component gone, no imports in IdentityModal, no test refs, no lingering comment references)"
    - "Grep for 'shareIdentity(' in src/ui/ returns zero hits — the client function is deleted alongside the picker"
    - "Grep for 'deleteIdentity(' in src/ui/ returns zero hits — the client function is deleted alongside the DELETE endpoint"
    - "router.delete(\"/:id\", ...) no longer appears in src/backend/database/routes/identities.ts — the DELETE handler is physically removed"
    - "app.use(\"/identities\", identityShareRoutes) no longer appears in src/backend/database/database.ts and neither does the import of identity-share.js"
    - "IdentityModal.tsx no longer renders <ShareIdentityPicker> anywhere and no longer imports it — the DialogHeader picker slot is gone"
    - "npx vitest run --related src/backend/database/routes/identities.ts src/ui/features/pretty-view/IdentityModal.tsx exits 0 — the deletions do not break unrelated tests"
    - "npx tsc --noEmit exits 0 — TypeScript is clean across the deletion set"
  artifacts:
    - path: "src/backend/database/routes/identity-share.ts"
      provides: "DELETED — the entire file is removed from disk (both the source and the .test.ts sibling)"
    - path: "src/backend/database/routes/identity-share.test.ts"
      provides: "DELETED — Phase 38 wave 1 test file removed alongside the source"
    - path: "src/ui/features/pretty-view/ShareIdentityPicker.tsx"
      provides: "DELETED — the Phase 38 wave 2 picker component removed wholesale"
    - path: "src/ui/features/pretty-view/ShareIdentityPicker.test.tsx"
      provides: "DELETED — Phase 38 wave 2 test file (imports { ShareIdentityPicker } from './ShareIdentityPicker') — TSC-fails the instant the picker source is deleted; must go alongside"
    - path: "src/ui/features/pretty-view/IdentityModal.share.test.tsx"
      provides: "DELETED — Phase 38 wave 2 IdentityModal ↔ ShareIdentityPicker integration test (imports type ShareIdentityPickerProps from './ShareIdentityPicker' AND `* as PickerModule from './ShareIdentityPicker'`) — TSC-fails the instant the picker source is deleted; must go alongside"
    - path: "src/backend/database/routes/identities.ts"
      provides: "router.delete('/:id', ...) block deleted (currently L667-697); publicIdentity() + POST + PUT + GET / + GET /:id/avatar remain untouched by this plan (rewired in 69-02)"
    - path: "src/backend/database/database.ts"
      provides: "Import of identityShareRoutes removed (L29); app.use('/identities', identityShareRoutes) line removed (L1846) along with its explanatory comment block (L1842-L1846)"
    - path: "src/ui/features/pretty-view/IdentityModal.tsx"
      provides: "Import of ShareIdentityPicker (L99) removed; all ShareIdentityPicker-related props/state removed (alreadySharedUserIds state L261-263, its reset useEffect L276-278, handleShareSuccess callback L282-296); JSX rendering the <ShareIdentityPicker/> element removed (~L1370-L1375)"
    - path: "src/ui/api/identities-api.ts"
      provides: "shareIdentity() function + IdentityCloneCollisionError-like ShareIdentityResponse type + all Phase 38 comment blocks deleted (L357-L396); deleteIdentity() function deleted (L133-L139) along with its DELETE-endpoint doc comments"
    - path: "src/ui/api/user-management-api.ts"
      provides: "Stale L26 comment 'Consumed by ShareIdentityPicker to populate the DropdownMenu content.' is stripped (single-line removal). The getUsersListBasic function + its route + its interface stay intact — the route has non-Phase-38 uses (or is now inert but harmless); only the lying comment goes. Prevents the <done> grep gate from tripping on a comment that references a deleted component."
  key_links:
    - from: "database.ts app.use(/identities, ...) chain"
      to: "identities.ts remaining routes (GET /, POST /, PUT /:id, GET /:id/avatar)"
      via: "Express route mount order — the generic identitiesRoutes mount at ~L1876 still owns the four surviving routes; only identityShareRoutes is unmounted"
      pattern: "app\\.use\\(\"/identities\", identitiesRoutes\\)"
---

<objective>
Wave 1 wholesale removals: kill the share endpoint (backend routes + tests + frontend picker + frontend client function + database.ts wiring) and kill the DELETE /identities/:id endpoint + its frontend client function. Everything deleted here is either (a) obsolete under the disk-authoritative model this phase moves toward, or (b) dead code with no UI wiring today (the DELETE endpoint).

Purpose: Shrinks the surface area BEFORE Wave 2's rewires touch identities.ts + the birth/clone flows. Any code path we can delete today is one less thing Wave 2 has to reason about. Also unblocks the schema drop in Wave 4 — the share endpoint is currently the largest INSERT path into the identities table; deleting it removes an obstacle the migration would otherwise have to survive.

Output: (a) identity-share.ts + identity-share.test.ts + ShareIdentityPicker.tsx + ShareIdentityPicker.test.tsx + IdentityModal.share.test.tsx physically removed from disk. (b) identities.ts DELETE handler removed. (c) database.ts import + mount line for identityShareRoutes removed. (d) IdentityModal.tsx pruned of all Phase 38 share-picker state, props, callbacks, and JSX. (e) identities-api.ts pruned of shareIdentity() + deleteIdentity() functions + Phase 38 doc comments. (f) user-management-api.ts stale ShareIdentityPicker comment stripped. (g) TSC clean; scoped tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-kill-the-identities-db-table-disk-becomes-sole-source-of-tru/CONTEXT.md
@.planning/shapes/shape-kill-identities-table.md
@CLAUDE.md

@src/backend/database/database.ts
@src/backend/database/routes/identities.ts
@src/backend/database/routes/identity-share.ts
@src/backend/database/routes/identity-share.test.ts
@src/ui/features/pretty-view/IdentityModal.tsx
@src/ui/features/pretty-view/IdentityModal.share.test.tsx
@src/ui/features/pretty-view/ShareIdentityPicker.tsx
@src/ui/features/pretty-view/ShareIdentityPicker.test.tsx
@src/ui/api/identities-api.ts
@src/ui/api/user-management-api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete identity-share.ts + identity-share.test.ts + ShareIdentityPicker.tsx + ShareIdentityPicker.test.tsx + IdentityModal.share.test.tsx wholesale; prune database.ts wiring; prune identities-api.ts client + types; prune IdentityModal.tsx picker slot + Phase 38 state; strip stale ShareIdentityPicker comment from user-management-api.ts</name>
  <files>src/backend/database/routes/identity-share.ts, src/backend/database/routes/identity-share.test.ts, src/ui/features/pretty-view/ShareIdentityPicker.tsx, src/ui/features/pretty-view/ShareIdentityPicker.test.tsx, src/ui/features/pretty-view/IdentityModal.share.test.tsx, src/backend/database/database.ts, src/ui/api/identities-api.ts, src/ui/api/user-management-api.ts, src/ui/features/pretty-view/IdentityModal.tsx</files>
  <read_first>
    - .planning/phases/69-kill-the-identities-db-table-disk-becomes-sole-source-of-tru/CONTEXT.md (§ Scope edges bullet 3 — "Delete `identity-share.ts` wholesale (all routes, all handlers, all tests). Remove imports/references to both from any router-wiring file.")
    - .planning/shapes/shape-kill-identities-table.md (§ Shape — "Share flow dissolves entirely (obsolete now that disk is universal)")
    - src/backend/database/routes/identity-share.ts (whole file — being deleted; understand what it exports and what depends on it before pulling the trigger)
    - src/backend/database/routes/identity-share.test.ts (whole file — being deleted alongside the source)
    - src/ui/features/pretty-view/ShareIdentityPicker.tsx (whole file — being deleted)
    - src/ui/features/pretty-view/ShareIdentityPicker.test.tsx (whole file — imports { ShareIdentityPicker } from "./ShareIdentityPicker" at L102; TSC-fails the moment the picker source is deleted; must be removed alongside)
    - src/ui/features/pretty-view/IdentityModal.share.test.tsx (whole file — imports `type ShareIdentityPickerProps from "./ShareIdentityPicker"` at L31 AND `* as PickerModule from "./ShareIdentityPicker"` at L168; also asserts on identity.id which is being narrowed in Wave 3; MUST be removed alongside — the picker is gone, the test loses its subject)
    - src/backend/database/database.ts L17-30 (identity route imports — the `import identityShareRoutes from "./routes/identity-share.js"` line at L29 is the import to delete) and L1842-1846 (the app.use mount block for identityShareRoutes with its 3-line comment)
    - src/ui/api/identities-api.ts L133-139 (deleteIdentity function — delete this) and L357-396 (Phase 38 share block: the comment banner L357-376, ShareIdentityResponse interface L378-381, shareIdentity function L383-396)
    - src/ui/api/user-management-api.ts L16-26 (Phase 38 comment block including the stale L26 "Consumed by ShareIdentityPicker to populate the DropdownMenu content." — that single line is what trips the src/ grep gate for "ShareIdentityPicker" if left in; the function itself stays because /users/list-basic is a general-purpose backend route)
    - src/ui/features/pretty-view/IdentityModal.tsx L99 (import ShareIdentityPicker), L252-296 (Phase 38 wave 2 alreadySharedUserIds state + reset useEffect + handleShareSuccess callback), and the JSX `<ShareIdentityPicker>` render site around L1370-1375 (grep for "ShareIdentityPicker" in the file to confirm exact line range in current tree)
  </read_first>
  <action>
    Do the deletions in this exact order (last-consumer-first so intermediate TSC failures point at leftover consumers, not at freshly-missing modules):

    1) IdentityModal.tsx — remove the ShareIdentityPicker consumer:
       - Delete the import line `import { ShareIdentityPicker } from "./ShareIdentityPicker";` (currently ~L99).
       - Delete the `const [alreadySharedUserIds, setAlreadySharedUserIds] = useState<Set<string>>(...)` state declaration + its Phase 38 comment banner (currently L252-263).
       - Delete the `useEffect(() => { setAlreadySharedUserIds(new Set<string>()); }, [identity.identityKey]);` reset block + its comment (currently L265-278).
       - Delete the `handleShareSuccess` useCallback + its comment (currently L280-296).
       - Delete the `<ShareIdentityPicker identityId={identity.id} identityKey={identity.identityKey} alreadySharedUserIds={alreadySharedUserIds} onShareSuccess={handleShareSuccess} />` JSX element (currently around L1370-1375). Remove the whole JSX block; do NOT leave an empty <></> or placeholder. If the surrounding container becomes visually empty as a result, that is expected and desired — the DialogHeader will render without the share button.
       - Verify no lingering references to `alreadySharedUserIds`, `setAlreadySharedUserIds`, `handleShareSuccess`, or `ShareIdentityPicker` remain in the file after the edits.

    2) identities-api.ts — remove the shareIdentity + deleteIdentity client functions:
       - Delete `deleteIdentity` function + its short comment (currently L133-139).
       - Delete the entire Phase 38 block: the L357-376 comment banner, the L378-381 `ShareIdentityResponse` interface, and the L383-396 `shareIdentity` function (all one contiguous block).
       - Do NOT delete the `IdentityCloneCollisionError` (that belongs to /identities/clone, out of scope).
       - Do NOT delete the `getUsersListBasic` import path in user-management-api (unrelated).

    3) user-management-api.ts — strip the stale ShareIdentityPicker comment:
       - Delete the single line at ~L26: `// Consumed by ShareIdentityPicker to populate the DropdownMenu content.`
       - Do NOT delete the surrounding L16-25 Phase-38 comment block wholesale — its explanation of the /users/list-basic route (server-side ne(users.id, requester), explicit-columns select, empty-array-on-single-user) is still accurate and useful documentation. Just the one lying line goes.
       - Do NOT touch the `getUsersListBasic` function itself — the backend `/users/list-basic` route lives in `src/backend/database/routes/user-admin-routes.ts` (out of Phase 69 scope). The route is generic-purpose; if no consumer remains after 69-01, it becomes dead-but-harmless code (a future cleanup can prune the route + client + tests together). NOT this plan's problem.
       - Post-edit: `grep -c "ShareIdentityPicker" src/ui/api/user-management-api.ts` returns 0.

    4) ShareIdentityPicker.tsx — delete the file entirely:
       - `rm src/ui/features/pretty-view/ShareIdentityPicker.tsx`.

    5) ShareIdentityPicker.test.tsx — delete the file entirely:
       - `rm src/ui/features/pretty-view/ShareIdentityPicker.test.tsx`.
       - Rationale: L102 imports `{ ShareIdentityPicker } from "./ShareIdentityPicker"` — TSC errors the instant step 4 lands. Deleting the test alongside is the correct move (the picker is gone; the test's subject is gone).

    6) IdentityModal.share.test.tsx — delete the file entirely:
       - `rm src/ui/features/pretty-view/IdentityModal.share.test.tsx`.
       - Rationale: L31 imports `type { ShareIdentityPickerProps } from "./ShareIdentityPicker"` AND L168 does `import * as PickerModule from "./ShareIdentityPicker"`. Both TSC-fail after step 4. The test file also asserts on `identity.id` (BASE_IDENTITY L179 declares `id: "id-share-1"`) which is being narrowed off the Identity type in Wave 3 — even if the picker were kept, this test would need to be rewritten. Delete alongside the picker.

    7) database.ts — unmount + unimport the share router:
       - Delete the `import identityShareRoutes from "./routes/identity-share.js";` line (currently L29) along with any leading comment referencing Phase 38 (L25-28 in current tree).
       - Delete the app.use block for identityShareRoutes (currently L1842-1846: the 4-line comment banner L1842-1845 plus the `app.use("/identities", identityShareRoutes);` line at L1846).

    8) identity-share.ts + identity-share.test.ts — delete the files:
       - `rm src/backend/database/routes/identity-share.ts src/backend/database/routes/identity-share.test.ts`.

    9) identities.ts — delete the DELETE handler:
       - Delete the entire `router.delete("/:id", authenticateJWT, async (req: Request, res: Response) => { ... });` block (currently L667-697). This handler had no UI wiring today (per CONTEXT.md § Scope edges bullet 3 — "DELETE is being removed entirely; ... dead endpoint, no UI wiring today"). Keep the surrounding routes (PUT /:id at L343-665 above, GET /:id/avatar at L704-796 below) intact — those are Wave 2's scope, not Wave 1's.
       - Do NOT touch publicIdentity(), POST /, PUT /:id, GET /, or GET /:id/avatar — all of those are Wave 2 rewires, not Wave 1 deletions.

    After all deletions, run `npx tsc --noEmit` and the scoped test command in <verify>. TSC MUST be clean — if any consumer of the deleted functions still exists (e.g., a stray test file that mocks shareIdentity), delete or update that reference too.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -30 &amp;&amp; npx vitest run --related src/backend/database/routes/identities.ts src/backend/database/database.ts src/ui/features/pretty-view/IdentityModal.tsx src/ui/api/identities-api.ts src/ui/api/user-management-api.ts 2>&amp;1 | tail -40</automated>
  </verify>
  <done>
    - `ls src/backend/database/routes/identity-share.ts src/backend/database/routes/identity-share.test.ts src/ui/features/pretty-view/ShareIdentityPicker.tsx src/ui/features/pretty-view/ShareIdentityPicker.test.tsx src/ui/features/pretty-view/IdentityModal.share.test.tsx 2>&amp;1` reports all five files as "No such file or directory"
    - `grep -rn "identity-share\|ShareIdentityPicker\|shareIdentity\|deleteIdentity" src/ 2>&amp;1 | grep -v -E "node_modules|\.d\.ts" | wc -l` returns 0 — deliberately drops the earlier `\.test\.tsx?:` exception because the offending test files are deleted in this plan, so ANY surviving hit (source, test, or comment) is an escapee. Includes the frontend test dir path in the search per BLOCKER 1 fix.
    - `grep -c "ShareIdentityPicker" src/ui/api/user-management-api.ts` returns 0 (stale comment stripped per BLOCKER 2 fix)
    - `grep -c "router.delete" src/backend/database/routes/identities.ts` returns 0
    - `grep -c "identityShareRoutes" src/backend/database/database.ts` returns 0
    - `npx tsc --noEmit` exits with status 0
    - `npx vitest run --related src/backend/database/routes/identities.ts src/backend/database/database.ts src/ui/features/pretty-view/IdentityModal.tsx src/ui/api/identities-api.ts src/ui/api/user-management-api.ts` exits 0 (any surviving tests for these files still pass after the deletions)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deleted-route surface | Requests to POST /identities/:id/share and DELETE /identities/:id now 404 at the Express router level (no route matches). |
| deleted-frontend UI | The DialogHeader share button disappears entirely; users cannot invoke shareIdentity() from the UI (function no longer exists). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01-01 | Denial of Service | orphaned bookmarks / prior share-endpoint URLs | accept | Callers hitting the deleted endpoint receive a 404 from Express's default handler — no crash, no leak. Ashley confirmed at shape-lock that share is obsolete under disk-authoritative cosmetics; recovery for any hypothetical stuck client is `git revert` at the deploy layer, not a re-add of the endpoint. |
| T-69-01-02 | Data Integrity | in-flight share requests during deploy | accept | Ashley is the only user of the deployed instance today; deploy discipline (deadman rollback timer + full-suite green ship gate) covers the deploy window. No cross-user share ever needs to complete post-cutover — disk is the sharing mechanism now. |
| T-69-01-03 | Compatibility | tests that mocked shareIdentity / deleteIdentity | mitigate | Every test file for a deleted route/function/component is deleted alongside per CONTEXT.md § Scope edges bullet 14 ("Every test file for a deleted route/function/component gets deleted alongside"). The two frontend picker tests (ShareIdentityPicker.test.tsx + IdentityModal.share.test.tsx) go with the picker per BLOCKER 1 fix. TSC catches any test that mocked these symbols; the scoped vitest run in <verify> catches any surviving mock-then-import stub. |
| T-69-01-04 | Data Integrity | stale doc comments referencing deleted symbols | mitigate | user-management-api.ts L26 comment is the known lying-comment surface (BLOCKER 2). Post-plan grep gate is tightened to include comments (`grep -rn ... | grep -v node_modules` — no more `.test.` exclusion) so any future stale comment referencing the deleted picker would trip. |
| T-68-SC | Tampering | npm installs | mitigate | NO NEW PACKAGES. |
</threat_model>

<verification>
Scoped test run:
```bash
cd /home/ubuntu/skynet-tina && npx vitest run --related \
  src/backend/database/routes/identities.ts \
  src/backend/database/database.ts \
  src/ui/features/pretty-view/IdentityModal.tsx \
  src/ui/api/identities-api.ts \
  src/ui/api/user-management-api.ts
```

TSC repo-wide sanity check:
```bash
cd /home/ubuntu/skynet-tina && npx tsc --noEmit 2>&1 | tail -30
```

Grep-based dead-reference sweep (ALL must return 0 or empty). Note: NO `.test.` exclusion this time — the two picker test files are deleted in this plan, so any surviving hit (including in tests) is an escapee:
```bash
grep -rn "identity-share" src/ | grep -v ".planning/"
grep -rn "ShareIdentityPicker" src/
grep -rn "shareIdentity" src/
grep -rn "deleteIdentity" src/
grep -c "router.delete" src/backend/database/routes/identities.ts
grep -c "identityShareRoutes" src/backend/database/database.ts
grep -c "ShareIdentityPicker" src/ui/api/user-management-api.ts
```

Ship-gate (orchestrator, NOT executor): full `npx vitest run` green before docker build.
</verification>

<success_criteria>
- identity-share.ts + identity-share.test.ts + ShareIdentityPicker.tsx + ShareIdentityPicker.test.tsx + IdentityModal.share.test.tsx no longer exist on disk
- database.ts no longer imports or mounts identityShareRoutes
- identities.ts no longer contains a DELETE handler
- identities-api.ts no longer exports shareIdentity() or deleteIdentity() (and the ShareIdentityResponse type is gone)
- IdentityModal.tsx no longer imports ShareIdentityPicker or holds any Phase 38 state / callbacks
- user-management-api.ts no longer contains a comment referencing ShareIdentityPicker
- TSC repo-wide is clean
- Scoped vitest run is green
- Grep sweep for the deleted symbols returns 0 hits in src/ (excluding .planning/)
</success_criteria>

<output>
Create `.planning/phases/69-kill-the-identities-db-table-disk-becomes-sole-source-of-tru/69-01-SUMMARY.md` capturing:
- Which files were physically removed (rm) vs edited (Edit)
- Any surprises during TSC cleanup (e.g., a stray import somewhere unexpected)
- Confirmation that no lingering references to Phase 38 share flow remain outside .planning/ (source OR comment OR test)
- Handoff note to Wave 2: identities.ts still has POST + PUT /:id + GET / + GET /:id/avatar (unchanged); Wave 2 rewires PUT + GET-avatar to :identityKey and GET / to fanout
- Note on `/users/list-basic` backend route disposition — if getUsersListBasic has zero consumers after this plan, flag as future-cleanup candidate (out of Phase 69 scope)
</output>
</output>
