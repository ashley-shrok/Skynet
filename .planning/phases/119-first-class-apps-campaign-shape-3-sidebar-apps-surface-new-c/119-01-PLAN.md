---
phase: 119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/api/fleet-status-types.ts
  - src/ui/api/fleet-status-client.ts
  - src/ui/api/fleet-status-client.test.ts
autonomous: true
requirements:
  - D-14
  - D-16
tags:
  - frontend
  - websocket
  - types
must_haves:
  truths:
    - "Frontend FrontendOutboundFrame union includes app-snapshot, app-update, app-gone arms"
    - "createFleetStatusClient accepts onAppSnapshot / onAppUpdate / onAppGone optional callbacks"
    - "WS dispatch switch fires the matching callback when a real app frame arrives on the wire"
    - "Unknown frame types still hit the default branch without throwing"
  artifacts:
    - path: src/ui/api/fleet-status-types.ts
      provides: "AppState + FrontendAppSnapshotFrame + FrontendAppUpdateFrame + FrontendAppGoneFrame + widened FrontendOutboundFrame union"
      contains: "type: \"app-snapshot\""
    - path: src/ui/api/fleet-status-client.ts
      provides: "Three new switch cases + three optional callback fields on FleetStatusClientOptions"
      contains: "case \"app-snapshot\""
    - path: src/ui/api/fleet-status-client.test.ts
      provides: "Dispatch tests for each app frame arm"
      contains: "app-snapshot"
  key_links:
    - from: "src/ui/api/fleet-status-client.ts (onmessage switch)"
      to: "src/ui/api/fleet-status-types.ts (FrontendOutboundFrame union)"
      via: "type narrowing on parsed.type"
      pattern: "case \"app-(snapshot|update|gone)\""
    - from: "backend wire-protocol.ts AppStateSchema"
      to: "frontend fleet-status-types.ts AppState"
      via: "hand-maintained mirror (Pitfall 1 — MUST stay in sync)"
      pattern: "hostId, slug, title, description, port, hasIcon, createdAtMs, isHealthy, healthMessage"
---

<objective>
Close Pitfall 1 from RESEARCH.md: extend the frontend WS type mirror and dispatch switch so app-snapshot / app-update / app-gone frames from Phase 118's channel are recognised and dispatched (per D-14) rather than silently dropped in the switch default. Introduces no consumers yet — the store slice + wiring land in 119-02 and 119-03. This is the load-bearing first task the rest of the frontend chain depends on.

Purpose: Without this extension, every subsequent plan on the frontend can compile but observe no frames at runtime.

Output: Widened frontend type union + three new dispatch cases + three optional callbacks + tests proving each arm dispatches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c/119-CONTEXT.md
@.planning/phases/119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c/119-RESEARCH.md
@.planning/phases/119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c/119-PATTERNS.md
@.planning/campaigns/first-class-apps/shape-sidebar-apps-surface.md
@.planning/phases/118-first-class-apps-sweep-registry-shape-2/118-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend fleet-status-types.ts with AppState + three app frame arms + widened union</name>
  <files>src/ui/api/fleet-status-types.ts</files>
  <read_first>
    - src/ui/api/fleet-status-types.ts (whole file — current union definition around lines 293-338; find FRAME_SCHEMA_VERSION const and existing FrontendSnapshotFrame / FrontendUpdateFrame / FrontendGoneFrame / FrontendPongFrame / FrontendIdentityArchivedFrame declarations)
    - src/backend/fleet-status/wire-protocol.ts (lines 610-782 — the authoritative AppStateSchema at 632-644 and app-snapshot / app-update / app-gone frame schemas at 646-663 — the frontend interfaces must mirror these field-for-field)
    - .planning/phases/119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c/119-PATTERNS.md (section "src/ui/api/fleet-status-types.ts (MODIFY — add three frame arms)" — the exact new interfaces to add)
    - .planning/phases/119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c/119-CONTEXT.md D-14 (atomic reconciliation), D-16 (small standalone store slice)
  </read_first>
  <behavior>
    - `FrontendOutboundFrame` (as a TS union) accepts a discriminant `type: "app-snapshot"` with an `apps: AppState[]` field
    - `FrontendOutboundFrame` accepts `type: "app-update"` with an `app: AppState` field
    - `FrontendOutboundFrame` accepts `type: "app-gone"` with `hostId: string` + `slug: string` fields
    - `AppState` interface fields exactly mirror the backend `AppStateSchema` at wire-protocol.ts:632-644: `hostId: string; slug: string; title: string; description: string; port: number | null; hasIcon: boolean; createdAtMs: number; isHealthy: boolean; healthMessage: string | null;`
    - All new interfaces carry `schemaVersion: typeof FRAME_SCHEMA_VERSION` matching existing frontend frame interfaces
  </behavior>
  <action>Add the `AppState` interface plus three new exported interfaces `FrontendAppSnapshotFrame`, `FrontendAppUpdateFrame`, `FrontendAppGoneFrame` to `src/ui/api/fleet-status-types.ts`. Field types per D-14 mirror wire-protocol.ts:632-663 verbatim (hostId + slug as strings — matches backend; not number). Widen the existing `FrontendOutboundFrame` union type to include all three new arms in the same order they appear on the backend. Do NOT touch `SessionState`, `FrontendSnapshotFrame`, `FrontendUpdateFrame`, `FrontendGoneFrame`, `FrontendPongFrame`, or `FrontendIdentityArchivedFrame`. Do NOT introduce a helper function or a "widen later" placeholder — the union must be widened in this task per D-16. Place `AppState` immediately after `SessionState` for symmetry; place the three frame interfaces immediately after `FrontendIdentityArchivedFrame`.</action>
  <verify>
    <automated>npm run type-check 2>&amp;1 | grep -v '^#' | tee /tmp/119-01-tsc.log; test $(grep -c "fleet-status-types.ts" /tmp/119-01-tsc.log) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "export interface AppState" src/ui/api/fleet-status-types.ts` returns 1
    - Source: `grep -c "FrontendAppSnapshotFrame" src/ui/api/fleet-status-types.ts` returns at least 2 (declaration + union entry)
    - Source: `grep -c "FrontendAppUpdateFrame" src/ui/api/fleet-status-types.ts` returns at least 2
    - Source: `grep -c "FrontendAppGoneFrame" src/ui/api/fleet-status-types.ts` returns at least 2
    - Source: `grep -E '^\s*hostId: string;' src/ui/api/fleet-status-types.ts | wc -l` returns at least 2 (AppState + FrontendAppGoneFrame)
    - Source: `grep -c "healthMessage: string | null" src/ui/api/fleet-status-types.ts` returns 1
    - Behavior: `npm run type-check` reports zero errors originating from `fleet-status-types.ts`
  </acceptance_criteria>
  <done>The three app arms are declared in the frontend type mirror, and TypeScript reports no errors on the file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend fleet-status-client.ts with three optional callbacks + three dispatch cases + dispatch tests</name>
  <files>src/ui/api/fleet-status-client.ts, src/ui/api/fleet-status-client.test.ts</files>
  <read_first>
    - src/ui/api/fleet-status-client.ts (whole file — pay special attention to lines 66-82 for FleetStatusClientOptions shape, line 100 for the opts destructure, and lines 164-235 for the onmessage switch — the `identity-archived` case at ~216-231 is the cleanest analog for the new cases)
    - src/ui/api/fleet-status-client.test.ts (whole file — find the existing dispatch tests for snapshot / update / gone / identity-archived — the app-frame dispatch tests mirror them 1:1)
    - src/ui/api/fleet-status-types.ts (post-Task-1 — for the new AppState + frame arm types the switch narrows on)
    - .planning/phases/119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c/119-PATTERNS.md (section "src/ui/api/fleet-status-client.ts (MODIFY — add three switch cases + three callbacks)" — the exact case bodies including structured console.info calls)
  </read_first>
  <behavior>
    - Test: constructing the client with an `onAppSnapshot` callback and delivering a wire message `{schemaVersion:FRAME_SCHEMA_VERSION,type:"app-snapshot",apps:[{...}]}` fires the callback with the apps array
    - Test: `onAppUpdate` callback fires with the parsed AppState on a real `app-update` message
    - Test: `onAppGone` callback fires with `(hostId, slug)` on a real `app-gone` message
    - Test: omitting all three callbacks (they are optional) does NOT throw when app frames arrive — the switch case still runs the `console.info(...)` line and skips the callback
    - Test: unknown frame types still fall through to the existing default branch and are handled per the pre-existing behavior (no regression)
  </behavior>
  <action>Add three optional callbacks to `FleetStatusClientOptions` in `src/ui/api/fleet-status-client.ts` — `onAppSnapshot?: (apps: AppState[]) => void`, `onAppUpdate?: (app: AppState) => void`, `onAppGone?: (hostId: string, slug: string) => void`. Destructure all three on the existing `const { url, onSnapshot, ... } = opts;` line. Import `AppState` from `./fleet-status-types.js` alongside existing type imports. In the onmessage switch, add three new `case` blocks after `case "identity-archived":` and before the `default:` branch — one per frame type — each following the exact structured-logging + optional-callback pattern from PATTERNS.md section "src/ui/api/fleet-status-client.ts". Structured log operations MUST be `fleet_status_client_app_snapshot`, `fleet_status_client_app_update`, `fleet_status_client_app_gone` (per Shared Patterns in PATTERNS.md). Do NOT modify existing snapshot/update/gone/identity-archived/pong cases. Do NOT modify the default branch. Do NOT add a re-export of `AppState` from this module — it lives in `fleet-status-types.ts`. Then extend `src/ui/api/fleet-status-client.test.ts` with a `describe("app-frame dispatch (Phase 119)", ...)` block containing five tests: dispatch each of the three arms, omit-callback no-op, and unknown-type-default-passthrough regression. Reuse the existing mock-WebSocket harness pattern already in the file (do NOT invent a new harness).</action>
  <verify>
    <automated>npx vitest related --run src/ui/api/fleet-status-client.ts src/ui/api/fleet-status-client.test.ts src/ui/api/fleet-status-types.ts 2>&amp;1 | tee /tmp/119-01-vitest.log; grep -qE '(Tests +[0-9]+ passed|passed \([0-9]+\))' /tmp/119-01-vitest.log</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "onAppSnapshot" src/ui/api/fleet-status-client.ts` returns at least 3 (interface field + destructure + switch invocation)
    - Source: `grep -c "onAppUpdate" src/ui/api/fleet-status-client.ts` returns at least 3
    - Source: `grep -c "onAppGone" src/ui/api/fleet-status-client.ts` returns at least 3
    - Source: `grep -c 'case "app-snapshot":' src/ui/api/fleet-status-client.ts` returns 1
    - Source: `grep -c 'case "app-update":' src/ui/api/fleet-status-client.ts` returns 1
    - Source: `grep -c 'case "app-gone":' src/ui/api/fleet-status-client.ts` returns 1
    - Source: `grep -c "fleet_status_client_app_snapshot" src/ui/api/fleet-status-client.ts` returns 1
    - Source: `grep -c "fleet_status_client_app_update" src/ui/api/fleet-status-client.ts` returns 1
    - Source: `grep -c "fleet_status_client_app_gone" src/ui/api/fleet-status-client.ts` returns 1
    - Test: `npx vitest related --run src/ui/api/fleet-status-client.ts src/ui/api/fleet-status-client.test.ts` exits 0 and reports every new Phase-117 test as passing
    - Behavior: constructing the client without any of the three new callbacks and delivering an app-snapshot frame does NOT throw (they are optional)
    - Behavior: for each of the three app frame types, the corresponding callback fires exactly once per frame with the parsed payload
  </acceptance_criteria>
  <done>The frontend WS client recognises and dispatches all three app frame arms; existing session/identity-archived/pong dispatch is untouched; scoped tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS server → WS client | Backend-authored frames enter the browser JS runtime; frontend parses via existing JSON.parse + Zod-mirror discipline. No user-controlled data on this arm. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-01-01 | Tampering | fleet-status-types.ts frontend mirror drifts from backend wire-protocol.ts | mitigate | Field-for-field mirror validated against wire-protocol.ts:632-663 during Task 1; tests in Task 2 assert real frame shapes dispatch correctly (Pitfall 1 from RESEARCH.md). |
| T-119-01-02 | Denial of Service | Hostile server pushes malformed app frame → client crashes | mitigate | Existing Zod-parse discipline in the switch's parent path already handles malformed frames; new cases only run after `parsed.type` narrows to a valid arm. Unknown types still fall through to the default branch (regression-tested in Task 2). |
| T-119-01-03 | Information Disclosure | Frame carries a hostId the user isn't authorized for | accept | Backend already filters via `app-frame-filter.ts` (Phase 118 D-15); client does NOT re-check (per D-14 authority split). |
| T-119-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; no supply-chain surface. |
</threat_model>

<verification>
- `npm run type-check` reports zero errors from `fleet-status-types.ts` or `fleet-status-client.ts`
- `npx vitest related --run src/ui/api/fleet-status-client.ts src/ui/api/fleet-status-client.test.ts src/ui/api/fleet-status-types.ts` exits 0
- Frontend build does not regress: no removed imports break existing files that read from `fleet-status-types.ts`
</verification>

<success_criteria>
- All must_haves' truths are observable in code + tests (mirror declared, callbacks accepted, dispatch fires per frame type, unknown types unaffected)
- Zero changes to backend files, sweep script, or existing session-frame handling
- Scoped vitest run green; TypeScript clean
</success_criteria>

<output>
Create `.planning/phases/119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c/119-01-SUMMARY.md` when done, listing the extended interfaces + switch cases and confirming Task 2's five new tests pass.
</output>
