---
phase: 119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c
plan: 04
type: execute
wave: 3
depends_on:
  - 119-02
  - 119-03
files_modified:
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
tags:
  - frontend
  - integration
  - sidebar
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-15
must_haves:
  truths:
    - "The Apps section renders as the first content group below the search input and above the Pinned group in PrettyConversationsPanel.tsx (per D-01)"
    - "The section header mirrors the Archived section chrome verbatim: button + AppWindow icon + 'Apps' label + rule-line + rotating ChevronDown (per D-02)"
    - "The section is collapsed on every mount: `useState(false)` for appsExpanded (per D-03)"
    - "When collapsed, ZERO AppTile instances and no empty-state prompt are in the DOM — content lives inside the {appsExpanded && ...} gate (per D-03 lazy-render)"
    - "When expanded and appTiles.length === 0, the empty-state renders the string 'Ask an agent to make an app for you.' verbatim, italic, muted, inside the expanded region (per D-04)"
    - "The section is ALWAYS in the DOM regardless of appTiles.length — NOT gated on `> 0` (per D-05)"
    - "The section renders OUTSIDE the search-vs-three-zone ternary at PrettyConversationsPanel.tsx:1833 — visible during active search too (per RESEARCH.md Pitfall 2)"
  artifacts:
    - path: src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
      provides: "New .pv-apps-section group with appsExpanded state hook, AppWindow header, empty-state prompt, and AppTile list"
      contains: "pv-apps-section"
      contains_all:
        - "appsExpanded"
        - "useAppTiles"
        - "AppTile"
        - "AppWindow"
        - "Ask an agent to make an app for you."
  key_links:
    - from: "PrettyConversationsPanel.tsx (post-integration)"
      to: "useAppTiles hook (from app-tiles-store.ts, Plan 119-02)"
      via: "React hook call at panel body scope"
      pattern: "const appTiles = useAppTiles"
    - from: "PrettyConversationsPanel.tsx (post-integration)"
      to: "AppTile component (from AppTile.tsx, Plan 119-03)"
      via: "map over appTiles rendering <AppTile key={...} app={app}/>"
      pattern: "AppTile.*app=\\{app\\}"
    - from: "PrettyConversationsPanel.tsx section header"
      to: "lucide-react AppWindow icon"
      via: "import extension on the existing lucide-react import"
      pattern: "AppWindow.*from.*lucide-react|from.*lucide-react.*AppWindow"
---

<objective>
Integrate the AppTile component + useAppTiles hook into `PrettyConversationsPanel.tsx` as a new `.pv-apps-section` group. The section is the first content group under the search input (D-01), mirrors the Archived section chrome verbatim (D-02) but WITHOUT the outer `.length > 0` gate (D-05), collapsed on mount with lazy-render (D-03), shows the empty-state prompt when expanded with zero tiles (D-04), and renders OUTSIDE the search-vs-three-zone ternary so it stays visible when the user is searching (RESEARCH.md Pitfall 2).

Purpose: Concretises D-01/D-02/D-03/D-04/D-05 in the one file that owns the sidebar layout; the last frontend integration step before end-to-end painting is possible.

Output: One modified file with a new section block + one hook call + one lucide import extension + one useAppTiles/AppTile import.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c/117-CONTEXT.md
@.planning/phases/119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c/117-RESEARCH.md
@.planning/phases/119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c/117-PATTERNS.md
@.planning/campaigns/first-class-apps/shape-sidebar-apps-surface.md
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add appsExpanded state + useAppTiles hook + Apps section JSX block above the search-vs-three-zone ternary</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx</files>
  <read_first>
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (WHOLE file — critical line ranges: L59 for the lucide-react import that needs `AppWindow` added; L737 for the `archivedExpanded` state hook pattern; L1760-1799 for `.pv-panel-scroll` + search-container structure; L1809-1823 for the loading-strip block whose indentation the Apps section mirrors; L1833-1858 for the search-vs-three-zone ternary — Apps section renders BEFORE this line; L1995-2033 for the Archived section chrome to mirror)
    - src/ui/state/app-tiles-store.ts (post-119-02 — for the `useAppTiles` export path and return type)
    - src/ui/features/pretty-conversations/AppTile.tsx (post-119-03 — for the `AppTile` import path and `{ app: AppState }` prop shape)
    - .planning/phases/119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c/117-PATTERNS.md (section "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (MODIFY — Apps section chrome)" — the exact JSX block with the D-05 removal of the outer `.length > 0` gate and the D-04 empty-state prompt inside the expanded gate)
    - .planning/phases/119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c/117-CONTEXT.md D-01, D-02, D-03, D-04, D-05
    - .planning/phases/119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c/117-RESEARCH.md § Pitfall 2 (section MUST render outside the ternary)
  </read_first>
  <behavior>
    - When the panel mounts, the Apps section header is in the DOM immediately (D-05 always-present); the section body content is NOT in the DOM (D-03 collapsed by default + lazy-render)
    - Clicking the Apps section header toggles `appsExpanded` and mounts the section body content
    - When `appsExpanded === true` and `useAppTiles() === []`, the body renders exactly one child: `<div className="pv-apps-empty px-4 py-2 text-[13px] italic text-[#5c6070]/85">Ask an agent to make an app for you.</div>` (D-04 verbatim string)
    - When `appsExpanded === true` and `useAppTiles()` returns tiles, the body renders one `<AppTile key="${hostId}:${slug}" app={app}/>` per tile in the hook's returned order (already sorted per D-15 by the store hook)
    - Typing into the search input DOES NOT hide the Apps section — it remains rendered above the ternary
    - The Pinned group and the rest of the panel content are visually and logically unchanged
  </behavior>
  <action>Extend the lucide-react import at `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx:59` to include `AppWindow` alongside the existing icons (Archive, ChevronDown, etc.). Add new imports below the existing feature imports: `import { AppTile } from "./AppTile";` and `import { useAppTiles } from "../../state/app-tiles-store";` (verify exact relative-path form and `.js` suffix discipline against sibling imports — repo uses ESM-with-`.js` in some places, path aliases in others). Add near the existing `const [archivedExpanded, setArchivedExpanded] = useState(false);` at ~L737: `const [appsExpanded, setAppsExpanded] = useState(false);` and `const appTiles = useAppTiles();` — same call site, adjacent lines. Insert the Apps section JSX block BETWEEN the loading strip block at L1809-1823 and the search-vs-three-zone ternary at L1833, at the SAME JSX indentation as `{!fleetSessionsLoaded && (...)}` — outside the ternary (Pitfall 2). Use the exact block shape from PATTERNS.md section "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (MODIFY — Apps section chrome)" applied to Apps: NO outer `.length > 0` gate (D-05 differentiator), always render `<div className="pv-panel-group pv-apps-section">`, header button carrying `data-testid="pretty-conversations-apps-header"`, `aria-expanded={appsExpanded}`, `aria-controls="pv-apps-section-content"`; header contents = `<AppWindow className="size-3 text-[#5c6070]/85 shrink-0" aria-hidden="true"/>` + `<span className="text-[13px] font-semibold uppercase tracking-[0.08em] text-[#5c6070]/85 shrink-0">Apps</span>` + `<span aria-hidden="true" className="flex-1 h-px bg-[linear-gradient(90deg,rgba(255,255,255,0.06),transparent)]"/>` + `<ChevronDown className={\`size-3 text-[#5c6070]/85 shrink-0 transition-transform ${appsExpanded ? "rotate-180" : ""}\`} aria-hidden="true"/>`. Body: `{appsExpanded && (<div id="pv-apps-section-content">{appTiles.length === 0 ? <div className="pv-apps-empty px-4 py-2 text-[13px] italic text-[#5c6070]/85">Ask an agent to make an app for you.</div> : appTiles.map((app) => <AppTile key={\`${app.hostId}:${app.slug}\`} app={app}/>)}</div>)}`. Do NOT nest the Apps section inside the ternary (Pitfall 2 — would disappear on search). Do NOT gate the outer `<div className="pv-panel-group pv-apps-section">` on any length check (D-05). Do NOT bolt the useAppTiles call into an existing hook — it stands alone. Do NOT touch the Archived section block at L1995-2033 or the Pinned block or the loading strip block. Do NOT modify any test in the sibling test file — integration tests land in Plan 119-06.</action>
  <verify>
    <automated>npx vitest related --run src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx src/ui/features/pretty-conversations/AppTile.tsx src/ui/state/app-tiles-store.ts 2>&amp;1 | tee /tmp/119-04.log; grep -qE '(no tests|Tests +[0-9]+ passed|passed \([0-9]+\))' /tmp/119-04.log; npm run type-check 2>&amp;1 | tee /tmp/119-04-tsc.log; test $(grep -c "PrettyConversationsPanel.tsx" /tmp/119-04-tsc.log) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "pv-apps-section" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns at least 2 (wrapper + content id or class use)
    - Source: `grep -c "AppWindow" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns at least 2 (import + JSX)
    - Source: `grep -c "useAppTiles" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns at least 2 (import + call)
    - Source: `grep -c "AppTile" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns at least 2 (import + JSX; separate from AppWindow — count matches on word boundaries)
    - Source: `grep -c "appsExpanded" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns at least 4 (state declaration + setter call + aria-expanded + gate expression)
    - Source: `grep -c "Ask an agent to make an app for you\\." src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns 1 (D-04 verbatim)
    - Source: `grep -c 'data-testid="pretty-conversations-apps-header"' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns 1 (D-02 mirror of Archived testid pattern)
    - Source: `grep -c 'aria-controls="pv-apps-section-content"' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns 1
    - Source: NO outer `appTiles.length > 0` gate on the section wrapper — `grep -E 'appTiles\.length\s*>\s*0.*pv-apps-section|pv-apps-section.*appTiles\.length\s*>\s*0' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns nothing (D-05 lock)
    - Behavior: `npm run type-check` reports zero errors originating from `PrettyConversationsPanel.tsx`
    - Behavior: `git diff src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` shows the Apps section block appearing BEFORE the search-vs-three-zone ternary (line 1833 in the pre-edit file), not INSIDE either of its branches (Pitfall 2)
    - Behavior: `git diff src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` shows NO modifications to the Archived section block (~L1995-2033 pre-edit), the Pinned group, the loading strip, or the search-vs-three-zone ternary bodies — only additions above the ternary
    - Test: `npx vitest related --run src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx src/ui/features/pretty-conversations/AppTile.tsx src/ui/state/app-tiles-store.ts` exits 0 (existing panel tests must still pass — integration tests for the new section land in Plan 119-06)
  </acceptance_criteria>
  <done>The Apps section renders in the sidebar per D-01/D-02/D-03/D-04/D-05, above the ternary per Pitfall 2, existing panel content untouched, scoped tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Store hook → Panel render | `useAppTiles()` return value crosses into JSX; consumed only as read-only array + rendered via AppTile which owns its own escape discipline (Plan 119-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-04-01 | Tampering | Section rendered inside search-vs-three-zone ternary disappears on active search | mitigate | Task action mandates insertion BEFORE line 1833 outside the ternary; acceptance criterion verifies via git diff position (Pitfall 2). |
| T-119-04-02 | Tampering | Section gated on `appTiles.length > 0` and disappears when empty | mitigate | Grep-gated acceptance criterion forbids the length > 0 gate on the wrapper; D-05 lock. |
| T-119-04-03 | Injection (XSS) | Empty-state string rendered as JSX text | mitigate | D-04 literal string in JSX text node — React auto-escapes; no substitution or template. |
| T-119-04-04 | Tampering | Content rendered when collapsed (D-03 lazy-render violated) | mitigate | The `{appsExpanded && ...}` gate is explicit; Plan 119-06 integration tests verify no AppTile / empty-state in DOM when collapsed. |
| T-119-04-05 | Elevation of Privilege | Client accidentally re-filters tiles by host visibility | accept | Panel does not filter — hook returns whatever backend approved; per D-14. |
| T-119-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; `AppWindow` is an existing lucide-react export in v1.28.0 (RESEARCH.md Assumption A3 — LOW risk fallback: Boxes / Grid / LayoutGrid / Package are drop-in substitutes). |
</threat_model>

<verification>
- `npx vitest related --run src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx src/ui/features/pretty-conversations/AppTile.tsx src/ui/state/app-tiles-store.ts` exits 0
- `npm run type-check` reports zero errors originating from `PrettyConversationsPanel.tsx`
- `git diff src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` shows only additions (new imports, new state hook, new JSX block above the ternary) — no modifications to existing sections
</verification>

<success_criteria>
- All must_haves' truths are verifiable via source-grep + subsequent Plan 119-06 integration tests
- Existing Archived / Pinned / search / loading-strip / three-zone-view behaviors unchanged
- Frontend now paints tiles end-to-end when Phase 118's backend pushes a frame + when Plan 119-05's icon endpoint serves an icon
</success_criteria>

<output>
Create `.planning/phases/119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c/119-04-SUMMARY.md` when done, listing the added imports + state hook + JSX block location (relative to the ternary line) + a note that integration tests land in Plan 119-06.
</output>
