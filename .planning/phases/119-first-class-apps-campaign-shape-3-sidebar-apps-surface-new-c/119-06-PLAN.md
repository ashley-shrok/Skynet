---
phase: 119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c
plan: 06
type: execute
wave: 4
depends_on:
  - 119-04
  - 119-05
files_modified:
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
tags:
  - frontend
  - integration-test
  - lazy-render
requirements:
  - D-01
  - D-03
  - D-04
  - D-05
  - D-18
must_haves:
  truths:
    - "Integration test A15: Apps section header is in the DOM when appTiles is empty (D-05 always-present invariant)"
    - "Integration test A16: When the section is COLLAPSED (default mount state), ZERO AppTile instances and NO empty-state prompt are in the DOM (D-03 lazy-render invariant)"
    - "Integration test A17: Clicking the section header expands the section; the empty-state prompt 'Ask an agent to make an app for you.' becomes visible (D-04)"
    - "Integration test A18: With mockAppTiles populated and section expanded, each mock tile renders as an AppTile instance in the sorted order the hook returns (D-01 placement + D-15 hook order)"
    - "Integration test A19: Typing in the search input DOES NOT hide the Apps section header (RESEARCH.md Pitfall 2 — section renders outside the search-vs-three-zone ternary)"
    - "Integration test A20: The Apps section renders ABOVE the Pinned group (D-01 placement)"
    - "Full three-layer coverage (D-18) is realised: component tests exist (Plan 119-03), store tests exist (Plan 119-02), integration tests exist (this plan)"
  artifacts:
    - path: src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
      provides: "New describe block covering the Apps section integration (A15-A20) using a mockAppTiles module-level swap mirroring the mockArchivedFleetRows pattern"
      contains: "Phase 119 Apps section"
  key_links:
    - from: "PrettyConversationsPanel.test.tsx new describe block"
      to: "vi.mock('@/state/app-tiles-store', ...)"
      via: "module-level mock exposing a mutable mockAppTiles array"
      pattern: "vi\\.mock.*app-tiles-store"
    - from: "PrettyConversationsPanel.test.tsx new describe block"
      to: "PrettyConversationsPanel render + fireEvent"
      via: "render()/screen/fireEvent from @testing-library/react"
      pattern: "screen\\.getByTestId.*pretty-conversations-apps-header"
---

<objective>
Close D-18's three-layer testing requirement by adding integration coverage for the Apps section to `PrettyConversationsPanel.test.tsx`. Component-level coverage lives in Plan 119-03 (`AppTile.test.tsx`); store/subscription coverage lives in Plan 119-02 (`app-tiles-store.test.ts`). This plan adds A15-A20 tests to the existing panel integration test file (mirroring how Phase 115's A10-A14 landed) — verifying section-always-present (D-05), lazy-render (D-03), empty-state (D-04), populated ordering (D-01/D-15), search-safety (Pitfall 2), and position-above-Pinned (D-01 placement).

Purpose: Prove the integration boundary the visual + wire tests can't observe alone: that PrettyConversationsPanel weaves the store hook, the AppTile component, and the sidebar chrome together per the D-decision locks.

Output: New `describe("PrettyConversationsPanel: Phase 119 Apps section", ...)` block appended to the existing panel test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c/117-CONTEXT.md
@.planning/phases/119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c/117-RESEARCH.md
@.planning/phases/119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c/117-PATTERNS.md
@src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add A15-A20 integration tests for the Apps section (mirror A10-A14 Archived pattern)</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx (WHOLE file — pay special attention to: the vi.mock block for conversation-store at ~L244-330 with the `mockArchivedFleetRows` module-level array + `useArchivedFleetRows: () => mockArchivedFleetRows` mock entry, the render harness helper, the A10-A14 Archived-section describe block at ~L2625-2733 which is the exact structural mirror for this new block, and the `beforeEach` cleanup pattern)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (post-Plan-119-04 — for the exact data-testid + JSX structure the tests interrogate: `pretty-conversations-apps-header`, `pv-apps-section-content` id, `.pv-apps-empty` class, the Pinned group's identifier in the DOM)
    - src/ui/state/app-tiles-store.ts (post-Plan-119-02 — for the `useAppTiles` return type / export shape the mock replicates)
    - src/ui/features/pretty-conversations/AppTile.tsx (post-Plan-119-03 — for the `AppTile` render surface the integration tests observe; e.g. the tile's aria-label pattern for lookups)
    - .planning/phases/119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c/117-PATTERNS.md (section "PrettyConversationsPanel.test.tsx extension (MODIFY — integration)" — the exact mock swap pattern and A15/A16 test structure)
    - .planning/phases/119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c/117-CONTEXT.md D-01, D-03, D-04, D-05, D-18
    - .planning/phases/119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c/117-RESEARCH.md § Pitfall 2 (section MUST render outside ternary)
  </read_first>
  <behavior>
    - Test A15: With mockAppTiles=[] and a fresh panel mount, `screen.getByTestId("pretty-conversations-apps-header")` succeeds (section header IS present even with zero tiles) — D-05
    - Test A16: With mockAppTiles=[] and a fresh panel mount (collapsed by default), `screen.queryByText("Ask an agent to make an app for you.")` returns null and `screen.queryByRole("button", {name: /App tile:/})` returns null (content not in DOM when collapsed) — D-03 lazy-render
    - Test A17: With mockAppTiles=[] and after `fireEvent.click(screen.getByTestId("pretty-conversations-apps-header"))`, `screen.getByText("Ask an agent to make an app for you.")` succeeds — D-04
    - Test A18: With mockAppTiles=[appA, appB, appC] and section expanded, `screen.getAllByRole("button", {name: /App tile:/}).length === 3` and the returned elements appear in the same order the hook returned (mock is set to the pre-sorted array; the panel is expected to render in hook-return order — no additional sort in the panel)
    - Test A19: With mockAppTiles=[] on mount, typing into the search input (`fireEvent.change(screen.getByRole("searchbox"), { target: { value: "xyz" } })` — the panel's search-container at PrettyConversationsPanel.tsx:1760-1799 renders `<input type="search">` which RTL exposes via the "searchbox" role; if that lookup misses, fall back to `screen.getByPlaceholderText(/search/i)` per the panel's placeholder pattern) does NOT hide `screen.getByTestId("pretty-conversations-apps-header")` — D-05 + Pitfall 2
    - Test A20: With mockAppTiles=[appA] and section expanded, the DOM position of `pretty-conversations-apps-header` appears BEFORE the DOM position of the Pinned-group identifier (D-01 placement — Apps first)
  </behavior>
  <action>Extend `src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` with (1) a module-level `let mockAppTiles: AppState[] = [];` declaration near the sibling `mockArchivedFleetRows` block; (2) a `vi.mock("@/state/app-tiles-store", () => ({ useAppTiles: () => mockAppTiles, publishAppSnapshot: vi.fn(), publishAppUpdate: vi.fn(), publishAppGone: vi.fn() }))` (verify the mock path aliases against how conversation-store is mocked in the same file — repo may use `../../state/app-tiles-store` relative form; mirror what the panel actually imports); (3) `beforeEach(() => { mockAppTiles = []; })` inside the new describe; (4) a new describe block `describe("PrettyConversationsPanel: Phase 119 Apps section", () => { ... })` appended at the end of the file containing the six tests A15-A20 above. Use existing render harness helper conventions (whatever the Archived-section tests at L2625-2733 use). AppState fixture: `{ hostId: "1", slug: "scratch-test", title: "Scratch", description: "test", port: null, hasIcon: true, createdAtMs: 0, isHealthy: true, healthMessage: null }` — extend for multi-tile cases. For A20 use `Node.compareDocumentPosition` or `.compareDocumentPosition(otherEl) & Node.DOCUMENT_POSITION_FOLLOWING` to assert the Apps header comes before the Pinned-group identifier in DOM order (find the Pinned identifier by whatever data-testid or class the existing Pinned tests already use — grep the same file for existing Pinned lookups). Do NOT modify existing A10-A14 Archived tests or any other pre-existing test. Do NOT add tests that duplicate component-level coverage (icon/fallback, healthMessage, context-menu) — those live in AppTile.test.tsx per Plan 119-03. Do NOT add tests that duplicate store-level coverage (publish fns, sort tiebreak) — those live in app-tiles-store.test.ts per Plan 119-02. Do NOT add a full-suite invocation to the verify command (D-19 — orchestrator runs full suite pre-deploy, not the executor).</action>
  <verify>
    <automated>npx vitest related --run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx src/ui/state/app-tiles-store.ts src/ui/features/pretty-conversations/AppTile.tsx 2>&amp;1 | tee /tmp/119-06.log; grep -qE '(Tests +[0-9]+ passed|passed \([0-9]+\))' /tmp/119-06.log; grep -q "Phase 119 Apps section" /tmp/119-06.log</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "Phase 119 Apps section" src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` returns at least 1
    - Source: `grep -c "mockAppTiles" src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` returns at least 3 (declaration + reset + at-least-one test use)
    - Source: `grep -c "app-tiles-store" src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` returns at least 1 (vi.mock call)
    - Source: `grep -c "pretty-conversations-apps-header" src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` returns at least 3 (A15, A16, A19 lookups)
    - Source: `grep -c "Ask an agent to make an app for you\\." src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` returns at least 2 (A16 negative + A17 positive)
    - Source: existing A10-A14 Archived tests unchanged — `git diff src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` shows only appended new content plus the new mock swap, not modifications to prior tests
    - Test: `npx vitest related --run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` exits 0 with all six new A15-A20 tests passing AND all pre-existing A1-A14 tests still passing
    - Behavior: A16 concretely observes zero `AppTile` instances in the DOM when the section is collapsed (assert via `.queryAllByRole("button", {name: /App tile:/}).length === 0`)
    - Behavior: A20 uses `compareDocumentPosition` to assert the Apps header DOM node precedes the Pinned-group DOM node
  </acceptance_criteria>
  <done>Six new integration tests cover the Apps section per D-01/D-03/D-04/D-05/D-18; all pre-existing tests in the file continue to pass; three-layer coverage (component + store + integration) is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Not applicable — this plan adds tests only; no runtime code paths or data crossings.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-06-01 | Tampering | New tests break pre-existing Archived / Pinned / search tests via mock leakage | mitigate | `beforeEach(() => { mockAppTiles = []; })` inside the new describe scope; existing mockArchivedFleetRows block untouched (grep-verified). |
| T-119-06-02 | Denial of Service | New tests overshadow existing describes via naming collision | mitigate | Describe block name explicitly namespaced `"PrettyConversationsPanel: Phase 119 Apps section"` — no pre-existing describe uses that exact string. |
| T-119-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- `npx vitest related --run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx src/ui/state/app-tiles-store.ts src/ui/features/pretty-conversations/AppTile.tsx` exits 0
- Grep confirms A10-A14 Archived tests and the mockArchivedFleetRows swap are untouched
- New describe block appears at the end of the file, and prints "Phase 119 Apps section" in the vitest run output
</verification>

<success_criteria>
- All must_haves' truths are observable via passing tests
- D-18 three-layer coverage is complete across all six plans: component (119-03), store/subscription (119-02), integration (119-06); backend route (119-05)
- No modifications to existing tests
</success_criteria>

<output>
Create `.planning/phases/119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c/119-06-SUMMARY.md` when done, listing the six A15-A20 test IDs and a cross-reference showing which prior plan owns component-level (119-03), store-level (119-02), and route-level (119-05) coverage. Include a one-line agent-UAT reminder that D-20 requires post-deploy real-icon UAT with a scratch app on t1000 — this is the orchestrator's / campaign-close job, NOT this executor's job.
</output>
