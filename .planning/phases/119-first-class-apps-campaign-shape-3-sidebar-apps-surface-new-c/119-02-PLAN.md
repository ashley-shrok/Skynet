---
phase: 119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c
plan: 02
type: execute
wave: 2
depends_on:
  - 119-01
files_modified:
  - src/ui/state/app-tiles-store.ts
  - src/ui/state/app-tiles-store.test.ts
  - src/ui/AppShell.tsx
tags:
  - frontend
  - state-management
  - websocket
requirements:
  - D-14
  - D-15
  - D-16
  - D-17
must_haves:
  truths:
    - "Client store is populated atomically on every app-snapshot / app-update / app-gone frame (per D-14)"
    - "useAppTiles() returns the current tile list sorted by title (case-insensitive locale-aware) with ${hostId}:${slug} tiebreak (per D-15)"
    - "The store is a standalone module, NOT bolted onto any conversation/identity store (per D-16)"
    - "publishAppSnapshot replaces the whole map; publishAppUpdate upserts one key; publishAppGone deletes one key"
    - "Two apps with the same title on different hosts stay in stable order across an app-update frame (Pitfall 6 mitigation)"
    - "AppShell.tsx wires the three new callbacks on createFleetStatusClient to the store's publish functions"
  artifacts:
    - path: src/ui/state/app-tiles-store.ts
      provides: "In-memory Map<hostId:slug, AppState> + publish fns + useAppTiles hook + __resetForTest"
      contains: "publishAppSnapshot"
    - path: src/ui/state/app-tiles-store.test.ts
      provides: "Unit tests for publish fns + hook memo + sort stability (Pitfall 6 regression)"
      contains: "same title on different hosts"
    - path: src/ui/AppShell.tsx
      provides: "createFleetStatusClient wires onAppSnapshot / onAppUpdate / onAppGone → publish fns"
      contains: "publishAppSnapshot"
  key_links:
    - from: "src/ui/AppShell.tsx createFleetStatusClient call"
      to: "src/ui/state/app-tiles-store.ts publishAppSnapshot/Update/Gone"
      via: "callback wiring in the client options object"
      pattern: "onAppSnapshot: \\(apps\\) => \\{ publishAppSnapshot"
    - from: "AppTile / PrettyConversationsPanel consumers"
      to: "src/ui/state/app-tiles-store.ts useAppTiles hook"
      via: "React hook + useSyncExternalStore subscription"
      pattern: "useAppTiles"
---

<objective>
Add a standalone in-memory store slice for app tiles at `src/ui/state/app-tiles-store.ts` that consumes the three app frames extended by 119-01, exposes a `useAppTiles()` React hook returning a stably-sorted tile array, and wire the frontend WS client in AppShell.tsx to dispatch each frame to the matching publish function. Establishes the data source that 119-03 (AppTile component) and 119-04 (PrettyConversationsPanel integration) will consume.

Purpose: Concretises D-14 atomic-reconciliation + D-15 stable sort + D-16 standalone store slice + D-17 no pre-first-frame state. Every load-bearing runtime behaviour of the tile list lives here.

Output: New store module + tests + AppShell wiring. No UI surface yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c/117-CONTEXT.md
@.planning/phases/119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c/117-RESEARCH.md
@.planning/phases/119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c/117-PATTERNS.md
@.planning/campaigns/first-class-apps/shape-sidebar-apps-surface.md
@src/ui/state/session-working-store.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create app-tiles-store.ts (Map + listeners + publish fns + useAppTiles hook) with tests</name>
  <files>src/ui/state/app-tiles-store.ts, src/ui/state/app-tiles-store.test.ts</files>
  <read_first>
    - src/ui/state/session-working-store.ts (WHOLE file — this is the exact analog; pay attention to the state shape at ~207-225, the notify/subscribe pair, the publishFleetStatusSessionGone shape at ~731-751 for the delete pattern, the useSessionIsWorking hook shape at ~946-953 for the useSyncExternalStore reader, and the `__resetForTest` export at ~1151-1154)
    - src/ui/state/session-working-store.test.ts (WHOLE file — the mirror test structure this new test file adopts)
    - src/ui/api/fleet-status-types.ts (post-119-01 — for the AppState import)
    - .planning/phases/119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c/117-PATTERNS.md (section "src/ui/state/app-tiles-store.ts (NEW — client subscription slice)" — the exact publish/hook shape with sort comparator)
    - .planning/phases/119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c/117-CONTEXT.md D-14, D-15, D-16, D-17
  </read_first>
  <behavior>
    - Test: `useAppTiles()` returns `[]` initially (empty map)
    - Test: `publishAppSnapshot([appA, appB, appC])` then `useAppTiles()` returns all three, sorted by title case-insensitively
    - Test: `publishAppSnapshot([appA, appB])` REPLACES the map — a prior appC is gone
    - Test: `publishAppUpdate(existingApp)` (same `${hostId}:${slug}` key) upserts — no duplication, isHealthy/title changes reflect
    - Test: `publishAppUpdate(newApp)` inserts a new key
    - Test: `publishAppGone(hostId, slug)` removes a key that exists; is a no-op when the key is absent (does not throw)
    - Test: sort tie-break — two apps with title "Weather" on hosts "1" and "2" stay in the same order after `publishAppUpdate` on one of them (Pitfall 6 mitigation)
    - Test: sort is locale-aware case-insensitive — "apple" < "Banana" < "cherry" (base sensitivity)
    - Test: `subscribe()` returns a disposer that removes the listener from the listener set
    - Test: `__resetForTest()` clears state and notifies (used by other tests to reset between suites)
  </behavior>
  <action>Create `src/ui/state/app-tiles-store.ts` using the pattern in PATTERNS.md section "src/ui/state/app-tiles-store.ts (NEW — client subscription slice)". Store shape: `let state = { map: new Map<string, AppState>() };` keyed by `${hostId}:${slug}`; `snapshotVersion` counter; `listeners: Set<() => void>`; `notify()` increments the counter and calls every listener; `subscribe(cb)` adds and returns a disposer that deletes. Publish fns: `publishAppSnapshot(apps)` REPLACES the map (build a fresh Map, iterate, set new state, notify); `publishAppUpdate(app)` clones the current map, sets the `${hostId}:${slug}` key, replaces state, notifies; `publishAppGone(hostId, slug)` early-returns if the key is absent, otherwise clones map + deletes + replaces state + notifies. Reader: `useAppTiles(): AppState[]` uses `useSyncExternalStore(subscribe, getMapSnapshot, getMapSnapshot)` where `getMapSnapshot` returns `state.map` as `ReadonlyMap`, then applies `useMemo(() => Array.from(snapshot.values()).sort(comparator), [snapshot])`. Comparator MUST be `(a, b) => a.title.localeCompare(b.title, undefined, { sensitivity: "base" }) || \`${a.hostId}:${a.slug}\`.localeCompare(\`${b.hostId}:${b.slug}\`)` per D-15 + Pitfall 6. Export `__resetForTest()` that reinitialises state and notifies. Import `AppState` from `../api/fleet-status-types.js`. Then create `src/ui/state/app-tiles-store.test.ts` covering the ten behaviours above; reuse the harness convention from `session-working-store.test.ts` (renderHook from @testing-library/react is fine); each test calls `__resetForTest()` in a `beforeEach`. Do NOT persist to localStorage / sessionStorage / IndexedDB. Do NOT add throttling / debouncing / batching to publish fns — atomic per-frame reconciliation per D-14. Do NOT expose the internal `state` variable as a mutable export.</action>
  <verify>
    <automated>npx vitest related --run src/ui/state/app-tiles-store.ts src/ui/state/app-tiles-store.test.ts 2>&amp;1 | tee /tmp/119-02-store.log; grep -qE '(Tests +[0-9]+ passed|passed \([0-9]+\))' /tmp/119-02-store.log</automated>
  </verify>
  <acceptance_criteria>
    - Source: `test -f src/ui/state/app-tiles-store.ts` exits 0
    - Source: `grep -c "^export function publishAppSnapshot" src/ui/state/app-tiles-store.ts` returns 1
    - Source: `grep -c "^export function publishAppUpdate" src/ui/state/app-tiles-store.ts` returns 1
    - Source: `grep -c "^export function publishAppGone" src/ui/state/app-tiles-store.ts` returns 1
    - Source: `grep -c "^export function useAppTiles" src/ui/state/app-tiles-store.ts` returns 1
    - Source: `grep -c "^export function __resetForTest" src/ui/state/app-tiles-store.ts` returns 1
    - Source: `grep -c "useSyncExternalStore" src/ui/state/app-tiles-store.ts` returns at least 1
    - Source: `grep -c 'sensitivity: "base"' src/ui/state/app-tiles-store.ts` returns 1
    - Source: `grep -c "localStorage\\|sessionStorage\\|indexedDB" src/ui/state/app-tiles-store.ts` returns 0 (no persistence — D-14 atomic model)
    - Behavior: `publishAppSnapshot([{title:"Weather",hostId:"1",slug:"w",...},{title:"Weather",hostId:"2",slug:"w",...}])` followed by `publishAppUpdate({title:"Weather",hostId:"2",slug:"w",isHealthy:false,...})` leaves useAppTiles() returning host-1's tile at index 0 and host-2's tile at index 1 (stable order across update)
    - Test: `npx vitest related --run src/ui/state/app-tiles-store.ts src/ui/state/app-tiles-store.test.ts` exits 0 with all ten behavior tests passing
  </acceptance_criteria>
  <done>Store slice exists as a standalone module with atomic-reconcile publish fns, stably-sorted reader hook, and unit tests covering both dispatch paths and sort stability.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire AppShell.tsx createFleetStatusClient callbacks to app-tiles-store publish fns</name>
  <files>src/ui/AppShell.tsx</files>
  <read_first>
    - src/ui/AppShell.tsx (lines 60-120 for the import block; lines 620-680 for the createFleetStatusClient({...}) call — the exact site that gets the three new callbacks; look at the shape of the existing onSnapshot / onUpdate / onGone / onIdentityArchived callbacks as the analog)
    - src/ui/api/fleet-status-client.ts (post-119-01 — for the FleetStatusClientOptions shape confirming onAppSnapshot/Update/Gone are optional and their exact signatures)
    - src/ui/state/app-tiles-store.ts (post-Task-1 above — for the publish fn exports and their signatures)
    - .planning/phases/119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c/117-PATTERNS.md (section "src/ui/AppShell.tsx (MODIFY — wire callbacks to publish fns)" — the exact wiring shape)
  </read_first>
  <behavior>
    - `createFleetStatusClient({...})` in AppShell.tsx is invoked with an options object that includes `onAppSnapshot`, `onAppUpdate`, and `onAppGone` callback fields
    - Each callback body calls the matching publish function from `app-tiles-store` with the frame payload passed through unchanged
    - Existing `onSnapshot`, `onUpdate`, `onGone`, `onIdentityArchived` callbacks remain byte-identical (no accidental refactoring during the edit)
  </behavior>
  <action>Add an import to `src/ui/AppShell.tsx` for `publishAppSnapshot, publishAppUpdate, publishAppGone` from the new `@/state/app-tiles-store` path (match the alias style used by other imports in the file — likely relative `./state/app-tiles-store.js` given the ESM discipline; verify against sibling imports of state stores). In the `createFleetStatusClient({...})` call at ~L626-670, add three fields alongside the existing callbacks: `onAppSnapshot: (apps) => { publishAppSnapshot(apps); }`, `onAppUpdate: (app) => { publishAppUpdate(app); }`, `onAppGone: (hostId, slug) => { publishAppGone(hostId, slug); }`. Do NOT modify the bodies of `onSnapshot`, `onUpdate`, `onGone`, or `onIdentityArchived`. Do NOT open a new WebSocket — the same `/fleet-status/ws` connection is already carrying app frames per Phase 118 D-14. Do NOT re-check host visibility on the client — backend is the authority per D-14 and the RESEARCH.md anti-pattern. Do NOT introduce any error-boundary / try-catch wrap on the callback bodies — the publish fns are pure map mutations + notify, no failure surface.</action>
  <verify>
    <automated>npx vitest related --run src/ui/AppShell.tsx src/ui/state/app-tiles-store.ts src/ui/api/fleet-status-client.ts 2>&amp;1 | tee /tmp/119-02-shell.log; grep -qE '(no tests|Tests +[0-9]+ passed|passed \([0-9]+\))' /tmp/119-02-shell.log; npm run type-check 2>&amp;1 | tee /tmp/119-02-tsc.log; test $(grep -c "AppShell.tsx" /tmp/119-02-tsc.log) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "publishAppSnapshot" src/ui/AppShell.tsx` returns at least 2 (import + wiring)
    - Source: `grep -c "publishAppUpdate" src/ui/AppShell.tsx` returns at least 2
    - Source: `grep -c "publishAppGone" src/ui/AppShell.tsx` returns at least 2
    - Source: `grep -c "onAppSnapshot:" src/ui/AppShell.tsx` returns 1
    - Source: `grep -c "onAppUpdate:" src/ui/AppShell.tsx` returns 1
    - Source: `grep -c "onAppGone:" src/ui/AppShell.tsx` returns 1
    - Source: `grep -c "app-tiles-store" src/ui/AppShell.tsx` returns at least 1
    - Behavior: `npm run type-check` reports zero errors originating from `AppShell.tsx`
    - Behavior: existing `onSnapshot` / `onUpdate` / `onGone` / `onIdentityArchived` callback bodies at the same call site are byte-unchanged (`git diff src/ui/AppShell.tsx` should only add lines around the new callbacks, not modify existing ones)
    - Test: `npx vitest related --run src/ui/AppShell.tsx` exits 0 (or reports "no tests" if no direct AppShell test exists; adjacent test files continue to pass)
  </acceptance_criteria>
  <done>AppShell.tsx wires the three new callbacks; TypeScript is clean; existing session/identity dispatch is untouched; the frontend chain is now: WS → client dispatch → AppShell callbacks → publish fns → store → hook consumers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS callbacks → store map | Data crossing from WS-parsed frame into JS Map state; no user input on this arm |
| Store map → React consumers | Read-only via useSyncExternalStore; consumers cannot mutate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-02-01 | Tampering | Store slice bolted onto conversation-store | mitigate | D-16 explicit — new standalone module at `src/ui/state/app-tiles-store.ts`; task action forbids mixing. |
| T-119-02-02 | Denial of Service | Rapid frame stream thrashes the sort → DOM shuffle | mitigate | D-15 stable comparator with `${hostId}:${slug}` tiebreak; regression-tested in Task 1 (Pitfall 6). |
| T-119-02-03 | Information Disclosure | Client re-filters host visibility, drifts from backend | mitigate | Task 2 action forbids client-side host-access re-check; backend `app-frame-filter.ts` remains sole authority (per D-14 + Phase 118 D-15). |
| T-119-02-04 | Tampering | Store persists to localStorage → stale data across sessions | mitigate | Grep-gated in acceptance criteria to zero occurrences of localStorage/sessionStorage/indexedDB; matches D-14 atomic-per-frame model. |
| T-119-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- `npx vitest related --run src/ui/state/app-tiles-store.ts src/ui/state/app-tiles-store.test.ts src/ui/AppShell.tsx src/ui/api/fleet-status-client.ts` exits 0
- `npm run type-check` reports zero errors from files touched in this plan
- Store test file exercises: snapshot-replace, update-upsert, update-insert, gone-delete, gone-noop-on-absent, sort case-insensitive, sort tiebreak stability, subscribe-dispose, resetForTest
</verification>

<success_criteria>
- All must_haves' truths are observable via tests and code
- Zero touches to conversation-store / session-working-store / any identity store
- No new WebSocket opened; existing `/fleet-status/ws` client is the sole channel
</success_criteria>

<output>
Create `.planning/phases/119-first-class-apps-campaign-shape-3-sidebar-apps-surface-new-c/119-02-SUMMARY.md` when done, listing the store module surface (publish fns + hook + resetForTest) and confirming AppShell.tsx wiring.
</output>
