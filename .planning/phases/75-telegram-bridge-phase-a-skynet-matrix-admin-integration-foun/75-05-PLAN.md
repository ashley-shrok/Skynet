---
phase: 75-telegram-bridge-phase-a-skynet-matrix-admin-integration-foun
plan: 05
type: execute
wave: 3
depends_on:
  - 75-02
  - 75-04
files_modified:
  - src/backend/matrix/matrix-admin-client.integration.test.ts
  - substrate/skills/agent-relay/SKILL.md
autonomous: false
requirements:
  - MXA-01
  - MXA-06
tags:
  - matrix
  - integration-test
  - skill-docs
user_setup:
  - service: "@skynet-admin credential ingestion"
    why: "The integration test needs the admin creds actually populated in Skynet's DB (matrix_admin_creds row) — the Wave-3 human step is running the one-shot ingestion from ~/.claude/roles/box-maintainer/bounties/skynet-matrix-admin-integration/credentials.txt via a REPL call to setMatrixAdminCreds({...}) OR a small ingestion script the executor writes."
    env_vars:
      - name: INTEGRATION_TESTS
        source: "Set INTEGRATION_TESTS=1 in the shell before running the integration test suite; unset it in normal CI so the file skips."
    dashboard_config:
      - task: "Verify @skynet-admin still has admin:true on the Synapse relay"
        location: "curl -s -H 'Authorization: Bearer <token>' http://100.113.23.63:8008/_synapse/admin/v2/users/@skynet-admin:thenasty.taild9b663.ts.net | grep admin"

must_haves:
  truths:
    - "The end-to-end integration test proves the admin foundation delivers what Phase B needs — mint a fresh token via loginAsUser and successfully send an m.room.message with it"
    - "Test is gated behind INTEGRATION_TESTS=1 so normal CI does not need Synapse reachability"
    - "substrate/skills/agent-relay/SKILL.md gets a light note documenting Skynet's new admin role (Phase A) — one paragraph, not a rewrite"
    - "The Phase A completion criterion is verifiable end-to-end: creating a Skynet agent-identity creates its relay account in the same operation (via 75-04 smoke), human mxid registration works (via 75-03 supertest), Skynet can force-manage any relay room via admin API (via 75-02 unit + this integration test)"
  artifacts:
    - path: src/backend/matrix/matrix-admin-client.integration.test.ts
      provides: "End-to-end integration test: loginAsUser → send m.room.message as the minted user"
      exports: []
      contains: "INTEGRATION_TESTS"
    - path: substrate/skills/agent-relay/SKILL.md
      provides: "One-paragraph note reflecting Skynet's new admin role over the relay (Phase A)"
      contains: "Phase 75\\|Skynet admin"
  key_links:
    - from: "src/backend/matrix/matrix-admin-client.integration.test.ts"
      to: "src/backend/matrix/matrix-admin-client.ts loginAsUser"
      via: "direct import + call against the live thenasty Synapse"
      pattern: "loginAsUser.*await"
    - from: "src/backend/matrix/matrix-admin-client.integration.test.ts"
      to: "Synapse /_matrix/client/v3/rooms/.../send"
      via: "fetch with the freshly-minted access token"
      pattern: "rooms/.*send"
---

<objective>
Verify the Phase 75 admin foundation delivers what Phase B needs by running an end-to-end integration test that: (1) mints a fresh access_token for a real Matrix user via `loginAsUser`, (2) uses that token to POST an m.room.message to a Matrix room, (3) confirms the message lands. If this test passes against the live thenasty Synapse (100.113.23.63:8008), we know the admin foundation is sufficient for Phase B's Telegram bridge substrate promotion to hang off — the whole reason Phase 75 exists.

Purpose: this is the Phase A completion-criterion proof. RESEARCH.md § Open Question 5 explicitly recommends this test: "Include one end-to-end integration test in Phase A that exercises loginAsUser(mxid) → use returned token to send m.room.message → confirm the send lands. This proves the admin foundation delivers what Phase B needs, and catches any admin-permission gotchas early."

Also land the MXA-06 doc update: a light one-paragraph note in `substrate/skills/agent-relay/SKILL.md` reflecting that Skynet now owns admin capability over the relay. Not a rewrite — the shape explicitly says "may want light updates … to reflect Skynet's admin role once it lands."

Output: one gated integration test file + one skill doc update + a human checkpoint that confirms the ingestion of @skynet-admin creds happened + the test ran green + the doc reads correctly. NO frontend UI changes (Phase A scope-out).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-telegram-bridge-phase-a-skynet-matrix-admin-integration-foun/75-CONTEXT.md
@.planning/phases/75-telegram-bridge-phase-a-skynet-matrix-admin-integration-foun/75-RESEARCH.md
@.planning/phases/75-telegram-bridge-phase-a-skynet-matrix-admin-integration-foun/75-PATTERNS.md
@.planning/phases/75-telegram-bridge-phase-a-skynet-matrix-admin-integration-foun/75-02-SUMMARY.md
@.planning/phases/75-telegram-bridge-phase-a-skynet-matrix-admin-integration-foun/75-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: End-to-end integration test — loginAsUser then send m.room.message</name>
  <files>src/backend/matrix/matrix-admin-client.integration.test.ts</files>
  <read_first>
    - src/backend/matrix/matrix-admin-client.ts (from Plan 02 — the loginAsUser primitive)
    - src/backend/matrix/matrix-admin-creds-store.ts (from Plan 01 — getMatrixAdminCreds for reading homeserver + admin token)
    - .planning/phases/75-telegram-bridge-phase-a-skynet-matrix-admin-integration-foun/75-RESEARCH.md § Open Questions #5 (the recommended integration test) + § Environment Availability (Synapse reachability status) + § Ground-truth verification (confirmed @skynet-admin admin:true on 2026-09-06)
    - .planning/phases/75-telegram-bridge-phase-a-skynet-matrix-admin-integration-foun/75-PATTERNS.md § matrix-admin-client.integration.test.ts row (gated integration test pattern)
    - substrate/skills/agent-relay/SKILL.md (relay conventions — rooms, message shape)
    - substrate/skills/agent-relay/send.sh (existing helper that sends m.room.message — mirror the URL shape and body shape for the send step)
  </read_first>
  <behavior>
    - When INTEGRATION_TESTS is NOT set: the entire test file skips (describe.skipIf or a top-level if-guard).
    - When INTEGRATION_TESTS=1: test opens a real HTTP connection to http://100.113.23.63:8008 and:
      1. Calls `loginAsUser("@skynet-admin:thenasty.taild9b663.ts.net")` — verifies we get {ok:true, accessToken:"syt_..."}. (Test uses the admin account as the "impersonated" user for simplicity; a fresh mxid is not required since we're proving the primitive works.)
      2. Uses the returned accessToken to POST `PUT /_matrix/client/v3/rooms/<test-room>/send/m.room.message/<txnId>` with body `{msgtype:"m.text", body:"Phase 75 integration test <ISO timestamp>"}` — verifies response.ok.
      3. Verifies the message landed by GETting `/_matrix/client/v3/rooms/<test-room>/messages?dir=b&limit=1` with the same token and asserting the returned event's `content.body` matches what we sent.
      4. Cleans up: no cleanup needed — messages are ephemeral test noise in a dedicated integration-test room.
    - Test uses a dedicated integration-test room whose room_id is read from env var `MATRIX_INTEGRATION_ROOM_ID` (falls back to a default the executor picks + documents in the SUMMARY — likely a fresh room created during Wave 3 setup).
  </behavior>
  <action>
    Create `src/backend/matrix/matrix-admin-client.integration.test.ts` (implements OQ5 recommendation from RESEARCH.md § Open Questions #5). At the top of the file, gate the entire describe block behind `process.env.INTEGRATION_TESTS === "1"` — use `describe.skipIf(!gate)("matrix-admin-client integration (live thenasty Synapse)", () => { ... })` where `gate = process.env.INTEGRATION_TESTS === "1"`. If Vitest 4.x does not expose `describe.skipIf` in this repo's version, fall back to `describe.skip` behind an `if (!gate)` sibling; executor picks the cleanest form based on what's already used elsewhere in the codebase.

    Inside the gated describe, add ONE test `it("mints a token via loginAsUser then sends + reads an m.room.message", async () => { ... })` that runs the four-step behavior above. Use `loginAsUser` from the matrix-admin-client + native fetch for the send + read.

    Requirements:
    - The test file MUST NOT run in the default `npm test` suite. Gate via env flag (also mirror this decision in package.json's scripts if a `test:integration` script already exists — check first, add only if the pattern is already established).
    - Test needs the @skynet-admin creds actually populated in the Skynet DB (matrix_admin_creds singleton row). The Wave 3 checkpoint task confirms this ingestion happened before running the test.
    - Failure modes:
      - If getMatrixAdminCreds returns null → test fails with a message telling the operator to run the ingestion step.
      - If loginAsUser returns !ok → test fails with the status + error code (proves @skynet-admin no longer has admin:true, or the token was rotated).
      - If the send/read round-trip mismatches → test fails with the diff (proves an admin-permission gotcha or a Synapse regression).
    - NEVER log the accessToken in the test output. Log operation names + response statuses only.
    - Timeout: 60_000 ms for the whole test — network round-trips over tailnet are typically < 1 s but be generous.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina && npx vitest run src/backend/matrix/matrix-admin-client.integration.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "INTEGRATION_TESTS" src/backend/matrix/matrix-admin-client.integration.test.ts` returns >= 1.
    - `grep -c "describe.skipIf\|if.*!gate\|if.*!process.env.INTEGRATION_TESTS" src/backend/matrix/matrix-admin-client.integration.test.ts` returns >= 1.
    - `grep -c "loginAsUser" src/backend/matrix/matrix-admin-client.integration.test.ts` returns >= 1.
    - `grep -c "m.room.message\|rooms/.*send\|rooms/.*messages" src/backend/matrix/matrix-admin-client.integration.test.ts` returns >= 2 (send + read).
    - `INTEGRATION_TESTS=0 npx vitest run src/backend/matrix/matrix-admin-client.integration.test.ts` exits 0 with all tests SKIPPED (no network calls).
    - `grep -c "accessToken\|token" src/backend/matrix/matrix-admin-client.integration.test.ts | awk '{if ($1 <= 8) print "PASS: token references bounded"; else print "FAIL: token referenced " $1 " times, review for leaks"}'` — bounded token references (used, never logged).
  </acceptance_criteria>
  <done>The integration test file exists, is gated (does not run in default CI), and when the human runs it with INTEGRATION_TESTS=1 against the live thenasty Synapse it proves the loginAsUser + send-message round trip.</done>
</task>

<task type="auto">
  <name>Task 2: Light update to agent-relay/SKILL.md documenting Skynet's admin role</name>
  <files>substrate/skills/agent-relay/SKILL.md</files>
  <read_first>
    - substrate/skills/agent-relay/SKILL.md (the entire file — locate the natural section to add the paragraph, likely near the "trust posture" or "who owns what" area)
    - .planning/phases/75-telegram-bridge-phase-a-skynet-matrix-admin-integration-foun/75-CONTEXT.md § Prior context (context on the Skynet/relay/bridge/admin history the paragraph should reflect)
    - .planning/phases/75-telegram-bridge-phase-a-skynet-matrix-admin-integration-foun/75-RESEARCH.md § State of the Art (Nina bridge → admin foundation transition)
  </read_first>
  <action>
    Add ONE paragraph to `substrate/skills/agent-relay/SKILL.md` in the most natural existing section (executor's judgment — likely near the trust-posture or ownership discussion). Content:

    - State that Skynet now holds admin capability over the relay (as of Phase 75, Sep 2026).
    - Note the practical implication for agents using this skill: agents' relay accounts CAN be created by Skynet during identity birth (writing relay.json to the agent's identity folder), in addition to the existing self-register path via `/id <name>`. The existing self-register path is UNTOUCHED — nothing about how the skill works today changes.
    - Note that human relay accounts remain externally created (owned by the human), and Skynet stores only the mxid mapping.
    - Reference Phase 75 by number and date. Keep the paragraph to 3-5 sentences.

    Do NOT rewrite existing content. Do NOT add a new top-level section unless the existing structure genuinely calls for one. Do NOT touch send.sh, recv.sh, or any other script in the skill directory — those are Phase B concerns.

    Do NOT surface as a "coming soon" — the admin foundation ships in this phase. Do NOT mention Phase B — that's premature (roadmap addition happens after Phase A completes).
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina && grep -c "Phase 75\|Skynet admin\|skynet-admin" substrate/skills/agent-relay/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Ec "Phase 75|Skynet admin|@skynet-admin" substrate/skills/agent-relay/SKILL.md` returns >= 1 after the edit.
    - Line count of SKILL.md increased by 3-10 lines (one paragraph, not a rewrite).
    - `grep -c "self-register\|/id <name>" substrate/skills/agent-relay/SKILL.md` — count unchanged from pre-edit (we did not touch the self-register documentation).
    - Manual read confirms the paragraph accurately reflects the phase's scope and reads coherently in-context.
  </acceptance_criteria>
  <done>The skill file has a coherent one-paragraph note about Skynet's admin role; no other skill content disturbed; agents reading this skill see the current state accurately.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human checkpoint — @skynet-admin creds ingested, integration test green, doc reads correctly</name>
  <what-built>Wave 3 lands two things: an end-to-end integration test (gated behind INTEGRATION_TESTS=1) that proves the admin foundation delivers what Phase B needs, and a light update to substrate/skills/agent-relay/SKILL.md documenting Skynet's new admin role. Before this checkpoint releases, the executor must have also completed the one-shot ingestion of @skynet-admin creds from ~/.claude/roles/box-maintainer/bounties/skynet-matrix-admin-integration/credentials.txt into the matrix_admin_creds table (via a REPL call to setMatrixAdminCreds({...}) or a small ingestion script) — this is a runbook step that turns the parked bounty credentials into a live entry Skynet's admin client can use.</what-built>
  <how-to-verify>
    1. On the Skynet t1000 backend, run: `INTEGRATION_TESTS=1 npx vitest run src/backend/matrix/matrix-admin-client.integration.test.ts` — expect green with 1 passing test (the loginAsUser + send/read round trip against live thenasty Synapse). Any failure means either the ingestion step didn't happen, the admin creds are stale, or Synapse changed in a breaking way.
    2. Run: `curl -sSf http://100.113.23.63:8008/_synapse/admin/v1/server_version` — expect `{"server_version":"1.157.2"}` (or newer). If Synapse is unreachable from t1000, the integration test can't pass — check tailnet.
    3. Verify the creds are in the DB (as an operator, not as a plaintext leak): `sqlite3 /path/to/skynet.db "SELECT id, user_id, homeserver_base FROM matrix_admin_creds"` — expect one row with id=1 and user_id="@skynet-admin:...". Do NOT SELECT access_token or password — they're encrypted anyway.
    4. Read substrate/skills/agent-relay/SKILL.md around the new paragraph — confirm it reads coherently, does not contradict the existing self-register documentation, and accurately reflects the Phase A scope (does not mention Phase B).
    5. Run the three-user mxid import that Plan 03 enables: `curl -si -X POST -H "Cookie: <admin-jwt>" -H "Content-Type: application/json" -d '{"mxid":"@ashley:thenasty.taild9b663.ts.net"}' http://localhost:30001/users/<ashley-id>/mxid` — expect 200 {ok:true}. Repeat for Zoe and Laura. Verify: `sqlite3 skynet.db "SELECT id, username, mxid FROM users WHERE mxid IS NOT NULL"` returns three rows.
    6. Optional: manually invoke the birth endpoint against a throwaway test agent name to confirm the full 1-8 step sequence lands a working relay.json on the target host. Cleanup: remove the test agent's folder + Synapse account after.
  </how-to-verify>
  <resume-signal>Type "approved" if the integration test is green, the doc reads correctly, and the three-account mxid import worked. Describe any issues otherwise (integration test red / creds missing / doc unclear / mxid import 4xx) — the planner should route the fix to a follow-up plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| INTEGRATION_TESTS env → real Synapse network I/O | Test only runs when explicitly enabled; default CI is unaffected |
| @skynet-admin creds file (bounty credentials.txt) → matrix_admin_creds row | One-shot ingestion; post-ingestion, credentials.txt can be zeroed/deleted per bounty wind-down todo #7 |
| minted user access_token → send/read on Matrix room | Token is short-lived if valid_until_ms is set; the integration test doesn't set validity so token lives as long as the account |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-23 | Information Disclosure | integration test leaks accessToken in output | mitigate | Test logs status codes + operation names only; NEVER logs the token or its prefix; grep in acceptance_criteria bounds token references |
| T-75-24 | Information Disclosure | bounty credentials.txt remains on disk post-ingestion | mitigate | Wave 3 checkpoint documents the post-ingestion cleanup as a runbook step; operator zeros/deletes credentials.txt manually after confirming ingestion |
| T-75-25 | Denial of Service | integration test spams the live thenasty Synapse | accept | Single test, single round trip; the relay handles this trivially |
| T-75-26 | Repudiation | agent-relay/SKILL.md edit history unclear | mitigate | Atomic per-task commit per CLAUDE.md commit hygiene; commit message documents the change scope |
| T-75-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages in this plan |
</threat_model>

<verification>
- `INTEGRATION_TESTS=0 npx vitest run src/backend/matrix/matrix-admin-client.integration.test.ts` exits 0 with all tests SKIPPED (proves the gate works — normal CI unaffected).
- `INTEGRATION_TESTS=1 npx vitest run src/backend/matrix/matrix-admin-client.integration.test.ts` exits 0 with 1 passing (proves the admin foundation works end-to-end against the live thenasty Synapse). Requires the human-checkpoint's ingestion step to have completed.
- `git diff substrate/skills/agent-relay/SKILL.md` shows a small additive edit (3-10 lines), no deletions.
- Full suite regression: `npm test` exits 0 (the integration test is skipped in this run because INTEGRATION_TESTS is not set).
- `npx tsc --noEmit` exits 0.
</verification>

<success_criteria>
- Phase A completion criterion (from CONTEXT.md § Vehicle notes) is verifiable and verified: creating a Skynet agent-identity creates its relay account in the same operation (via 75-04 orchestrator + this task's smoke sanity), humans have their externally-created relay mxid registered via a non-UI provisioning path (via 75-03 endpoint + this checkpoint's three-account import), and Skynet can force-manage any relay room via the admin API (via 75-02 joinRoom/makeRoomAdmin primitives + this integration test's send-message proof).
- The bounty at ~/.claude/roles/box-maintainer/bounties/skynet-matrix-admin-integration/ can enter wind-down state per its bounty.json todo list.
- Phase B's ROADMAP addition can proceed with confidence that the admin foundation is real, tested, and operationally ready.
</success_criteria>

<output>
Create `.planning/phases/75-telegram-bridge-phase-a-skynet-matrix-admin-integration-foun/75-05-SUMMARY.md` when done, noting: (1) the integration test's exact steps + green status against live Synapse, (2) the SKILL.md paragraph verbatim, (3) confirmation the three mxid imports (Ashley/Zoe/Laura) landed, (4) confirmation the bounty credentials.txt was zeroed/deleted, (5) the Phase A completion-criterion checklist with pass/fail for each item.
</output>
