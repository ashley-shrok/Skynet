---
phase: 11-skynet-transformation-purge-dead-termix-surfaces-first-slice
plan: 04
type: execute
wave: 4
depends_on:
  - 11-01
  - 11-02
  - 11-03
files_modified:
  - .planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-BUILD-VERIFY-LOG.md
  - .planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-UAT-CHECKLIST.md
  - .planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-PATCHES-MD-ENTRY.md
autonomous: true
requirements:
  - PURGE-01
  - PURGE-02
  - PURGE-03
  - PURGE-04
  - PURGE-05
tags: [ui, docs, build-verify, uat, patches-md]

must_haves:
  truths:
    - "A build-verify log exists at 11-BUILD-VERIFY-LOG.md capturing tsc/vitest/build output, cross-referenced against Wave 2 (post-Plan-02) and Wave 3 (post-Plan-03) baselines"
    - "A UAT checklist exists at 11-UAT-CHECKLIST.md enumerating the observable behaviors Ashley must verify post-deploy (desktop landing, mobile landing, RDP still works, no dashboard/host-manager/admin/settings surfaces reachable)"
    - "A patches-md entry draft exists at 11-PATCHES-MD-ENTRY.md ready for Tina to paste into termix-patches.md at patch #138 (first Phase 11 patch)"
    - "Zero source-tree modifications in this plan — this is pure documentation authoring"
    - "The build-verify log documents the 4 pre-existing ComposeBox test failures inherited from Phase 10 as a KNOWN BASELINE, not a Phase 11 regression"
  artifacts:
    - path: ".planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-BUILD-VERIFY-LOG.md"
      provides: "tsc + vitest + npm run build output evidence with counts and status"
      contains: "tsc"
    - path: ".planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-UAT-CHECKLIST.md"
      provides: "Ashley's post-deploy verification walkthrough — desktop + mobile + regression"
      contains: "Ashley"
    - path: ".planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-PATCHES-MD-ENTRY.md"
      provides: "Paste-ready patch #138 entry for Tina's termix-patches.md catalog"
      contains: "patch #138"
  key_links:
    - from: "11-BUILD-VERIFY-LOG.md"
      to: "11-UAT-CHECKLIST.md"
      via: "the build-verify log's PASS status is the input to Ashley starting the UAT walkthrough"
      pattern: "UAT"
---

<objective>
Author three documentation deliverables that close Phase 11 for the deploy handoff to Ashley: (1) a build-verify log capturing tsc + vitest + npm run build output as evidence of code-complete-clean state, (2) a UAT checklist enumerating the observable behaviors Ashley must verify post-deploy, (3) a patches-md entry draft ready for Tina to paste at patch #138. No source-tree modifications — pure documentation.

Purpose: Phase 11 is a Ship-of-Theseus purge with high blast radius (5 files modified, 2 files deleted, heavy AppShell surgery). Before Ashley greenlights a deploy, we need concrete evidence the code is tsc-clean + test-green + build-clean, plus a checklist Ashley walks post-deploy to confirm the LOOK — the landing surface actually landed, the AppRail actually disappeared, RDP actually still works.

Output: Three markdown files under `.planning/phases/11-.../`. Ashley reads them; if PASS, patch #138 goes into termix-patches.md and the deploy happens per fork discipline (batched deploys per fleet-standing "batch patches into meaningful deploys" rule per CONTEXT.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-CONTEXT.md
@.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-01-STRIP-LIST.md
@.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-02-SUMMARY.md
@.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-03-SUMMARY.md
@.planning/phases/10-pretty-conversations-visual-language-rework/10-BUILD-VERIFY-LOG.md
@.planning/phases/10-pretty-conversations-visual-language-rework/10-UAT-CHECKLIST.md
@.planning/phases/10-pretty-conversations-visual-language-rework/10-PATCHES-MD-ENTRY.md
@.planning/phases/10-pretty-conversations-visual-language-rework/deferred-items.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 11-BUILD-VERIFY-LOG.md capturing tsc + vitest + npm run build evidence</name>
  <files>.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-BUILD-VERIFY-LOG.md</files>
  <read_first>
    - /home/ubuntu/termix/.planning/phases/10-pretty-conversations-visual-language-rework/10-BUILD-VERIFY-LOG.md (Phase 10's build-verify log — the format to mirror)
    - /home/ubuntu/termix/.planning/phases/10-pretty-conversations-visual-language-rework/deferred-items.md (the 4 pre-existing ComposeBox test failures baseline — this plan MUST reference them as inherited baseline, not new regression)
    - /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-02-SUMMARY.md (Plan 02 execution summary, if written by Plan 02 executor)
    - /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-03-SUMMARY.md (Plan 03 execution summary, if written by Plan 03 executor)
  </read_first>
  <action>
Run three verification commands from the repo root and capture their output verbatim:

```
cd /home/ubuntu/termix
npx tsc --noEmit 2>&1 | tail -20
```

```
cd /home/ubuntu/termix
npx vitest run 2>&1 | tail -40
```

```
cd /home/ubuntu/termix
npm run build 2>&1 | tail -30
```

Then write `/home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-BUILD-VERIFY-LOG.md` with the following sections:

## Section 1: tsc --noEmit
- Exit code observed
- Full output (or `Clean — 0 errors`)
- Comparison to baselines: Wave 2 (post-Plan-02), Wave 3 (post-Plan-03), Phase 10 tip (from 10-BUILD-VERIFY-LOG.md)

## Section 2: npx vitest run
- Total tests run
- Passing count
- Failing count
- List of failing tests by file + name
- Explicit annotation: "The 4 pre-existing ComposeBox failures documented in `.planning/phases/10-pretty-conversations-visual-language-rework/deferred-items.md` are INHERITED BASELINE. If those 4 are the only failures, Phase 11 has ZERO net-new regressions." — verify this claim against the actual output.
- If NEW failures appear beyond the 4 inherited: FLAG THEM. Do not treat Phase 11 as code-complete-clean; route back to Plan 02 or Plan 03 for a fix commit.
- If Test 11 (settingsRowSlot position) shows as "skipped" or "removed": that is expected per Plan 03 — annotate.
- Delta counts: Phase 10 tip was 499/503 or 504/508 (verify from 10-BUILD-VERIFY-LOG.md); Phase 11 Wave 1 added 4 PrettyLandingCard tests (Plan 02); Phase 11 Wave 3 removed 1 test (Test 11 prune). Expected: +3 net test count.

## Section 3: npm run build
- Exit code observed
- Build duration in seconds
- Bundle size delta vs Phase 10 tip (from 10-BUILD-VERIFY-LOG.md). Expected: SIGNIFICANT REDUCTION because AppRail.tsx (~280 lines) + SettingsRow.tsx (~200 lines) + 11 panel-branch conditionals + rail-view state machine are all stripped. Note the exact byte or KB delta.

## Section 4: Grep hygiene gates (Phase 11 non-negotiables)
Run and capture the count for each:
- `grep -rn "from \"@/sidebar/AppRail\"" src/ | wc -l` — expected 0
- `grep -rn "from \"@/sidebar/SettingsRow\"" src/ | wc -l` — expected 0
- `grep -rn "AppRail" src/ | grep -v "pinAppRail" | grep -v "\.md$" | grep -v "^\s*//" | grep -v "^\s*\*" | wc -l` — expected 0 (no code-line hits)
- `grep -rn "SettingsRow" src/ | grep -v "\.md$" | grep -v "^\s*//" | grep -v "^\s*\*" | wc -l` — expected 0
- `grep -rn "renderSettingsMenuItems" src/ | grep -v "\.md$" | wc -l` — expected 0
- `grep -rn "railView\|handleRailClick\|sidebarTitle\|RailView" src/ | grep -v "\.md$" | wc -l` — expected 0
- `grep -rn "settingsRowSlot" src/ | grep -v "\.md$" | grep -v "^\s*//" | wc -l` — expected 0
- `grep -rn "DashboardTab" src/ | grep -v "src/ui/dashboard/" | grep -v "\.md$" | wc -l` — expected 0 (DashboardTab only lives under src/ui/dashboard/ for Phase 12+ deletion)
- `test -f src/ui/sidebar/AppRail.tsx && echo EXISTS || echo GONE` — expected GONE
- `test -f src/ui/sidebar/SettingsRow.tsx && echo EXISTS || echo GONE` — expected GONE
- `test -f src/ui/features/pretty-view/PrettyLandingCard.tsx && echo EXISTS || echo GONE` — expected EXISTS

Any FAIL row is a Phase 11 non-negotiable violation. Do not proceed to UAT authoring until every gate returns the expected count. Route back to the relevant plan and fix.

## Section 5: Requirement traceability
Table mapping PURGE-01 through PURGE-05 to the specific commits + verification evidence:
- PURGE-01: landing surface swap → evidence: Plan 02 commits + `grep -c "PrettyLandingCard" src/ui/shell/tabUtils.tsx` returns 2
- PURGE-02: AppRail file deleted → evidence: Plan 03 Task 3 commit + `test ! -f src/ui/sidebar/AppRail.tsx`
- PURGE-03: no visible UI path to dead surfaces → evidence: Plan 03 Task 1 commit + Section 4 grep gates
- PURGE-04: backend untouched → evidence: `git log --name-only --since="Phase 11 start SHA" | grep "^src/backend/" | wc -l` returns 0
- PURGE-05: RDP still works → evidence: `onRdpRowClick` preserved in AppShell (grep), Guacamole imports/render paths untouched — verified by AUTOMATED test suite green + deferred to Ashley UAT for the runtime click-through walkthrough

Save the file. Do NOT commit yet — Task 3 handles the docs commit.
  </action>
  <verify>
    <automated>test -f /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-BUILD-VERIFY-LOG.md && grep -qE "Section 1|## tsc" /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-BUILD-VERIFY-LOG.md && grep -qE "Section 2|vitest" /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-BUILD-VERIFY-LOG.md && grep -qE "Section 3|npm run build|build" /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-BUILD-VERIFY-LOG.md && grep -qE "PURGE-01" /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-BUILD-VERIFY-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/11-.../11-BUILD-VERIFY-LOG.md`
    - File contains all 5 sections (tsc, vitest, npm run build, grep gates, requirement traceability)
    - Section 4 lists at least 10 grep gates with their expected counts
    - Section 5 covers all 5 PURGE-XX requirements with evidence pointers
    - The 4 pre-existing ComposeBox failures are explicitly named as INHERITED BASELINE (not new regression)
    - If any grep gate fails or any new test failure appears, the log FLAGS it and this plan does NOT close as PASS
  </acceptance_criteria>
  <done>Build-verify log written with tsc/vitest/build evidence, grep hygiene gates all PASS or explicit FAIL flags, requirement traceability covers PURGE-01..PURGE-05.</done>
</task>

<task type="auto">
  <name>Task 2: Author 11-UAT-CHECKLIST.md — Ashley's post-deploy verification walkthrough</name>
  <files>.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-UAT-CHECKLIST.md</files>
  <read_first>
    - /home/ubuntu/termix/.planning/phases/10-pretty-conversations-visual-language-rework/10-UAT-CHECKLIST.md (Phase 10's UAT format — mirror it: Desktop / Mobile / Cross-viewport regression sections)
    - /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-CONTEXT.md (the "Success Criteria" list is the primary UAT source — items 1-6 map directly to PURGE-01..PURGE-05)
  </read_first>
  <action>
Write `/home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-UAT-CHECKLIST.md` following Phase 10's format. Sections:

## Desktop UAT (Chrome / Safari — Ashley's laptop)

Enumerate 8-12 non-negotiable observations:
1. Fresh page-load at `https://term.gigaashley.click/` (no hash-fragment, no persisted tabs) — main pane shows a warm-glass empty-landing card (NOT the Termix dashboard with host cards / stats bars). Card contains no "Termix" or "Dashboard" text.
2. Left AppRail (the ~40px-wide column of icon buttons for Dashboard / Hosts / Sessions / Credentials / Connections / Quick-Connect / SSH Tools / Snippets / History / Split Screen / Network Graph / User Profile / Admin Settings) is GONE. Nothing renders where it used to be.
3. Sidebar is the pretty-conversations panel — the same list of conversations Phase 10 shipped, with pencil-icon new-session button, RDP-host-sentinel rows at bottom.
4. Persistent top-left chevron toggle (Phase 10 Wave 3) still works — click closes sidebar, click again opens.
5. Click an active conversation row → PrettyView chat surface renders in the main pane (existing Phase 6-10 behavior preserved).
6. Click an RDP-host-sentinel row → Guacamole pane opens for that RDP host (PURGE-05).
7. Click the pencil icon → NewSessionDialog opens with host picker (Phase 6-7 behavior preserved).
8. No gear icon in the panel header (already removed patch #133; verify).
9. No settings menu, no admin surface, no host manager, no snippets manager reachable via any click, keyboard shortcut, or URL — even direct navigation to `#tab=host-manager` OR `#admin-settings` OR similar hash-fragments should NOT surface the dead UI (may 404, may fall back to landing, planner's call — but the dead surfaces must NOT render).
10. Existing keyboard shortcuts unchanged: double-shift opens command palette, Ctrl+Shift+O toggles pretty mode in terminal panes, etc.

## Mobile UAT (iOS Safari / installed PWA — Ashley's iPhone)

Enumerate 6-8 items:
1. Fresh page-load lands on the pretty-conversations list view (unchanged from Phase 10).
2. Tap a conversation row → view screen opens with the PrettyView chat surface.
3. Top-left back button returns to list.
4. No bottom navigation bar (already deleted in Phase 6; verify).
5. Scroll to bottom of the pretty-conversations list — NO settings row at the bottom (SettingsRow deleted in Phase 11).
6. RDP-host row tap opens Guacamole pane (PURGE-05).
7. iOS PWA reinstall (remove + re-add from home screen) — safe-area seam still gray, no visible regression from Phase 10's #0a0b12 rebase.

## Cross-viewport regression checks

Enumerate 4-6 items to verify nothing existing broke:
1. Message queue drawer opens/closes at bottom of terminal panes (Ctrl+M).
2. Pretty-view compose box submits messages, WipBubble renders, session-holding overlay behaves (Phase 2-10 preserved).
3. RDP session actually connects + renders (not just launches — Ashley verifies the actual remote desktop is usable).
4. Existing multi-session terminal panes — switching between conversations preserves live WebSocket, terminal buffer, pretty-view scroll position (Phase 6 TG-05 persistence contract preserved).
5. Refresh the page while a terminal session is active — the session's row still appears (fleet-native list Phase 7 behavior preserved).

## Failure route-back table

| Symptom | Route to |
|---------|----------|
| Landing shows Termix dashboard | Plan 02 Task 2 (tabUtils.tsx case dashboard swap) |
| AppRail still visible | Plan 03 Task 1 Commit B (AppRail mount removal in AppShell) or Plan 03 Task 3 (file deletion) |
| SettingsRow still visible on mobile | Plan 03 Task 1 Commit B or Plan 03 Task 2 |
| RDP row click no longer opens Guacamole | Plan 03 Task 1 Commit B (onRdpRowClick handler preserved) |
| Sidebar renders blank | Plan 03 Task 1 Commit B (PrettyConversationsPanel mount preserved) |
| tsc / build broken | Route to whichever plan / task's commit surfaces the tsc error |
| Test 11 regression | Plan 03 Commit A (Test 11 prune) — verify the prune actually deleted the describe block |

## Post-UAT deploy runbook

Batched with subsequent purge phases per fleet-standing "batch patches into meaningful deploys" rule (Ashley 2026-07-23). No deploy inside this phase unless Ashley explicitly greenlights an early deploy of just the landing swap + AppRail removal. If Ashley greenlights early: paste 11-PATCHES-MD-ENTRY.md at patch #138 in `~/.claude/identities/tina/termix-patches.md`, bump the ordinal count, commit the pin, run the standard docker compose build + up cycle per the fork's current deploy discipline (Ashley noted 15-min deadman regime retired 2026-07-21; use whatever supersedes it).

Save the file. Do NOT commit yet — Task 3 handles the docs commit.
  </action>
  <verify>
    <automated>test -f /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-UAT-CHECKLIST.md && grep -qE "Desktop UAT" /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-UAT-CHECKLIST.md && grep -qE "Mobile UAT" /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-UAT-CHECKLIST.md && grep -qE "Cross-viewport|regression" /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-UAT-CHECKLIST.md && grep -qE "Failure route-back" /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-UAT-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/11-.../11-UAT-CHECKLIST.md`
    - File contains at least 4 sections: Desktop UAT, Mobile UAT, Cross-viewport regression, Failure route-back
    - Desktop UAT enumerates at least 8 non-negotiable items
    - Mobile UAT enumerates at least 6 items
    - Cross-viewport regression enumerates at least 4 items including RDP-verify
    - Failure route-back table maps at least 6 symptoms to plan / task locations
    - Every UAT item is expressed as an OBSERVABLE assertion Ashley can check (not "make sure X is right" — instead "on fresh page-load, the main pane shows a warm-glass empty card")
    - "no gear icon" / "no settings menu" / "no admin surface" verifications are present
    - RDP-verify item is present (PURGE-05 acceptance)
  </acceptance_criteria>
  <done>UAT checklist written with actionable observations Ashley can walk post-deploy; failure route-back table gives clear signals of where to fix.</done>
</task>

<task type="auto">
  <name>Task 3: Author 11-PATCHES-MD-ENTRY.md and commit all three Phase 11 docs deliverables</name>
  <files>.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-PATCHES-MD-ENTRY.md</files>
  <read_first>
    - /home/ubuntu/termix/.planning/phases/10-pretty-conversations-visual-language-rework/10-PATCHES-MD-ENTRY.md (Phase 10's multi-commit patch entry — the format to mirror)
    - /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-BUILD-VERIFY-LOG.md (Task 1 output — quote from it in the patch entry)
    - /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-01-STRIP-LIST.md (Plan 01 output — patch entry cites it as the audit source)
  </read_first>
  <action>
Write `/home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-PATCHES-MD-ENTRY.md` as a paste-ready patch #138 entry for Tina's `~/.claude/identities/tina/termix-patches.md`. Mirror Phase 10's multi-commit-under-one-pin format from `10-PATCHES-MD-ENTRY.md`. Include:

- Patch title: `patch #138 — Skynet transformation: purge dead Termix surfaces (first slice)`
- Patch summary paragraph: 3-5 sentences describing what the patch does at Ashley-level (landing swap, AppRail retirement, SettingsRow retirement, rail-view state machine strip, 10 dead panels unreachable). Reference the CONTEXT.md phase boundary lock ("first slice" — Phase 12+ handles the rest).
- Commit list — enumerate every commit from Plans 01-04 with SHA placeholders:
  - Plan 01 (docs): 1 commit (strip-list)
  - Plan 02 (landing swap): 3 commits (PrettyLandingCard + tabUtils + AppShell label)
  - Plan 03 (retirement): 5 commits (Test 11 prune, AppShell surgery, panel prop drop, SettingsRow delete, AppRail delete)
  - Plan 04 (docs): 1 commit (the docs commit made in this task)
- Verification evidence: paste the tsc/vitest/build results from 11-BUILD-VERIFY-LOG.md (compressed to a table)
- Grep-hygiene gate results from Section 4 of the build-verify log
- Bytes/KB delta from the bundle (AppRail.tsx and SettingsRow.tsx deletion + the AppShell rail-view state-machine strip should net a ~500-800 line reduction; note the actual delta from Plan 03's build)
- References: `.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/` (the phase directory), `~/.claude/identities/tina/bounties/skynet-transformation-purge-dead-surfaces/` (the bounty), `11-01-STRIP-LIST.md` (the audit input)
- Batching note: patch #138 batches with subsequent Phase 12+ purge patches per Ashley 2026-07-23 "batch patches into meaningful deploys" rule. No standalone deploy unless Ashley greenlights.
- NO Co-Authored-By trailer (fork convention per 260723-bbt quick task pattern)

Then commit all three Phase 11 docs deliverables (11-BUILD-VERIFY-LOG.md, 11-UAT-CHECKLIST.md, 11-PATCHES-MD-ENTRY.md) in ONE commit as:

```
docs(phase-11): build-verify log + UAT checklist + patch #138 entry draft
```

Use `git add` on specific file paths only (no `-A`, no `.`). No source files should be staged.

Post-commit verification:
- `git log -1 --name-only` shows exactly the 3 markdown files under `.planning/phases/11-.../`
- `git status --porcelain` returns empty
  </action>
  <verify>
    <automated>test -f /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-PATCHES-MD-ENTRY.md && grep -qE "patch #138|patch 138" /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-PATCHES-MD-ENTRY.md && grep -qE "AppRail|Skynet|purge" /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-PATCHES-MD-ENTRY.md && cd /home/ubuntu/termix && git log -1 --name-only --format="%s" 2>/dev/null | grep -qE "phase-11.*docs|build-verify|UAT|patch #138"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/11-.../11-PATCHES-MD-ENTRY.md`
    - Contains the string "patch #138" (or "patch 138")
    - Contains "Skynet" AND "AppRail" AND "purge"
    - Enumerates commit list from Plans 01-04 (with placeholders or actual SHAs)
    - References the bounty at `~/.claude/identities/tina/bounties/skynet-transformation-purge-dead-surfaces/`
    - Includes "no Co-Authored-By trailer" AND "batches with subsequent Phase 12+ purge patches" (fork conventions)
    - One commit landed touching only the 3 Phase 11 docs files (11-BUILD-VERIFY-LOG.md, 11-UAT-CHECKLIST.md, 11-PATCHES-MD-ENTRY.md)
    - `git status --porcelain` is empty after the commit
    - No files under `src/` are in the commit's file list
  </acceptance_criteria>
  <done>Patch #138 entry drafted; all three Phase 11 docs deliverables committed in one docs commit; Phase 11 is code-complete-pending-Ashley-UAT-and-deploy-greenlight.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-verify-log→Ashley-deploy-decision | The build-verify log's PASS status is the primary input Ashley uses to decide whether to run the deploy. A FALSE PASS (log claims tsc-clean but tsc actually fails) would land a broken deploy. Task 1 mitigates by running the commands live and capturing verbatim output — no hand-typed status. |
| UAT-checklist→Ashley-walkthrough | The UAT checklist is what Ashley walks post-deploy. Missing an observation (e.g., forgot to include the RDP-verify) means Ashley may sign off on a broken deploy. Task 2 mitigates by mapping UAT items to CONTEXT.md's Success Criteria (PURGE-01..PURGE-05) — every requirement has at least one UAT observation. |
| patches-md-entry→fork-catalog-integrity | Tina's `termix-patches.md` is the authoritative catalog of every fork patch. A stale patch #138 entry (referencing wrong SHAs, wrong file counts) would leak into the fork's provenance history. Task 3 mitigates by templating SHA placeholders — Tina fills the actual SHAs at paste time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Tampering (false PASS in build-verify) | 11-BUILD-VERIFY-LOG.md | mitigate | Task 1 mandates running `npx tsc --noEmit`, `npx vitest run`, `npm run build` live and pasting the output verbatim into the log. No hand-typed status. If any grep gate fails, the log FLAGS it and the plan does not close as PASS. |
| T-11-04-02 | Information Disclosure | Phase 11 docs | accept | The docs contain internal file paths, line numbers, commit references — same class of information as the source repo and planning tree already expose. No PII, no credentials. |
| T-11-04-03 | Denial of Service (missed UAT observation → broken deploy signed off) | 11-UAT-CHECKLIST.md | mitigate | Task 2 acceptance criteria requires at least one UAT observation per PURGE-XX requirement AND at least one RDP-verify observation. Failure route-back table maps observed symptoms to plan/task locations so any post-deploy regression traces back to a specific fix location. |
| T-11-04-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this plan. No supply-chain surface. |
</threat_model>

<verification>
- 11-BUILD-VERIFY-LOG.md exists with tsc + vitest + build evidence and all grep hygiene gates.
- 11-UAT-CHECKLIST.md exists with Desktop + Mobile + Cross-viewport + Failure-route-back sections.
- 11-PATCHES-MD-ENTRY.md exists with patch #138 draft ready for Tina to paste.
- One commit landed touching only the 3 Phase 11 docs files (Task 3).
- No source files under `src/` modified in this plan.
</verification>

<success_criteria>
- Phase 11 is code-complete-pending-Ashley-UAT-and-deploy-greenlight state.
- Ashley has a concrete UAT checklist to walk post-deploy.
- Tina has a paste-ready patch #138 entry for termix-patches.md.
- If Ashley's UAT reports any failure, the failure-route-back table gives an immediate signal of which plan/task to route back to.
</success_criteria>

<output>
Create `.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-04-SUMMARY.md` when done.
</output>
