---
phase: 11-skynet-transformation-purge-dead-termix-surfaces-first-slice
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/ui/AppShell.tsx
  - src/ui/shell/tabUtils.tsx
  - src/ui/features/pretty-view/PrettyLandingCard.tsx
autonomous: true
requirements:
  - PURGE-01
  - PURGE-05
tags: [ui, app-shell, landing-surface, skynet-purge, palette-pv]

must_haves:
  truths:
    - "On desktop, a fresh page-load with no URL hash-fragment lands on the pretty-conversations sidebar with a warm-glass empty-landing card in the main pane (per PURGE-01) — NOT the Termix dashboard"
    - "On mobile, page-load lands on the pretty-conversations list view (unchanged from Phase 10) with the mobile back-button flow to the main pane preserved"
    - "The `<DashboardTab>` component is no longer imported anywhere in `src/ui/shell/tabUtils.tsx` (nor anywhere else in `src/` outside of `src/ui/dashboard/`)"
    - "The main pane's empty-landing card uses `--color-pv-*` palette tokens per CONTEXT.md palette authority — NOT Termix's `--background` token"
    - "RDP/VNC/Guacamole sessions launch and render exactly as before (PURGE-05); no Terminal.tsx / GuacamoleApp / tab plumbing / WebSocket lifecycle change"
    - "TypeScript compiles clean and the existing test suite passes at the same baseline as Wave 1's tip (documented pre-existing 4 ComposeBox failures unchanged)"
  artifacts:
    - path: "src/ui/features/pretty-view/PrettyLandingCard.tsx"
      provides: "The warm-glass empty-landing card rendered in the main pane when no conversation is selected"
      contains: "PrettyLandingCard"
      min_lines: 20
    - path: "src/ui/shell/tabUtils.tsx"
      provides: "Modified renderTabContent — case dashboard now renders <PrettyLandingCard/> instead of <DashboardTab/>"
      contains: "PrettyLandingCard"
    - path: "src/ui/AppShell.tsx"
      provides: "Modified initial tab + closeTab fallback per strip-list Section A"
  key_links:
    - from: "src/ui/shell/tabUtils.tsx"
      to: "src/ui/features/pretty-view/PrettyLandingCard.tsx"
      via: "import from features/pretty-view"
      pattern: "from \"@/features/pretty-view/PrettyLandingCard\""
    - from: "src/ui/features/pretty-view/PrettyLandingCard.tsx"
      to: "--color-pv-* CSS variables"
      via: "className tokens sourced from pretty-view design system"
      pattern: "color-pv-|pv-base|rgba\\(240"
---

<objective>
Swap the desktop landing surface from the Termix dashboard (`<DashboardTab>`) to a warm-glass empty-landing card (`<PrettyLandingCard>`) rendered inside `renderTabContent` when a tab of type `"dashboard"` is active. This preserves the existing "dashboard" TabType as a load-bearing fallback identifier for closeTab / effectiveSelectedTabId / URL-restore paths while removing every UI path to the DashboardTab component tree. Delivers PURGE-01 (desktop landing = pretty-conversations panel + PrettyView-style main pane) and preserves PURGE-05 (RDP/VNC untouched).

Purpose: Ashley's 2026-07-23 UAT quote: "I really feel like we need to get away from this termix front end stuff before any of this is worth quibbling over." The Termix dashboard is the most visible dead surface on session load. Swapping the landing kills that first impression without a large state-machine rewire — the "dashboard" identifier stays as a load-bearing fallback token, the RENDERING becomes a pretty-view-native empty card.

Output: One new component file (`PrettyLandingCard.tsx`), one modified `renderTabContent` switch (`tabUtils.tsx`), and one AppShell.tsx label update (the initial tab's `label` no longer says "Dashboard" — uses `t("nav.conversations.title", { defaultValue: "Conversations" })` or similar since the tab label surfaces in browser tab title). Atomic commit sequence — one commit per file per Phase 10 Wave 4 precedent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-CONTEXT.md
@.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-01-STRIP-LIST.md
@.planning/phases/10-pretty-conversations-visual-language-rework/10-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create PrettyLandingCard.tsx — the warm-glass empty-landing card</name>
  <files>src/ui/features/pretty-view/PrettyLandingCard.tsx</files>
  <read_first>
    - /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-01-STRIP-LIST.md (Section A — landing-surface swap targets)
    - /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-CONTEXT.md (palette authority: `--color-pv-*` — `--color-pv-base-end` = `#0a0b12`, `--color-pv-base-start` = `#141520`; also "no settings" lock)
    - /home/ubuntu/termix/src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (lines 260-330 — the empty-state "No conversations yet" glass card is the visual language to mirror)
    - /home/ubuntu/termix/src/ui/features/pretty-view/PrettyView.tsx (skim only lines 1-60 — confirm the pretty-view directory layout conventions before adding a new file here)
    - /home/ubuntu/termix/src/ui/index.css (grep for `--color-pv-` and `pv-conv-wip` to see the token catalog available)
  </read_first>
  <behavior>
    - Test 1: `<PrettyLandingCard />` renders a container with `data-pv-landing-card="true"` attribute (for grep-verification in downstream plans)
    - Test 2: Component is centered in its parent container (uses flex layout `flex flex-col items-center justify-center` or equivalent)
    - Test 3: Component's outer container uses an inline `style` prop containing warm-neutral palette-authority values. Assertion is on the DOM element's `.style` attribute (accessible via `element.getAttribute("style")` OR `element.style.background` in JSDOM), NOT on computed CSS or Tailwind class-name variants. The inline style MUST contain at least one of: `rgba(240,235,224` (warm-cream text), `rgba(255,220,170` (Phase 10 patch #133 warm-glow), OR `hsla(` referencing pretty-view palette values. This mandates the component author put the warm-glass background/border/shadow tokens on the inline `style={{...}}` prop of the outer container rather than in a Tailwind arbitrary-value class — the inline-style approach is the least ambiguous way to verify `--color-pv-*` palette tokens landed AND it survives JSDOM's lack of computed-style resolution (JSDOM does NOT parse Tailwind or CSS variables; only inline styles are queryable). See patch #133 precedent for the panel-header rgba conversion.
    - Test 4: Component renders idle, static content — no animation, no spinner (Ashley's motion guardrail per patch #72 precedent); accepts no props
  </behavior>
  <action>
Create `/home/ubuntu/termix/src/ui/features/pretty-view/PrettyLandingCard.tsx` as a new React functional component. The component takes NO props (it is the empty-landing card shown when the initial "dashboard"-typed tab is active — meaning no conversation has been selected yet).

Requirements per CONTEXT.md `<decisions>` palette authority:
- Use warm-neutral pretty-view palette values. Reference Phase 10's PrettyConversationsPanel empty state (`src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` around line 283) for the visual idiom — an idle glass card centered in the scroll region with subtle border, backdrop-blur, and neutral warm-cream text.
- The card contains a short welcoming label (e.g., "Select a conversation" or "Skynet" as the primary label; the exact text is executor discretion but MUST NOT reference "Termix", "Dashboard", or any dead-surface name).
- Add a `data-pv-landing-card="true"` DOM attribute on the outer container element for downstream grep-verification.
- Use `flex flex-col flex-1 items-center justify-center` on the outer container so the card centers in the tabUtils portal target (which is a full-height div).
- NO animation, NO spinner, NO shadcn primitives (per Phase 10 patch #133 precedent — the pretty-conversations surface went shadcn-free).
- **Palette tokens go on the inline `style={{ background: ..., border: ..., boxShadow: ... }}` prop of the outer container**, NOT in a Tailwind arbitrary-value class. Use warm-neutral `rgba(240,235,224,0.9)` (or similar Phase 10 patch #133 value) for primary label text via inline style. Reason: the Test 3 assertion queries the DOM `.style` attribute (JSDOM does not resolve computed CSS variables or Tailwind classes at test time), and inline-style declaration is also the most legible traceability path to the `--color-pv-*` palette-authority rule from CONTEXT.md.
- Card body: warm-neutral flat `rgba` background + subtle `hsla` border + `backdrop-filter: blur(20px)` — all applied via inline `style` — mirror the Phase 10 empty-state glass card conventions (grep `PrettyConversationsPanel.tsx` around lines 260-330 to see the exact pattern).

Component signature:
```
export function PrettyLandingCard(): JSX.Element
```

Create a companion test file `src/ui/features/pretty-view/PrettyLandingCard.test.tsx` covering the 4 test cases in `<behavior>`. Use `@testing-library/react` (existing project convention — see `src/ui/features/pretty-view/DropOverlay.test.tsx` for the pattern). Test file layout:
- `describe("PrettyLandingCard", () => { ... })`
- Test 1: renders element with `[data-pv-landing-card="true"]` present
- Test 2: outer container has centering classes present (assert on className via regex OR use `getByTestId` if you add a testid)
- Test 3: outer container's inline `style` attribute (via `element.getAttribute("style")` OR `element.style.background` / `.borderColor` / `.boxShadow`) contains at least one of the warm-neutral palette markers — regex match on `/rgba\(240,\s*235,\s*224/` OR `/rgba\(255,\s*220,\s*170/` OR `/hsla\(/`. This assertion runs against the inline style attribute, NOT `window.getComputedStyle()` (JSDOM does not resolve computed CSS variables or Tailwind).
- Test 4: no `animate-`, no `<svg class*="animate">`, no `aria-busy` attribute anywhere in the rendered subtree

Commit as: `feat(pretty-view): add PrettyLandingCard empty-state landing component (Phase 11)`
  </action>
  <verify>
    <automated>cd /home/ubuntu/termix && test -f src/ui/features/pretty-view/PrettyLandingCard.tsx && test -f src/ui/features/pretty-view/PrettyLandingCard.test.tsx && grep -q "data-pv-landing-card" src/ui/features/pretty-view/PrettyLandingCard.tsx && grep -qE "style=\\{\\{" src/ui/features/pretty-view/PrettyLandingCard.tsx && grep -qE "rgba\\(|hsla\\(" src/ui/features/pretty-view/PrettyLandingCard.tsx && npx vitest run src/ui/features/pretty-view/PrettyLandingCard.test.tsx 2>&1 | grep -qE "([1-9][0-9]* passed|Tests\\s+[1-9])"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/ui/features/pretty-view/PrettyLandingCard.tsx` exists
    - File `src/ui/features/pretty-view/PrettyLandingCard.test.tsx` exists
    - `grep -c "PrettyLandingCard" src/ui/features/pretty-view/PrettyLandingCard.tsx` returns >= 2 (export + JSDoc-optional mention)
    - `grep -c "data-pv-landing-card" src/ui/features/pretty-view/PrettyLandingCard.tsx` returns >= 1
    - `grep -cE "style=\\{\\{" src/ui/features/pretty-view/PrettyLandingCard.tsx` returns >= 1 (inline `style={{...}}` prop present per Test 3 mandate)
    - `grep -cE "Termix|Dashboard|LayoutDashboard" src/ui/features/pretty-view/PrettyLandingCard.tsx` returns 0 (no dead-surface names in the new landing)
    - `grep -cE "animate-|animate=|useEffect\\(|useState\\(" src/ui/features/pretty-view/PrettyLandingCard.tsx` returns 0 (static, no lifecycle, no motion — Ashley's motion guardrail)
    - Test file's Test 3 asserts against `.getAttribute("style")` OR `element.style.<property>`, NOT against `window.getComputedStyle(...)` — verify by grepping the test file for `getComputedStyle` and expecting 0 hits: `grep -c "getComputedStyle" src/ui/features/pretty-view/PrettyLandingCard.test.tsx` returns 0
    - `npx vitest run src/ui/features/pretty-view/PrettyLandingCard.test.tsx` exits with all 4 tests passing
    - `npx tsc --noEmit` exits 0
    - `git log -1 --name-only --format="%s" | grep -qE "PrettyLandingCard|pretty-view.*landing"` — commit lands with the right scope
  </acceptance_criteria>
  <done>PrettyLandingCard.tsx + its test file exist, the 4 behavior tests pass, tsc clean, one commit landed touching only these 2 files. Test 3 asserts on inline `style` prop (not computed CSS) so the palette-authority traceability is verifiable in JSDOM.</done>
</task>

<task type="auto">
  <name>Task 2: Wire PrettyLandingCard into renderTabContent's "dashboard" case</name>
  <files>src/ui/shell/tabUtils.tsx</files>
  <read_first>
    - /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-01-STRIP-LIST.md (Section A items 6, 7)
    - /home/ubuntu/termix/src/ui/shell/tabUtils.tsx (full file — this task swaps one import + one switch case)
    - /home/ubuntu/termix/src/ui/features/pretty-view/PrettyLandingCard.tsx (just created — confirm the exact export name)
  </read_first>
  <action>
Modify `/home/ubuntu/termix/src/ui/shell/tabUtils.tsx` to replace the `<DashboardTab>` render site with `<PrettyLandingCard/>`. Exact edits:

1. At line 26 (`import { DashboardTab } from "@/dashboard/DashboardTab";`) — REMOVE this import line.
2. At line 5 (`  LayoutDashboard,` inside the lucide-react imports) — KEEP this import for now if `tabIcon` still uses it at line 89-90. If Task 3 strips the initial-tab "dashboard" label but keeps the TabType, `tabIcon` still needs to handle `case "dashboard"` for stray callers, so LayoutDashboard stays. Note: this icon does not appear on any visible UI (no tab strip in Phase 6+), but it's referenced by the exhaustive-switch — keep it. If tsc complains it's unused, then and only then remove it.
3. Add a new import: `import { PrettyLandingCard } from "@/features/pretty-view/PrettyLandingCard";` after the existing pretty-view imports (there are no pretty-view imports in this file yet — add it near the other feature imports around lines 22-28).
4. At lines 187-193 — REPLACE the entire `case "dashboard":` block:
   ```
   case "dashboard":
     return (
       <DashboardTab
         onOpenSingletonTab={onOpenSingletonTab!}
         onOpenTab={onOpenTab!}
       />
     );
   ```
   WITH:
   ```
   case "dashboard":
     // Phase 11 landing-surface swap (PURGE-01): renders the pretty-view-native
     // empty-landing card in place of the old Termix DashboardTab. The
     // "dashboard" TabType is preserved as a load-bearing fallback identifier
     // in effectiveSelectedTabId + doCloseTab; the DashboardTab component
     // itself becomes unreachable from any UI path and is slated for Phase
     // 12+ deletion alongside the rest of src/ui/dashboard/.
     return <PrettyLandingCard />;
   ```

Commit as: `feat(shell): swap dashboard render site to PrettyLandingCard (Phase 11 PURGE-01)`

Post-edit verification (inline, before commit):
- `grep -c "DashboardTab" src/ui/shell/tabUtils.tsx` returns 0
- `grep -c "PrettyLandingCard" src/ui/shell/tabUtils.tsx` returns 2 (import + render call)
- `npx tsc --noEmit` exits 0

IF `npx tsc --noEmit` reports LayoutDashboard as unused, THEN also remove the `LayoutDashboard,` line from the lucide-react imports and note this in the SUMMARY.
  </action>
  <verify>
    <automated>cd /home/ubuntu/termix && grep -c "DashboardTab" src/ui/shell/tabUtils.tsx | grep -q "^0$" && grep -q "PrettyLandingCard" src/ui/shell/tabUtils.tsx && npx tsc --noEmit 2>&1 | grep -qvE "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DashboardTab" src/ui/shell/tabUtils.tsx` returns exactly `0`
    - `grep -c "from \"@/dashboard/DashboardTab\"" src/ui/shell/tabUtils.tsx` returns exactly `0`
    - `grep -c "PrettyLandingCard" src/ui/shell/tabUtils.tsx` returns exactly `2` (one import statement, one JSX render call)
    - `grep -c "case \"dashboard\":" src/ui/shell/tabUtils.tsx` returns exactly `2` (one in `tabIcon`, one in `renderTabContent` — the switch structures are preserved)
    - `grep -rn "DashboardTab" src/ | grep -v "src/ui/dashboard/" | grep -v "\\.md$" | wc -l` returns `0` (DashboardTab now only lives in `src/ui/dashboard/` for Phase 12+ deletion)
    - `npx tsc --noEmit` exits 0
    - `git log -1 --name-only --format="%s" | grep -qE "shell.*PrettyLandingCard|dashboard.*PrettyLandingCard|PURGE-01"` — commit lands with the right scope
  </acceptance_criteria>
  <done>renderTabContent's `case "dashboard"` renders `<PrettyLandingCard/>` instead of `<DashboardTab>`; DashboardTab is not imported anywhere outside `src/ui/dashboard/`; tsc clean; one commit landed touching only tabUtils.tsx.</done>
</task>

<task type="auto">
  <name>Task 3: Update AppShell initial-tab + closeTab-fallback labels for the landing swap</name>
  <files>src/ui/AppShell.tsx</files>
  <read_first>
    - /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-01-STRIP-LIST.md (Section A items 1-4)
    - /home/ubuntu/termix/src/ui/AppShell.tsx (lines 178-195 for the initial state seed; lines 1160-1195 for doCloseTab fallback)
  </read_first>
  <action>
Modify `/home/ubuntu/termix/src/ui/AppShell.tsx` to update the two synthetic-"dashboard"-tab creation sites so their `label` no longer surfaces as "Dashboard" in the browser tab title (which reads from the tab's label). The "dashboard" TabType identifier is PRESERVED — only the label changes.

**Baseline capture (BEFORE any edits — record for the delta verification):**
```
BEFORE_LABEL_COUNT=$(grep -c 't("nav.conversations.title"' src/ui/AppShell.tsx)
echo "BEFORE_LABEL_COUNT=${BEFORE_LABEL_COUNT}"
```
Record the exact number in the commit message body or SUMMARY. Expected pre-edit value: 0 (this task is the FIRST introduction of `t("nav.conversations.title"` as a `label:` value at those specific tab-creation sites).

Exact edits:

1. Line 185: `label: t("nav.dashboard"),` (in the initial useState seed at lines 180-188) — REPLACE with `label: t("nav.conversations.title", { defaultValue: "Conversations" }),`. This is the label that surfaces in `document.title` when the "dashboard" tab is active (Ashley never sees this on session load post-Phase 11 because the sidebar renders "Conversations" and there is no tab strip; but the browser tab title still reads it and must not say "Dashboard").
2. Line 1187: `label: t("nav.dashboard"),` (in doCloseTab's synthetic-tab fallback at lines 1181-1190 — the block that creates a new "dashboard" tab when the user closes the last one) — REPLACE with the SAME expression: `label: t("nav.conversations.title", { defaultValue: "Conversations" }),`.

Do NOT modify:
- The `id: "dashboard"` field (preserved as a stable fallback identifier).
- The `instanceId: "dashboard"` field (same).
- The `type: "dashboard"` field (preserved so effectiveSelectedTabId / closeTab / URL-restore paths still resolve — the render becomes `<PrettyLandingCard/>` via Task 2's tabUtils edit).
- The `hostlessTypes` array at line 830 containing `"dashboard"` (URL restore path — preserved).
- Any other `railView`, `sidebarPanelContent`, `AppRail`, `SettingsRow` references — those are Plan 03's targets.

Commit as: `chore(app-shell): retire "Dashboard" label from initial + fallback tabs (Phase 11 PURGE-01)`

Post-edit verification (inline, before commit):

**Line-anchored delta gate (derivable, per checker W-2 fix):**
```
# The two lines this task modifies MUST contain t("nav.conversations.title"
grep -nE '^\s*label: t\("nav\.conversations\.title"' src/ui/AppShell.tsx
```
Expected output: at least 2 line-numbered matches. The line numbers should correspond to the initial useState seed and the doCloseTab fallback (approximately lines 185 and 1187 pre-edit; may shift by 0-1 lines due to expression length).

```
# The old label expression MUST be gone at the modified sites
grep -c 't("nav.dashboard")' src/ui/AppShell.tsx
```
Expected output: 0.

```
# Baseline-plus-two delta check (secondary confirmation)
AFTER_LABEL_COUNT=$(grep -c 't("nav.conversations.title"' src/ui/AppShell.tsx)
test "${AFTER_LABEL_COUNT}" -eq "$((BEFORE_LABEL_COUNT + 2))"
```
Expected: exit 0 (count grew by exactly 2 — both new label lines landed, no more, no less).

Additional gates:
- `grep -c 'type: "dashboard"' src/ui/AppShell.tsx` returns 2 (initial seed + doCloseTab fallback — preserved by design)
- `npx tsc --noEmit` exits 0
- Run the full existing test suite: `npx vitest run` — should show no new failures beyond the documented 4 pre-existing ComposeBox failures (from `.planning/phases/10-pretty-conversations-visual-language-rework/deferred-items.md`). AppShell.persistence.test.tsx MUST still pass.
  </action>
  <verify>
    <automated>cd /home/ubuntu/termix && grep -c 't("nav.dashboard")' src/ui/AppShell.tsx | grep -q "^0$" && test "$(grep -cE '^\\s*label: t\\("nav\\.conversations\\.title"' src/ui/AppShell.tsx)" -ge 2 && grep -c 'type: "dashboard"' src/ui/AppShell.tsx | grep -q "^2$" && npx tsc --noEmit 2>&1 | grep -qvE "error TS" && npx vitest run src/ui/AppShell.persistence.test.tsx 2>&1 | grep -qE "passed"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 't("nav.dashboard")' src/ui/AppShell.tsx` returns exactly `0`
    - **Line-anchored delta gate (derivable per checker W-2 fix):** `grep -cE '^\s*label: t\("nav\.conversations\.title"' src/ui/AppShell.tsx` returns exactly `2` (two new `label:` lines land at the two modified sites — no more, no less; the `^\s*label:` anchor filters out any coincidental `t("nav.conversations.title"` usage in other syntactic positions like sidebarHeader that would confuse a bare "at least 2" count)
    - `grep -c 'id: "dashboard"' src/ui/AppShell.tsx` returns exactly `2` (initial seed + doCloseTab fallback — preserved)
    - `grep -c 'type: "dashboard"' src/ui/AppShell.tsx` returns exactly `2` (same)
    - `grep -c '"dashboard"' src/ui/AppShell.tsx` returns >= `6` (id + type + instanceId at both sites + line 830 hostlessTypes + line 1175 closeTab return value — the "dashboard" TabType identifier is PRESERVED)
    - `npx tsc --noEmit` exits 0
    - `npx vitest run src/ui/AppShell.persistence.test.tsx` exits with all tests passing (persistence contract unchanged)
    - Full `npx vitest run` exits with no NEW failures beyond the 4 pre-existing ComposeBox failures documented in `deferred-items.md` (Wave 1 baseline: 499/503 or better)
    - `git log -1 --name-only --format="%s" | grep -qE "app-shell.*Dashboard|PURGE-01|Phase 11"` — commit lands with the right scope
  </acceptance_criteria>
  <done>The initial and fallback synthetic-dashboard-tab creations use `t("nav.conversations.title", ...)` as their label; the `id`/`instanceId`/`type` fields are preserved unchanged; the line-anchored delta grep confirms exactly 2 new label lines landed at those sites; tsc clean; test suite green at baseline; one commit landed touching only AppShell.tsx.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tab-state-machine→render-target | The "dashboard" TabType is a load-bearing identifier in effectiveSelectedTabId (`AppShell.tsx:526-534`), doCloseTab fallback (`AppShell.tsx:1175, 1181-1190`), and URL restore (`AppShell.tsx:830`). Changing the identifier requires updating ALL three sites in lockstep. This plan does NOT change the identifier — it only changes what the identifier renders. Risk = low. |
| landing-swap→visible-UI | The main-pane render for `case "dashboard"` was `<DashboardTab>` (rich SessionDashboard tree with host cards); becomes `<PrettyLandingCard/>` (idle warm-glass card). Any keyboard shortcut / URL / event that previously opened a "dashboard" tab now lands on the idle card instead — this is INTENDED per PURGE-01. |
| RDP/VNC preservation | `case "rdp"`, `case "vnc"`, `case "telnet"` in renderTabContent are UNTOUCHED. The Guacamole app + xterm.js panes render exactly as before. This is PURGE-05's guarantee. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Denial of Service (regression) | AppShell tab-state machine | mitigate | Preserve the `id/instanceId/type = "dashboard"` fields VERBATIM at both synthetic-tab creation sites. Only the `label` field changes. AppShell.persistence.test.tsx run gate catches any URL-restore or persistence contract regression. Line-anchored delta grep confirms the label swap landed at exactly the two intended sites. |
| T-11-02-02 | Tampering (accidental deletion of live functionality) | RDP/VNC/Guacamole render path | mitigate | `renderTabContent` `case "rdp"`, `case "vnc"`, `case "telnet"` blocks are NOT modified by this plan. Explicit acceptance criterion in Task 2: only `case "dashboard"` is touched. Grep-verify `case "rdp"` and `case "vnc"` still present at same line ranges post-edit. |
| T-11-02-03 | Information Disclosure | PrettyLandingCard content | accept | The landing card is a static welcoming card with no data fetching. No PII exposure risk. Test 4 in Task 1's behavior block asserts static content (no useEffect / useState) — this is not just style, it's a security surface reduction (no data fetch = no accidental data exposure). |
| T-11-02-04 | Elevation of Privilege | AppRail / admin route reachability | accept-for-now | AppRail-mediated navigation to `admin-settings` still works after Plan 02 (AppRail retirement is Plan 03). Plan 02 does NOT close every UI path to admin surfaces — that closure happens in Plan 03. Between Plan 02's commit and Plan 03's commit, the tree is a WIP state where landing is pretty-view but admin is still reachable via AppRail. This is an accepted per-plan-atomicity trade-off; Plan 03 closes it. |
| T-11-02-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this plan. No supply-chain surface. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 after each task and at the plan boundary.
- `npx vitest run src/ui/features/pretty-view/PrettyLandingCard.test.tsx` all pass (4 tests). Test 3 asserts on inline `style` attribute, not computed CSS.
- `npx vitest run src/ui/AppShell.persistence.test.tsx` all pass (Phase 6-7 persistence contract preserved).
- Full `npx vitest run` exits with no new failures beyond the documented 4 pre-existing ComposeBox failures.
- `grep -rn "DashboardTab" src/ | grep -v "src/ui/dashboard/" | grep -v "\.md$"` returns zero hits (DashboardTab is imported only from the deferred-Phase-12 dashboard/ tree).
- `grep -c "PrettyLandingCard" src/ui/shell/tabUtils.tsx` returns 2 (import + JSX).
- Line-anchored delta gate: `grep -cE '^\s*label: t\("nav\.conversations\.title"' src/ui/AppShell.tsx` returns exactly 2.
- Three commits landed on `feat/tab-title-from-tmux` (or the current working branch), one per file touched.
</verification>

<success_criteria>
- On desktop, a fresh page-load (no `#tab=` fragment, no persisted tabs) renders the pretty-conversations sidebar + a warm-glass empty-landing card in the main pane — NO Termix dashboard, NO host cards, NO snippet cards, NO admin chrome (per PURGE-01).
- Mobile page-load renders the pretty-conversations list view (unchanged from Phase 10).
- RDP hosts still open Guacamole panes exactly as before (PURGE-05 preserved — Task 2 explicitly leaves `case "rdp"` / `case "vnc"` / `case "telnet"` untouched).
- Existing tests remain green at baseline; 4 new tests added for PrettyLandingCard all pass. Test 3's inline-style assertion enforces palette-authority traceability (per CONTEXT.md `--color-pv-*` lock) in a JSDOM-verifiable way.
</success_criteria>

<output>
Create `.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
