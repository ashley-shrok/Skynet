---
phase: 11-skynet-transformation-purge-dead-termix-surfaces-first-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-01-STRIP-LIST.md
autonomous: true
requirements:
  - PURGE-01
  - PURGE-02
  - PURGE-03
tags: [ui, app-shell, enumeration, prove-dead, skynet-purge]

must_haves:
  truths:
    - "A single authoritative strip-list document exists at .planning/phases/11-.../11-01-STRIP-LIST.md that enumerates every file, import, mount, and state-machine field slated for deletion in Plans 02 + 03"
    - "The strip-list is grounded in verified grep evidence — every claim like 'X imports AppRail' cites a grep command + output line count"
    - "The strip-list explicitly names the landing-surface swap targets: initial-tab creation site, closeTab dashboard-fallback site, tabUtils dashboard render case"
    - "The strip-list explicitly names every dead-surface entry point reachable via AppRail OR SettingsRow (the two settings surfaces Ashley locked as 'we are not having settings at all')"
    - "The strip-list explicitly enumerates every AppRail-only state field (profileDropdownOpen + setter) with grep-verified consumer analysis so Plan 03 does not re-derive"
    - "The strip-list distinguishes IN-PHASE-11 deletion targets from DEFERRED-TO-PHASE-12+ files (per Ashley's phase-scope-fence locking DashboardTab / HostsPanel / SessionsPanel / snippet+admin panels for follow-up phases)"
  artifacts:
    - path: ".planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-01-STRIP-LIST.md"
      provides: "Authoritative enumeration of every deletion target for Plans 02 + 03"
      contains: "AppRail.tsx"
  key_links:
    - from: "11-01-STRIP-LIST.md"
      to: "src/ui/AppShell.tsx"
      via: "cited line numbers for each mount/state field to strip"
      pattern: "AppShell\\.tsx:\\d+"
    - from: "11-01-STRIP-LIST.md"
      to: "src/ui/sidebar/AppRail.tsx"
      via: "cited import consumers via `grep -rn`"
      pattern: "grep -rn"
---

<objective>
Produce the authoritative strip-list document (`11-01-STRIP-LIST.md`) enumerating every file, import, mount, and state-machine field slated for deletion in Plan 02 (landing-surface swap) and Plan 03 (AppRail + SettingsRow retirement). This is the "prove dead" enumeration pass per CONTEXT.md `<specifics>`: locate every deletion target, verify with grep, cite exact line numbers. No source-tree modifications.

Purpose: Give Plans 02 and 03 an audited target list so their tasks name exact identifiers/paths rather than "find and remove all references." Prevents half-deletions and scope creep — a strip-list this plan produces IS the input contract for downstream plans.

Output: One markdown file at `.planning/phases/11-.../11-01-STRIP-LIST.md`. No source code changes. Zero commits touching `src/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate AppRail deletion targets and produce 11-01-STRIP-LIST.md</name>
  <files>.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-01-STRIP-LIST.md</files>
  <read_first>
    - /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-CONTEXT.md (LOCKED scope + non-negotiables — decisions here are authoritative)
    - /home/ubuntu/termix/src/ui/AppShell.tsx (full file — the mount site + state-machine host; you cannot enumerate deletion targets without seeing every railView reference)
    - /home/ubuntu/termix/src/ui/sidebar/AppRail.tsx (the component being deleted — read fully to know its export surface AND its full prop list so Plan 01 can enumerate every AppShell-side state field that ONLY feeds AppRail)
    - /home/ubuntu/termix/src/ui/sidebar/SettingsRow.tsx (the second deletion target per Ashley's "we are not having settings at all" lock — read fully to know its export surface + what it routes to)
    - /home/ubuntu/termix/src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (consumer of the `settingsRowSlot` prop — the prop and its Test 11 must go away with SettingsRow)
    - /home/ubuntu/termix/src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx (Test 11 identification for prune target)
    - /home/ubuntu/termix/src/ui/shell/tabUtils.tsx (renderTabContent — the `case "dashboard"` render site + `DashboardTab` import)
    - /home/ubuntu/termix/.planning/phases/10-pretty-conversations-visual-language-rework/10-04-SUMMARY.md (the atomic-per-file-deletion pattern to mirror — read fully to understand the verification-gate pattern)
  </read_first>
  <action>
Create the file `/home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-01-STRIP-LIST.md` with the following sections. Every claim must cite the grep command that verified it (paste the command + first line of output OR the count) so the executor of Plans 02/03 does not re-litigate discovery.

## Section A: Landing-surface swap targets (Plan 02 input)

Enumerate the exact code sites Plan 02 modifies:

1. `src/ui/AppShell.tsx:180-188` — initial `useState<Tab[]>` seed containing `{id:"dashboard", type:"dashboard"}`. Plan 02 replaces the seed with an empty array `[]` OR replaces the "dashboard" tab type with a new "landing" type — enumerate BOTH options here; Plan 02 executor picks based on which minimizes knock-on effects to `effectiveSelectedTabId` (line 526-534), URL restore logic (line 780-830), and closeTab fallback (line 1175 + 1181-1190).
2. `src/ui/AppShell.tsx:189` — `useState("dashboard")` for `activeTabId`. Consequence of the initial-tab decision above.
3. `src/ui/AppShell.tsx:1175` — `remaining.length > 0 ? remaining[remaining.length - 1].id : "dashboard"` fallback in `doCloseTab`.
4. `src/ui/AppShell.tsx:1181-1190` — the `if (next.length === 0) return [{id:"dashboard", type:"dashboard", ...}]` synthetic-dashboard-tab creation in `doCloseTab`.
5. `src/ui/AppShell.tsx:830` — `const hostlessTypes: TabType[] = ["dashboard", "tunnel"]` in URL restore path — cite because it depends on the dashboard tab type being preserved as a valid `TabType` for restore. Plan 02 executor decides whether to keep or strip; if strip, note `"dashboard"` also has to be removed from `src/types/ui-types.ts:157` union (grep-verify only the `"dashboard" |` occurrence).
6. `src/ui/shell/tabUtils.tsx:187-193` — `case "dashboard": return <DashboardTab ... />` render site. Plan 02 replaces with a warm-glass empty-landing card scoped to `src/ui/features/pretty-view/` per palette authority (`--color-pv-*`) from CONTEXT.md.
7. `src/ui/shell/tabUtils.tsx:26` — `import { DashboardTab } from "@/dashboard/DashboardTab"` — becomes unused when line 187-193 is replaced.
8. `src/ui/shell/tabUtils.tsx:89-90` — `case "dashboard": return <LayoutDashboard ... />` in `tabIcon` function. Plan 02 may keep this for TabType safety or strip if the "dashboard" TabType is removed.

DEFERRED to Phase 12+ (do NOT list as Plan 02/03 deletion targets):
- `src/ui/dashboard/DashboardTab.tsx`, `src/ui/dashboard/Dashboard.tsx`, `src/ui/dashboard/SessionDashboard.tsx`, `src/ui/dashboard/cards/**`, `src/ui/dashboard/panels/**` — file-tree deletion belongs to Phase 12+ per phase-scope-fence.

Recommendation for Plan 02: create a new `src/ui/features/pretty-view/PrettyLandingCard.tsx` component (warm-glass idle card matching the pretty-conversations empty-state visual language, using `--color-pv-*` tokens per palette authority). Rendered inline in tabUtils where `case "dashboard"` used to render `<DashboardTab>`. This preserves the "dashboard" TabType as a load-bearing fallback in effectiveSelectedTabId / closeTab logic while removing every UI path to the Termix dashboard component tree. This IS the "delete not gate" pattern applied minimally: the `<DashboardTab>` render site is REMOVED (the import too), a new landing card replaces it, DashboardTab.tsx becomes dead code slated for Phase 12+.

## Section B: AppRail deletion targets (Plan 03 input)

1. File to delete: `src/ui/sidebar/AppRail.tsx`. Verify no test file exists at `src/ui/sidebar/AppRail.test.tsx` (paste `ls src/ui/sidebar/AppRail*` output).
2. Import to remove — `src/ui/AppShell.tsx:20`: `import { AppRail } from "@/sidebar/AppRail";`
3. Import to remove — `src/ui/AppShell.tsx:21`: `import type { RailView } from "@/sidebar/AppRail";`
4. Mount to remove — `src/ui/AppShell.tsx:1834-1847`: the `{sidebarOpen && !isTouchDevice && <AppRail .../>}` JSX block.
5. Import to remove — `src/ui/sidebar/SettingsRow.tsx:42`: `import type { RailView } from "@/sidebar/AppRail";` (becomes unused when SettingsRow is deleted in Plan 03).
6. Grep-verification command Plan 03 must run POST-deletion:
   ```
   grep -rn "AppRail" src/ | grep -v "\.md$"
   ```
   Expected result: zero hits in code files. Comment-only references in locale JSON files (`src/ui/locales/translated/*.json` "pinAppRail" translation keys) may remain — those are dead translation strings for the "Pin App Rail" setting that was in an earlier upstream Termix version; they were already dead before this phase (no `pinAppRail` reference in production code). Enumerate the exact list of locale files with `pinAppRail` (paste `grep -l 'pinAppRail' src/ui/locales/translated/*.json | wc -l`) and note them as "already-dead translation strings, out of scope for Phase 11 — dead-string sweep is a follow-up hygiene task."

## Section C: SettingsRow deletion targets (Plan 03 input)

Ashley 2026-07-23 lock ("we are not having settings at all — this is total, not partial") means SettingsRow MUST die with AppRail. Enumerate:

1. File to delete: `src/ui/sidebar/SettingsRow.tsx`. Verify no test file exists at `src/ui/sidebar/SettingsRow.test.tsx` (paste `ls src/ui/sidebar/SettingsRow*` output).
2. Import to remove — `src/ui/AppShell.tsx:83`: `import { SettingsRow } from "@/sidebar/SettingsRow";`
3. Mount to remove — `src/ui/AppShell.tsx:1437-1441`: the `settingsRowSlot={isTouchDevice ? <SettingsRow .../> : undefined}` JSX (the SettingsRow becomes the entire slot's content and the slot itself is dropped in Plan 03).
4. Comment history-annotations at `src/ui/AppShell.tsx:1832` and `src/ui/AppShell.tsx:2056` — cite them; Plan 03 executor may leave or strip per historical-annotation policy (Phase 10 Wave 4 precedent kept comment-only history references).
5. Grep-verification command Plan 03 must run POST-deletion:
   ```
   grep -rn "SettingsRow\|renderSettingsMenuItems" src/ | grep -v "\.md$"
   ```
   Expected result: zero hits in code files.

## Section D: PrettyConversationsPanel `settingsRowSlot` prop deletion targets (Plan 03 input)

With SettingsRow gone, the `settingsRowSlot` prop on `PrettyConversationsPanel` is orphaned. Enumerate:

1. Type field to remove — `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx:121`: `settingsRowSlot?: ReactNode;`
2. Destructure field to remove — `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx:105`: `settingsRowSlot,`
3. JSX render site to remove — `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx:418`: `{settingsRowSlot}`
4. Test to prune — `src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx:520-548`: Test 11 `describe("PrettyConversationsPanel: mobile settings slot position"...)`. The entire `describe` block deletes; header-comment index in the file's top-of-file comment (line 13: `//  11)  settingsRowSlot renders at BOTTOM of scroll region on mobile`) also updates.
5. Also strip `ReactNode` from the `import { useState, type ReactNode } from "react";` on `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx:43` IF grep shows `ReactNode` is used only for `settingsRowSlot`. Verify with `grep -n "ReactNode" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` and cite the count.

## Section E: Rail-view state machine + AppRail-only state deletion targets (Plan 03 input)

The `railView` state machine becomes dead once AppRail + SettingsRow are gone (nothing invokes `handleRailClick`, nothing sets `setRailView` other than dead-surface openSingletonTab branches). AppRail also owns a profile-dropdown open/close state field on the AppShell side that is fed to it as a prop pair and has no other consumer. Enumerate every field:

1. `src/ui/AppShell.tsx:233`: `const [railView, setRailView] = useState<RailView>("conversations");`
2. `src/ui/AppShell.tsx:234`: `const [profileDropdownOpen, setProfileDropdownOpen] = useState(false);` — **AppRail-only state, MUST strip alongside AppRail deletion.** Grep-verified consumer analysis (paste this exact command + output in the strip-list):
   ```
   grep -rn 'profileDropdownOpen\|setProfileDropdownOpen' src/
   ```
   Expected hits (as of Plan 01 authoring):
   - `src/ui/AppShell.tsx:234` — the useState declaration (this is the definition itself).
   - `src/ui/AppShell.tsx:1841` — `profileDropdownOpen={profileDropdownOpen}` in the AppRail mount JSX (dies with AppRail mount in Task 1b).
   - `src/ui/AppShell.tsx:1842` — `onProfileDropdownChange={setProfileDropdownOpen}` in the AppRail mount JSX (dies with AppRail mount in Task 1b).
   - `src/ui/sidebar/AppRail.tsx:121` — destructure of `profileDropdownOpen` prop (dies when AppRail.tsx is deleted in Task 4).
   - `src/ui/sidebar/AppRail.tsx:132` — `profileDropdownOpen: boolean;` in AppRailProps (dies with the file).
   - `src/ui/sidebar/AppRail.tsx:245` — `open={profileDropdownOpen}` in the DropdownMenu JSX (dies with the file).
   
   Conclusion: `profileDropdownOpen` is EXCLUSIVELY an AppRail-driven state. Zero surviving consumers exist post-Task-1b (AppRail mount removed) — the useState declaration becomes dead. **Plan 03 Task 1b strips line 234 (declaration) as part of Commit B alongside the other AppShell surgery.**
   
   Safety gate for Plan 03 Task 1b executor (run POST-AppRail-mount-removal but PRE-line-234-deletion):
   ```
   grep -rn 'profileDropdownOpen\|setProfileDropdownOpen' src/ | grep -v "^src/ui/sidebar/AppRail.tsx" | grep -v "^src/ui/AppShell.tsx:234:"
   ```
   Expected result: ZERO hits. If ANY hit survives (a new live consumer discovered mid-revision that wasn't in this Plan 01 grep), the executor MUST NOT delete line 234 — instead route back to Plan 01 to re-audit AND remove `profileDropdownOpen` from Plan 03's deletion action for this commit.

3. `src/ui/AppShell.tsx:333-346`: `const sidebarTitle: Record<RailView, string> = { ... };` — becomes dead (no more RailView).
4. `src/ui/AppShell.tsx:1264-1272`: `function handleRailClick(view: RailView) { ... }` — becomes dead.
5. `src/ui/AppShell.tsx:1274-1282`: `function editHostInManager(host: Host) { ... }` — becomes dead (its consumer `<HostsPanel onEditHost={editHostInManager} />` is stripped along with the {railView==="hosts"} branch).
6. `src/ui/AppShell.tsx:1097-1125` (inside `openSingletonTab`): the `host-manager`, `user-profile`, `admin-settings` branches — become dead (no caller). **`openSingletonTab` disposition (unresolved-in-context ambiguity — Plan 03 Task 1b executor must re-grep and decide):**
   
   `openSingletonTab` is defined at AppShell.tsx:1095. Grep at Plan 01 authoring time shows callers: AppRail (line 1844 — dies with mount in Task 1b), ConnectionsPanel (line 1658 — dies with panel-branch strip in Task 1b), and renderTabContent at line 2037 (survives; passed as 6th arg to tabUtils).
   
   `renderTabContent`'s consumer of `onOpenSingletonTab` is exclusively the `case "dashboard"` render of DashboardTab — which Plan 02 Task 2 REPLACES with `<PrettyLandingCard/>` (a prop-less component). So after Plan 02 lands, `onOpenSingletonTab` in tabUtils becomes an unused-but-optional prop. After Plan 03 Task 1b strips the AppRail + ConnectionsPanel branches, `openSingletonTab`'s call sites in AppShell drop to ZERO.
   
   Plan 03 Task 1b executor MUST run this grep AFTER the AppRail mount + panel-branch strip lands (before line-234 deletion / cleanup pass):
   ```
   grep -rn "openSingletonTab" src/
   ```
   Expected surviving hits (post-strip) should be: the definition at AppShell:1095, the pass-through at 2037, and tabUtils's prop signature. If those three are the ONLY hits AND tabUtils no longer invokes it for any surviving TabType (verify by reading tabUtils's `case` blocks — post-Plan-02, only rdp/vnc/telnet/terminal/tunnel/split-screen/etc consume args, none use onOpenSingletonTab), THEN mark `openSingletonTab` + its pass-through at line 2037 for strip in the SAME Commit B.
   
   If any live consumer surfaces (URL-restore path, `deepLinkOpen`, or an event handler), KEEP the function with an inline comment `// load-bearing: <exact consumer file:line>` naming what still uses it. Do NOT strip on suspicion. This resolves the earlier fork ambiguity that noted openSingletonTab as "possibly dead."

7. `src/ui/AppShell.tsx:1697-1725`: `const sidebarHeader = ( ... {sidebarTitle[railView]} ... );` — the header consumes `sidebarTitle[railView]`. When railView is stripped, `sidebarHeader` shows only the "Conversations" title (drop the Record lookup, hardcode the i18n key `t("nav.conversations.title", { defaultValue: "Conversations" })`).
8. `src/ui/AppShell.tsx:1400-1687`: `const sidebarPanelContent = (...)` — the entire block currently renders 11 sibling `{railView === "X" && <XPanel />}` conditionals. All EXCEPT the `railView === "conversations"` branch become dead. Plan 03 strips ALL non-conversations branches, leaving ONLY the `<PrettyConversationsPanel .../>` wrapper (drop the outer `<div className="hidden">` toggle too since it's the only panel).
9. `src/ui/AppShell.tsx:22-31 + 59`: the 11 panel imports (`HostsPanel`, `SessionsPanel`, `QuickConnectPanel`, `SshToolsPanel`, `SnippetsPanel`, `HistoryPanel`, `SplitScreenPanel`, `UserProfilePanel`, `AdminSettingsPanel`, `CredentialsPanel`, `ConnectionsPanel`) — become unused when their `{railView === "X"}` branches are stripped. Plan 03 removes ALL 11 import lines. The panel files themselves STAY on disk for Phase 12+ per scope-fence.

## Section F: Verification gate (mandatory for BOTH Plan 02 and Plan 03)

Every deletion plan MUST close its atomic commit sequence with these gates (paste the exact commands):

```
grep -rn "AppRail" src/ | grep -v "\.md$" | grep -v "pinAppRail" | wc -l   # Expected: 0 after Plan 03
grep -rn "SettingsRow\|renderSettingsMenuItems" src/ | grep -v "\.md$" | wc -l   # Expected: 0 after Plan 03
grep -rn "DashboardTab" src/ | grep -v "\.md$" | grep -v "dashboard/" | wc -l   # Expected: 0 outside src/ui/dashboard/ after Plan 02
grep -rn "profileDropdownOpen\|setProfileDropdownOpen" src/ | grep -v "\.md$" | wc -l   # Expected: 0 after Plan 03 Task 1b + Task 4
npx tsc --noEmit   # Expected exit 0
npx vitest run   # Expected: no NEW failures beyond the documented pre-existing 4 ComposeBox failures (deferred-items.md)
npm run build   # Expected exit 0 (run once at the Plan 04 boundary, not per-commit)
```

## Section G: What is EXPLICITLY out of scope for Phase 11

Per CONTEXT.md `<deferred>`, mirror the exclusions here so the executor of Plans 02 + 03 has a bright line:

- `src/ui/sidebar/HostsPanel.tsx` / `HostManager.tsx` / `HostCredentialList.tsx` / `HostEditor*.tsx` / `HostShareModal.tsx` / `CredentialEditorView.tsx` / `CredentialsPanel.tsx` — Phase 12+.
- `src/ui/sidebar/SessionsPanel.tsx` / `SnippetsPanel.tsx` / `SplitScreenPanel.tsx` / `SshToolsPanel.tsx` / `QuickConnectPanel.tsx` / `HistoryPanel.tsx` / `UserProfilePanel.tsx` — Phase 12+.
- `src/ui/sidebar/AdminSettingsPanel.tsx` / `AdminApiKeysSection.tsx` / `AdminIdentitiesSection.tsx` / `AdminManagementSections.tsx` / `AdminSettingsSections.tsx` / `AdminSettingsShared.tsx` / `AdminUserDialogs.tsx` / `ConnectionsPanel.tsx` / `SidebarTree.tsx` — Phase 12+.
- `src/ui/dashboard/**` — Phase 12+.
- Backend routes (`/host/db/*`, `/identities/*`) + encrypted-SQLite data layer — untouched (PURGE-04).
- Terminal / RDP / VNC / Guacamole panes + tab plumbing — untouched (PURGE-05).

Save the file. Do NOT modify any source files (`.ts`, `.tsx`, `.json` etc.). This plan is a discovery + documentation pass only.
  </action>
  <verify>
    <automated>test -f /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-01-STRIP-LIST.md && grep -qc "Section A" /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-01-STRIP-LIST.md && grep -qc "Section B" /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-01-STRIP-LIST.md && grep -qc "Section C" /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-01-STRIP-LIST.md && grep -qc "Section D" /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-01-STRIP-LIST.md && grep -qc "Section E" /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-01-STRIP-LIST.md && grep -qc "Section F" /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-01-STRIP-LIST.md && grep -qc "Section G" /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-01-STRIP-LIST.md && grep -q "profileDropdownOpen" /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-01-STRIP-LIST.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-01-STRIP-LIST.md`
    - File contains all 7 sections (A through G) — verify with `grep -c "^## Section" 11-01-STRIP-LIST.md` returns 7
    - Section A cites `AppShell.tsx:180` OR `AppShell.tsx:180-188` (initial tab seed line)
    - Section A cites `tabUtils.tsx:187` OR `tabUtils.tsx:187-193` (dashboard render case)
    - Section B cites `AppShell.tsx:20`, `AppShell.tsx:21`, and `AppShell.tsx:1834` OR nearby line numbers for the AppRail mount
    - Section C cites `AppShell.tsx:83` and `AppShell.tsx:1439` OR nearby line numbers for the SettingsRow import + mount
    - Section D names `PrettyConversationsPanel.tsx` lines for the `settingsRowSlot` prop type + destructure + JSX render site
    - Section E enumerates `profileDropdownOpen` + `setProfileDropdownOpen` as an AppRail-only strip target (item 2) with grep-verified consumer analysis
    - Section E provides the exact grep safety-gate command for the Plan 03 Task 1b executor to re-run before deleting line 234
    - Section E enumerates the 11 panel imports (HostsPanel, SessionsPanel, QuickConnectPanel, SshToolsPanel, SnippetsPanel, HistoryPanel, SplitScreenPanel, UserProfilePanel, AdminSettingsPanel, CredentialsPanel, ConnectionsPanel)
    - Section E provides an `openSingletonTab` disposition protocol (grep + keep-or-strip decision tree) for the Plan 03 Task 1b executor
    - Section F contains the exact `grep -rn "AppRail" src/` verification command Plan 03 will run
    - Section F contains the `grep -rn "profileDropdownOpen\|setProfileDropdownOpen" src/` gate expected to hit 0 after Plan 03
    - Section G explicitly lists dashboard/ and the sidebar/ panel files as OUT-OF-SCOPE-FOR-PHASE-11 (Phase 12+ deletion targets)
    - Zero source files modified: `git status src/ 2>/dev/null | wc -l` returns 0
  </acceptance_criteria>
  <done>The 11-01-STRIP-LIST.md file exists with all 7 sections; every claim about a deletion target cites the AppShell.tsx / tabUtils.tsx / PrettyConversationsPanel.tsx line number that Plans 02 and 03 will act on; profileDropdownOpen is explicitly enumerated with grep-verified consumer analysis; no source files under `src/` have been modified.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the strip-list document</name>
  <files>.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-01-STRIP-LIST.md</files>
  <read_first>
    - /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-01-STRIP-LIST.md (the file just written — confirm content before commit)
  </read_first>
  <action>
Stage and commit the strip-list file to git with the message:

```
docs(phase-11): enumerate deletion targets for landing-swap + AppRail retirement

Plan 01 (enumeration) output: 11-01-STRIP-LIST.md. Cites exact line numbers
in AppShell.tsx / tabUtils.tsx / PrettyConversationsPanel.tsx for every
deletion target Plan 02 (landing surface) and Plan 03 (AppRail + SettingsRow
+ rail-view state machine + profileDropdownOpen AppRail-only state) will act
on. Also enumerates the Phase 12+ deferred targets (sidebar/ panel files,
dashboard/ tree) for the scope fence.
```

Use `git add` on the specific file path only (no `-A`, no `.`). No source files should be staged.
  </action>
  <verify>
    <automated>cd /home/ubuntu/termix && git log -1 --name-only --format="%s" 2>/dev/null | grep -qE "11-01-STRIP-LIST\.md|phase-11.*deletion targets"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --name-only` shows commit that touches ONLY `.planning/phases/11-.../11-01-STRIP-LIST.md`
    - `git status --porcelain` shows a clean working tree (no untracked or modified files remaining from this plan)
    - Commit message contains "phase-11" and "deletion targets" (or equivalent per action text)
    - No files under `src/` are in the commit's file list
  </acceptance_criteria>
  <done>Strip-list committed as a single documentation commit with no source-tree impact; Plans 02 and 03 can now consume the strip-list as their deletion-target contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner→executor | The strip-list is the ONLY input contract Plans 02 and 03 have for what to delete. If the strip-list omits a target, that target survives Phase 11 and re-appears as a bug (surface still reachable via UI OR broken import after deletion). |
| executor→disk | This plan writes one markdown file. No source-tree modifications. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Information Disclosure | 11-01-STRIP-LIST.md | mitigate | The strip-list contains internal file paths + line numbers but no secrets, credentials, or PII. Committed to the same repo state as the source code it references. No additional exposure vs. reading the source directly. |
| T-11-01-02 | Tampering | 11-01-STRIP-LIST.md | mitigate | Verify accuracy at write-time by cross-checking each cited line number against the actual file content (executor reads AppShell.tsx / tabUtils.tsx / PrettyConversationsPanel.tsx in `<read_first>` and validates line numbers before writing). Downstream plans do NOT re-derive targets from source; they consume the strip-list. If strip-list is wrong, downstream plans propagate the error — mitigation is the executor's mandatory line-number cross-check. |
| T-11-01-03 | Denial of Service (regression scope creep) | Downstream Plans 02 + 03 | mitigate | Section G explicitly enumerates OUT-OF-SCOPE Phase 12+ files. This is the phase-scope-fence — if Plan 02 or 03 executor sees a target NOT in the strip-list (e.g., discovers a stray Dashboard.tsx reference during their work), the executor MUST route back to the planner rather than expanding scope. |
| T-11-01-04 | Elevation of Privilege (Plan 03 deletes a live consumer state) | AppShell state consumers post-AppRail-strip | mitigate | Section E item 2 (profileDropdownOpen) and item 6 (openSingletonTab) provide EXPLICIT grep safety-gate commands for the Plan 03 Task 1b executor to re-run at edit time. If a live consumer surfaces that wasn't in Plan 01's grep, the executor is DIRECTED to keep the state and route back — never delete on suspicion. |
</threat_model>

<verification>
- The 11-01-STRIP-LIST.md file exists in the phase directory.
- File contains all 7 sections A-G with the exact section headings.
- Every deletion target claim cites a line number OR a grep command with its expected result.
- Section E item 2 explicitly enumerates `profileDropdownOpen` + `setProfileDropdownOpen` with consumer analysis.
- Zero source files modified: `git status src/` returns empty.
- One commit landed with only the strip-list file in its diff.
</verification>

<success_criteria>
- Plan 02 executor and Plan 03 executor can read the strip-list and know exactly what to modify without further discovery reads of the source tree beyond the specific files named in each of their own `<read_first>` blocks.
- The strip-list distinguishes Phase 11 deletion targets (AppRail, SettingsRow, `settingsRowSlot` prop, dashboard landing swap, rail-view state machine, `profileDropdownOpen` AppRail-only state) from Phase 12+ deferred targets (sidebar/ panels, dashboard/ tree).
- Route consistency between Plan 01 (enumeration) and Plan 03 (deletion) is guaranteed: nothing in Plan 03's deletion action is absent from Plan 01's strip-list.
</success_criteria>

<output>
Create `.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-01-SUMMARY.md` when done.
</output>
</content>
</invoke>