---
phase: 11-skynet-transformation-purge-dead-termix-surfaces-first-slice
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - src/ui/AppShell.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
  - src/ui/sidebar/AppRail.tsx
  - src/ui/sidebar/SettingsRow.tsx
autonomous: true
requirements:
  - PURGE-02
  - PURGE-03
  - PURGE-04
  - PURGE-05
tags: [ui, app-shell, sidebar, deletion, cleanup, skynet-purge, apprail-retirement]

must_haves:
  truths:
    - "The AppRail component file src/ui/sidebar/AppRail.tsx no longer exists on disk (PURGE-02)"
    - "The SettingsRow component file src/ui/sidebar/SettingsRow.tsx no longer exists on disk (Ashley's 'we are not having settings at all' lock)"
    - "Zero imports of @/sidebar/AppRail or @/sidebar/SettingsRow remain anywhere under src/ (PURGE-02)"
    - "The rail-view state machine in AppShell.tsx (railView state, handleRailClick, sidebarTitle Record, RailView type, profileDropdownOpen state) is fully removed (PURGE-03)"
    - "No visible UI navigation path exists from the pretty-conversations sidebar to the Termix dashboard, host manager, snippets manager, admin console, or any settings surface (PURGE-03)"
    - "Backend routes /host/db/* and /identities/* are unchanged — this plan touches zero backend files (PURGE-04)"
    - "Phase 7's RDP-host-sentinel row in the pretty-conversations panel continues to open Guacamole panes via the onRdpRowClick handler (PURGE-05)"
    - "TypeScript compiles clean at EVERY commit boundary (Test 11 prune → AppShell surgery → panel prop drop → SettingsRow delete → AppRail delete — five atomic commits, tsc-clean-per-commit per Phase 10 Wave 4 precedent)"
    - "The test suite passes at the Wave 2 baseline (documented pre-existing 4 ComposeBox failures unchanged)"
  artifacts:
    - path: "src/ui/AppShell.tsx"
      provides: "AppShell without the rail-view state machine, without AppRail/SettingsRow imports+mounts, without the profileDropdownOpen AppRail-only state, without the 10 dead-panel branches"
      contains: "PrettyConversationsPanel"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "PrettyConversationsPanel without the vestigial settingsRowSlot prop"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx"
      provides: "Test file with Test 11 (settingsRowSlot position) removed"
  key_links:
    - from: "src/ui/AppShell.tsx"
      to: "PrettyConversationsPanel"
      via: "the ONLY sidebar-panel-content rendered"
      pattern: "PrettyConversationsPanel"
    - from: "src/ui/AppShell.tsx"
      to: "AppRail"
      via: "MUST NOT be present after this plan"
      pattern: "AppRail"
---

<objective>
Retire the left AppRail (PURGE-02), retire the SettingsRow (Ashley's total-not-partial "no settings" lock), and strip the rail-view state machine in AppShell.tsx (railView + sidebarTitle + handleRailClick + editHostInManager + profileDropdownOpen — the AppRail-only state per Plan 01 Section E item 2) along with the 10 dead sidebar-panel-content branches (host manager, credentials, sessions, quick-connect, ssh-tools, snippets, history, split-screen, connections, user-profile, admin-settings). Removes every visible UI path from the pretty-conversations sidebar to a dead surface (PURGE-03). Preserves the backend + tab plumbing + RDP wiring untouched (PURGE-04, PURGE-05).

Purpose: The AppRail is the primary UI entry point to every Termix dead surface. SettingsRow is the mobile duplicate entry point. Both die together per Ashley 2026-07-23 lock: "we are not having settings at all — this is total, not partial." Once these two surfaces are gone AND the state machine that drove them is stripped, the pretty-conversations sidebar is the ONLY visible sidebar chrome, and its only rows are conversation rows + RDP-sentinel rows.

**Structural change (checker B-3 fix):** The original Plan 03 wrapped the coordinated 3-file edit (Test 11 prune → AppShell surgery → panel prop drop) under a SINGLE `<verify>` gate that only ran once after all three commits landed. The Phase 10 Wave 4 tsc-clean-per-commit precedent (and the threat model's own commit-boundary guarantee) demands EACH commit's atomicity be verified against tsc + targeted vitest before the next commit lands. This revision SPLITS that one task into three sequential tasks — Task 1 (Test 11 prune), Task 2 (AppShell surgery), Task 3 (panel prop drop) — each with its own `<verify><automated>` gate. SettingsRow delete moves to Task 4, AppRail delete moves to Task 5.

Output: Two file deletions (AppRail.tsx, SettingsRow.tsx), one AppShell.tsx surgery, one PrettyConversationsPanel.tsx prop-and-JSX prune, one PrettyConversationsPanel.test.tsx test-11 prune. Atomic-per-file commit sequence per Phase 10 Wave 4 precedent (targeting 5 commits, 5 separate `<verify>` gates).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-CONTEXT.md
@.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-01-STRIP-LIST.md
@.planning/phases/10-pretty-conversations-visual-language-rework/10-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (was 1a): Prune Test 11 (settingsRowSlot mobile position) from PrettyConversationsPanel.test.tsx</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx</files>
  <read_first>
    - /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-01-STRIP-LIST.md (Section D item 4 — Test 11 identification)
    - /home/ubuntu/termix/src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx (lines 11-13 for file-header comment index; lines 520-548 for Test 11 describe block)
    - /home/ubuntu/termix/.planning/phases/10-pretty-conversations-visual-language-rework/10-04-SUMMARY.md ("Ordering Adjustment" section — precedent for atomic-tsc-clean-per-commit; Test 11 goes first because removing a test is scope-independent of the prop's presence)
  </read_first>
  <action>
This task lands ordering-first because removing the test is scope-independent of the prop's presence: the prop still exists on the panel and is still passed from AppShell at this point in the commit sequence. Removing the test leaves tsc + vitest clean.

Modify `/home/ubuntu/termix/src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx`:

1. Delete the entire `describe("PrettyConversationsPanel: mobile settings slot position", () => { ... });` block (lines 520-549 based on the current file — verify at edit time and delete the exact block).
2. Update the file-header comment index at line 13: change `//  11)  settingsRowSlot renders at BOTTOM of scroll region on mobile` to `//  11)  RETIRED — settingsRowSlot prop dropped in Phase 11 (Ashley's "no settings" lock)`.
3. Do NOT renumber Tests 12-15 — Phase 10 Wave 4 precedent kept renumbering off the table to preserve blame-history readability.

Commit as: `test(pretty-conversations): prune Test 11 (settingsRowSlot mobile position — Phase 11 retires settings surface)`

Post-edit verification (BEFORE commit):
- `grep -c 'describe("PrettyConversationsPanel: mobile settings slot position"' src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` returns 0 (Test 11 block removed).
- `grep -c 'RETIRED — settingsRowSlot' src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` returns 1 (comment updated).
- `npx tsc --noEmit` exits 0.
- `npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` — expect all remaining tests to pass (14 tests — 15 original minus Test 11).
  </action>
  <verify>
    <automated>cd /home/ubuntu/termix && grep -c 'describe("PrettyConversationsPanel: mobile settings slot position"' src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx | grep -q "^0$" && grep -q "RETIRED — settingsRowSlot" src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx && npx tsc --noEmit 2>&1 | grep -qvE "error TS" && npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx 2>&1 | grep -qE "passed"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'describe("PrettyConversationsPanel: mobile settings slot position"' src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` returns exactly `0`
    - `grep -c "RETIRED — settingsRowSlot" src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` returns exactly `1`
    - `npx tsc --noEmit` exits 0
    - `npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` shows 14 tests passing (Test 11 pruned; Tests 1-10, 12-15 all still pass)
    - `git log -1 --name-only --format="%s"` shows the commit touches ONLY `src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx`
    - Commit message contains "Test 11" AND "settingsRowSlot"
  </acceptance_criteria>
  <done>Test 11 pruned atomically; tree is tsc-clean and vitest-green; single-file commit landed; Task 2 can now proceed with AppShell surgery.</done>
</task>

<task type="auto">
  <name>Task 2 (was 1b): AppShell.tsx surgery — strip rail-view state machine, AppRail/SettingsRow mounts, profileDropdownOpen, and 10 dead panel branches</name>
  <files>src/ui/AppShell.tsx</files>
  <read_first>
    - /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-01-STRIP-LIST.md (Sections B, C, E — every deletion target with line numbers, INCLUDING Section E item 2 profileDropdownOpen + item 6 openSingletonTab disposition protocol)
    - /home/ubuntu/termix/src/ui/AppShell.tsx (full file — this is the heaviest single-file surgery in Phase 11)
    - /home/ubuntu/termix/.planning/phases/10-pretty-conversations-visual-language-rework/10-04-SUMMARY.md ("Ordering Adjustment (Rule 3 — Blocking Issue Avoidance)" section — precedent for atomic-tsc-clean-per-commit reordering)
  </read_first>
  <action>
Modify `/home/ubuntu/termix/src/ui/AppShell.tsx` in ONE commit landing all of the following edits (this task lands second because at this point, PrettyConversationsPanel still accepts `settingsRowSlot` as optional; AppShell dropping the prop-passthrough leaves the tree tsc-clean under TypeScript optional-prop rules).

1. Remove imports (top of file):
   - Line 20: `import { AppRail } from "@/sidebar/AppRail";` — DELETE
   - Line 21: `import type { RailView } from "@/sidebar/AppRail";` — DELETE
   - Lines 22-31 (10 sidebar panel imports: HostsPanel, SessionsPanel, QuickConnectPanel, SshToolsPanel, SnippetsPanel, HistoryPanel, SplitScreenPanel, UserProfilePanel, AdminSettingsPanel, CredentialsPanel) — DELETE ALL 10.
   - Line 59: `import { ConnectionsPanel } from "@/sidebar/ConnectionsPanel";` — DELETE
   - Line 83: `import { SettingsRow } from "@/sidebar/SettingsRow";` — DELETE

2. Remove state fields and derived structures (order matters — do the AppRail mount removal in step 7 FIRST, then re-run the safety grep for profileDropdownOpen, THEN delete line 234):
   - Line 233: `const [railView, setRailView] = useState<RailView>("conversations");` — DELETE
   - Lines 333-346: the `sidebarTitle: Record<RailView, string> = { ... }` const — DELETE
   - Line 234: `const [profileDropdownOpen, setProfileDropdownOpen] = useState(false);` — **DELETE per Plan 01 Section E item 2** (AppRail-only state; consumer analysis proved zero surviving consumers post-AppRail-strip).
   
   **MANDATORY safety gate for line 234 deletion (per Plan 01 Section E item 2):** After removing the AppRail mount at step 7 below but BEFORE deleting line 234, run:
   ```
   grep -rn 'profileDropdownOpen\|setProfileDropdownOpen' src/ | grep -v "^src/ui/sidebar/AppRail.tsx" | grep -v "^src/ui/AppShell.tsx:234:"
   ```
   Expected output: ZERO hits. If ANY hit appears, DO NOT delete line 234 — a live consumer exists that was not in Plan 01's grep. Instead: (a) keep line 234 with a `// load-bearing: <exact consumer file:line>` comment, (b) drop the `profileDropdownOpen` deletion from this commit's action, and (c) route back to update Plan 01's strip-list Section E for post-hoc accuracy. Do NOT delete on suspicion.

3. Strip dead branches from openSingletonTab (defined at line 1095):
   - The `if (type === "host-manager") { ... }` block (lines 1097-1118) — DELETE ENTIRELY.
   - The `if (type === "user-profile" || type === "admin-settings") { ... }` block (lines 1120-1125) — DELETE ENTIRELY.
   - **`openSingletonTab` full-function disposition** (per Plan 01 Section E item 6 protocol): After the above dead-branch deletions AND after step 7's AppRail mount removal, run this repo-wide grep:
     ```
     grep -rn "openSingletonTab" src/
     ```
     Expected surviving hits should be limited to:
     - The definition at `AppShell.tsx:1095` (or wherever it lands after edits above).
     - The pass-through in `renderTabContent(..., openSingletonTab, ...)` at approximately `AppShell.tsx:2037`.
     - The optional-prop signature `onOpenSingletonTab?: (type: TabType) => void` in `src/ui/shell/tabUtils.tsx`.
     - Zero hits under `src/ui/features/`, `src/ui/sidebar/` (AppRail is now stripped so ConnectionsPanel's caller dies with the mount removal), or elsewhere.
     
     IF the grep returns ONLY those 3 categories AND no surviving TabType `case` in `tabUtils.tsx` invokes `onOpenSingletonTab` (post-Plan-02, `case "dashboard"` renders `<PrettyLandingCard/>` which takes no props, so `onOpenSingletonTab` has no live consumer), THEN strip the `openSingletonTab` function definition entirely AND drop the pass-through at line 2037. The tabUtils optional prop remains (TypeScript allows the call site to pass undefined).
     
     IF the grep surfaces ANY additional live consumer (URL-restore path, `deepLinkOpen`, event handlers, keyboard shortcut handlers), KEEP the function with an inline comment `// load-bearing: <exact consumer file:line>` naming what still uses it. Do NOT strip on suspicion. Document the disposition in this commit's message body: either "openSingletonTab stripped (grep confirmed no surviving consumers)" or "openSingletonTab kept (load-bearing for <consumer>)".

4. Strip dead helper functions:
   - Lines 1264-1272: `function handleRailClick(view: RailView) { ... }` — DELETE ENTIRELY.
   - Lines 1274-1282: `function editHostInManager(host: Host) { ... }` — DELETE ENTIRELY.

5. Rewrite `sidebarPanelContent` (currently lines 1400-1687):

The current block renders 11 sibling railView-conditional branches. Strip ALL of them EXCEPT the `conversations` branch. Replace the entire const definition with (paste into AppShell.tsx replacing the old block):

```jsx
const sidebarPanelContent = (
  <div className="flex flex-col flex-1 min-h-0 overflow-hidden">
    <div className="flex flex-col flex-1 min-h-0">
      <PrettyConversationsPanel
        variant={isMobile ? "mobile" : "desktop"}
        onConversationSelected={
          isTouchDevice ? () => navigateToView() : undefined
        }
        hostTree={realHostTree}
        onCreateSession={({ host, sessionName }) => {
          const newTabId = openTab(host, "terminal", undefined, {
            targetTmuxSession: sessionName ?? null,
            label: sessionName ?? undefined,
            allowCreateTmux: true,
          });
          selectConversationDeferred(newTabId);
          if (isTouchDevice) navigateToView();
          if (isMobile) setSidebarOpen(false);
        }}
        onDetachedRowClick={(row) => {
          const host = row.host;
          if (!host) return;
          const sessionName = row.targetTmuxSession;
          if (!sessionName) return;
          const newTabId = openTab(host, "terminal", undefined, {
            targetTmuxSession: sessionName,
            label: sessionName,
            allowCreateTmux: false,
          });
          selectConversationDeferred(newTabId);
          if (isTouchDevice) navigateToView();
          if (isMobile) setSidebarOpen(false);
        }}
        onRdpRowClick={(row) => {
          const host = row.host;
          if (!host) return;
          const newTabId = openTab(host, "rdp");
          selectConversationDeferred(newTabId);
          if (isTouchDevice) navigateToView();
          if (isMobile) setSidebarOpen(false);
        }}
      />
    </div>
  </div>
);
```

Explanatory comments describing the retired state machine can go ABOVE the `const sidebarPanelContent` declaration as multiline `/* ... */` history annotation. Keep the comment short. Do NOT restore the old railView-branching.

Note the deletions from the mount site:
- The `settingsRowSlot={...}` prop line — DELETED.
- The 10 sibling `{railView === "X" && <XPanel />}` branches — DELETED.

6. Update sidebarHeader (lines 1691-1725):
   - At line 1699: `{sidebarTitle[railView]}` — REPLACE with `{t("nav.conversations.title", { defaultValue: "Conversations" })}`.

7. Remove the AppRail mount (lines 1826-1847 — the JSX block AND its preceding comment block):
   - Delete lines 1826-1847 entirely. The block starts with `{/* Skinny icon rail — non-touch devices only... */}` and ends with the closing `)}` of the `{sidebarOpen && !isTouchDevice && (<AppRail .../>)}` render conditional. This mount removal deletes the `profileDropdownOpen={profileDropdownOpen}` (line ~1841) and `onProfileDropdownChange={setProfileDropdownOpen}` (line ~1842) prop passes — after this step lands, the ONLY remaining `profileDropdownOpen` reference in AppShell.tsx is the useState declaration at line 234, which step 2's safety-gate grep now verifies before deletion.

8. Preserve untouched:
   - All `openTab`, `connectHost`, `doCloseTab`, `closeTab`, `assignPane` functions.
   - The `sidebarOpen` / `setSidebarOpen` state (still consumed by the persistent-toggle chevron).
   - The persistent-toggle chevron block (lines 1778-1824).
   - The mobile-list-screen + mobile-view-screen render branches.
   - The normal-view container + tab-node portal loop.
   - ALL RDP/VNC/Guacamole tab-lifecycle code paths.
   - The initial-tab and doCloseTab-fallback (Plan 02 owned these — `type: "dashboard"` preserved as fallback identifier).
   - Comment-only `//` and `/* ... */` annotations mentioning AppRail / SettingsRow / RailView / ConversationsPanel are ACCEPTABLE per Phase 10 Wave 4 policy (they are historical annotations, not code). Executor may leave them or sweep them — either is fine.

Commit message: `refactor(app-shell): strip rail-view state machine + AppRail/SettingsRow mounts + 10 dead panel branches + profileDropdownOpen (Phase 11 PURGE-02, PURGE-03)`

Include in the commit body:
- The `openSingletonTab` disposition decision (stripped OR kept, with grep evidence).
- Confirmation that the `profileDropdownOpen` safety-gate grep returned 0 hits before line 234 deletion.

Post-edit verification (inline, before commit):
- `grep -v "^\s*//" src/ui/AppShell.tsx | grep -v "^\s*\*" | grep -cE "^\s*import.*AppRail"` returns 0
- `grep -v "^\s*//" src/ui/AppShell.tsx | grep -v "^\s*\*" | grep -cE "^\s*import.*SettingsRow"` returns 0
- `grep -c "railView\b" src/ui/AppShell.tsx` returns 0
- `grep -c "handleRailClick" src/ui/AppShell.tsx` returns 0
- `grep -c "profileDropdownOpen\|setProfileDropdownOpen" src/ui/AppShell.tsx` returns 0
- `grep -c "PrettyConversationsPanel" src/ui/AppShell.tsx` returns >= 1
- `grep -c "<AppRail" src/ui/AppShell.tsx` returns 0
- `grep -c "<SettingsRow" src/ui/AppShell.tsx` returns 0
- `grep -c "settingsRowSlot" src/ui/AppShell.tsx` returns 0
- `npx tsc --noEmit` exits 0
- `npx vitest run src/ui/AppShell.persistence.test.tsx` all pass

At this commit boundary, the tree is tsc-clean because PrettyConversationsPanel still accepts `settingsRowSlot` as an optional prop and TypeScript allows AppShell to not pass it. Task 3 drops the panel-side prop next.
  </action>
  <verify>
    <automated>cd /home/ubuntu/termix && test $(grep -v "^\s*//" src/ui/AppShell.tsx | grep -v "^\s*\*" | grep -cE "import.*AppRail|<AppRail|handleRailClick|editHostInManager|profileDropdownOpen") -eq 0 && test $(grep -c "railView\b" src/ui/AppShell.tsx) -eq 0 && test $(grep -c "sidebarTitle" src/ui/AppShell.tsx) -eq 0 && test $(grep -c "settingsRowSlot" src/ui/AppShell.tsx) -eq 0 && grep -q "PrettyConversationsPanel" src/ui/AppShell.tsx && npx tsc --noEmit 2>&1 | grep -qvE "error TS" && npx vitest run src/ui/AppShell.persistence.test.tsx 2>&1 | grep -qE "passed"</automated>
  </verify>
  <acceptance_criteria>
    - Non-comment count of `AppRail` in `src/ui/AppShell.tsx` is 0 — check with `grep -v "^\s*//" src/ui/AppShell.tsx | grep -v "^\s*\*" | grep -c "AppRail"`
    - Non-comment count of `SettingsRow` in `src/ui/AppShell.tsx` is 0 — same grep pattern
    - `grep -c "railView\b" src/ui/AppShell.tsx` (all lines) returns 0
    - `grep -c "handleRailClick" src/ui/AppShell.tsx` (all lines) returns 0
    - `grep -c "sidebarTitle" src/ui/AppShell.tsx` (all lines) returns 0
    - `grep -c "editHostInManager" src/ui/AppShell.tsx` (all lines) returns 0
    - `grep -c "profileDropdownOpen\|setProfileDropdownOpen" src/ui/AppShell.tsx` (all lines) returns 0
    - `grep -c "settingsRowSlot" src/ui/AppShell.tsx` (all lines) returns 0
    - `grep -c "PrettyConversationsPanel" src/ui/AppShell.tsx` returns >= 2 (import + JSX)
    - `grep -c "HostsPanel\|SessionsPanel\|CredentialsPanel\|QuickConnectPanel\|SshToolsPanel\|SnippetsPanel\|HistoryPanel\|SplitScreenPanel\|ConnectionsPanel\|UserProfilePanel\|AdminSettingsPanel" src/ui/AppShell.tsx` (non-comment lines) returns 0
    - `npx tsc --noEmit` exits 0 (tree is tsc-clean at this commit boundary; PrettyConversationsPanel's optional settingsRowSlot prop is unpassed but the panel still declares it)
    - `npx vitest run src/ui/AppShell.persistence.test.tsx` all pass
    - Commit message body includes the `openSingletonTab` disposition (stripped OR kept-load-bearing with cited consumer) AND confirmation the `profileDropdownOpen` safety-gate grep returned 0
    - Single-file commit lands touching ONLY `src/ui/AppShell.tsx`
  </acceptance_criteria>
  <done>Rail-view state machine gone; AppRail mount gone; SettingsRow mount gone; profileDropdownOpen state gone (with safety-gate evidence); settingsRowSlot mount-side prop gone; openSingletonTab disposition documented; tsc clean; persistence tests green; single-file commit landed.</done>
</task>

<task type="auto">
  <name>Task 3 (was 1c): Drop settingsRowSlot prop from PrettyConversationsPanel.tsx</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx</files>
  <read_first>
    - /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-01-STRIP-LIST.md (Section D — settingsRowSlot prop targets)
    - /home/ubuntu/termix/src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (lines 100-135 for the current prop signature; line 418 for the settingsRowSlot JSX render site; line 43 for the ReactNode import)
  </read_first>
  <action>
This task lands third because Task 2's AppShell surgery already stopped passing the `settingsRowSlot` prop — the panel currently accepts an optional prop that no caller passes. Dropping the prop from the panel's type + destructure + JSX render site is now scope-independent of any caller.

Modify `/home/ubuntu/termix/src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`:

1. Line 105: `settingsRowSlot,` inside the destructure — DELETE.
2. Line 121: `settingsRowSlot?: ReactNode;` inside the type signature — DELETE.
3. Line 418: `{settingsRowSlot}` — DELETE (this is the JSX render site inside the scroll region).
4. Line 43: `import { useState, type ReactNode } from "react";` — verify with `grep -c "ReactNode" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` post-edit. If the count drops to 0, remove `, type ReactNode` from the import.
5. File-header comments at lines 18-23 and 33 that mention `settingsRowSlot` — update to note the retirement, e.g., `//   - settingsRowSlot prop retired in Phase 11 (Ashley's "no settings" lock — SettingsRow deleted alongside AppRail)`. Comment-only sweep, no functional change.

Commit as: `refactor(pretty-conversations): drop vestigial settingsRowSlot prop (Phase 11 PURGE-03)`

Post-edit verification (BEFORE commit):
- `grep -v "^\s*//" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx | grep -c "settingsRowSlot"` returns 0 (non-comment lines have zero hits)
- `npx tsc --noEmit` exits 0
- `npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` — expect all 14 tests to pass (Test 11 was pruned in Task 1)
- Full `npx vitest run` — expect no new failures beyond the 4 pre-existing ComposeBox failures
  </action>
  <verify>
    <automated>cd /home/ubuntu/termix && test $(grep -v "^\s*//" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx | grep -c "settingsRowSlot") -eq 0 && npx tsc --noEmit 2>&1 | grep -qvE "error TS" && npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx 2>&1 | grep -qE "passed"</automated>
  </verify>
  <acceptance_criteria>
    - Non-comment count of `settingsRowSlot` in `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` is 0 — check with `grep -v "^\s*//" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx | grep -c "settingsRowSlot"`
    - Comment-only mentions of `settingsRowSlot` acceptable per historical-annotation policy
    - `npx tsc --noEmit` exits 0
    - `npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` shows 14 tests passing
    - Full `npx vitest run` shows no NEW failures beyond documented pre-existing 4 ComposeBox failures
    - Single-file commit lands touching ONLY `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`
  </acceptance_criteria>
  <done>PrettyConversationsPanel API is clean of the vestigial settingsRowSlot prop; tsc clean; 14-of-14 panel tests green; single-file commit landed.</done>
</task>

<task type="auto">
  <name>Task 4 (was 2): Delete src/ui/sidebar/SettingsRow.tsx</name>
  <files>src/ui/sidebar/SettingsRow.tsx</files>
  <read_first>
    - /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-01-STRIP-LIST.md (Section C)
    - /home/ubuntu/termix/src/ui/sidebar/SettingsRow.tsx (the file to delete — read to confirm no unexpected named exports beyond `SettingsRow` and `renderSettingsMenuItems`)
  </read_first>
  <action>
Prerequisite check (before deleting):

Run `grep -rn "SettingsRow\|renderSettingsMenuItems" src/ | grep -v "^src/ui/sidebar/SettingsRow.tsx" | grep -v "\.md$"` — expected output: ZERO hits in non-comment code lines (Task 2's AppShell surgery removed the import and mount). If any hit appears in a `.tsx` / `.ts` code file (non-comment), STOP and route back to Task 2 — an import survived.

Delete the file:
```
git rm src/ui/sidebar/SettingsRow.tsx
```

Post-delete verification:
- `test ! -f src/ui/sidebar/SettingsRow.tsx` — file MUST NOT exist.
- `grep -rn "from \"@/sidebar/SettingsRow\"" src/ | wc -l` returns 0.
- `grep -rn "from \"./SettingsRow\"" src/ | wc -l` returns 0.
- `grep -rn "renderSettingsMenuItems" src/ | grep -v "\.md$" | wc -l` returns 0 (this helper was ONLY exported from SettingsRow.tsx).
- `npx tsc --noEmit` exits 0.
- `npx vitest run` exits with no new failures.

Commit as: `chore(sidebar): delete retired SettingsRow.tsx (Phase 11 PURGE-03 — Ashley's "no settings" lock)`

**Deletion order rationale (per threat model file-deletion boundary):** SettingsRow deletes BEFORE AppRail (Task 5) because SettingsRow.tsx line 42 has `import type { RailView } from "@/sidebar/AppRail"`. Deleting AppRail first would leave SettingsRow.tsx with a broken import for one commit — tsc-broken in isolation. This ordering keeps every intermediate commit tsc-clean.
  </action>
  <verify>
    <automated>cd /home/ubuntu/termix && test ! -f src/ui/sidebar/SettingsRow.tsx && grep -rn "from \"@/sidebar/SettingsRow\"" src/ | wc -l | tr -d "\n" | grep -q "^0$" && grep -rn "renderSettingsMenuItems" src/ | grep -v "\\.md$" | wc -l | tr -d "\n" | grep -q "^0$" && npx tsc --noEmit 2>&1 | grep -qvE "error TS"  </verify>
  <acceptance_criteria>
    - `test ! -f src/ui/sidebar/SettingsRow.tsx` exits 0 (file gone)
    - `grep -rn "from \"@/sidebar/SettingsRow\"" src/` returns 0 hits
    - `grep -rn "from \"./SettingsRow\"" src/` returns 0 hits
    - `grep -rn "renderSettingsMenuItems" src/` (excluding `.md` files) returns 0 hits
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` shows no new failures beyond documented pre-existing 4 ComposeBox failures
    - `git log -1 --stat --format="%s" | grep -qE "SettingsRow|PURGE-03"` shows the delete commit
  </acceptance_criteria>
  <done>SettingsRow.tsx deleted from disk; zero imports remain; tsc clean; tests green; commit landed.</done>
</task>

<task type="auto">
  <name>Task 5 (was 3): Delete src/ui/sidebar/AppRail.tsx</name>
  <files>src/ui/sidebar/AppRail.tsx</files>
  <read_first>
    - /home/ubuntu/termix/.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-01-STRIP-LIST.md (Section B)
    - /home/ubuntu/termix/src/ui/sidebar/AppRail.tsx (the file to delete — read to confirm no unexpected named exports beyond `AppRail` and `type RailView`)
  </read_first>
  <action>
Prerequisite check (before deleting):

Run `grep -rn "AppRail\|RailView" src/ | grep -v "^src/ui/sidebar/AppRail.tsx" | grep -v "\.md$" | grep -v "pinAppRail" | grep -v "^\s*//" | grep -v "^\s*\*"` — expected output: ZERO hits (Task 2's AppShell surgery removed the AppShell.tsx imports, Task 4's SettingsRow deletion removed the SettingsRow.tsx type-only import). If any code-line hit appears in a `.tsx` / `.ts` file, STOP and route back to Task 2 or Task 4 — an import survived.

Note: `pinAppRail` in `src/ui/locales/translated/*.json` is a dead translation string from an earlier upstream Termix "Pin App Rail" setting — those hits are documented in the strip-list as already-dead / out-of-scope for Phase 11. Exclude them from the gate.

Delete the file:
```
git rm src/ui/sidebar/AppRail.tsx
```

Post-delete verification (per checker W-3: `npm run build` is NOT run in this per-commit gate — it lives in Plan 04 Task 1 as the final Plan-boundary gate; keeping only tsc + grep here yields faster feedback for the per-commit deletion cycle):
- `test ! -f src/ui/sidebar/AppRail.tsx` — file MUST NOT exist.
- `grep -rn "from \"@/sidebar/AppRail\"" src/ | wc -l` returns 0.
- `grep -rn "from \"./AppRail\"" src/ | wc -l` returns 0.
- `grep -rn "^import.*AppRail\b" src/ | wc -l` returns 0.
- Repo-wide grep hygiene sweep: `grep -rn "AppRail\|RailView" src/ | grep -v "pinAppRail" | grep -v "\.md$"` — expected: ONLY comment-only historical annotations remain (`//` or `/*` lines in AppShell.tsx that describe the retirement, per Phase 10 Wave 4 policy — these ARE acceptable). Verify each remaining hit is inside a comment line.
- `npx tsc --noEmit` exits 0.
- Full `npx vitest run` exits with no new failures.

Note: `npm run build` (Vite production build) was previously in this per-commit gate but has been moved to Plan 04 Task 1 (the phase-boundary build-verify log). Rationale: per-commit build runs are slow (~30-60s) and provide no signal that `tsc --noEmit` doesn't already give for deletion commits — tsc-clean plus vitest-green is the correct per-commit gate. Full production-build verification happens once at the phase boundary in Plan 04. No coverage lost.

Commit as: `chore(sidebar): delete retired AppRail.tsx (Phase 11 PURGE-02)`

Post-commit repo-wide gate (paste output into SUMMARY):
```
grep -rn "AppRail" src/ | grep -v "pinAppRail" | grep -v "\.md$"
```
Expected: only comment-only lines. Any non-comment line is a bug — route back and fix in a follow-up commit BEFORE closing the plan.
  </action>
  <verify>
    <automated>test ! -f src/ui/sidebar/AppRail.tsx && grep -rn "from \"@/sidebar/AppRail\"" src/ | wc -l | tr -d "\n" | grep -q "^0$" && grep -rn "AppRail" src/ | grep -v "pinAppRail" | grep -v "\\.md$" | grep -v "^\\s*//" | grep -v "^\\s*\\*" | grep -v "^\\s*\\*\\*" | wc -l | tr -d "\n" | grep -q "^0$" && npx tsc --noEmit 2>&1 | grep -qvE "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/ui/sidebar/AppRail.tsx` exits 0 (file gone)
    - `grep -rn "from \"@/sidebar/AppRail\"" src/` returns 0 hits
    - `grep -rn "from \"./AppRail\"" src/` returns 0 hits
    - Repo-wide `grep -rn "AppRail" src/ | grep -v "pinAppRail" | grep -v "\.md$"` returns ONLY comment-only historical annotations (no non-comment code hits)
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` shows no new failures beyond documented pre-existing 4 ComposeBox failures
    - `git log -1 --stat --format="%s" | grep -qE "AppRail|PURGE-02"` shows the delete commit
    - Note: `npm run build` verification is deferred to Plan 04 Task 1 (phase-boundary gate) per checker W-3 fix — not required at this per-commit boundary.
  </acceptance_criteria>
  <done>AppRail.tsx deleted from disk; zero code-line references remain in src/; tsc clean; tests green; commit landed. Phase-boundary `npm run build` verification happens in Plan 04 Task 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Per-commit tsc-clean boundary (revised per checker B-3) | Each of the 5 tasks in this plan lands ONE commit with ONE `<verify>` gate. Task 1 (Test 11 prune) is scope-independent of the prop's presence; Task 2 (AppShell surgery) is heavy but leaves the tree tsc-clean because PrettyConversationsPanel still accepts settingsRowSlot as optional; Task 3 (panel prop drop) is scope-independent of any caller since Task 2 already stopped passing the prop; Task 4 (SettingsRow delete) precedes Task 5 (AppRail delete) because SettingsRow imports `type RailView` from AppRail. Every intermediate commit is tsc-clean and vitest-green. This is the Phase 10 Wave 4 precedent applied to Phase 11's largest surgery. |
| AppShell state-machine surgery | The rail-view state machine (railView, handleRailClick, sidebarTitle, editHostInManager, openSingletonTab dead branches, profileDropdownOpen) has been carrying dead-surface navigation state for years. Stripping it in one commit (Task 2) is a large-blast-radius change; a subtle bug (missed reference, dropped-but-consumed variable) manifests as tsc-fail or vitest-fail at commit time. Task 2's inline verification gate catches this. The `profileDropdownOpen` safety-gate grep (per Plan 01 Section E item 2) is mandatory before deletion. The `openSingletonTab` disposition protocol (per Plan 01 Section E item 6) is mandatory to determine keep-or-strip. |
| PrettyConversationsPanel API cleanup | The settingsRowSlot prop is optional and unused after Task 2 ships. Dropping it in Task 3 is safe because TypeScript optional-prop rules allow the panel to build without callers passing it. Test 11 pruned in Task 1 because it was the only test asserting on the prop. |
| File deletion order matters for tsc-clean-per-commit | Delete SettingsRow.tsx BEFORE AppRail.tsx because SettingsRow.tsx's `import type { RailView } from "@/sidebar/AppRail"` (line 42) means AppRail must survive until SettingsRow.tsx is gone. Deleting AppRail first would leave SettingsRow.tsx with a broken import for one commit — tsc-broken in isolation. Phase 10 Wave 4's precedent for atomic-tsc-clean-per-commit demands this ordering. |
| Comment-only historical annotations preserved | Phase 10 Wave 4 kept comment-only mentions of retired components as documentation. This plan follows the same policy — `//` and `/* */` mentions of AppRail / SettingsRow / RailView inside surviving files are ACCEPTABLE and count as 0 for the code-hit gate. Grep patterns use `grep -v "^\s*//" | grep -v "^\s*\*"` to filter comments. |
| Per-commit build gate scope (checker W-3) | `npm run build` (Vite production build) runs ONCE at the phase boundary in Plan 04 Task 1, NOT in every per-commit gate. Rationale: for deletion commits, `tsc --noEmit` catches every type-level regression that `npm run build` would; the additional 30-60s per commit for a Vite bundle build provides no unique signal at the per-commit level. Full production-build verification remains a plan-boundary non-negotiable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Denial of Service (regression — sidebar renders nothing on load) | AppShell sidebarPanelContent | mitigate | Task 2's rewritten sidebarPanelContent keeps the PrettyConversationsPanel mount. Verification includes `grep -c "PrettyConversationsPanel" src/ui/AppShell.tsx` returns >= 2. AppShell.persistence.test.tsx run gate catches sidebar-mount regressions. |
| T-11-03-02 | Denial of Service (RDP host row broken) | onRdpRowClick handler | mitigate | Task 2's rewritten sidebarPanelContent preserves the `onRdpRowClick` handler VERBATIM from the current lines 1530-1537. Grep `onRdpRowClick` post-edit to confirm it survives. PURGE-05 explicitly demands RDP rows continue to work. |
| T-11-03-03 | Tampering (accidental deletion of tab-lifecycle machinery) | openTab / doCloseTab / effectiveSelectedTabId | mitigate | Task 2 explicitly names the "preserve untouched" list. Grep verification confirms `openTab` and `doCloseTab` still exist. The tab-node portal loop at lines 2005-2048 stays untouched. AppShell.persistence.test.tsx catches any regression here. |
| T-11-03-04 | Elevation of Privilege (admin route reachable post-purge) | Any surviving admin UI path | mitigate | After Task 2, the AppRail mount + SettingsRow mount + openSingletonTab admin branches are all removed. Grep post-Task-2: `grep -rn "AdminSettingsPanel\|admin-settings" src/` — any hit outside of type-union definitions (TabType) or backend files is a bug. Additionally, PURGE-04 preserves backend routes — the `/admin/*` endpoints on the backend still respond, but no visible UI reaches them. |
| T-11-03-05 | Denial of Service (build fails after deletion) | Vite production build | mitigate | Task 5 (AppRail deletion) ends with tsc + vitest gates. Full `npm run build` verification lives at the Plan 04 Task 1 phase boundary (per checker W-3 fix). If the phase-boundary build fails, Plan 04 routes back to identify the missing dependency and the offending Plan 03 commit is reverted or fixed in a follow-up commit before Plan 04 closes. |
| T-11-03-06 | Tampering (deleting live consumer state) | AppShell profileDropdownOpen useState | mitigate | Task 2's `profileDropdownOpen` safety-gate grep (per Plan 01 Section E item 2) MUST run AFTER the AppRail mount removal but BEFORE line 234 deletion. If ANY hit surfaces outside AppRail.tsx (which is scheduled for deletion in Task 5) and outside line 234 itself, the executor keeps line 234 with a load-bearing comment and routes back to Plan 01. |
| T-11-03-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this plan. No supply-chain surface. |
</threat_model>

<verification>
- After Task 1: Test 11 pruned. Tree tsc-clean. PrettyConversationsPanel.test.tsx shows 14 tests passing.
- After Task 2: AppShell surgery landed. Tree tsc-clean because PrettyConversationsPanel's optional settingsRowSlot prop is now unpassed but still declared. `profileDropdownOpen` deleted per safety-gate grep evidence in commit message. `openSingletonTab` disposition documented in commit message.
- After Task 3: PrettyConversationsPanel prop drop landed. Tree tsc-clean.
- After Task 4: SettingsRow.tsx deleted. `grep -rn "SettingsRow\|renderSettingsMenuItems" src/` returns 0. tsc-clean.
- After Task 5: AppRail.tsx deleted. Repo-wide `grep -rn "AppRail" src/ | grep -v "pinAppRail" | grep -v ".md$"` returns only comment-only lines. tsc-clean. `npx vitest run` no new failures.
- `npm run build` runs ONCE at the Plan 04 Task 1 phase-boundary gate (per checker W-3 fix), not per-commit.
- Total commit count: 5 (Test 11 prune + AppShell surgery + panel prop drop + SettingsRow delete + AppRail delete). Each has its own `<verify><automated>` gate (per checker B-3 fix).
</verification>

<success_criteria>
- The AppRail component file is deleted (PURGE-02).
- The SettingsRow component file is deleted (Ashley "no settings" lock).
- Zero imports of `@/sidebar/AppRail` or `@/sidebar/SettingsRow` in `src/` (PURGE-02).
- The rail-view state machine is stripped from AppShell.tsx (railView, sidebarTitle, handleRailClick, editHostInManager, profileDropdownOpen — the AppRail-only state per Plan 01 Section E item 2) (PURGE-03).
- No visible UI path from the pretty-conversations sidebar to any dead surface remains (PURGE-03).
- Backend files unchanged (PURGE-04).
- RDP-host-sentinel rows still open Guacamole via `onRdpRowClick` (PURGE-05).
- Every commit boundary tsc-clean; targeted tests green; single-file commits per Phase 10 Wave 4 precedent (5 commits total, each with its own `<verify>` gate).
- `npm run build` verification deferred to Plan 04 Task 1 phase-boundary gate (per checker W-3).
</success_criteria>

<output>
Create `.planning/phases/11-skynet-transformation-purge-dead-termix-surfaces-first-slice/11-03-SUMMARY.md` when done.
</output>
