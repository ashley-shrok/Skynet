---
phase: 30-pane-state-backend-authoritative-no-client-inference
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/claude-session/pane-state-emitter.ts
  - src/backend/claude-session/pane-state-emitter.test.ts
  - src/backend/claude-session/claude-session-server.ts
autonomous: true
requirements: [PS30-01, PS30-03, PS30-07]
tags: [backend, websocket, state-machine, pane-state]

must_haves:
  truths:
    - "Backend emits a single authoritative pane_state frame carrying { type: 'pane_state', state: 'active' | 'holding' | 'dormant' | 'inactive' | 'error', reason?: string }"
    - "Backend emits the current pane_state on every fresh WS attach (after connectToPane discovery completes) so a fresh client immediately learns the current truth without waiting for a state change"
    - "Every existing dormant / session_holding / session_holding_cleared / session_changed / inactive emit site funnels through the pane_state emitter — legacy frames remain on the wire (backward-compat) AND a matching pane_state frame is emitted alongside"
    - "The emitter is idempotent per pane: emitting the same (state, reason) twice in a row emits the wire frame ONCE and dedupes subsequent identical writes (matches existing dormantLastEmitted convention)"
    - "Unit tests exercise every state transition (active→holding via id_reset reason, active→holding via discovery_diff reason, holding→active via session_changed, active→dormant/undormant, active→inactive with holding_timeout reason, error state) and assert the correct wire frame is emitted with the correct reason"
  artifacts:
    - path: "src/backend/claude-session/pane-state-emitter.ts"
      provides: "PaneState wire-frame type + createPaneStateEmitter factory (per-connection) + emit(state, reason?) API + dedupe logic + emitCurrent() for attach-time send"
      exports: ["PaneState", "PaneStateWireFrame", "createPaneStateEmitter", "type PaneStateEmitter"]
    - path: "src/backend/claude-session/pane-state-emitter.test.ts"
      provides: "Unit tests for the emitter: initial-emit-on-attach, transition matrix, dedupe, error path"
      contains: "createPaneStateEmitter"
    - path: "src/backend/claude-session/claude-session-server.ts"
      provides: "Per-connection emitter instantiated in the connection scope; every dormant/session_holding/session_holding_cleared/session_changed/inactive emit site funnels through emitter.emit(...) alongside the existing wire frame; emitter.emitCurrent() called after connectToPane discovery completes"
      contains: "createPaneStateEmitter"
  key_links:
    - from: "src/backend/claude-session/claude-session-server.ts transitionToHolding"
      to: "paneStateEmitter.emit('holding', reason)"
      via: "direct call alongside ws.send({type:'session_holding'})"
      pattern: "paneStateEmitter\\.emit\\(\"holding\""
    - from: "src/backend/claude-session/claude-session-server.ts transitionToActiveNew"
      to: "paneStateEmitter.emit('active')"
      via: "direct call alongside ws.send({type:'session_changed'})"
      pattern: "paneStateEmitter\\.emit\\(\"active\""
    - from: "src/backend/claude-session/claude-session-server.ts transitionToDead"
      to: "paneStateEmitter.emit('inactive', 'holding_timeout')"
      via: "direct call alongside ws.send({type:'inactive', reason:'holding_timeout'})"
      pattern: "paneStateEmitter\\.emit\\(\"inactive\""
    - from: "src/backend/claude-session/claude-session-server.ts connectToPane discovery success"
      to: "paneStateEmitter.emitCurrent()"
      via: "attach-time initial emit so a fresh client learns current truth without waiting for a state change"
      pattern: "paneStateEmitter\\.emitCurrent\\(\\)"
---

<objective>
Establish the wire-protocol foundation for Phase 30 (per D-signal-set + D-migration in CONTEXT.md): introduce a single authoritative `pane_state` frame type and a per-connection emitter that consolidates today's five racing WS emit paths (dormant, session_holding, session_holding_cleared, session_changed, inactive) into one funnel. Existing frame types stay on the wire (backward-compat this phase — deprecation is deferred per CONTEXT.md § Deferred), but a matching `pane_state` frame is emitted alongside every existing emit site. The emitter also exposes `emitCurrent()` which is called once after `connectToPane` discovery completes so a fresh client immediately receives the current truth (per PS30-07).

Purpose: Frontend Plan 30-03 depends on the `PaneState` type shape (imported for the WS handler + resolve-phase reducer). Plan 30-02 depends on the emitter's `emit("holding", "id_reset")` API to trigger holding on parser observation. Both consumers block on this plan.

Output: `src/backend/claude-session/pane-state-emitter.ts` (~120 LOC — small factory + type + dedupe), `src/backend/claude-session/pane-state-emitter.test.ts` (~200 LOC — transition matrix), and edits to `src/backend/claude-session/claude-session-server.ts` funneling every existing transition through the new emitter + wiring `emitCurrent()` at the attach-time discovery success site.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/30-pane-state-backend-authoritative-no-client-inference/30-CONTEXT.md
@.planning/phases/29-unified-session-entry-state-machine-single-resolving-spinner/29-CONTEXT.md
@src/backend/claude-session/claude-session-server.ts
@src/backend/claude-session/layer1-detect.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create pane-state-emitter module (type + factory + dedupe)</name>
  <files>src/backend/claude-session/pane-state-emitter.ts, src/backend/claude-session/pane-state-emitter.test.ts</files>
  <read_first>
    - src/backend/claude-session/layer1-detect.ts (whole file — copy the "pure module, no I/O imports, integration seam co-located" pattern verbatim; the emitter is per-connection stateful but the SHAPE mirrors layer1-detect: type union + factory function + injected wsSend for testability)
    - src/backend/claude-session/claude-session-server.ts lines 95-100 (existing frame type doc — insert PaneState next to dormant/session_holding/session_changed/inactive), lines 1015-1025 (dormantLastEmitted dedupe pattern to mirror), lines 2145-2170 (transitionToHolding shape), lines 2188-2231 (transitionFromHoldingToActiveSameFile shape), lines 2240-2328 (transitionToActiveNew shape), lines 2336-2359 (transitionToDead shape)
    - .planning/phases/30-pane-state-backend-authoritative-no-client-inference/30-CONTEXT.md § Signal set (LOCKED) + § Backend observations feeding pane_state + § Migration strategy
  </read_first>
  <behavior>
    - Test 1: `createPaneStateEmitter({ wsSend })` returns an object with `emit(state, reason?)`, `emitCurrent()`, and `getCurrent()` methods; getCurrent() returns `null` before any emit
    - Test 2: `emit("active")` sends `JSON.stringify({ type: "pane_state", state: "active" })` (no reason field when omitted) via injected wsSend; getCurrent() returns `{ state: "active", reason: undefined }`
    - Test 3: `emit("holding", "id_reset")` sends `JSON.stringify({ type: "pane_state", state: "holding", reason: "id_reset" })` and getCurrent() reflects it
    - Test 4: `emit("active")` then `emit("active")` (same state, no reason) → wsSend called EXACTLY ONCE (dedupe on identical writes — mirrors dormantLastEmitted pattern at claude-session-server.ts:1015-1020)
    - Test 5: `emit("holding", "id_reset")` then `emit("holding", "discovery_diff")` → wsSend called TWICE (reason differs = not a dedupe)
    - Test 6: `emit("holding", "id_reset")` then `emit("active")` then `emit("holding", "id_reset")` → wsSend called THREE times (dedupe is against the LAST emit, not any prior emit)
    - Test 7: `emitCurrent()` before any emit → wsSend NOT called (nothing to re-emit); getCurrent() still returns null
    - Test 8: `emit("active")` then `emitCurrent()` → wsSend called TWICE with identical payload (emitCurrent bypasses dedupe — attach-time forced re-emit for a fresh client that missed the original)
    - Test 9: Full transition matrix — for each valid state in {active, holding, dormant, inactive, error} and each documented reason set from CONTEXT.md § Backend observations (holding: id_reset | discovery_diff | pid_death | exit_scan; inactive: holding_timeout | no_session | session_marked_inactive; error: file_unreadable | tracking_error), emit produces the expected wire frame
    - Test 10: `emit("nonexistent" as any)` — TypeScript exhaustiveness prevents this at compile time (use `_exhaust: never` sentinel like resolve-phase.ts:166 does); test file uses a `// @ts-expect-error` comment to prove the compile-time gate exists
  </behavior>
  <action>
    Create `src/backend/claude-session/pane-state-emitter.ts` with:

    1. Type union `PaneState` = `"active" | "holding" | "dormant" | "inactive" | "error"` (matches D-signal-set values LOCKED in CONTEXT.md).

    2. Wire frame type `PaneStateWireFrame` = `{ type: "pane_state"; state: PaneState; reason?: string }` (matches D-signal-set per CONTEXT.md § Signal set — the `reason` field is optional-string diagnostics; do NOT constrain to a literal union at the wire level, since backend observations may add new reasons over time without a wire-schema bump per D-migration).

    3. Factory function `createPaneStateEmitter(deps: { wsSend: (data: string) => void }): PaneStateEmitter` that returns `{ emit, emitCurrent, getCurrent }`.

       - Internal per-instance state: `let current: { state: PaneState; reason: string | undefined } | null = null`
       - `emit(state, reason?)`: build the frame object, compare against `current` via `state === current?.state && reason === current?.reason` (strict-equal on both fields, treating undefined as a value not a wildcard); if identical → return early (dedupe); else update `current` + call `wsSend(JSON.stringify(frame))`. When `reason` is undefined, OMIT the `reason` key from the wire frame (use conditional-key construction: `reason !== undefined ? { type, state, reason } : { type, state }`) — cleaner wire shape + matches the T-30-01 "no undefined leaks" mitigation below.
       - `emitCurrent()`: if `current === null` → return early; else call `wsSend(JSON.stringify(...))` with the current value (bypasses dedupe — this is the attach-time forced re-emit)
       - `getCurrent()`: return `current` (or null)

    4. Exports: `PaneState`, `PaneStateWireFrame`, `createPaneStateEmitter`, `type PaneStateEmitter` (the return type of the factory — named type alias, not inline).

    5. Add the `_exhaust: never` compile-time exhaustiveness sentinel inside `emit()` after the state validation (identical pattern to resolve-phase.ts:166 — narrows the union to empty after all branches consumed).

    Create `src/backend/claude-session/pane-state-emitter.test.ts` with vitest imports (`import { describe, expect, it, vi } from "vitest";`), and 10 test cases per the behavior spec above. Use `vi.fn()` to spy on wsSend and assert exact call counts + payload strings. NO ws / ssh2 / logger imports — the emitter is pure per-connection state + a callback, testable in isolation.

    Do NOT modify claude-session-server.ts in this task. Task 2 handles integration.
  </action>
  <verify>
    <automated>npx vitest run src/backend/claude-session/pane-state-emitter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - pane-state-emitter.ts exports `createPaneStateEmitter` (grep: `grep -c "^export function createPaneStateEmitter" src/backend/claude-session/pane-state-emitter.ts` → 1)
    - pane-state-emitter.ts exports `PaneState`, `PaneStateWireFrame`, `PaneStateEmitter` types (grep: `grep -cE "^export (type|interface) (PaneState|PaneStateWireFrame|PaneStateEmitter)" src/backend/claude-session/pane-state-emitter.ts` → 3)
    - pane-state-emitter.ts has zero I/O imports beyond node builtins (grep: `grep -cE "^import.*from \"(ssh2|ws|winston|pino|logger|\\.\\./)" src/backend/claude-session/pane-state-emitter.ts` → 0)
    - `npx vitest run src/backend/claude-session/pane-state-emitter.test.ts` exits 0 with all 10 test cases passing
    - `npx tsc --noEmit` exits 0 (proves _exhaust: never sentinel type-checks)
  </acceptance_criteria>
  <done>
    Emitter module + test file exist; all 10 unit tests pass; TypeScript exhaustiveness gate proven via _exhaust: never sentinel; no I/O imports; ready for integration in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire emitter into claude-session-server (funnel every transition + emit on attach)</name>
  <files>src/backend/claude-session/claude-session-server.ts</files>
  <read_first>
    - src/backend/claude-session/claude-session-server.ts lines 95-100 (frame type documentation block — add pane_state entry), lines 940-946 (transitionToDead call site for helpers.transitionToDead), lines 1015-1025 (dormant emit inside __applyDormantPollTickForTests seam — call site funnel), lines 1136-1177 (dormant emits inside __applyDormantPollWithRediscoveryForTests seam), lines 1418-1424 (per-connection state box declarations — add paneStateEmitter here), lines 1560-1603 (onLine dispatch), lines 2145-2168 (transitionToHolding — funnel), lines 2188-2231 (transitionFromHoldingToActiveSameFile — funnel), lines 2240-2328 (transitionToActiveNew — funnel), lines 2336-2359 (transitionToDead — funnel), lines 3710-3735 (connectToPane message handler entry), lines 4400-4650 (initial discovery/attach flow inside the connectToPane message handler — locate the post-discovery-success emit site for emitCurrent())
    - src/backend/claude-session/pane-state-emitter.ts (result of Task 1 — for the API surface)
    - .planning/phases/30-pane-state-backend-authoritative-no-client-inference/30-CONTEXT.md § Backend observations feeding pane_state (maps existing transitions to the correct (state, reason) pairs)
  </read_first>
  <action>
    Wire the emitter created in Task 1 into `claude-session-server.ts`. Every existing WS emit site for the five legacy frame types stays intact (backward-compat per D-migration in CONTEXT.md) — the funnel is ADDITIVE: `paneStateEmitter.emit(...)` is called alongside the existing `ws.send(JSON.stringify({...}))` call.

    Step 1 — Import the emitter at the top of the file (near the existing `import { tailSessionFile } from "./session-file-tail.js"` and `import { applyLineToLayer1State } from "./layer1-detect.js"`):
    `import { createPaneStateEmitter, type PaneStateEmitter } from "./pane-state-emitter.js";`

    Step 2 — Update the frame-type documentation block near line 95 with a new bullet BETWEEN the existing `dormant` and `wake_result` entries:
    `*     { type: "pane_state", state: "active"|"holding"|"dormant"|"inactive"|"error", reason?: string } // Phase 30 PS30-01: authoritative pane-entry state verdict; emitted on connectToPane attach + on every transition; existing dormant/session_holding/session_changed/inactive frames remain on wire for backward compat this phase (deprecation deferred)`

    Step 3 — Declare the per-connection emitter in the connection scope, right after the existing `let layer1: Layer1State = { mostRecentUserTurnIsIdReset: null };` declaration around line 1424. Use the direct ws.send closure that the existing transitions use:
    `const paneStateEmitter: PaneStateEmitter = createPaneStateEmitter({ wsSend: (data: string) => { if (!stopped && ws.readyState === WebSocket.OPEN) { try { ws.send(data); } catch { /* ws may be mid-close */ } } } });`

    Step 4 — At every existing WS emit site for the five legacy frame types, add a matching `paneStateEmitter.emit(...)` call. Map:

    | Existing emit | New pane_state call |
    |---|---|
    | `ws.send(JSON.stringify({ type: "session_holding" }))` inside `transitionToHolding(reason)` at ~L2154 | `paneStateEmitter.emit("holding", reason)` — reason forwards through, so `"id_reset"` or `"discovery_diff"` reach the wire |
    | `ws.send(JSON.stringify({ type: "session_holding_cleared" }))` inside `transitionFromHoldingToActiveSameFile()` at ~L2218 | `paneStateEmitter.emit("active", "same_file_recovery")` — the false-alarm-recovery reason surfaced for diagnostics |
    | `ws.send(JSON.stringify({ type: "session_changed", newSessionFile }))` inside `transitionToActiveNew(newSessionFile)` at ~L2292 | `paneStateEmitter.emit("active", "session_changed")` |
    | `ws.send(JSON.stringify({ type: "inactive", reason }))` inside `transitionToDead(reason)` at ~L2340 | `paneStateEmitter.emit("inactive", reason)` — reason forwards `"holding_timeout"` through |
    | `ws.send(JSON.stringify({ type: "dormant", dormant: true, wakingSince }))` inside dormant-poll seams at ~L1018, ~L1136, ~L4563 | `paneStateEmitter.emit("dormant")` — no reason field needed; wakingSince stays on the legacy dormant frame only |
    | `ws.send(JSON.stringify({ type: "dormant", dormant: false }))` inside dormant-poll seams at ~L1020, ~L1177 | `paneStateEmitter.emit("active", "dormancy_cleared")` |
    | `ws.send(JSON.stringify({ type: "inactive", reason: result.reason }))` at ~L4643 (initial-discovery-inactive path) | `paneStateEmitter.emit("inactive", result.reason)` |

    Do NOT change the shape of the existing ws.send calls — the legacy wire frames stay byte-identical (backward-compat is the whole point).

    IMPORTANT — dormant-poll seams: the `__applyDormantPollTickForTests` and `__applyDormantPollWithRediscoveryForTests` helpers at lines 979-1206 are per-tick reducers with injected wsSend deps. Do NOT add paneStateEmitter as a new injectable dep — that would break the test-seam contract. Instead, at the CALL SITES that wrap these seams (around L4122-4142 for the timer callback and any other wrapping site), after the seam returns, check whether `dormantLastEmitted` changed AND emit through paneStateEmitter using the closure-scoped emitter. Concretely: capture `dormantLastEmitted` before the await, compare after, and if the value changed, call `paneStateEmitter.emit("dormant")` (when the new value is true) or `paneStateEmitter.emit("active", "dormancy_cleared")` (when the new value is false). This keeps the pure-reducer seams unchanged.

    Step 5 — Attach-time initial emit (PS30-07). Find the post-discovery-success site inside the `connectToPane` message handler where `startActiveSessionFlow` begins execution — the initial `session` frame is emitted at ~L3810 (`ws.send(JSON.stringify({ type: "session", pid, sessionFile }))`). Immediately AFTER that emit, add:

    `paneStateEmitter.emit("active");`

    This sets the emitter's current state so the connection has a defined pane_state from the moment discovery reports "active". For the inactive-discovery branch at ~L4643, the existing legacy `ws.send({type:"inactive", reason})` already fires alongside the new `paneStateEmitter.emit("inactive", result.reason)` from Step 4 — that path already sets current-state at attach, no additional emitCurrent() needed.

    For panes where discovery reports a dormant sentinel present, the dormant-poll seams at ~L1136 fire `paneStateEmitter.emit("dormant")` on first-observation — that's also attach-time state establishment via the same funnel.

    NOTE: `emitCurrent()` (the bypass-dedupe re-emit) is NOT called anywhere in Phase 30 Plan 01 — every attach flow naturally produces at least one `emit(...)` through the normal transition sites. `emitCurrent()` is a Phase-30-follow-up hook for a future WS re-attach after transient drop (deferred per CONTEXT.md § Deferred "backward compat this phase"). The API exists and is unit-tested in Task 1 for completeness — do not add a caller here.

    Step 6 — Do NOT change `changeoverState`, `layer1`, `dormantLastEmitted`, or any of the existing per-connection state variables. The emitter's internal `current` is INDEPENDENT bookkeeping that mirrors the transitions rather than replacing them.

    Do NOT modify `session-file-parser.ts` — that's Plan 30-02's job.
    Do NOT modify `usePaneResolvingMachine.ts`, `resolve-phase.ts`, or `PrettyView.tsx` — that's Plan 30-03's job.
  </action>
  <verify>
    <automated>npx vitest run src/backend/claude-session/ && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "paneStateEmitter\\.emit\\(" src/backend/claude-session/claude-session-server.ts | grep -v '^#'` returns at least 7 (the 5 transition sites + dormant true/false = 7 minimum; more if the dormant-poll seam-wrapper adds 2)
    - `grep -c "createPaneStateEmitter" src/backend/claude-session/claude-session-server.ts` returns exactly 1 (the single per-connection instantiation)
    - `grep -c 'ws.send(JSON.stringify({ type: "session_holding" }))' src/backend/claude-session/claude-session-server.ts` returns 1 (the legacy emit is PRESERVED, not deleted)
    - `grep -c 'ws.send(JSON.stringify({ type: "session_changed"' src/backend/claude-session/claude-session-server.ts` returns 1 (legacy PRESERVED)
    - `grep -c 'ws.send(JSON.stringify({ type: "inactive"' src/backend/claude-session/claude-session-server.ts` returns at least 2 (legacy PRESERVED at both transitionToDead + initial-discovery-inactive)
    - Every existing test in src/backend/claude-session/ still passes: `npx vitest run src/backend/claude-session/` exits 0 (proves backward-compat of legacy frames)
    - `npx tsc --noEmit` exits 0 (proves the emitter import + type usage compiles)
  </acceptance_criteria>
  <done>
    Emitter instantiated once per connection; every existing WS emit for dormant/session_holding/session_holding_cleared/session_changed/inactive has a matching paneStateEmitter.emit(...) call alongside; connectToPane attach path emits pane_state via the natural transition sites; legacy wire frames unchanged; all existing tests green; tsc clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session file → parser → wire | Untrusted JSONL bytes from the target box's disk cross into the backend's WS emit path via parseSessionLine + Layer 1 detector. Trust boundary already existed pre-Phase-30; Phase 30 does NOT extend it. |
| backend → WS client | The new `pane_state` frame is emitted by the backend and consumed by the frontend. Existing WS boundary; the new frame type joins the existing frame-shape schema. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Information disclosure | paneStateEmitter.emit — `reason` string is forwarded to the wire from backend closure-scoped values (e.g. `holdingReason` local, `result.reason` from execCommand output) | mitigate | Emitter interface accepts `reason?: string` — but the CALLERS in claude-session-server.ts pass ONLY string-literal reasons from the fixed set enumerated in Step 4's mapping table (id_reset, discovery_diff, same_file_recovery, session_changed, holding_timeout, dormancy_cleared, plus the discovery result.reason which was already reaching the wire via the legacy inactive frame pre-Phase-30). No user input, no session-file bytes, no filesystem paths pass through the reason field. |
| T-30-02 | Denial of service | paneStateEmitter.emit called from ticker callbacks + onLine hot path | accept | Dedupe by (state, reason) equality means back-to-back identical transitions produce ONE wire frame. Worst case (rapid legit state churn) is bounded by the transition set: at most one emit per state change per connection per tick. Same throughput profile as existing dormant frame emission (which already runs on the same ticker at 3s cadence). |
| T-30-03 | Tampering | Legacy frame shapes preserved for backward-compat this phase | mitigate | Explicit acceptance criterion #3-5 grep-gates the legacy emit lines to prove they were NOT deleted during the funnel work. Existing tests in claude-session-server.*.test.ts continue passing = proof no legacy consumer regressed. |
| T-30-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan (emitter uses zero external deps; test uses vitest already in devDeps). Package legitimacy gate N/A. |
</threat_model>

<verification>
- `npx vitest run src/backend/claude-session/pane-state-emitter.test.ts` — all 10 emitter unit tests pass
- `npx vitest run src/backend/claude-session/` — full backend claude-session suite green (proves no legacy frame consumer regressed)
- `npx tsc --noEmit` — TypeScript exhaustiveness sentinel + emitter type surface all resolve
- grep-gates on Task 2 acceptance_criteria prove the legacy wire frames were preserved AND the funnel was added
</verification>

<success_criteria>
Plan 30-01 is complete when:
- `src/backend/claude-session/pane-state-emitter.ts` exports the wire frame type + factory + dedupe logic per the LOCKED signal set (D-signal-set in 30-CONTEXT.md)
- `src/backend/claude-session/pane-state-emitter.test.ts` has 10 passing unit tests covering the transition matrix + dedupe + emitCurrent bypass + compile-time exhaustiveness
- Every existing dormant/session_holding/session_holding_cleared/session_changed/inactive emit site in claude-session-server.ts funnels through paneStateEmitter.emit(...) alongside the existing ws.send(...) call
- Legacy wire frames remain byte-identical (backward-compat per PS30-02 + D-migration)
- Attach-time pane_state is established via the natural transition sites (per PS30-07: fresh client attaches → discovery success → transition emit fires alongside → client learns current truth on first frame)
- Full backend test suite green + tsc clean
- Foundation for Plans 30-02 (parser id_reset → emit holding) and 30-03 (frontend consumes new frame) is in place
</success_criteria>

<output>
Create `.planning/phases/30-pane-state-backend-authoritative-no-client-inference/30-01-SUMMARY.md` when done. Include:
- List of transition sites funneled (exact line numbers for downstream Plan 30-03's WS handler consumer)
- The full reason-code vocabulary used (id_reset, discovery_diff, same_file_recovery, session_changed, holding_timeout, dormancy_cleared, plus any discovery result.reason surfaced) — Plan 30-03 uses this for its wire-handler test cases
- Confirmation that legacy frames are preserved on the wire (backward-compat unbroken)
- Any surprises encountered wiring dormant-poll seams (the test-seam-vs-wrapper split noted in Step 4 is the tricky part)
</output>
