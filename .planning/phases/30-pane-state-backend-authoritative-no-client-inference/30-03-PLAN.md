---
phase: 30-pane-state-backend-authoritative-no-client-inference
plan: 03
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - src/ui/features/pretty-view/resolve-phase.ts
  - src/ui/features/pretty-view/resolve-phase.test.ts
  - src/ui/features/pretty-view/usePaneResolvingMachine.ts
  - src/ui/features/pretty-view/usePaneResolvingMachine.test.tsx
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-view/PrettyView.phase29.test.tsx
autonomous: true
requirements: [PS30-04, PS30-05, PS30-06, PS30-07, PS30-08]
tags: [frontend, state-machine, pretty-view, pane-state, cleanup]

must_haves:
  truths:
    - "usePaneResolvingMachine consumes EXACTLY TWO inputs: wsTransportState and paneState — no third axis, no entry-trigger effects, no rearmSnapshotRef, no isVisible input, no paneKey sentinel, no ~150ms delay-arm timer, no requestRetry callback, no D-11 client-inference"
    - "resolve-phase.ts encodes the LOCKED truth table from 30-CONTEXT.md § Truth table (LOCKED): failed-permanently → error overlay; not-connected/opening with no paneState received → resolving spinner; not-connected/opening with previous paneState → keep last known overlay (don't flicker); open with no paneState received → resolving spinner; open with paneState received → overlay for that state (holding/dormant/inactive) or normal message view (active)"
    - "All ~10 captureFirstFrame call sites in PrettyView.tsx are DELETED — the entire client-inference machinery is gone: no isHolding local state slot mirroring phase (only kept if ComposeBox still needs the isHolding prop; verify by grep), no dormant local state slot (only kept if DormancyOverlay reads directly), no backendFirstFrame concept, no captureFirstFrame useCallback"
    - "Patch #381's onResetClicked captureFirstFrame('session_holding') is DELETED — the reset button no longer needs a frontend client-hint because the backend session-file parser + Layer 1 tail-state reducer (Plans 30-01 + 30-02) will emit pane_state:holding within milliseconds of the /id reset landing in the JSONL"
    - "PrettyView.tsx has a new WS handler case 'pane_state' that stores parsed.state into a local paneState React state slot; every legacy frame handler (session_holding, session_holding_cleared, session_changed, dormant, inactive) STILL EXISTS but their captureFirstFrame calls are DELETED — legacy frames are retained only for non-phase-related side effects (setMessages([]) on session_changed heavy-reset, setDormant(true) if DormancyOverlay reads it directly, etc.)"
    - "Overlay mount gates are: SessionHoldingOverlay → paneState === 'holding'; DormancyOverlay → paneState === 'dormant'; PrettyViewErrorOverlay → wsTransportState === 'failed-permanently' OR paneState === 'error'; inactive fallback → paneState === 'inactive'; resolving spinner → wsTransportState !== 'open' AND paneState === null (no last-known to show) — the D-11 'transient drop don't flicker' rule renders the last-known overlay when transport drops after paneState was received"
  artifacts:
    - path: "src/ui/features/pretty-view/resolve-phase.ts"
      provides: "Simplified pure reducer with the LOCKED Phase-30 truth table; type unions updated: WsTransportState (not-connected|opening|open|failed-permanently) unchanged from Phase 29; PaneState (active|holding|dormant|inactive|error|null) — null represents 'not yet received'; RenderedState (resolving|active|holding|dormant|inactive|error) — the six overlay-mount outcomes"
      exports: ["resolveRenderedState", "type WsTransportState", "type PaneState", "type RenderedState"]
    - path: "src/ui/features/pretty-view/usePaneResolvingMachine.ts"
      provides: "Trivial hook — reduced from ~380 LOC to ~30 LOC; wraps resolveRenderedState with the pane-key reset for cold mount"
      min_lines: 20
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "WS handler case 'pane_state' stores parsed.state; overlay mount gates rewritten to paneState === '...' form; captureFirstFrame call sites deleted; onResetClicked simplified (no captureFirstFrame); patch #381 anti-pattern eliminated"
      contains: "paneState"
    - path: "src/ui/features/pretty-view/resolve-phase.test.ts"
      provides: "Rewritten truth-table tests for the new (wsTransportState, paneState) → RenderedState mapping; every combination in the LOCKED truth table exercised"
    - path: "src/ui/features/pretty-view/usePaneResolvingMachine.test.tsx"
      provides: "Rewritten tests for the trivial hook — only cold-mount reset behavior; ALL entry-trigger tests DELETED (that machinery no longer exists)"
    - path: "src/ui/features/pretty-view/PrettyView.phase29.test.tsx"
      provides: "Rewritten integration tests — replaced with phase-30-equivalent tests: pane_state:holding arrives → SessionHoldingOverlay mounts; pane_state:active arrives → overlay unmounts; reset button click → NO captureFirstFrame; failed-permanently transport → PrettyViewErrorOverlay mounts"
  key_links:
    - from: "src/ui/features/pretty-view/PrettyView.tsx WS onmessage handler"
      to: "case \"pane_state\" → setPaneState(parsed.state)"
      via: "storing backend-authoritative verdict into local state slot"
      pattern: 'case "pane_state"'
    - from: "src/ui/features/pretty-view/PrettyView.tsx overlay mount JSX"
      to: "paneState === 'holding' | 'dormant' | 'error' | 'inactive' guards"
      via: "backend-authoritative mount decisions replacing client inference"
      pattern: 'paneState === "holding"|paneState === "dormant"|paneState === "inactive"|paneState === "error"'
    - from: "src/ui/features/pretty-view/PrettyView.tsx onResetClicked"
      to: "no captureFirstFrame call — trusts backend to emit pane_state:holding within ms of /id reset landing"
      via: "patch #381 anti-pattern elimination"
      pattern: "onResetClicked"
---

<objective>
Consummate Phase 30's core promise on the frontend (per PS30-04 + PS30-05 + PS30-06): reduce `usePaneResolvingMachine` to a trivial `(wsTransportState, paneState) → RenderedState` derivation with all client-side inference machinery DELETED. This is the single largest source-code diff of the phase — targeting the ~10 captureFirstFrame call sites in PrettyView.tsx, patch #381's onResetClicked client-hint hack, the D-11 "message frame swaps to active" rule, the rearmSnapshotRef pattern, and every entry-trigger effect (cold-mount, warm re-focus, PWA foreground).

Per CONTEXT.md § Migration strategy → Frontend: strip in one pass. The hook drops to ~30 LOC (was ~380). Overlay mount gates flip from client-derived to backend-authoritative (`paneState === "holding"` etc.). Existing legacy WS handler cases (session_holding, session_holding_cleared, session_changed, dormant, inactive) remain for their non-phase side effects (setMessages([]) on session_changed heavy-reset, etc.) but their captureFirstFrame invocations delete.

Per CONTEXT.md § Truth table (LOCKED) — the new reducer:

| wsTransportState | paneState received? | → RenderedState |
|---|---|---|
| `failed-permanently` | any | `error` |
| `not-connected` / `opening` | never received (null) | `resolving` |
| `not-connected` / `opening` | previously received | last-known paneState's overlay |
| `open` | not yet received (null) | `resolving` |
| `open` | received | overlay for that state OR normal message view (active) |

Purpose: PS30-04 requires the client-side machinery removal; PS30-05 requires patch #381 deletion; PS30-06 requires the truth table implementation; PS30-08 requires full-suite green with no captureFirstFrame remnants.

Output: Rewritten `resolve-phase.ts` (~80 LOC → ~90 LOC with the new fifth row for "not-connected with previous paneState"), rewritten `usePaneResolvingMachine.ts` (~380 LOC → ~30 LOC), major edits to `PrettyView.tsx` (delete ~10 captureFirstFrame sites, patch #381 onResetClicked cleanup, mount gate flips, new WS handler case), rewritten test files for the three source modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-pane-state-backend-authoritative-no-client-inference/30-CONTEXT.md
@.planning/phases/30-pane-state-backend-authoritative-no-client-inference/30-01-SUMMARY.md
@src/backend/claude-session/pane-state-emitter.ts
@src/ui/features/pretty-view/resolve-phase.ts
@src/ui/features/pretty-view/usePaneResolvingMachine.ts
@src/ui/features/pretty-view/PrettyView.tsx
@src/ui/features/pretty-view/SessionHoldingOverlay.tsx
@src/ui/features/pretty-view/DormancyOverlay.tsx
@src/ui/features/pretty-view/PrettyViewErrorOverlay.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite resolve-phase.ts with the LOCKED Phase-30 truth table + rewrite tests</name>
  <files>src/ui/features/pretty-view/resolve-phase.ts, src/ui/features/pretty-view/resolve-phase.test.ts</files>
  <read_first>
    - src/ui/features/pretty-view/resolve-phase.ts (whole file — replacing the pure reducer)
    - src/ui/features/pretty-view/resolve-phase.test.ts (whole file — replacing the test suite with Phase-30 truth-table coverage)
    - .planning/phases/30-pane-state-backend-authoritative-no-client-inference/30-CONTEXT.md § Truth table (LOCKED) — the target mapping
    - .planning/phases/30-pane-state-backend-authoritative-no-client-inference/30-01-SUMMARY.md (for the PaneState value list from the backend emitter — must match)
    - src/backend/claude-session/pane-state-emitter.ts (for the shared PaneState wire values — the frontend type must be a superset that adds `null` for "not yet received")
  </read_first>
  <behavior>
    - Test 1 (failed-permanently short-circuit): resolveRenderedState("failed-permanently", null) → "error"
    - Test 2 (failed-permanently overrides paneState): resolveRenderedState("failed-permanently", "active") → "error"
    - Test 3 (opening, no paneState): resolveRenderedState("opening", null) → "resolving"
    - Test 4 (not-connected, no paneState): resolveRenderedState("not-connected", null) → "resolving"
    - Test 5 (opening, previously received paneState="active"): resolveRenderedState("opening", "active") → "active" (D-11 don't-flicker)
    - Test 6 (not-connected, previously received paneState="holding"): resolveRenderedState("not-connected", "holding") → "holding" (D-11 don't-flicker)
    - Test 7 (opening, previously received paneState="dormant"): resolveRenderedState("opening", "dormant") → "dormant" (D-11 don't-flicker)
    - Test 8 (open, no paneState yet): resolveRenderedState("open", null) → "resolving"
    - Test 9 (open, paneState="active"): resolveRenderedState("open", "active") → "active"
    - Test 10 (open, paneState="holding"): resolveRenderedState("open", "holding") → "holding"
    - Test 11 (open, paneState="dormant"): resolveRenderedState("open", "dormant") → "dormant"
    - Test 12 (open, paneState="inactive"): resolveRenderedState("open", "inactive") → "inactive"
    - Test 13 (open, paneState="error"): resolveRenderedState("open", "error") → "error"
    - Test 14 (full 4×6 matrix): every (wsTransportState, paneState) combination exercised in one truth-table describe block; asserts exact RenderedState for each
    - Test 15 (type surface): compile-time exhaustiveness via _exhaust: never sentinel (no runtime test — proves via tsc)
  </behavior>
  <action>
    Rewrite `src/ui/features/pretty-view/resolve-phase.ts` from scratch (whole-file replace):

    1. Retire the current type unions Phase-29 named:
       - `type WsState` → RENAME to `type WsTransportState` (values unchanged: "not-connected" | "opening" | "open" | "failed-permanently") — matches CONTEXT.md § Signal set wire naming
       - `type BackendFirstFrame` → DELETE entirely (this axis is gone in Phase 30)
       - `type Phase` → RENAME to `type RenderedState`; values are the six overlay-mount outcomes: `"resolving" | "active" | "holding" | "dormant" | "inactive" | "error"` (unchanged from Phase 29's Phase, only the name changes for terminology cleanliness — "phase" implied a state machine, "RenderedState" is what it actually is)
       - NEW: `type PaneState = "active" | "holding" | "dormant" | "inactive" | "error"` — MUST match the backend emitter's PaneState wire values exactly (import? no — do NOT import from backend; type is duplicated by convention because the wire is the contract, and cross-boundary TypeScript imports create build-tool coupling; add a comment referencing pane-state-emitter.ts for the invariant)

    2. Rewrite the pure function:

       ```
       export function resolveRenderedState(
         wsTransportState: WsTransportState,
         paneState: PaneState | null,
       ): RenderedState
       ```

       Branch order (LOCKED — matches CONTEXT.md § Truth table row-by-row):

       (a) `if (wsTransportState === "failed-permanently") return "error"` — the ONLY path to error phase from transport; if paneState is also "error" the return is still "error" (T-2 collapses to same value)

       (b) `if (wsTransportState === "open" && paneState !== null) return paneState` — the happy path: backend has spoken, transport is up, render the verdict directly

       (c) `if (wsTransportState === "open" && paneState === null) return "resolving"` — waiting for first backend frame; render spinner

       (d) `if (paneState !== null) return paneState` — transport transient drop (not-connected or opening) but we have a previous verdict → keep rendering it (D-11 "don't flicker" rule); if `paneState === "error"` here we render error too (unified with transport-error)

       (e) `return "resolving"` — final catch: transport not open + no paneState received yet

    3. Compile-time exhaustiveness sentinel — the branch structure above collapses to a single terminal expression, so the `_exhaust: never` narrowing is less mechanical than Phase 29's per-value branches. Instead, add an exhaustiveness assertion INSIDE branch (b) that narrows the PaneState:

       ```
       if (wsTransportState === "open" && paneState !== null) {
         switch (paneState) {
           case "active":
           case "holding":
           case "dormant":
           case "inactive":
           case "error":
             return paneState;
           // F1 acknowledgment (plan-checker 2026-08-10): the default branch below
           // exists SOLELY as a compile-time exhaustiveness gate. Runtime never
           // reaches it — the five case branches above cover every value of the
           // PaneState union (see resolve-phase.ts type declaration). Its purpose
           // is that if a future backend change adds a new PaneState value (e.g.
           // "waking") without updating the frontend union to match, TypeScript's
           // narrowing rule flags `_exhaust: never` as a type error at
           // `npx tsc --noEmit` time — the build fails loudly rather than the
           // frontend silently rendering a stale/wrong overlay. This is the
           // pattern's whole value; the runtime pass-through of `_exhaust` is a
           // dead-code formality required to satisfy TypeScript's return-value
           // completeness check.
           default: {
             const _exhaust: never = paneState;
             return _exhaust;
           }
         }
       }
       ```

       This proves at build-time that if the backend adds a new PaneState value without updating the frontend union, `npx tsc --noEmit` fails.

    4. Add a large JSDoc block above the function referencing PS30-04 + PS30-05 + PS30-06 + CONTEXT.md § Truth table (LOCKED) verbatim. Explain that this is a pure reducer with NO I/O imports (grep-gated by structural test).

    Rewrite `src/ui/features/pretty-view/resolve-phase.test.ts` from scratch:

    - Import `resolveRenderedState`, `WsTransportState`, `PaneState`, `RenderedState` from `./resolve-phase`
    - 15 tests per the behavior spec
    - Test 14 (the 4×6 matrix) uses a table-driven describe.each pattern to make future additions safe
    - No React imports, no renderHook, no vitest mocks — pure function, pure tests
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/resolve-phase.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^export function resolveRenderedState" src/ui/features/pretty-view/resolve-phase.ts` returns 1
    - `grep -c "^export type WsTransportState" src/ui/features/pretty-view/resolve-phase.ts` returns 1
    - `grep -c "^export type PaneState" src/ui/features/pretty-view/resolve-phase.ts` returns 1
    - `grep -c "^export type RenderedState" src/ui/features/pretty-view/resolve-phase.ts` returns 1
    - `grep -c "BackendFirstFrame\|backendFirstFrame" src/ui/features/pretty-view/resolve-phase.ts` returns 0 (old axis DELETED)
    - `grep -cE "^import " src/ui/features/pretty-view/resolve-phase.ts` returns 0 (no I/O imports; pure reducer)
    - `npx vitest run src/ui/features/pretty-view/resolve-phase.test.ts` exits 0 with all 15 tests passing
    - `npx tsc --noEmit` exits 0 (exhaustiveness sentinel + type surface valid)
  </acceptance_criteria>
  <done>
    resolve-phase.ts encodes the LOCKED Phase-30 truth table; PaneState type mirrors backend emitter values; RenderedState surface unchanged from Phase-29 Phase (only name changes); all 15 truth-table tests pass; compile-time exhaustiveness gate proven; foundation for Task 2 (hook) and Task 3 (PrettyView integration) is in place.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reduce usePaneResolvingMachine.ts to trivial derivation + rewrite tests</name>
  <files>src/ui/features/pretty-view/usePaneResolvingMachine.ts, src/ui/features/pretty-view/usePaneResolvingMachine.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/usePaneResolvingMachine.ts (whole file — replacing it entirely; the current ~380 LOC drops to ~30 LOC)
    - src/ui/features/pretty-view/usePaneResolvingMachine.test.tsx (whole file — most tests DELETE because the machinery they exercise is gone)
    - src/ui/features/pretty-view/resolve-phase.ts (per Task 1 — for the RenderedState/WsTransportState/PaneState types)
    - .planning/phases/30-pane-state-backend-authoritative-no-client-inference/30-CONTEXT.md § Frontend state machine simplifies to consume exactly two inputs
  </read_first>
  <behavior>
    - Test 1: `renderHook(() => usePaneResolvingMachine({ hostId: 1, tmuxSession: "s1", wsTransportState: "open", paneState: "active" }))` returns `{ renderedState: "active", paneState: "active" }`
    - Test 2: Rerender with `paneState: "holding"` → returns `{ renderedState: "holding", paneState: "holding" }` — no delay, no snapshot, immediate transition
    - Test 3: Rerender with `wsTransportState: "opening"` (transient drop) after previous paneState → returns `{ renderedState: "holding", paneState: "holding" }` (D-11 don't-flicker: keep last-known)
    - Test 4: Initial render with `paneState: null, wsTransportState: "opening"` → returns `{ renderedState: "resolving", paneState: null }`
    - Test 5: Cold mount reset — first render with hostId=1, tmuxSession="s1", paneState="active"; rerender with hostId=2 (fresh pane), paneState=null → the machine does NOT retain the previous paneState across paneKey change (the caller is expected to reset its own paneState state slot; this hook is stateless so the reset is caller-responsibility — Test 5 asserts that the hook does NOT retain internal state that would carry paneState=active into the fresh pane)
    - Test 6: NO entry-trigger effect tests — the machinery is gone; if any test references cold-mount effect, warm re-focus effect, PWA foreground effect, rearmSnapshotRef, snapshot, delay-arm, spinner, requestRetry — those tests DELETE
    - Test 7: NO isVisible prop test — isVisible is no longer a hook input
    - Test 8: Hook interface — `renderHook(() => usePaneResolvingMachine({ wsTransportState, paneState })).result.current` has exactly two properties: `renderedState` and `paneState` (echo-through); zero other props (no requestRetry, no showSpinner, no wsState, no backendFirstFrame)
  </behavior>
  <action>
    Rewrite `src/ui/features/pretty-view/usePaneResolvingMachine.ts` from scratch as a trivial derivation:

    ```
    import { resolveRenderedState, type WsTransportState, type PaneState, type RenderedState } from "./resolve-phase";

    export interface UsePaneResolvingMachineDeps {
      wsTransportState: WsTransportState;
      paneState: PaneState | null;
    }

    export interface UsePaneResolvingMachineResult {
      renderedState: RenderedState;
      paneState: PaneState | null;
    }

    export function usePaneResolvingMachine(
      deps: UsePaneResolvingMachineDeps,
    ): UsePaneResolvingMachineResult {
      const renderedState = resolveRenderedState(deps.wsTransportState, deps.paneState);
      return { renderedState, paneState: deps.paneState };
    }
    ```

    That's the whole hook. NO useState, NO useEffect, NO useRef, NO useCallback, NO subscriptions. The hook is a thin wrapper around the pure reducer so the caller has a stable named point for phase-30 identity + a test seam for future re-parameterization.

    DELETE from the hook file (was ~380 LOC):
    - `hostId` / `tmuxSession` / `paneKey` — the caller resets its own paneState React state slot on cold mount (see Task 3 for PrettyView's paneKey-driven setPaneState(null)); the hook stays pure
    - `isVisible` — no longer an entry trigger (backend re-emits pane_state on WS re-attach; frontend just observes)
    - `hasResolvedThisPaneRef` / `paneKeyRef` / `rearmSnapshotRef` / `prevIsVisibleRef` / `isVisibleRef` / `wsStateRef` / `backendFirstFrameRef` — all sentinel refs GONE
    - Three entry-trigger useEffects (cold-mount, warm re-focus, PWA foreground) — GONE
    - Resolution detector useEffect — GONE (the pure reducer runs every render)
    - Spinner delay-arm useEffect + setTimeout(150ms) — GONE (D-04 anti-flash delay is out; backend emits pane_state fast enough that the flash risk is gone; if it comes back, add a paint delay at the RENDER site in PrettyView, not in the hook)
    - `requestRetry` callback — GONE (D-09 retry button remains, but its handler lives inline in PrettyView.tsx and simply setRetryKey(k=>k+1) + resetPaneState(null); no hook involvement)
    - All the Phase-29 comment blocks explaining rearm + snapshot + two-mode semantics — GONE (irrelevant to the new architecture)

    Add a NEW JSDoc block above the hook explaining Phase 30's simplification: this hook exists as a thin wrapper for testing seam parity + future re-parameterization. The pure reducer in resolve-phase.ts does the real work.

    Rewrite `src/ui/features/pretty-view/usePaneResolvingMachine.test.tsx` from scratch:

    - Import `usePaneResolvingMachine` from `./usePaneResolvingMachine`
    - Import `renderHook` from `@testing-library/react`
    - 8 tests per the behavior spec
    - DELETE all Phase-29 tests exercising entry-triggers / snapshot / delay-arm / requestRetry (that machinery is GONE; the test file drops from ~500+ LOC of tests to ~80 LOC)

    IMPORTANT: this task does NOT touch PrettyView.tsx. The hook rewrite happens first so the type surface is stable; Task 3 rewires PrettyView.tsx against the new surface.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/usePaneResolvingMachine.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l src/ui/features/pretty-view/usePaneResolvingMachine.ts` returns fewer than 60 lines total (was ~375; enforces the "trivial hook" contract)
    - `grep -c "useState\|useEffect\|useRef\|useCallback" src/ui/features/pretty-view/usePaneResolvingMachine.ts` returns 0 (proves the hook is truly trivial — no React state/effect machinery)
    - `grep -c "setTimeout" src/ui/features/pretty-view/usePaneResolvingMachine.ts` returns 0 (the D-04 delay-arm is gone; D-04 anti-flash is deferred per D-migration in CONTEXT.md)
    - `grep -c "rearmSnapshotRef\|hasResolvedThisPaneRef\|prevIsVisibleRef\|paneKeyRef" src/ui/features/pretty-view/usePaneResolvingMachine.ts` returns 0 (all sentinel refs GONE)
    - `grep -c "isVisible\|backendFirstFrame\|BackendFirstFrame\|requestRetry\|showSpinner" src/ui/features/pretty-view/usePaneResolvingMachine.ts` returns 0 (old surface GONE)
    - `grep -c "^export function usePaneResolvingMachine" src/ui/features/pretty-view/usePaneResolvingMachine.ts` returns 1
    - `grep -c "wsTransportState.*paneState" src/ui/features/pretty-view/usePaneResolvingMachine.ts` returns at least 1 (proves the two-input signature)
    - `npx vitest run src/ui/features/pretty-view/usePaneResolvingMachine.test.tsx` exits 0 with all 8 tests passing
  </acceptance_criteria>
  <done>
    Hook reduced from ~380 LOC to <60 LOC; zero React state/effect machinery inside the hook; exactly two inputs (wsTransportState, paneState); resolve-phase pure reducer does the derivation; all 8 tests pass; type surface stable and ready for Task 3 to rewire PrettyView.tsx.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewire PrettyView.tsx — new pane_state WS handler + delete captureFirstFrame + delete patch #381 + flip overlay mount gates + rewrite integration tests</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx, src/ui/features/pretty-view/PrettyView.phase29.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/PrettyView.tsx (whole file — this is the largest diff of the phase)
    - src/ui/features/pretty-view/PrettyView.phase29.test.tsx (whole file — Phase 29 integration tests being replaced with Phase-30-equivalent tests)
    - src/ui/features/pretty-view/resolve-phase.ts (per Task 1 — new type surface)
    - src/ui/features/pretty-view/usePaneResolvingMachine.ts (per Task 2 — new hook surface)
    - src/backend/claude-session/pane-state-emitter.ts (per Plan 30-01 — for the wire frame shape {type:"pane_state", state, reason?})
    - .planning/phases/30-pane-state-backend-authoritative-no-client-inference/30-01-SUMMARY.md (for the full reason-code vocabulary the WS handler may receive)
    - .planning/phases/30-pane-state-backend-authoritative-no-client-inference/30-CONTEXT.md § Overlay mount gates (LOCKED) — the target mount conditions
    - .planning/phases/30-pane-state-backend-authoritative-no-client-inference/30-CONTEXT.md § specifics — the patch #381 site is around line 379-383; the D-11 "any live message swaps back to active" comment block is around L1069; extensive list of captureFirstFrame sites via grep
  </read_first>
  <action>
    This is the largest single diff of Phase 30. Approach in five sub-steps to keep it reviewable:

    Sub-step A — Add new state slot + WS handler case.

    Add a new React state slot near the existing `const [backendFirstFrame, setBackendFirstFrame] = useState<BackendFirstFrame>("not-yet");` (around L632):

    ```
    // Phase 30: paneState — backend-authoritative verdict from the pane_state
    // wire frame. null = not yet received (fresh mount + WS opening).
    const [paneState, setPaneState] = useState<PaneState | null>(null);
    ```

    Import `PaneState` from `./resolve-phase` at the top of the file (near the existing resolve-phase import).

    In the WS onmessage handler switch statement (around L1054 for the case dispatch), add a NEW case BEFORE the existing `case "session":` case:

    ```
    case "pane_state": {
      // Phase 30 PS30-01/PS30-04: backend-authoritative pane-entry verdict.
      // Backend emits on WS attach (after connectToPane discovery) + on every
      // state change. Frontend just stores it — no inference, no client
      // hints, no captureFirstFrame. Overlay mount gates below read
      // paneState === "..." directly.
      // parsed.state is one of "active"|"holding"|"dormant"|"inactive"|"error";
      // parsed.reason is optional diagnostic string, currently unused by the UI.
      setPaneState(parsed.state);
      break;
    }
    ```

    The `parsed` variable in this scope is typed by parseSessionLine's ParsedLine union — extend the WS frame parser type at the top of the WS onmessage handler to include `{type:"pane_state", state:PaneState, reason?:string}` as a valid discriminated union member. Look for the existing type union at ~L1052 (before the switch) and add the new variant.

    Sub-step B — Cold-mount paneState reset.

    In the fresh-pane useEffect at ~L933-970 (the block that clears messages/status/errorMessage/contextPct/harnessTasks/backgroundedAgents/backgroundedShells/planPending/isHolding/backendFirstFrame on paneKey change), add a companion reset:

    ```
    setPaneState(null);
    ```

    This is the caller responsibility referenced in Task 2 Test 5 — a fresh pane clears its own paneState state slot so the machine's `paneState === null` short-circuits to `resolving` per truth-table row 4.

    Sub-step C — Delete ALL captureFirstFrame call sites + the useCallback declaration + zombie phase-29 comment blocks.

    **F3 acknowledgment (plan-checker 2026-08-10):** every captureFirstFrame call site being deleted is surrounded (above and often below) by phase-29 comment blocks that EXPLAIN the client-inference machinery (why captureFirstFrame is being called on this frame, how backendFirstFrame flips, what the D-11 "any live message swaps to active" rule does, etc.). When the code deletes, THE COMMENTS EXPLAINING THE CODE MUST DELETE TOO — leaving zombie prose referencing deleted machinery is worse than useless (it misleads future readers into thinking the machinery still exists). Enumerated zombie comment classes to remove alongside their code:

    - "phase-29:" prefixed comments describing captureFirstFrame invocations (dozens of instances; grep-gated below)
    - "phase-29 (plan 29-05 test-audit fix)" comment blocks describing the D-11 client-inference rule for specific frame types
    - "usePaneResolvingMachine: captureFirstFrame(...) calls at the per-case handlers below flip backendFirstFrame off 'not-yet'" comment blocks
    - "captureFirstFrame runs inside the WS onmessage handler which captures..." comment blocks
    - "backendFirstFrameRef" comment blocks (~L611-631 lead-in and the captureFirstFrame useCallback JSDoc at ~L633)
    - Any comment mentioning "captureFirstFrame" as a subject that is being deleted

    KEEP comments that describe non-phase-29 behavior even if they SIT NEAR a captureFirstFrame call site — e.g. the "quick 260808-cd6: live-frame auto-dismiss" comment at ~L1016 describes the setDormant(false) behavior which stays; only the captureFirstFrame line inside that block deletes, and the surrounding comment about auto-dismiss stays (edit the comment to remove the "capture..." sentence if present, keep the auto-dismiss rationale).

    Identify every captureFirstFrame call site (grep confirms ~10-14 sites — the count from CONTEXT.md § specifics is "~10 call sites"; get the exact count via `grep -c "captureFirstFrame(" src/ui/features/pretty-view/PrettyView.tsx`). Sites include:
    - L381 (onResetClicked — patch #381 anti-pattern; DELETE per PS30-05)
    - L1045 (dormantRef live-frame auto-dismiss — DELETE captureFirstFrame call; keep the setDormant(false) alongside)
    - L1058, L1069, L1079, L1087, L1095, L1106 (message/image/relay_outbound/relay_inbound/malformed_line/other content-frame captureFirstFrame("active") — the D-11 "any live message swaps back to active" client-inference rule; DELETE all)
    - L1135, L1140 (inactive frame captures — DELETE)
    - L1162, L1192 (dormant frame captures — DELETE the captureFirstFrame; keep the setDormant calls if DormancyOverlay reads dormant directly)
    - L1296, L1317, L1362 (session_holding/session_holding_cleared/session_changed captures — DELETE)

    ALSO DELETE:
    - The `captureFirstFrame` useCallback declaration at ~L634
    - The `backendFirstFrameRef` useRef declaration at ~L631
    - The `backendFirstFrame` useState declaration at ~L632-633
    - The `setBackendFirstFrame` call in the fresh-pane useEffect (L967-968)
    - The `backendFirstFrame` prop passed to usePaneResolvingMachine (L916)
    - The `backendFirstFrame` reference in the wsState derivation at L899 — REWRITE the derivation per Sub-step D below
    - Import of `BackendFirstFrame` from `./resolve-phase` (that type is DELETED per Task 1)

    Preserve legacy WS handler case bodies for their non-phase side effects:
    - `case "session"`: keeps setPid + setSessionFile + setStatus("streaming")
    - `case "message"` / `case "image"` / `case "relay_outbound"` / `case "relay_inbound"` / `case "malformed_line"`: keep setMessages append + setStatus("streaming") — DELETE captureFirstFrame
    - `case "session_holding"`: keep setIsHolding(true) (ComposeBox still reads isHolding prop per line L2270; ComposeBox is out-of-scope per CONTEXT.md § Deferred + Phase 29 CONTEXT § Out of scope; see Sub-step F for the alignment note)
    - `case "session_holding_cleared"`: keep setIsHolding(false); DELETE captureFirstFrame("active")
    - `case "session_changed"`: keep the heavy state reset (setMessages/setHarnessTasks/setContextPct/setBackgroundedAgents/setBackgroundedShells/setPlanPending/setIsHolding(false)/publishSessionHasBackgroundedWork/setStatus("streaming")/clearAsideState); DELETE captureFirstFrame("active")
    - `case "dormant"`: keep setDormant(parsed.dormant); DELETE captureFirstFrame(parsed.dormant ? "dormant" : "active"); DELETE the wakingSince state slot updates ONLY if they were tied to captureFirstFrame (they weren't — leave them)
    - `case "inactive"`: keep setStatus("inactive") + setInactiveReason; DELETE the captureFirstFrame(inactive) call at L1135; DELETE the captureFirstFrame(inactive) at L1140

    Sub-step D — Rewrite wsTransportState derivation.

    The current derivation at L896-903 references `backendFirstFrame`. Rewrite it WITHOUT the backendFirstFrame axis (it's DELETED) — the new derivation reads only status + reconnectAttempts:

    ```
    const wsTransportState: WsTransportState =
      status === "error" && reconnectAttempts >= MAX_RECONNECT_ATTEMPTS
        ? "failed-permanently"
        : status === "streaming" || status === "inactive"
          ? "open"
          : status === "connecting" && reconnectAttempts === 0
            ? "not-connected"
            : "opening";
    ```

    Rename local variable `wsState` → `wsTransportState` throughout PrettyView.tsx to match the Phase-30 wire-nomenclature from CONTEXT.md § Signal set.

    Sub-step E — Rewrite usePaneResolvingMachine invocation.

    Replace the current call at L910-917 with the new two-input surface:

    ```
    const { renderedState, paneState: currentPaneState } = usePaneResolvingMachine({
      wsTransportState,
      paneState,
    });
    ```

    Note: `renderedState` replaces `phase` throughout the file — rename every `phase === "..."` → `renderedState === "..."` (SessionHoldingOverlay mount gate at ~L1995, DormancyOverlay mount gate at ~L2000, PrettyViewLoadingOverlay mount gate at ~L2012, PrettyViewErrorOverlay mount gate at ~L2029, etc.). The overlay mount gates now read:

    - `renderedState === "holding"` → SessionHoldingOverlay (unchanged from Phase 29 conceptually — the source of the value differs)
    - `renderedState === "dormant"` → DormancyOverlay (unchanged)
    - `renderedState === "resolving"` → PrettyViewLoadingOverlay (unchanged from Phase 29 minus the showResolvingSpinner delay-arm gate which is DELETED per Task 2)
    - `renderedState === "error"` → PrettyViewErrorOverlay (unchanged)
    - `renderedState === "inactive"` → existing inactive fallback banner (search for the current banner site; may be gated on status === "inactive" today — flip to renderedState === "inactive")

    DELETE the `showResolvingSpinner` variable + its usage in the spinner mount gate at L2012 (Task 2 deleted the delay-arm inside the hook). Replace `{renderedState === "resolving" && showResolvingSpinner && <PrettyViewLoadingOverlay />}` with `{renderedState === "resolving" && <PrettyViewLoadingOverlay />}`.

    DELETE the `requestRetry` destructure + `handleRetry` useCallback at L919-931. Replace PrettyViewErrorOverlay's onRetry prop (~L2029) with a simpler inline handler:

    ```
    onRetry={() => {
      reconnectAttemptsRef.current = 0;
      setReconnectAttempts(0);
      setStatus("connecting");
      setPaneState(null); // clear last-known so we re-enter resolving until backend re-emits pane_state
      setRetryKey((k) => k + 1);
    }}
    ```

    Sub-step F — onResetClicked cleanup (patch #381).

    At ~L379-383, the current implementation is:

    ```
    const onResetClicked = useCallback(() => {
      setIsHolding(true);
      captureFirstFrame("session_holding");
    }, []);
    ```

    Rewrite to:

    ```
    const onResetClicked = useCallback(() => {
      // Phase 30 PS30-05: patch #381 anti-pattern (captureFirstFrame client
      // hint) DELETED. Backend session-file parser (Plan 30-02) fires
      // pane_state:holding within ms of /id reset landing in the JSONL;
      // frontend just waits for the backend truth. setIsHolding(true) also
      // deleted — it was only there for the pre-Phase-29 SessionHoldingOverlay
      // mount gate and the Phase-29 backendFirstFrame client-inference bridge;
      // both are GONE. The ComposeBox still reads isHolding prop (~L2270);
      // rewire that prop to read renderedState === "holding" instead:
      // isHolding={renderedState === "holding"} at the JSX site.
    }, []);
    ```

    In practice: onResetClicked becomes a no-op (or delete the useCallback entirely and remove the onReset prop from the reset button JSX). The reset button is now purely a UI trigger for a tmux keystroke (which happens elsewhere in the reset-button component); no client-side state manipulation is needed because the backend will observe /id reset and emit pane_state:holding.

    Sub-step G — Flip ComposeBox isHolding prop derivation.

    At L2270 where `isHolding={isHolding}` is passed to ComposeBox, flip to `isHolding={renderedState === "holding"}`. If nothing else in the file uses the local `isHolding` state slot after Sub-step C's edits (grep to verify), DELETE the useState declaration at L324 as well. If ComposeBox is the ONLY consumer of isHolding, this deletes the last remaining setIsHolding call sites — audit with `grep -c "setIsHolding" src/ui/features/pretty-view/PrettyView.tsx` after Sub-step F and delete if zero-count.

    Similarly, evaluate the `dormant` local state slot at L346 — if DormancyOverlay reads dormant directly via prop (grep for `dormant={` in the DormancyOverlay JSX around L2000), keep the state slot; if the overlay is now gated on `renderedState === "dormant"` only and no prop consumer remains, DELETE the state slot.

    Sub-step H — Rewrite PrettyView.phase29.test.tsx to phase-30 integration tests.

    The current file exercises Phase-29 machinery (captureFirstFrame + backendFirstFrame + entry-triggers + delay-arm). All of that is GONE. Rewrite the file (or rename to `PrettyView.phase30.test.tsx` — planner discretion) with new tests:

    - Test A: mount PrettyView with mocked WS; send `{type:"pane_state", state:"active"}` frame; assert no overlay is mounted (just message view)
    - Test B: mount + send `{type:"pane_state", state:"holding"}`; assert SessionHoldingOverlay is mounted
    - Test C: mount + send `{type:"pane_state", state:"holding"}` then `{type:"pane_state", state:"active"}`; assert SessionHoldingOverlay unmounts and message view is visible
    - Test D: mount + no pane_state received; assert PrettyViewLoadingOverlay is mounted (renderedState === "resolving")
    - Test E: mount + WS transport reaches MAX_RECONNECT_ATTEMPTS; assert PrettyViewErrorOverlay is mounted (renderedState === "error")
    - Test F: mount + send `{type:"pane_state", state:"active"}`; then simulate transient WS drop (wsTransportState → "opening" via status="connecting" + reconnectAttempts=1); assert the overlay does NOT flip to resolving (D-11 don't-flicker: renderedState stays "active" because last-known paneState is retained)
    - Test G: click reset button; assert onResetClicked does NOT call any client-side pane_state-mutating code (grep-based assertion: rendered JSX has no captureFirstFrame reference; runtime assertion: fires no state updates besides the tmux keystroke path which is out of scope)
    - Test H (per F4 in Sub-step I below): backend-side round-trip latency approximation — mock the parser observation channel per Sub-step I's spec; assert paneStateEmitter's wsSend spy fires SYNCHRONOUSLY with a pane_state:holding frame before the next microtask tick. Bounds the backend-side latency to WS transit only; frontend-side receive-to-render is covered by Tests A-G.

    Use the existing test scaffolding from PrettyView.phase29.test.tsx as a starting point (mock WS + renderHook + fireEvent for the reset button). Preserve the existing test infrastructure setup (widened ResizeObserver polyfill etc. per PrettyView.aside.test.tsx pattern).

    Sub-step I — F4 latency acknowledgment: reset-click → pane_state:holding round-trip.

    **F4 (plan-checker 2026-08-10):** deleting patch #381's optimistic client hint (Sub-step F) means the visible-flash bug that patch #381 originally fixed COULD regress if the backend parser → pane_state emit → frontend receive round-trip latency exceeds ~1 frame (~16ms at 60fps, or ~100ms as a generous visible-flash threshold).

    **Explicit acknowledgment (on record in the plan):** the planner accepts sub-frame latency risk without a production measurement gate. Rationale:

    1. **The parser observation lands on the SAME event-loop tick as the /id reset user turn appearing in the JSONL tail** — the tail's onLine callback fires the parser observation synchronously; the emitter's ws.send fires on the same tick (no await, no debounce). Wire-level latency is bounded by the WS RTT to the client's browser, which on Ashley's tailnet-only fleet is typically <20ms.

    2. **The reset button's tmux keystroke path (the user-visible click → /id reset in JSONL) is the SLOW part of the round-trip** — the tmux keystroke has its own inherent RTT (SSH exec + tmux send-keys + Claude Code processing the /id reset command + JSONL write). That latency dominates by 50-100x over the WS emit-and-receive path. Patch #381 was working around a Phase-29 client-inference gap (frontend didn't KNOW to enter holding until backendFirstFrame flipped), not a network-latency gap.

    3. **After Phase 30, the backend is the sole source of truth for pane_state, so there is NO frontend state that could be more up-to-date than the backend even in principle.** A client hint would either lie (say holding when backend hasn't confirmed) or duplicate what the backend already says.

    Integration test Test H (added to Sub-step H's list): approximate the round-trip via a scripted mock — dispatch a synthesized /id reset user-turn line into the backend session-file tail's onLine handler (via the existing test seam `__applyLayer1LineForTests` in layer1-detect.ts OR the equivalent parser-observation seam that Plan 30-02 Task 2's implementer may add if convenient — planner allows either seam), assert the paneStateEmitter's wsSend spy fires SYNCHRONOUSLY (no setTimeout, no queueMicrotask observed) before the next microtask tick. This proves the backend-side latency is bounded to WS transit only; browser-side receive-to-render is React's normal setState → re-render cycle (<16ms in a hot component).

    **Fallback plan if UAT surfaces flash regression** (documented here, out of scope for this plan's implementation): add an OPTIMISTIC CLIENT HINT that is DISTINCT from patch #381's client-source-of-truth pattern:

    - On reset button click, immediately call `setPaneState("holding")` locally — this OVERRIDE-hints the state machine to render the holding overlay without waiting for the backend
    - The moment ANY pane_state frame arrives from the backend (even one that says something other than "holding"), the local state slot updates to the backend's value — backend authority WINS, no persistence of the client hint
    - Semantically: the client hint fills the sub-100ms latency window with a best-guess overlay; the backend then confirms or corrects
    - This is DIFFERENT from patch #381 in that patch #381 was a client-inference source-of-truth that could disagree with the backend indefinitely (until a backendFirstFrame update overrode it); the new design has the backend override on the very next frame, no matter what state the backend reports
    - Implementation would live in the onResetClicked handler: `onClick={() => { setPaneState("holding"); /* tmux keystroke fires as normal */ }}` — deletable in one line if UAT shows the hint is not needed
    - This plan does NOT implement the fallback. It documents it for the deploy-orchestrator's UAT loop.

    Add integration test Test H to Sub-step H's list before finalizing:

    - **Test H:** mock parser observation channel (call Plan 30-02's onLine consumer directly with a synthesized /id reset raw line, using the paneStateEmitter mock's wsSend spy); assert wsSend was called synchronously with a pane_state:holding frame BEFORE the vitest event loop tick advances (no `await` needed between the trigger and the assertion). This test approximates the backend-side round-trip; frontend-side receive-to-render is covered by Tests A-G above.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-view/ && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "captureFirstFrame" src/ui/features/pretty-view/PrettyView.tsx` returns 0 (all call sites AND the useCallback declaration DELETED — PS30-04 + PS30-05 gate)
    - `grep -c "backendFirstFrame\|BackendFirstFrame" src/ui/features/pretty-view/PrettyView.tsx` returns 0 (the axis is GONE)
    - `grep -c 'case "pane_state"' src/ui/features/pretty-view/PrettyView.tsx` returns 1 (new WS handler case exists)
    - `grep -c 'setPaneState' src/ui/features/pretty-view/PrettyView.tsx` returns at least 3 (the pane_state case, the fresh-pane reset, the retry-button reset)
    - `grep -c 'renderedState === "holding"' src/ui/features/pretty-view/PrettyView.tsx` returns at least 2 (SessionHoldingOverlay mount gate + ComposeBox isHolding prop derivation)
    - `grep -c 'renderedState === "dormant"' src/ui/features/pretty-view/PrettyView.tsx` returns at least 1 (DormancyOverlay mount gate)
    - `grep -c 'renderedState === "error"' src/ui/features/pretty-view/PrettyView.tsx` returns at least 1 (PrettyViewErrorOverlay mount gate)
    - `grep -c 'renderedState === "resolving"' src/ui/features/pretty-view/PrettyView.tsx` returns at least 1 (PrettyViewLoadingOverlay mount gate)
    - `grep -c 'renderedState === "inactive"' src/ui/features/pretty-view/PrettyView.tsx` returns at least 1 (inactive fallback banner mount gate)
    - `grep -c 'wsTransportState' src/ui/features/pretty-view/PrettyView.tsx` returns at least 2 (derivation + hook invocation)
    - `grep -c 'showSpinner\|showResolvingSpinner\|requestRetry\|handleRetry' src/ui/features/pretty-view/PrettyView.tsx` returns 0 (Phase-29 machinery GONE)
    - `grep -c "rearmSnapshotRef\|hasResolvedThisPaneRef" src/ui/features/pretty-view/PrettyView.tsx` returns 0 (sentinel refs GONE)
    - `npx vitest run src/ui/features/pretty-view/` exits 0 with zero failures (proves rewritten tests + existing test suite compatibility)
    - `npx tsc --noEmit` exits 0
    - No new `setTimeout` calls introduced in the WS handler or overlay mount paths: `grep -c "setTimeout" src/ui/features/pretty-view/PrettyView.tsx` unchanged from pre-plan baseline OR reduced (the delay-arm setTimeout inside the hook was already relocated per Task 2)
    - **F3 zombie comment cleanup gate — phase-29 client-inference comment blocks removed alongside their code:**
    - `grep -c "phase-29 (plan 29-05 test-audit fix)" src/ui/features/pretty-view/PrettyView.tsx` returns 0 (all D-11 zombie explanatory blocks removed alongside the captureFirstFrame calls they described)
    - `grep -c "backendFirstFrame" src/ui/features/pretty-view/PrettyView.tsx` returns 0 (already gated above, but explicitly re-listed here: comment prose referencing backendFirstFrame is also removed — the identifier appears zero times whether in code OR in comments)
    - `grep -c "captureFirstFrame" src/ui/features/pretty-view/PrettyView.tsx` returns 0 (already gated above, but re-emphasized: comment prose referencing captureFirstFrame is also removed — including JSDoc blocks that documented the useCallback that has been deleted)
    - `grep -c "usePaneResolvingMachine: captureFirstFrame" src/ui/features/pretty-view/PrettyView.tsx` returns 0 (this specific comment lead-in appears in multiple case-handler blocks — all instances gone)
    - **F4 acknowledgment on record**: Plan explicitly acknowledges sub-frame latency risk from deleting patch #381 without a production measurement gate; integration Test H approximates backend-side round-trip; UAT-time flash regression fallback documented (client-hint-with-backend-override) but out of scope for implementation
  </acceptance_criteria>
  <done>
    PrettyView.tsx has the new pane_state WS handler; every captureFirstFrame call site + the useCallback DELETED; patch #381's onResetClicked client-hint DELETED; overlay mount gates read paneState/renderedState directly; ComposeBox isHolding prop derived from renderedState; PrettyView.phase29.test.tsx rewritten as phase-30 integration tests (Tests A-H including Test H's backend-side round-trip latency approximation per F4); F3 zombie phase-29 comment blocks removed alongside their code (grep-gated via "phase-29 (plan 29-05 test-audit fix)" returning 0); F4 latency acknowledgment on record with UAT-fallback client-hint-with-backend-override design documented; full pretty-view suite green; tsc clean; the client-inference machinery is completely gone (code AND comments — grep-gated).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → WS client (pane_state frame) | The new pane_state frame is emitted by the backend (trusted) and consumed by the frontend. Same trust boundary as existing WS frames. |
| WS onmessage handler → React state | parsed.state values are backend-authoritative; the frontend does NOT re-validate the enum (TypeScript type + `default` case in the WS switch drop unknown frames). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03-01 | Spoofing | Malicious WS server (compromised backend) sends `{type:"pane_state", state:"active"}` when the pane is actually recycling, hiding the holding overlay from Ashley | accept | This threat is not new to Phase 30 — the pre-Phase-30 backend already had authoritative say over `session_holding` / `dormant` / `inactive` frames. Ashley's threat model already accepts a compromised backend as game-over (per CLAUDE.md: Skynet IS the gateway; a compromised backend loses everything). Phase 30 does not expand the trust surface. |
| T-30-03-02 | Tampering | Frontend receives an unknown pane_state value (e.g. backend emits a new value added post-deploy without frontend update) | mitigate | The `default:` branch in the WS onmessage switch drops unknown frames silently (existing pattern for unknown frame types). The resolve-phase exhaustiveness sentinel (`_exhaust: never`) fails `tsc --noEmit` at compile time if a new PaneState value is added to the frontend union without a matching switch branch — preventing silent misrender. The paneState React state slot uses `PaneState | null`; if TypeScript is bypassed at runtime (e.g. via a JSON with a bogus state value), the resolve-phase switch's default catches it and returns via _exhaust, which at runtime throws (compile-time protection is primary; runtime is defense-in-depth). |
| T-30-03-03 | Information disclosure | pane_state.reason field surfaced in UI could leak backend internals | mitigate | Phase 30 does NOT surface reason in the UI (per Task 3 Sub-step A: `parsed.reason is optional diagnostic string, currently unused by the UI`). Future UI enhancements that display reason must go through a fresh threat review. |
| T-30-03-04 | Denial of service | Backend spams `pane_state` frames | mitigate | The emitter (Plan 30-01) dedupes by (state, reason); duplicate transitions produce zero wire frames. Frontend setPaneState is a React state update — cheap and batched. Worst case is per-transition, not per-line. |
| T-30-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan (all edits use existing React + vitest + testing-library which are already in devDeps). Package legitimacy gate N/A. |
</threat_model>

<verification>
- `npx vitest run src/ui/features/pretty-view/resolve-phase.test.ts` — all 15 truth-table tests pass
- `npx vitest run src/ui/features/pretty-view/usePaneResolvingMachine.test.tsx` — all 8 trivial-hook tests pass
- `npx vitest run src/ui/features/pretty-view/PrettyView.phase29.test.tsx` (or renamed .phase30.) — all 8 integration tests pass (Tests A-H, including Test H's F4 backend-side latency approximation)
- `npx vitest run src/ui/features/pretty-view/` — full pretty-view suite green (proves no regression to non-phase-30 pretty-view components — ChatMessage, ComposeBox, DropOverlay, ImageBubble, etc.)
- `npx vitest run` — FULL project suite green (proves no cross-cutting regression; SPEC constraint from Phase 29 that Phase 30 inherits)
- `npx tsc --noEmit` — no type errors
- Grep gates on Task 3 acceptance_criteria prove the client-inference machinery is completely gone
</verification>

<success_criteria>
Plan 30-03 is complete when:
- resolve-phase.ts encodes the LOCKED Phase-30 truth table (PS30-06); pure reducer with zero I/O imports; F1 acknowledgment comment on the default: exhaustiveness branch documents its compile-time-only purpose (runtime never reaches it)
- usePaneResolvingMachine.ts is a trivial <60-LOC derivation with zero React state/effect machinery (PS30-04)
- PrettyView.tsx has a new `case "pane_state"` WS handler storing parsed.state into a paneState React state slot
- All ~10 captureFirstFrame call sites AND the useCallback declaration DELETED (PS30-04)
- Patch #381's onResetClicked client-hint captureFirstFrame DELETED (PS30-05)
- Overlay mount gates read paneState / renderedState directly (backend-authoritative — PS30-04 + PS30-06)
- ComposeBox isHolding prop derived from renderedState === "holding" (not from local state)
- PrettyView.phase29.test.tsx rewritten as Phase-30 integration tests exercising the new architecture
- Full pretty-view test suite green + full project vitest green + tsc clean (PS30-08 full-suite green precondition)
- Grep gates prove the client-inference machinery is gone: zero captureFirstFrame, zero backendFirstFrame, zero rearmSnapshotRef, zero hasResolvedThisPaneRef
- **F3 grep gate satisfied**: zero "phase-29 (plan 29-05 test-audit fix)" comment blocks remain — zombie explanatory prose deleted alongside the code it described
- **F4 acknowledgment on record**: plan text explicitly accepts sub-frame latency risk from deleting patch #381; integration Test H approximates backend-side round-trip latency (synchronous emit before microtask tick); UAT-time flash regression fallback (client-hint-with-backend-override, distinct from patch #381's client-source-of-truth pattern) documented for the deploy-orchestrator's UAT loop but out of scope for this plan's implementation
- Executor STOPS at commit-with-tests-green per the deploy protocol LOCK in <additional_context>
</success_criteria>

<output>
Create `.planning/phases/30-pane-state-backend-authoritative-no-client-inference/30-03-SUMMARY.md` when done. Include:
- Exact count of captureFirstFrame sites deleted (should match the grep count from CONTEXT.md § specifics — "~10 sites")
- Confirmation that patch #381's onResetClicked cleanup is complete (line reference + before/after snippet)
- Which local state slots survived the cleanup (isHolding? dormant?) and why (which prop consumers still need them, if any — e.g. ComposeBox isHolding is now derived from renderedState so isHolding local state slot may be fully deletable)
- Any surprises encountered wiring the wsTransportState derivation (backendFirstFrame was in the derivation as a proof-of-life shortcut; without it, does the derivation cover the "cold mount with dormant/holding first frame" case correctly via just status="streaming"|"inactive" — or does the pane_state frame arrive fast enough that the status transition happens naturally?)
- Confirmation that the PrettyView.phase29 → PrettyView.phase30 test rewrite covers the seven scenarios A-G
- Full-suite green confirmation with the exact `npx vitest run` output line count / failure count
</output>
