---
phase: 118-first-class-apps-sweep-registry-shape-2
plan: 04
type: execute
wave: 4
depends_on:
  - 118-02
  - 118-03
files_modified:
  - src/backend/fleet-status/ssh-poll-orchestrator.ts
  - src/backend/fleet-status/ssh-poll-orchestrator.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "A per-host sweep tick that returns one `SweepAppLine` results in exactly one `publishAppUpdate` call on the registry with the adapted `AppState` (all seven D-05 fields correctly mapped from snake_case wire names to camelCase AppState fields)."
    - "A per-host sweep tick that succeeds and emits fewer apps than the previous tick calls `publishAppGoneByHostSlug` exactly once for each slug that dropped out."
    - "A sweep that FAILS (channel returns null / exec times out / schema mismatch) does NOT invoke the app-reconciliation block — no `publishAppGoneByHostSlug` calls, no update to `lastTickLiveApps` (D-12 SSH-transient safety)."
    - "A health-flip (same slug present in both ticks, `is_healthy` flipped) emits exactly ONE `publishAppUpdate` on the flip tick and ZERO `publishAppGoneByHostSlug` — D-13 update-not-gone-and-re-add discipline."
    - "The `lastTickLiveApps` set tracks ALL slugs in the picture (healthy OR unhealthy — D-02 inclusion), NOT just healthy ones. Failure to track this correctly causes unhealthy apps to be gone-and-re-added every tick."
  artifacts:
    - path: src/backend/fleet-status/ssh-poll-orchestrator.ts
      provides: "`PerHostState` gains `lastTickLiveApps: Set<string>` field alongside `lastTickLiveTreeIdentities`; per-host state creation site initializes to `new Set<string>()`; the batch-path per-host handler consumes `parsed.appLines`, adapts each to `AppState`, calls `publishAppUpdate`, then runs a reconciliation block (mirroring the identity reconciliation at L1770-1792) that publishes `publishAppGoneByHostSlug` for slugs that dropped out."
      contains: "lastTickLiveApps"
    - path: src/backend/fleet-status/ssh-poll-orchestrator.test.ts
      provides: "`MockRegistry` gains `publishedAppUpdates` + `publishedAppGone` arrays + stub methods; `makeSweepJsonl` gains `apps?` parameter; at least five new reconciliation tests (A1-A5) covering first-tick / removal / SSH-failure-no-flap / health-flip / schema-mismatch."
      contains: "publishedAppUpdates"
  key_links:
    - from: src/backend/fleet-status/ssh-poll-orchestrator.ts (pollOneHostBatch success path)
      to: src/backend/fleet-status/subscription-registry.ts (publishAppUpdate)
      via: "compose-and-publish loop `for (const appLine of parsed.appLines) deps.registry.publishAppUpdate(host.id, adaptAppLineToState(host.id, appLine))` placed adjacent to the existing identity compose-and-publish loop at L1760-1768"
      pattern: "publishAppUpdate\\(host\\.id"
    - from: src/backend/fleet-status/ssh-poll-orchestrator.ts (reconciliation block)
      to: src/backend/fleet-status/subscription-registry.ts (publishAppGoneByHostSlug)
      via: "diff-then-publish-gone loop mirroring the identity reconciliation at L1787-1789, placed immediately after the identity reconciliation block (L1792) and INSIDE the sweep-success scope"
      pattern: "publishAppGoneByHostSlug\\(host\\.id"
---

<objective>
Wire the batch-path per-host handler in `ssh-poll-orchestrator.ts` (3178 lines) to consume the `SweepAppLine` entries produced by 118-02's `parseSweepJsonl` and fan them out via the `publishAppUpdate` / `publishAppGoneByHostSlug` methods added by 118-03. This includes an adapter (snake_case wire → camelCase `AppState`) and a per-host reconciliation-on-success block that mirrors the Phase 115 `67b4a7ef` identity reconciliation (D-11 says: verbatim shape, applied to apps). This is the plan where the orchestrator's cache converges with the sweep's truth for apps.

Purpose: Deliver D-11 (adopt Phase 115's reconciliation verbatim, for apps), D-12 (reconciliation ONLY on sweep success — SSH failures don't flap), D-13 (health-flip = update, not gone+add). Also delivers the orchestrator-side of D-01 + D-02 (adapter respects the Python's inclusion decisions — orchestrator does NOT re-check; every emitted line becomes an `AppState` in the map).

Output: `ssh-poll-orchestrator.ts` gains one new `PerHostState` field, its initializer, an adapter helper `adaptAppLineToState`, a compose-and-publish loop for `parsed.appLines`, and a reconciliation block immediately after the identity reconciliation. `ssh-poll-orchestrator.test.ts` (8860+ lines) gains `MockRegistry` extensions, `makeSweepJsonl` extension, and five reconciliation tests (A1-A5).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/118-first-class-apps-sweep-registry-shape-2/116-CONTEXT.md
@.planning/phases/118-first-class-apps-sweep-registry-shape-2/116-RESEARCH.md
@.planning/phases/118-first-class-apps-sweep-registry-shape-2/116-PATTERNS.md
@.planning/phases/118-first-class-apps-sweep-registry-shape-2/118-02-SUMMARY.md
@.planning/phases/118-first-class-apps-sweep-registry-shape-2/118-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PerHostState field + adapter + compose-publish loop + reconciliation block</name>
  <files>src/backend/fleet-status/ssh-poll-orchestrator.ts</files>
  <read_first>
    - src/backend/fleet-status/ssh-poll-orchestrator.ts targeted ranges (do NOT read the full 3178-line file — use Grep + narrow Reads):
      - L1-50 (imports + module doc)
      - L426-486 (PerHostState interface — L476-485 is the exact `lastTickLiveTreeIdentities` docblock template for the new field)
      - L1390-1400 (sweep exec timeout constant — verify it's 8000ms per RESEARCH § Q5, not the CONTEXT's assumed 5000ms)
      - L1750-1800 (the identity compose-and-publish loop L1760-1768 + the identity reconciliation block L1770-1792 — the exact code paragraphs to place the app siblings adjacent to)
      - L2990-3016 (per-host state creation site — L3004-3008 is where `lastTickLiveTreeIdentities: new Set<string>()` is initialized; the new `lastTickLiveApps: new Set<string>()` goes immediately after)
    - src/backend/fleet-status/sweep-schema.ts POST-118-02 — the `SweepAppLine` interface and `parseSweepJsonl` return shape including `appLines`.
    - src/backend/fleet-status/wire-protocol.ts POST-118-03 — the `AppState` type + its camelCase field names (`hostId`, `slug`, `title`, `description`, `port`, `hasIcon`, `createdAtMs`, `isHealthy`, `healthMessage`) — the adapter target shape.
    - src/backend/fleet-status/subscription-registry.ts POST-118-03 — the `SubscriptionRegistry` interface with `publishAppUpdate` / `publishAppGoneByHostSlug` / `getAppSnapshot`.
    - .planning/phases/118-first-class-apps-sweep-registry-shape-2/116-RESEARCH.md § Q2 (PerHostState extension shape — sibling field NOT new struct), § Q5 (sweep exec timeout is 8000ms — verify), § Pitfall 1 (reconciliation-on-success gate), § Pitfall 3 (health-flip discipline — tracking set holds ALL slugs in the picture, healthy OR unhealthy).
    - .planning/phases/118-first-class-apps-sweep-registry-shape-2/116-PATTERNS.md § 5 ssh-poll-orchestrator (analogs verbatim — the identity reconciliation code paragraph L1770-1792 is the exact template).
    - .planning/phases/118-first-class-apps-sweep-registry-shape-2/116-CONTEXT.md § D-11 (adopt Phase 115's reconciliation verbatim for apps), § D-12 (ONLY on sweep success), § D-13 (health-flip is update).
    - git show 67b4a7ef -- src/backend/fleet-status/ssh-poll-orchestrator.ts — the Phase 115 diff establishing the identity reconciliation pattern. Read for the load-bearing invariant: reconciliation lives inside the sweep-success scope, after all compose+publish loops, before `return { ok: true, ... }`. All `{ ok: false }` early-returns above bypass it.
  </read_first>
  <behavior>
    (Verified via existing `MockRegistry` + `makeSweepJsonl` fixture — Task 2 extends both.)
    - A1: A per-host sweep tick emitting two app lines results in exactly two `publishAppUpdate` calls (one per app, correctly adapted) and ZERO `publishAppGoneByHostSlug` calls (nothing to reconcile against — first tick has empty `lastTickLiveApps`).
    - A2: Two ticks — tick 1 emits apps {a, b}, tick 2 emits {a} — assert exactly one `publishAppGoneByHostSlug` call on tick 2 for slug "b", and one `publishAppUpdate` call for slug "a". Zero unexpected updates for "b" on tick 2.
    - A3: Three ticks — tick 1 emits {a}, tick 2 channel returns null (SSH failure — `{ok:false}` early return), tick 3 emits {a}. Assert ZERO `publishAppGoneByHostSlug` calls across the whole sequence. Assert `hostState.lastTickLiveApps` still contains {a} after tick 2 (untouched — RESEARCH § Pitfall 1).
    - A4: Two ticks — tick 1 emits {a, is_healthy: true}, tick 2 emits {a, is_healthy: false}. Assert exactly ONE `publishAppUpdate` on tick 2 (with `isHealthy: false` and `healthMessage: "not running — ask an agent to check on it"`) and ZERO `publishAppGoneByHostSlug`. Regression on Pitfall 3.
    - A5: Sweep emits an app line with `schema_version: 999`. Assert the batch path abandons this connection and latches `sweepSchemaMismatchThisConnection` (same behavior as identity schema mismatch — verified by the parser's `schemaMismatch` flag flip; the exact orchestrator response should match how the existing schema-mismatch case is handled).
  </behavior>
  <action>
    Modify `src/backend/fleet-status/ssh-poll-orchestrator.ts` — surgical additions, DO NOT refactor or reformat existing code.

    (a) PERHOSTSTATE FIELD — locate `PerHostState` interface (L426-486) and specifically the `lastTickLiveTreeIdentities` field declaration (L476-485). Immediately below it (after L485), add a new `lastTickLiveApps: Set<string>` field. The docblock preceding the field must state: this is the per-host reconciliation set of app slugs seen in the last successful sweep tick; populated at the end of each successful sweep with EVERY slug that entered the picture (healthy OR unhealthy per D-02 carve-out — NOT filtered to healthy-only, otherwise unhealthy apps would gone-and-re-add every tick, defeating D-13); diff'd against previous tick on next success to fire `publishAppGoneByHostSlug` for silent removals (folder deleted, unit removed by `archive-app.sh`, etc.); reconciliation ONLY runs on sweep success per D-12 (the `{ok:false}` early returns above in `pollOneHostBatch` never reach the reconciliation block, so transient SSH failures don't flap the sidebar); mirrors Phase 115's `lastTickLiveTreeIdentities` discipline verbatim.

    (b) INIT SITE — locate the per-host state creation block (L2994-3016; specifically L3004-3008 where `lastTickLiveTreeIdentities: new Set<string>()` is set). Immediately after that line, add `lastTickLiveApps: new Set<string>(),` with a short docblock comment stating: initialized empty so the FIRST successful sweep for this host publishes nothing gone (no previous tick to diff against); subsequent ticks compare against this and emit `publishAppGoneByHostSlug` for anything that dropped from the picture.

    (c) ADAPTER HELPER `adaptAppLineToState` — add a small module-level helper (near the identity adapter functions if any exist — or at top-of-file in a helpers section). Signature: `function adaptAppLineToState(hostId: string, line: SweepAppLine): AppState`. Body: return an object with fields `hostId` (from the arg), `slug` (from `line.slug`), `title` (from `line.title`), `description` (from `line.description`), `port` (from `line.port`), `hasIcon` (from `line.has_icon` — snake_case → camelCase), `createdAtMs` (from `line.created_at_ms`), `isHealthy` (from `line.is_healthy`), `healthMessage` (from `line.health_message`). This is a straight snake_case → camelCase field copy. NO validation (the wire-protocol Zod schema in Plan 118-03 is the runtime gate; the adapter is a pure transformation). NO defensive undefined checks (if the Python emits an incomplete line, Zod at wire time will reject — the orchestrator is not the validation layer).

    (d) IMPORTS — add `SweepAppLine` from `./sweep-schema` and `AppState` from `./wire-protocol` (match the file's existing extension convention — `.js` / `.ts` / extensionless — grep the top of the file).

    (e) COMPOSE-AND-PUBLISH LOOP — locate the identity compose-and-publish loop at L1760-1768 (the `for (const identityLine of parsed.identityLines) { ... }` structure). Immediately after that loop closes (before the identity reconciliation block at L1770), add a sibling `for (const appLine of parsed.appLines)` loop whose body calls `deps.registry.publishAppUpdate(host.id, adaptAppLineToState(host.id, appLine))`. Precede the loop with a short docblock explaining: compose-and-publish for source-C apps; the adapter is a pure field copy; `publishAppUpdate` handles fan-out. This loop is inside the sweep-SUCCESS scope. Any `{ok:false}` early-return above bypasses it — same guard the identity loop already benefits from.

    (f) RECONCILIATION BLOCK — locate the identity reconciliation block (L1770-1792). Immediately after L1792 (`hostState.lastTickLiveTreeIdentities = thisTickLiveTreeIdentities;`), add the sibling app reconciliation block. Structure: declare `const thisTickLiveApps = new Set<string>()`; iterate `parsed.appLines` calling `thisTickLiveApps.add(line.slug)` for every line (ALL slugs — healthy AND unhealthy, matching D-02 inclusion); iterate `hostState.lastTickLiveApps` calling `deps.registry.publishAppGoneByHostSlug(host.id, previousSlug)` for every slug NOT in `thisTickLiveApps`; finally assign `hostState.lastTickLiveApps = thisTickLiveApps`. Precede the block with a docblock stating: mirrors the identity reconciliation pattern for apps; any slug that was in the previous tick's picture but is NOT in this tick's picture (folder deleted, unit removed, ceased passing D-01 or D-02 for any reason) gets a `publishAppGoneByHostSlug` so the registry drops it from the cached apps map; load-bearing constraint (RESEARCH § Pitfall 3) — the tracking set holds ALL slugs in the picture, healthy OR unhealthy, NOT just healthy — otherwise D-13 (health-flip = update, not gone+add) breaks; runs ONLY on sweep success — the `{ok:false}` early returns above never reach here (same guard as identity reconciliation).

    (g) VERIFY GUARD PLACEMENT — after inserting (e) and (f), open the file at the top of `pollOneHostBatch` and trace every `return { ok: false, ... }` early-return path. Confirm ALL of them are ABOVE both the new compose-and-publish loop and the new reconciliation block. If any early-return sits below either new block, MOVE the new blocks up so they sit below all early-returns (equivalently: the new blocks live in the same scope as the existing identity reconciliation, which is already correctly placed). Do NOT introduce a new early-return.

    (h) NO CHANGES to identity or pid handling. This plan is purely additive per PATTERNS § 5 delta.
  </action>
  <verify>
    <automated>npx vitest related --run src/backend/fleet-status/ssh-poll-orchestrator.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "lastTickLiveApps" src/backend/fleet-status/ssh-poll-orchestrator.ts` returns at least 4 (interface field declaration + init site + compose-block use + reconciliation-block use in the two loops).
    - `grep -c "adaptAppLineToState" src/backend/fleet-status/ssh-poll-orchestrator.ts` returns at least 2 (helper declaration + call site).
    - `grep -c "publishAppUpdate(host.id" src/backend/fleet-status/ssh-poll-orchestrator.ts` returns exactly 1.
    - `grep -c "publishAppGoneByHostSlug(host.id" src/backend/fleet-status/ssh-poll-orchestrator.ts` returns exactly 1.
    - `grep -c "parsed.appLines" src/backend/fleet-status/ssh-poll-orchestrator.ts` returns at least 2 (compose loop + reconciliation loop).
    - `npx tsc --noEmit` reports zero new type errors — proves the adapter output matches `AppState` exactly.
    - The new reconciliation block sits AFTER the identity reconciliation block (spatially: L1793+ after the current L1792 identity end). Verify: `awk '/lastTickLiveTreeIdentities = thisTickLiveTreeIdentities/{seen=1} seen && /lastTickLiveApps = thisTickLiveApps/{print NR; exit}' src/backend/fleet-status/ssh-poll-orchestrator.ts` prints a line number greater than the identity reconciliation end.
    - No `{ok:false}` early return has been added or moved (regression on the SSH-failure safety guard — visually check the `pollOneHostBatch` body ordering).
    - `git diff src/backend/fleet-status/ssh-poll-orchestrator.ts | grep -Ec '^-.*(publishSessionState|publishIdentity|lastTickLiveTreeIdentities)'` returns 0 (no removals from existing identity/session code paths).
  </acceptance_criteria>
  <done>
    - The four surgical additions (PerHostState field, init, adapter helper, compose+publish loop, reconciliation block) all landed.
    - Type errors: zero new.
    - Identity + pid + session code paths untouched.
    - Reconciliation sits inside sweep-success scope after the identity reconciliation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend MockRegistry + makeSweepJsonl + add 5 reconciliation tests</name>
  <files>src/backend/fleet-status/ssh-poll-orchestrator.test.ts</files>
  <read_first>
    - src/backend/fleet-status/ssh-poll-orchestrator.test.ts targeted ranges (this file is ~8860+ lines; use Grep to jump):
      - L200-276 — `MockRegistry` class — the seam to extend. L253-259 is the Phase 115 `publishIdentityGoneByName` stub — the exact template shape for the new app-scoped stubs.
      - L325-405 — `makeSweepJsonl(input)` fixture helper. Currently accepts `identities` + `pids` arrays; must extend to accept an `apps?` array.
      - L411-495 — `buildDeps(overrides)` — no changes needed, just knowing this is the way tests get a mock `deps` object with the mock registry attached.
      - L7965-8060 — the Phase 115 "Test P115-06 archive-routing-N" cases. These are the EXACT template for the new reconciliation tests. Read for: how a test wires the channel setResponse, starts the orchestrator, awaits a tick, asserts on `deps.registry.publishedXxx` arrays.
    - src/backend/fleet-status/subscription-registry.ts POST-118-03 — the `SubscriptionRegistry` interface with the new methods. `MockRegistry` must implement this interface.
    - src/backend/fleet-status/sweep-schema.ts POST-118-02 — the `SweepAppLine` interface. Needed to type the `makeSweepJsonl` `apps` parameter and the emitted line objects.
    - .planning/phases/118-first-class-apps-sweep-registry-shape-2/116-RESEARCH.md § Q7 test seams (MockRegistry pattern + makeSweepJsonl extension + Test A1-A5 recipes), § Q9 MockRegistry pattern gap (do NOT shoehorn app-scoped gone into `publishedGone`; use separate app arrays).
    - .planning/phases/118-first-class-apps-sweep-registry-shape-2/116-PATTERNS.md § Test Pattern Assignments → ssh-poll-orchestrator.test.ts (analogs + deltas verbatim).
    - .planning/phases/118-first-class-apps-sweep-registry-shape-2/116-CONTEXT.md § D-21 (four-layer test discipline — parse handled in 118-01/02, reconciliation is this plan).
  </read_first>
  <behavior>
    - A1: First-tick publishes updates, zero gone.
    - A2: Second tick with one app missing publishes exactly one gone.
    - A3: Sweep failure between two success ticks does NOT flap (zero gone across the sequence).
    - A4: Health flip → one publishAppUpdate on tick 2, zero publishAppGoneByHostSlug.
    - A5: Schema mismatch on app lines triggers batch fallback (latches `sweepSchemaMismatchThisConnection`).
  </behavior>
  <action>
    Modify `src/backend/fleet-status/ssh-poll-orchestrator.test.ts`:

    (a) EXTEND MockRegistry (~L205-276):
      - Add two public arrays: `publishedAppUpdates: Array<{ hostId: string; app: AppState }> = []` and `publishedAppGone: Array<{ hostId: string; slug: string }> = []`. Do NOT shoehorn into `publishedGone` (RESEARCH § Q9).
      - Add three stub methods matching the `SubscriptionRegistry` interface added by 118-03: `publishAppUpdate(hostId, app)` pushes `{ hostId, app }` into `publishedAppUpdates`; `publishAppGoneByHostSlug(hostId, slug)` pushes `{ hostId, slug }` into `publishedAppGone`; `getAppSnapshot()` returns an empty array (not exercised by these tests; safe stub).
      - Confirm `MockRegistry implements SubscriptionRegistry` still compiles (TypeScript will catch drift with the interface — this is the belt-and-braces guard that Phase 115's L253-259 comment mentions).

    (b) EXTEND `makeSweepJsonl` (~L339-405):
      - Widen the input parameter type to include `apps?: Array<Partial<SweepAppLine> & { slug: string }>` (following the existing pattern for `identities: Array<Partial<SweepIdentityLine> & { identity: string }>`).
      - After the existing identity + pid emission loops, add a loop over `input.apps ?? []`. For each raw entry, construct a `SweepAppLine` object with `line_kind: "app"`, `schema_version: version`, `slug: raw.slug`, and defaults-when-omitted for the other fields (`title` defaults to `"Test App ${raw.slug}"`, `description` defaults to `"Test app description"`, `port` defaults to `9591`, `has_icon` defaults to `false`, `created_at_ms` defaults to `1_700_000_000_000`, `is_healthy` defaults to `true`, `health_message` defaults to `null`). Push `JSON.stringify(line)` into the accumulator array. Preserve the existing default-when-omitted discipline the identity fixture uses.

    (c) NEW RECONCILIATION TESTS — model each on the Phase 115 "Test P115-06 archive-routing-N" pattern at L7965-8060. Use the same wireBatchProbe / setResponse / start-orchestrator / awaitTick / assert idiom. Number the tests P118-04-01..05 or per the file's existing convention (grep for "Test P115-06" naming to see the exact style).

      Test A1: `apps: [{ slug: "todo" }, { slug: "notes" }]` on the FIRST sweep response. Await one tick. Assert `deps.registry.publishedAppUpdates.length === 2` AND `deps.registry.publishedAppGone.length === 0`. Assert the two published `AppState` objects have `slug: "todo"` + `slug: "notes"` respectively.

      Test A2: Tick 1 sets response to `apps: [{ slug: "todo" }, { slug: "notes" }]`, await tick, then tick 2 sets response to `apps: [{ slug: "todo" }]`, await tick. Assert `deps.registry.publishedAppGone.length === 1` AND `deps.registry.publishedAppGone[0].slug === "notes"` (exactly one gone for "notes"). Assert `deps.registry.publishedAppUpdates` contains at least two entries with `slug: "todo"` (once per tick) — the update-set includes "todo" from both ticks because the compose-publish loop fires per tick regardless.

      Test A3: Tick 1: `apps: [{ slug: "todo" }]`, await tick. Tick 2: channel returns null / times out (use whatever mechanism the file's existing SSH-failure tests use — grep for `null` or `timeout` in the file's `channel.setResponse` idioms; the Phase 115 archive-routing tests likely have an example). Await tick. Tick 3: `apps: [{ slug: "todo" }]`, await tick. Assert `deps.registry.publishedAppGone.length === 0` across the entire sequence. Regression on RESEARCH § Pitfall 1 (transient SSH failures must NOT flap the sidebar).

      Test A4: Tick 1: `apps: [{ slug: "todo", is_healthy: true, health_message: null }]`, await tick. Tick 2: `apps: [{ slug: "todo", is_healthy: false, health_message: "not running — ask an agent to check on it" }]`, await tick. Assert on tick 2: `deps.registry.publishedAppGone.length === 0` AND exactly ONE `publishedAppUpdates` entry from tick 2 (filter by whichever tick-boundary marker the test uses; simplest is `deps.registry.publishedAppUpdates.length === 2` total, with the second entry having `app.isHealthy === false` and `app.healthMessage === "not running — ask an agent to check on it"`). Regression on Pitfall 3 (health-flip is UPDATE not gone+add).

      Test A5: Sweep response uses `makeSweepJsonl({ apps: [{ slug: "todo" }], schemaVersionOverride: 999 })`. Await tick. Assert the batch path abandons this connection — the exact assertion depends on how the file's existing schema-mismatch tests (Phase 92 territory — grep `schemaMismatch` or `sweepSchemaMismatchThisConnection`) assert it. Match that pattern.

    (d) NO CHANGES to existing tests, no reordering, no rewrites. Purely additive.
  </action>
  <verify>
    <automated>npx vitest related --run src/backend/fleet-status/ssh-poll-orchestrator.ts src/backend/fleet-status/ssh-poll-orchestrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest related --run src/backend/fleet-status/ssh-poll-orchestrator.ts src/backend/fleet-status/ssh-poll-orchestrator.test.ts` exits 0 with every test passing (regression guard on the ~8860 existing tests + the five new reconciliation tests).
    - `grep -c "publishedAppUpdates" src/backend/fleet-status/ssh-poll-orchestrator.test.ts` returns at least 4 (declaration + stub push + at least two test assertions).
    - `grep -c "publishedAppGone" src/backend/fleet-status/ssh-poll-orchestrator.test.ts` returns at least 4 (declaration + stub push + assertions in A1, A2, A3, A4).
    - `grep -c "apps: \[" src/backend/fleet-status/ssh-poll-orchestrator.test.ts` returns at least 5 (one per new test's makeSweepJsonl call).
    - `grep -c "not running — ask an agent to check on it" src/backend/fleet-status/ssh-poll-orchestrator.test.ts` returns at least 1 (the A4 health-flip assertion).
    - `MockRegistry implements SubscriptionRegistry` compiles — verified by TypeScript check inside vitest.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    - MockRegistry gains the two arrays + two stub methods + getAppSnapshot stub.
    - makeSweepJsonl gains the `apps?` parameter and its emission loop.
    - Five reconciliation tests A1-A5 present and passing.
    - All ~8860 existing tests still passing.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parsed `SweepAppLine` → `AppState` adapter | Fields flow from untrusted JSONL (parsed but not runtime-validated by the schema module) into the wire boundary. Zod validation at wire-protocol layer (Plan 118-03) is the runtime gate. |
| `pollOneHostBatch` success gate → reconciliation | The reconciliation block MUST NOT run on any `{ok:false}` path. A misplaced block could cause spurious app-gone flapping on transient SSH failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-04-RE | DoS / Availability | Reconciliation running on `{ok:false}` path (Pitfall 1) | mitigate | Reconciliation block sits INSIDE the sweep-success scope, AFTER all compose-publish loops, BEFORE `return { ok: true, ... }`. All `{ok:false}` early-returns above bypass it (same guard identity reconciliation gets by virtue of position). Test A3 exercises this: sweep failure between two success ticks emits ZERO gone calls. |
| T-118-04-HF | Correctness | Health-flip mis-tracked as gone-then-re-add (Pitfall 3) | mitigate | The tracking set `lastTickLiveApps` holds ALL slugs in the picture (healthy OR unhealthy per D-02), NOT filtered to healthy-only. Compose-publish loop over `parsed.appLines` publishes an update for EVERY line — including health-flipped ones. Test A4 exercises this: tick 2 with same slug + flipped health emits exactly one update, zero gone. |
| T-118-04-AD | Input Validation | Adapter passes malformed `AppState` to publishAppUpdate | mitigate | The Zod schema `AppStateSchema` in wire-protocol.ts (Plan 118-03) is the runtime validation gate. If the adapter's output would fail Zod validation, the fan-out serializes to JSON but the CLIENT rejects — no protocol corruption on the server. TypeScript type check on the adapter's return type (`AppState`) also catches most drift at compile time. |
| T-118-04-SP | Spoofing | Fake hostId in publishAppUpdate | mitigate | `host.id` comes from the batch-path per-host handler's outer context — the host being currently polled. Adapter uses `host.id` verbatim; no external caller supplies it. |
| T-118-04-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies. |
</threat_model>

<verification>
- `npx vitest related --run src/backend/fleet-status/ssh-poll-orchestrator.ts src/backend/fleet-status/ssh-poll-orchestrator.test.ts` exits 0.
- `npx tsc --noEmit` exits 0.
- The five reconciliation tests A1-A5 exist and pass.
- The identity reconciliation block and identity compose-publish loop are byte-for-byte unchanged (`git diff` shows additions only in the app-adjacent areas).
- No new `{ok:false}` early-return introduced (visual/manual review of `pollOneHostBatch` control flow).
- The sweep exec timeout (currently 8000ms per RESEARCH § Q5) is unchanged.
</verification>

<success_criteria>
- `PerHostState.lastTickLiveApps` field, its initializer, `adaptAppLineToState` helper, compose-and-publish loop, and reconciliation block all landed.
- Existing identity + pid + session code paths untouched.
- MockRegistry extension + makeSweepJsonl extension + five reconciliation tests all green.
- Plan 118-05 has a live publish pipeline to exercise its filter tests against.
- Deploy motion (Ashley-coordinated, per Standing Directive 3) can proceed after 118-05 lands — this plan alone is NOT deployable because it introduces unfiltered app-frame fan-out (see T-118-03-IL disposition).
</success_criteria>

<output>
Create `.planning/phases/118-first-class-apps-sweep-registry-shape-2/118-04-SUMMARY.md` when done, capturing:
- The exact line numbers where the PerHostState field, init site, compose-publish loop, and reconciliation block landed (post-edit — verify by grep in the SUMMARY).
- Any RESEARCH.md line-number citations that had drifted (especially L1770-1792 and L2994-3016).
- Confirmation that the sweep exec timeout constant was unchanged (should be 8000ms).
- Confirmation that all `{ok:false}` early-returns in `pollOneHostBatch` still sit ABOVE both the new compose-publish loop and the new reconciliation block.
- Any tests that had to be adjusted because MockRegistry's interface changed (any test constructing a `MockRegistry` and now needing the new methods — expected: none, because MockRegistry constructor is unchanged and the new methods are optional-like additions).
- Line count delta on both files.
</output>
