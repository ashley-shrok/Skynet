---
phase: 59-coral-drop-target-affordance-on-empty-prettyview-conv-list-c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/AppShell.tsx
  - src/ui/AppShell.empty-pv-drop-tint.test.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
autonomous: true
requirements: []  # N/A — UAT-derived followup patch #517 (bounty: drop-target-affordance-empty-pv-and-convlist-close). ROADMAP Requirements: TBD.
tags: [drag-drop, coral-overlay, empty-pv, conv-list, patch-517, phase-57-precedent, ui-affordance]

must_haves:
  truths:
    - "During a text/plain conv-list-row drag over the empty PrettyView area (splitTree === null), a coral tint overlay is visible in the drop target"
    - "During an application/x-skynet-badge drag over the PrettyConversationsPanel outermost div, a coral tint overlay is visible in the drop target"
    - "The coral overlay uses palette values verbatim from Phase 57's SplitView (fill rgba(255, 184, 150, 0.22) + border 2px solid rgba(255, 184, 150, 0.60))"
    - "The overlay clears immediately on drop, on dragleave that exits the container's bounding rect, and on window-level dragend (Escape-cancel)"
    - "The overlay does NOT appear for OS file drags (Files in dataTransfer.types) on either target"
    - "The overlay does NOT appear on the empty-PV target when splitTree !== null (Pane's own overlay handles that state — architectural exclusion)"
    - "The overlay does NOT appear on the conv-list panel target for application/x-skynet-row-only drags (row rearrange, not badge close)"
    - "The overlay is pointer-events:none — the underlying drop handler (empty-PV setSplitTree ladder / panel 6-step gauntlet) still fires normally on drop"
    - "Structured logs `[empty-pv-drop-preview] visible=<bool> splitTreeNull=<bool>` and `[convlist-drop-preview] visible=<bool>` fire once per state transition (zone-change-gated, NOT per-mouse-move)"
    - "The tint does NOT flicker when the cursor crosses a child DOM boundary inside the container (bounding-rect guard on dragleave)"
  artifacts:
    - path: "src/ui/AppShell.tsx"
      provides: "empty-PV drop wrapper with coral tint overlay + dragover/dragleave/drop/dragend state machinery"
      contains: "rgba(255, 184, 150, 0.22)"
    - path: "src/ui/AppShell.tsx"
      provides: "empty-PV drop wrapper with coral tint overlay + dragover/dragleave/drop/dragend state machinery"
      contains: "rgba(255, 184, 150, 0.60)"
    - path: "src/ui/AppShell.tsx"
      provides: "data-testid marker for the empty-PV drop-preview overlay"
      contains: "empty-pv-drop-preview"
    - path: "src/ui/AppShell.tsx"
      provides: "structured log prefix for empty-PV drop preview"
      contains: "[empty-pv-drop-preview]"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "conv-list panel drop wrapper with coral tint overlay + dragover/dragleave/drop/dragend state machinery"
      contains: "rgba(255, 184, 150, 0.22)"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "conv-list panel drop wrapper with coral tint overlay + dragover/dragleave/drop/dragend state machinery"
      contains: "rgba(255, 184, 150, 0.60)"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "data-testid marker for the conv-list drop-preview overlay"
      contains: "convlist-drop-preview"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "structured log prefix for conv-list drop preview"
      contains: "[convlist-drop-preview]"
    - path: "src/ui/AppShell.empty-pv-drop-tint.test.tsx"
      provides: "8 scaffold-based tests covering Gap 1 tint states (A-H per CONTEXT.md §Test coverage)"
      contains: "empty-pv-drop-preview"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx"
      provides: "8 new tests covering Gap 2 tint states (A-H per CONTEXT.md §Test coverage)"
      contains: "convlist-drop-preview"
  key_links:
    - from: "src/ui/AppShell.tsx (empty-PV wrapper onDragOver at :2260)"
      to: "isConvRowDragOver state setter"
      via: "React synthetic onDragOver handler extension (post-type-gate)"
      pattern: "setIsConvRowDragOver\\(true\\)"
    - from: "src/ui/AppShell.tsx (empty-PV wrapper new onDragLeave)"
      to: "isConvRowDragOver state clearer"
      via: "bounding-rect stateless guard (mirror SplitView.tsx:286-313)"
      pattern: "getBoundingClientRect\\(\\)"
    - from: "src/ui/AppShell.tsx (window-level dragend useEffect)"
      to: "isConvRowDragOver state clearer"
      via: "window.addEventListener('dragend', …) with cleanup (mirror SplitView.tsx:378-381)"
      pattern: "addEventListener\\(.dragend."
    - from: "src/ui/AppShell.tsx (overlay JSX render gate)"
      to: "splitTree === null architectural exclusion"
      via: "&& splitTree === null in render gate"
      pattern: "splitTree === null"
    - from: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (handlePanelDragOver extension)"
      to: "isBadgeDragOver state setter"
      via: "React synthetic onDragOver extension (post application/x-skynet-badge type-gate)"
      pattern: "setIsBadgeDragOver\\(true\\)"
    - from: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (new handlePanelDragLeave)"
      to: "isBadgeDragOver state clearer"
      via: "bounding-rect stateless guard, type-gated to badge MIME"
      pattern: "getBoundingClientRect\\(\\)"
    - from: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (window-level dragend useEffect)"
      to: "isBadgeDragOver state clearer"
      via: "window.addEventListener('dragend', …) with cleanup"
      pattern: "addEventListener\\(.dragend."
---

<objective>
Add coral drop-target-affordance tint overlays to two symmetric drop targets that currently accept drops silently (no visual feedback):

1. **Gap 1 — empty PrettyView area** (`src/ui/AppShell.tsx:2258-2379` wrapper, `splitTree === null` state): tint on text/plain conv-list-row dragover.
2. **Gap 2 — conv-list panel drop-to-close** (`src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx:1358-1364` wrapper): tint on application/x-skynet-badge dragover.

Both gaps: apply Phase 57's SplitView coral overlay pattern verbatim — same palette (`rgba(255, 184, 150, 0.22)` fill + `2px solid rgba(255, 184, 150, 0.60)` border), same state-clear discipline (drop → clear, bounding-rect-guarded dragleave → clear, window-level dragend → clear for Escape-cancel), same zone-change-gated structured-log discipline (`[empty-pv-drop-preview]` and `[convlist-drop-preview]`).

Purpose: The panel drop-to-close is a **destructive** gesture (drops a badge → closes the session, tears down the WS, removes the tab). Ashley UAT 2026-08-28 verbatim: *"when I'm hovering over the conversation list, and I have her badge in hand, there's no tint or anything. So it doesn't necessarily seem interactable."* A silent destructive drop target erodes trust — the tint IS the "yes, dropping here will do something" signal. The empty-PV gap is a smaller papercut but ships in the same patch #517 for visual-language consistency with Phase 57's Pane preview.

Output:
- `src/ui/AppShell.tsx` — additive extension of existing empty-PV drop wrapper (new state, extended onDragOver, new onDragLeave, useEffect for window dragend, sibling overlay div gated by `isConvRowDragOver && splitTree === null`).
- `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` — additive extension of existing panel drop handlers (new state, extended handlePanelDragOver, new handlePanelDragLeave, useEffect for window dragend, sibling overlay div gated by `isBadgeDragOver`).
- `src/ui/AppShell.empty-pv-drop-tint.test.tsx` — NEW dedicated scaffold-based test file (mirrors `AppShell.split-tree.test.tsx` MechanismScaffold pattern) with 8 tests (A-H).
- `src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` — 8 new tests (A-H) appended to the existing Phase 58 conv-list drop suite.

Ships as ONE commit per task (2 commits total) inside patch #517. Deploy/full-suite/coord-room work is orchestrator-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/59-coral-drop-target-affordance-on-empty-prettyview-conv-list-c/59-CONTEXT.md
@.planning/phases/57-drop-preview-overlay-edge-zone-hit-testing-replace-placehold/57-CONTEXT.md
@.planning/phases/58-identity-badge-drag-as-third-gesture-press-and-drag-rearrang/58-CONTEXT.md

# Phase 57 reference implementation — palette source of truth, state pattern,
# bounding-rect flicker guard, window-level dragend cleanup, zone-change-gated log
@src/ui/shell/SplitView.tsx

# Gap 1 target site (empty-PV wrapper at :2258-2379, existing patch #510+#514 mechanics)
@src/ui/AppShell.tsx

# Gap 2 target site (conv-list panel wrapper at :1358-1364, existing Phase 58 Plan 02 mechanics)
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx

# Test scaffolding references
@src/ui/AppShell.split-tree.test.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gap 1 — coral tint overlay on empty PrettyView drop target (AppShell.tsx)</name>
  <files>src/ui/AppShell.tsx, src/ui/AppShell.empty-pv-drop-tint.test.tsx</files>

  <read_first>
    - src/ui/AppShell.tsx (READ :2240-2490 in a single pass — the empty-PV wrapper you are modifying; also skim :1-100 for imports/hooks currently available at file scope so you know whether useState/useEffect are already imported, and grep for `setSplitTree`/`splitTree` local-state declaration to see where the state lives that the overlay's render gate reads)
    - src/ui/shell/SplitView.tsx (READ :200-460 in a single pass — this IS the reference implementation you are mirroring: state pattern, dragover with type-gate + preventDefault + setState-with-bailout, dragleave with type-gate FIRST + bounding-rect stateless guard + ref reset, drop with clear-state-immediately + stopPropagation, window-level dragend cleanup useEffect, sibling overlay div with palette values inline, zone-change-gated structured log via prevZoneRef. The whole Pane inner function is your pattern. Ignore the edge-zone geometry — Gap 1 is single-zone boolean, NOT 4-edge, so you skip computeEdgeZone / overlayGeometryForZone entirely.)
    - .planning/phases/59-coral-drop-target-affordance-on-empty-prettyview-conv-list-c/59-CONTEXT.md (READ the full file — locked palette, MIME discriminators, edge cases, and especially §Existing code / Portal caveat for AppShell.tsx explaining why React synthetic listeners are safe on THIS wrapper even though Pane needed native DOM listeners)
    - src/ui/AppShell.split-tree.test.tsx (READ :1-350 in a single pass — this is the MechanismScaffold pattern authorized as the fallback for AppShell testing. Your new test file mirrors this shape: a minimal component that reproduces the empty-PV wrapper's drop-target mechanism in isolation. Reproduce the imports pattern, the beforeEach cleanup, the scaffold-component-with-registerHandle idiom.)
  </read_first>

  <behavior>
    Test A (in new `AppShell.empty-pv-drop-tint.test.tsx`): dispatch `dragOver` on the empty-PV wrapper scaffold with `dataTransfer.types = ['text/plain']` when `splitTree === null` → an element with `data-testid="empty-pv-drop-preview"` is present in the DOM after the event.
    Test B: dispatch `dragOver` with `dataTransfer.types = ['Files']` (with or without text/plain also present — mirror the existing AppShell.tsx:2272 guard) → NO `data-testid="empty-pv-drop-preview"` element rendered.
    Test C: hydrate scaffold with a non-null splitTree, then dispatch `dragOver` with `dataTransfer.types = ['text/plain']` → NO `data-testid="empty-pv-drop-preview"` element rendered (splitTree !== null gate excludes it; Pane's own overlay owns that state per Phase 57).
    Test D: dispatch `dragOver` then `drop` (both with text/plain) → overlay is present after dragOver, ABSENT after drop (drop clears state immediately, mirroring SplitView.tsx:323).
    Test E: dispatch `dragOver` with text/plain, then dispatch `dragend` on `window` → overlay is present after dragOver, ABSENT after window-level dragend (Escape-cancel path, mirroring SplitView.tsx:378-381).
    Test F: dispatch `dragOver` with text/plain, then dispatch `dragLeave` on the wrapper with `clientX/clientY` INSIDE the wrapper's bounding rect (simulate a child-boundary crossing) → overlay STAYS present (bounding-rect guard prevents flicker). Achieve this by stubbing `getBoundingClientRect` on the wrapper to return a known rect, then firing `dragLeave` with coordinates inside that rect.
    Test G: dispatch `dragOver` with text/plain, then dispatch `dragLeave` with `clientX/clientY` OUTSIDE the wrapper's bounding rect → overlay is ABSENT (bounding-rect guard permits real exit).
    Test H: dispatch `dragOver` with text/plain, then dispatch `dragOver` AGAIN (no zone change since single-zone boolean) → `console.info` spy sees `[empty-pv-drop-preview] visible=true splitTreeNull=true` called EXACTLY ONCE (zone-change gate — the second identical dragover does NOT re-log). Then dispatch `drop` → an ADDITIONAL `[empty-pv-drop-preview] visible=false splitTreeNull=true` log fires (total 2 calls).
  </behavior>

  <action>
Modify the empty-PV drop wrapper at `src/ui/AppShell.tsx:2258-2379`. All changes are ADDITIVE — do NOT change the existing payload-resolution ladder (:2314-2331), the `setSplitTree` block (:2352-2372), the `selectConversationDeferred` call (:2377), the `setFocusedTabId` call (:2378), or either type-gate return-early (:2271-2272 / :2276-2277). The existing patch #510 + #514 mechanics stay identical.

**Step 1 — Add local state.** Above the wrapper's JSX return (i.e. in AppShell's component body, near the other useState declarations), add:
```
const [isConvRowDragOver, setIsConvRowDragOver] = useState(false);
const prevEmptyPvVisibleRef = useRef<boolean | null>(null);
```
Rationale: ref-based zone-change gate mirrors `SplitView.tsx:223` `prevZoneRef` — synchronous ref-write inside the handler body avoids React 18 strict-mode double-fire from the setState functional-updater. `null` initial (not `false`) so the first `false → false` transition (impossible in practice but defensive) doesn't emit a spurious first log.

**Step 2 — Extend the existing `onDragOver` at :2260-2274.** AFTER the existing `if (!e.dataTransfer.types.includes("text/plain")) return;` / `if (e.dataTransfer.types.includes("Files")) return;` / `e.preventDefault();` block, add the tint-state block:
```
// Phase 59 Gap 1 tint state — post type-gate so Files drags never trigger.
setIsConvRowDragOver(true);
// Zone-change gate for structured log. Ref-write is atomic with log emission
// (mirror SplitView.tsx:271-277). splitTreeNull captured at gate time.
const splitTreeNull = splitTree === null;
if (prevEmptyPvVisibleRef.current !== true) {
  // eslint-disable-next-line no-console
  console.info(`[empty-pv-drop-preview] visible=true splitTreeNull=${splitTreeNull}`);
  prevEmptyPvVisibleRef.current = true;
}
```
Rationale: setState is set unconditionally (React bails on identity, so setting true when already true is a no-op — safe). The log gate uses the ref for the actual emission-suppression discipline.

**Step 3 — Add a NEW `onDragLeave` handler on the wrapper div.** Add `onDragLeave={(e) => { … }}` alongside the existing `onDragOver` and `onDrop`. Body:
```
// Type-gate FIRST — scoped to text/plain drags only (mirror SplitView.tsx:292).
// Unrelated dragleaves (browser file drags, native OS drags) never clear tint state.
if (!e.dataTransfer.types.includes("text/plain")) return;
const rect = e.currentTarget.getBoundingClientRect();
// Bounding-rect stateless guard (mirror SplitView.tsx:301-305) — robust
// against dragleaves fired when the cursor crosses child DOM boundaries.
const stillInside =
  e.clientX >= rect.left &&
  e.clientX <= rect.right &&
  e.clientY >= rect.top &&
  e.clientY <= rect.bottom;
if (stillInside) return;
setIsConvRowDragOver(false);
if (prevEmptyPvVisibleRef.current !== false) {
  // eslint-disable-next-line no-console
  console.info(`[empty-pv-drop-preview] visible=false splitTreeNull=${splitTree === null}`);
  prevEmptyPvVisibleRef.current = false;
}
```

**Step 4 — Extend the existing `onDrop` at :2275-2379.** At the VERY TOP of the handler body — BEFORE the existing splitTree-non-null skip-log at :2293-2299, and BEFORE the payload resolution — add the state-clear block (mirror SplitView.tsx:323-324 pattern: drop always clears state immediately, even for the skip path):
```
// Phase 59 Gap 1 tint clear — clear FIRST regardless of downstream branches.
setIsConvRowDragOver(false);
if (prevEmptyPvVisibleRef.current !== false) {
  // eslint-disable-next-line no-console
  console.info(`[empty-pv-drop-preview] visible=false splitTreeNull=${splitTree === null}`);
  prevEmptyPvVisibleRef.current = false;
}
```
Placement rationale: before the type-gate return-earlys is intentional — even a non-text/plain drop that reaches this handler should clear tint state (defensive; type-gates should have prevented tint from ever setting true, but idempotent clear is safe).

**Step 5 — Add a `useEffect` for window-level `dragend` cleanup.** Place near AppShell's other top-level effects. Mirror `SplitView.tsx:378-381`:
```
// Phase 59 Gap 1 — window-level dragend for Escape-cancel path. Escape
// cancels a drag WITHOUT moving the cursor, so no dragleave fires; dragend
// on the drag source is the only reliable signal. Idempotent — clearing
// already-false state is a no-op.
useEffect(() => {
  const onDragEnd = () => {
    setIsConvRowDragOver(false);
    if (prevEmptyPvVisibleRef.current !== false) {
      // eslint-disable-next-line no-console
      console.info(`[empty-pv-drop-preview] visible=false splitTreeNull=${splitTree === null}`);
      prevEmptyPvVisibleRef.current = false;
    }
  };
  window.addEventListener("dragend", onDragEnd);
  return () => window.removeEventListener("dragend", onDragEnd);
}, [splitTree]);
```
Note the `[splitTree]` dep — the log emits current splitTree state, so the closure must re-bind when splitTree changes. If lint pushes back on the ref-inside-effect pattern, the eslint-disable comment for exhaustive-deps is acceptable per Phase 57 precedent.

**Step 6 — Add the overlay JSX sibling.** Inside the empty-PV wrapper div (the `<div className="relative flex flex-col flex-1 min-h-0 overflow-hidden" onDragOver={…} onDrop={…} onDragLeave={…}>` at :2258), as a FIRST child (before the `<div className="flex flex-col flex-1 min-w-0 min-h-0 overflow-hidden">` at :2249… wait, the wrapper CONTAINS that div at :2249; the wrapper starts at :2258 and its first current child is at :2380 `<div className="absolute inset-0" style={{ display: hasSplit ? "flex" : "none" }}>`). Insert the overlay as sibling to `!isMobile && (<div className="absolute inset-0">…SplitView…)` at :2382 and the normal-view container at :2428. Add BEFORE both, as sibling. Overlay div:
```
{isConvRowDragOver && splitTree === null && (
  <div
    data-testid="empty-pv-drop-preview"
    className="absolute inset-0 pointer-events-none"
    style={{
      background: "rgba(255, 184, 150, 0.22)",
      border: "2px solid rgba(255, 184, 150, 0.60)",
      zIndex: 30,
      transition: "opacity 120ms ease",
    }}
  />
)}
```
Palette values inline verbatim from `SplitView.tsx:446-447` — NEVER refactor to a CSS custom property in this phase (out of scope per CONTEXT.md §Out of scope). `pointer-events-none` is LOAD-BEARING — the overlay must not intercept drops; drop still fires on the underlying wrapper. `zIndex: 30` sits above the normalViewRef's `zIndex: 10` at :2439-2444 but below the mobile-back-chevron at z-30 (they don't co-occupy this container). Render gate `isConvRowDragOver && splitTree === null` enforces the architectural exclusion (edge case #4 + #6 in CONTEXT.md): the tint disappears the moment splitTree becomes non-null even if state hasn't been cleared, deferring to the Pane's own overlay.

**Step 7 — Create `src/ui/AppShell.empty-pv-drop-tint.test.tsx`.** NEW file. Mirror `AppShell.split-tree.test.tsx` MechanismScaffold pattern: build a minimal component that reproduces JUST the empty-PV wrapper's drop-target mechanism (state + handlers + overlay JSX + window dragend effect) so tests don't have to mount the full AppShell with its ~30 imports. The scaffold must expose:
- A `simulateSplitTreeNonNull` boolean prop (Tests C requires splitTree !== null).
- A `data-testid="empty-pv-drop-wrapper"` on the outer wrapper so tests can dispatch events on it.
- The exact overlay JSX with `data-testid="empty-pv-drop-preview"` and the render gate `isTintActive && splitTreeNull` matching the AppShell code.
- The same handler bodies from Steps 2-5 above.

Write the 8 tests (A-H) per the `<behavior>` block. Use `fireEvent.dragOver`, `fireEvent.dragLeave`, `fireEvent.drop` from `@testing-library/react` — the `AppShell.split-tree.test.tsx` file uses `render` + `act` from the same library. For window-level dragend (Test E), use `fireEvent(window, new Event("dragend", { bubbles: true }))` or `window.dispatchEvent(new Event("dragend"))` inside `act(() => …)`. For DataTransfer stubs, reuse the same Map-backed pattern from `PrettyConversationsPanel.test.tsx:4078-4090` (`makeConvListDataTransferStub`) — inline the helper OR extract it to a shared test-util module (executor decides; inlining is fine for a single test file). For the bounding-rect assertion (Tests F/G), stub `getBoundingClientRect` on the wrapper element via `HTMLElement.prototype.getBoundingClientRect = vi.fn().mockReturnValue({...})` per-test with `beforeEach`/`afterEach` restore, OR by directly overriding on the queried element via `Object.defineProperty`.

For the log-assertion tests (Test H), use `vi.spyOn(console, "info").mockImplementation(() => {})` at test start and restore in `afterEach` — same pattern as `PrettyConversationsPanel.test.tsx:4205`.

**Portal caveat resolution:** Per CONTEXT.md §Existing code, plan-phase verified during planning that the empty-PV wrapper contains the `tabs.map(createPortal(renderTabContent(...), tabNode, tab.id))` loop inline in its JSX (:2447-2484). Because the createPortal call is a React-tree child of the wrapper, React synthetic events DO bubble to the wrapper's onDragOver/onDrop — the same mechanism that has been working since patch #510. React synthetic listeners are SAFE here; do NOT convert to native DOM listeners. (This is different from the Pane case in patch #514, where the portal call was in AppShell but the portal-target was in Pane — React-tree parentage did NOT match DOM-tree parentage there. Here, both align.)

**Structured log discipline (per box-maintainer directive Ashley 2026-08-11):** Both log call-sites use `console.info` (mirror Phase 57's `SplitView.tsx:273`). Prefix is `[empty-pv-drop-preview]` verbatim. Format is `visible=<bool> splitTreeNull=<bool>` — no JSON.stringify, no positional args, no template-literal-with-object-interpolation. Ref-gated so a mouse hovering the wrapper does NOT flood the console-forward log with per-mouse-move noise.

**Rebase risk / coordination:** AppShell.tsx sees heavy cross-fleet activity per CONTEXT.md §Vehicle notes. Touch surface is localized to :2258-2486 (wrapper) + a new useEffect near existing effects + `useState` + `useRef` additions near existing state — additive on existing wiring. Standard `git pull --rebase` + coord-room BEFORE/AFTER motion at ship time (orchestrator handles ship; executor commits the work).

**Explicitly OUT of scope for this task:** (a) any change to Phase 57 Pane drop-preview or edge-zone hit-testing at SplitView.tsx:439-454; (b) any change to patch #510 / #514 mechanics (payload resolution ladder, setSplitTree, splitTree non-null skip-log); (c) any change to the coral palette values themselves; (d) refactoring to CSS custom properties; (e) animation timing beyond a 120ms opacity transition; (f) mobile handling (empty-PV drop is desktop-only per patch #510).
  </action>

  <verify>
    <automated>cd /home/ubuntu/skynet-tanya &amp;&amp; npx vitest run src/ui/AppShell.empty-pv-drop-tint.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - Source: `grep -c 'rgba(255, 184, 150, 0.22)' src/ui/AppShell.tsx` outputs a count ≥ 1 (verbatim palette fill inside the new overlay).
    - Source: `grep -c 'rgba(255, 184, 150, 0.60)' src/ui/AppShell.tsx` outputs a count ≥ 1 (verbatim palette border inside the new overlay).
    - Source: `grep -c 'data-testid="empty-pv-drop-preview"' src/ui/AppShell.tsx` outputs a count = 1 (single overlay JSX site).
    - Source: `grep -v '^\s*//' src/ui/AppShell.tsx | grep -c '\[empty-pv-drop-preview\]'` outputs a count ≥ 3 (log call in onDragOver, onDragLeave, onDrop, plus useEffect for window dragend — at least three code call-sites; the `-v '^\s*//'` filter excludes single-line JS comment prose and prevents header-comment drift from counting).
    - Source: `grep -c 'window.addEventListener("dragend"' src/ui/AppShell.tsx` outputs a count ≥ 1 (window-level dragend cleanup useEffect present).
    - Source: `grep -c 'setIsConvRowDragOver' src/ui/AppShell.tsx` outputs a count ≥ 4 (set-true on dragover, set-false on dragleave, set-false on drop, set-false in window dragend).
    - Source: `grep -c 'pointer-events-none' src/ui/AppShell.tsx | head -1` — the count is ≥ 1 and specifically the new overlay div includes `pointer-events-none` (verify by inspection of the overlay JSX block).
    - Source: `grep -B2 'data-testid="empty-pv-drop-preview"' src/ui/AppShell.tsx` shows the render gate line contains BOTH `isConvRowDragOver` AND `splitTree === null` (architectural exclusion enforced).
    - Test: `npx vitest run src/ui/AppShell.empty-pv-drop-tint.test.tsx` exits 0 with 8/8 tests passing (Tests A-H per `<behavior>`).
    - Test: `npx vitest run src/ui/AppShell.split-tree.test.tsx` exits 0 with the existing test count unchanged (no regression in the sibling scaffold — the empty-PV wrapper additions must not break existing split-tree hydration or persistence behavior).
    - Behavior (asserted inside Test A of the new test file): dispatching `dragOver` with `dataTransfer.types = ['text/plain']` on the wrapper causes a query for `data-testid="empty-pv-drop-preview"` to return a non-null element.
    - Behavior (asserted inside Test H): the `console.info` spy sees `[empty-pv-drop-preview] visible=true splitTreeNull=true` exactly once for two consecutive identical dragover events (zone-change gate).
    - CLI: `git status --porcelain -- src/ui/AppShell.tsx src/ui/AppShell.empty-pv-drop-tint.test.tsx` shows both files as modified/added post-work.
    - Regression: `npx vitest run src/ui/AppShell.persistence.test.tsx` exits 0 with no regressions (mount-lifecycle contract intact).
  </acceptance_criteria>

  <done>
    Gap 1 shipped as one atomic commit: (a) AppShell.tsx empty-PV wrapper carries new useState + useRef + extended onDragOver + new onDragLeave + extended onDrop + window-dragend useEffect + sibling overlay div gated by `isConvRowDragOver && splitTree === null`; (b) new `AppShell.empty-pv-drop-tint.test.tsx` scaffold-based test file with 8 passing tests (A-H); (c) grep gates for palette values, testid, and log prefix all pass; (d) sibling `AppShell.split-tree.test.tsx` and `AppShell.persistence.test.tsx` still pass (no regression). Commit message: `feat(59-01): coral tint overlay on empty PrettyView drop target (Gap 1) + AppShell.empty-pv-drop-tint.test.tsx (Tests A-H)`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gap 2 — coral tint overlay on conv-list panel drop-to-close (PrettyConversationsPanel.tsx)</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx, src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx</files>

  <read_first>
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (READ :1240-1400 in a single pass — the existing Phase 58 Plan 02 wiring you are extending: `handlePanelDragOver` at :1294-1303, `handlePanelDrop` at :1304-1338, panel root at :1358-1364 with `onDragOver={handlePanelDragOver}` / `onDrop={handlePanelDrop}`. Also skim :1-80 for the imports currently in scope (useState / useEffect / useRef availability) so you know whether new imports are needed.)
    - src/ui/shell/SplitView.tsx (READ :200-460 in a single pass — same Phase 57 reference implementation as Task 1: state pattern, bounding-rect dragleave guard, window-level dragend cleanup, zone-change-gated log via prevZoneRef, palette values inline in the overlay JSX at :446-447. Gap 2 is single-zone boolean, NOT 4-edge — skip computeEdgeZone / overlayGeometryForZone.)
    - .planning/phases/59-coral-drop-target-affordance-on-empty-prettyview-conv-list-c/59-CONTEXT.md (READ the full file — locked palette, MIME discriminator `application/x-skynet-badge`, edge cases especially §Edge case #1 flicker guard + §Edge case #2 Escape-cancel + §Edge case #3 non-matching drag types)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx (READ :4055-4398 in a single pass — the existing Phase 58 Plan 02 conv-list drop test suite. Your new tests A-H append to this file. Reuse the `makeConvListDataTransferStub` helper at :4078-4090. Follow the same describe-block idiom.)
  </read_first>

  <behavior>
    Test A (appended to `PrettyConversationsPanel.test.tsx`): render PrettyConversationsPanel, dispatch `dragOver` on `[data-testid="pretty-conversations-panel"]` with `dataTransfer.types = ['application/x-skynet-badge']` → element with `data-testid="convlist-drop-preview"` is present.
    Test B: dispatch `dragOver` with `dataTransfer.types = ['application/x-skynet-row']` (stray row drag; the Phase 56 row payload) → NO `data-testid="convlist-drop-preview"` element (row drags do NOT trigger badge tint).
    Test C: dispatch `dragOver` with `dataTransfer.types = ['Files']` → NO `data-testid="convlist-drop-preview"` element (OS file drags never trigger).
    Test D: dispatch `dragOver` with badge MIME, then `drop` with the full Phase 58 Plan 02 valid-badge payload → overlay is present after dragOver, ABSENT after drop, AND the existing `onCloseSession` callback still fires (regression assertion — the 6-step validation gauntlet is not perturbed by the new tint state).
    Test E: dispatch `dragOver` with badge MIME, then dispatch `dragend` on `window` → overlay is present after dragOver, ABSENT after window-level dragend (Escape-cancel).
    Test F: dispatch `dragOver` with badge MIME, then `dragLeave` on the panel with `clientX/clientY` INSIDE the panel's bounding rect (child row-boundary crossing simulation) → overlay STAYS present.
    Test G: dispatch `dragOver` with badge MIME, then `dragLeave` with `clientX/clientY` OUTSIDE the bounding rect → overlay ABSENT.
    Test H: dispatch `dragOver` with badge MIME, then `dragOver` AGAIN with badge MIME → `console.info` spy sees `[convlist-drop-preview] visible=true` EXACTLY ONCE (zone-change gate). Then dispatch `drop` → an ADDITIONAL `[convlist-drop-preview] visible=false` log fires (total 2 calls with this prefix).
  </behavior>

  <action>
Modify the conv-list panel drop handlers at `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx:1240-1400`. All changes are ADDITIVE — do NOT change the existing 6-step validation gauntlet in `handlePanelDrop` at :1304-1337 (MIME read → JSON.parse → tabId shape check → openTabIds guard → preventDefault → structured [convlist-drop] log → onCloseSession callback). The existing Phase 58 Plan 02 mechanics stay identical.

**Step 1 — Add local state.** Above `handlePanelDragOver` at :1294 (i.e. inside the PrettyConversationsPanel component body, near existing hooks), add:
```
const [isBadgeDragOver, setIsBadgeDragOver] = useState(false);
const prevConvlistVisibleRef = useRef<boolean | null>(null);
```
Rationale: same ref-based zone-change-gate pattern as Task 1, mirroring `SplitView.tsx:223`.

**Step 2 — Extend `handlePanelDragOver` at :1294-1303.** AFTER the existing `if (types && Array.from(types).indexOf("application/x-skynet-badge") !== -1) { e.preventDefault(); }` block (inside the same if-body OR immediately after the preventDefault), add the tint-state block:
```
setIsBadgeDragOver(true);
if (prevConvlistVisibleRef.current !== true) {
  // eslint-disable-next-line no-console
  console.info(`[convlist-drop-preview] visible=true`);
  prevConvlistVisibleRef.current = true;
}
```
Placement: INSIDE the type-gate if-body (post-preventDefault) so non-badge drags (row drags, OS file drags) NEVER trigger tint state — matches CONTEXT.md §Edge case #3 (non-matching drag types must return early without setting state true). Do NOT move the setState / log outside the type-gate.

**Step 3 — Add a NEW `handlePanelDragLeave` function** alongside `handlePanelDragOver` and `handlePanelDrop`:
```
const handlePanelDragLeave = (e: React.DragEvent<HTMLDivElement>) => {
  // Type-gate FIRST — scoped to badge drags only (mirror SplitView.tsx:292
  // pattern + PrettyConversationsPanel.tsx:1300 existing type-gate shape).
  // Unrelated dragleaves (row drags, OS file drags) never clear tint state.
  const types = e.dataTransfer?.types;
  if (!(types && Array.from(types).indexOf("application/x-skynet-badge") !== -1)) return;
  const rect = e.currentTarget.getBoundingClientRect();
  // Bounding-rect stateless guard (mirror SplitView.tsx:301-305) — robust
  // against dragleaves fired when the cursor crosses child row boundaries.
  const stillInside =
    e.clientX >= rect.left &&
    e.clientX <= rect.right &&
    e.clientY >= rect.top &&
    e.clientY <= rect.bottom;
  if (stillInside) return;
  setIsBadgeDragOver(false);
  if (prevConvlistVisibleRef.current !== false) {
    // eslint-disable-next-line no-console
    console.info(`[convlist-drop-preview] visible=false`);
    prevConvlistVisibleRef.current = false;
  }
};
```

**Step 4 — Extend `handlePanelDrop` at :1304-1338.** At the VERY TOP of the handler body — BEFORE Step 1 (`const raw = e.dataTransfer?.getData(...)`) — add the state-clear block:
```
// Phase 59 Gap 2 tint clear — clear FIRST regardless of downstream 6-step
// gauntlet outcome. Defensive: even a non-badge drop that reaches this
// handler (shouldn't be possible given the dragover type-gate) still
// clears state. Idempotent.
setIsBadgeDragOver(false);
if (prevConvlistVisibleRef.current !== false) {
  // eslint-disable-next-line no-console
  console.info(`[convlist-drop-preview] visible=false`);
  prevConvlistVisibleRef.current = false;
}
```
Placement rationale: BEFORE the existing `if (raw === "") return;` early-return so that even a non-badge drop clears tint state (defensive; the dragover type-gate should have prevented tint from setting true, but a race-condition safety net doesn't hurt). The existing 6-step gauntlet then runs unchanged.

**Step 5 — Add a `useEffect` for window-level `dragend` cleanup.** Place alongside the component's existing effects (or immediately below the new handler declarations):
```
// Phase 59 Gap 2 — window-level dragend for Escape-cancel path. Escape
// cancels a drag WITHOUT moving the cursor, so no dragleave fires; dragend
// on the drag source (IdentityBadge per Phase 58 Plan 01) is the only
// reliable signal. Idempotent — clearing already-false state is a no-op.
useEffect(() => {
  const onDragEnd = () => {
    setIsBadgeDragOver(false);
    if (prevConvlistVisibleRef.current !== false) {
      // eslint-disable-next-line no-console
      console.info(`[convlist-drop-preview] visible=false`);
      prevConvlistVisibleRef.current = false;
    }
  };
  window.addEventListener("dragend", onDragEnd);
  return () => window.removeEventListener("dragend", onDragEnd);
}, []);
```
Empty dep array is correct — the ref-write pattern doesn't need re-binding on any prop change (no closure over changing state like Task 1's splitTree).

**Step 6 — Wire `handlePanelDragLeave` onto the panel root at :1358-1364.** Add `onDragLeave={handlePanelDragLeave}` to the existing prop-list on the outermost `<div … data-testid="pretty-conversations-panel">`. Position adjacent to the existing `onDragOver` and `onDrop` for readability.

**Step 7 — Add the overlay JSX sibling.** As a FIRST child of the panel root `<div className="relative flex flex-col flex-1 min-h-0 overflow-hidden pb-[env(safe-area-inset-bottom)]" data-testid="pretty-conversations-panel" …>` at :1358 (i.e. BEFORE the `<div className="pv-panel-header shrink-0" …>` at :1378 header block), insert:
```
{isBadgeDragOver && (
  <div
    data-testid="convlist-drop-preview"
    className="absolute inset-0 pointer-events-none"
    style={{
      background: "rgba(255, 184, 150, 0.22)",
      border: "2px solid rgba(255, 184, 150, 0.60)",
      zIndex: 30,
      transition: "opacity 120ms ease",
    }}
  />
)}
```
Palette values inline verbatim from `SplitView.tsx:446-447`. `pointer-events-none` is LOAD-BEARING — the overlay must not intercept drops; drop fires on the underlying panel div. `zIndex: 30` sits above the panel's normal content stacking (safe default; adjust if UAT reveals a specific z-conflict with WeeklyUsageMeter or PrettyBountyCountBadge, but 30 matches Task 1 for consistency).

**Step 8 — Add 8 new tests to `PrettyConversationsPanel.test.tsx`.** APPEND a new `describe("PrettyConversationsPanel: Phase 59 — coral tint overlay on badge drop-to-close", () => { … })` block after the existing Phase 58 describe block (around :4398, before the final closing `});`). Reuse the `makeConvListDataTransferStub` helper — it's already in-scope in this file (defined at :4078). Follow the same idiom: `render(<PrettyConversationsPanel variant="desktop" onDeactivateRow={() => {}} onCloseSession={onCloseSession} openTabIds={["tab-tina-42"]} />)`, then `getByTestId("pretty-conversations-panel")`, then `fireEvent.dragOver(panel, { dataTransfer: dt })` / `fireEvent.drop(panel, …)` / `fireEvent.dragLeave(panel, …)`.

Write Tests A-H per `<behavior>`. Assertions:
- Presence: `expect(queryByTestId("convlist-drop-preview")).not.toBeNull()`.
- Absence: `expect(queryByTestId("convlist-drop-preview")).toBeNull()`.
- Regression (Test D): after drop, `expect(onCloseSession).toHaveBeenCalledWith("tab-tina-42")` — the existing 6-step gauntlet still fires.
- Log spy (Test H): `const infoSpy = vi.spyOn(console, "info").mockImplementation(() => {});`, then filter calls with `call[0].startsWith("[convlist-drop-preview] ")`, assert exactly 1 for the double-dragover and exactly 2 after adding a drop.
- Bounding-rect (Tests F/G): stub `getBoundingClientRect` on the panel element via `Object.defineProperty(panel, "getBoundingClientRect", { value: () => ({ left: 100, top: 100, right: 500, bottom: 500, x: 100, y: 100, width: 400, height: 400, toJSON() { return this; } }) })`, then fire `dragLeave` with `clientX: 300, clientY: 300` (inside) OR `clientX: 50, clientY: 50` (outside).
- Window dragend (Test E): `act(() => { window.dispatchEvent(new Event("dragend")); })`.

For DataTransfer with types-only (no getData needed) for dragover events, the `makeConvListDataTransferStub({ "application/x-skynet-badge": "" })` shape works — `types` getter reflects the Map keys. For Test C's Files gate, use `makeConvListDataTransferStub({ "Files": "" })`.

**Explicitly OUT of scope for this task:** (a) any change to the existing Phase 58 Plan 02 6-step validation gauntlet at :1304-1337; (b) any change to the existing `handlePanelDragOver` type-gate at :1300 (the preventDefault stays exactly as-is, only new state/log lines added inside the same if-body); (c) any change to the badge drag source at IdentityBadge.tsx; (d) any change to the coral palette values themselves; (e) refactoring to CSS custom properties.

**Rebase risk:** PrettyConversationsPanel.tsx was just modified by Phase 58 patch #516 (HEAD `cb80f673` per CONTEXT.md §Vehicle notes). Standard `git pull --rebase` + coord-room BEFORE/AFTER motion at ship time.
  </action>

  <verify>
    <automated>cd /home/ubuntu/skynet-tanya &amp;&amp; npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - Source: `grep -c 'rgba(255, 184, 150, 0.22)' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` outputs a count ≥ 1 (verbatim palette fill inside the new overlay).
    - Source: `grep -c 'rgba(255, 184, 150, 0.60)' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` outputs a count ≥ 1 (verbatim palette border inside the new overlay).
    - Source: `grep -c 'data-testid="convlist-drop-preview"' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` outputs a count = 1 (single overlay JSX site).
    - Source: `grep -v '^\s*//' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx | grep -c '\[convlist-drop-preview\]'` outputs a count ≥ 3 (log call-sites in handlePanelDragOver, handlePanelDragLeave, handlePanelDrop, plus useEffect for window dragend — at least three code call-sites; the `-v '^\s*//'` filter excludes single-line JS comment prose).
    - Source: `grep -c 'window.addEventListener("dragend"' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` outputs a count ≥ 1 (window-level dragend cleanup useEffect present).
    - Source: `grep -c 'setIsBadgeDragOver' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` outputs a count ≥ 4 (set-true in dragover, set-false in dragleave, set-false in drop, set-false in window dragend).
    - Source: `grep -c 'handlePanelDragLeave' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` outputs a count = 2 (function declaration + prop assignment on panel root).
    - Source: `grep -c 'onDragLeave={handlePanelDragLeave}' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` outputs a count = 1 (handler wired onto panel root JSX).
    - Source: `grep -B2 'data-testid="convlist-drop-preview"' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` shows the render gate line contains `isBadgeDragOver` (single-condition gate).
    - Source: The existing `handlePanelDrop` 6-step gauntlet is intact — `grep -c 'openTabIds.includes(tabId)' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` outputs a count ≥ 1 (T-58-02-01 security guard preserved) AND `grep -c '\[convlist-drop\] close tabId=' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` outputs a count ≥ 1 (Phase 58 structured log preserved).
    - Test: `npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` exits 0 with the pre-existing test count PLUS 8 new tests (A-H) all passing.
    - Test: The existing Phase 58 Plan 02 tests (A-G at :4093-4325) still pass unchanged — the tint additions must not perturb the drop gauntlet.
    - Behavior (asserted inside new Test A): dispatching `dragOver` with `dataTransfer.types = ['application/x-skynet-badge']` on the panel causes a query for `data-testid="convlist-drop-preview"` to return a non-null element.
    - Behavior (asserted inside new Test D — regression): after `dragOver` then `drop` with valid badge payload, both `queryByTestId("convlist-drop-preview")` returns null AND `onCloseSession` was called with the correct tabId (the tint clear does NOT short-circuit the gauntlet).
    - Behavior (asserted inside new Test H): the `console.info` spy sees `[convlist-drop-preview] visible=true` exactly once for two consecutive identical dragover events (zone-change gate).
    - CLI: `git status --porcelain -- src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` shows both files as modified post-work.
  </acceptance_criteria>

  <done>
    Gap 2 shipped as one atomic commit: (a) PrettyConversationsPanel.tsx carries new useState + useRef + extended handlePanelDragOver + new handlePanelDragLeave + extended handlePanelDrop + window-dragend useEffect + sibling overlay div gated by `isBadgeDragOver` + `onDragLeave={handlePanelDragLeave}` wired on panel root; (b) PrettyConversationsPanel.test.tsx has 8 new tests (A-H) appended in a new Phase 59 describe block, all passing; (c) grep gates for palette values, testid, log prefix, and gauntlet-preservation all pass; (d) existing Phase 58 Plan 02 tests (A-G) still pass unchanged. Commit message: `feat(59-01): coral tint overlay on conv-list panel drop-to-close (Gap 2) + PrettyConversationsPanel.test.tsx Phase 59 (Tests A-H)`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dataTransfer.types on empty-PV wrapper (Task 1) | Attacker-controlled MIME types in a drag payload could try to trigger the tint on unrelated drags. Existing type-gate at :2271-2272 (text/plain required, Files excluded) already blocks this AT THE DRAG-HANDLER LEVEL. New tint state is set POST-type-gate, so this boundary is already defended. |
| dataTransfer.types on conv-list panel (Task 2) | Same shape — existing type-gate at :1300 (application/x-skynet-badge required) already filters. New tint state is set INSIDE the type-gate if-body, so this boundary is already defended. |
| window-level dragend listener (both tasks) | Attaches to `window`, so ANY dragend fires the clear-state callback. Idempotent (clearing already-false state is a no-op) and effect-only (no state mutation cascading downstream, no user data touched). Low-value target — nothing to spoof / tamper. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01 | Spoofing | dataTransfer.types on empty-PV dragover | accept | Existing patch #510 type-gate at AppShell.tsx:2271-2272 (text/plain required, Files excluded) already filters. New tint code sits POST-gate — inherits the existing defense. No new attack surface. |
| T-59-02 | Spoofing | dataTransfer.types on conv-list panel dragover | accept | Existing Phase 58 Plan 02 type-gate at PrettyConversationsPanel.tsx:1300 (application/x-skynet-badge required) already filters. New tint code sits INSIDE the gate if-body — inherits the existing defense. No new attack surface. |
| T-59-03 | Tampering | Overlay intercepts drop event, preventing underlying handler | mitigate | Overlay is `pointer-events-none` (LOAD-BEARING) in BOTH tasks. Drop events fire on the underlying wrapper/panel div. Test D on both gaps regression-asserts the underlying handler (setSplitTree / onCloseSession) STILL fires after drop. |
| T-59-04 | DoS (log flooding) | console.info emission per dragover event | mitigate | Zone-change-gate via `prevEmptyPvVisibleRef` / `prevConvlistVisibleRef`. Logs fire ONCE per state transition (`false → true`, `true → false`). Per-mouse-move dragover events do NOT re-log. Test H on both gaps asserts exactly 1 log for two identical consecutive dragovers. Mirrors Phase 57's `prevZoneRef` discipline. |
| T-59-05 | Repudiation | Missing audit trail for drop-preview state transitions | mitigate | Structured log with fixed prefixes `[empty-pv-drop-preview]` / `[convlist-drop-preview]` and stable format (`visible=<bool>` + optional `splitTreeNull=<bool>` for Gap 1). Zone-change-gated but ALWAYS emits on real transitions — reliable audit for the console-forward log. |
| T-59-SC | Tampering | npm/pip/cargo installs | accept | NO new package installs in this phase. Only additive touches to existing source files + one new test file. No RESEARCH.md package legitimacy audit needed. |

**Package legitimacy audit:** N/A — this phase does not add any new dependencies. Zero-install phase.
</threat_model>

<verification>
Phase-level verification (executor completes both tasks, then runs):

1. **Scoped test suites both green:**
   - `npx vitest run src/ui/AppShell.empty-pv-drop-tint.test.tsx` — 8/8 passing.
   - `npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` — pre-existing count + 8 new tests, all passing.
2. **No regression in sibling suites for the two touched files:**
   - `npx vitest run src/ui/AppShell.split-tree.test.tsx` — unchanged count, all passing.
   - `npx vitest run src/ui/AppShell.persistence.test.tsx` — unchanged count, all passing.
3. **Palette + testid + log-prefix grep gates all pass** (per per-task `<acceptance_criteria>`).
4. **Two commits landed on the branch:**
   - `feat(59-01): coral tint overlay on empty PrettyView drop target (Gap 1) …`
   - `feat(59-01): coral tint overlay on conv-list panel drop-to-close (Gap 2) …`

Full-suite `npx vitest run` (all ~3092+ tests) is orchestrator-only per role directive — executor stops after scoped-green + commits. Docker build / deploy / skynet-patches.md entry / coord-room posts are orchestrator-only.
</verification>

<success_criteria>
- [ ] Task 1 code lands: AppShell.tsx empty-PV wrapper carries new tint state + handlers + overlay div.
- [ ] Task 1 tests land: new `AppShell.empty-pv-drop-tint.test.tsx` file with 8 passing tests (A-H).
- [ ] Task 2 code lands: PrettyConversationsPanel.tsx carries new tint state + handlers + overlay div + handlePanelDragLeave wired on root.
- [ ] Task 2 tests land: 8 new tests (A-H) appended to PrettyConversationsPanel.test.tsx in a Phase 59 describe block, all passing.
- [ ] Coral palette values `rgba(255, 184, 150, 0.22)` fill + `2px solid rgba(255, 184, 150, 0.60)` border are present verbatim in BOTH files (grep gates pass).
- [ ] Structured log prefixes `[empty-pv-drop-preview]` (Gap 1) and `[convlist-drop-preview]` (Gap 2) present in both files, zone-change-gated (Test H asserts exactly-once emission for repeated dragovers).
- [ ] `pointer-events-none` on BOTH overlay divs (underlying drop handlers still fire — regression Test D asserts onCloseSession still called on Gap 2 drop).
- [ ] Render gates: Gap 1 = `isConvRowDragOver && splitTree === null` (architectural exclusion for Pane-owned state); Gap 2 = `isBadgeDragOver` (single condition, no exclusion needed).
- [ ] Both files have bounding-rect stateless guard in dragleave (Test F asserts no flicker on child boundary crossings).
- [ ] Both files have window-level dragend cleanup useEffect (Test E asserts Escape-cancel path clears state).
- [ ] Type-gates preserved: empty-PV requires text/plain AND excludes Files; conv-list panel requires application/x-skynet-badge (Tests B/C on both gaps assert no tint on non-matching drags).
- [ ] Existing Phase 58 Plan 02 conv-list drop tests (A-G at :4093-4325) still pass with zero modifications — the tint additions are strictly additive.
- [ ] Two scoped-suite atomic commits landed; NO deploy/ship/patch-entry work performed by executor.
</success_criteria>

<output>
Create `.planning/phases/59-coral-drop-target-affordance-on-empty-prettyview-conv-list-c/59-01-SUMMARY.md` when done.
</output>
