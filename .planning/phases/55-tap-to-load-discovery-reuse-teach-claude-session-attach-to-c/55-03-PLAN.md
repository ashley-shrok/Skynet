---
phase: 55-tap-to-load-discovery-reuse-teach-claude-session-attach-to-c
plan: 03
type: execute
wave: 2
depends_on: [55-01]
files_modified:
  - src/backend/claude-session/session-file-discovery.ts
  - src/backend/claude-session/session-file-discovery.test.ts
  - src/backend/claude-session/claude-session-server.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "On connectToPane, if the shared session-file cache has an entry for (hostId, tmuxSession), the attach handler calls startActiveSessionFlow with cached { pid, sessionFile } and does NOT call discoverClaudeSession (cache-hit fast path)"
    - "On cache-miss, attach calls discoverClaudeSessionBatched which resolves pid+sessionFile in ONE SSH exec (round-trip count drops from 4 to 1)"
    - "Every attach emits exactly one 'Claude session discovery path' structured log carrying path (shared-hit | batched-fresh) and durationMs — measured from the moment cache lookup starts to the moment the branch decision produces the pid+sessionFile"
    - "Cache-hit path still invokes startActiveSessionFlow which runs readSessionFileRange probe internally, so the { type: 'session', pid, sessionFile, totalLines } frame carries a real totalLines (RESEARCH § Pitfall 3)"
    - "Cache-hit path still reaches the aside subsystem registration (inline block after startActiveSessionFlow at L7199 today) — no aside registration hoisting needed because startActiveSessionFlow is the exact call site the aside block follows in both branches"
    - "Batched fallback failure taxonomy preserved: no_tmux_session / not_claude / no_pid_session_file / no_open_session_file / exec_error all still surface with the same reason strings"
  artifacts:
    - path: src/backend/claude-session/session-file-discovery.ts
      provides: "New exported discoverClaudeSessionBatched(conn, sessionName) that runs the 4-step discovery in ONE SSH exec"
      exports: [discoverClaudeSessionBatched]
    - path: src/backend/claude-session/session-file-discovery.test.ts
      provides: "New tests for discoverClaudeSessionBatched covering all 5 result branches"
    - path: src/backend/claude-session/claude-session-server.ts
      provides: "connectToPane handler shim at ~L6776 that reads shared cache, branches to fast-path or batched-fresh, emits observability log per attach"
      contains: "readSessionFileCache("
  key_links:
    - from: src/backend/claude-session/claude-session-server.ts
      to: src/backend/fleet-status/session-file-cache.ts
      via: "import readSessionFileCache from '../fleet-status/session-file-cache.js'"
      pattern: "readSessionFileCache\\("
    - from: src/backend/claude-session/claude-session-server.ts
      to: src/backend/claude-session/session-file-discovery.ts
      via: "import discoverClaudeSessionBatched from './session-file-discovery.js'"
      pattern: "discoverClaudeSessionBatched\\("
---

<objective>
Land the READER half of the phase: (a) a new `discoverClaudeSessionBatched` in `session-file-discovery.ts` that compresses the current 4 serial SSH exec round-trips into ONE, and (b) a shim in `claude-session-server.ts` at the pre-discovery site (~L6776) that first consults the shared session-file cache (Plan 55-01) for a `{ pid, sessionFile }` answer. On cache-hit, `startActiveSessionFlow` is called immediately — skipping discovery entirely. On cache-miss, `discoverClaudeSessionBatched` fires as the fresh-discovery path. Every attach emits ONE observability log line naming the path taken (`shared-hit` | `batched-fresh`) and the duration in ms, measured from the moment cache lookup starts.

Purpose: This is where the phase's ~5s → ~50ms (cache-hit) / ~500ms (cache-miss) speedup is realized. Fast path is a Map read; batched-fresh path is a single ssh2 exec instead of 4 serial ones. The observability log makes it debuggable in production: if a tap ever feels slow again, the log answers "which part is slow" without new instrumentation. Downstream recovery (discovery-repoll ticker at L4197, frontend `lastKnownSessionFileRef` rotation-reset at commit `3e0f7c54`) already handles stale-cache reads — no invalidation protocol is introduced here.

Output: One new exported function (`discoverClaudeSessionBatched`) + its tests in `session-file-discovery.ts` / `.test.ts`; a shim + observability log at L~6776 of `claude-session-server.ts` that reroutes the existing `discoverClaudeSession(conn, tmuxSession)` call through the cache-hit / batched-fresh decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-tap-to-load-discovery-reuse-teach-claude-session-attach-to-c/55-CONTEXT.md
@.planning/phases/55-tap-to-load-discovery-reuse-teach-claude-session-attach-to-c/55-RESEARCH.md
@CLAUDE.md
@src/backend/claude-session/session-file-discovery.ts
@src/backend/claude-session/session-file-discovery.test.ts
@src/backend/claude-session/claude-session-server.ts
@src/backend/claude-session/discover-identity-session-file.ts
@src/backend/fleet-status/session-file-cache.ts
@src/backend/ssh/tmux-helper.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add discoverClaudeSessionBatched — single-exec fresh discovery</name>
  <files>
    src/backend/claude-session/session-file-discovery.ts,
    src/backend/claude-session/session-file-discovery.test.ts
  </files>
  <read_first>
    src/backend/claude-session/session-file-discovery.ts,
    src/backend/claude-session/session-file-discovery.test.ts,
    src/backend/claude-session/discover-identity-session-file.ts,
    src/backend/ssh/tmux-helper.ts,
    .planning/phases/55-tap-to-load-discovery-reuse-teach-claude-session-attach-to-c/55-RESEARCH.md
  </read_first>
  <behavior>
    - discoverClaudeSessionBatched(conn, sessionName) runs ONE execCommand call whose script sequences all 4 discovery steps (pane_pid via tmux display-message, descendant walk via ps+awk, PID-file read via cat, JSONL existence test via test -f) and emits structured stdout parseable by JS
    - Happy path: single exec returns pid + sessionFile; helper returns { status: "active", pid, sessionFile }
    - Missing pane_pid (empty / unparseable output from tmux display-message): { status: "inactive", reason: "no_tmux_session" }
    - No claude in descendant tree (awk BFS emits empty): { status: "inactive", reason: "not_claude" }
    - PID file missing or malformed JSON / missing sessionId/cwd: { status: "inactive", reason: "no_pid_session_file" }
    - Constructed JSONL path does not exist on disk: { status: "inactive", reason: "no_open_session_file" }
    - SSH exec throws or 3s timeout expires: { status: "inactive", reason: "exec_error" }
    - The 4-step timeout budget is a SINGLE Promise.race of DISCOVERY_EXEC_TIMEOUT_MS (3000ms) — batched has one round-trip so one timeout gate suffices (down from 4 in the serial helper)
    - Slug construction (cwd → dir slug) stays in JS, NOT in shell (per RESEARCH § "Anti-pattern to avoid" — fragile across Claude Code versions)
    - Existing discoverClaudeSession is NOT removed and NOT modified — batched is an ADDITIVE new export
  </behavior>
  <action>
    Modify `src/backend/claude-session/session-file-discovery.ts` — append a new exported function `discoverClaudeSessionBatched(conn: Client, sessionName: string): Promise<ClaudeSessionDiscoveryResult>` AFTER the existing `discoverClaudeSession` function. Do NOT modify or remove `discoverClaudeSession` — it stays for backward compatibility and as the trusted-known-good fallback. Do NOT change the `ClaudeSessionDiscoveryResult` type union.

    Import `shellSingleQuote` from `./discover-identity-session-file.js` (path relative to session-file-discovery.ts is `./discover-identity-session-file.js` — same directory).

    The batched script is one string produced by JS `+` concatenation. Every shell statement MUST be terminated with `;` — same LOAD-BEARING hazard called out in the existing `walkScript`, `pidFileScript`, and `testScript` comments at L92-99 / L160-164 / L228 of this file, and in `discover-identity-session-file.ts:198-200` (`buildDiscoveryScript`). Follow that exact convention.

    Script structure (all steps concatenated into one exec):

    - Step 1: Set `TMUX_SESSION` to `shellSingleQuote(sessionName)`; capture `PANE_PID=$(tmux display-message -p -t "$TMUX_SESSION" '#{pane_pid}' 2>/dev/null)`; if empty or not-a-positive-integer, `echo NO_TMUX_SESSION` and `exit 0`.
    - Step 2: The exact same awk BFS from the existing `walkScript` (L100-119) — copy it verbatim, replacing `PID=${panePid}` with `PID="$PANE_PID"` since PANE_PID is now a shell variable. Capture output as `CLAUDE_PID=$(...)`. If empty, `echo NOT_CLAUDE` and `exit 0`.
    - Step 3: `PID_FILE=$HOME/.claude/sessions/$CLAUDE_PID.json`; if `! -f "$PID_FILE"`, `echo NO_PID_SESSION_FILE` and `exit 0`. Otherwise `SESSION_JSON=$(cat "$PID_FILE")` and `HOME_VAL="$HOME"`.
    - Step 4 (emit): `echo OK`; `echo "$CLAUDE_PID"`; `echo "$HOME_VAL"`; `echo "---SESSION-JSON---"`; `printf '%s' "$SESSION_JSON"` — the printf keeps the JSON on one raw chunk without an added trailing newline. The JS parser then splits on lines; first line is the status marker, second is the claude PID string, third is HOME, and everything after the `---SESSION-JSON---` delimiter is the raw JSON blob.

    Reason for keeping HOME + SESSION_JSON in stdout instead of constructing the JSONL path in shell: the slug logic (`cwd.replace(/[./~]/g, "-")`) is fragile across Claude Code versions and lives in JS at L224 of the existing helper. Duplicating it in awk/sed would drift. RESEARCH § Pattern 2 documents this trade-off explicitly.

    After parsing SESSION_JSON, apply the exact same failure taxonomy as the existing helper (invalid JSON / missing sessionId / missing cwd → `no_pid_session_file`). Slugify cwd via the identical `cwd.replace(/[./~]/g, "-")` rule (copy the comment about Stacy 2026-08-08 verbatim from L222-223). Construct `constructedPath = ${homeVal}/.claude/projects/${slug}/${sessionId}.jsonl`.

    Then — because the batched script has NOT yet verified the JSONL exists on disk — issue ONE follow-up test-f exec. This is a deliberate second round-trip when the PID file was found; RESEARCH's "one round-trip instead of ten-plus" is measured against the current 4-step serial baseline, and dropping the existence check from a well-defined-in-shell candidate path is where the batched script MOST realistically may be wrong (fresh session, JSONL not yet created). Keep the test-f as a second exec so the helper never returns `{ status: "active" }` for a nonexistent file. Use `if [ -f "${constructedPath}" ]; then printf '%s' "${constructedPath}"; fi` — identical to the current `testScript` at L229-232.

    (Effective round-trip count: 2 for cache-miss vs 4 today. That is still an ~2x speedup on cache-miss and preserves the on-disk-verify safety guarantee. RESEARCH § "one round-trip instead of ten-plus" was the CONTEXT.md figure; the SUCCESS CRITERION of ~500ms cache-miss holds at 2 round-trips on a 50-100ms link.)

    Wrap both execCommand calls in `Promise.race([exec, timeoutReject])` using the existing `DISCOVERY_EXEC_TIMEOUT_MS` constant. On throw / timeout at ANY step, return `{ status: "inactive", reason: "exec_error" }` (same as serial helper).

    Then modify `src/backend/claude-session/session-file-discovery.test.ts`:

    Add a new `describe("discoverClaudeSessionBatched — single-exec discovery", () => { ... })` block AT THE END of the file. Reuse the module-level `vi.mock("../ssh/tmux-helper.js", ...)` setup already at the top of the file. In `beforeEach`, `vi.mocked(execCommand).mockReset(); vi.useRealTimers();`.

    Because the batched helper uses `execCommand` for BOTH the main script AND the test-f script, and each script has a unique substring, wire the mock dispatcher on:
    - Main batched script contains: `"tmux display-message"` AND `"awk -v root="` AND `"---SESSION-JSON---"` (uniquely identifies the batched script — the serial `walkScript` has `awk -v root=` but not `tmux display-message` because pane_pid comes from `queryPanePid` in the serial helper).
    - Test-f script contains: `'if [ -f "'` and no other batched markers.

    Tests to add:

    - `Test batched-1: happy path — full 4-step success in one exec` — Mock main exec to return `"OK\n12345\n/home/ubuntu\n---SESSION-JSON---\n{\"sessionId\":\"abc123\",\"cwd\":\"/home/ubuntu/project\"}"`; mock test-f exec to return `"/home/ubuntu/.claude/projects/-home-ubuntu-project/abc123.jsonl"`. Call `discoverClaudeSessionBatched(fakeConn, "test-session")`. Assert result `{ status: "active", pid: 12345, sessionFile: "/home/ubuntu/.claude/projects/-home-ubuntu-project/abc123.jsonl" }`. Assert exec was called exactly TWICE.

    - `Test batched-2: no pane_pid → no_tmux_session` — Main exec returns `"NO_TMUX_SESSION"`. Assert result `{ status: "inactive", reason: "no_tmux_session" }`. Assert exec was called exactly ONCE (test-f never fires).

    - `Test batched-3: pane_pid found but no claude in tree → not_claude` — Main exec returns `"NOT_CLAUDE"`. Assert `{ status: "inactive", reason: "not_claude" }`. Exec called once.

    - `Test batched-4: claude found but PID file missing → no_pid_session_file` — Main exec returns `"NO_PID_SESSION_FILE"`. Assert `{ status: "inactive", reason: "no_pid_session_file" }`. Exec called once.

    - `Test batched-5: PID JSON malformed → no_pid_session_file` — Main exec returns `"OK\n12345\n/home/ubuntu\n---SESSION-JSON---\n{not json"`. Assert `{ status: "inactive", reason: "no_pid_session_file" }`. Exec called once.

    - `Test batched-6: PID JSON missing sessionId → no_pid_session_file` — Main exec returns `"OK\n12345\n/home/ubuntu\n---SESSION-JSON---\n{\"cwd\":\"/x\"}"`. Assert `{ status: "inactive", reason: "no_pid_session_file" }`.

    - `Test batched-7: JSONL not on disk → no_open_session_file` — Main exec returns valid OK payload; test-f returns `""`. Assert `{ status: "inactive", reason: "no_open_session_file" }`. Exec called TWICE.

    - `Test batched-8: main exec throws → exec_error` — Main exec `mockRejectedValue(new Error("ssh gone"))`. Assert `{ status: "inactive", reason: "exec_error" }`.

    - `Test batched-9: test-f exec throws → exec_error` — Main exec returns valid OK; test-f rejects. Assert `{ status: "inactive", reason: "exec_error" }`.

    - `Test batched-10: main exec times out (>3s) → exec_error` — Use `vi.useFakeTimers()`, make main exec return a `new Promise` that never resolves. Advance timers by 3001ms; await result. Assert `{ status: "inactive", reason: "exec_error" }`.

    Every test MUST assert the `execCommand` mock call count via `expect(vi.mocked(execCommand).mock.calls.length).toBe(<expected>)` — this is the round-trip-count guarantee the phase's speedup depends on.
  </action>
  <verify>
    <automated>npx vitest run src/backend/claude-session/session-file-discovery.test.ts -t "discoverClaudeSessionBatched"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^export function discoverClaudeSessionBatched\|^export async function discoverClaudeSessionBatched" src/backend/claude-session/session-file-discovery.ts` returns 1 (new helper exported).
    - `grep -c "^export function discoverClaudeSession\|^export async function discoverClaudeSession" src/backend/claude-session/session-file-discovery.ts` returns 2 (both the original and the new batched — original is preserved unmodified).
    - `grep -n "tmux display-message" src/backend/claude-session/session-file-discovery.ts` — appears ONLY inside the new batched script (the serial helper uses `queryPanePid`, not this string).
    - `grep -v '^\s*//\|^\s*\*' src/backend/claude-session/session-file-discovery.ts | grep -c "shellSingleQuote"` ≥ 1 (batched script uses `shellSingleQuote(sessionName)`; matches non-comment lines only per grep-gate hygiene).
    - All 10 `Test batched-*` cases pass under `npx vitest run src/backend/claude-session/session-file-discovery.test.ts -t "discoverClaudeSessionBatched"`.
    - Pre-existing `discoverClaudeSession` tests still pass — `npx vitest run src/backend/claude-session/session-file-discovery.test.ts` full-file green.
    - `npm run build:backend` completes with exit code 0.
  </acceptance_criteria>
  <done>
    Batched helper is a working additive export; all 10 new tests + the original 14 tests are green; original helper unchanged; backend build clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire cache-hit shim + batched-fresh fallback + observability log in connectToPane</name>
  <files>
    src/backend/claude-session/claude-session-server.ts,
    src/backend/claude-session/session-file-discovery.test.ts
  </files>
  <read_first>
    src/backend/claude-session/claude-session-server.ts,
    src/backend/claude-session/session-file-discovery.ts,
    src/backend/fleet-status/session-file-cache.ts,
    .planning/phases/55-tap-to-load-discovery-reuse-teach-claude-session-attach-to-c/55-RESEARCH.md,
    .planning/phases/55-tap-to-load-discovery-reuse-teach-claude-session-attach-to-c/55-CONTEXT.md
  </read_first>
  <behavior>
    - Cache-hit path: connectToPane observes a non-null readSessionFileCache entry → emits shared-hit observability log → calls startActiveSessionFlow({ pid, sessionFile, tmuxSession, hostId }) using the cached pid + sessionFile → returns without ever calling discoverClaudeSession or discoverClaudeSessionBatched
    - Cache-miss path: readSessionFileCache returns null → calls discoverClaudeSessionBatched (NOT the legacy serial discoverClaudeSession) → emits batched-fresh observability log with durationMs measured from the pre-cache-lookup t0 → downstream inactive/active branches at existing L~6786-7199 fire as they do today
    - The pre-existing "Claude session discovery result" structured log at L~6777 stays in place on the cache-miss path (RESEARCH § "Note" — it's the discovery-result log, NOT the phase's new path log)
    - Aside subsystem registration (inline in connectToPane handler AFTER startActiveSessionFlow at L7199) runs on both branches because startActiveSessionFlow is called from both — the shim does NOT need to hoist or duplicate any aside code (RESEARCH § Assumption A2 confirmed by direct code read: aside block follows startActiveSessionFlow in the enclosing handler, not inside the closure)
    - Cache-hit ordering: sshConn MUST be established (via connectOneShot at ~L5962) BEFORE the cache lookup — startActiveSessionFlow needs sshConn for readSessionFileRange probe and tailSessionFile even on cache-hit (RESEARCH § Pitfall 5)
    - Observability log is emitted EXACTLY ONCE per attach — never twice (guard against emitting on both branches of an if/else)
    - Frontend wire schema unchanged: no new WS frame types, no changes to { type: "session", ... } frame contents (RESEARCH scope-lock — frontend not modified this phase)
  </behavior>
  <action>
    Modify `src/backend/claude-session/claude-session-server.ts`:

    1. Add imports near the existing block at lines 10-11 (adjacent to `discoverClaudeSession` and `discoverIdentitySessionFile`):
       - `import { discoverClaudeSession, discoverClaudeSessionBatched } from "./session-file-discovery.js";` — REPLACE the current `import { discoverClaudeSession } from "./session-file-discovery.js";` at line 10 with this combined import. Do NOT add a second import statement.
       - `import { readSessionFileCache } from "../fleet-status/session-file-cache.js";` — new import.

    2. Locate the current discovery call site at L~6776:
       ```
       const result = await discoverClaudeSession(conn, tmuxSession);
       sshLogger.info("Claude session discovery result", { ... });
       ```
       (This is the ONLY unbatched, top-level discoverClaudeSession call in the connectToPane handler — do NOT touch the dormant-poll seam's internal `discoverSession: (c, s) => discoverClaudeSession(...)` at ~L6879 which is a different code path.)

    3. Replace that block with the following structure (write in TypeScript, NOT as a fenced code block in the plan — this is directive prose):

       - Capture `const discoveryT0 = Date.now();` BEFORE the cache lookup.
       - `const cached = readSessionFileCache(hostId, tmuxSession);` — note: `hostId` at this scope is a `number` from `msg.hostId` validated at L5921-5929; the cache module coerces via `String()` internally per Plan 55-01.
       - If `cached !== null`:
         - Emit ONE structured `sshLogger.info("Claude session discovery path", { operation: "claude_session_discovery_path", userId, sessionId, hostId, tmuxSession, path: "shared-hit", durationMs: Date.now() - discoveryT0 });`
         - Call `startActiveSessionFlow({ pid: cached.pid, sessionFile: cached.sessionFile, tmuxSession, hostId });` — this is the same signature that L7199 uses today for the fresh-discovery active path. That call flows through the closure defined at L6002 which handles readSessionFileRange probe, session-metadata frame emit, context-% timer, discovery-repoll timer, and tail-start.
         - After the startActiveSessionFlow call, `return;` — the enclosing connectToPane handler's remaining branches (inactive/dormant classification at L~6786+ and the active-flow call at L7199) MUST be skipped on cache-hit.
         - IMPORTANT: The aside subsystem block that follows L7199 in the current code path is INLINE inside `startActiveSessionFlow` (comment at L7192-7198 confirms). The cache-hit return skips ONLY the redundant L7199 call — it does NOT skip the aside registration, because that registration lives inside `startActiveSessionFlow` itself. Executor MUST re-read L7192-7199 during implementation to confirm this ordering before writing the `return;` line. If (contrary to RESEARCH § Assumption A2) the aside block is discovered to live OUTSIDE `startActiveSessionFlow`, STOP and surface the finding; do not silently ship a cache-hit that skips aside registration.
       - Else (cache-miss):
         - `const result = await discoverClaudeSessionBatched(conn, tmuxSession);`
         - Emit ONE structured `sshLogger.info("Claude session discovery path", { operation: "claude_session_discovery_path", userId, sessionId, hostId, tmuxSession, path: "batched-fresh", durationMs: Date.now() - discoveryT0 });`
         - Preserve the pre-existing `sshLogger.info("Claude session discovery result", { operation: "claude_session_discovery", userId, sessionId, hostId, tmuxSession, status: result.status });` log EXACTLY as it is today (this is the existing discovery-outcome log — the phase adds the path log ALONGSIDE it, not as a replacement, per RESEARCH § "Note").
         - Fall through to the existing `if (result.status === "inactive") { ... }` branch and the final `startActiveSessionFlow({ pid: result.pid, sessionFile: result.sessionFile, tmuxSession, hostId });` at L7199 — those blocks are UNCHANGED from today.

    4. Verify NO other call site in the file switches from `discoverClaudeSession` to the batched variant. Specifically, the dormant-poll seam at L~6879 (`discoverSession: (c, s) => discoverClaudeSession(c as import("ssh2").Client, s)`) MUST remain calling the LEGACY serial `discoverClaudeSession`. Dormant polling has different concurrency semantics (ticker under `dormantPollInFlight` guard, called at 3s cadence) and is out of scope for this phase's speedup (scope reminder from CONTEXT.md — dormant-identity wake path is deferred). Batched is used ONLY on cold attach.

    Then modify `src/backend/claude-session/session-file-discovery.test.ts`:

    Add a new `describe("Phase 55: connectToPane cache-hit vs batched-fresh integration", () => { ... })` block. Because `startActiveSessionFlow` is an internal closure and hard to test at unit-test scope, use a NARROW seam-check test at the discovery boundary instead:

    - `Test integration-1: cache-hit skips discovery entirely` — Import `writeSessionFileCache, __clearAllSessionFileCacheForTests` from `../fleet-status/session-file-cache.js`. Before the test, `__clearAllSessionFileCacheForTests()`; then `writeSessionFileCache("7", "aqua", { sessionFile: "/x/a.jsonl", pid: 42 })`. Then in the test, assert `readSessionFileCache("7", "aqua")` returns the entry AND `readSessionFileCache(7, "aqua")` also returns it (proves the cross-type coercion the shim depends on). Also assert that after the cache hit, `discoverClaudeSessionBatched` would NOT be invoked — this test is a contract check on the cache primitive against the shim's expected call shape; it does NOT spin up the WS server.

    - `Test integration-2: batched-fresh path is what the shim calls on miss` — Assert `discoverClaudeSessionBatched` exists and is callable with `(fakeConn, "aqua")` after mocking `execCommand` to return `"OK\n42\n/home/x\n---SESSION-JSON---\n{\"sessionId\":\"s1\",\"cwd\":\"/home/x/proj\"}"` and test-f `"/home/x/.claude/projects/-home-x-proj/s1.jsonl"`. Assert result `.status === "active"` and `.pid === 42`. This anchors the shim's cache-miss branch to a batched call that actually returns active on well-formed data.

    - `Test integration-3: cache-miss falls straight through with no wait / no fleet-status coupling` — Direct behavioral test of the CONTEXT.md § "What would make it wrong" clause: *"Attach never blocks pending fleet-status work (or explicit test that empty cache falls straight through without wait)."* Setup: `__clearAllSessionFileCacheForTests()` (guarantee empty cache). Then write an ENTRY under a DIFFERENT key: `writeSessionFileCache("999", "some-other-identity", { sessionFile: "/x/other.jsonl", pid: 999 })`. Assert `readSessionFileCache(7, "aqua")` returns `null` (the write under a different key does NOT satisfy this read). Then, without any await on fleet-status or any timer/interval, immediately assert that the cache-miss code path invokes `discoverClaudeSessionBatched` — mock `execCommand` to record its first argument and return the OK payload from integration-2; assert `execCommand` was called synchronously (no `setTimeout`, no `Promise.race` against a fleet-status tick, no polling). Also assert the branch takes the OBSERVABILITY `path: "batched-fresh"` log value, NOT `path: "shared-hit"`. This is the direct test of "opportunistic read, no blocking" that the shape philosophy hangs on.

    (Deeper end-to-end coverage — WebSocket accept + full attach flow — is deferred to Ashley's UAT per CONTEXT.md success criterion; the plan does NOT introduce a WS-server integration test harness. The three integration tests above lock the contract at the seam the shim depends on: cache-primitive cross-type coercion (integration-1), batched happy path (integration-2), and cache-miss immediate-fallthrough with correct path-log branch (integration-3).)
  </action>
  <verify>
    <automated>npx vitest run src/backend/claude-session/session-file-discovery.test.ts -t "Phase 55"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "readSessionFileCache" src/backend/claude-session/claude-session-server.ts` — exactly TWO matches: one import at the top, one call at the shim inside connectToPane (line number between L5911 and L7199).
    - `grep -n "discoverClaudeSessionBatched" src/backend/claude-session/claude-session-server.ts` — exactly TWO matches: one import, one call (inside the cache-miss branch of the shim).
    - `grep -n "discoverClaudeSession\b" src/backend/claude-session/claude-session-server.ts` — the legacy serial helper is STILL referenced at least once (specifically in the dormant-poll seam at ~L6879 — `discoverSession: (c, s) => discoverClaudeSession(...)`). This proves the dormant-poll path was NOT accidentally rewired.
    - `grep -c 'sshLogger.info("Claude session discovery path"' src/backend/claude-session/claude-session-server.ts` returns exactly 2 (one for shared-hit branch, one for batched-fresh branch; NOT 1 (would mean only one branch instrumented) and NOT 3+ (would mean duplicated in both branches somehow)).
    - `grep -c 'sshLogger.info("Claude session discovery result"' src/backend/claude-session/claude-session-server.ts` returns exactly 1 (the pre-existing log preserved).
    - `grep -c "path: \"shared-hit\"\|path: 'shared-hit'" src/backend/claude-session/claude-session-server.ts` returns exactly 1.
    - `grep -c "path: \"batched-fresh\"\|path: 'batched-fresh'" src/backend/claude-session/claude-session-server.ts` returns exactly 1.
    - Both `Test integration-*` cases pass under `npx vitest run src/backend/claude-session/session-file-discovery.test.ts -t "Phase 55"`.
    - Pre-existing claude-session-server tests (all of `claude-session-server.*.test.ts`) still pass — `npx vitest run src/backend/claude-session` full-directory green.
    - `npm run build:backend && npm run build` completes with exit code 0 (both backend AND frontend build clean per CLAUDE.md fleet-directive that backend-only typecheck is `build:backend` + `build`, NOT `npx tsc --noEmit`).
    - `git diff src/ui/` shows ZERO changes (frontend untouched per CONTEXT.md scope — the whole change is server-side).
    - `git diff src/backend/claude-session/claude-session-server.ts | grep -c "startActiveSessionFlow"` — added lines that reference `startActiveSessionFlow` are at MOST 1 (the cache-hit branch's call); the existing L7199 call is unchanged (proves we did not double-call startActiveSessionFlow on cache-miss).
  </acceptance_criteria>
  <done>
    Cache-hit path calls startActiveSessionFlow with cached { pid, sessionFile } and returns; cache-miss path calls discoverClaudeSessionBatched and falls through to the existing inactive/active branches; observability log fires exactly once per attach with correct path value + durationMs; dormant-poll seam still uses legacy serial helper; frontend untouched; backend + frontend build clean; all pre-existing claude-session tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Attacker-controlled tmuxSession in shim | Bounded — `tmuxSession` is validated at L5921-5929 (must be non-empty string; upstream WS-attach validator restricts to safe subset per RESEARCH § Security Domain V5). |
| Cache-hit path bypasses discovery | Bounded — cached values were written by the trusted orchestrator (Plan 55-02); no attacker path into the cache exists. |
| Batched shell script content | Trusted construction — `shellSingleQuote(sessionName)` wraps the only variable interpolation; same defense-in-depth pattern used by `buildDiscoveryScript` per RESEARCH. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-07 | Tampering | Batched script shell injection via tmuxSession | mitigate | `shellSingleQuote(sessionName)` wraps the single interpolation point; upstream WS-attach validator restricts input; batched script uses the same one-line JS `+` concat + `;` terminator discipline as `walkScript` (RESEARCH § Pitfall 2). |
| T-55-08 | Spoofing | Stale cache serves wrong session on rotation-race | accept | Downstream recovery (discovery-repoll ticker at L4197 + frontend `lastKnownSessionFileRef` rotation-reset commit `3e0f7c54`) catches within ~2-3s per CONTEXT.md philosophy. No new invalidation protocol added — the trade-off is intentional. |
| T-55-09 | Information Disclosure | Observability log leaks path/tmuxSession | accept | Same fields already logged by the pre-existing "Claude session discovery result" log at L6777 (userId, sessionId, hostId, tmuxSession). Adding path + durationMs does not widen the disclosure surface. |
| T-55-10 | Denial of Service | Aside registration skipped on cache-hit → broken BTW extraction | mitigate | RESEARCH § Assumption A2 documents the aside block lives INSIDE `startActiveSessionFlow` and runs on both branches. The shim's return statement is placed AFTER `startActiveSessionFlow` invocation, so the closure's aside registration always runs. Executor MUST re-verify L7192-7199 during implementation and halt if the assumption breaks. |
</threat_model>

<verification>
- `npx vitest run src/backend/claude-session` — all claude-session tests green (existing + new Phase 55 integration tests).
- `npx vitest run src/backend/fleet-status` — all fleet-status tests green (regression check — nothing this plan touches should affect fleet-status, but the shared cache module now has both readers and writers).
- `npm run build:backend && npm run build` — exit 0.
- `git diff src/ui/` shows no changes.
- The five grep-count acceptance checks in Task 2 all pass with the stated exact counts.
</verification>

<success_criteria>
- Cache-hit path in connectToPane skips discovery and jumps straight to startActiveSessionFlow with cached { pid, sessionFile } — provably by grep count on `readSessionFileCache`.
- Cache-miss path uses discoverClaudeSessionBatched (the new 2-round-trip helper), not the legacy 4-round-trip serial helper.
- Exactly one observability log line per attach carrying path (shared-hit or batched-fresh) and durationMs.
- Aside subsystem registration is NOT accidentally skipped on cache-hit (verified by executor re-reading L7192-7199 during implementation).
- Dormant-poll seam's internal use of legacy `discoverClaudeSession` is preserved untouched.
- Frontend files unchanged.
- Backend + frontend builds clean.
- All pre-existing claude-session tests green.
</success_criteria>

<output>
Create `.planning/phases/55-tap-to-load-discovery-reuse-teach-claude-session-attach-to-c/55-03-SUMMARY.md` when done, recording:
- Exact line numbers of the shim insertion (start + end) in claude-session-server.ts
- Round-trip count actually delivered by discoverClaudeSessionBatched on cache-miss (should be 2 — main script + test-f)
- Confirmation (with L7192-7199 verbatim quote or line-referenced note) that the aside block lives inside startActiveSessionFlow and reaches on cache-hit
- Number of new tests added to session-file-discovery.test.ts (should be 10 batched tests + 3 integration tests = 13 new)
</output>
