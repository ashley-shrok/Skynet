---
phase: 90-relay-mediated-group-conversations-sub-slice-d-relay-session
plan: 05
type: execute
wave: 3
depends_on: [90-00, 90-01, 90-02, 90-04]
files_modified:
  - src/ui/features/relay-room-pane/RelayRoomPane.tsx
  - src/ui/features/relay-room-pane/RelayRoomPane.test.tsx
  - src/ui/features/relay-room-pane/RelayMessageList.tsx
  - src/ui/features/relay-room-pane/RelayMessageList.test.tsx
  - src/ui/features/relay-room-pane/IdentityBadgeRow.tsx
  - src/ui/features/relay-room-pane/IdentityBadgeRow.test.tsx
  - src/ui/features/relay-room-pane/relay-room-api.ts
  - src/ui/features/relay-room-pane/error-state.tsx
  - src/ui/features/relay-room-pane/error-state.test.tsx
  - src/ui/state/viewing-user-store.ts
  - src/ui/state/viewing-user-store.test.ts
autonomous: true
requirements: [D-07, D-09, D-11, D-12, D-12-fork-consumption, D-13, D-14, D-17, D-18, D-19, W-8-viewingUserMxid-source]
tags: [frontend, relay-pane, skeleton, message-list, identity-badge-row, error-state, humans-only-badges, D-07, D-09, D-18, viewing-user-store]
must_haves:
  truths:
    - "New `RelayRoomPane` component tree renders under `src/ui/features/relay-room-pane/` — parallel to pretty-view/, NEVER modifying pretty-view/."
    - "`IdentityBadgeRow` renders humans first (alphabetical by displayName) then agents (alphabetical by identityKey), with the viewing user excluded (D-07)."
    - "Per-human badge in the row uses the plain identity badge with NO appendage — absence of the appendage is how the viewer distinguishes humans from agents at a glance (D-09). Per-agent badge is a placeholder in this plan (Plan 06 adds the meter/reset appendage)."
    - "`RelayMessageList` renders inbound bubbles via the FORKED `RelayRoomInboundBubble` primitive from Plan 02 Task 3 (D-12 fork clarification — expanded-always, no-collapse, no pointer-detect), outbound bubbles via the `OutboundBubble` primitive from Plan 02 (D-13), and paginates by event-id cursor via `LoadMoreOlderButton` reused as-is at initial load size 20 (D-14 matches pretty view 1:1)."
    - "Empty room (no messages) renders the presence row + empty middle + normal compose area — NO special empty-state chrome (D-17)."
    - "Room-not-found / membership-lost (403/404 from backend via the `inactive` WS frame OR 404 from the participants REST endpoint) renders the D-18 friendly error state, not a crash. Copy: 'This conversation is no longer available.' Optional subline: 'You may have been removed from this room.' NO retry button."
    - "Inbound bubbles with an attachment render a minimal placeholder text (D-19): `attachment: <filename>` — no media, no fetch."
    - "Every bubble body renders via React text children — NEVER `dangerouslySetInnerHTML` (T-17-03-01 security discipline preserved)."
    - "`viewingUserMxid` source (W#8 resolution): new small `src/ui/state/viewing-user-store.ts` fetches the viewing user's mxid via a small dedicated backend endpoint (or extends `getUserInfo()` — planner picks the smallest-diff path; SEE ACTION for the grep-based selection). Exposes `useViewingUserMxid(): string | null` hook. `RelayRoomPane` consumes this hook — the mxid is NOT threaded down from the shell as a prop."
  artifacts:
    - path: "src/ui/features/relay-room-pane/RelayRoomPane.tsx"
      provides: "Top-level relay-pane component composing IdentityBadgeRow (top), RelayMessageList (middle) — compose comes in Plan 06."
      contains: "export function RelayRoomPane"
    - path: "src/ui/features/relay-room-pane/RelayMessageList.tsx"
      provides: "Bubble list wrapper — renders RelayRoomInboundBubble (forked) + OutboundBubble; hosts LoadMoreOlderButton for pagination (Plan 06 wires the WS call)."
      contains: "export function RelayMessageList"
    - path: "src/ui/features/relay-room-pane/IdentityBadgeRow.tsx"
      provides: "Horizontal row of participants — humans (plain badge, no appendage per D-09) + agents (placeholder cell for Plan 06 to fill), sorted humans-then-agents alphabetical within each role, viewing user excluded."
      contains: "export function IdentityBadgeRow"
    - path: "src/ui/features/relay-room-pane/error-state.tsx"
      provides: "D-18 friendly error state component."
      contains: "export function RelayRoomErrorState"
    - path: "src/ui/features/relay-room-pane/relay-room-api.ts"
      provides: "Type-only WS frame + HTTP payload contracts mirroring Plan 04's server-side shapes."
      contains: "export type RelayRoomServerEvent"
    - path: "src/ui/state/viewing-user-store.ts"
      provides: "useViewingUserMxid() hook — single source of truth for the viewing user's Matrix mxid (W#8 resolution)."
      contains: "export function useViewingUserMxid"
  key_links:
    - from: "src/ui/features/relay-room-pane/RelayMessageList.tsx"
      to: "src/ui/features/relay-room-pane/RelayRoomInboundBubble.tsx"
      via: "consumes the Plan 02 Task 3 fork (D-12 fork clarification) — NOT the pretty-view RelayInboundBubble"
      pattern: "import.*RelayRoomInboundBubble"
    - from: "src/ui/features/relay-room-pane/RelayMessageList.tsx"
      to: "src/ui/components/OutboundBubble.tsx"
      via: "Plan 02 primitive consumption (D-13)"
      pattern: "import.*OutboundBubble"
    - from: "src/ui/features/relay-room-pane/RelayMessageList.tsx"
      to: "src/ui/features/pretty-view/LoadMoreOlderButton.tsx"
      via: "reuse-as-is per D-14 no-hooks pure component"
      pattern: "import.*LoadMoreOlderButton"
    - from: "src/ui/features/relay-room-pane/IdentityBadgeRow.tsx"
      to: "src/ui/features/terminal/IdentityBadge.tsx"
      via: "reuse existing badge primitive per D-08"
      pattern: "import.*IdentityBadge"
    - from: "src/ui/features/relay-room-pane/RelayRoomPane.tsx"
      to: "src/ui/state/viewing-user-store.ts"
      via: "W#8 resolution — mxid via hook, not via prop"
      pattern: "useViewingUserMxid"
---

<objective>
Build the RelayRoomPane component skeleton — everything except the compose box, per-agent badge appendage, and live-subscription wiring (those land in Plan 06). This plan delivers:

1. **`RelayRoomPane.tsx`** — top-level pane component with the layout structure (IdentityBadgeRow at top, RelayMessageList in the middle, a placeholder slot for the compose box that Plan 06 fills). Fetches participants via the Plan 04 `GET /relay-room/:roomId/participants` endpoint and passes them to the badge row.

2. **`RelayMessageList.tsx`** — bubble list wrapper that dispatches per-frame render: `RelayRoomInboundBubble` (FORKED — Plan 02 Task 3, per D-12 fork clarification), `OutboundBubble` (Plan 02 primitive per D-13). Hosts `LoadMoreOlderButton` at the top (reuse-as-is per D-14 pure-component). Accepts a `history: MatrixEvent[]` prop from the parent for now — Plan 06 wires the WS subscription that populates it.

3. **`IdentityBadgeRow.tsx`** — horizontal participant row (D-07 humans-first-then-agents-alphabetical, viewing-user excluded). Per-human badge uses the plain `IdentityBadge` with NO appendage (D-09). Per-agent badge is a PLACEHOLDER in this plan — a cell that renders the plain badge only; Plan 06 replaces it with `AgentBadgeWithAppendage` carrying the meter + reset.

4. **`error-state.tsx`** — D-18 friendly error component ('This conversation is no longer available.').

5. **`relay-room-api.ts`** — type-only wire contract module (client + server frame discriminated unions) mirroring Plan 04's server-side shapes.

6. **`viewing-user-store.ts`** — NEW small state module exposing `useViewingUserMxid()` hook (W#8 resolution). RelayRoomPane consumes this hook to get the viewing user's mxid, which drives (a) IdentityBadgeRow's D-07 self-exclusion filter and (b) RelayMessageList's inbound-vs-outbound discrimination.

Purpose: interface-first — establish the component tree + type contracts so Plan 06's compose-box + per-agent-badge + WS wiring lands on a well-defined seam. All D-11/D-12/D-13/D-14/D-17/D-18/D-19 render-side decisions are honored here; the send-side decisions (D-15/D-16) come in Plan 06. Warning W#8 (viewingUserMxid sourcing) is resolved by this plan — the mxid comes from `useViewingUserMxid` hook, not from any ambient auth context or prop-threading contortion.

Output: the pane skeleton mounts, renders participants + history from props (WS subscription and send-round-trip stubbed until Plan 06), correctly handles empty-room (D-17) + error-state (D-18) + attachment-placeholder (D-19). NO edits to pretty view (D-01/D-03 — except the D-03 waiver landed in Wave 0 Plan 00 for the mechanical contextPct swap, which does NOT apply here).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-CONTEXT.md
@.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-PATTERNS.md
@.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-RESEARCH.md
@.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-02-PLAN.md
@.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-04-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create relay-room-api.ts type contracts + error-state.tsx component + viewing-user-store.ts hook (W#8 resolution)</name>
  <files>
    src/ui/features/relay-room-pane/relay-room-api.ts,
    src/ui/features/relay-room-pane/error-state.tsx,
    src/ui/features/relay-room-pane/error-state.test.tsx,
    src/ui/state/viewing-user-store.ts,
    src/ui/state/viewing-user-store.test.ts
  </files>
  <read_first>
    src/ui/api/claude-session-api.ts (structural mirror — top-of-file JSDoc, openXSocket primitive, discriminated-union frame shapes at L25-100 for server events + L429 for client payloads),
    src/ui/features/pretty-view/PrettyViewErrorOverlay.tsx (analog for the error-state visual — dark card, warm-cream text, centered layout),
    src/ui/main-axios.ts (L109-117 UserInfo type — does NOT include mxid today; L1741 getUserInfo() — the existing user-info endpoint; W#8 resolution: viewingUserMxid needs a source, and the smallest-diff option is a new small backend endpoint `/users/me/mxid` OR extending UserInfo. Grep the /users/me endpoint on the backend side; if extending UserInfo is a 1-line backend change, that's simplest. Otherwise the store fetches from a dedicated endpoint. Planner picks and documents),
    src/ui/state/session-working-store.ts (structural mirror for the store shape — external-store subscription pattern via useSyncExternalStore; grep for the export shape),
    .planning/phases/90-.../90-PATTERNS.md § relay-room-api.ts + error-state.tsx sections,
    .planning/phases/90-.../90-04-PLAN.md (Plan 04's server-side frame shapes — MUST match on the frontend)
  </read_first>
  <behavior>
    - relay-room-api.ts Type Test 1: `RelayRoomServerEvent` discriminated union carries `session` | `history_batch` | `live_event` | `send_ack` | `send_error` | `participants` | `error` | `inactive` — every member matches the Plan 04 server-side frame shape byte-for-byte (field names, optionality, types).
    - relay-room-api.ts Type Test 2: `RelayRoomClientPayload` discriminated union carries `connectToRoom` | `send_message` | `fetch_older_range` — matches Plan 04's client-side frame shape.
    - relay-room-api.ts Type Test 3: `openRelayRoomSocket()` returns a `WebSocket` pointed at `/relay-room/websocket/` with `wss:` (if location.protocol === 'https:') or `ws:` otherwise (mirror openClaudeSessionSocket at L14-23).
    - relay-room-api.ts Type Test 4: `MatrixEvent` type mirrors the Plan 03 backend shape — minimum fields `event_id`, `type`, `sender`, `origin_server_ts`, `content`, `unsigned?.transaction_id` (Pitfall 4 correlation field).

    - error-state Test 5: renders 'This conversation is no longer available.' as visible text.
    - error-state Test 6: does NOT render a retry button (grep for role='button' inside — should be 0 or absent).
    - error-state Test 7: optional `subline` prop renders 'You may have been removed from this room.' when provided.
    - error-state Test 8: body content rendered via React text children — no `dangerouslySetInnerHTML`.

    - viewing-user-store Test 9 (W#8): `useViewingUserMxid()` returns `null` on initial mount; after the fetch resolves, returns the mxid string; on refetch, returns the updated value (reactive).
    - viewing-user-store Test 10: fetch failure (401 or network error) leaves the value as null; a warning is logged via structured console.warn (NEVER JSON.stringify raw error).
    - viewing-user-store Test 11: the fetch is fired ONCE per app-mount (idempotent — subsequent calls from other consumers return the cached value without re-fetching).
    - viewing-user-store Test 12: hook contract — subscribes via `useSyncExternalStore` (assert via render-count behavior when the underlying value changes).
  </behavior>
  <action>
    Create `src/ui/features/relay-room-pane/relay-room-api.ts` as a TYPE-ONLY wire contract module. Structure mirrors `src/ui/api/claude-session-api.ts`:

    - Top-of-file JSDoc naming the backend server (relay-room-stream-server from Plan 04) and the WS route (`/relay-room/websocket/`).
    - Export `openRelayRoomSocket(): WebSocket` — mirror `openClaudeSessionSocket` shape from claude-session-api.ts L14-23 with the URL path swapped to `/relay-room/websocket/`.
    - Export `MatrixEvent` type — minimum fields: `event_id: string`, `type: string`, `sender: string`, `origin_server_ts: number`, `content: Record<string, unknown>`, `unsigned?: { transaction_id?: string; [k: string]: unknown }`. The `unsigned.transaction_id` field is the Pitfall 4 correlation carrier — Plan 06 reads it to match pending sends.
    - Export `RelayRoomServerEvent` discriminated union covering the 8 frame types Plan 04's WS server emits: `session` | `history_batch` | `live_event` | `send_ack` | `send_error` | `participants` | `error` | `inactive`. Each member's field shape MUST match Plan 04's server-side emission byte-for-byte (grep Plan 04 for each frame's shape).
    - Export `RelayRoomClientPayload` discriminated union covering the 3 client-side payloads: `ConnectToRoomPayload { type: 'connectToRoom', roomId: string }`, `SendMessagePayload { type: 'send_message', body: string, txnId: string }` (txnId == mqid per Pitfall 4), `FetchOlderRangePayload { type: 'fetch_older_range', beforeEventId: string, count: number }` (count = 20 per D-14).
    - Export `HumanParticipant { mxid: string; displayName: string; userId: number }` and `AgentParticipant { mxid: string; identityKey: string }` for the `participants` frame and the REST endpoint response.
    - Export `RelayRoomParticipantsResponse { humans: HumanParticipant[]; agents: AgentParticipant[] }` for the Plan 04 `GET /relay-room/:roomId/participants` endpoint.

    NO runtime code beyond `openRelayRoomSocket()`. Everything else is type-only. Do NOT add default values or runtime constants that would drift from Plan 04 server-side numbers (e.g., don't hardcode `count = 20` here — the consumer picks 20 per D-14).

    Create `src/ui/features/relay-room-pane/error-state.tsx`:
    - Export `RelayRoomErrorState({title, subline}: {title?: string; subline?: string})`.
    - Default `title` = 'This conversation is no longer available.'
    - Optional subline; when provided, renders below the title with a lighter treatment.
    - NO retry button (RESEARCH.md § Open Question 3 RESOLVED — there is nothing to retry).
    - Visual treatment: mirror `src/ui/features/pretty-view/PrettyViewErrorOverlay.tsx` — dark card, centered, warm-cream text. Grep the analog file at plan time for the exact class strings; copy them verbatim.
    - Body content rendered via React text children — NO `dangerouslySetInnerHTML`.

    Create `error-state.test.tsx` covering the 4 error-state behaviors above per PATTERNS.md § 7 test-file structure.

    **W#8 resolution — create `src/ui/state/viewing-user-store.ts`:**

    Small state module exposing `useViewingUserMxid(): string | null`. Implementation choice (grep-based selection at plan time):

    - **Option A (RECOMMENDED — smallest-diff)**: extend the existing `getUserInfo()` return (`src/ui/main-axios.ts` L109-117 `UserInfo`) to include `mxid?: string`. The backend `/users/me` endpoint (grep the exact route in `src/backend/database/routes/user-admin-routes.ts` or similar) then returns `users.mxid` alongside the existing fields. Frontend store fires `getUserInfo()` once on app-mount, caches the response, exposes `useViewingUserMxid()` that reads the cached `mxid` field. If the backend endpoint doesn't easily accept a `mxid` field addition, fall back to Option B.

    - **Option B**: dedicated endpoint `GET /users/me/mxid` returning `{mxid: string}`. New small route module `src/backend/database/routes/user-mxid.ts` — mounted via `app.use("/users", userMxidRoutes)` (or extending the existing user-admin-routes with the new endpoint). Frontend store fires this endpoint once on mount.

    Regardless of chosen option, the store shape:
    ```
    let cachedMxid: string | null = null;
    let listeners = new Set<() => void>();
    let fetchInFlight = false;

    async function fetchViewingUserMxid() {
      if (fetchInFlight || cachedMxid !== null) return;
      fetchInFlight = true;
      try {
        // Option A: const info = await getUserInfo(); cachedMxid = info.mxid ?? null;
        // Option B: const {data} = await authApi.get<{mxid: string}>("/users/me/mxid"); cachedMxid = data.mxid;
        // ... update + notify listeners
      } catch (err) {
        console.warn({operation: "viewing_user_mxid_fetch_failed", err: (err as Error)?.message ?? "unknown"});
      } finally {
        fetchInFlight = false;
      }
    }

    export function useViewingUserMxid(): string | null {
      return useSyncExternalStore(
        (cb) => { listeners.add(cb); fetchViewingUserMxid(); return () => listeners.delete(cb); },
        () => cachedMxid,
        () => cachedMxid,
      );
    }
    ```

    Add tests in `viewing-user-store.test.ts` covering behaviors 9-12. Mock `authApi.get` / `getUserInfo` via `vi.mock`. Verify (a) initial null, (b) resolves to mxid after fetch, (c) subsequent subscribers get the cached value without re-fetching, (d) failure keeps null + logs a warning.

    **Backend endpoint touch**: if Option A is chosen, add the `mxid` field to whatever backend handler returns `UserInfo` (grep `users.mxid` reads under `src/backend/database/routes/` — likely `user-admin-routes.ts` or a `/users/me` handler; verify the change is 1-3 lines). If Option B, add the new route module. Backend build clean gate: `npm run build:backend` on the backend-touching sub-task.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build:backend &amp;&amp; npx vitest run src/ui/features/relay-room-pane/error-state.test.tsx src/ui/state/viewing-user-store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/features/relay-room-pane/relay-room-api.ts` exists and exports `openRelayRoomSocket`, `MatrixEvent`, `RelayRoomServerEvent`, `RelayRoomClientPayload`, `HumanParticipant`, `AgentParticipant`, `RelayRoomParticipantsResponse`.
    - `grep -c "/relay-room/websocket/" src/ui/features/relay-room-pane/relay-room-api.ts` returns >= 1 (URL constant used).
    - `grep -c "unsigned" src/ui/features/relay-room-pane/relay-room-api.ts` returns >= 1 (Pitfall 4 correlation field present on MatrixEvent).
    - `grep -c "\"connectToRoom\"\\|\"send_message\"\\|\"fetch_older_range\"" src/ui/features/relay-room-pane/relay-room-api.ts` returns >= 3 (all client payload types present).
    - `grep -c "\"session\"\\|\"history_batch\"\\|\"live_event\"\\|\"send_ack\"\\|\"send_error\"\\|\"participants\"\\|\"error\"\\|\"inactive\"" src/ui/features/relay-room-pane/relay-room-api.ts` returns >= 8 (all server event types present).
    - `src/ui/features/relay-room-pane/error-state.tsx` + test file exist.
    - `grep -c "This conversation is no longer available" src/ui/features/relay-room-pane/error-state.tsx` returns >= 1.
    - `grep -c "dangerouslySetInnerHTML" src/ui/features/relay-room-pane/error-state.tsx` returns 0.
    - `grep -c "retry\\|Retry" src/ui/features/relay-room-pane/error-state.tsx` returns 0 (no retry button per D-18 recommendation).
    - `src/ui/state/viewing-user-store.ts` + test file exist.
    - `grep -c "export function useViewingUserMxid\\|export const useViewingUserMxid" src/ui/state/viewing-user-store.ts` returns exactly 1 (W#8 hook).
    - `grep -c "useSyncExternalStore" src/ui/state/viewing-user-store.ts` returns >= 1 (store subscription pattern).
    - `grep -c "\"viewing_user_mxid_fetch_failed\"" src/ui/state/viewing-user-store.ts` returns >= 1 (structured warn log).
    - `grep -c "JSON.stringify(" src/ui/state/viewing-user-store.ts` returns 0 (structured-logging discipline).
    - `npx tsc --noEmit` exits 0.
    - `npm run build:backend` exits 0 (if backend touch in Option A/B).
    - `npx vitest run src/ui/features/relay-room-pane/error-state.test.tsx src/ui/state/viewing-user-store.test.ts` — all 12 tests pass.
    - No modification to `src/ui/features/pretty-view/`.
  </acceptance_criteria>
  <done>
    Type contracts + error-state component + viewing-user-store hook land under `src/ui/features/relay-room-pane/` + `src/ui/state/`. Tasks 2 + 3 in this plan and all of Plan 06 can import from the type module + the viewing-user hook without further wire-shape work. W#8 resolved. Zero pretty-view modification.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build RelayMessageList + IdentityBadgeRow components (render-only; consuming the FORKED RelayRoomInboundBubble per D-12 fork clarification)</name>
  <files>
    src/ui/features/relay-room-pane/RelayMessageList.tsx,
    src/ui/features/relay-room-pane/RelayMessageList.test.tsx,
    src/ui/features/relay-room-pane/IdentityBadgeRow.tsx,
    src/ui/features/relay-room-pane/IdentityBadgeRow.test.tsx
  </files>
  <read_first>
    src/ui/features/relay-room-pane/RelayRoomInboundBubble.tsx (Plan 02 Task 3 output — the FORKED expanded-always bubble; this task consumes it, NOT the pretty-view RelayInboundBubble which stays untouched per D-03),
    src/ui/features/pretty-view/LoadMoreOlderButton.tsx (D-14 reuse target — pure component with hasOlder/status/error/onClick props; do NOT modify),
    src/ui/components/OutboundBubble.tsx (Plan 02 primitive — props: content, ts, pendingState, attachments),
    src/ui/features/terminal/IdentityBadge.tsx (D-08 badge primitive; NOTE: it currently uses `absolute top-4 right-5 z-[101]` positioning per L102 — the row wrapper must establish a non-absolute positioning context OR the inner render fragment must be extracted; planner-flex per PATTERNS.md § IdentityBadgeRow.tsx note),
    src/ui/features/pretty-view/PrettyView.tsx (message-list render + fetch_older_range_batch handler at L2077-2100 for the frame-dispatch pattern; do NOT modify),
    .planning/phases/90-.../90-PATTERNS.md § RelayMessageList.tsx + IdentityBadgeRow.tsx sections
  </read_first>
  <behavior>
    - RelayMessageList Test 1: given `history: MatrixEvent[]` prop with two events (one from another user, one from viewingUserMxid), renders one `RelayRoomInboundBubble` for the other-user event and one `OutboundBubble` for the viewing user's event.
    - RelayMessageList Test 2: viewing-user event with `unsigned.transaction_id` matching a pending mqid in `pendingSends` prop -> renders the OutboundBubble with `pendingState={null}` (Plan 06 will actually remove the pending entry; this plan just wires the render side).
    - RelayMessageList Test 3: inbound event whose content has an `msgtype: 'm.image'` or `m.file` or contains an `info.filename` / `body: 'attachment: <name>'` shape renders the minimal D-19 placeholder text `attachment: <filename>` — no image fetch, no thumbnail. The placeholder text is passed as the `body` prop to `RelayRoomInboundBubble`.
    - RelayMessageList Test 4: LoadMoreOlderButton renders at the top of the list with `hasOlder` prop derived from props (`hasOlder: boolean`); clicking fires the parent's `onLoadOlder` callback.
    - RelayMessageList Test 5: empty history array -> renders an empty middle (no bubbles), NOT an empty-state chrome message (D-17 — the empty middle IS the state; the pane's compose bar which Plan 06 adds handles the 'what does the user do' question).
    - RelayMessageList Test 6: message body rendered via React text children — no `dangerouslySetInnerHTML`.
    - RelayMessageList Test 7 (D-12 fork consumption verification): the import path resolves to `./RelayRoomInboundBubble` (the fork under `src/ui/features/relay-room-pane/`), NOT `../pretty-view/RelayInboundBubble` (the original). Grep the import statement.

    - IdentityBadgeRow Test 1: given `humans: [{mxid, displayName: 'Zoe', userId: 2}, {mxid, displayName: 'Ashley', userId: 1}]`, `agents: [{mxid, identityKey: 'nelly'}, {mxid, identityKey: 'ada'}]`, `viewingUserMxid: <ashley-mxid>`, renders (in order): Zoe (human), ada (agent), nelly (agent). Ashley is excluded (viewing-user filter per D-07). Humans first, then agents; alphabetical within each role.
    - IdentityBadgeRow Test 2: per-human badge renders WITHOUT any appendage — grep for the appendage marker (test data attribute `data-appendage="true"` on the appendage wrapper) returns 0 on the human badges. Absence of the appendage IS how the viewer distinguishes humans from agents per D-09.
    - IdentityBadgeRow Test 3: per-agent badge renders a PLACEHOLDER cell (Plan 06 will replace this with `AgentBadgeWithAppendage`). The placeholder MUST render the plain badge (so the row layout is stable across plans) and expose a `data-slot="agent-badge-appendage"` container for Plan 06 to fill. This plan does NOT render the meter or reset.
    - IdentityBadgeRow Test 4: narrow-viewport behavior — the row uses `overflow-x-auto` so on narrow viewports the badges scroll horizontally (D-20 reasonable fallback).
    - IdentityBadgeRow Test 5: badges use the existing `IdentityBadge` primitive — grep confirms the import (`import { IdentityBadge } from ...`).
    - IdentityBadgeRow Test 6: absolute positioning of the IdentityBadge is neutralized — the badge in the row wrapper is NOT `position: absolute` (either via a wrapper establishing a non-absolute context, or via an inner-render extraction; planner picks per PATTERNS.md § IdentityBadgeRow.tsx note).
  </behavior>
  <action>
    Create `src/ui/features/relay-room-pane/RelayMessageList.tsx`. This component is pure-render for now — Plan 06 wires the WS. Props:
    - `history: MatrixEvent[]` (parent passes; ordered oldest -> newest).
    - `viewingUserMxid: string` (used to discriminate inbound vs outbound).
    - `pendingSends: Array<{mqid: string; content: string; state: 'sending' | 'failed'}>` (parent passes; Plan 06 wires the state machine).
    - `hasOlder: boolean`, `loadOlderStatus: 'idle' | 'in-flight' | 'error'`, `loadOlderError: string | null`, `onLoadOlder: () => void` (LoadMoreOlderButton props passthrough).
    - `roomTitle: string | null` (passed to RelayRoomInboundBubble as the `room` prop per its contract).

    Render flow (mirror PrettyView.tsx L2077-2100 dispatch pattern; do NOT modify PrettyView):
    1. Top: `<LoadMoreOlderButton hasOlder={hasOlder} status={loadOlderStatus} error={loadOlderError} onClick={onLoadOlder} />` — reuse-as-is per D-14.
    2. For each event in history (oldest -> newest):
       - If `event.sender === viewingUserMxid`: render `<OutboundBubble content={extractBody(event)} ts={event.origin_server_ts} pendingState={findPending(event.unsigned?.transaction_id, pendingSends)?.state ?? null} />`. `findPending` looks up by `unsigned.transaction_id == mqid` (Pitfall 4 correlation).
       - Otherwise (inbound): render `<RelayRoomInboundBubble room={roomTitle ?? ''} sender={event.sender} body={extractBody(event)} ts={event.origin_server_ts} />` — consumes the FORKED bubble from Plan 02 Task 3 (D-12 fork clarification). Import path: `./RelayRoomInboundBubble` (relative — the fork lives in the same directory as RelayMessageList). NOTE: the fork has NO `hostId` prop (removed with the pointer-detect machinery) so the caller doesn't need to pass one.
       - Attachment handling (D-19): if the event's content has `msgtype` in ('m.image', 'm.file', 'm.video', 'm.audio') OR `content.info?.filename` OR `content.url` (Matrix mxc://), extract the filename (from `content.body` or `content.info.filename`) and pass `body: 'attachment: <filename>'` to the bubble instead of the raw content body. No image fetch, no thumbnail, no mxc:// resolution.

    Body-extraction helper `extractBody(event: MatrixEvent): string`: returns `event.content.body` if it's a string; otherwise returns the empty string. NEVER passes non-string content to bubble body. Handles the attachment D-19 case as above.

    NO WS state, NO subscription — the parent (RelayRoomPane, Plan 06 wires) manages history + pendingSends state and passes them down.

    **CRITICAL — D-12 fork consumption:** the import for the inbound bubble MUST be from `./RelayRoomInboundBubble` (the Plan 02 Task 3 fork). Do NOT import from `../pretty-view/RelayInboundBubble` (that's the original, which stays untouched for pretty view per D-03). Test 7 grep-verifies the import path.

    Create `src/ui/features/relay-room-pane/IdentityBadgeRow.tsx`. Props:
    - `humans: HumanParticipant[]` (from Plan 04 endpoint response).
    - `agents: AgentParticipant[]` (same).
    - `viewingUserMxid: string`.

    Render flow (per D-07 humans-first-alphabetical + D-09 humans-no-appendage):
    1. `humansOther = humans.filter(h => h.mxid !== viewingUserMxid).sort((a, b) => a.displayName.localeCompare(b.displayName))`.
    2. `agentsSorted = [...agents].sort((a, b) => a.identityKey.localeCompare(b.identityKey))`.
    3. Outer container: `<div className="flex flex-row items-start gap-2 px-3 py-2 overflow-x-auto" data-testid="relay-room-identity-badge-row">`.
    4. For each human: render `<HumanBadgeCell {...human} />` — this cell wraps `IdentityBadge` in a wrapper that neutralizes the absolute positioning (either wrap in a `relative` container or extract the inner render fragment from IdentityBadge L122-190; planner picks — the simpler path is a `<div className="relative">` wrapper that establishes the positioning context).
    5. For each agent: render `<AgentBadgeCellPlaceholder {...agent} />` — a placeholder cell that renders the plain badge PLUS an empty container with `data-slot="agent-badge-appendage"` for Plan 06 to swap out with the full AgentBadgeWithAppendage component.

    NO appendage on human badges (D-09). NO meter, NO reset on any badge in this plan (Plan 06 adds those to the agent badges). The placeholder ensures the row layout is stable when Plan 06 lands.

    Create tests for both components in the matching `.test.tsx` files per PATTERNS.md § 7 test-file structure. Use `@testing-library/react` and `fireEvent` for the LoadMoreOlderButton click test. Use a `makeMatrixEvent` factory helper for message-list tests.

    NO modification to `src/ui/features/pretty-view/*.tsx`. Import LoadMoreOlderButton from pretty-view but do not modify it. RelayRoomInboundBubble is the Plan 02 Task 3 fork — imported from the sibling relay-room-pane directory, NOT from pretty-view.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run src/ui/features/relay-room-pane/RelayMessageList.test.tsx src/ui/features/relay-room-pane/IdentityBadgeRow.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/features/relay-room-pane/RelayMessageList.tsx` + test file exist.
    - `src/ui/features/relay-room-pane/IdentityBadgeRow.tsx` + test file exist.
    - `grep -c "import.*RelayRoomInboundBubble" src/ui/features/relay-room-pane/RelayMessageList.tsx` returns >= 1 (D-12 fork consumption — Test 7).
    - `grep -c "import.*RelayInboundBubble\"" src/ui/features/relay-room-pane/RelayMessageList.tsx` returns 0 (NOT importing the pretty-view original; the fork name is `RelayRoomInboundBubble`; the trailing double-quote in the grep is defensive against partial matches).
    - `grep -c "\\.\\./pretty-view/RelayInboundBubble" src/ui/features/relay-room-pane/RelayMessageList.tsx` returns 0 (NOT importing from pretty-view).
    - `grep -c "import.*OutboundBubble" src/ui/features/relay-room-pane/RelayMessageList.tsx` returns >= 1 (Plan 02 primitive).
    - `grep -c "import.*LoadMoreOlderButton" src/ui/features/relay-room-pane/RelayMessageList.tsx` returns >= 1 (D-14 reuse).
    - `grep -c "import.*IdentityBadge" src/ui/features/relay-room-pane/IdentityBadgeRow.tsx` returns >= 1 (D-08 badge primitive).
    - `grep -c "dangerouslySetInnerHTML" src/ui/features/relay-room-pane/RelayMessageList.tsx src/ui/features/relay-room-pane/IdentityBadgeRow.tsx` returns 0.
    - `grep -c "\"attachment:\"" src/ui/features/relay-room-pane/RelayMessageList.tsx` returns >= 1 (D-19 placeholder emitted).
    - `grep -c "unsigned.transaction_id\\|unsigned\\?.transaction_id\\|unsigned\\[.transaction_id" src/ui/features/relay-room-pane/RelayMessageList.tsx | head -1` returns non-zero count (Pitfall 4 correlation read site present).
    - `grep -c "viewingUserMxid" src/ui/features/relay-room-pane/IdentityBadgeRow.tsx` returns >= 1 (D-07 self-exclusion prop).
    - `grep -c "data-slot=\"agent-badge-appendage\"" src/ui/features/relay-room-pane/IdentityBadgeRow.tsx` returns >= 1 (Plan 06 slot marker present).
    - `grep -c "overflow-x-auto" src/ui/features/relay-room-pane/IdentityBadgeRow.tsx` returns >= 1 (D-20 narrow-viewport fallback).
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run src/ui/features/relay-room-pane/RelayMessageList.test.tsx src/ui/features/relay-room-pane/IdentityBadgeRow.test.tsx` — all 13 behavior tests pass.
    - No file under `src/ui/features/pretty-view/` is modified (`git diff --name-only src/ui/features/pretty-view/` returns empty).
  </acceptance_criteria>
  <done>
    RelayMessageList + IdentityBadgeRow render correctly from props (WS wiring comes in Plan 06). D-07 humans-first + D-09 humans-no-appendage + D-12 fork consumption (RelayRoomInboundBubble, NOT the pretty-view original) + D-13 OutboundBubble consumption + D-14 LoadMoreOlderButton reuse + D-19 attachment placeholder honored. Zero pretty-view modification.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build RelayRoomPane top-level component composing IdentityBadgeRow + RelayMessageList + participant fetch + error-state gate + viewing-user hook consumption (W#8)</name>
  <files>
    src/ui/features/relay-room-pane/RelayRoomPane.tsx,
    src/ui/features/relay-room-pane/RelayRoomPane.test.tsx
  </files>
  <read_first>
    src/ui/features/relay-room-pane/IdentityBadgeRow.tsx (Task 2 output),
    src/ui/features/relay-room-pane/RelayMessageList.tsx (Task 2 output),
    src/ui/features/relay-room-pane/error-state.tsx (Task 1 output),
    src/ui/features/relay-room-pane/relay-room-api.ts (Task 1 output — RelayRoomParticipantsResponse shape),
    src/ui/state/viewing-user-store.ts (Task 1 output — useViewingUserMxid hook; W#8 resolution),
    src/ui/features/pretty-view/PrettyView.tsx (structural model for pane layout — top area for badges, middle scroller for messages, bottom for compose; do NOT modify),
    src/ui/api/main-axios.ts (authApi helper for fetching the participants endpoint),
    .planning/phases/90-.../90-PATTERNS.md § RelayRoomPane.tsx section (component structure + WS setup pattern to mirror in Plan 06; this plan handles only the render + participant fetch + viewing-user consumption)
  </read_first>
  <behavior>
    - Test 1: on mount, calls `authApi.get('/relay-room/:roomId/participants')` (via a small module-local wrapper) with the roomId prop; sets internal state to the response.
    - Test 2: passes `humans` + `agents` + `viewingUserMxid` down to `IdentityBadgeRow`. `viewingUserMxid` comes from `useViewingUserMxid()` (W#8), NOT from a prop.
    - Test 3: passes `history` + `viewingUserMxid` + `pendingSends` + LoadMoreOlder props down to `RelayMessageList`. In this plan `history` starts as an empty array + `pendingSends` empty + `hasOlder = false` + `loadOlderStatus = 'idle'` — Plan 06 replaces these with real WS-derived state.
    - Test 4: renders a bottom placeholder slot (a `<div data-slot="compose-box" />` cell) that Plan 06 will fill with the `ComposeBoxShell` invocation.
    - Test 5: participant fetch 404 (from Plan 04 endpoint) -> renders `<RelayRoomErrorState />` in place of the badge-row + message-list + compose slot (D-18 friendly error state).
    - Test 6: participant fetch 401 (session expired) -> falls back to a generic 'session expired' error state (planner-flex; can be a simpler render path than D-18 — planner picks; log via console.warn).
    - Test 7: empty-room state — participant fetch succeeds with `humans: [otherUser]`, `agents: []`, and `history = []` -> renders the badge row (with the one other human) + empty middle (no bubbles) + compose slot (D-17: empty middle + normal compose bar; NO empty-state chrome).
    - Test 8: viewing-user-only room (rare — user is alone in the room) -> humans list filters out the viewer, agents empty -> renders an EMPTY badge row (no badges) + empty middle + compose slot. This is still not an error state; the pane renders normally.
    - Test 9: structured logging on mount — `console.info({ operation: 'relay_room_pane_mount', roomId, ... })`. NEVER pass raw response body or event objects to console.info (PATTERNS.md § 2).
    - Test 10 (W#8 wiring): if `useViewingUserMxid()` returns null (fetch still pending), the pane renders a small loading state (or renders the badge row with the human list INCLUDING what would otherwise be the viewing user — since we can't filter without knowing the mxid). Planner picks the graceful-degradation strategy; document the choice. RECOMMENDED: wait for viewingUserMxid before mounting IdentityBadgeRow (small loading spinner or blank badge row area for the ~50ms until the initial user-info fetch resolves).
  </behavior>
  <action>
    Create `src/ui/features/relay-room-pane/RelayRoomPane.tsx`. Props:
    - `roomId: string` (from the tab; Plan 07 wires this from the sessionKind branch).
    - `roomTitle: string | null`.
    - `isVisible: boolean` (per-tab visibility flag from the shell — mirrors PrettyView's `isVisible` prop; Plan 06 uses it to gate WS connect/disconnect).
    - `className?: string`.

    NOTE: `viewingUserMxid` is NOT a prop — it's sourced via `useViewingUserMxid()` from `@/state/viewing-user-store` (W#8 resolution). The Plan 07 wrapper (RelayRoomSessionPane) does NOT need to thread the mxid — the pane fetches it itself.

    Component body (this plan lands render + participant fetch + viewing-user consumption only; Plan 06 adds WS + compose + optimistic-send):
    1. `const viewingUserMxid = useViewingUserMxid();` — W#8 hook, may be null initially.
    2. Fetch participants on mount via `authApi.get('/relay-room/' + encodeURIComponent(roomId) + '/participants')` (encodeURIComponent on the client side too, defense-in-depth). Store in `participants` state. Handle 404 -> set `errorState = 'room-not-found'`. Handle 401 -> set `errorState = 'auth-expired'`. Handle other errors -> log-and-render error state.
    3. Layout structure (flex column filling parent):
       ```
       <div className={cn('h-full w-full flex flex-col', props.className)}>
         {errorState === 'room-not-found' && <RelayRoomErrorState />}
         {errorState === 'auth-expired' && <RelayRoomErrorState title="Session expired" subline="Please refresh to sign back in." />}
         {!errorState && viewingUserMxid !== null && (
           <>
             <IdentityBadgeRow humans={participants?.humans ?? []} agents={participants?.agents ?? []} viewingUserMxid={viewingUserMxid} />
             <RelayMessageList history={history} viewingUserMxid={viewingUserMxid} pendingSends={pendingSends} hasOlder={false} loadOlderStatus="idle" loadOlderError={null} onLoadOlder={() => { }} roomTitle={roomTitle} />
             <div data-slot="compose-box" className="shrink-0" /> {/* Plan 06 fills */}
           </>
         )}
         {!errorState && viewingUserMxid === null && (
           <div className="flex items-center justify-center h-full text-sm text-[rgba(232,228,216,0.5)]">Loading…</div>
         )}
       </div>
       ```
    4. Local state: `participants` (typed as `RelayRoomParticipantsResponse | null`), `errorState` (typed as `'room-not-found' | 'auth-expired' | null`), `history` (empty array in this plan; Plan 06 replaces), `pendingSends` (empty array in this plan; Plan 06 replaces).
    5. Structured mount logging: `useEffect(() => { console.info({ operation: 'relay_room_pane_mount', roomId, initialIsVisible: isVisible }); }, []);` — never pass raw response bodies.

    Tests in `RelayRoomPane.test.tsx`. Mock `authApi.get` via `vi.mock('@/api/main-axios')` (or the correct import path). Mock `useViewingUserMxid` via `vi.mock('@/state/viewing-user-store')` returning a controllable value (null initially, then a stable mxid). Use a `makeParticipantsResponse` factory helper. Cover the 10 behaviors above.

    NO WS code in this plan. NO compose-box code. NO per-agent badge appendage (Plan 06 owns those). This plan is intentionally scoped to render + participant fetch + error gate + viewing-user hook consumption so Plan 06 has a clean seam to wire in.

    NO modification to `src/ui/features/pretty-view/*.tsx` (D-01/D-03). Import LoadMoreOlderButton from pretty-view only in Task 2 files; this task does not touch pretty-view imports.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run src/ui/features/relay-room-pane/RelayRoomPane.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/features/relay-room-pane/RelayRoomPane.tsx` + test file exist.
    - `grep -c "authApi.get.*relay-room.*participants\\|/relay-room/.*participants" src/ui/features/relay-room-pane/RelayRoomPane.tsx` returns >= 1 (participant fetch wired).
    - `grep -c "useViewingUserMxid" src/ui/features/relay-room-pane/RelayRoomPane.tsx` returns >= 1 (W#8 hook wired — NOT a prop).
    - `grep -c "viewingUserMxid: string" src/ui/features/relay-room-pane/RelayRoomPane.tsx` returns 0 (mxid is NOT a prop on RelayRoomPane).
    - `grep -c "import.*IdentityBadgeRow\\|from.*IdentityBadgeRow" src/ui/features/relay-room-pane/RelayRoomPane.tsx` returns >= 1.
    - `grep -c "import.*RelayMessageList\\|from.*RelayMessageList" src/ui/features/relay-room-pane/RelayRoomPane.tsx` returns >= 1.
    - `grep -c "import.*RelayRoomErrorState\\|from.*error-state" src/ui/features/relay-room-pane/RelayRoomPane.tsx` returns >= 1.
    - `grep -c "data-slot=\"compose-box\"" src/ui/features/relay-room-pane/RelayRoomPane.tsx` returns >= 1 (Plan 06 slot marker).
    - `grep -c "relay_room_pane_mount" src/ui/features/relay-room-pane/RelayRoomPane.tsx` returns >= 1 (structured logging).
    - `grep -c "JSON.stringify(" src/ui/features/relay-room-pane/RelayRoomPane.tsx` returns 0 (structured-logging discipline).
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run src/ui/features/relay-room-pane/RelayRoomPane.test.tsx` — all 10 tests pass.
    - No file under `src/ui/features/pretty-view/` is modified.
  </acceptance_criteria>
  <done>
    RelayRoomPane skeleton composes IdentityBadgeRow (top) + RelayMessageList (middle) + a `data-slot="compose-box"` cell (bottom, Plan 06 fills). Participant fetch wired against the Plan 04 REST endpoint with D-18 error-state gate. `viewingUserMxid` sourced from `useViewingUserMxid()` hook (W#8 resolution). Empty room (D-17) + attachment placeholder (D-19 via Task 2) + humans-alphabetical-then-agents-alphabetical (D-07 via Task 2) + humans-no-appendage (D-09 via Task 2) all render correctly. Zero pretty-view modification.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 04 REST/WS -> browser components | Backend has already gated access + scrubbed tokens. Frontend consumes typed responses. |
| Matrix event body -> DOM render | Untrusted sender content; render via React text children only. |
| viewingUserMxid via useViewingUserMxid hook -> row filter | Sourced from `getUserInfo()` / `/users/me/mxid` (backend-authoritative, JWT-gated); used for D-07 self-exclusion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-05-T1 | Tampering (XSS) | RelayMessageList body render, error-state | mitigate | React text children only; grep gate enforces `dangerouslySetInnerHTML` count = 0 across all three new files. Preserves T-17-03-01 discipline. |
| T-90-05-T2 | Tampering (SSRF) | Attachment placeholder rendering | mitigate | D-19 renders `attachment: <filename>` only; NEVER auto-fetches mxc:// URLs or remote media. The FORKED RelayRoomInboundBubble has no fetch call at all (Plan 02 Task 3 grep gate ensures `fetch(` count = 0 in the fork). |
| T-90-05-I1 | Information Disclosure | D-18 error-state copy | mitigate | Copy is deliberately non-specific ('no longer available'); does NOT distinguish 'kicked' vs 'never a member' — Plan 04 backend already returns same-code for both. Frontend renders the same UI regardless. |
| T-90-05-I2 | Information Disclosure | Structured logging | mitigate | Never pass raw response bodies to console.info; PATTERNS.md § 2 discipline. Grep gate enforces `JSON.stringify(` count = 0 in new files. |
| T-90-05-I3 | Information Disclosure | viewing-user-store cached mxid | accept | mxid is not a secret (it's the user's own public identifier already visible in the room to other participants). Cached in-memory only, never persisted, never sent anywhere other than the initial fetch. |
| T-90-FE-01 | Optimistic-send correlation (pending -> real replacement) | RelayMessageList | mitigate (partial) | This plan wires the render-side lookup (`unsigned.transaction_id == mqid` via `findPending` helper); Plan 06 wires the pending-send state machine and calls the correlation. Split across plans because the state machine is compose-box-adjacent. |

`security_enforcement=true`. T-90-FE-01 completion depends on Plan 06 (compose + optimistic send). T-90-BE-* threats already mitigated in Plan 04 backend; this plan trusts those gates.
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npm run build:backend` clean (if Option A/B backend touch in Task 1).
- `npx vitest run` scoped to all touched test files reports all tests passing.
- No modification to `src/ui/features/pretty-view/` (D-01/D-03 upheld — the D-03 waiver landed in Wave 0 Plan 00 does not apply here).
- Grep gates in acceptance criteria satisfied (dangerouslySetInnerHTML = 0, JSON.stringify = 0, humans-alphabetical + D-07 self-exclusion wiring, D-09 no-appendage wiring, D-19 placeholder wiring, D-18 error-state wiring, D-12 fork consumption verification, W#8 useViewingUserMxid wiring).
</verification>

<success_criteria>
Relay pane skeleton mounts, renders participants + empty message list + compose-box slot placeholder. All render-side D-decisions (D-07, D-09, D-11, D-12 fork consumption, D-13, D-14, D-17, D-18, D-19) honored. `viewingUserMxid` sourced via `useViewingUserMxid()` hook (W#8 resolution — new `src/ui/state/viewing-user-store.ts`). Plan 06 can drop the WS hook + compose box + per-agent badge appendage into the pre-wired slots without further scaffolding. Zero pretty-view modification.
</success_criteria>

<output>
Create `.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-05-SUMMARY.md` when done.
</output>
