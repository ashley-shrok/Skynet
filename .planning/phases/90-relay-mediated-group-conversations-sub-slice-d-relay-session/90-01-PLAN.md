---
phase: 90-relay-mediated-group-conversations-sub-slice-d-relay-session
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/api/sessions-api.ts
  - src/ui/api/sessions-api.test.ts
  - src/ui/state/conversation-store.ts
  - src/ui/state/conversation-store.test.ts
  - src/types/ui-types.ts
autonomous: true
requirements: [D-01-scope-anchor, D-15-kind-discriminator-consumption]
tags: [types, discriminator, session-list, tab-model, foundation, slice-d, kind-marker]
must_haves:
  truths:
    - "Frontend `RemoteTmuxSession` carries the optional `kind: 'harness' | 'relay-room'` discriminator returned by the Phase 89 Plan 04 /sessions/list handler."
    - "Frontend `FleetSession` (sidebar row model) carries the same `kind` marker plus `roomId`/`roomTitle` fields so a relay-room row-click can propagate roomId to the tab-open handler."
    - "Frontend `Tab` type carries `sessionKind`/`relayRoomId`/`relayRoomTitle` so downstream pane-mounting dispatch (Plan 07) can branch without re-reading the sidebar store."
    - "Backward-compat: consumers reading `kind` treat `undefined` as `'harness'` (matches pre-Phase-89 rehydrated caches)."
  artifacts:
    - path: "src/ui/api/sessions-api.ts"
      provides: "RemoteTmuxSession widened with optional kind + relay fields."
      contains: "kind?"
    - path: "src/ui/state/conversation-store.ts"
      provides: "FleetSession widened with kind + roomId + roomTitle; bumped localStorage cache-version constant to invalidate stale objects."
      contains: "roomId?"
    - path: "src/types/ui-types.ts"
      provides: "Tab widened with sessionKind + relayRoomId + relayRoomTitle."
      contains: "sessionKind?"
  key_links:
    - from: "src/ui/state/conversation-store.ts"
      to: "src/ui/api/sessions-api.ts"
      via: "type re-use for kind marker"
      pattern: "kind\\?: \"harness\" \\| \"relay-room\""
    - from: "src/types/ui-types.ts"
      to: "src/ui/state/conversation-store.ts"
      via: "row -> tab shape carries relay fields at tab-open time"
      pattern: "relayRoomId\\?: string"
---

<objective>
Widen three frontend type surfaces (API response, sidebar store row, tab model) to carry the Phase-89 `kind` discriminator + relay-room fields (roomId, roomTitle) end-to-end from `/sessions/list` down to the tab that will host the pane. This plan lands ONLY the type widening + backward-compat defense; no orchestration, no branching, no new UI. Downstream plans (07) will consume these fields at the tab-mount site.

Purpose: Interface-first ordering (per planner playbook). Every subsequent plan in this phase reads or writes these types; landing them in Wave 1 unblocks parallel work on the shared primitives (Plan 02), backend Matrix primitives (Plan 03), and the WS server (Plan 04) with no scavenger hunts for the discriminator shape.

Output: `RemoteTmuxSession` / `FleetSession` / `Tab` widened + tests updated + cache-version bumped so stale localStorage entries invalidate cleanly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-CONTEXT.md
@.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-RESEARCH.md
@.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-PATTERNS.md
@.planning/phases/89-relay-mediated-group-conversations-sub-slice-b-session-model/89-04-SUMMARY.md
@src/backend/database/routes/sessions-merge-helper.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen RemoteTmuxSession + FleetSession + Tab with kind discriminator and relay-room fields</name>
  <files>
    src/ui/api/sessions-api.ts,
    src/ui/api/sessions-api.test.ts,
    src/ui/state/conversation-store.ts,
    src/ui/state/conversation-store.test.ts,
    src/types/ui-types.ts
  </files>
  <read_first>
    src/backend/database/routes/sessions-merge-helper.ts (Phase 89 SessionListItem discriminated-union — HarnessSessionRow / RelayRoomSessionRow / SessionListItem; kind is 'harness' | 'relay-room' kebab-case),
    src/ui/api/sessions-api.ts (current RemoteTmuxSession shape at ~L4-14; getSessionList() call site),
    src/ui/state/conversation-store.ts (current FleetSession shape at ~L145-166; localStorage cache-version constant sequence ~L1049-1074, currently v3),
    src/types/ui-types.ts (current Tab interface),
    .planning/phases/90-.../90-PATTERNS.md § 'src/ui/api/sessions-api.ts' widen section (backward-compat rule: kind undefined -> harness),
    .planning/phases/90-.../90-RESEARCH.md § Code Example 3 (Kind-Discriminator Branch Wiring — target field names + optionality)
  </read_first>
  <behavior>
    - Test 1 (sessions-api.test.ts): `RemoteTmuxSession` typed as accepting `kind: 'harness'` + harness fields (hostId, sessionName, created, role, lastMessageAt, aiTitle) OR `kind: 'relay-room'` + relay fields (id, roomId, roomTitle, lastActivityAt, createdAt, updatedAt); `kind` field is OPTIONAL at the type level (backward-compat with pre-Phase-89 caches).
    - Test 2 (sessions-api.test.ts): `getSessionList()` deserialization test with a fixture where response body is a merged list containing one `kind: 'harness'` row and one `kind: 'relay-room'` row — both round-trip through the type without cast/assertion.
    - Test 3 (conversation-store.test.ts): FleetSession accepts optional `kind`, `roomId`, `roomTitle`; a FleetSession with `kind: 'relay-room'` and `roomId: '!abc:server'` is constructable and stored/retrievable through the existing store surface.
    - Test 4 (conversation-store.test.ts): localStorage cache-version bump — reading a persisted cache written under the OLD version returns empty/reset state (invalidation semantics preserved; do NOT attempt migration).
    - Test 5 (conversation-store.test.ts): backward-compat — a FleetSession retrieved with `kind === undefined` is treated as harness by any code path that switches on kind (grep for the discriminator read sites; if none exist yet, this is a no-op assertion at type level).
  </behavior>
  <action>
    Widen three type surfaces to carry the Phase-89 kind discriminator + relay-room fields per D-15 consumption. Field name is `kind` (not `type`) — matches Phase 89 Plan 04 wire-protocol lock. Union values are `'harness' | 'relay-room'` kebab-case.

    In `src/ui/api/sessions-api.ts`: extend the exported `RemoteTmuxSession` interface with these OPTIONAL fields — `kind?: 'harness' | 'relay-room'`, `id?: string`, `roomId?: string`, `roomTitle?: string | null`, `lastActivityAt?: string | null`, `createdAt?: string`, `updatedAt?: string`. Keep every existing harness field as-is (do not tighten). Preserve the exported name (`RemoteTmuxSession`) — do NOT rename to `RemoteSession` in this plan (keeps call-site diff small). A discriminated-union refactor is optional and only if it does not cascade into >5 call-site edits; if it does, keep the flat-optional shape.

    In `src/ui/state/conversation-store.ts`: extend the exported `FleetSession` type with the same three fields — `kind?: 'harness' | 'relay-room'`, `roomId?: string`, `roomTitle?: string | null`. Bump the localStorage cache-version constant (currently `v3` per the sequence around L1049-1074) to `v4` so persisted objects without the new fields invalidate cleanly on next mount rather than being deserialized into a stale shape. The bump is the only migration mechanism — no runtime data migration code.

    In `src/types/ui-types.ts`: extend the `Tab` interface with `sessionKind?: 'harness' | 'relay-room'`, `relayRoomId?: string`, `relayRoomTitle?: string | null`. Field name is `sessionKind` (not `kind`) on `Tab` to avoid collision with any existing `kind` field on tab-related types; if grep confirms no collision, `kind` is also acceptable — planner's discretion, document the choice in the Tab JSDoc.

    Add tests in the two existing test files (create `sessions-api.test.ts` if missing — follow PATTERNS.md § 7 test-file structure) covering the five behaviors above. Backward-compat defense: any code path that reads `kind` MUST treat `undefined` as `'harness'` (per PATTERNS.md backward-compat rule); if such a read site already exists somewhere touched by this widening, adjust it accordingly (unlikely — this plan is type-only).

    NO edits to `src/ui/features/pretty-view/*` (D-01/D-03). NO new orchestration or branching (that's Plan 07). NO backend touches. Scoped tests only per fleet rule.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run src/ui/api/sessions-api.test.ts src/ui/state/conversation-store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "kind\\?: \"harness\" \\| \"relay-room\"" src/ui/api/sessions-api.ts src/ui/state/conversation-store.ts` returns matches in both files.
    - `grep -n "sessionKind\\?: " src/types/ui-types.ts` returns exactly one match on the Tab interface.
    - `grep -n "relayRoomId\\?: string" src/types/ui-types.ts` returns exactly one match.
    - localStorage cache-version constant in `src/ui/state/conversation-store.ts` incremented (grep the version literal; must be one greater than pre-plan value).
    - `npx tsc --noEmit` exits 0 (frontend build clean).
    - `npx vitest run src/ui/api/sessions-api.test.ts src/ui/state/conversation-store.test.ts` reports every new test passing; no pre-existing test regresses (compare pre/post pass counts on the touched files).
    - No file under `src/ui/features/pretty-view/` is modified (`git diff --name-only` shows zero entries there).
    - No file under `src/backend/` is modified.
  </acceptance_criteria>
  <done>
    Frontend type surfaces carry the Phase-89 kind discriminator + relay-room fields; existing tests still pass; cache-version bump invalidates stale localStorage; every downstream plan can consume the new fields without further type work.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /sessions/list response -> frontend | Backend-controlled JSON; already-validated Phase 89 rows. New optional fields are additive; no untrusted input introduced by this plan. |
| localStorage rehydration | Client-controlled storage; version bump invalidates stale shapes on cache-version mismatch. No new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-01-T1 | Tampering | FleetSession localStorage cache | mitigate | Cache-version bump (`v3` -> `v4`) forces re-hydrate from server on first mount; old-shape objects are discarded, not re-typed. |
| T-90-01-I1 | Information Disclosure | Widened type surface | accept | No PII added; roomId + roomTitle are already returned by the Phase 89 backend to the same authenticated user. No new server data exposed. |
| T-90-01-E1 | Elevation of Privilege | Discriminator field | accept | Backend is source of truth for kind assignment; frontend treating `undefined` as harness is a safe fallback (never elevates a harness row into relay-room orchestration). |

`security_enforcement=true` — no high-severity threats introduced by type widening. This is a foundation plan; downstream backend plans (03, 04) carry the T-90-BE-01/02/03 threats.
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npx vitest run` scoped to the touched test files reports zero regressions.
- `git diff --stat` limited to `src/ui/api/sessions-api*.ts`, `src/ui/state/conversation-store*.ts`, `src/types/ui-types.ts` — no pretty-view or backend touches.
- Grep gates in acceptance criteria satisfied.
</verification>

<success_criteria>
Type widening lands with backward-compat defense; downstream plans (02, 03, 04, 05, 06, 07) can consume `kind` / `roomId` / `roomTitle` / `sessionKind` / `relayRoomId` / `relayRoomTitle` fields without further type work. Zero regression on pretty view (D-01/D-03).
</success_criteria>

<output>
Create `.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-01-SUMMARY.md` when done.
</output>
