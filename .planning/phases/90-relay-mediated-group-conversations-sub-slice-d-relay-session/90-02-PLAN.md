---
phase: 90-relay-mediated-group-conversations-sub-slice-d-relay-session
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/components/OutboundBubble.tsx
  - src/ui/components/OutboundBubble.test.tsx
  - src/ui/components/ComposeBoxShell.tsx
  - src/ui/components/ComposeBoxShell.test.tsx
  - src/ui/features/relay-room-pane/RelayRoomInboundBubble.tsx
  - src/ui/features/relay-room-pane/RelayRoomInboundBubble.test.tsx
autonomous: true
requirements: [D-02, D-02b, D-02c, D-03, D-04, D-05, D-06, D-12, D-12-fork-clarification, D-13]
tags: [primitives, extraction, outbound-bubble, compose-shell, room-inbound-bubble-fork, shared-components, slice-d]
must_haves:
  truths:
    - "`OutboundBubble` renders a right-aligned user-turn bubble with the ChatMessage isUser visual verbatim (gradient + border + shadow + text color) and supports pendingState 'sending' | 'failed' | null per D-13."
    - "`ComposeBoxShell` renders a textarea + Send button with the ComposeBox Row-2 visual verbatim and exposes `upperArea` + `attachButton` slot props so pretty view (future) can fill Row-1 and the relay pane (Plan 06) can pass null per D-04/D-05."
    - "`RelayRoomInboundBubble` is a FORK (COPY-extraction) of `RelayInboundBubble` that renders EXPANDED by default, has NO collapse/expand toggle, and does NOT run pointer-detection — because relay-room bubbles come from the relay directly (D-11) not from parsed transcripts, so the pointer-detect path has no work to do (D-12 fork clarification per Ashley 2026-09-08 verbatim: *\"in relay sessions, the collapsed nature of relay bubbles would not be what we want. And there really wouldn't be a collapse feature in the relay sessions because it doesn't make sense.\"*)."
    - "Pretty view (`src/ui/features/pretty-view/*.tsx`) is NOT modified by this plan — the primitives + fork are COPIES, not shared imports that pretty view is migrated onto (D-03). The original RelayInboundBubble stays in pretty view untouched."
    - "All three components render body content via React text children — never `dangerouslySetInnerHTML` — matching the fleet-wide security discipline (T-17-03-01 preserved for the fork)."
  artifacts:
    - path: "src/ui/components/OutboundBubble.tsx"
      provides: "Standalone right-aligned user-bubble primitive with pendingState + attachments props."
      contains: "export function OutboundBubble"
    - path: "src/ui/components/ComposeBoxShell.tsx"
      provides: "Compose textarea + Send button shell with upperArea + attachButton slots."
      contains: "export function ComposeBoxShell"
    - path: "src/ui/features/relay-room-pane/RelayRoomInboundBubble.tsx"
      provides: "Forked expanded-always sender-attributed inbound bubble for the relay-room pane (D-12 fork clarification)."
      contains: "export function RelayRoomInboundBubble"
    - path: "src/ui/components/OutboundBubble.test.tsx"
      provides: "Behavior tests for pending/failed/settled variants + attachments."
      contains: "describe(\"OutboundBubble\""
    - path: "src/ui/components/ComposeBoxShell.test.tsx"
      provides: "Behavior tests for Enter-to-send, Shift-Enter newline, upperArea slot, attachButton slot, canSend disabled state."
      contains: "describe(\"ComposeBoxShell\""
    - path: "src/ui/features/relay-room-pane/RelayRoomInboundBubble.test.tsx"
      provides: "Behavior tests for expanded-always render + no-collapse-toggle + no-pointer-detect."
      contains: "describe(\"RelayRoomInboundBubble\""
  key_links:
    - from: "src/ui/components/OutboundBubble.tsx"
      to: "src/ui/features/pretty-view/ChatMessage.tsx"
      via: "visual verbatim copy of isUser branch — NOT a runtime dependency"
      pattern: "bg-\\[linear-gradient\\(160deg,rgba\\(45,55,80,0.55\\)"
    - from: "src/ui/components/ComposeBoxShell.tsx"
      to: "src/ui/features/pretty-view/ComposeBox.tsx"
      via: "visual verbatim copy of Row 2 — NOT a runtime dependency"
      pattern: "min-h-8!"
    - from: "src/ui/features/relay-room-pane/RelayRoomInboundBubble.tsx"
      to: "src/ui/features/pretty-view/RelayInboundBubble.tsx"
      via: "COPY-extraction — same sender-hue visual encoding + left-alignment + resolved-identity dot; NOT a runtime dependency; no collapse machinery, no pointer-detect"
      pattern: "resolveMxidToIdentity"
---

<objective>
Extract THREE shared / forked primitives that the relay pane will consume (Plans 05 + 06):

1. **`OutboundBubble`** — the right-aligned user-turn bubble visual, COPIED from ChatMessage's isUser branch (ChatMessage.tsx L420-594) with the assistant path stripped and the pendingState/attachments contract preserved (D-02.b + D-13).

2. **`ComposeBoxShell`** — the compose textarea + Send button visual shell, COPIED from ComposeBox.tsx Row 2 (L2635-2740) with `upperArea?: ReactNode` + `attachButton?: ReactNode` slot props so pretty view (future) can fill Row 1 and the relay pane (Plan 06) can pass null per D-04/D-05.

3. **`RelayRoomInboundBubble`** — FORK (COPY-extraction) of `RelayInboundBubble` (`src/ui/features/pretty-view/RelayInboundBubble.tsx`) that renders EXPANDED by default, has NO collapse/expand toggle, and does NOT run pointer-detection. Per Ashley 2026-09-08 (verbatim during plan-checker review): *"in relay sessions, the collapsed nature of relay bubbles would not be what we want. And there really wouldn't be a collapse feature in the relay sessions because it doesn't make sense."* D-11 establishes relay-room bubbles come from the relay directly (not parsed transcripts) — the pointer-detect path from the original was for the transcript-parsed case and has no work to do here. Preserves the sender-hue visual encoding + left-alignment + resolved-identity dot from the original.

Purpose: D-02 says share truly-primitive pieces only; D-03 says do NOT migrate pretty view onto the extracted primitives in this slice — the strongest guarantee pretty view does not regress is not editing it. The primitives are COPIES living alongside their originals; a future convergence slice migrates ChatMessage/ComposeBox/RelayInboundBubble to consume them if drift becomes real. The RelayRoomInboundBubble fork specifically addresses the D-12 fork clarification Ashley locked during plan-checker review — the relay-room pane's inbound bubble has different UX requirements (always-expanded) from the harness-pane's peer-agent-chatter bubble (collapsed-by-default), and forking rather than parameterizing keeps the original untouched.

Output: three new primitive components (two shared under `src/ui/components/`, one relay-room-pane-scoped under `src/ui/features/relay-room-pane/`) with scoped tests, ready to be imported by Plans 05 (list) and 06 (compose + send).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-CONTEXT.md
@.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-PATTERNS.md
@.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract OutboundBubble primitive (COPY from ChatMessage isUser branch)</name>
  <files>
    src/ui/components/OutboundBubble.tsx,
    src/ui/components/OutboundBubble.test.tsx
  </files>
  <read_first>
    src/ui/features/pretty-view/ChatMessage.tsx (isUser branch at L420-594 — bubble wrapper, gradient, shadow, sending-spinner, attachments render; source of the verbatim copy),
    src/ui/features/pretty-view/RelayInboundBubble.tsx (L186 comment — 'body rendered via {body} in JSX (NOT dangerouslySetInnerHTML)' discipline to preserve),
    src/ui/features/pretty-view/AttachmentChipStrip.tsx (props contract for the attachments render branch at ChatMessage L525-572),
    src/ui/features/pretty-view/ChatMessage.test.tsx (isUser test subset — shape to mirror),
    .planning/phases/90-.../90-PATTERNS.md § OutboundBubble.tsx section (extraction rule + verbatim gradient + sending-spinner + attachments branch + what NOT to include list)
  </read_first>
  <behavior>
    - Test 1: renders content via React text child (query `screen.getByText('hello')` after `render(<OutboundBubble content="hello" />)`) and NOT via `dangerouslySetInnerHTML`.
    - Test 2: right-aligned wrapper — the outer `<div>` carries class `justify-end` (query by test id or by class match).
    - Test 3: `pendingState="sending"` renders the twin-arc Loader2 spinner (query `data-pv-bubble-spinner` or `[aria-hidden="true"]` inline within the bubble).
    - Test 4: `pendingState="failed"` sets `data-pv-bubble-failed="true"` on the bubble element and applies the red inline-style background/border.
    - Test 5: `pendingState={null}` renders no spinner and no failed marker.
    - Test 6: `attachments={[{filename:'a.png',size:100,mimetype:'image/png'}]}` + non-empty content renders the caption text above an AttachmentChipStrip with `readOnly=true`.
    - Test 7: no `!isUser` speak-button or assistant-branch DOM present (query for typical assistant markers returns null — proves the assistant path is stripped).
  </behavior>
  <action>
    Create `src/ui/components/OutboundBubble.tsx` as a COPY (not shared import) of ChatMessage.tsx's isUser branch (ChatMessage L420-594). Strip the `isUser` discriminator branch entirely — this primitive is user-only. Drop assistant-only machinery: `onLongPressSpeak`, `onOpenEditor`, `autoplayArmed`, the `injected` (parseInjectedUserTurn) branch (settled-attachment shape not needed here — the `attachments` prop covers pending-with-attachments), and the L595+ speak-button block.

    Props contract (per PATTERNS.md § OutboundBubble.tsx props contract):
    - `content: string` (rendered as React text child; NEVER dangerouslySetInnerHTML per RelayInboundBubble discipline).
    - `ts?: number` (optional; hover-title timestamp via `<div title={new Date(ts).toLocaleString()}>`).
    - `pendingState?: 'sending' | 'failed' | null`.
    - `attachments?: Array<{filename: string; size: number; mimetype: string}>`.

    Preserve VERBATIM from ChatMessage.tsx L420-594:
    - Bubble wrapper `<div>` with `justify-end` and inline-style-driven failed-red treatment (`background: 'hsla(0, 60%, 35%, 0.90)'`, `borderColor: 'hsla(0, 70%, 50%, 0.85)'`).
    - `data-pv-bubble-failed="true"` attribute in failed state.
    - User-branch gradient block (ChatMessage L465-477): `bg-[linear-gradient(160deg,rgba(45,55,80,0.55),rgba(28,35,55,0.6))]`, text color `#dfe3ee`, border `rgba(120,140,180,0.2)`, shadow multi-layer.
    - `rounded-[var(--radius-pv-bubble)]`, `px-[12px] py-[7px]`, `backdrop-blur-xl saturate-150`, `[-webkit-backdrop-filter:blur(20px)_saturate(1.6)]`.
    - Loader2 spinner block (ChatMessage L581-594) with `data-pv-bubble-spinner`, `aria-hidden`, `animate-spin opacity-70`.
    - Attachments-with-caption branch (ChatMessage L525-572) rendering the caption then `AttachmentChipStrip` with `readOnly=true` and no-op `onRemove`.

    Create scoped tests in `src/ui/components/OutboundBubble.test.tsx` per PATTERNS.md § 7 test-file structure. Use vitest + @testing-library/react + jest-dom/vitest. A `makeProps` factory helper for defaults.

    Extraction discipline: DO NOT modify `src/ui/features/pretty-view/ChatMessage.tsx` (D-03). If a shared style constant or provider context is needed, inline it as a local constant in `OutboundBubble.tsx` rather than importing it — the primitive must be standalone so ChatMessage is not indirectly affected (RESEARCH.md Assumption A1).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run src/ui/components/OutboundBubble.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/components/OutboundBubble.tsx` and `src/ui/components/OutboundBubble.test.tsx` exist.
    - `grep -c "dangerouslySetInnerHTML" src/ui/components/OutboundBubble.tsx` returns 0.
    - `grep -c "bg-\\[linear-gradient(160deg,rgba(45,55,80,0.55),rgba(28,35,55,0.6))\\]" src/ui/components/OutboundBubble.tsx` returns >= 1 (verbatim gradient present).
    - `grep -c "data-pv-bubble-failed" src/ui/components/OutboundBubble.tsx` returns >= 1.
    - `grep -c "data-pv-bubble-spinner" src/ui/components/OutboundBubble.tsx` returns >= 1.
    - `grep -c "isUser" src/ui/components/OutboundBubble.tsx` returns 0 (no discriminator branch survived the strip).
    - No file under `src/ui/features/pretty-view/` is modified in this task (`git diff --name-only src/ui/features/pretty-view/` returns empty).
    - `npx vitest run src/ui/components/OutboundBubble.test.tsx` reports all 7 behavior tests passing.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    OutboundBubble primitive lives under src/ui/components/ with the ChatMessage isUser visual verbatim + pending/failed/settled + attachments contract; pretty view untouched; ready for Plans 05 + 06 to import.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract ComposeBoxShell primitive with upperArea + attachButton slot props</name>
  <files>
    src/ui/components/ComposeBoxShell.tsx,
    src/ui/components/ComposeBoxShell.test.tsx
  </files>
  <read_first>
    src/ui/features/pretty-view/ComposeBox.tsx (Row 2 at L2635-2740 — textarea + Send button visual; NOTE: full file is 3608 lines, only extract Row 2 verbatim — grep for the 'VISUAL-08' comment marker for the Send-button gradient hard-lock),
    src/ui/components/textarea.tsx (shadcn Textarea primitive — imports),
    src/ui/components/button.tsx (shadcn Button primitive — imports),
    src/ui/features/pretty-view/ComposeBox.test.tsx (Row-2 test subset — shape to mirror; extract only the tests relevant to the shell surface, not the meter/reset/queue tests),
    .planning/phases/90-.../90-PATTERNS.md § ComposeBoxShell.tsx section (extraction rule D-04+D-06, Row 2 wrapper, textarea styling verbatim, twin-arc spinner, props contract),
    .planning/phases/90-.../90-RESEARCH.md § Code Example 2 (props shape: value/onChange/onSend/upperArea/attachButton/placeholder/canSend/className)
  </read_first>
  <behavior>
    - Test 1: renders textarea + Send button; typing into the textarea updates the controlled value (fire input change; assert value prop).
    - Test 2: Enter (without shift) on the textarea fires `onSend` with the trimmed value AND clears the textarea; Shift+Enter fires no onSend and inserts a newline (or preserves textarea default newline behavior).
    - Test 3: Empty / whitespace-only text — Enter does NOT fire onSend; Send button is disabled.
    - Test 4: `canSend={false}` disables the Send button regardless of text (grep the button `disabled` attribute).
    - Test 5: `upperArea` slot — passing `upperArea={<div data-testid="upper">upper</div>}` renders the node above the textarea row; passing `upperArea={undefined}` renders nothing above the textarea (grep DOM order).
    - Test 6: `attachButton` slot — passing `attachButton={<button data-testid="attach">A</button>}` renders adjacent to the textarea; passing `attachButton={undefined}` renders nothing.
    - Test 7: `placeholder` prop propagates to the textarea `placeholder` attribute (default: 'Type a message…' when not supplied).
    - Test 8: click on Send button with valid text fires onSend with trimmed value.
  </behavior>
  <action>
    Create `src/ui/components/ComposeBoxShell.tsx` as a COPY (not shared import) of ComposeBox.tsx Row 2 (L2635-2740). Do NOT extract Row 1 (meter/reset/queue/stop/thumbs-up/recap) — that's pretty-view-specific per D-04. Expose it via slot props so pretty view (future convergence slice) could fill Row 1 and the relay pane passes null.

    Props contract (per RESEARCH.md § Code Example 2 + PATTERNS.md § ComposeBoxShell.tsx props contract):
    - `value: string`, `onChange: (v: string) => void` — CONTROLLED textarea (the shell does not own state; pane orchestration does, so the pane can also drive optimistic-send clear semantics from outside). Alternative: internal-state variant with `onSend` only — planner-flex, but controlled is preferred because Plan 06 needs to programmatically clear on optimistic-send success/failure paths.
    - `onSend: (text: string) => void` — fired on Enter (without shift) and Send-button click; the shell passes the trimmed text.
    - `upperArea?: ReactNode` — slot rendered above the textarea row; null / undefined -> nothing rendered (D-04 for relay pane).
    - `attachButton?: ReactNode` — slot rendered adjacent to the textarea; null / undefined -> nothing rendered (D-05 for relay pane — attach button HIDDEN, not disabled).
    - `placeholder?: string` — textarea placeholder (default: 'Type a message…').
    - `canSend?: boolean` — external disable gate; when false, Send button is disabled regardless of text.
    - `className?: string` — outer container class merge via `cn()`.

    Preserve VERBATIM from ComposeBox.tsx L2635-2740:
    - Row 2 outer wrapper `<div className="flex items-end gap-2">` (items-end so Send pins to textarea bottom edge as it grows).
    - Textarea styling classes: `resize-none w-full h-full`, `min-h-8!` (shadcn Textarea default is min-h-[80px]; `!` overrides — load-bearing), `bg-[rgba(10,12,20,0.5)]!` and `text-[#f0ebe0]` (per patch #82 — `!` load-bearing).
    - Send-button VISUAL-08 hard-lock amber gradient — copy from ComposeBox.tsx Row-2 block below L2740 verbatim (grep for `VISUAL-08` and `amber` at plan time to locate the exact constants). Do NOT alter the gradient.
    - Enter-to-send keyboard handler: `if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); const trimmed = text.trim(); if (trimmed.length === 0) return; onSend(trimmed); onChange(''); }` — the shell fires onSend with the trimmed text and clears via onChange('').

    Create scoped tests in `src/ui/components/ComposeBoxShell.test.tsx` per PATTERNS.md § 7 test-file structure covering the 8 behaviors above. Use vitest + @testing-library/react + fireEvent for keyboard + click.

    Extraction discipline: DO NOT modify `src/ui/features/pretty-view/ComposeBox.tsx` (D-03). Extract as a standalone primitive; ComposeBox continues consuming its own private Row 2 verbatim. RESEARCH.md § Code Example 2 note: 'Pretty view's ComposeBox is a 3608-line file that will NOT consume this primitive in slice D (D-03)'.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run src/ui/components/ComposeBoxShell.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/components/ComposeBoxShell.tsx` and `src/ui/components/ComposeBoxShell.test.tsx` exist.
    - `grep -c "upperArea" src/ui/components/ComposeBoxShell.tsx` returns >= 2 (prop declared + prop consumed in JSX).
    - `grep -c "attachButton" src/ui/components/ComposeBoxShell.tsx` returns >= 2.
    - `grep -c "min-h-8!" src/ui/components/ComposeBoxShell.tsx` returns >= 1 (verbatim override present).
    - `grep -c "e.key === \"Enter\" && !e.shiftKey" src/ui/components/ComposeBoxShell.tsx | grep -v '^#' | wc -l` returns >= 1 (Enter-to-send handler present).
    - `grep -c "dangerouslySetInnerHTML" src/ui/components/ComposeBoxShell.tsx` returns 0.
    - No file under `src/ui/features/pretty-view/` is modified in this task.
    - `npx vitest run src/ui/components/ComposeBoxShell.test.tsx` reports all 8 behavior tests passing.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    ComposeBoxShell primitive lives under src/ui/components/ with textarea + Send button visual verbatim + upperArea + attachButton slot props + Enter-to-send + canSend disable gate; pretty view untouched; ready for Plan 06 to import with both slots null (D-04 + D-05).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fork RelayRoomInboundBubble as expanded-always COPY of RelayInboundBubble (D-12 fork clarification, Ashley 2026-09-08)</name>
  <files>
    src/ui/features/relay-room-pane/RelayRoomInboundBubble.tsx,
    src/ui/features/relay-room-pane/RelayRoomInboundBubble.test.tsx
  </files>
  <read_first>
    src/ui/features/pretty-view/RelayInboundBubble.tsx (FULL 226-line file — the source of the COPY. Key structural landmarks: L69-83 props + identity resolution via `resolveMxidToIdentity`; L78 `const [collapsed, setCollapsed] = useState(true);` — REMOVE in fork; L90-95 bubble hue + inline-style — PRESERVE VERBATIM; L98-128 `detectFilePointer` + fetchState + useEffect — REMOVE ENTIRELY in fork per D-11 (relay-room bubbles come from relay directly, not parsed transcripts, so there is no pointer to detect); L130-152 outer wrapper + bubble div + hue-tinted classes — PRESERVE; L143 collapsed/expanded padding branch — collapse to expanded-only (`px-[18px] py-[14px]`); L154-181 collapse-toggle button — REMOVE ENTIRELY, replace with a static header div carrying avatar-dot + displayName + room; L184-222 collapsed-gated body render — INLINE the body render unconditionally (no more `!collapsed &&` gate)),
    src/ui/features/pretty-view/relay-mxid-resolve.ts (`resolveMxidToIdentity(mxid, byKey)` — reused verbatim),
    src/ui/state/identities-store.ts (`useIdentities()` — reused verbatim),
    src/ui/features/pretty-view/RelayInboundBubble.test.tsx (test-file structure to mirror — extract the identity-resolution + hue-encoding tests; DROP the collapse-toggle tests; DROP the pointer-detect tests),
    .planning/phases/90-.../90-RESEARCH.md § D-12 FORK CLARIFICATION (Ashley 2026-09-08 verbatim quote — the load-bearing quote justifying the fork)
  </read_first>
  <behavior>
    - Test 1: renders EXPANDED by default — the body content is visible on initial render without any user interaction. Query `screen.getByText(body)` returns the element without needing to click anything first.
    - Test 2: NO collapse/expand toggle button in the DOM — grep the render for `role="button"` should return 0 (or only the outer wrapper if it happens to have a role, but no toggle behavior). More specifically: `aria-expanded` attribute is NOT present anywhere in the bubble DOM.
    - Test 3: NO click handler on the header — clicking the sender-name / avatar-dot / room-label area does NOT toggle any state (verify via `fireEvent.click` on the header area + assert body remains visible with no re-render assertion change).
    - Test 4: NO pointer-detection is invoked — pass a body containing a pretend pointer-shape string (e.g., `[cwd:xyz] path/to/file.txt (12 lines)`) and verify that (a) the `📄` file icon marker does NOT appear (the pointer-detect path would insert this), (b) `fetch()` is NOT called (mock global fetch via `vi.spyOn(global, 'fetch')` and assert call count is 0), (c) the raw body text renders verbatim.
    - Test 5: identity resolution preserved — mxid resolved to `{colorHue, displayName}` via `resolveMxidToIdentity`; the avatar-dot data-attribute carries the resolved color; the displayName is visible in the header (same behavior as the original).
    - Test 6: hue-tinted bubble background preserved — the inline `background` style carries the sender-hue gradient (`hsla(${bubbleHue},50%,38%,0.55)` shape) — VERBATIM from the original at L92.
    - Test 7: unresolved mxid falls back to hue 210 (Ashley 2026-08-18 "don't really care about the fallback") — VERBATIM behavior from the original at L90.
    - Test 8: left-alignment preserved — outer wrapper carries `justify-start` (mirror the original's L131 `data-testid="relay-inbound-wrap"` OR a distinct `data-testid="relay-room-inbound-wrap"` for the fork; planner picks a distinct testid to avoid cross-file test collisions).
    - Test 9: body rendered via React text child (Security T-17-03-01 preserved) — grep the file for `dangerouslySetInnerHTML` returns 0; `screen.getByText(body)` matches verbatim.
    - Test 10: `ts` prop propagates to the bubble div's `title` attribute for hover-timestamp (same as the original at L133).
    - Test 11: NO "via recv.sh" footer — the original's L212-220 footer is dropped (relay-room bubbles don't come via recv.sh; they come via the relay-room WS server per Plan 04, so the attribution would be misleading). Planner-flex: keep the footer removed OR replace with a room-appropriate attribution OR drop the footer entirely — the cleanest option is drop.
  </behavior>
  <action>
    Create `src/ui/features/relay-room-pane/RelayRoomInboundBubble.tsx` as a COPY-extraction (NOT a shared import) of `src/ui/features/pretty-view/RelayInboundBubble.tsx`. This is the D-12 fork clarification per Ashley 2026-09-08 (verbatim quote: *"in relay sessions, the collapsed nature of relay bubbles would not be what we want. And there really wouldn't be a collapse feature in the relay sessions because it doesn't make sense."*).

    **Verbatim-preserve from the source file:**
    - Imports: `useIdentities` from `@/state/identities-store`, `resolveMxidToIdentity` from `../pretty-view/relay-mxid-resolve` (or copy the resolver inline if D-03 forbids importing across the pretty-view boundary — grep the D-03 waiver scope; the resolver is a pure helper with no runtime state so importing is likely safe. Planner picks; document the choice).
    - `NEUTRAL_GREY` constant (L54).
    - Props shape adapted:
      ```
      export interface RelayRoomInboundBubbleProps {
        room: string;         // room title or room ID (planner picks)
        sender: string;       // mxid of the sender
        body: string;         // message body (rendered as React text child)
        ts?: number;          // ms-epoch timestamp; rendered as hover title
        // NO hostId prop — the original's hostId was for the /relay-pointer fetch;
        // this fork has no pointer-detect so hostId is not needed.
      }
      ```
    - Identity resolution: `const { byKey } = useIdentities(); const { colorHue, displayName } = resolveMxidToIdentity(sender, byKey);`.
    - `avatarColor` (L82-83) — verbatim.
    - `bubbleHue` fallback to 210 (L90) — verbatim.
    - `bubbleStyle` inline `background` + `borderColor` + `boxShadow` (L91-95) — verbatim.
    - Outer wrapper `<div className="flex justify-start">` — verbatim (L131), planner picks a distinct `data-testid` (e.g., `relay-room-inbound-wrap`) to avoid test-file collision with the original.
    - Bubble div classes VERBATIM from L137-152 EXCEPT: replace the collapsed/expanded padding branch (L143 `collapsed ? "px-[12px] py-[7px]" : "px-[18px] py-[14px]"`) with the expanded-only variant `"px-[18px] py-[14px]"` (no ternary).
    - Header: replace the L154-181 collapse-toggle button with a static `<div>` (NOT `<button>`) carrying the same avatar-dot + displayName + room content:
      ```
      <div
        data-testid="relay-room-inbound-header"
        className={cn(
          "flex items-center gap-1 text-xs mb-1",
          "text-[rgba(232,_228,_216,_0.6)]",
          "font-[JetBrains_Mono_Variable,ui-monospace,monospace]",
        )}
      >
        <span
          data-testid="relay-room-inbound-avatar-dot"
          data-avatar-color={avatarColor}
          aria-hidden="true"
          className="inline-block w-2 h-2 rounded-full flex-shrink-0"
          style={{ color: avatarColor, backgroundColor: avatarColor }}
        />
        {displayName} · {room}
      </div>
      ```
      NO collapse-toggle glyph (`▶` / `▼` at L180), NO `aria-expanded`, NO onClick handler, NO `cursor-pointer bg-transparent border-0` button-styling — this is a static presentational header.
    - Body: inline the body render UNCONDITIONALLY (no `!collapsed &&` gate at L184). Just:
      ```
      <div data-testid="relay-room-inbound-body">
        <div className="whitespace-pre-wrap">{body}</div>
      </div>
      ```
      NO pointer-detect branch, NO `fetch()`, NO `📄` icon rendering, NO fetch-state machinery, NO useEffect for fetch, NO "via recv.sh" footer (drop entirely — see behavior Test 11).

    **Remove entirely from the source (D-12 fork clarification):**
    - `useState<boolean>(true)` for `collapsed` (L78).
    - `useState<FetchState>` (L100).
    - `detectFilePointer` import + call (L6 + L98).
    - `useEffect` for pointer-fetch (L102-128).
    - `FetchState` type (L47-51).
    - The collapse-toggle button entirely.
    - The pointer-detect body branch entirely (L187-206).
    - The "via recv.sh" footer (L212-220).

    **Preserve security discipline (T-17-03-01):** body rendered as React text child, NEVER `dangerouslySetInnerHTML`. Grep-verify.

    Create `RelayRoomInboundBubble.test.tsx` covering the 11 behaviors above. Mock `useIdentities()` via `vi.mock('@/state/identities-store')`. Use `vi.spyOn(global, 'fetch')` to prove no fetch is called (Test 4). Use a `makeProps` factory helper.

    Extraction discipline: DO NOT modify `src/ui/features/pretty-view/RelayInboundBubble.tsx` (D-03). The fork is a COPY. If a shared style constant is needed, inline it (mirror the discipline used in Task 1 for OutboundBubble). The original RelayInboundBubble stays in pretty view untouched; a future convergence slice can migrate PrettyView to consume a parameterized primitive if drift becomes real.

    Docblock at the top of `RelayRoomInboundBubble.tsx` explicitly states the fork rationale + Ashley quote so future maintainers understand why there are two nearly-identical files (avoids naive "let's dedupe" refactor attempts).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run src/ui/features/relay-room-pane/RelayRoomInboundBubble.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/features/relay-room-pane/RelayRoomInboundBubble.tsx` + test file exist.
    - `grep -c "export function RelayRoomInboundBubble" src/ui/features/relay-room-pane/RelayRoomInboundBubble.tsx` returns exactly 1.
    - `grep -c "useState" src/ui/features/relay-room-pane/RelayRoomInboundBubble.tsx` returns 0 (no collapse state, no fetch state — the fork is stateless).
    - `grep -c "detectFilePointer\\|fetchState\\|FetchState" src/ui/features/relay-room-pane/RelayRoomInboundBubble.tsx` returns 0 (pointer-detect entirely removed).
    - `grep -c "aria-expanded\\|onClick.*setCollapsed" src/ui/features/relay-room-pane/RelayRoomInboundBubble.tsx` returns 0 (no collapse toggle).
    - `grep -c "fetch(" src/ui/features/relay-room-pane/RelayRoomInboundBubble.tsx` returns 0 (no runtime fetch — the pointer-fetch path is gone).
    - `grep -c "useEffect" src/ui/features/relay-room-pane/RelayRoomInboundBubble.tsx` returns 0 (the only useEffect in the source was for the pointer-fetch; it's gone).
    - `grep -c "resolveMxidToIdentity" src/ui/features/relay-room-pane/RelayRoomInboundBubble.tsx` returns >= 1 (identity resolution preserved).
    - `grep -c "bubbleHue\\|colorHue" src/ui/features/relay-room-pane/RelayRoomInboundBubble.tsx` returns >= 2 (sender-hue visual encoding preserved).
    - `grep -c "justify-start" src/ui/features/relay-room-pane/RelayRoomInboundBubble.tsx` returns >= 1 (left-alignment preserved).
    - `grep -c "dangerouslySetInnerHTML" src/ui/features/relay-room-pane/RelayRoomInboundBubble.tsx` returns 0 (T-17-03-01 security discipline preserved).
    - `grep -c "via recv.sh" src/ui/features/relay-room-pane/RelayRoomInboundBubble.tsx` returns 0 (footer dropped).
    - No file under `src/ui/features/pretty-view/` is modified in this task (`git diff --name-only src/ui/features/pretty-view/` returns empty — the original RelayInboundBubble stays put).
    - `npx vitest run src/ui/features/relay-room-pane/RelayRoomInboundBubble.test.tsx` reports all 11 behavior tests passing.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    RelayRoomInboundBubble fork lives under `src/ui/features/relay-room-pane/` with expanded-always render, no collapse machinery, no pointer-detect, preserved sender-hue encoding + left-alignment + resolved-identity dot; pretty view's original RelayInboundBubble untouched. Ready for Plan 05 Task 2 (RelayMessageList) to import as the inbound-bubble renderer for the relay-room pane.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bubble body content -> DOM | User's own composed text (OutboundBubble) OR another participant's relayed text (RelayRoomInboundBubble) is rendered back. The primitive discipline (React text children only) applies uniformly so the same primitives are safe for both trusted-source and untrusted-source content. |
| Textarea input | User-controlled input; standard React controlled textarea posture. No SSR / no innerHTML. |
| Sender mxid → identity resolution | Backend-authoritative mxid (Phase 88); resolved on the client via useIdentities + resolveMxidToIdentity; unresolved falls to neutral grey / hue 210. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-02-T1 | Tampering (XSS) | OutboundBubble body render | mitigate | Body rendered via React text child; grep gate enforces `dangerouslySetInnerHTML` count = 0. Preserves RelayInboundBubble discipline. |
| T-90-02-T2 | Tampering (XSS) | ComposeBoxShell textarea | mitigate | Standard React controlled textarea; no innerHTML; grep gate enforces `dangerouslySetInnerHTML` count = 0. |
| T-90-02-T3 | Tampering (XSS) | RelayRoomInboundBubble body render | mitigate | Body rendered via React text child (T-17-03-01 preserved from the source). Grep gate enforces `dangerouslySetInnerHTML` count = 0. |
| T-90-02-T4 | SSRF / Tampering | Pointer-fetch path REMOVED in fork | accept | The fork does NOT call fetch (grep gate enforces count = 0 for `fetch(`). Zero SSRF surface. The pointer-detect + fetch path was a source-file feature that has no work to do for relay-room bubbles (D-11 relay bubbles come direct, not parsed). |
| T-90-02-R1 | Repudiation | Primitives + fork are copies, not shared with pretty view | accept | D-03 explicit — the strongest guarantee pretty view does not regress is not editing it. A future convergence slice migrates ChatMessage/ComposeBox/RelayInboundBubble onto the primitives if drift becomes real. |

`security_enforcement=true` — no high-severity threats; this plan is pure-frontend primitive extraction + fork. Backend threats (T-90-BE-*) do not apply.
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npx vitest run` scoped to the three new test files reports all tests passing.
- `git diff --stat src/ui/features/pretty-view/` returns empty (D-01 + D-03 upheld — pretty view is not modified; the D-03 waiver applies only to Wave 0 PrettyView.tsx contextPct swap and Wave 0 ComposeBox.tsx reset-dispatch rewire, NOT to this Wave 1 plan).
- Grep gates in acceptance criteria satisfied — most importantly the fork's `useState=0` + `fetch=0` + `useEffect=0` gates enforcing that the collapse + pointer-detect machinery is entirely removed.
</verification>

<success_criteria>
Three primitives land: two shared under `src/ui/components/` (OutboundBubble + ComposeBoxShell) and one relay-room-pane-scoped under `src/ui/features/relay-room-pane/` (RelayRoomInboundBubble fork). Pretty view is not modified (D-01/D-03). Plan 05 (message list) can import OutboundBubble (via `@/components/OutboundBubble`) + RelayRoomInboundBubble (from `./RelayRoomInboundBubble`); Plan 06 (compose + send) can import ComposeBoxShell (via `@/components/ComposeBoxShell`) — without further extraction work. D-12 fork clarification (Ashley 2026-09-08) fully realized.
</success_criteria>

<output>
Create `.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-02-SUMMARY.md` when done.
</output>
