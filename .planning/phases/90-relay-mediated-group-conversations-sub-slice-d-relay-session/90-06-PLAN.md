---
phase: 90-relay-mediated-group-conversations-sub-slice-d-relay-session
plan: 06
type: execute
wave: 4
depends_on: [90-02, 90-04, 90-05]
files_modified:
  - src/ui/features/relay-room-pane/use-relay-room-stream.ts
  - src/ui/features/relay-room-pane/use-relay-room-stream.test.ts
  - src/ui/features/relay-room-pane/AgentBadgeWithAppendage.tsx
  - src/ui/features/relay-room-pane/AgentBadgeWithAppendage.test.tsx
  - src/ui/features/relay-room-pane/RelayRoomPane.tsx
  - src/ui/features/relay-room-pane/RelayRoomPane.test.tsx
  - src/ui/features/relay-room-pane/IdentityBadgeRow.tsx
autonomous: true
requirements: [D-04, D-05, D-06, D-08, D-10, D-15, D-16, T-90-FE-01, Pitfall-2, Pitfall-4]
tags: [frontend, relay-pane, ws-hook, per-agent-badge, compose, optimistic-send, live-subscription, D-08, D-10, D-15, D-16]
must_haves:
  truths:
    - "`useRelayRoomStream({userId, roomId, isVisible})` opens the WS to `/relay-room/websocket/`, sends `connectToRoom`, dispatches server frames, exposes `{history, participants, error, sendMessage(body, mqid), fetchOlder(beforeEventId)}`."
    - "`AgentBadgeWithAppendage` renders the plain IdentityBadge plus a shrunk meter + reset appendage sourcing state from `useSessionIsWorking(\\`${hostId}:${tmuxSessionName}\\`)` — the EXACT same store key format pretty view reads (D-10 correctness invariant / Pitfall 2)."
    - "Compose box in the pane uses `ComposeBoxShell` from Plan 02 with `upperArea={undefined}` (D-04) and `attachButton={undefined}` (D-05) — nothing above the textarea, no attach button visible."
    - "Send round-trip: on Enter, generates an mqid (`pv-optim-*` prefix — same shape pretty view uses), calls `sendMessage(body, mqid)` on the WS hook (which serializes to `send_message` frame with `txnId = mqid`), and inserts a pending OutboundBubble entry into local `pendingSends` state (D-15/D-16)."
    - "Pending -> real bubble replacement: when a `live_event` arrives with `sender === viewingUserMxid` AND `unsigned.transaction_id === pending.mqid`, the pending entry is removed from `pendingSends` and the real event is appended to `history` (Pitfall 4 correlation). Fallback: 20s timeout flips the pending entry to `state: 'failed'` (matches pretty view's PENDING_SEND_TIMEOUT_MS_NORMAL = 20_000)."
    - "Reset appendage on each agent badge fires through the SAME send-input channel pretty view uses for `/id reset` — NOT a new plumbing path (D-10). Recommended implementation: new backend endpoint `POST /id-reset/:hostId/:tmuxSession` that dispatches the same input the WS path does, so relay-pane badges don't have to mount hidden PrettyView WS connections per agent."
    - "WS lifecycle: open on mount + `isVisible=true`; close on `isVisible=false` or unmount; reconnect with linear-with-cap backoff (2s/4s/6s/8s/8s, MAX_RECONNECT_ATTEMPTS=5) mirroring PrettyView.tsx patch #148."
  artifacts:
    - path: "src/ui/features/relay-room-pane/use-relay-room-stream.ts"
      provides: "Custom hook: opens WS, dispatches frames, exposes history + participants + sendMessage + fetchOlder + error."
      contains: "export function useRelayRoomStream"
    - path: "src/ui/features/relay-room-pane/AgentBadgeWithAppendage.tsx"
      provides: "IdentityBadge + shrunk meter + reset appendage sourcing state from useSessionIsWorking with the SAME session-key format pretty view reads (D-10)."
      contains: "export function AgentBadgeWithAppendage"
    - path: "src/ui/features/relay-room-pane/RelayRoomPane.tsx"
      provides: "Now composes useRelayRoomStream + ComposeBoxShell + AgentBadgeWithAppendage into the fully-wired pane."
      contains: "useRelayRoomStream"
    - path: "src/ui/features/relay-room-pane/IdentityBadgeRow.tsx"
      provides: "Swaps the Plan 05 agent-badge placeholder for AgentBadgeWithAppendage — per-agent meter/reset live."
      contains: "AgentBadgeWithAppendage"
  key_links:
    - from: "src/ui/features/relay-room-pane/AgentBadgeWithAppendage.tsx"
      to: "src/ui/state/session-working-store.ts"
      via: "D-10 correctness — same store key `${hostId}:${tmuxSessionName}` pretty view reads"
      pattern: "useSessionIsWorking\\("
    - from: "src/ui/features/relay-room-pane/use-relay-room-stream.ts"
      to: "src/ui/features/relay-room-pane/relay-room-api.ts"
      via: "consumes openRelayRoomSocket + frame type contracts"
      pattern: "openRelayRoomSocket\\("
    - from: "src/ui/features/relay-room-pane/RelayRoomPane.tsx"
      to: "src/ui/components/ComposeBoxShell.tsx"
      via: "Plan 02 primitive with upperArea + attachButton both null (D-04 + D-05)"
      pattern: "ComposeBoxShell"
---

<objective>
Complete the RelayRoomPane end-to-end by landing three interlocking pieces:

1. **`useRelayRoomStream` hook** — WS lifecycle (open / reconnect / close on visibility flip), frame dispatch (`session` / `history_batch` / `live_event` / `send_ack` / `send_error` / `participants` / `error` / `inactive`), pending-send FIFO state machine mirroring PrettyView.tsx L1112-1304 (D-16 — same shape, different match source per Pitfall 4), and the outward API `{history, participants, error, sendMessage, fetchOlder}`.

2. **`AgentBadgeWithAppendage` component** — IdentityBadge + a shrunk meter + reset appendage. The meter reads `contextPct` and the working / recycling state from `useSessionIsWorking(\`${hostId}:${tmuxSessionName}\`)` — the D-10 correctness invariant (same store key pretty view reads). The reset button dispatches through the SAME per-agent send channel pretty view uses, NOT a new plumbing path — recommended via a new backend `/id-reset/:hostId/:tmuxSession` endpoint that fires the same input.

3. **RelayRoomPane wire-up** — replace the Plan 05 stubs with real state: the participant fetch is retained, but now `useRelayRoomStream` drives history / pendingSends / hasOlder / loadOlderStatus. The compose slot is filled with `<ComposeBoxShell upperArea={undefined} attachButton={undefined} ... onSend={handleSend} />` (D-04 + D-05). The `handleSend` closure generates an mqid, appends a pending entry, and calls `stream.sendMessage(body, mqid)`. The `IdentityBadgeRow` agent-cell placeholder from Plan 05 is swapped for `<AgentBadgeWithAppendage ... />`.

Purpose: this is the phase-closing plan that turns the skeleton into a working relay pane. Every D-decision that touches the send round-trip (D-15 / D-16), the per-agent affordances (D-08 / D-10), the compose-box vanish (D-04 / D-05), or the pending-bubble lifecycle (Pitfall 4 correlation, T-90-FE-01) lands here.

Output: fully-wired RelayRoomPane that fetches history via WS, subscribes to live events, sends via the viewing user's relay identity, replaces pending bubbles on echo-back, shows per-agent context meters + reset. No compose changes on pretty view; no per-agent state divergence (D-10 verified by a dedicated regression test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-CONTEXT.md
@.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-PATTERNS.md
@.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-RESEARCH.md
@.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-02-PLAN.md
@.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-04-PLAN.md
@.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-05-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build useRelayRoomStream hook — WS lifecycle + frame dispatch + pending-send FIFO with mqid==txnId correlation</name>
  <files>
    src/ui/features/relay-room-pane/use-relay-room-stream.ts,
    src/ui/features/relay-room-pane/use-relay-room-stream.test.ts
  </files>
  <read_first>
    src/ui/features/pretty-view/PrettyView.tsx (WS lifecycle at L1750-1830 including openClaudeSessionSocket + reconnect + connectToPane; pending-send FIFO state machine at L1112-1304 including PENDING_SEND_TIMEOUT_MS_NORMAL=20_000, flipToFailed, mqid generation with 'pv-optim-*' prefix; frame dispatch at L2077-2100 — copy the SHAPE, replace the wire protocol; do NOT modify),
    src/ui/api/claude-session-api.ts (openClaudeSessionSocket at L14-23 as structural model),
    src/ui/features/relay-room-pane/relay-room-api.ts (Plan 05 Task 1 output — openRelayRoomSocket, RelayRoomServerEvent, RelayRoomClientPayload, MatrixEvent, HumanParticipant, AgentParticipant),
    .planning/phases/90-.../90-PATTERNS.md § use-relay-room-stream.ts + Optimistic-send correlation shape sections,
    .planning/phases/90-.../90-RESEARCH.md § Common Pitfalls Pitfall 4 (Matrix unsigned.transaction_id echo-back correlation) and § Assumption A4 (verify with curl probe against local Synapse BEFORE committing to the pattern)
  </read_first>
  <behavior>
    - Test 1: hook mounts with `isVisible: true` -> calls `openRelayRoomSocket()`, sends `connectToRoom { roomId }` on `ws.onopen`.
    - Test 2: server `session { roomId, roomTitle }` frame -> stored in internal state (no external effect beyond enabling subsequent history to render).
    - Test 3: server `history_batch { events, hasMore }` frame -> `history` state becomes those events; `hasOlder` becomes `hasMore`.
    - Test 4: server `live_event { event }` frame with sender != viewingUserMxid -> event appended to `history`.
    - Test 5 (Pitfall 4 correlation — T-90-FE-01): server `live_event { event }` with `sender === viewingUserMxid` AND `event.unsigned.transaction_id === <pending mqid>` -> the pending entry is REMOVED from `pendingSends` AND the real event is appended to `history` — no duplicate bubble.
    - Test 6: server `send_ack { txnId, eventId }` frame -> optional acknowledgement (may be redundant with the live_event echo-match; if present, marks the pending entry as `state: 'settled'` OR removes it if the live_event already arrived). Planner picks the exact semantics; document choice in the hook JSDoc.
    - Test 7: server `send_error { txnId, reason }` frame -> pending entry with matching mqid flips to `state: 'failed'` (matches PrettyView flipToFailed at L1112-1304).
    - Test 8: 20s elapsed since pending entry insertion with no matching live_event or send_ack -> pending entry flips to `state: 'failed'` (PENDING_SEND_TIMEOUT_MS_NORMAL = 20_000).
    - Test 9: `sendMessage(body, mqid)` fires `ws.send(JSON.stringify({type: 'send_message', body, txnId: mqid}))` AND inserts a pending entry `{mqid, content: body, sentAt: Date.now(), state: 'sending', timer: <setTimeout id>}` into `pendingSends`.
    - Test 10: `fetchOlder(beforeEventId)` fires `ws.send(JSON.stringify({type: 'fetch_older_range', beforeEventId, count: 20}))` (count = 20 per D-14).
    - Test 11 (WS lifecycle): `isVisible` flips false -> WS is closed; flips back to true -> WS is re-opened; reconnect attempts follow the linear-with-cap schedule (2s, 4s, 6s, 8s, 8s) up to MAX_RECONNECT_ATTEMPTS = 5 (PATTERNS.md § reconnect constants).
    - Test 12: server `inactive { reason: 'not_member' | 'not_found' }` frame -> `error` state becomes 'room-not-found' (or the reason-typed variant); RelayRoomPane's Plan 05 error-state gate renders D-18 (D-18 upstream wiring already lands via error state prop / callback).
    - Test 13: server `error { message }` frame -> `error` state becomes 'protocol'; hook continues to receive further frames unless the WS itself closes.
    - Test 14: cleanup on unmount -> clearTimeout on every pending entry's timer; ws.close() called.
  </behavior>
  <action>
    Create `src/ui/features/relay-room-pane/use-relay-room-stream.ts` as a custom React hook. Signature:

    `export function useRelayRoomStream(opts: { userId: number; roomId: string; viewingUserMxid: string; isVisible: boolean }): { history: MatrixEvent[]; participants: RelayRoomParticipantsResponse | null; error: RelayRoomError | null; pendingSends: PendingSend[]; hasOlder: boolean; loadOlderStatus: 'idle' | 'in-flight' | 'error'; loadOlderError: string | null; sendMessage(body: string, mqid: string): void; fetchOlder(beforeEventId: string): void }`

    Where:
    - `RelayRoomError = 'room-not-found' | 'auth-expired' | 'protocol' | null`.
    - `PendingSend = { mqid: string; content: string; sentAt: number; state: 'sending' | 'failed'; timer: number | null }` — MIRROR PrettyView.tsx L1112-1304 shape verbatim per D-16.

    WS lifecycle (mirror PrettyView.tsx L1750-1830):
    1. On mount + `isVisible === true`: `const ws = openRelayRoomSocket()`; `wsRef.current = ws`.
    2. `ws.onopen`: `ws.send(JSON.stringify({type: 'connectToRoom', roomId}))`.
    3. `ws.onmessage`: parse JSON; type-narrow via `frame.type`; dispatch to the appropriate state-update path (Tests 2-8, 12, 13).
    4. `ws.onclose`: schedule reconnect with linear-with-cap backoff — 2s, 4s, 6s, 8s, 8s (MAX_RECONNECT_ATTEMPTS = 5); reset the attempt counter on `visibilitychange` (mirror PrettyView's discipline).
    5. On `isVisible === false` OR unmount: `ws.close()`; clear every pending-send timer.

    Pending-send FIFO state machine (mirror PrettyView.tsx L1112-1304 verbatim per D-16 — DIFFERENT match source only, per Pitfall 4):
    - `sendMessage(body, mqid)`: 
      1. If `ws.readyState !== WebSocket.OPEN`, defer OR fail immediately (planner-flex — document choice; PrettyView defers with a queued-send; the simpler path is fail-immediately with `state: 'failed'`).
      2. `ws.send(JSON.stringify({type: 'send_message', body, txnId: mqid}))`.
      3. `setPendingSends(prev => [...prev, {mqid, content: body, sentAt: Date.now(), state: 'sending', timer: setTimeout(() => flipToFailed(mqid, 'timeout'), PENDING_SEND_TIMEOUT_MS_NORMAL)}])`.
    - `flipToFailed(mqid, reason)`: same as PrettyView L1290-1304 — find the pending entry, clearTimeout the timer, set `state: 'failed'`.
    - On `live_event` frame with `sender === viewingUserMxid` AND `event.unsigned.transaction_id === pending.mqid`:
      1. clearTimeout the pending entry's timer.
      2. Remove the pending entry from `pendingSends`.
      3. Append the real event to `history` (so the OutboundBubble now renders WITHOUT `pendingState: 'sending'`).
    - On `send_error` frame with matching txnId: same as `flipToFailed`.

    Constants:
    - `const PENDING_SEND_TIMEOUT_MS_NORMAL = 20_000;` (PATTERNS.md § pending-send FIFO state machine + PrettyView L139). NO dormant-timeout variant — relay-room panes have no dormant analog per PATTERNS.md § Don't Hand-Roll note ('relay-room "dormant" is not a meaningful concept').
    - `const MAX_RECONNECT_ATTEMPTS = 5;` (PATTERNS.md § reconnect constants + PrettyView L84).
    - `const LOAD_OLDER_COUNT = 20;` (PATTERNS.md § pagination click handler + D-14 match-pretty-view).

    Verify Assumption A4 (RESEARCH.md L720) — Matrix's `unsigned.transaction_id` echo-back — via a curl probe against local Synapse BEFORE committing to the mqid==txnId pattern. If the echo-back is unreliable in the local Synapse (Test 5 falls apart in integration), fall back to content+sender+ts-window matching (documented in the hook JSDoc as a Plan B). This verification is a research task at the start of this Plan's execution — if verification fails, the hook's match logic changes but the hook's outer API stays the same.

    Structured logging at boundaries via `console.info` with explicit fields (never `JSON.stringify(event)`): `operation: 'relay_room_ws_open' | 'relay_room_ws_close' | 'relay_room_ws_reconnect' | 'relay_room_send_message' | 'relay_room_send_ack' | 'relay_room_send_error' | 'relay_room_fetch_older'`. Fields: userId, roomId, mqid (for send-related), reason (for close/error). NEVER log raw event bodies or Matrix response payloads.

    Add tests in `use-relay-room-stream.test.ts` covering the 14 behaviors above. Use `vi.stubGlobal('WebSocket', MockWebSocket)` where `MockWebSocket` is a minimal test double exposing `.send`, `.close`, `.onopen`, `.onmessage`, `.onclose`, `.readyState`. Mirror the mocking patterns from `src/ui/features/pretty-view/PrettyView.load-more.test.tsx` and `PrettyView.optimistic-bubbles.test.tsx` (PATTERNS.md § use-relay-room-stream.ts analog).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run src/ui/features/relay-room-pane/use-relay-room-stream.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/features/relay-room-pane/use-relay-room-stream.ts` + test file exist.
    - `grep -c "PENDING_SEND_TIMEOUT_MS_NORMAL\\s*=\\s*20_000\\|PENDING_SEND_TIMEOUT_MS_NORMAL\\s*=\\s*20000" src/ui/features/relay-room-pane/use-relay-room-stream.ts` returns >= 1 (D-16 matches pretty view).
    - `grep -c "MAX_RECONNECT_ATTEMPTS\\s*=\\s*5" src/ui/features/relay-room-pane/use-relay-room-stream.ts` returns >= 1 (reconnect discipline).
    - `grep -c "openRelayRoomSocket" src/ui/features/relay-room-pane/use-relay-room-stream.ts` returns >= 1 (WS setup).
    - `grep -c "unsigned.transaction_id\\|unsigned\\?.transaction_id" src/ui/features/relay-room-pane/use-relay-room-stream.ts | head -1` returns non-zero count (Pitfall 4 correlation read site).
    - `grep -c "\"send_message\"\\|type:.*send_message" src/ui/features/relay-room-pane/use-relay-room-stream.ts | head -1` returns non-zero count (WS send-frame emission).
    - `grep -c "\"connectToRoom\"" src/ui/features/relay-room-pane/use-relay-room-stream.ts` returns >= 1.
    - `grep -c "\"fetch_older_range\"" src/ui/features/relay-room-pane/use-relay-room-stream.ts` returns >= 1.
    - `grep -c "JSON.stringify(event\\|JSON.stringify(e)" src/ui/features/relay-room-pane/use-relay-room-stream.ts` returns 0 (structured-logging discipline).
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run src/ui/features/relay-room-pane/use-relay-room-stream.test.ts` — all 14 behavior tests pass, especially Test 5 (Pitfall 4 correlation) which is the T-90-FE-01 regression gate.
    - No file under `src/ui/features/pretty-view/` is modified.
  </acceptance_criteria>
  <done>
    useRelayRoomStream hook opens WS, dispatches all 8 server frame types, exposes the 4-way outward API + pendingSends state, implements D-16 pending-send FIFO with mqid==txnId correlation (Pitfall 4 / T-90-FE-01) + 20s failure timeout, reconnects with linear-with-cap backoff on visibility flip. Zero pretty-view modification.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build AgentBadgeWithAppendage — reuse useSessionIsWorking with the exact same session-key format pretty view reads (D-10 / Pitfall 2)</name>
  <files>
    src/ui/features/relay-room-pane/AgentBadgeWithAppendage.tsx,
    src/ui/features/relay-room-pane/AgentBadgeWithAppendage.test.tsx
  </files>
  <read_first>
    src/ui/features/terminal/IdentityBadge.tsx (badge primitive to reuse per D-08; note absolute positioning at L102 — the appendage wrapper must establish a non-absolute context or the inner render fragment must be extracted; Plan 05 Task 2 already made this call for the row cells — follow the same pattern),
    src/ui/features/pretty-view/ComposeBox.tsx (meter well at L2287-2298 + reset cell at L2305-2334 + segment band-color computation at L2353-2410; SHRINK these to badge-appendage size but preserve the exact SEG_COUNT + threshold colors per D-08 'same visual language, just smaller'; do NOT modify),
    src/ui/state/session-working-store.ts (useSessionIsWorking + useSessionIsRecycling at L931-1022; session key format `${hostId}:${tmuxSessionName}`),
    src/ui/state/identities-store.ts (buildIdentityHostsFromFleet at L74 — the fleet-derived identity -> host mapping that resolves an agent's (hostId, tmuxSessionName) from its identityKey; Plan 06 Task 3 wires the lookup),
    .planning/phases/90-.../90-PATTERNS.md § AgentBadgeWithAppendage.tsx section (key resolution pattern — D-10 correctness invariant; meter well + reset cell verbatim shrink; reset send-path recommendation),
    .planning/phases/90-.../90-RESEARCH.md § Common Pitfalls Pitfall 2 (per-agent state channel drift — D-10 violation)
  </read_first>
  <behavior>
    - Test 1: renders the plain `IdentityBadge` for the given `identityKey` (grep for the IdentityBadge import + JSX use).
    - Test 2 (**D-10 / Pitfall 2 regression gate**): calls `useSessionIsWorking(\`${hostId}:${tmuxSessionName}\`)` with the EXACT key format `${hostId}:${tmuxSessionName}` — assert via `vi.spyOn` on the mocked hook that the call argument is a string of that shape. If a future refactor swaps the key format, this test fails.
    - Test 3: `contextPct = 30` -> meter renders 30% lit segments (green band; SEG_COUNT * 30/100 rounded to nearest lit-count).
    - Test 4: `contextPct = 50` -> amber band (threshold 45 per PATTERNS.md § segment band-color).
    - Test 5: `contextPct = 80` -> red band (threshold 78).
    - Test 6: `contextPct = null` -> meter renders all-neutral (unknown state); title tooltip shows 'Context (unknown)'.
    - Test 7: reset button click -> fires the parent's `onResetClicked()` callback (Task 3 wires this to the backend endpoint or PrettyView send channel per the planner's chosen implementation).
    - Test 8: `isRecycling === true` -> meter shows a subtle pulsing / draining state (mirror PrettyView's `isDraining` / `isHolding` render — copy the classes verbatim).
    - Test 9: appendage container carries `data-appendage="true"` for the IdentityBadgeRow's D-09 humans-no-appendage test to remain a valid discriminator (Plan 05 Task 2's test grep-checks that human badges do NOT have `data-appendage="true"`).
    - Test 10: `--meter-width` CSS custom property set to a shrunk value (e.g., 6rem — planner picks; PATTERNS.md § meter well pattern recommends 6rem vs the pane-wide 12rem).
  </behavior>
  <action>
    Create `src/ui/features/relay-room-pane/AgentBadgeWithAppendage.tsx`. Props:
    - `identityKey: string` (agent identity key, lowercased).
    - `mxid: string` (from participants response).
    - `hostId: number` (resolved from `buildIdentityHostsFromFleet` at identities-store.ts L74; Task 3 in this plan wires the lookup site).
    - `tmuxSessionName: string` (the agent's own tmux session name — the D-10 session-working-store key partner).
    - `contextPct: number | null` (from the same source pretty view reads — Task 3 wires this to the identities-store or its bespoke feed; if there's no clean single-source-of-truth for contextPct that a badge can subscribe to today, this prop is threaded from a per-agent WS or hook that the identities-store already exposes — grep the identities-store for a `contextPct` field, or fall back to a null default with a follow-up ticket if the source is not readily available).
    - `onResetClicked: () => void` (Task 3 wires the send-path).

    D-10 correctness invariant (Pitfall 2 mitigation):
    - `const sessionKey = \`${hostId}:${tmuxSessionName}\`;` — EXACTLY this format (not `mxid`-keyed, not `identityKey`-keyed).
    - `const isWorking = useSessionIsWorking(sessionKey);` — same store, same key, same subscription pretty view uses.
    - `const isRecycling = useSessionIsRecycling(sessionKey);` — likewise.
    - If two stores drift, D-10 is silently violated and the same agent's meter reads different values on two surfaces (Pitfall 2 failure mode). Test 2 is the dedicated regression gate.

    Render structure (per PATTERNS.md § AgentBadgeWithAppendage.tsx):
    1. Wrapper `<div className="relative flex flex-col items-center gap-1">` — non-absolute positioning context for the reused IdentityBadge.
    2. `<IdentityBadge identityKey={identityKey} />` (or its inner-render fragment — planner picks per Plan 05 Task 2's approach for row cells).
    3. Appendage wrapper `<div data-appendage="true" className="mt-1">` — Test 9's discriminator.
    4. Meter well (VERBATIM shrink of ComposeBox.tsx L2287-2298): copy the class string, override `--meter-width` to a smaller value (e.g., `6rem`), keep `SEG_COUNT = 12` for visual parity. The `role="meter"`, `aria-label`, `aria-valuemin`, `aria-valuemax`, `aria-valuenow`, `title` attributes are preserved from the analog.
    5. Segment band-color computation VERBATIM (ComposeBox L2353-2410): `band = contextPct == null ? 'green' : contextPct >= 78 ? 'red' : contextPct >= 45 ? 'amber' : 'green'`. `litGreenBg` / `litAmberBg` / `litRedBg` / `dimNeutralBg` — copy the constants inline (do NOT re-import from ComposeBox to preserve D-03 no-modification-to-pretty-view).
    6. Reset cell (VERBATIM shrink of ComposeBox.tsx L2305-2334): `<button type="button" onClick={onResetClicked} aria-label="Reset context window" title="Reset context window" className="...same as analog with shrunk h-full w-6 -> w-4 or similar..."><RotateCcw className="size-3" /></button>`. The button is the primary reset affordance for THIS agent — same visual + behavior as pretty view, just smaller and repositioned.

    Extraction discipline: do NOT modify `src/ui/features/pretty-view/ComposeBox.tsx` (D-03). Copy the classes + constants inline; the component is standalone. This mirrors the Plan 02 primitive-extraction discipline.

    Add tests in `AgentBadgeWithAppendage.test.tsx` covering the 10 behaviors above. Mock `useSessionIsWorking` + `useSessionIsRecycling` via `vi.mock('@/state/session-working-store')` with `vi.mocked(useSessionIsWorking).mockReturnValue(true/false)`. Test 2 uses `vi.mocked(useSessionIsWorking).mock.calls[0][0]` to assert on the argument format.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run src/ui/features/relay-room-pane/AgentBadgeWithAppendage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/features/relay-room-pane/AgentBadgeWithAppendage.tsx` + test file exist.
    - `grep -c "useSessionIsWorking(\`\\${hostId}:\\${tmuxSessionName}\`)" src/ui/features/relay-room-pane/AgentBadgeWithAppendage.tsx | head -1` returns non-zero count (D-10 exact key format).
    - `grep -c "useSessionIsRecycling" src/ui/features/relay-room-pane/AgentBadgeWithAppendage.tsx` returns >= 1.
    - `grep -c "import.*IdentityBadge\\|from.*IdentityBadge" src/ui/features/relay-room-pane/AgentBadgeWithAppendage.tsx` returns >= 1 (D-08 badge reuse).
    - `grep -c "role=\"meter\"" src/ui/features/relay-room-pane/AgentBadgeWithAppendage.tsx` returns >= 1 (meter well semantic role preserved).
    - `grep -c "data-appendage=\"true\"" src/ui/features/relay-room-pane/AgentBadgeWithAppendage.tsx` returns >= 1 (Plan 05 D-09 discriminator).
    - `grep -c "RotateCcw" src/ui/features/relay-room-pane/AgentBadgeWithAppendage.tsx` returns >= 1 (reset icon).
    - `grep -c "onResetClicked" src/ui/features/relay-room-pane/AgentBadgeWithAppendage.tsx` returns >= 2 (prop declared + prop consumed).
    - No modification to `src/ui/features/pretty-view/`.
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run src/ui/features/relay-room-pane/AgentBadgeWithAppendage.test.tsx` — all 10 tests pass, especially Test 2 (D-10 exact-key-format regression gate) which prevents Pitfall 2 drift.
  </acceptance_criteria>
  <done>
    AgentBadgeWithAppendage renders the plain IdentityBadge + shrunk meter + reset appendage sourcing state from useSessionIsWorking with the EXACT same session-key format pretty view reads (D-10 correctness invariant; Pitfall 2 mitigated with a dedicated regression test). Reset button fires an onResetClicked callback that Task 3 wires to the send channel. Zero pretty-view modification.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire useRelayRoomStream + AgentBadgeWithAppendage + ComposeBoxShell into RelayRoomPane + IdentityBadgeRow; close the send round-trip end-to-end</name>
  <files>
    src/ui/features/relay-room-pane/RelayRoomPane.tsx,
    src/ui/features/relay-room-pane/RelayRoomPane.test.tsx,
    src/ui/features/relay-room-pane/IdentityBadgeRow.tsx
  </files>
  <read_first>
    src/ui/features/relay-room-pane/RelayRoomPane.tsx (Plan 05 Task 3 skeleton — stubbed history / pendingSends / compose slot; this task fills the stubs),
    src/ui/features/relay-room-pane/IdentityBadgeRow.tsx (Plan 05 Task 2 skeleton — agent cells are placeholders; this task swaps the placeholder for AgentBadgeWithAppendage),
    src/ui/features/relay-room-pane/use-relay-room-stream.ts (Task 1 output),
    src/ui/features/relay-room-pane/AgentBadgeWithAppendage.tsx (Task 2 output),
    src/ui/components/ComposeBoxShell.tsx (Plan 02 primitive — props: value/onChange/onSend/upperArea/attachButton/placeholder/canSend/className),
    src/ui/state/identities-store.ts (buildIdentityHostsFromFleet at L74 — the fleet-derived identityKey -> hostId mapping used to resolve per-agent state keys; also `useIdentities()` for identityKey -> displayName/hue lookups),
    src/ui/features/pretty-view/ComposeBox.tsx (grep for the 'pv-optim-' mqid prefix generator — Plan 06 mqid should follow the same convention for cross-pane debuggability; do NOT modify),
    .planning/phases/90-.../90-PATTERNS.md § AgentBadgeWithAppendage.tsx reset-send-path recommendation
  </read_first>
  <behavior>
    - Test 1: RelayRoomPane on mount instantiates `useRelayRoomStream({userId, roomId, viewingUserMxid, isVisible})` and threads `stream.history` -> `RelayMessageList` history prop, `stream.pendingSends` -> pendingSends prop, `stream.hasOlder` -> hasOlder prop, `stream.loadOlderStatus` -> loadOlderStatus prop, `stream.fetchOlder(oldestEventId)` -> onLoadOlder callback.
    - Test 2: compose slot `<div data-slot="compose-box" />` from Plan 05 is REPLACED with `<ComposeBoxShell upperArea={undefined} attachButton={undefined} onSend={handleSend} value={composeText} onChange={setComposeText} placeholder="Message the room…" />`. `upperArea` and `attachButton` are BOTH undefined (D-04 + D-05).
    - Test 3 (**D-15 + D-16 send round-trip**): `handleSend('hello')` generates an mqid (e.g., `pv-optim-<uuid>` or a monotonic counter suffix — same shape as pretty view's mqid), calls `stream.sendMessage('hello', mqid)`, and (via the stream's side effect) inserts a pending entry into pendingSends AND fires a `send_message` frame on the WS with `txnId: mqid`.
    - Test 4 (Pitfall 4 / T-90-FE-01 end-to-end): a `live_event` frame arriving with `sender === viewingUserMxid` AND `unsigned.transaction_id === <the mqid from Test 3>` causes the pending entry to be removed AND the real event to appear in history. The DOM after this transition shows ONE OutboundBubble (the real one), NOT two (pending + real).
    - Test 5: IdentityBadgeRow — every agent participant renders as `<AgentBadgeWithAppendage ...>` with `hostId` + `tmuxSessionName` resolved via `buildIdentityHostsFromFleet` (identities-store.ts L74) from the agent's identityKey. NO agent renders the Plan 05 placeholder.
    - Test 6: per-agent reset click -> RelayRoomPane's onResetClicked handler fires. Recommended implementation: POST to a new backend endpoint `POST /id-reset/:hostId/:tmuxSession` (Task 3 does NOT create this endpoint — leave it as a follow-up task deferred to a later phase / bugfix wave; document the deferral in the RelayRoomPane JSDoc). If the endpoint doesn't exist, the handler logs a warning and no-ops (do NOT crash; the meter still updates via the shared session-working-store so at least the read side works). Alternative planner-flex: fire an existing PrettyView send channel if one is already exposed cross-pane — grep to confirm before committing.
    - Test 7: participants frame handling — `useRelayRoomStream`'s `participants` state is used INSTEAD of the Plan 05 REST fetch when it arrives (the WS's `participants` frame is authoritative once received; the REST fetch is a fallback for the transient window between mount and first WS frame). Alternatively: keep the REST fetch as-is and use the WS `participants` frame only for live updates (planner-flex).
    - Test 8: WS `inactive { reason }` frame -> `error` prop from hook becomes `'room-not-found'` -> RelayRoomPane's Plan 05 error-state gate renders `<RelayRoomErrorState />` (D-18 confirmed end-to-end).
    - Test 9: no regression on Plan 05's participant-fetch 404 path — that path still renders D-18 error state (belt-and-suspenders with the WS `inactive` frame).
    - Test 10: structured logging on send — `console.info({operation: 'relay_room_send', mqid, roomId})`. NEVER log the message body (privacy) or mxid credentials.
  </behavior>
  <action>
    Update `src/ui/features/relay-room-pane/RelayRoomPane.tsx` — replace the Plan 05 stubs with real state driven by `useRelayRoomStream`:

    1. Instantiate the hook: `const stream = useRelayRoomStream({userId, roomId, viewingUserMxid, isVisible});`.
    2. Local state for compose text: `const [composeText, setComposeText] = useState('');`.
    3. `handleSend` closure:
       ```
       const handleSend = useCallback((body: string) => {
         const mqid = generateMqid(); // e.g., `pv-optim-${crypto.randomUUID()}` — same prefix pretty view uses
         stream.sendMessage(body, mqid);
         setComposeText(''); // clear immediately per COMPOSE-02 discipline (Enter clears)
         console.info({operation: 'relay_room_send', mqid, roomId}); // NEVER log body
       }, [stream, roomId]);
       ```
       `generateMqid()` helper: `pv-optim-${crypto.randomUUID()}` or the shared utility if pretty view exposes one — grep for the mqid generator in `src/ui/features/pretty-view/ComposeBox.tsx` first; if it's not shared, define locally (D-03 forbids modifying ComposeBox).
    4. Replace the Plan 05 stubbed `history: []` / `pendingSends: []` / `hasOlder: false` / etc. props with `stream.history` / `stream.pendingSends` / `stream.hasOlder` / `stream.loadOlderStatus` / `stream.loadOlderError` / `() => stream.fetchOlder(stream.history[0]?.event_id ?? '')`.
    5. Replace the `<div data-slot="compose-box" />` cell with `<ComposeBoxShell upperArea={undefined} attachButton={undefined} value={composeText} onChange={setComposeText} onSend={handleSend} placeholder="Message the room…" canSend={stream.error === null} className="shrink-0" />`.
    6. Error-state gate: unify with hook error — `const displayError = errorState ?? (stream.error === 'room-not-found' ? 'room-not-found' : null);` render `<RelayRoomErrorState />` when `displayError === 'room-not-found'`.
    7. Participants: use `stream.participants ?? restFetchedParticipants` (Plan 05's REST fetch remains as the initial-load fallback for the transient window before the WS's `participants` frame arrives).
    8. Pass `handleAgentReset` down to `IdentityBadgeRow` (which threads it to each AgentBadgeWithAppendage): `const handleAgentReset = useCallback((identityKey: string, hostId: number, tmuxSessionName: string) => { /* POST /id-reset/:hostId/:tmuxSession or log-and-no-op if endpoint deferred */ }, []);`.

    Update `src/ui/features/relay-room-pane/IdentityBadgeRow.tsx` — replace the Plan 05 agent-cell placeholder with real `AgentBadgeWithAppendage`:

    1. Add new prop `onAgentReset: (identityKey: string, hostId: number, tmuxSessionName: string) => void`.
    2. For each agent in `agentsSorted`, resolve `(hostId, tmuxSessionName)` via `buildIdentityHostsFromFleet` from `src/ui/state/identities-store.ts` (grep for the correct export — `useIdentities()` may expose the mapping via a computed property; if not, call `buildIdentityHostsFromFleet(fleetSessions)` directly and index by identityKey).
    3. Render `<AgentBadgeWithAppendage identityKey={agent.identityKey} mxid={agent.mxid} hostId={resolvedHostId} tmuxSessionName={resolvedTmuxSessionName} contextPct={contextPct} onResetClicked={() => onAgentReset(agent.identityKey, resolvedHostId, resolvedTmuxSessionName)} />`.
    4. `contextPct` resolution: source from the identities-store or a per-agent hook if one exists; grep for a `contextPct` field on the agent identity type. If no clean source exists in-store today, pass `null` (the meter renders in unknown state; a follow-up phase wires the source. Document the deferral in the AgentBadgeWithAppendage JSDoc). PATTERNS.md § AgentBadgeWithAppendage.tsx notes 'the value from same source pretty view reads' — grep pretty view for how it obtains contextPct (WS `context_pct` frame handled in PrettyView.tsx L2269-2273); if the store already publishes it, subscribe to that store here.
    5. Do NOT render the Plan 05 placeholder `<AgentBadgeCellPlaceholder>` anymore; the agent cell is now the fully-wired AgentBadgeWithAppendage.

    Reset send-path (planner's call per PATTERNS.md § AgentBadgeWithAppendage.tsx recommendation):
    - **Option A (recommended)**: New backend endpoint `POST /id-reset/:hostId/:tmuxSession` that dispatches the same input `/id reset` the WS path uses. If Ashley wants this in the same phase, add a follow-up task; otherwise document as deferred and log-and-no-op on click.
    - **Option B**: Cross-mount hidden PrettyView WS per agent — REJECTED (heavy; violates 'don't invent new plumbing').
    - **Option C**: Discover an existing cross-pane send-input dispatcher — grep for one before committing to Option A.

    This task defers the backend `/id-reset/` endpoint to a follow-up (out of scope for slice D per the phase's minimal-primitive extraction discipline; the meter READ path via useSessionIsWorking IS the D-10 correctness win; reset from the relay pane can ship in a v1.1 follow-up). Frontend wires the button + no-op handler with a warning log.

    Add tests in `RelayRoomPane.test.tsx` and update `IdentityBadgeRow.test.tsx` covering the 10 behaviors above. Mock `useRelayRoomStream` via `vi.mock('./use-relay-room-stream')` returning a controllable stream double. Mock `buildIdentityHostsFromFleet` if needed.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run src/ui/features/relay-room-pane/RelayRoomPane.test.tsx src/ui/features/relay-room-pane/IdentityBadgeRow.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useRelayRoomStream(" src/ui/features/relay-room-pane/RelayRoomPane.tsx` returns >= 1 (hook wired).
    - `grep -c "ComposeBoxShell" src/ui/features/relay-room-pane/RelayRoomPane.tsx` returns >= 1 (compose slot filled).
    - `grep -c "upperArea={undefined}\\|upperArea={null}" src/ui/features/relay-room-pane/RelayRoomPane.tsx` returns >= 1 (D-04).
    - `grep -c "attachButton={undefined}\\|attachButton={null}" src/ui/features/relay-room-pane/RelayRoomPane.tsx` returns >= 1 (D-05).
    - `grep -c "AgentBadgeWithAppendage" src/ui/features/relay-room-pane/IdentityBadgeRow.tsx` returns >= 1 (agent cell swapped from placeholder).
    - `grep -c "AgentBadgeCellPlaceholder" src/ui/features/relay-room-pane/IdentityBadgeRow.tsx` returns 0 (Plan 05 placeholder replaced).
    - `grep -c "stream.sendMessage(\\|stream\\.sendMessage(" src/ui/features/relay-room-pane/RelayRoomPane.tsx` returns >= 1 (send round-trip wired).
    - `grep -c "generateMqid\\|pv-optim-" src/ui/features/relay-room-pane/RelayRoomPane.tsx` returns >= 1 (mqid generation site).
    - `grep -c "\"relay_room_send\"" src/ui/features/relay-room-pane/RelayRoomPane.tsx` returns >= 1 (structured send log).
    - `grep -c "buildIdentityHostsFromFleet\\|identities-store" src/ui/features/relay-room-pane/IdentityBadgeRow.tsx` returns >= 1 (agent host+session resolution).
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run src/ui/features/relay-room-pane/RelayRoomPane.test.tsx src/ui/features/relay-room-pane/IdentityBadgeRow.test.tsx` — all new tests pass; every pre-existing test (from Plan 05) still passes (compare pre/post pass counts).
    - No file under `src/ui/features/pretty-view/` is modified.
  </acceptance_criteria>
  <done>
    RelayRoomPane fully wired end-to-end: fetches history via WS, subscribes to live events, sends via useRelayRoomStream (which serializes to WS `send_message` frame + inserts pending entry), replaces pending bubbles on Pitfall-4 correlated echo-back, renders per-agent context meters via D-10 same-store-key discipline, reset buttons fire (endpoint deferred, no-op-with-warning stub landed), compose box shows textarea + Send only (D-04 upper area gone, D-05 attach hidden). Every T-90-FE-01 + D-08/10/15/16 requirement satisfied. Zero pretty-view modification.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS frames from Plan 04 backend -> hook state | Backend has already gated access + scrubbed tokens. Hook trusts the frame shape. |
| Compose textarea -> WS send | User input; validated by Plan 04 backend (matrix-message-send.ts input validation). |
| session-working-store subscription | Shared with pretty view; D-10 correctness invariant enforced by exact-key-format test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-FE-01 | Optimistic-send correlation (spoofing / repudiation) | useRelayRoomStream pending-send FIFO | mitigate | Pitfall 4 mqid == Matrix txnId correlation via `unsigned.transaction_id` echo-back. Test 5 in Task 1 is the dedicated regression gate. Assumption A4 verification (curl probe) at plan-start. Fallback content+sender+ts-window match documented if echo-back is unreliable. |
| T-90-06-T1 | Tampering (XSS) | Message body / participants rendering | mitigate | Task 3 render path inherits Plan 05's React-text-children discipline. Grep gate `dangerouslySetInnerHTML` = 0 on all touched files. |
| T-90-06-I1 | Information Disclosure | Structured logging on send | mitigate | `relay_room_send` log NEVER includes the message body (privacy) or mxid credentials. Only mqid + roomId. Structured fields, no `JSON.stringify(event)`. |
| T-90-06-D1 | Denial of Service | Pending-send timers | mitigate | Every pending entry's timer is clearTimeout'd on live_event / send_ack / send_error match + on unmount. Test 14 in Task 1 asserts unmount cleanup. |
| T-90-06-R1 | Repudiation | Reset button deferred to Option A follow-up | accept | Reset from relay pane is not v1-critical. Meter READ via D-10 same-store-key IS the correctness win. Follow-up backend endpoint tracked in RelayRoomPane JSDoc. |
| T-90-06-D2 | D-10 correctness (Pitfall 2 drift) | AgentBadgeWithAppendage | mitigate | Task 2 Test 2 is a dedicated regression gate on the useSessionIsWorking key format (`${hostId}:${tmuxSessionName}`). A refactor that swaps the format silently violates D-10; the test fails. |

`security_enforcement=true`. T-90-FE-01 (optimistic-send correlation) and D-10 drift (Pitfall 2) both mitigated with dedicated regression tests. T-90-BE-* threats already mitigated at Plan 04 backend.
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npx vitest run` scoped to the five touched test files reports all tests passing.
- No modification to `src/ui/features/pretty-view/` (D-01 + D-03).
- Grep gates in acceptance criteria satisfied — most importantly Task 1 Test 5 (T-90-FE-01) and Task 2 Test 2 (D-10 exact-key regression).
- Assumption A4 (Matrix `unsigned.transaction_id` echo-back) verified via curl probe at plan-start; documented outcome in Task 1 hook JSDoc.
</verification>

<success_criteria>
Relay pane is fully functional end-to-end for the D-15 send round-trip + D-16 optimistic-send lifecycle + D-08/D-10 per-agent affordances + D-04/D-05 compose-box configuration. Every D-decision in the phase (D-01 through D-20) is now realized somewhere in Plans 01-06. Plan 07 handles only the last mile: kind-discriminator branch wiring at the tab-mount site + the RelayRoomSessionPane wrapper.
</success_criteria>

<output>
Create `.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-06-SUMMARY.md` when done.
</output>
