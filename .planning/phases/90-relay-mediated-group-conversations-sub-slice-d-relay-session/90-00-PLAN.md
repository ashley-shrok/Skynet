---
phase: 90-relay-mediated-group-conversations-sub-slice-d-relay-session
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/backend/fleet-status/contextpct-store.ts
  - src/backend/fleet-status/contextpct-store.test.ts
  - src/backend/fleet-status/types.ts
  - src/backend/fleet-status/wire-protocol.ts
  - src/backend/fleet-status/fleet-status-server.ts
  - src/backend/fleet-status/fleet-status-server.test.ts
  - src/backend/claude-session/claude-session-server.ts
  - src/backend/database/routes/agent-reset.ts
  - src/backend/database/routes/agent-reset.test.ts
  - src/backend/database/database.ts
  - src/ui/api/fleet-status-client.ts
  - src/ui/api/fleet-status-client.test.ts
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-view/PrettyView.test.tsx
  - docker/nginx.conf
  - docker/nginx-https.conf
autonomous: true
requirements: [D-10, D-03-waiver-fleet-status-source-swap, D-03-waiver-reset-endpoint, Pitfall-2, Pitfall-6, Pitfall-7]
tags: [wave-0, foundation, fleet-status, contextPct, agent-reset, D-03-waiver, mechanical-swap, blocker-1]
must_haves:
  truths:
    - "Backend `contextPctStore` is a per-session in-memory shared map keyed on `${hostId}:${tmuxSessionName}` that carries the latest `contextPct: number | null` value. Every existing `context_pct` WS emission in `claude-session-server.ts` (L3219 dormant branch + L7074 primary contextPctTimer branch) is dual-written into this store — the WS emission STAYS unchanged for backwards compatibility with any consumer that has not yet migrated (per D-10 delivery mechanism)."
    - "Fleet-status response is extended with a per-session `contextPct: number | null` field. Every fleet-status tick populates the field from the shared map (null when the map has no entry for a session, or when the latest value is null)."
    - "Frontend `useSessionContextPct(hostId, tmuxSession)` hook is added to `src/ui/api/fleet-status-client.ts`. It subscribes to the fleet-status stream and returns the latest `contextPct` value for the given session key. Both PrettyView (via the D-03 mechanical waiver) and the future relay-pane badge appendage (Plan 06) consume this hook."
    - "PrettyView's local `useState<number|null>(null)` for contextPct (PrettyView.tsx L570) is REPLACED by a call to `useSessionContextPct(hostId, tmuxSession ?? '')`. The WS `case \"context_pct\"` handler at PrettyView.tsx L2269 becomes a no-op (or is removed). The meter still reads correctly post-refactor — this is a mechanical state-source swap with zero UX change (D-03 waiver, Alice 2026-09-08)."
    - "New backend endpoint `POST /agent-reset/:hostId/:tmuxSessionName` (`src/backend/database/routes/agent-reset.ts`) dispatches `/id reset` via the same input mechanism ComposeBox uses today (see `dispatchResetPayload` at ComposeBox.tsx L1867-1888 for the payload shape and dispatch site; the endpoint replicates the input-dispatch seam server-side, not the compose-box UI-side fx)."
    - "The endpoint is JWT-authenticated and access-gated: the authenticated user MUST own/have-access-to the target host+tmuxSession (grep the existing fleet-ownership gate for the pattern; missing/wrong owner returns 404 with no oracle)."
    - "PrettyView's existing meter-well reset button (ComposeBox.tsx L1877 `funnel.send(payload, {trigger:'reset'})`) is rewired to hit `POST /agent-reset/:hostId/:tmuxSessionName` — mechanical swap from the current mechanism to the new endpoint, same behavior end-to-end. This lets the relay-pane badge appendage (Plan 06 Task 3) call the SAME endpoint as a first-class caller."
    - "Nginx dual-update: `location ^~ /agent-reset/` blocks land in BOTH `docker/nginx.conf` AND `docker/nginx-https.conf` (Pitfall 6)."
    - "Backend build clean per Pitfall 7 — `npm run build:backend` gate on every backend TS touch."
  artifacts:
    - path: "src/backend/fleet-status/contextpct-store.ts"
      provides: "Per-session contextPct shared map (in-memory) with set/get/delete + subscribe primitives."
      contains: "export function setContextPct\\|export function getContextPct"
    - path: "src/backend/fleet-status/types.ts"
      provides: "Per-session record shape extended with contextPct."
      contains: "contextPct"
    - path: "src/backend/fleet-status/wire-protocol.ts"
      provides: "SessionStateSchema extended with contextPct (nullable optional per additive-optional invariant)."
      contains: "contextPct"
    - path: "src/backend/database/routes/agent-reset.ts"
      provides: "POST /agent-reset/:hostId/:tmuxSessionName endpoint dispatching /id reset via the same input seam ComposeBox uses."
      contains: "router.post"
    - path: "src/ui/api/fleet-status-client.ts"
      provides: "useSessionContextPct(hostId, tmuxSession) hook — single source of truth for both PrettyView and AgentBadgeWithAppendage."
      contains: "useSessionContextPct"
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "contextPct sourced from useSessionContextPct hook (was local useState); context_pct WS handler no-op / removed."
      contains: "useSessionContextPct"
    - path: "docker/nginx.conf"
      provides: "location block for /agent-reset/ (HTTP dev)."
      contains: "location.*=.*/agent-reset\\|location ~ /agent-reset"
    - path: "docker/nginx-https.conf"
      provides: "matching location block for /agent-reset/ (HTTPS prod)."
      contains: "location.*=.*/agent-reset\\|location ~ /agent-reset"
  key_links:
    - from: "src/backend/claude-session/claude-session-server.ts"
      to: "src/backend/fleet-status/contextpct-store.ts"
      via: "dual-write on every context_pct emission — L3219 dormant branch + L7074 primary timer branch"
      pattern: "setContextPct\\("
    - from: "src/backend/fleet-status/fleet-status-server.ts"
      to: "src/backend/fleet-status/contextpct-store.ts"
      via: "populate contextPct field on every fleet-status tick"
      pattern: "getContextPct\\("
    - from: "src/ui/features/pretty-view/PrettyView.tsx"
      to: "src/ui/api/fleet-status-client.ts"
      via: "D-03 mechanical waiver — swap useState for hook"
      pattern: "useSessionContextPct\\("
    - from: "src/ui/features/pretty-view/ComposeBox.tsx"
      to: "src/backend/database/routes/agent-reset.ts"
      via: "reset button dispatches to new endpoint (mechanical rewire; ComposeBox untouched EXCEPT for the dispatch call target)"
      pattern: "/agent-reset/"
---

<objective>
Wave 0 unblocks Plans 05 + 06 by making fleet-status the SINGLE SOURCE OF TRUTH for `contextPct` and by adding a reusable `POST /agent-reset/:hostId/:tmuxSessionName` endpoint that both PrettyView (existing reset button, mechanical rewire) and the relay-pane badge appendage (Plan 06 Task 3, first-class caller) invoke.

**Why this plan exists (BLOCKER #1 resolution):** D-08 (per-agent meter appendage) and D-10 (reset appendage sourced from the same per-agent state channel pretty view uses) cannot be delivered while `contextPct` lives ONLY in PrettyView's local `useState`. The plan-checker correctly flagged that both decisions would be silently deferred without a state-promotion refactor. Alice greenlit this refactor on 2026-09-08 as an explicit D-03 waiver — the PrettyView touch is a MECHANICAL state-source swap (swap `useState` for hook; no-op the WS handler) with zero UX or behavior change; the rest of pretty view remains off-limits.

Three coordinated pieces:

1. **Backend fleet-status contextPct promotion.** New `contextpct-store.ts` (per-session in-memory shared map keyed on `${hostId}:${tmuxSessionName}`). Modify the two existing `context_pct` WS emission sites in `claude-session-server.ts` (L3219 dormant branch + L7074 primary `contextPctTimer` branch) to ALSO write into the shared map — WS emission STAYS unchanged (backwards compat during transition; nothing else consumes it after this plan, but leaving the WS emission avoids risk to any observer we might have missed). Extend `fleet-status/types.ts` + `wire-protocol.ts` with `contextPct: number | null` on the per-session record shape. Populate on every fleet-status tick from the shared map.

2. **Frontend fleet-status hook + PrettyView mechanical swap (D-03 waiver).** Add `useSessionContextPct(hostId, tmuxSession)` to `src/ui/api/fleet-status-client.ts`. In PrettyView.tsx: swap L570 `const [contextPct, setContextPct] = useState<number | null>(null)` for `const contextPct = useSessionContextPct(hostId, tmuxSession ?? '')`. Remove/no-op the WS `case "context_pct"` handler at L2269 (backend still emits — frontend no longer consumes the WS variant since fleet-status is authoritative). Zero UX change. Zero behavior change. Existing `PrettyView.tsx L3704 contextPct={contextPct}` prop-down to ComposeBox continues working identically.

3. **New agent-reset endpoint.** `POST /agent-reset/:hostId/:tmuxSessionName` (`src/backend/database/routes/agent-reset.ts`). Access-gated (JWT + fleet-ownership check for the target host/session). Dispatches the same `/id reset` input ComposeBox dispatches today via `dispatchResetPayload` → `funnel.send` (ComposeBox.tsx L1867-1888). The endpoint replicates the server-side dispatch seam; the client-side compose-box UI fx stay in ComposeBox untouched. Rewire ComposeBox's existing `funnel.send` call site — that ONE line becomes an `authApi.post` to the new endpoint (mechanical swap; the drain-sweep animation + text-clearing + error-message-set behavior is preserved verbatim, only the dispatch call target changes). Register the new router at `src/backend/database/database.ts` alongside the existing `app.use("/sessions", sessionsRoutes)` block (L1917). Add nginx `location ^~ /agent-reset/` blocks in BOTH `docker/nginx.conf` AND `docker/nginx-https.conf` (Pitfall 6).

Purpose: after Wave 0, contextPct has a single source of truth (fleet-status), agent-reset has a single seam (the new endpoint), and both surfaces (PrettyView + future relay-pane badge) subscribe/dispatch identically. Plan 06 Task 2 (badge meter) can drop `useSessionContextPct(hostId, tmuxSession)` in; Plan 06 Task 3 (badge reset) can drop `authApi.post('/agent-reset/${hostId}/${tmuxSession}')` in — no scavenger hunts, no "how do we source this" questions.

Output: fleet-status contextPct plumbing lands end-to-end; PrettyView is mechanically state-swap-safe (regression tests prove the meter still reads correctly); new agent-reset endpoint lands with auth + access gates; nginx dual-update lands; backend build clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-CONTEXT.md
@.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-RESEARCH.md
@.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend fleet-status contextPct promotion — shared store + dual-write from claude-session-server + fleet-status field</name>
  <files>
    src/backend/fleet-status/contextpct-store.ts,
    src/backend/fleet-status/contextpct-store.test.ts,
    src/backend/fleet-status/types.ts,
    src/backend/fleet-status/wire-protocol.ts,
    src/backend/fleet-status/fleet-status-server.ts,
    src/backend/fleet-status/fleet-status-server.test.ts,
    src/backend/claude-session/claude-session-server.ts
  </files>
  <read_first>
    src/backend/claude-session/claude-session-server.ts (L3174-3232 for the dormant-branch context_pct emission site; L7020-7080 for the primary contextPctTimer emission site — both must dual-write into the shared store),
    src/backend/fleet-status/types.ts (existing per-session record schema — extend with contextPct),
    src/backend/fleet-status/wire-protocol.ts (SessionStateSchema at L312-340 — add contextPct as z.number().nullable().optional() per the additive-optional invariant documented at L306-310, matching the shape used by lastMessageAt / aiTitle / dormant / recycling),
    src/backend/fleet-status/fleet-status-server.ts (tick loop that publishes per-session records; find the populate site and add contextPct: getContextPct(sessionKey) ?? null),
    src/backend/utils/database-save-trigger.ts (invariant reference — Wave 0 does NOT write to any user row; flag if inspection shows a DB touch),
    .planning/phases/90-.../90-RESEARCH.md § Pattern 3 (reuse per-agent state channel; the REVISED note describes the promotion mechanism)
  </read_first>
  <behavior>
    - contextpct-store Test 1: `setContextPct(hostId, tmuxSession, pct)` stores the value; `getContextPct(hostId, tmuxSession)` returns it. Round-trip.
    - contextpct-store Test 2: `getContextPct(hostId, tmuxSession)` returns `null` for an unknown session key (never throws, never undefined-shape).
    - contextpct-store Test 3: `setContextPct(hostId, tmuxSession, null)` explicitly stores null (dormant → null is a valid signal per PrettyView's context_pct dormant branch); subsequent `getContextPct` returns null.
    - contextpct-store Test 4: `deleteContextPct(hostId, tmuxSession)` removes the entry; subsequent `getContextPct` returns null.
    - contextpct-store Test 5: session key format is `${hostId}:${tmuxSession}` (matches session-working-store convention documented in RESEARCH.md § Pattern 3 and Pitfall 2 — D-10 correctness).
    - claude-session-server Test 6 (dormant branch, L3219): when the WS emits `{type:"context_pct", pct, dormant:true}`, the same emission ALSO calls `setContextPct(hostId, activeTmuxSession, pct)` in the shared store. Test via `vi.spyOn` on the store's setter; assert the setter received the pct value in the dormant branch.
    - claude-session-server Test 7 (primary timer branch, L7074): when the WS emits `{type:"context_pct", pct}`, the same emission ALSO calls `setContextPct(hostId, activeTmuxSession, pct)`. Test symmetric to Test 6.
    - claude-session-server Test 8 (WS-emission preservation): the WS `context_pct` emission itself is NOT removed — grep-verify `ws.send(JSON.stringify({ type: "context_pct"` appears BOTH before and after the plan (line-count comparison; both call sites at L3219 + L7074 stay).
    - fleet-status-server Test 9: on every tick, the per-session record for a session with a stored contextPct includes `contextPct: <that value>`; a session with no stored value gets `contextPct: null`.
    - fleet-status-server Test 10: additive-optional invariant preserved — SessionStateSchema still parses records from an older shape (no contextPct field) without validation failure.
  </behavior>
  <action>
    Create `src/backend/fleet-status/contextpct-store.ts` — a small in-memory keyed store mirroring `session-working-store.ts`'s API shape. Exports:
    - `setContextPct(hostId: number, tmuxSession: string, pct: number | null): void`
    - `getContextPct(hostId: number, tmuxSession: string): number | null`
    - `deleteContextPct(hostId: number, tmuxSession: string): void`
    - Internal `Map<string, number | null>` keyed on `${hostId}:${tmuxSession}` (exact format matching session-working-store per D-10 correctness).

    Add the `contextpct-store.test.ts` covering behaviors 1-5. Use vitest.

    Modify `src/backend/claude-session/claude-session-server.ts` — TWO precise edits at L3219 and L7074:

    - **L3219 dormant branch**: BEFORE the existing `wsSend(JSON.stringify({ type: "context_pct", pct, dormant: true }));` line, insert `setContextPct(hostId, activeTmuxSession, pct);`. Import `setContextPct` from `../fleet-status/contextpct-store.js`. Verify `activeTmuxSession` is in scope — grep the surrounding function; if it's a different variable name at that site, use the correct name. If the site does not have `hostId` in scope but has a session-identifier variable, resolve to `hostId + tmuxSession` at the emission call site.
    - **L7074 primary timer branch**: same shape — BEFORE `ws.send(JSON.stringify({ type: "context_pct", pct }));`, insert `setContextPct(hostId, activeTmuxSession, pct);`. Same import.
    - **KEEP the existing `ws.send` / `wsSend` calls unchanged** (backwards compat during transition; test 8 grep-verifies).

    Extend `src/backend/fleet-status/types.ts` — add `contextPct: number | null` to the per-session inbound record shape if that shape is used to represent live state (grep existing definitions; the exact schema name may be `SessionState` or similar).

    Extend `src/backend/fleet-status/wire-protocol.ts` — add `contextPct: z.number().nullable().optional()` to `SessionStateSchema` at L312-340. Follow the exact pattern used by `lastMessageAt` (L325) / `aiTitle` (L327) / `dormant` (L329) / `recycling` (L331) / `lastStopAt` (L333). Docblock comment noting Phase 90 Wave 0 alongside the existing block-comment pattern. FRAME_SCHEMA_VERSION stays at 1 (additive-optional invariant explicitly documented at L306-310).

    Extend `src/backend/fleet-status/fleet-status-server.ts` — on every per-session record publication, populate `contextPct: getContextPct(state.hostId, state.tmuxSession ?? '') ?? null`. If the tmuxSession is null (dormant sessions with no live tmux), fall back to null (Test 9). Import `getContextPct` from `./contextpct-store.js`.

    Add tests in `contextpct-store.test.ts` (behaviors 1-5) and extend `fleet-status-server.test.ts` (behaviors 9-10). For behaviors 6-8, add tests in `claude-session-server.test.ts` (or if that file is too large / hard to add to, in a scoped side-file like `claude-session-server.contextpct-dual-write.test.ts`). Mock the store via `vi.mock('../fleet-status/contextpct-store.js')`.

    NO DB writes → DatabaseSaveTrigger.forceSave does NOT apply (invariant preserved). NO new secrets. Structured logging at boundaries: `databaseLogger.debug({operation: 'context_pct_store_set', hostId, tmuxSession, pct})` on each set — high-volume, guard with a debug-level check; NEVER `JSON.stringify(event)`.
  </action>
  <verify>
    <automated>npm run build:backend &amp;&amp; npx vitest run src/backend/fleet-status/contextpct-store.test.ts src/backend/fleet-status/fleet-status-server.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/backend/fleet-status/contextpct-store.ts` + test file exist.
    - `grep -c "export function setContextPct\\|export function getContextPct" src/backend/fleet-status/contextpct-store.ts` returns >= 2.
    - `grep -c "setContextPct(" src/backend/claude-session/claude-session-server.ts` returns >= 2 (one dual-write per emission site — dormant + primary timer branches).
    - `grep -c "ws.send(JSON.stringify({ type: \"context_pct\"\\|wsSend(JSON.stringify({ type: \"context_pct\"" src/backend/claude-session/claude-session-server.ts` returns >= 2 (WS emissions PRESERVED — backwards compat during transition).
    - `grep -c "contextPct:.*z\\.number().nullable().optional()" src/backend/fleet-status/wire-protocol.ts` returns >= 1 (schema field added following additive-optional invariant).
    - `grep -c "getContextPct(" src/backend/fleet-status/fleet-status-server.ts` returns >= 1 (populate site).
    - `npm run build:backend` exits 0 (Pitfall 7).
    - `npx vitest run` scoped to the three touched test files reports all tests passing.
    - No modification to `src/ui/features/pretty-view/` in THIS task (Task 2 owns that touch).
    - No DB writes → `DatabaseSaveTrigger.forceSave` invariant not triggered.
  </acceptance_criteria>
  <done>
    Fleet-status is the authoritative source of contextPct. Both PrettyView (after Task 2 mechanical swap) and Plan 06's badge appendage subscribe to the same fleet-status field via the same key format. WS emissions from claude-session-server preserved for backwards compat. Backend build clean. Ready for Task 2 to swap PrettyView's local useState for the hook.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Frontend fleet-status hook + PrettyView mechanical state-source swap (D-03 waiver)</name>
  <files>
    src/ui/api/fleet-status-client.ts,
    src/ui/api/fleet-status-client.test.ts,
    src/ui/features/pretty-view/PrettyView.tsx,
    src/ui/features/pretty-view/PrettyView.test.tsx
  </files>
  <read_first>
    src/ui/api/fleet-status-client.ts (existing fleet-status subscription hooks + client — the new hook lives alongside; grep for the existing hook shape and mirror it),
    src/ui/api/fleet-status-types.ts (frontend-side per-session record type — extend to include contextPct if the type is imported by fleet-status-client),
    src/ui/features/pretty-view/PrettyView.tsx (L570 — `const [contextPct, setContextPct] = useState<number | null>(null);` — THIS is the site to swap; L2269-2273 — `case "context_pct": { ... setContextPct(parsed.pct); break; }` — THIS is the WS handler to no-op; L3704 `contextPct={contextPct}` — the prop-down to ComposeBox which continues working identically after the swap; L1815-1830 — the wsMessage type-narrow that gates the parsed frame branch; the context_pct case at L2269 becomes unreachable / a no-op after the swap since the frontend no longer needs to consume the WS variant),
    src/ui/features/pretty-view/PrettyView.test.tsx (test-suite structure — extend with a mechanical-swap regression test proving contextPct still renders correctly post-refactor)
  </read_first>
  <behavior>
    - fleet-status-client Test 1: `useSessionContextPct(hostId, tmuxSession)` returns the current `contextPct` for the given session from the subscribed fleet-status stream; returns null when the session record has no contextPct field OR when the session is not present in the current fleet-status snapshot.
    - fleet-status-client Test 2: subscribing/unsubscribing behavior — mounting a component that uses the hook subscribes; unmounting cleans up (mirror the existing hook pattern in this file).
    - fleet-status-client Test 3: hook key format — internal lookup uses `${hostId}:${tmuxSession}` OR whatever the fleet-status client's existing per-session lookup key is (grep-verify at plan time; MUST match the backend store key to preserve D-10 correctness).
    - fleet-status-client Test 4: reactive updates — a change to the underlying fleet-status snapshot re-renders consumers (useSyncExternalStore semantics).
    - PrettyView Test 5 (mechanical swap regression): the ComposeBox's contextPct prop still receives the current value after the swap. Mount PrettyView with mocked fleet-status returning `contextPct=42` for the target session; assert ComposeBox receives `contextPct={42}`.
    - PrettyView Test 6 (mechanical swap regression): PrettyView renders identically before/after the WS handler no-op — meter still updates when fleet-status snapshot changes; NO regression on any pre-existing PrettyView test.
    - PrettyView Test 7 (WS handler no-op): a `context_pct` WS frame arriving at PrettyView's onmessage handler does NOT throw, does NOT crash, does NOT trigger a stale setter (the useState is gone). The type-narrow gate at L1815-1830 may still exist for other reasons — the CASE branch body at L2269-2273 becomes a no-op (or is removed entirely; planner picks — no-op is safer if any downstream code path still references the frame).
  </behavior>
  <action>
    Extend `src/ui/api/fleet-status-client.ts` with a new hook `useSessionContextPct(hostId: number, tmuxSession: string): number | null`. Mirror the existing hook pattern in the file (grep for existing `useSession*` or similar exports). The hook subscribes to the fleet-status stream via `useSyncExternalStore` and returns the `contextPct` field for the matching session record (matching on `${hostId}:${tmuxSession}` OR the existing key convention — grep to confirm).

    Frontend-side type extension: if `src/ui/api/fleet-status-types.ts` (or wherever the per-session record type is defined on the frontend) needs `contextPct: number | null` added, add it as OPTIONAL to preserve backward-compat with any cached snapshots.

    Add tests in `fleet-status-client.test.ts` covering behaviors 1-4.

    **PrettyView.tsx mechanical swap (D-03 WAIVER — greenlit Alice 2026-09-08):**

    - **At L570**: REPLACE `const [contextPct, setContextPct] = useState<number | null>(null);` with `const contextPct = useSessionContextPct(hostId, tmuxSession ?? '');`. Import `useSessionContextPct` from `../../api/fleet-status-client` (or the correct relative path). The variable `contextPct` continues to exist with the same type + same behavior end-to-end.
    - **At L2269-2273**: REPLACE the case body content with a no-op (or remove the case entirely if TypeScript strictness allows). Suggested no-op body:
      ```
      case "context_pct": {
        // Phase 90 Wave 0: fleet-status is now the single source of truth for
        // contextPct (D-10 delivery mechanism, Alice 2026-09-08 D-03 waiver).
        // Backend still emits this frame for backwards compat during transition;
        // the frontend no longer consumes it here — the meter reads via
        // useSessionContextPct at L570.
        break;
      }
      ```
    - **At L3704**: NO CHANGE — `contextPct={contextPct}` continues working; the variable name and type are identical.
    - **Do NOT touch anything else in PrettyView.tsx.** The mechanical swap discipline: only these two sites change. Every other line of PrettyView.tsx stays byte-for-byte identical. Grep-verify with `git diff --stat src/ui/features/pretty-view/PrettyView.tsx` — the change count should be small (single-digit lines outside the two swap sites + one import).

    Add regression tests in `PrettyView.test.tsx` covering behaviors 5-7. Mock `useSessionContextPct` via `vi.mock('@/api/fleet-status-client')` returning a controllable value. The regression tests are the guardrail: if a future refactor breaks the mechanical-swap invariant, these tests fail.

    NO edits to `src/ui/features/pretty-view/ComposeBox.tsx` in this task (Task 3 owns the reset-button rewire). NO edits to any other pretty-view file. ComposeBox continues receiving `contextPct` via props unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run src/ui/api/fleet-status-client.test.ts src/ui/features/pretty-view/PrettyView.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function useSessionContextPct\\|export const useSessionContextPct" src/ui/api/fleet-status-client.ts` returns exactly 1.
    - `grep -c "useSessionContextPct(hostId" src/ui/features/pretty-view/PrettyView.tsx` returns exactly 1 (swap site).
    - `grep -c "useState<number | null>(null)" src/ui/features/pretty-view/PrettyView.tsx` — count DECREASES by exactly 1 vs pre-plan baseline (the contextPct useState is gone; other unrelated useStates remain).
    - `grep -c "setContextPct" src/ui/features/pretty-view/PrettyView.tsx` returns 0 (setter is gone alongside the useState).
    - The WS `context_pct` case block still exists (no-op body) OR is removed cleanly — either way, `grep -c "case \"context_pct\":" src/ui/features/pretty-view/PrettyView.tsx` returns 0 or 1 (planner's choice); tsc passes.
    - `grep -c "contextPct={contextPct}" src/ui/features/pretty-view/PrettyView.tsx` returns >= 1 (prop-down to ComposeBox preserved).
    - `git diff --stat src/ui/features/pretty-view/PrettyView.tsx` shows small (~5-15 line) mechanical diff, NOT a rewrite. Verify no unrelated changes.
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run src/ui/features/pretty-view/PrettyView.test.tsx` — every pre-existing PrettyView test still passes (compare pre/post pass counts; ZERO regression).
    - `npx vitest run src/ui/api/fleet-status-client.test.ts` — all new hook tests pass.
    - No modification to `src/ui/features/pretty-view/ComposeBox.tsx` in THIS task.
    - No modification to any other file under `src/ui/features/pretty-view/`.
  </acceptance_criteria>
  <done>
    PrettyView reads contextPct from fleet-status via `useSessionContextPct` — mechanical state-source swap complete. WS `context_pct` handler is a no-op. Meter renders identically. Zero UX change, zero behavior change. Regression tests guardrail the invariant. Ready for Task 3 (agent-reset endpoint + ComposeBox rewire) and downstream Plans 05/06 to consume the same hook.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Agent-reset backend endpoint + ComposeBox reset dispatch rewire + nginx dual-update</name>
  <files>
    src/backend/database/routes/agent-reset.ts,
    src/backend/database/routes/agent-reset.test.ts,
    src/backend/database/database.ts,
    src/ui/features/pretty-view/ComposeBox.tsx,
    src/ui/features/pretty-view/ComposeBox.test.tsx,
    docker/nginx.conf,
    docker/nginx-https.conf
  </files>
  <read_first>
    src/backend/database/routes/sessions.ts (JWT middleware + Express router pattern — structural model; L40 authManager.createAuthMiddleware + L294 authenticateJWT for the auth gate),
    src/backend/database/database.ts (L1830-1930 — router mount block; L1917 `app.use("/sessions", sessionsRoutes)` is the exact pattern the new `app.use("/agent-reset", agentResetRoutes)` mount mirrors),
    src/backend/claude-session/claude-session-server.ts (grep the existing text-send path — the mechanism that dispatches typed input into a session; the new endpoint calls this same seam so /id reset lands the same way as when a user types it in ComposeBox),
    src/ui/features/pretty-view/ComposeBox.tsx (L1867-1888 dispatchResetPayload — the client-side dispatch chain: builds "/id reset" payload; calls `funnel.send(payload, {trigger: "reset"})` at L1877; on success clears text + calls onResetClicked. Task 3 rewires ONLY the `funnel.send` call site — the drain-sweep animation at L1848-1862 in fireResetSyncFx stays; the text clear + onResetClicked stay; the error-message path stays. Grep for the funnel.send import site and understand the funnel API to know whether to keep the funnel wrapper or replace it),
    docker/nginx.conf (L726-749 existing /claude-session/websocket/ block — WS pattern; L701 /fleet-status/ block — REST prefix pattern; the new /agent-reset/ block follows the REST prefix shape),
    docker/nginx-https.conf (matching HTTPS-side blocks),
    .planning/phases/90-.../90-RESEARCH.md § Don't Hand-Roll table (Reset input dispatch row — recommendation to add the endpoint and rewire ComposeBox)
  </read_first>
  <behavior>
    - agent-reset Test 1: unauthenticated request -> 401 (JWT middleware intercepts).
    - agent-reset Test 2: authenticated user tries to reset a host/tmuxSession they don't own -> 404. Same status for 'session not found' and 'not owner' (no oracle per Security V8 pattern established in Plan 04).
    - agent-reset Test 3: authenticated user with fleet access to the target host/tmuxSession -> the endpoint dispatches the `/id reset` input via the same seam ComposeBox's `funnel.send({trigger:'reset'})` uses today. Response 200 with `{ok: true, mqid?: string}` (mqid optional — depends on whether the dispatch layer returns one).
    - agent-reset Test 4: invalid hostId path arg (non-numeric) -> 400.
    - agent-reset Test 5: invalid tmuxSessionName (fails tmux-safe grammar — grep the existing tmux-name validator; if none exists, `/^[a-zA-Z0-9_-]{1,64}$/`) -> 400.
    - agent-reset Test 6: the dispatch call MUST include the same optional body-text passthrough ComposeBox supports (`/id reset (foo)` when body is non-empty); the endpoint accepts optional `body?: string` in the request payload and constructs the payload identically to ComposeBox's `dispatchResetPayload` at L1867-1876.
    - agent-reset Test 7: structured logging — `databaseLogger.info({operation: 'agent_reset_ok', userId, hostId, tmuxSession})` on success; `.warn({operation: 'agent_reset_denied', ...})` on 404; `.error({operation: 'agent_reset_dispatch_failed', ...})` on dispatch error. NEVER log the body content (privacy).
    - agent-reset Test 8: NO user-row DB write → DatabaseSaveTrigger.forceSave invariant does NOT apply (flag if inspection surfaces a write).

    - ComposeBox Test 9 (rewire regression): the reset button click still fires the drain-sweep animation (fireResetSyncFx) — verify via test-visible state or side-effect (isDraining flag or a spy on pulseOnTimerRef/pulseOffTimerRef).
    - ComposeBox Test 10 (rewire regression): the reset button click still calls `onResetClicked?.()` on dispatch success — mock the new `authApi.post('/agent-reset/...')` to resolve; assert onResetClicked was called (same discipline as pre-plan behavior at L1879-1882).
    - ComposeBox Test 11 (rewire regression): the reset button click still clears the textarea on success (setText("") + clearAfterSend) — assert via the textarea's value going from "hello" → "".
    - ComposeBox Test 12 (rewire regression): dispatch failure (authApi.post rejects) → error message is set + textarea + onResetClicked are NOT called (mirrors the pre-plan `dispatched=false` branch at L1885-1887).
    - ComposeBox Test 13: no regression on any pre-existing ComposeBox test — all previous ComposeBox.*.test.tsx files still pass.

    - nginx: BOTH docker/nginx.conf AND docker/nginx-https.conf carry `location.*/agent-reset` blocks (planner picks between `location = /agent-reset/...` with regex OR `location ^~ /agent-reset/` prefix). Diff between the two files' new-block content shows identical proxy_pass shape.
  </behavior>
  <action>
    Create `src/backend/database/routes/agent-reset.ts` — new Express router module. Structural mirror: `src/backend/database/routes/sessions.ts`. Single POST endpoint:

    ```
    router.post(
      "/:hostId/:tmuxSessionName",
      authenticateJWT,
      async (req, res) => {
        const hostId = parseInt(req.params.hostId, 10);
        if (!Number.isInteger(hostId) || hostId <= 0) {
          return res.status(400).json({ok: false, error: "invalid_host_id"});
        }
        const tmuxSession = req.params.tmuxSessionName;
        if (!/^[a-zA-Z0-9_-]{1,64}$/.test(tmuxSession)) {
          return res.status(400).json({ok: false, error: "invalid_tmux_session"});
        }
        // Access-control gate: verify req.userId owns/has-access-to the (hostId, tmuxSession).
        // Grep for the existing fleet-ownership gate pattern — likely used by /claude-session/websocket/
        // upgrade auth. Reuse the same gate. Missing/wrong owner -> 404 (no oracle).
        const owned = await verifyUserOwnsSession(req.userId, hostId, tmuxSession);
        if (!owned) {
          databaseLogger.warn({operation: "agent_reset_denied", userId: req.userId, hostId, tmuxSession});
          return res.status(404).json({ok: false, error: "not_found"});
        }
        const optionalBody = typeof req.body?.body === "string" ? req.body.body : "";
        const trimmed = optionalBody.trim();
        const payload = trimmed
          ? `/id reset (${collapseNewlinesForSend(trimmed)})`
          : "/id reset";
        // Dispatch through the same seam ComposeBox uses today.
        // ComposeBox calls funnel.send(payload, {trigger:'reset'}); the backend-side
        // equivalent is the input-dispatch seam claude-session-server exposes.
        // Grep the send-input pathway in claude-session-server for the exact primitive.
        const dispatch = await dispatchSessionInput(hostId, tmuxSession, payload, {trigger: "reset"});
        if (!dispatch.ok) {
          databaseLogger.error({operation: "agent_reset_dispatch_failed", userId: req.userId, hostId, tmuxSession, error: dispatch.error});
          return res.status(502).json({ok: false, error: "dispatch_failed"});
        }
        databaseLogger.info({operation: "agent_reset_ok", userId: req.userId, hostId, tmuxSession});
        return res.status(200).json({ok: true, mqid: dispatch.mqid ?? null});
      },
    );
    ```

    Grep `dispatchSessionInput` / equivalent primitive in `src/backend/claude-session/`; if none exists as a shared primitive, extract the input-send path from the WS handler into a small helper so both the WS path AND the new endpoint can call it. Alternative planner-flex: if the input-send is tightly coupled to a live WS connection, the endpoint spawns a synthetic connection OR the endpoint proxies to the WS server via an internal RPC. Planner picks the smallest-diff path; document the choice in the endpoint JSDoc.

    Grep `verifyUserOwnsSession` / equivalent fleet-ownership gate — reuse the SAME check `/claude-session/websocket/` uses on upgrade so access parity holds.

    Register the router in `src/backend/database/database.ts` alongside `app.use("/sessions", sessionsRoutes)` at L1917. Add `import agentResetRoutes from "./routes/agent-reset.js";` in the imports block at L61 (alongside `import sessionsRoutes from "./routes/sessions.js";`) and `app.use("/agent-reset", agentResetRoutes);` after L1917.

    Add tests in `src/backend/database/routes/agent-reset.test.ts` covering behaviors 1-8. Use supertest style (mirror `sessions.test.ts`). Mock `dispatchSessionInput` + `verifyUserOwnsSession` via `vi.mock`.

    **ComposeBox rewire (mechanical D-03-waiver — Alice 2026-09-08):**

    Only touch `dispatchResetPayload` at L1867-1888 in ComposeBox.tsx. Replace the `funnel.send(payload, {trigger:'reset'})` call on L1877 with an `authApi.post` to the new endpoint. Preserve every other line of dispatchResetPayload verbatim: the trimmed check, the payload construction, the drain-sweep already-fired-via-fireResetSyncFx, the onResetClicked-on-success discipline, the setText clear, the clearAfterSend, the "Not connected" error branch.

    Suggested rewire shape:
    ```
    function dispatchResetPayload(body: string) {
      const trimmed = body.trim();
      // Endpoint constructs the /id reset payload server-side from body — pass body verbatim.
      authApi.post(`/agent-reset/${hostId}/${encodeURIComponent(tmuxSession)}`, { body: trimmed })
        .then((response) => {
          if (response?.data?.ok === true) {
            onResetClicked?.();
            setText("");
            clearAfterSend();
          } else {
            setErrorMessage("Not connected — try again in a moment");
          }
        })
        .catch(() => {
          setErrorMessage("Not connected — try again in a moment");
        });
    }
    ```
    `hostId` and `tmuxSession` must be in scope in this function — grep the surrounding component; if they're not directly available, plumb via props (which is a bigger diff; if so, planner picks whether to (a) plumb them as new props to ComposeBox or (b) keep the funnel.send wrapper and have the funnel.send implementation itself delegate to the new endpoint. Option (b) is smaller-diff — grep `funnel.send` implementation to see if it's a small enough seam to rewire there instead. **Planner picks the smallest-diff option; document the choice.**)

    Add ComposeBox rewire regression tests (behaviors 9-13) in `src/ui/features/pretty-view/ComposeBox.test.tsx` OR a new co-located `ComposeBox.reset-endpoint-rewire.test.tsx` (planner picks based on test-file size + convention). Mock `authApi.post` via `vi.mock('@/main-axios')` and assert the URL + body + success/failure branches.

    **Nginx dual-update (Pitfall 6):**

    In BOTH `docker/nginx.conf` AND `docker/nginx-https.conf`, add a new block for the `/agent-reset/` prefix. Follow the REST-prefix pattern used by existing `/fleet-status/` block or the `/sessions/*` -> backend forward. Both blocks byte-identical except for HTTPS-vs-HTTP-specific edge concerns (Caddy vs raw).

    Suggested block:
    ```
    location ^~ /agent-reset/ {
      proxy_pass http://127.0.0.1:<BACKEND_PORT>;
      proxy_http_version 1.1;
      proxy_set_header Host $http_host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $proxy_x_forwarded_proto;
      proxy_read_timeout 60s;
      proxy_connect_timeout 10s;
      client_max_body_size 4k;
    }
    ```
    Port + exact header shape: grep the neighboring `/sessions/*` or `/fleet-status/` block for the actual patterns and replicate. Both nginx files MUST be updated in the same commit (Pitfall 6).

    Do NOT add nginx tests to the automated vitest suite (nginx does not run in unit-test context). Verification: diff between the two files' new-block content should have identical proxy config.
  </action>
  <verify>
    <automated>npm run build:backend &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run src/backend/database/routes/agent-reset.test.ts src/ui/features/pretty-view/ComposeBox.test.tsx &amp;&amp; diff &lt;(grep -A 15 "location.*/agent-reset" docker/nginx.conf) &lt;(grep -A 15 "location.*/agent-reset" docker/nginx-https.conf) | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/backend/database/routes/agent-reset.ts` + test file exist.
    - `grep -c "router.post" src/backend/database/routes/agent-reset.ts` returns >= 1.
    - `grep -c "app.use(\"/agent-reset\"" src/backend/database/database.ts` returns exactly 1 (router mounted).
    - `grep -c "import agentResetRoutes from \"./routes/agent-reset.js\"\\|from \"\\./routes/agent-reset" src/backend/database/database.ts` returns >= 1 (import).
    - `grep -c "/agent-reset/" src/ui/features/pretty-view/ComposeBox.tsx` returns >= 1 (ComposeBox rewired to hit new endpoint — either via direct authApi.post or via funnel.send delegating).
    - `grep -c "location.*/agent-reset" docker/nginx.conf` returns >= 1.
    - `grep -c "location.*/agent-reset" docker/nginx-https.conf` returns >= 1.
    - `grep -c "\"agent_reset_ok\"\\|\"agent_reset_denied\"\\|\"agent_reset_dispatch_failed\"" src/backend/database/routes/agent-reset.ts` returns >= 3 (structured logging at boundaries).
    - `npm run build:backend` exits 0 (Pitfall 7).
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run src/backend/database/routes/agent-reset.test.ts` — all 8 endpoint tests pass.
    - `npx vitest run src/ui/features/pretty-view/ComposeBox.test.tsx` — pre-existing tests + new rewire regression tests all pass. ZERO regression on existing ComposeBox behavior (drain-sweep, text-clear, onResetClicked, error message).
    - No modification to `src/ui/features/pretty-view/PrettyView.tsx` in THIS task (Task 2 owns that touch; here ComposeBox alone is touched).
    - No DB writes → DatabaseSaveTrigger.forceSave invariant not triggered.
  </acceptance_criteria>
  <done>
    Agent-reset endpoint lands as the single seam both PrettyView and future relay-pane badge appendage dispatch through. ComposeBox's existing reset button is mechanically rewired (drain-sweep, text-clear, onResetClicked, error-message discipline all preserved verbatim — only the dispatch call target changes). Nginx dual-update lands. Access-control + JWT auth + structured logging all in place. Backend build clean. Plan 06 Task 3 can now drop a symmetric `authApi.post('/agent-reset/${hostId}/${tmuxSession}')` call from the badge appendage — same behavior, same endpoint, single seam.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend claude-session-server → fleet-status contextpct-store | Backend-internal shared map; no external exposure. |
| Backend fleet-status-server → browser fleet-status client | Backend-authoritative per-session record shape; browser consumes read-only snapshot. |
| Frontend PrettyView → backend fleet-status (mechanical swap) | Backend-authoritative; PrettyView is a passive consumer post-swap. Zero UX change, zero behavior change. |
| Browser → POST /agent-reset/:hostId/:tmuxSessionName | Untrusted client-supplied hostId + tmuxSessionName + optional body. Validated + JWT-authed + access-gated. |
| Backend agent-reset router → claude-session input-dispatch seam | Backend-internal; reuses same input-dispatch path as existing WS handler. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-00-I1 | Information Disclosure | contextPct promoted to fleet-status | accept | contextPct is not PII (already visible on the harness pane meter today). Fleet-status is user-scoped by existing gates; per-session records are only sent to users with fleet access. No new data exposure. |
| T-90-00-T1 | Tampering (D-03 violation regression) | PrettyView.tsx mechanical swap | mitigate | Regression tests (Task 2 Tests 5-7) prove meter still reads correctly post-swap; git diff --stat gate enforces the swap is mechanical (small diff, single-digit-line changes outside the two swap sites); D-03 waiver documented in the case-body comment + RESEARCH.md. |
| T-90-00-E1 | Elevation of Privilege | POST /agent-reset — cross-user reset | mitigate | JWT auth + `verifyUserOwnsSession(req.userId, hostId, tmuxSession)` gate before dispatch. Same status (404) for 'not owner' and 'not found' — no oracle (Security V8). Test 2 is the dedicated regression gate. |
| T-90-00-T2 | Tampering (input validation) | hostId + tmuxSessionName path args | mitigate | hostId: `Number.isInteger + > 0` gate. tmuxSession: `/^[a-zA-Z0-9_-]{1,64}$/` grammar gate (or existing tmux-name validator if one exists). encodeURIComponent on the client-side call for defense-in-depth (Test 3 URL construction). |
| T-90-00-D1 | Denial of Service | /agent-reset spam | mitigate | Existing JWT rate-limit infrastructure applies if configured (grep authenticateJWT for the rate-limit middleware; if none exists, add a per-user token bucket similar to Plan 04's WS rate limit — 5 resets per 30s window is a reasonable v1 cap). |
| T-90-00-I2 | Information Disclosure | Structured logging | mitigate | agent_reset_* log entries include userId + hostId + tmuxSession (safe operational fields) but NEVER the body content (privacy — the body may contain user-typed context). |
| T-90-00-T3 | Tampering (nginx dual-update) | Pitfall 6 discipline | mitigate | BOTH nginx configs carry `/agent-reset/` block; automated diff verify. Skipping one deploys silently-broken in that environment. |
| T-90-00-SC | Tampering (supply chain) | npm installs | n/a | This plan installs NO new packages. |

`security_enforcement=true`. T-90-00-E1 (cross-user reset) has a dedicated regression test. T-90-00-T1 (D-03 swap regression) has three regression tests plus a git-diff-size gate.
</threat_model>

<verification>
- `npm run build:backend` exits 0 (Pitfall 7).
- `npx tsc --noEmit` exits 0.
- `npx vitest run` scoped to all touched test files reports all tests passing, especially:
  - Task 1 behaviors 1-10 (contextPct store + dual-write + fleet-status field)
  - Task 2 behaviors 5-7 (PrettyView mechanical-swap regression gates)
  - Task 3 behaviors 1-13 (endpoint + ComposeBox rewire regression gates)
- No file under `src/ui/features/pretty-view/` OTHER than PrettyView.tsx (Task 2) and ComposeBox.tsx (Task 3) is modified.
- PrettyView.tsx `git diff --stat` shows small mechanical diff (~5-15 lines).
- ComposeBox.tsx `git diff --stat` shows small mechanical diff (~10-30 lines — the single dispatch call rewire).
- Both nginx configs carry `location.*/agent-reset` blocks (grep gate + diff verify).
- No DB writes → DatabaseSaveTrigger.forceSave invariant not triggered.
</verification>

<success_criteria>
Wave 0 delivers the D-10 delivery mechanism (per Alice 2026-09-08 plan-checker resolution):

1. **contextPct is authoritative in fleet-status** — dual-written on emission from claude-session-server, published on every fleet-status tick, subscribed by both PrettyView (mechanical D-03 waiver) and (in Plan 06) the relay-pane badge appendage. Zero drift risk. D-10 correctness is a subscription-level invariant, not a manual-coordination burden.

2. **Agent-reset is a single seam** — new endpoint dispatches through the same input path ComposeBox's funnel.send uses today; ComposeBox is mechanically rewired to hit the endpoint; Plan 06 Task 3's relay-pane badge appendage will hit the same endpoint. Reset behaves identically regardless of which surface fires it.

3. **PrettyView is untouched EXCEPT for the mechanical state-source swap** (D-03 waiver, greenlit Alice 2026-09-08). Regression tests are the guardrail; small-diff gate is the check.

4. **Nginx dual-update lands** for the new `/agent-reset/` prefix (Pitfall 6).

5. **Backend build clean** (Pitfall 7).

Plan 06 Task 2 can now consume `useSessionContextPct(hostId, tmuxSessionName)` directly; Plan 06 Task 3 can call `authApi.post('/agent-reset/${hostId}/${encodeURIComponent(tmuxSessionName)}', {body: ''})` from the badge-reset click handler. No scavenger hunts, no follow-up deferrals.
</success_criteria>

<output>
Create `.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-00-SUMMARY.md` when done.
</output>
