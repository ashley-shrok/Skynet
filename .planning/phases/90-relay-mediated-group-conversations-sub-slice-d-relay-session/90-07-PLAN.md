---
phase: 90-relay-mediated-group-conversations-sub-slice-d-relay-session
plan: 07
type: execute
wave: 5
depends_on: [90-01, 90-05, 90-06]
files_modified:
  - src/ui/shell/RelayRoomSessionPane.tsx
  - src/ui/shell/RelayRoomSessionPane.test.tsx
  - src/ui/shell/tabUtils.tsx
  - src/ui/shell/tabUtils.test.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
  - src/ui/AppShell.tsx
autonomous: true
requirements: [D-15-consumption, D-18, Kind-Discriminator-Branch, Sidebar-To-Tab-Kind-Thread, BLOCKER-3-fix]
tags: [frontend, shell, pane-wrapper, kind-branch, tab-router, sidebar-to-tab, wire-up, wave-close, correct-file-path, correct-dispatch-chain]
must_haves:
  truths:
    - "`RelayRoomSessionPane` component wraps `RelayRoomPane` with the same shell contract `IdentitySessionPane` follows (forwardRef, imperative-handle no-ops for relay-pane-irrelevant actions like togglePrettyMode/toggleMessageQueue) — mirrors src/ui/shell/IdentitySessionPane.tsx. NOTE: `viewingUserMxid` is NOT threaded down as a prop — RelayRoomPane sources it internally via `useViewingUserMxid()` (Plan 05 W#8 resolution)."
    - "`tabUtils.tsx` selection dispatcher grows a third branch: when `tab.sessionKind === 'relay-room'` AND `tab.relayRoomId` is set, mount `<RelayRoomSessionPane>`; otherwise fall through to the existing identity-pane / terminal-tab branches unchanged (defense: log-and-fall-through if sessionKind is 'relay-room' but relayRoomId is missing)."
    - "**Sidebar row-click dispatch chain (BLOCKER #3 fix — CORRECTED FILE PATH + CORRECTED DISPATCH CHAIN):** the row-click handler lives at `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` L980-1003 `handleRowSelect` (NOT `src/ui/sidebar/PrettyConversationsPanel.tsx` which does not exist). Row-click for a normal conversation-row calls `selectConversation(row.id)` from `conversation-store.ts` L1225 + optional `onConversationSelected?.(row.id)` callback (L993, L998). Does NOT go through `openSessionInTree` (which is a DROP handler for split-tree layout at `AppShell.tsx` L1663, unrelated to normal row-click)."
    - "The `kind` marker + roomId + roomTitle ride along with the FleetSession row (widened by Plan 01). The tab-spawn logic that reacts to `selectConversation`/`onConversationSelected` must branch on the row's `kind` and construct the Tab with `sessionKind` + `relayRoomId` + `relayRoomTitle` fields set for relay-room rows. The concrete edit sites (PrettyConversationsPanel handleRowSelect + AppShell openTab-adjacent + wherever selectConversation drives tab spawning) are enumerated in the task actions below."
    - "Fresh page-load with a running relay-room session on the fleet: clicking the sidebar row opens a relay pane (NOT a pretty view or terminal). Verified end-to-end via a rendering integration test that fakes `/sessions/list` returning `kind: 'relay-room'` and asserts the mounted pane component is RelayRoomSessionPane."
    - "Existing harness-session click flow is byte-identical — no regression on pretty view mounting for `kind: 'harness'` rows. Grep-verify that `tabUtils.tsx` still contains the pre-existing `IdentitySessionPane` and `TerminalTabContent` branches unchanged."
  artifacts:
    - path: "src/ui/shell/RelayRoomSessionPane.tsx"
      provides: "Peer of IdentitySessionPane for relay-room tabs; mounts RelayRoomPane inside the shell contract."
      contains: "export const RelayRoomSessionPane"
    - path: "src/ui/shell/tabUtils.tsx"
      provides: "Third branch on TerminalOrIdentitySessionPane dispatcher for kind === 'relay-room'."
      contains: "sessionKind === \"relay-room\""
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "handleRowSelect widened with a relay-room branch that fires a new callback prop (or a widened onConversationSelected signature) carrying kind + roomId + roomTitle."
      contains: "relay-room"
    - path: "src/ui/AppShell.tsx"
      provides: "The callback prop wiring on the PrettyConversationsPanel invocation site — the branch that creates the Tab with sessionKind + relayRoomId + relayRoomTitle."
      contains: "sessionKind: \"relay-room\""
  key_links:
    - from: "src/ui/shell/tabUtils.tsx"
      to: "src/ui/shell/RelayRoomSessionPane.tsx"
      via: "kind === 'relay-room' branch mounts the wrapper"
      pattern: "RelayRoomSessionPane"
    - from: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      to: "src/ui/state/conversation-store.ts (selectConversation) + AppShell.tsx (callback branch that spawns Tab)"
      via: "row-click threads kind + roomId + roomTitle via a new callback prop OR a widened onConversationSelected signature; downstream tab-spawn constructs Tab with sessionKind + relayRoomId + relayRoomTitle"
      pattern: "sessionKind"
    - from: "src/ui/shell/RelayRoomSessionPane.tsx"
      to: "src/ui/features/relay-room-pane/RelayRoomPane.tsx"
      via: "wrapper mounts the fully-wired pane (Plan 06)"
      pattern: "RelayRoomPane"
---

<objective>
Close the phase by wiring the fully-built relay pane (Plans 05 + 06) into the app shell so a sidebar click on a `kind: 'relay-room'` row actually opens the RelayRoomPane. Three coordinated changes:

1. **`RelayRoomSessionPane.tsx` (NEW)** — peer of `src/ui/shell/IdentitySessionPane.tsx`. Wraps `RelayRoomPane` with the shell contract (forwardRef, imperative-handle no-ops for relay-pane-irrelevant actions like togglePrettyMode / toggleMessageQueue / disconnect). This is the shell-level type-compatibility layer so `tabUtils.tsx` can drop it in. NOTE: `viewingUserMxid` is NOT a prop — the pane sources it via `useViewingUserMxid()` (Plan 05 W#8 resolution).

2. **`tabUtils.tsx` widen** — the existing `TerminalOrIdentitySessionPane` dispatcher at L153-219 currently has two branches (`isIdentityPane ? IdentitySessionPane : TerminalTabContent`). Add a THIRD branch FIRST (most-specific discriminator wins): `if (tab.sessionKind === 'relay-room' && tab.relayRoomId) return <RelayRoomSessionPane ...>`. If `sessionKind` is 'relay-room' but `relayRoomId` is missing, log-and-fall-through rather than crash (defense against inconsistent tab-open call sites).

3. **Row-click dispatch — BLOCKER #3 fix** — CORRECTED FILE PATH: `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` (NOT `src/ui/sidebar/PrettyConversationsPanel.tsx`, which does not exist). CORRECTED DISPATCH CHAIN: `handleRowSelect` at L980-1003 calls `selectConversation(row.id)` + `onConversationSelected?.(row.id)` for the default (conversation-row) branch — NOT `openSessionInTree` (that's a split-tree DROP handler at AppShell.tsx L1663). The kind marker rides along with the FleetSession row (Plan 01 widened `FleetSession` with `kind` + `roomId` + `roomTitle`). Widen the row-click path so relay-room rows: (a) add a new branch in `handleRowSelect` that fires a new callback prop (e.g., `onRelayRoomRowClick(row)`) BEFORE the default selectConversation path — analogous to how the existing rdpHostRow and fleetOnly branches gate ahead of the default (L991-1002); (b) AppShell wires the new callback to a `openTab`-style call that constructs a Tab with `sessionKind: 'relay-room'` + `relayRoomId: row.roomId` + `relayRoomTitle: row.roomTitle` (mirroring the existing onDetachedRowClick / onRdpRowClick shape at AppShell.tsx L2074-2095).

Purpose: this is the last mile. Every D-decision, every T-90-BE-*, every T-90-FE-*, every extraction, every WS wiring is done — this plan turns the abstract 'RelayRoomPane exists' into 'clicking a relay-room row in the sidebar opens the RelayRoomPane'. After this plan, the phase is deliverable to Ashley for UAT. BLOCKER #3 is fixed: the file path and dispatch chain match the actual codebase.

Output: tab-mount branch wired + sidebar-to-tab kind-threading wired via the CORRECT dispatch chain + RelayRoomSessionPane wrapper landed. A `kind: 'relay-room'` row in the sidebar opens the relay pane end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-CONTEXT.md
@.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-PATTERNS.md
@.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-RESEARCH.md
@.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-01-PLAN.md
@.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-05-PLAN.md
@.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-06-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RelayRoomSessionPane shell wrapper (peer of IdentitySessionPane)</name>
  <files>
    src/ui/shell/RelayRoomSessionPane.tsx,
    src/ui/shell/RelayRoomSessionPane.test.tsx
  </files>
  <read_first>
    src/ui/shell/IdentitySessionPane.tsx (structural mirror — forwardRef pattern at L79-406, imperative-handle exposure, mount logging at L109-118, edge-case logging at L121-128; do NOT modify),
    src/ui/features/relay-room-pane/RelayRoomPane.tsx (Plan 06 output — the pane component this wrapper mounts; NOTE: does NOT accept viewingUserMxid as a prop — sources internally via useViewingUserMxid),
    src/types/ui-types.ts (Tab type — read the relayRoomId + relayRoomTitle + sessionKind fields Plan 01 added),
    .planning/phases/90-.../90-PATTERNS.md § RelayRoomSessionPane.tsx section (structural mirror IdentitySessionPane.tsx L79-406 + what NOT to include per D-04)
  </read_first>
  <behavior>
    - Test 1: renders `<RelayRoomPane roomId={roomId} roomTitle={roomTitle} isVisible={isVisible} />` inside a `<div className="h-full w-full relative flex flex-col">` wrapper. NO viewingUserMxid prop threaded (Plan 05 Task 3 refactor — RelayRoomPane sources internally).
    - Test 2: forwardRef exposes an imperative handle with `togglePrettyMode: () => void`, `toggleMessageQueue: () => void`, `disconnect: () => void`, and any other methods the IdentitySessionPane handle exposes (grep for `useImperativeHandle` in IdentitySessionPane.tsx to enumerate). All are no-ops in the relay-pane wrapper (relay panes have no terminal mode, no queue drawer, no per-pane WS-disconnect concept beyond what RelayRoomPane owns internally).
    - Test 3: mount logs a structured record via console.info: `{operation: 'relay_room_session_pane_mount', tabId, roomId}`. Never `JSON.stringify` the tab or event objects.
    - Test 4: does NOT render a `Terminal`, `MessageQueueDrawer`, `IdentityBadge` (the top-right corner one — relay pane has its own IdentityBadgeRow), or `IdentityModal` (planner may reuse in a later slice; not v1 per PATTERNS.md § RelayRoomSessionPane.tsx 'What NOT to include').
    - Test 5: props threading — `tab.relayRoomId` -> RelayRoomPane's roomId prop; `tab.relayRoomTitle` -> roomTitle prop; `isVisible` prop from the shell -> RelayRoomPane's isVisible prop.
  </behavior>
  <action>
    Create `src/ui/shell/RelayRoomSessionPane.tsx`. Structural mirror: `src/ui/shell/IdentitySessionPane.tsx`.

    Signature:
    ```
    export interface RelayRoomSessionPaneProps {
      tab: Tab;
      roomId: string;
      roomTitle: string | null;
      isVisible: boolean;
      onCloseTab?: () => void;
    }
    export interface RelayRoomPaneHandle {
      togglePrettyMode: () => void;
      toggleMessageQueue: () => void;
      disconnect: () => void;
      // ... other IdentityPaneHandle methods; grep for the full interface in IdentitySessionPane.tsx and mirror the shape so tab.ref usage is polymorphic
    }
    export const RelayRoomSessionPane = forwardRef<RelayRoomPaneHandle, RelayRoomSessionPaneProps>(
      function RelayRoomSessionPane({tab, roomId, roomTitle, isVisible, onCloseTab}, ref) {
        useEffect(() => {
          console.info({operation: 'relay_room_session_pane_mount', tabId: tab.id, roomId});
        }, []);

        useImperativeHandle(ref, () => ({
          togglePrettyMode: () => {}, // no-op — relay panes have no terminal mode
          toggleMessageQueue: () => {}, // no-op — no queue drawer
          disconnect: () => {}, // WS lifecycle owned inside RelayRoomPane via visibility
          // ... other no-ops mirroring IdentityPaneHandle
        }), []);

        return (
          <div className="h-full w-full relative flex flex-col">
            <RelayRoomPane
              roomId={roomId}
              roomTitle={roomTitle}
              isVisible={isVisible}
              className="flex-1 min-h-0"
            />
          </div>
        );
      },
    );
    ```

    Do NOT include: Terminal, MessageQueueDrawer, top-right IdentityBadge, IdentityModal. Per PATTERNS.md § RelayRoomSessionPane.tsx 'What NOT to include'.

    NO `viewingUserMxid` prop — Plan 05 Task 3 established that RelayRoomPane sources it internally via `useViewingUserMxid()` (W#8 resolution). The wrapper is agnostic to the mxid.

    Structured logging discipline per PATTERNS.md § 2 — explicit fields, never JSON.stringify raw objects.

    Add tests in `RelayRoomSessionPane.test.tsx` covering the 5 behaviors above. Mock `RelayRoomPane` via `vi.mock('@/features/relay-room-pane/RelayRoomPane')` returning a test-controllable double so this test is scoped to the wrapper contract only.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run src/ui/shell/RelayRoomSessionPane.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/shell/RelayRoomSessionPane.tsx` + test file exist.
    - `grep -c "import.*RelayRoomPane\\|from.*RelayRoomPane" src/ui/shell/RelayRoomSessionPane.tsx` returns >= 1.
    - `grep -c "useImperativeHandle" src/ui/shell/RelayRoomSessionPane.tsx` returns >= 1 (shell contract preserved).
    - `grep -c "togglePrettyMode.*=> {}\\|togglePrettyMode: () => {}" src/ui/shell/RelayRoomSessionPane.tsx` returns >= 1 (no-op wired).
    - `grep -c "\"relay_room_session_pane_mount\"" src/ui/shell/RelayRoomSessionPane.tsx` returns >= 1 (mount log).
    - `grep -c "JSON.stringify(" src/ui/shell/RelayRoomSessionPane.tsx` returns 0.
    - `grep -c "Terminal\\|MessageQueueDrawer\\|IdentityModal" src/ui/shell/RelayRoomSessionPane.tsx` returns 0 (per PATTERNS.md 'What NOT to include').
    - `grep -c "viewingUserMxid" src/ui/shell/RelayRoomSessionPane.tsx` returns 0 (Plan 05 Task 3 sources it internally in RelayRoomPane; wrapper is agnostic).
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run src/ui/shell/RelayRoomSessionPane.test.tsx` — all 5 tests pass.
    - No file under `src/ui/features/pretty-view/` is modified.
    - No modification to `src/ui/shell/IdentitySessionPane.tsx` (D-03 spirit — leave the existing wrapper untouched).
  </acceptance_criteria>
  <done>
    RelayRoomSessionPane shell wrapper lands, mirrors IdentitySessionPane's contract, mounts RelayRoomPane inside the standard pane wrapper. Ready for Task 2 to reference from tabUtils.tsx.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen tabUtils.tsx TerminalOrIdentitySessionPane dispatcher with the relay-room branch (FIRST, most-specific-wins)</name>
  <files>
    src/ui/shell/tabUtils.tsx,
    src/ui/shell/tabUtils.test.tsx
  </files>
  <read_first>
    src/ui/shell/tabUtils.tsx (existing TerminalOrIdentitySessionPane dispatcher at L153-219 — the two-branch code that grows a third branch),
    src/ui/shell/RelayRoomSessionPane.tsx (Task 1 output),
    src/types/ui-types.ts (Tab type with sessionKind + relayRoomId + relayRoomTitle from Plan 01),
    .planning/phases/90-.../90-PATTERNS.md § tabUtils.tsx section (widened-shape example — new branch placed FIRST because it's the most-specific discriminator; defensive fall-through if sessionKind is 'relay-room' but relayRoomId is missing)
  </read_first>
  <behavior>
    - Test 1 (regression on existing paths): `tab.sessionKind === 'harness'` (or undefined — backward-compat) AND `identityKey` present -> mounts `IdentitySessionPane`. NO regression on the existing branch.
    - Test 2 (regression): `tab.sessionKind === 'harness'` (or undefined) AND no `identityKey` -> mounts `TerminalTabContent`. NO regression.
    - Test 3 (NEW): `tab.sessionKind === 'relay-room'` AND `tab.relayRoomId` set -> mounts `<RelayRoomSessionPane tab={tab} roomId={tab.relayRoomId} roomTitle={tab.relayRoomTitle ?? null} isVisible={isVisible} onCloseTab={onCloseTab} />`.
    - Test 4 (defensive): `tab.sessionKind === 'relay-room'` AND `tab.relayRoomId` MISSING -> logs a warning `console.warn('relay-room tab missing relayRoomId', {tabId: tab.id})` and falls through to the existing dispatcher (NOT a crash; the existing branch renders the tab as best-effort, likely as a terminal — which shows nothing useful but does not white-screen the app).
    - Test 5: no regression on any existing tabUtils.tsx test.
    - Test 6: the new branch is placed FIRST in the dispatch order (grep verifies the source order — RelayRoomSessionPane branch appears BEFORE the IdentitySessionPane branch in the source).
  </behavior>
  <action>
    Update `src/ui/shell/tabUtils.tsx` — widen the existing `TerminalOrIdentitySessionPane` dispatcher (at L153-219 per PATTERNS.md § tabUtils.tsx) with a third branch placed FIRST.

    Before existing branches:
    ```
    // NEW branch (Wave 5 — Plan 90-07) — placed FIRST because it's the most-specific discriminator.
    // Reads the sessionKind + relayRoomId fields Plan 90-01 added to Tab. Backward-compat:
    // any tab WITHOUT sessionKind (or with sessionKind === 'harness') falls through to the
    // existing identity-pane / terminal branches below, unchanged.
    if (tab.sessionKind === "relay-room") {
      if (!tab.relayRoomId) {
        console.warn("relay-room tab missing relayRoomId", { tabId: tab.id });
        // Fall through to existing dispatcher — the tab renders as best-effort, no crash.
      } else {
        return (
          <RelayRoomSessionPane
            tab={tab}
            roomId={tab.relayRoomId}
            roomTitle={tab.relayRoomTitle ?? null}
            isVisible={isVisible}
            onCloseTab={onCloseTab}
          />
        );
      }
    }
    // Existing identity-pane path unchanged
    const isIdentityPane = /* ... existing logic ... */;
    if (isIdentityPane) { return <IdentitySessionPane {...props} />; }
    return <TerminalTabContent {...props} />;
    ```

    Import `RelayRoomSessionPane` from `./RelayRoomSessionPane` (or the correct relative path — grep for the existing IdentitySessionPane import to match style).

    Add tests in `tabUtils.test.tsx` (create if missing — follow existing shell-test conventions; grep for a similar existing test file structure). Cover the 6 behaviors above. Use `vi.mock('./RelayRoomSessionPane')`, `vi.mock('./IdentitySessionPane')`, `vi.mock('./TerminalTabContent')` (or their correct paths) with double components that render distinct data-testids so branch selection is asserted via `screen.getByTestId`.

    NO modification to the existing identity-pane or terminal branches — they must render byte-for-byte identically for `sessionKind !== 'relay-room'` inputs (Test 1 + Test 2 regression gates).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run src/ui/shell/tabUtils.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sessionKind === \"relay-room\"" src/ui/shell/tabUtils.tsx` returns >= 1 (new branch present).
    - `grep -c "RelayRoomSessionPane" src/ui/shell/tabUtils.tsx` returns >= 2 (import + JSX use).
    - `grep -c "relay-room tab missing relayRoomId" src/ui/shell/tabUtils.tsx` returns >= 1 (defensive log).
    - The relay-room branch appears BEFORE the identity-pane branch in source (verify by line-number ordering: the line matching `sessionKind === "relay-room"` has a LOWER line number than the line matching `isIdentityPane`).
    - `grep -c "IdentitySessionPane" src/ui/shell/tabUtils.tsx` returns the same or higher count as pre-plan (existing branch untouched).
    - `grep -c "TerminalTabContent" src/ui/shell/tabUtils.tsx` returns the same or higher count as pre-plan (existing branch untouched).
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run src/ui/shell/tabUtils.test.tsx` — all 6 tests pass, especially Tests 1 + 2 (regression gates on existing branches).
    - No file under `src/ui/features/pretty-view/` is modified.
  </acceptance_criteria>
  <done>
    tabUtils.tsx dispatcher grows a third branch for kind === 'relay-room' placed FIRST (most-specific-wins); defensive fall-through on missing relayRoomId; existing identity-pane + terminal branches unchanged (regression gates). Zero pretty-view modification.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: BLOCKER #3 fix — Thread kind + roomId + roomTitle via the CORRECT dispatch chain (PrettyConversationsPanel handleRowSelect + AppShell callback wiring)</name>
  <files>
    src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx,
    src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx,
    src/ui/AppShell.tsx
  </files>
  <read_first>
    src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (L980-1003 handleRowSelect — the ACTUAL row-click handler; L971-975 comment documenting the branch priority (rdpHostRow → onRdpRowClick; fleetOnly → onDetachedRowClick; default → selectConversation); L274-312 for the existing callback prop shapes onConversationSelected / onDetachedRowClick / onRdpRowClick; add a new callback prop for the relay-room branch alongside),
    src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx (existing test file — the primary; also `.chain.test.tsx`, `.new-role-button.test.tsx`, `.clone-dialog.test.tsx` — pick one to extend OR create a new co-located `.relay-room.test.tsx` file),
    src/ui/state/conversation-store.ts (L145-166 FleetSession type + L1225 selectConversation — Plan 01 widened FleetSession with kind + roomId + roomTitle; grep for the row-shape passed to handleRowSelect),
    src/types/ui-types.ts (Tab type — Plan 01 widened with sessionKind + relayRoomId + relayRoomTitle),
    src/ui/AppShell.tsx (L2036-2095 — the PrettyConversationsPanel invocation site with onConversationSelected / onDetachedRowClick / onRdpRowClick callback wiring; also L2059 `openTab(host, 'terminal', ..., {targetTmuxSession, label, allowCreateTmux})` — the openTab shape to mirror for the new relay-room branch; L2070 `selectConversationDeferred(newTabId)` — the store-side follow-up to fire after openTab),
    grep for the exact FleetSession -> ConversationRowShape mapping to confirm the row object passed to handleRowSelect carries `kind` + `roomId` + `roomTitle` (Plan 01 widened this; if not, add the passthrough),
    .planning/phases/90-.../90-PATTERNS.md § tabUtils.tsx section (kind marker origin — sidebar-click reads row.kind and forwards to the tab-open flow)
  </read_first>
  <behavior>
    - Test 1 (existing harness rows — regression): clicking a FleetSession row with `kind: 'harness'` (or undefined) fires `selectConversation(row.id)` + `onConversationSelected?.(row.id)` UNCHANGED — no new branch intercepts. Downstream AppShell wiring is byte-identical for these rows.
    - Test 2 (NEW — PrettyConversationsPanel row-click widen): clicking a FleetSession row with `kind: 'relay-room'` AND `roomId: '!abc:server'` AND `roomTitle: 'Design room'` fires a NEW callback (e.g., `onRelayRoomRowClick?.(row)`) — placed as a new priority branch in `handleRowSelect` BEFORE the default `selectConversation` path but AFTER the existing rdpHostRow + fleetOnly branches (to preserve their existing priority). The default path is NOT taken for relay-room rows. `onConversationSelected?.(row.id)` still fires (matches the discipline of the rdpHostRow + fleetOnly branches at L993/L998).
    - Test 3 (defensive): FleetSession row with `kind: 'relay-room'` but `roomId` MISSING -> handleRowSelect logs a warning `console.warn('relay-room row missing roomId', {rowId: row.id})` and falls through to the default selectConversation path (defensive; the backend shouldn't emit this state per Plan 04 wire discipline).
    - Test 4 (NEW — AppShell callback wiring): AppShell passes `onRelayRoomRowClick={(row) => { const newTabId = openTab(null, 'terminal', undefined, {sessionKind: 'relay-room', relayRoomId: row.roomId, relayRoomTitle: row.roomTitle ?? null, label: row.roomTitle ?? row.roomId}); selectConversationDeferred(newTabId); ...touchDevice/mobile follow-ups... }}` (mirroring the shape of the existing onDetachedRowClick at L2074-2086). The openTab signature MAY need widening — grep openTab in AppShell to confirm; add optional `sessionKind` / `relayRoomId` / `relayRoomTitle` to the openTab options bag if not already there.
    - Test 5 (end-to-end integration — PrettyConversationsPanel): mock the row list with one harness row + one relay-room row; render PrettyConversationsPanel with a spied `onRelayRoomRowClick`; click the relay-room row; assert `onRelayRoomRowClick` was called with the row (carrying `kind: 'relay-room'` + `roomId` + `roomTitle`) AND `onConversationSelected` was called with the row.id.
    - Test 6 (end-to-end integration — AppShell): mock the openTab store call; drive AppShell's onRelayRoomRowClick callback with a relay-room row; assert openTab was called with `sessionKind: 'relay-room'` + `relayRoomId` + `relayRoomTitle` in the options bag.
    - Test 7 (full-loop): tab created with `sessionKind: 'relay-room'` -> tabUtils dispatches to `RelayRoomSessionPane` (Task 2 verifies this branch; here we verify the tab shape lands correctly).
  </behavior>
  <action>
    **BLOCKER #3 fix — CORRECT FILE PATH + CORRECT DISPATCH CHAIN.**

    Update `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` (NOT `src/ui/sidebar/PrettyConversationsPanel.tsx` — that file does not exist):

    1. **Add a new callback prop** alongside the existing `onDetachedRowClick` / `onRdpRowClick` / `onConversationSelected` at L274-312. Suggested shape:
       ```typescript
       // NEW (Plan 90-07): fired when the user clicks a row whose FleetSession
       // carries kind === "relay-room" (Phase 89 sidebar merge). AppShell owns
       // the tab-spawn side effect; the panel only surfaces the callback here.
       onRelayRoomRowClick?: (row: ConversationRowShape) => void;
       ```

    2. **Widen `handleRowSelect` at L980-1003** with a new priority branch BEFORE the default `selectConversation` path but AFTER the existing rdpHostRow + fleetOnly branches. Follow the exact discipline the existing branches use (early return, fire `onConversationSelected` alongside):
       ```typescript
       const handleRowSelect = (row: ConversationRowShape) => {
         addToActiveSet(row.id);
         if (row.rdpHostRow && onRdpRowClick) {
           onRdpRowClick(row);
           onConversationSelected?.(row.id);
           return;
         }
         if (row.fleetOnly && onDetachedRowClick) {
           onDetachedRowClick(row);
           onConversationSelected?.(row.id);
           return;
         }
         // NEW (Plan 90-07) — relay-room branch. Placed AFTER rdp + fleetOnly to
         // preserve their existing priority (a row is at most one of these types
         // — a relay-room row is neither rdpHostRow nor fleetOnly).
         if (row.kind === "relay-room" && onRelayRoomRowClick) {
           if (!row.roomId) {
             console.warn("relay-room row missing roomId", { rowId: row.id });
             // Fall through to default path — best-effort, no crash.
           } else {
             onRelayRoomRowClick(row);
             onConversationSelected?.(row.id);
             return;
           }
         }
         selectConversation(row.id);
         onConversationSelected?.(row.id);
       };
       ```

    3. **Verify FleetSession -> ConversationRowShape passthrough.** Grep `ConversationRowShape` in PrettyConversationsPanel.tsx and the row-mapping site (likely upstream in `conversation-store.ts` or a shape-mapping helper). The row shape passed to `handleRowSelect` MUST carry `kind` + `roomId` + `roomTitle`. Plan 01 widened the underlying `FleetSession` type; if the row-mapping helper strips these fields, ADD the passthrough (a 3-line change: destructure + include in the returned shape). If the shape mapping is trivial (`{...fleetSession, ...uiFields}`), the fields ride through automatically.

    Update `src/ui/AppShell.tsx` at the PrettyConversationsPanel invocation site (L2036-2095):

    1. **Wire the new `onRelayRoomRowClick` callback prop** alongside the existing onDetachedRowClick + onRdpRowClick at L2074-2095. Mirror the shape of onDetachedRowClick (which is the closest analog — both open a session pane on click):
       ```typescript
       onRelayRoomRowClick={(row) => {
         if (!row.roomId) {
           console.warn("relay-room row missing roomId at AppShell", { rowId: row.id });
           return;
         }
         const newTabId = openTab(null, "terminal", undefined, {
           sessionKind: "relay-room",
           relayRoomId: row.roomId,
           relayRoomTitle: row.roomTitle ?? null,
           label: row.roomTitle ?? row.roomId,
           allowCreateTmux: false, // relay-room tabs have no tmux to create
         });
         selectConversationDeferred(newTabId);
         if (isTouchDevice) navigateToView();
         if (isMobile) setSidebarOpen(false);
       }}
       ```

    2. **Widen the `openTab` signature (or options bag)** to accept the new fields. Grep the `openTab` definition (likely in `conversation-store.ts` or a helper) — the existing signature is `openTab(host, kind, ..., opts)` per L2059. Add optional `sessionKind?: 'harness' | 'relay-room'` + `relayRoomId?: string` + `relayRoomTitle?: string | null` to the options bag. Inside openTab, when constructing the Tab object, thread these fields into `tab.sessionKind` + `tab.relayRoomId` + `tab.relayRoomTitle` (Plan 01 widened `Tab` with these fields).

    3. **First arg is null** for the relay-room branch — relay-room tabs have NO Host (the room is on the relay, not on any host). Verify the openTab signature accepts null host OR add a defensive branch inside openTab that treats null host + `sessionKind: 'relay-room'` as a valid combination. Grep openTab for existing null-host handling; if none, add it.

    Add tests in `PrettyConversationsPanel.test.tsx` (or a new co-located `.relay-room.test.tsx` — planner picks based on file-size + convention). Cover behaviors 1-3, 5. Mock `onRelayRoomRowClick` via `vi.fn()` and assert on the arguments passed. Test 3 (defensive) uses a row with `kind: 'relay-room'` + `roomId: undefined` and asserts the console.warn fires + default selectConversation runs.

    For behaviors 4, 6, 7 (AppShell + tabUtils integration): extend AppShell-side tests if any exist (grep for AppShell.test.tsx or shell-integration tests). If AppShell has no test file, the integration is covered by Task 2's tabUtils tests + the PrettyConversationsPanel behavior tests + a small documented manual verification (planner picks — tests for AppShell.tsx are commonly deferred if the file is orchestration-heavy).

    Do NOT rewrite the entire click handler; add a minimal branch for the relay-room case above the default logic. Do NOT touch the rdpHostRow or fleetOnly branches (Test 1 regression gate).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx src/ui/features/pretty-conversations/PrettyConversationsPanel.relay-room.test.tsx 2&gt;/dev/null || npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` exists and is modified (the CORRECT file path per BLOCKER #3 — NOT `src/ui/sidebar/PrettyConversationsPanel.tsx` which does not exist).
    - `grep -c "row.kind === \"relay-room\"" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx | head -1` returns non-zero count (new branch present).
    - `grep -c "onRelayRoomRowClick" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns >= 2 (prop declared + called in handleRowSelect).
    - `grep -c "relay-room row missing roomId" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns >= 1 (defensive log).
    - `grep -c "openSessionInTree" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` returns 0 in the row-click branch (BLOCKER #3 fix — the row-click chain does NOT go through openSessionInTree, which is a split-tree DROP handler at AppShell.tsx L1663).
    - `grep -c "sessionKind: \"relay-room\"" src/ui/AppShell.tsx` returns >= 1 (new tab-construction site at the callback wiring).
    - `grep -c "onRelayRoomRowClick=" src/ui/AppShell.tsx` returns >= 1 (callback prop wired at the PrettyConversationsPanel invocation site).
    - `grep -c "relayRoomId:" src/ui/AppShell.tsx` returns >= 1 (kind + roomId + roomTitle threading in openTab options).
    - Existing harness-row-click behavior byte-for-byte preserved (Test 1 regression gate — no new intercept for `kind` undefined or `kind === 'harness'` rows).
    - Existing rdpHostRow + fleetOnly branches at L991-1000 are UNCHANGED (grep-verify — same L-numbers, same body).
    - `npx tsc --noEmit` exits 0.
    - Row-click tests pass (all pre-existing + the new relay-room branch tests).
    - No file under `src/ui/features/pretty-view/` is modified.
    - Full-loop integration: `npx vitest run --related src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx src/ui/shell/tabUtils.tsx src/ui/shell/RelayRoomSessionPane.tsx src/ui/features/relay-room-pane/` reports all tests passing across the wired loop.
  </acceptance_criteria>
  <done>
    Sidebar row-click threads kind + roomId + roomTitle from FleetSession → `onRelayRoomRowClick` callback → AppShell `openTab(...)` with the widened options bag → Tab with sessionKind + relayRoomId + relayRoomTitle → tabUtils.tsx dispatcher (Task 2) picks RelayRoomSessionPane for kind='relay-room' tabs → RelayRoomSessionPane wrapper (Task 1) mounts RelayRoomPane (Plan 06). The end-to-end user flow works. Phase 90 slice D is deliverable. BLOCKER #3 fixed: correct file path, correct dispatch chain.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FleetSession row -> Tab construction | Backend-supplied fields (Phase 89 /sessions/list); already access-gated per user; new fields are additive. |
| Tab.sessionKind + relayRoomId -> tabUtils dispatcher | Frontend-internal; already-validated Tab shape. No untrusted input introduced. |
| RelayRoomSessionPane -> RelayRoomPane -> Plan 04 backend | Established trust chain via Plan 04 auth gates + Plan 06 hook + participants fetch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-07-T1 | Tampering | tabUtils dispatcher branch order | mitigate | Relay-room branch placed FIRST (most-specific discriminator). Grep gate on line-number ordering ensures a future refactor doesn't accidentally reorder branches. Backward-compat: sessionKind undefined = harness, so old cached tabs still route to existing branches. |
| T-90-07-R1 | Repudiation | Defensive fall-through on missing relayRoomId / missing roomId | accept | Both tabUtils (missing relayRoomId) and PrettyConversationsPanel (missing roomId) log-and-fall-through / log-and-skip when the discriminator is inconsistent. Never crash. The backend should never emit this state; the defense is belt-and-suspenders. |
| T-90-07-I1 | Information Disclosure | Sidebar-row-click threading | mitigate | Only threads fields already visible on FleetSession (backend already gates per user). No new data exposure. |
| T-90-07-D1 | Denial of Service | Regression on existing harness rows | mitigate | Task 2 Tests 1 + 2 AND Task 3 Test 1 are dedicated regression gates. If any of them fails, the existing pretty-view + terminal flows are broken and the plan does not merge. |
| T-90-07-P1 | Wrong-file-path / wrong-dispatch-chain (BLOCKER #3 mitigation) | Plan artifact correctness | mitigate | Corrected file path (`src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`) and dispatch chain (`handleRowSelect` L980-1003, calls `selectConversation` + `onConversationSelected`; NOT `openSessionInTree` which is a split-tree DROP handler). Grep gate `openSessionInTree = 0 in the row-click branch` enforces the discipline. Read_first section calls out the exact L-numbers to prevent scavenger-hunt drift. |

`security_enforcement=true`. No new backend touches; all threats are frontend-scoped and regression-gated. BLOCKER #3 mitigation is a plan-artifact correctness gate (the file path + dispatch chain in the plan match the actual codebase).
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npx vitest run` scoped to the touched test files reports all tests passing.
- No modification to `src/ui/features/pretty-view/` (D-01/D-03 upheld — the Wave 0 D-03 waiver in Plan 00 does not apply here).
- No modification to `src/backend/*` (Plans 00 + 03 + 04 own backend).
- Grep gates in acceptance criteria satisfied — most importantly:
  - CORRECT file path: `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` (BLOCKER #3 fix).
  - CORRECT dispatch chain: NO openSessionInTree in the row-click branch (BLOCKER #3 fix).
  - branch-ordering + backward-compat + regression tests.
- Full-loop scoped test `npx vitest run --related src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx src/ui/shell/tabUtils.tsx src/ui/shell/RelayRoomSessionPane.tsx src/ui/features/relay-room-pane/` reports all tests passing.
</verification>

<success_criteria>
Phase 90 slice D is deliverable: a Skynet user with a running relay-room session on the fleet clicks the sidebar row (dispatched via the CORRECT chain: PrettyConversationsPanel `handleRowSelect` → new `onRelayRoomRowClick` callback → AppShell openTab → Tab with sessionKind + relayRoomId + relayRoomTitle → tabUtils dispatcher → RelayRoomSessionPane wrapper → RelayRoomPane) and lands on the RelayRoomPane end-to-end — presence row at top, message history in the middle, compose box at the bottom with textarea + send only (D-04 upper gone, D-05 attach hidden). Sending a message goes out through their own relay identity (D-15). Pending bubbles replace with real bubbles on echo-back (D-16 / T-90-FE-01 / Pitfall 4). Per-agent context meters read fleet-status (D-10 / Pitfall 2, Wave 0 hook — SAME source PrettyView reads). Per-agent reset dispatches to Wave 0 endpoint (D-10 / SAME seam PrettyView uses). Room-not-found renders the D-18 friendly error state, not a crash. Every D-decision (D-01 through D-20) landed across Plans 00-07. Zero pretty-view modification (D-01 + D-03 — except the Wave 0 mechanical waivers Ashley greenlit 2026-09-08).

Executor's remit stops here — code + commit + scoped tests green. Deploy is orchestrator-owned per fleet rule, deferred to arc-close after slices C + E.
</success_criteria>

<output>
Create `.planning/phases/90-relay-mediated-group-conversations-sub-slice-d-relay-session/90-07-SUMMARY.md` when done.
</output>
