---
phase: 28-prettyview-virtualization-correctness-cluster-post-phase-27
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/PrettyView.tsx
  - src/ui/features/pretty-view/PrettyView.virtualization.test.tsx
autonomous: true
requirements:
  - "correctness follow-up to Phase 27 requirements (no new REQ-IDs; addresses H3, H4, M1, M2, M4 from /tmp/pv-virtualization-review.md)"
must_haves:
  truths:
    - "H3: PrettyView's custom observeElementRect returns a callable () => void cleanup on every branch — no branch returns bare undefined — so TanStack Virtual can safely call cleanup on rebind without TypeError"
    - "H4: PrettyView's custom observeElementRect re-derives instance.scrollElement inside its read() closure on every invocation, so a stale RO firing against an old DOM node after scroll-container remount reports the CURRENT element's dimensions (not the stale captured one) — or bails cleanly with cb-not-called when transient null"
    - "M1: useVirtualizer options include scrollMargin: 12 (matching the outer scroll container's py-3 = 12px top padding) so scrollTop math aligns with the sized-container start; a comment names py-3 (PrettyView.tsx :1816) as the source-of-truth to update if the class changes"
    - "M2: useVirtualizer initialRect is { width: 1024, height: 600 } (down from 4096) so the first paint before ResizeObserver fires mounts ~5-10 bubbles instead of ~60, preserving the phase's bounded-DOM goal through the transient pre-RO window"
    - "M4: getItemKey uses a non-colliding string fallback prefix (__oob_${i}) instead of `?? i` so a real integer eventId '5' can never collide with a fallback key 5; if the fallback branch is ever hit, a console.warn fires with i and messages.length for empirical diagnosis"
    - "src/ui/features/pretty-view/use-auto-scroll.ts remains byte-identical to HEAD~1 (byte-preserve invariant — hook is kept as reference for the eventual pv-auto-scroll-redesign bounty)"
    - "npx vitest run exits 0 with zero test failures across the full frontend suite"
    - "npx tsc --noEmit exits 0"
    - "Test 2 (auto-scroll rAF-chain, currently it.skip) remains it.skip — NOT re-enabled — because auto-scroll is TEMP-disabled per patch #373"
  artifacts:
    - path: "src/ui/features/pretty-view/PrettyView.tsx"
      provides: "Corrected observeElementRect + useVirtualizer options (scrollMargin, initialRect, getItemKey)"
      contains: "scrollMargin"
    - path: "src/ui/features/pretty-view/PrettyView.virtualization.test.tsx"
      provides: "Extended test coverage for H3 no-throw-on-rebind, H4 stale-element defensive behavior, M2 initialRect assertion, M4 non-colliding fallback"
      contains: "__oob_"
  key_links:
    - from: "src/ui/features/pretty-view/PrettyView.tsx observeElementRect"
      to: "TanStack Virtual cleanup contract"
      via: "return () => {} on every early-return branch (not bare undefined)"
      pattern: "return \\(\\) => \\{\\}"
    - from: "src/ui/features/pretty-view/PrettyView.tsx observeElementRect read() closure"
      to: "instance.scrollElement (dynamic, may change identity across remounts)"
      via: "re-read instance.scrollElement at read()-time instead of relying on the captured const"
      pattern: "instance\\.scrollElement"
    - from: "src/ui/features/pretty-view/PrettyView.tsx useVirtualizer options"
      to: "outer scroll container's py-3 padding at PrettyView.tsx :1816"
      via: "scrollMargin: 12 with a source-of-truth comment"
      pattern: "scrollMargin"
    - from: "src/ui/features/pretty-view/PrettyView.tsx getItemKey"
      to: "TanStack Virtual measurement cache stability"
      via: "string prefix __oob_${i} guarantees no collision with real integer eventIds"
      pattern: "__oob_"
---

<objective>
Fix the five correctness issues (H3, H4, M1, M2, M4) surfaced by the independent code review of Phase 27's PrettyView virtualization integration (see `/tmp/pv-virtualization-review.md`). All five fixes live in the same ~50-line region of `PrettyView.tsx` (useVirtualizer options + custom observeElementRect at lines 634-680), so they cluster into a single plan with three tasks:

  1. **Task 1** — H3 + H4: correctness of the custom `observeElementRect` (cleanup contract compliance + stale-element defensive re-derivation), plus test coverage for both.
  2. **Task 2** — M1 + M2 + M4: correctness of `useVirtualizer` options (scrollMargin for py-3 alignment; smaller initialRect to preserve bounded-DOM through the pre-RO window; non-colliding getItemKey fallback with diagnostic warn), plus test coverage.
  3. **Task 3** — Full-suite verification + byte-preserve invariant check on `use-auto-scroll.ts` + strict tsc pass. Guarantees the fixes ship green.

Purpose: Phase 27 shipped virtualization successfully but the review flagged five real correctness bugs (two HIGH, three MEDIUM). H3 is a TanStack contract violation that has not thrown yet only because the library defensively guards against it; H4 is a stale-closure bug that manifests on scroll-container remount cycles; M1 is off-by-12 today and a real bug the day `scrollToIndex` ships; M2 defeats the bounded-DOM goal for the transient pre-RO window on every mount / paneKey change / reconnect; M4 silently masks what would otherwise be a diagnosable state-shape bug AND can collide with real integer eventIds. All five are small, atomic, single-file — the cost of NOT fixing them accumulates every deploy.

Output: 2 commits (Task 1 + Task 2), each atomic and bisectable; full frontend suite green; use-auto-scroll.ts byte-identical to HEAD~1; deploy motion is orchestrator-only (tina) per fleet rule, NOT part of this plan.

**Explicitly out of scope** (moot because auto-scroll is TEMP-disabled per patch #373):
  - H1 (accessory-mount pin blind spot)
  - H2 (image-bubble decode re-pin race)
  - M5 (forceStickAndJumpRef assignment during render)
  - M7 (forceStickAndJump on failed sends)

**Also out of scope:**
  - L1, L2 (stale/factually-wrong auto-scroll comments at PrettyView.tsx :1801 and :1888-1891 — captured in bounty pv-auto-scroll-redesign)
  - L3-L10 (follow-up nits)
  - M3 (effect-ordering comment cleanup) — not correctness, style-only
  - M6 (handleComposeSend maintainability trap) — style-only
  - Auto-scroll redesign (separate bounty pv-auto-scroll-redesign)
  - use-auto-scroll.ts modifications (byte-preserve invariant)
  - Any file outside `src/ui/features/pretty-view/`
  - Deploy motion (docker build, recreate, push, patches.md entry, coord-room announces) — orchestrator-only per fleet rule
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Full review source-of-truth for all five in-scope findings and the four out-of-scope findings the executor should NOT touch.
@/tmp/pv-virtualization-review.md

# Primary target file — all five fixes are in this region.
@src/ui/features/pretty-view/PrettyView.tsx

# Existing test file — extend this (or add sibling) with H3/H4/M2/M4 assertions.
# Note the widened ResizeObserver polyfill pattern (capturedROCallbacks) at :246-306
# and the HTMLElement.prototype.offsetHeight override on [data-pv-bubble] at :291-305 —
# both patterns are reusable for the new tests. Test 2 (auto-scroll rAF-chain) is
# currently it.skip per patch #373; DO NOT re-enable.
@src/ui/features/pretty-view/PrettyView.virtualization.test.tsx

# DO NOT MODIFY — byte-preserve invariant. Referenced here for the executor to keep
# in mind when reading the observeElementRect region (composeScrollRefs at :684-689
# threads useAutoScroll's scrollRef alongside scrollElRef.current to the same DOM node).
@src/ui/features/pretty-view/use-auto-scroll.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix H3 (observeElementRect cleanup contract) + H4 (stale scrollElement closure) with test coverage</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx, src/ui/features/pretty-view/PrettyView.virtualization.test.tsx</files>
  <action>
    Edit the custom `observeElementRect` implementation on `useVirtualizer` in `src/ui/features/pretty-view/PrettyView.tsx` (currently at approximately lines 664-679 — locate via `grep -n "observeElementRect" PrettyView.tsx`).

    **H3 fix — cleanup contract:** Change the two bare `return;` early-exit statements in the `observeElementRect` callback so each returns a no-op cleanup function `() => {}` instead of bare `undefined`. Specifically:
      - The `if (!el) return;` branch (currently returns undefined when `instance.scrollElement` is null) becomes `if (!el) return () => {};`
      - The `if (!win) return;` branch (currently returns undefined when `instance.targetWindow` is null) becomes `if (!win) return () => {};`
    Rationale (per review H3): TanStack Virtual expects `observeElementRect` to always return a `() => void` cleanup and calls it on later rebind. Returning bare `undefined` risks `TypeError: undefined is not a function` on the rebind — currently only masked by TanStack's defensive `typeof cleanup === 'function'` guard, which we should not rely on. The existing `if (!win.ResizeObserver) return () => {};` line already follows this convention — the two earlier branches must match.

    **H4 fix — stale scrollElement closure:** Inside the `read()` inner closure (currently reads `el.offsetWidth` / `el.offsetHeight` from the outer `const el = instance.scrollElement`), re-derive the current scroll element from `instance.scrollElement` on every invocation so a stale RO firing after a container remount reports dimensions for the CURRENT element (not the captured-at-bind stale one). Structure:
      1. Rename the outer `const el` to something like `const bindEl` (used only to install the RO — the observed target at bind time).
      2. Inside `read()`, declare a fresh `const cur = instance.scrollElement as HTMLElement | null;` — if null, return early WITHOUT calling `cb(...)` so a transient null does not propagate a zero rect through to the virtualizer.
      3. Use `cur.offsetWidth || 1024` and `cur.offsetHeight || 4096` to compute w/h (preserve the existing 1024/4096 offset-zero fallbacks — those still exist for the JSDOM/first-paint reason described in the pre-existing comment at :653-663).
      4. Continue to install the RO on `bindEl` via `ro.observe(bindEl)` and disconnect it via the returned cleanup. This preserves the OLD observation site (so the OLD element continues to be watched for its own resizes) but the CALLBACK reports for the CURRENT element — the intended behavior per review H4 suggested direction at /tmp/pv-virtualization-review.md :103-110.
      5. Preserve the existing comment block (lines 653-663) explaining the JSDOM / first-paint rationale. Add a new one-line comment above the `read()` closure noting "re-derive from instance.scrollElement on every fire so a stale RO on the old scroll container reports current dimensions (H4)".

    **DO NOT** modify any other useVirtualizer option in this task — M1/M2/M4 are Task 2. **DO NOT** touch `composeScrollRefs`, `scrollElRef`, or any code outside the `observeElementRect` region.

    **Test coverage additions** to `src/ui/features/pretty-view/PrettyView.virtualization.test.tsx` (extend the existing describe block; add two new `it(...)` cases at the end of the describe body, after Test 5b at ~line 686):

      - **Test 6 — H3 no-throw on cleanup with null scrollElement:** Render PrettyView with a WS stub that stays in `connecting` status (no flipToStreaming call), so the scroll container is not mounted and `scrollElRef.current` remains null through initial mount. Then transition to streaming to force TanStack Virtual to rebind observeElementRect against a now-non-null scrollElement. Assert that no exception is thrown across mount → status flip → unmount. The mechanism: TanStack calls the previous null-branch cleanup on rebind; if that cleanup is bare undefined, calling it throws TypeError. Wrap the render + flipToStreaming + unmount sequence in a try/catch and assert catch was not entered. Additionally, use `vi.spyOn(console, 'error')` to catch React's error-boundary chatter — assert `console.error` was not called with any TypeError.

      - **Test 7 — H4 defensive read against stale scrollElement:** Render PrettyView, flipToStreaming, fire a small batch (5 messages), locate the outer scroll container via `getOuterScrollContainer(container)`. Manually override `instance.scrollElement` indirectly by mutating the outer scroll container's `offsetHeight` via `Object.defineProperty`, then invoke every captured RO callback in `capturedROCallbacks` via `act(() => { for (const cb of capturedROCallbacks) cb([], {} as ResizeObserver); })`. The virtualizer should re-read dimensions and NOT crash even if the callback references an element whose offsetHeight has changed. Since JSDOM cannot easily simulate a full scroll-container identity swap (that would require unmount/remount of PrettyView with the same session), the practical assertion is: after the callback fires, the DOM still shows a valid sized virtualizer container (getSizedVirtualizerContainer does not throw) AND `container.querySelectorAll('[data-pv-bubble]').length` is > 0. This proves the read() closure did not crash on the (potentially stale-looking) offsetHeight re-read. Comment in the test body explaining this is a JSDOM-appropriate proxy for the true H4 concern (which requires a real browser env to fully witness).

    Both new tests reuse the existing `beforeEach` / `afterEach` scaffolding, `wsStubs`, `flipToStreaming`, `fireMessageBatch`, `getOuterScrollContainer`, `getSizedVirtualizerContainer`, `shrinkScrollContainer`, and `capturedROCallbacks` — no new infrastructure required.

    Commit message (one atomic commit): `fix(pv-virt): observeElementRect returns () => {} on null branches + re-reads scrollElement in read closure (H3, H4)`
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx vitest run src/ui/features/pretty-view/PrettyView.virtualization.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `observeElementRect` in PrettyView.tsx returns `() => {}` (not bare undefined) on every early-return branch — verified via `grep -c "return;" src/ui/features/pretty-view/PrettyView.tsx` on the observeElementRect region (filter to just the callback body) showing 0 bare `return;` lines inside `observeElementRect`.
    - The `read()` closure inside `observeElementRect` re-derives `instance.scrollElement` on every call — verified by `grep -A 10 "const read = " src/ui/features/pretty-view/PrettyView.tsx | grep -v '^#'` showing `instance.scrollElement` inside the read body (not just at the outer const).
    - PrettyView.virtualization.test.tsx has two new `it(...)` cases named "Test 6" (H3) and "Test 7" (H4) — verified via `grep -c "Test 6\|Test 7" src/ui/features/pretty-view/PrettyView.virtualization.test.tsx` returning 2.
    - `npx vitest run src/ui/features/pretty-view/PrettyView.virtualization.test.tsx` exits 0 with all pre-existing tests still passing (Test 1, 3, 4, 5a, 5b) PLUS Test 6 and Test 7 both passing. Test 2 remains `it.skip`.
    - One atomic commit created; git message starts with `fix(pv-virt): observeElementRect returns () => {}`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix M1 (scrollMargin) + M2 (initialRect) + M4 (getItemKey fallback) with test coverage</name>
  <files>src/ui/features/pretty-view/PrettyView.tsx, src/ui/features/pretty-view/PrettyView.virtualization.test.tsx</files>
  <action>
    Edit the `useVirtualizer` options object in `src/ui/features/pretty-view/PrettyView.tsx` (currently starting at approximately line 636 — locate via `grep -n "useVirtualizer" PrettyView.tsx`). Apply THREE distinct edits to the options object, each with an accompanying comment explaining the rationale:

    **M1 fix — add scrollMargin: 12:** Insert `scrollMargin: 12,` into the useVirtualizer options object (place it near the other integer options like `overscan: 5`). Add an inline comment: `// Matches the outer scroll container's py-3 (= 12px) top padding — the sized virtualizer container starts at scrollTop offset 12, not 0. Source-of-truth for the 12: the "px-4 py-3" className on the composeScrollRefs div at PrettyView.tsx :1816 — UPDATE both if the padding class changes.`
    Rationale (per review M1): TanStack Virtual computes visible items via `scrollTop - scrollMargin` (default 0). Since the sized inner container starts at `offsetTop = 12px` inside the outer scroll container's `py-3` padding, without `scrollMargin: 12` the "visible slice" is off-by-12 today (absorbed by overscan for the top edge) AND the day `scrollToIndex` ships it would land 12px too high. Static value chosen (vs dynamic computed-from-getComputedStyle in a useEffect) for simplicity and speed — the comment names the source-of-truth to update if class drifts.

    **M2 fix — reduce initialRect.height:** Change the existing `initialRect: { width: 1024, height: 4096 }` to `initialRect: { width: 1024, height: 600 }`. Update the surrounding comment (currently at :648-663) to acknowledge the new value: keep the JSDOM/first-paint rationale (still applies — RO stub never fires in JSDOM), but revise the "In real browsers this is transient (RO fires within a frame)" line to note that 600 was chosen so the first paint mounts ~5-10 items (600/80 + 10 overscan ≈ 17) instead of ~60 (4096/80 + 10 ≈ 61), preserving the phase's bounded-DOM goal through the transient pre-RO window on every mount / paneKey change / reconnect. Width stays 1024 (only height affects virtualization slicing per review L5).
    Rationale (per review M2): With `estimateSize=80` + `overscan=5`, initialRect.height=4096 yields ~60 real bubble mounts on the first paint before the ResizeObserver fires and contracts — defeats the phase's bounded-DOM goal in the transient window (worse on mobile). Reducing to 600 caps the transient at ~17 items; the RO corrects on the next frame.

    **M4 fix — non-colliding getItemKey fallback + diagnostic warn:** Change `getItemKey: (i) => messages[i]?.eventId ?? i,` to a small function body that:
      1. Reads `const evt = messages[i]?.eventId;`
      2. If `evt !== undefined` (truthiness check on undefined specifically — a real eventId of empty string "" is degenerate but should not trip the fallback path), return `evt`.
      3. Else, `console.warn(\`[pv-virtual] getItemKey out-of-range i=${i} messages.length=${messages.length}\`);` and return `` `__oob_${i}` `` (a string with underscore prefix guaranteed not to collide with any real eventId string).
    Add a new comment block above the getItemKey line: `// Diagnostic fallback: the review's M4 finding argued the "race" the previous fallback protected against is not real (count and messages come from the same render). If TanStack Virtual ever calls getItemKey with i >= messages.length, that indicates a genuine bug — we surface it via console.warn AND avoid the eventId-collision hazard of returning i (a real integer eventId "5" would collide with fallback 5, invalidating the measurement cache). The __oob_${i} string prefix is loud enough to spot in DOM inspection AND safe from collision. If the warn never fires in ~1 week of production traffic, convert to a bare throw or drop the fallback per the review's Option (a).`

    **DO NOT** modify `observeElementRect`, `estimateSize`, `overscan`, `count`, or `getScrollElement` — those are outside this task's scope (H3/H4 are Task 1; L3 estimateSize is out of scope entirely per phase_scope).

    **Test coverage additions** to `src/ui/features/pretty-view/PrettyView.virtualization.test.tsx` (extend the existing describe block; add two new `it(...)` cases at the end after Test 7):

      - **Test 8 — M2 initial-paint bounded DOM without shrinkScrollContainer:** Render PrettyView, flipToStreaming, fire 120 messages via fireMessageBatch, then WITHOUT calling shrinkScrollContainer or manually firing RO callbacks, immediately query `container.querySelectorAll("[data-pv-bubble]")` and assert `.length <= 20`. This proves the reduced initialRect (600px) yields a bounded first paint even when the JSDOM RO stub never fires — the pre-M2 behavior would have rendered ~61 bubbles on the first paint from initialRect.height=4096. Cap of 20 chosen conservatively: 600/80 + 10 overscan = 17.5, rounded up + a couple margin. Note in comment: this is the JSDOM-observable proxy for the true M2 concern (real browsers benefit even more because the RO eventually fires and contracts further).

      - **Test 9 — M4 non-colliding fallback + warn on out-of-range i:** This test is harder to trigger directly (getItemKey is not called with i >= messages.length under normal virtualizer operation), so use a proxy assertion: fire a batch of messages with distinct real eventIds, then query `container.querySelectorAll("[data-pv-bubble]")` and assert every rendered bubble's `data-event-id` is a real message eventId (starts with `evt-` or the pattern used in the test — NOT `__oob_`). If ANY bubble has `data-event-id="__oob_..."` under normal operation, that's a real bug the fallback is now surfacing. Also add a `vi.spyOn(console, 'warn')` before the render and assert `console.warn` was NOT called with any `[pv-virtual]` prefixed message during normal fire-messages-and-render flow. This is the guardrail: normal operation must never trip the fallback; if it ever does, this test fails loudly.

    Both new tests reuse the existing beforeEach/afterEach scaffolding — no new infrastructure required.

    Commit message (one atomic commit): `fix(pv-virt): add scrollMargin=12 (M1) + shrink initialRect.height 4096→600 (M2) + non-colliding getItemKey fallback with diagnostic warn (M4)`
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx vitest run src/ui/features/pretty-view/PrettyView.virtualization.test.tsx 2>&1 | tail -40</automated>
  </verify>
  <done>
    - useVirtualizer options object in PrettyView.tsx contains `scrollMargin: 12` — verified via `grep -n "scrollMargin" src/ui/features/pretty-view/PrettyView.tsx` returning at least one line inside the useVirtualizer options.
    - useVirtualizer initialRect is `{ width: 1024, height: 600 }` — verified via `grep -A 1 "initialRect" src/ui/features/pretty-view/PrettyView.tsx | grep -v '^#' | grep -c "4096"` returning 0 (the 4096 literal is gone from the initialRect line; may still appear in the offsetHeight fallback `cur.offsetHeight || 4096` from Task 1, which is fine).
    - getItemKey uses `__oob_` string fallback — verified via `grep -c "__oob_" src/ui/features/pretty-view/PrettyView.tsx` returning >= 1.
    - getItemKey emits a console.warn on the fallback path — verified via `grep -B 1 -A 5 "__oob_" src/ui/features/pretty-view/PrettyView.tsx | grep -c "console.warn"` returning >= 1.
    - PrettyView.virtualization.test.tsx has two new `it(...)` cases named "Test 8" (M2) and "Test 9" (M4) — verified via `grep -c "Test 8\|Test 9" src/ui/features/pretty-view/PrettyView.virtualization.test.tsx` returning 2.
    - `npx vitest run src/ui/features/pretty-view/PrettyView.virtualization.test.tsx` exits 0 with all pre-existing tests still passing (Test 1, 3, 4, 5a, 5b, and Test 6/7 from Task 1) PLUS Test 8 and Test 9 both passing. Test 2 remains `it.skip`.
    - One atomic commit created; git message starts with `fix(pv-virt): add scrollMargin=12`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full-suite verification + tsc + byte-preserve invariant check on use-auto-scroll.ts</name>
  <files>(read-only — no source changes; may create at most a temporary shell log)</files>
  <action>
    Run three verification commands in order and abort with a loud failure if any of the three exits non-zero.

    **Step 1 — Full frontend vitest suite (0-failure requirement, fleet rule Ashley 2026-08-05):**
      ```
      cd /home/ubuntu/skynet && npx vitest run 2>&1 | tail -50
      ```
      Read the final "Test Files" and "Tests" summary lines. Assert:
        - Test Files: N passed with 0 failed.
        - Tests: N passed with 0 failed (Test 2 in PrettyView.virtualization.test.tsx should show as `skipped`, NOT `failed`, and skipped counts do not count against pass/fail).
      If ANY test fails — even a pre-existing flaky one unrelated to this phase — the fleet rule REQUIRES fixing it in the same session (Ashley 2026-08-05 "never leave tests failing"). Loudly surface the failing test name and file in the executor's final report so the orchestrator can decide whether to root-cause-fix or roll back Task 1/2.

    **Step 2 — TypeScript strict check (0-error requirement):**
      ```
      cd /home/ubuntu/skynet && npx tsc --noEmit 2>&1 | tail -20
      ```
      Assert exit code 0. Any TypeScript error introduced by Task 1 or Task 2 (e.g., a typo in the H4 re-derived-inside-read pattern, a missing `as HTMLElement | null` cast on the fresh `const cur = ...`, or a getItemKey return type mismatch) surfaces here.

    **Step 3 — Byte-preserve invariant on use-auto-scroll.ts:**
      ```
      cd /home/ubuntu/skynet && git diff HEAD~2 HEAD -- src/ui/features/pretty-view/use-auto-scroll.ts
      ```
      (HEAD~2 covers Task 1's + Task 2's commits — the hook must be untouched across both.)
      Assert this diff output is EMPTY. If it produces ANY output, Task 1 or Task 2 accidentally modified the hook — the byte-preserve invariant is violated (the hook is kept for reference for the eventual pv-auto-scroll-redesign bounty, per PrettyView.tsx :618-620 comment). Loudly surface the diff in the final report.

    NO source-code changes in this task — this is a pure verification-and-report task. If any of the three checks fails, report the failure to the orchestrator and STOP; do NOT attempt to patch Task 1/2 without a fresh planning cycle.

    NO commit for this task — verification only.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx vitest run 2>&1 | tail -20 && echo "---TSC---" && npx tsc --noEmit 2>&1 | tail -5 && echo "---USE-AUTO-SCROLL DIFF (must be empty)---" && git diff HEAD~2 HEAD -- src/ui/features/pretty-view/use-auto-scroll.ts | head -5</automated>
  </verify>
  <done>
    - `npx vitest run` exits 0 with 0 test failures across the full frontend suite (skipped tests do not count as failures).
    - `npx tsc --noEmit` exits 0.
    - `git diff HEAD~2 HEAD -- src/ui/features/pretty-view/use-auto-scroll.ts` produces zero lines of output (byte-identical to two commits ago — Task 1's commit + Task 2's commit did not touch it).
    - No new commit created by this task (verification-only).
    - Final report to orchestrator: "Phase 28 code + tests complete. All five review findings (H3, H4, M1, M2, M4) fixed; use-auto-scroll.ts untouched; full suite green; tsc clean. Ready for orchestrator deploy motion."
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TanStack Virtual ↔ PrettyView.tsx | PrettyView passes a custom `observeElementRect` implementation to the library; library expects a specific cleanup contract (H3) and re-invokes observeElementRect on scrollElement change (H4). Contract mismatch = library-side crash. |
| React render ↔ browser DOM | `getItemKey` runs synchronously during virtualizer render and produces React keys that TanStack Virtual uses to cache measurements. Key collisions (M4) silently invalidate the cache. |
| Test polyfill (`ResizeObserver` stub) ↔ TanStack Virtual per-item ROs | The widened capturing polyfill from `PrettyView.virtualization.test.tsx :246-306` captures ALL RO constructions in a shared array. The new H4 test invokes captured callbacks manually to simulate real-browser RO delivery — if the polyfill is later narrowed to a single-slot stub, tests silently break. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | TanStack Virtual observeElementRect cleanup contract (PrettyView.tsx :664-679) | mitigate | H3 fix returns `() => {}` on every branch; Test 6 asserts no-throw across mount→streaming→unmount. |
| T-28-02 | Tampering | observeElementRect closure captures stale scrollElement (PrettyView.tsx :665) | mitigate | H4 fix re-derives `instance.scrollElement` inside read(); Test 7 provides a JSDOM-appropriate proxy assertion (real-browser identity swap requires manual UAT). |
| T-28-03 | Repudiation | getItemKey silently falls back to index (PrettyView.tsx :647) masking a state-shape bug | mitigate | M4 fix (a) uses non-colliding `__oob_${i}` string prefix so no measurement-cache corruption via integer eventId collision; (b) emits console.warn on the fallback path so any occurrence is observable in production browser dev-tools; Test 9 asserts normal operation never trips the warn. |
| T-28-04 | Denial of Service | initialRect.height=4096 mounts ~60 real bubble subtrees on first paint (mobile ×60 render cost) | mitigate | M2 fix reduces to 600 (~17 bubbles on first paint); Test 8 asserts bounded DOM without RO firing. |
| T-28-05 | Denial of Service | scrollMargin=0 with py-3=12px padding — off-by-12 in scroll-to-index math (future feature) | mitigate | M1 fix adds `scrollMargin: 12` with source-of-truth comment naming `PrettyView.tsx :1816` py-3 class as the update-together target. |
| T-28-06 | Information Disclosure | console.warn from getItemKey fallback (M4) — could leak message internals if messages structure ever contains PII in eventIds | accept | eventIds are opaque session-local strings assigned by the JSONL tailer (uuid-shaped); no PII. Warn message includes only `i` and `messages.length` (integers), not message content. |
| T-28-SC | Tampering | npm installs (supply chain) | accept | No new dependencies added — this phase only edits existing PrettyView.tsx + tests. Package Legitimacy Gate not triggered (no `npm install` in tasks). |
</threat_model>

<verification>
- Automated: `npx vitest run` full frontend suite green (0 fail, all skips explicit).
- Automated: `npx tsc --noEmit` exits 0.
- Automated: `git diff HEAD~2 HEAD -- src/ui/features/pretty-view/use-auto-scroll.ts` empty (byte-preserve invariant).
- Automated: grep-based done-criteria on each task (see individual `<done>` blocks).
- Manual: NONE — plan is executor-autonomous. Deploy motion (docker build, recreate, HTTPS 200 verify, push, patches.md entry, coord-room announces) is orchestrator-only per fleet rule.
</verification>

<success_criteria>
- All 5 in-scope review findings (H3, H4, M1, M2, M4) are fixed in `src/ui/features/pretty-view/PrettyView.tsx`.
- Test coverage exists for H3 (Test 6), H4 (Test 7 JSDOM proxy), M2 (Test 8 first-paint bounded DOM), and M4 (Test 9 no-fallback-under-normal-operation). M1's correctness is validated indirectly via the full-suite pass — no dedicated test because `scrollToIndex` doesn't ship yet (per review: "Non-blocking today because no scroll-to-index feature exists; would need adding at that time").
- 2 atomic commits — Task 1 (H3+H4), Task 2 (M1+M2+M4); Task 3 is verification only.
- `use-auto-scroll.ts` is byte-identical to HEAD~2 (byte-preserve invariant across both Task 1 and Task 2 commits).
- `npx vitest run` full frontend suite: 0 failures. Test 2 remains `it.skip` (auto-scroll disabled per patch #373).
- `npx tsc --noEmit` exits 0.
- 4 out-of-scope HIGH/MEDIUM findings (H1, H2, M5, M7) remain untouched — they are moot until auto-scroll is redesigned in a separate bounty (`pv-auto-scroll-redesign`).
</success_criteria>

<output>
Create `.planning/phases/28-prettyview-virtualization-correctness-cluster-post-phase-27-/28-01-SUMMARY.md` when Task 3 completes green. SUMMARY should include:
  - Before/after snippets of the observeElementRect region (H3, H4).
  - Before/after snippets of the useVirtualizer options object (M1, M2, M4).
  - Names + assertions of the 4 new tests (Test 6, 7, 8, 9).
  - Confirmation that `use-auto-scroll.ts` is byte-identical to HEAD~2.
  - Final `vitest run` and `tsc --noEmit` output tails.
  - Handoff note to orchestrator: "Ready for deploy motion. No source-of-truth changes to nginx, backend, or docker — pure frontend correctness fixes."
</output>
