---
phase: 109-stt-provider-swap-amazon-transcribe-to-amazon-nova-sonic-on
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/voice/nova-sonic-adapter.ts
  - src/backend/voice/nova-sonic-adapter.test.ts
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - D-PROV
  - D-SDK
  - D-TRANSPORT
  - D-CREDS
  - D-REGION
  - D-SINGLETON
  - D-EVENTS
  - D-AUDIOOUT
  - D-AUDIO
  - D-FILTER
  - D-TERM
  - D-PROMPT
  - D-TEARDOWN
  - D-ASYNCGEN
  - D-PKG

must_haves:
  truths:
    - "New file src/backend/voice/nova-sonic-adapter.ts exists and exports `transcribeNovaSonic(pcmBuffer: Buffer): Promise<string>`."
    - "Adapter constructs a module-level `BedrockRuntimeClient` exactly ONCE at import time (per D-SINGLETON)."
    - "Adapter uses NodeHttp2Handler with requestTimeout=300_000, sessionTimeout=300_000, maxConcurrentStreams=20 (per D-TRANSPORT)."
    - "Adapter passes NO `credentials` field to the BedrockRuntimeClient constructor — IMDS via SDK default chain (per D-CREDS)."
    - "Adapter uses `region: 'us-east-1'` and modelId `amazon.nova-2-sonic-v1:0` (per D-REGION + D-PROV). Fallback model `amazon.nova-sonic-v1:0` is named in the docstring only, NOT selectable at runtime."
    - "Adapter yields events via `async function*` generator wrapping each JSON blob as `{ chunk: { bytes: new TextEncoder().encode(JSON.stringify(event)) } }` (per D-ASYNCGEN — the landmine from aws-sdk-js-v3 issue #7125)."
    - "Send sequence emits exactly (in order): `sessionStart` → `promptStart` (containing both textOutputConfiguration AND audioOutputConfiguration) → SYSTEM contentStart → SYSTEM textInput → SYSTEM contentEnd → USER audio contentStart → N × audioInput (base64-encoded 60ms LPCM chunks paced at 5x real-time) → USER contentEnd → promptEnd → sessionEnd (per D-EVENTS + D-AUDIOOUT + D-AUDIO)."
    - "Audio input is chunked at 1920 bytes per chunk (960 samples × 2 bytes = 60 ms of 16 kHz s16le mono), base64-encoded per chunk (per D-AUDIO)."
    - "Response consumer filters textOutput events strictly to `role === 'USER'`; ASSISTANT textOutput events and ALL audioOutput events are dropped (per D-FILTER)."
    - "Response loop terminates on `completionEnd` event (per D-TERM)."
    - "System prompt is a transcription-only nudge (per D-PROMPT — Claude's discretion permits iteration on exact wording, but the intent must be transcription-only)."
    - "Teardown does NOT leak the post-stream ValidationException observed in the Python experiment (per D-TEARDOWN): the response stream is fully consumed BEFORE the sessionEnd is issued, OR a post-completionEnd ValidationException is caught and swallowed. Adapter tests verify the accumulated transcript is returned without an uncaught exception."
    - "New adapter test file src/backend/voice/nova-sonic-adapter.test.ts passes green in isolation."
    - "package.json REMOVES `@aws-sdk/client-transcribe-streaming` and ADDS `@aws-sdk/client-bedrock-runtime` (^3.784.0 or newer) + `@smithy/node-http-handler` (^4.0.4 or newer) (per D-PKG). package-lock.json is regenerated via `npm install`."
  artifacts:
    - path: "src/backend/voice/nova-sonic-adapter.ts"
      provides: "transcribeNovaSonic public export + module-level BedrockRuntimeClient singleton + async-generator event sender + role=USER filter + clean teardown"
      contains: "InvokeModelWithBidirectionalStreamCommand"
      min_lines: 180
    - path: "src/backend/voice/nova-sonic-adapter.test.ts"
      provides: "Mocked-SDK tests covering singleton ctor, event sequence, filter, teardown, error propagation"
      contains: "transcribeNovaSonic"
      min_lines: 200
    - path: "package.json"
      provides: "Dependency swap: -@aws-sdk/client-transcribe-streaming, +@aws-sdk/client-bedrock-runtime, +@smithy/node-http-handler"
      contains: "@aws-sdk/client-bedrock-runtime"
  key_links:
    - from: "src/backend/voice/nova-sonic-adapter.ts (module scope)"
      to: "@aws-sdk/client-bedrock-runtime BedrockRuntimeClient"
      via: "new BedrockRuntimeClient({ region: 'us-east-1', requestHandler: new NodeHttp2Handler({...}) })"
      pattern: "new BedrockRuntimeClient"
    - from: "src/backend/voice/nova-sonic-adapter.ts (transcribeNovaSonic)"
      to: "client.send(new InvokeModelWithBidirectionalStreamCommand({ modelId, body: <async iterable> }))"
      via: "SDK bidirectional-stream invocation"
      pattern: "InvokeModelWithBidirectionalStreamCommand"
    - from: "src/backend/voice/nova-sonic-adapter.ts (response consumer loop)"
      to: "textOutput events with role === 'USER'"
      via: "for-await over response.body, JSON.parse of event.chunk.bytes, dispatch on event.textOutput / event.completionEnd"
      pattern: "role.*=.*[\"']USER[\"']"
---

<objective>
Land the new Amazon Nova Sonic on Bedrock STT adapter as a fresh module (`src/backend/voice/nova-sonic-adapter.ts`) plus its mocked-SDK unit tests, and swap the package.json dependencies to bring in `@aws-sdk/client-bedrock-runtime` (≥ 3.784.0) + `@smithy/node-http-handler` (≥ 4.0.4) while removing `@aws-sdk/client-transcribe-streaming`. Ports the working Python event schema from the 2026-09-12 experiment (RESULTS-2026-09-12.md) verbatim to TypeScript via the Node SDK's `InvokeModelWithBidirectionalStreamCommand`. This plan is INDEPENDENT of Plan 02 (webmToPcm16k) and can run in parallel with it — Wave 1.

Purpose: replace the Amazon Transcribe streaming provider with a Nova Sonic adapter that is a byte-compatible drop-in for the "buffer → transcript" contract voice.ts consumes. Empirical evidence (RESULTS-2026-09-12.md) shows Nova Sonic beats Transcribe on accuracy at ~5x real-time, so this is the cutover foundation. Wave 3 (Plan 04) removes the old transcribe-adapter once voice.ts is rewired (Plan 03).

Output: adapter file, adapter test file, updated package.json + package-lock.json. voice.ts is UNTOUCHED in this plan — the rewire happens in Plan 03 (Wave 2). transcribe-adapter.ts continues to exist alongside nova-sonic-adapter.ts through Wave 2; both are removed together in Wave 3 (Plan 04) after voice.ts stops importing either.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/109-stt-provider-swap-amazon-transcribe-to-amazon-nova-sonic-on-/109-CONTEXT.md
@src/backend/voice/transcribe-adapter.ts
@src/backend/voice/transcribe-adapter.test.ts
@src/backend/voice/polly-adapter.ts
@src/backend/voice/aws-errors.ts
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add npm deps (bedrock-runtime + node-http-handler), remove transcribe-streaming</name>
  <files>package.json, package-lock.json</files>

  <read_first>
    - /home/ubuntu/skynet-sky/package.json (locate the current `@aws-sdk/client-transcribe-streaming` entry at line 46 and the adjacent AWS SDK dep block to keep alphabetical ordering)
    - /home/ubuntu/fleet/roles/box-maintainer/bounties/stt-nova-sonic-migration/RECON.md § 5 (Node SDK story — pins `@aws-sdk/client-bedrock-runtime ^3.785` + `@smithy/node-http-handler ^4.0.4` per the Amazon websocket-nodejs sample)
    - .planning/phases/109-stt-provider-swap-amazon-transcribe-to-amazon-nova-sonic-on-/109-CONTEXT.md § Implementation Decisions → SDK + transport, § package.json
  </read_first>

  <behavior>
    - Per D-PKG: `@aws-sdk/client-transcribe-streaming` is REMOVED from `dependencies`.
    - Per D-SDK: `@aws-sdk/client-bedrock-runtime` is ADDED at `^3.784.0` (or latest satisfying the ≥ 3.784.0 floor — pick a specific published version at install time). Bedrock-runtime versions predating 3.784.0 do NOT expose `InvokeModelWithBidirectionalStreamCommand` and MUST NOT be selected.
    - Per D-TRANSPORT: `@smithy/node-http-handler` is ADDED at `^4.0.4` (or latest satisfying ≥ 4.0.4). Older majors do NOT export `NodeHttp2Handler` with the shape this adapter needs.
    - `npm install` runs cleanly and regenerates `package-lock.json` with the new resolved versions. The install completes with no `ERESOLVE` failure.
    - No other dependencies are touched.
  </behavior>

  <action>
    Per D-SDK + D-TRANSPORT + D-PKG: edit `/home/ubuntu/skynet-sky/package.json` to REMOVE the `"@aws-sdk/client-transcribe-streaming": "^3.1129.0"` line, ADD `"@aws-sdk/client-bedrock-runtime": "^3.784.0"` (or newer published version — resolve to the exact latest at install time), and ADD `"@smithy/node-http-handler": "^4.0.4"` (or newer). Preserve alphabetical ordering within the `dependencies` block (bedrock-runtime lands where transcribe-streaming used to sit; node-http-handler lands in the `@smithy/*` section, alphabetically after any existing `@smithy` entries).

    Then run `npm install` in the repository root to regenerate `package-lock.json`. Do NOT hand-edit `package-lock.json` — it must be produced by npm.

    Do NOT touch any devDependencies. Do NOT bump any other dep. Do NOT run `npm audit fix` — out of scope.
  </action>

  <verify>
    <automated>cd /home/ubuntu/skynet-sky &amp;&amp; grep -c '"@aws-sdk/client-transcribe-streaming"' package.json | grep -q '^0$' &amp;&amp; grep -c '"@aws-sdk/client-bedrock-runtime"' package.json | grep -q '^1$' &amp;&amp; grep -c '"@smithy/node-http-handler"' package.json | grep -q '^1$'</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '"@aws-sdk/client-transcribe-streaming"' /home/ubuntu/skynet-sky/package.json` returns `0`.
    - `grep -c '"@aws-sdk/client-bedrock-runtime"' /home/ubuntu/skynet-sky/package.json` returns `1` (present exactly once in dependencies).
    - `grep -c '"@smithy/node-http-handler"' /home/ubuntu/skynet-sky/package.json` returns `1`.
    - The resolved bedrock-runtime version in `package-lock.json` is `>= 3.784.0`. Verify: `node -e "const l = require('/home/ubuntu/skynet-sky/package-lock.json'); const v = l.packages['node_modules/@aws-sdk/client-bedrock-runtime'].version; const [maj,min,pat] = v.split('.').map(Number); process.exit(maj>3||(maj===3&&(min>784||(min===784&&pat>=0)))?0:1)"` exits 0.
    - The resolved `@smithy/node-http-handler` version in `package-lock.json` is `>= 4.0.4` (same check pattern).
    - `package-lock.json` exists and has been regenerated (mtime newer than the pre-install state).
  </acceptance_criteria>

  <done>package.json + package-lock.json swapped: transcribe-streaming out; bedrock-runtime ≥ 3.784.0 + node-http-handler ≥ 4.0.4 in.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the failing nova-sonic-adapter test file (RED) — mocked SDK topology mirroring transcribe-adapter.test.ts</name>
  <files>src/backend/voice/nova-sonic-adapter.test.ts</files>

  <read_first>
    - /home/ubuntu/skynet-sky/src/backend/voice/transcribe-adapter.test.ts (entire file — this is the mock topology to mirror byte-for-byte at the harness level: vi.hoisted ctor spies, vi.mock function-ctor pattern, dynamic import AFTER the mock, async-iterable stream fixtures)
    - /home/ubuntu/skynet-sky/src/backend/voice/polly-adapter.test.ts (parallel example of the same vi.hoisted + vi.mock function-ctor pattern for an AWS SDK client)
    - /home/ubuntu/fleet/roles/box-maintainer/bounties/stt-nova-sonic-migration/RECON.md § 3a (integration recipe — event shape and body: generateChunks() async-iterable pattern)
    - /home/ubuntu/fleet/roles/box-maintainer/bounties/stt-nova-sonic-migration/RESULTS-2026-09-12.md (empirical event sequence from the Python experiment — the test fixtures should mirror this)
    - .planning/phases/109-stt-provider-swap-amazon-transcribe-to-amazon-nova-sonic-on-/109-CONTEXT.md § Nova Sonic event schema (locked send sequence + receive processing)
  </read_first>

  <behavior>
    Test topology mirrors `transcribe-adapter.test.ts` — mocked `@aws-sdk/client-bedrock-runtime` + `@smithy/node-http-handler` via `vi.hoisted` ctor spies + `vi.mock` function-ctor pattern, adapter imported dynamically AFTER the mock is installed.

    Required test coverage:

    - **Test 1 (singleton, D-SINGLETON):** `BedrockRuntimeClient` constructor called exactly once at module import. Assertion: `bedrockClientCtor.mock.calls.length === 1`.
    - **Test 2 (region + no credentials, D-CREDS + D-REGION):** The ctor call receives `{ region: 'us-east-1', requestHandler: <NodeHttp2Handler-instance> }` and does NOT have a `credentials` property. Assertion: `expect(bedrockClientCtor.mock.calls[0][0]).not.toHaveProperty('credentials'); expect(bedrockClientCtor.mock.calls[0][0].region).toBe('us-east-1')`.
    - **Test 3 (NodeHttp2Handler config, D-TRANSPORT):** The `NodeHttp2Handler` constructor was called with `{ requestTimeout: 300_000, sessionTimeout: 300_000, maxConcurrentStreams: 20 }`. Assertion: `expect(nodeHttp2HandlerCtor.mock.calls[0][0]).toEqual({ requestTimeout: 300_000, sessionTimeout: 300_000, maxConcurrentStreams: 20 })`.
    - **Test 4 (command shape + modelId, D-PROV):** `transcribeNovaSonic(Buffer.alloc(1920))` invokes `client.send` with an `InvokeModelWithBidirectionalStreamCommand` where `modelId === 'amazon.nova-2-sonic-v1:0'` and `body` is an async iterable (has `[Symbol.asyncIterator]`). Assertion: `expect(input.modelId).toBe('amazon.nova-2-sonic-v1:0'); expect(typeof input.body[Symbol.asyncIterator]).toBe('function')`.
    - **Test 5 (event sequence, D-EVENTS + D-AUDIOOUT + D-ASYNCGEN):** Consume the generated `body` async-iterable in the test, JSON.parse each chunk's bytes back to an object, and assert the event ordering matches the locked sequence exactly: `sessionStart`, `promptStart` (containing both `textOutputConfiguration` AND `audioOutputConfiguration`), `contentStart{role:'SYSTEM',type:'TEXT'}`, `textInput{...system prompt...}`, `contentEnd` (SYSTEM), `contentStart{role:'USER',type:'AUDIO', audioInputConfiguration:{mediaType:'audio/lpcm', sampleRateHertz:16000, sampleSizeBits:16, channelCount:1, audioType:'SPEECH', encoding:'base64'}}`, then at least one `audioInput`, then `contentEnd` (USER), `promptEnd`, `sessionEnd`. The `promptStart.audioOutputConfiguration` must be present per D-AUDIOOUT (empirical Amazon requirement — sending without it hits `ValidationException: AudioOutputConfiguration must be set`).
    - **Test 6 (audio chunking, D-AUDIO):** Feed a `Buffer.alloc(1920 * 5)` (5 chunks worth of 60 ms LPCM). Assert exactly 5 `audioInput` events are emitted. Each event's `content` field is a base64 string; `Buffer.from(content, 'base64').length === 1920`.
    - **Test 7 (role=USER filter, D-FILTER):** Mock `client.send` to return a response whose `body` yields (a) a textOutput with `role: 'USER', content: 'hello world'`, (b) a textOutput with `role: 'ASSISTANT', content: 'sure thing!'`, (c) an audioOutput event, (d) a `completionEnd`. Assertion: `transcribeNovaSonic(...)` resolves to `'hello world'` — ASSISTANT text and audio output are discarded.
    - **Test 8 (empty transcript):** When the mocked response yields ONLY ASSISTANT textOutput + audioOutput + completionEnd, `transcribeNovaSonic` resolves to `''`.
    - **Test 9 (clean teardown, D-TEARDOWN):** When the mocked response yields the USER transcript + completionEnd AND THEN throws a `ValidationException` (name = 'ValidationException') as the next iteration, the adapter STILL resolves with the captured transcript and does NOT propagate the post-completionEnd ValidationException as an unhandled rejection. Prefer the "swallow post-completionEnd ValidationException" implementation over the "consume everything before sessionEnd" one for testability, but either is acceptable — the test asserts the observable behavior (transcript returned, no uncaught throw).
    - **Test 10 (error propagation, non-Access-Denied):** When `client.send` rejects with a generic Error (name = 'NetworkError'), `transcribeNovaSonic` rejects with the same error unchanged (voice.ts owns the isAwsAccessDenied classification — this adapter does NOT translate errors).
    - **Test 11 (AccessDenied is not swallowed by the adapter):** When `client.send` rejects with an `AccessDeniedException` (name + $metadata.httpStatusCode = 403), `transcribeNovaSonic` rejects with the same error unchanged. This mirrors the transcribe-adapter contract — voice.ts's `isAwsAccessDenied(err)` handles the 503 branch.
    - **Test 12 (singleton reuse across calls):** Calling `transcribeNovaSonic` twice results in `bedrockClientCtor.mock.calls.length === 1` (constructor is not re-invoked).

    The test file is written FIRST (RED). Task 3 makes it green.
  </behavior>

  <action>
    Per D-EVENTS + D-FILTER + D-TERM + D-AUDIO + D-AUDIOOUT + D-SINGLETON + D-CREDS + D-REGION + D-TRANSPORT + D-PROV + D-TEARDOWN + D-ASYNCGEN: create `/home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.test.ts` following the topology in `transcribe-adapter.test.ts` (studied via `<read_first>`).

    Structure the file as:

    1. `vi.hoisted` spies: `bedrockClientCtor`, `invokeCmdCtor`, `sendMock`, `nodeHttp2HandlerCtor`.
    2. `vi.mock('@aws-sdk/client-bedrock-runtime', () => ({ BedrockRuntimeClient: function-ctor, InvokeModelWithBidirectionalStreamCommand: function-ctor }))`.
    3. `vi.mock('@smithy/node-http-handler', () => ({ NodeHttp2Handler: function-ctor }))`.
    4. Dynamic import: `const { transcribeNovaSonic } = await import('./nova-sonic-adapter.js')`.
    5. Helper `fakeResponseBody(events: Array<{...}>)` returning an async-iterable of `{ chunk: { bytes: TextEncoder.encode(JSON.stringify(event)) } }` — used to drive the response side of `sendMock.mockResolvedValueOnce({ body: fakeResponseBody([...]) })`.
    6. Twelve `it(...)` blocks per the behavior table above, each asserting on `bedrockClientCtor` / `invokeCmdCtor` / `sendMock` / the resolved value / the rejection.

    For Test 5 (event sequence), collect the yielded events by draining the body iterable inside the test body: `const collected = []; for await (const frame of input.body) { collected.push(JSON.parse(new TextDecoder().decode(frame.chunk.bytes))); }`. Then assert the ORDER of top-level keys: `expect(Object.keys(collected[0])).toContain('sessionStart')`, etc. Do NOT assert on internal Uint8Array bytes — assert on the parsed JSON objects.

    For Test 9 (teardown), the "throws after completionEnd" case is modeled by the fake response body yielding the terminal event AND THEN throwing on the next iteration: `async *[Symbol.asyncIterator]() { yield event1; yield event2; yield {completionEnd:{}}; const err = new Error('post-stream ValidationException'); err.name='ValidationException'; throw err; }`. The adapter is expected to either (a) return before consuming the throwing tail because completionEnd terminates the loop, OR (b) catch the ValidationException specifically. Test asserts observable behavior only: transcript is captured, no uncaught throw.

    Do NOT invoke real ffmpeg, real AWS, or real HTTP/2 anywhere. All I/O is mocked. Match the naming conventions of `transcribe-adapter.test.ts` verbatim.
  </action>

  <verify>
    <automated>cd /home/ubuntu/skynet-sky &amp;&amp; test -f src/backend/voice/nova-sonic-adapter.test.ts &amp;&amp; grep -c "transcribeNovaSonic" src/backend/voice/nova-sonic-adapter.test.ts | awk '$1>=6'</automated>
  </verify>

  <acceptance_criteria>
    - `test -f /home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.test.ts` succeeds.
    - `grep -c "transcribeNovaSonic" /home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.test.ts` returns AT LEAST 6 (used across multiple test bodies).
    - `grep -c "InvokeModelWithBidirectionalStreamCommand" /home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.test.ts` returns AT LEAST 1 (the vi.mock factory).
    - `grep -c "NodeHttp2Handler" /home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.test.ts` returns AT LEAST 1 (the vi.mock factory).
    - `grep -c "@aws-sdk/client-bedrock-runtime" /home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.test.ts` returns AT LEAST 1.
    - `grep -c "amazon.nova-2-sonic-v1:0" /home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.test.ts` returns AT LEAST 1 (Test 4).
    - `grep -c "audioOutputConfiguration" /home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.test.ts` returns AT LEAST 1 (Test 5 assertion for D-AUDIOOUT).
    - `grep -cE "role.*USER" /home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.test.ts` returns AT LEAST 1 (Test 7).
    - `grep -cE "role.*ASSISTANT" /home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.test.ts` returns AT LEAST 1 (Test 7 negative assertion).
    - `grep -c "ValidationException" /home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.test.ts` returns AT LEAST 1 (Test 9).
    - The file has AT LEAST 12 `it(` statements: `grep -c "^  it\\|^    it\\|^it\\|it(" /home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.test.ts | awk '$1>=12'` succeeds.
    - Running the tests confirms RED (adapter does not yet exist): `npx vitest run src/backend/voice/nova-sonic-adapter.test.ts` exits non-zero with an import error mentioning `nova-sonic-adapter`.
  </acceptance_criteria>

  <done>Test file exists with all 12 test cases, mocks `@aws-sdk/client-bedrock-runtime` + `@smithy/node-http-handler`, and fails RED because the adapter has not been written yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write nova-sonic-adapter.ts (GREEN) — port Python event schema to TS via bidirectional stream + async generator</name>
  <files>src/backend/voice/nova-sonic-adapter.ts</files>

  <read_first>
    - /home/ubuntu/skynet-sky/src/backend/voice/transcribe-adapter.ts (pattern for module-singleton client, IMDS-only ctor, audio chunk generator, streaming loop, error propagation — line-by-line reference for the "shape" of the new adapter)
    - /home/ubuntu/skynet-sky/src/backend/voice/polly-adapter.ts (parallel example of the singleton + region hardcode + no-credentials pattern that STAYS untouched; use it as a second reference for what "correct AWS SDK singleton usage in Skynet" looks like)
    - /home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.test.ts (the RED tests from Task 2 — they are the contract; adapter must make them GREEN without modification)
    - /home/ubuntu/fleet/roles/box-maintainer/bounties/stt-nova-sonic-migration/RECON.md § 3a + § 5 (integration recipe with client construction + generator pattern + landmines list, especially #1: `body` MUST be async iterable)
    - /home/ubuntu/fleet/roles/box-maintainer/bounties/stt-nova-sonic-migration/RESULTS-2026-09-12.md (empirical event sequence from the working Python harness — the wire schema this adapter must reproduce)
    - .planning/phases/109-stt-provider-swap-amazon-transcribe-to-amazon-nova-sonic-on-/109-CONTEXT.md § Implementation Decisions (full authoritative decision set)
    - Amazon reference sample (external): https://github.com/aws-samples/amazon-nova-samples/tree/main/speech-to-speech/sample-codes/websocket-nodejs — TypeScript, same tech stack. Adapter is a near-drop-in port with role=USER filter added.
  </read_first>

  <behavior>
    Public API: `export async function transcribeNovaSonic(pcmBuffer: Buffer): Promise<string>`.

    Contract:
    - Input `pcmBuffer` is raw LPCM 16 kHz s16le mono. Caller (voice.ts, Plan 03) guarantees this shape via `webmToPcm16k` (Plan 02).
    - Returns the joined USER transcript captured from Nova Sonic's textOutput events. Empty string if no USER textOutput fired.
    - Throws the raw SDK error unchanged for AWS failures (AccessDenied, throttling, network). Upstream voice.ts owns the isAwsAccessDenied → 503 classification via `aws-errors.ts`.

    Module-level singleton (D-SINGLETON + D-CREDS + D-REGION + D-TRANSPORT):
    - `const client = new BedrockRuntimeClient({ region: 'us-east-1', requestHandler: new NodeHttp2Handler({ requestTimeout: 300_000, sessionTimeout: 300_000, maxConcurrentStreams: 20 }) })` at module scope.
    - NO `credentials` field on the ctor arg — IMDS via SDK default provider chain.

    Send sequence (D-EVENTS + D-AUDIOOUT + D-AUDIO + D-ASYNCGEN):
    - Build an `async function* generateEvents()` that yields event frames in this exact order:
      1. `sessionStart` with `inferenceConfiguration: { maxTokens: 512, topP: 0.9, temperature: 0.7 }`.
      2. `promptStart` with BOTH `textOutputConfiguration: { mediaType: 'text/plain' }` AND `audioOutputConfiguration: { mediaType: 'audio/lpcm', sampleRateHertz: 24000, sampleSizeBits: 16, channelCount: 1, voiceId: 'matthew', encoding: 'base64', audioType: 'SPEECH' }` (D-AUDIOOUT — Amazon rejects if audioOutputConfiguration is absent, even when the audio response is discarded).
      3. SYSTEM `contentStart{ type: 'TEXT', role: 'SYSTEM' }`.
      4. SYSTEM `textInput{ content: SYSTEM_PROMPT }` where SYSTEM_PROMPT is the transcription-only nudge from CONTEXT.md § System prompt (Claude's discretion permits iteration on wording; intent must be transcription-only).
      5. SYSTEM `contentEnd`.
      6. USER `contentStart{ type: 'AUDIO', role: 'USER', audioInputConfiguration: { mediaType: 'audio/lpcm', sampleRateHertz: 16000, sampleSizeBits: 16, channelCount: 1, audioType: 'SPEECH', encoding: 'base64' } }`.
      7. For each 60 ms slice of `pcmBuffer` (1920 bytes = 960 samples × 2 bytes): yield `audioInput{ content: <base64 of the slice> }`. Slices are paced at 5x real-time (see below).
      8. USER `contentEnd`.
      9. `promptEnd`.
      10. `sessionEnd`.
    - Each yielded frame is `{ chunk: { bytes: new TextEncoder().encode(JSON.stringify(<event-object>)) } }` (D-ASYNCGEN).
    - Pacing: 60 ms of audio per chunk at 5x real-time means each chunk is emitted every 12 ms (60 / 5). Implement via `await new Promise(r => setTimeout(r, 12))` between yields. Drift correction is not required for the ceiling — 5x is the empirically-validated safe operating point (10x drops audio, unlimited drops all — from RESULTS-2026-09-12.md).

    Invoke command:
    - `const response = await client.send(new InvokeModelWithBidirectionalStreamCommand({ modelId: 'amazon.nova-2-sonic-v1:0', body: generateEvents() }))` (D-PROV).
    - Docstring names `amazon.nova-sonic-v1:0` as the documented fallback model (one-line change if v2 becomes unavailable); do NOT expose as a runtime flag.

    Response consumer (D-FILTER + D-TERM + D-TEARDOWN):
    - `for await (const event of response.body!)`.
    - Decode `event.chunk.bytes` via `new TextDecoder().decode(event.chunk.bytes)` and `JSON.parse` it.
    - If the parsed object has a `textOutput` field with `role === 'USER'`, push `textOutput.content` onto a `transcripts: string[]` accumulator.
    - If `textOutput.role === 'ASSISTANT'`: SKIP (discarded per D-FILTER).
    - If the object has an `audioOutput` field: SKIP (discarded per D-FILTER).
    - If the object has a `completionEnd` field: BREAK the for-await loop (D-TERM).
    - Malformed JSON in a frame: log at info level and continue (per threat model — do not crash the stream).
    - Post-completionEnd: wrap the for-await loop in a try/catch that specifically swallows `ValidationException` (name-based; other errors propagate). This addresses the Python experiment's post-stream ValidationException without hiding real signals (D-TEARDOWN).
    - Return `transcripts.join(' ')`.

    Docstring requirements:
    - File-level docblock at the top mirrors `transcribe-adapter.ts`'s docblock format: locked design decisions listed with references to CONTEXT.md decisions D-PROV / D-SDK / D-TRANSPORT / D-CREDS / D-REGION / D-SINGLETON / D-EVENTS / D-AUDIOOUT / D-AUDIO / D-FILTER / D-TERM / D-PROMPT / D-TEARDOWN / D-ASYNCGEN.
    - Public `transcribeNovaSonic` docstring names the fallback model `amazon.nova-sonic-v1:0` as a one-line change path (per D-PROV — documented fallback, not a runtime flag).
  </behavior>

  <action>
    Per D-PROV + D-SDK + D-TRANSPORT + D-CREDS + D-REGION + D-SINGLETON + D-EVENTS + D-AUDIOOUT + D-AUDIO + D-FILTER + D-TERM + D-PROMPT + D-TEARDOWN + D-ASYNCGEN: create `/home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.ts` (target ~200-250 lines including docblock).

    Imports:
    - `BedrockRuntimeClient`, `InvokeModelWithBidirectionalStreamCommand` from `@aws-sdk/client-bedrock-runtime`.
    - `NodeHttp2Handler` from `@smithy/node-http-handler`.
    - No import from `aws-errors.ts` — the adapter throws SDK errors unchanged (voice.ts classifies).

    Module scope:
    - Constants for the audio contract: `CHUNK_BYTES = 1920` (60 ms of 16 kHz s16le mono), `PACE_MS = 12` (60 ms of audio / 5x real-time = 12 ms wallclock per chunk), `SAMPLE_RATE_HZ = 16000`.
    - `MODEL_ID = 'amazon.nova-2-sonic-v1:0'` (D-PROV; docblock names `'amazon.nova-sonic-v1:0'` as the documented fallback).
    - `SYSTEM_PROMPT` string — transcription-only nudge; verbatim from CONTEXT.md § System prompt is acceptable, or a lightly-iterated equivalent preserving the "transcribe-only, do not answer, short acknowledgment" intent.
    - `client` singleton: `new BedrockRuntimeClient({ region: 'us-east-1', requestHandler: new NodeHttp2Handler({ requestTimeout: 300_000, sessionTimeout: 300_000, maxConcurrentStreams: 20 }) })`. NO `credentials` property (D-CREDS).

    `transcribeNovaSonic(pcmBuffer)`:
    1. Build the `async function* generateEvents()` per the send sequence in `<behavior>`. Wrap each event in `{ chunk: { bytes: new TextEncoder().encode(JSON.stringify(event)) } }`.
    2. `await client.send(new InvokeModelWithBidirectionalStreamCommand({ modelId: MODEL_ID, body: generateEvents() }))`.
    3. Wrap the `for await (const event of response.body)` loop in try/catch. Inside: `JSON.parse(new TextDecoder().decode(event.chunk.bytes))`; wrap the parse in its own try/catch that logs at info level (use `databaseLogger` from `../utils/logger.js` — mirror voice.ts's logger import pattern) and continues on parse failure. Dispatch:
       - `parsed.textOutput?.role === 'USER'` → push `parsed.textOutput.content` onto `transcripts`.
       - `parsed.completionEnd` → break the for-await loop.
       - Otherwise (ASSISTANT text, audioOutput, other): ignore.
    4. Outer catch (post-completionEnd stream errors): if `err.name === 'ValidationException'` AND at least one textOutput has been consumed → swallow (log info). Otherwise: rethrow. This is the D-TEARDOWN guard.
    5. Return `transcripts.join(' ')`.

    Do NOT add a runtime model-selection flag. Do NOT import from `transcribe-adapter.ts`. Do NOT expose `chunkGenerator` publicly (keep it inside `transcribeNovaSonic` scope — the test drains `body` from the mocked `client.send` call, which captures the generator naturally).

    Do NOT introduce a `credentials` field. Do NOT set a fallback `region` env var. Do NOT expose a second export.

    Landmine mitigation baked in:
    - #1 (body must be async iterable): use `async function*`, not a plain `{ chunk: { bytes } }` object.
    - #2 (audioOutputConfiguration required): always emit both textOutputConfiguration and audioOutputConfiguration on promptStart.
    - #3 (5x pace ceiling): PACE_MS = 12 not 6 or 0.
    - #4 (8-min session timeout): documented in the file-level docblock but not code-guarded — max real-world clip is ~2 min.
    - #5 (HTTP/2 shutdown quirks): documented in the file-level docblock; no code guard needed for Skynet's long-lived Express pattern.
  </action>

  <verify>
    <automated>cd /home/ubuntu/skynet-sky &amp;&amp; npx vitest run src/backend/voice/nova-sonic-adapter.test.ts --reporter=default</automated>
  </verify>

  <acceptance_criteria>
    - `test -f /home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.ts` succeeds.
    - `wc -l /home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.ts | awk '$1>=180'` succeeds (min 180 lines including docblock).
    - `grep -c "InvokeModelWithBidirectionalStreamCommand" /home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.ts` returns AT LEAST 1 (import + usage).
    - `grep -c "NodeHttp2Handler" /home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.ts` returns AT LEAST 1 (import + usage).
    - `grep -c "BedrockRuntimeClient" /home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.ts` returns AT LEAST 1.
    - `grep -c "amazon.nova-2-sonic-v1:0" /home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.ts` returns AT LEAST 1 (the MODEL_ID constant or literal).
    - `grep -c "amazon.nova-sonic-v1:0" /home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.ts` returns AT LEAST 1 (docstring names the fallback per D-PROV).
    - **CRITICAL: `grep '"credentials"\|credentials:' /home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.ts | grep -v '^\s*\*\|^\s*//'` returns ZERO matches (per D-CREDS — IMDS only, no explicit credentials).**
    - `grep -cE "role\s*===\s*['\"]USER['\"]" /home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.ts` returns AT LEAST 1 (D-FILTER).
    - `grep -c "audioOutputConfiguration" /home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.ts` returns AT LEAST 1 (D-AUDIOOUT).
    - `grep -cE "async\s+function\s*\*" /home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.ts` returns AT LEAST 1 (D-ASYNCGEN).
    - `grep -c "completionEnd" /home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.ts` returns AT LEAST 1 (D-TERM).
    - `grep -c "us-east-1" /home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.ts` returns AT LEAST 1 (D-REGION).
    - `grep -c "ValidationException" /home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.ts` returns AT LEAST 1 (D-TEARDOWN swallow).
    - `grep -cE "requestTimeout.*300_?000|300000" /home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.ts` returns AT LEAST 1 (D-TRANSPORT).
    - `grep -cE "maxConcurrentStreams.*20" /home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.ts` returns AT LEAST 1 (D-TRANSPORT).
    - `npx vitest run src/backend/voice/nova-sonic-adapter.test.ts` exits 0. All 12 tests pass green.
    - `npx tsc --noEmit -p /home/ubuntu/skynet-sky/tsconfig.json` succeeds (no type errors introduced — note: transcribe-adapter.ts still exists and still compiles; it is deleted in Wave 3 / Plan 04).
  </acceptance_criteria>

  <done>nova-sonic-adapter.ts exists, all 12 unit tests from Task 2 pass, IMDS-only credentials confirmed, event schema locked, USER-only filter locked, clean teardown implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| voice.ts → nova-sonic-adapter | Adapter is called with a raw LPCM Buffer produced upstream by ffmpeg (webmToPcm16k, Plan 02). Buffer is untrusted-origin bytes from an authenticated browser upload. |
| nova-sonic-adapter → Bedrock (Amazon) | AWS SDK sends signed HTTP/2 frames over TLS. IMDS credentials, us-east-1 endpoint. |
| Bedrock → nova-sonic-adapter | Response is a bidirectional-stream of eventstream frames. Frame bytes are UTF-8 JSON per Amazon's published schema; malformed bytes are possible in theory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-01-01 | Tampering | Audio buffer input to adapter | mitigate | Adapter treats buffer as opaque bytes; no shell interpolation (SDK signs and encodes internally). Bytes-only path — no argv, no query string. |
| T-109-01-02 | Information disclosure | AccessDenied error surface | accept-and-defer | Adapter throws SDK error unchanged; voice.ts (Plan 03) owns `isAwsAccessDenied` → 503 classification. Error message text is NOT surfaced to clients. |
| T-109-01-03 | Denial of service | Malformed event bytes crash the response loop | mitigate | Wrap `JSON.parse(new TextDecoder().decode(...))` per event in its own try/catch that logs at info level and continues (does NOT break the loop). Prevents a single corrupt frame from killing the transcript accumulator. |
| T-109-01-04 | Information disclosure | ASSISTANT tokens leaked into transcript | mitigate | `role === 'USER'` filter is the sole gate for accumulating textOutput content (D-FILTER). ASSISTANT text and audioOutput events are dropped. Unit test 7 asserts. |
| T-109-01-05 | Denial of service | Post-stream ValidationException propagated as uncaught rejection | mitigate | Wrap the for-await loop in try/catch that specifically swallows ValidationException AFTER at least one textOutput has been consumed (D-TEARDOWN). Unit test 9 asserts. |
| T-109-01-06 | Elevation of privilege | Explicit `credentials` on BedrockRuntimeClient disables IMDS chain | mitigate | Adapter ctor uses `{ region, requestHandler }` only — NO `credentials` property. Test 2 asserts. Grep gate in acceptance criteria hard-fails if a `credentials:` field appears in the source. |
| T-109-01-07 | Repudiation | Silent drop of transcript on adapter internal error | mitigate | AccessDenied and generic AWS errors REJECT the adapter promise (Tests 10 + 11). Upstream voice.ts logs the failure with structured operation tag. |
| T-109-01-SC | Tampering | npm/pip/cargo installs | mitigate | New deps `@aws-sdk/client-bedrock-runtime` + `@smithy/node-http-handler` are OFFICIAL AWS + Smithy packages (aws organization on GitHub). Legitimacy: LEGITIMATE — well-known, widely-used, no ASSUMED/SUS/SLOP status. No blocking checkpoint required. |
</threat_model>

<verification>
- `npx vitest run src/backend/voice/nova-sonic-adapter.test.ts` — GREEN (12/12 tests pass).
- `grep -c '"credentials"\|credentials:' src/backend/voice/nova-sonic-adapter.ts` — returns 0 (IMDS-only invariant, D-CREDS).
- `grep -c "InvokeModelWithBidirectionalStreamCommand" src/backend/voice/nova-sonic-adapter.ts` — returns ≥ 1.
- `grep -c "role\s*===\s*['\"]USER['\"]" src/backend/voice/nova-sonic-adapter.ts` — returns ≥ 1 (D-FILTER).
- `grep -c "@aws-sdk/client-transcribe-streaming" package.json` — returns 0 (removed).
- `grep -c "@aws-sdk/client-bedrock-runtime" package.json` — returns 1 (added).
- `grep -c "@smithy/node-http-handler" package.json` — returns 1 (added).
- `npx tsc --noEmit -p tsconfig.json` — succeeds (adapter compiles; transcribe-adapter still exists through Wave 2 and continues to compile; deletion is Wave 3).
</verification>

<success_criteria>
- New adapter file exists at the mandated path, honors every locked decision in CONTEXT.md § Implementation Decisions (D-PROV through D-ASYNCGEN).
- All 12 unit tests in nova-sonic-adapter.test.ts pass green.
- Module-level BedrockRuntimeClient singleton constructed once with IMDS-only credentials.
- Package deps swapped: transcribe-streaming out, bedrock-runtime + node-http-handler in.
- No import of the new adapter from voice.ts yet — that wiring is Plan 03.
</success_criteria>

<output>
Create `.planning/phases/109-stt-provider-swap-amazon-transcribe-to-amazon-nova-sonic-on-/109-01-SUMMARY.md` when done.
</output>
