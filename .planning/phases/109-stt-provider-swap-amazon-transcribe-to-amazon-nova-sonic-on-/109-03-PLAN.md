---
phase: 109-stt-provider-swap-amazon-transcribe-to-amazon-nova-sonic-on
plan: 03
type: execute
wave: 2
depends_on:
  - 109-01
  - 109-02
files_modified:
  - src/backend/database/routes/voice.ts
  - src/backend/database/routes/voice.test.ts
autonomous: true
requirements:
  - D-REWIRE
  - D-CUTOVER
  - D-FILTER
  - D-TESTS

must_haves:
  truths:
    - "`voice.ts` no longer imports `transcribeBuffer` from `transcribe-adapter.js` and no longer imports `transcribeBufferChunked` from `transcribe-orchestrator.js`."
    - "`voice.ts` DOES import `transcribeNovaSonic` from `../../voice/nova-sonic-adapter.js` and calls it exactly once inside `handleTranscribe`."
    - "The `CHUNKED_THRESHOLD_BYTES` constant is REMOVED from `voice.ts`. The byte-length ternary that dispatched to `transcribeBufferChunked` is REMOVED. `handleTranscribe` has a single-adapter code path (D-CUTOVER)."
    - "`transcodeForTranscribe`'s return shape simplifies to `{ buffer: Buffer, sampleRateHz: number }` — the `mediaEncoding` field is REMOVED (Nova Sonic accepts only LPCM). The FLAC passthrough branch (ext === 'flac') and the OGG passthrough branch (ext === 'ogg') are REMOVED (D-REWIRE — production is always WebM from MediaRecorder; edge cases have never fired). The single path is: WebM → `webmToPcm16k(buf)` → `{ buffer: pcmBuf, sampleRateHz: 16000 }`."
    - "The `webmToFlac` import in `voice.ts` is REMOVED. The new `webmToPcm16k` import from `../../voice/audio-transcode.js` replaces it."
    - "Preserved verbatim: the disk-bank fire-and-forget write (mkdir + writeFile at line ~127-142), the slash-command transform post-processing (WAKE_WORD_REGEX + fetchSkillCatalog + applyServerSlashTransform), the `isAwsAccessDenied` → 503 classifier branch, and the generic-error 502 branch. Only the transcode + adapter-invocation lines change."
    - "`voice.test.ts` no longer imports `transcribeBufferChunked` from `../../voice/transcribe-orchestrator.js` and no longer references it in any `vi.mock` factory."
    - "`voice.test.ts` DOES include a `vi.mock('../../voice/nova-sonic-adapter.js', ...)` factory that returns `{ transcribeNovaSonic: vi.fn() }`, and DOES include a `vi.mock('../../voice/audio-transcode.js', ...)` factory that returns `{ webmToPcm16k: vi.fn(async (buf: Buffer) => buf) }` (replacing the current `webmToFlac` stub)."
    - "The Phase 100 chunked-path describe block (T1 fast-path routing, T2 chunked routing, T3 chunked AccessDenied, T4 chunked generic-error, T5 slash-transform on chunked, T6 disk-bank on chunked) at lines 750-903 of the current voice.test.ts is DELETED. Its functional coverage (AccessDenied → 503, slash-transform preservation, disk-bank ordering) is REPLACED by equivalent tests operating on the single-path `transcribeNovaSonic` call."
    - "The existing tests that already mock `transcribeSendMock` for the single-stream `transcribeBuffer` path (roughly lines 293-747) are REWRITTEN to mock `transcribeNovaSonic` instead. Test assertions for `MediaEncoding === 'flac'` / `MediaSampleRateHertz === 48000` are DELETED — Nova Sonic replaces both fields with the LPCM 16 kHz contract."
    - "PRESERVED test cases (each ported to mock `transcribeNovaSonic`): (a) 200 + text on success, (b) 400 on missing file field, (c) disk-bank writes raw multipart bytes BEFORE transcode with correct filename pattern + mkdir-before-transcode ordering, (d) 502 on `webmToPcm16k` throw, (e) AccessDenied → 503, (f) slash-transform applied when `hostId` supplied AND transcript matches wake word."
    - "The scoped vitest command `npx vitest run --related src/backend/database/routes/voice.ts src/backend/voice/nova-sonic-adapter.ts src/backend/voice/audio-transcode.ts` passes green."
    - "`npx tsc --noEmit -p tsconfig.json` succeeds."
  artifacts:
    - path: "src/backend/database/routes/voice.ts"
      provides: "handleTranscribe rewired to call transcribeNovaSonic via a single webmToPcm16k transcode step; CHUNKED_THRESHOLD_BYTES + chunked branch + mediaEncoding + FLAC/OGG passthrough removed"
      contains: "transcribeNovaSonic"
    - path: "src/backend/database/routes/voice.test.ts"
      provides: "Rewritten transcribe-endpoint tests: mocks transcribeNovaSonic + webmToPcm16k; Phase 100 chunked tests deleted; preserved-coverage tests ported"
      contains: "transcribeNovaSonic"
  key_links:
    - from: "src/backend/database/routes/voice.ts (handleTranscribe)"
      to: "src/backend/voice/nova-sonic-adapter.ts (transcribeNovaSonic)"
      via: "single-line adapter call: const transcript = await transcribeNovaSonic(transcodeResult.buffer);"
      pattern: "transcribeNovaSonic\\("
    - from: "src/backend/database/routes/voice.ts (transcodeForTranscribe)"
      to: "src/backend/voice/audio-transcode.ts (webmToPcm16k)"
      via: "single-path transcode: WebM → PCM 16 kHz mono"
      pattern: "webmToPcm16k\\("
    - from: "src/backend/database/routes/voice.ts (handleTranscribe catch block)"
      to: "src/backend/voice/aws-errors.ts (isAwsAccessDenied)"
      via: "AccessDenied classification unchanged from Phase 98 — reused for Nova Sonic errors"
      pattern: "isAwsAccessDenied"
---

<objective>
Rewire `src/backend/database/routes/voice.ts::handleTranscribe` to use the new Nova Sonic adapter (from Plan 01) and the new `webmToPcm16k` transcode helper (from Plan 02), and delete the Phase 100 chunked-path branching from both the route handler and its test suite. This is the integration point that flips STT from Amazon Transcribe to Amazon Nova Sonic (D-CUTOVER — single-adapter code path, no feature flag, revert path is `git revert`).

Depends on Plan 01 (`nova-sonic-adapter.ts` must exist and export `transcribeNovaSonic`) and Plan 02 (`webmToPcm16k` must exist as an export of `audio-transcode.ts`). Wave 2 runs serially after both Wave 1 plans complete.

Purpose: land the cutover atomically at the integration boundary. After this plan, the browser-facing `/voice/transcribe` request contract is unchanged, but the internal path is: multipart upload → disk-bank raw bytes → `webmToPcm16k` → `transcribeNovaSonic(pcmBuf)` → slash-transform → response. The `transcribe-adapter.ts` and `transcribe-orchestrator.ts` modules still exist on disk at the end of this plan but nothing in `voice.ts` imports them; Plan 04 (Wave 3) deletes them.

Output: `voice.ts` rewired (~30 lines changed, ~15 lines net delete); `voice.test.ts` rewritten (Phase 100 tests deleted ~150 lines, remaining tests re-mocked ~50 lines edited). Zero changes to the request/response contract; zero changes to the disk-bank behavior; zero changes to the slash-transform behavior; zero changes to the AccessDenied → 503 or generic → 502 error surfaces.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/109-stt-provider-swap-amazon-transcribe-to-amazon-nova-sonic-on-/109-CONTEXT.md
@src/backend/database/routes/voice.ts
@src/backend/database/routes/voice.test.ts
@src/backend/voice/nova-sonic-adapter.ts
@src/backend/voice/audio-transcode.ts
@src/backend/voice/aws-errors.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire voice.ts::handleTranscribe — drop chunked branch, drop mediaEncoding, drop FLAC/OGG passthrough, call transcribeNovaSonic</name>
  <files>src/backend/database/routes/voice.ts</files>

  <read_first>
    - /home/ubuntu/skynet-sky/src/backend/database/routes/voice.ts:1-30 (import block — locate the transcribeBuffer / transcribeBufferChunked / webmToFlac imports to REMOVE, and the imports of synthesizeToPcm / isAwsAccessDenied / slash-transform helpers to PRESERVE)
    - /home/ubuntu/skynet-sky/src/backend/database/routes/voice.ts:39-108 (constants + transcodeForTranscribe — CHUNKED_THRESHOLD_BYTES is deleted; return shape simplifies; FLAC/OGG passthrough branches are deleted)
    - /home/ubuntu/skynet-sky/src/backend/database/routes/voice.ts:112-229 (handleTranscribe — ternary at lines 155-165 is the target; disk-bank at 127-142 preserved; slash-transform at 172-211 preserved; catch block at 213-228 preserved)
    - /home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.ts (contract of transcribeNovaSonic — buffer in, string out; throws SDK errors unchanged; voice.ts owns AccessDenied classification)
    - /home/ubuntu/skynet-sky/src/backend/voice/audio-transcode.ts (webmToPcm16k signature — Buffer in, Promise<Buffer> out; 16 kHz LPCM s16le mono)
    - .planning/phases/109-stt-provider-swap-amazon-transcribe-to-amazon-nova-sonic-on-/109-CONTEXT.md § voice.ts rewiring (D-REWIRE: locked deletion of FLAC/OGG passthrough + CHUNKED_THRESHOLD_BYTES + mediaEncoding field)
  </read_first>

  <behavior>
    Import changes:
    - REMOVE `import { transcribeBuffer } from "../../voice/transcribe-adapter.js";` (line 19).
    - REMOVE `import { transcribeBufferChunked } from "../../voice/transcribe-orchestrator.js";` (line 20).
    - REMOVE `import { webmToFlac } from "../../voice/audio-transcode.js";` (line 21).
    - ADD `import { transcribeNovaSonic } from "../../voice/nova-sonic-adapter.js";` (place next to the polly-adapter import at line 18 alphabetically).
    - ADD `import { webmToPcm16k } from "../../voice/audio-transcode.js";` (replace the removed webmToFlac import).

    Constants:
    - REMOVE the `CHUNKED_THRESHOLD_BYTES` export at line 42-46 (with its multi-line comment).

    `transcodeForTranscribe`:
    - Signature simplifies. Old return: `{ buffer: Buffer; mediaEncoding: "flac" | "ogg-opus"; sampleRateHz: number }`. New return: `{ buffer: Buffer; sampleRateHz: number }`.
    - Body drops the `if (ext === "flac")` and `if (ext === "ogg")` early returns.
    - The default path becomes: `const pcmBuf = await webmToPcm16k(buf); return { buffer: pcmBuf, sampleRateHz: 16000 };`.
    - Docstring update: mention Nova Sonic + LPCM 16 kHz as the single accepted shape; drop references to Amazon Transcribe / FLAC 48 kHz / Ogg-Opus remux. Reference D-REWIRE + D-AUDIO from CONTEXT.md.

    `handleTranscribe`:
    - The ternary at lines 155-165 becomes a single line: `const transcript = await transcribeNovaSonic(transcodeResult.buffer);`.
    - The success log at lines 167-170 is UPDATED to remove `mediaEncoding` and `path` fields (both no longer meaningful): `databaseLogger.info(\`[voice-server] transcribe-ok textLen=${transcript.length}\`, { operation: "voice_transcribe" });`.
    - The disk-bank block at 127-142 is PRESERVED verbatim.
    - The slash-transform block at 172-211 is PRESERVED verbatim.
    - The catch block at 213-228 is PRESERVED verbatim (AccessDenied → 503, generic → 502).
    - `req.file` guard at 114-116 preserved.
    - `extFromMimetype` helper preserved (still used by the disk-bank filename builder).

    Everything else in voice.ts is untouched: `handleSpeak`, `handleSpeakStream`, the router bindings, `rejectBridgeServiceOnSpeak`, `SPEAK_TEXT_MAX`, `DEFAULT_VOICE`, `SAMPLE_PHRASE`, all the Polly-related code.
  </behavior>

  <action>
    Per D-REWIRE + D-CUTOVER + D-FILTER: modify `/home/ubuntu/skynet-sky/src/backend/database/routes/voice.ts` per the import / constant / helper / handler changes in `<behavior>` above.

    Import block:
    - Delete `import { transcribeBuffer } from "../../voice/transcribe-adapter.js";`
    - Delete `import { transcribeBufferChunked } from "../../voice/transcribe-orchestrator.js";`
    - Change `import { webmToFlac } from "../../voice/audio-transcode.js";` to `import { webmToPcm16k } from "../../voice/audio-transcode.js";`
    - Add `import { transcribeNovaSonic } from "../../voice/nova-sonic-adapter.js";` alphabetically near the polly-adapter import (line 18).

    Constants block:
    - Delete `export const CHUNKED_THRESHOLD_BYTES = 80_000;` and its 4-line comment block above (currently lines 42-46).

    `transcodeForTranscribe` function:
    - Rewrite the body to a single-path implementation: call `await webmToPcm16k(buf)` and return `{ buffer: pcmBuf, sampleRateHz: 16000 }`. The `ext` parameter is retained (still passed by callers) but is unused in the body (`void ext;` acknowledgment).
    - Update the docstring above the function to name D-REWIRE + D-AUDIO from CONTEXT.md, remove all references to `webmToFlac` / `mediaEncoding` / Transcribe / FLAC / Ogg-Opus, and describe the single-path WebM → LPCM 16 kHz transcode.

    `handleTranscribe` function:
    - Replace lines 155-165 (the ternary) with a single line: `const transcript = await transcribeNovaSonic(transcodeResult.buffer);`.
    - Replace the success log at lines 167-170 with the shorter shape from `<behavior>` above (drop `mediaEncoding=... path=...`).
    - PRESERVE everything else in handleTranscribe verbatim: the `req.file` guard, the disk-bank block (127-142), the slash-transform block (172-211), the catch block (213-228).

    Do NOT change:
    - `handleSpeak`, `handleSpeakStream`, or any Polly-related code.
    - The `POST /transcribe` route middleware chain (multipartOriginGuard → authenticateJWT → multer.single → handleTranscribe).
    - `extFromMimetype`, `DEFAULT_VOICE`, `SPEAK_TEXT_MAX`, `SAMPLE_PHRASE`.
    - The multer 25 MB `fileSize` cap.
    - The `authenticateJWT` gate order (authenticateJWT before multer).
    - The disk-bank filename builder logic (timestamp / userId / size / random tail).
    - The 25 MB multer cap, the Phase 103 multipartOriginGuard, or any T-16-* security invariants documented in the file header.

    Do NOT delete `transcribe-adapter.ts` or `transcribe-orchestrator.ts` in this plan. They are still present on disk but no longer imported by voice.ts. Plan 04 handles the file deletions.

    Do NOT introduce a feature flag, an env-var toggle, or any other runtime dispatch — D-CUTOVER locks single-adapter path.
  </action>

  <verify>
    <automated>cd /home/ubuntu/skynet-sky &amp;&amp; grep -c "transcribeNovaSonic" src/backend/database/routes/voice.ts | awk '$1>=2' &amp;&amp; grep -cE "transcribeBuffer\b|transcribeBufferChunked|webmToFlac|CHUNKED_THRESHOLD_BYTES" src/backend/database/routes/voice.ts | grep -q '^0$'</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "transcribeNovaSonic" /home/ubuntu/skynet-sky/src/backend/database/routes/voice.ts` returns AT LEAST 2 (import + call site).
    - `grep -c "webmToPcm16k" /home/ubuntu/skynet-sky/src/backend/database/routes/voice.ts` returns AT LEAST 2 (import + call site).
    - `grep -cE "\btranscribeBuffer\b" /home/ubuntu/skynet-sky/src/backend/database/routes/voice.ts` returns 0 (import + call site both removed).
    - `grep -c "transcribeBufferChunked" /home/ubuntu/skynet-sky/src/backend/database/routes/voice.ts` returns 0.
    - `grep -c "webmToFlac" /home/ubuntu/skynet-sky/src/backend/database/routes/voice.ts` returns 0.
    - `grep -c "CHUNKED_THRESHOLD_BYTES" /home/ubuntu/skynet-sky/src/backend/database/routes/voice.ts` returns 0.
    - `grep -c "mediaEncoding" /home/ubuntu/skynet-sky/src/backend/database/routes/voice.ts` returns 0 (removed from return shape + log line).
    - `grep -cE '"flac"|"ogg-opus"' /home/ubuntu/skynet-sky/src/backend/database/routes/voice.ts` returns 0 (FLAC/OGG passthrough branches deleted).
    - `grep -c "isAwsAccessDenied" /home/ubuntu/skynet-sky/src/backend/database/routes/voice.ts` returns AT LEAST 2 (import + catch-block classifier — preserved for Nova Sonic AccessDenied).
    - `grep -cE "applyServerSlashTransform|fetchSkillCatalog|WAKE_WORD_REGEX" /home/ubuntu/skynet-sky/src/backend/database/routes/voice.ts` returns AT LEAST 3 (slash-transform preservation).
    - `grep -cE "STT_RECORDINGS_DIR|transcribe-bank-write" /home/ubuntu/skynet-sky/src/backend/database/routes/voice.ts` returns AT LEAST 2 (disk-bank preservation).
    - `grep -cE "handleSpeak|handleSpeakStream" /home/ubuntu/skynet-sky/src/backend/database/routes/voice.ts` returns AT LEAST 4 (Polly path fully preserved).
    - `npx tsc --noEmit -p /home/ubuntu/skynet-sky/tsconfig.json` succeeds (transcribe-adapter.ts still exists and still compiles as an unimported module; Plan 04 deletes it).
  </acceptance_criteria>

  <done>voice.ts::handleTranscribe rewired to Nova Sonic single-path; chunked branch + mediaEncoding + FLAC/OGG passthrough deleted; disk-bank + slash-transform + AccessDenied surfaces preserved; TypeScript compiles clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite voice.test.ts transcribe-endpoint tests — mock transcribeNovaSonic + webmToPcm16k, delete Phase 100 tests, preserve behavioral coverage</name>
  <files>src/backend/database/routes/voice.test.ts</files>

  <read_first>
    - /home/ubuntu/skynet-sky/src/backend/database/routes/voice.test.ts:1-140 (file-level docblock + `vi.hoisted` block + AWS SDK vi.mock factories + audio-transcode + transcribe-orchestrator + skill-catalog + fs mocks — the mock scaffolding to rewire)
    - /home/ubuntu/skynet-sky/src/backend/database/routes/voice.test.ts:110-135 (imports block including CHUNKED_THRESHOLD_BYTES, transcribeBufferChunked, webmToFlac — remove these; add transcribeNovaSonic + webmToPcm16k)
    - /home/ubuntu/skynet-sky/src/backend/database/routes/voice.test.ts:270-290 (beforeEach mock resets — swap transcribeSendMock/webmToFlac for transcribeNovaSonic/webmToPcm16k)
    - /home/ubuntu/skynet-sky/src/backend/database/routes/voice.test.ts:293-747 (existing handleTranscribe describe block — REWRITE each test to mock transcribeNovaSonic instead of transcribeSendMock; DELETE MediaEncoding / MediaSampleRateHertz assertions)
    - /home/ubuntu/skynet-sky/src/backend/database/routes/voice.test.ts:750-903 (Phase 100 chunked-path describe block T1-T6 — DELETE ENTIRELY; equivalent coverage lands in the rewritten handleTranscribe block)
    - .planning/phases/109-stt-provider-swap-amazon-transcribe-to-amazon-nova-sonic-on-/109-CONTEXT.md § Testing surface (D-TESTS)
  </read_first>

  <behavior>
    Mock scaffolding surgery at the top of the file:

    - In the `vi.hoisted` block (line 29-43): remove `transcribeClientCtor`, `startStreamCmdCtor`, `transcribeSendMock`; add `transcribeNovaSonicMock: vi.fn()`. Keep `pollyClientCtor`, `synthesizeSpeechCmdCtor`, `pollySendMock`.
    - Delete the `vi.mock("@aws-sdk/client-transcribe-streaming", ...)` factory block (lines 58-68). Nova Sonic uses `@aws-sdk/client-bedrock-runtime` at the adapter layer, but the route tests mock the adapter facade directly (matches the polly-adapter approach used in Phase 98).
    - Add `vi.mock("../../voice/nova-sonic-adapter.js", () => ({ transcribeNovaSonic: transcribeNovaSonicMock }));` in the mock section.
    - Delete the `vi.mock("../../voice/transcribe-orchestrator.js", ...)` factory (lines 78-80).
    - In `vi.mock("../../voice/audio-transcode.js", ...)` (lines 72-74): replace `{ webmToFlac: vi.fn(async (buf: Buffer) => buf) }` with `{ webmToPcm16k: vi.fn(async (buf: Buffer) => buf) }`.

    Imports block (lines 114-125):
    - Remove `CHUNKED_THRESHOLD_BYTES` from the `./voice.js` destructure.
    - Swap `import { webmToFlac } from "../../voice/audio-transcode.js";` → `import { webmToPcm16k } from "../../voice/audio-transcode.js";`.
    - Remove `import { transcribeBufferChunked } from "../../voice/transcribe-orchestrator.js";`; add `import { transcribeNovaSonic } from "../../voice/nova-sonic-adapter.js";`.

    `beforeEach` (lines 270-283):
    - Remove `transcribeSendMock.mockReset()`, `startStreamCmdCtor.mockClear()`, `vi.mocked(webmToFlac).mockReset()`, `vi.mocked(webmToFlac).mockImplementation(...)`, `vi.mocked(transcribeBufferChunked).mockReset()`.
    - Add `transcribeNovaSonicMock.mockReset(); vi.mocked(webmToPcm16k).mockReset(); vi.mocked(webmToPcm16k).mockImplementation(async (buf: Buffer) => buf);`.

    The `fakeTranscriptStream` helper (lines 227-255) becomes dead code after the transcribe-streaming mock is removed — DELETE it.

    Existing `handleTranscribe` describe block (lines 293-747) — rewrite each test:

    - "returns 200 with {text} from Transcribe when webm arrives (flac default path)" → rename to "returns 200 with {text} from Nova Sonic when webm arrives (single path)". Replace `transcribeSendMock.mockResolvedValueOnce(...)` with `transcribeNovaSonicMock.mockResolvedValueOnce("hello world")`. Replace the `MediaEncoding === 'flac'` / `MediaSampleRateHertz === 48000` assertions with `expect(transcribeNovaSonicMock).toHaveBeenCalledTimes(1); expect(vi.mocked(webmToPcm16k)).toHaveBeenCalledTimes(1);`. Assert the buffer passed to `transcribeNovaSonic` is the return value of `webmToPcm16k`.
    - "returns 400 when req.file is missing" — UNCHANGED (does not touch the STT provider).
    - "disk-bank writes raw multipart bytes BEFORE any transcode (Pitfall 6)" — swap `webmToFlac` invocation-order assertion for `webmToPcm16k`; swap `transcribeSendMock` for `transcribeNovaSonicMock.mockResolvedValueOnce("hello")`. This is the D-14 preservation gate.
    - "returns 502 when webmToFlac throws" → rename to "returns 502 when webmToPcm16k throws". Replace `vi.mocked(webmToFlac).mockRejectedValueOnce(...)` with `vi.mocked(webmToPcm16k).mockRejectedValueOnce(new Error("pcm transcode failed"))`. Response assertion unchanged: 502 + `{ error: "STT error", status: 502 }`.
    - AccessDenied → 503 test: replace `transcribeSendMock.mockRejectedValueOnce(makeAccessDeniedError())` with `transcribeNovaSonicMock.mockRejectedValueOnce(makeAccessDeniedError())`. Response assertion unchanged: 503 + `{ error: "voice STT unavailable", status: 503 }`.
    - Slash-transform tests (any test with `hostId` + wake-word transcript): swap `transcribeSendMock` for `transcribeNovaSonicMock`. Transformed-text assertion unchanged.
    - Any test asserting `startStreamCmdCtor` / `transcribeSendMock` internals is DELETED — those are transcribe-streaming-specific.

    Phase 100 describe block (lines 750-903): DELETE the entire describe block. Residual coverage lives in the rewritten handleTranscribe tests above:
    - T1 (fast-path routing) — obsolete; no chunked path exists.
    - T2 (chunked routing) — obsolete.
    - T3 (chunked AccessDenied → 503) — covered by the single-path AccessDenied test.
    - T4 (chunked generic → 502) — covered by the single-path 502-on-generic-error test.
    - T5 (slash-transform on chunked) — covered by the single-path slash-transform test.
    - T6 (disk-bank on chunked) — covered by the single-path disk-bank test.

    Do NOT touch `handleSpeak` or `handleSpeakStream` tests — Polly tests unchanged.
  </behavior>

  <action>
    Per D-REWIRE + D-CUTOVER + D-FILTER + D-TESTS: rewrite `/home/ubuntu/skynet-sky/src/backend/database/routes/voice.test.ts` per `<behavior>` above.

    Execute the mock scaffolding surgery, imports swap, beforeEach swap, `fakeTranscriptStream` deletion, per-test rewrite, and Phase 100 describe-block deletion as specified.

    Test rename discipline: keep test names descriptive and reflect Nova Sonic. The parent describe becomes `describe("handleTranscribe (Amazon Nova Sonic on Bedrock)"...)`. Individual test names update FLAC/Transcribe references to Nova Sonic references only where the test name mentions the specific provider — pure behavior names (like "returns 400 when req.file is missing") stay unchanged.

    Do NOT introduce new test utilities. Do NOT modify `makeReq`, `makeRes`, `makeAccessDeniedError` helpers. Do NOT alter the fs, skill-catalog, or Polly SDK mock factories.

    After the rewrite, the file contains exactly ONE describe block for handleTranscribe (`describe("handleTranscribe (Amazon Nova Sonic on Bedrock)"...)`) plus the unchanged handleSpeak and handleSpeakStream blocks. All references to `transcribeSendMock`, `startStreamCmdCtor`, `transcribeClientCtor`, `transcribeBufferChunked`, `webmToFlac`, `fakeTranscriptStream`, `CHUNKED_THRESHOLD_BYTES`, `MediaEncoding`, `MediaSampleRateHertz`, and `Phase 100 chunked` are gone.
  </action>

  <verify>
    <automated>cd /home/ubuntu/skynet-sky &amp;&amp; grep -c "transcribeNovaSonic" src/backend/database/routes/voice.test.ts | awk '$1>=8' &amp;&amp; grep -cE "transcribeBuffer\b|transcribeBufferChunked|webmToFlac|transcribeSendMock|startStreamCmdCtor|CHUNKED_THRESHOLD_BYTES|Phase 100 chunked|MediaEncoding|MediaSampleRateHertz" src/backend/database/routes/voice.test.ts | grep -q '^0$' &amp;&amp; npx vitest run src/backend/database/routes/voice.test.ts --reporter=default</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "transcribeNovaSonic" /home/ubuntu/skynet-sky/src/backend/database/routes/voice.test.ts` returns AT LEAST 8 (hoisted mock name + module mock factory + import + beforeEach reset + test bodies).
    - `grep -cE "\btranscribeBuffer\b" /home/ubuntu/skynet-sky/src/backend/database/routes/voice.test.ts` returns 0.
    - `grep -c "transcribeBufferChunked" /home/ubuntu/skynet-sky/src/backend/database/routes/voice.test.ts` returns 0.
    - `grep -c "webmToFlac" /home/ubuntu/skynet-sky/src/backend/database/routes/voice.test.ts` returns 0.
    - `grep -c "transcribeSendMock" /home/ubuntu/skynet-sky/src/backend/database/routes/voice.test.ts` returns 0.
    - `grep -c "startStreamCmdCtor" /home/ubuntu/skynet-sky/src/backend/database/routes/voice.test.ts` returns 0.
    - `grep -c "CHUNKED_THRESHOLD_BYTES" /home/ubuntu/skynet-sky/src/backend/database/routes/voice.test.ts` returns 0.
    - `grep -c "MediaEncoding\|MediaSampleRateHertz" /home/ubuntu/skynet-sky/src/backend/database/routes/voice.test.ts` returns 0.
    - `grep -c "Phase 100 chunked" /home/ubuntu/skynet-sky/src/backend/database/routes/voice.test.ts` returns 0.
    - `grep -c "fakeTranscriptStream" /home/ubuntu/skynet-sky/src/backend/database/routes/voice.test.ts` returns 0 (dead helper deleted).
    - `grep -c "webmToPcm16k" /home/ubuntu/skynet-sky/src/backend/database/routes/voice.test.ts` returns AT LEAST 4 (mock factory + import + beforeEach reset + at least one test body).
    - `grep -c "@aws-sdk/client-transcribe-streaming" /home/ubuntu/skynet-sky/src/backend/database/routes/voice.test.ts` returns 0 (vi.mock factory deleted).
    - `grep -c "nova-sonic-adapter" /home/ubuntu/skynet-sky/src/backend/database/routes/voice.test.ts` returns AT LEAST 2 (vi.mock factory + import).
    - `grep -c "AccessDenied\|voice STT unavailable" /home/ubuntu/skynet-sky/src/backend/database/routes/voice.test.ts` returns AT LEAST 2 (AccessDenied → 503 preservation test present).
    - `grep -c "slash-transform\|WAKE_WORD_REGEX\|/gsd status\|applyServerSlashTransform\|slashCommandTransform\|hostId" /home/ubuntu/skynet-sky/src/backend/database/routes/voice.test.ts` returns AT LEAST 1 (slash-transform preservation test present).
    - `grep -cE "disk-bank|mkdir|writeFile" /home/ubuntu/skynet-sky/src/backend/database/routes/voice.test.ts` returns AT LEAST 2 (disk-bank preservation test present).
    - `npx vitest run src/backend/database/routes/voice.test.ts` exits 0. All tests (Nova Sonic block + Polly blocks) pass green.
    - Scoped test gate: `npx vitest run --related src/backend/database/routes/voice.ts src/backend/voice/nova-sonic-adapter.ts src/backend/voice/audio-transcode.ts` exits 0.
  </acceptance_criteria>

  <done>voice.test.ts rewritten: mocks transcribeNovaSonic + webmToPcm16k; Phase 100 chunked-path tests deleted; preserved-coverage tests (200-happy-path, missing-file, disk-bank ordering, webmToPcm16k throw → 502, AccessDenied → 503, slash-transform) ported; scoped vitest green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser client → /voice/transcribe | Authenticated multipart upload. Multer 25 MB `fileSize` cap and Phase 103 multipart-origin guard preserved. authenticateJWT gate runs BEFORE multer parses the body (T-16-04). |
| handleTranscribe → transcribeNovaSonic | Handler passes an LPCM buffer produced by webmToPcm16k. Nova Sonic adapter throws SDK errors unchanged; handler owns AccessDenied → 503 classification. |
| handleTranscribe → disk-bank | Fire-and-forget write of the ORIGINAL multipart bytes to /app/stt-recordings/ (or STT_RECORDINGS_DIR env override). Preserved verbatim from Phase 98 Plan 06 — audio-recoverability guarantee across transcode failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-03-01 | Information disclosure | AccessDenied error leaked as 5xx with raw SDK detail | mitigate | Preserved catch-block classifier: `isAwsAccessDenied(err)` returns clean `{ error: "voice STT unavailable", status: 503 }`, no error text or stack surfaced. Test asserted. |
| T-109-03-02 | Information disclosure | Generic AWS errors (network, throttling, ValidationException from Bedrock) leaking wire details | mitigate | Preserved catch-block returns fixed `{ error: "STT error", status: 502 }` — no err.message interpolation. Structured operation-tagged log at error level captures details server-side only. |
| T-109-03-03 | Denial of service | Oversized WebM buffer bloating memory | mitigate | Multer 25 MB `fileSize` cap (line 57 of voice.ts) preserved. ffmpeg's memory profile on a 25 MB WebM decode is bounded. No new guard needed. |
| T-109-03-04 | Tampering | AccessDenied surface classification changes because Nova Sonic emits a different exception name | verify | Nova Sonic on Bedrock surfaces `AccessDeniedException` (SDK v3 canonical name) with `$metadata.httpStatusCode: 403`. `isAwsAccessDenied` in `aws-errors.ts` matches on both — no adapter change needed. Test asserts via `makeAccessDeniedError()` helper. |
| T-109-03-05 | Repudiation | Transcript loss because disk-bank runs AFTER transcode throws | mitigate | Disk-bank fire-and-forget write is INITIATED before transcode call (line 139 of current voice.ts). Preserved verbatim. mkdir-before-transcode invocation ordering asserted in the disk-bank test (D-14 gate). |
| T-109-03-06 | Elevation of privilege | Bridge service tokens accessing STT (transcribe) | inherit | The `POST /transcribe` route does NOT apply `rejectBridgeServiceOnSpeak` (bridge tokens are permitted for transcribe per Phase 98 design). This plan does not change route middleware. |
| T-109-03-07 | Information disclosure | Log lines carrying transcript content | accept | Log emits `textLen=<n>` only, never the transcript body. Preserved from Phase 98. |
| T-109-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan (deps land in Plan 01). N/A. |
</threat_model>

<verification>
- `npx vitest run src/backend/database/routes/voice.test.ts` — GREEN.
- Scoped test gate: `npx vitest run --related src/backend/database/routes/voice.ts src/backend/voice/nova-sonic-adapter.ts src/backend/voice/audio-transcode.ts` — GREEN.
- `npx tsc --noEmit -p tsconfig.json` — succeeds.
- `grep -c "transcribeNovaSonic" src/backend/database/routes/voice.ts` — ≥ 2.
- `grep -cE "transcribeBuffer\b|transcribeBufferChunked|webmToFlac|CHUNKED_THRESHOLD_BYTES" src/backend/database/routes/voice.ts` — returns 0.
- `grep -cE "MediaEncoding|MediaSampleRateHertz" src/backend/database/routes/voice.ts` — returns 0.
- `grep -cE "'flac'|'ogg-opus'|\"flac\"|\"ogg-opus\"" src/backend/database/routes/voice.ts` — returns 0 (FLAC/OGG passthrough branches deleted per D-REWIRE).
</verification>

<success_criteria>
- voice.ts handleTranscribe rewired to call `transcribeNovaSonic(pcmBuf)` on a single-path transcode from `webmToPcm16k`.
- CHUNKED_THRESHOLD_BYTES deleted; ternary branch deleted; FLAC/OGG passthrough deleted; mediaEncoding return-field deleted.
- Disk-bank / slash-transform / AccessDenied → 503 / generic → 502 surfaces preserved verbatim.
- voice.test.ts rewritten to mock the new adapter facade; Phase 100 chunked-path tests deleted; behavioral coverage preserved via ported single-path tests.
- Scoped vitest green.
- transcribe-adapter.ts + transcribe-orchestrator.ts still exist on disk (unimported) — Plan 04 deletes them.
</success_criteria>

<output>
Create `.planning/phases/109-stt-provider-swap-amazon-transcribe-to-amazon-nova-sonic-on-/109-03-SUMMARY.md` when done.
</output>
