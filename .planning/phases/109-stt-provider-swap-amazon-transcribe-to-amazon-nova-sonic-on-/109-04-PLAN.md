---
phase: 109-stt-provider-swap-amazon-transcribe-to-amazon-nova-sonic-on
plan: 04
type: execute
wave: 3
depends_on:
  - 109-03
files_modified:
  - src/backend/voice/transcribe-adapter.ts
  - src/backend/voice/transcribe-adapter.test.ts
  - src/backend/voice/transcribe-adapter.integration.test.ts
  - src/backend/voice/transcribe-orchestrator.ts
  - src/backend/voice/transcribe-orchestrator.test.ts
  - src/backend/voice/audio-chunker.ts
  - src/backend/voice/audio-chunker.test.ts
  - src/backend/voice/word-stitcher.ts
  - src/backend/voice/word-stitcher.test.ts
  - src/backend/voice/semaphore.ts
  - src/backend/voice/semaphore.test.ts
autonomous: true
requirements:
  - D-DELETE
  - D-REWIRE

must_haves:
  truths:
    - "The Phase 100 chunked-parallel STT subsystem is DELETED IN FULL: `transcribe-orchestrator.ts` + `.test.ts`, `audio-chunker.ts` + `.test.ts`, `word-stitcher.ts` + `.test.ts`, `semaphore.ts` + `.test.ts` — 8 files total, ~2196 lines of production + test code."
    - "The Amazon Transcribe streaming adapter is DELETED: `transcribe-adapter.ts` + `.test.ts` + `.integration.test.ts` — 3 files total, ~600 lines."
    - "Combined phase-109 net delete: 11 files, ~2800 lines of TypeScript retired."
    - "No remaining tree references to the deleted modules: `grep -rn 'transcribe-orchestrator\\|transcribe-adapter\\|audio-chunker\\|word-stitcher\\|voice/semaphore\\|transcribeBuffer\\|transcribeBufferChunked\\|transcribeBufferWithItems' src/` returns ZERO non-comment matches after this plan lands."
    - "`chunk-and-stitch.ts` REMAINS in place — it is used by `polly-adapter.ts` for TTS packing (verified in RECON § 2). It is NOT part of this deletion set."
    - "`src/backend/ssh/host-semaphore-registry.ts` REMAINS in place — it is a DIFFERENT semaphore from the voice-scoped one being deleted, used elsewhere in the codebase (SSH connection concurrency). Only `src/backend/voice/semaphore.ts` is deleted."
    - "`runFfmpeg`, `webmToOggOpus`, `webmToFlac`, `webmToPcm16k`, and the `SILENCE_TRIM_*` exports in `audio-transcode.ts` are UNTOUCHED. `runFfmpeg` is retained even though `audio-chunker.ts::sliceFlac` (its only remaining consumer besides the webm-* helpers) is deleted, because `webmToOggOpus`/`webmToFlac`/`webmToPcm16k` all continue to use it. The `webmToOggOpus` and `webmToFlac` helpers are retained even though voice.ts no longer imports them (D-REWIRE locks their voice.ts consumer's deletion but does NOT lock the helpers' deletion — they're harmless dead exports that can be pruned in a future cleanup phase without cost)."
    - "`npx tsc --noEmit -p /home/ubuntu/skynet-sky/tsconfig.json` succeeds after all deletions — no stale imports leak through the build."
    - "`npm run build:backend && npm run build` (full backend + frontend production build) exits 0."
    - "Scoped vitest gate covering the remaining voice code passes: `npx vitest run src/backend/voice/ src/backend/database/routes/voice.test.ts` exits 0."
  artifacts:
    - path: "src/backend/voice/transcribe-adapter.ts"
      provides: "DELETED — Amazon Transcribe streaming adapter (both transcribeBuffer and transcribeBufferWithItems exports). File must not exist after this plan."
      contains: "MUST NOT EXIST"
    - path: "src/backend/voice/transcribe-orchestrator.ts"
      provides: "DELETED — Phase 100 chunked-parallel orchestrator. Must not exist."
      contains: "MUST NOT EXIST"
    - path: "src/backend/voice/audio-chunker.ts"
      provides: "DELETED — silence-boundary detection + sliceFlac. Must not exist."
      contains: "MUST NOT EXIST"
    - path: "src/backend/voice/word-stitcher.ts"
      provides: "DELETED — word-level item stitcher for Phase 100. Must not exist."
      contains: "MUST NOT EXIST"
    - path: "src/backend/voice/semaphore.ts"
      provides: "DELETED — voice-scoped semaphore for Phase 100 concurrency cap. Must not exist. NOTE: src/backend/ssh/host-semaphore-registry.ts is a different file and IS PRESERVED."
      contains: "MUST NOT EXIST"
  key_links:
    - from: "delete transcribe-orchestrator.ts"
      to: "voice.ts no longer imports transcribeBufferChunked (Plan 03 deleted the import)"
      via: "dependency severance verified by grep"
      pattern: "transcribeBufferChunked"
    - from: "delete transcribe-adapter.ts"
      to: "voice.ts no longer imports transcribeBuffer (Plan 03 deleted the import) AND transcribe-orchestrator.ts (this plan's other deletion) no longer imports transcribeBufferWithItems"
      via: "no in-tree consumers remain"
      pattern: "transcribeBuffer(Chunked|WithItems)?"
---

<objective>
Retire the two dead STT subsystems left in the tree after Plan 03's voice.ts rewire: (1) the Phase 100 chunked-parallel orchestrator (`transcribe-orchestrator.ts` + `audio-chunker.ts` + `word-stitcher.ts` + `semaphore.ts` + their .test.ts siblings — 8 files) and (2) the Amazon Transcribe streaming adapter (`transcribe-adapter.ts` + `transcribe-adapter.test.ts` + `transcribe-adapter.integration.test.ts` — 3 files). Verify no stale imports remain via grep + typecheck + full backend build.

Depends on Plan 03 (voice.ts must no longer import from any of these files). Wave 3 — runs serially after Plan 03 GREENs its scoped vitest gate.

Purpose: complete the cutover by removing the code paths that voice.ts no longer references. Total net delete lands at ~2800 lines (2196 lines of Phase 100 subsystem per CONTEXT.md § Deletions + ~600 lines of Transcribe adapter + tests). No new code introduced in this plan.

Output: 11 files removed from disk. TypeScript compiles clean. Backend + frontend production builds succeed. Scoped vitest run on the voice subsystem is green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/109-stt-provider-swap-amazon-transcribe-to-amazon-nova-sonic-on-/109-CONTEXT.md
@src/backend/database/routes/voice.ts
@src/backend/voice/audio-transcode.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the Phase 100 chunked-parallel STT subsystem (8 files) and the Amazon Transcribe streaming adapter (3 files)</name>
  <files>src/backend/voice/transcribe-adapter.ts, src/backend/voice/transcribe-adapter.test.ts, src/backend/voice/transcribe-adapter.integration.test.ts, src/backend/voice/transcribe-orchestrator.ts, src/backend/voice/transcribe-orchestrator.test.ts, src/backend/voice/audio-chunker.ts, src/backend/voice/audio-chunker.test.ts, src/backend/voice/word-stitcher.ts, src/backend/voice/word-stitcher.test.ts, src/backend/voice/semaphore.ts, src/backend/voice/semaphore.test.ts</files>

  <read_first>
    - /home/ubuntu/skynet-sky/src/backend/database/routes/voice.ts (confirm Plan 03 has landed — the file no longer imports from any of the target files)
    - /home/ubuntu/skynet-sky/src/backend/voice/transcribe-orchestrator.ts:36-46 (confirms the chunked-orchestrator imports `transcribeBufferWithItems` from transcribe-adapter and imports from audio-chunker + word-stitcher — everything in this deletion set forms a closed dependency subgraph)
    - /home/ubuntu/skynet-sky/src/backend/voice/polly-adapter.ts (confirms polly-adapter does NOT import from any deletion target — it uses `chunk-and-stitch.ts` which is NOT in this deletion set)
    - /home/ubuntu/skynet-sky/src/backend/voice/chunk-and-stitch.ts (confirms this file stays — used by polly-adapter for TTS text packing)
    - /home/ubuntu/skynet-sky/src/backend/ssh/host-semaphore-registry.ts (confirms this file is DIFFERENT from voice/semaphore.ts and is PRESERVED — voice/semaphore.ts is a per-invocation concurrency cap for the voice orchestrator, host-semaphore-registry is a per-SSH-host serializer used all over the SSH subsystem)
    - .planning/phases/109-stt-provider-swap-amazon-transcribe-to-amazon-nova-sonic-on-/109-CONTEXT.md § Deletions (D-DELETE: locked file list)
    - /home/ubuntu/fleet/roles/box-maintainer/bounties/stt-nova-sonic-migration/RECON.md § 2 (dependency verification — audio-chunker/word-stitcher/voice-semaphore are ONLY imported by transcribe-orchestrator; transcribeBufferWithItems is ONLY used by transcribe-orchestrator; chunk-and-stitch.ts is used by polly-adapter and stays)
  </read_first>

  <behavior>
    Perform the deletion in a single mechanical pass:

    Files to remove (verify each exists before delete, but treat non-existence as a soft-warn-and-continue since Plan 03 may have left the tree in various intermediate states):
    - `src/backend/voice/transcribe-adapter.ts`
    - `src/backend/voice/transcribe-adapter.test.ts`
    - `src/backend/voice/transcribe-adapter.integration.test.ts`
    - `src/backend/voice/transcribe-orchestrator.ts`
    - `src/backend/voice/transcribe-orchestrator.test.ts`
    - `src/backend/voice/audio-chunker.ts`
    - `src/backend/voice/audio-chunker.test.ts`
    - `src/backend/voice/word-stitcher.ts`
    - `src/backend/voice/word-stitcher.test.ts`
    - `src/backend/voice/semaphore.ts`
    - `src/backend/voice/semaphore.test.ts`

    Files that MUST NOT be deleted (guard against overreach):
    - `src/backend/voice/chunk-and-stitch.ts` — used by polly-adapter (TTS text-packing).
    - `src/backend/voice/chunk-and-stitch.test.ts` — same reason.
    - `src/backend/ssh/host-semaphore-registry.ts` — different semaphore, used broadly in SSH subsystem.
    - `src/backend/voice/audio-transcode.ts` — retains `runFfmpeg` + `webmToOggOpus` + `webmToFlac` + `webmToPcm16k` + `SILENCE_TRIM_*` exports. `webmToPcm16k` is the sole active consumer (via voice.ts); `webmToOggOpus` + `webmToFlac` are now dead exports but harmless.
    - `src/backend/voice/audio-transcode.test.ts` — retains its coverage of the surviving helpers.
    - `src/backend/voice/aws-errors.ts` — reused by both handleTranscribe (Nova Sonic AccessDenied) and handleSpeak/handleSpeakStream (Polly AccessDenied). Untouched.
    - `src/backend/voice/nova-sonic-adapter.ts` + `.test.ts` — the whole point of this phase; untouched.
    - `src/backend/voice/polly-adapter.ts` + tests — Polly TTS path; untouched.
    - `src/backend/voice/riff-header-builder.ts` + tests — Polly path; untouched.
    - `src/backend/voice/polly-voice-catalog.ts` + tests — Polly path; untouched.
    - `src/backend/voice/skill-catalog.ts` + tests — slash-transform helper; untouched.
    - `src/backend/voice/slashCommandTransform.ts` + tests — slash-transform helper; untouched.
    - `src/backend/voice/voice-migration.ts` + tests — untouched.

    Stale-reference verification (the phase's D-DELETE truth):
    - `grep -rn 'transcribe-orchestrator\|transcribe-adapter\|audio-chunker\|word-stitcher\|voice/semaphore\|transcribeBuffer\|transcribeBufferChunked\|transcribeBufferWithItems' src/` should return zero non-comment matches. Any residual reference is a bug in Plan 03's rewire and must be fixed here.

    Build gates (each must pass after deletion):
    1. `npx tsc --noEmit -p /home/ubuntu/skynet-sky/tsconfig.json` — no compilation errors.
    2. `npm run build:backend` — backend production build succeeds (compiles TS + resolves imports without dangling references).
    3. `npm run build` — frontend production build succeeds (client-side voice code was already invariant per CONTEXT.md § Out of scope, so this is a safety-net check).

    Scoped test gate:
    - `npx vitest run src/backend/voice/ src/backend/database/routes/voice.test.ts` — all remaining voice-subsystem tests green (nova-sonic-adapter, audio-transcode, aws-errors, chunk-and-stitch, polly-adapter, polly-voice-catalog, riff-header-builder, skill-catalog, slashCommandTransform, voice-migration, voice.test.ts).

    Note on `package-lock.json` and `node_modules`: Plan 01 already removed `@aws-sdk/client-transcribe-streaming` from package.json + regenerated the lockfile. This plan does NOT touch package.json. If `node_modules/@aws-sdk/client-transcribe-streaming` still exists on disk locally, that is expected — it will be pruned on the next `npm install --production` inside the Docker build. Do NOT run `npm prune` here.

    Note on git: use `git rm` if the tree is a git repo, else plain `rm`. Since the working tree is `/home/ubuntu/skynet-sky` (not a git repo in this workspace per env), plain `rm` is fine.
  </behavior>

  <action>
    Per D-DELETE + D-REWIRE: delete the 11 files listed under `<files>` above using `rm -f` (or `git rm -f` if the tree is under git tracking — check with `test -d /home/ubuntu/skynet-sky/.git && echo yes`; use `git rm` when yes, plain `rm` otherwise).

    Execute the deletions:
    ```
    rm -f /home/ubuntu/skynet-sky/src/backend/voice/transcribe-adapter.ts \
          /home/ubuntu/skynet-sky/src/backend/voice/transcribe-adapter.test.ts \
          /home/ubuntu/skynet-sky/src/backend/voice/transcribe-adapter.integration.test.ts \
          /home/ubuntu/skynet-sky/src/backend/voice/transcribe-orchestrator.ts \
          /home/ubuntu/skynet-sky/src/backend/voice/transcribe-orchestrator.test.ts \
          /home/ubuntu/skynet-sky/src/backend/voice/audio-chunker.ts \
          /home/ubuntu/skynet-sky/src/backend/voice/audio-chunker.test.ts \
          /home/ubuntu/skynet-sky/src/backend/voice/word-stitcher.ts \
          /home/ubuntu/skynet-sky/src/backend/voice/word-stitcher.test.ts \
          /home/ubuntu/skynet-sky/src/backend/voice/semaphore.ts \
          /home/ubuntu/skynet-sky/src/backend/voice/semaphore.test.ts
    ```

    After deletion, run the stale-reference gate:
    ```
    grep -rn 'transcribe-orchestrator\|transcribe-adapter\|audio-chunker\|word-stitcher\|voice/semaphore\|transcribeBuffer\|transcribeBufferChunked\|transcribeBufferWithItems' /home/ubuntu/skynet-sky/src/ | grep -vE '^[^:]+:[^:]+:\s*(//|\*| \* |# )'
    ```
    Expected: empty output. Any match indicates a stale import that Plan 03 missed. If a match appears in a comment/docblock only, remove the comment reference (grep filter above catches leading `//` and `/*` lines but does not handle all comment shapes — inspect any match manually).

    Then run the build + test gates:
    ```
    cd /home/ubuntu/skynet-sky
    npx tsc --noEmit -p tsconfig.json
    npm run build:backend
    npm run build
    npx vitest run src/backend/voice/ src/backend/database/routes/voice.test.ts
    ```

    Each gate must exit 0. If any fails, do NOT force-through — the failure indicates a residual reference or a broken import that must be fixed before this plan can complete.

    Do NOT delete `chunk-and-stitch.ts`, `chunk-and-stitch.test.ts`, `audio-transcode.ts`, `audio-transcode.test.ts`, `aws-errors.ts`, `aws-errors.test.ts`, `nova-sonic-adapter.ts`, `nova-sonic-adapter.test.ts`, `polly-adapter.ts`, `polly-adapter.test.ts`, `polly-adapter.integration.test.ts`, `polly-voice-catalog.ts`, `polly-voice-catalog.test.ts`, `riff-header-builder.ts`, `riff-header-builder.test.ts`, `skill-catalog.ts`, `skill-catalog.test.ts`, `slashCommandTransform.ts`, `slashCommandTransform.test.ts`, `voice-migration.ts`, `voice-migration.test.ts`, or `/home/ubuntu/skynet-sky/src/backend/ssh/host-semaphore-registry.ts`.

    Do NOT touch package.json or package-lock.json — Plan 01 already handled the dep swap. Do NOT run `npm install`, `npm prune`, or any dependency mutation.

    Do NOT introduce new files, new comments, or new "TODO" markers.

    Do NOT restore any deleted file if the tests reveal a missing reference — instead, patch the referencing file to remove the reference. The deletion set is locked by D-DELETE.
  </action>

  <verify>
    <automated>cd /home/ubuntu/skynet-sky &amp;&amp; test ! -e src/backend/voice/transcribe-adapter.ts &amp;&amp; test ! -e src/backend/voice/transcribe-orchestrator.ts &amp;&amp; test ! -e src/backend/voice/audio-chunker.ts &amp;&amp; test ! -e src/backend/voice/word-stitcher.ts &amp;&amp; test ! -e src/backend/voice/semaphore.ts &amp;&amp; STALE=$(grep -rnE 'transcribe-orchestrator|transcribe-adapter|audio-chunker|word-stitcher|voice/semaphore|transcribeBuffer|transcribeBufferChunked|transcribeBufferWithItems' src/ 2>/dev/null | grep -vE ':\s*(//|\*|#)' | wc -l) &amp;&amp; test "$STALE" = "0"</automated>
  </verify>

  <acceptance_criteria>
    - `test ! -e /home/ubuntu/skynet-sky/src/backend/voice/transcribe-adapter.ts` succeeds.
    - `test ! -e /home/ubuntu/skynet-sky/src/backend/voice/transcribe-adapter.test.ts` succeeds.
    - `test ! -e /home/ubuntu/skynet-sky/src/backend/voice/transcribe-adapter.integration.test.ts` succeeds.
    - `test ! -e /home/ubuntu/skynet-sky/src/backend/voice/transcribe-orchestrator.ts` succeeds.
    - `test ! -e /home/ubuntu/skynet-sky/src/backend/voice/transcribe-orchestrator.test.ts` succeeds.
    - `test ! -e /home/ubuntu/skynet-sky/src/backend/voice/audio-chunker.ts` succeeds.
    - `test ! -e /home/ubuntu/skynet-sky/src/backend/voice/audio-chunker.test.ts` succeeds.
    - `test ! -e /home/ubuntu/skynet-sky/src/backend/voice/word-stitcher.ts` succeeds.
    - `test ! -e /home/ubuntu/skynet-sky/src/backend/voice/word-stitcher.test.ts` succeeds.
    - `test ! -e /home/ubuntu/skynet-sky/src/backend/voice/semaphore.ts` succeeds.
    - `test ! -e /home/ubuntu/skynet-sky/src/backend/voice/semaphore.test.ts` succeeds.
    - **Guardrails (files that MUST still exist):** `test -e /home/ubuntu/skynet-sky/src/backend/voice/chunk-and-stitch.ts` AND `test -e /home/ubuntu/skynet-sky/src/backend/ssh/host-semaphore-registry.ts` AND `test -e /home/ubuntu/skynet-sky/src/backend/voice/audio-transcode.ts` AND `test -e /home/ubuntu/skynet-sky/src/backend/voice/nova-sonic-adapter.ts` AND `test -e /home/ubuntu/skynet-sky/src/backend/voice/aws-errors.ts` AND `test -e /home/ubuntu/skynet-sky/src/backend/voice/polly-adapter.ts` — all succeed.
    - Stale-import check: `grep -rnE 'transcribe-orchestrator|transcribe-adapter|audio-chunker|word-stitcher|voice/semaphore|transcribeBuffer|transcribeBufferChunked|transcribeBufferWithItems' /home/ubuntu/skynet-sky/src/ | grep -vE ':\s*(//|\*|#)'` returns EMPTY output (zero non-comment matches).
    - `npx tsc --noEmit -p /home/ubuntu/skynet-sky/tsconfig.json` exits 0.
    - `cd /home/ubuntu/skynet-sky && npm run build:backend` exits 0.
    - `cd /home/ubuntu/skynet-sky && npm run build` exits 0 (frontend build unaffected — client-side voice code was invariant per CONTEXT.md § Out of scope).
    - `cd /home/ubuntu/skynet-sky && npx vitest run src/backend/voice/ src/backend/database/routes/voice.test.ts` exits 0.
    - Line-count sanity: `git status --short 2>/dev/null | grep '^D' | wc -l` returns AT LEAST 11 (only meaningful if the tree is under git; otherwise skip). If git-tracked, alternatively run `git diff --stat HEAD 2>/dev/null | tail -1` and confirm the deletion count reports 11 files with ~2196 deletions from the Phase 100 files + ~600 deletions from the transcribe-adapter files = ≥ 2000 combined deletions.
  </acceptance_criteria>

  <done>All 11 target files deleted; no stale imports remain; typecheck + full build + scoped vitest all green; guarded-preserve files (chunk-and-stitch, host-semaphore-registry, audio-transcode, nova-sonic-adapter, aws-errors, polly-adapter) confirmed still present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev filesystem → repo working tree | This plan performs unlink operations on tracked source files. No runtime trust boundary crossed — this is compile-time / test-time only. |
| Build system → deleted-file references | If any file the build references is deleted without updating the reference, `tsc --noEmit` and `npm run build` fail. This is the enforcement mechanism. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-04-01 | Denial of service | Broken production build after deletion | mitigate | Build + typecheck gates run inline after the deletion. If any gate fails, the plan does NOT complete — the executor fixes the residual reference before proceeding. |
| T-109-04-02 | Tampering | Wrong file deleted (over-broad rm glob) | mitigate | Explicit per-file `rm -f <absolute-path>` list — no wildcards, no glob patterns, no directory-scoped removals. Guardrail acceptance criteria list the files that MUST still exist (chunk-and-stitch, host-semaphore-registry, audio-transcode, nova-sonic-adapter, aws-errors, polly-adapter). |
| T-109-04-03 | Denial of service | Stray import in a non-voice module (unlikely — RECON § 2 verified) | mitigate | Post-delete grep gate across ALL of `src/` (not just `src/backend/voice/`) catches any stray import in adjacent subsystems. Non-comment filter is applied. |
| T-109-04-04 | Repudiation | Deleted files still reachable via bundler due to stale build artifacts | accept | Docker production build is a fresh from-scratch compilation from `src/` — no stale artifacts survive `docker build`. The dev-time `npm run build:backend` similarly starts from `src/` each invocation. |
| T-109-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan. The npm dep swap was locked in Plan 01. No package.json or lockfile mutations here. |
</threat_model>

<verification>
- File-existence check: 11 target files gone; 6 guardrail files present.
- Stale-import grep: zero non-comment matches across `src/`.
- `npx tsc --noEmit -p tsconfig.json` — GREEN.
- `npm run build:backend` — GREEN.
- `npm run build` — GREEN.
- `npx vitest run src/backend/voice/ src/backend/database/routes/voice.test.ts` — GREEN.
</verification>

<success_criteria>
- 11 dead files (Phase 100 subsystem + Transcribe adapter + their tests) removed.
- No stale imports; TypeScript compiles clean.
- Backend + frontend production builds pass.
- Voice-subsystem scoped vitest passes.
- Guardrail files (chunk-and-stitch, host-semaphore-registry, audio-transcode, nova-sonic-adapter, aws-errors, polly-adapter, and all Polly/riff/slash/skill-catalog/voice-migration modules) untouched.
- Ready for the phase's ship-gate (full-suite vitest + Playwright smoke run orchestrator-side per D-SHIP, immediately before docker build).
</success_criteria>

<output>
Create `.planning/phases/109-stt-provider-swap-amazon-transcribe-to-amazon-nova-sonic-on-/109-04-SUMMARY.md` when done.
</output>
