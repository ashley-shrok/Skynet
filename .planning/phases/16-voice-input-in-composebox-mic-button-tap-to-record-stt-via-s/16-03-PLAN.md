---
phase: 16-voice-input-in-composebox-mic-button-tap-to-record-stt-via-s
plan: 03
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - src/ui/features/pretty-view/ComposeBox.tsx
  - src/ui/features/pretty-view/ComposeBox.voice.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "ComposeBox imports useVoiceRecording + MicButton + RecordingControls from local pretty-view files"
    - "When `voice.state === 'idle'`, the send-button slot renders one of: the existing send/X-morph button (when text is non-empty OR asideActive) OR a new MicButton (when text is empty AND not asideActive) — planner's precise rule below"
    - "When `voice.state === 'recording'`, the send-button slot renders RecordingControls instead of the send button — the mic-cancel-append-send affordances OWN the slot"
    - "When `voice.state === 'transcribing'`, the send-button slot renders the existing send button in a disabled state (so the user cannot double-fire); the RecordingControls are hidden"
    - "voice.endAppend passes the current `text` state to the hook; on resolve, ComposeBox setText(result.glued) to update the textarea"
    - "voice.endSend passes the current `text` state to the hook; on resolve, ComposeBox setText(result.glued) THEN calls the SAME handleSend() function that the existing send button uses — do NOT invent a parallel send path (D-16-05)"
    - "voice.errorMessage flows into the ComposeBox `errorMessage` state OR a new `voiceError` state (planner decides) — surfaces to the user like other compose errors"
    - "The existing ComposeBox test suite (ComposeBox.test.tsx + ComposeBox.aside-morph.test.tsx + ComposeBox.aside-props.test.tsx) still passes with zero test edits required — the mic button only appears when text is empty, so existing typed-send tests are unaffected"
    - "Integration test verifies the voice flow: render ComposeBox with an idle-empty-text state, assert MicButton is present, mock navigator.mediaDevices.getUserMedia and fetch, tap MicButton, assert state transitions, tap the send action in RecordingControls, assert onSend was called with the transcribed+glued payload"
  artifacts:
    - path: "src/ui/features/pretty-view/ComposeBox.tsx"
      provides: "ComposeBox now hosts the mic button in idle-empty state and RecordingControls in recording state"
      contains: "useVoiceRecording"
    - path: "src/ui/features/pretty-view/ComposeBox.voice.test.tsx"
      provides: "vitest integration test for the voice flow inside ComposeBox"
      contains: "describe"
  key_links:
    - from: "src/ui/features/pretty-view/ComposeBox.tsx"
      to: "src/ui/features/pretty-view/useVoiceRecording.ts"
      via: "import { useVoiceRecording }"
      pattern: "useVoiceRecording"
    - from: "src/ui/features/pretty-view/ComposeBox.tsx"
      to: "src/ui/features/pretty-view/MicButton.tsx"
      via: "import { MicButton }"
      pattern: "MicButton"
    - from: "src/ui/features/pretty-view/ComposeBox.tsx"
      to: "src/ui/features/pretty-view/RecordingControls.tsx"
      via: "import { RecordingControls }"
      pattern: "RecordingControls"
    - from: "voice.endSend resolve handler in ComposeBox.tsx"
      to: "handleSend() (existing function)"
      via: "direct call after setText(glued)"
      pattern: "handleSend\\(\\)"
---

<objective>
Wire the Plan 02 primitives (useVoiceRecording hook + MicButton + RecordingControls) into ComposeBox.tsx so the send-button slot shows the mic button when the textarea is empty, swaps to the three-button recording controls while recording, and appends/sends the transcript when the user taps append or send. All voice-triggered sends go through the SAME existing `handleSend()` function so the D-50 newline collapse, COMPOSE-04 hard-lock (no optimistic display), and patch #57 draft persistence all apply — no parallel send paths.

Purpose: The mic button lives INSIDE the textarea slot (same slot as the current send button per D-16-01) — this is the forward-compatible design for the queued `message-queue-in-pretty-view` bounty where each textarea will host its own controls. The three-button recording state IS the recording indicator; no timer, no waveform, no level meter (per D-16-06, Ashley explicitly declined). Tests must not require edits to the existing ComposeBox test suite — the new mic-in-idle-empty behavior is additive.
Output: ComposeBox.tsx has voice wired; a new focused test file exercises the voice flow; existing tests remain green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-voice-input-in-composebox-mic-button-tap-to-record-stt-via-s/16-CONTEXT.md
@.planning/phases/16-voice-input-in-composebox-mic-button-tap-to-record-stt-via-s/16-02-SUMMARY.md
@src/ui/features/pretty-view/ComposeBox.tsx
@src/ui/features/pretty-view/ComposeBox.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire voice hook + MicButton + RecordingControls into ComposeBox with a scoped visibility rule</name>
  <files>src/ui/features/pretty-view/ComposeBox.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/ComposeBox.tsx lines 1450-1562 (the CURRENT send-button slot — the bare `<button>` at line 1503 that morphs between Send and X-for-Resume; the voice affordances swap INTO this same slot area)
    - src/ui/features/pretty-view/ComposeBox.tsx lines 700-765 (handleSend function — the LOAD-BEARING send path that voice.endSend must call after appending the transcript; do NOT duplicate its logic)
    - src/ui/features/pretty-view/ComposeBox.tsx lines 200-260 (props destructuring + text state setter — needed to know how `setText` is wired, and how `asideActive` + `sendDisabled` + `queueArmed` gate the current button)
    - src/ui/features/pretty-view/useVoiceRecording.ts (from Plan 02 — the hook's exported interface)
    - src/ui/features/pretty-view/MicButton.tsx (from Plan 02 — its props)
    - src/ui/features/pretty-view/RecordingControls.tsx (from Plan 02 — its props)
    - .planning/phases/16-voice-input-in-composebox-mic-button-tap-to-record-stt-via-s/16-CONTEXT.md § Frontend state machine (mirror the prototype's transitions)
  </read_first>
  <behavior>
    (This task lands the wiring — the tests below are for Task 2. Deferring behavior assertions to Task 2 keeps this a single-focused-change task within the tsc-clean discipline.)
  </behavior>
  <action>
    In `src/ui/features/pretty-view/ComposeBox.tsx`, add imports near the top (with the other local pretty-view imports): `import { useVoiceRecording } from "./useVoiceRecording";`, `import { MicButton } from "./MicButton";`, `import { RecordingControls } from "./RecordingControls";`. Inside the component body (near the other hook calls / state — the section around lines 230-320 with the other useState / useCallback lines), add `const voice = useVoiceRecording();`.

    **Slot-rendering rule (this is the CORE of the wiring — implement exactly):**

    Around the existing send-button `<button>` at line 1503-1554, wrap the rendering in a conditional structure:

    - Compute `const showMicButton = voice.state === "idle" && text.trim().length === 0 && !asideActive && !queueArmed && !hasAttachments;` — the mic replaces the send button ONLY when the textarea is empty (whitespace-only counts as empty), the aside-morph is NOT active, the queue is NOT armed, and no attachments are staged. In every other idle scenario the existing send button (or its X-for-Resume morph) still renders exactly as it does today.
    - Compute `const showRecordingControls = voice.state === "recording";` — while recording, the three-button controls OWN the slot.
    - Compute `const showTranscribingSend = voice.state === "transcribing";` — during the STT round-trip, the existing send button renders in a disabled state (prevents rapid-tap during the ~0.4-1.5s transcribe window).

    Render logic in the slot area (around line 1494 — the current bare `<button>` at 1503):

    - If `showRecordingControls`: render `<RecordingControls onCancel={handleVoiceCancel} onAppend={handleVoiceAppend} onSend={handleVoiceSend} />` INSTEAD of the send button. Do NOT render both.
    - Else if `showMicButton`: render `<MicButton onClick={voice.start} title="Record voice" />` INSTEAD of the send button. Do NOT render both.
    - Else: render the EXISTING `<button>` (send / X-for-Resume morph) exactly as today, but if `showTranscribingSend` add `disabled={true}` to its existing disabled expression (or set a new local `sendDisabledForTranscribe` var and OR it into the existing disabled prop) so the send button visually disables during transcribe.

    Define these handler callbacks inside the component (near handleSend, around line 720):

    ```
    // Prose description — NOT to be pasted verbatim as code:
    // handleVoiceCancel → calls voice.cancel(); no textarea change; state returns to idle.
    // handleVoiceAppend → const result = await voice.endAppend(text); if result: setText(result.glued); (textarea now holds original + transcript, waiting for user to send or edit); if !result: errorMessage flows via voice.errorMessage rendered below.
    // handleVoiceSend → const result = await voice.endSend(text); if result: setText(result.glued); then call handleSend() — this must call the SAME existing function; the setText + handleSend sequence uses React setState batching, so read the glued value directly and pass it to handleSend if handleSend depends on the local `text` state (which it does — check line 725 `const trimmed = text.trim()`). Because setText is async, handleSend called immediately after setText would still see the OLD text. Two options: (a) refactor handleSend to accept an optional override payload, or (b) inline the send logic post-setText via a useEffect that watches for a `pendingVoiceSend` flag. Recommended: OPTION A — add a small refactor to handleSend to accept `overridePayload?: string` and use it in place of the trimmed-text if present. This is a minimal, testable change that preserves all other handleSend behavior (attachment branching, D-50 newline collapse, COMPOSE-04 hard-lock). Document the refactor with an inline comment referring to this plan and D-16-05.
    ```

    Wire `voice.errorMessage` into the existing `errorMessage` display block at line 1557 — the block already renders `{errorMessage && <div>...</div>}`. Extend to render `voice.errorMessage` as well: either merge the two (e.g., `const displayError = errorMessage || voice.errorMessage;`) or add a second block. Planner picks whichever is cleaner.

    **What must NOT change:**
    - handleSend's existing behavior (attachment branching at line 736, D-50 newline collapse at 750, error handling at 759-763) — preserve verbatim aside from adding the optional overridePayload param.
    - The X-for-Resume morph when asideActive === true (lines 1503-1554) — this stays exactly as-is; the voice-visibility rule already excludes asideActive.
    - handleResetSend, handleThumbsUp, handleQueueArm, or any other button handler.
    - The Textarea component itself, its ref, its onChange, its onKeyDown — voice is a sibling affordance in the slot.
    - The Textarea's className, minRows, or the AttachmentChipStrip mount — those are Phase 9/05 concerns.
    - The compose-drafts autosave hook chain — the draft body still saves + hydrates as before.

    Add a top-of-file comment block near the other patch-narrative comments (e.g., around the "Compose-and-send box for the pretty view." header at line 12) documenting the Phase 16 wiring: the slot-visibility rule, the sync-getUserMedia constraint owned by the hook (not repeated here), and the handleSend overridePayload plumbing for voice.endSend.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx tsc --noEmit && npx vitest run src/ui/features/pretty-view/ComposeBox.test.tsx src/ui/features/pretty-view/ComposeBox.aside-morph.test.tsx src/ui/features/pretty-view/ComposeBox.aside-props.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useVoiceRecording" src/ui/features/pretty-view/ComposeBox.tsx` returns >= 2 (import + call site)
    - `grep -c "MicButton" src/ui/features/pretty-view/ComposeBox.tsx` returns >= 2 (import + JSX use)
    - `grep -c "RecordingControls" src/ui/features/pretty-view/ComposeBox.tsx` returns >= 2 (import + JSX use)
    - `grep -c "voice.state" src/ui/features/pretty-view/ComposeBox.tsx` returns >= 2 (idle-vs-recording branching)
    - `grep -c "voice.start\\|voice.cancel\\|voice.endAppend\\|voice.endSend" src/ui/features/pretty-view/ComposeBox.tsx` returns >= 4 (all four hook methods used)
    - `npx tsc --noEmit` exits 0
    - `npx vitest run src/ui/features/pretty-view/ComposeBox.test.tsx` exits 0 (existing typed-send tests still pass — proves the mic-visibility rule correctly excludes the non-empty-text case)
    - `npx vitest run src/ui/features/pretty-view/ComposeBox.aside-morph.test.tsx` exits 0 (aside-morph tests still pass — proves asideActive gate still works)
    - `npx vitest run src/ui/features/pretty-view/ComposeBox.aside-props.test.tsx` exits 0
  </acceptance_criteria>
  <done>
    ComposeBox mounts useVoiceRecording, renders MicButton in idle-empty state and RecordingControls while recording, wires the voice send action through the SAME handleSend function (with an overridePayload param), preserves the X-for-Resume aside morph, does not modify any existing tests. TypeScript clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test for the ComposeBox voice flow</name>
  <files>src/ui/features/pretty-view/ComposeBox.voice.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/ComposeBox.test.tsx (canonical ComposeBox test pattern — same vi.mock("@/api/compose-drafts-api") setup + `baseProps` helper + render/screen/fireEvent idioms are needed here; mirror this file's setup)
    - src/ui/features/pretty-view/ComposeBox.aside-morph.test.tsx (peer test file — matches the state-machine assertion style with fireEvent + async findBy queries)
    - src/ui/features/pretty-view/useVoiceRecording.test.ts (from Plan 02 — mock patterns for navigator.mediaDevices, MediaRecorder, and globalThis.fetch that this test will reuse)
    - src/ui/features/pretty-view/ComposeBox.tsx (post-Task 1 state — to know exactly which aria-labels + DOM structure the test queries)
  </read_first>
  <behavior>
    - Test 1: On mount with `text=""` and `asideActive=false` and no attachments, the MicButton is present in the DOM (query by `aria-label="Record voice"` or by role=button + accessible name).
    - Test 2: On mount with `text="hello"`, the MicButton is NOT present (the existing send button owns the slot). Existing send button still queryable by aria-label="Send".
    - Test 3: On mount with `asideActive=true`, the MicButton is NOT present (X-for-Resume owns the slot). Verified by asserting the button with aria-label="Resume" is present and no button with aria-label="Record voice" exists.
    - Test 4: Tapping the MicButton calls `navigator.mediaDevices.getUserMedia`. Verify by asserting the mock was called with `{audio: true}`.
    - Test 5: After getUserMedia + MediaRecorder resolve and state transitions to "recording", the DOM re-renders: MicButton is gone; three RecordingControls buttons are present (aria-labels "Cancel recording", "Append transcript", "Send transcript").
    - Test 6: Tapping the Cancel button in RecordingControls returns to idle-with-empty-text state (mic button visible again, no fetch call made to /voice/transcribe).
    - Test 7: Tapping the Append button in RecordingControls triggers a fetch to `/voice/transcribe` (verify via mocked fetch call), waits for the mocked STT response `{text: "hello world"}`, and updates the textarea state so it now contains "hello world" (verify by asserting the textarea's value). onSend from props was NOT called.
    - Test 8: Tapping the Send button in RecordingControls triggers the same fetch, and after the transcript resolves the onSend prop IS called with the transcribed payload "hello world" (or with the glued text if `text` was non-empty at start — but Test 8 uses empty starting text so it's just the transcript). This test proves the D-16-05 rule that voice-send routes through the SAME handleSend path.
    - Test 9: When the mocked fetch returns HTTP 500, state returns to idle and an error message is visible in the DOM (query for a text substring like "STT error" or the voice.errorMessage content).
  </behavior>
  <action>
    Create `src/ui/features/pretty-view/ComposeBox.voice.test.tsx`. Copy the vi.mock("@/api/compose-drafts-api") boilerplate from ComposeBox.test.tsx lines 1-25 verbatim (same reason: compose-drafts autosave fires on every keystroke, tests should not touch network). Copy the `baseProps` helper too.

    Mock `navigator.mediaDevices.getUserMedia` in `beforeEach` — the useVoiceRecording hook's tests already do this (Plan 02 Task 1), copy that mock pattern. Mock `window.MediaRecorder` as a minimal class stub with `.start()`, `.stop()`, `.mimeType`, `.ondataavailable`, `.onstop`. Mock `globalThis.fetch` via `vi.spyOn(globalThis, "fetch")` and set up per-test return values (default: `{ok: true, json: async () => ({text: "hello world"})}`).

    Write the nine behavior tests. For each state transition test, use `act()` + `await` after firing the interaction so React batching flushes. For the "recording controls appear after mic tap" test, remember: MediaRecorder is instantiated inside a `.then()` callback in the hook (the sync-getUserMedia pattern), so after `fireEvent.click(micButton)` you must await a microtask (either an explicit `await act(async () => {})` or a `findByRole` query that has built-in polling). Use `findByRole` or `findByLabelText` where possible — they auto-await the DOM update.

    For Test 8, the assertion is `expect(props.onSend).toHaveBeenCalledWith("hello world")` — this proves voice.endSend routed through the same handleSend that consumes the onSend prop. This is the load-bearing test for D-16-05.

    Do NOT add tests for iOS Safari specific behavior (unmockable in vitest). Manual UAT already covered iOS PWA in the prototype per CONTEXT.md.

    Do NOT modify the existing ComposeBox.test.tsx / ComposeBox.aside-morph.test.tsx / ComposeBox.aside-props.test.tsx — the new mic-visibility rule is additive.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx tsc --noEmit && npx vitest run src/ui/features/pretty-view/ComposeBox.voice.test.tsx src/ui/features/pretty-view/ComposeBox.test.tsx src/ui/features/pretty-view/ComposeBox.aside-morph.test.tsx src/ui/features/pretty-view/ComposeBox.aside-props.test.tsx src/ui/features/pretty-view/useVoiceRecording.test.ts src/ui/features/pretty-view/RecordingControls.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/features/pretty-view/ComposeBox.voice.test.tsx` exists
    - `grep -c "^  it(\\|^    it(\\|it(\"" src/ui/features/pretty-view/ComposeBox.voice.test.tsx` returns >= 9
    - `grep -c "getUserMedia" src/ui/features/pretty-view/ComposeBox.voice.test.tsx` returns >= 2 (mock setup + call assertion)
    - `grep -c "/voice/transcribe" src/ui/features/pretty-view/ComposeBox.voice.test.tsx` returns >= 1
    - `grep -c "MediaRecorder" src/ui/features/pretty-view/ComposeBox.voice.test.tsx` returns >= 1
    - `grep -c "toHaveBeenCalledWith" src/ui/features/pretty-view/ComposeBox.voice.test.tsx` returns >= 1 (the D-16-05 onSend assertion)
    - `npx tsc --noEmit` exits 0
    - `npx vitest run src/ui/features/pretty-view/ComposeBox.voice.test.tsx` exits 0 with all nine tests passing
    - `npx vitest run src/ui/features/pretty-view/ComposeBox.test.tsx src/ui/features/pretty-view/ComposeBox.aside-morph.test.tsx src/ui/features/pretty-view/ComposeBox.aside-props.test.tsx` exits 0 (existing tests unaffected)
    - `npx vitest run src/ui/features/pretty-view/useVoiceRecording.test.ts src/ui/features/pretty-view/RecordingControls.test.tsx` exits 0 (Plan 02 tests unaffected)
  </acceptance_criteria>
  <done>
    Integration test exists, covers the nine key behaviors including the D-16-05 same-send-path assertion, all existing ComposeBox tests still pass, all Plan 02 tests still pass. TypeScript clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ComposeBox → useVoiceRecording | In-process, same trust domain |
| ComposeBox → handleSend (via voice.endSend result) | In-process; the SAME send path typed messages use — no new trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-13 | Tampering | Voice-triggered send bypassing the existing send-safety gates | mitigate | voice.endSend routes through the SAME `handleSend()` function via the `overridePayload` parameter — D-50 newline collapse, COMPOSE-04 hard-lock (no optimistic bubble), and attachment-branching all still apply because handleSend's body is unchanged aside from the payload source. Test 8 in the integration test suite asserts this by verifying `props.onSend` is called with the transcribed payload (proving the code path went through handleSend, not a parallel send) |
| T-16-14 | Repudiation | User claims the transcript is wrong / they didn't send it | accept | User has the transcript in the textarea before send (in append mode). In send mode, the user pressed the SEND button — same as typing + send. The transcript is user-audible-and-visible before the send action. Same audit trail as typed messages |
| T-16-15 | Elevation-of-privilege | Mic button rendering while asideActive would let a user record during a locked morph state | mitigate | The `showMicButton` gate explicitly excludes `asideActive === true`. Test 3 in the integration suite asserts this (asideActive + text="" → no MicButton visible). The X-for-Resume morph OWNS the slot in asideActive state, matching Phase 14 semantics |
| T-16-16 | Denial-of-Service | Rapid tap on mic button during transcribe window | mitigate | showTranscribingSend disables the send button; showMicButton is false when state !== "idle"; showRecordingControls is false when state === "transcribing". No affordance is tappable during the transcribe window, so rapid-tap cannot double-fire. Hook-level guards in Plan 02 already handled the state-transition guards; this plan just makes the UI reflect them |
| T-16-SC | Tampering | npm installs | mitigate | No new npm installs required in this plan — all imports are local. Package legitimacy gate inherited from Plan 02 (which itself introduced no new packages) |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run src/ui/features/pretty-view/` exits 0 for the full pretty-view test suite (proves no regressions to ChatMessage, AsideBubble, WipBubble, PlanPendingBubble, PrettyView, etc.)
- `grep -c "useVoiceRecording\\|MicButton\\|RecordingControls" src/ui/features/pretty-view/ComposeBox.tsx` returns >= 6 (imports + JSX + hook call — three each)
- `grep -c "overridePayload\\|voice.endSend" src/ui/features/pretty-view/ComposeBox.tsx` returns >= 1 (proves the send-refactor path exists)
- The nine integration tests all pass, including Test 8 (the D-16-05 assertion)
</verification>

<success_criteria>
- ComposeBox renders MicButton in the idle-empty-not-morphed state
- ComposeBox renders RecordingControls during recording
- voice.endSend calls the same handleSend → onSend prop chain typed sends use (D-16-05 preserved and tested)
- Existing ComposeBox test suite unchanged and green
- New integration test covers the nine key voice-flow behaviors
- Zero scope creep beyond src/ui/features/pretty-view/ComposeBox.tsx + one new test file
</success_criteria>

<output>
Create `.planning/phases/16-voice-input-in-composebox-mic-button-tap-to-record-stt-via-s/16-03-SUMMARY.md` when done. Summary should note: (a) exact form of the handleSend refactor (overridePayload param signature), (b) the precise slot-visibility rule as landed (any deviations from `showMicButton = idle && empty && !aside && !queue && !attachments`), (c) how voice.errorMessage was merged into the existing errorMessage display, (d) test counts before + after (existing suite unchanged, new suite added).
</output>
