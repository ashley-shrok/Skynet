---
phase: 16-voice-input-in-composebox-mic-button-tap-to-record-stt-via-s
plan: 04
type: execute
wave: 4
depends_on: ["16-03"]
files_modified:
  - ~/.claude/identities/tina/skynet-patches.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "skynet-patches.md contains a new patch write-up for voice input in ComposeBox"
    - "The write-up appears in the SAME sequence as the code lands (docs inline, never queued — per Ashley's 2026-07-27 fleet directive)"
    - "The write-up covers: patch number (determined by scanning existing catalog + incrementing), motivation, files touched (backend + frontend + docker), rebase risk (medium, additive on pretty-view surface), test surface (unit + integration)"
    - "The catalog header count is updated to reflect the new patch number"
    - "No source code files under src/ or docker/ or .planning/ are modified in this plan — this is a doc-only patch"
  artifacts:
    - path: "~/.claude/identities/tina/skynet-patches.md"
      provides: "Updated patch catalog with the voice-input entry"
      contains: "voice"
  key_links:
    - from: "~/.claude/identities/tina/skynet-patches.md new entry"
      to: "src/backend/database/routes/voice.ts + src/ui/features/pretty-view/useVoiceRecording.ts + docker/nginx.conf + docker/nginx-https.conf"
      via: "Files-touched section of the write-up"
      pattern: "voice.ts\\|useVoiceRecording\\|nginx"
---

<objective>
Add the voice-input patch write-up to `~/.claude/identities/tina/skynet-patches.md` — the fork's patch catalog — per Ashley's 2026-07-27 fleet directive that docs land INLINE with the code, never queued for a later batch. The write-up is a self-contained narrative covering motivation, files touched, rebase risk, and test surface, following the exact format the catalog uses for other patches.

Purpose: The catalog is Tina's memory of every numbered patch on the fork (`feat/tab-title-from-tmux`, currently at 149 patches per the file header). New patches must land in the catalog in the SAME commit-adjacent moment as the code, so a fresh Tina session can rebase or diagnose without archaeology. This plan honors that.
Output: One modified markdown file in the identity directory; nothing in the Skynet repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-voice-input-in-composebox-mic-button-tap-to-record-stt-via-s/16-CONTEXT.md
@.planning/phases/16-voice-input-in-composebox-mic-button-tap-to-record-stt-via-s/16-01-SUMMARY.md
@.planning/phases/16-voice-input-in-composebox-mic-button-tap-to-record-stt-via-s/16-02-SUMMARY.md
@.planning/phases/16-voice-input-in-composebox-mic-button-tap-to-record-stt-via-s/16-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Determine the next patch number and add the voice-input entry to skynet-patches.md</name>
  <files>~/.claude/identities/tina/skynet-patches.md</files>
  <read_first>
    - ~/.claude/identities/tina/skynet-patches.md lines 1-40 (header + the "ONE HUNDRED FORTY-NINE numbered patches" count — needs incrementing)
    - ~/.claude/identities/tina/skynet-patches.md lines 40-120 (the FIRST patch entry — canonical format: bullet with `**patch #N (name)**` header, sub-bullets for motivation / root cause / fix / files-touched / rebase risk)
    - ~/.claude/identities/tina/skynet-patches.md (tail — the LAST/most-recent patch entry so the format used for recent entries is mirrored; the older entries can drift in format, always follow the most recent)
    - .planning/phases/16-voice-input-in-composebox-mic-button-tap-to-record-stt-via-s/16-CONTEXT.md § Locked design pins + Backend endpoint contract + Fleet-directive constraints (source material for the write-up narrative)
    - .planning/phases/16-voice-input-in-composebox-mic-button-tap-to-record-stt-via-s/16-01-SUMMARY.md, 16-02-SUMMARY.md, 16-03-SUMMARY.md (concrete file-touched + test-count numbers from the executed work — use verbatim in the "files touched" section)
  </read_first>
  <action>
    **Step 1 — Determine the patch number.** Grep the file for `**Skynet patch**: compose runs \`skynet-patched:local\`` (line ~15) and read the "ONE HUNDRED FORTY-NINE numbered patches" phrase. Increment by ONE (unless the executor discovers a newer patch has landed between now and execution — if so, use the newest count + 1). Also verify by scanning the file for the highest `patch #N` reference in the body (e.g., `grep -oP 'patch #\d+' ~/.claude/identities/tina/skynet-patches.md | sort -u -n -k1.7 | tail -5`) — the highest number is the last-landed patch; +1 is the voice-input patch.

    **Step 2 — Update the header count.** Change "ONE HUNDRED FORTY-NINE" to the spelled-out incremented count (e.g., "ONE HUNDRED FIFTY" if going from 149 → 150). Match the exact spelling style used ("ONE HUNDRED FORTY-NINE" uses hyphenated tens).

    **Step 3 — Append the new patch entry at the END of the patches list.** Use the SAME markdown structure as the most recent patch entry in the file. The entry MUST include the following sub-sections (as sub-bullets or short paragraphs, matching the surrounding style):

    - **Patch header:** `**Patch #<N>: voice input in pretty-view ComposeBox — mic button + tap-to-record + tailnet STT proxy**` (or similar; match how other headers are structured — `**Patch #<N>: <one-line summary>**`)
    - **Motivation:** Ashley wanted one-hand voice input for pretty-view sessions. Prototype at ~/.claude/identities/tina/bounties/add-voice-input/prototype.html UAT-passed on iOS PWA 2026-07-27 ("holy shit that just worked 100%, in a add-to-home-screen PWA"). This patch integrates the prototype's flow into the real ComposeBox.
    - **Root cause:** n/a (new feature, not a bug fix)
    - **Fix summary:** Three-part shape — (1) backend proxy `/voice/transcribe` reverse-proxies multipart audio to tailnet faster-whisper STT on GigaAshleyPC (100.80.122.111:8000/v1/audio/transcriptions); (2) frontend `useVoiceRecording` hook + `MicButton` + `RecordingControls` components own the state machine (idle → recording → transcribing → idle) with iOS Safari's sync-getUserMedia constraint preserved; (3) ComposeBox mounts the mic button in the send-button slot when the textarea is idle+empty and swaps to the three-button record controls (cancel / append / send) while recording. Voice-triggered sends route through the SAME existing handleSend function (D-16-05).
    - **Files touched:** List EXACTLY the files that landed. Backend: `src/backend/database/routes/voice.ts`, `src/backend/database/routes/voice.test.ts`, `src/backend/database/database.ts` (one new route mount + import). Frontend: `src/ui/features/pretty-view/useVoiceRecording.ts`, `src/ui/features/pretty-view/useVoiceRecording.test.ts`, `src/ui/features/pretty-view/MicButton.tsx`, `src/ui/features/pretty-view/RecordingControls.tsx`, `src/ui/features/pretty-view/RecordingControls.test.tsx`, `src/ui/features/pretty-view/ComposeBox.tsx` (mic slot wiring + handleSend overridePayload refactor), `src/ui/features/pretty-view/ComposeBox.voice.test.tsx`. Docker: `docker/nginx.conf`, `docker/nginx-https.conf` (matching /voice location blocks per fork gotcha).
    - **Test surface:** Unit tests for the backend route (multipart passthrough, error surface, timeout, oversized-body reject). Unit tests for the hook (state transitions, sync-getUserMedia verification, fetch shape, glue rule, error paths). Unit tests for RecordingControls (three-way dispatch, palette classes, accessible labels). Integration test for ComposeBox (slot-visibility rule, MediaRecorder mock, fetch mock, D-16-05 same-send-path assertion). NO iOS Safari unit test — the prototype already UAT-passed on Ashley's iOS PWA (documented in CONTEXT.md and the bounty tracker).
    - **Rebase risk:** MEDIUM — additive on the fork's pretty-view surface + docker configs, no upstream Skynet surface touched. Backend route file is new. ComposeBox.tsx modification is the biggest surface: adds imports + a hook call + slot-conditional rendering + a small handleSend override-payload refactor. The mic-visibility gate excludes text-non-empty + asideActive + queueArmed + hasAttachments, so no existing ComposeBox behavior regresses. Nginx blocks are new locations (no reordering of existing blocks). If upstream Skynet ever adds a `/voice` route we'll conflict — currently no such upstream route exists.
    - **Related bounty:** ~/.claude/identities/tina/bounties/add-voice-input/ (bounty tracker with prototype + design history)
    - **Deferred (v2):** streaming STT (backend batch-only currently), voice input in message-queue-in-pretty-view textareas (layers on when that bounty ships — mic-inside-textarea design is forward-compatible), auto-retry on STT error (first version surfaces the error and lets the user retry manually).

    **Step 4 — Verify the count in the header narrative matches the new highest patch number.** After appending, re-read the header to confirm the ONE HUNDRED FIFTY / FIFTY-ONE / etc. count matches the new highest patch # in the body.

    Do NOT touch any other patch entry. Do NOT reorder existing entries. Do NOT modify the invariants section, deploy-runbook reference, or box-map.md reference at the top of the file.
  </action>
  <verify>
    <automated>grep -c "voice.ts\|useVoiceRecording\|/voice/transcribe" ~/.claude/identities/tina/skynet-patches.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "voice input in pretty-view\|voice input in ComposeBox\|voice input in the pretty" ~/.claude/identities/tina/skynet-patches.md` returns >= 1 (patch header exists)
    - `grep -c "useVoiceRecording" ~/.claude/identities/tina/skynet-patches.md` returns >= 1 (files-touched section names the hook)
    - `grep -c "src/backend/database/routes/voice.ts" ~/.claude/identities/tina/skynet-patches.md` returns >= 1
    - `grep -c "docker/nginx.conf\|docker/nginx-https.conf" ~/.claude/identities/tina/skynet-patches.md` returns >= 1 (docker files mentioned)
    - `grep -c "100.80.122.111\|faster-whisper" ~/.claude/identities/tina/skynet-patches.md` returns >= 1 (STT service documented)
    - The header count phrase (e.g., "ONE HUNDRED FORTY-NINE") is updated by exactly one (verified by re-reading the header)
    - The new patch entry is at the END of the patches list (not inserted mid-file); verified by checking the last-added-patch bullet
    - No existing patch entries are modified (verified by diffing the file against its state before the edit — planner's expectation is a pure-append edit + header count bump, no other diffs)
  </acceptance_criteria>
  <done>
    skynet-patches.md contains the voice-input write-up at the end of the patches list, header count is incremented, and the entry covers motivation + files-touched + rebase risk per the format of neighboring entries.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No trust boundaries introduced | This plan is a doc-only edit outside the Skynet source tree; no new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-17 | Information disclosure | Catalog write-up mentioning the STT tailnet IP | accept | The tailnet IP `100.80.122.111` appears in the catalog write-up because it's the LOCKED URL from CONTEXT.md and part of the fix summary. The catalog lives under `~/.claude/identities/tina/` — Ashley's private identity directory, not committed to any public repo. Same disclosure posture as CONTEXT.md / RESEARCH.md / bounty.json which also carry the IP |
| T-16-SC | Tampering | No installs | mitigate | Doc-only edit; no npm/pip/cargo activity |
</threat_model>

<verification>
- `grep -c "voice" ~/.claude/identities/tina/skynet-patches.md` returns > prior baseline count (verified by diff)
- The most-recent-patch header count matches the highest `patch #N` reference in the body
- No src/ or docker/ or .planning/ files modified in this plan
</verification>

<success_criteria>
- New patch entry exists in skynet-patches.md
- Header count incremented by one
- Files-touched section names all seven+ files landed by Plans 01-03
- No collateral edits to other patch entries or the invariants section
</success_criteria>

<output>
Create `.planning/phases/16-voice-input-in-composebox-mic-button-tap-to-record-stt-via-s/16-04-SUMMARY.md` when done. Summary should note: (a) the patch number assigned (149+1 = 150 unless drift), (b) the exact header count-phrase substitution made, (c) confirmation that no other patch entries were modified.
</output>
