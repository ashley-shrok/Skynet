---
phase: 121-workspace-file-browser
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/api/workspace-api.ts
  - src/ui/features/pretty-view/workspace-error-copy.ts
autonomous: true
requirements: [D-19, D-20, D-23, D-24]

must_haves:
  truths:
    - "All 9 backend endpoints have a matching client-side helper in workspace-api.ts (list, readFile, writeFile, deleteEntry, rename, mkdir, createFile, upload, download)"
    - "Every helper throws an Error whose .message IS the backend error-class string (e.g. 'permission_denied', 'not_found', 'not_a_directory') so callers can switch on it (mirrors fetchHostFileUrl at editable-file-api.ts:131)"
    - "workspace-error-copy.ts exports WORKSPACE_ERROR_COPY keyed by every error class the backend produces (host_unreachable, not_found, too_large, path_forbidden, path_traversal, permission_denied, unknown_host, ssh_timeout, invalid_hostname, invalid_body, not_a_file, invalid_identity_key, not_a_directory, already_exists, not_empty, generic)"
    - "Every heading and body in WORKSPACE_ERROR_COPY reads naturally to a non-developer — no HTTP codes, shell terms, or path strings (D-23, D-24)"
  artifacts:
    - path: "src/ui/api/workspace-api.ts"
      provides: "9 async client helpers wrapping /workspace endpoints + WorkspaceEntry/ListResponse/ReadFileResponse type exports"
      exports: ["WorkspaceEntry", "ListResponse", "ReadFileResponse", "listWorkspace", "readWorkspaceFile", "writeWorkspaceFile", "deleteWorkspaceEntry", "renameWorkspaceEntry", "mkdirWorkspace", "createWorkspaceFile", "uploadWorkspaceFile", "downloadWorkspaceFileUrl"]
      min_lines: 160
    - path: "src/ui/features/pretty-view/workspace-error-copy.ts"
      provides: "WORKSPACE_ERROR_COPY map (heading + body per error class) + resolveWorkspaceErrorCopy(errorClass) helper"
      exports: ["WORKSPACE_ERROR_COPY", "resolveWorkspaceErrorCopy", "WorkspaceErrorCopy"]
      min_lines: 80
  key_links:
    - from: "src/ui/api/workspace-api.ts"
      to: "backend /workspace endpoints (Plan 121-01)"
      via: "authApi.post/put/delete/get to '/workspace/list' etc."
      pattern: "authApi\\.(post|put|delete|get).*['\"]\\/workspace"
    - from: "src/ui/api/workspace-api.ts"
      to: "backend error-class strings"
      via: "throws Error(backendClass) preserving classification for UI"
      pattern: "new Error\\(backendClass\\)"
    - from: "src/ui/features/pretty-view/workspace-error-copy.ts"
      to: "consumer-user register (D-23)"
      via: "static human-readable heading + body per class"
      pattern: "heading:.*body:"
---

<objective>
Build the frontend wire adapters and consumer-user error copy that the WorkspaceTab UI (Plan 121-03) and the goal-backward verification (Plan 121-05) will consume. Two files, no side effects, no UI — a pure typed surface over the backend from Plan 121-01.

Purpose: implements D-19 (reuse fetchHostFileUrl error-preserving pattern), D-20 (error copy taxonomy — do NOT import from EditableFileModal, redeclare per the zero-risk option in RESEARCH § Pattern 8), D-23 + D-24 (every user-visible string readable by a non-developer — no HTTP codes, no path strings, no shell/SSH jargon).

Output: `src/ui/api/workspace-api.ts` (9 fetch helpers + wire types) and `src/ui/features/pretty-view/workspace-error-copy.ts` (WORKSPACE_ERROR_COPY map + resolver). Both files are pure data + async helpers — no React, no UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/121-workspace-file-browser/121-CONTEXT.md
@.planning/phases/121-workspace-file-browser/121-RESEARCH.md
@.planning/phases/121-workspace-file-browser/121-PATTERNS.md
@src/ui/api/editable-file-api.ts
@src/ui/features/pretty-view/EditableFileModal.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build workspace-api.ts with 9 fetch helpers + wire type exports</name>
  <files>src/ui/api/workspace-api.ts</files>
  <read_first>
    - src/ui/api/editable-file-api.ts (analog for the error-class-preserving axios error handling pattern L131-163 — lift the isAxiosError branch verbatim, changing HostFileFetchError to WorkspaceError)
    - .planning/phases/121-workspace-file-browser/121-PATTERNS.md § workspace-api.ts (full pattern lift table)
    - .planning/phases/121-workspace-file-browser/121-RESEARCH.md § Pattern 5 (Wire shape for /workspace/list response) — WorkspaceEntry type shape is authoritative
    - .planning/phases/121-workspace-file-browser/121-01-PLAN.md (Task 1 behavior block — the exact request/response shapes for all 9 endpoints)
  </read_first>
  <behavior>
    Exports (all named):
    - `type WorkspaceEntry = { name: string; type: "file" | "directory" | "symlink"; size: number | null; mtimeMs: number; path: string }`
    - `type ListResponse = { entries: WorkspaceEntry[]; path: string }`
    - `type ReadFileResponse = { contentBase64: string; sizeBytes: number; filename: string; extension: string | null }`
    - `async function listWorkspace(identityKey, hostId, relativePath): Promise<ListResponse>` — POST /workspace/list
    - `async function readWorkspaceFile(identityKey, hostId, relativePath): Promise<ReadFileResponse>` — POST /workspace/read-file
    - `async function writeWorkspaceFile(identityKey, hostId, relativePath, content: string): Promise<void>` — PUT /workspace/write-file
    - `async function deleteWorkspaceEntry(identityKey, hostId, relativePath): Promise<void>` — DELETE /workspace/entry (body sent via axios delete with data option)
    - `async function renameWorkspaceEntry(identityKey, hostId, from, to): Promise<void>` — POST /workspace/rename
    - `async function mkdirWorkspace(identityKey, hostId, relativePath): Promise<void>` — POST /workspace/mkdir
    - `async function createWorkspaceFile(identityKey, hostId, relativePath): Promise<void>` — POST /workspace/create-file
    - `async function uploadWorkspaceFile(identityKey, hostId, relativePath, file: File, onProgress?: (loaded, total) => void): Promise<void>` — POST /workspace/upload multipart (FormData with fields identityKey/hostId/relativePath + file); pass axios onUploadProgress if onProgress supplied
    - `function downloadWorkspaceFileUrl(identityKey, hostId, relativePath): string` — synchronous builder that returns `/workspace/download?identityKey=...&hostId=...&relativePath=...` (URL-encoded); caller triggers by setting window.location or creating an anchor. Note: uses relative URL because backend + frontend share the same origin under the nginx proxy.

    Every async helper wraps its axios call in try/catch. Catch block:
    ```
    if (axios.isAxiosError(error)) {
      const backendClass = error.response?.data?.error;
      if (typeof backendClass === "string" && backendClass.length > 0) {
        const rich = new Error(backendClass);
        rich.name = "WorkspaceError";
        throw rich;
      }
    }
    handleApiError(error, "<operation label>");
    throw error;
    ```
    Operation labels: "list workspace", "read workspace file", "write workspace file", "delete workspace entry", "rename workspace entry", "create workspace folder", "create workspace file", "upload workspace file".

    Import: `import axios from "axios"; import { authApi, handleApiError } from "@/main-axios";`

    NO React imports. NO UI. NO stateful anything.
  </behavior>
  <action>
    Create the file. Follow the analog editable-file-api.ts pattern for every helper. For DELETE /workspace/entry: axios's delete method accepts data via config (`authApi.delete("/workspace/entry", { data: { identityKey, hostId, relativePath } })`).

    For the upload helper: `const form = new FormData(); form.append("identityKey", identityKey); form.append("hostId", String(hostId)); form.append("relativePath", relativePath); form.append("file", file); await authApi.post("/workspace/upload", form, { headers: { "Content-Type": "multipart/form-data" }, onUploadProgress: onProgress ? (e) => onProgress(e.loaded, e.total ?? file.size) : undefined });` — do NOT set Content-Type manually if authApi's axios instance strips it; verify by reading the authApi setup if needed (main-axios.ts). Fallback: let axios auto-set the multipart boundary by omitting Content-Type and passing FormData directly.

    For downloadWorkspaceFileUrl: pure string builder — `return \`/workspace/download?identityKey=\${encodeURIComponent(identityKey)}&hostId=\${encodeURIComponent(String(hostId))}&relativePath=\${encodeURIComponent(relativePath)}\`;`

    Add a top-of-file doc block explaining: this is the sole client-side surface to /workspace; every helper preserves the backend error-class in Error.message so the consumer (WorkspaceTab) can switch on it and pick copy from WORKSPACE_ERROR_COPY (Plan 121-02 Task 2 output).
  </action>
  <verify>
    <automated>cd /home/ubuntu/fleet/identities/echo-box-maintainer/workspace/skynet &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -c "src/ui/api/workspace-api.ts" | grep -q "^0$" &amp;&amp; echo TSC_CLEAN_WORKSPACE_API</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/ui/api/workspace-api.ts
    - `grep -c "export async function" src/ui/api/workspace-api.ts` returns ≥ 8 (async helpers)
    - `grep -c "export function downloadWorkspaceFileUrl" src/ui/api/workspace-api.ts` returns 1 (sync helper)
    - `grep -c "export type" src/ui/api/workspace-api.ts` returns ≥ 3 (WorkspaceEntry, ListResponse, ReadFileResponse)
    - `grep -c "authApi\\." src/ui/api/workspace-api.ts` returns ≥ 8 (one per async helper)
    - `grep -c "WorkspaceError" src/ui/api/workspace-api.ts` returns ≥ 8 (error preservation in every helper)
    - `grep -c "handleApiError" src/ui/api/workspace-api.ts` returns ≥ 8 (fallback path in every helper)
    - `grep -c "import.*React\|import.*useState\|import.*useEffect" src/ui/api/workspace-api.ts` returns 0 (pure API layer — no React)
    - `npx tsc --noEmit` produces zero errors mentioning this file
  </acceptance_criteria>
  <done>All 9 client helpers exported; TSC clean; error-class preservation matches editable-file-api.ts pattern; no React coupling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build workspace-error-copy.ts with WORKSPACE_ERROR_COPY map + resolver in consumer-user register (D-23, D-24)</name>
  <files>src/ui/features/pretty-view/workspace-error-copy.ts</files>
  <read_first>
    - src/ui/features/pretty-view/EditableFileModal.tsx L62-146 (analog FILE_URL_ERROR_COPY map — copy the overlapping entries verbatim per RESEARCH Pattern 8 zero-risk option; do NOT touch EditableFileModal.tsx)
    - .planning/phases/121-workspace-file-browser/121-CONTEXT.md § D-23, D-24 (consumer-user register — non-developer audience)
    - .planning/phases/121-workspace-file-browser/121-PATTERNS.md § workspace-error-copy.ts (full sample map with heading/body pairs — use verbatim as a starting point)
  </read_first>
  <behavior>
    Exports:
    - `type WorkspaceErrorCopy = { heading: string; body: string }`
    - `const WORKSPACE_ERROR_COPY: Record<string, WorkspaceErrorCopy>` — must include every class the backend produces (Plan 121-01):
      - Shared with FILE_URL_ERROR_COPY: host_unreachable, not_found, too_large, not_a_file, path_forbidden, path_traversal, permission_denied, unknown_host, ssh_timeout, invalid_hostname, invalid_body, generic
      - Workspace-specific additions: not_a_directory, already_exists, not_empty, invalid_identity_key
    - `function resolveWorkspaceErrorCopy(errorClass: string | undefined): WorkspaceErrorCopy` — returns `WORKSPACE_ERROR_COPY[errorClass] ?? WORKSPACE_ERROR_COPY.generic`

    Consumer-user register (D-23, D-24) — every heading + body:
    - No HTTP status codes visible
    - No developer terms: "SSH", "socket", "stack", "stdin", "regex", "curl", "SFTP", "octal", "dotfile", "repo", "chmod", "fork", "path", "escape", "URL", "endpoint", "server-side" — none may appear in user-visible strings
    - No file paths, no shell commands
    - Body sentences are complete English sentences with a period
    - Tone matches EditableFileModal FILE_URL_ERROR_COPY: friendly, short, actionable

    Sample copy (from PATTERNS.md § workspace-error-copy.ts — use as-is unless improvement is obvious):
    - host_unreachable: heading "Host unreachable", body "The box may be offline or the connection is down. Try again in a moment."
    - not_found: heading "File not found", body "No such file or folder at that path." — replace "at that path" with "here" to strip the path-shaped word if D-24 flag warrants
    - too_large: heading "File too large", body "This file is too large to open in the editor. Use the Download button instead."
    - not_a_file: heading "Not a file", body "That's a folder, not a file."
    - not_a_directory: heading "Not a folder", body "That's a file, not a folder."
    - already_exists: heading "Already exists", body "Something with that name already exists here. Choose a different name."
    - not_empty: heading "Folder not empty", body "Remove everything inside the folder first, then delete it."
    - permission_denied: heading "Permission denied", body "You don't have access to that file or folder."
    - path_forbidden: heading "Off limits", body "That location is not accessible."
    - path_traversal: heading "Invalid name", body "That name contains characters that aren't allowed."
    - unknown_host: heading "Unknown host", body "That box isn't registered, or you don't have access to it."
    - ssh_timeout: heading "Connection timed out", body "The box is slow or unreachable. Try again in a moment."
    - invalid_hostname: heading "Something went wrong", body "The address looks wrong. Refresh and try again."
    - invalid_identity_key: heading "Something went wrong", body "Could not identify the agent. Try closing and reopening."
    - invalid_body: heading "Something went wrong", body "The request was malformed. Refresh and try again."
    - generic: heading "Something went wrong", body "An unexpected error occurred. Try again, or refresh the page."
  </behavior>
  <action>
    Create the file. Redeclare the overlapping entries (do NOT import FILE_URL_ERROR_COPY from EditableFileModal — that constant is module-private and touching EditableFileModal is out of scope). Zero-risk option per RESEARCH.md Pattern 8.

    File shape:
    - Top doc block explaining consumer-user register (D-23, D-24) and why entries are redeclared rather than imported.
    - `export type WorkspaceErrorCopy = { heading: string; body: string };`
    - `export const WORKSPACE_ERROR_COPY: Record<string, WorkspaceErrorCopy> = { ... };` — populate every class listed in the behavior section.
    - `export function resolveWorkspaceErrorCopy(errorClass: string | undefined): WorkspaceErrorCopy { return WORKSPACE_ERROR_COPY[errorClass ?? ""] ?? WORKSPACE_ERROR_COPY.generic; }`

    No React imports. No component code. Pure data + one 3-line helper.

    Register self-check: after writing, grep for developer terms in the map values — the grep gate below enforces this.
  </action>
  <verify>
    <automated>cd /home/ubuntu/fleet/identities/echo-box-maintainer/workspace/skynet &amp;&amp; grep -cE "SSH|SFTP|stdin|stderr|regex|curl|octal|dotfile|chmod|\\bfork\\b|endpoint|server-side|stack trace|HTTP|status code" src/ui/features/pretty-view/workspace-error-copy.ts | grep -q "^0$" &amp;&amp; echo REGISTER_CLEAN &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -c "workspace-error-copy" | grep -q "^0$" &amp;&amp; echo TSC_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/ui/features/pretty-view/workspace-error-copy.ts
    - `grep -c "export const WORKSPACE_ERROR_COPY" src/ui/features/pretty-view/workspace-error-copy.ts` returns 1
    - `grep -c "export function resolveWorkspaceErrorCopy" src/ui/features/pretty-view/workspace-error-copy.ts` returns 1
    - `grep -c "export type WorkspaceErrorCopy" src/ui/features/pretty-view/workspace-error-copy.ts` returns 1
    - Every backend error class present as a top-level key — assert: `for k in host_unreachable not_found too_large not_a_file not_a_directory already_exists not_empty permission_denied path_forbidden path_traversal unknown_host ssh_timeout invalid_hostname invalid_identity_key invalid_body generic; do grep -c "\"$k\":" src/ui/features/pretty-view/workspace-error-copy.ts | grep -q "^1$" || echo "MISSING: $k"; done` produces no MISSING lines
    - `grep -cE "SSH|SFTP|stdin|stderr|regex|curl|octal|dotfile|chmod|\\bfork\\b|endpoint|server-side|stack trace|\\bHTTP\\b|status code" src/ui/features/pretty-view/workspace-error-copy.ts` returns 0 (D-23, D-24 consumer register)
    - `grep -c "import.*React\|import.*useState" src/ui/features/pretty-view/workspace-error-copy.ts` returns 0 (pure data — no React)
    - `grep -c "FILE_URL_ERROR_COPY\|EditableFileModal" src/ui/features/pretty-view/workspace-error-copy.ts` returns 0 (redeclare, do NOT touch existing file per RESEARCH Pattern 8)
    - `npx tsc --noEmit` produces zero errors mentioning this file
  </acceptance_criteria>
  <done>Map covers every backend class; every string reads as consumer-user copy; no React; no dependency on FILE_URL_ERROR_COPY; TSC clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser JS → workspace-api.ts helpers | Helpers are called by WorkspaceTab; identityKey/hostId/relativePath come from user interaction; validation happens in Plan 121-01 backend |
| workspace-error-copy.ts strings → DOM | Every string rendered by React; must be safe as text nodes (no HTML injection risk — content is static in this file) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-P2-01 | Information Disclosure | Error copy leaking developer detail to non-developer audience | mitigate | Every heading + body in WORKSPACE_ERROR_COPY passes the register-check grep gate (no SSH/SFTP/HTTP/etc terms); D-23 and D-24 enforced at file-authoring time. |
| T-118-P2-02 | Information Disclosure | Backend error class string carrying err.message or path into UI | accept | Upstream mitigation in Plan 121-01 (T-118-07 — T-40-05 invariant): backend response bodies are strictly `{ error: "<class>" }`. workspace-api.ts trusts that contract; if backend violates it, T-118-07 test catches it. No client-side scrubbing needed. |
| T-118-P2-03 | Tampering | XSS via error class value used as object key | accept | `WORKSPACE_ERROR_COPY[errorClass]` is object property access, not HTML injection. Value returned is a WorkspaceErrorCopy struct with static strings — always safe. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- All 9 backend endpoints have a matching client helper
- All 16 error classes present in WORKSPACE_ERROR_COPY
- Zero developer terms in user-visible copy
</verification>

<success_criteria>
- workspace-api.ts exports 8 async helpers + 1 sync URL builder + 3 types
- workspace-error-copy.ts exports the map + resolver + type; covers every backend class; passes the register grep gate
- Both files TSC-clean, zero React coupling
- Both files ready to be imported by WorkspaceTab in Plan 121-03
</success_criteria>

<output>
Create `.planning/phases/121-workspace-file-browser/121-02-SUMMARY.md` with:
- Exports (types + functions) with signatures
- Error classes covered by WORKSPACE_ERROR_COPY (list)
- Consumer register grep gate result
- Files created (2 files, line counts)
</output>
