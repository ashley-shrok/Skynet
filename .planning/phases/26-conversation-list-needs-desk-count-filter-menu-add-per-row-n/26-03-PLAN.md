---
phase: 26-conversation-list-needs-desk-count-filter-menu
plan: 03
type: execute
wave: 3
depends_on:
  - 26-02
files_modified:
  - src/ui/features/pretty-conversations/PrettyBountyCountBadge.tsx
  - src/ui/features/pretty-conversations/PrettyBountyCountBadge.test.tsx
  - src/ui/features/pretty-conversations/PrettyConversationRow.tsx
  - src/ui/features/pretty-conversations/pretty-conversations.css
autonomous: true
requirements: []
must_haves:
  truths:
    - "A row whose identity has pinnedCount>0 AND needsDeskCount>0 renders a single pill with both numbers separated by a middle-dot (U+00B7)"
    - "A row with pinnedCount>0 AND needsDeskCount===0 renders '3·' (right side blank inside the pill)"
    - "A row with pinnedCount===0 AND needsDeskCount>0 renders '·1' (left side blank inside the pill)"
    - "A row with pinnedCount===0 AND needsDeskCount===0 (or the pair is undefined pre-fetch) renders null (no pill)"
    - "The pill occupies the SAME .pv-meta slot as today's badge (order preserved: [deactivate][pin][badge][ready-dot])"
    - "Both halves inherit --pv-hue from the .pv-row parent — no dual-hue treatment"
    - "PrettyConversationRow consumes the new useBountyCounts (plural) hook returning the pair, and passes both numbers to PrettyBountyCountBadge"
  artifacts:
    - path: "src/ui/features/pretty-conversations/PrettyBountyCountBadge.tsx"
      provides: "Combined pin·desk pill component consuming {pinnedCount, needsDeskCount} pair"
      contains: "needsDeskCount"
    - path: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx"
      provides: "Row wiring — consumes useBountyCounts (plural) and forwards the pair to PrettyBountyCountBadge"
      contains: "useBountyCounts"
    - path: "src/ui/features/pretty-conversations/pretty-conversations.css"
      provides: "CSS for the combined-pill separator layout (unchanged .pv-bounty-badge outer chrome + inner half/separator classes)"
      contains: "pv-bounty-badge"
  key_links:
    - from: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx"
      to: "src/ui/state/bounty-counts-store.ts"
      via: "useBountyCounts hook consumption"
      pattern: "useBountyCounts"
    - from: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx"
      to: "src/ui/features/pretty-conversations/PrettyBountyCountBadge.tsx"
      via: "PrettyBountyCountBadge prop pass-through"
      pattern: "PrettyBountyCountBadge"
---

<objective>
Refactor `PrettyBountyCountBadge` from a bare-integer pill into a two-half combined `pin·desk` pill per CONTEXT.md §decisions "Row-display format — LOCKED". Rewire `PrettyConversationRow` to consume the widened `useBountyCounts` (plural) hook from Plan 26-02 and forward the pair to the badge.

The pill IS the single visible artifact for both counts. It reuses the exact `.pv-meta` slot and the exact `.pv-bounty-badge` outer chrome (min-width, height, padding, border-radius, colors, mobile bump at max-width:767.98px) so the row height and .pv-meta composition do NOT change. Only the pill's inner content structure changes to accommodate the two-half layout.

Purpose: Ashley's per-row visibility requires seeing BOTH counts at a glance without doubling `.pv-meta` width (rejected alternative (a) — two side-by-side badges). Middle-dot separator (`·`, U+00B7) is the locked format. Zero on one side renders that side blank so the pill remains readable when only one counter is active.

Output:
- `PrettyBountyCountBadge` accepts `{pinnedCount, needsDeskCount}` (both `number | undefined`) and renders per the rendering rule table from CONTEXT.md §specifics.
- Row wiring switched to `useBountyCounts` (plural) — same call site as today, same host-id derivation, but forwards the pair.
- CSS updated with per-half spans + a middle-dot separator span; outer `.pv-bounty-badge` chrome unchanged.
- Existing badge test suite widened to cover all 4 rendering-rule cases + a mobile-bump smoke assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-conversation-list-needs-desk-count-filter-menu-add-per-row-n/26-CONTEXT.md
@.planning/phases/26-conversation-list-needs-desk-count-filter-menu-add-per-row-n/26-01-PLAN.md
@.planning/phases/26-conversation-list-needs-desk-count-filter-menu-add-per-row-n/26-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebuild PrettyBountyCountBadge as the combined pin·desk pill</name>
  <files>src/ui/features/pretty-conversations/PrettyBountyCountBadge.tsx, src/ui/features/pretty-conversations/PrettyBountyCountBadge.test.tsx, src/ui/features/pretty-conversations/pretty-conversations.css</files>
  <read_first>
    - src/ui/features/pretty-conversations/PrettyBountyCountBadge.tsx (whole 27-line current file — the bare-integer pill and its data-testid convention)
    - src/ui/features/pretty-conversations/PrettyBountyCountBadge.test.tsx (whole 42-line current test — 4 tests covering undefined/0/1/99 cases)
    - src/ui/features/pretty-conversations/pretty-conversations.css lines 986-1049 (the .pv-bounty-badge base rule + the max-width:767.98px mobile-bump rule)
    - .planning/phases/26-conversation-list-needs-desk-count-filter-menu-add-per-row-n/26-CONTEXT.md (§decisions "Row-display format — LOCKED" + §specifics "Badge component: rendering rule table")
  </read_first>
  <behavior>
    - Test 1a (null when BOTH counts are undefined pre-fetch): render `<PrettyBountyCountBadge pinnedCount={undefined} needsDeskCount={undefined} />` — `container.firstChild` is null; `queryByTestId("pv-bounty-badge")` is null.
    - Test 1b (null when BOTH counts are 0): render with `pinnedCount={0}` and `needsDeskCount={0}` — same null result.
    - Test 1c (null when pair is undefined — pre-fetch shorthand): render with `pinnedCount={undefined}` and `needsDeskCount={0}` — null (undefined = "not yet fetched", never renders half a pill until the pair lands).
    - Test 2 (both nonzero — `"3·1"`): render `pinnedCount={3} needsDeskCount={1}`; assert `getByTestId("pv-bounty-badge").textContent === "3·1"`; assert both half spans present with the numbers; assert the separator span carries the middle-dot character U+00B7.
    - Test 3 (pinned-only — `"3·"`): render `pinnedCount={3} needsDeskCount={0}`; assert textContent === `"3·"` (right side blank); assert `getByTestId("pv-bounty-badge-pinned").textContent === "3"` and `getByTestId("pv-bounty-badge-needs-desk").textContent === ""` (empty span present but empty).
    - Test 4 (needs-desk-only — `"·1"`): render `pinnedCount={0} needsDeskCount={1}`; assert textContent === `"·1"`; symmetric per-half testid assertions.
    - Test 5 (large numbers do not truncate): render `pinnedCount={99} needsDeskCount={12}`; textContent === `"99·12"`.
    - Test 6 (mixed undefined semantics — if only ONE half is undefined but the other is a real number, treat undefined as 0 for that half so the pill still renders): render `pinnedCount={3} needsDeskCount={undefined}` — should render `"3·"` (undefined treated as 0 for display when at least one half is a real number). This matches "pre-fetch = undefined pair; post-fetch = both halves populated" semantics from Plan 26-02, where the store always publishes both halves together, so a mixed-undefined case in practice only surfaces during an edge case where a caller manually passes undefined for one half. Test locks the render rule for that edge.
  </behavior>
  <action>
    Replace `src/ui/features/pretty-conversations/PrettyBountyCountBadge.tsx` with a new implementation matching this exact prop signature: `export function PrettyBountyCountBadge({ pinnedCount, needsDeskCount }: { pinnedCount: number | undefined; needsDeskCount: number | undefined }): JSX.Element | null`.

    Rendering logic:
    - Coerce both inputs via `const p = pinnedCount ?? 0; const d = needsDeskCount ?? 0;`.
    - If the pair is fully unpopulated (both original inputs undefined) OR both coerced values are 0 → return `null`. Combine these into `if ((pinnedCount === undefined && needsDeskCount === undefined) || (p === 0 && d === 0)) return null;`.
    - Otherwise return a single `<span className="pv-bounty-badge" data-testid="pv-bounty-badge">` containing three inner spans in order:
      1. `<span className="pv-bounty-badge-half pv-bounty-badge-half--pinned" data-testid="pv-bounty-badge-pinned">{p > 0 ? String(p) : ""}</span>`
      2. `<span className="pv-bounty-badge-sep" aria-hidden="true">{"·"}</span>` (middle-dot U+00B7)
      3. `<span className="pv-bounty-badge-half pv-bounty-badge-half--needs-desk" data-testid="pv-bounty-badge-needs-desk">{d > 0 ? String(d) : ""}</span>`
    - Add an `aria-label` on the outer span carrying a screen-reader-friendly summary, e.g. `aria-label={\`${p} pinned, ${d} needs desk\`}` — cheap accessibility win, doesn't affect visual output.

    Update the top-of-file block comment to describe the widened pill shape, the 4-case rendering rule table, and cite CONTEXT.md D-01 (row-display format LOCKED).

    In `src/ui/features/pretty-conversations/PrettyBountyCountBadge.test.tsx`:
    - Replace all 4 existing tests with the 6 tests in `<behavior>`. Preserve the file's top-of-file block comment updated to describe the widened pill shape.

    In `src/ui/features/pretty-conversations/pretty-conversations.css` §.pv-bounty-badge block (lines ~986-1003):
    - Preserve the outer `.pv-bounty-badge` rule verbatim EXCEPT bump `min-width` from `20px` to `28px` to accommodate two-half content when both are single digits (test the render at desktop scale to confirm). If tighter fit is preferable, change `min-width` to `24px` and rely on padding + child widths — pick 28px as the safe starting value; downstream visual UAT may tune it.
    - Add three new rules AFTER the base .pv-bounty-badge rule:
      ```
      .pv-bounty-badge-half {
        display: inline-block;
        min-width: 8px;
        text-align: center;
      }
      .pv-bounty-badge-half--pinned {
        text-align: right;
      }
      .pv-bounty-badge-half--needs-desk {
        text-align: left;
      }
      .pv-bounty-badge-sep {
        display: inline-block;
        margin: 0 1px;
        opacity: 0.7;
      }
      ```
    - In the @media (max-width: 767.98px) block (lines ~1043-1049), bump the outer `.pv-bounty-badge` `min-width` proportionally: from mobile 32px to ~44px so two mobile-scale digits fit; keep the other mobile properties (`height: 28px`, `padding: 0 9px`, `font-size: 19px`, `line-height: 28px`) unchanged. If executor tuning shows 44px is too aggressive, 40px is an acceptable alternate; note the choice in the SUMMARY.

    Do NOT change `.pv-meta` layout, do NOT touch other CSS classes, do NOT add a new file. All new selectors nest under the existing `.pv-bounty-badge` scope so no cross-component leakage.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx vitest run src/ui/features/pretty-conversations/PrettyBountyCountBadge.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^export function PrettyBountyCountBadge" src/ui/features/pretty-conversations/PrettyBountyCountBadge.tsx` shows exactly one export line with the widened `{ pinnedCount, needsDeskCount }` prop signature.
    - `grep -c "count:" src/ui/features/pretty-conversations/PrettyBountyCountBadge.tsx` returns 0 (old singular `count` prop retired).
    - `grep -c "pv-bounty-badge-half\|pv-bounty-badge-sep" src/ui/features/pretty-conversations/PrettyBountyCountBadge.tsx` returns ≥ 3 (per-half + separator classNames).
    - `grep -c "\\\\u00B7\|·" src/ui/features/pretty-conversations/PrettyBountyCountBadge.tsx` returns ≥ 1 (middle-dot character present in the source).
    - `grep -c "pv-bounty-badge-half\|pv-bounty-badge-sep" src/ui/features/pretty-conversations/pretty-conversations.css` returns ≥ 4 (three new selectors + one shared use if applicable).
    - `npx vitest run src/ui/features/pretty-conversations/PrettyBountyCountBadge.test.tsx` exits 0 with all 6 tests passing.
    - Test 2's assertion `getByTestId("pv-bounty-badge").textContent === "3·1"` proves the concatenated pill renders both halves + separator.
    - Test 3 + Test 4 prove the blank-side render rule for `"3·"` and `"·1"` respectively.
    - Test 1a + Test 1b + Test 1c prove the null-render rules for undefined/0/0 combinations.
  </acceptance_criteria>
  <done>Combined pin·desk pill component + widened test suite green; CSS updated with per-half spans + separator + min-width bump; outer .pv-bounty-badge chrome preserved so `.pv-meta` composition and row height do not change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire PrettyConversationRow to consume useBountyCounts (plural) and pass the pair to the badge</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationRow.tsx, src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-conversations/PrettyConversationRow.tsx lines 74-100 (imports including line 78 `import { useBountyCount } from "@/state/bounty-counts-store"`)
    - src/ui/features/pretty-conversations/PrettyConversationRow.tsx lines 195-215 (the useBountyCount call site including identity resolution + host-id parseInt + the `pinnedCount` local)
    - src/ui/features/pretty-conversations/PrettyConversationRow.tsx lines 495-515 (the `.pv-meta` render site where `<PrettyBountyCountBadge count={pinnedCount} />` is composed inline)
    - src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx (whole file — existing bounty-badge assertions; identify every place that references `useBountyCount` mock or the badge's prop shape)
    - .planning/phases/26-conversation-list-needs-desk-count-filter-menu-add-per-row-n/26-02-PLAN.md (Plan 26-02 signature for `useBountyCounts` — the pair)
    - .planning/phases/26-conversation-list-needs-desk-count-filter-menu-add-per-row-n/26-CONTEXT.md (§decisions "Frontend store — LOCKED")
  </read_first>
  <behavior>
    - Test A (row with identity + non-zero pinnedCount + zero needsDeskCount): mock `useBountyCounts` to return `{pinnedCount: 3, needsDeskCount: 0}`; render the row; assert `getByTestId("pv-bounty-badge").textContent === "3·"`.
    - Test B (row with identity + zero pinnedCount + non-zero needsDeskCount): mock returns `{pinnedCount: 0, needsDeskCount: 1}`; textContent === `"·1"`.
    - Test C (row with identity + both nonzero): mock returns `{pinnedCount: 3, needsDeskCount: 1}`; textContent === `"3·1"`.
    - Test D (row with identity + both zero): mock returns `{pinnedCount: 0, needsDeskCount: 0}`; `queryByTestId("pv-bounty-badge")` is null.
    - Test E (row without identity — sessionMatchKey → null): mock `useBountyCounts` returns undefined (identityKey null short-circuit); `queryByTestId("pv-bounty-badge")` is null (row does not render the pill for non-identity rows).
    - Test F (pre-fetch state — mock returns undefined): `queryByTestId("pv-bounty-badge")` is null (pair pre-fetch, badge renders null).
    - Any existing test that used to assert `count={0}` OR `count={3}` on the badge is updated to the pair-shape.
  </behavior>
  <action>
    In `src/ui/features/pretty-conversations/PrettyConversationRow.tsx`:

    (1) Line 78 import: change `import { useBountyCount } from "@/state/bounty-counts-store";` to `import { useBountyCounts } from "@/state/bounty-counts-store";`.

    (2) Lines ~209-212 call site: rename local `const pinnedCount = useBountyCount(...)` to `const bountyCounts = useBountyCounts(identity?.identityKey ?? null, Number.isFinite(rowHostIdNum) ? rowHostIdNum : null);`. Preserve the surrounding `rowHostIdNum` derivation (line 208) and the identity resolution (lines 195-197) verbatim.

    (3) Lines ~201-207 block comment: update to reference the pair-return shape and to clarify that `useBountyCounts(null, ...)` short-circuits to `undefined` (the pair is `undefined` pre-fetch OR when identityKey is null).

    (4) Line ~513 render site: change `<PrettyBountyCountBadge count={pinnedCount} />` to `<PrettyBountyCountBadge pinnedCount={bountyCounts?.pinnedCount} needsDeskCount={bountyCounts?.needsDeskCount} />`. Preserve the `.pv-meta` slot and the enclosing block comment (lines ~502-512) noting the badge's null-when-both-zero contract; update the comment prose to describe the pair-based null rules per Plan 26-03 Task 1's rendering table.

    (5) Line 508 comment: change `useBountyCount above` to `useBountyCounts above`.

    In `src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx`:

    (6) Locate every `vi.mock("@/state/bounty-counts-store", ...)` block (search the test file for the string `bounty-counts-store`). Widen the mock so it exports `useBountyCounts` (plural) returning the pair. Update every per-test mock override to return the pair shape `{pinnedCount: N, needsDeskCount: M} | undefined` instead of a bare `number | undefined`.

    (7) Update every existing badge-visibility assertion to use the pair semantics from `<behavior>`. Add Tests A-F above as new `it(...)` cases (or fold into existing describe blocks if the file already has a "bounty badge visibility" describe).

    (8) If the row test file also mocks `useBountyCount` under the OLD name at multiple call sites, do a full search-and-replace within the test file — no legacy `useBountyCount` references remaining.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && npx vitest run src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useBountyCount\b" src/ui/features/pretty-conversations/PrettyConversationRow.tsx` returns 0 (old name retired everywhere in source, including comments).
    - `grep -c "useBountyCounts\b" src/ui/features/pretty-conversations/PrettyConversationRow.tsx` returns ≥ 2 (import + call site).
    - `grep -c "<PrettyBountyCountBadge count=" src/ui/features/pretty-conversations/PrettyConversationRow.tsx` returns 0 (old singular `count=` prop retired).
    - `grep -c "<PrettyBountyCountBadge pinnedCount=" src/ui/features/pretty-conversations/PrettyConversationRow.tsx` returns 1 (widened prop pass-through).
    - `grep -c "useBountyCount\b" src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` returns 0 (old mock name retired).
    - `npx vitest run src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` exits 0 with Tests A-F all passing.
    - After Task 1 + Task 2 both land: `npx tsc --noEmit` for `PrettyConversationRow.tsx` and `PrettyBountyCountBadge.tsx` is clean. Cross-repo typecheck may still show errors in `PrettyConversationsPanel.tsx` (that file's `useAllBountyCounts` consumer for the filter closes in Plan 26-04, running in parallel wave 3).
  </acceptance_criteria>
  <done>Row consumes the widened `useBountyCounts` hook, forwards the pair to the badge; the row test suite is green with 6 pair-shape tests locking each of the 4 rendering-rule cases + the pre-fetch + no-identity edge cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Store → row/badge | Pure client-side hook consumption. No new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05 | Information Disclosure | Rendered numeric counts on the row | accept | Counts are already visible on the row today (pinned only). Adding needs_desk exposes one additional integer per identity to the same viewer who already sees pinned. Single-tenant Skynet per PROJECT.md; no cross-user leak surface. |

**Attack surface note:** No new endpoints, no user input, no auth surface — purely a render refactor and hook rename. Threat model is lightweight per the `<security_context>` note in the planning context.
</threat_model>

<verification>
- `npx vitest run src/ui/features/pretty-conversations/PrettyBountyCountBadge.test.tsx src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` exits 0.
- After Task 1 + Task 2: pretty-view row cluster (badge + row) typechecks clean; the panel-level `useAllBountyCounts` consumer may still be typecheck-red — closed in the parallel Plan 26-04.
- Combined wave-3 completes when BOTH Plans 26-03 and 26-04 land; full-repo `npx tsc --noEmit` should exit 0 at that point.
</verification>

<success_criteria>
- `PrettyBountyCountBadge` renders the combined `pin·desk` pill per the 4-case rendering rule table (both, pinned-only, needs-desk-only, neither).
- `PrettyConversationRow` consumes `useBountyCounts` (plural) and forwards the pair to the badge.
- Row height and `.pv-meta` composition unchanged (outer `.pv-bounty-badge` chrome preserved; only `min-width` bumped to accommodate two digits).
- Both touched test files fully green; Tests locking each rendering-rule case.
</success_criteria>

<output>
Create `.planning/phases/26-conversation-list-needs-desk-count-filter-menu-add-per-row-n/26-03-SUMMARY.md` when done. Include: (a) exact new `PrettyBountyCountBadge` prop signature, (b) confirmation that the middle-dot character U+00B7 is present in source (not a hyphen/interpunct near-miss), (c) test-run output for badge + row test files, (d) any tuning notes on the CSS `min-width` bump (desktop/mobile) if the executor picked values different from the plan's 28px/44px suggestions.
</output>
