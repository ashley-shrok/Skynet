---
phase: 47-load-more-button-in-prettyview-manual-reveal-of-older-messag
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-view/LoadMoreOlderButton.tsx
  - src/ui/features/pretty-view/LoadMoreOlderButton.test.tsx
autonomous: true
requirements: []
tags:
  - pretty-view
  - ui-component
  - phase-47
must_haves:
  truths:
    - "A user can see a distinct button glyph at the top of a pretty-view message list when older messages exist behind the current view."
    - "A user clicking the button while the previous click is still in-flight has no effect (the click is blocked by the disabled state) — no duplicate requests can fire."
    - "A user whose last click failed sees a visible error affordance (aria-label + visible glyph state) and can click again to retry."
    - "A screen-reader user hears state-appropriate context on each of the three states via role=status + aria-label."
    - "In-flight state renders the repo-canonical twin-arc spinner SVG (per patch #467 / commit df4d7543), NOT lucide-react's Loader2."
  artifacts:
    - path: "src/ui/features/pretty-view/LoadMoreOlderButton.tsx"
      provides: "NEW presentational component — pure function of props (status + error + onClick); 3-state UI (idle/in-flight/error) with role=status + tri-branch aria-label; disabled during in-flight; retains click during error (retry contract)"
      contains: "export function LoadMoreOlderButton"
      min_lines: 60
    - path: "src/ui/features/pretty-view/LoadMoreOlderButton.test.tsx"
      provides: "NEW component test — 7 tests covering visibility gating, click handling, disabled-during-flight, spinner in in-flight, error state, retry-on-error, aria-label per state"
      contains: "describe(\"LoadMoreOlderButton"
      min_lines: 100
  key_links:
    - from: "src/ui/features/pretty-view/LoadMoreOlderButton.tsx"
      to: "src/ui/features/pretty-view/PrettyView.tsx (Plan 04 consumer)"
      via: "component import + <LoadMoreOlderButton status={loadOlderState} error={loadOlderError} onClick={handleLoadOlder} /> mount at top of scroll container"
      pattern: "LoadMoreOlderButton"
    - from: "src/ui/features/pretty-view/LoadMoreOlderButton.tsx"
      to: "src/ui/components/button (Button primitive)"
      via: "import { Button } from '@/components/button' — same import path used in DormancyOverlay.tsx"
      pattern: "Button"
---

<objective>
Ship the presentational `LoadMoreOlderButton` component + its component-level test suite in complete isolation from PrettyView.tsx integration. This is a pure function of props — no state, no effects, no refs, no WebSocket knowledge. The button knows how to render three visual states given `status: "idle" | "in-flight" | "error"` and how to invoke `onClick` when clicked-while-not-disabled; that's it.

Purpose: Wave-1 parallel with Plan 01 (no file overlap with any other plan). Frees Plan 04 (Wave 2) to focus on state management + scroll-anchoring + WS handler wiring without also having to design the component's visual grammar. Also lets the component's aria + retry + spinner details land under focused test coverage separately from the integration test coverage.

Output: One NEW component file + one NEW test file. Component is not yet mounted anywhere; that happens in Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-load-more-button-in-prettyview-manual-reveal-of-older-messag/47-CONTEXT.md
@.planning/phases/47-load-more-button-in-prettyview-manual-reveal-of-older-messag/47-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create LoadMoreOlderButton.test.tsx (RED — 7 failing tests before component exists)</name>
  <files>src/ui/features/pretty-view/LoadMoreOlderButton.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/AttachmentChipStrip.test.tsx (READ IN FULL — the DEFINITIVE analog for small-presentational-component tests: describe/it structure, render/screen/fireEvent imports, factory-helper pattern, presence + click + progress-state + error-variant test shapes)
    - src/ui/features/pretty-view/DormancyOverlay.tsx (READ L86-193 — the three-state discrimination logic + role=status + tri-branch aria-label pattern that Plan 02 Task 2 will replicate; the tests need to know what strings the aria-label produces)
    - src/ui/features/pretty-view/ComposeBox.tsx (READ L2551-2564 — the twin-arc spinner SVG that in-flight state renders; the test asserts this SVG shape, not lucide-react's Loader2)
    - .planning/phases/47-load-more-button-in-prettyview-manual-reveal-of-older-messag/47-PATTERNS.md § "src/ui/features/pretty-view/LoadMoreOlderButton.test.tsx (NEW — component test)" (L481-546)
  </read_first>
  <behavior>
    - Test 1: When `hasOlder={false}` (or the equivalent gating prop), component returns null (no wrapper DOM). Uses `expect(container.firstChild).toBeNull()`. Matches AttachmentChipStrip.test.tsx L17-22 pattern.
    - Test 2: When idle + hasOlder=true, component renders a Button element (`getByRole("button")` returns a match) with aria-label matching `/Load older messages/i`.
    - Test 3: Clicking the button when status=idle invokes the `onClick` prop exactly once with no arguments. Uses `vi.fn()` for onClick and `fireEvent.click`.
    - Test 4: Clicking the button when status="in-flight" does NOT invoke `onClick` (assert `onClick` mock has 0 calls after the click). This is the single-request-in-flight guard from CONTEXT.md § What would make it wrong.
    - Test 5: When status="in-flight", the twin-arc spinner SVG is present. Assertion: `container.querySelector("svg.animate-spin")` returns non-null AND that svg element has exactly 2 `<path>` children (matching the twin-arc shape from ComposeBox.tsx:2552-2564). Also assert: `container.querySelector("svg")` does NOT have the lucide `Loader2` circle+path shape — no assertion on the raw path `d=` attribute is needed if the 2-path count uniquely distinguishes twin-arc from Loader2.
    - Test 6: When status="error" and error="TIMEOUT", the aria-label matches `/couldn't load|failed|error/i` AND contains the string "TIMEOUT". Also assert the button is still clickable (`disabled` attribute not present or false).
    - Test 7: When status="error" and clicked, `onClick` is invoked (retry contract — error state does NOT block re-click, per CONTEXT.md § Fail visibly + § Philosophy "Fail visibly").
  </behavior>
  <action>
    Create `src/ui/features/pretty-view/LoadMoreOlderButton.test.tsx` following the AttachmentChipStrip.test.tsx structure verbatim.

    Imports (copy shape from AttachmentChipStrip.test.tsx L1-6):
      - `import { describe, it, expect, vi } from "vitest"`
      - `import { render, screen, fireEvent } from "@testing-library/react"`
      - `import { LoadMoreOlderButton } from "./LoadMoreOlderButton"`
      - `import type { LoadMoreOlderButtonProps } from "./LoadMoreOlderButton"` (the component MUST export this props type — see Task 2 action)

    Add a factory helper `makeProps(overrides: Partial<LoadMoreOlderButtonProps> = {}): LoadMoreOlderButtonProps` that returns default props: `hasOlder: true`, `status: "idle"`, `error: null`, `onClick: vi.fn()`. Overrides merge on top. Matches AttachmentChipStrip.test.tsx L9-15 `makeAtt` pattern.

    Then write the 7 `it(...)` blocks from the `<behavior>` list above. Assertion styles:
      - Test 1: `const { container } = render(<LoadMoreOlderButton {...makeProps({ hasOlder: false })} />); expect(container.firstChild).toBeNull();`
      - Test 2: `render(<LoadMoreOlderButton {...makeProps()} />); expect(screen.getByRole("button")).toBeTruthy(); expect(screen.getByRole("button").getAttribute("aria-label")).toMatch(/Load older messages/i);`
      - Test 3: `const onClick = vi.fn(); render(<LoadMoreOlderButton {...makeProps({ onClick })} />); fireEvent.click(screen.getByRole("button")); expect(onClick).toHaveBeenCalledTimes(1);`
      - Test 4: same setup with `status: "in-flight"`; assert `onClick).toHaveBeenCalledTimes(0)` after click. Note: HTML `disabled` attribute already blocks click events at the DOM level, so `fireEvent.click` on a disabled button fires the click event but React's onClick handler is not invoked by browser semantics; the test verifies this end-to-end.
      - Test 5: `const { container } = render(<LoadMoreOlderButton {...makeProps({ status: "in-flight" })} />); const svg = container.querySelector("svg.animate-spin"); expect(svg).not.toBeNull(); expect(svg?.querySelectorAll("path").length).toBe(2);`
      - Test 6: assert aria-label + not-disabled per Test 6 behavior above.
      - Test 7: same shape as Test 3 with `status: "error"`, `error: "TIMEOUT"`; assert `onClick).toHaveBeenCalledTimes(1)`.

    Group all 7 under one `describe("LoadMoreOlderButton", () => { ... })` block.

    Run the test file — ALL 7 SHOULD FAIL because LoadMoreOlderButton.tsx doesn't exist yet. That failure is the RED step; Task 2 makes them GREEN.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; npx vitest run src/ui/features/pretty-view/LoadMoreOlderButton.test.tsx 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `src/ui/features/pretty-view/LoadMoreOlderButton.test.tsx` exists.
    - `grep -c "^\s*it(" src/ui/features/pretty-view/LoadMoreOlderButton.test.tsx` returns 7 (exactly 7 test cases).
    - `grep -n "twin-arc\|animate-spin\|querySelectorAll.*path.*length.*2" src/ui/features/pretty-view/LoadMoreOlderButton.test.tsx` returns at least 1 match (Test 5 twin-arc discriminator present).
    - `grep -n "Loader2" src/ui/features/pretty-view/LoadMoreOlderButton.test.tsx` returns 0 matches (test does NOT reference Loader2 as an accepted spinner shape; it either asserts twin-arc affirmatively or is silent about lucide alternatives).
    - `npx vitest run src/ui/features/pretty-view/LoadMoreOlderButton.test.tsx` fails with all 7 tests red (module-not-found on `./LoadMoreOlderButton` is the expected root cause) — this is the RED gate that proves the tests would actually catch a missing implementation.
  </acceptance_criteria>
  <done>7 failing tests exist and fail because LoadMoreOlderButton.tsx doesn't exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create LoadMoreOlderButton.tsx implementation (GREEN — makes Task 1's 7 tests pass)</name>
  <files>src/ui/features/pretty-view/LoadMoreOlderButton.tsx</files>
  <read_first>
    - src/ui/features/pretty-view/DormancyOverlay.tsx (READ IN FULL — the STRUCTURAL analog: three-state component with role=status + tri-branch aria-label + Button + wrapper + non-null-on-mount posture. Copy layout shape; adapt the state labels + copy the twin-arc spinner for in-flight)
    - src/ui/features/pretty-view/ComposeBox.tsx (READ L2551-2564 — the exact twin-arc SVG markup to copy VERBATIM for the in-flight state)
    - src/ui/features/pretty-view/AsideBubble.tsx (READ L40-45 — the "presentational purity" comment establishing the convention: pure function of props, no hooks inside; this convention applies to LoadMoreOlderButton.tsx too)
    - src/ui/features/pretty-view/AttachmentChipStrip.tsx (READ IN FULL — a good example of a small presentational component that exports its Props type as `AttachmentChipStripProps` from the same file; LoadMoreOlderButton follows the same export pattern for `LoadMoreOlderButtonProps`)
    - src/ui/features/pretty-view/LoadMoreOlderButton.test.tsx (READ IN FULL — Task 1 output; the tests define the contract this task must satisfy)
    - .planning/phases/47-load-more-button-in-prettyview-manual-reveal-of-older-messag/47-PATTERNS.md § "src/ui/features/pretty-view/LoadMoreOlderButton.tsx (NEW — presentational component)" (L109-208)
  </read_first>
  <behavior>
    All 7 tests from Task 1 pass on `npx vitest run src/ui/features/pretty-view/LoadMoreOlderButton.test.tsx`. Specifically:
    - Test 1 GREEN: component returns `null` when `hasOlder === false`.
    - Test 2 GREEN: component renders a `<button>` (via Button primitive) with the expected aria-label when idle+hasOlder.
    - Test 3 GREEN: idle-state click invokes onClick.
    - Test 4 GREEN: in-flight-state click does NOT invoke onClick (disabled attribute enforces via native browser semantics).
    - Test 5 GREEN: in-flight-state renders the twin-arc SVG (svg with class `animate-spin` and exactly 2 `<path>` children).
    - Test 6 GREEN: error-state aria-label includes the error string.
    - Test 7 GREEN: error-state click invokes onClick (retry works).
  </behavior>
  <action>
    Create `src/ui/features/pretty-view/LoadMoreOlderButton.tsx` following DormancyOverlay.tsx's structure and the AsideBubble.tsx purity convention.

    Imports (mirror DormancyOverlay.tsx L56-58 pattern):
      - `import { cn } from "@/lib/utils"` (may not be needed for a minimal version; include only if class-composition is used)
      - `import { Button } from "@/components/button"` — same import path used across pretty-view. Verify by grepping `import.*Button.*from` in `src/ui/features/pretty-view/`.
      - `import type { JSX } from "react"` (or the current fleet convention — grep existing pretty-view components for JSX-return-type imports)
      - Do NOT import `Loader2` from `lucide-react` — the in-flight spinner is the twin-arc inline SVG, not a lucide glyph (Ashley 2026-08-19 commit df4d7543 rejected Loader2 for the wobbling-centroid issue).
      - MAY import a non-loading glyph like `ChevronUp` or `History` from `lucide-react` for the idle-state icon (planner's discretion — CONTEXT.md/PATTERNS.md do not lock this).

    Export the props type BY NAME (must match Task 1's `import type { LoadMoreOlderButtonProps }`):
      - `export type LoadMoreOlderButtonProps = { hasOlder: boolean; status: "idle" | "in-flight" | "error"; error: string | null; onClick: () => void; }`
      - Consider adding an optional `className?: string` if it aids the Plan 04 mount site; the test doesn't require it so it's discretionary.

    Component body (mirror DormancyOverlay.tsx L86-193 structure):
      1. Early return `null` when `!hasOlder` — matches Test 1. Do this BEFORE any other logic to keep the "no lie" invariant impossible to accidentally violate (CONTEXT.md § What would make it wrong: "the button's presence is a promise that clicking it will produce something").
      2. Derive `disabled` locally: `const disabled = status === "in-flight";` — this is the single-request-in-flight guard.
      3. Derive `aria-label` via tri-branch pattern (COPY the DormancyOverlay.tsx L99-105 shape verbatim in structure; substitute the strings):
         ```
         const ariaLabel =
           status === "error"
             ? `Couldn't load older messages — ${error ?? "unknown error"} — tap to retry`
             : status === "in-flight"
               ? "Loading older messages…"
               : "Load older messages";
         ```
         The `${error ?? "unknown error"}` fallback keeps the label meaningful when status=error but error prop is null.
      4. Wrap the return in a `<div>` (matches DormancyOverlay.tsx wrapper pattern L181-193). Include `role="status"` on the wrapper (COPY DormancyOverlay.tsx L98). Apply layout classes: pretty-view convention is centered horizontally at the top of the scroll container — `className="flex justify-center py-2"` (or the closest equivalent using existing pretty-view class conventions; grep the pretty-view files for `py-2` or `justify-center` to align with the existing rhythm).
      5. Inside the wrapper render a `<Button size="sm" variant="secondary" disabled={disabled} onClick={onClick} aria-label={ariaLabel}>` (mirror DormancyOverlay.tsx L182-191).
      6. Button children depend on `status`:
         - When `status === "in-flight"`: render the twin-arc spinner SVG COPIED VERBATIM from ComposeBox.tsx L2552-2564 (the exact 2-path shape). Alongside optionally a text label "Loading…" (planner's discretion; test doesn't require the label text — only the SVG shape).
         - When `status === "error"`: render an error glyph (planner picks — `AlertCircle` from lucide is a natural choice) alongside the text "Retry" — the visual+text combination makes the retry affordance discoverable per CONTEXT.md § Fail visibly. Ensure the visual glyph is a lucide icon so no risk of duplicating the twin-arc shape (Test 5 relies on twin-arc being unique to in-flight state).
         - When `status === "idle"`: render the chosen idle-state glyph (planner picks — `ChevronUp` or `History`) alongside text "Load older messages" (or shorter "Load older"; text is discretionary because Test 2 only asserts the aria-label match).

    Purity contract (mirror AsideBubble.tsx L40-45 comment): add a top-of-file JSDoc block explicitly stating the component is a pure function of props — no `useState`, no `useEffect`, no `useRef`, no `useCallback`, no internal event handlers. All state lives in the parent (Plan 04 PrettyView.tsx). This posture makes the component trivially testable and mount-safe under React.StrictMode.

    Do NOT add any effect that measures the DOM, subscribes to scroll events, or reads/writes WebSocket state. That coupling belongs in Plan 04.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tina &amp;&amp; npx vitest run src/ui/features/pretty-view/LoadMoreOlderButton.test.tsx 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `src/ui/features/pretty-view/LoadMoreOlderButton.tsx` exists.
    - `grep -n "export function LoadMoreOlderButton\|export const LoadMoreOlderButton" src/ui/features/pretty-view/LoadMoreOlderButton.tsx` returns exactly 1 match.
    - `grep -n "export type LoadMoreOlderButtonProps\|export interface LoadMoreOlderButtonProps" src/ui/features/pretty-view/LoadMoreOlderButton.tsx` returns exactly 1 match (satisfies Task 1's `import type` requirement).
    - `grep -c "useState\|useEffect\|useRef\|useCallback" src/ui/features/pretty-view/LoadMoreOlderButton.tsx` returns 0 (purity contract preserved — no hooks inside a presentational component).
    - `grep -n "Loader2" src/ui/features/pretty-view/LoadMoreOlderButton.tsx` returns 0 matches (twin-arc convention enforced).
    - `grep -c "M21 12 A9 9 0 0 0 12 3\|M3 12 A9 9 0 0 0 12 21" src/ui/features/pretty-view/LoadMoreOlderButton.tsx` returns 2 (both twin-arc path `d=` attribute values present verbatim from ComposeBox.tsx:2557-2558).
    - `grep -n "role=\"status\"" src/ui/features/pretty-view/LoadMoreOlderButton.tsx` returns exactly 1 match.
    - `npx vitest run src/ui/features/pretty-view/LoadMoreOlderButton.test.tsx` exits 0 with all 7 tests green.
    - `npx tsc --noEmit` exits 0 (component + test both typecheck).
  </acceptance_criteria>
  <done>Component compiles and all 7 tests from Task 1 pass green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM → component | Click events on the rendered button; component invokes `onClick` prop only when `!disabled`; native browser semantics enforce the disabled gate. |
| props → aria-label | The `error` prop is interpolated into the aria-label; a malicious `error` string could inject through screen-reader output (low severity — no XSS since aria-label is not HTML-parsed). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-06 | Denial of Service | Click storm during in-flight | mitigate | HTML `disabled` attribute + React onClick suppression when `status === "in-flight"`; Test 4 verifies. |
| T-47-07 | Information Disclosure | error string in aria-label | accept | Screen-reader receives the raw error text; no XSS surface (aria-label is text-only). Error strings originate from server-side handler (Plan 03) which returns short structured messages, not stack traces. |
| T-47-08 | Repudiation | User claims "I clicked but nothing happened" | mitigate | Fail-visibly requirement: error state renders a distinct aria-label + visible glyph + retains clickability — user can retry immediately and no failure is silent. Test 6+7 lock this. |
</threat_model>

<verification>
- `npx vitest run src/ui/features/pretty-view/LoadMoreOlderButton.test.tsx` exits 0 with 7/7 tests passing.
- `npx tsc --noEmit` clean.
- Twin-arc-SVG-not-Loader2 grep gates pass.
- Purity contract grep gate (no react hooks in the component) passes.
</verification>

<success_criteria>
1. `LoadMoreOlderButton.tsx` exists as a pure-props presentational component with the exact `LoadMoreOlderButtonProps` type.
2. `LoadMoreOlderButton.test.tsx` exists with 7 tests, all green.
3. In-flight state renders the twin-arc SVG (per patch #467 / commit df4d7543), NOT lucide-react's Loader2.
4. Error state remains clickable (retry contract per CONTEXT.md § Fail visibly).
5. Component returns null when `hasOlder === false` (no-lie invariant per CONTEXT.md § What would make it wrong).
6. Component has zero React hooks (purity contract preserved).
</success_criteria>

<output>
Create `.planning/phases/47-load-more-button-in-prettyview-manual-reveal-of-older-messag/47-02-SUMMARY.md` when done, documenting: (a) the chosen idle-state glyph (ChevronUp / History / other), (b) the chosen error-state glyph (AlertCircle / other), (c) the final visible button label strings if any (or note "aria-label only, no visible label"), (d) the exact wrapper classes used for mounting-site alignment.
</output>
