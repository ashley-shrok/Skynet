---
phase: 07-fleet-native-conversation-list
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/ui/state/conversation-store.ts
  - src/ui/state/conversation-store.test.ts
  - src/ui/sidebar/ConversationsPanel.tsx
  - src/ui/sidebar/NewSessionButton.tsx
  - src/ui/AppShell.tsx
autonomous: true
requirements: [TG-15, TG-16, TG-18]

must_haves:
  truths:
    - "Remote-desktop host rows sit at the BOTTOM of the conversation list — one row per RDP-enabled host, rendered with a monitor icon in the avatar slot (no identity hue, no identity name, just the host name + monitor glyph)"
    - "An RDP row exists as long as the host's `enableRdp === true` flag is set, regardless of whether an RDP tab is currently open in this browser tab. Toggling `enableRdp` off (via host edit) makes the row vanish; toggling it on makes it appear (after realHostTree refresh)"
    - "Clicking an RDP row opens the RDP tab using the existing tab lifecycle mechanism (openTab(host, 'rdp')) — no changes to RDP disconnect/reconnect behavior, no changes to guacd/guacamole, no changes to Terminal.tsx or pretty-view"
    - "The existing New Session button is re-styled as the Telegram-native pencil: the Plus icon becomes a Pencil (Pencil / Edit / PenTool from lucide-react — planner's discretion within the Telegram-native vocabulary). Function is UNCHANGED — button still opens the same NewSessionDialog with the same host picker + session-name flow (Plan 06-04 machinery preserved verbatim)"
    - "On mobile viewports (`useIsTouchDevice() === true`), the gear icon in the ConversationsPanel header does NOT render; the settings row in the panel scroller (Plan 06-03's SettingsRow mount) is the SOLE admin/settings entry point on mobile"
    - "On desktop viewports (`useIsTouchDevice() === false`), the gear icon in the ConversationsPanel header DOES render; the SettingsRow inside the scroller does NOT render (AppShell's existing gate at line 1348 already ensures this — no change needed there). The gear is the SOLE admin/settings entry point on desktop"
    - "Both entry points continue to route to the same SETTINGS_MENU_ITEMS registry (Plan 06-02's shared renderSettingsMenuItems helper) — this plan does NOT modify what happens inside the menu, only which of the two entry points renders per viewport"
    - "Every existing Phase 6 + Plan 07-01 behavior preserved verbatim: fleet-native data source (from 07-01), attached/detached visual sameness, click-detached transparent attach, per-session pins, host grouping with separators, mobile list-vs-view flow, sidebar collapse, session persistence, URL fragments, T-06-02-01 mount-lifecycle contract, all Phase 6 UAT items"
  artifacts:
    - path: "src/ui/state/conversation-store.ts"
      provides: "RDP row derivation from state.hostsFlat — one synthetic row per host where host.enableRdp === true, emitted at the BOTTOM of the ConversationList (below all identity-tmux HostGroups, below all fleet-only rows). RDP rows carry a new optional marker `rdpHostRow?: boolean` (analog of Plan 07-01's fleetOnly flag) so ConversationsPanel can route their click to a Host-typed RDP open handler."
      contains: "rdpHostRow"
    - path: "src/ui/state/conversation-store.test.ts"
      provides: "Vitest cases proving: (a) enableRdp: true host emits one RDP row at the bottom of the derived list, (b) enableRdp: false host does NOT emit an RDP row, (c) RDP rows appear AFTER all HostGroups in the derived output, (d) RDP row count matches count of enableRdp hosts (one row per host)"
      min_new_tests: 4
    - path: "src/ui/sidebar/ConversationsPanel.tsx"
      provides: "showGear gate updated to include `!useIsTouchDevice()` (TG-18 mobile fix); RDP row rendering branch below the grouped section, rendering as a ConversationRow with a monitor-icon avatar override; onRdpRowClick prop wired to handle RDP row taps via AppShell"
      contains: "useIsTouchDevice"
      contains_2: "onRdpRowClick"
      contains_3: "rdpHostRow"
    - path: "src/ui/sidebar/NewSessionButton.tsx"
      provides: "Plus icon replaced with Pencil (or PenTool / SquarePen — Telegram-native pencil vocabulary; planner picks one and commits). Icon size + button className preserved verbatim to keep visual weight consistent with Plan 06-04's placement."
      contains: "Pencil"
    - path: "src/ui/AppShell.tsx"
      provides: "onRdpRowClick handler passed to ConversationsPanel mount: resolves row → Host (via same row.host that the store populated via hostsFlat) → openTab(host, 'rdp'); mobile navigateToView + isMobile setSidebarOpen(false)"
      contains: "onRdpRowClick"
  key_links:
    - from: "src/ui/state/conversation-store.ts::computeSnapshot"
      to: "state.hostsFlat"
      via: "iterate hostsFlat entries + filter host.enableRdp === true + emit synthetic RDP rows at BOTTOM of grouped section"
      pattern: "enableRdp"
    - from: "src/ui/sidebar/ConversationsPanel.tsx"
      to: "@/hooks/use-is-touch-device::useIsTouchDevice"
      via: "showGear gate additive term"
      pattern: "useIsTouchDevice"
    - from: "src/ui/sidebar/NewSessionButton.tsx"
      to: "lucide-react::Pencil"
      via: "icon import + JSX replacement"
      pattern: "Pencil|PenTool|SquarePen"
    - from: "src/ui/AppShell.tsx"
      to: "openTab(host, 'rdp')"
      via: "onRdpRowClick handler in ConversationsPanel mount"
      pattern: "onRdpRowClick"
---

<objective>
Complete the Phase 7 UAT-visible surface: (a) render one RDP-host row per RDP-enabled host at the bottom of the conversation list with a monitor-icon avatar and no identity hue, clicking to open the RDP tab via the existing tab lifecycle; (b) re-style the existing NewSessionButton's Plus icon as the Telegram-native pencil (function unchanged); (c) fix the Phase 6 mobile gear/settings-row duplication by gating `showGear` on `!useIsTouchDevice()` in addition to the existing `onRailClick` typeof check.

Purpose: TG-15, TG-16, TG-18. This is the second and final source-modifying plan of Phase 7. Together with Plan 07-01's data-source reshape, this delivers every scope-of-Phase-7 requirement.

Output: RDP rows emitted at the BOTTOM of the derived ConversationList by the conversation-store (using state.hostsFlat from Plan 07-01), rendered by ConversationsPanel with a monitor-icon override; NewSessionButton with a pencil glyph; ConversationsPanel showGear gate that renders gear on desktop only + preserves the existing mobile SettingsRow-only rendering. Zero touches to guacamole backend, RDP tab lifecycle, pretty-view, terminal, or docker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-fleet-native-conversation-list/07-CONTEXT.md
@.planning/shapes/shape-fleet-native-conversation-list.md
@.planning/phases/07-fleet-native-conversation-list/07-01-PLAN.md
@.planning/phases/06-telegram-like-interface/06-02-SUMMARY.md
@.planning/phases/06-telegram-like-interface/06-03-SUMMARY.md
@.planning/phases/06-telegram-like-interface/06-04-SUMMARY.md
@src/ui/state/conversation-store.ts
@src/ui/sidebar/ConversationsPanel.tsx
@src/ui/sidebar/ConversationRow.tsx
@src/ui/sidebar/NewSessionButton.tsx
@src/ui/AppShell.tsx
@src/types/ui-types.ts
</context>

<hard_constraints>
- **NO deploy this plan.** Plan 07-03 is the deploy checkpoint.
- **Depends on Plan 07-01 being merged.** This plan extends `computeSnapshot()` in conversation-store which Plan 07-01 restructured; adds cases to `conversation-store.test.ts` after Plan 07-01's Tests 23-30; extends `ConversationsPanel.tsx` where Plan 07-01 added `onDetachedRowClick`; extends `AppShell.tsx` where Plan 07-01 added `hostsById` + onDetachedRowClick handler. Executing this plan requires Plan 07-01's commits present.
- **Tab lifecycle UNTOUCHABLE per CONTEXT.md §Scope Fence item #1.** RDP-row click CALLS `openTab(host, 'rdp')` (existing entry point — Plan 06-04 established this as the canonical open-a-tab path) — do NOT introduce a new RDP-specific tab-lifecycle mechanism, do NOT modify openTab's signature, do NOT bypass PERSISTENT_TAB_TYPES. Existing RDP disconnect/reconnect behavior stays byte-for-byte.
- **NO touches to guacamole.** `src/ui/features/guacamole/**` remains at 0 changes. RDP row rendering is a NEW row-render path in ConversationsPanel; it CALLS the existing RDP open flow. The RDP tab component (guacamole/RemoteDesktop.tsx or whatever the current name is) is untouched.
- **NO changes to what the settings menu contains or does.** The SETTINGS_MENU_ITEMS registry from Plan 06-02 (SettingsRow.tsx) is untouched. This plan only changes WHICH entry point renders where, not the menu contents. The plan-06-02 shared renderer + registry stays canonical.
- **NO changes to NewSessionDialog.** Plan 06-04's dialog (host picker + name input + validation + Cancel/Open) is untouched. Only NewSessionButton's icon changes.
- **NO changes to Terminal.tsx, pretty-view, Terminal WS layer, session-file tail, or backend.** Same fences as Phase 6 + Plan 07-01.
- **NO new npm dependencies.** Pencil icon is already available in `lucide-react` (existing dep — verified: `grep -c 'lucide-react' package.json` is 1). Use it directly.
- **Scope fence:** ZERO touches to `src/ui/features/pretty-view/**`, `src/ui/features/terminal/Terminal.tsx`, `src/ui/features/guacamole/**`, `src/backend/**`, `docker/**`, `package.json`, `package-lock.json`. Verified via grep gates.
- **RDP row: NO identity hue, NO identity avatar, NO identity name.** Per shape file: monitor icon in the avatar slot, host name as the row label. ConversationRow's existing identity resolution (via useIdentities + sessionMatchKey) MUST be bypassed for RDP rows. Either a `rdpHostRow` prop on ConversationRow that overrides its icon rendering, or a dedicated small RdpConversationRow component. Planner's discretion — pick whichever keeps the diff smallest.
- **RDP rows can NOT be pinned.** Same rationale as fleet-only rows (Plan 07-01): pinning requires an id that survives across page loads within session lifetime, and RDP-row ids are `rdp-host::${hostId}` (deterministic per host, but not tab-scoped). Existing pinConversation guard (rejects ids not in openTabs) already covers this — verify with a Vitest case, do not add explicit rejection code.
</hard_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend conversation-store with RDP row derivation from hostsFlat + Vitest cases</name>
  <files>src/ui/state/conversation-store.ts, src/ui/state/conversation-store.test.ts</files>
  <read_first>
    - src/ui/state/conversation-store.ts (post-Plan-07-01 state — the file with FleetSession + updateFleetSessions + updateHostsFlat + fleetOnly-marked rows already added)
    - src/ui/state/conversation-store.test.ts (post-Plan-07-01 state — Tests 1-30 all present)
    - src/types/ui-types.ts line 106 (`enableRdp: boolean` field on Host)
    - .planning/phases/07-fleet-native-conversation-list/07-01-PLAN.md §"NEW derived ConversationRow shape extension" — you are adding a THIRD optional row-shape marker analog to `fleetOnly`
    - .planning/shapes/shape-fleet-native-conversation-list.md §Shape (bullet 3 — "one row per RDP-enabled host"; bullet 4 — "Row content: monitor icon + host name. No identity hue, no avatar, no identity name")
    - CONTEXT.md §decisions §"RDP row rendering (TG-15)" (row content locked; placement bottom-of-list; row persistence tied to enableRdp flag NOT tab state; pin capability = planner's call → NO pin)
  </read_first>
  <behavior>
    Store extension:

    1. NEW row shape marker: extend the existing `ConversationRow` type with ONE more optional field: `rdpHostRow?: boolean`
       - `rdpHostRow === true` marks a row synthesized from a Host with `enableRdp === true`
       - Row shape: 5 core keys + optional `fleetOnly` (from Plan 07-01) + optional `rdpHostRow` (this plan)
       - Test 8's row-shape assertion (already updated in Plan 07-01 to filter out `fleetOnly`) MUST also filter out `rdpHostRow`

    2. NEW derived `computeSnapshot()` extension — extend the function to emit RDP rows AFTER all HostGroups (both openTabs + fleet-only). Implementation shape:
       - After the existing fleet+openTabs HostGroup emission loop, iterate `state.hostsFlat.entries()` in host-tree order (use `collectHostOrder(state.hostTree)` for deterministic ordering; hosts not in the tree — orphan RDP hosts — emit at the end in hostsFlat iteration order, sibling of the existing orphan-tab fallback)
       - For each Host where `host.enableRdp === true`, emit a synthetic ConversationRow:
         - `id: "rdp-host::" + host.id` (deterministic per host; matches the fleet id convention pattern)
         - `type: "rdp"` (from TabType)
         - `label: host.name` (the host name is the row label — matches shape file "just the host name + monitor glyph")
         - `host: host` (the Host object itself)
         - `targetTmuxSession: null` (RDP has no tmux session concept)
         - `rdpHostRow: true`
       - These rows go into a NEW field of the ConversationList shape — but the simplest option is to append them as one FINAL HostGroup at the end of `grouped`, with `hostId: "__rdp__"` (sentinel host id) and `hostName: ""` (empty string — the ConversationsPanel render path detects `hostId === "__rdp__"` and renders the RDP rows as a bottom section without a host header).
       - Alternative: extend ConversationList shape with a NEW optional field `rdpHosts: ConversationRow[]` alongside `pinned` + `grouped`. Cleaner separation but requires the panel to consume the new field. Planner's recommendation: use the `hostId: "__rdp__"` sentinel-group approach to minimize the ConversationList type diff; ConversationsPanel already iterates `grouped.map(...)` and can special-case `group.hostId === "__rdp__"` in the render path.
       - CHOOSE ONE approach and commit. Document the choice in the SUMMARY.
       - Rows are emitted in host-tree order (matches the identity-tmux group ordering).

    3. NO change to pins — RDP rows are NOT pinnable. The existing pinConversation guard already rejects ids not in openTabs; verify this via a Vitest case (Test 34 below).

    4. NO change to `updateHostsFlat` action (it already exists from Plan 07-01). RDP row derivation is a READ of `state.hostsFlat` inside `computeSnapshot`.

    5. Preserve every existing action + hook + test-only helper verbatim. Additive-only.

    Tests to add to conversation-store.test.ts (append after Plan 07-01's Test 30):

    - **Test 31 (RDP row emission):** with hostsFlat containing hostA(enableRdp=true) + hostB(enableRdp=false), the derived ConversationList's RDP section (either `grouped` with `hostId === "__rdp__"` OR the new `rdpHosts` field, per planner's decision above) contains exactly 1 row for hostA with `rdpHostRow === true`, `label === "hostA"`, `id === "rdp-host::" + hostA.id`, and NO row for hostB.
    - **Test 32 (RDP rows appear AFTER all openTabs+fleet HostGroups):** with openTabs=[terminal on hostA], fleetSessions=[work on hostA], hostsFlat=[hostA(enableRdp=true), hostB(enableRdp=true)], the derived ConversationList orders: (openTabs row for hostA) → (fleet-only row for hostA "work") → (RDP row for hostA + RDP row for hostB). Assert this via inspection of grouped order or the concatenated (grouped + rdpHosts) sequence.
    - **Test 33 (RDP row vanishes on enableRdp: false):** call `updateHostsFlat(Map[hostA(enableRdp=true)])` → assert 1 RDP row present. Then call `updateHostsFlat(Map[hostA(enableRdp=false)])` → assert 0 RDP rows. Simulates Ashley toggling RDP off in the host editor.
    - **Test 34 (RDP rows cannot be pinned):** call `pinConversation("rdp-host::" + hostA.id)` → assert `state.pinnedIds` remains empty (existing guard at conversation-store.ts:350-358 rejects ids not in openTabs; RDP row ids never appear in openTabs). This is a defense-in-depth regression test.

    All existing Tests 1-30 MUST still pass unchanged.
  </behavior>
  <action>
    Per D-07 (RDP rows at bottom, monitor icon, no hue, no name), D-08 (row persistence tied to enableRdp flag), extend `src/ui/state/conversation-store.ts` with the `rdpHostRow?: boolean` optional field on the ConversationRow type; extend `computeSnapshot()` to iterate `state.hostsFlat` filtered on `host.enableRdp === true`, emit synthetic rows with `id: "rdp-host::" + host.id`, and place them at the BOTTOM of the derived list. Use the sentinel HostGroup approach (`hostId: "__rdp__"`) OR extend the ConversationList type with an `rdpHosts` field — planner picks and commits, documenting in SUMMARY.

    Append 4 new Vitest cases (Tests 31-34) to `src/ui/state/conversation-store.test.ts`. Update the row-shape filter (from Plan 07-01) to ALSO filter out `rdpHostRow` when asserting against the locked 5-key set. Do NOT modify Tests 1-30.

    Zero touches to actions or hooks — additive derivation only. Zero touches to `src/ui/sidebar/`, `src/ui/AppShell.tsx`, `src/ui/features/`, `src/backend/`.
  </action>
  <verify>
    <automated>
      # Row shape extension
      test $(grep -c "rdpHostRow" src/ui/state/conversation-store.ts) -ge 3 &&
      test $(grep -c "enableRdp" src/ui/state/conversation-store.ts) -ge 2 &&
      test $(grep -c "rdp-host::" src/ui/state/conversation-store.ts) -ge 2 &&
      # Test count grew by 4
      test $(grep -c "^  it(" src/ui/state/conversation-store.test.ts) -ge 34 &&
      test $(grep -cE "rdpHostRow|rdp-host::|enableRdp" src/ui/state/conversation-store.test.ts) -ge 4 &&
      # Vitest passes
      npx vitest run src/ui/state/conversation-store.test.ts 2>&1 | grep -qE "([3-9][0-9]|[1-9][0-9]{2,}) passed" &&
      # Type-check
      npx tsc --noEmit --skipLibCheck 2>&1 | grep "error TS" | wc -l | grep -qE "^0$" &&
      # No polling regression
      test $(grep -cE "setInterval|setTimeout" src/ui/state/conversation-store.ts) -eq 0 &&
      # Scope fence
      git diff --stat src/ui/features/pretty-view/ src/ui/features/terminal/Terminal.tsx src/ui/features/guacamole/ src/backend/ docker/ package.json package-lock.json 2>&1 | grep -qE "\|" && exit 1 || true
    </automated>
  </verify>
  <done>
    - `rdpHostRow?: boolean` optional field added to ConversationRow type
    - `computeSnapshot()` extended to emit synthetic RDP rows for hosts with `enableRdp === true`, positioned BELOW all HostGroups
    - RDP row ids follow `rdp-host::${host.id}` convention
    - Placement approach chosen (sentinel HostGroup OR new `rdpHosts` field) and documented
    - 4 new Vitest cases (Tests 31-34) pass; all pre-existing Tests 1-30 still pass
    - Type-check clean; scope fence intact; no polling
  </done>
</task>

<task type="auto">
  <name>Task 2: NewSessionButton pencil re-style + ConversationsPanel gear-mobile-fix + RDP row rendering + onRdpRowClick prop + AppShell handler wire-up</name>
  <files>src/ui/sidebar/NewSessionButton.tsx, src/ui/sidebar/ConversationsPanel.tsx, src/ui/AppShell.tsx</files>
  <read_first>
    - src/ui/sidebar/NewSessionButton.tsx (34 lines — you change ONE import + ONE JSX line; do NOT restructure the button chrome)
    - src/ui/sidebar/ConversationsPanel.tsx (post-Plan-07-01 state — has `onDetachedRowClick` prop + `handleRowSelect` helper; you extend it further with the mobile gear gate + RDP row rendering + onRdpRowClick prop)
    - src/ui/sidebar/ConversationRow.tsx (150 lines per Plan 06-01 SUMMARY — check whether it already accepts an icon-override prop; if not, add ONE prop `iconOverride?: ReactNode` OR handle the RDP-row rendering as a separate small inline `<button>` in ConversationsPanel that mirrors ConversationRow's chrome without the identity resolution. Planner's discretion — pick whichever is smaller. Recommend the inline path: RDP rows are visually distinct enough (no hue, no avatar, just monitor icon) that reusing ConversationRow with heavy prop overrides may be lossier than a small parallel render path.)
    - src/ui/hooks/use-is-touch-device.ts (verify the hook signature — planner's expectation: `export function useIsTouchDevice(): boolean`)
    - src/ui/AppShell.tsx (post-Plan-07-01 state — has the fleet fetch effect + hostsById memo + updateHostsFlat effect + onDetachedRowClick handler in the ConversationsPanel mount site around line 1345-1372; you add ONE more prop passthrough: `onRdpRowClick`)
    - .planning/phases/07-fleet-native-conversation-list/07-CONTEXT.md §decisions §"RDP row rendering (TG-15)", §"Pencil re-style (TG-16)", §"Mobile gear/settings-row duplication fix (TG-18)"
    - lucide-react (confirm which pencil-family icons exist — plausible names: `Pencil`, `PencilLine`, `PenTool`, `SquarePen`, `Edit`, `Edit2`, `Edit3`. Pick ONE that reads as Telegram-native pencil and commits.)
  </read_first>
  <behavior>
    **NewSessionButton.tsx changes:**

    1. Change import line from `import { Plus } from "lucide-react";` to `import { Pencil } from "lucide-react";` (or `SquarePen`, `PenTool` — planner picks; recommend `Pencil` as the most Telegram-native).
    2. Change JSX line `<Plus className="size-3 shrink-0" />` to `<Pencil className="size-3 shrink-0" />` (preserve size + shrink-0 verbatim so the button chrome is byte-identical except the icon).
    3. Everything else — `type`, `onClick`, `title`, `aria-label`, `className`, `t("nav.newSession")` — UNCHANGED.

    **ConversationsPanel.tsx changes:**

    1. Add `import { useIsTouchDevice } from "@/hooks/use-is-touch-device";` to the imports.
    2. Change `const showGear = typeof onRailClick === "function";` to `const showGear = typeof onRailClick === "function" && !useIsTouchDevice();`.
    3. Add NEW optional prop `onRdpRowClick?: (row: ConversationRow) => void;` to the props interface (analog of onDetachedRowClick).
    4. In `handleRowSelect(row)` (introduced by Plan 07-01), extend the branch: check `row.rdpHostRow` BEFORE `row.fleetOnly`:
       - `if (row.rdpHostRow && onRdpRowClick) { onRdpRowClick(row); onConversationSelected?.(row.id); return; }`
       - `if (row.fleetOnly && onDetachedRowClick) { onDetachedRowClick(row); onConversationSelected?.(row.id); return; }`
       - Else: existing `selectConversation(row.id); onConversationSelected?.(row.id);`
    5. Render the RDP rows section. Depending on Task 1's decision:
       - **If sentinel HostGroup:** in the existing `{grouped.map((group) => (...))}` block, when `group.hostId === "__rdp__"`, render each row using a small inline RdpRow renderer (monitor icon + host name + no hue + no avatar) OR reuse ConversationRow with a `rdpHostRow` prop that internally suppresses identity rendering. Do NOT render the semibold host-header line for the __rdp__ group (there's no meaningful group name — the shape file said "just the host name + monitor glyph").
       - **If new `rdpHosts` field:** destructure `rdpHosts` from `useConversations()` alongside `pinned` + `grouped`, and render it as a separate block AFTER `{grouped.map(...)}` and BEFORE `{settingsRowSlot}`. Same visual chrome: monitor icon + host name, no header separator, one row per host.
    6. Use `Monitor` icon from lucide-react for the RDP avatar slot (already available; grep-verify: `grep -rn "Monitor" src/ui --include="*.tsx" | head -3` will show existing use in the fork). If Monitor is not already imported to lucide-react bindings, add it to the imports.
    7. RDP row `onClick` fires `handleRowSelect(row)` — routes through the shared helper to onRdpRowClick.
    8. RDP row visual: same row height as ConversationRow (h-9 or whatever the existing row height is — inspect ConversationRow.tsx and match). No hover-reveal pin toggle (RDP rows are not pinnable). No `selected` state visual (RDP rows can be selected if their opened RDP tab is selected; the `selected` visual should still apply — the store already emits selectedId; check row.id against selectedId in the render).
    9. DO NOT touch settingsRowSlot render (still renders below grouped/rdpHosts, before empty-state). DO NOT touch NewSessionButton render. DO NOT touch the pinned section. DO NOT touch the empty-state branch (RDP rows still render when isEmpty is true — an "empty" list of tmux sessions is still "empty of conversations" but the RDP rows exist as fleet facts; consider whether isEmpty should be redefined to `pinned.length === 0 && grouped.length === 0 && rdpHosts.length === 0` — planner's call. Recommend YES — an empty state message alongside visible RDP rows would be confusing; suppress the empty message when RDP rows exist).

    **AppShell.tsx changes:**

    1. In the ConversationsPanel mount site (currently ending at line 1372-1373 area after `onCreateSession`), ADD ONE new prop between the existing props (order in file doesn't matter, but grouping with the other row-click handlers is idiomatic):
       ```tsx
       // Plan 07-02 (TG-15): RDP row click opens the RDP tab via the
       // existing openTab lifecycle. row.host is guaranteed populated by
       // the store (RDP rows are synthesized from state.hostsFlat entries
       // where host.enableRdp === true). Mobile navigateToView + isMobile
       // setSidebarOpen(false) match onCreateSession + onDetachedRowClick.
       onRdpRowClick={(row) => {
         const host = row.host;
         if (!host) return;
         openTab(host, "rdp");
         if (isTouchDevice) navigateToView();
         if (isMobile) setSidebarOpen(false);
       }}
       ```
    2. NO other AppShell changes. NO new imports (openTab, isTouchDevice, isMobile, navigateToView, setSidebarOpen — all already in scope for the Plan 06/07-01 handlers).
    3. `openTab(host, "rdp")` intentionally OMITS the `options` arg — RDP tabs don't use targetTmuxSession or allowCreateTmux. `openTab` already defaults these to null/false when omitted (verified from AppShell.tsx:918-923).
    4. NO `selectConversationDeferred` call for RDP row clicks — `openTab` calls `setActiveTabId(tabId)` at line 979, which flows into the AppShell→store mirror effect and updates the visible pane. The pendingSelectId race defense from Plan 06-04 is specifically for the new-session flow's `openTab → immediate selectConversation` batched-setTabs race; the RDP row click doesn't need it because AppShell's own setActiveTabId path is synchronous (well, batched-then-effect, but the effect fires on the same render pass). Rely on Plan 06-02's store↔AppShell mirror effect to keep selectedId in sync with activeTabId — the same way connectHost / URL-restore / keyboard nav work today. If UAT reveals a visible race (RDP row click briefly shows the old pane before the new one), Plan 07-03 can amend to use selectConversationDeferred here. Ship the simpler path first.
    5. Actually — reconsider. The detached-row-click handler in Plan 07-01 uses `selectConversationDeferred(newTabId)`. For consistency, the RDP row handler SHOULD also use `selectConversationDeferred(openTab(host, "rdp"))`. Concrete gain: symmetric behavior across all "opens-a-new-tab" surfaces; concrete cost: one extra call. Recommend: DO use selectConversationDeferred here — the consistency win outweighs the marginal complexity, and the pendingSelectId slot is already imported into AppShell.
    6. UPDATED handler:
       ```tsx
       onRdpRowClick={(row) => {
         const host = row.host;
         if (!host) return;
         const newTabId = openTab(host, "rdp");
         selectConversationDeferred(newTabId);
         if (isTouchDevice) navigateToView();
         if (isMobile) setSidebarOpen(false);
       }}
       ```
  </behavior>
  <action>
    Per D-09 (pencil re-style — Telegram-native, function unchanged), D-10 (mobile gear/settings-row dedup — showGear += !useIsTouchDevice()), D-11 (RDP row rendering at bottom with monitor icon + no hue + no name), D-12 (RDP click uses openTab('rdp') + selectConversationDeferred for symmetry with detached-row and new-session flows):

    In `src/ui/sidebar/NewSessionButton.tsx`, swap `Plus` for `Pencil` (or planner-chosen Telegram-native pencil-family icon from lucide-react) in the import line and the JSX. Preserve all other button chrome verbatim.

    In `src/ui/sidebar/ConversationsPanel.tsx`, (a) import `useIsTouchDevice` from `@/hooks/use-is-touch-device`; (b) update `showGear` gate to `typeof onRailClick === "function" && !useIsTouchDevice()` (TG-18 fix); (c) add `onRdpRowClick?: (row: ConversationRow) => void` optional prop; (d) extend `handleRowSelect(row)` to route `row.rdpHostRow` → onRdpRowClick BEFORE routing `row.fleetOnly` → onDetachedRowClick BEFORE the default selectConversation path; (e) render RDP rows per Task 1's decision (sentinel HostGroup OR new `rdpHosts` field on ConversationList) as a bottom section with monitor icon + host name + no header separator + no hue; (f) update `isEmpty` derivation to include the RDP row count (an empty state message should not render when RDP rows are visible).

    In `src/ui/AppShell.tsx`, add ONE new prop `onRdpRowClick` to the ConversationsPanel mount site: handler resolves `row.host` → openTab(host, "rdp") → selectConversationDeferred(newTabId) → mobile navigateToView → isMobile setSidebarOpen(false). Zero other AppShell changes.

    Zero touches to Terminal.tsx, pretty-view, guacamole, backend, docker, package.json, NewSessionDialog.tsx, ConversationRow.tsx internals beyond an optional icon-override prop if that path is chosen.
  </action>
  <verify>
    <automated>
      # NewSessionButton pencil re-style
      test $(grep -c "^import { Plus }" src/ui/sidebar/NewSessionButton.tsx) -eq 0 &&
      test $(grep -cE "Pencil|PenTool|SquarePen|Edit[0-9]?" src/ui/sidebar/NewSessionButton.tsx) -ge 2 &&
      test $(grep -c "<Plus " src/ui/sidebar/NewSessionButton.tsx) -eq 0 &&
      # ConversationsPanel gear gate updated
      test $(grep -c "useIsTouchDevice" src/ui/sidebar/ConversationsPanel.tsx) -ge 2 &&
      test $(grep -c 'showGear = typeof onRailClick === "function" && !useIsTouchDevice()' src/ui/sidebar/ConversationsPanel.tsx) -eq 1 &&
      # ConversationsPanel onRdpRowClick + RDP rendering
      test $(grep -c "onRdpRowClick" src/ui/sidebar/ConversationsPanel.tsx) -ge 3 &&
      test $(grep -c "rdpHostRow" src/ui/sidebar/ConversationsPanel.tsx) -ge 1 &&
      test $(grep -cE "Monitor" src/ui/sidebar/ConversationsPanel.tsx) -ge 1 &&
      # AppShell onRdpRowClick handler
      test $(grep -c "onRdpRowClick" src/ui/AppShell.tsx) -ge 1 &&
      test $(grep -B2 -A6 "onRdpRowClick={(row)" src/ui/AppShell.tsx | grep -c 'openTab(host, "rdp")') -ge 1 &&
      test $(grep -B2 -A6 "onRdpRowClick={(row)" src/ui/AppShell.tsx | grep -c "selectConversationDeferred") -ge 1 &&
      # No new npm dependencies
      test -z "$(git diff --stat package.json package-lock.json 2>&1)" &&
      # No polling regression (still true from 07-01)
      test $(grep -B2 -A15 "getSessionList()" src/ui/AppShell.tsx | grep -cE "setInterval|setTimeout") -eq 0 &&
      # T-06-02-01 mount-lifecycle: createPortal loop still ONE mount site
      test $(grep -c "createPortal(" src/ui/AppShell.tsx) -eq 1 &&
      # Full Vitest suite passes
      npx vitest run --project frontend 2>&1 | grep -qE "passed" &&
      # Type-check clean
      npx tsc --noEmit --skipLibCheck 2>&1 | grep "error TS" | wc -l | grep -qE "^0$" &&
      # Scope fence
      git diff --stat src/ui/features/pretty-view/ src/ui/features/terminal/Terminal.tsx src/ui/features/guacamole/ src/backend/ docker/ package.json package-lock.json src/ui/sidebar/NewSessionDialog.tsx 2>&1 | grep -qE "\|" && exit 1 || true
    </automated>
  </verify>
  <done>
    - NewSessionButton uses a Telegram-native pencil-family icon from lucide-react (Plus removed entirely)
    - ConversationsPanel `showGear` now gated on `!useIsTouchDevice()` in addition to the `onRailClick` typeof check (TG-18)
    - RDP rows render at the BOTTOM of the ConversationsPanel scroller with monitor icon + host name + no hue + no header separator
    - RDP row click routes through onRdpRowClick → AppShell handler → openTab(host, "rdp") + selectConversationDeferred + mobile navigateToView + isMobile setSidebarOpen(false)
    - `isEmpty` state suppressed when RDP rows are present
    - Zero new npm deps, zero touches to Terminal.tsx / pretty-view / guacamole / backend / docker / package.json / NewSessionDialog
    - Full frontend Vitest suite passes; tsc clean; scope fence intact
    - T-06-02-01 mount-lifecycle contract preserved (createPortal still one mount site)
    - Ready for Plan 07-03 (deploy checkpoint)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RDP row click → openTab(host, 'rdp') | Reuses existing tab lifecycle entry point Phase 6 established. No new backend or endpoint surface. |
| Host.enableRdp flag → RDP row emission | The flag comes from the host record (SQLite, AES-encrypted, admin-owned). No user input at row render time. |
| ConversationsPanel gear render → useIsTouchDevice | Client-side viewport signal already used across the fork. No new trust boundary introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | Host with `enableRdp === undefined` (nullable field) | mitigate | Filter with `host.enableRdp === true` (strict identity check), not `if (host.enableRdp)`. Undefined coerces to false; missing field on legacy Host records emits no RDP row. Test 33 exercises the true→false transition; add an implicit case: hosts without the field don't appear. |
| T-07-02-02 | Information Disclosure | RDP row exposes host name in the list | accept | Same host names Ashley already sees in HostsPanel, SessionsPanel, SidebarTree, double-shift menu. No new disclosure. |
| T-07-02-03 | Denial of Service | Many RDP-enabled hosts flooding the list | accept | At Ashley's fleet scale (~20 hosts, per Plan 06-02 SUMMARY notes), a few RDP rows at the bottom are negligible. No virtualization needed. |
| T-07-02-04 | Elevation of Privilege | onRdpRowClick bypassing authorization | accept | openTab(host, "rdp") is the exact same call path connectHost + HostsPanel + SessionsPanel use today. Backend guacd authentication + host authorization are the security boundary; frontend row click doesn't bypass them. |
| T-07-02-05 | Tampering | Mobile viewport race with `useIsTouchDevice()` | mitigate | The hook is deterministic within a page-load (media query detection). If Ashley resizes on desktop across breakpoints, the gear vanishes when useIsTouchDevice flips true — acceptable behavior matching Plan 06-03's mobile flow gate on the same signal. No SSR concern (Skynet is client-only React). |
| T-07-02-SC | Tampering | Supply chain — new npm dependencies | mitigate | Zero new npm dependencies. `Pencil` + `Monitor` icons are in `lucide-react` (existing dep). `useIsTouchDevice` already imported by AppShell. Verified by grep gate. |
</threat_model>

<verification>
- **RDP row at bottom (TG-15):** UAT — with Ashley's fleet containing at least one RDP-enabled host, the conversation list shows a row for that host at the BOTTOM with a monitor icon and the host name; no identity hue, no identity avatar, no identity name. Verified via UAT in Plan 07-03.
- **RDP row click opens desktop (TG-15):** UAT — clicking the RDP row opens the RDP tab and shows it; existing RDP disconnect/reconnect behavior unchanged.
- **RDP row persistence tied to enableRdp flag (TG-15):** UAT — toggle enableRdp off on a host (via HostEditor), refresh, verify the row is gone. Toggle back on, refresh, verify it returns.
- **Pencil re-style (TG-16):** UAT — the New Session button in ConversationsPanel shows a pencil glyph (not a plus). Function is unchanged — clicking opens the same NewSessionDialog with host picker + name input + Cancel/Open.
- **Mobile gear absent (TG-18):** UAT — open Skynet on a touch device (or `#mv=1` + narrow viewport dev-tools), verify: (a) no gear icon in the ConversationsPanel header, (b) SettingsRow visible at the bottom of the scroller, (c) tapping SettingsRow opens the dropdown with all admin destinations.
- **Desktop gear present (TG-18):** UAT — open Skynet on desktop, verify: (a) gear icon visible in the ConversationsPanel header, (b) no SettingsRow visible at the bottom of the scroller (AppShell's existing `isTouchDevice ? <SettingsRow /> : undefined` prop passthrough on the settingsRowSlot handles this), (c) clicking gear opens the same dropdown with all admin destinations.
- **Phase 6 + Plan 07-01 behavior preserved:** all Plan 07-01 UAT items (fleet visibility, attached/detached sameness, click-detached transparent attach, no polling) still hold; all Plan 06-05 UAT items still hold. Verified via full frontend Vitest suite passing + Plan 07-03 UAT walk.
</verification>

<success_criteria>
- CONTEXT.md §Success Criteria items #4 (RDP rows at bottom), #5 (pencil re-style), #6 (mobile no duplicate settings) all pass under UAT.
- Every requirement TG-15, TG-16, TG-18 has a concrete implementation asserted by tests and/or grep gates.
- Zero regressions to Phase 6 or Plan 07-01 behavior — full test suite passes.
- Zero new npm dependencies; zero backend/docker/package.json changes.
- Zero touches to scope-fenced files (guacamole, pretty-view, Terminal.tsx, backend, docker).
- Ready for Plan 07-03 (deploy checkpoint).
</success_criteria>

<output>
Create `.planning/phases/07-fleet-native-conversation-list/07-02-SUMMARY.md` when done. Include:
- Frontmatter with tags, dependency graph, tech-stack, key-files, key-decisions (including planner-picked pencil icon name + planner-picked RDP placement approach — sentinel HostGroup vs new rdpHosts field), patterns-established, requirements-completed (empty — Plan 07-03 UAT is the completion checkpoint), duration, completed date
- Accomplishments (RDP rows landed; pencil re-styled; TG-18 mobile gear duplicate fix landed; all UAT-visible surfaces of Phase 7 now in place)
- Task Commits (atomic per task)
- Files Created/Modified list with line counts
- Verification (grep gate results, Vitest counts, tsc status, scope-fence checks)
- Decisions Made (pencil icon choice + RDP placement approach + any inline RdpRow vs ConversationRow reuse choice)
- Deviations from Plan (if any)
- Threat Flags (mitigations landed)
- Next Phase Readiness (downstream notes for Plan 07-03: UAT checklist items covering TG-12..18, patches-md #106 draft prep, deploy-runbook.md callout)
- Self-Check block
</output>
