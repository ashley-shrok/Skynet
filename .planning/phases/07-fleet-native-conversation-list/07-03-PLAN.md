---
phase: 07-fleet-native-conversation-list
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified: []
autonomous: false
requirements: [TG-12, TG-13, TG-14, TG-15, TG-16, TG-17, TG-18]

must_haves:
  truths:
    - "The build succeeds cleanly (`npm run build` returns 0) and the resulting `dist/assets/*.js` bundle contains the load-bearing Phase 7 signals — fleet-native dedup/render markers, RDP row emission code, pencil icon reference, useIsTouchDevice gate on ConversationsPanel"
    - "A UAT checklist walking TG-12..18 exists in-repo at `.planning/phases/07-fleet-native-conversation-list/07-UAT-CHECKLIST.md`, structured for Ashley to work through post-deploy in a single sitting, covering both mobile and desktop paths and explicit negative-space checks (no polling, no attached/detached distinction, no plain-SSH rows)"
    - "A patches-md #106 entry draft exists at `.planning/phases/07-fleet-native-conversation-list/07-PATCHES-MD-ENTRY.md`, formatted to paste into `~/.claude/identities/tina/skynet-patches.md` at PIN time, following the multi-commit format Phase 6 patch #105 established"
    - "The deploy is executed behind the mandatory 15-min deadman rollback per `~/.claude/identities/tina/deploy-runbook.md`, with Ashley providing explicit green-light before the arm-deadman step runs. Fork's DEPLOY DISCIPLINE (CLAUDE.md) is honored verbatim — no exceptions."
  artifacts:
    - path: ".planning/phases/07-fleet-native-conversation-list/07-UAT-CHECKLIST.md"
      provides: "Ordered checklist for Ashley's post-deploy validation — one section per requirement TG-12..TG-18, plus regression coverage of Phase 6 (Plans 06-01..06-05) and Plan 07-01's data-source correctness"
    - path: ".planning/phases/07-fleet-native-conversation-list/07-PATCHES-MD-ENTRY.md"
      provides: "Draft entry for skynet-patches.md #106, formatted per patch #105's multi-commit precedent (single PIN spanning atomic commits across Plans 07-01 + 07-02)"
    - path: ".planning/phases/07-fleet-native-conversation-list/07-03-SUMMARY.md"
      provides: "Final SUMMARY closing Phase 7 — records the deploy outcome, deadman disarm confirmation, UAT status, and bounty-tracker close (`/close telegram-like-interface` after Ashley's UAT sign-off)"
  key_links:
    - from: "dist/assets/*.js"
      to: "Plan 07-01 fleet-native code"
      via: "grep for distinctive markers surviving minification"
      pattern: "fleet::|rdp-host::|updateFleetSessions"
    - from: "deploy invocation"
      to: "~/.claude/identities/tina/deploy-runbook.md"
      via: "verbatim step-by-step walkthrough including deadman arm + narrow pkill disarm"
      pattern: "deploy-runbook"
    - from: "07-UAT-CHECKLIST.md"
      to: "every TG-12..18 requirement"
      via: "explicit section headings + step-by-step verify instructions"
      pattern: "TG-1[2-8]"
---

<objective>
Deploy checkpoint for Phase 7. Verify the build carries the load-bearing Phase 7 signals into production bytes, generate a Nyquist UAT checklist for Ashley covering every TG-12..18 requirement plus Phase 6 regression items, draft the patches-md #106 entry for post-UAT pin, and execute the deadman-armed deploy under Ashley's explicit green-light per fork DEPLOY DISCIPLINE.

Purpose: TG-12..18 (all Phase 7 requirements). This plan is the requirement-completion checkpoint — every TG-XX is marked complete in the SUMMARY only after Ashley's UAT signs off on the checklist. Also the phase-close and bounty-close checkpoint (`/close telegram-like-interface` runs against the shared Phase 6 + Phase 7 bounty tracker after UAT).

Output: Three markdown artifacts (07-UAT-CHECKLIST.md, 07-PATCHES-MD-ENTRY.md, 07-03-SUMMARY.md) — zero source diffs from this plan itself. The deploy is executed within this plan's Task 4 checkpoint under Ashley's separate green-light.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-fleet-native-conversation-list/07-CONTEXT.md
@.planning/shapes/shape-fleet-native-conversation-list.md
@.planning/phases/07-fleet-native-conversation-list/07-01-PLAN.md
@.planning/phases/07-fleet-native-conversation-list/07-02-PLAN.md
@.planning/phases/06-telegram-like-interface/06-05-SUMMARY.md
</context>

<hard_constraints>
- **ZERO source diffs from this plan.** All three tasks produce only `.planning/phases/07-fleet-native-conversation-list/*.md` artifacts. No touches to `src/`, `docker/`, `package.json`, or any code file. `git diff --stat src/ docker/ package.json package-lock.json` MUST return empty at the end of this plan.
- **Deploy is Ashley-gated.** Task 4 is a `checkpoint:human-verify` — the executor pauses and waits for Ashley's "approved" (or specific issues) before running `docker compose up -d --force-recreate skynet`. NO auto-deploy. NO skipping the deadman arm.
- **Deadman is mandatory** per CLAUDE.md DEPLOY DISCIPLINE + `~/.claude/identities/tina/deploy-runbook.md`. The exact commands in Task 4's steps mirror the runbook verbatim, including the sentinel-cleanup-before-arm, the `nohup sudo -b bash -c 'sleep 900; [ ! -f /tmp/skynet-keep-patched ] && bash /opt/skynet/.tmp-revert.sh'` pattern, and the narrow-pkill disarm `sudo pkill -f 'sleep 900; \[ ! -f /tmp/skynet-keep-patched'`.
- **Blast radius is asymmetric per CLAUDE.md.** A bad deploy loses Ashley access to her whole fleet (Skynet is the gateway). Every step in Task 4 assumes rollback-first thinking — the deadman is the safety net, not the last resort.
- **UAT checklist MUST cover every TG-12..18 explicitly**, with one section per requirement, plus a "Phase 6 regression" section walking TG-01..11 to prove Phase 7 didn't break Phase 6, plus a "Plan 07-01 data-source" section walking the specific detached-row-click + fleet-visibility + no-polling behaviors.
- **Patches-md #106 draft MUST follow patch #105 precedent** (multi-commit format under one PIN, since Phase 7 lands as 4+ atomic commits across Plans 07-01 + 07-02).
</hard_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Build verification — npm run build clean + dist bytes contain Phase 7 signals + Phase 6 signals still present + scope-fence structural checks pass</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/phases/07-fleet-native-conversation-list/07-01-PLAN.md §"NEW derived ConversationRow shape extension" (fleet-only ids, fleetOnly marker)
    - .planning/phases/07-fleet-native-conversation-list/07-02-PLAN.md §Task 1 behavior (RDP row ids, rdpHostRow marker, placement decision)
    - .planning/phases/06-telegram-like-interface/06-05-SUMMARY.md §build-verify pattern (approach template)
  </read_first>
  <action>
    Build verification steps:

    1. `npm run build` → exit code 0. If any error, STOP and report to Ashley (do NOT attempt to auto-fix; fixes are Plan 07-01 or 07-02 amendments, not deploy-checkpoint concerns).

    2. Dist bytes contain Phase 7 signals (grep against `dist/assets/*.js`):
       - `grep -c 'fleet::' dist/assets/*.js` ≥ 1 (fleet-only row id prefix — string literal, Vite preserves)
       - `grep -c 'rdp-host::' dist/assets/*.js` ≥ 1 (RDP row id prefix)
       - `grep -c 'updateFleetSessions' dist/assets/*.js` may be 0 if Vite mangles the identifier — fallback to `grep -c '/sessions/list' dist/assets/*.js` ≥ 1 to prove the fleet-fetch endpoint is called from bundled code (this string comes from `authApi.get("/sessions/list")` in sessions-api.ts and survives minification because it's a URL literal)
       - Pencil icon: `grep -cE 'Pencil|PenTool|SquarePen' dist/assets/*.js` — MAY be mangled by Vite's lucide-react tree-shaker; fallback is `grep -c '"nav.newSession"' dist/assets/*.js` ≥ 1 (the i18n key survives minification and proves the button ships)
       - Note: these are SMOKE gates, not exhaustive proofs. Grep-hit expectations are documented with fallbacks because Vite minification behavior varies.

    3. Dist bytes still contain Phase 6 signals (regression):
       - `grep -c 'ConversationsPanel' dist/assets/*.js` ≥ 1 (may be mangled → fallback: `grep -c 'nav.conversations' dist/assets/*.js` ≥ 1)
       - `grep -c 'TabBar' dist/assets/*.js` — should be 0 (TabBar was DELETED in Plan 06-02; a hit means dead code somehow returned)
       - `grep -c 'MobileBottomBar' dist/assets/*.js` — should be 0 (deleted in Plan 06-03)
       - `grep -c 'appendChild' dist/assets/*.js` ≥ 1 (patch #35 tabNodesRef DOM-move mechanism — the createPortal loop's appendChild survives minification)

    4. Full frontend Vitest suite: `npx vitest run --project frontend` — all tests pass (baseline was 301/301 post-Phase-6; expected post-Phase-7 ≥313 with new Tests 23-30 from 07-01 and Tests 31-34 from 07-02 and Test 4 from AppShell.persistence.test.tsx via 07-01).

    5. Type-check clean: `npx tsc --noEmit --skipLibCheck` — zero errors.

    6. Scope-fence structural checks against HEAD:
       - `git diff --stat src/ui/features/pretty-view/` empty
       - `git diff --stat src/ui/features/terminal/Terminal.tsx` empty
       - `git diff --stat src/ui/features/guacamole/` empty
       - `git diff --stat src/backend/` empty
       - `git diff --stat docker/` empty
       - `git diff --stat package.json package-lock.json` empty
       - `git diff --stat src/ui/sidebar/NewSessionDialog.tsx` empty (Plan 07-02 explicitly said no dialog changes)
       - `git diff --stat src/ui/sidebar/ConversationRow.tsx` — allowed to have SMALL diff if Plan 07-02 chose the ConversationRow-with-iconOverride-prop path for RDP rendering; otherwise empty. Report which path landed.

    7. If any gate fails, report to Ashley with the failing gate + suggested amendment path (07-01 or 07-02 amendment) and STOP. Do NOT proceed to Task 2/3/4 with a failing build.

    Zero source diffs. This task is pure verification against the pre-existing checked-in state.
  </action>
  <verify>
    <automated>
      npm run build 2>&1 | tail -5 &&
      test -d dist/assets &&
      test $(grep -c 'fleet::\|rdp-host::' dist/assets/*.js 2>/dev/null | awk -F: '{s+=$2} END {print s+0}') -ge 2 &&
      test $(grep -c 'nav.conversations\|/sessions/list' dist/assets/*.js 2>/dev/null | awk -F: '{s+=$2} END {print s+0}') -ge 2 &&
      test $(grep -c 'TabBar\|MobileBottomBar' dist/assets/*.js 2>/dev/null | awk -F: '{s+=$2} END {print s+0}') -eq 0 &&
      test $(grep -c 'appendChild' dist/assets/*.js 2>/dev/null | awk -F: '{s+=$2} END {print s+0}') -ge 1 &&
      npx vitest run --project frontend 2>&1 | grep -qE "passed" &&
      npx tsc --noEmit --skipLibCheck 2>&1 | grep "error TS" | wc -l | grep -qE "^0$" &&
      # Scope fence — allow ConversationRow diff if 07-02's icon-override path was chosen, otherwise strict
      git diff --stat src/ui/features/pretty-view/ src/ui/features/terminal/Terminal.tsx src/ui/features/guacamole/ src/backend/ docker/ package.json package-lock.json src/ui/sidebar/NewSessionDialog.tsx 2>&1 | grep -qE "\|" && exit 1 || true
    </automated>
  </verify>
  <done>
    - `npm run build` returns 0
    - Dist bytes contain Phase 7 markers (fleet::, rdp-host::, sessions/list endpoint) AND still contain Phase 6 markers (nav.conversations, appendChild) AND do NOT contain deleted-Phase-6-file markers (TabBar, MobileBottomBar)
    - Full frontend Vitest suite passes
    - tsc --noEmit --skipLibCheck clean
    - Scope fence structural checks pass; if ConversationRow.tsx has diff, report which Plan 07-02 path landed
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate UAT checklist walking every TG-12..18 requirement plus Phase 6 regression coverage</name>
  <files>.planning/phases/07-fleet-native-conversation-list/07-UAT-CHECKLIST.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md §"Fleet-native Conversation List (Phase 7)" (TG-12..18 canonical text)
    - .planning/REQUIREMENTS.md §"Telegram-like Interface (Phase 6)" (TG-01..11 for regression walk)
    - .planning/phases/07-fleet-native-conversation-list/07-CONTEXT.md §success_criteria (10 items — Ashley's post-deploy true/false statements)
    - .planning/phases/06-telegram-like-interface/06-05-SUMMARY.md §UAT walk (template for structure — this plan mirrors that shape)
    - .planning/phases/07-fleet-native-conversation-list/07-01-PLAN.md §verification (specific detached-row / fleet-visibility / no-polling UAT items)
    - .planning/phases/07-fleet-native-conversation-list/07-02-PLAN.md §verification (specific RDP-row / pencil / mobile-gear UAT items)
  </read_first>
  <action>
    Create `.planning/phases/07-fleet-native-conversation-list/07-UAT-CHECKLIST.md` structured as follows:

    ```markdown
    # Phase 7 UAT Checklist — Fleet-native Conversation List

    **For:** Ashley
    **Post-deploy validation of patch #106 (Phase 7)**
    **Deployed:** <auto-fill at deploy time>
    **Deadman armed:** <auto-fill>
    **Deadman disarm deadline:** <auto-fill — deploy time + 15min>

    ## How to use this checklist

    Work through top-to-bottom. Each item has a specific action + expected result + "if this fails" note. Mark [x] as you go. If ANY item under "Phase 7 core" fails, disarm the deadman by NOT touching /tmp/skynet-keep-patched — rollback fires automatically. If any item under "Phase 6 regression" fails but Phase 7 core passes, decide case-by-case whether to disarm.

    ## Phase 7 core — the new behavior

    ### TG-12: Fleet-native list on fresh page-load
    - [ ] Open Skynet in a fresh incognito window on desktop. Wait for the page to load fully. Expected: the ConversationsPanel shows one row per running tmux session across all your reachable hosts. If: only shows tabs from this browser tab (empty on fresh load) → Plan 07-01's fetch effect isn't firing; check DevTools console + Network for /sessions/list request. If: shows nothing at all → verify /sessions/list responds (curl or Network tab).

    ### TG-13: Attached vs detached rows visually indistinguishable
    - [ ] From the fresh page-load state above, click one of the rows to attach to it. Wait for it to open. Return to the list (back button on mobile, or just look at the sidebar on desktop). Expected: the row you clicked looks identical to the rows you haven't clicked — no brightness delta, no dot, no italic, no spinner. If: any visual difference → check ConversationRow for accidentally-added state-based styling.

    ### TG-14: Click-a-detached-row transparently attaches
    - [ ] Click a row you haven't opened yet in this session. Expected: pane opens immediately, no dialog, no confirmation, no "connect" step. Single click = attached + selected + shown. If: dialog appears → check AppShell onDetachedRowClick handler is wired.

    ### TG-15: RDP host rows at bottom
    - [ ] Scroll to the bottom of the ConversationsPanel. Expected: one row per RDP-enabled host, monitor icon in the avatar slot, host name as the label, no identity hue, no identity name. If: no rows → check host.enableRdp flag on the affected hosts. If: hue or avatar → check the RDP-render branch in ConversationsPanel didn't pick up the identity resolution.
    - [ ] Click an RDP row. Expected: RDP tab opens, remote desktop connects normally. Existing RDP disconnect/reconnect unchanged.
    - [ ] In HostEditor, disable RDP on one host. Refresh browser. Expected: that host's RDP row is gone. Re-enable RDP. Refresh again. Expected: row returns.

    ### TG-16: Pencil re-style
    - [ ] Look at the New Session button in the ConversationsPanel. Expected: pencil glyph (not plus). Function unchanged — clicking opens the NewSessionDialog with host picker + optional session name.

    ### TG-17: No polling
    - [ ] Open DevTools → Network tab. Filter for "sessions/list". Refresh Skynet. Expected: exactly ONE request to /sessions/list at page-load, ZERO subsequent requests over the next 5 minutes (idle) or on tab focus/blur or on hosts-changed events. If: repeat requests → polling regression, check Plan 07-01 fetch effect deps.

    ### TG-18: Mobile no duplicate settings
    - [ ] Open Skynet on mobile (or desktop with narrow viewport + touch device emulation). Expected: NO gear icon in the ConversationsPanel header. SettingsRow visible at the bottom of the scroller. Tapping SettingsRow opens the dropdown with all admin destinations.
    - [ ] Open Skynet on desktop (mouse + non-touch viewport). Expected: gear icon visible in the ConversationsPanel header. NO SettingsRow at the bottom of the scroller. Clicking gear opens the same dropdown.

    ## Phase 7 additional — from Plan 07-01 verification block

    - [ ] Session-identity dedup: open a tab for hostA/tmux-session "work" via the pencil (creates a fresh session named "work"). Verify the ConversationsPanel shows ONE row for it, not two (would be one from openTabs + one from fleet-only if dedup didn't fire).
    - [ ] Existing openTab-derived rows still work: click a row that came from openTabs (either your pencil-created one, or a row you attached earlier this session). Expected: switches instantly with no reconnect (T-06-02-01 persistence contract preserved).

    ## Phase 6 regression — verify TG-01..11 still hold

    (Compressed walk — expected behavior should match Phase 6 UAT sign-off from patch #105.)

    - [ ] TG-01: sidebar shows flat host-grouped list; session-end vanishes rows.
    - [ ] TG-02: pin/unpin works; pins float above host groups.
    - [ ] TG-03: only one conversation visible at a time; no tab strip.
    - [ ] TG-04: internal experience unchanged — pretty view, terminal, RDP all open into their existing pane types.
    - [ ] TG-05: session persistence within page-load — switch away and back, no reconnect, scroll position preserved.
    - [ ] TG-06: mobile list-vs-view flow with top-left back button + browser back gesture works.
    - [ ] TG-07: mobile bottom nav absent (deleted in Plan 06-03).
    - [ ] TG-08: desktop sidebar collapse behavior unchanged.
    - [ ] TG-09: new-session button opens the host-picker dialog; empty name allowed (auto-fills from tmux window title).
    - [ ] TG-10: admin destinations reachable via gear (desktop) OR SettingsRow (mobile) — routes to all Plan 06-02 destinations.
    - [ ] TG-11: no toggle to bring tabs back; TabBar unconditionally gone.

    ## Deadman disarm — ONLY after every item above is [x]

    On the EC2 host, run:

    ```
    sudo touch /tmp/skynet-keep-patched
    sudo pkill -f 'sleep 900; \[ ! -f /tmp/skynet-keep-patched'
    ```

    Verify no revert cron/nohup is still running:
    ```
    ps -ef | grep 'sleep 900' | grep -v grep
    ```

    Expected: empty output.

    ## If a UAT item fails

    - If a Phase 7 core item (TG-12..18) fails → DO NOT touch /tmp/skynet-keep-patched. Deadman fires at the 15-min mark; production rolls back to pre-patch state automatically. Note which item failed for follow-up amendment.
    - If only a Phase 7 additional item fails → decide by severity. If session-identity dedup broken (double-rendered rows), disarm-and-rollback via explicit `bash /opt/skynet/.tmp-revert.sh` (see deploy-runbook).
    - If only a Phase 6 regression item fails → decide by severity. Phase 6 UAT signed off at patch #105 so any regression is Phase 7's doing.

    ---
    *Phase: 07-fleet-native-conversation-list*
    *Checklist generated: <auto-fill>*
    *Deploy runbook reference: `~/.claude/identities/tina/deploy-runbook.md`*
    ```

    Auto-fill placeholders left as `<auto-fill>` — the executor should replace them at file-creation time with the current date and estimated deploy timestamps. If uncertain, leave placeholders and let Ashley fill in at deploy time.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/07-fleet-native-conversation-list/07-UAT-CHECKLIST.md &&
      test $(grep -c "^### TG-1[2-8]" .planning/phases/07-fleet-native-conversation-list/07-UAT-CHECKLIST.md) -eq 7 &&
      test $(grep -c "^- \[ \] TG-0[1-9]\|^- \[ \] TG-1[01]" .planning/phases/07-fleet-native-conversation-list/07-UAT-CHECKLIST.md) -eq 11 &&
      test $(grep -c "deadman" .planning/phases/07-fleet-native-conversation-list/07-UAT-CHECKLIST.md) -ge 3 &&
      test $(grep -c "deploy-runbook" .planning/phases/07-fleet-native-conversation-list/07-UAT-CHECKLIST.md) -ge 1 &&
      test $(grep -c "skynet-keep-patched" .planning/phases/07-fleet-native-conversation-list/07-UAT-CHECKLIST.md) -ge 2
    </automated>
  </verify>
  <done>
    - 07-UAT-CHECKLIST.md exists with one section per TG-12..18 (7 sections) + Phase 6 regression walk (11 items) + Plan 07-01 additional items + deadman disarm instructions + rollback fallback notes
    - Deploy runbook reference present
    - Deadman sentinel + pkill commands present verbatim from runbook
  </done>
</task>

<task type="auto">
  <name>Task 3: Draft patches-md #106 entry (multi-commit format, patch #105 precedent)</name>
  <files>.planning/phases/07-fleet-native-conversation-list/07-PATCHES-MD-ENTRY.md</files>
  <read_first>
    - ~/.claude/identities/tina/skynet-patches.md (specifically patch #105's multi-commit format — the template for Phase 7's entry)
    - .planning/phases/07-fleet-native-conversation-list/07-01-PLAN.md §success_criteria + §task list (Tasks 1-2 with commit shapes)
    - .planning/phases/07-fleet-native-conversation-list/07-02-PLAN.md §success_criteria + §task list (Tasks 1-2 with commit shapes)
    - .planning/phases/06-telegram-like-interface/06-05-SUMMARY.md §Task 3 output (patch #105 entry as it landed — canonical format reference)
  </read_first>
  <action>
    Create `.planning/phases/07-fleet-native-conversation-list/07-PATCHES-MD-ENTRY.md` with a draft patch #106 entry structured for post-UAT paste into `~/.claude/identities/tina/skynet-patches.md`. Format:

    ```markdown
    # Patch #106 — Fleet-native conversation list

    **Status:** Deployed <date>, UAT signed off <date>, PIN active
    **Commits:** (list all atomic commits from Plans 07-01 + 07-02 with SHAs at PIN time)
    **Branch:** feat/tab-title-from-tmux
    **Bounty:** telegram-like-interface (closed via `/close` after this patch's UAT — bounty spans both patch #105 and patch #106 as ONE ship arc across two deploy cycles per Phase 6 + Phase 7 ROADMAP.md notes)

    ## What it does

    Extends Phase 6's Telegram-like conversation list from "browser-tab's open tabs" to "fleet-discovered tmux sessions unioned with open tabs, deduplicated by session identity." A fresh page-load now shows every running tmux session across every reachable host — the same set the current sidebar host-tree + double-shift menu display. Adds remote-desktop host rows at the bottom (one per RDP-enabled host, monitor icon, no identity hue). Re-styles the New Session button as the Telegram-native pencil. Fixes the mobile gear/settings-row duplication from Phase 6 (gear desktop-only, settings-row mobile-only).

    ## Motivating gap

    Phase 6 (patch #105) shipped the conversation-list metaphor but the list's data source was scoped too narrowly — it mirrored only the browser-tab's currently-open Skynet tabs. On a fresh mobile page-load with no tabs open, the list showed "no active conversations" even when Ashley had running sessions across her fleet. Phase 7 makes the list honest to the "these are your conversations, always" metaphor Phase 6 aimed at.

    ## Changed files (net)

    - `src/ui/state/conversation-store.ts` — extended with FleetSession type + updateFleetSessions + updateHostsFlat actions + union+dedup derivation + optional fleetOnly + rdpHostRow row markers
    - `src/ui/state/conversation-store.test.ts` — +12 Vitest cases (Tests 23-34) covering fleet-only render, dedup, no-polling guards, RDP row emission + persistence
    - `src/ui/sidebar/ConversationsPanel.tsx` — extended with useIsTouchDevice-gated gear (TG-18 fix), onDetachedRowClick + onRdpRowClick props, handleRowSelect helper routing fleetOnly / rdpHostRow / default paths, RDP row render section at bottom
    - `src/ui/sidebar/NewSessionButton.tsx` — Plus icon swapped for <planner-picked pencil-family icon>
    - `src/ui/AppShell.tsx` — one-shot getSessionList() fetch effect (empty deps), hostsById memo keyed on stableHostTreeKey, updateHostsFlat effect, onDetachedRowClick handler (allowCreateTmux: false — attach not create), onRdpRowClick handler (openTab + selectConversationDeferred)
    - `src/ui/AppShell.persistence.test.tsx` — +1 test (Test 4) asserting fleet-derived row shape through the store

    ## Constraints preserved

    - Every Phase 6 behavior verbatim: pins, host grouping, mobile list-vs-view, sidebar collapse, session persistence within page-load, URL fragment schemes, T-06-02-01 mount-lifecycle contract
    - Tab lifecycle mechanisms untouchable: RDP disconnect/reconnect, identity-tmux attach/detach, pretty-view mount-on-identity-resolution all preserved byte-for-byte
    - Zero new npm dependencies
    - Zero backend / docker / package.json changes
    - Zero touches to src/ui/features/pretty-view/, src/ui/features/terminal/Terminal.tsx, src/ui/features/guacamole/, src/backend/, docker/, package.json
    - Snapshot-on-page-load fleet discovery — NO polling, NO real-time push, NO focus/visibility refetch

    ## Deploy

    Deployed <date> behind mandatory 15-min deadman rollback per deploy-runbook.md. UAT walk covered TG-12..18 + Phase 6 regression items TG-01..11. Deadman disarmed at <time> after Ashley's sign-off.

    ## Follow-ups considered but NOT in this patch

    - Real-time polling / push chrome (deliberately out per TG-17)
    - Plain-SSH host rows (deliberately out — Ashley never creates plain-SSH one-shots)
    - Attached-vs-detached visual distinction (deliberately out per TG-13)
    - Per-conversation activity/unread signals (deferred to v2, same as Phase 6)
    - Cross-device / cross-session state sync (deliberately out — Ashley refreshes to update)

    ## Bounty closure

    Bounty `telegram-like-interface` (Tina's identity) tracks Phase 6 + Phase 7 as one arc across two ship steps. Closed via `/close telegram-like-interface` after this patch's UAT sign-off.
    ```

    Fill in `<date>`, `<time>`, `<planner-picked pencil-family icon>` (from Plan 07-02's landed SUMMARY), and commit SHAs at Ashley-gated PIN time (after UAT signs off). Executor writes the draft; actual PIN happens post-UAT.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/07-fleet-native-conversation-list/07-PATCHES-MD-ENTRY.md &&
      test $(grep -c "^# Patch #106" .planning/phases/07-fleet-native-conversation-list/07-PATCHES-MD-ENTRY.md) -eq 1 &&
      test $(grep -c "Fleet-native\|fleet-native\|FleetSession" .planning/phases/07-fleet-native-conversation-list/07-PATCHES-MD-ENTRY.md) -ge 3 &&
      test $(grep -c "TG-1[2-8]\|TG-0[1-9]\|TG-1[01]" .planning/phases/07-fleet-native-conversation-list/07-PATCHES-MD-ENTRY.md) -ge 5 &&
      test $(grep -c "deadman\|deploy-runbook" .planning/phases/07-fleet-native-conversation-list/07-PATCHES-MD-ENTRY.md) -ge 2 &&
      test $(grep -c "bounty\|telegram-like-interface" .planning/phases/07-fleet-native-conversation-list/07-PATCHES-MD-ENTRY.md) -ge 2
    </automated>
  </verify>
  <done>
    - 07-PATCHES-MD-ENTRY.md exists with Phase 6 patch #105 multi-commit format precedent honored
    - What-it-does + motivating gap + changed files + constraints + deploy + follow-ups sections present
    - Bounty closure line references the shared Phase 6 + Phase 7 arc
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Ashley-gated deadman-armed deploy per fork DEPLOY DISCIPLINE</name>
  <what-built>
    Plan 07-01 landed the fleet-native store extension + one-shot fetch. Plan 07-02 landed the RDP rows + pencil re-style + mobile gear fix. Task 1 verified the build carries Phase 7 signals into dist bytes + preserved Phase 6 signals. Task 2 generated the UAT checklist. Task 3 drafted the patches-md #106 entry.

    What ships in this deploy:
    - Fresh page-load shows fleet's running tmux sessions (TG-12, TG-17)
    - Attached and detached rows visually indistinguishable (TG-13)
    - Click-a-detached-row transparently attaches (TG-14)
    - RDP host rows at bottom with monitor icon + no hue (TG-15)
    - New Session button re-styled as Telegram-native pencil (TG-16)
    - Mobile gear/settings-row duplication fixed (TG-18)
    - Every Phase 6 behavior preserved verbatim

    Blast radius: full — Skynet is the gateway to the fleet. If Phase 7 breaks the ConversationsPanel or AppShell in production, Ashley loses access to every managed box until the deadman fires or she rolls back manually. Deadman is MANDATORY per fork DEPLOY DISCIPLINE.
  </what-built>
  <how-to-verify>
    **Before the deploy, verify:**

    1. On this machine (Skynet repo host):
       - `git status` clean — no uncommitted changes
       - `git log --oneline -5` shows Plans 07-01 + 07-02 atomic commits on `feat/tab-title-from-tmux`
       - `npm run build` re-run (Task 1 result should be cached; if not, re-run)
       - `.planning/phases/07-fleet-native-conversation-list/07-UAT-CHECKLIST.md` exists and covers TG-12..18
       - `.planning/phases/07-fleet-native-conversation-list/07-PATCHES-MD-ENTRY.md` exists

    2. Ashley confirms:
       - She has the UAT checklist open in a second window / phone
       - She's ready to work through the 15-min UAT window post-deploy
       - She acknowledges the deadman-fires-if-she-doesn't-disarm contract

    3. Ashley approves the deploy explicitly. Only after "approved" (or equivalent explicit go-ahead) does the executor proceed.

    **Deploy steps (executed only after Ashley's approval — mirrors `~/.claude/identities/tina/deploy-runbook.md`):**

    a. Push the branch to the deploy remote (`git push origin feat/tab-title-from-tmux`) OR sync via the fork's existing deploy path (whichever is fresh in Ashley's box-map.md).

    b. On the EC2 host (via SSM per CLAUDE.md — no public SSH), navigate to `/opt/skynet` and pull the branch.

    c. Sentinel cleanup (belt-and-braces before deadman arm):
       ```
       sudo rm -f /tmp/skynet-keep-patched
       ```

    d. Arm the deadman (verbatim from deploy-runbook.md):
       ```
       nohup sudo -b bash -c 'sleep 900; [ ! -f /tmp/skynet-keep-patched ] && bash /opt/skynet/.tmp-revert.sh' >/dev/null 2>&1 &
       ```
       Note wall-clock time: this is the 15-min countdown.

    e. Deploy:
       ```
       docker compose up -d --force-recreate skynet
       ```

    f. Wait for container health (`docker compose ps skynet` → healthy, ~10-30s).

    g. Notify Ashley the container is up. Point her at the UAT checklist. Note the deadman deadline: deploy time + 15min.

    **Ashley works through the UAT checklist (Task 2 output).**

    **If Ashley signs off (all UAT items [x]):**

    h. Ashley (or executor with Ashley's approval) disarms the deadman on the EC2 host:
       ```
       sudo touch /tmp/skynet-keep-patched
       sudo pkill -f 'sleep 900; \[ ! -f /tmp/skynet-keep-patched'
       ```
       Verify no `sleep 900` process remains: `ps -ef | grep 'sleep 900' | grep -v grep` — empty output.

    i. PIN the patches-md entry: paste 07-PATCHES-MD-ENTRY.md contents (with dates/commits/pencil-icon-name filled in) into `~/.claude/identities/tina/skynet-patches.md` under patch #106.

    j. Close the bounty: `/close telegram-like-interface` (shared with Phase 6 — closes now that both ship steps are UAT-signed).

    **If Ashley reports a UAT failure:**

    - DO NOT touch `/tmp/skynet-keep-patched`. The deadman fires at the 15-min mark and rolls back automatically.
    - OR explicitly roll back before the deadline: `bash /opt/skynet/.tmp-revert.sh`.
    - Note the failing UAT item + expected vs actual behavior. This becomes a Plan 07-01 or 07-02 amendment (not a Plan 07-03 concern).

    **NO exception to the deadman.** Per CLAUDE.md + Ashley's 2026-07-03 lock: every deploy behind the deadman, even when Ashley is at the keyboard.
  </how-to-verify>
  <resume-signal>
    Ashley provides one of:
    - `approved` (deploy proceeds, executor arms deadman + runs `docker compose up`)
    - `approved, deploy but I'll UAT tomorrow` (deploy proceeds, deadman fires unless Ashley disarms — she accepts the rollback if she doesn't sign off in 15min)
    - `wait, [issue]` (executor stops, does not deploy, reports issue back for amendment)
    - `deploy without deadman` (executor REFUSES — deadman is mandatory per CLAUDE.md, no exceptions; escalate)
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deploy invocation → production EC2 | Skynet container replaced in production; fleet-access blast radius is asymmetric — a bad build takes down every managed box's browser SSH+RDP surface. |
| Deadman arm → 15-min countdown | If UAT reveals a critical bug and Ashley forgets to disarm, the deadman fires automatically at T+15min. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Denial of Service | Bad build wedges Skynet container | mitigate | Task 1 build verification (npm run build clean + dist bytes contain Phase 7 + Phase 6 markers) gates Task 4. Deadman rollback at T+15min if UAT fails. |
| T-07-03-02 | Tampering | Deadman not armed before docker compose up | mitigate | Task 4 checkpoint's deploy steps enforce arm-BEFORE-deploy ordering. Ashley's explicit approval + executor discipline. Sentinel cleanup step (`rm -f /tmp/skynet-keep-patched`) ensures a stale sentinel from a prior deploy doesn't neutralize the new deadman. |
| T-07-03-03 | Elevation of Privilege | Deploy runs without Ashley's approval | mitigate | Task 4 is `checkpoint:human-verify gate="blocking"` — executor cannot proceed without Ashley's `approved` signal. Auto-advance is refused per hard_constraint. |
| T-07-03-04 | Information Disclosure | Deploy exposes Phase 7 code to fleet | accept | Phase 7 code is not sensitive (UI-layer reshape). No new secrets, no new credentials, no new schemas. |
| T-07-03-SC | Supply Chain | Package legitimacy at deploy time | mitigate | Zero new npm dependencies verified in Plans 07-01 + 07-02. No package-lock.json drift. Existing dependencies unchanged. |
</threat_model>

<verification>
- Build succeeds; dist bytes contain Phase 7 + Phase 6 markers; scope fence structural checks pass.
- UAT checklist covers TG-12..18 + Phase 6 regression + Plan 07-01 additional items + deadman disarm instructions.
- Patches-md #106 draft exists in Phase 6 patch #105 precedent format.
- Deploy executed under Ashley's explicit green-light behind the mandatory 15-min deadman.
- Deadman disarmed (or automatically fires) based on UAT outcome.
- Bounty `telegram-like-interface` closed via `/close` after UAT sign-off (post-deploy administrative step; not a code artifact).
</verification>

<success_criteria>
- `npm run build` returns 0; dist bytes verify.
- 07-UAT-CHECKLIST.md exists and is comprehensive.
- 07-PATCHES-MD-ENTRY.md exists as a paste-ready patch #106 draft.
- Deploy checkpoint reached and Ashley provides a decision (approved / issue / defer).
- If approved: production deploy behind deadman completes; Ashley works UAT within 15 min or rollback fires; if UAT passes, deadman disarmed + patches-md pinned + bounty closed.
- Requirements TG-12..18 marked complete in 07-03-SUMMARY.md only AFTER Ashley's UAT sign-off.
</success_criteria>

<output>
Create `.planning/phases/07-fleet-native-conversation-list/07-03-SUMMARY.md` at the end of the plan. Include:
- Frontmatter with tags, dependency graph, requirements-completed (TG-12..18 — populated only after Ashley's UAT sign-off), duration, completed date
- Accomplishments (build verified, UAT checklist generated, patches-md draft ready, deploy executed under Ashley's approval, deadman armed and disarmed on schedule, patches-md pinned, bounty closed)
- Task Commits (Tasks 1-3 are markdown artifact commits; Task 4 has no source commit — it's operational)
- Files Created list with line counts (07-UAT-CHECKLIST.md, 07-PATCHES-MD-ENTRY.md, 07-03-SUMMARY.md)
- Deploy log (deploy timestamp, container-healthy timestamp, UAT-start timestamp, deadman-disarm timestamp, or rollback-fired timestamp if UAT failed)
- UAT outcome (item-by-item pass/fail with Ashley's notes)
- Threat Flags (deadman fired-or-disarmed; any UAT-revealed threats amended in follow-up plans)
- Phase 7 close — all TG-12..18 marked complete OR follow-up plan needed
- Bounty closure — `/close telegram-like-interface` outcome recorded
- Self-Check block

Also update `.planning/STATE.md` current position to reflect Phase 7 complete or in-remediation.
Also update `.planning/ROADMAP.md` Phase 7 status column to "Complete" with the deploy date.
Also update `.planning/REQUIREMENTS.md` traceability table for TG-12..18 from "Pending" to "Complete".
</output>
