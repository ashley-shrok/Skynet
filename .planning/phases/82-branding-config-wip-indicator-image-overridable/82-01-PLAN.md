---
phase: 82-branding-config-wip-indicator-image-overridable
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/branding/branding-config-loader.ts
  - src/backend/branding/branding-config-loader.test.ts
  - src/backend/branding/assert-boot.test.ts
  - docker/branding-defaults/branding.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "Backend BrandingConfig type carries a new required field wipIndicatorPath: string (per 82-CONTEXT.md LOCKED 'Schema shape')"
    - "isValidBrandingShape rejects a config missing wipIndicatorPath and rejects a config where wipIndicatorPath is not a string"
    - "HARDCODED_FALLBACK in branding-config-loader.ts contains wipIndicatorPath: '/branding/wip-cube.webp' (per 82-CONTEXT.md LOCKED 'Bundled default value')"
    - "docker/branding-defaults/branding.json contains wipIndicatorPath: '/branding/wip-cube.webp' — byte-for-byte matches HARDCODED_FALLBACK value (per 82-CONTEXT.md LOCKED 'Bundled default value')"
    - "branding-config-loader.test.ts validFixture() carries wipIndicatorPath and has new missing-field + wrong-type shape-guard cases"
    - "assert-boot.test.ts makeValidLoadResult carries wipIndicatorPath so its full-shape fixture stays valid against the extended shape guard (verify — Phase 74 pattern precedent)"
    - "npx vitest run src/backend/branding/branding-config-loader.test.ts exits 0"
    - "npx vitest run src/backend/branding/assert-boot.test.ts exits 0"
  artifacts:
    - path: "src/backend/branding/branding-config-loader.ts"
      provides: "Extended BrandingConfig type + shape guard branch + HARDCODED_FALLBACK wipIndicatorPath entry"
      contains: "wipIndicatorPath"
    - path: "src/backend/branding/branding-config-loader.test.ts"
      provides: "Updated validFixture() + new shape-guard tests for wipIndicatorPath"
      contains: "wipIndicatorPath"
    - path: "docker/branding-defaults/branding.json"
      provides: "Bundled default JSON with wipIndicatorPath field"
      contains: "wipIndicatorPath"
  key_links:
    - from: "src/backend/branding/branding-config-loader.ts"
      to: "docker/branding-defaults/branding.json"
      via: "getBundledDefaults() → readFileSync → isValidBrandingShape parse — bundled JSON must satisfy the extended shape guard"
      pattern: "wipIndicatorPath"
    - from: "HARDCODED_FALLBACK in branding-config-loader.ts"
      to: "docker/branding-defaults/branding.json"
      via: "byte-for-byte agreement invariant (Phase 70 D-14 no-config-deploy-preserves-behavior rule)"
      pattern: "wipIndicatorPath.*=.*\"/branding/wip-cube.webp\""
---

<objective>
Extend the backend BrandingConfig schema with a new required field `wipIndicatorPath: string` (mirrors Phase 74 image-asset pattern — `iconPath` / `wordmarkPath` variant, NOT the intentionally-empty `avatarDirectorSpec` variant). Update the shape guard, HARDCODED_FALLBACK, bundled JSON default, and the backend shape-guard test file in the same atomic plan so the loader, the file it reads, and the tests that validate the extension all agree on the extended shape.

Purpose: gives the frontend mirror (Plan 82-03) and the WipBubble rewire (Plan 82-04) a schema field to read from. This is the backend half of the atomic contract Phase 82 stands on.

Output:
- Extended `BrandingConfig` type at `src/backend/branding/branding-config-loader.ts` with `wipIndicatorPath: string` appended after `faviconPath` and before `pwaIcons` (per 82-CONTEXT.md § "Claude's Discretion" recommended order — image-URL group).
- Extended `isValidBrandingShape` rejecting missing or non-string `wipIndicatorPath`.
- Extended `HARDCODED_FALLBACK` constant with `wipIndicatorPath: "/branding/wip-cube.webp"`.
- Updated `docker/branding-defaults/branding.json` with the same field and value.
- Updated `validFixture()` in `branding-config-loader.test.ts` + two new shape-guard test cases (missing field, wrong type).
- Updated `makeValidLoadResult()` in `assert-boot.test.ts` to carry `wipIndicatorPath` so its full-shape fixture stays valid — no other change to that file (no new assertions; boot gate is not affected by this phase).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82-branding-config-wip-indicator-image-overridable/82-CONTEXT.md
@src/backend/branding/branding-config-loader.ts
@src/backend/branding/branding-config-loader.test.ts
@src/backend/branding/assert-boot.test.ts
@docker/branding-defaults/branding.json
@.planning/phases/74-control-style-of-avatar-generation-through-branding-config/74-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend backend BrandingConfig type + shape guard + HARDCODED_FALLBACK + bundled JSON + shape-guard tests</name>
  <files>src/backend/branding/branding-config-loader.ts, src/backend/branding/branding-config-loader.test.ts, docker/branding-defaults/branding.json</files>
  <read_first>
    - src/backend/branding/branding-config-loader.ts (full file — L41-60 BrandingConfig type; L76-93 HARDCODED_FALLBACK; L161-187 isValidBrandingShape; L125-155 getBundledDefaults memoized reader — memoization matters for test isolation, see Task 3 rationale)
    - src/backend/branding/branding-config-loader.test.ts (full file — L44-56 bundledDefaultJson mock object; L101-118 validFixture(); L131-251 all Phase 74 tests establishing the shape-guard test pattern)
    - docker/branding-defaults/branding.json (full 13-line file — note actual appName is "SKYNET" uppercase, NOT "Skynet" — do NOT normalize; only add the new field at end)
    - .planning/phases/82-branding-config-wip-indicator-image-overridable/82-CONTEXT.md (§ "Schema shape (LOCKED)", § "Bundled default value (LOCKED)", § "Shape guard (LOCKED)", § "Test surface (LOCKED)")
    - .planning/phases/74-control-style-of-avatar-generation-through-branding-config/74-01-PLAN.md (direct precedent — same atomic-schema-extension mechanics; use this pattern verbatim for the loader edits + JSON edit)
  </read_first>
  <behavior>
    - Test 1 (accepts valid extended config): loadBrandingConfig() called with a fixture containing wipIndicatorPath: "/branding/wip-cube.webp" returns that config unchanged (round-trips through the shape guard).
    - Test 2 (rejects missing wipIndicatorPath): loadBrandingConfig() called with a fixture where wipIndicatorPath is `delete`'d returns bundled defaults (which now carry wipIndicatorPath: "/branding/wip-cube.webp"); the loader's silent shape-rejection branch fires.
    - Test 3 (rejects non-string wipIndicatorPath): loadBrandingConfig() called with a fixture where wipIndicatorPath: 42 (number) returns bundled defaults — shape guard rejects at the typeof branch.
    - Existing Tests 1–7 (Phase 74 director-spec + gamma coverage) continue to pass — validFixture() extension does not regress them.
  </behavior>
  <action>
    In `src/backend/branding/branding-config-loader.ts`:

    1. Extend the exported `BrandingConfig` type at L41-60 to add `wipIndicatorPath: string` — placed AFTER `faviconPath: string` (L46) and BEFORE `pwaIcons` (L47), grouping with the image-URL fields per 82-CONTEXT.md § "Claude's Discretion" recommended order. Add a one-line comment above the new field: `// Phase 82: operator-overridable WIP indicator image; bundled default IS the fallback (unlike Phase 74's intentionally-empty avatarDirectorSpec — no boot gate needed).`
    2. Extend the `HARDCODED_FALLBACK` constant at L76-93 with `wipIndicatorPath: "/branding/wip-cube.webp"` placed AFTER `faviconPath: "/branding/favicon.svg"` (L81) and BEFORE `pwaIcons` (L82). The value MUST be the exact string `/branding/wip-cube.webp` — routes through the branding router's `/branding/*` handler which falls back to `/app/branding-defaults/wip-cube.webp` (relocated by Plan 82-02).
    3. Extend `isValidBrandingShape` at L161-187 with one new branch: `if (typeof o.wipIndicatorPath !== "string") return false;`. Place it AFTER the `pwaIcons` array loop (L169-176) and BEFORE the Phase 74 `avatarDirectorSpec` branch (L180) so the field-order in the guard matches the field-order in the type declaration. Match existing branch style: inline `typeof` check, no zod, single-line return-on-fail.
    4. Do NOT change anything about `loadBrandingConfig()`, `resolveAssetPath()`, `getBundledDefaults()` internals, or the loader's never-throws contract. Extension is type-level + one shape-guard branch + one fallback-constant entry only.

    In `docker/branding-defaults/branding.json`:

    5. Add one new field at the end of the JSON object, immediately after the `pwaIcons` array closes but BEFORE the existing `avatarDirectorSpec` and `avatarGammaDefault` entries — final JSON should read (in this key order): appName, shortName, iconPath, wordmarkPath, faviconPath, wipIndicatorPath, pwaIcons, avatarDirectorSpec, avatarGammaDefault. Value: `"/branding/wip-cube.webp"`. Preserve the file's existing 2-space indentation and existing quote style. NOTE: this file's `appName` is `"SKYNET"` uppercase — do NOT normalize to "Skynet"; only add the one new line.
    6. `jq -e '.wipIndicatorPath == "/branding/wip-cube.webp"' docker/branding-defaults/branding.json` MUST exit 0 after the edit.

    In `src/backend/branding/branding-config-loader.test.ts`:

    7. Update the `bundledDefaultJson` mock object at L44-56 to add `wipIndicatorPath: "/branding/wip-cube.webp"` placed after `faviconPath` and before `pwaIcons` (matching the JSON file's key order from step 5). This keeps the mocked `readFileSync` return value satisfying the extended shape guard, so Phase 74's existing Test 6 (loadBrandingConfig returns bundled defaults on ENOENT) still passes.
    8. Update the `validFixture()` helper at L101-118 to add `wipIndicatorPath: "/branding/wip-cube.webp"` in the same position. All 7 existing Phase 74 tests use this helper — they must all still pass after the addition.
    9. Add a new `describe` block (or extend the existing one) with two new `it(...)` tests placed after the existing Test 7:
       - `it("Test 8: rejects when wipIndicatorPath is missing → bundled defaults", ...)` — pattern-match Phase 74 Test 2 exactly: freshLoader() → build `bad = validFixture()` → `delete bad.wipIndicatorPath` (cast to Partial<>) → set state.configJson = bad → `await loadBrandingConfig()` → assert `cfg.wipIndicatorPath === "/branding/wip-cube.webp"` (bundled defaults returned).
       - `it("Test 9: rejects when wipIndicatorPath is a number → bundled defaults", ...)` — pattern-match Phase 74 Test 3 exactly: `bad = { ...validFixture(), wipIndicatorPath: 42 }` → assert bundled defaults returned (assert on `cfg.wipIndicatorPath === "/branding/wip-cube.webp"` AND `cfg.pwaIcons.length === 2` — the 2-icon bundled shape distinguishes from validFixture's 1-icon shape).
    10. DO NOT delete or modify any existing test in this file. DO NOT change the vi.mock setup, the `state` object, the `freshLoader()` helper, or the `describe` heading. Additive edits only.

    Anti-pattern reminder: do NOT add a fallback branch in the loader that fills in `wipIndicatorPath` when the operator config lacks it — the shape guard's rejection is the correct behavior (bundled defaults come back as one unit). Do NOT populate the bundled JSON with a different string than `/branding/wip-cube.webp` — the router will 404 the asset if the string doesn't match the actual filename that Plan 82-02 lands in `/app/branding-defaults/`.
  </action>
  <verify>
    <automated>npx vitest run src/backend/branding/branding-config-loader.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c '^\s*wipIndicatorPath' src/backend/branding/branding-config-loader.ts (excluding comment lines via `grep -v '^\s*//'`) returns >= 2 (type declaration + HARDCODED_FALLBACK entry)
    - grep -c 'wipIndicatorPath' src/backend/branding/branding-config-loader.ts returns >= 3 (type + fallback + shape-guard branch)
    - grep -c 'wipIndicatorPath' src/backend/branding/branding-config-loader.test.ts returns >= 5 (bundledDefaultJson + validFixture + Test 8 body + Test 9 body + Test 9 assertion)
    - jq -e '.wipIndicatorPath == "/branding/wip-cube.webp"' docker/branding-defaults/branding.json exits 0
    - `npx vitest run src/backend/branding/branding-config-loader.test.ts` exits 0 with at least 9 passing tests (7 existing Phase 74 + 2 new Phase 82)
    - No existing test in branding-config-loader.test.ts has been deleted or its body altered — verify by grepping for the existing 7 `it("Test 1:` through `it("Test 7:` markers and confirming they're still present verbatim
  </acceptance_criteria>
  <done>
    All acceptance criteria above hold. The backend loader's BrandingConfig type + shape guard + HARDCODED_FALLBACK now carry wipIndicatorPath, the bundled JSON matches byte-for-byte, and the test file covers accept + reject-missing + reject-wrong-type without regressing any Phase 74 test.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update assert-boot.test.ts full-shape fixture to carry wipIndicatorPath (Phase 74 pattern precedent)</name>
  <files>src/backend/branding/assert-boot.test.ts</files>
  <read_first>
    - src/backend/branding/assert-boot.test.ts (full file — L33-47 LoadResult type; L53-67 makeValidLoadResult() helper; L69-72 mock setup)
    - src/backend/branding/branding-config-loader.ts (post-Task-1 state — the BrandingConfig type that LoadResult mirrors informally)
    - .planning/phases/82-branding-config-wip-indicator-image-overridable/82-CONTEXT.md (§ "Test surface (LOCKED)" bullet 2 — "update `assert-boot.test.ts` `makeValidLoadResult` if it also carries a full-shape fixture (verify)")
  </read_first>
  <behavior>
    - All 6 existing tests in assert-boot.test.ts continue to pass unchanged. The boot-gate contract exercised by this file is NOT affected by Phase 82 (per 82-CONTEXT.md "No boot gate" LOCKED). The only edit here is fixture hygiene: keeping the local `LoadResult` type + `makeValidLoadResult()` factory field-for-field aligned with the extended backend BrandingConfig, matching the Phase 74 test-hygiene precedent.
  </behavior>
  <action>
    In `src/backend/branding/assert-boot.test.ts`:

    1. Extend the local `LoadResult` type at L33-47 to add `wipIndicatorPath: string;` placed after `faviconPath: string;` (L37) and before `pwaIcons` (L38). Do NOT mark it optional — it's a required field on the backend BrandingConfig, and the local type mirrors that shape.
    2. Extend `makeValidLoadResult()` at L53-67 to add `wipIndicatorPath: "/branding/wip-cube.webp"` placed after `faviconPath: "/branding/favicon.svg"` (L58) and before `pwaIcons` (L59-62). Value MUST match the backend HARDCODED_FALLBACK from Task 1 byte-for-byte.
    3. Do NOT add any new tests. Do NOT modify the existing 6 tests, the mocks, the `exitSpy` setup, or the `freshGate()` helper. Do NOT touch `assert-boot.ts` production code — Phase 82 explicitly does NOT modify the boot gate (per 82-CONTEXT.md § "No boot gate" LOCKED and § "Scope Fence" bullet 1).

    Rationale: `assertBrandingConfigAtBoot()` (the production function under test) currently calls `loadBrandingConfig()` and only inspects `avatarDirectorSpec`. It has no read on `wipIndicatorPath` and no reason to. Extending the fixture is pure test-hygiene — keeps the fixture shape valid so any FUTURE assertion added downstream doesn't hit an undefined field.
  </action>
  <verify>
    <automated>npx vitest run src/backend/branding/assert-boot.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'wipIndicatorPath' src/backend/branding/assert-boot.test.ts returns >= 2 (LoadResult type entry + makeValidLoadResult return-object entry)
    - `npx vitest run src/backend/branding/assert-boot.test.ts` exits 0 with 6 passing tests (Tests 1 through 6 all still present — verify by grepping for `it("Test 1` through `it("Test 6`)
    - No modification to src/backend/branding/assert-boot.ts — `git diff --name-only src/backend/branding/assert-boot.ts` reports the file unchanged in this task's commits
    - No new `it(...)` blocks introduced — grep -c 'it(' src/backend/branding/assert-boot.test.ts unchanged from baseline (baseline: 6)
  </acceptance_criteria>
  <done>
    All acceptance criteria above hold. The full-shape fixture in assert-boot.test.ts now carries wipIndicatorPath so it stays valid against the extended BrandingConfig type; the boot gate itself is untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → `/etc/skynet/branding/branding.json` | Untrusted JSON at loader boundary. Phase 70 contract: `loadBrandingConfig()` never-throws; malformed shape → bundled defaults returned. No new surface introduced by this plan — reuses the existing shape guard with one added branch. |
| operator → `/etc/skynet/branding/wip-cube.webp` (override path) | Untrusted binary asset. Path-containment already enforced by Phase 70's `resolveAssetPath()` V5/V12 guard (branding-config-loader.ts L280-311). This plan adds NO new asset-resolution code path. |
| backend loader → bundled `/app/branding-defaults/branding.json` | Image-baked default file. Path is fixed-absolute (compile-time); no operator input reaches this write path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-01-01 | Tampering | operator-authored `branding.json` shape drift (e.g. `wipIndicatorPath: 42` — number not string) | mitigate | Extended `isValidBrandingShape` rejects with the added `typeof` branch (Task 1 step 3); loader returns bundled defaults which carry the valid `"/branding/wip-cube.webp"` string, so `WipBubble` (post-Plan-82-04) never sees an invalid src prop. |
| T-82-01-02 | Tampering | HARDCODED_FALLBACK vs bundled JSON drift (values diverge) | mitigate | Task 1 step 2 + step 5 both set the value to the exact string `"/branding/wip-cube.webp"`. The acceptance criteria enforce this via grep + jq. Phase 70 D-14 no-config-deploy-preserves-behavior rule stays intact. |
| T-82-01-03 | Information Disclosure | `wipIndicatorPath` exposed via unauthenticated `GET /api/branding` response | accept | The field is a public URL path; no PII, no credentials. Matches existing Phase 70 disposition for `iconPath` / `wordmarkPath` / `faviconPath` (all publicly-fetchable asset URLs). |
| T-82-01-SC | Tampering | Zero new npm/pip/cargo installs in this plan | accept | Plan is pure type extension + JSON edit + test file additions. No package installs; no Package Legitimacy Audit needed. Phase 70/74 dependencies already validated. |
</threat_model>

<verification>
- `npx vitest run src/backend/branding/branding-config-loader.test.ts` exits 0 (9+ passing tests: 7 existing Phase 74 + 2 new Phase 82)
- `npx vitest run src/backend/branding/assert-boot.test.ts` exits 0 (6 passing tests unchanged from Phase 74)
- `jq -e '.wipIndicatorPath == "/branding/wip-cube.webp"' docker/branding-defaults/branding.json` exits 0
- Backend `BrandingConfig` type declaration in branding-config-loader.ts contains `wipIndicatorPath: string` between `faviconPath` and `pwaIcons`
- `HARDCODED_FALLBACK` in branding-config-loader.ts contains `wipIndicatorPath: "/branding/wip-cube.webp"` between `faviconPath` and `pwaIcons`
- `isValidBrandingShape` in branding-config-loader.ts contains a `typeof o.wipIndicatorPath !== "string"` rejection branch
- src/backend/branding/assert-boot.ts is unmodified (Phase 82 explicitly does not touch the boot gate per 82-CONTEXT.md § "Scope Fence" bullet 1)
</verification>

<success_criteria>
- Backend BrandingConfig type carries `wipIndicatorPath: string` as a required field
- Shape guard rejects configs that omit or mis-type `wipIndicatorPath`, with test coverage of both rejection cases
- HARDCODED_FALLBACK and docker/branding-defaults/branding.json both carry `wipIndicatorPath: "/branding/wip-cube.webp"` byte-for-byte
- assert-boot.test.ts fixture stays valid against the extended shape (Phase 74 test-hygiene precedent)
- Loader remains never-throws — no boot enforcement added for the new field (bundled default IS the fallback; no gate needed)
- All existing Phase 74 tests still pass; no existing tests deleted or altered
- No new dependencies; no changes to production boot code (`assert-boot.ts` untouched)
- Plan 82-02 will `git mv public/wip-cube.webp docker/branding-defaults/wip-cube.webp` in parallel; no file overlap with this plan
</success_criteria>

<output>
Create `.planning/phases/82-branding-config-wip-indicator-image-overridable/82-01-SUMMARY.md` when done
</output>
