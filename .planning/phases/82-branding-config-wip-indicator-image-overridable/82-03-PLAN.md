---
phase: 82-branding-config-wip-indicator-image-overridable
plan: 03
type: execute
wave: 2
depends_on:
  - 82-01
files_modified:
  - src/ui/branding/branding-store.ts
  - src/ui/branding/branding-fetch.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Frontend BrandingConfig type in branding-store.ts carries wipIndicatorPath: string, field-for-field matching backend BrandingConfig from Plan 82-01 (position: after faviconPath, before pwaIcons)"
    - "getBundledDefaultsSentinel() in branding-store.ts returns an object containing wipIndicatorPath: '/branding/wip-cube.webp' — byte-for-byte matching docker/branding-defaults/branding.json AND backend HARDCODED_FALLBACK"
    - "isBrandingConfig type guard in branding-fetch.ts rejects a fetched response missing wipIndicatorPath OR where wipIndicatorPath is not a string"
    - "`npx tsc --noEmit` completes without new BrandingConfig-related errors after the type extension"
  artifacts:
    - path: "src/ui/branding/branding-store.ts"
      provides: "Extended frontend BrandingConfig type + sentinel entry for wipIndicatorPath"
      contains: "wipIndicatorPath"
    - path: "src/ui/branding/branding-fetch.ts"
      provides: "Extended isBrandingConfig shape guard rejecting missing/wrong-type wipIndicatorPath"
      contains: "wipIndicatorPath"
  key_links:
    - from: "src/ui/branding/branding-store.ts BrandingConfig type"
      to: "src/backend/branding/branding-config-loader.ts BrandingConfig type"
      via: "GET /api/branding wire-format contract — both types must declare identical fields in identical order (comment invariant at branding-store.ts L58-64)"
      pattern: "wipIndicatorPath.*string"
    - from: "getBundledDefaultsSentinel() in branding-store.ts"
      to: "docker/branding-defaults/branding.json"
      via: "byte-for-byte match invariant (Phase 70 Pitfall 5 mitigation contract per comment at branding-store.ts L22-26 and L71-74)"
      pattern: "wipIndicatorPath.*=.*\"/branding/wip-cube.webp\""
---

<objective>
Mirror the Plan 82-01 backend `BrandingConfig` schema extension onto the frontend. Add `wipIndicatorPath: string` to the exported `BrandingConfig` type in `src/ui/branding/branding-store.ts`, to the `getBundledDefaultsSentinel()` return-object, and to the `isBrandingConfig` shape guard in `src/ui/branding/branding-fetch.ts`. Values must agree byte-for-byte with the backend HARDCODED_FALLBACK and the bundled JSON default.

Purpose: keeps the wire-format contract between backend and frontend intact (a shape mismatch would cause TypeScript compile errors at the consumer, per the invariant documented at branding-store.ts L58-64). Also ensures a defensive shape guard on the frontend so the store never receives a `wipIndicatorPath: undefined` response that would blow up WipBubble's `<img src={undefined}>` after Plan 82-04's rewire.

Output:
- Extended `BrandingConfig` type at `src/ui/branding/branding-store.ts` with `wipIndicatorPath: string` appended after `faviconPath` and before `pwaIcons`
- Extended `getBundledDefaultsSentinel()` with `wipIndicatorPath: "/branding/wip-cube.webp"` in the same position
- Extended `isBrandingConfig` type guard in `branding-fetch.ts` with `if (typeof o.wipIndicatorPath !== "string") return false;`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82-branding-config-wip-indicator-image-overridable/82-CONTEXT.md
@.planning/phases/82-branding-config-wip-indicator-image-overridable/82-01-PLAN.md
@src/ui/branding/branding-store.ts
@src/ui/branding/branding-fetch.ts
@src/backend/branding/branding-config-loader.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend frontend BrandingConfig type + bundled sentinel to include wipIndicatorPath</name>
  <files>src/ui/branding/branding-store.ts</files>
  <read_first>
    - src/ui/branding/branding-store.ts (full 181-line file — L50-67 BrandingConfig type declaration with Phase 74 mirror comment at L57-64; L76-94 getBundledDefaultsSentinel() with byte-for-byte-match comment at L87-88)
    - src/backend/branding/branding-config-loader.ts (post-Plan-82-01 state — L41-60 extended BrandingConfig type; L76-93 extended HARDCODED_FALLBACK — the type this file must mirror byte-for-byte)
    - docker/branding-defaults/branding.json (post-Plan-82-01 state — bundled JSON now contains `"wipIndicatorPath": "/branding/wip-cube.webp"`; this is the wire-format the sentinel mirrors)
    - .planning/phases/82-branding-config-wip-indicator-image-overridable/82-CONTEXT.md (§ "Schema shape (LOCKED)", § "Bundled default value (LOCKED)")
  </read_first>
  <action>
    In `src/ui/branding/branding-store.ts`:

    1. Extend the exported `BrandingConfig` type at L50-67 to add `wipIndicatorPath: string;` placed AFTER `faviconPath: string;` (L55) and BEFORE `pwaIcons` (L56). Field order MUST match backend branding-config-loader.ts post-Plan-82-01 exactly. Add a one-line comment above the new field: `// Phase 82: operator-overridable WIP indicator image src (bundled default IS the fallback — no boot gate).`
    2. Extend `getBundledDefaultsSentinel()` at L76-94 to add `wipIndicatorPath: "/branding/wip-cube.webp"` placed AFTER `faviconPath: "/branding/favicon.svg"` (L81) and BEFORE `pwaIcons` (L82-85). Value MUST match docker/branding-defaults/branding.json AND backend HARDCODED_FALLBACK byte-for-byte. This is the Phase 70 Pitfall 5 mitigation contract from 70-RESEARCH.md preserved by the L87-88 comment invariant — do not break it.
    3. Do NOT change `publishBrandingConfig`, `subscribe`, `notify`, `useBrandingConfig`, `__resetForTest`, `snapshotVersion`, or ANY comment block. Additive edits only.

    No frontend consumer other than `WipBubble` (post-Plan-82-04) will read `wipIndicatorPath`. The type mirror exists to satisfy the wire-format contract — `/api/branding` publishes the whole config through Phase 70's fetch/publish pipeline, and a shape mismatch would surface as a TypeScript compile error at the publish call site (branding-fetch.ts publishBrandingConfig(json)).
  </action>
  <verify>
    <automated>grep -c 'wipIndicatorPath' src/ui/branding/branding-store.ts | grep -v '^0$' && grep -c 'wipIndicatorPath.*"/branding/wip-cube.webp"' src/ui/branding/branding-store.ts | grep -v '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'wipIndicatorPath' src/ui/branding/branding-store.ts returns >= 2 (type declaration + sentinel entry)
    - grep -c 'wipIndicatorPath: "/branding/wip-cube.webp"' src/ui/branding/branding-store.ts returns exactly 1 (only the sentinel — the type declaration uses `: string`, not the literal value)
    - The exported `BrandingConfig` type in branding-store.ts declares fields in this order: appName, shortName, iconPath, wordmarkPath, faviconPath, wipIndicatorPath, pwaIcons, avatarDirectorSpec, avatarGammaDefault — visually confirm order matches backend branding-config-loader.ts post-Plan-82-01
    - No signature change to `publishBrandingConfig`, `subscribe`, `useBrandingConfig`, or `__resetForTest` (grep for their function declarations shows byte-identical signatures pre and post)
    - Anti-pattern comment block at L28-36 (no React Context, no external state package) is unchanged (verify by grep for the "DO NOT introduce a React Context provider" phrase — must still be present verbatim)
  </acceptance_criteria>
  <done>
    All acceptance criteria above hold. The frontend BrandingConfig type + sentinel now mirror the backend field-for-field including wipIndicatorPath, and the values agree byte-for-byte across backend HARDCODED_FALLBACK, bundled JSON, and frontend sentinel.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend isBrandingConfig shape guard in branding-fetch.ts to reject missing/wrong-type wipIndicatorPath</name>
  <files>src/ui/branding/branding-fetch.ts</files>
  <read_first>
    - src/ui/branding/branding-fetch.ts (full 86-line file — L45-55 isBrandingConfig type guard; L60-85 fetchBrandingConfig() using the guard before publishBrandingConfig)
    - src/backend/branding/branding-config-loader.ts (post-Plan-82-01 state — L161-187 extended isValidBrandingShape as the pattern to match; note frontend guard is more lax than backend — it does NOT validate pwaIcons entries or the Phase 74 fields, per the current L45-55 shape)
    - .planning/phases/82-branding-config-wip-indicator-image-overridable/82-CONTEXT.md (§ "Shape guard (LOCKED)" — mandates the guard extension on both loader AND branding-fetch.ts equivalent)
  </read_first>
  <action>
    In `src/ui/branding/branding-fetch.ts`:

    1. Extend the `isBrandingConfig` type guard at L45-55 with one new branch: `if (typeof o.wipIndicatorPath !== "string") return false;`. Place it AFTER `if (typeof o.faviconPath !== "string") return false;` (L52) and BEFORE `if (!Array.isArray(o.pwaIcons)) return false;` (L53). Match existing branch style: inline `typeof` check, no zod, single-line return-on-fail.
    2. Do NOT change `fetchBrandingConfig()`, the try/catch structure, the silent-no-op-on-failure semantics (L74-85), or any comment block. The shape guard extension is the ONLY change.
    3. Do NOT add validation for Phase 74 fields (`avatarDirectorSpec`, `avatarGammaDefault`) even though they're currently un-validated at this frontend boundary — that's out of scope for Phase 82 (per 82-CONTEXT.md § "Scope Fence"). Only wipIndicatorPath is added.

    Rationale for validating on frontend even though backend already validates: the network is a distinct trust boundary per the comment at L38-43. A compromised/misrouted response (e.g. nginx SPA fallback serving index.html with `Content-Type: application/json` header spoofed, or a stale backend running a pre-Phase-82 build) could return a partial shape. The defensive guard keeps the store's sentinel intact rather than corrupting it with a `wipIndicatorPath: undefined` that WipBubble (post-Plan-82-04) would then render as `<img src={undefined}>` — a broken-image rectangle in the UI.
  </action>
  <verify>
    <automated>grep -c 'typeof o.wipIndicatorPath !== "string"' src/ui/branding/branding-fetch.ts | grep -v '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'wipIndicatorPath' src/ui/branding/branding-fetch.ts returns exactly 1 (the one new guard branch)
    - grep -c 'typeof o.wipIndicatorPath !== "string"' src/ui/branding/branding-fetch.ts returns exactly 1
    - The new branch appears LEXICALLY BETWEEN the faviconPath check and the pwaIcons check — verify with: `grep -n 'faviconPath\|wipIndicatorPath\|pwaIcons' src/ui/branding/branding-fetch.ts` — the line numbers must be strictly increasing in that order (faviconPath < wipIndicatorPath < pwaIcons)
    - No modification to `fetchBrandingConfig()` — its function signature and body are unchanged (verify by grep for `export async function fetchBrandingConfig(): Promise<void>` and confirm the surrounding try/catch structure is byte-identical)
    - Phase 74 fields remain un-validated in isBrandingConfig — grep -c 'avatarDirectorSpec\|avatarGammaDefault' src/ui/branding/branding-fetch.ts returns 0 (unchanged from baseline; Phase 82 does NOT expand validation surface for Phase 74 fields)
  </acceptance_criteria>
  <done>
    All acceptance criteria above hold. The frontend fetch-boundary shape guard now rejects malformed responses that lack wipIndicatorPath or carry a non-string value, and the fetchBrandingConfig contract is otherwise untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network → frontend (fetch `/api/branding`) | Distinct trust boundary from the backend loader. Comment at branding-fetch.ts L38-43 documents the "re-validate here" rationale: a compromised/misrouted response (nginx SPA fallback returning HTML, stale backend build, MITM) is rejected quietly rather than corrupting the store. |
| frontend store → React consumers | `useBrandingConfig()` returns `BrandingConfig` — a shape mismatch would surface as TypeScript compile error at the consumer, NOT a runtime crash. Safe by construction post-tsc. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-03-01 | Tampering | Malformed `/api/branding` response with missing or non-string wipIndicatorPath | mitigate | Task 2 adds the `typeof o.wipIndicatorPath !== "string"` branch to isBrandingConfig; the store retains its bundled-default sentinel (which now carries the valid `"/branding/wip-cube.webp"` string per Task 1). Same silent-retain semantics as Phase 70 for the other fields. |
| T-82-03-02 | Tampering | Frontend sentinel drifts from backend HARDCODED_FALLBACK / bundled JSON default | mitigate | Task 1 step 2 hard-codes the value as `"/branding/wip-cube.webp"`; acceptance criteria assert exactly-1 grep match on that literal. Cross-file byte-agreement is a phase-level invariant enforced by verification checklist. |
| T-82-03-03 | Information Disclosure | wipIndicatorPath field visible to unauthenticated frontend | accept | Same disposition as Phase 70's iconPath / wordmarkPath / faviconPath — publicly-fetchable URL paths, no PII, no credentials. Matches Phase 70 T-70-03-04 disposition. |
| T-82-03-SC | Tampering | Zero new npm installs in this plan | accept | Pure type extension + one guard branch — no package installs, no dependency changes. |
</threat_model>

<verification>
- `grep -c 'wipIndicatorPath' src/ui/branding/branding-store.ts` returns >= 2 (type + sentinel)
- `grep -c 'wipIndicatorPath: "/branding/wip-cube.webp"' src/ui/branding/branding-store.ts` returns exactly 1 (sentinel value literal)
- `grep -c 'typeof o.wipIndicatorPath !== "string"' src/ui/branding/branding-fetch.ts` returns exactly 1
- Field order in frontend BrandingConfig type matches backend BrandingConfig type (both: appName, shortName, iconPath, wordmarkPath, faviconPath, wipIndicatorPath, pwaIcons, avatarDirectorSpec, avatarGammaDefault)
- `npx tsc --noEmit` completes without any NEW BrandingConfig-related errors (baseline errors, if any, are unchanged — compare grep output against pre-plan baseline)
- No changes to `publishBrandingConfig`, `useBrandingConfig`, `__resetForTest`, `fetchBrandingConfig` signatures or comment blocks (additive edits only)
</verification>

<success_criteria>
- Frontend BrandingConfig type carries `wipIndicatorPath: string` in field-order agreement with backend
- Bundled-default sentinel value `"/branding/wip-cube.webp"` agrees byte-for-byte with backend HARDCODED_FALLBACK (Plan 82-01) and docker/branding-defaults/branding.json (Plan 82-01)
- Frontend fetch-boundary shape guard rejects missing or wrong-type wipIndicatorPath responses
- No consumer-facing behavior change from this plan — Plan 82-04 is what actually renders the new field to the DOM
- Plan 82-04 can safely read `useBrandingConfig().wipIndicatorPath` and be guaranteed a string (from the sentinel at initial paint, from the published operator config after boot fetch resolves)
</success_criteria>

<output>
Create `.planning/phases/82-branding-config-wip-indicator-image-overridable/82-03-SUMMARY.md` when done
</output>
