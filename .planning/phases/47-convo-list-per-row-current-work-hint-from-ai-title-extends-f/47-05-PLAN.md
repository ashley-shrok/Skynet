---
phase: 47-convo-list-per-row-current-work-hint-from-ai-title-extends-f
plan: 05
type: execute
wave: 4
depends_on: [47-04]
files_modified:
  - src/ui/features/pretty-conversations/PrettyConversationRow.tsx
  - src/ui/features/pretty-conversations/pretty-conversations.css
  - src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx
autonomous: true
requirements: []
tags: [pretty-conversation-row, css, working-spinner, avatar-corner-badges, meta-retirement, fade-truncation, v14-locked-shape]

must_haves:
  truths:
    - "Title line: identity name + one space + hostname wrapped in parens `(hostname)` — parens SAME font-size as identity name, alpha ~0.85, mask-image right-edge fade for truncation instead of ellipsis"
    - "Subtitle line: the identity's current aiTitle string (from the aiTitle prop threaded in Plan 47-04); when aiTitle is null, render single italic ellipsis '…' at muted alpha (~0.55) so row height anchors regardless of ai-title presence; Server icon dropped; fade-truncation applied via mask-image"
    - "Working indicator (INVERSION): idle rows have NOTHING; working rows get slow dashed spinner ring on `.pv-avatar::before`. Gate rule (Ashley 2026-08-19 verbatim, DO NOT WIDEN): `showSpinner = !(inActiveSet && isWorking === false && !isRecycling && !hasQueuePending)` — the SAME 4-input boolean the ready-dot uses, evaluated the same way, with the FINAL boolean inverted"
    - "`.pv-meta` right column retired: ready-dot JSX removed; PrettyBountyCountBadge REUSED VERBATIM but repositioned to absolute avatar corners (Pin bottom-left, Monitor bottom-right); `.pv-meta` element removed from row markup (or kept as empty stub — implementation choice, prefer removal per CONTEXT.md § .pv-meta right column retirement)"
    - "PrettyBountyCountBadge.tsx component and its `.pv-bounty-badge*` CSS class values are UNCHANGED VERBATIM (patch #468 preserved) — only the badge's positioning host changes from `.pv-meta` to inside `.pv-avatar` at absolute corners"
  artifacts:
    - path: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx"
      provides: "Row markup restructured per v14 shape — title line has (hostname) suffix; subtitle line is aiTitle text; ready-dot JSX deleted; PrettyBountyCountBadge relocated inside .pv-avatar; .pv-meta element removed (or empty stub)"
      contains: "aiTitle"
    - path: "src/ui/features/pretty-conversations/pretty-conversations.css"
      provides: "`.pv-avatar` gets position:relative + overflow:visible; new `.pv-avatar::before` spinner CSS (p05-slow-dashed pattern from ring-patterns.html); `.pv-label` + `.pv-host` gain mask-image right-edge fade; `.pv-host` restyled per v14 (color, size, weight, text-shadow); `.pv-ready-dot` block deleted; `.pv-meta` block deleted or reduced to stub; badge-wrap absolute-positioning rules added at avatar bottom corners; @keyframes for the spinner rotation"
      contains: "conic-gradient"
    - path: "src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx"
      provides: "New tests covering: title line with (hostname) suffix, subtitle renders aiTitle when present, subtitle renders '…' when aiTitle null, working spinner class/ring present per inverted-boolean rule, .pv-meta absent from markup, Pin badge at avatar bottom-left, Monitor badge at avatar bottom-right"
      contains: "Phase 47 Plan 05"
  key_links:
    - from: "PrettyConversationRow's aiTitle prop (added Plan 47-04)"
      to: "the subtitle line's rendered text content"
      via: "conditional render: aiTitle ? <span className='pv-ai-title'>{aiTitle}</span> : <span className='pv-ai-title pv-ai-title--placeholder'>…</span>"
      pattern: "aiTitle \\? .* aiTitle"
    - from: "PrettyConversationRow's inverted-boolean spinner gate"
      to: "the .pv-row.active-set:is(.working,.recycling) selector that triggers .pv-avatar::before"
      via: "JS-only className toggle (do NOT widen the CSS-only gate — CSS already handles it via `.working` and `.recycling` classes the row emits today)"
      pattern: "\\.pv-avatar::before"
---

<objective>
Land the v14 locked visual shape on PrettyConversationRow — the biggest surface change of Phase 47. Restructure the row markup per Ashley's 2026-08-19 design lock: identity name + `(hostname)` parens on the title line; aiTitle as fade-truncated subtitle; inverted-boolean working spinner on the avatar (replaces the ready-dot); PrettyBountyCountBadge V12 wraps relocated to absolute avatar corners; `.pv-meta` right column retired. All CSS + markup + tests here — the type surface, hook subscriptions, and prop threading landed in Plans 47-01/03/04.

Purpose: complete Phase 47's end-to-end delivery. After this plan ships, Ashley sees the v14 shape in production on every convo-list row that has an aiTitle.

Output: PrettyConversationRow renders per v14 shape verbatim; PrettyBountyCountBadge.tsx unchanged (verbatim reuse per CONTEXT.md § Badge relocation V12 style reuse); pretty-conversations.css block for `.pv-ready-dot` and `.pv-meta` deleted, replaced with `.pv-avatar::before` spinner + absolute-positioned badge-wrap rules; test coverage for all v14 shape invariants including the inverted-boolean spinner gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-convo-list-per-row-current-work-hint-from-ai-title-extends-f/47-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restructure PrettyConversationRow markup per v14 shape — title-line hostname parens, subtitle aiTitle, ready-dot deletion, badge relocation to avatar corners, .pv-meta retirement</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationRow.tsx</files>
  <read_first>
    - src/ui/features/pretty-conversations/PrettyConversationRow.tsx lines 145-251 (props destructure + interface — Plan 47-04 added aiTitle prop; this task consumes it)
    - src/ui/features/pretty-conversations/PrettyConversationRow.tsx lines 889-1121 (the render tree — .pv-row className composition at ~L889, avatar block at ~L966-984, body block at ~L1003-1025 with pv-label + pv-host, pv-meta block at ~L1055-1121 with PrettyBountyCountBadge + ready-dot; ALL of this changes shape)
    - src/ui/features/pretty-conversations/PrettyConversationRow.tsx lines 15-50 (top-of-file docblock; extend with a Phase 47 Plan 05 v14-shape note documenting the retired symbols: ready-dot render, .pv-meta wrapper, hostname-on-sublabel; and the new symbols: title-parens suffix, aiTitle subtitle, spinner ring)
    - src/ui/features/pretty-conversations/PrettyBountyCountBadge.tsx (all 70 lines — REUSED VERBATIM; do not modify. Only its DOM position moves.)
    - .planning/phases/47-convo-list-per-row-current-work-hint-from-ai-title-extends-f/47-CONTEXT.md § Locked design (title line / subtitle line / working indicator / badge relocation / .pv-meta retirement / fallback rules)
    - ~/.claude/roles/box-maintainer/bounties/convo-list-current-work-hint/v14-console-snippet.js lines 100-165 (the live-app taste showing exact DOM shape Ashley signed off on — read this end to end)
  </read_first>
  <behavior>
    - Test 1 (title parens): a row with identity displayName "Tanya" and host name "skynet-ec2" renders a `.pv-label` whose textContent is "Tanya (skynet-ec2)" — parens contain the hostname, single space between name and parens.
    - Test 2 (title with no host): a row where `row.host` is null renders `.pv-label` with just the identity displayName, no trailing parens or space.
    - Test 3 (subtitle with aiTitle present): a row with `aiTitle="Fix bug X"` renders a subtitle span whose textContent is "Fix bug X".
    - Test 4 (subtitle with aiTitle null): a row with `aiTitle={null}` renders a subtitle span containing "…" with italic styling AND a `pv-ai-title--placeholder` (or similar) class marker.
    - Test 5 (subtitle: no Server icon): the subtitle line renders NO Server svg from lucide (per CONTEXT.md § Subtitle line — Server icon dropped since hostname is now on the title line).
    - Test 6 (spinner-inverted-boolean gate — working): a row with `inActiveSet=true` AND `isWorking=true` renders with the `.working` class (unchanged from today); assertion of Plan 47-05 shape: the `.pv-ready-dot` element is ABSENT (retired); the spinner is a CSS pseudo-element on `.pv-avatar::before` so it is not directly queryable in jsdom — this test is a defense-in-depth assertion that the `.pv-ready-dot` element is not in the DOM tree ever (retired invariant).
    - Test 7 (idle row — no ready-dot, no spinner class action needed): a row with `inActiveSet=true, isWorking=false, isRecycling=false, hasQueuePending=false` renders WITHOUT the `.pv-ready-dot` element in the DOM (the ready-dot render is fully deleted). CSS handles spinner-non-appearance via the `.pv-avatar::before` selector NOT matching (since neither `.working` nor `.recycling` is applied).
    - Test 8 (.pv-meta absent from markup): the row's queryable DOM does NOT contain any element with className including `pv-meta` (element retired per CONTEXT.md § .pv-meta right column retirement).
    - Test 9 (Pin badge inside avatar): a row with `pinnedCount > 0` renders the `.pv-bounty-badge-wrap` for Pin INSIDE the `.pv-avatar` element (queryable via `.pv-avatar .pv-bounty-badge-wrap[data-testid="pv-bounty-badge-pinned"]`).
    - Test 10 (Monitor badge inside avatar): a row with `needsDeskCount > 0` renders the `.pv-bounty-badge-wrap` for Monitor INSIDE the `.pv-avatar`.
    - Test 11 (subtitleMode prop retirement OR compat): the pre-existing `subtitleMode` prop either continues to be accepted (for backward compat with existing render-site callers) but has no effect (the new subtitle is always aiTitle regardless of the prop's value), OR is fully removed and all 5 render sites in PrettyConversationsPanel.tsx are updated to drop the prop. Choose based on scope: if removal is safe (grep confirms subtitleMode has zero other consumers), remove; else accept-and-ignore with a docblock note. Document the choice in the SUMMARY.
  </behavior>
  <action>
    Restructure the row's render tree per the v14 locked shape.

    1. **Update the top-of-file docblock (lines 15-50)** with a "Phase 47 Plan 05 (v14 locked shape)" note. Enumerate retired symbols: `.pv-meta` element, `.pv-ready-dot` element + inline style="display: block" hack, Server-icon-in-sublabel render path, subtitleMode prop-driven sublabel branching. Enumerate new symbols: title-line hostname-parens suffix `(hostname)`, aiTitle subtitle line with placeholder-ellipsis fallback, PrettyBountyCountBadge relocation to inside `.pv-avatar` at absolute corners, inverted-boolean working spinner via CSS-only `.pv-avatar::before` (JS emits the same `.working` / `.recycling` classes — inversion happens at the CSS gate, not in JS). Cite 47-CONTEXT.md § Locked design and Ashley 2026-08-19 verbatim quote on the invert-final-boolean rule.

    2. **Update the props destructure** (lines 145-161): `aiTitle` is already added by Plan 47-04. If `subtitleMode` prop is going to be REMOVED (Test 11 choice), drop it from the destructure AND the interface. Otherwise keep it in the interface with a "Phase 47 Plan 05 — accepted for backward compat, has no runtime effect; new subtitle is always aiTitle" comment.

    3. **Rewrite the `<div className="pv-body">` block (currently lines 1003-1025)**:
       - `.pv-label` span now renders: `{identity ? identity.displayName : row.label}` followed by (when `row.host` is present) a suffix `<span className="pv-hostname-suffix"> ({row.host.name})</span>`. The suffix's className is a new marker for CSS to apply the alpha-0.85 subtle-softer treatment (CONTEXT.md § Title line: "SAME font-size as identity name (not smaller), alpha .85, inherits font-family").
       - Below `.pv-label`, replace the current `.pv-host` block (which renders hostname + Server icon OR identity.title based on subtitleMode) with a NEW subtitle span. Use class `pv-ai-title` (or reuse `pv-host` if easier — the CSS block reshape in Task 2 keys off whatever className this element carries; pick one consistently). Content:
         - When `aiTitle !== null`: `<span className="pv-ai-title">{aiTitle}</span>`.
         - When `aiTitle === null`: `<span className="pv-ai-title pv-ai-title--placeholder">…</span>` (single ellipsis U+2026, italic styling handled by CSS).
       - Drop the Server icon import if no other render path uses it (grep to confirm before removing the import).

    4. **Rewrite the `<div className="pv-avatar">` block (currently lines 966-984)** to accommodate the corner-badge slots:
       - Inside the avatar div, BEFORE the identity avatar / initial letter / fallback icon render (preserve those verbatim), add:
         ```
         <PrettyBountyCountBadge pinnedCount={bountyCounts?.pinnedCount} needsDeskCount={bountyCounts?.needsDeskCount} />
         ```
         (Move the existing invocation from inside `.pv-meta` up here.) The badge component's own render produces two `.pv-bounty-badge-wrap` spans (one for Pin, one for Monitor); the CSS in Task 2 absolute-positions each wrap at the appropriate avatar corner.
       - Alternative if the badge component's own container is inconvenient (its `.pv-bounty-badge` outer span groups both wraps in a flex row — inconvenient for absolute-positioning them independently): instead of calling the badge component here, render the two wraps directly using the same DOM shape the component emits, so each wrap is a direct child of `.pv-avatar` and CSS can absolute-position them independently. Preserve the wrap markup + inner icon + count-pill VERBATIM from PrettyBountyCountBadge.tsx (lines 44-68) — copy the JSX shape, do NOT modify the component's own file. Add a code comment: "Duplicated JSX shape from PrettyBountyCountBadge.tsx to enable independent absolute-positioning at avatar corners; keep the two shapes in sync manually. Alternative refactor (extract sub-component) rejected for Phase 47 scope — CONTEXT.md § Badge relocation V12 style reuse locks the source component as verbatim-preserved."
       - Prefer the alternative (direct-render duplication) if it keeps the CSS simpler; document the choice in the SUMMARY.

    5. **Delete the `<div className="pv-meta">` block (currently lines 1055-1121)** entirely, INCLUDING:
       - The PrettyBountyCountBadge invocation (moved to inside `.pv-avatar` per step 4).
       - The `.pv-ready-dot` span (lines 1109-1120) with its inline `style={{ display: "block" }}` and the `isWorking === false && !isRecycling && !hasQueuePending && (...)` JSX gate. FULLY REMOVED — the working-spinner is CSS-only on `.pv-avatar::before` and appears when the row's className rollup includes `.working` OR `.recycling` (per Ashley's verbatim invert-final-boolean rule: same 4-input gate, CSS-only inversion via `.pv-row.active-set:is(.working, .recycling)`).
       - Retain the docblock comments as a Phase 47 Plan 05 "retired" note if useful for future readers, else delete.
       - The row's grid template (currently `40px 1fr auto` for avatar/body/meta) changes to `40px 1fr` (avatar/body) — this lives in CSS Task 2, but note here that the JSX no longer has the third grid child.

    6. **Do NOT modify** the rowClassName composition at line 889-902 — the `.working` and `.recycling` classes must continue to be emitted exactly as today (that's the JS side of the inverted-boolean rule; the CSS side is where the inversion happens in Task 2 via `.pv-row.active-set:is(.working, .recycling) .pv-avatar::before`). The `.pv-row.active-set` and `.pv-row.working` and `.pv-row.recycling` classes are ALL already emitted by the current row per lines 893-895 — this task changes NOTHING at the className site.

    7. **Do NOT modify** PrettyBountyCountBadge.tsx (verbatim reuse per CONTEXT.md § Badge relocation).

    8. **If `subtitleMode` prop retirement was chosen** (Test 11): also update PrettyConversationsPanel.tsx at all 5 PrettyConversationRowLive render sites (search-flat, pinned, middle, RDP, hidden) to drop the `subtitleMode="identityTitle"` and `subtitleMode={row.rdpHostRow === true ? undefined : "identityTitle"}` props. Grep-verify no other consumer exists. If retention was chosen, leave the panel sites unchanged.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pv-meta' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` <= 2 (any remaining occurrences are only in retired-comment prose, not in JSX className; ideally 0)
    - `grep -Fc 'pv-ready-dot' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` == 0 (span fully removed; comments referencing it may live in docblock, so allow up to 2 in prose — verify manually)
    - `grep -Fc 'data-pv-conv-ready-dot' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` == 0 (data attribute retired)
    - `grep -Fc 'display: "block"' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` == 0 (inline-style hack retired)
    - `grep -c 'aiTitle' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` >= 6 (prop consume + subtitle-render conditional + placeholder branch + docblock references + interface)
    - `grep -Fc 'pv-hostname-suffix' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` >= 1 (title-line parens marker present)
    - `grep -Fc 'pv-ai-title' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` >= 2 (subtitle span + placeholder-branch variant)
    - PrettyBountyCountBadge.tsx UNTOUCHED: `git diff HEAD -- src/ui/features/pretty-conversations/PrettyBountyCountBadge.tsx` returns empty (verbatim-preserved per CONTEXT.md badge lock)
    - PrettyBountyCountBadge markup (or verbatim-duplicated shape) present inside `.pv-avatar`: `grep -A 30 'className="pv-avatar"' src/ui/features/pretty-conversations/PrettyConversationRow.tsx | grep -c 'pv-bounty-badge'` >= 1
    - Row grid template unchanged in JSX (CSS Task 2 owns the grid-template-columns change): the row's outer wrapper className composition is untouched
    - `npx vitest run src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` exit 0 (pre-existing tests may need updates for the retired ready-dot / added subtitle — Task 3 owns the test-file rewrite; this task must at minimum not add NEW failures beyond those Task 3 addresses)
    - `npm run build` exit 0 (typecheck green)
    - `grep -c 'as any\|@ts-expect-error' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` == 0
  </acceptance_criteria>
  <done>PrettyConversationRow markup per v14 shape: title line has `(hostname)` suffix, subtitle is aiTitle (or ellipsis placeholder), ready-dot fully deleted, `.pv-meta` retired, PrettyBountyCountBadge markup relocated inside `.pv-avatar` (either via component call or verbatim JSX duplication); PrettyBountyCountBadge.tsx untouched; typecheck green; test file updates land in Task 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite pretty-conversations.css per v14 shape — avatar corner-badge positioning, spinner ring on .pv-avatar::before, fade-truncation on title + subtitle, .pv-ready-dot + .pv-meta blocks deleted, title-parens styling, subtitle restyle</name>
  <files>src/ui/features/pretty-conversations/pretty-conversations.css</files>
  <read_first>
    - src/ui/features/pretty-conversations/pretty-conversations.css lines 490-660 (AVATAR DISC + PV-BODY + PV-META + READY-DOT blocks — most of this rewrites)
    - src/ui/features/pretty-conversations/pretty-conversations.css lines 1138-1270 (V12 badge block + mobile bump — these classes stay verbatim; only their positioning ancestor changes to `.pv-avatar` via new rules)
    - ~/.claude/roles/box-maintainer/bounties/convo-list-current-work-hint/ring-patterns.html lines 196-218 (the p05 dashed-spinner CSS — the conic-gradient + mask + animation values Ashley picked. Copy verbatim, adjust variable name from `--hue` to Skynet's `--pv-hue`.)
    - ~/.claude/roles/box-maintainer/bounties/convo-list-current-work-hint/v14-console-snippet.js lines 29-92 (the live-taste CSS Ashley signed off — copy the mask-image right-edge fade, the .v14-av-pin/.v14-av-desk absolute positioning at `bottom: -4px; left/right: -8px`, the subtitle color/size/weight/text-shadow. Translate class names from v14-* to Skynet's production names.)
  </read_first>
  <behavior>
    Not applicable as a TDD RED cycle — CSS-only changes. Test coverage lives in Task 3 (DOM-shape assertions) + Nyquist visual UAT (post-deploy, orchestrator scope). This task's correctness is verified by:
    - Manual eyeball at implementation time via `npm run dev` on a session with a real ai-title (Ashley will UAT post-deploy).
    - CSS-syntax validity via `npm run build` producing a valid CSS bundle exit 0.
    - Source assertions in the <acceptance_criteria> below.
  </behavior>
  <action>
    Rewrite the CSS blocks per the v14 locked shape.

    1. **`.pv-avatar` block (lines 497-518)**: add `position: relative;` and `overflow: visible;` so the corner-badge wraps can extend outside the circle bounds. Preserve all existing gradient / border / box-shadow / size rules verbatim. Add a Phase 47 Plan 05 comment.

    2. **New `.pv-avatar::before` spinner block** (add after the `.pv-avatar` block, before `.pv-body`):
       - Copy the p05 CSS from ring-patterns.html lines 197-218 verbatim, substituting `hsla(var(--hue), 70%, 65%, 1)` with `hsla(var(--pv-hue, 210), 70%, 65%, 1)` and adjusting the selector.
       - Selector: `.pv-row.active-set:is(.working, .recycling) .pv-avatar::before` (matches the v14 console snippet's `.pv-row.v14taste.active-set:is(.working, .recycling) .pv-avatar::before`). NOTE: this is the ENTIRE inverted-boolean gate on the CSS side — the JS gate the ready-dot used (`isWorking === false && !isRecycling && !hasQueuePending`) is INVERTED here by using `:is(.working, .recycling)` (which is the NEGATION of the ready-dot's `.active-set:not(.working):not(.recycling)` inside the `.active-set` scope). The `hasQueuePending` axis is NOT surfaced as a row className (it lives only in JS today per PrettyConversationRow.tsx lines 1097-1100). CONTEXT.md accepts this tradeoff: a queued-message row will still show the spinner, since queue-pending is not a CSS class. Add an inline docblock citing 47-CONTEXT.md § Working indicator INVERSION and Ashley's "that is fine for now" acceptance.
       - Add `@keyframes pv-spinner-spin { to { transform: rotate(360deg); } }` at file scope (mirror the v14-console-snippet's `@keyframes v14-spin`); reference it in the spinner block's `animation: pv-spinner-spin 3s linear infinite;`. Verify no existing `@keyframes pv-spinner-spin` name collision via grep.
       - No transition on the pseudo-element's appearance (rotation itself is the motion).
       - `pointer-events: none;` and `z-index: 1;` (matches v14-console-snippet).

    3. **New badge-wrap absolute-positioning rules** (add after the spinner block):
       ```
       .pv-avatar .pv-bounty-badge-wrap { position: absolute; bottom: -4px; z-index: 3; pointer-events: none; }
       .pv-avatar .pv-bounty-badge-wrap[data-testid="pv-bounty-badge-pinned"] { left: -8px; }
       .pv-avatar .pv-bounty-badge-wrap[data-testid="pv-bounty-badge-needs-desk"] { right: -8px; }
       ```
       (This is illustrative; the executor writes the real rules. Adjust selectors if Task 1 chose the verbatim-JSX-duplication route with different data-testid values.)
       Do NOT modify the pre-existing `.pv-bounty-badge*` rules at lines 1144-1180 — those preserve the icon size + count-pill styling verbatim per CONTEXT.md § Badge relocation V12 style reuse.

    4. **`.pv-body .pv-label` block (lines 541-551)**: replace `text-overflow: ellipsis;` with `text-overflow: clip;` and add `-webkit-mask-image: linear-gradient(to right, #000 0, #000 calc(100% - 22px), transparent 100%); mask-image: linear-gradient(to right, #000 0, #000 calc(100% - 22px), transparent 100%);`. This is the v14-locked fade-truncation from v14-console-snippet lines 82-84.

    5. **New `.pv-label .pv-hostname-suffix` block** (add near `.pv-label`): match the title-line hostname parens style per CONTEXT.md § Title line: `font-size: inherit; font-weight: inherit; color: rgba(232, 228, 216, 0.85);`. Consumed by Task 1's new suffix span.

    6. **`.pv-body .pv-host` block (lines 553-569)**: this block was for the OLD hostname sublabel. Repurpose it (if Task 1 kept the `.pv-host` classname on the new subtitle) OR rename to `.pv-ai-title` (if Task 1 chose the new classname). Apply the v14 subtitle styling from v14-console-snippet lines 86-92:
       - `color: #f0ece0;`
       - `font-size: 13.5px;`
       - `font-weight: 500;`
       - `text-shadow: 0 1px 2px rgba(0, 0, 0, 0.4);`
       - `text-overflow: clip;` + mask-image right-edge fade (same as Step 4)
       - `white-space: nowrap; overflow: hidden;` (preserved)
       - Remove the `display: flex; align-items: center; gap: 5px;` (no more Server icon child; single text span)
       - Remove the `svg` rule at lines 566-569 (no Server svg to style)
       - Add a placeholder-variant selector `.pv-ai-title--placeholder` (or equivalent) with `opacity: 0.55; font-style: italic;` per v14-console-snippet line 94 + CONTEXT.md § Fallback: no ai-title yet.

    7. **`.pv-meta` block (lines 571-595)**: DELETE the entire block. If Task 1 kept `.pv-meta` as an empty stub JSX-side, keep a minimal placeholder rule `.pv-meta { display: none; }` and note the retirement in a comment. Otherwise delete cleanly.

    8. **`.pv-ready-dot` block (lines 610-637) + mobile bump at lines 1263-1270**: DELETE ALL rules. Add a retirement comment at the deletion site referencing Phase 47 Plan 05 and the ready-dot's replacement by the working-spinner on `.pv-avatar::before`.

    9. **`.pv-row:not(.working):not(.recycling) .pv-ready-dot { display: block }`** (line 635-637): delete (part of ready-dot retirement).

    10. **`.pv-row.working` empty selector (lines 643-646)**: the block was a Phase-34-era placeholder; can stay as a no-op OR expand to include the spinner-related hooks. Prefer leaving it minimal — the spinner is on `.pv-avatar::before` which is scoped by the ancestor `.pv-row.active-set:is(.working, .recycling)` selector, not by the row itself.

    11. **Grid-template on `.pv-row` (search for `grid-template-columns` in the file, likely near line ~350-450 where the pv-row block lives)**: change from `40px 1fr auto` (avatar / body / meta) to `40px 1fr` (avatar / body). Add a Phase 47 Plan 05 comment.

    12. **Mobile section (@media (max-width: 767.98px), lines 1188-1270)**: remove the `.pv-ready-dot` mobile rule at lines 1263-1270 (retired). Add a mobile-bump rule for the spinner ring `inset` value if needed (Ashley may want a slightly wider ring on mobile — leave as identical to desktop unless UAT reveals otherwise; document as "unchanged; adjust post-UAT if Ashley requests" in the SUMMARY).

    Do NOT touch any of the identity-hue rules, RDP block, hidden section, panel header, or search-input CSS — those are outside Phase 47 scope.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Fc 'pv-ready-dot' src/ui/features/pretty-conversations/pretty-conversations.css` <= 2 (retirement comment allowed; no live selectors)
    - `grep -Fc 'display: block' src/ui/features/pretty-conversations/pretty-conversations.css` shows no live `.pv-ready-dot` rule
    - `grep -Fc '.pv-avatar::before' src/ui/features/pretty-conversations/pretty-conversations.css` >= 1 (spinner selector present)
    - `grep -Fc 'conic-gradient' src/ui/features/pretty-conversations/pretty-conversations.css` >= 1 (p05 spinner CSS landed)
    - `grep -Fc '@keyframes pv-spinner-spin' src/ui/features/pretty-conversations/pretty-conversations.css` == 1 (new keyframe declared exactly once)
    - `grep -Ec 'mask-image:\s*linear-gradient\(to right' src/ui/features/pretty-conversations/pretty-conversations.css` >= 2 (fade-truncation on both title and subtitle)
    - `grep -Fc 'position: relative' src/ui/features/pretty-conversations/pretty-conversations.css` includes at least the `.pv-avatar` rule (grep-verify manually)
    - `grep -Fc 'overflow: visible' src/ui/features/pretty-conversations/pretty-conversations.css` includes at least the `.pv-avatar` rule
    - `.pv-avatar .pv-bounty-badge-wrap` absolute-positioning rules present: `grep -Ec 'pv-avatar.*pv-bounty-badge-wrap|pv-bounty-badge-wrap.*absolute' src/ui/features/pretty-conversations/pretty-conversations.css` >= 1
    - Grid-template retired for 3-column shape: `grep -Ec 'grid-template-columns:\s*40px\s+1fr\s+auto' src/ui/features/pretty-conversations/pretty-conversations.css` == 0 (3-col retired) AND `grep -Ec 'grid-template-columns:\s*40px\s+1fr' src/ui/features/pretty-conversations/pretty-conversations.css` >= 1 (2-col landed)
    - Pre-existing `.pv-bounty-badge*` rules UNCHANGED: `git diff HEAD -- src/ui/features/pretty-conversations/pretty-conversations.css | grep -Ec '^-.*\.pv-bounty-badge[^-]' | grep -v '^0$'` — verify no deletions of the badge class rules
    - `grep -c 'Phase 47' src/ui/features/pretty-conversations/pretty-conversations.css` >= 3 (v14 shape + spinner + retirement markers)
    - `npm run build` exit 0 (CSS compiles cleanly)
  </acceptance_criteria>
  <done>pretty-conversations.css restructured per v14 shape: `.pv-avatar` position:relative + overflow:visible; `.pv-avatar::before` p05 slow-dashed spinner ring under `.pv-row.active-set:is(.working, .recycling)`; badge-wrap absolute-positioned at avatar bottom corners; `.pv-label` + subtitle both have mask-image right-edge fade + text-overflow:clip; subtitle restyled per v14 (color #f0ece0, 13.5px, weight 500, text-shadow); `.pv-ready-dot` and `.pv-meta` blocks deleted; grid-template shrunk to 2-col; @keyframes pv-spinner-spin declared; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update + extend PrettyConversationRow.test.tsx for v14 shape (title-parens, subtitle aiTitle, spinner-inversion source assertions, badge-inside-avatar, .pv-meta-absent, ready-dot-absent)</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx (all lines — study the existing render helper pattern used at Test 1 line 202, Test 6 line 249, Test 9 line 376, etc. Reuse the render pattern verbatim.)
    - src/ui/features/pretty-conversations/PrettyConversationRow.tsx (post-Task-1 shape — the new markup this test file must exercise)
    - src/ui/features/pretty-conversations/pretty-conversations.css (post-Task-2 shape — DOM shape is stable, but note some pre-existing tests may query `.pv-ready-dot` or `.pv-meta` selectors that no longer exist; those tests need updating or deletion)
    - src/ui/features/pretty-conversations/PrettyBountyCountBadge.test.tsx (all lines — this test file MUST continue to pass unchanged since PrettyBountyCountBadge.tsx is unchanged; verify no assumption in the badge test breaks because the badge is now inside a different DOM ancestor)
  </read_first>
  <behavior>
    Tests to ADD (Phase 47 Plan 05 describe block):
    - Test P47-01 (title parens with host): render row with identity displayName "Tanya" + host "skynet-ec2"; assert `.pv-label` textContent === "Tanya (skynet-ec2)" AND `.pv-hostname-suffix` child span with textContent " (skynet-ec2)" is present.
    - Test P47-02 (title without host): render row with `row.host = null`; assert `.pv-label` textContent === identity displayName; assert `.pv-hostname-suffix` is absent.
    - Test P47-03 (subtitle with aiTitle): render row with `aiTitle="Fix bug X"`; assert `.pv-ai-title` textContent === "Fix bug X" AND is NOT `.pv-ai-title--placeholder`.
    - Test P47-04 (subtitle null → placeholder): render row with `aiTitle={null}`; assert `.pv-ai-title` textContent contains "…" (U+2026) AND has class `.pv-ai-title--placeholder`.
    - Test P47-05 (no Server icon in subtitle): render row with `aiTitle="X"` + host; assert the subtitle span contains ZERO `svg` children with `aria-hidden="true"` (Server icon deleted).
    - Test P47-06 (ready-dot fully absent): render row with any combination of isWorking / isRecycling / hasQueuePending / inActiveSet; assert `container.querySelector('[data-pv-conv-ready-dot]')` === null AND `container.querySelector('.pv-ready-dot')` === null. Iterate a few state combos for coverage.
    - Test P47-07 (.pv-meta element absent): render row with any state; assert `container.querySelector('.pv-meta')` === null.
    - Test P47-08 (Pin badge inside avatar, at bottom-left corner marker): render row with `pinnedCount=3` (via mocking useBountyCounts or seeding the source); assert `container.querySelector('.pv-avatar [data-testid="pv-bounty-badge-pinned"]')` is not null.
    - Test P47-09 (Monitor badge inside avatar, at bottom-right corner marker): render row with `needsDeskCount=2`; assert `container.querySelector('.pv-avatar [data-testid="pv-bounty-badge-needs-desk"]')` is not null.
    - Test P47-10 (row emits .working class when isWorking=true, still — CSS gates spinner via this class + .active-set): render row with `inActiveSet=true, isWorking=true`; assert the root `.pv-row` element has both `working` and `active-set` classes. (This is a defense-in-depth test locking the JS emission that the CSS-only spinner gate depends on — the pre-existing className composition at line 889-902 must NOT be regressed.)
    - Test P47-11 (idle row does NOT emit .working — CSS-only spinner correctly hides): render row with `inActiveSet=true, isWorking=false, isRecycling=false`; assert `.pv-row` does NOT have `working` and does NOT have `recycling` classes.

    Tests to UPDATE or DELETE (pre-existing):
    - Any test asserting on `.pv-ready-dot` presence → update to assert absence, or delete outright.
    - Any test asserting on `.pv-meta` presence → update to assert absence, or delete outright.
    - Any test asserting on the sublabel's Server icon → delete or update to assert absence.
    - Any test using `subtitleMode` prop with expectations about the sublabel's textContent being `identity.title` or `identity.displayName` or `hostname` → update to assert the new subtitle is aiTitle-driven, OR (if `subtitleMode` was retained per Task 1's Test 11 choice) update expectations to reflect the new shape (subtitle is always aiTitle regardless of subtitleMode).
  </behavior>
  <action>
    Add Phase 47 Plan 05 test coverage + update pre-existing tests broken by the v14 markup change.

    1. Append a new `describe("PrettyConversationRow: Phase 47 Plan 05 v14 shape", () => { ... })` block at the end of the test file. Include Tests P47-01 through P47-11 per the <behavior> block.

    2. For each pre-existing test that queries `.pv-ready-dot` (grep the file: `grep -n 'pv-ready-dot\|data-pv-conv-ready-dot' src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx`), either:
       - Delete the test if it was solely locking the ready-dot render (no longer valid).
       - Update the assertion to expect absence (`expect(...).toBeNull()`) if the test still exercises meaningful state (e.g., a test checking the ready-dot appears iff certain conditions hold — invert to "no ready-dot ever appears").

    3. For each pre-existing test that queries `.pv-meta` (grep: `grep -n 'pv-meta' src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx`), similarly delete or update to assert absence.

    4. For each pre-existing test that asserts on the sublabel rendering with `subtitleMode="identityTitle"` producing `identity.title` OR the hostname / Server-icon-with-hostname combination, update expectations to reflect the new shape: sublabel is now the aiTitle prop's content (or placeholder ellipsis when aiTitle is null). Use `render(<PrettyConversationRow ... aiTitle="expected string" />)` and assert on the aiTitle textContent.

    5. If a pre-existing test explicitly renders the row without wiring bountyCounts and asserts the badge is absent, update it to also verify the badge's DOM position (should be inside `.pv-avatar` even when counts are present — the badge component's own zero-count-null-return contract still holds, so a row with `pinnedCount=undefined, needsDeskCount=undefined` still has no badge rendered).

    6. Do NOT modify PrettyBountyCountBadge.test.tsx — that file tests the badge component in isolation and its DOM ancestor is irrelevant to the badge's own behavior.

    7. Verify PrettyConversationsPanel.test.tsx does not have tests that break under the new markup — grep for `pv-ready-dot` and `pv-meta` in that file. If matches exist, note them for a follow-up Task or a scope-extension of Task 3 within this plan (prefer updating in this plan rather than deferring, since Panel.test.tsx belongs to the same domain).

    Test infrastructure: reuse the existing `render()` helper + `container.querySelector` assertion pattern from the file's top-of-file examples. Do NOT introduce new test libraries or mock frameworks.
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-conversations/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'describe.*Phase 47 Plan 05' src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` >= 1
    - `grep -c 'aiTitle' src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` >= 8 (Tests P47-01/02/03/04 all exercise aiTitle prop values + at least a few pre-existing test updates)
    - `grep -c 'pv-hostname-suffix' src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` >= 1 (Test P47-01)
    - `grep -c 'pv-ai-title' src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` >= 3 (Tests P47-03, P47-04, P47-05)
    - `grep -c 'pv-ai-title--placeholder' src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` >= 1 (Test P47-04)
    - `grep -c 'pv-ready-dot' src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` shows only assertions expecting ABSENCE (grep-verify: every match's line contains `toBeNull` or `not.toBeInTheDocument` or similar negative assertion pattern)
    - `grep -c 'pv-meta' src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` — similarly all matches assert absence
    - `npx vitest run src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` exit 0 (all Phase 47 Plan 05 tests pass + all pre-existing tests updated pass)
    - `npx vitest run src/ui/features/pretty-conversations/PrettyBountyCountBadge.test.tsx` exit 0 (badge test file unchanged AND still passes — verbatim-preserved component behavior held)
    - `npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` exit 0 (panel tests may need small updates for markup changes; if they do, this Task's scope covers them)
    - `npx vitest run src/ui/features/pretty-conversations/` exit 0 (full folder green)
    - `npx vitest run` exit 0 (full-suite green — no regression elsewhere; note pre-existing timing flakes in unrelated PrettyView tests documented in 44-02/44-03 SUMMARYs are acceptable if isolated re-run passes)
    - `npm run build` exit 0
    - `grep -c 'as any\|@ts-expect-error' src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` == 0
  </acceptance_criteria>
  <done>PrettyConversationRow.test.tsx has a new Phase 47 Plan 05 describe block covering Tests P47-01 through P47-11 for the v14 shape; pre-existing tests referencing `.pv-ready-dot` or `.pv-meta` updated to assert absence or deleted; PrettyBountyCountBadge.test.tsx unchanged and still passes; PrettyConversationsPanel.test.tsx updated if needed; full pretty-conversations/ folder tests green; full-suite green (allowing for pre-existing unrelated timing flakes).</done>
</task>

</tasks>

<verification>
`npm run build && npm run build:backend && npx vitest run src/ui/features/pretty-conversations/` all exit 0. Full-suite `npx vitest run` remains green (pre-existing unrelated timing flakes in PrettyView tests per 44-02/44-03 SUMMARYs are acceptable).
</verification>

<success_criteria>
- Every convo-list row with an aiTitle displays it as the fade-truncated subtitle line.
- Every convo-list row's title line is `identityName (hostname)` when host is present, fade-truncated.
- Working state on the row is signaled by the slow dashed spinner ring on the avatar via CSS-only `.pv-row.active-set:is(.working, .recycling) .pv-avatar::before` — the inversion Ashley locked verbatim; ready-dot fully retired.
- PrettyBountyCountBadge V12 style preserved verbatim; badge wraps relocated to absolute avatar corners (Pin bottom-left, Monitor bottom-right).
- `.pv-meta` element retired; row grid template shrunk to `40px 1fr`.
- Fade-truncation via mask-image + text-overflow:clip on both title and subtitle lines.
- Test coverage locks all v14 invariants; PrettyBountyCountBadge.test.tsx passes unchanged.
</success_criteria>

<output>
Create `.planning/phases/47-convo-list-per-row-current-work-hint-from-ai-title-extends-f/47-05-SUMMARY.md` when done.
</output>
