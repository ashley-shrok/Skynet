---
phase: 47-convo-list-per-row-current-work-hint-from-ai-title-extends-f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/fleet-status/wire-protocol.ts
  - src/backend/fleet-status/wire-protocol.test.ts
  - src/ui/api/fleet-status-types.ts
  - src/ui/api/sessions-api.ts
  - src/ui/state/conversation-store.ts
  - src/ui/state/conversation-store.cache.test.ts
  - src/ui/state/conversation-store.test.ts
autonomous: true
requirements: []
tags: [wire-type, fleet-status, ai-title, cache-key-bump, sessions-list]

must_haves:
  truths:
    - "SessionStateSchema (backend zod) accepts an optional nullable aiTitle field alongside lastMessageAt"
    - "SessionState (ui) interface carries optional aiTitle field alongside lastMessageAt"
    - "RemoteTmuxSession wire type carries optional aiTitle field alongside lastMessageAt"
    - "FleetSession type carries optional aiTitle field; cache round-trip preserves aiTitle; cache key is bumped v2 → v3 so pre-Phase-47 v2 entries do not rehydrate and seed working-store with missing aiTitle"
  artifacts:
    - path: "src/backend/fleet-status/wire-protocol.ts"
      provides: "SessionStateSchema.aiTitle z.string().nullable().optional() addition"
      contains: "aiTitle: z.string().nullable().optional()"
    - path: "src/ui/api/fleet-status-types.ts"
      provides: "SessionState.aiTitle?: string | null"
      contains: "aiTitle?: string | null"
    - path: "src/ui/api/sessions-api.ts"
      provides: "RemoteTmuxSession.aiTitle?: string | null"
      contains: "aiTitle?: string | null"
    - path: "src/ui/state/conversation-store.ts"
      provides: "FleetSession.aiTitle?: string | null; FLEET_CACHE_KEY bumped v2 → v3; readFleetSessionsCache + writeFleetSessionsCache carry aiTitle; isFleetSession accepts undefined/null/string for aiTitle"
      contains: "skynet:convo-fleet-cache:v3"
  key_links:
    - from: "backend SessionStateSchema (zod runtime validator)"
      to: "ui SessionState (compile-time interface)"
      via: "hand-mirrored shape — both files must be edited in this plan"
      pattern: "aiTitle.*string.*null"
    - from: "FLEET_CACHE_KEY v3 in conversation-store.ts"
      to: "CACHE_KEY in conversation-store.cache.test.ts"
      via: "shared literal that MUST match verbatim across both files"
      pattern: "skynet:convo-fleet-cache:v3"
---

<objective>
Extend the wire-type surface for the phase-47 ai-title signal, mirroring the shape Phase 44 established for `lastMessageAt`. This is a pure additive-optional type extension across four files (backend zod schema, ui interface, sessions-list REST type, conversation-store FleetSession type) plus a load-bearing cache-key bump from v2 to v3.

Purpose: unblock every subsequent Phase 47 plan (backend scraper 47-02, working-store third axis 47-03, AppShell seed + row redesign 47-04/47-05). No consumer wiring in this plan — pure type extensions.

Output: wire types accept `aiTitle: string | null | undefined` on all four sites; FleetSession cache round-trip preserves aiTitle; cache key advances to v3 so any pre-Phase-47 v2 cache entry is discarded on cold-load (preventing a v2 rehydrate from seeding the working-store with a stale FleetSession that lacks aiTitle).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-convo-list-per-row-current-work-hint-from-ai-title-extends-f/47-CONTEXT.md
@.planning/phases/44-fix-convo-list-recency-signal-switch-dormant-live-paths-to-i/44-01-SUMMARY.md
@.planning/phases/44-fix-convo-list-recency-signal-switch-dormant-live-paths-to-i/44-03-SUMMARY.md
@.planning/phases/44-fix-convo-list-recency-signal-switch-dormant-live-paths-to-i/44-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend wire-type surface with aiTitle on all four sites (SessionStateSchema, SessionState, RemoteTmuxSession, FleetSession)</name>
  <files>src/backend/fleet-status/wire-protocol.ts, src/ui/api/fleet-status-types.ts, src/ui/api/sessions-api.ts, src/ui/state/conversation-store.ts</files>
  <read_first>
    - src/backend/fleet-status/wire-protocol.ts (see current SessionStateSchema at lines 102-113; mirror Phase 41 Plan 03's `lastMessageAt: z.number().nullable().optional()` addition — new field goes IMMEDIATELY BELOW lastMessageAt)
    - src/ui/api/fleet-status-types.ts (see SessionState interface at lines 88-107; mirror the lastMessageAt block comment style — new field's own block comment references Phase 47 CONTEXT.md and the harness format `{"type":"ai-title","aiTitle":"…","sessionId":"…"}` in JSONL)
    - src/ui/api/sessions-api.ts (all 39 lines; see RemoteTmuxSession at lines 4-12; add aiTitle after lastMessageAt with matching comment style)
    - src/ui/state/conversation-store.ts lines 145-158 (FleetSession type) and lines 1049-1141 (isFleetSession predicate + readFleetSessionsCache + writeFleetSessionsCache); mirror the Phase 44 Plan 04 lastMessageAt addition pattern exactly
    - .planning/phases/44-fix-convo-list-recency-signal-switch-dormant-live-paths-to-i/44-04-SUMMARY.md (§ What Landed — for the exact isFleetSession defensive-reject pattern to mirror)
  </read_first>
  <behavior>
    - Test 1 (backend zod): a SessionState frame carrying `aiTitle: "Fix bug X"` parses successfully via SessionStateSchema.parse — the added field is accepted and preserved on the parsed object.
    - Test 2 (backend zod): a SessionState frame carrying `aiTitle: null` parses successfully via SessionStateSchema.parse.
    - Test 3 (backend zod): a SessionState frame OMITTING aiTitle entirely parses successfully — the field is optional at wire-schema level (matches lastMessageAt's `.optional().nullable()` treatment).
    - Test 4 (backend zod): a SessionState frame carrying `aiTitle: 42` (wrong type) is rejected — z.string() enforces the type when the field is present.
    - FRAME_SCHEMA_VERSION remains at whatever value Phase 44 landed (deliberately NOT bumped — additive+optional extensions do not require a version bump per the wire-protocol.ts block-comment invariant); this is a source-assertion in the acceptance criteria below.
  </behavior>
  <action>
    Add `aiTitle` field to four artifacts, immediately below the existing `lastMessageAt` field on each, using matching optional-nullable treatment.

    1. `src/backend/fleet-status/wire-protocol.ts`: add `aiTitle: z.string().nullable().optional()` to SessionStateSchema after `lastMessageAt`. Add a block-comment ABOVE the new line (mirror the Phase 41 Plan 03 comment for lastMessageAt at lines 82-99) referencing Phase 47 CONTEXT.md § domain, citing the harness format `{"type":"ai-title","aiTitle":"…","sessionId":"…"}` as the source, and reiterating the FRAME_SCHEMA_VERSION-not-bumped invariant. Keep the ordering: fields sorted by concern (existing group: hostId/tmuxSession/sessionId/pid/status/waitingFor/backgroundTasks/updatedAt/lastMessageAt/aiTitle).

    2. `src/ui/api/fleet-status-types.ts`: add `aiTitle?: string | null` to the SessionState interface after `lastMessageAt?: number | null`. Add a matching block comment (mirror the lastMessageAt comment at lines 97-106) referencing Phase 47 CONTEXT.md and stating the frontend consumer treats `undefined` and `null` identically (both → working-store cache holds null → row subtitle renders the fallback ellipsis).

    3. `src/ui/api/sessions-api.ts`: add `aiTitle?: string | null` to RemoteTmuxSession after `lastMessageAt?: number | null` with a single-line inline comment: `// Phase 47 — inline current-work hint from /sessions/list route. Optional for compat with pre-Phase-47 backends; consumers treat undefined as null.`

    4. `src/ui/state/conversation-store.ts`:
       - Extend FleetSession type (currently at lines 145-158) with `aiTitle?: string | null` after lastMessageAt. Add a block comment mirroring Phase 44 Plan 04's lastMessageAt-on-FleetSession block: "Phase 47 — inline current-work hint carried from /sessions/list. Consumed by AppShell to feed seedSessionAiTitle (Plan 47-03 chokepoint); NOT stamped on ConversationRow directly (that path stays via working-store useSessionAiTitle hook). Optional so pre-Phase-47 backend responses (or a v2 cache rehydrate that predates this field) deserialize into a FleetSession object that simply omits the field — seed loop then calls with `?? null`."
       - Extend `isFleetSession` predicate (lines 1049-1072): after the lastMessageAt-accept block, add a mirroring block for aiTitle — accept undefined, null, or string; REJECT any other type defensively so a corrupt cache entry never seeds working-store with a non-string aiTitle (comment MUST cite Phase 47 rationale: last-wins reconciliation would poison the store with garbage otherwise).
       - Extend `readFleetSessionsCache` (lines 1083-1113): add `aiTitle: item.aiTitle ?? null` to the pushed valid entry alongside lastMessageAt, mirroring the coerce-undefined-to-null pattern the Phase 44 Plan 04 comment justifies.
       - Extend `writeFleetSessionsCache` (lines 1123-1141): add `aiTitle: s.aiTitle ?? null` to the `canonical` map projection alongside lastMessageAt.

    All four additions are pure additive optional-nullable extensions per the Phase 44 Plan 03 precedent (44-03-SUMMARY.md § What Landed § sessions-api.ts). Do not import consumers, do not touch scraper code, do not touch UI row components — this task's diff is limited to type surface + cache round-trip fields.
  </action>
  <verify>
    <automated>npx vitest run src/backend/fleet-status/wire-protocol.test.ts src/ui/state/conversation-store.cache.test.ts src/ui/state/conversation-store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'aiTitle' src/backend/fleet-status/wire-protocol.ts` >= 2 (schema line + block comment reference)
    - `grep -c 'aiTitle' src/ui/api/fleet-status-types.ts` >= 2 (interface line + block comment reference)
    - `grep -c 'aiTitle' src/ui/api/sessions-api.ts` == 1 (interface line only; single-line inline comment counts as prose in same line)
    - `grep -c 'aiTitle' src/ui/state/conversation-store.ts` >= 5 (FleetSession field + isFleetSession predicate + readFleetSessionsCache + writeFleetSessionsCache + at least one comment reference)
    - `grep -Fc 'z.string().nullable().optional()' src/backend/fleet-status/wire-protocol.ts` >= 1 (matches the aiTitle addition; may pre-exist for other fields — this is a lower bound)
    - `grep -n 'FRAME_SCHEMA_VERSION' src/backend/fleet-status/wire-protocol.ts` shows the schema-version constant value UNCHANGED from Phase 44 (compare against `git show HEAD:src/backend/fleet-status/wire-protocol.ts | grep FRAME_SCHEMA_VERSION`)
    - `grep -c 'aiTitle' src/backend/fleet-status/wire-protocol.test.ts` >= 3 (three of Tests 1/2/3/4 mention aiTitle in fixtures OR assertions)
    - `npx vitest run src/backend/fleet-status/wire-protocol.test.ts` exit 0 with the 4 new aiTitle-parse tests passing
    - `grep -c 'as any\|@ts-expect-error' $(git diff --name-only HEAD~1 HEAD 2>/dev/null || echo src/backend/fleet-status/wire-protocol.ts src/ui/api/fleet-status-types.ts src/ui/api/sessions-api.ts src/ui/state/conversation-store.ts)` == 0
    - `npm run build:backend` exit 0
    - `npm run build` exit 0 (frontend + typecheck green — this catches consumers that break under the new optional field, which should be zero since additive-optional)
  </acceptance_criteria>
  <done>Wire-type surface for aiTitle landed on all four sites (backend zod schema, ui interface, sessions-api RemoteTmuxSession, conversation-store FleetSession) with mirroring optional-nullable treatment; four new zod-parse tests pass in wire-protocol.test.ts; no consumers touched; both builds green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bump FLEET_CACHE_KEY v2 → v3 + carry aiTitle through cache round-trip tests</name>
  <files>src/ui/state/conversation-store.ts, src/ui/state/conversation-store.cache.test.ts, src/ui/state/conversation-store.test.ts</files>
  <read_first>
    - src/ui/state/conversation-store.ts lines 1029-1141 (the FLEET_CACHE_KEY declaration at line 1047 + isFleetSession + read/write cache functions — Task 1 already extended these for the aiTitle field; this task bumps the version literal)
    - src/ui/state/conversation-store.cache.test.ts (all 150 lines — find CACHE_KEY at line 29; find SAMPLE_A / SAMPLE_B fixtures; find the "write-only-canonical-fields" test)
    - src/ui/state/conversation-store.test.ts lines 1008-1015 (removeFleetSession Test R1 FLEET_CACHE_KEY = "skynet:convo-fleet-cache:v2" — MUST bump to v3), lines 2983-2991 (the Phase 44 Plan 04 Test E "writes to v2 cache key (v1 not written)" — MUST update to v3-vs-v2 semantics), lines 3055-3075 (the Phase 44 Plan 04 Test F "returns [] when only a v1 (pre-bump) cache entry exists" — MUST update to v2-pre-bump-vs-v3 semantics)
    - .planning/phases/44-fix-convo-list-recency-signal-switch-dormant-live-paths-to-i/44-04-SUMMARY.md § Rule 1 auto-fix bundled with Task 3 (documents the shape of the analogous v1→v2 bump; this task performs the analogous v2→v3 bump with matching test-update scope)
  </read_first>
  <behavior>
    - Test 1 (cache-key literal): the FLEET_CACHE_KEY constant in conversation-store.ts reads `"skynet:convo-fleet-cache:v3"`; both matching sites in the test files (CACHE_KEY in cache.test.ts, FLEET_CACHE_KEY in conversation-store.test.ts) read the same v3 literal.
    - Test 2 (round-trip): writing a FleetSession with `aiTitle: "Fix bug X"` and reading it back yields the same aiTitle string.
    - Test 3 (round-trip): writing a FleetSession with `aiTitle: null` and reading it back yields null.
    - Test 4 (round-trip): writing a FleetSession that omits aiTitle (undefined) yields null on read (mirrors the coerce-undefined-to-null Phase 44 Plan 04 Test B).
    - Test 5 (v2 → v3 invalidation): a v2 cache entry present in localStorage is NOT rehydrated by readFleetSessionsCache; only entries at the v3 key are read.
    - Test 6 (canonical fields): the "write-only-canonical-fields" test in cache.test.ts expects 7 keys `[aiTitle, created, hostId, hostName, lastMessageAt, role, sessionName]` (was 6 pre-Phase-47, matching Phase 44 Plan 04's expansion 5→6).
    - Test 7 (isFleetSession defensive reject): passing a would-be FleetSession object with `aiTitle: 42` (number, wrong type) causes isFleetSession to return false — cache entry rejected.
  </behavior>
  <action>
    Perform the v2 → v3 cache-key bump + extend Phase 44 Plan 04 tests to cover the new aiTitle round-trip.

    1. `src/ui/state/conversation-store.ts` line 1047: change `const FLEET_CACHE_KEY = "skynet:convo-fleet-cache:v2";` to `const FLEET_CACHE_KEY = "skynet:convo-fleet-cache:v3";`. Update the docblock immediately above (lines 1030-1046 — the Phase 44 Plan 04 comment justifying v1→v2) to APPEND a Phase 47 note explaining the v2→v3 bump uses the same rationale: a v2 rehydrate would seed FleetSession objects lacking `aiTitle` (undefined → coerced to null by readFleetSessionsCache → AppShell seed loop's `s.aiTitle ?? null` fires seedSessionAiTitle with null → last-wins no-op → cache stays empty for that session's aiTitle → row renders the fallback ellipsis instead of the last-known ai-title from the v2 cache. Because ai-title is last-wins not max-wins, we prefer a clean cold-start over a silently-empty rehydrate for cached rows on deploy).

    2. `src/ui/state/conversation-store.cache.test.ts`:
       - line 29: bump `const CACHE_KEY = "skynet:convo-fleet-cache:v2";` to `"skynet:convo-fleet-cache:v3"`.
       - SAMPLE_A + SAMPLE_B fixtures: add `aiTitle: null` to SAMPLE_A and `aiTitle: "Fix cache round-trip"` to SAMPLE_B (mirror the Phase 44 Plan 04 pattern of writing one null + one populated case to exercise both round-trip branches).
       - The "write-only-canonical-fields" test: update the expected key count from 6 to 7 and update the expected sorted-key list to include `aiTitle` in alphabetical position (BEFORE `created`).

    3. `src/ui/state/conversation-store.test.ts`:
       - line 1013 (removeFleetSession Test R1): bump `FLEET_CACHE_KEY = "skynet:convo-fleet-cache:v2"` to v3.
       - line 2983 area (Phase 44 Plan 04 Test E "writes to v2 cache key"): rename/retitle to reflect v3 semantics; the test now writes to v3 and asserts v2 key is not written. Update `FLEET_CACHE_KEY_V2` local const rename to `FLEET_CACHE_KEY_V3` and adjust `localStorage.removeItem("skynet:convo-fleet-cache:v1")` cleanup to also remove v2 in the setup so tests start from clean slate.
       - line 3060-3061 area: `expect(localStorage.getItem("skynet:convo-fleet-cache:v3")).not.toBeNull();` and `expect(localStorage.getItem("skynet:convo-fleet-cache:v2")).toBeNull();` (was v2/v1 — bump one step).
       - line 3071 area (Phase 44 Plan 04 Test F "returns [] when only a v1 (pre-bump) cache entry exists"): bump v1 to v2 throughout the test body (write v2 payload, assert readFleetSessionsCache returns [] because the reader now looks for v3).
       - Add ONE new test in the Phase 47-01 describe block: "isFleetSession rejects aiTitle of wrong type (number)" — construct an object with all valid canonical fields except `aiTitle: 42`, call isFleetSession(x), assert false.
       - Add ONE new test: "writeFleetSessionsCache + readFleetSessionsCache round-trip preserves aiTitle (both null and string cases)" — write a two-item fleet with aiTitle:null + aiTitle:"Fix bug X", read back, assert both survive verbatim.

    Do NOT touch AppShell.tsx (seed loop already dereferences `s.aiTitle ?? null` — that lives in Plan 47-04). Do NOT touch working-store (that lives in Plan 47-03). Do NOT touch pretty-conversations components.
  </action>
  <verify>
    <automated>npx vitest run src/ui/state/conversation-store.ts src/ui/state/conversation-store.cache.test.ts src/ui/state/conversation-store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Fc 'skynet:convo-fleet-cache:v3' src/ui/state/conversation-store.ts` == 1 (the FLEET_CACHE_KEY constant only)
    - `grep -Fc 'skynet:convo-fleet-cache:v3' src/ui/state/conversation-store.cache.test.ts` >= 1
    - `grep -Fc 'skynet:convo-fleet-cache:v2' src/ui/state/conversation-store.ts` == 0 (v2 fully retired from source)
    - `grep -Fc 'skynet:convo-fleet-cache:v2' src/ui/state/conversation-store.cache.test.ts` == 0 (test bumped to v3)
    - `grep -c 'skynet:convo-fleet-cache' src/ui/state/conversation-store.test.ts` matches the pattern where every reference to the FLEET_CACHE_KEY_V2 constant is renamed to FLEET_CACHE_KEY_V3 (spot-check: `grep -Fc 'FLEET_CACHE_KEY_V2' src/ui/state/conversation-store.test.ts` == 0; `grep -Fc 'FLEET_CACHE_KEY_V3' src/ui/state/conversation-store.test.ts` >= 1)
    - `grep -c 'aiTitle' src/ui/state/conversation-store.cache.test.ts` >= 3 (SAMPLE_A + SAMPLE_B + canonical-fields test expectation)
    - `grep -c 'aiTitle' src/ui/state/conversation-store.test.ts` >= 2 (the two new tests: isFleetSession defensive-reject + round-trip)
    - `npx vitest run src/ui/state/conversation-store.cache.test.ts` exit 0 with all cache tests passing (was 9/9 pre-plan; now 9/9 with SAMPLE fixtures + canonical-fields test updated)
    - `npx vitest run src/ui/state/conversation-store.test.ts` exit 0 (all Phase 44 Plan 04 tests updated to v3 semantics + 2 new Phase 47-01 tests)
    - `npm run build` exit 0
    - `grep -c 'as any\|@ts-expect-error' src/ui/state/conversation-store.ts src/ui/state/conversation-store.cache.test.ts src/ui/state/conversation-store.test.ts` == 0
  </acceptance_criteria>
  <done>FLEET_CACHE_KEY at v3 in source + both test files; cache round-trip preserves aiTitle for null and string cases; canonical-fields test asserts 7 keys; isFleetSession defensive-rejects non-string aiTitle; all cache + conversation-store tests pass; both builds green.</done>
</task>

</tasks>

<verification>
`npm run build && npm run build:backend && npx vitest run src/backend/fleet-status/wire-protocol.test.ts src/ui/state/conversation-store.ts src/ui/state/conversation-store.cache.test.ts src/ui/state/conversation-store.test.ts` all exit 0.
</verification>

<success_criteria>
- SessionStateSchema, SessionState, RemoteTmuxSession, FleetSession all carry `aiTitle` as optional/nullable.
- FLEET_CACHE_KEY is at v3 in source and both test files (v2 fully retired).
- Cache round-trip preserves aiTitle for null and string cases; canonical-fields test asserts 7 keys.
- isFleetSession defensively rejects aiTitle of non-string non-null non-undefined types.
- No consumers touched (backend scraper, working-store, AppShell, row components untouched).
- FRAME_SCHEMA_VERSION unchanged (additive-optional does not warrant a bump per wire-protocol block-comment invariant).
</success_criteria>

<output>
Create `.planning/phases/47-convo-list-per-row-current-work-hint-from-ai-title-extends-f/47-01-SUMMARY.md` when done.
</output>
