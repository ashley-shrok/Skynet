---
phase: 47-convo-list-per-row-current-work-hint-from-ai-title-extends-f
plan: 02
type: execute
wave: 2
depends_on: [47-01]
files_modified:
  - src/backend/database/routes/sessions.ts
  - src/backend/database/routes/sessions.test.ts
  - src/backend/fleet-status/ssh-poll-orchestrator.ts
  - src/backend/fleet-status/ssh-poll-orchestrator.test.ts
autonomous: true
requirements: []
tags: [backend, ai-title, jsonl-scraper, sessions-list, ssh-poll-orchestrator, wire-emit]

must_haves:
  truths:
    - "/sessions/list route emits aiTitle per session row — the LAST occurrence of `{\"type\":\"ai-title\",\"aiTitle\":\"…\",\"sessionId\":\"…\"}` in the discovered JSONL's tail — or null on any failure path (no discovery, empty tail, malformed line, timeout, throw)"
    - "ssh-poll-orchestrator publishes aiTitle on every SessionState frame (WS live path) alongside lastMessageAt, derived from the same jsonlPath cache"
    - "Per-session failure isolation preserved: aiTitle derivation timeouts or throws leave the row's aiTitle=null and never kill the sibling rows on the same host"
  artifacts:
    - path: "src/backend/database/routes/sessions.ts"
      provides: "TmuxSessionRow.aiTitle: string | null field + scanTailForLatestAiTitle helper + per-row aiTitle derivation inside the existing Promise.all rows.map dispatch"
      contains: "aiTitle: string | null"
    - path: "src/backend/fleet-status/ssh-poll-orchestrator.ts"
      provides: "PidCacheEntry.aiTitle: string | null; processPid derives aiTitle from a second bounded tail-read (or SAME tailRaw as lastMessageAt if possible) using the cached jsonlPath; SessionState state.aiTitle = derivedAiTitle in state composition"
      contains: "aiTitle: derivedAiTitle"
  key_links:
    - from: "sessions.ts scanTailForLatestAiTitle helper"
      to: "ssh-poll-orchestrator.ts equivalent scanTailForLatestAiTitle helper"
      via: "hand-mirrored constant + function (per Phase 44 Plan 01 precedent — no cross-import; CONTEXT.md § domain: 'no new shared module' inherits from Phase 43 scope decision)"
      pattern: "type.*ai-title"
    - from: "processPid tail-read of jsonlPath in orchestrator"
      to: "computeFingerprint state.aiTitle axis"
      via: "aiTitle enters the fingerprint so an ai-title change is a publish-trigger even when status + backgroundTasks + lastMessageAt are unchanged"
      pattern: "state.aiTitle"
---

<objective>
Land the backend scraper for the ai-title signal on BOTH backend read paths — the dormant `/sessions/list` REST route and the live ssh-poll-orchestrator WS publish path — mirroring the architecture Phase 44 established for `lastMessageAt`. The scraper is a bounded `tail -c 262144` read + grep-for-`"type":"ai-title"` + `tail -1` + `jq -r '.aiTitle // empty'` derivation returning the string or null.

Purpose: unblock Plan 47-03 (working-store third axis) and Plan 47-04 (AppShell seed + row consumer) by providing the wire-side ai-title values on both feeds. Preserves the single-source-of-truth architecture Phase 44 established — the JSONL is authoritative and both feeds derive from the same discovery path.

Output: `/sessions/list` responses include `aiTitle: string | null` on every row; fleet-status WS `SessionState` frames include `aiTitle: string | null` on every publish; failure-isolation semantics identical to Phase 44's lastMessageAt (per-row Promise.race + try/catch on sessions.ts; fail-open on orchestrator tail-scan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-convo-list-per-row-current-work-hint-from-ai-title-extends-f/47-CONTEXT.md
@.planning/phases/44-fix-convo-list-recency-signal-switch-dormant-live-paths-to-i/44-01-SUMMARY.md
@.planning/phases/44-fix-convo-list-recency-signal-switch-dormant-live-paths-to-i/44-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend /sessions/list route with per-session aiTitle derivation (mirror Phase 44 Plan 01 lastMessageAt shape)</name>
  <files>src/backend/database/routes/sessions.ts, src/backend/database/routes/sessions.test.ts</files>
  <read_first>
    - src/backend/database/routes/sessions.ts (all 275 lines — study the existing Phase 44 Plan 01 lastMessageAtBlock at lines 184-238; the aiTitle derivation must sit as a THIRD parallel block alongside roleResolveBlock + lastMessageAtBlock inside the Promise.all([...]) at line 240)
    - src/backend/database/routes/sessions.test.ts (find the Phase 44 Plan 01 describe block "GET /sessions/list — lastMessageAt derivation" — mirror its 6-test shape for the new aiTitle describe block; the vi.mock of discoverIdentitySessionFile is already in place)
    - .planning/phases/44-fix-convo-list-recency-signal-switch-dormant-live-paths-to-i/44-01-SUMMARY.md (§ What Landed — for the exact fixture patterns to mirror)
    - .planning/phases/47-convo-list-per-row-current-work-hint-from-ai-title-extends-f/47-CONTEXT.md § Backend scraper mechanics (the exact `tail -c 262144 | grep | tail -1 | jq -r` shell pipeline)
  </read_first>
  <behavior>
    - Test 1 (happy path): a session's discovered JSONL contains exactly one `{"type":"ai-title","aiTitle":"Fix bug X","sessionId":"..."}` line → row's aiTitle field is `"Fix bug X"`.
    - Test 2 (last-wins on multiple ai-title lines): JSONL contains three ai-title lines with different aiTitle strings emitted at different timestamps → row's aiTitle is the string from the LAST line (chronological last, i.e. the last one in file order — `tail -1` after grep).
    - Test 3 (discovery-null): discoverIdentitySessionFile returns null → row's aiTitle is null; sibling row with successful discovery keeps its string aiTitle.
    - Test 4 (no ai-title lines in tail): discovery succeeds, tail contains only message + tool_use lines (no ai-title) → row's aiTitle is null.
    - Test 5 (malformed ai-title JSON): JSONL contains a line matching `"type":"ai-title"` but jq extraction returns empty (malformed body) → row's aiTitle is null, logs debug, no throw.
    - Test 6 (timeout): discovery / tail exec hang → Promise.race trips at PER_HOST_TIMEOUT_MS → row's aiTitle is null, sibling row unaffected, response time bounded.
    - Test 7 (contract lock): every row in the response has `"aiTitle" in row === true` (server always emits, matching the "required-on-server-but-null-when-unknown" invariant Phase 44 Plan 01 locked for lastMessageAt).
  </behavior>
  <action>
    Extend `sessions.ts` and its test file with the aiTitle third-axis derivation.

    1. `src/backend/database/routes/sessions.ts`:
       - Extend the TmuxSessionRow interface (currently lines 64-71) with `aiTitle: string | null;` after `lastMessageAt`. Required-on-server, null-when-unknown (matches Phase 44 Plan 01's invariant for lastMessageAt).
       - Add a new module-scope helper `scanTailForLatestAiTitle(tailContents: string): string | null` after `scanTailForNewestMessageAt` (line 62). Implementation:
         - Split tailContents on `\n`, iterate lines, filter to those where `line.includes('"type":"ai-title"')` (cheap substring pre-filter before JSON.parse).
         - Track the LAST matching line's parsed aiTitle. For each match: try/catch `JSON.parse(line)`; if parse succeeds and result has `typeof result.aiTitle === "string"`, update the sticky `latest` variable to `result.aiTitle`.
         - Return the final `latest` (starts null; updated on every valid parse; falsy JSON, missing field, wrong type = skip).
         - Add a docblock citing 47-CONTEXT.md § Backend scraper mechanics and Phase 47 harness format `{"type":"ai-title","aiTitle":"…","sessionId":"…"}`.
       - Extend the row-init object (line 138-148) with `aiTitle: null as string | null` alongside `lastMessageAt: null as number | null`.
       - Inside the existing `rows.map(async (row) => {...})` at line 158, refactor: currently the lastMessageAtBlock reads the JSONL tail with `tail -n 200`. The aiTitle derivation needs a LARGER tail window (`tail -c 262144` per CONTEXT.md § Backend scraper mechanics — 256KB) so we can find an ai-title line that may be older than the last 200 message-bearing lines. Two options:
         * OPTION A (preferred if tail size doesn't harm perf materially): consolidate the two tail reads into one `tail -c 262144` invocation, run scanTailForNewestMessageAt AND scanTailForLatestAiTitle over the same buffer. Same discovery lookup, same tail, both signals emerge. This mirrors CONTEXT.md § Backend scraper mechanics: "If Phase 44's scan already reads/discovers the JSONL, the ai-title tail-read can share the same discovery lookup result to avoid a duplicate discoverIdentitySessionFile call per row."
         * OPTION B: keep lastMessageAt on `tail -n 200` and add a SECOND parallel tail-read `tail -c 262144 | grep '"type":"ai-title"' | tail -1 | jq -r '.aiTitle // empty'` shell pipeline. Two exec calls per row but semantically clean.
         Pick OPTION A unless the 256KB read materially degrades roleResolve wall-clock: a helper `readSessionRecencySignals(conn, tmuxSession) → { lastMessageAt: number|null, aiTitle: string|null }` that does discovery once + one `tail -c 262144` + both scans. Refactor lastMessageAtBlock to become recencySignalsBlock. Document the choice in a code comment citing 47-CONTEXT.md § Backend scraper mechanics.
       - The block's try/catch on failure sets BOTH `row.aiTitle = null` AND `row.lastMessageAt = null`, logs `operation: "sessions_list_recency_signals_skip"` (rename from `sessions_list_last_message_at_skip` if consolidating — retain the old operation name as fallback if keeping two blocks).
       - `scanTailForLatestAiTitle` uses `JSON.parse` NOT `jq` — the shell-pipe `jq -r '.aiTitle // empty'` from CONTEXT.md is one implementation option, but doing the parse in-process is cheaper (one shell exec instead of jq subprocess) and matches the `parseSessionLine` in-process pattern the Phase 44 Plan 01 scanner uses. Only the shell tail pipeline stays.

    2. `src/backend/database/routes/sessions.test.ts`:
       - Add new `describe("GET /sessions/list — aiTitle derivation (Phase 47 Plan 02)", () => {...})` block with 7 tests corresponding to the <behavior> block above.
       - Reuse the existing `mockedDiscover = vi.mocked(discoverIdentitySessionFile)` + `jsonlMessageLine` fixture pattern from Phase 44 Plan 01.
       - Add a fixture helper `jsonlAiTitleLine(sessionId: string, aiTitle: string): string` that emits `JSON.stringify({ type: "ai-title", aiTitle, sessionId }) + "\n"`.
       - Extend the 6 pre-existing Phase 44 Plan 01 lastMessageAt tests each with `expect(row.aiTitle).toBeNull()` (or the correct expected value) — every response row must carry the aiTitle contract.
       - Extend the 4 pre-existing Phase 43 role-resolution tests with the same aiTitle:null presence assertion.

    Do NOT touch ssh-poll-orchestrator (that lives in Task 2). Do NOT touch wire-protocol.ts or fleet-status-types.ts (those landed in Plan 47-01).
  </action>
  <verify>
    <automated>npx vitest run src/backend/database/routes/sessions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'aiTitle' src/backend/database/routes/sessions.ts` >= 5 (interface + helper declaration + row-init + block-body write + at least one comment)
    - `grep -n 'function scanTailForLatestAiTitle' src/backend/database/routes/sessions.ts` == 1 (helper declaration present)
    - `grep -n 'TmuxSessionRow' src/backend/database/routes/sessions.ts` shows aiTitle in the interface body: `grep -A 8 'interface TmuxSessionRow' src/backend/database/routes/sessions.ts | grep -c 'aiTitle: string | null'` >= 1
    - `grep -c '"type":"ai-title"' src/backend/database/routes/sessions.ts` >= 1 (either the shell grep pattern OR the substring pre-filter comment/code)
    - `grep -c 'aiTitle' src/backend/database/routes/sessions.test.ts` >= 14 (7 new tests each asserting at least twice, plus fixture builder and updates to 10 pre-existing tests)
    - `grep -c 'describe.*Phase 47 Plan 02' src/backend/database/routes/sessions.test.ts` >= 1
    - `grep -c 'jsonlAiTitleLine' src/backend/database/routes/sessions.test.ts` >= 4 (fixture helper used in multiple tests)
    - `npx vitest run src/backend/database/routes/sessions.test.ts` exit 0 (was 10/10 pre-plan → now 10 pre-existing + 7 new = 17/17)
    - `npm run build:backend` exit 0
    - `npm run build` exit 0
    - `grep -c 'as any\|@ts-expect-error' src/backend/database/routes/sessions.ts src/backend/database/routes/sessions.test.ts` == 0
    - Verify per-row failure isolation preserved: `grep -n 'Promise.race' src/backend/database/routes/sessions.ts | wc -l` >= 2 (per-block races preserved; if consolidated into one recencySignalsBlock, this may be 2 — one for the outer host + one for the recency block)
  </acceptance_criteria>
  <done>/sessions/list route emits aiTitle per session row derived from the LAST `{"type":"ai-title",...}` line in the discovered JSONL's tail (or null on any failure); 7 new tests + updates to 10 pre-existing tests all pass; per-row failure isolation preserved; both builds green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish aiTitle on every SessionState frame from ssh-poll-orchestrator (mirror lastMessageAt derivation shape)</name>
  <files>src/backend/fleet-status/ssh-poll-orchestrator.ts, src/backend/fleet-status/ssh-poll-orchestrator.test.ts</files>
  <read_first>
    - src/backend/fleet-status/ssh-poll-orchestrator.ts (all 869 lines — study PidCacheEntry lines 109-150 and the tail-read + scanTailForNewestMessageAt derivation inside processPid lines 500-550; mirror the same shape for aiTitle)
    - src/backend/fleet-status/ssh-poll-orchestrator.test.ts (find the Phase 44 Plan 02 describe block "Phase 44 Plan 02" with tests G/H/I/J/K — mirror the fixture-reshape pattern with a helper `jsonlAiTitleLine` matching the sessions.test.ts helper)
    - .planning/phases/44-fix-convo-list-recency-signal-switch-dormant-live-paths-to-i/44-02-SUMMARY.md § patterns-established (SshChannel-adapter pattern + tightened stale-tick condition — the aiTitle derivation uses the SAME cached jsonlPath, so it inherits stale-tail-rediscovery for free; it does NOT get its own stale-tick counter — the counter is a rotation-defense for lastMessageAt, and aiTitle is a passenger on the same tail-read)
  </read_first>
  <behavior>
    - Test 1 (happy path): a PID with a discovered JSONL containing one ai-title line → published SessionState.aiTitle equals that string.
    - Test 2 (last-wins): JSONL contains multiple ai-title lines → published aiTitle equals the LAST one in file order.
    - Test 3 (no ai-title lines in tail): JSONL exists, has messages but no ai-title lines → published aiTitle is null.
    - Test 4 (discovery null): tmuxSession is null OR discovery returns null → published aiTitle is null (matches lastMessageAt behavior).
    - Test 5 (aiTitle-change publish trigger): computeFingerprint includes aiTitle as a distinct axis → a poll cycle where ONLY aiTitle changes (status/backgroundTasks/updatedAt/lastMessageAt all identical) STILL fires publishSessionState.
    - Test 6 (cache preserves aiTitle across ticks): on tick 2 with an unchanged tail, PidCacheEntry.aiTitle survives; on tick 3 with a new ai-title line, the cache advances.
  </behavior>
  <action>
    Extend ssh-poll-orchestrator to derive and publish aiTitle alongside lastMessageAt.

    1. `src/backend/fleet-status/ssh-poll-orchestrator.ts`:
       - Extend PidCacheEntry (lines 109-150) with `aiTitle: string | null` after lastMessageAt. Add a docblock comment: "Phase 47 — cached derived latest ai-title from the same JSONL tail-read that feeds lastMessageAt. Passenger on the same jsonlPath — no independent stale-tick counter (aiTitle inherits invalidation via the shared cached jsonlPath). Last-wins semantics: fresher tail-scan overwrites whether or not the value changed. Publishes iff computeFingerprint sees a change."
       - Add module-scope constant `AI_TITLE_LINE_PREFIX` and helper `function scanTailForLatestAiTitle(tailContents: string): string | null` after scanTailForNewestMessageAt (line 247). Same in-process JSON.parse pattern as Task 1's helper — DO NOT cross-import; per CONTEXT.md § Backend scraper mechanics + Phase 44 Plan 01 precedent (44-01 SUMMARY.md decisions § MESSAGE_BEARING_KINDS re-declared locally), the two backend read paths keep local copies.
       - In processPid at the tail-read block (currently lines 505-550): AFTER the existing `scanned = scanTailForNewestMessageAt(tailRaw)` at line 511, add `let scannedAiTitle: string | null = null; if (tailRaw !== null && tailRaw.trim() !== "") { scannedAiTitle = scanTailForLatestAiTitle(tailRaw); }` — reuse the SAME tailRaw so we do NOT double-read.
       - **IMPORTANT — tail-width consideration:** the existing derivation uses `tail -n 200` (line count). Ai-title lines may be older than the last 200 lines. Bump the tail command to `tail -c 262144` (256KB — matches CONTEXT.md § Backend scraper mechanics; this is the SAME tail-read shared with sessions.ts Task 1). The scanTailForNewestMessageAt logic is unaffected — it iterates lines regardless of the source's byte-vs-line bound. Verify via manual eyeball that no test in ssh-poll-orchestrator.test.ts hardcodes the `tail -n 200` shape (grep the test file).
       - Extend the last-wins reconciliation for aiTitle: since ai-title is last-wins (not max-wins like lastMessageAt), the derivation is straightforward — `derivedAiTitle = scannedAiTitle !== null ? scannedAiTitle : (cached?.aiTitle ?? null)`. NOTE: this preserves the cache when scan returns null (transient SSH hiccup, or a tick where the tail is empty), matching lastMessageAt's "fail-open" semantics. If a session's ai-title truly disappears (e.g. new session with no ai-title lines yet), the cache eventually starts at null and stays null.
       - Extend SessionState composition (line 593) with `aiTitle: derivedAiTitle,` after `lastMessageAt: derivedLastMessageAt,`.
       - Extend computeFingerprint (line 321) to include aiTitle: the return string becomes `${state.status}|${state.waitingFor ?? ""}|${bgKey}|${state.updatedAt}|${state.lastMessageAt ?? ""}|${state.aiTitle ?? ""}`. Add a docblock line: "Phase 47: aiTitle is a distinct axis of the fingerprint — an ai-title change (topic drift) is a state-change publish trigger even when status + backgroundTasks + lastMessageAt are unchanged."
       - Extend both `livenessMap.set(...)` sites (lines 621-641) with `aiTitle: derivedAiTitle,` after the lastMessageAt fields.

    2. `src/backend/fleet-status/ssh-poll-orchestrator.test.ts`:
       - Add fixture helper `jsonlAiTitleLine(sessionId: string, aiTitle: string): string` — same shape as Task 1's helper.
       - Add new `describe("Phase 47 Plan 02 — aiTitle derivation and publish", () => {...})` block with 6 tests corresponding to the <behavior> block above.
       - Update the pre-existing Phase 44 Plan 02 test G's assertion to also check `state.aiTitle === null` (no ai-title lines in the fixture).
       - Update pre-existing computeFingerprint test (if one exists — grep for `computeFingerprint` in the test file) to include aiTitle in the fingerprint format assertion.
       - Update any test that hardcodes the tail command string `tail -n 200` — change to whatever the new shape is (`tail -c 262144` per the source edit above). Grep-find: `grep -Fn 'tail -n 200' src/backend/fleet-status/ssh-poll-orchestrator.test.ts`.

    Do NOT touch discover-identity-session-file.ts. Do NOT touch working-store — that lives in Plan 47-03. Do NOT touch AppShell or row components.
  </action>
  <verify>
    <automated>npx vitest run src/backend/fleet-status/ssh-poll-orchestrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'aiTitle' src/backend/fleet-status/ssh-poll-orchestrator.ts` >= 8 (PidCacheEntry field + helper declaration + processPid derivation + SessionState field + fingerprint + 2 livenessMap.set sites + comments)
    - `grep -n 'function scanTailForLatestAiTitle' src/backend/fleet-status/ssh-poll-orchestrator.ts` == 1 (single declaration)
    - `grep -Fc 'state.aiTitle' src/backend/fleet-status/ssh-poll-orchestrator.ts` >= 1 (fingerprint axis)
    - `grep -Fc 'aiTitle: derivedAiTitle' src/backend/fleet-status/ssh-poll-orchestrator.ts` >= 3 (state composition + 2 livenessMap.set sites)
    - `grep -Fc 'tail -c 262144' src/backend/fleet-status/ssh-poll-orchestrator.ts` >= 1 (tail-width bumped)
    - `grep -Fc 'tail -n 200' src/backend/fleet-status/ssh-poll-orchestrator.ts` == 0 (old tail-width fully retired; if kept for a different reason, this criterion must be adjusted — verify at implementation time)
    - `grep -c 'aiTitle' src/backend/fleet-status/ssh-poll-orchestrator.test.ts` >= 10
    - `grep -c 'describe.*Phase 47 Plan 02' src/backend/fleet-status/ssh-poll-orchestrator.test.ts` >= 1
    - `grep -c 'jsonlAiTitleLine' src/backend/fleet-status/ssh-poll-orchestrator.test.ts` >= 4
    - `npx vitest run src/backend/fleet-status/ssh-poll-orchestrator.test.ts` exit 0 (was 26 pre-plan → now 26 pre-existing possibly updated + 6 new = 32/32 with all pre-existing tests still green)
    - `npx vitest run src/backend/fleet-status/` exit 0 (10 files ~130 tests + new coverage)
    - `npm run build:backend` exit 0
    - `npm run build` exit 0
    - `grep -c 'as any\|@ts-expect-error' src/backend/fleet-status/ssh-poll-orchestrator.ts src/backend/fleet-status/ssh-poll-orchestrator.test.ts` == 0
  </acceptance_criteria>
  <done>ssh-poll-orchestrator publishes SessionState.aiTitle on every frame derived from the shared jsonlPath tail-read; fingerprint includes aiTitle as a distinct axis so ai-title-only changes still trigger publish; livenessMap caches aiTitle across ticks; 6 new tests + updated pre-existing tests all pass; both builds green.</done>
</task>

</tasks>

<verification>
`npm run build && npm run build:backend && npx vitest run src/backend/database/routes/sessions.test.ts src/backend/fleet-status/ssh-poll-orchestrator.test.ts` all exit 0. Full-suite `npx vitest run` remains green (no regression in other files).
</verification>

<success_criteria>
- Both backend read paths (dormant REST + live WS) derive aiTitle from the discovered JSONL tail.
- Failure isolation preserved on both paths (Phase 44's shape verbatim).
- Fingerprint includes aiTitle so a topic-drift-only change still triggers publish.
- Zero cross-import between the two scanner implementations (per CONTEXT.md and Phase 44 Plan 01 precedent).
</success_criteria>

<output>
Create `.planning/phases/47-convo-list-per-row-current-work-hint-from-ai-title-extends-f/47-02-SUMMARY.md` when done.
</output>
