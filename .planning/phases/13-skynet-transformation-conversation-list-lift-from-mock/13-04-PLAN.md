---
phase: 13-skynet-transformation-conversation-list-lift-from-mock
plan: 04
type: execute
wave: 3
depends_on:
  - 13-01
  - 13-02
  - 13-03
files_modified:
  - .planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-04-UAT-DIAG-LOG.md
autonomous: false
requirements:
  - SHAPE-05
tags: [ui, uat, diagnostics, ready-dot, mobile-scroll, safe-area]

must_haves:
  truths:
    - "A UAT-diagnostic log at 13-04-UAT-DIAG-LOG.md exists capturing Ashley's post-lift observations on: (a) ready-for-attention dot visibility (SHAPE-05), (b) mobile iPhone scroll-freeze status, (c) 100dvh/100vh safe-area padding escape status"
    - "If SHAPE-05 dot visibility PASSES the lift-alone test (Ashley clicks 3+ conversations and each row shows a dot when its agent is idle), the log records PASS and this plan closes as PASS without further source changes"
    - "If SHAPE-05 dot visibility FAILS the lift-alone test, the log records the specific failure mode observed AND enumerates the 4 diagnostic candidates preserved from the merged conversation-list-idle-vs-wip-state bounty (Terminal.tsx isIdle null-start, sessionWorkingKey mismatch, activeSet sessionStorage populate, PrettyConversationRowLive Rules-of-Hooks) with the next investigation step for each — this closes as PARTIAL with a route-back note"
    - "Mobile scroll-freeze observation: if the CSS restructuring from Waves 1+2 obviates the freeze, the log records that; if the freeze still repros, the log captures the mobile devtools signal (touch event handler? scroll-container overflow? pointer-events conflict?) and enumerates the next diag round"
    - "Safe-area padding observation: if the `pb-[env(safe-area-inset-bottom)]` workaround on `PrettyConversationsPanel.tsx:233` can be reverted (100dvh/100vh chain no longer escapes AppShell), the log records that; if the workaround must stay, the log documents which ancestor is the safe-area escape root cause"
    - "This plan is a checkpoint plan — the executor performs OBSERVATIONS AND ANALYSIS via automated tools where possible, then hands off to a human-verify checkpoint where Ashley UATs on both mobile (iPhone PWA) and desktop viewports and confirms the observed states. No source changes to `src/` in this plan — investigations that reveal root causes route back to a follow-up plan; this plan documents the state, not the fix"
    - "No edits to source files under `src/` in this plan — this is diagnostic + UAT authoring only. If diagnostic investigation surfaces a source-fix need, that fix is a follow-up plan (would become 13-04-follow-up or 13-05 depending on scope) — this plan documents the ROUTE-BACK, not the source fix itself"
    - "No edits under `src/ui/features/pretty-view/`, `src/ui/components/`, `src/ui/ssh/`, `src/ui/features/terminal/` — SHAPE-06 scope boundary; even diagnostic READS of those files are permissible (understanding context) but no writes"
  artifacts:
    - path: ".planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-04-UAT-DIAG-LOG.md"
      provides: "Post-lift UAT observations + diagnostic-candidate investigation notes + route-back recommendations for any surviving failures"
      contains: "SHAPE-05"
  key_links:
    - from: "13-04-UAT-DIAG-LOG.md"
      to: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx (Wave 1)"
      via: "if dot visibility fails, verify the Wave-1 rendering of `<span aria-label='ready' className='pv-ready-dot' />` under `.pv-meta` when `inActiveSet && isWorking === false`"
      pattern: "pv-ready-dot"
    - from: "13-04-UAT-DIAG-LOG.md"
      to: "src/ui/state/session-working-store.ts + src/ui/features/terminal/Terminal.tsx"
      via: "diagnostic candidate B (sessionWorkingKey mismatch) — verify the row's targetTmuxSession matches Terminal.tsx's published tmuxSessionName"
      pattern: "sessionWorkingKey"
    - from: "13-04-UAT-DIAG-LOG.md"
      to: "src/ui/state/conversation-store.ts activeSet sessionStorage"
      via: "diagnostic candidate C (activeSet populate on fresh session) — verify sessionStorage rehydration path"
      pattern: "activeSet|sessionStorage"
---

<objective>
Post-lift UAT + diagnostic-candidate investigation for the ready-for-attention dot visibility (SHAPE-05), mobile iPhone scroll-freeze, and 100dvh/100vh safe-area padding escape. All three of these issues have been failure-mode observations across prior phases:
  - **Dot visibility (SHAPE-05):** patch #137 wired the store + row rendering, but Ashley's UAT reported the dot not visible on desktop after clicking into 3+ conversations. Wave 1's rewrite of the row (13-01) may fix this indirectly by cleaning up the render tree; if not, this plan investigates the 4 diagnostic candidates preserved from the merged `conversation-list-idle-vs-wip-state` bounty.
  - **Mobile scroll-freeze:** patch series has documented mobile iPhone PWA scroll-freeze in the conversation list. The Wave 1 CSS restructuring (retiring the JS-computed inline styles + Tailwind layout scaffolding on the row) may resolve this; if not, this plan captures the mobile devtools signal and enumerates the next diag round.
  - **Safe-area padding escape:** the `pb-[env(safe-area-inset-bottom)]` workaround on `PrettyConversationsPanel.tsx:233` (patch #126) is a per-scroller band-aid over a root-cause 100dvh/100vh chain escape somewhere in AppShell's height chain. If Wave 2's AppShell chevron rebase happened to affect the height chain, this plan verifies whether the workaround can be reverted; if not, documents where the chain escapes.

Purpose: Implements SHAPE-05 from REQUIREMENTS.md. This is a CHECKPOINT plan — the executor performs automated diagnostic READS (grep the store, cat the current renders), then hands off to a HUMAN-VERIFY checkpoint where Ashley UATs on both mobile and desktop and confirms the observed states. Any surviving failures route back to a source-fix follow-up plan (this plan documents the route-back, not the source fix).

Output: A single markdown file `13-04-UAT-DIAG-LOG.md` capturing observations + investigation notes + route-back recommendations. No source changes in `src/`. Delivered as 1 atomic commit (docs) after the human-verify checkpoint clears.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-CONTEXT.md
@.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-01-SUMMARY.md
@.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-02-SUMMARY.md
@.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-03-SUMMARY.md
@src/ui/features/pretty-conversations/PrettyConversationRow.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
@src/ui/state/session-working-store.ts
@src/ui/state/conversation-store.ts
@src/ui/features/terminal/Terminal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated diagnostic pass — read post-lift source state; produce PRELIMINARY-DIAG.md draft</name>
  <files>.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-04-UAT-DIAG-LOG.md</files>
  <read_first>
    - /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-CONTEXT.md § "Diagnostic candidates (dot visibility, if lift alone doesn't fix)" — the 4 candidates and their locations
    - /home/ubuntu/skynet/src/ui/features/pretty-conversations/PrettyConversationRow.tsx (Wave 1 output — verify the ready-dot render block emits `<span aria-label='ready' className='pv-ready-dot' />` inside `.pv-meta` under the conditional `{inActiveSet && isWorking === false && ...}`)
    - /home/ubuntu/skynet/src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (Wave 2 output — verify `PrettyConversationRowLive` still calls `useSessionWorking(sessionKey)` at a stable hook-call site AND `sessionWorkingKey(row)` still returns `${row.host?.id}:${row.targetTmuxSession ?? ''}` for non-null hosts; verify `activeSet.has(row.id)` reads from the hoisted `activeSet` at the panel top)
    - /home/ubuntu/skynet/src/ui/features/pretty-conversations/pretty-conversations.css (verify `.pv-row.active-set:not(.working) .pv-ready-dot { display: block }` present; verify `.pv-ready-dot` base rule has `display: none` initial state)
    - /home/ubuntu/skynet/src/ui/state/session-working-store.ts (verify the publish/subscribe API + `useSessionWorking` hook — how does Terminal.tsx publish?)
    - /home/ubuntu/skynet/src/ui/state/conversation-store.ts (verify `selectConversation` still adds to activeSet as a side effect — the sessionStorage-backed populate on fresh page load)
    - /home/ubuntu/skynet/src/ui/features/terminal/Terminal.tsx (READ-ONLY diagnostic; verify Terminal.tsx publishes isIdle to session-working-store — grep for `session-working-store` import + calls to publish/set)
  </read_first>
  <action>
Perform an AUTOMATED diagnostic pass — read the post-lift source state and produce a PRELIMINARY-DIAG.md draft that Ashley then reads before UATing.

Write to `.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-04-UAT-DIAG-LOG.md` with the following sections:

## Section 1: Post-lift source state — dot visibility (SHAPE-05)

For each of the 4 diagnostic candidates, run a specific grep + summary:

**Candidate A: Terminal.tsx isIdle null-start (ticker not fired post-recreate?)**
  - `grep -n "isIdle\|session-working-store\|setSessionWorking\|publishSessionWorking" src/ui/features/terminal/Terminal.tsx | head -20`
  - Summarize: how does Terminal.tsx compute isIdle? What triggers the initial publish? Does the WebSocket ticker have to fire before isIdle transitions from null to true/false? Is there a startup path that publishes isIdle=true (=idle) immediately when the pane connects and Claude is at a prompt?
  - Note: if Terminal.tsx never publishes when Claude is idle at startup, the store's initial value stays null and the row's `isWorking === false` conditional never matches — the dot never shows
  - Provisional verdict: PASS (Terminal publishes on connect) / SUSPECT (only publishes on state change from a running-state) / UNKNOWN (cannot determine from source alone; needs runtime observation)

**Candidate B: sessionWorkingKey mismatch (row.targetTmuxSession null vs Terminal publishing real tmuxSessionName)**
  - `grep -n "sessionWorkingKey" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`
  - `grep -n "tmuxSessionName\|targetTmuxSession\|hostConfig\|session-working-store" src/ui/features/terminal/Terminal.tsx | head -20`
  - Summarize: the panel computes sessionWorkingKey as `${row.host?.id}:${row.targetTmuxSession ?? ''}` — an empty tmux session string when null. What key does Terminal.tsx publish under? If Terminal publishes `${hostConfig.id}:${realTmuxName}` where realTmuxName is the actual tmux session name (e.g., "claude-agent-1"), the two keys mismatch and the store hook returns null → row never gets isWorking===false → dot never shows.
  - Provisional verdict: PASS (keys align) / MISMATCH DETECTED (keys diverge) / UNKNOWN

**Candidate C: activeSet sessionStorage populate on fresh session**
  - `grep -n "activeSet\|sessionStorage" src/ui/state/conversation-store.ts | head -30`
  - Summarize: does activeSet populate from sessionStorage on module load? What triggers the first .has(row.id)===true after a fresh page reload? Is there a race where the row renders before activeSet rehydrates?
  - Provisional verdict: PASS (rehydrates synchronously at module load) / RACE POSSIBLE (rehydrates in effect after first render) / UNKNOWN

**Candidate D: PrettyConversationRowLive Rules-of-Hooks compliance**
  - `grep -n "PrettyConversationRowLive\|useSessionWorking" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`
  - Summarize: does PrettyConversationRowLive call `useSessionWorking(sessionKey)` at the top of the component body BEFORE any early return, conditional, or loop? Are there any conditional hook calls that would trigger a Rules-of-Hooks warning?
  - Provisional verdict: PASS (compliant, no conditional hooks) / VIOLATION (conditional hooks detected)

For each candidate ALSO record whether Wave 1's row rewrite may have changed the render conditional. The Wave 1 row emits `{inActiveSet && isWorking === false && <span ... />}` — same conditional as pre-lift. So Wave 1 does not directly fix any of the 4 candidates; it may fix an indirect issue if the JS-computed inline style was somehow interfering with the render (unlikely but possible).

## Section 2: Post-lift source state — mobile scroll-freeze

  - `grep -n "onTouchStart\|onTouchMove\|onTouchEnd\|passive:\|preventDefault" src/ui/features/pretty-conversations/PrettyConversationRow.tsx | head -20`
  - `grep -n "overflow-y-auto\|overscroll-behavior" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`
  - Summarize: did Wave 1 change the touch event listener attachment strategy? Are the swipe touch handlers still non-preventDefault (passive-friendly)? Is there a `touch-action` CSS declaration on the row that might block vertical scroll? Did Wave 1's CSS include a `touch-action: pan-y` (permit vertical, block horizontal) or similar?
  - If Wave 1 CSS does NOT include a `touch-action` declaration on `.pv-row`, mobile scroll should behave the same as pre-Wave-1 (which had the freeze). Recommend adding `.pv-row { touch-action: pan-y; }` to pretty-conversations.css to explicitly permit vertical browser scroll and constrain to horizontal swipe.
  - Provisional verdict: OBVIATED BY WAVE 1 (touch-action added or handler strategy changed) / UNCHANGED FROM PRE-WAVE-1 (freeze may still repro)

## Section 3: Post-lift source state — 100dvh/100vh safe-area padding escape

  - `grep -rn "100dvh\|100vh" src/ui/AppShell.tsx | head -20`
  - `grep -n "pb-\[env(safe-area-inset-bottom)\]\|paddingBottom.*safe-area" src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`
  - Summarize: how many 100dvh/100vh references exist in the AppShell height chain? Did Wave 2's chevron rebase affect any of them? The workaround at `PrettyConversationsPanel.tsx:233` is `pb-[env(safe-area-inset-bottom)]` on the panel's outer wrapper. If AppShell's outer wrapper `<div className='flex w-screen bg-background' style={{ height: '100dvh', paddingTop: 'max(env(safe-area-inset-top), 0px)' }}>` (line ~1401) does not also apply `paddingBottom: 'max(env(safe-area-inset-bottom), 0px)'`, that's the escape root cause — the 100dvh height includes the safe-area strip but no padding compensates.
  - Provisional verdict: WORKAROUND STILL NEEDED (root cause 100dvh chain escape at AppShell outer) / MAY BE REVERTABLE (Wave 2 chevron rebase may have added the missing paddingBottom) / UNCLEAR (needs runtime UAT)

## Section 4: Recommended next steps

Based on Sections 1-3, enumerate concrete next-step actions for each of the 3 areas:

For SHAPE-05:
  - If Candidates A-D all provisional-verdict PASS but Ashley UATs and dot still doesn't show: the failure mode is NOT one of the 4 candidates — expand investigation to a 5th candidate (e.g., CSS specificity war between `.pv-ready-dot` base `display: none` and `.pv-row.active-set:not(.working) .pv-ready-dot { display: block }`)
  - If any candidate provisional-verdict SUSPECT: enumerate the specific verification runtime observation Ashley needs to perform (e.g., open browser devtools console, `document.querySelectorAll('[aria-label="ready"]').length` after clicking 3 conversations)

For mobile scroll-freeze:
  - If OBVIATED verdict: no next steps beyond Ashley UAT confirmation
  - If UNCHANGED verdict: dedicated mobile devtools capture session — Ashley connects iPhone to Mac Safari devtools, records touch events during a scroll-freeze repro

For safe-area padding:
  - If REVERTABLE verdict: propose a follow-up plan that reverts the workaround
  - If WORKAROUND NEEDED verdict: no action this phase (nice-to-have; not phase-blocking per SHAPE-04)

## Section 5: Ashley UAT walkthrough (deferred to Task 2 checkpoint)

Placeholder pointing to Task 2's human-verify checkpoint.

Commit: `docs(phase-13): draft 13-04 UAT-DIAG-LOG preliminary automated diag pass`

Do NOT modify any source file in this task. If any of the read files reveal a critical bug (e.g., PrettyConversationRowLive has a conditional hook call — Rules-of-Hooks violation), record it in Section 4 as a routed-back critical fix but do NOT fix in this task. The fix routes to a follow-up plan.
  </action>
  <verify>
    <automated>test -f /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-04-UAT-DIAG-LOG.md && grep -qE "Section 1|SHAPE-05" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-04-UAT-DIAG-LOG.md && grep -qE "Candidate A|Terminal\.tsx isIdle" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-04-UAT-DIAG-LOG.md && grep -qE "Candidate B|sessionWorkingKey" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-04-UAT-DIAG-LOG.md && grep -qE "Candidate C|activeSet" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-04-UAT-DIAG-LOG.md && grep -qE "Candidate D|PrettyConversationRowLive|Rules-of-Hooks" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-04-UAT-DIAG-LOG.md && grep -qE "Section 2|scroll-freeze" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-04-UAT-DIAG-LOG.md && grep -qE "Section 3|safe-area|100dvh" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-04-UAT-DIAG-LOG.md && grep -qE "Section 4|Recommended next steps" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-04-UAT-DIAG-LOG.md && cd /home/ubuntu/skynet && [ -z "$(git diff --stat src/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `13-04-UAT-DIAG-LOG.md` file exists with all 5 sections
    - Section 1 covers all 4 diagnostic candidates with a provisional verdict (PASS/SUSPECT/MISMATCH/UNKNOWN) for each
    - Section 2 covers mobile scroll-freeze with a provisional verdict
    - Section 3 covers 100dvh/100vh safe-area with a provisional verdict
    - Section 4 enumerates concrete next-step actions for each of the 3 areas
    - Section 5 is a placeholder for Task 2's human-verify checkpoint
    - `git diff --stat src/` returns empty — no source changes in this task
    - Docs commit landed
  </acceptance_criteria>
  <done>Preliminary automated diagnostic pass documented; 4 candidates + mobile scroll + safe-area all have provisional verdicts + next-step recommendations; no source changes; ready for Ashley UAT checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Ashley UAT checkpoint — walk desktop + mobile confirmation of SHAPE-05 dot visibility, mobile scroll, safe-area padding</name>
  <what-built>Post-Wave-1+2+3 conversation list is live in a local dev instance (`npm run dev`) reflecting the lift-from-mock changes. Wave 1 restructured the row to class-toggle state variants; Wave 2 rewrote the panel header + AppShell chevron; Wave 3 retired PinAction's Skynet button chrome. The full mock lift has landed in source. This checkpoint is the first time Ashley SEES the result — Task 1's automated diag pass gave provisional verdicts based on source reads; only human UAT confirms the actual behavior.</what-built>
  <how-to-verify>
    Ashley performs the following on both DESKTOP (Chrome / Safari on her laptop) and MOBILE (iPhone PWA connected to the local dev server, or the staging URL if deployed):

    **A. Overall visual parity with the mock (SHAPE-01/02/03/04 sanity):**
      1. Open the local dev instance (or staging URL if `docker compose up -d --force-recreate skynet` was greenlit). Land on the conversation list.
      2. Compare side-by-side against `~/.claude/identities/tina/bounties/skynet-transformation/prototype.html` (Full-intensity + Normal density variant). Visual gestalt should match: warm-cream rows with hue-tinted bubbles for active-set, ambient recession for out-of-set, UPPERCASE panel-header title, transparent pencil button, bare-icon pin with hue-drop-shadow on pinned rows.
      3. Report OVERALL PARITY: PASS / DIFFERENCE_NOTED (with specific delta).

    **B. Ready-for-attention dot visibility (SHAPE-05 — the primary verification of this plan):**
      1. Freshly reload the page (Cmd+R / iOS reload). Verify the activeSet starts empty.
      2. Click into 3 different conversations one after another (Ashley Prime, Tina, and any third). Each click adds the row to the activeSet.
      3. For each of the 3 clicked rows, once its agent is idle (Claude is at a prompt, not typing), the row should show a bright hue-cream ready-dot in its `.pv-meta` column (right side of the row).
      4. If a clicked row's agent is currently working (Claude is generating a response), it should NOT show a dot — the dot appears once Claude finishes.
      5. Rows NOT in the activeSet (rows Ashley did NOT click this session) should NEVER show the dot regardless of their agent's state.
      6. Report: PASS (dots visible on 3+ idle-clicked rows) / FAIL_NO_DOTS (no dots visible on any clicked row) / FAIL_PARTIAL (some rows show dots, others don't) / FAIL_WRONG_ROWS (dots on non-clicked rows).

    **C. Mobile iPhone PWA scroll test:**
      1. On iPhone PWA, land on the conversation list.
      2. Scroll from top to bottom of the list. Do not stop mid-scroll — momentum through.
      3. Scroll back up. Try a fast flick + a slow drag.
      4. Report: PASS (scroll smooth, no freeze) / FAIL_FREEZE (scroll locks partway; confirm which gesture triggers) / FAIL_INTERMITTENT (freeze on some scrolls not others).

    **D. Safe-area padding on iPhone PWA (nice-to-have per SHAPE-04):**
      1. On iPhone PWA with the conversation list scrolled to the bottom, verify the last row is NOT scrolled behind the home indicator (the black bar at the very bottom of the iPhone screen).
      2. Report: PASS (last row above home indicator) / FAIL (last row underneath / partially obscured).

    **E. Pretty-view interior scope verification (SHAPE-06):**
      1. Click into any pretty-view chat and confirm the interior looks IDENTICAL to pre-Phase-13 (bubbles, compose box, IdentityBadge, message rendering, chat-column background all unchanged).
      2. Report: PASS (interior identical) / FAIL (any interior element changed — routes back as SHAPE-06 scope violation).

    **F. RDP row + shadcn dialogs (SHAPE-06 preservation):**
      1. Click into an RDP-host-sentinel row → Guacamole pane opens; visual is unchanged from pre-Phase-13 (Skynet theme classes preserved for RDP/shadcn per Ship-of-Theseus rule).
      2. Report: PASS (RDP visual unchanged) / FAIL (RDP visual regressed).

    Ashley's confirmation output goes into `13-04-UAT-DIAG-LOG.md` Section 5 as a table:

    | Item | Verdict | Notes |
    |------|---------|-------|
    | A. Overall parity | PASS/DIFFERENCE_NOTED | ... |
    | B. Dot visibility | PASS/FAIL_* | ... |
    | C. Mobile scroll | PASS/FAIL_* | ... |
    | D. Safe-area padding | PASS/FAIL | ... |
    | E. Pretty-view interior | PASS/FAIL | ... |
    | F. RDP + shadcn | PASS/FAIL | ... |

    ROUTE-BACK for each FAIL:
    - Item A: enumerate which visual delta — could route to Wave 1 (row), Wave 2 (panel/AppShell), or Wave 3 (pin)
    - Item B: consult Task 1's Section 1 provisional verdicts; the SUSPECT candidate is the primary suspect. If all were PASS, expand to CSS specificity investigation (5th candidate)
    - Item C: capture iPhone Safari devtools touch event trace; may become a follow-up plan
    - Item D: ROUTE to AppShell outer wrapper paddingBottom fix (nice-to-have; not phase-blocking)
    - Item E: HARD FAIL — routes back to whichever Wave/plan touched the pretty-view interior (scope violation)
    - Item F: HARD FAIL — routes back to whichever Wave/plan touched shadcn primitives or RDP dialogs (scope violation)

    On approval: Ashley responds with the completed table via chat OR types "approved SHAPE-05 with observations recorded in Section 5" AND the executor commits the completed 13-04-UAT-DIAG-LOG.md with the observation table appended to Section 5.
  </how-to-verify>
  <resume-signal>Ashley responds with the completed A-F verdict table (via chat) OR types "approved SHAPE-05" AND executor appends her verdicts to 13-04-UAT-DIAG-LOG.md Section 5, or Ashley reports specific failures with observations for route-back</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Automated diagnostic verdicts → Ashley UAT | Task 1's provisional verdicts (PASS/SUSPECT/MISMATCH/UNKNOWN) are inputs to Ashley's UAT decision. A false-PASS in Task 1 could bias Ashley toward accepting broken behavior. Mitigated by Task 2's UAT being HUMAN-OBSERVED (Ashley sees the actual dot render, not a source read) — the human observation is dispositive. |
| Ashley UAT verdicts → source-fix follow-up plan | If Ashley reports FAIL on any item, a follow-up plan handles the source fix. This plan documents the route-back but does NOT execute the source fix — that boundary is intentional to prevent scope creep of the checkpoint plan. |
| Pretty-view interior boundary → SHAPE-06 | Item E of the UAT explicitly checks pretty-view interior parity as a scope-boundary probe. If interior changed, that's a SHAPE-06 violation and routes back as a scope failure — not a bug fix in this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-01 | Tampering (executor tempted to fix source instead of documenting route-back) | 13-04-UAT-DIAG-LOG.md scope | mitigate | Plan acceptance criteria explicitly forbids source changes in `src/` in this plan. `git diff --stat src/` verified empty. Follow-up plan handles any source fix. |
| T-13-04-02 | Denial of Service (Ashley UAT reveals a scope violation from Wave 1/2/3) | pretty-view interior boundary | mitigate | Item E of the UAT probes pretty-view interior parity. If violation detected, route-back is unambiguous. This is a defensive check; the Wave 1/2/3 plans' scope grep gates should have caught it earlier. |
| T-13-04-03 | Information Disclosure | 13-04-UAT-DIAG-LOG.md content | accept | Log contains file paths, line references, provisional verdicts — same class of information as other planning tree docs. |
| T-13-04-04 | Denial of Service (mobile scroll-freeze fix requires touch-action CSS that Wave 1 did not include) | pretty-conversations.css | mitigate | Task 1's Section 2 recommends `.pv-row { touch-action: pan-y; }` if the freeze reproduces. If Ashley confirms scroll works without the addition, no action needed. If freeze reproduces, follow-up plan adds the CSS declaration (single-line change, trivial). |
| T-13-04-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this plan — docs authoring only. No supply-chain surface. |
</threat_model>

<verification>
- `13-04-UAT-DIAG-LOG.md` file exists with all 5 sections (Task 1 grep gates)
- All 4 diagnostic candidates have provisional verdicts (Task 1 acceptance criteria)
- Task 1's `git diff --stat src/` returns empty — no source changes
- Task 2 is a checkpoint requiring Ashley's human-observed verdict on 6 items (A-F)
- Ashley's completed A-F verdict table is appended to Section 5 of the log
- Any FAIL routes back to a source-fix follow-up plan (or Wave rework if scope violation)
- On PASS across all items, this plan closes as PASS and Wave 4 docs (13-05) proceeds
</verification>

<success_criteria>
- SHAPE-05 satisfied post-lift OR clear route-back documented with next-step actions for any candidate that FAILs
- Mobile scroll-freeze either OBVIATED or captured with a next-step diag round proposal
- Safe-area padding either REVERTABLE or documented as staying (nice-to-have, not phase-blocking)
- Pretty-view interior + shadcn/RDP scope preservation confirmed by human UAT (Items E and F)
- No source changes in this plan — this is diagnostic + UAT authoring only
- If ANY item FAILs and requires a source fix, a follow-up plan is spawned (not created in this plan; recommended in the route-back note)
</success_criteria>

<output>
Create `.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-04-SUMMARY.md` when done, and ensure `13-04-UAT-DIAG-LOG.md` contains Ashley's completed A-F table in Section 5.
</output>
