---
phase: 13-skynet-transformation-conversation-list-lift-from-mock
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/ui/features/pretty-conversations/PinAction.tsx
  - src/ui/features/pretty-conversations/pretty-conversations.css
autonomous: true
requirements:
  - SHAPE-03
tags: [ui, css, pretty-conversations, lift-from-mock, pin-action]

must_haves:
  truths:
    - "`PinAction.tsx` desktop size renders as a bare icon (no wrapper button chrome — no bg, no border, no rounded rect, no hover:bg) — the mock's `.pv-meta .pv-pin` treatment"
    - "`PinAction.tsx` desktop pinned icon uses `color: hsla(var(--pv-hue), 80%, 70%, 0.95)` + `filter: drop-shadow(0 0 4px hsla(var(--pv-hue), 80%, 60%, 0.55))` — the mock's SHAPE-03 hue-cream + hue-glow treatment"
    - "The mock's `.pv-row:not(.pinned) .pv-meta .pv-pin { display: none }` rule (declared in Wave 1's CSS) hides the pin element entirely on unpinned rows — verified by the CSS file grep gate"
    - "The 2 remaining Skynet theme-class hits in the conversation-list subtree — `text-muted-foreground/60` at PinAction.tsx line 97, `text-muted-foreground/60 hover:text-foreground` at lines 98 and 101 — are eliminated; grep for `text-muted-foreground|hover:text-foreground|bg-background|bg-card|text-foreground|border-border|muted-foreground` across `src/ui/features/pretty-conversations/` returns 0 non-comment hits"
    - "The final Skynet theme-class hit in PrettyConversationRow.tsx — `text-muted-foreground/60` on the Pin glyph (line 655 area, `className={`${pinGlyphSize} ${hue == null ? 'text-muted-foreground/60' : ''}`}`) — was retired in Wave 1 as part of the row rewrite. If Wave 1 did not catch it, THIS PLAN'S grep gate will fail and route back to Wave 1 — the gate is dispositive"
    - "PinAction mobile size (48x48 hue-tinted disc inside the swipe-reveal strip) is preserved verbatim — the mock does not treat mobile swipe-reveal because the mock is a desktop layout and mobile swipe-reveal is a fork-only affordance; keep the current mobile visual"
    - "The `size='mobile'` branch of PinAction is UNCHANGED (still emits a 48x48 hue-tinted disc with the current bg/border/box-shadow) — the SHAPE-03 lift applies specifically to `size='desktop'` per Ashley's context (pin buttons in the desktop main list are what she called out as 'totally obnoxious'; mobile swipe-reveal is fine)"
    - "`npx tsc --noEmit` exits 0 after the commit"
    - "`npx vitest run` all green"
    - "No edits under `src/ui/features/pretty-view/`, `src/ui/components/`, `src/ui/ssh/`, `src/ui/features/terminal/` — SHAPE-06 scope boundary"
    - "No edits to `src/ui/features/pretty-conversations/PrettyConversationRow.tsx` (Wave 1 owns it — parallel-safety with 13-02) or `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` (13-02 owns it)"
    - "No edits to `src/ui/AppShell.tsx` (13-02 owns the chevron edit — parallel-safety)"
  artifacts:
    - path: "src/ui/features/pretty-conversations/PinAction.tsx"
      provides: "Pin button component with mock's bare-icon + hue-drop-shadow desktop treatment; mobile 48x48 hue-tinted disc preserved"
      contains: "hsla"
    - path: "src/ui/features/pretty-conversations/pretty-conversations.css"
      provides: "Augmented CSS with .pv-pin-action-desktop selector for bare-icon-with-glow treatment"
      contains: ".pv-pin-action-desktop"
  key_links:
    - from: "src/ui/features/pretty-conversations/PinAction.tsx"
      to: "src/ui/features/pretty-conversations/pretty-conversations.css .pv-pin-action-desktop"
      via: "className='pv-pin-action-desktop' on the desktop branch; --pv-hue custom property inherited from parent .pv-row driving the hsla() color + drop-shadow filter"
      pattern: "pv-pin-action-desktop"
    - from: ".pv-row:not(.pinned) .pv-meta .pv-pin"
      to: "display: none"
      via: "CSS rule declared in Wave 1's pretty-conversations.css; hides the pin element entirely on unpinned rows"
      pattern: "display:\\s*none"
---

<objective>
Retire PinAction.tsx's Skynet button chrome on the DESKTOP size (`w-6 h-6 rounded-md bg-transparent border-0 hover:bg-white/[0.06] text-muted-foreground/60`) and lift the mock's SHAPE-03 bare-icon-with-hue-drop-shadow treatment (`color: hsla(var(--pv-hue), 80%, 70%, 0.95)` + `filter: drop-shadow(0 0 4px hsla(var(--pv-hue), 80%, 60%, 0.55))`) plus rely on Wave 1's `.pv-row:not(.pinned) .pv-meta .pv-pin { display: none }` CSS rule to hide the pin element entirely on unpinned rows. Eliminate the 2 remaining Skynet theme-class hits in the conversation-list subtree (PinAction.tsx lines 97, 98, 101). The MOBILE size (48x48 hue-tinted disc inside the swipe-reveal strip) is preserved verbatim — that's a fork-only affordance the mock does not cover, and Ashley's "obnoxious" complaint was specifically about the desktop main-list pin buttons.

Purpose: Implements SHAPE-03 from REQUIREMENTS.md § "Skynet Shape Completion — Conversation List + Shell Chrome Lift-from-Mock (Phase 13)". Ashley called out this session verbatim: "The pin buttons are totally obnoxious." Lifting the mock's bare-icon-with-glow treatment for desktop retires the button chrome; the CSS hide-on-unpinned rule (from Wave 1) makes them invisible unless pinned — matching the mock exactly.

This plan runs in PARALLEL with 13-02 because files_modified are disjoint (13-02: PrettyConversationsPanel.tsx + AppShell.tsx; 13-03: PinAction.tsx). Both depend on Wave 1 (13-01) which declared the CSS file's `.pv-pin` selectors and the hide-on-unpinned rule. If 13-01's CSS did not include a `.pv-pin-action-desktop` selector (Wave 1 focused on the row markup), this plan augments the CSS with the missing selector.

Output: rewritten PinAction.tsx desktop branch (~30 lines added CSS class name + hue custom property), augmented pretty-conversations.css with a `.pv-pin-action-desktop` selector, mobile branch untouched. Delivered as 1-2 atomic commits, tsc-clean + vitest-green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-CONTEXT.md
@.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-01-SUMMARY.md
@src/ui/features/pretty-conversations/PinAction.tsx
@src/ui/features/pretty-conversations/pretty-conversations.css
@src/ui/features/pretty-conversations/PrettyConversationRow.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite PinAction.tsx desktop branch to mock's bare-icon-with-hue-drop-shadow treatment + augment CSS with .pv-pin-action-desktop selector</name>
  <files>src/ui/features/pretty-conversations/PinAction.tsx, src/ui/features/pretty-conversations/pretty-conversations.css</files>
  <behavior>
    - PinAction desktop branch emits `<button type='button' className='pv-pin-action-desktop' onClick={onClick} title={label} aria-label={label} data-testid={dataTestId ?? 'pin-action'}>{pinned ? <PinOff /> : <Pin />}</button>`
    - The button element carries NO bg, NO border, NO rounded-md wrapper, NO hover:bg — all button-chrome retired; CSS `.pv-pin-action-desktop { background: transparent; border: 0; padding: 0; cursor: pointer; display: inline-flex; align-items: center; justify-content: center; width: 20px; height: 20px; }` applies bare-icon styling
    - The button's icon color + drop-shadow come from CSS: `.pv-pin-action-desktop { color: hsla(var(--pv-hue, 216), 80%, 70%, 0.95); filter: drop-shadow(0 0 4px hsla(var(--pv-hue, 216), 80%, 60%, 0.55)); }` — `--pv-hue` is inherited from the parent `.pv-row` (Wave 1 emitted it as inline style)
    - No-hue-fallback: when `.pv-row` has no `--pv-hue` set (hue==null case — neutral rows), the `var(--pv-hue, 216)` fallback provides a default hue value. CSS's `var()` fallback is the standard mechanism. Alternatively, `.pv-row.rdp .pv-pin-action-desktop { color: var(--color-pv-fg-muted); filter: none; }` overrides for RDP rows (though RDP rows never render PinAction per the Row's contract; declare defensively anyway)
    - PinAction mobile branch (size='mobile') stays UNCHANGED — same 48x48 hue-tinted disc treatment with the current bg/border/box-shadow; that's the swipe-reveal strip's fork-only affordance
    - Behavior: onClick still forwards the raw event to the outer handler; e.stopPropagation lives in the parent Row.onPinClick handler (unchanged); no state management, no gesture handling — this component is a dumb visual
    - Test: existing PinAction usage in PrettyConversationRow.test.tsx (Test 8: desktop pin e.stopPropagation) continues to pass — the test asserts click event forwarding, not the button's visual chrome
    - Grep: `grep -rE 'text-muted-foreground|hover:text-foreground|bg-background|bg-card|text-foreground|border-border|muted-foreground' src/ui/features/pretty-conversations/` returns 0 non-comment hits (this is the definitive Skynet-theme-class purge for the conversation-list subtree)
  </behavior>
  <read_first>
    - /home/ubuntu/skynet/src/ui/features/pretty-conversations/PinAction.tsx (current 124-line implementation — read the entire file; retire the desktop branch lines 84-122, preserve the mobile branch lines 48-83; retire the `iconColorStyle` and `iconClassBase` derivations at the top of the desktop branch)
    - /home/ubuntu/skynet/src/ui/features/pretty-conversations/pretty-conversations.css (from Wave 1 — verify `.pv-row:not(.pinned) .pv-meta .pv-pin { display: none }` is declared; if `.pv-pin-action-desktop` is NOT declared, this task adds it)
    - /home/ubuntu/.claude/identities/tina/bounties/skynet-transformation/prototype.html (mock lines 332-337 — `.meta .pin { color: hsla(var(--hue), 80%, 70%, 0.95); filter: drop-shadow(0 0 4px hsla(var(--hue), 80%, 60%, 0.55)); }`; lines 337 `.row:not(.pinned) .meta .pin { display: none; }`; mock does not have a "pin-action button" per se — it uses the pin GLYPH directly with those styles; the fork's PinAction is a wrapper button for accessibility + click handling that the mock's static HTML doesn't need)
    - /home/ubuntu/skynet/src/ui/features/pretty-conversations/PrettyConversationRow.tsx (Wave 1 output — verify the row emits the PinAction inside `.pv-meta` and that the Wave 1 rewrite already retired the pin glyph's `text-muted-foreground/60` hit at line ~655; if NOT, flag as a Wave 1 gap and route back BEFORE proceeding with this task)
  </read_first>
  <action>
Rewrite the desktop branch of `src/ui/features/pretty-conversations/PinAction.tsx` — specifically lines 84-122 (after the mobile branch return). Mobile branch (lines 48-83) is UNCHANGED.

**New desktop branch structure:**

Retire lines 88-101 (the `iconColorStyle` and `iconClassBase` derivations — dead code after the class-based rewrite).

Retire lines 103-122 (the button JSX with Skynet theme classes) and replace with a simplified structure:

The desktop return becomes:
  - Button element with `className='pv-pin-action-desktop'`
  - No inline `style={iconColorStyle}` — CSS handles color via `.pv-pin-action-desktop` selector reading `var(--pv-hue)` inherited from parent row
  - `type='button'`, `onClick={onClick}`, `title={label}`, `aria-label={label}`, `data-testid={dataTestId ?? 'pin-action'}` — all preserved verbatim from current implementation
  - Inner icon: `pinned ? <PinOff /> : <Pin />` with NO size classes (Tailwind `w-3.5 h-3.5` retired); CSS `.pv-pin-action-desktop svg { width: 14px; height: 14px; }` handles size

**Augment pretty-conversations.css with the .pv-pin-action-desktop selector.** Add these rules AFTER the `.pv-meta .pv-pin` block from Wave 1:

The additions:
  - `.pv-pin-action-desktop { display: inline-flex; align-items: center; justify-content: center; width: 20px; height: 20px; padding: 0; background: transparent; border: 0; cursor: pointer; color: hsla(var(--pv-hue, 216), 80%, 70%, 0.95); filter: drop-shadow(0 0 4px hsla(var(--pv-hue, 216), 80%, 60%, 0.55)); transition: filter 120ms ease, color 120ms ease; -webkit-tap-highlight-color: transparent; }`
  - `.pv-pin-action-desktop svg { width: 14px; height: 14px; stroke-width: 2; }`
  - `.pv-pin-action-desktop:hover { filter: drop-shadow(0 0 6px hsla(var(--pv-hue, 216), 80%, 60%, 0.75)); }` — hover boosts the glow strength
  - `.pv-pin-action-desktop:focus-visible { outline: 2px solid hsla(var(--pv-hue, 216), 70%, 60%, 0.6); outline-offset: 2px; }` — focus ring using hue for a11y

**Hide-on-unpinned handling:** Wave 1's CSS already declared `.pv-row:not(.pinned) .pv-meta .pv-pin { display: none }` which hides the pin GLYPH. The PinAction desktop button is a separate element — its hover-reveal behavior on desktop (opacity-0 group-hover:opacity-100 in the current Row) was retired in Wave 1. Wave 1 also should have added a rule to hide the PinAction on unpinned+non-hovered desktop rows. Verify: `grep -E 'pv-pin-action-desktop.*display|not\(.pinned\).*pv-pin-action' pretty-conversations.css`. If the Wave 1 CSS does NOT include a hide rule for `.pv-pin-action-desktop` on unpinned rows, add it here:
  - `.pv-row.pv-row--desktop:not(.pinned):not(:hover) .pv-pin-action-desktop { display: none; }` — hides the pin-action button on unpinned desktop rows unless the row is hovered
  - `.pv-row.pv-row--desktop:not(.pinned):not(:focus-within) .pv-pin-action-desktop { display: none; }` — additional gate for keyboard focus (allows pinning via keyboard nav)
  - Combine into one rule: `.pv-row.pv-row--desktop:not(.pinned):not(:hover):not(:focus-within) .pv-pin-action-desktop { display: none; }`

**Retire:** `iconColorStyle`, `iconClassBase` computation blocks (dead code after the CSS-class rewrite). The desktop branch is now ~15 lines total (down from ~30).

**Preserve verbatim:**
  - Imports (Pin, PinOff, useTranslation, MouseEvent type)
  - Prop shape (hue, pinned, size, onClick, data-testid) — same signature
  - Label derivation (`t("nav.conversations.unpin"...)` / `t("nav.conversations.pin"...)`)
  - Mobile branch (lines 48-83) — entire mobile size='mobile' return

**Atomic commits:**
  1. `feat(phase-13): rewrite PinAction desktop to mock's bare-icon-with-hue-glow` — source file rewrite
  2. `style(phase-13): add .pv-pin-action-desktop bare-icon-with-glow selectors to pretty-conversations.css` — CSS augmentation

Combine into ONE commit if the executor judges the split unnecessary (the two edits are tightly coupled). Claude's discretion.

Post-commits: `npx tsc --noEmit` exits 0; `npx vitest run` all green.

**Final Skynet-theme-class purge verification for the conversation-list subtree:**

Run `grep -rE 'text-muted-foreground|hover:text-foreground|bg-background|bg-card|text-foreground|border-border|muted-foreground' src/ui/features/pretty-conversations/ | grep -v '\.md:' | grep -v '^\s*//' | grep -v '^\s*\*'`. Expected output: EMPTY. If any hit remains, either:
  - It's in PinAction.tsx (this plan) — fix here
  - It's in PrettyConversationRow.tsx (Wave 1) — Wave 1 did not fully purge; ROUTE BACK to Wave 1 before this plan closes
  - It's in PrettyConversationsPanel.tsx (13-02) — 13-02 did not fully purge; ROUTE BACK to 13-02 before this plan closes
  - It's in a comment or docs — acceptable, filtered by the grep flags above

**Do NOT touch:**
  - `src/ui/features/pretty-conversations/PrettyConversationRow.tsx` (Wave 1 owns it — 13-01)
  - `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` (13-02 owns it)
  - `src/ui/AppShell.tsx` (13-02 owns the chevron edit)
  - `src/ui/features/pretty-view/`, `src/ui/components/`, `src/ui/ssh/`, `src/ui/features/terminal/` — SHAPE-06 scope boundary
  - Mobile branch of PinAction.tsx (lines 48-83) — preserved verbatim
  </action>
  <verify>
    <automated>test -f /home/ubuntu/skynet/src/ui/features/pretty-conversations/PinAction.tsx && grep -qE "['\"]pv-pin-action-desktop['\"]" /home/ubuntu/skynet/src/ui/features/pretty-conversations/PinAction.tsx && ! grep -vE '^\s*//' /home/ubuntu/skynet/src/ui/features/pretty-conversations/PinAction.tsx | grep -qE 'text-muted-foreground|hover:text-foreground|rounded-md bg-transparent border-0 hover:bg-white' && grep -qE '\.pv-pin-action-desktop' /home/ubuntu/skynet/src/ui/features/pretty-conversations/pretty-conversations.css && grep -qE 'hsla\(var\(--pv-hue' /home/ubuntu/skynet/src/ui/features/pretty-conversations/pretty-conversations.css && grep -qE 'drop-shadow.*hsla\(var\(--pv-hue' /home/ubuntu/skynet/src/ui/features/pretty-conversations/pretty-conversations.css && THEME_HITS=$(grep -rE 'text-muted-foreground|hover:text-foreground|bg-background|bg-card|text-foreground|border-border|muted-foreground' /home/ubuntu/skynet/src/ui/features/pretty-conversations/ | grep -vE '\.md:' | grep -vE '^\s*//' | grep -vE '/\*|\* ' | wc -l) && [ "$THEME_HITS" -eq 0 ] && cd /home/ubuntu/skynet && npx tsc --noEmit 2>&1 | tail -5 > /tmp/tsc-13-03.out && ! grep -qE 'error TS' /tmp/tsc-13-03.out && npx vitest run src/ui/features/pretty-conversations/ 2>&1 | tail -30 | grep -qE 'PASS|passed'</automated>
  </verify>
  <acceptance_criteria>
    - PinAction.tsx desktop branch uses `className='pv-pin-action-desktop'`
    - PinAction.tsx contains no non-comment lines with `text-muted-foreground/60`, `hover:text-foreground`, `rounded-md bg-transparent border-0 hover:bg-white/[0.06]`
    - PinAction.tsx mobile branch (lines 48-83) is UNCHANGED — verify via `git diff src/ui/features/pretty-conversations/PinAction.tsx` shows changes only in the desktop branch region
    - pretty-conversations.css contains `.pv-pin-action-desktop` selector
    - pretty-conversations.css contains `hsla(var(--pv-hue` (used in color + drop-shadow filter)
    - pretty-conversations.css contains a hide-on-unpinned rule for `.pv-pin-action-desktop` on non-hovered non-focused desktop rows (the mock's hide-when-not-pinned invariant)
    - Full grep `grep -rE 'text-muted-foreground|hover:text-foreground|bg-background|bg-card|text-foreground|border-border|muted-foreground' src/ui/features/pretty-conversations/ | grep -vE '\.md:|^\s*//|/\*' | wc -l` returns 0
    - `npx tsc --noEmit` exits 0
    - `npx vitest run src/ui/features/pretty-conversations/` all green
    - No edits under `src/ui/features/pretty-view/`, `src/ui/components/`, `src/ui/ssh/`, `src/ui/features/terminal/`, PrettyConversationRow.tsx, PrettyConversationsPanel.tsx, AppShell.tsx
    - 1-2 atomic commits landed (source + CSS)
  </acceptance_criteria>
  <done>PinAction.tsx desktop branch retired to mock's bare-icon-with-hue-drop-shadow treatment; mobile branch preserved verbatim; CSS augmented with .pv-pin-action-desktop selectors including hide-on-unpinned rule; all Skynet theme-class hits in src/ui/features/pretty-conversations/ purged; tsc-clean; tests green; atomic commits landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PinAction desktop CSS class → parent .pv-row --pv-hue inheritance | The desktop button's color + filter reference `var(--pv-hue)` inherited from the parent `.pv-row` element. If the parent row somehow does not carry the custom property (Wave 1 regression), the desktop pin would render with the fallback hue (216 = blue) instead of the identity's actual hue. Mitigated by Wave 1's contract that hue-bearing rows emit `--pv-hue` inline, and by Wave 3 plan 13-04's UAT walkthrough verifying the pin color matches the row hue at 3+ identities. |
| Wave 1 CSS + Wave 3 CSS additions → single-file coherence | Both Wave 1 (row + panel selectors) and Wave 3 (pin-action selectors) write to pretty-conversations.css. Later CSS may override earlier CSS via selector specificity. Mitigated by Wave 3 appending to the end of the file (later cascade), and by Wave 3 grep verification that Wave 1's key selectors (`.pv-row`, `.pv-row:not(.pinned) .pv-meta .pv-pin`) survive. |
| Mobile swipe-reveal 48x48 disc → mock non-coverage | The mock is a desktop layout; the mobile swipe-reveal 48x48 disc is a fork-only affordance. Preserving it verbatim (not lifting anything from the mock for mobile pin) is the correct scoping decision — Ashley's "obnoxious" complaint was about the DESKTOP list. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03-01 | Tampering (accidental mobile branch change) | PinAction.tsx | mitigate | Task 1 acceptance criteria requires `git diff` shows edits only in the desktop branch region (lines ~84-122). The mobile branch grep (`grep -A 30 "if (size === 'mobile')" PinAction.tsx`) should be byte-equivalent pre and post. |
| T-13-03-02 | Denial of Service (row hue not inherited into pin button) | CSS var(--pv-hue) inheritance | mitigate | Task 1 CSS declares `var(--pv-hue, 216)` with a fallback so the pin always renders SOMETHING even if inheritance fails. Wave 3 plan 13-04's UAT walkthrough verifies the pin hue tracks the row hue at 3+ identities. |
| T-13-03-03 | Repudiation (which layer purged the last Skynet theme-class hit) | conversation-list subtree | mitigate | Task 1's final grep gate is the definitive test — if it fails, the failure points at whichever file still has the hit (PinAction, PrettyConversationRow from Wave 1, or PrettyConversationsPanel from 13-02). Route-back is unambiguous. |
| T-13-03-04 | Denial of Service (pinned rows on unpinned CSS hide rule collision) | pretty-conversations.css | mitigate | Wave 1 CSS declares `.pv-row:not(.pinned) .pv-meta .pv-pin { display: none }` — this targets the pin GLYPH (an svg emitted as `<Pin className='pv-pin' aria-hidden='true' />` on pinned rows in the row's meta). Wave 3 adds `.pv-row.pv-row--desktop:not(.pinned):not(:hover):not(:focus-within) .pv-pin-action-desktop { display: none }` — this targets the pin-ACTION button (the click-to-pin/unpin control). Different elements, different selectors — no collision. Verified by keeping them logically distinct with different class names (.pv-pin vs .pv-pin-action-desktop). |
| T-13-03-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs. Pure CSS + JSX text edit. No supply-chain surface. |
</threat_model>

<verification>
- PinAction.tsx desktop branch uses `pv-pin-action-desktop` class name (grep gate)
- Skynet theme-class hits purged from PinAction.tsx (grep-negation gate)
- Mobile branch of PinAction.tsx unchanged (visual inspection via `git diff`)
- pretty-conversations.css contains `.pv-pin-action-desktop` selector with `hsla(var(--pv-hue` color + drop-shadow (grep gate)
- Full conversation-list subtree grep for Skynet theme-class hits returns 0 (final purge gate)
- `npx tsc --noEmit` exits 0
- `npx vitest run src/ui/features/pretty-conversations/` all green
- No edits to files outside the two files_modified list
</verification>

<success_criteria>
- SHAPE-03 satisfied: PinAction desktop uses mock's bare-icon-with-hue-drop-shadow treatment; unpinned rows hide the pin element via CSS
- All 413 Skynet theme-class hits in `src/ui/features/pretty-conversations/` (which was 2 remaining after Waves 1+11+12 — verify from grep) are eliminated — the conversation-list subtree is 100% palette-tokenized
- Mobile pin (swipe-reveal 48x48) preserved — no regression to the mobile affordance Ashley uses on iPhone
- Wave 4 (post-lift verification) can proceed with a clean conversation-list subtree
</success_criteria>

<output>
Create `.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-03-SUMMARY.md` when done.
</output>
