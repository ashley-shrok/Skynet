---
phase: 13-skynet-transformation-conversation-list-lift-from-mock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/features/pretty-conversations/pretty-conversations.css
  - src/ui/features/pretty-conversations/PrettyConversationRow.tsx
  - src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx
  - src/ui/features/pretty-conversations/tokens.ts
  - src/main.tsx
autonomous: true
requirements:
  - SHAPE-01
tags: [ui, css, pretty-conversations, lift-from-mock, row]

must_haves:
  truths:
    - "A real CSS file `src/ui/features/pretty-conversations/pretty-conversations.css` exists containing the mock's flat selectors (.pv-row, .pv-avatar, .pv-body, .pv-meta, .pv-ready-dot) with the mock's class-toggle state variants (.selected, .active-set, .working, .pinned, .ambient, .rdp)"
    - "The CSS file is imported so the styles reach the app (either at the module top of PrettyConversationRow.tsx or via src/main.tsx alongside src/ui/index.css)"
    - "`PrettyConversationRow.tsx` renders semantic markup matching the mock: `<div className='pv-row ...state...' style={{'--pv-hue': hue}}>` + `<div className='pv-avatar'>` + `<div className='pv-body'><span className='pv-label'/><span className='pv-host'/></div>` + `<div className='pv-meta'>...</div>` — with state variants (selected, active-set, working, pinned, ambient, rdp) applied as CSS classes toggled via className string composition, NOT JS-computed inline style objects for the base + variant treatments"
    - "JS-computed inline styles for base row body + avatar + ambient + selected + hover overlays are retired — the only inline style on `.pv-row` is the `--pv-hue: {hue}` custom property (drives the CSS hsla() expressions)"
    - "Tailwind layout scaffolding on the row/avatar/body/meta divs (`flex-1 min-w-0 flex flex-col gap-0.5`, `shrink-0 flex items-center gap-1.5`, `rounded-full`, `w-12 h-12`/`w-10 h-10`, `px-4 py-3`/`px-3 py-2.5`, `gap-3`/`gap-2.5`) is retired — the mock's CSS handles layout via `display: flex`, `flex: 1`, `padding: 10px 12px`, etc."
    - "Surviving JS-only concerns stay in `PrettyConversationRow.tsx`: swipe reveal (mobile), ready-dot conditional render (`{inActiveSet && isWorking === false && ...}`), avatar image src selection (identity.avatarUrl vs initial letter vs tabIcon fallback), click handlers, aria-labels, `--pv-hue` custom property emission"
    - "The `variant='mobile' | 'desktop'` prop still drives density (e.g., body class `pv-row--mobile` vs `pv-row--desktop`), swipe wiring gates, and dimensions — but via CSS density variants, not per-property Tailwind classes"
    - "PrettyConversationRow.test.tsx is updated to assert the new class-based rendering: `.pv-row` present, state classes toggled (`.selected` when selected, `.active-set` when inActiveSet, `.working` when isWorking===true, `.pinned` when pinned, `.rdp` when isRdp), inline style contains `--pv-hue: {hue}` for hue-bearing rows, `[data-testid=pv-avatar]` present, ready-dot span present under the exact same conditions as before (inActiveSet && isWorking===false)"
    - "All original 18 test cases in PrettyConversationRow.test.tsx pass (rewritten to the new markup shape); swipe state machine behavior unchanged; RDP row still skips swipe + PinAction; e.stopPropagation on pin click still verified; no-identity avatar fallback still emits tabIcon"
    - "`npx tsc --noEmit` exits 0 after each atomic commit in this plan"
    - "`npx vitest run src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` all green after each atomic commit"
    - "No edits under `src/ui/features/pretty-view/`, `src/ui/components/`, `src/ui/ssh/`, `src/ui/features/terminal/` — SHAPE-06 scope boundary preserved"
  artifacts:
    - path: "src/ui/features/pretty-conversations/pretty-conversations.css"
      provides: "Real CSS file holding the mock's flat selectors + class-toggle state variants — the source of truth for row rendering after this plan lands"
      contains: ".pv-row"
      min_lines: 120
    - path: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx"
      provides: "Row component rendering the mock's semantic markup with class-toggle state variants + surviving JS-only concerns (swipe, ready-dot, avatar src)"
      contains: "pv-row"
    - path: "src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx"
      provides: "18 tests asserting the new class-based rendering + all preserved behavior"
      contains: "pv-row"
  key_links:
    - from: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx"
      to: "src/ui/features/pretty-conversations/pretty-conversations.css"
      via: "import statement OR src/main.tsx side-effect import; className string composition consumes selectors defined in the CSS file"
      pattern: "pretty-conversations\\.css|pv-row"
    - from: "src/ui/features/pretty-conversations/PrettyConversationRow.tsx"
      to: ".pv-row inline style"
      via: "`--pv-hue: {hue}` custom property emission for hue-bearing rows drives the CSS hsla() expressions"
      pattern: "--pv-hue"
---

<objective>
Lift the mock's flat CSS + class-toggle state-variant recipe (mock v4 at `~/.claude/identities/tina/bounties/skynet-transformation/prototype.html`) directly onto the conversation row. Extract the mock's `.row` / `.avatar` / `.body` / `.meta` / `.dot` selectors + `.selected` / `.active-set` / `.working` / `.pinned` / `.rdp` state variants into a real CSS file at `src/ui/features/pretty-conversations/pretty-conversations.css` (prefixed `.pv-*` to avoid class-name collisions with anything else in the app), then rewrite `PrettyConversationRow.tsx` to emit the mock's semantic markup with class-toggle state variants — retiring the ~700 lines of JS-computed inline style + Tailwind layout scaffolding that currently produce a Skynet-flavored approximation of the mock. Surviving JS-only concerns (swipe reveal, ready-dot conditional render, avatar image src, click handlers) stay in the component. State variants become CSS class toggles (`className={cn('pv-row', selected && 'selected', inActiveSet && 'active-set', ...)}`), not JS-branch style-object computations.

Purpose: Implements SHAPE-01 from REQUIREMENTS.md § "Skynet Shape Completion — Conversation List + Shell Chrome Lift-from-Mock (Phase 13)". This is the foundational plan of Phase 13 — every downstream plan (panel header, PinAction, dot investigation, docs) depends on the CSS file existing and the row rendering the new markup. This is what Ashley called out this session: "active conversations like ones that I've already loaded into since I loaded the page are not glowing fully like they were supposed to." Lifting the mock's Full-intensity ambient recession + class-toggle state variants (mock v4 lock) fixes the recession-too-aggressive symptom AND retires the JS-computed style pattern that failed to keep in sync with the mock.

Output: pretty-conversations.css (new file, ~150 lines), rewritten PrettyConversationRow.tsx (~450 lines down from 709), rewritten PrettyConversationRow.test.tsx (18 tests updated to new markup), updated tokens.ts (retain only swipe constants — layout tokens retire), src/main.tsx CSS import (if chosen over module-top import). Delivered as 3-4 atomic commits, tsc-clean + vitest-green per commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/ui/features/pretty-conversations/PrettyConversationRow.tsx
@src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx
@src/ui/features/pretty-conversations/tokens.ts
@src/ui/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pretty-conversations.css lifting the mock's flat CSS verbatim (prefixed .pv-*), wire the CSS import so styles reach the app</name>
  <files>src/ui/features/pretty-conversations/pretty-conversations.css, src/main.tsx</files>
  <read_first>
    - /home/ubuntu/.claude/identities/tina/bounties/skynet-transformation/prototype.html (the mock — read the entire `<style>` block from line 7 through line 543; every selector Ashley signed off on lives there)
    - /home/ubuntu/skynet/src/ui/index.css (lines 110-152 — the `--color-pv-*` token authority; the CSS file uses these tokens by reference, does NOT re-declare them)
    - /home/ubuntu/skynet/src/main.tsx (see where `import "./ui/index.css"` sits; new CSS import follows the same pattern OR is imported from PrettyConversationRow.tsx module top — Task 2 will import from the component)
    - /home/ubuntu/skynet/src/ui/features/pretty-conversations/PrettyConversationRow.tsx (current inline-style patterns lines 159-361 — the values we're replacing; the CSS file must produce visually equivalent output for the ACTIVE-SET rows and the mock's exact values for the AMBIENT rows)
  </read_first>
  <action>
Create a new file at `src/ui/features/pretty-conversations/pretty-conversations.css` containing the mock's flat CSS lifted from prototype.html's `<style>` block, with these transformations:

**Selector prefix rename:** Every class name gets prefixed `pv-` (short for "pretty-view / pretty-conversations" — matches the token prefix used everywhere else in the fork). Rename map:
  - `.row` → `.pv-row`
  - `.avatar` → `.pv-avatar`
  - `.body` → `.pv-body`
  - `.body .label` → `.pv-body .pv-label`
  - `.body .host` → `.pv-body .pv-host`
  - `.body .host svg` → `.pv-body .pv-host svg`
  - `.meta` → `.pv-meta`
  - `.meta .pin` → `.pv-meta .pv-pin`
  - `.ready-dot` → `.pv-ready-dot`
  - `.row.selected` → `.pv-row.selected`
  - `.row.pinned` → `.pv-row.pinned`
  - `.row.working` → `.pv-row.working`
  - `.row.active-set` → `.pv-row.active-set`
  - `.row.rdp` → `.pv-row.rdp`
  - `.row:not(.pinned) .meta .pin` → `.pv-row:not(.pinned) .pv-meta .pv-pin`
  - `.row.active-set:not(.working) .ready-dot` → `.pv-row.active-set:not(.working) .pv-ready-dot`
  - `.rdp-divider` → `.pv-rdp-divider` (Panel plan 13-02 will use it — declare here for reuse)

**Hue custom property rename:** The mock uses `--hue: 216;` on `.row`. Rename to `--pv-hue: 216;` on `.pv-row` throughout the CSS file. Every `hsla(var(--hue), ...)` becomes `hsla(var(--pv-hue), ...)`. The JS component (Task 2) emits `style={{ ['--pv-hue' as any]: hue }}` for hue-bearing rows.

**Ambient recession scope simplification:** The mock scopes ambient recession under `body[data-active-set-enabled="on"]` because the toggle exists in the mock. In the app there is no toggle — ambient IS the default for non-active-set rows. Rewrite those selectors to scope on the row itself instead of the body attribute:
  - `body[data-active-set-enabled="on"] .row:not(.active-set):not(.rdp)` becomes `.pv-row.ambient`
  - `body[data-active-set-enabled="on"] .row:not(.active-set):not(.rdp) .avatar` becomes `.pv-row.ambient .pv-avatar`
  - `body[data-active-set-enabled="on"] .row:not(.active-set):not(.rdp):hover` becomes `.pv-row.ambient:hover`
  - `body[data-active-set-enabled="on"] .row:not(.active-set) .body .label` becomes `.pv-row.ambient .pv-body .pv-label`
  - `body[data-active-set-enabled="on"] .row:not(.active-set) .body .host` becomes `.pv-row.ambient .pv-body .pv-host`
The JS component sets `.ambient` on the row when `!isRdp && !inActiveSet`. This is the same logic as the current `isAmbient` derivation — moved from JS-computed style to CSS class.

**Reduced-intensity variant NOT included.** Skip the `body[data-intensity="reduced"]` and `body[data-intensity="selection-only"]` blocks — mock v4 lock is Full-intensity, and Ashley called those exploratory. Similarly skip the density variants (`body[data-density="tight"]`, `body[data-density="cozy"]`) — mock v4 lock is Normal density. We ship only Full+Normal.

**Density branching via variant class:** Add ONE size variant for the mobile/desktop `variant` prop split — chunky mobile rows (72px min-height) vs standard desktop rows (62px min-height). Add these two rules AT THE END of the CSS file (after the base .pv-row block):
  - `.pv-row--mobile { min-height: 72px; padding: 12px 16px; gap: 12px; }`
  - `.pv-row--mobile .pv-avatar { width: 48px; height: 48px; font-size: 18px; }`
  - `.pv-row--mobile .pv-body .pv-label { font-size: 15.5px; }`
  - `.pv-row--mobile .pv-body .pv-host { font-size: 12.5px; }`
  - `.pv-row--desktop { min-height: 62px; padding: 10px 12px; gap: 12px; }`
  - `.pv-row--desktop .pv-avatar { width: 40px; height: 40px; font-size: 15px; }`
  - `.pv-row--desktop .pv-body .pv-label { font-size: 13.5px; }`
  - `.pv-row--desktop .pv-body .pv-host { font-size: 11.5px; }`
(Values pulled from PrettyConversationRow.tsx current variant-derived dimensions at lines 139-151; validate against the current rendering to preserve density on both viewports.)

**Do NOT include:** `.stage`, `.controls`, `.notes`, `body::before`, `body { background: ... }` — those are mock-page chrome, not conversation-list styling. The panel `.panel` block is included but Panel plan 13-02 will consume it via its own rewrite (declare `.pv-panel`, `.pv-panel-header`, `.pv-panel-header .pv-title`, `.pv-panel-header .pv-pencil`, `.pv-panel-scroll` from mock lines 148-199 — Panel plan will consume them). Include these in this Wave-1 CSS file so both Wave-1 row and Wave-2 panel share one CSS file (Claude's discretion per CONTEXT.md decisions section).

**Palette references:** The `.pv-panel`, `.pv-panel-header`, `.pv-panel-scroll` blocks reference `--color-pv-surface-quiet`, `--color-pv-surface-quiet-alt`, `--color-pv-border-quiet`, `--color-pv-fg`, `--color-pv-fg-muted`, `--radius-pv-card` — those tokens already exist at src/ui/index.css:119-146. Do NOT re-declare them in this CSS file; reference them by var().

**Panel-header + pencil completeness (locked in Wave 1 so Wave 2 doesn'''t need to touch the CSS file):** Include these specific declarations in the .pv-panel-header + .pv-title + .pv-pencil blocks so Wave 2'''s panel-header rewrite consumes ONLY the JSX, not the CSS file:

  - .pv-panel-header block: display: flex; align-items: center; justify-content: space-between; padding: 14px 16px; border-bottom: 1px solid var(--color-pv-border-quiet);
  - .pv-panel-header .pv-title block: flex: 1; font-size: 12px; font-weight: 700; letter-spacing: 0.1em; text-transform: uppercase; color: var(--color-pv-fg);
  - .pv-panel-header .pv-pencil block: width: 32px; height: 32px; display: inline-flex; align-items: center; justify-content: center; color: var(--color-pv-fg-muted); background: transparent; border: 1px solid transparent; border-radius: 8px; cursor: pointer; user-select: none; -webkit-tap-highlight-color: transparent; transition: background 120ms ease, color 120ms ease, border-color 120ms ease;
  - .pv-panel-header .pv-pencil:hover block: background: rgba(220, 225, 245, 0.06); border-color: var(--color-pv-border-quiet); color: var(--color-pv-fg);
  - .pv-panel-header .pv-pencil svg block: width: 18px; height: 18px; stroke-width: 2;

These declarations make the CSS file self-contained for the panel header. Wave 2 plan 13-02 has files_modified = [PrettyConversationsPanel.tsx, PrettyConversationsPanel.test.tsx, AppShell.tsx] — NO CSS file edit — parallel-safe with Wave 2 plan 13-03 which owns pretty-conversations.css augmentation for .pv-pin-action-desktop.

**Wire the CSS import:** Add `import "./ui/features/pretty-conversations/pretty-conversations.css";` to `src/main.tsx` immediately after `import "./ui/index.css";`. This side-effect import makes the CSS globally available so both PrettyConversationRow and PrettyConversationsPanel (Wave 2) consume the same stylesheet.

**Atomic commit:** `feat(phase-13): lift mock CSS into pretty-conversations.css + wire import`

**Do NOT touch** `src/ui/features/pretty-view/`, `src/ui/components/`, `src/ui/ssh/`, `src/ui/features/terminal/` — SHAPE-06 scope boundary.
  </action>
  <verify>
    <automated>test -f /home/ubuntu/skynet/src/ui/features/pretty-conversations/pretty-conversations.css && grep -c '^\.pv-row' /home/ubuntu/skynet/src/ui/features/pretty-conversations/pretty-conversations.css | awk '{ if ($1 >= 5) print "PASS"; else print "FAIL count=" $1 }' | grep -q PASS && grep -q 'var(--pv-hue' /home/ubuntu/skynet/src/ui/features/pretty-conversations/pretty-conversations.css && grep -q 'var(--color-pv-fg' /home/ubuntu/skynet/src/ui/features/pretty-conversations/pretty-conversations.css && grep -q '\.pv-row\.selected' /home/ubuntu/skynet/src/ui/features/pretty-conversations/pretty-conversations.css && grep -q '\.pv-row\.pinned' /home/ubuntu/skynet/src/ui/features/pretty-conversations/pretty-conversations.css && grep -q '\.pv-row\.active-set' /home/ubuntu/skynet/src/ui/features/pretty-conversations/pretty-conversations.css && grep -q '\.pv-row\.ambient' /home/ubuntu/skynet/src/ui/features/pretty-conversations/pretty-conversations.css && grep -q '\.pv-row:not(\.pinned) \.pv-meta \.pv-pin' /home/ubuntu/skynet/src/ui/features/pretty-conversations/pretty-conversations.css && grep -q '\.pv-ready-dot' /home/ubuntu/skynet/src/ui/features/pretty-conversations/pretty-conversations.css && grep -q '\.pv-row--mobile' /home/ubuntu/skynet/src/ui/features/pretty-conversations/pretty-conversations.css && grep -q '\.pv-row--desktop' /home/ubuntu/skynet/src/ui/features/pretty-conversations/pretty-conversations.css && grep -q '\.pv-panel-header' /home/ubuntu/skynet/src/ui/features/pretty-conversations/pretty-conversations.css && grep -qE '^\s*import\s+.*pretty-conversations\.css' /home/ubuntu/skynet/src/main.tsx && ! grep -q 'body\[data-intensity="reduced"\]' /home/ubuntu/skynet/src/ui/features/pretty-conversations/pretty-conversations.css && ! grep -q 'body\[data-density=' /home/ubuntu/skynet/src/ui/features/pretty-conversations/pretty-conversations.css && cd /home/ubuntu/skynet && npx tsc --noEmit 2>&1 | tail -5 | grep -qE 'error|Error' && echo FAIL_TSC || echo PASS_TSC | grep -q PASS_TSC</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/features/pretty-conversations/pretty-conversations.css` exists
    - Contains at least 5 `.pv-row` selectors (base + selected + pinned + working + active-set + rdp + ambient)
    - Every hue-driven hsla() uses `var(--pv-hue)` — grep count of `var(--pv-hue` >= 20 (base + selected + pinned dot + ambient + variants)
    - References palette tokens by var (grep of `var(--color-pv-fg` returns >= 1; `var(--color-pv-surface-quiet` returns >= 1)
    - Contains `.pv-row:not(.pinned) .pv-meta .pv-pin { display: none }` (the mock's SHAPE-03 hide rule — Wave 3 plan will consume)
    - Contains `.pv-row.active-set:not(.working) .pv-ready-dot { display: block }` (the mock's SHAPE-01 dot-visibility rule)
    - Contains `.pv-row--mobile` and `.pv-row--desktop` density variants (min-height 72px and 62px respectively)
    - Contains `.pv-panel`, `.pv-panel-header`, `.pv-panel-header .pv-title`, `.pv-panel-header .pv-pencil` (declared in this Wave-1 file for Wave-2 consumption)
    - `.pv-panel-header` declaration includes `justify-content: space-between` (so Wave 2 does not need to augment the CSS)
    - `.pv-panel-header .pv-title` includes `font-size: 12px`, `font-weight: 700`, `letter-spacing: 0.1em`, `text-transform: uppercase`
    - `.pv-panel-header .pv-pencil` includes `width: 32px`, `height: 32px`, `border-radius: 8px`, `background: transparent`, `border: 1px solid transparent`, `cursor: pointer`, `user-select: none`, `-webkit-tap-highlight-color: transparent`
    - `.pv-panel-header .pv-pencil svg` block includes `width: 18px` and `height: 18px`
    - Does NOT contain `body[data-intensity=` or `body[data-density=` — reduced/selection-only/density variants NOT ported
    - `src/main.tsx` imports the CSS file exactly once
    - `npx tsc --noEmit` exits 0 after the commit
    - No source edits under `src/ui/features/pretty-view/`, `src/ui/components/`, `src/ui/ssh/`, `src/ui/features/terminal/`
    - No `--color-pv-*` token re-declaration (the CSS file references them by var, does NOT define them)
  </acceptance_criteria>
  <done>pretty-conversations.css exists with all mock selectors lifted + prefixed + palette-tokenized + ambient-scope-simplified + variant-density-added; src/main.tsx imports it; tsc-clean; atomic commit landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite PrettyConversationRow.tsx to emit mock's semantic markup with class-toggle state variants + rewrite PrettyConversationRow.test.tsx to assert new class-based rendering</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationRow.tsx, src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx, src/ui/features/pretty-conversations/tokens.ts</files>
  <behavior>
    - Rendering: root div is `<div className={cn('pv-row', variantClass, selected && 'selected', inActiveSet && 'active-set', isWorking && 'working', pinned && 'pinned', isAmbient && 'ambient', isRdp && 'rdp')} style={{ '--pv-hue': hue ?? undefined }}>` — state variants are CSS classes, hue is the only inline style
    - variantClass is `pv-row--mobile` when variant==='mobile', `pv-row--desktop` when variant==='desktop'
    - Body markup follows mock structure: `<div className='pv-avatar'>...</div><div className='pv-body'><span className='pv-label'>{row.label}</span>{row.host && <span className='pv-host'>...</span>}</div><div className='pv-meta'>...</div>`
    - Meta children order (per mock): PinAction (both variants when non-RDP), then ready-dot span — same conditional gates as before
    - Test 1 (selected-row hue treatment): assert root has `pv-row` and `selected` classes AND inline style contains `--pv-hue: 216` (or the current fixture hue value)
    - Test 2 (mobile swipe past 40px opens): unchanged behavior — assert `data-swiped-open='true'` after gesture
    - Test 3 (mobile swipe under threshold snaps closed): unchanged behavior
    - Test 4 (vertical gesture > 12px yields): unchanged
    - Test 5 (tap on swiped-open row closes): unchanged
    - Test 6 (tap on closed row fires onSelect): unchanged
    - Test 7 (RDP row has no swipe strip and no PinAction): assert root class contains `rdp` AND no `[data-testid=pin-action]` present
    - Test 8 (desktop pin e.stopPropagation): unchanged
    - Test 9 (no-identity avatar tabIcon fallback): assert `.pv-avatar` contains an svg (from tabIcon helper)
    - Test 10 (pinned desktop always shows pin): assert root class contains `pinned` AND `[data-testid=pin-action]` present in the DOM (visibility now driven by CSS `.pv-row:not(.pinned) .pv-meta .pv-pin` — TEST DOES NOT assert visibility since CSS pseudo-selectors don't run in jsdom; test only asserts markup presence + `pinned` class, which is the reliable jsdom signal)
    - Test 11 (no identity-chip in DOM): unchanged
    - Test 12 (unselected non-RDP row with hue renders full hue-bubble): assert root class contains neither `selected` nor `ambient` AND inline style contains `--pv-hue: {hue}`
    - Test 13 (inActiveSet+isWorking===false renders ready-dot): assert `[aria-label=ready]` span present under `.pv-meta` — same as before
    - Test 14 (RDP row + inActiveSet+isWorking===false uses neutral ready-dot fill): assert `[aria-label=ready]` span exists (CSS handles color); with `.rdp` class on root, the ready-dot span may or may not render depending on CSS `.pv-row.active-set:not(.working) .pv-ready-dot { display: block }` — since RDP rows carry `isWorking={null}` per PrettyConversationsPanel, the test remains: assert NO ready-dot for the isWorking=null case (Test 16 covers this); for Test 14, adjust: assert that WHEN isWorking===false AND inActiveSet===true AND row is RDP, ready-dot IS in DOM (component-level render), but real-app RDP rows never satisfy this because Panel passes isWorking=null — document this discrepancy inline as a jsdom-vs-real-app note
    - Test 15 (inActiveSet+isWorking===true renders NO ready-dot): assert `[aria-label=ready]` NOT in DOM AND root has `working` class
    - Test 16 (inActiveSet+isWorking===null renders NO ready-dot): assert `[aria-label=ready]` NOT in DOM
    - Test 17 (!inActiveSet+isWorking===false renders NO ready-dot): assert `[aria-label=ready]` NOT in DOM
    - Test 18 (!inActiveSet && !isRdp renders ambient): assert root class contains `ambient` — the CSS handles the flat-hsla background; test asserts CLASS PRESENCE not computed style
    - Test 18b (RDP row exempt from ambient): assert root class contains `rdp` and does NOT contain `ambient`
    - All 18 (+ 18b) tests pass
  </behavior>
  <read_first>
    - /home/ubuntu/skynet/src/ui/features/pretty-conversations/PrettyConversationRow.tsx (the current 709-line implementation being replaced — read the prop shape, identity-resolution block, swipe state machine, ready-dot render — everything staying stays as-is)
    - /home/ubuntu/skynet/src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx (the current 18-test suite — every test's fixture setup + assertion shape is what the rewrite matches; the mocks at top of file are unchanged)
    - /home/ubuntu/skynet/src/ui/features/pretty-conversations/pretty-conversations.css (from Task 1 — the CSS file whose selectors this component targets)
    - /home/ubuntu/.claude/identities/tina/bounties/skynet-transformation/prototype.html (the semantic markup structure — read lines 572-703, the panel + rows body, to see exact DOM shape)
    - /home/ubuntu/skynet/src/ui/features/pretty-conversations/tokens.ts (only retain swipe constants: PC_SWIPE_REVEAL, PC_SWIPE_THRESHOLD, PC_SWIPE_ANGLE_TOLERANCE — retire PC_ROW_MIN_H_MOBILE and PC_ROW_MIN_H_DESKTOP since CSS handles those now)
    - /home/ubuntu/skynet/src/lib/utils.ts (find the `cn` helper — imported here; if none exists, use `clsx` if in package.json, else concatenate strings manually)
  </read_first>
  <action>
Rewrite `src/ui/features/pretty-conversations/PrettyConversationRow.tsx` with the mock's semantic markup + class-toggle state variants. The new file is ~450 lines (down from 709). Layout:

**Imports:** Preserve `useCallback, useRef, useState, KeyboardEvent, MouseEvent, TouchEvent` from react. Keep `Pin, Server` from lucide-react. Keep `tabIcon`, `sessionMatchKey`, `useIdentities`, `ConversationRowShape`, `PinAction`, `PC_SWIPE_REVEAL, PC_SWIPE_THRESHOLD, PC_SWIPE_ANGLE_TOLERANCE`. Add `cn` helper import if it exists at `@/lib/utils` (grep first — if absent, write a local `cn(...parts: (string | false | undefined | null)[]): string { return parts.filter(Boolean).join(' '); }` helper at the top of the file).

**Retire `CSSProperties` type import** if no longer needed — only remaining inline style is `{ ['--pv-hue']: hue }` which is CSSProperties with a string-indexed extension. Cast as needed via `as unknown as CSSProperties` or `style={ ...({ '--pv-hue': hue } as Record<string, string | number>) }`.

**Prop shape preserved verbatim:** Row, selected, pinned, variant, onSelect, onTogglePin, onSwipeOpenChange, forceClosed, isWorking, inActiveSet — same signatures, same defaults.

**Identity resolution preserved verbatim:** identitiesByKey, key, identity, hue, isRdp, isAmbient — same derivations.

**Swipe state machine preserved verbatim:** swipedOpen, dxLive, startXRef, startYRef, activeRef, baseDxRef, effectiveOpen, emitSwipeOpenChange, onTouchStart, onTouchMove, onTouchEnd, onBodyClick, onBodyKeyDown, onPinClick — all unchanged.

**New: class composition.** Compute className string via `cn`:
```
const variantClass = variant === 'mobile' ? 'pv-row--mobile' : 'pv-row--desktop';
const rowClassName = cn(
  'pv-row',
  variantClass,
  selected && 'selected',
  inActiveSet && 'active-set',
  isWorking === true && 'working',
  pinned && 'pinned',
  isAmbient && 'ambient',
  isRdp && 'rdp',
);
```
(DO NOT put fenced code in the executor's action — this is planner-side spec. Above is the concept the executor implements.)

**New: hue custom property.** The ONLY inline style on `.pv-row` is `{ '--pv-hue': hue }` for hue-bearing rows. When hue is null, omit the style property entirely (CSS uses fallback values or the .ambient/.rdp branches don't reference --pv-hue for their backgrounds — verify against Task 1's CSS file: neutral rows and RDP rows should use non-hue backgrounds in CSS).

**New: markup structure follows the mock exactly.**
  - Wrapper div stays `<div className='relative overflow-hidden' data-conversation-id={row.id} ...>` for mobile (so swipe transform is clipped); desktop wrapper is `<div className='relative'>` — same swipe-strip wrapper needs
  - Mobile swipe-reveal strip: unchanged — `{isMobile && !isRdp && <div style={{ width: PC_SWIPE_REVEAL + 'px' }} ...><PinAction ... /></div>}`
  - Row body: `<div role='button' tabIndex={0} className={rowClassName} style={{ '--pv-hue': hue }} onClick={onBodyClick} ...>` — no Tailwind layout classes on this div, CSS handles everything
  - Avatar: `<div className='pv-avatar' data-testid='pcrow-avatar'>{identity ? (identity.avatarUrl ? <img ... /> : <span>{initial}</span>) : <span className='pv-avatar-fallback-icon'>{tabIcon(row.type)}</span>}</div>` — retain `data-testid='pcrow-avatar'` for existing tests
  - Body: `<div className='pv-body'><span className='pv-label'>{row.label}</span>{row.host && <span className='pv-host'><Server className='pv-host-icon' aria-hidden='true' /> {row.host.name}</span>}</div>`
  - Meta: `<div className='pv-meta'>{pinned && <Pin className='pv-pin' aria-hidden='true' />}{!isMobile && !isRdp && <PinAction hue={hue} pinned={pinned} size='desktop' onClick={onPinClick} />}{inActiveSet && isWorking === false && <span aria-label='ready' data-pv-conv-ready-dot='true' className='pv-ready-dot' />}</div>`

**Note on the ready-dot render conditional:** The CSS rule `.pv-row.active-set:not(.working) .pv-ready-dot { display: block }` uses CSS to hide/show. But we ALSO gate at JS level so the DOM never contains a ready-dot span when `isWorking` is null/true — this preserves current test expectations (Tests 15, 16, 17). The double gate is intentional: JS gate is stricter than CSS (isWorking null vs isWorking===false). The CSS gate handles the `.active-set:not(.working)` combination and provides a visibility escape hatch if the JS gate ever regresses.

**Mobile swipe transform:** Keep the transform-based swipe reveal. Because CSS-only cannot express dynamic dx during a gesture, the swipe transform stays inline: `style={{ ...({'--pv-hue': hue} as any), transform: 'translateX({targetDx}px)', transition: ... }}`. This is the ONE remaining inline style on `.pv-row` for mobile variant. Document this in a comment.

**Retire completely:** rowMinH, rowPadding, rowGap, avatarSize, avatarInnerIconSize, avatarInitialTextSize, labelTextSize, hostTextSize, hostIconSize, pinGlyphSize, line1Gap, line2Gap, avatarStyle, gradC1/C2/borderRgba/hairlineRgba/glowRgba computations, ambientBase, fullBubbleBase, baseBodyStyle, selectedOverlay, hoverOverlay, ambientHoverOverlay, fullBubbleHoverOverlay, hoverBorderColor, hoverHairline, hoverGlow, hovered state, desktopBodyTransition, wrapperClass, bodyBaseClass, desktopPinVisibilityClass. All of the above become CSS in pretty-conversations.css. The line count drop is ~250 lines removed.

**Retire hover state management:** No more `const [hovered, setHovered] = useState(false)`. CSS `:hover` handles hover natively. Remove onMouseEnter/onMouseLeave handlers.

**Retire desktopPinVisibilityClass:** No more `opacity-0 group-hover/pcrow:opacity-100` — CSS `.pv-row:not(.pinned) .pv-meta .pv-pin { display: none }` handles hide-when-not-pinned. Desktop hover-reveal of the PinAction (for UNpinned rows) is achieved via CSS: `.pv-row.pv-row--desktop:not(:hover):not(.pinned) .pv-meta [data-testid=pin-action] { display: none }` — declare THIS additional rule in pretty-conversations.css if you did not include it in Task 1 (Wave 3 plan 13-03 will refine — but Task 1's CSS already contains `.pv-row:not(.pinned) .pv-meta .pv-pin { display: none }` which hides the PIN GLYPH; the hover-reveal PinAction is a separate element and needs its own hide rule). **Add** to Task 1's CSS OR add here as a stopgap — Wave 3 plan 13-03 finalizes the pin visibility rules. If you add here, do it in a separate atomic commit as part of Task 2's markup edit.

**Update tokens.ts:** Retire `PC_ROW_MIN_H_MOBILE` and `PC_ROW_MIN_H_DESKTOP` — no longer referenced. Keep `PC_SWIPE_REVEAL`, `PC_SWIPE_THRESHOLD`, `PC_SWIPE_ANGLE_TOLERANCE` (still driving the swipe state machine). Grep the repo for uses of `PC_ROW_MIN_H_MOBILE` and `PC_ROW_MIN_H_DESKTOP` before deleting — if only PrettyConversationRow.tsx references them, safe to delete.

**Rewrite PrettyConversationRow.test.tsx (18 tests + 18b):** Follow the behavior contract above. Every test that used to inspect `body.getAttribute("style")` for JS-computed inline hsla values now inspects `body.className` for the class toggle. Tests using `[role="button"]` for the row body stay — the role is preserved. Update the header comment block to explain the migration (from inline-style assertions to class-based assertions).

Specifically for each test:
  - Test 1: was `expect(rawStyle).toContain('hsla(216, 70%, 60%')` — now `expect(body.className).toContain('selected')` AND `expect(body.getAttribute('style')).toContain('--pv-hue: 216')`
  - Tests 2-6, 8, 11: same behavior, adapt DOM query if `.querySelector('[role="button"]')` selector needs adjustment (probably unchanged)
  - Test 7 (RDP no swipe strip + no pin): `expect(body.className).toContain('rdp')` AND `expect(wrapper.querySelector('[data-testid="pin-action"]')).toBeNull()`
  - Test 9 (no-identity avatar fallback): `expect(wrapper.querySelector('.pv-avatar')).toBeTruthy()` AND `expect(wrapper.querySelector('.pv-avatar svg')).toBeTruthy()` (or by testid)
  - Test 10 (pinned desktop pin present in DOM): the CSS-based hide rule doesn't run in jsdom so the `[data-testid="pin-action"]` element IS in the DOM regardless of pinned; adjust assertion to check the row has the `pinned` class AND that pin-action exists — same signal as before (was: opacity-100). If Test 10 relied on visibility-of-pin-button-as-proxy-for-pinned-class, adjust to check className directly (the reliable jsdom signal)
  - Tests 12-17: use `body.className.includes('...')` for the state class the test asserts about
  - Test 18 (ambient class present): `expect(body.className).toContain('ambient')` — replaces the pre-migration assertion on flat-hsla-background inline style
  - Test 18b (RDP exempt from ambient): `expect(body.className).toContain('rdp')` AND `expect(body.className).not.toContain('ambient')`

**Atomic commits:**
  1. `feat(phase-13): rewrite PrettyConversationRow with mock's semantic markup + class-toggle state variants` — the source file rewrite
  2. `test(phase-13): update PrettyConversationRow tests to assert class-based rendering` — the test file rewrite
  3. `refactor(phase-13): retire unused row-min-height layout tokens` — the tokens.ts cleanup (only if the delete is safe — grep first)

After all commits: `npx tsc --noEmit` exits 0 AND `npx vitest run src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` all green.

**Do NOT touch** `src/ui/features/pretty-view/`, `src/ui/components/`, `src/ui/ssh/`, `src/ui/features/terminal/`, `src/ui/features/pretty-conversations/PinAction.tsx` (Wave 3 owns it), `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` (Wave 2 owns it). If the executor sees a temptation to fix PinAction's `text-muted-foreground/60` hits in this task, RESIST — Wave 3 plan 13-03 owns PinAction.
  </action>
  <verify>
    <automated>test -f /home/ubuntu/skynet/src/ui/features/pretty-conversations/PrettyConversationRow.tsx && grep -c 'className=' /home/ubuntu/skynet/src/ui/features/pretty-conversations/PrettyConversationRow.tsx | awk '{ if ($1 >= 3) print "PASS"; else print "FAIL" }' | grep -q PASS && grep -qE "['\"]pv-row['\"]" /home/ubuntu/skynet/src/ui/features/pretty-conversations/PrettyConversationRow.tsx && grep -qE "['\"]pv-avatar['\"]" /home/ubuntu/skynet/src/ui/features/pretty-conversations/PrettyConversationRow.tsx && grep -qE "['\"]pv-body['\"]" /home/ubuntu/skynet/src/ui/features/pretty-conversations/PrettyConversationRow.tsx && grep -qE "['\"]pv-meta['\"]" /home/ubuntu/skynet/src/ui/features/pretty-conversations/PrettyConversationRow.tsx && grep -qE "['\"]pv-label['\"]" /home/ubuntu/skynet/src/ui/features/pretty-conversations/PrettyConversationRow.tsx && grep -qE "['\"]pv-host['\"]" /home/ubuntu/skynet/src/ui/features/pretty-conversations/PrettyConversationRow.tsx && grep -qE '(?:selected|active-set|working|pinned|ambient|rdp)' /home/ubuntu/skynet/src/ui/features/pretty-conversations/PrettyConversationRow.tsx && grep -q "'--pv-hue'" /home/ubuntu/skynet/src/ui/features/pretty-conversations/PrettyConversationRow.tsx && ! grep -v '^\s*//' /home/ubuntu/skynet/src/ui/features/pretty-conversations/PrettyConversationRow.tsx | grep -qE 'text-muted-foreground/60|hover:text-foreground' && grep -c '^' /home/ubuntu/skynet/src/ui/features/pretty-conversations/PrettyConversationRow.tsx | awk '{ if ($1 <= 550) print "PASS_LINES"; else print "FAIL_LINES=" $1 }' | grep -q PASS_LINES && cd /home/ubuntu/skynet && npx tsc --noEmit 2>&1 | tail -5 > /tmp/tsc.out && ! grep -qE 'error TS' /tmp/tsc.out && npx vitest run src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx 2>&1 | tail -20 | grep -qE 'PASS|passed'</automated>
  </verify>
  <acceptance_criteria>
    - `PrettyConversationRow.tsx` file exists, line count 350-550 (down from 709)
    - Emits `pv-row`, `pv-avatar`, `pv-body`, `pv-label`, `pv-host`, `pv-meta` class names in JSX
    - Root row div uses className string composition with `selected`, `active-set`, `working`, `pinned`, `ambient`, `rdp` state toggles — verify grep shows all six state class-name string literals
    - Root row div carries `--pv-hue` CSS custom property inline for hue-bearing rows
    - No `bg-background`, `bg-card`, `text-foreground`, `border-border`, `text-muted-foreground/60`, `hover:text-foreground` in code lines (comments allowed)
    - No JS-computed inline styles for base row body / avatar / ambient / selected / hover overlays — the ONLY inline style on `.pv-row` is `--pv-hue` (mobile also spreads `transform: translateX(...)` for the swipe, which is the documented exception)
    - Retire JS `useState` hover branch — no `const [hovered, setHovered]` remains
    - `PrettyConversationRow.test.tsx` all 18+ tests pass — `npx vitest run src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` exit 0
    - `npx tsc --noEmit` exits 0 (no type errors)
    - No edits under `src/ui/features/pretty-view/`, `src/ui/components/`, `src/ui/ssh/`, `src/ui/features/terminal/`, `src/ui/features/pretty-conversations/PinAction.tsx`, `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`
    - tokens.ts still exports `PC_SWIPE_REVEAL`, `PC_SWIPE_THRESHOLD`, `PC_SWIPE_ANGLE_TOLERANCE`; PC_ROW_MIN_H_* removed only if no consumers remain (grep-verified)
    - 3 atomic commits landed (source, test, tokens) — verify via `git log --oneline -5`
  </acceptance_criteria>
  <done>PrettyConversationRow.tsx rewritten with mock's semantic markup + class-toggle state variants; JS-computed inline styles retired; hover state retired; all 18+ tests updated and passing; tsc-clean; tokens.ts pruned; atomic commits landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mock v4 CSS → pretty-conversations.css | Ashley signed off on the mock 2026-07-23. The lifted CSS is the visual truth. Any transformation of a mock value (rename, palette-token substitute, ambient-scope simplification) risks divergence — mitigated by lifting selectors verbatim and using palette tokens that already produce the mock's exact hex values in the live app. |
| PrettyConversationRow.tsx → CSS class-toggle state | The row's rendered visual state is now driven by the CSS class it applies + the CSS file's selector cascade. If the class-toggle logic in JS diverges from `isAmbient`, `selected`, `inActiveSet`, `isWorking`, `pinned`, `isRdp` conditions, the visual state disagrees with the store state. Mitigated by preserving the existing derivation logic verbatim and only changing the style-emission mechanism. |
| Test file → jsdom render vs real-browser render | jsdom does not run CSS pseudo-selectors like `:hover` or apply `display: none` from stylesheets. Tests that used to assert visibility-by-opacity now assert class-presence-as-proxy. This is a documented limitation, not a regression — real-browser UAT (Wave 3 plan 13-04) covers the CSS-driven visibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | Tampering (mock value drift during lift) | pretty-conversations.css | mitigate | Lift selectors verbatim from prototype.html lines 219-405 (row, avatar, body, meta, ready-dot, RDP, ambient blocks). Rename `.row` → `.pv-row` and `--hue` → `--pv-hue` but preserve every numeric value (padding, radius, hsla percentages, alpha, blur strength, shadow offsets, filter values). |
| T-13-01-02 | Information Disclosure | class-name collision | accept | The `.pv-*` prefix is idempotent — no other file in the fork uses this prefix. Grep-verified: `grep -rn 'pv-row\\|pv-avatar' src/ | grep -v pretty-conversations | wc -l` returns 0 before Task 1. If a collision surfaces, add a longer prefix (`.pvconv-*`). |
| T-13-01-03 | Denial of Service (row visual regression under one specific hue value) | class-toggle CSS | mitigate | Task 2 tests parameterize over multiple hue values (fixture uses 216 primarily, some tests use null). Real-app UAT (Wave 3 plan 13-04) walks Ashley through 3+ active sessions with distinct hues — visual regression at any single hue surfaces. |
| T-13-01-04 | Repudiation (which layer owns the ambient recession) | dual-gate: JS isAmbient class + CSS ambient block | mitigate | The class-toggle IS the ambient signal; the CSS block IS the visual response. Neither layer alone applies ambient. If a bug reproduces in production, either the JS class-toggle is missing (grep source) or the CSS block is missing (grep CSS file). Comment blocks in both files reference each other. |
| T-13-01-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this plan — CSS file is pure text; class-name composition uses existing lucide-react + react + optional cn helper. No supply-chain surface. |
</threat_model>

<verification>
- pretty-conversations.css exists at expected path with expected selectors (grep gates in Task 1 verify)
- PrettyConversationRow.tsx line count in target range (350-550) — grep gates in Task 2 verify
- Zero JS-computed inline hsla for base row body — grep for `hsla(\$\{hue\}` in the source file returns 0 hits in non-comment lines
- All 18+ tests pass — vitest gate in Task 2 verifies
- `npx tsc --noEmit` exits 0 — tsc gate in both tasks verifies
- No edits under scope-locked directories — spot-check via `git diff --stat` reveals only files in `src/ui/features/pretty-conversations/` and `src/main.tsx`
- CSS import wired in main.tsx — grep gate in Task 1 verifies
- Ambient class-toggle logic preserved verbatim — spot-check `isAmbient = !isRdp && !inActiveSet` line in Task 2's rewritten file
- `--pv-hue` custom property emitted for hue-bearing rows — grep gate in Task 2 verifies
</verification>

<success_criteria>
- SHAPE-01 satisfied: PrettyConversationRow.tsx renders using pretty-conversations.css whose selectors match the mock's structure; state variants are class toggles, not JS-computed styles
- Ambient recession values match the mock (not the current over-recessed 0.16 alpha) — verified by CSS diff against prototype.html
- All existing behavior preserved: swipe reveal, ready-dot conditional, avatar image src selection, click handlers, e.stopPropagation
- Test suite green — no regression from the rewrite
- The row line count roughly halves (709 → ~450), evidence of successful JS-inline-style retirement
- Wave 2 (Panel + AppShell) and Wave 3 (PinAction) can proceed from this foundation
</success_criteria>

<output>
Create `.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-01-SUMMARY.md` when done.
</output>
