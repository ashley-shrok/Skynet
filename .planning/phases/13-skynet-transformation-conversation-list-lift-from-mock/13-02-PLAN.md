---
phase: 13-skynet-transformation-conversation-list-lift-from-mock
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
  - src/ui/AppShell.tsx
autonomous: true
requirements:
  - SHAPE-02
  - SHAPE-04
tags: [ui, css, pretty-conversations, lift-from-mock, panel, appshell, shell-chrome]

must_haves:
  truths:
    - "`PrettyConversationsPanel.tsx` header row uses the mock's `.pv-panel-header` treatment: 14px 16px padding, hairline `border-bottom: 1px solid --color-pv-border-quiet`, layout via CSS from Wave-1's pretty-conversations.css"
    - "Header title (desktop variant) uses the mock's `.pv-panel-header .pv-title` treatment: 12px font-size + 700 font-weight + `letter-spacing: 0.1em` + UPPERCASE (text-transform) + `--color-pv-fg` color — NO more 13px + weight-600 + tight-tracking + mixed-case (retired filled-glass-chunky treatment)"
    - "Pencil button (both variants when onCreateSession is wired) uses the mock's `.pv-panel-header .pv-pencil` treatment: 32x32 dimensions + transparent bg + transparent border + `border-radius: 8px` + `--color-pv-fg-muted` icon color — NO more `w-[34px] h-[34px] rounded-full bg-white/[0.04] border border-white/[0.09]` filled-glass-pill treatment"
    - "AppShell.tsx sidebar-toggle chevron area (currently ~L1400-1479) uses only `--color-pv-*` palette tokens for color decisions — no `bg-background`, `bg-card`, `text-foreground`, `text-muted-foreground`, `bg-[rgba(20,22,28,0.85)]` Skynet-theme values on the chevron's own render or its immediate container"
    - "AppShell chevron button visual treatment matches the mock's transparent-icon-with-rounded-md button aesthetic (32x32, transparent bg + border, border-radius 8px, --color-pv-fg-muted icon color) — no filled-glass pill on the chevron button; hover uses `.pv-panel-header .pv-pencil:hover` treatment (rgba(220,225,245,0.06) bg tint)"
    - "The `min-h-0` + `overflow-hidden` scroll-region structure of PrettyConversationsPanel.tsx is preserved verbatim (do not touch the scroll region body — that's Wave 3 or a future safe-area investigation)"
    - "The panel container itself carries `pv-panel` class OR the panel container class treatment references the mock's `.pv-panel` gradient + border + backdrop-blur + shadow — either via CSS class OR retain the current wrapper structure and only change the header (Claude's discretion — CONTEXT.md says panel container can stay with current AppShell-side treatment); at minimum SHAPE-02 requires the HEADER changes, panel-container is a nice-to-have that this plan MAY include if executor judges it safe"
    - "PrettyConversationsPanel.test.tsx is updated to assert new header markup: title has class `pv-title`, pencil button has class `pv-pencil`, header row has class `pv-panel-header` — replacing the previous assertions on `text-[13px] font-semibold` and `bg-white/[0.04] rounded-full` visual classes"
    - "`npx tsc --noEmit` exits 0 after each atomic commit"
    - "`npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` all green after each atomic commit"
    - "AppShell.tsx edits are STRICTLY scoped to the sidebar-toggle chevron button area (currently lines 1400-1479 — the `{!isMobileListScreen && (<button ...>)}` block). No other AppShell edits — no tab state changes, no keyboard handler changes, no WebSocket lifecycle changes"
    - "No edits under `src/ui/features/pretty-view/`, `src/ui/components/`, `src/ui/ssh/`, `src/ui/features/terminal/` — SHAPE-06 scope boundary"
    - "No edits to `src/ui/features/pretty-conversations/PrettyConversationRow.tsx` (Wave 1 owns it) or `src/ui/features/pretty-conversations/PinAction.tsx` (Wave 3 owns it) — parallel-safety guarantee"
  artifacts:
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "Panel container with rewritten header (title + pencil) using mock's class-toggle treatment; scroll region + empty state + NewSessionDialog wiring preserved verbatim"
      contains: "pv-panel-header"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx"
      provides: "Updated tests asserting new header markup structure (pv-panel-header, pv-title, pv-pencil classes)"
      contains: "pv-panel-header"
    - path: "src/ui/AppShell.tsx"
      provides: "Sidebar-toggle chevron area rebased to --color-pv-* palette + mock's transparent-icon-rounded-md button treatment"
      contains: "--color-pv"
  key_links:
    - from: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      to: "src/ui/features/pretty-conversations/pretty-conversations.css"
      via: "className='pv-panel-header'/'pv-title'/'pv-pencil' — consumes selectors declared in Wave 1's CSS file"
      pattern: "pv-panel-header|pv-title|pv-pencil"
    - from: "src/ui/AppShell.tsx"
      to: "src/ui/index.css --color-pv-* tokens"
      via: "chevron button uses --color-pv-fg-muted for icon color, --color-pv-border-quiet for hover border, etc."
      pattern: "--color-pv-|color-pv-fg-muted"
---

<objective>
Rewrite the PrettyConversationsPanel's header row to use the mock v4's `.panel-header` + `.title` + `.pencil` class-toggle treatment (SHAPE-02), and rebase the AppShell.tsx sidebar-toggle chevron area (the top-left persistent chevron introduced in Phase 10 Wave 3 at ~L1407) to use `--color-pv-*` palette tokens + the mock's transparent-icon-with-rounded-md button aesthetic (SHAPE-04). Ashley called this session verbatim: "the bar at the top that says like conversations or something that to me looks like it's coming out of old Skynet stuff" — that's the panel header's current 13px mixed-case chunky title + filled-glass pencil pill; and "the bar at the top that says the name of the session still looks Skynet" — that's the AppShell shell chrome around the sidebar-toggle chevron (NOT the pretty-view IdentityBadge interior which is locked out of scope per SHAPE-06).

Purpose: Implements SHAPE-02 (panel header lift) and SHAPE-04 (shell chrome around sidebar-toggle) from REQUIREMENTS.md § "Skynet Shape Completion — Conversation List + Shell Chrome Lift-from-Mock (Phase 13)". This plan runs in parallel with 13-03 (PinAction rewrite) because their `files_modified` are disjoint. Both depend on Wave 1 (13-01) which declared the CSS file's `.pv-panel-header` / `.pv-title` / `.pv-pencil` selectors that this plan consumes.

Output: rewritten PrettyConversationsPanel.tsx (only the header — scroll region + empty state + NewSessionDialog wiring preserved), updated PrettyConversationsPanel.test.tsx (header assertions updated), scoped edit to AppShell.tsx sidebar-toggle chevron block only. Delivered as 2-3 atomic commits, tsc-clean + vitest-green per commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-CONTEXT.md
@.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-01-SUMMARY.md
@src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
@src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
@src/ui/features/pretty-conversations/pretty-conversations.css
@src/ui/AppShell.tsx
@src/ui/index.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite PrettyConversationsPanel header (title + pencil) to mock's class-toggle treatment; update panel tests to assert new markup</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx, src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx</files>
  <behavior>
    - Header row div uses `className='pv-panel-header'` — CSS handles layout (display: flex; align-items: center; padding: 14px 16px; border-bottom: 1px solid var(--color-pv-border-quiet))
    - Header row still branches on `variant` for the title: `variant='desktop'` renders the title span; `variant='mobile'` renders an empty aria-hidden span (justify-between right-anchors the pencil per mock note)
    - Title span uses `className='pv-title'` — CSS handles typography (font-size: 12px; font-weight: 700; letter-spacing: 0.1em; text-transform: uppercase; color: var(--color-pv-fg))
    - Pencil button uses `<button type='button' className='pv-pencil' aria-label={newSessionLabel} title={newSessionLabel} onClick={() => setNewSessionDialogOpen(true)}>` — CSS handles dimensions (32x32) + transparent bg + transparent border + border-radius 8px + hover treatment
    - Pencil icon is `<Pencil />` from lucide-react at size 18 (mock spec width/height 18) — CSS `color: var(--color-pv-fg-muted)` on `.pv-pencil` cascades into currentColor for the SVG stroke
    - Scroll region body: UNCHANGED. `<div className='flex-1 min-h-0 overflow-y-auto flex flex-col'>` and the empty state + pinned map + grouped map + RDP divider + NewSessionDialog stay as-is
    - The outer container `<div className='relative flex flex-col flex-1 min-h-0 overflow-hidden pb-[env(safe-area-inset-bottom)]' data-testid='pretty-conversations-panel'>` stays UNCHANGED
    - Existing 15-item test suite continues to pass; header-specific tests are updated per below
    - Panel test: header markup — assert `[data-testid='pretty-conversations-panel'] .pv-panel-header` present; when variant='desktop', assert `.pv-title` contains text 'Conversations'; when variant='mobile', assert `.pv-title` NOT present in DOM (empty aria-hidden span instead)
    - Panel test: pencil button — assert `[aria-label='New session']` has className containing 'pv-pencil'; retire the previous assertion about `w-[34px] h-[34px] rounded-full bg-white/[0.04]`
    - Panel test: gear-desktop-only and settingsRowSlot-mobile-bottom assertions — those were retired earlier phases; verify they are absent from the file and do NOT re-add
    - Panel test: dispatcher routing (onRdpRowClick, onDetachedRowClick, selectConversation fallback) — UNCHANGED, tests pass
    - Panel test: swipe coordination + forceClosed prop plumbing — UNCHANGED, tests pass
  </behavior>
  <read_first>
    - /home/ubuntu/skynet/src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (current 429-line implementation — read especially the header block at lines 231-273 which is what changes; scroll region + empty state + NewSessionDialog wiring at lines 275-427 stays as-is)
    - /home/ubuntu/skynet/src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx (current 15-test suite — the header assertions get updated; dispatcher + swipe coordination tests stay)
    - /home/ubuntu/skynet/src/ui/features/pretty-conversations/pretty-conversations.css (from Wave 1 — the file declaring `.pv-panel-header`, `.pv-title`, `.pv-pencil` selectors that this task consumes; verify those selectors exist before starting)
    - /home/ubuntu/.claude/identities/tina/bounties/skynet-transformation/prototype.html (mock — lines 163-199 declare `.panel`, `.panel-header`, `.panel-header .title`, `.panel-header .pencil`, `.panel-header .pencil:hover`, `.panel-scroll`; lines 572-578 show semantic markup: `<div class='panel-header'><div class='title'>Conversations</div><div class='pencil' title='New session'><svg .../></div></div>`)
    - /home/ubuntu/skynet/src/ui/index.css (lines 119-146 — palette tokens; the CSS from Wave 1 references these tokens by var())
  </read_first>
  <action>
Rewrite the header block in `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`. Specifically edit lines 231-273 (the current `<div className='shrink-0 flex items-center justify-between px-4 py-3 border-b border-white/[0.06]'>` header container and its children) to the mock's semantic markup with class-toggle treatment.

**New header block replaces the current one:**

The header container div's className becomes `'pv-panel-header shrink-0'` — retain `shrink-0` because it defends against parent-flex shrinking, but the mock's padding/layout/border-bottom come from CSS class `pv-panel-header`. Remove `flex items-center justify-between px-4 py-3 border-b border-white/[0.06]` — those Tailwind utilities become CSS in pretty-conversations.css from Wave 1.

Inside the header:
  - Left slot (title or empty): when `variant === 'desktop'`, emit `<span className='pv-title'>{headerLabel}</span>` where headerLabel is the existing `t("nav.conversations.title", { defaultValue: "Conversations" })`; when `variant === 'mobile'`, emit `<span aria-hidden='true' />` (unchanged from current) — keep justify-between (via CSS `.pv-panel-header` `display: flex; align-items: center; justify-content: space-between` — verify Wave 1 CSS declares justify-content; if not, ADD to Wave 1 CSS via this task's execution or annotate as follow-up)
  - Right slot (pencil): when `showPencilButton`, emit `<button type='button' onClick={() => setNewSessionDialogOpen(true)} aria-label={newSessionLabel} title={newSessionLabel} className='pv-pencil'><Pencil /></button>` — the Pencil icon carries no size utility; CSS `.pv-pencil svg { width: 18px; height: 18px; }` handles size (add to CSS if not present)

**Retire from JSX:**
  - `w-[34px] h-[34px] rounded-full` — CSS handles dimensions + radius
  - `bg-white/[0.04] border border-white/[0.09]` — CSS handles bg + border via `.pv-pencil { background: transparent; border: 1px solid transparent; }` and `:hover { background: rgba(220,225,245,0.06); border-color: var(--color-pv-border-quiet); color: var(--color-pv-fg); }`
  - `text-[#f0eae0]` — CSS handles color via `.pv-pencil { color: var(--color-pv-fg-muted); }` cascading to currentColor for SVG stroke
  - `hover:bg-white/[0.08]` — CSS handles via `.pv-pencil:hover`
  - `transition-colors` — CSS handles via `.pv-pencil { transition: background 120ms ease, color 120ms ease, border-color 120ms ease; }` (declared in Wave 1 CSS)
  - `[-webkit-tap-highlight-color:transparent]` — retain via CSS OR keep inline utility. Recommendation: add to Wave 1 CSS `.pv-pencil { -webkit-tap-highlight-color: transparent; }`. If Wave 1 CSS is complete, either accept the small delta and add via this task's execution OR keep the Tailwind utility on the button as an inline overlay — Claude's discretion, but prefer CSS-only for the header
  - `cursor-pointer select-none` — CSS handles via `.pv-pencil { cursor: pointer; user-select: none; }`
  - `<Pencil className='size-4' />` — retire size-4; CSS handles size via `.pv-pencil svg { width: 18px; height: 18px; }`

**Retain from JSX (unchanged):**
  - `<div className='flex items-center gap-1'>` wrapping the pencil button — retain as-is; the mock declares only a title + pencil, no gap wrapper, but this repo's headers historically wrap right-side controls; retain the wrapper so future additions (any additional right-side control) don't need re-layout. Alternatively, unwrap and put `<button>` directly as sibling of title — Claude's discretion. Simpler is better; unwrap if only one control exists.

**CSS additions needed (if not already in Wave 1 CSS):**
Any of the following that Wave 1 CSS did not include should be added here or (preferably) added to pretty-conversations.css. Add via a separate atomic edit to pretty-conversations.css if needed:
  - `.pv-panel-header { justify-content: space-between; }` — if the mock's original was `display: flex; align-items: center` without justify-content, that was because it only had one child using `flex: 1`. Since we're using two slots (title + pencil) and want the pencil right-anchored regardless of title presence, add justify-content: space-between
  - `.pv-pencil svg { width: 18px; height: 18px; stroke-width: 2; }`
  - `.pv-pencil { cursor: pointer; user-select: none; -webkit-tap-highlight-color: transparent; }` (if not already in Wave 1)

**Update PrettyConversationsPanel.test.tsx** — 15 tests, only header-touching ones change. Rewrite the header assertions:
  - The test that verifies "Conversations" title text on desktop: continue asserting text presence, but also assert `expect(titleElement.className).toContain('pv-title')` — replace prior assertions on `text-[13px]` / `font-semibold` / `tracking-tight`
  - The test that verifies pencil button on both variants: continue asserting `expect(pencilButton.getAttribute('aria-label')).toBe('New session')`, but also assert `expect(pencilButton.className).toContain('pv-pencil')` — replace prior assertions on `w-[34px] h-[34px]` / `rounded-full` / `bg-white/[0.04]`
  - The test that verifies header row structure: continue asserting `[data-testid='pretty-conversations-panel']` root present, adjust any assertion on the header row's className from `border-b border-white/[0.06]` to `.pv-panel-header` (may already have no such assertion — verify by reading the test file)
  - The test that verifies mobile variant emits empty left slot: assert `wrapper.querySelector('.pv-title')` returns null when variant='mobile'
  - Every other test (empty state, ordering, RDP-sentinel, dispatcher routing, swipe coordination, forceClosed) stays UNCHANGED

**Atomic commits:**
  1. `feat(phase-13): rewrite PrettyConversationsPanel header to mock's class-toggle treatment` — source file rewrite
  2. `test(phase-13): update PrettyConversationsPanel header tests to assert pv-panel-header/pv-title/pv-pencil` — test file rewrite
  3. (only if CSS additions needed) `style(phase-13): add justify-content + svg-size rules to pretty-conversations.css .pv-panel-header/.pv-pencil` — CSS augmentation

Post-commits: `npx tsc --noEmit` exits 0; `npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` all green.

**Do NOT touch:**
  - `src/ui/features/pretty-conversations/PrettyConversationRow.tsx` (Wave 1 owns it)
  - `src/ui/features/pretty-conversations/PinAction.tsx` (Wave 3 plan 13-03 owns it — parallel-safety)
  - `src/ui/features/pretty-view/`, `src/ui/components/`, `src/ui/ssh/`, `src/ui/features/terminal/` — SHAPE-06 scope boundary
  - The scroll region body of PrettyConversationsPanel.tsx (lines 275-410 area) — empty state + pinned map + grouped map + RDP divider + NewSessionDialog wiring stay verbatim
  </action>
  <verify>
    <automated>test -f /home/ubuntu/skynet/src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx && grep -qE "['\"]pv-panel-header['\"]" /home/ubuntu/skynet/src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx && grep -qE "['\"]pv-title['\"]" /home/ubuntu/skynet/src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx && grep -qE "['\"]pv-pencil['\"]" /home/ubuntu/skynet/src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx && ! grep -v '^\s*//' /home/ubuntu/skynet/src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx | grep -qE 'w-\[34px\] h-\[34px\] rounded-full' && ! grep -v '^\s*//' /home/ubuntu/skynet/src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx | grep -qE "text-\[13px\] font-semibold" && grep -qE "['\"]pv-panel-header['\"]" /home/ubuntu/skynet/src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx && grep -qE "['\"]pv-pencil['\"]" /home/ubuntu/skynet/src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx && cd /home/ubuntu/skynet && npx tsc --noEmit 2>&1 | tail -5 > /tmp/tsc-13-02-t1.out && ! grep -qE 'error TS' /tmp/tsc-13-02-t1.out && npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx 2>&1 | tail -30 | grep -qE 'PASS|passed'</automated>
  </verify>
  <acceptance_criteria>
    - PrettyConversationsPanel.tsx source contains `pv-panel-header`, `pv-title`, `pv-pencil` class-name string literals
    - No non-comment lines in PrettyConversationsPanel.tsx contain `w-[34px] h-[34px] rounded-full` or `text-[13px] font-semibold` on the header row/button (retired filled-glass-pill + mixed-case-chunky treatment)
    - PrettyConversationsPanel.test.tsx asserts `pv-panel-header` and `pv-pencil` class presence — no assertion on retired styles
    - Scroll region body untouched (lines 275-410 of current file — the empty state div, pinned map, grouped map, RDP divider, NewSessionDialog remain byte-for-byte equivalent)
    - `npx tsc --noEmit` exits 0
    - `npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` all green
    - No edits under `src/ui/features/pretty-view/`, `src/ui/components/`, `src/ui/ssh/`, `src/ui/features/terminal/`, `src/ui/features/pretty-conversations/PrettyConversationRow.tsx`, `src/ui/features/pretty-conversations/PinAction.tsx`
    - 2 atomic commits landed (source rewrite, test update) — NO CSS file edit (parallel-safety with 13-03)
  </acceptance_criteria>
  <done>PrettyConversationsPanel header rewritten to mock's pv-panel-header/pv-title/pv-pencil class-toggle treatment; scroll region + empty state + NewSessionDialog wiring preserved verbatim; tests updated and passing; tsc-clean; atomic commits landed.</done>
</task>

<task type="auto">
  <name>Task 2: Rebase AppShell.tsx sidebar-toggle chevron area to --color-pv-* palette + mock's transparent-icon-rounded-md button treatment</name>
  <files>src/ui/AppShell.tsx</files>
  <read_first>
    - /home/ubuntu/skynet/src/ui/AppShell.tsx (lines 1400-1490 — the persistent top-left sidebar-toggle chevron block introduced in Phase 10 Wave 3; this is the ONLY block this task touches)
    - /home/ubuntu/skynet/src/ui/index.css (lines 110-152 — `--color-pv-*` token authority; `--color-pv-surface-quiet` = rgba(25,26,34,0.5), `--color-pv-border-quiet` = rgba(220,225,245,0.06), `--color-pv-fg-muted` = #a89a80, `--color-pv-fg` = #e8e4d8)
    - /home/ubuntu/.claude/identities/tina/bounties/skynet-transformation/prototype.html (lines 177-191 — `.panel-header .pencil` treatment: 32x32, transparent bg + border, border-radius 8px, --color-pv-fg-muted icon, hover: rgba(220,225,245,0.06) bg + border-quiet border + --color-pv-fg text)
    - /home/ubuntu/skynet/src/ui/features/pretty-conversations/pretty-conversations.css (Wave 1 output — the `.pv-pencil` CSS class already declared. If the CSS class is available globally, this task can use `className='pv-pencil'` on the chevron. If not, this task uses inline Tailwind classes mirroring the same treatment.)
    - /home/ubuntu/skynet/src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx (Task 1 rewrite — same class treatment applied to the panel pencil; the AppShell chevron should VISUALLY match)
  </read_first>
  <action>
Scoped edit to `src/ui/AppShell.tsx` — ONLY the block from the comment `{/* Phase 10 Wave 3: persistent top-left sidebar-toggle chevron. */}` at line ~1407 through the closing `)}` of `{!isMobileListScreen && (...)}` at line ~1479. Do NOT touch anything else in AppShell — no state changes, no keyboard handler changes, no WebSocket lifecycle changes, no other visual chrome changes. If the executor finds a Skynet-theme value elsewhere in AppShell tempting to fix, RESIST — SHAPE-04 scope is the chevron area only.

**Current classes on the chevron `<button>` (line ~1459):**
```
"fixed flex items-center justify-center w-8 h-8 rounded-lg bg-[rgba(20,22,28,0.85)] backdrop-blur-[10px] backdrop-saturate-150 border border-white/[0.08] shadow-[0_2px_8px_rgba(0,0,0,0.35)] text-muted-foreground hover:text-foreground transition-colors cursor-pointer"
```

**Skynet-theme classes present (must be retired):**
  - `bg-[rgba(20,22,28,0.85)]` — an inline rgba that predates the palette-token discipline; replace with `bg-transparent` (matching mock `.pv-pencil` transparent bg)
  - `border-white/[0.08]` — Tailwind white/N syntax bypasses the palette; replace with `border-transparent` on rest, and add hover to use `hover:border-[var(--color-pv-border-quiet)]` (matching mock hover behavior — border-color transitions on hover)
  - `text-muted-foreground` — Skynet theme token (uses --muted-foreground OKLCH); replace with `text-[color:var(--color-pv-fg-muted)]`
  - `hover:text-foreground` — Skynet theme token; replace with `hover:text-[color:var(--color-pv-fg)]`
  - `shadow-[0_2px_8px_rgba(0,0,0,0.35)]` — mock's `.pv-pencil` has no drop shadow; retire (the mock's pencil is a bare transparent button — no shadow, no filled glass)
  - `backdrop-blur-[10px] backdrop-saturate-150` — mock's pencil does not backdrop-blur; retire (mock's panel does the blur, the pencil is transparent so no reason to blur through it)
  - `rounded-lg` — Tailwind rounded-lg is 8px which matches mock's `.pv-pencil` border-radius: 8px; KEEP (or migrate to `rounded-md` if the executor prefers Tailwind idioms — but 8px is the target)

**New class treatment on the chevron button (mock's `.pv-pencil` equivalent):**
Replace the class string above with:
```
"fixed flex items-center justify-center w-8 h-8 rounded-lg bg-transparent border border-transparent text-[color:var(--color-pv-fg-muted)] hover:bg-[rgba(220,225,245,0.06)] hover:border-[color:var(--color-pv-border-quiet)] hover:text-[color:var(--color-pv-fg)] transition-colors cursor-pointer"
```

Alternative (preferred by planner if executor finds it cleaner): use `className='pv-pencil fixed cursor-pointer'` and add fixed positioning + z-index via inline style — this defers all color/border/hover to the shared CSS class defined in Wave 1's pretty-conversations.css. Verify the `.pv-pencil` selector is a globally available style (import in main.tsx from Wave 1 covers this). If chosen, retain the inline `style={{ top: 'max(env(safe-area-inset-top), 8px)', left: 'max(env(safe-area-inset-left), 8px)', zIndex: 30 }}` block for positioning (fixed positioning + safe-area anchor cannot come from the shared class).

**Retain unchanged:**
  - The `!isMobileListScreen && ...` outer conditional (renders only outside mobile list screen)
  - The onClick logic (isTouchDevice ? navigateToList() : setSidebarOpen(!sidebarOpen))
  - The aria-label + title translation logic
  - The inline `style={{ top: 'max(env(safe-area-inset-top), 8px)', left: 'max(env(safe-area-inset-left), 8px)', zIndex: 30 }}` positioning block
  - The nested `<span>` with rotate transform for the chevron direction
  - The `<ChevronLeft className='size-4' />` icon (or scale down to size-[18px] to match mock's pencil svg size if the executor judges it a visual improvement — Claude's discretion, both are within acceptable range)

**Do NOT touch:**
  - Anything above line ~1400 (the `<div className='flex w-screen bg-background' style={{ height: '100dvh', paddingTop: 'max(env(safe-area-inset-top), 0px)' }}>` outer wrapper — note `bg-background` here is a Skynet theme class; retiring it is OUT OF SCOPE per SHAPE-04 which is specifically the chevron area. The 100dvh and paddingTop are also NOT in scope for this plan — SHAPE-04 mentions safe-area padding as "nice-to-have, not phase-blocking" and Wave 3 plan 13-04 handles it structurally if needed)
  - Anything below line ~1479 (the sidebar rendering conditionals, main content area, mobile-view branch)
  - Any keyboard handler, WebSocket, tab state, or store subscription — this is strictly a visual change to the chevron button

**Post-condition grep:** After the edit, `grep -n 'bg-\[rgba(20,22,28,0.85)\]\|border-white/\[0.08\]\|text-muted-foreground\|hover:text-foreground\|backdrop-blur-\[10px\]' src/ui/AppShell.tsx | wc -l` should NOT include lines from the chevron block (grep should return 0 in the range 1400-1490). Other AppShell references to `text-muted-foreground` may exist elsewhere (they are OUT OF SCOPE and left alone).

**Atomic commit:** `feat(phase-13): rebase AppShell sidebar-toggle chevron to --color-pv-* palette + mock pencil treatment`

**Do NOT touch:**
  - Any `src/ui/features/pretty-view/*.tsx` file (SHAPE-06 scope boundary)
  - Any `src/ui/components/*.tsx` file (SHAPE-06 scope boundary — shadcn primitives preserved for upstream rebase-ability)
  - Any `src/ui/ssh/dialogs/*` or `src/ui/features/terminal/*` file (SHAPE-06 scope boundary)
  - AppShell parts outside the chevron block (state, effects, refs, tab logic, WebSocket lifecycle, keyboard chords, sidebar Sheet content)
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet && awk '/Phase 10 Wave 3: persistent top-left sidebar-toggle chevron/,/^\s*\)\}\s*$/' src/ui/AppShell.tsx > /tmp/chevron-block.txt && test -s /tmp/chevron-block.txt && ! grep -qE 'bg-\[rgba\(20,22,28,0\.85\)\]|border-white/\[0\.08\]|shadow-\[0_2px_8px|backdrop-blur-\[10px\]|backdrop-saturate-150' /tmp/chevron-block.txt && ! grep -vE '^\s*//' /tmp/chevron-block.txt | grep -qE 'text-muted-foreground|hover:text-foreground' && grep -qE 'color-pv-fg-muted|pv-pencil|--color-pv' /tmp/chevron-block.txt && npx tsc --noEmit 2>&1 | tail -5 > /tmp/tsc-13-02-t2.out && ! grep -qE 'error TS' /tmp/tsc-13-02-t2.out && npx vitest run 2>&1 | tail -30 | grep -qE 'passed|Test Files'</automated>
  </verify>
  <acceptance_criteria>
    - AppShell.tsx chevron block (lines 1400-1490 area) contains references to `--color-pv-*` tokens or `pv-pencil` class — grep for `color-pv-fg-muted|pv-pencil|--color-pv` in the block returns >= 1
    - The chevron block does NOT contain `bg-[rgba(20,22,28,0.85)]`, `border-white/[0.08]`, `shadow-[0_2px_8px_rgba(0,0,0,0.35)]`, `backdrop-blur-[10px]`, `backdrop-saturate-150` (all retired)
    - The chevron block does NOT contain `text-muted-foreground` or `hover:text-foreground` in NON-COMMENT lines (retired Skynet theme classes)
    - Nothing outside the chevron block is modified in AppShell.tsx — verify via `git diff --stat src/ui/AppShell.tsx` and inspect the diff range (should be roughly lines 1400-1490 only; comment tweaks OK, structural changes outside NOT OK)
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` overall test suite green (or unchanged from baseline)
    - No edits under `src/ui/features/pretty-view/`, `src/ui/components/`, `src/ui/ssh/`, `src/ui/features/terminal/`, `src/ui/features/pretty-conversations/PrettyConversationRow.tsx`, `src/ui/features/pretty-conversations/PinAction.tsx`
    - 1 atomic commit landed (source edit)
  </acceptance_criteria>
  <done>AppShell sidebar-toggle chevron area rebased to --color-pv-* palette + mock's transparent-icon-rounded-md treatment; edit scope strictly limited to the chevron block; tsc-clean; test suite green; atomic commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Panel header CSS → shared pretty-conversations.css | The header markup consumes `.pv-panel-header` / `.pv-title` / `.pv-pencil` selectors declared in Wave 1's CSS file. If Wave 1 CSS did not declare a required selector (e.g., `justify-content: space-between` on `.pv-panel-header`), the header layout regresses. Mitigated by Task 1 verifying the CSS file's selectors and adding a small CSS augmentation commit if needed. |
| AppShell chevron edit → scope boundary | AppShell.tsx is 2000+ lines and touches virtually every user-visible feature. A single accidental edit outside the chevron block could regress tab plumbing, mobile flow, sidebar behavior, or WebSocket lifecycle. Mitigated by Task 2's strict scope grep + `git diff --stat` verification that only the chevron block was changed. |
| Pretty-view interior boundary → SHAPE-06 | Ashley called out this session: "leave the pretty view chat interior alone." The IdentityBadge inside PrettyView is what she sees BEHIND the AppShell chevron, and confusion about which is which risks scope drift. Mitigated by explicit SHAPE-06 acceptance criteria + do-not-touch list on `src/ui/features/pretty-view/`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Tampering (accidental AppShell state change outside chevron scope) | AppShell.tsx | mitigate | Task 2 acceptance criteria requires `git diff --stat src/ui/AppShell.tsx` inspection — only the chevron block should show changes. If the diff extends beyond the block, the plan does not close as PASS. |
| T-13-02-02 | Denial of Service (panel header layout regression on one variant) | PrettyConversationsPanel.tsx | mitigate | Task 1 tests exercise both `variant='mobile'` and `variant='desktop'` header assertions. If either regresses (title missing on desktop, pencil misaligned on mobile), the test suite catches it before merge. |
| T-13-02-03 | Information Disclosure | palette-token references | accept | The palette tokens (`--color-pv-fg-muted`, etc.) are already exposed in `src/ui/index.css` and used by every other pretty-* surface. No new exposure. |
| T-13-02-04 | Tampering (scope drift — executor edits pretty-view interior tempted by "matches the chevron aesthetic") | src/ui/features/pretty-view/ | mitigate | Task 1 and Task 2 acceptance criteria explicitly list `src/ui/features/pretty-view/` as do-not-touch. Verify via `git diff --stat` — no files under that directory should appear. |
| T-13-02-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this plan. Pure JSX/CSS text edits. No supply-chain surface. |
</threat_model>

<verification>
- PrettyConversationsPanel.tsx header contains pv-panel-header/pv-title/pv-pencil class names (grep gate in Task 1)
- Retired filled-glass-pill classes absent from header (grep-negation gate in Task 1)
- PrettyConversationsPanel.test.tsx updated to assert new class-based header (grep gate in Task 1)
- AppShell.tsx chevron block references --color-pv-* or pv-pencil (grep gate in Task 2)
- Retired Skynet theme classes absent from chevron block (grep-negation gate in Task 2)
- AppShell.tsx edit scope limited to chevron block (`git diff --stat` inspection)
- `npx tsc --noEmit` exits 0 after each atomic commit (tsc gate in both tasks)
- Scroll region of PrettyConversationsPanel.tsx (lines 275-410) unchanged (visual inspection + acceptance criteria)
- No edits under `src/ui/features/pretty-view/`, `src/ui/components/`, `src/ui/ssh/`, `src/ui/features/terminal/`, PrettyConversationRow.tsx, PinAction.tsx
</verification>

<success_criteria>
- SHAPE-02 satisfied: panel header uses mock's class-toggle treatment (12px + 700 + 0.1em + UPPERCASE title; 32x32 + transparent + border-radius 8px + fg-muted pencil)
- SHAPE-04 satisfied: AppShell chevron area uses --color-pv-* palette + mock's transparent-icon-rounded-md treatment
- Panel header no longer looks Skynet — Ashley's "coming out of old Skynet stuff" complaint from this session addressed
- AppShell shell chrome around the chevron no longer looks Skynet — Ashley's "still looks Skynet" complaint addressed
- Scroll region + empty state + NewSessionDialog wiring preserved verbatim — no risk of regressing the panel's other content
- Wave 3 (PinAction rewrite) runs in parallel with this plan because files_modified are disjoint
- Wave 4 (post-lift verification) can proceed once this and 13-03 both land
</success_criteria>

<output>
Create `.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-02-SUMMARY.md` when done.
</output>
