---
phase: 13-skynet-transformation-conversation-list-lift-from-mock
plan: 05
type: execute
wave: 4
depends_on:
  - 13-01
  - 13-02
  - 13-03
  - 13-04
files_modified:
  - .planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-BUILD-VERIFY-LOG.md
  - .planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-UAT-CHECKLIST.md
  - .planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-PATCHES-MD-ENTRY.md
autonomous: true
requirements:
  - SHAPE-06
  - SHAPE-07
tags: [ui, docs, build-verify, uat, patches-md, phase-boundary]

must_haves:
  truths:
    - "A build-verify log at 13-BUILD-VERIFY-LOG.md exists capturing tsc + vitest + npm run build output as evidence of phase-boundary code-complete-clean state (mirrors Phase 11's 11-04-PLAN + Phase 12's 12-07-PLAN pattern)"
    - "A UAT checklist at 13-UAT-CHECKLIST.md exists enumerating the observable behaviors Ashley must verify post-deploy on both desktop and mobile — covering SHAPE-01 (row lift), SHAPE-02 (panel header), SHAPE-03 (pin action), SHAPE-04 (AppShell chevron), SHAPE-05 (dot visibility outcome from 13-04), SHAPE-06 (pretty-view interior + shadcn preserved), SHAPE-07 (build clean)"
    - "A patches-md entry draft at 13-PATCHES-MD-ENTRY.md exists ready for Tina to paste into `~/.claude/identities/tina/skynet-patches.md` at the next patch number (call it #140 or whatever the next patch number is when this ships)"
    - "The UAT deploy runbook cites `~/.claude/identities/tina/deploy-runbook.md` as authoritative, references the fleet-standing 'batch patches into meaningful deploys' rule, includes the check-before-recreate one-liner, and notes the fork CLAUDE.md 15-min deadman reference is retired"
    - "The build-verify log Section 4 grep-hygiene gates include a scope-boundary gate: `grep -rE 'text-muted-foreground|hover:text-foreground|bg-background|bg-card|text-foreground|border-border|muted-foreground' src/ui/features/pretty-conversations/ | grep -vE '\\.md:|^\\s*//|/\\*' | wc -l` returns 0 (final SHAPE-03 purge verification)"
    - "The build-verify log Section 4 includes a SHAPE-06 scope-boundary gate: `git diff --stat <phase-13-start-sha> HEAD -- src/ui/features/pretty-view/ src/ui/components/ src/ui/ssh/ src/ui/features/terminal/ | wc -l` returns 0 (proof that Phase 13 touched none of these directories)"
    - "The patches-md entry references the master bounty `~/.claude/identities/tina/bounties/skynet-transformation/` and does NOT introduce a sibling bounty (per CONTEXT.md § meta-lesson)"
    - "Zero source-tree modifications in this plan — pure documentation authoring"
    - "The phase closes as Skynet-SHAPE-COMPLETE-pending-Ashley-UAT-and-deploy-greenlight (the full Ship-of-Theseus movement Phases 11+12+13 completes at this milestone)"
    - "No edits under `src/` in this plan"
  artifacts:
    - path: ".planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-BUILD-VERIFY-LOG.md"
      provides: "tsc + vitest + npm run build output evidence, grep hygiene gates including the final Skynet-theme-class purge gate + SHAPE-06 scope-boundary gate, requirement traceability for SHAPE-01..SHAPE-07"
      contains: "tsc"
    - path: ".planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-UAT-CHECKLIST.md"
      provides: "Ashley's post-deploy verification walkthrough — desktop + mobile + cross-viewport regression + failure-route-back table + authoritative deploy runbook citation"
      contains: "SHAPE-01"
    - path: ".planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-PATCHES-MD-ENTRY.md"
      provides: "Paste-ready patch entry for Tina's skynet-patches.md catalog — closes the Ship-of-Theseus movement (Phases 11+12+13)"
      contains: "Skynet"
  key_links:
    - from: "13-BUILD-VERIFY-LOG.md"
      to: "13-UAT-CHECKLIST.md"
      via: "the build-verify log's PASS status is the input to Ashley starting the UAT walkthrough"
      pattern: "UAT"
    - from: "13-UAT-CHECKLIST.md"
      to: "~/.claude/identities/tina/deploy-runbook.md"
      via: "authoritative deploy source citation (NOT stale fork CLAUDE.md deadman reference)"
      pattern: "deploy-runbook\\.md"
    - from: "13-PATCHES-MD-ENTRY.md"
      to: "~/.claude/identities/tina/bounties/skynet-transformation/"
      via: "master bounty reference — Ship-of-Theseus movement closeout"
      pattern: "skynet-transformation"
---

<objective>
Author three documentation deliverables that close Phase 13 for the deploy handoff to Ashley: (1) `13-BUILD-VERIFY-LOG.md` capturing tsc + vitest + npm run build output + grep-hygiene gates including the final SHAPE-03 Skynet-theme-class purge verification AND the SHAPE-06 scope-boundary verification; (2) `13-UAT-CHECKLIST.md` enumerating observable behaviors Ashley walks post-deploy for SHAPE-01 through SHAPE-07; (3) `13-PATCHES-MD-ENTRY.md` paste-ready patch entry ready for Tina to paste into `~/.claude/identities/tina/skynet-patches.md`. No source-tree modifications — pure documentation.

Purpose: Implements SHAPE-06 (scope-boundary evidence) and SHAPE-07 (build-clean evidence). Phase 13 is the FINAL slice of the Ship-of-Theseus movement (Phases 11+12+13). This plan is the phase-boundary closeout — after this, Ashley UATs at parity with the mock on mobile + desktop, and if she signs off, the Skynet-shape-of-Telegram-mobile-app transformation is complete. The master bounty `~/.claude/identities/tina/bounties/skynet-transformation/` closes on Ashley's UAT sign-off.

Output: Three markdown files under `.planning/phases/13-.../`. Ashley reads them; if PASS, the next patch entry goes into `~/.claude/identities/tina/skynet-patches.md` and the deploy happens per fork DEPLOY DISCIPLINE (batched per fleet-standing rule).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-CONTEXT.md
@.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-01-SUMMARY.md
@.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-02-SUMMARY.md
@.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-03-SUMMARY.md
@.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-04-SUMMARY.md
@.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-04-UAT-DIAG-LOG.md
@.planning/phases/11-skynet-transformation-purge-dead-skynet-surfaces-first-slice/11-BUILD-VERIFY-LOG.md
@.planning/phases/11-skynet-transformation-purge-dead-skynet-surfaces-first-slice/11-UAT-CHECKLIST.md
@.planning/phases/11-skynet-transformation-purge-dead-skynet-surfaces-first-slice/11-PATCHES-MD-ENTRY.md
@.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-BUILD-VERIFY-LOG.md
@.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-UAT-CHECKLIST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 13-BUILD-VERIFY-LOG.md capturing tsc + vitest + npm run build evidence, grep hygiene gates, and requirement traceability</name>
  <files>.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-BUILD-VERIFY-LOG.md</files>
  <read_first>
    - /home/ubuntu/skynet/.planning/phases/11-skynet-transformation-purge-dead-skynet-surfaces-first-slice/11-BUILD-VERIFY-LOG.md (the format to mirror — same 5-section structure)
    - /home/ubuntu/skynet/.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-BUILD-VERIFY-LOG.md (Phase 12's format — same 5-section structure)
    - /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-01-SUMMARY.md through 13-04-SUMMARY.md (execution summaries from prior plans)
    - /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-04-UAT-DIAG-LOG.md (Ashley's UAT observations — informs Section 2 test-suite delta discussion)
  </read_first>
  <action>
Run three verification commands from the repo root and capture their output verbatim.

```
cd /home/ubuntu/skynet
npx tsc --noEmit 2>&1 | tail -20
```

```
cd /home/ubuntu/skynet
npx vitest run 2>&1 | tail -40
```

```
cd /home/ubuntu/skynet
npm run build 2>&1 | tail -30
```

Then write `13-BUILD-VERIFY-LOG.md` with the following sections:

## Section 1: tsc --noEmit
- Exit code observed
- Full output (or "Clean — 0 errors")
- Comparison to baselines: Wave 1 (post-13-01), Wave 2 (post-13-02, 13-03), Wave 3 (post-13-04), Phase 12 tip (from 12-BUILD-VERIFY-LOG.md)

## Section 2: npx vitest run
- Total tests run
- Passing count
- Failing count
- List of failing tests by file + name
- Delta vs Phase 12 tip: Phase 13 rewrites PrettyConversationRow.test.tsx (18 tests preserved but assertions changed) and PrettyConversationsPanel.test.tsx (15 tests, only header ones updated). Expected: same total test count as Phase 12 tip (or +/-2 if a test was split or combined).
- Any pre-existing ComposeBox test failures documented in Phase 10's deferred-items.md are INHERITED BASELINE, not Phase 13 regressions. Verify count matches Phase 12 tip.

## Section 3: npm run build (phase-boundary gate)
- Exit code observed
- Build duration in seconds
- Bundle size delta vs Phase 12 tip. Expected: SMALL DELTA — Phase 13 swaps JS-computed inline styles for a static CSS file (roughly wash) and retires ~250 lines of JS in PrettyConversationRow.tsx (minor shrink). The CSS file itself adds ~150 lines to a new chunk. Net expected: bundle roughly unchanged, possibly slight shrink.
- Explicit annotation: this is the FIRST full `npm run build` verification after Wave 1's CSS file + Wave 1/2/3 source rewrites landed. If build FAILS, route back to identify which commit introduced the regression.

## Section 4: Grep hygiene gates (Phase 13 non-negotiables)
Run and capture the count for each:

**SHAPE-01 gates (row + CSS):**
- `test -f src/ui/features/pretty-conversations/pretty-conversations.css && echo EXISTS || echo MISSING` — expected EXISTS
- `grep -c '^\.pv-row' src/ui/features/pretty-conversations/pretty-conversations.css` — expected >= 5
- `grep -c 'pv-row' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` — expected >= 3
- `grep -c '^' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` — expected 350-550 (line count reduction target)

**SHAPE-02 gates (panel header):**
- `grep -c 'pv-panel-header\|pv-title\|pv-pencil' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` — expected >= 3
- `grep -vE '^\s*//' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx | grep -c 'w-\[34px\] h-\[34px\] rounded-full\|text-\[13px\] font-semibold'` — expected 0

**SHAPE-03 gates (pin action + final Skynet theme-class purge):**
- `grep -c 'pv-pin-action-desktop' src/ui/features/pretty-conversations/PinAction.tsx` — expected >= 1
- `grep -c 'pv-pin-action-desktop' src/ui/features/pretty-conversations/pretty-conversations.css` — expected >= 1
- `grep -rE 'text-muted-foreground|hover:text-foreground|bg-background|bg-card|text-foreground|border-border|muted-foreground' src/ui/features/pretty-conversations/ | grep -vE '\.md:|^\s*//|/\*|\* ' | wc -l` — expected 0 (final purge — the SHAPE-03 deep gate)

**SHAPE-04 gates (AppShell chevron):**
- Extract the chevron block: `awk '/Phase 10 Wave 3: persistent top-left sidebar-toggle chevron/,/^\s*\)\}\s*$/' src/ui/AppShell.tsx > /tmp/chevron.txt`
- `grep -c '\-\-color-pv\|pv-pencil' /tmp/chevron.txt` — expected >= 1
- `grep -vE '^\s*//' /tmp/chevron.txt | grep -cE 'bg-\[rgba\(20,22,28,0\.85\)\]|border-white/\[0\.08\]|text-muted-foreground|hover:text-foreground|backdrop-blur-\[10px\]'` — expected 0

**SHAPE-06 scope-boundary gate (proof that Phase 13 touched none of the locked directories):**

Identify the Phase 13 start SHA. If unavailable, fall back to Phase 12's final commit SHA (approximates the phase boundary). Run:

- `git diff --stat <phase-13-start-sha> HEAD -- src/ui/features/pretty-view/ | wc -l` — expected 0
- `git diff --stat <phase-13-start-sha> HEAD -- src/ui/components/ | wc -l` — expected 0
- `git diff --stat <phase-13-start-sha> HEAD -- src/ui/ssh/ | wc -l` — expected 0
- `git diff --stat <phase-13-start-sha> HEAD -- src/ui/features/terminal/ | wc -l` — expected 0

If ANY of these returns > 0, the SHAPE-06 scope boundary was violated — flag as HARD FAIL, route back to the offending plan.

**SHAPE-07 gates:**
- tsc exit code 0 (from Section 1)
- vitest all green or unchanged from Phase 12 baseline (from Section 2)
- npm run build exit code 0 (from Section 3)

Any FAIL row is a Phase 13 non-negotiable violation. Do not proceed to UAT authoring until every gate returns the expected count. Route back to the relevant plan and fix.

## Section 5: Requirement traceability
Table mapping SHAPE-01 through SHAPE-07 to the specific commits + verification evidence:
- SHAPE-01: row lifted to mock's flat CSS class-toggle → evidence: 13-01 commits + Section 4 SHAPE-01 gates
- SHAPE-02: panel header lifted → evidence: 13-02 commits + Section 4 SHAPE-02 gates
- SHAPE-03: pin action lifted + Skynet theme-class purge → evidence: 13-03 commits + Section 4 SHAPE-03 deep gate
- SHAPE-04: AppShell chevron rebased → evidence: 13-02 commits + Section 4 SHAPE-04 gates
- SHAPE-05: dot visibility (from 13-04 Ashley UAT) → evidence: 13-04-UAT-DIAG-LOG.md Section 5 verdict; PASS/FAIL noted
- SHAPE-06: pretty-view + shadcn + SSH/RDP + terminal directories untouched → evidence: Section 4 SHAPE-06 scope-boundary gate
- SHAPE-07: tsc + vitest + build clean → evidence: Sections 1-3

Save the file. Do NOT commit yet — Task 3 handles the docs commit.
  </action>
  <verify>
    <automated>test -f /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-BUILD-VERIFY-LOG.md && grep -qE "Section 1|tsc --noEmit" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-BUILD-VERIFY-LOG.md && grep -qE "Section 2|vitest" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-BUILD-VERIFY-LOG.md && grep -qE "Section 3|npm run build" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-BUILD-VERIFY-LOG.md && grep -qE "Section 4|Grep hygiene" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-BUILD-VERIFY-LOG.md && grep -qE "SHAPE-01" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-BUILD-VERIFY-LOG.md && grep -qE "SHAPE-06|scope-boundary" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-BUILD-VERIFY-LOG.md && grep -qE "text-muted-foreground" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-BUILD-VERIFY-LOG.md && grep -qE "SHAPE-07|Section 5|traceability" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-BUILD-VERIFY-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/13-.../13-BUILD-VERIFY-LOG.md`
    - Contains all 5 sections (tsc, vitest, npm run build, grep gates, requirement traceability)
    - Section 4 lists SHAPE-01 through SHAPE-07 grep gates with expected counts
    - Section 4 includes the final Skynet-theme-class purge gate for `src/ui/features/pretty-conversations/` (SHAPE-03 deep gate) with expected count 0
    - Section 4 includes the SHAPE-06 scope-boundary gate (git diff --stat against pretty-view/components/ssh/terminal) with expected count 0
    - Section 5 covers all 7 SHAPE-XX requirements with evidence pointers
    - Pre-existing ComposeBox test failures from Phase 10 explicitly named as INHERITED BASELINE
    - If any grep gate fails, the log FLAGS it and does NOT close as PASS
  </acceptance_criteria>
  <done>Build-verify log written with tsc + vitest + build evidence + grep gates including the final Skynet-theme-class purge + SHAPE-06 scope-boundary evidence; requirement traceability covers SHAPE-01..SHAPE-07.</done>
</task>

<task type="auto">
  <name>Task 2: Author 13-UAT-CHECKLIST.md — Ashley's post-deploy verification walkthrough covering SHAPE-01..SHAPE-07</name>
  <files>.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-UAT-CHECKLIST.md</files>
  <read_first>
    - /home/ubuntu/skynet/.planning/phases/11-skynet-transformation-purge-dead-skynet-surfaces-first-slice/11-UAT-CHECKLIST.md (the format to mirror — Desktop UAT + Mobile UAT + Cross-viewport regression + Failure route-back table + Post-UAT deploy runbook sections)
    - /home/ubuntu/skynet/.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-UAT-CHECKLIST.md (Phase 12's format — same structure)
    - /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-CONTEXT.md (§ "Success Criteria" from ROADMAP is the primary UAT source — items 1-10 map to SHAPE-01..SHAPE-07)
    - /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-04-UAT-DIAG-LOG.md (Ashley's post-Wave-3 dev-instance UAT already covered SHAPE-05; post-deploy UAT re-verifies at production URL)
    - /home/ubuntu/.claude/identities/tina/deploy-runbook.md (AUTHORITATIVE deploy source — cite this, NOT the fork CLAUDE.md 15-min deadman)
    - /home/ubuntu/.claude/identities/tina/bounties/skynet-transformation/prototype.html (the mock — Ashley compares post-deploy conversation list against this side-by-side)
  </read_first>
  <action>
Write `13-UAT-CHECKLIST.md` following Phase 11+12's format. Sections:

## Desktop UAT (Chrome / Safari — Ashley's laptop)

Enumerate 10-12 non-negotiable observations covering SHAPE-01..SHAPE-07:

1. **SHAPE-01/02:** Fresh page-load at `https://term.gigaashley.click/` — sidebar renders the pretty-conversations panel with UPPERCASE title ("CONVERSATIONS" not "Conversations" in mixed-case) and 32x32 transparent pencil icon (not filled-glass pill). Panel-header font: 12px + weight-700 + letter-spacing 0.1em. Compare side-by-side against `~/.claude/identities/tina/bounties/skynet-transformation/prototype.html` (open the mock in a second browser tab); title typography matches.

2. **SHAPE-01:** Each conversation row uses the mock's `.row` treatment. Full-intensity + Normal density variant. Row background: 160deg gradient of `hsla(hue, 50%, 38%, 0.55)` → `hsla(hue, 45%, 24%, 0.60)` with 0.32 hue border + warm cream 0.18 top inset rim + hue 0.20 hairline + hue 0.18 outer glow at 32px. Hover lifts translateY(-1px) + strengthens hue glow. Selected uses strongest treatment (0.55 hue border, 1px hue ring, 56px glow). Compare against mock — bubble treatment matches.

3. **SHAPE-01 (ambient recession):** Rows Ashley has NOT clicked in this browser session read as ambient/recessed: flat hue rgba background (NOT gradient), muted border, no drop shadow, no backdrop-blur. This addresses Ashley's this-session complaint that "active conversations like ones that I've already loaded into ... are not glowing fully like they were supposed to. It seems like they more get like a glowing border or something." Post-lift, active-set rows glow fully; ambient rows recede but are still legible.

4. **SHAPE-03:** Pin buttons render as bare icons with hue-drop-shadow on PINNED rows only. Unpinned rows show no pin button chrome (mock's `.row:not(.pinned) .meta .pin { display: none }` rule). Hover over an unpinned desktop row — the bare-icon pin action appears; click to pin; unhover — it disappears. This addresses Ashley's this-session complaint that "the pin buttons are totally obnoxious."

5. **SHAPE-04:** Top-left sidebar-toggle chevron (persistent top-left button) uses transparent bg + no filled-glass pill + `--color-pv-fg-muted` icon color. Compare against the mock's `.panel-header .pencil` treatment — visually consistent (bare button, no chrome).

6. **SHAPE-05:** Click 3 different conversations one after another. Once each agent is idle, its row shows a bright hue-cream ready-dot in the right `.pv-meta` column. Rows NOT in Ashley's activeSet (not clicked this session) never show the dot. Rows where the agent is currently working ("Claude is thinking...") do not show the dot until the response finishes.

7. **SHAPE-06:** Click into any pretty-view chat — the interior is IDENTICAL to pre-Phase-13 (bubbles, compose box, IdentityBadge, message rendering, chat-column background all unchanged). No visual regression to the chat interior.

8. **SHAPE-06:** Right-click a session → OPKSSHDialog / SSHAuthDialog / TmuxSessionPicker open — visual is IDENTICAL to pre-Phase-13 (Skynet theme classes preserved for shadcn + SSH dialogs per Ship-of-Theseus rule).

9. **SHAPE-06:** Click into an RDP-host-sentinel row → Guacamole pane opens; xterm.js terminal opens for SSH sessions — visuals IDENTICAL to pre-Phase-13.

10. **Cross-viewport:** New session pencil button opens NewSessionDialog (host picker). Dialog behavior unchanged.

11. **Cross-viewport:** Persistent top-left chevron toggle collapses/expands sidebar as before.

12. **SHAPE-07:** Existing keyboard shortcuts unchanged: double-shift command palette, Ctrl+Shift+O pretty mode toggle.

## Mobile UAT (iOS Safari / installed PWA — Ashley's iPhone)

Enumerate 8-10 items:

1. **SHAPE-01/02:** Fresh page-load on iPhone PWA lands on the pretty-conversations list view. Header renders empty-left + right-anchored pencil (mock's mobile variant). Rows use the mock's chunky treatment (72px min-height, 48px avatar).

2. **SHAPE-03:** Swipe-left on a non-RDP row reveals the mobile 48x48 pin action disc (preserved fork affordance — the mock's desktop layout does not cover this). Pin/unpin works. Swipe right or tap the row body closes the reveal.

3. **SHAPE-05:** Same test as Desktop item 6 — click 3 conversations, verify dots appear on idle rows.

4. **Mobile scroll (from 13-04 verdict):** Scroll from top to bottom of the conversation list — no freeze. Fast flick + slow drag both work. Copy the verdict from 13-04-UAT-DIAG-LOG.md Section 5 Item C (PASS or FAIL_*).

5. **Safe-area (from 13-04 verdict):** Last row is not scrolled underneath the iPhone home indicator. Copy the verdict from 13-04-UAT-DIAG-LOG.md Section 5 Item D.

6. **SHAPE-06:** Tap into a pretty-view chat — interior identical to pre-Phase-13.

7. **SHAPE-06:** Tap an RDP-host-sentinel row → Guacamole pane opens (RDP visual unchanged).

8. **Mobile PWA reinstall:** Remove PWA from home screen, re-add. Safe-area seam still gray (matches Phase 10-12 rebase); no regression.

## Cross-viewport regression checks

Enumerate 4-6 items:

1. Message queue drawer opens/closes at bottom of terminal panes (Ctrl+M).
2. Pretty-view compose box submits messages, WipBubble renders, session-holding overlay behaves.
3. RDP session actually connects + renders (not just launches — Ashley verifies the actual remote desktop is usable).
4. Multi-session terminal panes — switching between conversations preserves live WebSocket, terminal buffer, pretty-view scroll position.
5. Refresh the page while a session is active — the session's row still appears (fleet-native list Phase 7 behavior preserved).
6. Direct navigation to `#hosts`, `#admin`, `#snippets` still returns 404-equivalent or PrettyLandingCard (Phase 11 unreachability preserved — no regression to dead-surface reachability).

## Failure route-back table

| Symptom | Route to |
|---------|----------|
| Row visual doesn't match mock (e.g., wrong padding, wrong gradient) | Plan 13-01 (pretty-conversations.css lift + PrettyConversationRow rewrite) |
| Panel header title still mixed-case / filled-glass pencil pill | Plan 13-02 Task 1 (header rewrite) |
| Pin buttons still visible on unpinned desktop rows | Plan 13-03 (PinAction desktop rewrite + hide-on-unpinned CSS rule) |
| Skynet theme classes surviving in src/ui/features/pretty-conversations/ | Plan 13-03 grep gate — investigate which file still has the hit |
| AppShell chevron still filled-glass rgba(20,22,28,0.85) | Plan 13-02 Task 2 (AppShell chevron rebase) |
| Ready-for-attention dot still not visible after 3+ conversation clicks | Plan 13-04 diagnostic candidates A-D — start with the SUSPECT verdict from 13-04-UAT-DIAG-LOG.md |
| Mobile scroll-freeze reproduces | Follow-up plan — capture iPhone Safari devtools touch trace |
| Pretty-view chat interior changed | HARD FAIL — SHAPE-06 scope violation; route back to whichever Wave/plan modified src/ui/features/pretty-view/ |
| Shadcn dialog / RDP visual changed | HARD FAIL — SHAPE-06 scope violation; route back to whichever Wave/plan modified src/ui/components/ or src/ui/ssh/ or src/ui/features/terminal/ |
| tsc / build broken | Route to whichever plan/task's commit surfaces the tsc error |

## Post-UAT deploy runbook

**AUTHORITATIVE SOURCE:** `~/.claude/identities/tina/deploy-runbook.md`. This is the current, self-contained procedure for shipping `skynet-patched:local` onto skynet-ec2. Follow the steps in that file verbatim.

**Stale reference note — do NOT follow the fork's CLAUDE.md 15-min deadman regime.** The fork's `CLAUDE.md` still contains a `Deploy safety` line reading `Every 'docker compose up -d --force-recreate skynet' runs behind the 15-min deadman rollback timer (/opt/skynet/.tmp-revert.sh) — no exceptions, per Ashley 2026-07-03, even when she is at the keyboard.` This constraint has been RETIRED fleet-wide since 2026-07-21 — Ashley's SSM-tmux-attach fallback replaced the deadman's role. The fork's CLAUDE.md update is a separate open bounty (`claude-md-15min-deadman-stale`). Ignore the fork CLAUDE.md's 15-min deadman line; use `~/.claude/identities/tina/deploy-runbook.md`.

**FLEET-STANDING BATCHING RULE (Ashley 2026-07-23):** "Batch patches into meaningful deploys — one patch ≠ one deploy." The Phase 13 patch (call it #140 or whatever the next available patch number is when this ships) MUST NOT auto-deploy. Batch until Ashley explicitly greenlights.

**CHECK-BEFORE-RECREATE ONE-LINER** (per `~/.claude/identities/tina/tina.md` § learned preferences — survived deadman retirement):

Before EVERY `docker compose up -d --force-recreate skynet`, grep the compose file image line:

```
grep 'image:' /opt/skynet/docker-compose.yml | grep -q skynet-patched:local || \
  sudo sed -i 's|image: ghcr.io/lukegus/skynet:latest|image: skynet-patched:local|' /opt/skynet/docker-compose.yml
```

Idempotent — no-op when compose is already patched, corrects when it's been reverted.

**Deploy flow (only if Ashley greenlights):**

1. Apply + commit + push + build (per deploy-runbook.md step 1). `git push` BEFORE build — the build script clones from GitHub.
2. Ask Ashley for explicit go-ahead for THIS deploy window.
3. Run the check-before-recreate one-liner above.
4. `cd /opt/skynet && sudo docker compose up -d --force-recreate skynet`
5. Wait for `healthy`; corroborate the patch shipped by grepping the dist for the patch's signature bytes.
6. Tell Ashley to test; on her "pin it," bump `~/.claude/identities/tina/skynet-patches.md` header count and paste the 13-PATCHES-MD-ENTRY.md content at the next patch number.
7. If broken: manual rollback per deploy-runbook.md step 8.

## Master bounty closeout note

After Ashley's UAT signs off at parity with the mock (mobile + desktop), the master bounty `~/.claude/identities/tina/bounties/skynet-transformation/` closes — the entire Ship-of-Theseus movement (Phases 11+12+13) is complete. The Skynet SHAPE (Telegram-mobile-app-of-Skynet) is done. Ashley's next set of Skynet bounties will be NEW-FEATURE work (pretty-view enhancements, etc.) — not further Ship-of-Theseus purge.

Save the file. Do NOT commit yet — Task 3 handles the docs commit.
  </action>
  <verify>
    <automated>test -f /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-UAT-CHECKLIST.md && grep -qE "Desktop UAT" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-UAT-CHECKLIST.md && grep -qE "Mobile UAT" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-UAT-CHECKLIST.md && grep -qE "Cross-viewport|regression" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-UAT-CHECKLIST.md && grep -qE "Failure route-back" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-UAT-CHECKLIST.md && grep -qE "SHAPE-01|SHAPE-02|SHAPE-03|SHAPE-04|SHAPE-05|SHAPE-06|SHAPE-07" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-UAT-CHECKLIST.md && grep -q "deploy-runbook.md" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-UAT-CHECKLIST.md && grep -qE "batch patches|batching rule|meaningful deploys" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-UAT-CHECKLIST.md && grep -qE "check-before-recreate|grep 'image:'" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-UAT-CHECKLIST.md && grep -qE "master bounty|skynet-transformation" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-UAT-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/13-.../13-UAT-CHECKLIST.md`
    - Contains at least 5 sections: Desktop UAT, Mobile UAT, Cross-viewport regression, Failure route-back, Post-UAT deploy runbook
    - Desktop UAT enumerates at least 10 non-negotiable items covering SHAPE-01..SHAPE-07
    - Mobile UAT enumerates at least 6 items
    - Cross-viewport regression enumerates at least 4 items including RDP-verify
    - Failure route-back table maps at least 8 symptoms to plan/task locations INCLUDING the SHAPE-06 scope-violation route-backs
    - Post-UAT deploy runbook cites `~/.claude/identities/tina/deploy-runbook.md` as authoritative
    - Post-UAT deploy runbook notes fork CLAUDE.md 15-min deadman reference is retired
    - Post-UAT deploy runbook references batching rule + check-before-recreate one-liner
    - Master bounty closeout note references `~/.claude/identities/tina/bounties/skynet-transformation/` as the master bounty closing on Ashley's UAT
    - Every UAT item is expressed as an OBSERVABLE assertion Ashley can check
  </acceptance_criteria>
  <done>UAT checklist written with actionable observations Ashley can walk post-deploy; SHAPE-01..SHAPE-07 all covered; authoritative deploy runbook citation replaces stale fork CLAUDE.md 15-min deadman reference; batching rule + check-before-recreate one-liner included; failure route-back table gives clear signals of where to fix; master bounty closeout note included.</done>
</task>

<task type="auto">
  <name>Task 3: Author 13-PATCHES-MD-ENTRY.md and commit all three Phase 13 docs deliverables</name>
  <files>.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-PATCHES-MD-ENTRY.md</files>
  <read_first>
    - /home/ubuntu/skynet/.planning/phases/11-skynet-transformation-purge-dead-skynet-surfaces-first-slice/11-PATCHES-MD-ENTRY.md (the format to mirror — multi-commit-under-one-pin structure)
    - /home/ubuntu/skynet/.planning/phases/12-skynet-transformation-purge-dead-frontend-surfaces-second-slice/12-PATCHES-MD-ENTRY.md (Phase 12's entry — same format)
    - /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-BUILD-VERIFY-LOG.md (Task 1 output — quote from it in the patch entry)
    - /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-04-UAT-DIAG-LOG.md (13-04 output — quote Ashley's UAT verdicts)
  </read_first>
  <action>
Write `13-PATCHES-MD-ENTRY.md` as a paste-ready patch entry for Tina's `~/.claude/identities/tina/skynet-patches.md`. Mirror Phase 11+12's multi-commit-under-one-pin format. Include:

- **Patch title:** `patch #NNN — Skynet transformation: conversation list lift-from-mock (final Ship-of-Theseus slice)` (where NNN is the next available patch number when this ships — call it #140 as placeholder; Tina fills at paste time)
- **Patch summary paragraph:** 3-5 sentences at Ashley-level: "This patch lifts the mock v4 (`~/.claude/identities/tina/bounties/skynet-transformation/prototype.html`) verbatim onto the conversation list. Retires ~250 lines of JS-computed inline styles + Tailwind layout scaffolding in `PrettyConversationRow.tsx` (down from 709 lines) in favor of a real `pretty-conversations.css` file. Panel header adopts the mock's UPPERCASE + tracking + transparent-pencil treatment. Pin action becomes bare-icon-with-hue-glow. AppShell top-left chevron rebased to `--color-pv-*` palette. Ashley's session complaints ('pin buttons totally obnoxious', 'panel header looks Skynet', 'ambient rows not glowing fully') addressed. Closes the Ship-of-Theseus movement (Phases 11+12+13) — the Skynet SHAPE is complete."
- **Commit list** — enumerate every commit from Plans 01-05 with SHA placeholders:
  - Plan 13-01: 3 commits (CSS file + src rewrite + test rewrite + tokens.ts cleanup)
  - Plan 13-02: 2-3 commits (Panel header rewrite + tests + AppShell chevron rebase)
  - Plan 13-03: 1-2 commits (PinAction desktop + CSS augmentation)
  - Plan 13-04: 1 commit (UAT-DIAG-LOG draft + observations)
  - Plan 13-05: 1 commit (this docs commit)
- **Verification evidence:** paste the tsc/vitest/build results from 13-BUILD-VERIFY-LOG.md (compressed to a table)
- **Grep hygiene gate results** from Section 4 of the build-verify log, INCLUDING the final Skynet-theme-class purge gate (SHAPE-03 deep) and the SHAPE-06 scope-boundary gate
- **Bundle delta:** small — the CSS file adds ~150 lines but PrettyConversationRow.tsx sheds ~250 lines of JS + Tailwind; net expected wash or slight shrink
- **References:**
  - `.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/` (the phase directory)
  - `~/.claude/identities/tina/bounties/skynet-transformation/` (the MASTER bounty — Ship-of-Theseus movement closeout; do NOT reference a sibling bounty)
  - `~/.claude/identities/tina/bounties/skynet-transformation/prototype.html` (mock v4, LOCKED — the source of truth)
  - `~/.claude/identities/tina/tina.md § Skynet direction — the app IS Telegram` (mental model)
  - `~/.claude/identities/tina/deploy-runbook.md` (authoritative deploy source)
- **Batching note:** patch batches per fleet-standing "batch patches into meaningful deploys" rule. No standalone deploy unless Ashley greenlights.
- **Master bounty closeout:** After Ashley's UAT signs off at parity with the mock, the master bounty `skynet-transformation` closes. The Ship-of-Theseus movement (Phases 11+12+13) is complete.
- **NO Co-Authored-By trailer** (fork convention).

Then commit all three Phase 13 docs deliverables (13-BUILD-VERIFY-LOG.md, 13-UAT-CHECKLIST.md, 13-PATCHES-MD-ENTRY.md) in ONE commit as:

```
docs(phase-13): build-verify log + UAT checklist + patch entry draft
```

Use `git add` on specific file paths only (no `-A`, no `.`). No source files should be staged.

Post-commit verification:
- `git log -1 --name-only` shows exactly the 3 markdown files under `.planning/phases/13-.../`
- `git status --porcelain` returns empty
  </action>
  <verify>
    <automated>test -f /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-PATCHES-MD-ENTRY.md && grep -qE "patch #[0-9]+|patch [0-9]+" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-PATCHES-MD-ENTRY.md && grep -qE "Skynet|lift-from-mock|Ship-of-Theseus" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-PATCHES-MD-ENTRY.md && grep -qE "skynet-transformation" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-PATCHES-MD-ENTRY.md && grep -qE "prototype\.html|mock v4" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-PATCHES-MD-ENTRY.md && grep -qE "deploy-runbook\.md" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-PATCHES-MD-ENTRY.md && grep -qE "batches|batching|meaningful deploys" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-PATCHES-MD-ENTRY.md && grep -qE "master bounty|closeout" /home/ubuntu/skynet/.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-PATCHES-MD-ENTRY.md && cd /home/ubuntu/skynet && git log -1 --name-only --format="%s" 2>/dev/null | grep -qE "phase-13.*docs|build-verify|UAT|patch"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/13-.../13-PATCHES-MD-ENTRY.md`
    - Contains a patch number reference (e.g., "patch #140" or "patch NNN")
    - Contains "Skynet" AND "Ship-of-Theseus" AND "lift-from-mock"
    - Enumerates commit list from Plans 01-05 with placeholders or actual SHAs
    - References the master bounty at `~/.claude/identities/tina/bounties/skynet-transformation/` (NOT a sibling bounty)
    - References the mock at `~/.claude/identities/tina/bounties/skynet-transformation/prototype.html`
    - References `~/.claude/identities/tina/deploy-runbook.md` as authoritative deploy source
    - Includes batching rule reference
    - Includes master bounty closeout note ("Ship-of-Theseus movement complete after Ashley UAT")
    - No "Co-Authored-By" trailer in the entry text (fork convention)
    - One commit landed touching only the 3 Phase 13 docs files
    - `git status --porcelain` is empty after the commit
    - No files under `src/` are in the commit's file list
  </acceptance_criteria>
  <done>Patch entry drafted with master-bounty-closeout note; all three Phase 13 docs deliverables committed in one docs commit; Phase 13 is code-complete-pending-Ashley-UAT-and-deploy-greenlight; the Ship-of-Theseus movement (Phases 11+12+13) is one Ashley UAT away from complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-verify-log → Ashley-deploy-decision | The build-verify log's PASS status is the primary input Ashley uses to decide whether to run the deploy. A FALSE PASS (log claims tsc-clean but tsc actually fails) would land a broken deploy. Task 1 mitigates by running the commands live and capturing verbatim output. |
| UAT-checklist → Ashley-walkthrough | The UAT checklist is what Ashley walks post-deploy. Missing an observation (e.g., forgot to include SHAPE-06 pretty-view interior parity probe) risks a scope violation sneaking through. Task 2 mitigates by mapping every UAT item to a SHAPE-XX requirement. |
| UAT-runbook → authoritative deploy source | The Post-UAT deploy runbook cites `~/.claude/identities/tina/deploy-runbook.md` as the ONLY authoritative source. The fork's CLAUDE.md 15-min deadman reference is stale. Task 2 explicitly names the stale reference. |
| patches-md-entry → fork-catalog-integrity | Tina's `skynet-patches.md` is the authoritative catalog. A stale patch entry (wrong SHAs, wrong file counts) would leak into the fork's provenance history. Task 3 templates SHA placeholders — Tina fills at paste time. |
| patches-md-entry → master-bounty-not-sibling | Per CONTEXT.md § meta-lesson, sibling bounty spawning is the failure pattern that caused Tina to lose the Skynet vision. Task 3 explicitly references the MASTER bounty `skynet-transformation` and does NOT reference or suggest a sibling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05-01 | Tampering (false PASS in build-verify) | 13-BUILD-VERIFY-LOG.md | mitigate | Task 1 runs tsc/vitest/build LIVE and captures verbatim output. Grep gates for SHAPE-01..07 mechanically detect regressions. |
| T-13-05-02 | Denial of Service (missed UAT observation → SHAPE-06 scope violation ships) | 13-UAT-CHECKLIST.md | mitigate | Task 2 acceptance criteria requires at least one UAT observation per SHAPE-XX requirement AND explicit SHAPE-06 scope-violation route-backs in the failure table. |
| T-13-05-03 | Tampering (following stale deploy procedure) | fork CLAUDE.md 15-min deadman reference | mitigate | Task 2's Post-UAT deploy runbook cites deploy-runbook.md as authoritative AND explicitly names fork CLAUDE.md's 15-min deadman line as stale/retired 2026-07-21. |
| T-13-05-04 | Denial of Service (deploy without check-before-recreate) | Compose file image line | mitigate | Task 2's Post-UAT deploy runbook includes the check-before-recreate grep+sed one-liner verbatim. |
| T-13-05-05 | Repudiation (sibling bounty spawning risk — Ship-of-Theseus fragmentation) | 13-PATCHES-MD-ENTRY.md | mitigate | Task 3 acceptance criteria explicitly references master bounty (`~/.claude/identities/tina/bounties/skynet-transformation/`) and does NOT reference any sibling. This closes the meta-lesson from CONTEXT.md at the phase-boundary artifact level. |
| T-13-05-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this plan. |
</threat_model>

<verification>
- 13-BUILD-VERIFY-LOG.md exists with tsc + vitest + build evidence and all grep hygiene gates (Task 1 grep gates)
- Section 4 grep gates cover SHAPE-01..SHAPE-07 with expected counts + the final Skynet-theme-class purge gate + SHAPE-06 scope-boundary gate
- 13-UAT-CHECKLIST.md exists with Desktop + Mobile + Cross-viewport + Failure-route-back + Post-UAT deploy runbook sections (Task 2 grep gates)
- Post-UAT deploy runbook cites deploy-runbook.md, notes stale fork CLAUDE.md deadman, references batching rule + check-before-recreate one-liner
- 13-PATCHES-MD-ENTRY.md exists with patch entry draft referencing master bounty (Task 3 grep gates)
- Master bounty closeout note included in both UAT-CHECKLIST and PATCHES-MD-ENTRY (Ship-of-Theseus movement completes on Ashley UAT)
- One commit landed touching only the 3 Phase 13 docs files (Task 3 acceptance criteria)
- No source files under `src/` modified in this plan
</verification>

<success_criteria>
- SHAPE-06 satisfied at documentation level: build-verify log's SHAPE-06 scope-boundary gate proves no edits under pretty-view/components/ssh/terminal
- SHAPE-07 satisfied at documentation level: build-verify log's tsc/vitest/build all clean
- Phase 13 is code-complete-pending-Ashley-UAT-and-deploy-greenlight
- Ashley has a concrete UAT checklist covering SHAPE-01..SHAPE-07 for post-deploy verification
- Ashley has an authoritative deploy runbook citation that will not misdirect
- Tina has a paste-ready patch entry for skynet-patches.md that references the master bounty (not a sibling)
- The Ship-of-Theseus movement (Phases 11+12+13) is one Ashley UAT away from complete
</success_criteria>

<output>
Create `.planning/phases/13-skynet-transformation-conversation-list-lift-from-mock/13-05-SUMMARY.md` when done.
</output>
