---
phase: 03-session-changeover-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/claude-session/claude-session-server.ts
autonomous: true
requirements:
  - CHANGEOVER-01
  - CHANGEOVER-02
  - CHANGEOVER-04
  - CHANGEOVER-05
must_haves:
  truths:
    - "When a client's currently-tailed session's JSONL emits a line matching the `<command-name>/exit</command-name>` marker, the backend transitions per-connection state to `holding` and sends one `{type:\"session_holding\"}` WS frame within one tail line of the marker arriving (Layer 1 raw-line scan, edge-triggered, sub-second latency per D-30 hard lock)"
    - "The existing 3s ticker runs a full `discoverClaudeSession(sshConn, tmuxSession)` call every tick and compares the resolved `sessionFile` path against the currently-tailed path; when they differ, the backend transitions to `active_new`, stops the old tail, clears all buffered per-session state, starts a new tail on the new file, and sends `{type:\"session_changed\", newSessionFile}` (Layer 2 discovery-repoll backstop per D-30; covers SIGTERM-fallback AND recover-in-different-cwd)"
    - "If the ticker detects a changed `sessionFile` without a prior `/exit` seen (SIGTERM-fallback path), the backend emits `session_holding` immediately followed by `session_changed` on the SAME tick — never `session_changed` alone"
    - "If `holding` persists for N consecutive discovery ticks without a new `sessionFile` appearing (N=15 = 45s per CONTEXT.md suggestion), the backend emits a single terminal `{type:\"inactive\", reason:\"holding_timeout\"}` frame, stops all polling and tailing, and transitions to `dead` (does not auto-reconnect; client falls through to FALLBACK-01)"
    - "On `session_changed`, ALL buffered per-session state resets before the new tail starts: `harnessTasksLastSerialized` → null, `backgroundedAgents` map cleared + `backgroundedAgentsLastSerialized` → `\"[]\"`, `pendingPlans` cleared + `pendingPlansLastSerialized` → `\"null\"`, `hasSeenExit` → false, `holdingTicks` → 0, `currentSessionFile` → new path, `sessionIdFromFile` → new UUID basename. The harness-tasks poller is torn down and (if the new UUID validates) restarted against the NEW sessionId so it queries `~/.claude/tasks/<newUUID>/` rather than staying stuck on the old UUID (BLOCKER fix from plan-checker 2026-07-18)"
    - "The WebSocket is NEVER torn down by the backend on session change — only `tailHandle.stop()` and a fresh `tailSessionFile` call happen; `sshConn` is preserved so the same connection multiplexes the new tail + the same ticker"
    - "Handles Nelly's recover-in-different-cwd case (2026-07-15): same session UUID moves projects subdir → `discoverClaudeSession` returns a new `sessionFile` path → detected identically to a recycle, no special-case branch needed (CHANGEOVER-05). In this case the UUID is preserved so `setupHarnessTasksPoller` is a no-op restart to the same UUID — still safe (idempotent teardown+start)"
    - "A structured log entry lands at INFO on each state transition — `claude_session_holding`, `claude_session_changed`, `claude_session_dead` — with the `oldSessionFile` / `newSessionFile` where applicable, mirroring the existing `claude_session_pane_switch` / `claude_session_tail_start` log shape"
  artifacts:
    - path: "src/backend/claude-session/claude-session-server.ts"
      provides: "Layer 1 raw-line scan for `/exit` marker in the tail's onLine callback; Layer 2 discovery repoll branch inside a new 3s poller (co-located with context-% and harness-tasks pollers); per-connection state extensions (hasSeenExit, holdingTicks, currentSessionFile, sessionIdFromFile); state-transition helper functions (transitionToHolding, transitionToActiveNew, transitionToDead); tasks-poller setup helper (setupHarnessTasksPoller) that transitionToActiveNew calls to rebind the poller command to the NEW sessionId (BLOCKER fix); teardown-and-restart-tail path that preserves sshConn"
      contains: "session_holding"
    - path: "src/backend/claude-session/claude-session-server.ts"
      provides: "The WS event emit sites for the two new frame types `{type:\"session_holding\"}` and `{type:\"session_changed\", newSessionFile}`, and the terminal `{type:\"inactive\", reason:\"holding_timeout\"}` on dead transition"
      contains: "session_changed"
  key_links:
    - from: "session-file-tail.ts onLine callback in claude-session-server.ts"
      to: "raw-line exit-marker detector"
      via: "if line includes '<command-name>/exit</command-name>' AND state === 'active' → transitionToHolding()"
      pattern: "command-name>/exit</command-name"
    - from: "existing 3s ticker in claude-session-server.ts"
      to: "discoverClaudeSession() repoll"
      via: "additional branch in the setInterval body alongside context-% and harness-tasks; single in-flight guard to prevent overlap; compares result.sessionFile !== currentSessionFile"
      pattern: "discoverClaudeSession"
    - from: "transitionToActiveNew"
      to: "tailSessionFile restart"
      via: "tailHandle.stop() → clear buffered state → tailHandle = tailSessionFile(sshConn, newSessionFile, onLine, onError)"
      pattern: "tailSessionFile"
    - from: "transitionToActiveNew"
      to: "harness-tasks poller rebind against new UUID"
      via: "derive newSessionIdFromFile from newSessionFile basename → setupHarnessTasksPoller(newSessionIdFromFile) → clears existing timer + recomputes tasksCmd + starts fresh setInterval (BLOCKER fix — old code baked the initial UUID into the shell command forever)"
      pattern: "setupHarnessTasksPoller"
    - from: "state machine"
      to: "WS client"
      via: "ws.send(JSON.stringify({type:'session_holding'})) and ws.send(JSON.stringify({type:'session_changed', newSessionFile}))"
      pattern: "session_holding|session_changed"
---

<objective>
Land the backend half of session changeover detection: the two-layer detector (raw-line `/exit` scan + discovery repoll backstop), the per-connection state machine (`active` → `holding` → `active_new` → `active`, with `holding` → `dead` timeout), the tear-down-and-restart-tail path that reuses the existing SSH connection, the harness-tasks-poller rebind against the new sessionId so tasks for the fresh session actually surface (BLOCKER fix from plan-checker 2026-07-18), and the two new outbound WS event types (`session_holding`, `session_changed`) plus the terminal `holding_timeout` inactive frame.

This is a single-file plan against `src/backend/claude-session/claude-session-server.ts`. All new behavior LAYERS onto the existing pane WS handler alongside patch #43's tail lifecycle, patch #52's context-% and harness-tasks pollers, and patches #61/#63's raw-line scans — none of them are being rewritten, replaced, or moved. The executor MUST preserve every existing behavior byte-for-byte and slot the new code at the natural extension points identified below.

Delivers CHANGEOVER-01 (backend detection half), CHANGEOVER-02 (two-layer detection: edge-triggered raw-line + discovery-repoll backstop), CHANGEOVER-04 (backend-side per-session state reset INCLUDING the harness-tasks poller rebind), and CHANGEOVER-05 (recover-in-different-cwd works because the same `discoverClaudeSession` call handles it — no special-case branch needed; UUID is preserved in this case so the tasks-poller rebind is a no-op idempotent restart, still safe). CHANGEOVER-03 (holding-band UI, no WS teardown) is DELIVERED HERE on the backend side (no WS teardown — this plan is where that constraint holds); the frontend banner half lives in Plan 03-02. The client-side portion of CHANGEOVER-04 (frontend messages/harnessTasks/contextPct reset) lives in Plan 03-02.

Purpose: Close the "pretty-view stops updating after `/id reset`" gap. Today, when Ashley runs `/id reset`, supervisor kills claude, a new claude starts writing to a new .jsonl, and pretty view keeps tailing the DEAD file forever — nothing appears, no error, no signal. This plan makes the backend notice within 1s (raw-line) or 5s (repoll backstop) and switch tails cleanly. Additionally: the fresh session's harness tasks (from `~/.claude/tasks/<newUUID>/`) surface correctly because the poller command is rebound to the new UUID rather than staying stuck on the pre-recycle UUID (the BLOCKER the plan-checker caught).

Output: The backend WS bridge on port 30011 correctly detects and hands off across recycle, SIGTERM-fallback, and recover-in-different-cwd. Build clean; ready for the Plan 03-02 frontend consumer.

Extraction discipline: The executor MUST read `src/backend/claude-session/claude-session-server.ts` END-TO-END before editing. All state variables must be co-located in the natural spot alongside the existing per-connection `let` declarations (~lines 126-164). All new code inside the ticker MUST slot next to the existing context-% and harness-tasks branches, not before them and not after `teardownPane`. The raw-line scan MUST slot inside the existing `try { const obj = JSON.parse(line) as ...` block in the tail's onLine callback (~lines 560-702) — either as a NEW sibling raw-string check BEFORE the JSON.parse (cheaper) or inside the existing user-content-block loop (co-located with patches #61/#63). Prefer the pre-parse string check: `if (line.includes("<command-name>/exit</command-name>"))` — patch #61's precedent is inside the try-block because it needed the parsed content array; ours only needs the raw string.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-session-changeover-detection/03-CONTEXT.md
@.planning/phases/01-live-session-stream-to-browser-read-only-pretty-view/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend per-connection state and add state-transition helpers alongside the existing pane WS handler</name>
  <read_first>
    - src/backend/claude-session/claude-session-server.ts — read END-TO-END (752 lines). This is a single-file plan and the executor MUST understand the existing shape before editing. Focus especially on:
      - Lines 126-164 — existing per-connection `let` declarations (sshConn, tailHandle, contextPctTimer, contextPctInFlight, harnessTasksTimer, harnessTasksInFlight, harnessTasksLastSerialized, backgroundedAgents, backgroundedAgentsLastSerialized, pendingPlans, pendingPlansLastSerialized, stopped). New state slots in RIGHT AFTER `stopped = false`.
      - Lines 166-194 — `teardownPane` helper. Its shape drives the reset logic for state-transition helpers.
      - Lines 240-336 — the `ws.on("message")` connectToPane handler. Understand where `sshConn` gets assigned (line 332), where the discovery result branches (line 338), and where the FALLBACK-01 inactive-exit path lives (line 348) so the new state machine doesn't tangle with it.
      - Lines 338-346, 366-384 — the discovery + tail-start branch. The new state machine's INITIAL `currentSessionFile` gets set here after the successful `discoverClaudeSession` returns `active` and before `tailSessionFile` is called.
      - Lines 460-537 — the EXISTING harness-tasks poller. Note carefully that (a) `sessionIdFromFile` is derived from `result.sessionFile.basename` at CONNECTION-SETUP TIME as a `const`, (b) `tasksCmd` uses that UUID INLINE in the shell string as a `const`, and (c) the poller is wrapped in `if (UUID_RE.test(sessionIdFromFile))`. Task 3 will refactor this whole block into a helper (`setupHarnessTasksPoller(newSessionIdFromFile: string)`) so `transitionToActiveNew` (this task) can call it to rebind against the NEW UUID — the BLOCKER the plan-checker flagged.
    - src/backend/claude-session/session-file-discovery.ts — read END-TO-END (112 lines). The `discoverClaudeSession(conn, sessionName)` signature and result shape drive Layer 2's repoll. The `ClaudeSessionDiscoveryResult` discriminated union is what the ticker compares against.
    - src/backend/claude-session/session-file-tail.ts — read END-TO-END (143 lines). The `TailHandle` shape (`{ stop: () => void }`) and the `tailSessionFile(conn, absolutePath, onLine, onError): TailHandle` signature are what the state-transition helpers call. Confirm that `tailSessionFile` can be called AGAIN on the same `conn` after a prior `tailHandle.stop()` — ssh2 supports multiple concurrent exec channels so this is safe; the new tail becomes a new exec on the same connection.
  </read_first>
  <files>src/backend/claude-session/claude-session-server.ts</files>
  <action>
Extend the existing per-connection state block (~ line 164, immediately after `let stopped = false;`) with the new state-machine fields. Then define three helper functions co-located with the existing `teardownPane` helper (~ line 166) that encapsulate the state transitions.

**New per-connection `let` declarations** (add immediately after `let stopped = false;`, ~line 164):

```
// Phase 3 session-changeover state machine. Per D-30 (two-layer detection):
// `active`  — currently tailing a live session; ticker also polls context-% + tasks.
// `holding` — recycle in progress: /exit was seen OR discovery repoll showed a
//             changed sessionFile, but the new tail has not started yet
//             (Layer 1 fires this on `/exit`; Layer 2 fires it same-tick when
//             it spots a changed file with no prior /exit).
// `dead`    — holding timed out (~45s); one final `{type:"inactive"}` was sent
//             and all pollers/tail were stopped. WS remains open so the client
//             can render FALLBACK-01; no auto-restart from here.
type ChangeoverState = "active" | "holding" | "dead";
let changeoverState: ChangeoverState = "active";
let currentSessionFile: string | null = null;    // set on first discovery success and each session_changed
let sessionIdFromFile: string | null = null;     // UUID basename of currentSessionFile; drives harness-tasks poller cmd (BLOCKER fix)
let hasSeenExit = false;                         // Layer 1 flag; reset on session_changed
let holdingTicks = 0;                            // # of 3s ticks in `holding`; timeout at HOLDING_TIMEOUT_TICKS
let discoveryRepollInFlight = false;             // guard against slow SSH pileups (mirrors contextPctInFlight)
```

**BLOCKER fix rationale** (from plan-checker 2026-07-18) — `sessionIdFromFile` is per-connection `let` state, NOT a `const` inside the harness-tasks poller setup as it is today. The existing patch #52c code bakes the initial-connect UUID into the `tasksCmd` shell string as a `const`. After `/id reset` recycle, the tail switches to a new session file with a NEW UUID, but the tasks-poller shell command keeps querying `~/.claude/tasks/<oldUUID>/` forever → new session's tasks never surface. Promoting to `let` lets `transitionToActiveNew` (below) call `setupHarnessTasksPoller(newSessionIdFromFile)` (defined in Task 3's refactor) to teardown-and-restart the poller against the new UUID. CHANGEOVER-05's recover-in-different-cwd case is UUID-preserving so this is a no-op restart — still safe (idempotent teardown+start).

Add module-level constant near the existing `CONTEXT_PCT_INTERVAL_MS` (~line 410) OR at file top with a comment — planner's call, but keep it near the other tuning constants for discoverability:

```
// Holding timeout: 15 * 3s = 45s. Per D-31 and CONTEXT.md § holding timeout —
// Nelly's timing note: "new .jsonl appears within ~5s; fully-loaded identity
// ~30-70s later." 45s catches the "no new file appeared" degenerate case
// (recycle failed to relaunch anything) while giving normal recycles headroom.
// If this tunes out on live use, bump it up by editing this constant only —
// no state machine change required.
const HOLDING_TIMEOUT_TICKS = 15;
```

**State-transition helpers** (define AFTER the existing `teardownPane` declaration, ~line 195). All three helpers close over the same `let` variables and `ws`, so they must be defined inside the WS `connection` callback like `teardownPane`, NOT at module scope. Read the existing `teardownPane` shape before writing these — they follow the same closure pattern.

Helper 1: **`transitionToHolding(reason: "exit_marker" | "discovery_diff")`**. Emits the `session_holding` WS frame IFF state is currently `active` (guards against double-fire when Layer 1 and Layer 2 race — /exit lands on the tail's onLine milliseconds before the ticker's repoll notices the file changed; the ticker's transitionToHolding call must be idempotent). Sets `changeoverState = "holding"` and `holdingTicks = 0`. Logs at INFO with `operation: "claude_session_holding"`, `reason`, `userId`, `hostId`, `tmuxSession`, `currentSessionFile` (as `oldSessionFile`).

Helper 2: **`transitionToActiveNew(newSessionFile: string)`**. Called by the ticker's discovery-repoll branch when a changed file is detected. Must:
- Capture `oldSessionFile = currentSessionFile;` and `oldSessionIdFromFile = sessionIdFromFile;` FIRST (used for logging and for the tasks-poller rebind gate).
- Stop the existing tail (`tailHandle?.stop(); tailHandle = null;`).
- Clear ALL buffered per-session state (this is the CHANGEOVER-04 backend-side reset):
  - `harnessTasksLastSerialized = null;`
  - `backgroundedAgents.clear(); backgroundedAgentsLastSerialized = "[]";`
  - `pendingPlans.clear(); pendingPlansLastSerialized = "null";`
  - `hasSeenExit = false;`
  - `holdingTicks = 0;`
- Derive `newSessionIdFromFile` from `newSessionFile` using the same slug logic patch #52c uses (~ line 482-486):

    ```
    const newSessionIdFromFile = newSessionFile
      .replace(/\\/g, "/")
      .split("/")
      .pop()!
      .replace(/\.jsonl$/, "");
    ```

- Update `currentSessionFile = newSessionFile;` and `sessionIdFromFile = newSessionIdFromFile;`.
- **BLOCKER fix — rebind the harness-tasks poller against the new UUID.** Call `setupHarnessTasksPoller(newSessionIdFromFile);` (defined in Task 3). The helper internally:
  - Clears any existing `harnessTasksTimer` (idempotent — no-op if null).
  - Guards `if (!UUID_RE.test(newSessionIdFromFile)) return;` — a non-UUID basename means no tasks dir, poller stays torn down (matches existing patch #52c behavior).
  - Recomputes `tasksCmd` inline with the NEW UUID and starts a fresh `setInterval`.
  This is safe for CHANGEOVER-05's recover-in-different-cwd case: `newSessionIdFromFile === oldSessionIdFromFile` because the UUID is preserved on `--resume`; the teardown-and-start against the same UUID is a no-op-equivalent (poller keeps polling the same directory). No conditional needed on `oldSessionIdFromFile !== newSessionIdFromFile` — the helper is idempotent.
- Set `changeoverState = "active";`.
- Send `{type:"session_changed", newSessionFile}` frame. DO NOT re-emit a `session` metadata frame — Plan 03-02's client handler for `session_changed` does not need the pid (unused client-side per verified reading of PrettyView.tsx). If a future feature needs the pid, extend `SessionChangedEvent` to carry it — but that's out-of-scope for this phase.
- Log at INFO: `operation: "claude_session_changed"`, `userId`, `hostId`, `tmuxSession`, `oldSessionFile` (captured pre-mutation), `newSessionFile`, and `sessionIdChanged: oldSessionIdFromFile !== newSessionIdFromFile` (boolean for post-deploy debugging — helps distinguish recycle from recover-in-different-cwd in the logs).
- Call `tailSessionFile(sshConn, newSessionFile, onLine, onError)` to restart the tail. The onLine and onError callbacks are the SAME functions used for the initial tail (~lines 542, 723) — do NOT duplicate them; Task 2 extracts them into named local closures inside the WS handler.

Helper 3: **`transitionToDead(reason: "holding_timeout")`**. Called by the ticker when `changeoverState === "holding" && ++holdingTicks >= HOLDING_TIMEOUT_TICKS`. Must:
- Emit one terminal `{type:"inactive", reason:"holding_timeout"}` frame.
- Call `teardownPane()` (stops both existing pollers + the discovery-repoll timer added in Task 3 + the tail + closes the SSH connection). The pane is dead, no further work will happen, releasing the SSH connection matches the initial-inactive-exit path at line 358-362.
- Set `changeoverState = "dead";`.
- Log at INFO: `operation: "claude_session_dead"`, `reason`, `userId`, `hostId`, `tmuxSession`, `currentSessionFile` (final value).

CRITICAL CONSTRAINT: All three helpers must be idempotent-safe against `stopped === true`. If the WS closed mid-transition, no ws.send calls should throw; wrap in try/catch matching the existing pattern (see line 522-528). Reference lines: 244-253 shows the `if (stopped)` idempotency guard the whole file uses.

**Declaration order note (W1 fix from plan-checker)** — the canonical top-to-bottom order in the WS `connection` callback body is:

    state vars (this task)
    → teardownPane (existing)
    → const onLine (Task 2)
    → const onError (Task 2)
    → setupHarnessTasksPoller (Task 3)
    → transitionToHolding (this task)
    → transitionToActiveNew (this task; references onLine, onError, setupHarnessTasksPoller)
    → transitionToDead (this task; references teardownPane)
    → ws message handler body (unchanged shape)

Task 2 and Task 3 place their declarations to satisfy this order without needing conditional hoisting fallbacks. Follow this order exactly; do not deviate. TypeScript's function-hoisting and closure-scope rules make this order compile without use-before-declaration errors because all cross-references happen at call-time inside async callbacks (discoveryRepollTimer, tail's onLine), never at synchronous handler-body execution.

Do NOT rename any existing variables. Do NOT reorder any existing code. Do NOT move `teardownPane`. All additions are strictly additive.
  </action>
  <verify>
    <automated>grep -q 'type ChangeoverState = "active" | "holding" | "dead"' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q 'let changeoverState: ChangeoverState = "active"' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q 'let currentSessionFile: string | null = null' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q 'let sessionIdFromFile: string | null = null' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q 'let hasSeenExit = false' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q 'let holdingTicks = 0' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q 'let discoveryRepollInFlight = false' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q 'HOLDING_TIMEOUT_TICKS' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q 'transitionToHolding' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q 'transitionToActiveNew' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q 'transitionToDead' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q 'setupHarnessTasksPoller(newSessionIdFromFile)' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q '"claude_session_holding"' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q '"claude_session_changed"' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q '"claude_session_dead"' src/backend/claude-session/claude-session-server.ts</automated>
  </verify>
  <acceptance_criteria>
    - The `ChangeoverState` type union exists and is `"active" | "holding" | "dead"`.
    - All SIX new per-connection `let` declarations exist inside the WS `connection` callback (NOT at module scope) alongside the existing `let` block: `changeoverState`, `currentSessionFile`, `sessionIdFromFile`, `hasSeenExit`, `holdingTicks`, `discoveryRepollInFlight`.
    - The `HOLDING_TIMEOUT_TICKS` constant exists and is set to 15.
    - Three transition helper functions (`transitionToHolding`, `transitionToActiveNew`, `transitionToDead`) are defined inside the WS `connection` callback (they close over `changeoverState`, `holdingTicks`, `currentSessionFile`, `sessionIdFromFile`, `hasSeenExit`, `ws`, etc.).
    - `transitionToActiveNew` clears the FIVE per-session buffered-state fields (`harnessTasksLastSerialized`, `backgroundedAgents`, `backgroundedAgentsLastSerialized`, `pendingPlans`, `pendingPlansLastSerialized`), resets `hasSeenExit` and `holdingTicks`, updates `currentSessionFile` AND `sessionIdFromFile`, calls `setupHarnessTasksPoller(newSessionIdFromFile)` for the tasks-poller rebind (BLOCKER fix), and calls `tailSessionFile` for a tail restart.
    - `transitionToActiveNew`'s log line includes a `sessionIdChanged: boolean` field distinguishing recycle from recover-in-different-cwd.
    - All three transitions log at INFO with the specified `operation:` value and include `oldSessionFile` / `newSessionFile` context where applicable.
    - No existing state variables were renamed, no existing code was reordered, and `teardownPane` was not moved.
    - `tsc --noEmit` passes: `npx tsc --noEmit 2>&1 | grep -v -E "(node_modules|\.d\.ts)" | grep -i "claude-session-server" | grep -i error` returns nothing.
  </acceptance_criteria>
  <done>Per-connection state extended with `sessionIdFromFile` (BLOCKER-fix state); three transition helpers defined; `transitionToActiveNew` explicitly calls `setupHarnessTasksPoller` for the poller rebind against the new UUID. Ready to wire the two detection layers into the tail callback (Task 2) and the ticker (Task 3). Nothing yet fires because the helpers are unreferenced.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Layer 1 — raw-line `/exit` scan in the tail onLine callback, initial currentSessionFile+sessionIdFromFile assignment, and named onLine/onError closures for reuse in transitionToActiveNew</name>
  <read_first>
    - src/backend/claude-session/claude-session-server.ts — lines 366-384 (the session-metadata emit and tail-start sequencing right after discovery), lines 482-486 (the `sessionIdFromFile` derivation slug logic — this task also does the initial assignment of `sessionIdFromFile` at connect time using the same logic), lines 539-744 (the `tailSessionFile(...)` call, its inline onLine callback with the patches #61/#63 raw-line scans, and its inline onError callback). Understand:
      - Where `result.sessionFile` becomes available — line 342 (after `await discoverClaudeSession(...)`).
      - Where the initial WS `{type:"session"...}` frame is sent — line 368.
      - The onLine callback shape (line 542-722) — arrow function, `if (stopped || ws.readyState !== WebSocket.OPEN) return;` guard first, then the patches #61/#63 raw-line JSON.parse scan block, then the `parseSessionLine(line)` + parsed.kind === "message" emit.
      - The onError callback shape (line 723-742) — logs the error and emits `tail_error` if the WS is open.
  </read_first>
  <files>src/backend/claude-session/claude-session-server.ts</files>
  <action>
Two changes in this task, both in the same code neighborhood (~line 366 through end of the WS message handler).

**Change 1 — Set `currentSessionFile` AND `sessionIdFromFile` at initial-tail-start time.**

Immediately after the successful `discoverClaudeSession` result (~line 384, right after the `Starting Claude session tail` log entry), set:

```
currentSessionFile = result.sessionFile;
sessionIdFromFile = result.sessionFile
  .replace(/\\/g, "/")
  .split("/")
  .pop()!
  .replace(/\.jsonl$/, "");
```

This gives the ticker's discovery-repoll branch a baseline to compare against for `currentSessionFile`, and gives Task 3's harness-tasks poller a UUID to use. Without setting `sessionIdFromFile` here, the initial tasks-poller setup in Task 3 would have to re-derive it — coupling the two tasks. Setting it once here keeps a single source of truth (matches the BLOCKER-fix state-var approach from Task 1).

Without the `currentSessionFile = ...` line, the first ticker comparison would treat the current file as "changed" and immediately fire `transitionToActiveNew` on itself.

**Change 2 — Extract the tail's onLine and onError callbacks as named local functions AND wire the Layer 1 raw-line `/exit` scan.**

The existing code (~lines 539-744) inlines the onLine and onError callbacks directly in the `tailSessionFile(conn, result.sessionFile, (line) => {...}, (err) => {...})` call. `transitionToActiveNew` (defined in Task 1) needs to call `tailSessionFile(sshConn, newSessionFile, onLine, onError)` — so we must extract these two callbacks to named `const`s within the WS handler closure so both call sites can reference them.

**Declaration placement** (W1 fix from plan-checker — canonical single order, no conditional hoisting fallbacks). Per the canonical order named in Task 1's action, `const onLine` and `const onError` are declared AFTER `teardownPane` (existing, ~line 194) and BEFORE `setupHarnessTasksPoller` (Task 3, ~post-line-537) / the transition helpers (Task 1, ~line 195). Concretely: place both `const` declarations immediately after `teardownPane` in the connection callback body. The transition helpers from Task 1 (which appear later in the file due to the order in Task 1's action) will reference `onLine`/`onError` inside their bodies, but those references execute at call-time (inside async setInterval callbacks), long after the synchronous handler body has finished declaring everything. TypeScript's `const`-scoping and JavaScript's closure semantics handle this correctly given the declaration order.

Refactor the tail-start code so the shape becomes:

```
// Near the top of the connection callback, right after teardownPane:
const onLine = (line: string) => {
  if (stopped || ws.readyState !== WebSocket.OPEN) return;

  // Phase 3 Layer 1: raw-line /exit marker scan (BEFORE JSON.parse for
  // cheapness). Empirical (verified 2026-07-18): /exit lands as a
  // type:"user" turn with content
  // "<command-name>/exit</command-name>\n            <command-message>exit</command-message>..."
  // Sub-second edge-triggered detection of graceful recycle. The
  // discovery repoll in the ticker (Task 3) catches SIGTERM-fallback and
  // recover-in-different-cwd. See CONTEXT.md D-30 for two-layer rationale.
  if (
    !hasSeenExit &&
    changeoverState === "active" &&
    line.includes("<command-name>/exit</command-name>")
  ) {
    hasSeenExit = true;
    transitionToHolding("exit_marker");
    // Fall through — the parser may still emit the /exit turn as a
    // message (per D on parser-not-touched: Ashley wants slash commands
    // visible). The state transition is orthogonal to whether the /exit
    // text renders as a chat bubble.
  }

  // ... existing patches #61 + #63 raw-line JSON.parse scan block ...
  // ... existing parseSessionLine + message emit block ...
};

const onError = (err: Error) => {
  // ... existing body from lines 723-742 ...
};

// Later in the connection callback, at the same site as the current
// tailSessionFile call (~line 539), replace the inline lambdas with:
tailHandle = tailSessionFile(conn, result.sessionFile, onLine, onError);
```

CRITICAL — preservation constraints:
- The bodies of onLine (patches #61/#63 raw-line JSON.parse scan; parseSessionLine + message emit) and onError (logger + tail_error emit) MUST be byte-for-byte preserved from the existing inline callbacks. Only the shape wrapping them changes (inline → named const).
- The Layer 1 scan goes FIRST inside onLine, right after the `if (stopped || ws.readyState !== WebSocket.OPEN) return;` guard. It's a raw-string check (`line.includes(...)`) — no JSON.parse needed for it — so it's cheap enough to run on every line.
- The `!hasSeenExit && changeoverState === "active"` guard prevents (a) re-firing if `/exit` appears twice for any reason and (b) firing during the `holding` window (defensive; shouldn't happen because the tail should be stopped by then, but belt-and-braces).
- The comment inside the scan block explicitly notes that we fall through to the parser — Ashley's HARD LOCK from CONTEXT.md § HARD REJECTED is "keep slash commands visible." Do not add any parser-side filter.

Do NOT change the `parseSessionLine` call, the message emit, the patches #61/#63 scans, or the onError body. Do NOT change `tailSessionFile` itself in `session-file-tail.ts` — that file is explicitly out-of-scope for Phase 3.

**Order-follow discipline (W1 fix):** Task 1's action names the canonical top-to-bottom order in the connection callback body. Follow it exactly. The old plan text carried a conditional "if TypeScript complains… hoist" branch — that fallback is REMOVED per plan-checker directive. The canonical order compiles correctly on the first try; if executor sees a use-before-declaration error, they've deviated from the order and should re-check placement rather than reorder further.
  </action>
  <verify>
    <automated>grep -q 'currentSessionFile = result.sessionFile' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q 'sessionIdFromFile = result.sessionFile' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q 'command-name>/exit</command-name' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q 'hasSeenExit = true' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q 'transitionToHolding("exit_marker")' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q 'const onLine = ' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q 'const onError = ' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q 'tailHandle = tailSessionFile(conn, result.sessionFile, onLine, onError)' src/backend/claude-session/claude-session-server.ts &amp;&amp; ! grep -q 'tailHandle = tailSessionFile(\s*conn,\s*result.sessionFile,\s*(line:' src/backend/claude-session/claude-session-server.ts</automated>
  </verify>
  <acceptance_criteria>
    - `currentSessionFile = result.sessionFile;` is set immediately after the successful discovery (before or at the same site as the initial tail-start log).
    - `sessionIdFromFile` is derived from `result.sessionFile` basename using the slug logic and assigned adjacent to `currentSessionFile`.
    - The tail's onLine and onError callbacks are extracted as `const onLine = (line: string) => {...}` and `const onError = (err: Error) => {...}` inside the WS handler closure, placed immediately after `teardownPane` per the W1 canonical order.
    - The `tailSessionFile` call now passes `onLine` and `onError` by reference (not inline lambdas).
    - The Layer 1 raw-line `/exit` scan is present inside onLine, positioned right after the `if (stopped || ws.readyState !== WebSocket.OPEN) return;` guard and BEFORE the patches #61/#63 JSON.parse block.
    - The scan guard is `!hasSeenExit && changeoverState === "active" && line.includes("<command-name>/exit</command-name>")`.
    - When triggered, the scan sets `hasSeenExit = true` and calls `transitionToHolding("exit_marker")` — no `return` after the transition (falls through so the parser still emits the /exit turn as a chat bubble per Ashley's HARD LOCK).
    - The existing bodies of the patches #61/#63 raw-line scan, the `parseSessionLine` + message emit, and the onError body are byte-for-byte preserved (verify by diff against the pre-task state — the executor should verify this before commit).
    - `tsc --noEmit` still passes.
    - Compilation succeeds — `npm run build` completes without errors.
  </acceptance_criteria>
  <done>Layer 1 (raw-line `/exit` scan) is armed and edge-triggers `holding` state within one tail line of the marker landing. `currentSessionFile` and `sessionIdFromFile` baselines are set. `onLine`/`onError` are named closures that `transitionToActiveNew` (Task 1) can now reuse for the restart tail. The state machine remains stuck in `holding` forever until Task 3 wires Layer 2.</done>
</task>

<task type="auto">
  <name>Task 3: Extract setupHarnessTasksPoller helper, wire Layer 2 — discovery repoll branch as a third 3s poller, holding-timeout enforcement</name>
  <read_first>
    - src/backend/claude-session/claude-session-server.ts — lines 385-458 (the context-% poller: `contextPctTimer = setInterval(() => {...}, CONTEXT_PCT_INTERVAL_MS)` — the model for how a poller is structured with in-flight guarding, `stopped` / `ws.readyState` checks, and `finally` cleanup). Lines 460-537 (the harness-tasks poller — THIS IS THE BLOCK BEING REFACTORED INTO A HELPER PER THE BLOCKER FIX; read it end-to-end so the helper preserves the existing behavior byte-for-byte apart from the UUID being a parameter instead of a captured const). Understand that both pollers run inside the SAME `ws.on("message")` handler's `connectToPane` branch, allocated PER pane connection, cleared in `teardownPane`.
    - src/backend/claude-session/session-file-discovery.ts — the `discoverClaudeSession` return shape:
      - `{ status: "active", pid: number, sessionFile: string }`
      - `{ status: "inactive", reason: string }`
    - Task 1 output (already in the file): `changeoverState`, `currentSessionFile`, `sessionIdFromFile`, `hasSeenExit`, `holdingTicks`, `discoveryRepollInFlight`, and the three transition helpers (`transitionToHolding`, `transitionToActiveNew`, `transitionToDead`) — all in scope inside the WS message handler. `transitionToActiveNew` already contains a call to `setupHarnessTasksPoller(newSessionIdFromFile)` which this task defines.
    - Task 2 output (already in the file): the named `onLine` / `onError` closures that `transitionToActiveNew` reuses, plus the initial `sessionIdFromFile` assignment.
  </read_first>
  <files>src/backend/claude-session/claude-session-server.ts</files>
  <action>
Two sub-changes in this task: (A) extract the existing harness-tasks poller setup into a helper the BLOCKER fix's `transitionToActiveNew` can call, (B) add a new discovery-repoll poller alongside context-% and harness-tasks.

---

**Sub-change A — Extract `setupHarnessTasksPoller(newSessionIdFromFile: string): void` helper (BLOCKER fix).**

The existing harness-tasks poller (~ lines 482-537) is currently structured as:

```
const sessionIdFromFile = result.sessionFile...pop()!.replace(/\.jsonl$/, "");
const UUID_RE = /^[a-f0-9]{8}-.../i;
if (UUID_RE.test(sessionIdFromFile)) {
  const HARNESS_TASKS_INTERVAL_MS = 3000;
  const tasksCmd = `for f in "$HOME/.claude/tasks/${sessionIdFromFile}"/*.json; do ...`;
  harnessTasksTimer = setInterval(() => { ... }, HARNESS_TASKS_INTERVAL_MS);
}
```

Refactor to extract the poller-setup half into a helper function defined per the W1 canonical order (immediately AFTER `const onError` from Task 2, and BEFORE the transition helpers from Task 1). Concretely:

```
// Constants can move to module scope OR stay inside the helper — planner's
// call. Recommend module scope for HARNESS_TASKS_INTERVAL_MS and UUID_RE
// so they're not re-allocated per call:
const HARNESS_TASKS_INTERVAL_MS = 3000;
const UUID_RE = /^[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$/i;

// (These may already exist at module scope adjacent to CONTEXT_PCT_INTERVAL_MS
// after this refactor — if they were previously function-scoped consts inside
// the pre-refactor block, move them up to module scope now.)

// Inside the connection callback body, after `const onError = ...`:
const setupHarnessTasksPoller = (newSessionIdFromFile: string): void => {
  // Teardown existing timer if any — idempotent, matches teardownPane's
  // pattern. This makes `setupHarnessTasksPoller` safe to call from
  // `transitionToActiveNew` at any state.
  if (harnessTasksTimer) {
    clearInterval(harnessTasksTimer);
    harnessTasksTimer = null;
  }
  // Also clear the last-serialized dedupe so the first emit after a
  // restart fires unconditionally (matches transitionToActiveNew's
  // reset list — but doing it here belt-and-braces means direct
  // callers of setupHarnessTasksPoller don't need to remember).
  harnessTasksLastSerialized = null;
  // No UUID → no tasks dir → no poller. Same guard patch #52c uses.
  if (!UUID_RE.test(newSessionIdFromFile)) return;

  const tasksCmd = `for f in "$HOME/.claude/tasks/${newSessionIdFromFile}"/*.json; do [ -f "$f" ] && { tr '\\n' ' ' < "$f"; echo; }; done 2>/dev/null`;
  harnessTasksTimer = setInterval(() => {
    if (stopped || ws.readyState !== WebSocket.OPEN) return;
    if (!sshConn) return;
    if (harnessTasksInFlight) return;
    harnessTasksInFlight = true;
    const connSnapshot = sshConn;
    execCommand(connSnapshot, tasksCmd)
      .then((output) => {
        // ... byte-for-byte identical body to the pre-refactor block:
        // parse JSONs, sort by id, JSON.stringify, dedupe against
        // harnessTasksLastSerialized, emit on change ...
      })
      .catch(() => { /* silent */ })
      .finally(() => { harnessTasksInFlight = false; });
  }, HARNESS_TASKS_INTERVAL_MS);
};
```

CRITICAL preservation for sub-change A:
- The body inside the `setInterval` callback (the JSON parse, sort, serialize, emit-on-change logic) MUST be byte-for-byte preserved from the pre-refactor block. Only the enclosing structure moves.
- The initial-connect call to `setupHarnessTasksPoller` REPLACES the existing inline `if (UUID_RE.test(sessionIdFromFile))` block (~lines 490-537). At the initial-connect site, delete the entire pre-refactor inline block and replace it with a single call: `setupHarnessTasksPoller(sessionIdFromFile!);` — `sessionIdFromFile` is guaranteed non-null at this point because Task 2 assigned it just above.
- The helper is idempotent (clears any existing timer first), so `transitionToActiveNew`'s call is safe even if the previous poller was still running.

Rationale (BLOCKER fix restated): before this refactor, `sessionIdFromFile` and `tasksCmd` were `const`s captured at initial-connect time. After `/id reset` recycle, the tasks-poller shell command kept querying the pre-recycle UUID's tasks directory forever. Now, `transitionToActiveNew` calls `setupHarnessTasksPoller(newSessionIdFromFile)` and the poller is torn down + restarted with the fresh UUID baked into `tasksCmd`. CHANGEOVER-04 harness-tasks reset is now real end-to-end.

---

**Sub-change B — Add Layer 2 discovery-repoll poller as a THIRD independent `setInterval`.**

**Decision — separate 3s timer OR add to existing?**

CONTEXT.md § D-30 says "layer on the existing 3s poller." The two existing pollers (context-%, harness-tasks) are separate `setInterval` calls, each with its own timer handle and in-flight guard. There are two ways to interpret "existing 3s poller":

(a) Add discovery repoll as a THIRD independent `setInterval` running at 3000ms alongside the other two. Independent guard (`discoveryRepollInFlight` — declared in Task 1). Cleared in `teardownPane`. Same timing cadence, different handle. This is the LOWEST-RISK layering — zero coupling to the other two pollers' in-flight state or callback bodies, zero refactor of existing code.

(b) Fold all three into a single 3s `setInterval` that fires all three exec-command calls in parallel. Slightly less overhead (one setInterval instead of three) but couples the three pollers together and risks that a slow discovery repoll delays the next tick's context-% + tasks refresh.

CHOICE: **Option (a) — separate `setInterval`, same cadence.** Matches the existing code's precedent (each poller owns its own timer + guard + teardown). No refactor of context-% or harness-tasks pollers. The three timers all use the same 3s cadence but drift is not a concern here — they're logically independent signals.

Add a NEW module-level constant near `HOLDING_TIMEOUT_TICKS` (Task 1) and the existing `CONTEXT_PCT_INTERVAL_MS` / `HARNESS_TASKS_INTERVAL_MS`:

```
const DISCOVERY_REPOLL_INTERVAL_MS = 3000;
```

Add a NEW per-connection `let discoveryRepollTimer: NodeJS.Timeout | null = null;` alongside `contextPctTimer` and `harnessTasksTimer` (~line 128-130).

Update `teardownPane` (~line 166-194) to clear it alongside the other two:

```
if (discoveryRepollTimer) {
  clearInterval(discoveryRepollTimer);
  discoveryRepollTimer = null;
}
```

Also reset the Phase 3 per-connection state inside `teardownPane` (so a full-pane teardown-and-reconnect starts clean — even though the connectToPane handler at line 295-304 already calls `teardownPane` before starting a new pane and re-initializes fresh):

```
changeoverState = "active";
currentSessionFile = null;
sessionIdFromFile = null;
hasSeenExit = false;
holdingTicks = 0;
discoveryRepollInFlight = false;
```

**Add the discovery-repoll poller** immediately after the `setupHarnessTasksPoller(sessionIdFromFile!);` initial-connect call (which itself REPLACED the old inline harness-tasks setup block from sub-change A), still before the `tailHandle = tailSessionFile(...)` call:

```
// Phase 3 Layer 2: discovery-repoll backstop. Runs the SAME full
// discoverClaudeSession call the initial connectToPane branch used, once per
// tick, on the same SSH connection. On change:
//   - active + new file  → transitionToHolding("discovery_diff") then
//                          transitionToActiveNew(newFile) same tick
//                          (SIGTERM-fallback path — /exit never landed)
//   - holding + new file → transitionToActiveNew(newFile)
//                          (normal recycle after /exit)
//   - holding + no file  → increment holdingTicks; if >= timeout, dead
//   - inactive (never claude again after holding) → transitionToDead via timeout
//
// Full discovery each tick (not a cached `ls -t $projects_dir/*.jsonl`) is
// LOAD-BEARING per Nelly's recover-in-different-cwd case (2026-07-15): the
// same session id can move projects subdirs, so the projects dir may not be
// the same one we started from. discoverClaudeSession walks
// pane→pid→cwd→slug fresh each tick and correctly follows the move.
// See CONTEXT.md D-30 and CHANGEOVER-05.
discoveryRepollTimer = setInterval(() => {
  if (stopped || ws.readyState !== WebSocket.OPEN) return;
  if (!sshConn) return;
  if (discoveryRepollInFlight) return;
  discoveryRepollInFlight = true;
  const connSnapshot = sshConn;
  discoverClaudeSession(connSnapshot, tmuxSession)
    .then((result) => {
      if (stopped || ws.readyState !== WebSocket.OPEN) return;
      if (changeoverState === "dead") return; // idempotent guard

      if (result.status === "active") {
        if (result.sessionFile !== currentSessionFile) {
          // File moved: recycle OR recover-in-different-cwd.
          if (changeoverState === "active") {
            // SIGTERM-fallback path: /exit never landed but the file
            // changed. Emit both holding and changed on the SAME tick.
            transitionToHolding("discovery_diff");
          }
          transitionToActiveNew(result.sessionFile);
        }
        // else: same file, still active — no state change here. If we
        // were already in `holding` AND the file matches
        // currentSessionFile, that means our pre-holding file is still
        // on disk (unusual — supervisor should have rotated it after
        // /exit). We fall through to the holdingTicks++ block below,
        // which lets the timeout progress. Do NOT reset holdingTicks
        // in this same-file branch: reset happens only in
        // transitionToActiveNew.
      } else {
        // status === "inactive": no claude in the pane right now.
        // Bare-shell gap during recycle is expected — do NOT flip to
        // dead on a single inactive tick. Only flip on holding timeout.
        // (If we were in `active` and suddenly the pane has no claude
        // AND no /exit was seen, something crashed — transition to
        // holding so the client shows the banner while we wait for a
        // recover.)
        if (changeoverState === "active") {
          transitionToHolding("discovery_diff");
        }
      }

      // W2 fix from plan-checker: the holdingTicks++ check below fires on
      // EVERY holding tick — including the same-file-active branch above
      // (where we did NOT reset holdingTicks) and the inactive branch (which
      // may have just flipped us to holding this tick). Intentional: if the
      // pre-holding file still exists but no new file has appeared for
      // HOLDING_TIMEOUT_TICKS, declare dead. A stuck same-file result
      // during holding still counts against the timeout, otherwise the
      // pane could sit in "recycling…" indefinitely on a supervisor bug.
      if (changeoverState === "holding") {
        holdingTicks++;
        if (holdingTicks >= HOLDING_TIMEOUT_TICKS) {
          transitionToDead("holding_timeout");
        }
      }
    })
    .catch(() => {
      /* Silent — same posture as the context-pct and harness-tasks
         pollers. A discovery failure on one tick shouldn't kill the
         session; the next tick tries again. */
    })
    .finally(() => {
      discoveryRepollInFlight = false;
    });
}, DISCOVERY_REPOLL_INTERVAL_MS);
```

CRITICAL PLACEMENT: This block is INDEPENDENT of the harness-tasks poller — it does not require a valid UUID (a non-UUID sessionIdFromFile just means no tasks poller runs, but discovery still needs to fire). Place it OUTSIDE any UUID-conditional (there's no such wrapping condition once sub-change A moves the tasks-poller into `setupHarnessTasksPoller`). Structural sibling of `setupHarnessTasksPoller(sessionIdFromFile!);` call, and of the context-% `setInterval` block that precedes it.

Do NOT change the context-% poller. Do NOT change `teardownPane`'s existing body except to (a) add the new clearInterval for `discoveryRepollTimer` and (b) add the six state-reset lines listed above (five Phase-3 state vars plus the new `sessionIdFromFile = null;`). Do NOT change the initial connectToPane path — the initial discovery, initial tail-start, and initial `session` metadata emit all stay identical apart from the harness-tasks setup being replaced by the `setupHarnessTasksPoller(sessionIdFromFile!)` call.

Post-task validation: manually walk the four state-machine paths through the code to confirm correctness:
1. **Happy path** — Ashley runs /id reset → next tail line contains `<command-name>/exit</command-name>` → Layer 1 fires: state=holding, hasSeenExit=true, session_holding emitted. Within 5s, ticker sees new sessionFile → transitionToActiveNew: derives newSessionIdFromFile, calls setupHarnessTasksPoller(newSessionIdFromFile) which teardown+restart the tasks poller against the new UUID, tail restart, state=active, hasSeenExit=false, session_changed emitted with sessionIdChanged:true. Frontend clears + rehydrates. Tasks for the fresh session start surfacing within one 3s tick.
2. **SIGTERM fallback** — /id reset → supervisor's graceful /exit didn't take → supervisor SIGTERMs claude → new claude starts → old tail dies naturally (file rotated). Layer 1 never fires (no /exit line). Within 5s, ticker sees new sessionFile → both transitionToHolding("discovery_diff") AND transitionToActiveNew fire same tick. Same tasks-poller rebind as (1). Two frames emitted back-to-back.
3. **Recover-in-different-cwd** — claude crashed → supervisor waits N ticks → runs `claude --resume <oldId>` in a different workdir → same session UUID, DIFFERENT projects/slug subdir. `discoverClaudeSession` walks the pane fresh each tick, returns the new path. Same code path as (2) — but newSessionIdFromFile === oldSessionIdFromFile, so setupHarnessTasksPoller is an idempotent no-op restart against the same UUID. sessionIdChanged log field is false; easy to distinguish from recycle in the logs.
4. **Dead — recycle failed to relaunch** — /id reset → supervisor kills claude → new claude never starts. Layer 1 fires (state=holding). 15 ticks pass, all inactive OR stuck-same-file. holdingTicks reaches HOLDING_TIMEOUT_TICKS (W2 fix: the same-file-during-holding branch does not reset the counter, so the countdown progresses). transitionToDead fires → inactive frame with reason "holding_timeout" → teardownPane closes everything. WS stays open (default), client renders FALLBACK-01.
  </action>
  <verify>
    <automated>grep -q 'DISCOVERY_REPOLL_INTERVAL_MS = 3000' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q 'let discoveryRepollTimer' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q 'if (discoveryRepollTimer)' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q 'clearInterval(discoveryRepollTimer)' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q 'discoverClaudeSession(connSnapshot, tmuxSession)' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q 'transitionToActiveNew(result.sessionFile)' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q 'transitionToDead("holding_timeout")' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q 'holdingTicks++' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q 'transitionToHolding("discovery_diff")' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q 'const setupHarnessTasksPoller' src/backend/claude-session/claude-session-server.ts &amp;&amp; grep -q 'setupHarnessTasksPoller(sessionIdFromFile' src/backend/claude-session/claude-session-server.ts &amp;&amp; npm run build 2>&amp;1 | tail -5 | grep -q "built in"</automated>
  </verify>
  <acceptance_criteria>
    - `DISCOVERY_REPOLL_INTERVAL_MS = 3000` constant exists.
    - `discoveryRepollTimer: NodeJS.Timeout | null = null` per-connection let exists alongside `contextPctTimer` and `harnessTasksTimer`.
    - `teardownPane` clears `discoveryRepollTimer` and resets the six Phase-3 state vars (`changeoverState`, `currentSessionFile`, `sessionIdFromFile`, `hasSeenExit`, `holdingTicks`, `discoveryRepollInFlight`).
    - `setupHarnessTasksPoller(newSessionIdFromFile: string): void` helper is defined inside the WS connection callback body (placed immediately after `const onError = ...` per the W1 canonical order, before the transition helpers).
    - The helper clears any existing `harnessTasksTimer`, clears `harnessTasksLastSerialized`, guards on `UUID_RE.test(...)`, recomputes `tasksCmd` with the new UUID inline, and starts a fresh `setInterval` with the byte-for-byte-preserved body.
    - The initial-connect call site now uses `setupHarnessTasksPoller(sessionIdFromFile!)` instead of the pre-refactor inline `if (UUID_RE.test(sessionIdFromFile)) { const HARNESS_TASKS_INTERVAL_MS = ...; const tasksCmd = ...; harnessTasksTimer = setInterval(...); }` block.
    - `HARNESS_TASKS_INTERVAL_MS` and `UUID_RE` are moved to module scope (or preserved as function-scope constants inside the helper — planner's discretion, matches other tuning-constant placement).
    - A third `setInterval` block exists, calling `discoverClaudeSession(connSnapshot, tmuxSession)`.
    - The setInterval body handles all four state-machine paths correctly (verified by manually stepping through in the plan-checker review):
      - active + new file → transitionToHolding("discovery_diff") + transitionToActiveNew (SIGTERM-fallback).
      - holding + new file → transitionToActiveNew.
      - holding + no new file (inactive OR stuck-same-file) → holdingTicks++; if >= timeout → transitionToDead("holding_timeout"). W2 fix: same-file-during-holding branch does NOT reset holdingTicks.
      - active + inactive (no claude found) → transitionToHolding("discovery_diff") (defensive; treated as recover-pending).
    - The W2 fix comment ("Note: the holdingTicks++ check below fires on EVERY holding tick, including this same-file-active branch...") is present in the poller body.
    - The discovery poller has an in-flight guard using `discoveryRepollInFlight`.
    - The .catch() is silent (matches context-% and harness-tasks posture).
    - `npm run build` completes without TypeScript errors.
  </acceptance_criteria>
  <done>Backend session-changeover detection is fully wired end-to-end for all four state-machine paths. Layer 1 catches the ~80% graceful-recycle case within 1s; Layer 2 catches SIGTERM-fallback, recover-in-different-cwd, and holding-timeout dead cases within 3-45s. The harness-tasks poller correctly rebinds to the fresh session's UUID on every `transitionToActiveNew` (BLOCKER fix) so CHANGEOVER-04 is real end-to-end for tasks. `session_holding` and `session_changed` WS frames are emitted at the correct edges. `holding_timeout` inactive frame lands on true failure. Frontend (Plan 03-02) now has the wire-level events it needs.</done>
</task>

</tasks>

<verification>
- `changeoverState`, `currentSessionFile`, `sessionIdFromFile`, `hasSeenExit`, `holdingTicks`, `discoveryRepollInFlight`, `discoveryRepollTimer` all declared alongside the existing per-connection state, inside the WS `connection` callback (not module scope).
- `HOLDING_TIMEOUT_TICKS = 15` and `DISCOVERY_REPOLL_INTERVAL_MS = 3000` constants added; `HARNESS_TASKS_INTERVAL_MS` and `UUID_RE` moved to module scope or kept inside the setupHarnessTasksPoller helper.
- `setupHarnessTasksPoller(newSessionIdFromFile: string): void` helper defined; called from both the initial-connect path (replacing the inline block) and from `transitionToActiveNew`.
- Three transition helpers (`transitionToHolding`, `transitionToActiveNew`, `transitionToDead`) defined inside the WS handler closure; `transitionToActiveNew` derives `newSessionIdFromFile` from `newSessionFile` and calls `setupHarnessTasksPoller(newSessionIdFromFile)` for the BLOCKER-fix poller rebind.
- Canonical declaration order in the connection callback body (W1 fix): state vars → teardownPane → const onLine → const onError → setupHarnessTasksPoller → transitionToHolding → transitionToActiveNew → transitionToDead → ws message handler body. No conditional hoisting fallback in the plan.
- `teardownPane` extended to (a) clear `discoveryRepollTimer` and (b) reset the six Phase 3 state vars.
- `currentSessionFile = result.sessionFile` AND `sessionIdFromFile = <slug>` both set at initial-tail-start time.
- Tail's `onLine` and `onError` extracted as named `const` closures immediately after teardownPane.
- Layer 1 raw-line `/exit` scan added at the top of `onLine`, guarded by `!hasSeenExit && changeoverState === "active"`.
- Layer 2 discovery-repoll `setInterval` added as third poller, colocated with context-% and setupHarnessTasksPoller call, handles all four state paths. W2 comment in place.
- Initial-connect call site uses `setupHarnessTasksPoller(sessionIdFromFile!)` instead of the pre-refactor inline block.
- No parser file (`session-file-parser.ts`) changes.
- No tail file (`session-file-tail.ts`) changes.
- No discovery file (`session-file-discovery.ts`) changes.
- `npm run build` succeeds.
- `session_holding`, `session_changed`, and `holding_timeout` (as an inactive reason) are all emit sites in the file (grep-verifiable).
- The existing behaviors (patches #43 tail lifecycle, #52b/#56/#59 context-%, #52c harness-tasks apart from the helper extraction refactor, #61 backgrounded-agents, #63 plan-pending) are byte-for-byte preserved in body.
- The harness-tasks-poller setInterval body is byte-for-byte preserved from the pre-refactor code; only the enclosing structure (function definition + parameter for the UUID) changed.
</verification>

<success_criteria>
- CHANGEOVER-01 (backend detection half): backend server-side detects both `/id reset` recycle and `claude --resume` recover cases and hands off the tail without user intervention.
- CHANGEOVER-02 (two-layer detection): edge-triggered raw-line scan (Layer 1) fires within one tail line of the `/exit` marker; discovery-repoll backstop (Layer 2) fires within 3-6s of a changed sessionFile in SIGTERM-fallback and recover cases.
- CHANGEOVER-04 (backend-side per-session state reset): per-session buffered state (`harnessTasksLastSerialized`, `backgroundedAgents` map, `backgroundedAgentsLastSerialized`, `pendingPlans`, `pendingPlansLastSerialized`, `hasSeenExit`, `holdingTicks`, `sessionIdFromFile`, `currentSessionFile`) all reset at `transitionToActiveNew` before the new tail starts. The harness-tasks poller is rebound to the new UUID via `setupHarnessTasksPoller(newSessionIdFromFile)` so tasks for the FRESH session actually surface (BLOCKER fix from plan-checker 2026-07-18).
- CHANGEOVER-05 (recover-in-different-cwd): `discoverClaudeSession` walks pane→pid→cwd→slug fresh each tick, so a `claude --resume <oldId>` in a different workdir is detected identically to a recycle; no special-case branch. UUID is preserved so tasks-poller rebind is an idempotent no-op restart against the same UUID.
- Backend-side portion of CHANGEOVER-03: WS is NOT torn down on session change; only tailHandle stops and restarts, sshConn is preserved, pollers keep running (with the tasks-poller REBOUND to new UUID).
- Terminal `dead` transition after 45s of no-new-file holding emits one inactive frame with `reason:"holding_timeout"` and stops all pollers + closes SSH connection; WS remains open so client renders FALLBACK-01. The W2 fix ensures stuck-same-file-during-holding cases still count against the timeout.
- All state transitions log at INFO with structured operation strings for post-deploy debugging: `claude_session_holding`, `claude_session_changed` (with `sessionIdChanged` boolean for recycle-vs-recover distinction), `claude_session_dead`.
- `npm run build` clean.
- No changes to `session-file-parser.ts`, `session-file-tail.ts`, or `session-file-discovery.ts`.
- No new backend routes (nginx caveat does not apply).
</success_criteria>

<output>
Create `.planning/phases/03-session-changeover-detection/03-01-SUMMARY.md` when done. Note in the summary:
- The four state-machine paths that were exercised in mental-model walk-through (happy path, SIGTERM fallback, recover-in-different-cwd, dead timeout) and any that couldn't be exercised because they depend on a live recycle event (which will be verified at batch deploy time per CONTEXT.md § Deploy discipline).
- Confirmation of the W1 canonical declaration order in the connection callback body (state vars → teardownPane → onLine → onError → setupHarnessTasksPoller → transitionToHolding → transitionToActiveNew → transitionToDead → ws message handler body).
- Whether `HARNESS_TASKS_INTERVAL_MS` and `UUID_RE` were moved to module scope or kept inside the helper — either is acceptable per the plan.
- Whether the `sshConn` teardown in `transitionToDead` was kept (via `teardownPane()`) — plan calls for keeping via `teardownPane()`; note if a different choice was made and why.
- Confirmation that no `session-file-parser.ts`, `session-file-tail.ts`, or `session-file-discovery.ts` changes were made — those files are HARD LOCK out of scope for Phase 3 per CONTEXT.md § Domain.
- Confirmation that the harness-tasks-poller BLOCKER fix landed correctly: `sessionIdFromFile` is per-connection `let` state; `setupHarnessTasksPoller` teardown-and-restarts; `transitionToActiveNew` calls it with the newly derived UUID.
- The three new WS emit types (`session_holding`, `session_changed`, `inactive` with reason `holding_timeout`) that Plan 03-02 will consume.
- Any deviation from the suggested `HOLDING_TIMEOUT_TICKS = 15` (45s) value and rationale.
</output>
