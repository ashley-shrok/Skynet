---
phase: 47-convo-list-per-row-current-work-hint-from-ai-title-extends-f
plan: 04
type: execute
wave: 3
depends_on: [48-01, 48-03]
files_modified:
  - src/ui/AppShell.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx
  - src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx
autonomous: true
requirements: []
tags: [appshell, seed-wire, panel, hook-subscription, prop-threading, aiTitle]

must_haves:
  truths:
    - "AppShell's /sessions/list mount effect seeds working-store with aiTitle per row via seedSessionAiTitle, at BOTH the cached-rehydrate path AND the fresh-fetch path (mirror Phase 44 Plan 04 seedSessionLastMessageAt wiring)"
    - "PrettyConversationRowLive subscribes to useSessionAiTitle(sessionKey) and threads aiTitle through as a new prop to PrettyConversationRow (which consumes it in Plan 48-05)"
    - "All PrettyConversationRowLive render sites (search-flat, pinned, middle, RDP, hidden) receive the aiTitle prop uniformly — no site left un-wired"
  artifacts:
    - path: "src/ui/AppShell.tsx"
      provides: "seedSessionAiTitle import + 2 seed-loop call sites in the /sessions/list effect (cached loop + fresh loop) alongside the existing seedSessionLastMessageAt calls"
      contains: "seedSessionAiTitle(s.hostId, s.sessionName, s.aiTitle ?? null)"
    - path: "src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx"
      provides: "useSessionAiTitle subscription inside PrettyConversationRowLive + aiTitle prop threaded to PrettyConversationRow at all render sites"
      contains: "useSessionAiTitle(sessionKey)"
  key_links:
    - from: "AppShell /sessions/list handler (both cached + fresh paths)"
      to: "seedSessionAiTitle chokepoint (Plan 48-03)"
      via: "per-row for loop calling seedSessionAiTitle(hostId, sessionName, aiTitle ?? null) — same shape as the pre-existing seedSessionLastMessageAt loops"
      pattern: "seedSessionAiTitle\\(s\\.hostId"
    - from: "PrettyConversationRowLive hook subscription"
      to: "PrettyConversationRow aiTitle prop"
      via: "single new prop threaded through the {...rowProps} spread with an explicit aiTitle={aiTitle} suffix so it's greppable and not lost in the spread"
      pattern: "aiTitle=\\{aiTitle\\}"
---

<objective>
Wire the ai-title signal end-to-end on the client side: AppShell's /sessions/list mount effect seeds the working-store with aiTitle per row (matching the Phase 44 Plan 04 lastMessageAt seed wiring exactly), and PrettyConversationRowLive subscribes to the useSessionAiTitle hook and threads the value through to PrettyConversationRow as a new prop.

Purpose: unblock Plan 48-05 (PrettyConversationRow markup + CSS redesign consumes the aiTitle prop as the subtitle content). Once this plan lands, the ai-title value is available at every render site — Plan 48-05 is pure presentation.

Output: Every PrettyConversationRowLive instance in the panel (search-flat, pinned, middle, RDP, hidden — 5 render sites total) receives an aiTitle prop wired via useSessionAiTitle; AppShell's cold-cache + fresh-fetch paths both seed the working-store per row. The PrettyConversationRow prop signature is EXTENDED with an optional aiTitle field but the component's render tree is NOT yet modified — that's Plan 48-05. Backward-compatible: passing the new prop without a consumer breaks nothing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-convo-list-per-row-current-work-hint-from-ai-title-extends-f/48-CONTEXT.md
@.planning/phases/44-fix-convo-list-recency-signal-switch-dormant-live-paths-to-i/44-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire AppShell /sessions/list handler to seed working-store with aiTitle per row (both cached + fresh paths)</name>
  <files>src/ui/AppShell.tsx</files>
  <read_first>
    - src/ui/AppShell.tsx lines 585-635 (the /sessions/list mount effect that Plan 44-04 wired for seedSessionLastMessageAt) — this task adds the analogous seedSessionAiTitle calls in the SAME two locations (cached loop at ~L606, fresh loop at ~L621)
    - src/ui/AppShell.tsx around line 88 (the session-working-store import block containing seedSessionLastMessageAt) — extend with seedSessionAiTitle
    - .planning/phases/44-fix-convo-list-recency-signal-switch-dormant-live-paths-to-i/44-04-SUMMARY.md § What Landed § AppShell.tsx (documents the exact 2-site pattern this task mirrors)
    - src/ui/state/session-working-store.ts (Plan 48-03 landed seedSessionAiTitle — this is the callee)
    - src/ui/state/conversation-store.ts FleetSession type (Plan 48-01 added `aiTitle?: string | null` — this is the source data)
  </read_first>
  <behavior>
    - Test 1 (via existing AppShell.persistence.test.tsx or similar): on AppShell mount with a cached FleetSession fleet containing rows with aiTitle values, seedSessionAiTitle is called once per row with the row's hostId + sessionName + aiTitle. Absent that test, this task's behavior lock is provided by manual grep-verification of the two call sites — the test coverage for the seed API itself lives in Plan 48-03.
    - Test 2 (fresh path): on getSessionList() resolve with rows carrying aiTitle, seedSessionAiTitle fires per row.
    - Test 3 (undefined-safe): a row whose aiTitle is `undefined` (pre-Phase-47 backend response) calls seedSessionAiTitle with `null` (per `?? null` coalesce).
  </behavior>
  <action>
    Extend AppShell's /sessions/list mount effect with the aiTitle seed loop, mirroring the Phase 44 Plan 04 seedSessionLastMessageAt wiring exactly.

    1. Extend the session-working-store import block (near line 88) with `seedSessionAiTitle` alongside the existing `seedSessionLastMessageAt`. Preserve alphabetical or existing import ordering — the file already has an established convention; follow it.

    2. In the /sessions/list effect at lines 590-635:
       - Inside the cached-hit block (currently line 597-608, after the pre-existing `for (const s of cached)` loop that calls seedSessionLastMessageAt at line 606): add a SECOND per-row call INSIDE the same for-loop body (single loop, both signals seeded per row) — `seedSessionAiTitle(s.hostId, s.sessionName, s.aiTitle ?? null);`. Update the comment above the loop to mention aiTitle alongside lastMessageAt.
       - Inside the fresh-fetch block (currently line 616-625, after the pre-existing `for (const s of fresh)` loop that calls seedSessionLastMessageAt at line 621): add the analogous seedSessionAiTitle call inside the same for-loop body.
       - Add inline `// Phase 48 Plan 04` markers on the two new lines (mirror the pattern of the pre-existing `// Phase 44 Plan 04` comments above the seedSessionLastMessageAt calls) for git-grep-ability of the wire history.

    3. Do NOT change anything else in AppShell — no other effects, no imports beyond seedSessionAiTitle, no state, no props.

    Do NOT touch PrettyConversationsPanel.tsx (Task 2). Do NOT touch PrettyConversationRow.tsx (Plan 48-05).
  </action>
  <verify>
    <automated>npx vitest run src/ui/AppShell.persistence.test.tsx 2>/dev/null; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'seedSessionAiTitle' src/ui/AppShell.tsx` == 3 (1 import + 2 call sites)
    - `grep -Fc 'seedSessionAiTitle(s.hostId, s.sessionName, s.aiTitle ?? null)' src/ui/AppShell.tsx` == 2 (exact call shape in both cached + fresh paths)
    - `grep -c 'Phase 48 Plan 04' src/ui/AppShell.tsx` >= 2 (site markers on both call sites)
    - Pre-existing Phase 44 Plan 04 seedSessionLastMessageAt calls untouched: `grep -Fc 'seedSessionLastMessageAt(s.hostId, s.sessionName, s.lastMessageAt ?? null)' src/ui/AppShell.tsx` == 2 (unchanged; regression proof)
    - `npm run build` exit 0 (typecheck green — the seedSessionAiTitle signature from Plan 48-03 matches call shape)
    - `npx vitest run src/ui/AppShell.persistence.test.tsx` exit 0 IF that test file exists (may pass 4/4 unchanged if the tests don't specifically assert on the seed loops — additive-only wiring should not regress persistence tests)
    - `grep -c 'as any\|@ts-expect-error' src/ui/AppShell.tsx` == 0 (or unchanged from HEAD — verify no new escape hatches added)
    - Scope fence: `git diff --name-only HEAD -- src/ui/features/ src/backend/` returns empty (this task only touches AppShell.tsx)
  </acceptance_criteria>
  <done>AppShell's /sessions/list handler seeds the working-store with aiTitle per row in BOTH cached-rehydrate and fresh-fetch paths, mirroring the Phase 44 Plan 04 seedSessionLastMessageAt wiring shape exactly; typecheck + build green; no regression to persistence tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread aiTitle through PrettyConversationRowLive to PrettyConversationRow at every render site</name>
  <files>src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx, src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx</files>
  <read_first>
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx lines 80-95 (import block; extend with useSessionAiTitle)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx lines 164-221 (PrettyConversationRowLive definition — this is where the useSessionAiTitle subscription lands and the aiTitle prop is threaded through)
    - src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx lines 1280-1477 (five PrettyConversationRowLive render sites: search-flat block ~L1284, pinned block ~L1320, middle block ~L1349, RDP block ~L1404, hidden block ~L1459 — all five must be verified for uniform prop-wiring)
    - src/ui/features/pretty-conversations/PrettyConversationRow.tsx lines 145-251 (the props destructure block + the interface — extend the interface with `aiTitle?: string | null` as a new prop, defaulted to null in the destructure; DO NOT consume it in the render tree in this task, that's Plan 48-05's scope)
    - src/ui/state/session-working-store.ts (Plan 48-03 landed useSessionAiTitle — this is the hook consumed here)
  </read_first>
  <behavior>
    - Test 1: PrettyConversationRowLive with a sessionKey whose working-store record has `aiTitle: "Fix bug X"` renders a PrettyConversationRow whose aiTitle prop is "Fix bug X". Verifiable by rendering the panel with a seeded working-store and inspecting the resulting PrettyConversationRow's prop values via a spy / test-render assertion pattern already used in PrettyConversationsPanel.test.tsx.
    - Test 2: PrettyConversationRowLive with a null sessionKey (RDP row where `sessionWorkingKey(row)` returns null) passes aiTitle={null} to PrettyConversationRow.
    - Test 3: PrettyConversationRowLive whose sessionKey has no working-store record yet (session never published) passes aiTitle={null} to PrettyConversationRow.
    - Test 4: All 5 render sites (search-flat, pinned, middle, RDP, hidden) pass an aiTitle prop through — no site left un-wired. This is a source-assertion, verifiable via grep on the panel file.
  </behavior>
  <action>
    Thread the aiTitle hook subscription and prop through PrettyConversationRowLive.

    1. `src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx`:
       - Extend the session-working-store import block (line 85) with `useSessionAiTitle` alongside the existing `useSessionIsWorking`. Preserve import ordering.
       - Inside PrettyConversationRowLive (lines 164-221):
         - After the existing `const hasQueuePending = useSessionQueuePending(sessionKey);` at line 211, add `const aiTitle = useSessionAiTitle(sessionKey);`. Include an inline comment: "Phase 48 Plan 04 — subscribes to the working-store's aiTitle axis (Plan 48-03 chokepoint) for the row's (host, tmuxSession) key. Same key shape as the other three working-store hooks above. Returns string | null; null for null-key rows (RDP) and for known-key rows the store hasn't seen an ai-title for yet — PrettyConversationRow's subtitle-line fallback (Plan 48-05) handles the null case."
         - Extend the returned `<PrettyConversationRow {...rowProps} />` at line 213-220 with `aiTitle={aiTitle}` explicitly (do NOT hide it in the spread — it must be greppable for source assertions).
       - Do NOT touch any of the 5 render sites (search-flat / pinned / middle / RDP / hidden) — they pass sessionKey unchanged and the aiTitle threading happens INSIDE PrettyConversationRowLive at a single site. This is the panel's abstraction contract: PrettyConversationRowLive owns the working-store subscriptions; render sites just pass raw props + sessionKey.

    2. `src/ui/features/pretty-conversations/PrettyConversationRow.tsx`:
       - Extend the props destructure at lines 145-161 with `aiTitle = null` (default null so any test that constructs the row without the prop keeps working).
       - Extend the props interface at lines 162-251 with `aiTitle?: string | null;` alongside `hasQueuePending` (or wherever fits the existing grouping — cluster with the working-state axes). Add a docblock comment: "Phase 48 Plan 04 — the identity's freshest ai-title sourced from the working-store's aiTitle axis (see PrettyConversationsPanel.PrettyConversationRowLive at L~211). Null when no ai-title has been published yet, or when this row has no working-store key (RDP rows). Consumed in Plan 48-05 as the row's subtitle content; NOT yet rendered by this task's scope — the prop is added here so the type surface is stable before Plan 48-05 wires the visual."
       - Do NOT modify the render tree in this task. The prop is accepted but not consumed. This preserves the pre-Plan-48-05 visual verbatim so Plan 48-04 lands cleanly without any visible regression on the current row layout.

    3. `src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx`:
       - Add at least 2 tests to the appropriate describe block:
         * Test A: Panel with a seeded working-store (via seedSessionAiTitle on the row's key) renders the target row's PrettyConversationRow with aiTitle prop equal to the seeded value. Achievable via the same spy / mock pattern the file already uses for other Phase 44 Plan 04 assertions — grep the file for `vi.mock` and `PrettyConversationRow` to find the existing patterns.
         * Test B: Panel with an RDP row renders that row's PrettyConversationRow with aiTitle={null} (RDP rows have sessionKey === null → hook short-circuits to null).
       - If no existing test file structure supports observing PrettyConversationRow's received props directly, extend the row-render assertion using a spy on the PrettyConversationRow component (vi.mock the module and inspect vi.mocked calls' args). Precedent for this pattern exists in Phase 22/24/26 tests — grep the pretty-conversations test folder for `vi.mock.*PrettyConversationRow` or `useSessionIsWorking` mock patterns.

    Do NOT touch AppShell.tsx (Task 1 already covered). Do NOT touch pretty-conversations.css (Plan 48-05 does the visual work). Do NOT modify PrettyConversationRow.tsx render tree (Plan 48-05 owns that).
  </action>
  <verify>
    <automated>npx vitest run src/ui/features/pretty-conversations/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'useSessionAiTitle' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` == 2 (1 import + 1 call site inside PrettyConversationRowLive)
    - `grep -Fc 'aiTitle={aiTitle}' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` == 1 (explicit prop pass on the single PrettyConversationRow render inside PrettyConversationRowLive — not hidden in a spread)
    - `grep -c 'aiTitle' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` >= 3 (destructure default + interface field + comment)
    - `grep -n 'aiTitle\??: string' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` >= 1 (interface field present)
    - `grep -c 'Phase 48' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx` >= 2 (site markers)
    - `grep -c 'Phase 48' src/ui/features/pretty-conversations/PrettyConversationRow.tsx` >= 1 (comment marker)
    - PrettyConversationRow render tree UNCHANGED in this task: `git diff HEAD -- src/ui/features/pretty-conversations/PrettyConversationRow.tsx | grep -Ec '^\+ *<[a-zA-Z]|^\+ *</'` should be low (no new JSX tags added — only the destructure + interface). This is a defense-in-depth against accidental scope creep into Plan 48-05.
    - `npx vitest run src/ui/features/pretty-conversations/PrettyConversationsPanel.test.tsx` exit 0 (existing 71+ tests still pass + at least 2 new aiTitle-threading tests)
    - `npx vitest run src/ui/features/pretty-conversations/PrettyConversationRow.test.tsx` exit 0 (no regression — the added prop with default null does not break any existing test)
    - `npm run build` exit 0
    - `grep -c 'as any\|@ts-expect-error' src/ui/features/pretty-conversations/PrettyConversationsPanel.tsx src/ui/features/pretty-conversations/PrettyConversationRow.tsx` == 0
  </acceptance_criteria>
  <done>PrettyConversationRowLive subscribes to useSessionAiTitle(sessionKey) and threads aiTitle explicitly through to PrettyConversationRow at the single site where PrettyConversationRow is rendered inside PrettyConversationRowLive; PrettyConversationRow accepts aiTitle prop but does not yet consume it in render (Plan 48-05 owns the visual); at least 2 new panel tests lock the wire; existing tests all green; build green.</done>
</task>

</tasks>

<verification>
`npm run build && npx vitest run src/ui/features/pretty-conversations/ src/ui/AppShell.persistence.test.tsx 2>/dev/null` all exit 0. Full-suite `npx vitest run` remains green.
</verification>

<success_criteria>
- AppShell seeds working-store with aiTitle per row on BOTH cached + fresh paths.
- PrettyConversationRowLive owns the useSessionAiTitle subscription and threads the value through to PrettyConversationRow as a prop.
- PrettyConversationRow accepts the new prop with a null default; render tree unchanged (Plan 48-05 owns the visual consumption).
- Zero visible visual regression — all Plan 48-04 work is data-plumbing.
- At least 2 new panel tests lock the aiTitle-threading contract.
</success_criteria>

<output>
Create `.planning/phases/48-convo-list-per-row-current-work-hint-from-ai-title-extends-f/48-04-SUMMARY.md` when done.
</output>
