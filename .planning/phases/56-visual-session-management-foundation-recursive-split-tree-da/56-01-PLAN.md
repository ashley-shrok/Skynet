---
phase: 56-visual-session-management-foundation-recursive-split-tree-da
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/lib/split-tree.ts
  - src/ui/lib/split-tree.test.ts
  - src/ui/lib/split-tree-url.ts
  - src/ui/lib/split-tree-url.test.ts
autonomous: true
requirements: []
tags:
  - split-tree
  - visual-session-management
  - phase-56
must_haves:
  truths:
    - "A `SplitNode` recursive discriminated union is exported that can represent (a) an empty layout (null root), (b) a single session leaf (`{kind:'session', tabId}`), and (c) an internal split of exactly two children with a `direction: 'horizontal' | 'vertical'` axis. Constant-ratio 50/50 is a rendering choice — the type carries no ratio field (locked decision Ashley 2026-08-28: no draggable pane dividers)."
    - "Tree-mutation helpers `insertAtEdge(root, targetPath, newLeaf, edge)` and `removeLeaf(root, tabId)` are pure functions that produce a new tree; the second collapses any resulting one-child split into its surviving child so no `{kind:'split', children:[X, null]}` degenerate shape is ever emitted (invariant: every split has exactly two non-null children)."
    - "A stable `SplitPath` addressing scheme (array of `0 | 1` indices from root) uniquely identifies every leaf in a tree; `findLeaf(root, tabId)` returns its path, `getNodeAt(root, path)` returns the node."
    - "`encodeSplitTreeToUrl(root)` produces a URLSearchParams-compatible string that round-trips through `decodeSplitTreeFromUrl(str)` for every valid tree, including empty (null) and deep-nested asymmetric trees up to depth 8."
    - "The URL codec is legible in a debugger: reading the encoded string reveals which sessions are in the layout and roughly how they are arranged (not a base64 opaque blob). Reference concrete decode: `s=aqua,nelly&t=h(0,v(1,-))` is a valid encoding shape or equivalent legible schema; the executor picks the exact grammar and documents it in the module header."
    - "`decodeSplitTreeFromUrl` returns null for any malformed input (missing bracket, unknown direction char, index out of bounds, non-integer index, cycle-implying-repeat) — never throws to the caller."
    - "The codec is `tabId`-agnostic at the URL layer: it encodes a session-address alphabet (host + tmux-session pair, matching `src/ui/lib/tab-url.ts`'s existing `tmux:<host>:<session>` spec form) and a tree of INDEX references into that alphabet. Rationale: real Skynet tab ids are ephemeral millisecond-precision strings (see AppShell.tsx openTabCounter L346) that regenerate every page load; the URL must reference sessions by their durable identity (host+session name), not the ephemeral id."
    - "`encodeSplitTreeToUrl(null)` returns the empty string; `decodeSplitTreeFromUrl('')` returns null (they compose to null-round-trip, matching the URL-restore semantic of 'no split state present')."
  artifacts:
    - path: "src/ui/lib/split-tree.ts"
      provides: "NEW module — `SplitNode` discriminated-union type + `SplitDirection` type + `SplitPath` type + pure helpers `findLeaf`, `getNodeAt`, `insertAtEdge`, `removeLeaf`, `collectTabIds`. No React imports; no window/DOM references. Every helper returns a new tree (immutable transformation) so React reference equality drives re-renders correctly."
      exports: [SplitNode, SplitDirection, SplitPath, DropEdge, findLeaf, getNodeAt, insertAtEdge, removeLeaf, collectTabIds]
      contains: "export type SplitNode"
    - path: "src/ui/lib/split-tree-url.ts"
      provides: "NEW module — `encodeSplitTreeToUrl(root, sessionAddress)` and `decodeSplitTreeFromUrl(str, resolver)` — the URL-source-of-truth codec. Session-address alphabet (host + tmux-session pair) is passed through the caller-supplied `sessionAddress(tabId) => TabSpec | null` function (matches src/ui/lib/tab-url.ts `specForTab` shape) so this module has NO dependency on the Tab type. Decode uses a caller-supplied resolver `(TabSpec) => tabId | null` for the same reason."
      exports: [encodeSplitTreeToUrl, decodeSplitTreeFromUrl]
      contains: "export function encodeSplitTreeToUrl"
    - path: "src/ui/lib/split-tree.test.ts"
      provides: "Vitest tests for tree helpers — 10+ cases across the four listed helpers + invariant preservation on removeLeaf."
    - path: "src/ui/lib/split-tree-url.test.ts"
      provides: "Vitest tests for round-trip encode/decode, malformed-input tolerance, and empty-tree null-round-trip."
  key_links:
    - from: "src/ui/lib/split-tree-url.ts"
      to: "src/ui/lib/tab-url.ts"
      via: "TabSpec type-only import for the session-address alphabet — the codec's TabSpec structural shape MUST match tab-url.ts's exported `TabSpec` so the two URL codecs can share addressing conventions"
      pattern: "import type \\{ TabSpec \\}"
    - from: "src/ui/shell/SplitView.tsx (Plan 56-02 consumer)"
      to: "src/ui/lib/split-tree.ts"
      via: "Plan 56-02's SplitView refactor imports SplitNode + insertAtEdge to drive the recursive renderer; AppShell's URL-restore + URL-sync effects import the codec"
      pattern: "SplitNode|insertAtEdge"
---

<objective>
Land the shared, side-effect-free foundations Phase 56 depends on before the SplitView refactor (Plan 56-02) or the row-drag wire-up (Plan 56-03) touch React components. Two NEW pure-TypeScript modules under `src/ui/lib/`:

1. `split-tree.ts` — the recursive `SplitNode` discriminated union + immutable helper functions (`findLeaf`, `getNodeAt`, `insertAtEdge`, `removeLeaf`, `collectTabIds`). This is the data-model replacement for the retired `paneTabIds: (string|null)[]` fixed-grid shape. NO React imports; NO window/DOM references — this module runs in any TS environment. Constant-ratio 50/50 is baked in by omission: the split node carries NO ratio field (locked decision Ashley 2026-08-28: no draggable pane dividers).

2. `split-tree-url.ts` — the URL codec. `encodeSplitTreeToUrl(root, sessionAddress)` and `decodeSplitTreeFromUrl(str, resolver)`. Session-address alphabet (host + tmux-session pair) is passed through caller-supplied resolvers so this module has NO dependency on the Tab type from `src/types/ui-types.ts` (isolates blast radius). Encode form is legible in a debugger, not a base64 blob — CONTEXT.md § What would make it wrong hangs on this.

Purpose: Both consumers (Plan 56-02's SplitView tree renderer + AppShell URL-restore/URL-sync integration, and Plan 56-03's row-drop-splits-cell insertion) need these primitives stable and tested before the render layer changes. Isolating them here lets Plans 02 + 03 focus purely on wiring, and lets THIS plan's tests exercise the tree invariants (no single-child split, path-uniqueness, malformed-URL tolerance) in isolation.

Output: TWO new files + TWO new test files under `src/ui/lib/`. No behavior change on their own — no React tree consumes these modules until Plan 02 lands. `npx tsc --noEmit` clean + `npx vitest run src/ui/lib/split-tree.test.ts src/ui/lib/split-tree-url.test.ts` green is the observable end state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-visual-session-management-foundation-recursive-split-tree-da/56-CONTEXT.md
@.planning/shapes/shape-visual-session-management.md
@CLAUDE.md
@src/ui/lib/tab-url.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create split-tree.ts — the recursive SplitNode type + pure immutable helpers</name>
  <files>
    src/ui/lib/split-tree.ts,
    src/ui/lib/split-tree.test.ts
  </files>
  <read_first>
    - src/ui/lib/tab-url.ts (READ IN FULL — this is the sister URL module the codec's addressing alphabet mirrors; the executor MUST NOT invent a divergent session-address shape. Note the `TabSpec` interface at L43-47 and `specForTab` at L121-138.)
    - .planning/phases/56-visual-session-management-foundation-recursive-split-tree-da/56-CONTEXT.md (§ locked decisions — the "no draggable pane dividers → no ratio field" invariant lives here)
    - .planning/shapes/shape-visual-session-management.md (§ shape + § what would make it wrong — the "URL becomes an unreadable soup" and "silently breaks on reload" failure modes are what this plan's tests guard against)
  </read_first>
  <behavior>
    - Test 1: `SplitNode` type union has exactly TWO variants: `{ kind: 'session'; tabId: string }` (leaf) and `{ kind: 'split'; direction: 'horizontal' | 'vertical'; children: [SplitNode, SplitNode] }` (internal). The type is exported. The root of a whole tree is `SplitNode | null` (null = empty layout).
    - Test 2: `findLeaf(null, 'x')` returns null. `findLeaf({kind:'session', tabId:'x'}, 'x')` returns `[]` (empty path = root leaf). `findLeaf({kind:'session', tabId:'x'}, 'y')` returns null. On a 2-deep tree with tabId 'z' at children[1].children[0], `findLeaf(root, 'z')` returns `[1, 0]`.
    - Test 3: `getNodeAt(root, path)` returns the node at that path; returns null if the path is out-of-bounds or hits a leaf mid-descent. `getNodeAt(null, [])` returns null.
    - Test 4: `insertAtEdge(null, [], newLeaf, 'left')` returns `newLeaf` (first-drop-into-empty root becomes the leaf verbatim, ignoring edge). Rationale: an empty layout has no cell to split against; the FIRST drop just plants the seed.
    - Test 5: `insertAtEdge(existingLeaf, [], newLeaf, 'left')` returns `{kind:'split', direction:'vertical', children:[newLeaf, existingLeaf]}` (left edge splits vertically, new leaf on the LEFT side). Similarly: 'right' → `[existingLeaf, newLeaf]` vertical; 'top' → `[newLeaf, existingLeaf]` horizontal; 'bottom' → `[existingLeaf, newLeaf]` horizontal. The `direction` is the AXIS of the divider, i.e. 'horizontal' means the split is stacked top-over-bottom with a HORIZONTAL divider; 'vertical' means side-by-side with a VERTICAL divider. Document this mapping in a comment at the top of the file so Plan 56-02's renderer imports the same mental model.
    - Test 6: `insertAtEdge` on a deep target (target path `[1, 0]` in a 2-deep tree) preserves every non-target subtree by reference (structural sharing — verifiable via `Object.is` on sibling subtrees before/after). Only the path from root to the split-point is rewritten.
    - Test 7: `insertAtEdge` throws (or returns null — executor picks) if `newLeaf` is not a session leaf, or if the target path does not resolve to a leaf. Rule of thumb: internal-node splits are always the RESULT of an insertion at an existing leaf; you never insert directly into an existing split node.
    - Test 8: `removeLeaf(root, tabId)` on a single-leaf tree matching the tabId returns null. On a 2-way split where one child matches, returns the OTHER child verbatim (the surviving sibling REPLACES the parent split — the one-child collapse rule). On a 3-deep tree where the removed leaf is at `[1, 0]`, the split at `[1]` collapses to its surviving child at `[1, 1]`, which promotes into the root's child slot at index 1.
    - Test 9: `removeLeaf` on a non-existent tabId returns the input tree UNCHANGED by reference (`Object.is(before, after) === true`) — cheap no-op for callers.
    - Test 10 (invariant): After ANY sequence of `insertAtEdge` + `removeLeaf` calls, every internal `{kind:'split'}` node has EXACTLY two non-null children of type SplitNode (no `null` children, no single-child splits, no arrays of length ≠ 2). Test asserts by a recursive walker at the end of a 20-operation randomized sequence.
    - Test 11: `collectTabIds(root)` walks the tree left-to-right and returns the tabIds in traversal order. Duplicates are impossible (a leaf can only appear in one place in a tree by construction; `insertAtEdge` does not check for duplicates so callers wanting dup-safety must call `removeLeaf(tree, sameId)` first before re-inserting elsewhere — this is exactly what Plan 56-02's move-between-cells flow will do).
  </behavior>
  <action>
    Create the NEW file `src/ui/lib/split-tree.ts`. Module structure:

    Header JSDoc block referencing:
      - Phase 56 CONTEXT.md § What this is (foundation-phase scope) items 1 + 5.
      - The locked decision "no draggable pane dividers → no ratio field" (Ashley 2026-08-28).
      - The invariant: every `{kind:'split'}` node has exactly two non-null children.
      - The edge-to-direction mapping: `left|right` produce `direction:'vertical'` (side-by-side, vertical divider); `top|bottom` produce `direction:'horizontal'` (stacked, horizontal divider). New leaf lands on the SIDE that matches the edge name.
      - The intended consumer (Plan 56-02's SplitView refactor + Plan 56-03's row-drop-splits-cell wire).

    Exports (types then helpers):

      - `export type SplitDirection = 'horizontal' | 'vertical'` — the divider axis. Comment: "'horizontal' = horizontal divider = top pane over bottom pane. 'vertical' = vertical divider = left pane beside right pane. Matches HTML/CSS convention where 'horizontal rule' is a left-to-right line."

      - `export type DropEdge = 'left' | 'right' | 'top' | 'bottom'` — the cell edge a drop landed nearest to. Consumed by `insertAtEdge`.

      - `export type SplitNode = { kind: 'session'; tabId: string } | { kind: 'split'; direction: SplitDirection; children: [SplitNode, SplitNode] }` — the tree. Root of a whole tree is `SplitNode | null` (null = empty layout, no session shown).

      - `export type SplitPath = number[]` — sequence of `0 | 1` indices from root. Empty array = root. Executor can use `readonly number[]` if preferred; either works.

      - `export function findLeaf(root: SplitNode | null, tabId: string): SplitPath | null` — DFS. Returns the path to the leaf whose tabId matches, or null if not found.

      - `export function getNodeAt(root: SplitNode | null, path: SplitPath): SplitNode | null` — walks the path, returning null on any dead-end (leaf mid-path, index out of bounds, null root).

      - `export function insertAtEdge(root: SplitNode | null, targetPath: SplitPath, newLeaf: SplitNode, edge: DropEdge): SplitNode` — the CORE mutator. Behavior spelled out in Tests 4-7 above. NEVER mutates the input tree — every node on the rewritten path is freshly allocated; every subtree NOT on the rewritten path is structurally shared (Test 6). If `root === null`, the return value is `newLeaf` verbatim (edge is ignored; first-drop-into-empty). If the target path resolves to an internal split node (not a leaf), throw `new Error("split-tree.insertAtEdge: target path resolves to internal split, not a leaf")` — this is a caller-side bug we want to surface, not silently swallow.

      - `export function removeLeaf(root: SplitNode | null, tabId: string): SplitNode | null` — walks the tree; if the leaf is not found, returns the input by reference (Test 9's identity check). If found, walks back up the path collapsing any single-child split into the surviving child (Test 8's collapse rule). If the removed leaf was the root, returns null.

      - `export function collectTabIds(root: SplitNode | null): string[]` — left-to-right DFS traversal collecting every leaf's tabId. Used by AppShell to reconcile the tree against the tabs array (identify orphaned leaves whose tabs were closed elsewhere).

    Then create `src/ui/lib/split-tree.test.ts` with Vitest tests covering Tests 1-11 above. Use `describe("split-tree")` at the top. For Test 6, use `Object.is` to assert reference identity on sibling subtrees. For Test 10, generate 20 random insert/remove operations against a starting single-leaf tree (use `Math.seedrandom` OR a simple LCG — no new deps; a plain `let seed = 1; const rand = () => (seed = (seed * 1103515245 + 12345) & 0x7fffffff) / 0x7fffffff` inline is fine) and assert the invariant walker at the end.

    Do NOT depend on any React, DOM, or ssh2 imports. This file is pure TS — must be compilable and testable in isolation. Do NOT import from `src/types/ui-types.ts` — the tree references `tabId: string`, not the `Tab` type, so the two data models decouple cleanly.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tanya && (set -o pipefail; npx vitest run src/ui/lib/split-tree.test.ts 2>&1 | tail -20)</automated>
  </verify>
  <acceptance_criteria>
    - File `src/ui/lib/split-tree.ts` exists.
    - `grep -n "^export type SplitNode" src/ui/lib/split-tree.ts` returns exactly 1 match.
    - `grep -n "^export type SplitDirection" src/ui/lib/split-tree.ts` returns exactly 1 match.
    - `grep -n "^export type DropEdge" src/ui/lib/split-tree.ts` returns exactly 1 match.
    - `grep -n "^export type SplitPath" src/ui/lib/split-tree.ts` returns exactly 1 match.
    - `grep -cE "^export function (findLeaf|getNodeAt|insertAtEdge|removeLeaf|collectTabIds)" src/ui/lib/split-tree.ts` returns 5 (all five helpers exported).
    - `grep -cE "^import.*(react|React|window|document|ssh2)" src/ui/lib/split-tree.ts` returns 0 (no React/DOM/backend imports).
    - `grep -cE "^import.*from.*['\"]\\@?/types/ui-types" src/ui/lib/split-tree.ts` returns 0 (the tree module does not couple to the Tab type).
    - `grep -c "ratio\|size\|weight\|percent" src/ui/lib/split-tree.ts` returns 0 (locked decision: no ratio field). Comment strings with the word "ratio" (e.g. "constant-ratio 50/50") are allowed — the grep is intentionally case-sensitive on the field-name-hunting variants. Use `grep -v '^\s*//\|^\s*\*' src/ui/lib/split-tree.ts | grep -cE "\\b(ratio|size|weight|percent)\\s*:"` returns 0 as the strict check.
    - All 11 test cases in `src/ui/lib/split-tree.test.ts` pass under `npx vitest run src/ui/lib/split-tree.test.ts`.
    - `npx tsc --noEmit` exits 0 (module compiles cleanly).
  </acceptance_criteria>
  <done>Split-tree module exports the recursive type and all five pure helpers; every helper is structurally-sharing where possible; every internal split invariant is enforced across the randomized-op test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create split-tree-url.ts — the URL codec that round-trips SplitNode via the tab-url.ts session-address alphabet</name>
  <files>
    src/ui/lib/split-tree-url.ts,
    src/ui/lib/split-tree-url.test.ts
  </files>
  <read_first>
    - src/ui/lib/tab-url.ts (READ IN FULL — the URL codec's `TabSpec` shape at L43-47 is what this codec's session-address alphabet references. `encodeTabSpec` at L91-97 shows how a single spec is packed into a URL-safe string; this new codec reuses that idea per session in the alphabet.)
    - src/ui/lib/split-tree.ts (READ AFTER TASK 1 — the SplitNode + SplitPath + SplitDirection types this codec serializes)
    - .planning/phases/56-visual-session-management-foundation-recursive-split-tree-da/56-CONTEXT.md (§ What would make it wrong — "URL becomes an unreadable soup" and "silently breaks on reload because references sessions that no longer exist"; the graceful-degradation clause drives the resolver signature)
  </read_first>
  <behavior>
    - Test 1: `encodeSplitTreeToUrl(null, resolver)` returns the empty string.
    - Test 2: `decodeSplitTreeFromUrl('', resolver)` returns null.
    - Test 3: `encodeSplitTreeToUrl(singleLeaf, resolver)` where `resolver(singleLeaf.tabId)` returns `{ protocol: 'tmux', host: 'skynet-t1000', session: 'aqua' }` produces a legible short string containing both `aqua` and `t1000` (or their URL-encoded forms). Debugger-legibility check: `assert(url.includes('aqua') || url.includes(encodeURIComponent('aqua')))`.
    - Test 4 (round-trip, primary): For a randomly-generated tree of depth 3-5 (built via 20 `insertAtEdge` calls from Task 1), calling `decodeSplitTreeFromUrl(encodeSplitTreeToUrl(tree, sessionAddress), resolver)` returns a tree that is DEEPLY-EQUAL (JSON.stringify comparison acceptable) to the original. Use a synthetic Tab table where each tabId maps to a unique `{host, session}` pair.
    - Test 5 (round-trip, empty-tree): `decodeSplitTreeFromUrl(encodeSplitTreeToUrl(null, resolver), resolver)` returns null.
    - Test 6 (malformed tolerance): `decodeSplitTreeFromUrl('completely-not-a-valid-encoding', resolver)` returns null (no throw).
    - Test 7 (malformed tolerance, missing bracket): `decodeSplitTreeFromUrl('t=h(0,1', resolver)` (or whatever open-brace-without-close corresponds to the executor's chosen grammar) returns null.
    - Test 8 (malformed tolerance, unknown direction char): `decodeSplitTreeFromUrl('t=q(0,1)', resolver)` (direction char not h/v) returns null.
    - Test 9 (out-of-bounds session index): `decodeSplitTreeFromUrl('s=aqua&t=h(0,99)', resolver)` where the alphabet has 1 entry returns null.
    - Test 10 (graceful degradation on resolver miss): When the URL references a session (host+session pair) that the resolver returns null for — i.e. the session no longer exists in the current tabs — the decoder MUST NOT return null. It MUST drop that leaf from the tree and collapse any resulting one-child split via the `removeLeaf` collapse rule (reuse the Task 1 helper). This directly satisfies CONTEXT.md § "A saved URL layout silently breaks on reload because it references sessions that no longer exist. Degradation must be graceful: drop missing sessions from the layout, keep valid ones, never blank-screen." Test setup: encode a 3-session tree; decode with a resolver that returns null for the middle session; assert the result is a 2-session tree containing the other two, structurally valid per the tree invariant.
    - Test 11 (all-resolver-misses → null): If the resolver returns null for EVERY session in the encoded URL, decoded result is null (empty layout).
    - Test 12 (adversarial input): Very long input string (>50KB) returns null in bounded time (no exponential parser blowup). Executor can validate by encoding a 200-node tree, measuring encode length, then feeding a deliberately-malformed 50KB blob and asserting the call returns null in <100ms via `vi.useFakeTimers` or by direct measurement.
  </behavior>
  <action>
    Create the NEW file `src/ui/lib/split-tree-url.ts`. Module structure:

    Header JSDoc block referencing:
      - Phase 56 CONTEXT.md § What this is item 2 (URL as source of truth) and § What would make it wrong (unreadable-soup + silently-breaks-on-reload clauses).
      - The sister codec at `src/ui/lib/tab-url.ts` — the addressing alphabet mirrors `TabSpec` structural shape so the two URL schemes are consistent.
      - The chosen wire grammar (executor documents the exact form here; a suggested legible form is described below).

    Imports:
      - `import type { TabSpec } from "./tab-url"` (type-only, so no runtime coupling).
      - `import { type SplitNode, type SplitPath, removeLeaf } from "./split-tree"` — the collapse rule reuse for graceful-degradation on resolver miss.

    Suggested wire grammar (executor is free to adjust; must document the final choice in the module header):

      `s=<spec1>~<spec2>~...~<specN>&t=<tree>`

      - `s=` = the session alphabet, `~`-separated (avoids clashing with `&` and `,`). Each `<specN>` uses the same encoding as `encodeTabSpec` from tab-url.ts (`tmux:<host>:<session>` etc.), URL-encoding applied to embedded chars.
      - `t=` = the tree structure. Grammar:
        - A leaf is a single digit or multi-digit index into the alphabet: `0`, `1`, `12`.
        - A split is `h(A,B)` or `v(A,B)` where `h`/`v` is the direction char (`h` for horizontal divider = stacked, `v` for vertical divider = side-by-side), and A/B are recursively-nested tree fragments.
        - Empty tree: the `t=` parameter is absent entirely (or `t=` empty).
      - Example: two sessions in a horizontal split → `s=tmux:t1000:aqua~tmux:t1000:nelly&t=h(0,1)`.
      - Example: three-session L-shape → `s=tmux:t1000:aqua~tmux:t1000:nelly~tmux:t1000:zack&t=v(0,h(1,2))`.

    Exports:

      - `export function encodeSplitTreeToUrl(root: SplitNode | null, sessionAddress: (tabId: string) => TabSpec | null): string`
        - If `root === null`, return "".
        - Walk the tree once with `collectTabIds` (from Task 1) to build the alphabet in traversal order. If ANY tabId maps to a null spec (session lacks a durable address — dashboard tab, malformed tab), skip that leaf as if the tab did not exist (call `removeLeaf` on the input tree to strip it before encoding). This makes the encoder resilient to dashboard tabs accidentally reaching the tree (defense-in-depth; AppShell's Plan 56-02 wire should also filter, but the encoder must not blow up if it doesn't).
        - Serialize the alphabet: URL-encode each spec's host and session components via encodeURIComponent, join with `~`.
        - Serialize the tree structure via a recursive stringifier: leaf → its alphabet index, split → `h(A,B)` or `v(A,B)`.
        - Return `s=<alphabet>&t=<tree>` (URLSearchParams-compatible so it can be spliced into the same fragment string `tab-url.ts` writes).

      - `export function decodeSplitTreeFromUrl(str: string, resolver: (spec: TabSpec) => string | null): SplitNode | null`
        - If `str === ""` return null.
        - Parse via URLSearchParams. Read `s` and `t`. If either is missing, return null.
        - Parse the alphabet: split on `~`, decode each spec via a `parseTabSpec` (executor may inline a minimal parser matching the encode form; do NOT import `parseTabParam` from tab-url.ts because that function has a slightly different grammar shape that includes a `?tab=` legacy path). For each spec, call `resolver(spec)` — this is caller-supplied and returns the current-session tabId for that host+session pair, or null if no such session exists in the current tabs.
        - Parse the tree with a recursive-descent parser:
          - Reject if any bracket is unmatched.
          - Reject if the direction char is not `h` or `v`.
          - Reject if a leaf index is not an integer or is out-of-bounds against the alphabet length.
          - Bound recursion depth at 20 (defense against exponential-nesting DOS). If exceeded, return null.
        - After parsing, walk the result and REPLACE each leaf whose resolver returned null with a marker. Then use `removeLeaf` (Task 1 helper) or an equivalent inline collapse pass to strip missing-session leaves, collapsing single-child splits into their surviving child.
        - If after collapse the tree is empty, return null.
        - Otherwise return the collapsed tree.
        - On ANY parse error (syntax, out-of-bounds, depth-exceeded), return null. NEVER throw to the caller — the URL is untrusted input.

    Then create `src/ui/lib/split-tree-url.test.ts` with Vitest tests for Tests 1-12 above. Use `describe("split-tree-url")`. For the graceful-degradation test (Test 10), set up a resolver that returns null for one specific host+session pair and non-null for the others; assert the tree comes back with that leaf stripped and any single-child parent collapsed.

    Do NOT import from `src/types/ui-types.ts` — the codec references TabSpec (from tab-url.ts) not Tab. Do NOT import from React or window/DOM. This module is pure TS.
  </action>
  <verify>
    <automated>cd /home/ubuntu/skynet-tanya && (set -o pipefail; npx vitest run src/ui/lib/split-tree-url.test.ts 2>&1 | tail -20)</automated>
  </verify>
  <acceptance_criteria>
    - File `src/ui/lib/split-tree-url.ts` exists.
    - `grep -n "^export function encodeSplitTreeToUrl" src/ui/lib/split-tree-url.ts` returns exactly 1 match.
    - `grep -n "^export function decodeSplitTreeFromUrl" src/ui/lib/split-tree-url.ts` returns exactly 1 match.
    - `grep -c "import type { TabSpec }" src/ui/lib/split-tree-url.ts` returns 1 (type-only import; no runtime coupling to tab-url.ts).
    - `grep -cE "^import.*(react|React|window|document|ssh2)" src/ui/lib/split-tree-url.ts` returns 0.
    - `grep -cE "^import.*from.*['\"]\\@?/types/ui-types" src/ui/lib/split-tree-url.ts` returns 0.
    - `grep -c "throw" src/ui/lib/split-tree-url.ts` returns 0 (the decoder catches every error path internally per CONTEXT.md "never blank-screen" — the encoder may have internal invariant throws but the exported entrypoint shape must not surface any). If the executor prefers an internal throw+catch pattern, wrap it inside a try around the top of decodeSplitTreeFromUrl; the top-level function must never throw to callers.
    - All 12 test cases in `src/ui/lib/split-tree-url.test.ts` pass under `npx vitest run src/ui/lib/split-tree-url.test.ts`.
    - `npx tsc --noEmit` exits 0.
    - Related scope check: `npx vitest run --related src/ui/lib/split-tree.ts src/ui/lib/split-tree-url.ts 2>&1 | tail -5` reports green.
  </acceptance_criteria>
  <done>Codec round-trips valid trees; malformed inputs return null instead of throwing; missing-session graceful degradation drops orphaned leaves and collapses single-child parents; module has zero React/DOM/backend imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL fragment → decoder | The `t=`/`s=` payload comes from the URL, which is user-editable and shareable. The decoder MUST tolerate any input string without throwing, return null on malformed shapes, and cap recursion depth to prevent DOS via deeply-nested brackets. |
| Alphabet resolver → tree | The `resolver(spec) => tabId | null` is supplied by AppShell (Plan 56-02) and looks up sessions in the current tab set. A saved URL layout may reference sessions that no longer exist; graceful degradation drops those leaves per CONTEXT.md § What would make it wrong. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01 | Denial of Service | decodeSplitTreeFromUrl recursion depth | mitigate | Hard recursion-depth cap of 20 (deeper trees are physically unusable on a screen — 20 splits is >1M nested cells). Exceeding the cap returns null, not throws. Test 12 verifies bounded parse time on a 50KB adversarial input. |
| T-56-02 | Tampering | Malformed URL causes blank-screen | mitigate | Every parse error path returns null (Tests 6-9). AppShell's URL-restore path (Plan 56-02) then falls back to "no split" state — the layout is lost, but the app renders. This is exactly CONTEXT.md § "degrades gracefully". |
| T-56-03 | Information Disclosure | Encoded URL leaks session names to share-link recipients | accept | Session names (tmux session id) are already visible in identity badges + conversation-list rows to anyone with app access. The URL codec encoding them does not widen the disclosure surface beyond what's already on screen. Ashley's shape-file spec (§ "The URL is the layout ... share your current view with future-you-across-devices") REQUIRES this exposure to work. |
| T-56-04 | Tampering | Adversarial URL contains non-existent sessions | mitigate | Resolver-miss graceful degradation (Test 10): unknown sessions are dropped, single-child splits collapse per Task 1's `removeLeaf` rule, layout is partial but valid. |
</threat_model>

<verification>
- `npx vitest run src/ui/lib/split-tree.test.ts src/ui/lib/split-tree-url.test.ts` — all tests green.
- `npx tsc --noEmit` — clean.
- Grep-hygiene checks in the acceptance criteria all pass with the stated exact counts.
- No file outside `src/ui/lib/` is touched by this plan. `git diff --stat` shows only the four new files.
</verification>

<success_criteria>
1. Two NEW pure-TS modules in `src/ui/lib/` compile and test green in isolation.
2. `SplitNode` type carries no ratio/size/weight field (locked decision: constant-ratio splits only).
3. `insertAtEdge` produces structurally-shared trees (Object.is preserves non-touched subtrees).
4. `removeLeaf` collapses single-child splits per the invariant (Tests 8, 10).
5. `encodeSplitTreeToUrl` produces a debugger-legible string containing session names, not a base64 blob.
6. `decodeSplitTreeFromUrl` returns null on malformed input, never throws.
7. Missing-session graceful degradation drops orphaned leaves and collapses their parent (Test 10).
8. `npx vitest run --related` on both modules green.
</success_criteria>

<output>
Create `.planning/phases/56-visual-session-management-foundation-recursive-split-tree-da/56-01-SUMMARY.md` when done, documenting:
- The exact wire grammar chosen for the URL codec (with a concrete example URL for a 3-session L-shape tree).
- Confirmation that no ratio field appears anywhere in `SplitNode` (grep result).
- Confirmation that `decodeSplitTreeFromUrl` has zero top-level `throw` statements.
- Any deviations from the suggested edge-to-direction mapping (left/right → vertical, top/bottom → horizontal) and why.
- The exact recursion depth cap chosen (should be 20 unless justified otherwise).
</output>
</content>
</invoke>