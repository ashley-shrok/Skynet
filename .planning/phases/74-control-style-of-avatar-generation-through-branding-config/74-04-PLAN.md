---
phase: 74-control-style-of-avatar-generation-through-branding-config
plan: 04
type: execute
wave: 3
depends_on:
  - 74-03
files_modified: []
autonomous: false
requirements:
  - STYLE-08 (delete the manual avatar-generation runbook file + per-identity prompt archive files from the local box's role folder; per 74-CONTEXT.md Scope Edges "In" bullet 6)
  - STYLE-09 (scrub any repo-side references to the deleted runbook path — per Ashley `<additional_context>` load-bearing anti-pattern; the three identity-avatar-batch.ts header refs are handled in Plan 03, this plan sweeps for any other in-repo refs)
must_haves:
  truths:
    - "~/.claude/roles/box-maintainer/runbooks/avatar-flow.md no longer exists on disk"
    - "~/.claude/roles/box-maintainer/avatar-prompts/ directory is empty or does not exist (all four files deleted: amelia.md, beatrice.md, becky.md, george.md)"
    - "A repo-wide grep for `avatar-flow` in files that are NOT .planning/phases/20-* historical artifacts returns 0 hits (the three identity-avatar-batch.ts header comments were scrubbed by Plan 03 Task 1; this plan is the final sweep for anything missed)"
    - "A repo-wide grep for `avatar-prompts/` in files that are NOT .planning/phases/20-* returns 0 hits"
    - "Ashley (or the human checkpoint runner) has confirmed the runbook file's content is fully retired — nothing has quietly become live-again per 74-CONTEXT.md § What would make it wrong §4"
  artifacts: []
  key_links: []
---

<objective>
Execute the local-filesystem deletions that retire the manual avatar-generation runbook and its per-identity prompt archive files, then do a final repo-side sweep to catch any lingering references to those paths that Plan 03 didn't handle.

Per D-08 (74-CONTEXT.md Scope Edges "In" bullet 6) — delete the manual avatar-generation runbook + prompt archives. Per Ashley `<additional_context>` — these files live in tina's local `~/.claude/roles/`, NOT in the Skynet repo. The deletions are `rm` operations on the local filesystem, NOT `git rm`. They do not appear in any git commit against the Skynet repo.

Per 74-CONTEXT.md § "What would make it wrong" §4: "If the manual runbook file's deletion loses something that's still needed. Ashley confirmed all its outrigger content is retired, but a scope-check during execution should re-verify nothing has quietly become live-again." Hence this plan includes a checkpoint:human-verify BEFORE the deletions, so a human eyeball the file once more before the delete runs.

Per Ashley `<additional_context>` — the three header-comment references in `identity-avatar-batch.ts` at L16/L122/L180 are scrubbed by Plan 03 Task 1. This plan's grep sweep is the final safety net for anything Plan 03 might have missed elsewhere in the repo (excluding historical planning artifacts in .planning/phases/20-* which per 74-RESEARCH.md Pitfall 5 should be left alone).

Purpose: closes the loop on Phase 74's "runbook + archive deletion" scope-edge and prevents the codebase from carrying stale pointers to deleted files.

Output:
- Local filesystem: five files deleted from `~/.claude/roles/box-maintainer/`
- Repo-side: zero remaining references to `avatar-flow.md` or `avatar-prompts/` outside of `.planning/phases/20-*` historical artifacts (verified by grep)
- No new files created, no source files modified beyond what Plan 03 already did

**Not part of this plan** (orchestrator-owned per fleet rule that subagents do not do deploys):
- Pre-deploy manual seed of `/opt/skynet/branding/branding.json` on t1000 with the current LoL spec (per Ashley resolution #2 — orchestrator writes the seed via SSM before the container restart that picks up new code).
- Coord-room BEFORE/AFTER posts per box-maintainer.md § Container mutations serialize.
- `git push` + `docker build` + `docker compose up --force-recreate` — orchestrator ship motion.
- Stacy briefing DM on the Skynet.aithercloud.com relay (per Ashley resolution #3) — briefing content explains the new required field, boot-fail behavior, and offers tina's t1000 seed content as reference. Stacy authors her OWN aesthetic content; tina does NOT operate on T800 directly per box-maintainer.md L17.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-control-style-of-avatar-generation-through-branding-config/74-CONTEXT.md
@.planning/phases/74-control-style-of-avatar-generation-through-branding-config/74-RESEARCH.md
@.planning/phases/74-control-style-of-avatar-generation-through-branding-config/74-03-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human re-verifies runbook + prompt-archive content is fully retired before deletion</name>
  <files>~/.claude/roles/box-maintainer/runbooks/avatar-flow.md, ~/.claude/roles/box-maintainer/avatar-prompts/amelia.md, ~/.claude/roles/box-maintainer/avatar-prompts/beatrice.md, ~/.claude/roles/box-maintainer/avatar-prompts/becky.md, ~/.claude/roles/box-maintainer/avatar-prompts/george.md</files>
  <what-built>
    Plans 01-03 have shipped the code side: schema extended, boot gate wired, route rewired, tests updated. This checkpoint gates the final local-filesystem deletions to prevent the failure mode called out in 74-CONTEXT.md § "What would make it wrong" §4 — "if the manual runbook file's deletion loses something that's still needed. Ashley confirmed all its outrigger content is retired, but a scope-check during execution should re-verify nothing has quietly become live-again."

    Files staged for deletion (all local to tina's `~/.claude/roles/box-maintainer/`, NOT in the Skynet repo):
    - ~/.claude/roles/box-maintainer/runbooks/avatar-flow.md (471 lines — the full manual avatar-generation runbook)
    - ~/.claude/roles/box-maintainer/avatar-prompts/amelia.md (95 lines)
    - ~/.claude/roles/box-maintainer/avatar-prompts/beatrice.md (134 lines)
    - ~/.claude/roles/box-maintainer/avatar-prompts/becky.md (105 lines)
    - ~/.claude/roles/box-maintainer/avatar-prompts/george.md (63 lines)
  </what-built>
  <action>
    Pause execution and present the five staged files (listed under `<files>` and `<what-built>`) to the human for a final scope re-verification. The human MUST re-read the runbook and prompt-archive files end-to-end and confirm nothing has quietly become live-again (per 74-CONTEXT.md § What would make it wrong §4) before Task 2 runs the deletions. Follow the numbered verification steps in `<how-to-verify>` below. Do NOT proceed to Task 2 until the human types "approved". If the human surfaces a lingering-live-dependency finding, STOP and escalate — do not delete.
  </action>
  <how-to-verify>
    1. Read `~/.claude/roles/box-maintainer/runbooks/avatar-flow.md` end-to-end (471 lines). Confirm:
       - Every referenced workflow is either already implemented in the app (chat-drafter → 3-parallel-image-gen → gamma → candidate cache → pick pipeline lives in `src/backend/database/routes/identity-avatar-batch.ts`), OR is intentionally retired (Nelly handoff, Matrix media upload flow, colorHue-picking guidance, ashley-laptop archive notes).
       - The aesthetic content the runbook § 2-3 describes is what the operator will seed into `/opt/skynet/branding/branding.json` at deploy time (orchestrator-owned pre-deploy step, per Ashley resolution #2).
       - The `avatarGammaDefault: 0.7` value from § 5 is preserved as the bundled default (Plan 01 Task 1) AND as the intended operator-seed value.

    2. Read each of the four per-identity prompt-archive files (`amelia.md`, `beatrice.md`, `becky.md`, `george.md`). Confirm they are archival records of prompts already used to generate committed avatar assets, not live workflows.

    3. Sanity-grep to catch any live app path that quietly relies on these files:
       ```bash
       grep -rn "avatar-flow\|avatar-prompts" /home/ubuntu/skynet-tina --include="*.ts" --include="*.tsx" --include="*.js" --include="*.json" 2>/dev/null | grep -v ".planning/phases/20-"
       ```
       Expected: 0 hits (Plan 03 Task 1 scrubbed the three identity-avatar-batch.ts header comments; the .planning/phases/20-* artifacts are historical per Pitfall 5 and remain untouched). If ANY hit surfaces, do NOT proceed — flag it and revise Plan 03 to catch the stragglers.

    4. Confirm the pre-deploy branding.json seed step is queued in the orchestrator's ship checklist (not part of this executor plan, but referenced in `<objective>` above — the seed is what makes t1000 boot cleanly after Phase 74 lands).

    Type "approved" to proceed with the five deletions, OR describe what's missing and stop.
  </how-to-verify>
  <verify>
    <human-check>Human explicitly typed "approved" after completing the four numbered verification steps in `<how-to-verify>`. If the human raised any lingering-live-dependency finding instead of approving, Task 2 MUST NOT run — escalate for a plan revision.</human-check>
  </verify>
  <done>
    Human explicitly approved the deletions after re-verifying all five files' contents are retired and the sanity-grep returned zero live-code hits.
  </done>
  <resume-signal>Type "approved" to proceed to Task 2, or describe issues found (e.g. "runbook § 7 references a workflow the app does not have yet — do NOT delete")</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Delete local runbook + prompt-archive files, then run final repo-side grep sweep</name>
  <files></files>
  <read_first>
    - .planning/phases/74-control-style-of-avatar-generation-through-branding-config/74-CONTEXT.md § "Scope edges" "In" bullet 6 + § "What would make it wrong" §4
    - .planning/phases/74-control-style-of-avatar-generation-through-branding-config/74-RESEARCH.md § "Pitfall 5: Deleting the runbook + archive files as a git commit" (this task is NOT a git operation — pure local `rm`) + § "Files to DELETE (outside repo, `rm` not `git rm`)"
    - Ashley `<additional_context>`: file-deletion note — the executor runs the `rm` locally, these files are NOT tracked in the Skynet repo, deletion produces no git commit
  </read_first>
  <action>
    1. Run the five `rm` commands, ONE at a time, checking for existence first so the task is idempotent (safe to re-run):
       ```bash
       [ -f ~/.claude/roles/box-maintainer/runbooks/avatar-flow.md ] && rm ~/.claude/roles/box-maintainer/runbooks/avatar-flow.md
       [ -f ~/.claude/roles/box-maintainer/avatar-prompts/amelia.md ] && rm ~/.claude/roles/box-maintainer/avatar-prompts/amelia.md
       [ -f ~/.claude/roles/box-maintainer/avatar-prompts/beatrice.md ] && rm ~/.claude/roles/box-maintainer/avatar-prompts/beatrice.md
       [ -f ~/.claude/roles/box-maintainer/avatar-prompts/becky.md ] && rm ~/.claude/roles/box-maintainer/avatar-prompts/becky.md
       [ -f ~/.claude/roles/box-maintainer/avatar-prompts/george.md ] && rm ~/.claude/roles/box-maintainer/avatar-prompts/george.md
       ```
       (Some rm invocations may be no-ops if the human-verify checkpoint discovered a file already deleted — that's fine.)

    2. If the `avatar-prompts/` directory is empty after the four file deletions AND no other files were placed there by another workflow, remove the empty directory too:
       ```bash
       rmdir ~/.claude/roles/box-maintainer/avatar-prompts/ 2>/dev/null || true
       ```
       (`rmdir` only succeeds if empty — if any residual file exists, leave the dir alone.)

    3. Run the final repo-side reference sweep to catch any lingering pointers to the deleted paths:
       ```bash
       grep -rn "avatar-flow" /home/ubuntu/skynet-tina --include="*.ts" --include="*.tsx" --include="*.js" --include="*.json" --include="*.md" 2>/dev/null | grep -v "^/home/ubuntu/skynet-tina/.planning/phases/20-" | grep -v "^/home/ubuntu/skynet-tina/.planning/phases/74-"
       grep -rn "avatar-prompts" /home/ubuntu/skynet-tina --include="*.ts" --include="*.tsx" --include="*.js" --include="*.json" --include="*.md" 2>/dev/null | grep -v "^/home/ubuntu/skynet-tina/.planning/phases/20-" | grep -v "^/home/ubuntu/skynet-tina/.planning/phases/74-"
       ```
       Both greps MUST return zero non-.planning/phases/20-* and non-.planning/phases/74-* hits. The phase-20 exclusion is because those are historical planning artifacts documenting what shipped as Phase 20 and per Pitfall 5 should be left alone. The phase-74 exclusion is because this phase's own planning docs legitimately reference the paths in the context of retiring them.

    4. If ANY hit surfaces in the grep sweep at a live-code path (src/, docker/, scripts/, etc.), STOP and surface the finding — do not silently ignore. Options: (a) scrub the reference in a small follow-up commit within this task, (b) escalate to Ashley for a decision. Per Ashley `<additional_context>` and 74-RESEARCH.md Pitfall 5, phase-20 planning artifacts stay; anything else should have been caught by Plan 03 Task 1 (three known refs in identity-avatar-batch.ts) — a surprise hit means a coverage gap.

    5. There is NO git commit produced by this task on the runbook + prompt-archive deletions (they are outside the repo tree). If Task action 4's grep finds a stray in-repo reference and scrubs it, THAT scrub produces a small commit with prefix `docs(74-04):` or `chore(74-04):` per fleet commit-hygiene rule.

    Anti-pattern reminder: DO NOT `git rm` these files — they are not in the repo. DO NOT delete the `runbooks/` parent directory itself (contains other unrelated runbooks like `css-fast-path.md` and `user-onboarding.md`; verify via `ls ~/.claude/roles/box-maintainer/runbooks/` before/after). ONLY the single `avatar-flow.md` file inside `runbooks/` is deleted.
  </action>
  <verify>
    <automated>test ! -f ~/.claude/roles/box-maintainer/runbooks/avatar-flow.md && test ! -f ~/.claude/roles/box-maintainer/avatar-prompts/amelia.md && test ! -f ~/.claude/roles/box-maintainer/avatar-prompts/beatrice.md && test ! -f ~/.claude/roles/box-maintainer/avatar-prompts/becky.md && test ! -f ~/.claude/roles/box-maintainer/avatar-prompts/george.md && echo "all five files deleted"</automated>
  </verify>
  <done>
    - `test ! -f ~/.claude/roles/box-maintainer/runbooks/avatar-flow.md` succeeds
    - `test ! -f ~/.claude/roles/box-maintainer/avatar-prompts/amelia.md` succeeds
    - `test ! -f ~/.claude/roles/box-maintainer/avatar-prompts/beatrice.md` succeeds
    - `test ! -f ~/.claude/roles/box-maintainer/avatar-prompts/becky.md` succeeds
    - `test ! -f ~/.claude/roles/box-maintainer/avatar-prompts/george.md` succeeds
    - `ls ~/.claude/roles/box-maintainer/runbooks/` still shows `css-fast-path.md` and `user-onboarding.md` (unrelated runbooks preserved)
    - Repo-wide grep for `avatar-flow` in src/**/*, docker/**/*, scripts/**/* returns 0 hits
    - Repo-wide grep for `avatar-prompts` in src/**/*, docker/**/*, scripts/**/* returns 0 hits
    - If a repo-side scrub was needed at Task action 4, a small commit was made with prefix `docs(74-04):` or `chore(74-04):`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem `~/.claude/roles/box-maintainer/` → executor `rm` | Deletion path is fixed absolute; not user-input-derived. No shell interpolation of untrusted values. |
| repo-side reference sweep grep | Read-only operation on the repo tree; no writes without a human-scope confirmation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-04-01 | Denial of Service (loss of live knowledge) | Deleting a file that a live workflow still depends on | mitigate | Task 1 is a checkpoint:human-verify — human eyeballs the runbook + all four prompt-archive files before Task 2 runs the deletions. Deletions in Task 2 are guarded by `[ -f … ] &&` idempotency so a re-run after partial completion does not error. |
| T-74-04-02 | Tampering | Overly-broad `rm` glob deletes unrelated runbooks | mitigate | Task 2 enumerates five explicit absolute paths — no globs, no `find -exec rm`. `runbooks/` parent directory NOT deleted (contains unrelated files: css-fast-path.md + user-onboarding.md). `avatar-prompts/` parent optionally `rmdir`'d only if empty (rmdir fails safe on non-empty). |
| T-74-04-03 | Elevation of Privilege | Deletion path is user-input-derived | accept (n/a) | Paths are fixed absolute literals hardcoded in the plan. No dynamic path construction. |
| T-74-04-04 | Denial of Service (silent stale reference) | Grep sweep misses a live-code reference to the deleted files | mitigate | Task 2 grep sweep is repo-wide across .ts/.tsx/.js/.json/.md; exclusions are only for known-historical .planning/phases/20-* and this phase's own docs. Any other hit STOPS the task and requires escalation. Plan 03 Task 1 already handled the three known identity-avatar-batch.ts header comments; this sweep is the safety net for anything Plan 03 missed. |
| T-74-04-SC | Tampering | Zero new npm/pip installs in this plan | accept | No package installs — plan is a set of `rm` calls + grep sweeps. |
</threat_model>

<verification>
- `test ! -f ~/.claude/roles/box-maintainer/runbooks/avatar-flow.md` succeeds (deleted)
- All four `avatar-prompts/*.md` files deleted (verified via `test ! -f`)
- `~/.claude/roles/box-maintainer/runbooks/` still contains `css-fast-path.md` and `user-onboarding.md` (unrelated runbooks preserved)
- `grep -rn "avatar-flow\|avatar-prompts" /home/ubuntu/skynet-tina --include="*.ts" --include="*.tsx" --include="*.js" --include="*.json" 2>/dev/null | grep -v ".planning/phases/20-" | grep -v ".planning/phases/74-"` returns 0 lines
- Human checkpoint (Task 1) was explicitly approved before deletions ran
</verification>

<success_criteria>
- Five local files deleted from `~/.claude/roles/box-maintainer/` (idempotently — safe to re-run)
- Zero non-historical, non-phase-74 repo-side references to the deleted paths remain
- Human confirmation on file content one last time before deletion (per 74-CONTEXT.md § "What would make it wrong" §4 scope re-verification)
- No git commit produced for the local-filesystem deletions (they are not in the repo)
- If a stray in-repo reference surfaces during the sweep and gets scrubbed, a small `docs(74-04):` or `chore(74-04):` commit lands per fleet commit-hygiene rule
</success_criteria>

<output>
Create `.planning/phases/74-control-style-of-avatar-generation-through-branding-config/74-04-SUMMARY.md` when done
</output>
