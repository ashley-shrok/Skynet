services:
  skynet-dev:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: skynet-dev
    restart: unless-stopped
    ports:
      - "8081:8080"
    volumes:
      - skynet-dev-data:/app/data
    environment:
      PORT: "8080"
      NODE_ENV: development
    depends_on:
      - guacd-dev
    networks:
      - skynet-dev-net

  guacd-dev:
    image: guacamole/guacd:1.6.0
    container_name: guacd-dev
    restart: unless-stopped
    networks:
      - skynet-dev-net

volumes:
  skynet-dev-data:
    driver: local

networks:
  skynet-dev-net:
    driver: bridge
